# Cargo manifest for scenario (c) — remove-header (x-blocked).
# Per phase 25.1 SPEC §9.1 row (c) + Task 15 PLAN AMEND-A1.
# Builds to target/wasm32-wasip1/release/c_remove_header.wasm.
[package]
name = "c_remove_header"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
proxy-wasm = "=0.2.4"

[profile.release]
opt-level = "s"
lto = true
