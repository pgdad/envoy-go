# Cargo manifest for scenario (g) — property-read-method.
# Per phase 25.1 SPEC §9.1 row (g) + Task 15 PLAN AMEND-A1.
# Builds to target/wasm32-wasip1/release/g_property_method.wasm.
[package]
name = "g_property_method"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
proxy-wasm = "=0.2.4"

[profile.release]
opt-level = "s"
lto = true
