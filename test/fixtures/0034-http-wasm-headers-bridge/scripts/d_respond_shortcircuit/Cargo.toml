# Cargo manifest for scenario (d) — respond-shortcircuit (403/denied).
# Per phase 25.1 SPEC §9.1 row (d) + Task 15 PLAN AMEND-A1.
# Builds to target/wasm32-wasip1/release/d_respond_shortcircuit.wasm.
[package]
name = "d_respond_shortcircuit"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
proxy-wasm = "=0.2.4"

[profile.release]
opt-level = "s"
lto = true
