# envoy-go bootstrap for fixture 0101-stats-sink-graphite.
#
# Functionally equivalent to envoy.yaml modulo:
#   - cluster type: STATIC (envoy-go convention per ADR-0002) vs STRICT_DNS.
#   - cluster endpoint addresses: 127.0.0.1 (envoy-go runs on the host loopback)
#     vs host.docker.internal (reference Envoy runs in a Docker container).
#   - admin/listener ports independently allocated by the runner.
#   - graphite_statsd receiver address: 127.0.0.1:<subjStatsdPort> (subject
#     loopback) vs <HostGatewayIP>:<refStatsdPort> (bridge gateway for the
#     reference container).
#
# Single-listener plaintext H1 topology: one downstream HTTP/1.1 listener routes
# every request to the HTTPFixedBody backend (cluster {{.BackendName}}). A
# bootstrap-level stats_sinks[] graphite_statsd entry (GraphiteStatsdSink
# TypeURL, UDP address 127.0.0.1:<subjStatsdPort>, prefix, an EXPLICIT
# max_bytes_per_datagram cap — D-GR-CAP, honored not strict-rejected) points at
# the driver-owned in-process statsdrecv UDP receiver. A SHORT
# stats_flush_interval (500ms) drives fast deterministic convergence.
#
# See envoy.yaml's header comment for the full D-GR-CAP batching design
# rationale (the deliberately-long backendName forcing oversized cluster-tagged
# lines to always flush alone, vs the short statPrefix keeping HCM-tagged lines
# small enough to co-batch) — identical on this side.
#
# NO node field is needed — graphite_statsd carries no proxy identifier.
#
# Templating: rendered by the driver's mustRender() at SubjectConfig time.
# Substitution keys:
#   {{.AdminPort}}     — admin endpoint port (runner-allocated).
#   {{.ListenerPort}}  — l_test listener port (runner-allocated).
#   {{.BackendPort}}   — HTTPFixedBody backend port (runner-allocated, loopback).
#   {{.BackendName}}   — backend cluster name (FIXED identical both sides).
#   {{.StatPrefix}}    — HCM stat_prefix (FIXED identical both sides).
#   {{.GraphiteHost}}  — statsdrecv receiver host (127.0.0.1 for subject loopback).
#   {{.GraphitePort}}  — statsdrecv receiver port (driver-allocated).
#   {{.Prefix}}        — graphite_statsd metric prefix (baked identically on both sides).
#   {{.MaxBytesPerDatagram}} — the configured batching cap (D-GR-CAP, ADR-0275).

admin:
  address:
    socket_address: { address: 0.0.0.0, port_value: {{.AdminPort}} }

# Bootstrap-level stats sink: the graphite_statsd UDP sink, with an EXPLICIT
# max_bytes_per_datagram cap (D-GR-CAP — honored, not strict-rejected).
# address.socket_address carries 127.0.0.1:<subjStatsdPort> — the driver-owned
# in-process statsdrecv receiver on the host loopback. A SHORT
# stats_flush_interval (500ms) drives fast deterministic convergence. prefix is
# baked identically on both sides.
stats_sinks:
  - name: envoy.stat_sinks.graphite_statsd
    typed_config:
      "@type": type.googleapis.com/envoy.extensions.stat_sinks.graphite_statsd.v3.GraphiteStatsdSink
      address:
        socket_address: { protocol: UDP, address: {{.GraphiteHost}}, port_value: {{.GraphitePort}} }
      prefix: {{.Prefix}}
      max_bytes_per_datagram: {{.MaxBytesPerDatagram}}
stats_flush_interval: 0.5s

static_resources:
  listeners:
    - name: l_test
      address: { socket_address: { address: 0.0.0.0, port_value: {{.ListenerPort}} } }
      filter_chains:
        - filters:
            - name: envoy.filters.network.http_connection_manager
              typed_config:
                "@type": type.googleapis.com/envoy.extensions.filters.network.http_connection_manager.v3.HttpConnectionManager
                codec_type: HTTP1
                stat_prefix: {{.StatPrefix}}
                route_config:
                  name: rc_test
                  virtual_hosts:
                    - name: vh_test
                      domains: ["*"]
                      routes:
                        - match: { prefix: "/" }
                          route: { cluster: {{.BackendName}} }
                http_filters:
                  - name: envoy.filters.http.router
                    typed_config:
                      "@type": type.googleapis.com/envoy.extensions.filters.http.router.v3.Router

  clusters:
    # backend: HTTPFixedBody subprocess (17-byte fixed body) on host loopback.
    - name: {{.BackendName}}
      type: STATIC
      lb_policy: ROUND_ROBIN
      load_assignment:
        cluster_name: {{.BackendName}}
        endpoints:
          - lb_endpoints:
              - endpoint:
                  address:
                    socket_address: { address: 127.0.0.1, port_value: {{.BackendPort}} }
