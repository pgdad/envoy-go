# Cargo manifest for scenario f_shared_data_read_after_write — fixture 0036-http-wasm-body-and-advanced.
#
# Per phase 25.2 SPEC §8.1.1 + Task 20 PLAN + AMEND-A1:
# proxy-wasm-rust-sdk =0.2.4 (pinned exact). Builds to
# target/wasm32-wasip1/release/f_shared_data_read_after_write.wasm. The resulting blob is vendored
# at ../../bytecode/f_shared_data_read_after_write.wasm + consumed by BOTH reference Envoy v1.37.2
# (V8 runtime) AND envoy-go (wazero runtime) via DataSource.Filename →
# on-disk read. Exports proxy_abi_version_0_2_1 per AMEND-A6.
[package]
name = "f_shared_data_read_after_write"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
proxy-wasm = "=0.2.4"

[profile.release]
opt-level = "s"
lto = true
