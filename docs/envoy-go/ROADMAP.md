# envoy-go Roadmap

## Schema

Phases are tracked as rows in the tables below. Columns:

| Column | Meaning |
|---|---|
| `id` | Phase identifier (e.g. `00`, `04`, `04.1`). Sub-phases use a decimal suffix (see §6 of `BOOTSTRAP_PROMPT.md`). |
| `title` | Short human-readable title matching the phase directory slug under `docs/envoy-go/phases/`. |
| `depends-on` | Comma-separated list of phase ids that must be `done` before this phase can enter `in-progress`. Empty for the first phase. |
| `status` | One of `planned`, `in-progress`, `blocked`, `done`. |
| `sub-phases` | If the phase was split (see §6), this lists the sub-phase ids (e.g. `04.1, 04.2`). Empty otherwise. |
| `summary` | One-line summary of the phase's differential surface at completion. |

### Invariants

- **Append-only history.** Rows are never deleted. Only `status` and `sub-phases` columns are updated in place. Sub-phases get their own rows.
- **Status transitions.** `planned` → `in-progress` → (`blocked` or `done`). `blocked` is an exit state only when an upstream dependency is unavailable; the phase resumes on the same row when unblocked.
- **Sub-phase rows.** When a parent phase is split, the parent stays `in-progress` with `sub-phases` listing children; each child gets its own row whose `depends-on` references the prior child (so children execute in order unless stated otherwise).
- **Families (§9 headings) are not rows.** Each heading is expanded into per-phase rows when a phase under that family enters `in-progress`. Do not pre-populate rows for unbrainstormed phases.

---

## MVP Trunk (phases 00–08)

These phases ship in order. Each depends on the previous being `done`, because each adds a primitive the next phase relies on. Splitting (§6) is permitted within any phase.

| id | title | depends-on | status | sub-phases | summary |
|---|---|---|---|---|---|
| 00 | bootstrap | — | done |  | Bootstrap: repo layout, CI, Docker reference Envoy, differential harness skeleton, `ENVOY_TARGET.md` pin, trivial echo fixture. Harness boots; one TCP echo fixture green. |
| 01 | static-bootstrap-config | 00 | done |  | Static bootstrap config loader (node, admin, static_resources skeleton). Config parses; admin `/ready` behaves like Envoy. |
| 02 | tcp-proxy | 01 | done |  | Listener + TCP proxy filter + static cluster + round-robin LB (plaintext). TCP proxy fixture green. |
| 03 | tls | 02 | done |  | Downstream TLS termination + upstream TLS origination + SNI. TLS TCP fixture green. |
| 04 | http-1.1 | 03 | done |  | HTTP connection manager (HTTP/1.1) + route match + router filter + direct_response. HTTP/1.1 routing fixture green. |
| 05 | http-2 | 04 | done | 05.1, 05.2 | HTTP/2 downstream + upstream (low-level framer, own conn mgr). HTTP/2 fixture green; `h2spec` above threshold. Split planner-time per ADR-0045 (`BOOTSTRAP_PROMPT.md` §6.2; phase-05 SPEC §11.1 split-by-surface). |
| 05.1 | downstream-h2 | 04 | done |  | Downstream HTTP/2 termination (own framer, own ServerConn, ALPN dispatch); first non-vacuous conformance gate `h2spec` (53/53 PASS at the ADR-0051 pin); `--allow-h2c` test-only flag; `CONFORMANCE_PINS.md`; `BEHAVIOR_CONTRACT.md ## HTTP/2` scaffold. No new differential fixture (gate a vacuously green per ADR-0045; routed-to-upstream H2 differential surface deferred to 05.2 + fixture 0004). |
| 05.2 | upstream-h2 | 05.1 | done |  | Upstream HTTP/2 origination (`Cluster.DialH2`, own `ClientConn`, `routerActionH2`, cluster `HttpProtocolOptions` parsing); fixture `0004-h2-routing` (full HTTPS h2 end-to-end); closes ADR-0035 H2 leg; extends `BEHAVIOR_CONTRACT.md ## HTTP/2` with upstream + fixture-0004 rules. |
| 06 | observability-baseline | 05 | done | 06.1, 06.2 | Observability baseline: stats + Prometheus admin endpoint + access log (file sink, Envoy default format). Access log + Prometheus fixtures green. Split planner-time per ADR-0045 (`BOOTSTRAP_PROMPT.md` §6.2; phase-06 SPEC + BRAINSTORM §1 split-by-surface). |
| 06.1 | stats-prometheus | 05 | done |  | Internal `stats` package (atomic-counter Registry, lock-free hot path, no third-party Prometheus dependency); `/stats/prometheus` admin endpoint; 17 stat-emit call sites across listener / HCM / cluster (counters + gauges only — histograms deferred); fixture `0005-prometheus-stats` (per-counter delta-equality + per-gauge snapshot-equality on the 17 names against reference Envoy under a 5-request defined load); populates `BEHAVIOR_CONTRACT.md ## Stat-name mapping` (rules SN1–SN8); bundles 05.2 REVIEW M-9 (h2RouterActionAdapter log line). |
| 06.2 | access-log | 06.1 | done |  | Internal `accesslog` package (file sink, Envoy default format formatter, async writer); HCM access-log emit hooks; fixture `0006-access-log`; populates `BEHAVIOR_CONTRACT.md ## Access log field mapping`. Closes the parent row `06` at its phase-done commit (mirrors 05 / 05.1 / 05.2 closure pattern). |
| 07 | filter-chain-framework | 06 | done | 07.1, 07.2 | Filter chain framework: iteration protocol, per-route config, extension registry (HTTP side under HCM); listener-side chain-match completion (listener_filters, FilterChainMatch beyond SNI, default_filter_chain). Framework fixtures green; trivial pluggable filter covers all iteration states. Split planner-time per ADR-0045 (`BOOTSTRAP_PROMPT.md` §6.2; phase-07 BRAINSTORM §1 split-by-surface). |
| 07.1 | http-filter-framework | 06 | done |  | HTTP filter iteration protocol (Envoy-faithful with async-resume; narrow method set; status enums settled) + extension registry (`*HTTPRegistry` threaded; freeze-after-boot mirrors `*stats.Registry` LBP-1) + 3-tier `typed_per_filter_config` merge (Route > VirtualHost > RouteConfiguration; most-specific override) + trivial real filter `envoy.filters.http.cors` (differential fixture `0007a`) + test-only probe filter `envoy.filters.http.envoy_go_test` (structural fixture `0007b`); router moves from `internal/filter/hcm/actions.go` to a new `internal/filter/http/router/` terminal-filter package; populates `BEHAVIOR_CONTRACT.md ## HTTP filter chain` with the four §11 empirical-pin blocks; supersedes ADR-0040 totally; partially supersedes ADR-0042; amends ADR-0041 silent-ignore set. |
| 07.2 | listener-chain-completion | 07.1 | done |  | `listener_filters` framework (`tls_inspector` as the first concrete filter — `original_dst` deferred to a future phase per Decision F) + `FilterChainMatch` fields beyond SNI (destination_port, prefix_ranges, source_*, application_protocols/ALPN, transport_protocol) + `Listener.default_filter_chain` (promoted from phase-03 parse-error). Differential fixture `0008-listener-chain-match` (dual-listener `l_test_a` + `l_test_b` plaintext; 5-connection workload across 4 chains + 1 `default_filter_chain`; per-connection backend-port equivalence). Closes the parent row `07` at its phase-done commit (mirrors 05 / 05.1 / 05.2 + 06 / 06.1 / 06.2 closure pattern). Partially supersedes ADR-0033. Anticipated ADRs ADR-0077..ADR-0083 (per SPEC §10). |
| 08 | admin-api-and-drain | 07 | done | 08.1, 08.2 | Minimum admin API (config_dump, stats, clusters, listeners, ready, server_info) + graceful drain. Admin + drain fixtures green. Split planner-time per ADR-0045 (`BOOTSTRAP_PROMPT.md` §6.2; phase-08 BRAINSTORM §1 split-by-surface). |
| 08.1 | admin-endpoints | 07 | done |  | Four new read-only admin endpoints under the existing `internal/admin/` HTTP/1.1 mux: `GET /config_dump` (`application/json` via `protojson` over `*adminv3.ConfigDump`), `GET /clusters` (`text/plain`), `GET /listeners` (`text/plain`), `GET /server_info` (`application/json` via `protojson` over `*adminv3.ServerInfo`); constructor-widening pattern threads `*bootstrap.Bootstrap` + `*cluster.Manager` + `*listener.Manager` into `admin.New()`; new `cluster.Manager.Clusters()` snapshot accessor; `BEHAVIOR_CONTRACT.md ## Admin API — /ready` restructured into `## Admin API` umbrella with five per-endpoint subsections + four new equivalence-matrix rows. Differential fixture `0009-admin-config-dump` (per-endpoint equivalence under a 5-request defined load with per-endpoint tolerance discipline). New fuzzer `FuzzConfigDumpFormat` (10 fuzzers post-08.1). Anticipated ADRs ADR-0084..ADR-0090 (per SPEC §8). Empirical-pin block (SPEC §11) executed IN-SESSION against reference Envoy v1.37.2 per ADR-0004. |
| 08.2 | graceful-drain | 08.1 | done |  | New `internal/drain/` package (drain-state machine LIVE → DRAINING → exit) + `cmd/envoy-go/main.go` SIGTERM-handler upgrade + `internal/listener.Manager.Drain` + `internal/cluster.Manager.Drain` + `POST /drain_listeners` admin endpoint + `/ready` DRAINING-state body extension (partially supersedes ADR-0015) + `/server_info` `state`-field DRAINING transition + `BEHAVIOR_CONTRACT.md ### /drain_listeners` subsection + `## Graceful drain` umbrella section. Differential fixture `0010-graceful-drain` green. Closes the parent row `08` at its phase-done commit (mirrors 05 / 05.1 / 05.2 + 06 / 06.1 / 06.2 + 07 / 07.1 / 07.2 closure pattern). 08.2's phase-done is the BOOTSTRAP_PROMPT.md §8 MVP-trunk-close commit. |
| 09 | http-filter-fault | 08 | done |  | New `internal/filter/http/fault/` package implementing `envoy.filters.http.fault` (Envoy v1.37.2 canonical fault filter) under the 07.1 framework; first concrete phase under §9 HTTP filters family. MVP envelope per SPEC §1 (revised per §11 empirical pins): `delay.percentage` + `delay.fixed_delay` (async-resume via `time.AfterFunc` + `cb.ContinueDecoding`); `abort.percentage` + `abort.http_status` constrained `[200, 600)` per PGV pin §11.1 (terminal-replace via `cb.SendLocalReply` with body byte-exact `fault filter abort` (18 bytes, no trailing newline) + 4-header set per §11.3); `headers` field for request-header gating with StringMatcher.exact only per §11.8; `max_active_faults` concurrency cap (LBP-1 sixth application). Differential fixture `0011-http-fault` green (4 scenarios per SPEC §7.1: delay-only listener-inherited, combined delay+abort per-route, per-route wholesale-override, headers-field exact-match gate; the BRAINSTORM-anticipated 5th "header-driven abort" scenario drops per §11.5 amendment). FIRST production filter exercising async-resume on the request side (cors only short-circuited via SendLocalReply). Deferred per `BOOTSTRAP_PROMPT.md` §9 family-expansion: `response_rate_limit`, `abort.grpc_status` (gRPC family), all four runtime-key fields (Runtime + hot restart family), **`delay.header_delay` + `abort.header_abort` proto sub-messages COUPLED with the four `x-envoy-fault-{delay,abort}-request[-percentage]` request headers** (deferred together per §11.5 + ADR-0104 — the request-header path requires the proto sub-messages to activate; cannot be cleanly separated), `upstream_cluster`, `downstream_nodes`, `disable_downstream_cluster_stats`, `filter_enabled` / `filter_enabled_runtime`, HeaderMatcher non-exact variants (each gets a deferral ADR per ADR-0040 format). 5 stats per §11.6 (4 counters + 1 gauge; 17→22-name BEHAVIOR_CONTRACT extension; route A — `fault.response_rl_injected` permanently-zero counter for parity). Anticipated ADRs ADR-0100..ADR-0107 (per BRAINSTORM §9; ADR-0104 repurposed from implementation to deferral). Per ADR-0106 (BRAINSTORM Decision 12), HTTP filters family expands as flat top-level rows (no parent 09 row with sub-phases); each filter is its own coherent phase. ADR-0045 surface-split release valve stays available if SPEC/PLAN find > ~1500 LoC / > ~25 tasks. |
| 10 | http-filter-header-mutation | 09 | done |  | New `internal/filter/http/header_mutation/` package implementing `envoy.filters.http.header_mutation` (Envoy v1.37.2 canonical header-mutation filter) under the 07.1 framework. THIRD §9 family-row (after cors @ 07.1, fault @ 09). MVP envelope per SPEC §1 (revised per §11 empirical pins): `mutations.request_mutations` + `mutations.response_mutations` (both directions; AppendAction × 4 + Remove + `keep_empty_value` boundary per §11.2 confirmation; multi-valued OVERWRITE-collapse + APPEND-preserve per §11.4 confirmation); `HeaderMutationPerRoute` + `most_specific_header_mutations_wins` (both true/false; multi-tier evaluation across Route/VirtualHost/RouteConfiguration tiers; cross-tier ordering per §11.5 confirmation matches proto comment verbatim). Protected-header set per §11.1 (`{:method, :path, :authority, :scheme, :status, host}` case-insensitive on `host`) — **CONFIG-LOAD-TIME enforcement** (NOT silent runtime no-op as BRAINSTORM hypothesized; MAJOR amendment to BRAINSTORM Decision 11 per §1.1 + ADR-0111). Differential fixture `0012-http-header-mutation` (4 scenarios per SPEC §7.1: listener-only, per-route override, multi-tier flag=false least-specific-wins, multi-tier flag=true most-specific-wins; the BRAINSTORM-anticipated 5th "protected-headers" scenario drops per §11.1 amendment — migrates to unit-test territory since protection is config-load-time). NEW framework method `PerRouteConfig.ResolveAllTiers` (~60 LoC sibling to existing `Resolve` per ADR-0073; amends ADR-0073) + new symmetric `RequestRouteConfigsAllTiers` / `ResponseRouteConfigsAllTiers` callbacks (~30 LoC) + new EAGER per-route validation hook (~40 LoC framework delta per SPEC §6.7 / §12 deferred decision 3). Zero new stats per §11.3 confirmation (`## Stat-name mapping ### 22-name table` UNCHANGED in phase 10; analogous to cors no-stats per ADR-0074). Anticipated ADRs ADR-0108..ADR-0113 (6 ADRs per SPEC §8; ADR-0114 stats-absence dropped per §8.1 consolidation — folded inline into §13.1 BEHAVIOR_CONTRACT subsection). Deferred per ADR-0040 format: `mutations.query_parameter_mutations` (path-query rewriting subsystem; ADR-0112); header-value formatter substitution (`%REQ(:path)%` etc — Envoy command-string subsystem; ADR-0113). Per ADR-0106, §9 family-rows are flat top-level rows; phase 10 lands as row `10`, NOT as a sub-phase of any §9 parent. ADR-0045 surface-split release valve stays available if PLAN finds > ~1500 LoC / > ~25 tasks; SPEC's position is single-row. |
| 11 | http-filter-local-ratelimit | 10 | done |  | New `internal/filter/http/localratelimit/` package implementing `envoy.filters.http.local_ratelimit` (Envoy v1.37.2 canonical local rate-limiting filter) under the 07.1 framework. FOURTH §9 family-row (after cors @ 07.1, fault @ 09, header_mutation @ 10). MVP envelope per SPEC §1.1: 5 fields consumed (`stat_prefix`, `token_bucket{max_tokens, tokens_per_fill, fill_interval}`, `status{code}` default 429, plus the `LocalRateLimitPerRoute` per-route container); 14 fields silently ignored under inline deferral (BRAINSTORM-anticipated ADR-0120 omnibus dropped per SPEC §8.1; deferral organized by 8 family-clusters at SPEC §2.1 + §13.5: descriptor-action; Runtime + shadow-mode; xDS cluster-state; response-side header injection; per-connection lifecycle; multi-stage; X-RateLimit headers + vh policy; gRPC trailer). Token-bucket primitive Option A — lazy refill on access, single sync.Mutex per bucket, no per-bucket goroutine, monotonic time via `time.Now().UnixNano()` (Go ≥1.9 monotonic guarantee). FIRST production filter where per-route TPFC override implies independent stateful resources (per-route entries each own a `tokenBucket` per ADR-0117 = ADR-0073 amendment); cors / fault / header_mutation per-route is data-only. Differential fixture `0013-http-local-ratelimit` (4 scenarios per SPEC §7.1: basic-allow, basic-rate-limited, refill-after-fill_interval ±10ms, per-route-override). Stat surface 22→26 names (4 new counters; `enforced == rate_limited` MVP invariant per ADR-0118; filter-specific Prometheus tag-extractor `envoy_local_http_ratelimit_prefix` per SPEC §11.5 — corrects BRAINSTORM-hypothesized `envoy_http_conn_manager_prefix`). Rate-limited response status 429, body byte-exact `local_rate_limited` (18 bytes; no LF), 4-header set lowercase wire-form (`content-length`, `content-type`, `date`, `server: envoy` — `server: envoy` corrects BRAINSTORM-hypothesized `envoy-go`); SendLocalReply mechanism mirrors fault abort. Empirical pin §11.2c: `fill_interval` 50ms minimum is filter-internal Envoy check (NOT PGV); envoy-go `New` factory mirrors. Empirical pin §11.2/§11.4: `filter_enabled` + `filter_enforced` `RuntimeFractionalPercent` fields default to 0% in reference Envoy; fixture configs MUST set both to 100% explicitly on BOTH sides for differential equivalence (envoy-go silent-ignores; behavior is always-100%). NO new framework primitive required (unlike phase 10's `ResolveAllTiers`); reuses existing 3-tier `PerRouteConfig.Resolve`. 15th fuzzer `FuzzLocalRateLimitConfigParse`. Anticipated ADRs ADR-0114..ADR-0119 (6 ADRs per SPEC §8; ADR-0120 omnibus dropped per §8.1; ADR-0114 captures package-name divergence from header_mutation's underscore-preserving directory pattern — phase 11 uses `localratelimit/` no-underscore directory). Per ADR-0106 (validated at phases 09 + 10 + 11), §9 family-rows are flat top-level rows; phase 11 lands as row `11`, NOT as a sub-phase of any §9 parent. ADR-0045 surface-split release valve stays available if PLAN finds > ~1500 LoC / > ~25 tasks; SPEC's position is single-row at ~1100–1400 LoC / ~12–16 tasks. |
| 12 | http-filter-csrf | 11 | done |  | New `internal/filter/http/csrf/` package implementing `envoy.filters.http.csrf` (Envoy v1.37.2 canonical same-origin enforcement filter) under the 07.1 framework. FIFTH §9 family-row (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11). MVP envelope per SPEC §1.1 (revised per §11 empirical pins): 1 field actively consumed (`additional_origins[].StringMatcher.exact` only — non-exact variants dropped at PARSE time per ADR-0101 §3); 1 field PGV-validated-not-honored (`filter_enabled` — REQUIRED at parse-time per §11.11 amendment, MAJOR REVISION from BRAINSTORM "silent-ignore" hypothesis; runtime always-100%-active); 1 field silent-ignored entirely (`shadow_enabled` — optional at parse, always-never-shadow at runtime; couples to Runtime + hot restart family). Method gate `{POST, PUT, DELETE, PATCH}` confirmed (§11.1). Source-origin extraction trichotomy per §11.2 amendment (MAJOR REVISION from BRAINSTORM 2-case hypothesis): (i) `Origin: null` literal → empty source NO Referer fallback; (ii) `Origin:` empty/absent → fall back to Referer's hostAndPort; (iii) `Origin:` non-empty unparseable → verbatim string used as source. Comparison algorithm HOST:PORT-only equality per §11.3 + §11.7 + §11.8 amendments (MAJOR REVISION from BRAINSTORM "full-origin with normalization" hypothesis): scheme STRIPPED on both sides (X-Forwarded-Proto irrelevant); NO case folding; NO default-port stripping; trailing slash stripped via URL parser; `additional_origins[].exact` matches `host[:port]` form NOT full URL with scheme (operator footgun documented in BEHAVIOR_CONTRACT §13.1 + §13.4). Rejection wire shape confirmed: `SendLocalReply(403)` + body byte-exact `Invalid origin` (14 bytes, no LF) + 4-header set lowercase wire-form (`content-length: 14`, `content-type: text/plain`, `date: <RFC1123>`, `server: envoy`); reuses fault.abort/local_ratelimit primitive (§11.10). Per-route TPFC wholesale-override per ADR-0073 — data-only AND **stats SHARED with listener-level** (per §11.9 amendment; diverges from phase 11 local_ratelimit precedent which had independent per-route stats per ADR-0117). NO ADR-0073 amendment required. NO new framework primitive (thinnest §9 family-row to date). Stat surface 26→29 names (3 new counters per HCM stat_prefix: `request_valid`, `request_invalid`, `missing_source_origin`; `shadow_request_invalid` dropped from MVP — confirmed Envoy v1.37.2 also doesn't emit it under all-defaults config per §11.6; shadow-only mode reuses the regular 3-counter family); namespace anchors at HCM stat_prefix (reuses existing `envoy_http_conn_manager_prefix` Prometheus tag-extraction per §11.6 — NO new SN flattening rule; UNLIKE phase 11). Differential fixture `0014-http-csrf` (6 scenarios per SPEC §7.1: same-origin allowed, cross-origin rejected, additional_origins host:port match, missing source-origin rejected, Referer fallback, per-route wholesale override with shared stats; GET passthrough unit-only). Fixture configs MUST set `filter_enabled.default_value: 100/HUNDRED` explicitly on both sides per §11.11 amendment (PGV-required by Envoy at parse-time). 16th fuzzer `FuzzCsrfPolicyConfigParse`. Anticipated ADRs ADR-0120..ADR-0124 (5 ADRs per SPEC §8; no consolidation; no ADR-0073 amendment paragraph; no ADR-0061 amendment). Per ADR-0106 (validated at phases 09 + 10 + 11 + 12), §9 family-rows are flat top-level rows; phase 12 lands as row `12`, NOT as a sub-phase of any §9 parent. ADR-0045 surface-split release valve stays available if PLAN finds > ~1500 LoC / > ~25 tasks; SPEC's position is single-row at ~750-1100 LoC / ~10-14 tasks. |
| 13 | http-filter-buffer | 12 | done | 2026-05-09 | New `internal/filter/http/buffer/` package implementing `envoy.filters.http.buffer` (Envoy v1.37.2 canonical "buffer entire request body before forwarding upstream" filter) under the 07.1 framework. SIXTH §9 family-row (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11, csrf @ 12). 1 listener-level field consumed (`max_request_bytes` UInt32Value; envoy-go-own validation: non-nil + value > 0 + value ≤ 1048576 / 1 MiB per ADR-0126). FIRST §9 family-row to interact with the framework's body-buffering machinery; FIRST §9 family-row whose per-route proto introduces a top-level `disabled` boolean shortcut as a first-class shape — codifies "disabled-OR-override" as the **5th canonical per-route discipline** (ADR-0125). Body-counting algorithm — STREAMING-CAP ONLY (per ADR-0127 v2 + SPEC §11.6 empirical refutation of Content-Length fast-fail): `DecodeHeaders` returns `Continue` on bodied + non-disabled (NOT `StopIteration` — envoy-go synchronous-HCM dispatch constraint per ADR-0128); `DecodeData` returns `DataContinue` mid-stream (NOT `DataStopIterationAndBuffer`; HCM's `connection.go` bodyBuf already accumulates all bytes per ADR-0076 + ADR-0128); overflow fires `SendLocalReply(413, "Payload Too Large", connClose)` + `DataStopIterationNoBuffer`; terminal chunk invokes `maybeAddContentLength` (chunked → fixed-CL conversion observable upstream, mirrors `buffer_filter.cc:91-97`). Wire shape byte-equivalent to reference Envoy on every observable axis. Per-route stats SHARED with listener-level (mirrors phase-12 csrf ADR-0124; DIVERGES from phase-11 local_ratelimit ADR-0117). Stat surface stays at 29 names (phase 13 contributes ZERO new stat-table entries per SPEC §1.1 amendment 5 — reference Envoy emits no `envoy_http_buffer_*` counter family; overflow observable via existing `downstream_rq_4xx`). 6-scenario differential fixture `0015-http-buffer`. 17th fuzzer `FuzzBufferConfigParse`. 4 ADRs: ADR-0125 (package + 5th canonical per-route), ADR-0126 (compiledConfig + parse-time validation), ADR-0127 v2 (body-counting algorithm + maybeAddContentLength + 413 wire reuse; in-place updated at Task 12 to retract 100-Continue addendum + reflect Continue/DataContinue landed algorithm), ADR-0128 (NEW: framework primitives at `internal/filter/hcm/connection.go` — synthetic empty-terminal RunDecodeData + post-body CL reconciliation; FIRST framework deltas since phase 07.1). Phase 13 retires the SPEC §4 "no framework deltas" invariant. Per ADR-0106, §9 family-rows are flat top-level rows; phase 13 lands as row `13`. |
| 14 | http-filter-compressor | 13 | done | 2026-05-10 | New `internal/filter/http/compressor/` package implementing `envoy.filters.http.compressor` (Envoy v1.37.2 canonical response-side compression filter, gzip-only MVP) under the 07.1 framework. SEVENTH §9 family-row (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11, csrf @ 12, buffer @ 13). MVP envelope shipped per phase 14 SPEC §1 + §1.1 amendments: 6 listener consumed + 9 listener silent-ignored + 2 codec (Gzip) consumed + 3 codec silent-ignored = 8 grand-total consumed + 12 silent-ignored + 1 parse-rejected (non-Gzip `compressor_library` TypeURLs per ADR-0130). Body algorithm Path B (buffer-then-compress; framework as-is) per ADR-0131 — operates on `resp.Body` materialized by HCM's encode-chain at `connection.go:467-475` (one-shot full-body `RunEncodeData(ctx, resp.Body, true)`); writes back via new framework primitive `EncoderFilterCallbacks.OverwriteBody(b []byte)` per ADR-0131 §Decision (vi) (~20-25 LoC framework delta at `internal/filter/http/{callbacks.go, chain.go}` + `internal/filter/hcm/{connection.go, h2dispatch.go}`; symmetric to phase-13 ADR-0128 decode-side primitives); wire-shape divergence (envoy-go: `Content-Encoding: gzip` + fixed `Content-Length: <gzipped>` + identity transfer; Envoy: streaming `Content-Encoding: gzip` + chunked transfer + no CL) accepted with explicit forward-pointer to future encode-side streaming framework phase. Per-route TPFC `disabled`-OR-`overrides.response_direction_config` shape — SECOND row using ADR-0125 5th canonical disabled-OR-override (after phase-13 buffer); per-route override surface is FILTER-SPECIFIC and NARROWER than the listener-level config (only `remove_accept_encoding_header` per `ResponseDirectionOverrides` proto + per-route library swap silent-ignored); ADR-0125 gained amendment §(viii)-(x) at SPEC commit; no new per-route ADR. Stat surface 29→46 names (17 new counters per HCM stat_prefix: `header_compressor_overshadowed`, `header_compressor_used`, `header_identity`, `header_not_valid`, `header_wildcard`, `no_accept_header`, `not_compressed_etag`, `response.{compressed, content_length_too_small, not_compressed, total_compressed_bytes, total_uncompressed_bytes}`, `request.{compressed, content_length_too_small, not_compressed, total_compressed_bytes, total_uncompressed_bytes}`); NO new SN flattening rule (uses existing SN2 per phase 14 SPEC §1.1 amendment 3; the BRAINSTORM-hypothesized Rule SN10 was refuted at SPEC time when empirical scrape revealed the existing HCM-anchored SN2 sufficient). Per-route stats SHARED with listener-level (mirrors phase-12 csrf ADR-0124 + phase-13 buffer ADR-0125; DIVERGES from phase-11 local_ratelimit ADR-0117 INDEPENDENT). Differential fixture `0016-http-compressor` (6 scenarios: compress-text-default, skip-content-type, skip-min-content-length, skip-on-etag, per-route-disabled, per-route-rmAE-override). FIRST §9 row using **decompress-and-compare body-assertion discipline** per ADR-0133 — compressed bytes are STRUCTURALLY non-byte-exact between Go `compress/gzip` and Envoy libz (different block-boundary + Huffman-tree choices; multi-encoding gzip-format spec admits both as valid); decompressed bytes ARE byte-equivalent. 17/17 differential fixtures green at phase-done. 18th fuzzer `FuzzCompressorConfigParse`. **6 ADRs landed** (PLAN-time table called for 5; ADR-0044 ADR-on-impl escape-valve added a 6th at Task-14 follow-up): ADR-0129 (Task 2 — package + ENCODER+DECODER HTTPFilter + 17-counter filterStats), ADR-0130 (Task 2 — compiledConfig + codec-library Any-dispatch + Gzip level mapping), ADR-0131 (Task 4 — Path B + wire-shape divergence + EncoderFilterCallbacks.OverwriteBody framework primitive), ADR-0132 (Task 8 — 17-counter stat surface + SN2 reuse + per-route SHARED stats), ADR-0133 (Task 11 — differential-fixture decompress-and-compare body-assertion discipline), **ADR-0134 (Task 14 follow-up — unanticipated HCM `directResponseAction.response_headers_to_add` plumbing + `OVERWRITE_IF_EXISTS_OR_ADD`-only AppendAction parse-gate; surfaced at integration time when fixture-0016 scenario 2 image/png content-type-skip required route-level override on direct_response body)**. Plus ADR-0125 amendment §(viii)-(x) (already landed at SPEC commit). 4 per-side empirical-divergence counters pinned at Task 14 (`header_compressor_used`, `header_not_valid`, `response_not_compressed`, `request_not_compressed`) — reference Envoy v1.37.2 + envoy-go MVP diverge by design choice on each; both sides locked via `counterModePerSideExact`. Phase 14 does NOT promote ADR-0076's `per_connection_buffer_limit_bytes` / `per_request_buffer_limit_bytes` silent-ignore (phase 14 MVP is response-only; cap-promotion forward-pointer stays open for a future row). Per ADR-0106, §9 family-rows are flat top-level rows; phase 14 lands as row `14`. |
| 15 | http-filter-bandwidth-limit | 14 | done | 2026-05-11 | New `internal/filter/http/bandwidthlimit/` package implementing `envoy.filters.http.bandwidth_limit` (Envoy v1.37.2 canonical symmetric request+response throttle filter; **limit in KiB/s — kibibytes-per-second per proto comment line 95 + SPEC §1.1 amendment 6; NOT kilobits-per-second as BRAINSTORM hypothesized**) under the 07.1 framework. EIGHTH §9 family-row (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11, csrf @ 12, buffer @ 13, compressor @ 14). MVP envelope per SPEC §1 + §1.1 (**10 amendments authored at SPEC time** per §11 empirical-pin resolution): 4 fields consumed (`stat_prefix` PGV REQUIRED min_len=1, `enable_mode` all 4 values DISABLED/REQUEST/RESPONSE/REQUEST_AND_RESPONSE, `limit_kbps` UInt64Value PGV gte=1 + CODE-LEVEL REQUIRED at per-route per filter source `"limit must be set for per route filter config"`, `fill_interval` Duration default 50ms PGV [20ms, 1s]); 3 fields silent-ignored (`runtime_enabled` always-100%; `enable_response_trailers` always-no-trailers per future trailer-emission framework phase; `response_trailer_prefix` couples). Body algorithm Path B-async (buffer-then-delayed-emit via `time.AfterFunc` + `cb.ContinueDecoding/Encoding`) per ADR-0137 with **kbps-per-tick throttle math** per §1.1 amendment 6 + §11.P15: `chunk_size = limit_kbps × 1024 × fill_interval_seconds`; `throttle = ceil(body/chunk_size) × fill_interval`. Non-endStream `DecodeData`/`EncodeData` returns `DataContinue` (envoy-go HCM synchronous-dispatch deadlock avoidance per Task-14 finding (d); mirrors phase-13 buffer precedent; only endStream invocation returns `DataStopIterationAndBuffer` + arms timer; wire outcomes byte-equivalent). Wire-shape divergence (envoy-go Path B-async silent-then-blast vs Envoy Path A rate-paced chunks at exact `fill_interval` cadence; **per-side wall-clock equivalent within ±70ms tolerance** per §11.P9 + Task-14 adopted per-side discipline — SPEC §11.P9(c) cross-side claim REFUTED at Task 14 for bodies within initial-burst capacity, Envoy's initial-burst-discount diverges from envoy-go's deterministic ceil-formula) documented with forward-pointer to future encode-side streaming framework phase. **ZERO framework deltas** — FIRST §9 row since phase-12 csrf to introduce no new primitives; composes against phase-09 fault async-resume + phase-13 ADR-0128 decode-side body-buffering + phase-14 ADR-0131 encode-side `OverwriteBody` (anticipated: NOT invoked; framework's buffered-return path returns bytes unchanged) — load-bearing demonstration of phase-13/14 framework primitives' reusability. Per-route TPFC uses the same bare `BandwidthLimit` proto via TPFC (NO `BandwidthLimitPerRoute` wrapper per §11.P1 + §1.1 amendments 1 + 2; **phase-15 introduces a NEW canonical per-route pattern — bare-message-via-TPFC + code-level-required-`limit_kbps`-at-per-route — adjacent to ADR-0117's 4th canonical; documented at ADR-0125 §(xi) in-place amendment as the 6th canonical entry**; BRAINSTORM-hypothesized 5th canonical disabled-OR-override REFUTED). Stat surface 46→**60 names** (14 new active: 8 counters + 6 gauges; namespace `<stat_prefix>.http_bandwidth_limit.<counter>` underscore-infix NOT HCM-rooted per §11.P11; Prometheus inlines stat_prefix into base name `envoy_<stat_prefix>_http_bandwidth_limit_<counter>{}` per §11.P10 — NO tag-extractor + NO new SN10 rule); plus 2 unconditional Envoy histograms (`request_transfer_duration`, `response_transfer_duration`) allow-listed via twin-series-filter divergence-window per §1.1 amendment 9. **`*_enabled` + `*_enforced` counters use `counterModePerSideExact`** (Task-14 finding (b): Envoy's initial-burst-discount + per-request bump-on-active-side diverges from envoy-go's deterministic DecodeData-driven bump + per-tick-ticks-at-completion semantic); 6 gauges NOT asserted per Task-14 finding (c) (transient/noisy). Per-route stats INDEPENDENT per ADR-0139 + §11.P4 + §11.P14 (SECOND row using stateful-override-with-INDEPENDENT-stats per ADR-0117 precedent; phase-11 local_ratelimit FIRST). Differential fixture `0017-http-bandwidth-limit` (6 scenarios: response-only throttle, request-only throttle, REQUEST_AND_RESPONSE symmetric, tiny-body within-burst, per-route DISABLED-via-`enable_mode`, per-route override with own stat_prefix; per-side wall-clock tolerance ±70ms; byte-exact body equivalence — bandwidth_limit does NOT transform bytes); 18/18 differential fixtures green at phase-done. 19th fuzzer `FuzzBandwidthLimitConfigParse`. **5 ADRs landed** (ADR-0135..ADR-0139 anchored at impl-time per ADR-0044): ADR-0135 (Task 2 — package + types + factory + 14-stat `filterStats`), ADR-0136 (Task 2 — compiledConfig + parse-time validation + 3-silent-ignored discipline), ADR-0137 (Task 3 — kbps-per-tick `throttleDuration` helper + Path B-async + wire-shape divergence + non-endStream DataContinue HCM-deadlock-avoidance fix), ADR-0138 (Task 8 — 14-stat surface + namespace + Prometheus inline-prefix + histogram twin-series allow-list), ADR-0139 (Task 7 — per-route INDEPENDENT-stats wiring + `newFilterStatsIfAbsent` post-Freeze idempotent registration + `sync.Map` lazy-cache). Plus ADR-0125 §(xi) amendment paragraph (landed at SPEC commit). **Operational foot-gun** discovered at §11 probeJ: listener-level missing `limit_kbps` + active `enable_mode` causes runtime hang on both Envoy + envoy-go (byte-equivalent foot-gun) — documented at §1.1 amendment 10 + §13.4 forward-pointer notes. Per ADR-0106, §9 family-rows are flat top-level rows; phase 15 lands as row `15`. |
| 16 | http-filter-rbac | 15 | done | 2026-05-12 | New `internal/filter/http/rbac/` package (`rbac.go` 1191 LoC + `evaluator.go` 854 LoC + `doc.go` 148 LoC = **2193 LoC production**) + new top-level `internal/matcher/` package (`matcher.go` 510 + `doc.go` 75 = **585 LoC production**) implementing `envoy.filters.http.rbac` (Envoy v1.37.2 canonical role-based-access-control filter; decode-side dual-engine policy gate) under the 07.1 framework. NINTH §9 family-row (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11, csrf @ 12, buffer @ 13, compressor @ 14, bandwidth_limit @ 15). MVP envelope per SPEC §1 + §1.1 (12 amendments authored at SPEC time; all 18 §11 empirical pins resolved IN-SESSION against Envoy v1.37.2): 7 top-level fields consumed proto-faithful — `rules` + `matcher` (rules_specifier UDPA-field-alias; rules wins when both set per `rbac.pb.go:38`), `shadow_rules` + `shadow_matcher` (shadow_rules_specifier UDPA-field-alias; shadow_rules wins per `rbac.pb.go:53`), `rules_stat_prefix`, `shadow_rules_stat_prefix`, `track_per_rule_stats`. Inside `config.rbac.v3.RBAC`: `action` enum ALLOW + DENY + LOG-partial (LOG = always-allow + match-runs + `access_log_hint` metadata silent divergence-window per SPEC §1.1 amendment 5; folds into `allowed` counter per amendment 8 — NO separate `logged` counter — REFUTES BRAINSTORM 5-counter hypothesis); `policies` map<name, Policy> lexicographic-name walk; `audit_logging_options` silent-ignored (`[#not-implemented-hide:]` upstream); CEL three fields silent-ignored per amendment 6. Permission MVP **Large 11** (11 of 14): any, header, url_path, destination_ip, destination_port, destination_port_range, requested_server_name, and_rules, or_rules, not_rule, sourced_metadata (always-no-match runtime). Permission DEFERRED (3): metadata + matcher + uri_template (PARSE-REJECT). Principal MVP **Large 11** (11 of 14 per amendment 7 — Principal has 14 variants not 13): any, authenticated (three-case algorithm per amendment 12; nil-principal_name → `len(DownstreamPrincipal()) > 0`; non-nil → StringMatcher iteration over URI SAN/DNS SAN/Subject DN candidates; plaintext → FALSE), direct_remote_ip, remote_ip, header, url_path, and_ids, or_ids, not_id, sourced_metadata, filter_state (last two always-no-match). Principal DEFERRED (3): source_ip + metadata + `custom` (NEW 14th variant per amendment 7 — PARSE-REJECT). Per-route TPFC `RBACPerRoute{rbac: <RBAC>|nil}` — NEW **7th canonical** per-route pattern (absent-implies-disabled-OR-wholesale-override; structurally DISTINCT from 5th canonical's explicit-disabled-bool-in-oneof + 6th canonical's bare-message-via-TPFC); documented at ADR-0125 §(xii) amendment paragraph (authored at Task 10 per planner-time decision 14). Stat surface 60→**64 base names** (4 active counters: `allowed`, `denied`, `shadow_allowed`, `shadow_denied`); per-policy counter family lazy-allocated when `track_per_rule_stats: true` (template-form `<base>.policy.<policy_name>.<suffix>` — the `.policy.` infix segment was empirically refined at Task 8 reference-Envoy v1.37.2 scrape per ADR-0145, REFINES SPEC §13.2 stub which omitted the segment). Per-route stats **INDEPENDENT** per ADR-0145 + §5 (third row using stateful-override-with-INDEPENDENT-stats discipline after phase-11 ADR-0117 + phase-15 ADR-0139). HCM-rooted namespace `http.<HCM_stat_prefix>.rbac.<rules_stat_prefix>.<counter>` SN2-reuse RATIFIED at Task 8 empirical scrape — NO new SN10 rule. **TWO new framework primitives** — FIRST §9 row since phase 14 to introduce non-zero framework deltas + FIRST single phase to introduce TWO: (i) ADR-0144 — `DecoderFilterCallbacks.DownstreamPrincipal() []string` TLS-principal accessor (URI SAN + DNS SAN + Subject DN CN priority-ordered; cross-phase-reusable by future filters jwt_authn/ext_authz/oauth2/ext_proc); (ii) ADR-0142 — `internal/matcher/` NEW top-level package + `Matcher.New(tree, supportedActionTypes)` + `Matcher.Evaluate(MatchContext)` + `MatchContext` interface + PARSE-REJECT-for-unknown-TypeURL discipline (cross-phase-reusable). Deny-path wire shape `SendLocalReply(403, "RBAC: access denied" 19-byte ASCII, {content-length: 19, content-type: text/plain, date, server: envoy} lowercase wire-form, keep-alive)` per amendment 10 + §11.P5. Differential fixture `0018-http-rbac` (**8 scenarios**: allow-by-header-match; deny-no-match; allow-by-url-path; allow-by-destination-port; allow-by-direct-remote-ip; allow-by-tls-principal-via-mTLS-listener; per-route 7th-canonical disabled-via-absent; per-route wholesale-override with INDEPENDENT stat namespace + shadow). **19/19 differential fixtures green** at phase-done. **20th fuzzer** `FuzzRBACConfigParse` lands at `internal/filter/http/rbac/fuzz_test.go`. **7 anticipated ADRs landed** (ADR-0140..ADR-0146 anchored at impl-time per ADR-0044 + ADR-0125 §(xii) amendment paragraph at Task 10): ADR-0140 (Task 2 — package shape + DECODER-only HTTPFilter + 4-base-counter filterStats + lazy-per-policy via `NewCounterIfAbsent` + deny-path wire shape + boot-registration ordering), ADR-0141 (Task 2 — compiledConfig 7-field decomposition + dual-engine dispatch + UDPA-field-alias precedence + envoy-go defensive PGV-mirror + LOG-partial folds into `allowed` + CEL three-field silent-ignore + audit_logging_options silent-ignore), ADR-0142 (Task 3 — matcher-engine evaluator framework primitive at NEW `internal/matcher/` package), ADR-0143 (Tasks 4+5 — Permission + Principal Large 11+11 + AND/OR/NOT recursive combinators + 6 PARSE-REJECT + Principal_Authenticated three-case + SourcedMetadata/FilterState always-no-match), ADR-0144 (Task 6 — TLS-principal accessor framework primitive + HCM dispatch plumbing + canonical 3-cert-field scope per D11), ADR-0145 (Task 8 — 4-base-counter HCM-rooted SN2-reuse RATIFIED + lazy per-policy `.policy.` infix REFINED + per-route INDEPENDENT-stats + `NewCounterIfAbsent` flip), ADR-0146 (Task 9 — shadow-evaluation discipline + LOG-partial divergence-window + per-policy emission lazy + `response_code_details` divergence-window + shadow access-log deferred + BEHAVIOR_CONTRACT §13.4 anchoring). Plus **ADR-0147 unanticipated** (Task 13 follow-up per ADR-0044 ADR-on-impl escape-valve) — phase-03 TLS-layer `require_client_certificate=true` blanket-rejection LIFTED scoped to well-formed mTLS configs (validation_context.trusted_ca PEM provided) to enable fixture 0018 scenario 6 mTLS-principal exercise; maps onto stdlib `crypto/tls.RequireAndVerifyClientCert` + `ClientCAs` pool. Plus ADR-0125 §(xii) amendment paragraph (authored at Task 10 — NEW 7th canonical entry — ADR-0125's roster grows from 6 to 7). 6 phase-done gates verified at Task 15: Gate A (build+vet+lint clean exit 0); Gate B (race tests clean across all packages); Gate C (h2spec 53/53 PASS at ADR-0051 pin); Gate D (20 fuzzers green at 30s each); Gate E (19 differential fixtures 0000-0018 PASS); Gate F (BEHAVIOR_CONTRACT §13.1-§13.6 populated per 6-edit bundle). 16-task PLAN executed single-row per phase-13/14/15 LoC-borderline precedent; ADR-0045 surface-split release valve NOT invoked despite SPEC author's brainstorm-time hypothesis (Task count 16 well-under 25; production LoC ~2778 above SPEC-time ~1500 estimate but stayed coherent single-row). Per ADR-0106, §9 family-rows are flat top-level rows; phase 16 lands as row `16`. |
| 17 | http-filter-jwt-authn | 16 | done | 2026-05-14 | New `internal/filter/http/jwtauthn/` package (`jwtauthn.go` 845 LoC + `evaluator.go` 662 LoC + `provider.go` 707 LoC + `doc.go` 185 LoC = **2399 LoC production**) + new top-level `internal/jwks/` package (`jwks.go` 626 LoC + `doc.go` 77 LoC = **703 LoC production**) + new top-level `internal/jwt/` package (`jwt.go` 555 LoC + `doc.go` 98 LoC = **653 LoC production**) + new `test/helpers/jwksbackend/` test-helper (`jwksbackend.go` 104 LoC + `doc.go` 23 LoC = **127 LoC**) implementing `envoy.filters.http.jwt_authn` (Envoy v1.37.2 canonical JWT bearer-token validation filter, RS+ES algorithm family, Both-JWKS-source proto-faithful, Full-6 JwtRequirement variants, All-4 token extraction sources, Full-header-side post-validation side-effects, 8th-canonical per-route delegation via requirement_map) under the 07.1 framework. Shipped via the 15-task phase-17 PLAN; 8 ADRs anchored at impl-time (ADR-0148..ADR-0155 §Decision + §Consequences bodies) + ADR-0125 §(xiii) amendment landed at SPEC commit; **ADR-0044 escape-valve NOT triggered** (0 impl-time-unanticipated ADRs — the anticipated TLS-trust-store / clear_route_cache / sync-primitive surfaces did not materialize). Differential fixture `0019-http-jwt-authn` (8 scenarios) + 21st fuzzer `FuzzJwtAuthnConfigParse` green; §11.P7 SN2-reuse RATIFIED at the Task-13 fixture-0019 empirical scrape; two Task-13-discovered counter divergence-windows documented (`allowed` on bypass paths; `jwt_cache_*` structurally unreachable under MVP). TENTH §9 family-row (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11, csrf @ 12, buffer @ 13, compressor @ 14, bandwidth_limit @ 15, rbac @ 16). MVP envelope per SPEC §1 + §1.1 (12 amendments authored at SPEC time; all 16 §11 empirical pins resolved IN-SESSION against Envoy v1.37.2): **6 listener fields total per `[#next-free-field: 7]`** (REFINES BRAINSTORM 5-field framing per §1.1 amendment 1) — 5 consumed (`providers`, `rules`, `bypass_cors_preflight`, `requirement_map`, `strip_failure_response`) + 1 silent-ignored (`filter_state_rules` per §8 deferral 12 NEW); **13-of-21 JwtProvider fields consumed** (REFINES BRAINSTORM 13-of-17 framing per §1.1 amendment 2 — proto has 21 fields; 8 silent-ignored: 4 dynamic-metadata-family + jwt_cache_config + 3 v1.37.x claim-coverage `subjects`/`require_expiration`/`max_lifetime` per §1.1 amendment 3); `clock_skew_seconds` 60s default HONORED in MVP per §1.1 amendment 7 (BRAINSTORM-missed); 6 JwtRequirement oneof variants all consumed; 6-algorithm allow-list (RS256/384/512 + ES256/384/512; HS family + EdDSA + `none` + PS family deferred); All 4 extraction sources (default Authorization Bearer + access_token query param + configured from_headers + from_params + from_cookies); case-sensitive + URL-decoded for params with first-value-only multi-value handling per §11.P14 + case-sensitive + verbatim for cookies per §11.P15. Per-route TPFC: NEW **8th canonical pattern** (`oneof{disabled(bool) | requirement_name(string)}` — REFUTES BRAINSTORM `Empty`-form hypothesis per §1.1 amendment 5 + §11.P9; string-reference-delegation into listener-level requirement_map; dangling-reference RUNTIME-RESOLVED per §1.1 amendment 6 + §11.P12 — REFUTES BRAINSTORM PARSE-REJECT hypothesis; mirrors Envoy filter_config.cc `findPerRouteVerifier`; on miss emits 403 + "Failed JWT authentication: Wrong requirement_name: <name>"); ADR-0125 §(xiii) in-place amendment paragraph landed at SPEC commit (NEW 8th canonical; roster grows from 7 to 8). Deny-path wire shape per §1.1 amendments 8 + 11 + 12: **401 default + 403 specifically for `JwtAudienceNotAllowed`** per §11.P1 (REFINES BRAINSTORM "always 401" hypothesis); body = canonical jwt_verify_lib `getStatusString(status)` mapping (~70 strings; ~10 commonly-hit; e.g., `"Jwt is missing"` 14B, `"Jwt verification fails"` 22B, `"Audiences in Jwt are not allowed"` 32B); WWW-Authenticate = `Bearer realm="<original_uri>"` + conditional `, error="invalid_token"` for non-`JwtMissed` per §1.1 amendment 12 + §11.P2 (REFINES BRAINSTORM realm=issuer hypothesis — realm uses request URI); `strip_failure_response=true` strips both body AND WWW-Authenticate per §11.P3; **`response_code_details = "jwt_authn_access_denied{<reason>}"`** divergence-window NEW finding per §1.1 amendment 11 (envoy-go MVP DEFERS field emission per phase-04 HCM scope; mirrors phase-16 ADR-0146 + §8.12 + joint closure with phase-16 forward-pointer item 8); RFC 6750-conformant Bearer challenge (FIRST §9 row). **TWO new framework primitives** (SECOND consecutive two-primitive phase after phase 16): (i) HTTP-outbound JWKS fetcher at NEW `internal/jwks/` package (location LOCKED at SPEC time per §3.1 — JWKS-specific NOT generic httpclient; async fetch + per-thread cache + scheduled refresh **5s before TTL** per §11.P5 (`RefetchBeforeExpiredSec=5s`; default 10-min cache per `DefaultCacheExpirationSec=600s`) + **FIXED-INTERVAL** failed-refetch retry per §11.P4 (`DefaultRefetchAfterFailedSec=1s`; configurable via `JwksAsyncFetch.failed_refetch_duration`) — REFUTES BRAINSTORM exponential-backoff hypothesis; fast_listener mode controls blocking-vs-non-blocking initial fetch; cross-phase-reusable for future ext_authz HTTP-mode + oauth2 token-endpoint) anchored at ADR-0150; (ii) JWS/JWT verifier at NEW `internal/jwt/` package (`Parse` + `Token.VerifySignature` with RS+ES allow-list + `Token.ValidateClaims` with clock-skew tolerance + `Token.PayloadClaim` dot-notation extractor; ~20 canonical error sentinels mirroring jwt_verify_lib; pure-Go stdlib `crypto/rsa`+`crypto/ecdsa`+`encoding/base64.RawURLEncoding`; cross-phase-reusable) anchored at ADR-0151. **Stat surface 64 → 71 names** (REFINES BRAINSTORM "~72 names" + REFUTES per-provider scaling hypothesis per §1.1 amendment 9 + §11.P6): 7 base counters per HCM stat_prefix (`allowed` + `cors_preflight_bypassed` + `denied` + `jwks_fetch_success` + `jwks_fetch_failed` + `jwt_cache_hit` + `jwt_cache_miss`); **NO per-provider scaling** (REFUTES BRAINSTORM 8-per-provider hypothesis — Envoy emits filter-wide counters at HCM stat_prefix scope; multiple providers contribute to same `allowed`/`denied`); **NO gauges, NO histograms** (REFUTES BRAINSTORM `jwks_fetch_in_progress` gauge hypothesis); 5 of 7 actively emitted under MVP (`jwt_cache_hit`/`jwt_cache_miss` structurally unreachable per §8 deferral 8 — gated on `jwt_cache_config` deferral); canonical naming `cors_preflight_bypassed` NOT `bypassed_cors_preflight` per §1.1 amendment 10; HCM-rooted SN2-reuse namespace `http.<HCM_stat_prefix>.jwt_authn.<counter>` (RATIFIED-PENDING-IMPL-TIME-EMPIRICAL-SCRAPE at Task 8 per phase-16 §10 lesson (c); SOLE Task-8 closure pin in phase-17). Per-route stats **SHARED** with listener-level (mirrors phase-12 csrf + phase-13 buffer + phase-14 compressor SHARED; DIVERGES from phase-11/15/16 INDEPENDENT; pure-delegation per-route spawns no new policy-evaluation state). Differential fixture `0019-http-jwt-authn` (8 scenarios: valid-RS256-RemoteJwks, valid-ES256-LocalJwks, missing-token-deny, expired-token-deny, bad-signature-deny, bypass-cors-preflight, per-route 8th-canonical `requirement_name` delegation, per-route 8th-canonical `disabled: true`; optional 9th/10th scenarios deferred to impl-time: forward_payload_header + claim_to_headers backend-arrival + requires_any multi-provider OR). NEW test-helper `test/helpers/jwksbackend/` (in-process HTTP JWKS server; spawn-per-fixture lifecycle). 21st fuzzer `FuzzJwtAuthnConfigParse`. **8 anticipated ADRs** (LARGEST §9-row roster to date alongside phase-16's 7+1=8): ADR-0148 (package shape + DECODER-only + 7-counter filterStats + boot-registration alphabetical-after-header_mutation + deny-path wire shape) + ADR-0149 (compiledConfig 5-of-6 outer + 13-of-21 JwtProvider + 6-variant JwtRequirement evaluator + algorithm allow-list + side-effect emit-order + listener-level rules dispatch + `requires` honored proto-faithful per §1.1 amendment 4 — REFUTES BRAINSTORM PARSE-REJECT-deprecated hypothesis + clock_skew_seconds HONORED per §1.1 amendment 7) + ADR-0150 (HTTP-outbound JWKS fetcher framework primitive at `internal/jwks/` with fixed-interval failed-refetch + 5s-before-TTL refresh) + ADR-0151 (JWT verifier framework primitive at `internal/jwt/` with RS+ES allow-list + dot-notation claim extractor + array-claim rejection per §11.P10) + ADR-0152 (token extraction all 4 sources + iteration order + first-match-wins + case-sensitivity + URL-decode + first-value-only multi-value) + ADR-0153 (per-route 8th canonical `oneof{disabled(bool) | requirement_name(string)}` + runtime-resolve + SHARED-stats + ADR-0125 §(xiii) amendment paragraph) + ADR-0154 (stat surface 7-counter filter-wide + SN2-reuse + SHARED per-route + `cors_preflight_bypassed` canonical naming) + ADR-0155 (deny-path 401-or-403 dispatch + canonical jwt_verify_lib body + WWW-Authenticate Bearer challenge with conditional error=invalid_token + strip_failure_response strips both + response_code_details divergence-window). Plus ADR-0125 §(xiii) amendment paragraph (landed at SPEC commit). ADR-0044 escape-valve held in reserve for ~1 impl-time-unanticipated ADR (phase-13 ADR-0127-v2 + phase-14 ADR-0134 + phase-16 ADR-0147 precedent; likely surfaces: TLS-trust-store coupling for mTLS-to-JWKS endpoint OR HCM framework gap for clear_route_cache interaction OR sync-primitive lift for RemoteJwks-fetch-during-request callback path). **ADR-0045 split decision: SINGLE-ROW, NOT INVOKED** (LoC estimate refined post-§11 to ~1800-2200 LoC production; framework primitives scope narrowed by §11-pin resolutions; mirrors phase-13/14/15/16 single-row-borderline-LoC precedent). Per ADR-0106, §9 family-rows are flat top-level rows; phase 17 lands as row `17`, NOT as a sub-phase of any §9 parent. |
| 18 | http-filter-ext-authz | 17 | in-progress | 18.1, 18.2 | New `internal/filter/http/extauthz/` package implementing `envoy.filters.http.ext_authz` (Envoy v1.37.2 canonical external-authorization filter; decode-side gate delegating the allow/deny decision to an external service) under the 07.1 framework. **SPLIT into 18.1 + 18.2 at the phase-18 SPEC commit per ADR-0045 / ADR-0164** — the §10 empirical-pin resolution (28-field `ExtAuthz` proto surface; two structurally distinct transports; brand-new `internal/grpcclient/` gRPC infrastructure; ~2400–3600 LoC) CONFIRMED the BRAINSTORM's HIGH split-readiness leaning; the structurally clean cut is by service-mode. ELEVENTH §9 family-row (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11, csrf @ 12, buffer @ 13, compressor @ 14, bandwidth_limit @ 15, rbac @ 16, jwt_authn @ 17). MVP envelope per BRAINSTORM §1.1 (6-question dialogue): **Both service modes proto-faithful** (`grpc_service` + `http_service` — the dominant ADR-0045 split-readiness driver); **request body included** proto-faithful (`with_request_body{max_request_bytes, allow_partial_message, pack_as_bytes}` reusing the phase-13 ADR-0128 decode-side body-buffering primitive); **Full gRPC AttributeContext population** (source/destination Peers incl. `principal` via the phase-16 ADR-0144 `DownstreamPrincipal()` reuse + `certificate`; full `request.http`; `tls_session.sni`; `context_extensions`); **Full header-side mutation** (request-side `allowed_headers`/`headers_to_add`; allow-path upstream injection via `OkHttpResponse`/`allowed_upstream_headers`; deny-path downstream copying via `DeniedHttpResponse`/`allowed_client_headers`); error-posture fields (`failure_mode_allow`, `failure_mode_allow_header_add`, `status_on_error`, `validate_mutations`) consumed proto-faithful. Per-route TPFC: **5th canonical REUSE** (`ExtAuthzPerRoute{oneof{disabled(bool) | check_settings(CheckSettings)}}` maps onto the existing buffer/compressor disabled-OR-narrower-override sum-type; SHARED stats) — **FIRST §9 row to REUSE an ADR-0125 canonical rather than add one; NO ADR-0125 amendment paragraph** (breaks the phase-13..17 per-phase-roster-growth streak). **TWO new framework primitives** (THIRD consecutive two-primitive §9 row after rbac + jwt_authn): (i) gRPC-client outbound primitive at NEW `internal/grpcclient/` (envoy-go's FIRST gRPC infrastructure of any kind — dial + connection-management + the `envoy.service.auth.v3.Authorization/Check` stub + error-classification; cross-phase-reusable for ext_proc + global_ratelimit) anchored at ADR-0158; (ii) HTTP-outbound auth-check composing against the phase-17 `internal/jwks/Fetcher` outbound-HTTP structure (SPEC author decides generalize-to-`internal/httpclient/` vs ext_authz-local-client) anchored at ADR-0159. Plus TWO framework REUSES — phase-13 ADR-0128 decode-side body-buffering + phase-16 ADR-0144 `DownstreamPrincipal()`. Deny-path wire shape: `SendLocalReply` with status/body/headers from the auth response (gRPC `DeniedHttpResponse` / HTTP-mode response) or `status_on_error` — FIRST §9 row whose deny-path status code is NOT fixed by the filter. Stat surface resolved at the SPEC §5.P6 empirical scrape to **71 → 77 names** (6 filter counters `ok`/`denied`/`error`/`disabled`/`failure_mode_allowed`/`invalid` under `http.<HCM_stat_prefix>.ext_authz.*`; HCM-rooted SN2-reuse; `disabled` STRUCTURALLY UNREACHABLE under MVP — increments only via the deferred runtime `filter_enabled` gate; the brainstorm `cx_http_*` + `disabled=on-per-route` hypotheses REFUTED). Differential surface SPLIT with the phase: fixture `0020-http-ext-authz-http` (18.1, ~7 HTTP-mode scenarios, 1 NEW test-helper — in-process HTTP auth server, 22nd fuzzer `FuzzExtAuthzConfigParse`) + fixture `0021-http-ext-authz-grpc` (18.2, ~6–8 gRPC-mode scenarios, 1 NEW test-helper — in-process gRPC `Authorization/Check` auth server, 23rd fuzzer). **9 ADRs** anchored (ADR-0156..ADR-0164; ADR-0156..ADR-0163 §Context-drafted at the SPEC commit, §Decision + §Consequences at IMPL per ADR-0044; **ADR-0164** = the ADR-0045 split-application ADR, landed IN FULL at the SPEC commit per the phase-08 ADR-0084 precedent; SPEC-time ADR-count revision 8 anticipated → 9 landed per ADR-0044; **NO ADR-0125 amendment** — the FIRST §9 row to REUSE an existing ADR-0125 canonical, ADR-0163 records the explicit no-amendment 5th-canonical-REUSE decision). Per ADR-0106, §9 family-rows are flat top-level rows; phase 18 lands as row `18` + sub-phase rows `18.1` / `18.2` per the ADR-0045 split invoked at the SPEC commit (BRAINSTORM §1.4 flagged HIGH split-readiness — the SPEC §5 empirical-pin resolution CONFIRMED it). |
| 18.1 | ext-authz-http | 17 | done | 2026-05-15 | First sub-phase of the phase-18 ADR-0045 split (`18` parent). Lands `envoy.filters.http.ext_authz` in **HTTP service mode** — the foundational filter. Shipped via the **15-task phase-18.1 PLAN** (Tasks 1–14 + Task 15 REVIEW). Production: `internal/filter/http/extauthz/` package (`extauthz.go` 1087 LoC + `check.go` 411 LoC + `attributes.go` 435 LoC + `doc.go` 192 LoC = **2125 LoC production**) + new `test/helpers/extauthzhttp/` test-helper (`extauthzhttp.go` 190 LoC + `doc.go` 27 LoC = **217 LoC**). **DECODER-only** `HTTPFilter` (`Encoder: nil`; 5th §9 row to ship pure decode-side, mirrors phase-12 csrf + phase-13 buffer + phase-14 compressor + phase-16 rbac). Key impl decisions: dual-mode `compiledConfig` envelope (the `services`-oneof dispatch is defined here; `grpc_service` PARSE-REJECTs in 18.1 with an envoy-go-strict error, activated by 18.2); HTTP-outbound auth-check (ADR-0159 disposition (b) — thin ext_authz-local `httpAuthClient` in `check.go`, NOT a generalized `internal/httpclient/`; ZERO retry per SPEC §12.2; composes-against the phase-17 `internal/jwks/Fetcher` outbound-HTTP structure); request-side header filtering via top-level `ExtAuthz.allowed_headers`/`disallowed_headers` (`ListStringMatcher` five-mode: exact/prefix/suffix/contains/`ignore_case`/`safe_regex`; `custom` PARSE-REJECT) + `AuthorizationRequest.headers_to_add` + `path_prefix`; deprecated `AuthorizationRequest.allowed_headers` (#1) HONORED-IF-PRESENT for backward-compat (ADR-0160 D6); HTTP-mode bidirectional header-mutation (`AuthorizationResponse.{allowed_upstream_headers, allowed_upstream_headers_to_append, allowed_client_headers}` — ADR-0161 HTTP-mode portion) + `validate_mutations` gating → `invalid` counter; `with_request_body` via ADR-0128 phase-13 decode-side body-buffering reuse (ADR-0162; `allow_partial_message:false` over-limit → `SendLocalReply(413, …)` + `connection: close`; auth NOT called on over-limit); per-route **5th-canonical REUSE** + SHARED-stats (ADR-0163 — FIRST §9 row to REUSE an ADR-0125 canonical; NO ADR-0125 amendment; `ExtAuthzPerRoute{oneof{disabled:true | check_settings(CheckSettings)}}`; PGV `const: true` on `disabled` + PGV-required `override` oneof); 6-counter filter stat surface (`ok`/`denied`/`error`/`disabled`/`failure_mode_allowed`/`invalid` under `http.<HCM_stat_prefix>.ext_authz.*`; HCM-rooted SN2-reuse; `disabled` STRUCTURALLY UNREACHABLE under MVP; stat-table 71 → 77; §18.P6/§18.P7 RATIFIED-PENDING closures confirmed at Task 8 empirical scrape); error-posture fields (`failure_mode_allow`/`failure_mode_allow_header_add`/`status_on_error`/`validate_mutations`) consumed proto-faithful (ADR-0157); async-resume outbound-call leg + per-request cancellable `context.Context` + `OnDestroy` cancel (phase-09 primitive reuse); boot-registration (14th extension-registry entry, alphabetical between `envoygotest` and `fault`); deny-path `SendLocalReply` with status/body/headers from auth response (ADR-0156 — FIRST §9 row whose deny-path status code is NOT fixed by the filter; §18.P11 deny-path header-set ordering RATIFIED at Task 13). Differential fixture `0020-http-ext-authz-http` (**7 HTTP-mode scenarios**: allow / deny→verbatim-auth-body / error→`status_on_error` / `failure_mode_allow` / `with_request_body` / per-route `disabled:true` / per-route `check_settings`) + 1 NEW test-helper (in-process HTTP auth server `test/helpers/extauthzhttp/`) + the 22nd fuzzer `FuzzExtAuthzConfigParse`; **21/21 differential fixtures 0000–0020 green** at phase-done. **7 anticipated ADRs LANDED** (ADR-0156/0157/0159/0160(HTTP-mode portion)/0161(HTTP-mode portion)/0162/0163; §Decision + §Consequences bodies authored at impl-time Tasks 2/2/3/4/5/6/7 per ADR-0044 ADR-on-impl convention; §Context drafts landed at SPEC commit `308e9b6`). **ADR-0044 escape-valve NOT triggered** (0 impl-time-unanticipated ADRs — the anticipated async-resume race-guard surface D4 → `mu`/`done` guard + `context.WithCancel` sufficed; ADR-0165 NOT authored; next-free ADR is ADR-0165 for phase 18.2). **NO ADR-0125 amendment paragraph** — confirmed at phase-done via `grep -cE '^\*\*(xiv)\*\*' docs/envoy-go/DECISIONS.md = 0`. BEHAVIOR_CONTRACT.md 6-edit bundle applied at Task 14 (§13.1 ext_authz subsection + §13.2 stat table 71→77 + §13.3 equivalence-matrix row + §13.4 Phase 18.1 forward-pointer notes + §13.6 per-route canonical cross-reference + §13.7 HTTP outbound auth-check framework note). SPEC at `docs/envoy-go/phases/18.1-ext-authz-http/SPEC.md`; PLAN at `docs/envoy-go/phases/18.1-ext-authz-http/PLAN.md`. |
| 18.2 | ext-authz-grpc | 18.1 | in-progress |  | Second sub-phase of the phase-18 ADR-0045 split (`18` parent). Lands `envoy.filters.http.ext_authz` in **gRPC service mode** against the 18.1 package surface — the secondary-transport landing: the NEW `internal/grpcclient/` gRPC-client framework primitive (ADR-0158 — envoy-go's FIRST gRPC infrastructure of any kind; thin generic `Dialer` (cluster-name → `*grpc.ClientConn` via `grpc.WithContextDialer((*cluster.Cluster).Dial)` + `WithTransportCredentials(insecure)` — TLS terminates at the cluster-manager layer per the §11.P13 in-session SPEC scrape) + ext_authz-typed `AuthClient` wrapper (`envoy.service.auth.v3.Authorization/Check` stub from go-control-plane v1.32.4 — no codegen); cross-phase-reusable for ext_proc + global_ratelimit; one `*grpc.ClientConn` per (cluster_name, compiledConfig) pair created at config-load time and shared across all per-stream Check calls), the `grpc_service` arm activated in the `compiledConfig` dispatch (ADR-0157 §Decision amended at 18.2 IMPL; `core.GrpcService.GoogleGrpc` PARSE-REJECTs envoy-go-strict; `initial_metadata` + `retry_policy` SILENT-IGNORED), the gRPC-mode `AttributeContext`/`CheckRequest` builder (ADR-0160 gRPC-mode portion — source/destination Peers; `source.principal` via the phase-16 ADR-0144 `DownstreamPrincipal()` reuse; `source.certificate` gated by `include_peer_certificate`; `tls_session.sni` gated by `include_tls_session`; `destination.principal` populates AUTOMATICALLY from the listener TLS cert per §11.P4 in-session SCRAPE refinement — NOT gated; `request.http` per parent §5.P4 + §11.P4 in-session — pseudo-headers lowercased; HCM injects `x-envoy-auth-partial-body` + `x-forwarded-proto` + `x-request-id` into the map; `request.time` as a `Timestamp`; `context_extensions` merge listener+per-route; `encode_raw_headers` `headers`-vs-`header_map` discipline), the `CheckResponse` → disposition mapping incl. `OkHttpResponse`/`DeniedHttpResponse` header mutation (ADR-0161 gRPC-mode portion — DeniedHttpResponse headers VERBATIM unlike HTTP-mode's matcher-filtered headers; OkHttpResponse.{headers (append_action), headers_to_remove}; `OkHttpResponse.response_headers_to_add` DEFERRED — decode-side-only filter shape; `validate_mutations` gating identical to HTTP-mode). Differential fixture `0021-http-ext-authz-grpc` (**8 scenarios**: 7 mirroring 0020 + 1 gRPC-only `OkHttpResponse` upstream mutation; three-listener topology l_test_a/b/c per failure_mode_allow scoping) + 1 NEW test-helper `test/helpers/extauthzgrpc/` (FIRST in-process gRPC server in envoy-go's test tree; plaintext h2c on ephemeral port; scriptable per-`:path` CheckResponse) + 23rd fuzzer `FuzzCheckResponseMapping` (fuzz `*CheckResponse` proto-bytes → `mapGRPCResponse` → `checkDisposition`; the existing `FuzzExtAuthzConfigParse` corpus extends with `grpc_service` config variants — no separate config-parse fuzzer needed). The mode-agnostic `compiledConfig` envelope + `checkDisposition` + 6-counter `filterStats` + deny-path `SendLocalReply` + async-resume leg + per-route 5th-canonical REUSE + `with_request_body` ADR-0128 reuse all carry forward UNCHANGED from 18.1; `compiledConfig` struct field-shape stays field-final per ADR-0157 §Decision (gRPC-specific config — `include_*` gates, `encode_raw_headers`, `pack_as_bytes` — captured directly in the closure's lexical scope, NOT promoted to struct fields). File layout: extend existing `check.go` (+250–400 LoC: buildGRPCCheckFn + mapGRPCResponse + dispositions) + `attributes.go` (+200–350 LoC: buildAttributeContext + helpers); NO new `grpc_*.go` files. 18.2-landing ADRs: ADR-0158 (full §Decision + §Consequences) + ADR-0157 §Decision AMENDMENT (grpc_service activation) + ADR-0160/0161 gRPC-mode portions. **All 13 parent SPEC §5 empirical pins CLOSED RATIFIED at the 18.2 SPEC commit** — the §18.P4 `tls_session` portion + §18.P13 gRPC dial / TLS-to-auth-cluster plumbing were RATIFIED-PENDING-IMPL-TIME at the parent SPEC commit and closed in-session at the 18.2 SPEC scrape on 2026-05-15 (TLS-fronted downstream listener + TLS-fronted gRPC auth cluster + Go gRPC `Authorization/Check` server fronted with self-signed TLS; verbatim CheckRequest captured at SPEC §11). 18.2 IMPL has zero RATIFIED-PENDING pins. **ADR-0044 escape-valve held in reserve for ~0–1 impl-time-unanticipated ADRs** (the §11.P13 closure REMOVES the most-likely escape-valve surface — the cluster-manager coupling for `EnvoyGrpc` cluster-name resolution + TLS-to-auth-cluster plumbing was confirmed clean with no new TLS-layer lift needed; the §11.P4 in-session refinement of `destination.principal` automatic population is folded into ADR-0160 §Decision rather than a new ADR). 2 phase-18.1 forward-pointers CLOSED (gRPC arm activation + `context_extensions` consumption). The 18.2 phase-done commit closes BOTH row `18.2` AND the parent row `18` per parent SPEC §8 rollup discipline. SPEC at `docs/envoy-go/phases/18.2-ext-authz-grpc/SPEC.md` (this commit; supersedes the README.md stub authored at the parent SPEC commit). Successor session: phase-18.2 PLAN (`superpowers:writing-plans`). |

---

## Feature Families (09+)

Each heading below becomes one or more brainstormed phases when it enters `in-progress`. Do **not** pre-populate per-phase rows here; add them at brainstorming time. Splitting (§6) applies as it would to trunk phases.

### HTTP filters family

Header manipulation, cors, compression, fault, local + global rate limit, jwt_authn, rbac, ext_authz, ext_proc, oauth2, csrf, buffer, lua, wasm, adaptive concurrency, admission control, bandwidth limit.

### Network filters family

redis, mongo, kafka_broker, thrift, zookeeper [scope TBD], echo, direct_response, sni_cluster, rbac network.

### Load balancing family

least_request, random, ring_hash, maglev, subset LB, locality-weighted LB, priority load balancing, panic thresholds.

### Upstream robustness family

Active health checks HTTP/TCP/gRPC/custom, outlier detection variants, circuit breakers, retries + hedging, per-protocol connection pooling.

### HTTP/3 + QUIC family

quic-go transport, downstream H3 listener, upstream H3 cluster, `h3spec` gate.

### gRPC family

gRPC bridge, gRPC-Web, gRPC-JSON transcoding, interop conformance.

### xDS / dynamic config family

ADS, delta xDS, LDS, CDS, RDS, EDS, SDS, RTDS, reconnection, initial-fetch timeout.

### Observability family

gRPC ALS, OTLP access log, OTel/Zipkin/Jaeger/Datadog/XRay tracing, stats sinks, tap filter.

### Runtime + hot restart family

Runtime layer (RTDS consumer); hot-restart / graceful-drain semantics beyond phase 08's minimum.

### WASM host family

Own multi-phase sub-project: ABI, engine binding, proxy-wasm conformance.

### Deprecated / edge features

Explicit out-of-scope ADRs unless later re-opened.
