# envoy-go Roadmap

## Schema

Phases are tracked as rows in the tables below. Columns:

| Column | Meaning |
|---|---|
| `id` | Phase identifier (e.g. `00`, `04`, `04.1`). Sub-phases use a decimal suffix (see §6 of `BOOTSTRAP_PROMPT.md`). |
| `title` | Short human-readable title matching the phase directory slug under `docs/envoy-go/phases/`. |
| `depends-on` | Comma-separated list of phase ids that must be `done` before this phase can enter `in-progress`. Empty for the first phase. |
| `status` | One of `planned`, `in-progress`, `blocked`, `done`. |
| `sub-phases` | If the phase was split (see §6), this lists the sub-phase ids (e.g. `04.1, 04.2`). Empty otherwise. |
| `summary` | One-line summary of the phase's differential surface at completion. |

### Invariants

- **Append-only history.** Rows are never deleted. Only `status` and `sub-phases` columns are updated in place. Sub-phases get their own rows.
- **Status transitions.** `planned` → `in-progress` → (`blocked` or `done`). `blocked` is an exit state only when an upstream dependency is unavailable; the phase resumes on the same row when unblocked.
- **Sub-phase rows.** When a parent phase is split, the parent stays `in-progress` with `sub-phases` listing children; each child gets its own row whose `depends-on` references the prior child (so children execute in order unless stated otherwise).
- **Families (§9 headings) are not rows.** Each heading is expanded into per-phase rows when a phase under that family enters `in-progress`. Do not pre-populate rows for unbrainstormed phases.

---

## MVP Trunk (phases 00–08)

These phases ship in order. Each depends on the previous being `done`, because each adds a primitive the next phase relies on. Splitting (§6) is permitted within any phase.

| id | title | depends-on | status | sub-phases | summary |
|---|---|---|---|---|---|
| 00 | bootstrap | — | done |  | Bootstrap: repo layout, CI, Docker reference Envoy, differential harness skeleton, `ENVOY_TARGET.md` pin, trivial echo fixture. Harness boots; one TCP echo fixture green. |
| 01 | static-bootstrap-config | 00 | done |  | Static bootstrap config loader (node, admin, static_resources skeleton). Config parses; admin `/ready` behaves like Envoy. |
| 02 | tcp-proxy | 01 | done |  | Listener + TCP proxy filter + static cluster + round-robin LB (plaintext). TCP proxy fixture green. |
| 03 | tls | 02 | done |  | Downstream TLS termination + upstream TLS origination + SNI. TLS TCP fixture green. |
| 04 | http-1.1 | 03 | done |  | HTTP connection manager (HTTP/1.1) + route match + router filter + direct_response. HTTP/1.1 routing fixture green. |
| 05 | http-2 | 04 | done | 05.1, 05.2 | HTTP/2 downstream + upstream (low-level framer, own conn mgr). HTTP/2 fixture green; `h2spec` above threshold. Split planner-time per ADR-0045 (`BOOTSTRAP_PROMPT.md` §6.2; phase-05 SPEC §11.1 split-by-surface). |
| 05.1 | downstream-h2 | 04 | done |  | Downstream HTTP/2 termination (own framer, own ServerConn, ALPN dispatch); first non-vacuous conformance gate `h2spec` (53/53 PASS at the ADR-0051 pin); `--allow-h2c` test-only flag; `CONFORMANCE_PINS.md`; `BEHAVIOR_CONTRACT.md ## HTTP/2` scaffold. No new differential fixture (gate a vacuously green per ADR-0045; routed-to-upstream H2 differential surface deferred to 05.2 + fixture 0004). |
| 05.2 | upstream-h2 | 05.1 | done |  | Upstream HTTP/2 origination (`Cluster.DialH2`, own `ClientConn`, `routerActionH2`, cluster `HttpProtocolOptions` parsing); fixture `0004-h2-routing` (full HTTPS h2 end-to-end); closes ADR-0035 H2 leg; extends `BEHAVIOR_CONTRACT.md ## HTTP/2` with upstream + fixture-0004 rules. |
| 06 | observability-baseline | 05 | done | 06.1, 06.2 | Observability baseline: stats + Prometheus admin endpoint + access log (file sink, Envoy default format). Access log + Prometheus fixtures green. Split planner-time per ADR-0045 (`BOOTSTRAP_PROMPT.md` §6.2; phase-06 SPEC + BRAINSTORM §1 split-by-surface). |
| 06.1 | stats-prometheus | 05 | done |  | Internal `stats` package (atomic-counter Registry, lock-free hot path, no third-party Prometheus dependency); `/stats/prometheus` admin endpoint; 17 stat-emit call sites across listener / HCM / cluster (counters + gauges only — histograms deferred); fixture `0005-prometheus-stats` (per-counter delta-equality + per-gauge snapshot-equality on the 17 names against reference Envoy under a 5-request defined load); populates `BEHAVIOR_CONTRACT.md ## Stat-name mapping` (rules SN1–SN8); bundles 05.2 REVIEW M-9 (h2RouterActionAdapter log line). |
| 06.2 | access-log | 06.1 | done |  | Internal `accesslog` package (file sink, Envoy default format formatter, async writer); HCM access-log emit hooks; fixture `0006-access-log`; populates `BEHAVIOR_CONTRACT.md ## Access log field mapping`. Closes the parent row `06` at its phase-done commit (mirrors 05 / 05.1 / 05.2 closure pattern). |
| 07 | filter-chain-framework | 06 | done | 07.1, 07.2 | Filter chain framework: iteration protocol, per-route config, extension registry (HTTP side under HCM); listener-side chain-match completion (listener_filters, FilterChainMatch beyond SNI, default_filter_chain). Framework fixtures green; trivial pluggable filter covers all iteration states. Split planner-time per ADR-0045 (`BOOTSTRAP_PROMPT.md` §6.2; phase-07 BRAINSTORM §1 split-by-surface). |
| 07.1 | http-filter-framework | 06 | done |  | HTTP filter iteration protocol (Envoy-faithful with async-resume; narrow method set; status enums settled) + extension registry (`*HTTPRegistry` threaded; freeze-after-boot mirrors `*stats.Registry` LBP-1) + 3-tier `typed_per_filter_config` merge (Route > VirtualHost > RouteConfiguration; most-specific override) + trivial real filter `envoy.filters.http.cors` (differential fixture `0007a`) + test-only probe filter `envoy.filters.http.envoy_go_test` (structural fixture `0007b`); router moves from `internal/filter/hcm/actions.go` to a new `internal/filter/http/router/` terminal-filter package; populates `BEHAVIOR_CONTRACT.md ## HTTP filter chain` with the four §11 empirical-pin blocks; supersedes ADR-0040 totally; partially supersedes ADR-0042; amends ADR-0041 silent-ignore set. |
| 07.2 | listener-chain-completion | 07.1 | done |  | `listener_filters` framework (`tls_inspector` as the first concrete filter — `original_dst` deferred to a future phase per Decision F) + `FilterChainMatch` fields beyond SNI (destination_port, prefix_ranges, source_*, application_protocols/ALPN, transport_protocol) + `Listener.default_filter_chain` (promoted from phase-03 parse-error). Differential fixture `0008-listener-chain-match` (dual-listener `l_test_a` + `l_test_b` plaintext; 5-connection workload across 4 chains + 1 `default_filter_chain`; per-connection backend-port equivalence). Closes the parent row `07` at its phase-done commit (mirrors 05 / 05.1 / 05.2 + 06 / 06.1 / 06.2 closure pattern). Partially supersedes ADR-0033. Anticipated ADRs ADR-0077..ADR-0083 (per SPEC §10). |
| 08 | admin-api-and-drain | 07 | done | 08.1, 08.2 | Minimum admin API (config_dump, stats, clusters, listeners, ready, server_info) + graceful drain. Admin + drain fixtures green. Split planner-time per ADR-0045 (`BOOTSTRAP_PROMPT.md` §6.2; phase-08 BRAINSTORM §1 split-by-surface). |
| 08.1 | admin-endpoints | 07 | done |  | Four new read-only admin endpoints under the existing `internal/admin/` HTTP/1.1 mux: `GET /config_dump` (`application/json` via `protojson` over `*adminv3.ConfigDump`), `GET /clusters` (`text/plain`), `GET /listeners` (`text/plain`), `GET /server_info` (`application/json` via `protojson` over `*adminv3.ServerInfo`); constructor-widening pattern threads `*bootstrap.Bootstrap` + `*cluster.Manager` + `*listener.Manager` into `admin.New()`; new `cluster.Manager.Clusters()` snapshot accessor; `BEHAVIOR_CONTRACT.md ## Admin API — /ready` restructured into `## Admin API` umbrella with five per-endpoint subsections + four new equivalence-matrix rows. Differential fixture `0009-admin-config-dump` (per-endpoint equivalence under a 5-request defined load with per-endpoint tolerance discipline). New fuzzer `FuzzConfigDumpFormat` (10 fuzzers post-08.1). Anticipated ADRs ADR-0084..ADR-0090 (per SPEC §8). Empirical-pin block (SPEC §11) executed IN-SESSION against reference Envoy v1.37.2 per ADR-0004. |
| 08.2 | graceful-drain | 08.1 | done |  | New `internal/drain/` package (drain-state machine LIVE → DRAINING → exit) + `cmd/envoy-go/main.go` SIGTERM-handler upgrade + `internal/listener.Manager.Drain` + `internal/cluster.Manager.Drain` + `POST /drain_listeners` admin endpoint + `/ready` DRAINING-state body extension (partially supersedes ADR-0015) + `/server_info` `state`-field DRAINING transition + `BEHAVIOR_CONTRACT.md ### /drain_listeners` subsection + `## Graceful drain` umbrella section. Differential fixture `0010-graceful-drain` green. Closes the parent row `08` at its phase-done commit (mirrors 05 / 05.1 / 05.2 + 06 / 06.1 / 06.2 + 07 / 07.1 / 07.2 closure pattern). 08.2's phase-done is the BOOTSTRAP_PROMPT.md §8 MVP-trunk-close commit. |
| 09 | http-filter-fault | 08 | done |  | New `internal/filter/http/fault/` package implementing `envoy.filters.http.fault` (Envoy v1.37.2 canonical fault filter) under the 07.1 framework; first concrete phase under §9 HTTP filters family. MVP envelope per SPEC §1 (revised per §11 empirical pins): `delay.percentage` + `delay.fixed_delay` (async-resume via `time.AfterFunc` + `cb.ContinueDecoding`); `abort.percentage` + `abort.http_status` constrained `[200, 600)` per PGV pin §11.1 (terminal-replace via `cb.SendLocalReply` with body byte-exact `fault filter abort` (18 bytes, no trailing newline) + 4-header set per §11.3); `headers` field for request-header gating with StringMatcher.exact only per §11.8; `max_active_faults` concurrency cap (LBP-1 sixth application). Differential fixture `0011-http-fault` green (4 scenarios per SPEC §7.1: delay-only listener-inherited, combined delay+abort per-route, per-route wholesale-override, headers-field exact-match gate; the BRAINSTORM-anticipated 5th "header-driven abort" scenario drops per §11.5 amendment). FIRST production filter exercising async-resume on the request side (cors only short-circuited via SendLocalReply). Deferred per `BOOTSTRAP_PROMPT.md` §9 family-expansion: `response_rate_limit`, `abort.grpc_status` (gRPC family), all four runtime-key fields (Runtime + hot restart family), **`delay.header_delay` + `abort.header_abort` proto sub-messages COUPLED with the four `x-envoy-fault-{delay,abort}-request[-percentage]` request headers** (deferred together per §11.5 + ADR-0104 — the request-header path requires the proto sub-messages to activate; cannot be cleanly separated), `upstream_cluster`, `downstream_nodes`, `disable_downstream_cluster_stats`, `filter_enabled` / `filter_enabled_runtime`, HeaderMatcher non-exact variants (each gets a deferral ADR per ADR-0040 format). 5 stats per §11.6 (4 counters + 1 gauge; 17→22-name BEHAVIOR_CONTRACT extension; route A — `fault.response_rl_injected` permanently-zero counter for parity). Anticipated ADRs ADR-0100..ADR-0107 (per BRAINSTORM §9; ADR-0104 repurposed from implementation to deferral). Per ADR-0106 (BRAINSTORM Decision 12), HTTP filters family expands as flat top-level rows (no parent 09 row with sub-phases); each filter is its own coherent phase. ADR-0045 surface-split release valve stays available if SPEC/PLAN find > ~1500 LoC / > ~25 tasks. |
| 10 | http-filter-header-mutation | 09 | done |  | New `internal/filter/http/header_mutation/` package implementing `envoy.filters.http.header_mutation` (Envoy v1.37.2 canonical header-mutation filter) under the 07.1 framework. THIRD §9 family-row (after cors @ 07.1, fault @ 09). MVP envelope per SPEC §1 (revised per §11 empirical pins): `mutations.request_mutations` + `mutations.response_mutations` (both directions; AppendAction × 4 + Remove + `keep_empty_value` boundary per §11.2 confirmation; multi-valued OVERWRITE-collapse + APPEND-preserve per §11.4 confirmation); `HeaderMutationPerRoute` + `most_specific_header_mutations_wins` (both true/false; multi-tier evaluation across Route/VirtualHost/RouteConfiguration tiers; cross-tier ordering per §11.5 confirmation matches proto comment verbatim). Protected-header set per §11.1 (`{:method, :path, :authority, :scheme, :status, host}` case-insensitive on `host`) — **CONFIG-LOAD-TIME enforcement** (NOT silent runtime no-op as BRAINSTORM hypothesized; MAJOR amendment to BRAINSTORM Decision 11 per §1.1 + ADR-0111). Differential fixture `0012-http-header-mutation` (4 scenarios per SPEC §7.1: listener-only, per-route override, multi-tier flag=false least-specific-wins, multi-tier flag=true most-specific-wins; the BRAINSTORM-anticipated 5th "protected-headers" scenario drops per §11.1 amendment — migrates to unit-test territory since protection is config-load-time). NEW framework method `PerRouteConfig.ResolveAllTiers` (~60 LoC sibling to existing `Resolve` per ADR-0073; amends ADR-0073) + new symmetric `RequestRouteConfigsAllTiers` / `ResponseRouteConfigsAllTiers` callbacks (~30 LoC) + new EAGER per-route validation hook (~40 LoC framework delta per SPEC §6.7 / §12 deferred decision 3). Zero new stats per §11.3 confirmation (`## Stat-name mapping ### 22-name table` UNCHANGED in phase 10; analogous to cors no-stats per ADR-0074). Anticipated ADRs ADR-0108..ADR-0113 (6 ADRs per SPEC §8; ADR-0114 stats-absence dropped per §8.1 consolidation — folded inline into §13.1 BEHAVIOR_CONTRACT subsection). Deferred per ADR-0040 format: `mutations.query_parameter_mutations` (path-query rewriting subsystem; ADR-0112); header-value formatter substitution (`%REQ(:path)%` etc — Envoy command-string subsystem; ADR-0113). Per ADR-0106, §9 family-rows are flat top-level rows; phase 10 lands as row `10`, NOT as a sub-phase of any §9 parent. ADR-0045 surface-split release valve stays available if PLAN finds > ~1500 LoC / > ~25 tasks; SPEC's position is single-row. |
| 11 | http-filter-local-ratelimit | 10 | done |  | New `internal/filter/http/localratelimit/` package implementing `envoy.filters.http.local_ratelimit` (Envoy v1.37.2 canonical local rate-limiting filter) under the 07.1 framework. FOURTH §9 family-row (after cors @ 07.1, fault @ 09, header_mutation @ 10). MVP envelope per SPEC §1.1: 5 fields consumed (`stat_prefix`, `token_bucket{max_tokens, tokens_per_fill, fill_interval}`, `status{code}` default 429, plus the `LocalRateLimitPerRoute` per-route container); 14 fields silently ignored under inline deferral (BRAINSTORM-anticipated ADR-0120 omnibus dropped per SPEC §8.1; deferral organized by 8 family-clusters at SPEC §2.1 + §13.5: descriptor-action; Runtime + shadow-mode; xDS cluster-state; response-side header injection; per-connection lifecycle; multi-stage; X-RateLimit headers + vh policy; gRPC trailer). Token-bucket primitive Option A — lazy refill on access, single sync.Mutex per bucket, no per-bucket goroutine, monotonic time via `time.Now().UnixNano()` (Go ≥1.9 monotonic guarantee). FIRST production filter where per-route TPFC override implies independent stateful resources (per-route entries each own a `tokenBucket` per ADR-0117 = ADR-0073 amendment); cors / fault / header_mutation per-route is data-only. Differential fixture `0013-http-local-ratelimit` (4 scenarios per SPEC §7.1: basic-allow, basic-rate-limited, refill-after-fill_interval ±10ms, per-route-override). Stat surface 22→26 names (4 new counters; `enforced == rate_limited` MVP invariant per ADR-0118; filter-specific Prometheus tag-extractor `envoy_local_http_ratelimit_prefix` per SPEC §11.5 — corrects BRAINSTORM-hypothesized `envoy_http_conn_manager_prefix`). Rate-limited response status 429, body byte-exact `local_rate_limited` (18 bytes; no LF), 4-header set lowercase wire-form (`content-length`, `content-type`, `date`, `server: envoy` — `server: envoy` corrects BRAINSTORM-hypothesized `envoy-go`); SendLocalReply mechanism mirrors fault abort. Empirical pin §11.2c: `fill_interval` 50ms minimum is filter-internal Envoy check (NOT PGV); envoy-go `New` factory mirrors. Empirical pin §11.2/§11.4: `filter_enabled` + `filter_enforced` `RuntimeFractionalPercent` fields default to 0% in reference Envoy; fixture configs MUST set both to 100% explicitly on BOTH sides for differential equivalence (envoy-go silent-ignores; behavior is always-100%). NO new framework primitive required (unlike phase 10's `ResolveAllTiers`); reuses existing 3-tier `PerRouteConfig.Resolve`. 15th fuzzer `FuzzLocalRateLimitConfigParse`. Anticipated ADRs ADR-0114..ADR-0119 (6 ADRs per SPEC §8; ADR-0120 omnibus dropped per §8.1; ADR-0114 captures package-name divergence from header_mutation's underscore-preserving directory pattern — phase 11 uses `localratelimit/` no-underscore directory). Per ADR-0106 (validated at phases 09 + 10 + 11), §9 family-rows are flat top-level rows; phase 11 lands as row `11`, NOT as a sub-phase of any §9 parent. ADR-0045 surface-split release valve stays available if PLAN finds > ~1500 LoC / > ~25 tasks; SPEC's position is single-row at ~1100–1400 LoC / ~12–16 tasks. |
| 12 | http-filter-csrf | 11 | done |  | New `internal/filter/http/csrf/` package implementing `envoy.filters.http.csrf` (Envoy v1.37.2 canonical same-origin enforcement filter) under the 07.1 framework. FIFTH §9 family-row (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11). MVP envelope per SPEC §1.1 (revised per §11 empirical pins): 1 field actively consumed (`additional_origins[].StringMatcher.exact` only — non-exact variants dropped at PARSE time per ADR-0101 §3); 1 field PGV-validated-not-honored (`filter_enabled` — REQUIRED at parse-time per §11.11 amendment, MAJOR REVISION from BRAINSTORM "silent-ignore" hypothesis; runtime always-100%-active); 1 field silent-ignored entirely (`shadow_enabled` — optional at parse, always-never-shadow at runtime; couples to Runtime + hot restart family). Method gate `{POST, PUT, DELETE, PATCH}` confirmed (§11.1). Source-origin extraction trichotomy per §11.2 amendment (MAJOR REVISION from BRAINSTORM 2-case hypothesis): (i) `Origin: null` literal → empty source NO Referer fallback; (ii) `Origin:` empty/absent → fall back to Referer's hostAndPort; (iii) `Origin:` non-empty unparseable → verbatim string used as source. Comparison algorithm HOST:PORT-only equality per §11.3 + §11.7 + §11.8 amendments (MAJOR REVISION from BRAINSTORM "full-origin with normalization" hypothesis): scheme STRIPPED on both sides (X-Forwarded-Proto irrelevant); NO case folding; NO default-port stripping; trailing slash stripped via URL parser; `additional_origins[].exact` matches `host[:port]` form NOT full URL with scheme (operator footgun documented in BEHAVIOR_CONTRACT §13.1 + §13.4). Rejection wire shape confirmed: `SendLocalReply(403)` + body byte-exact `Invalid origin` (14 bytes, no LF) + 4-header set lowercase wire-form (`content-length: 14`, `content-type: text/plain`, `date: <RFC1123>`, `server: envoy`); reuses fault.abort/local_ratelimit primitive (§11.10). Per-route TPFC wholesale-override per ADR-0073 — data-only AND **stats SHARED with listener-level** (per §11.9 amendment; diverges from phase 11 local_ratelimit precedent which had independent per-route stats per ADR-0117). NO ADR-0073 amendment required. NO new framework primitive (thinnest §9 family-row to date). Stat surface 26→29 names (3 new counters per HCM stat_prefix: `request_valid`, `request_invalid`, `missing_source_origin`; `shadow_request_invalid` dropped from MVP — confirmed Envoy v1.37.2 also doesn't emit it under all-defaults config per §11.6; shadow-only mode reuses the regular 3-counter family); namespace anchors at HCM stat_prefix (reuses existing `envoy_http_conn_manager_prefix` Prometheus tag-extraction per §11.6 — NO new SN flattening rule; UNLIKE phase 11). Differential fixture `0014-http-csrf` (6 scenarios per SPEC §7.1: same-origin allowed, cross-origin rejected, additional_origins host:port match, missing source-origin rejected, Referer fallback, per-route wholesale override with shared stats; GET passthrough unit-only). Fixture configs MUST set `filter_enabled.default_value: 100/HUNDRED` explicitly on both sides per §11.11 amendment (PGV-required by Envoy at parse-time). 16th fuzzer `FuzzCsrfPolicyConfigParse`. Anticipated ADRs ADR-0120..ADR-0124 (5 ADRs per SPEC §8; no consolidation; no ADR-0073 amendment paragraph; no ADR-0061 amendment). Per ADR-0106 (validated at phases 09 + 10 + 11 + 12), §9 family-rows are flat top-level rows; phase 12 lands as row `12`, NOT as a sub-phase of any §9 parent. ADR-0045 surface-split release valve stays available if PLAN finds > ~1500 LoC / > ~25 tasks; SPEC's position is single-row at ~750-1100 LoC / ~10-14 tasks. |
| 13 | http-filter-buffer | 12 | done | 2026-05-09 | New `internal/filter/http/buffer/` package implementing `envoy.filters.http.buffer` (Envoy v1.37.2 canonical "buffer entire request body before forwarding upstream" filter) under the 07.1 framework. SIXTH §9 family-row (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11, csrf @ 12). 1 listener-level field consumed (`max_request_bytes` UInt32Value; envoy-go-own validation: non-nil + value > 0 + value ≤ 1048576 / 1 MiB per ADR-0126). FIRST §9 family-row to interact with the framework's body-buffering machinery; FIRST §9 family-row whose per-route proto introduces a top-level `disabled` boolean shortcut as a first-class shape — codifies "disabled-OR-override" as the **5th canonical per-route discipline** (ADR-0125). Body-counting algorithm — STREAMING-CAP ONLY (per ADR-0127 v2 + SPEC §11.6 empirical refutation of Content-Length fast-fail): `DecodeHeaders` returns `Continue` on bodied + non-disabled (NOT `StopIteration` — envoy-go synchronous-HCM dispatch constraint per ADR-0128); `DecodeData` returns `DataContinue` mid-stream (NOT `DataStopIterationAndBuffer`; HCM's `connection.go` bodyBuf already accumulates all bytes per ADR-0076 + ADR-0128); overflow fires `SendLocalReply(413, "Payload Too Large", connClose)` + `DataStopIterationNoBuffer`; terminal chunk invokes `maybeAddContentLength` (chunked → fixed-CL conversion observable upstream, mirrors `buffer_filter.cc:91-97`). Wire shape byte-equivalent to reference Envoy on every observable axis. Per-route stats SHARED with listener-level (mirrors phase-12 csrf ADR-0124; DIVERGES from phase-11 local_ratelimit ADR-0117). Stat surface stays at 29 names (phase 13 contributes ZERO new stat-table entries per SPEC §1.1 amendment 5 — reference Envoy emits no `envoy_http_buffer_*` counter family; overflow observable via existing `downstream_rq_4xx`). 6-scenario differential fixture `0015-http-buffer`. 17th fuzzer `FuzzBufferConfigParse`. 4 ADRs: ADR-0125 (package + 5th canonical per-route), ADR-0126 (compiledConfig + parse-time validation), ADR-0127 v2 (body-counting algorithm + maybeAddContentLength + 413 wire reuse; in-place updated at Task 12 to retract 100-Continue addendum + reflect Continue/DataContinue landed algorithm), ADR-0128 (NEW: framework primitives at `internal/filter/hcm/connection.go` — synthetic empty-terminal RunDecodeData + post-body CL reconciliation; FIRST framework deltas since phase 07.1). Phase 13 retires the SPEC §4 "no framework deltas" invariant. Per ADR-0106, §9 family-rows are flat top-level rows; phase 13 lands as row `13`. |
| 14 | http-filter-compressor | 13 | done | 2026-05-10 | New `internal/filter/http/compressor/` package implementing `envoy.filters.http.compressor` (Envoy v1.37.2 canonical response-side compression filter, gzip-only MVP) under the 07.1 framework. SEVENTH §9 family-row (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11, csrf @ 12, buffer @ 13). MVP envelope shipped per phase 14 SPEC §1 + §1.1 amendments: 6 listener consumed + 9 listener silent-ignored + 2 codec (Gzip) consumed + 3 codec silent-ignored = 8 grand-total consumed + 12 silent-ignored + 1 parse-rejected (non-Gzip `compressor_library` TypeURLs per ADR-0130). Body algorithm Path B (buffer-then-compress; framework as-is) per ADR-0131 — operates on `resp.Body` materialized by HCM's encode-chain at `connection.go:467-475` (one-shot full-body `RunEncodeData(ctx, resp.Body, true)`); writes back via new framework primitive `EncoderFilterCallbacks.OverwriteBody(b []byte)` per ADR-0131 §Decision (vi) (~20-25 LoC framework delta at `internal/filter/http/{callbacks.go, chain.go}` + `internal/filter/hcm/{connection.go, h2dispatch.go}`; symmetric to phase-13 ADR-0128 decode-side primitives); wire-shape divergence (envoy-go: `Content-Encoding: gzip` + fixed `Content-Length: <gzipped>` + identity transfer; Envoy: streaming `Content-Encoding: gzip` + chunked transfer + no CL) accepted with explicit forward-pointer to future encode-side streaming framework phase. Per-route TPFC `disabled`-OR-`overrides.response_direction_config` shape — SECOND row using ADR-0125 5th canonical disabled-OR-override (after phase-13 buffer); per-route override surface is FILTER-SPECIFIC and NARROWER than the listener-level config (only `remove_accept_encoding_header` per `ResponseDirectionOverrides` proto + per-route library swap silent-ignored); ADR-0125 gained amendment §(viii)-(x) at SPEC commit; no new per-route ADR. Stat surface 29→46 names (17 new counters per HCM stat_prefix: `header_compressor_overshadowed`, `header_compressor_used`, `header_identity`, `header_not_valid`, `header_wildcard`, `no_accept_header`, `not_compressed_etag`, `response.{compressed, content_length_too_small, not_compressed, total_compressed_bytes, total_uncompressed_bytes}`, `request.{compressed, content_length_too_small, not_compressed, total_compressed_bytes, total_uncompressed_bytes}`); NO new SN flattening rule (uses existing SN2 per phase 14 SPEC §1.1 amendment 3; the BRAINSTORM-hypothesized Rule SN10 was refuted at SPEC time when empirical scrape revealed the existing HCM-anchored SN2 sufficient). Per-route stats SHARED with listener-level (mirrors phase-12 csrf ADR-0124 + phase-13 buffer ADR-0125; DIVERGES from phase-11 local_ratelimit ADR-0117 INDEPENDENT). Differential fixture `0016-http-compressor` (6 scenarios: compress-text-default, skip-content-type, skip-min-content-length, skip-on-etag, per-route-disabled, per-route-rmAE-override). FIRST §9 row using **decompress-and-compare body-assertion discipline** per ADR-0133 — compressed bytes are STRUCTURALLY non-byte-exact between Go `compress/gzip` and Envoy libz (different block-boundary + Huffman-tree choices; multi-encoding gzip-format spec admits both as valid); decompressed bytes ARE byte-equivalent. 17/17 differential fixtures green at phase-done. 18th fuzzer `FuzzCompressorConfigParse`. **6 ADRs landed** (PLAN-time table called for 5; ADR-0044 ADR-on-impl escape-valve added a 6th at Task-14 follow-up): ADR-0129 (Task 2 — package + ENCODER+DECODER HTTPFilter + 17-counter filterStats), ADR-0130 (Task 2 — compiledConfig + codec-library Any-dispatch + Gzip level mapping), ADR-0131 (Task 4 — Path B + wire-shape divergence + EncoderFilterCallbacks.OverwriteBody framework primitive), ADR-0132 (Task 8 — 17-counter stat surface + SN2 reuse + per-route SHARED stats), ADR-0133 (Task 11 — differential-fixture decompress-and-compare body-assertion discipline), **ADR-0134 (Task 14 follow-up — unanticipated HCM `directResponseAction.response_headers_to_add` plumbing + `OVERWRITE_IF_EXISTS_OR_ADD`-only AppendAction parse-gate; surfaced at integration time when fixture-0016 scenario 2 image/png content-type-skip required route-level override on direct_response body)**. Plus ADR-0125 amendment §(viii)-(x) (already landed at SPEC commit). 4 per-side empirical-divergence counters pinned at Task 14 (`header_compressor_used`, `header_not_valid`, `response_not_compressed`, `request_not_compressed`) — reference Envoy v1.37.2 + envoy-go MVP diverge by design choice on each; both sides locked via `counterModePerSideExact`. Phase 14 does NOT promote ADR-0076's `per_connection_buffer_limit_bytes` / `per_request_buffer_limit_bytes` silent-ignore (phase 14 MVP is response-only; cap-promotion forward-pointer stays open for a future row). Per ADR-0106, §9 family-rows are flat top-level rows; phase 14 lands as row `14`. |
| 15 | http-filter-bandwidth-limit | 14 | done | 2026-05-11 | New `internal/filter/http/bandwidthlimit/` package implementing `envoy.filters.http.bandwidth_limit` (Envoy v1.37.2 canonical symmetric request+response throttle filter; **limit in KiB/s — kibibytes-per-second per proto comment line 95 + SPEC §1.1 amendment 6; NOT kilobits-per-second as BRAINSTORM hypothesized**) under the 07.1 framework. EIGHTH §9 family-row (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11, csrf @ 12, buffer @ 13, compressor @ 14). MVP envelope per SPEC §1 + §1.1 (**10 amendments authored at SPEC time** per §11 empirical-pin resolution): 4 fields consumed (`stat_prefix` PGV REQUIRED min_len=1, `enable_mode` all 4 values DISABLED/REQUEST/RESPONSE/REQUEST_AND_RESPONSE, `limit_kbps` UInt64Value PGV gte=1 + CODE-LEVEL REQUIRED at per-route per filter source `"limit must be set for per route filter config"`, `fill_interval` Duration default 50ms PGV [20ms, 1s]); 3 fields silent-ignored (`runtime_enabled` always-100%; `enable_response_trailers` always-no-trailers per future trailer-emission framework phase; `response_trailer_prefix` couples). Body algorithm Path B-async (buffer-then-delayed-emit via `time.AfterFunc` + `cb.ContinueDecoding/Encoding`) per ADR-0137 with **kbps-per-tick throttle math** per §1.1 amendment 6 + §11.P15: `chunk_size = limit_kbps × 1024 × fill_interval_seconds`; `throttle = ceil(body/chunk_size) × fill_interval`. Non-endStream `DecodeData`/`EncodeData` returns `DataContinue` (envoy-go HCM synchronous-dispatch deadlock avoidance per Task-14 finding (d); mirrors phase-13 buffer precedent; only endStream invocation returns `DataStopIterationAndBuffer` + arms timer; wire outcomes byte-equivalent). Wire-shape divergence (envoy-go Path B-async silent-then-blast vs Envoy Path A rate-paced chunks at exact `fill_interval` cadence; **per-side wall-clock equivalent within ±70ms tolerance** per §11.P9 + Task-14 adopted per-side discipline — SPEC §11.P9(c) cross-side claim REFUTED at Task 14 for bodies within initial-burst capacity, Envoy's initial-burst-discount diverges from envoy-go's deterministic ceil-formula) documented with forward-pointer to future encode-side streaming framework phase. **ZERO framework deltas** — FIRST §9 row since phase-12 csrf to introduce no new primitives; composes against phase-09 fault async-resume + phase-13 ADR-0128 decode-side body-buffering + phase-14 ADR-0131 encode-side `OverwriteBody` (anticipated: NOT invoked; framework's buffered-return path returns bytes unchanged) — load-bearing demonstration of phase-13/14 framework primitives' reusability. Per-route TPFC uses the same bare `BandwidthLimit` proto via TPFC (NO `BandwidthLimitPerRoute` wrapper per §11.P1 + §1.1 amendments 1 + 2; **phase-15 introduces a NEW canonical per-route pattern — bare-message-via-TPFC + code-level-required-`limit_kbps`-at-per-route — adjacent to ADR-0117's 4th canonical; documented at ADR-0125 §(xi) in-place amendment as the 6th canonical entry**; BRAINSTORM-hypothesized 5th canonical disabled-OR-override REFUTED). Stat surface 46→**60 names** (14 new active: 8 counters + 6 gauges; namespace `<stat_prefix>.http_bandwidth_limit.<counter>` underscore-infix NOT HCM-rooted per §11.P11; Prometheus inlines stat_prefix into base name `envoy_<stat_prefix>_http_bandwidth_limit_<counter>{}` per §11.P10 — NO tag-extractor + NO new SN10 rule); plus 2 unconditional Envoy histograms (`request_transfer_duration`, `response_transfer_duration`) allow-listed via twin-series-filter divergence-window per §1.1 amendment 9. **`*_enabled` + `*_enforced` counters use `counterModePerSideExact`** (Task-14 finding (b): Envoy's initial-burst-discount + per-request bump-on-active-side diverges from envoy-go's deterministic DecodeData-driven bump + per-tick-ticks-at-completion semantic); 6 gauges NOT asserted per Task-14 finding (c) (transient/noisy). Per-route stats INDEPENDENT per ADR-0139 + §11.P4 + §11.P14 (SECOND row using stateful-override-with-INDEPENDENT-stats per ADR-0117 precedent; phase-11 local_ratelimit FIRST). Differential fixture `0017-http-bandwidth-limit` (6 scenarios: response-only throttle, request-only throttle, REQUEST_AND_RESPONSE symmetric, tiny-body within-burst, per-route DISABLED-via-`enable_mode`, per-route override with own stat_prefix; per-side wall-clock tolerance ±70ms; byte-exact body equivalence — bandwidth_limit does NOT transform bytes); 18/18 differential fixtures green at phase-done. 19th fuzzer `FuzzBandwidthLimitConfigParse`. **5 ADRs landed** (ADR-0135..ADR-0139 anchored at impl-time per ADR-0044): ADR-0135 (Task 2 — package + types + factory + 14-stat `filterStats`), ADR-0136 (Task 2 — compiledConfig + parse-time validation + 3-silent-ignored discipline), ADR-0137 (Task 3 — kbps-per-tick `throttleDuration` helper + Path B-async + wire-shape divergence + non-endStream DataContinue HCM-deadlock-avoidance fix), ADR-0138 (Task 8 — 14-stat surface + namespace + Prometheus inline-prefix + histogram twin-series allow-list), ADR-0139 (Task 7 — per-route INDEPENDENT-stats wiring + `newFilterStatsIfAbsent` post-Freeze idempotent registration + `sync.Map` lazy-cache). Plus ADR-0125 §(xi) amendment paragraph (landed at SPEC commit). **Operational foot-gun** discovered at §11 probeJ: listener-level missing `limit_kbps` + active `enable_mode` causes runtime hang on both Envoy + envoy-go (byte-equivalent foot-gun) — documented at §1.1 amendment 10 + §13.4 forward-pointer notes. Per ADR-0106, §9 family-rows are flat top-level rows; phase 15 lands as row `15`. |
| 16 | http-filter-rbac | 15 | done | 2026-05-12 | New `internal/filter/http/rbac/` package (`rbac.go` 1191 LoC + `evaluator.go` 854 LoC + `doc.go` 148 LoC = **2193 LoC production**) + new top-level `internal/matcher/` package (`matcher.go` 510 + `doc.go` 75 = **585 LoC production**) implementing `envoy.filters.http.rbac` (Envoy v1.37.2 canonical role-based-access-control filter; decode-side dual-engine policy gate) under the 07.1 framework. NINTH §9 family-row (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11, csrf @ 12, buffer @ 13, compressor @ 14, bandwidth_limit @ 15). MVP envelope per SPEC §1 + §1.1 (12 amendments authored at SPEC time; all 18 §11 empirical pins resolved IN-SESSION against Envoy v1.37.2): 7 top-level fields consumed proto-faithful — `rules` + `matcher` (rules_specifier UDPA-field-alias; rules wins when both set per `rbac.pb.go:38`), `shadow_rules` + `shadow_matcher` (shadow_rules_specifier UDPA-field-alias; shadow_rules wins per `rbac.pb.go:53`), `rules_stat_prefix`, `shadow_rules_stat_prefix`, `track_per_rule_stats`. Inside `config.rbac.v3.RBAC`: `action` enum ALLOW + DENY + LOG-partial (LOG = always-allow + match-runs + `access_log_hint` metadata silent divergence-window per SPEC §1.1 amendment 5; folds into `allowed` counter per amendment 8 — NO separate `logged` counter — REFUTES BRAINSTORM 5-counter hypothesis); `policies` map<name, Policy> lexicographic-name walk; `audit_logging_options` silent-ignored (`[#not-implemented-hide:]` upstream); CEL three fields silent-ignored per amendment 6. Permission MVP **Large 11** (11 of 14): any, header, url_path, destination_ip, destination_port, destination_port_range, requested_server_name, and_rules, or_rules, not_rule, sourced_metadata (always-no-match runtime). Permission DEFERRED (3): metadata + matcher + uri_template (PARSE-REJECT). Principal MVP **Large 11** (11 of 14 per amendment 7 — Principal has 14 variants not 13): any, authenticated (three-case algorithm per amendment 12; nil-principal_name → `len(DownstreamPrincipal()) > 0`; non-nil → StringMatcher iteration over URI SAN/DNS SAN/Subject DN candidates; plaintext → FALSE), direct_remote_ip, remote_ip, header, url_path, and_ids, or_ids, not_id, sourced_metadata, filter_state (last two always-no-match). Principal DEFERRED (3): source_ip + metadata + `custom` (NEW 14th variant per amendment 7 — PARSE-REJECT). Per-route TPFC `RBACPerRoute{rbac: <RBAC>|nil}` — NEW **7th canonical** per-route pattern (absent-implies-disabled-OR-wholesale-override; structurally DISTINCT from 5th canonical's explicit-disabled-bool-in-oneof + 6th canonical's bare-message-via-TPFC); documented at ADR-0125 §(xii) amendment paragraph (authored at Task 10 per planner-time decision 14). Stat surface 60→**64 base names** (4 active counters: `allowed`, `denied`, `shadow_allowed`, `shadow_denied`); per-policy counter family lazy-allocated when `track_per_rule_stats: true` (template-form `<base>.policy.<policy_name>.<suffix>` — the `.policy.` infix segment was empirically refined at Task 8 reference-Envoy v1.37.2 scrape per ADR-0145, REFINES SPEC §13.2 stub which omitted the segment). Per-route stats **INDEPENDENT** per ADR-0145 + §5 (third row using stateful-override-with-INDEPENDENT-stats discipline after phase-11 ADR-0117 + phase-15 ADR-0139). HCM-rooted namespace `http.<HCM_stat_prefix>.rbac.<rules_stat_prefix>.<counter>` SN2-reuse RATIFIED at Task 8 empirical scrape — NO new SN10 rule. **TWO new framework primitives** — FIRST §9 row since phase 14 to introduce non-zero framework deltas + FIRST single phase to introduce TWO: (i) ADR-0144 — `DecoderFilterCallbacks.DownstreamPrincipal() []string` TLS-principal accessor (URI SAN + DNS SAN + Subject DN CN priority-ordered; cross-phase-reusable by future filters jwt_authn/ext_authz/oauth2/ext_proc); (ii) ADR-0142 — `internal/matcher/` NEW top-level package + `Matcher.New(tree, supportedActionTypes)` + `Matcher.Evaluate(MatchContext)` + `MatchContext` interface + PARSE-REJECT-for-unknown-TypeURL discipline (cross-phase-reusable). Deny-path wire shape `SendLocalReply(403, "RBAC: access denied" 19-byte ASCII, {content-length: 19, content-type: text/plain, date, server: envoy} lowercase wire-form, keep-alive)` per amendment 10 + §11.P5. Differential fixture `0018-http-rbac` (**8 scenarios**: allow-by-header-match; deny-no-match; allow-by-url-path; allow-by-destination-port; allow-by-direct-remote-ip; allow-by-tls-principal-via-mTLS-listener; per-route 7th-canonical disabled-via-absent; per-route wholesale-override with INDEPENDENT stat namespace + shadow). **19/19 differential fixtures green** at phase-done. **20th fuzzer** `FuzzRBACConfigParse` lands at `internal/filter/http/rbac/fuzz_test.go`. **7 anticipated ADRs landed** (ADR-0140..ADR-0146 anchored at impl-time per ADR-0044 + ADR-0125 §(xii) amendment paragraph at Task 10): ADR-0140 (Task 2 — package shape + DECODER-only HTTPFilter + 4-base-counter filterStats + lazy-per-policy via `NewCounterIfAbsent` + deny-path wire shape + boot-registration ordering), ADR-0141 (Task 2 — compiledConfig 7-field decomposition + dual-engine dispatch + UDPA-field-alias precedence + envoy-go defensive PGV-mirror + LOG-partial folds into `allowed` + CEL three-field silent-ignore + audit_logging_options silent-ignore), ADR-0142 (Task 3 — matcher-engine evaluator framework primitive at NEW `internal/matcher/` package), ADR-0143 (Tasks 4+5 — Permission + Principal Large 11+11 + AND/OR/NOT recursive combinators + 6 PARSE-REJECT + Principal_Authenticated three-case + SourcedMetadata/FilterState always-no-match), ADR-0144 (Task 6 — TLS-principal accessor framework primitive + HCM dispatch plumbing + canonical 3-cert-field scope per D11), ADR-0145 (Task 8 — 4-base-counter HCM-rooted SN2-reuse RATIFIED + lazy per-policy `.policy.` infix REFINED + per-route INDEPENDENT-stats + `NewCounterIfAbsent` flip), ADR-0146 (Task 9 — shadow-evaluation discipline + LOG-partial divergence-window + per-policy emission lazy + `response_code_details` divergence-window + shadow access-log deferred + BEHAVIOR_CONTRACT §13.4 anchoring). Plus **ADR-0147 unanticipated** (Task 13 follow-up per ADR-0044 ADR-on-impl escape-valve) — phase-03 TLS-layer `require_client_certificate=true` blanket-rejection LIFTED scoped to well-formed mTLS configs (validation_context.trusted_ca PEM provided) to enable fixture 0018 scenario 6 mTLS-principal exercise; maps onto stdlib `crypto/tls.RequireAndVerifyClientCert` + `ClientCAs` pool. Plus ADR-0125 §(xii) amendment paragraph (authored at Task 10 — NEW 7th canonical entry — ADR-0125's roster grows from 6 to 7). 6 phase-done gates verified at Task 15: Gate A (build+vet+lint clean exit 0); Gate B (race tests clean across all packages); Gate C (h2spec 53/53 PASS at ADR-0051 pin); Gate D (20 fuzzers green at 30s each); Gate E (19 differential fixtures 0000-0018 PASS); Gate F (BEHAVIOR_CONTRACT §13.1-§13.6 populated per 6-edit bundle). 16-task PLAN executed single-row per phase-13/14/15 LoC-borderline precedent; ADR-0045 surface-split release valve NOT invoked despite SPEC author's brainstorm-time hypothesis (Task count 16 well-under 25; production LoC ~2778 above SPEC-time ~1500 estimate but stayed coherent single-row). Per ADR-0106, §9 family-rows are flat top-level rows; phase 16 lands as row `16`. |
| 17 | http-filter-jwt-authn | 16 | done | 2026-05-14 | New `internal/filter/http/jwtauthn/` package (`jwtauthn.go` 845 LoC + `evaluator.go` 662 LoC + `provider.go` 707 LoC + `doc.go` 185 LoC = **2399 LoC production**) + new top-level `internal/jwks/` package (`jwks.go` 626 LoC + `doc.go` 77 LoC = **703 LoC production**) + new top-level `internal/jwt/` package (`jwt.go` 555 LoC + `doc.go` 98 LoC = **653 LoC production**) + new `test/helpers/jwksbackend/` test-helper (`jwksbackend.go` 104 LoC + `doc.go` 23 LoC = **127 LoC**) implementing `envoy.filters.http.jwt_authn` (Envoy v1.37.2 canonical JWT bearer-token validation filter, RS+ES algorithm family, Both-JWKS-source proto-faithful, Full-6 JwtRequirement variants, All-4 token extraction sources, Full-header-side post-validation side-effects, 8th-canonical per-route delegation via requirement_map) under the 07.1 framework. Shipped via the 15-task phase-17 PLAN; 8 ADRs anchored at impl-time (ADR-0148..ADR-0155 §Decision + §Consequences bodies) + ADR-0125 §(xiii) amendment landed at SPEC commit; **ADR-0044 escape-valve NOT triggered** (0 impl-time-unanticipated ADRs — the anticipated TLS-trust-store / clear_route_cache / sync-primitive surfaces did not materialize). Differential fixture `0019-http-jwt-authn` (8 scenarios) + 21st fuzzer `FuzzJwtAuthnConfigParse` green; §11.P7 SN2-reuse RATIFIED at the Task-13 fixture-0019 empirical scrape; two Task-13-discovered counter divergence-windows documented (`allowed` on bypass paths; `jwt_cache_*` structurally unreachable under MVP). TENTH §9 family-row (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11, csrf @ 12, buffer @ 13, compressor @ 14, bandwidth_limit @ 15, rbac @ 16). MVP envelope per SPEC §1 + §1.1 (12 amendments authored at SPEC time; all 16 §11 empirical pins resolved IN-SESSION against Envoy v1.37.2): **6 listener fields total per `[#next-free-field: 7]`** (REFINES BRAINSTORM 5-field framing per §1.1 amendment 1) — 5 consumed (`providers`, `rules`, `bypass_cors_preflight`, `requirement_map`, `strip_failure_response`) + 1 silent-ignored (`filter_state_rules` per §8 deferral 12 NEW); **13-of-21 JwtProvider fields consumed** (REFINES BRAINSTORM 13-of-17 framing per §1.1 amendment 2 — proto has 21 fields; 8 silent-ignored: 4 dynamic-metadata-family + jwt_cache_config + 3 v1.37.x claim-coverage `subjects`/`require_expiration`/`max_lifetime` per §1.1 amendment 3); `clock_skew_seconds` 60s default HONORED in MVP per §1.1 amendment 7 (BRAINSTORM-missed); 6 JwtRequirement oneof variants all consumed; 6-algorithm allow-list (RS256/384/512 + ES256/384/512; HS family + EdDSA + `none` + PS family deferred); All 4 extraction sources (default Authorization Bearer + access_token query param + configured from_headers + from_params + from_cookies); case-sensitive + URL-decoded for params with first-value-only multi-value handling per §11.P14 + case-sensitive + verbatim for cookies per §11.P15. Per-route TPFC: NEW **8th canonical pattern** (`oneof{disabled(bool) | requirement_name(string)}` — REFUTES BRAINSTORM `Empty`-form hypothesis per §1.1 amendment 5 + §11.P9; string-reference-delegation into listener-level requirement_map; dangling-reference RUNTIME-RESOLVED per §1.1 amendment 6 + §11.P12 — REFUTES BRAINSTORM PARSE-REJECT hypothesis; mirrors Envoy filter_config.cc `findPerRouteVerifier`; on miss emits 403 + "Failed JWT authentication: Wrong requirement_name: <name>"); ADR-0125 §(xiii) in-place amendment paragraph landed at SPEC commit (NEW 8th canonical; roster grows from 7 to 8). Deny-path wire shape per §1.1 amendments 8 + 11 + 12: **401 default + 403 specifically for `JwtAudienceNotAllowed`** per §11.P1 (REFINES BRAINSTORM "always 401" hypothesis); body = canonical jwt_verify_lib `getStatusString(status)` mapping (~70 strings; ~10 commonly-hit; e.g., `"Jwt is missing"` 14B, `"Jwt verification fails"` 22B, `"Audiences in Jwt are not allowed"` 32B); WWW-Authenticate = `Bearer realm="<original_uri>"` + conditional `, error="invalid_token"` for non-`JwtMissed` per §1.1 amendment 12 + §11.P2 (REFINES BRAINSTORM realm=issuer hypothesis — realm uses request URI); `strip_failure_response=true` strips both body AND WWW-Authenticate per §11.P3; **`response_code_details = "jwt_authn_access_denied{<reason>}"`** divergence-window NEW finding per §1.1 amendment 11 (envoy-go MVP DEFERS field emission per phase-04 HCM scope; mirrors phase-16 ADR-0146 + §8.12 + joint closure with phase-16 forward-pointer item 8); RFC 6750-conformant Bearer challenge (FIRST §9 row). **TWO new framework primitives** (SECOND consecutive two-primitive phase after phase 16): (i) HTTP-outbound JWKS fetcher at NEW `internal/jwks/` package (location LOCKED at SPEC time per §3.1 — JWKS-specific NOT generic httpclient; async fetch + per-thread cache + scheduled refresh **5s before TTL** per §11.P5 (`RefetchBeforeExpiredSec=5s`; default 10-min cache per `DefaultCacheExpirationSec=600s`) + **FIXED-INTERVAL** failed-refetch retry per §11.P4 (`DefaultRefetchAfterFailedSec=1s`; configurable via `JwksAsyncFetch.failed_refetch_duration`) — REFUTES BRAINSTORM exponential-backoff hypothesis; fast_listener mode controls blocking-vs-non-blocking initial fetch; cross-phase-reusable for future ext_authz HTTP-mode + oauth2 token-endpoint) anchored at ADR-0150; (ii) JWS/JWT verifier at NEW `internal/jwt/` package (`Parse` + `Token.VerifySignature` with RS+ES allow-list + `Token.ValidateClaims` with clock-skew tolerance + `Token.PayloadClaim` dot-notation extractor; ~20 canonical error sentinels mirroring jwt_verify_lib; pure-Go stdlib `crypto/rsa`+`crypto/ecdsa`+`encoding/base64.RawURLEncoding`; cross-phase-reusable) anchored at ADR-0151. **Stat surface 64 → 71 names** (REFINES BRAINSTORM "~72 names" + REFUTES per-provider scaling hypothesis per §1.1 amendment 9 + §11.P6): 7 base counters per HCM stat_prefix (`allowed` + `cors_preflight_bypassed` + `denied` + `jwks_fetch_success` + `jwks_fetch_failed` + `jwt_cache_hit` + `jwt_cache_miss`); **NO per-provider scaling** (REFUTES BRAINSTORM 8-per-provider hypothesis — Envoy emits filter-wide counters at HCM stat_prefix scope; multiple providers contribute to same `allowed`/`denied`); **NO gauges, NO histograms** (REFUTES BRAINSTORM `jwks_fetch_in_progress` gauge hypothesis); 5 of 7 actively emitted under MVP (`jwt_cache_hit`/`jwt_cache_miss` structurally unreachable per §8 deferral 8 — gated on `jwt_cache_config` deferral); canonical naming `cors_preflight_bypassed` NOT `bypassed_cors_preflight` per §1.1 amendment 10; HCM-rooted SN2-reuse namespace `http.<HCM_stat_prefix>.jwt_authn.<counter>` (RATIFIED-PENDING-IMPL-TIME-EMPIRICAL-SCRAPE at Task 8 per phase-16 §10 lesson (c); SOLE Task-8 closure pin in phase-17). Per-route stats **SHARED** with listener-level (mirrors phase-12 csrf + phase-13 buffer + phase-14 compressor SHARED; DIVERGES from phase-11/15/16 INDEPENDENT; pure-delegation per-route spawns no new policy-evaluation state). Differential fixture `0019-http-jwt-authn` (8 scenarios: valid-RS256-RemoteJwks, valid-ES256-LocalJwks, missing-token-deny, expired-token-deny, bad-signature-deny, bypass-cors-preflight, per-route 8th-canonical `requirement_name` delegation, per-route 8th-canonical `disabled: true`; optional 9th/10th scenarios deferred to impl-time: forward_payload_header + claim_to_headers backend-arrival + requires_any multi-provider OR). NEW test-helper `test/helpers/jwksbackend/` (in-process HTTP JWKS server; spawn-per-fixture lifecycle). 21st fuzzer `FuzzJwtAuthnConfigParse`. **8 anticipated ADRs** (LARGEST §9-row roster to date alongside phase-16's 7+1=8): ADR-0148 (package shape + DECODER-only + 7-counter filterStats + boot-registration alphabetical-after-header_mutation + deny-path wire shape) + ADR-0149 (compiledConfig 5-of-6 outer + 13-of-21 JwtProvider + 6-variant JwtRequirement evaluator + algorithm allow-list + side-effect emit-order + listener-level rules dispatch + `requires` honored proto-faithful per §1.1 amendment 4 — REFUTES BRAINSTORM PARSE-REJECT-deprecated hypothesis + clock_skew_seconds HONORED per §1.1 amendment 7) + ADR-0150 (HTTP-outbound JWKS fetcher framework primitive at `internal/jwks/` with fixed-interval failed-refetch + 5s-before-TTL refresh) + ADR-0151 (JWT verifier framework primitive at `internal/jwt/` with RS+ES allow-list + dot-notation claim extractor + array-claim rejection per §11.P10) + ADR-0152 (token extraction all 4 sources + iteration order + first-match-wins + case-sensitivity + URL-decode + first-value-only multi-value) + ADR-0153 (per-route 8th canonical `oneof{disabled(bool) | requirement_name(string)}` + runtime-resolve + SHARED-stats + ADR-0125 §(xiii) amendment paragraph) + ADR-0154 (stat surface 7-counter filter-wide + SN2-reuse + SHARED per-route + `cors_preflight_bypassed` canonical naming) + ADR-0155 (deny-path 401-or-403 dispatch + canonical jwt_verify_lib body + WWW-Authenticate Bearer challenge with conditional error=invalid_token + strip_failure_response strips both + response_code_details divergence-window). Plus ADR-0125 §(xiii) amendment paragraph (landed at SPEC commit). ADR-0044 escape-valve held in reserve for ~1 impl-time-unanticipated ADR (phase-13 ADR-0127-v2 + phase-14 ADR-0134 + phase-16 ADR-0147 precedent; likely surfaces: TLS-trust-store coupling for mTLS-to-JWKS endpoint OR HCM framework gap for clear_route_cache interaction OR sync-primitive lift for RemoteJwks-fetch-during-request callback path). **ADR-0045 split decision: SINGLE-ROW, NOT INVOKED** (LoC estimate refined post-§11 to ~1800-2200 LoC production; framework primitives scope narrowed by §11-pin resolutions; mirrors phase-13/14/15/16 single-row-borderline-LoC precedent). Per ADR-0106, §9 family-rows are flat top-level rows; phase 17 lands as row `17`, NOT as a sub-phase of any §9 parent. |
| 18 | http-filter-ext-authz | 17 | done | 18.1, 18.2 | New `internal/filter/http/extauthz/` package implementing `envoy.filters.http.ext_authz` (Envoy v1.37.2 canonical external-authorization filter; decode-side gate delegating the allow/deny decision to an external service) under the 07.1 framework. **SPLIT into 18.1 + 18.2 at the phase-18 SPEC commit per ADR-0045 / ADR-0164** — the §10 empirical-pin resolution (28-field `ExtAuthz` proto surface; two structurally distinct transports; brand-new `internal/grpcclient/` gRPC infrastructure; ~2400–3600 LoC) CONFIRMED the BRAINSTORM's HIGH split-readiness leaning; the structurally clean cut is by service-mode. ELEVENTH §9 family-row (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11, csrf @ 12, buffer @ 13, compressor @ 14, bandwidth_limit @ 15, rbac @ 16, jwt_authn @ 17). MVP envelope per BRAINSTORM §1.1 (6-question dialogue): **Both service modes proto-faithful** (`grpc_service` + `http_service` — the dominant ADR-0045 split-readiness driver); **request body included** proto-faithful (`with_request_body{max_request_bytes, allow_partial_message, pack_as_bytes}` reusing the phase-13 ADR-0128 decode-side body-buffering primitive); **Full gRPC AttributeContext population** (source/destination Peers incl. `principal` via the phase-16 ADR-0144 `DownstreamPrincipal()` reuse + `certificate`; full `request.http`; `tls_session.sni`; `context_extensions`); **Full header-side mutation** (request-side `allowed_headers`/`headers_to_add`; allow-path upstream injection via `OkHttpResponse`/`allowed_upstream_headers`; deny-path downstream copying via `DeniedHttpResponse`/`allowed_client_headers`); error-posture fields (`failure_mode_allow`, `failure_mode_allow_header_add`, `status_on_error`, `validate_mutations`) consumed proto-faithful. Per-route TPFC: **5th canonical REUSE** (`ExtAuthzPerRoute{oneof{disabled(bool) | check_settings(CheckSettings)}}` maps onto the existing buffer/compressor disabled-OR-narrower-override sum-type; SHARED stats) — **FIRST §9 row to REUSE an ADR-0125 canonical rather than add one; NO ADR-0125 amendment paragraph** (breaks the phase-13..17 per-phase-roster-growth streak). **TWO new framework primitives** (THIRD consecutive two-primitive §9 row after rbac + jwt_authn): (i) gRPC-client outbound primitive at NEW `internal/grpcclient/` (envoy-go's FIRST gRPC infrastructure of any kind — dial + connection-management + the `envoy.service.auth.v3.Authorization/Check` stub + error-classification; cross-phase-reusable for ext_proc + global_ratelimit) anchored at ADR-0158; (ii) HTTP-outbound auth-check composing against the phase-17 `internal/jwks/Fetcher` outbound-HTTP structure (SPEC author decides generalize-to-`internal/httpclient/` vs ext_authz-local-client) anchored at ADR-0159. Plus TWO framework REUSES — phase-13 ADR-0128 decode-side body-buffering + phase-16 ADR-0144 `DownstreamPrincipal()`. Deny-path wire shape: `SendLocalReply` with status/body/headers from the auth response (gRPC `DeniedHttpResponse` / HTTP-mode response) or `status_on_error` — FIRST §9 row whose deny-path status code is NOT fixed by the filter. Stat surface resolved at the SPEC §5.P6 empirical scrape to **71 → 77 names** (6 filter counters `ok`/`denied`/`error`/`disabled`/`failure_mode_allowed`/`invalid` under `http.<HCM_stat_prefix>.ext_authz.*`; HCM-rooted SN2-reuse; `disabled` STRUCTURALLY UNREACHABLE under MVP — increments only via the deferred runtime `filter_enabled` gate; the brainstorm `cx_http_*` + `disabled=on-per-route` hypotheses REFUTED). Differential surface SPLIT with the phase: fixture `0020-http-ext-authz-http` (18.1, ~7 HTTP-mode scenarios, 1 NEW test-helper — in-process HTTP auth server, 22nd fuzzer `FuzzExtAuthzConfigParse`) + fixture `0021-http-ext-authz-grpc` (18.2, ~6–8 gRPC-mode scenarios, 1 NEW test-helper — in-process gRPC `Authorization/Check` auth server, 23rd fuzzer). **9 ADRs** anchored (ADR-0156..ADR-0164; ADR-0156..ADR-0163 §Context-drafted at the SPEC commit, §Decision + §Consequences at IMPL per ADR-0044; **ADR-0164** = the ADR-0045 split-application ADR, landed IN FULL at the SPEC commit per the phase-08 ADR-0084 precedent; SPEC-time ADR-count revision 8 anticipated → 9 landed per ADR-0044; **NO ADR-0125 amendment** — the FIRST §9 row to REUSE an existing ADR-0125 canonical, ADR-0163 records the explicit no-amendment 5th-canonical-REUSE decision). Per ADR-0106, §9 family-rows are flat top-level rows; phase 18 lands as row `18` + sub-phase rows `18.1` / `18.2` per the ADR-0045 split invoked at the SPEC commit (BRAINSTORM §1.4 flagged HIGH split-readiness — the SPEC §5 empirical-pin resolution CONFIRMED it). |
| 18.1 | ext-authz-http | 17 | done | 2026-05-15 | First sub-phase of the phase-18 ADR-0045 split (`18` parent). Lands `envoy.filters.http.ext_authz` in **HTTP service mode** — the foundational filter. Shipped via the **15-task phase-18.1 PLAN** (Tasks 1–14 + Task 15 REVIEW). Production: `internal/filter/http/extauthz/` package (`extauthz.go` 1087 LoC + `check.go` 411 LoC + `attributes.go` 435 LoC + `doc.go` 192 LoC = **2125 LoC production**) + new `test/helpers/extauthzhttp/` test-helper (`extauthzhttp.go` 190 LoC + `doc.go` 27 LoC = **217 LoC**). **DECODER-only** `HTTPFilter` (`Encoder: nil`; 5th §9 row to ship pure decode-side, mirrors phase-12 csrf + phase-13 buffer + phase-14 compressor + phase-16 rbac). Key impl decisions: dual-mode `compiledConfig` envelope (the `services`-oneof dispatch is defined here; `grpc_service` PARSE-REJECTs in 18.1 with an envoy-go-strict error, activated by 18.2); HTTP-outbound auth-check (ADR-0159 disposition (b) — thin ext_authz-local `httpAuthClient` in `check.go`, NOT a generalized `internal/httpclient/`; ZERO retry per SPEC §12.2; composes-against the phase-17 `internal/jwks/Fetcher` outbound-HTTP structure); request-side header filtering via top-level `ExtAuthz.allowed_headers`/`disallowed_headers` (`ListStringMatcher` five-mode: exact/prefix/suffix/contains/`ignore_case`/`safe_regex`; `custom` PARSE-REJECT) + `AuthorizationRequest.headers_to_add` + `path_prefix`; deprecated `AuthorizationRequest.allowed_headers` (#1) HONORED-IF-PRESENT for backward-compat (ADR-0160 D6); HTTP-mode bidirectional header-mutation (`AuthorizationResponse.{allowed_upstream_headers, allowed_upstream_headers_to_append, allowed_client_headers}` — ADR-0161 HTTP-mode portion) + `validate_mutations` gating → `invalid` counter; `with_request_body` via ADR-0128 phase-13 decode-side body-buffering reuse (ADR-0162; `allow_partial_message:false` over-limit → `SendLocalReply(413, …)` + `connection: close`; auth NOT called on over-limit); per-route **5th-canonical REUSE** + SHARED-stats (ADR-0163 — FIRST §9 row to REUSE an ADR-0125 canonical; NO ADR-0125 amendment; `ExtAuthzPerRoute{oneof{disabled:true | check_settings(CheckSettings)}}`; PGV `const: true` on `disabled` + PGV-required `override` oneof); 6-counter filter stat surface (`ok`/`denied`/`error`/`disabled`/`failure_mode_allowed`/`invalid` under `http.<HCM_stat_prefix>.ext_authz.*`; HCM-rooted SN2-reuse; `disabled` STRUCTURALLY UNREACHABLE under MVP; stat-table 71 → 77; §18.P6/§18.P7 RATIFIED-PENDING closures confirmed at Task 8 empirical scrape); error-posture fields (`failure_mode_allow`/`failure_mode_allow_header_add`/`status_on_error`/`validate_mutations`) consumed proto-faithful (ADR-0157); async-resume outbound-call leg + per-request cancellable `context.Context` + `OnDestroy` cancel (phase-09 primitive reuse); boot-registration (14th extension-registry entry, alphabetical between `envoygotest` and `fault`); deny-path `SendLocalReply` with status/body/headers from auth response (ADR-0156 — FIRST §9 row whose deny-path status code is NOT fixed by the filter; §18.P11 deny-path header-set ordering RATIFIED at Task 13). Differential fixture `0020-http-ext-authz-http` (**7 HTTP-mode scenarios**: allow / deny→verbatim-auth-body / error→`status_on_error` / `failure_mode_allow` / `with_request_body` / per-route `disabled:true` / per-route `check_settings`) + 1 NEW test-helper (in-process HTTP auth server `test/helpers/extauthzhttp/`) + the 22nd fuzzer `FuzzExtAuthzConfigParse`; **21/21 differential fixtures 0000–0020 green** at phase-done. **7 anticipated ADRs LANDED** (ADR-0156/0157/0159/0160(HTTP-mode portion)/0161(HTTP-mode portion)/0162/0163; §Decision + §Consequences bodies authored at impl-time Tasks 2/2/3/4/5/6/7 per ADR-0044 ADR-on-impl convention; §Context drafts landed at SPEC commit `308e9b6`). **ADR-0044 escape-valve NOT triggered** (0 impl-time-unanticipated ADRs — the anticipated async-resume race-guard surface D4 → `mu`/`done` guard + `context.WithCancel` sufficed; ADR-0165 NOT authored; next-free ADR is ADR-0165 for phase 18.2). **NO ADR-0125 amendment paragraph** — confirmed at phase-done via `grep -cE '^\*\*(xiv)\*\*' docs/envoy-go/DECISIONS.md = 0`. BEHAVIOR_CONTRACT.md 6-edit bundle applied at Task 14 (§13.1 ext_authz subsection + §13.2 stat table 71→77 + §13.3 equivalence-matrix row + §13.4 Phase 18.1 forward-pointer notes + §13.6 per-route canonical cross-reference + §13.7 HTTP outbound auth-check framework note). SPEC at `docs/envoy-go/phases/18.1-ext-authz-http/SPEC.md`; PLAN at `docs/envoy-go/phases/18.1-ext-authz-http/PLAN.md`. |
| 18.2 | ext-authz-grpc | 18.1 | done | 2026-05-15 | Second sub-phase of the phase-18 ADR-0045 split (`18` parent). Lands `envoy.filters.http.ext_authz` in **gRPC service mode** against the 18.1 package surface — the secondary-transport landing: the NEW `internal/grpcclient/` gRPC-client framework primitive (ADR-0158 — envoy-go's FIRST gRPC infrastructure of any kind; thin generic `Dialer` (cluster-name → `*grpc.ClientConn` via `grpc.WithContextDialer((*cluster.Cluster).Dial)` + `WithTransportCredentials(insecure)` — TLS terminates at the cluster-manager layer per the §11.P13 in-session SPEC scrape) + ext_authz-typed `AuthClient` wrapper (`envoy.service.auth.v3.Authorization/Check` stub from go-control-plane v1.32.4 — no codegen); cross-phase-reusable for ext_proc + global_ratelimit; one `*grpc.ClientConn` per (cluster_name, compiledConfig) pair created at config-load time and shared across all per-stream Check calls), the `grpc_service` arm activated in the `compiledConfig` dispatch (ADR-0157 §Decision amended at 18.2 IMPL; `core.GrpcService.GoogleGrpc` PARSE-REJECTs envoy-go-strict; `initial_metadata` + `retry_policy` SILENT-IGNORED), the gRPC-mode `AttributeContext`/`CheckRequest` builder (ADR-0160 gRPC-mode portion — source/destination Peers; `source.principal` via the phase-16 ADR-0144 `DownstreamPrincipal()` reuse; `source.certificate` gated by `include_peer_certificate`; `tls_session.sni` gated by `include_tls_session`; `destination.principal` populates AUTOMATICALLY from the listener TLS cert per §11.P4 in-session SCRAPE refinement — NOT gated; `request.http` per parent §5.P4 + §11.P4 in-session — pseudo-headers lowercased; HCM injects `x-envoy-auth-partial-body` + `x-forwarded-proto` + `x-request-id` into the map; `request.time` as a `Timestamp`; `context_extensions` merge listener+per-route; `encode_raw_headers` `headers`-vs-`header_map` discipline), the `CheckResponse` → disposition mapping incl. `OkHttpResponse`/`DeniedHttpResponse` header mutation (ADR-0161 gRPC-mode portion — DeniedHttpResponse headers VERBATIM unlike HTTP-mode's matcher-filtered headers; OkHttpResponse.{headers (append_action), headers_to_remove}; `OkHttpResponse.response_headers_to_add` DEFERRED — decode-side-only filter shape; `validate_mutations` gating identical to HTTP-mode). Differential fixture `0021-http-ext-authz-grpc` (**8 scenarios**: 7 mirroring 0020 + 1 gRPC-only `OkHttpResponse` upstream mutation; three-listener topology l_test_a/b/c per failure_mode_allow scoping) + 1 NEW test-helper `test/helpers/extauthzgrpc/` (FIRST in-process gRPC server in envoy-go's test tree; plaintext h2c on ephemeral port; scriptable per-`:path` CheckResponse) + 23rd fuzzer `FuzzCheckResponseMapping` (fuzz `*CheckResponse` proto-bytes → `mapGRPCResponse` → `checkDisposition`; the existing `FuzzExtAuthzConfigParse` corpus extends with `grpc_service` config variants — no separate config-parse fuzzer needed). The mode-agnostic `compiledConfig` envelope + `checkDisposition` + 6-counter `filterStats` + deny-path `SendLocalReply` + async-resume leg + per-route 5th-canonical REUSE + `with_request_body` ADR-0128 reuse all carry forward UNCHANGED from 18.1; `compiledConfig` struct field-shape stays field-final per ADR-0157 §Decision (gRPC-specific config — `include_*` gates, `encode_raw_headers`, `pack_as_bytes` — captured directly in the closure's lexical scope, NOT promoted to struct fields). File layout: extend existing `check.go` (+250–400 LoC: buildGRPCCheckFn + mapGRPCResponse + dispositions) + `attributes.go` (+200–350 LoC: buildAttributeContext + helpers); NO new `grpc_*.go` files. 18.2-landing ADRs: ADR-0158 (full §Decision + §Consequences) + ADR-0157 §Decision AMENDMENT (grpc_service activation) + ADR-0160/0161 gRPC-mode portions. **All 13 parent SPEC §5 empirical pins CLOSED RATIFIED at the 18.2 SPEC commit** — the §18.P4 `tls_session` portion + §18.P13 gRPC dial / TLS-to-auth-cluster plumbing were RATIFIED-PENDING-IMPL-TIME at the parent SPEC commit and closed in-session at the 18.2 SPEC scrape on 2026-05-15 (TLS-fronted downstream listener + TLS-fronted gRPC auth cluster + Go gRPC `Authorization/Check` server fronted with self-signed TLS; verbatim CheckRequest captured at SPEC §11). 18.2 IMPL has zero RATIFIED-PENDING pins. **ADR-0044 escape-valve held in reserve for ~0–1 impl-time-unanticipated ADRs** (the §11.P13 closure REMOVES the most-likely escape-valve surface — the cluster-manager coupling for `EnvoyGrpc` cluster-name resolution + TLS-to-auth-cluster plumbing was confirmed clean with no new TLS-layer lift needed; the §11.P4 in-session refinement of `destination.principal` automatic population is folded into ADR-0160 §Decision rather than a new ADR). 2 phase-18.1 forward-pointers CLOSED (gRPC arm activation + `context_extensions` consumption). The 18.2 phase-done commit closes BOTH row `18.2` AND the parent row `18` per parent SPEC §8 rollup discipline. SPEC at `docs/envoy-go/phases/18.2-ext-authz-grpc/SPEC.md`; PLAN at `docs/envoy-go/phases/18.2-ext-authz-grpc/PLAN.md` (14-task plan; Tasks 1–13 IMPL + Task 14 REVIEW). **Shipped via the 14-task phase-18.2 PLAN.** Production code: NEW `internal/grpcclient/` package (envoy-go's FIRST gRPC infrastructure of any kind — `Dialer` + `AuthClient` thin wrapper composing `google.golang.org/grpc` v1.70.0 + go-control-plane v1.32.4 `envoy.service.auth.v3.Authorization/Check` stub) + extensions to `internal/filter/http/extauthz/` (`buildGRPCCheckFn` + `buildAttributeContext` + `mapGRPCResponse` + `dispatchOutboundCheck` seeding) + 6 new methods on `envoyhttp.DecoderFilterCallbacks` (the cross-phase-reusable callback-surface extension — `DownstreamRemoteAddr` / `DownstreamLocalAddr` / `DownstreamTLSServerName` / `DownstreamTLSPeerCertDER` / `DownstreamProtocol` / `ListenerPrincipal`) + cluster-manager amendment (plaintext h2c upstream relaxation per ADR-0166). New test-helper `test/helpers/extauthzgrpc/` (FIRST in-process gRPC server in envoy-go's test tree; plaintext h2c on ephemeral port). Differential fixture `0021-http-ext-authz-grpc` (8 scenarios: 7 mirroring 0020 + 1 gRPC-only `OkHttpResponse` upstream mutation; three-listener topology l_test_a/b/c per failure_mode_allow scoping) + 23rd fuzzer `FuzzCheckResponseMapping`. **22/22 differential fixtures 0000–0021 green** at phase-done. **5 ADRs anchored at 18.2 IMPL** (ADR-0158 full §Decision + §Consequences — gRPC-client framework primitive; ADR-0157 §Decision AMENDMENT in-place at Task 3 — `grpc_service` arm activation; ADR-0160 gRPC-mode portion at Task 5; ADR-0161 gRPC-mode portion at Task 6; **ADR-0165** callback-surface extension at Task 4 — the ADR-0044 escape-valve fired at planner-time D3 + D12 per the SPEC §13.5/§6.5/§6.6 amendment; **ADR-0166** plaintext h2c upstream relaxation — unanticipated fixup-time amendment at Task 11.5 between Task 11 and Task 12). **ADR-0044 escape-valve fired TWICE** (once at PLAN time → ADR-0165; once at IMPL-fixup time → ADR-0166). NO ADR-0125 amendment (the 5th-canonical REUSE is mode-agnostic). BEHAVIOR_CONTRACT.md 8-patch bundle applied at Task 13 (§13.1 ext_authz subsection gRPC-mode expansion + §13.2 stat-table 77-count UNCHANGED clarification + §13.3 fixture 0021 equivalence-matrix row + §13.4 NEW `### Phase 18.2 forward-pointer notes` subsection + §13.5 `## HTTPFilterCallbacks` AMENDMENT documenting the 6 new methods + NEW top-level `## gRPC client framework primitive (per phase 18.2 ADR-0158)` umbrella). All 6 phase-done gates GREEN at this commit. **Closes BOTH row 18.2 AND parent row 18 at this commit** per parent SPEC §8 parent-rollup discipline. |
| 19 | http-filter-ext-proc | 18 | done | 19.1, 19.2 | New `internal/filter/http/extproc/` package implementing `envoy.filters.http.ext_proc` (Envoy v1.37.2 canonical external-processor filter; both-decode-and-encode-side filter delegating per-stage mutation decisions for both the request AND the response to an external service over a bidirectional gRPC stream or a JSON-transcoded HTTP POST per stage) under the 07.1 framework. **PARENT row CLOSED at 2026-05-16 — rollup-done AT THE SAME COMMIT as row 19.2 per parent SPEC §8 rollup discipline.** **SPLIT into 19.1 + 19.2 at the phase-19 SPEC commit per ADR-0045 / ADR-0176** — the §5 empirical-pin resolution + the SPEC-time scrape closure of §19.P11 + §19.P12 (BOTH conditional ADR-0174 + ADR-0175 REFUTED-AT-SPEC-TIME → both fire load-bearing rather than provisionally) ENLARGED the BRAINSTORM's HIGH split-readiness leaning; the structurally clean cut is by feature-class (19.1 = headers stages + bidi-stream primitive + JSON codec + ADR-0174 encode-side callback symmetry; 19.2 = body-stage activation + ADR-0175 encode-side body-buffering primitive). TWELFTH §9 family-row (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11, csrf @ 12, buffer @ 13, compressor @ 14, bandwidth_limit @ 15, rbac @ 16, jwt_authn @ 17, ext_authz @ 18 with its 18.1+18.2 split). MVP envelope per BRAINSTORM §1.1 (6-question dialogue): **Headers + BUFFERED body** processing mode (Q2 — STREAMED + BUFFERED_PARTIAL + FULL_DUPLEX_STREAMED PARSE-REJECT envoy-go-strict; trailers SKIP-only); **Both service modes proto-faithful** (Q3 — `grpc_service` bidi-stream + `http_service` JSON-transcoded headers-only per the proto's constraint at `ExtProcHttpService`); **Full mutation proto-faithful** (Q4 — `CommonResponse.{header_mutation, body_mutation, clear_route_cache, status}` CONTINUE/CONTINUE_AND_REPLACE + multi-stage `ImmediateResponse{status, headers, body, grpc_status, details}` + `mutation_rules` per-header gating); **Full mode-override proto-faithful** (Q5 — `allow_mode_override` + `allowed_override_modes` + mid-stream re-eval + `max_message_timeout` bounding `override_message_timeout` extensions); **per-route 5th-canonical REUSE** (Q6 — `ExtProcPerRoute{oneof{disabled(bool) | overrides(ExtProcOverrides)}}` maps onto the existing ADR-0125 5th canonical; SHARED stats) — **SECOND CONSECUTIVE §9 family-row (after phase 18) to REUSE rather than add; NO ADR-0125 amendment paragraph** (strengthens the ADR-0125 roster-not-monotonic lesson). **Framework story dominated by REUSE rather than NEW** — FIRST §9 row to demonstrate the load-bearing reusability of FIVE prior framework primitives: ADR-0158 `internal/grpcclient/Dialer` (FIRST cross-phase consumer outside phase-18.2 itself; phase 19 EXTENDS with ADR-0169 bidi-stream wrapper alongside the unary `*AuthClient`); ADR-0165 6 `DecoderFilterCallbacks` methods (FIRST cross-phase consumer; populate `request_attributes` envelope at request_headers stage); ADR-0128 decode-side body-buffering (reused for `request_body_mode = BUFFERED` at 19.2); ADR-0166 cluster-manager plaintext h2c upstream relaxation (reused for the fixture-0022 `c_ext_proc` test cluster); phase-09 async-resume primitive (reused on BOTH decode and encode side — FIRST encode-side async-resume consumer). **TWO NEW framework primitives + ONE NEW filter-local JSON codec + TWO CONFIRMED encode-side framework deltas** (ADR-0174 fires at 19.1 SPEC time per §5.P12 REFUTED — symmetric `EncoderFilterCallbacks` extension; ADR-0175 fires at 19.2 SPEC time per §5.P11 REFUTED — encode-side body-buffering primitive). Deny-path wire shape: multi-stage `ImmediateResponse` via `SendLocalReply` — FIRST §9 row whose deny-path can fire at request_headers / request_body / response_headers / response_body stages (body-stage variants land at 19.2). FIRST §9 row to ship BOTH `StreamDecoderFilter` AND `StreamEncoderFilter` participation in a single filter package (phase-14 compressor's encode-only is the only prior encode-side precedent). Stat surface hypothesized at the SPEC §5.P4 RATIFIED-PENDING-IMPL-TIME to **77 → ~86 names** (+9 counters: `streams_started` / `stream_msgs_sent` / `stream_msgs_received` / `spurious_msgs_received` / `streams_failed` / `streams_closed` / `failure_mode_allowed` / `override_message_timeout_received` / `override_message_timeout_ignored` under `http.<HCM_stat_prefix>.ext_proc.*`; HCM-rooted SN2-reuse). Differential surface SPLIT with the phase: fixture `0022-http-ext-proc-grpc` (19.1, ~6-8 headers-only scenarios, 1 NEW test-helper — FIRST in-tree bidi-stream gRPC `ExternalProcessor.Process` server, 24th fuzzer `FuzzExtProcConfigParse`) + fixture `0023-http-ext-proc-body` (19.2, ~4-6 body-stage scenarios, REUSES 19.1 test-helper, 25th fuzzer `FuzzProcessingResponseMapping`). **10 ADRs** anchored (ADR-0167..ADR-0176; ADR-0167..ADR-0175 §Context-drafted at the SPEC commit, §Decision + §Consequences at IMPL per ADR-0044; **ADR-0176** = the ADR-0045 split-application ADR, landed IN FULL at the SPEC commit per the phase-08 ADR-0084 + phase-18 ADR-0164 precedents; SPEC-time ADR-count revision 7-9 anticipated → 10 landed per ADR-0044 (the 2 brainstorm-CONDITIONAL ADR-0174/ADR-0175 both fire load-bearing per the SPEC-time scrape closure of §19.P11 + §19.P12); **NO ADR-0125 amendment** — SECOND CONSECUTIVE §9 row to REUSE the 5th canonical; ADR-0173 records the explicit no-amendment 5th-canonical-REUSE decision). Per ADR-0106, §9 family-rows are flat top-level rows; phase 19 lands as row `19` + sub-phase rows `19.1` / `19.2` per the ADR-0045 split invoked at the SPEC commit. |
| 19.1 | http-filter-ext-proc-headers | 18 | done | 2026-05-16 | First sub-phase of the phase-19 ADR-0045 split (`19` parent). **IMPL DONE at 2026-05-16**: 15-task PLAN-confirmed (Task 1 PROGRESS preamble + 17-precondition verification; Tasks 2-14 IMPL — 24th fuzzer `FuzzExtProcConfigParse` clean at 30s + BEHAVIOR_CONTRACT 8-edit bundle landed per SPEC §13 + DECISIONS final-state aligned + STATE/ROADMAP advance; Task 15 REVIEW per superpowers:requesting-code-review). Final 8-ADR roster anchored (ADR-0167..ADR-0174 §Decision + §Consequences bodies all landed at their Lands-in-Tasks per ADR-0044). **D12 hypothesis HELD** — NO impl-time-unanticipated ADR fired; ADR-0177 stays unconsumed (reserved for 19.2). Three RATIFIED-PENDING-IMPL-TIME pins closed at Task 13: §19.P4 RATIFIED-WITH-AMENDMENT (9 hypothesized counters all present in reference Envoy v1.37.2; 8 reference-only counters DEFERRED to 19.2+ activation); §19.P7 RATIFIED-BY-CONSTRUCTION (cache-on-first-use established by single resolution entry point); §19.P8 RATIFIED on the codec-options axis (snake_case + omit-zero + enum-as-string protojson options match reference; three envelope-content divergences DOCUMENTED-IN-PLACE as 19.2 surfaces per ADR-0170 §Consequences). Parent row 19 STAYS in-progress (closes at 19.2's phase-done AT THE SAME commit per parent SPEC §8 rollup discipline). Lands `envoy.filters.http.ext_proc` in **headers-stages-only mode** + the bidi-stream framework primitive + the JSON codec + the encode-side callback symmetry extension. Scope per parent SPEC §2: NEW `internal/filter/http/extproc/` package; `ExternalProcessor` proto parsing with `grpc_service`/`http_service` mutual-exclusion + body-mode PARSE-REJECT (BUFFERED activates at 19.2) + trailer-mode PARSE-REJECT (permanently) + STREAMED-only flag PARSE-REJECT (permanently); NEW `*ProcessorClient` bidi-stream wrapper at `internal/grpcclient/processor_client.go` EXTENDING ADR-0158 `*Dialer` (NEW typed wrapper alongside `*AuthClient` — FIRST cross-phase consumer of ADR-0158 outside phase-18.2 itself; bidi-stream Send/Recv/CloseSend lifecycle per HTTP transaction; one *grpc.ClientConn per (cluster_name, compiledConfig) pair) anchored at ADR-0169; NEW `ProcessingRequest`/`ProcessingResponse` JSON codec at `extproc/json.go` (filter-local for MVP — protojson defaults RATIFIED-PENDING-IMPL-TIME per §5.P8) anchored at ADR-0170; per-stage state machine + mode_override re-eval (header-response paths only per §5.P1 RATIFIED-AND-REFINED; `allow_mode_override` + `allowed_override_modes` validation + `max_message_timeout` bounding `override_message_timeout` per §5.P10) anchored at ADR-0171; CommonResponse header_mutation per direction per stage + `mutation_rules` per-header gating per §5.P3 + `clear_route_cache` + `route_cache_action` mutual-exclusion + request-headers-stage-only-application per §5.P5 + multi-stage ImmediateResponse with `*HeaderMutation` + gRPC-downstream-detection via content-type sniff for `grpc_status` translation per §5.P2 anchored at ADR-0172 (header-mode portion); per-route 5th-canonical REUSE (`ExtProcPerRoute.override` oneof PGV-required; `disabled` PGV `const: true`; `overrides` `*ExtProcOverrides`; MVP-CONSUMED `processing_mode` + `grpc_service`; `async_mode`/`request_attributes`/`response_attributes`/`metadata_options`/`grpc_initial_metadata` silent-ignored/deferred per §5.P6 RATIFIED (the per-route override 7-field roster INCLUDES `request_attributes`/`response_attributes` at #3/#4 flagged `[#not-implemented-hide:]` — distinct from the top-level `ExternalProcessor.request_attributes`/`response_attributes` at #5/#6 which ARE MVP-consumed); cache-on-first-use per §5.P7 RATIFIED-PENDING-IMPL-TIME) + SHARED-stats + 9-counter filter stat surface (`streams_started`/`stream_msgs_sent`/`stream_msgs_received`/`spurious_msgs_received`/`streams_failed`/`streams_closed`/`failure_mode_allowed`/`override_message_timeout_received`/`override_message_timeout_ignored` under `http.<HCM_stat_prefix>.ext_proc.*`; HCM-rooted SN2-reuse; RATIFIED-PENDING-IMPL-TIME at §5.P4) anchored at ADR-0173; NEW symmetric `EncoderFilterCallbacks` extension — 6 new methods (`DownstreamRemoteAddr`/`DownstreamLocalAddr`/`DownstreamTLSServerName`/`DownstreamTLSPeerCertDER`/`DownstreamProtocol`/`ListenerPrincipal`) mirroring ADR-0165's decode-side surface; reuses ADR-0165's chain-field plumbing (SET-once at HCM dispatch BEFORE either decode or encode dispatch; NO new chain primitives); REFUTED at §5.P12 SPEC-time scrape → fires load-bearing — anchored at ADR-0174; BOTH-DECODE-AND-ENCODE filter shape + 9-counter filterStats + multi-stage `SendLocalReply` mechanism (request_headers + response_headers stages; body-stage variants at 19.2; FIRST §9 row to emit `SendLocalReply` from the encode side at the response_headers stage) anchored at ADR-0167; dual-mode `compiledConfig` envelope (with body-mode 19.1 PARSE-REJECT) anchored at ADR-0168. Differential fixture `0022-http-ext-proc-grpc` (~6-8 headers-only scenarios: gRPC allow + header set, gRPC immediate_response at request_headers, gRPC response_headers mutation, gRPC mode_override mid-stream, gRPC failure_mode_allow, HTTP allow headers-only, per-route disabled, per-route processing_mode override; three-listener topology l_test_a/b/c) + 1 NEW test-helper `test/helpers/extprocgrpc/` (FIRST in-tree bidi-stream gRPC `ExternalProcessor.Process` server; extends the phase-18.2 unary `extauthzgrpc` pattern to bidi) + 24th fuzzer `FuzzExtProcConfigParse`. **19.1-landing ADRs**: ADR-0167 + ADR-0168 + ADR-0169 + ADR-0170 + ADR-0171 (header-mode portion) + ADR-0172 (header_mutation + ImmediateResponse-at-headers portion) + ADR-0173 + ADR-0174; ADR-0176 already at the parent SPEC commit. 19.1 IMPL closes the 3 RATIFIED-PENDING pins at the fixture-harness empirical scrape (§5.P4 stat surface + §5.P7 cache-on-first-use per-route + §5.P8 JSON codec wire shape). ADR-0044 escape-valve held in reserve for ~0-2 impl-time-unanticipated ADRs (the SPEC-time scrape closure of §19.P11/§19.P12 REMOVES the most-likely escape-valve surfaces); most-likely IMPL-time surfaces: bidi-stream cancellation discipline + HCM stream-lifecycle interaction; JSON codec wire-shape divergence from `protojson` defaults if §5.P8 scrape surfaces deltas; route_cache_action interaction with phase-10/17 `cb.ClearRouteCache()`. SPEC at `docs/envoy-go/phases/19.1-http-filter-ext-proc-headers/SPEC.md` (drafted at this SPEC commit). |
| 19.2 | http-filter-ext-proc-body | 19.1 | done | 2026-05-16 | Second sub-phase of the phase-19 ADR-0045 split (`19` parent). **IMPL DONE at 2026-05-16**: 11-task PLAN-confirmed (Task 1 PROGRESS preamble + 12-precondition verification; Tasks 2-10 IMPL — ADR-0175 encode-side `BufferEncodedBody` framework primitive + ADR-0168/0171/0172 §Decision AMENDMENTs in-place at Tasks 3/4/6 per ADR-0044 + body-stage attribute envelope builders + extproc.go body-stage integration + race tests + differential fixture 0023 + 25th fuzzer `FuzzProcessingResponseMapping` clean at 30s + BEHAVIOR_CONTRACT 7-edit bundle landed per SPEC §13 + DECISIONS final-state aligned + STATE/ROADMAP advance — both row 19.2 AND parent row 19 flipped to done AT THE SAME COMMIT; Task 11 REVIEW per superpowers:requesting-code-review). Final 4 §Decision-touchpoints landed (ADR-0175 §Decision + §Consequences full body at Task 2 + rework `8ac3939`; ADR-0168 §Decision AMENDMENT at Task 3 + §Consequences refresh at Task 7; ADR-0171 §Decision AMENDMENT + §Consequences at Task 4 + rework `9fb5137`; ADR-0172 §Decision AMENDMENT + §Consequences at Task 6). **D10 hypothesis HELD** — NO impl-time-unanticipated ADR fired; ADR-0177 stays unconsumed (reserved for future-phase surfaces). Two empirical-pin AMENDMENT scope-reductions surfaced at the fixture-0023 scrape: (I) reference Envoy v1.37.2 body-stage `body_mutation` returns 500 → scenarios (b)+(d) re-scoped to OBSERVABILITY-only; (II) envoy-go HCM encode-side `SendLocalReply` framework gap → scenario (c) re-routed from response_body → request_body. NEITHER consumes an ADR; both recorded as forward-pointers at BEHAVIOR_CONTRACT.md `### Phase 19.2 forward-pointer notes` for future-phase closure. Per-message timer enforcement LIFTED to behavioral via `context.WithTimeout` cancel-and-rebuild (planner-time D4); D5 attribute-roster crystallization HOLDS (body-stage CEL roster MIRRORS header-stage roster + `request.size`/`response.size` body-stage-natural additions). Stat surface STAYS at 86 names (9 ext_proc counters UNCHANGED; 8 reference-only counters DEFERRED to 19.3+). Lands `envoy.filters.http.ext_proc` body-stage participation against the 19.1 package surface — the body-mode extension landing. Scope per parent SPEC §2: NEW encode-side body-buffering framework primitive (analogous to phase-13 ADR-0128 decode-side; buffers response body across `EncodeData` calls until end_stream; exposes the buffered bytes via a new `EncoderFilterCallbacks.BufferEncodedBody() []byte` method; releases the buffered-and-possibly-mutated body to the downstream wire-write path) anchored at ADR-0175; body-mode activation in `compiledConfig` (ADR-0168 §Decision AMENDMENT — replaces the 19.1 PARSE-REJECT for `request_body_mode = BUFFERED` and `response_body_mode = BUFFERED` with the body-stage dispatch; STREAMED + BUFFERED_PARTIAL + FULL_DUPLEX_STREAMED continue PARSE-REJECT envoy-go-strict permanently); 4-stage state-machine extension + per-message timer behavioral (ADR-0171 §Decision AMENDMENT); body-stage dispatch + `body_mutation` discipline (ADR-0172 §Decision AMENDMENT — request_body stage uses ADR-0128 decode-side body-buffering reuse; response_body stage uses NEW ADR-0175 encode-side body-buffering; `CommonResponse.body_mutation` oneof body/clear; `streamed_response` arm PARSE-REJECT); `CommonResponse.status = CONTINUE_AND_REPLACE` handling (combined header+body replacement at header stages with body-mode BUFFERED; TREATED AS CONTINUE at body stages); multi-stage `ImmediateResponse` extension to body stages (request_body + response_body in addition to header stages from 19.1). Differential fixture `0023-http-ext-proc-body` (6 scenarios per SPEC §7.2 — 7 sub-scenarios counting f-a + f-b; REUSES 19.1 test-helper `test/helpers/extprocgrpc/` with new scripted response sequences) + 25th fuzzer `FuzzProcessingResponseMapping` (clean at 30s; ~2.36M execs / 329 interesting inputs). **19.2-landing ADRs**: ADR-0175 + ADR-0168 §Decision AMENDMENT + ADR-0171 §Decision AMENDMENT + ADR-0172 §Decision AMENDMENT. **All 6 phase-done gates GREEN at this commit.** SPEC at `docs/envoy-go/phases/19.2-http-filter-ext-proc-body/SPEC.md`; PLAN at `docs/envoy-go/phases/19.2-http-filter-ext-proc-body/PLAN.md`; PROGRESS at `docs/envoy-go/phases/19.2-http-filter-ext-proc-body/PROGRESS.md` (11-task ledger). The SPEC scope-pins the body-mode envelope (BUFFERED only for gRPC-service-mode; STREAMED + BUFFERED_PARTIAL + FULL_DUPLEX_STREAMED + HTTP-service-mode body PARSE-REJECT permanently); anchors ADR-0175 §Decision intent (encode-side `BufferEncodedBody` framework primitive — analog of phase-13 ADR-0128); scopes ADR-0168 / ADR-0171 / ADR-0172 §Decision AMENDMENTS for in-place edit at 19.2 IMPL per ADR-0044; scopes differential fixture `0023-http-ext-proc-body` (6 scenarios per §7.2); scopes 25th fuzzer `FuzzProcessingResponseMapping`; carries forward 17 of 18 19.1 §8 deferred items (item 1 body-mode activation closes for gRPC-service-mode; HTTP-service-mode body PARSE-REJECT continues); SETTLES 19.1 §12 deferred decision #7 (`CONTINUE_AND_REPLACE` handling); holds at 9 counters / 86 stat names (8 reference-Envoy counters DEFERRED to 19.3+); defers all 3 ADR-0170 §Consequences envelope-content divergences; reassigns Carryforward M TLS-fixture closure to the TLS-fixture phase. **NO new ADR numbers consumed at SPEC** (D12 / D13 hypothesis HELD at 19.2 SPEC time — ADR-0175 was reserved at the parent SPEC commit; ADR-0177 stays unconsumed). The next session is the 19.2 PLAN-authoring session (lifecycle-state 2 → 3) per `superpowers:writing-plans`. Lands `envoy.filters.http.ext_proc` body-stage participation against the 19.1 package surface — the body-mode extension landing. Scope per parent SPEC §2: NEW encode-side body-buffering framework primitive (analogous to phase-13 ADR-0128 decode-side; buffers response body across `EncodeData` calls until end_stream; exposes the buffered bytes via a new `EncoderFilterCallbacks` method; releases the buffered-and-possibly-mutated body to the downstream wire-write path) — REFUTED at parent §5.P11 SPEC-time scrape (`internal/filter/http/chain.go:403` comment "encode-side StopIterationAndBuffer is park-only" + reads of `RunEncodeData` confirm the encode-side has no replay buffer; phase-14 `OverwriteBody` ADR-0131 is per-call replacement, NOT buffer-and-hold) → fires load-bearing — anchored at ADR-0175; body-mode activation in `compiledConfig` (ADR-0168 §Decision AMENDMENT — replaces the 19.1 PARSE-REJECT for `request_body_mode = BUFFERED` and `response_body_mode = BUFFERED` with the body-stage dispatch; STREAMED + BUFFERED_PARTIAL + FULL_DUPLEX_STREAMED continue PARSE-REJECT envoy-go-strict permanently); body-stage dispatch + body_mutation discipline (ADR-0172 body-mode AMENDMENT — request_body stage uses ADR-0128 decode-side body-buffering reuse; response_body stage uses NEW ADR-0175 encode-side body-buffering; `CommonResponse.body_mutation` oneof body/clear; `streamed_response` arm PARSE-REJECT since STREAMED is out of envelope); `CommonResponse.status = CONTINUE_AND_REPLACE` handling (supersedes both header_mutation + body_mutation with combined-replacement semantics; honored only at header stages with body-mode BUFFERED); multi-stage ImmediateResponse extension to body stages (request_body + response_body stages in addition to header stages from 19.1). Differential fixture `0023-http-ext-proc-body` (~4-6 body-stage scenarios: gRPC body BUFFERED + body_mutation replace, gRPC body BUFFERED + clear_body, gRPC CONTINUE_AND_REPLACE, gRPC multi-stage immediate_response at response_body, gRPC response_body mutation; REUSES 19.1 test-helper `test/helpers/extprocgrpc/` with new scripted response sequences) + 25th fuzzer `FuzzProcessingResponseMapping`. **19.2-landing ADRs**: ADR-0175 + ADR-0168 §Decision AMENDMENT + ADR-0171 body-mode AMENDMENT + ADR-0172 body-mode AMENDMENT. ADR-0044 escape-valve in reserve for ~0-1 impl-time-unanticipated ADRs; most-likely surfaces: buffered-body-release-vs-stream-reset interaction with the framework's encode-chain primitive; mutation_rules application to body bytes (unlikely per the header-specific proto doc). The 19.2 phase-done commit closes BOTH row `19.2` AND the parent row `19` per parent SPEC §8 rollup discipline. SPEC at `docs/envoy-go/phases/19.2-http-filter-ext-proc-body/SPEC.md` (drafted at 19.2's lifecycle-state 1; sibling README stub at `docs/envoy-go/phases/19.2-http-filter-ext-proc-body/README.md` drafted at the phase-19 parent SPEC commit). |
| 20 | http-filter-oauth2 | 19 | done | 2026-05-17 | New `internal/filter/http/oauth2/` package implementing `envoy.filters.http.oauth2.v3.OAuth2` (Envoy v1.37.2 canonical OAuth 2.0 authentication filter; browser-facing redirect + cookie-session sign-in / refresh / sign-out under the 07.1 framework). THIRTEENTH §9 family-row (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11, csrf @ 12, buffer @ 13, compressor @ 14, bandwidth_limit @ 15, rbac @ 16, jwt_authn @ 17, ext_authz @ 18, ext_proc @ 19). **BRAINSTORM DONE at 2026-05-17** via a 10-question dialogue (Q1 MVP envelope — Standard OAuth 2.0 sign-in + refresh + sign-out; Q2 `internal/httpclient/` generalization trigger — EXTRACT NOW per the ADR-0159 §Future Work third-consumer trigger; Q3 SDS posture — filesystem-path SDS only; Q4 token-encryption envelope — Proto-faithful AES-GCM default-on honor `disable_token_encryption=true` skip; Q5 per-route discipline — No per-route support, listener-scoped only per the v1.37.x oauth2 proto's absence of an `OAuth2PerRoute` message; Q6 ADR-0045 split-readiness leaning — MEDIUM-HIGH, SPEC author makes final call; Q7 filesystem-SDS location — NEW shared `internal/sdsfile/` package; Q8 SDS reload posture — fsnotify-based hot-reload; Q9 cookie HMAC scheme — HMAC-SHA256(hmac_secret, host + BearerToken + OauthExpires) Envoy-default composition; Q10 token-endpoint POST body format under URL_ENCODED_BODY — `client_secret_post` in body). MVP envelope per BRAINSTORM §1.1: ~17 of 26 `OAuth2Config` fields consumed (sign-in + refresh + sign-out; `signout_path`; `use_refresh_token`; `default_expires_in`; `default_refresh_token_expires_in`; `deny_redirect_matcher`; `preserve_authorization_header`; `disable_token_encryption=false` default AES-GCM encryption ON; `pass_through_matcher`; `auth_scopes`; `resources`); 4-of-5 `OAuth2Credentials` (no BASIC_AUTH); 5-of-7 `CookieNames` (no `oauth_nonce`, no `code_verifier` — both deferred with PKCE). Defers ~10 of 26 `OAuth2Config` fields per BRAINSTORM §8 (BASIC_AUTH, `retry_policy`, `end_session_endpoint`, id_token + validation, PKCE, `cookie_configs`, `disable_*_set_cookie` trio, `csrf_token_expires_in`, `code_verifier_token_expires_in`, `cookie_domain`; SDS non-filesystem ConfigSource variants; per-route override NEVER). **FIVE framework primitives** per BRAINSTORM §3: (1) NEW top-level `internal/httpclient/` package (Q2 EXTRACT NOW; ~150-250 LoC; ADR-0177); (2) NEW top-level `internal/sdsfile/` package (Q7 + Q8 bundled; ~160-200 LoC; ADR-0178); (3) NEW filter-local AES-GCM token-encryption helper at `oauth2/tokens.go` (Q4; ~150-200 LoC; ADR-0182); (4) IN-PLACE ADR-0150 §Decision AMENDMENT (jwks `Fetcher` refactored to consume `internal/httpclient/` per ADR-0044); (5) IN-PLACE ADR-0159 §Decision AMENDMENT (extauthz `httpAuthClient` refactored to consume `internal/httpclient/` per ADR-0044; closes the ADR-0159 §Future Work forward-pointer load-bearing). **FIVE framework REUSES** per BRAINSTORM §3.6: ADR-0144 `DownstreamPrincipal()` NOT consumed (no TLS-principal interaction); ADR-0150 jwks NOT consumed for id_token (deferred); ADR-0151 jwt verifier NOT consumed (id_token deferred); Phase-04 HCM `SendLocalReply` + Location header for 302 redirects to authorization_endpoint; Phase-09 async-resume primitive for token_endpoint POST during callback-flow handling. **Per-route REUSE-by-absence** (Q5; no `OAuth2PerRoute` message in v1.37.x proto; listener-scoped-only enforcement; PARSE-REJECT if anyone places oauth2 TPFC under a route/virtualHost); **THIRD CONSECUTIVE §9 row** with no ADR-0125 amendment paragraph (after phase 18 per ADR-0163 + phase 19 per ADR-0173 both REUSED the 5th canonical; phase 20's REUSE-by-absence is a stronger form — the absence itself is the lesson). **MEDIUM-HIGH split-readiness leaning** per Q6 (LoC envelope between phase-17 jwt_authn 3855 single-row and phase-19 ext_proc 5000+ split); SPEC author makes the final call after the empirical-pin scrape closes the LoC envelope (estimate ~3500-4000 LoC production); most-likely split-by-feature-class shape: 20.1 = framework refactor (`internal/httpclient/` + `internal/sdsfile/` + ADR-0150/0159 in-place AMENDMENTs + extauthz/jwks refactor) + oauth2 sign-in flow (HMAC cookie + redirects + token_endpoint POST + URL_ENCODED_BODY + pass_through_matcher + auth_scopes + resources); 20.2 = AES-GCM token encryption + refresh-token rotation + sign-out + `deny_redirect_matcher` + `preserve_authorization_header`. Stat surface hypothesis 86 → ~94 names (~8 HCM-rooted SN2-reuse counters under `http.<HCM_stat_prefix>.oauth2.*`: `oauth_unauthorized_rq` / `oauth_failure` / `oauth_passthrough` / `oauth_success` / `oauth_refreshtoken_success` / `oauth_refreshtoken_failure` / `signout_completed` / `cookie_decrypt_failure` — claim approximate; SPEC-time empirical pin §20.P8 confirms; the `cookie_decrypt_failure` counter is an envoy-go-strict departure from Envoy reference flagged for ADR-0080 discipline review at SPEC time per §20.P11). Differential fixture `0024-http-oauth2` (~6-8 scenarios per BRAINSTORM §6: `sign_in_happy_path` / `cookie_present_passthrough` / `pass_through_matcher` / `refresh_token_rotation` / `signout_flow` / `bad_state_401` / `token_endpoint_failure_500` / `disable_token_encryption_true`) + NEW `test/helpers/oauthbackend/` test-helper (in-process OAuth 2.0 authorization-server backend; stdlib `net/http/httptest`-based mock authorization_endpoint + token_endpoint + optional-stub JWKS for future id_token reuse) + 26th fuzzer `FuzzOAuth2ConfigParse` in `internal/filter/http/oauth2/fuzz_test.go` (current count 25 post-phase-19.2 → 26 post-phase-20). **7-9 ADRs anticipated** (ADR-0177..ADR-0185 per BRAINSTORM §7): ADR-0177 `internal/httpclient/` framework primitive + ADR-0178 `internal/sdsfile/` filesystem-path Secret reader with fsnotify hot-reload + ADR-0179 oauth2 HMAC cookie composition canonicalization + ADR-0180 oauth2 three-flow state-machine + deny-path wire shape + listener-scoped-only enforcement + ADR-0181 oauth2 cookie envelope (5-of-7 CookieNames) + Set-Cookie attribute discipline + ADR-0182 AES-GCM token-encryption scheme + ADR-0183 refresh-token rotation timing + ADR-0184 sign-out flow + `deny_redirect_matcher` integration + ADR-0185 (likely) token_endpoint POST body format under URL_ENCODED_BODY (`client_secret_post`); plus TWO IN-PLACE §Decision AMENDMENTs to ADR-0150 + ADR-0159 per ADR-0044 in-place edit discipline (not new ADR numbers). ADR-0044 escape-valve held in reserve at IMPL-time for ~0-2 impl-time-unanticipated ADRs. Per ADR-0106, §9 family-rows are flat top-level rows; phase 20 lands as row `20` initially (this BRAINSTORM-time row-registration is an explicit user-direction departure from the phase-09..19.2 ROADMAP-row-add-at-SPEC convention); sub-phase rows `20.1` / `20.2` may appear at the SPEC commit if the ADR-0045 split fires per Q6 MEDIUM-HIGH leaning. BRAINSTORM at `docs/envoy-go/phases/20-http-filter-oauth2/BRAINSTORM.md` (lifecycle-state 0 → 1 commit). **SPEC DONE at 2026-05-17** (15-section SPEC at `docs/envoy-go/phases/20-http-filter-oauth2/SPEC.md`; structure mirrors phase-19.1 SPEC; ~831 LoC). **Single-row landing settled per ADR-0045** (NOT split into 20.1+20.2 per BRAINSTORM-anticipated MEDIUM-HIGH lean — phase-17 jwt_authn 3855 single-row precedent applies; LoC envelope re-estimated post-empirical-scrape at ~3400-3900 — slightly tighter than BRAINSTORM's ~3500-4000 due to S1/S2/S5 SPEC-time simplifications). **12 §20.P empirical pins ALL CLOSED at SPEC time** via parallel-subagent fan-out against v1.37.2 reference Envoy (disposition matrix: 6 RATIFIED + 4 REFUTED + 1 PARTIAL → SPEC-decided + 1 RATIFIED-AS-ABSENT): §20.P1 RATIFIED (zero-retry default); §20.P2 RATIFIED (host-only cookie); §20.P3 PARTIAL → SPEC-decided (POST callback PARSE-REJECT envoy-go-strict; GET-only); §20.P4 REFUTED (5-input newline-joined HMAC NOT 3-input; id_token + refresh_token empty when absent); §20.P5 REFUTED (AES-256-CBC NOT AES-GCM); §20.P6 RATIFIED (`ConfigSource.PathConfigSource` field 8); §20.P7 RATIFIED (no `OAuth2PerRoute` message); §20.P8 REFUTED (6 counters wire-exact NOT 8 — BRAINSTORM over-counted); §20.P9 REFUTED (constant 18-byte `"OAuth flow failed."`; NEVER 500); §20.P10 RATIFIED (byte-exact 4-field auth-code + 3-field refresh-token templates); §20.P11 RATIFIED-AS-ABSENT (fall-back returns ciphertext-as-plaintext; envoy-go-strict departure CLOSED); §20.P12 RATIFIED (proto default 600s). **5 SPEC-time questions settled** (S1 AES-256-CBC mirror upstream; S2 drop runtime gate; S3 gated PKCE template — 4-field MVP / 5-field future; S4 dual-encoding HMAC — emit Base64 accept both encodings; S5 6-counter wire-exact drop `signout_completed` + `cookie_decrypt_failure`). **§1.1 AMEND-1..AMEND-7 amendment block cataloged** (AMEND-1 algorithm swap GCM→CBC; AMEND-2 5-input HMAC; AMEND-3 deny-path 302+401 only no-500; AMEND-4 stat surface 86→92 names 6-counter wire-exact; AMEND-5 PKCE-gated 4-field template; AMEND-6 field-locus corrections — `cookie_domain` on OAuth2Credentials NOT OAuth2Config + `cookie_configs` is `*CookieConfigs` wrapper + `forward_bearer_token` MVP-CONSUMED; AMEND-7 per-route HCM-parse-time PARSE-REJECT + NEW `Partitioned` cookie attribute deferral). **9 NEW ADRs landed at SPEC commit** (§Context drafts per ADR-0044): ADR-0177 NEW `internal/httpclient/` (closes ADR-0159 §Future Work load-bearing — third-consumer trigger fired) + ADR-0178 NEW `internal/sdsfile/` (fsnotify + atomic-swap; PARSE-REJECT non-filesystem ConfigSource arms + deprecated path field + secret_file indirect; NEW go.mod fsnotify dep) + ADR-0179 HMAC composition (5-input + dual-encoding) + ADR-0180 state-machine + deny-path 302+401 + listener-scoped + explicit no-ADR-0125-amendment classification + ADR-0181 cookie envelope + 6-counter stat surface (CLOSES §20.P11 as RATIFIED-AS-ABSENT) + ADR-0182 AES-256-CBC token-encryption + ADR-0183 refresh-token rotation timing + race-vs-rotation + ADR-0184 sign-out flow + ADR-0185 token_endpoint POST body templates byte-exact. **2 IN-PLACE §Decision AMENDMENT-anticipation paragraphs at ADR-0150 + ADR-0159** per ADR-0044 (AMENDMENT bodies + ADR-0159 §Future Work CLOSURE-AT-PHASE-20 paragraph land at IMPL Task 2). **THIRD CONSECUTIVE §9 row to skip ADR-0125 amendment** (REUSE-by-absence stronger form than 5th-canonical REUSE). **FIRST §9 family-row to CLOSE a prior-phase load-bearing forward-pointer** (ADR-0159 §Future Work third-outbound-HTTP-consumer-trigger → closed at phase 20 per Q2 EXTRACT NOW). Stat surface 86 → 92 names (NOT 94 per BRAINSTORM). Differential fixture `0024-http-oauth2` per SPEC §7: 9 wire-level expectations across 8 scenario directories (a sign_in_happy_path; b1+b2 cookie_passthrough; c pass_through_matcher; d refresh_token_rotation; e signout_flow; f bad_state_401; g token_endpoint_5xx_302; h token_endpoint_4xx_401; i disable_token_encryption_true) + 2-or-3-listener topology + NEW `test/helpers/oauthbackend/` package (~250-350 LoC; Scripted{Authz,Token}Responses + ValidCookieEnvelope + TamperedStateCookie helpers) + 26th fuzzer `FuzzOAuth2ConfigParse`. Six-gate checklist (A/B/C/D/E/F) + cross-package regression matrix (jwt_authn fixture 0019 + extauthz HTTP fixture 0020 GREEN post-refactor; extauthz gRPC fixture 0021 untouched). SPEC §15 18-item acceptance checklist for the IMPL phase-done reviewer. BEHAVIOR_CONTRACT 10-edit bundle anticipated per SPEC §13 (NEW `### envoy.filters.http.oauth2` subsection + 2 NEW framework-primitive umbrella subsections + stat-table 86→92 extension + ADR-0125 cross-reference + ADR-0159 forward-pointer CLOSURE paragraph + 2 envoy-go-strict departure records + `### Phase 20 forward-pointer notes` subsection + ADR-0150 jwks REFACTORED-AT-PHASE-20 paragraph). 8 RATIFIED-PENDING-IMPL-TIME items deferred to IMPL per SPEC §12 (5 wire-shape byte-confirmations + 2 library-behavioral + 1 cross-phase regression). ADR-0044 escape-valve in reserve for ~0-2 impl-time-unanticipated ADRs at IMPL. Next-free ADR post-SPEC commit: ADR-0186. **PLAN DONE at 2026-05-17** (14-task PLAN at `docs/envoy-go/phases/20-http-filter-oauth2/PLAN.md`; ~1332 LoC; structurally mirrors phase-19.1 PLAN). PLAN settles **19 planner-time decisions D1-D19** (D1 Task 2 sub-grouping into 3 sub-commits 2a/2b/2c — NEW `internal/httpclient/` + ADR-0177 + paired ADR-0150 jwks + ADR-0159 extauthz in-place refactors; D2 PARSE-REJECT byte-stable wording with `oauth2:` prefix; D3 SDS Secret paths at `test/fixtures/0024-http-oauth2/secrets/`; D4 3 race-test groups `TestWatcher_DebounceRace_*` + `TestRefreshTokenRotation_Concurrent_*` + `TestAesKeySwap_Concurrent_*`; D5 cross-package regression cmd shape `go test -count=1 ./test/differential/ -run 'Test.*001[9]|002[01]'`; D6 stat-name compile-time guard via package-level `const` declarations + table-driven assertion test; D7 26th fuzzer `FuzzOAuth2ConfigParse` corpus seed roster ~60 seeds; D8 single-process-wide `*httpclient.Client` singleton at boot; D9 Tasks 3+4+6+7 parallel-dispatch opportunity disjoint-files; D10 3-listener fixture topology l_test_a/b/c; D11 NO additional ADR fires at IMPL hypothesis — next-free ADR-0186 stays unconsumed; D12 fsnotify dep version v1.x latest minor; D13 `*sdsfile.Watcher` lifecycle owned by `*compiledConfig` + closed at filter teardown; D14 refresh-token rotation no-per-stream-serialization per ADR-0183; D15 POST callback PARSE-REJECT byte-stable wording; D16 SPEC §12 wire-shape items A1-A5 fixture-0024 scenario closures (A1+A4 at f+h; A2+A3+A5 at a); D17 SPEC §12 library-behavioral items B6+B7 unit/race closures (B6 at Task 7 unit + Task 12 scenario b2; B7 at Task 3 unit + race); D18 SPEC §12 cross-phase regression matrix C8 closure via D5 + Task 14 Gate D; D19 boot-registration alphabetical at line 135 between localratelimit + rbac). Per-task IMPL anchors per ADR-0044: Task 1 PROGRESS preamble + 17-precondition verification; Task 2 NEW `internal/httpclient/` + ADR-0150 + ADR-0159 in-place AMENDMENTs + §Future Work CLOSURE-AT-PHASE-20 paragraph (3 sub-commits); Task 3 NEW `internal/sdsfile/` + ADR-0178; Task 4 `hmac.go` + ADR-0179; Task 5 `decode_headers.go` + `callback.go` dispatcher + ADR-0180; Task 6 `cookies.go` + `stats.go` + ADR-0181; Task 7 `tokens.go` AES-256-CBC + ADR-0182 + AES-key-swap race tests; Task 8 refresh-token rotation in `callback.go` + ADR-0183 + concurrent race tests; Task 9 `signout.go` + ADR-0184; Task 10 `oauth_client.go` + `urlEncode` helper + ADR-0185; Task 11 full filter integration in `oauth2.go` + `compiled_config.go` + boot-registration + ADR final-state alignment; Task 12 differential fixture `0024-http-oauth2` + NEW `test/helpers/oauthbackend/` + 26th fuzzer + SPEC §12 A1-A5 + B6 + B7 + C8 pin closures; Task 13 BEHAVIOR_CONTRACT.md 10-edit bundle per SPEC §13 (atomic landing per ADR-0052); Task 14 six-gate phase-done verification (A build / B vet+lint / C race / D differential / E fuzz / F h2spec) + STATE.md re-advance + ROADMAP row 20 flip in-progress→done + REVIEW.md per `superpowers:requesting-code-review`. Next session is the phase-20 IMPL session (lifecycle-state 3 → 6) per `superpowers:subagent-driven-development` (recommended per project memory `feedback_execution_style.md`) OR `superpowers:executing-plans`. **IMPL DONE at 2026-05-17** (14 tasks landed Tasks 1-14 + 1 follow-up Task 12 follow-up wiring `handleCallback` auth-code POST; ~3590 lines of docs + ~11032 insertions across 34 production+test Go files = ~14622 cumulative IMPL insertions vs master; 16 production Go files materialized per SPEC §6.11). **9 NEW ADRs §Decision + §Consequences full bodies anchored** (ADR-0177 `internal/httpclient/` framework primitive at Task 2a `c0a058c` + ADR-0178 `internal/sdsfile/` framework primitive at Task 3 `cf36070` + ADR-0179 oauth2 HMAC composition at Task 4 `cbed2d1` + ADR-0180 state-machine + deny-path at Task 5 `43b1282` + ADR-0181 cookie envelope + 6-counter stat surface at Task 6 `c503cca` + ADR-0182 AES-256-CBC token-encryption at Task 7 `d7e159b` + ADR-0183 refresh-token rotation at Task 8 `df33e4b` + Task 8 follow-up `f9f007a` D15-literal revert + ADR-0184 sign-out flow at Task 9 `21cda4c` + ADR-0185 token_endpoint POST templates at Task 10 `ac47edc`); **2 IN-PLACE §Decision AMENDMENT bodies anchored** at Task 2b `e01573d` (ADR-0150 jwks Fetcher refactor) + Task 2c `ea56c29` (ADR-0159 extauthz httpAuthClient refactor + §Future Work CLOSURE-AT-PHASE-20 paragraph — **FIRST §9 family-row to CLOSE a prior-phase load-bearing forward-pointer** per ADR-0044 + Q2 EXTRACT NOW); **Task 11** `02590c7` full filter integration + boot-registration at alphabetical line 135 per D19; **Task 12** `24e3df9` fixture `0024-http-oauth2` + `test/helpers/oauthbackend/` (~643 LoC) + 26th fuzzer `FuzzOAuth2ConfigParse` (~30 corpus seeds — D7 ~60-seed target landed at ~30 — DOCUMENTED FUTURE WORK); **Task 12 follow-up** `6396eab` wires `handleCallback` auth-code POST + `applyTokenEndpointResponse` full disposition matrix per §4.5 + §4.7 + AMEND-3 (9 new tests); **Task 13** `2af6f12` BEHAVIOR_CONTRACT 10-edit bundle (+265 / -2 LoC per ADR-0052 atomic landing). **All 6 phase-done gates GREEN** at Task 14: Gate A build clean; Gate B vet + golangci-lint clean; Gate C race clean repo-wide (re-run once for pre-existing port-bind flake at fixture-0020 `l_test_b:46566 bind: address already in use` — documented Task 9 reviewer pre-existing flake class); Gate D 25/25 differential fixtures GREEN (0000-0024); Gate E 26th fuzzer `FuzzOAuth2ConfigParse` clean (3,620,991 execs at 21k-167k/sec; 82 new interesting / 401 total) + 3 spot-checks clean (`FuzzExtProcConfigParse` + `FuzzBootstrapLoad` + `FuzzHCMConfigParse`); Gate F h2spec 53/53 PASS at ADR-0051 pin. **D11 hypothesis HELD across all 14 tasks** — ADR-0186 stays UNCONSUMED at phase-20 IMPL phase-done; `grep -cE '^## ADR-0186' docs/envoy-go/DECISIONS.md` returns 0; next-free ADR stays at `ADR-0186`. **THIRD CONSECUTIVE §9 row to skip ADR-0125 amendment** (REUSE-by-absence stronger form than 5th-canonical REUSE — proto absence of `OAuth2PerRoute` enforces listener-scoped-only via HCM-parse-time PARSE-REJECT per §5.2). **Stat surface 86 → 92 names** (6-counter wire-exact: `oauth_unauthorized_rq` / `oauth_failure` / `oauth_passthrough` / `oauth_success` / `oauth_refreshtoken_success` / `oauth_refreshtoken_failure`; `signout_completed` + `cookie_decrypt_failure` ABSENT per S5 + AMEND-4 + §20.P11 RATIFIED-AS-ABSENT). **SPEC §15 18-item acceptance checklist: 17 GREEN + 1 GREEN-WITH-DOCUMENTED-EXCEPTIONS** (item 7 fixture-0024 9-scenario coverage GREEN at the 7-of-11 landed-subset boundary per PLAN Task 12 scope decision — see REVIEW.md §2 per-item disposition; the 4 deferred scenarios (d refresh-token rotation; g token_endpoint 5xx → 302; h token_endpoint 4xx → 401; i `disable_token_encryption=true`) are documented future-work items in fixture-0024 README + REVIEW.md §6). **Recognized future-work items** (forward-pointers carried to future phases — NONE blocking phase-done; all in REVIEW.md §6): (i) fixture-0024 cross-side reference-Envoy promotion (currently REFERENCE-LESS subject-only structural per Task 12 scope decision — full cross-side differential deferred to a future fixture-extension task); (ii) 2-listener vs SPEC §7.2 3-listener topology — `l_test_b` `disable_token_encryption=true` scenario (i) blocked by go-control-plane v1.32.4 proto field absence (closes after the go-control-plane v1.37.x bump); (iii) fuzzer corpus seed count ~30 vs D7 target ~60 (additive corpus expansion deferred); (iv) HMAC `domain` empty-string subtlety discovered at Task 12 follow-up (host-only-default preserves invariant; operators with non-empty `cookie_domain` may see HMAC mismatch — documented in BEHAVIOR_CONTRACT §13.D Phase 20 forward-pointer notes); (v) v1.32.4 vs v1.37.x go-control-plane proto bump for 4 missing fields (`disable_token_encryption`, `Partitioned` cookie attribute, etc.); (vi) ~6 reviewer observations (sdsfile lifecycle comment; secret_file PARSE-REJECT swallowed; JSON fall-through; 700-LoC compiled_config split candidate; etc. — all in REVIEW.md §6). **REVIEW.md authored** per `superpowers:requesting-code-review` at `docs/envoy-go/phases/20-http-filter-oauth2/REVIEW.md` (~350 LoC; 18-item SPEC §15 acceptance verification + D1-D19 planner-decision-disposition record + per-Task summary + known limitations + 6-gate verbatim outputs + next-phase handoff). Phase 20 is **READY FOR SQUASH-MERGE TO MASTER** per project memory `feedback_git_worktrees.md` + ADR-0005 §Decision 4. |
| 21 | http-filter-adaptive-concurrency | 20 | done | 2026-05-18 | New `internal/filter/http/adaptive_concurrency/` package implementing `envoy.filters.http.adaptive_concurrency.v3.AdaptiveConcurrency` (Envoy v1.37.2 canonical adaptive-concurrency filter; Gradient-1 controller that estimates `minRTT` from sampled request round-trip latency and continuously adjusts a per-HCM-instance concurrency limit to bound tail latency under load) under the 07.1 framework. FOURTEENTH §9 family-row (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11, csrf @ 12, buffer @ 13, compressor @ 14, bandwidth_limit @ 15, rbac @ 16, jwt_authn @ 17, ext_authz @ 18 with its 18.1+18.2 split, ext_proc @ 19 with its 19.1+19.2 split, oauth2 @ 20). **BRAINSTORM DONE at 2026-05-18** via a 5-question dialogue (Q1 scope ambition — Pragmatic middle: full Gradient-1 algorithmic surface with PARSE-REJECT for `enabled.runtime_key != ""` deferring the RTDS RuntimeFeatureFlag to a future Runtime/RTDS family phase; Q2 differential-fixture strategy — cross-side with FAKE-TIME injection via an inline Clock seam, two-layer test taxonomy: subject-only step-driven algorithmic-fidelity unit tests + a `0025-http-adaptive-concurrency` structural cross-side fixture with SPEC-time D-question on partial cross-side byte-exact promotion for the 503-overflow leg; Q3 clock seam — INLINE Clock interface in `internal/filter/http/adaptive_concurrency/clock.go`, NO new `internal/clock/` framework primitive, NO new ADR for the primitive, forward-pointer EXTRACT-NOW trigger when a second timer-driven filter materializes; Q4 phase split — single phase 21 with the ADR-0045 split-gate at REST, SPEC author revisits if scope drifts; Q5 stat surface — full upstream 7-name parity 1 counter + 6 gauges under `<filter_stat_prefix>.adaptive_concurrency.<config_stat_prefix>.gradient_controller.<stat>` byte-exact template). MVP envelope per BRAINSTORM §1.1: full `GradientControllerConfig` sub-surface consumed (`sample_aggregate_percentile` default p50, `concurrency_limit_calculation_params{max_concurrency_limit, concurrency_update_interval}`, `min_rtt_calc_params{interval, request_count, jitter, min_concurrency, buffer}`) + `concurrency_limit_exceeded_status` (default 503) + static `enabled.default_enabled` honored; PARSE-REJECT any `enabled.runtime_key != ""`. **ZERO NEW framework primitives + ZERO IN-PLACE AMENDMENTs** per BRAINSTORM §3 — phase 21 is the **LEANEST framework-delta §9 row to date** (FIRST §9 row since phase 14 compressor to introduce ZERO new `internal/` primitives) and the FIRST §9 row to land with the framework-delta posture entirely confined to in-package code. **SEVEN framework REUSES** per BRAINSTORM §3.2-§3.8: (1) `internal/stats/` Counter + Gauge support (gauge support already first-class in `internal/stats/gauge.go::Gauge` over `atomic.Int64`); (2) HTTPRegistry boot-time registration (`internal/filter/http/registry.go::HTTPRegistry`; 16 HTTP filters wired post-phase-21 with `adaptiveconcurrency.New` alphabetical-first); (3) per-request filter interface (decode/encode hooks); (4) HCM-parse-time PARSE-REJECT path (invalid percentile, zero interval, zero request_count, jitter > 100%, `enabled.runtime_key != ""`, oneof-absent); (5) REUSE-by-absence per-route enforcement (no `AdaptiveConcurrencyPerRoute` proto message; **FOURTH CONSECUTIVE §9 row to skip ADR-0125 amendment** — extending the third-consecutive run from phase-20); (6) existing fuzzer-corpus framework (`internal/filter/http/fuzz_test.go` cross-filter fuzzer registry); (7) existing differential-fixture framework (`test/fixtures/` + `test/differential/runner_test.go`). Stat surface hypothesis 92 → 99 names per BRAINSTORM §5: 1 counter (`rq_blocked`) + 6 gauges (`concurrency_limit`, `gradient` encoded ×1000 int64, `burst_queue_size`, `sampled_rtt` encoded as ns int64, `min_rtt` encoded as ns int64, `recalculating_min_rtt` 0/1) under the upstream-byte-exact `<filter_stat_prefix>.adaptive_concurrency.<config_stat_prefix>.gradient_controller.<stat>` prefix template (SPEC-time §10 empirical pin obligation per ADR-0004 confirms template byte-exactness). Float-valued gauge encoding convention (ns for time-typed; ×1000 for ratio-typed gradient) anchored at ADR-0188-candidate (or in-place §Decision body on an existing stats-family ADR; SPEC author decides). Differential fixture `0025-http-adaptive-concurrency` per BRAINSTORM §6: REFERENCE-LESS subject-only structural at first cut (matching phase-20's 0024-http-oauth2 precedent; ~4 wire-level expectations across scenario directories: parse OK, 503 on overflow under `max_concurrency_limit=1 + min_concurrency=1 + 2 concurrent slow requests` trap, stat-surface delta, pass-through-when-disabled) + single-listener topology + 27th fuzzer `FuzzAdaptiveConcurrencyConfigParse` in `internal/filter/http/adaptive_concurrency/fuzz_test.go` (current count 26 post-phase-20 → 27 post-phase-21; ~30 corpus seeds at the standard baseline + PARSE-REJECT arm coverage for the 6 invalid-config classes). Subject-only algorithmic-fidelity test layer via step-driven `FakeClock` in `internal/filter/http/adaptive_concurrency/controller_test.go` deterministically exercises gradient calc, minRTT recalc windows, jitter range, burst queue accounting, and 503-on-overflow. **2-3 ADRs anticipated** (ADR-0186..ADR-0188 per BRAINSTORM §7): ADR-0186 Gradient-1 controller state machine + clock seam inline + FAKE-TIME differential strategy + sorted-slice percentile aggregation + gradient formula bit-exact citation against upstream `gradient_controller.cc`; ADR-0187 RTDS `enabled.RuntimeFeatureFlag` deferral PARSE-REJECT (static-default-enabled honored; `runtime_key != ""` triggers HCM-parse-time PARSE-REJECT with forward-pointer to the future Runtime/RTDS family phase); ADR-0188-candidate float-valued gauge encoding convention (anchored as a new framework convention OR as in-place §Decision body on a stats-family ADR — SPEC author decides). **LOW-MEDIUM split-readiness leaning** per BRAINSTORM §1.4 (anticipated ~1200-1500 LoC + ~14-16 tasks; the natural split axes don't carve cleanly because the controller state machine + FAKE-TIME tests are tightly coupled and the framework delta is zero); SPEC author re-evaluates if scope drifts past the ADR-0045 split-gate at SPEC time. **D11-style hypothesis** per BRAINSTORM §7.4: ADR-0189 stays UNCONSUMED at phase-21 phase-done (carrying the next-free escape valve forward to phase 22); most-likely escape-valve surfaces hypothesized at PLAN time (PKCS#7-padding-analog edge cases; fsnotify-event-debounce-analog edge cases; urlEncode-charset-analog edge cases) all close via in-place §Decision body wording at existing ADRs. **4 SPEC-time D-questions** per BRAINSTORM §10: D1 float-gauge encoding convention anchor (ADR-0188 new vs in-place §Decision); D2 503-overflow cross-side promotion candidate (REFERENCE-LESS subject-only structural vs partial cross-side byte-exact via the deterministic 2-concurrent-slow-requests trap); D3 gradient_controller.cc line-citation depth (ADR-0186 line-exact lemmata vs SPEC-prose-only); D4 stat-prefix template byte-exact pin (§10 IN-SESSION verification against reference Envoy v1.37.2 per ADR-0004). 6-8 deferred items per BRAINSTORM §8 (RTDS runtime keying; cross-side byte-exact algorithmic parity; alternative ConcurrencyControllerConfig oneof arms; CircllHist/HDR Hist percentile aggregation upgrade; stat-prefix template byte-exact pin SPEC-time-closure; float-gauge encoding convention anchor; gradient_controller.cc line-citation depth; multi-listener topology variant). **Phase-21 picks up ZERO closures from prior phases' deferred-items lists** per BRAINSTORM §9 (no extension of the FIRST-§9-row-to-CLOSE-prior-load-bearing-forward-pointer milestone from phase-20). Per ADR-0106, §9 family-rows are flat top-level rows; phase 21 lands as row `21` initially (this BRAINSTORM-time row-registration follows the phase-20 BRAINSTORM-time row-registration precedent — explicit user-direction departure from the phase-09..19.2 ROADMAP-row-add-at-SPEC convention); single-row landing anticipated; sub-phase rows `21.1` / `21.2` may appear at SPEC commit if the ADR-0045 split fires at SPEC time. BRAINSTORM at `docs/envoy-go/phases/21-http-filter-adaptive-concurrency/BRAINSTORM.md` (lifecycle-state 0 → 1 commit). **SPEC DONE at 2026-05-18** (15-section SPEC at `docs/envoy-go/phases/21-http-filter-adaptive-concurrency/SPEC.md`; structure mirrors phase-20 SPEC; ~839 LoC). **Single-row landing settled per ADR-0045** (NOT split into 21.1+21.2 per BRAINSTORM-anticipated LOW-MEDIUM lean — the natural split axes don't carve cleanly per BRAINSTORM §1.4 + SPEC §3.5; LoC envelope re-estimated post-empirical-scrape at ~1370-1830, slightly above BRAINSTORM's ~1200-1500 due to AMEND-2 algorithmic-invariant state-machine additions but well below the ADR-0045 split-gate at ~1500 LoC / 25 tasks). **5 §21.P empirical pins ALL CLOSED at SPEC time** via parallel-subagent fan-out against v1.37.2 reference Envoy (disposition matrix: 1 RATIFIED + 3 PARTIAL + 1 RESOLVED-to-AMENDMENT): §21.P1 RATIFIED (503-overflow cross-side promotion D2 — trap sound at `min=max=1` HARD-clamp; CAS arrival-order deterministic per `gradient_controller.cc:209-233`; 503 body byte-exact `"reached concurrency limit"` 25 bytes + `content-type: text/plain` + `content-length: 25`); §21.P2 PARTIAL (algorithmic invariants D3 — gradient formula `clamp(0.5, min_rtt × (1 + buffer) / sample_rtt, 2.0)` RATIFIED per `gradient_controller.cc:190-192` + new-limit `currentLimit × gradient + sqrt(currentLimit × gradient)` RATIFIED per `cc:198-206`; 4 AMENDs: minRTT-aggregation uses `sample_aggregate_percentile` quantile NOT MIN per `cc:176-182` + jitter additive `[0, interval × jitter_pct)` to next-interval delay per `cc:152-160` + 5-consecutive-min forced-recalc trigger per `cc:281-283` + first-tick-immediate-window-entry semantics per `cc:55-92`); §21.P3 PARTIAL (stat surface D4 — 3 stat-name renames `sampled_rtt`→`sample_rtt_msecs` + `min_rtt`→`min_rtt_msecs` + `recalculating_min_rtt`→`min_rtt_calculation_active` per `gradient_controller.h:27-34`; RTT in MILLISECONDS upstream `duration_cast<milliseconds>` per `cc:75-76,154-155` / envoy-go-strict departure to nanoseconds `time.Duration.Nanoseconds()`; template SIMPLIFIED — no `<filter_stat_prefix>` or `<config_stat_prefix>` segments exist; template is `http.<HCM_stat_prefix>.adaptive_concurrency.gradient_controller.<stat>` with two hardcoded literal infixes per `config.cc:15-31`; `gradient × 1000` int64 RATIFIED per `cc:176`; `min_rtt_calculation_active` `Accumulate` import-mode deferred per SPEC §8 item 7); §21.P4 PARTIAL (PARSE-REJECT + defaults + `enabled` semantics + ConcurrencyControllerConfig oneof — proto field rename `concurrency_limit_calculation_params`→`concurrency_limit_params` per `adaptive_concurrency.proto:56-57`; no separate `gradient_controller.proto` at v1.37.2 — GradientControllerConfig lives inline; `enabled` defaults OFF when absent per `BoolValue` proto-default REFUTES BRAINSTORM §2.1; `min_rtt_calc_params.interval` not proto-required PGV `gte: 1ms` with `fixed_value` alternative path per `proto:42-43` + `cc:47-50`; 5 additional PGV PARSE-REJECT arms BRAINSTORM missed — `concurrency_limit_params` required-message + `min_rtt_calc_params` required-message + `max_concurrency_limit == 0` when set + `min_concurrency == 0` when set + `fixed_value` envoy-go-strict deferral; ConcurrencyControllerConfig oneof solo `gradient_controller_config` RATIFIED-AS-ABSENT for alternative arms); §21.P5 RESOLVED-to-IN-PLACE-AMENDMENT (D1 float-gauge encoding convention anchor → IN-PLACE §Decision AMENDMENT on ADR-0059 NOT new ADR-0188 — the BRAINSTORM-anticipated ADR-0188 candidate COLLAPSES; ns for time-typed; ×1000 for ratio-typed; 0/1 for bool-typed; ~20-30 LoC delta with NEW `internal/stats/conv.go` `boolToInt` helper + comment-only `gauge.go` cross-reference; no signature change to `*stats.Gauge`). **§1.1 AMEND-1..AMEND-7 amendment block cataloged** (AMEND-1 proto-schema corrections C1 field rename + C2 proto-file consolidation + C3 interval validation nuance + C4 fixed_value PARSE-REJECT; AMEND-2 algorithmic-invariant corrections C1 percentile-not-MIN + C2 jitter-to-next-interval + C3 5-consec-trigger + C4 first-tick semantics; AMEND-3 stat-name+encoding+template corrections C1 3 renames + C2 template simplification + C3 RTT-ms-vs-ns envoy-go-strict departure + C4 gradient ×1000 + C5 Accumulate import-mode deferral; AMEND-4 `enabled` empty-default OFF refutation; AMEND-5 PARSE-REJECT roster expansion 5 additional arms; AMEND-6 503-overflow cross-side byte-exact promotion RATIFIED; AMEND-7 D1 resolution — ADR-0188-candidate COLLAPSES to ADR-0059 §Decision AMENDMENT). **2 NEW ADRs landed at SPEC commit** (§Context drafts per ADR-0044): ADR-0186 Gradient-1 controller state machine + inline `Clock` seam (NOT framework primitive) + FAKE-TIME differential strategy + sorted-slice percentile aggregation (NOT CircllHist; ≤ 1 bin-width divergence acceptable per BRAINSTORM §8 item 4) + gradient formula + new-limit calculation + minRTT recalc with `sample_aggregate_percentile`-quantile + jitter additive-to-next-interval-delay + 5-consec forced-recalc trigger + first-tick semantics + line-cited algorithmic lemmata against `gradient_controller.cc` per §21.P-D3 + `fixed_value` PARSE-REJECT; ADR-0187 RTDS `enabled.RuntimeFeatureFlag` deferral PARSE-REJECT (static-default honored; `runtime_key != ""` triggers HCM-parse-time PARSE-REJECT with forward-pointer to future Runtime/RTDS family phase) + `enabled` empty-default OFF semantics per AMEND-4 — REFUTES BRAINSTORM §2.1. **1 IN-PLACE §Decision AMENDMENT-anticipation paragraph at ADR-0059** per ADR-0044 (AMENDMENT body lands at IMPL Task 4). **FOURTH CONSECUTIVE §9 row to skip ADR-0125 amendment** (REUSE-by-absence per SPEC §5.4; no `AdaptiveConcurrencyPerRoute` proto message in v1.32.4 or v1.37.x). **LEANEST framework-delta §9 row to date** (ZERO new `internal/` framework primitives — only the in-package `Clock` seam at `clock.go` + the in-place ADR-0059 §Decision AMENDMENT). Stat surface 92 → 99 names per AMEND-3 (7-name roster). Differential fixture `0025-http-adaptive-concurrency` per SPEC §7: 4 scenario directories — (a) parse_ok REFERENCE-LESS + (b) overflow_503 **PARTIAL CROSS-SIDE BYTE-EXACT per AMEND-6** + (c) stat_surface REFERENCE-LESS + (d) pass_through_when_disabled REFERENCE-LESS + single-listener topology + 27th fuzzer `FuzzAdaptiveConcurrencyConfigParse`. Six-gate checklist (A/B/C/D/E/F) per SPEC §7.4 + SPEC §14.5 + cross-package regression matrix per SPEC §12 item C8 (post-ADR-0059 AMENDMENT). SPEC §15 18-item acceptance checklist for the IMPL phase-done reviewer. BEHAVIOR_CONTRACT 7-edit bundle anticipated per SPEC §13 (NEW `### envoy.filters.http.adaptive_concurrency` subsection + stat-table 92→99 extension + ADR-0059 §Decision AMENDMENT cross-reference + 2 envoy-go-strict departure records (RTT-ms-vs-ns + sorted-slice-vs-CircllHist) + NEW `### Phase 21 forward-pointer notes` subsection + Per-route canonical patterns cross-reference table caption update). 8 RATIFIED-PENDING-IMPL-TIME items deferred to IMPL per SPEC §12 (4 wire-shape byte-confirmations + 3 library-behavioral + 1 cross-phase regression). ADR-0044 escape-valve in reserve for ~0-2 impl-time-unanticipated ADRs at IMPL. **D11-style hypothesis STRENGTHENED at SPEC commit**: ADR-0188 + ADR-0189 stay UNCONSUMED at phase-21 IMPL phase-done (HOLD-with-known-risk; 2-slot escape-valve buffer absorbs one impl-time-unanticipated ADR without breaking the next-phase escape-valve invariant). Next-free ADR post-SPEC commit: **ADR-0188**. **PLAN DONE at 2026-05-18** (1154-LoC PLAN.md at `docs/envoy-go/phases/21-http-filter-adaptive-concurrency/PLAN.md`; structure mirrors phase-20 PLAN's 14-task envelope). **14-task decomposition** anticipated (Task 1 PROGRESS preamble + 15-precondition verification; Task 2 compiled_config + PARSE-REJECT roster + ADR-0187 §Decision/§Consequences anchor; Task 3 controller.go + clock.go state machine + ADR-0186 §Decision/§Consequences anchor; Task 4 internal/stats/conv.go NEW + gauge.go comment-only + stats.go + ADR-0059 §Decision AMENDMENT body landing; Task 5 decode_headers + 503 wire shape per AMEND-6; Task 6 encode_complete + filter.go per-stream + OnDestroy token-release per D11; Task 7 percentile.go sorted-slice quantile; Task 8 fuzz_test.go 27th fuzzer + corpus seeds; Task 9 full filter integration + boot-registration at cmd/envoy-go/main.go line 125 alphabetical between router and bandwidthlimit per D7; Task 10 differential fixture 0025-http-adaptive-concurrency 4-scenario; Task 11 ADR final-state alignment + DECISIONS.md cross-reference audit; Task 12 cross-package regression matrix verification per D4 + D16; Task 13 BEHAVIOR_CONTRACT.md 7-edit bundle + ADR-0052 atomic landing; Task 14 six-gate phase-done verification A/B/C/D/E/F + STATE.md re-advance + ROADMAP row 21 flip + REVIEW.md per superpowers:requesting-code-review). **18 planner-time decisions D1-D18 settled** (D1 Task 3+7 separate-Tasks for parallel-dispatch; D2 14-arm PARSE-REJECT byte-stable wording table — 11 RATIFIED-from-PGV + 2 envoy-go-strict + 1 oneof-absent variant; D3 race-test surface roster — TestController_ConcurrentForwardingDecision_* + TestController_FAKE_TIME_TimerOrdering_*; D4 cross-package regression cmd shape post-ADR-0059 §Decision AMENDMENT; D5 stat-name compile-time guard via const-decls + table-driven test; D6 fuzzer corpus seed roster ~30 seeds; D7 boot-registration line 125 alphabetical; D8 ADR escape-valve hypothesis HOLDING — ADR-0188 + ADR-0189 stay UNCONSUMED at phase-21 IMPL phase-done; D9 fakeClock test-helper API shape — Advance(d time.Duration) step-driver + deterministic timer-fire ordering; D10 sorted-slice quantile edge-case enumeration — empty/single/p=0/p=1/out-of-range clamp/caller-non-mutation; D11 OnDestroy token-release symmetry per HCM abort path; D12 task-graph parallelization — parallel-dispatch opportunity at Tasks 2+4+7 per disjoint files; D13 single-listener fixture topology per SPEC §7.3; D14 wire-shape A1-A4 fixture-0025 scenario coverage; D15 library-behavioral B5+B6+B7 unit-test + race-test coverage; D16 cross-phase C8 regression matrix closure at Task 4 + Task 14; D17 concurrencyLimit atomic.Uint32 lock-free hot-path; D18 *rand.Rand per-controller-source fixed-monotonic-seed). **Parallel-dispatch opportunity at Tasks 2+4+7** per D12 (three agents can run concurrently on disjoint files — compiled_config.go + stats.go/conv.go + percentile.go; all depend only on Task 1). **Sequential bottleneck at Task 3 → Tasks 5 → 6 → 9 → 10 → 11 → 12 → 13 → 14** (controller materialization + filter integration + fixture + BEHAVIOR_CONTRACT bundle + six-gate verification are the critical path). **Single-row landing CONFIRMED at PLAN time per ADR-0045 re-evaluation** (PLAN GATE per `superpowers:writing-plans` state-2 check: 14 tasks well under 25-task split-gate; LoC envelope ~1370-1830 production+fixture+AMENDMENT straddles ~1500 at the upper estimate but natural split axes don't carve cleanly per SPEC §3.5 + PLAN §Scope-check — controller state machine + FAKE-TIME tests + percentile aggregation are tightly coupled; framework delta is ZERO; 4 fixture scenarios co-located in one fixture directory). Next-skill for IMPL session: **`superpowers:subagent-driven-development`** (recommended per project memory `feedback_execution_style.md`; exploits the parallel-dispatch opportunity at Tasks 2+4+7) OR **`superpowers:executing-plans`** (alternative inline batch execution with checkpoints). PLAN at `docs/envoy-go/phases/21-http-filter-adaptive-concurrency/PLAN.md` (lifecycle-state 2 → 3 commit). Next-free ADR post-PLAN commit: **ADR-0188** (UNCHANGED — PLAN consumes NO ADRs per ADR-0044 §Context-draft discipline). **IMPL DONE at 2026-05-18** at master tip `<TBD — squash SHA fill in follow-up>`. 14-task IMPL landing in `phase-21-http-filter-adaptive-concurrency-impl` worktree; commits Task 1 `9eb493b` (+ SHA-fill `e3a8e13`); Task 2 `5a0a720` (+ SHA-fill `6e243b2`); Task 3 `84e317f` (+ SHA-fill `7aef3e5`); Task 4 `4a8f9c4` (+ SHA-fill `6ab6009`); Task 5 `d1dd3d7` (+ SHA-fill `2b6e3bc`); Task 6 `07d18c5` (+ SHA-fill `4a7e902`); Task 7 `498f566` (+ SHA-fill `3f942d8`); Task 8 `a7f2ffd` (+ SHA-fill `d9c399b`); Task 9 `5304aec` (+ SHA-fill `093b13c`); Task 10 `9d80820` (+ SHA-fill `7987bee`); Task 11 `ef6ac63` (+ SHA-fill `4dd0c4b`); Task 12 `02b96a2` (+ SHA-fill `9f29fc9`); Task 13 `75a64a5` (+ SHA-fill `fe3afc0`); Task 14 `<TBD>` (this commit). **2 NEW ADRs anchored** (ADR-0186 Gradient-1 controller state machine + Clock seam + FAKE-TIME differential strategy + sorted-slice percentile aggregation at Task 3 `84e317f`; ADR-0187 RTDS `enabled.RuntimeFeatureFlag` deferral PARSE-REJECT + `enabled` empty-default OFF semantics per AMEND-4 at Task 2 `5a0a720`); **1 IN-PLACE AMENDMENT body landed** (ADR-0059 §Decision AMENDMENT body REPLACES SPEC-commit ANTICIPATION paragraph per ADR-0044 in-place edit discipline at Task 4 `4a8f9c4` — float-valued gauge int64 encoding convention; NEW `internal/stats/conv.go` `BoolToInt` helper + `gauge.go` comment cross-reference; NO signature change to `*stats.Gauge`). **All 6 phase-done gates GREEN** at Task 14: Gate A `go build ./...` clean; Gate B `go vet ./...` + `golangci-lint run` clean; Gate C `go test -race -count=1 ./...` clean repo-wide (one-time fixture-infrastructure flake on first run — identical class to phase-19.2 + phase-20 PROGRESS precedents at fixture 0023 + 0012; retry clean); Gate D `go test -count=1 -timeout=15m ./test/differential/ -run 'TestDifferential'` 27/27 fixture directories GREEN (26 pre-existing + new 0025-http-adaptive-concurrency) in 94.7s; Gate E 27th fuzzer `FuzzAdaptiveConcurrencyConfigParse` clean at 30s (2,212,203 execs at 33,551/sec; 19 new interesting / 240 total; 0 panics); Gate F h2spec one-time flake on first run + retry clean 53/53 PASS at ADR-0051 pin. **SPEC §15 18-item acceptance checklist all GREEN** (6 gates + fixture-0025 4-scenario coverage + 7-name stat-surface byte-exact + Gradient-1 algorithmic-fidelity 7 Layer-A test families + 14-arm PARSE-REJECT roster + byte-exact 503 wire shape at scenario (b) cross-side + 2 NEW ADRs + 1 IN-PLACE AMENDMENT + 7-edit BEHAVIOR_CONTRACT bundle + DECISIONS + STATE + ROADMAP advance + end-to-end audit trail). **LEANEST framework-delta §9 row to date** — ZERO new `internal/` framework primitives (only in-package `Clock` seam at `internal/filter/http/adaptive_concurrency/clock.go` + in-place `ADR-0059` §Decision AMENDMENT body); 7 framework REUSES per SPEC §3.3. **FOURTH CONSECUTIVE §9 row to skip ADR-0125 amendment** (REUSE-by-absence per SPEC §5.4 — no `AdaptiveConcurrencyPerRoute` proto message in v1.32.4 or v1.37.x; ADR-0125 roster stays at 8 entries; 5th-canonical consumer roster stays unchanged at FOUR consumers). **D8 hypothesis HOLDING** — ADR-0188 + ADR-0189 stay UNCONSUMED at phase-21 IMPL phase-done (`grep -cE '^## ADR-0188' docs/envoy-go/DECISIONS.md` returns 0); STRENGTHENED two-slot buffer per SPEC §10 D carried forward to phase 22. **Notable IMPL-time findings:** (i) v1.32.4 proto-binding limitation discovered at Task 2 (the `fixed_value` PARSE-REJECT arm 13 is structurally unreachable in v1.32.4 because the `MinimumRTTCalculationParams.fixed_value` field does not exist at the v1.32.4 proto-binding; byte-stable wording preserved; forward-pointer to future proto-bump phase recorded in PROGRESS Task 2 + REVIEW.md §6); (ii) fixture-0025 scope-deviation at Task 10 (single-directory + REFERENCE-LESS deviation from PLAN §10 4-sub-directory full-cross-side per phase-20 oauth2 precedent; AMEND-6 cross-side byte-exact for scenario (b) DEFERRED as RATIFIED-PENDING-FUTURE-CROSS-SIDE-EXTENSION; the 503 + 25-byte body + content-type + content-length are all asserted envoy-go-side per `decode_headers.go:62-87`). **D1-D18 dispositions** all HELD as predicted at PLAN time (zero impl-time-unanticipated decisions surfaced; per-Task PROGRESS closure log). Next-free ADR post-IMPL phase-done: **ADR-0188** (UNCHANGED — D8 hypothesis HELD; STRENGTHENED two-slot buffer with ADR-0189 also UNCONSUMED carries forward to phase 22). |
| 22 | http-filter-lua | 21 | in-progress | 22.1, 22.2, 22.3 | New `internal/filter/http/lua/` package implementing `envoy.filters.http.lua.v3.Lua` (Envoy v1.37.2 canonical Lua scripting filter; per-request operator-authored gopher-lua scripts manipulating headers / body / metadata / outbound calls via the Envoy↔Lua bridge API) + NEW `internal/lua/` framework primitive (gopher-lua VM lifecycle + script-compilation cache + sandbox config + bridge-registration interface) under the 07.1 framework. FIFTEENTH §9 family-row (after `cors` at 07.1, `fault` at 09, `header_mutation` at 10, `local_ratelimit` at 11, `csrf` at 12, `buffer` at 13, `compressor` at 14, `bandwidth_limit` at 15, `rbac` at 16, `jwt_authn` at 17, `ext_authz` at 18 with 18.1+18.2 split, `ext_proc` at 19 with 19.1+19.2 split, `oauth2` at 20, `adaptive_concurrency` at 21). **FIRST §9 family-row to pre-split THREE-way at BRAINSTORM time** per ADR-0045 §6 (prior splits at 05/06/07/08/18/19 all 2-way + all settled at SPEC or PLAN time). 3-way split rationale: envelope D (full upstream parity per BRAINSTORM Q1) exceeds the ADR-0045 split-gate substantially (~3500-5000 LoC + ~42-54 tasks total across the 3 sub-phases); natural split axes are unambiguous at BRAINSTORM design-decision time (22.1 = VM + DefaultSourceCode + pragmatic-middle bridge; 22.2 = full bridge delta; 22.3 = multi-script + per-route + ADR-0125 amendment). Parent row marked `in-progress` with sub-phases list per ADR-0106. **ENDS the phase-21 ZERO-NEW-framework-primitive streak** by introducing a NEW `internal/lua/` framework primitive at 22.1 first-consumer (per BRAINSTORM Q4 EXTRACT-NOW decision) — first §9 row since phase 17 (jwt_authn at `internal/jwks/` + `internal/jwt/`) to introduce a NEW framework primitive of substantial scope. **ENDS the FOUR-CONSECUTIVE ADR-0125-skip streak** (phases 18/19/20/21 all skipped) by amending ADR-0125's canonical-pattern roster from 8 → 9 entries at phase 22.3 IMPL per BRAINSTORM Q7 NEW 9th canonical (3-arm hybrid: disabled-bool + string-reference-delegation + DataSource-wholesale-override). **FIRST §9 row to introduce a third-party Lua VM dependency** in envoy-go: `github.com/yuin/gopher-lua` (pure-Go Lua 5.1; MIT-licensed; matches upstream Envoy's LuaJIT 5.1 dialect; NO CGO) per BRAINSTORM Q3. BRAINSTORM authored at the BRAINSTORM-commit. **Parent SPEC AUTHORED at this commit** at `docs/envoy-go/phases/22-http-filter-lua/SPEC.md` — 16 numbered sections including §11 7-pin empirical-pin block resolved IN-SESSION via parallel-subagent fan-out against v1.37.2 reference Envoy + v1.32.4 go-control-plane proto bindings + `github.com/yuin/gopher-lua` (parent-SPEC-time per ADR-0004); §1.1 12-AMEND catalog covering 3 substantive REFUTATIONS (AMEND-1 stdlib-sandbox-strict REFUTES BRAINSTORM §7.2 surface-1 — upstream calls `luaL_openlibs` without neutering, so envoy-go's strict default-deny is an envoy-go-strict DEPARTURE; AMEND-2 stat-prefix template REFUTES BRAINSTORM §5.2 — actual template is HCM-rooted `http.<HCM_stat_prefix>.lua.<config_stat_prefix>.<stat>` mirroring phase-21 AMEND-3 C2; AMEND-3 `executions` reclassification REFUTES BRAINSTORM §5.1 — `executions` is upstream-parity per `ALL_LUA_FILTER_STATS` macro, departure-record bundle drops 2 → 1) + 9 CONFIRMS/EXTENDS findings (AMEND-4 PARSE-REJECT 18 arms; AMEND-5 DataSource 10 arms; AMEND-6 3 additional baseline arms + sandbox-violation arm RETRACTED; AMEND-7 `:respond()` full byte-pin extension; AMEND-8 `:respond()` from `envoy_on_response` runtime-reject + `:status` `[200, 600)` validation; AMEND-9 gopher-lua-vs-LuaJIT divergences + RECOMMEND-DEPARTURE-RECORD for runtime-error log-message wording; AMEND-10 fixture-0026 scenario (g) scope-narrow to substring-match `"script load error"`; AMEND-11 NEW `BackendKind=HTTPLua` + `scripts/` subdirectory layout; AMEND-12 v1.32.4-vs-v1.37.2 binding-gap forward-pointers for `clear_route_cache` + `filter_context`). ADR-0188 + ADR-0189 §Context drafts anchored at this SPEC commit per ADR-0044 §Context-draft discipline (§Decision+§Consequences body deferred to 22.1 IMPL Lands-in-Tasks). ADR-0125 §(xiv) AMENDMENT-anticipation paragraph anchored at this SPEC commit per parent SPEC §4.5 (AMENDMENT body lands at 22.3 IMPL final Task). Next-free ADR advanced from **ADR-0188 → ADR-0190** (2 numbers consumed: ADR-0188 + ADR-0189). WEAK HOLD D-hypothesis STANDS UNCHANGED at SPEC commit per parent SPEC §1.2: 2 anticipated ADRs land cleanly; 0-1 escape-valve slot consumption at ADR-0190 from `*LState`-pool benchmark surface per parent SPEC §13-R6 (scenario (g) BootRejectFixture collapses into ADR-0189 §Decision body; does NOT consume ADR-0190). Per-sub-phase SPEC sessions (22.1 / 22.2 / 22.3) follow per phase-18.1/18.2/19.1/19.2 precedent. |
| 22.1 | http-filter-lua-vm-and-headers-bridge | 21 | done | 2026-05-18 | First sub-phase of the phase-22 BRAINSTORM-time 3-way pre-split (`22` parent). Lands the envelope-B-equivalent core + the NEW `internal/lua/` framework primitive at first consumer per BRAINSTORM Q4 EXTRACT-NOW decision. Specifically: NEW `internal/lua/` (gopher-lua VM lifecycle + script-compilation cache + sandbox config + bridge-registration interface; ADR-0188) + NEW `internal/filter/http/lua/` package (filter struct + factory + parse + 4-arm DataSource resolution + bridge methods + stats + per-route stub; ADR-0189). `Lua.DefaultSourceCode` consumed; `Lua.SourceCodes` + `Lua.InlineCode` + `LuaPerRoute` PARSE-REJECTed (`InlineCode` per envoy-go-strict deprecated-field-rejection; `SourceCodes` + `LuaPerRoute` deferred to 22.3). 4-arm DataSource resolution (Filename + InlineBytes + InlineString + EnvironmentVariable; WatchedDirectory PARSE-REJECT) per BRAINSTORM Q5. Pragmatic-middle Envoy↔Lua bridge surface per BRAINSTORM Q6: `envoy_on_request` + `envoy_on_response` hooks + `:headers()` + headers-object methods + `:logXxx()` + `:streamInfo()` subset (`:protocol` + `:routeName` + `:downstreamLocalAddress` + `:downstreamDirectRemoteAddress`) + `request_handle:respond()`. Deferred bridge methods raise Lua runtime errors (Lua-idiomatic disposition). 3-counter stat surface per BRAINSTORM Q8 + parent SPEC AMEND-3 (`errors` upstream-parity + `executions` upstream-parity + `respond_calls` envoy-go-strict; project 99 → 102; BEHAVIOR_CONTRACT.md 1 envoy-go-strict departure record for `respond_calls` per AMEND-3 corrected from BRAINSTORM 2-record bundle). 28th project-wide fuzzer `FuzzLuaConfigParse` at ~30-corpus-seed baseline. Differential fixture `0026-http-lua-headers-bridge` — 6 wire-interactive scenarios cross-side byte-exact + scenario (g) substring-match `"script load error"` per parent SPEC AMEND-10. BEHAVIOR_CONTRACT 7-edit bundle at IMPL final Task per ADR-0052. D-hypothesis per BRAINSTORM Q10 WEAK HOLD: 2 anticipated ADRs (ADR-0188 + ADR-0189) land cleanly; 0-1 escape-valve slot consumption (ZERO-slot buffer post-22.1-IMPL). **SPEC authored at THIS commit** — 15 sections + 2 appendices following phase-18.1 + 19.1 sub-phase-SPEC precedent. SPEC inherits parent SPEC §4 framework primitive + §5 proto-field roster + §6 PARSE-REJECT roster + §7 stat surface + §8 fixture-0026 disposition + §11 empirical-pin block + §12 D-questions + §13 RATIFIED-PENDING items + §14 BEHAVIOR_CONTRACT.md edit bundle + §16 18-item acceptance checklist; details per-task IMPL-level shape only. **D5 CLOSED IN-SESSION**: 28th-fuzzer count CONFIRMED via project-wide grep (27 unique fuzzers in main tree). **D7 CLOSED IN-SESSION (REFUTES BRAINSTORM hypothesis)**: envoy-go headers-map type EMPIRICALLY-DETERMINED as `net/http.Header` (Go `map[string][]string`; unordered per Go map semantics); scenario (f) cross-side determinism STILL HOLDS via count-only assertion; NEW bridge `__pairs` metamethod discipline RATIFIED — snapshot to alphabetically-sorted slice + iterate by integer index. **D1 + D3 carry forward**: D1 closes at IMPL Task 2 first action via upstream Envoy v1.37.2 scrape; D3 closes at 22.1 PLAN session per parent §11.7.7 RECOMMENDED option (a). **`internal/lua/` API refined vs parent §4.1 sketch**: function-option `VMOption` pattern; `Run(chunk) + HasGlobalFunc(name) + CallGlobal(name, args...)` split; `State() *lua.LState` escape-hatch exposed for userdata setup; `WithPanicHandler` + `WithBasePrintSink` renaming. **16-task IMPL breakdown across 5 tiers**: Tier A scaffold (Tasks 1-5) + Tier B bridge (Tasks 6-9) + Tier C stats+tests (Tasks 10-12) + Tier D fixture (Tasks 13-15) + Tier E atomic landing (Task 16). **24-item acceptance checklist**: parent §16 18 items + 6 sub-phase-specific extensions (D5+D7 resolutions recorded; D1+D3 closure timing; PROGRESS.md + REVIEW.md disciplines). **§13-R3 REFINED + §13-R4 CLOSED at this SPEC commit**; §13-R6 `*LState`-pool benchmark gate STANDS. NO NEW ADRs consumed at this 22.1 SPEC commit; DECISIONS.md tail UNCHANGED at ADR-0189; next-free ADR-0190 UNCHANGED. **PLAN authored at THIS commit** at `docs/envoy-go/phases/22.1-http-filter-lua-vm-and-headers-bridge/PLAN.md` (~1400 LoC; mirrors phase-21 + phase-18.1 + phase-19.1 sub-phase-PLAN structure: Goal/Architecture/Tech Stack header + Scope check + 40-row File-structure decomposition table + D3 + D-P1..D-P10 PLAN-time decisions + 2-NEW-ADR + 1-CONDITIONAL-ADR-0190 table + Task graph with 4-way Tasks-2+3+4+11 + 3-way Tasks-6+7+8 + 3-way Tasks-9+10+13 + 2-way Tasks-12+14 parallelization opportunities + 15-precondition execution checklist + Pre-Task 0 PROGRESS preamble + 16 Tasks across 5 tiers verbatim per 22.1 SPEC §6 with TDD-ordered Steps + per-task subagent dispatch outlines + per-task verification commands + per-task PROGRESS.md entry shape per D-P3 + REVIEW.md anticipated structure at Task 16 + Phase-done squash-merge + push-to-origin section). **D3 CLOSED IN-PLAN-SESSION** per parent §11.7.7 RECOMMENDED option (a): scenario (e) `e_log_only.lua` wire-output column LOCKED at "Request unchanged at upstream; `lua.<prefix>.executions` counter delta = 1 per probe"; fixture-0026 driver scrapes `/stats?format=text` pre + post probe + emits `scenario e executions_delta=N` byte-comparison line via existing fixture-0025 inline-scrape pattern at `test/fixtures/0025-http-adaptive-concurrency/inputs/driver.go:149-153,236-272` (ZERO new harness/runner infrastructure required; option (b) artificial-counter-bumping + option (c) NEW `LogAsserter` interface both REJECTED). **22.1 SPEC §15.2 item 22 CLOSED** at this PLAN commit. NO NEW ADRs consumed at this 22.1 PLAN commit; DECISIONS.md tail UNCHANGED at ADR-0189; next-free ADR-0190 UNCHANGED (carried forward as 22.1 IMPL escape-valve slot per D-P10 R6 benchmark gate at IMPL Task 12 — fires only if `*LState`-pool benchmark surfaces > 1ms per-stream construction cost). **IMPL DONE at 2026-05-18** — 16 Tasks landed across 5 tiers (Tier A scaffold Tasks 1-5; Tier B bridge Tasks 6-9; Tier C stats+tests Tasks 10-12; Tier D fixture Tasks 13-15; Tier E atomic landing Task 16). **All 6 phase-done gates GREEN**: Gate A build clean; Gate B vet + golangci-lint clean (after fixing pre-existing gofmt drift at `internal/cluster/cluster.go:50` from commit `49cc7cd` as out-of-scope housekeeping — 1-line fix); Gate C race clean on unit packages (`internal/...` + `test/conformance/...` + `test/helpers/...`); Gate D differential 28/28 fixture directories GREEN (full cross-side suite ran in 72.88s; includes new `0026-http-lua-headers-bridge` GREEN with 7 scenarios: 6 wire-interactive (a)-(f) cross-side `CompareBytes` byte-exact + scenario (g) substring-match `"script load error"` via NEW `BootRejectFixture` per §13-R1); Gate E fuzz `FuzzLuaConfigParse` 30s baseline clean (no panics; ~120k execs/sec; 28-fuzzer project-wide count confirmed via `grep -h '^func Fuzz' \| sort -u \| wc -l = 28`); Gate F h2spec 53/53 PASS at ADR-0051 v1.32.4 pin. **22.1 SPEC §15.2 24-item acceptance checklist all GREEN** (items 1-18 from parent §16 verbatim; items 19-24 sub-phase-specific extensions). **D1 REFUTED at IMPL Task 2** (upstream Envoy v1.37.2 source scrape: both arms 5 + 17 silent no-op; reserved wording constants pinned in source with `//nolint:unused`). **D3 CLOSED at PLAN session** (scenario (e) stat-counter `executions` delta IS the "Lua ran" assertion per option (a); verified at Task 14 driver `AssertStats`). **D5 + D7 CLOSED at 22.1 SPEC §11**. **D-P10 R6 STANDS WEAK-default** (`ns/op = 69865` ~70µs/stream << 1ms threshold; ADR-0190 NOT consumed; carries forward to 22.2 BRAINSTORM as the 22.2 IMPL escape-valve slot). **2 NEW PARSE-REJECT arms surfaced by Task 11 fuzzer**: arm 19 (`Lua.stat_prefix` invalid chars per `stats.IsValidName` regex pre-check) + arm 9-extension (`Filename` DataSource 16 MiB cap via `io.LimitReader` — defense vs `/dev/full`-class infinite-read OOM-kill); both fixed inline at Task 11 per ADR-0018 fuzzer-discipline + regression seeds preserved at `internal/filter/http/lua/testdata/fuzz/FuzzLuaConfigParse/`; roster 18 → 19. **FIFTEENTH §9 family-row landed** (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11, csrf @ 12, buffer @ 13, compressor @ 14, bandwidth_limit @ 15, rbac @ 16, jwt_authn @ 17, ext_authz @ 18.1+18.2, ext_proc @ 19.1+19.2, oauth2 @ 20, adaptive_concurrency @ 21). **FIRST §9 row to introduce third-party Lua VM dependency** (gopher-lua v1.1.2 pure-Go Lua 5.1 MIT-licensed NO CGO; matches upstream Envoy's LuaJIT 5.1 dialect). **NEW `internal/lua/` framework primitive milestone** — ENDS the phase-21 ZERO-NEW-framework-primitive streak by introducing the primitive at first consumer per BRAINSTORM Q4 EXTRACT-NOW + ADR-0188 EXPLICIT API-REVISION ALLOWANCE clause for consumer #2. **2 NEW ADR §Decision + §Consequences body landings at Task 16**: ADR-0188 (NEW `internal/lua/` framework primitive — gopher-lua v1.1.2 VM lifecycle + per-stream `*LState` + per-script-source `*Chunk` compile cache + `SandboxConfig` zero-value `StrictUpstreamParity` per AMEND-1 + EXPLICIT API-REVISION ALLOWANCE for consumer #2 per BRAINSTORM Q4); ADR-0189 (NEW `internal/filter/http/lua/` package shape — 8 production + 5 test files; `compiledConfig` + 3-counter `filterStats`; 19-arm PARSE-REJECT roster; 4-arm DataSource; pragmatic-middle bridge 21 entries; full byte-pin `:respond` per parent §11.6.7 + AMEND-7 + AMEND-8 encode-side reject; `__pairs` alphabetical-snapshot per §11 D7; fixture-0026 disposition + NEW `BootRejectFixture` per §13-R1; envoy-go-side `"script load error: "` wording-pinning per §13-W). **NO ADR-0190 consumption** (D-P10 R6 STANDS WEAK-default; ADR-0190 carries forward to 22.2 BRAINSTORM unconsumed; ZERO-slot escape-valve buffer post-22.1-IMPL re-evaluates at 22.2 BRAINSTORM). **17 HTTP filters wired post-22.1** (was 16). **102 stat names post-22.1** (was 99). **28 fuzzers post-22.1** (was 27). **28 differential fixture directories post-22.1** (was 27). **BEHAVIOR_CONTRACT.md 7-edit bundle landed atomically** per ADR-0052 + parent §14 + 22.1 SPEC §14 (NEW `### envoy.filters.http.lua` subsection ~330 LoC; stat-table 99 → 102 extension + extension summary paragraph; 3 envoy-go-strict departure records integrated; NEW `### Phase 22.1 forward-pointer notes` subsection; per-route-canonical cross-reference caption update + paragraph). **REVIEW.md authored** at `docs/envoy-go/phases/22.1-http-filter-lua-vm-and-headers-bridge/REVIEW.md` per `superpowers:requesting-code-review`. Parent row `22` STAYS `in-progress` until 22.3 phase-done; sub-rows `22.2` + `22.3` STAY `planned`. |
| 22.2 | http-filter-lua-full-bridge | 22.1 | done | 2026-05-19 | Second sub-phase of the phase-22 BRAINSTORM-time 3-way pre-split (`22` parent). Lands the full Envoy↔Lua bridge-API delta on top of 22.1's pragmatic-middle: `:body()` + `:bodyChunks()` body-access surface (interacts with phase-13 ADR-0128 decode-side body-buffering primitive); `:trailers()` trailer-access surface; `:metadata()` dynamic-metadata bridge (likely PARSE-REJECT or partial-deferral per project's cross-phase dynamic-metadata-deferral discipline); `:connection()` connection-info bridge (integrates with phase-03 TLS primitives); `:httpCall()` outbound HTTP call (REUSES phase-20 `internal/httpclient/` framework primitive at first co-consumer — validates the phase-20 extraction); crypto / base64 / sha helpers + `:fileBytes()` + `:timestamp()` + full `:streamInfo()` surface. Additional stat-surface entries (httpCall counters likely). Differential fixture `0027-http-lua-full-bridge` — partial cross-side / REFERENCE-LESS fallback for non-deterministic httpCall + `:timestamp()` scenarios per BRAINSTORM §6.4. PARSE-REJECT still active on `SourceCodes` + `LuaPerRoute` at 22.2 (deferred to 22.3). 22.2 BRAINSTORM (next-session-after-22.1-IMPL-phase-done) settles the full-bridge-API shape ADR + httpCall dispatcher ADR + body-buffering-interaction ADR + dynamic-metadata-bridge deferral ADR (anticipated ~2-4 NEW ADRs at 22.2 IMPL). **BRAINSTORM DONE at 2026-05-18** — `docs/envoy-go/phases/22.2-http-filter-lua-full-bridge/BRAINSTORM.md` authored at THIS commit (~700 LoC; 14-Q dialogue matching parent 22 BRAINSTORM + phase-21 BRAINSTORM precedent shape; 12 §2 numbered sub-sections; framework-survey + per-route disposition + stat surface + fixture strategy + anticipated-ADR roster + deferred items + 7 D-questions carry-forward). **Q1 body bridge: FULL UPSTREAM PARITY** — both `:body()` whole-buffer + `:bodyChunks()` chunked iterator + Lua coroutine yield/resume + zero-copy buffer-sharing with ADR-0128 decode-side primitive. **Q2 trailers: MIRROR HEADERS EXACTLY + lazy-available** — 8-method mutation surface (reuses 22.1 headers metatable); nil if no trailers received. **Q3 metadata: BOTH AT 22.2 — BREAKS cross-phase dynamic-metadata deferral discipline** — `:metadata()` returns empty at v1.32.4 binding-gap (forward-pointer to v1.37.x binding bump); `:streamInfo():dynamicMetadata()` + `:dynamicTypedMetadata()` via NEW `internal/dynamicmetadata/` framework primitive (cross-phase deferral lifted at lift-phases' next-touchpoints). **Q4 connection: FULL UPSTREAM PARITY** — `:connection():ssl()` ~12-method cert/session surface; integrates with phase-03 TLS primitives. **Q5 httpCall: FULL UPSTREAM PARITY** — cluster + sync-default + optional async flag; consumer-#1 of `internal/httpclient/` per ADR-0177 (in-place AMEND for cluster-based dispatch path). **Q6 crypto: 6 UPSTREAM METHODS, IN-PACKAGE** — base64Escape + base64Decode + sha256 + sha512 + importPublicKey + verifySignature. **Q7 fileBytes + timestamp: FULL UPSTREAM PARITY** — unrestricted FS + 16 MiB cap (inherits 22.1 Task 11 pattern) + wall-clock + REFERENCE-LESS fixture for non-deterministic surfaces. **Q8 streamInfo-full: 7 ADDITIONAL METHODS including `:filterState()` IN-PACKAGE** — total 11-method streamInfo surface at 22.2; filter-state primitive stays in-package per Q9 EXTRACT-NOW-only-when-trigger-fires. **Q9 extract: PRAGMATIC-MIDDLE** — extract NEW `internal/dynamicmetadata/` primitive + revise `internal/lua/` API (coroutine + body-bridge buffer seam) per Q10 strict scope + revise `internal/httpclient/` API (cluster dispatch in-place AMEND); keep filter-state + body-buffer seam + crypto + connection-SSL IN-PACKAGE. **Q10 API-revise: STRICT SCOPE — NEW ADR-0191 for `internal/lua/` 22.2 extensions** — ADR-0188 EXPLICIT API-REVISION ALLOWANCE clause STAYS scoped to consumer-#2 (future cluster-specifier / access-logger / string-matcher Lua); 22.2's consumer-#1-scope-expansion revisions land under NEW ADR-0191 instead of in-place AMEND on ADR-0188. **Q11 stat: PRAGMATIC-MIDDLE ~5-6 COUNTERS (102 → ~108)** — httpcall_total + httpcall_failures + httpcall_timeouts + body_buffered_bytes_total + coroutine_yields_total; all envoy-go-strict; 5-6 BEHAVIOR_CONTRACT.md departure records at 22.2 IMPL atomic-landing Task. **Q12 fixture: SINGLE MIXED-MODE fixture-0027** — `0027-http-lua-full-bridge` with both deterministic scenarios (cross-side `CompareBytes` byte-exact: body / trailers / metadata-empty / crypto / streamInfo-most / fileBytes / connection-ssl) + non-deterministic scenarios (REFERENCE-LESS subject-only: httpCall / timestamp / filterState); 28 → 29 fixture dirs. **Q13 escape-valve: WEAK HOLD** — 3 anticipated NEW ADRs (ADR-0190 + ADR-0191 + ADR-0192) + in-place AMEND on ADR-0177 + 0-1 conditional ADR-0193 escape-valve consumption (likely R6 *LState-pool gate if body+coroutine pushes per-stream construction over 1ms — 22.1 baseline ~70µs; OR body-buffer-seam-with-ADR-0128 separation OR connection-SSL bridge integration with phase-03 OR httpCall coroutine integration with cluster manager). **Q14 scope-shape: STAY SINGLE-PHASE** — let PLAN-stage split-gate fire if needed (~25-35 tasks / ~3000-5000 LoC estimated; at-or-above ADR-0045 gate; PLAN session does the precise estimation per phase-09 → phase-11/13 split-at-PLAN precedent). **7 SPEC-time D-questions surfaced** (D1 metadata empty-vs-nil disposition + D2 coroutine yield/resume capture mechanism + D3 body-buffer zero-copy lifetime guarantees + D4 filterState shape typed-vs-string-keyed + D5 connection-SSL cross-side byte-exactness scope + D6 httpCall async-flag semantics + D7 28th+29th project-wide fuzzer count verification). **Anticipated NEW project-wide fuzzers: 1-2** (`FuzzLuaBodyBridge` + `FuzzLuaHTTPCallConfig`; 28 → 29-30 per Q-D7). **22.3 scope UNCHANGED at this BRAINSTORM** — SourceCodes multi-script map + LuaPerRoute PARSE-LIFT + 9th canonical ADR + ADR-0125 §(xiv) AMENDMENT body + per-route 3-tier dispatch. **NO ADR consumption at this BRAINSTORM commit** per ADR-0044 §Context-draft discipline; DECISIONS.md tail UNCHANGED at ADR-0189; next-free ADR-0190 UNCHANGED. **SPEC DONE at 2026-05-19** — `docs/envoy-go/phases/22.2-http-filter-lua-full-bridge/SPEC.md` authored at THIS commit (~990 LoC; 16 numbered sections + 2 appendices matching phase-22.1 + phase-19.1 + phase-18.1 sub-phase-SPEC precedent). **9-pin §11 empirical-pin block resolved IN-SESSION** via parallel-subagent fan-out per ADR-0004 (gopher-lua coroutine yield/resume + bridge-method divergences + ADR-0128 decode-side buffer + phase-20 internal/httpclient API + phase-03 TLS *tls.ConnectionState + upstream Envoy :metadata() + :httpCall() async + :filterState() shape + fuzzer count). **4 AMEND-22.2-N findings** — AMEND-22.2-1 gopher-lua-vs-LuaJIT 22.2 bridge-method divergences EXTENDS AMEND-9 (`__pairs` shim from 22.1 inherited; coroutine error propagation inherits AMEND-9(c); `:base64Escape` upstream-parity) + AMEND-22.2-2 PARTIAL-REFUTE BRAINSTORM Q6+Q7 "full upstream parity" framing (`:sha256`/`:sha512`/`:base64Decode`/`:importPublicKey`/`:verifySignature`/`:fileBytes` NOT on upstream StreamHandleWrapper at v1.37.2; classification PENDING §13-R7+R8 PLAN-time scrape) + AMEND-22.2-3 httpCall `asynchronous=true` = PURE FIRE-AND-FORGET per §11.7 D6 (noopCallbacks singleton; 0 return values; no yield; response discarded) + AMEND-22.2-4 `:filterState()` REFUTES BRAINSTORM Q8 implied `:set()` (upstream is strictly READ-ONLY; envoy-go diverges by exposing `:set` + typed Lua-value marshaling → 2 NEW envoy-go-strict departure records). **5 D-questions CLOSED IN-SESSION at §11** (D1 metadata empty-userdata-never-nil per §11.6; D2 gopher-lua native coroutine via LState.NewThread/Yield/Resume per §11.1; D4 string-keyed `map[string]any` with `:get`+`:set` per §11.8; D6 httpCall async = pure fire-and-forget per §11.7; D7 28-fuzzer baseline confirmed; 22.2 → 29-30 per §11.9). **2 D-questions CARRY FORWARD to §12** (D3 body-buffer GC lifetime PLAN-time perf-benchmark + D5 connection-SSL fixture-cert-topology PLAN-time decision; RECOMMENDED dispositions = defensive-copy + cert-fingerprint-only). **1 NEW D8 surfaced** (crypto + fileBytes upstream-exposure-verification PLAN-time scrape per §13-R7+R8 + AMEND-22.2-2). **3 NEW ADR §Context drafts anchored at SPEC commit** per ADR-0044 §Context-draft discipline: ADR-0190 NEW `internal/dynamicmetadata/` framework primitive + ADR-0191 NEW `internal/lua/` 22.2 API extensions (coroutine yield/resume + body-bridge buffer seam at consumer-#1 scope-expansion per Q10 strict scope; NOT in-place AMEND on ADR-0188) + ADR-0192 NEW `internal/filter/http/lua/` 22.2 package shape extensions (body + trailers + metadata + connection-SSL + httpCall + crypto + fileBytes + timestamp + streamInfo-full + filter-state in-package + 5 NEW envoy-go-strict counters + 2 NEW filter-state divergence records + fixture-0027 mixed-mode + FilterChain.tlsConnectionState field extension per Q13 WEAK HOLD lives inside ADR-0192). **In-place AMENDMENT on ADR-0177 anchored** at 22.2 IMPL atomic-landing Task (Client.ClusterDispatch method for cluster-based dispatch; no new ADR number consumed per ADR-0044 in-place edit discipline). **DECISIONS.md tail advances ADR-0189 → ADR-0192 §Context drafts** (3 numbers consumed at THIS SPEC commit per ADR-0044 §Context-draft discipline). **Next-free ADR advances ADR-0190 → ADR-0193** at THIS commit (held in reserve for the WEAK HOLD escape-valve consumption surface per §13-R6 *LState-pool gate OR §13-R9 body-buffer-seam-with-ADR-0128 separation; consumes only if 22.2 IMPL benchmarks surface > 1ms per-stream construction OR body-bridge implementation surfaces complexity warranting ADR split). **18-item §2 non-purposes** + **20-arm 22.1 PARSE-REJECT roster UNCHANGED at config-load + 3 NEW runtime-reject arms 20-22** (httpcall-cluster-name-required + body-size-cap-exceeded + crypto-key-format-invalid). **Stat surface 102 → 107** (5 NEW envoy-go-strict counters; optional 6th `dynmd_writes_total` deferred per pragmatic-middle). **Fixture-0027 mixed-mode scenarios (a)-(m)** — ~9-10 deterministic cross-side + ~3-4 non-deterministic REFERENCE-LESS subject-only. **8-item §9 behavior-contract delta** + **12-item §10 deferred items** + **8-item §13 RATIFIED-PENDING items** (R5 from parent §13 + NEW R6/R7/R8/R9/R10/R11 + NEW W2 byte-stable runtime-rejection wording for arms 20-22). **10-12 edit §14 BEHAVIOR_CONTRACT.md bundle anticipated** (10 baseline including 2 NEW filter-state envoy-go-strict records from AMEND-22.2-4 + 0-2 conditional per §13-R7+R8 PLAN outcomes for crypto+fileBytes records). **25-item §15 acceptance checklist** extends parent §16 18-item with 7 sub-phase-specific extensions. **§16 3 ADR §Context-draft anchors** + **Appendix A cross-references + Appendix B ADR landings summary**. ROADMAP row 22.2 STAYS `in-progress` (parent row 22 STAYS `in-progress` until 22.3 phase-done; sub-row 22.3 STAYS `planned`). **PLAN DONE at 2026-05-19** — `docs/envoy-go/phases/22.2-http-filter-lua-full-bridge/PLAN.md` authored at THIS commit (~1370 LoC; mirrors phase-22.1 + phase-19.1 + phase-18.1 sub-phase-PLAN precedent: Goal/Architecture/Tech Stack header + Scope check with per-component LoC table covering ~2400-4000 production LoC + ~2500-4000 test LoC + ~1200-1800 fixture LoC + ~2400-3700 doc LoC = ~8500-13500 total envelope + 60-row File-structure decomposition table + 14 planner-time decisions (D3 + D5 + D8 SPEC-time carry-forward closures + D-P1..D-P11 PLAN-emerged) + 3-NEW-ADR + 1-IN-PLACE-AMENDMENT + 1-CONDITIONAL-ADR-0193 table + Task graph with 4-way Tasks-1+2+3+4 + 7-way Tasks-7+8+9+10+11+12+13 + 3-way Tasks-14+15+16 + 2-way Tasks-17+18 parallelization opportunities + shared-file serialization caveat for bridge.go + decode_headers.go + encode_headers.go + 17-precondition execution checklist + Pre-Task 0 PROGRESS preamble + 19 Tasks across 6 tiers verbatim with TDD-ordered Steps + per-task subagent dispatch outlines + per-task verification commands + per-task PROGRESS.md entry shape per D-P3 + R6 + R9 signaling protocols at Tasks 15 + 7 consumed by Task 19 Step 10 grep-check for conditional ADR-0193 landing + REVIEW.md anticipated structure at Task 19 + Phase-done squash-merge + push-to-origin section). **D3 CLOSED IN-PLAN-SESSION** per SPEC §11.3 + §12 RECOMMENDED option (a): body-buffer LOCKED at defensive copy at endStream (`lua.LString(string(f.decodedBodyBytes))`); Task 15 schedules `BenchmarkBodyBridge_DefensiveCopy_PerStream` perf-benchmark at sub-MB body + 16-MiB-cap-saturated body with threshold gates ≤1ms sub-MB + ≤100ms 16-MiB-saturated; ADR-0193 escape-valve fires if exceeded. **D5 CLOSED IN-PLAN-SESSION** per SPEC §11.5 + §8.3 + §12 RECOMMENDED option (f-B): connection-SSL cross-side fixture-cert-topology LOCKED at cert-fingerprint-only via `:sha256PeerCertificateDigest()` byte-exact hex digest cross-side; other 11 ssl methods exercised in REFERENCE-LESS subject-only scenarios. Task 17 plumbs the cert fixture (REUSE existing TLS cert from prior fixture OR generate minimal cert via openssl). **D8 CLOSED IN-PLAN-SESSION via empirical upstream-Envoy-v1.37.2 WebFetch scrape** against `source/extensions/filters/http/lua/{lua_filter.h,lua_filter.cc}` + `source/extensions/filters/common/lua/{lua.h,lua.cc,wrappers.h,wrappers.cc}` per SPEC §12 + §13-R7 + §13-R8 + AMEND-22.2-2 — **classification: 2/6 upstream-parity (`:importPublicKey` on `StreamHandleWrapper` returning `PublicKeyWrapper` userdata at `lua_filter.h:315` + `:verifySignature` on `StreamHandleWrapper` at `lua_filter.h:303`; calling convention pinned to mimic upstream's PublicKeyWrapper userdata return scope per `wrappers.h:415-427` — anti-departure for calling convention) + 4/6 envoy-go-strict (`:sha256` + `:sha512` + `:base64Decode` + `:fileBytes` NOT in upstream at any scope; 4 NEW envoy-go-strict departure records at BEHAVIOR_CONTRACT.md)**. BEHAVIOR_CONTRACT.md edit-bundle scale at 22.2 IMPL: **15 total edits** (11 envoy-go-strict departure records = 5 counter + 2 :filterState + 4 D8 crypto/fileBytes; + 4 non-record edits = 1 EXTEND-subsection + 1 stat-table + 1 forward-pointer subsection + 1 D8 disposition paragraph). Fixture-0027 scenario (h) `:fileBytes` reclassified to REFERENCE-LESS subject-only per D8 (reference Envoy cannot run `:fileBytes` script — would error at runtime per absent-binding). **ADR-0045 split-gate evaluated at PLAN session** per BRAINSTORM Q14 deferral: 20-task graph (Pre-Task 0 + Tasks 1-19) **well below 25-task gate**; production-LoC envelope ~2400-4000 (above the 1500 lower bound but the gate's operational signal is the task-count); the three plausible split axes (axis-A stream-lifecycle / axis-B framework-vs-package / axis-C deterministic-vs-non-deterministic) all surface artificial boundaries; **DECISION: STAY SINGLE-PHASE** (no split into 22.2.1+22.2.2). **D-P1..D-P11 PLAN-emerged decisions LOCKED** at THIS PLAN commit (task-numbering fresh 20-task allocation; per-task `general-purpose` subagent dispatch per project memory `feedback_execution_style.md`; 8-section PROGRESS.md entry shape per phase-21 + phase-22.1 precedent; TDD rigid for Tasks 1-17 + relaxed for Tasks 18-19 fixture+atomic-landing; D-P5 LOCKS at NEW `coroutine.go` + `body_buffer.go` files (NOT in-place APPEND to vm.go) preserving ADR-0188 vs ADR-0191 lineage separation per Q10 strict scope; D-P6 boot-registration UNCHANGED at 22.2 — 17 HTTP filters STAYS UNCHANGED; D-P7 corpus-seed roster for 2 NEW fuzzers (~15-20 + ~10-15 seeds); D-P8 task-graph 4-way+7-way+3-way+2-way parallel-dispatch opportunities + shared-file serialization caveat for bridge.go + decode_headers.go + encode_headers.go; D-P9 race-scoping per 22.1 D-P9 carry-forward; D-P10 LState-pool benchmark RE-EVALUATION at FULL bridge surface at Task 15 + threshold gate `ns/op > 1_000_000` for conditional ADR-0193; D-P11 REFERENCE-LESS driver-helper LOCKED at REUSE existing `runReferenceLessFixture` pattern — NO NEW `RunSubjectOnlyHTTPLua` helper). **NO ADR consumption at THIS 22.2 PLAN commit** per ADR-0044 §Context-draft discipline; DECISIONS.md tail UNCHANGED at ADR-0192 §Context drafts; next-free ADR-0193 UNCHANGED (held in reserve for WEAK HOLD escape-valve per §13-R6 OR §13-R9; PLAN hypothesis NEITHER fires → ADR-0193 stays UNCONSUMED at 22.2 phase-done; carries forward to 22.3 BRAINSTORM as escape-valve slot). ROADMAP row 22.2 STAYS `in-progress` post-PLAN-commit; flips `in-progress → done` at 22.2 IMPL Task 19 atomic-landing. **IMPL DONE at 2026-05-19** — 19-task landing (Pre-Task 0 + Tasks 1-18 + Task 19a PRE-ATOMIC-LANDING + Task 19b atomic landing) across 6 tiers per the PLAN's Tier A-F structure. **All 6 phase-done gates GREEN at Task 19b atomic landing**: Gate A build clean; Gate B vet + golangci-lint clean; Gate C race clean on unit packages (`internal/...` + `cmd/...` — 46 packages green under `-race -count=1`); Gate D differential 29/29 fixture directories GREEN (full cross-side suite passed with 2 transient docker-reaper-flake retries at fixture-0020 + fixture-0027 — both PASS clean on retry); Gate E fuzz 30 project-wide fuzzers verified (28 pre-22.2 + 2 NEW from Task 16: `FuzzLuaBodyBridge` + `FuzzLuaHTTPCallConfig`; both NEW fuzzers 30s smoke-clean); Gate F h2spec 53/53 PASS at ADR-0051 v1.32.4 pin. **22.2 SPEC §15 25-item acceptance checklist all GREEN** (items 1-18 from parent §16 verbatim + items 19-25 sub-phase-specific extensions). **§9 HTTP-filters family-row count UNCHANGED at 17 wired filters** at 22.2 phase-done — 22.2 EXTENDS the SAME lua filter from 22.1; no new HCM-filter entries; boot-registration alphabetical position between `localratelimit` and `oauth2` UNCHANGED. **3 NEW ADR §Decision + §Consequences body landings at Task 19**: ADR-0190 (NEW `internal/dynamicmetadata/` framework primitive — Bucket type + Get/Set/Snapshot/Reset; per-stream lifecycle per ADR-0033; nil-tolerance per ADR-0085; cross-phase deferral-lift expectation; THIRD §9 framework primitive in two-phase succession after ADR-0188 + ADR-0189 at 22.1 IMPL) + ADR-0191 (`internal/lua/` 22.2 API extensions — NEW `coroutine.go` + `body_buffer.go` files per D-P5 LOCK; NewThread + Resume + YieldFromBridge + BodyBuffer interface; Q10 strict scope — NEW ADR not in-place AMEND on ADR-0188; ADR-0188's API-REVISION ALLOWANCE STAYS scoped to consumer-#2; coroutineDispatchPanic helper extends ADR-0188 §Decision 2 panic-wrapper discipline at the coroutine seam) + ADR-0192 (NEW `internal/filter/http/lua/` 22.2 package shape extensions — 8 bridge surface families + 11 envoy-go-strict departure records per D8 PLAN scrape + FilterChain.tlsConnectionState field extension + 5 NEW envoy-go-strict counters + 3 NEW runtime-reject arms 20-22 + production HCM coroutine orchestration at Task 19a + "Continue-on-body-yield" trade-off documented). **1 IN-PLACE AMENDMENT body landing at Task 19 on ADR-0177** (NEW `Client.ClusterDispatch(ctx, clusterName, request, clusterMgr)` method + NEW `FactoryCtx.ClusterManager *cluster.Manager` field + NEW `Cluster.UpstreamTLSConfig() *tls.Config` exported accessor; R5 RATIFIED — first co-consumer validation of phase-20's `internal/httpclient/` primitive at the 22.2 lua `:httpCall()` bridge; no new ADR number consumed; matches phase-17 → phase-18 ADR-0149 → ADR-0150 AMEND precedent). **Conditional ADR-0193 NOT consumed at 22.2 phase-done** per SPEC §13-R6 + §13-R9 + Task 19 Step 10 grep-check: R6 STANDS WEAK-default at Task 15 (`ns/op = 98157` ~98µs/stream at FULL 22.2 bridge surface; well under 1ms threshold); R9 STAYS embedded in ADR-0192 from Task 7 + Task 15 (body-bridge implementation surface did NOT introduce ADR-warranting complexity beyond ADR-0192 §Context); ADR-0193 carries forward to 22.3 BRAINSTORM as the 22.3 IMPL escape-valve slot. **D3 + D5 + D8 closure record**: D3 CLOSED at PLAN session per SPEC §11.3 + §12 RECOMMENDED option (a) defensive copy at endStream — RATIFIED at Task 15 perf-benchmark threshold gates (sub-MB body at ~103µs ≤ 1ms; 16-MiB-saturated at ~9.3ms ≤ 100ms); D5 CLOSED at PLAN session per SPEC §11.5 + §12 RECOMMENDED option (f-B) cert-fingerprint-only — RATIFIED at Task 17 cert fixture plumbing + fixture-0027 scenario (f) cross-side via `:sha256PeerCertificateDigest()` byte-exact; D8 CLOSED at PLAN session via empirical upstream-Envoy-v1.37.2 WebFetch scrape — RATIFIED at IMPL with 4 envoy-go-strict departure records landed at BEHAVIOR_CONTRACT.md 15-edit bundle for `:sha256` + `:sha512` + `:base64Decode` + `:fileBytes`. **R5/R6/R7/R8/R9/R10/R11/W2 disposition record**: R5 RATIFIED at Task 4 (first co-consumer validation of `internal/httpclient/`); R6 STANDS WEAK-default at Task 15; R7+R8 D8 PLAN-scrape closed at PLAN session (4/6 envoy-go-strict; 2/6 upstream-parity); R9 STAYS embedded in ADR-0192 at Tasks 7+15; R10 30-fuzzer count CONFIRMED at Task 16; R11 REUSE existing runReferenceLessFixture per D-P11 PLAN LOCK + Task 18 IMPL; W2 byte-stable runtime-reject wording for arms 20-22 PINNED at Task 14. **BEHAVIOR_CONTRACT.md 15-edit bundle landed atomically** per ADR-0052 + SPEC §14 (1 EXTEND-subsection + 1 stat-table 102→107 + 5 NEW counter rows + 2 `:filterState()` envoy-go-strict divergence records + 1 `:dynamicMetadata()` flat-accessor record + 1 `:body()` return-shape record + 4 D8 crypto/fileBytes records + 1 D8 disposition paragraph + 1 NEW `#### Phase 22.2 forward-pointer notes` subsection = 15 edits total; ~+830 LoC). **Stat surface 102 → 107** (5 NEW envoy-go-strict counters: httpcall_total + httpcall_failures + httpcall_timeouts + body_buffered_bytes_total + coroutine_yields_total; optional 6th `dynmd_writes_total` DEFERRED per BRAINSTORM Q11 pragmatic-middle). **Fixture directory count 28 → 29** (fixture-0027 mixed-mode: 8 deterministic cross-side + 5 non-deterministic REFERENCE-LESS subject-only; 13 scenarios total). **Fuzzer count 28 → 30** (NEW `FuzzLuaBodyBridge` + `FuzzLuaHTTPCallConfig` per D-P7 corpus-seed roster). **REVIEW.md authored** at `docs/envoy-go/phases/22.2-http-filter-lua-full-bridge/REVIEW.md` per `superpowers:requesting-code-review` (~500 LoC; mirrors phase-22.1 REVIEW.md structure). **Production HCM coroutine orchestration green-light at Task 19a (PRE-ATOMIC-LANDING)** — `decode_headers.go` + `encode_headers.go` + `lua.go` rewritten to invoke operator hooks as coroutines via `vm.NewThread()` + `vm.Resume(child, fn, ud)`; "Continue on body-yield" trade-off documented (multi-decoder-filter topologies that depend on body-after-yield header-mutation visibility ARE a known limitation flagged for REVIEW.md / phase-22.3). Parent row `22` STAYS `in-progress` until 22.3 phase-done; sub-row `22.3` STAYS `planned`. |
| 22.3 | http-filter-lua-multi-script-and-per-route | 22.2 | planned |  | Third sub-phase of the phase-22 BRAINSTORM-time 3-way pre-split (`22` parent). Lands the multi-script + per-route surface + the NEW 9th canonical per-route shape + the ADR-0125 IN-PLACE AMENDMENT (roster 8 → 9) per BRAINSTORM Q7. `Lua.SourceCodes` named-script map consumed (per-name script compilation + cache); `LuaPerRoute` 3-arm oneof consumed (`disabled: bool` → 5th canonical's disabled-bool arm; `name: string` → 8th canonical's string-reference-delegation pattern; `source_code: *core.DataSource` → DataSource-typed wholesale-override). NEW 9th canonical per-route shape ADR anchors the 3-arm hybrid (combines 5th + 8th in a single oneof; structurally distinct from all 8 prior canonicals) + the SHARED stat-discipline (per-route errors charge to listener-level `lua.<config_stat_prefix>.errors`; matches jwt_authn 8th canonical SHARED per ADR-0154). ADR-0125 IN-PLACE AMENDMENT body adds the 9th canonical's specification + the lua-row first-use citation; ADR-0125's `§canonical-per-route-roster` grows from 8 → 9. Per-route 3-tier dispatch (per-route override → listener-level default → no-op) reuses existing 3-tier resolution from phase 13/14/15 + extends with the Name string-reference-delegation pattern (the 8th canonical's discipline). Differential fixture `0028-http-lua-multi-script-and-per-route` — cross-side byte-exact for the deterministic multi-script + per-route scenarios. 22.3 BRAINSTORM (next-session-after-22.2-IMPL-phase-done) settles the 9th canonical ADR + the ADR-0125 AMENDMENT body wording (anticipated ~1-2 NEW ADRs + 1 IN-PLACE AMENDMENT at 22.3 IMPL). **Phase 22 (parent row) flips `in-progress → done` at the 22.3 IMPL Task** that lands ROADMAP row 22's per-cell IMPL-done annotation. §9 HTTP-filters family closes from 4 remaining rows (post-phase-21) to 3 remaining rows (post-phase-22): `wasm`, `admission_control`, `global rate limit`. |

---

## Feature Families (09+)

Each heading below becomes one or more brainstormed phases when it enters `in-progress`. Do **not** pre-populate per-phase rows here; add them at brainstorming time. Splitting (§6) applies as it would to trunk phases.

### HTTP filters family

Header manipulation, cors, compression, fault, local + global rate limit, jwt_authn, rbac, ext_authz, ext_proc, oauth2, csrf, buffer, lua, wasm, adaptive concurrency, admission control, bandwidth limit.

### Network filters family

redis, mongo, kafka_broker, thrift, zookeeper [scope TBD], echo, direct_response, sni_cluster, rbac network.

### Load balancing family

least_request, random, ring_hash, maglev, subset LB, locality-weighted LB, priority load balancing, panic thresholds.

### Upstream robustness family

Active health checks HTTP/TCP/gRPC/custom, outlier detection variants, circuit breakers, retries + hedging, per-protocol connection pooling.

### HTTP/3 + QUIC family

quic-go transport, downstream H3 listener, upstream H3 cluster, `h3spec` gate.

### gRPC family

gRPC bridge, gRPC-Web, gRPC-JSON transcoding, interop conformance.

### xDS / dynamic config family

ADS, delta xDS, LDS, CDS, RDS, EDS, SDS, RTDS, reconnection, initial-fetch timeout.

### Observability family

gRPC ALS, OTLP access log, OTel/Zipkin/Jaeger/Datadog/XRay tracing, stats sinks, tap filter.

### Runtime + hot restart family

Runtime layer (RTDS consumer); hot-restart / graceful-drain semantics beyond phase 08's minimum.

### WASM host family

Own multi-phase sub-project: ABI, engine binding, proxy-wasm conformance.

### Deprecated / edge features

Explicit out-of-scope ADRs unless later re-opened.
