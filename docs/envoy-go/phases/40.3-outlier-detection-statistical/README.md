# Phase 40.3 — `outlier-detection-statistical` (the statistical detectors + the per-interval aggregation runtime)

**Status:** PLAN DONE (lifecycle-state 2→3 at this commit; `docs/envoy-go/phases/40.3-outlier-detection-statistical/{SPEC,PLAN}.md`). The **THIRD (FINAL) leg** of the phase-40 pre-authorized 3-leg by-detector-class split: the STATISTICAL passive-outlier detectors — `success_rate` (eject hosts below `mean − (stdev_factor/1000)×stddev` across the cluster) + `failure_percentage` (eject hosts with `failure% ≥ threshold`) — over a NEW per-interval cross-host aggregation runtime (the project's FIRST outlier background goroutine). Builds over the 40.1/40.2 substrate (ADR-0245/0246, REUSED UNCHANGED) with NO router-seam re-design — the EXISTING `RecordUpstreamResult` seam already fires on EVERY request (the 40.1 forward-compatibility design), so 40.3 re-touches neither the router nor the seam. The SPEC was authored directly (the BRAINSTORM is DONE for the family); the §11 D-OD3-* empirical pins were EXECUTED IN-SESSION (2026-06-18) live against `envoyproxy/envoy:contrib-v1.37.2`. Counts at SPEC commit UNCHANGED: stat surface **1141** / fixtures **73** / fuzzers **42** / BackendKind tail **35** / DECISIONS tail **ADR-0246** (next-free **ADR-0247**). Row 40 STAYS `in-progress` (it flips `done` at the 40.3 IMPL six-gate — the final leg consuming the 3-leg split). **Next → the phase-40.3 IMPL** (`superpowers:subagent-driven-development` — execute `PLAN.md` Tasks 1–13).

**What this leg is.** 40.3 reuses the 40.1/40.2 ejection lifecycle (`tryEject` + the cap + CAS + the lazy un-eject + the `available` LB pick) wholesale and adds: the **success_rate** detector + the **failure_percentage** detector, both **SWEEP-driven** (evaluated on the periodic `interval` timer, NOT per-request); the **per-interval aggregation runtime** — a per-cluster background goroutine (the first outlier goroutine; the 39.1 `StartHealthChecks`/`Drain` lifecycle precedent) that each interval snapshots+resets the windowed per-host `(intervalTotal, intervalSuccess)` counters, computes the eligible-host set + the cross-host mean/stddev, and ejects; and the windowed per-host counters fed by the EXISTING seam. The two statistical detectors coexist with the per-request-synchronous consecutive detectors on the same `outlierDetector`.

**SPEC-firmed deltas (the §11 D-OD3-* live pins drove these):**
- **AMEND-OD3-1** — `success_rate` ejects-by-default (`enforcing_success_rate` default 100); `failure_percentage` is detect-only-by-default (`enforcing_failure_percentage` default 0) — the 40.2 gateway-vs-5xx asymmetry.
- **AMEND-OD3-2** — the statistical detectors are SWEEP-driven (the first outlier goroutine; ejection ~1 `interval` after per-host volume accrues), distinct from the per-request consecutive detectors.
- **AMEND-OD3-3** — the per-detector eject double-count is NOT replicated: when both detectors flag one host in a sweep the reference bumps `ejections_active`/`enforced_total` by 2 (per-detector); envoy-go's CAS-once-only ejects a host once → the fixtures isolate a single enforcing detector (a recorded departure on the both-enforce-same-host case).
- **AMEND-OD3-4** — stat surface **+8** (1141 → 1149): the full statistical name block `ejections_{detected,enforced}_{success_rate,failure_percentage,local_origin_success_rate,local_origin_failure_percentage}`, registered unconditionally; the EXTERNAL success_rate + failure_percentage logic is implemented (4 driven), the LOCAL-ORIGIN statistical variants' logic is DEFERRED (4 names emit 0 for surface parity — a recorded departure); the 3 legacy aliases stay departures (envoy-go emits 17 of the reference's 20 outlier names).
- **AMEND-OD3-5** — the aggregation arithmetic: `mean − (stdev_factor/1000)×stddev` (success_rate); `failure% ≥ threshold` (failure_percentage); each gated by its own `*_minimum_hosts` + `*_request_volume`. `success_rate_stdev_factor: 0` is ACCEPTED (no reject). The success-rate threshold can go negative with few hosts/high variance → a success_rate fixture needs enough good hosts (or a tuned factor) for a positive threshold.
- **AMEND-OD3-6** — ZERO new BackendKind (reuse `HTTP503Responder` 35 for the bad host + `HTTPEcho` + the `PerHostBackendKind` runner override for the ≥`minimum_hosts` topology).
- **AMEND-OD3-7** — retro-impact NONE on `0069`/`0070`/`0071`: they are 3-host clusters (3 < `minimum_hosts` 5) → the statistical detectors don't run; the +8 names emit 0 on both sides (not asserted) → no fixture change.

**The differential proof (sweep-driven statistical ejection; +1 or +2 fixtures).** `0072-outlier-detection-success-rate` (a 503 host statistically ejected via `success_rate`; default enforcing ⇒ success_rate enforces + failure_percentage detect-only; cross-side `ejections_enforced_success_rate` parity + `ejections_detected_failure_percentage ≥ 1` + `ejections_enforced_failure_percentage == 0`) + (optionally folded) `0073-outlier-detection-failure-percentage` (`enforcing_failure_percentage: 100` + `enforcing_success_rate: 0` ⇒ failure_percentage the sole enforcer). Both ≥ `minimum_hosts` hosts (K healthy + 1 bad), poll `/stats` until `ejections_active == 1` on both sides (the `0069` poll-to-converge + warmup + delta-counter pattern); 2 `-count=1` deliberate breaks each. NO new fuzzer.

**Anticipated at the 40.3 IMPL:** fixtures 73 → 74 or 75 (`0072` [+ `0073`]), BackendKind tail 35 (UNCHANGED), DECISIONS tail ADR-0246 → ADR-0247 (next-free ADR-0248), stat surface 1141 → 1149 (+8), fuzzers 42 (UNCHANGED), ZERO new packages + ZERO new go.mod modules. 1 ADR (ADR-0247). Row 40 flips `in-progress → done` at the 40.3 IMPL six-gate (the final leg consuming the 3-leg split — `reference_roadmap_split_phase_row_done`; NO parent rollup per ADR-0106).

**Masters (read first, in order):**

1. [`./SPEC.md`](./SPEC.md) — **the phase-40.3 SPEC.** The §1.1 AMEND-OD3-1..7 block; the §3 architecture (the parse extension + the windowed counters + the per-interval aggregation sweep + the goroutine lifecycle + the concurrency model); the §5 proto roster (fields 6–9, 16–20, `interval`); the §6 reject roster (6 arms); the §7 stat surface +8; the §8 `0072`/`0073` differential design (ZERO new BackendKind); the §11 D-OD3-* live-pin block; the §12 PLAN/IMPL D-questions; the §13 ADR-0247 §Context draft.
2. [`../40-outlier-detection/BRAINSTORM.md`](../40-outlier-detection/BRAINSTORM.md) — the phase-40 charter (the 3-leg split §1.4; the 40.3 scope §8).
3. [`../40.1-outlier-detection-consecutive-5xx/SPEC.md`](../40.1-outlier-detection-consecutive-5xx/SPEC.md) + [`../40.2-outlier-detection-other-consecutive/SPEC.md`](../40.2-outlier-detection-other-consecutive/SPEC.md) — the 40.1/40.2 architecture the 40.3 statistical detectors build over (ADR-0245/0246; the seam + ejection dimension + `available` predicate + cap + lazy un-eject + `tryEject` 40.3 REUSES UNCHANGED).

2b. [`./PLAN.md`](./PLAN.md) — **the phase-40.3 PLAN.** The 13-task TDD spine; the D-S40.3-1..8 resolutions (the house reject wording; the windowed-counter `atomic.Uint64` placement + the population-stddev snapshot; the `record` classification; the `StartOutlierDetection`/`odCancel`/`odWG`/`Drain` sweep lifecycle; the `0072`/`0073` single-sourced constants + the volume-floor fix; two dirs; NO FURTHER SPLIT; register-all-8); the per-task code + file structure; the exit-delta table.

The PROGRESS.md follows at the IMPL — the per-sub-phase-dir precedent: 36.1/36.2, 39/39.2, 40.1/40.2.
