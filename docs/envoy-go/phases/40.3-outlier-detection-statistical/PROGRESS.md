# Phase 40.3 IMPL — PROGRESS

The statistical outlier detectors (`outlier_detection.success_rate` + `failure_percentage`) + the per-interval aggregation runtime (the first outlier sweep goroutine, `StartOutlierDetection`/`Drain` lifecycle) over the phase-40.1/40.2 passive-health substrate. Executed subagent-driven per `docs/envoy-go/phases/40.3-outlier-detection-statistical/PLAN.md` (13 tasks). **STATUS: COMPLETE — all 13 tasks done; six-gate GREEN; full 75-dir differential GREEN; ADR-0247 landed IN-PLACE; ROADMAP row 40 flipped `done` (the phase-40 outlier-detection family is COMPLETE).**

## Exit counts (as built)

- stat surface: **1141 → 1149** (+8 statistical `outlier_detection.*` — 4 EXTERNAL driven `ejections_{detected,enforced}_{success_rate,failure_percentage}` + 4 LOCAL-ORIGIN `ejections_{detected,enforced}_local_origin_{success_rate,failure_percentage}` logic-deferred-emit-0)
- fixtures: **73 → 75** (tail `0073-outlier-detection-failure-percentage`; `0072`+`0073`)
- fuzzers: **42** (UNCHANGED — no new wire decoder)
- BackendKind tail: **35** (`HTTP503Responder`; UNCHANGED — `0072`/`0073` reuse it + `HTTPEcho` via `PerHostBackendKind`)
- DECISIONS tail: **ADR-0246 → ADR-0247** (next-free **ADR-0248**)
- New Go packages: **0** · New go.mod modules: **0**

ROADMAP row 40 flipped `in-progress → done` at this IMPL (the FINAL leg consuming the 3-leg by-detector-class split — `reference_roadmap_split_phase_row_done`; NO parent rollup per ADR-0106). The Upstream-robustness family STAYS OPEN (3 candidates remain: circuit breakers / retries + hedging / per-protocol connection pooling).

## IMPL base commit

`a7e942d` (worktree HEAD, branch `phase-40.3-impl`; sits on the phase-40.3 PLAN squash `42bcf63`).

## Baselines captured (pre-IMPL, at worktree HEAD)

- **`go build ./...`** — PASS (clean; exit 0)
- **`go vet ./...`** — PASS (clean; exit 0)
- **`gofmt -l internal/ test/ cmd/`** — PASS (empty output — no drift; exit 0)
- **`go test ./internal/... 2>&1 | tail -20`** — PASS (all packages `ok`, no failures; exit 0)
- **Full differential suite** (`go test ./test/differential/ -count=1`) — see "Differential baseline" below.
- **Stat surface** — **1141** (carried forward from phase-40.2 exit; tracked as a documented running total, no count-script; SPEC §14 / §7). Verified arithmetically at the 40.3 exit: **1141 + 8 = 1149** against the Task 7 registration test.

### Differential baseline

The full 73-dir suite (`go test ./test/differential/ -count=1`) is **known-green from the branch base** — master squash `42bcf63` (the phase-40.3 PLAN) landed with the full 73-dir differential GREEN, and this worktree branches directly off it with no production changes at Task 1. Harness/environment validated at Task 1 by a representative single-fixture probe: `go test ./test/differential/ -run 'TestDifferential/0069' -count=1` → **PASS** (2.1s; Docker 28.1.1 + `envoyproxy/envoy:contrib-v1.37.2` cached + the `reference_docker_probe_bridge_network` bridge net all working). The full **75-dir** suite is the real gate at Task 11 (after `0072`/`0073` land).

## Starting counts (pre-IMPL)

- stat surface: **1141** · fixtures: **73** (tail `0071-outlier-detection-local-origin`) · fuzzers: **42** · BackendKind tail: **35** (`HTTP503Responder`) · DECISIONS tail: **ADR-0246** (next-free **ADR-0247**)

## Anticipated exit deltas (PLAN / SPEC §7–§8 / §14)

| Count | Before | After |
|---|---|---|
| Stat surface | 1141 | **1149** (+8 statistical outlier_detection stats: 4 external-driven + 4 local-origin name-present-logic-deferred) |
| Fixtures | 73 | **75** (`0072` success-rate + `0073` failure-percentage; two-dir per the PLAN / `0067`/`0068` precedent) |
| Fuzzers | 42 | **42** (unchanged — no new wire decoder) |
| BackendKind tail | 35 (`HTTP503Responder`) | **35** (unchanged — reuse `HTTP503Responder` + `PerHostBackendKind` for the K+1 topology) |
| DECISIONS tail | ADR-0246 | **ADR-0247** (next-free ADR-0248) |
| New Go packages | — | 0 |
| New go.mod modules | — | 0 |

The +8 stat names (scoped `cluster.<name>.outlier_detection.`): `ejections_{detected,enforced}_success_rate`, `ejections_{detected,enforced}_failure_percentage`, `ejections_{detected,enforced}_local_origin_success_rate` (logic deferred — emits 0), `ejections_{detected,enforced}_local_origin_failure_percentage` (logic deferred — emits 0).

ROADMAP row 40 STAYS `in-progress` until the 40.3 leg (the FINAL leg) lands — at Task 13 it flips to `done` (per `reference_roadmap_split_phase_row_done`).

## Task checklist

- [x] Task 1 — Baselines + PROGRESS scaffold
- [x] Task 2 — `parseOutlierDetection` extension (the 8 statistical fields + `interval`-as-load-bearing) + the 6 reject arms (commit `6c078b6`)
- [x] Task 3 — The windowed `(intervalTotal, intervalSuccess)` counters + the `record` classification side-effect (commit `acd81a7`)
- [x] Task 4 — The `success_rate` aggregation (`evalSuccessRate`) + the +2 success_rate stat handles (commit `f0336e5`)
- [x] Task 5 — The `failure_percentage` aggregation (`evalFailurePercentage`) + the +2 failure_percentage stat handles + zero-traffic eligibility guard (commit `ee7e6d2`)
- [x] Task 6 — The per-interval sweep + the background goroutine + the `StartOutlierDetection`/`Drain` lifecycle (commit `02873b5`)
- [x] Task 7 — The +8 statistical stat registrations (extend `registerStats`; 1141 → 1149) + the 4 local-origin handles
- [x] Task 8 — The `0072` cross-side success-rate fixture (K=5+1 topology) (fixtures 73 → 74)
- [x] Task 9 — The `0073` cross-side failure-percentage fixture (fixtures 74 → 75)
- [x] Task 10 — `0072`/`0073` deliberate breaks + 20-run flake
- [x] Task 11 — Full 75-dir differential + six-gate
- [x] Task 12 — ADR-0247 body + BEHAVIOR_CONTRACT delta (commit `791540d`)
- [x] Task 13 — Completion bundle + ROADMAP row 40 → `done`

## Task notes

- **Task 13** (DONE) — The completion bundle (DOCS-only; NO code/squash/merge/push — the controller squashes after). Updated the 5 files: (1) `docs/envoy-go/STATE.md` — PREPENDED a new active-phase block `phase 40.3 (outlier-detection-statistical) IMPL done` (the two statistical detectors + the windowed counters + the `record` classification + the sweep + the first outlier goroutine `StartOutlierDetection`/`Drain` lifecycle + the +8 stats + the `0072`/`0073` fixtures + ADR-0247 + row 40 → done), demoting the PLAN-done block to "prior active-phase"; updated the count footer to **stat 1149 / fixtures 75 / fuzzers 42 / BackendKind tail 35 / DECISIONS tail ADR-0247 (next-free ADR-0248)** + the phase-directory (SPEC+PLAN+PROGRESS+README; PROGRESS landed) + lifecycle-state/next-skill (→ a FRESH BRAINSTORM for a new subject; the phase-40 family COMPLETE) + last-commit (`__IMPL_SQUASH_SHA__` placeholder — the controller fills it) + last-updated (IMPL done) + next-free ADR (ADR-0248). (2) `docs/envoy-go/ROADMAP.md` — flipped **row 40** `in-progress → done` + filled the sub-phase completion column (40.1/40.2/40.3 all done; the by-detector-class split CONSUMED; NO parent rollup per ADR-0106; the Upstream-robustness FAMILY stays OPEN with 3 candidates) + appended the **40.3 IMPL DONE 2026-06-18** as-built record (matching the row-39 done-row format). (3) THIS `PROGRESS.md` — `[x] Task 13`, STATUS COMPLETE, the exit-counts summary + this note. (4) `docs/envoy-go/phases/40.3-outlier-detection-statistical/README.md` — status PLAN DONE → IMPL DONE + the as-built exit counts. (5) `next-prompt.txt` — rolled the cold-start FORWARD to a FRESH BRAINSTORM (`superpowers:brainstorming`) for a NEW subject (the phase-40 outlier-detection family CONSUMED; likely one of the 3 remaining Upstream-robustness candidates or a new family row; the human picks at the BRAINSTORM), with the `__IMPL_SQUASH_SHA__` placeholder for the not-yet-existing squash SHA + the as-built counts (1149 / 75 / 42 / 35 / ADR-0247, next-free ADR-0248). **Final six-gate re-run** (confirm the doc edits didn't break the build): `go build ./...` ✓; `go vet ./...` ✓; `gofmt -l internal/ test/ cmd/` empty ✓; `go test ./internal/... -count=1` all `ok` ✓; the `0072` smoke `go test ./test/differential/ -run 'TestDifferential/0072' -count=1` PASS ✓ (the full 75-dir was confirmed GREEN at Task 11; these are docs-only edits). Committed locally on `phase-40.3-impl` (NO squash/merge/push — controller does Step 4).
- **Task 11** (DONE) — Full six-gate (ADR-0052) GREEN: `go build ./...` ✓; `go vet ./...` ✓; `gofmt -l internal/ test/ cmd/` empty ✓; `golangci-lint run ./...` ✓; `go test ./internal/... -race -count=1` all `ok` ✓; **`go test ./test/differential/ -count=1` → 75/75 GREEN** (227s). The `0069`/`0070`/`0071` retro-invariant HOLDS (AMEND-OD3-7 — 3-host < `minimum_hosts` default 5 → the statistical detectors no-op; the +8 names emit 0 on both sides, not asserted by their `StatsAsserter`s; they passed UNCHANGED in the green 75). **Flake note:** the first full-suite run hit a single transient `0025-http-adaptive-concurrency` `subj start: subject ready: EOF` (a subject-subprocess STARTUP flake under full-suite resource pressure — NOT an assertion mismatch, NOT a 40.3 fixture, and `StartOutlierDetection` is a no-op for the non-outlier 0025 config); `0025` then passed **3/3 in isolation** and the full 75-dir re-run was clean 75/75 — confirmed pre-existing startup-flake class, not a 40.3 regression. Counts confirmed: stat surface **1149**, fixtures **75**, fuzzers 42, BackendKind tail 35.
- **Task 10** (DONE) — Deliberate-break protocol (`-count=1` on every run, `TestDifferential/<NNNN>` selector; controller-run in-place via Edit→run→Edit-restore, NO `git checkout`, per `reference_differential_break_protocol_count1` + the worktree-detach hazard). All 4 breaks FAILED-as-required and were restored to a clean `git diff`: **0072-A** (`evalSuccessRate` early-`return` no-op) → subject `ejections_active` never converged to 1 (FP detect-only ⇒ nothing ejects) → FAIL ✓; **0072-B** (round-robin leaf consult `available`→`isHealthy` at `loadbalancer.go:61`) → warmup never stabilized (503 host stayed in worker rotation) → FAIL ✓; **0073-A** (`evalFailurePercentage` early-`return` no-op) → `ejections_active` never converged (SR detect-only here) → FAIL ✓; **0073-B** (FP eligibility floor made unreachable: `w.total > reqVolume*1000`) → no host eligible, never ejects → FAIL ✓. After each restore, `git diff` empty; both fixtures PASS clean (`007[23]` → ok 5.9s). **20-run flake gate: 0072 = 20/20, 0073 = 20/20** (zero flakes, no timing widening needed). No production-code commit (verification only); this PROGRESS note is the durable record.
- **Task 9** (DONE) — Created the `0073-outlier-detection-failure-percentage` cross-side differential fixture (driver.go, driver_test.go, expectations.yaml, README.md) + registered the blank-import in `test/differential/runner_test.go` (after `0072`, alphabetical). The SYMMETRIC TWIN of `0072` with the enforcer FLIPPED: cluster `c_fp`, lb ROUND_ROBIN, **6** endpoints (host0..4 HTTPEcho healthy + host5 HTTP503Responder always-503) on BOTH sides — REUSES `PerHostBackendKind`/`BackendKindAt` + `HTTP503Responder` (BackendKind 35); NO new BackendKind. `outlier_detection`: **failure_percentage the SOLE enforcer** (threshold 85, minimum_hosts 2, request_volume 10, enforcing 100), **success_rate detect-only** (minimum_hosts 2, request_volume 10, enforcing 0), consecutive_5xx/consecutive_gateway_failure explicitly OFF (0), interval 1s, base_ejection_time 30s, max_ejection_percent 100. Single-sourced const block; `refContainerListenerPort = 19162` (VERIFIED next-free: 0072=19161). Same `0072` eject-drive → poll-converge → warmup → measured-load → cross-side parity flow via `StatsAsserter`. Cross-asserts (both sides): ejections_active == 1, ejections_enforced_total == 1, ejections_detected_failure_percentage >= 1, ejections_enforced_failure_percentage >= 1, **ejections_enforced_success_rate == 0 (LIVE == 0 — the LOAD-BEARING isolation that SR does NOT enforce)**, ejections_detected_consecutive_5xx == 0 (LIVE == 0; consecutive detectors OFF), upstream_rq_2xx delta == 60, upstream_rq_5xx delta == 0, + the reference upstream_rq_total > 0 decode-ran guard. **ejections_detected_success_rate LEFT UNASSERTED** — SR is detect-only / not the detector under test; whether the bad host's 0% rate crosses the SR threshold depends on the cross-host distribution and need not match cross-side (per the Task-9 directive: avoid an unavoidable cross-side count mismatch; the load-bearing isolation is enforced_success_rate == 0). Recovery arm DEFERRED (AMEND-OD1). **Unit test** `go test ./test/fixtures/0073-outlier-detection-failure-percentage/driver/ -count=1` → `ok`. **Cross-side differential** `go test ./test/differential/ -run 'TestDifferential/0073' -count=1 -v` → `--- PASS: TestDifferential/0073-outlier-detection-failure-percentage (3.03s)`, converged on the FIRST run, NO timing widening needed. gofmt/vet/golangci-lint on `test/` all clean. **Fixtures 74 → 75** (filesystem dirs under `test/fixtures/`, tail now `0073`).
- **Task 1** (DONE) — Created this `PROGRESS.md`. Captured the pre-IMPL six-gate baseline at worktree HEAD `a7e942d`: `go build ./...` exit 0, `go vet ./...` exit 0, `gofmt -l internal/ test/ cmd/` empty, `go test ./internal/...` all `ok`. Confirmed the pre-IMPL counts: fixtures **73** (filesystem dirs under `test/fixtures/`, tail `0071`), stat surface **1141**, fuzzers 42, BackendKind tail 35, DECISIONS tail ADR-0246. Recorded the anticipated exit deltas (stat 1149 / fixtures 75 / fuzzers 42 / BackendKind 35 / ADR-0247). No production code changes.
- **Task 8** (DONE) — Created the `0072-outlier-detection-success-rate` cross-side differential fixture (driver.go, driver_test.go, expectations.yaml, README.md) + registered the blank-import in `test/differential/runner_test.go` (after `0071`). Topology: cluster `c_sr`, lb ROUND_ROBIN, **6** endpoints (host0..4 HTTPEcho healthy + host5 HTTP503Responder always-503) on BOTH sides — REUSES the existing `PerHostBackendKind`/`BackendKindAt` interface + `HTTP503Responder` (BackendKind 35); NO new BackendKind. `outlier_detection`: success_rate armed (minimum_hosts 2, request_volume 10, stdev_factor 1900, enforcing 100), failure_percentage detect-only (minimum_hosts 2, request_volume 10, enforcing 0), consecutive_5xx/consecutive_gateway_failure explicitly OFF (0), interval 1s, base_ejection_time 30s, max_ejection_percent 100. Single-sourced const block; `refContainerListenerPort = 19161` (VERIFIED next-free: 0069=19158, 0070=19159, 0071=19160). The `0069` eject-drive → poll-converge → warmup → measured-load → cross-side parity flow via `StatsAsserter`. Cross-asserts (both sides): ejections_active == 1, ejections_enforced_total == 1, ejections_detected_success_rate >= 1, ejections_enforced_success_rate >= 1, ejections_detected_failure_percentage >= 1 (LIVE cross-side match — fp detect-only trips on host5's 100% failure at the same sweep), ejections_enforced_failure_percentage == 0 (LIVE == 0; fp detect-only), ejections_detected_consecutive_5xx == 0 (LIVE == 0; consecutive detectors OFF), upstream_rq_2xx delta == 60, upstream_rq_5xx delta == 0, + the reference upstream_rq_total > 0 decode-ran guard. Recovery arm DEFERRED (AMEND-OD1). **Unit test** `go test ./test/fixtures/0072-outlier-detection-success-rate/driver/ -count=1` → `ok`. **Cross-side differential** `go test ./test/differential/ -run 'TestDifferential/0072' -count=1` → PASS, **3/3 runs green** (~3.2-3.5s each), NO timing widening needed (converged on the first run). gofmt/vet/golangci-lint on `test/` all clean. **Fixtures 73 → 74** (filesystem dirs under `test/fixtures/`, tail now `0072`).
- **Task 7** (DONE) — Extended `outlierDetector.registerStats` (`internal/cluster/outlier.go`) to allocate all **8** statistical `outlier_detection.*` counters UNCONDITIONALLY on any outlier cluster (the 40.2 precedent): the 4 external `ejections_{detected,enforced}_{success_rate,failure_percentage}` (driven by the Tasks 4/5 sweep — previously nil at runtime, now live) plus the 4 local-origin variants `ejections_{detected,enforced}_local_origin_{success_rate,failure_percentage}` (logic DEFERRED — registered for surface parity, emit 0; SPEC §2 / AMEND-OD3-4). Added the 4 LO handles to the `outlierDetector` struct adjacent to the SR/FP handles. Registry receiver `r`, prefix var `op` (`prefix + "outlier_detection."`). Extended `outlierStatNames()` roster (9 → 17 names) + the `TestRegisterOutlierStats_Present` non-nil block (9 → 17 handles) + the `_Absent` test (asserts none registered); updated the `9 → 17` doc-comments. **Stat surface 1141 → 1149** (+8), verified against the registration test. `go test ./internal/cluster/ -run RegisterOutlier` PASS; full `go test ./internal/... -count=1` all `ok`; gofmt/vet/lint clean.
