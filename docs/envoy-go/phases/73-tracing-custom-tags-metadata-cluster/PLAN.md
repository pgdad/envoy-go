# PLAN 73 — tracing `custom_tags` `metadata` type, `CLUSTER` MetadataKind only — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL — `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **Stage:** PLAN (lifecycle-state 2 → 3). Docs-only — **ZERO production `.go`**; the only two files in the phase-directory delta are this `PLAN.md` and `PROGRESS.md`. Worktree `.worktrees/phase-73-plan`, branch `phase-73-tracing-custom-tags-metadata-cluster-plan`, tip **`79f96455`** (the phase-73 SPEC squash — master), per `feedback_git_worktrees`.
>
> **Row 73 STAYS `in-progress`** — the IMPL flips it `done` at its six-gate (ADR-0106, the SOLE leg — a SINGLE FLAT ROW, §10). **ADR-0295's §Context is ALREADY DRAFTED** at the SPEC squash (`DECISIONS.md:17211-17229`, STATUS **PROPOSED**, §Context only, closing at `:17229` with `*(§Decision + §Consequences land at the phase-73 IMPL.)*` — **the LAST line of the file**). The IMPL **COMPLETES ADR-0295 IN PLACE** by APPENDING `### Decision (landed at the phase-73 IMPL)` + `### Consequences` **after that retained footer line** — it does NOT append a new ADR, does NOT renumber, and must NOT create a second `### Decision` heading. **RE-DERIVED at this tip: inside the ADR-0295 block, `### Context` = 1, `### Decision` = 0, `### Consequences` = 0** ⇒ T9's APPEND instruction is sound. DECISIONS tail stays **ADR-0295**, next-free **ADR-0296** (`grep -c '^## ADR-0296' docs/envoy-go/DECISIONS.md` → 0). **This PLAN adds NO ADR content; DECISIONS is UNTOUCHED at the PLAN.**
>
> **Baselines RE-DERIVED at `79f96455` (`[RUN]` in the worktree, NOT copied):** fixtures **118** (`ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0116-tracing-custom-tags-metadata-host` ⇒ next fixture **`0117`**; `test/fixtures/0117*` does not exist; `grep -rn '10117' test/ | wc -l` ⇒ **0**, port free) · fuzzers **55** (`grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`) · BackendKind tail **38** (`H2GoawayResponder`, `test/differential/fixture/fixture.go:614`) · stat surface **1201** (no mechanical command; the doc figure) · DECISIONS tail **ADR-0295** (PROPOSED; next-free ADR-0296) · go.mod modules **2** (lineage figure — re-check `git diff go.mod` after tidy at T8).
>
> **Sentinel expectation:** check (1) prints `NOT DONE: row 73`; check (2) prints **3** via the full-phrase form ONLY — `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` (`reference_sentinel_deferred_sentence_live_vs_historical` — cite the command, never the adjective); check (3) prints `NEVER OPENED: gRPC/Runtime/WASM`. **All three RUN at THIS PLAN close** (the PLAN-close checklist in Global Constraints — NOT §Task 9, which is the **IMPL**'s close). ⚠️ **Check (1) DOES go silent at this row's IMPL close, exactly as at phase 72** — row 73 is the **LAST** non-`done` row (**100 `done` + 1 `in-progress` among the 101 rows check (1) can SEE; 103 rows total, the 2 it cannot see (00, 04) both `done`** at this tip; verified by flipping it to `done` in a scratch copy, after which check (1) prints NOTHING). **The sentinel still does NOT fire and `stop` must NOT be created, because checks (2) and (3) both print.** *(An earlier draft of this PLAN claimed the opposite; it was REFUTED by its own adversarial pass — see §1.4/V2-M1.)* **No deferred-sentence edit at ANY pre-IMPL stage of this row** (SPEC §12, the phase-57 precedent).
>
> **⚠️ NO PARALLEL STREAM.** Master (`79f96455`) IS the SPEC squash. `git diff --stat d51d33dc 79f96455 -- '*.go' go.mod go.sum test/` is **EMPTY** — the only delta over the SPEC's cited re-derivation tree is docs (`SPEC.md` + the ADR-0295 §Context append + STATE + the router). So the production tree is byte-identical to what the SPEC re-derived, and §1 is a full independent re-verification (which found **ZERO drift on code** and **NINE new findings**) plus the structural decisions the SPEC delegated to the PLAN.
>
> **⚠️ RE-DERIVE, do not execute.** A PLAN is not evidence (a PLAN's cites drift; a SPEC's do too). Where this document cites, go look; where it claims control flow, walk the call graph; default to REFUTED (`feedback_brief_citations_not_evidence`, `reference_quoting_is_not_executing`). **Take every `file:line` from THIS document or from SPEC 73 — never from the phase-70/71/72 documents, whose cites are STALE** (SPEC §1.1 (i): phase 72's `descend`/`structpbValueToString` `:128`/`:151` are `:172-186`/`:195-212` here).

---

## 1. Re-derivation ledger — every SPEC §3/§9/§11/§12/§14 anchor re-opened at `79f96455`

**All SPEC anchors RE-DERIVED at `79f96455` by TWO independent read-only re-derivation agents on disjoint remits**, plus controller re-verification of every load-bearing correction (`reference_a_drift_correction_is_itself_a_claim` — a correction is itself a claim) — agent A over `internal/cluster/{cluster.go,manager.go,subset.go,locality.go,priority.go,health.go}` + `internal/tracing/{config.go,resolve.go,config_test.go,resolve_test.go}` + `internal/filter/hcm/{accesslog_emit.go,connection.go,h2dispatch.go,h3dispatch.go,fuzz_test.go}` + the emit test callers + the collision greps + the counts; agent B over `BEHAVIOR_CONTRACT.md` + `ROADMAP.md` + `DECISIONS.md` + `STATE.md` + `next-prompt.txt` + the memory roster + a **scratch-copy APPLICATION** of the §12 narrow.

**RESULT: ZERO DRIFT and ZERO REFUTED on every CODE anchor. Every `file:line` in SPEC §3/§10/§11 holds byte-exactly. NINE new findings (F1–F9), THREE of them stale-claim carriers the SPEC does not assign, and TWO of them `old_string` uniqueness hazards that would have bitten at the IMPL.** Two SPEC-level cites are **REFUTED as stale-by-one-stage** (F5) — both are warnings about text that the SPEC's own commit already fixed. All four identifier collision greps are 0 at tip.

### 1.1 The RD-* ledger

| # | Anchor / SPEC claim | RE-DERIVED at `79f96455` | Where |
|---|---|---|---|
| **RD-TREE** | SPEC re-derived at `d51d33dc`; is there a parallel stream? | **NO.** `git diff --stat d51d33dc HEAD -- '*.go' go.mod go.sum test/` ⇒ **EMPTY**. Not one `.go`, `go.mod`, `go.sum` or `test/` byte changed between the SPEC's tip and this one; `79f96455` is docs-only (4 files: `DECISIONS.md`, `STATE.md`, `SPEC.md`, `next-prompt.txt`). ⇒ the SPEC's `d51d33dc`-dated anchors are re-derivable verbatim, and `feedback_parallel_stream_mints_fresh_drift` finds nothing to re-mint **this time** — re-run the check at the IMPL tip anyway. | all |
| **RD-IDENT** | SPEC §5: four drafted identifiers free | **ALL FOUR FREE — 0 hits each** (`internal/`): `kindMetadataCluster` **0** · `clusterMetaLookup` **0** · word-boundary `grep -rnE '(^\|[^A-Za-z])ClusterMetaLookup'` **0** · `clusterFilterMetadata` **0**. ⚠️ **SPEC §5's "phase 72 recorded 51 naive `MetaLookup` hits, all `RouteMetaLookup`" is a STALE phase-72-era figure and is now wrong in BOTH count and composition** (F8): at this tip naive `grep -rn 'MetaLookup' internal/` ⇒ **98**, `RouteMetaLookup` ⇒ **29**, word-bounded `MetaLookup` ⇒ **39**. **State the RULE (use the word-boundary form), cite NO number.** | T1, T2, T3 |
| **RD-KIND** | SPEC §3.2: APPEND `kindMetadataCluster` AFTER `kindMetadataHost` ⇒ iota == 6 | **CONFIRMED.** `customTagKind` block **`config.go:55-62`** (`:55` `const (`, `:62` `)`) = `kindLiteral`(0,`:56`) / `kindRequestHeader`(1,`:57`) / `kindEnvironment`(2,`:58`) / `kindMetadata`(3,`:59`) / `kindMetadataRoute`(4,`:60`) / `kindMetadataHost`(5,`:61`). **`kindMetadataHost` IS LAST** ⇒ an APPENDED constant lands at **iota == 6**. **APPEND, never INSERT** — the constants are unexported and never serialized, so a renumber recompiles silently and fails NOWHERE loudly; Break G is the guard. **The gofmt re-align is arithmetically CONFIRMED:** the comment column is set by the 17-char `kindMetadataRoute`; `kindMetadataCluster` is **19** chars ⇒ gofmt re-aligns the trailing comments on **`:58`/`:59`/`:60`/`:61`** (+2 spaces each). **This is NOT a pure-append diff; a 4-line comment-column shift is EXPECTED, not damage.** *(Arithmetic, not executed — the SPEC's `+21/−5` is its own MEASURED figure and this PLAN does not re-assert it as a target.)* | T2 |
| **RD-REJECT** | SPEC §3.2/§3.6: REPLACE the CLUSTER reject (`:268-269`) with an accept arm cloning HOST (`:270-286`) | **CONFIRMED — all six arms EXACT with verbatim strings.** `case ct.GetMetadata() != nil:` `:228`; `md := ct.GetMetadata()` `:229`; **`k := md.GetKind()` `:230`** (its own landed comment names the phase-70 SEVERE); unset-A `:232-233` `"kind required"`; REQUEST accept `:234-250`; ROUTE accept `:251-267`; **CLUSTER reject `:268-269`** `case k.GetCluster() != nil:` → `"tracing: custom_tags metadata tag %q cluster kind unsupported"` (the arm REPLACED); **HOST accept `:270-286`** (the CLONE SOURCE — §1.2); `default:` unset-B `:287-288` `"kind required"`; `:289` closes the inner switch; `missing type` `:291`. First-wins dedup **`:293-297`** structurally independent and UNCHANGED. ⚠️ **`old_string` UNIQUENESS:** the REQUEST/ROUTE/HOST accept BODIES are near-identical (differing only in the `case` line and the `Kind:` token). The lift's `old_string` must span **`:267`+`:268-269`+`:270`** together (the `kindMetadataRoute` spec line → the reject → the `k.GetHost()` case line). | T2 |
| **RD-NOIMPORT-CFG** | SPEC §3.2/§4: config.go adds ZERO imports | **CONFIRMED.** `k` is INFERRED from `md.GetKind()` (never spelled), and the new arm calls `k.GetCluster()` on that inferred `k`, exactly as the five landed arms do. `metadatav3` is NOT imported and is not needed. | T2 |
| **RD-PROTO** | SPEC §3.1: the kind-getters are on `*metadatav3.MetadataKind`; `md.GetCluster()` does not compile | **CONFIRMED for the FOURTH and LAST time.** `GetCluster()` at `type/metadata/v3/metadata.pb.go:178`; the oneof wrapper `MetadataKind_Cluster_` at `:206-209` (field **3**); `MetadataKind_Cluster` at `:373-377` is an **EMPTY message** ⇒ `cluster: {}` is its only well-formed spelling. **`*CustomTag_Metadata`'s ONLY getters are `GetKind`/`GetMetadataKey`/`GetDefaultValue` — there is NO `GetCluster()` on `md`.** The arm branches on **`k.GetCluster()`**. | T2 |
| **RD-SPEC-STRUCT** | SPEC §3.2: `CustomTagSpec` needs NO new field | **CONFIRMED — `config.go:69-79`**, with `MetaNamespace` `:75` / `MetaPath` `:76` / `DefaultValue` `:77` / `HasDefault` `:78` all present. **NO new field.** The field-comment column is set by `MetaNamespace`/`customTagKind` (13 chars each); adding no field ⇒ **no re-align here** — only the comment TEXT of `:71`/`:75`/`:76`/`:77`/`:78` goes stale. | T2 |
| **RD-ENDPOINT** | SPEC §3.4: ADD `clusterFilterMetadata` + `ClusterMetaLookup` beside the phase-72 pair | **CONFIRMED — `Endpoint` is `cluster.go:45-86`.** The phase-72 `filterMetadata map[string]*structpb.Struct` field is at **`:79`** (its doc comment `:72-78`); `MetaLookup` is **`:111-117`** (doc `:105-110`); `IsZero()` `:103`; `addr` `:85`. **`cluster.go` ALREADY imports `structpb` at `:21`** ⇒ **ZERO new imports** — a genuine difference from phase 72, which paid one. Both clone sources quoted verbatim at §1.2. | T1 |
| **RD-CLUSTER-NOMETA** | SPEC §3.3 item 2: `cluster.Cluster` retains NO metadata (the fact that kills seam B's "it avoids `internal/cluster`" claim) | **CONFIRMED — `type Cluster struct` at `cluster.go:157`, spanning `:157-269`, with EXACTLY 32 fields and NOT ONE metadata field.** Verified two ways: full field-name enumeration, and `awk 'NR>=157&&NR<=270' \| grep -i metadata` ⇒ **zero hits**. `grep -cE '^\t[a-zA-Z_]'` ⇒ **32**. Seam B would have needed its own retention field + accessor here. | §1 (decision record) |
| **RD-POPULATE** | SPEC §3.4: populate via a loop in `buildCluster` after the `err != nil` guard, NOT via an `extractEndpoints` parameter | **CONFIRMED, with the anchor window quoted verbatim at §1.2.** `buildCluster` is a **FREE function** (not a method) at **`manager.go:402`**, carrying `c *clusterv3.Cluster` on that same line; `la := c.GetLoadAssignment()` `:429`; `endpoints, err := extractEndpoints(la, name)` **`:433`**; the guard **`:433-436`**; `validatePanicThresholdRange` `:437`. ⚠️ **`old_string` UNIQUENESS:** `if err != nil {\n\t\treturn nil, err\n\t}` is NOT unique in this file — the insert anchor MUST include `:429-433` (the `la :=` lines) or `:437`. | T1 |
| **RD-PLACEMENT** ⚠️ | SPEC §3.4/§16 M3: the loop must precede ALL LB construction; after `:472` it silently zeroes the subset/locality/priority LBs while round-robin stays green | **CONFIRMED — AND THE MECHANISM IS NOW VERIFIED BY READING EACH CONSTRUCTOR, not inferred.** There are **ELEVEN** references to `endpoints` after `:433` (`:464`, `:468`, `:472`, `:481`, `:504`, `:516`, `:527`, `:539`, `:550`, `:566`, `:583`). Of these, **THREE CONSTRUCTORS RETAIN A VALUE COPY — the SPEC's list — across FOUR distinct copy SITES** (verifier V1 surfaced the fourth; an earlier draft of this row said "FOUR retain a value copy" while naming only three constructors — that mismatch is CORRECTED here): `newSubsetLB` (`:481` → **TWO** sites: `subset.go:168` `groups[k] = append(groups[k], ep)` **and** `subset.go:193` `match = append(match, ep)`, the `fallbackDefaultSubset` branch — **both re-derived by grep at the PLAN tip; an earlier draft cited `subset.go:164`, which is the `if !ok {` line, NOT the copy**), `newLocalityWeightedLB` (`:516` → `locality.go` `byLocality[ep.Locality] = append(…, ep)`), `newPriorityLB` (`:550` → `priority.go` `byPriority[ep.Priority] = append(…, ep)`). **`buildLeafLB` (`:472`) retains the SLICE, not the elements** (`roundRobin{endpoints: endpoints}` at `manager.go:344`) — it ALIASES the same backing array, **which is precisely why the round-robin fast path would stay green while the other three silently carried a zeroed field.** The remaining references are harmless: `:464`/`:504`/`:539` (`newClusterHealth`) read only `ep.Addr()`, `:527` (`distinctPriorities`) reads only `ep.Priority`, `:468`/`:583` copy the slice HEADER (aliases ⇒ mutation still visible). ⇒ **PIN THE LOOP IMMEDIATELY AFTER THE `:433-436` GUARD**, and give T1 an explicit **subset-LB** assertion (Break D) so the constraint is ENFORCED rather than documented. | T1 |
| **RD-EXTRACT** | SPEC §3.4/S2: the loop form leaves `extractEndpoints`'s signature AND `manager_test.go` byte-identical; the parameter form would drag 10 test callers | **CONFIRMED EXACTLY.** `func extractEndpoints(la *endpointv3.ClusterLoadAssignment, clusterName string) ([]Endpoint, error)` at **`manager.go:863`**; **exactly ONE production call site (`:433`)**; **exactly TEN test callers, all in `manager_test.go`, at `1467, 1482, 1520, 1828, 1854, 1886, 1911, 1924, 2253, 2295`** — byte-for-byte the SPEC §3.4 list. `manager.go` imports **neither `structpb` nor `corev3`** (block `:3-17`), confirming the counter-claim for the rejected parameter form. **The loop introduces no new type name in `manager.go` ⇒ ZERO new imports.** Also confirmed: the boot-reject strings SPEC §3.3 cites — `:890` `zero endpoints across all locality groups` and `:427` `only STATIC clusters supported`. ⚠️ **PRECISION (verifier V1 SEVERE-1):** what this row actually establishes is that **the 10-caller set does not move** — that is the invariant the loop-vs-parameter choice buys. Whole-file byte-identity of `manager_test.go` is a SEPARATE, WEAKER claim that holds ONLY because T1's four manager-side tests are routed to the NEW `cluster_meta_test.go`; an earlier draft of this PLAN scheduled them INTO `manager_test.go` while simultaneously sha256-gating it byte-untouched in six places, which is unsatisfiable. Both the caller-set pin and the sha256 gate now hold. | T1 |
| **RD-BYTEUNCHANGED** | SPEC §11: `:754`/`:883`/`:884`/`:885` byte-unchanged | **CONFIRMED** — `defaultSubset` `:754`; the phase-38 `ScalarsFromStruct(… ["envoy.lb"])` projection `:883`; the `Endpoint{…}` composite literal `:884` (the SOLE populating literal repo-wide, already carrying phase-72's `filterMetadata:` key); the `addr` precompute `:885`. **This row adds NO key to `:884`** — the new field is populated by the T1 loop, not the literal. That is the S2 decision's whole point and is what keeps `extractEndpoints` untouched. | T1, T8 |
| **RD-RESOLVE-6TH** | SPEC §3.4: `ResolveCustomTags` grows a SIXTH nil-tolerant one-arg `clusterMetaLookup` | **CONFIRMED — the 5-param signature is exact at `resolve.go:46`** (quoted verbatim at §1.2). Phase 73 APPENDS a 6th `clusterMetaLookup func(ns string) (*structpb.Value, bool)`, shaped identically to `hostMetaLookup`. The 3rd/4th/5th lookups are UNTOUCHED. The file doc block is **`:12-45`** and its `hostMetaLookup` paragraph (`:38-45`) is the exact paragraph the CLUSTER clause mirrors — a GENUINE edit site (SPEC §11 cites it as going INCOMPLETE rather than false). | T3 |
| **RD-CLUSTER-ARM** | SPEC §3.4: a `kindMetadataCluster` arm descending the FULL `MetaPath` | **CONFIRMED — the clone source is `resolve.go:131-160`** (the `kindMetadataHost` arm; `:161` closes the `switch`, `:162` the `for`, `:164` the func) ⇒ **the new arm inserts between `:160` and `:161`**. Its `descend` call is at **`:150`** — confirming the SPEC §16 V2-MINOR correction (`:150`, not `:152`). Quoted verbatim at §1.2. The REQUEST `[1:]` slice is REQUEST-only. | T3 |
| **RD-DESCEND / RD-SERIALIZE** | SPEC §3.4: both reusable VERBATIM | **CONFIRMED.** `func descend` **`:172-186`** (doc `:166-171`); `func structpbValueToString` **`:195-212`**. Kind-agnostic and source-shape-independent ⇒ reused UNCHANGED, and the reuse is provable by sha256 over the two function bodies at T3. `structpb` already imported ⇒ **NO new import**. ⚠️ **These are the cites SPEC §1.1 (i) warns are STALE in the phase-72 documents (`:128`/`:151`). Use THESE.** | T3 |
| **RD-CFGTEST** | SPEC §10 T2: the pre-known red is EXACTLY TWO `/cluster` subtests | **CONFIRMED ON EVERY LEG.** Table rows **`config_test.go:639`** (in `TestNewConfigRejectCustomTagMetadata_RejectKinds`, func `:633`) and **`:764`** (in `…MetadataRoute_RejectKinds`, func `:758`), both reading `{"cluster", &metadatav3.MetadataKind{Kind: &metadatav3.MetadataKind_Cluster_{Cluster: &metadatav3.MetadataKind_Cluster{}}}, "cluster kind unsupported"},`. **BYTE-IDENTICAL to each other — `grep -Fxc` ⇒ 2**, so an `old_string` on either alone is NOT unique (include the following `{"unset-kind", nil, "kind required"},` line plus the enclosing func). The FAILURES print at **`:648`** and **`:773`** (`t.Fatalf("NewConfig(%s) err = nil, want reject; got %+v", …)`) — **do not read the two pairs as a contradiction.** Stale doc comments at **`:627-632`** and **`:754-757`**. The iota pin is **`:856-867`** (`TestCustomTagKindMetadataHostConstValue`), and the source spells the assertions as `!=` **guards** asserting the values **ARE** 5 and 4. | T2 |
| **RD-TESTCALLERS** | SPEC §10 T3: 49 real `resolve_test.go` callers (raw grep 50; `:107` a literal) | **CONFIRMED EXACTLY.** `grep -c 'ResolveCustomTags(' internal/tracing/resolve_test.go` ⇒ **50**. **`:107` is a `t.Errorf` FORMAT-STRING literal** (`t.Errorf("ResolveCustomTags(nil, ...) = %+v, want nil", got)`); its real call is **`:106`**. Exhaustively verified: `grep -n '"[^"]*ResolveCustomTags(' ` returns **only `:107`**, and no comment line carries the token. ⇒ **49 real callers**, at **35, 73, 98, 106, 125, 132, 139, 147, 189, 199, 208, 217, 245, 255, 269, 277, 285, 293, 305, 312, 341, 350, 359, 368, 404, 417, 433, 442, 450, 458, 470, 477, 508, 517, 526, 535, 546, 559, 602, 615, 631, 640, 648, 656, 667, 683, 690, 714, 715** — each gains a trailing `nil`. **Gate by explicit LINE NUMBER, not a bare regex** (the phase-72 T3 precedent). ⚠️ `:714`/`:715` are ADJACENT and BOTH real. **Do NOT adopt 50.** | T3 |
| **RD-SEAM** | SPEC §1/§3.4: the closure is built LOCALLY from the in-scope `picked`; the 3 signatures + 18 callers BYTE-UNTOUCHED | **CONFIRMED — the row's central cost claim is structurally sound.** The three emit methods are **9-parameter** with **`picked cluster.Endpoint` as the 4th**: `emitAccessLog` **`:27`**, `emitAccessLogH2` **`:87`**, `emitAccessLogH3` **`:149`**. The three `ResolveCustomTags(` calls sit INSIDE those bodies at **`:57`/`:118`/`:179`**, all currently **5-arg** (ending `…, routeMetaLookup, picked.MetaLookup)`). Span gates `:30`/`:89`/`:151` (byte-identical). `UpstreamCluster: ""` at `:51`/`:112`/`:173` — **UNTOUCHED by this row** (SPEC §2). ⇒ appending `picked.ClusterMetaLookup` as a 6th argument needs **NO signature change and NO caller change**. ⚠️ **NEW (F2): `:57` and `:179` are BYTE-IDENTICAL** (`grep -Fxc` ⇒ 2 — H3 reuses `reqHeaderLookupH1(r)`). **The SPEC does not flag this.** An `old_string` for either alone is NOT unique. | T4 |
| **RD-CALLERS** | SPEC §11: 18 emit callers, 5+6+7, BYTE-UNTOUCHED | **CONFIRMED — every one of the SPEC §11 table's line numbers and `picked` arguments HOLDS.** `connection.go` (5): `:330`/`:464`/`:597`/`:699` `Endpoint{}` · **`:777` `picked`**. `h2dispatch.go` (6): **`:313` `picked`** (provably always zero — `directResponseAction.asRouterActionH2()` returns `cluster.Endpoint{}` at **`actions.go:169`**, RE-DERIVED) · `:396`/`:530` `Endpoint{}` · **`:577`/`:584`/`:613` `picked`**. `h3dispatch.go` (7): `:130`/`:210`/`:280`/`:341` `Endpoint{}` · **`:367`/`:373`/`:395` `picked`**. ⚠️ **COUNT TRAP:** a raw `grep -c 'f\.emitAccessLog' h2dispatch.go` returns **7** — the extra is the DOC COMMENT at **`h2dispatch.go:188`**. Use `grep -cE 'emitAccessLog(H2\|H3)?\('` ⇒ correct **6**. ⚠️ `:577`/`:584` and `:367`/`:373` differ ONLY in leading indentation. **These 18 sites are NOT edited by this row** — verify mechanically at T8. | T4, T8 |
| **RD-SPANCAP** | SPEC §11: 6 PRE-Decide + 12 span-capable (7 real-`picked` + 5 zero) | **CONFIRMED — re-derived independently.** `traceDecision` DECLARED at `connection.go:317` / **`h2dispatch.go:266` (a struct FIELD, not a local — which is why the H2 emit sites read `c.traceDecision`)** / `h3dispatch.go:122`; ASSIGNED at `connection.go:553` / `h2dispatch.go:449` / `h3dispatch.go:255`. **PRE-Decide (6, can NEVER emit a span):** `connection.go:330`/`:464`, `h2dispatch.go:313`/`:396`, `h3dispatch.go:130`/`:210`. **Span-capable (12):** 5 zero-endpoint (`connection.go:597`/`:699`, `h2dispatch.go:530`, `h3dispatch.go:280`/`:341`) + 7 real-`picked`. ⚠️ **T4's zero-`picked` test targets the 5, NOT the raw 10 syntactic `Endpoint{}` literals** (6 of which can never emit a span). | T4 |
| **RD-EMITTESTS** | SPEC §1/§10 T4/§11: 34 emit test callers (13 + 21); gate on CALLER-SET invariance, NOT a hash | **CONFIRMED — and the gating regex must be pinned (F4).** With `grep -cE 'emitAccessLog(H2\|H3)?\('`: `accesslog_emit_test.go` ⇒ **13**, `span_emit_test.go` ⇒ **21**, total **34**. ⚠️ **A raw `grep -c 'emitAccessLog'` returns 23 for `span_emit_test.go`** — `:4` and `:5` are doc-comment prose. **If T4's gate is scripted with the naive form it reads 23 and a reviewer sees a false DRIFT.** Caller lines: `accesslog_emit_test.go` 29, 57, 68, 78, 94, 104, 132, 149, 167, 182, 203, 219, 233; `span_emit_test.go` 121, 155, 180, 205, 227, 241, 261, 293, 316, 339, 356, 368, 393, 412, 494, 550, 689, 710, 732, 759, 771. **A sha256 gate on these two files is UNEXECUTABLE by construction — T4 ADDS tests to both** (SPEC §1's explicit correction of its own earlier draft). | T4, T8 |
| **RD-FUZZ** | SPEC §7: ADD `meta_cluster_ok`; REPOINT `meta_bad`; narrow the stale `:89-98` comment | **CONFIRMED ON EVERY LEG, and the repoint's necessity re-established from the source.** ⚠️ **Terminology: `withMetaTags` is NOT a function** — `grep -n 'func withMetaTags'` returns NOTHING. It is a local **variable** assigned at **`:99`** to `mkHCM(func(h *hcmv3.HttpConnectionManager) {…})`, spanning **`:99-122`**, with its `f.Add` at **`:123`**. The four tags: `meta_ok`/REQUEST **`:102`**, `meta_route_ok`/ROUTE **`:107`**, `meta_host_ok`/HOST **`:112`**, **`meta_bad`/CLUSTER `:117-119`** — and `meta_bad` **IS LAST** (the slice closes `:120`), which is exactly what makes §7's first-error-return trap real. **`meta_bad` carries `Kind: &metadatav3.MetadataKind{Kind: &metadatav3.MetadataKind_Cluster_{…}}` and NO `MetadataKey` field at all** ⇒ after the lift it falls THROUGH the new accept arm and rejects at `empty namespace` (`config.go:273`), **still red, silently, nothing going green** — and that shape is the R2e **DEPARTURE**. **The repoint is genuinely mandatory.** `metadatav3` already imported (`:13`) ⇒ zero new imports. Stale doc comment **`:89-98`**, false in THREE places (`:93` "one REJECTED CLUSTER-kind tag", `:93-94` "CLUSTER stays an envoy-go-strict departure, ADR-0080", `:97-98` "the CLUSTER kind-unsupported reject arm"). Count STAYS **55**. | T5 |
| **RD-FIXTURE** | SPEC §8: clone the `0116` chassis into a TWO-CLUSTER `0117` | **CONFIRMED — chassis re-derived in full.** `0116-tracing-custom-tags-metadata-host/` = exactly **5 files**: `driver/driver.go` (754 lines), `envoy.yaml` (153), `envoy-go.yaml` (140), `expectations.yaml` (200), `README.md` (222) — **NO `scripts/`**. Reference cluster `envoy.yaml:118-136` `type: STRICT_DNS` + `dns_lookup_family: V4_ONLY` + `{{.BackendHost}}/{{.BackendPort}}`; subject `envoy-go.yaml` `type: STATIC` + `127.0.0.1`. Single route `- match: { prefix: "/" }` → `route: { cluster: c_backend }` (`envoy.yaml:104-105`). The two `custom_tags` at `envoy.yaml:79-97`. Endpoint metadata block at `envoy.yaml:130-136`. Driver constants: `refListenerPort = 10116` (`:111`), `numPlain = 8`, `numCont = 4`, `numTotal = 12`, `probePath = "/trace"`, `BackendCount() → 1` (`:244`), `BackendKind() → fixture.HTTPFixedBody` (`:245`). Assertion helpers already KEY-BASED: `spanAttrMap` `:600`, `assertAttrPresent` `:609`, `assertAttrString` `:618`, `assertHostTags` `:576` (Errorf per property). ⚠️ **`upstream_cluster` is asserted `assertAttrPresent` ONLY (`:515`), never by value** — because envoy-go emits it EMPTY (`reference_tracing_upstream_cluster_framework_gap`). **It therefore CANNOT be used to partition spans by cluster** — see T6's design. | T6 |
| **RD-REGISTER** | SPEC §8: a one-line blank-import registration | **CONFIRMED.** `test/differential/runner_test.go:143` `_ "github.com/pgdad/envoy-go/test/fixtures/0116-tracing-custom-tags-metadata-host/driver"` is the LAST line of the block ⇒ **`0117`'s line goes at `:144`**. | T6 |
| **RD-BC** | SPEC §9: B1–B5 anchors | **ALL FIVE CONFIRMED.** `:688` is the `custom_tags` paragraph, **3866 characters** (3890 bytes — multibyte em-dashes), and the SPEC §9 B1 quoted sentence is **BYTE-PRESENT, count == 1 document-wide**, at char index 3267. `grep -n 'unset-kind'` ⇒ **exactly ONE hit, `:688`** ⇒ B1 and B2 genuinely edit the SAME physical line and **must be applied as ONE `old_string`**. `:690` is the phase-72 B2 clause (4116 chars); `:692` the BOUNDARIES paragraph (1747 chars, currently "shared by all three MetadataKinds"); `:745` the deferred-field bullet (1421 chars). The three "no span on a 404 no-route" clauses all present at `:690`, `:714`, `:745`… ⚠️ **precisely: `:690`, `:714`, `:744`**. ⚠️ **NEW PRECISION (F7-adjacent): `:688` and `:690` are SEPARATE physical lines** (`:689` is blank). SPEC §9's "same physical line" applies to **B1+B2 on `:688` only**; **B3 on `:690` is its own `old_string`** — do not over-read it into a three-way merge. | T7 |
| **RD-ROADMAP** | SPEC §12: the pinned narrow; the three stale claims | **ALL CONFIRMED, and the narrow was APPLIED IN A SCRATCH COPY rather than argued.** Row 73 is **`ROADMAP.md:135`**, **8 fields** (matching rows 70/71/72), status field `in-progress`. **All THREE stale claims live on `:135`, each occurring EXACTLY ONCE** (byte-verified by exact-substring count, not grep). The live Observability `candidates:` sentence is on **`:203`** — ⚠️ **that line is 38,182 characters** (the whole family paragraph); **no whole-line anchor is workable, and the pinned `old_string` is the only edit surface.** **The SPEC §12 `old_string` occurs EXACTLY ONCE document-wide** (Python exact-substring count ⇒ 1, at char index 857998 → line 203; the leading space is present and significant). **APPLIED in scratch: `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:'` ⇒ 3 (UNCHANGED); the post-narrow sentence is well-formed, retains the `candidates:` marker, still matches the check-(2) regex, and carries FOUR candidates** — the `ssl` stat family, `spawn_upstream_span`, `http_service`, force-trace. **SPEC §12/S6 fully CONFIRMED, including the "drop the bare `custom_tags` prefix" note.** | T9 |
| **RD-ADR** | SPEC §14: the ADR-0295 block shape; the ADR-0294 completed template | **CONFIRMED.** ADR-0295 runs **`:17211`–`:17229`** (EOF); inside it `### Context` **1** / `### Decision` **0** / `### Consequences` **0**; STATUS **PROPOSED** (`:17213`); the footer `*(§Decision + §Consequences land at the phase-73 IMPL.)*` is **`:17229`, the LAST line of the file** ⇒ the IMPL appends after it. §14's "pinned by what LANDED at 71 and 72" is itself TRUE: `git show 2a82cc7b -- DECISIONS.md \| grep -c '^+### Decision'` ⇒ **0**, and the same for the phase-71 SPEC `60f12146` ⇒ **0**. **ADR-0294 (`:17173`–`:17210`) is the COMPLETE template**: STATUS COMPLETE `:17175`, `### Context` `:17177` (with a bracketed in-place `[CORRECTED at the phase-72 IMPL: …]` at `:17185` — **the precedent for F6's fix**), the footer **RETAINED** at `:17187`, `### Decision (landed at the phase-72 IMPL)` `:17189` with a **7-item numbered list** for a **9**-task spine (T8/T9 fold into §Consequences), `### Consequences` `:17201` with **seven `-` bullets**. **Do not assume 1:1 task↔item.** | T9 |
| **RD-STATE** | SPEC §15: the ADR-0288 singleton greps return 2, not 1 | **CONFIRMED — 2 each.** `next-skill:` at **`:7`, `:18`**; `lifecycle-state:` at **`:7`, `:17`**; `next-free ADR:` at **`:7`, `:21`**. ⚠️ **PHRASING CORRECTION: `STATE.md:7` is the FIRST hit, not the second.** SPEC §15 states it correctly (unordered); the ROUTER's phrasing ("the second hit is `STATE.md:7`") inverts it. Substance holds: **count == 2, one of which is the RULE STATEMENT at `:7` — never "fix" it to 1.** §Current is `:11`–`:23` (heading `:11`, live bullets `:15`-`:21`, terminator `:23`). `## Recent lineage` `:40`, preamble **`:42`**, exactly **FIVE** bullets at `:44`/`:46`/`:48`/`:50`/`:52`. | PLAN close |
| **RD-ROUTER** | SPEC §16 F6: `next-prompt.txt:70` carries the refuted hazard claim | **REFUTED AS STALE-BY-ONE-STAGE (F5).** `next-prompt.txt` IS git-TRACKED (`git ls-files --error-unmatch` exit 0). **Line 70 is EMPTY** (`awk 'NR==70{print length($0)}'` ⇒ **0**). `grep -n 'REMOVES the clause'` ⇒ **exactly ONE hit, `:50`**, and there it appears **quoted inside a `**REFUTED**` warning**, not as a live claim. **The SPEC's own commit already fixed this; no edit is owed.** | PLAN close |
| **RD-MEMORY** | SPEC §13: 29 cited memory names; two memory writes owed | **ALL 29 CITED NAMES EXIST** (extracted by regex from `SPEC.md`, per-name file test; ZERO missing). ⚠️ **BOTH SPEC §13 memory obligations were ALREADY DISCHARGED at the SPEC close** — `reference_endpoint_metalookup_host_metadata_seam.md` (mtime 2026-07-24 06:18) now explicitly RETRACTS the "a future `CLUSTER`-kind row RE-PAYS it" claim **and** restates 29 → **34** with the 13+21 split and the cannot-sha256-gate consequence; `reference_cluster_tag_gated_on_pick_not_route.md` EXISTS (mtime 06:19) and is indexed at `MEMORY.md:122`. **The PLAN and IMPL owe NOTHING on those two.** But see **F7** for one that IS owed. | T9 |
| **RD-BASELINE** | SPEC §15 counts | **ALL CONFIRMED at tip:** fixtures **118** (tail `0116-…`; `0117` absent; port `10117` ⇒ **0** hits under `test/`) · fuzzers **55** · BackendKind tail **38** (`fixture.go:614`) · DECISIONS tail **ADR-0295** PROPOSED (`grep -c '^## ADR-0296'` ⇒ 0) · stat surface **1201** (the two latest-phase BEHAVIOR_CONTRACT statements, `:831` and `:847`, both read 1201; **no mechanical command exists**). | T8 |

### 1.2 The clone skeletons the SPEC delegated to the PLAN (each RE-DERIVED verbatim at `79f96455`, not invented)

> ⚠️ **GOFMT TRAP (the phase-72 F5a precedent).** SPEC §3.2's quoted CLUSTER skeleton compresses the three guards onto single lines (`if mk.GetKey() == "" { return … }`). **That is NOT gofmt-clean** — `gofmt` expands a single-line `if b { return … }` into a three-line block, so pasting the SPEC's form fires T2's `gofmt -l` gate. The forms below are the **byte-accurate landed shapes**, copied from the clone sources at this tip. Use THESE.
>
> ⚠️ **MISSPELL TRAP (the phase-72 M3 precedent).** `.golangci.yml` enables `misspell` with `locale: US`. Keep every new doc comment free of British spellings (`analog`, not `analogue`). The skeletons below are misspell-clean. (`lll` is NOT enabled — do not reformat long lines for a linter that never runs.)

**(a) The `Endpoint.clusterFilterMetadata` field (T1, `internal/cluster/cluster.go`)** — APPENDED beside the phase-72 `filterMetadata` field (`:79`), whose doc comment (`:72-78`) is the clone source:

```go
	// clusterFilterMetadata retains the OWNING CLUSTER's RAW per-namespace static
	// metadata (clusters[].metadata.filter_metadata), ALIASING the already-parsed
	// proto map — zero new allocation. Added phase 73 so a CLUSTER-kind tracing
	// custom_tag can address ANY namespace and walk a NESTED path. It rides the
	// ENDPOINT rather than cluster.Cluster because the reference gates a
	// CLUSTER-kind tag on the PICKED UPSTREAM HOST, not on the matched route
	// (probed at the phase-73 SPEC). Every endpoint of one cluster shares the
	// SAME map. NOT part of the dial identity: Addr() ignores it.
	clusterFilterMetadata map[string]*structpb.Struct
```

**(b) The `ClusterMetaLookup` accessor (T1)** — placed beside `MetaLookup` (`:111-117`), whose doc (`:105-110`) is the clone source:

```go
// ClusterMetaLookup returns the OWNING CLUSTER's static metadata for the
// namespace ns, wrapped as a structpb StructValue (or (nil,false) when the
// cluster carries no metadata / the namespace is absent). The CLUSTER analog of
// MetaLookup above; threaded as a method value at the three tracing emit sites.
// Safe on the ZERO Endpoint (nil map → (nil,false)).
func (e Endpoint) ClusterMetaLookup(ns string) (*structpb.Value, bool) {
	st := e.clusterFilterMetadata[ns]
	if st == nil {
		return nil, false
	}
	return structpb.NewStructValue(st), true
}
```

**NO import is added** — `structpb` is already at `cluster.go:21` (RD-ENDPOINT). ⚠️ **The nil guard is REQUIRED by the accessor's own `(nil,false)` contract, but it is NOT observably load-bearing through `ResolveCustomTags`** (the phase-72 F5b finding: `descend`'s first hop returns `(nil,false)` without panicking, and `MetaPath` is min-1 by parse validation) ⇒ **Break C must assert on the ACCESSOR'S OWN RETURN.**

**(c) The `buildCluster` populate loop (T1, `internal/cluster/manager.go`)** — inserted **IMMEDIATELY after the `:433-436` guard and BEFORE `:437`**. The anchor window, verbatim at tip:

```go
	la := c.GetLoadAssignment()
	if la == nil {
		return nil, fmt.Errorf("cluster: %q: missing load_assignment", name)
	}
	endpoints, err := extractEndpoints(la, name)
	if err != nil {
		return nil, err
	}
	if err := validatePanicThresholdRange(c, name); err != nil {
```

The loop goes between `	}` (`:436`) and `	if err := validatePanicThresholdRange` (`:437`):

```go
	// Phase 73: stamp the OWNING CLUSTER's static filter_metadata onto every
	// endpoint, so a CLUSTER-kind tracing custom_tag resolves off the PICKED
	// host (the reference's own gate). This MUST run before ANY load-balancer
	// construction below: buildLeafLB retains the SLICE (aliasing this backing
	// array) but newSubsetLB / newLocalityWeightedLB / newPriorityLB each copy
	// each Endpoint BY VALUE, so a later populate would leave those three LB
	// shapes carrying a zeroed field while round-robin silently stayed green.
	if fm := c.GetMetadata().GetFilterMetadata(); len(fm) > 0 {
		for i := range endpoints {
			endpoints[i].clusterFilterMetadata = fm
		}
	}
```

⚠️ **`old_string` uniqueness (RD-POPULATE):** `if err != nil {\n\t\treturn nil, err\n\t}` is NOT unique in `manager.go`. Anchor the insert on the `endpoints, err := extractEndpoints(la, name)` line plus the guard plus `validatePanicThresholdRange`. **`extractEndpoints`'s signature, `manager_test.go`, `:754`, `:883`, `:884` and `:885` all stay BYTE-UNCHANGED** — the loop adds no key to the `:884` literal and no import to `manager.go` (the `map[string]*structpb.Struct` type name never appears; `fm` is inferred).

**(d) The `parseCustomTags` CLUSTER accept arm (T2, `config.go`)** — REPLACES the `:268-269` reject; clones the LANDED HOST accept (`:270-286`) verbatim except two tokens (`GetHost`→`GetCluster`, `kindMetadataHost`→`kindMetadataCluster`). Indentation is **three tabs** for the `case`, four for its body:

```go
			case k.GetCluster() != nil:
				mk := md.GetMetadataKey()
				if mk.GetKey() == "" {
					return nil, fmt.Errorf("tracing: custom_tags metadata tag %q empty namespace", tag)
				}
				if len(mk.GetPath()) == 0 {
					return nil, fmt.Errorf("tracing: custom_tags metadata tag %q empty path", tag)
				}
				path := make([]string, 0, len(mk.GetPath()))
				for _, seg := range mk.GetPath() {
					if seg.GetKey() == "" {
						return nil, fmt.Errorf("tracing: custom_tags metadata tag %q empty path segment", tag)
					}
					path = append(path, seg.GetKey())
				}
				dv := md.GetDefaultValue()
				spec = CustomTagSpec{Key: tag, Kind: kindMetadataCluster, MetaNamespace: mk.GetKey(), MetaPath: path, DefaultValue: dv, HasDefault: dv != ""}
```

**IDENTICAL to the HOST arm except the `case` line and `Kind: kindMetadataCluster`.** `MetaPath` carries the FULL path. **⚠️ Branch on `k.GetCluster()` on the bound `k` (`:230`) — `md.GetCluster()` DOES NOT EXIST and does not compile (RD-PROTO).** `config.go` adds ZERO imports. ⚠️ **A first-in-lineage cosmetic, recorded so a reviewer does not read it as a renumber:** the lifted reject sits BEFORE the HOST arm, so after the lift the SOURCE order is REQUEST/ROUTE/CLUSTER/HOST while the IOTA order is …Route=4/Host=5/Cluster=6. **The two orders disagree for the first time. Harmless.**

**(e) The `kindMetadataCluster` const (T2)** — APPENDED to the `:55-62` block, after `kindMetadataHost` (`:61`), before the `:62` closing paren ⇒ **iota == 6**. Expect gofmt to re-align `:58-61`'s trailing comments (RD-KIND).

```go
	kindMetadataCluster // reads a CLUSTER-kind static owning-cluster metadata value (metadata_key.key + path), request_header default rule
```

**(f) The `kindMetadataCluster` resolve arm (T3, `resolve.go`)** — inserted between `:160` and `:161`; clones the LANDED `kindMetadataHost` arm (`:131-160`). Indentation is **two tabs** for the `case`, three for the body:

```go
		case kindMetadataCluster:
			// Mirror kindMetadataHost exactly: the CLUSTER source yields the WHOLE
			// namespace struct (the OWNING cluster's
			// clusters[].metadata.filter_metadata[ns], stamped onto every Endpoint at
			// cluster-build time), so descend the FULL MetaPath (the [1:] slice is a
			// REQUEST-only Bucket-pre-keying artifact). clusterMetaLookup may be nil,
			// and picked may be the ZERO Endpoint — at the 5 span-capable local-reply
			// sites, and ALSO at the post-decision upstream emit sites whenever the
			// upstream ACQUIRE failed (the phase-72 departure B2, of which a
			// CLUSTER-kind tag is the FIFTH consumer). POST-acquire failures forward
			// the REAL endpoint and resolve normally. → default / omit.
			var v *structpb.Value
			var ok bool
			if clusterMetaLookup != nil {
				v, ok = clusterMetaLookup(s.MetaNamespace)
			}
			if ok {
				v, ok = descend(v, s.MetaPath) // FULL path, NOT s.MetaPath[1:]
			}
			if ok {
				if str, emit := structpbValueToString(v); emit {
					out = append(out, KV{Key: s.Key, Str: str})
					continue
				}
			}
			if s.HasDefault {
				out = append(out, KV{Key: s.Key, Str: s.DefaultValue})
			} // else omit (append nothing)
```

**(g) The `ResolveCustomTags` signature growth (T3, `resolve.go:46`)** — a SIXTH param appended after `hostMetaLookup`; the 3rd/4th/5th UNTOUCHED. The landed declaration is a **single line**; match that formatting. This expansion is for readability only:

```go
func ResolveCustomTags(
    specs []CustomTagSpec,
    headerLookup func(string) ([]string, bool),
    metaLookup func(ns, key string) (*structpb.Value, bool),          // REQUEST — unchanged, two-arg
    routeMetaLookup func(ns string) (*structpb.Value, bool),          // ROUTE   — unchanged, one-arg
    hostMetaLookup func(ns string) (*structpb.Value, bool),           // HOST    — unchanged, one-arg
    clusterMetaLookup func(ns string) (*structpb.Value, bool),        // CLUSTER — NEW, one-arg, nil-tolerant
) []KV
```

The doc block `:12-45` gains a CLUSTER clause mirroring the `hostMetaLookup` paragraph at `:38-45`. **NO new import** (`structpb` already imported).

**(h) The 6th argument at the three emit call sites (T4, `accesslog_emit.go:57`/`:118`/`:179`)** — append `picked.ClusterMetaLookup` after `picked.MetaLookup`:

```go
tracing.ResolveCustomTags(f.tracingConfig.CustomTags, reqHeaderLookupH1(r), metaLookup, routeMetaLookup, picked.MetaLookup, picked.ClusterMetaLookup)
```

⚠️ **`:57` and `:179` are BYTE-IDENTICAL (F2)** — both use `reqHeaderLookupH1(r)`; only `:118` differs (`reqHeaderLookupH2(req)`). **An `old_string` on either alone is NOT unique. Anchor with surrounding context or edit by line number.**

**(i) The `meta_cluster_ok` seed ADD + the `meta_bad` REPOINT (T5, `fuzz_test.go`)** — inside the `withMetaTags` **variable** (`:99-122`). ADD, cloning the `meta_host_ok` block (`:112-116`):

```go
				{Tag: "meta_cluster_ok", Type: &tracingv3.CustomTag_Metadata_{Metadata: &tracingv3.CustomTag_Metadata{
					Kind:         &metadatav3.MetadataKind{Kind: &metadatav3.MetadataKind_Cluster_{Cluster: &metadatav3.MetadataKind_Cluster{}}},
					MetadataKey:  &metadatav3.MetadataKey{Key: "envoy.test", Path: []*metadatav3.MetadataKey_PathSegment{{Segment: &metadatav3.MetadataKey_PathSegment_Key{Key: "k"}}}},
					DefaultValue: "fb",
				}}},
```

REPOINT `meta_bad` (`:117-119`) — **to `Kind: &metadatav3.MetadataKind{}`, the kind message PRESENT with its oneof unset**, which envoy-go rejects at `config.go:288` and the reference PGV-rejects `field: "kind", reason: is required` ⇒ **PARITY, executed both sides (R3b)**:

```go
				{Tag: "meta_bad", Type: &tracingv3.CustomTag_Metadata_{Metadata: &tracingv3.CustomTag_Metadata{
					Kind: &metadatav3.MetadataKind{},
				}}},
```

⚠️ **Do NOT repoint to a nil/absent `kind`** (the BRAINSTORM's proposal) — that is the `config.go:233` **DEPARTURE** arm (R3a). And do NOT leave `meta_bad` as-is: its `MetadataKey`-less shape becomes the R2e **DEPARTURE** post-lift and rejects **silently** at `empty namespace` with nothing going green. `metadatav3` already imported (`:13`) ⇒ zero new imports.

### 1.3 Findings this re-derivation surfaced that the SPEC does NOT carry

**Three are STALE-CLAIM CARRIERS the SPEC leaves unassigned; two are `old_string` uniqueness hazards that would have bitten at the IMPL; two are stale SPEC cites; two are precision corrections.** All are folded into the tasks below.

- **F1 (MODERATE) — the ADR-0295 HEADING still carries BOTH refuted claims, and SPEC §16 asserts they were fixed.** ⚠️ **Cite §16 F2 as the primary refutation, not §16 M1.** M1 says the seam over-claim was "corrected in **both places**", where "places" plausibly means SPEC §3.3 + ADR-0295 **§Context** — and the heading is not literally §Context, so M1's letter is defensible. It is **§16 F2** — "the unit is now pinned once (**2 ARMS / 3 SPELLINGS**) and **propagated**" — that the heading flatly falsifies. **The BODY was fixed; the HEADING was not.** `DECISIONS.md:17211` still reads *"the route-carried seam would have manufactured **three divergence classes**"* and *"the departure roster narrows to **THREE arms, not two**"*. Both substrings occur **exactly once document-wide, both on `:17211`** (controller-verified). The body at `:17225` correctly reads *"**TWO envoy-go reject ARMS remain DEPARTURES, across THREE probed config spellings**"*, so the heading is the SOLE surviving carrier — **and it is the line every future reader sees first.** **Strengthened at the adversarial pass: `SPEC.md` carries ZERO occurrences of either refuted substring and so does the SPEC squash commit message, so `DECISIONS.md:17211` is the sole surviving carrier REPO-WIDE, not merely within the ADR.** ⇒ **T9 owes a FOURTH correction** beyond §12's three. This is the "cited-but-unfixed" class recurring inside the very section written to prevent it.
- **F2 (MODERATE) — `accesslog_emit.go:57` and `:179` are BYTE-IDENTICAL.** `grep -Fxc` ⇒ **2** (H3 reuses `reqHeaderLookupH1(r)`). The SPEC flags exactly this hazard for `config_test.go:639`/`:764` but **misses it here**, and T4 edits all three sites. An `old_string` on either alone silently edits the wrong one or fails. ⇒ **T4 Step 2 must edit by line number or with surrounding context, and verify THREE occurrences of `picked.ClusterMetaLookup` afterwards.**
- **F3 (MINOR) — an EIGHTH stale production comment in `config.go`, outside the SPEC's roster.** SPEC §1.1/S5 enumerates 9 `config.go` sites. **`config.go:190` carries a second instance**: `// boot-reject (both reject — NOT a departure). All substrings are ADR-0080-distinct.` That claim goes FALSE for the same reason `:185`'s does — after the lift, `:233` and `:288` **both** emit `kind required`, so the substrings are no longer all distinct. ⇒ **T2 narrows `:190` too.**
- **F4 (MINOR) — the emit-test caller gate needs a pinned regex.** A raw `grep -c 'emitAccessLog' span_emit_test.go` returns **23**, not 21 (`:4`/`:5` are doc-comment prose). The 13/21/34 figures only reproduce under `grep -cE 'emitAccessLog(H2|H3)?\('`. ⇒ **T4 and T8 pin the gated form**, or a reviewer reads a false DRIFT. (Same class as the `h2dispatch.go:188` doc-comment trap in RD-CALLERS, which phase 72's T8 already had to pin.)
- **F5 (MINOR, two parts) — TWO SPEC warnings are STALE-BY-ONE-STAGE; both are already fixed and NO edit is owed.**
  - **(a)** SPEC §12 (line 361) warns that *"the SAME stale narrow-hazard sentence also lives in `STATE.md`'s LIVE `next-skill:` block."* **REFUTED at this tip:** `grep -n 'REMOVES the clause' STATE.md` returns **only `:46`**, the **HISTORICAL** phase-73-BRAINSTORM lineage bullet (which SPEC §12 itself says to leave alone). The LIVE `:18` block already carries the **CORRECTED** form (*"the narrow SHORTENS the clause, leaving four candidates; check (2) STAYS 3"*).
  - **(b)** SPEC §16 F6 cites *"the router `next-prompt.txt:70`"* as carrying the refuted claim. **`:70` is EMPTY**; the phrase is at **`:50`**, inside an explicit `**REFUTED**` warning. Both were fixed by the SPEC's own commit. ⇒ **Record as re-verified-clean; do not "fix" either, and do not carry the stale text forward at the PLAN roll.**
- **F6 (MINOR) — ADR-0294 (COMPLETE) still ends on the claim this row refutes.** `DECISIONS.md:17209`, the §Consequences *Memory updates* bullet, closes: *"only `CLUSTER` remains — the natural next candidate, and **the one that RE-PAYS the 18-caller threading tax this row skipped**."* This is the identical claim the phase-73 SPEC refuted and that the landed memory has already retracted. It is named by **no** SPEC section. ⇒ **T9 SHOULD correct it in place with a bracketed `[CORRECTED at the phase-73 IMPL: …]` note — the precedent is ADR-0294's OWN `:17185`**, which carries exactly such a bracket for the refuted "cycle guard". *(Alternatively, decline explicitly on the grounds that ADR-0294 is a historical record — but decline in writing; do not leave it unaddressed.)*
- **F7 (MINOR) — ONE memory write IS owed, and it is not either of the two SPEC §13 names.** Both SPEC §13 obligations are **ALREADY DISCHARGED** (RD-MEMORY). But `reference_host_tag_resolves_at_pick_time.md:19` still closes *"Probe the reference's pool-overflow and circuit-breaker arms first; phase 72 left **both** UNPROBED."* SPEC §3.6/S12 and §9 B3(ii) establish the **circuit-breaker arm is now EXECUTED-PARITY on the reference (S8)**, leaving **pool-overflow alone** unprobed. ⇒ **T9 narrows that memory.** (Scope it exactly as the SPEC does: the REFERENCE half is EXECUTED; envoy-go's half is RE-DERIVED, not cross-side probed.)
- **F8 (MINOR) — SPEC §5's "51 naive `MetaLookup` hits" is a stale phase-72 figure.** At tip: naive **98**, `RouteMetaLookup` **29**, word-bounded **39**. The RULE (use the word-boundary form) is right; the number is not. ⇒ **state the rule, cite no number** (`reference_a_drift_correction_is_itself_a_claim` — three unreproducible figures is the RD-LIT pattern from phase 72).
- **F9 (INFORMATIONAL, strengthens the SPEC) — the populate-loop hazard's mechanism, verified rather than inferred.** There are **eleven** references to `endpoints` after `:433`, not four. The SPEC's four-constructor list is nonetheless **exactly right** as the set of *value-retaining* consumers, and the reason round-robin would stay green is now pinned: **`buildLeafLB` retains the slice HEADER (`roundRobin{endpoints: endpoints}`, `manager.go:344`) and therefore ALIASES the backing array**, while `newSubsetLB`/`newLocalityWeightedLB`/`newPriorityLB` each `append(…, ep)` **by value** — **THREE constructors across FOUR copy sites**, since `newSubsetLB` copies in BOTH its selector-grouping loop (`subset.go:168`) and its `fallbackDefaultSubset` branch (`subset.go:193`); the second site was surfaced by verifier V1 and is what makes a `DEFAULT_SUBSET` config an alternative Break-D detector. The other four references (`newClusterHealth` ×3, `distinctPriorities`) project only `ep.Addr()`/`ep.Priority` and retain nothing. ⇒ the pin is unchanged; its justification is now complete, and **Break D targets the subset LB specifically because that is the cheapest of the three value-copying shapes to construct in a test** — but ONLY through a `hasMatch=true` pick (see T1 test 3; the `PickEndpoint` route is vacuous).

### 1.4 Adversarial-pass record

*(This section is written AFTER the adversarial pass, from what the verifiers actually found — never asserted in advance over a placeholder. That failure mode is the phase-69 cited-but-unwritten class, which phase 72's V2 caught being self-certified against; §1.4 is populated at the PLAN close and its contents are mirrored into `PROGRESS.md`.)*

**STATUS: COMPLETE.** Two verifiers, disjoint remits, both in PRIVATE scratch, both reporting against this branch's tip. **Verdicts: V1 — BUILDABLE AS WRITTEN with 1 severe + 1 moderate + 3 minor; V2 — PASS WITH FIXES (1 major + 1 moderate + 6 minor).** Every finding below was RE-VERIFIED by the controller at the PLAN tip before folding — a drift correction is itself a claim (`reference_a_drift_correction_is_itself_a_claim`) — and all are now folded into this document.

| # | Verifier | Severity | Finding | Disposition |
|---|---|---|---|---|
| **S1** | V1 | **SEVERE** | `manager_test.go` was scheduled for FOUR test additions (T1 Step 1 items 2-5) while simultaneously sha256-gated **BYTE-UNTOUCHED** in six places (`:270`, `:325`, `:362`, `:620`, `:621`, `:663`). The gate is unsatisfiable as written — the identical defect this PLAN already diagnosed for the emit test files (RD-EMITTESTS), never applied to itself. | **FOLDED.** T1's four manager-side tests routed to a NEW `internal/cluster/cluster_meta_test.go` (collision-checked: no `internal/cluster/*meta*_test.go` exists); the sha256 gate is KEPT. V1 executed this routing — `sha256sum -c` ⇒ `manager_test.go: OK`. RD-EXTRACT now states the real invariant (the **10-caller set** doesn't move) rather than the weaker whole-file proxy. |
| **S2** | V1 | **MODERATE** | T1 test 3's suggested PRIMARY mechanism — `Cluster.PickEndpoint()` — does **NOT** discriminate: **Break D stays GREEN through it**, making the row's distinguishing break VACUOUS. | **FOLDED.** Mechanism re-verified by the controller at the tip: `PickEndpoint` passes `SubsetMatch{}, false` (`cluster.go:531-532`) ⇒ `hasMatch==false` ⇒ `subsetLB.Pick` (`subset.go:213-219`) skips `s.subsets` entirely ⇒ `ANY_ENDPOINT` fallback is `factory(endpoints)` over the ORIGINAL slice (`subset.go:183-185`) ⇒ aliased ⇒ invisible. Test 3 now pinned on the package-internal `cl.lb.(*subsetLB).Pick(…, hasMatch=true)` form. **Zero new exported symbols** — `NewSubsetMatch` (`:68`), `SubsetValue` (`:28`) already exported, `subsetLB` (`:140`) same-package. |
| **S3** | V1 | minor | RD-PLACEMENT cited `subset.go:164` for the value-copy; `:164` is `if !ok {`. | **FOLDED** ⇒ `subset.go:168`, re-derived by grep. A wrong cite inside a table headlined "ZERO DRIFT" is the `reference_verification_table_launders_wrong_cites` class. |
| **S4** | V1 | minor | T2 Step 1 is self-contradictory on ordering: it converts the two `/cluster` reject rows out of the tables, then asks for the reds those same rows produce. | **FOLDED** — rows RETAINED through Steps 2-3, `Kind:6` red recorded, conversion AFTER. V1 executed the retained form: exactly two `/cluster` subtests fire and `Kind:6` PRINTS. |
| **S5** | V1 | minor | Skeleton (e) is not gofmt-clean as pasted (`gofmt -l internal/tracing/` non-silent; `:58`-`:61` trailing comments +2 spaces). | **FOLDED** as a measured note at T2 Step 2 — run `gofmt -w` before any `gofmt -l`-must-be-silent gate. (a)(b)(c)(d)(f)(h) are clean as pasted. |
| **S6** | controller | minor | RD-PLACEMENT asserted "**FOUR** RETAIN A VALUE COPY" while naming only THREE constructors — an internal count/list mismatch neither verifier flagged. | **FOLDED** ⇒ **THREE constructors across FOUR copy sites**, the fourth being `newSubsetLB`'s `fallbackDefaultSubset` branch (`subset.go:193`). F9 reconciled to match. |
| **M1** | V2 | **MAJOR** | The PLAN claimed check (1) does **NOT** go silent at this row's IMPL close. **REFUTED** — row 73 is the LAST non-`done` row (100 `done` + 1 `in-progress` among the 101 rows check (1) can SEE; 103 rows total, the 2 it cannot see (00, 04) both `done`). | **FOLDED** in three places. Verified by flipping row 73 to `done` in a scratch ROADMAP copy: check (1) prints NOTHING. The sentinel still does not fire (checks 2 and 3 print) and `stop` must NOT be created. |
| **M2** | V2 | moderate | The PLAN-close checklist specified ONE sentinel run while the same document's T9 mandates TWICE (worktree + landed master post-squash-push). | **FOLDED** ⇒ TWICE, matching T9 and the landed phase-72/73 precedent (`next-prompt.txt:31`; router commit `d51d33dc`). |
| **m3-m8** | V2 | minor | Break R missing from two of three indexes · a dangling `(§2)` that resolves to **SPEC** §2 · `PROGRESS.md`'s stage pointer read "PLAN done" while its own adversarial gate was open · SPEC §13's two newly-surfaced deferred candidates get no ROADMAP home · check (1)'s regex silently skips 2 of 103 rows (em-dash "after" column; both `done`, so no impact today). | **ALL FOLDED**; the last two as explicitly-recorded deliberate omissions / known blind spots rather than silent ones. |
| **F1′** | V2 | precision | F1 cited SPEC §16 **M1** as the refutation; M1's "corrected in both places" plausibly means SPEC §3.3 + ADR-0295 §Context, and the heading is not literally §Context. | **FOLDED** ⇒ cite §16 **F2** ("pinned once … and propagated"), which the heading flatly falsifies. V2 also strengthened F1: neither refuted substring occurs in `SPEC.md` or the SPEC commit message ⇒ `DECISIONS.md:17211` is the **sole surviving carrier repo-wide**. |

**What the verifiers CONFIRMED (recorded so it is not re-litigated).** V1 built T1→T4 from §1.2's skeletons pasted verbatim and reports: build/vet/lint/`go mod tidy -diff` all clean; **ZERO new imports on all five functionally-edited files and +0 modules**; `unsafe.Sizeof(Endpoint{})` **112 → 120** exactly as predicted; **exactly ONE new exported symbol** by `go doc -all` set-diff; T4's entry state is **exactly THREE** `not enough arguments in call to tracing.ResolveCustomTags` errors at `accesslog_emit.go:57/:118/:179`; the seven-file sha256 gate **OK**; the 49-caller `resolve_test.go` transform exact (raw grep 50, `:107` is a `t.Errorf` literal); `descend`/`structpbValueToString` byte-identical ⇒ reused verbatim; **Breaks K2/M2 compile clean when the 5th/6th args are swapped**, proving the distinctness tests are genuinely load-bearing; the 18-caller 5+6+7 partition and the "5+7+7=19" trap both real; all four collision greps **0**; F1/F3/F5/F6/F7/F8 exact. V2 independently re-derived the docs-only envelope, the ADR-0295 1/0/0 precondition, the §12 narrow (re-applied in scratch: still 3, four candidates, well-formed), the sentinel, every count, and both memory obligations (already discharged at the SPEC close). **Neither verifier found any drift in the SPEC→PLAN code anchors.**

**What was NOT verified (stated so the IMPL does not inherit false confidence).** V1 ran **nothing reference-side** — no Docker, no Envoy probes; every reference/PGV claim (R1x, R2e, R3a/R3b, the S8 circuit-breaker parity, the `field: "kind", reason: is required` PGV reject) is unverified by V1 and rests on the SPEC's own executed probe record. **T5-T7 and T9 were not executed**: no fuzz-seed edit, no dispatch-verify, no `0117` fixture (it does not exist yet), no BEHAVIOR_CONTRACT/DECISIONS/ROADMAP/STATE edits. **Breaks N/O/P/Q/R untested.** The `http.url` partition reconnaissance (T6 Step 1) is unaddressed — it remains the IMPL's first live risk. The full differential suite was not run. Break M2's *firing* behavior at span level is unproven (only its compile-clean half, which is the load-bearing half).

The remit split, fixed in advance: **V1 — code claims + by-execution** (build the row from §1.2's skeletons in a PRIVATE scratch copy per `reference_parallel_subagents_private_scratch`; reproduce the red sets, the iota-6 print, the byte-identity gates, the `resolve_test.go` 49-caller transform, the `old_string` uniqueness hazards F2/RD-REJECT/RD-CFGTEST, and the T3→T4 non-compiling entry state). **V2 — process, consistency, SPEC-coverage and stage-close mechanics** (envelope docs-only; ROADMAP/DECISIONS untouched; row 73 `in-progress`; the ADR-0295 1/0/0 precondition; the §12 narrow re-applied in scratch; the sentinel; every count; the nine findings re-derived, since **a drift correction is itself a claim**).

---

## Global Constraints

- **ONE stage per session.** This session: the PLAN only. No production `.go`. After it lands: roll to the phase-73 IMPL.
- **⚠️ THIS PLAN's OWN stage-close checklist** *(distinct from §Task 9, which closes the IMPL — every task T1–T9 below is an IMPL task; phase 72's V2 found a PLAN that listed none of its own).* At the PLAN close the controller: creates `PLAN.md` + **`PROGRESS.md`** (the only two files in the phase-directory delta) · rolls **STATE §Current IN PLACE, lifecycle 2 → 3** (never prepend above §Current — the ADR-0288 rule), re-caps the §Recent lineage at five (PREPEND a phase-73-PLAN bullet, DROP `:52`'s phase-72 SPEC) **and updates its PREAMBLE at `:42`** naming the correct newest + dropped bullet · rolls `next-prompt.txt` to the phase-73 **IMPL** (TRACKED despite `.gitignore`; edit in the stage worktree) · **re-runs the three sentinel checks MECHANICALLY — TWICE: once in the worktree AND AGAIN on LANDED master AFTER the squash-push** (the phase-72/73 precedent — a stage worktree can disagree with what actually landed, and the post-push re-run is the one that counts; `next-prompt.txt:31` records the phase-73 SPEC close doing exactly this, and commit `d51d33dc` is the router follow-up that did it) — expecting `NOT DONE: row 73` / **3** / `NEVER OPENED: gRPC/Runtime/WASM` ⇒ does NOT fire, no `stop` · re-runs every count in the worktree · leaves **DECISIONS and ROADMAP UNTOUCHED** and **row 73 `in-progress`** · squash-pushes. ⚠️ **The ADR-0288 singleton greps must read 2, not 1** — the extra hit is `STATE.md:7`, the RULE STATEMENT (RD-STATE). **Never "fix" it to 1.**
- **FIVE functionally-edited production files, ZERO new packages** (SPEC §4, §11): `internal/cluster/cluster.go` · `internal/cluster/manager.go` · `internal/tracing/config.go` · `internal/tracing/resolve.go` · `internal/filter/hcm/accesslog_emit.go`. **New exported symbols: EXACTLY ONE** — `func (e cluster.Endpoint) ClusterMetaLookup(ns string) (*structpb.Value, bool)` — plus the `ResolveCustomTags` signature growth (a 6th param), the unexported `kindMetadataCluster` const and the unexported `Endpoint.clusterFilterMetadata` field. `descend`/`structpbValueToString`/`CustomTagSpec`'s metadata fields/`MetaLookup` are REUSED.
- **⚠️ ZERO NEW IMPORTS ANYWHERE.** `cluster.go` already has `structpb` (`:21`); the `buildCluster` loop names no new type in `manager.go`; `config.go`/`resolve.go`/`accesslog_emit.go` gain none. This is a genuine difference from phase 72, which paid one. **+0 go.mod modules** — verify by `go mod tidy -diff` EMPTY at T8.
- **BYTE-UNTOUCHED, sha256-asserted at T4 and T8 — SEVEN production files:** `internal/filter/hcm/{connection.go,h2dispatch.go,h3dispatch.go}` (**all 18 emit callers**) · `internal/filter/http/chain.go` · `internal/cluster/subset.go` · **`internal/filter/hcm/actions.go`** · **`internal/filter/hcm/route.go`**. The last two matter because they are exactly what the REJECTED route-carried seam would have touched. Plus `internal/cluster/manager_test.go`, `extractEndpoints`'s signature, and `internal/dynamicmetadata`/`internal/xds`/`internal/tls`/`internal/boot`/`internal/listener`/`internal/bootstrap`/`validate/`.
- **⚠️ CALLER-SET-INVARIANT, NOT byte-identical — and the two gates are NOT interchangeable:** `accesslog_emit_test.go` (**13** callers) and `span_emit_test.go` (**21**) = **34**. **T4 ADDS tests to both, so a sha256 gate on them is UNEXECUTABLE BY CONSTRUCTION** (SPEC §1's correction of its own draft). Gate them on: caller counts **13/21** via `grep -cE 'emitAccessLog(H2|H3)?\('` (**the gated form — the naive one returns 23 for `span_emit_test.go`, F4**) + the three emit signatures unchanged + a `diff` against the old blobs yielding **ZERO `<` lines** (purely additive — the phase-72 stronger test).
- **⚠️ `internal/cluster` CANNOT be asserted BYTE-UNTOUCHED. Pin the SHAPE instead** (the phase-72 precedent): exactly ONE added field + ONE added accessor + ONE added loop, with **`ScalarsFromStruct` (`subset.go`), `defaultSubset` (`manager.go:754`), the `envoy.lb` projection (`:883`), the `Endpoint{…}` literal (`:884`), the `addr` precompute (`:885`) and `extractEndpoints`'s signature all byte-unchanged**, and **ZERO added imports**.
- **⚠️ THE POPULATE LOOP'S PLACEMENT IS LOAD-BEARING** (RD-PLACEMENT/F9). It sits **IMMEDIATELY after the `:433-436` guard, BEFORE `:437`** and therefore before all eleven consumers of `endpoints`. Placed after `:472` it would leave the **subset / locality-weighted / priority** LBs carrying a ZEROED field while the round-robin fast path stayed green — because `buildLeafLB` aliases the slice while the other three copy each `Endpoint` by value. **No landed test catches this, and the pool-HIT provenance test would NOT catch it either.** Break D is the enforcement.
- **`CLUSTER` MetadataKind ONLY.** After this row **TWO reject ARMS remain DEPARTURES, across THREE config spellings** (SPEC §3.6 — **count the unit explicitly; "three arms" is the refuted framing, F1**): (a) **`config.go:233`**, the fully-unset `kind`, reachable by TWO spellings (`metadata:` with no `kind`, and `metadata: {}`); (b) **the `empty namespace` arm in its entirely-absent-`metadata_key` spelling** — **NEW, never before recorded**, invisible because envoy-go emits ONE identical string across a PARITY case (present-but-empty) and a DEPARTURE case (omitted). `config.go:288` (present-but-empty `kind` oneof) is **PARITY**. Empty-path / empty-segment / empty-`tag` / missing-type are **PGV-PARITY**. **TWO error-string COLLISIONS are documented and NEITHER is fixed here** (`empty namespace` across parity+departure; `kind required` from both surviving unset arms after the lift) — splitting them changes landed error strings (ADR-0080 territory) and is a separate deliberate decision.
- **The default rule is `request_header`, NOT `environment`:** a present-but-empty CLUSTER metadata value emits `""` (does NOT fall to the default); absent + non-empty default → default; absent + empty/omitted default → omit. `HasDefault = DefaultValue != ""`.
- **The CLUSTER resolve descends the FULL `MetaPath`** — NOT `[1:]`. The REQUEST `[1:]` (`resolve.go:98`) is a Bucket-pre-keying artifact (`reference_route_metadata_resolve_full_metapath`, which after this row covers three of four kinds). EXECUTED-confirmed for CLUSTER at the SPEC (R1x).
- **The 6th arg APPENDS at the three `ResolveCustomTags` call sites ONLY.** The three emit SIGNATURES and all 18 emit callers are UNCHANGED — the row's central cost claim, verified BY HASH at T4 Step 4 and again at T8.
- **A cost this row ACCEPTS and records, now DOUBLED:** both `picked.MetaLookup` and `picked.ClusterMetaLookup` are evaluated unconditionally inside the sample block, before `ResolveCustomTags`'s `len(specs)==0` early return, so **every sampled span now gains +2 allocs / +256 B** even with no `custom_tags` configured. ACCEPTED (it matches how all four lookups are already threaded; a sampled span already allocates ~16 attributes), with the mitigation NAMED for a future profiling row: parse-time `hasHostTags`/`hasClusterTags` booleans. **Naming it a second time is deliberate — this is the point at which the pattern stops being free.**
- **The closure is a LAYERING rule, NOT a cycle guard.** `internal/cluster` does not depend on `internal/tracing`, so a direct import would build clean. Keep the closure (it preserves the layering the `go list -deps` gate asserts) but **never justify it as preventing a cycle** — that justification was refuted at phase 72 and corrected in ADR-0294 §Context; do not re-introduce it into ADR-0295.
- **Counts at the IMPL:** fixtures **118 → 119** (`0117-tracing-custom-tags-metadata-cluster`) · fuzzers **55 (+0)** — a seed ADD plus a REPOINT; `f.Add` is not `^func Fuzz` (`reference_fuzzer_count_docs_drift`) · stat surface **1201 (+0)** — a span attribute registers no stat; **NO `TestNoNewStat` guard obligation for tracing rows** (the phase-71 V2 finding — do NOT manufacture a cited-but-unwritten one) · BackendKind **38 (+0)** · go.mod **+0** · ZERO new packages · DECISIONS tail stays **ADR-0295** (completed IN PLACE at the IMPL; next-free ADR-0296).
- **The pinned §9 wording lands MECHANICALLY** — B1–B5 are named obligations with the SPEC §9 replacement text; never silent rewrites, never paraphrases. They land at T7; ADR-0295 completes at T9.
- **Per-task hygiene** (`feedback_pertask_gofmt_lint`): `gofmt -l` + `go vet` + `golangci-lint run` on every touched package.
- **Worktree discipline** (`feedback_git_worktrees` / `feedback_subagent_worktree_detach` / `feedback_subagent_worktree_path_targeting` / **`reference_bash_cwd_reset_commits_to_main`**): pin the canonical root and use **`git -C <abs-worktree-path>`** for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run. Deliberate breaks restore with **`git restore` only**; breaks run **AFTER** committing (`reference_break_protocol_commit_first`).
- **Subagents commit locally; the controller squash-pushes at stage-close** (`feedback_subagents_no_push`, `feedback_push_to_origin`). Locate commits by SUBJECT (`git log --grep 'phase 73'`), never by position.
- **Known pre-existing flakes — do NOT reflex-classify as phase-73 regressions:** the `internal/cluster` `-race` outlier flake (`TestOutlierDetector_ConcurrentEjectExactlyOnce`, `outlier_test.go:766` — **load-bearing this row**; `reference_cluster_race_outlier_flake`); the full-suite startup flake; the SDS `init_fetch_timeout` dial-budget flake; and **TWO newly-observed, UNINDEXED load flakes** in byte-identical packages, isolate-green and NOT root-caused: `internal/httpclient TestOptions_ZeroValue_NoOpDefaults` and `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery`.

### Break protocol (binding on every task)

- **A break must COMPILE** (`reference_plan_break_instructions_dont_compile`). Breaks flagged `[NOT pre-compiled — substitution rule applies]`: at IMPL time, if it does not compile, **substitute a compiling equivalent, REPORT the substitution, record the TRUE result** — and remember that **a substitution's RATIONALE is itself a claim** (the phase-72 T4 precedent, where a stated rationale was false and had to be corrected).
- **A break must DISCRIMINATE** (`reference_probe_must_discriminate`): before recording it as proof, ask what the OTHER hypothesis would have printed. **⚠️ Concretely for T1:** a `ClusterMetaLookup` nil-guard break routed through a span attribute is **VACUOUS** (`descend` already returns `(nil,false)` without panicking and `MetaPath` is min-1 by parse validation) — assert on the accessor's own return.
- **A method value on the ZERO `Endpoint` is NON-nil.** Do not write a test or comment conflating the nil-lookup mechanism with the zero-`Endpoint` mechanism — they are different mechanisms sharing one outcome (`reference_endpoint_metalookup_host_metadata_seam`; two landed comments made exactly this error at phase 72).
- **`-count=1` on EVERY differential break** (`reference_differential_break_protocol_count1`); caching serves a stale PASS.
- **Confirm WHICH assertion fired** (`reference_deliberate_break_wrong_assertion`) — and whether a second property's firing is ENTAILED by the first.
- **A break that does NOT fire is a FINDING** — record it honestly in `PROGRESS.md`; do not route around it.
- **Full selector only:** `-run 'TestDifferential/0117-tracing-custom-tags-metadata-cluster'` — never bare `0117` (`reference_differential_run_selector`).
- **`Errorf` per independent property; `Fatalf` only for broken preconditions** (`reference_fatalf_makes_assertions_unreachable`).
- **The cluster metadata must be SERVED-this-arm** — the span must carry the STATIC cluster value, not a vacuous default. Break P is the anti-vacuity control; **Break Q is unique to this row** and is the only one that proves *selected*, not *only*.

### Identifier roster (`reference_spec_drafted_identifier_collision_check`)

**Verified FREE at `79f96455` (`internal/`, all 0 hits — RD-IDENT):** `kindMetadataCluster` · `clusterMetaLookup` · word-boundary `ClusterMetaLookup` · `clusterFilterMetadata`. ⚠️ **Use the word-boundary form for the accessor grep** — a naive substring `MetaLookup` grep returns many `RouteMetaLookup`/`MetaLookup` hits that are NOT collisions. **State the rule; cite no number (F8).** **REUSED (landed, free to reference):** `descend` · `structpbValueToString` · `CustomTagSpec.MetaNamespace`/`MetaPath`/`DefaultValue`/`HasDefault` · `kindMetadata`/`kindMetadataRoute`/`kindMetadataHost` · `MetaLookup` · `filterMetadata` · `ScalarsFromStruct` · `upstreamHostString`. **`metadatav3`** is the ESTABLISHED alias (`fuzz_test.go:13`). **Fixture:** `test/fixtures/0117*` does not exist; reference port **10117** is free (0 hits under `test/`). **Any FURTHER name the IMPL coins** (the two cluster names, the namespace/key strings, the two tag keys, any test name): grep first, record the check.

---

## File structure

```
internal/cluster/cluster.go                       [EDIT]  T1 (the clusterFilterMetadata field + the nil-safe ClusterMetaLookup accessor; ZERO new imports — structpb already at :21)
internal/cluster/manager.go                       [EDIT]  T1 (the buildCluster populate LOOP, IMMEDIATELY after the :433-436 guard and BEFORE :437; :754/:883/:884/:885 + extractEndpoints's signature BYTE-UNCHANGED; ZERO new imports)
internal/cluster/cluster_test.go                  [EDIT]  T1 (ClusterMetaLookup: present ns → wrapped struct; absent ns → (nil,false); ZERO Endpoint → (nil,false) no panic; nil *Struct → (nil,false))
internal/cluster/cluster_meta_test.go             [NEW]   T1 (the populate is live; THE SUBSET-LB ASSERTION (Break D's target); the envoy.lb projection + defaultSubset byte-unchanged guard; the pool-HIT provenance test)
                                                          ⚠️ A NEW FILE, *NOT* manager_test.go — that file is sha256-gated BYTE-UNTOUCHED at T4/T8 and adding tests to it makes its own gate UNSATISFIABLE (verifier V1 SEVERE-1). Name collision-checked at the PLAN tip: no internal/cluster/*meta*_test.go exists. Same package ⇒ buildCluster/newSubsetLB/subsetLB reachable, zero new exported symbols.
internal/tracing/config.go                        [EDIT]  T2 (kindMetadataCluster APPENDED at iota==6; the CLUSTER accept arm replacing the :268-269 reject; the stale comments incl. :185 AND :190 (F3); NO new import)
internal/tracing/config_test.go                   [EDIT]  T2 (the two byte-identical /cluster rows :639/:764 flip reject→accept; the two stale doc comments :627-632/:754-757; the kindMetadataCluster==6 pin; unset rows STAY reject)
internal/tracing/resolve.go                       [EDIT]  T3 (the clusterMetaLookup 6th param; the kindMetadataCluster arm between :160 and :161, FULL MetaPath; the :12-45 doc CLUSTER clause; NO new import)
internal/tracing/resolve_test.go                  [EDIT]  T3 (CLUSTER path-walk single/multi/unresolvable; default/omit/present-empty matrix; nil-tolerance; 49 existing calls += trailing nil — gate by LINE, raw grep is 50)
internal/filter/hcm/accesslog_emit.go             [EDIT]  T4 (:57/:118/:179 ONLY — append picked.ClusterMetaLookup as the 6th arg; ⚠️ :57 and :179 are BYTE-IDENTICAL (F2); the 3 SIGNATURES UNCHANGED; no import)
internal/filter/hcm/span_emit_test.go             [EDIT]  T4 (NEW: live per-site H1/H2/H3 CLUSTER-metadata span tests + a zero-picked test; the 21 existing callers BYTE-STABLE — additive only, NOT hash-gated)
internal/filter/hcm/accesslog_emit_test.go        [EDIT?] T4 (additive only if needed; the 13 existing callers BYTE-STABLE — additive only, NOT hash-gated)
internal/filter/hcm/fuzz_test.go                  [EDIT]  T5 (withMetaTags += meta_cluster_ok; meta_bad REPOINTED to Kind: &metadatav3.MetadataKind{}; narrow the stale :89-98 comment; +0 func Fuzz)
test/fixtures/0117-tracing-custom-tags-metadata-cluster/  [ADD]  T6 (driver/, envoy.yaml, envoy-go.yaml, expectations.yaml, README.md — NO scripts/), T6 (FIVE breaks N/O/P/Q + R — four SPEC-named plus R, PLAN-added)
test/differential/runner_test.go                  [EDIT]  T6 (ONE blank-import line at :144, after the 0116 line at :143)
docs/envoy-go/BEHAVIOR_CONTRACT.md                [EDIT]  T7 (B1+B2 as ONE old_string on :688; B3 on :690 as its OWN old_string; B4 :692; B5 :745)
docs/envoy-go/DECISIONS.md                        [EDIT]  T9 (ADR-0295 completed IN PLACE — APPEND ### Decision + ### Consequences after the :17229 footer; NO second §Decision heading; ⚠️ FIX THE HEADING's two refuted claims (F1); ⚠️ correct ADR-0294:17209 (F6))
docs/envoy-go/ROADMAP.md                          [EDIT]  T9 (row 73 → done; the pinned :203 narrow; the THREE stale row-cell claims)
internal/filter/hcm/{connection.go,h2dispatch.go,h3dispatch.go,actions.go,route.go} · internal/filter/http/chain.go · internal/cluster/subset.go · internal/cluster/manager_test.go · internal/xds/** · internal/tls/** · internal/boot/** · internal/listener/** · internal/bootstrap/** · validate/** · internal/dynamicmetadata/**  [BYTE-UNTOUCHED — sha256]
```

---

## Task 1 — `internal/cluster`: the `Endpoint.clusterFilterMetadata` field + the `ClusterMetaLookup` accessor + the `buildCluster` populate LOOP

**Files:**
- Modify: `internal/cluster/cluster.go` (the field beside `:79`; the accessor beside `:111-117`) · `internal/cluster/manager.go` (the loop between `:436` and `:437` ONLY)
- Test: `internal/cluster/cluster_test.go` (EDIT — test 1) · `internal/cluster/cluster_meta_test.go` (**NEW** — tests 2-5). ⚠️ **NOT `manager_test.go`: it is sha256-gated BYTE-UNTOUCHED at T4/T8** (V1 S1).

**Interfaces:**
- Produces: the unexported `Endpoint.clusterFilterMetadata map[string]*structpb.Struct` field + the exported `func (e Endpoint) ClusterMetaLookup(ns string) (*structpb.Value, bool)`. Consumed by `accesslog_emit.go` (T4) as the method value `picked.ClusterMetaLookup`.
- Consumes: `c.GetMetadata().GetFilterMetadata()` (both nil-receiver-safe) and `structpb.NewStructValue`.
- **ZERO new imports** — `structpb` is already at `cluster.go:21`, and the loop names no new type in `manager.go`.

**Entry state:** clean `79f96455`-derived branch; `go test ./internal/cluster/ -count=1` green.

- [ ] **Step 0 — re-run the FOUR collision greps at the IMPL tip** (SPEC §5 requires the re-run "at the PLAN and again at the IMPL tip"; `feedback_parallel_stream_mints_fresh_drift`). All four must be **0**: `kindMetadataCluster` · `clusterMetaLookup` · `clusterFilterMetadata` · **word-boundary** `grep -rnE '(^|[^A-Za-z])ClusterMetaLookup' --include='*.go' internal/ test/ validate/ cmd/`. ⚠️ **Use the word-boundary form; a naive substring grep returns many non-collisions (F8) — record the rule, not a number.** Also re-run `git diff --stat 79f96455 HEAD -- '*.go' go.mod go.sum test/` to confirm no parallel stream appeared. Record all results in `PROGRESS.md`. A non-zero result on any grep BLOCKS the task until the name is re-picked.

**Design (RE-DERIVED; §1.2 skeletons (a)(b)(c)):** the field ALIASES the already-parsed proto map ⇒ **zero new allocation per endpoint**; every endpoint of one cluster shares the SAME map. It is retained BESIDE — never replacing — the phase-72 `filterMetadata` (endpoint-level) and the phase-38 `envoy.lb` scalars-only `Metadata` projection, because **ANY `filter_metadata` namespace is addressable and `envoy.lb` is NOT privileged** (`reference_envoy_lb_namespace_not_privileged`), and a CLUSTER tag must also walk NESTED paths.

- [ ] **Step 1 — write the failing tests (red-first).**
  1. `cluster_test.go` — `TestEndpoint_ClusterMetaLookup`: (a) an `Endpoint` whose `clusterFilterMetadata` has `{"ns": {Fields: {"k": stringValue("v")}}}` → `ClusterMetaLookup("ns")` returns `(non-nil, true)` and `GetStructValue().GetFields()["k"].GetStringValue() == "v"`; (b) absent namespace → `(nil,false)`; (c) **the ZERO `Endpoint{}`** → `(nil,false)` **without panicking**; (d) `"ns"` mapped to a nil `*structpb.Struct` → `(nil,false)` (the guard's own contract). **(e) INDEPENDENCE:** an `Endpoint` carrying BOTH `filterMetadata` and `clusterFilterMetadata` with the SAME namespace but DIFFERENT values → `MetaLookup` returns the endpoint value and `ClusterMetaLookup` returns the cluster value. *(This is the unit-level form of the SPEC's W5a source-distinctness finding, and it is the only test that catches the two fields being crossed.)*
  ⚠️ **Tests 2-5 go in a NEW file `internal/cluster/cluster_meta_test.go`, NOT in `manager_test.go`** (verifier V1 SEVERE-1). `manager_test.go` is sha256-gated BYTE-UNTOUCHED at T4 and T8; adding tests to it makes its own gate unsatisfiable, which is the identical defect this PLAN already diagnosed for the emit test files (RD-EMITTESTS). The gate is KEPT — V1 executed this routing and `sha256sum -c` reports `manager_test.go: OK`. Same package, so `buildCluster`/`newSubsetLB`/`subsetLB` are all reachable and **zero new exported symbols** are needed.

  2. `cluster_meta_test.go` — **the populate is LIVE**: build a cluster whose `clusters[].metadata.filter_metadata` carries a NON-`envoy.lb` namespace with a NESTED value; assert the built `Endpoint.ClusterMetaLookup("<ns>")` resolves it, and that an endpoint-level `lb_endpoints[].metadata` value does NOT leak into it.
  3. `cluster_meta_test.go` — **THE SUBSET-LB ASSERTION (Break D's target; the row's distinguishing test).** Build a cluster carrying BOTH `clusters[].metadata.filter_metadata` AND an `lb_subset_config` selector (so `newSubsetLB` at `:481` runs and value-copies each `ep`), then reach an `Endpoint` **through the subset LB** and assert `ClusterMetaLookup` resolves.
     ⚠️ **REACH IT VIA THE PACKAGE-INTERNAL FORM — `cl.lb.(*subsetLB).Pick(0, false, NewSubsetMatch(map[string]SubsetValue{…}), true)`.** An earlier draft of this step suggested `Cluster.PickEndpoint()` with a subset-selecting config; **verifier V1 EXECUTED that form under Break D and it stayed GREEN — the break is VACUOUS through `PickEndpoint`.** Mechanism, re-verified here at the PLAN tip: `PickEndpoint` calls `c.lb.Pick(0, false, SubsetMatch{}, false)` (`cluster.go:531-532`), so `hasMatch == false` and `subsetLB.Pick` (`subset.go:213-219`) never consults `s.subsets` — it goes straight to the fallback, and under `ANY_ENDPOINT` that fallback is `factory(endpoints)` over the **ORIGINAL slice** (`subset.go:183-185`) ⇒ `roundRobin` ALIASES the backing array ⇒ the zeroed field is invisible. (`NO_FALLBACK` can't pick at all; `DEFAULT_SUBSET` *does* fire, because that branch appends into a fresh slice at `subset.go:193` — the fourth copy site.) The `hasMatch == true` form is the only one that traverses `s.subsets` and therefore the only one that sees a value-copied endpoint. **This needs no new exported symbol** — `NewSubsetMatch` (`subset.go:68`) and `SubsetValue` (`:28`) are already exported and `subsetLB` (`:140`) is same-package; the ONE-new-exported-symbol envelope is intact.
     **This is the ONLY assertion that catches a populate loop placed after the LB construction** — the round-robin path shares the aliased backing array and would stay green (RD-PLACEMENT/F9).
  4. `cluster_meta_test.go` — **the byte-unchanged guard**: the phase-38 projection still behaves exactly as before (an `envoy.lb` scalar still lands in `Endpoint.Metadata` as a `SubsetValue`; a non-scalar `envoy.lb` key is still DROPPED from `Metadata`), `defaultSubset` unchanged, and the phase-72 `MetaLookup` still resolves endpoint metadata.
  5. `cluster_meta_test.go` — **the pool-HIT provenance test**: drive a real H1 pool MISS → `PutIdleH1` → HIT and assert `ClusterMetaLookup` still resolves on the hit. *(EXECUTED-clean at the SPEC; it must stay a live guard because a reconstructing pool path would zero the field on reuse while fresh-dial tests stayed green.)*

  Run `go test ./internal/cluster/ -count=1`. **Expected: FAIL** — `ClusterMetaLookup`/`clusterFilterMetadata` undefined (compile error). **Record the verbatim red.**

- [ ] **Step 2 — add the field + accessor** (§1.2 (a)(b)). APPEND the field beside `filterMetadata` (`:79`); place `ClusterMetaLookup` beside `MetaLookup` (`:111-117`). **Do NOT touch `Addr()`, the `addr` cache, `IsZero()`, `MetaLookup`, or the non-comparability comment. Do NOT add an import** (`structpb` is at `:21`).

- [ ] **Step 3 — add the populate LOOP** (§1.2 (c)). **Insert between `:436` and `:437` — IMMEDIATELY after the `err != nil` guard, BEFORE `validatePanicThresholdRange`.** ⚠️ **Anchor the `old_string` on `endpoints, err := extractEndpoints(la, name)` + the guard + `validatePanicThresholdRange` — the bare guard is NOT unique in this file (RD-POPULATE).** **`:754`/`:883`/`:884`/`:885` and `extractEndpoints`'s signature stay BYTE-UNCHANGED — verify with `git diff`.**

- [ ] **Step 4 — run the tests.** `go test ./internal/cluster/ -count=1`. **Expected: PASS** — all five `ClusterMetaLookup` cases, the live populate, **the subset-LB assertion**, the byte-unchanged guard and the pool-HIT provenance test; every pre-existing cluster test green (subset LB, maglev, ring_hash, health, locality, priority all untouched).

- [ ] **Step 5 — verify the SHAPE mechanically.** `git diff -U0 -- internal/cluster/manager.go` is a SINGLE insertion hunk between `:436` and `:437` (no other hunk); `git diff` on `cluster.go` shows exactly ONE added field + ONE added accessor and **no import-block hunk**; `internal/cluster/subset.go` and `manager_test.go` absent from `git diff --numstat`. Measure `unsafe.Sizeof(cluster.Endpoint{})` (SPEC MEASURED **112 → 120**; record the ACTUAL). Confirm **exactly ONE new exported symbol** by an AST/`go doc -all` symbol-set diff. **Prove the ALIASING mechanically** — mutate the source proto map after `buildCluster` and observe the change through `ClusterMetaLookup` (a copy would be invisible).

- [ ] **Step 6 — breaks (AFTER committing).**
  - **Break A [populate dropped]:** remove the loop → test 2 (the live populate) FIRES. `git restore`; re-green. *(Discriminates: proves the loop — not some other path — makes the namespace reachable.)*
  - **Break B [wrong source]:** populate from the ENDPOINT's metadata (`lbe.GetMetadata().GetFilterMetadata()`) instead of the CLUSTER's `c.GetMetadata()...` → test 1(e)'s independence assertion FIRES while `MetaLookup`'s own tests stay green. `git restore`; re-green. `[NOT pre-compiled — substitution rule applies]` *(Discriminates: proves the two retention fields are genuinely source-distinct — the W5a finding made executable. Note `lbe` is scoped to `extractEndpoints`, so the substitution may need a different always-wrong source; report it.)*
  - **Break C [nil guard dropped]:** remove `if st == nil { return nil, false }` and `return structpb.NewStructValue(st), true` unconditionally → **tests 1(b)/1(c)/1(d) FIRE on the ACCESSOR'S OWN RETURN** (`NewStructValue(nil)` is non-nil). `git restore`; re-green. `[NOT pre-compiled — substitution rule applies]` **⚠️ Assert on `ClusterMetaLookup`'s return, NOT on a span attribute — a span-routed break is VACUOUS.**
  - **Break D [placement — THE ROW'S DISTINGUISHING BREAK]:** move the loop from `:437` to **immediately after the `newSubsetLB` call at `:481`** (still "after the `extractEndpoints` call", still compiling, still green on round-robin) → **test 3 (the subset-LB assertion) FIRES while tests 2/4/5 STAY GREEN.** `git restore`; re-green. `[NOT pre-compiled — substitution rule applies]` *(Discriminates HARD: this is the SPEC §16 M3 hazard made executable. The round-robin path stays green because `buildLeafLB` aliases the backing array; only the value-copying subset LB sees the zeroed field. **If test 3 does NOT fire here, test 3 is not actually reaching a subset-copied endpoint and must be rewritten — that is a FINDING, not a pass.** Verifier V1 EXECUTED exactly this and found it in advance: through `Cluster.PickEndpoint()` the break stays GREEN, which is why test 3 is now pinned on the package-internal `subsetLB.Pick(…, hasMatch=true)` form. V1's measured output under Break D, with the corrected form: `SubsetChild` and `SubsetDefaultFallback` FAIL, `RoundRobinPath` / populate-live / aliases PASS — the required discrimination.)*

- [ ] **Step 7 — hygiene + `-race` + commit.** `gofmt -l internal/cluster` silent · `go vet ./internal/cluster/` · `golangci-lint run ./internal/cluster/` · `go test ./internal/cluster/ -race -count=1` (⚠️ **`TestOutlierDetector_ConcurrentEjectExactlyOnce`, `outlier_test.go:766`, is a PRE-EXISTING intermittent failure reproduced on the UNMODIFIED baseline — isolate-re-run; do NOT re-classify**).

**Commit:** `cluster(phase 73 T1): Endpoint retains the OWNING CLUSTER's RAW per-namespace static metadata — a second clusterFilterMetadata map[string]*structpb.Struct field ALIASING c.GetMetadata().GetFilterMetadata() (zero new allocation, sizeof 112->120 B) + a nil-safe ClusterMetaLookup(ns) accessor beside the phase-72 MetaLookup, populated by a LOOP in buildCluster placed IMMEDIATELY after the extractEndpoints err guard and BEFORE ALL LB CONSTRUCTION (buildLeafLB aliases the slice, but newSubsetLB/newLocalityWeightedLB/newPriorityLB copy each Endpoint BY VALUE — a later placement would silently zero the field on three LB shapes while round-robin stayed green, pinned by an explicit subset-LB assertion); extractEndpoints's signature, manager_test.go, :754/:883/:884/:885 all BYTE-UNCHANGED and ZERO new imports anywhere`

---

## Task 2 — `internal/tracing/config.go`: the `kindMetadataCluster` const + the CLUSTER accept arm

**Files:**
- Modify: `internal/tracing/config.go` (the `customTagKind` block `:55-62`; the `:268-269` CLUSTER reject; the stale comments `:48-52`, `:71`, `:75`-`:78`, `:180-186`, **`:190` (F3)**)
- Test: `internal/tracing/config_test.go` (the two byte-identical `/cluster` rows `:639`/`:764`; the stale doc comments `:627-632`/`:754-757`; the new iota pin)

**Interfaces:**
- Produces: `kindMetadataCluster` const (**iota == 6**); the CLUSTER accept path building `CustomTagSpec{Kind: kindMetadataCluster, MetaNamespace, MetaPath (FULL), DefaultValue, HasDefault}`. Consumed by `resolve.go` (T3).
- Consumes: `md.GetKind()` (→ `*metadatav3.MetadataKind`, bound as `k` at `:230`) then **`k.GetCluster()` on `k`, NOT on `md`** (RD-PROTO). **NO new import.**

**Entry state:** T1 landed; `go test ./internal/tracing/ -count=1` green (so the two `/cluster` subtests are genuinely the only pre-known red set).

**⚠️ The pre-known red set is EXACTLY TWO subtests**, both `t.Fatalf("NewConfig(%s) err = nil, want reject; got %+v", tc.name, got)`:
- `TestNewConfigRejectCustomTagMetadata_RejectKinds/cluster` — TABLE ROW `:639`, assertion prints at **`:648`**
- `TestNewConfigRejectCustomTagMetadataRoute_RejectKinds/cluster` — TABLE ROW `:764`, assertion prints at **`:773`**

⚠️ **`:639`/`:764` are the `old_string` targets; `:648`/`:773` are where the FAILURE prints. Not a contradiction.** ⚠️ **The two table rows are BYTE-IDENTICAL to each other (`grep -Fxc` ⇒ 2) — an `old_string` on either alone is NOT unique. Include the following `{"unset-kind", nil, "kind required"},` line and/or the enclosing func.**

**The live regression guard — these MUST STAY reject:** the `unset-kind` rows `:640`/`:765` (`"kind required"`), and any further unset-kind guard elsewhere in the file (re-grep; phase 72 found a third at `:460-464` — **re-derive its current line, do not copy that number**).

- [ ] **Step 1 — write the failing tests (red-first).** In `config_test.go`:
  1. ⚠️ **ORDERING IS LOAD-BEARING (verifier V1 MINOR-4): RETAIN the two `/cluster` reject rows THROUGH Steps 2-3, record the `Kind:6` red they produce, and only THEN convert them.** If Step 1 removes them first, the predicted red at the end of this step cannot fire at all and the by-EXECUTION iota proof is lost. **Convert** the two `/cluster` rows out of the `_RejectKinds` tables (AFTER Step 3) and add `TestNewConfigAcceptCustomTagMetadataCluster_Accept`: a `CustomTag{Tag:"t", Type:CustomTag_Metadata_{Metadata:{Kind:Cluster, MetadataKey:{Key:"ns", Path:[{Key:"a"},{Key:"b"}]}, DefaultValue:"d"}}}` parses to `CustomTagSpec{Key:"t", Kind:kindMetadataCluster, MetaNamespace:"ns", MetaPath:["a","b"], DefaultValue:"d", HasDefault:true}`. **The two-segment path is load-bearing** — it is what a `[1:]` slip would break.
  2. `…MetadataCluster_RejectStructural` — empty namespace / empty path / empty path segment for a CLUSTER-kind tag (PGV-PARITY).
  3. `…MetadataCluster_HasDefaultFalseWhenEmpty` — `DefaultValue:""` → `HasDefault == false`.
  4. `…MetadataCluster_FirstWinsDedup` — two same-key tags (a CLUSTER metadata + a later literal) → the FIRST wins.
  5. **Assert the const's VALUE** — `kindMetadataCluster == 6`, beside the landed `:856-867` pins (which assert `kindMetadataHost` IS 5 and `kindMetadataRoute` IS 4, spelled as `!=` guards). **This pins APPEND-not-INSERT mechanically; a silent renumber would otherwise fail nowhere.**

  Run `go test ./internal/tracing/ -run 'MetadataCluster' -count=1`. **Expected: FAIL** — `kindMetadataCluster` undefined (compile error). Record the verbatim red. Then run the full package and record the two predicted `/cluster` failures — **the output should print `Kind:6`**, which is the by-EXECUTION proof of iota==6 (do not infer it from the source).

- [ ] **Step 2 — add the const** (§1.2 (e)). **APPEND after `kindMetadataHost` (`:61`), before the `:62` closing paren** ⇒ iota == 6. **NEVER INSERT earlier.** ⚠️ **Expect gofmt to re-align the trailing comments on `:58-61` — that 4-line comment-column shift is EXPECTED, not damage (RD-KIND).** Verifier V1 MEASURED it: pasting skeleton (e) leaves `gofmt -l internal/tracing/` **NON-SILENT**, and `gofmt -d` shows exactly `:58`-`:61` trailing comments shifting **+2 spaces each** and nothing else. **So run `gofmt -w` BEFORE any per-task `gofmt -l`-must-be-silent gate, or that gate trips on expected re-alignment.** Skeletons (a)(b)(c)(d)(f)(h) are gofmt-clean AS PASTED — (e) is the sole exception. No struct change; no new import.

- [ ] **Step 3 — replace the `:268-269` CLUSTER reject** with the accept arm (§1.2 (d), **the gofmt-clean multi-line form**). **Branch on `k.GetCluster()` — `md.GetCluster()` DOES NOT COMPILE.** ⚠️ **Anchor the `old_string` on `:267`+`:268-269`+`:270` (the `kindMetadataRoute` spec line → the reject → the `k.GetHost()` case line) — the three accept bodies are near-identical (RD-REJECT).** Keep unset-A `:232-233`, the REQUEST/ROUTE/HOST accepts, `default:` `:287-288`, the empty-tag reject and the first-wins dedup `:293-297` UNCHANGED.

- [ ] **Step 4 — narrow the stale PRODUCTION comments.** `:48-52` (the `customTagKind` doc — add the CLUSTER source) · `:71` (the `Kind` field comment) · `:75`/`:76`/`:77`/`:78` (the four `CustomTagSpec` field comments — add `kindMetadataCluster` to each `Kind==…` list) · **`:180-186` (the `parseCustomTags` doc)**, especially **`:185`** — `// CLUSTER/unset kinds reject LOUDLY with distinct substrings` must narrow to **the unset kinds only**, ⚠️ **and it must NOT simply say "distinct": after the lift `:233` and `:288` BOTH emit `kind required`, so the distinctness claim goes false in its own right** · **`:190` (F3)** — `// All substrings are ADR-0080-distinct.` goes false for the same reason. **Record both string collisions as documented boundaries; do NOT split the strings here** (that changes landed error strings — ADR-0080 territory, deferred).

- [ ] **Step 5 — narrow the TWO stale TEST doc comments.** `config_test.go:627-632` and `:754-757` both assert CLUSTER rejects → narrow BOTH to the unset kinds only.

- [ ] **Step 6 — run the tests.** `go test ./internal/tracing/ -count=1`. **Expected: PASS** — the five new tests green; the unset rows `:640`/`:765` (and any third guard) STILL reject; `grep -rn "cluster kind unsupported" internal/ test/ validate/ cmd/` ⇒ **0 hits**; the literal/request_header/environment/REQUEST/ROUTE/HOST arms untouched.

- [ ] **Step 7 — breaks (AFTER committing).**
  - **Break E [accept→reject]:** make the `k.GetCluster()!=nil` arm return an error instead of building the spec → test 1 FIRES. `git restore`; re-green. `[NOT pre-compiled — substitution rule applies]` *(the phase-72 precedent: the naive form leaves `dv` declared-and-unused ⇒ a compile error; substitute `_ = md.GetDefaultValue()` and report it.)*
  - **Break F [kind confusion]:** swap the CLUSTER and HOST arms (accept HOST-as-cluster) → **TWO independent properties fire: test 1 (CLUSTER accept) AND the landed HOST accept tests.** **Confirm WHICH fired, and that BOTH did** — they are independent, not an entailment. `git restore`; re-green. `[NOT pre-compiled — substitution rule applies]`
  - **Break G [silent renumber]:** INSERT `kindMetadataCluster` BEFORE `kindMetadataHost` (so CLUSTER==5, HOST==6) → **test 5 AND the landed `kindMetadataHost == 5` pin (`:861`) FIRE.** `git restore`; re-green. *(Discriminates: these are the ONLY assertions that catch a renumber — a full renumber otherwise recompiles clean package-wide, which is exactly why the append rule needs a mechanical guard.)*
  - **Break H [HasDefault]:** set `HasDefault: true` unconditionally → test 3 FIRES. `git restore`; re-green.
  - **Break H2 [the `[1:]` slip at PARSE time]:** build the spec with `MetaPath: path[1:]` → test 1's two-segment assertion FIRES. `git restore`; re-green. *(Cheap, and it pins the FULL-path rule at the parse boundary as well as the resolve boundary — the phase-72 reviewer added exactly this probe ad hoc.)*

- [ ] **Step 8 — hygiene + commit.** `gofmt -l internal/tracing` silent · `go vet ./internal/tracing/` · `golangci-lint run ./internal/tracing/`.

**Commit:** `tracing(phase 73 T2): custom_tags metadata CLUSTER parse — kindMetadataCluster APPENDED at iota==6 (EXECUTION-proven by the red printing Kind:6; never inserted — a renumber fails nowhere, pinned by an explicit ==6 assertion) + the CLUSTER accept arm (a verified byte-clone of the HOST accept, MetaPath FULL, HasDefault = DefaultValue != "") replacing the :268-269 cluster-kind reject, the LAST kind-specific reject; branches on k.GetCluster() on the bound k (md.GetCluster() does not exist); BOTH unset-kind arms UNCHANGED (:233 a DEPARTURE across two spellings, :288 PARITY); the const block is NOT a pure-append diff (gofmt re-aligns :58-61); NO new import; narrows the stale production comments incl. :185 AND :190, whose "ADR-0080-distinct substrings" claims go false because :233 and :288 now COLLIDE on "kind required" — a documented boundary, deliberately not split`

---

## Task 3 — `internal/tracing/resolve.go`: the `clusterMetaLookup` 6th param + the `kindMetadataCluster` arm

**Files:**
- Modify: `internal/tracing/resolve.go` (the `ResolveCustomTags` signature `:46`; the doc block `:12-45`; a new `case` between `:160` and `:161`)
- Test: `internal/tracing/resolve_test.go`

**Interfaces:**
- Produces: `ResolveCustomTags(specs, headerLookup, metaLookup, routeMetaLookup, hostMetaLookup, clusterMetaLookup)` — the new nil-tolerant 6th param (ONE-arg, shaped like `hostMetaLookup`); the `kindMetadataCluster` resolve arm.
- Consumes: `descend` (`:172-186`) + `structpbValueToString` (`:195-212`) **REUSED VERBATIM**; `structpb.Value`, `KV`.
- Reuses UNTOUCHED: the five landed arms. **NO new import.**

**Entry state:** T1–T2 landed; `go test ./internal/tracing/ -count=1` green.

**Design (§1.2 (f)(g)):** the arm clones the LANDED `kindMetadataHost` arm (`:131-160`) — the `request_header` default rule (present-empty EMITS `""`), a ONE-arg lookup, and `descend(v, s.MetaPath)` on the **FULL** path, NOT `s.MetaPath[1:]` (the REQUEST-only Bucket artifact at `:98`).

- [ ] **Step 1 — write the failing tests (red-first).** The **49** existing `ResolveCustomTags(...)` calls each gain a trailing `nil` (6th arg — no CLUSTER tags). ⚠️ **GATE BY EXPLICIT LINE NUMBER, not a bare regex** — a raw grep returns **50**; `:107` is a `t.Errorf` STRING LITERAL and `:106` is its real call (RD-TESTCALLERS). **Re-grep at the IMPL tip and re-derive the list; do NOT copy 50.** Add CLUSTER tests driving a fake `clusterMetaLookup`:
  1. **path-walk single** — `MetaPath:["k"]` → `KV{Key,"v"}`.
  2. **path-walk multi** — `MetaPath:["a","b"]` → `KV{Key,"deep"}`; an unresolvable segment → default/omit; an unresolvable MIDDLE segment → default/omit.
  3. **the serialization table** — string→raw; number→`42`/`3.14`; bool→`true`/`false`; struct→`{"a":"b"}`; list→`["x","y","z"]` (assert the EXACT compact string; `reference_detrand_race_catches_protojson_value_substring`); terminal `NullValue`→unresolvable. *(A thin CLUSTER-arm re-assert, not a re-derivation of the table.)*
  4. **the default matrix** — present-non-empty→emit; present-EMPTY→emit `""` (**NOT** the default); absent namespace→default (if `HasDefault`) else omit.
  5. **nil-`clusterMetaLookup` tolerance** — a `kindMetadataCluster` spec with `clusterMetaLookup==nil` → default/omit, no panic.
  6. **CLUSTER ≡ HOST ≡ ROUTE on an identical source** — the same namespace struct + the same `MetaPath` through `routeMetaLookup`, `hostMetaLookup` and `clusterMetaLookup` yield the SAME `KV` (pins that the clone is faithful and the FULL-path rule is shared across three of four kinds).
  7. **SOURCE DISTINCTNESS** — four specs sharing one `MetaPath` but different kinds, with all four lookups returning DIFFERENT values → each `KV` carries its OWN source's value. *(The unit-level form of the SPEC's W5a finding, and the assertion that catches a crossed argument at `:46`.)*

  Run `go test ./internal/tracing/ -run 'Resolve' -count=1`. **Expected: FAIL** — `ResolveCustomTags` arity mismatch. Record the verbatim red.

- [ ] **Step 2 — write the arm + grow the signature** (§1.2 (f)(g)). Insert the `case` between `:160` and `:161`. Add the 6th param at `:46` (single line — match the landed formatting). Add a CLUSTER clause to the doc block `:12-45`, mirroring the `hostMetaLookup` paragraph at `:38-45`. `descend`/`structpbValueToString` reused VERBATIM — **NO new import**.

- [ ] **Step 3 — run the tests.** `go test ./internal/tracing/ -count=1`. **Expected: PASS**.

- [ ] **Step 4 — prove the REUSE by hash.** `sha256` over the `descend` (`:172-186`) and `structpbValueToString` (`:195-212`) bodies must be IDENTICAL before and after; a `diff` of the landed HOST arm at its old vs new line span must produce **no output**; `resolve.go`'s diff should show exactly ONE removed line (the old 5-param signature). Record in `PROGRESS.md`.

- [ ] **Step 5 — breaks (AFTER committing).**
  - **Break I [wrong slice — THE CRUX]:** descend `s.MetaPath[1:]` instead of the full `s.MetaPath` → tests 1/2's single/multi-segment assertions FIRE **while EVERY REQUEST, ROUTE and HOST subtest stays GREEN** (proving the arms are not entangled). `git restore`; re-green. *(This is the exact break that reproduced at phases 71 and 72.)*
  - **Break J [default rule — clone the wrong arm]:** make the present-empty case OMIT (the `kindEnvironment` rule) instead of emitting `""` → test 4's present-empty assertion FIRES **and nothing else** (the two rules predict DIFFERENT observables). `git restore`; re-green.
  - **Break K [nil-tolerance dropped]:** call `clusterMetaLookup(...)` without the `!= nil` guard → test 5 FIRES. **This COMPILES** (the phase-72 K precedent, EXECUTED); the nil func value panics at call time and test 5 catches it. **No substitution needed.** `git restore`; re-green.
  - **Break K2 [argument crossed]:** swap the 5th and 6th arguments at the call — **⚠️ they are ADJACENT and IDENTICALLY TYPED, so the compiler CANNOT catch it** → test 7 (source distinctness) and test 6 FIRE. `git restore`; re-green. *(The phase-72 whole-branch review found exactly this class for `routeMetaLookup`/`picked.MetaLookup`; with a fourth identically-typed lookup the hazard is now strictly worse, and test 7 is what makes it catchable.)*

- [ ] **Step 6 — `-race`.** `go test ./internal/tracing/ -race -count=1` (the protojson value-substring path).

- [ ] **Step 7 — hygiene + commit.** `gofmt -l internal/tracing` silent · `go vet` · `golangci-lint run ./internal/tracing/`.

**Commit:** `tracing(phase 73 T3): custom_tags metadata CLUSTER resolve — ResolveCustomTags grows a nil-tolerant one-arg clusterMetaLookup 6th param (the 3rd/4th/5th lookups UNTOUCHED) + a kindMetadataCluster arm descending the FULL MetaPath (NOT [1:] — the REQUEST slice is a Bucket-pre-keying artifact, so three of the four kinds now walk the full path); descend/structpbValueToString reused VERBATIM (proven by sha256); the request_header default rule (present-empty EMITS ""); the :12-45 doc gains a CLUSTER clause; NO new import; 49 resolve_test.go callers += trailing nil (raw grep 50 — :107 is a t.Errorf string literal, gated by line); a source-distinctness test pins the four ADJACENT IDENTICALLY-TYPED lookups against a crossed argument the compiler cannot catch`

---

## Task 4 — `internal/filter/hcm/accesslog_emit.go`: thread `picked.ClusterMetaLookup` at the THREE call sites (signatures + 18 callers UNTOUCHED)

**Files:**
- Modify: `internal/filter/hcm/accesslog_emit.go` — **`:57`/`:118`/`:179` ONLY**
- Test: `internal/filter/hcm/span_emit_test.go` (NEW tests; the 21 existing callers byte-stable), optionally `accesslog_emit_test.go` (additive only; 13 callers byte-stable)

**Interfaces:**
- Produces: `picked.ClusterMetaLookup` (T1's method value) passed as the **6th** `ResolveCustomTags` argument at each of the three call sites.
- Consumes: the already-present `picked cluster.Endpoint` (the 4th parameter of all three emit methods, `:27`/`:87`/`:149`) and T3's 6th param.
- **NO new import.**

**Entry state:** T1–T3 landed — and **`internal/filter/hcm` does NOT COMPILE** until Step 2. T3 grew `ResolveCustomTags` from 5 to 6 params but touched only `resolve.go`/`resolve_test.go`, leaving the three call sites 5-arg. **Expected red:**
```
internal/filter/hcm/accesslog_emit.go:57:NNN: not enough arguments in call to tracing.ResolveCustomTags
internal/filter/hcm/accesslog_emit.go:118:NNN: not enough arguments in call to tracing.ResolveCustomTags
internal/filter/hcm/accesslog_emit.go:179:NNN: not enough arguments in call to tracing.ResolveCustomTags
```
⚠️ **Do NOT conflate the two arities.** What makes this row (like phase 72) cheap is that the **emit-method signatures** do not change, so the 18 callers are untouched. The **`ResolveCustomTags` arity DOES change**, exactly as in 70/71/72 — so T4's red is an ordinary COMPILE error, **not** a value mismatch. *(Phase 72's V1 caught precisely this conflation in a PLAN draft; the columns shift with the added argument, so quote the line:col from the ACTUAL run rather than predicting it.)*

**⚠️ THE ROW'S CENTRAL COST CLAIM — verify it mechanically, do not assume it.** The three emit SIGNATURES and **all 18 emit callers** stay BYTE-UNTOUCHED, because the closure is built LOCALLY from the in-scope `picked` rather than threaded as a parameter (RD-SEAM). The **34 existing emit test callers** (13 + 21) are likewise untouched — though both test files are ADDITIVE edit sites.

- [ ] **Step 1 — write the failing tests (red-first).** In `span_emit_test.go` (clone the phase-72 HOST-metadata span tests, swapping the endpoint metadata source for the CLUSTER one):
  1. **Live per-site CLUSTER-metadata span tests, ×3 (H1/H2/H3 independently).** ⚠️ **`clusterFilterMetadata` is UNEXPORTED, so a `package hcm` test cannot build one by literal — and adding a test constructor would mint a SECOND new exported symbol, contradicting this row's envelope (exactly ONE).** Use the POPULATE PATH, which needs zero new symbols: `cluster.NewManager(bs, stats.NewRegistry())` → `Manager.Get("c_test")` → **`Cluster.PickEndpoint()`** (already exported, no dial). A non-`envoy.lb` namespace with a NESTED value on `clusters[].metadata` round-trips through the T1 loop and out of `ep.ClusterMetaLookup(...)`, driven through `emitAccessLog`/`emitAccessLogH2`/`emitAccessLogH3` with a SAMPLING `traceDecision`, asserting the exported span carries the resolved `{tag,"v"}` attribute. **All three call sites must be proven live independently** — a single-helper test would leave two of three unproven.
  2. **A first-class ZERO-`picked` test** — the SAME config with `picked = cluster.Endpoint{}` → the tag falls to `default_value` (and, with an empty default, is OMITTED entirely — assert by PRESENCE, not `== ""`, since the `request_header` rule makes present-but-empty emit `""`). **This is the 5-of-12-span-capable arm** (`connection.go:597`/`:699`, `h2dispatch.go:530`, `h3dispatch.go:280`/`:341`). ⚠️ **Do NOT describe this as "10 of 18" or "11 of 18"** — those raw figures count 6 PRE-Decide sites that can never emit a span at all.
  3. **A CLUSTER-vs-HOST distinctness span test** — one config where the endpoint metadata and the cluster metadata carry the SAME namespace+key with DIFFERENT values, and both a HOST tag and a CLUSTER tag are configured → each span attribute carries its OWN source's value. *(The end-to-end form of Break K2's property; it is what catches the two method values being crossed at the call site, which the compiler cannot.)*

  Run `go test ./internal/filter/hcm/ -count=1`. **Expected: FAIL — a COMPILE error**, the three `not enough arguments` (see Entry state). Record the verbatim red. ⚠️ **The new tests' resolved-value assertions are NOT what proves the thread is live at this point — they cannot even run yet. Their liveness is proven by Breaks L and M after Step 2.**

- [ ] **Step 2 — append the 6th argument** at `accesslog_emit.go:57`, `:118`, `:179` (§1.2 (h)). ⚠️ **`:57` and `:179` are BYTE-IDENTICAL (F2) — edit by LINE NUMBER or with surrounding context; an `old_string` on either alone is not unique.** **Do NOT touch the three signatures. Do NOT touch any of the 18 callers. Do NOT add an import.** Afterwards verify `grep -c 'picked.ClusterMetaLookup' internal/filter/hcm/accesslog_emit.go` ⇒ **3**.

- [ ] **Step 3 — run the tests.** `go test ./internal/filter/hcm/ -count=1`. **Expected: PASS** — the three live per-site CLUSTER-span tests + the zero-`picked` test + the distinctness test green; all 34 pre-existing emit test callers green with NO edit.

- [ ] **Step 4 — verify the cost claim MECHANICALLY (not by eye) — the TWO-PART envelope gate.** **The two parts are NOT interchangeable.**
  - **(a) SHA256 byte-identity on SEVEN production files:** `connection.go`, `h2dispatch.go`, `h3dispatch.go`, `internal/filter/http/chain.go`, `internal/cluster/subset.go`, **`internal/filter/hcm/actions.go`**, **`internal/filter/hcm/route.go`**. *(The last two are exactly what the rejected route-carried seam would have touched — they are the mechanical proof that the seam decision was honoured.)* Confirm each path EXISTS first (anti-vacuous), then hash.
  - **(b) CALLER-SET invariance — NOT a hash — on `accesslog_emit_test.go` and `span_emit_test.go`**, since this very task ADDS tests to both: assert the caller counts hold at **13** and **21** using **`grep -cE 'emitAccessLog(H2|H3)?\('`** (⚠️ **the gated form — the naive `grep -c 'emitAccessLog'` returns 23 for `span_emit_test.go`, F4**), that the three emit signatures are unchanged, and that a `diff` against the pre-T4 blobs yields **ZERO `<` lines** (purely additive — stronger than counting `-` lines).
  - Also: `git diff --stat` shows `accesslog_emit.go` + the test file(s) ONLY within `internal/filter/hcm`; `accesslog_emit.go`'s import block byte-identical; `git diff -U0` on it is exactly THREE lines. Record in `PROGRESS.md`. **A failure here refutes the row's headline claim and must be reported, not worked around.**

- [ ] **Step 5 — breaks (AFTER committing).**
  - **Break L [6th arg dropped at ONE site]:** pass `nil` instead of `picked.ClusterMetaLookup` at `:57` (H1) only → the H1 live CLUSTER-span test's resolved-value assertion FIRES while the H2/H3 tests stay green. `git restore`; re-green. *(Discriminates per-site — proves each call site is independently load-bearing, which a single combined test could not.)*
  - **Break M [wrong lookup]:** substitute a lookup that always returns `(nil,false)` → the three resolved-value assertions FIRE and the zero-`picked` test STAYS green (it already expects default/omit) — **confirm WHICH fired.** `git restore`; re-green. `[NOT pre-compiled — substitution rule applies]`
  - **Break M2 [5th/6th crossed at the call site]:** swap `picked.MetaLookup` and `picked.ClusterMetaLookup` at all three sites — **the compiler CANNOT catch this** → test 3 (CLUSTER-vs-HOST distinctness) FIRES while the landed phase-72 HOST thread tests may stay green (they pass the zero `Endpoint` or a HOST-only config). **Confirm WHICH fired.** `git restore`; re-green. *(This is the phase-72 whole-branch-review probe, promoted to a first-class break because this row makes the hazard strictly worse — there are now FOUR adjacent identically-typed lookups.)*

- [ ] **Step 6 — hygiene + `-race` + commit.** `gofmt -l internal/filter/hcm` silent · `go vet` · `golangci-lint run ./internal/filter/hcm/` · `go test ./internal/filter/hcm/ -race -count=1`.

**Commit:** `hcm(phase 73 T4): thread picked.ClusterMetaLookup as the 6th ResolveCustomTags arg at accesslog_emit.go:57/:118/:179 — the CLUSTER-kind metadata custom_tag now resolves the OWNING CLUSTER's static metadata onto the ingress span on both exporters, gated on the PICKED HOST exactly as the reference gates it. THE THREE EMIT SIGNATURES AND ALL 18 EMIT CALLERS ARE BYTE-UNTOUCHED (sha256 on SEVEN production files incl. actions.go and route.go — precisely what the rejected route-carried seam would have touched); the two emit TEST files are gated on CALLER-SET invariance (13+21=34) rather than a hash, since this task adds tests to both; a first-class zero-picked test pins the 5 span-capable local-reply sites, and a CLUSTER-vs-HOST distinctness test pins the two ADJACENT IDENTICALLY-TYPED method values against a crossing the compiler cannot catch`

---

## Task 5 — fuzz: the `meta_cluster_ok` seed ADD + the MANDATORY `meta_bad` REPOINT (+0 fuzzers)

**Files:**
- Modify: `internal/filter/hcm/fuzz_test.go` (the `withMetaTags` **variable** `:99-122`; the stale comment `:89-98`)

**Entry state:** T1–T4 landed. Fuzzer `FuzzHCMConfigParse`; seed `withMetaTags` (RD-FUZZ). ⚠️ **`withMetaTags` is a VARIABLE, not a function** — `grep 'func withMetaTags'` returns nothing.

- [ ] **Step 1 — ADD the `meta_cluster_ok` seed** (§1.2 (i)), cloning the `meta_host_ok` block (`:112-116`). `metadatav3` already imported (`:13`) ⇒ zero new imports. Re-derive the exact `MetadataKind_Cluster_`/`MetadataKind_Cluster` wrapper spellings at the tip.

- [ ] **Step 2 — REPOINT `meta_bad`** (`:117-119`) **to `Kind: &metadatav3.MetadataKind{}`** — the kind message PRESENT with its oneof unset. ⚠️ **THIS IS MANDATORY, and for a sharper reason than "it would stop rejecting":** as-is, `meta_bad` carries a CLUSTER kind and **no `MetadataKey` at all**, so post-lift it falls THROUGH the new accept arm and rejects at `empty namespace` — **still red, silently, with nothing going green and no test catching it** — and that shape is precisely the R2e **DEPARTURE**, so leaving it converts a parity-reject seed into a departure-shaped one. ⚠️ **Do NOT use the BRAINSTORM's proposed nil/absent-`kind` target — that is the OTHER departure arm (`config.go:233`, R3a).** The chosen target is **PARITY, EXECUTED both sides** (envoy-go `config.go:288`; the reference PGV-rejects `field: "kind", reason: is required`). *(Equally valid parity alternatives, also executed: `MetadataKey: &metadatav3.MetadataKey{}` → `empty namespace`; a key-set/path-empty shape → `empty path`.)*

- [ ] **Step 3 — narrow the stale doc comment `:89-98`.** It is false in THREE places: `:93` "one REJECTED CLUSTER-kind tag", `:93-94` "CLUSTER stays an envoy-go-strict departure, ADR-0080", `:97-98` "the CLUSTER kind-unsupported reject arm". Update the header (`Phase 70/71/72` → `70/71/72/73`), the mid-block enumeration and the closing sentence. ⚠️ **Do NOT disturb the adjacent dispatch-order sentence** (`:94-96`, *"The custom_tags loop runs BEFORE the provider check …"*) — it is CORRECT and shares lines with the stale text. Re-grep the tokens at the tip before building any `old_string`. *(Phase 72's T5 over-edited this exact block once and had to revert.)*

- [ ] **Step 4 — dispatch-verify BOTH arms, with the discrimination stated BEFORE the run** (`reference_probe_must_discriminate`). As a one-off scratch check (NOT committed), temporarily instrument BOTH the `kindMetadataCluster` ACCEPT arm and the `config.go:288` reject arm; run `go test ./internal/filter/hcm/ -run FuzzHCMConfigParse -count=1 -v`; confirm `meta_cluster_ok` reaches the **CLUSTER ACCEPT** arm AND the repointed `meta_bad` reaches the **`config.go:288`** reject — **not merely that it still rejects, since it rejects either way.** ⚠️ **Because `:233` and `:288` now emit the IDENTICAL string `kind required`, the error text CANNOT distinguish them — the instrumentation must tag the two arms separately or the check is laundered by the collision.** ⚠️ **The first-error-return trap:** `parseCustomTags` returns on the first error and `meta_bad` is LAST, so **seeing the later seed's line at all proves the earlier ones did not short-circuit.** Restore; `git diff` empty. **Record the ACTUAL output in `PROGRESS.md`.**

- [ ] **Step 5 — reconcile the count.** `grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l` → **55** BEFORE and AFTER (`f.Add` is not `^func Fuzz`). A short active-fuzz smoke (`-fuzztime 10s`) — no panic; **NO corpus artifacts committed** (verify `git status` clean of `testdata/fuzz/`).

- [ ] **Step 6 — hygiene + commit.** `gofmt -l internal/filter/hcm` silent · `go vet` · `golangci-lint run ./internal/filter/hcm/`.

**Commit:** `hcm(phase 73 T5): fuzz — an ADDITIVE meta_cluster_ok seed exercising the new CLUSTER accept arm, plus the MANDATORY meta_bad REPOINT to Kind: &metadatav3.MetadataKind{} (present kind, oneof unset — the config.go:288 PARITY arm). The repoint is required for a sharper reason than "it would stop rejecting": as-is the seed carries no MetadataKey at all, so post-lift it falls THROUGH the accept arm and rejects SILENTLY on empty namespace — which is itself the newly-discovered absent-metadata_key DEPARTURE, converting a parity seed into a departure-shaped one; the BRAINSTORM's proposed nil-kind target is the OTHER departure. Dispatch-verified with the arms tagged separately, since :233 and :288 now COLLIDE on "kind required" and the error text alone would launder the result; narrows the stale :89-98 comment without disturbing the adjacent dispatch-order sentence; +0 fuzzers, 55->55`

---

## Task 6 — fixture `0117-tracing-custom-tags-metadata-cluster` (OTLP, TWO CLUSTERS) + breaks (fixtures 118 → 119)

**Files:**
- Create: `test/fixtures/0117-tracing-custom-tags-metadata-cluster/{driver/driver.go, envoy.yaml, envoy-go.yaml, expectations.yaml, README.md}` — **NO `scripts/`**
- Modify: `test/differential/runner_test.go` (ONE blank-import line at **`:144`**, immediately after the `0116` line at `:143`)

**Entry state:** T1–T5 landed. Next fixture `0117`; reference port **10117** free (0 hits under `test/`).

**Design (SPEC §8 — ONE OTLP fixture, NO Zipkin dir; clone the `0116` chassis):**

- Both YAMLs clone `0116` (H1 listener → `HTTPFixedBody` backend, HCM OTLP tracing provider → the driver-owned `test/helpers/otlptrace.Server` receiver, `random_sampling:100`, ports templated except `refListenerPort = 10117`). `BackendKind` stays `HTTPFixedBody` (+0, tail **38**); **`BackendCount` stays 1**; NO writer.
- **⚠️ TWO CLUSTERS, NOT ONE — the S10 decision, and the whole point of the fixture.** `0116` shipped with a limitation it had to confess in review: a single cluster cannot discriminate *selected* from *only*.
  - **`c_backend_a`** and **`c_backend_b`**, both `type: STRICT_DNS` (reference) / `STATIC` (subject), **both pointing at the SAME single `HTTPFixedBody` backend host:port**, differing ONLY in `metadata.filter_metadata[<ns>][cl_k]` — `v-cluster-a-0117` vs `v-cluster-b-0117`. ⚠️ **The metadata block sits on the CLUSTER (a sibling of `name:`/`type:`), NOT on `lb_endpoints[]`** — that is `0116`'s source and putting it there is Break Q's failure mode.
  - **Two routes**: `prefix: /a` → `c_backend_a`, `prefix: /b` → `c_backend_b`. Deterministic by PATH, so **no LB spread enters the assertion** (`reference_round_robin_offset_randomized` never engages).
- **TWO CLUSTER tags on the single HCM**, asserted cross-side EXACT key+value, **KEY-BASED** (intra-custom-tag order is ARBITRARY **across processes** — reuse `0116`'s `spanAttrMap`/`assertAttrString`): `cl_hit` → `metadata_key{key:<ns>, path:[cl_k]}` resolving to the per-cluster STRING; `cl_default` → an absent path segment, falling to `default_value`. **Four assertions across two paths.**
- **⚠️ The namespace must NOT be `envoy.lb`** — use a deliberately arbitrary one (`envoy.test`, as `0116` did), so the fixture is the cross-side proof that any namespace is addressable.
- **⚠️ STRING VALUES ONLY.** A multi-key struct value is NOT cross-side comparable (the reference serializes struct keys in an ARBITRARY order; Go sorts), scalar-vs-nested numbers use DIFFERENT reference renderers, and **top-level scalar numbers render at ~6 significant digits on the reference — biting from `1000000`→`1e+06`, far earlier than phase 72's example implied.** Also, Envoy's YAML loader turns any non-integer scalar into a STRING.
- **⚠️ PER-PATH SPAN PARTITIONING — the one genuinely new driver problem, and it needs deciding before code is written.** The assertion is "each span carries ITS OWN cluster's value", so spans must be attributable to the path that produced them. **`upstream_cluster` CANNOT be used** — `0116` asserts it `assertAttrPresent` only, because **envoy-go emits it EMPTY** (`reference_tracing_upstream_cluster_framework_gap`).
  - **PRIMARY mechanism — partition by the `http.url` attribute.** It is present on BOTH sides. ⚠️ **`0116` asserts it with `assertAttrPresent`, NOT `assertAttrString`, precisely because its value "varies by host/path encoding"** — so **Step 1 must first DUMP `http.url` on both sides and confirm the path is recoverable** (e.g. by suffix/`strings.Contains`) before any assertion depends on it.
  - **NAMED FALLBACK (use this if the dump shows `http.url` is not reliably path-bearing cross-side): two DRIVE BATCHES with a poll barrier.** Fire the `/a` workload, `pollSpanCount` to `N_a`, snapshot `srv.Spans()` (exactly the `/a` set); then fire `/b`, poll to `N_a+N_b`, and take the TAIL as the `/b` set. This attributes by arrival batch rather than by attribute and needs no cross-side value agreement. **Record which mechanism was used and why.**
  - ⚠️ **Keep `numTotal` at 12** (split `numPlain` 8 → 4+4 and `numCont` 4 → 2+2 across the two paths) so the landed span-count assertions and the `spans_sent == 12` stat assertion stay consistent (`reference_fixture_workload_constant_desync` — changing N/K desyncs count slices, and it stays masked until `-count=1`).
- **Inherited cross-side asymmetry, already proven safe:** reference clusters are `STRICT_DNS` (`host.docker.internal`, ADR-0010) while envoy-go's are `STATIC`. Here the metadata sits on the CLUSTER rather than the endpoint, so **DNS resolution is not even in the path** — strictly easier than `0116`.

- [ ] **Step 1 — the `http.url` reconnaissance (BEFORE writing assertions).** Stand up the fixture with the two clusters/routes and a temporary per-side attribute dump (the `FIXTURE_0116_DUMP=1` precedent). Confirm on BOTH sides that each span's path is recoverable. **Choose the partition mechanism, record the choice and the evidence in `PROGRESS.md`.**
- [ ] **Step 2–3 — write driver/YAMLs/expectations/README.** Clone the `0116` chassis; add the second cluster and the two prefix routes; move the metadata onto BOTH clusters; rename the tags/values; reuse `spanAttrMap`/`assertAttrPresent`/`assertAttrString`; `Errorf` per property. `BackendCount() → 1`, `BackendKind() → fixture.HTTPFixedBody`, self-register in `init()`. Add the blank import at `runner_test.go:144`. ⚠️ **`expectations.yaml` is prose (ADR-0019) — and unlike `0116` it must NOT confess a selected-vs-only limitation, because this fixture removes it. State positively what the two-cluster design proves.**
- [ ] **Step 4 — run.** `go test ./test/differential/ -run 'TestDifferential/0117-tracing-custom-tags-metadata-cluster' -count=1`. **Expected: PASS**; fixture count **119**. Confirm the cluster metadata is **SERVED** (each span carries its own cluster's static value, not a vacuous default) and that each of the four assertions fires its equality check the expected number of times (a span-count `Fatalf` precondition makes an empty-slice silent pass structurally impossible).
- [ ] **Step 5 — hygiene + commit.**

- [ ] **Step 6 — breaks (AFTER committing; `-count=1`, FULL selector, confirm WHICH fired).**
  - **Break N [static-value swap on ONE cluster only]:** change `c_backend_a`'s metadata value on ONE side → the `cl_hit` value-equality assertion FIRES **subject-side only**, proving the green is not a shared-constant tautology. `git restore`; re-green.
  - **Break O [default drop]:** empty `cl_default`'s `default_value` on both sides → the tag OMITS → the **PRESENCE** branch FIRES with **ZERO value-mismatch firings**, discriminating OMIT from emitted-as-`""`. `git restore`; re-green.
  - **Break P [anti-vacuity control]:** point `cl_hit`'s `metadata_key.key` at a namespace ABSENT from both clusters → `cl_hit`'s assertions FIRE while `cl_default`'s fire **ZERO** ⇒ the green depends on the cluster-metadata/namespace binding, not on a default match that would pass regardless. `git restore`; re-green. `[NOT pre-compiled — substitution rule applies]`
  - **Break Q [THE SELECTION BREAK — unique to this fixture]:** **swap the two clusters' metadata values** → the `cl_hit` assertion FIRES **on BOTH paths**. `git restore`; re-green. *(This is the ONLY break that proves *selected*, not *only* — the limitation `0116` had to confess. If it fires on only one path, the partition mechanism is broken and that is a FINDING.)*
  - **Break R [source relocation control, RECOMMENDED]:** move the metadata block from the CLUSTER onto `lb_endpoints[0]` (where `0116` has it) while keeping `kind: cluster` → both sides fall to default → `cl_hit` FIRES. *(Discriminates CLUSTER from HOST: proves the fixture reads the CLUSTER source, not a leftover endpoint source.)* `git restore`; re-green. `[NOT pre-compiled — substitution rule applies]`

**Commit:** `differential(phase 73 T6): fixture 0117-tracing-custom-tags-metadata-cluster — the 0116 OTLP chassis with TWO clusters carrying DIFFERENT static metadata.filter_metadata behind two path-prefix routes onto the SAME single backend, so the fixture DISCRIMINATES selected-cluster from only-cluster (the limitation 0116 had to confess) with zero LB spread and BackendCount 1; two CLUSTER custom_tags asserted cross-side EXACT key+value key-based, STRING values only, an arbitrary non-envoy.lb namespace, NO writer; breaks N/O/P/Q/R with Q the selection break and P the anti-vacuity control (fixtures 118->119, reference port 10117)`

---

## Task 7 — BEHAVIOR_CONTRACT delta (B1–B5) — pinned VERBATIM

**Files:**
- Modify: `docs/envoy-go/BEHAVIOR_CONTRACT.md`

**Entry state:** T1–T6 landed. Docs-only. **Anchor by SYMBOL / first-clause and RE-DERIVE every line number at the IMPL tip** — `:688`/`:690`/`:692`/`:745` are this PLAN's re-derived values and will shift if anything above them changes.

- [ ] **B1 + B2 — APPLY AS ONE `old_string` ON `:688`.** ⚠️ **They edit the SAME physical line** (`grep -n 'unset-kind'` ⇒ exactly ONE hit, `:688`), so a second edit would fail against text the first rewrote. ⚠️ **HIGHLY DELICATE — `:688` is ONE 3866-character paragraph** interleaving the false claims with still-true ones (the `literal`/`request_header`/`environment` semantics, the shared serialization rule, the default/omit rule, the four-arm locality proof, most of the PGV-parity clause). **Edit with surrounding context, never on a bare fragment.**
  - **B1:** the `metadata` type flips to CONSUMED for **all four** MetadataKinds, naming the CLUSTER source as the owning cluster's `clusters[].metadata.filter_metadata[ns]` reached over the new `cluster.Endpoint.ClusterMetaLookup()`; *"Three of the four … only `CLUSTER` remains reject-only"* → **all four consumed**; the FULL-`MetaPath` sentence extends to CLUSTER (**three of four kinds walk the full path; only REQUEST pre-keys on `path[0]`**).
  - **The load-bearing sentence being replaced** (byte-verified present, count == 1): *"The `CLUSTER`/unset-kind (sub-case A — a fully-unset `MetadataKind`) arms reject loudly (envoy-go-strict DEPARTURE — the reference boots them); the empty-namespace/empty-path/empty-segment and present-but-empty-oneof (sub-case B) rejects are PGV-PARITY (both reject)."* ⚠️ **Its first half is false AND its second half is PARTLY false.** `empty-namespace` is a **DEPARTURE in its absent-`metadata_key` spelling**, so the PGV-PARITY list is over-broad by one arm and must be **SPLIT, not merely trimmed at the front.**
  - **B2:** the departure roster narrows to **TWO reject ARMS across THREE config spellings** — (a) `config.go:233`'s fully-unset `kind` (two spellings: no `kind` field, and `metadata: {}`), and (b) the **entirely-absent `metadata_key`** (one spelling), **NEW and never before recorded**. ⚠️ **Count the unit explicitly — "THREE arms" is the refuted framing (F1).** Plus: envoy-go emits ONE error string (`empty namespace`) across a PARITY case and a DEPARTURE case, and after this row the two surviving unset arms ALSO share `kind required` — **two documented string collisions, NEITHER fixed here.**
- [ ] **B3 — `:690`, its OWN `old_string`** (⚠️ **`:689` is blank; `:690` is a SEPARATE physical line from `:688` — do not merge it into B1+B2**). EXTEND the phase-72 B2 clause, do not duplicate it: a CLUSTER-kind tag becomes the **FIFTH consumer** of the same PRE-EXISTING zero-`picked` gap, at the SAME breadth and SAME site set. **Two narrowings land with it:** (i) the reference's rule is EXECUTED-established as **pick-gated for CLUSTER too** — cluster selected + no host picked ⇒ `default_value` **while `upstream_cluster` sits on the same span** — so the *"materially worse justification"* framing is **RETIRED**; (ii) the **circuit-breaker arm moves from "likely parity, unprobed" to PARITY on EXECUTED reference evidence (S8)**, narrowing the unprobed set to **pool-overflow alone**. ⚠️ **Scope (ii) EXACTLY: the REFERENCE half is EXECUTED; envoy-go's half is RE-DERIVED from the landed clause's own citations, NOT cross-side probed.**
- [ ] **B4 — `:692`:** "shared by all three MetadataKinds" → **four**; the Zipkin empty-string drop RE-CONFIRMED and proven GENERIC via the `node_id`/`zone` built-ins in the same payload; the scalar-number precision boundary **re-quantified as biting from `1000000`**; the arbitrary struct-key order re-confirmed across three fresh processes; and the intra-custom-tag emit order sharpened to **arbitrary PER PROCESS** (not merely "not config order").
- [ ] **B5 — `:745`:** the tracing `metadata` boundary line narrows to the **three unset/absent arms**. ⚠️ DELICATE — this bullet mixes the false metadata-kind roster with the still-true `spawn_upstream_span`/`http_service`/`resource_detectors`/`sampler` strict-reject list AND the still-true B2 clause.
- [ ] **Verify UNCHANGED:** the `literal`/`request_header`/`environment`/REQUEST/ROUTE/HOST lines + `max_path_tag_length` + the built-in-span-attrs neighborhood + the Zipkin tag-roster paragraph (use `--word-diff=porcelain`). **The three "no span on a 404 no-route" clauses (`:690`, `:714`, `:744`) all STAY TRUE and constrain the fixture** — a no-route arm is not expressible. ⚠️ **`upstream_cluster` must NOT appear in the new text** (`reference_tracing_upstream_cluster_framework_gap` — do not conflate the span-TAG framework gap with the CLUSTER MetadataKind; they are different features).

**Commit:** `docs(phase 73 T7): BEHAVIOR_CONTRACT B1-B5 — flip the custom_tags metadata CLUSTER line to CONSUMED, completing ALL FOUR MetadataKinds (the owning cluster's clusters[].metadata.filter_metadata path-walk over the new cluster.Endpoint.ClusterMetaLookup, gated on the PICKED HOST exactly as the reference gates it); B1+B2 applied as ONE old_string since they share :688; the departure roster narrowed to TWO reject ARMS across THREE config spellings — NOT "three arms" — including a NEWLY DISCOVERED, never-recorded departure (an entirely-absent metadata_key, which the reference ACCEPTS), and the PGV-PARITY list SPLIT rather than trimmed because empty-namespace is a departure in that one spelling; two documented error-string collisions recorded and neither fixed; B3 EXTENDS the phase-72 pick-time clause to a FIFTH consumer, RETIRES the "materially worse justification" framing on probed evidence, and moves the circuit-breaker arm to PARITY (reference EXECUTED, envoy-go RE-DERIVED), leaving pool-overflow the sole unprobed arm`

---

## Task 8 — VERIFY: the six-gate + layering check + full differential + `-race` + counts + envelope audit

Controller-run on the frozen pre-stage-close HEAD. ⚠️ **Use `git -C <abs-worktree-path>` and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (NEVER `master`) before every gate** (`reference_bash_cwd_reset_commits_to_main` — at the phase-72 close a cwd reset made a "final gates" run validate master's pre-row tree, GREEN while proving nothing).

- [ ] 1. `gofmt -l internal/ test/ cmd/` — SILENT
- [ ] 2. `go vet ./...` — exit 0
- [ ] 3. `go build ./...` — exit 0
- [ ] 4. `go mod tidy -diff` EMPTY + `git diff --exit-code master -- go.mod go.sum` EMPTY (**+0 modules** — this row adds **ZERO new imports anywhere**)
- [ ] 5. `golangci-lint run ./...` — exit 0
- [ ] 6. **FULL differential:** `go test ./test/differential/ -count=1` — all **119** dirs, exit 0. The 118 pre-existing dirs byte-stable. ⚠️ a `subject ready: EOF` on an UNRELATED fixture is the known startup race — **isolate-re-run**.

**Plus:**
- [ ] **Layering check:** `go list -deps ./internal/tracing | grep -E 'envoy-go/internal/(filter|cluster)'` (**no `...`**) ⇒ **EMPTY**. ⚠️ **A DISCIPLINE check, NOT a cycle check** — `internal/cluster` carries no `internal/tracing` edge either, so a direct import would build clean. **Do not re-introduce the refuted "cycle guard" justification.**
- [ ] **`-race` on touched packages:** `go test ./internal/cluster/ ./internal/tracing/ ./internal/filter/hcm/ -race -count=1`. ⚠️ **`TestOutlierDetector_ConcurrentEjectExactlyOnce` (`internal/cluster/outlier_test.go:766`) is a PRE-EXISTING intermittent failure reproduced on the UNMODIFIED baseline — isolate-re-run; do NOT re-classify** unless it reproduces deterministically or the failure text differs. Same for the two **newly-observed, unindexed** load flakes (`internal/httpclient TestOptions_ZeroValue_NoOpDefaults`, `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery`) — isolate-green at the SPEC, NOT root-caused, NOT attributable to this row.
- [ ] **Counts MECHANICAL, never copied:** fixtures **119** (tail `0117-tracing-custom-tags-metadata-cluster`) · fuzzers **55** (`^func Fuzz`) · BackendKind **38** · DECISIONS tail **ADR-0295** · stat surface **1201 (+0)** — a span attribute registers no stat; there is **NO mechanical stat command**, and **NO `TestNoNewStat` guard is owed for a tracing row** (do not manufacture one) · go.mod diff EMPTY.
- [ ] **Envelope audit — pin the SHAPE for `internal/cluster`, BYTE-UNTOUCHED for the rest.** Classify EVERY file in `git diff --name-only master`; `git diff --name-only | grep -vE '^(internal/|test/|docs/)'` ⇒ EMPTY.
  - **`internal/cluster` SHAPE:** exactly ONE added field + ONE added accessor in `cluster.go` (**and NO import-block hunk**); exactly ONE added hunk in `manager.go`, **located between the `extractEndpoints` guard and `validatePanicThresholdRange`** — verify by CONTENT that `:754`/`:883`/`:884`/`:885` are byte-identical to master, and separately that `extractEndpoints`'s signature and `manager_test.go` are byte-identical. **`subset.go` BYTE-UNTOUCHED (sha256).**
  - **BYTE-UNTOUCHED (assert by sha256, after confirming each path EXISTS — anti-vacuous):** `internal/filter/hcm/{connection.go,h2dispatch.go,h3dispatch.go,actions.go,route.go}` · `internal/filter/http/chain.go` · `internal/cluster/subset.go` · `internal/cluster/manager_test.go` · `internal/xds` · `internal/tls` · `internal/boot` · `internal/listener` · `internal/bootstrap` · `validate/` · `internal/dynamicmetadata`.
  - **CALLER-SET-INVARIANT (NOT hashed):** `accesslog_emit_test.go` **13** + `span_emit_test.go` **21** = **34**, via `grep -cE 'emitAccessLog(H2|H3)?\('` (⚠️ **the gated form — F4**), plus ZERO `<` lines vs the master blobs.
  - **The 18-caller roster re-derived independently = 5 + 6 + 7.** ⚠️ Use `grep -E '^[^/]*[^a-zA-Z0-9_]f\.emitAccessLog(H2|H3)?\(' | grep -vE '^\s*//'` — a raw `grep -c` inflates `h2dispatch.go` to **7** (the doc comment at `:188`). **h3's seven are ALL REAL; only h2 is inflated — the trap is reading 5+7+7=19.**
  - **Exactly ONE new exported symbol** (`cluster.Endpoint.ClusterMetaLookup`) plus the `ResolveCustomTags` signature growth — establish by an **AST symbol-set diff** per changed package at master vs HEAD, not a diff grep. ZERO new packages/modules/stats/BackendKinds.
  - **The deferred doc-comment check:** `cluster.go`'s new accessor comment claims it is *"threaded as a method value at the three tracing emit sites"* — FALSE when T1 writes it, TRUE after T4. **Confirm it became true** (three `picked.ClusterMetaLookup` at `accesslog_emit.go:57`/`:118`/`:179`). *(Phase 72's T8 had to carry exactly this deferred check.)*
  - **No stray artifacts:** `git status --short` and `git clean -nd` EMPTY; no `testdata/fuzz/` additions; zero break/debug residue in any `.go`; the fixture tree on disk == the tracked set == 5 files, NO `scripts/`; **the MAIN checkout clean.**

**ROW 73 FLIPS `in-progress` → `done` at this gate** (ADR-0106, the SOLE leg — a SINGLE FLAT ROW).

*(No separate commit — T8's evidence lands in `PROGRESS.md` at T9.)*

---

## Task 9 — ADR-0295 completed IN PLACE + stage-close (controller-adjacent)

- [ ] **ADR-0295: COMPLETE IN PLACE** — **APPEND** `### Decision (landed at the phase-73 IMPL)` + `### Consequences` **after the existing `*(§Decision + §Consequences land at the phase-73 IMPL.)*` hand-off line (`:17229`, currently the LAST line of the file — RETAIN it, the ADR-0294 precedent keeps its footer at `:17187`)**. Flip the STATUS banner to **COMPLETE**. **Do NOT append a new ADR; do NOT renumber; do NOT create a SECOND `### Decision` heading** — verify `grep -c '^### Decision'` inside the block is **0 BEFORE** and **1 AFTER**. Tail stays ADR-0295; next-free ADR-0296. §Decision records the landed mechanism (the `clusterFilterMetadata` field + `ClusterMetaLookup` accessor + the **placement-pinned** `buildCluster` loop; `kindMetadataCluster` at iota==6 + the CLUSTER accept arm; the `clusterMetaLookup` 6th param + the FULL-`MetaPath` arm; `picked.ClusterMetaLookup` at the three emit call sites with signatures + 18 callers untouched; the two-cluster `0117` fixture); §Consequences records the counts, the departure roster, the two string collisions, the six-gate and the memory updates. *(ADR-0294's §Decision uses a **7-item numbered list** for a 9-task spine — T8/T9 fold into §Consequences. Do not assume 1:1.)*
  - [ ] ⚠️ **FIX THE ADR-0295 HEADING — the FOURTH stale claim (F1), which SPEC §16 asserts was already fixed and was not.** `DECISIONS.md:17211` still reads *"the route-carried seam would have manufactured **three divergence classes**"* (refuted — it is **ONE** expressible class, S8; S5/S5b are boot-rejected by envoy-go so seam B could not diverge where envoy-go cannot go) and *"the departure roster narrows to **THREE arms, not two**"* (refuted — the unit is **TWO ARMS / THREE SPELLINGS**). Both substrings occur **exactly once document-wide, both on `:17211`**. The BODY at `:17225` is already correct, so **the heading is the sole surviving carrier — and it is the line future readers see first.** Correct both in place at the same edit.
  - [ ] ⚠️ **CORRECT ADR-0294 `:17209` (F6)** — its §Consequences *Memory updates* bullet still closes *"only `CLUSTER` remains — the natural next candidate, and **the one that RE-PAYS the 18-caller threading tax this row skipped**."* This row DISPROVES it (sha256 across seven production files). Add a bracketed `[CORRECTED at the phase-73 IMPL: …]` note **using ADR-0294's OWN `:17185` precedent**. *(If you instead decline on the grounds that ADR-0294 is a historical record, say so IN WRITING in `PROGRESS.md` — do not leave it unaddressed.)*
- [ ] **ROADMAP row 73 → `done`** at the six-gate (ADR-0106, SOLE leg). **NARROW the deferred sentence NOW (and ONLY now):** apply the SPEC §12 `old_string`/`new_string` on **`:203`**. ⚠️ **RE-VERIFY `count == 1` at the IMPL tip before building the edit** (`reference_a_drift_correction_is_itself_a_claim`) — verified 1 at this PLAN tip, and the narrow was APPLIED IN SCRATCH here: **check (2) STAYS 3**, the sentence stays well-formed, retains the `candidates:` marker, still matches the check-(2) regex, and carries **FOUR** candidates (the `ssl` stat family + `spawn_upstream_span` + `http_service` + force-trace). ⚠️ **Dropping the bare `custom_tags` prefix is REQUIRED** — the three survivors are sibling *tracing* fields, not `custom_tags` sub-types.
  - [ ] ⚠️ **RECORD, in one sentence, that SPEC §13's two NEWLY-SURFACED deferred items are deliberately NOT added to the `candidates:` sentence** — the absent-`metadata_key` DEPARTURE (§13 item 1, *"the first item any future 'close the metadata departures' row must include"*) and the two error-string COLLISIONS (§13 item 2). The §12 `new_string` does not name them, so a future roller reading only the deferred sentence will not see them; they DO land in BEHAVIOR_CONTRACT B2 (T7) and ADR-0295 §Consequences (T9), so nothing is lost — **but the omission must be deliberate and written down, not silent.**
  - [ ] ⚠️ **CORRECT THE THREE STALE CLAIMS IN THE ROW-73 CELL (`ROADMAP.md:135`), each occurring exactly once** — (a) the narrow-hazard sentence (*"REMOVES the clause … since `CLUSTER` is its last item"*) — REFUTED, the narrow SHORTENS it; (b) *"the ONLY remaining reject arms are the two unset-kind ones"* — REFUTED, a second departure arm survives; (c) the `0117` description as a single-cluster *"static `clusters[].metadata` hit arm + a `default_value` arm"* — SUPERSEDED by the TWO-CLUSTER decision. **Do not propagate any of them.** Row-73 field count must stay **8** (a shell pipe in the cell would break both the table and the sentinel's field-5 parse — the phase-72 near-miss).
- [ ] **STATE.md:** edit §Current pointer **IN PLACE** (lifecycle 3 → DONE; row 73 `done`); demote to §Recent lineage **capped at five** — and **update the lineage PREAMBLE (`:42`)** naming the correct newest + dropped bullet. Update counts (fixtures **119**, DECISIONS ADR-0295 COMPLETE). ⚠️ **The ADR-0288 singleton greps must read 2, not 1** — the extra hit is `:7`, the RULE STATEMENT. **Never "fix" it to 1.**
- [ ] **PROGRESS.md:** finalize — every break's ACTUAL firing assertion, the verbatim red-first records, T1's shape/aliasing/sizeof evidence, **Break D's result specifically** (the placement guard), T4's two-part envelope verification, T5's arm-tagged dispatch-verify output, T6's partition-mechanism choice + evidence, any break substitutions **with their rationale flagged as a claim**, and any break that did NOT fire.
- [ ] **Router roll** (`next-prompt.txt` — TRACKED despite `.gitignore`; edit in the stage worktree; locate commits by SUBJECT). ⚠️ **Check (1) DOES go silent when row 73 flips — row 73 is the LAST non-`done` row (100 `done` + 1 `in-progress` among the 101 rows check (1) can SEE; 103 rows total, the 2 it cannot see (00, 04) both `done` at the PLAN tip, verified by flipping it in a scratch copy). That is the NORMAL post-IMPL state and is NEVER on its own a termination condition; checks (2) and (3) still print, so the sentinel does NOT fire and `stop` must NOT be created. Re-run all three and record the ACTUAL output rather than predicting it.** The roller SELF-PICKS the next subject at the next BRAINSTORM per the 2026-07-12 standing directive unless the sentinel fires. ⚠️ **Do not carry forward any of the four refuted claims.**
- [ ] ⚠️ **Know check (1)'s blind spot before leaning on it** (PRE-EXISTING, no action owed): **2 of 103 ROADMAP table rows are silently skipped, and for TWO DIFFERENT reasons** — re-derived here by set-differencing all `| <num> |` rows against the rows check (1) actually matches, rather than by inspecting the regex: `| 00 | bootstrap | — | done |` is missed because the field `[0-9.,  ]*` does not match an **em-dash** "after" column, while `| 04 | http-1.1 | 03 | done |` is missed because the slug field `[a-z0-9-]+` does not match the **dot** in `http-1.1`. *(An earlier draft attributed both to the em-dash — a single-cause diagnosis that would have under-predicted which future rows go invisible; any dotted slug does too.)* Both rows are `done`, so there is no current impact — but check (1) is **not exhaustive**, which matters given how much of the stage-close mechanics leans on it.
- [ ] **Sentinel re-run MECHANICALLY, TWICE — once in the stage worktree and AGAIN on LANDED master AFTER the squash-push** (the phase-72/73 precedent: a stage worktree can disagree with what actually landed, and the post-push re-run is the one that counts). Expect (2) ⇒ **3** and (3) ⇒ `NEVER OPENED: gRPC/Runtime/WASM`; record (1)'s ACTUAL output. **Create `stop` if and ONLY if all three print nothing.**
- [ ] **Memory updates owed.** ⚠️ **BOTH SPEC §13 obligations are ALREADY DISCHARGED at the SPEC close** (`reference_endpoint_metalookup_host_metadata_seam` already retracts the "CLUSTER re-pays" claim and carries 29 → **34**; `reference_cluster_tag_gated_on_pick_not_route` exists and is indexed at `MEMORY.md:122`) — **verify, do not rewrite.** What IS owed:
  - [ ] **(F7) Narrow `reference_host_tag_resolves_at_pick_time.md`** — its closing line still says *"phase 72 left **both** UNPROBED"* for pool-overflow **and** circuit-breaker. **The circuit-breaker arm is now EXECUTED-PARITY on the reference (S8); pool-overflow alone remains.** Scope it exactly: reference EXECUTED, envoy-go RE-DERIVED.
  - [ ] Update `reference_endpoint_metalookup_host_metadata_seam` with the LANDED outcome (the `ClusterMetaLookup` line is already anticipated there — confirm it is now past tense and accurate).
  - [ ] Consider a memory for **the populate-placement hazard** (a retention field stamped onto a slice must be written before every VALUE-COPYING consumer; aliasing constructors mask it) — it is a reusable, non-obvious lesson that generalizes beyond this row.
- [ ] **Squash-push by the controller** at stage-close.

**Commit (stage-close docs):** `phase 73 (tracing-custom-tags-metadata-cluster) IMPL: …` (controller composes at close).

---

## Self-review against SPEC-73

| SPEC obligation | Where |
|---|---|
| the `Endpoint.clusterFilterMetadata` field + nil-safe `ClusterMetaLookup` accessor (§3.4) | T1 |
| the `buildCluster` populate LOOP, **placement-pinned before all LB construction**, with an explicit subset-LB assertion (§3.4, §16 M3) | T1 (test 3 + Break D) |
| `extractEndpoints`'s signature + `manager_test.go` + `:754`/`:883`/`:884`/`:885` byte-unchanged; ZERO new imports (§3.4, §4) | T1, T8 |
| the pool-HIT provenance test (§3.4) | T1 (test 5) |
| `kindMetadataCluster` APPENDED at **iota==6**, EXECUTION-proven by the red printing `Kind:6` (§3.2) | T2 |
| the CLUSTER accept arm (cloning HOST, `MetaPath` FULL) replacing the `:268-269` reject; both unset arms UNCHANGED (§3.2/§3.6/§6) | T2 |
| the pre-known red is EXACTLY TWO `/cluster` subtests; table rows `:639`/`:764` (byte-identical ⇒ non-unique), failures print `:648`/`:773` (§10 T2) | T2 |
| the `clusterMetaLookup` 6th param + the resolve arm; the `request_header` default rule; the FULL-path descent (§3.4) | T3 |
| `descend`/`structpbValueToString` REUSED VERBATIM; `internal/tracing` stays filter- AND cluster-free (§3.4, §4) | T3 (Step 4), T8 (layering) |
| **49** `resolve_test.go` callers, gated by LINE (raw grep 50; `:107` a literal) (§10 T3) | T3 |
| T3's honest entry condition: after T3 `internal/filter/hcm` does NOT compile — the **`ResolveCustomTags`** arity, NOT the emit arity (§10 T3) | T4 (Entry state) |
| `picked.ClusterMetaLookup` at the three call sites and NOWHERE else (§3.4, §11) | T4 |
| the **TWO-PART** envelope gate — sha256 on SEVEN production files; CALLER-SET invariance (13+21=34) on the two emit test files, NOT a hash (§1, §10 T4, §11) | T4 (Step 4), T8 |
| a first-class zero-`picked` test — the **5** span-capable zero sites, not the raw 10/11 (§11) | T4 (test 2) |
| the `meta_cluster_ok` seed ADD + the MANDATORY `meta_bad` REPOINT to `Kind: &metadatav3.MetadataKind{}`; dispatch-verify BOTH arms (§7) | T5 |
| ONE OTLP fixture `0117`, **TWO CLUSTERS / two routes / one backend**, STRING values only, non-`envoy.lb` namespace, NO writer, the four SPEC-named breaks incl. the selection break, **plus a fifth (R) added by this PLAN** (§8) | T6 |
| BC B1–B5 pinned wording; **B1+B2 as ONE `old_string` on `:688`**; B3's scope kept EXACT (§9) | T7 |
| a SINGLE FLAT ROW of NINE tasks; ADR-0045 valve armable-but-unconsumed (§10) | §1, this table |
| six-gate + layering + full-119-dir + `-race` + counts + SHAPE-pinned envelope audit (§10 T8, §15) | T8 |
| +0 packages / +0 modules / +0 stats / +0 fuzzers / +0 BackendKinds (§4, §7) | T5, T8 |
| ADR-0295 completed IN PLACE, no new ADR, no second §Decision heading (§14) | T9 |
| the ROADMAP narrow AT THE IMPL, `count==1` re-verified, check (2) STAYS 3 (§12) | T9 |
| the THREE stale ROADMAP row-cell claims corrected (§12) | T9 |
| the memory obligations (§13) — **verified ALREADY DISCHARGED** | T9 |
| **+ PLAN-surfaced (F1):** the ADR-0295 HEADING's two refuted claims — a FOURTH stale carrier §16 says was fixed and was not | T9 |
| **+ PLAN-surfaced (F2):** `accesslog_emit.go:57`/`:179` byte-identical ⇒ non-unique `old_string` | T4 |
| **+ PLAN-surfaced (F3):** `config.go:190`'s "ADR-0080-distinct" claim, an EIGHTH stale comment | T2 |
| **+ PLAN-surfaced (F4):** the emit-test caller gate needs the **gated** regex (naive ⇒ 23) | T4, T8 |
| **+ PLAN-surfaced (F5):** two SPEC warnings are stale-by-one-stage; NO edit owed — recorded so no stage "fixes" them | §1.3 |
| **+ PLAN-surfaced (F6):** ADR-0294 `:17209`'s refuted closing claim | T9 |
| **+ PLAN-surfaced (F7):** `reference_host_tag_resolves_at_pick_time` is stale on "both unprobed" | T9 |
| **+ PLAN-surfaced:** the crossed-adjacent-lookup hazard (FOUR identically-typed params) — Break K2 + M2 + two distinctness tests | T3, T4 |
| **+ PLAN-surfaced:** the `0117` per-path partition problem (`upstream_cluster` unusable) — primary + named fallback | T6 (Step 1) |

**Task count: 9** — matching SPEC §10's enumerated NINE (comfortably under the ADR-0045 ~15 ceiling). **ADR-0045 escape valve ARMABLE, UNCONSUMED — no split**: the seam decision removed the one thing that could have forced one, and the `internal/cluster` change is a near-verbatim phase-72 clone with the placement constraint as its only genuinely new risk (enforced by Break D rather than documented). Sequencing: **T1** (cluster substrate, independent) → **T2** (parse) → **T3** (resolve, consumes T2's const) → **T4** (wires T1's accessor + T3's 6th param) → **T5** (fuzz) → **T6** (fixture) → **T7** (BC) → **T8/T9** (close).

**⚠️ The IMPL's standing instruction: a PLAN is not evidence either.** **RE-DERIVE this document; do not execute it.** Where it cites, go look; where it claims control flow, walk the call graph; default to REFUTED. Start where this PLAN is most confident (all re-derived read-only at the PLAN tip, §1): the **zero-drift** anchor set, the §1.2 clone skeletons (cloning the LANDED phase-72 arms verbatim in their **gofmt-clean, misspell-clean** form), the `picked`-already-in-scope cost claim, the 18-caller roster with its span-capability partition, and the nine findings — **each of which is itself a claim and must be re-run before it becomes an `old_string`** (`reference_a_drift_correction_is_itself_a_claim`). ⚠️ **Note what re-derivation did to the SPEC's own self-assessment: §16 asserts an over-claim was "corrected in both places" and it was corrected in only one (F1), and TWO of its warnings point at text its own commit already fixed (F5). A document's account of its own corrections is not evidence that they landed.**
