# Phase 73 Brainstorm — tracing `custom_tags` (METADATA tag type, `CLUSTER` MetadataKind) (the NINETEENTH Observability-family row *(chain ordinal — §2.1 records the inherited off-by-one)*; the SEVENTH tracing `custom_tags` capability and the FOURTH and **LAST** of the four `metadata` MetadataKinds — it CLOSES the quartet; lifts the `tracing: custom_tags metadata tag %q cluster kind unsupported` reject — the message VERBATIM as it appears at `config.go:269`, NOT a compressed paraphrase — and resolves the SELECTED CLUSTER's static `metadata.filter_metadata` onto the ingress span; the unset kind stays PARSE-REJECTED loudly (envoy-go-strict, ADR-0080); +0 stats / +0 packages / +0 modules / +0 fuzzers; anticipated ONE new fixture)

**Status: BRAINSTORM. Docs-only. ZERO production `.go`. Row 73 registered `in-progress` at this commit per the ROADMAP §Schema invariant.**

---

## 1. Mission and scope confirmation (73 — the FOURTH and LAST `metadata` MetadataKind; the row that CLOSES the quartet)

### 1.1 What phase 73 delivers as a self-contained whole (the selected cluster's static metadata on the ingress span)

Phase 70 landed the tracing `custom_tags` `metadata` type for the **`REQUEST`** MetadataKind (the per-request dynamic-metadata `Bucket`); phase 71 landed **`ROUTE`** (the matched route's static config metadata, over `FilterChain.RouteMetaLookup`); phase 72 landed **`HOST`** (the selected upstream endpoint's `lb_endpoints[].metadata`, over `cluster.Endpoint.MetaLookup`). Phase 73 lifts the **`CLUSTER`** reject — RE-DERIVED at tip as `internal/tracing/config.go:268-269`:

```go
case k.GetCluster() != nil:
	return nil, fmt.Errorf("tracing: custom_tags metadata tag %q cluster kind unsupported", tag)
```

— and resolves the **SELECTED CLUSTER's** static metadata (`clusters[].metadata.filter_metadata[ns]`, walked by `MetadataKey.path`) onto the ingress SERVER span as a `{tag, value}` attribute, on BOTH the OTLP and Zipkin exporters via the shared `ResolveCustomTags` → `Span.Attrs []KV` seam (phase 46). After it lands, **all four MetadataKinds are consumed and the only remaining reject arms are the two unset-kind arms** (`config.go:232-233` and `:287-288`) — the PARITY/DEPARTURE split phase 71's S3 established.

This is a complete, useful, differential-provable capability: a span tag carrying a label attached to the cluster the route actually selected (tier/team/environment labels are the canonical reference use). **It is the LAST of the four `metadata` MetadataKinds — this row CLOSES the quartet.**

### 1.2 What phase 73 does NOT deliver (forward to §8)

- `spawn_upstream_span` / OTel `http_service` / OTel `sampler` / OTel `resource_detectors` / tracing `verbose` / force-trace. §8.
- The downstream TLS handshake-outcome `ssl` stat family (ADR-0286 C3 framework surgery + a NEW stat surface). §8.
- `access_log[].filter` — re-costed this session and **still deferred**, but the evidence for it is now much stronger (§2.1 records an EXECUTION proof of the divergence). §8.
- The pick-time `picked`-propagation row (BEHAVIOR_CONTRACT B2). §8 — and see §2.4, because the seam decision this row makes INTERACTS with it.
- `hcm.access_log_options` and the `stdout`/`stderr` access loggers — two candidates NEWLY SURFACED by this session's sweep and recorded as deferred (§2.1, §8).

### 1.3 Phase-done as the NINETEENTH Observability-family row landing (family STAYS OPEN)

Row 73 is the nineteenth Observability-family row *(the CHAIN ordinal — row 69 carries no ordinal word, so the chain runs one short of the mechanical count of 20; kept as-is deliberately, §2.1)* and the seventh tracing `custom_tags` capability (literal @ 59, request_header @ 62, environment @ 63, metadata/`REQUEST` @ 70, metadata/`ROUTE` @ 71, metadata/`HOST` @ 72, metadata/`CLUSTER` @ 73). After it lands the Observability family STAYS OPEN — `spawn_upstream_span`, `http_service`, `sampler`, `resource_detectors`, `verbose`, force-trace, and the `ssl` stat family remain deferred candidates, joined by `access_log[].filter`, `stats_flush_on_admin`, `hcm.access_log_options`, the `stdout`/`stderr` loggers and the B2 pick-time row.

**⚠️ This row RETIRES the `metadata`-MetadataKind line of cheap follow-ons.** §2.1 records the consequence plainly: after phase 73 there is no remaining cheap tracing slice, and phase 74's self-pick will have to leave the `custom_tags` lineage. That is stated here so the next roller does not spend a session rediscovering it.

### 1.4 ADR-0045 split readiness — anticipated a SINGLE FLAT ROW (escape-valve armable) *(SPEC confirms)*

Anticipated a SINGLE FLAT ROW, the same shape as phases 70, 71 and 72 — but **the task count is SEAM-CONDITIONAL and the two seams are NOT diff-comparable** (§2.4, corrected after adversarial review): **~8–10 tasks under seam A**, and **materially more under seam B**, which re-pays the 18-caller threading tax (three emit signatures + 18 call sites + the emit-test callers). Under either seam it stays flat. The ADR-0045 split valve is armable-but-unconsumed: the one thing that could force a split is D-CTMC-SEAM turning up behavioral disturbance in `internal/cluster` (seam A) or in the H2 carrier / weighted-route path (seam B).

### 1.5 Seed-stub alignment + package placement — ALL edits in EXISTING files, ZERO new packages

Every edit lands in EXISTING files: `internal/tracing/config.go` (lift the CLUSTER reject → an accept arm), `internal/tracing/resolve.go` (a `kindMetadataCluster` arm + a new lookup param), `internal/filter/hcm/accesslog_emit.go` (the three `ResolveCustomTags` call sites — touched under **BOTH** seams), plus — depending on §2.4 — `internal/cluster/{cluster.go,manager.go}` (seam A) **or** `internal/filter/hcm/{connection.go,h2dispatch.go,h3dispatch.go}` (seam B, which must read `entry` at the 5 H1 + 7 H3 sites AND thread a new emit parameter through all 18 callers), plus `test/fixtures/` + `docs/`. NO new package. *(Corrected after adversarial review: an earlier draft listed `accesslog_emit.go` as seam-B-only and omitted `connection.go`/`h3dispatch.go` from seam B entirely — V2 M5 + V1 S1.)*

**Blast-radius note carried forward from phase 72:** `internal/cluster` stopped being a never-touched surface at phase 72, so the "assert BYTE-UNTOUCHED" envelope-audit shape is no longer available for it under seam A; the audit must pin the change's SHAPE, exactly as phase 72's did. Under seam B, `internal/cluster` IS byte-untouchable and `internal/filter/hcm/h2dispatch.go` is not.

### 1.6 No prebrainstorm-notes branch

No off-master prebrainstorm-notes branch applies (`reference_phase_11_local_ratelimit_prebrainstorm` is `local_ratelimit`-only).

### 1.7 Phase 73's relationship to the existing seams — a reject-lift whose ONLY open question is WHERE the cluster's identity is read

Phases 70 and 71 each had to *transport* a new source to the emit seam and each paid an 18-caller threading tax. Phase 72 skipped that tax entirely because its source (`picked cluster.Endpoint`) was already the 4th parameter of all three `emitAccessLog*` helpers. RE-DERIVED at tip, the three `ResolveCustomTags` call sites now read:

```
internal/filter/hcm/accesslog_emit.go:57   tracing.ResolveCustomTags(f.tracingConfig.CustomTags, reqHeaderLookupH1(r),   metaLookup, routeMetaLookup, picked.MetaLookup)
internal/filter/hcm/accesslog_emit.go:118  tracing.ResolveCustomTags(f.tracingConfig.CustomTags, reqHeaderLookupH2(req), metaLookup, routeMetaLookup, picked.MetaLookup)
internal/filter/hcm/accesslog_emit.go:179  tracing.ResolveCustomTags(f.tracingConfig.CustomTags, reqHeaderLookupH1(r),   metaLookup, routeMetaLookup, picked.MetaLookup)
```

and the signature is five-parameter (`internal/tracing/resolve.go:46`, re-derived verbatim):

```go
func ResolveCustomTags(specs []CustomTagSpec, headerLookup func(string) ([]string, bool), metaLookup func(ns, key string) (*structpb.Value, bool), routeMetaLookup func(ns string) (*structpb.Value, bool), hostMetaLookup func(ns string) (*structpb.Value, bool)) []KV
```

**The row's whole cost question is: what does the SIXTH parameter's closure read from?** Two seams were found INDEPENDENTLY by the two cost agents this session. ⚠️ **Only ONE of them avoids the 18-caller thread** — an earlier draft of this document claimed both did, and BOTH adversarial verifiers independently refuted it (§2.4, §11.4). Seam A's source (`picked`) is already an emit parameter; seam B's source (`entry`) is a *dispatch*-function local the emit helpers never receive, so seam B re-pays the full phase-70/71 tax. They are NOT cost-equivalent and NOT behaviorally equivalent. §2.4 states both; D-CTMC-SEAM decides.

---

## 2. Design decisions

### 2.1 Row + subject confirmation: the Observability family continues with tracing `custom_tags` (`metadata`/`CLUSTER`) *(SELF-PICKED per the standing directive → phase 73 row registered)*

The FIRST decision, made AUTONOMOUSLY (no human pick) per the 2026-07-12 standing directive. Picked as the **smallest defensible ROW-SIZED candidate** after INVESTIGATING each candidate's size against source THIS session (two independent read-only cost-assessment agents at tip `5aaadf13`; §11). Row 73 registers `in-progress` AT this BRAINSTORM commit per the ROADMAP §Schema invariant.

**Why `custom_tags` (`metadata`/`CLUSTER`) is smallest-defensible:** it sits on landed substrate on FOUR axes — (a) the phase-70 `descend` (`resolve.go:172`) + `structpbValueToString` (`:195`) machinery reused **VERBATIM** *(cites corrected after review — an earlier draft said `:145`/`:168`, which match NO commit in the lineage; V1 walked all four)*; (b) the config parse arm — the `k.GetHost() != nil` accept at `config.go:270-286` is a drop-in clone (namespace/path/segment PGV-parity rejects + a `CustomTagSpec{...}` build) with `kindMetadataHost` swapped for a new `kindMetadataCluster`; (c) the resolve arm is a byte-level clone of the landed HOST arm, FULL-`MetaPath` (`reference_route_metadata_resolve_full_metapath`); (d) the fixture chassis is the just-landed `0116-tracing-custom-tags-metadata-host` with the metadata block relocated from `lb_endpoints[]` to the cluster. It carries NO ADR headwind and is **+0 stat** (a span attribute, not a stat). **And it CLOSES the four-MetadataKind quartet** — a completing row, not another open-ended slice.

**⚠️ THE HEADLINE STALE-COST CORRECTION (`reference_deferred_candidate_cost_restale`) — `CLUSTER`'s inherited "MODERATE ~11–13 tasks, RE-PAYS the 18-caller thread" adjective is REFUTED — but on ONE seam, not two.** *(SCOPED DOWN after adversarial review: an earlier draft claimed a "convergent refutation from disjoint evidence" and BOTH verifiers independently refuted that framing — §11.4. What actually converged is narrower and is stated below.)* The inherited framing (carried in phase 72's BRAINSTORM §2.1, §8 and §11, the ROADMAP Observability paragraph, STATE.md's next-skill line and next-prompt.txt) says the selected CLUSTER is unreachable at the emit seam because `router.go:255` publishes only `Picked() cluster.Endpoint`, so a `CLUSTER` row needs a `routeAction` interface method plus a `SetClusterMetadata`/`ClusterMetaLookup` pair — re-paying the exact tax `HOST` skipped. **The `router.go:255` fact is TRUE and was re-derived. The COST CONSEQUENCE drawn from it is FALSE.** Both agents refuted it, independently and by different routes:

- **Agent A found seam A (endpoint-carried).** `extractEndpoints` has **exactly ONE non-test call site** — `internal/cluster/manager.go:433` — and the `*clusterv3.Cluster` is in scope there (`la := c.GetLoadAssignment()` at `:429`). So the owning cluster's `filter_metadata` can ride the SAME retention pattern phase 72 just built for the endpoint: a second aliasing map field on `Endpoint`, an `Endpoint.ClusterMetaLookup(ns)` accessor beside `MetaLookup` (`cluster.go:111-117`), and `picked.ClusterMetaLookup` at the three emit sites. **Zero 18-caller thread, zero `routeAction` change, zero `route.go`/`actions.go` edits.**
- **Agent B found seam B (route-carried) — a real SOURCE, but NOT a thread-free one.** `clusterRouteAction` holds a live `*cluster.Cluster` (`internal/filter/hcm/actions.go:183`; `:182` is the struct header) and the matched `entry` is in scope at every H1 and H3 emit **call site**: `dispatchRequest` spans `connection.go:312-780` with `entry` bound at `:319` and all five sites (`:330`, `:464`, `:597`, `:699`, `:777`) inside it; `h3dispatch.go` binds `entry` at `:125` with all seven sites (`:130`, `:210`, `:280`, `:341`, `:367`, `:373`, `:395`) inside; H2 already carries the carrier template (`h2dispatch.go:158` `routeMetadata: entry.metadata`, fielded `:250`, consumed `:375`). ⚠️ **But `entry` being in scope at the CALL SITES is NOT the same as it being available where the closure is needed** — the three `ResolveCustomTags` calls live INSIDE the emit helpers, which receive no `entry`. Seam B therefore needs a new emit parameter threaded through all 18 sites: **the full phase-70/71 tax.** (V1 proved it by execution: `accesslog_emit.go:57:7: undefined: entry`.)

**⚠️ WHAT ACTUALLY CONVERGED, STATED EXACTLY (both verifiers, SEVERE).** The two agents converged on the *source* half of the refutation: **the selected cluster IS reachable at the emit seam without a `routeAction` interface method** — the half of the inherited framing that is genuinely false. They did NOT both refute the 18-caller half. **Seam A avoids the thread; seam B RE-PAYS it** (§2.4). The cost correction is therefore SINGLE-SOURCED, and the row is SMALL **on seam A**. Recorded this way because the earlier draft's "convergent refutation from disjoint evidence is the strongest form this project gets" was exactly the self-certifying rhetoric this project's review discipline exists to catch.

**This is still the phase-72 lesson recurring, and it is worth naming as a pattern rather than an incident.** In both cases the enabling fact was already on the record and nobody derived its cost consequence: at phase 72 it was "the picked `Endpoint` IS in scope at the seam" (phase 71's BRAINSTORM said so in as many words); here it is phase 72's own `Endpoint` metadata-retention field. **A deferral's cost adjective must be re-derived, not re-read — and this is now the SECOND consecutive row where doing so shrank the pick from MODERATE to SMALL.**

**⚠️ A SECOND CORRECTION, this one to a LANDED CODE COMMENT (`reference_code_comment_not_evidence`).** `internal/filter/hcm/accesslog_emit.go:51` / `:112` / `:173` each read `UpstreamCluster:   "", // not available at this seam`. Re-derived: the comment is TRUE of the function's parameter list and **FALSE of reachability one layer up** — every caller has `entry` (H1/H3) or can carry it (H2). A comment with the authority of proximity has been discouraging a cheap row. **Two precision notes:** (i) agent B mis-located this hard-code onto the access-log record — it is on `tracing.SpanInputs`, and `accesslog.Record` has no `UpstreamCluster` field at all; (ii) agent B's line cites (`:52`/`:112`/`:172`) drifted — the controller re-grepped and the actual lines are **`:51`/`:112`/`:173`**. Recorded per `reference_a_drift_correction_is_itself_a_claim`: a correction is itself a claim. **The `upstream_cluster` span TAG remains a separate, unchartered feature — this row does not fix it**, and phase 72's category-error warning stands.

**Rejected alternatives (recorded per the standing directive; each RE-DERIVED/SIZED against source this session — §11):**

- **`access_log[].filter` (the nearest rival, and the one the incoming router flagged as "a strong FUTURE pick")** — the two agents DISAGREED on size (A: SMALL ~8–9; B: MODERATE ~9–11), and **per the phase-72 precedent the disagreement resolves AGAINST the candidate.** What is NOT in dispute, and is now much better evidenced than when it was named: the divergence is REAL and agent A established the **envoy-go half BY EXECUTION** rather than by grep, and V1 independently REPRODUCED it — booting envoy-go with a config carrying `access_log[].filter.status_code_filter` runs clean, while the identical config with the arm renamed to `bogus_filter_xyz` fails with `unknown field "bogus_filter_xyz"` from the `DiscardUnknown:false` pass. So `filter` is a KNOWN field, ACCEPTED, and SILENTLY IGNORED. ⚠️ **Scope that correctly (V2 m3): what was EXECUTED is the envoy-go side ONLY** — that envoy-go ignores the field is proven; that the REFERENCE honors it is inferred from the proto contract and was NOT probed, so the *divergence* is very well evidenced but strictly INFERRED end-to-end. A live behavioral divergence, not an unimplemented knob (`grep -rn "GetFilter()" --include=*.go .` ⇒ **0** non-test hits; the project's own `bootstrap.go:286` doc comment already admits *"AccessLog.filter STAY inert (deferred)"*). **What agent B found that agent A priced as tractable and B did not:** access-log sinks are a single GLOBAL FLAT `[]accesslog.Sink` — built in `cmd/envoy-go/main.go` from three separate loops and handed UNCHANGED to every HCM (`internal/filter/hcm/config.go:363` `accessLog: accessLogSinks`) — so the `access_log[]` entry→sink identity is **lost before the emit path**, and a per-entry predicate must first reconstruct it across all three bootstrap config structs. Controller-verified. **Both agents independently flagged the same implementation hazard**, which is a strong signal the row is real but not free: a decorator/wrapper sink embedding the `Sink` INTERFACE promotes only `Submit`/`Close`, so `alsHeaderCaptureUnion`'s `s.(headerCaptureSink)` type-assert (the interface at `config.go:392`, the function at `:400`, the assert itself at `:403`; `:376` is only the call site) would silently LOSE header capture on a wrapped ALS/OTLP sink — the `reference_conn_wrap_method_no_promote` class. Also re-derived: **13 reject arms, not 12** (PGV marks `filter_specifier` required, so `filter: {}` must reject too). **Deferred, and re-flagged as the strongest FUTURE pick** — now with the execution proof and both hazards on the record so the next roller starts from evidence rather than an adjective.
- **The pick-time `picked`-propagation row (BEHAVIOR_CONTRACT B2)** — SMALL (~6–8) and the smallest *production* diff of everything assessed, but **probe-dominated and NOT independent of this row** (see §2.4). Both agents re-derived it and AGREED on the shape: `ep` is in scope at every acquire-failure return, H3 needs no work at all (`h3dispatch.go:216` `rf.SetAction(action)` — H3 reuses the H1 action verbatim), the retry/hedge/weighted wrappers propagate `ep` transparently and need no edits, and the three access-log consumers are exactly `internal/accesslog/{format.go:56,otlpformat.go:47,mapping.go:78}`. **Both also independently found the fact the incoming framing missed:** `errConnPoolOverflow` is raised AFTER the LB pick (`internal/cluster/cluster.go:720-722` returns a NON-ZERO `ep`), so the conn-pool-overflow returns carry a real host too — which makes the row's scope a genuine fork (connect-failure only ⇒ 2 landed tests flip: `router_test.go:195`, `:334`; + overflow ⇒ a third, `:598`; while `:493`, the `max_requests` admission arm where no pick ever happens, must NOT flip). **Why deferred:** the reference's attribution rule is failure-class-specific and UNPROBED for pool-overflow and circuit-breaker (phase 72 left both unprobed and said so), so the row is dominated by SPEC-stage live probing rather than code — and a row whose size is "however many probe arms it takes" is not the smallest defensible pick. Deferred, with the probe cross-product named in §8.
- **`hcm.access_log_options` (NEWLY SURFACED this session by agent B)** — field 56 of `HttpConnectionManager`, three sub-fields (`access_log_flush_interval`, `flush_access_log_on_new_request`, `flush_log_on_tunnel_successfully_established`). Controller-verified: `grep -rn 'GetAccessLogOptions\|GetFlushAccessLogOnNewRequest' internal/ --include='*.go' | grep -v _test` ⇒ **0**. Same silent-divergence class as `access_log[].filter` and plausibly cheaper (`flush_access_log_on_new_request: true` means one extra `Record` submit at the top of each of the three dispatch functions, provable through the already-built `0006` line-by-line cross-side driver). **Deferred on evidence depth, not on size** — only ONE of the two agents assessed it, it was not costed in depth, and picking a candidate on a single un-cross-checked derivation is exactly what the two-agent discipline exists to prevent. **Recorded as a strong FUTURE pick alongside `access_log[].filter`.**
- **`stdout`/`stderr` access loggers silently ignored (NEWLY SURFACED, found INDEPENDENTLY by BOTH agents)** — `internal/bootstrap/bootstrap.go:952-956` falls through to `return nil` for any `typed_config` type-URL that is not file/http-grpc/tcp-grpc/otlp (the guard at `:952`, its comment `:953-954`, the `return nil` at `:955`; `:957-958` are the `FileAccessLog` unmarshal, a different thing). `StdoutAccessLog` is a common config; the reference writes to stdout and envoy-go writes nothing, with a clean boot. Both agents rated it SMALL (~6–7) on production cost and both independently identified the SAME blocker: **provability.** The reference is a container, so its stdout returns via `docker logs` rather than a bind-mounted file, which means the landed `fixture.ReferenceLogMounter`/`AccessLogAsserter` chassis does NOT apply and a new asserter shape is owed. Agent B additionally noted an inconsistency worth fixing whenever this is taken: `bootstrap.go:947` promoted `tcp_grpc` to a loud reject, but the comment six lines below (`:953`) still lists it beside `stdout` as "silently ignored". Deferred.
- **`stats_flush_on_admin`** — MODERATE (~8–9). The reject at `internal/bootstrap/bootstrap.go:606-607` re-derived EXACT. The production seam is genuinely small (`internal/statssink/flusher.go:46` `flushOnce()` is ALREADY a standalone one-shot; the `Flusher` is in scope at `main.go:287` before `admin.New` at `:333`), but the row crosses three subsystems, and **`admin.New` has 53 call sites** (1 production + 52 in-package tests) so a constructor param would re-pay a 53-caller tax for one boolean — a setter is the only sane shape. Two behaviors are unprobed (WHICH admin endpoints trigger the flush; whether the reference final-flushes on drain). Decisively: **its cross-side assertion is a timing-shaped NEGATIVE** ("nothing flushed during a quiet window longer than the interval, then a batch after `GET /stats`"), the flakiest shape in this harness, and a negative has no gauge to poll (`reference_health_check_propagation_warmup`'s poll-the-gauge mitigation does not apply). One concrete IMPL obligation recorded: `internal/bootstrap/statssink_fuzz_test.go:130-131` seeds `stats_flush_on_admin` as a REJECT — that seed's classification INVERTS and must be re-pointed. Deferred.
- **QUIC `QuicProtocolOptions` tuning** — **RE-COSTED UPWARD to LARGE (~11–12), a correction to the incoming "SMALL but needs ~2 tasks of harness" adjective.** Both empty-literal cites re-derived EXACT (`internal/listener/quic.go:37` `quic.Listen(udpConn, tlsCfg, &quic.Config{})`, `:145` `QUICConfig: &quic.Config{}`), and `internal/listener/manager.go:360-361`'s own doc comment admits the nested knobs are *"accepted-and-ignored"*. But the row must BUILD its own observability before it can assert anything: `test/helpers/h3.go` constructs a fresh `http3.Transport` per call with no connection caching (confirmed verbatim), so `idle_timeout`/`connection_keepalive`/port-migration are **unobservable, period**; `max_concurrent_streams` needs a release-barrier backend; and the window-size knobs are throughput/timing observables that are realistically UNassertable cross-side. Net: ~1 cross-side-assertable knob for ~2 tasks of new harness plus the mapping work, on an H3 fixture chassis that is **exactly one fixture deep** (`0104-http3-downstream-get`). This is the only candidate where harness work exceeds production work. Rejected.
- **Opening the `Runtime` family** — MODERATE (~9) for a defensible row, and agent B's derivation reframes it usefully. `internal/runtime/` is `doc.go` only (re-confirmed) and `layered_runtime` is boot-rejected at `internal/bootstrap/bootstrap.go:568-569` (pinned by two tests). The key question is not "can a runtime layer be parsed" but **"what would light up?" — and the answer is NOTHING for free.** Agent B enumerated ~10 runtime-adjacent sites and **every one is itself a parse-reject or a documented silent-ignore** (5 rejects in `admission_control/compiled_config.go:179-192`, 1 in `adaptive_concurrency/compiled_config.go:171`, `jwtauthn/jwtauthn.go:631`, `hcm/config.go:637`, plus documented ignores in csrf/bandwidthlimit/compressor/fault/ext_authz). They are *rejecting* consumers, not *waiting* ones; each lift is its own row. The smallest defensible Runtime row is therefore a self-contained one — `layered_runtime` `static_layer` parse + an in-memory snapshot + an admin `GET /runtime` dump (envoy-go's admin surface has no `/runtime` today; `0009-admin-config-dump` is the chassis precedent). Real, but larger than a follow-on inside an open family, and it needs a live probe of the reference's `/runtime` JSON shape. Deferred.
- **xDS candidates** — the "no persistent stream" finding is RE-CONFIRMED MECHANICALLY and it is decisive for most of the family: `grep -n "for {" internal/xds/stream.go` returns **no output** (exit 1), and the file's four functions are `fetchSecret:50` / `applyResponse:98` / `fetchValidationSecret:123` / `applyValidationResponse:169`, each one `Send` → one `Recv()` → ACK/NACK → return. `initial_fetch_timeout` is already landed (`internal/xds/config.go:11-13`, honored at `:75`). **CDS/EDS/LDS/RDS/ADS/Delta are all LARGE** — additionally blocked behind the `dynamic_resources` boot-reject at `bootstrap.go:565-566` — and `google_grpc` is a whole second transport rejected consistently in five places. The smallest xDS rows are inside the SDS envelope: **multiple `tls_certificate_sds_secret_configs`** (SMALL ~7 — one reject arm at `internal/xds/config.go:24` plus a multi-slot provider) and the **"compose-two" cert-SDS + validation-SDS row** whose deferral is already SPEC'd in-tree at `internal/boot/boot.go:148-151` (SMALL–MODERATE ~8). Both are defensible future picks; both lose to `CLUSTER` on substrate freshness. **Upstream SDS** re-derived MODERATE (~9–10) with its boot-ORDER blocker intact and the `reference_xds_config_seam_transitive_cycle_guard` constraint live. Deferred wholesale.
- **`spawn_upstream_span`** — LARGE. `BuildServerSpan` is still the only span builder; a CLIENT span needs a new model, an upstream-leg timing seam that does not exist, parent/child linkage and a two-span export path. Deferred.
- **OTel `http_service` / `sampler` / `resource_detectors`** — MODERATE each, all three still live rejects inside `parseOTel`. `http_service` is a whole protobuf-over-HTTP transport; `sampler` and `resource_detectors` are `TypedExtensionConfig` extension points needing a registry plus ≥1 concrete impl. Deferred.
- **tracing `verbose`** — MODERATE. Needs per-stream span logs/events, a field `Span` does not have; adding one ripples both exporters plus the receiver-side assertions. Deferred.
- **force-trace / internal-request detection** — LARGEST. Still ZERO `x-envoy-internal` / `x-envoy-force-trace` handling; needs internal-vs-external classification plus edge header sanitization. Deferred.
- **the downstream TLS handshake-outcome `ssl` stat family** — LARGE, framework surgery (ADR-0286 C3's adjective HOLDS, as it did at phase 72). Deferred.
- **A SIXTH stats sink** — still **UNAVAILABLE** rather than merely large: the only unconsumed `stats_sinks[]` extension is `wasm`. Deferred.
- **Response-flags plumbing (NEWLY NAMED by agent B)** — MODERATE (~10). `accesslog.Record` has no `ResponseFlags` field, `SpanInputs.ResponseFlags` is hard-coded `"-"` at `accesslog_emit.go:53`/`:114`/`:175`, and `ActionResponse.localOrigin` (`router.go:95`) is unexported with no accessor. Good SUBSTRATE — it unblocks `response_flag_filter`, the tracing `ResponseFlags` attribute and Lua/wasm stream-info parity — but not small, and not a capability on its own. Deferred.
- **Opening `gRPC` or `WASM`** — unchanged from phase 72's adjudication and re-confirmed: `gRPC` is gated on an HCM response-trailer framework row (`RunDecodeTrailers`/`RunEncodeTrailers` still have ZERO production callers), and **`WASM` remains a ROADMAP BOOKKEEPING ARTIFACT** — `internal/wasm/` is a substantially-built wazero host and rows 25/25.1/25.2/25.3 are all `done`, so `NEVER OPENED: WASM` describes built work. **Deliberately NOT acted on at this docs-only row**, for the same reason as phase 72: the stage-close mechanics of §12 depend on that sentinel output. Flagged again for a future single deliberate correction.
- **⚠️ A LIVE DIVERGENCE FOUND EN ROUTE, recorded but NOT chartered (agent B).** The global access-log sink flattening described above is not only a cost factor for `access_log[].filter` — it is a bug in its own right: with two listeners configured with DIFFERENT access-log files, envoy-go writes **every request to BOTH files**. It has never been caught because all six access-log fixtures carry exactly one `access_log:` entry (controller-verified: `0006`, `0081`, `0082`, `0083`, `0084`, `0085`, one each). Recorded here so it is diagnosed rather than rediscovered; it is a prerequisite the `access_log[].filter` row must confront anyway.
- **⚠️ ROADMAP BOOKKEEPING — the inherited ordinal artifact, NOT acted on.** The Observability-family CHAIN ordinal remains ONE LOW against a mechanical count (row 69 carries no ordinal word, so 70/71/72 each ran one short and 73 inherits it: this row is the NINETEENTH by chain, the twentieth mechanically). **Phase 73 KEEPS the chain ordinal deliberately** — renumbering 73 alone would make it inconsistent with three landed `done` rows. Recorded so the eventual correction is one deliberate edit rather than a rediscovery. Every "NINETEENTH" in this document is the CHAIN ordinal.

### 2.2 Scope: the `metadata` type, `CLUSTER` MetadataKind ONLY; the unset kind stays PARSE-REJECTED *(self-answered; the incremental-arm precedent)*

Phase 73 supports `kind == CLUSTER`. After it lands, all four MetadataKinds are accepted and **only the two unset-kind arms still reject** — `config.go:232-233` (nil `*MetadataKind`, the DEPARTURE half) and `:287-288` (present-empty oneof, the PARITY half), the split phase 71's S3 established. This mirrors the project's landed incremental-arm posture (`custom_tags` TYPES one at a time at 59/62/63; MetadataKinds one at a time at 70/71/72/73).

### 2.3 The resolve seam: a NEW `kindMetadataCluster` arm; a SIXTH nil-tolerant one-arg param *(self-answered shape; SPEC pins — D-CTMC-RESOLVE-SEAM)*

Phase 73 anticipates a **SIXTH nil-tolerant parameter `clusterMetaLookup func(ns string) (*structpb.Value, bool)`**, shaped IDENTICALLY to `routeMetaLookup`/`hostMetaLookup` (one-arg, namespace → the whole namespace struct wrapped as a `StructValue`). The `kindMetadataCluster` arm is then a byte-level clone of the landed `kindMetadataHost` arm:

- `v, ok := clusterMetaLookup(s.MetaNamespace)`
- `if ok { v, ok = descend(v, s.MetaPath) }` — the **FULL** `MetaPath`, NOT `[1:]`. The `[1:]` lives ONLY in the `kindMetadata`/REQUEST arm and is a Bucket-pre-keying artifact, not a general rule (`reference_route_metadata_resolve_full_metapath`).
- `structpbValueToString(v)` — `descend` and `structpbValueToString` **REUSED VERBATIM**. Both are proto-agnostic, so `internal/tracing` stays filter-free and cluster-free (the layering check is unaffected).
- `clusterMetaLookup == nil` ⇒ cluster specs use default/omit, keeping all existing behavior byte-identical when no CLUSTER tags are configured.

The config-side addition is a `kindMetadataCluster` constant. **PLACEMENT is load-bearing: APPEND after `kindMetadataHost`** — re-derived at tip, `config.go:55-62` currently holds six constants at `iota` 0–5 with `kindMetadataHost` LAST, so the new one is **`iota == 6`**. Inserting before it would silently renumber three landed kinds; the constants are unexported and never serialized, so nothing would fail loudly (phase 72's Break G is the precedent that catches exactly this). **Collision check RUN at tip:** `grep -rn 'kindMetadataCluster\|clusterMetaLookup\|ClusterMetaLookup' internal/` ⇒ ZERO occurrences — the names are free (`reference_spec_drafted_identifier_collision_check`; re-run at the SPEC tip anyway).

### 2.4 ⚠️ THE LOAD-BEARING QUESTION — TWO viable seams, NOT cost-equivalent and NOT behaviorally equivalent (D-CTMC-SEAM)

This is phase 73's D-CTMH-ENDPOINT-METADATA analogue and the one decision the SPEC must make on evidence rather than convenience.

**⚠️ CORRECTION FOLDED IN (adversarial verifier V1, SEVERE) — an earlier draft of this section opened "Both seams avoid the 18-caller thread." THAT IS FALSE, and it was the document's headline claim.** **Only seam A avoids the thread. Seam B RE-PAYS it in full.** The reason is structural and V1 proved it by execution: seam B's source is `entry`, which lives in the **dispatch** functions (`dispatchRequest`, `runH3`, `Match`/`WriteH2`), but the three `ResolveCustomTags` call sites live **inside the emit helpers**, whose parameter lists (`accesslog_emit.go:27`/`:87`/`:149`) carry **no `entry`** — and nothing on `*Filter` carries per-request route state (`f.table` is the config-time route table). Phase 72 skipped the tax precisely because `picked` was ALREADY the 4th parameter; seam B has no such free-rider. Building the closure at the call sites therefore needs a NEW parameter ⇒ all three signatures change ⇒ all 18 call sites change ⇒ **exactly the phase-70/71 tax.** V1's proof, from a scratch copy:

```
$ # `_ = entry` inserted inside emitAccessLog, immediately before the Export call
$ go build ./internal/filter/hcm/
internal/filter/hcm/accesslog_emit.go:57:7: undefined: entry
```

**The consequence for §2.1's headline is real and is folded there too:** the "convergent refutation from disjoint evidence" collapses to a **single-source refutation**. Agent B's seam is a genuine *source* discovery — the cluster IS reachable from the route entry — but it is NOT a thread-free one. The stale-cost correction SURVIVES, on seam A alone. V1 also notes that even a chain-carried variant (a `chain.SetClusterMetadata`/`ClusterMetaLookup` pair, the phase-71 shape) does not escape, since it still adds an argument at all 18 sites.

**So the two seams differ on THREE axes, not two: the threading tax, what the tag resolves to when no endpoint was acquired, and which package absorbs the change.**

**SEAM A — endpoint-carried (agent A).** Retain the owning cluster's `filter_metadata` on `Endpoint`, populated through `extractEndpoints`'s single call site (`manager.go:433`, `*clusterv3.Cluster` in scope at `:429`), exposed as `Endpoint.ClusterMetaLookup(ns)` beside the phase-72 `MetaLookup` (`cluster.go:111-117`), passed as `picked.ClusterMetaLookup` at the three emit sites.
- **For:** a near-verbatim phase-72 clone. `internal/filter/hcm` sees a three-line diff; `connection.go`/`h2dispatch.go`/`h3dispatch.go`/`chain.go`/`route.go`/`actions.go` all BYTE-UNTOUCHED. One aliasing map pointer per `Endpoint` (sizeof 112 → 120 B), zero new allocation, zero new import (`structpb` already imported at `cluster.go`). **It also DISSOLVES the weighted-cluster problem entirely** — the picked endpoint definitionally belongs to the cluster the weighted selector actually chose, so weighted routes resolve correctly with no extra work.
- **Against:** it **INHERITS the BEHAVIOR_CONTRACT B2 zero-`Endpoint` departure verbatim.** On every upstream-ACQUIRE failure sub-path `picked` is the zero `Endpoint`, so a CLUSTER tag falls to `default_value`/omit — **even though the reference knows the cluster regardless of whether any endpoint was reachable.** ⚠️ **CORRECTION FOLDED IN (V1, MODERATE):** an earlier draft called this "a WIDER departure than B2 is for HOST". **It is not wider in EXTENT** — under seam A it fires on exactly the same site set as B2, both gated on `picked.IsZero()` at the same emit sites. It is **the same breadth with a materially worse justification**: for HOST, "no endpoint picked ⇒ no endpoint metadata" is arguably coherent; for CLUSTER it is not, because the cluster was selected by the route before any endpoint was tried. The breadth claim is retracted; the coherence claim stands. (Phase 72 already shipped one over-broad departure claim that had to be retracted in review — this is the same class, caught pre-landing.)
- Data-provenance obligation carried forward from phase 72: re-walk BOTH connection pools to confirm neither reconstructs an `Endpoint` from the cached addr string, or the new field arrives zeroed on pool hits while fresh-dial tests stay green. Phase 72 walked this and found it CLEAN — but a positive finding is exactly the kind that goes stale unnoticed.

**SEAM B — route-carried (agent B).** Read the cluster off the matched route entry: `entry.action.(*clusterRouteAction).cluster` (the field at `actions.go:183`; `:182` is the struct header), with `entry` in scope at all five H1 sites (`connection.go:312-780`, bound `:319`) and all seven H3 sites (`h3dispatch.go:125`), ONE new H2 carrier field cloning the landed `routeMetadata` template (`h2dispatch.go:158`/`:250`/`:375`) — **and, unavoidably, a NEW emit parameter threaded through all three signatures and all 18 call sites**, because the `ResolveCustomTags` calls sit inside the emit helpers where `entry` does not exist.
- **For:** the cluster is known whenever a route matched, **independent of whether an endpoint was acquired** — so it does NOT inherit B2. `internal/cluster` stays BYTE-UNTOUCHED. ⚠️ **CORRECTION FOLDED IN (V1, SEVERE) — an earlier draft wrote that a CLUSTER tag then "resolves on local-reply/500/connect-failure paths exactly as the reference does". The `500` arm is FALSE and is the exact error class phase 72 had to retract.** The synthetic-500 emit sites are **PRE-Decide with a nil `traceDecision`**, so **NO SPAN IS EXPORTED AT ALL** and no tag of any kind can resolve there under EITHER seam — `connection.go:459-460` says so in a landed comment (*"traceDecision is nil here (PRE-Decide path) — no span for this synthetic 500"*), matched at `h3dispatch.go:210` and `h2dispatch.go:164-166`. **Partition by SPAN CAPABILITY, not by whether a value is zero** (`reference_host_tag_resolves_at_pick_time`): of the 18 emit callers, 6 are pre-decision and can never emit a span, so they are `N/A — no counterpart` and must NEVER be written as parity or departure in either direction. **Seam B's advantage is real only on the post-Decide upstream-ACQUIRE-failure class** (and on filter-generated local replies at `:597`/`:699`, which ARE span-capable). Stated this narrowly on purpose: phase 72 shipped exactly this over-broad shape and had to retract it in review.
- **Against:** **it RE-PAYS the 18-caller threading tax in full** (three emit signatures + 18 call sites + the emit-test callers — the phase-70/71 shape, and the very tax the stale-cost correction was thought to have escaped), plus an H2 carrier field and a type assertion at the emit seam; and the **`weightedClusterRouteAction` arm is genuinely unreachable** — re-derived, `internal/filter/http/router/router_weighted.go:119` selects `a := actions[sel.pick()]` INSIDE the returned closure, so a statically-resolved cluster at HCM build time is wrong for weighted routes. That needs either a documented departure or an ADR-0080 reject on `metadata`/`CLUSTER` + `weighted_clusters` on the same route. The `direct_response` arm has no cluster at all ⇒ default/omit either way.

**The trade is real but NOT symmetric** *(corrected after review — an earlier draft called it symmetric, which flattered seam B by ~18 call sites plus three signature changes)*: seam A buys a much smaller diff and free weighted-route correctness at the price of inheriting B2's departure with a worse justification; seam B buys correct behavior on the post-Decide acquire-failure class at the price of the FULL threading tax, an H2 carrier field and a weighted-route hole. **D-CTMC-SEAM must decide on PROBED reference behavior, not on diff size** — specifically, what the reference emits for a CLUSTER tag on (i) an upstream connect failure, (ii) a no-route 404, and (iii) a weighted route. Anticipated but NOT assumed: the reference resolves CLUSTER at route-selection time, which would favor seam B on fidelity. **A hybrid is also admissible** (seam A's retention plus a route-side fallback when `picked` is zero) and the SPEC should price it rather than treating the choice as binary.

**Interaction with the deferred B2 row, stated explicitly:** if the pick-time `picked`-propagation row lands FIRST, seam A's objection largely evaporates. This row does not depend on that one, but the SPEC should note the ordering so a future roller does not "fix" the same gap twice.

### 2.5 The remaining unsupported MetadataKind rejects loudly — the envoy-go-strict DEPARTURE narrows to the unset arms *(self-answered; ADR-0080)*

RE-DERIVED at tip:

| arm | line | disposition at 73 |
|---|---|---|
| `k == nil` → `"... kind required"` | `config.go:232-233` | UNCHANGED (nil `*MetadataKind`; the DEPARTURE half) |
| `k.GetRequest() != nil` accept | `config.go:234-250` | UNCHANGED (phase 70) |
| `k.GetRoute() != nil` accept | `config.go:251-267` | UNCHANGED (phase 71) |
| `k.GetCluster() != nil` → `"... cluster kind unsupported"` | `config.go:268-269` | **REPLACED by an accept arm** |
| `k.GetHost() != nil` accept | `config.go:270-286` | UNCHANGED (phase 72) — **the clone source for phase 73** |
| `default:` → `"... kind required"` | `config.go:287-288` | UNCHANGED (present-empty oneof; the PARITY half) |

The accept arm clones `config.go:270-286` exactly, swapping `kindMetadataHost` → `kindMetadataCluster`. Note the receiver fact that cost phase 70 a SEVERE: the four kind-getters live on `*metadatav3.MetadataKind` (the bound `k := md.GetKind()`), **NOT** on `*tracingv3.CustomTag_Metadata` — `md.GetCluster()` does not compile.

**A stale-comment class this row inherits (`reference_code_comment_not_evidence`).** Phase 72 had to narrow nine comments that went false the moment its accept arm landed. Phase 73 lifts the LAST reject arm, so the same sweep is owed and is likely LARGER: every comment saying "CLUSTER rejects", "the metadata kinds that reject", or enumerating three-of-four accepted kinds goes false. **The SPEC must run the sweep rather than assume phase 72 caught them all** — at minimum `internal/tracing/{config.go,config_test.go}`, `internal/filter/hcm/fuzz_test.go`, and any BEHAVIOR_CONTRACT prose.

### 2.6 Fixture posture: anticipated ONE new fixture (OTLP) on the `0116` chassis — NO writer *(SPEC confirms D-CTMC-FIXTURE)*

A CLUSTER metadata custom tag is an OBSERVABLE span attribute, so it IS differential-provable. Per the dispatch constraint (`reference_differential_fixture_dispatch_constraint` — one fixture dir = ONE runner branch) the anticipated posture is a NEW `test/fixtures/0117-tracing-custom-tags-metadata-cluster` dir (OTLP provider, asserting the tag on the OTLP span via the `test/helpers/otlptrace` receiver), modelled on the just-landed `0116` chassis with the metadata block relocated from `lb_endpoints[0].metadata` to the cluster's own `metadata`.

**Like 0115/0116 and unlike 0114, NO runtime writer is needed** — the source is a STATIC `clusters[].metadata.filter_metadata{ns: {...}}` block read identically by subject and reference. Two CLUSTER `custom_tags`: one resolving to the static value, one whose namespace/path is absent so it falls through to `default_value` — both asserted **cross-side EXACT key+value**. **STRING values only** (`reference_structpb_tag_cross_side_string_only`: the reference's key order for multi-key structs is ARBITRARY where Go sorts, number precision differs, and Zipkin drops empty-string values). **The namespace must NOT be assumed to be `envoy.lb`** — `reference_envoy_lb_namespace_not_privileged`: ANY `filter_metadata` namespace is addressable, proven by live probe AND by the landed `0116` fixture, so the fixture should deliberately use an arbitrary namespace exactly as `0116` did. Single-endpoint cluster so the pick is deterministic (`reference_round_robin_offset_randomized`).

**Two fixture obligations this row carries that 0116 did not.** (i) If seam B is chosen, the fixture SHOULD include an arm proving the tag resolves on a path where no endpoint was acquired — that is seam B's entire advantage and leaving it unasserted would make the seam choice unprovable. (ii) `0116`'s landed limitation must not be repeated silently: with exactly one endpoint it could not discriminate *selected* from *only*, and that had to be named explicitly in review. A single-cluster fixture has the analogous limit — it cannot discriminate *selected cluster* from *only cluster*. **Name it in `expectations.yaml` up front**, or add a second cluster with a deterministic route split.

Whether a SECOND Zipkin dir is warranted is D-CTMC-FIXTURE (anticipated **NO** — the shared `Span.Attrs` seam plus unit tests prove both exporters; the 59/62/63/70/71/72 precedent). Anticipated: fixtures **118 → 119**.

### 2.7 Fuzz posture: a SEED ADDITION to the EXISTING `FuzzHCMConfigParse` — and a MANDATORY repoint *(SPEC confirms D-CTMC-FUZZSEED)*

RE-DERIVED at tip, the `withMetaTags` seed carries four tags, **in this order**: `meta_ok` (`:102`, REQUEST accept), `meta_route_ok` (`:107`, ROUTE accept), `meta_host_ok` (`:112`, HOST accept), and `meta_bad` (`:117`, **CLUSTER — the live reject-arm seed, and it is LAST**). *(Order corrected after review — an earlier draft listed `meta_bad` third. The order is load-bearing to this section's own dispatch-verify trap below, so getting it wrong would have undermined the very argument it supports.)*

**Phase 73 REQUIRES a repoint, and this is the one place it is strictly more expensive than phase 72.** Phase 72 was purely additive because it removed the HOST arm, which no seed pointed at. Phase 73 removes the CLUSTER arm — **which is exactly what `meta_bad` exercises.** So `meta_bad` must be repointed to a still-rejecting arm, and after this row the ONLY rejecting arms are the two unset-kind ones. Anticipated: repoint `meta_bad` to a **nil/absent `kind`** (the `config.go:232-233` DEPARTURE arm) and add a `meta_cluster_ok` accept seed. The SPEC must dispatch-verify BOTH — that the new accept seed reaches the `kindMetadataCluster` arm AND that the repointed `meta_bad` still reaches a reject — with the discrimination stated BEFORE the run (`reference_probe_must_discriminate`; phase 72's T5 is the template, including the trap that a first-error return means seeing the LATER seed's line at all proves the earlier one did not short-circuit).

Fuzzer count STAYS **55** — re-derived mechanically at tip with the canonical `internal/`-scoped command, `grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l` ⇒ **55** (an `f.Add` seed is not a fuzzer — `reference_fuzzer_count_docs_drift`).

### 2.8 Stat surface hypothesis: +0 *(self-answered; a span attribute registers no stat)*

Anticipated stat surface **1201 (+0)**, UNCHANGED. Per the phase-71 V2 finding, tracing rows carry **no `TestNoNewStat`-style guard obligation** — do not manufacture a cited-but-unwritten one.

---

## 3. Framework-survey result — a reject-lift + one seam decision; ZERO new packages/modules

### 3.1 Framework: a small seam at most

No new interface and no new package-level type beyond a `kindMetadataCluster` constant (reusing the existing `CustomTagSpec` metadata fields), a new nil-tolerant `clusterMetaLookup` parameter on `ResolveCustomTags`, and — under seam A — one added field plus one accessor on `cluster.Endpoint`, or — under seam B — one H2 carrier field. Every other symbol (`descend`, `structpbValueToString`, the metadata spec fields) is pre-existing and reused.

### 3.2 NEW packages: NONE

All edits land in `internal/tracing` + (`internal/cluster` under seam A | `internal/filter/hcm` under seam B) + `test/fixtures` + `docs/`. `internal/dynamicmetadata` is NOT touched. NO new package, so `reference_new_subpackage_pulls_transitive_module` does not bite — still re-check `git diff go.mod` after tidy at the IMPL.

### 3.3 go.mod modules: NONE

`type.tracing.v3.CustomTag_Metadata` + `type.metadata.v3.{MetadataKind,MetadataKey}` are already reachable (consumed by the 70/71/72 arms). Under seam A, `structpb` is ALREADY imported by `internal/cluster/cluster.go` (phase 72 added it) — so this row anticipates **ZERO new import lines in `cluster.go`**. ⚠️ **But NOT in `manager.go` (V1 M1 + V2 M2, MODERATE — and a RECURRENCE of phase 72's own folded finding #2 in mirror form).** `internal/cluster/manager.go` imports neither `structpb` nor `corev3` (re-derived at `:3-17`), so a third `extractEndpoints` parameter typed `map[string]*structpb.Struct` **adds ONE import there** — V1 proved it: `internal/cluster/manager.go:863:105: undefined: structpb`. The zero-import carrier, if that matters, is `*clusterv3.Cluster`, which IS already imported. **An import LINE is still not a go.mod MODULE, so `+0 modules` holds either way** (V1: `go mod tidy -diff` EXIT 0, empty). `go mod tidy -diff` anticipated EMPTY; the tracked go.mod-modules lineage figure stays **2** (per STATE.md this is the phase-61.2 count — `quic-go` direct + `qpack` indirect — **NOT a repo total**; the single `go.mod` requires 67 modules, controller-verified).

### 3.4 REUSES

- **phase-46** the tracing engine: `Span` / `Span.Attrs []KV` / `BuildServerSpan`, the OTLP + Zipkin exporters, the `test/helpers/otlptrace` receiver.
- **phase-59/62/63** the `custom_tags` pipeline: `parseCustomTags` (parse home + first-wins dedup) and `ResolveCustomTags` (resolve home).
- **phase-70** the `metadata`-kind machinery: `descend` + `structpbValueToString` reused VERBATIM; the `CustomTagSpec` metadata fields; the six-arm kind switch; the `k := md.GetKind()` receiver fact.
- **phase-72** the nearest template: the `kindMetadataHost` config accept arm (`config.go:270-286`) and its resolve arm as byte-level clone sources; `Endpoint.MetaLookup` (`cluster.go:111-117`) as the accessor-shape precedent AND, under seam A, the retention-field pattern (`cluster.go:72-79`); `0116` as the fixture chassis.
- **phase-71** `SetRouteMetadata`/`RouteMetaLookup` and the H2 `routeMetadata` carrier (`h2dispatch.go:158`/`:250`/`:375`) as the seam-B template.
- **`FuzzHCMConfigParse`** as the fuzz host (a seed addition + one repoint, no new fuzzer).

---

## 4. Bootstrap-level applicability — a PER-LISTENER HCM filter config, with the SOURCE in a STATIC CLUSTER

Like its predecessors, `custom_tags` is a PER-LISTENER HCM `tracing` sub-field parsed by `parseCustomTags`. No bootstrap change. Phase 73 shares phase 72's structural wrinkle: the SOURCE lives in `static_resources.clusters[]` — here on the cluster's own `metadata` rather than on `lb_endpoints[].metadata` — so the fixture's config surface spans both the listener and the cluster, but both remain static and no bootstrap-level field is newly consumed.

**One thing to record: `clusters[].metadata` is accepted-and-ignored today.** Agent A booted a config carrying `clusters[].metadata.filter_metadata` and it ran clean. Unlike `access_log[].filter` this is NOT a live divergence, because the only consumer (a CLUSTER-kind custom tag) is currently parse-REJECTED — so nothing silently misbehaves. **This row is a knob-lift, not a bug-fix**, and saying so plainly is part of the honest case for it.

---

## 5. Stat surface hypothesis — +0 (73)

A span attribute registers no stat. Anticipated stat surface **1201 (+0)**. No `stats.IsValidName` exposure (the tag key is a span-attribute key, not a stat name).

---

## 6. Anticipated edit sites (SPEC RE-DERIVES each at tip — a BRAINSTORM cite is not evidence, `feedback_brief_citations_not_evidence`)

All line numbers below were RE-OPENED at tip `5aaadf13` by the controller while writing this document (§11.3 records the cites that needed correcting).

- `internal/tracing/config.go:268-269` — LIFT the CLUSTER reject → an accept arm cloning the HOST accept (`:270-286`), building `CustomTagSpec{Key: tag, Kind: kindMetadataCluster, MetaNamespace: mk.GetKey(), MetaPath: path, DefaultValue: dv, HasDefault: dv != ""}`. Both unset-kind rejects (`:232-233`, `:287-288`) UNCHANGED. Anticipated ZERO new imports.
- `internal/tracing/config.go:55-62` — a new `kindMetadataCluster` constant APPENDED after `kindMetadataHost` (**`iota == 6`**; never insert).
- `internal/tracing/resolve.go:46` — a SIXTH nil-tolerant `clusterMetaLookup func(ns string) (*structpb.Value, bool)` parameter, appended after `hostMetaLookup`; the doc comment extended.
- `internal/tracing/resolve.go` (a new arm after the `kindMetadataHost` arm) — `case kindMetadataCluster:` descending the **FULL** `MetaPath`, reusing `descend` + `structpbValueToString`, same default rule. NO new import.
- **Seam A only** — `internal/cluster/cluster.go:80` a second aliasing retention field beside `filterMetadata` (`:72-79` is that field's doc COMMENT, not the field); `cluster.go:111-117` a `ClusterMetaLookup(ns)` accessor beside `MetaLookup`; `internal/cluster/manager.go:433` + `:863` a third `extractEndpoints` parameter carrying `c.GetMetadata().GetFilterMetadata()`. The phase-38 `ScalarsFromStruct` projection at `manager.go:883` and `defaultSubset` at `:754` stay BYTE-UNCHANGED. ⚠️ **ONE new import in `manager.go`** — corrected after review (§3.3): an earlier draft said ZERO, generalizing from `cluster.go`; `manager.go` imports neither `structpb` nor `corev3`, and V1's build produced `manager.go:863:105: undefined: structpb`. Passing `*clusterv3.Cluster` instead would keep it at zero.
- **Seam B only** — `internal/filter/hcm/h2dispatch.go:158`/`:250`/`:375` one new carrier field cloning `routeMetadata`; a `clusterRouteAction` type assertion at the emit seam (the field at `internal/filter/hcm/actions.go:183`, read-only); `entry` read at the 5 H1 (`connection.go`) + 7 H3 (`h3dispatch.go`) sites; **AND a new parameter on all three emit signatures threaded through all 18 call sites** — the full phase-70/71 tax (§2.4, V1 S1).
- `internal/filter/hcm/accesslog_emit.go:57`, `:118`, `:179` — pass the `clusterMetaLookup` closure as the 6th `ResolveCustomTags` argument (touched under BOTH seams). ⚠️ **UNDER SEAM A ONLY**, the three emit function SIGNATURES (`:27`, `:87`, `:149`) are anticipated UNCHANGED and the 18 emit callers BYTE-UNTOUCHED — re-derive and re-confirm at the SPEC, since it is the row's central cost claim (phase 72 verified the analogous claim BY SHA256, not by reading). **Under seam B all three signatures and all 18 callers CHANGE** — an earlier draft asserted the BYTE-UNTOUCHED envelope "under BOTH seams", which both verifiers refuted (§2.4).
- `internal/tracing/config_test.go` — **the tests the row MUST FLIP**: the `/cluster` reject-kind table cases. Phase 72's precedent was EXACTLY TWO such subtests for `/host`; the SPEC must enumerate the `/cluster` set at its own tip rather than assume the same count, and the sibling unset-kind rows in the same tables stay reject and are the live regression guard.
- `internal/tracing/resolve_test.go` — resolve/path-walk/default/nil-lookup tests, PLUS a trailing `nil` sixth argument at every existing `ResolveCustomTags` call. **Count re-derived at tip: `grep -c 'ResolveCustomTags(' internal/tracing/resolve_test.go` ⇒ 50 raw hits.** Phase 72's lesson applies directly: its raw grep returned 33 of which ONE was a `t.Errorf` STRING LITERAL, giving 32 real callers. **Re-grep and exclude literals by an explicit line gate at the PLAN; do NOT adopt the raw count.**
- `internal/filter/hcm/fuzz_test.go` — ADD a `meta_cluster_ok` seed AND **REPOINT `meta_bad` off CLUSTER to a still-rejecting (unset-kind) arm** (§2.7); narrow the seed block's stale comments.
- **A repo-wide stale-comment sweep** (§2.5) — this row lifts the LAST reject arm, so every comment enumerating rejecting kinds goes false.
- `test/fixtures/0117-tracing-custom-tags-metadata-cluster/` — the new OTLP fixture, cloning the `0116` chassis; + the one-line `runner_test.go` registration.
- `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the CLUSTER-kind tracing edit + the departure-roster narrow to the unset arms; **and, under seam A, a NEW departure clause for the zero-`Endpoint` case (§2.4).**
- `docs/envoy-go/DECISIONS.md` — ADR-0295 (§Context at the SPEC, §Decision + §Consequences at the IMPL IN PLACE).
- `docs/envoy-go/ROADMAP.md` — the deferred-sentence narrow: the `CLUSTER` candidate rolls OUT of the live Observability `candidates:` sentence at the IMPL row-done edit (phase-57 precedent). **⚠️ That sentence currently names `CLUSTER` as the LAST unconsumed MetadataKind, so this narrow REMOVES the whole `metadata`-MetadataKind clause rather than shortening it** — the SPEC/PLAN must check whether what remains is still a well-formed sentence with at least one candidate, or sentinel check (2) changes count.

---

## 7. BRAINSTORM-time open questions to the SPEC (the D-CTMC-* docket)

Marked **[PROBE]** where a live arm against `envoyproxy/envoy:contrib-v1.37.2` is REQUIRED, **[CODE]** where a read-only re-derivation suffices. Probe hygiene carries forward: a fresh container per arm (`feedback_probe_fresh_container_per_arm`), a bridge network (`reference_docker_probe_bridge_network`), the host-gateway literal plus `dns_lookup_family: V4_ONLY` (`reference_host_gateway_ip_docker_desktop`), decode-ran asserted per arm, and every arm must DISCRIMINATE (`reference_probe_must_discriminate`).

- **D-CTMC-SCOPE** **[CODE]** — `CLUSTER` MetadataKind only; the unset kind stays reject-only. *Anticipated: confirmed.*
- **D-CTMC-PROTO** **[CODE]** — the `MetadataKind_Cluster_` field shape at the pin, and specifically that `GetCluster()` lives on `*metadatav3.MetadataKind` (the phase-70 SEVERE receiver fact). *Anticipated: `k.GetCluster()` on the bound `k`; `md.GetCluster()` does not compile.*
- **D-CTMC-SEAM** **[PROBE + CODE]** — **THE LOAD-BEARING QUESTION (§2.4).** Seam A (endpoint-carried) vs seam B (route-carried) vs a hybrid. The PROBE half must pin what the reference emits for a CLUSTER tag on (i) an upstream connect failure, (ii) a no-route 404, (iii) a weighted route — because those three arms are exactly where the seams DIVERGE, and a probe that only exercises the happy path is consistent with both and therefore proves nothing. The CODE half must price the H2 carrier field, the weighted-route disposition, and (for seam A) re-walk endpoint provenance through BOTH connection pools.
- **D-CTMC-WIRE** **[PROBE]** — does the reference emit the CLUSTER tag as `{key = the literal tag name, value = the serialized string}` on the OTLP span? *Anticipated: identical to REQUEST/ROUTE/HOST.*
- **D-CTMC-ZIPKIN-WIRE** **[PROBE]** — the same tag in the Zipkin `tags` map. Matters because envoy-go covers both exporters from ONE `Attrs` seam and the row ships no Zipkin fixture, so the probe IS the evidence.
- **D-CTMC-VALUE-SERIALIZE** **[PROBE]** — the `structpb.Value` → string table for a CLUSTER-kind tag. *Anticipated: the phase-70 P3 table exactly.* Matters because `structpbValueToString` is being reused VERBATIM.
- **D-CTMC-DEFAULT** **[PROBE]** — the `default_value` / omit rule when the namespace or path is absent, and the present-empty-string edge. *Anticipated: the `request_header` rule.*
- **D-CTMC-NOCLUSTER** **[PROBE + CODE]** — the CLUSTER analogue of phase 72's D-CTMH-NOHOST, and **it is NOT the same question.** What happens on a no-route 404 (no cluster selected at all), a `direct_response` route (no cluster by construction), and a local reply? Note the partition phase 72 had to learn the hard way: **partition by SPAN CAPABILITY, not by whether a value is zero** — 6 of the 18 emit sites are PRE-Decide and can never emit a span at all, so they cannot be written as parity or departure in either direction (`reference_host_tag_resolves_at_pick_time`).
- **D-CTMC-CONFIG-KIND** **[CODE]** — `kindMetadataCluster` APPENDED at `iota == 6`, never inserted. Collision check RUN at tip (ZERO hits for all three drafted identifiers); re-run at the SPEC tip.
- **D-CTMC-REJECT** **[PROBE]** — the accept arm's structural edge rejects (empty `metadata_key.key` / empty `path` / empty path segment — PGV parity), plus one arm confirming the reference ACCEPTS a `CLUSTER` metadata tag. **And a NEW obligation unique to this row:** after phase 73 the ONLY reject arms are the two unset-kind ones, so the SPEC must re-confirm the phase-71 S3 split wording (nil `*MetadataKind` = DEPARTURE; present-empty oneof = PARITY) is still accurate **and still the complete departure roster** — the BEHAVIOR_CONTRACT edit depends on it.
- **D-CTMC-FIXTURE** **[CODE]** — ONE new OTLP dir `0117-tracing-custom-tags-metadata-cluster` on the `0116` chassis; a static `clusters[].metadata` source, NO writer; two CLUSTER tags (static hit + `default_value`) asserted cross-side EXACT key+value, **STRING values only**; the selected-vs-only-cluster limit NAMED up front (§2.6); a seam-B-proving failure-path arm if seam B is chosen. Pin `BackendCount >= 1` and the full-subtest `-run` selector; run breaks with `-count=1` AFTER committing.
- **D-CTMC-FUZZSEED** **[CODE]** — ADD `meta_cluster_ok`; **REPOINT `meta_bad`** off CLUSTER to an unset-kind arm (§2.7 — mandatory this row, unlike phase 72); dispatch-verify BOTH with the discrimination stated before the run. +0 fuzzers (55).
- **D-CTMC-STALECOMMENT** **[CODE]** — the repo-wide sweep for comments that go false when the LAST reject arm is lifted (§2.5). Phase 72 found nine; this row plausibly finds more.
- **D-CTMC-SPLIT** **[CODE]** — SINGLE FLAT ROW anticipated (~8–10 tasks; ADR-0045 valve armable-but-unconsumed).

---

## 8. What phase 73 does NOT deliver (forward)

- **`access_log[].filter`** — the strongest FUTURE pick, now with an EXECUTION proof of the divergence (§2.1) and two named hazards (the global sink flattening; the `headerCaptureSink` non-promotion class) plus the 13-arm reject roster. A single-arm slice (`status_code_filter`) on the `0006` `ReferenceLogMounter`/`AccessLogAsserter` chassis is the shape. **`runtime_key` is a NON-blocker and both agents proved it**: PGV forces it non-empty, but `layered_runtime` is boot-rejected and `internal/runtime/` is a placeholder, so neither side can resolve a runtime key and both fall to `default_value` — guaranteed parity.
- **`hcm.access_log_options`** — NEWLY NAMED; ZERO production consumers; same silent-divergence class, plausibly cheaper. Deferred on evidence depth (one agent only). A strong FUTURE pick.
- **`stdout`/`stderr` access loggers** — NEWLY NAMED by BOTH agents; SMALL production cost, but the reference's stdout returns via `docker logs` so the landed log-mounting chassis does not apply and a new asserter shape is owed.
- **The pick-time `picked`-propagation row (B2)** — deferred, with the probe cross-product named: connect-failure (EXECUTED at phase 72), pool-overflow and circuit-breaker (both UNPROBED), and the scope fork (2 vs 3 landed tests flip). **Read `reference_host_tag_resolves_at_pick_time` before picking it** — an over-broad reading would chase a non-bug, since post-ACQUIRE 502s are PARITY.
- `spawn_upstream_span` (LARGE) / OTel `http_service` (MODERATE) / `sampler` + `resource_detectors` (MODERATE each) / tracing `verbose` (MODERATE) / force-trace (LARGEST). §2.1.
- The downstream TLS handshake-outcome `ssl` stat family (ADR-0286 C3). §2.1.
- `stats_flush_on_admin` (MODERATE — three subsystems, a 53-caller constructor to route around, a timing-shaped negative assertion, and a fuzz seed whose classification inverts). §2.1.
- A sixth stats sink — **unavailable**, not merely deferred. §2.1.
- Response-flags plumbing (MODERATE substrate — unblocks `response_flag_filter`, the tracing `ResponseFlags` attribute, Lua/wasm stream-info parity). §2.1.
- All xDS candidates — the smallest are multiple `tls_certificate_sds_secret_configs` (SMALL ~7) and the in-tree-SPEC'd "compose-two" row (SMALL–MODERATE ~8); everything else is blocked behind the `dynamic_resources` boot-reject and the total absence of any persistent xDS stream. All HTTP/3 candidates (`QuicProtocolOptions` RE-COSTED UPWARD to LARGE). §2.1.
- Opening a never-opened family (gRPC — gated on an HCM response-trailer framework row; Runtime — genuinely unbuilt but lights up ZERO existing consumers; WASM — a ROADMAP bookkeeping artifact). §2.1.
- **The global access-log sink flattening divergence** — diagnosed this session, recorded, NOT chartered. §2.1.

---

## 9. ADR-0045 split readiness + ADR roster

Anticipated a **SINGLE FLAT ROW** of ~8–10 tasks (ADR-0045 valve armable-but-unconsumed). Under seam A the shape is a near-verbatim phase-72 clone; under seam B it trades the `internal/cluster` task for an H2-carrier task plus a weighted-route disposition — comparable either way.

**ONE ADR anticipated: ADR-0295.** Re-derived MECHANICALLY in the worktree: `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1` ⇒ `## ADR-0294`, and `grep -c '^## ADR-0295'` ⇒ **0**, so next-free is **`ADR-0295`**. ADR-0294 is COMPLETE. ADR-0295's §Context drafts at the **SPEC** per ADR-0044 (the DECISIONS tail flips ADR-0294 → ADR-0295 AT that commit); §Decision + §Consequences complete **IN PLACE at the IMPL**, no renumber; next-free after it is ADR-0296. **DECISIONS.md is UNTOUCHED at this BRAINSTORM.**

No new ADR beyond ADR-0295 is anticipated — the seam decision (§2.4) FOLDS INTO ADR-0295 rather than taking its own number, the phase-62/63/70/71/72 precedent. **One caveat:** if D-CTMC-SEAM picks seam A and the SPEC concludes the wider-than-B2 departure needs its own recorded decision, that is still an ADR-0295 §Consequences clause, not a second ADR.

---

## 10. Envelope + counts (anticipated at the phase-73 IMPL; docs-only at this BRAINSTORM)

- ZERO new production packages. Edits in EXISTING files only.
- Blast radius depends on §2.4, **and the two seams do NOT share an envelope**. **Seam A:** touches `internal/cluster` (SHAPE-pinned audit, not BYTE-UNTOUCHED — the phase-72 precedent) and leaves `internal/filter/hcm` at a three-line diff; `internal/filter/http/chain.go`, `connection.go`, `h2dispatch.go`, `h3dispatch.go`, the 18 emit callers and the emit-test callers are anticipated BYTE-UNTOUCHED (V1 confirmed all of these sha256-identical to baseline in a scratch build of seam A). **Seam B:** `internal/cluster` is BYTE-UNTOUCHED, but `connection.go`, `h2dispatch.go`, `h3dispatch.go`, the three emit signatures, the 18 callers AND the emit-test callers all CHANGE. *(An earlier draft asserted the BYTE-UNTOUCHED list "under BOTH seams"; both verifiers refuted it — §2.4, §11.4.)* **Under BOTH seams:** `internal/dynamicmetadata`, `internal/xds`, `internal/tls`, `internal/boot`, `internal/listener`, `internal/bootstrap`, `validate/` anticipated BYTE-UNTOUCHED.
- Layering check: `go list -deps ./internal/tracing` must still show **no `internal/filter` and no `internal/cluster` edge**. ⚠️ **It is a DISCIPLINE check, not a cycle check** — phase 72 established by execution (three times) that `internal/cluster` carries no `internal/tracing` edge either, so a direct import would build clean; the earlier "cycle guard" justification was REFUTED. **Do not reintroduce it.** Re-run at the IMPL with `go list -deps`, no `...`.
- +0 stats → **1201**. +0 fuzzers → **55**. +0 BackendKinds → **38**. +0 go.mod modules → **2** (the tracked lineage figure).
- fixtures **118 → 119** (`0117-tracing-custom-tags-metadata-cluster`, OTLP).
- DECISIONS tail **ADR-0294 → ADR-0295** (at the SPEC; next-free ADR-0296).

**Counts UNCHANGED at this BRAINSTORM (docs-only; ALL re-run MECHANICALLY in the worktree at this close, using the CANONICAL commands recorded in STATE.md):** fixtures **118** (`ls -d test/fixtures/[0-9]*/ | wc -l` ⇒ 118; numeric tail `0116-tracing-custom-tags-metadata-host`) · fuzzers **55** (`grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l` ⇒ 55) · stat surface **1201** (BEHAVIOR_CONTRACT doc count; NO mechanical command) · BackendKind **38** (`H2GoawayResponder`) · DECISIONS tail **ADR-0294** COMPLETE (next-free **ADR-0295**) · go.mod modules **2** (the phase-61.2 lineage figure; the single `go.mod` requires **67**, controller-verified).

**⚠️ A CONTESTED COUNT, reported per `reference_a_drift_correction_is_itself_a_claim` rather than silently resolved.** Agent A reported three fixture figures from three commands (116 / 118 / a cited 117) and correctly declined to pick one. The controller resolves it: the CANONICAL command from STATE.md is `ls -d test/fixtures/[0-9]*/ | wc -l` ⇒ **118**. Agent A's `ls test/fixtures | grep -cE '^[0-9]{4}-'` ⇒ 116 excludes the suffixed siblings `0007a-cors` and `0007b-iteration-probe`; the **117** it saw is the PRE-phase-72 figure (phase 72 moved 117 → 118), not a competing measurement of the current tree. **The canonical count is 118 and it is not genuinely contested** — but the three-command divergence is recorded so a future roller does not re-litigate it.

---

## 11. Sized-against-source — the cost derivations (two independent read-only agents at tip `5aaadf13`)

Two read-only cost-assessment agents RE-DERIVED each candidate's size against source THIS session, explicitly NOT trusting the phase-72 deferral adjectives. **They RANKED DIFFERENTLY — A put `access_log[].filter` first and `CLUSTER` third; B put `CLUSTER` first and `access_log[].filter` sixth — and the disagreement is itself the most useful output.** Where they agreed, the finding is strong; where they disagreed, §2.1 resolves conservatively.

- **`metadata`/`CLUSTER` (the pick) — SMALL ~8–10 UNDER SEAM A; larger under seam B.** **Both agents independently refuted the SOURCE half of the inherited "MODERATE ~11–13, re-pays the 18-caller thread" adjective** — the selected cluster IS reachable at the emit seam without a `routeAction` interface method. ⚠️ **They did NOT both refute the THREAD half, and an earlier draft of this document claimed they did** ("two DIFFERENT seams that avoid it … convergent refutation from disjoint evidence"). BOTH adversarial verifiers refuted that framing independently (§11.4): agent A's seam A avoids the thread because `picked` is already an emit parameter; agent B's seam B **re-pays it**, because its source `entry` is a dispatch-function local the emit helpers never receive. **The cost correction is SINGLE-SOURCED and the row is SMALL on seam A.** The config and resolve arms are byte-level clones of phase 72's HOST pair; `descend`/`structpbValueToString` reused VERBATIM. The ONE genuinely new cost versus phase 72 is the **mandatory `meta_bad` fuzz repoint** (§2.7). +0 stats/pkgs/modules/fuzzers/BackendKinds.
- **`access_log[].filter` — CONTESTED: A says SMALL ~8–9, B says MODERATE ~9–11.** Resolved AGAINST the candidate per the phase-72 precedent for a split verdict. Both agree the divergence is real; A proved it BY EXECUTION. Both independently flagged the `headerCaptureSink` non-promotion hazard. B additionally found the global sink flattening, which A priced as tractable and B did not. A found the 13th reject arm (empty oneof, PGV-required). §2.1.
- **The pick-time `picked`-propagation row — SMALL ~6–8, both agents.** Strong agreement on shape, and both independently found that conn-pool overflow returns a NON-ZERO `ep` (`cluster.go:720-722`) — which the incoming framing missed and which forks the row's scope. Probe-dominated. §2.1.
- **`hcm.access_log_options` — SMALL ~7 (agent B only).** ZERO production consumers, controller-verified. Deferred on evidence depth. §2.1.
- **`stdout`/`stderr` access loggers — SMALL ~6–7 (BOTH agents, independently).** Both identified the same provability blocker. §2.1.
- **`stats_flush_on_admin` — MODERATE ~8–9 (agent A).** Small production seam, three subsystems, 53-caller constructor, timing-shaped negative assertion, inverting fuzz seed. §2.1.
- **QUIC `QuicProtocolOptions` — RE-COSTED UPWARD to LARGE ~11–12 (agent A).** The only candidate where harness work exceeds production work. §2.1.
- **Runtime family — MODERATE ~9 (agent B).** Lights up ZERO existing consumers; all ~10 runtime-adjacent sites are themselves rejects or documented ignores. §2.1.
- **xDS — the "no persistent stream" finding RE-CONFIRMED MECHANICALLY.** Smallest rows are inside the SDS envelope (~7–8); everything else LARGE. §2.1.
- **`spawn_upstream_span` / `http_service` / `sampler` / `resource_detectors` / `verbose` / force-trace / the `ssl` stat family / a 6th sink** — unchanged from phase 72's adjudication, re-confirmed. §2.1.

### 11.1 What was and was NOT verified by execution

**Agent A ran boot probes** (a scratch-built `./cmd/envoy-go`, the repo tree left unmodified — `git status --porcelain` showed only the pre-existing untracked `.claude/` before and after): the `access_log[].filter` accept-vs-unknown-field discrimination, a `clusters[].metadata` boot, and a `stats_flush_on_admin`-adjacent read. `go build ./...` and `go vet ./...` both exit 0 at baseline.

**What was NOT done, stated plainly:** **neither agent built this row.** Phase 72's BRAINSTORM carried a scratch build of its full claimed shape (its §11.1); phase 73's does not. The seam analysis in §2.4 is a READ-ONLY derivation, and **no probe has confirmed that either seam compiles, let alone that it produces a green cross-side assertion.** That is the SPEC's job. Nothing in this document may be cited as execution evidence for the row's buildability.

**Nor was any reference behavior probed.** The three D-CTMC-SEAM arms (connect failure / no-route 404 / weighted route) are the questions that DECIDE the seam, and all three are open. A SPEC that picks a seam on diff size rather than on probed reference behavior would be picking the wrong way.

### 11.2 Controller re-derivation of the agents' load-bearing claims

Per `feedback_brief_citations_not_evidence`, the controller RE-OPENED the claims the pick rests on rather than trusting either report. ⚠️ **This table was itself adversarially re-checked, and TWO of its "HOLDS" verdicts carried drifted cites** (marked below). That is the sharpest lesson of this stage: *a verification pass can launder a wrong cite into apparent authority*. The verdicts survived; the line numbers did not.

| claim | source | controller result |
|---|---|---|
| `clusterRouteAction` holds `cluster *cluster.Cluster` | B | **HOLDS, cite CORRECTED** — the field is `actions.go:183`; `:182` is the struct header (both verifiers) |
| `entry` in scope at all 5 H1 emit sites | B | **HOLDS, span CORRECTED** — `dispatchRequest` spans `connection.go:312-780` (not `:312-784`; `:782-784` is `drainAndClose`'s doc comment), `entry` at `:319`, sites `:330`/`:464`/`:597`/`:699`/`:777` (both verifiers) |
| `entry` in scope at all 7 H3 emit sites | B | **HOLDS** — `h3dispatch.go:125`, sites `:130`/`:210`/`:280`/`:341`/`:367`/`:373`/`:395` |
| the H2 `routeMetadata` carrier template | B | **HOLDS** — `h2dispatch.go:158` / `:250` / `:375` |
| `extractEndpoints` has ONE non-test call site | A | **HOLDS** — `manager.go:433`; the `*clusterv3.Cluster` is in scope (`:429`) |
| `cluster.Cluster` has no metadata field | A + B | **HOLDS** — struct at `cluster.go:157`; no metadata field |
| `type Cluster struct` is at `:157`, not `:132` | A | **HOLDS** — A's correction is right |
| `kindMetadataHost` is LAST at `iota == 5` | A | **HOLDS** — `config.go:55-62` |
| the CLUSTER reject line | A | **HOLDS** — `config.go:269`, arm `:268-269` |
| `ResolveCustomTags` takes 5 params | A + B | **HOLDS** — `resolve.go:46` |
| `access_log_options` has ZERO production consumers | B | **HOLDS** — grep ⇒ 0 |
| the global sink flattening | B | **HOLDS** — `config.go:363`; all 6 access-log fixtures carry 1 entry |
| `UpstreamCluster: ""` is at `:52`/`:112`/`:172` | B | **DRIFT** — actual `:51`/`:112`/`:173`; and it is on `tracing.SpanInputs`, not the access-log record |
| fixtures "contested" between 116/117/118 | A | **RESOLVED** — canonical command ⇒ **118**; see §10 |

### 11.3 Cite-drift found and corrected against the INCOMING router/STATE framing

1. **`internal/cluster/cluster.go:132` for `type Cluster struct` is WRONG** — it is `:157`. The cite appears in phase 72's BRAINSTORM §2.1/§8 and was carried into next-prompt.txt.
2. ⚠️ **RETRACTED — this "correction" was ITSELF WRONG, and BOTH verifiers caught it.** An earlier draft claimed *"`internal/filter/hcm/route.go:52-59` for the `routeAction` interface is off by one — it is `:52-60`"*. Re-derived independently by V1 and V2: the interface opens at `:52` and closes at `:59`; `:60` is BLANK. **The incoming cite was CORRECT and the correction introduced the error** — an `old_string` built on `:52-60` would have grabbed a blank line. This is `reference_a_drift_correction_is_itself_a_claim` firing on a document that cites that very memory two paragraphs earlier, and it is the same defect class V1 caught in the phase-72 PLAN (its RD-FUZZ "correction"). **The drift tally in this section therefore drops from 6 items to 5.**
3. **The `UpstreamCluster: ""` hard-code is on `tracing.SpanInputs`, at `accesslog_emit.go:51`/`:112`/`:173`** — not on the access-log record (which has no such field), and not at the lines either agent cited.
4. **The "`CLUSTER` re-pays the 18-caller thread" cost consequence is REFUTED** (§2.1) — the `router.go:255` FACT it rests on is true; the conclusion drawn from it is not.
5. **"go.mod modules 2" is a LINEAGE figure, not a repo total** — re-derived: ONE `go.mod`, requiring **67** modules. Stated so the SPEC does not "correct" it.
6. **"fixtures 117"** in phase-72-era prose is the PRE-phase-72 figure; the current canonical count is **118**.


### 11.4 Verifier fold — corrections to THIS DOCUMENT'S OWN earlier draft (2026-07-23)

Two independent adversarial verifiers read this document at `f98d2106` (committed, unpushed) in PRIVATE scratch — **V1** (code claims + by-execution) and **V2** (process, consistency, stage-close mechanics). Their findings are folded in ABOVE at the point of use; the ledger is here so the SPEC can see what moved and why. **BOTH found SEVERE defects, and they converged on the same one from disjoint starting points** — V1 by building the row, V2 by reading the document against itself.

| # | severity | finding | disposition |
|---|---|---|---|
| 1 | **SEVERE** (both, independently) | **"Both seams avoid the 18-caller thread" is FALSE for seam B** — the document's HEADLINE claim. Seam B's source `entry` is a *dispatch*-function local; the three `ResolveCustomTags` calls live inside the emit helpers, which receive no `entry` and have no per-request route state on `*Filter`. A 6th lookup sourced from `entry` needs a new emit parameter at all 18 sites — the phase-70/71 tax. V1 proved it: `accesslog_emit.go:57:7: undefined: entry`. V2 found it as a self-contradiction: §6/§10 declared `connection.go`/`h3dispatch.go`/the 18 callers BYTE-UNTOUCHED "under BOTH seams" while §2.4's seam-B mechanism requires reading `entry` in exactly those files. | **SCOPED DOWN everywhere.** §2.4 now opens with the correction; §1.4 sizing is seam-conditional; §1.5, §6, §10, §11 corrected; §2.1's "convergent refutation from disjoint evidence" REPLACED by what actually converged (the SOURCE half only — the cluster is reachable without a `routeAction` method). **The pick SURVIVES: the row is SMALL on seam A**, which V1 built green. Also corrected in the ROADMAP row + charter sentence, STATE.md, next-prompt.txt and the commit message, since the claim had already propagated to all five. |
| 2 | **SEVERE** (V1) | **§2.4 asserted parity on the `500` arm, where envoy-go emits NO SPAN AT ALL** — the exact class phase 72's V1 forced retracted. The synthetic-500 sites are PRE-Decide with a nil `traceDecision` (`connection.go:459-460`, `h3dispatch.go:210`, `h2dispatch.go:164-166` all say so in landed comments). §7's D-CTMC-NOCLUSTER correctly warned against this while §2.4's prose reverted to it — the two sections disagreed. | `500` REMOVED from the list; seam B's advantage narrowed to the **post-Decide upstream-ACQUIRE-failure class** (+ span-capable filter-generated local replies), with the `N/A — no counterpart` class named explicitly and the SPAN-CAPABILITY partition restated. |
| 3 | MODERATE (both) | **"ZERO new imports" is FALSE for seam A's `manager.go` edit** — a mirror-form RECURRENCE of phase 72's own folded finding #2. The claim was checked against `cluster.go` and generalized. `manager.go` imports neither `structpb` nor `corev3`; V1's build: `manager.go:863:105: undefined: structpb`. | §3.3 and §6 corrected to ONE new import in `manager.go`, with `*clusterv3.Cluster` named as the zero-import carrier. **`+0 go.mod modules` SURVIVES** (V1: `go mod tidy -diff` EXIT 0). |
| 4 | MODERATE (both) | **§11.3 item 2's drift "correction" was ITSELF WRONG.** `route.go:52-59` was the CORRECT incoming cite; the interface closes at `:59` and `:60` is blank. `reference_a_drift_correction_is_itself_a_claim` firing on a document that cites that memory. | Item 2 RETRACTED in place and restated as an EXACT-HOLD; drift tally 6 → **5**. |
| 5 | MODERATE (both) | **§11.2 — the controller's own "re-derivation" table carried TWO drifted cites while certifying them HOLDS:** `actions.go:182` is the struct header (field is `:183`), and `dispatchRequest` spans `:312-780` not `:312-784`. | Both corrected in the table and at every propagation site; a warning added to §11.2's preamble that *a verification pass can launder a wrong cite into apparent authority*. |
| 6 | MODERATE (V1) | **"a WIDER departure than B2 is for HOST"** is unsupported in EXTENT — seam A fires on exactly B2's site set, both gated on `picked.IsZero()`. | Breadth claim RETRACTED; restated as **the same breadth with a materially worse justification**. (Phase 72 shipped one over-broad departure claim that had to be retracted; this one was caught pre-landing.) |
| 7 | MODERATE (V2) | **§11.4 was MISSING and §12 created no obligation to add it** — the lineage's BRAINSTORMs carry a verifier-fold section, and this session's two verifiers had no declared home in the landed document. | **This section**, plus a new §12 bullet making the fold a declared stage-close mechanic. |
| 8 | MODERATE (V2) | **§1.5's file roster contradicted §2.4/§6**, listing `accesslog_emit.go` as seam-B-only when it is touched under BOTH seams, and omitting `connection.go`/`h3dispatch.go` from seam B. | §1.5 rewritten. |
| 9 | MINOR (V1, ×4) | §2.1 cite drift: `descend`/`structpbValueToString` cited as `resolve.go:145`/`:168`, which match **no commit in the lineage** (actual `:172`/`:195`; V1 walked all four); `alsHeaderCaptureUnion`'s type-assert is `config.go:403` not `:376`; the stdout fall-through is `:952-956` not `:955-958`; "two lines below" is six. | All corrected in place. |
| 10 | MINOR (V2) | **§2.7 listed the fuzz-seed order wrong** — `meta_bad` is LAST (`:117`), not third — **and the order is load-bearing to that section's own dispatch-verify trap argument.** | Corrected, with line numbers, and the self-undermining noted. |
| 11 | MINOR (V2) | **"proved BY EXECUTION" overstated** the `access_log[].filter` evidence: what was executed is envoy-go-side only; the reference half was never probed (§11.1 says so). | Rescoped to "the envoy-go half BY EXECUTION … the divergence is strictly INFERRED end-to-end". |
| 12 | MINOR (V2, ×2) | §2.6 never cited `reference_envoy_lb_namespace_not_privileged` though next-prompt.txt names it load-bearing for the fixture; §2.1's provenance list said phase-72 "§8" where it is also §11. | Both added/corrected. |
| — | INFORMATIONAL | V1 **BUILT SEAM A** in private scratch: `gofmt` silent · `go build` 0 · `go vet` 0 · `go mod tidy -diff` EMPTY · layering check clean (no `internal/filter`, no `internal/cluster` edge) · red set **EXACTLY TWO** `/cluster` subtests printing **`Kind:6`** (⇒ `iota == 6` by execution) with both unset-kind rows still PASS · `unsafe.Sizeof(Endpoint)` **112 → 120** · seven files sha256-identical to baseline. **The real `resolve_test.go` caller count is 49, not the raw 50** — `:107` is a `t.Errorf` string literal, exactly the phase-72 `:106` trap; V1's patch of those 49 built clean. V2 verified all SEVEN §12 declarations performed, the ROADMAP pure-append and byte-identical deferred sentences by sha256, the ADR-0288 singleton rule, the lineage preamble (the phase-72 stale-preamble defect does **not** recur), the sentinel, every count, the ordinal (mechanically **20**, exactly one row — 69 — carrying no ordinal word), and that **all 59 memory names cited across the three files EXIST**. | Recorded; the 49-vs-50 figure and the `Kind:6` proof are carried into §6 and the SPEC's owed roster. |

**What NEITHER verifier could check, stated plainly:** **no Docker probe was run by anyone this session.** Every reference-side behavior behind D-CTMC-SEAM/WIRE/ZIPKIN-WIRE/VALUE-SERIALIZE/DEFAULT/NOCLUSTER/REJECT is UNVERIFIED — including the anticipation that the reference resolves CLUSTER at route-selection time, which is the very thing that would favor seam B. Also unchecked: the "13 reject arms" PGV claim (envoy-go runs no PGV), the reference half of the `runtime_key` parity argument, and — the one seam-A risk that remains genuinely open — **the endpoint-provenance obligation** (V1's build proves compile + unit-test green; it does NOT prove the new field survives a connection-pool HIT).

---

## 12. Stage-close mechanics (this BRAINSTORM; the CONTROLLER executes these, not this document)

- **Row 73 registered `in-progress`** in ROADMAP §Schema at the stage-close commit (a new subject re-opens sentinel check (1)). Flips `done` at the phase-73 IMPL six-gate (ADR-0106, the SOLE leg — a SINGLE FLAT ROW, `reference_roadmap_split_phase_row_done`).
- **ROADMAP takes a SECOND edit at the same commit: a phase-73 charter sentence APPENDED to the `### Observability family` heading paragraph** (naming the row, its anticipated envelope, the stale-cost correction, the newly-surfaced deferred candidates, and that the family STAYS OPEN). Declared here because declared mechanics must match performed mechanics — the phase-70/72 precedent. The edit is ADDITIONS ONLY and the live Observability **deferred sentence is BYTE-UNCHANGED** (it narrows at the IMPL, not here); sentinel check (2) must still return exactly **3**.
- **NO deferred sentence is narrowed at the BRAINSTORM** (the phase-57 precedent). The `CLUSTER` candidate rolls OUT at the **phase-73 IMPL row-done edit**. ⚠️ **§6 flags a real hazard for that future edit:** `CLUSTER` is the LAST item in its clause, so the narrow REMOVES the clause rather than shortening it — the IMPL must confirm the sentence still parses and still carries at least one candidate, or check (2) changes count.
- **DECISIONS UNTOUCHED** (ADR-0295 §Context drafts at the SPEC per ADR-0044).
- **STATE §Current rolled IN PLACE** (lifecycle DONE → 1; lineage re-capped at five **with its PREAMBLE updated** — the ADR-0288 rule; a stale preamble was a REAL defect caught during phase 72). Never prepend a new block above §Current.
- **next-prompt.txt rolled to the phase-73 SPEC** (TRACKED despite `.gitignore`; edit it in the stage worktree — `reference_next_prompt_tracked_despite_gitignore`).
- **Sentinel re-run MECHANICALLY at close** (does NOT fire; `stop` NOT created): (1) prints `NOT DONE: row 73` (the registration re-opens check (1)); (2) ⇒ **3**; (3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`. Checks (2)+(3) print ⇒ abundant work remains. (Check (3)'s `WASM` line is the bookkeeping artifact documented in §2.1 — left AS-IS.)
- **The adversarial record is FOLDED, not deferred** (§11.4). Two independent verifiers ran against the committed draft in PRIVATE scratch (`reference_parallel_subagents_private_scratch`) BEFORE the squash-push; **both found SEVERE defects**, and every finding is folded at its point of use with the ledger in §11.4. This bullet exists because an earlier draft of §12 listed no such obligation — V2 M4.
- **Next → the phase-73 SPEC** (the D-CTMC-* live-probe arms against `envoyproxy/envoy:contrib-v1.37.2`; RE-DERIVE every §6 edit site at the SPEC tip; **resolve D-CTMC-SEAM, the load-bearing one, on PROBED reference behavior rather than diff size**; draft ADR-0295 §Context).
