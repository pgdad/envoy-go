# PROGRESS 73 — tracing `custom_tags` `metadata` type, `CLUSTER` MetadataKind only

> Live task ledger for the phase-73 IMPL. The PLAN (`PLAN.md`) is the spine; this file records what ACTUALLY happened per task — red-first verbatims, WHICH break assertion fired (and any that did NOT), substitutions **and whether their stated rationale survived scrutiny**, T1's Break D result (the placement guard), T4's two-part envelope verification, T5's arm-tagged dispatch-verify output, T6's partition-mechanism choice, and the six-gate evidence. **The Task ledger's Status/Commit columns are populated at the IMPL, not the PLAN.**

## Stage pointer

- **PLAN done** (2026-07-24; flipped only AFTER the §Adversarial-pass record below was populated from the verifiers' actual reports — writing "done" over an open gate is the very class the PENDING banner guarded against) — the 9-task SINGLE-FLAT-ROW TDD spine (T1–T9) landed; the SPEC §3/§9/§10/§11/§12/§14 anchors RE-DERIVED at `79f96455` (**ZERO DRIFT and ZERO REFUTED on every CODE anchor; NINE new findings F1–F9, three of them stale-claim carriers the SPEC leaves unassigned and two of them `old_string` uniqueness hazards**) by TWO independent read-only re-derivation agents on disjoint remits, plus controller re-verification of every load-bearing correction (`reference_a_drift_correction_is_itself_a_claim`) — agent A over `internal/cluster/{cluster.go,manager.go,subset.go,locality.go,priority.go,health.go}` + `internal/tracing/{config.go,resolve.go,config_test.go,resolve_test.go}` + `internal/filter/hcm/{accesslog_emit.go,connection.go,h2dispatch.go,h3dispatch.go,fuzz_test.go}` + the emit test callers + the collision greps + the counts; agent B over `BEHAVIOR_CONTRACT.md` + `ROADMAP.md` + `DECISIONS.md` + `STATE.md` + `next-prompt.txt` + the memory roster + a **scratch-copy APPLICATION of the §12 narrow**. Fresh worktree `.worktrees/phase-73-plan` off master `79f96455` (**== the phase-73 SPEC squash; NO parallel stream** — `git diff --stat d51d33dc 79f96455 -- '*.go' go.mod go.sum test/` is EMPTY, so the production tree is byte-identical to what the SPEC re-derived), branch `phase-73-tracing-custom-tags-metadata-cluster-plan`. Row 73 STAYS `in-progress`; **DECISIONS UNTOUCHED at the PLAN** (ADR-0295 stays PROPOSED, §Context only — verified 1 `### Context` / **0** `### Decision` / **0** `### Consequences` inside the block, so T9's APPEND is sound); **NO deferred sentence narrowed at the PLAN** (it narrows at the IMPL row-done edit, the phase-57 precedent); ROADMAP UNTOUCHED.

- **IMPL done** (2026-07-24) — **ROW 73 FLIPS `in-progress` → `done`** at the T8 six-gate (ADR-0106, the SOLE leg — a SINGLE FLAT ROW); **ADR-0295 COMPLETED IN PLACE** (§Decision + §Consequences APPENDED after the RETAINED hand-off footer; tail stays ADR-0295, next-free ADR-0296). Fresh worktree `.worktrees/phase-73-impl` off master `bea4178c` (the phase-73 PLAN squash), branch `phase-73-tracing-custom-tags-metadata-cluster-impl`; subagent-driven, subagents committing LOCALLY only, controller squash-push at close. Seven task commits + this docs close. **Six-gate ALL GREEN; the FULL 119-dir differential `ok 410.259s` EXIT 0, ZERO failures.** **EVERY break fired — NOT ONE failed to fire** (§Break ledger below). **THREE of the PLAN's own instructions were REFUTED BY EXECUTION** (§PLAN refutations below), and they are recorded rather than quietly fixed.

## Adversarial-pass record (PLAN §1.4)

**STATUS: COMPLETE** — populated at the PLAN close from the verifiers' ACTUAL reports. Full ledger in `PLAN.md` §1.4; the summary and the two consequential findings are mirrored here.

Two verifiers, disjoint remits, both in PRIVATE scratch (`reference_parallel_subagents_private_scratch`), both against this branch's tip. **V1 (code claims + by-execution; built T1→T4 from §1.2's skeletons pasted VERBATIM): BUILDABLE AS WRITTEN — 1 severe, 1 moderate, 3 minor. V2 (process, consistency, SPEC-coverage, stage-close mechanics): PASS WITH FIXES — 1 major, 1 moderate, 6 minor.** **Neither found any drift in the SPEC→PLAN code anchors.** Every load-bearing correction was RE-VERIFIED by the controller at the PLAN tip before folding, and one further defect (**S6**) was found by the controller during that re-verification and folded too.

**The two that changed the IMPL's instructions:**
- **S1 (V1, SEVERE)** — `manager_test.go` was scheduled for four test additions AND sha256-gated BYTE-UNTOUCHED in six places: an unsatisfiable gate, the identical defect this PLAN already diagnosed for the emit test files but never applied to itself. ⇒ **T1's four manager-side tests now land in a NEW `internal/cluster/cluster_meta_test.go`** (collision-checked); the sha256 gate is KEPT and V1 executed the routing (`manager_test.go: OK`).
- **S2 (V1, MODERATE)** — T1 test 3's suggested `Cluster.PickEndpoint()` mechanism leaves **Break D GREEN**, i.e. the row's distinguishing break would have been VACUOUS. `PickEndpoint` passes `hasMatch=false` (`cluster.go:531-532`), so `subsetLB.Pick` skips `s.subsets` and falls back to `factory(endpoints)` over the ORIGINAL slice (`subset.go:183-185`) — aliased, invisible. ⇒ **test 3 is now pinned on the package-internal `cl.lb.(*subsetLB).Pick(…, hasMatch=true)` form**, which needs zero new exported symbols.

Also folded: **M1 (V2, MAJOR)** — the PLAN claimed check (1) does NOT go silent at this row's IMPL close; REFUTED (row 73 is the LAST non-`done` row). **M2** — the PLAN-close sentinel runs TWICE, not once. **S3** `subset.go:164`→`:168`. **S4** T2's reject rows must be RETAINED through Steps 2-3 or the `Kind:6` red cannot fire. **S5** skeleton (e) needs `gofmt -w` before any `gofmt -l` gate. **S6** RD-PLACEMENT said "FOUR retain a value copy" while naming three constructors ⇒ **three constructors, four copy sites**. Plus V2's m3-m8 and the F1 citation precision (§16 **F2**, not M1).

⚠️ **NOT verified — the IMPL must not inherit false confidence.** V1 ran **nothing reference-side** (no Docker, no Envoy probes): every reference/PGV claim rests on the SPEC's own probe record. **T5-T7 and T9 were not executed**; **Breaks N/O/P/Q/R are untested**; the `0117` fixture does not exist yet and **T6 Step 1's `http.url` partition reconnaissance is unaddressed — it remains the IMPL's first live risk**; the full differential suite was not run; Break M2's firing at span level is unproven (only its compile-clean half, which is the load-bearing half).

## Task ledger (Status/Commit filled at the IMPL)

| Task | Status | Commit | Red-first / breaks / notes |
|---|---|---|---|
| T1 `internal/cluster` (`clusterFilterMetadata` + `ClusterMetaLookup` + the placement-pinned `buildCluster` loop) | **DONE** | `f6f3cede` | **Original red/design notes:** Step 0 re-runs the FOUR collision greps at the IMPL tip (all 0 at the PLAN tip) **plus the parallel-stream check**. Red: `undefined: ClusterMetaLookup`/`clusterFilterMetadata`. Field ALIASES `c.GetMetadata().GetFilterMetadata()` (zero new alloc, sizeof 112→120 expected — record the ACTUAL); accessor nil-safe on the ZERO `Endpoint`. **ZERO new imports** (`structpb` already at `cluster.go:21`) — a genuine difference from phase 72. ⚠️ **The LOOP goes IMMEDIATELY after the `:433-436` guard, BEFORE `:437`** — `buildLeafLB` (`:472`) ALIASES the slice so round-robin would stay green, while `newSubsetLB` (`:481`), `newLocalityWeightedLB` (`:516`) and `newPriorityLB` (`:550`) each `append(…, ep)` BY VALUE. `:754`/`:883`/`:884`/`:885`/`extractEndpoints`'s signature/`manager_test.go` BYTE-UNCHANGED. ⚠️ **The four manager-side tests go in a NEW `internal/cluster/cluster_meta_test.go`, NOT `manager_test.go`** — that file is sha256-gated byte-untouched, and adding to it makes its own gate unsatisfiable (V1 S1). Breaks **A** (populate dropped) / **B** (wrong source — the CLUSTER-vs-endpoint distinctness made executable) / **C** (nil guard — **assert on the ACCESSOR'S OWN RETURN, not a span attribute, or it is VACUOUS**) / **D** (**THE ROW'S DISTINGUISHING BREAK** — move the loop after `:481`; the subset-LB assertion must FIRE while the other tests STAY GREEN; **if it does not fire, test 3 isn't reaching a subset-copied endpoint and that is a FINDING, not a pass**). ⚠️ **Break D is VACUOUS through `Cluster.PickEndpoint()` — V1 EXECUTED it GREEN.** Reach the subset endpoint via the package-internal `cl.lb.(*subsetLB).Pick(0, false, NewSubsetMatch(…), true)`; only `hasMatch=true` traverses `s.subsets`. Prove ALIASING mechanically. `-race` (⚠️ pre-existing `outlier_test.go:766` flake — isolate-re-run, do NOT re-classify). |
| T2 config parse (`kindMetadataCluster` + the CLUSTER accept arm) | **DONE** | `1e74f996` | **Original red/design notes:** red is `undefined: kindMetadataCluster`, then — ⚠️ **only if the two `/cluster` reject rows are RETAINED through Steps 2-3 and converted AFTERWARDS (V1 S4); converting them first makes the red unfireable** — **EXACTLY TWO** `/cluster` subtests — TABLE ROWS `config_test.go:639`/`:764` (⚠️ **BYTE-IDENTICAL to each other, `grep -Fxc` ⇒ 2 ⇒ an `old_string` on either alone is NOT unique**), failures printing at `:648`/`:773` — **and the output should print `Kind:6`, the by-EXECUTION proof of iota==6**. APPEND after `kindMetadataHost` (`:61`), never insert. ⚠️ **NOT a pure-append diff — gofmt re-aligns `:58-61`'s trailing comments (19-char name vs the 17-char column-setter); that shift is EXPECTED, not damage.** Branch on `k.GetCluster()` on the bound `k` (`md.GetCluster()` does not exist). Anchor the lift on `:267`+`:268-269`+`:270` — the three accept bodies are near-identical. Both unset arms STAY reject. Narrow the stale comments incl. `:185` **and `:190` (F3)**, whose "ADR-0080-distinct substrings" claims go false because `:233` and `:288` now COLLIDE on `kind required` — **a documented boundary, deliberately NOT split**. Breaks **E** (accept→reject; `[NOT pre-compiled]` — the naive form leaves `dv` unused) / **F** (kind confusion — TWO independent properties, confirm BOTH) / **G** (silent renumber — the ONLY assertions that catch it) / **H** (HasDefault) / **H2** (the `[1:]` slip at PARSE time). `grep -rn "cluster kind unsupported"` ⇒ 0 hits. |
| T3 resolve arm (`clusterMetaLookup` 6th param + the `kindMetadataCluster` arm) | **DONE** | `91f354d8` | **Original red/design notes:** red is the `ResolveCustomTags` arity mismatch. **49** real `resolve_test.go` callers += trailing `nil` — ⚠️ **raw grep returns 50; `:107` is a `t.Errorf` STRING LITERAL and `:106` is its real call; GATE BY EXPLICIT LINE NUMBER, not a bare regex, and re-derive the list at the IMPL tip.** Arm inserts between `:160` and `:161`, cloning `:131-160`. **FULL `MetaPath`, NOT `[1:]`**; the `request_header` default rule (present-empty EMITS `""`); `descend` (`:172-186`) / `structpbValueToString` (`:195-212`) reused VERBATIM — **prove by sha256**. The `:12-45` doc gains a CLUSTER clause. NO new import. Breaks **I** (wrong slice — THE CRUX; every REQUEST/ROUTE/HOST subtest must STAY green) / **J** (default rule — the two rules predict DIFFERENT observables) / **K** (nil-tolerance — **COMPILES, no substitution needed**) / **K2** (5th/6th crossed — ⚠️ **ADJACENT and IDENTICALLY TYPED, so the compiler CANNOT catch it**; the source-distinctness test is what makes it catchable). `-race`. |
| T4 the three emit call sites (`picked.ClusterMetaLookup`) | **DONE** | `97428088` | **Original red/design notes:** ⚠️ **Entry state: `internal/filter/hcm` does NOT compile** — three `not enough arguments in call to tracing.ResolveCustomTags` at `:57`/`:118`/`:179`. That is the **`ResolveCustomTags` arity** changing exactly as in 70/71/72 — **do NOT conflate it with the EMIT-signature arity, which is genuinely unchanged and IS this row's cost claim** (phase 72's V1 M2 caught exactly this conflation). Quote the line:col from the ACTUAL run; the columns shift. ⚠️ **`:57` and `:179` are BYTE-IDENTICAL (F2) — edit by line number or with context; verify 3 occurrences afterwards.** **THE TWO-PART ENVELOPE GATE, and the parts are NOT interchangeable:** (a) **sha256 byte-identity on SEVEN production files** — `connection.go`, `h2dispatch.go`, `h3dispatch.go`, `chain.go`, `subset.go`, **`actions.go`**, **`route.go`** (the last two are exactly what the REJECTED route-carried seam would have touched — the mechanical proof the seam decision was honoured); (b) **CALLER-SET invariance, NOT a hash**, on `accesslog_emit_test.go` (**13**) and `span_emit_test.go` (**21**) = **34**, since this task ADDS tests to both — ⚠️ **use `grep -cE 'emitAccessLog(H2\|H3)?\('`; the naive `grep -c` returns 23 for `span_emit_test.go` (F4)** — plus unchanged signatures and a `diff` yielding ZERO `<` lines. Tests: live per-site H1/H2/H3 CLUSTER-span tests via the POPULATE PATH (`NewManager`→`Get`→`PickEndpoint()`; **no test constructor — the envelope allows exactly ONE new exported symbol**), a first-class zero-`picked` test (**the 5 span-capable zero sites, NOT the raw 10/11**), and a CLUSTER-vs-HOST distinctness test. Breaks **L** (per-site) / **M** (wrong lookup) / **M2** (5th/6th crossed at the call site — the compiler cannot catch it). `-race`. |
| T5 fuzz seed ADD + the MANDATORY `meta_bad` REPOINT | **DONE** | `ba92316c` | **Original red/design notes:** ⚠️ **`withMetaTags` is a VARIABLE (`:99-122`), not a function.** ADD `meta_cluster_ok` cloning the `meta_host_ok` block (`:112-116`). **REPOINT `meta_bad` (`:117-119`) to `Kind: &metadatav3.MetadataKind{}`** — present kind, oneof unset, the `config.go:288` **PARITY** arm. ⚠️ **The repoint is MANDATORY for a sharper reason than "it would stop rejecting": as-is the seed carries a CLUSTER kind and NO `MetadataKey` at all, so post-lift it falls THROUGH the accept arm and rejects SILENTLY on `empty namespace` — which is itself the newly-discovered absent-`metadata_key` DEPARTURE, converting a parity-reject seed into a departure-shaped one. And the BRAINSTORM's proposed nil-`kind` target is the OTHER departure (`config.go:233`).** Narrow the stale `:89-98` comment (false in THREE places) **without disturbing the adjacent dispatch-order sentence** (phase 72 over-edited this exact block once and reverted). ⚠️ **Dispatch-verify BOTH arms with the arms TAGGED SEPARATELY — `:233` and `:288` now emit the IDENTICAL string `kind required`, so the error text alone would launder the result.** The first-error-return trap: `meta_bad` is LAST, so seeing its line at all proves the earlier seeds did not short-circuit. Count 55→55 (+0). NO corpus artifacts. |
| T6 fixture `0117` (OTLP, TWO CLUSTERS, NO writer) | **DONE** | `849632c0` | **Original red/design notes:** the `0116` chassis with **TWO clusters** (`c_backend_a`/`c_backend_b`) carrying DIFFERENT `metadata.filter_metadata` **on the CLUSTER, not on `lb_endpoints[]`**, both pointing at the **SAME single backend**, behind `prefix: /a` and `prefix: /b` routes ⇒ deterministic by PATH, zero LB spread, `BackendCount` **1**. **This DISCRIMINATES *selected* from *only* — the limitation `0116` had to confess — so `expectations.yaml` must state that positively rather than confess a limit.** Two CLUSTER tags (`cl_hit`/`cl_default`), cross-side EXACT key+value, **KEY-BASED** (intra-tag order is arbitrary **across processes**). **STRING values only** (arbitrary reference struct-key order; top-level scalars render ~6 sig-digits, **biting from `1000000`**; nested numbers use a DIFFERENT renderer). Non-`envoy.lb` namespace. ⚠️ **THE NEW DRIVER PROBLEM — per-path span partitioning. `upstream_cluster` is UNUSABLE** (envoy-go emits it EMPTY; `0116` asserts it present-only). **PRIMARY: partition by `http.url` — but `0116` asserts that `assertAttrPresent` only because its value "varies by host/path encoding", so Step 1 must DUMP it on both sides and confirm the path is recoverable BEFORE any assertion depends on it. NAMED FALLBACK: two drive batches with a poll barrier.** Record which was used and why. ⚠️ Keep `numTotal` at **12** (split 8→4+4, 4→2+2) so the span-count and `spans_sent` assertions stay consistent (`reference_fixture_workload_constant_desync`). Registration at `runner_test.go:144`. Breaks **N** (value swap, ONE side) / **O** (default drop — must fire PRESENCE with ZERO value-mismatches) / **P** (**anti-vacuity control**) / **Q** (**THE SELECTION BREAK — the only one that proves *selected*, not *only*; if it fires on one path only, the partition is broken and that is a FINDING**) / **R** (source relocation — discriminates CLUSTER from HOST). Fixtures 118→119; port 10117. |
| T7 BEHAVIOR_CONTRACT B1–B5 | **DONE** | `b9225e08` | **Original red/design notes:** ⚠️ **B1 and B2 EDIT THE SAME PHYSICAL LINE (`:688`, one 3866-char paragraph; `grep -n 'unset-kind'` ⇒ exactly ONE hit) and MUST be applied as ONE `old_string`.** ⚠️ **B3's target `:690` is a SEPARATE physical line (`:689` is blank) and is its OWN `old_string` — do not merge it into a three-way edit.** B1: `metadata` CONSUMED for **all four** MetadataKinds; the CLUSTER source over `cluster.Endpoint.ClusterMetaLookup()`; three of four kinds walk the FULL path. ⚠️ **The replaced sentence's first half is FALSE and its second half is PARTLY false — `empty-namespace` is a DEPARTURE in its absent-`metadata_key` spelling, so the PGV-PARITY list must be SPLIT, not trimmed at the front.** B2: the roster narrows to **TWO reject ARMS across THREE config spellings** — ⚠️ **"THREE arms" is the refuted framing (F1)** — plus the two documented error-string collisions, NEITHER fixed. B3: EXTEND (not duplicate) the phase-72 clause to a FIFTH consumer; RETIRE the "materially worse justification" framing; move circuit-breaker to **PARITY** — ⚠️ **scope it exactly: reference EXECUTED (S8), envoy-go RE-DERIVED, not cross-side probed** — leaving pool-overflow the sole unprobed arm. B4 (`:692`): three→**four** kinds; Zipkin empty-drop proven GENERIC; scalar precision re-quantified; emit order arbitrary PER PROCESS. B5 (`:745`): narrow to the three unset/absent arms. ⚠️ **`upstream_cluster` must NOT appear in the new text** — a different feature. RE-DERIVE all four line numbers at the IMPL tip. |
| T8 verify (six-gate + envelope) | **DONE** | *(no commit — gate run at the frozen HEAD)* | **Original red/design notes:** gofmt / vet / build / `go mod tidy -diff` EMPTY + `go.mod`/`go.sum` no-diff (**+0 modules — ZERO new imports anywhere**) / lint + the **FULL 119-dir** differential + the layering check (`go list -deps ./internal/tracing`, **no `...`**, filter- AND cluster-free — ⚠️ **a DISCIPLINE check, NOT a cycle check; do not re-introduce the refuted "cycle guard"**) + `-race` on the 3 touched pkgs (⚠️ `internal/cluster` load-bearing; `outlier_test.go:766` PRE-EXISTING) + counts + the envelope audit **pinning the SHAPE for `internal/cluster`** (one field + one accessor + one loop + **no import hunk**). ⚠️ **The 18-caller re-derivation must exclude `h2dispatch.go:188` (a DOC COMMENT) — a raw `grep -c` reads 5+7+7=19; h3's seven are ALL REAL, only h2 is inflated.** Exactly **ONE** new exported symbol by AST symbol-set diff. **Confirm the deferred T1 doc-comment claim became TRUE** (three `picked.ClusterMetaLookup` at `:57`/`:118`/`:179`). ⚠️ **Use `git -C <abs-worktree-path>` and tripwire `pwd` + branch + commit-count before every gate** — at the phase-72 close a cwd reset made a "final gates" run validate master's pre-row tree, GREEN while proving nothing. **ROW 73 FLIPS `done` at this gate.** |
| T9 ADR-0295 + close | **DONE** | *(this docs+memory close)* | **Original red/design notes:** ADR-0295 COMPLETED IN PLACE — **APPEND** §Decision + §Consequences after the `:17229` footer (**RETAIN it**); **NO second `### Decision` heading** (verify 0 before / 1 after); no renumber; tail stays ADR-0295, next-free ADR-0296. ⚠️ **FIX THE ADR-0295 HEADING (F1) — a FOURTH stale claim that SPEC §16 asserts was already fixed and was NOT:** `:17211` still carries *"manufactured **three divergence classes**"* (it is **ONE** expressible class) and *"**THREE arms, not two**"* (the unit is **TWO ARMS / THREE SPELLINGS**); the body at `:17225` is already correct, so the heading is the sole surviving carrier. ⚠️ **CORRECT ADR-0294 `:17209` (F6)** — its refuted *"the one that RE-PAYS the 18-caller threading tax"* closing claim, using ADR-0294's OWN `:17185` bracketed-correction precedent (or decline IN WRITING). ROADMAP row 73 → `done` + the pinned `:203` narrow (**re-verify `count == 1` first**; check (2) STAYS **3**, FOUR candidates remain, drop the bare `custom_tags` prefix) + the **THREE** stale row-cell claims at `:135` (field count must stay **8**). STATE §Current IN PLACE + the lineage re-capped at five **with its PREAMBLE (`:42`) updated** (⚠️ the ADR-0288 singleton greps read **2**, not 1 — the extra hit is `:7`, the RULE STATEMENT; **never "fix" it to 1**). Router roll (⚠️ **check (1) DOES go silent when row 73 flips — it is the LAST non-`done` row, 100 `done` + 1 `in-progress` among the 101 rows check (1) can SEE; 103 rows total, the 2 it cannot see (00, 04) both `done`; that is the NORMAL post-IMPL state and is NEVER on its own a termination condition. Checks (2)+(3) still print ⇒ the sentinel does NOT fire and `stop` must NOT be created. Record the ACTUAL output.**). **Sentinel re-run TWICE — in the worktree AND on LANDED master after the squash-push.** Memory: ⚠️ **both SPEC §13 obligations are ALREADY DISCHARGED — verify, do not rewrite**; what IS owed is **F7** (narrow `reference_host_tag_resolves_at_pick_time`: circuit-breaker is now EXECUTED-PARITY, pool-overflow alone unprobed) plus a candidate memory for the populate-placement hazard. |

## Findings carried from the PLAN (RE-DERIVED at `79f96455`; RE-VERIFY at the IMPL tip)

**Every finding below is itself a claim** (`reference_a_drift_correction_is_itself_a_claim`) — re-run the grep before any of them becomes an `old_string`.

- **F1 (MODERATE) — the ADR-0295 HEADING still carries BOTH refuted claims, and SPEC §16 says they were fixed.** The body (`:17225`) was corrected; the heading (`DECISIONS.md:17211`) was not. Both substrings occur exactly once document-wide, both there — and **zero times in `SPEC.md` or the SPEC commit message, so the heading is the sole surviving carrier REPO-WIDE.** ⚠️ **Cite §16 F2 ("the unit is now pinned once … and propagated"), not §16 M1** — M1's "corrected in both places" plausibly means SPEC §3.3 + ADR-0295 §Context, and the heading is not literally §Context, so M1's letter is defensible while F2's is not. ⇒ **T9, a FOURTH correction beyond §12's three.**
- **F2 (MODERATE) — `accesslog_emit.go:57` and `:179` are BYTE-IDENTICAL** (`grep -Fxc` ⇒ 2). The SPEC flags this hazard for `config_test.go:639`/`:764` but misses it here, and T4 edits all three sites. ⇒ **T4.**
- **F3 (MINOR) — an EIGHTH stale production comment, `config.go:190`** (`All substrings are ADR-0080-distinct.`), outside the SPEC's 9-site roster; false for the same reason `:185` is. ⇒ **T2.**
- **F4 (MINOR) — the emit-test caller gate needs the GATED regex.** Naive `grep -c 'emitAccessLog' span_emit_test.go` ⇒ **23**, not 21 (`:4`/`:5` are doc-comment prose). ⇒ **T4, T8.**
- **F5 (MINOR, two parts) — TWO SPEC warnings are STALE-BY-ONE-STAGE; NO edit is owed.** (a) SPEC §12:361 says the stale narrow-hazard sentence lives in `STATE.md`'s LIVE `next-skill:` block — **it does not**; only `:46` (HISTORICAL) carries it, and `:18` already carries the CORRECTED form. (b) SPEC §16 F6 cites `next-prompt.txt:70` — **`:70` is EMPTY**; the phrase is at `:50` inside an explicit `**REFUTED**` warning. Recorded so no stage "fixes" already-fixed text.
- **F6 (MINOR) — ADR-0294 `:17209` (COMPLETE) still ends on the claim this row refutes** (*"the one that RE-PAYS the 18-caller threading tax this row skipped"*). Named by no SPEC section. ⇒ **T9** (bracketed correction, per ADR-0294's own `:17185` precedent — or an explicit written decline).
- **F7 (MINOR) — ONE memory write IS owed, and it is neither of the SPEC §13 names** (both of which are **already discharged**). `reference_host_tag_resolves_at_pick_time.md:19` still says *"phase 72 left **both** UNPROBED"*; the circuit-breaker arm is now EXECUTED-PARITY (S8), leaving pool-overflow alone. ⇒ **T9.**
- **F8 (MINOR) — SPEC §5's "51 naive `MetaLookup` hits" is a stale phase-72 figure** (at tip: naive 98 / `RouteMetaLookup` 29 / word-bounded 39). The RULE is right; the number is not. ⇒ **state the rule, cite no number** (the RD-LIT pattern).
- **F9 (INFORMATIONAL — strengthens the SPEC) — the populate-loop hazard's mechanism, verified rather than inferred.** ELEVEN references to `endpoints` follow `:433`; the SPEC's four-constructor list is exactly the set that RETAINS a value copy. **`buildLeafLB` retains the slice HEADER (`roundRobin{endpoints: endpoints}`, `manager.go:344`) and therefore ALIASES the backing array — which is precisely why round-robin would stay green** — while `newSubsetLB`/`newLocalityWeightedLB`/`newPriorityLB` each `append(…, ep)` by value — **THREE constructors across FOUR copy sites** (`newSubsetLB` copies in BOTH its selector-grouping loop, `subset.go:168`, AND its `fallbackDefaultSubset` branch, `subset.go:193`; V1 surfaced the second, and an earlier draft cited `:164`, which is the `if !ok {` line). `newClusterHealth` (×3) and `distinctPriorities` project only `ep.Addr()`/`ep.Priority` and retain nothing. ⇒ the pin is unchanged; **Break D targets the subset LB because it is the cheapest of the three value-copying shapes to construct in a test — but ONLY through a `hasMatch=true` pick.**

### Cross-cutting hazards (bind every task)

- **A break that does not COMPILE proves nothing**; a break that fires the WRONG assertion proves nothing either; **a substitution's RATIONALE is itself a claim** (phase 72's T4 shipped a false one).
- **A method value on the ZERO `Endpoint` is NON-nil.** Never conflate the nil-lookup mechanism with the zero-`Endpoint` mechanism — two landed comments made exactly that error at phase 72.
- **FOUR adjacent, identically-typed lookup parameters** now sit at `ResolveCustomTags`; the compiler cannot catch a crossing. Breaks **K2** and **M2** plus the two distinctness tests are the only guards.
- **Known PRE-EXISTING flakes — never reflex-classify as phase-73 regressions:** `internal/cluster` `-race` (`outlier_test.go:766`), the full-suite startup flake, the SDS dial-budget flake, and **two newly-observed unindexed load flakes** (`internal/httpclient TestOptions_ZeroValue_NoOpDefaults`, `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery`) — isolate-green, NOT root-caused.
- **Controller worktree hazard** (`reference_bash_cwd_reset_commits_to_main`): use `git -C <abs-worktree-path>` everywhere; tripwire `pwd` + branch (**NEVER `master`**) + commit count before any commit or gate run.

---

# IMPL record (populated at the phase-73 IMPL close)

## Per-task outcome

**T1 — `internal/cluster` substrate (`f6f3cede`).** Red-first: `undefined: ClusterMetaLookup` / `undefined: clusterFilterMetadata`, recorded verbatim from an actual run. Landed shape: an unexported `Endpoint.clusterFilterMetadata map[string]*structpb.Struct` field ALIASING `c.GetMetadata().GetFilterMetadata()` beside phase-72's `filterMetadata`, plus an exported nil-safe `func (e Endpoint) ClusterMetaLookup(ns string) (*structpb.Value, bool)`, populated by a LOOP in `buildCluster` placed **IMMEDIATELY after the `extractEndpoints` err guard, BEFORE ALL LB CONSTRUCTION**.
- **Aliasing PROVED MECHANICALLY, not asserted:** mutating the source proto map AFTER `buildCluster` is visible through the accessor — including a **brand-new namespace**, which proves the map HEADER itself is shared and not merely its values. A copy would have been invisible in both cases.
- **`unsafe.Sizeof(Endpoint{})` 112 → 120, MEASURED** (not projected).
- **ZERO new imports anywhere** — `cluster.go` already imported `structpb`. A genuine difference from phase 72, which added one.
- `extractEndpoints`'s signature, `manager_test.go`, `:754`/`:883`/`:884`/`:885` all BYTE-UNCHANGED; `manager.go`'s diff is **PURELY ADDITIVE (zero `-` lines)**, so those anchors hold by construction rather than by assertion.
- Test 3 drives the package-internal `cl.lb.(*subsetLB).Pick(…, hasMatch=true)` per the PLAN's V1-S2 correction, needing zero new exported symbols. **The `PickEndpoint` vacuity was RE-CONFIRMED LIVE at this tip** — under Break D a subset-configured `PickEndpoint` still returned `ok=true`, exactly as V1 predicted, so the correction was load-bearing and not merely cautious.

**T2 — config parse (`1e74f996`).** Red-first: `undefined: kindMetadataCluster`, then — with the two `/cluster` reject rows RETAINED through Steps 2-3 per V1 S4 — the pre-known red was **EXACTLY TWO `/cluster` subtests**, and the failure output **PRINTED `Kind:6`**: the by-EXECUTION proof that `kindMetadataCluster` was APPENDED at iota == 6 rather than inserted. gofmt re-aligned exactly `:58`-`:61` (+2 spaces each), as predicted — expected, not damage. The CLUSTER accept arm replaces the `:268-269` reject (the LAST kind-specific reject in the file), branching on `k.GetCluster()` off the bound `k`. BOTH unset-kind arms UNCHANGED and still rejecting. `grep -rn "cluster kind unsupported"` ⇒ 0 hits. Stale comments narrowed including `:185` **and `:190` (F3)**, whose "ADR-0080-distinct substrings" claims go false because `:233` and `:288` now COLLIDE on `kind required` — a documented boundary, deliberately NOT split.

**T3 — resolve arm (`91f354d8`).** Red-first: the `ResolveCustomTags` arity mismatch. `descend` + `structpbValueToString` reuse **PROVEN BY SHA256** (identical before/after); the landed HOST arm byte-identical at its shifted span; exactly ONE removed line in the whole diff. 49 `resolve_test.go` callers took a trailing `nil`, gated by EXPLICIT LINE NUMBER (raw grep 50; `:107` is a `t.Errorf` string literal) — **the PLAN's 49-line list held element-for-element**. FULL `MetaPath`, never `[1:]`. NO new import; `internal/tracing` stays filter- and cluster-free.

**T4 — the three emit call sites (`97428088`).** Entry state as predicted: exactly THREE `not enough arguments in call to tracing.ResolveCustomTags` — the **`ResolveCustomTags`** arity, NOT the emit-signature arity. THREE production lines landed. Two-part envelope:
- **(a) sha256 byte-identity HOLDS on the SEVEN production files** — `connection.go`, `h2dispatch.go`, `h3dispatch.go`, `chain.go`, `subset.go`, **`actions.go`**, **`route.go`** — the last two being exactly what the REJECTED route-carried seam would have touched, so the seam decision is honoured MECHANICALLY. Plus `manager_test.go`.
- **(b) the CALLER-SET half was REFUTED — see §PLAN refutations #1.** The intended invariant was verified in a STRONGER form instead.

**T5 — fuzz (`ba92316c`).** `meta_cluster_ok` seed ADDED to the `withMetaTags` seed **variable**; `meta_bad` REPOINTED to `Kind: &metadatav3.MetadataKind{}` (present kind, oneof unset — the `config.go:288` PARITY arm). **Dispatch-verified with THREE hypotheses stated BEFORE the run and the arms TAGGED SEPARATELY** (necessary because `:233` and `:288` now emit the IDENTICAL string `kind required`, so the error text alone would have laundered the result). Actual output:

```
PROBE ARM=CLUSTER_ACCEPT tag=meta_cluster_ok
PROBE ARM=DEFAULT_UNSET_KIND tag=meta_bad
```

`meta_bad` is still LAST, so seeing its line at all proves the earlier seeds did not short-circuit. **TWO hypotheses REFUTED by that run:** H-B (that `MetadataKind{}` might not survive the proto roundtrip and would hit the `k == nil` DEPARTURE arm) and H-C (short-circuit). Fuzzers 55 → 55; no corpus artifacts.

**T6 — fixture `0117` (`849632c0`).** Two clusters (`c_backend_a`/`c_backend_b`) with DIFFERENT `metadata.filter_metadata` behind `prefix: /a` and `prefix: /b`, both onto the SAME single backend ⇒ deterministic by PATH, zero LB spread, `BackendCount` 1.
- **PARTITION MECHANISM: `http.url` (PRIMARY). The named batch-poll fallback was NOT needed.** Evidence from Step 1's mandated dump: `http.url` renders **BYTE-IDENTICALLY on both sides** (`"http://trace.example/a"` / `".../b"`) across all 24 spans, partitioning **6/6 per side with ZERO unclassified**. The driver nonetheless matches by **SUFFIX, not equality**, because `0116` declined to pin `http.url` by value and this fixture does not take on that pin.
- **Partition sizes are `Fatalf` PRECONDITIONS**, so a broken partition can never render the value assertions dead code (`reference_fatalf_makes_assertions_unreachable`, applied in the direction it actually permits).
- **Live re-confirmation of the framework gap in the same dump:** the reference emits `upstream_cluster="c_backend_a"`/`"c_backend_b"`, **envoy-go emits `""`** — which is precisely why `upstream_cluster` was unusable as the discriminator.
- Fixtures 118 → 119; reference port 10117; NO writer; STRING values only; non-`envoy.lb` namespace; assertions KEY-BASED.

**T7 — BEHAVIOR_CONTRACT (`b9225e08`).** Four hunks only, in a 5730-line file; line count unchanged. B1+B2 applied as ONE `old_string` on their shared physical line; B3 as its own. Exactly ONE new `upstream_cluster` mention, inside B3(i), in reference-side phrasing — the framework gap stays a SEPARATE unchartered feature. B3 moves the circuit-breaker arm to PARITY, **scoped exactly: the REFERENCE half is EXECUTED, envoy-go's half is RE-DERIVED and NOT cross-side probed**, leaving pool-overflow the sole unprobed arm.

**T8 — six-gate (no commit; run at the frozen HEAD with `git -C <abs-worktree-path>` and a `pwd` + branch + commit-count tripwire).** gofmt SILENT · `go vet ./...` 0 · `go build ./...` 0 · `go mod tidy -diff` EMPTY + `go.mod`/`go.sum` no-diff vs master (**+0 modules**) · `golangci-lint run ./...` 0 · **FULL differential `ok 410.259s` EXIT 0, ZERO failures**, `0117` CONFIRMED to run BY NAME (`=== RUN TestDifferential/0117-tracing-custom-tags-metadata-cluster` → PASS) · layering check `go list -deps ./internal/tracing | grep -E 'envoy-go/internal/(filter|cluster)'` EMPTY (⚠️ **a DISCIPLINE check, NOT a cycle check — the refuted "cycle guard" framing is not re-introduced**) · `-race` green on all three touched packages, the pre-existing `outlier_test.go:766` flake NOT reproducing · **exactly ONE new exported symbol** by AST set-diff (`internal/cluster` 58 → 59, the single line `func (e Endpoint) ClusterMetaLookup(ns string) (*structpb.Value, bool)`; `internal/tracing` 49 → 49 with only the `ResolveCustomTags` signature growing; `internal/filter/hcm` 6 → 6) · `manager.go` diff PURELY ADDITIVE · `cluster.go` exactly 2 hunks, NO import-block hunk · 18-caller roster re-derived **5+6+7** with the gated regex · fixture tree exactly 5 files, NO `scripts/` · `git status` / `git clean -nd` EMPTY · nothing changed outside `internal/`, `test/`, `docs/`. **Counts at exit:** fixtures **119** · fuzzers **55** · stat surface **1201 (+0)** · BackendKind **38 (+0)** · go.mod modules **2 (+0)** · ZERO new packages.

**T9 — ADR-0295 + close (this commit).** ADR-0295 COMPLETED IN PLACE: §Decision + §Consequences APPENDED after the RETAINED `*(§Decision + §Consequences land at the phase-73 IMPL.)*` footer; `### Decision` inside the block **0 before, 1 after**; tail stays ADR-0295, next-free ADR-0296; STATUS flipped to COMPLETE. The **FOUR** stale claims corrected (the ROADMAP row-73 cell's three + the ADR-0295 heading's two over-claims, the heading having been the SOLE surviving carrier repo-wide). ADR-0294 `:17209` gained a bracketed `[CORRECTED at the phase-73 IMPL: …]` note per ADR-0294's own §Context precedent. The deferred sentence narrowed with `count == 1` re-verified FIRST; check (2) STAYS **3** with FOUR candidates. Row 73 → `done`, field count **8 before and after**.

## Break ledger — every break's ACTUAL firing assertion

**NO break failed to fire at any task.** Two needed a second layer or a supplement; both are recorded with their rationale flagged as a claim.

| Break | Task | Fired? | WHICH assertion fired |
|---|---|---|---|
| A (populate dropped) | T1 | YES | the BUILD-level populate assertions |
| B (wrong source) | T1 | YES — **but only as TWO breaks at TWO layers** | B1 (loop-level) fires the BUILD-level assertions while `TestEndpoint_ClusterMetaLookup` stays GREEN; B2 (accessor-level) is the ONLY one that fires test 1(e). See §PLAN refutations #3. |
| C (nil guard) | T1 | YES | the ACCESSOR'S OWN RETURN, not a span attribute — so it is not vacuous |
| **D (loop placement — THE ROW'S DISTINGUISHING BREAK)** | T1 | **YES, with EXACT discrimination** | **`SubsetChild` and `SubsetDefaultFallback` FAIL** while `RoundRobinPathStaysGreen`, populate-live, aliasing, projection and pool-HIT provenance ALL stay GREEN. Reached through the package-internal `subsetLB.Pick(…, hasMatch=true)`; the `Cluster.PickEndpoint()` route was RE-CONFIRMED VACUOUS live at this tip. |
| E (accept→reject) | T2 | YES | the CLUSTER accept subtests |
| F (kind confusion) | T2 | YES — **BOTH properties fired independently** | confirmed separately, not as one aggregate failure |
| G (silent renumber) | T2 | YES | **exactly the two iota pins and nothing else** — the only assertions that catch a renumber |
| H (HasDefault) | T2 | YES | the `HasDefault` assertion |
| H2 (`[1:]` slip at PARSE time) | T2 | YES | the parse-time `MetaPath` assertion |
| I (wrong slice — THE CRUX) | T3 | YES | the CLUSTER subtests **while the REQUEST/ROUTE/HOST half stayed ENTIRELY green ⇒ the four arms are not entangled** |
| J (default rule) | T3 | YES | the observable the two competing rules predict differently |
| K (nil-tolerance) | T3 | YES | compiles as written; no substitution needed |
| K2 (5th/6th crossed at a test call site) | T3 | YES | the source-distinctness test |
| **K2b (crossed INSIDE the production arms)** | T3 | YES — **agent-ADDED supplement** | **Rationale, flagged as a claim and VERIFIED:** K2a as written edits the very test that catches it, so it cannot cleanly isolate the guard. K2b crosses inside the production arms instead. **Both fire; the compiler is SILENT both ways.** |
| L (per-site) | T4 | YES | one call site at a time, proving each independently load-bearing |
| M (wrong lookup) | T4 | YES | the CLUSTER span-attribute assertion |
| **M2 (5th/6th crossed at the call site)** | T4 | **YES — and this is the one that mattered** | the compiler AND `go vet` AND `golangci-lint` were **ALL SILENT**; test 3 fired with the **CROSSING SIGNATURE — values TRANSPOSED (`dual_host="from-cluster"`, `dual_cluster="from-host"`)**, which is what identifies a crossing rather than a mere omission |
| N (value swap, ONE side) | T6 | YES | subject-side only ⇒ the green is not a shared-constant tautology |
| O (default drop) | T6 | YES | the PRESENCE branch with ZERO value-mismatch firings ⇒ OMIT discriminated from emitted-as-`""` |
| P (anti-vacuity control) | T6 | YES | the hit-tag value assertions, while the default tag fires ZERO |
| **Q (THE SELECTION BREAK)** | T6 | **YES — on BOTH paths, 24 firings** | ⇒ **the fixture DISCRIMINATES *selected* from *only*** — the exact limitation `0116` had to confess. Firing on one path only would have meant a broken partition, i.e. a FINDING. |
| R (source relocation) | T6 | YES | BOTH sides fall to default ⇒ CLUSTER ≠ HOST cross-side |

**Substitutions:** exactly one supplement (K2b, above) and one two-layer split (Break B, below). **No break instruction failed to compile**, so no compile-forced substitution was needed anywhere.

## THREE PLAN refutations found BY EXECUTION

1. **T4's `span_emit_test.go ⇒ 21` caller-count gate is UNSATISFIABLE BY CONSTRUCTION.** Every T4 test must drive through `emitAccessLog`/`emitAccessLogH2`/`emitAccessLogH3`, so **each new test IS a new caller**. Landed: 13 + **29** = **42**, not 34. **This is the SAME defect class the PLAN diagnosed TWICE for sha256 gates** — RD-EMITTESTS, and V1's SEVERE-1 on `manager_test.go` — **and then re-committed one level down** when it downgraded the gate from sha256 to caller-count without re-asking whether the task's own tests move the number. The invariant actually intended HOLDS, and was verified in a **stronger** form: the 21 pre-existing callers byte-identical **at UNCHANGED line numbers**, the first 779 lines hashing identically to master, and **ZERO deletion lines** in both emit test files.
2. **T1 invalidated a landed BEHAVIOR_CONTRACT citation the PLAN never flagged.** The B2 pool-overflow sentence cited `cluster.go:722` (`AcquireH1` returning the picked `ep` alongside the overflow error); T1's +23 lines shifted it to **`:745`**. Corrected in place at T7. A full sweep of every `.go:NNN` cite in that BC region found **no others stale**. *(Generalisable: a production-line insertion silently invalidates prose citations elsewhere in the repo; the sweep, not the single fix, is the obligation.)*
3. **Break B needs TWO breaks at TWO layers, not one.** The PLAN says a wrong-source populate makes test 1(e) fire; **it cannot** — 1(e) hand-constructs the `Endpoint` and never calls `buildCluster`. Executed: the loop-level break fires the BUILD-level assertions while `TestEndpoint_ClusterMetaLookup` stays GREEN; only an ACCESSOR-level wrong source fires 1(e).

## Minor, disclosed

T5's commit message embeds `config.go:233`/`:288`, which T2 had already shifted to `:244`/`:314`. Cosmetic, in a commit message only; **NOT rewritten** (rewriting a landed commit message to fix a line cite is a worse trade than recording the drift here).

## Check (1) blind spot — RE-DERIVED, and BIGGER than recorded

Re-derived mechanically at this close by set-differencing every table row against the rows check (1) actually matches: **check (1) sees 101 of 105 table rows.** The four it cannot see are `00` (an em-dash "after" column), `04` (the DOT in the slug `http-1.1`), and — **not previously recorded** — **`28.1a`/`28.1b` (a LETTER SUFFIX in the number field, which `[0-9.]+` cannot match)**. The prior "2 of 103" figure came from a set-difference whose own baseline regex ALSO excluded the letter-suffixed rows, so it under-counted its own blind spot **for a third reason**. All four rows are `done`, so there is no current impact — but check (1) is less exhaustive than documented, and any future dotted slug, em-dash cell **or letter-suffixed row number** goes invisible too.
