# SPEC 73 — tracing `custom_tags` `metadata` type, `CLUSTER` MetadataKind only (the NINETEENTH Observability-family row *(chain ordinal)* + the SEVENTH tracing `custom_tags` row + the **FOURTH and LAST** `metadata` MetadataKind — it CLOSES the quartet; lifts the `tracing: custom_tags metadata tag %q cluster kind unsupported` reject at `internal/tracing/config.go:268-269` and resolves the OWNING CLUSTER's static `metadata.filter_metadata[ns]` — carried on `cluster.Endpoint` beside the phase-72 `filterMetadata` field, reached as `picked.ClusterMetaLookup` — onto the ingress span via the shared `ResolveCustomTags`→`Span.Attrs []KV` seam [both OTLP + Zipkin]; a SINGLE FLAT ROW of NINE tasks; ONE new OTLP fixture `0117`)

> **Stage:** SPEC (lifecycle-state 1 → 2). Docs-only — **ZERO production `.go`**; the only two files touched are this `SPEC.md` and the ADR-0295 §Context append to `DECISIONS.md`. Worktree `.worktrees/phase-73-spec`, branch `phase-73-tracing-custom-tags-metadata-cluster-spec`, tip `d51d33dc` (the phase-73 BRAINSTORM squash + its router follow-up), per `feedback_git_worktrees`.
>
> **Row 73 STAYS `in-progress`** — the IMPL flips it `done` at its six-gate (ADR-0106, the SOLE leg — a SINGLE FLAT ROW, §10). **ADR-0295's §Context DRAFTS HERE** (ADR-0044-as-used); §Decision/§Consequences land at the IMPL IN PLACE. **The DECISIONS tail flips ADR-0294 → ADR-0295 AT THIS SPEC COMMIT** (§14) — the §Context append IS the tail flip; next-free becomes ADR-0296.
>
> **Evidence base — SIX independent agents ran this SPEC, three read-only and three live-probe fleets.** Code RE-DERIVED at `d51d33dc` by two disjoint agents (an anchor re-derivation over every BRAINSTORM §6 site + the proto/PGV surface + the 18-site emit roster + the collision greps; and a **by-execution costing agent that BUILT seam A end-to-end, priced seam B by execution, and refuted the hybrid**), plus a third that ran the repo-wide stale-comment sweep and the ROADMAP/BEHAVIOR_CONTRACT/DECISIONS anchor pass. **TWENTY-ONE EXECUTED live-probe arms** against `envoyproxy/envoy:contrib-v1.37.2` across three fleets on disjoint port ranges and container prefixes (a FRESH container per arm, bridge networks, the host-gateway literal + `dns_lookup_family: V4_ONLY`, a `literal` positive-control tag in EVERY bootstrap, export-actually-ran asserted per arm, every arm's discrimination stated BEFORE the run). Claims below are attributed **EXECUTED** (a named probe arm) or **RE-DERIVED** (a declaration reading with `file:line` at `d51d33dc`) or **BUILT** (a compile/test result from the scratch build); a reading is never presented as a probe. **RE-DERIVE this document; do not execute it** (`feedback_brief_citations_not_evidence`, `reference_quoting_is_not_executing`).
>
> **Baselines re-verified mechanically at `d51d33dc`:** fixtures **118** (`ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0116-tracing-custom-tags-metadata-host` ⇒ next fixture **`0117`**, and reference port `10117` has ZERO hits under `test/` — free) · fuzzers **55** (`grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`) · DECISIONS tail **ADR-0294** (COMPLETE; `grep -c '^## ADR-0295'` → **0**, FREE) · BackendKind tail **38** · stat surface **1201** · go.mod modules **2** (lineage figure).
>
> **Sentinel expectation at this stage:** check (1) prints `NOT DONE: row 73`; checks (2)+(3) unchanged (three live `candidates:` sentences; `NEVER OPENED: gRPC/Runtime/WASM`). Re-run mechanically in the worktree at this close — **it does NOT fire; no `stop`.** No deferred sentence is edited at ANY pre-IMPL stage of this row (§12).

---

## 1. Purpose / Mission

Phase 46 built the HCM-native tracing engine — the per-request ingress SERVER `Span` with its built-in STRING attributes, exported over BOTH the OTLP and Zipkin transports through the shared provider-neutral `Span.Attrs []KV` seam (ADR-0260/0261). Phases 59/62/63 added the first three `custom_tags` SOURCE types (`literal` ADR-0277, `request_header` ADR-0283, `environment` ADR-0284). Phase 70 (ADR-0292) added the FOURTH and LAST SOURCE type — the `metadata` type — scoped to **`REQUEST`**; phase 71 (ADR-0293) added **`ROUTE`**; phase 72 (ADR-0294) added **`HOST`**.

Phase 73 lands the **FOURTH and LAST `metadata` MetadataKind — `CLUSTER` — and CLOSES the quartet.** It lifts the `tracing: custom_tags metadata tag %q cluster kind unsupported` reject (`config.go:268-269`, RE-DERIVED verbatim) and resolves an operator-configured `{tag, metadata:{kind:CLUSTER, metadata_key:{key:<ns>, path:[<seg>...]}, default_value:<fb>}}` out of the **OWNING CLUSTER's static metadata** — `clusters[].metadata.filter_metadata[ns] → *structpb.Struct` — walks the `MetadataKey.path` in FULL, serializes the resolved `structpb.Value` to a STRING, and emits it as a `{tag, value}` span attribute on BOTH exporters.

**⚠️ THE ROW'S HEADLINE FINDING — the load-bearing SPEC question was answered by execution, and the answer INVERTS what the BRAINSTORM anticipated.** BRAINSTORM §2.4 framed the seam choice as a trade: seam A (endpoint-carried) is much cheaper but "INHERITS the B2 zero-`Endpoint` departure … with a materially worse justification", while seam B (route-carried) is "closer on fidelity" because "the cluster is selected by the route BEFORE any endpoint is tried". §2.4 explicitly anticipated *"the reference resolves CLUSTER at route-selection time, which would favor seam B on fidelity"* and instructed the SPEC to decide on probed behavior, not diff size.

**PROBED, that anticipation is FALSE. The reference gates a CLUSTER-kind tag on the PICKED UPSTREAM HOST, exactly as it gates a HOST-kind tag** — the two kinds move in lockstep across every failure class, and only the metadata block read differs. Three EXECUTED arms show a cluster SELECTED but **no host picked** — zero endpoints (`UH`, S5), DNS-empty (`UH`, S5b) and circuit-breaker overflow (`UO`, S8) — each falling to `default_value` **while `upstream_cluster: c_backend` is present on the very same span**, proving the cluster was known and deliberately not used as the source. ⚠️ **Of those three, only S8 DISCRIMINATES** the pick-gate from the competing "non-empty host SET" hypothesis (§3.3) — S5 and S5b both produce an empty host set and are consistent with both readings, so they replicate each other's confound rather than corroborating independently. The conclusion is single-arm-load-bearing and is stated that way throughout. So **seam A is not merely the cheaper seam; it is the BEHAVIORALLY FAITHFUL one**, and seam B would have DIVERGED on the one no-pick class envoy-go can actually express.

**The row's cost claim, EXECUTION-CONFIRMED by hash — with its scope stated exactly.** As in phase 72, the source is already a parameter of all three emit functions (`picked cluster.Endpoint`, the 4th parameter at `accesslog_emit.go:27`/`:87`/`:149`) and the three `ResolveCustomTags` call sites (`:57`/`:118`/`:179`) sit INSIDE them. Two independent scratch builds proved by sha256 that **SEVEN production files are BYTE-IDENTICAL to baseline** — `connection.go`, `h2dispatch.go`, `h3dispatch.go`, `internal/filter/http/chain.go`, `internal/cluster/subset.go`, **`internal/filter/hcm/actions.go`** and **`internal/filter/hcm/route.go`** (the last two being exactly what the rejected seam would have touched) — plus `internal/cluster/manager_test.go` and `extractEndpoints`'s signature.

⚠️ **`accesslog_emit_test.go` and `span_emit_test.go` are NOT in the sha256 set, and an earlier draft of this document wrongly put them there.** The costing build hashed them identical only because it wrote no new tests; the SHIPPED row adds live per-site span tests and a zero-`picked` test to both (§10 T4), so a byte-identical gate on them is unexecutable by construction. Their real invariant is **CALLER-SET invariance**: the three emit signatures are unchanged, so the existing **34** emit test callers (13 + 21) are untouched and both files are **PURELY ADDITIVE** — the gate is `grep -c` on the caller counts plus a `diff` yielding ZERO `<` lines, not a hash. Stated separately because conflating "the file is unchanged" with "the callers are unchanged" is precisely the kind of unexecutable gate a reviewer has to catch, and this one was caught here rather than at the IMPL.

The row composes on landed substrate: **ZERO new packages, +0 go.mod modules, +0 stats, +0 BackendKinds, +0 fuzzers**. `descend` (`resolve.go:172-186`) and `structpbValueToString` (`resolve.go:195-212`) are REUSED VERBATIM — EXECUTED-confirmed correct for CLUSTER with **ZERO CLUSTER-specific serialization difference** (§3.5).

### 1.1 BRAINSTORM anticipations — what HELD, what this SPEC SHARPENS or REFUTES *(the drift ledger; drift found is the point)*

**HELD (re-derived / EXECUTED / BUILT this session).** The CLUSTER reject arm `config.go:268-269` and its verbatim message; the six-arm switch with all its spans and error strings (`:232-233` nil-kind, `:234-250` REQUEST, `:251-267` ROUTE, `:268-269` CLUSTER, `:270-286` HOST, `:287-288` default); the `k := md.GetKind()` bind at `:230` with all four kind-getters on `*metadatav3.MetadataKind` (`GetCluster()` at `metadata.pb.go:178`; **`md.GetCluster()` does not exist on `*CustomTag_Metadata`, whose only getters are `GetKind`/`GetMetadataKey`/`GetDefaultValue`** — the phase-70 SEVERE receiver fact, RE-CONFIRMED). The `customTagKind` block `:55-62` with six constants at iota 0-5 and `kindMetadataHost` LAST ⇒ an APPENDED `kindMetadataCluster` at **iota == 6** — **EXECUTION-CONFIRMED, not inferred**: the scratch build's failing test printed `Kind:6`. `CustomTagSpec` `:69-79` with `MetaNamespace`/`MetaPath`/`DefaultValue`/`HasDefault` all present ⇒ **NO new field**. `ResolveCustomTags` `resolve.go:46` at exactly 5 params. The `kindMetadataHost` resolve arm `:131-160` as a byte-level clone source, descending the FULL `MetaPath` at **`:150`** (the ROUTE arm's is `:120`; the REQUEST `[1:]` at `:98` is REQUEST-only). `descend` `:172-186` + `structpbValueToString` `:195-212`. `Endpoint` `:45-86` with the phase-72 `filterMetadata` field at **`:79`** (`:72-78` is its doc comment) and `MetaLookup` `:111-117`; `cluster.go` ALREADY imports `structpb` at `:21`. `type Cluster struct` at `:157` carrying **NO metadata field** (32 fields enumerated). `extractEndpoints` at `manager.go:863` with **exactly ONE production call site, `manager.go:433`**, and the `*clusterv3.Cluster` in scope there as the `buildCluster` parameter `c` (`manager.go:402`), `la := c.GetLoadAssignment()` at `:429`. `clusterRouteAction`'s `cluster *cluster.Cluster` field at `actions.go:183` (`:182` is the struct header). The `routeAction` interface at `route.go:52-59` (`:60` blank — the phase-73 BRAINSTORM's §11.3 retraction was right). The 5+6+7 = **18** emit roster with every line number, and the 6 PRE-Decide / 12 span-capable partition. The `/cluster` reject-kind test set is **EXACTLY TWO** subtests (`config_test.go:639` and `:764`) — the phase-72 `/host` count reproduced, not assumed. The `withMetaTags` seed at `fuzz_test.go:99-122` with `meta_bad` **LAST at `:117`** on CLUSTER. Collision greps `kindMetadataCluster` / `clusterMetaLookup` / `ClusterMetaLookup` ⇒ **0, 0, 0**. Any `filter_metadata` namespace is addressable for CLUSTER (`envoy.lb` NOT privileged). +0 stats/packages/modules/fuzzers/BackendKinds; SINGLE FLAT ROW — all HELD.

**SHARPENED or REFUTED — each flagged where it lands:**

| # | BRAINSTORM said | Sharpened / REFUTED to | § |
|---|---|---|---|
| **S1** | **D-CTMC-SEAM (the load-bearing question):** the two seams differ on fidelity; *"Anticipated but NOT assumed: the reference resolves CLUSTER at route-selection time, which would favor seam B on fidelity."* Seam A "INHERITS the B2 departure … the same breadth with a **materially worse justification**", since "the cluster was selected by the route before any endpoint was tried" (§2.4) | **REFUTED BY EXECUTION. The reference gates a CLUSTER tag on the PICKED HOST, not on the matched route.** Cluster selected + NO host picked ⇒ the tag falls to `default_value` on three arms — zero endpoints (`UH`), DNS-empty (`UH`), circuit-breaker overflow (`UO`) — **with `upstream_cluster` present on the same span**, so the cluster was demonstrably known and deliberately unused, and a co-configured HOST control moved in lockstep in every arm. ⚠️ **The refutation is SINGLE-sourced, not triple-sourced** *(an earlier draft of this row said "triple")*: S5 and S5b both produce an EMPTY HOST SET and are therefore consistent with a competing "gate = non-empty host set" hypothesis, so only **S8** discriminates (§3.3). ⇒ **DECIDED: SEAM A** — the faithful seam, not the cheap compromise. **And the "materially worse justification" claim is itself REFUTED** — "no endpoint picked ⇒ no cluster metadata" is not a rationalization, it is *the reference's own rule*. | §3.3 |
| **S2** | **§2.4/§6 seam A shape:** a third `extractEndpoints` parameter carrying `c.GetMetadata().GetFilterMetadata()`, costing **ONE new import in `manager.go`** (`manager.go:…: undefined: structpb` — quoted without a column, since the line and column both shift with the loop insert and the chosen parameter name; three agents reproduced the ERROR, at three different positions) | **A STRICTLY CHEAPER FORM EXISTS AND WAS BUILT — adopt it.** Populating in `buildCluster` *after* the `extractEndpoints` call (an 8-line loop aliasing `c.GetMetadata().GetFilterMetadata()` onto `endpoints[i]`) leaves **`extractEndpoints`'s signature AND `manager_test.go` byte-identical to baseline** — the signature change would have dragged **10** test callers (`manager_test.go:1467,1482,1520,1828,1854,1886,1911,1924,2253,2295`) — and adds **ZERO new imports anywhere** (the `map[string]*structpb.Struct` never appears in a `manager.go` declaration). BUILT: `go build`/`go vet` exit 0, the predicted red set exactly the two `/cluster` subtests, the pool-HIT provenance test green. Measured diff **6 files, +128/−58** vs the briefed form's 7 files, +136/−71. | §3.4 |
| **S3** | **§2.5/§1.1/§12 + the ROADMAP row + STATE:** *"After it lands, the ONLY remaining reject arms are the two unset-kind ones"* (`config.go:232-233` DEPARTURE / `:287-288` PARITY — the phase-71 S3 split) | **REFUTED — a SECOND departure arm survives phase 73 and has NEVER been recorded.** The post-row roster is **TWO reject ARMS across THREE config spellings** (§3.6 pins the unit; the quoted baseline counted two REJECT arms of which only one was a departure, so the two figures were never commensurable). EXECUTED: a `metadata` block whose **`metadata_key` is entirely ABSENT** is **ACCEPTED by the reference** (`--mode validate` exit 0 AND a real boot serving 200 with a live span emitting `default_value`), while envoy-go rejects it `empty namespace`. It is masked because **envoy-go emits the SAME `empty namespace` string for two structurally different configs** — `metadata_key: {}` present-but-empty (PGV-rejected by the reference ⇒ PARITY) and `metadata_key` omitted (accepted ⇒ **DEPARTURE**). The reference distinguishes them by **message PRESENCE, not content**. The BEHAVIOR_CONTRACT edit and ADR-0295 must carry three departure entries. | §3.6 |
| **S4** | **§2.7 D-CTMC-FUZZSEED:** *"Phase 73 REQUIRES a repoint… `meta_bad` must be repointed to a still-rejecting arm… Anticipated: repoint `meta_bad` to a **nil/absent `kind`** (the `config.go:232-233` DEPARTURE arm)"* | **BOTH HALVES CORRECTED, and the anticipated TARGET IS THE WRONG ONE.** (a) The repoint is **not mandatory for the seed to keep rejecting**: `meta_bad` (`fuzz_test.go:117-119`) carries a CLUSTER kind and **no `MetadataKey` at all**, so after the lift it falls THROUGH the new accept arm into `mk.GetKey() == ""` and rejects `empty namespace` — **still red, silently, with nothing going green**. Three agents found this independently. (b) The repoint IS mandatory for a *sharper* reason: that exact shape is precisely the S3 DEPARTURE, so leaving it converts a parity-reject seed into a departure-shaped one, silently. (c) **The BRAINSTORM's proposed target — a nil/absent `kind` — is the OTHER DEPARTURE arm** (EXECUTED: the reference ACCEPTS an absent `kind` and emits `default_value`). **The correct target is `Kind: &metadatav3.MetadataKind{}`** — the kind message PRESENT with its oneof unset: envoy-go rejects at `config.go:288`, the reference PGV-rejects `field: "kind", reason: is required`. **PARITY, executed both sides.** | §7 |
| **S5** | **§2.5 / §6 D-CTMC-STALECOMMENT:** *"Phase 72 found NINE and they were a real defect class. Run the sweep; do not assume phase 72 caught them all"* | **TWENTY GOES-FALSE sites, SIX of them DELICATE — more than double phase 72's.** *(The tally counts sites that go FALSE; it deliberately EXCLUDES `resolve.go:12-45`, which goes INCOMPLETE rather than false and is carried as an §11 edit obligation instead. It also counts TWO ROADMAP sites, where §12/T9 now name THREE — see T9.)* 9 in `config.go` (incl. `:48-52`, five `CustomTagSpec` field comments `:71`/`:75`/`:76`/`:77`/`:78`, and `:180-186`), 4 in `config_test.go` (two doc comments + the two byte-identical table rows), 2 in `fuzz_test.go`, 3 in BEHAVIOR_CONTRACT, 2 in ROADMAP. **Plus a second-order finding the sweep alone surfaced:** after the lift the two surviving unset-kind arms emit the **IDENTICAL** string `kind required`, so `config.go:185`'s *"reject LOUDLY with **distinct** substrings"* goes false in a second way and must not simply be narrowed to "the unset kinds". | §11 |
| **S6** | **§6/§12 + the ROADMAP row-73 cell:** *"`CLUSTER` is the LAST item in its clause, so that narrow REMOVES the clause rather than shortening it"* — flagged as a hazard that could change sentinel check (2)'s count | **OVER-STATED — the hazard is real but half its size, and check (2) STAYS 3.** `CLUSTER` is the last item **inside the parenthetical**, but the `metadata` item is the **FIRST of a four-member slash-list** (`metadata(…)`/`spawn_upstream_span`/`http_service`/force-trace). Removing it leaves **four** candidates (those three plus the `ssl` stat family), a well-formed sentence, and the `candidates:` marker intact. The exact `old_string`/`new_string` is pinned at §12; **the ROADMAP row-73 cell's own ⚠️ sentence is itself a stale claim** and is corrected at the IMPL rather than propagated. | §12 |
| **S7** | **§2.4 seam B:** *"`entry` is in scope at ALL 5 H1 emit CALL SITES … and ALL 7 H3 sites. H2 is the only split"* | **CONFIRMED BY EXECUTION, with the split proven exactly.** `_ = entry` inserted before all 18 production emit call sites and built with `-gcflags=-e` yields **exactly 6 errors, all in `h2dispatch.go`** (`:313`,`:397`,`:532`,`:580`,`:588`,`:618`), and none in `connection.go` or `h3dispatch.go`. Moot for the decided seam, but it is the first time the claim was executed rather than read. | §3.3 |
| **S8** | *(unraised — the BRAINSTORM assumed seam B trades the `internal/cluster` task away)* | **SEAM B DOES NOT AVOID THE `internal/cluster` WORK — it RELOCATES it and adds the thread on top.** EXECUTED: `cluster.Cluster` carries no metadata accessor (`type *cluster.Cluster has no field or method MetaLookup`), so seam B needs its OWN retention field + exported accessor on `Cluster`, **plus** a 52-site thread (18 production + **34** emit test callers, enumerated by compile error), **plus** the H2 carrier triple, **plus** a weighted-clusters disposition. §10's "comparable either way" sizing is wrong: **9 tasks vs 12-13.** | §3.3 |
| **S9** | *(unraised)* | **THE HYBRID IS STRICTLY WORSE THAN EITHER PURE SEAM — proven, not argued.** A route-side fallback when `picked.IsZero()` compiles on H1/H3 (`f.table` is in scope inside the emit helpers) but **CANNOT compile on H2**: `emitAccessLogH2` receives an `h2.H2Request` while `routeTable.match` takes `*http.Request` (`cannot use req (variable of struct type h2.H2Request) as *http.Request`). The H2 arm must therefore take seam B's carrier field anyway. The hybrid costs seam A **plus** a duplicate match on H1/H3 **plus** seam B's H2 half. **Priced, then rejected — not assumed away.** | §3.3 |
| **S10** | **§2.6 fixture:** the `0116` chassis with the source relocated; *"A single-cluster fixture … cannot discriminate selected cluster from only cluster. **Name it in `expectations.yaml` up front**, or add a second cluster with a deterministic route split"* | **TAKE THE SECOND OPTION — it is nearly free and it removes the limitation instead of documenting it.** TWO clusters carrying DIFFERENT metadata, both pointing at the SAME single backend, with TWO path-prefix routes: deterministic, zero LB spread (`reference_round_robin_offset_randomized` never engages), `BackendCount` stays **1**. This DISCRIMINATES *selected* from *only* — the exact limitation `0116` had to confess in review — and it cross-side-proves the per-request selection EXECUTED at S3. | §8 |
| **S11** | *(unraised)* | **INTRA-CUSTOM-TAG EMIT ORDER IS ARBITRARY ACROSS PROCESSES, not merely "not config order".** EXECUTED: a byte-identical bootstrap emitted `m,z,ctl_lit,a,q` on two fresh containers and `q,a,ctl_lit,z,m` on a third. Phase 72 recorded "an INTERNAL iteration order"; this sharpens it to per-process hash order. Assertions MUST be key-based — envoy-go's `upsertAttr`/`Span.Attrs` already are, and the `0117` driver must be too. | §3.5 |
| **S12** | *(unraised — the phase-72 B2 clause lists circuit-breaker as "likely PARITY and **also unprobed**")* | **THE CIRCUIT-BREAKER ARM IS NOW PROBED ON THE REFERENCE, and it is PARITY.** EXECUTED (S8): with `max_connections/max_pending_requests/max_requests/max_retries` all `0`, the reference 503s `UO` and the tag falls to `default_value` — **the breaker trips BEFORE the host pick**, which is exactly envoy-go's ordering (`TryAcquireRequest` precedes the LB pick). This NARROWS B2's unprobed set from two arms to one (pool-overflow remains unprobed). ⚠️ Scope it exactly: the REFERENCE half is EXECUTED; envoy-go's half is RE-DERIVED from the landed B2 clause's own citations, not cross-side probed. | §3.6, §9 |

**Cosmetic drift corrected (five, none code-blocking).** (i) The phase-72 SPEC's `descend`/`structpbValueToString` cites `:128`/`:151` are STALE at this tip — they are `:172-186`/`:195-212`; successors must copy from HERE, not from 72. (ii) `Endpoint.filterMetadata` is at `cluster.go:79`, not `:72-79` (that span is its doc comment) — the BRAINSTORM §2.4 cite was to the comment. (iii) The phase-72 `filterMetadata` populate is **not a standalone line** — it is an inline field inside the `manager.go:884` composite literal, so any PLAN naming "the populate line" must target `:884` as a whole. (iv) `resolve_test.go`'s raw `grep -c 'ResolveCustomTags('` returns **50**; `:107` is a `t.Errorf` STRING LITERAL (`:106` is its real call) ⇒ **49 real callers** — two agents derived 49 independently, matching the BRAINSTORM §11.4 figure; **do not adopt 50**. (v) The "29 emit test callers" figure in ADR-0294 is the PRE-phase-72 count; the current measured figure is **34** (13 in `accesslog_emit_test.go` + 21 in `span_emit_test.go`, enumerated by compile error) — phase 72's own T4 added the five span tests. Not a drift, a moved baseline; it matters only under seam B, which is not being taken.

### 1.2 SPEC-time verification record

**Read-only re-derivation (agent A1)** re-opened every BRAINSTORM §6 anchor plus the proto/PGV surface, the `Cluster` field census, the emit roster, the `/cluster` test set, the fuzz seed and the collision greps at `d51d33dc`, reporting a verbatim quote per cite.

**By-execution costing (agent A2), in a private `cp -a` copy with `.git` disabled**, BUILT seam A end to end: `gofmt -l` silent · `go build ./...` exit 0 · `go vet ./...` exit 0 · `go mod tidy -diff` EMPTY ⇒ **+0 modules** · `golangci-lint run` exit 0 on all three touched packages · `go list -deps ./internal/tracing | grep -E 'internal/(filter|cluster)'` **EMPTY** · the full `./internal/... ./cmd/... ./validate/...` suite ran **75 packages** with **exactly the two predicted `/cluster` subtests failing**, the failure printing `Kind:6` ⇒ **`kindMetadataCluster` == iota 6 BY EXECUTION** · `unsafe.Sizeof(cluster.Endpoint{})` **112 → 120** · **seven production files sha256-IDENTICAL to baseline** (the build also hashed the two emit TEST files identical, but only because it wrote no new tests — see §1 and §10 T4) · the exported-API delta exactly **one** new symbol (`Endpoint.ClusterMetaLookup`) plus the `ResolveCustomTags` widening · and a **pool-HIT provenance test that PASSES and DISCRIMINATES** (breaking the H1 pool hit to reconstruct the `Endpoint` fires the intended assertion, not an earlier one). It then priced seam B by execution (the 52-site thread floor, the `entry` scope split, the absent `Cluster` accessor, the weighted hole) and REFUTED the hybrid by compile error.

**Twenty-one EXECUTED live-probe arms** across three fleets: **S0/S0b/S1/S2/S3/S4/S5/S5b/S6/S7/S8** (the seam docket), **W1-W7** (wire, Zipkin, serialization, default/omit, source distinctness, locality siblings), **R1-R6** (the accept/reject roster, the unset-kind sub-cases, the no-cluster classes, the fuzz-repoint target, `clusters[].metadata` inertness). Every fleet independently confirmed the canonical worktree's `git status --porcelain` unchanged at start and end, and removed every container and network it created.

**Cross-fleet corroboration, recorded because it is what makes the seam decision safe:** three independent agents reached the same disposition for the no-cluster classes from different configurations — `direct_response` → `default_value` (S4 and R4a), 404 `NR` → span emitted on the reference with `default_value` (S2 and R4c), and the connect-refused `UF` → **RESOLVES** (S1 and W7a, two fleets, two bootstraps). Where a single fleet is the only source, this document says so.

**Adversarial-pass record:** §16.

---

## 2. Non-purposes (deferred; per BRAINSTORM §1.2 + §8)

NOT the two remaining unset-kind rejects, NOT the newly-discovered absent-`metadata_key` reject (all three stay rejects — §3.6; the departure roster narrows to THREE, it does not empty). NOT any other tracing follow-on (`spawn_upstream_span` / OTel `http_service` / `sampler` / `resource_detectors` / `verbose` / force-trace). NOT the downstream TLS handshake-outcome `ssl` stat family (ADR-0286 C3). NOT a separate Zipkin fixture (the `Span.Attrs` seam is shared and W2 proved the Zipkin emit LIVE — the 59/62/63/70/71/72 single-OTLP-dir precedent). NOT any new provider/transport/stat/package/module/BackendKind. **NOT the pick-time `picked` propagation row** (BEHAVIOR_CONTRACT B2 — this row becomes its FIFTH consumer, it does not create or widen it; §3.6, §13). **NOT the `upstream_cluster` span TAG** — a SEPARATE, unchartered feature; `accesslog_emit.go:51`/`:112`/`:173`'s `UpstreamCluster: ""` is untouched by this row, and phase 72's category-error warning stands (`reference_tracing_upstream_cluster_framework_gap`). **NOT a fix for the Zipkin empty-string drop, the number-precision edges, or the arbitrary struct-key order** (all PRE-EXISTING, all shared by the landed REQUEST/ROUTE/HOST kinds — §3.5). NOT `clusters[].typed_filter_metadata` (a `MetadataKey` can only ever address `filter_metadata`). NOT a change to `cluster.Cluster`, `internal/dynamicmetadata`, `internal/filter/http/chain.go`, `internal/filter/hcm/{connection,h2dispatch,h3dispatch,actions,route}.go`, `internal/cluster/subset.go`, the phase-38 `envoy.lb` projection, `ScalarsFromStruct`, `defaultSubset` or the subset LB (all byte-untouched — §11).

---

## 3. The change — the D-CTMC-* docket disposed one-for-one

### 3.1 D-CTMC-SCOPE / D-CTMC-PROTO — the `CLUSTER`-only slice on the exact v1.32.4 proto *(RE-DERIVED; R1 EXECUTED)*

Module **`github.com/envoyproxy/go-control-plane/envoy v1.32.4`** (`go.mod:10`). `CustomTag.GetMetadata()` returns `*CustomTag_Metadata{Kind, MetadataKey, DefaultValue}` whose **ONLY getters are `GetKind()`/`GetMetadataKey()`/`GetDefaultValue()`**. The four kind-getters live on `*MetadataKind` — **`GetCluster()` at `type/metadata/v3/metadata.pb.go:178`** — so the arm branches on **`k.GetCluster()` on the bound `k := md.GetKind()`** (`config.go:230`); **`md.GetCluster()` DOES NOT EXIST and does not compile.** The phase-70 SEVERE, RE-CONFIRMED for the fourth and last time. The oneof wrapper is `MetadataKind_Cluster_` (`:206-209`, field number **3**) and `MetadataKind_Cluster` (`:373-377`) is an **EMPTY message** — so `cluster: {}` is its only well-formed spelling. All in the direct `go-control-plane/envoy v1.32.4` ⇒ **+0 go.mod modules** (BUILT: `go mod tidy -diff` EMPTY).

### 3.2 D-CTMC-CONFIG-KIND / the accept arm — a `kindMetadataCluster` const at iota 6 + a CLUSTER accept cloning HOST *(RE-DERIVED; BUILT)*

`config.go`: **APPEND** `kindMetadataCluster` to the `customTagKind` iota block (`:55-62`, currently `kindLiteral`=0 … `kindMetadataHost`=5) **AFTER `kindMetadataHost`** ⇒ **iota == 6, EXECUTION-CONFIRMED** (the red-first output prints `Kind:6`). **APPEND, never INSERT** — the constants are unexported and never serialized, so a renumber recompiles silently and fails NOWHERE loudly; phase 72's Break G is the precedent. Note `config_test.go:856-867` already pins `kindMetadataHost != 5` / `kindMetadataRoute != 4`; the IMPL should add the symmetric `kindMetadataCluster != 6` guard.

⚠️ **A gofmt consequence the PLAN must expect: this block is NOT a pure-append diff.** `kindMetadataCluster` is 19 characters against the block's current alignment column (set by the 17-character `kindMetadataRoute`), so gofmt **re-aligns the trailing comments on `:58-61`**. The costing agent's measured `config.go` diff is `+21 / −5`, consistent with exactly this. Do not treat a 4-line comment-column shift as unintended damage.

The CLUSTER arm REPLACES the `:268-269` reject, cloning the HOST accept (`:270-286`) with exactly two token substitutions (`GetHost`→`GetCluster`, `kindMetadataHost`→`kindMetadataCluster`):
```go
case k.GetCluster() != nil:
	mk := md.GetMetadataKey()
	if mk.GetKey() == "" {
		return nil, fmt.Errorf("tracing: custom_tags metadata tag %q empty namespace", tag)
	}
	if len(mk.GetPath()) == 0 {
		return nil, fmt.Errorf("tracing: custom_tags metadata tag %q empty path", tag)
	}
	path := make([]string, 0, len(mk.GetPath()))
	for _, seg := range mk.GetPath() {
		if seg.GetKey() == "" {
			return nil, fmt.Errorf("tracing: custom_tags metadata tag %q empty path segment", tag)
		}
		path = append(path, seg.GetKey())
	}
	dv := md.GetDefaultValue()
	spec = CustomTagSpec{Key: tag, Kind: kindMetadataCluster, MetaNamespace: mk.GetKey(), MetaPath: path, DefaultValue: dv, HasDefault: dv != ""}
```
`MetaPath` carries the FULL path (no `[1:]` — REQUEST-only). `config.go` adds **ZERO imports**. The first-wins dedup (`:293-297`) is UNCHANGED. **Note a first-in-lineage cosmetic:** the reject being lifted sits BEFORE the HOST arm, so an in-place lift leaves the source order REQUEST/ROUTE/CLUSTER/HOST while the iota order becomes …Route=4/Host=5/Cluster=6. The two orders disagree for the first time. Harmless; recorded so a reviewer does not read it as a renumber.

The three structural rejects are **PGV-PARITY, EXECUTED per arm at R2** with the reference's verbatim C++ text: empty or absent `metadata_key.key` → `MetadataKeyValidationError.Key: value length must be at least 1 characters`; absent **and** zero-length `path` → the IDENTICAL `MetadataKeyValidationError.Path: value must contain at least 1 item(s)`; empty segment key → `PathSegmentValidationError.Key: …`, and a segment with an unset oneof → `field: "segment", reason: is required`. The empty-`tag` reject (`CustomTagValidationError.Tag: …`) and the missing-type reject (`field: "type", reason: is required`) are likewise PARITY. **`--mode validate` and a real boot agreed on every single arm — ZERO disagreements** across the whole R-fleet.

### 3.3 D-CTMC-SEAM — **DECIDED: SEAM A (endpoint-carried), in the `buildCluster`-loop form** *(the S1/S2/S8/S9 dispositions; EXECUTED S0-S8 + W5/W7a + R4; BUILT)*

**This is the row's single most important deliverable, and it was decided on probed reference behavior.**

**What the reference actually does.** A CLUSTER-kind tag resolves from `clusters[].metadata.filter_metadata[<ns>]` **of the cluster that owns the PICKED UPSTREAM HOST**. Its availability gate is the SAME gate as the HOST kind; only the metadata block read differs. The evidence, per arm:

| arm | class | reference behavior | what it rules out |
|---|---|---|---|
| **S0** | 200 OK, cluster metadata present | tag RESOLVES to the static value | pins the SOURCE FIELD as `clusters[].metadata` |
| **S0b** | key present ONLY on `lb_endpoints[].metadata`, cluster block REMOVED | CLUSTER → `default_value`; a co-configured HOST control → the endpoint value | CLUSTER and HOST are **SOURCE-DISTINCT** |
| **W5b** | namespace present on the cluster but the KEY absent; the key present on the endpoint AND the route | CLUSTER → `default_value` | **NO fallback to any other source**, at namespace OR value level (the strong form — the namespace *was* there) |
| **S1 / W7a** | host PICKED, connect REFUSED (503 `UF`) | tag **RESOLVES** from the picked-but-unreachable host's cluster | not gated on upstream SUCCESS. **Two fleets, two bootstraps, same result** |
| **S5** | cluster SELECTED, `load_assignment.endpoints: []` (503 `UH`) | tag → `default_value`, **while `upstream_cluster: c_backend` IS on the span** | **not gated on route/cluster SELECTION** |
| **S5b** | cluster SELECTED, STRICT_DNS name unresolvable ⇒ zero hosts (503 `UH`) | agrees with S5 | an independent replicate of S5 by a different mechanism |
| **S8** | cluster SELECTED, circuit breaker all-zero (503 `UO`) | tag → `default_value` | a THIRD independent replicate; the breaker trips **before** the pick |
| **S3** | `weighted_clusters` c_a(50)/c_b(50), distinct metadata, 20 requests | **perfect 1:1** with the span's own `upstream_cluster` — 9×(c_a,v-A), 11×(c_b,v-B) | resolution is **PER-REQUEST**, off the cluster that request went to |
| **S4 / R4a** | `direct_response` (no cluster ever selected) | span emitted; tag → `default_value` | — |
| **R4b** | `cluster_header` unresolvable (503 `NC`) | span emitted; tag → `default_value` | — |
| **S2 / R4c** | no route match (404 `NR`) | span emitted; tag → `default_value`; **no `upstream_cluster` attribute at all** | — |

⇒ **The line the reference draws is "was a HOST picked", not "was a cluster selected".** S1 alone would have condemned seam A; the no-pick arms show *why* S1 resolves, and the reason is the pick.

**⚠️ WHICH ARM ACTUALLY CARRIES THE WEIGHT — stated because a probe that does not DISCRIMINATE proves nothing** (`reference_probe_must_discriminate`). The competing hypothesis is *"the gate is a NON-EMPTY HOST SET"* rather than *"a host was picked for THIS request"*. **S5 and S5b both produce an empty host set**, so they are consistent with BOTH readings and replicate each other's confound; they are evidence about the reference's no-pick behavior, not independent confirmations of the pick-gate. **Only S8 discriminates** — the host set is non-empty, the breaker trips before any pick, and the tag still falls to `default_value`. So the pick-gate conclusion rests on **ONE discriminating arm plus S1's converse**, and this document does not describe it as triple-sourced. *(An earlier draft did; both are recorded in §16.)* It is still the right conclusion — S1 and S8 together bracket the gate from opposite sides — but a future row re-opening this owes a second discriminating no-pick arm with a non-empty host set (pool-overflow is the obvious candidate, and it is the same arm B2 still lacks).

**THE DECISION: SEAM A.** Retain the OWNING cluster's `filter_metadata` on `cluster.Endpoint` beside the phase-72 `filterMetadata` field, expose it as `Endpoint.ClusterMetaLookup(ns)`, and pass `picked.ClusterMetaLookup` at the three `ResolveCustomTags` call sites. Seam A's gate is `picked` being non-zero — **the reference's own gate**. It reproduces the reference EXACTLY on every probed class envoy-go can express: **S0, S0b, W5b, S3, S4 and S8**. ⚠️ **S5/S5b and R4b are NOT parity claims and must not be written as any** — S5 and S5b are **BOOT-REJECTED by envoy-go** (`manager.go:890` `cluster: %q: zero endpoints across all locality groups`; `manager.go:427` `only STATIC clusters supported`), so they have **no envoy-go counterpart at all**, and R4b (`cluster_header`, 503 `NC`) was never probed on envoy-go's side and stays **UNCLASSIFIED**. Seam A's single residual divergence (S1) is the phase-72 B2 departure inherited **verbatim, at the same breadth, on the same site set, and now with the reference's own rule as its justification rather than an apology** (§3.6).

**Why seam B is REJECTED — on fidelity FIRST, cost second.**
1. **It would MANUFACTURE a divergence that does not exist today.** Reading the cluster off the matched route resolves whenever a route matched, so it would emit the cluster's value where the reference emits `default_value` on every no-pick class. ⚠️ **Scoped honestly — that is ONE class expressible in envoy-go, not three** *(an earlier draft of this section said three, counting S5 and S5b; both are boot-rejected by envoy-go, so seam B could not diverge there because envoy-go cannot get there — §16)*: **S8**, the circuit-breaker `UO` class, is the one arm where seam B would demonstrably contradict the reference on a config envoy-go boots. One manufactured divergence is still disqualifying for a row whose stated purpose is reference fidelity — but the argument is one arm wide, and inflating it 3× was exactly the over-claim class phase 72 had to retract.
2. **It does NOT avoid the `internal/cluster` work.** EXECUTED: `type *cluster.Cluster has no field or method MetaLookup` — `Cluster` retains no metadata at all (32 fields, RE-DERIVED). Seam B relocates the retention from `Endpoint` to `Cluster`; it does not delete it.
3. **It re-pays the full thread — 52 sites, enumerated by compile error.** 18 production (`connection.go` ×5, `h2dispatch.go` ×6, `h3dispatch.go` ×7) + **34** emit test (13 `accesslog_emit_test.go` + 21 `span_emit_test.go`). The measured `nil`-everywhere thread FLOOR, with no retention, no carrier and no weighted disposition, is already 6 files / +55 / −55.
4. **It needs the H2 carrier triple**, because `entry` is provably NOT in scope at any of the 6 `h2dispatch.go` emit sites (EXECUTED: exactly 6 `undefined: entry` errors, all in that file, none in `connection.go` or `h3dispatch.go`).
5. **It leaves a weighted-route HOLE.** EXECUTED: `type *weightedClusterRouteAction has no field or method cluster` — the per-request selection happens INSIDE the returned closure (`router_weighted.go:119` H1 / `:147` H2), so no statically-resolved cluster exists at HCM build time. Seam B would owe a documented departure or an ADR-0080 reject for `metadata`/`CLUSTER` + `weighted_clusters` on the same route — **against a reference that EXECUTED-resolves weighted routes perfectly (S3)**. Under seam A the problem **dissolves**: the picked endpoint definitionally belongs to the cluster the selector chose.
6. It moves five currently byte-identical files into the diff, and costs **12-13 tasks vs 9**.

**Why the HYBRID is REJECTED — priced, then refuted by compile error.** A route-side fallback when `picked.IsZero()` compiles on H1/H3 (`f.table` is in scope inside the emit helpers, and `e.action.(*clusterRouteAction).cluster` type-asserts cleanly) but **cannot compile on H2**: `emitAccessLogH2` receives an `h2.H2Request` while `routeTable.match` takes `*http.Request` (`cannot use req (variable of struct type h2.H2Request) as *"net/http".Request value in argument to f.table.match`). The H2 arm must take seam B's carrier field regardless. The hybrid therefore costs seam A **plus** a duplicate route match on H1/H3 **plus** seam B's H2 half — and it would buy only the S1 class while *breaking* S5/S5b/S8, since a fallback that fires whenever `picked` is zero fires in exactly those three arms too. **Strictly worse on both axes.**

**A THIRD seam exists and is named here so the docket is CLOSED rather than merely two-sided.** `*Filter` carries `clusters *cluster.Manager` (`internal/filter/hcm/config.go:95`), which IS in scope inside all three emit helpers, and `Manager.Get(name) (*Cluster, bool)` (`manager.go:206`) is exported — so **"seam C", resolving the owning `*Cluster` by NAME at the emit seam, is expressible today.** It is nonetheless DOMINATED, for two reasons neither of which is diff size: (i) **the cluster NAME is not in scope at the emit seam either** — that is precisely the `accesslog_emit.go:51`/`:112`/`:173` `UpstreamCluster: ""` framework gap this row declares out of scope (§2) — so seam C still costs a thread, merely of a `string` instead of a lookup; and (ii) **whatever gate the name is threaded on inherits seam B's fidelity problem**, because a name known at route-match time resolves on the no-pick classes where the reference does not. Recorded rather than omitted: an earlier draft presented "two seams and a hybrid" as exhaustive, and it was not.

**Ordering note, restated now that it is decided:** if the deferred pick-time `picked`-propagation row (B2) lands later, seam A's single residual divergence closes automatically with no phase-73 rework — the same `picked` that carries HOST metadata carries cluster metadata. The two rows compose; neither duplicates the other.

### 3.4 D-CTMC-RESOLVE-SEAM + the retention shape — a SIXTH nil-tolerant param, the closure built LOCALLY *(the S2 decision; RE-DERIVED + BUILT)*

**Retention.** `Endpoint` gains a SECOND unexported aliasing field beside the phase-72 `filterMetadata` (`cluster.go:79`) — `clusterFilterMetadata map[string]*structpb.Struct` — plus an EXPORTED nil-safe accessor `func (e Endpoint) ClusterMetaLookup(ns string) (*structpb.Value, bool)` beside `MetaLookup` (`cluster.go:111-117`), wrapping the namespace struct via `structpb.NewStructValue` and returning `(nil,false)` on a nil map / absent namespace / nil struct. `cluster.go` ALREADY imports `structpb` (`:21`, phase 72) ⇒ **ZERO new imports**. The map ALIASES the already-parsed proto map ⇒ **zero new allocation per endpoint**; `unsafe.Sizeof(Endpoint)` **112 → 120 B** (MEASURED, +8 = one map header word).

**⚠️ POPULATE IN `buildCluster`, NOT VIA AN `extractEndpoints` PARAMETER (the S2 decision).** The `*clusterv3.Cluster` is in scope at `manager.go:433` as `buildCluster`'s own parameter `c` (`manager.go:402`), so an 8-line loop:

**⚠️⚠️ PLACEMENT IS LOAD-BEARING AND MUST BE PINNED, NOT LEFT AS "AFTER THE CALL".** `buildCluster` hands the returned `[]Endpoint` to LB constructors that take **VALUE copies at build time** — `buildLeafLB` (`manager.go:472`), `newSubsetLB` (`:481`, which copies each `ep` into `groups` at `subset.go:161-168`), `newLocalityWeightedLB` (`:516`) and `newPriorityLB` (`:550`). "After the call" is satisfied by any line in `433..~590`, and **a loop placed after `:472` would leave the subset / locality-weighted / priority LBs carrying a ZEROED `clusterFilterMetadata` while the round-robin fast path — which shares the slice — stayed green.** That is a silent, LB-SHAPE-DEPENDENT bug that no landed test catches, and that this SPEC's own pool-HIT provenance test would NOT catch either (it exercises the H1 pool, not an LB shape). **Pin the loop IMMEDIATELY after the `err != nil` guard at `:433-436` and BEFORE any LB construction**, and give T1 an explicit subset-LB assertion so the constraint is enforced rather than merely documented.
```go
if fm := c.GetMetadata().GetFilterMetadata(); len(fm) > 0 {
	for i := range endpoints {
		endpoints[i].clusterFilterMetadata = fm
	}
}
```
leaves **`extractEndpoints`'s signature AND `manager_test.go` byte-identical to baseline**. A third `extractEndpoints` parameter would instead drag **10** test callers (`manager_test.go:1467,1482,1520,1828,1854,1886,1911,1924,2253,2295`) and — if typed `map[string]*structpb.Struct` — add a `structpb` import to `manager.go`, which imports neither `structpb` nor `corev3` (EXECUTED: `manager.go:…: undefined: structpb` — position varies with the insert, the error does not). The `*clusterv3.Cluster` carrier avoids the import but not the 10 callers. **The loop form avoids both.** The phase-38 `ScalarsFromStruct` projection (`manager.go:883`), the `Endpoint{…}` composite literal (`:884`), the `addr` precompute (`:885`) and `defaultSubset` (`:754`) all stay BYTE-UNCHANGED. Measured diff: **6 files, +128/−58** (vs 7 files, +136/−71 for the briefed form).

**Provenance — EXECUTED, not reasoned.** The new field must survive a connection-pool HIT or it would arrive zeroed on reuse while fresh-dial tests stay green. All **38** `Endpoint{` composite literals in production were classified (`grep -rn 'Endpoint{' --include='*.go' . | grep -v '_test\.go' | wc -l` ⇒ **38**; *an earlier draft said 39 with a breakdown summing to 37 — BOTH verifiers caught it independently, §16*): **exactly ONE populating literal repo-wide** (`manager.go:884`, isolated by `grep -rn 'Endpoint{[^}]'`), 3 comment mentions, **10** zero-value emit arguments (`connection.go:330`/`:464`/`:597`/`:699`, `h3dispatch.go:130`/`:210`/`:280`/`:341`, `h2dispatch.go:396`/`:530` — consistent with §11's "10 of 18 pass a literal `cluster.Endpoint{}`", which the 9 contradicted), 2 zero-value action returns, 2 router `picked :=` initializers, 17 `return Endpoint{}, …, err` sentinels, 3 container literals. 1+3+10+2+2+17+3 = **38**. **No path reconstructs an `Endpoint` from a cached address string** — the H1 pool HIT returns the stored struct verbatim (`cluster.go:715` `return p, p.ep, nil`), `addr` is only a map key, and H2 binds a fresh `c.lb.Pick(...)` threaded by value. A scratch test driving a real MISS → `PutIdleH1` → HIT **PASSES**, and **DISCRIMINATES**: breaking the hit to reconstruct fires the intended assertion (`POOL HIT: ClusterMetaLookup = (_, false), want true — the new field was ZEROED on the hit`), not an earlier one. *(Coverage limit named: the executed pool-hit test covers H1; H2/H3 rest on the exhaustive literal census plus reading.)*

**Resolve.** `ResolveCustomTags` (`resolve.go:46`, currently 5 params) grows a **SIXTH nil-tolerant param `clusterMetaLookup func(ns string) (*structpb.Value, bool)`** — shaped identically to `routeMetaLookup`/`hostMetaLookup`, appended after `hostMetaLookup`; the earlier three lookups are UNTOUCHED. The `kindMetadataCluster` arm clones the landed `kindMetadataHost` arm (`:131-160`) verbatim except the kind and the lookup name:
```go
var v *structpb.Value
var ok bool
if clusterMetaLookup != nil { v, ok = clusterMetaLookup(s.MetaNamespace) }
if ok { v, ok = descend(v, s.MetaPath) }   // the FULL MetaPath, NOT [1:]
```
then the shared default rule. **The FULL-`MetaPath` rule is EXECUTED-confirmed for CLUSTER** (R1x: a two-segment `[cl_nested, inner]` path resolved to the nested leaf), so CLUSTER joins ROUTE and HOST against REQUEST's `[1:]` Bucket artifact (`reference_route_metadata_resolve_full_metapath` now covers three of four kinds). `descend` + `structpbValueToString` REUSED VERBATIM; **NO new import**; `internal/tracing` stays filter-free AND cluster-free (BUILT: the layering grep is EMPTY).

**Where the closure is built.** NOT a new parameter on the three `emitAccessLog*` helpers: it is constructed LOCALLY inside each helper from the already-present `picked cluster.Endpoint` and passed as the 6th argument at `accesslog_emit.go:57`/`:118`/`:179`, as a METHOD VALUE `picked.ClusterMetaLookup` — the phase-72 shape exactly. **The three signatures and all 18 callers are UNCHANGED (sha256-proven).** Two things this seam is NOT justified by, both corrected in the landed ADR-0294 and restated here so they are not re-introduced: it is **not a cycle guard** (`internal/cluster` carries no `internal/tracing` edge, so a direct import builds clean — a self-imposed LAYERING rule the `go list -deps` gate asserts), and it is **not cheaper than the alternatives** (phase 72 MEASURED all three closure shapes at 1 alloc / 128 B). It is chosen for symmetry with `picked.MetaLookup` one argument to its left.

**The cost this SPEC records rather than hides**, carried forward from phase 72 and now doubled: the argument is evaluated unconditionally inside the sample block, before `ResolveCustomTags`'s `len(specs)==0` early return, so **every sampled span now gains +2 allocs / +256 B** (`picked.MetaLookup` + `picked.ClusterMetaLookup`) even when no `custom_tags` are configured. ACCEPTED — it matches how all four lookups are already threaded, and a sampled span already allocates ~16 attributes — with the mitigation named for a future profiling row: gate behind parse-time `hasHostTags`/`hasClusterTags` booleans. Naming it a second time is deliberate; it is the point at which the pattern stops being free.

### 3.5 D-CTMC-WIRE / D-CTMC-ZIPKIN-WIRE / D-CTMC-VALUE-SERIALIZE / D-CTMC-DEFAULT *(EXECUTED W1-W4, S6, S7, R2g/R2l)*

**Wire (W1).** The attribute key is the **LITERAL `tag` name** (not namespaced); the value is ALWAYS `stringValue`; all custom tags sit **strictly AFTER** the 16 built-ins (`node_id, zone, guid:x-request-id, http.url, http.method, downstream_cluster, user_agent, http.protocol, peer.address, request_size, response_size, component, upstream_cluster, upstream_cluster.name, http.status_code, response_flags`). **Intra-custom-tag order is ARBITRARY ACROSS PROCESSES** — a byte-identical bootstrap emitted `m,z,ctl_lit,a,q` twice and `q,a,ctl_lit,z,m` on a third fresh container (S11) — and `metadata`- and `literal`-kind tags interleave in that same arbitrary order. **Assertions MUST be key-based.** envoy-go's `upsertAttr`/`Span.Attrs` already are; the `0117` driver must be too.

**Namespace addressability (S6, W5).** **ANY `filter_metadata` namespace is addressable and `envoy.lb` is NOT privileged** — four namespaces resolved in one config, including one with no dot at all (`plainns`) and `envoy.lb` itself as an ordinary peer. This extends the phase-72 HOST finding to CLUSTER (`reference_envoy_lb_namespace_not_privileged`), and the `0117` fixture uses a deliberately arbitrary namespace for exactly that reason.

**Source distinctness (W5a/W5b).** With the SAME namespace+key planted at three levels with three different values, four tags sharing one `metadata_key` resolved **fully source-distinct**: CLUSTER→the cluster value, HOST→the endpoint value, ROUTE→the route value, REQUEST→its default. W5b is the strong form: with the namespace present on the cluster but the KEY absent (and the key present on both the endpoint and the route), the CLUSTER tag still fell to `default_value` — ruling out namespace-level AND value-level fallback.

**Cluster-adjacent siblings (W6).** `LocalityLbEndpoints.metadata` contributes **NOTHING** to a CLUSTER tag (present-only → default; same key at both levels → the CLUSTER value wins), re-confirming the phase-72 HOST ruling for the fourth kind. And `ClusterLoadAssignment.metadata` **does not exist as a field** at the pin — `no such field: 'metadata'`, a boot reject — so that question is vacuous, not negative. ⇒ **A CLUSTER-kind `MetadataKey` has exactly ONE source: `Cluster.metadata.filter_metadata[<ns>]`.** No departure owed, and the retention shape needs no merge.

**Default rule (W4, S7, R2g/R2l), identical to the `request_header` rule phases 70/71/72 landed.** A resolved value WINS over `default_value` — **including a resolved EMPTY STRING, which emits `""` and does NOT fall through to a configured non-empty default** (the rule is PRESENCE, not truthiness — the decisive edge, EXECUTED both ways). An absent namespace, an unresolvable FIRST segment, an unresolvable LATER segment and an unresolvable LAST-of-three all → `default_value` when non-empty and **OMIT the attribute ENTIRELY** when not. Omission was proven positively rather than by absence-of-evidence: in the same OTLP payload the empty built-ins `node_id`/`zone` appear as attributes with genuinely empty values (raw bytes `\n\x07node_id\x12\x02\n\x00`), while the no-default tags have **count 0** — so OTLP demonstrably *can* carry an empty attribute and chose not to emit these. An explicitly-empty `default_value: ""` is indistinguishable from no default. Multi-segment nested paths walk. `HasDefault = DefaultValue != ""` is consistent with every observation.

**Serialization (W3) — ZERO CLUSTER-specific difference; reuse `structpbValueToString` VERBATIM.** The probe used **JSON bootstraps** rather than YAML, so genuine `structpb` numeric/bool/null types were expressible and the YAML integer-or-string trap never applied. Confirmed matching envoy-go's landed renderer: string→raw unquoted · present-empty→`""` emitted · embedded `"`/`\`/`\n`/`\t`→raw, unescaped · bool→`true`/`false` · small/integral numbers→plain decimal · struct/list→compact JSON with no whitespace · deep and empty structs/lists → `{"l1":{"l2":{"l3":"deepv"}}}` / `{}` / `[]` · terminal `null`→UNRESOLVABLE (default-or-omit) · nested `null`→JSON `null` inside the value · absent key / absent namespace / unresolvable segment / descend-into-a-scalar / descend-into-a-null → default-or-omit.

**Three PRE-EXISTING divergences re-confirmed, one of them newly QUANTIFIED lower than phase 72 recorded:**
1. **Top-level scalar numbers render at ~6 significant digits** on the reference where Go's `protojson` is full-precision — and **the boundary bites far earlier than phase 72's `9007199254740992`→`9.0072e+15` example suggested**: `1000000`→`1e+06` and `1234567`→`1.23457e+06` (`999999` still renders plain). A fixture author reading only phase 72's example would think six-digit integers were safe. They are not.
2. **A struct with 2+ keys serializes in an ARBITRARY key order** — three fresh processes gave `d,b,c,a` / `a,d,b,c` / `a,b,d,c` for a config-ordered `b,a,c,d` — where Go always SORTS.
3. **Numbers NESTED in a struct/list use a DIFFERENT renderer** than top-level scalars (full precision for integrals; `0.33333333333333331` at 17 digits vs Go's `0.3333333333333333`).

All three are PRE-EXISTING (the same reference code path serves REQUEST/ROUTE/HOST) and none is CLUSTER-specific. **They constrain the fixture, not the code: `0117` asserts STRING values only** (`reference_structpb_tag_cross_side_string_only`).

**Zipkin (W2) — the row's ONLY Zipkin evidence, since no Zipkin fixture ships.** The tag appears in the Zipkin v2 `tags` map under the literal name as a JSON string, **byte-identical to OTLP across the entire value matrix** (plain string, embedded quotes/backslashes/newlines/tabs, bools, every number rendering `42`/`9.0072e+15`/`1e+20`/`0.5`/`1.23457e+06`/`-0`/`0.333333`, single/multi/deep/empty structs, lists, empty lists, nested `null`) and for the whole default/omit rule. **ONE divergence, RE-CONFIRMED and proven GENERIC:** the reference's Zipkin serializer **DROPS a tag whose resolved value is the EMPTY STRING**, where OTLP emits `""`. The discriminator is in the same payload — the empty built-ins `node_id` and `zone` are dropped too — so it is a property of the reference's Zipkin serializer, not of the custom-tag seam or the CLUSTER kind, and it equally affects the landed REQUEST/ROUTE/HOST kinds. Recorded as a documented boundary (§9 B4); envoy-go's shared `Attrs` seam emits uniformly on both exporters and is NOT changed by this row. *(Incidental, worth knowing for probe design: a `literal` tag with `value: ""` is PGV-rejected at boot, so a literal positive control can never itself be empty.)*

### 3.6 D-CTMC-NOCLUSTER + D-CTMC-REJECT — the departure/parity roster *(EXECUTED R1-R4, S1-S8; RE-DERIVED across all 18 sites)*

**The reject roster — and it is NOT a two-arm roster.** The reference ACCEPTS a `CLUSTER` metadata tag (validate exit 0 AND a real boot serving 200 with a live span) ⇒ lifting envoy-go's reject removes a genuine departure. The R-fleet then enumerated every remaining `metadata`-tag configuration envoy-go rejects and probed each:

| envoy-go error substring | trigger | reference | verdict | arm |
|---|---|---|---|---|
| `custom_tags empty tag` | `tag: ""` | REJECT (`CustomTagValidationError.Tag`) | **PARITY** | R2f |
| `custom_tags tag %q missing type` | no type oneof | REJECT (`field: "type", reason: is required`) | **PARITY** | R2k |
| `... kind required` (`config.go:233`, `k == nil`) | `metadata:` block with **no `kind`** | **ACCEPT** — boots, emits `default_value` | **DEPARTURE** | R3a |
| — same arm, same string | `metadata: {}` entirely empty | **ACCEPT** — boots, tag omitted (no default) | **DEPARTURE** (same arm, second spelling) | R3c |
| `... kind required` (`config.go:288`, `default:`) | `kind: {}` — present, oneof unset | REJECT (`MetadataValidationError.Kind … is required`) | **PARITY** | R3b |
| `... empty namespace` | `metadata_key` **present** with `key: ""`, absent `key`, or `{}` | REJECT (`MetadataKeyValidationError.Key`) | **PARITY** | R2a/R2h/R2j |
| `... empty namespace` | **`metadata_key` entirely ABSENT** | **ACCEPT** — boots, emits `default_value` | **⚠️ DEPARTURE — NEW, never recorded** | R2e |
| `... empty path` | `path` absent or `path: []` | REJECT (`MetadataKeyValidationError.Path`) | **PARITY** | R2b/R2c |
| `... empty path segment` | `- key: ""` or `- {}` | REJECT (`PathSegmentValidationError.Key` / `field: "segment"`) | **PARITY** | R2d/R2i |

⇒ **After phase 73, TWO envoy-go reject ARMS remain DEPARTURES, across THREE probed config spellings** — and the SECOND arm is NEW (S3). **⚠️ Count the unit explicitly, because an earlier draft did not and the mismatch propagated into four documents (§16):** the departures are (a) **`config.go:233`**, the fully-unset `kind`, reachable by TWO distinct config spellings (`metadata:` with no `kind` field, and `metadata: {}` entirely empty), and (b) **the `empty namespace` arm when `metadata_key` is ENTIRELY ABSENT** — one spelling, and **never before recorded**. That is **2 arms / 3 spellings**, not "three arms" and not "three entries". The prior framing this replaces — *"the ONLY remaining reject arms are the two unset-kind ones"* — counted two REJECT arms of which only ONE was a departure, so the baseline and the new figure were never in the same unit to begin with. (b) was invisible because **envoy-go emits the identical `empty namespace` string for a present-but-empty `metadata_key` (PARITY) and an omitted one (DEPARTURE)** — the reference distinguishes them by message PRESENCE, not content. And a second string collision is created BY this row: after the lift, `config.go:233` and `config.go:288` both emit `kind required`, so the landed *"reject LOUDLY with **distinct** substrings"* claim goes false in its own right (§11). **Both collisions are recorded as documented boundaries and NEITHER is fixed here** — splitting them would change landed error strings and is a separate, deliberate decision (§13).

**The no-cluster behavior — a first-class contract clause.** All 18 emit sites were RE-ENUMERATED at tip with their `picked` argument. **Partition by SPAN-CAPABILITY first** (`reference_host_tag_resolves_at_pick_time`) — the span block is gated on `traceDecision != nil && traceDecision.Sample` (`accesslog_emit.go:30`/`:89`/`:151`), and `traceDecision` is assigned at `connection.go:553` / `h2dispatch.go:449` / `h3dispatch.go:255`, so every site above its file's assignment can never produce a span at all: **6 PRE-Decide** (`connection.go:330`/`:464`, `h2dispatch.go:313`/`:396`, `h3dispatch.go:130`/`:210`) + **12 span-capable** (7 carrying a real `picked`, 5 carrying the zero endpoint). Full roster at §11.

| class | reference (EXECUTED) | envoy-go (RE-DERIVED) | verdict |
|---|---|---|---|
| cluster selected, host picked, 200 | tag RESOLVES | resolves | **PARITY** |
| `direct_response` / post-Decode local reply | span emitted; tag → default/omit | span-capable, zero endpoint ⇒ default/omit | **PARITY** (S4, R4a) |
| **cluster selected, host PICKED, connect REFUSED (503 `UF`)** | **tag RESOLVES** from the picked host's cluster | zero endpoint ⇒ default/omit | **DEPARTURE** (S1, W7a — two fleets) |
| cluster selected, **circuit breaker trips pre-pick (503 `UO`)** | tag → `default_value` | admission gate precedes the pick ⇒ zero endpoint ⇒ default/omit | **PARITY** (S8 — see the scope note) |
| cluster selected, ZERO hosts (503 `UH`) | tag → `default_value` | **config BOOT-REJECTED** (`zero endpoints across all locality groups`) | **N/A — no envoy-go counterpart** (S5/S5b) |
| no route match (404 `NR`) | span emitted; tag → default/omit | **NO SPAN AT ALL** — the no-match sites are PRE-Decide | **N/A — a PRE-EXISTING documented departure**, not a CLUSTER-tag disposition (S2, R4c) |
| `cluster_header` unresolvable (503 `NC`) | span emitted; tag → default/omit | not probed on envoy-go's side | **UNCLASSIFIED — do not write it up** (R4b) |

**The DEPARTURE, scoped exactly (§9 B2 extension).** Phase 73 makes a CLUSTER-kind tag the **FIFTH consumer** of the PRE-EXISTING zero-`picked` gap — after the three access-log upstream-host consumers and phase 72's HOST tag. It is EXECUTED-proven for the **connect-failure class ONLY**; **pool-overflow remains UNPROBED on the reference**; and the row does not widen the gap by one site. **Three claims from the BRAINSTORM are corrected here, not softened:**
- **"WIDER than B2 is for HOST" — already retracted at the BRAINSTORM and confirmed retracted:** seam A fires on exactly B2's site set, both gated on `picked.IsZero()`.
- **"the same breadth with a materially worse justification" — REFUTED.** The reference itself declines to resolve a CLUSTER tag when no host was picked (S5/S5b/S8). "No endpoint picked ⇒ no cluster metadata" is the reference's rule, not envoy-go's excuse.
- **Circuit-breaker is now PARITY, not an unprobed departure candidate (S12).** ⚠️ Scope: the REFERENCE half is EXECUTED (S8, `UO`, all-zero breaker, tag → default). envoy-go's half is RE-DERIVED from the landed B2 clause's own citations (`TryAcquireRequest` precedes `AcquireH1`/`AcquireH2Stream`), **not cross-side probed**. Stated this narrowly on purpose: an over-broad parity claim is exactly what phase 72 had to retract in review.

### 3.7 `clusters[].metadata` is INERT today — a knob-lift, not a bug-fix *(EXECUTED R6)*

Booting a config carrying `clusters[].metadata.filter_metadata` with **zero** `custom_tags` and driving traffic, against a control with the block removed: the **stat-name sets are byte-identical** (669 lines each, `diff` empty), the **span tag-key sets are identical**, and zero stats mention the namespace or value. The block round-trips verbatim into `/config_dump` and does nothing else observable. ⇒ **`clusters[].metadata` is accepted-and-inert at this pin absent a CLUSTER-kind tag** — unlike `access_log[].filter`, nothing silently misbehaves today, so **this row is a knob-lift, not a bug-fix**, and saying so plainly is part of the honest case for it. *(Coverage limit named: probed without `lb_subset_config` and without `typed_filter_metadata`; R6 establishes inertness for the configuration probed, not universally.)*

---

## 4. Framework primitives — 0 new packages, 0 new go.mod modules

No new interface and no new package-level type beyond the `kindMetadataCluster` const, the `clusterMetaLookup` parameter, and one added field + one added accessor on `cluster.Endpoint`. Everything else (`descend`, `structpbValueToString`, `CustomTagSpec`'s metadata fields, `MetaLookup`, the `Span.Attrs`/both-exporter seams, the `otlptrace` receiver) is pre-existing and REUSED. **NO file gains an import** (`cluster.go` already has `structpb` at `:21`; the `buildCluster` loop introduces no new type name in `manager.go`; `config.go`/`resolve.go`/`accesslog_emit.go` gain none). ⇒ **+0 go.mod modules**, EXECUTION-verified (`go mod tidy -diff` EMPTY). The layering check holds: `go list -deps ./internal/tracing` shows **no `internal/filter` and no `internal/cluster` edge** (EXECUTED). **This is a DISCIPLINE check, not a cycle check** — neither edge would cycle; do not re-introduce the refuted "cycle guard" justification. Re-run at the IMPL with `go list -deps`, no `...` (`reference_xds_config_seam_transitive_cycle_guard`).

## 5. Identifier hygiene *(collision checks — RE-DERIVED at `d51d33dc`, independently by two agents)*

`grep -rn 'kindMetadataCluster' internal/` → **0** · `grep -rn 'clusterMetaLookup' internal/` → **0** · `grep -rnE '(^|[^A-Za-z])ClusterMetaLookup' internal/` → **0** · `clusterFilterMetadata` as an `Endpoint` field → **0**. All four drafted identifiers are free (`reference_spec_drafted_identifier_collision_check`). ⚠️ Use the word-boundary form for the accessor grep — phase 72 recorded that a naive substring `grep MetaLookup` returns 51 hits that are all `RouteMetaLookup`, and the same trap now also catches `MetaLookup` itself. **Re-run every grep at the PLAN and again at the IMPL tip** (`feedback_parallel_stream_mints_fresh_drift`).

## 6. Reject roster — added / retained

**ADDED: none.** **REMOVED: one** — the `cluster kind unsupported` arm (`config.go:268-269`), the LAST kind-specific reject. **RETAINED unchanged:** `kind required` ×2 (`:233` sub-case A — a **DEPARTURE**, reachable by two config spellings; `:288` sub-case B — **PARITY**); the empty-namespace reject (`empty namespace`) — **PARITY when `metadata_key` is present-but-empty, a NEW DEPARTURE when it is entirely absent**, the two sharing one error string; the empty-path and empty-path-segment rejects (PGV-PARITY); the empty-`tag` and missing-type rejects (PARITY); the first-wins dedup. ⇒ **TWO of the retained arms are DEPARTURES (`:233`, and `empty namespace` in its absent-`metadata_key` spelling); everything else is PARITY.** **Neither string collision is split here** (§13).

## 7. Stat surface **+0** · Fuzz **+0 fuzzers, a seed ADD plus a MANDATORY REPOINT** *(D-CTMC-STATS / D-CTMC-FUZZSEED)*

**Stats +0 → 1201.** A span attribute registers no stat; the tag key is a config literal used as a span-attribute key, not a stat name (`reference_dynamic_stat_name_charset_guard` does not apply). Per the phase-71 V2 finding, tracing rows carry **no `TestNoNewStat`-style guard obligation** — do not manufacture a cited-but-unwritten one.

**Fuzz +0 → 55.** `f.Add` is not `^func Fuzz` (`reference_fuzzer_count_docs_drift`). Two edits to `withMetaTags` (`fuzz_test.go:99-122`, whose four tags sit at `:102` `meta_ok`/REQUEST, `:107` `meta_route_ok`/ROUTE, `:112` `meta_host_ok`/HOST, `:117` `meta_bad`/CLUSTER — **`meta_bad` is LAST**):

1. **ADD `meta_cluster_ok`** — a valid `MetadataKind_Cluster_` + `MetadataKey` — exercising the new accept arm.
2. **REPOINT `meta_bad`** — and **the BRAINSTORM's proposed target is the wrong one (S4).** The seed carries a CLUSTER kind and **no `MetadataKey` at all** (`:117-119`), so after the lift it falls through the accept arm and rejects `empty namespace`: **still red, silently, nothing goes green, no test catches it.** Worse, that exact shape is the R2e **DEPARTURE**, so leaving it converts a parity-reject seed into a departure-shaped one. The BRAINSTORM's proposed nil/absent-`kind` target is the OTHER departure (R3a). **Repoint to `Kind: &metadatav3.MetadataKind{}`** — the kind message PRESENT with its oneof unset: envoy-go rejects at `config.go:288` (`kind required`), the reference PGV-rejects `field: "kind", reason: is required`. **PARITY, EXECUTED both sides** (R3b). *(Equally valid parity alternatives, also executed: `MetadataKey: &metadatav3.MetadataKey{}` → `empty namespace`; a key-set/path-empty shape → `empty path`.)*

**Dispatch-verify BOTH, with the discrimination stated BEFORE the run** (`reference_probe_must_discriminate`; phase 72's T5 is the template): that `meta_cluster_ok` reaches the `kindMetadataCluster` ACCEPT arm, AND that the repointed `meta_bad` reaches the `config.go:288` reject — **not merely that it still rejects**, since it rejects either way. ⚠️ The **first-error-return trap**: `parseCustomTags` returns on the first error, and `meta_bad` is LAST, so seeing the later seed's instrumentation line at all proves the earlier ones did not short-circuit. Parse precedes the `provider required` check. **Same task: narrow the stale `:89-98` doc comment** (`reference_code_comment_not_evidence`).

## 8. Differential fixture — ONE new `0117-tracing-custom-tags-metadata-cluster` (OTLP) *(D-CTMC-FIXTURE — DECIDED)*

**DECISION: ONE new OTLP fixture, NO Zipkin dir** (the 59/62/63/70/71/72 precedent; the `Span.Attrs` seam is shared and W2 proved the Zipkin emit LIVE across the full value matrix). Cloned from the `0116` chassis — 5 files (`driver/driver.go`, `envoy.yaml`, `envoy-go.yaml`, `expectations.yaml`, `README.md`) + a **one-line blank-import registration** at `test/differential/runner_test.go`. `BackendKind` stays `HTTPFixedBody` (+0, tail **38**), **`BackendCount` stays 1** (`reference_differential_backendcount_min_one`), NO writer (the source is static config on both sides), reference port **10117** (free — 0 hits under `test/`).

**⚠️ TWO CLUSTERS, NOT ONE — the S10 decision.** `0116` shipped with a limitation it had to confess in review: a single endpoint cannot discriminate *selected* from *only*. The cluster analogue is avoidable for almost nothing:

- **`c_backend_a`** and **`c_backend_b`**, both `type: STRICT_DNS` (reference) / `STATIC` (subject), **both pointing at the SAME single `HTTPFixedBody` backend host:port**, differing ONLY in `metadata.filter_metadata[<ns>][cl_k]` — `v-cluster-a-0117` vs `v-cluster-b-0117`.
- **Two routes**: `prefix: /a` → `c_backend_a`, `prefix: /b` → `c_backend_b`. Deterministic by PATH, so **no LB spread enters the assertion** (`reference_round_robin_offset_randomized` never engages) and `BackendCount` stays **1**.
- Drive BOTH paths; assert each span carries its OWN cluster's value. **This DISCRIMINATES selected-from-only** and cross-side-proves the per-request selection EXECUTED at S3 — a strictly better outcome than naming the limitation in `expectations.yaml`.

**Two CLUSTER tags on the single HCM**, asserted cross-side EXACT key+value, **key-based** (W1/S11: intra-tag order is arbitrary per process): `cl_hit` → `metadata_key{key:<ns>, path:[cl_k]}`, resolving to the per-cluster STRING; `cl_default` → an absent path segment, falling to `default_value`. Four assertions across two paths.

**⚠️ The namespace must NOT be `envoy.lb`** — S6/W5 proved any namespace is addressable and `envoy.lb` is ordinary; use a deliberately arbitrary one (`envoy.test`, as `0116` did) so the fixture is the cross-side proof of that generality (`reference_envoy_lb_namespace_not_privileged`).

**⚠️ STRING values only.** §3.5's three boundaries constrain the fixture: arbitrary multi-key struct key order, ~6-significant-digit top-level scalar numbers (**biting from `1000000` — earlier than phase 72's example implied**), and a different renderer for nested numbers. A struct-valued or numeric tag is NOT cross-side comparable (`reference_structpb_tag_cross_side_string_only`).

**Inherited cross-side asymmetry, already proven safe:** the reference clusters are `STRICT_DNS` (host.docker.internal, ADR-0010) while envoy-go's are `STATIC`. `0064-lb-subset` and `0116` are the landed precedents for metadata surviving that asymmetry; here the metadata sits on the CLUSTER rather than the endpoint, so DNS resolution is not even in the path.

**Break protocol:** commit FIRST (`reference_break_protocol_commit_first`), run breaks with `-count=1` (`reference_differential_break_protocol_count1`), use the FULL subtest selector `TestDifferential/0117-tracing-custom-tags-metadata-cluster` (`reference_differential_run_selector`), and confirm WHICH assertion fires per break (`reference_deliberate_break_wrong_assertion`). **Minimum FOUR breaks:** (N) the static-value swap on ONE cluster only — must fire subject-side only, proving the green is not a shared-constant tautology; (O) the default-drop — must fire the PRESENCE branch with ZERO value-mismatch firings, discriminating OMIT from emitted-as-`""`; (P) **the anti-vacuity control** — repoint `cl_hit` at an absent namespace, which must fire `cl_hit`'s assertions while `cl_default`'s fire ZERO, so the green depends on the cluster-metadata binding rather than a default match that would pass regardless; (Q) **the selection break, unique to this fixture** — swap the two clusters' metadata values, which must fire on BOTH paths and is the only break that proves *selected*-not-*only*. A break that does not compile proves nothing — substitute an equivalent and say so (`reference_plan_break_instructions_dont_compile`).

## 9. Behavior-contract edit map — pinned *(all edits land at the IMPL, atomically with ADR-0295)*

- **B1** — `BEHAVIOR_CONTRACT.md:688` (the `custom_tags` paragraph): the `metadata` type flips to CONSUMED for **all four** MetadataKinds, naming the CLUSTER source as the owning cluster's `clusters[].metadata.filter_metadata[ns]` reached over the new `cluster.Endpoint.ClusterMetaLookup()`; "Three of the four … only `CLUSTER` remains reject-only" → all four consumed; the FULL-`MetaPath` sentence extends to CLUSTER (three of four kinds walk the full path; only REQUEST pre-keys on `path[0]`). ⚠️ **HIGHLY DELICATE** — this is one **3866-char** paragraph (`awk 'NR==688{print length($0)}'`) interleaving the false claims with still-true ones (the `literal`/`request_header`/`environment` semantics, the shared serialization rule, the default/omit rule, the four-arm locality proof, most of the PGV-parity clause). The load-bearing sentence is:
> `` The `CLUSTER`/unset-kind (sub-case A — a fully-unset `MetadataKind`) arms reject loudly (envoy-go-strict DEPARTURE — the reference boots them); the empty-namespace/empty-path/empty-segment and present-but-empty-oneof (sub-case B) rejects are PGV-PARITY (both reject). ``

⚠️ **Its first half is false AND its second half is PARTLY false** — an earlier draft of this bullet called the second half still-true, which would have left a wrong claim standing. S3/R2e establishes that **`empty-namespace` is a DEPARTURE in its absent-`metadata_key` spelling**, so the PGV-PARITY list is over-broad by one arm and must be split, not merely trimmed at the front. This is the phase-72 hazard recurring; edit with surrounding context, never on a bare fragment, and apply B1+B2 as one `old_string`.
- **B2** — the departure-roster narrow, to **TWO reject ARMS across THREE config spellings** (§3.6): `config.go:233`'s fully-unset `kind` (two spellings — no `kind`, and `metadata: {}`) and the **entirely-absent `metadata_key`** (one spelling), the latter NEW and never before recorded. ⚠️ **B1 and B2 EDIT THE SAME PHYSICAL LINE** — the departure roster B2 narrows lives inside `BEHAVIOR_CONTRACT.md:688`, the same 3866-char paragraph B1 edits (`grep -n 'unset-kind'` returns exactly one in-scope hit, at `:688`). **Apply B1 and B2 as ONE `old_string`**, or the second will fail against text the first already rewrote. Plus the note that envoy-go emits ONE error string across a PARITY case and a DEPARTURE case (`empty namespace`), and that after this row the two surviving `kind required` arms also share a string — **two documented string collisions, neither fixed here.**
- **B3** — the phase-72 B2 clause (`BEHAVIOR_CONTRACT.md:690`) EXTENDED, not duplicated: a CLUSTER-kind tag becomes the FIFTH consumer of the same PRE-EXISTING zero-`picked` gap, at the SAME breadth and the SAME site set. **Two narrowings land with it:** (i) the reference's rule is EXECUTED-established as *pick-gated for CLUSTER too* — cluster selected + no host picked ⇒ `default_value` on three independent arms — so the "worse justification" framing is retired; (ii) the **circuit-breaker arm moves from "likely parity, unprobed" to PARITY on EXECUTED reference evidence** (S8), narrowing the unprobed set to pool-overflow alone. Scope (ii) exactly: reference EXECUTED, envoy-go RE-DERIVED.
- **B4** — the documented BOUNDARIES paragraph (`:692`): "shared by all three MetadataKinds" → **four**; the Zipkin empty-string drop RE-CONFIRMED and proven generic via the `node_id`/`zone` built-ins; the scalar-number precision boundary **re-quantified as biting from `1000000`**; the arbitrary struct-key order re-confirmed across three fresh processes; and the intra-custom-tag emit order sharpened to **arbitrary per process**.
- **B5** — `BEHAVIOR_CONTRACT.md:745` (the deferred-field list): the tracing `metadata` boundary line narrows to the three unset/absent arms. ⚠️ DELICATE — this bullet mixes the false metadata-kind roster with the still-true `spawn_upstream_span`/`http_service`/`resource_detectors`/`sampler` strict-reject list AND the still-true B2 clause. The three "no span on a 404 no-route" clauses (`:690`, `:714`, `:744`) all STAY TRUE and constrain the fixture: a no-route arm is not expressible.

## 10. Test plan + task surface *(D-CTMC-SPLIT — DECISION: a SINGLE FLAT ROW of NINE tasks)*

**DECISION: a SINGLE FLAT ROW of NINE tasks (T1-T9)** (ADR-0045 valve armable-but-unconsumed). The seam decision removed the one thing that could have forced a split: seam A's `internal/cluster` change is a near-verbatim phase-72 clone with **zero** behavioral disturbance (provenance EXECUTED-clean, §3.4), and the seam-B/hybrid shapes that would have cost 12-13 tasks are rejected on fidelity. The measured production footprint is a near-exact clone of the landed phase-72 IMPL, which shipped in nine.

Anticipated TDD spine, each task red-first → green → break-verified:
- **T1** — `Endpoint.clusterFilterMetadata` + the nil-safe `ClusterMetaLookup` accessor + the `buildCluster` populate LOOP (`extractEndpoints`, `manager_test.go`, `manager.go:883`/`:884`/`:885`/`:754` and `subset.go` all byte-unchanged; ZERO new imports; sizeof 112→120; Step 0 re-runs the four collision greps). ⚠️ **The guard break must assert on the ACCESSOR'S OWN RETURN**, not through a span — the phase-72 F5b finding: `descend` on a `NewStructValue(nil)` wrapper returns `(nil,false)` without panicking, and `MetaPath` is min-1 by parse validation, so a span-routed guard break is VACUOUS. ⚠️ **A method value on the ZERO `Endpoint` is NON-nil** — do not write a test that conflates the nil-lookup mechanism with the zero-`Endpoint` mechanism (`reference_endpoint_metalookup_host_metadata_seam`). Carry the pool-HIT provenance test.
- **T2** — `kindMetadataCluster` APPENDED at **iota==6** + the CLUSTER accept arm replacing the `:268-269` reject + the `config.go` and `config_test.go` stale comments. **Pre-known red: EXACTLY TWO `/cluster` subtests** (`config_test.go:639` in `TestNewConfigRejectCustomTagMetadata_RejectKinds`, `:764` in `…MetadataRoute_RejectKinds`), the failure printing `Kind:6`. ⚠️ **`:639`/`:764` are the TABLE-ROW lines — the correct `old_string` targets — while the failure output prints at `:648` and `:773`, the `t.Fatalf` lines.** Do not read the two pairs as a contradiction. ⚠️ Those two table lines are **byte-identical to each other** (`grep -Fxc` ⇒ 2) — an `old_string` on either alone is NOT unique; include surrounding context. Both tables degenerate to a single `unset-kind` row; their doc comments (`:627-632`, `:754-757`) go false. The sibling unset rows STAY rejecting and are the live regression guard. Add the symmetric `kindMetadataCluster != 6` pin beside the landed `config_test.go:856-867` pins, which assert that `kindMetadataHost` **IS** 5 and `kindMetadataRoute` **IS** 4 (the source spells them as `!=` guards).
- **T3** — the `clusterMetaLookup` 6th param + the `kindMetadataCluster` resolve arm (FULL `MetaPath`) + **49** trailing-`nil` `resolve_test.go` callers (raw grep 50; `:107` is a `t.Errorf` string literal, `:106` is its real call — re-grep and gate by line, do NOT adopt 50). ⚠️ Entry state: after T3 the `internal/filter/hcm` package does NOT compile (`not enough arguments in call to tracing.ResolveCustomTags` ×3). That is the **`ResolveCustomTags` arity** changing exactly as in 70/71/72 — do not conflate it with the EMIT-signature arity, which is genuinely unchanged and is the row's cost claim (phase 72's V1 M2 caught precisely this conflation).
- **T4** — `picked.ClusterMetaLookup` appended at `accesslog_emit.go:57`/`:118`/`:179` and **NOWHERE ELSE**; live per-site H1/H2/H3 span tests + a first-class zero-`picked` test (the **5** span-capable zero sites, not the raw 10). **The envelope gate is TWO-PART and the parts are not interchangeable** (§1, §11): (a) **SHA256 byte-identity on SEVEN production files** — `connection.go`, `h2dispatch.go`, `h3dispatch.go`, `chain.go`, `subset.go`, `actions.go`, `route.go`; (b) **CALLER-SET invariance, NOT byte-identity, on `accesslog_emit_test.go` and `span_emit_test.go`**, since this very task ADDS tests to both — assert the caller counts hold at 13 and 21, that the three emit signatures are unchanged, and that a `diff` against the old blobs yields ZERO `<` lines (purely additive, the phase-72 stronger test). A sha256 gate on those two is unexecutable by construction and an earlier draft asserted one.
- **T5** — the `meta_cluster_ok` seed ADD + the **`meta_bad` REPOINT to `Kind: &metadatav3.MetadataKind{}`** + the stale `:89-98` comment narrow; dispatch-verify BOTH arms with the discrimination stated first (§7).
- **T6** — the `0117` two-cluster fixture + breaks N/O/P/**Q** (§8).
- **T7** — BEHAVIOR_CONTRACT B1-B5 (§9).
- **T8** — the six-gate + a SHAPE-pinned `internal/cluster` envelope audit (§11).
- **T9** — ADR-0295 completed IN PLACE + ROADMAP row-73-`done` + the deferred-sentence narrow + STATE/router + **the memory writes (§15)**. ⚠️ **The ROADMAP row-73 cell carries THREE stale claims, not one** (§12): its ⚠️ narrow-hazard sentence (refuted by S6), its *"the ONLY remaining reject arms are the two unset-kind ones"* clause (refuted by S3), and its `0117` fixture description as *"a static `clusters[].metadata` hit arm + a `default_value` arm"* (superseded by S10's TWO-CLUSTER decision). Correct all three; do not propagate any of them.

**⚠️ Six-gate hazards, all PRE-EXISTING:** (i) `TestOutlierDetector_ConcurrentEjectExactlyOnce` (`internal/cluster/outlier_test.go:766`) is a known intermittent `-race` failure reproduced on the UNMODIFIED baseline; `internal/cluster` is a package this row touches, so its `-race` leg is load-bearing. **Isolate-re-run; do NOT re-classify** (`reference_cluster_race_outlier_flake`). (ii) The full-suite startup flake (`reference_differential_fullsuite_startup_flake`). (iii) The SDS init-fetch dial-budget flake (`reference_sds_init_fetch_timeout_dial_budget_flake`) — the costing agent hit it once under load. (iv) **NEWLY OBSERVED and NOT in the flake index:** `TestOptions_ZeroValue_NoOpDefaults` (`internal/httpclient`) and `TestServerConn_TinyWindowDelivery` (`internal/filter/hcm/h2`) each failed once under parallel load in packages that were **byte-identical to baseline** (`diff -r` exit 0) and were isolate-green at `-count=3`/`-count=5`. Recorded as load artifacts, NOT root-caused, and NOT attributed to this row.

## 11. Edit-site roster + the 18-caller classification *(RE-DERIVED at `d51d33dc`)*

**Production (5 files):** `internal/cluster/cluster.go` (the field beside `:79` + the accessor beside `:111-117`; NO new import) · `internal/cluster/manager.go` (the `buildCluster` populate loop after `:433`; `:754`/`:883`/`:884`/`:885` byte-unchanged) · `internal/tracing/config.go` (`:55-62` const, `:268-269` → accept, the stale comments) · `internal/tracing/resolve.go` (`:46` param, a new arm after `:131-160`, the `:12-45` doc) · `internal/filter/hcm/accesslog_emit.go:57`/`:118`/`:179`.
**Tests:** `internal/tracing/{config_test.go,resolve_test.go}` · `internal/cluster/cluster_test.go` · `internal/filter/hcm/{accesslog_emit_test.go,span_emit_test.go,fuzz_test.go}` *(the two emit test files are ADDITIVE-only — new tests, zero modified callers)*.
**Fixture + docs:** `test/fixtures/0117-…/` + `runner_test.go` + BEHAVIOR_CONTRACT + DECISIONS + ROADMAP + STATE + next-prompt.
**BYTE-UNTOUCHED, sha256-proven in two independent scratch builds — SEVEN production files:** `connection.go` · `h2dispatch.go` · `h3dispatch.go` · `internal/filter/http/chain.go` · `internal/cluster/subset.go` · **`internal/filter/hcm/actions.go`** · **`internal/filter/hcm/route.go`** — the last two matter because they are exactly what seam B would have touched. Plus `internal/cluster/manager_test.go`, `extractEndpoints`'s signature, and `internal/dynamicmetadata`/`internal/xds`/`internal/tls`/`internal/boot`/`internal/listener`/`internal/bootstrap`/`validate/`.
**CALLER-SET-INVARIANT (NOT byte-identical — T4 adds tests to both):** `accesslog_emit_test.go` (13 callers) and `span_emit_test.go` (21 callers) — **34** together. Gate them on caller count + unchanged emit signatures + a purely-additive `diff`, never on a hash (§10 T4).

**⚠️ The `internal/cluster` envelope is SHAPE-pinned, not byte-untouched** (the phase-72 precedent): one added field + one added accessor + one added populate loop, with `ScalarsFromStruct`, `defaultSubset`, the `envoy.lb` projection, the `Endpoint{…}` literal and `extractEndpoints`'s signature all byte-unchanged.

**The 18 emit sites, RE-ENUMERATED with their `picked` argument** (5 `connection.go` + 6 `h2dispatch.go` + 7 `h3dispatch.go`; every BRAINSTORM line number HELD). `traceDecision` is declared at `connection.go:317` / `h2dispatch.go:266` / `h3dispatch.go:122` and ASSIGNED at `connection.go:553` / `h2dispatch.go:449` / `h3dispatch.go:255`:

| file:line | span-capable? | `picked` | class |
|---|---|---|---|
| `connection.go:330` | **NO** (pre-Decide) | `Endpoint{}` | 404 no-match |
| `connection.go:464` | **NO** (pre-Decide) | `Endpoint{}` | defensive 500 |
| `h2dispatch.go:313` | **NO** (pre-Decide) | `picked` ≡ always zero (`actions.go:169`) | 404 no-match |
| `h2dispatch.go:396` | **NO** (pre-Decide) | `Endpoint{}` | defensive 500 |
| `h3dispatch.go:130` | **NO** (pre-Decide) | `Endpoint{}` | 404 no-match |
| `h3dispatch.go:210` | **NO** (pre-Decide) | `Endpoint{}` | defensive 500 |
| `connection.go:597` / `:699` | YES | `Endpoint{}` | local reply ×2 |
| `h2dispatch.go:530` | YES | `Endpoint{}` | local reply |
| `h3dispatch.go:280` / `:341` | YES | `Endpoint{}` | local reply ×2 |
| `connection.go:777` | YES | **`picked`** | H1 completed |
| `h2dispatch.go:577` / `:584` / `:613` | YES | **`picked`** | H2 completed |
| `h3dispatch.go:367` / `:373` / `:395` | YES | **`picked`** | H3 completed |

**18 = 6 PRE-Decide (never a span) + 12 span-capable.** Of the 12: **7 carry a real `picked`**, **5 carry the zero endpoint**. Syntactically 10 of 18 pass a literal `cluster.Endpoint{}` and `h2dispatch.go:313`'s `picked` is provably always zero — but 6 of those can never reach a span, so **the tracing-relevant zero-endpoint set is 5, not 11**. Separately, the 7 real-`picked` sites carry the ZERO endpoint on every upstream-ACQUIRE failure sub-path (§3.6).

## 12. Sentinel maintenance — a SENTENCE-NARROWING row, narrowed AT THE IMPL (the phase-57 precedent)

The live Observability `candidates:` sentence (`ROADMAP.md:203`) names *"tracing custom_tags `metadata` for the LAST unconsumed MetadataKind (`CLUSTER`, reject-only — …)"*. **NO deferred sentence is edited at this SPEC** — keeping EXACTLY ONE live match through the pre-IMPL stages is what makes check (2) meaningful (`reference_sentinel_deferred_sentence_live_vs_historical`: the live grep needs `candidates:` adjacent; `candidates **were**:` recaps are historical).

**The hazard is REAL but HALF ITS ADVERTISED SIZE (S6), and check (2) STAYS 3.** `CLUSTER` is the last item inside the **parenthetical**, but the `metadata` item is the FIRST of a four-member slash-list. The IMPL edit, pinned here with uniqueness verified (`count == 1` in the file at this tip — **re-verify at the IMPL tip**):

> **old_string** (leading space significant): `` + tracing custom_tags `metadata` for the LAST unconsumed MetadataKind (`CLUSTER`, reject-only — the `REQUEST` kind landed at phase 70/ADR-0292, the `ROUTE` kind at phase 71/ADR-0293, the `HOST` kind at phase 72/ADR-0294, so THREE of the four are now consumed)/`spawn_upstream_span`/`http_service`/force-trace.``
> **new_string:** `` + tracing `spawn_upstream_span`/`http_service`/force-trace.``

Dropping the bare `custom_tags` prefix is REQUIRED — `spawn_upstream_span`/`http_service`/force-trace are sibling *tracing* fields, not `custom_tags` sub-types, so retaining it would leave a malformed clause. **After the narrow the sentence still carries FOUR candidates** (the `ssl` stat family + those three), is well-formed, and retains the `candidates:` marker ⇒ **check (2) stays 3. No project-level event.**

**⚠️ THE ROADMAP ROW-73 CELL CARRIES THREE STALE CLAIMS, not one — all three corrected at the IMPL, none propagated.** An earlier draft of this section named only the first.
1. **The narrow hazard** — *"a narrow that REMOVES the clause rather than shortening it, since `CLUSTER` is its last item"* (`ROADMAP.md:135`, quote verified byte-accurate). REFUTED by S6 above.
2. **The reject roster** — *"after it lands the ONLY remaining reject arms are the two unset-kind ones (`config.go:232-233` DEPARTURE / `:287-288` PARITY, the phase-71 S3 split)"*. REFUTED by S3: a SECOND departure arm survives, riding the `empty namespace` reject.
3. **The fixture description** — *"ONE new OTLP fixture (`0117-…`, a static `clusters[].metadata` hit arm + a `default_value` arm, STRING values only, NO writer)"*. SUPERSEDED by S10's TWO-CLUSTER decision (§8).

⚠️ **The SAME stale narrow-hazard sentence also lives in `STATE.md`'s LIVE `next-skill:` block and in `next-prompt.txt`** — both of which this stage rewrites, so the real risk is verbatim carry-forward, not a missed edit. The phase-73 PLAN router must carry the CORRECTED form. (`STATE.md`'s `prior active-phase:` copies are HISTORICAL and must be left alone.) And per `reference_a_drift_correction_is_itself_a_claim`, **this SPEC's correction is itself a claim** — the IMPL re-runs the grep and re-verifies `count == 1` before building any `old_string` on it.

**At this SPEC close:** (1) `NOT DONE: row 73`; (2) ⇒ **3**; (3) `NEVER OPENED: gRPC/Runtime/WASM`. Does NOT fire; no `stop`.

## 13. Deferred items *(the BRAINSTORM §8 roster carried, MINUS this row's pickup, PLUS what this SPEC surfaced)*

Carried: `spawn_upstream_span` · OTel `http_service` / `sampler` / `resource_detectors` · tracing `verbose` · force-trace · the `ssl` stat family (ADR-0286 C3) · **`access_log[].filter`** (the strongest FUTURE pick — a live behavioral divergence, with the global sink-flattening and `headerCaptureSink` non-promotion hazards on record) · **`hcm.access_log_options`** and the **`stdout`/`stderr` access loggers** (both newly named at the BRAINSTORM) · `stats_flush_on_admin` · response-flags plumbing · all xDS and HTTP/3 candidates · the never-opened families · the diagnosed-but-unchartered global access-log sink flattening.

**NEWLY SURFACED by this SPEC:**
1. **The absent-`metadata_key` DEPARTURE** (§3.6, R2e) — the reference ACCEPTS a `metadata` tag with no `metadata_key` and emits `default_value`; envoy-go rejects `empty namespace`. A small, self-contained strictness row, and the first item any future "close the metadata departures" row must include.
2. **Two error-string COLLISIONS** — envoy-go emits `empty namespace` for both a PARITY case and a DEPARTURE case, and after this row emits `kind required` from both surviving unset arms. Splitting them would change landed error strings (ADR-0080 territory) and is a deliberate decision, not a cleanup; deferred with the evidence attached.
3. **The pick-time `picked`-propagation row (B2)** — unchanged in shape, but its cost/benefit is now better known: this row makes a CLUSTER tag its FIFTH consumer, **the circuit-breaker arm is now EXECUTED-PARITY on the reference (S8)** so a future row owes only the pool-overflow probe, and landing it would close phase 73's single residual divergence with **zero phase-73 rework**.
4. **Two unindexed test flakes** — `internal/httpclient TestOptions_ZeroValue_NoOpDefaults` and `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery`, each observed once under parallel load in byte-identical packages, isolate-green. Test-hardening candidates, not feature rows; recorded so a future six-gate does not misattribute them.
5. **`clusters[].typed_filter_metadata`** — never addressable by a `MetadataKey`, so not a gap; recorded so it is not re-litigated.
6. **The `upstream_cluster` span TAG** — carried explicitly here because §2 defers it while the BRAINSTORM §8 roster this section inherits does not list it, so §13 would otherwise read as complete while omitting it. It remains a SEPARATE unchartered feature blocked on the `accesslog_emit.go:51`/`:112`/`:173` framework gap (`reference_tracing_upstream_cluster_framework_gap`), and phase 72's category-error warning stands: this row does NOT bring it closer, and "seam C" (§3.3) is where the two would touch.

**⚠️ MEMORY WRITES OWED AT THIS CLOSE** — declared here because the SPEC would otherwise close with none, and TWO LANDED memories now carry claims THIS STAGE refuted:
- **`reference_endpoint_metalookup_host_metadata_seam`** states that *"a future `CLUSTER`-kind row RE-PAYS it: the selected cluster's identity is not at the emit seam."* **REFUTED** — seam A avoids the thread entirely, sha256-proven across seven files. The same memory carries the *"29 emit test callers"* figure, which is a PRE-phase-72 baseline (current: 34). Both must be corrected.
- A **NEW** memory for the pick-gate finding: *a CLUSTER-kind metadata tag is gated on the PICKED HOST, not the matched route — probe the no-pick classes, and note that a zero-endpoint arm does NOT discriminate the gate because it confounds "no pick" with "empty host set".* This is the reusable lesson, and it generalizes `reference_host_tag_resolves_at_pick_time` from HOST to the whole `metadata` family.

## 13.1 Deferred-item hygiene

§2 (non-purposes) and §13 must name the same set. Cross-checked at this close: every item §2 defers appears above, including the two §2 items the BRAINSTORM §8 roster does not carry (the `upstream_cluster` span TAG, item 6, and `typed_filter_metadata`, item 5).

## 14. ADR continuity — the ADR-0295 §Context DRAFT (anchored here; full entry at the phase-73 IMPL)

Per ADR-0044-as-used, **ADR-0295's §Context lands at THIS SPEC commit** (the DECISIONS tail flips ADR-0294 → ADR-0295 here); §Decision + §Consequences append IN PLACE at the phase-73 IMPL, **no renumber**; next-free becomes **ADR-0296**.

**⚠️ The SPEC-stage block shape is pinned by what ACTUALLY LANDED at phases 71 and 72** (both verified by `git show` of the SPEC commits, not by reading the SPEC's own description of itself — phase 72's SPEC draft carried an anticipated `### Decision` HEADING that its V2 caught before landing): heading line · a `> **STATUS: PROPOSED — §Context drafted at the phase-73 SPEC per ADR-0044.** …` blockquote · **exactly ONE** `### Context (drafted at the phase-73 SPEC)` heading · N context paragraphs, the last conventionally opening *"The design (§Decision anticipated): …"* · and the italic footer `*(§Decision + §Consequences land at the phase-73 IMPL.)*`. **NO `### Decision` heading. NO `### Consequences` heading.** The IMPL's `grep -c '^### Decision'` inside the block must read **0** before it appends — verified **0** at this close.

## 15. Exit — counts + expectations at SPEC-DONE

Docs-only: `SPEC.md` (new) + the ADR-0295 §Context append + STATE §Current rolled IN PLACE (lifecycle 1 → 2, lineage re-capped at five **with its PREAMBLE updated** — the ADR-0288 rule; a stale preamble was a REAL defect caught during phase 72) + `next-prompt.txt` rolled to the phase-73 **PLAN** (NOT the IMPL — the four-stage lifecycle) + **the two memory writes named at §13**. **ROADMAP UNTOUCHED**; row 73 STAYS `in-progress`. Counts UNCHANGED apart from the DECISIONS tail: fixtures **118** · fuzzers **55** · stat surface **1201** · BackendKind tail **38** · go.mod modules **2** · DECISIONS tail **ADR-0295** (next-free ADR-0296). Anticipated at the IMPL: fixtures **118 → 119**, everything else +0.

⚠️ **The ADR-0288 singleton check, CORRECTED — verify it with `grep -n`, not `grep -c`.** At master and at this tip, `grep -c 'next-skill:'` / `'lifecycle-state:'` / `'next-free ADR:'` in `STATE.md` each return **2**, not 1: one hit is the §Current live pointer, the other is `STATE.md:7`, the ADR-0288 RULE STATEMENT itself, which names all three tokens in backticks. **The invariant is "exactly ONE LIVE pointer, in §Current", so the correct post-roll expectation is 2 with the second hit inside §Current** — a stage that "fixed" the count to 1 would delete the rule.

## 16. Adversarial-pass record

TWO independent verifiers ran against the COMMITTED draft (`66b23e71`) in PRIVATE scratch before the squash-push (`reference_parallel_subagents_private_scratch`), on disjoint remits. **Both found real defects. ZERO SEVERE, TWELVE MODERATE, ELEVEN MINOR — every one folded in ABOVE at the point of use; this section is the ledger, written from what they found rather than asserted in advance.** Two findings were reached INDEPENDENTLY by both verifiers, which is the strongest signal this process produces.

**V1 — code claims + by-execution.** V1 re-opened every anchor and **BUILT the row from this SPEC's own skeletons**, confirming BY EXECUTION: `gofmt` silent · `go build`/`go vet` 0 · `go mod tidy -diff` EMPTY · `golangci-lint` 0 on all three touched packages *(and noting the repo's `.golangci.yml` enables no `lll`, so phase 72's line-length trap cannot recur)* · the layering grep EMPTY · the full 75-package suite failing **exactly** the two predicted `/cluster` subtests, printing **`Kind:6`** · `unsafe.Sizeof(Endpoint)` **112 → 120** · ZERO new imports anywhere, with the `structpb` counter-claim reproduced for the rejected parameter form · the gofmt re-alignment of `config.go:58-61` and the **+21/−5** const-block diff, exactly as §3.2 predicts · `resolve_test.go` raw 50 / real 49 with `:107` the literal and `:106` its call, all 49 patched and compiling · the two `/cluster` table lines byte-identical by sha256 (`grep -Fxc` ⇒ 2) · the `entry` scope split at exactly 6 `h2dispatch.go` errors and none elsewhere · the hybrid's H2 compile failure verbatim · and **the fuzz repoint arm-DISCRIMINATED** by temporarily tagging both `kind required` arms so the string collision could not launder the result — confirming the as-is seed rejects `empty namespace`, the BRAINSTORM's target hits the `:233` DEPARTURE arm, and this SPEC's target hits the `:288` PARITY arm.

**V1's SIX MODERATE, all folded:** **(M1)** §3.3 claimed seam A "reproduces the reference EXACTLY" on **S5/S5b/R4b** while §3.6's own table marks S5/S5b `N/A — no envoy-go counterpart` (boot-rejected at `manager.go:890` and `:427`) and R4b `UNCLASSIFIED — do not write it up` — **the document contradicted itself, and the same over-claim had ALREADY LANDED in ADR-0295 §Context**; consequently seam B would manufacture **ONE** expressible divergence class, not three. *(This is the phase-72 over-claim class recurring; it is now corrected in both places and in the commit record.)* **(M2)** the pick-gate refutation was called "triple-sourced", but S5 and S5b **share a confound** — both produce an empty host set, so both are consistent with a competing "gate = non-empty host SET" hypothesis — leaving **S8 as the sole discriminating arm**. **(M3)** the `buildCluster` populate loop was specified only as "after the call", which is satisfied by any line in `433..~590`; placed after `:472` it would leave the **subset / locality-weighted / priority** LBs carrying a ZEROED field while the round-robin fast path stayed green — a silent LB-shape-dependent bug the pool-HIT test would NOT catch. **(M4)** the `Endpoint{` census was wrong three ways (39 claimed, 38 actual, breakdown summing to 37) and contradicted §11's own "10 of 18". **(M5)** T4's sha256 gate was **unexecutable for 2 of its 9 files** — T4 adds tests to `accesslog_emit_test.go`/`span_emit_test.go`, so they cannot also be byte-identical; the costing build hashed them identical only because it wrote no tests. **(M6)** a **THIRD seam** was never named: `f.clusters *cluster.Manager` is in scope inside all three emit helpers, so the "two seams and a hybrid" framing was not exhaustive. Plus six MINOR (the red set prints at `:648`/`:773` while the table rows are `:639`/`:764`; a compile-error column quoted inconsistently and unstably; a `!=`-guard phrasing that read as its own inverse; the sweep tally vs `resolve.go:12-45`; the stale hazard sentence also living in `STATE.md`'s live block; and a measured `+119` vs the SPEC's `+128`, a doc-comment-volume difference only — recorded so the IMPL does not treat `+128` as a target). **Verdict: BUILDABLE as specified** — every code-level claim reproduced; the defects were in argumentation and bookkeeping, not design.

**V2 — process, consistency, and stage-close mechanics.** V2 independently re-ran every mechanical check and CONFIRMED: the envelope is genuinely docs-only (`git diff --name-only master..HEAD | grep -c '\.go$'` ⇒ **0**); ROADMAP byte-untouched; check (2) ⇒ **3** and the §12 narrow verified by APPLYING it in a scratch copy (still 3, well-formed, four candidates, marker intact — **§12 fully correct including the "drop the bare `custom_tags` prefix" note**); the sentinel does not fire and no `stop` exists; the ADR-0295 block shape exact (1 Context / 0 Decision / 0 Consequences / PROPOSED / footer) **and §14's "pinned by what LANDED" claim itself true** (`git show 2a82cc7b | grep -c '^+### Decision'` ⇒ 0); all **29** cited memory names EXIST and the three most load-bearing are used faithfully; NINE tasks enumerated; every count reproduced; and **all five §1.1 cosmetic drift corrections RE-DERIVED CORRECT** — which matters, because `reference_a_drift_correction_is_itself_a_claim` is precisely the trap a correction-heavy ledger sets for itself.

**V2's SIX MODERATE, all folded:** **(F1)** the document's LAST line was a dangling `**See §16.1 below.**` pointing at a section that did not exist — **the cited-but-unwritten class recurring inside the very section written to prevent it**; removed. **(F2)** "the departure roster is THREE, not two" **counted spellings on one side and arms on the other**, and every enumeration beneath the headline listed only TWO — a mismatch that had already propagated into the SPEC, the ADR, and the commit subject, and that would have had an IMPL editor write two entries under a header demanding three; the unit is now pinned once (**2 ARMS / 3 SPELLINGS**) and propagated. **(F3)** §9's B1 and B2 **edit the same physical line** (`:688`) while B2 carried no anchor at all — and B1's delicacy note called the sentence's second half "still-true" when **this SPEC's own S3 falsifies part of it**. **(F4)** the SPEC declared **no memory-write obligation**, while two landed memories now carry claims it refuted — including `reference_endpoint_metalookup_host_metadata_seam`'s "a future `CLUSTER`-kind row RE-PAYS it", cited by §10 T1 without flagging that this row disproves it. **(F5)** the ROADMAP row-73 cell carries **three** stale claims where §12/T9 assigned one. **(F6)** the router `next-prompt.txt:70` carries the refuted hazard claim, and the stale-comment sweep never scoped `next-prompt.txt` — a verbatim-carry-forward risk at the very roll this stage performs. Plus five MINOR (the HOST arm's `descend` is at `:150` not `:152`; the `Endpoint{` census — **found independently, matching V1's M4**; "~5000-char" is 3866; §13 omitted the `upstream_cluster` tag that §2 defers; front-matter phrasing that read as if `DECISIONS.md` were production `.go`). **V2 also CORRECTED a check this stage would otherwise have run wrong:** the ADR-0288 singleton greps return **2**, not 1, because `STATE.md:7` is the rule statement itself — pinned at §15 so no future stage "fixes" the count by deleting the rule.

**What NEITHER verifier could check, stated plainly.** **No probe was re-run** — the containers were torn down by the fleets that created them, so every reference-side fact behind D-CTMC-SEAM/WIRE/ZIPKIN-WIRE/VALUE-SERIALIZE/DEFAULT/NOCLUSTER/REJECT rests on the three probe fleets' own executed evidence. V1 attacked those conclusions for **LICENCE rather than truth** and found the seam decision sound but its corroboration over-stated (M1/M2) — which is exactly the failure mode a licence attack is for. Neither verified the `0117` fixture design, its four breaks, the pool-HIT provenance test or the new span tests, none of which exist yet; neither ran `-race` or the full 118-dir differential. The circuit-breaker PARITY label rests on an EXECUTED reference arm plus a RE-DERIVED envoy-go reading, and V1 confirmed only the reading — the SPEC labels it that way and no more.
