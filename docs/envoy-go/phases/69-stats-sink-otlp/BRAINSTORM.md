# Phase 69 Brainstorm — `stats-sink-otlp` (the next Observability-family row; the OpenTelemetry OTLP metrics stats sink `envoy.stat_sinks.open_telemetry` — the FIFTH `stats_sinks[]` consumer, over the landed phase-47 flush subsystem, the ADR-0263 delta engine, the ADR-0264 tag extractor, and the unary-OTLP grpcclient precedent; anticipated ZERO new production packages / +0 go.mod modules / +0 stats / +0 BackendKinds / +0 fuzzers; anticipated fixtures 113 → 114–115 (`0112`, possibly a knob sibling); new exported symbols land ONLY in existing exported-roster packages `internal/statssink` + `internal/grpcclient` — the `internal/xds` zero-new-symbol discipline is UNTOUCHED)

> **Stage:** BRAINSTORM (lifecycle-state 0 → 1). Docs-only — ZERO production `.go`. Worktree `.worktrees/phase-69-brainstorm`, branch `phase-69-stats-sink-otlp-brainstorm`, off master `c7f029e6` (the phase-68 IMPL squash), per `feedback_git_worktrees`.
>
> **Row 69 registers `in-progress`** at the registration step per the ROADMAP §Schema invariant; it flips `done` at the phase-69 IMPL six-gate (ADR-0106; per leg if split — §2.11). The roller SELF-PICKED this subject per the 2026-07-12 standing directive (no human pick; the termination sentinel does NOT fire — checks (2)+(3) still print).
>
> **Evidence base — TWO input dossiers produced by parallel agents this session, each in a PRIVATE scratch OUTSIDE the repo** (`reference_parallel_subagents_private_scratch`): **(P)** EXECUTED reference probes — `envoyproxy/envoy:contrib-v1.37.2`, fresh container per arm, a throwaway unary-Export Go receiver per arm (distinct process + port + capture file = served-this-arm asserted), 16 numbered conclusions cited below as "P-N". **(B)** a mechanical code re-derivation at `c7f029e6` — substrate, envelope, re-derived rival costs, collision/count checks. Claims below are attributed **EXECUTED** (a Dossier P arm/conclusion) or **RE-DERIVED** (declaration reading with `file:line` at `c7f029e6`, re-opened for THIS document — `feedback_brief_citations_not_evidence`); a reading is never presented as a probe. **RE-DERIVE this document; do not execute it.**
>
> **Baselines re-verified mechanically at `c7f029e6` (this session):** fixtures **113** (`ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0111-tls-cvc-empty-dynamic-fallback`, port **10447** at `envoy.yaml:67` ⇒ next fixture `0112`, next port **10448**) · fuzzers **55** (`grep -rn '^func Fuzz' --include='*.go' internal/`) · DECISIONS tail **ADR-0290** (next-free **ADR-0291**) · BackendKind tail **38** · stat surface **1201** (the BEHAVIOR_CONTRACT doc-count CARRY, not mechanically re-verified — there is NO mechanical counting command, per STATE.md) · go.mod modules **2** (lineage figure; `go.opentelemetry.io/proto/otlp v1.0.0` is a DIRECT require, go.mod:17).

---

## 1. Mission and scope confirmation (69 — a NEW consumer of a fully-landed sink subsystem, NOT new flush/registry/dial machinery)

### 1.1 What phase 69 delivers as a self-contained whole

The `envoy.stat_sinks.open_telemetry` stats sink (typed_config `envoy.extensions.stat_sinks.open_telemetry.v3.SinkConfig`): per `stats_flush_interval` tick, convert the full-registry `[]*dto.MetricFamily` snapshot to ONE unary `ExportMetricsServiceRequest` (Counter → monotonic `Sum`, Gauge → `Gauge`; ONE resourceMetrics / ONE scopeMetrics / empty scope — P-2) and Export it over the cluster-dialed gRPC `MetricsService` (gRPC is the ONLY transport in v1.37.2 — P-1). The CORE leg ships the reference's TRUE defaults — tag-extracted names + tags-as-attributes (the two `BoolValue` doc-defaults-true are REAL, proved by the both-false discriminating arm — P-5) — plus `prefix` and the two delta knobs per §2 dockets.

### 1.2 What phase 69 does NOT deliver (forward to §8)

Not histogram export (envoy-go has NO histograms — ADR-0060, `internal/stats/registry.go:1-18` "Histogram is reserved per ADR-0060 and not registered" — a NAMED COVERAGE BOUNDARY, §2.3). Not `resource_detectors` / `custom_metric_conversions` (fields of the v1.37.2 proto that DO NOT EXIST in the pinned go-control-plane v1.32.4 `SinkConfig` — §2.2, a version-skew boundary). Not an http_service transport (none exists in v1.37.2 — P-1). Not a reference-matching final shutdown flush (envoy-go's D-MS-FINAL-FLUSH "no final flush on Done", `flusher.go:31-32`, is a landed sink-family posture — carried; the reference's stop-coincident flush drops histograms anyway, P-15). Not `stats_flush_on_admin` (strict-rejected, `bootstrap.go:539-541` — unchanged).

### 1.3 Phase-done as the next Observability-family row (family STAYS OPEN)

The FIFTH `stats_sinks[]` consumer after metrics_service / statsd / dog_statsd / graphite_statsd (the four-arm dispatch at `internal/bootstrap/bootstrap.go:547-566`). The Observability family stays open — its live `candidates:` sentence (ROADMAP:198) retains the `ssl` stat family + tracing custom_tags candidates after this row rolls "OTLP-metrics stats sink" out AT THE IMPL (§9).

### 1.4 ADR-0045 split readiness — anticipated BY-CONCERN 2-leg, collapse-to-flat docketed *(SPEC/PLAN confirms D-OTLPM-SPLIT — §2.11)*

### 1.5 Package placement — ZERO new production packages

`internal/statssink` gains `otlp.go` (`OTLPMetricsSink` implementing the landed `Sink` interface, `sink.go:18-21`); `internal/grpcclient` gains the `OTLPMetricsClient` stanza; `internal/bootstrap` gains one parse arm; `cmd/envoy-go` gains one build-loop arm. `test/helpers/otlpmetrics` is a new TEST-ONLY package (the `otlplogs`/`otlptrace`/`metricsservice` precedent — all three verified present in `test/helpers/`). **NO `internal/xds`/`internal/boot`/`internal/listener`/`internal/tls` edit — the phase-68 streak-break is not repeated; the new exported symbols land in packages that are already exported-client/sink rosters** (§3, §5).

### 1.6 Relationship to the existing seams

REUSES verbatim: the `Sink` interface + `Flusher`/`flushOnce` (`flusher.go:13-51` — untouched); `snapshot` (`mapping.go:22-47`); `deltaState` (delta.go:18, ADR-0263); `stats.ExtractTags` (`internal/stats/name.go:47`) via the `labelMapper` logic (label.go:13-24, ADR-0264); the descriptor-derived TypeURL idiom (`bootstrap.go:224`); the shared `dialConn` + `connHolder` unary-OTLP client shape (`grpcclient.go:474` `NewOTLPLogsClient`, `:521` `NewOTLPTracesClient`); the `main.go:203-276` statsSinks build loop + the LIFO flusher-then-Close drain (`main.go:286-291`); `FuzzStatsSinkConfigParse` (`internal/bootstrap/statssink_fuzz_test.go:19`).

### 1.7 Why NOW — the twice-deferred "loses on SIZE" adjective is STALE (`reference_deferred_candidate_cost_restale`)

Phase 58 deferred this as "a whole new sink"; phase 68 re-deferred it as "loses on size" (BRAINSTORM-68 §2.1). Re-derived at tip (B §1, re-opened here): every hard part is LANDED substrate — the Sink/Flusher subsystem (47.1), the delta engine (ADR-0263), the tag extractor (ADR-0264), the TypeURL dispatch + sibling strict-reject scaffold, the Dialer + an EXACT unary-OTLP client precedent, the driver-owned-receiver pattern, and BOTH proto dependencies at their pinned versions (`go.opentelemetry.io/proto/otlp@v1.0.0/collector/metrics/v1/` verified present in the module cache; `SinkConfig` getters verified at `go-control-plane/envoy@v1.32.4/extensions/stat_sinks/open_telemetry/v3/open_telemetry.pb.go:96-131`). What remains: a mapping file, a parse arm, a ~40-line client stanza, a wiring arm, a test helper, 1–2 fixtures. The `.gw.go` transitive-import check (`reference_new_subpackage_pulls_transitive_module`): `collector/metrics/v1`'s import set is IDENTICAL to the already-consumed `collector/logs/v1` (`grpc-ecosystem/grpc-gateway/v2 v2.16.0` already indirect) ⇒ **anticipated +0 go.mod modules** (re-verify `go mod tidy -diff` at IMPL).

---

## 2. Design decisions (D-OTLPM-* — mnemonic "OTLPM" = OTLP-Metrics; collision-free: `[RUN]` repo grep `D-OTLPM` over `*.md`+`*.go` → 0 hits. Proposed identifiers `[RUN]`-checked per `reference_spec_drafted_identifier_collision_check`: `OTLPMetricsSink` / `OTLPMetricsClient` / `NewOTLPMetricsClient` / `otlpmetrics` / `parseOpenTelemetrySinkConfig` / `otlpMetricsClient` → 0 hits each; `internal/accesslog` uses `otlp*.go` FILE names inside `package accesslog` — no conflict)

### 2.1 Row + subject confirmation: `stats-sink-otlp` *(SELF-PICKED per the standing directive)* — the FULL rejected roster adjudicated

**The standing directive is "smallest DEFENSIBLE candidate first"; the pick is argued, per rival:**

**SDS rotation** — REJECTED for this row. At tip the provider is EXPLICITLY initial-fetch-only (`internal/xds/provider.go:12-15`; `fetchSecret` does ONE SotW exchange and abandons the stream, `stream.go:50-91`), and the install side holds a per-chain STATIC `*stdtls.Config` — the `GetConfigForClient` SNI callback was REMOVED at phase 07.2 (`internal/listener/manager.go:135-141`). Rotation therefore needs (a) a long-lived background-mutator stream goroutine (full-suite `-race` + drain lifecycle), (b) a mutable secret holder + re-introduction of a dynamic TLS callback across `internal/tls`+`internal/listener`+`internal/xds`+boot, (c) a runtime NACK/rollback posture where today every SDS error is boot-FAIL, and (d) the SPEC-68 §13 sequencing note that the FILE-based `watched_directory` positive control lands FIRST — i.e. really TWO rows. High differential value, but the largest live xDS candidate short of upstream SDS. NOT smallest.

**DataSource `environment_variable`** — REJECTED. The code is trivial (`internal/tls/datasource.go:36-37` is the reject; `os.LookupEnv` + semantics probe), but the differential needs the same PEM injected as an env var into BOTH the reference container and the subprocess — the D-ENV-HARNESS per-fixture env-injection seam SPEC-63 explicitly weighed and DECLINED. Small IF the harness seam is accepted; still gated on that harness decision, low urgency. Blocked-by-charter, not by size.

**Tracing custom_tags `metadata`** — REJECTED. The old "~19 call-site threading" costing is STALE-HIGH — the per-request seam is now `ResolveCustomTags` (`internal/tracing/resolve.go:14`) at exactly THREE call sites (`internal/filter/hcm/accesslog_emit.go:55/116/177`); but the REAL cost is unbuilt: per-request dynamic/route/host METADATA is not plumbed to those sites at all (grep clean in `accesslog_emit.go`), and its differential value depends on fixtures that SET dynamic metadata. Medium with an unbounded plumbing unknown; `spawn_upstream_span`/`http_service` are larger still.

**The tiny riders** (the `validation_context_type` missing-`default:` arm, `crl` strict-reject, a require=false RBAC/principal fixture arm) — none is row-sized: the first two are ~5-line reject-adds with unit-only observables (the `default:` arm additionally needs a PGV-forecloses probe, `reference_pgv_forecloses_go_hazard`); the RBAC arm is +1 fixture, zero production code — genuinely cross-side but thin as a "phase". **The RBAC/principal fixture arm is NAMED the best RIDER candidate** — the SPEC MAY attach it to a 69 leg if it costs ≤1 task, else it stays deferred (§8). None replaces a row.

**QuicProtocolOptions** — REJECTED; unchanged sizing: NO reject to lift, ~1 of 9 knobs maps to quic-go, and the existing 0104 observable does not move — near-zero differential value. Not defensible at any size.

**Upstream SDS** — BLOCKED; RESTATED VERBATIM-BY-REFERENCE, not re-derived (per charter and the phase-68 precedent): it carries the flagged VALUE-level constructibility cycle (`boot.NewSDSProvider` needs `grpcclient.New(cm)` needs the `*cluster.Manager` being constructed) which the TYPE-level cycle guard PASSES; needs a boot-model reshape (mechanism recorded at BRAINSTORM-66 §2.1 / SPEC-68 §13).

**The `ssl` stat family** — RESTATED: chartered FRAMEWORK-SURGERY per ADR-0286 §Consequences C3, named in ROADMAP:198; phases 67/68 confirmed envoy-go still emits ZERO `ssl.*`. Not shrinkable by adjective; posture unchanged.

**CDS/EDS/LDS/RDS · ADS · Delta-xDS · RTDS · `google_grpc` · gRPC/Runtime/WASM family openers** — each REJECTED in one line honestly: the four resource types + ADS/Delta/RTDS all require applying updates to a RUNNING proxy — the same live-reconfig substrate SDS rotation lacks (per-chain static configs, `manager.go:135-141`) — boot-model reshapes, not-smallest by an order of magnitude; `google_grpc` is transport plumbing with weak behavioral differential; the three never-opened families are family-opening-sized with no landed seam.

**Why OTLP-metrics wins:** it is the ONLY candidate whose cost collapsed to "a mapping file + a parse arm + a client stanza" on landed substrate (§1.7), it is ALREADY reference-probed end-to-end this session (Dossier P, 16 conclusions), it lands a rich cross-side observable (payload names/values/types/temporality via a driver-owned receiver), and it NARROWS a live `candidates:` sentence at row-done (§9). Smallest defensible, by the same test phase 68 applied.

### 2.2 D-OTLPM-SCOPE — honor the pinned SinkConfig's SIX fields; the v1.37.2 skew is a NAMED boundary *(RE-DERIVED + EXECUTED)*

The pinned go-control-plane v1.32.4 `SinkConfig` has EXACTLY six fields (getters verified `open_telemetry.pb.go:96-131`): `grpc_service` (sole `protocol_specifier` oneof arm), `report_counters_as_deltas` (scalar bool), `report_histograms_as_deltas` (scalar bool), `emit_tags_as_attributes` (`*wrapperspb.BoolValue`), `use_tag_extracted_name` (`*wrapperspb.BoolValue`), `prefix` (string). The v1.37.2 REFERENCE proto additionally carries `resource_detectors` + `custom_metric_conversions` (Dossier P proto fetch) — ABSENT from the pinned dep, hence unparseable here: they arrive as protobuf UNKNOWN FIELDS and would be SILENTLY IGNORED by `UnmarshalTo`. **DOCKET (D-OTLPM-SKEW, §10):** whether the parse arm must detect-and-reject unknown fields on this Any (strictness parity) or record the silent-ignore as a named version-skew boundary. Omitting `grpc_service` is a reference PGV boot-reject (`protocol_specifier … is required` — P-arm 4a / P-1); envoy-go mirrors it with an explicit reject (the metrics_service `cluster_name`-required precedent, `bootstrap.go:588-594`) — here PGV-parity and the merge-model precondition coincide, so this one is NOT left to `reference_pgv_forecloses_go_hazard` discretion.

### 2.3 D-OTLPM-HISTO — histograms are a NAMED COVERAGE BOUNDARY; `report_histograms_as_deltas` reject-vs-inert DOCKETED *(RE-DERIVED; the phase-47 precedent CHECKED, not assumed)*

envoy-go registers NO histograms (ADR-0060, `registry.go:1-18`), so the reference's Histogram stream (19 default explicitBounds, P-7; circllhist-estimated sums, P-13) has NO envoy-go counterpart — a shared-with-phase-47 coverage boundary. **How phase 47 handled the SAME boundary (verified):** `parseMetricsServiceConfig` STRICT-REJECTS a non-default `histogram_emit_mode` — "envoy-go has no histograms" (`bootstrap.go:595-597`). That precedent points at REJECTING `report_histograms_as_deltas: true`, NOT accept-inert — the initial accept-inert lean is REFUTED by the repo check. Counter-argument for accept-inert: with zero histograms the knob is VACUOUSLY honored (nothing on the wire changes), whereas a non-default `histogram_emit_mode` would change WHICH stats are emitted. **DOCKET (§10): lean REJECT per the landed precedent** (strictness is the house style, ADR-0080), with accept-inert as the argued alternative; the SPEC decides with the ADR wording.

### 2.4 D-OTLPM-DEFAULTS — the two BoolValue knobs DEFAULT TRUE ⇒ tag extraction + attributes are CORE-leg, day one *(EXECUTED P-5)*

Baseline (knobs absent) emits TAG-EXTRACTED names + tags-as-attributes (`cluster.upstream_cx_total` + `envoy.cluster_name`); both-false emits full dotted names with 0 of 143 datapoints carrying any attribute (P-5, the discriminating direction). So the absent-wrapper default is TRUE — INVERTED from metrics_service's scalar-false `emit_tags_as_labels` — and the CORE leg cannot ship "full names, no attributes" as its default. The ADR-0264 extractor (`stats.ExtractTags`, `name.go:47`) is the exact machinery; the OTLP sink maps extracted tags to `KeyValue` attributes instead of LabelPairs. **Parse trap:** both knobs are `*wrapperspb.BoolValue` — fixture YAML takes the BARE SCALAR (`use_tag_extracted_name: false`), never `{value: false}` (`reference_protojson_wrapper_scalar_not_object`); nil-wrapper ⇒ true (the doc default), NOT `.GetValue()`'s zero-false (the metrics_service `.GetValue()` idiom at `bootstrap.go:600` is the WRONG template here — invert explicitly).

### 2.5 D-OTLPM-STARTTIME — the reference's factor-1000 µs bug is NOT cloned; assertion posture DOCKETED *(EXECUTED P-12)*

The reference emits `startTimeUnixNano` in MICROSECONDS (16 digits) while `timeUnixNano` is genuine ns — a wire-level factor-1000 bug; gauge datapoints carry NO startTime at all (P-12). envoy-go emits CORRECT nanoseconds (`reference_wire_format_both_sides_see_same_bytes` protects frame validity, not bug-cloning — a byte-for-byte clone of a known reference bug is the one thing that rule does NOT demand). Consequence for the differential (`reference_streaming_sink_differential_framing`): assert the aggregated PAYLOAD — names/values/types/temporality/attributes — and assert startTime STRUCTURALLY PER SIDE (subject: ns-magnitude + constant across cumulative flushes; reference: pin its own observed shape or leave unasserted), NEVER cross-side startTime equality. **DOCKET (§10):** the exact per-side structural assertion (magnitude-band vs digit-count vs unasserted).

### 2.6 D-OTLPM-DUPNAME — same-name stats are SEPARATE Metric entries; assert order-insensitively *(EXECUTED P-6)*

In tag-extracted mode, stats sharing a residual name are SEPARATE `Metric` entries (one dataPoint each), distinguished only by attributes — 18 duplicated names per baseline flush; NOT merged into one Metric with multiple dataPoints (P-6). envoy-go MATCHES the shape (one Metric per registry stat). The receiver/asserter keys on (name, attribute-set) and is ORDER-INSENSITIVE (Walk order vs the reference's internal order is not a contract). DOCKET the exact keying to the SPEC.

### 2.7 D-OTLPM-RESOURCE — resource attrs are PER-SIDE pins *(EXECUTED P-3)*

The reference emits exactly `telemetry.sdk.name=envoy` / `telemetry.sdk.language=cpp` / `telemetry.sdk.version=<build>` (no node id; scope EMPTY `{}` — P-3, P-2). envoy-go's values necessarily differ (language, version). The analogue of `reference_differential_reference_parses_full_message` applies: pin per-side `resource` content, never cross-side equality. **DOCKET (D-OTLPM-NODE, §10):** what envoy-go emits (its own `telemetry.sdk.*` triple is the lean — name=envoy-go, language=go, version=<build>); scope stays empty.

### 2.8 D-OTLPM-FAILOPEN — fail-open + retry-per-flush; the sink is cluster-dialed via the grpcclient path; the feedback loop noted *(EXECUTED P-14, P-15)*

With the collector port closed the reference boots, serves traffic normally, and the ONLY failure signal is the sink cluster's OWN upstream stats (`upstream_cx_connect_fail` ≈ 1/flush; no dedicated sink-failure stat, no overflow counter, no log warning — P-14). envoy-go's posture: the landed `MetricsServiceSink` shape — bounded channel (cap 8) + writer goroutine + drop-newest + rate-limited drop LOG (not a stat) + retry-once-per-Export + idempotent Close (`sink.go:33-209`) — but UNARY (the `internal/accesslog` otlpsink precedent). **Dial path decision:** reuse the grpcclient `Dialer` route exactly as metrics_service/OTLP-logs do (`main.go:209`, `grpcclient.go:474`) — NOT the statsd-TCP `DialSink` unaccounted path; per `reference_cluster_sink_dial_unaccounted`, fixtures must NOT cross-side-assert the sink cluster's `upstream_cx_*` (the two sides' dial accounting differs). **Feedback loop (P-15):** the sink cluster's own stats are themselves exported — fixture assertions must never depend on sink-cluster self-stat VALUES (they move with the export activity itself); assert application-cluster subsets.

### 2.9 D-OTLPM-DELTA — `report_counters_as_deltas` rides the landed deltaState; the wire facts pinned *(EXECUTED P-9, P-10)*

Delta Sums keep `isMonotonic: true`; temporality flips to DELTA; per-window values are true deltas (6,0,0,… vs cumulative 6,6,6,…); startTime CHAINS to the previous flush's timeUnixNano (first window's startTime is the µs-bugged server-start — envoy-go chains correctly from real ns); gauges unaffected; the two delta knobs are INDEPENDENT (P-9, P-10). The ADR-0263 `deltaState` (delta.go:18, applied in the writer so an enqueue-drop never latches — `sink.go:61-70`) is reused as-is. The knob fixture carries the `reference_delta_sink_differential_stability_barrier` (assert the SUM is STILL K after ≥2 further flushes) + `reference_periodic_sink_differential_two_receivers` (two per-side receivers, hard Close) disciplines.

### 2.10 D-OTLPM-FIXTURE — `0112-stats-sink-otlp` (port 10448) on the metricsservice-fixture chassis *(direction; SPEC pins)*

Driver-owned `test/helpers/otlpmetrics` unary-Export accumulating server (`reference_differential_grpc_receiver_driver_owned` — NOT a BackendKind; tail stays 38), one instance PER SIDE. Assertions: NAMED-SUBSET only (`reference_stats_sink_emits_used_only` — the reference exports only USED stats where used ≠ non-zero and the set GROWS with use, P-4; envoy-go walks the FULL frozen registry, `mapping.go:22-47` / the AMEND-TCP-USEDONLY note at `statsd_tcp.go:78-80`): counter Sums monotonic-cumulative with matching values on driven traffic, gauge Gauges, tag-extracted names + attributes (the TRUE default — §2.4), per-side resource pins (§2.7), order-insensitive dup-name keying (§2.6), structural startTime (§2.5). **Fixture-count RANGE 113 → 114–115**: `0112` core; POSSIBLY `0113` for the knob arm (deltas + prefix + explicit-false naming — the 0089/0090 metrics_service pair precedent). One fixture dir = ONE runner branch. **DOCKET (§10):** the exact fixture split. Prefix composes with tag extraction (`probe_prefix.<name>`, dot added — P-11).

### 2.11 D-OTLPM-SPLIT — BY-CONCERN 2-leg anticipated; collapse-to-flat docketed honestly

The family precedent (rows 44/45/46/47; phase 47's own ADR-0262→0263→0264 progression) suggests **69.1** core cumulative export honoring the true-default tag behavior + `0112` / **69.2** knobs (`report_counters_as_deltas` via deltaState, `prefix`, explicit-false naming) + knob fixture. HONESTLY stated the other way: the delta engine and tag extractor are LANDED substrate — the knobs are parameter-threading, not new machinery — so a SINGLE FLAT ROW (~9–11 tasks) is defensible too. The ADR-0045 valve arms at the PLAN; the SPEC picks with the task-count evidence in hand.

### 2.12 D-OTLPM-STATS / D-OTLPM-FUZZSEED — +0 stats; +0 fuzzers (seeds/dispatch only, dispatch-verified)

**Stats +0** (surface stays 1201): the sink EMITS stats, it does not create them; the reference has NO sink-health stat — its only failure signal is ordinary cluster stats (P-14) — so no envoy-go sink-health stat is warranted either; drops stay LOGGED not counted (the D-MS-STATS-FINAL precedent, `sink.go:44-47`). **Fuzz +0** (count stays 55): the new parse arm slots into `FuzzStatsSinkConfigParse` (`statssink_fuzz_test.go:19`) as SEEDS; the SPEC must dispatch-verify the seed side reaches the NEW arm (the phase-66/67/68 seed-vacuity trap — `reference_vacuous_break_receiver_normalizes` family). DOCKET (§10).

---

## 3. Framework-survey result — a fifth consumer of landed machinery; ZERO new production packages/modules; new exports ONLY in existing exported-roster packages

**The change surface:** `internal/statssink/otlp.go` (NEW FILE, existing package — `OTLPMetricsSink` + a package-local `otlpMetricsClient` interface seam mirroring `metricsClient` at `sink.go:28-31`, keeping the production package grpcclient-free) · `internal/bootstrap/bootstrap.go` (+1 descriptor-derived TypeURL var, +1 `parseOpenTelemetrySinkConfig`, +1 config struct + slice, the `:565` default-reject grows four → five named TypeURLs — `reference_strict_reject_sibling_typeurl_gap`) · `internal/grpcclient/grpcclient.go` (+`OTLPMetricsClient`/`NewOTLPMetricsClient`/`Export`/`Close` — the `:474`/`:521` stanza cloned, ~3-4 exported symbols) · `cmd/envoy-go/main.go` (one more arm in the `:206-276` build loop feeding the SAME `statsSinks` slice + Flusher; the `:206` gate condition grows one clause). **Stated LOUDLY: the new exported symbols land in `internal/statssink` + `internal/grpcclient` — both are established exported rosters (four sink constructors; a client-per-service roster) — this is the NORMAL growth mode for these packages, and the `internal/xds` zero-new-symbol discipline (broken once, deliberately, at phase 68) is UNTOUCHED.** NEW packages: `test/helpers/otlpmetrics` TEST-ONLY. go.mod modules: +0 anticipated (§1.7; re-check `git diff go.mod` after tidy). Flusher/registry/mapping: BYTE-UNTOUCHED.

## 4. Bootstrap-level applicability — a pure `stats_sinks[]` bootstrap consumer

The entire config surface is `parseStatsSinks` (`internal/bootstrap/bootstrap.go:532-569`; there is NO stats-sink logic in top-level `validate/` — B, re-checked) + the main.go wiring. No listener, filter, cluster-config, or xDS involvement; `stats_flush_interval` semantics unchanged (default 5s; D-MS-FLUSH-INERT preserved — the OTLP slice joins the `:206` any-sink-present gate).

## 5. Stat surface + envelope hypothesis — +0 stats (P-14 cited); the envelope's notable facts

+0 stats (§2.12). Envelope: functional production = `internal/statssink/otlp.go` (new file) + `internal/bootstrap/bootstrap.go` + `internal/grpcclient/grpcclient.go` + `cmd/envoy-go/main.go`. NOTHING under `internal/xds`/`internal/tls`/`internal/boot`/`internal/listener`. The cycle-guard posture: statssink already imports only stats + proto DTOs (`sink.go:3-13`, `flusher.go:3-8`); the otlp file adds the otlp proto imports only — no grpcclient import (the `metricsClient`-seam precedent, `sink.go:23-31`).

## 6. Edit-site enumeration — cited by SYMBOL with `file:line` as of master `c7f029e6` (SPEC re-derives)

**Production:** `internal/bootstrap/bootstrap.go` — `parseStatsSinks` (`:532-569`) `[ADD the fifth arm; grow the :565 sibling-reject to five TypeURLs]` · `metricsServiceTypeURL` (`:224`) `[CLONE the descriptor-derived idiom]` · `parseMetricsServiceConfig` (`:580-604`) `[TEMPLATE — but invert the BoolValue default handling, §2.4, and mirror the histogram-knob posture per D-OTLPM-HISTO]` · `StatsSinkConfig` (`:357-363`) `[SIBLING struct]`. `internal/statssink` — `Sink` (`sink.go:18-21`) `[IMPLEMENT]` · `MetricsServiceSink` writer/Close shape (`sink.go:50-209`) `[CLONE, unary]` · `deltaState` (delta.go:18) `[REUSE]` · `labelMapper`/`stats.ExtractTags` (label.go:13-24, `name.go:47`) `[REUSE logic → attributes]` · `snapshot` (`mapping.go:22-47`) `[REUSE untouched]` · `Flusher` (`flusher.go:13-51`) `[UNTOUCHED]`. `internal/grpcclient/grpcclient.go` — `NewOTLPLogsClient` (`:474`) / `NewOTLPTracesClient` (`:521`) `[CLONE → NewOTLPMetricsClient over colmetricspb]`. `cmd/envoy-go/main.go` — the sink build loop + gate (`:206-276`) `[ADD arm]` · `NewFlusher` (`:275`) + the drain defer (`:286-291`) `[UNTOUCHED]`.

**Test/harness:** `test/helpers/otlpmetrics` `[ADD — per-side accumulating unary receiver]` · `test/fixtures/0112-stats-sink-otlp/` (port 10448) `[ADD]` (+ possible knob fixture — §2.10) · `FuzzStatsSinkConfigParse` seeds (`statssink_fuzz_test.go:19`) `[ADD, dispatch-verified]` · unit tests (parse arm incl. wrapper-default inversion + skew posture; mapping Counter/Gauge/attrs/prefix/delta; sink writer/Close).

**Docs:** BEHAVIOR_CONTRACT (the OTLP sink section: six-field surface, histogram boundary, startTime posture, fail-open) · DECISIONS ADR-0291 (§Context at the SPEC) · ROADMAP/STATE/next-prompt `[REGISTRATION/STAGE-CLOSE steps — NOT this document]`.

## 7. Anticipated ADRs — ONE at the phase-69 IMPL: ADR-0291 (`stats-sink-otlp`)

ADR-0291 (§Context drafts at the SPEC per ADR-0044 — the DECISIONS tail flips ADR-0290 → ADR-0291 AT THE SPEC; DECISIONS is NOT touched at this BRAINSTORM). It extends the ADR-0262/0263/0264 sink lineage with the first OTLP-wire consumer of the flush subsystem. Next-free after: ADR-0292. (If §2.11 resolves to a 2-leg split the SPEC states whether both legs share ADR-0291 — the phase-47 multi-ADR precedent argues one ADR per leg is ALSO available; SPEC call.)

## 8. Deferred items (the SPEC-68 §13 roster carried MINUS this pickup, PLUS what this row newly defers)

Carried: `xds-sds-upstream-server-cert` (VALUE-level cycle — restated §2.1, do not re-derive) · SDS rotation (`watched_directory` positive control FIRST) · the `validation_context_type` missing-`default:` arm · `crl` · the compose-two CVC edge (`seen>1`) · the `ssl` stat family (framework surgery, ADR-0286 C3) · HTTP/3 `QuicProtocolOptions` · DataSource `environment_variable` (D-ENV-HARNESS-gated) · tracing custom_tags `metadata` / `spawn_upstream_span` / `http_service` / force-trace remainder · CDS/EDS · LDS/RDS · ADS · Delta xDS · RTDS · `google_grpc` · gRPC/Runtime/WASM family openers · QUIC client-auth · the cert-provider oneof arms (subsumed by the missing-`default:` item) · the require=false RBAC/principal fixture arm (**named the best RIDER — §2.1; SPEC may attach**) · the plain SDS-VC empty-served retained departure · the S2/S3 set-but-broken-served behaviors (stay boot-FAIL matching the reference NACK — SPEC-68 §3.2) · reclassifying the ACKed-empty SDS resource as a stream-level SUCCESS (D-EDF-STREAMPOSTURE option (a), deferred — SPEC-68 §3.4) · the ADR-0080 + ADR-0044 citation drifts. **REMOVED: OTLP-metrics stats sink (THIS pickup).**

**Newly deferred at THIS row:** histogram export (blocked on ADR-0060 — reopening histograms is its own framework row) · `resource_detectors` + `custom_metric_conversions` (v1.37.2-only fields absent from the pinned proto — a version-skew boundary, revisited at any go-control-plane bump; §2.2) · a reference-matching final shutdown flush (D-MS-FINAL-FLUSH carried; the reference's final flush drops histograms anyway — P-15).

## 9. Cross-references against prior phases' deferred-items lists — a SENTENCE-NARROWING pickup, narrowed AT THE IMPL (the phase-57 precedent)

This pickup IS named in a live `candidates:` sentence — ROADMAP:198 opens "candidates: OTLP-metrics stats sink + the downstream TLS handshake-outcome `ssl` stat family … + tracing custom_tags …" (`[RUN]`-verified at `c7f029e6`) — the first sentence-narrowing pickup since the current sentence format began. **Per the verified phase-57 precedent, NO sentence is narrowed at ANY pre-IMPL stage:** at the phase-57 BRAINSTORM commit ("phase 57 (stats-sink-graphite) BRAINSTORM: the cheapest Observability follow-on…", `d2fe54df`) the chartered `graphite` candidate REMAINED in the sentence; it rolled OUT only at the phase-57 IMPL ("…family deferred-list rolls graphite OUT, dog_statsd explicit-zero parity IN", `2825ca90` — located by SUBJECT per `reference_next_prompt_tracked_despite_gitignore` discipline). So: ROADMAP:198 rolls "OTLP-metrics stats sink + " OUT at the phase-69 IMPL row-done edit, not before; sentinel check (2) keeps printing THREE live sentences UNCHANGED throughout the row's lifecycle (`reference_sentinel_deferred_sentence_live_vs_historical` — the sentence stays a LIVE `candidates:` sentence afterward because the `ssl` family + tracing custom_tags candidates remain in it); checks (1)/(3) unchanged (row 69 in-progress re-opens (1); `NEVER OPENED: gRPC/Runtime/WASM` still prints) ⇒ the sentinel does NOT fire at any stage of this row.

## 10. BRAINSTORM-time open questions for SPEC-time resolution

- **D-OTLPM-HISTO:** reject `report_histograms_as_deltas: true` (the checked `bootstrap.go:595-597` precedent; lean) vs accept-inert (vacuously-honored argument). §2.3.
- **D-OTLPM-SKEW:** unknown-field posture for the v1.37.2-only `resource_detectors`/`custom_metric_conversions` on the typed_config Any — detect-and-reject vs named silent-ignore boundary. §2.2.
- **D-OTLPM-STARTTIME:** the exact per-side structural startTime assertion (magnitude-band / digit-count / unasserted-reference-side); NEVER cross-side equality. §2.5.
- **D-OTLPM-DUPNAME:** the receiver's (name, attribute-set) keying + order-insensitivity mechanics. §2.6.
- **D-OTLPM-NODE:** envoy-go's own resource-attribute triple. §2.7.
- **D-OTLPM-SPLIT:** BY-CONCERN 2-leg vs SINGLE FLAT ROW (both argued §2.11); the ADR-0045 valve arms at the PLAN; ADR-per-leg question (§7).
- **D-OTLPM-FIXTURE:** one fixture or two (113→114 vs 115); the knob-arm composition (deltas + prefix + explicit-false in ONE fixture, the 0090 precedent). §2.10.
- **D-OTLPM-FUZZSEED:** seed shapes for the new parse arm + the dispatch-side verification that seeds REACH it. §2.12.
- **RIDER:** whether the require=false RBAC/principal fixture arm attaches (≤1 task) or stays deferred. §2.1.

## 11. Prior-phase lessons applied

- `reference_deferred_candidate_cost_restale` — the pick's whole argument: "loses on SIZE" was re-derived at tip and found stale (§1.7), exactly as the phase-66→68 Design-B lineage taught.
- `feedback_brief_citations_not_evidence` / `reference_quoting_is_not_executing` — every load-bearing anchor re-opened at `c7f029e6` for this document; the reference behavior EXECUTED (Dossier P), not cited from Envoy docs — and the doc-default-true claim was probed in the DISCRIMINATING direction (both-false arm), per `reference_probe_must_discriminate`.
- The §2.3 precedent check REFUTED this charter's initial accept-inert lean — a live demonstration of checking the repo before adopting a tiebreaker.
- `reference_protojson_wrapper_scalar_not_object` — the two BoolValue knobs in fixture YAML; the inverted nil-wrapper default called out at the exact WRONG-template line (`bootstrap.go:600`).
- `reference_stats_sink_emits_used_only` + the used≠non-zero refinement (P-4) — named-subset assertions only.
- `reference_streaming_sink_differential_framing` / `reference_differential_grpc_receiver_driver_owned` / `reference_periodic_sink_differential_two_receivers` / `reference_delta_sink_differential_stability_barrier` / `reference_cluster_sink_dial_unaccounted` — the fixture disciplines pre-committed (§2.5, §2.8–2.10).
- `reference_spec_drafted_identifier_collision_check` — all four proposed identifiers + the D-mnemonic grepped clean BEFORE the SPEC adopts them (§2 header).
- P-16 carried as a SPEC-probe harness pin: any SPEC-time re-probe puts `dns_lookup_family: V4_ONLY` on the sink cluster (Docker Desktop AAAA-preference makes AUTO silently connect-fail — the near-false-negative shape of `reference_docker_probe_bridge_network`).
- `feedback_git_worktrees` / `reference_parallel_subagents_private_scratch` — fresh worktree; both input dossiers in private scratch outside the repo.

## 12. Section closeout

Phase 69 lands the OTLP metrics stats sink as the fifth `stats_sinks[]` consumer: one unary Export per flush over a cluster-dialed gRPC client, Counter→monotonic Sum / Gauge→Gauge, tag-extracted names + tags-as-attributes as the TRUE default (P-5), cumulative default with the delta knob riding the landed ADR-0263 engine (P-9), prefix composing (P-11), fail-open with retry-per-flush (P-14), and the reference's startTime µs bug deliberately NOT cloned (P-12 — per-side structural assertion). Constructible on fully-landed substrate (B, re-derived §1.7): zero new production packages, +0 modules/stats/fuzzers/BackendKinds, fixtures 113→114–115, new exports only in the existing statssink/grpcclient rosters. Adjudicated (§2.1): the smallest defensible candidate at tip — every rival is either harness-gated, plumbing-unbuilt, framework-surgery, boot-model-reshape, or not row-sized. The SPEC re-derives, settles D-OTLPM-HISTO/SKEW/STARTTIME/SPLIT/FIXTURE, and flips the DECISIONS tail to ADR-0291. ROADMAP:198 narrows at the IMPL, not before (the phase-57 precedent); the sentinel does NOT fire.

Adversarially verified before landing (an independent code-claims + probe-capture + adjudication verifier, verdict SOUND-WITH-CORRECTIONS): one MODERATE (the §8 deferred-roster carry dropped two SPEC-68 §13 newly-deferred items) + three MINOR corrections (the 1201 doc-count relabel, the `sink.go` :209 range widening, two identifiers added to the §2 collision roster — both re-grepped 0 in this worktree) folded in; zero SEVERE; design direction unchanged.
