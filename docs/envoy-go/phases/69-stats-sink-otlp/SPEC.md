# SPEC 69 — the OpenTelemetry OTLP metrics stats sink (`envoy.stat_sinks.open_telemetry`; the SIXTH stats-sink-lineage row + the FIFTH `stats_sinks[]` consumer; a NEW `internal/statssink/otlp.go` `OTLPMetricsSink` over the LANDED phase-47 `Flusher`/`Sink` seam — per-flush ONE unary `Export` over a cluster-dialed `OTLPMetricsClient`, Counter → monotonic cumulative `Sum` / Gauge → `Gauge`, tag-extracted residual names + tags-as-`KeyValue`-attributes as the TRUE default; a SINGLE FLAT ROW; fixtures `0112` core + `0113` knobs)

> **Stage:** SPEC (lifecycle-state 1 → 2). Docs-only — ZERO production `.go`. Worktree `.worktrees/phase-69-spec`, branch `phase-69-stats-sink-otlp-spec`, tip `7a293a06` (the phase-69 BRAINSTORM squash), per `feedback_git_worktrees`.
>
> **Row 69 STAYS `in-progress`** — the IMPL flips it `done` at its six-gate (ADR-0106, the SOLE leg — a SINGLE FLAT ROW, §10). **ADR-0291's §Context DRAFTS HERE** (ADR-0044-as-used); §Decision/§Consequences land at the IMPL IN PLACE. **The DECISIONS tail flips ADR-0290 → ADR-0291 AT THIS SPEC COMMIT** (§14) — the §Context append IS the tail flip; next-free becomes ADR-0292.
>
> **Evidence base — code RE-DERIVED at `7a293a06` this SPEC** (an independent re-derivation agent re-opened EVERY anchor in BRAINSTORM §6/§11 + resolved the code-gated dockets — the reject-arm roster, the `SinkConfig` six-field/wrapper layout, the `DiscardUnknown` posture, the fuzz dispatch, the grpcclient stanza, the collision greps) **+ the BRAINSTORM's Dossier P** (EXECUTED discriminating probes run THIS session: `envoyproxy/envoy:contrib-v1.37.2`, fresh container per arm, a throwaway per-arm unary-Export Go receiver with served-this-arm asserts — 16 numbered conclusions "P-N", adversarially verified before the BRAINSTORM landed). Claims below are attributed **EXECUTED** (a Dossier P conclusion, named) or **RE-DERIVED** (a declaration reading with `file:line` at `7a293a06`, re-opened this SPEC); a reading is never presented as a probe. **NO fresh reference probe was run this SPEC and none is owed** — every D-OTLPM docket resolves on Dossier P (which discriminated every behavioral gate, incl. the doc-default-true knobs via the both-false arm — P-5) + code re-derivation + house-style precedent; NO docket decision hinges on unverified reference behavior (contrast SPEC-68, where D-EDF-CLASSIFY genuinely needed a fresh S2/S3 discriminator). This is stated honestly, not to skip rigor. **RE-DERIVE this document; do not execute it** (`feedback_brief_citations_not_evidence`, `reference_quoting_is_not_executing`).
>
> **Baselines re-verified mechanically at `7a293a06`:** fixtures **113** (`ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0111-tls-cvc-empty-dynamic-fallback`, in-container port **10447** ⇒ next fixture `0112`, next port **10448**) · fuzzers **55** (`grep -rn '^func Fuzz' --include='*.go' internal/`) · DECISIONS tail **ADR-0290** (next-free **ADR-0291**; `grep -c '^## ADR-0291'` → 0, FREE) · BackendKind tail **38** · stat surface **1201** (BEHAVIOR_CONTRACT doc count) · go.mod modules **2** (lineage figure; the single `go.mod` requires 67, incl. `go.opentelemetry.io/proto/otlp v1.0.0` DIRECT at `go.mod:17`).
>
> **Sentinel expectation at this stage:** check (1) prints `NOT DONE: row 69` (registered `in-progress` at the BRAINSTORM stage-close); checks (2)+(3) unchanged (three live `candidates:` sentences at ROADMAP:179/189/198; `NEVER OPENED: gRPC/Runtime/WASM`). **No deferred sentence is edited at ANY stage of this row** — "OTLP-metrics stats sink" stays in the live Observability `candidates:` sentence and rolls OUT at the phase-69 IMPL row-done edit, per the verified phase-57 graphite precedent (§12).

---

## 1. Purpose / Mission

Phase 47 opened the bootstrap `stats_sinks[]` surface and built the sink-agnostic flush subsystem: the `Flusher` snapshotting the frozen `stats.Registry` per `stats_flush_interval`, the `Sink` interface, the `snapshot()` Counter/Gauge → `[]*dto.MetricFamily` mapping, the `deltaState` per-flush Counter delta (ADR-0263), the `labelMapper`/`stats.ExtractTags` tag core (ADR-0264), and the post-`Freeze` `main.go` build. Phases 48/49/50/55/57 added four more `stats_sinks[]` consumers (statsd/dog_statsd/graphite_statsd + the statsd TCP transport) purely additively — each a new `Sink` impl, no new framework piece.

Phase 69 lands the **SIXTH stats-sink-lineage row + the FIFTH `stats_sinks[]` consumer**: the OpenTelemetry OTLP metrics stats sink `envoy.stat_sinks.open_telemetry` (typed_config `envoy.extensions.stat_sinks.open_telemetry.v3.SinkConfig`). Per `stats_flush_interval` tick, the `OTLPMetricsSink` converts the full-registry `[]*dto.MetricFamily` snapshot to ONE `ExportMetricsServiceRequest` (Counter → monotonic cumulative `Sum`, Gauge → `Gauge`; ONE resourceMetrics / ONE scopeMetrics / empty scope — P-2) and `Export`s it over a cluster-dialed gRPC `MetricsService` (gRPC is the ONLY transport in v1.37.2 — P-1). The CORE behavior is the reference's TRUE defaults — **tag-extracted residual names + tags-as-`KeyValue`-attributes** (the two `*wrapperspb.BoolValue` knobs are doc-default-TRUE, proved by the both-false discriminating arm — P-5) — plus `prefix` and the `report_counters_as_deltas` DELTA knob (riding the landed ADR-0263 `deltaState`). It composes on FULLY-LANDED substrate: **ZERO new production packages, +0 go.mod modules, +0 stats, +0 BackendKinds, +0 fuzzers** (a `FuzzStatsSinkConfigParse` seed); new exported symbols land ONLY in the existing `internal/statssink` + `internal/grpcclient` rosters — the `internal/xds` zero-new-symbol discipline (broken once, deliberately, at phase 68) is UNTOUCHED.

### 1.1 BRAINSTORM anticipations — what HELD, what this SPEC CORRECTS *(the drift ledger; drift found is the point)*

**HELD (re-derived / re-probed this session):** the four-arm `parseStatsSinks` dispatch + the sibling-reject default arm (`bootstrap.go:532-569`, reject at `:564-565`) — adding the OTLP arm grows the reject TypeURL roster **four → five** (`reference_strict_reject_sibling_typeurl_gap`) · the descriptor-derived `metricsServiceTypeURL` idiom (`:224`) as the CLONE template · the `Sink` interface + `metricsClient` seam (`sink.go:18-21`/`:28-31`) — statssink imports NO grpcclient, NO otlp proto (B10) · `deltaState` (`delta.go:18`) + `labelMapper`/`stats.ExtractTags` (`label.go:13-24`, `:38`) reusable verbatim · `snapshot` (`mapping.go:22-47`) untouched · `NewOTLPLogsClient`/`NewOTLPTracesClient` (`grpcclient.go:474`/`:521`) as the UNARY clone template · the `main.go:206` any-sink gate + `:275` `NewFlusher` + `:286-291` LIFO drain · `FuzzStatsSinkConfigParse` (`statssink_fuzz_test.go:19`) reaches an arm by the seed's `@type` · `go.opentelemetry.io/proto/otlp v1.0.0` DIRECT with `collector/metrics/v1` vendored (`ExportMetricsServiceRequest` / `MetricsServiceClient` symbols present) ⇒ **+0 modules** · the pinned go-control-plane v1.32.4 `SinkConfig` has EXACTLY six fields (`:96-131`) · all proposed identifiers collision-free (§5) · counts (113 / tail `0111`@10447 / 55 / ADR-0290).

**CORRECTED — each flagged where it lands:**

| # | BRAINSTORM/router said | Corrected to | § |
|---|---|---|---|
| C1 | **D-OTLPM-SKEW:** the v1.37.2-only `resource_detectors`/`custom_metric_conversions` "arrive as protobuf UNKNOWN FIELDS and would be **SILENTLY IGNORED** by `UnmarshalTo`" (BRAINSTORM §2.2/§8) — detect-and-reject vs a named silent-ignore boundary, OPEN | **RESOLVED — NOT silently ignored; already boot-REJECTED for free.** envoy-go parses the whole bootstrap document with `protojson.UnmarshalOptions{DiscardUnknown: false}` (`bootstrap.go:510` — RE-DERIVED, the ONLY `DiscardUnknown` on the bootstrap path, and it is `false`). So a bootstrap carrying `resource_detectors`/`custom_metric_conversions` on an `open_telemetry` `SinkConfig` — fields absent from the pinned v1.32.4 descriptor — is DETECTED AND REJECTED at boot (a protojson "unknown field" error), PROVIDED the `SinkConfig` descriptor is blank-imported (required anyway for `@type` resolution). **ZERO new detect-and-reject code owed**; a named envoy-go-STRICT version-skew DEPARTURE (the v1.37.2 reference HONORS these fields, envoy-go boot-rejects a config that sets them; ADR-0080), revisited at any go-control-plane bump. | §3.3 |
| C2 | The two `*wrapperspb.BoolValue` knobs default TRUE; "invert the nil-wrapper default from the metrics_service `.GetValue()` template" (BRAINSTORM §2.4) | **Confirmed AND pinned exactly.** The two wrapper knobs are `emit_tags_as_attributes` (`:117`) + `use_tag_extracted_name` (`:124`), BOTH doc-default TRUE; the two DELTA knobs (`report_counters_as_deltas` `:103`, `report_histograms_as_deltas` `:110`) are SCALAR `bool`; `prefix` `:131` scalar string. Read the wrappers as `w := cfg.GetX(); enabled := w == nil || w.GetValue()` (nil → **TRUE**) — the OPPOSITE of the metrics_service `msc.GetReportCountersAsDeltas().GetValue()` template (`bootstrap.go:600`, nil → false). `reference_protojson_wrapper_scalar_not_object`: fixture YAML takes the BARE scalar (`use_tag_extracted_name: false`), never `{value:false}`. | §3.5 |
| C3 | "CLONE the `MetricsServiceSink` writer/Close shape (`sink.go:50-209`), unary" (BRAINSTORM §6) | **Sharpened: the sink uses the UNARY-Export bounded-channel writer, NOT the streaming `MetricsServiceSink` lifecycle.** `MetricsServiceSink` is CLIENT-STREAMING `StreamMetrics` (identifier-once + reconnect-resend, `sink.go:158-209`, B5). The OTLP sink does ONE UNARY `Export` per flush (P-2), so its writer is the `internal/accesslog` OTLP-logs / `internal/tracing` OTLPExporter shape — bounded channel + writer goroutine + **retry-once-per-`Export`** + drop-newest + rate-limited drop LOG + idempotent `Close` — with a plain `client.Export(ctx, req)`, no stream lifecycle. | §3.7 |
| C4 | `internal/grpcclient` gains `OTLPMetricsClient` (BRAINSTORM §3) | **Named a collision hazard.** The OTLP metrics service client type in `colmetricspb` is ITSELF named `MetricsServiceClient` — the SAME simple name as `metricsv3.MetricsServiceClient` already used by the streaming metrics_service sink (F14). Distinct packages, but the grpcclient wrapper MUST disambiguate: `OTLPMetricsClient` (collision-free — §5), aliasing `colmetricspb "go.opentelemetry.io/proto/otlp/collector/metrics/v1"`. | §3.6 |
| C5 | anchor line cites | Minor drift, none design-affecting: `StatsSinkConfig` struct is **`:357-372`** (cited `:357-363` — the `EmitTagsAsLabels` field extends it); the go-control-plane path is `github.com/envoyproxy/go-control-plane/envoy@v1.32.4/extensions/stat_sinks/open_telemetry/v3/…` (no `/envoy/` path segment — the split module roots `extensions/` directly). | §11 |

### 1.2 SPEC-time verification record

The re-derivation agent (read-only, tree at `7a293a06`) re-opened all 18 anchor groups + resolved the code-gated dockets: **no SEVERE drift; two minor anchor drifts (C5) folded in; the one design-material finding (C1, the `DiscardUnknown:false` strict posture) inverts the BRAINSTORM's silent-ignore lean and is carried into §3.3 + the ADR.**

**Adversarial-pass record (TWO independent verifiers ran against the draft in PRIVATE scratch before landing, `reference_parallel_subagents_private_scratch`):** **V1 — code-claims re-derivation + a DECISIVE probe of the load-bearing C1 claim.** V1 built a standalone offline Go module (blank-loading the v1.32.4 `open_telemetry/v3` descriptor) and unmarshaled a `google.protobuf.Any` (`@type` → `SinkConfig`) carrying the v1.37-only `resource_detectors` field with `protojson.UnmarshalOptions{DiscardUnknown:false}`: it **ERRORED with `unknown field "resource_detectors"`**, and with `DiscardUnknown:true` it silently dropped it. So protojson DOES recurse into the Any-expanded message and reject the version-skew field — **the D-OTLPM-SKEW "boot-rejected for free" claim HOLDS under executable probe** (not merely re-derived). V1 also re-confirmed the six-field/wrapper-default-TRUE layout, the histogram-reject precedent, the four→five sibling roster, the `NewOTLPLogsClient` unary shape + the `colmetricspb.MetricsServiceClient` collision, the fuzz `@type` dispatch, and all collisions clean. **ZERO SEVERE, ZERO MODERATE; one MINOR** ("byte-for-byte clone" wording — the wrapper narrows `Export`'s return to `error`), folded into §3.6. **V2 — process/consistency/adjudication:** all stage-close mechanics PASS (the tail flip; row 69 stays `in-progress`; the STATE pointer edited in place with five lineage bullets; next-prompt rolled to the PLAN; the sentinel re-run does NOT fire; no deferred sentence narrowed; the envelope docs-only; the docket disposed one-for-one; the SPLIT/FIXTURE/RIDER decisions defensible; the §13 roster carried with nothing dropped). **ZERO SEVERE, ZERO MODERATE; two MINOR** (§14 mirror faithfulness), folded above. **No SEVERE or MODERATE across either pass; the design direction — a fifth `stats_sinks[]` consumer on landed substrate, single flat row — is unchanged from the BRAINSTORM.**

---

## 2. Non-purposes (deferred; per BRAINSTORM §1.2 + §8)

Not histogram export (envoy-go registers NO histograms — ADR-0060, `mapping.go` "Histograms have no envoy-go analog"; the reference's Histogram stream with 19 default bounds, P-7, has no envoy-go counterpart — a NAMED COVERAGE BOUNDARY, and `report_histograms_as_deltas: true` is STRICT-REJECTED, §3.4). Not `resource_detectors` / `custom_metric_conversions` (v1.37.2-only fields absent from the pinned v1.32.4 `SinkConfig`; boot-rejected for free by the strict protojson layer — §3.3). Not an http_service transport (none exists in v1.37.2 — P-1; gRPC is the sole `protocol_specifier`). Not a reference-matching final shutdown flush (the landed sink-family "no final flush on Done" posture is carried, `flusher.go:31-32`; the reference's stop-coincident flush drops histograms anyway — P-15). Not `stats_flush_on_admin` (strict-rejected, `bootstrap.go:539-541` — unchanged). Not the require=false RBAC/principal fixture RIDER (stays deferred — §3.10, §13). Not SDS rotation, upstream SDS, the `ssl` stat family, tracing `metadata`, or any never-opened family (the BRAINSTORM §2.1 rejected roster, unchanged).

---

## 3. The change — the D-OTLPM-* docket disposed one-for-one

### 3.1 The core — a fifth `Sink` on the landed flush subsystem *(RE-DERIVED; EXECUTED P-1/P-2)*

`internal/statssink/otlp.go` (NEW FILE, existing package) adds `OTLPMetricsSink` implementing `Sink` (`Submit([]*dto.MetricFamily)` / `Close() error`, `sink.go:18-21`) + a package-local `otlpMetricsClient` interface seam (`Export(ctx, *colmetricspb.ExportMetricsServiceRequest) error` + `Close() error`) mirroring `metricsClient` (`sink.go:28-31`) so the production sink stays grpcclient-free (test fakes it). `Submit` maps the fan-out batch to ONE `ExportMetricsServiceRequest`: ONE `resourceMetrics` (the `telemetry.sdk.*` resource, §3.8) / ONE `scopeMetrics` (empty `scope` `{}` — P-2/P-3) / one `Metric` per registry family. Counter → `Sum{aggregationTemporality: CUMULATIVE, isMonotonic: true}`; Gauge → `Gauge`. The sink dials via the grpcclient `Dialer` route exactly as metrics_service / OTLP-logs do (`main.go`, `grpcclient.go`), NOT the statsd UDP path — so fixtures MUST NOT cross-side-assert the sink cluster's `upstream_cx_*` (`reference_cluster_sink_dial_unaccounted`; the two sides' dial accounting differs).

### 3.2 D-OTLPM-SCOPE — honor the six `SinkConfig` fields; `grpc_service` required *(RE-DERIVED F15)*

The pinned v1.32.4 `SinkConfig` has EXACTLY six fields (getters `:96-131`): `grpc_service` (`*v3.GrpcService`, the sole `protocol_specifier` oneof arm), `report_counters_as_deltas` (scalar bool), `report_histograms_as_deltas` (scalar bool), `emit_tags_as_attributes` (`*wrapperspb.BoolValue`), `use_tag_extracted_name` (`*wrapperspb.BoolValue`), `prefix` (string). `parseOpenTelemetrySinkConfig` projects them into a NEW `OTLPSinkConfig{ClusterName, ReportCountersAsDeltas, UseTagExtractedName, EmitTagsAsAttributes, Prefix}` (`Bootstrap.OTLPSinkConfigs []OTLPSinkConfig`, a PARALLEL slice beside `StatsSinkConfigs`/`StatsdSinkConfigs`/…). **`grpc_service` REQUIRED:** omitting it is a reference PGV boot-reject (`protocol_specifier … is required` — P-1); envoy-go mirrors it with an explicit reject via the Dialer-cluster gate (the metrics_service `cluster_name`-required precedent, `bootstrap.go:588-594`) — here PGV-parity and the "no transport ⇒ no sink" precondition coincide. A missing/non-H2 cluster `log.Fatalf`s at sink build (REFERENCE-PARITY, the metrics_service precedent); an empty `cluster_name` is rejected; `google_grpc` is rejected; a non-V3 `transport_api_version` is rejected (the metrics_service reject set, mirrored).

### 3.3 D-OTLPM-SKEW — **RESOLVED: the v1.37.2-only fields are boot-rejected for free by the strict protojson layer** *(RE-DERIVED — the design-material correction, C1)*

envoy-go's `Load` reads `protojson.UnmarshalOptions{DiscardUnknown: false}` and `Unmarshal`s the ENTIRE bootstrap (`bootstrap.go:510`). Once the `SinkConfig` descriptor is blank-imported (required for `@type` resolution + the descriptor-derived `openTelemetrySinkTypeURL`), a bootstrap that sets `resource_detectors` or `custom_metric_conversions` on the `open_telemetry` `SinkConfig` hits an "unknown field" error at boot — those fields do not exist in the pinned v1.32.4 descriptor. **DECISION: no new code; the version-skew is handled by the EXISTING strict layer** — a named envoy-go-STRICT DEPARTURE (the v1.37.2 reference boots+honors them; envoy-go boot-rejects), documented in BC (§9) + the ADR (§14), revisited at any go-control-plane bump. A SUBJECT unit test pins it (a `SinkConfig` document with `resource_detectors` → boot-fail). This SUPERSEDES the BRAINSTORM's "silently ignored / detect-and-reject open" framing: the reject already exists structurally; `UnmarshalTo` in `parseOpenTelemetrySinkConfig` never sees those bytes because protojson rejects the document first.

### 3.4 D-OTLPM-HISTO — **DECISION: STRICT-REJECT `report_histograms_as_deltas: true`** *(RE-DERIVED — the checked phase-47 precedent, refuting the accept-inert lean)*

envoy-go registers NO histograms (ADR-0060), so the knob is observationally inert. Two options: (a) REJECT `report_histograms_as_deltas: true` (strict), (b) accept-inert (vacuously honored). **DECISION: REJECT (a)** — mirroring the metrics_service `parseMetricsServiceConfig` which STRICT-REJECTS a non-default `histogram_emit_mode` ("envoy-go has no histograms", `bootstrap.go:595-597`, RE-DERIVED verbatim this SPEC) and the ADR-0080 house style. A `report_histograms_as_deltas: true` boot-reject with a distinct substring (e.g. `open_telemetry report_histograms_as_deltas is not supported (envoy-go has no histograms)`); absent/`false` accepted-and-inert. The accept-inert alternative is NAMED in the ADR as the argued counterpoint (with zero histograms the knob changes nothing on the wire, UNLIKE `histogram_emit_mode` which changes WHICH stats emit) — but strictness wins per the landed precedent. A SUBJECT unit test (no fixture — the reference boots the knob, so it is not cross-side-informative, the metrics_service reject-test precedent).

### 3.5 D-OTLPM-DEFAULTS — the two BoolValue knobs default TRUE ⇒ tag extraction + attributes are the CORE behavior *(EXECUTED P-5; RE-DERIVED C2)*

Baseline (knobs absent) emits TAG-EXTRACTED residual names + tags-as-attributes (`cluster.upstream_cx_total` + a `KeyValue` attribute `envoy.cluster_name`); both-false emits full dotted names with 0 datapoints carrying attributes (P-5, the discriminating direction). So the absent-wrapper default is **TRUE**. The reads (pinned, C2): `useTagExtractedName := cfg.GetUseTagExtractedName() == nil || cfg.GetUseTagExtractedName().GetValue()`; `emitAttrs := cfg.GetEmitTagsAsAttributes() == nil || cfg.GetEmitTagsAsAttributes().GetValue()` — nil → TRUE. When `useTagExtractedName` is true, the sink projects `stats.ExtractTags(family.Name)`'s residual DOTTED name (the ADR-0264 core, `label.go:38`, reused) as the OTLP metric `name`; when `emitAttrs` is true it maps each extracted tag to a `KeyValue` datapoint attribute keyed `"envoy." + TrimPrefix(key, "envoy_")` (the dotted-key form the `labelMapper` already produces, projected to `KeyValue` instead of `LabelPair`). When either is false the sink emits the full dotted name / omits attributes. The mapping builds the sink's OWN protos and never mutates the shared `Flusher` snapshot fan-out (`flusher.go:47-49`, the ADR-0263/0264 hard constraint).

### 3.6 D-OTLPM-CLIENT — the `OTLPMetricsClient` grpcclient stanza *(RE-DERIVED C11; the C4 disambiguation)*

`internal/grpcclient/grpcclient.go` gains `OTLPMetricsClient` (struct + `NewOTLPMetricsClient(d *Dialer, clusterName string)` + `Export(ctx, *colmetricspb.ExportMetricsServiceRequest) error` + `Close() error`), cloning the `NewOTLPLogsClient` shape (`:474-501`) over `colmetricspb.NewMetricsServiceClient(conn)` via `dialConn(d, "OTLP metrics client", clusterName)` + the embedded `connHolder`. **Signature note (adversarial-pass MINOR):** the OTLP-logs `Export` returns `(*ExportLogsServiceResponse, error)`; the wrapper narrows the unary return to `error` alone (discarding the `*ExportMetricsServiceResponse`), matching the `otlpMetricsClient` seam (§3.1) — so this is a shape clone with a narrowed return, not literally byte-for-byte. New alias `colmetricspb "go.opentelemetry.io/proto/otlp/collector/metrics/v1"`. **The wrapper name is `OTLPMetricsClient` DELIBERATELY** (C4): `colmetricspb`'s own client type is named `MetricsServiceClient`, colliding on the simple name with `metricsv3.MetricsServiceClient`; the wrapper disambiguates. The `connHolder` doc roster comment (`:156`) gains "OTLPMetricsClient" (documentation-only). New exports (`OTLPMetricsClient`/`NewOTLPMetricsClient`/`Export`/`Close`) land in the EXISTING grpcclient client-per-service roster — the normal growth mode for this package.

### 3.7 D-OTLPM-FAILOPEN — the UNARY-Export bounded-channel writer; fail-open + retry-per-flush *(EXECUTED P-14/P-15; RE-DERIVED C3)*

The sink's writer is the UNARY OTLP shape (C3): a bounded channel (cap 8, the `sink.go:33` default) + a writer goroutine that does `client.Export(ctx, req)` with **retry-once-per-Export**, drop-newest on channel-full, a rate-limited drop LOG (NOT a stat), and idempotent `Close` (`sync.Once` + drain-grace + cancel) — the `internal/accesslog` OTLP-logs sink / `internal/tracing` OTLPExporter precedent, NOT the streaming `MetricsServiceSink` lifecycle. With the collector down the sink fails open (the reference boots + serves + the ONLY failure signal is the sink cluster's own `upstream_cx_connect_fail` — no dedicated sink-health stat — P-14) ⇒ **+0 stats** (§7). **Feedback loop (P-15):** the sink cluster's own stats are themselves exported, so fixtures assert APPLICATION-cluster subsets only, never sink-cluster self-stat VALUES.

### 3.8 D-OTLPM-RESOURCE / D-OTLPM-NODE — envoy-go's own `telemetry.sdk.*` triple; per-side pins *(EXECUTED P-3; DECISION)*

The reference emits exactly `telemetry.sdk.name=envoy` / `telemetry.sdk.language=cpp` / `telemetry.sdk.version=<build>` as `resource.attributes`, no node id, empty scope (P-3/P-2). **DECISION (D-OTLPM-NODE):** envoy-go emits its OWN three-key triple — `telemetry.sdk.name`, `telemetry.sdk.language`, `telemetry.sdk.version` (values are envoy-go's: name `envoy-go`, language `go`, version its build/version string; the PLAN pins the exact strings from what the binary exposes) — and an empty `scope`. `reference_differential_reference_parses_full_message` applies: the `0112` differential asserts the three resource-attribute KEYS present on BOTH sides + per-side VALUES (never cross-side value equality); scope empty on both.

### 3.9 D-OTLPM-STARTTIME + D-OTLPM-DELTA + D-OTLPM-DUPNAME — the wire facts pinned *(EXECUTED P-6/P-9/P-10/P-12)*

- **STARTTIME (DECISION):** the reference emits `startTimeUnixNano` in MICROSECONDS (a factor-1000 bug, 16 digits) while `timeUnixNano` is genuine ns; gauge datapoints carry NO startTime (P-12). envoy-go emits CORRECT nanoseconds — the bug is deliberately NOT cloned (`reference_wire_format_both_sides_see_same_bytes` protects frame VALIDITY, not bug-cloning). Assertion posture: assert `startTimeUnixNano` STRUCTURALLY PER SIDE — subject: ns-magnitude (same digit-count/magnitude band as `timeUnixNano`) AND constant across cumulative flushes (cumulative start-time fixed at process start); reference: left UNASSERTED (its µs shape is its own). **NEVER cross-side startTime equality.**
- **DELTA (`report_counters_as_deltas`):** Sum flips `aggregationTemporality` to DELTA, KEEPS `isMonotonic: true`, per-window values are true deltas (6,0,0,… vs cumulative 6,6,6,…), startTime CHAINS to the previous flush's `timeUnixNano` (envoy-go chains from real ns, not the µs-bugged first window); gauges unaffected; the two delta knobs INDEPENDENT (P-9/P-10). Rides the landed `deltaState` (`delta.go`) applied in the writer so an enqueue-drop never latches (the ADR-0263 per-sink framing).
- **DUPNAME (DECISION):** same-residual-name stats are SEPARATE `Metric` entries (one dataPoint each), distinguished by attributes — 18 duplicated names per baseline flush, NOT merged (P-6). envoy-go MATCHES (one Metric per registry family). The driver-owned receiver keys on **(metric-name, sorted-attribute-set)**, ORDER-INSENSITIVE (Walk order vs the reference's internal order is not a contract).

`prefix` composes with tag extraction (`<prefix>.<residual>`, dot added — P-11).

### 3.10 RIDER — the require=false RBAC/principal fixture arm STAYS DEFERRED *(DECISION)*

The BRAINSTORM named it "the best RIDER" to attach if ≤1 task. RE-DERIVED: it is OFF-SUBJECT (RBAC, unrelated to stats sinks) and NOT ≤1-task once its own fixture driver + three-arm assertions + deliberate breaks + BC delta are counted (~2 tasks). Attaching an unrelated RBAC fixture to a stats-sink row would muddy the row's envelope and ADR-0291's scope. **DECISION: stays deferred (§13), does NOT attach.** The row is coherent as the OTLP-metrics sink alone.

---

## 4. Framework primitives — 0 new packages, 0 new go.mod modules, new exports only in existing rosters

No new seam, applier, resource type, or wire shape. **REUSES verbatim:** the `Sink` interface + `Flusher`/`flushOnce` (untouched) · `snapshot` (`mapping.go:22-47`) · `deltaState` (ADR-0263) · `stats.ExtractTags` via the `labelMapper` logic (ADR-0264) projected to `KeyValue` · the descriptor-derived TypeURL idiom (`bootstrap.go:224`) · the shared `dialConn`/`connHolder` unary-OTLP client shape (`grpcclient.go:474`/`:521`) · the `main.go:206-291` build-loop + LIFO drain · `FuzzStatsSinkConfigParse`. **+0 go.mod modules** (`go.opentelemetry.io/proto/otlp v1.0.0` DIRECT, `collector/metrics/v1` vendored — same module as the consumed logs/trace collectors; the `.gw.go` import set matches; re-check `git diff go.mod` after tidy at the IMPL — `reference_new_subpackage_pulls_transitive_module`). **The cycle guard STANDS TYPE-level:** `otlp.go` adds only the otlp proto imports — no grpcclient import (the `otlpMetricsClient` seam, §3.1); re-verify `go list -deps ./internal/statssink` (no `...`) at the IMPL.

## 5. Identifier hygiene *(collision checks — RE-DERIVED at `7a293a06`)*

`.go` grep repo-wide → **ZERO hits** for `OTLPMetricsSink` / `OTLPMetricsClient` / `NewOTLPMetricsClient` / `otlpMetricsClient` / `parseOpenTelemetrySinkConfig` / `otlpmetrics` / `D-OTLPM` (all doc-only). `test/helpers/otlpmetrics` does NOT exist (the `otlplogs`/`otlptrace`/`metricsservice` sibling precedents ARE present). In-container ports `10448`/`10449` free (`0111` took 10447). The one NAMED collision (C4): `colmetricspb.MetricsServiceClient` shares a simple name with `metricsv3.MetricsServiceClient` — the wrapper is `OTLPMetricsClient` to disambiguate. PLAN re-greps any further drafted names (`reference_spec_drafted_identifier_collision_check`).

## 6. Reject roster — added / retained

**No existing reject is retired or flipped.** NEW rejects this row (all SUBJECT unit tests — the reference boots them, so they are not cross-side-informative): (1) the `open_telemetry` sibling arm added to `parseStatsSinks` — the `:564-565` default reject's TypeURL roster grows four → five (`reference_strict_reject_sibling_typeurl_gap`: the new `case openTelemetrySinkTypeURL:` AND the error string BOTH extend); (2) `report_histograms_as_deltas: true` (§3.4, distinct substring); (3) `grpc_service`/`cluster_name` required + empty-cluster + `google_grpc` + non-V3 (§3.2, mirrored from metrics_service); (4) the version-skew fields are rejected for FREE by the strict protojson layer (§3.3 — no new arm, a structural reject). `report_counters_as_deltas` / `use_tag_extracted_name` / `emit_tags_as_attributes` / `prefix` are CONSUMED (not rejected). Roster byte-diffed at the IMPL (`reference_lifted_reject_hidden_enforcement`).

## 7. Stat surface **+0** · Fuzz **+0 fuzzers, SEEDS only, dispatch-verified** *(D-OTLPM-STATS / D-OTLPM-FUZZSEED)*

**Stats: +0; surface stays 1201.** The sink EMITS stats, it does not create them; the reference has NO sink-health stat (P-14) so no envoy-go sink-health stat is warranted; drops stay LOGGED not counted (the D-MS-STATS-FINAL precedent). A `TestNoNewStat_OTLPRegistrationGuard` pins it (the metrics_service/statsd precedent).

**Fuzz: +0 fuzzers; count stays 55.** The new parse arm slots into `FuzzStatsSinkConfigParse` as a SEED whose `stats_sinks[].typed_config` `@type` is `type.googleapis.com/envoy.extensions.stat_sinks.open_telemetry.v3.SinkConfig` (RE-DERIVED E13 — a seed reaches an arm purely by its `@type`; the fuzzer defines `msType`/`statsdType` constants and interpolates them, so an `otlpType` constant + a seed is the pattern). **Dispatch-verify (the named trap):** the `@type` resolves ONLY if the `SinkConfig` descriptor is blank-imported in `bootstrap.go` (else protojson errors "type not registered" BEFORE dispatch, hitting neither the new arm nor the sibling-reject — a vacuity trap in the `reference_vacuous_break_receiver_normalizes` family). The PLAN dispatch-verifies the seed REACHES `parseOpenTelemetrySinkConfig` (not the protojson type-registry error, not the sibling default) before committing it.

## 8. Differential fixtures — `0112` core + `0113` knobs *(D-OTLPM-FIXTURE — DECIDED: TWO fixtures)*

**DECISION: TWO fixtures** (the metrics_service `0089`/`0090` core/deltas precedent — the two assertion models genuinely differ, and one fixture dir = ONE runner branch, `reference_differential_fixture_dispatch_constraint`). Each uses TWO driver-owned per-side `test/helpers/otlpmetrics` receivers with a hard `Close()` (`reference_periodic_sink_differential_two_receivers`; a shared receiver contaminates the snapshot). Assertions are NAMED-SUBSETS only (`reference_stats_sink_emits_used_only` — the reference exports only USED stats where used ≠ non-zero and the set GROWS with use, P-4; envoy-go walks the FULL frozen registry). Reference sink `address` cluster pins `dns_lookup_family: V4_ONLY` (P-16, the Docker Desktop AAAA gotcha).

- **`0112-stats-sink-otlp` (port 10448) — the DEFAULT arm.** Knobs absent ⇒ cumulative + tag-extracted residual names + tags-as-attributes (the TRUE default, §3.5). K=7 deterministic requests/side; poll each side's receiver to converge a deterministic COUNTER subset (`cluster.<backend>.upstream_rq_total`, `http.<stat_prefix>.downstream_rq_total`, `http.<stat_prefix>.downstream_rq_2xx`) to Sum `value == K`. ASSERT both sides: the subset present as monotonic-cumulative `Sum` with `value == K` + the tag-extracted residual name + the expected `KeyValue` attribute set (keyed by `(name, sorted-attrs)`, §3.9); the three `telemetry.sdk.*` resource KEYS present (per-side values, §3.8); startTime ns-magnitude + constant per subject side (§3.9). UNasserted cross-side: the whole family set/count (surfaces differ — no envoy-go histograms), reference startTime, the sink cluster's `upstream_cx_*` (§3.1/§3.7), message framing.
- **`0113-stats-sink-otlp-knobs` (port 10449) — the KNOB arm.** `report_counters_as_deltas: true` + `prefix: <p>` + `use_tag_extracted_name: false` + `emit_tags_as_attributes: false` in ONE config (coherent: DELTA-temporality + full-dotted-PREFIXED names + no attributes). ASSERT the per-name RUNNING DELTA-SUM `== K` across flushes with a POST-CONVERGENCE STABILITY BARRIER (idle flushes confirming the SUM no longer climbs, `reference_delta_sink_differential_stability_barrier` — else the first flush's delta is indistinguishable from the absolute) on the `<prefix>.<full-dotted>` counter subset, DELTA `aggregationTemporality`, `isMonotonic` retained, NO attributes; gauges absolute. This ONE fixture proves all three knobs (deltas + prefix + both-false). The mixed combos (deltas+attributes-on compose; one-true-one-false naming) are `Submit` unit tests (the D-MSL-BOTH-KNOBS compose-smoke precedent).

**Fixtures 113 → 115.** Deliberate breaks per fixture (`-count=1`, full selectors, `Errorf` per property, confirm WHICH fired): `0112` — a Counter→Gauge type swap; a tag-extraction disable (residual→full-dotted, must fire the subset poll); a resource-key drop. `0113` — an absolute-not-delta break (post-barrier SUM overshoots K, the `0090` shape); a barrier-masks break; a prefix-drop. A NON-break control: reversing the receiver's attribute iteration must NOT fail (proving the `(name, sorted-attrs)` keying is order-insensitive, §3.9). Never treat a docker-proxy accept as liveness — a served-this-arm receiver assert + a real Export is the truth.

## 9. Behavior-contract edit map — pinned VERBATIM *(all edits land at the IMPL, atomically with ADR-0291)*

*(Anchors re-derived at `7a293a06`; the OTLP section inserts AFTER the graphite section `### Stats sinks — the graphite_statsd UDP sink …` [BC:817] and BEFORE `### Does not yet apply to (stats sinks)` [BC:835].)*

**B1 — NEW section `### Stats sinks — the OpenTelemetry (OTLP) metrics sink (per phase 69 ADR-0291)`.** Modeled on the graphite section (BC:817-833). Pins: the FIFTH `stats_sinks[]` consumer + the SECOND `envoy.extensions.…` typed-extension stat-sink dispatch (after graphite); the six-field surface (`grpc_service` required; the two `*wrapperspb.BoolValue` doc-default-TRUE knobs `use_tag_extracted_name`/`emit_tags_as_attributes` — bare scalar in YAML, nil→TRUE; the two scalar delta knobs; `prefix`); ONE unary `Export` per flush over the cluster-dialed `OTLPMetricsClient` (Counter → monotonic cumulative `Sum` / Gauge → `Gauge`; ONE resourceMetrics/scopeMetrics, empty scope); the TRUE default = tag-extracted residual names + tags-as-`KeyValue`-attributes (P-5); `report_counters_as_deltas` → DELTA temporality + `isMonotonic` retained + startTime chaining (the ADR-0263 `deltaState`); `prefix` composes; the `telemetry.sdk.*` resource triple (per-side values); fail-open + retry-per-flush, +0 self-stats; the reference's factor-1000 µs `startTimeUnixNano` bug NOT cloned (per-side structural startTime, never cross-side); STRICT-REJECT `report_histograms_as_deltas: true` (no histograms, ADR-0060) + the version-skew `resource_detectors`/`custom_metric_conversions` boot-rejected by the strict protojson layer (a named envoy-go-strict departure); byte-stable when unconfigured; the `0112`/`0113` differentials (two per-side driver-owned `test/helpers/otlpmetrics` receivers, named subsets, delta-SUM stability barrier on `0113`, `V4_ONLY` sink cluster). The two inherited departures (the reference emits USED-only stats ⇒ named subsets; the reference's own startTime µs bug).

**B2 — BC:836 `### Does not yet apply to (stats sinks)` first bullet — UPDATE the sibling-reject example.** The bullet currently reads "… + a sibling sink TypeURL **(OTLP-metrics)** …" — using OTLP-metrics as the EXAMPLE of a still-rejected sibling. Once OTLP-metrics is SUPPORTED, the example MUST change to a genuinely-unsupported sink TypeURL (e.g. `envoy.stat_sinks.wasm` / `hystrix` — the PLAN picks a real-but-unhandled one, `reference_sibling_reject_test_needs_real_typeurl`), and the sentence adds that `open_telemetry` is now CONSUMED (the six fields, with `report_histograms_as_deltas`/version-skew rejected). The "Histograms (no envoy-go analog, ADR-0060)" + "no drain-flush at shutdown" bullets stay, EXTENDED to name the OTLP `report_histograms_as_deltas` reject.

**B3 — BC:593 / the access-log intro neighborhood.** No change owed (the OTLP ACCESS-log sink `open_telemetry` at BC:638 is a DIFFERENT extension — `envoy.extensions.access_loggers.open_telemetry` vs the stats sink `envoy.stat_sinks.open_telemetry`; the SPEC notes the name-adjacency to forestall a reviewer conflation, but no BC edit).

## 10. Test plan + task surface *(D-OTLPM-SPLIT — DECISION: a SINGLE FLAT ROW)*

**DECISION: SINGLE FLAT ROW** (~10-12 tasks). The BRAINSTORM leaned "BY-CONCERN 2-leg anticipated" by analogy to phase 47 — but phase 47 SPLIT because it was BUILDING the substrate (core → deltas → tags). ALL that substrate is landed: the `deltaState` (ADR-0263), the `ExtractTags` core (ADR-0264), the unary-OTLP client precedent, the TypeURL dispatch. So the knobs are pure parameter-threading over landed machinery — exactly the situation of the phase-48/49/57 sink rows, ALL of which were SINGLE FLAT ROWS (statsd/dog_statsd/graphite each honored their knobs day-1 in one row). No two-package surface can strand a leg. **ADR-0045 escape valve ARMABLE but UNCONSUMED**; ONE ADR (ADR-0291). Sketch (the PLAN decomposes; TDD):

- **T1** — `internal/grpcclient`: `OTLPMetricsClient` + `NewOTLPMetricsClient`/`Export`/`Close` over `colmetricspb` (§3.6); unit test with a fake conn.
- **T2** — `internal/statssink/otlp.go`: `OTLPMetricsSink` + the `otlpMetricsClient` seam + the mapping (Counter→cumulative monotonic Sum / Gauge→Gauge; the `telemetry.sdk.*` resource; empty scope; `use_tag_extracted_name`/`emit_tags_as_attributes` via `ExtractTags`→name/`KeyValue`; `prefix`; `report_counters_as_deltas` via `deltaState`→DELTA; correct-ns startTime + cumulative-constant / delta-chaining) + the UNARY bounded-channel writer/retry/Close (§3.1/3.5/3.7/3.9). The largest task. Unit tests: the mapping matrix (types/temporality/attrs/names/prefix/delta/startTime) + the writer drop/retry/Close + `-race`.
- **T3** — `internal/bootstrap`: `openTelemetrySinkTypeURL` (descriptor-derived, blank-import the `SinkConfig` proto) + `parseOpenTelemetrySinkConfig` + `OTLPSinkConfig` + `Bootstrap.OTLPSinkConfigs` + the fifth `parseStatsSinks` arm + the sibling-reject roster four→five + the nil→TRUE wrapper reads + the `report_histograms_as_deltas` reject + the `grpc_service`/cluster/`google_grpc`/non-V3 rejects (§3.2-3.5). Unit tests: parse-accept (all six fields) + each reject + the version-skew boot-reject (§3.3) + the wrapper-default inversion.
- **T4** — `cmd/envoy-go/main.go`: the `:206` gate `|| len(bs.OTLPSinkConfigs) > 0` + a build sub-loop (`NewOTLPMetricsClient` → `NewOTLPMetricsSink` → append to `statsSinks`); `NewFlusher`/drain UNTOUCHED.
- **T5** — `test/helpers/otlpmetrics`: the per-side unary-Export accumulating receiver, `(name, sorted-attrs)`-keyed order-insensitive, with `Sum`/`Gauge`/temporality/resource-key/startTime accessors + a delta-SUM accessor (the `metricsservice`/`statsdrecv` driver-owned precedent — NOT a BackendKind, tail stays 38).
- **T6-T7** — fixtures `0112` (default) + `0113` (knobs) + deliberate breaks (§8). `-count=1`; full selectors; confirm WHICH fired; the order-insensitive NON-break control.
- **T8** — fuzz: the `otlpType` seed + dispatch-verification (§7).
- **T9** — BC delta (§9 B1-B2).
- **T10** — six-gate + full 115-dir differential + `-race` (FULL `internal/statssink` + `test/helpers/otlpmetrics` — the writer goroutine is a background mutator, `reference_full_suite_race_after_background_mutator`) + the cycle guard (`go list -deps ./internal/statssink` — no grpcclient edge) + `go mod tidy -diff` empty + envelope audit (`internal/xds`/`internal/tls`/`internal/boot`/`internal/listener` BYTE-UNTOUCHED; production = `internal/statssink/otlp.go` + `internal/bootstrap/bootstrap.go` + `internal/grpcclient/grpcclient.go` + `cmd/envoy-go/main.go`).
- **T11** — ADR-0291 completed IN PLACE (§Decision/§Consequences) + ROADMAP row-done (rolls "OTLP-metrics stats sink + " OUT of ROADMAP:198) + STATE/router (controller-adjacent, stage-close).

Coverage properties, each an independent `Errorf` (`reference_fatalf_makes_assertions_unreachable`): the type/temporality mapping; the TRUE-default tag-extraction+attributes; the nil→TRUE wrapper inversion; the delta-SUM==K + stability barrier; the prefix composition; the version-skew + histogram rejects; the correct-ns startTime (per side) + order-insensitive dup-name keying; the +0-stats guard; byte-stable-when-unconfigured.

## 11. Edit-site roster *(RE-DERIVED at `7a293a06`)*

**Production — `internal/statssink/otlp.go` (NEW FILE):** `OTLPMetricsSink` `[IMPLEMENT `Sink`]` + `otlpMetricsClient` seam + the mapping + the unary writer. **`internal/statssink` REUSED untouched:** `Sink`/`metricsClient` seam (`sink.go:18-21`/`:28-31`), `deltaState` (`delta.go:18`), `labelMapper`/`ExtractTags` (`label.go:13-24`/`:38`), `snapshot` (`mapping.go:22-47`), `Flusher` (`flusher.go:13-51`). **`internal/bootstrap/bootstrap.go`:** `parseStatsSinks` `:532-569` `[ADD the fifth arm; grow the `:564-565` reject to five TypeURLs]` · `metricsServiceTypeURL` `:224` `[CLONE the descriptor idiom → `openTelemetrySinkTypeURL`; blank-import `SinkConfig`]` · `parseMetricsServiceConfig` `:580-604` `[TEMPLATE — invert the wrapper defaults (nil→TRUE), mirror the histogram reject]` · `StatsSinkConfig` `:357-372` `[SIBLING struct → `OTLPSinkConfig`]` · the slice fields `:431-469` `[ADD `OTLPSinkConfigs`]` · the strict `protojson` posture `:510` `[VERIFY DiscardUnknown:false — the free version-skew reject]`. **`internal/grpcclient/grpcclient.go`:** `NewOTLPLogsClient` `:474` / `NewOTLPTracesClient` `:521` `[CLONE → `NewOTLPMetricsClient` over `colmetricspb`]` · `connHolder` roster comment `:156` `[APPEND, doc-only]`. **`cmd/envoy-go/main.go`:** the gate `:206` + build loop + `NewFlusher` `:275` + drain `:286-291` `[ADD one gate clause + one sub-loop; drain UNTOUCHED]`.

**Test/harness:** `test/helpers/otlpmetrics` `[ADD]` · `test/fixtures/0112-stats-sink-otlp/` (port 10448) + `test/fixtures/0113-stats-sink-otlp-knobs/` (port 10449) `[ADD]` · `FuzzStatsSinkConfigParse` seed (`statssink_fuzz_test.go:19`) `[ADD, dispatch-verified]` · unit tests (parse incl. wrapper inversion + histogram/skew rejects; mapping Sum/Gauge/attrs/prefix/delta/startTime; writer drop/retry/Close; registration guard). **UNTOUCHED:** `internal/xds`, `internal/tls`, `internal/boot`, `internal/listener`.

**Docs:** BEHAVIOR_CONTRACT per §9 `[IMPL]` · DECISIONS ADR-0291 §Context **AT THIS COMMIT** (§14), completion at the IMPL · ROADMAP/STATE/next-prompt `[STAGE-CLOSE]`.

## 12. Sentinel maintenance — a SENTENCE-NARROWING row, narrowed AT THE IMPL (the phase-57 precedent)

"OTLP-metrics stats sink" IS named in the live Observability `candidates:` sentence (ROADMAP:198). **Per the verified phase-57 graphite precedent, NO sentence is narrowed at ANY pre-IMPL stage** — at the phase-57 BRAINSTORM the chartered `graphite` candidate REMAINED in the sentence; it rolled OUT only at the phase-57 IMPL (`2825ca90`, located by SUBJECT). So ROADMAP:198 rolls "OTLP-metrics stats sink + " OUT at the phase-69 IMPL row-done edit, NOT before; the sentence stays a LIVE `candidates:` sentence afterward (the `ssl` family + tracing custom_tags candidates remain — `reference_sentinel_deferred_sentence_live_vs_historical`). Check (1) keeps printing `NOT DONE: row 69` until the IMPL six-gate; checks (2)+(3) unchanged (`grep -cE 'remaining deferred \(not-yet-chartered\) candidates:'` → 3; `NEVER OPENED: gRPC/Runtime/WASM`) ⇒ the sentinel does NOT fire. **Nothing is owed at this SPEC.**

## 13. Deferred items *(the BRAINSTORM §8 roster carried MINUS this row's pickup)*

Carried unchanged: `xds-sds-upstream-server-cert` (VALUE-level cycle — do not re-derive) · SDS rotation (`watched_directory` positive control FIRST) · the `validation_context_type` missing-`default:` arm · `crl` · the compose-two CVC edge (`seen>1`) · the `ssl` stat family (framework surgery, ADR-0286 C3) · HTTP/3 `QuicProtocolOptions` · DataSource `environment_variable` (D-ENV-HARNESS-gated) · tracing custom_tags `metadata` / `spawn_upstream_span` / `http_service` / force-trace remainder · CDS/EDS · LDS/RDS · ADS · Delta xDS · RTDS · `google_grpc` · gRPC/Runtime/WASM family openers · QUIC client-auth · the cert-provider oneof arms · **the require=false RBAC/principal fixture arm** (the BRAINSTORM's named RIDER — STAYS deferred, §3.10) · the plain SDS-VC empty-served retained departure · the S2/S3 set-but-broken-served behaviors (boot-FAIL matching the reference NACK) · reclassifying the ACKed-empty SDS resource as a stream-level SUCCESS (D-EDF-STREAMPOSTURE option (a)) · the ADR-0080 + ADR-0044 citation drifts. **REMOVED: OTLP-metrics stats sink (THIS pickup).**

**Newly deferred at THIS row:** histogram export (blocked on ADR-0060 — reopening histograms is its own framework row; `report_histograms_as_deltas: true` STRICT-REJECTED, §3.4) · `resource_detectors` + `custom_metric_conversions` (v1.37.2-only fields absent from the pinned proto — boot-rejected by the strict protojson layer, a named version-skew departure revisited at any go-control-plane bump, §3.3) · a reference-matching final shutdown flush (the sink-family "no final flush on Done" posture carried; the reference's final flush drops histograms anyway — P-15) · an http_service OTLP transport (none in v1.37.2).

**Memory update owed at the IMPL:** the version-skew posture — a pinned dependency lacking a NEWER proto field means a config setting it is boot-REJECTED FOR FREE by the `DiscardUnknown:false` protojson layer (NOT silently ignored, as an `UnmarshalTo`-only reading would suggest — the whole-document protojson pass rejects the unknown field before the per-message `UnmarshalTo` ever runs); a named envoy-go-strict version-skew departure, no new code. And: the OTLP two-`*wrapperspb.BoolValue` knobs default TRUE ⇒ a nil wrapper reads `== nil || .GetValue()`, the INVERSE of the scalar-`.GetValue()` idiom — check the proto TYPE per field, do not template blind (`reference_protojson_wrapper_scalar_not_object` extended).

## 14. ADR continuity — the ADR-0291 §Context DRAFT (anchored here; full entry at the phase-69 IMPL)

*(The canonical entry is in `docs/envoy-go/DECISIONS.md`, appended at this SPEC commit — the tail flips ADR-0290 → ADR-0291 there; the IMPL completes the entry IN PLACE. This blockquote reproduces its prose; the STATUS-banner + `### Context` heading wrapper follows the DECISIONS file convention, so the framing differs from the blockquote form below while the content is the same.)*

> **ADR-0291 — the OpenTelemetry OTLP metrics stats sink (`envoy.stat_sinks.open_telemetry`, `SinkConfig`; the SIXTH stats-sink-lineage row + the FIFTH `stats_sinks[]` consumer; a NEW `internal/statssink/otlp.go` `OTLPMetricsSink` over the LANDED phase-47 `Flusher`/`Sink` seam — per-flush ONE unary `Export` (`ExportMetricsServiceRequest`) over a cluster-dialed `OTLPMetricsClient` (the unary OTLP-logs/traces client precedent), Counter → monotonic cumulative `Sum` / Gauge → `Gauge`, tag-extracted residual names + tags-as-`KeyValue`-attributes as the TRUE default (the two `*wrapperspb.BoolValue` knobs doc-default TRUE, nil→true, INVERTING the metrics_service `.GetValue()` template), `report_counters_as_deltas` riding the ADR-0263 `deltaState` (DELTA temporality, `isMonotonic` retained, startTime chaining), `prefix` composing, the reference's factor-1000 µs `startTimeUnixNano` bug deliberately NOT cloned (per-side structural startTime, never cross-side equality), fail-open + retry-per-flush with NO sink-health stat; `report_histograms_as_deltas: true` STRICT-REJECTED (envoy-go has no histograms, ADR-0060) + the v1.37.2-only `resource_detectors`/`custom_metric_conversions` boot-rejected by the existing `DiscardUnknown:false` protojson strict layer (a named version-skew departure); a SINGLE FLAT ROW — FLIPS ROW 69 `done`; the Observability family STAYS OPEN; new exports ONLY in the existing `internal/statssink`+`internal/grpcclient` rosters — the `internal/xds` zero-new-symbol discipline UNTOUCHED).**
>
> **§Context (drafted at the phase-69 SPEC per ADR-0044).** Phase 47 opened the bootstrap `stats_sinks[]` surface and built the sink-agnostic flush subsystem (the `Flusher` walking the frozen `stats.Registry` per `stats_flush_interval`, the `Sink` interface, the Counter/Gauge → `[]*dto.MetricFamily` `snapshot()`, the `deltaState` per-flush Counter delta [ADR-0263], the `labelMapper`/`stats.ExtractTags` tag core [ADR-0264], the post-`Freeze` `main.go` build). Phases 48/49/50/55/57 added four more `stats_sinks[]` consumers (statsd/dog_statsd/graphite + the statsd TCP transport) purely additively — each a new `Sink`, no new framework piece. Phase 69 adds the SIXTH stats-sink-lineage row + the FIFTH `stats_sinks[]` consumer: the OpenTelemetry OTLP metrics stats sink. A discriminating live probe (`envoyproxy/envoy:contrib-v1.37.2`, fresh container per arm, a throwaway per-arm unary-Export Go receiver with served-this-arm asserts; the phase-69 BRAINSTORM's Dossier P, 16 conclusions, adversarially verified) pinned: gRPC is the ONLY transport in v1.37.2 (PGV boot-reject when `protocol_specifier` is absent); ONE unary `Export` per flush carrying ONE `resourceMetrics` / ONE `scopeMetrics` (empty scope) / one `Metric` per stat; the resource attributes are exactly `telemetry.sdk.name`/`telemetry.sdk.language`/`telemetry.sdk.version` (no node id); Counter → monotonic `Sum` (default CUMULATIVE) and Gauge → `Gauge`; the reference emits ONLY used stats and the set GROWS with use; the TRUE default is tag-extracted residual names + tags-as-`KeyValue` attributes (proved by the both-false discriminating arm — `emit_tags_as_attributes` and `use_tag_extracted_name`, both `*wrapperspb.BoolValue` DOC-DEFAULT TRUE); same-residual-name stats are SEPARATE `Metric` entries distinguished by attributes; `report_counters_as_deltas: true` flips temporality to DELTA while RETAINING `isMonotonic` and chaining startTime; `prefix` composes with tag extraction; the reference emits `startTimeUnixNano` in MICROSECONDS (a factor-1000 bug) while `timeUnixNano` is genuine ns and gauges carry no startTime; with the collector down the reference fails open and retries per flush with NO sink-health stat (the only failure signal is the sink cluster's own upstream stats). envoy-go registers NO histograms (ADR-0060), so the reference's Histogram stream (19 default bounds) has no counterpart — a NAMED COVERAGE BOUNDARY.
>
> **The design (§Decision anticipated).** A NEW `internal/statssink/otlp.go` `OTLPMetricsSink` implements the landed `Sink` interface, mapping the `Flusher`'s `[]*dto.MetricFamily` fan-out to one `ExportMetricsServiceRequest` (Counter → monotonic cumulative `Sum`, Gauge → `Gauge`; the `telemetry.sdk.*` resource with envoy-go's own values [name `envoy-go`, language `go`, version its build string]; empty scope). The two `*wrapperspb.BoolValue` knobs are read `w == nil || w.GetValue()` (nil → TRUE — the INVERSE of the metrics_service scalar `.GetValue()` idiom): when `use_tag_extracted_name` is true the metric name is `stats.ExtractTags`'s residual dotted name (the ADR-0264 core reused); when `emit_tags_as_attributes` is true each extracted tag becomes a `KeyValue` datapoint attribute keyed `envoy.<tag>`; `prefix` prepends `<prefix>.`. `report_counters_as_deltas` rides the ADR-0263 `deltaState` (DELTA temporality, `isMonotonic` retained, startTime chaining, gauges absolute), applied in the writer so an enqueue-drop never latches. envoy-go emits CORRECT nanosecond startTime — the reference's µs factor-1000 bug is deliberately NOT cloned. The sink dials via the grpcclient `Dialer` (the metrics_service / OTLP-logs precedent) through a NEW `internal/grpcclient` `OTLPMetricsClient` (the unary `NewOTLPLogsClient` clone over `go.opentelemetry.io/proto/otlp/collector/metrics/v1` — named `OTLPMetricsClient` to disambiguate from that package's own `MetricsServiceClient` type); the writer is the UNARY bounded-channel + writer-goroutine + retry-once-per-`Export` + drop-newest + idempotent-`Close` shape (the OTLP-logs/traces sink precedent), NOT the streaming metrics_service lifecycle. A `parseOpenTelemetrySinkConfig` bootstrap arm lifts the `open_telemetry` TypeURL (descriptor-derived, blank-imported) into a fifth `parseStatsSinks` dispatch arm (the sibling-reject default roster grows four → five, `reference_strict_reject_sibling_typeurl_gap`) and projects the six fields into a new `OTLPSinkConfig` / `Bootstrap.OTLPSinkConfigs`; `main.go` fans it into the shared `statsSinks []Sink` slice + shared `Flusher`. `report_histograms_as_deltas: true` is STRICT-REJECTED (mirroring the metrics_service `histogram_emit_mode` reject — "envoy-go has no histograms", ADR-0080); the v1.37.2-only `resource_detectors`/`custom_metric_conversions` are boot-rejected for free by envoy-go's whole-document `protojson.UnmarshalOptions{DiscardUnknown: false}` pass (a named envoy-go-strict version-skew departure, revisited at any go-control-plane bump — NOT a silent-ignore). Envelopes: +0 packages, +0 go.mod modules (`go.opentelemetry.io/proto/otlp v1.0.0` DIRECT with `collector/metrics/v1` vendored), +0 stats (the sink emits stats, does not create them; the reference has no sink-health stat; drops are LOGGED not counted), +0 fuzzers (a `FuzzStatsSinkConfigParse` seed on the `open_telemetry` `@type`, dispatch-verified to reach the new arm), +0 BackendKinds (a driver-owned `test/helpers/otlpmetrics` receiver); functional production edits in `internal/statssink/otlp.go` (new file) + `internal/bootstrap/bootstrap.go` + `internal/grpcclient/grpcclient.go` + `cmd/envoy-go/main.go`; new exported symbols ONLY in the existing `internal/statssink`/`internal/grpcclient` rosters (the normal growth mode for those packages) — the `internal/xds` zero-new-symbol discipline UNTOUCHED; `internal/xds`/`internal/tls`/`internal/boot`/`internal/listener` byte-untouched. Proven by two differentials over per-side driver-owned receivers: `0112-stats-sink-otlp` (default: cumulative monotonic `Sum`/`Gauge`, tag-extracted names, tags-as-attributes, `Sum value == K`, the three resource keys per-side, correct-ns startTime) and `0113-stats-sink-otlp-knobs` (`report_counters_as_deltas` + `prefix` + both bool knobs false: the per-name running DELTA-SUM `== K` with a post-convergence stability barrier on `<prefix>.<full-dotted>` counters, no attributes) — fixtures 113 → 115; assertions are NAMED SUBSETS (the reference emits used-only stats), never cross-side startTime equality, never the sink cluster's own `upstream_cx_*` (dial-unaccounted + the export feedback loop), with `dns_lookup_family: V4_ONLY` on the sink cluster. A SINGLE FLAT ROW (the phase-48/49/57 sink precedent — the substrate is landed, so the knobs are parameter-threading, not new machinery; the ADR-0045 valve is armable but unconsumed). The Observability family STAYS OPEN (the `ssl` stat family + tracing custom_tags candidates remain).
>
> *(§Decision + §Consequences land at the phase-69 IMPL.)*

## 15. Exit — counts + expectations at SPEC-DONE

**Counts at this SPEC commit (docs-only apart from the DECISIONS append):** fixtures **113** · fuzzers **55** · stat surface **1201** · BackendKind **38** · go.mod modules **2** · **DECISIONS tail flips ADR-0290 → ADR-0291 AT THIS COMMIT** (the §Context draft append IS the tail flip; next-free becomes ADR-0292; the router's "re-derive, never trust" tail check must expect `## ADR-0291`).

**Anticipated at the phase-69 IMPL:** fixtures **113 → 115** (`0112-stats-sink-otlp` @10448 + `0113-stats-sink-otlp-knobs` @10449) · stat surface **1201 (+0)** · fuzzers **55 (+0)** · BackendKind **38 (+0)** · go.mod **2 (+0)** · ZERO new packages · new exported symbols ONLY in `internal/statssink` (`OTLPMetricsSink`/`NewOTLPMetricsSink`) + `internal/grpcclient` (`OTLPMetricsClient`/`NewOTLPMetricsClient`) — the `internal/xds` discipline UNTOUCHED · functional production edits in `internal/statssink/otlp.go` + `internal/bootstrap/bootstrap.go` + `internal/grpcclient/grpcclient.go` + `cmd/envoy-go/main.go` · `internal/xds`/`internal/tls`/`internal/boot`/`internal/listener` BYTE-UNTOUCHED · ADR-0291 completed IN PLACE · ROADMAP:198 narrows "OTLP-metrics stats sink + " OUT at the row-done edit.

**Sentinel:** re-verified expectation — check (1) keeps printing `NOT DONE: row 69`; checks (2)+(3) unchanged; **no deferred-sentence edit at any stage** (§12); no `stop` file.

**Probe hygiene:** no fresh probe ran this SPEC (§ header — the BRAINSTORM's Dossier P discriminated every behavioral gate this session; no docket hinges on unverified reference behavior); the worktree contains only this SPEC + the DECISIONS append at commit time.

**Next → the phase-69 PLAN.** Its inheritance: the SINGLE FLAT ROW ~11-task spine (§10); the six-field parse with the nil→TRUE wrapper inversion (§3.5) + the histogram reject (§3.4) + the free version-skew reject (§3.3); the UNARY-Export bounded-channel writer, NOT the streaming lifecycle (§3.7); the `OTLPMetricsClient` disambiguation (§3.6); the `deltaState`/`ExtractTags` reuse (§3.5/3.9); the TWO fixtures with the delta-SUM stability barrier on `0113` (§8); the fuzz-seed dispatch-verification trap (§7); the pinned §9 BC edit map (incl. the BC:836 sibling-example swap) applied MECHANICALLY. **RE-DERIVE this document; do not execute it.** Where it cites, go look; where it claims control flow, walk the call graph; default to REFUTED.
