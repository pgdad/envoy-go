# Phase 29.3 — `network-filter-mongo-fault-delay-and-access-log`

**Status:** SPEC authored (lifecycle-state 1 → 2 at this commit; ready for 29.3 PLAN authoring per `superpowers:writing-plans`). PLAN / PROGRESS / REVIEW artifacts still pending; land when this sub-phase enters subsequent lifecycle states.

**What this sub-phase is.** 29.3 is the **third and FINAL of the three phase-29 BRAINSTORM-time pre-split sub-phases** (`29` parent; the 3-way split settled at the parent SPEC §3). It lands the framework's FIFTH structural extension — the **async halt/resume seam** — and its first consumer, mongo **fault-delay injection**; the **mongo access log**; **`cx_drain_close`**; and CLOSES the deferred **close-direction seam** (D-P4 → `cx_destroy_*` VALUE parity). Unlike 29.1/29.2 (framework-ZERO-touch), 29.3 is the **framework-SURGERY** sub-phase — the ONE consolidated ripple (ADR-0219) where the deferred framework work converges. This sub-phase carries the **parent-row-29 ROLLUP**: parent row 29 flips `in-progress → done` ATOMICALLY with sub-row 29.3 (the 18/19/22/24/25/26/28 precedent), closing the FOURTH §9 Network-filters-family row.

**Masters (read first, in order):**

1. [`../29-network-filter-mongo-proxy/SPEC.md`](../29-network-filter-mongo-proxy/SPEC.md) — **the parent SPEC is this sub-phase's master.** Its §3.2 (the 29.3 scope detail), §4.1 (the async halt/resume seam shape — the three real extensions), §7 (the `delays_injected`/`cx_drain_close`/`cx_destroy_*` created-at/incremented-at split), §8.4 (the `0052` fixture envelope), §11.6 (fault-delay semantics), §11.7 (continueReading re-dispatch), §11.8 (access-log format), §11.10 (close direction + drain posture), and §12 (D-P5/D-P7/D-P12) all remain authoritative; this SPEC executes + refines them.
2. [`../29.1-network-filter-mongo-wire-and-requests/SPEC.md`](../29.1-network-filter-mongo-wire-and-requests/SPEC.md) + [`../29.2-network-filter-mongo-responses-and-correlation/SPEC.md`](../29.2-network-filter-mongo-responses-and-correlation/SPEC.md) — the as-built request + response surface this completes: the parsed-but-unconsumed `delay`/`access_log` config (29.1), the active-query list + the `start time.Time` per entry + the per-connection `mu` + the `OnDestroy` residual drain (29.2), the eager `delays_injected`/`cx_drain_close`/`cx_destroy_*` roster + their `0051` presence-only arms (the D-P4 boundary this CLOSES).
3. `docs/envoy-go/DECISIONS.md` ADR-0226 §Context (this sub-phase's ADR; §Decision/§Consequences body lands at the 29.3 IMPL) + ADR-0225/0224 (the response/request side this builds on) + ADR-0221 (the WriteFilter seam + the §Consequences halt-consumer anticipation this CONSUMES, refined to the READ side) + ADR-0213/0215 (the chain framework + TerminalFilter seam this extends) + ADR-0223 (the minimal-critical-section synchronization) + ADR-0219 (the no-ripple / one-ripple discipline) + ADR-0044/0052/0060.
4. The precedents: `internal/filter/http/fault/fault.go` (the FaultDelay `rollPercent`/`time.AfterFunc`/`markedActive` eval+cancel) + `internal/accesslog/` (the `AsyncFileSink` async-writer the formatter seam extends) + `internal/drain/manager.go` (the phase-08.2 `IsDraining()` drain decision) + `internal/filter/tcpproxy/filter.go` (the two pump goroutines — the close-direction recording site) + `test/fixtures/0051-mongo-responses/` (the `0052` driver template + `TCPMongoResponder` BackendKind 30).

**Authoritative sub-phase SPEC:** [`./SPEC.md`](./SPEC.md) — the async halt/resume seam (the three extensions) + fault-delay injection + the access-log formatter seam + `cx_drain_close` + the close-direction seam (D-P4 CLOSED) + fixture `0052` + the parent-row-29 ROLLUP.

**Scope at 29.3** (per the parent SPEC §3.2 + ROADMAP row 29.3):

- (a) The **async halt/resume seam** (the framework's FIFTH structural extension): ACTIVE asynchronous `ContinueReading` (a resume from a `time.AfterFunc` goroutine advances past the halting filter, dispatches the buffered bytes, re-evaluates terminal readiness); cross-goroutine safety (a per-`chainRuntime` halt mutex behind an atomic fast-path so never-halting chains are byte-identical); post-handoff read-halt honoring (`replayRead` + `readChainConn.Read` withhold-until-resume — lifting the 28.1b §3.5 boundary for halt purposes only). Anticipated zero new exported API; `TerminalFilter.Handle` UNCHANGED.
- (b) **Fault-delay injection** (the seam's first consumer): the parsed `delay` FaultDelay consumed — per-decoded-request-message eval with a re-entrancy guard; `rollPercent`-style percentage (deterministic at 100%); `time.AfterFunc` → `ContinueReading()` on fire; `delays_injected` at timer-arm; StopIteration-while-pending; timer cancel on destroy.
- (c) The **mongo access log**: the parsed `access_log` path consumed — one JSON line per decoded message both directions (`time`/`message`/`upstream_host`); a pluggable formatter seam on `internal/accesslog` (HCM byte-identical); timing-bearing → unit goldens + a coverage boundary, NO fixture dir.
- (d) **`cx_drain_close`**: reply-completion drain close (the active-query list empties + the drain signal active → increment + `Connection().Close(FlushWrite)`); a minimal drain-state accessor threads the phase-08.2 `*drain.Manager` to the network filter callbacks.
- (e) The **close-direction seam — D-P4 CLOSED**: which `tcp_proxy` pump EOF'd first → `chainRuntime.closeDirection` → a callbacks accessor → the `OnDestroy` direction-keyed `cx_destroy_local/remote_with_active_rq` increment (VALUE parity; the 29.2 presence-only boundary resolved).
- (f) Fixture **`0052-mongo-fault-delay`** (cross-side; DETERMINISTIC 100%-probability arms; the pre/post-handoff delay arms; the no-delay non-perturbation arm; `cx_drain_close`; the `cx_destroy_*` VALUE-parity arms; timing NEVER compared) + the R4 deliberate-break.
- (g) The **completion bundle**: ADR-0226 §Decision/§Consequences body in place; the BEHAVIOR_CONTRACT 29.3 bundle (incl. the framework async-halt/resume subsection + the access-log + runtime-key boundaries); the six-gate; STATE/ROADMAP advance (sub-row 29.3 `in-progress → done` **AND the parent-row-29 ROLLUP** — parent `in-progress → done` ATOMICALLY).

**D-question resolutions (this SPEC).** **D-P12** (halt-state synchronization): RESOLVED at the semantic level — a per-`chainRuntime` halt mutex behind an atomic fast-path + post-handoff withhold-until-resume; the exact primitive (Mutex+Cond vs channel) is an IMPL D-question (D-S29.3-1). **D-P7** (access-log formatter seam): RESOLVED — a pluggable formatter on `AsyncFileSink` (default `Default` → HCM byte-identical) + a mongo carrier (D-S29.3-4). **D-P5** (delay-PGV fixture arms + header_delay): RESOLVED — the AMEND-B9 reject arms stay unit-test-only; `header_delay` = parse-accept-no-delay. The drain-state + close-direction accessors are a minimal callbacks-extension ripple (the `*drain.Manager` is at the listener but not yet at network filters; the close direction is a tcp_proxy + chain.go touch — the 29.2 D-P4 deferral target). See SPEC §3.1–§3.5 + §10.

**Predecessor:** Phase 29.2 complete (IMPL squash `620a3d0`; SPEC squash `13907f1`; PLAN squash `0783776`).

**Successor:** the phase-29 family-row CLOSES at the 29.3 IMPL (the parent-row-29 ROLLUP). After phase 29, 3 §9 Network-filters-family candidates remain (`redis`/`kafka_broker`/`thrift`) — each its own future family phase with its own brainstorm-time risk assessment.
