# Phase 39 — `upstream-health-check` (active health checks)

**Status:** PLAN DONE (lifecycle-state 2 → 3 at this commit; `docs/envoy-go/phases/39-upstream-health-check/PLAN.md` — SPEC §10 decomposed into **16 bite-sized TDD tasks**; the FINAL ADR-0045 split-gate re-check → **NO FURTHER SPLIT** [~500–800 prod LoC / 16 tasks — both axes under the gate; single flat 39.1 leg]; the §12 D-S39-1..D-S39-6 RESOLVED [file placement = `health.go` + the six-LB `*clusterHealth` field; hosts start HEALTHY + first-check-immediate; envoy-go's own byte-stable rejects + tcp/grpc 39.1 DEPARTURE-reject; `0066` constants = 2-live/1-dead, interval/timeout 0.5s, unhealthy_threshold 1, N=100, poll-`membership_healthy==2`; injectable `probeOnce` + cancel-in-Drain test hygiene]; plan-document-reviewer APPROVED). **Next → the phase-39.1 IMPL** (`superpowers:subagent-driven-development` per `feedback_execution_style` — execute PLAN.md Tasks 1–16 in a fresh worktree; Task 1 creates PROGRESS.md). ALL counts UNCHANGED at this SPEC (stat surface 1125 / fixtures 67 / fuzzers 42 / BackendKind tail 33 / DECISIONS tail ADR-0241, next-free ADR-0242). **SPEC-firmed deltas vs the BRAINSTORM:** (AMEND-HC1) first-check-immediate (thresholds gate only subsequent transitions; `/clusters` health_flags `healthy`/`failed_active_hc`); (AMEND-HC2) the PGV reject wordings pinned (`field: "health_checker"… is required` / `…Path: value length must be at least 1 characters` / `…Interval/Timeout/UnhealthyThreshold: value is required`); the TCP checker parse-ACCEPTS at the reference → envoy-go 39.1 DEPARTURE-rejects tcp/grpc checkers; `expected_statuses` default 200; (AMEND-HC4) the full reference stat set pinned, MVP **+7 → stat surface 1132** (not the BRAINSTORM's ~1131; the deferred-feature stats {passive_failure/verify_cluster/degraded/membership_change/degraded/excluded/update_no_rebuild} are recorded departures); (AMEND-HC5) `healthy_panic_threshold` default 50%, **strict `<`** (50% does NOT panic), `lb_healthy_panic`++ per panic request → `0066` uses 2-live/1-dead (66%); (AMEND-HC8) ZERO new go.mod module + `google.golang.org/grpc v1.70.0` ALREADY a direct dep → 39.2 gRPC uses `grpc_health_v1` directly.

**What this phase is.** Phase 39 is the **FIRST Upstream-robustness-family row** — the first NEW feature family opened since §9 (Network filters) closed at phase 33 (phases 34–38 were all Load-balancing-family rows). It lands `Cluster.health_checks` (`[]core.config.core.v3.HealthCheck`, proto field 8) — **active** upstream health checking (periodic outbound probes that mark hosts healthy/unhealthy) over an HTTP + TCP + gRPC checker roster — plus the project's first per-host **health-state dimension** and the health-aware LB pick that makes all six LB constructs route only to healthy hosts (with a panic-threshold fallback). It is the family **keystone**: the host health-state primitive it introduces (i) unblocks the open Load-balancing family's 3 health-gated candidates {locality-weighted LB, priority load balancing, panic thresholds}, and (ii) is the substrate the sibling rows {outlier detection, circuit breakers, retries + hedging, connection pooling} compose with. Until phase 39, `Endpoint` carried no health field and `internal/cluster` had no background runtime.

**The envelope (BRAINSTORM; a pre-authorized 39.1/39.2 by-codec split).**

- **39.1 — the keystone + the HTTP checker** (this leg): the per-cluster host-health registry (`map[string]*hostHealth` keyed by `Endpoint.Addr()`; atomic healthy + consecutive-result counters; `Endpoint` unchanged) + the active-HC runtime (per cluster × per `health_check` background goroutine; `interval`/`timeout`-driven; `unhealthy_threshold`/`healthy_threshold` transitions; the `Manager.StartHealthChecks(ctx)` boot start + the `Manager.Drain()` cancel/wait — the project's FIRST cluster-level background runtime) + the HTTP probe codec (`GET <path>` + `expected_statuses`, reusing the H1/H2 client) + the health-aware LB pick (Approach A across all six constructs — skip unhealthy + re-pick; ring_hash/maglev walk-to-next-healthy preserving key stability; the `healthy_panic_threshold` fallback) + the `0066-health-check-http` differential. The two ADRs (ADR-0242 + ADR-0243) land here. Anticipated **~500–800 prod LoC / ~14–20 tasks**.
- **39.2 — TCP + gRPC checkers** (deferred): `tcp_health_check` (connect / send-receive) + `grpc_health_check` (`grpc.health.v1.Health/Check` over the H2 path), reusing the 39.1 health-state + runtime + LB substrate UNCHANGED (the framework-zero-touch second consumer, the thrift-33 reuse precedent). Anticipated +1 BackendKind (a gRPC SERVING responder) + a candidate `FuzzGrpcHealthResponse`. Its own SPEC/PLAN/IMPL; may itself split (TCP vs gRPC) at its SPEC.

**Key BRAINSTORM decisions.** (Q0) the next subject opens a NEW family — **Upstream robustness** (chosen over xDS/gRPC/HTTP3 as the foundational reliability layer; it unblocks the 3 health-gated LB candidates). (Q0b) the first sub-project = **active health checks** (the keystone — establishes the health boundary the family exists to deliver; chosen over the cleaner-to-differential retries/circuit-breakers). (Q1) the checker roster = **HTTP + TCP + gRPC**, split by codec (39.1 HTTP keystone / 39.2 TCP+gRPC). (Q2) the health-aware LB = **Approach A: pick-time filter + panic fallback**, with the health view **build-time-injected** (host health is CLUSTER state, NOT a per-request `Pick` widening — the deliberate CONTRAST with the ADR-0235 hash-key / ADR-0239 subset-match ctx-carry pick-inputs; the `Pick` signature + exported `Cluster` surface stay BYTE-STABLE; behavior-neutral when all healthy → every existing fixture stays byte-identical).

**The differential proof (poll-to-convergence; a NEW determinism shape).** ONE fixture (39.1): `0066-health-check-http` — an HTTP listener routing to a cluster with `health_checks: [http_health_check]` over {a LIVE HTTP backend, a DEAD unbound endpoint}, on BOTH sides, with short `interval`/`timeout` + low `unhealthy_threshold`. The driver POLLS the admin `/stats` until the dead host converges to unhealthy on BOTH sides (no fixed `time.Sleep`), THEN runs the load phase and asserts 100% of N requests land on the LIVE host + cross-side `health_check.*`/`membership_*` parity + `upstream_rq_total` cross-equal. The dead-host-via-unbound-address design needs **NO new BackendKind** (the live host reuses the HTTP backends; tail stays 33) and **NO new fuzzer** (the probe response reuses the fuzzed H1/H2 parser; a threshold-transition property test is unit-level; fuzzers stay 42). Every leg proven live via `-count=1` deliberate-breaks (drop-the-filter → traffic leaks to the dead host; break-the-poll-predicate → never converges).

**Masters (read first, in order):**

1. [`./PLAN.md`](./PLAN.md) — **the phase-39.1 IMPL plan** (16 bite-sized TDD tasks: `hostHealth` registry → `clusterHealth` view → HTTP probe → checker runtime → parse+rejects → thread `*clusterHealth` into the six constructs → health-aware pick [rr/random/least_request then ring_hash/maglev walk] → stat registrations → `StartHealthChecks`/`Drain` lifecycle + `main.go` → `0066` fixture → break/flake → full re-verify → ADR bodies → completion bundle).
1. [`./SPEC.md`](./SPEC.md) — the §1.1 AMEND-HC1..HC8 block; the §6 reject roster; the §7 stat surface +7; the §8 `0066` differential; the §11 live-pin block; the §13 ADR §Context drafts.
1. [`./BRAINSTORM.md`](./BRAINSTORM.md) — **the phase-39 charter.** The Q0/Q0b/Q1/Q2 decisions; the v1.32.4 `Cluster.health_checks` + `core.v3.HealthCheck` (the oneof + timing/threshold envelope) + `common_lb_config.healthy_panic_threshold` (default 50%) + `HealthStatus` enum verification (§2.1); the host health-state dimension + the cluster-level runtime + the health-aware pick architecture (§1.7 / §2.3 / §2.4); the 39.1/39.2 by-codec split (§1.4 / §2.2); the NEW `health_checks` reject surface (§2.6); the poll-to-convergence differential design (§2.5 / §6); the 2 anticipated ADRs (§7); the §10 D-HC1..D-HC8 pins the SPEC executes.
2. [`../../ENVOY_TARGET.md`](../../ENVOY_TARGET.md) — the current pin (`envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0f…`, ADR-0227).
3. `docs/envoy-go/DECISIONS.md` — **ADR-0232** (the LB acquire/release seam — the OPTION-C byte-stable-surface discipline the build-time health-view injection REUSES) + **ADR-0235/0239** (the hash-key / subset-match per-request pick-inputs — the ctx-carry STYLE the health view CONTRASTS) + **ADR-0236/0238** (ring_hash/maglev — the policies that gain walk-to-next-healthy) + **ADR-0230** (the redis upstream-pool runtime seam — the prior framework-runtime precedent) + **ADR-0024** (the per-cluster LB-state scope — UNAMENDED) + ADR-0106/0045/0044/0052/0080/0227. (ADR-0242 + ADR-0243 §Context are DRAFTS at the SPEC — the full entries land at the phase-39.1 IMPL per ADR-0044.)
4. `internal/cluster/{cluster.go (the Endpoint type:33 + the Pick/Dial/AcquireH1 funnels),manager.go (extractEndpoints:557 + the lb_policy switch + the policy factory + Drain():214),loadbalancer.go,ringhash.go,maglev.go,leastrequest.go,random.go,subset.go (the six constructs that gain the health-view consult)}` + `internal/cluster/dial_h2.go` + the H1 pool (the HTTP probe client) + `internal/admin/admin.go` (the `/stats` poll-to-converge endpoint) + `cmd/envoy-go/main.go` (the StartHealthChecks boot call) + `test/fixtures/0065-weighted-clusters/` (the cross-side HTTP differential precedent `0066` follows).

**Scope at phase 39.1** (per the BRAINSTORM + ROADMAP row 39):

- The host health-state dimension + the active-HC runtime + the HTTP probe codec (ADR-0242); the health-aware LB pick across all six constructs (ADR-0243); the `0066-health-check-http` fixture (fixtures 67 → 68; stat surface 1125 → 1132 [+7, SPEC-firmed]; DECISIONS tail → ADR-0243).
- ZERO new package, ZERO new go.mod module (39.1). A NEW per-host state dimension + a NEW cluster-level background runtime; the exported `Cluster` surface byte-stable. A NEW `health_checks` reject surface (§2.6).

**Counts (at the 39.1 IMPL):** fixtures 67 → 68 (`0066`); fuzzers 42 → 42 (deliberate — the probe response reuses the fuzzed H1/H2 parser); stat surface 1125 → **1132** (+7 SPEC-firmed — `health_check.{attempt,success,failure,network_failure}` counters + `health_check.healthy` gauge + `membership_healthy` gauge + `lb_healthy_panic` counter; emitted only on clusters with `health_checks`; the deferred-feature stats {passive_failure/verify_cluster/degraded/membership_change/degraded/excluded/update_no_rebuild} recorded departures — SPEC §7/AMEND-HC4); BackendKind tail 33 → 33 (deliberate); DECISIONS.md tail **ADR-0241 → ADR-0243** (ADR-0242 + ADR-0243; §Context drafts at the SPEC, bodies at the IMPL per ADR-0044; next-free ADR-0244). Counts UNCHANGED at the SPEC commit.

**Predecessor:** Phase 38 done (`load-balancer-subset` 38.1 + `weighted-clusters` 38.2; the Load-balancing family — stays OPEN with 3 health-gated candidates that phase 39 unblocks).

**Successor:** the phase-39 PLAN (`superpowers:writing-plans` — decompose SPEC §10 into ~14–20 bite-sized TDD tasks), then the 39.1 IMPL, then 39.2; thereafter the next Upstream-robustness-family row (4 candidates remain after 39) or a brainstormable health-gated LB candidate.
