# PROGRESS — phase 87 (h2-double-slash-path-routing)

## BRAINSTORM — done 2026-08-12

## What landed

Docs-only: **ZERO production `.go`, ZERO test `.go`.** `docs/envoy-go/phases/87-h2-double-slash-path-routing/BRAINSTORM.md` (new) · `ROADMAP.md` **row 87 registered `in-progress`** (236 -> 237 lines, 118 -> 119 data rows; the row sits at `:149`) · `next-prompt.txt` sentinel **`want` 118 -> 119 in the SAME commit as the row** (via the roll to the phase-87 SPEC) · `STATE.md` rolled **IN PLACE** · `STATE_HISTORY.md` appended (the unique-oldest §Recent entry evicted at the five-entry cap), verified STRICTLY APPEND-ONLY. `DECISIONS.md` and `BEHAVIOR_CONTRACT.md` **BYTE-UNTOUCHED**; this stage adds **NO ADR** (next-free stays ADR-0309; strict `PROPOSED` guard stays 0 — the phase-87 SPEC re-arms it).

## Method

**SELF-PICKED** per the 2026-07-12 standing directive; no banked mid-lifecycle work existed (phase 86 CLOSED, all 118 rows `done` — check (1) was SILENT at `want=118`, the third silent reading in project history, before this stage re-opened it at row 87). ⚠️ **NAMED DEPARTURE: no investigation agents this stage** — the probes (a tip binary built with `-o` into session scratch; four `curl --http2-prior-knowledge --path-as-is` executions over h2c; two `net/url` `go run` differentials; code reads and greps at `638ef78a`) were run INLINE by the controller; nothing in the repo tree was edited by any probe. Ports 47400-47402 template values only.

## The stage's headline

**envoy-go's hand-rolled H2 downstream codec mis-parses a leading `//` in the origin-form `:path`, and it fails two ways — reproduced by execution at this tip.** `internal/filter/hcm/h2/stream.go`'s `buildRequest` parses `:path` with `url.Parse` (a generic RFC-3986 URI parser), which reads a leading `//` as a network-path reference and peels the authority into `u.Host`, corrupting `u.Path`. Measured end-to-end over h2c (positive control `GET /` -> `routed-ok` 200): `GET //foo` -> **404** (empty path, routing miss); `GET //foo/bar` -> **silent mis-route to `/bar`** (leading segment swallowed as authority, served against the wrong path with no error); mid-path `/a//b` -> 200 (unaffected). ⚠️ **The defect is H2-specific**: the identical `//foo` over HTTP/1.1 returns `routed-ok` 200 (H1 is served by Go `net/http`), and H3 delegates request-target parsing to the `quic-go`/`http3` library — so the blast radius is exactly ONE production site (the `url.Parse(path)` call in `buildRequest`). The fix primitive is `url.ParseRequestURI` (measured at this tip: preserves `//foo`, keeps asterisk-form `*`, splits the query), to be settled at the SPEC by prototype.

## The pick

**Row 87 `h2-double-slash-path-routing`, a core-HCM / HTTP-routing MAINTENANCE row claiming NO family ordinal** (the row-85/row-86 precedent — a maintenance row repairs a landed deliverable and does not extend a charter). Provenance: the phase-74 BRAINSTORM sweep's *"Newly surfaced this session and NONE chartered:"* prose (historical, NOT a live `candidates:` sentence, deliberately untouched), carried in `STATE.md`/`next-prompt.txt`'s documentary-defects list — OUTSIDE the six family windows, so nothing narrows at row-done. Rejected with re-derived costs (BRAINSTORM §2.1): the CONTINUATION two-sided repair (strongest KNOWN product defect but 2-4x row 85, gate does not exist — the natural next LARGE row, charter as a split phase); the stat-surface recount (cheapest but changes nothing real; rides a future +0 row); `ssl.connection_error` (+444 whole-`.go` floor); `test/conformance/grpc/` (9/26 reachable); REVIEW.md restoration (process-not-product); the D-86-CONN `client.Close` gate (~10 test-only lines, too thin — a fold-in); hygiene fold-ins (thin, test/process only).

## Sentinel (measured, before AND after the ROADMAP edit)

BEFORE at `want=118` (repo tip): (1) SILENT · (2) SIX `:196 :202 :208 :218 :224 :232` · (3) SILENT. AFTER at `want=119` (worktree, row registered): input **237 lines / 119 data rows** · (1) **`NOT DONE: row 87`** (the single expected line while open) · (2) SIX, anchors shifted uniformly +1 to **`:197 :203 :209 :219 :225 :233`** (the single inserted line at `:149` sits above all six windows) · (3) SILENT. Conjunction fails ⇒ **does NOT fire**; `stop` NOT created (checked at the repo root AND the worktree, both sides). All four doctoring NCs fired both sides:
- **row-62 doctoring** (`NC LANDED? [ in-progress ]` inspected first): BEFORE -> `NOT DONE: row 62` alone (the silent side still LOOKS); AFTER -> `NOT DONE: row 62` AND `NOT DONE: row 87`.
- **want off-by-one**: BEFORE `want=117` -> `GATE FAIL: examined 118 data rows, expected 117`; AFTER `want=118` -> `GATE FAIL: examined 119 data rows, expected 118` (plus `NOT DONE: row 87`).
- **check-(3) doctoring** (residual 2 -> 0 confirmed first): `NEVER OPENED: gRPC` fired, `WASM` correctly silent.
- **check-(2) one-arm**: long arm alone **5**, short arm alone **1**, union **6** (a one-arm strip is not an NC for the union).

Leak axes (whole-file count, BEFORE -> AFTER): lines **236 -> 237** · data rows **118 -> 119** · union **6 -> 6** · `-family row` **95/67 -> 95/67** (the row's MAINTENANCE phrasing deliberately does not match check (3)'s `<slug>-family row` pattern — rows 76/78/85/86 precedent) · `gRPC-family row` **2 -> 2** · `Operational-tooling-family row` **3 -> 3** · new-slug occurrences **2**, both inside row 87's own line (the `| h2-double-slash-path-routing |` id cell and the phase-dir path). The ARM-A malformed-row figure {119, 131} is UNAFFECTED — those rows sit above the `:149` insertion point (not re-run with the fragile escape-aware command; the binding leak-invariance proof is the whole-file counts above).

## SPEC — done 2026-08-13

### What landed

Docs-only: **ZERO production `.go`, ZERO test `.go` in the landed tree.** `SPEC.md` (new, all seven §4 questions disposed BY EXECUTION) · `DECISIONS.md` **ADR-0309 §Context appended, STATUS `PROPOSED`** (strict guard **0 -> 1**; append-only, base a byte-exact prefix; `^---$` stays 216; tail moves to ADR-0309, next-free ADR-0310) · this file · `STATE.md` rolled IN PLACE · `STATE_HISTORY.md` appended · `next-prompt.txt` rolled to the phase-87 PLAN. `ROADMAP.md` and `BEHAVIOR_CONTRACT.md` **BYTE-UNTOUCHED** (row 87 stays `in-progress` at `:149`, `want` stays 119; the contract's `## HTTP/2` rule lands at the IMPL riding ADR-0309).

### Method

⚠️ **NAMED DEPARTURE CONTINUES (BRAINSTORM-87 / SPEC-86 precedent): no investigation agents — every probe INLINE by the controller.** The centerpiece: a **compiling, TEST-GREEN prototype in a DETACHED worktree** (`wt-87-proto` at `d7fd87fd`; net **+10 production `.go`**, one file; diff + 63-line unit probe captured to session scratch; worktree DELETED at close, tree-clean proven). Probes: 17-case `net/url` differential · tip + prototype binaries (`-o` into scratch) · `curl --path-as-is` batteries over h2c · a ~120-line raw HEADERS-frame client (x/net, scratch module — also the Q5 feasibility measurement) · the x/net-Transport literal-path probe through `test/helpers.H2CRoundTrip` · reference container `b87-main` (`contrib-v1.37.2`, torn down BY NAME, 0 remaining) · `TestDifferential/0004-h2-routing` PASS against the prototype tree · full `go test ./...` on the prototype tree. Ports 47410-47411 / 47420-47421, transient only.

### The stage's headline

**All seven questions closed by measurement; the fix is `url.ParseRequestURI` + a 3-line fragment reject, measured at exactly +10 net production lines; the differential cost feared by the BRAINSTORM EVAPORATED** — x/net's `http2.Transport` delivers a literal leading-`//` `:path` unmodified (measured through the repo's own helper: the tip 404s/mis-routes through it exactly as with `curl --path-as-is`), so the `0004-h2-routing` extension (one `//edge` route + one appended drive loop) needs NO raw-frame writer and NO subprocess. Reference EXECUTED: `//foo` → 200, `//foo/bar` → routed against the FULL path, `/foo#frag` → **400**, rootless/bare-query → **connection teardown** — so the BRAINSTORM §3 expectation is CONFIRMED and the two NEW `ParseRequestURI` rejects plus the explicit fragment guard all move envoy-go TOWARD the reference (D-87-FRAG, D-87-REJECT-SHAPE: direction parity, stream-level wire shape, unit-level only). Both failure arms proven RED at tip / GREEN at prototype with the routed path asserted by BODY (`//foo/bar` → `WRONG-bar` at tip). ⚠️ **Flake finding:** the 86-IMPL's uncaptured `internal/boot` failure RECURRED WITH IDENTITY — `TestSDSEndToEnd_FetchFailure_BootFailsClosed/silent_SDS_server…` (`boot_sds_e2e_test.go:551`, `DeadlineExceeded` vs the initial-fetch-timeout message under full-suite load); cleared scoped x5 + full package; SDS dial-budget class, now named.

### Sentinel (SPEC) — ONE side (a SPEC does not touch ROADMAP.md; byte-untouched verified by empty diff)

Recorded output at this close: **(1) `NOT DONE: row 87` at `want=119` — the single expected line, denominator silent · (2) SIX at `:197 :203 :209 :219 :225 :233` · (3) SILENT ⇒ the conjunction does NOT fire; `stop` NOT created** (verified absent at repo root AND stage worktree). All four NCs fired: row-62 doctoring => `NOT DONE: row 62` AND `NOT DONE: row 87` (`NC LANDED? [ in-progress ]` inspected first) · `want=118` => `GATE FAIL: examined 119 data rows, expected 118` · check-(3) doctoring (residual 2 -> 0 confirmed first) => `NEVER OPENED: gRPC`, WASM silent · check-(2) one-arm 5/1, union 6. Leak axes invariant: `-family row` 95/67 · `gRPC-family row` 2 · `Operational-tooling-family row` 3.

## PLAN — done 2026-08-13

### What landed

Docs-only: **ZERO production `.go`, ZERO test `.go`.** `PLAN.md` (new) · this file · `STATE.md` rolled IN PLACE · `STATE_HISTORY.md` appended (the 2026-08-10 two-way tie evicted by list position) · `next-prompt.txt` rolled to the phase-87 IMPL. `ROADMAP.md`, `DECISIONS.md` and `BEHAVIOR_CONTRACT.md` all **BYTE-UNTOUCHED** (row 87 stays `in-progress` at `:149`, `want` stays 119, the strict `PROPOSED` guard stays **ARMED at 1** — the IMPL disarms it).

### Method

⚠️ **NAMED DEPARTURE, THIRD CONSECUTIVE STAGE: no investigation agents — every probe INLINE by the controller.** Subagent-driven remains the standing preference and returns at the IMPL. Probes: a throwaway 13-row `buildRequest` table written into the worktree, run, and **DELETED**; a 17-case `net/url` `go run` in `/tmp`; the tip binary built with `-o` into scratch and booted on a scratch h2c config; a throwaway drive probe run through **the repo's own `helpers.H2CRoundTrip`** — the same x/net path the `0004` driver uses; and the SPEC's `stream.go` edit re-applied as a throwaway patch for the GREEN counter-proof. Ports **47430-47431** transient, released with `ss` proof. NO docker. **Teardown proven:** patch reverted and `sha256sum -c` OK against the pre-patch capture, both probe files deleted, `git status --porcelain` and `git diff --stat master` both EMPTY.

### The stage's headline

**Both RED anchors re-proven by execution at the PLAN tip with failure lines read — and the run found one claim NO prior stage had measured.** Unit layer: **8 of 13 rows RED** (`//foo` → `Path=""`, `//foo/bar` → `Path="/bar"`, `//` and `//foo?x=1` → `Path=""`, all four reject rows accepted silently), with **five accept rows GREEN at the tip** now labeled REGRESSION CONTROLS, not anchors. Differential layer, driven through the repo's own helper against the tip binary on the post-extension route table: `GET //edge` → **404 empty body**, `GET //edge/health` → **200 `OK\n`** (the silent mis-route). GREEN counter-proof with the throwaway patch: both arms → `200 edge-ok`, unit table 13/13, `internal/filter/hcm/...` green. ⚠️ **THE NEW MEASUREMENT: `prefix: "//edge"` really does match `//edge/health` through envoy-go's own router post-fix** — the SPEC inferred the D-87-DIFF recipe from parse behavior alone; it is now measured end-to-end. ⚠️ **AND A REFINEMENT THE IMPL NEEDS: the `//edge` pre-fix 404 is a route-MISS with an EMPTY body** (`Path=""` carries no `prefix: "/"`), not the catch-all's `not found\n` — so that arm's load-bearing assertion is **status**, while `//edge/health`'s is **body**. Neither arm alone catches both failure modes. ⚠️ **AND A CONTRACT SENTENCE THIS ROW REFUTES:** `BEHAVIOR_CONTRACT.md:2034` claims *"there's no stdlib net/http parsing to inject normalisations"* on the H2 path — false since 05.1; the IMPL's contract edit is a RIDER on it (ADR-0307 form), drafted in PLAN §5.1.

### Sentinel (PLAN) — ONE side (a PLAN does not touch ROADMAP.md; byte-untouched verified by EMPTY DIFF against master)

Recorded output at this close, input measured **237 lines / 119 data rows**: **(1) `NOT DONE: row 87` at `want=119` — the single expected line, denominator silent · (2) SIX at `:197 :203 :209 :219 :225 :233` · (3) SILENT ⇒ the conjunction does NOT fire; `stop` NOT created** (verified absent at repo root AND stage worktree). All four NCs fired: row-62 doctoring => `NOT DONE: row 62` AND `NOT DONE: row 87` (`NC LANDED? [ in-progress ]` inspected first) · `want=118` => `GATE FAIL: examined 119 data rows, expected 118` (plus the expected `NOT DONE: row 87`) · check-(3) doctoring (residual **2 -> 0** confirmed on the doctored copy first) => `NEVER OPENED: gRPC`, WASM correctly silent · check-(2) one-arm **5** long / **1** short, union **6**. Leak axes invariant: `-family row` **95 occurrences / 67 lines** · `gRPC-family row` **2** · `Operational-tooling-family row` **3**.

## IMPL — done 2026-08-13

### What landed

**PHASE 87 CLOSED. ROW 87 FLIPPED `done`** at `ROADMAP.md:149` — the ONLY `ROADMAP.md` edit (`numstat 1 1`, the status cell alone; the summary prose verified BYTE-IDENTICAL by sha256 over the status-blanked row, the row-86 precedent), `want` STAYS **119**, 237 lines / 119 data rows unchanged. Production **+13 net `.go`** in ONE file (`internal/filter/hcm/h2/stream.go`, `14 1`); test **+206 net `.go`** (`internal/filter/hcm/h2/stream_test.go` `169 0`; `test/fixtures/0004-h2-routing/driver/driver.go` `41 4`); non-`.go` **+40 net** across four `0004` fixture files. `DECISIONS.md` **18114 -> 18144** (ADR-0309 COMPLETED IN PLACE; strict `PROPOSED` guard **1 -> 0**). `BEHAVIOR_CONTRACT.md` **5957 -> 5958**. `STATE_HISTORY.md` **488 -> 490**.

### Method

⚠️ **SUBAGENT-DRIVEN RETURNS, as the PLAN said it would** — a NAMED RETURN from the three-stage inline departure of BRAINSTORM/SPEC/PLAN. Four task agents on disjoint file sets (T0 unit table · T1 `0004` extension · T4a ADR-0309 · T4b contract), each with private scratch, each committing LOCALLY with EXPLICIT PATHSPECS on the one stage branch (never `git add -A`, never an unscoped `git restore`); the controller did T2, the ROADMAP flip, the sentinel battery, the STATE roll, the gates and the squash. **The controller re-derived every load-bearing agent claim by execution rather than accepting the report** — which is how the ADR path-cite defect below was caught.

### The stage's headline

**The hand-rolled H2 downstream codec now parses the origin-form `:path` as a request-target.** `buildRequest` swaps `url.Parse` -> `url.ParseRequestURI` behind a three-line `strings.IndexByte(path, '#')` fragment guard. TDD held INSIDE the one commit: **both RED censuses were observed with failure lines read BEFORE the production edit** (T0 and T1 are both prerequisites of T2 — landing the ten-line fix first would have converted both censuses into unfalsifiable green-on-arrival claims). Unit layer: **8 of 13 rows RED at the tip, matching PLAN §1.1's expected set EXACTLY** — `//foo` -> `Path=""`, `//` -> `""`, `//foo/bar` -> `"/bar"` (the silent mis-route), `//foo?x=1` -> `""`, and all four reject rows silently ACCEPTED. Differential layer: `TestDifferential/0004-h2-routing` RED on the `//edge` **status** arm (`subj drive: //edge: status=404, want 200`), the named subtest confirmed PRINTED. Post-fix: unit **13/13**, `0004` GREEN against the pinned reference (digest byte-matching `ENVOY_TARGET.md`), `go test ./internal/filter/hcm/... -count=1` ok.

### Realized cost vs the PLAN §6 floor — THE FLOOR HELD

Total `.go` **+219 net** against PLAN §6's **~135-236** band — **INSIDE it**, with production `+13` inside the measured 10-16 band. ⚠️ **POST-REVIEW figure: 29 lines higher than the pre-review count**, because the final review added the `rootless_fragment` ordering pin, the `percent_escaped_slash` row and the `wantRawPath` field. The conclusion is unchanged; the number is not, and the pre-review figure was stale for the length of one commit. ⚠️ **This BREAKS ten consecutive `reference_measured_prototype_is_a_lower_bound` firings**, and ADR-0309 §Consequences (iii) records WHY rather than treating it as a trend: this row's entire production surface was ONE grep-confirmed call site measured twice by a compiling test-green prototype before the IMPL began, and the PLAN had already absorbed this row's two late-added test classes into its own band. **Nothing here licenses treating a multi-file prototype's measurement as an estimate.**

### Findings this stage adds

- ⚠️ **(1) THE DIFFERENTIAL'S TWO ARMS CANNOT BOTH BE OBSERVED RED IN A SINGLE PRE-FIX RUN — the SPEC/PLAN "fails BOTH arms" claim was only HALF-demonstrated by the natural run.** `drive()` returns on the first error, so the `//edge` status failure short-circuits and `//edge/health` is NEVER ISSUED (the pre-fix subject emitted 28 `hcm: h2: EOF` lines — 27 standing + arm 1 — and bailed). Arm 2's RED was proven SEPARATELY by a temporary arm-ORDER SWAP: `//edge/health: body="OK\n", want "edge-ok"` **arriving with status 200**, independently confirming that a status-only assertion there would have read GREEN. Swap reverted path-scoped, committed order re-verified. **Generalized in ADR-0309 §Consequences (vii): in a FAIL-FAST driver a multi-arm RED census needs ONE RUN PER ARM, or every arm after the first is an unproven claim riding on the first arm's failure — and it reads as proven, because the run did fail.**
- ⚠️ **(2) A WRONG PATH CITE WAS MINTED INSIDE THIS BRANCH AND CAUGHT PRE-SQUASH.** ADR-0309 §Consequences (iii) as first drafted cited `test/differential/fixtures/0004-h2-routing/driver/driver.go`; the real path is `test/fixtures/0004-h2-routing/driver/driver.go`. Found by mechanically resolving EVERY path cite in the new block against the filesystem, not by reading. Same class as the three citation defects the phase-86 IMPL caught in ADR-0308 — **the lineage has now minted one in each of two consecutive rows.**
- ⚠️ **(3) A PRE-EXISTING STALE ANCHOR, RECORDED NOT FIXED: `BEHAVIOR_CONTRACT.md`'s two existing riders cite the ADR-0052 mandate as ``ADR-0052 `:1821` ``, and line 1821 is retry-policy / hedging prose (phase 42.2 material).** The anchor has drifted. This row's rider therefore names **ADR-0052 with NO line anchor** rather than propagating a wrong cite; repairing the two pre-existing cites is OUT OF SCOPE and is now a named documentary defect.
- **(4) The `envoy.yaml` / `envoy-go.yaml` numstats are ASYMMETRIC (`6 1` vs `4 0`) and that is CORRECT** — only `envoy.yaml` carries a route-count claim in its header comment block (`3 routes` -> `4 routes`); `envoy-go.yaml` documents itself as mirroring `envoy.yaml` and owed no such edit. Verified by reading both headers, not assumed from the numbers.

### Gates (T5, run LAST against the FINAL tree)

**Full differential 121/121 `INNER_EXIT=0`**, zero FAIL / SKIP / `no driver registered`, ANCHORED panic gate SILENT, 382.3 s · **`go test ./...` on the FINAL tree rc=0**, zero FAIL, panic gate SILENT, **127 packages ok**, differential green in-suite (383.3 s) · `-race` on `internal/filter/hcm/...` clean · `gofmt -l` (gated on OUTPUT), `go vet`, `golangci-lint` (misspell locale US) clean on every touched package · **h2spec `95 tests, 94 passed, 1 skipped, 0 failed` from THIS row's own run**, skip invariantly `6.9.2/2`, and **§8.1.2.1 Pseudo-Header Fields 4/4 with the two new rejects LIVE** · counts each with its NC: fixtures **121 +0** · fuzzers **55/48 +0** · BackendKind tail **38 +0** · `go mod tidy -diff` EMPTY · **stat-surface DELTA 0 by the SAME command both sides** (`327`/`79` in base AND tip; base in a DISPOSABLE detached worktree, removed with proof — ⚠️ a DIFFERENT form from the lineage's carried `145/21`; no absolute corrected, none carried).

⚠️ **TWO FLAKES, BOTH DOCUMENTED CLASSES, BOTH WITH IDENTITY CAPTURED BEFORE ANY RE-RUN, NEITHER TOUCHING THIS ROW.** (1) **The differential's FIRST launch ABORTED at fixture 89 of 121** — `0087-tracing-otlp`, OTLP receiver `0.0.0.0:35323: bind: address already in use` — so **32 fixtures NEVER RAN while the harness wrapper exited 0**; only the captured `INNER_EXIT=1` told them apart. The whole-repo run reproduced the class on `0081-grpc-access-log` (ALS receiver, `0.0.0.0:35097`). **`0087` joins `0081` as a named member of the driver-owned receiver port race.** Cleared by a dedicated launch, 121/121. ⚠️ **An aborted log must never be quoted as coverage — it reads as 88 passes.** (2) **`TestSDSEndToEnd_FetchFailure_BootFailsClosed/silent_SDS_server…`** (`internal/boot/boot_sds_e2e_test.go:551`, gRPC `DeadlineExceeded` instead of the initial-fetch-timeout message under full-suite load) — **byte-for-byte the member the phase-87 SPEC named, a THIRD recurrence with a now-stable signature**; cleared scoped `-count=5` + full package, and both later whole-repo runs green. `internal/filter/hcm/h2` passed INSIDE the run that failed.

**Six-gate posture — departures NAMED, compliance not claimed:** grpc-conformance deferred in writing · proxy-wasm 10/16 · **no `REVIEW.md`** (standing lineage departure since phase 25.3; this row's adversarial review ran as a subagent and is recorded above, not as a file).

### Sentinel (IMPL) — BOTH SIDES of the row flip, run MECHANICALLY, output RECORDED not predicted

Input measured **237 lines / 119 data rows** on both sides. **PRE-flip:** (1) **`NOT DONE: row 87`** ALONE at `want=119` (denominator silent) · (2) SIX at `:197 :203 :209 :219 :225 :233` · (3) SILENT. **POST-flip:** (1) **SILENT — the FOURTH silent reading in project history** · (2) SIX at the SAME six anchors · (3) SILENT. **The condition is a CONJUNCTION ⇒ the sentinel does NOT fire, and `stop` was NOT created** (verified ABSENT at repo root AND stage worktree, both sides).

⚠️ **A SILENT CHECK (1) IS EXACTLY THE READING A BROKEN CHECK PRODUCES, so the row-62 doctoring NC is the load-bearing evidence of this whole stage — and it FIRED on both sides:** PRE-flip => `NOT DONE: row 62` AND `NOT DONE: row 87`; POST-flip => **`NOT DONE: row 62` ALONE — the silent side still LOOKS** (`NC LANDED? [ in-progress ]` inspected before trusting either result). The other three NCs fired both sides: `want=118` => `GATE FAIL: examined 119 data rows, expected 118` · check-(3) doctoring (residual **2 -> 0** confirmed on the doctored copy FIRST) => `NEVER OPENED: gRPC` with WASM correctly silent · check-(2) one-arm **5** long / **1** short, union **6** (a one-arm strip is NOT an NC for the union).

**Every leak axis INVARIANT across the flip:** `-family row` **95 occurrences / 67 LINES** (⚠️ `--` passed before the pattern; the two forms are both correct and neither is "the" count) · `gRPC-family row` **2** · `Operational-tooling-family row` **3**. **Nothing narrows in any family-window sentence** — row 87's provenance is the phase-74 BRAINSTORM sweep prose, OUTSIDE all six windows — confirmed by re-running the union AFTER the flip, not forecast.

### The final whole-branch review — SIX findings, all dispositioned, THREE of them real coverage gaps

A most-capable-model adversarial reviewer ran against the whole branch before the squash (the phase-86 precedent). It found nothing wrong with the production edit — a 50-case `Parse`-vs-`ParseRequestURI` differential it built itself confirmed the two primitives differ on EXACTLY the four repairs and the two new reject families and nowhere else — and its "reverse hidden-enforcement" hunt (does anything in the tree DEPEND on the absence of the two new rejects?) came back **empty after an enumerated search**: no test anywhere constructs a rootless, bare-query or fragment-bearing `:path`; the h2 fuzzers carry no path-bearing seeds; `buildRequest` has exactly ONE production caller; CONNECT/authority-form is rejected by the untouched `missing :path` check BEFORE the new code; `%23` is correctly NOT caught by the byte guard; and upstream forwarding reads the raw wire value, never `req.URL`. **What it did find, and what was done:**

- ⚠️ **(F3, FIXED — the most valuable finding) D-87-FRAG's central decision, that the guard sits BEFORE the parse, was pinned by ZERO of the thirteen rows.** The reviewer built both implementations and ran the landed table against each: **0/13 rows discriminated them.** Every reject row was either fragment-bearing-but-otherwise-valid (`/foo#frag`, `/foo?a=b#frag`) or invalid-without-a-fragment (`foo`, `?a=b`) — and the ordering only matters at the INTERSECTION, which no row occupied. **A `rootless_fragment` row (`foo#frag`) was added**, and the fix was negative-controlled: with the guard moved below the parse, **that row ALONE goes red** (`Msg = "bad :path", want "fragment in :path"` plus the `Underlying` nil-ness assertion), 1 FAIL of 15, and the revert was sha256-verified. **This is the `reference_liveness_break_needs_failing_baseline` family applied to a DECISION rather than to a fix: the ADR argued the ordering at length and the suite could not tell it from its opposite.**
- ⚠️ **(F2, FIXED) The new contract bullet claimed coverage the tests did not provide** — it said percent-escape `Path`/`RawPath` semantics were "asserted … per-form by the `buildRequest` path-forms table", and the table had **no percent-escape row and asserted `RawPath` nowhere**. The `verification_table_launders_wrong_cites` shape: a reader sees "asserted" and stops. **Fixed on BOTH sides** — a `wantRawPath` field and a `/foo%2Fbar` row (`Path` `/foo/bar`, `RawPath` `/foo%2Fbar`) were added, AND the bullet was rewritten to state the assertion SPLIT precisely: the leading-`//` forms alone are asserted CROSS-SIDE; everything else is UNIT-level only.
- ⚠️ **(F7, FIXED) Two overclaims in this stage's own ADR text, both falsifiable as written.** §Decision (a)'s *"byte-identical … on every form the codec accepts today"* is contradicted by the row's OWN anchors (`url.Parse` *accepts* all seven of them and gets `Path` wrong on each) — now reads **"accepts AND ROUTES CORRECTLY"**, with the qualifier's load-bearing role stated. And §Consequences (i)'s *"a `:path` that is not a valid request-target is now REJECTED"* overclaims: **opaque / scheme-form targets (`example.com:80`, `a:b/c`) are STILL ABSORBED** — verified by execution, byte-identical on both primitives with an EMPTY `Path`, so they still produce the same silent 404 — now scoped to the three named families in both the ADR and the contract.
- **(F4 + F5, RECORDED as §Consequences (ix), deliberately NOT repaired):** this row makes `ADR-0057`'s *"27 sequential H/2 round-trips per side"* stale (now 29), and its contract edit is **NOT tail-only**, so every by-line citation into `BEHAVIOR_CONTRACT.md` at or below `:2036` shifts **+1** across `ROADMAP.md`, `DECISIONS.md`, `STATE_HISTORY.md` and ~12 historical phase docs. ⚠️ **The ADR blockquote's "zero existing by-line citations into this file shift" is about `DECISIONS.md` and does NOT extend to the contract edit** — said so explicitly. Two affected anchors were measured ALREADY STALE at base, so this row worsens pre-existing drift rather than originating it.
- **(F6, FIXED)** the new test's doc comment cited `stream.go ~:465`, the PRE-fix parse line (now `:478`) — replaced with a by-SYMBOL cite, since the row's own ADR warns that locator has drifted twice.
- **(F1, the close itself)** correctly flagged the then-unfilled gates placeholder, the missing STATE roll and an untracked `PROGRESS.md.new` — all close-in-progress artifacts, completed after the review.

## NEXT

**The post-87 SELF-PICKED BRAINSTORM.** Phase 87 is CLOSED and every chartered ROADMAP row is `done`, so the 2026-07-12 standing directive applies: the roller self-picks the next subject, smallest defensible candidate first, recording the pick and the rejected alternatives with costs RE-DERIVED at that tip. ⚠️ **The sentinel STILL does not fire** — check (1) is now SILENT and check (3) is SILENT, but check (2) prints SIX and the condition is a CONJUNCTION. **The strongest named candidate is the CONTINUATION two-sided repair** (server-side discard in `conn.go`'s `dispatchFrame` `*http2.ContinuationFrame` arm, plus client-side blindness and the client SETTINGS gap in `h2/client.go`, all named in ADR-0307; h2spec measured NOT to gate either side; estimated 2-4x row 85 — charter as a SPLIT phase).
