# Phase 33 — `network-filter-thrift-proxy`

**Status:** SPEC authored (lifecycle-state 1 → 2 at this commit; the D-T1..D-T9 empirical pins EXECUTED live against the contrib reference Envoy — a hand-built canned-REPLY Thrift backend on a docker bridge network, the framed×binary round-trip CONFIRMED ran + a routing-miss local-exception confirmed — + go-control-plane `/envoy` v1.32.4 bindings + upstream Envoy v1.37.2 CORE source; AMEND-T1..T7 recorded; the ADR-0231 §Context anchored; ready for the phase-33 PLAN per `superpowers:writing-plans`). PLAN / PROGRESS artifacts still pending; land when this phase enters subsequent lifecycle states.

**What this phase is.** Phase 33 is the **SEVENTH §9 Network-filters-family row** (a flat top-level row per ADR-0106; ROADMAP row 33, depends-on 32). It lands `envoy.filters.network.thrift_proxy` (proto `envoy.extensions.filters.network.thrift_proxy.v3.ThriftProxy`, in the EXISTING CORE `/envoy v1.32.4` module — where `redis_proxy/v3` lives, NOT `/contrib`; the BRAINSTORM dep-framing correction, confirmed at SPEC D-T1) — the project's **SECOND terminal routing proxy** (after redis_proxy at phase 32) in a NEW `internal/filter/network/thriftproxy/` package (the 11th network-filter built-in). Like redis_proxy it IS the connection terminator (no `tcp_proxy` behind it → no observational MVP): it decodes a Thrift message, routes by method name to an upstream cluster, round-trips the request through a pooled upstream connection, and writes the reply back downstream. It is framework-**ZERO-touch** — the **FIRST row to REUSE a previously-built framework seam unchanged** (the ADR-0230 upstream connection-pool / cluster-routing seam, `internal/filter/network/upstreampool.go`), validating its redis-scoped YAGNI sizing. It adds **ZERO new go.mod dep** (thrift_proxy is a CORE `/envoy` extension). **Thrift is the LAST §9 Network-filters candidate — after phase 33 phase-done the §9 Network-filters family CLOSES.**

**The envelope (BRAINSTORM Q1).** A SINGLE-PAIR SINGLE-ROUTE-TERMINAL MVP: the Thrift message-begin codec for ONE transport×protocol pair (**framed × binary**, pinned live — D-T2) under `payload_passthrough` (route on the message-begin envelope — method name + message type + `seq_id` — and forward the struct body opaquely) + `route_config` consumed as a single exact-match/match-all method route to ONE upstream cluster + the REUSED upstream round-trip (one-conn-per-downstream, synchronous single-flight, positional correlation; `seq_id` parse-validated + passed-through, NOT demux-load-bearing) + a router-only nested thrift-filter sub-chain (the implicit `envoy.filters.thrift.router`) + a LOCAL `UnknownMethod` Thrift exception on a routing miss (zero upstream — the redis-PING analogue) + the EAGER global `thrift.<stat_prefix>.` request/response stat roster. DEFERRED (parse-accept-vs-reject per field, pinned at SPEC D-T4): the rest of the 3×3 transport×protocol matrix, full `route_config` richness, the non-router thrift_filters, full struct-body parsing, seq_id-keyed multiplexing, `max_requests_per_connection`, `access_log`; the close-direction counters created-but-never-incremented (a coverage boundary); the `request_time_ms` histogram deferred (ADR-0060).

**The seven empirical amendments (this SPEC session, D-T1..D-T9 executed live).** The §11 scrape against the live contrib reference image + go-control-plane `/envoy` v1.32.4 + upstream v1.37.2 CORE source CONFIRMED the SPEC-blocking pins and REFINED/REFUTED several BRAINSTORM hypotheses (SPEC §1.1):

- **AMEND-T1** — TypeURL `…thrift_proxy.v3.ThriftProxy` (carries `extensions.`); thrift_proxy/v3 is CORE `/envoy v1.32.4` (where redis_proxy/v3 lives); **ZERO new go.mod dep** (the redis D32-1 posture; CONFIRMS the BRAINSTORM correction).
- **AMEND-T2** — the framed×binary pair + the round-trip ran LIVE (the working YAML reusable verbatim); framed = 4-byte BE frame-len, binary strict message-begin = `0x8001 00 <msgtype>` + i32 name-len + name + i32 seq_id; payload_passthrough reads message-begin only; AUTO auto-detects framed-binary identically.
- **AMEND-T3** — the roster is EAGER + table-bounded: **24 counters + 1 gauge (`request_active`) + 1 histogram (`request_time_ms`)**; REFUTES the BRAINSTORM (NO `response_business_exception`; +`request_passthrough`/`response_passthrough` + the 5 router counters). The Prometheus arm is **SINGLE-label-HOIST** `envoy_thrift_<leaf>{envoy_thrift_prefix="<sp>"}` (the redis-32 / mongo shape, NOT kafka inline). Stat surface **1091 → 1116 (+25)**.
- **AMEND-T4** — a routing MISS is answered LOCALLY with a Thrift `UnknownMethod` EXCEPTION, ZERO upstream (the redis-PING analogue); a decode error → a local `ProtocolError` exception; a route HIT proxies. SPEC-BLOCKING (it determines the fixture traffic + stat counts).
- **AMEND-T5** — the reference REMAPS the upstream `seq_id` to 0 and RESTORES the original downstream; correlation is POSITIONAL (one in-flight RPC per upstream conn), NOT seq_id-keyed; the MVP passes `seq_id` through → downstream byte-equivalence holds (the upstream seq_id is a per-side coverage boundary).
- **AMEND-T6** — the close-direction counters `cx_destroy_local/remote_with_active_rq` DO fire on the LIVE routing-miss path (`cx_destroy_local_with_active_rq: 1`) — SHARPER than redis-32; the MVP creates-but-never-increments them + keeps the conn open on a local reply (the framework-gap coverage boundary, D-T8); the `0057` miss arm asserts them PER-SIDE (subject==0).
- **AMEND-T7** — only `stat_prefix` is a hard PGV gate; `route_config` is NOT required; the un-chosen transport/protocol pairs are envoy-go-strict DEPARTURE rejects (the reference parse-accepts them — so unit-tested, NOT a cross-side boot-reject).

**Split (BRAINSTORM Q-split, D-T9a confirmed).** SINGLE unsplit §9 flat row (~790–1275 production LoC / ~16–20 tasks — the seam is REUSED, so the seam-first driver of redis's 2-way split is gone; the kafka-31 single-flat-row precedent). UNDER the ADR-0045 gate. The pre-authorized 33.1-codec / 33.2-stats split axis STAYS UNCONSUMED (the kafka-31 / mongo-29.1 precedent); the PLAN re-checks the gate.

**Masters (read first, in order):**

1. [`./BRAINSTORM.md`](./BRAINSTORM.md) — **the phase-33 charter.** The Q0/Q1/Q-seam/Q-split decisions (§2); the framework-survey (§3 — zero seam touch, the ADR-0230 seam reused); the stat-surface hypothesis (§5); the fixture envelope (§6 — `0057`/`0058` + the `TCPThriftResponder`); the anticipated ADR-0231 (§7); the deferred items (§8); the §10 D-T1..D-T9 empirical pins this SPEC executes.
2. [`./SPEC.md`](./SPEC.md) — **the authoritative phase SPEC.** The empirical-finding-driven scope (§1.1, AMEND-T1..T7); the single-row split disposition (§3.0); the package + framework reuse (§3 — the REUSED seam); the proto roster + PGV arms (§5/§6); the stat surface (§7 — the 25-name eager roster + the `thrift.` SINGLE-label-hoist prom arm); the differential taxonomy (§8 — `0057`/`0058` + the `TCPThriftResponder`); the ~16–20-task PLAN spine (§10); the D-T1..D-T9 empirical-pin block (§11); the framed×binary wire format (Appendix A).
3. [`../../ENVOY_TARGET.md`](../../ENVOY_TARGET.md) — the current pin (`envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0f…`, ADR-0227) — the reference image the cross-side differential boots.
4. `docs/envoy-go/DECISIONS.md` — ADR-0231 §Context (this phase's ADR; §Decision/§Consequences body at the 33 IMPL per ADR-0044) + **ADR-0230** (the upstream-pool seam — REUSED unchanged) + ADR-0229 (the redis_proxy as-built — the terminal-routing-proxy precedent) + ADR-0228 (kafka_broker — the single-flat-row precedent) + ADR-0227 (the contrib pin) + ADR-0106 (§9 flat-row discipline) + ADR-0045 (split-gate) + ADR-0060 (histograms deferred) + ADR-0044 (ADR §-body timing).
5. `internal/filter/network/upstreampool.go` (the seam thrift REUSES unchanged) + `internal/filter/network/redisproxy/` (the terminal-proxy package shape thrift mirrors) + `internal/stats/name.go` (the `redis.` SINGLE-label-hoist arm the new `thrift.` arm mirrors) + `test/differential/fixture/fixture.go` (the `StatsAsserter` + the BackendKind enum, tail 32).

**Scope at phase 33** (per the SPEC + ROADMAP row 33):

- (a) The **`internal/filter/network/thriftproxy/` package** — TypeURL via `proto.MessageName`; the config parse (`stat_prefix` required; `transport`/`protocol` accept `{AUTO,FRAMED}×{AUTO,BINARY}` else a departure reject; `route_config` → the method-routing table; `payload_passthrough`; the rest parse-accepted/deferred); the Thrift codec (framed frame + binary message-begin + opaque struct-skip + reply classifier + the `AppException` encoder); the `TerminalFilter.Handle` request→reply pump (HIT round-trip via the REUSED seam / MISS local exception); the EAGER 25-name roster.
- (b) **Registration** as the 11th built-in + the `thrift_proxy/v3` blank-import in `bootstrap.go` (CORE `/envoy` — ZERO new go.mod dep).
- (c) The **`thrift.` SINGLE-label-hoist Prometheus arm** in `internal/stats/name.go`.
- (d) **Differential fixtures** `0057-thrift-roundtrip` (cross-side; downstream-Thrift-response byte-equivalence + `StatsAsserter`; route-HIT + route-MISS arms) + `0058-thrift-boot-reject` (missing `stat_prefix`); a NEW BackendKind (the synthesized `TCPThriftResponder`, anticipated value 33); the 42nd fuzzer `FuzzThriftDecode`.
- (e) **ADR-0231** + the BEHAVIOR_CONTRACT 33 bundle (stat surface 1091 → 1116) + STATE/ROADMAP advance + the row-33 `in-progress → done` flip at the IMPL six-gate (a flat §9 row — NO parent rollup; the §9 family CLOSES).

**Counts (advance at IMPL):** fixtures 58 → 60; fuzzers 41 → 42; stat surface 1091 → 1116 (+25); BackendKind tail 32 → 33; DECISIONS.md tail ADR-0230 → ADR-0231 (next-free ADR-0232). ZERO framework-seam extension (the ADR-0230 seam REUSED unchanged — the FIRST reuse).

**Predecessor:** Phase 32 CLOSED (the `redis_proxy` §9 parent row; ADR-0229 + ADR-0230; stat surface 1091; fixtures 58; the SIXTH §9 row).

**Successor:** NONE in the §9 Network-filters family — thrift is the LAST candidate. After phase 33 phase-done the §9 candidate count drops 1 → 0 and the **§9 Network-filters family CLOSES**.
