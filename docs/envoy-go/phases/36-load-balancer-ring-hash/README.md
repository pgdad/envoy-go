# Phase 36 — `load-balancer-ring-hash`

**Status:** PLAN DONE (36.1 leg; lifecycle-state 2 → 3 at this commit; `docs/envoy-go/phases/36-load-balancer-ring-hash/PLAN.md` — the SPEC §10 36.1 spine decomposed into 10 bite-sized TDD tasks; D-S36-1..7 resolved [the affinity proof is the aggregate-count modular invariant: every subject per-backend count ≡ 0 mod 16, so no new BackendKind / harness primitive]; ADR-0045 re-check → NO FURTHER SPLIT for 36.1 [~365 prod LoC / 10 tasks]; plan-document-reviewer GREEN). The SPEC settles: `Cluster.LbPolicy RING_HASH` (`envoy.config.cluster.v3`, enum value 2) — the project's **fourth LB policy** and its **first consistent-hashing policy** — as a Ketama ring (`ringHashLB`) that **EXTENDS the ADR-0232 seam's PICK-INPUT half** (the widening ADR-0232 §Consequences named) via a ctx-carried `uint64` hash key, with the EXPORTED `Cluster` surface BYTE-STABLE (the OPTION-C discipline preserved; the ONE new exported symbol is the additive `cluster.WithHashKey`). TWO ADRs: **ADR-0235** (the seam extension) + **ADR-0236** (the policy). The FINAL ADR-0045 decision: **CONSUME the pre-authorized 36.1/36.2 by-plane split** (36.1 = seam + `ringHashLB` + manager + tcp_proxy `source_ip` plane; 36.2 = HTTP route `hash_policy` plane). **Next → the 36.1 IMPL** (`superpowers:subagent-driven-development` — execute the 10-task PLAN); 36.2 follows with its own SPEC → PLAN → IMPL. The Load-balancing family stays OPEN — 5 candidates remain after 36.

**What this phase is.** Phase 36 is the **THIRD Load-balancing-family row** — the FIRST to EXTEND the ADR-0232 seam (phase 34 built it; phase 35 reused it unchanged). RING_HASH is the policy ADR-0232 §Consequences names: *"the hash-keyed RING_HASH/MAGLEV will additionally require widening `Pick` to receive a request-derived hash key … the next LB phase that lands a hash policy must plan for it, NOT expect the zero-churn drop-in."* The seam widens `loadBalancer.Pick() → Pick(hashKey uint64, hasHash bool)`; the three incumbent policies (roundRobin/leastRequest/randomLB) ignore the two new params (behavior-neutral — all 62 fixtures byte-identical); `ringHashLB` consumes them (no hash → a random ring position). ZERO new go.mod dep (the enum + `RingHashLbConfig` + `type.v3.HashPolicy` + `RouteAction.HashPolicy` are all in the pinned `/envoy v1.32.4`; xxHash64 is hand-rolled, ~45 LoC — not a dep). 5 family candidates remain after 36 {maglev, subset LB, locality-weighted LB, priority load balancing, panic thresholds} — maglev the cheap follow-on reusing this seam.

**The envelope (BRAINSTORM Q1, BOTH PLANES; the split CONSUMED).** `RING_HASH` accepted by `cluster.Manager` (a `case Cluster_RING_HASH` parsing `Cluster.RingHashLbConfig` — min/max ring size [defaults 1024/8388608 self-supplied] + `hash_function` [XX_HASH/MURMUR_HASH_2] — through a HAND-ROLLED gate mirroring the reference's TWO validation layers [the PGV `value must be less than or equal to 8388608` + the runtime `ring hash: minimum_ring_size (N) > maximum_ring_size (M)`]; the lb-policy reject TEXT extends `…, RANDOM` → `…, RANDOM, RING_HASH`) + a `ringHashLB` (a Ketama ring built once — per entry `xxHash64("addr:port_i")`, `ceil(min/N)` entries/host equal-weight; `Pick(hashKey)` binary-searches the first ring point `>= key`, wrap; the upstream v1.37.2 mirror). The hash key rides `ctx`: tcp_proxy computes `xxHash64(bare client IP)` on new-conn (36.1); the HTTP router computes the route `hash_policy` key per-request (header `xxHash64(value)` + connection-properties source_ip, combined `rotl64(prev,1)^new`, terminal short-circuit; cookie/query/filter_state DEPARTURE-rejected; 36.2). DEFERRED: MAGLEV, the `load_balancing_policy` extension point, the deferred HTTP key sources, weighted-ring richness, cross-side host IDENTITY (infeasible in the harness — per-side affinity instead).

**The eight empirical pins (this SPEC session, D-RH1..D-RH8 executed live — SPEC §1.1 / §11):** **AMEND-RH1** the v1.32.4 surface re-pinned (enum 2; `RingHashLbConfig` min/max/hash_function; **PGV has NO min≤max rule** — runtime-only; defaults self-supplied; ZERO new dep); **AMEND-RH2** the Ketama algorithm pinned from source (xxHash64 seed 0 over `"addr:port_i"`; first-`>=` wrap lookup; byte-exact reproducible in isolation); **AMEND-RH3** the per-plane compute (tcp/HTTP source_ip = `xxHash64(bare IP)`; HTTP header = `xxHash64(value)`); **AMEND-RH4** REFUTED zero-delta — RING_HASH emits **3 new `ring_hash_lb.*` gauges** (surface 1116 → **1119**; MIRRORED; cross-side-exact — keyed on ring-config + host count only); **AMEND-RH5** the 7-variant validate matrix (min>max runtime reject + over-cap PGV reject; MAGLEV accepts; mismatched-oneof silent) + the 3-site blast radius (RING_HASH → MAGLEV retarget) + NO boot-reject dir; **AMEND-RH6** the HTTP subset (header + conn-props source_ip; cookie/query/filter_state defer) + the `rotl64(prev,1)^new` combine; **AMEND-RH7** the seam surface (`Pick(uint64,bool)`; ctx-carry via `WithHashKey`) + ~490 prod LoC/~16–20 tasks → CONSUME the split + xxHash64 hand-rolled; **AMEND-RH8** REFUTED cross-side identity — Docker source-IP NAT + per-side ring layouts force PER-SIDE affinity+spread (the header plane is the true cross-side proof).

**The differential proof (per-side affinity + spread; the 0059/0060 precedent).** TWO fixtures: `0061-lb-ring-hash` (36.1; tcp source_ip) — a SUBJECT-SIDE multi-`127.0.0.x` affinity(deterministic, cardinality-1)+spread(`>=2`) proof (the bind is subject-only; Docker NATs the reference to one source IP) + cross-side byte-equivalence + a cross-side `StatsAsserter` (the 3 `ring_hash_lb.*` gauges cross-equal); `0062-lb-ring-hash-http` (36.2; HTTP header) — a NAT-transparent TRUE cross-side affinity+spread proof on BOTH sides. The affinity leg is DETERMINISTIC/EXACT (same key → same backend, not a σ-band); the spread count is a coarse `>= 2` threshold. **NO new BackendKind** (`TCPEcho` + existing HTTP backends reused; tail stays 33), **NO new fuzzer** (no wire decode; a ring-lookup property test is unit-level; fuzzers stay 42). Every leg proven live via `-count=1` deliberate-breaks (scatter-the-key / collapse-the-ring / drop-Inc / corrupt-gauge).

**Masters (read first, in order):**

1. [`./BRAINSTORM.md`](./BRAINSTORM.md) — **the phase-36 charter.** The Q0/Q1/Q-seam/Q-split decisions; the v1.32.4 enum-2 + `RingHashLbConfig` verification (§2.1); the seam-extension rationale (§1.7 / §3.1); the affinity+spread differential design (§2.5/§6); the two anticipated ADRs (§7); the §10 D-RH1..D-RH8 pins this SPEC executed.
2. [`./SPEC.md`](./SPEC.md) — **the authoritative phase SPEC.** The empirical-finding-driven scope (§1.1, AMEND-RH1..RH8); the CONSUMED-split disposition (§3.0); the policy + seam-extension design (§3); the proto roster (§5); the two-layer reject gate (§6); the +3-gauge stat surface (§7); the `0061`/`0062` affinity+spread design (§8); the 36.1/36.2 PLAN spine (§10); the D-RH1..D-RH8 empirical-pin block (§11); the ADR-0235 + ADR-0236 §Context DRAFTS (§13).
3. [`../../ENVOY_TARGET.md`](../../ENVOY_TARGET.md) — the current pin (`envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0f…`, ADR-0227).
4. `docs/envoy-go/DECISIONS.md` — **ADR-0232** (the LB acquire/release seam — EXTENDED here; read the §Consequences PICK-INPUT-half anticipation phase 36 consummates) + ADR-0233 (least_request) + ADR-0234 (random — the RNG/seam-reuse posture) + ADR-0024 (the per-cluster LB-state scope — UNAMENDED) + ADR-0106/0045/0044/0052/0080/0060/0219. (ADR-0235 + ADR-0236 §Context are DRAFTS in SPEC §13 — the full entries land at the 36.1 IMPL per ADR-0044.)
5. `internal/cluster/{loadbalancer,leastrequest,random,manager,cluster}.go` (the seam `Pick` + `noopRelease` + the lb_policy switch + the reject text + `parseLeastRequestLbConfig` [the gate precedent] + `newPCGRNG` + the `Dial`/`AcquireH1`/`PickEndpoint` Pick funnel the SPEC widens) + `internal/cluster/manager_test.go:320` (the `TestManager_Error_UnsupportedLBPolicy` retarget) + the two consumers `internal/filter/tcpproxy/filter.go` (the `Dial(ctx)` site) + `internal/filter/http/router/router.go` (the `AcquireH1`/`Dial` sites) + `test/differential/fixture/fixture.go` (the `DistributionAsserter`) + `test/fixtures/0059-lb-least-request/` + `0060-lb-random/` (the per-side band precedent).

**Scope at phase 36** (per the SPEC + ROADMAP row 36; the split CONSUMED):

- **36.1** — (a) the **ADR-0235 seam extension** (`Pick(hashKey, hasHash)`; the `hashKeyCtxKey`/`WithHashKey`/`hashKeyFrom` ctx-carry; the 3 incumbent policies' ignored params; the byte-stable exported surface); (b) the **ADR-0236 policy** (`ringHashLB` + the pure-Go `xxHash64` + the Ketama ring + the no-hash fallback); manager acceptance (the `case Cluster_RING_HASH` + `parseRingHashLbConfig` two-layer gate + the reject text + the `manager_test.go` retarget + the 3 mirrored `ring_hash_lb.*` gauges); the **tcp_proxy `source_ip` plane**; the `0061-lb-ring-hash` fixture (fixtures 62 → 63; surface 1116 → 1119; DECISIONS tail → ADR-0236).
- **36.2** — the **HTTP route `hash_policy` plane** (header + connection-properties source_ip + the combine + terminal); the `0062-lb-ring-hash-http` fixture (fixtures 63 → 64); its own SPEC/PLAN/IMPL.
- ONE seam EXTENSION (the PICK-INPUT half), ZERO new package, ZERO new go.mod dep.

**Counts (at IMPL):** fixtures 62 → 63 (36.1) → 64 (36.2); fuzzers 42 → 42 (deliberate); stat surface 1116 → **1119** (36.1 — +3 `ring_hash_lb.*` gauges, the FIRST non-zero LB-stat delta; AMEND-RH4); BackendKind tail 33 → 33 (deliberate); DECISIONS.md tail **ADR-0234 STAYS at THIS SPEC commit** (the ADR-0235 + ADR-0236 §Context are DRAFTS in SPEC §13; the full entries + bodies land at the 36.1 IMPL per ADR-0044; next-free ADR-0235).

**Predecessor:** Phase 35 done (`random`; the ADR-0232 seam's FIRST reuse).

**Successor:** the 36.1 PLAN, then the 36.1 IMPL, then 36.2 (SPEC→PLAN→IMPL); thereafter the next Load-balancing-family row (maglev the cheap seam-reusing follow-on; 5 candidates remain after 36).
