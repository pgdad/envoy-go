# PROGRESS — phase 88 (h2-continuation-frames)

## BRAINSTORM — done 2026-08-13

## What landed

Docs-only: **ZERO production `.go`, ZERO test `.go`.** `docs/envoy-go/phases/88-h2-continuation-frames/BRAINSTORM.md` (new) · this `PROGRESS.md` (new) · `ROADMAP.md` **row 88 registered `in-progress`** (237 -> 238 lines, 119 -> 120 data rows; the row sits at `:150`; `git diff --numstat` = **`1 0`**) · `next-prompt.txt` sentinel **`want` 119 -> 120 in the SAME commit as the row** (via the roll to the phase-88 SPEC) · `STATE.md` rolled **IN PLACE** (64 lines, seven anchored singletons verified at 1 each, §Recent at exactly FIVE prior entries) · `STATE_HISTORY.md` appended 490 -> 492 (the unique-oldest §Recent entry evicted at the five-entry cap), verified **STRICTLY APPEND-ONLY** (`numstat 2 0`, base a byte-exact prefix via `cmp`). `DECISIONS.md` and `BEHAVIOR_CONTRACT.md` **BYTE-UNTOUCHED**; this stage adds **NO ADR** (next-free stays ADR-0310; strict `PROPOSED` guard stays 0 — the phase-88 SPEC re-arms it).

## Method

**SELF-PICKED** per the 2026-07-12 standing directive; no banked mid-lifecycle work existed (phase 87 CLOSED, all 119 rows `done` — check (1) was SILENT at `want=119`, the **fifth** silent reading in project history, before this stage re-opened it at row 88). ⚠️ **NAMED DEPARTURE, FOURTH CONSECUTIVE STAGE: no investigation agents** — every probe was run INLINE by the controller. Probes at `244e482f`: the tip binary built with `-o` into session scratch; **`probe88`**, a raw HEADERS/CONTINUATION h2c client (x/net `Framer` + `hpack`, with an h2c echo-backend serve mode) **written into the worktree, built, and DELETED at close**; a stock `x/net/http2.Transport`; ONE reference container (`b88-main`, `contrib-v1.37.2`) torn down **BY NAME** with 0 remaining. Ports 47450/47451 (subject), 47455 (echo backend), 47470 (reference) — transient only. ⚠️ **FOUR pre-existing containers belonging to other work were present and DELIBERATELY UNTOUCHED.** Tree proven clean at close: `git status --porcelain` and `git diff --stat master` show only this stage's intended edits.

## The stage's headline

**envoy-go discards HTTP/2 CONTINUATION frames on BOTH the downstream and the upstream path, and the defect is reachable by ORDINARY CLIENTS.** `internal/filter/hcm/h2/conn.go`'s `dispatchFrame` (cite BY SYMBOL) returns `nil` for `*http2.ContinuationFrame` behind a comment whose two clauses are **both false** — `ReadMetaHeaders` is assigned only in `test/differential/runner_test.go`, never in `internal/`, so the arm IS reached in normal usage — while `onHeaders` decodes with `HeadersEnded()` but **never branches on it**, dispatching the PARTIAL field set and leaving the HPACK dynamic table permanently diverged from the peer's. `client.go` carries the same defect with **no `ContinuationFrame` case at all** (`:417`/`:428`).

Reproduced by execution with a **green positive control on both sides** (single-frame `GET /marked` -> 200 both): `:path` in the CONTINUATION -> subject **`RST_STREAM PROTOCOL_ERROR`** vs reference **200**; a header in the CONTINUATION -> subject **200 with the header SILENTLY GONE** (its own un-split control echoes it) vs reference **200 `SAW-X-PROBE`**; one split request then an ordinary un-split one -> subject **`GOAWAY COMPRESSION_ERROR`** vs reference **200** — **the blast radius of one split request is the CONNECTION, not the request**; upstream-side, a padded RESPONSE header block -> **response headers silently dropped through envoy-go** (`len(x-resp-pad)=0` at 20000/40000 B) while a direct-to-backend **bypass control** preserves them.

⚠️ **THE FACT THAT CHANGED THE PRIORITY, NEW AT THIS STAGE:** a stock `x/net/http2.Transport` splits a header block past the default 16384-byte `SETTINGS_MAX_FRAME_SIZE`, and at 20000 B the subject answered **200 while dropping `accept-encoding`, `user-agent` AND the padding header**. A large cookie or JWT suffices. Every prior stage treated CONTINUATION as an exotic wire shape.

⚠️ **A COST REDUCTION FOUND BY EXECUTION:** the row does **not** owe an RFC 9113 §6.10 ordering state machine. An interleaved PING yields `GOAWAY PROTOCOL_ERROR` on both sides, **but the subject's green is not envoy-go's doing** — the log reads `framer: connection-error code=1`, i.e. `translateFramerErr` mapping an error from **x/net's own `Framer.checkFrameOrder`**. Attributing it to envoy-go would have repeated the `reference_code_comment_not_evidence` error the false comment already committed once. What is owed is narrower: **accumulate fragments until `END_HEADERS`, decode once, per direction.**

⚠️ **ONE QUESTION LEFT OPEN AND DELIBERATELY NOT CLAIMED:** whether the CLIENT leg also desyncs its HPACK decoder the way the server leg provably does. Two consecutive large-response arms both succeeded, which is *consistent with* per-request upstream connections but does **not** establish it. The SPEC settles it by probe.

## Two execution findings worth carrying

- ⚠️ **The first echo-arm reading was VACUOUS and only the positive control caught it.** Arms C and C0 both returned `502 bad gateway` — the split and un-split arms agreed, which reads exactly like "no divergence". The cause was the backend, not the codec: envoy-go mirrors the downstream protocol upstream and sent the H2 preface to an HTTP/1.1-only Python server (`code 505, message Invalid HTTP version (2.0)`). Replacing it with an h2c backend made the arm discriminate. **A pair of arms that agree is not evidence of parity until the control is green.**
- ⚠️ **`pgrep -f` / `pkill -f` killed the tool call (exit 144)**, matching the controller's own shell command line — `reference_pkill_f_kills_own_tool_call`, fired live. Background probes were subsequently killed by PID with the `bash -c` line filtered out, and ports verified released with `ss -ltn`.

## The pick

**Row 88 `h2-continuation-frames`, a core-HCM / HTTP-2-codec MAINTENANCE row claiming NO family ordinal** (the row-85/86/87 precedent). ⚠️ **Unlike row 87, this row's provenance is INSIDE a sentinel window** — the gRPC-family paragraph at `ROADMAP.md:204` names *"the CONTINUATION-discard defect"* among its deferred candidates. It is filed as core-HCM maintenance because the defect is protocol-generic (reached by any H2 client with a large header block and any H2 upstream with a large response header block); **the counter-argument is recorded in BRAINSTORM §5 rather than hidden**, and the `:204` sentence narrows at ROW-DONE, which is a sentinel-affecting edit that must be MEASURED on both sides at the IMPL, never forecast.

The standing directive says *smallest defensible candidate first*, and **this row is not the smallest by line count — that is recorded rather than argued away.** It is chartered because every genuinely smaller candidate is **not a product defect at all** (nothing to reproduce, so no red anchor), and the two smaller *product* candidates are both larger on re-derived cost. Severity is doing the work here, and it is **measured** severity. Rejected with every cost re-derived at this tip (BRAINSTORM §4): `ssl.connection_error` (+444 whole-`.go` floor, still absent as a landed stat name); `test/conformance/grpc/` (still only `h2spec/` + `proxy-wasm/`; 9/26 reachable); the stat-surface recount (rides a +0 row; re-measured the occurrence form at `NewCounter(` **327** / `NewGauge(` **79** — a different form from the lineage's carried 145/21, **no absolute corrected or carried**); REVIEW.md restoration (37 of 129 dirs; process-not-product); the D-86-CONN `client.Close` gate (~10 lines — a fold-in); hygiene fold-ins.

**D-88-SEQ PROPOSED: a SPLIT phase, two legs — 88.1 downstream/server (`conn.go`), 88.2 upstream/client (`client.go`)** — both independently red-anchored, 88.1 alone shippable. ⚠️ **NO sub-phase ROWS minted at this stage** (the row-84 precedent is a split recorded in PROSE with a single parent row); minting them later moves `want` a second time and is a sentinel-affecting edit.

## Sentinel (measured, BEFORE and AFTER the ROADMAP edit)

BEFORE at `want=119` (input **237 lines / 119 data rows**): (1) **SILENT — the FIFTH silent reading in project history**, and CORRECT because every row really was `done` · (2) SIX `:197 :203 :209 :219 :225 :233` · (3) SILENT. AFTER at `want=120` (input **238 lines / 120 data rows**): (1) **`NOT DONE: row 88`** ALONE, denominator silent · (2) SIX, anchors shifted uniformly **+1** to **`:198 :204 :210 :220 :226 :234`** (the single inserted line at `:150` sits above all six windows) · (3) SILENT. Conjunction fails ⇒ **does NOT fire**; `stop` NOT created (checked at the repo root AND the worktree, both sides). ⚠️ **A silent check (1) is exactly what a BROKEN check produces, so the row-62 NC is the load-bearing evidence of the BEFORE side.** All four doctoring NCs fired both sides:

- **row-62 doctoring** (`NC LANDED? [ in-progress ]` inspected first): BEFORE -> `NOT DONE: row 62` **alone** (the silent side still LOOKS); AFTER -> `NOT DONE: row 62` AND `NOT DONE: row 88`.
- **want off-by-one**: BEFORE `want=118` -> `GATE FAIL: examined 119 data rows, expected 118`; AFTER `want=119` -> `GATE FAIL: examined 120 data rows, expected 119` (plus `NOT DONE: row 88`).
- **check-(3) doctoring** (residual **2 -> 0** confirmed first): `NEVER OPENED: gRPC` fired, `WASM` correctly silent.
- **check-(2) one-arm**: long arm alone **5**, short arm alone **1**, union **6** (a one-arm strip is not an NC for the union).

Leak axes (whole-file count, BEFORE -> AFTER): lines **237 -> 238** · data rows **119 -> 120** · union **6 -> 6** · `-family row` **95/67 -> 95/67** (⚠️ pass `--`; the row's MAINTENANCE phrasing deliberately does not match check (3)'s `<slug>-family row` pattern — the rows 76/78/85/86/87 precedent) · `gRPC-family row` **2 -> 2** · `Operational-tooling-family row` **3 -> 3**. The ARM-A malformed-row figure {119, 131} is UNAFFECTED — those rows sit above the `:150` insertion point (not re-run with the fragile escape-aware command; the binding leak-invariance proof is the whole-file counts above).

## Anticipated counts

fixtures **121** (tail `0119-grpc-unary-trailers`, next-free `0120`; **NC observed** — `mkdir .../0120-nc-fake` => 122, removed => 121) — ⚠️ **the fixture delta is NOT yet anticipated +0**, extend-vs-mint is a SPEC decision · fuzzers **55 / 48 files** anticipated +0 (⚠️ a reassembly buffer is a plausible fuzz target — the SPEC must say yes or no explicitly rather than inherit +0) · BackendKind tail **38** anticipated +0 (a TAIL VALUE, not a count — 39 declarations, `TCPEcho = 0`) · `go.mod` anticipated +0 (the fix needs `bytes` and the already-imported `golang.org/x/net/http2`; no new sub-package) · stat surface anticipated **DELTA 0**, asserted by the same command both sides, **no absolute carried** (1205 vs 1207 stays contested). Anticipated ADR **ADR-0310**, TAIL-derived (`grep -c '^## ADR-0310'` => **0**; headings+1 COLLIDES at the ADR-0209 gap).

## Cost

**ESTIMATED ~+60-140 net production `.go` over TWO files / ~+300-700 net test.** ⚠️ **THIS IS AN ESTIMATE, NOT A MEASUREMENT, AND THE DISTINCTION IS THE POINT.** The carried "2-4x row 85 (+1046 realized)" figure is superseded because the §6.10 finding removes the largest imagined component. **The SPEC MUST ENUMERATE BY COMPILING PROTOTYPE**: `reference_measured_prototype_is_a_lower_bound` fired **ten consecutive times** before row 87 broke the streak, and ADR-0309 §Consequences (iii) records that row 87's escape rested on **one grep-confirmed call site measured twice by a working prototype**. This row has at least **three decode sites across two files** plus per-connection state; nothing about row 87 licenses treating this estimate as a floor that holds.

## NEXT

**SPEC** — dispose the eight BRAINSTORM §7 questions BY EXECUTION: Q1 the reassembly primitive decided by compiling prototype (per-connection accumulator vs `Framer.ReadMetaHeaders`, ⚠️ the latter changes the frame type the entire dispatch switch receives — **price both**); Q2 bounding the accumulator (CONTINUATION-flood class; a new stat name would break the anticipated stat-surface +0); Q3 the client-leg desync question left open above; Q4 the differential proof shape (⚠️ a stock transport reproduces this **without** a raw framer, so the cheap path is an EXTENSION — confirm by execution; `probe88` is the measured fallback, described in BRAINSTORM §2 well enough to rebuild, **not landed**); Q5 the reject-shape divergence post-fix; Q6 the false `dispatchFrame` comment and any contract sentence this row refutes (⚠️ grep for the SENTENCE); Q7 confirm or refute D-88-SEQ; Q8 re-confirm h2spec is **not** a red anchor. Draft **ADR-0310 §Context** STATUS `PROPOSED` (re-arms the strict guard **0 -> 1**). `ROADMAP.md` must be **BYTE-UNTOUCHED** at the SPEC (empty diff), `want` stays **120**, row 88 stays `in-progress`.

---

## SPEC — done 2026-08-14

## What landed

Docs-only: **ZERO production `.go`, ZERO test `.go`.** `SPEC.md` (new) · this `PROGRESS.md` entry · `DECISIONS.md` **ADR-0310 §Context** STATUS `PROPOSED` (**18150 -> 18166**, `numstat 16 0`, base a byte-exact PREFIX via `cmp`; headings **308 -> 309**, tail `## ADR-0310`, STATUS census **22 -> 23**, `^---$` **STAYS 216** — ⚠️ **a new ADR takes NO `---` separator**, verified against ADR-0309's own predecessor line before landing; the strict `^> **STATUS: PROPOSED` guard **0 -> 1**, the live pointer the phase-88 IMPL disarms) · `STATE.md` rolled IN PLACE · `STATE_HISTORY.md` appended. `ROADMAP.md` and `BEHAVIOR_CONTRACT.md` **BYTE-UNTOUCHED** (empty diff); row 88 stays `in-progress` at `:150`, `want` stays **120**.

## Method

⚠️ **NAMED INLINE DEPARTURE, FIFTH CONSECUTIVE STAGE** — no investigation agents; every probe INLINE by the controller. Centrepiece: **TWO COMPILING PROTOTYPES in DETACHED worktrees** (`wt-88-proto-a` the accumulator, `wt-88-proto-b` the `ReadMetaHeaders` alternative), both **DELETED WHOLE at close** after being measured. Plus `probe88` rebuilt from BRAINSTORM §2 with new arms J/K, a `q4probe` package driving the repo's OWN `test/helpers.H2CRoundTrip`, h2spec run at BOTH tip and prototype, and an h2c echo backend. Ports 47480/47481/47485. **No container created at this stage.**

## The stage's headline

**ALL EIGHT BRAINSTORM §7 QUESTIONS DISPOSED BY EXECUTION — AND THE BRAINSTORM'S CENTRAL STRUCTURAL CLAIM IS REFUTED.**

⚠️ **THERE ARE THREE LEGS, NOT TWO, AND THE READ FIX ALONE IS A REGRESSION.** `WriteContinuation` appears NOWHERE in `internal/`: both header-write sites emit the whole encoded block as one `WriteHeaders(... EndHeaders: true)`, and x/net's `WriteHeaders` does not split. That gap is MASKED at the tip (the read side drops the headers before they can be re-emitted) and UNMASKED by the read fix — the padded-upstream-response arm turns `200`-with-silent-loss into a hard **`http2: frame too large`**. Raising the probe client's `MaxReadFrameSize` to 1 MiB restores a full-fidelity 200, **proving envoy-go emitted ONE oversized HEADERS frame**. The request direction passed only because x/net's **Server** defaults to a 1 MiB read frame size while its **Transport** uses the 16 KiB spec default — **the write gap is direction-independent.** ⇒ revised **D-88-SEQ: 88.1 server read · 88.2 client read · 88.3 write (both directions)**, with the **MEASURED constraint that 88.3 lands with or before 88.2**.

**D-88-PRIM decided by prototype: the per-connection accumulator.** `Framer.ReadMetaHeaders` REFUTED on four grounds, three measured — ⚠️ **it compiles cleanly with zero diagnostics and then HANGS** (`ReadFrame` starts returning `*http2.MetaHeadersFrame`, which matches no arm of either `dispatchFrame` and falls through to the silently-ignore default; a scoped `-run TestServerConn` was `Terminated` at a 45 s timeout); it **mutates the decoder it is handed** (`SetEmitFunc`/`SetEmitEnabled`/`SetMaxStringLength`, and `decodeBlock` is built entirely on that emit callback); it **pre-empts envoy-go's own pseudo-header validation** with differently-worded x/net `StreamError`s in a family h2spec §8.1.2.1 covers 4/4 and row 87 just touched; blast radius **6 non-test sites + 37 test-side references** vs the accumulator's 3 production files. **And it does nothing for the write leg.**

**Cost MEASURED: net +150 production `.go` over THREE files** (`client.go` 136/91, `conn.go` 81/14, `framer.go` 38/0 — inflated on both sides by pure code motion; IMPL band ~+150-190), quoted as a **FLOOR**. Existing `internal/filter/hcm/...` suites GREEN on the prototype; `go.mod` +0 and stat-surface DELTA 0 both **MEASURED on the prototype** (`NewCounter(|NewGauge(` reads **406** in BOTH trees).

## Three findings worth carrying

- ⚠️ **h2spec's §6.10 CONTINUATION section passes 6/6 AT THE TIP THAT DISCARDS EVERY CONTINUATION FRAME** — including *"Sends multiple CONTINUATION frames preceded by a HEADERS frame"* — with §6.2 4/4 and §8.1.2.1 4/4, and the suite reading `95 tests, 94 passed, 1 skipped, 0 failed` **identically at tip and prototype**. Those tests assert the connection's *reaction*, never that the fields arrived. **A conformance section that NAMES the feature is fully green while the feature is entirely absent.**
- ⚠️ **A PROBE THAT COULD NOT ANSWER ITS OWN QUESTION.** The client-leg desync question was first probed with a single 20 KB header — which **exceeds the 4096-byte HPACK dynamic table and is therefore never indexed**, so dropping it cannot desync anything. Re-probed with 200 small indexable headers: tip delivers **165 of 200** twice with no connection-fatal error, and three distinct upstream source ports show **envoy-go opens a fresh upstream connection per request**. The client leg is **silent-loss-only, masked by connection lifecycle rather than by code safety** — recorded as a bounded measurement, NOT as "the client leg is safe".
- ⚠️ **A STALE SERVER MADE THE PROTOTYPE READ IDENTICAL TO THE TIP.** The first prototype battery reproduced the tip's failures exactly, which looks precisely like "the fix does nothing". The prototype binary had never started — `bind: address already in use` on the admin port, because a `pgrep -f 'a\|b'` alternation matched nothing and the tip binary still held it. Discriminated by the server log plus a `strings` symbol assertion (the fix's error string: **2** occurrences in the prototype binary, **0** in the tip). ⚠️ **Every subsequent reading asserts the bound binary with `ss -ltnp` first.** A separate instance of the same class: a probe edit that failed `go build` left the OLD binary in place and produced a stale, plausible reading — caught by asserting the new symbol in the binary.

## Q6 — the contract has NO sentence to correct, and that is itself the finding

`BEHAVIOR_CONTRACT.md`'s `## HTTP/2` section (`:2019`, 63 lines to `:2081`) makes **no claim** about CONTINUATION, header-block framing, `END_HEADERS` or frame size. Every whole-file `CONTINUATION` hit (`:782`, `:798`, `:820`, `:823`, `:825`, `:5109`) is the unrelated **tracing** sense ("continuation requests" continuing an inbound trace) and `:5281` is an async-resume "continuation site". **The IMPL ADDS a bullet rather than riding a correction** (unlike row 87). The one landed FALSE artefact this row kills is the `dispatchFrame` comment — whose "handled by framer as part of `ReadMetaHeaders`" clause describes a configuration the prototype shows would **hang the server**.

## Sentinel (ONE side — a SPEC edits no ROADMAP; the binding proof is the EMPTY `ROADMAP.md` diff)

Input **238 lines / 120 data rows**. (1) **`NOT DONE: row 88`** ALONE, denominator silent · (2) **SIX** at `:198 :204 :210 :220 :226 :234` · (3) SILENT ⇒ the conjunction fails, `stop` NOT created (verified absent at repo root AND stage worktree). All four NCs fired: row-62 doctoring (`NC LANDED? [ in-progress ]` inspected first) ⇒ `NOT DONE: row 62` AND `NOT DONE: row 88`; `want=119` ⇒ `GATE FAIL: examined 120 data rows, expected 119`; check-(3) doctoring (residual **2 -> 0** confirmed first) ⇒ `NEVER OPENED: gRPC` with WASM correctly silent; check-(2) one-arm **5**/**1**, union **6**. Leak axes INVARIANT: `-family row` **95 occurrences / 67 LINES**, `gRPC-family row` **2**, `Operational-tooling-family row` **3**.

## NEXT

**PLAN** — task decomposition under the REVISED three-leg D-88-SEQ honouring the **88.3-with-or-before-88.2** constraint, with each leg's RED anchor re-proven at the PLAN tip; **resolve SPEC §5's fixture blocker** (`0004`'s backend does NOT echo request headers, and `HTTPHeaderMutation` is HTTP/1.1 so it cannot sit behind an H2 downstream) with the test-side line count MEASURED; freeze §4's two open decisions (the over-size error code — `FRAME_SIZE_ERROR` vs `ENHANCE_YOUR_CALM`, unmeasured against the reference — and the stat question, default none); rule on §2's arm-E `LastStreamID` **1 -> 0** delta; the unit-test roster for `writeHeaderBlock` and the accumulator (⚠️ its three guard arms are **unreachable behind x/net's `checkFrameOrder`** and must be labelled defence-in-depth, not counted as coverage); a per-task budget on top of the measured `~+150-190` floor.

## PLAN — done 2026-08-14

## What landed

Docs-only: **ZERO production `.go`, ZERO test `.go`.** `PLAN.md` (new) · this `PROGRESS.md` entry · `STATE.md` rolled IN PLACE · `STATE_HISTORY.md` appended · `next-prompt.txt` rolled to the phase-88 IMPL. `ROADMAP.md`, `DECISIONS.md` and `BEHAVIOR_CONTRACT.md` all **BYTE-UNTOUCHED** (row 88 stays `in-progress` at `:150`, `want` stays **120**, the strict `^> **STATUS: PROPOSED` guard stays **ARMED at 1** — the IMPL disarms it by completing ADR-0310).

## Method

⚠️ **SUBAGENT-DRIVEN RETURNS — a NAMED RETURN from a FIVE-STAGE inline departure** (87 BRAINSTORM/SPEC/PLAN + 88 BRAINSTORM/SPEC all ran inline). Four probe agents on disjoint detached worktrees (`wt-88-plan-a` read legs + prototype, `-b` write leg + reference error code, `-c` fixture blocker, `-d` guard reachability + roster), disjoint port bands **47490-47529**, private scratch each, **each committing NOTHING and each proving its tree clean**. The controller ran the sentinel battery, the counts, the cross-agent reconciliation, and **re-derived every load-bearing agent claim by execution** — which is how a wrong reference-image cite was caught inside this stage. ONE reference container (`b88-plan-b`, `contrib-v1.37.2`) torn down BY NAME; pre-existing containers from other work left untouched.

## The stage's headline

⚠️ **THIS PLAN REFUTES ITS OWN SPEC ON Q3, AND THE ROW GOT MORE URGENT.** SPEC §6 concluded the client leg is *"silent-loss-only, masked by CONNECTION LIFECYCLE"*, resting on three distinct upstream source ports showing a fresh upstream connection per request. **MEASURED at this tip the upstream connection is POOLED AND REUSED — `127.0.0.1:34754` for all three requests — so the masking does not exist and the damage is CUMULATIVE:** request 1 loses **84 of 300** headers silently, request 2 **HANGS** (`context deadline exceeded` after 8.004 s), request 3 gets **502** while envoy-go tears down the pooled upstream with `GOAWAY COMPRESSION_ERROR` — hitting unrelated concurrent traffic. **88.2 is not the optional leg; it has the worst user-visible failure.** The SPEC recorded its own result as a bounded measurement rather than "the client leg is safe" — that caution was right, and the bound has now been re-measured in the opposite direction.

## Four SPEC arm parameters REFUTED — the control variable is the ENCODED block size

The SPEC's arm sizes were chosen against **raw** header bytes; Huffman shrinks values 20-40%, so three prescribed arms **never split and were vacuous**: **G at 20000 B** wrote `HEADERS len=16042` — one frame, all four headers arrive; **I at `?big=20000`** did not split at high entropy; **K at `?many=200`** wrote `HEADERS len=15175` < 16384 and delivered 200/200 twice. Re-run at parameters that actually split (`?many=300` ⇒ `HEADERS 16384` + `CONTINUATION 6328`). ⚠️ **AND ENTROPY SILENTLY MOVES THE SPLIT POINT** — a compressible `'B'x20000` pad *did* split where a high-entropy pad of the same size did not. **Every arm must pin the split by asserting the WIRE FRAMES, never a byte count.**

⚠️ **THE LOSS IS PARTIAL, NOT TOTAL — so the SPEC's intended fixture assertion was a DECOY.** Two independent seams agree against SPEC §5's *"both headers absent at 20 000 B"*: the h2c seam reads `NHDR 4 -> 1` and 216/300, and `0004`'s TLS-h2 seam at 32000 B shows the small sentinel **ARRIVING** with `padlen=0` in both directions. Fields encoded BEFORE the split survive; fields at or after it are lost. **The load-bearing assertion is the LENGTH of the large field** — and **never** *which* headers survive, which is x/net encoder ordering, not a contract.

## The third leg is TWO-SIDED and only one side is independently anchorable

**88.3s (server/response write) IS red-anchored at the unmodified tip** — `direct_response` + 40 `header_mutation` response mutations ⇒ `RAW-FRAME: HEADERS len=29408 flags=0x04 END_HEADERS`, exceeding the peer's advertised 16384 by 13024 bytes, with a conforming x/net peer getting `http2: frame too large`; a non-vacuous 5915-byte control is clean. **88.3c (client/request write) is NOT** — decode-side filter mutations never reach the H2 upstream request (`h2dispatch.go:457` calls `SetH2Request` **before** the decode chain; a pre-existing framework property, **not** a phase-88 defect), and a large downstream block is capped on read. **Its minimum unmasking prerequisite is the read fix.** Independently corroborated on the upstream wire: with `http2debug=2` the backend read `HEADERS len=35023` / `len=32022` from envoy-go against a peer advertising 16384. ⇒ **D-88-SEQ FROZEN: ONE ATOMIC IMPL COMMIT, TDD held inside, NO sub-phase rows minted** (`want` stays 120).

## The two open decisions, frozen — and BOTH candidates refuted

**D-88-CODE:** measured 5x against `contrib-v1.37.2`, the reference emits **`RST_STREAM INTERNAL_ERROR(2)`, stream-scoped, with NO GOAWAY at all** (`http2.too_many_headers`, stat `http2.header_overflow`, 60 KiB `max_request_headers_kb`) — **neither `FRAME_SIZE_ERROR` nor `ENHANCE_YOUR_CALM`**. The real divergence is **SCOPE**, which nobody had framed. **RULED: connection-scoped `ErrEnhanceYourCalm` at 16 MiB**, because under the decided byte-accumulator a stream-scoped reject would leave the HPACK table permanently desynced — the very defect this row fixes. ⚠️ **Recorded as a cost tradeoff, NOT an impossibility:** a decode-and-discard drain path would make stream-scoped parity achievable, and it is filed as a deferred candidate rather than silently foreclosed. **Both divergences (scope AND value) are NAMED in the contract.** **D-88-STAT: no new stat**, delta 0. **D-88-LASTID: PINNED** — `LastStreamID` **1 -> 0** CONFIRMED by execution, origin confirmed as x/net (`framer: connection-error code=1` on both sides), pinned together with a behavioural improvement the SPEC did not claim: **the tip still proxies the truncated request and returns 200 after emitting the GOAWAY; the prototype does not.**

## ⚠️ The SPEC's "defence in depth" instruction is REFUTED — the bound is REAL coverage

The SPEC ordered all three accumulator guard arms labelled *"unreachable, not counted as coverage"*. Measured two independent ways: nil-accumulator and wrong-stream **CONFIRMED unreachable** (isolation proven — with `AllowIllegalReads` the byte-identical inputs both deliver cleanly, so `checkFrameOrder` is the sole gate), but **the 16 MiB over-bound arm is REACHABLE**: `frames=1025 accumulated=16793600 bound=16777216 firstErr=<nil>` at the framer level, and end-to-end **the tip absorbed a 19.6 MB flood with no error and still proxied the request**. ⚠️ **Nothing upstream binds** — x/net's `maxHeaderListSize` lives inside `readMetaFrame`, which envoy-go never enables, so **envoy-go does not inherit x/net's CVE-2023-45288 CONTINUATION-flood mitigation**, and `SETTINGS_MAX_HEADER_LIST_SIZE` is never advertised. ⚠️ **And the exposure runs the other way: the TIP is safe because discarding retains nothing — THIS ROW'S ACCUMULATOR INTRODUCES the retention, and the bound is the mitigation for a hazard the fix itself creates.** Labelling it dead code would have dropped a real security test.

## The fixture blocker RESOLVED — MEASURED at 115 lines, +0 on every count axis

**Option 2 REFUTED, not merely disfavoured:** only three fixture backends read request headers (`0012`, `0015`, `0005`) and `grep -ln http2` over all three returns **nothing** — no fixture backend both reflects headers and speaks H2. **Option 1 chosen: extend `0004` in place.** Its two route tables are identical and `prefix: "/api"` already forwards to an H2 cluster, so **zero YAML edits on either side**, and both codec legs are exercised. **MEASURED `37 0` backend + `78 0` driver = 115 insertions, 0 deletions, 2 files, 0 new files** (re-derived by the controller from the saved patch, not accepted from the report), plus ~9 modified doc-count lines (29 -> 31; ⚠️ the "27" at `README.md:34` is **correct** for the pre-phase-87 prefix and must not be "fixed"). Both arms RED with verbatim lines, **both sides executed**, the response arm isolated by a separate run because the driver is fail-fast. **The negative controls carry the proof: 1024 B PASS, 16000 B PASS, 32000 B FAIL — the flip is at the FRAME-SPLIT boundary, not a size threshold**, which rules out "envoy-go just caps header size". Deltas: fixtures **121 +0**, BackendKind tail **38 +0**, `0120` stays unconsumed, no new port, blank imports **121 +0** — all three registration gates already satisfied.

## The roster is 11 rows, not 4 — and the SPEC's four would have missed the likeliest bug

MEASURED at **130 lines**, 11/11 printed and passing. A break arm that **hardcodes 16384 and ignores the peer's advertised value reddens ONLY `peer_max_larger` and `peer_max_max_legal`** — **every row the SPEC proposed stays GREEN**, so a hardcoded implementation would ship undetected. Two further break arms run: an always-trailing empty CONTINUATION reddens 7 rows; leaking END_STREAM onto the last CONTINUATION is **isolating**. ⚠️ **The zero-`MaxFrameSize` case is genuinely reachable** — `driveHandshake` (`settings_validate_test.go:38`) sends `fr.WriteSettings()` with no arguments, so that whole suite already runs at `MaxFrameSize == 0` — and ⚠️ **the policy is ALREADY LANDED TWICE** (`conn.go:778-783`, `client.go:854-858`, with the RFC 9113 §6.5.2 cite): **reuse the sibling constant, do not mint one.** With the guard deliberately removed the codec emits **unbounded zero-length frames**.

## Two method findings this stage adds

- ⚠️ **THE BINARY-ASSERTION TRAP HAS A FALSE-POSITIVE FORM.** `strings <tip-binary> | grep -c 'writeHeaderBlock'` reads **9 on the UNMODIFIED tip** — Go's bundled `net/http.(*http2writeResHeaders).writeHeaderBlock`. The naive assertion reads as *already present*. **The discriminating form is the qualified name** `hcm/h2\.(\*framer)\.writeHeaderBlock` ⇒ **0 tip / 2 fixed**. Re-derived first-hand by the controller.
- ⚠️ **THE ARCHIVE-ABSENCE GUARD IS FAIL-UNSAFE FOR THIS STAGE'S OWN EVICTION TARGET.** `phase 87 (h2-double-slash-path-routing) BRAINSTORM done` already reads **1** whole-file pre-append, because the phase-88 SPEC's own archived bullet at `:494` names it as the tie partner it did *not* evict. Expecting 0 and seeing 1 reads as "already archived, skip the append" — **which would LOSE the entry.** Replaced with a **discriminating own-label form** (extract each eviction bullet's own backticked label): target **0**, NC on `phase 86 (validate-sds-nil-provider) IMPL done` **1**. ⚠️ **Also refuted: the carried claim that `- **prior active-phase:**` reads 0 on archived entries — it reads 163.**

## Sentinel (PLAN) — ONE side; a PLAN edits no ROADMAP, and the binding proof is the EMPTY diff

Input **238 lines / 120 data rows**. (1) **`NOT DONE: row 88`** ALONE, denominator silent · (2) **SIX** at `:198 :204 :210 :220 :226 :234` · (3) SILENT ⇒ the conjunction fails, **`stop` NOT created** (verified absent at repo root AND stage worktree). All four NCs fired: row-62 doctoring (`NC LANDED? [ in-progress ]` inspected FIRST) ⇒ `NOT DONE: row 62` AND `NOT DONE: row 88`; `want=119` ⇒ `GATE FAIL: examined 120 data rows, expected 119`; check-(3) doctoring (residual **2 -> 0** confirmed first) ⇒ `NEVER OPENED: gRPC` with WASM correctly silent; check-(2) one-arm **5**/**1**, union **6**. Leak axes INVARIANT: `-family row` **95 occurrences / 67 LINES**, `gRPC-family row` **2**, `Operational-tooling-family row` **3**. ⚠️ **The `-` flag trap fired live at this stage** (`ugrep: invalid option`) — always pass `--`.

## Cost

**Production MEASURED at net ~+189** (read legs `client.go` +140/−88 and `conn.go` +82/−12 ⇒ +122; write leg `client.go` +5/−3, `conn.go` +6/−3, `framer.go` +62/−0 ⇒ +67), IMPL band **~+190-240** — at the top of the SPEC's ~+150-190. ⚠️ **Of the read leg's 222 insertions, 94 (42%) are pure code motion** (86 lines byte-identical after de-indent, 8 mechanical substitutions) **and ~95 are comments — genuinely new executable code is ~70-80 lines.** Test band **~+400-580** (differential 115 MEASURED, roster 130 MEASURED, accumulator/arm-E/flood units 150-250 not measured). **Quoted as a FLOOR**, with three newly-discovered un-enumerated classes NAMED: `hasPriority`/PRIORITY-flag on a split HEADERS (**entirely unprobed**), split trailers, and a split block on a concurrently-reset stream.

## NEXT

**IMPL** — one atomic commit under the frozen D-88-SEQ, TDD held inside (T0 unit RED census -> T1 differential RED census, ONE RUN PER ARM -> T2/T3 production -> T4 GREEN -> T5 docs -> T6 gates LAST). Row 88 flips `done` at `ROADMAP.md:150` (status cell ALONE, summary prose sha256-verified byte-identical); the `:204` gRPC-family window loses *"the CONTINUATION-discard defect"* (11 -> 10 candidates — check (2) **should** stay SIX, but ⚠️ **MEASURE BOTH SIDES, NEVER FORECAST**); `want` stays **120**; the strict `PROPOSED` guard goes **1 -> 0** by completing ADR-0310.
