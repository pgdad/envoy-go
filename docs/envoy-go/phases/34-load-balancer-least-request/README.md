# Phase 34 — `load-balancer-least-request`

**Status:** SPEC authored (lifecycle-state 1 → 2 at this commit; the D-L1..D-L7 empirical pins EXECUTED live against the contrib reference Envoy — held-open-conn LB probes + per-host `/clusters` scrapes + a 10-variant `--mode validate` matrix on a docker bridge network — + go-control-plane `/envoy` v1.32.4 bindings + upstream Envoy v1.37.2 source; AMEND-L1..L7 recorded; the ADR-0232 + ADR-0233 §Context drafts anchored; ready for the phase-34 PLAN per `superpowers:writing-plans`). PLAN / PROGRESS artifacts still pending; land when this phase enters subsequent lifecycle states.

**What this phase is.** Phase 34 is the **FIRST Load-balancing-family row** — the row that OPENS the Load-balancing family (a flat top-level row per ADR-0106; ROADMAP row 34, depends-on 33). It lands `Cluster.LbPolicy LEAST_REQUEST` (config message `Cluster.LeastRequestLbConfig`, `envoy.config.cluster.v3` — already in the pinned core `/envoy v1.32.4` module; **ZERO new go.mod dep**, D-L1) — the project's **first LB policy beyond ROUND_ROBIN**. Unlike every §9 row the subject is not a filter: it is a cluster-scoped endpoint-selection policy, and its structural piece — the **LB acquire/release seam** (ADR-0232) — is the project's **first framework seam OUTSIDE `internal/filter/network`**. Every future family row ({random, ring_hash, maglev, subset LB, locality-weighted LB, priority load balancing, panic thresholds} — 7 remain after 34) implements the same interface (the ADR-0230 build-at-first-consumer logic).

**The envelope (BRAINSTORM Q1, LEAN).** `LEAST_REQUEST` accepted by `cluster.Manager` (the lb_policy reject TEXT changes — the ONE deliberate byte-stable-reject change; blast radius pinned at D-L5: three sites, NO fixture pins it) + an un-weighted P2C `leastRequest` LB mirroring the upstream v1.37.2 source semantics EXACTLY (sample `choice_count` hosts WITH replacement, strict `<` on the raw active count, first-drawn wins ties, no clamp at `choice_count >= n` — D-L6) + cx-as-rq active-count semantics (an open TCP-proxied connection IS one active request — live-pinned, D-L2) over the reshaped unexported `loadBalancer` interface (`Pick() (Endpoint, release func(), error)` — OPTION C per D-L7: the EXPORTED `Cluster` surface stays byte-stable, ZERO consumer churn, release composed into the existing ADR-0063 `connWithGauge` Close-once hook). DEFERRED: `active_request_bias`/`slow_start_config` (DEPARTURE rejects when set — behavior-bearing), FULL_SCAN (not expressible on the legacy surface — even the reference unknown-field-rejects `enable_full_scan`; D-L3), the `load_balancing_policy` extension point, all other policies (rejected with the NEW byte-stable text; the reference accepts RANDOM/RING_HASH — a recorded departure), locality/panic/health-filtering, rq-scoped HTTP active accounting (the idle-pooled-conn approximation boundary).

**The seven empirical amendments (this SPEC session, D-L1..D-L7 executed live — SPEC §1.1):** **AMEND-L1** the three-knob surface re-pinned + the manager calls NO PGV (the `choice_count >= 2` reject is hand-rolled) + `least_request_lb_config` is an `lb_config` ONEOF member; **AMEND-L2** cx-as-rq CONFIRMED live (`rq_active == cx_active` per host 1:1; tcp_proxy charges rq-per-cx); **AMEND-L3** FULL_SCAN deferred with reference-side proof; with-replacement sampling → `choice_count == N` NOT deterministic; `choice_count > N` legal + quasi-deterministic (the `0059` band basis); **AMEND-L4** ZERO stat-name delta (surface STAYS 1116); the StatsAsserter set pinned (cx cross-equal; `upstream_rq_total` per-side — the subject's tcpproxy never increments it); **AMEND-L5** the 10-variant validate matrix + the 3-site blast radius + NO boot-reject dir; **AMEND-L6** the full P2C semantics from v1.37.2 source (+ the equal-weights short-circuit: bias never enters the equal-weight path; injectable-RNG posture mirrors upstream); **AMEND-L7** the seam is OPTION C (~155–255 prod LoC → the single flat row holds with margin; ADR-0024 unamended).

**The differential proof (family-level proof-shape novelties, deliberate).** ONE new fixture `0059-lb-least-request` (cross-side; chain `[tcp_proxy]` both sides; 3 `TCPEcho` backends REUSED — **NO new BackendKind**, tail stays 33): a hold-4 + burst-60 workload with a **band-based per-side `AssertDistribution`** (conservation + starvation `c1 <= 12` + concentration-shape `c2 >= 16` — the FIRST band-based use of the asserter; per-side because P2C is RNG-driven on both sides, NEVER cross-side-exact) + a cross-side `StatsAsserter` prong; **NO new fuzzer** (no wire decode — the FIRST no-fuzzer phase since the fuzzing regime began; fuzzers stay 42); every band leg proven live via deliberate-breaks with `-count=1` (inverted-comparison + no-op-release + dropped-Inc).

**Masters (read first, in order):**

1. [`./BRAINSTORM.md`](./BRAINSTORM.md) — **the phase-34 charter.** The Q0/Q1/Q-seam/Q-split decisions; the v1.32.4 three-knob verification + the FULL_SCAN-knob-ABSENT finding (§2.1); the framework survey (§3 — the seam, zero packages, zero deps); the fixture envelope (§6); the anticipated ADRs (§7); the §10 D-L1..D-L7 pins this SPEC executed.
2. [`./SPEC.md`](./SPEC.md) — **the authoritative phase SPEC.** The empirical-finding-driven scope (§1.1, AMEND-L1..L7); the single-row split disposition (§3.0); the seam + policy design (§3 — OPTION C); the proto roster + the oneof (§5); the reject matrix (§6); the zero-delta stat surface (§7); the `0059` band-arm design (§8); the ~10-task PLAN spine (§10); the D-L1..D-L7 empirical-pin block (§11); the live probe config + numbers (Appendix A).
3. [`../../ENVOY_TARGET.md`](../../ENVOY_TARGET.md) — the current pin (`envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0f…`, ADR-0227).
4. `docs/envoy-go/DECISIONS.md` — ADR-0232 + ADR-0233 §Context (this phase's ADRs; bodies at the 34 IMPL per ADR-0044) + ADR-0024 (the per-cluster LB-state scope — cross-referenced, NOT amended) + ADR-0063 (`connWithGauge` — the release attach-point) + ADR-0230 (the build-at-first-consumer seam-scoping precedent) + ADR-0106/0045/0044/0052/0060.
5. `internal/cluster/{loadbalancer,manager,cluster}.go` (the seam site + the lb_policy guard + the pick/dial funnel) + `test/differential/fixture/fixture.go` (the `DistributionAsserter`) + `test/fixtures/0001-tcp-proxy-rr/` (the harness shape `0059` transposes).

**Scope at phase 34** (per the SPEC + ROADMAP row 34):

- (a) The **ADR-0232 seam** — the unexported `loadBalancer` interface reshape (`Pick() (Endpoint, release func(), error)`); `roundRobin` no-op release (behavior-neutral — all 60 existing fixtures byte-identical); release threading inside `cluster.go` only (the `connWithGauge` composition; release-on-dial-failure; `PickEndpoint` immediate-release; the `AcquireH1` pool-hit immediate-release).
- (b) The **ADR-0233 policy** — the `leastRequest` type (P2C per AMEND-L6; per-endpoint `atomic.Int64`; injectable RNG); manager acceptance + `parseLeastRequestLbConfig` (the §6 accept/reject matrix); the NEW lb-policy reject text.
- (c) The **`0059-lb-least-request`** fixture (the band arm + the stats prong) — fixtures 60 → 61.
- (d) **ADR-0232/0233 bodies** + the BEHAVIOR_CONTRACT 34 bundle (stat surface UNCHANGED at 1116) + STATE/ROADMAP advance + the row-34 `in-progress → done` flip at the IMPL six-gate (a flat family row — NO parent rollup).

**Counts (at IMPL):** fixtures 60 → 61; fuzzers 42 → 42 (deliberate); stat surface 1116 → 1116 (deliberate); BackendKind tail 33 → 33 (deliberate); DECISIONS.md tail ADR-0231 → **ADR-0233 at THIS SPEC commit** (both §Contexts; next-free ADR-0234; bodies at IMPL).

**Predecessor:** Phase 33 CLOSED (`thrift_proxy`; the §9 Network-filters family CLOSED — 0 candidates remain).

**Successor:** the next Load-balancing-family row (TBD via brainstorm; 7 candidates remain — the ADR-0232 seam is the asset they reuse).
