# Phase 40.2 — `outlier-detection-other-consecutive` (the gateway + local-origin consecutive detectors)

**Status:** PLAN DONE (lifecycle-state 2 → 3 at this commit; `docs/envoy-go/phases/40.2-outlier-detection-other-consecutive/PLAN.md`). The **SECOND leg** of the phase-40 pre-authorized 3-leg by-detector-class split: the OTHER consecutive passive-outlier detectors — `consecutive_gateway_failure` (eject on N consecutive gateway-class 5xx `{502,503,504}`) + `consecutive_local_origin_failure` (eject on N consecutive connect/reset failures, split=true only) + the `split_external_local_origin_errors` accounting switch. Builds over the 40.1 substrate (ADR-0245) with NO router-seam re-design — it makes the 40.1-reserved `UpstreamResult.LocalOriginErr` field live. The PLAN decomposes SPEC §10 into **12 bite-sized TDD tasks** (the §12 D-S40.2-1..7 questions RESOLVED; the FINAL ADR-0045 split-gate re-check → NO FURTHER SPLIT [~150–260 prod LoC / 12 tasks]; plan-document-reviewer Approved-with-minors — the B1 `Endpoint`-not-comparable `IsZero()` fix + M2 applied). **Next → the phase-40.2 IMPL** (`superpowers:subagent-driven-development`). Counts UNCHANGED at this PLAN (stat surface **1137** / fixtures **71** / fuzzers **42** / BackendKind tail **35** / DECISIONS tail **ADR-0245**, next-free **ADR-0246**).

**What this leg is.** 40.2 reuses the 40.1 ejection lifecycle wholesale and adds: the **gateway** detector (gateway-class 5xx `{502,503,504}`; active-by-default but detect-only by default — `enforcing_consecutive_gateway_failure` default 0; gateway-checked-first, an ejection clears both consecutive streaks), the **local-origin** detector (connect/reset failures, active only when `split_external_local_origin_errors: true`), and the **split accounting switch** (false ⇒ local-origin failures mapped to the 5xx/gateway detectors; true ⇒ a dedicated local-origin detector + externals to the 5xx/gateway detectors). The one genuinely-new framework touch: surface the picked endpoint on the shared `Cluster.Dial`/`AcquireH1` connect-failure path so a local-origin failure attributes to the host the LB selected, then populate `UpstreamResult.LocalOriginErr: true` at the five router connection-failure sites.

**SPEC-firmed deltas (the §11 D-OD2-* live pins drove these):**
- **AMEND-OD2-1** — the gateway detector is active-by-default + detect-only-by-default (`enforcing_consecutive_gateway_failure` default 0); at 40.2 envoy-go now emits `ejections_detected_consecutive_gateway_failure` on the EXISTING `0069`'s 503s (matching the reference) → the `0069` `StatsAsserter` is WIDENED to cross-assert it (no longer a 40.1 departure); `0069`'s routing/ejection is unchanged.
- **AMEND-OD2-2** — gateway-checked-first + the once-ejected counter reset: with both `consecutive_5xx` and `consecutive_gateway_failure` configured + gateway enforcing, a 503 host ejects via gateway and `detected_consecutive_5xx` stays 0 (live-pinned).
- **AMEND-OD2-3** — the `split_external_local_origin_errors` accounting (the load-bearing pin): split=false ⇒ a connect failure → `consecutive_5xx`/gateway detectors (local-origin detector inactive); split=true ⇒ the same connect failure → `consecutive_local_origin_failure` ONLY (externals untouched). External 5xx always feed the gateway/5xx detectors regardless of split.
- **AMEND-OD2-4** — the local-origin attribution seam: `LocalOriginErr: true` populated at the H1 acquire-503/write-502/read-502 + H2 dial-502/roundtrip-502 sites; the picked endpoint surfaced from the shared connect-failure path (today returns `Endpoint{}`); the LB-pick-itself-failed path attributes to no host (no seam call); the ctx-cancel H2 path is a downstream cancel (no seam call).
- **AMEND-OD2-5** — stat surface **+4** (1137 → 1141): `ejections_{detected,enforced}_consecutive_gateway_failure` + `ejections_{detected,enforced}_consecutive_local_origin_failure`; the double-count EXTENDED (gateway/local-origin ejects bump `ejections_enforced_total` too); the legacy + statistical stats stay DEFERRED (40.3).
- **AMEND-OD2-6** — ZERO new BackendKind: `0070` (gateway) reuses `HTTP503Responder` (35; 503 ∈ gateway set); `0071` (local-origin) reuses the `allocDeadPort()` dead-host mechanism (`0066`/`0067` precedent). Tail STAYS 35.

**The differential proof (traffic-driven ejection; +2 fixtures).** `0070-outlier-detection-consecutive-gateway-failure` (a 503 host ejected via the gateway detector; cross-side `ejections_detected/enforced_consecutive_gateway_failure` parity + `detected_consecutive_5xx == 0`) + `0071-outlier-detection-local-origin` (a dead/connect-refused host ejected via the local-origin detector under split=true; cross-side local-origin stat parity + `detected_consecutive_5xx == 0`). Both poll `/stats` until `ejections_active == 1` on both sides (the `0066`/`0069` poll-to-converge + warmup + delta-counter pattern, `reference_health_check_propagation_warmup`); 2 `-count=1` deliberate breaks each. The split=false local-origin-as-5xx arm is a `record`-level unit test. NO new fuzzer.

**Anticipated at the 40.2 IMPL:** fixtures 71 → 73 (`0070` + `0071`), BackendKind tail 35 (UNCHANGED), DECISIONS tail ADR-0245 → ADR-0246 (next-free ADR-0247), stat surface 1137 → 1141 (+4), fuzzers 42 (UNCHANGED), ZERO new packages + ZERO new go.mod modules. 1 ADR (ADR-0246). Row 40 STAYS `in-progress` (flips `done` only when the 40.3 final leg lands — `reference_roadmap_split_phase_row_done`).

**Masters (read first, in order):**

1. [`./PLAN.md`](./PLAN.md) — **the phase-40.2 IMPL plan.** The §12 D-S40.2-1..7 resolutions (incl. the `tryEject` refactor, the gateway-first ordering, the `Endpoint.IsZero()` fix, `0070`/`0071` as two dirs); the file structure; the 12-task TDD spine (parse + counters → gateway detector → local-origin/split → +4 stats → the `LocalOriginErr` seam + endpoint surfacing → `0070`/`0071` fixtures → breaks/flake → `0069` widen + six-gate → ADR-0246 + completion); the per-task code + run commands; the Exit-deltas table.
2. [`./SPEC.md`](./SPEC.md) — **the phase-40.2 SPEC.** The §1.1 AMEND-OD2-1..6 block; the §3 architecture (the parse/detector extension + the gateway/local-origin/split branch + the `LocalOriginErr` seam population + the picked-endpoint attribution); the §5 proto roster (fields 10–14); the §6 two reject arms; the §7 stat surface +4; the §8 `0070`/`0071` differential design (ZERO new BackendKind); the §11 D-OD2-* live-pin block; the §12 PLAN/IMPL D-questions; the §13 ADR-0246 §Context draft.
3. [`../40-outlier-detection/BRAINSTORM.md`](../40-outlier-detection/BRAINSTORM.md) — the phase-40 charter (the 3-leg split §1.4; the 40.2 scope §8).
4. [`../40.1-outlier-detection-consecutive-5xx/SPEC.md`](../40.1-outlier-detection-consecutive-5xx/SPEC.md) — the 40.1 architecture the 40.2 detectors extend (ADR-0245; the seam + ejection dimension + `available` predicate + cap + lazy un-eject 40.2 REUSES UNCHANGED).

The PROGRESS.md follows at the IMPL (the per-sub-phase-dir precedent: 36.1/36.2, 39/39.2, 40.1).
