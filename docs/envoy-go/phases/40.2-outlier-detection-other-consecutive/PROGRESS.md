# Phase 40.2 IMPL — PROGRESS

The other consecutive outlier detectors (`outlier_detection.consecutive_gateway_failure` + `consecutive_local_origin_failure` + the `split_external_local_origin_errors` branch) over the phase-40.1 passive-health substrate. Executed subagent-driven per `docs/envoy-go/phases/40.2-outlier-detection-other-consecutive/PLAN.md` (12 tasks). **STATUS: IMPL DONE (2026-06-17) — six-gate GREEN, full 73-dir differential GREEN; ADR-0246 landed; counts at exit stat surface 1141 / fixtures 73 / fuzzers 42 / BackendKind tail 35 / DECISIONS tail ADR-0246 (next-free ADR-0247).**

## IMPL base commit

`3c71648`

## Baselines captured (pre-IMPL, at worktree HEAD)

- **`go build ./...`** — PASS (clean; exit 0)
- **`go vet ./...`** — PASS (clean; exit 0)
- **`gofmt -l internal/ test/`** — PASS (empty output — no drift)
- **`go test ./internal/... 2>&1 | tail -20`** — PASS (all packages `ok`, no failures)
- **Full differential suite** (`go test ./test/differential/ -count=1`) — **71-dir GREEN** (229.6s test time / ~231s wall)
- **Stat surface** — **1137** (carried forward from phase-40.1; tracked as a documented running total, no count-script; verified arithmetically: 1137 + 4 = 1141 at exit against the Task 5 registration test)

## Starting counts (pre-IMPL)

- stat surface: **1137** · fixtures: **71** · fuzzers: **42** · BackendKind tail: **35** (`HTTP503Responder`) · DECISIONS tail: **ADR-0245** (next-free ADR-0246)

## Exit counts (post-IMPL, at the completion bundle)

- stat surface: **1141** (+4 `ejections_{detected,enforced}_consecutive_{gateway,local_origin}_failure`) · fixtures: **73** (Task 7 added `0070`; Task 8 added `0071`) · fuzzers: **42** (unchanged) · BackendKind tail: **35** (`HTTP503Responder` — unchanged; `0071` reuses `allocDeadPort`) · DECISIONS tail: **ADR-0246** (next-free ADR-0247)

## Anticipated exit deltas (PLAN / SPEC §14)

| Count | Before | After |
|---|---|---|
| Stat surface | 1137 | **1141** (+4 outlier_detection stats) |
| Fixtures | 71 | **73** (`0070` gateway + `0071` local-origin) |
| Fuzzers | 42 | **42** (unchanged) |
| BackendKind tail | 35 (`HTTP503Responder`) | **35** (unchanged) |
| DECISIONS tail | ADR-0245 | **ADR-0246** (next-free ADR-0247) |
| New Go packages | — | 0 |
| New go.mod modules | — | 0 |

ROADMAP row 40 STAYS `in-progress` (40.3 leg pending — row flips `done` only when ALL legs land, per `reference_roadmap_split_phase_row_done`).

## Task checklist

- [x] Task 1 — Baselines + PROGRESS scaffold
- [x] Task 2 — `parseOutlierDetection` extension (gateway/local-origin/split) + the `consecGw`/`consecLO` counters + the 2 reject arms
- [x] Task 3 — Refactor the eject into `tryEject` + the gateway-detector path (`recordExternal5xx`, gateway-first)
- [x] Task 4 — The local-origin detector + the `split` branch
- [x] Task 5 — The +4 stat registrations (extend `registerStats`; 1137 → 1141)
- [x] Task 6 — The seam — surface the picked endpoint + populate `LocalOriginErr` at the five connection-failure sites
- [x] Task 7 — The `0070` cross-side gateway fixture
- [x] Task 8 — The `0071` cross-side local-origin fixture (split=true; dead host)
- [x] Task 9 — `0070`/`0071` deliberate breaks + 20-run flake
- [x] Task 10 — Widen the `0069` `StatsAsserter` + full 73-dir differential + six-gate
- [x] Task 11 — ADR-0246 body + BEHAVIOR_CONTRACT delta
- [x] Task 12 — Completion bundle

## Task notes

- **Task 12** (DONE) — DOCS-only completion bundle (no production/fixture/test code). Advanced the project bookkeeping to "phase 40.2 IMPL done": `STATE.md` (prepended the `phase 40.2 ... IMPL done` active-phase block + demoted the PLAN-done block to prior; updated lifecycle-state / next-skill = the phase-40.3 SPEC / last-commit / last-updated / phase-directory [now SPEC+PLAN+PROGRESS+README] / next-free-ADR; the count footer → stat surface **1141** / fixtures **73** / fuzzers **42** / BackendKind tail **35** / DECISIONS tail **ADR-0246**, next-free **ADR-0247**); `ROADMAP.md` (row 40 — appended the 40.2-IMPL-DONE note; **row STAYS `in-progress`** — the 40.3 statistical-detector leg is still pending, per `reference_roadmap_split_phase_row_done`); this `PROGRESS.md` (STATUS IMPL DONE + exit counts + the "Starting counts (pre-IMPL)" fixtures corrected 73 → **71** [the true pre-IMPL value; 73 is the post-Task-7/8 value]); `README.md` (status PLAN DONE → IMPL DONE + as-built summary); `next-prompt.txt` (rolled the cold-start forward to the phase-40.3 SPEC — the FINAL leg; the squash SHA left as a `<SQUASH_SHA>` placeholder for the controller to fill post-squash). **Final six-gate** (ADR-0052) re-run after the doc edits — all GREEN: (1) `go build ./...` ok; (2) `go vet ./...` ok; (3) `gofmt -l internal/ test/` empty; (4) `golangci-lint run ./...` ok; (5) `go test ./internal/... -count=1` ok; (6) `go test ./test/differential/ -count=1` — **73/73 dirs GREEN** (full suite, no `-run`). Counts at exit: stat surface **1141**, fixtures **73**, fuzzers **42**, BackendKind tail **35**, DECISIONS tail **ADR-0246** (next-free ADR-0247). Row 40 STAYS `in-progress`.
- **Task 11** (DONE) — DOCS only (no production code). Wrote the **ADR-0246** full entry in `docs/envoy-go/DECISIONS.md` (sibling of ADR-0245): §Context PROMOTED + refined from the SPEC §13 DRAFT (PROPOSED → ACCEPTED); §Decision (10-point body — the two detectors + the split accounting, gateway-checked-FIRST + the once-ejected short-circuit, the `record`/`recordExternal5xx`/`recordLocalOrigin` branch, the `tryEject` refactor shared by all three detectors, the live `LocalOriginErr` seam at the 5 connection-failure sites + the picked-endpoint surfacing on `AcquireH1`/`Dial`/`DialH2`, the `(Endpoint).IsZero()` no-host guard [Endpoint non-comparable], the +4 stats, the `consecGw`/`consecLO` counters on `hostHealth`, the `0069` widening + the split=false unit coverage); §Consequences (byte-stable when no `outlier_detection`; the 40.1 substrate REUSED UNCHANGED; statistical detectors + recovery arm DEFERRED to 40.3; the all-connection-failures-are-one-local-origin-class simplification; ZERO new packages/deps; counts at phase-done). **DECISIONS tail now ADR-0246** (next-free ADR-0247). Extended the `### Cluster — passive health (outlier detection)` BEHAVIOR_CONTRACT subsection: the gateway detector (active-by-default, detect-only by default, gateway-checked-first + once-ejected short-circuit), the local-origin detector + `split_external_local_origin_errors` accounting, the live `LocalOriginErr` seam, the +4 stats + extended double-count, the connection-failure-classification simplification, the `0069` gateway-detected widening (40.1 AMEND-OD4 departure LIFTED). **Stat-surface running total 1137 → 1141** bumped in BOTH the subsection header AND the `## Stat-name mapping` Phase-40.2 running-total block. `go build ./...` exit 0. All accurate to the as-built `outlier.go`/`cluster.go`/`router*.go` (verified the exact +4 stat names, the gateway-first `return`-on-eject short-circuit, the 5 seam sites, the `IsZero` guard). No inaccuracies found requiring resolution against source.
- **Task 10** (DONE) — Widened the `0069` `StatsAsserter` for AMEND-OD2-1: the gateway detector is active-by-default + detect-only, so host2's 503s now trip `cluster.c_od.outlier_detection.ejections_detected_consecutive_gateway_failure` on BOTH sides (it does NOT eject via the gateway path — `enforcing_consecutive_gateway_failure` defaults to 0; the 5xx detector still ejects). Added `assertAtLeast(t, sd.side, sd.st, statEjectDetectedGw, 1)` (using `0069`'s OWN `assertAtLeast`, which treats ABSENT as an error — so it is a genuine present-on-both-sides cross-assertion) + the `statEjectDetectedGw` stat key; removed the 40.1 AMEND-OD4 exclusion comment (driver + package-doc + README + expectations.yaml). Cluster name confirmed `c_od`. **Liveness proof** (no production change): temporarily appended `_BREAK` to the stat key → `--- FAIL: TestDifferential/0069`: both `reference:` AND `subject:` reported `...ejections_detected_consecutive_gateway_failure_BREAK ABSENT in /stats` (proving the real counter is present on both sides). Restored → PASS. **`TestDifferential/0069`** PASS in ~3.0s. **Six-gate** (ADR-0052) all GREEN: (1) `go build ./...` ok; (2) `go vet ./...` ok; (3) `gofmt -l internal/ test/` empty; (4) `golangci-lint run ./...` ok; (5) `go test ./internal/... -count=1` ok; (6) `go test ./test/differential/ -count=1` — **73/73 dirs GREEN** in ~225s (full suite, no `-run`). No regression in any other fixture (no byte-stability break from Tasks 2–8). Counts at exit: stat surface **1141**, fixtures **73**, fuzzers **42** (unchanged), BackendKind tail **35** (unchanged). Test-asserter widening + doc only — no production code touched.
- **Task 9** (DONE) — Verification-only (no production/fixture code change; all four breaks restored via `git restore`, HEAD stayed on `phase-40.2-outlier-detection-other-consecutive`; post-restore `git diff` clean). Each fixture's production path adversarially confirmed live (`-count=1` on every run; `TestDifferential/<NNNN>` selector):
  - **Break A/0070** — `internal/cluster/outlier.go:244-257` (the `if gateway { ... }` arm inside `recordExternal5xx`) no-op'd → `--- FAIL: TestDifferential/0070`: `converge: subject: cluster.c_gw.outlier_detection.ejections_active did not converge to 1 within 30s (last seen 0)`. The 5xx detector cannot fire (`consecutive_5xx`=100), so suppressing the gateway arm leaves the host un-ejected. Restored.
  - **Break B/0070** — `internal/cluster/loadbalancer.go:61` reverted `rr.health.available(ep)` → `rr.health.isHealthy(ep)` (drops the `!isEjected` check, keeping the ejected host in rotation) → `--- FAIL: TestDifferential/0070`: `warmup: subject: data path did not stabilize to 10 consecutive 200s within 15s (last status 503, err <nil>) — 503 host still in worker rotation?`. Restored.
  - **Break A/0071** — `internal/cluster/outlier.go:279-287` (the split=true arm of `recordLocalOrigin`: the `consecLOEnabled`/`consecLO.Add`/detect/eject block) no-op'd → `--- FAIL: TestDifferential/0071`: `converge: subject: cluster.c_lo.outlier_detection.ejections_active did not converge to 1 within 45s (last seen 0)`. The dead host never ejects. Restored.
  - **Break B/0071** — `internal/cluster/cluster.go:382` (the `AcquireH1` dial-failure return) reverted `return nil, ep, ...` → `return nil, Endpoint{}, ...` → the zero addr is unknown to `health.states` so `record`'s unknown-addr guard `if !ok { return }` short-circuits, `consecLO` never increments → `--- FAIL: TestDifferential/0071`: `converge: subject: cluster.c_lo.outlier_detection.ejections_active did not converge to 1 within 45s (last seen 0)`. Restored.
  - Post-restore both fixtures PASS (`0070` 2.1s, `0071` 2.1s). **20-run flake gate**: `0070` **20/20** PASS in 65s; `0071` **20/20** PASS in 67s (current committed deadlines — `0071` already at 45s; no widen needed). No flake; no vacuous assertion (every break bit with the expected message).
- **Task 8** (DONE) — Added the `0071-outlier-detection-local-origin` cross-side fixture (modelled on `0066` dead-host + `0070` outlier StatsAsserter). Cluster `c_lo`, ROUND_ROBIN, 3 endpoints {host0/1 HTTPEcho live, host2 = the dead `allocDeadPort` host}, `outlier_detection: { consecutive_local_origin_failure: 5, enforcing_consecutive_local_origin_failure: 100, split_external_local_origin_errors: true, interval: 10s, base_ejection_time: 30s, max_ejection_percent: 100 }` on BOTH sides. `refContainerListenerPort = 19160` (next-free; 0070 = 19159, confirmed free). `BackendCount() == 2` — the dead host is an INJECTED unbound endpoint (the 0066 mechanism: bind 0.0.0.0:0, capture port, close, memoize), NOT a runner backend, NO `PerHostBackendKind`, NO new BackendKind. `AssertStats` drives 5xx-tolerant GET / until the dead host accrues `consecutive_local_origin_failure` (each pick → connect refused → the H1 `AcquireH1` Task-6 seam → `RecordUpstreamResult{LocalOriginErr: true}` → the LO detector with split=true), poll-converges `ejections_active==1` (deadline widened to **45s** — a dead-host connect-refused can be slower than an HTTP 503), warms up, then asserts cross-side (`StatsAsserter`): `ejections_active==1`, `ejections_enforced_total>=1`, `ejections_{detected,enforced}_consecutive_local_origin_failure>=1`, AND the load-bearing `ejections_detected_consecutive_5xx==0` (split=true routes the local-origin failure to the LO detector ONLY — a split-branch regression mapping it to gateway-class 5xx would lift this off 0; the `assertEq` absent-as-0 carve-out swallows ABSENT not present>0). Unit driver test FAIL-then-PASS (`backendIdxFromBody` + the `allocDeadPort` memoization pin + constants); `go test ./test/differential/ -run 'TestDifferential/0071' -count=1` PASS in **2.94s**; gofmt/vet/golangci-lint clean. Fixtures **72 → 73**.
- **Task 7** (DONE) — Added the `0070-outlier-detection-consecutive-gateway-failure` cross-side fixture (modelled on `0069`). Cluster `c_gw`, ROUND_ROBIN, 3 endpoints {host0/1 HTTPEcho, host2 HTTP503Responder}, `outlier_detection: { consecutive_gateway_failure: 5, enforcing_consecutive_gateway_failure: 100, consecutive_5xx: 100, interval: 10s, base_ejection_time: 30s, max_ejection_percent: 100 }` on BOTH sides. `refContainerListenerPort = 19159` (next-free; 0069 = 19158). Reuses `PerHostBackendKind` + `HTTP503Responder` (BackendKind 35) — NO runner/BackendKind addition. `AssertStats` drives gateway-class 503s until host2 ejects via the gateway detector, poll-converges `ejections_active==1` on both sides, warms up, then asserts cross-side (`StatsAsserter`): `ejections_active==1`, `ejections_enforced_total>=1`, `ejections_{detected,enforced}_consecutive_gateway_failure>=1`, AND the load-bearing `ejections_{detected,enforced}_consecutive_5xx==0` (the gateway-first short-circuit: consecutive_5xx=100 unreachable + the gateway eject returns before the 5xx detector → 5xx counters stay 0). Unit driver test FAIL-then-PASS; `go test ./test/differential/ -run 'TestDifferential/0070' -count=1` PASS in ~2.0s; gofmt/vet/golangci-lint clean. Fixtures **71 → 72**.
- **Task 5** (DONE) — `registerStats` now allocates the +4 gateway/local-origin detector counters UNCONDITIONALLY on every outlier cluster (`ejections_{detected,enforced}_consecutive_gateway_failure` + `ejections_{detected,enforced}_consecutive_local_origin_failure`), matching the 40.1-five `r.NewCounter(op + ...)` style. Stat surface **1137 → 1141** (+4). `TestRegisterOutlierStats_{Present,Absent}` extended to pin all **9** outlier names (present on an outlier cluster, absent on a non-outlier cluster) — FAIL-then-PASS confirmed; full `go test ./internal/... -count=1` green. Folded in the cosmetic sweep of the now-stale `record` local-origin call-site comment.
