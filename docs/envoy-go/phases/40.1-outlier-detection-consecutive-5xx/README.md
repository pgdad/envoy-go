# Phase 40.1 — `outlier-detection-consecutive-5xx` (passive ejection: the keystone leg)

**Status:** SPEC DONE (lifecycle-state 1 → 2 at this commit; `docs/envoy-go/phases/40.1-outlier-detection-consecutive-5xx/SPEC.md`). The **keystone leg** of the phase-40 pre-authorized 3-leg by-detector-class split: passive outlier detection on the single `consecutive_5xx` detector — a host returning N consecutive 5xx on real traffic is EJECTED from the LB-eligible set for `base_ejection_time`, then automatically returned. The §11 D-OD-* empirical pins were EXECUTED IN-SESSION (2026-06-15) live against `envoyproxy/envoy:contrib-v1.37.2` per `reference_docker_probe_bridge_network`. **Next → the phase-40.1 PLAN** (`superpowers:writing-plans`) OR a new subject. ALL counts UNCHANGED at this SPEC (stat surface **1132** / fixtures **70** / fuzzers **42** / BackendKind tail **34** / DECISIONS tail **ADR-0244**, next-free **ADR-0245**).

**What this leg is.** 40.1 lands the load-bearing substrate the whole phase-40 reuses: the per-request **passive seam** (`cluster.RecordUpstreamResult(ep, UpstreamResult{StatusCode, LocalOriginErr})` from the live H1/H2 router drivers back to the cluster), the **ejection-state dimension** on `hostHealth` (`ejected`/`unejectAtNanos`/`ejectCount`/`consec5xx`, distinct from the active-HC `healthy` bit), the **`available = isHealthy && !isEjected`** LB-pick predicate (byte-identical when no `outlier_detection`), and the **`consecutive_5xx` detector** + the ejection lifecycle. REUSES the ADR-0242 `clusterHealth`/`hostHealth` registry + the ADR-0243 build-time-injected health-aware LB pick.

**SPEC-firmed deltas vs the BRAINSTORM (the §11 live pins drove these):**
- **AMEND-OD1** — un-eject is interval-SWEEP-driven in the reference (~`base_ejection_time + ≤1×interval`); envoy-go 40.1 uses LAZY (observation-driven) un-eject, goroutine-free; `interval` parse-accepted but its sweep role is a recorded departure; the recovery arm is DEFERRED in `0069` (no cross-side un-eject-timing assertion).
- **AMEND-OD2** — ejection duration is flat `base_ejection_time` at 40.1; the reference's `base × num_ejections` linear backoff + its per-healthy-interval decay both need the sweep → DEFERRED.
- **AMEND-OD3** — `max_ejection_percent` cap pinned LIVE: eject iff `(ejected+1)*100/total <= cap`; else `ejections_overflow++` (1/3=33.3% → cap≥34 ejects, cap 33 overflows). `0069` uses `max_ejection_percent: 100`.
- **AMEND-OD4** — stat surface **+5** (1132 → 1137): `ejections_active` (gauge), `ejections_enforced_total`, `ejections_overflow`, `ejections_detected_consecutive_5xx`, `ejections_enforced_consecutive_5xx`; the double-count (`enforced_total` + `enforced_consecutive_5xx` both ++) REPLICATED; the legacy + other-variant stats DEFERRED; the reference's 503-trips-gateway-detected behavior NOT replicated (no 40.1 gateway detector) — `0069` excludes that counter.
- **AMEND-OD5** — the seam lives in the LIVE `doH1ClusterAction` (`router.go:655`) + `doH2ClusterAction` (`router_h2.go:100`) success sites ONLY (NOT the test-only legacy `do`/`doH2`); the pre-pick dial/acquire-failure paths are 40.2 local-origin; weighted routes covered free.
- **AMEND-OD6** — `0069` needs a +1 BackendKind (`HTTP503Responder`, an in-process always-503 responder); 2 healthy backends reuse `HTTPEcho`.

**The differential proof (traffic-driven ejection; a NEW determinism shape).** ONE fixture (40.1): `0069-outlier-detection-consecutive-5xx` — a cluster {2 healthy `HTTPEcho`, 1 always-503} + `outlier_detection{consecutive_5xx, base_ejection_time, max_ejection_percent: 100}`, on BOTH sides. Drive 5xx traffic to trigger ejection, POLL `/stats` until `ejections_active == 1` on both sides (no fixed sleep; the `0066` poll-to-converge + warmup + delta-counter pattern, `reference_health_check_propagation_warmup`), THEN assert traffic concentrates on the 2 healthy hosts + cross-side `outlier_detection.ejections_*` parity (excluding the gateway-detected counter). 2 `-count=1` deliberate breaks; the un-eject/recovery arm DEFERRED. NO new fuzzer.

**Anticipated at the 40.1 IMPL:** fixtures 70 → 71 (`0069`), BackendKind tail 34 → 35 (`HTTP503Responder`), DECISIONS tail ADR-0244 → ADR-0245 (next-free ADR-0246), stat surface 1132 → 1137 (+5), fuzzers 42 (UNCHANGED), ZERO new packages + ZERO new go.mod modules. 1 ADR (ADR-0245).

**Masters (read first, in order):**

1. [`./SPEC.md`](./SPEC.md) — **the phase-40.1 SPEC.** The §1.1 AMEND-OD1..OD6 block; the §3 architecture (the seam + the ejection dimension + the `available` predicate + the detector/lifecycle); the §5 proto roster; the §6 reject roster; the §7 stat surface +5; the §8 `0069` differential + the +1 BackendKind; the §11 D-OD-* live-pin block; the §12 PLAN/IMPL D-questions; the §13 ADR-0245 §Context draft.
1. [`../40-outlier-detection/BRAINSTORM.md`](../40-outlier-detection/BRAINSTORM.md) — the phase-40 charter (the Q-decisions; the 3-leg split; the Approach-A architecture the SPEC firms).

The PLAN.md follows at the 40.1 PLAN session (`superpowers:writing-plans` — SPEC §10 → bite-sized TDD tasks; the FINAL ADR-0045 split-gate re-check; the §12 D-S40.1-* resolutions), then PROGRESS.md at the IMPL.
