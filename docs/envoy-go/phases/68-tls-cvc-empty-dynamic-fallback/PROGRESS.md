# PROGRESS 68 — TLS `combined_validation_context` empty-dynamic fallback

> Live task ledger for the phase-68 IMPL. The PLAN (`PLAN.md`) is the spine; this file records what ACTUALLY happened per task — red-first verbatims, WHICH break assertion fired (and any that did NOT), substitutions, and the six-gate evidence. Populated at the IMPL, not the PLAN.

## Stage pointer

- **IMPL done** (2026-07-19) — the full 9-task spine executed subagent-driven (fresh implementer per task + per-task adversarial reviewer + a final whole-branch review), worktree `.worktrees/phase-68-impl`, branch `phase-68-tls-cvc-empty-dynamic-fallback-impl` off master `4b9b5bfe`. **Row 68 flipped `done` at the T8 six-gate.** Every PLAN anchor re-derived per task; ZERO anchors refuted (the PLAN's RD-LINES boundaries all held). One brief bug found (T2's `-run` filter misses test 3's name — worked around, recorded). Counts at exit: fixtures **113** · fuzzers **55** · stat **1201** · BackendKind **38** · DECISIONS tail **ADR-0290 COMPLETE** (next-free ADR-0291) · go.mod modules **2**.
- **PLAN done** (2026-07-19) — the 9-task TDD spine (T1–T9) landed; adversarial verification found ONE SEVERE (the RD3 two-factor break, corrected to the union-pool upper-bound) + RD-DUPSITE + MINORs, all folded in.

## Task ledger (filled at the IMPL)

| Task | Status | Commit | Red-first / breaks / notes |
|---|---|---|---|
| T1 xds sentinel + dual-%w | **done** | f6c89787 | Red-first recorded (compile-fail then assertion-fail). S1/S1b→sentinel; S2/S3/empty-resources→errValidation-only, all green. Break A fired the `errors.Is(err, ErrEmptyValidationContext)` assertion (stream_test.go:327); Break B fired the `==false` scope-pin assertion (stream_test.go:345). RD-DUPSITE honored: applyResponse twins (:99/:103) untouched; :170 empty-resources stays errValidation-only. provider.go byte-untouched. Cycle guard: stats+xds only. Reviewer: spec ✅, quality approved, 0 findings. |
| T2 tls fallback branch | **done** | 32eb2a4b | Red-first recorded (tests 1/2/4; test 3 green regression pin, no red owed). Break C (`\|\| true` widened gate) fired test 3's boot-FAIL assertion; Break D fired test 2's require=true reject-substring; Break E fired test 1's ClientCAs/ClientAuth assertions. P5 block byte-intact (diff hunk starts below it); `:203` reject byte-identical at the new second call site (count 1→2 by design); every fallback path returns — nil-pool unreachable. Brief bug found+handled: the Step-1 `-run` filter misses test 3's name (ran separately). Reviewer: spec ✅, quality approved, 0 findings. |
| T3 sdsserver Option | **done** | b5d53c3f | Red-first recorded (`undefined: WithEmptyValidationContext`). Break F fired the `GetTrustedCa()==nil` assertion (S1-vs-S2 discriminator proven live). Default `!emptyVC` branch textually identical to the prior shape. Reviewer: spec ✅, quality approved; 1 Minor (Fatal on the oneof precondition mirrors the sibling test's precedent — carried to final-review triage). |
| T4 fuzz | **done** | 81b1691f | All three dispatch-verify arms confirmed: `downstream-sds-empty`→nil via FALLBACK; `downstream`→dies at the retained CVC-not-supported gate; `downstream-sds`→nil via pool substitution (different reason). Diagnostic + scratch flips removed (grep-verified). Fuzzer count 55→55; 10s fuzz smoke clean, no corpus committed. 19/19 seed subtests green. Reviewer: spec ✅, quality approved, 0 findings. |
| T5 fixture 0111 | **done** | 788bcc57 | First-run PASS (8.43s; reviewer re-ran independently, PASS). Non-vacuity walked by the reviewer arm-by-arm: trusted depends on the fallback installing CA_A (SDS genuinely serves S1); untrusted rejection is real verify-fail (`unknown authority` live in subject logs); none is alert-116 require enforcement. Fixture count 113; port 10447; blank import added in runner_test.go (chartered per-fixture wiring). NOTE: the PLAN's "ARM-UNIQUE secret name" is realized as fixture-distinct `edf_validation_ca` shared across arms, matching the 0110 chassis (arms discriminate by client cert; reviewer-adjudicated correct). Reviewer: spec ✅, quality approved. |
| T6 0111 breaks | **done** | 22acb9f4 (empty; tree byte-identical to 788bcc57) | **G1** (symmetric union-pool CA_A∪CA_B, substitution: concatenated-PEM via `caAInlinePEM`) FIRED the untrusted-arm structuralCheck (`untrusted=ACCEPTED`) with CompareBytes EQUAL — the untrusted arm upper-bounds the fallback pool to CA_A. **G2** (bare-polite swap, substitution: temporary `sendPolite` mode) PASS = the EXPECTED RD3 non-fire, recorded as the finding: forced-send is NOT observably load-bearing at require=true. **H** triplet: (a) symmetric CA_A→CA_B swap + structuralCheck disabled → ships PASS (CompareBytes blind to symmetric flips); (b) re-enabled → structuralCheck FIRED (trusted+untrusted); (c) asymmetric swap → CompareBytes mismatch at byte offset 8. **I** (side-gated one-char pin perturb) → subject-side pin fired ALONE (reviewer re-executed this break live — exact match). **J** (SDSPort mis-target) → served-this-arm precondition fired BEFORE any verdict. Four substitutions reported for the `[NOT pre-compiled]` breaks. Final `git diff 788bcc57 -- test/` EMPTY; re-green PASS. Reviewer: evidence ✅ per break (quoted assertions grep-verified in driver.go), approved, 0 findings. |
| T7 BEHAVIOR_CONTRACT | **done** | b0a9c6b4 | B1/B2(a)/B3-lead character-faithful to SPEC §9 (reviewer word-diffed). B2(b)/(c) re-anchored to S2/S3 — the OLD (b) named `trusted_ca:{}` as boot-FAIL, now FALSE (S1b falls back); retired correctly. `trusted_ca:{filename:""}` dropped-not-moved (set specifier → still boot-FAILs via os.ReadFile("") — reviewer walked it; the contract states nothing false). B4 matches the landed 0111 incl. the RD3 caveat (forced-send does NOT flip the require=true observable). Four verify-unchanged items zero-diff. Reviewer: spec ✅, quality approved, 0 findings. |
| T8 verify | **done** | (controller-run at b0a9c6b4) | Six-gate ALL GREEN: gofmt SILENT · vet 0 · build 0 · `go mod tidy -diff` EMPTY + `git diff master -- go.mod go.sum` NO DIFF · `golangci-lint run ./...` 0 · FULL differential `ok github.com/pgdad/envoy-go/test/differential 379.894s` EXIT 0 (all 113 dirs, zero flakes — no isolate-re-run needed). Cycle guard: `go list -deps ./internal/xds` ⇒ internal/stats + internal/xds ONLY. `-race` on internal/tls + internal/xds + sdsserver: all ok (no init_fetch_timeout recurrence). Counts MECHANICAL: fixtures **113** (tail `0111-tls-cvc-empty-dynamic-fallback`) · fuzzers **55** · BackendKind **38** (`H2GoawayResponder = 38`) · DECISIONS tail **ADR-0290** (ADR-0291 count 0) · stat surface **1201** (BC doc count, +0 — no mechanical command per STATE.md:31) · go.mod modules 2 (+0). Envelope audit (`git diff master --stat`): functional production = internal/tls/config.go + internal/xds/{secret,stream}.go ONLY; **provider.go ABSENT** (RD-PROV); internal/boot / internal/listener / validate ABSENT; sdsserver = the one chartered Option; runner_test.go = the one blank-import wiring line; ONE new exported symbol (`ErrEmptyValidationContext`). |
| T9 ADR-0290 + close | **done** | 35627656 (docs) + the stage-close squash | ADR-0290 completed IN PLACE (§Decision + §Consequences appended; STATUS → COMPLETE; tail stays ADR-0290, `grep -c '^## ADR-0291'` → 0). ROADMAP row 68 → `done` (status cell only; deferred sentences UNTOUCHED). STATE.md §Current pointer rolled (fixtures 113; lineage capped at five; next → phase-69 BRAINSTORM). Memory updates landed by the controller: `reference_go_client_cert_withholding` extended with the RD3 require=true physics; NEW `reference_sds_empty_ack_narrow_classifier` (ACK-vs-NACK specifier line, dual-`%w` two-sentinel carry, helper served-shape envelope lesson); MEMORY.md index updated. Router rolled to the phase-69 BRAINSTORM; sentinel re-run mechanically at close (check 1 SILENT post-flip; 2 prints 3; 3 prints gRPC/Runtime/WASM ⇒ does NOT fire; no `stop`). Controller squash-push at close. |

## Final whole-branch review (post-T9, pre-merge)

**ONE CRITICAL found and FIXED (commit 9526d635); the branch was NOT merged until the fix landed and re-review passed.** The T1-landed sentinel gate carried a second disjunct (`|| vc.GetTrustedCa().GetSpecifier() == nil`) classifying the SDS-served `trusted_ca:{}` shape ("S1b") as ACK-and-fallback. That contradicted the ADR-0287/SPEC-66 §3.9 arm-(b) LIVE probe already in the repo: the reference **PGV-NACKs** that exact served shape (`CertificateValidationContextValidationError.TrustedCa … field: "specifier", reason: is required`) and fails closed — only the fully-absent `trusted_ca` (an empty `CertificateValidationContext`, PGV-valid) is ACKed and merged away. The S1b disjunct entered at the PLAN (RD-SENT) via an envoy-go-INTERNAL parser argument (both disjuncts route to the same `dataSourceBytes` branch — true of envoy-go, silent about the reference) and was never reference-probed (Dossier P covered S1/S2/S3 only). **Fix:** gate narrowed to `vc.GetTrustedCa() == nil`; the S1b test case moved to the no-sentinel side (red-first re-run against the wide gate — both assertions fired — then green under the narrow); comments in secret.go/stream.go/config.go, ADR-0290 §Decision/§Consequences (+ a FINAL-REVIEW CORRECTION bullet), BC item 3/B1/item 10/0111-¶, STATE.md, the 0111 fixture wording, and the sdsserver test name all reconciled; the stale `config.go:427-429` E1 citation symbol-anchored (the review's Minor 2). Fixture 0111 unaffected (it serves the fully-absent shape); re-run green (8.4s). The T3 Fatal-vs-Errorf Minor was triaged ACCEPTABLE (the Fatal is load-bearing: with the wrong oneof arm, `GetTrustedCa()==nil` would pass vacuously). Memory extended: `reference_sds_empty_ack_narrow_classifier` records the corrected per-shape table AND the cautionary half — classify each served shape by REFERENCE evidence for that shape, never by internal routing. Re-review after the fix: READY TO MERGE.

**Correction note on the T1 row, the T7 row, and RD-SENT below:** where they say "S1/S1b → sentinel" / "the disjunction is precise" / "`trusted_ca:{}` … retired correctly (S1b falls back)", read them as the stage-faithful record of what those tasks landed; the final review narrowed the classifier to S1-only and re-reversed the T7 retirement (this section governs).

## Findings carried from the PLAN (RE-DERIVED at `fba6d385`; RE-VERIFY at the IMPL tip)

- **RD-DUAL / RD-PROV:** `stream.go:164` is `%w: %v` (flattens); dual-`%w` there preserves both sentinels; `provider.go:112` still NACKs on `errValidation` and returns the full chain → `config.go` sees the sentinel with NO `provider.go` edit.
- **RD-P5:** the CVC arm does NOT move in phase 68 (no hoist); the P5 block `:158-164` stays BYTE-INTACT in place — the SPEC's `[MOVE INTACT]` re-derives to byte-preserve.
- **RD3 (EMPIRICALLY SETTLED):** at `require_client_certificate: true`, a BARE forced-send→polite regression does NOT change the correct-impl observable (both → `untrusted=rejected`). Forced-send is NOT observably load-bearing here — a permissive `CA_A∪CA_B` pool advertises CA_B so the polite client sends `client_B` too (the union hazard is caught in BOTH modes; the draft's "upper-bound via forced-send" two-factor break was UNSOUND and is replaced). The untrusted ARM upper-bounds the pool (Break G, union-pool, fires in both modes); forced-send is retained for meaning (exercises verify-and-reject vs collapsing into the `none` arm) + cross-side symmetry. The SPEC §8 "must go vacuous-green" is 0110 (require=false) physics that does NOT transfer.
- **RD-SENT:** the sentinel gate `vc.GetTrustedCa()==nil || vc.GetTrustedCa().GetSpecifier()==nil` is precise (both route to `dataSourceBytes`'s "none set" branch); S2's set-but-empty `inline_bytes` has a SET specifier → stays "parse failure", no sentinel.
- **RD-LINES:** four sub-field rejects `:448-461`; fetch-error `if err != nil` `:184`; DEPARTURE comment `:185-193`; empty-both/inline no-anchor arm `:199-214` (reject `:203`).
