# PROGRESS 72 — tracing `custom_tags` `metadata` type, `HOST` MetadataKind only

> Live task ledger for the phase-72 IMPL. The PLAN (`PLAN.md`) is the spine; this file records what ACTUALLY happened per task — red-first verbatims, WHICH break assertion fired (and any that did NOT), substitutions, the T4 cost-claim hash verification, the T5 dispatch-verify trap result, and the six-gate evidence. **The Task ledger is populated at the IMPL, not the PLAN.**

## Stage pointer

- **PLAN done** (2026-07-23) — the 9-task SINGLE-FLAT-ROW TDD spine (T1–T9) landed; the SPEC §3/§9/§11 anchors RE-DERIVED at `2a82cc7b` (**68 of 69 EXACT-HOLD; ONE MINOR drift (RD-LIT); ZERO SPEC claims refuted; FIVE new findings, three of them UNNAMED EDIT-SITE OBLIGATIONS**) by TWO independent read-only re-derivation agents on disjoint remits — agent A over `internal/tracing/{config.go,resolve.go,config_test.go,resolve_test.go}` + the go-control-plane proto shapes + the collision greps + an EXECUTED `structpb.NewStructValue(nil)` probe; agent B over `internal/cluster/{cluster.go,manager.go,subset.go,outlier_test.go}` + `internal/filter/hcm/{accesslog_emit.go,connection.go,h2dispatch.go,h3dispatch.go,fuzz_test.go}` + the emit test callers + the `0115`/`0064` fixture chassis + the counts. Fresh worktree `.worktrees/phase-72-plan` off master `2a82cc7b` (**== the phase-72 SPEC squash; NO parallel stream** — `git diff --stat 0150b04c 2a82cc7b -- '*.go' go.mod go.sum test/` is EMPTY, so the production tree is byte-identical to what the SPEC re-derived), branch `phase-72-tracing-custom-tags-metadata-host-plan`. Row 72 STAYS `in-progress`; **DECISIONS UNTOUCHED at the PLAN** (ADR-0294 stays PROPOSED, §Context only); **NO deferred sentence narrowed at the PLAN** (rolls OUT at the IMPL row-done, the phase-57 precedent); ROADMAP UNTOUCHED.

## Adversarial-pass record (PLAN §1.4)

*(TWO independent verifiers ran against the PLAN draft in PRIVATE scratch before landing — `reference_parallel_subagents_private_scratch`; the real repo left untouched, no worktrees registered. Records below are written from their actual reports; where a verifier found a defect, the fold is visible in PLAN.md at the point of use.)*

**BOTH found real defects: ZERO SEVERE · SEVEN MODERATE · TWELVE MINOR — all folded.** The single most valuable outcome: **V1 REFUTED one of the PLAN's own "drift corrections"**.

- **V1 — code-claims re-derivation + by-execution build probes.** Copied the tree to private scratch, **applied every §1.2 skeleton verbatim and BUILT the row**: `gofmt -l` SILENT · `go build ./...` 0 · `go vet ./...` 0 · `go mod tidy -diff` EMPTY (**+0 modules**) · layering check EMPTY · **sha256: `connection.go`/`h2dispatch.go`/`h3dispatch.go`/`accesslog_emit_test.go`/`chain.go`/`subset.go` byte-identical to baseline ⇒ THE CENTRAL COST CLAIM CONFIRMED BY HASH.** Predicted red set **EXACTLY TWO** `/host` subtests (output printed `Kind:5` ⇒ iota==5). Also EXECUTED-confirmed: F5a (gofmt expands the SPEC's compressed guards), **F5b** (`descend(NewStructValue(nil), ["k"])` → `(nil,false)` **without panicking**, identical to the guarded path ⇒ a span-routed Break C would be VACUOUS), Break G's discrimination, the FULL-`MetaPath` equivalence and the `[1:]` failure mode, the T5 dispatch-verify run ahead of time (`HOST ACCEPT arm reached for tag meta_host_ok` / `CLUSTER REJECT arm reached for tag meta_bad`), F4's reality (a direct `internal/cluster` import into `internal/tracing` builds clean ⇒ no cycle), `unsafe.Sizeof(Endpoint)` **104 → 112**, and ~55 discrete cites. **ZERO SEVERE. THREE MODERATE:** **(M1) RD-FUZZ's "drift correction" was ITSELF WRONG** — `CLUSTER/HOST` is on `fuzz_test.go:92`, so the SPEC's `:92-93` was CORRECT; `:93-94` holds no such token and an `old_string` built on it would have missed the stale claim and garbled the adjacent correct sentence ⇒ reverted, RD-FUZZ restored to EXACT-HOLD, drift tally 2 → **1**, EXACT-HOLD 67 → **68**. **(M2) T4's entry state + red classification were both false** — after T3 the package does NOT compile (three `not enough arguments in call to tracing.ResolveCustomTags` at `:57`/`:118`/`:179`); the draft conflated the *emit-signature* arity (genuinely unchanged — the row's real distinction from 70/71) with the *`ResolveCustomTags`* arity (which changes exactly as in 70/71) ⇒ T4 entry state + Step 1 rewritten. **(M3) the `MetaLookup` doc-comment skeleton FAILED `golangci-lint`** — `analogue` is flagged by `misspell` (`locale: US`, enabled), breaking T1 Step 6 and T8 gate 5 on a verbatim paste ⇒ one-word fix to `analog`. Plus NINE MINOR (4th-arg distribution; RD-LIT's replacement figures also unreproducible; an unscoped `map[Endpoint]` grep; the zero-value over-count; **Break D not expressible at the call site** ⇒ substitution pre-named; **Break K over-warned** ⇒ it compiles, no substitution needed; Break F's phrasing backwards; a moot `lll` caveat; and a "test constructor" alternative that would have minted a THIRD exported symbol ⇒ replaced with `Cluster.PickEndpoint()`, EXECUTED as the zero-new-symbol path). **Verdict: BUILDABLE AS WRITTEN** after M3's one-word fix.
- **V2 — process, consistency, SPEC-coverage and stage-close mechanics.** Confirmed mechanically: docs-only envelope, ZERO production `.go`; DECISIONS + ROADMAP UNTOUCHED; row 72 `in-progress`; **ADR-0294 PROPOSED, §Context-only, `grep -c '^### Decision'` ⇒ 0 inside the block** (T9's APPEND instruction sound; the SPEC's V2 F1/F2 fix held) with the refuted "(the cycle guard)" wording present (F4 real); all five counts re-derive; every `117` scoped current and every `118` scoped IMPL-post-state; the three sentinel commands producing exactly the predicted output, no `stop`; SPEC §10/§11 production coverage complete with **no `TestNoNewStat`-class guard manufactured or omitted**; task count 9; break letters **A–Q unique and sequential**; break protocol fully bound; format faithful to phase 71. **ZERO SEVERE. FOUR MODERATE:** (F1) `PROGRESS.md` missing while five places cited it as a record channel ⇒ created; (F2) **§1.4/§1.5 asserted the adversarial record in the PAST TENSE while §1.5 was an empty placeholder, and self-certified immunity to that exact defect** (the phase-69 cited-but-unwritten class, aggravated) ⇒ the split COLLAPSED into one section populated from the real reports; (F3) the header's sentinel line pointed at "§Task 9" for the PLAN close, but §Task 9 is the **IMPL**'s close where check (1) goes SILENT — and the PLAN listed **none of its own stage-close obligations** ⇒ pointer corrected + a PLAN-close checklist added to Global Constraints; (F4) the `accesslog_emit_test.go` SPEC-roster departure silently inverted into a BYTE-UNTOUCHED gate ⇒ named explicitly in RD-EMITTESTS. Plus THREE MINOR (six-vs-**seven** stale comments; a `SPEC §13` miscite for the memory updates — §13 is "Deferred items"; RD-IDENT's re-run unassigned ⇒ now **T1 Step 0**). **Verdict: stage-close mechanics PASS with the required fixes, all applied.**

**What neither verifier could check, stated plainly:** the seven SPEC-time live-probe arms (P1–P7) were NOT re-run (containers cleaned up), so the reference behaviours behind B1–B4 and the `0116` design rest on the SPEC's own executed evidence. **Nobody has verified that `0116` produces a green cross-side assertion** — that is T6's job at the IMPL, and the non-`envoy.lb` namespace is genuinely new cross-side ground (RD-0064).

## Task ledger (filled at the IMPL)

| Task | Status | Commit | Red-first / breaks / notes |
|---|---|---|---|
| T1 `internal/cluster` (`Endpoint.filterMetadata` + `MetaLookup` + the `manager.go:884` populate) | **DONE** | `74a1d4bb` + `bbe1ab50` (review fixes) | **Step 0 collision greps at the IMPL tip, all 0:** `kindMetadataHost` 0 · `hostMetaLookup` 0 · `filterMetadata` 0 · word-boundary `MetaLookup` 0 (the naive substring returns **51**, all `RouteMetaLookup`-prefixed — not a collision). **All T1 anchors EXACT-HOLD at tip, ZERO drift** (`cluster.go:43-75`/`:49`/`:92`; `manager.go:883`/`:884`/`:754`; `subset.go:277-279`; `cluster.go` imported NEITHER `structpb` NOR `corev3`). **Red-first:** a 9-error compile failure (`undefined: filterMetadata` / `e.MetaLookup undefined`). **Green:** `go test ./internal/cluster/ -count=1` ok; `-race` ok. **Breaks A/B/C/D ALL FIRED, none vacuous, each re-derived INDEPENDENTLY by the adversarial reviewer in a private detached copy:** **A** (populate dropped) fired `manager_test.go:2257` *"MetaLookup(\"envoy.test\") ok = false"*, the `MetaLookup` unit test + `defaultSubset` guard correctly staying PASS · **B** (wrong source) `[SUBSTITUTION: the brief's literal `GetFilterMetadata()["envoy.lb"]` does NOT type-check — it is a `*structpb.Struct`, not a map; used the brief's own "single-entry map" parenthetical + a temporary `structpb` import in manager.go]` fired ONLY `:2257` while the `envoy.lb`-projection guard stayed PASS ⇒ the discriminating signature Break A alone cannot show — **the P2 finding made executable** · **C** (nil guard dropped) COMPILES and fires `cluster_test.go:925`/`:932`/`:939` printing `MetaLookup("absent") = (struct_value:{}, true), want (nil, false)` — **on the ACCESSOR'S OWN RETURN, no `descend`/span/`ResolveCustomTags` in the path ⇒ F5b satisfied, NOT vacuous** · **D** (projection disturbed) via the PRE-NAMED `subset.go` substitution, fired `:1476`/`:2301`/`:2334`; `subset.go` re-asserted byte-identical by sha256 (`9bc0c716…60ee` before/after). **SHAPE pin VERIFIED BY MACHINE:** `git diff HEAD~1 -U0 -- manager.go` is a SINGLE `@@ -884 +884 @@` hunk; `:883`/`:754` byte-identical AND unshifted; `subset.go` absent from `--numstat`; **exactly ONE new exported symbol** (`go doc -all` symbol delta at HEAD vs HEAD~1 = `+func (e Endpoint) MetaLookup(...)`, 46 → 47). **ALIASING PROVED MECHANICALLY** (mutating the source proto map after `extractEndpoints` is visible through `MetaLookup`; a copy would be invisible); `unsafe.Sizeof(Endpoint)` **112** (104 → 112 confirmed); `MetaLookup` 0 allocs/op. `go mod tidy -diff` EMPTY; go.mod/go.sum NO DIFF vs master. ⚠️ `structpb` imported UNALIASED (matching `subset.go:8` at the tip, per the brief's "re-derive the grouping at the tip"; the package identifier is `structpb` either way ⇒ T4 unaffected). ⚠️ `outlier_test.go:766` failed ONCE under `-race` — the documented PRE-EXISTING RD-RACEFLAKE; isolate-re-run at `-count=5` and a full `-race` re-run both green, NOT re-classified, NOT "fixed". **Adversarial review: Spec ✅ / Quality Approved**, ONE Important (this ledger row, now written) + FOUR Minor, THREE FIXED at `bbe1ab50`: `manager_test.go:2257`/`:2305` `Fatalf` → `Errorf` (`reference_fatalf_makes_assertions_unreachable` — under Break A the abort really did kill two INDEPENDENT downstream properties) · **a PLAN §1.2 skeleton correction: the doc comment's "the HCM chain's `RouteMetaLookup`" is wrong about the package** — `RouteMetaLookup` lives in `internal/filter/http/chain.go`, package `internal/filter/http`, NOT `internal/filter/hcm` ⇒ corrected in place · a production no-metadata-path assertion added to `TestExtractEndpoints_NoMetadataIsNilMap` (proved non-vacuous against a deliberate break in scratch). The FOURTH Minor is deferred BY DESIGN: `cluster.go:108`'s *"threaded as a method value at the three tracing emit sites"* is FALSE until T4 lands ⇒ **T8 envelope audit must confirm it became true** (three `picked.MetaLookup` occurrences at `accesslog_emit.go:57`/`:118`/`:179`). Original red/design notes: | `undefined: MetaLookup`/`filterMetadata`. Field ALIASES `lbe.GetMetadata().GetFilterMetadata()` (zero new alloc, sizeof 104→112 B); accessor nil-safe on the ZERO `Endpoint`. ONE new import (`structpb`). `:883` + `:754` + `subset.go` BYTE-UNCHANGED. Breaks A (populate dropped) / B (wrong source — the P2 finding made executable) / C (nil guard — **assert on the ACCESSOR'S return, not a span attribute; F5b**) / D (projection disturbed). `-race` (⚠️ pre-existing `outlier_test.go:766` flake). |
| T2 config parse (`kindMetadataHost` + the HOST accept arm) | **DONE** | `2aa98e45` + `afdee55f` (review fixes) | **ZERO drift — all 21 re-derived anchors EXACT-HOLD** (the `:54-60` iota block with `kindMetadataRoute` LAST; `k := md.GetKind()` `:227`; the ROUTE clone source `:248-264`; the `:267-268` HOST reject). **Red-first: the pre-known set was EXACTLY TWO** `/host` subtests (`…Metadata_RejectKinds/host` assert `:648`, `…MetadataRoute_RejectKinds/host` assert `:774`) — both printing **`Kind:5`**, the by-execution proof of iota==5. **`kindMetadataHost == 5` CONFIRMED BY EXECUTION** (a reviewer probe printed the whole enum: `kindLiteral=0 … kindMetadataRoute=4 kindMetadataHost=5`), not by reading the source. **The accept arm is a VERIFIED byte-clone of the ROUTE arm** — a mechanical diff of the two 16-line bodies yields **exactly ONE differing line** (`Kind:`). **`config.go` added ZERO imports** — proven by diffing lines 1-30 old vs new (import block BYTE-IDENTICAL; the first diff hunk is `@@ -50`). **No `[1:]` crept into the parse path** (`grep '\[1:\]' internal/tracing/*.go` ⇒ only the pre-existing REQUEST `resolve.go:84`), and a reviewer probe changing `MetaPath: path` → `path[1:]` FIRED the two-segment accept assertion ⇒ the FULL-path pin is live, not decorative. **Breaks E/F/G/H ALL FIRED.** **G re-derived INDEPENDENTLY by the reviewer and its discrimination claim CONFIRMED:** a full silent renumber left **exactly ONE failing test package-wide** (`kindMetadataHost = 4, want 5`) with `go build ./...` + `internal/filter/hcm` + `internal/cluster` all still green — a renumber genuinely fails NOWHERE else, and both `Errorf`s fired (a `Fatalf` would have hidden the ROUTE half). **F fired BOTH independent properties** (`/cluster` rows now accept: `:648`/`:773`; HOST now rejects: `:880`/`:947`/`:960`/`:978`), and **independence was proven MECHANICALLY IN BOTH DIRECTIONS** — Break E fires property 2 alone, a reviewer probe accepting CLUSTER-only fires property 1 alone ⇒ neither entails the other. `[SUBSTITUTION: Break E's naive form leaves `dv` declared-and-unused (compile error) ⇒ `dv := md.GetDefaultValue()` → `_ = md.GetDefaultValue()`; reported per `reference_plan_break_instructions_dont_compile`. Break F needed NO substitution despite its `[NOT pre-compiled]` flag.]` **Regression guards VERIFIED STILL REJECTING:** the CLUSTER rows, BOTH unset-kind rows, and the third uncited guard `config_test.go:460-464` (outside every diff hunk — the first is `@@ -624`); `grep -rn "host kind unsupported" internal/ test/ validate/ cmd/` ⇒ **0 hits**. **The nine stale comments narrowed** (7 production lines in 4 groups + 2 test doc comments). ⚠️ **SEQUENCING DEVIATION (deliberate, reported, and ADJUDICATED CORRECT):** the brief's Step 1 is self-contradictory — it orders the two `/host` rows REMOVED *and* their verbatim failures RECORDED, which is impossible in that order; the accept arm was landed first, the failures captured, then the rows removed. The reviewer corroborated the intermediate states independently by line-number arithmetic (the captured errors are +1 against the final file, reconciling only if the rows were still present and the `:627` narrow not yet applied). **Adversarial review: Spec ✅ / Quality Approved**, ZERO Critical/Important, THREE Minor — TWO fixed at `afdee55f` plus one escalated-and-authorized: **`config.go:174` "Three source types are supported" while the block lists FOUR** — stale since phase 70 and **missed INSIDE the very doc block T2 was rewriting** (the stronger miss) ⇒ corrected · **`config_test.go:584` `// reqMetaKind … (the only accepted kind)`** — false since phase 71, flagged by the implementer and correctly left alone on scope discipline, then authorized here rather than deferred (a deferral would have been a cited-but-unwritten obligation of exactly the class this PLAN was criticized for) ⇒ narrowed to match its `routeMetaKind`/`hostMetaKind` neighbours' style · **an unrequested SIXTH test** (`…MetadataHost_RejectKinds`) proven **byte-for-byte identical** to two landed tests (`diff` exit 0 on all three bodies) ⇒ zero added coverage, DELETED as scope creep; the report's "a third independent witness for Break F" justification was inaccurate — a literal copy fires in lockstep. Third Minor left as-is (a short comment-fill line, cosmetic). Original red/design notes: | `undefined: kindMetadataHost`, then exactly TWO `/host` subtests (`_RejectKinds/host` row `:639` assert `:648`; `Route_RejectKinds/host` row `:765` assert `:774`). APPEND at **iota==5**, never insert. Branch on `k.GetHost()` on the bound `k` (`md.GetHost()` does not exist). CLUSTER `:638`/`:764` + unset `:640`/`:766` + the third guard `:460-464` STAY reject. Narrows 6 stale production (F2) + 2 stale test (F1) comments. Breaks E/F/**G (silent renumber — the only assertion that catches it)**/H. NO new import. |
| T3 resolve arm (`hostMetaLookup` 5th param + the `kindMetadataHost` arm) | **DONE** | `f2c73ad3` + `fc55ea2f` (review fixes) | **ZERO drift — all 9 cited anchors EXACT-HOLD.** Red-first recorded verbatim (the compiler's arity mismatch). **The 32-caller roster reproduced BYTE-FOR-BYTE** — 33 raw hits, `:106` excluded as the `t.Errorf` STRING LITERAL via an explicit line-number gate in the transform (NOT a bare regex); the reviewer independently confirmed all 32 removed lines are real `ResolveCustomTags(` calls and that `:106`'s literal appears in the diff as an untouched CONTEXT line. **`resolve.go`'s diff has exactly ONE removed line** (the old 4-param signature) — the new signature is a byte-superset. **REUSE PROVEN BY HASH:** `sha256` over `descend`+`structpbValueToString` identical old vs new (`7cb1611f…`); `sha256` over the four pre-existing arms identical (`c690b49d…`); a direct `diff` of the landed ROUTE arm at its old vs new line span produces **no output**. **NO new import.** **The HOST arm vs its ROUTE clone source: the ONLY divergences are the case label, `routeMetaLookup`→`hostMetaLookup`, and the comment — ZERO logic drift.** **The designed intermediate red is EXACTLY THREE errors and nothing else** — `go build ./...` AND `go vet ./...` (which type-checks test files too) report only `accesslog_emit.go:57`/`:118`/`:179` `not enough arguments`; no other package, no other error. **Breaks I/J/K ALL FIRED; NONE needed substitution.** **I (THE CRUX) re-derived independently: descending `s.MetaPath[1:]` fires FOUR HOST test funcs while EVERY REQUEST and ROUTE subtest stays GREEN ⇒ the arms are NOT entangled**, and a purpose-built discriminator subtest prints the wrong hypothesis by name (*"resolved the WHOLE namespace struct … the arm descended MetaPath[1:]"*). **J fired EXACTLY ONE assertion** — present-empty got `DEF` where the `request_header` rule wants `""` — cleanly separating that rule from the `environment` rule, since the two predict DIFFERENT observables. **K COMPILES** (`go build ./internal/tracing/` exit 0 ⇒ the PLAN's correction of the earlier over-warning is CONFIRMED by execution) and the nil-func panic is attributed to the right test rather than crashing opaquely. **⚡ A REVIEWER-INITIATED BREAK BEYOND THE BRIEF — the whitespace pin:** removing `json.Compact` from `structpbValueToString` still PASSES under a plain build (detrand off) but **FIRES the HOST `list` assertion reproducibly under `-race` across 3 runs** ⇒ the serialization assertions are genuinely exact-compact-string, not substring, and `-race` is what bites (`reference_detrand_race_catches_protojson_value_substring`, matching the ROUTE precedent). `-race` green. **Adversarial review: Spec ✅ / Quality Approved**, ZERO Critical/Important, THREE Minor — **TWO FIXED at `fc55ea2f`, and both were the same real error**: the doc block and a test doc comment CONFLATED two distinct mechanisms, claiming the nil-lookup case is what the ZERO `Endpoint` produces. It is not — **a method value on the ZERO `Endpoint` is NON-NIL** (it returns `(nil,false)` via `MetaLookup`'s own nil-map guard), and T4 passes `picked.MetaLookup` UNCONDITIONALLY, so after T4 no production caller passes nil at all: the nil-tolerance is a DEFENSIVE CONTRACT sharing an outcome with the ZERO-Endpoint path, not the same path. (The mirrored `routeMetaLookup` gloss IS accurate for ITS callers — `connection.go:330` really does pass nil — which is exactly why the mirror misled.) Both comments now state the two mechanisms separately. The third Minor was report-only (the implementer's report quoted `--stat`'s total-changed-lines as insertion counts; the true `--numstat` is `32 1` / `275 32`) ⇒ **the wrong figures were NOT carried into this ledger.** TWO tests beyond the brief (a `wrong-namespace` control and an `unresolvable-middle` multi-segment case) adjudicated **justified coverage, not scope creep** — each discriminates a real wrong-implementation hypothesis. Original red/design notes: | `ResolveCustomTags` arity (32 real callers + the new tests; raw grep 33, `:106` is a string literal). FULL-path descent (NOT `[1:]`); the `request_header` default rule; `descend`/`structpbValueToString` reused VERBATIM; the `:12-31` doc gains a HOST clause; NO new import. Breaks I (wrong slice — the crux) / J (default rule) / K (nil-tolerance). `-race`. |
| T4 the three emit call sites (`picked.MetaLookup`) | **DONE** | `ef77640c` | **THE ROW'S HEADLINE COST CLAIM HOLDS — RE-VERIFIED FROM SCRATCH BY THE ADVERSARIAL REVIEWER, NOT TAKEN ON TRUST.** `git diff --stat HEAD~1 HEAD` ⇒ exactly TWO files: `accesslog_emit.go` (3 changed lines) + `span_emit_test.go` (**224 insertions / 0 deletions**). sha256 MATCH working-tree vs the pre-T4 blob for `connection.go` (`722687c5…`), `h2dispatch.go` (`00eed239…`), `h3dispatch.go` (`aec5926e…`), `accesslog_emit_test.go` (`a5b72936…`), `chain.go` (`df98e17b…`) and `subset.go` (`9bc0c716…`) — all six BYTE-UNTOUCHED. **The three emit SIGNATURES print byte-identical at identical line numbers `:27`/`:87`/`:149`**, 4th param still `picked cluster.Endpoint`. **`span_emit_test.go` proven PURELY ADDITIVE by the STRONGER test** — `diff` old-blob vs worktree yields **ZERO `<` lines** (nothing removed *or modified*, stronger than counting `-` lines); the 16 existing callers hash `7c8e0602…` on BOTH sides. `accesslog_emit.go`'s import block byte-identical ⇒ **NO new import**; `git diff -U0` is exactly THREE lines, identical in shape (`routeMetaLookup)` → `routeMetaLookup, picked.MetaLookup)`). **ZERO new exported symbols** (an exported-symbol set diff of `package hcm` at HEAD~1 vs HEAD yields ONLY the four new `Test*` funcs; all four helpers unexported and 0-hit outside the test file). **The populate path was used as required** — `NewManager` → `Get` → `PickEndpoint()`, confirmed to call `lb.Pick` + `release()` and **NOT dial** — so no test constructor and no third exported symbol was minted. Entry red recorded verbatim: exactly the three predicted `not enough arguments in call to tracing.ResolveCustomTags` at `:57`/`:118`/`:179`. **Breaks L/M BOTH FIRED and were BOTH re-derived independently.** **L** (`nil` at `:57` only) ⇒ **exactly ONE `--- FAIL` package-wide** — H1's `span attr htag = "", want host-resolved-value` — with H2/H3 PASS ⇒ **the three tests are genuinely per-site, and each call site is independently load-bearing**. **M** ⇒ exactly THREE `--- FAIL`s, one per site (`:696`/`:717`/`:739`), while the zero-`picked` test AND the phase-70 REQUEST and phase-71 ROUTE thread tests all stayed PASS ⇒ **the 3rd and 4th arguments were not perturbed**. `[SUBSTITUTION: Break M used a named package-level always-miss func rather than three inline closures — for identical shape at all three sites. ⚠️ The implementer's stated rationale ("so the break stays gofmt-clean") is FALSE and is corrected here: the reviewer EXECUTED the brief's literal inline-closure form and it is gofmt-clean, compiles, and discriminates byte-for-byte identically. Per `reference_a_drift_correction_is_itself_a_claim`, a substitution's RATIONALE is itself a claim.]` **⚡ A REVIEWER-INITIATED PROBE BEYOND THE BRIEF:** changing the T3 HOST arm to `descend(v, s.MetaPath[1:])` compiles and fires **all three** HOST tests while zero-`picked` stays green ⇒ the 2-segment nested path pins the FULL-`MetaPath` property **at the hcm seam too**, not only in `internal/tracing`. `-race` clean; gofmt/vet/lint silent. **The zero-`picked` test asserts BOTH arms** — `default_value` → `"fallback"`, and an empty default → **OMITTED**, via a PRESENCE check rather than `== ""` (the correct call, since the `request_header` rule makes present-but-empty emit `""`). ⚠️ **A PRECISION HAZARD FOR T8, independently confirmed:** a raw `grep -c 'f\.emitAccessLog' h2dispatch.go` returns **7**, not 6 — `h2dispatch.go:188` is a DOC COMMENT. A structural regex (`^\s*(c\.)?f\.emitAccessLog(H2|H3)?\(`) returns the correct **5 / 6 / 7 = 18**. Same class as T3's `:106` string literal ⇒ T8's mechanical audit must pin the exclusion. The reviewer also re-adjudicated the zero-endpoint split BY VALUE: only 10 sites are syntactic `cluster.Endpoint{}` literals, but `h2dispatch.go:313` takes `picked` from `directResponseAction.asRouterActionH2()` which returns `cluster.Endpoint{}` unconditionally (`actions.go:169`) ⇒ **11 zero sites = 6 PRE-Decide + 5 span-capable**, and the new test's comment (with all five cited line numbers) is ACCURATE. **Adversarial review: Spec ✅ / Quality Approved**, ZERO Critical/Important, THREE Minor — **NO code change required**: two were report-accuracy defects (the line-count arithmetic and the false substitution rationale), both corrected in THIS row rather than carried forward; the third (the zero-`picked` test's two arms coupled by a `Fatalf` precondition) is convention-consistent with the landed phase-70/71 siblings and was deliberately not changed. Original notes: Red is a **COMPILE error** — three `not enough arguments in call to tracing.ResolveCustomTags` at `:57`/`:118`/`:179` (T3 grew the ResolveCustomTags arity without touching its call sites). The EMIT-SIGNATURE arity is what stays unchanged; do not conflate the two. Append the 5th arg at `:57`/`:118`/`:179` ONLY. **THE 3 SIGNATURES + ALL 18 CALLERS + THE 29 EMIT TEST CALLERS STAY BYTE-UNTOUCHED — verify BY HASH (Step 4); a failure here refutes the row's headline claim.** Live HOST-span test ×3 (H1/H2/H3 independently) + a first-class zero-`picked` test (**the 5 span-capable zero sites, NOT the raw 11-of-18**). Breaks L (per-site) / M (wrong lookup). `-race`. |
| T5 fuzz seed + stale-comment narrow | **DONE** | `fc16d396` + `0a94cf28` (comment completion + independent re-verify) | **The `:92-93` anchor RE-GREPPED at the IMPL tip and CONFIRMED** — the token `CLUSTER/HOST` on `:92`, the close `departures, ADR-0080).` on `:93`, exactly as the SPEC said and as V1's refutation of the draft's `:93-94` "correction" established. **The seed is PURELY ADDITIVE and `meta_bad` STAYS on CLUSTER — no repoint** (controller-verified by reading the diff: 5 added lines for `meta_host_ok`, 2 changed comment lines, nothing else). **The adjacent dispatch-order sentence is BYTE-UNCHANGED** (`The custom_tags loop runs BEFORE the provider check (tracing/config.go: parseCustomTags precedes "provider required")`) — controller-verified at the tip, the specific hazard the PLAN warned about since it SHARES line `:93`. **THE DISPATCH-VERIFY TRAP — RUN TWICE, BY TWO DIFFERENT AGENTS, THE SECOND NOT TAKING THE FIRST ON TRUST. CONFIRMED:** temporary uncommitted stderr instrumentation at BOTH the HOST accept arm and the CLUSTER reject arm in `config.go` printed, verbatim and in this order: `T5-DISCRIMINATOR: tag="meta_host_ok" reached HOST accept arm` / `T5-DISCRIMINATOR: tag="meta_bad" reached CLUSTER reject arm`. **The discrimination was stated BEFORE the run** (`reference_probe_must_discriminate`): the alternative hypothesis — an earlier typeURL/provider reject intercepting the seed — would have produced **silence on BOTH lines**, because `parseCustomTags` returns on the FIRST error and `meta_bad` sits AFTER `meta_host_ok` in the tag slice, so **seeing the CLUSTER line at all proves `meta_host_ok` did not short-circuit the loop**. `config.go` restored via `git restore`; `git diff` empty. **Counts: 55 BEFORE and 55 AFTER** (`^func Fuzz`; an `f.Add` seed is not a fuzzer — `reference_fuzzer_count_docs_drift`). A 10s active-fuzz smoke: no panic, **NO corpus artifacts created or committed** (`git status` verified clean of `testdata/fuzz/`). **ONE controller finding, fixed at `0a94cf28`:** after the narrow the block comment UNDER-CLAIMED — its header still read `Phase 70/71` and its closing sentence still enumerated only the REQUEST, ROUTE and CLUSTER arms, **omitting the HOST accept-append that is the entire point of the tag phase 72 added**. A comment that is incomplete about what the seed now exercises is the same `reference_code_comment_not_evidence` class this row is retiring ⇒ header → `Phase 70/71/72` and the HOST clause added to both the mid-block enumeration and the closing sentence, with the two protected phrases re-grepped afterwards and confirmed byte-unchanged. *(Self-reported by the implementer and worth recording: an initial over-edit rewrote the whole block including the protected dispatch-order sentence; it was caught before any check ran, reverted, and redone as the minimal narrow.)* Original notes: PURELY ADDITIVE `meta_host_ok`; **`meta_bad` STAYS on CLUSTER — no repoint**. Narrow the stale comment at **`:92-93`** (the SPEC was right; a draft "correction" to `:93-94` was refuted). Do NOT disturb the adjacent dispatch-order sentence sharing `:93`. Dispatch-verify the seed reaches the HOST accept arm (`config.go:128` < `:138`) and record the ACTUAL result. Count 55→55 (+0). |
| T6 fixture `0116` (OTLP, NO writer) | **DONE** | `a1d99561` + `b35d6a22` (review fix) | **THE ROW'S NEW CROSS-SIDE GROUND IS ANSWERED: the REFERENCE resolves an ARBITRARY (non-`envoy.lb`) `filter_metadata` namespace for a `kind: {host: {}}` tag — SPEC probe P2 is now backed by a LANDED FIXTURE.** Namespace `envoy.test`. A per-side attribute dump (`FIXTURE_0116_DUMP=1`) shows all **12 REFERENCE spans** (real `envoyproxy/envoy:contrib-v1.37.2`, STRICT_DNS via `host.docker.internal`) carrying `host_hit = STRING("v-host-0116")` — **the endpoint value, NOT the configured default** — so the reference both resolves an arbitrary namespace AND propagates `lb_endpoints[].metadata` through DNS resolution. **The STOP condition was NOT triggered and NO silent fallback to `envoy.lb` was made.** **Cross-side PASS**, re-run INDEPENDENTLY by the reviewer with the FULL selector + `-count=1` (`--- PASS … 7.29s`), and again alongside `0114`+`0115` (all PASS, 20.83s). **Fixtures 117 → 118**; registration at `runner_test.go:143` exactly as predicted; port **10116** unique; NO `scripts/` dir; go.mod/go.sum untouched; +0 BackendKinds. **All 15 re-derived chassis anchors HELD (no drift).** **NOT VACUOUS BY COUNT — verified, not assumed:** 12 spans per side, and each of the four assertions (`host_hit`/`host_default` × ref/subj) fired its equality check **12 times**; both `assertHostTags` loops are preceded by a span-count `Fatalf` precondition, so an empty-slice silent pass is STRUCTURALLY IMPOSSIBLE. **The two YAMLs' metadata blocks are BYTE-IDENTICAL to each other** (`diff` of the extracted blocks ⇒ empty), as are the two `custom_tags` blocks. **STRING VALUES ONLY** — the sole metadata value is `host_k: v-host-0116`; no struct-valued or numeric tag is asserted anywhere. **Breaks N/O/P/Q ALL FIRED; NONE needed substitution** (all YAML edits ⇒ the compile hazard does not apply); **P and Q were re-derived INDEPENDENTLY by the reviewer**, along with O and N. **P (THE ANTI-VACUITY CONTROL) HOLDS:** pointing `host_hit`'s `metadata_key.key` at an absent namespace fires **12 ref + 12 subj** value assertions (each falling to `unused-default-0116`) while `host_default` fires **ZERO** ⇒ the green depends on the endpoint-metadata/namespace binding, **not on a default match that would pass regardless**. **Q discriminates HOST from ROUTE:** moving the metadata block back onto the route while KEEPING `kind: host` fires 12+12 ⇒ a HOST-kind tag does not read route metadata on EITHER side. **O fired the PRESENCE branch** (24 × `missing custom tag key "host_default"`) with **ZERO value-mismatch firings** ⇒ the tag is genuinely OMITTED, not emitted-as-`""` — the competing hypothesis would have printed a value mismatch, so **O is the strongest of the three: it discriminates OMIT from EMPTY-STRING, exactly the RD-DEFAULT rule the PLAN pins only in prose**. **N fired SUBJECT-SIDE ONLY** (12 firings, 0 reference) ⇒ not a shared-constant tautology. Each break restored with `git restore` and re-greened. ⚠️ **FREE PARITY EVIDENCE — but record the qualifier:** breaks O/P/Q each fired IDENTICALLY on both sides, which is genuine evidence the two implementations agree on those three edges (empty-default → omit; wrong namespace → default; HOST-kind ignores route metadata). **This is BREAK-TIME, UNPINNED evidence — the green fixture asserts NONE of those three edges, so a future reference behaviour change there would leave the suite green. It must not be cited as coverage.** **Adversarial review: Spec ✅ / Quality Approved**, ZERO Critical/Important, THREE Minor. **ONE FIXED at `b35d6a22`:** the driver/README/expectations all say "the SELECTED UPSTREAM endpoint's" metadata, but with exactly ONE `lb_endpoint` the fixture **cannot discriminate selected-endpoint from only-endpoint/first-endpoint/cluster-wide** — the single-endpoint design is deliberate and correct (a second endpoint would introduce LB spread, `reference_round_robin_offset_randomized`), but an UNNAMED limit reads as coverage it does not have ⇒ named explicitly in `expectations.yaml`'s `## NOT covered here` and in the README's parallel section. The other two are informational: the chassis-inherited `Fatalf` ordering that makes the HOST assertions reachable-only-if-the-inherited-structural-block-passes (**byte-inherited from the 0115/0114 chassis, NOT a T6 regression** — a chassis-level item for a future row), and the `file:line` cites embedded in the fixture's prose (all re-derived accurate today; established practice since `0115`). The reviewer also re-derived the cited anchors at tip rather than trusting them (`manager.go:884`; `accesslog_emit.go:57`/`:118`/`:179`; the T4 zero-`picked` unit test), walked every `expectations.yaml` line against the driver, and confirmed the fixture drops NO assertion from the `0115` chassis other than the two renamed tags. The SPEC §B2 pick-time-vs-success departure is genuinely out of scope here (every probe returns 200 ⇒ the failure path is never exercised) and is documented as such in BOTH the README and `expectations.yaml`, so this fixture cannot be mistaken for coverage of it. Original notes: The `0115` chassis with the metadata block RELOCATED from the route onto `lb_endpoints[0].metadata` (sibling of `endpoint:`, 16-space indent; after `envoy.yaml:116` / `envoy-go.yaml:112`) + both `kind:` flipped to `host: {}`. **STRING values only** (arbitrary reference struct-key order). Cross-side EXACT key+value, key-based. Registration at `runner_test.go:143`. Breaks N/O/**P (wrong-namespace anti-vacuity control)**/Q (source relocation — discriminates HOST from ROUTE). Fixtures 117→118. |
| T7 BEHAVIOR_CONTRACT B1–B4 | **DONE** | `ea41e55c` + `efd726b6` (accuracy fixes) | Docs-only, ZERO `.go`. Both SPEC cites (`:688`, `:741`) re-derived by first-clause grep; **every edit applied by unique text match, never by line number**. **Every prescribed claim VERIFIED AGAINST THE LANDED CODE by BOTH the implementer and the reviewer** — the departure roster is now genuinely `CLUSTER` (`config.go:269`) + the two unset arms (`:233` sub-case A, `:288` sub-case B) and **no `host` reject arm survives**; the FULL-`MetaPath` HOST descent (`resolve.go:137-141`) against REQUEST's `MetaPath[0]`+`[1:]` (`:88-92`); the `request_header` default rule (`resolve.go:186` `raw, incl. "" (present-empty EMIT)`); **BOTH exporters** (`span.go:147-155` OTLP + `zipkin.go:87-93` both read `s.Attrs`); the accessor/field/populate; and the boot-reject string quoted VERBATIM from `manager.go:890`. **Neither REFUTED parity claim was reintroduced** — the 404 arm is written as *"NO SPAN AT ALL … not a parity claim"* and the zero-endpoint arm as *"BOOT-REJECTED … cannot be expressed at all"* — and `upstream_cluster` is never mentioned in the new text, keeping that framework gap separate from the HOST MetadataKind. **UNCHANGED lines confirmed BYTE-UNTOUCHED by `--word-diff=porcelain`:** the `literal`/`request_header`/`environment` sentences, `max_path_tag_length`, the built-in-span-attrs paragraph and the Zipkin tag-roster paragraph. **Adversarial review: Spec ✅ / Accuracy ❌ → FIXED at `efd726b6`** — ONE **IMPORTANT** and four Minor, all landed. **THE IMPORTANT ONE MATTERS BEYOND THIS ROW: the draft asserted envoy-go carries the ZERO `Endpoint` "on EVERY failure sub-path" — FALSE for the post-acquire 502 class.** Once the acquire SUCCEEDS `picked` is populated (`router.go:592` / `router_h2.go:153`) and is then returned on the upstream-write and read-response 502s, which are span-capable post-decision sites ⇒ **a HOST tag there resolves normally from the REAL endpoint: PARITY, not departure.** Proof: `router.go:604`/`:617` call `RecordUpstreamResult(picked, …)` UNGUARDED while the acquire-failure site at `:575` must guard `if !ep.IsZero()`. The departure is now scoped to the upstream-**ACQUIRE** failure sub-paths only. A future row reading the draft would have believed envoy-go loses HOST tags on ALL upstream failures and could have "fixed" a non-bug. **The fixer then caught FOUR further precision defects the reviewer had not named:** (i) the acquire-failure enumeration was INCOMPLETE — a FOURTH zero-`picked` acquire return exists, the defensive H2 bounded grant-race 503 at `router_h2.go:128`; (ii) the over-broad wording occurred **TWICE**, the second in the *Does not yet apply to* `custom_tags` bullet, which the review had not flagged; (iii) the paragraph's own bold HEADER still read *"on the upstream-FAILURE paths"*, so fixing only the body would have left the HEADLINE asserting the retracted claim; (iv) two cited line numbers had drifted (`picked = pooled.Endpoint()` is `:592`, not `:585`; `:604`/`:617` are the `RecordUpstreamResult` calls, the RETURNS are `:605`/`:618`). The four Minor: **the "connect-failure 503 class" mis-stated envoy-go's own status code** (503 is the REFERENCE's; envoy-go synthesizes H1 503 / **H2 502**, `router_h2.go:148`) · **an UNPROBED reference mechanism stated as fact** — "no pick occurs on either side, since `TryAcquireRequest` precedes any pick" attributes an envoy-go symbol's ordering to the reference, whose circuit-breaker ordering was never probed ⇒ split into envoy-go fact + reference presumption · **"emits uniformly on both exporters" contradicted THIS SAME FILE** (`zipkin.go:89` drops the `node_id`/`zone` KEYS; the file's own roster paragraph already records 14-vs-16) ⇒ narrowed to the true claim, that envoy-go applies no VALUE-based drop on either exporter · **a test COUNT off by one** — `router_test.go` carries FOUR zero-`picked` failure-path assertions (`:194`/`:333`/`:492`/`:597`), not three, though the three CLASSES are right ⇒ restated as classes with NO number (`reference_a_drift_correction_is_itself_a_claim`). **Two implementer judgement calls ADJUDICATED JUSTIFIED:** B1's extra P7 clause (locality-level metadata contributes NOTHING to HOST resolution ⇒ no departure owed) is EXECUTED-proven, stated as a NEGATIVE so it cannot over-claim, and prevents the future `CLUSTER`-kind row re-deriving it; and B4's "two named boundaries" → "three", which was arguably OBLIGATORY since leaving it at two would have gone false the instant B2 landed. The reviewer also caught a THIRD, UNDISCLOSED addition — a new sentence about the already-landed REQUEST kind's `path[0]`/`path[1:]` pre-keying — **verified ACCURATE** (`resolve.go:88-92`/`:110-113`/`:137-141`, matching `reference_route_metadata_resolve_full_metapath`) and kept. Original notes: B1 metadata → CONSUMED for REQUEST+ROUTE+HOST, departures narrow to CLUSTER/unset-A. **B2 the pick-time departure — keep the scope EXACT** (EXECUTED for connect-failure only; pool-overflow + circuit-breaker UNPROBED; the 404 no-match and zero-endpoint arms have NO envoy-go counterpart and must NOT be written as parity). B3 the Zipkin empty-drop + serialization boundaries. B4 the `:741` narrow. |
| T8 verify (six-gate + envelope) | **DONE — ALL GREEN** | (no separate commit; evidence here) | **SIX-GATE, run MECHANICALLY by the controller on the frozen pre-stage-close HEAD:** (1) `gofmt -l internal/ test/ cmd/` **SILENT** · (2) `go vet ./...` **EXIT 0** · (3) `go build ./...` **EXIT 0** · (4) `go mod tidy -diff` **EMPTY** + `git diff --exit-code master -- go.mod go.sum` **EMPTY** (**+0 modules**) · (5) `golangci-lint run ./...` **EXIT 0** · (6) **the FULL 118-dir differential `ok 406.088s`, EXIT 0, ZERO failures** (no startup flake this run; no isolate-re-run needed) — and **RE-RUN AT THE FINAL HEAD after the close-time doc/comment fixes: `ok 396.531s`, EXIT 0** (the only `.go` delta between the two runs is comment text in `resolve.go`, but this project verifies MECHANICALLY, not by reasoning about what "cannot" matter). **LAYERING CHECK:** `go list -deps ./internal/tracing | grep -E 'envoy-go/internal/(filter|cluster)'` (no `...`) ⇒ **EMPTY** — `internal/tracing` stays filter-free AND cluster-free. ⚠️ **This is a DISCIPLINE check, NOT a cycle check**: `go list -deps ./internal/cluster | grep internal/tracing` is likewise EMPTY, so the direct import would build clean (RD-LAYERING / F4, re-executed a third time here). **`-race` on the THREE touched packages, all green:** `internal/cluster` **ok 5.538s** (⚠️ NEWLY load-bearing this row — the PRE-EXISTING `outlier_test.go:766` outlier did NOT reproduce in this run) · `internal/tracing` **ok 1.084s** · `internal/filter/hcm` **ok 1.077s**. **COUNTS re-run MECHANICALLY, never copied:** fixtures **118** (tail `0116-tracing-custom-tags-metadata-host`) · fuzzers **55** (`^func Fuzz`) · BackendKind tail **38** · DECISIONS tail **ADR-0294** (`grep -c '^## ADR-0295'` ⇒ **0**) · stat surface **1201 (+0)** — a span attribute registers no stat; there is NO mechanical command and **NO `TestNoNewStat` guard is owed for a tracing row** (none was manufactured). **ENVELOPE AUDIT — a dedicated agent, PASS on all SEVEN sections with ZERO FINDINGS**, every claim by command against `master` (`b1e467e8`); 19 files, 2351+/76−, **every one classified** (5 production · 6 test · 5 fixture (new) · 2 docs · 1 registration), and `git diff --name-only | grep -vE '^(internal/|test/|docs/)'` ⇒ EMPTY. **(A)** exactly the FIVE claimed production files. **(B)** the `internal/cluster` SHAPE PIN holds — `cluster.go` = 1 import + 1 unexported field + 1 accessor and no other hunk; `manager.go` numstat `1 1`; **the `ScalarsFromStruct` projection and `defaultSubset` lines located BY CONTENT are byte-identical to master AND unshifted** (still `:883`/`:754`); `subset.go` sha `9bc0c716616e29af` both sides. **(C) THE CENTRAL COST CLAIM CONFIRMED BY HASH** — all 15 byte-untouched paths were first confirmed to EXIST (anti-vacuous), then `changed_files=0` each; sha256 identical HEAD vs master for `connection.go` `722687c5…`, `h2dispatch.go` `00eed239…`, `h3dispatch.go` `aec5926e…`, `accesslog_emit_test.go` `a5b72936…`, `chain.go` `df98e17b…`, `subset.go` `9bc0c716…`. **(D)** the 18-caller roster INDEPENDENTLY re-derived = **5 + 6 + 7**, with the doc-comment hazard pinned: a raw `grep -c` gives 5/**7**/7 and the h2 inflation is `h2dispatch.go:188` (a comment, no open paren). ⚠️ **Note the ASYMMETRY the reviewer surfaced — h3's SEVEN are ALL REAL; only h2 is inflated, so the trap is reading `5+7+7=19`.** Regex used: `grep -E '^[^/]*[^a-zA-Z0-9_]f\.emitAccessLog(H2|H3)?\(' \| grep -vE '^\s*//'`. **(E)** exactly **TWO** new exported symbols, established by an **AST symbol-set diff** of each changed package at master vs HEAD (not a diff grep): `internal/cluster` 78 → 79 (only `MetaLookup`; the struct line changes only by the UNEXPORTED `filterMetadata`), `internal/tracing` 125 → 125 (only `ResolveCustomTags` gaining `hostMetaLookup`), `internal/filter/hcm` 13 → 13 UNCHANGED. ZERO new packages/modules/stats/BackendKinds. **(F) THE DEFERRED T1 CHECK IS NOW TRUE** — `cluster.go`'s `MetaLookup` doc claim *"threaded as a method value at the three tracing emit sites"* was FALSE when written and is now verified: exactly three `picked.MetaLookup` at `accesslog_emit.go:57`/`:118`/`:179`, and the repo-wide non-test `ResolveCustomTags(` call set is exactly those three. **(G)** no stray artifacts — `git status --short` and `git clean -nd` both EMPTY; the six on-disk `testdata/fuzz` dirs are PRE-EXISTING at master and absent from the diff; zero break/debug residue in any `.go` (hits only in this file's prose); the fixture tree on disk == the tracked set == the 5 expected files, NO `scripts/`; the main checkout shows only `?? .claude/`. **ROW 72 FLIPS `in-progress` → `done` at this gate** (ADR-0106, the SOLE leg — a SINGLE FLAT ROW). Original obligations: gofmt/vet/build/`go mod tidy -diff`/lint + FULL **118-dir** differential + layering check (`go list -deps ./internal/tracing`, no `...`, filter- AND cluster-free — a **DISCIPLINE** check, not a cycle check) + `-race` on the 3 touched pkgs (⚠️ `internal/cluster` NEWLY load-bearing) + counts + envelope audit **pinning the SHAPE for `internal/cluster`** (one field + one accessor + one import + one changed line) since BYTE-UNTOUCHED is unassertable there. **ROW 72 FLIPS `done`.** |
| T9 ADR-0294 + close | **DONE** | `21c17a09` (ADR) + `c45eff8e` (ROADMAP/STATE/router) + `9e230644` + the §Context narrow | **ADR-0294 COMPLETED IN PLACE** — `### Decision (landed at the phase-72 IMPL)` + `### Consequences` APPENDED after the hand-off line, STATUS flipped to **COMPLETE**; **`grep -c '^### Decision'` inside the block: 0 BEFORE, 1 AFTER** (no duplicate heading); no renumber; tail stays **ADR-0294**, `grep -c '^## ADR-0295'` ⇒ **0** before and after. **THE REFUTED "cycle guard" JUSTIFICATION CORRECTED IN PLACE (F4)** — re-executed a THIRD time here (`go list -deps ./internal/cluster | grep internal/tracing` ⇒ EMPTY; `go list -deps ./internal/tracing | grep -E 'internal/(filter|cluster)'` ⇒ EMPTY), so a direct import WOULD build clean; §Context now carries a bracketed correction, §Decision states it positively as a self-imposed LAYERING rule, and the six-gate bullet labels `go list -deps` "a DISCIPLINE check, not a cycle check". **The claim was NOT propagated into §Decision.** **ROADMAP: row 72 `in-progress` → `done`** (ADR-0106, the SOLE leg) + **the deferred sentence NARROWED NOW and only now** — the live Observability `candidates:` sentence rolls `HOST` OUT, leaving **`CLUSTER`**-only, and STAYS a live `candidates:` match (the phase-57 precedent). Row 72's notes cell gained its SPEC + PLAN + IMPL summaries (⚠️ the row carried ONLY its BRAINSTORM summary, since the phase-72 SPEC and PLAN both landed ROADMAP-UNTOUCHED — handled per the row-71 precedent, whose IMPL close authored all three together). Row-72 field count re-verified at **8**, matching rows 70/71 (a first draft embedded shell pipes in the cell, yielding 11 fields — caught before commit; it would have broken the table AND the sentinel's field-5 parse). **STATE.md §Current rolled IN PLACE** (ADR-0288) — lifecycle 3 → DONE; verified EXACTLY ONE live `lifecycle-state:` / `next-skill:` / `next-free ADR:` / count block; lineage re-capped at FIVE **with its PREAMBLE updated** to name both the new newest and the dropped bullet; counts re-run in-worktree (fixtures **118**, fuzzers 55, stat 1201, BackendKind 38, tail ADR-0294 COMPLETE / next-free 0295, modules 2). An intermediate edit briefly minted a `last-updated-ARCHIVED-PRIOR:` line — **precisely the fossil ADR-0288 exists to forbid** — deleted before commit. **Router rolled to the phase-73 BRAINSTORM** (the roller SELF-PICKS per the 2026-07-12 standing directive; the candidate pool carried forward, the pick NOT made here), **and the refuted "cycle guard" justification was NOT carried forward** — it survives only as an explicit correction with a do-not-reintroduce instruction. **SENTINEL re-run MECHANICALLY after every edit — it does NOT fire; `stop` NOT created** (`ls stop` ⇒ no such file): (1) prints **NOTHING** (row 72 flipped; every chartered row now `done`) · (2) `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:'` ⇒ **3** (the HOST narrowing did NOT drop the whole Observability sentence) · (3) prints `NEVER OPENED: gRPC` / `Runtime` / `WASM`. **FOUR MEMORIES written** (`reference_endpoint_metalookup_host_metadata_seam` · `reference_envoy_lb_namespace_not_privileged` · `reference_host_tag_resolves_at_pick_time`, plus the `MEMORY.md` index rows). **FINAL WHOLE-BRANCH REVIEW: MERGE-READY — ZERO Critical, ZERO production defects, no missing or untested behaviour, no envelope breach.** It re-verified every `.go:NNN` cite embedded in a LIVE artifact against the FINAL tree (all correct), traced the feature end-to-end, and ran a probe no per-task review could: **swapping `routeMetaLookup` and `picked.MetaLookup`, which are ADJACENT and IDENTICALLY TYPED so the compiler cannot catch it — it is caught FOUR ways**, because the ROUTE thread-live test deliberately passes the ZERO `Endpoint`. It also confirmed `picked.MetaLookup does not escape` (`-gcflags=-m`) ⇒ the method value is stack-allocated, zero heap cost on the span path. **ONE Important + THREE Minor, ALL FIXED at `9e230644` + the §Context narrow.** The Important one matters most: **the `0116` README and `expectations.yaml` still carried the "every failure sub-path" wording this branch had RETRACTED** — they landed at T6, ONE COMMIT before the T7 correction, so the sweep missed them, and they are exactly the two artifacts a future row picking up the deferred pick-time gap reads FIRST ⇒ both narrowed to the ACQUIRE scope with the post-acquire-502 parity noted. Also fixed: ADR-0294's §Context contradicting its OWN §Consequences on a contested count ("three unit tests" vs the actual FOUR across THREE classes) ⇒ restated as classes with NO number; the `resolve.go` doc under-enumerating where the ZERO `Endpoint` comes from. ⚠️ **On that last one the FIXER REFUTED THE REVIEWER'S OWN COUNT** — the review named "three post-decision upstream sites"; a full enumeration of all 18 shows **SEVEN** forward a router-produced `picked` (the three chain-completion sites plus the four `RunEncodeHeaders`/`RunEncodeData` early-returns, which an acquire failure reaches with `actionErr == nil` and `status > 0`), matching SPEC §11's "only 7 of 18 can ever carry a real endpoint" ⇒ rather than swap one contested number for another, both comments now name the source **by CLASS**, accurate at either figure (`reference_a_drift_correction_is_itself_a_claim`). Two residual sites were flagged and adjudicated: ADR-0294's §Context summary clause (**narrowed here** — the ADR is the durable artifact future rows cite) and the retracted wording surviving in `SPEC.md`/`PLAN.md`/`STATE.md`'s lineage bullet (**deliberately LEFT** — those are historical stage records of what each stage concluded at the time; the correction is recorded in the CONTRACT, the ADR and this ledger). Original obligations: ADR-0294 COMPLETED IN PLACE — **APPEND** §Decision + §Consequences after the hand-off line; **NO second `### Decision` heading**; no renumber; tail stays ADR-0294, next-free ADR-0295. **⚠️ CORRECT the §Context's refuted "cycle guard" justification (F4).** ROADMAP row 72 → `done` + the `HOST` MetadataKind narrowed OUT of the live Observability `candidates:` sentence (CLUSTER becomes the residual). STATE §Current IN PLACE + the lineage preamble updated. Sentinel re-run. Router → phase-73 BRAINSTORM. Memory updates written. |

## Controller process incident at the close (recorded honestly; NO effect on the deliverable)

At the stage close the controller ran `git add -A && git commit` **in the MAIN checkout on `master`** instead of the stage worktree. Cause: the Bash tool's cwd persists between calls, but a `cd` outside the primary working directory **silently resets it back** (`Shell cwd was reset to /home/esa/git/envoy-go`) — a `cd` into the memory directory did exactly that, and the next relative-path git command therefore ran on `master`.

**Detected** by the tripwire `git rev-list --count b1e467e8..HEAD` printing **1** instead of the expected 25, with `git rev-parse --abbrev-ref HEAD` printing `master`. **Contents of the stray commit: ONLY untracked `.claude/` logs + `settings.json` — ZERO phase-72 work** (the doc edits had been made through absolute worktree paths and were never staged there). **It was never pushed** (`origin/master` was still `b1e467e8`). **Recovery:** `git reset --mixed b1e467e8` restored `master` exactly, with `.claude/` untracked again as before; verified by `git show master:test/fixtures/0116-.../README.md` ⇒ absent.

⚠️ **The more dangerous half:** the "final gates" run immediately after — gofmt/vet/build/tidy/unit tests — had therefore validated **master's PRE-ROW tree**, returning GREEN while proving nothing about this branch. **All gates were re-run in the worktree** (`pwd` confirmed first) and are recorded green above; the FULL differential was re-run at the final HEAD as well. Standing fix, now a memory (`reference_bash_cwd_reset_commits_to_main`): use `git -C <worktree>` for every git command in a worktree session, and tripwire with `pwd` + `rev-parse --abbrev-ref HEAD` + `rev-list --count` before any commit or gate run. This is the controller-side twin of `feedback_subagent_worktree_path_targeting`.

## Findings carried from the PLAN (RE-DERIVED at `2a82cc7b`; RE-VERIFY at the IMPL tip)

- **RD-EXACT:** **68 of 69** anchors EXACT-HOLD. Agent A: 21/21 items, 63/63 discrete cites, ZERO line drift. Agent B: 47/48. The SPEC's cites are adopted verbatim EXCEPT **RD-LIT**. ⚠️ A second "drift" (RD-FUZZ) was recorded in the draft and **REFUTED by V1** — restored to EXACT-HOLD.
- **RD-LIT (DRIFT, MINOR):** SPEC §3.5's "all **167** test literals are keyed" — the FIGURE does not reproduce (measured **177 lines / 209 occurrences**; `internal/cluster/*_test.go` alone 115/117). **The CLAIM survives, verified two ways: there is NOT ONE positional/unkeyed `Endpoint` literal** — the only near-miss (`manager_test.go:1508`) is a FORMAT STRING, and `[]Endpoint{{` not followed by `Ident:` yields zero hits. ⇒ adding a field is zero-positional-breakage. **Do NOT copy "167".** Also: one of the 35 production zero-value occurrences (`cluster.go:91`) is the doc comment ⇒ 34 real literals + 1 comment.
- **RD-FUZZ (EXACT-HOLD — a draft "correction" REFUTED by V1):** the stale `withMetaTags` comment spans **`fuzz_test.go:92-93`**, exactly as the SPEC says (token `CLUSTER/HOST` on **`:92`**, close `departures, ADR-0080).` on **`:93`**). The draft's "correction" to `:93-94` was WRONG — that span holds no `CLUSTER/HOST` token, so an `old_string` built on it would have missed the stale claim and garbled the adjacent, CORRECT dispatch-order sentence (which shares line `:93`). **Use `:92-93`; do not disturb the following sentence.**
- **RD-KIND / RD-REJECT / RD-NOIMPORT-CFG:** `customTagKind` `config.go:54-60`, `kindMetadataRoute` LAST at iota 4 ⇒ appended `kindMetadataHost` = **5**. `CustomTagSpec` `:67-77` has all four metadata fields ⇒ NO new field. HOST reject `:267-268`; ROUTE clone source `:248-264`; `k := md.GetKind()` `:227`; CLUSTER `:265-266`; unset-A `:229-230`; unset-B `:269-270`; empty-tag `:201-203`; first-wins dedup `:275-279`. config.go adds ZERO imports (`metadatav3` not imported, not needed — `k` is inferred).
- **RD-PROTO:** `*CustomTag_Metadata` has **exactly six methods** (`Reset`/`String`/`ProtoReflect`/`GetKind` `:373`/`GetMetadataKey` `:380`/`GetDefaultValue` `:387`) — **NO `GetHost()`**. `*MetadataKind.GetHost()` at `metadata.pb.go:185`; `MetadataKind_Host_` at `:211`. `GetFilterMetadata()` `base.pb.go:861-866` nil-receiver-safe. `structpb.NewStructValue` `struct.pb.go:396-398`.
- **RD-RESOLVE-5TH / RD-HOST-ARM / RD-DESCEND / RD-SERIALIZE:** `ResolveCustomTags` `resolve.go:32` currently **4-param**; append a one-arg `hostMetaLookup`. Clone source = the `kindMetadataRoute` arm `:95-116` (`:117`/`:118` close switch/for). REQUEST `[1:]` at `:84` is REQUEST-only. `descend` `:128-142` (kind-agnostic) + `structpbValueToString` `:151-168` (`json.Compact` LOAD-BEARING) reused VERBATIM. `structpb` at `resolve.go:9` ⇒ NO new import. Doc block `:12-31`, the `routeMetaLookup` paragraph at `:24-29`.
- **RD-ENDPOINT / RD-POPULATE / RD-NORIPPLE:** `Endpoint` `cluster.go:43-75` (`Metadata` `:49`, `addr` `:74`), `Addr()` `:81-86`, non-comparability comment `:88-91`, `IsZero()` `:92`. **cluster.go imports NEITHER `corev3` NOR `structpb`** ⇒ `structpb` is the row's ONE new import. `manager.go:883` computes `scalars`; **`:884` is the composite literal where `filterMetadata:` is added** — `:883` byte-unchanged, `:884` MUST change. `defaultSubset` `:754`. Never a map key (0), never `==`-compared (the sole text is the `:91` doc comment), never JSON-serialized (0 `json:` tags). ONE non-empty production literal. `Endpoint.Metadata` consumers: exactly TWO (`subset.go:163`/`:250`). `ScalarsFromStruct` `subset.go:263-285`, drop set `:277-279`, FIVE production callers.
- **RD-SEAM / RD-CALLERS / RD-SPANCAP / RD-EMITTESTS:** emit signatures `:27`/`:87`/`:149`, all **9-param** with `picked cluster.Endpoint` **4th**; `ResolveCustomTags` calls `:57`/`:118`/`:179`, all **4-arg**; gates `:30`/`:89`/`:151`; `upstreamHostString(picked)` `:72`/`:133`/`:194` (the SOLE current `picked` consumer); `UpstreamCluster: ""` `:51`; **`structpb` already imported `:9`**, `internal/cluster` at `:12`. 18 callers exact (5+6+7). `traceDecision` assigned `connection.go:553`/`h2dispatch.go:449`/`h3dispatch.go:255` ⇒ **6 PRE-Decide + 12 span-capable = 7 real-`picked` + 5 zero**. `h2dispatch.go:313`'s `picked` provably `cluster.Endpoint{}` (`actions.go:169`). 29 emit test callers (13+16) all 9-arg ⇒ genuinely untouched — but `span_emit_test.go` IS an edit site for T4's NEW tests.
- **RD-FIXTURE / RD-DRIVER / RD-0064 / RD-REGISTER:** `0115` = 5 files, NO `scripts/`; route metadata `envoy.yaml:92-95` / `envoy-go.yaml:91-94`; custom_tags `:66-84` / `:65-83`; reference `type: STRICT_DNS` `:105-116`, subject `type: STATIC` `:103-112`; **both ALREADY single-endpoint**. `metadata:` inserts as a SIBLING of `endpoint:` (16-space indent) after `envoy.yaml:116` / `envoy-go.yaml:112`. `spanAttrMap` `driver.go:576-582` is a key→value MAP; `assertAttrString` `:594-605`; `AssertStats` invocation `:432-433`; `refListenerPort = 10115` `:93`. ⚠️ **`0064-lb-subset` has NO YAML files — it is DRIVER-GENERATED** (`driver.go:206-226` reference / `:264-283` subject), **and its precedent covers `envoy.lb` ONLY** — namespace-generality rests on P2's probe. Registration line goes at `runner_test.go:143`.
- **RD-IDENT:** `kindMetadataHost` / `hostMetaLookup` / `filterMetadata` / word-boundary `MetaLookup` ⇒ **0 each**. The naive `grep MetaLookup` returns **51**, decomposing exactly as 22 `routeMetaLookup` + 27 `RouteMetaLookup` + 2 `TestFilterChain_RouteMetaLookup`.
- **RD-MOD / RD-LAYERING:** +0 modules — the ONLY new import repo-wide is `structpb` into `cluster.go` (existing module). `go list -deps ./internal/tracing | grep -E 'internal/(filter|cluster)'` ⇒ EMPTY. ⚠️ **The closure is a LAYERING rule, NOT a cycle guard** — `internal/cluster` does not depend on `internal/tracing` (re-executed), so a direct import would build clean. **ADR-0294's landed §Context still asserts the refuted "cycle guard" justification — T9 must correct it (F4).**
- **RD-RACEFLAKE:** `TestOutlierDetector_ConcurrentEjectExactlyOnce` `outlier_test.go:744-768`, assertion `:766` — PRE-EXISTING intermittent `-race` failure on the UNMODIFIED baseline; `internal/cluster` is newly touched ⇒ newly load-bearing at T8. Isolate-re-run; do NOT re-classify.
- **RD-BASELINE:** fixtures **117** · fuzzers **55** · BackendKind tail **38** (`fixture.go:614`) · DECISIONS tail **ADR-0294** PROPOSED (next-free ADR-0295) · `0116` absent, port **10116** free (0 hits) · `go build ./...` EXIT 0 · `go test ./internal/tracing/ -count=1` **ok** at baseline.

### New findings the SPEC does NOT carry (folded into the tasks)

- **F1 (MODERATE)** — two stale doc comments in `config_test.go` (`:627-629`, `:754-757`) asserting HOST rejects ⇒ **T2 narrows both to CLUSTER-only**.
- **F2 (MODERATE)** — **SEVEN** stale comment lines in FOUR groups in PRODUCTION `config.go` (`:48-51`, `:69`, `:73`/`:74`/`:75`/`:76`, `:178-183` — especially **`:182`** `// metadata CLUSTER/HOST/unset kinds reject LOUDLY`) ⇒ **T2 updates all six**. SPEC §11 scopes config.go to the const + the accept arm only.
- **F3 (MINOR)** — a THIRD, uncited unset-kind regression guard at `config_test.go:460-464` ⇒ **T2 verifies it still passes; no edit.**
- **F4 (MINOR)** — ADR-0294's §Context carries the "cycle guard" justification its own SPEC refuted ⇒ **T9 obligation.**
- **F5 (MINOR, two parts)** — (a) SPEC §3.2's compressed skeleton is **not gofmt-clean**; the PLAN carries the byte-accurate multi-line forms. (b) **`structpb.NewStructValue(nil)` is non-nil (EXECUTED), but the nil guard is NOT observably load-bearing through `ResolveCustomTags`** — `descend`'s first hop returns `(nil,false)` without panicking and `MetaPath` is min-1 by parse validation ⇒ **T1's guard break must assert on `MetaLookup`'s OWN return, or it is VACUOUS.**
