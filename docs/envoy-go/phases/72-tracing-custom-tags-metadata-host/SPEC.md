# SPEC 72 — tracing `custom_tags` `metadata` type, `HOST` MetadataKind only (the EIGHTEENTH Observability-family row + the SIXTH tracing `custom_tags` row + the THIRD `metadata` MetadataKind; lifts the `tracing: custom_tags metadata tag %q host kind unsupported` reject at `internal/tracing/config.go:267-268` and resolves the SELECTED UPSTREAM ENDPOINT's metadata out of `lb_endpoints[].metadata.filter_metadata[ns]` — over a NEW `filterMetadata` retention field on `cluster.Endpoint`, populated beside the phase-38 `envoy.lb` scalar projection — onto the ingress span via the shared `ResolveCustomTags`→`Span.Attrs []KV` seam [both OTLP + Zipkin]; `CLUSTER`/unset-kind reject loudly with distinct substrings [envoy-go-strict, ADR-0080]; a SINGLE FLAT ROW; ONE new OTLP fixture `0116`)

> **Stage:** SPEC (lifecycle-state 1 → 2). Docs-only — ZERO production `.go`, apart from the ADR-0294 §Context append to `DECISIONS.md`. Worktree `.worktrees/phase-72-spec`, branch `phase-72-tracing-custom-tags-metadata-host-spec`, tip `0150b04c` (the phase-72 BRAINSTORM squash + its router follow-up), per `feedback_git_worktrees`.
>
> **Row 72 STAYS `in-progress`** — the IMPL flips it `done` at its six-gate (ADR-0106, the SOLE leg — a SINGLE FLAT ROW, §10). **ADR-0294's §Context DRAFTS HERE** (ADR-0044-as-used); §Decision/§Consequences land at the IMPL IN PLACE. **The DECISIONS tail flips ADR-0293 → ADR-0294 AT THIS SPEC COMMIT** (§14) — the §Context append IS the tail flip; next-free becomes ADR-0295.
>
> **Evidence base — code RE-DERIVED at `0150b04c` this SPEC** (an independent read-only re-derivation agent re-opened EVERY anchor in BRAINSTORM §6/§11 + the go-control-plane `CustomTag_Metadata`/`MetadataKind`/`MetadataKey`/`corev3.Metadata` proto types + their PGV `.validate.go` rules + the `Endpoint` consumer census + endpoint provenance through ALL THREE dispatch paths + the fuzz host + the collision greps + the 18-site emit roster, and **BUILT the row's full claimed shape in scratch** — `go build ./...` EXIT 0, `go mod tidy -diff` EMPTY, the cycle guard clean, exactly the two predicted test failures) **+ SEVEN EXECUTED live-probe arms this SPEC** (the D-CTMH docket — `envoyproxy/envoy:contrib-v1.37.2`, a FRESH container per arm, bridge networks, the host-gateway literal + `dns_lookup_family: V4_ONLY`, a standalone host OTLP-gRPC receiver + a Zipkin v2 JSON collector, export-actually-ran asserted per arm — 7 numbered conclusions "P1..P7"). Claims below are attributed **EXECUTED** (a P-arm conclusion, named) or **RE-DERIVED** (a declaration reading with `file:line` at `0150b04c`); a reading is never presented as a probe. **RE-DERIVE this document; do not execute it** (`feedback_brief_citations_not_evidence`, `reference_quoting_is_not_executing`).
>
> **Baselines re-verified mechanically at `0150b04c`:** fixtures **117** (`ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0115-tracing-custom-tags-metadata-route` ⇒ next fixture `0116`, and reference port `10116` has ZERO hits under `test/` — free) · fuzzers **55** (`grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`) · DECISIONS tail **ADR-0293** (COMPLETE; next-free **ADR-0294**; `grep -c '^## ADR-0294'` → 0, FREE) · BackendKind tail **38** (`H2GoawayResponder`; note the tracked figure is the TAIL VALUE — the enum has 39 constants, 0..38) · stat surface **1201** · go.mod modules **2** (lineage figure; `corev3`/`structpb`/`type/metadata/v3`/`protojson` all resolve at existing directs).
>
> **Sentinel expectation at this stage:** check (1) prints `NOT DONE: row 72`; checks (2)+(3) unchanged (three live `candidates:` sentences; `NEVER OPENED: gRPC/Runtime/WASM`). **No deferred sentence is edited at ANY pre-IMPL stage of this row** — the `HOST` MetadataKind is NAMED in the live Observability `candidates:` sentence and rolls OUT at the phase-72 IMPL row-done edit, narrowing it to `CLUSTER`-only, per the verified phase-57 precedent (§12).

---

## 1. Purpose / Mission

Phase 46 built the HCM-native tracing engine — the per-request ingress SERVER `Span` with its built-in STRING attributes, exported over BOTH the OTLP and Zipkin transports through the shared provider-neutral `Span.Attrs []KV` seam (ADR-0260/0261). Phases 59/62/63 added the first three `custom_tags` SOURCE types (`literal` ADR-0277, `request_header` ADR-0283, `environment` ADR-0284). Phase 70 (ADR-0292) added the FOURTH and LAST SOURCE type — the `metadata` type — scoped to the **`REQUEST`** MetadataKind (the per-request dynamic-metadata `Bucket`); phase 71 (ADR-0293) added the **`ROUTE`** MetadataKind (the matched route's STATIC config metadata over `FilterChain.RouteMetaLookup`).

Phase 72 lands the **THIRD `metadata` MetadataKind — `HOST`**. It lifts the `tracing: custom_tags metadata tag %q host kind unsupported` reject (`config.go:267-268`, RE-DERIVED) and resolves an operator-configured `{tag, metadata:{kind:HOST, metadata_key:{key:<ns>, path:[<seg>...]}, default_value:<fb>}}` out of the **SELECTED UPSTREAM ENDPOINT's static metadata** — `lb_endpoints[].metadata.filter_metadata[ns] → *structpb.Struct` — walks the `MetadataKey.path`, serializes the resolved `structpb.Value` to a STRING, and emits it as a `{tag, value}` span attribute on BOTH exporters. `CLUSTER` and the unset kind continue to reject loudly with distinct substrings (envoy-go-strict, ADR-0080 — the reference ACCEPTS them, proved by P1).

**The row's central cost claim, EXECUTION-CONFIRMED (not cited).** Unlike phases 70 and 71 — each of which had to transport a new source to the emit seam and paid a threading tax across all 18 emit callers — the source is ALREADY a parameter of all three emit functions (`picked cluster.Endpoint`, the 4th parameter at `accesslog_emit.go:27`/`:87`/`:149`, RE-DERIVED), and the three `ResolveCustomTags` call sites (`:57`/`:118`/`:179`) sit INSIDE those same functions. The re-derivation agent built the full row in scratch and proved by sha256 that **`connection.go`, `h2dispatch.go`, `h3dispatch.go`, `internal/filter/http/chain.go`, `accesslog_emit_test.go` and `span_emit_test.go` are BYTE-IDENTICAL to baseline** — the 18 production emit callers and the 29 emit test callers are mechanically untouched, and `chain.go` is not touched at all (contrast phase 70's `DynamicMetadata()` and phase 71's `RouteMetaLookup()` adds).

The row composes on landed substrate: **ZERO new packages, +0 go.mod modules, +0 stats, +0 BackendKinds, +0 fuzzers**. `descend` (`resolve.go:128`) and `structpbValueToString` (`resolve.go:151`) are REUSED VERBATIM. Its ONE genuinely new blast-radius surface is `internal/cluster` (§3.5).

### 1.1 BRAINSTORM anticipations — what HELD, what this SPEC SHARPENS *(the drift ledger; drift found is the point)*

**HELD (re-derived / EXECUTED-probed this session):** the HOST reject arm `config.go:267-268` and its verbatim message + the ROUTE clone source `:248-264` + the CLUSTER reject `:265-266` + both unset-kind rejects `:229-230`/`:269-270` — all six arms EXACT with their verbatim error strings. The `k := md.GetKind()` bind at `:227` with all four kind-getters on `*metadatav3.MetadataKind` (the phase-70 V1 SEVERE receiver fact — `md.GetHost()` does not exist on `*CustomTag_Metadata`, whose ONLY getters are `GetKind`/`GetMetadataKey`/`GetDefaultValue`). The `customTagKind` block `:54-60` with 5 constants at iota 0-4 ⇒ an APPENDED `kindMetadataHost` lands at **iota == 5** (execution-confirmed — the scratch build's failing tests printed `Kind:5`). `CustomTagSpec` `:67-77` with `MetaNamespace`/`MetaPath`/`DefaultValue`/`HasDefault` all present ⇒ NO new field. `ResolveCustomTags` `:32` at exactly 4 params. The `[1:]` at `:84`, REQUEST-only. `descend` `:128-142` + `structpbValueToString` `:151-168`, verbatim-reusable. `Endpoint` `:43-75`, `Addr()` `:81-86` (precomputed), the non-comparability comment `:88-91`, `IsZero()` `:92`, `PooledH1Conn.Endpoint()` `:106`; `cluster.go` imports NEITHER `corev3` NOR `structpb`. `manager.go:883-884` + `defaultSubset` at `:754`. `ScalarsFromStruct` `:263-285` and its exact drop set. `withMetaTags` `:97-115` with `meta_bad` on CLUSTER ⇒ **no repoint needed**, and its stale doc sentence at `:92-93`. The two `config_test.go` rows to flip (`:639`/`:765`) and their assertions (`:648`/`:774`) — under the scratch build these were the **ONLY two failures across 70 packages**. The 33-vs-32 `resolve_test.go` caller count with the single string literal at `:106`. The 5+6+7=18 emit roster with every line number. Endpoint provenance through both pools is CLEAN. Collision greps `kindMetadataHost`/`hostMetaLookup`/`RawMetadata` ⇒ **0**. +0 stats/packages/modules/fuzzers, SINGLE FLAT ROW — all HELD.

**SHARPENED — each flagged where it lands:**

| # | BRAINSTORM said | Sharpened to | § |
|---|---|---|---|
| **S1** | **D-CTMH-ENDPOINT-METADATA:** retain "the whole `*corev3.Metadata`, or a `map[string]*structpb.Struct`, or a lazily-built per-namespace Value" — shape open (§2.5) | **DECIDED: a `filterMetadata map[string]*structpb.Struct` field ALIASING `lbe.GetMetadata().GetFilterMetadata()`.** P2 EXECUTED the load-bearing question and settled it: **`envoy.lb` is NOT privileged — ANY `filter_metadata` namespace is addressable**, so the phase-38 `envoy.lb`-scalars-only projection is genuinely insufficient and ALL namespaces must be retained. Shape chosen over `*corev3.Metadata` because it adds **ONE** import to `cluster.go` (`structpb`) rather than two, and states intent exactly: a `MetadataKey` can only ever address `filter_metadata`, never `typed_filter_metadata` (`base.pb.go:819`/`:826`). Both shapes were BUILT and compile clean; both measure `unsafe.Sizeof(Endpoint)` **104 → 112 B** and both ALIAS the already-parsed proto map ⇒ **zero new allocation per endpoint**. | §3.5 |
| **S2** | **D-CTMH-RESOLVE-SEAM:** an `Endpoint`-side accessor is "the tidier option — **it also localizes the `structpb` import**"; and a method value heap-allocates the wide receiver, so there are "**two cheaper shapes**" | **BOTH PREMISES FALSE; the conclusion survives on other grounds.** (a) `internal/filter/hcm/accesslog_emit.go` **ALREADY imports `structpb` at `:9`** — the phase-71 S2 import-centralization rationale does NOT transfer, and **no candidate shape adds an import there**. (b) MEASURED in a faithful emit shape (`testing.AllocsPerRun` + `-benchmem`): the method value, the inline closure, and the package-level adapter are **all exactly 1 alloc / 128 B** (the ns figures are machine-dependent and deliberately not pinned) — Go captures the read-only `picked` BY VALUE in the inline case too — while the *pointer*-receiver variant is **2 allocs, strictly worse**. The "faithful emit shape" qualifier is load-bearing and V1 confirmed it by execution: in a loop-hoisted micro-benchmark the pointer variant measures 1 alloc / 16 B, i.e. *better* — the realistic shape inverts it because `picked` must then escape too. There is no cheaper shape; pick on readability. **DECISION: the `Endpoint`-side accessor passed as a method value.** Also note what the closure is NOT justified by: V1 EXECUTED the counterfactual and a direct `internal/cluster` import into `internal/tracing` **builds clean** — there is no import cycle. The closure preserves a self-imposed LAYERING rule (§3.3), which is a good reason; "the cycle guard forces it" would have been a false one. | §3.3 |
| **S3** | **D-CTMH-NOHOST:** "10 of 18 emit sites pass a zero `cluster.Endpoint{}`… anticipated: treat as namespace-absent ⇒ the default/omit rule" | **WIDENS TWICE, and crossing the code with P6 yields a GENUINE DIVERGENCE — the row's most consequential finding.** (a) The 10/8 split is exact syntactically, but `h2dispatch.go:313`'s `picked` is the return of `directResponseAction.asRouterActionH2()`, which returns the literal `cluster.Endpoint{}` (`actions.go:169`) ⇒ 11 of 18 are always zero syntactically — but the tracing-relevant partition is SPAN-CAPABILITY (§11): **6 of the 18 are PRE-Decide and can never emit a span at all**, leaving **12 span-capable = 7 real-`picked` + 5 zero**. (b) Even those 7 are zero on EVERY failure sub-path: `router.go` initialises `picked := cluster.Endpoint{}` and assigns only after `AcquireH1` succeeds, so circuit-breaker 503, pool-overflow 503 and connect-failure (H1 503 / H2 502) all return the ZERO endpoint — **including the connect-failure branch guarded by `if !ep.IsZero()`, where a host demonstrably WAS picked**; `router_h2.go` is identical. (c) **P6 EXECUTED the reference and proved it resolves at LOAD-BALANCER PICK TIME, not on upstream success** — with a dead endpoint (connect refused → 503 `UF`) the reference emitted that picked-but-unreachable endpoint's OWN metadata value. ⇒ envoy-go is PARITY on the no-host classes (404/`NR`, local reply, zero-endpoint cluster/`UH` — all EXECUTED in P6) but **DIVERGES on the picked-then-failed classes**. (d) The divergence is **PRE-EXISTING**: `picked` reaches no span attribute today, and its sole current consumer — the access-log upstream-host field — is ALREADY empty on those paths, pinned by three landed UNIT tests and by NO differential fixture. Scoped as a NAMED DEPARTURE + a deferred candidate, NOT fixed here (§3.6). | §3.6 |
| **S4** | **D-CTMH-VALUE-SERIALIZE:** anticipated the phase-70 P3 table exactly, so `structpbValueToString` reuses VERBATIM | **CONFIRMED for every case phases 70/71 pinned (P3) — reuse is correct — but THREE boundaries are newly QUANTIFIED and one bites the fixture.** (i) a TOP-LEVEL SCALAR number renders at ~6 significant digits on the reference (`9007199254740992`→`9.0072e+15`, `1e20`→`1e+20`, `1.5e-7`→`1.5e-07`) where Go's `protojson` is full-precision; (ii) the reference uses a DIFFERENT renderer for numbers NESTED in a struct/list, so scalar and nested numbers diverge from each other WITHIN the reference; (iii) **a struct value with 2+ keys serializes in an ARBITRARY key order** (three orders observed for one 3-key struct; reproduced across two fresh processes) where Go always SORTS ⇒ **a multi-key struct-valued tag is NOT cross-side comparable and the `0116` fixture must assert STRING values only.** All three are PRE-EXISTING (the same reference code path serves REQUEST/ROUTE) and none is HOST-specific. Plus a fixture-authoring trap: Envoy's **YAML** loader turns any non-integer scalar into a STRING, so an `envoy.yaml` fixture CANNOT express a fractional metadata number. | §3.4, §8 |
| **S5** | **D-CTMH-ZIPKIN-WIRE:** anticipated identical to OTLP; the probe IS the Zipkin evidence since the row ships no Zipkin fixture | **Identical for strings, integers, doubles, bools, lists, embedded quotes/backslashes, the `null`→default fall-through and the whole default/omit rule — with ONE EXECUTED DIVERGENCE: the reference's Zipkin serializer DROPS a tag whose resolved value is the EMPTY STRING** from the `tags` map entirely, where OTLP emits an explicit `""`. Mechanically confirmed against the raw v2 JSON bodies over three fresh processes; the SAME drop hits the empty BUILT-INS `node_id`/`zone` ⇒ it is a property of the reference's Zipkin serializer, not of the custom-tag seam, and it is **PRE-EXISTING — it equally affects the landed REQUEST/ROUTE kinds.** Recorded as a documented boundary; NOT fixed here. | §3.4 |
| **S6** | **D-CTMH-FUZZSEED:** purely additive, no repoint | **CONFIRMED exactly** — `meta_bad` is on CLUSTER (`fuzz_test.go:110-112`) and NO seed points at HOST, so the arm this row removes is unexercised by any seed. Add `meta_host_ok`; leave `meta_bad`. **Plus an obligation the BRAINSTORM named and this SPEC pins:** the `withMetaTags` doc comment at `:92-93` asserts "CLUSTER/HOST stay envoy-go-strict departures" and becomes FALSE the moment the accept lands — narrow it to CLUSTER-only in the same task. | §7 |
| **S7** | *(unraised)* | **A PRE-EXISTING `-race` FLAKE SITS ON THE PACKAGE THIS ROW NEWLY TOUCHES.** `TestOutlierDetector_ConcurrentEjectExactlyOnce` (`internal/cluster/outlier_test.go:744-768`, assertion `:766`) fails intermittently under `-race`, **reproduced on the UNMODIFIED baseline** at `-count=400`. It is NOT phase-72's defect (the added field is never read by the outlier path), but phase 72 is the first row whose six-gate makes `-race ./internal/cluster/` load-bearing. Recorded as a known flake with an isolate-re-run instruction, matching `reference_0061_ring_hash_spread_flake` / `reference_sds_init_fetch_timeout_dial_budget_flake`. | §10 |
| **S8** | *(unraised by the BRAINSTORM, the router, or phases 70/71)* | **`LocalityLbEndpoints.metadata` is a SECOND, DISTINCT metadata source that envoy-go retains none of — so it had to be ruled out, not assumed away.** P7 EXECUTED four discriminating arms and proved the reference resolves a HOST-kind `MetadataKey` from the **selected endpoint's own metadata ONLY**: locality-level metadata does not resolve, does not act as a fallback, loses to the endpoint value on a key collision, and does not union. **⇒ NO departure is owed, and the S1 retention shape needs no locality merge.** Had it merged, the row's retention design would have been wrong. | §3.7 |

**Cosmetic drift corrected (three, none code-blocking):** the `kindMetadataRoute` resolve arm is `resolve.go:95-116`, not `:95-118` (`:117`/`:118` close the `switch`/`for`); `p.ep` is stashed at `cluster.go:710`, not `:709` (`:709` is the `Br:` field), and the pool-HIT return of `p.ep` at `:690` is load-bearing but was uncited; `pooledH2Conn` spans `h2pool.go:31-34`, not `:31-33`. Additionally, **the phase-71 SPEC's own cites are now STALE** (its first-wins dedup `:257-262` is `:275-279` at tip; its `descend`/`structpbValueToString` `:100`/`:123` are `:128`/`:151`) — flagged so this SPEC's successors copy from HERE, not from 71.

### 1.2 SPEC-time verification record

The read-only re-derivation agent (tree at `0150b04c`) re-opened every BRAINSTORM §6/§11 anchor plus the proto/PGV/consumer-census/provenance/collision anchors, and **BUILT the row's full claimed shape in private scratch**: `go build ./...` EXIT 0 · `go vet` EXIT 0 · `gofmt -l` silent · `golangci-lint` EXIT 0 on all three touched packages · the full `./internal/... ./cmd/... ./validate/...` suite ran **70 packages, of which exactly ONE failed, carrying exactly TWO failing subtests — the two predicted `/host` reject rows** · `go mod tidy -diff` EMPTY ⇒ +0 modules · `go list -deps ./internal/tracing | grep -E 'internal/(filter|cluster)'` EMPTY ⇒ the cycle guard holds · exactly SIX files differ (five production + `resolve_test.go`) · and sha256 proof that — **in that scratch build** — the 18 emit callers, the 29 emit test callers, `chain.go`, `subset.go`, `config_test.go` and `fuzz_test.go` are byte-untouched. (`config_test.go`/`fuzz_test.go` are genuine edit sites for the ROW, §11; the probe deliberately left them alone, which is exactly why the failure set was the two predicted `/host` subtests and nothing else.) Standalone in-package execution additionally proved the resolve semantics — nested and single-segment descent off the wrapped namespace struct, absent-namespace and unresolvable-segment → default/omit, `hostMetaLookup == nil` tolerance, HOST-arm ≡ ROUTE-arm on an identical source (the FULL-`MetaPath` rule confirmed for HOST), and **a ZERO-VALUE `cluster.Endpoint{}` lookup returning `(nil,false)` without panicking** — the S3 majority arm.

**Seven live-probe arms EXECUTED** against `contrib-v1.37.2` (fresh container per arm, bridge network, host-gateway literal + `dns_lookup_family: V4_ONLY`, export-actually-ran asserted per arm, every arm built to DISCRIMINATE between competing hypotheses): P1 accept/reject roster, P2 wire + namespace addressability, P3 serialization, P4 default/omit, P5 Zipkin, P6 no-host, P7 locality-metadata merge. **Adversarial-pass record:** TWO independent verifiers ran against the draft in PRIVATE scratch before landing (`reference_parallel_subagents_private_scratch`) — recorded at §16.

---

## 2. Non-purposes (deferred; per BRAINSTORM §1.2 + §8)

NOT the `CLUSTER` MetadataKind (stays reject-only — §3.6; the LAST of the four, MODERATE ~11-13 tasks, the natural phase-73 candidate). NOT the fully-unset-kind acceptance (both sub-cases stay reject — §3.6). NOT any other tracing follow-on (`spawn_upstream_span` / OTel `http_service` / `sampler` / `resource_detectors` / `verbose` / force-trace). NOT the downstream TLS handshake-outcome `ssl` stat family (ADR-0286 C3). NOT a separate Zipkin fixture (the `Span.Attrs` seam is shared and P5 proved the Zipkin emit LIVE — the 59/62/63/70/71 single-OTLP-dir precedent). NOT any new provider/transport/stat/package/module/BackendKind. **NOT the pick-time-vs-success-time `picked` propagation** (S3(c) — a named departure here, forwarded to §13 as a newly-surfaced deferred candidate, because it is a router-seam change whose blast radius reaches the access-log `upstream_host` field, not a tracing change). **NOT a fix for the Zipkin empty-string drop** (S5 — pre-existing, equally affecting the landed REQUEST/ROUTE kinds; a documented boundary). **NOT a fix for the number-precision / struct-key-order serialization boundaries** (S4 — pre-existing, carried from ADR-0292). NOT a change to the phase-38 `envoy.lb` scalar projection, `ScalarsFromStruct`, `defaultSubset`, or the subset LB (all byte-unchanged — §3.5). NOT a change to `internal/dynamicmetadata` or `internal/filter/http/chain.go` (byte-untouched — the source reaches the seam via `picked`, not via the chain).

---

## 3. The change — the D-CTMH-* docket disposed one-for-one

### 3.1 D-CTMH-SCOPE / D-CTMH-PROTO — the `HOST`-only slice on the exact v1.32.4 proto *(RE-DERIVED; P1 EXECUTED)*

Module **`github.com/envoyproxy/go-control-plane/envoy v1.32.4`**. `CustomTag.GetMetadata()` returns `*CustomTag_Metadata{Kind *metadatav3.MetadataKind (field 1), MetadataKey *metadatav3.MetadataKey (field 2), DefaultValue string (field 3)}` (`type/tracing/v3/custom_tag.pb.go:326-339`), whose **ONLY getters are `GetKind()`/`GetMetadataKey()`/`GetDefaultValue()`** (`:373`/`:380`/`:387`). The four kind-getters live on `*MetadataKind` — `GetRequest()` `:164`, `GetRoute()` `:171`, `GetCluster()` `:178`, **`GetHost()` `:185`** (`type/metadata/v3/metadata.pb.go`) — so the arm branches on **`k.GetHost()` on the bound `k := md.GetKind()`** (`config.go:227`); **`md.GetHost()` DOES NOT EXIST and does not compile.** This is the phase-70 V1 SEVERE, RE-CONFIRMED. `MetadataKey.GetKey()` is the namespace, `GetPath()` is `[]*MetadataKey_PathSegment`, each KEY-only. `corev3.Metadata.FilterMetadata` is `map[string]*structpb.Struct` (`config/core/v3/base.pb.go:819`) and `GetFilterMetadata()` is **nil-receiver-safe** (`:861-866`). All in the direct `go-control-plane/envoy v1.32.4` ⇒ **+0 go.mod modules**.

### 3.2 D-CTMH-CONFIG-KIND / the accept arm — a `kindMetadataHost` const + a HOST accept cloning ROUTE *(RE-DERIVED; build-probed)*

`config.go`: **APPEND** `kindMetadataHost` to the `customTagKind` iota block (`:54-60`) **AFTER `kindMetadataRoute`** ⇒ **iota == 5** (execution-confirmed). **APPEND, never INSERT before `kindMetadataRoute`.** Honesty about why: V1 executed a renumber and it recompiles consistently — nothing breaks today, because the constants are unexported, never serialized and never compared across builds. So this is HYGIENE, not a live hazard; follow it so it never becomes one. Collision-checked → 0 hits repo-wide (and note a naive `grep MetaLookup` returns 51 false positives that are all `RouteMetaLookup`; use the word-boundary form). REUSE the `CustomTagSpec` `MetaNamespace`/`MetaPath`/`DefaultValue`/`HasDefault` fields — **NO new field**.

The HOST arm REPLACES the `:267-268` reject, cloning the ROUTE accept (`:248-264`) byte-for-byte except the kind:
```
case k.GetHost() != nil:
    mk := md.GetMetadataKey()
    if mk.GetKey() == "" { return nil, fmt.Errorf("tracing: custom_tags metadata tag %q empty namespace", tag) }
    if len(mk.GetPath()) == 0 { return nil, fmt.Errorf("tracing: custom_tags metadata tag %q empty path", tag) }
    path := make([]string, 0, len(mk.GetPath()))
    for _, seg := range mk.GetPath() {
        if seg.GetKey() == "" { return nil, fmt.Errorf("tracing: custom_tags metadata tag %q empty path segment", tag) }
        path = append(path, seg.GetKey())
    }
    dv := md.GetDefaultValue()
    spec = CustomTagSpec{Key: tag, Kind: kindMetadataHost, MetaNamespace: mk.GetKey(), MetaPath: path, DefaultValue: dv, HasDefault: dv != ""}
```
`MetaPath` carries the FULL path (no `[1:]` — that is a REQUEST-only resolve-time artifact). config.go adds **ZERO imports** (`k.GetHost()` is a method call on the inferred `k`). The CLUSTER reject (`:265-266`) and both unset-kind rejects (`:229-230`, `:269-270`) are UNCHANGED; the first-wins dedup (`:275-279`) is UNCHANGED. The three structural rejects are **PGV-PARITY**, EXECUTED per-arm at P1 and cross-checked against the `.validate.go`: empty key → `MetadataKeyValidationError.Key: value length must be at least 1 characters`; absent **and** zero-length `path` → the IDENTICAL `MetadataKeyValidationError.Path: value must contain at least 1 item(s)`; empty segment → `PathSegmentValidationError.Key: …`.

### 3.3 D-CTMH-RESOLVE-SEAM + the resolve arm — a FIFTH nil-tolerant param, the closure built LOCALLY *(the S2 decision; RE-DERIVED + benchmark-EXECUTED)*

`ResolveCustomTags` (`resolve.go:32`, currently 4 params) grows a **FIFTH nil-tolerant param `hostMetaLookup func(ns string) (*structpb.Value, bool)`** — shaped identically to `routeMetaLookup` (one-arg: namespace → the whole namespace struct wrapped as a `StructValue`), appended after it; the REQUEST two-arg `metaLookup` and the ROUTE `routeMetaLookup` are UNTOUCHED. The `kindMetadataHost` arm clones the landed `kindMetadataRoute` arm (`:95-116`):
```
var v *structpb.Value; var ok bool
if hostMetaLookup != nil { v, ok = hostMetaLookup(s.MetaNamespace) }
if ok { v, ok = descend(v, s.MetaPath) }        // the FULL MetaPath, NOT [1:]
```
then the shared default rule. `descend` + `structpbValueToString` REUSED VERBATIM; **NO new import**; `internal/tracing` stays filter-free AND cluster-free (cycle guard EXECUTED clean).

**Where the closure is built — the whole threading story.** NOT a new parameter on the three `emitAccessLog*` helpers: it is constructed LOCALLY inside each helper from the already-present `picked cluster.Endpoint` and passed as the 5th `ResolveCustomTags` argument at `accesslog_emit.go:57`/`:118`/`:179`. **The three signatures and all 18 callers are UNCHANGED** (sha256-proven). **DECISION: an `Endpoint`-side accessor method passed as a method value** — `picked.MetaLookup`. Per S2 this costs exactly what every alternative costs (1 alloc / 128 B, MEASURED); it is chosen for readability, for keeping the nil-safety in one place beside the field it reads, and for symmetry with the `chain.RouteMetaLookup` / `chain.DynamicMetadata().Get` values already threaded at those same three lines. The closure is a **self-imposed LAYERING rule, not a compiler constraint** — `internal/tracing` stays filter- and cluster-free by discipline. V1 EXECUTED the counterfactual: adding an `internal/cluster` import to `internal/tracing` **builds clean** (`internal/cluster` does not import `internal/tracing`, directly or transitively), so there is NO import cycle to prevent. Keep the closure anyway — it preserves the layering the `go list -deps` check asserts (§4) — but do not justify it as a cycle.

**A cost this SPEC records rather than hides:** the argument is evaluated unconditionally inside the sample block, before `ResolveCustomTags`'s `len(specs)==0` early return, so **every sampled span gains +1 alloc / +128 B even when no `custom_tags` are configured**. ACCEPTED (it matches exactly how `metaLookup`/`routeMetaLookup` are already threaded, and a sampled span already allocates ~16 attributes); the mitigation — gating behind a parse-time `hasHostTags bool` — is named here so a future profiling row can take it without re-deriving.

### 3.4 D-CTMH-VALUE-SERIALIZE + D-CTMH-DEFAULT + D-CTMH-WIRE + D-CTMH-ZIPKIN-WIRE *(EXECUTED P2/P3/P4/P5)*

**Wire (P2).** The attribute key is the **LITERAL `tag` name** (not namespaced); the value is the serialized string; ALL custom tags sit **strictly AFTER** the 16 built-ins; **intra-custom-tag order is an INTERNAL iteration order, NOT config order** (config `z,a,m,q` emitted `q,m,z,a`) ⇒ assertions MUST be key-based (envoy-go's `upsertAttr`/`Span.Attrs` already are).

**Default rule (P4), identical to the `request_header` rule phases 70/71 landed:** a resolved value WINS over `default_value` — **including a resolved EMPTY STRING, which emits `""` and does NOT fall through to a configured non-empty default**; an absent namespace, an unresolvable FIRST segment, and an unresolvable LATER segment all → `default_value` when non-empty, **OMITTED entirely** when not. Nested multi-segment paths walk. `HasDefault = DefaultValue != ""` is consistent with every observation.

**Serialization (P3).** MATCHES the phase-70/71 table for every case those phases pinned ⇒ **reusing `structpbValueToString` VERBATIM is correct**: string→raw unquoted · present-empty→`""` emitted · bool→`true`/`false` · small/integral/short-decimal numbers→plain decimal · struct/list→compact JSON with no whitespace · terminal `null`→UNRESOLVABLE (default-or-omit, matching envoy-go's `("",false)` — a boundary phase 70 left untested, now EXECUTED-pinned) · nested `null`→JSON `null` inside the value · unresolvable segment / absent key / descend-into-a-scalar→default-or-omit. The three newly-quantified boundaries (S4) are documented, PRE-EXISTING, and constrain the fixture, not the code.

**Zipkin (P5).** The tag appears in the Zipkin v2 `tags` map under the literal name as a JSON string, coexisting with the built-ins, byte-identical to OTLP for strings/ints/doubles/bools/lists/embedded quotes+backslashes and for the whole default/omit rule. **ONE divergence: an EMPTY-STRING tag value is DROPPED from the Zipkin map, where OTLP emits `""`** — the same drop hits the empty built-ins `node_id`/`zone`, so it is a Zipkin-serializer property, PRE-EXISTING, and equally affects the landed REQUEST/ROUTE kinds. Recorded as a documented boundary (§9 B3); envoy-go's shared `Attrs` seam emits on both exporters uniformly and is NOT changed by this row.

### 3.5 D-CTMH-ENDPOINT-METADATA — the ONE genuinely new blast-radius surface *(the S1 decision; RE-DERIVED + build-probed; P2/P7 EXECUTED)*

**The gap, RE-DERIVED.** `Endpoint.Metadata` (`cluster.go:49`) retains only the phase-38 projection — the **`envoy.lb` namespace ONLY, scalars ONLY** — populated at `manager.go:883` via `ScalarsFromStruct` (`subset.go:263-285`), which routes `Value_StructValue`/`Value_ListValue`/`Value_NullValue`/nil into a DISCARDED `nonScalar` return. **P2 proved this insufficient by execution**: any `filter_metadata` namespace is addressable, and a HOST tag must also walk NESTED paths — exactly the `StructValue` kind the projection drops.

**DECISION (S1): ADD `filterMetadata map[string]*structpb.Struct` to `Endpoint`, populated beside the UNCHANGED projection** at `manager.go:884` from `lbe.GetMetadata().GetFilterMetadata()`, plus a nil-safe accessor `func (e Endpoint) MetaLookup(ns string) (*structpb.Value, bool)` wrapping `e.filterMetadata[ns]` via `structpb.NewStructValue` and returning `(nil,false)` on a nil map / absent namespace / nil struct. **`cluster.go` gains exactly ONE import (`structpb`)** — an already-required module, so **+0 go.mod modules** (`go mod tidy -diff` EMPTY, EXECUTED). The map ALIASES the already-parsed proto map ⇒ **zero new allocation per endpoint**; `unsafe.Sizeof(Endpoint)` **104 → 112 B** (MEASURED).

**The "no ripple" claim, VERIFIED BY CODE rather than by comment.** the `addr` cache field (`cluster.go:70-74`) is precomputed at `manager.go:885` and returned by `Addr()` (`:81-86`); the health-state map is keyed on `ep.Addr()` (`health.go:72`, with five use sites at `:90`/`:96`/`:119`/`:148`/`:404`); maglev sorts/hashes `Addr()` (`maglev.go:91`/`:99`), ring_hash likewise (`ringhash.go:90`); locality/priority grouping keys on the comparable `LocalityID` / `uint32`, never on `Endpoint` (`locality.go:90-91`, `priority.go:122`). **`Endpoint` is NEVER a map key, NEVER compared with `==`, and NEVER serialized** (no `json:` tags; the only `Endpoint{}`-comparison text repo-wide is the doc comment at `:91`). `Endpoint.Metadata`'s production consumers are exactly TWO (`subset.go:163`, `:250`) and `ScalarsFromStruct` has exactly FIVE production callers, all untouched. Composite literals: **ONE** non-empty keyed literal in production (`manager.go:884`) plus 35 zero-values, and all 167 test literals are keyed ⇒ an added field is zero-valued everywhere with **zero positional breakage**. All four BRAINSTORM §2.5 residual risks (subset semantics / dial identity / locality-priority grouping / per-endpoint memory) are **NEGATIVE**.

**Envelope consequence forwarded to the PLAN:** phase 72 CANNOT assert `internal/cluster` BYTE-UNTOUCHED (phases 70/71 could). The IMPL envelope audit must instead pin the SHAPE: one added field + one added accessor + one added populate line, with `ScalarsFromStruct`, `defaultSubset` (`manager.go:754`) and the `envoy.lb` projection **byte-unchanged**.

### 3.6 D-CTMH-NOHOST + D-CTMH-REJECT — the departure/parity roster *(the S3 finding; EXECUTED P1/P6; RE-DERIVED across all 18 sites)*

**The reject roster (P1, EXECUTED per arm).** The reference ACCEPTS a `HOST` metadata tag (validate exit 0 AND a real boot) ⇒ lifting envoy-go's reject removes a genuine departure. It STILL ACCEPTS `CLUSTER` ⇒ that reject **stays a real documented departure**. The phase-71 S3 unset-kind split is RE-CONFIRMED for HOST and cross-checked against the `.validate.go`: sub-case **A** (a fully-absent `kind`) is **ACCEPTED** by the reference — `CustomTag_Metadata.validate` carries **no `required` rule on `Kind`** — ⇒ envoy-go's reject is a **DEPARTURE**; sub-case **B** (a present-but-empty `kind: {}` oneof) is **PGV-REJECTED** (the reference's C++ PGV text, verbatim from P1: `MetadataValidationError.Kind … field: "kind", reason: is required` — note the GO generated symbols are named differently, `MetadataKindValidationError` / `CustomTag_MetadataValidationError`, so do not grep for the C++ string in the Go tree) ⇒ envoy-go's reject is **PARITY**.

**The no-host behavior — a first-class contract clause, not a footnote.** All 18 emit sites were RE-ENUMERATED at tip with their `picked` argument (roster at §11). **11 of 18 are ALWAYS the zero endpoint** (the BRAINSTORM's 10 syntactic zeros plus `h2dispatch.go:313`, whose `picked` is provably the literal `cluster.Endpoint{}` from `actions.go:169`); only 7 can ever carry a real endpoint, and **even those 7 are zero on every failure sub-path** (circuit-breaker 503, pool-overflow 503, connect-failure 503 — `router.go` and `router_h2.go` both initialise `picked := cluster.Endpoint{}` and assign only after the acquire succeeds).

P6 EXECUTED the reference across five discriminating arms, with a `literal` positive-control tag proving custom-tag resolution ran on every span:

| class | reference (EXECUTED) | envoy-go (RE-DERIVED — not probed cross-side) | verdict |
|---|---|---|---|
| endpoint SELECTED, 200 | tag RESOLVES | resolves | **PARITY** |
| `direct_response` / post-Decode local reply | span emitted; tag → `default_value`, OMITTED if none | span-capable, zero endpoint ⇒ default/omit | **PARITY** |
| **host PICKED, upstream connect REFUSED (503 `UF`)** | **tag RESOLVES from the picked-but-unreachable endpoint** | zero endpoint ⇒ default/omit | **DEPARTURE** |
| no route match (404 `NR`) | span emitted; tag → default/omit | **NO SPAN AT ALL** — the three no-match sites are PRE-Decide, `traceDecision` is nil, and the span block is gated on it (`accesslog_emit.go:30`) | **N/A — a PRE-EXISTING documented departure** (`BEHAVIOR_CONTRACT.md:741`), not a HOST-tag disposition |
| cluster with ZERO endpoints (503 `UH`) | span emitted; tag → default/omit | **config BOOT-REJECTED** — `cluster: %q: zero endpoints across all locality groups` (`manager.go:890`, pinned by `manager_test.go:442-443`) | **N/A — no envoy-go counterpart**; the `0116` fixture cannot express this arm |

⇒ **the line the reference draws is "was a host PICKED", not "did the upstream succeed"** — a generalization EXECUTED for the connect-refused arm only; pool-overflow and circuit-breaker were NOT probed (see the precision note below, which is part of the claim, not a footnote to it).

**The divergence is PRE-EXISTING, not introduced by this row — established by code, not asserted.** `picked` reaches NO span attribute today: the span block builds `SpanInputs` without touching it (`UpstreamCluster: ""` at `accesslog_emit.go:51`, "not available at this seam"). Its ONLY consumer today is `upstreamHostString(picked)` → `Record.UpstreamHost` (`:72`/`:133`/`:194`), which feeds three sinks — the default text `%UPSTREAM_HOST%` (`format.go:56`, `orDash`), the OTLP `UPSTREAM_HOST` operator (`otlpformat.go:47`), and the gRPC access-log `CommonProperties.UpstreamRemoteAddress` (`mapping.go:78`, left unset when empty). **So envoy-go's access-log upstream-host field is ALREADY empty on these paths, where the reference emits the picked host** — phase 72 would merely make a HOST-kind tag the FOURTH consumer of the same pre-existing gap. Three landed UNIT tests pin the current behavior deliberately (`router_test.go:333-334` dial-failure, `:492` circuit-breaker overflow, `:597` conn-pool overflow — the first's own doc comment names "a zero picked Endpoint (UpstreamHost renders empty → the formatter's literal `-`)"), and **NO landed differential fixture pins it**: `0006-access-log` asserts `UPSTREAM_HOST` cross-side but has only direct-response and routed-success arms (no 503 arm), and `0085` deliberately EXCLUDES the operator as non-deterministic; the three fixtures that DO drive these failure classes (`0074`, `0078`, `0079`) configure no `access_log` at all.

**SCOPED OUT of row 72 deliberately** (§2, §13). The counterfactual fix is genuinely small — `ep` is already in scope and populated at the pool-overflow and connect-failure returns (`AcquireH1` returns the real `ep` at `cluster.go:697`/`:705`; `AcquireH2Stream` at `h2pool.go:372`/`:401`/`:607`), so it is ~4 lines hoisting `picked = ep` above those returns — **but it flips three access-log consumers' output on a failure path and breaks three landed unit tests, which is an access-log behavior change, not a tracing one.** It belongs in its own row, costed on its own terms. Row 72 records it as a NAMED DEPARTURE (§9 B2) and forwards it as a newly-surfaced deferred candidate.

**Precision the departure clause must keep (do not overclaim):** the divergence is **EXECUTED-PROVEN for connect-failure 503 only** (P6 arm C). Pool-overflow 503 is *structurally* the same on envoy-go's side (a host IS picked, `picked` stays zero) but the reference's behavior there was **not probed**. Circuit-breaker 503 is likely PARITY and is **structurally un-fixable** in envoy-go by the same edit — the `TryAcquireRequest` gate (`router.go:541`, `router_h2.go:79`) precedes any pick, so no `ep` exists; populating it would require reordering the pick ahead of the admission gate, with its own LB-release consequences. The reference's circuit-breaker behavior was likewise not probed. A future row taking this on owes both arms.

### 3.7 D-CTMH-FIXTURE-SOURCE / the locality-metadata question — ENDPOINT-ONLY, no departure owed *(EXECUTED P7)*

`ClusterLoadAssignment.endpoints[]` (`LocalityLbEndpoints`) carries its OWN `metadata` field, a sibling of `lb_endpoints:` and separate from each endpoint's, and **envoy-go retains NO locality-group metadata at all** — so if the reference merged the two when resolving a HOST-kind `MetadataKey`, envoy-go could not reproduce it and would owe a departure. It does not. P7 first confirmed the field is genuinely accepted at the pin (`--mode validate` OK on a bootstrap carrying metadata at BOTH levels — so this is a real capability question, not a clean "field absent" negative), then ran four DISCRIMINATING arms with the same three HOST tags configured in every arm, each with a `default_value` so an unresolved key is distinguishable from a resolved one:

| arm | metadata placement | result |
|---|---|---|
| 1 | endpoint level ONLY (positive control) | RESOLVES (`t_host=v-host-EP`) |
| 2 | locality level ONLY | **falls to `default_value`** — locality metadata is NOT a fallback source |
| 3 | the SAME key at BOTH levels, different values | **ENDPOINT value wins** (`t_both=EP-WINS`) — rules out merge-with-locality-priority |
| 4 | DIFFERENT keys, one per level, same namespace | **no union** — the locality key stays unresolved; rules out merge-with-endpoint-priority |

Only "endpoint-only" survives all four. Corroborating (non-probe) structural evidence: Envoy models the two as DISTINCT attributes, mirrored in envoy-go's own WASM property surface — `XdsUpstreamHostMetadata()` and `XdsUpstreamHostLocalityMetadata()` are separate accessors (`internal/wasm/property.go:232-233`).

**P7: a HOST-kind `MetadataKey` resolves against the selected endpoint's own `lb_endpoints[].metadata.filter_metadata` ONLY — `LocalityLbEndpoints.metadata` contributes nothing. envoy-go's decision to retain no locality-group metadata is therefore NOT a divergence and the row owes NO departure on this point**, which also confirms the S1 retention shape (`filterMetadata` sourced from `lbe.GetMetadata()`) is exactly right and needs no locality merge. The fixture uses endpoint-level metadata only. *(Narrowing caveat: the arms used a single-endpoint, single-locality cluster; additional localities cannot widen an endpoint-only conclusion.)*

---

## 4. Framework primitives — 0 new packages, 0 new go.mod modules

No new interface and no new package-level type beyond the `kindMetadataHost` const, the `hostMetaLookup` parameter, and one added field + one added accessor on `cluster.Endpoint`. Everything else (`descend`, `structpbValueToString`, `CustomTagSpec`'s metadata fields, `upstreamHostString`, `ScalarsFromStruct`, the `Span.Attrs`/both-exporter seams, the `otlptrace` receiver) is pre-existing and REUSED. `cluster.go` gains ONE import (`structpb`); `config.go`, `resolve.go` and `accesslog_emit.go` gain **none** (`accesslog_emit.go` already imports `structpb` at `:9`). All imports resolve at existing directs ⇒ **+0 go.mod modules**, EXECUTION-verified (`go mod tidy -diff` EMPTY). The layering check holds: `go list -deps ./internal/tracing` shows **no `internal/filter` and no `internal/cluster` edge** (EXECUTED). Note this is a DISCIPLINE check, not a cycle check — neither edge would cycle (V1 executed it); the landed gate has historically been documented as *filter*-free, and this row extends the same discipline to `cluster` — re-run at the IMPL with `go list -deps`, no `...` (`reference_xds_config_seam_transitive_cycle_guard`).

## 5. Identifier hygiene *(collision checks — RE-DERIVED at `0150b04c`)*

`kindMetadataHost` → **0** · `hostMetaLookup` → **0** · `MetaLookup` word-boundary (`grep -rnE '(^|[^A-Za-z])MetaLookup'`) → **0** · `filterMetadata` as an `Endpoint` field → **0**. ⚠️ A naive substring `grep MetaLookup` returns **51** hits that are ALL `RouteMetaLookup` — use the word-boundary form (`reference_spec_drafted_identifier_collision_check`). Re-run every grep at the PLAN and again at the IMPL tip.

## 6. Reject roster — added / retained

**ADDED: none.** **REMOVED: one** — the `host kind unsupported` arm (`config.go:267-268`). **RETAINED unchanged:** `cluster kind unsupported` (`:265-266`, a real DEPARTURE — P1 proved the reference boots it); `kind required` × 2 (`:229-230` sub-case A, a DEPARTURE; `:269-270` sub-case B, PARITY); the empty-namespace / empty-path / empty-path-segment rejects (PGV-PARITY, P1); the empty-`tag` reject; the first-wins dedup.

## 7. Stat surface **+0** · Fuzz **+0 fuzzers, a purely ADDITIVE seed** *(D-CTMH-STATS / D-CTMH-FUZZSEED)*

**Stats +0 → 1201.** A span attribute registers no stat; the tag key is a config literal used as a span-attribute key, not a stat name (`reference_dynamic_stat_name_charset_guard` does not apply). Per the phase-71 V2 finding, tracing rows carry **no `TestNoNewStat`-style guard obligation** — do not manufacture a cited-but-unwritten one.

**Fuzz +0 → 55.** ADD a `meta_host_ok` tag (a valid `MetadataKind_Host_` + `MetadataKey`) to `withMetaTags` (`fuzz_test.go:97-115`); **`meta_bad` STAYS on CLUSTER — no repoint** (S6: no seed points at HOST, so the arm this row removes is unexercised). `f.Add` is not `^func Fuzz` ⇒ +0. **Dispatch-verify** the HCM fuzzer actually reaches the `kindMetadataHost` accept arm (parse precedes the `provider required` check). **Same task: narrow the stale `:92-93` doc comment** from "CLUSTER/HOST stay envoy-go-strict departures" to CLUSTER-only (`reference_code_comment_not_evidence`).

## 8. Differential fixture — ONE new `0116-tracing-custom-tags-metadata-host` (OTLP) *(D-CTMH-FIXTURE — DECIDED)*

**DECISION: ONE new OTLP fixture, NO Zipkin dir** (the 59/62/63/70/71 precedent; the `Span.Attrs` seam is shared and P5 proved the Zipkin emit LIVE). Cloned from the `0115` chassis — 5 files (`driver/driver.go`, `envoy.yaml`, `envoy-go.yaml`, `expectations.yaml`, `README.md`) + a **one-line blank-import registration** at `test/differential/runner_test.go` (the driver self-registers in `init()`). `BackendKind` stays `HTTPFixedBody` (+0, tail **38**), `BackendCount` stays **1** (`reference_differential_backendcount_min_one`), NO writer (the source is static config both sides), reference port **10116** (free).

**What changes vs `0115`:** (1) the `metadata: {filter_metadata: {<ns>: {…}}}` block moves OFF the route and ONTO `clusters[c_backend].load_assignment.endpoints[0].lb_endpoints[0].metadata`, in BOTH YAMLs; (2) the two `custom_tags` flip `kind:` from `route: {}` to `host: {}`; (3) nothing else — **the cluster is ALREADY single-endpoint on both sides**, so the pick is deterministic and no LB spread enters the assertion (`reference_round_robin_offset_randomized`).

**Two HOST tags, asserted cross-side EXACT key+value, key-based** (P2: intra-tag order is internal): one resolving to a **STRING** static value, one whose namespace/path is absent falling to `default_value`. **S4 constrains this: assert STRING values only** — a multi-key struct value is NOT cross-side comparable (arbitrary reference key order) and an `envoy.yaml` cannot even express a fractional number (the YAML→`Struct` integer-or-string trap).

**Inherited cross-side asymmetry, already proven safe:** the reference `c_backend` is `type: STRICT_DNS` (host.docker.internal, ADR-0010) while envoy-go's is `type: STATIC` (envoy-go supports STATIC only), so the reference must propagate `lb_endpoints[].metadata` through DNS resolution. Landed evidence: **`0064-lb-subset` already puts `lb_endpoints[].metadata` on a `type: STRICT_DNS` reference cluster against a `type: STATIC` subject and is green in the 117-dir suite.** P2/P4 additionally read the metadata back live through exactly this shape.

**Break protocol:** commit FIRST (`reference_break_protocol_commit_first`), run breaks with `-count=1` (`reference_differential_break_protocol_count1`), use the FULL subtest selector `TestDifferential/0116-tracing-custom-tags-metadata-host` (`reference_differential_run_selector`), and confirm WHICH assertion fires per break (`reference_deliberate_break_wrong_assertion`). Minimum three breaks: the static-value swap, the default-drop, and a wrong-namespace control proving the namespace binding is load-bearing rather than a vacuous default match.

## 9. Behavior-contract edit map — pinned *(all edits land at the IMPL, atomically with ADR-0294)*

- **B1** — `BEHAVIOR_CONTRACT.md:688` (the `custom_tags` paragraph): the `metadata` type flips to CONSUMED for **`REQUEST` + `ROUTE` + `HOST`** (three of four MetadataKinds), naming the HOST source as the selected upstream endpoint's `lb_endpoints[].metadata.filter_metadata[ns]`; the departure list narrows from `CLUSTER`/`HOST`/unset-A to **`CLUSTER`/unset-A**.
- **B2** — a NEW departure clause: **a HOST-kind tag resolves at UPSTREAM-SELECTION time on the reference (P6), so it emits the picked endpoint's metadata even when the upstream connect then FAILS; envoy-go's emit seam carries the zero `Endpoint` on every failure sub-path and therefore falls to `default_value`/omit there.** Scope it exactly: **EXECUTED-proven for connect-failure 503** (P6 arm C); structurally identical on envoy-go's side for pool-overflow 503 but the reference is **unprobed** there; circuit-breaker 503 is likely parity (no pick occurs on either side) and also unprobed. **PARITY holds on the post-Decode local-reply class** (EXECUTED on the reference; RE-DERIVED on envoy-go). The other two reference no-host arms have NO envoy-go counterpart and must NOT be written up as parity: a 404 no-match emits **no span at all** in envoy-go (a PRE-EXISTING documented departure, `BEHAVIOR_CONTRACT.md:741`), and a zero-endpoint cluster is **boot-rejected** (`manager.go:890`). Note this is a **PRE-EXISTING** seam gap that already leaves the access-log upstream-host field empty on the same paths (§3.6); phase 72 adds a fourth consumer of it, it does not create it.
- **B3** — a documented BOUNDARY (pre-existing, discovered this SPEC): the reference's **Zipkin** serializer DROPS empty-string tag values (and the empty `node_id`/`zone` built-ins) where OTLP emits `""`; and the number-precision / arbitrary-struct-key-order serialization edges carried from ADR-0292 are now quantified.
- **B4** — `BEHAVIOR_CONTRACT.md:741` (the deferred-field list): the tracing `metadata` boundary line narrows to `CLUSTER`/unset-A.

## 10. Test plan + task surface *(D-CTMH-SPLIT — DECISION: a SINGLE FLAT ROW)*

**DECISION: a SINGLE FLAT ROW of NINE tasks (T1-T9)** (ADR-0045 valve armable-but-unconsumed). D-CTMH-ENDPOINT-METADATA turned up **zero** behavioral disturbance in `internal/cluster` (§3.5), which was the one thing that could have forced a split. Anticipated TDD spine, each task red-first → green → break-verified: **T1** the `Endpoint.filterMetadata` field + `MetaLookup` accessor + `manager.go` populate (+ a guard that the `envoy.lb` projection is byte-unchanged) · **T2** `kindMetadataHost` + the config accept arm (**the two `config_test.go` `/host` rows at `:639`/`:765` are the pre-known red set — assertions at `:648`/`:774`; the sibling `/cluster` + unset rows stay reject and are the live regression guard**) · **T3** the `hostMetaLookup` 5th param + the `kindMetadataHost` resolve arm + the 32 trailing-`nil` `resolve_test.go` caller updates (re-grep: a raw count returns 33, the extra is the string literal at `:106`) · **T4** the three `accesslog_emit.go` call sites + a live HOST-metadata-span test AND a first-class **zero-`picked`** test (the majority arm) · **T5** the fuzz seed + the stale-comment narrow + dispatch-verify · **T6** the `0116` fixture + breaks · **T7** BEHAVIOR_CONTRACT B1-B4 · **T8** the six-gate · **T9** ADR-0294 completed IN PLACE + ROADMAP row-72-done + the deferred-sentence narrow + STATE/router.

**⚠️ Six-gate hazard, pre-existing (S7):** `TestOutlierDetector_ConcurrentEjectExactlyOnce` (`internal/cluster/outlier_test.go:766`) is a **known intermittent `-race` failure reproduced on the UNMODIFIED baseline**. `internal/cluster` is a package this row newly touches, so its `-race` leg is newly load-bearing. **Isolate-re-run on failure; do NOT re-classify it as a phase-72 regression** unless it reproduces deterministically or the failure text differs (`reference_0061_ring_hash_spread_flake` precedent). Also expect the unrelated full-suite startup flake (`reference_differential_fullsuite_startup_flake`).

## 11. Edit-site roster + the 18-caller classification *(RE-DERIVED at `0150b04c`)*

**Production (5 files):** `internal/cluster/cluster.go` (the field + accessor + the `structpb` import) · `internal/cluster/manager.go:884` (the populate; `:883` byte-unchanged) · `internal/tracing/config.go` (`:54-60` const, `:267-268` → accept) · `internal/tracing/resolve.go` (`:32` param, a new arm after `:95-116`, the `:12-31` doc) · `internal/filter/hcm/accesslog_emit.go:57`/`:118`/`:179`. **Tests:** `internal/tracing/{config_test.go,resolve_test.go}` · `internal/cluster/{cluster_test.go,manager_test.go}` · `internal/filter/hcm/{accesslog_emit_test.go,span_emit_test.go,fuzz_test.go}`. **Fixture + docs:** `test/fixtures/0116-…/` + `runner_test.go` + BEHAVIOR_CONTRACT + DECISIONS + ROADMAP + STATE.
**BYTE-UNTOUCHED (sha256-proven in the scratch build):** `connection.go` · `h2dispatch.go` · `h3dispatch.go` · `internal/filter/http/chain.go` · `internal/cluster/subset.go` · plus `internal/xds`/`internal/tls`/`internal/boot`/`internal/listener`/`internal/bootstrap`/`validate/`/`internal/dynamicmetadata`/`internal/filter/http/{ratelimit,lua}`.

The 18 emit sites, RE-ENUMERATED with their `picked` argument (5 `connection.go` + 6 `h2dispatch.go` + 7 `h3dispatch.go`; every BRAINSTORM line number HELD):

**Partition by SPAN-CAPABILITY first** — for a tracing row that is the decision-relevant axis, and partitioning by `picked` alone is what produced two false parity claims in an earlier draft of §3.6. The span block is gated on `traceDecision != nil && traceDecision.Sample` (`accesslog_emit.go:30`), and `traceDecision` is assigned at `connection.go:553` / `h2dispatch.go:449` / `h3dispatch.go:255`, so every emit site ABOVE its file's assignment is PRE-Decide and can never produce a span at all:

| file:line | span-capable? | `picked` | class |
|---|---|---|---|
| `connection.go:330` | **NO** (pre-Decide) | `Endpoint{}` | 404 no-match, no chain |
| `connection.go:464` | **NO** (pre-Decide) | `Endpoint{}` | defensive 500, non-router terminal |
| `h2dispatch.go:313` | **NO** (pre-Decide; its own comment at `:306` says so) | `picked` **≡ always zero** (`actions.go:169`) | 404 no-match |
| `h2dispatch.go:396` | **NO** (pre-Decide) | `Endpoint{}` | defensive 500 |
| `h3dispatch.go:130` | **NO** (pre-Decide) | `Endpoint{}` | 404 no-match, no chain |
| `h3dispatch.go:210` | **NO** (pre-Decide) | `Endpoint{}` | defensive 500 |
| `connection.go:597` / `:699` | YES | `Endpoint{}` | local reply after DecodeHeaders / during DecodeData |
| `h2dispatch.go:530` | YES | `Endpoint{}` | local reply |
| `h3dispatch.go:280` / `:341` | YES | `Endpoint{}` | local reply ×2 |
| `connection.go:777` | YES | **`picked`** | H1 completed |
| `h2dispatch.go:577` / `:584` / `:613` | YES | **`picked`** | H2 completed |
| `h3dispatch.go:367` / `:373` / `:395` | YES | **`picked`** | H3 completed |

**18 sites = 6 PRE-Decide (never a span) + 12 span-capable.** Of the 12 span-capable: **7 carry a real `picked`** and **5 carry the zero endpoint** (all post-Decode local replies). Syntactically 11 of 18 are always-zero — `h2dispatch.go:313`'s `picked` is provably the literal `cluster.Endpoint{}` from `actions.go:169`, and V1 confirmed the "provably" is EARNED (a negative `routeIdx` is set only in the no-match branch, whose action is `directResponseAction{404}`) — but **6 of those 11 can never reach a span**, so the tracing-relevant zero-endpoint set is **5**, not 11. Separately, the 7 real-`picked` sites carry the ZERO endpoint on every upstream-failure sub-path (§3.6). Provenance is CLEAN on all three paths — H1 returns the whole dial-time struct from the pool (`cluster.go:690` hit, `:710` stash), H2 binds a FRESH pick (`h2pool.go:313`; `Addr()` at `:317` is only the map key), and **H3 shares H1's walk exactly** (`h3dispatch.go:139` takes `asRouterAction()`, the H1 flavor) — a path the BRAINSTORM never covered. Repo-wide there is exactly **ONE** non-empty `Endpoint{…}` literal in production, so no reconstruction site exists.

## 12. Sentinel maintenance — a SENTENCE-NARROWING row, narrowed AT THE IMPL (the phase-57 precedent)

The live Observability `candidates:` sentence names *"tracing custom_tags `metadata` for the non-`REQUEST`/non-`ROUTE` MetadataKinds (`CLUSTER`/`HOST`, reject-only …)"*. **`HOST` rolls OUT at the phase-72 IMPL row-done edit**, narrowing it to `CLUSTER`-only. **NO deferred sentence is edited at this SPEC** — keeping EXACTLY ONE live match through the pre-IMPL stages is what makes check (2) meaningful (`reference_sentinel_deferred_sentence_live_vs_historical` — the live grep needs `candidates:` adjacent). At this close: (1) `NOT DONE: row 72`; (2) ⇒ **3**; (3) `NEVER OPENED: gRPC/Runtime/WASM`. Does NOT fire; no `stop`.

## 13. Deferred items *(the BRAINSTORM §8 roster carried, MINUS this row's pickup, PLUS what this SPEC surfaced)*

Carried: the **`CLUSTER`** MetadataKind (MODERATE ~11-13 tasks — the LAST of the four and the natural phase-73 candidate; needs a metadata field + accessor on `cluster.Cluster`, a `routeAction` method + a `SetClusterMetadata`/`ClusterMetaLookup` pair **re-paying the 18-caller thread**, and a disposition for `weightedClusterRouteAction`'s per-request in-closure selection) · `spawn_upstream_span` · OTel `http_service` / `sampler` / `resource_detectors` · tracing `verbose` · force-trace · the `ssl` stat family · `access_log[].filter` (**a strong FUTURE pick** — a live behavioral divergence) · `stats_flush_on_admin` · all xDS and HTTP/3 candidates · the never-opened families. **NEWLY SURFACED by this SPEC:** (i) **pick-time `picked` propagation on upstream-failure paths** (§3.6 / B2) — a PRE-EXISTING divergence that already empties the access-log upstream-host field on pool-overflow and connect-failure 503s; costed at ~4 production lines (`ep` is already in scope at both returns) but it flips three access-log consumers and breaks three landed unit tests (`router_test.go:333-334`/`:492`/`:597`), and **no differential fixture pins it**, so it is a self-contained ACCESS-LOG row with a tracing rider — a strong small FUTURE pick, and one that should first probe the reference on the pool-overflow and circuit-breaker arms this SPEC left unprobed; (ii) **the Zipkin empty-string tag drop** (B3) — pre-existing, affects the landed REQUEST/ROUTE kinds too; (iii) **the `internal/cluster` `-race` flake** (S7) — a test-hardening candidate, not a feature row.

## 14. ADR continuity — the ADR-0294 §Context DRAFT (anchored here; full entry at the phase-72 IMPL)

Per ADR-0044-as-used, **ADR-0294's §Context lands at THIS SPEC commit** (the DECISIONS tail flips ADR-0293 → ADR-0294 here); §Decision + §Consequences append IN PLACE at the phase-72 IMPL, **no renumber**; next-free becomes **ADR-0295**. The drafted §Context is appended to `DECISIONS.md` at this commit.

## 15. Exit — counts + expectations at SPEC-DONE

Docs-only: `SPEC.md` (new) + the ADR-0294 §Context append + STATE §Current rolled IN PLACE (lifecycle 1 → 2, lineage re-capped at five) + `next-prompt.txt` rolled to the phase-72 **PLAN** (NOT the IMPL — the four-stage lifecycle). **ROADMAP UNTOUCHED**; row 72 STAYS `in-progress`. Counts UNCHANGED apart from the DECISIONS tail: fixtures **117** · fuzzers **55** · stat surface **1201** · BackendKind tail **38** · go.mod modules **2** · DECISIONS tail **ADR-0294** (next-free ADR-0295). Anticipated at the IMPL: fixtures **117 → 118**, everything else +0.

## 16. Adversarial-pass record

TWO independent verifiers ran against this draft in PRIVATE scratch before landing (`reference_parallel_subagents_private_scratch`), on disjoint remits. **Both found real defects; every finding below was FOLDED IN, and the folds are visible above at the point of use.**

**V1 — code-claims re-derivation + by-execution.** V1 re-opened every anchor independently and **BUILT the row's claimed shape in a private copy of the tree**, confirming BY EXECUTION: exactly SIX files differ; the 18 emit callers, the 29 emit test callers and `chain.go` are byte-identical to baseline; exactly ONE package fails with exactly the TWO predicted `/host` subtests; `kindMetadataHost` lands at iota == 5 (the failure output printed `Kind:5`); `unsafe.Sizeof(Endpoint)` 104 → 112 B; the S2 allocation table (1 alloc / 128 B for all three shapes, 2 for the pointer receiver); `structpb.NewStructValue(nil)` is non-nil so the guard is load-bearing, and a zero `Endpoint{}.MetaLookup` returns `(nil,false)` without panicking; Go's `protojson` struct-key order is stable across 200 runs (so S4(iii) is a genuine cross-side hazard); the S7 `-race` flake REPRODUCED on the unmodified baseline (6 failures at `-count=400`); and the 33-raw/32-real `resolve_test.go` count. It also confirmed the entire anchor roster, the 18-caller line numbers, the "provably always zero" claim for `h2dispatch.go:313`, the `Endpoint` no-ripple census, every count, and every fixture claim. **ZERO SEVERE. THREE MODERATE, two of them claims that were FALSE:** (M1) the §3.6 table asserted PARITY on the 404 no-match arm, but envoy-go emits **no span at all** there — all three no-match sites are PRE-Decide with a nil `traceDecision` — a PRE-EXISTING documented departure, not a HOST-tag parity; (M2) the same table asserted PARITY on the zero-endpoint-cluster arm, but envoy-go **boot-rejects** that config (`manager.go:890`), so there is no counterpart at all; (M3) the claim that the closure is "MANDATORY … (the cycle guard)" was **FALSE** — V1 executed the counterfactual and a direct `internal/cluster` import into `internal/tracing` builds clean, so it is a self-imposed LAYERING rule, not a cycle. Plus (M4) the pick-time generalization outran its single executed arm, and six MINOR cite/wording defects. **All folded:** §3.6's table rewritten with a fourth verdict class (N/A — no counterpart), §9's B2 corrected, §3.3/§4/S2 re-justified on layering, the caveat moved adjacent to the claim it qualifies, and V1's structural insight — that **partitioning by `picked` alone is what produced both false parity claims** — adopted as a new SPAN-CAPABILITY partition in §11 (6 of 18 sites are PRE-Decide and can never emit a span; the tracing-relevant zero-endpoint set is 5, not 11). Verdict: **BUILDABLE as written, proven by execution.**

**V2 — process, consistency, and stage-close mechanics.** V2 independently re-ran every mechanical check and confirmed: row 72 stays `in-progress` with ROADMAP untouched; no deferred sentence narrowed (grep ⇒ 3); the DECISIONS tail flips to a unique ADR-0294 with next-free ADR-0295; STATE §Current rolled IN PLACE with correct ADR-0288 singletons and lifecycle 1 → 2; the lineage at exactly five bullets; every count (117 / 55 / 1201 / 38 / 2 / port 10116 free); the sentinel not firing with no `stop`; a genuinely docs-only envelope with zero production `.go` and nothing unintended in the diff; and the router pointing at the **PLAN**, not the IMPL. **ZERO SEVERE. FIVE MODERATE:** (F1/F2) the append included an anticipated `### Decision` HEADING that ADR-0293 did not have at its SPEC, contradicting this document's own "§Context append" declaration in four places and — the concrete hazard — setting up an IMPL agent to create a SECOND `### Decision` heading; (F3) the STATE §Recent lineage PREAMBLE was left stale, naming the wrong newest and dropped bullets, in the one file ADR-0288 exists to keep grep-trustworthy; (F4) §1.2 asserted this very adversarial pass in the past tense while §16 was still a placeholder — the phase-69 cited-but-unwritten defect class; (F5) "~10 tasks" against an enumerated NINE, replicated across three documents. Plus MINOR cite and ordering defects. **All folded:** the ADR realigned to the exact ADR-0293 SPEC-stage shape (§Context + a "The design (§Decision anticipated)" paragraph + a `*(§Decision + §Consequences land at the phase-72 IMPL.)*` hand-off line, stray `---` removed) with the IMPL instruction made explicit in the router; the preamble corrected; this section written; the task count fixed to NINE everywhere; the S-ledger reordered.

**What neither verifier could check, stated plainly:** the seven live-probe arms were not re-run (the containers were cleaned up), so P1–P7's reference behaviors rest on the probe fleets' own executed evidence — V1 attacked their stated conclusions for LICENCE rather than truth, and found P2, P7 and P3's key-order finding properly discriminating, with P6 the one whose generalization outran its arms (folded as M4). Nobody verified that the `0116` fixture design actually produces a green cross-side assertion; that is the IMPL's job.
