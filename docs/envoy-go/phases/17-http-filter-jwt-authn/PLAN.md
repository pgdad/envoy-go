# Phase 17 — HTTP filter `envoy.filters.http.jwt_authn` (`internal/filter/http/jwtauthn/`, NEW top-level `internal/jwks/` framework primitive, NEW top-level `internal/jwt/` framework primitive, NEW `test/helpers/jwksbackend/` test-helper, differential fixture `0019-http-jwt-authn` with 8 scenarios, `BEHAVIOR_CONTRACT.md ## HTTP filter chain ### envoy.filters.http.jwt_authn` extension + `## Stat-name mapping` 64→71 extension + `## Equivalence Matrix` 0019 row + `### Phase 17 forward-pointer notes` subsection + NEW `## JWKS framework primitive` top-level section + NEW `## JWT verifier framework primitive` top-level section, **TWO framework deltas** — SECOND CONSECUTIVE §9 row to introduce two simultaneously after phase 16) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended per project memory user preference `feedback_execution_style.md`) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Land `envoy.filters.http.jwt_authn` — Envoy v1.37.2's canonical JWT bearer-token validation filter, RS+ES algorithm family (RS256/384/512 + ES256/384/512 — 6 algorithms; HS family + EdDSA + `none` + PS family deferred), Both-JWKS-source proto-faithful (RemoteJwks with async fetch + per-thread cache + 5s-before-TTL scheduled refresh + fixed-1s failed-refetch retry per §11.P4 REFUTED-exponential-backoff + LocalJwks via DataSource inline-string/inline-bytes/filename), Full-6 JwtRequirement variants (provider_name + provider_and_audiences + requires_any + requires_all + allow_missing + allow_missing_or_failed per §11.P16), All-4 token extraction sources (default Authorization Bearer + access_token query param + configured from_headers with value_prefix substring-search + from_params case-sensitive URL-decoded first-value-only per §11.P14 + from_cookies case-sensitive verbatim per §11.P15), Full-header-side post-validation side-effects (strip-on-success → forward_payload_header → claim_to_headers dot-notation with array-claim-rejection per §11.P10 → clear_route_cache), 8th-canonical per-route delegation via requirement_map (`oneof{disabled(bool) | requirement_name(string)}` with string-reference-delegation per ADR-0125 §(xiii) amendment — landed at SPEC commit; runtime-resolve discipline mirrors Envoy filter_config.cc `findPerRouteVerifier`; on miss emits 403 + "Failed JWT authentication: Wrong requirement_name: <name>") — as the TENTH production HTTP filter in envoy-go (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11, csrf @ 12, buffer @ 13, compressor @ 14, bandwidth_limit @ 15, rbac @ 16), with byte-equivalent wire outcomes against reference Envoy v1.37.2 on every observable axis EXCEPT (a) the `response_code_details` field-emission divergence-window (envoy-go MVP: silent; Envoy: emits `"jwt_authn_access_denied{<failure_reason_with_spaces_as_underscores>}"` per §1.1 amendment 11 + §8 deferral 13 — joint closure with phase-16 rbac forward-pointer item 8 at a future response-code-details framework phase) AND (b) the `filter_state_rules` silent-ignore divergence-window (envoy-go MVP: silent; Envoy: runtime requirement-selection driven by `StreamInfo.FilterState[<name>]` per §1.1 amendment 1 + §8 deferral 12 — couples to future filter-state-family phase) AND (c) the dynamic-metadata family (`payload_in_metadata` + `header_in_metadata` + `failed_status_in_metadata` + `normalize_payload_in_metadata`) silent-ignore (envoy-go MVP: silent; Envoy: emits per §8 deferrals 1-4 — couples to dynamic-metadata family blocked at phase-16 forward-pointer item 9) AND (d) the validated-JWT LRU cache silent-ignore (envoy-go MVP: no-cache, each request re-validates; Envoy: 100-entry default LRU cache gated on `jwt_cache_config` per §11.P11 + §8 deferral 8 — high-RPS performance divergence; `jwt_cache_hit` + `jwt_cache_miss` counters structurally unreachable under MVP) AND (e) the 3 v1.37.x JwtProvider claim-coverage extensions (`subjects` + `require_expiration` + `max_lifetime`) silent-ignore (envoy-go MVP: silent; Envoy: enforces per §1.1 amendments 2 + 3 + §8 deferrals 15-17 — couples to claim-coverage-extension family) AND (f) the `clear_route_cache` implicit-on-side-effect trigger silent-ignore (envoy-go MVP: honors only explicit `clear_route_cache: true`; Envoy: also clears when `claim_to_headers` adds ≥1 header OR `payload_in_metadata` is set per §8 deferral 18), under the 07.1 framework with **TWO new framework deltas** (SECOND CONSECUTIVE §9 row to introduce two simultaneously, after phase-16 rbac's matcher + TLS-principal): (i) HTTP-outbound JWKS fetcher framework primitive at NEW top-level `internal/jwks/` package (LOCKED location per SPEC §3.1 — JWKS-specific NOT generic httpclient; `Fetcher` opaque type wrapping URI + cache duration + AsyncFetch + RetryPolicy; `New(uri, cacheDuration, asyncFetch, retryPolicy)` blocking-OR-non-blocking initial fetch per `fast_listener`; `Get(ctx)` returns cached JWKSet OR `ErrJwksNotReady`; refresh schedule **5s-before-TTL** via `time.AfterFunc` per §11.P5 RATIFIED-AND-EXTENDED — default 10-min cache duration per `DefaultCacheExpirationSec=600s`; failed-refetch **FIXED-INTERVAL 1s** per §11.P4 REFUTED-exponential-backoff — configurable via `JwksAsyncFetch.failed_refetch_duration`; inner-HTTP-request RetryPolicy honored via `retry_policy_` per Envoy v1.37.x precedent; `JWKSet.Lookup(kid, alg)` returns matching key via Envoy's `pickKeyAlgWithKid` logic) — anchored at ADR-0150; (ii) JWS/JWT verifier framework primitive at NEW top-level `internal/jwt/` package (pure-Go stdlib `crypto/rsa.VerifyPKCS1v15` + `crypto/ecdsa.Verify` + `encoding/base64.RawURLEncoding`; `Token` type with `RawHeader` + `RawPayload` + `RawSignature` + parsed `Header` + `Payload` maps + `Alg` + `Kid`; `Parse(raw)` parses 3-part `header.payload.signature` JWT structure with `JwtBadFormat` rejection on malformed input; `Token.VerifySignature(key, alg)` with RS+ES algorithm allow-list — PARSE-REJECT for unsupported algs returns `ErrJwtHeaderNotImplementedAlg`; `Token.ValidateClaims(opts)` checks exp + nbf + iss + aud per ValidateOptions with clock-skew tolerance per §1.1 amendment 7 — default 60s; `Token.PayloadClaim(path)` dot-notation extractor for nested claims per §11.P10 RATIFIED-AND-EXTENDED — array claims rejected via `ErrArrayClaim`; ~20 canonical error sentinels mirroring jwt_verify_lib status codes per §11.P1 RATIFIED-AND-EXTENDED `getStatusString()` table) — anchored at ADR-0151; both primitives cross-phase-reusable (future `ext_authz` HTTP-mode + `oauth2` token-endpoint flows compose against the JWKS fetcher's `http.Client` + retry-policy structure; future `jwt_claim_router` filter + `oauth2` token validation compose against the JWT verifier's `Parse` + `VerifySignature` + `ValidateClaims` + `PayloadClaim` API).

**Architecture:** New `internal/filter/http/jwtauthn/` package owning the filter implementation; DECODER-only `HTTPFilter` value (`Encoder: nil`; mirrors phase-12 csrf + phase-13 buffer + phase-16 rbac decoder-only precedent — 4th §9 row to ship pure decode-side; jwt_authn is a pre-body request-gate filter evaluated at `DecodeHeaders` time with the disposition computed BEFORE the request body forwards); package split into 3 production files + 2 test files: `jwtauthn.go` (filter type + factory + decode methods + `filterStats` struct + `compiledConfig` + `parsePerRoute` + `resolvePerRouteConfig` + `buildCompiledConfig` + `buildCompiledProvider` + `buildCompiledRule` + `buildCompiledRequirement` + `buildCompiledPerRoute` + `New` factory), `evaluator.go` (6-variant JwtRequirement evaluator per §6.8 + `evaluateProvider` per-token-iteration + side-effect emit-order per §6.9 + extraction-source iteration per §6.7), `provider.go` (compiledProvider helpers + JWKS source parsing + `applySideEffects` + `emitDenyResponse` + WWW-Authenticate header construction); `compiledConfig` shape per SPEC §6.2 with `providers map[string]*compiledProvider` + `rules []*compiledRule` (listener-level dispatch; first-match-wins) + `requirementMap map[string]*compiledRequirement` (named-requirement registry referenced by both listener-level rules AND per-route TPFC) + `bypassCorsPreflight bool` + `stripFailureResponse bool` + `stats *filterStats`; `compiledProvider` carries 13 consumed JwtProvider fields (issuer + audiences + jwksFetcher xor localJwks + forward + fromHeaders + fromParams + fromCookies + forwardPayloadHeader + padForwardPayloadHdr + claimToHeaders + clearRouteCache + clockSkew with default 60s per §1.1 amendment 7) — 8 silent-ignored (`payload_in_metadata` + `header_in_metadata` + `failed_status_in_metadata` + `normalize_payload_in_metadata` + `jwt_cache_config` + `subjects` + `require_expiration` + `max_lifetime` per §1.1 amendments 2 + 3 + §8 deferrals); `compiledRule` carries `matcher *router.RouteMatcher` (reuses phase-04 RouteMatch evaluator per §11.P9 PGV REQUIRED) + `requirement *compiledRequirement` (resolved either from inline `requires` OR from `requirement_map[name]` — BOTH ARMS HONORED per §1.1 amendment 4 — REFUTES BRAINSTORM PARSE-REJECT-deprecated hypothesis); `compiledRequirement` carries recursive 6-variant evaluator tree (kind enum providerName/providerAndAudiences/requiresAny/requiresAll/allowMissing/allowMissingOrFailed + provider pointer + audOverr override + children slice for AND/OR recursion); `filterStats` shape per §1.1 amendment 9 — **7 base counters** (`allowed` + `denied` + `corsPreflightBypassed` + `jwksFetchSuccess` + `jwksFetchFailed` + `jwtCacheHit` + `jwtCacheMiss`; 5 actively emitted under MVP; the last 2 are STRUCTURALLY UNREACHABLE per §8 deferral 8 jwt_cache_config silent-ignore); SHARED-stats discipline per ADR-0154 (mirrors phase-12/13/14 SHARED; DIVERGES from phase-11/15/16 INDEPENDENT — rationale: per-route is pure string-reference-delegation, spawns no new policy-evaluation state); `factoryState` holds `listenerRC *compiledConfig` + `perRoute sync.Map` (keyed by `*PerRouteConfig` proto pointer; lazy-cache for `compiledPerRoute` via `LoadOrStore` mirrors phase-11 + phase-15 + phase-16 precedent — but the per-route value is the lightweight `*compiledPerRoute{disabled, requirementName}` NOT a fresh `*compiledConfig` since per-route does NOT carry its own config in the 8th canonical); per-stream `filter` carries `state *factoryState` + `dcb` + `activeRC` (always listener-level config) + `perRoute *compiledPerRoute` + `passthrough bool` + `originalURI string` (captured at DecodeHeaders entry for WWW-Authenticate realm per §1.1 amendment 12 — REFINES BRAINSTORM `realm="<issuer>"` hypothesis); `DecodeHeaders` body per SPEC §6.6: capture `:path` into `f.originalURI` → resolve per-route TPFC via `dcb.RequestRouteConfig()` → case (a) per-route `disabled: true` → set `passthrough` + `HeaderContinue` (NO counter increments per §5.3) → CORS preflight bypass per §11.P1 + filter.cc verbatim (`isCorsPreflightRequest` = `:method == OPTIONS && origin != "" && access-control-request-method != ""` → `cors_preflight_bypassed +1` + `HeaderContinue`) → resolve requirement via `f.resolveRequirement(headers)` (per-route `requirement_name` case (c) → consult `f.activeRC.requirementMap[<name>]`; on miss emit `SendLocalReply(403, "Failed JWT authentication: Wrong requirement_name: <name>", nil)` + `denied +1` per §1.1 amendment 6; otherwise iterate listener-level `rules[]` first-match-wins; no-match → `nil` requirement → pass-through with NO counter increments — "no JWT verification required") → if requirement is non-nil, run `f.evaluateRequirement(req, headers)` → apply result via `f.applyResult(result, headers, req)` (on ALLOWED: `allowed +1` + apply side-effects (strip → forward_payload_header → claim_to_headers → clear_route_cache per §6.9) + `HeaderContinue`; on DENIED: `denied +1` + `f.emitDenyResponse(reason)` mapping 401 default + 403 specifically for `JwtAudienceNotAllowed` per §1.1 amendment 8 + §11.P1 — body = canonical jwt_verify_lib `getStatusString(status)` per §11.P1 RATIFIED-AND-EXTENDED ~70-string table; WWW-Authenticate = `Bearer realm="<original_uri>"` + conditional `, error="invalid_token"` for non-`JwtMissed` per §1.1 amendment 12 + §11.P2 RATIFIED-AND-REFINED; `strip_failure_response: true` → body becomes `""` AND no WWW-Authenticate header per §11.P3 RATIFIED + `HeaderStopIteration`); `DecodeData` + `DecodeTrailers` + `OnDestroy` pass-through / no-op (decode-side gate with no per-stream timers); per-route TPFC `PerRouteConfig` wrapper proto with REQUIRED oneof `RequirementSpecifier` carrying two arms `disabled` (**bool** varint at field 1; NOT `Empty` per §1.1 amendment 5 — REFUTES BRAINSTORM Empty-form hypothesis; PGV `required = true` at oneof level per §11.P9 + `config.pb.validate.go:2472-2481`) and `requirement_name` (string at field 2; PGV `min_len=1` per `config.pb.validate.go:2460-2462`); per-route 3 cases at `parsePerRoute`: (a) `disabled: true` → `compiledPerRoute{disabled: true, requirementName: ""}` (filter wholly inactive on route; NO counter increments); (b) `disabled: false` → `compiledPerRoute{disabled: false, requirementName: ""}` (oneof set to disabled-arm with false value; falls through to listener-level rules dispatch mirroring Envoy filter_config.cc); (c) `requirement_name: "<name>"` → `compiledPerRoute{disabled: false, requirementName: "<name>"}` (string-reference-delegation; listener-level `requirementMap[<name>]` consulted at request time per §1.1 amendment 6 + §11.P12 REFUTED-parse-reject); the **8th canonical per-route pattern** per ADR-0125 §(xiii) — landed at SPEC commit; the FIRST row to use **string-reference-delegation** discipline distinct from all 7 prior canonicals (see ADR-0125 §(xiii) distinction matrix); differential fixture `0019-http-jwt-authn` THREE-listener topology (`l_test_a` plaintext HCM filter chain `jwt_authn → router` + `l_jwks_backend` cluster for in-process JWKS server + echo-backend cluster `c_backend`) with 8 scenarios per SPEC §7.1 covering: scenario 1 valid-RS256-RemoteJwks-allow (RS256 token + RemoteJwks via in-process JWKS server; first-request triggers JWKS fetch; `allowed +1` + `jwks_fetch_success +1`), scenario 2 valid-ES256-LocalJwks-allow (ES256 token + inline LocalJwks; NO `jwks_fetch_*` increments — LocalJwks path), scenario 3 missing-token-deny (no Authorization header; 401 + body byte-exact `"Jwt is missing"` 14 bytes + WWW-Authenticate `Bearer realm="/"` WITHOUT error param per §1.1 amendment 12 — `JwtMissed` case omits error param; `denied +1`), scenario 4 expired-token-deny (token with `exp` in the past; 401 + body byte-exact `"Jwt is expired"` 14 bytes + WWW-Authenticate with `, error="invalid_token"`; `denied +1`), scenario 5 bad-signature-deny (token signed with non-matching private key; 401 + body byte-exact `"Jwt verification fails"` 22 bytes + WWW-Authenticate with error param; `denied +1`), **scenario 6 bypass-cors-preflight** (OPTIONS request + Origin header + Access-Control-Request-Method header; 200 backend-echo + `cors_preflight_bypassed +1`; NO `allowed/denied` increments), **scenario 7 per-route 8th-canonical `requirement_name` delegation** (per-route TPFC `PerRouteConfig{requirement_name: "alt-req"}` referencing listener-level `requirement_map[alt-req] = provider_name: provider-alt`; valid token for provider-alt; `allowed +1`), **scenario 8 per-route 8th-canonical `disabled: true`** (per-route TPFC `PerRouteConfig{disabled: true}`; no Authorization header; 200 backend-echo route bypasses validation; NO counter increments), byte-exact body assertion on ALL scenarios (allow paths backend-echo verbatim; deny paths canonical jwt_verify_lib strings byte-exact); per-counter delta byte-equivalence on the 5 active base counters per HCM stat_prefix; NEW test-helper `test/helpers/jwksbackend/` (in-process HTTP JWKS server with spawn-per-fixture lifecycle; `New(ctx, addr, routes)` starts the server; `Stop()` terminates; serves configurable JWK Set JSON content at configurable paths; both `envoy.yaml` and `envoy-go.yaml` wire to the same JWKS URI via cluster `c_jwks_backend`); RSA-2048 + ECDSA-P256 PKI generation at fixture-test-setup time via `pki/gen.go` (mirrors phase-16 `pki/gen.go` mTLS pattern but produces JWK Sets instead of X.509 cert chains — three RSA keypairs for RS256 providers + one ECDSA P-256 keypair for ES256 provider + one tampered-signing key for scenario 5).

**Tech Stack:** Go 1.26.2; `go-control-plane` v1.32.4 module + v1.37.2 cross-check (proto pin per ADR-0008; phase-17 SPEC §11 used both for cross-version sanity verification including the 21-field JwtProvider proto `[#next-free-field: 22]` + 6-field JwtAuthentication `[#next-free-field: 7]`); `protojson.Unmarshal` for proto decoding; `crypto/rsa.VerifyPKCS1v15` + `crypto/ecdsa.Verify` + `crypto/ecdsa.GenerateKey` + `crypto/rsa.GenerateKey` + `crypto/elliptic.P256` Go stdlib for signature verification + PKI generation; `encoding/base64.RawURLEncoding` Go stdlib for JWT base64url-decode of header + payload + signature; `encoding/json` Go stdlib for JWS/JWT JSON-decode of header + payload claims; `net/http.Client` Go stdlib for outbound HTTP JWKS fetch (wrapped by `internal/jwks/Fetcher`); `time.AfterFunc` + `time.Ticker` Go stdlib for JWKS refresh scheduling (wrapped by `internal/jwks/Fetcher`); `sync.Map` for per-route lazy-cache (reuse from phase-11 ADR-0117 + phase-15 ADR-0139 + phase-16 ADR-0145 precedent; phase-17 stores lightweight `*compiledPerRoute` NOT fresh `*compiledConfig` since per-route is delegation-only); `sync.Mutex` for `internal/jwks/Fetcher` cache state + refresh-timer lifecycle; reference Envoy `envoyproxy/envoy:v1.37.2` SHA `c5e8a68e52f4d4697a9adb280dbe415d77fedf1257e183dcb86205bd438f18bd` (per ADR-0008 + ENVOY_TARGET.md — unchanged through phase 17); golangci-lint 1.64.8 (ADR-0009 pin); Docker for differential harness; HTTP/1.1 plaintext fixture (no H2 differential coverage per SPEC §7.4); existing PathMatcher / HeaderMatcher / StringMatcher / CidrRange evaluators from phase-07.1 cors (shared infrastructure NOT needed for jwt_authn's leaf surface — jwt_authn's leaf matchers are JWT-claim-specific, NOT HTTP-routing-style); existing phase-04 `router.RouteMatcher` for listener-level `RequirementRule.match` evaluation.

---

## Scope check — why phase 17 ships as one row (not split)

Net change estimate (mirroring the 09 / 10 / 11 / 12 / 13 / 14 / 15 / 16 PLAN's component-table convention):

- `internal/filter/http/jwtauthn/doc.go` ~30
- `internal/filter/http/jwtauthn/jwtauthn.go` ~450-550 (filter + factory + types + `compiledConfig` + `compiledProvider` + `compiledRequirement` + `compiledRule` + `compiledPerRoute` + `filterStats` + `factoryState` + `parsePerRoute` + `resolvePerRouteConfig` + `buildCompiledConfig` + `buildCompiledProvider` + `buildCompiledRequirement` + `buildCompiledRule` + `buildCompiledPerRoute` + `newFilterStats` + `DecodeHeaders` body + `DecodeData` + `DecodeTrailers` + `OnDestroy` + `SetDecoderCallbacks` + `New` factory)
- `internal/filter/http/jwtauthn/evaluator.go` ~300-400 (6-variant `evaluateRequirement` switch + `evaluateProvider` per-token iteration + `extractTokens` 4-source iteration + extraction helper `parseQueryParam` + `parseCookies` + `isCorsPreflightRequest`)
- `internal/filter/http/jwtauthn/provider.go` ~200-300 (`buildCompiledProvider` JWKS source parsing + `readDataSource` for LocalJwks + `applySideEffects` 4-step emit-order + `stripExtractionSources` strip-on-forward=false discipline + `emitDenyResponse` 401/403 dispatch + WWW-Authenticate construction + `mapStatusToHTTPCode` + `encodeBase64URL` pad/unpad helper + `stringify` claim-coerce helper)
- `internal/filter/http/jwtauthn/jwtauthn_test.go` ~1500-2200 (11 unit-test groups per SPEC §14.1 + planner-time-emerging stats-namespace integration sub-group; ~80-120 test cases total)
- `internal/filter/http/jwtauthn/fuzz_test.go` ~85 (21st fuzzer in repo: `FuzzJwtAuthnConfigParse`; mirrors phase-16 fuzzer shape extended to the 5-field outer JwtAuthentication + 13-field-consumed JwtProvider + 6-variant JwtRequirement evaluator + recursive `requires_any/all` combinators; 13-seed corpus per SPEC §14.3)
- `internal/jwks/doc.go` ~30 (NEW top-level package; cross-phase-reusable per ADR-0150 §Decision)
- `internal/jwks/jwks.go` ~350-450 (NEW top-level package; `Fetcher` opaque type + `New` constructor with blocking-or-non-blocking initial fetch per `fast_listener` + `Get(ctx)` cache-hit-or-on-demand fetch returning `*JWKSet` + `Close()` lifecycle + refresh schedule via `time.AfterFunc` 5s-before-TTL + failed-refetch fixed-interval 1s default + `JWKSet` parsed JWK Set type + `JWKSet.Lookup(kid, alg)` pickKeyAlgWithKid logic + `AsyncFetch` + `RetryPolicy` config types + JWK Set JSON-parse helpers reading `keys[]` array elements with `kty`/`use`/`kid`/`alg`/`n`/`e`/`crv`/`x`/`y` field decoding + `crypto.PublicKey` conversion to `*rsa.PublicKey` or `*ecdsa.PublicKey` based on `kty` value; ~12 error sentinels per §11.P1 JWKS subset)
- `internal/jwks/jwks_test.go` ~300-450 (unit tests covering: blocking initial fetch success + failure; non-blocking `fast_listener: true` initial fetch returns `ErrJwksNotReady` until completion; refresh fires at `cacheDuration - 5s`; failed-refetch at fixed 1s interval; cached `Get` returns stale-then-fresh after refresh; `JWKSet.Lookup` kid-match + alg-fallback + miss returns `ErrJwksKidAlgMismatch`; JWK Set JSON-parse rejects malformed keys; concurrent `Get` + background refresh race-safety; `Close()` terminates refresh goroutine; ~25-35 test cases)
- `internal/jwt/doc.go` ~30 (NEW top-level package; cross-phase-reusable per ADR-0151 §Decision)
- `internal/jwt/jwt.go` ~300-400 (NEW top-level package; `Token` type with `RawHeader` + `RawPayload` + `RawSignature` + `Header` + `Payload` maps + `Alg` + `Kid`; `Parse(raw)` 3-part split + base64url-decode + JSON-decode per §11.P1 `JwtBadFormat` rejection; `VerifySignature(key, alg)` RS+ES algorithm allow-list dispatch via `crypto/rsa.VerifyPKCS1v15` + `crypto/ecdsa.Verify`; `ValidateClaims(opts)` exp + nbf + iss + aud checks with clock-skew tolerance + canonical error returns; `PayloadClaim(path)` dot-notation extractor with array-claim rejection per §11.P10; `ValidateOptions` struct with `Issuer` + `Audiences` + `ClockSkew` + `Now` + 3 silent-ignored fields per §1.1 amendment 3 (`RequireExpiration` + `MaxLifetime` + `Subjects` — receive at API but never enforced); `StringMatcher` placeholder + `ParseJWKSet(raw)` helper deferring JWKS-parse to jwks package OR co-located here; ~20 error sentinels per §11.P1 + §3.2 mirroring jwt_verify_lib status codes)
- `internal/jwt/jwt_test.go` ~250-400 (unit tests covering: `Parse` 3-part split + reject malformed (`JwtBadFormat`); base64url-decode rejection (`JwtHeaderParseErrorBadBase64` + `JwtPayloadParseErrorBadBase64` + `JwtSignatureParseErrorBadBase64`); JSON-decode rejection (`JwtHeaderParseErrorBadJson` + `JwtPayloadParseErrorBadJson`); `VerifySignature` RS256/384/512 positive cases via test-PKI keypairs; `VerifySignature` ES256/384/512 positive cases; `VerifySignature` rejection on tampered signature (`JwtVerificationFail`); `VerifySignature` rejection on alg outside allow-list (`JwtHeaderNotImplementedAlg`); `ValidateClaims` exp + nbf + clock-skew tolerance; `ValidateClaims` iss exact match + mismatch (`JwtUnknownIssuer`); `ValidateClaims` aud OR-semantic intersection + mismatch (`JwtAudienceNotAllowed`); `PayloadClaim` dot-notation nested extraction + scalar/string/bool types; `PayloadClaim` array-claim rejection (`ErrArrayClaim`); `RequireExpiration` + `MaxLifetime` + `Subjects` silent-ignored per amendment 3 verification; ~30-40 test cases)
- `cmd/envoy-go/main.go` +1 import line + 1 register line ~+3 (`httpReg.Register(jwtauthn.TypeURL, jwtauthn.New)` inserted alphabetical-after-header_mutation per ADR-0100 §2.2 + ADR-0148 §Decision; resulting block reads `router → bandwidthlimit → buffer → compressor → cors → csrf → envoygotest → fault → header_mutation → jwtauthn → localratelimit → rbac → header_mutation.RegisterPerRouteValidator → httpReg.Freeze()`)
- `test/helpers/jwksbackend/doc.go` ~25 (NEW shared test-helper per planner-time decision 6 — phase 17 introduces this shared helper for in-process HTTP JWKS server lifecycle)
- `test/helpers/jwksbackend/jwksbackend.go` ~80-120 (NEW shared test-helper; `Server` type with `Listener net.Listener` + opaque state; `New(ctx, addr, routes)` starts the server with `routes map[string]string` mapping path → JWK Set JSON content; `Stop()` terminates the server cleanly; `Server.Addr()` returns the listener's actual address for runner-driven port allocation; uses Go stdlib `net.Listen("tcp", addr)` + `http.Server.Serve` with a simple mux dispatching path matches; ~80-120 LoC main library)
- `test/helpers/jwksbackend/jwksbackend_test.go` ~80 (unit tests covering: server starts and serves configured routes; serves the JWK Set body byte-exact; multi-route dispatch; teardown closes the listener; concurrent client safety; missing-route returns 404)
- `test/differential/fixture/fixture.go` new `BackendKind` enum value (`HTTPJwtAuthn BackendKind = 16`) + doc-comment ~+15
- `test/differential/runner_test.go` blank-import addition + new switch-case for `HTTPJwtAuthn` reuses the existing `startEchoBackend` spawn helper from phase-14 Task 10 ~+10
- `test/fixtures/0019-http-jwt-authn/` (NEW DIRECTORY) — `envoy.yaml` ~175 + `envoy-go.yaml` ~175 + `expectations.yaml` ~80 + `README.md` ~120 + `inputs/driver.go` ~320 (8-scenario driver + JWKS-backend lifecycle helpers + RSA/ECDSA token generation helpers) + `pki/gen.go` ~140 (3 RSA-2048 + 1 ECDSA-P256 keypair generation with JWK Set serialization) = ~1010
- `docs/envoy-go/DECISIONS.md` 8 ADRs (ADR-0148..ADR-0155) authored at impl-time per ADR-0044 ADR-on-impl convention; **ADR-0125 §(xiii) amendment paragraph ALREADY landed at SPEC commit** per phase-13/14/15/16 in-place-amend-at-SPEC precedent (UNLIKE phase-16 where §(xii) deferred to Task 10 per planner-time decision 14 — phase 17 SPEC commit DID land the §(xiii) paragraph IN FULL); ~+800 LoC across the 8 new ADRs (the SPEC commit already authored §Context drafts for ADR-0148..ADR-0155 totaling ~+520 LoC; PLAN does NOT change these; IMPL adds §Decision + §Consequences bodies adding ~+280 LoC)
- `docs/envoy-go/BEHAVIOR_CONTRACT.md` per SPEC §13 patches — §13.1 NEW `### envoy.filters.http.jwt_authn` subsection (insertion alphabetical-after-header_mutation per ADR-0100 §2.2 — `bandwidth_limit < buffer < compressor < cors < csrf < fault < header_mutation < jwt_authn < local_ratelimit < rbac`; mirrors phase-16 §13.1 cadence but lands in alphabetical position) ~250 + §13.2 stat-table 64→71 names extension (7 new active counter rows; 2 structurally-unreachable noted) ~35 + §13.3 NEW equivalence-matrix row pointing at fixture 0019 ~5 + §13.4 NEW `### Phase 17 forward-pointer notes` subsection ~120 covering the 18-item deferral list (17 deferrals + 1 foot-gun) organized by 8 deferred-cluster families per SPEC §13.4 + §13.7 NEW `## JWKS framework primitive` top-level section ~25 per ADR-0150 + §13.8 NEW `## JWT verifier framework primitive` top-level section ~25 per ADR-0151 = ~+460 LoC
- `docs/envoy-go/ROADMAP.md` row `17` status flip `in-progress → done` + summary sharpening (post-impl counts; PLAN-confirmed 15-task + ~1900-2400 LoC production estimate + final ADR roster 8 anchored) ~+1 net
- `docs/envoy-go/STATE.md` advance per `BOOTSTRAP_PROMPT.md` §5 lifecycle ~rewrite-in-place
- `docs/envoy-go/phases/17-http-filter-jwt-authn/PROGRESS.md` (NEW; lifecycle artefact) ~700
- `docs/envoy-go/phases/17-http-filter-jwt-authn/REVIEW.md` (NEW; lifecycle artefact) ~260

**Production code: ~950-1250 LoC (filter impl in `jwtauthn.go` + `evaluator.go` + `provider.go` + `doc.go`) + ~650-850 LoC framework primitives across 4 files (`internal/jwks/{doc.go,jwks.go}` + `internal/jwt/{doc.go,jwt.go}`) + ~3 LoC main.go + ~105-145 LoC test-helper (`test/helpers/jwksbackend/*`) = ~1708-2248 LoC production + ~2130-3050 LoC tests (~1500-2200 unit jwtauthn + 85 fuzzer + ~300-450 jwks + ~250-400 jwt + ~80 jwksbackend + ~320 fixture driver) + ~1010 LoC fixture YAML/Go/PKI + ~1080 LoC docs (8 ADR bodies + BEHAVIOR_CONTRACT 6 patches + ROADMAP + STATE + PROGRESS + REVIEW) ≈ ~5928-7388 LoC total** (production-only ~1708-2248 LoC; **the high-end estimate crosses the ADR-0045 ~1500 LoC threshold; task count below is **15**, comfortably under the 25-task threshold**). Per the BOOTSTRAP_PROMPT.md §6.1 OR-trigger ("if PLAN.md > ~25 tasks OR > ~1500 LoC estimated → split") strict reading, the LoC leg trips at both the low-end (~1708 already > 1500) and high-end estimates. **PLAN's disposition: SINGLE-ROW** per the phase-13/14/15/16 LoC-borderline precedent (each of those phases stayed single-row despite their respective LoC + task-count estimates being at-or-near the thresholds; phase-16 PLAN's high-end was ~1208-1608 LoC — borderline; phase-17 PLAN's high-end ~2248 LoC is the LARGEST single-row §9-family production-LoC count to date, but the bulk of the surface is in the TWO new framework primitives — `internal/jwks/` ~350-450 LoC + `internal/jwt/` ~300-400 LoC — which are STRUCTURALLY isolated cross-phase-reusable packages; the filter package itself is ~950-1250 LoC). Rationale:

1. **Task count is the load-bearing trigger** for the ADR-0045 split-gate — the 25-task limit reflects mental-model + commit-cadence + review-grain considerations that LoC alone does not capture. 15 tasks is well within the comfortable single-session range (one fewer than phase-16's 16 since the ADR-0125 §(xiii) amendment paragraph ALREADY landed at SPEC commit, eliminating the phase-16-Task-10 analog).

2. **The two framework primitives are STRUCTURALLY isolated** — `internal/jwks/` + `internal/jwt/` live OUTSIDE `internal/filter/` (mirroring phase-16's `internal/matcher/`) explicitly to anchor cross-phase reusability. Each primitive has its own test surface; each builds against pure-Go stdlib (no inter-dependency at the API level — the filter consumes both, but the primitives don't consume each other). Splitting the phase would either ship one primitive WITHOUT its consumer in 17.1 (structurally honest but operationally wasteful — the primitive has no concrete consumer to validate against), or ship both primitives alongside the filter (which is the current shape).

3. **Subagent-driven-development applies cleanly to the two-primitive case** — Tasks 3 (`internal/jwks/`) and 4 (`internal/jwt/`) are INDEPENDENT of each other; they can run in parallel via `superpowers:subagent-driven-development` (preferred per user memory `feedback_execution_style.md`). The filter (Tasks 2, 5, 6, 7, 8, 9) consumes BOTH primitives but sequentially after both are landed. Splitting the phase artificially seams a coherent single-filter implementation across two phases.

4. **Phase-13/14/15/16 LoC-borderline precedent governs** — phase-13 production was ~~500 LoC; phase-14 ~588-678 LoC; phase-15 ~413-503 LoC (the lowest); phase-16 ~2778 LoC actual landed (PLAN-time estimate ~1208-1608 LoC). Phase-17 at ~1708-2248 LoC estimated is between phase-16's PLAN-time estimate and phase-16's actual landed; mirrors phase-16's single-row decision.

5. **ADR-0045 PRECEDENT was phase-05's surface-split** of a much larger phase (~33-40 tasks + ~3450 LoC across H2 codec + cluster integration + conformance suite + fixture) where BOTH legs of the gate tripped firmly. Phase-17 trips only the LoC leg + the task count is firmly under. Phase-05's split was structural; phase-17's single-row is structural.

The 8 anticipated ADRs (ADR-0148..ADR-0155) all have their `Lands-in-task` anchors set in the table at `## ADRs introduced by this plan` below per ADR-0044 ADR-on-impl convention (phase-13/15/16 precedent; phase-14's SPEC-time-pre-landing of ADR-0129..ADR-0133 is the divergent precedent). **ADR-0125 §(xiii) amendment paragraph ALREADY landed at SPEC commit `9070490`** per phase-13/14/15/16 in-place-amend-at-SPEC precedent — verified via `grep -nE '\(xiii\)' docs/envoy-go/DECISIONS.md` returning at least 1 match at master tip `75fdc2d`; **NO PLAN-time re-anchor needed** (UNLIKE phase-16 where §(xii) deferred to Task 10 per planner-time decision 14).

The natural ADR-0045 release-valve split per BRAINSTORM §1.4 / SPEC §10 (deferred-to-PLAN-time per SPEC §10 NOT-INVOKED decision) would be `17.1 = LocalJwks-only + JWT verifier + Full-6 + 8th-canonical + listener-level rules` (~800-1000 LoC production) and `17.2 = RemoteJwks + JWKS cache + scheduled refresh + retry/backoff + JWKS framework primitive` (~700-900 LoC production); PLAN explicitly rejects the split per rationale points 2-3 above (the JWKS fetcher is cross-phase-reusable framework primitive whose consumer is the filter — shipping fetcher without consumer in 17.2 is structurally awkward) and ships single-row.

---

## File structure (decomposition decisions locked in here)

| File | Status | Responsibility |
|---|---|---|
| `internal/filter/http/jwtauthn/doc.go` | NEW | Package doc enumerating: (a) typed_config surface (6 listener fields per §1.1 amendment 1 — `providers` + `rules` + `bypass_cors_preflight` + `requirement_map` + `strip_failure_response` + 1 silent-ignored `filter_state_rules`); (b) 13-of-21 JwtProvider fields consumed per §1.1 amendments 2 + 3 (issuer + audiences + remote_jwks/local_jwks oneof + forward + from_headers + from_params + from_cookies + forward_payload_header + pad_forward_payload_header + claim_to_headers + clear_route_cache + clock_skew_seconds; 8 silent-ignored: 4 dynamic-metadata family + jwt_cache_config + 3 v1.37.x claim-coverage extensions); (c) 6-variant JwtRequirement evaluator per §11.P16; (d) RS+ES algorithm allow-list (6 algorithms); (e) all-4 token extraction sources + iteration order + first-match-wins; (f) full-header-side post-validation side-effects (strip → forward_payload_header → claim_to_headers → clear_route_cache); (g) per-route TPFC `PerRouteConfig` wrapper proto **8th canonical per-route pattern** (oneof{disabled(bool) \| requirement_name(string)} with string-reference-delegation; per ADR-0125 §(xiii) landed at SPEC commit); per-route stats SHARED per ADR-0154 (mirrors phase-12/13/14 SHARED; DIVERGES from phase-11/15/16 INDEPENDENT); (h) public API surface (`TypeURL` const, `New` HTTPFilterFactory); (i) iteration protocol (DECODER-only: DecodeHeaders dispatch + SendLocalReply 401/403 + DecodeData/Trailers pass-through + OnDestroy no-op); (j) wire-shape divergence-windows from reference Envoy — `response_code_details` field-emission absent (envoy-go MVP; Envoy emits per amendment 11) / `filter_state_rules` silent-ignored (envoy-go MVP; Envoy enforces per amendment 1) / dynamic-metadata family silent-ignored / `jwt_cache_config` no-cache / 3 v1.37.x claim-coverage extensions silent-ignored / `clear_route_cache` implicit-on-side-effect trigger silent-ignored; (k) cross-cutting ADR anchors (ADR-0148 / ADR-0149 / ADR-0150 / ADR-0151 / ADR-0152 / ADR-0153 / ADR-0154 / ADR-0155 + ADR-0125 §(xiii)). Mirrors phase-16 `rbac/doc.go` shape adapted for the JWT validation surface. ~30 LoC. Per SPEC §1 + §6.1. |
| `internal/filter/http/jwtauthn/jwtauthn.go` | NEW | Main filter file — public surface + factory + types + DecodeHeaders body + filterStats. **Public surface** (per SPEC §6.1): `TypeURL = "type.googleapis.com/envoy.extensions.filters.http.jwt_authn.v3.JwtAuthentication"`; `New(tc *anypb.Any, ctx envoyhttp.FactoryCtx) (envoyhttp.FilterInstanceFactory, error)` factory. **Internal package consts:** `filterName = "envoy.filters.http.jwt_authn"`. **Unexported types** (per SPEC §6.2 + §6.3): `compiledConfig` (8 fields per §6.2: `providers map[string]*compiledProvider` + `rules []*compiledRule` + `requirementMap map[string]*compiledRequirement` + `bypassCorsPreflight bool` + `stripFailureResponse bool` + `stats *filterStats`); `compiledProvider` (13 consumed fields per §1.1 amendments 2 + 7: `issuer string` + `audiences []string` + `jwksFetcher *jwks.Fetcher` (RemoteJwks; non-nil exclusive with localJwks) + `localJwks *jwt.JWKSet` (LocalJwks; non-nil exclusive with jwksFetcher) + `forward bool` + `fromHeaders []headerLoc` + `fromParams []string` + `fromCookies []string` + `forwardPayloadHeader string` + `padForwardPayloadHdr bool` + `claimToHeaders []claimToHeader` + `clearRouteCache bool` + `clockSkew time.Duration` with default 60s per §1.1 amendment 7); `headerLoc` (2 fields: `name string` + `valuePrefix string`); `claimToHeader` (2 fields: `headerName string` + `claimName string`); `compiledRule` (2 fields: `matcher *router.RouteMatcher` reusing phase-04 RouteMatch evaluator per §11.P9 + `requirement *compiledRequirement` resolved from inline `requires` OR `requirement_map[name]` per §1.1 amendment 4); `compiledRequirement` (4 fields per §6.2 + §11.P16: `kind requirementKind` enum + `provider *compiledProvider` + `audOverr []string` + `children []*compiledRequirement`); `requirementKind` enum const block (6 values: `reqProviderName` / `reqProviderAndAudiences` / `reqRequiresAny` / `reqRequiresAll` / `reqAllowMissing` / `reqAllowMissingOrFailed`); `compiledPerRoute` (2 fields per §5.1: `disabled bool` + `requirementName string`); `factoryState` (2 fields per IMPL-1 pattern simplified for SHARED-stats: `listenerRC *compiledConfig` + `perRoute sync.Map` keyed by `*PerRouteConfig` proto pointer); `filter` (5 fields: `state *factoryState` + `dcb envoyhttp.DecoderFilterCallbacks` + `activeRC *compiledConfig` (always listener-level since per-route is delegation-only) + `perRoute *compiledPerRoute` + `passthrough bool` + `originalURI string` captured at DecodeHeaders for WWW-Authenticate realm per §1.1 amendment 12); `filterStats` (7 counters per §1.1 amendment 9 + §11.P6: `allowed` + `denied` + `corsPreflightBypassed` + `jwksFetchSuccess` + `jwksFetchFailed` + `jwtCacheHit` + `jwtCacheMiss` — last 2 STRUCTURALLY UNREACHABLE under MVP per §8 deferral 8). **Helpers** (per SPEC §6.4-§6.6 + §6.11 + §6.12): `buildCompiledConfig(c, ctx)` (parses providers map + requirement_map + rules list; silent-ignores filter_state_rules per §1.1 amendment 1; registers 7 base counters via `newFilterStats(ctx.Stats)` when `ctx.Stats != nil`); `buildCompiledProvider(name, p)` (parses 13 consumed fields; silent-ignores 8 fields; durationOrDefault for clock_skew_seconds; JWKS source oneof dispatch — `RemoteJwks` → `jwks.New(uri, cacheDur, asyncFetch, retryPolicy)`; `LocalJwks` → `readDataSource` + `jwt.ParseJWKSet(raw)`; neither set → PARSE-REJECT); `buildCompiledRequirement(r, providers)` (recursive 6-variant switch; provider_name lookup; provider_and_audiences with audience override; requires_any/all recursive children; allow_missing / allow_missing_or_failed terminal; default to reqAllowMissingOrFailed for nil requirement per `RequirementRule.requires` proto comment); `buildCompiledRule(r, providers, reqMap)` (parses `match` via `router.NewRouteMatcher` per §11.P9 REQUIRED; resolves `requirement_type` oneof — `Requires` → recursive buildCompiledRequirement, `RequirementName` → reqMap lookup with PARSE-REJECT on miss per §1.1 amendment 4 listener-level discipline, nil → reqAllowMissingOrFailed); `parsePerRoute(any)` (unmarshals to `*jwt_authnv3.PerRouteConfig`; PGV-mirror requirement_specifier-required + requirement_name min_len=1); `(s *factoryState) resolvePerRouteConfig(msg)` (lazy-cache + LoadOrStore mirroring phase-11/15/16 pattern; returns lightweight `*compiledPerRoute` per §6.12); `buildCompiledPerRoute(pr)` (3 cases per §5.1: disabled-true / disabled-false / requirement_name); `newFilterStats(reg)` (registers 7 counters under SN2-reuse namespace; nil-tolerance per ADR-0085); `durationOrDefault(v, def)` (helper for clock_skew_seconds default fallback). **DecodeHeaders body** (per SPEC §6.6): capture `:path` → resolve per-route → case (a) disabled=true → passthrough HeaderContinue; CORS preflight bypass check; resolve requirement (per-route requirement_name OR listener-level rules first-match; per-route runtime-resolve miss → 403 + "Failed JWT authentication: Wrong requirement_name: <name>" per §1.1 amendment 6); nil requirement → HeaderContinue pass-through; otherwise `evaluateRequirement` from evaluator.go → `applyResult` from provider.go. **DecodeData / DecodeTrailers / OnDestroy** — pass-through / no-op per SPEC §6.11. **SetDecoderCallbacks** stores `f.dcb = cb`. ~450-550 LoC. |
| `internal/filter/http/jwtauthn/evaluator.go` | NEW | Token extraction + JwtRequirement evaluator surface — the algorithmic core per SPEC §6.7 + §6.8. **`evalResult` struct** (4 fields: `allowed bool` + `err error` + `token *jwt.Token` + `provider *compiledProvider`). **`extractTokens(provider, headers) []extractedToken`** per SPEC §6.7 + §11.P14 + §11.P15: (1) configured from_headers in declared order with value_prefix substring-search via `strings.Index`; (2) default Authorization Bearer + access_token query param when no explicit extraction-sources set; (3) configured from_params case-sensitive URL-decoded first-value-only via `parseQueryParam`; (4) configured from_cookies case-sensitive verbatim value via `parseCookies`. **`extractedToken` struct** (3 fields: `raw string` + `src sourceKind` + `name string`). **`sourceKind` enum** (3 values: sourceHeader / sourceParam / sourceCookie). **`parseQueryParam(path, name) ([]string, bool)`** (uses Go stdlib `url.ParseQuery` for URL-decode + case-sensitive name match; returns slice of values + bool ok). **`parseCookies(cookieHeader) map[string]string`** (uses Go stdlib `net/http.Request.Cookies()` semantic; case-sensitive name keys + verbatim values per §11.P15). **`isCorsPreflightRequest(headers)`** per §11.P1 + filter.cc verbatim: `:method == OPTIONS && origin != "" && access-control-request-method != ""`. **`stripNonBase64URLChars(s string) string`** (helper for value_prefix-extracted JWT bytes — trims trailing whitespace/separators per Envoy extractor.cc). **`(f *filter) evaluateRequirement(req, headers) evalResult`** — 6-variant switch per SPEC §6.8 + §11.P16: `reqProviderName` → `evaluateProvider(req.provider, headers, req.provider.audiences)`; `reqProviderAndAudiences` → `evaluateProvider(req.provider, headers, req.audOverr)`; `reqRequiresAny` → OR-semantic short-circuit on first allowed=true, otherwise return last-failure err (per Envoy verifier.cc); `reqRequiresAll` → AND-semantic short-circuit on first allowed=false; `reqAllowMissing` → iterate all providers' extraction-sources, if any token extracted MUST validate (first-success-wins; present-and-invalid → fail), else missing-OK (per §11.P16 RATIFIED-AND-EXTENDED + SPEC §12.4 planner-time decision); `reqAllowMissingOrFailed` → always allowed=true. **`(f *filter) evaluateProvider(p, headers, effectiveAudiences) evalResult`** per SPEC §6.8: extract tokens for provider's source set; if empty → `evalResult{allowed: false, err: jwt.ErrJwtMissed}`; iterate each extracted token (first-success-wins): `jwt.Parse(raw)` → resolve key via `p.localJwks` OR `p.jwksFetcher.Get(ctx)` (with `jwksFetchSuccess/jwksFetchFailed` counter increments per §11.P6); `keyset.Lookup(t.Kid, t.Alg)` → `t.VerifySignature(key, t.Alg)` → `t.ValidateClaims({Issuer, Audiences, ClockSkew, Now})` with effectiveAudiences override per provider_and_audiences requirement; on success return `evalResult{allowed: true, token: t, provider: p}`. ~300-400 LoC. |
| `internal/filter/http/jwtauthn/provider.go` | NEW | compiledProvider helpers + side-effect emit-order + deny-path wire shape per SPEC §6.5 + §6.9 + §4. **`readDataSource(ds *envoycorev3.DataSource) ([]byte, error)`** (parses LocalJwks DataSource oneof: inline_string → []byte conversion; inline_bytes → verbatim bytes; filename → os.ReadFile; environment_variable → os.Getenv; envoy-go-side PGV mirror for unset oneof). **`parseRetryPolicy(rp *envoycorev3.RetryPolicy) *jwks.RetryPolicy`** (parses inner-HTTP-request RetryPolicy per §11.P4: num_retries with default 1 per Envoy proto comment + base_interval default 1s + max_interval default 10*base). **`(f *filter) applyResult(r evalResult, headers, req) FilterHeadersStatus`** per SPEC §6.9: on `!r.allowed` → `denied +1` + `emitDenyResponse(r.err)` + HeaderStopIteration; on allowed → `allowed +1` + `applySideEffects(r.token, r.provider, headers)` + HeaderContinue. **`(f *filter) applySideEffects(t, p, headers)`** — 4-step emit-order per SPEC §6.9 + §11.P10 + §11.P13: (1) strip-on-success (`!p.forward`) → `stripExtractionSources(headers, p, t)` strips Authorization/from_headers/from_params values; from_cookies UNTOUCHED per proto caveat; (2) `forward_payload_header` non-empty → `encodeBase64URL(t.RawPayload, p.padForwardPayloadHdr)` + `headers.Set(p.forwardPayloadHeader, encoded)`; (3) for each `claim_to_headers[]`: `t.PayloadClaim(ch.claimName)` dot-notation extraction; silent-skip on err (array-claim rejection or missing claim); `stringify(val)` coerce + `headers.Set(ch.headerName, s)`; (4) `clear_route_cache: true` → `f.dcb.ClearRouteCache()` per phase-10 ADR-0108 precedent. **`(f *filter) emitDenyResponse(reason)`** per SPEC §6.9 + §4 + §1.1 amendments 8 + 11 + 12: `code = mapStatusToHTTPCode(reason)` (401 default; 403 for `jwt.ErrJwtAudienceNotAllowed`); `f.activeRC.stripFailureResponse` branch: body=`""` + nil headers; else: body=reason.Error() + WWW-Authenticate header `Bearer realm="<f.originalURI>"` + conditional `, error="invalid_token"` for non-`ErrJwtMissed` per §1.1 amendment 12 + content-type: text/plain header; `f.dcb.SendLocalReply(code, body, hdrs)`. **`mapStatusToHTTPCode(reason) int`** per §1.1 amendment 8: `errors.Is(reason, jwt.ErrJwtAudienceNotAllowed)` → 403; else → 401. **`encodeBase64URL(raw string, pad bool) string`** per §11.P13 + SPEC §6.9: if `pad == false` (default), strip trailing `=` chars; else preserve. **`stripExtractionSources(headers, p, t)`** per SPEC §6.9 + JwtProvider.forward proto comment + planner-time decision 8: strip default Authorization Bearer if extracted; strip from_headers configured value; strip from_params query-param value (via `:path` rewrite using `url.ParseQuery` + re-serialize); from_cookies UNTOUCHED per proto caveat. **`stringify(val interface{}) (string, bool)`** per §11.P10: scalar coerce to string for int/float/bool/string; array → false (silent skip per Envoy tolerance). ~200-300 LoC. |
| `internal/filter/http/jwtauthn/jwtauthn_test.go` | NEW | Unit tests per SPEC §14.1 (11 SPEC-named groups + 1 stats-namespace integration sub-group surfaced at PLAN-time per planner-time decision 14). **Group 1 — Config parsing (per SPEC §14.1 #1 + §1.1 amendments 1-7):** ~18 cases covering JwtAuthentication 6-field outer-envelope (filter_state_rules silent-ignored at parse per amendment 1); JwtProvider 21-field surface (subjects + require_expiration + max_lifetime silent-ignored per amendment 3); PerRouteConfig.disabled bool form (NOT Empty per amendment 5); clock_skew_seconds default 60s consumed per amendment 7; 7-counter filterStats; cors_preflight_bypassed naming per amendment 10; envoy-go-side defensive PGV-mirror validation per §11.P9 (PerRouteConfig.requirement_specifier required; RemoteJwks.http_uri required; RequirementRule.match required; PerRouteConfig.requirement_name min_len=1); inline `RequirementRule.requires` honored proto-faithful per amendment 4 — REFUTES BRAINSTORM PARSE-REJECT-deprecated hypothesis. **Group 2 — Token extraction (per SPEC §14.1 #2 + §6.7 + §11.P14 + §11.P15):** ~12 cases covering default Authorization Bearer (when no explicit extraction-sources); default access_token query param; configured from_headers with value_prefix substring-search; configured from_params case-sensitive URL-decoded first-value-only per §11.P14; configured from_cookies case-sensitive verbatim per §11.P15; iteration order (headers → params → cookies); first-match-wins discipline; multi-value query first-value-only. **Group 3 — JWT parse + signature verify (per SPEC §14.1 #3 + `internal/jwt` API):** ~15 cases — covered fully in `internal/jwt/jwt_test.go` (Task 4); jwtauthn-side smoke tests verify Parse + VerifySignature integration via the filter's extraction → validation pipeline; ~3 integration smoke tests. **Group 4 — Claim validation (per SPEC §14.1 #4 + §6.8 + §11.P16):** ~10 cases — covered fully in `internal/jwt/jwt_test.go` (Task 4); jwtauthn-side smoke tests verify ValidateClaims integration; ~3 integration smoke tests. **Group 5 — Requirement evaluator (per SPEC §14.1 #5 + §6.8 + §11.P16):** ~12 cases covering provider_name simple case; provider_and_audiences with per-rule audience override; requires_any with short-circuit OR + last-failure-status; requires_all with short-circuit AND; allow_missing JWT absent → OK; allow_missing JWT present-and-invalid → FAIL; allow_missing_or_failed any outcome → OK; recursive combinators (requires_any inside requires_all + vice versa; 3 levels deep); empty requirement (proto nil) → reqAllowMissingOrFailed. **Group 6 — Side-effect emit-order (per SPEC §14.1 #6 + §6.9 + §11.P10 + §11.P13):** ~10 cases covering strip-on-success (forward=false) Authorization header removed; strip from_headers values; strip from_params query params (path rewrite); from_cookies UNTOUCHED per proto caveat; forward_payload_header base64url-encoded payload emitted with pad=true; same emitted with pad=false (no `=`); claim_to_headers string + numeric + bool claims emitted; claim_to_headers array claims silently skipped per §11.P10; nested-claim dot-notation extraction; clear_route_cache → cb.ClearRouteCache() invoked. **Group 7 — Per-route 8th canonical (per SPEC §14.1 #7 + §5 + §6.12):** ~9 cases covering `PerRouteConfig{disabled: true}` wholesale passthrough + NO counters per §5.1 case (a); `PerRouteConfig{disabled: false}` falls through to listener-level rules dispatch per §5.1 case (b); `PerRouteConfig{requirement_name: "<valid-name>"}` requirement_map resolved + named requirement applied per §5.1 case (c); `PerRouteConfig{requirement_name: "<dangling-name>"}` runtime-resolve fail → 403 + "Failed JWT authentication: Wrong requirement_name: <name>" + denied counter increment per §1.1 amendment 6; parsePerRoute PGV-mirror requirement_specifier-required; parsePerRoute requirement_name min_len=1; lazy-cache LoadOrStore identity by `*PerRouteConfig` pointer; concurrent resolvePerRouteConfig produces ONE compiledPerRoute per proto pointer. **Group 8 — Deny-path wire shape (per SPEC §14.1 #8 + §4 + §1.1 amendments 8 + 11 + 12):** ~14 cases covering 401 for JwtMissed body byte-exact `"Jwt is missing"` 14 bytes + WWW-Authenticate `Bearer realm="<original_uri>"` WITHOUT error param per §1.1 amendment 12 JwtMissed case; 401 for JwtExpired body byte-exact `"Jwt is expired"` 14 bytes + WWW-Authenticate with `, error="invalid_token"`; 401 for JwtVerificationFail body byte-exact `"Jwt verification fails"` 22 bytes + WWW-Authenticate; 401 for JwtUnknownIssuer body byte-exact `"Jwt issuer is not configured"` 28 bytes; **403 for JwtAudienceNotAllowed** body byte-exact `"Audiences in Jwt are not allowed"` 32 bytes per §1.1 amendment 8 — REFINES BRAINSTORM "always 401"; 401 for JwtBadFormat; 401 for JwtHeaderNotImplementedAlg; `strip_failure_response: true` → body empty + NO www-authenticate header per §11.P3; per-route runtime-resolve fail → 403 + "Failed JWT authentication: Wrong requirement_name: ..." + NO WWW-Authenticate header per §1.1 amendment 6; 4-header standard set lowercase wire-form (`content-length: <body length>`, `content-type: text/plain`, `date`, `server: envoy`); keep-alive disposition NO connection: close; original_uri capture at DecodeHeaders before route mutation. **Group 9 — CORS preflight bypass (per SPEC §14.1 #9 + §11.P1):** ~4 cases covering OPTIONS + Origin + Access-Control-Request-Method → 200 passthrough + cors_preflight_bypassed counter; OPTIONS without Origin → NOT bypassed (falls through to JWT validation); GET request even with Origin → NOT bypassed; bypass_cors_preflight=false → OPTIONS preflight goes to JWT validation. **Group 10 — RemoteJwks lifecycle integration (per SPEC §14.1 #10 + §6.10 + §11.P4 + §11.P5):** ~7 cases — covered fully in `internal/jwks/jwks_test.go` (Task 3); jwtauthn-side smoke tests verify Fetcher.Get integration with provider validation pipeline; ~3 integration smoke tests covering provider with RemoteJwks fetches JWKS at first request + jwks_fetch_success counter increment; provider with RemoteJwks fetch failure → jwks_fetch_failed counter increment + JwksFetchFail propagation. **Group 11 — `compiledPerRoute` lazy-cache (sync.Map LoadOrStore semantics):** ~3 cases covering identity-keyed by *PerRouteConfig proto pointer; concurrent resolvePerRouteConfig calls produce ONE compiledPerRoute per proto pointer; cache hit on subsequent requests with same per-route proto pointer. **Group 12 — Stats namespace integration (per planner-time decision 14 + ADR-0154):** ~5 cases covering all 7 base counters registered at New() time (unconditional non-lazy per SPEC §6.5); SN2-reuse no new SN10 rule per §11.P7; HCM-rooted path `http.<HCM>.jwt_authn.<counter>`; cors_preflight_bypassed canonical naming per amendment 10; jwt_cache_hit + jwt_cache_miss STRUCTURALLY UNREACHABLE under MVP (registered counters but never incremented since jwt_cache_config silent-ignored). Test helpers `mustAny(t, msg)` + `freshFactoryCtx()` + `freshFactoryCtxWithRegistry()` + `freshFilter(t, cfg)` + `signTokenRS256(t, claims, key)` + `signTokenES256(t, claims, key)` mirror phase-13/14/15/16 precedents. ~1500-2200 LoC. |
| `internal/filter/http/jwtauthn/fuzz_test.go` | NEW | `FuzzJwtAuthnConfigParse` — fuzzes arbitrary byte sequences as the `tc *anypb.Any` parameter to `New`. Asserts: `New` returns either `(factory, nil)` OR `(nil, error)`; never panics; never returns `(nil, nil)`. **Fuzzer axes**: random bytes / partial proto-shaped bytes / valid proto with random JwtProvider variants (including v1.37.x claim-coverage extensions silent-ignored) / valid proto with random RequirementRule variants / valid proto with all 6 JwtRequirement oneof variants including deeply-nested requires_any/all recursion / per-route TPFC variants (disabled-true / disabled-false / requirement_name with valid + dangling names) / filter_state_rules set (silent-ignored at parse). Per ADR-0018 "every parser/codec/filter ships a fuzzer". ~85 LoC; 30s budget per ADR-0018; **21st fuzzer overall** (post-phase-16's 20th `FuzzRBACConfigParse`). Seed corpus per SPEC §14.3: seed 0 minimal valid (one provider with LocalJwks + one rule); seed 1 provider with all 13 consumed fields; seed 2 provider with all 8 silent-ignored fields; seed 3 RemoteJwks + async_fetch + retry_policy; seed 4 all 4 extraction sources; seed 5 claim_to_headers nested dot-notation; seed 6 inline `requires` (honored per amendment 4); seed 7 requirement_name → requirement_map resolution; seed 8 all 6 JwtRequirement variants + recursive nesting; seed 9 filter_state_rules set; seed 10 bypass_cors_preflight + strip_failure_response both true; seed 11 per-route disabled: true; seed 12 per-route requirement_name (valid name). |
| `internal/jwks/doc.go` | NEW | Package doc for the NEW top-level package per ADR-0150: `// Package jwks implements an HTTP-outbound JWKS fetcher with async background refresh + thread-safe cache for jwt_authn's RemoteJwks endpoints. The package is cross-phase reusable: future filters consuming outbound-HTTP-from-filter primitives (e.g., ext_authz HTTP-mode, oauth2 token-endpoint flows) compose against the Fetcher type's http.Client + retry-policy structure. Lifecycle: one Fetcher per RemoteJwks configured in a JwtProvider; New() either blocks on initial fetch (fast_listener=false) or activates listener immediately (fast_listener=true); Get(ctx) returns cached JWKSet or ErrJwksNotReady; Close() stops the background refresh goroutine.` ~30 LoC. |
| `internal/jwks/jwks.go` | NEW | `Fetcher` opaque type wrapping URI + cache duration + AsyncFetch + RetryPolicy per ADR-0150 + SPEC §3.1. **Public API:** `Fetcher` opaque type; `New(uri string, cacheDuration time.Duration, asyncFetch *AsyncFetch, retryPolicy *RetryPolicy) (*Fetcher, error)` constructor (blocking or non-blocking initial fetch per `fast_listener`); `(f *Fetcher) Get(ctx context.Context) (*JWKSet, error)` returns cached value or `ErrJwksNotReady` for fast_listener-mode-before-completion; `(f *Fetcher) Close() error` stops background refresh; `AsyncFetch` config struct (FastListener bool + FailedRefetchDuration time.Duration); `RetryPolicy` config struct (NumRetries int + BaseInterval time.Duration + MaxInterval time.Duration); `JWKSet` parsed JWK Set type; `(s *JWKSet) Lookup(kid, alg string) (crypto.PublicKey, error)` returns matching key via Envoy's pickKeyAlgWithKid logic; `ParseJWKSet(raw []byte) (*JWKSet, error)` parses RFC 7517 §5 JWK Set JSON. **Internal structure:** `*Fetcher` carries `mu sync.Mutex` + `keys *JWKSet` + `ready chan struct{}` + `notReadyErr error` + `refreshTimer *time.Timer` + `client *http.Client` + `closed atomic.Bool`. **Refresh schedule** per §11.P5: successful fetch → schedule next refresh at `cacheDuration - 5s` via `time.AfterFunc`; failed fetch → schedule next retry at `failedRefetchDuration` (default 1s); `fast_listener: true` → initial fetch is non-blocking; `fast_listener: false` (default) → initial fetch is blocking at New() time. **Errors** (per §11.P1 JWKS subset): `ErrJwksFetchFail` + `ErrJwksParseError` + `ErrJwksKidAlgMismatch` + `ErrJwksNotReady` + `ErrJwksNoValidKeys` + ~7 more. **JWK Set JSON-parse helpers**: reads `keys[]` array elements with `kty` (RSA or EC) + `use` (sig) + `kid` + `alg` + `n`/`e` (RSA) or `crv`/`x`/`y` (EC); converts to `*rsa.PublicKey` (via base64url-decoded modulus + exponent → `big.Int`) or `*ecdsa.PublicKey` (via base64url-decoded coordinates → `elliptic.Curve` lookup by `crv`); rejects malformed keys with `ErrJwksParseError`. ~350-450 LoC. |
| `internal/jwks/jwks_test.go` | NEW | Unit tests per ADR-0150 acceptance + SPEC §14.1 Group 10. ~25-35 test cases covering: `TestNew_BlockingInitialFetch_Success` (fast_listener=false; initial fetch succeeds; New returns nil error); `TestNew_BlockingInitialFetch_Failure_ReturnsError` (fast_listener=false; initial fetch fails; New returns error per §6.10 + SPEC §12.3 planner-time decision); `TestNew_NonBlockingInitialFetch_ReturnsImmediately` (fast_listener=true; New returns immediately); `TestGet_BlockingMode_ReturnsCachedAfterInit` (Get returns cached JWKSet); `TestGet_NonBlockingMode_BeforeFetchCompletes_ReturnsErrJwksNotReady` (Get returns ErrJwksNotReady until background fetch completes); `TestGet_NonBlockingMode_AfterFetchCompletes_ReturnsCached` (Get returns cached after background fetch); `TestRefresh_FiresAtCacheDurationMinus5s` (refresh schedule per §11.P5); `TestFailedRefetch_FiresAtFixedInterval` (default 1s; configurable via FailedRefetchDuration); `TestFailedRefetch_NotExponentialBackoff` (refute BRAINSTORM exponential hypothesis per §11.P4); `TestJWKSetLookup_KidMatch_Success` (kid found in JWKS); `TestJWKSetLookup_KidMiss_AlgFallback_Success` (Envoy's pickKeyAlgWithKid logic); `TestJWKSetLookup_NoMatch_ErrJwksKidAlgMismatch`; `TestParseJWKSet_RSAKey_Success`; `TestParseJWKSet_ECDSAKey_P256_Success`; `TestParseJWKSet_ECDSAKey_P384_Success`; `TestParseJWKSet_ECDSAKey_P521_Success`; `TestParseJWKSet_MalformedJSON_ErrJwksParseError`; `TestParseJWKSet_MissingKeysArray_ErrJwksNoValidKeys`; `TestParseJWKSet_UnsupportedKty_ErrJwksParseError`; `TestClose_StopsRefreshGoroutine` (no goroutine leak after Close); `TestClose_Idempotent`; `TestConcurrent_GetAndRefresh_NoRace` (run under -race); `TestRetryPolicy_InnerHTTPRequest_HonoredByClient`. Test helpers `httptest.NewServer` for in-process JWKS-serving + `withMockTime(t, t time.Time)` for clock injection. ~300-450 LoC. |
| `internal/jwt/doc.go` | NEW | Package doc for the NEW top-level package per ADR-0151: `// Package jwt implements a JWS/JWT parser + signature verifier + claim validator for jwt_authn's JWT validation pipeline. The package is cross-phase reusable: future filters consuming JWT semantics (e.g., jwt_claim_router routing on claim values, oauth2 token validation) compose against Parse + Token.VerifySignature + Token.ValidateClaims + Token.PayloadClaim directly. Algorithm allow-list: RS256/384/512 + ES256/384/512 (6 algorithms; HS family + EdDSA + none + PS family deferred). Claim validation order: exp + nbf + iss + aud with clock-skew tolerance. ~20 canonical error sentinels mirror jwt_verify_lib status codes per §11.P1.` ~30 LoC. |
| `internal/jwt/jwt.go` | NEW | JWS/JWT verifier framework primitive per ADR-0151 + SPEC §3.2. **Public API:** `Token` type with `RawHeader` + `RawPayload` + `RawSignature` (string base64url) + `Header` map[string]interface{} (decoded JSON) + `Payload` map[string]interface{} + `Alg` string + `Kid` string; `Parse(raw string) (*Token, error)` parses 3-part `header.payload.signature` structure with `JwtBadFormat` rejection per §11.P1; `(t *Token) VerifySignature(key crypto.PublicKey, alg string) error` RS+ES allow-list dispatch via `crypto/rsa.VerifyPKCS1v15` (for RS256/384/512) + `crypto/ecdsa.Verify` (for ES256/384/512; r and s as `*big.Int` from signature byte-split); `ValidateOptions` struct (Issuer string + Audiences []string + ClockSkew time.Duration + Now time.Time + RequireExpiration bool + MaxLifetime time.Duration + Subjects *StringMatcher — last 3 silent-ignored per §1.1 amendment 3); `(t *Token) ValidateClaims(opts ValidateOptions) error` checks exp + nbf + iss + aud per ValidateOptions with clock-skew tolerance; `(t *Token) PayloadClaim(path string) (interface{}, error)` dot-notation extractor for nested claims per §11.P10 — array claims rejected via `ErrArrayClaim`; `StringMatcher` placeholder type (silent-ignored per amendment 3). **Errors** per §11.P1 RATIFIED-AND-EXTENDED jwt_verify_lib status table — ~20 sentinels: `ErrJwtMissed` ("Jwt is missing") + `ErrJwtBadFormat` + `ErrJwtHeaderBadAlg` + `ErrJwtHeaderNotImplementedAlg` + `ErrJwtHeaderParseErrorBadBase64` + `ErrJwtHeaderParseErrorBadJson` + `ErrJwtPayloadParseErrorBadBase64` + `ErrJwtPayloadParseErrorBadJson` + `ErrJwtSignatureParseErrorBadBase64` + `ErrJwtUnknownIssuer` ("Jwt issuer is not configured") + `ErrJwtAudienceNotAllowed` ("Audiences in Jwt are not allowed") + `ErrJwtVerificationFail` ("Jwt verification fails") + `ErrJwtExpired` ("Jwt is expired") + `ErrJwtNotYetValid` ("Jwt not yet valid") + `ErrArrayClaim` ("claim is array-valued; not supported"). **Internal helpers:** `base64UrlDecode` via `base64.RawURLEncoding.DecodeString`; `parseHeaderJSON` + `parsePayloadJSON` via `encoding/json.Unmarshal`; `algSigSize(alg)` maps RS/ES alg to signature byte size for split. ~300-400 LoC. |
| `internal/jwt/jwt_test.go` | NEW | Unit tests per ADR-0151 acceptance. ~30-40 test cases covering: `TestParse_ValidJWT_Success` (3 parts; decoded header + payload + alg + kid); `TestParse_NotThreeParts_ErrJwtBadFormat` (1 part, 2 parts, 4 parts); `TestParse_HeaderInvalidBase64_ErrJwtHeaderParseErrorBadBase64`; `TestParse_PayloadInvalidBase64_ErrJwtPayloadParseErrorBadBase64`; `TestParse_SignatureInvalidBase64_ErrJwtSignatureParseErrorBadBase64`; `TestParse_HeaderInvalidJSON_ErrJwtHeaderParseErrorBadJson`; `TestParse_PayloadInvalidJSON_ErrJwtPayloadParseErrorBadJson`; `TestParse_AlgFieldMissing_ErrJwtHeaderBadAlg`; `TestVerifySignature_RS256_Success` (positive case via test PKI); `TestVerifySignature_RS384_Success`; `TestVerifySignature_RS512_Success`; `TestVerifySignature_ES256_Success`; `TestVerifySignature_ES384_Success`; `TestVerifySignature_ES512_Success`; `TestVerifySignature_TamperedSignature_ErrJwtVerificationFail`; `TestVerifySignature_AlgOutsideAllowList_ErrJwtHeaderNotImplementedAlg` (HS256, EdDSA, none, PS256); `TestVerifySignature_WrongKeyForAlg_ErrJwtVerificationFail` (RSA key with ES alg); `TestValidateClaims_ExpInFuture_Success`; `TestValidateClaims_ExpInPast_ErrJwtExpired`; `TestValidateClaims_ExpWithinClockSkew_Success` (default 60s tolerance); `TestValidateClaims_NbfInPast_Success`; `TestValidateClaims_NbfInFuture_ErrJwtNotYetValid`; `TestValidateClaims_NbfWithinClockSkew_Success`; `TestValidateClaims_IssuerExact_Success`; `TestValidateClaims_IssuerEmpty_SkipsCheck`; `TestValidateClaims_IssuerMismatch_ErrJwtUnknownIssuer`; `TestValidateClaims_AudienceIntersection_Success` (OR-semantic per §6.8); `TestValidateClaims_AudienceEmpty_SkipsCheck`; `TestValidateClaims_AudienceMismatch_ErrJwtAudienceNotAllowed`; `TestPayloadClaim_TopLevelString_Success`; `TestPayloadClaim_NestedDotNotation_Success` ("user.email" traverses `payload[user][email]`); `TestPayloadClaim_ScalarTypes_AllSupported` (string, int, float, bool); `TestPayloadClaim_ArrayClaim_ErrArrayClaim` per §11.P10; `TestPayloadClaim_MissingPath_ReturnsNotFound`; `TestValidateOptions_RequireExpiration_SilentIgnoredPerAmendment3` (verify the field is accepted but never enforced); `TestValidateOptions_MaxLifetime_SilentIgnoredPerAmendment3`; `TestValidateOptions_Subjects_SilentIgnoredPerAmendment3`. Test helpers `freshRS256Key(t)` + `freshES256Key(t)` + `signTokenRS256(t, claims, key, kid)` + `signTokenES256(t, claims, key, kid)` for PKI generation + token assembly. ~250-400 LoC. |
| `cmd/envoy-go/main.go` | MODIFIED | NEW one-line `httpReg.Register(jwtauthn.TypeURL, jwtauthn.New)` registration inserted IMMEDIATELY AFTER the existing `httpReg.Register(header_mutation.TypeURL, header_mutation.New)` line and BEFORE the existing `httpReg.Register(localratelimit.TypeURL, localratelimit.New)` line per ADR-0100 §2.2 + ADR-0148 §Decision alphabetical-after-header_mutation discipline. Plus the matching `import "github.com/esalaine/envoy-go/internal/filter/http/jwtauthn"` alphabetically among the existing filter-package imports. The resulting block reads: `router → bandwidthlimit → buffer → compressor → cors → csrf → envoygotest → fault → header_mutation → jwtauthn → localratelimit → rbac → header_mutation.RegisterPerRouteValidator → httpReg.Freeze()`. ~+3 LoC delta (1 import + 1 register). Per SPEC §1 item 2 + ADR-0148. |
| `test/helpers/jwksbackend/doc.go` | NEW | Package doc — `// Package jwksbackend implements a minimal in-process HTTP JWKS server for differential fixtures whose driver needs to wire a RemoteJwks endpoint into both envoy.yaml and envoy-go.yaml. Used by phase 17 fixture 0019-http-jwt-authn for scenarios 1 + 7 (RemoteJwks-served JWK Sets). Lifecycle: spawn-per-fixture; the runner allocates a free TCP port, starts the JWKS backend on that port via New(), wires the cluster c_jwks_backend to that port in both yaml configs, runs the scenarios, then stops the backend at teardown via Stop().` ~25 LoC. Per SPEC §7.4. |
| `test/helpers/jwksbackend/jwksbackend.go` | NEW | In-process HTTP JWKS server per SPEC §7.4. **Public API:** `Server` type carrying `Listener net.Listener` + opaque state; `New(ctx context.Context, addr string, routes map[string]string) (*Server, error)` starts the server with `addr` (e.g., "127.0.0.1:0" for ephemeral port allocation) + `routes` mapping path → JWK Set JSON content; the server's HTTP mux dispatches GET requests on each configured path serving the corresponding JSON body with `Content-Type: application/json`; missing-route returns 404; `(s *Server) Stop()` terminates the server cleanly via `srv.Shutdown(ctx)`; `(s *Server) Addr() string` returns the listener's actual address (resolved for runner-driven port allocation). **Library:** Go stdlib `net.Listen("tcp", addr)` + `http.Server.Serve` + `http.NewServeMux` + `srv.Shutdown(ctx)`. ~80-120 LoC. Per SPEC §7.4. |
| `test/helpers/jwksbackend/jwksbackend_test.go` | NEW | Unit tests covering: `TestNew_StartsServerOnConfiguredAddr_ServesRoutes`; `TestServer_RouteServesJWKSetJSON_ByteExact`; `TestServer_MissingRoute_Returns404`; `TestServer_MultiRouteDispatch`; `TestServer_Stop_ClosesListener`; `TestServer_ConcurrentClient_NoRace`. ~80 LoC. |
| `test/differential/fixture/fixture.go` | MODIFIED | NEW `BackendKind` enum value `HTTPJwtAuthn BackendKind = 16` after the existing `HTTPRbac BackendKind = 15`. Doc-comment: "HTTPJwtAuthn reuses the existing echobackend helper at `test/helpers/echobackend/cmd/echobackend/main.go` for upstream routes + the NEW jwksbackend helper at `test/helpers/jwksbackend/` for in-process JWKS-serving. Three-listener-ish topology actually a 2-listener+JWKS-backend topology (l_test_a plaintext HCM with jwt_authn → router + l_jwks_backend plus cluster c_jwks_backend pointing at the jwksbackend subprocess). No mTLS in phase 17 (phase 17 fixture is HTTP/1.1 plaintext-only per SPEC §7.4)." ~+15 LoC delta. |
| `test/differential/runner_test.go` | MODIFIED | NEW blank-import addition `_ "github.com/esalaine/envoy-go/test/fixtures/0019-http-jwt-authn/inputs"` (alphabetical-after `0018`). NEW switch-case in BackendKind dispatch for `HTTPJwtAuthn` reusing the existing `startEchoBackend` helper from phase-14 Task 10 + spawning a `jwksbackend.New` instance per-test for the in-process JWKS server. ~+10 LoC delta. |
| `test/fixtures/0019-http-jwt-authn/` | NEW DIRECTORY | Differential fixture with 8 scenarios per SPEC §7. Plaintext-only topology with 1 echo-backend cluster + 1 jwks-backend cluster + 1 listener `l_test_a` HCM with `jwt_authn → router` filter chain. |
| `test/fixtures/0019-http-jwt-authn/envoy.yaml` | NEW | Reference Envoy bootstrap. Listener `l_test_a` (TCP plaintext; HCM chain `jwt_authn → router`) with listener-level JwtAuthentication config per SPEC §7.2 — 3 providers (provider-rs256 with RemoteJwks via jwksbackend cluster cache_duration 300s; provider-es256 with LocalJwks inline_string carrying ES256 JWK; provider-alt with RemoteJwks via jwksbackend); requirement_map alt-req = provider_name: provider-alt; rules match all paths with requires_any [provider-rs256, provider-es256]; bypass_cors_preflight: true. Routes: `/` direct_response to backend (scenarios 1-6); `/alt-req` with per-route TPFC `PerRouteConfig{requirement_name: "alt-req"}` (scenario 7); `/per-route-disabled` with per-route TPFC `PerRouteConfig{disabled: true}` (scenario 8). Cluster `c_backend` STRICT_DNS to echobackend subprocess. Cluster `c_jwks_backend` STRICT_DNS to jwksbackend subprocess. ~175 LoC. Per SPEC §7.2. |
| `test/fixtures/0019-http-jwt-authn/envoy-go.yaml` | NEW | Equivalent envoy-go bootstrap. Same listener + route + per-route map; cluster type STATIC. ~175 LoC. Per SPEC §7.2. |
| `test/fixtures/0019-http-jwt-authn/pki/gen.go` | NEW | RSA + ECDSA keypair generation for fixture 0019 per planner-time decision 11 + SPEC §7.3. **Algorithm:** `init()` generates at fixture-load time (NOT pre-baked) — 3 RSA-2048 keypairs (one per RemoteJwks-serving provider) + 1 ECDSA-P256 keypair (for LocalJwks-serving provider-es256) + 1 "tampered" key (NOT used for any JWK Set; used to sign scenario-5's bad-signature JWT). **Output:** PEM-encoded private keys for token signing + JWK Set JSON for serving via jwksbackend + LocalJwks inline_string content for envoy.yaml + envoy-go.yaml. **Library:** `crypto/x509` + `crypto/rsa` + `crypto/ecdsa` + `crypto/elliptic` + `encoding/pem` + `encoding/base64` + `encoding/json` Go stdlib. ~140 LoC. Per SPEC §7.3 + ADR-0151 §Consequences. |
| `test/fixtures/0019-http-jwt-authn/inputs/driver.go` | NEW | Go driver issuing the 8 scenarios per SPEC §7.7 mirroring phase-16 driver shape. Functions `runScenario1..runScenario8(ctx, baseURL_plaintext, jwksBaseURL) error`. Per-scenario assertion helper for byte-exact body (allow paths backend-echo verbatim; deny paths canonical jwt_verify_lib strings byte-exact) + per-counter delta byte-equivalence on the 5 active base counters. **jwksbackend lifecycle helper** `setupJWKSBackend(t, ctx, port, routes)` starts the in-process jwksbackend server at the runner-allocated port; teardown via `srv.Stop()` at test exit. **PKI helper** `signTokenRS256(t, payloadClaims, privKey, kid)` + `signTokenES256(...)` produces signed JWTs at test-setup time. **Counter-delta helper** `scrapeStats(t, baseURL)` + `assertCounterDelta(t, before, after, key, delta)` mirrors phase-16. Total estimated driver size: ~320 LoC. Per SPEC §7.7. |
| `test/fixtures/0019-http-jwt-authn/expectations.yaml` | NEW | Per-scenario allow-list + counter-delta map per SPEC §7.5. Documents the 8-scenario equivalence claim including the per-route 8th-canonical scenarios 7 + 8 + the cors-bypass scenario 6 + the divergence-window allow-list (`response_code_details` field ABSENT on envoy-go side; `filter_state_rules` not exercised; jwt_cache_* counters UNREACHABLE under MVP). ~80 LoC. Per SPEC §7.5 + planner-time decision 13 (counter-delta byte-equivalence convention from phase-13/14/15/16). |
| `test/fixtures/0019-http-jwt-authn/README.md` | NEW | Fixture overview + 8-scenario list + reference config citations + RSA-2048 + ECDSA-P256 PKI generation notes (init()-time fresh-key pattern; JWK Sets scoped to fixture only) + jwksbackend in-process server lifecycle notes + per-route 8th canonical (`disabled` bool + `requirement_name` string-reference) discipline note (ADR-0125 §(xiii) at SPEC commit) + SHARED-stats discipline note (ADR-0154) + counter-delta assertion discipline + divergence-window note (response_code_details + filter_state_rules + dynamic-metadata family + jwt_cache_config + v1.37.x claim-coverage all silent-ignored). ~120 LoC. Per SPEC §7.2. |
| `docs/envoy-go/DECISIONS.md` | MODIFIED | **8 NEW ADRs** (ADR-0148 + ADR-0149 + ADR-0150 + ADR-0151 + ADR-0152 + ADR-0153 + ADR-0154 + ADR-0155) — §Context drafts ALREADY landed at SPEC commit `9070490` per ADR-0044 ADR-on-impl convention; §Decision + §Consequences bodies authored at impl-time per the Lands-in-task table: ADR-0148 + ADR-0149 at Task 2; ADR-0150 at Task 3; ADR-0151 at Task 4; ADR-0152 at Task 5; ADR-0153 at Task 7; ADR-0154 at Task 8; ADR-0155 at Task 9. **ADR-0125 §(xiii) amendment paragraph ALREADY LANDED at SPEC commit** per phase-13/14/15/16 in-place-amend-at-SPEC precedent (UNLIKE phase-16's §(xii) which deferred to Task 10 per planner-time decision 14). ~+280 LoC across the 8 ADR §Decision + §Consequences bodies (§Context drafts already authored at SPEC commit adding ~+520 LoC; total ADR-related delta across SPEC + IMPL ~+800 LoC). |
| `docs/envoy-go/BEHAVIOR_CONTRACT.md` | MODIFIED | Per SPEC §13 patches — §13.1 `### envoy.filters.http.jwt_authn` subsection inserted **alphabetical-after `### envoy.filters.http.header_mutation`** per ADR-0100 §2.2 + SPEC §13.1 (the alphabetical position locks at SPEC time; the inserted subsection is the 11th HTTP-filter subsection landing between header_mutation and local_ratelimit) ~250 LoC; §13.2 stat-table 64→71 names extension (7 new active counter rows: allowed + denied + cors_preflight_bypassed + jwks_fetch_success + jwks_fetch_failed + jwt_cache_hit + jwt_cache_miss; the last 2 STRUCTURALLY UNREACHABLE under MVP per §8 deferral 8 — documented in the row) ~35 LoC; §13.3 NEW equivalence-matrix row pointing at fixture 0019 ~5 LoC; §13.4 NEW `### Phase 17 forward-pointer notes` subsection ~120 LoC covering the 17-item deferral + 1-foot-gun list organized by 8 deferred-cluster families (dynamic-metadata family items 1-4; algorithm-extension family items 5-7; caching-framework family items 8 + 11; claim-coverage-extension family items 15-17; filter-state-family item 12; response-code-details family item 13; access-log family item 14; CEL family item 10; operator-ergonomics family items 10-prev + 18; foot-gun JwtRequirement Set recursion-depth); **§13.7 NEW `## JWKS framework primitive` top-level section** ~25 LoC per ADR-0150 covering package shape + Fetcher lifecycle + refresh schedule + failed-refetch fixed-interval + cross-phase reuse intent + operator-facing semantics; **§13.8 NEW `## JWT verifier framework primitive` top-level section** ~25 LoC per ADR-0151 covering package shape + Token lifecycle + algorithm allow-list + claim validation order + cross-phase reuse intent + ~20 canonical error sentinels. Total ~+460 LoC. |
| `docs/envoy-go/ROADMAP.md` | MODIFIED | Row `17` status `in-progress → done` flip + summary sharpening (post-impl counts; PLAN-confirmed 15-task + ~1708-2248 LoC production estimate + final ADR roster 8 anchored). ~+1 net. |
| `docs/envoy-go/STATE.md` | MODIFIED | Advance per `BOOTSTRAP_PROMPT.md` §5 lifecycle ~rewrite-in-place. Final state: lifecycle-state `phase 17 done; awaiting next planning`; next-skill (none — phase complete); next-active-phase TBD by ROADMAP. |
| `docs/envoy-go/phases/17-http-filter-jwt-authn/PROGRESS.md` | NEW | Lifecycle artefact. Append-only log; each task lands one entry. Quote command outputs verbatim. Mirror phase 04-16 PROGRESS.md structure. ~700 LoC across 15 task entries. |
| `docs/envoy-go/phases/17-http-filter-jwt-authn/REVIEW.md` | NEW | Lifecycle artefact. End-of-phase review per `superpowers:requesting-code-review`. ~260 LoC. |

---

## Planner-time deferred-decision resolution (settles SPEC §12 + this PLAN's planner-time-emerged decisions)

The planner is required by SPEC §12 to settle the SPEC's eight deferred decisions before implementation; this PLAN settles all eight plus six that emerged at PLAN-drafting time (items 9–14 below). The fourteen resolutions are recorded in `PROGRESS.md`'s preamble (Task 1) and reproduced in summary form here so the implementer at each task can act without re-deriving them:

1. **D1 — `internal/jwks` package location LOCKED at JWKS-specific (NOT generic `internal/httpclient`) per SPEC §12.1.** JWKS-specific scope (JWK Set parsing, kid+alg lookup, refresh-5s-before-TTL schedule) warrants a dedicated package; future outbound-HTTP-needing filters (ext_authz, oauth2) MAY introduce a sibling `internal/httpclient/` OR compose against the JWKS package's lower-level primitives. ADR-0150 §Decision documents. Group 10 + `internal/jwks/jwks_test.go` tests verify the package surface. *Anchored: SPEC §12.1 + §3.1.*

2. **D2 — `internal/jwt` package error sentinel completeness LOCKED at ~20 entries per SPEC §12.2.** Initial error sentinel set covers the most-commonly-hit entries per §11.P1 table; future impl-time additions are non-blocking; cross-phase reusability preserved as long as Parse + VerifySignature + ValidateClaims + PayloadClaim API surface is stable. Group 3 + Group 4 + `internal/jwt/jwt_test.go` tests verify. *Anchored: SPEC §12.2 + §3.2 + §11.P1.*

3. **D3 — RemoteJwks initial-fetch failure behavior LOCKED at fail-loud-at-listener-load per SPEC §12.3.** When `fast_listener: false` (default) AND initial fetch FAILS, `jwks.New()` returns an error → entire JwtAuthentication config fails listener-load. DIVERGES from Envoy's `init_target_->ready()`-on-failure-too pattern; operationally cleaner for envoy-go's static-config-at-boot world. Impl-time alternative `mirror-Envoy-activate-with-error` documented at ADR-0150 §Decision (iii); MAY flip at impl-time if integration testing reveals operator surprise. *Anchored: SPEC §12.3 + §6.10.*

4. **D4 — `allow_missing` iteration across providers LOCKED at requires_any-style first-success-wins per SPEC §12.4.** When `allow_missing` requirement fires AND request carries a token, evaluator iterates across ALL providers; any token extracted via any provider's extraction-source set triggers validation against THAT provider; first-success wins; if all providers fail to validate, deny with the last failure's status; if NO token extracted, missing-OK. Phase-17 MVP simplification mirrors Envoy's permissive disposition. Impl-time may refine per Task-N empirical scrape of authenticator.cc's `allow_missing` iteration logic; ADR-0149 §Decision documents. *Anchored: SPEC §12.4 + §6.8 + §11.P16.*

5. **D5 — Per-route runtime-resolve error wire shape LOCKED at 403 + "Failed JWT authentication: Wrong requirement_name: <name>" + NO WWW-Authenticate header per SPEC §12.5 + §1.1 amendment 6.** Mirrors Envoy filter_config.cc verbatim. Impl-time alternative (500 internal server error) NOT selected — phase-17 MVP MIRRORS Envoy's 403 verbatim per §1.1 amendment 6. ADR-0153 codifies; ADR-0155 cross-references. *Anchored: SPEC §12.5 + §1.1 amendment 6 + §11.P12.*

6. **D6 — RS-vs-PS algorithm family scope LOCKED at RS family ONLY (RS256/384/512) per SPEC §12.6.** PS family (RSASSA-PSS with SHA-256/384/512) DEFERRED via §8 deferral 5 extension; phase-17 MVP scope per BRAINSTORM Q3 = "RS + ES family (six algorithms)" interpreted strictly as RS256/384/512 + ES256/384/512. Impl-time may add PS family if Task-N reveals fixture-needed coverage; ADR-0151 §Decision documents. *Anchored: SPEC §12.6 + §2.3 + §8 deferral 5.*

7. **D7 — JWT validation order — extraction vs evaluation precedence LOCKED at first-matching-rule-wins-with-OR-short-circuit per SPEC §12.7.** When listener-level rules has multiple `rules[]` entries with different `requires` requirements, FIRST-MATCHING rule's requirement applies; within the requirement (if `requires_any`), iteration is OR-with-short-circuit. Impl-time may discover ambiguity if request matches multiple rules' RouteMatch predicates (operator-config-driven ambiguity); per phase-04 router precedent, first-match wins. *Anchored: SPEC §12.7 + §6.8.*

8. **D8 — `forward = false` stripping discipline for default Authorization Bearer LOCKED at strip-entire-header per SPEC §12.8.** When `forward = false` (proto default) AND token was extracted from the default Authorization Bearer (not configured from_headers), strip the ENTIRE `Authorization` header (not just the `Bearer <token>` value). Mirrors Envoy's behavior per JwtProvider.forward proto comment ("the JWT is removed in the request"). Impl-time may refine if Envoy preserves Authorization header presence with non-Bearer parts intact (e.g., dual-auth schemes); phase-17 MVP strips entire header for simplicity. ADR-0149 §Decision documents. *Anchored: SPEC §12.8 + §6.9.*

9. **D9 — `internal/jwks/Fetcher` cleanup at OnDestroy NO-OP per planner-time decision (NEW; surfaces at PLAN-time).** Filter `OnDestroy` does NOT close the fetcher — fetcher is owned by `factoryState.listenerRC.providers[<name>]` and SHARED across all filter instances of the listener; lifetime is listener-lifetime; fetcher closes when listener drains (future graceful-drain integration; phase-17 MVP relies on goroutine-leak-on-restart per HCM lifecycle scope). Group 10 RemoteJwks lifecycle + `TestClose_StopsRefreshGoroutine` verifies the Close path is well-formed for the listener-drain-time invocation. *Anchored: SPEC §6.10 + ADR-0150.*

10. **D10 — Stat-namespace SN2-reuse hypothesis CONFIRMED-PENDING-IMPL-TIME at Task 8 per phase-16 §10 lesson (c) + SPEC §11.P7 RATIFIED-PENDING-IMPL-TIME (NEW; surfaces at PLAN-time).** Task 8 includes the canonical RATIFIED-PENDING closure step (empirical scrape of reference Envoy v1.37.2 stats output for fixture 0019's listener config; verify `http.<HCM>.jwt_authn.<counter>` shape matches SPEC's SN2-reuse hypothesis); if divergent, amend ADR-0154 §Decision in-place at Task 8 per planner-time discipline + phase-13 ADR-0127-v2 in-place-amendment precedent. *Anchored: SPEC §11.P7 + ADR-0154 + phase-16 §10 lesson (c).*

11. **D11 — Fixture 0019 PKI fresh-generation at fixture-load time NOT pre-baked per planner-time decision (NEW; surfaces at PLAN-time).** `init()` at fixture-load generates 3 RSA-2048 + 1 ECDSA-P256 + 1 tampered key freshly via Go stdlib `crypto/rsa.GenerateKey` + `crypto/ecdsa.GenerateKey`; JWK Set JSON serialized from keys at runtime; LocalJwks inline_string content + jwksbackend-served JWK Set bodies BOTH sourced from the same per-test PKI. Mirrors phase-16 mTLS PKI generation discipline. Avoids checked-in private keys in the repo. *Anchored: SPEC §7.3 + ADR-0151 §Consequences.*

12. **D12 — Test-helper `test/helpers/jwksbackend/` location LOCKED at top-level test/helpers/ NOT per-fixture per planner-time decision (NEW; surfaces at PLAN-time).** Mirrors phase-14 `test/helpers/echobackend/` precedent — phase 17 introduces this shared helper at the well-known shared-helper location anticipating future filter fixtures (ext_authz, oauth2) needing JWKS-serving behavior. Not embedded under `test/fixtures/0019-http-jwt-authn/` since the shared-helper anticipation pattern is the cross-phase-reusable shape. *Anchored: SPEC §7.4 + planner-time decision 6 inheritance from phase-14.*

13. **D13 — Counter-delta byte-equivalence assertion convention from phase-13/14/15/16 carried forward per planner-time decision (NEW; surfaces at PLAN-time).** Fixture 0019 driver scrapes `/stats/prometheus` before + after each scenario; computes counter delta; asserts byte-equivalence against reference Envoy's expected delta per scenario in `expectations.yaml`. The 5 active counters (allowed + denied + cors_preflight_bypassed + jwks_fetch_success + jwks_fetch_failed) asserted via `counterModeAggregate` (since SHARED-stats discipline aggregates across listener-level + per-route per ADR-0154); the 2 unreachable counters (jwt_cache_hit + jwt_cache_miss) NOT asserted (STRUCTURALLY UNREACHABLE under MVP per §8 deferral 8). *Anchored: SPEC §7.5 + ADR-0154 + phase-16 ADR-0145 precedent.*

14. **D14 — BEHAVIOR_CONTRACT §13.1 insertion at ALPHABETICAL-AFTER-header_mutation position per SPEC §13.1 + ADR-0100 §2.2 (NOT landing-chronological as phase-16 used) per planner-time decision (NEW; surfaces at PLAN-time).** Phase-16's planner-time decision 19 used landing-chronological insertion per the observed file state; phase-17 reverts to the SPEC §13.1 stated alphabetical convention since the alphabetical insertion point lies BETWEEN existing subsections (`header_mutation` + `local_ratelimit`) not at the end; alphabetical insertion is cleaner here than appending at the file tail. Impl-time verifies BEHAVIOR_CONTRACT.md current state shows subsections roughly alphabetically ordered between `bandwidth_limit` and `rbac` and adjusts insertion accordingly. *Anchored: SPEC §13.1 + ADR-0100 §2.2 + planner-time decision.*

---

## ADRs introduced by this plan

The eight ADRs anticipated by SPEC §10 (ADR-0148..ADR-0155). **§Context drafts ALREADY landed at SPEC commit `9070490`** per ADR-0044 ADR-on-impl convention. **§Decision + §Consequences bodies authored at IMPL-time** per the Lands-in-task table below (phase-13/15/16 pattern; UNLIKE phase-14 compressor's SPEC-time-pre-landing of full ADR bodies which is the divergent precedent). **ADR-0125 §(xiii) amendment paragraph ALREADY LANDED IN FULL at SPEC commit** per phase-13/14/15/16 in-place-amend-at-SPEC precedent — verify via `grep -nE '\(xiii\)' docs/envoy-go/DECISIONS.md` returning ≥1 match at master tip; **NO PLAN-time re-anchor needed** (UNLIKE phase-16's §(xii) which deferred to Task 10 per planner-time decision 14). Per-ADR Lands-in-task anchors per SPEC §10:

| ADR | Title | Lands-in-task |
|---|---|---|
| ADR-0148 | `internal/filter/http/jwtauthn/` package shape — single-token directory (underscore-stripped per ADR-0114) matching cors/fault/csrf/buffer/compressor/localratelimit/bandwidthlimit/rbac precedent + DECODER-only `HTTPFilter` value (`Encoder: nil`; 4th §9 row to ship pure decode-side per phase-12 csrf + phase-13 buffer + phase-16 rbac precedent) + 7-base-counter `filterStats` (per §1.1 amendment 9 + §11.P6 REFUTES BRAINSTORM 8-per-provider-scaling hypothesis; canonical names per ALL_JWT_AUTHN_FILTER_STATS macro: `allowed` + `cors_preflight_bypassed` + `denied` + `jwks_fetch_success` + `jwks_fetch_failed` + `jwt_cache_hit` + `jwt_cache_miss`; NO gauges; NO histograms) + unconditional counter allocation at New() time (NO lazy-allocation; mirrors phase-12 csrf + phase-13 buffer SHARED-stats discipline) + deny-path wire shape `SendLocalReply(401-or-403, getStatusString(reason), {www-authenticate: Bearer realm="<original_uri>"<, error="invalid_token">})` per §1.1 amendments 8 + 12 + §11.P1 + §11.P2 + boot-registration ordering (alphabetical-after-header_mutation per ADR-0100 §2.2) | Task 2 (package skeleton + types + factory + filterStats declaration) |
| ADR-0149 | `compiledConfig` shape + 5-of-6 outer-field consumed proto-faithful (per §1.1 amendment 1 — `filter_state_rules` silent-ignored; REFUTES BRAINSTORM 5-field framing — proto has 6 fields per `[#next-free-field: 7]`) + 13-of-21 JwtProvider consumed (per §1.1 amendments 2 + 3 + 7 — `subjects` + `require_expiration` + `max_lifetime` v1.37.x extensions silent-ignored; `clock_skew_seconds` HONORED in MVP per amendment 7) + 6-variant JwtRequirement evaluator (`provider_name` + `provider_and_audiences` + `requires_any` + `requires_all` + `allow_missing` + `allow_missing_or_failed` per §11.P16) + RS+ES algorithm allow-list (6 algorithms: RS256/384/512 + ES256/384/512) + side-effect emit-order (strip → forward_payload_header → claim_to_headers → clear_route_cache per §11.P10 + §11.P13) + listener-level rules dispatch (first-match-wins; inline `requires` + named `requirement_name` BOTH HONORED per §1.1 amendment 4 — REFUTES BRAINSTORM deprecation-PARSE-REJECT hypothesis; listener-level `RequirementRule.requirement_name` IS parse-reject at `buildCompiledRule` if name not in `requirement_map` per §11.P12 split semantic) + envoy-go-side defensive PGV-mirror validation (per §11.P9 — `RemoteJwks.http_uri` REQUIRED; `RequirementRule.match` REQUIRED; `PerRouteConfig.requirement_specifier` REQUIRED; `PerRouteConfig.requirement_name` PGV min_len=1; envoy-go-own error wording per phase-11 ADR-0115 + phase-15 ADR-0136 + phase-16 ADR-0141 precedent) | Task 2 (buildCompiledConfig + buildCompiledProvider + buildCompiledRequirement + buildCompiledRule + parsePerRoute + resolvePerRouteConfig land here) |
| ADR-0150 | HTTP-outbound JWKS fetcher framework primitive at NEW top-level package `internal/jwks/` (cross-phase reusable; future filters ext_authz HTTP-mode + oauth2 token-endpoint flows compose against the Fetcher type's http.Client + retry-policy structure) — `Fetcher` opaque type wrapping URI + cache duration + AsyncFetch + RetryPolicy + `New(uri, cacheDuration, asyncFetch, retryPolicy)` constructor (blocking-or-non-blocking initial fetch per `fast_listener`; per §6.10 + SPEC §12.3 planner-time decision blocking initial fetch failure returns error from New()) + `Get(ctx)` returns cached JWKSet or `ErrJwksNotReady` for fast_listener-mode-before-completion + `Close()` lifecycle (terminates background refresh goroutine) + refresh schedule **5s-before-TTL** via `time.AfterFunc` per §11.P5 RATIFIED-AND-EXTENDED (`RefetchBeforeExpiredSec=5s`; default 10-min cache duration per `DefaultCacheExpirationSec=600s`) + failed-refetch **FIXED-INTERVAL 1s** per §11.P4 REFUTED-exponential-backoff (`DefaultRefetchAfterFailedSec=1s`; configurable via `JwksAsyncFetch.failed_refetch_duration`) + inner-HTTP-request RetryPolicy honored via NumRetries + BaseInterval + MaxInterval + `JWKSet.Lookup(kid, alg)` Envoy's pickKeyAlgWithKid logic + ParseJWKSet(raw) RFC 7517 §5 JWK Set parsing with RSA + EC key type support | Task 3 (NEW package + Fetcher + AsyncFetch + RetryPolicy + JWKSet + JWKSet.Lookup + ParseJWKSet + refresh-timer + failed-refetch + Close lifecycle) |
| ADR-0151 | JWT verifier framework primitive at NEW top-level package `internal/jwt/` (cross-phase reusable; future filters consuming JWT semantics — jwt_claim_router routing on claim values, oauth2 token validation — compose against Parse + Token.VerifySignature + Token.ValidateClaims + Token.PayloadClaim directly) — `Token` type with `RawHeader` + `RawPayload` + `RawSignature` + parsed Header + Payload maps + Alg + Kid + `Parse(raw)` 3-part JWT structure with `JwtBadFormat` rejection per §11.P1 + `Token.VerifySignature(key, alg)` RS+ES algorithm allow-list (PARSE-REJECT unsupported algs via `ErrJwtHeaderNotImplementedAlg`; PS family + HS family + EdDSA + `none` all rejected per §11.P1 + planner-time decision 6) + `Token.ValidateClaims(opts)` exp + nbf + iss + aud checks with clock-skew tolerance per §1.1 amendment 7 (default 60s) + `Token.PayloadClaim(path)` dot-notation extractor with array-claim rejection per §11.P10 RATIFIED-AND-EXTENDED + 3 silent-ignored ValidateOptions fields per §1.1 amendment 3 (`RequireExpiration` + `MaxLifetime` + `Subjects` — accepted at API but never enforced — v1.37.x JWT-SVID-class extensions) + ~20 canonical error sentinels mirroring jwt_verify_lib status codes per §11.P1 RATIFIED-AND-EXTENDED `getStatusString()` table + pure-Go stdlib (`crypto/rsa.VerifyPKCS1v15` + `crypto/ecdsa.Verify` + `encoding/base64.RawURLEncoding`) | Task 4 (NEW package + Token + Parse + VerifySignature + ValidateClaims + PayloadClaim + ValidateOptions + ~20 error sentinels + RS+ES algorithm dispatch via crypto/rsa + crypto/ecdsa) |
| ADR-0152 | Token extraction across all 4 sources per SPEC §6.7 + §11.P14 + §11.P15 — default Authorization Bearer (when no explicit per-provider extraction-sources set) + default access_token query param (when no explicit extraction-sources set; case-sensitive lookup) + configured from_headers (declared order; value_prefix substring-search via `strings.Index`) + configured from_params (case-sensitive exact name match; URL-decode via `url.ParseQuery`; first-value-only multi-value handling per §11.P14 REFINED) + configured from_cookies (case-sensitive exact name match; cookie value verbatim NO URL-decode per §11.P15 RATIFIED) + iteration order matches Envoy extractor.cc (headers first → params → cookies) + first-success-wins discipline (within `evaluateProvider` per-token iteration) + empty-extraction = `JwtMissed` failure-reason mapping to `jwt.ErrJwtMissed` + `isCorsPreflightRequest` predicate per §11.P1 + filter.cc verbatim (`:method == OPTIONS && origin != "" && access-control-request-method != ""`) | Task 5 (extractTokens + parseQueryParam + parseCookies + isCorsPreflightRequest + stripNonBase64URLChars helpers + Group 2 tests + extraction-source iteration integration with filter pipeline) |
| ADR-0153 | Per-route 8th canonical pattern `oneof{disabled(bool) | requirement_name(string)}` (per §1.1 amendment 5 — REFUTES BRAINSTORM `disabled(Empty)` hypothesis; PerRouteConfig.Disabled is varint bool NOT marker-message Empty per §11.P9) + delegation via listener-level requirement_map + dangling-reference RUNTIME-RESOLVE at request time per §1.1 amendment 6 + §11.P12 (REFUTES BRAINSTORM parse-reject hypothesis; mirrors Envoy filter_config.cc `findPerRouteVerifier`; on miss emits `SendLocalReply(403, "Failed JWT authentication: Wrong requirement_name: <name>", nil)` — status 403 NOT 401; NO WWW-Authenticate header; body wraps error string) + per-route stats SHARED with listener-level per §5.2 + §11.P8 RATIFIED (NO per-route stat_prefix; pure delegation; mirrors phase-12 csrf + phase-13 buffer + phase-14 compressor SHARED-stats discipline; DIVERGES from phase-11 + phase-15 + phase-16 INDEPENDENT — rationale: per-route is pure string-reference-delegation; spawns no new policy-evaluation state) + ADR-0125 §(xiii) amendment paragraph cross-reference (NEW 8th canonical entry; ALREADY landed at SPEC commit; ADR-0125's roster grows from 7 to 8) + per-route 3 cases at `parsePerRoute`: (a) `disabled: true` → `compiledPerRoute{disabled: true, requirementName: ""}`; (b) `disabled: false` → `compiledPerRoute{disabled: false, requirementName: ""}` falls through to listener-level rules; (c) `requirement_name: "<name>"` → `compiledPerRoute{disabled: false, requirementName: "<name>"}` runtime-resolved | Task 7 (parsePerRoute + resolvePerRouteConfig + buildCompiledPerRoute + 3 cases + per-route runtime-resolve error path + Group 7 tests) |
| ADR-0154 | Stat surface 7 base counters (`allowed` + `denied` + `cors_preflight_bypassed` + `jwks_fetch_success` + `jwks_fetch_failed` + `jwt_cache_hit` + `jwt_cache_miss` per `ALL_JWT_AUTHN_FILTER_STATS` macro per §11.P6; the last 2 STRUCTURALLY UNREACHABLE under MVP per §8 deferral 8 jwt_cache_config silent-ignore) per HCM stat_prefix scope + NO per-provider scaling per §1.1 amendment 9 (REFUTES BRAINSTORM 8-per-provider-scaling hypothesis — Envoy emits filter-wide counters; multiple providers contribute to same allowed/denied) + NO gauges + NO histograms + canonical naming `cors_preflight_bypassed` per §1.1 amendment 10 (REFUTES BRAINSTORM `bypassed_cors_preflight` hypothesis) + HCM-rooted SN2-reuse namespace `http.<HCM_stat_prefix>.jwt_authn.<counter>` per §11.P7 (RATIFIED-PENDING-IMPL-TIME-EMPIRICAL-SCRAPE at Task 8 per phase-16 §10 lesson (c); SOLE Task-8 RATIFIED-PENDING closure pin in phase-17; if scrape divergent, amend ADR-0154 in-place per planner-time decision 10 + phase-13 ADR-0127-v2 in-place-amendment precedent) + per-route stats SHARED per §5.2 (NO INDEPENDENT-stats discipline; pure delegation; mirrors phase-12/13/14) + unconditional non-lazy counter allocation at New() time (all 7 counters registered regardless of `jwt_cache_config` setting; allocation is cheap and uniform) | Task 8 (newFilterStats + 7-counter registration + Task-8 RATIFIED-PENDING-IMPL-TIME-EMPIRICAL-SCRAPE for §11.P7 + Group 12 tests + ADR-0154 in-place amend disposition if scrape REFUTES SN2-reuse) |
| ADR-0155 | Deny-path wire shape — 401 for most failure-reasons + 403 specifically for `JwtAudienceNotAllowed` per §1.1 amendment 8 + §11.P1 (REFINES BRAINSTORM "always 401" hypothesis; mirrors Envoy filter.cc `code = (status == Status::JwtAudienceNotAllowed) ? Http::Code::Forbidden : Http::Code::Unauthorized`) + body = canonical jwt_verify_lib `getStatusString(status)` per §11.P1 RATIFIED-AND-EXTENDED (~70-string table; ~10 commonly-hit at runtime; canonical strings: `"Jwt is missing"` 14B + `"Jwt is expired"` 14B + `"Jwt verification fails"` 22B + `"Jwt issuer is not configured"` 28B + `"Audiences in Jwt are not allowed"` 32B + `"Jwt not yet valid"` 17B + `"Jwt header [alg] is not supported"` 33B + ~63 others) + WWW-Authenticate header per RFC 6750 `Bearer realm="<original_uri>"` + conditional `, error="invalid_token"` append for non-JwtMissed per §1.1 amendment 12 + §11.P2 (REFINES BRAINSTORM `realm="<issuer>"` hypothesis — realm uses request URI captured at DecodeHeaders before route mutation) + `strip_failure_response: true` strips both body AND www-authenticate per §11.P3 RATIFIED + `response_code_details = "jwt_authn_access_denied{<reason_with_spaces_as_underscores>}"` divergence-window per §1.1 amendment 11 + §8 deferral 13 (envoy-go MVP defers field emission per phase-04 HCM scope; mirrors phase-16 ADR-0146 response_code_details discipline; joint closure with phase-16 forward-pointer item 8 at future response-code-details framework phase) + per-route runtime-resolve error case emits 403 + plain body `"Failed JWT authentication: Wrong requirement_name: <name>"` (NO www-authenticate header) per §1.1 amendment 6 + 4-header standard set lowercase wire-form (`content-length: <body length>`, `content-type: text/plain` when body non-empty, `date: <RFC1123>`, `server: envoy`) + keep-alive disposition NO `connection: close` (pre-body decision) | Task 9 (DecodeHeaders body + applyResult + emitDenyResponse + mapStatusToHTTPCode + WWW-Authenticate construction + per-route runtime-resolve 403 path + Group 8 tests) |

**Plus ADR-0125 §(xiii) amendment paragraph** — **ALREADY LANDED IN FULL at SPEC commit `9070490`** per phase-13/14/15/16 in-place-amend-at-SPEC precedent. The amendment paragraph documents: phase 17 jwt_authn is the FIRST row to use the **8th canonical per-route pattern** (oneof{disabled(bool) | requirement_name(string)} with **string-reference-delegation** into a separately-named registry; explicit-disable-bool NOT Empty; runtime-resolve discipline; SHARED-stats discipline). Structurally distinct from all 7 prior canonicals — defining feature is STRING-REFERENCE-DELEGATION (per-route does NOT carry its own filter config; references-by-name into listener-level requirement_map). ADR-0125's canonical-pattern roster grows from 7 to 8. **NO PLAN-time re-anchor or impl-time authoring of the §(xiii) paragraph required** — verify presence via `grep -nE '\(xiii\)' docs/envoy-go/DECISIONS.md` returning ≥1 match at Task 1 cold-start.

The implementer at each impl-anchor task AUTHORS the ADR §Decision + §Consequences bodies in DECISIONS.md (in the slot of the existing §Context-draft for that ADR; ADR-0148 fills in after ADR-0147; ADR-0149 fills in after ADR-0148; etc.) AND includes the ADR in the commit message AND verifies via `grep -nE '^## ADR-XXXX' docs/envoy-go/DECISIONS.md` returning 1 match (the canonical authoring-check) AND verifies `Lands-in-task: Task N` field via `grep -nE '^Lands-in-task: Task N' docs/envoy-go/DECISIONS.md`.

**Inline supersessions / amendments anticipated** (cross-references only; **NO in-place ADR edits required by phase 17 — ADR-0125 §(xiii) already landed at SPEC commit**):

- **ADR-0073** (typed_per_filter_config 3-tier merge — most-specific override) — UNCHANGED in phase 17. Phase-17 jwt_authn's per-route discipline (8th canonical oneof{disabled-bool | requirement_name-string}) inherits ADR-0073's wholesale-not-merge semantic — the per-route entry returned by 3-tier resolution is the SOLE source of per-route disposition. ADR-0125 §(xiii) extends ADR-0125's canonical-shape catalog to 8 entries without amending ADR-0073. NO in-place edit.
- **ADR-0040** (out-of-scope deferrals format) — UNCHANGED in phase 17. The 17-item deferral list (per SPEC §8) is captured INLINE at BEHAVIOR_CONTRACT §13.4 (the `### Phase 17 forward-pointer notes` subsection). NO new deferral ADRs (mirrors phase 10-16 SPEC §8 collapse precedent).
- **ADR-0044** (ADR-on-impl convention) — UNCHANGED. The 8 ADRs (ADR-0148..ADR-0155) each carry a `Lands-in-task` field anchored at the first-use impl-task; ADR §Decision + §Consequences bodies are authored at impl-time per the phase-13/15/16 convention (UNLIKE phase-14's full-body-at-SPEC-time divergent precedent).
- **ADR-0061** (stats Registry + SN1–SN9 rules) — UNCHANGED in phase 17. NO new SN flattening rule per §1.1 amendment 9 + §11.P7 (SN2-reuse hypothesis at PLAN time; impl-time empirical scrape at Task 8 ratifies or amends per planner-time decision 10). jwt_authn reuses the existing `internal/stats/name.go` default-branch flatten with HCM-rooted `http.*` segment routing. Cross-reference recorded in ADR-0154 §Decision. NO in-place edit.
- **ADR-0072** (HTTPRegistry threaded constructor map) — UNCHANGED. Cross-reference recorded in ADR-0148 §Consequences. NO in-place edit.
- **ADR-0074** (filter set) — purely additive expansion recorded in ADR-0148 §Consequences. Filter set extends from {bandwidthlimit, buffer, compressor, cors, csrf, envoygotest, fault, header_mutation, localratelimit, rbac, router} to {bandwidthlimit, buffer, compressor, cors, csrf, envoygotest, fault, header_mutation, **jwtauthn**, localratelimit, rbac, router}. NO in-place edit.
- **ADR-0075 + ADR-0076** (HCM dispatch + framework body-buffer cap) — UNCHANGED. Phase-17 jwt_authn is decoder-only + pre-body; no body-buffering interaction. NO in-place edit.
- **ADR-0100 + ADR-0101 + ADR-0102** (FactoryCtx framework extension + runtimeConfig shape + StopIteration localReplyDone gate) — UNCHANGED. Phase-17 jwt_authn CONSUMES ADR-0100's `ctx.Stats` for filterStats registration + CONSUMES ADR-0102's StopIteration on DENY via `SendLocalReply` mechanism + mirrors ADR-0101's closure-capture-at-New + read-only-shared-after-New discipline. Cross-references recorded in ADR-0148 + ADR-0149 + ADR-0154. NO in-place edits.
- **ADR-0117** (per-route bucket isolation as ADR-0073 wholesale-override consequence; phase-11 local_ratelimit INDEPENDENT-stats) — UNCHANGED §Decision sections. Phase-17 jwt_authn DIVERGES from ADR-0117's INDEPENDENT-stats discipline (per §5.2 + ADR-0154 SHARED-stats — pure delegation per-route does NOT spawn new state). ADR-0153 + ADR-0154 cross-reference ADR-0117 as the divergent precedent. NO in-place edit.
- **ADR-0125** (canonical per-route patterns + §(viii)-(x) + §(xi) + §(xii) in-place amendments) — **§(xiii) AMENDMENT PARAGRAPH ALREADY LANDED at SPEC commit per phase-13/14/15/16 in-place-amend-at-SPEC precedent.** Phase 17 lands NO additional ADR-0125 amendment at IMPL time; verify §(xiii) presence at Task 1 cold-start.
- **ADR-0128** (HCM framework primitives — synthetic empty-terminal RunDecodeData + post-body CL reconciliation) — UNCHANGED in phase 17. Phase-17 jwt_authn is pre-body decoder-only; the synthetic empty-terminal RunDecodeData primitive is structurally unused (jwt_authn evaluates at DecodeHeaders before body bytes flow). Cross-reference recorded in ADR-0148 §Decision. NO in-place edit.
- **ADR-0131** (Path B body algorithm + OverwriteBody encode-side primitive) — UNCHANGED in phase 17. Phase-17 jwt_authn is encoder-nil; OverwriteBody is structurally unused. NO in-place edit.
- **ADR-0139 + ADR-0145** (per-route INDEPENDENT-stats wiring for bandwidth_limit + rbac) — UNCHANGED. Phase-17 jwt_authn DIVERGES from ADR-0139 + ADR-0145 INDEPENDENT-stats discipline (per §5.2 + ADR-0154 SHARED-stats); the underlying machinery (`buildCompiledConfigPerRoute` + `newFilterStatsIfAbsent` + `sync.Map` lazy-cache for per-route fresh `*compiledConfig`) is STRUCTURALLY UNUSED in phase 17 since per-route is delegation-only (carries `*compiledPerRoute` lightweight value NOT fresh `*compiledConfig`). NO in-place edit.

These fourteen cross-references land at the tasks that anchor each affected ADR (ADR-0148 + ADR-0149 at Task 2; ADR-0150 at Task 3; ADR-0151 at Task 4; ADR-0152 at Task 5; ADR-0153 at Task 7; ADR-0154 at Task 8; ADR-0155 at Task 9). **NO in-place ADR edits required by phase 17** — phase-13's ADR-0127-v2 in-place-update + phase-16's ADR-0125 §(xii) at Task 10 patterns DO NOT recur in phase 17 since ADR-0125 §(xiii) ALREADY landed at SPEC commit.

**ADR-0044 escape-valve** held in reserve for ~1 impl-time-unanticipated ADR per phase-13 ADR-0127-v2 + phase-14 ADR-0134 + phase-16 ADR-0147 precedent. Most likely surfaces per SPEC §10: (i) TLS-trust-store coupling for mTLS-to-JWKS endpoint (analogous to phase-16 ADR-0147 TLS-layer mTLS-lift; would surface if Task 3 or Task 12 reveals a need for fixture-CA-style TLS validation against the jwksbackend); (ii) HCM framework gap for `clear_route_cache` interaction (analogous to phase-14 ADR-0134 HCM directResponseAction header plumbing; would surface if Task 9 reveals HCM-side wiring needed for `cb.ClearRouteCache()` to propagate after side-effect emit); (iii) sync-primitive lift for RemoteJwks-fetch-during-request callback path (NEW class — outbound-network-from-filter primitive's first phase-level discovery point; would surface if Task 3 or Task 6 reveals a goroutine-safety concern with `jwksFetcher.Get` called from the per-stream filter path). PLAN reserves Task slot for the unanticipated ADR landing — phase-13/14/15/16 precedent: the unanticipated ADR landed at a follow-up task post-main-task-list-completion (e.g., phase-16 ADR-0147 landed at Task 13 follow-up). Phase-17 PLAN does NOT pre-reserve an explicit Task slot for the unanticipated ADR — if needed, it lands as a follow-up commit between Tasks 13 and 14 or as part of Task 14 BEHAVIOR_CONTRACT bundle commit.

---

## Execution preconditions

Before Task 1, the implementer cold-starts and verifies. **Worktree spawn discipline:** the impl session is expected to run on a fresh worktree branched off the PLAN tip per ADR-0003 + the per-phase-worktree convention (per the user's persistent preference for git worktrees recorded in `~/.claude/projects/-home-esa-git-envoy-go/memory/MEMORY.md`). The expected sequence (executed by the orchestrating session BEFORE invoking the impl session, OR by the impl session itself at cold-start if it's running standalone) is:

```bash
# From the master worktree (or any non-conflicting worktree):
git worktree add /home/esa/git/envoy-go/.worktrees/phase-17-http-filter-jwt-authn-impl \
                 -b phase-17-http-filter-jwt-authn-impl <PLAN-tip-SHA>
cd /home/esa/git/envoy-go/.worktrees/phase-17-http-filter-jwt-authn-impl
```

where `<PLAN-tip-SHA>` is the master tip after the PLAN.md commit + its SHA-fill follow-up (filled by the orchestrating session that landed the PLAN).

The 18 preconditions verified at Task 1 cold-start:

1. **Worktree branch.** `git rev-parse --abbrev-ref HEAD` returns `phase-17-http-filter-jwt-authn-impl` (the impl-stage worktree). If a SPEC-stage or PLAN-stage worktree is the only branch present, branch a fresh impl worktree from master HEAD per ADR-0003: `git worktree add .worktrees/phase-17-http-filter-jwt-authn-impl -b phase-17-http-filter-jwt-authn-impl master` then `cd` into it.
2. **Master tail.** `git log --oneline master | head -10` shows the PLAN.md commit (this plan) and its SHA-fill follow-up at the head, with the SPEC.md squash commit `9070490` and its SHA-fill follow-up `75fdc2d` immediately before, then the BRAINSTORM.md commits `01315cc` (squash) + `89aaf1e` (SHA-fill) + earlier phase 16 commits. If not, the cold-start environment is stale; resync via `git fetch origin master && git pull --ff-only`.
3. **Toolchain.** `go version` reports `go1.26.2` or newer. `golangci-lint version` reports `1.64.8` (ADR-0009 pin). `docker version` reports both client + server (the differential harness needs Docker).
4. **DECISIONS.md tail.** `grep '^## ADR-' docs/envoy-go/DECISIONS.md | sed 's/.*ADR-0*\([0-9]*\):.*/\1/' | sort -n | tail -1` returns `155` (the highest §Context-draft anchored at SPEC commit). If it returns a higher number, another phase has landed concurrently; re-verify the next-free numbers (phase-17 anticipates ADR-0148..ADR-0155 already anchored as §Context drafts at SPEC commit; PLAN-time + IMPL-time NO new ADR numbers added unless ADR-0044 escape-valve triggers).
5. **ADR-0125 amendment status.** `grep -nE '\(xii\)' docs/envoy-go/DECISIONS.md | head -3` returns at least one match (the phase-16 §(xii) amendment paragraph). `grep -nE '\(xiii\)' docs/envoy-go/DECISIONS.md` returns at least one match (the phase-17 §(xiii) amendment paragraph — **already landed at SPEC commit** `9070490` per phase-13/14/15/16 in-place-amend-at-SPEC precedent; NO further action needed). If `(xiii)` returns 0 matches, the SPEC-time landing is missing — investigate before proceeding (do NOT author the §(xiii) paragraph at PLAN time per planner-time decision: it landed at SPEC commit; if missing, the SPEC commit is incomplete and needs follow-up).
6. **SPEC SHA.** `git log -1 --format=%H -- docs/envoy-go/phases/17-http-filter-jwt-authn/SPEC.md` returns `9070490` (or descendant). If different, re-read SPEC + re-verify §11 empirical pins.
7. **PLAN SHA.** `git log -1 --format=%H -- docs/envoy-go/phases/17-http-filter-jwt-authn/PLAN.md` returns the PLAN commit's SHA (filled at PLAN-session end). If a different SHA OR earlier than the SPEC, PLAN has been amended — re-read PLAN.
8. **Pristine tree.** `git status --porcelain` returns empty.
9. **Pre-existing fixtures green at `-short` budget.** `go test -count=1 -short ./...` returns clean.
10. **Pre-existing differential suite green.** `go test -count=1 ./test/differential/ -run 'Test.*0000|Test.*0001|...|Test.*0018'` returns every fixture PASS. The 19 pre-existing fixtures (0000–0018) are the regression baseline.
11. **Pre-existing fuzzers run clean at 30s.** The 20 fuzzers from phases 02–16 run clean. Phase 17 adds the twenty-first (`FuzzJwtAuthnConfigParse` per Task 10).
12. **Reference Envoy image present.** `docker pull envoyproxy/envoy:v1.37.2` returns success; `docker image inspect envoyproxy/envoy:v1.37.2` returns the SHA `sha256:c5e8a68e52f4d4697a9adb280dbe415d77fedf1257e183dcb86205bd438f18bd` (ADR-0008 pin; unchanged through phase 17).
13. **`envoy.extensions.filters.http.jwt_authn.v3` proto package present in module closure.** `go doc github.com/envoyproxy/go-control-plane/envoy/extensions/filters/http/jwt_authn/v3 JwtAuthentication | head -5` returns the `JwtAuthentication` proto type's exported fields without an `import path failed` error. If it fails, `go mod download` (or `go mod tidy` if a version bump is needed).
14. **Pre-existing `internal/filter/http/jwtauthn/` directory does NOT exist.** `test ! -d internal/filter/http/jwtauthn && echo "ok: jwtauthn absent"` returns success.
15. **Pre-existing `internal/jwks/` directory does NOT exist.** `test ! -d internal/jwks && echo "ok: jwks absent"` returns success.
16. **Pre-existing `internal/jwt/` directory does NOT exist.** `test ! -d internal/jwt && echo "ok: jwt absent"` returns success.
17. **Pre-existing `test/helpers/jwksbackend/` directory does NOT exist.** `test ! -d test/helpers/jwksbackend && echo "ok: jwksbackend absent"` returns success.
18. **Pre-existing `cmd/envoy-go/main.go` registers exactly the TWELVE filters expected at master `75fdc2d`** — `grep -cE 'httpReg.Register' cmd/envoy-go/main.go` returns `12` matches: `router`, `bandwidthlimit`, `buffer`, `compressor`, `cors`, `csrf`, `envoygotest`, `fault`, `header_mutation`, `localratelimit`, `rbac`, plus the trailing freeze. If 13+, another filter has been added concurrently; re-verify the registration ordering before adding the jwtauthn line.

If all 18 preconditions pass, proceed to Task 1.

---

## Task 1: Execution-precondition check + PROGRESS.md preamble

**Files:**
- Create: `docs/envoy-go/phases/17-http-filter-jwt-authn/PROGRESS.md`

This task verifies the `## Execution preconditions` block above and creates PROGRESS.md so subsequent tasks have an append target. Per ADR-0044 ADR-on-impl convention, the 8 ADRs (ADR-0148..ADR-0155) §Decision + §Consequences bodies are NOT pre-landed at SPEC commit (only §Context drafts ARE; verify via grep); each ADR's §Decision + §Consequences body is authored AT its impl-time anchor task. The PROGRESS preamble ANTICIPATES the 8 ADRs (with each ADR's Lands-in-task anchor reproduced from this PLAN's per-ADR table) and records the planner-time decisions resolution.

**Precondition:** worktree exists at `phase-17-http-filter-jwt-authn-impl`; branch base is master tip after the PLAN.md SHA-fill follow-up; all 18 preconditions above report green.
**Artifact:** `docs/envoy-go/phases/17-http-filter-jwt-authn/PROGRESS.md` (new file).
**Acceptance:** all 18 preconditions report green; PROGRESS.md preamble entry committed; `git log -1 --format=%H -- docs/envoy-go/phases/17-http-filter-jwt-authn/PROGRESS.md` returns the Task 1 commit's SHA.

- [ ] **Step 1: Verify each precondition** — run each command from `## Execution preconditions` § above and confirm the expected output.

- [ ] **Step 2: Author `PROGRESS.md` preamble** — create `docs/envoy-go/phases/17-http-filter-jwt-authn/PROGRESS.md` with: (a) Preamble section summarizing the 18-precondition verification (verbatim command outputs captured); (b) ADR anticipation summary (the 8-ADR table from `## ADRs introduced by this plan` reproduced verbatim); (c) 14 planner-time decisions reproduced verbatim from `## Planner-time deferred-decision resolution` above; (d) Task 1 entry slot for the commit-SHA fill-in at this task's commit time.

- [ ] **Step 3: Commit**

```bash
git add docs/envoy-go/phases/17-http-filter-jwt-authn/PROGRESS.md
git commit -m "phase 17 Task 1: PROGRESS.md preamble + 18-precondition verification"
git log -1 --format=%H -- docs/envoy-go/phases/17-http-filter-jwt-authn/PROGRESS.md
# expect: a 40-char SHA (Task 1 commit)
```

---

## Task 2: `internal/filter/http/jwtauthn/` package — `doc.go` + `jwtauthn.go` skeleton (TypeURL, types, compiledConfig + compiledProvider + compiledRule + compiledRequirement + compiledPerRoute + factoryState + filter + filterStats + parsePerRoute + resolvePerRouteConfig + buildCompiledConfig + buildCompiledProvider STUB + buildCompiledRequirement + buildCompiledRule + buildCompiledPerRoute + newFilterStats + New factory) + `jwtauthn_test.go` Group 1 + Group 7 + Group 11 tests [ADR-0148, ADR-0149]

**Files:**
- Create: `internal/filter/http/jwtauthn/doc.go`
- Create: `internal/filter/http/jwtauthn/jwtauthn.go`
- Create: `internal/filter/http/jwtauthn/jwtauthn_test.go`
- Modify: `docs/envoy-go/DECISIONS.md` (fill in §Decision + §Consequences bodies for ADR-0148 + ADR-0149 — the §Context drafts already landed at SPEC commit)
- Modify: `docs/envoy-go/phases/17-http-filter-jwt-authn/PROGRESS.md` (append Task 2 entry)

Establishes the jwtauthn package skeleton + the compiledConfig/compiledProvider/compiledRule/compiledRequirement/compiledPerRoute shapes + the filterStats 7-base-counter struct + the New factory + the per-route resolver scaffolding. Task 2 lands two ADRs: ADR-0148 (package shape + decoder-only HTTPFilter + 7-base-counter filterStats + unconditional counter allocation + deny-path wire shape + boot-registration ordering) + ADR-0149 (compiledConfig + 5-of-6-outer-consumed + 13-of-21-JwtProvider-consumed + 6-variant JwtRequirement evaluator interface + algorithm allow-list + side-effect emit-order shape + envoy-go-defensive-PGV-mirror + `clock_skew_seconds` HONORED + inline `requires` honored proto-faithful). The `internal/jwks/` framework primitive used by `buildCompiledProvider` is STUBBED in Task 2 (buildCompiledProvider's RemoteJwks branch returns a sentinel error if invoked); the real `jwks.New(...)` call lands at Task 3. The `internal/jwt/` framework primitive is similarly stubbed (buildCompiledProvider's LocalJwks `jwt.ParseJWKSet` returns a placeholder error; the real impl lands at Task 4). The Group 5 (requirement evaluator) + Group 2 (token extraction) + Group 6 (side-effects) + Group 8 (deny-path wire shape) + Group 9 (CORS preflight) tests are STUBBED in Task 2 (testing skeleton scaffolds present but evaluator/extraction-helper/applySideEffects/emitDenyResponse not yet implemented); real coverage lands across Tasks 5, 6, 7, 9.

**Precondition:** Task 1 acceptance green.
**Artifact:** new package directory with `doc.go` + `jwtauthn.go` + `jwtauthn_test.go`; ADR-0148 + ADR-0149 in DECISIONS.md with `Lands-in-task: Task 2` fields + filled §Decision + §Consequences bodies.
**Acceptance:** Group 1 + Group 7 + Group 11 unit tests PASS; `go test -race -count=1 ./internal/filter/http/jwtauthn/...` exit 0; `go vet ./internal/filter/http/jwtauthn/...` exit 0; `grep -nE '^## ADR-0148|^## ADR-0149' docs/envoy-go/DECISIONS.md` returns 2 matches; `grep -nE '^Lands-in-task: Task 2' docs/envoy-go/DECISIONS.md | wc -l` returns 2.

- [ ] **Step 1: Write the Group 1 + Group 7 + Group 11 failing tests first** (per `superpowers:test-driven-development`). Implement the test cases enumerated in the File structure table for `jwtauthn_test.go` Group 1 (Config parsing — ~18 test cases) + Group 7 (Per-route 8th canonical — ~9 test cases) + Group 11 (compiledPerRoute lazy-cache — ~3 test cases). Use `mustAny`, `freshFactoryCtx`, `freshFactoryCtxWithRegistry` helpers mirroring phase-15 `bandwidthlimit_test.go` + phase-16 `rbac_test.go` precedent.

- [ ] **Step 2: Run tests to verify they FAIL** (`go test ./internal/filter/http/jwtauthn/ -run 'TestNew_|TestBuildCompiled|TestParsePerRoute|TestResolvePerRouteConfig' -v` — expect BUILD FAIL, package does not exist).

- [ ] **Step 3: Author `doc.go`** — package overview per the File structure table responsibility for `doc.go`.

- [ ] **Step 4: Author `jwtauthn.go`** — types + factory + helpers per the File structure table responsibility for `jwtauthn.go`. Pay specific attention to: (a) the `compiledConfig` shape with 5 consumed outer fields + 1 silent-ignored `filter_state_rules`; (b) the `compiledProvider` shape with 13 consumed JwtProvider fields + 8 silent-ignored; (c) the defensive PGV-mirror validation per §11.P9 — `RemoteJwks.http_uri` REQUIRED + `RequirementRule.match` REQUIRED + `PerRouteConfig.requirement_specifier` REQUIRED + `PerRouteConfig.requirement_name` PGV min_len=1; (d) the lazy-cache `sync.Map` per-route `LoadOrStore` per phase-11/15/16 precedent (but storing lightweight `*compiledPerRoute` NOT fresh `*compiledConfig`); (e) the listener-level `RequirementRule.requirement_name` PARSE-REJECT at `buildCompiledRule` if name not in `requirement_map` (per §11.P12 split semantic — listener-level is parse-reject, per-route is runtime-resolve); (f) the `clock_skew_seconds` HONORED via `durationOrDefault` with 60s default per §1.1 amendment 7; (g) the inline `requires` BOTH ARMS honored per §1.1 amendment 4 (REFUTES BRAINSTORM PARSE-REJECT-deprecated hypothesis); (h) the `buildCompiledProvider` STUB returning sentinel errors for RemoteJwks (Task 3) + LocalJwks (Task 4); (i) the `newFilterStats` registers 7 counters unconditionally (NO lazy-allocation for the SHARED-stats discipline).

- [ ] **Step 5: Run tests to verify they PASS** (`go test ./internal/filter/http/jwtauthn/ -run 'TestNew_|TestBuildCompiled|TestParsePerRoute|TestResolvePerRouteConfig' -v` — expect Group 1 + Group 7 + Group 11 tests PASS).

- [ ] **Step 6: Fill in ADR-0148 + ADR-0149 §Decision + §Consequences bodies in DECISIONS.md** — the §Context drafts ALREADY landed at SPEC commit; the IMPL task fills in the remaining sections. `Status: Accepted`, `Date: 2026-MM-DD` (impl-time date), `Lands-in-task: Task 2` fields per ADR-0044 ADR-on-impl convention. Body content per the ADR table at `## ADRs introduced by this plan` above.

- [ ] **Step 7: Commit**

```bash
git add internal/filter/http/jwtauthn/ docs/envoy-go/DECISIONS.md docs/envoy-go/phases/17-http-filter-jwt-authn/PROGRESS.md
git commit -m "phase 17 Task 2: jwtauthn package skeleton + compiledConfig + filterStats + Group 1+7+11 tests [ADR-0148, ADR-0149]"
grep -nE '^## ADR-0148|^## ADR-0149' docs/envoy-go/DECISIONS.md
# expect: 2 matches with verbatim titles per the table
```

---

## Task 3: `internal/jwks/` NEW top-level package — `doc.go` + `jwks.go` (Fetcher + New + Get + Close + AsyncFetch + RetryPolicy + JWKSet + JWKSet.Lookup + ParseJWKSet + refresh-timer + failed-refetch-fixed-interval) + `jwks_test.go` [ADR-0150]

**Files:**
- Create: `internal/jwks/doc.go`
- Create: `internal/jwks/jwks.go`
- Create: `internal/jwks/jwks_test.go`
- Modify: `internal/filter/http/jwtauthn/jwtauthn.go` (replace `buildCompiledProvider` RemoteJwks STUB with real `jwks.New(uri, cacheDur, asyncFetch, retryPolicy)` call)
- Modify: `docs/envoy-go/DECISIONS.md` (fill in ADR-0150 §Decision + §Consequences body — §Context already landed at SPEC commit)
- Modify: `docs/envoy-go/phases/17-http-filter-jwt-authn/PROGRESS.md` (append Task 3 entry)

Lands the NEW top-level `internal/jwks/` framework primitive per ADR-0150 + SPEC §3.1. Package exports `Fetcher` opaque type + `New(uri, cacheDuration, asyncFetch, retryPolicy)` constructor + `Get(ctx)` cache-hit-or-on-demand-fetch + `Close()` lifecycle + `AsyncFetch` + `RetryPolicy` config structs + `JWKSet` parsed JWK Set type + `JWKSet.Lookup(kid, alg)` Envoy pickKeyAlgWithKid logic + `ParseJWKSet(raw)` RFC 7517 §5 JWK Set parsing. Cross-phase reusable. Refresh schedule per §11.P5 — 5s-before-TTL via `time.AfterFunc`; default 10-min cache duration. Failed-refetch fixed-interval 1s default per §11.P4 — NOT exponential backoff. RetryPolicy honors inner-HTTP-request retries.

**Precondition:** Task 2 acceptance green.
**Artifact:** new package `internal/jwks/` directory with `doc.go` + `jwks.go` + `jwks_test.go`; jwtauthn.go `buildCompiledProvider` RemoteJwks branch updated to call `jwks.New`; ADR-0150 §Decision + §Consequences body in DECISIONS.md with `Lands-in-task: Task 3`.
**Acceptance:** jwks unit tests PASS; jwtauthn package's RemoteJwks-path Group 1 tests (subset previously STUBBED) PASS; `go test -race -count=1 ./internal/jwks/... ./internal/filter/http/jwtauthn/...` exit 0; `grep -nE '^## ADR-0150' docs/envoy-go/DECISIONS.md` returns 1 match.

- [ ] **Step 1: Write the jwks_test.go failing tests** per the File structure table responsibility (~25-35 test cases). Use `httptest.NewServer` for in-process JWKS-serving + `withMockTime(t, t time.Time)` for clock injection (Go test deterministic-time pattern).

- [ ] **Step 2: Run tests to verify they FAIL** (BUILD FAIL — package does not exist).

- [ ] **Step 3: Author `internal/jwks/doc.go` + `jwks.go`** per the File structure table. Pay specific attention to: (a) the `Fetcher` opaque type with `mu sync.Mutex` + `keys *JWKSet` + `ready chan struct{}` + `notReadyErr error` + `refreshTimer *time.Timer` + `client *http.Client` + `closed atomic.Bool` fields; (b) the blocking-initial-fetch path returning error from New() when `fast_listener: false` per planner-time decision 3; (c) the non-blocking-initial-fetch path with `ErrJwksNotReady` returned until completion when `fast_listener: true`; (d) the refresh schedule `cacheDuration - 5s` via `time.AfterFunc` clamped to 0 if `cacheDuration < 5s` per §11.P5; (e) the failed-refetch fixed-interval `failedRefetchDuration` default 1s per §11.P4 — NO exponential backoff; (f) the JWK Set JSON-parse via `encoding/json.Unmarshal` reading `keys[]` array with RSA + EC key types; (g) the `JWKSet.Lookup(kid, alg)` Envoy pickKeyAlgWithKid logic — match on kid first, fall back to first key with matching alg; (h) the `Close()` lifecycle stopping the refresh goroutine via `refreshTimer.Stop()` + closing `ready` if not already closed.

- [ ] **Step 4: Run tests to verify they PASS**.

- [ ] **Step 5: Replace `buildCompiledProvider` RemoteJwks STUB in `internal/filter/http/jwtauthn/jwtauthn.go`** with the real `jwks.New(uri, cacheDuration, asyncFetch, retryPolicy)` call. Re-enable the jwtauthn-side RemoteJwks Group 1 tests that were STUBBED at Task 2.

- [ ] **Step 6: Fill in ADR-0150 §Decision + §Consequences body in DECISIONS.md** — `Status: Accepted`, `Lands-in-task: Task 3`, body content per the ADR table.

- [ ] **Step 7: Commit**

```bash
git add internal/jwks/ internal/filter/http/jwtauthn/jwtauthn.go docs/envoy-go/DECISIONS.md docs/envoy-go/phases/17-http-filter-jwt-authn/PROGRESS.md
git commit -m "phase 17 Task 3: internal/jwks framework primitive + jwtauthn RemoteJwks wiring [ADR-0150]"
```

---

## Task 4: `internal/jwt/` NEW top-level package — `doc.go` + `jwt.go` (Token + Parse + VerifySignature + ValidateClaims + PayloadClaim + ValidateOptions + ~20 error sentinels + RS+ES algorithm dispatch) + `jwt_test.go` [ADR-0151]

**Files:**
- Create: `internal/jwt/doc.go`
- Create: `internal/jwt/jwt.go`
- Create: `internal/jwt/jwt_test.go`
- Modify: `internal/filter/http/jwtauthn/jwtauthn.go` (replace `buildCompiledProvider` LocalJwks STUB with real `jwt.ParseJWKSet(raw)` call)
- Modify: `docs/envoy-go/DECISIONS.md` (fill in ADR-0151 §Decision + §Consequences body — §Context already landed at SPEC commit)
- Modify: `docs/envoy-go/phases/17-http-filter-jwt-authn/PROGRESS.md` (append Task 4 entry)

Lands the NEW top-level `internal/jwt/` framework primitive per ADR-0151 + SPEC §3.2. Package exports `Token` type + `Parse(raw)` 3-part JWT structure parsing + `Token.VerifySignature(key, alg)` RS+ES allow-list dispatch + `Token.ValidateClaims(opts)` exp + nbf + iss + aud checks with clock-skew tolerance + `Token.PayloadClaim(path)` dot-notation extractor with array-claim rejection + `ValidateOptions` struct + ~20 canonical error sentinels mirroring jwt_verify_lib status codes per §11.P1. Cross-phase reusable. Pure-Go stdlib via `crypto/rsa.VerifyPKCS1v15` + `crypto/ecdsa.Verify` + `encoding/base64.RawURLEncoding`.

**Precondition:** Task 3 acceptance green. (NOTE: Task 3 + Task 4 are INDEPENDENT and MAY run in parallel via `superpowers:subagent-driven-development` if the impl session prefers parallel-task execution per user memory `feedback_execution_style.md`; if so, Task 4's preconditions become Task 2 acceptance green + Task 3's jwks-stub-acceptance green; the final jwtauthn-side integration of BOTH primitives can occur after both tasks complete.)
**Artifact:** new package `internal/jwt/` directory with `doc.go` + `jwt.go` + `jwt_test.go`; jwtauthn.go `buildCompiledProvider` LocalJwks branch updated to call `jwt.ParseJWKSet`; ADR-0151 §Decision + §Consequences body in DECISIONS.md with `Lands-in-task: Task 4`.
**Acceptance:** jwt unit tests PASS; jwtauthn package's LocalJwks-path Group 1 tests (subset previously STUBBED) PASS; `go test -race -count=1 ./internal/jwt/... ./internal/filter/http/jwtauthn/...` exit 0; `grep -nE '^## ADR-0151' docs/envoy-go/DECISIONS.md` returns 1 match.

- [ ] **Step 1: Write the jwt_test.go failing tests** per the File structure table responsibility (~30-40 test cases). Test helpers `freshRS256Key(t)` + `freshES256Key(t)` + `signTokenRS256(t, claims, key, kid)` + `signTokenES256(t, claims, key, kid)` for PKI generation + token assembly.

- [ ] **Step 2: Run tests to verify they FAIL** (BUILD FAIL — package does not exist).

- [ ] **Step 3: Author `internal/jwt/doc.go` + `jwt.go`** per the File structure table. Pay specific attention to: (a) the `Token` type with raw + decoded fields; (b) the `Parse` 3-part split + base64url-decode + JSON-decode with the appropriate canonical error per failure mode (`JwtBadFormat` + `JwtHeaderParseErrorBadBase64` + `JwtHeaderParseErrorBadJson` + `JwtPayloadParseErrorBadBase64` + `JwtPayloadParseErrorBadJson` + `JwtSignatureParseErrorBadBase64` + `JwtHeaderBadAlg`); (c) the `VerifySignature` RS+ES allow-list dispatch — RS256/384/512 via `crypto/rsa.VerifyPKCS1v15` with appropriate `crypto.Hash`; ES256/384/512 via `crypto/ecdsa.Verify` with `*big.Int` r+s split from signature byte half; PS family + HS family + EdDSA + `none` all return `ErrJwtHeaderNotImplementedAlg`; (d) the `ValidateClaims` order: exp → nbf → iss → aud per §6.8 with clock-skew tolerance applied to exp + nbf; (e) the `PayloadClaim` dot-notation traversal with array-claim rejection per §11.P10; (f) the 3 ValidateOptions silent-ignored fields per §1.1 amendment 3 (`RequireExpiration` + `MaxLifetime` + `Subjects` — accepted at API but never enforced); (g) the ~20 canonical error sentinels with byte-exact strings from jwt_verify_lib `getStatusString()` per §11.P1.

- [ ] **Step 4: Run tests to verify they PASS**.

- [ ] **Step 5: Replace `buildCompiledProvider` LocalJwks STUB in `internal/filter/http/jwtauthn/jwtauthn.go`** with the real `jwt.ParseJWKSet(raw)` call (NOTE: `ParseJWKSet` may live in `internal/jwks/` per SPEC §3.1 instead of `internal/jwt/`; resolve at Task 3 or Task 4 based on which package the JWK-Set-parsing helper naturally fits — SPEC §3.1 places it in `internal/jwks/` since JWKS-specific concerns; impl-time may co-locate if simpler).

- [ ] **Step 6: Fill in ADR-0151 §Decision + §Consequences body in DECISIONS.md** — `Status: Accepted`, `Lands-in-task: Task 4`, body content per the ADR table.

- [ ] **Step 7: Commit**

```bash
git add internal/jwt/ internal/filter/http/jwtauthn/jwtauthn.go docs/envoy-go/DECISIONS.md docs/envoy-go/phases/17-http-filter-jwt-authn/PROGRESS.md
git commit -m "phase 17 Task 4: internal/jwt framework primitive + jwtauthn LocalJwks wiring [ADR-0151]"
```

---

## Task 5: `evaluator.go` — Token extraction across all 4 sources + extraction helpers + isCorsPreflightRequest + Group 2 + Group 9 tests [ADR-0152]

**Files:**
- Create: `internal/filter/http/jwtauthn/evaluator.go` (partial — extraction surface only at this task; 6-variant evaluator added at Task 6)
- Modify: `internal/filter/http/jwtauthn/jwtauthn_test.go` (Group 2 + Group 9 tests — ~16 test cases)
- Modify: `docs/envoy-go/DECISIONS.md` (fill in ADR-0152 §Decision + §Consequences body — §Context already landed at SPEC commit)
- Modify: `docs/envoy-go/phases/17-http-filter-jwt-authn/PROGRESS.md` (append Task 5 entry)

Lands the token extraction subset of evaluator.go per ADR-0152 + SPEC §6.7 + §11.P14 + §11.P15. `extractTokens(provider, headers) []extractedToken` iterates the 4 extraction sources per Envoy extractor.cc — (1) configured from_headers in declared order with value_prefix substring-search via `strings.Index`; (2) default Authorization Bearer + access_token query param when no explicit extraction-sources set; (3) configured from_params case-sensitive URL-decoded first-value-only per §11.P14; (4) configured from_cookies case-sensitive verbatim value per §11.P15; iteration order matches Envoy headers → params → cookies. Helpers: `parseQueryParam(path, name)` Go stdlib `url.ParseQuery` + case-sensitive name match returning slice of values + bool ok; `parseCookies(cookieHeader)` Go stdlib semantics + case-sensitive keys + verbatim values; `isCorsPreflightRequest(headers)` per §11.P1 + filter.cc verbatim; `stripNonBase64URLChars` trim helper for value_prefix-extracted JWT bytes per Envoy extractor.cc.

**Precondition:** Task 4 acceptance green.
**Artifact:** new `evaluator.go` (extraction surface; 6-variant evaluator at Task 6); jwtauthn_test.go Group 2 + Group 9 added.
**Acceptance:** Group 2 + Group 9 tests PASS; `go test -race -count=1 ./internal/filter/http/jwtauthn/ -run 'TestExtract|TestParseQuery|TestParseCookies|TestIsCorsPreflight' -v` exit 0.

- [ ] **Step 1: Write Group 2 + Group 9 failing tests** (~16 token extraction + CORS preflight test cases per the File structure table).

- [ ] **Step 2: Run tests to verify they FAIL**.

- [ ] **Step 3: Author `evaluator.go` extraction surface** — `extractTokens` + `extractedToken` struct + `sourceKind` enum + `parseQueryParam` + `parseCookies` + `isCorsPreflightRequest` + `stripNonBase64URLChars` helpers per the File structure table responsibility. Pay specific attention to: (a) the default-extraction-sources gate — Authorization Bearer + access_token query param apply ONLY when `len(provider.fromHeaders) == 0 && len(provider.fromParams) == 0 && len(provider.fromCookies) == 0` per Envoy extractor.cc; (b) the from_params case-sensitive lookup + URL-decode via Go stdlib `url.ParseQuery` + first-value-only multi-value handling per §11.P14; (c) the from_cookies case-sensitive lookup + verbatim value (NO URL-decode) per §11.P15; (d) the `isCorsPreflightRequest` predicate `:method == OPTIONS && origin != "" && access-control-request-method != ""` per §11.P1 + filter.cc verbatim.

- [ ] **Step 4: Run tests to verify they PASS**.

- [ ] **Step 5: Fill in ADR-0152 §Decision + §Consequences body in DECISIONS.md** — `Status: Accepted`, `Lands-in-task: Task 5`, body content per the ADR table.

- [ ] **Step 6: Commit**

```bash
git add internal/filter/http/jwtauthn/ docs/envoy-go/DECISIONS.md docs/envoy-go/phases/17-http-filter-jwt-authn/PROGRESS.md
git commit -m "phase 17 Task 5: evaluator.go token extraction + 4 sources + CORS preflight + Group 2+9 tests [ADR-0152]"
```

---

## Task 6: `evaluator.go` — 6-variant JwtRequirement evaluator + `evaluateProvider` per-token iteration + Group 5 tests + integration with jwks.Fetcher + jwt.Token + Group 3 + Group 4 + Group 10 integration smoke tests

**Files:**
- Modify: `internal/filter/http/jwtauthn/evaluator.go` (add `evaluateRequirement` + `evaluateProvider` + `evalResult` struct + 6-variant switch + recursive AND/OR combinators)
- Modify: `internal/filter/http/jwtauthn/jwtauthn_test.go` (Group 5 evaluator tests — ~12 test cases + Group 3 + Group 4 + Group 10 smoke tests — ~9 integration cases)
- Modify: `docs/envoy-go/phases/17-http-filter-jwt-authn/PROGRESS.md` (append Task 6 entry)

Lands the JwtRequirement evaluator surface per SPEC §6.8 + §11.P16. 6-variant switch in `(f *filter) evaluateRequirement(req, headers) evalResult`: `reqProviderName` simple case; `reqProviderAndAudiences` with per-rule audience override; `reqRequiresAny` OR-semantic short-circuit on first allowed=true + last-failure-status; `reqRequiresAll` AND-semantic short-circuit on first allowed=false; `reqAllowMissing` iterates all providers' extraction-sources — first-success-wins, present-and-invalid → fail per §11.P16 + planner-time decision 4; `reqAllowMissingOrFailed` always allowed=true. `(f *filter) evaluateProvider(p, headers, effectiveAudiences) evalResult` per SPEC §6.8: extract tokens; if empty → `evalResult{allowed: false, err: jwt.ErrJwtMissed}`; iterate each extracted token (first-success-wins): `jwt.Parse(raw)` → resolve key via `p.localJwks` OR `p.jwksFetcher.Get(ctx)` (with `jwksFetchSuccess/jwksFetchFailed` counter increments per §11.P6) → `keyset.Lookup(t.Kid, t.Alg)` → `t.VerifySignature(key, t.Alg)` → `t.ValidateClaims({Issuer, Audiences: effectiveAudiences, ClockSkew, Now})` → on success return `evalResult{allowed: true, token: t, provider: p}`.

**Precondition:** Task 5 acceptance green; `internal/jwks/` + `internal/jwt/` packages from Tasks 3 + 4 available.
**Artifact:** evaluator.go extended with 6-variant evaluator + evaluateProvider; jwtauthn_test.go Group 5 + Group 3/4/10 smoke tests added.
**Acceptance:** Group 5 tests PASS; Group 3 + Group 4 + Group 10 integration smoke tests PASS; `go test -race -count=1 ./internal/filter/http/jwtauthn/ -run 'TestEvaluateRequirement|TestEvaluateProvider|TestJwksFetchSuccessCounter|TestJwksFetchFailedCounter' -v` exit 0.

- [ ] **Step 1: Write Group 5 failing tests** (~12 6-variant + recursive combinator test cases) + Group 3 + Group 4 + Group 10 integration smoke tests (~9 cases).

- [ ] **Step 2: Run tests to verify they FAIL**.

- [ ] **Step 3: Extend `evaluator.go` with the 6-variant evaluator + `evaluateProvider`** per the File structure table responsibility. Pay specific attention to: (a) the `requires_any` OR-semantic — track `lastErr` from each failed sub-evaluation; return `{allowed: false, err: lastErr}` if all fail (per Envoy verifier.cc); (b) the `requires_all` AND-semantic short-circuit on first failure; (c) the `allow_missing` provider-iteration per planner-time decision 4 — if any token extracted via any provider, MUST validate (else propagate failure); if no token → allowed (missing-OK); (d) the `evaluateProvider` first-success-wins iteration across multiple extracted tokens for the same provider; (e) the `jwksFetchSuccess/jwksFetchFailed` counter increments on RemoteJwks path (NOT on LocalJwks path); (f) the `ctx` for `jwksFetcher.Get(ctx)` derived from the filter callback (or `context.Background()` if filter callback doesn't expose Context — verify at impl time).

- [ ] **Step 4: Run tests to verify they PASS**.

- [ ] **Step 5: Commit**

```bash
git add internal/filter/http/jwtauthn/ docs/envoy-go/phases/17-http-filter-jwt-authn/PROGRESS.md
git commit -m "phase 17 Task 6: evaluator.go 6-variant JwtRequirement evaluator + evaluateProvider + Group 3+4+5+10 tests"
```

---

## Task 7: `provider.go` — per-route 8th canonical refinements + `parsePerRoute` PGV-mirror + `buildCompiledPerRoute` 3-cases + per-route runtime-resolve error path codification [ADR-0153]

**Files:**
- Create: `internal/filter/http/jwtauthn/provider.go` (partial — per-route helpers + readDataSource + parseRetryPolicy at this task; applySideEffects + emitDenyResponse at Task 9)
- Modify: `internal/filter/http/jwtauthn/jwtauthn.go` (move `parsePerRoute` + `buildCompiledPerRoute` into provider.go OR re-anchor them via inter-file calls — implementer's choice; the canonical-position is in provider.go for thematic cohesion)
- Modify: `internal/filter/http/jwtauthn/jwtauthn_test.go` (Group 7 per-route tests — ~9 cases already partially landed at Task 2; Task 7 finalizes with runtime-resolve error path coverage)
- Modify: `docs/envoy-go/DECISIONS.md` (fill in ADR-0153 §Decision + §Consequences body — §Context already landed at SPEC commit)
- Modify: `docs/envoy-go/phases/17-http-filter-jwt-authn/PROGRESS.md` (append Task 7 entry)

Lands the 8th canonical per-route pattern per ADR-0153 + SPEC §5 + §6.12. `parsePerRoute(any)` unmarshals + PGV-mirror requirement_specifier-required + requirement_name min_len=1; `buildCompiledPerRoute(pr)` 3 cases per §5.1: (a) `disabled: true` → `compiledPerRoute{disabled: true, requirementName: ""}`; (b) `disabled: false` → `compiledPerRoute{disabled: false, requirementName: ""}` falls through to listener-level rules dispatch; (c) `requirement_name: "<name>"` → `compiledPerRoute{disabled: false, requirementName: "<name>"}` runtime-resolved at request time; `(s *factoryState) resolvePerRouteConfig(msg)` mirrors phase-11/15/16 lazy-cache + LoadOrStore (but storing lightweight `*compiledPerRoute` NOT fresh `*compiledConfig`). The per-route runtime-resolve error path codifies at Task 7 (consumed by `DecodeHeaders` body at Task 9): when listener-level `requirementMap[<name>]` lookup miss, the filter emits `SendLocalReply(403, "Failed JWT authentication: Wrong requirement_name: <name>", nil)` + `denied +1` per §1.1 amendment 6 — mirrors Envoy filter_config.cc `findPerRouteVerifier`. ALSO at this task: `readDataSource(ds)` for LocalJwks (handles inline_string / inline_bytes / filename / environment_variable DataSource oneof + PGV mirror for unset oneof) + `parseRetryPolicy(rp)` for RemoteJwks inner-HTTP-request retry policy (num_retries default 1 + base_interval default 1s + max_interval default 10*base per §11.P4).

**Precondition:** Task 6 acceptance green.
**Artifact:** new provider.go (per-route helpers + readDataSource + parseRetryPolicy; applySideEffects + emitDenyResponse at Task 9); ADR-0153 §Decision + §Consequences body in DECISIONS.md.
**Acceptance:** Group 7 tests PASS including dangling-`requirement_name` runtime-resolve case; `go test -race -count=1 ./internal/filter/http/jwtauthn/ -run 'TestParsePerRoute|TestBuildCompiledPerRoute|TestResolvePerRouteConfig|TestPerRouteRuntimeResolve' -v` exit 0; `grep -nE '^## ADR-0153' docs/envoy-go/DECISIONS.md` returns 1 match.

- [ ] **Step 1: Write Group 7 finalization failing tests** — runtime-resolve dangling-`requirement_name` error path (the previously-stubbed test from Task 2's Group 7 batch).

- [ ] **Step 2: Run tests to verify they FAIL**.

- [ ] **Step 3: Author `provider.go` partial** — per-route helpers (parsePerRoute, buildCompiledPerRoute, resolvePerRouteConfig moved here for thematic cohesion OR kept in jwtauthn.go with cross-file references; implementer's choice; SPEC §6.12 places them in jwtauthn.go) + readDataSource + parseRetryPolicy. The DecodeHeaders runtime-resolve dispatch consuming `f.perRoute.requirementName` lookup against `f.activeRC.requirementMap` lands at Task 9 (DecodeHeaders body).

- [ ] **Step 4: Run tests to verify they PASS**.

- [ ] **Step 5: Fill in ADR-0153 §Decision + §Consequences body in DECISIONS.md** — `Status: Accepted`, `Lands-in-task: Task 7`, body content per the ADR table covering the 8th canonical pattern + string-reference-delegation + runtime-resolve discipline + SHARED-stats discipline + ADR-0125 §(xiii) cross-reference (already landed at SPEC commit).

- [ ] **Step 6: Commit**

```bash
git add internal/filter/http/jwtauthn/ docs/envoy-go/DECISIONS.md docs/envoy-go/phases/17-http-filter-jwt-authn/PROGRESS.md
git commit -m "phase 17 Task 7: provider.go per-route 8th canonical + readDataSource + parseRetryPolicy + Group 7 finalized [ADR-0153]"
```

---

## Task 8: Stat surface finalization — `newFilterStats` registration (7 base counters under SN2-reuse namespace) + Task-8 RATIFIED-PENDING-IMPL-TIME-EMPIRICAL-SCRAPE closure for §11.P7 (SOLE phase-17 RATIFIED-PENDING pin) + Group 12 stats integration tests [ADR-0154]

**Files:**
- Modify: `internal/filter/http/jwtauthn/jwtauthn.go` (finalize `newFilterStats` real impl — register 7 counters under SN2-reuse namespace; mark the 2 cache counters with `// STRUCTURALLY UNREACHABLE under MVP per §8 deferral 8` comment)
- Modify: `internal/filter/http/jwtauthn/jwtauthn_test.go` (Group 12 stats-namespace integration tests — 5 cases)
- Modify: `docs/envoy-go/DECISIONS.md` (fill in ADR-0154 §Decision + §Consequences body — §Context already landed at SPEC commit; if Task-8 empirical scrape REFUTES SN2-reuse hypothesis, amend ADR-0154 §Decision in-place per planner-time decision 10 + phase-13 ADR-0127-v2 precedent)
- Modify: `docs/envoy-go/phases/17-http-filter-jwt-authn/PROGRESS.md` (append Task 8 entry + record the empirical scrape output)

Lands the stat surface per ADR-0154. 7 base counters (allowed + denied + cors_preflight_bypassed + jwks_fetch_success + jwks_fetch_failed + jwt_cache_hit + jwt_cache_miss; last 2 STRUCTURALLY UNREACHABLE per §8 deferral 8) per HCM stat_prefix namespace. SN2-reuse hypothesis — `http.<HCM_stat_prefix>.jwt_authn.<counter>` (the existing HCM-stat-prefix tag-extractor handles verbatim; NO new SN10 rule). **Task 8 RATIFIED-PENDING-IMPL-TIME-EMPIRICAL-SCRAPE closure** for §11.P7 (the SOLE pin in phase-17 requiring Task-8 closure per phase-16 §10 lesson (c)): start a reference Envoy v1.37.2 with the 0019 fixture's listener config (or a simplified single-provider config); scrape `/stats/prometheus`; verify the actual stat namespace shape matches `http.<HCM>.jwt_authn.<counter>` (SN2-reuse). If scrape ratifies, no further action. If scrape refutes (e.g., reveals `<HCM>.jwt_authn.<counter>` without `http.` prefix mirroring phase-15 bandwidth_limit's underscore-infix shape), amend ADR-0154 §Decision in-place per planner-time decision 10 + phase-13 ADR-0127-v2 in-place-amendment precedent — introduce a new SN10 flattening rule if needed.

**Precondition:** Task 7 acceptance green.
**Artifact:** jwtauthn.go newFilterStats finalized; Group 12 tests passing; ADR-0154 §Decision + §Consequences in DECISIONS.md; Task 8 PROGRESS.md entry records the empirical scrape output verbatim.
**Acceptance:** Group 12 tests PASS; full Group 1-12 test run clean; counter-emission tests in Groups 5 + 6 + 8 + 12 all green; `grep -nE '^## ADR-0154' docs/envoy-go/DECISIONS.md` returns 1 match. **Impl-time empirical scrape** of reference Envoy v1.37.2 stats output for the 0019 fixture's listener config — verify the actual stat namespace shape against the SN2-reuse hypothesis; if divergent, amend ADR-0154 §Decision in-place at this task.

- [ ] **Step 1: Write Group 12 failing tests** (5 stats-namespace integration cases per the File structure table).

- [ ] **Step 2: Run tests to verify they FAIL**.

- [ ] **Step 3: Finalize `newFilterStats` in jwtauthn.go** — register 7 counters unconditionally under SN2-reuse namespace per ADR-0154; nil-tolerance per ADR-0085; the 2 cache counters registered but never incremented under MVP per §8 deferral 8 (STRUCTURALLY UNREACHABLE marker comment).

- [ ] **Step 4: Run tests to verify they PASS**.

- [ ] **Step 5: Impl-time empirical scrape for §11.P7** — start a reference Envoy with the 0019 fixture's listener config (OR a simplified single-provider config with `bypass_cors_preflight: true` + RemoteJwks pointing at a stand-up JWKS endpoint); fire 5 requests covering allow + deny + cors_preflight + jwks_fetch_success paths; scrape `/stats/prometheus`; capture the actual `jwt_authn` counter names + namespaces verbatim into PROGRESS.md Task 8 entry. Verify the shape matches `http.<HCM_stat_prefix>.jwt_authn.<counter>`. If divergent (e.g., flat `<prefix>.jwt_authn.<counter>` mirroring phase-15 bandwidth_limit's shape per §1.1 amendment 9 release-valve), amend ADR-0154 §Decision in-place to introduce SN10 rule.

- [ ] **Step 6: Fill in ADR-0154 §Decision + §Consequences body in DECISIONS.md** — `Status: Accepted`, `Lands-in-task: Task 8`, body content per the ADR table; record the §11.P7 empirical scrape disposition (RATIFIED-and-locked OR AMENDED-with-SN10) in the §Decision body.

- [ ] **Step 7: Commit**

```bash
git add internal/filter/http/jwtauthn/ docs/envoy-go/DECISIONS.md docs/envoy-go/phases/17-http-filter-jwt-authn/PROGRESS.md
git commit -m "phase 17 Task 8: stat surface finalization + §11.P7 RATIFIED-PENDING closure + Group 12 tests [ADR-0154]"
```

---

## Task 9: `DecodeHeaders` body — top-level dispatch + deny-path wire shape (401 default + 403 for JwtAudienceNotAllowed) + WWW-Authenticate construction + per-route runtime-resolve error path + side-effect emit-order + Group 5 (dispatch integration) + Group 6 (side-effects) + Group 8 (deny-path) tests [ADR-0155]

**Files:**
- Modify: `internal/filter/http/jwtauthn/jwtauthn.go` (fill in `DecodeHeaders` body + `resolveRequirement` per SPEC §6.6)
- Modify: `internal/filter/http/jwtauthn/provider.go` (add `applyResult` + `applySideEffects` + 4-step emit-order + `emitDenyResponse` + `mapStatusToHTTPCode` + WWW-Authenticate construction + `encodeBase64URL` + `stripExtractionSources` + `stringify` per SPEC §6.9)
- Modify: `internal/filter/http/jwtauthn/jwtauthn_test.go` (Group 5 dispatch integration tests + Group 6 side-effect emit-order tests + Group 8 deny-path wire shape tests — ~14 + ~10 + ~14 = ~38 cases)
- Modify: `docs/envoy-go/DECISIONS.md` (fill in ADR-0155 §Decision + §Consequences body — §Context already landed at SPEC commit)
- Modify: `docs/envoy-go/phases/17-http-filter-jwt-authn/PROGRESS.md` (append Task 9 entry)

Lands the request-time dispatch surface + deny-path wire shape + side-effect emit-order. `DecodeHeaders` body per SPEC §6.6 + §1.1 amendment 12: capture `:path` → `f.originalURI`; resolve per-route TPFC via `f.dcb.RequestRouteConfig()` → case (a) `disabled: true` → `passthrough=true` + `HeaderContinue` (NO counter increments); CORS preflight bypass check via `isCorsPreflightRequest` → `cors_preflight_bypassed +1` + `HeaderContinue`; resolve requirement via `f.resolveRequirement(headers)` — case (c) per-route `requirement_name` runtime-resolved against `f.activeRC.requirementMap[<name>]`; on miss → `denied +1` + `f.dcb.SendLocalReply(403, "Failed JWT authentication: Wrong requirement_name: <name>", nil)` + `HeaderStopIteration` per §1.1 amendment 6; otherwise iterate listener-level rules first-match-wins; nil requirement → `HeaderContinue` pass-through (no JWT verification required); evaluate requirement via Task 6's `evaluateRequirement` → apply result. `applyResult(r, headers, req)` per SPEC §6.9: on DENIED → `denied +1` + `emitDenyResponse(r.err)` + `HeaderStopIteration`; on ALLOWED → `allowed +1` + `applySideEffects(r.token, r.provider, headers)` + `HeaderContinue`. `applySideEffects` 4-step emit-order per SPEC §6.9 + §11.P10 + §11.P13: (1) strip-on-success via `stripExtractionSources` when `!p.forward` (strips Authorization/from_headers/from_params; from_cookies UNTOUCHED per proto caveat); (2) `forward_payload_header` → `encodeBase64URL(t.RawPayload, p.padForwardPayloadHdr)` + `headers.Set(p.forwardPayloadHeader, encoded)`; (3) `claim_to_headers[]` → `t.PayloadClaim(ch.claimName)` dot-notation + `stringify(val)` + `headers.Set(ch.headerName, s)` with silent-skip on err (array claim OR missing); (4) `clear_route_cache: true` → `f.dcb.ClearRouteCache()`. `emitDenyResponse(reason)` per SPEC §4 + §6.9 + §1.1 amendments 8 + 11 + 12: `code = mapStatusToHTTPCode(reason)` — 401 default + 403 for `jwt.ErrJwtAudienceNotAllowed`; `f.activeRC.stripFailureResponse` branch: body=`""` + nil headers; else: body=reason.Error() + WWW-Authenticate header `Bearer realm="<f.originalURI>"` + conditional `, error="invalid_token"` for non-`ErrJwtMissed` + content-type: text/plain header; `f.dcb.SendLocalReply(code, body, hdrs)`. `mapStatusToHTTPCode(reason)` per §1.1 amendment 8: `errors.Is(reason, jwt.ErrJwtAudienceNotAllowed)` → 403; else → 401. `encodeBase64URL(raw, pad)` per §11.P13: if `pad == false`, strip trailing `=`; else preserve. `stripExtractionSources` per planner-time decision 8: strip Authorization Bearer + from_headers value + from_params query-param (path rewrite); from_cookies UNTOUCHED. `stringify(val)` per §11.P10: scalar coerce string/int/float/bool; array → false (silent skip).

**Precondition:** Task 8 acceptance green.
**Artifact:** jwtauthn.go DecodeHeaders body finalized + provider.go applyResult + applySideEffects + emitDenyResponse finalized; Groups 5 + 6 + 8 tests passing; ADR-0155 §Decision + §Consequences in DECISIONS.md.
**Acceptance:** Groups 5 + 6 + 8 tests PASS; `go test -race -count=1 ./internal/filter/http/jwtauthn/ -run 'TestEvaluateRequirement|TestApplyResult|TestApplySideEffects|TestDecodeHeaders|TestEmitDenyResponse|TestMapStatusToHTTPCode|TestStripExtractionSources|TestEncodeBase64URL|TestStringify' -v` exit 0; full Group 1-12 test run clean.

- [ ] **Step 1: Write Group 5 dispatch integration + Group 6 side-effect emit-order + Group 8 deny-path wire shape failing tests** (~38 test cases per the File structure table).

- [ ] **Step 2: Run tests to verify they FAIL**.

- [ ] **Step 3: Implement DecodeHeaders body + resolveRequirement** in jwtauthn.go per SPEC §6.6 + §1.1 amendment 12 (original_uri capture).

- [ ] **Step 4: Implement applyResult + applySideEffects + 4-step emit-order + emitDenyResponse + mapStatusToHTTPCode + encodeBase64URL + stripExtractionSources + stringify** in provider.go per SPEC §6.9 + §1.1 amendments 8 + 11 + 12.

- [ ] **Step 5: Run tests to verify they PASS**.

- [ ] **Step 6: Fill in ADR-0155 §Decision + §Consequences body in DECISIONS.md** — `Status: Accepted`, `Lands-in-task: Task 9`, body content per the ADR table covering 401-or-403 dispatch + canonical jwt_verify_lib body + WWW-Authenticate Bearer challenge + strip_failure_response + response_code_details divergence-window + per-route runtime-resolve 403 error path.

- [ ] **Step 7: Commit**

```bash
git add internal/filter/http/jwtauthn/ docs/envoy-go/DECISIONS.md docs/envoy-go/phases/17-http-filter-jwt-authn/PROGRESS.md
git commit -m "phase 17 Task 9: DecodeHeaders body + deny-path wire shape + side-effect emit-order + Groups 5+6+8 tests [ADR-0155]"
```

---

## Task 10: `cmd/envoy-go/main.go` register `jwtauthn.New` under `jwtauthn.TypeURL` + fixture infrastructure (`BackendKind=HTTPJwtAuthn` enum + runner spawn helper switch-case) + `FuzzJwtAuthnConfigParse` 21st fuzzer + NEW `test/helpers/jwksbackend/` shared test-helper

**Files:**
- Modify: `cmd/envoy-go/main.go` (add import + register line alphabetical-after-header_mutation per ADR-0100 §2.2 + ADR-0148 §Decision)
- Modify: `test/differential/fixture/fixture.go` (add `HTTPJwtAuthn BackendKind = 16` enum value)
- Modify: `test/differential/runner_test.go` (blank-import + switch-case for HTTPJwtAuthn reusing `startEchoBackend` helper + new `startJWKSBackend` helper for the jwksbackend subprocess)
- Create: `internal/filter/http/jwtauthn/fuzz_test.go` (FuzzJwtAuthnConfigParse — 21st fuzzer)
- Create: `test/helpers/jwksbackend/doc.go` (NEW shared test-helper package per planner-time decision 12)
- Create: `test/helpers/jwksbackend/jwksbackend.go` (in-process HTTP JWKS server)
- Create: `test/helpers/jwksbackend/jwksbackend_test.go` (unit tests)
- Modify: `docs/envoy-go/phases/17-http-filter-jwt-authn/PROGRESS.md` (append Task 10 entry)

Wires the jwt_authn filter into the boot registry + adds the fixture-runner infrastructure for the 0019 fixture + adds the 21st fuzzer + lands the NEW shared test-helper `test/helpers/jwksbackend/` per planner-time decision 12. The fuzzer mirrors phase-16 fuzzer shape extended for the 6-field outer JwtAuthentication + 21-field JwtProvider + 6-variant JwtRequirement evaluator + recursive `requires_any/all` combinators per SPEC §14.3. The jwksbackend test-helper provides an in-process HTTP JWKS server lifecycle for differential-fixture use; spawn-per-fixture; serves configurable JWK Set JSON at known URIs.

**Precondition:** Task 9 acceptance green.
**Artifact:** main.go + fixture.go + runner_test.go + fuzz_test.go + jwksbackend/* updated; 21 fuzzers green at 30s.
**Acceptance:** `go build ./cmd/envoy-go/` exit 0; `grep -cE 'httpReg.Register' cmd/envoy-go/main.go` returns 12 (10 existing filters + jwtauthn + freeze-related); `grep -nE 'HTTPJwtAuthn' test/differential/fixture/fixture.go` returns 1; `go test -fuzz=FuzzJwtAuthnConfigParse -fuzztime=30s ./internal/filter/http/jwtauthn/` clean exit (no failure messages); seed corpus runs as regression tests via `go test ./internal/filter/http/jwtauthn/ -run FuzzJwtAuthnConfigParse -v`; jwksbackend unit tests PASS.

- [ ] **Step 1: Add jwtauthn import + register line** to main.go (alphabetical-after-header_mutation per ADR-0148 §Decision; the resulting block reads router → bandwidthlimit → buffer → compressor → cors → csrf → envoygotest → fault → header_mutation → jwtauthn → localratelimit → rbac → header_mutation.RegisterPerRouteValidator → httpReg.Freeze()).

- [ ] **Step 2: Add HTTPJwtAuthn BackendKind enum value** to fixture.go (after `HTTPRbac BackendKind = 15`).

- [ ] **Step 3: Author `test/helpers/jwksbackend/` package** — doc.go + jwksbackend.go + jwksbackend_test.go per the File structure table responsibility (~80-120 LoC main library + ~80 LoC tests). Verify `go test -race -count=1 ./test/helpers/jwksbackend/...` exit 0.

- [ ] **Step 4: Add blank-import + switch-case** to runner_test.go (reuses existing `startEchoBackend` helper from phase-14 Task 10; adds new `startJWKSBackend(t, ctx, addr, routes)` helper invoking `jwksbackend.New` for the in-process JWKS subprocess).

- [ ] **Step 5: Author FuzzJwtAuthnConfigParse fuzzer** in `internal/filter/http/jwtauthn/fuzz_test.go` per the File structure table responsibility (~85 LoC; 13-seed corpus per SPEC §14.3). Per ADR-0018 every parser/codec/filter ships a fuzzer.

- [ ] **Step 6: Run fuzzer at 30s budget** — `go test -fuzz=FuzzJwtAuthnConfigParse -fuzztime=30s ./internal/filter/http/jwtauthn/`. Expect clean exit.

- [ ] **Step 7: Run all 21 fuzzers regression** at 30s each — phase 02-15 fuzzers + phase-16 FuzzRBACConfigParse + phase-17 FuzzJwtAuthnConfigParse. All clean.

- [ ] **Step 8: Commit**

```bash
git add cmd/envoy-go/main.go test/differential/fixture/fixture.go test/differential/runner_test.go internal/filter/http/jwtauthn/fuzz_test.go test/helpers/jwksbackend/ docs/envoy-go/phases/17-http-filter-jwt-authn/PROGRESS.md
git commit -m "phase 17 Task 10: main.go register + fixture infra + FuzzJwtAuthnConfigParse (21st fuzzer) + jwksbackend test-helper"
```

---

## Task 11: Fixture 0019 — `inputs/driver.go` (8-scenario driver including RS256-RemoteJwks + ES256-LocalJwks; byte-exact body comparison + per-counter delta scrape + JWKS-backend lifecycle + RSA/ECDSA token signing)

**Files:**
- Create: `test/fixtures/0019-http-jwt-authn/inputs/driver.go`
- Modify: `docs/envoy-go/phases/17-http-filter-jwt-authn/PROGRESS.md` (append Task 11 entry)

Lands the 8-scenario fixture driver mirroring phase-16 driver shape. All scenarios use HTTP/1.1 plaintext (no H2 differential coverage per SPEC §7.4); no mTLS in phase 17. Scenarios 1 + 7 trigger RemoteJwks fetches against the in-process jwksbackend subprocess; scenario 2 exercises LocalJwks inline path; scenarios 3 + 4 + 5 exercise deny paths (missing + expired + bad signature) with byte-exact body + WWW-Authenticate assertions; scenario 6 exercises CORS preflight bypass; scenarios 7 + 8 exercise per-route 8th canonical (`requirement_name` delegation + `disabled: true`). Per-scenario assertions: byte-exact body (allow paths backend-echo verbatim; deny paths canonical jwt_verify_lib strings byte-exact); per-counter delta byte-equivalence on the 5 active base counters per HCM stat_prefix (allowed + denied + cors_preflight_bypassed + jwks_fetch_success + jwks_fetch_failed); WWW-Authenticate header byte-exact including conditional `, error="invalid_token"` append for non-JwtMissed scenarios.

**Precondition:** Task 10 acceptance green.
**Artifact:** new driver.go file; integrates with the existing differential runner via `fixture.RegisterFixture("0019-http-jwt-authn", &jwtAuthnDriver{})` per phase-15/16 convention.
**Acceptance:** driver compiles; the fixture is registered (verified via `grep -nE 'RegisterFixture\("0019-http-jwt-authn"' test/fixtures/0019-http-jwt-authn/inputs/driver.go` returning 1). Note: driver doesn't run end-to-end until Task 13 (YAMLs + expectations + PKI generation needed first).

- [ ] **Step 1: Author driver.go** per the File structure table responsibility — `runScenario1..runScenario8` functions + `setupJWKSBackend` lifecycle helper + `signTokenRS256` + `signTokenES256` PKI helpers + `scrapeStats` + `assertCounterDelta` helpers per phase-16 cadence + 5-base-counter assertion logic. ~320 LoC.

- [ ] **Step 2: Verify compilation** — `go build ./test/fixtures/0019-http-jwt-authn/inputs/...`.

- [ ] **Step 3: Commit**

```bash
git add test/fixtures/0019-http-jwt-authn/inputs/ docs/envoy-go/phases/17-http-filter-jwt-authn/PROGRESS.md
git commit -m "phase 17 Task 11: Fixture 0019 driver — 8 scenarios incl. RS256-RemoteJwks + ES256-LocalJwks + CORS preflight + per-route 8th canonical"
```

---

## Task 12: Fixture 0019 — `envoy.yaml` + `envoy-go.yaml` bootstraps + `pki/gen.go` RSA + ECDSA keypair generation + JWK Set serialization

**Files:**
- Create: `test/fixtures/0019-http-jwt-authn/envoy.yaml`
- Create: `test/fixtures/0019-http-jwt-authn/envoy-go.yaml`
- Create: `test/fixtures/0019-http-jwt-authn/pki/gen.go`
- Modify: `docs/envoy-go/phases/17-http-filter-jwt-authn/PROGRESS.md` (append Task 12 entry)

Lands the fixture bootstraps + PKI generation. Reference + subject bootstraps both with `l_test_a` (plaintext HCM filter chain `jwt_authn → router` with the 3-provider listener-level JwtAuthentication config: provider-rs256 with RemoteJwks via jwksbackend cluster; provider-es256 with LocalJwks inline_string; provider-alt with RemoteJwks via jwksbackend) + cluster `c_backend` (echobackend subprocess) + cluster `c_jwks_backend` (jwksbackend subprocess) + 3 routes (`/` for scenarios 1-6; `/alt-req` with per-route TPFC requirement_name for scenario 7; `/per-route-disabled` with per-route TPFC disabled: true for scenario 8). The PKI generator produces fresh fixture-PKI (3 RSA-2048 keypairs for RemoteJwks-serving providers + 1 ECDSA-P256 keypair for LocalJwks provider-es256 + 1 tampered key for scenario 5's bad-signature path) at fixture-load time per planner-time decision 11; JWK Set JSON serialization at runtime; LocalJwks inline_string content + jwksbackend-served JWK Set bodies BOTH sourced from the same per-test PKI.

**Precondition:** Task 11 acceptance green.
**Artifact:** YAMLs + pki/gen.go.
**Acceptance:** YAMLs lint clean; PKI generator produces valid keypairs + JWK Set JSON (verified via standalone Go test of `gen.go`).

- [ ] **Step 1: Author pki/gen.go** — fresh-keypair generation per the File structure table responsibility (~140 LoC). 3 RSA-2048 via `crypto/rsa.GenerateKey` + 1 ECDSA-P256 via `crypto/ecdsa.GenerateKey(elliptic.P256(), rand.Reader)`; PEM-encode private keys for token signing; JWK Set JSON serialization via `encoding/json.Marshal` of `{"keys":[{"kty":"RSA","alg":"RS256","kid":"<kid>","n":"<base64url-modulus>","e":"<base64url-exponent>"}, ...]}` for RSA + `{"kty":"EC","alg":"ES256","kid":"<kid>","crv":"P-256","x":"<base64url-x>","y":"<base64url-y>"}` for EC.

- [ ] **Step 2: Author envoy.yaml** — fixture bootstrap per the File structure table (~175 LoC).

- [ ] **Step 3: Author envoy-go.yaml** — equivalent envoy-go bootstrap (~175 LoC; cluster type STATIC).

- [ ] **Step 4: Verify YAML lint** — `docker run --rm -v $(pwd):/data envoyproxy/envoy:v1.37.2 -c /data/test/fixtures/0019-http-jwt-authn/envoy.yaml --mode validate` exit 0 (reference Envoy validates the YAML structure). Repeat for envoy-go.yaml via `go run ./cmd/envoy-go -c test/fixtures/0019-http-jwt-authn/envoy-go.yaml --mode validate` (or analogous validation invocation).

- [ ] **Step 5: Commit**

```bash
git add test/fixtures/0019-http-jwt-authn/ docs/envoy-go/phases/17-http-filter-jwt-authn/PROGRESS.md
git commit -m "phase 17 Task 12: Fixture 0019 YAMLs + RSA/ECDSA PKI gen + JWK Set serialization"
```

---

## Task 13: Fixture 0019 — `expectations.yaml` + `README.md` + driver counter-assertion fleshing + end-to-end differential pass (all 8 scenarios + all 20 fixtures)

**Files:**
- Create: `test/fixtures/0019-http-jwt-authn/expectations.yaml`
- Create: `test/fixtures/0019-http-jwt-authn/README.md`
- Modify: `test/fixtures/0019-http-jwt-authn/inputs/driver.go` (finalize counter-delta + per-scenario expected-value assertions with concrete expected values from runs against reference Envoy)
- Modify: `docs/envoy-go/phases/17-http-filter-jwt-authn/PROGRESS.md` (append Task 13 entry)

Lands the expectations.yaml + README documentation + finalizes the driver's counter-delta assertions with concrete expected values from running the fixture against reference Envoy. Validates the full 8-scenario differential pass. ALL 20 differential fixtures (0000-0019) green at this task's end.

**Precondition:** Task 12 acceptance green.
**Artifact:** expectations.yaml + README.md + finalized driver.go.
**Acceptance:** `go test -count=1 ./test/differential/ -run 'Test.*0000|Test.*0001|...|Test.*0019'` returns every fixture PASS including 0019; total runtime ~60-120s wallclock per SPEC §14.6.

- [ ] **Step 1: Run fixture 0019 dry-run against reference Envoy** — capture actual per-scenario counter deltas + body byte counts + header sets (especially WWW-Authenticate header byte-exact form for scenarios 3-5 deny paths).

- [ ] **Step 2: Author expectations.yaml** — per-scenario allow-list + counter-delta map per the captured reference data; document the divergence-window allow-list (`response_code_details` field ABSENT on envoy-go side per §1.1 amendment 11; `filter_state_rules` not exercised; jwt_cache_* counters UNREACHABLE under MVP).

- [ ] **Step 3: Author README.md** — fixture overview + 8-scenario narrative + RSA/ECDSA PKI notes + jwksbackend lifecycle notes + per-route 8th canonical notes (ADR-0125 §(xiii)) + SHARED-stats notes (ADR-0154) + divergence-window notes.

- [ ] **Step 4: Finalize driver counter-assertion logic** — replace placeholder expected values with concrete reference-side values; for scenarios 3-5 deny paths, byte-exact assert the WWW-Authenticate header value including conditional `, error="invalid_token"` append.

- [ ] **Step 5: Run end-to-end differential** — `go test -count=1 -v ./test/differential/ -run 'Test.*0019'` PASS.

- [ ] **Step 6: Run full regression** — `go test -count=1 ./test/differential/ -run 'Test.*'` ALL 20 fixtures PASS.

- [ ] **Step 7: Commit**

```bash
git add test/fixtures/0019-http-jwt-authn/ docs/envoy-go/phases/17-http-filter-jwt-authn/PROGRESS.md
git commit -m "phase 17 Task 13: Fixture 0019 expectations + README + end-to-end differential pass (20 fixtures green)"
```

---

## Task 14: BEHAVIOR_CONTRACT.md 6-edit bundle + ROADMAP row 17 in-progress→done + STATE.md advance + 6-gate phase-done verification

**Files:**
- Modify: `docs/envoy-go/BEHAVIOR_CONTRACT.md` (§13.1 + §13.2 + §13.3 + §13.4 + §13.7 + §13.8 — 6 patches per SPEC §13)
- Modify: `docs/envoy-go/ROADMAP.md` (row 17 `in-progress → done`)
- Modify: `docs/envoy-go/STATE.md` (advance to `phase 17 done; awaiting next planning`)
- Modify: `docs/envoy-go/phases/17-http-filter-jwt-authn/PROGRESS.md` (append Task 14 entry + 6-gate report)

Lands the BEHAVIOR_CONTRACT.md 6 patches per SPEC §13 + flips ROADMAP row 17 + advances STATE.md + runs the 6 phase-done gates per BOOTSTRAP_PROMPT.md §7.5.

**Precondition:** Task 13 acceptance green.
**Artifact:** BEHAVIOR_CONTRACT.md + ROADMAP.md + STATE.md updated; 6-gate report appended to PROGRESS.md.
**Acceptance:** All 6 phase-done gates green per the table at SPEC §14.7:
- Gate A: `go build ./...` exit 0; `go vet ./...` exit 0; `golangci-lint run` exit 0.
- Gate B: `go test -race -count=1 ./...` exit 0 across all packages including the new `internal/jwks/` + `internal/jwt/` + `internal/filter/http/jwtauthn/` + `test/helpers/jwksbackend/` packages.
- Gate C: h2spec 53/53 PASS at ADR-0051 pin.
- Gate D: 21 fuzzers green at 30s/each (~10.5 minutes wallclock).
- Gate E: 20 differential fixtures (0000-0019) PASS.
- Gate F: BEHAVIOR_CONTRACT.md §13.1 + §13.2 + §13.3 + §13.4 + §13.7 + §13.8 populated per SPEC §13 patches.

- [ ] **Step 1: Apply BEHAVIOR_CONTRACT.md §13.1 patch** — insert `### envoy.filters.http.jwt_authn` subsection at alphabetical-after-header_mutation position per ADR-0100 §2.2 + planner-time decision 14 (~250 LoC). Verify the current file state — if the existing subsection ordering is landing-chronological rather than alphabetical, fall back to landing-chronological insertion (append at the end of `## HTTP filter chain`) and update planner-time decision 14 in PROGRESS.md accordingly.

- [ ] **Step 2: Apply §13.2 patch** — stat-table 64→71 names extension (7 new active rows; 2 structurally-unreachable noted) (~35 LoC).

- [ ] **Step 3: Apply §13.3 patch** — Equivalence Matrix new row for fixture 0019 (~5 LoC).

- [ ] **Step 4: Apply §13.4 patch** — `### Phase 17 forward-pointer notes` subsection appended to existing `## Forward-pointer notes` section (~120 LoC; 17-deferral + 1-foot-gun list organized by 8 deferred-cluster families per SPEC §13.4).

- [ ] **Step 5: Apply §13.7 patch** — NEW `## JWKS framework primitive` top-level section (~25 LoC) per ADR-0150 covering package shape + Fetcher lifecycle + refresh schedule + failed-refetch fixed-interval + cross-phase reuse intent + operator-facing semantics.

- [ ] **Step 6: Apply §13.8 patch** — NEW `## JWT verifier framework primitive` top-level section (~25 LoC) per ADR-0151 covering package shape + Token lifecycle + algorithm allow-list + claim validation order + cross-phase reuse intent + ~20 canonical error sentinels.

- [ ] **Step 7: Flip ROADMAP row 17** to `in-progress → done` + sharpen summary with post-impl counts.

- [ ] **Step 8: Advance STATE.md** — `lifecycle-state: phase 17 done; awaiting next planning`; `last-commit: <Task 14 squash>`; `next-skill: (none — phase complete)`; `last-updated: <impl date>`.

- [ ] **Step 9: Run 6-gate phase-done verification** — execute all 6 gate commands per SPEC §14.7; capture verbatim outputs into PROGRESS.md Task 14 entry; all green.

- [ ] **Step 10: Commit**

```bash
git add docs/envoy-go/BEHAVIOR_CONTRACT.md docs/envoy-go/ROADMAP.md docs/envoy-go/STATE.md docs/envoy-go/phases/17-http-filter-jwt-authn/PROGRESS.md
git commit -m "phase 17 Task 14: BEHAVIOR_CONTRACT 6-edit bundle + STATE advance + 6-gate phase-done verification"
```

---

## Task 15: REVIEW.md — end-of-phase review per `superpowers:requesting-code-review` skill

**Files:**
- Create: `docs/envoy-go/phases/17-http-filter-jwt-authn/REVIEW.md`
- Modify: `docs/envoy-go/phases/17-http-filter-jwt-authn/PROGRESS.md` (append Task 15 entry)

Lands the end-of-phase review document per `superpowers:requesting-code-review` skill. Document covers: phase-17 deliverables + ADR roster final state (8 anchored §Decision+§Consequences bodies + ADR-0125 §(xiii) amendment landed at SPEC commit) + SPEC §15 15-claim acceptance checklist verification + the 16 §11 empirical-pin dispositions (6 RATIFIED-AND-EXTENDED + 2 RATIFIED + 1 RATIFIED-AND-REFINED + 1 RATIFIED-PENDING-IMPL-TIME confirmed at Task 8 + 3 REFUTED + 2 PARTIAL + 1 REFUTED-WITH-MIRROR-DECISION) + the 12 §1.1 amendment dispositions + the framework-delta impact assessment (TWO new primitives: `internal/jwks/` + `internal/jwt/`; cross-phase reuse intent codified at ADR-0150 + ADR-0151) + the divergence-window enumeration (`response_code_details` field-emission + `filter_state_rules` silent-ignored + dynamic-metadata family silent-ignored + `jwt_cache_config` no-cache + 3 v1.37.x claim-coverage extensions silent-ignored + `clear_route_cache` implicit-on-side-effect trigger silent-ignored) + ADR-0044 escape-valve disposition (whether 0 or 1 impl-time-unanticipated ADR landed per phase-13/14/16 precedent).

**Precondition:** Task 14 acceptance green.
**Artifact:** new REVIEW.md file.
**Acceptance:** REVIEW.md committed; phase-17 end-state captured.

- [ ] **Step 1: Author REVIEW.md** — structure per `superpowers:requesting-code-review` skill output template + phase-13/14/15/16 REVIEW.md precedent. ~260 LoC.

- [ ] **Step 2: Commit**

```bash
git add docs/envoy-go/phases/17-http-filter-jwt-authn/REVIEW.md docs/envoy-go/phases/17-http-filter-jwt-authn/PROGRESS.md
git commit -m "phase 17 Task 15: REVIEW.md — end-of-phase review"
```

---

## End of phase 17 implementation plan

This plan covers phase-17 jwt_authn — the TENTH §9 HTTP-filters family-row. After Task 15 commits, the phase is `done`; ROADMAP row 17 status reads `done`; STATE.md lifecycle-state reads `phase 17 done; awaiting next planning`; DECISIONS.md tail at ADR-0155 (the 8 ADRs §Context+§Decision+§Consequences all landed; ADR-0125 §(xiii) amendment paragraph landed at SPEC commit); BEHAVIOR_CONTRACT.md carries the 6-edit bundle from SPEC §13; 20 differential fixtures green (0000-0019); 21 fuzzers green at 30s budget; h2spec 53/53 PASS at ADR-0051 pin (unchanged through phase 17); build/vet/lint clean; race-tests clean across all 40+ packages including the 2 NEW framework primitives at `internal/jwks/` + `internal/jwt/`.

The TWO new framework primitives are explicitly cross-phase-reusable at introduction time — `internal/jwks/Fetcher` for future `ext_authz` HTTP-mode + `oauth2` token-endpoint flows; `internal/jwt` for future `jwt_claim_router` + `oauth2` token validation. Phase 17 is the SECOND CONSECUTIVE §9 family-row to introduce two framework primitives in a single phase (after phase 16's matcher-engine + TLS-principal accessor).

Phase 17 is the FIRST §9 family-row to use the **8th canonical per-route pattern** (string-reference-delegation via `oneof{disabled(bool) | requirement_name(string)}` into a separately-named registry). ADR-0125's canonical-pattern roster grows from 7 to 8 via the in-place §(xiii) amendment paragraph landed at SPEC commit.

Phase 17 is the FIRST §9 family-row to land an RFC 6750-conformant Bearer-token challenge deny-path (`SendLocalReply(401|403, getStatusString(reason), {www-authenticate: Bearer realm="<original_uri>"<, error="invalid_token">})`). The 401-vs-403 dispatch table mirrors Envoy verbatim per §1.1 amendment 8 — 403 specifically for `JwtAudienceNotAllowed`, 401 for all other failure-reasons.

After phase 17, the §9 HTTP filters family has TEN concrete filters (cors / fault / header_mutation / local_ratelimit / csrf / buffer / compressor / bandwidth_limit / rbac / jwt_authn); subsequent filters (ext_authz, ext_proc, oauth2, lua, wasm, adaptive_concurrency, admission_control, global_ratelimit) follow the same row-as-its-own-phase pattern per ADR-0106.
