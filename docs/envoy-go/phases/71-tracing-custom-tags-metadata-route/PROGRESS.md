# PROGRESS 71 — tracing `custom_tags` `metadata` type, `ROUTE` MetadataKind only

> Live task ledger for the phase-71 IMPL. The PLAN (`PLAN.md`) is the spine; this file records what ACTUALLY happened per task — red-first verbatims, WHICH break assertion fired (and any that did NOT), substitutions, the T5 dispatch-verify trap result, and the six-gate evidence. The Task ledger is populated at the IMPL, not the PLAN.

## Stage pointer

- **PLAN done** (2026-07-22) — the 9-task SINGLE-FLAT-ROW TDD spine (T1–T9) landed; the SPEC §11 anchors RE-DERIVED at `60f12146` (**32/32 anchor groups EXACT-HOLD — ZERO drift**; the tip is one docs-only commit ahead of the SPEC's cited `fbc86063`, no line shifted) via a read-only re-derivation agent that also confirmed the clone sources (the LANDED phase-70 REQUEST accept arm `config.go:228-244`, the `kindMetadata` REQUEST resolve arm `resolve.go:65-88`, `descend` `:100-114` + `structpbValueToString` `:123-140` reused VERBATIM, the `withMetaTags` seed `fuzz_test.go:94-108` with `meta_bad` a MetadataKey-less ROUTE tag) + the S2 fact (`structpb` ABSENT from `chain.go` — `grep -c` → 0; the new `RouteMetaLookup` method ADDS it) + the 18-caller map (5+6+7; exactly 3 no-chain-404 nil sites `connection.go:330`/`h2dispatch.go:313`/`h3dispatch.go:130`; `chain` var name uniform; the 4th arg APPENDS, the 3rd metaLookup arg untouched) + RD-NOIMPORT-CFG (config.go adds ZERO imports — `k.GetRoute()` on the inferred `k := md.GetKind()`) + RD-FIXTURE (the `0114` chassis has a Lua writer to DROP; `0115` uses static route metadata). **Adversarial verification (PLAN §1.2 — record below.)** Fresh worktree off master `60f12146`, branch `phase-71-tracing-custom-tags-metadata-route-plan`. Row 71 STAYS `in-progress`; DECISIONS UNTOUCHED at the PLAN (ADR-0293 stays PROPOSED); NO deferred sentence narrowed at the PLAN (rolls OUT at the IMPL row-done, the phase-57 precedent).

## Adversarial-pass record (PLAN §1.2)

*(TWO independent verifiers ran against the draft in PRIVATE scratch before landing — `reference_parallel_subagents_private_scratch`; the real repo left untouched, no worktrees registered.)*

- **V1 — code-claims re-derivation + by-execution compile probes.** ZERO SEVERE, ZERO MODERATE, ZERO MINOR — **BUILDABLE as written.** Re-derived every skeleton at `60f12146` + EXECUTED the pivotal S1 equivalence: `descend(NewStructValue(nsStruct), ["outer","inner"])` == REQUEST's `descend(metaLookup-result, ["inner"])`, both yield `"deep"` (IDENTICAL — the FULL-path descent is sound); single-segment resolves; unresolvable → false. The ROUTE accept skeleton byte-identical to REQUEST except `Kind`; `k.GetRoute()` on `*metadatav3.MetadataKind` (compile-proven); config.go NO new import; the `RouteMetaLookup` body nil-safe (EXECUTED against nil `*corev3.Metadata` → `(nil,false)`); `structpb` ABSENT from chain.go; the 18-caller map EXACT (3 nil sites); the fuzz Route→Cluster repoint compiles; `go build ./...` clean; +0 modules; collisions 0. One informational heads-up (not a defect): the naive `resolve_test.go` grep 21 includes a string-literal false positive — 20 real callers, already covered by the PLAN's re-grep instruction.
- **V2 — process/consistency/SPEC-coverage.** ZERO SEVERE, ZERO MODERATE, ZERO MINOR — **STAGE-CLOSE MECHANICS PASS.** Docs-only (only PLAN.md + PROGRESS.md untracked; ADR-0293 PROPOSED §Context-only; row 71 in-progress); SPEC §10/§11 coverage complete (ZERO cited-but-unwritten — no `TestNoNewStat` guard, correctly); ADR-0293 completes IN PLACE at the IMPL (no renumber, next-free ADR-0294); the deferred-sentence narrow at the IMPL row-done not the PLAN; counts consistent (116 now / 117 at IMPL, never current); the router rolls PLAN → phase-71 IMPL; S1–S5 all reflected; break protocol complete; format faithful to the phase-70 PLAN. **The three sentinel commands RUN:** (1) `NOT DONE: row 71`; (2) → **3**; (3) `NEVER OPENED: gRPC/Runtime/WASM` ⇒ does NOT fire.

## Task ledger (filled at the IMPL)

| Task | Status | Commit | Red-first / breaks / notes |
|---|---|---|---|
| T1 config parse (kindMetadataRoute + the ROUTE accept arm) | _pending_ | — | Red: `undefined: kindMetadataRoute`. Accept ROUTE (clone REQUEST, MetaPath FULL); reject CLUSTER/HOST/unset-kind (DEPARTURE) + empty ns/path/segment (PARITY) UNCHANGED. Branch on `k.GetRoute()` (`k := md.GetKind()`). NO new import. Breaks A/B/C. |
| T2 resolve arm (routeMetaLookup 4th param + kindMetadataRoute arm) | _pending_ | — | Red: `ResolveCustomTags` arity (20 callers + the new tests). The request_header default rule; FULL-path descent (NOT `[1:]`); `descend`/`structpbValueToString` reused VERBATIM; NO new import. Breaks D (wrong slice) / E (default rule) / F (nil-tolerance). `-race`. |
| T3 *FilterChain.RouteMetaLookup() accessor + structpb import | _pending_ | — | Red: `RouteMetaLookup undefined`. Wraps `RouteMetadata().GetFilterMetadata()[ns]` via `structpb.NewStructValue`; nil-safe; `(nil,false)` on absent. ADD the `structpb` import to chain.go. Break G (nil-guard). |
| T4 routeMetaLookup thread (18 callers) | _pending_ | — | Red: arity mismatch across the 3 emit methods + test callers. `chain.RouteMetaLookup` APPENDED as the 4th arg at 15 sites, a 2nd `nil` at the 3 no-chain-404 sites; the 3rd metaLookup arg UNTOUCHED. Live ROUTE-metadata-span test. Breaks H/I. `-race`. |
| T5 fuzz seed repurpose + dispatch-verify | _pending_ | — | `withMetaTags`: add `meta_route_ok` (valid ROUTE accept) + repoint `meta_bad` Route→Cluster. Dispatch-verify the seed reaches the metadata arm (`config.go:126` < `:136`). Count 55→55 (+0). |
| T6 fixture 0115 (OTLP, NO writer) | _pending_ | — | The `0114` chassis WITHOUT the Lua writer + a static route `metadata.filter_metadata` block + two ROUTE custom_tags (`route_hit`→the static value, `route_default`→`default_value`). Cross-side EXACT key+value. Breaks J/K/L. Fixtures 116→117. |
| T7 BEHAVIOR_CONTRACT B1–B2 | _pending_ | — | B1 flip the ROUTE line to CONSUMED (SPEC §9 pinned VERBATIM); B2 narrow the departures to CLUSTER/HOST + unset-A. B3 neighborhood byte-unchanged. |
| T8 verify (six-gate + envelope) | _pending_ | — | gofmt/vet/build/`go mod tidy -diff`/lint + FULL **117-dir** differential + cycle guard (`go list -deps ./internal/tracing` filter-free) + `-race` on the 3 touched pkgs + envelope audit + counts. **ROW 71 FLIPS `done`.** |
| T9 ADR-0293 + close | _pending_ | — | ADR-0293 COMPLETED IN PLACE (§Decision + §Consequences; STATUS PROPOSED→COMPLETE; tail stays ADR-0293, next-free ADR-0294 — no renumber). ROADMAP row 71 → `done` + the `ROUTE` MetadataKind narrowed OUT of the live Observability `candidates:` sentence (CLUSTER/HOST become the residual). STATE §Current rolled IN PLACE + counts. Sentinel re-run (does NOT fire). Router rolled to phase-72 BRAINSTORM; memory updates written. |

## Findings carried from the PLAN (RE-DERIVED at `60f12146`; RE-VERIFY at the IMPL tip)

- **RD-EXACT:** 32/32 SPEC §11 anchor groups exact at `60f12146` — no line-boundary correction to carry (the SPEC's cites adopted verbatim; the tip is one docs-only commit ahead of the SPEC's `fbc86063`, no line shifted).
- **RD-KIND:** `customTagKind` `config.go:53-58` = kindLiteral(0,`:54`)/kindRequestHeader(1,`:55`)/kindEnvironment(2,`:56`)/kindMetadata(3,`:57`); ADD `kindMetadataRoute` at iota==4. `CustomTagSpec` `:65-75` already has `MetaNamespace`/`MetaPath`/`DefaultValue`/`HasDefault` — NO new field.
- **RD-REJECT:** the ROUTE reject is `config.go:245-246` (`case k.GetRoute() != nil:` → `"tracing: custom_tags metadata tag %q route kind unsupported"`) — the whole `case` is REPLACED by an accept arm cloning the REQUEST accept `:228-244` (`Kind: kindMetadataRoute`). The `k := md.GetKind()` bind `:224`, unset-A `:226-227`, CLUSTER `:247-248`, HOST `:249-250`, `default:` unset-B `:251-252`, empty-tag `:198-200`, first-wins dedup `:257-261` UNCHANGED.
- **RD-NOIMPORT-CFG:** config.go imports (`:7-16`) do NOT include `metadatav3` and do NOT need to — `k.GetRoute()` is on the inferred `k := md.GetKind()`. The ROUTE arm adds ZERO imports to config.go. (fuzz_test.go's `metadatav3` import stays.)
- **RD-RESOLVE-4TH:** `ResolveCustomTags` `resolve.go:26` currently 3-param (`specs, headerLookup, metaLookup func(ns,key)...`); ADD a 4th `routeMetaLookup func(ns string) (*structpb.Value, bool)` (ONE-arg, DISTINCT from the two-arg `metaLookup`). The 3rd is UNTOUCHED.
- **RD-ROUTE-ARM:** the REQUEST arm `resolve.go:65-88` (`metaLookup(ns, MetaPath[0])` `:75` → `descend(v, MetaPath[1:])` `:78`). The ROUTE arm clones the default rule but calls `routeMetaLookup(ns)` (one-arg) + descends `descend(v, s.MetaPath)` — the FULL path (the `[1:]` is a REQUEST-only Bucket-pre-keying artifact).
- **RD-DESCEND / RD-SERIALIZE:** `descend` `resolve.go:100-114` (`func descend(v *structpb.Value, segs []string) (*structpb.Value, bool)`, empty segs → `(v,true)`) + `structpbValueToString` `:123-140` (string→raw incl. ""; NullValue→`("",false)`; else `protojson.Marshal`+`json.Compact` load-bearing) — both REUSED VERBATIM by the ROUTE arm. `resolve.go` imports `structpb` `:9`; NewStructValue NOT used there. NO new import in resolve.go.
- **RD-SEAM (S2):** `chain.go` does NOT import `structpb` (`grep -c` → 0). ADD `func (c *FilterChain) RouteMetaLookup(ns string) (*structpb.Value, bool)` beside `DynamicMetadata()` `:1022`; body wraps `RouteMetadata().GetFilterMetadata()[ns]` via `structpb.NewStructValue`, `(nil,false)` on nil/absent. `RouteMetadata()` `:1156` (field `routeMetadata *corev3.Metadata` `:199`); `GetFilterMetadata()` `base.pb.go:851` nil-receiver-safe; `NewStructValue` `struct.pb.go:396` = `func(*Struct) *Value`. ADD the structpb import to chain.go (existing module).
- **RD-CALLERS:** 18 callers exact (5 connection.go + 6 h2dispatch.go + 7 h3dispatch.go); the 3 nil sites `connection.go:330`/`h2dispatch.go:313`/`h3dispatch.go:130` (BEFORE the chain built); the 15 chain-in-scope sites `connection.go:464/597/699/777` · `h2dispatch.go:396/530/577/584/613` · `h3dispatch.go:210/280/341/367/373/395` pass `chain.RouteMetaLookup` (var name `chain` uniform). h3:210 is a chain site (traceDecision-nil-but-chain-in-scope, valid-but-never-reached). The 4th arg APPENDS; the existing 3rd metaLookup arg (`chain.DynamicMetadata().Get` / `nil`) is UNTOUCHED. `accesslog_emit.go` already imports `structpb` `:9` (gates `:30/:89/:151`; ResolveCustomTags calls `:57/:118/:179`).
- **RD-SEED-SEEDED:** `SetRouteMetadata` seeds `RouteMetadata()` per-request at `connection.go:433`/`h2dispatch.go:375`/`h3dispatch.go:195` — READ-ONLY substrate, NOT edited.
- **RD-FUZZ:** `withMetaTags` `fuzz_test.go:94-108`: `meta_ok` REQUEST (`:97-101`), `meta_bad` ROUTE MetadataKey-less (`:102-104`) — REPURPOSE (add `meta_route_ok` valid ROUTE accept; repoint `meta_bad` Route→Cluster). Dispatch: `parseCustomTags` `config.go:126` precedes `provider required` `:136`. `metadatav3` already imported. Count 55→55 (+0).
- **RD-FIXTURE:** `0114-tracing-custom-tags-metadata/` = `driver/driver.go`, `envoy.yaml`, `envoy-go.yaml`, `expectations.yaml`, `README.md`, `scripts/writer.lua`. Has a Lua writer (`dynamicMetadata():set(...)`) + two REQUEST tags. `0115` clones the OTLP chassis but DROPS the Lua filter/script/REQUEST tags, adds a static route `metadata.filter_metadata` block + two ROUTE custom_tags. All ports templated; NO writer.
- **RD-TESTCALLERS:** `resolve_test.go` has 20 direct `ResolveCustomTags(` sites (lines 34,72,97,105,124,131,138,146,188,198,207,216,244,254,268,276,284,292,304,311) — all 3-arg → += trailing `nil`. Two hcm test files (`accesslog_emit_test.go`, `span_emit_test.go`) call the emit methods → += trailing `nil`. Re-grep at the IMPL tip.
- **RD-IDENT:** `kindMetadataRoute`/`routeMetaLookup`/`RouteMetaLookup`(`(ns`) all collision-free repo-wide (0 hits each). `descend`/`structpbValueToString`/`CustomTagSpec` fields/`kindMetadata` reused (landed at phase 70).
- **RD-MOD:** +0 go.mod modules — the only new import is `structpb` into `chain.go` (an existing module — `corev3`/`structpb`/`type/metadata/v3`/`protojson` all resolve at the touched packages' dep closure). `go mod tidy -diff` anticipated EMPTY — re-check `git diff go.mod` after tidy at T8.
