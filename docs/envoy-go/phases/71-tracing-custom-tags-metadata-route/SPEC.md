# SPEC 71 — tracing `custom_tags` `metadata` type, `ROUTE` MetadataKind only (the SEVENTEENTH Observability-family row + the FIFTH tracing `custom_tags` row + the SECOND `metadata` MetadataKind; lifts the `custom_tags metadata route kind unsupported` reject at `internal/tracing/config.go:245-246` and resolves a `ROUTE`-kind route-config metadata value out of the matched route's STATIC `metadata.filter_metadata[ns]` — over the LANDED `FilterChain.RouteMetadata() *corev3.Metadata` accessor [`chain.go:1156`, phase 24.2 / ADR-0165] — onto the ingress span via the shared `ResolveCustomTags`→`Span.Attrs []KV` seam [both OTLP + Zipkin]; `CLUSTER`/`HOST`/unset-kind reject loudly with distinct substrings [envoy-go-strict, ADR-0080]; a SINGLE FLAT ROW; ONE new OTLP fixture `0115`)

> **Stage:** SPEC (lifecycle-state 1 → 2). Docs-only — ZERO production `.go`, apart from the ADR-0293 §Context append to `DECISIONS.md`. Worktree `.worktrees/phase-71-spec`, branch `phase-71-tracing-custom-tags-metadata-route-spec`, tip `fbc86063` (the phase-71 BRAINSTORM squash), per `feedback_git_worktrees`.
>
> **Row 71 STAYS `in-progress`** — the IMPL flips it `done` at its six-gate (ADR-0106, the SOLE leg — a SINGLE FLAT ROW, §10). **ADR-0293's §Context DRAFTS HERE** (ADR-0044-as-used); §Decision/§Consequences land at the IMPL IN PLACE. **The DECISIONS tail flips ADR-0292 → ADR-0293 AT THIS SPEC COMMIT** (§14) — the §Context append IS the tail flip; next-free becomes ADR-0294.
>
> **Evidence base — code RE-DERIVED at `fbc86063` this SPEC** (an independent read-only re-derivation agent re-opened EVERY anchor in BRAINSTORM §6/§11 + the go-control-plane `CustomTag_Metadata`/`MetadataKey`/`MetadataKind` proto types + the `corev3.Metadata` shape + the `structpb.NewStructValue` wrapper + the PGV `.validate.go` rules + the fuzz host + the collision greps + the 18-site emit-caller roster) **+ SIX EXECUTED live-probe arms this SPEC** (the D-CTMR docket — `envoyproxy/envoy:contrib-v1.37.2`, fresh container per arm, a STATIC route-config metadata source [NO writer — unlike phase 70's Lua REQUEST arm], a standalone host OTLP-gRPC receiver + a raw-TCP Zipkin dumper, decode-actually-ran asserted per arm — 6 numbered conclusions "P1..P6"). Claims below are attributed **EXECUTED** (a P-arm conclusion, named) or **RE-DERIVED** (a declaration reading with `file:line` at `fbc86063`); a reading is never presented as a probe. **RE-DERIVE this document; do not execute it** (`feedback_brief_citations_not_evidence`, `reference_quoting_is_not_executing`).
>
> **Baselines re-verified mechanically at `fbc86063`:** fixtures **116** (`ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0114-tracing-custom-tags-metadata` ⇒ next fixture `0115`; tracing/OTLP fixtures TEMPLATE all ports — no in-container port to pick) · fuzzers **55** (`grep -rn '^func Fuzz' --include='*.go' internal/ test/ | wc -l`) · DECISIONS tail **ADR-0292** (COMPLETE; next-free **ADR-0293**; `grep -c '^## ADR-0293'` → 0, FREE) · BackendKind tail **38** · stat surface **1201** · go.mod modules **2** (lineage figure; `corev3`/`structpb`/`type/metadata/v3`/`protojson` already resolve at the direct `go-control-plane/envoy v1.32.4` and the transitive `google.golang.org/protobuf`).
>
> **Sentinel expectation at this stage:** check (1) prints `NOT DONE: row 71` (registered `in-progress` at the BRAINSTORM stage-close); checks (2)+(3) unchanged (three live `candidates:` sentences; `NEVER OPENED: gRPC/Runtime/WASM`). **No deferred sentence is edited at ANY pre-IMPL stage of this row** — the `ROUTE` MetadataKind is NAMED in the live Observability `candidates:` sentence and rolls OUT at the phase-71 IMPL row-done edit, per the verified phase-57 graphite precedent (§12).

---

## 1. Purpose / Mission

Phase 46 built the HCM-native tracing engine — the per-request ingress SERVER `Span` with its built-in STRING attributes, exported over BOTH the OTLP (`OTLPExporter`) and Zipkin (`ZipkinExporter`) transports through the shared provider-neutral `Span.Attrs []KV` seam (ADR-0260/0261). Phases 59/62/63 added the first three `custom_tags` SOURCE types — `literal` (ADR-0277), `request_header` (ADR-0283), `environment` (ADR-0284). Phase 70 (ADR-0292) added the FOURTH and LAST `CustomTag` SOURCE type — the `metadata` type — scoped to the **`REQUEST`** MetadataKind (resolving a per-request dynamic-metadata `Bucket` value), leaving the other three MetadataKinds (`ROUTE`/`CLUSTER`/`HOST`) + an unset kind as loud strict-rejects.

Phase 71 lands the **SECOND `metadata` MetadataKind — `ROUTE`**. It lifts the `custom_tags metadata route kind unsupported` reject (`config.go:245-246`) and resolves an operator-configured `{tag, metadata:{kind:ROUTE, metadata_key:{key:<ns>, path:[<seg>...]}, default_value:<fb>}}` out of the **matched route's STATIC config metadata** — `route.metadata.filter_metadata[ns] → *structpb.Struct`, reachable at the emit sites over envoy-go's LANDED `func (c *FilterChain) RouteMetadata() *corev3.Metadata` accessor (`chain.go:1156`, phase 24.2 / ADR-0165, seeded per-request by `SetRouteMetadata(entry.metadata)` at the three dispatch sites `connection.go:433` / `h2dispatch.go:375` / `h3dispatch.go:195`). It walks the `MetadataKey.path`, serializes the resolved `structpb.Value` to a STRING, and emits it as a `{tag, value}` span attribute on BOTH exporters. `CLUSTER`/`HOST`/unset-kind continue to reject loudly with distinct substrings (envoy-go-strict, ADR-0080 — the reference ACCEPTS them, so this stays a real DEPARTURE, proved by P1).

The row composes on FULLY-LANDED substrate and clones the phase-70 machinery: **ZERO new packages, +0 go.mod modules, +0 stats, +0 BackendKinds, +0 fuzzers** (a `FuzzHCMConfigParse` seed extension). `descend` (`resolve.go:100`) and `structpbValueToString` (`resolve.go:123`) are REUSED VERBATIM. New exported symbols are confined to `internal/tracing` (the `ResolveCustomTags` signature grows a `routeMetaLookup` param) **plus ONE new accessor `func (c *FilterChain) RouteMetaLookup(ns string) (*structpb.Value, bool)` on `internal/filter/http`** — the ROUTE analogue of phase-70's `DynamicMetadata()` add (§3.5). The `internal/xds` zero-new-symbol discipline is UNTOUCHED.

### 1.1 BRAINSTORM anticipations — what HELD, what this SPEC SHARPENS *(the drift ledger; drift found is the point)*

**HELD (re-derived / EXECUTED-probed this session):** the ROUTE reject at `config.go:245-246` (`case k.GetRoute() != nil:` → `"route kind unsupported"`) + the REQUEST accept arm to clone (`:228-244`) + the CLUSTER/HOST/unset-kind rejects (`:247-252`) + the `customTagKind` const block (`:53-58`) + the `CustomTagSpec` struct (`:65-75`, `MetaNamespace`/`MetaPath`/`DefaultValue`/`HasDefault` all present and reusable) — RE-DERIVED exact. The `k := md.GetKind()` bind (`:224`) with the four kind-getters on `*metadatav3.MetadataKind` (the phase-70 V1 SEVERE) — RE-DERIVED exact. `descend` (`resolve.go:100-114`, `func(v *structpb.Value, segs []string) (*structpb.Value, bool)`) + `structpbValueToString` (`:123-140`) — reusable VERBATIM (proto-agnostic, source-shape-independent). `RouteMetadata()` (`chain.go:1156-1158`, `return c.routeMetadata`), `SetRouteMetadata` (`:1150`) seeded at the three dispatch sites, `DynamicMetadata()` (`:1022`, landed phase 70) — RE-DERIVED exact. The go-control-plane v1.32.4 proto (D-CTMR-PROTO): `CustomTag_Metadata{Kind *metadatav3.MetadataKind; MetadataKey *MetadataKey; DefaultValue string}` (`custom_tag.pb.go:326`, getters GetKind/GetMetadataKey/GetDefaultValue only), `MetadataKind` oneof getters `GetRequest`/`GetRoute`/`GetCluster`/`GetHost` on `*MetadataKind` (`metadata.pb.go:164-185`; `k.GetRoute()` a non-nil marker), `MetadataKey{Key; Path []*PathSegment}`, `PathSegment.GetKey()` — RE-DERIVED exact. **The six D-CTMR live-probe arms all HELD (P1..P6):** the reference boots ROUTE/CLUSTER/HOST; emits `{key=literal tag name, value=serialized string}` appended after built-ins on OTLP + in the Zipkin `tags` map; the P3 serialization table (string→raw / number→decimal / bool / struct·list→compact JSON); the `request_header` default rule (present-empty EMITS `""`); the nested path-walk. The 18-caller roster (5+6+7, 3 nil sites) is BYTE-IDENTICAL to phase 70. +0 stats/packages/modules/fuzzers, SINGLE FLAT ROW — all HELD.

**SHARPENED — each flagged where it lands:**

| # | BRAINSTORM/router said | Sharpened to | § |
|---|---|---|---|
| **S1** | **D-CTMR-RESOLVE-SEAM:** a new nil-tolerant `routeMetaLookup func(ns string) (*structpb.Value, bool)` param; the arm descends the FULL `MetaPath` (BRAINSTORM §2.3) | **CONFIRMED exact + the seam is a NEW 4th param, DISTINCT from `metaLookup`.** `ResolveCustomTags` currently has `(specs, headerLookup, metaLookup func(ns, key string) ...)` (`resolve.go:26`). The REQUEST `metaLookup` is TWO-arg (the Bucket pre-keys the first segment → `metaLookup(ns, MetaPath[0])` then `descend(v, MetaPath[1:])`). ROUTE is ONE-arg — `routeMetaLookup(ns)` returns the namespace struct wrapped as a `StructValue` via `structpb.NewStructValue`, and the arm descends the **FULL** `MetaPath` (the `[1:]` is a REQUEST-only artifact). So phase 71 adds a FOURTH param, not a re-use of the third. `descend`/`structpbValueToString` unchanged. | §3.3 |
| **S2** | **D-CTMR-BUCKET-SEAM:** thread a `chain.RouteMetadata`-derived closure at the 15 span sites (BRAINSTORM §2.5/§6) | **`structpb` is ABSENT from all three dispatch files + `chain.go`.** Inline closures would add the `structpb` import to `connection.go`/`h2dispatch.go`/`h3dispatch.go` (three files). **DECISION: add a NEW method `func (c *FilterChain) RouteMetaLookup(ns string) (*structpb.Value, bool)` on `internal/filter/http`** (wrapping `c.RouteMetadata().GetFilterMetadata()[ns]` via `structpb.NewStructValue`, `(nil,false)` on nil/absent), centralizing the `structpb` import to `chain.go` alone and threaded as a method value `chain.RouteMetaLookup` — SYMMETRIC with the landed `chain.DynamicMetadata().Get`. This is the ROUTE analogue of phase-70's `DynamicMetadata()` accessor add — ONE new export outside `internal/tracing`. | §3.5 |
| **S3** | **D-CTMR-REJECT:** unset-kind rejects loudly, an envoy-go-strict departure (BRAINSTORM §2.4) | **The unset-kind reject splits by sub-case.** RE-DERIVED against the PGV `.validate.go`: sub-case **A** (whole `*MetadataKind` nil — `config.go:226-227`): `CustomTag_Metadata.validate` has NO `required` rule on `Kind`, so the reference ACCEPTS → envoy-go's reject is a **DEPARTURE**. Sub-case **B** (MetadataKind present, oneof empty — `config.go:251-252` `default:`): `MetadataKind.validate` fires "value is required" (PGV oneof), so the reference REJECTS → envoy-go's reject is **PARITY**. Both already reject in the landed code (no code change); this is a WORDING refinement for the BC/ADR (the blanket "unset-kind is a departure" is precise only for sub-case A). | §3.6 |
| **S4** | **D-CTMR-FUZZSEED:** extend the `withMetaTags` seed with a ROUTE tag (BRAINSTORM §2.7) | **The existing `meta_bad` seed exercises the reject arm phase 71 REMOVES.** `fuzz_test.go:94-108`'s `withMetaTags` carries `meta_bad` = a ROUTE-kind tag with NO `MetadataKey` — today it hits the ROUTE-reject arm. After the lift it would hit the ROUTE-ACCEPT arm, then the empty-namespace reject (a different arm). **DECISION: repurpose** — ADD a VALID ROUTE-accept tag (`meta_route_ok`, namespace + segment + default) to exercise the new accept arm, and REPOINT `meta_bad`'s kind to `CLUSTER` (or `HOST`) so a kind-unsupported reject arm stays exercised. +0 fuzzers (`f.Add`, not `func Fuzz`). | §7 |
| **S5** | **D-CTMR-WIRE:** the reference emits the tag in config order (implicit) | **Intra-custom-tag order is INTERNAL iteration order, NOT config order (P2).** The observed OTLP order was `list, num, str, bool, …` — an internal map iteration, not the config sequence — though ALL custom tags are strictly AFTER the built-ins (`[16]..` after `[00]..[15]`). Assertions MUST be key-based (envoy-go's `upsertAttr`/`Span.Attrs` already are; the `0115` driver asserts by key). No code impact — a test-authoring note. | §8 |

### 1.2 SPEC-time verification record

The read-only re-derivation agent (tree at `fbc86063`) re-opened all BRAINSTORM §6/§11 anchors + the proto/PGV/fuzz/collision anchors + the `structpb.NewStructValue`/`GetFilterMetadata` shapes + traced the emit roster across all 18 call sites: **every anchor HELD at real line numbers; five sharpenings (S1 the distinct 4th param, S2 the `structpb`-import-centralizing `RouteMetaLookup` method, S3 the unset-kind sub-case split, S4 the fuzz-seed repurpose, S5 the internal-iteration-order note); NO code-blocking correction.** No claim was left on an un-re-derived citation. The six live-probe arms (P1..P6) were EXECUTED against `contrib-v1.37.2` with a STATIC route-config metadata source (no writer) and a decode-ran assert per arm; every SPEC behavioral assumption HELD (reference boots ROUTE/CLUSTER/HOST + PGV-rejects empty key/path; tag key = literal name; value = serialized string appended after built-ins; both exporters; the P3 table; present-empty emits `""`; multi-segment walk; unresolvable segment → default/omit). Collision greps: `kindMetadataRoute` → 0, `routeMetaLookup` → 0, `RouteMetaLookup` → 0. **Adversarial-pass record:** TWO independent verifiers ran against the draft in PRIVATE scratch before landing (`reference_parallel_subagents_private_scratch`) — recorded at §16.

---

## 2. Non-purposes (deferred; per BRAINSTORM §1.2 + §8)

NOT the `CLUSTER`/`HOST` MetadataKinds (rejected loudly — §3.6). `CLUSTER` is the documented `upstream_cluster` framework gap (`reference_tracing_upstream_cluster_framework_gap` — the selected cluster's identity/metadata is NOT plumbed to the span-emit seam; `accesslog_emit.go` hard-codes `UpstreamCluster: ""`); `HOST` has only the reduced `envoy.lb` scalar-subset map in scope at the seam, not the full `*corev3.Metadata`. NOT any other tracing follow-on (`spawn_upstream_span` / `http_service` / force-trace — each larger, all loud-rejects today). NOT a change to `max_path_tag_length` (orthogonal, untouched). NOT the 4 EMPTY built-in span attributes (`upstream_cluster`/`node_id`/`zone`/`peer.address` — the framework gap). NOT the `ssl` handshake-outcome stat family (framework surgery, ADR-0286 C3). NOT any new provider/transport/stat/package/module. NOT a full non-string-value serialization guarantee beyond the P3-pinned common types (number-precision edges + `NullValue` are documented boundaries carried from ADR-0292 — §3.3). NOT a separate Zipkin fixture (the `Span.Attrs` seam is shared and P3 proved the Zipkin emit LIVE — §8, the 59/62/63/70 single-OTLP-dir precedent). NOT a change to the `internal/dynamicmetadata` package (ROUTE reads route config, not the Bucket — that package is BYTE-UNTOUCHED).

---

## 3. The change — the D-CTMR-* docket disposed one-for-one

### 3.1 D-CTMR-SCOPE / D-CTMR-PROTO — the `ROUTE`-only slice on the exact v1.32.4 proto *(RE-DERIVED)*

`CustomTag.GetMetadata()` returns `*CustomTag_Metadata{Kind *metadatav3.MetadataKind, MetadataKey *metadatav3.MetadataKey, DefaultValue string}`. Kind detection (the landed shape, `config.go:224`): `k := md.GetKind()`; `k == nil` → unset sub-case A (reject); `k.GetRequest() != nil` → REQUEST (accepted @70); `k.GetRoute() != nil` → ROUTE (accept @71); `GetCluster`/`GetHost` non-nil → the two remaining departures (reject); `default:` → unset sub-case B (reject). `MetadataKey.GetKey()` is the namespace; `MetadataKey.GetPath()` is `[]*PathSegment`, each KEY-ONLY (`GetKey()`). config.go needs **NO new import** (the kind-getters are reached through `md.GetKind()` method calls without naming `metadatav3`; `kindMetadataRoute` is a package const). All in the direct `go-control-plane/envoy v1.32.4` — **+0 go.mod modules**.

### 3.2 D-CTMR-CONFIG-KIND / D-CTMR-CONFIG-SEAM — a `kindMetadataRoute` const + the ROUTE accept arm *(RE-DERIVED)*

`config.go`: add `kindMetadataRoute` to the `customTagKind` iota block (`:53-58`, inserting as iota==4 after `kindMetadata`; collision-checked → 0 hits) and REUSE the `CustomTagSpec` `MetaNamespace`/`MetaPath`/`DefaultValue`/`HasDefault` fields (NO new field). The `parseCustomTags` ROUTE arm REPLACES the `:245-246` reject, cloning the REQUEST accept (`:228-244`):
```
case k.GetRoute() != nil:
    mk := md.GetMetadataKey()
    if mk.GetKey() == "" { return nil, fmt.Errorf("tracing: custom_tags metadata tag %q empty namespace", tag) }
    if len(mk.GetPath()) == 0 { return nil, fmt.Errorf("tracing: custom_tags metadata tag %q empty path", tag) }
    path := make([]string, 0, len(mk.GetPath()))
    for _, seg := range mk.GetPath() {
        if seg.GetKey() == "" { return nil, fmt.Errorf("tracing: custom_tags metadata tag %q empty path segment", tag) }
        path = append(path, seg.GetKey())
    }
    dv := md.GetDefaultValue()
    spec = CustomTagSpec{Key: tag, Kind: kindMetadataRoute, MetaNamespace: mk.GetKey(), MetaPath: path, DefaultValue: dv, HasDefault: dv != ""}
```
IDENTICAL to the REQUEST arm except `Kind: kindMetadataRoute` and `MetaPath` carrying the FULL path (no `[1:]` slicing — that happens REQUEST-only at resolve). The `CLUSTER`/`HOST`/unset-kind rejects (`:247-252`) are UNCHANGED. The empty-namespace/empty-path/empty-segment rejects are PGV-PARITY (the reference boot-rejects them too, P1); the ROUTE lift makes the reference-accepted ROUTE tag PARSE on envoy-go; `CLUSTER`/`HOST`/unset-A stay envoy-go-strict DEPARTURES (P1/S3). First-wins dedup (`:257-262`) is UNCHANGED (a later same-key ROUTE tag drops after structural validation).

### 3.3 D-CTMR-RESOLVE-SEAM + D-CTMR-VALUE-SERIALIZE — the `kindMetadataRoute` resolve arm *(RE-DERIVED S1; EXECUTED P4/P6)*

`resolve.go`: `ResolveCustomTags` grows a FOURTH nil-tolerant param `routeMetaLookup func(ns string) (*structpb.Value, bool)` (distinct from the REQUEST `metaLookup`, S1) + a `kindMetadataRoute` arm:
```
var v *structpb.Value; var ok bool
if routeMetaLookup != nil { v, ok = routeMetaLookup(s.MetaNamespace) }  // nil-tolerant; namespace struct as a StructValue
if ok { v, ok = descend(v, s.MetaPath) }        // FULL MetaPath (NOT [1:] — the ROUTE difference)
if ok {
    if str, emit := structpbValueToString(v); emit {  // string→raw; non-string→compact JSON (P4)
        out = append(out, KV{Key: s.Key, Str: str}); continue
    }
}
if s.HasDefault { out = append(out, KV{Key: s.Key, Str: s.DefaultValue}) } // else omit
```
- **`routeMetaLookup(ns)`** returns `structpb.NewStructValue(md.GetFilterMetadata()[ns])` when the namespace struct is present, or `(nil,false)` when the `*corev3.Metadata` is nil OR the namespace is absent (`GetFilterMetadata()` is nil-receiver-safe; a map miss yields a nil `*structpb.Struct`). The returned Value is a `StructValue` kind, so `descend`'s first iteration walks its `.Fields[MetaPath[0]]` — the FULL path descends correctly from the wrapped namespace struct.
- **`descend`** and **`structpbValueToString`** are REUSED VERBATIM (proto-agnostic; source-shape-independent). The P4 table (string→raw, number→canonical decimal, bool→`true`/`false`, struct/list→`protojson.Marshal`+`json.Compact`) is EXECUTED-confirmed for ROUTE (P4). An unresolvable segment (P6 `[structk, nope]`) → `false` → default/omit.

The default rule is the `request_header` rule (present-empty EMITS `""`, D-CTMR-DEFAULT, P5): `HasDefault = DefaultValue != ""` at parse; the arm mirrors `kindRequestHeader`, NOT `kindEnvironment`. `resolve.go` needs NO new import (`structpb` present since phase 70; `structpb.NewStructValue` lives on the emit side, §3.5).

### 3.4 D-CTMR-DEFAULT — the `request_header` default rule *(EXECUTED P5)*

P5 (§1.1): ROUTE default/omit is IDENTICAL to phase-70 REQUEST — resolve-if-resolved (present-empty `""` emits `""`); else `default_value` if non-empty; else omit. `t_default_absent` (absent path + non-empty default) → emits the default; `t_default_empty` (present `""` value) → emits `""` (NOT the default); `t_omit` (absent path + no default) → OMITTED from the span. A `resolve_test.go` matrix pins all three arms + the P6 unresolvable-segment→default edge, each an independent `Errorf` (`reference_fatalf_makes_assertions_unreachable`).

### 3.5 D-CTMR-BUCKET-SEAM (route variant) — a NEW `*FilterChain.RouteMetaLookup()` method + a `routeMetaLookup` thread at the 18 emit sites *(the S2 decision; RE-DERIVED across all 18 call sites)*

The three emit methods `emitAccessLog`/`emitAccessLogH2`/`emitAccessLogH3` (`accesslog_emit.go:27`/`:87`/`:149`, `ResolveCustomTags` at `:57`/`:118`/`:179`) grow a nil-tolerant `routeMetaLookup func(ns string) (*structpb.Value, bool)` param passed straight to `ResolveCustomTags`. **DECISION (S2): add a NEW method `func (c *FilterChain) RouteMetaLookup(ns string) (*structpb.Value, bool)` on `internal/filter/http`** (beside `RouteMetadata()`/`DynamicMetadata()`):
```
func (c *FilterChain) RouteMetaLookup(ns string) (*structpb.Value, bool) {
    st := c.RouteMetadata().GetFilterMetadata()[ns]
    if st == nil { return nil, false }
    return structpb.NewStructValue(st), true
}
```
This centralizes the `structpb` import to `chain.go` alone (it currently lacks it) — cleaner than adding `structpb` to the three dispatch files via inline closures — and threads as a method value `chain.RouteMetaLookup`, SYMMETRIC with the landed `chain.DynamicMetadata().Get`. **KEY SIMPLIFIER (re-derived, unchanged from phase 70):** the span block fires ONLY when `traceDecision != nil && traceDecision.Sample`, and the 3 no-chain 404 sites (`connection.go:330` / `h2dispatch.go:313` / `h3dispatch.go:130`, which return/synthesize BEFORE the `chain` is built) pass `traceDecision == nil` — so `ResolveCustomTags` is provably NEVER reached there; those three sites pass `nil` for BOTH `metaLookup` and `routeMetaLookup`. The 15 span-capable callers pass `chain.RouteMetaLookup` (chain in scope at every one). This leaks NO `*corev3.Metadata` through the emit signatures (only `*structpb.Value`, already transitively present in `accesslog_emit.go`). The full 18-caller roster (§11) is applied MECHANICALLY at the IMPL.

### 3.6 D-CTMR-REJECT — the departure/parity split *(EXECUTED P1; RE-DERIVED the PGV `.validate.go`)*

P1 (`--mode validate` on `contrib-v1.37.2`): `kind:{route:{}}` / `{cluster:{}}` / `{host:{}}` all **BOOT-OK** on the reference (the oneofs are valid; `CustomTag_Metadata.Kind` is not PGV-`required`) — so envoy-go's loud reject of `CLUSTER`/`HOST` (and unset sub-case A) is a real envoy-go-strict DEPARTURE (ADR-0080); phase 71 LIFTS `ROUTE`. Empty `metadata_key.key` → reference PGV-REJECT (`min_len 1`); empty/omitted `path` → reference PGV-REJECT (`min_items 1`); empty path segment → PGV-REJECT (`min_len 1`) — so envoy-go's empty-namespace/empty-path/empty-segment rejects are PARITY. The unset-kind split (S3): sub-case A (nil `*MetadataKind`) is a DEPARTURE (reference accepts); sub-case B (present, empty oneof) is PARITY (`MetadataKind` oneof "value is required"). BEHAVIOR_CONTRACT records the categories distinctly. `config_test.go` pins each retained reject substring; the CLUSTER/HOST/unset-A departures are SUBJECT unit tests (the reference boots them, so NOT cross-side-informative — no fixture arm). No existing reject is retired beyond the ROUTE arm; the CLUSTER/HOST/unset rejects are UNCHANGED byte-for-byte.

---

## 4. Framework primitives — 0 new packages, 0 new go.mod modules

No new seam, applier, resource type, or wire shape. **REUSES verbatim:** the phase-46 `Span`/`Span.Attrs []KV`/`KV`/`BuildServerSpan`/`upsertAttr` + BOTH exporters (one `Attrs` change covers OTLP + Zipkin) · the phase-59/62/63/70 `parseCustomTags` (the parse home + first-wins dedup) + `ResolveCustomTags` (the resolve home) + the three `accesslog_emit.go` emit sites · the phase-70 `descend`/`structpbValueToString` (CLONED-once, reused verbatim) · the landed `FilterChain.RouteMetadata()` (`chain.go:1156`, the ROUTE-kind source; nil-tolerant via `GetFilterMetadata`) · `FuzzHCMConfigParse` (the fuzz host). **NEW exports:** `func (c *FilterChain) RouteMetaLookup(ns string) (*structpb.Value, bool)` on `internal/filter/http` (S2) + the `ResolveCustomTags` signature growth (a 4th param) + the `kindMetadataRoute` const (all in `internal/tracing`). **+0 go.mod modules:** `corev3`/`structpb`/`type/metadata/v3`/`protojson` all already resolve at existing directs; `chain.go` gains a `structpb` IMPORT (an existing module — `+0` go.mod), `internal/tracing` needs NO new import; `go mod tidy -diff` anticipated EMPTY (re-checked at the IMPL, `reference_new_subpackage_pulls_transitive_module`). **Cycle guard STANDS:** `internal/tracing` gains NO import of a filter package (the `descend` clone keeps it self-contained; the `RouteMetaLookup` wrapping lives on the `internal/filter/http` side, not `internal/tracing`); re-verify `go list -deps ./internal/tracing` (no `...`) at the IMPL.

## 5. Identifier hygiene *(collision checks — RE-DERIVED at `fbc86063`)*

`.go` grep repo-wide → **ZERO hits** for `kindMetadataRoute` / `routeMetaLookup` / `func (c *FilterChain) RouteMetaLookup`. `metadatav3` is the ESTABLISHED alias (ratelimit/fuzz_test.go) — not newly needed in config.go. `structpb.NewStructValue` exists with signature `func(*structpb.Struct) *structpb.Value` (a version-stable public API; the exact `struct.pb.go` line is version-dependent and NOT pinned). Fixture `0115-tracing-custom-tags-metadata-route` does not exist (tail `0114`); no port to claim (all templated).

## 6. Reject roster — added / retained

**No existing reject is retired or flipped beyond the ROUTE arm.** The `route kind unsupported` reject (`:245-246`) is REPLACED by the accept-ROUTE + the SAME three structural rejects (empty namespace / empty path / empty segment — PGV-PARITY, §3.2/§3.6). The `CLUSTER`/`HOST`/unset-kind rejects (`:247-252`) are UNCHANGED byte-for-byte. `literal`/`request_header`/`environment`/`metadata`-REQUEST arms + the empty-tag reject are UNCHANGED. Roster byte-diffed at the IMPL (`reference_lifted_reject_hidden_enforcement` — confirm the lifted `route` reject silently enforced nothing else; it did not — it was a pure kind-unsupported guard).

## 7. Stat surface **+0** · Fuzz **+0 fuzzers, SEED repurpose, dispatch-verified** *(D-CTMR-STATS / D-CTMR-FUZZSEED)*

**Stats: +0; surface stays 1201.** A span attribute is emitted on the wire, not registered. No `TestNoNewStat` guard is added (tracing has no per-tag stat surface; the phase-59/62/63/70 custom_tags rows added none).

**Fuzz: +0 fuzzers; count stays 55.** The ROUTE accept + the retained rejects are exercised by REPURPOSING the phase-70 `withMetaTags` seed (`fuzz_test.go:94-108`, S4): (a) ADD a VALID ROUTE-accept tag `meta_route_ok` (`Kind: MetadataKind_Route_`, `MetadataKey{Key:"envoy.test", Path:[{Key:"k"}]}`, a default) to exercise the new accept arm; (b) REPOINT the existing `meta_bad` tag's kind from `Route` to `Cluster` (keeping it MetadataKey-less) so a kind-unsupported reject arm stays exercised. **Dispatch-verify (the named trap):** the custom_tags loop runs BEFORE the provider check (`config.go:126` precedes the `provider required` at `:135`), so a well-formed HCM tracing block reaches the metadata arm. Reconcile `grep -rn '^func Fuzz' internal/ test/ | wc -l` == 55 BEFORE and AFTER (`reference_fuzzer_count_docs_drift`).

## 8. Differential fixture — ONE new `0115-tracing-custom-tags-metadata-route` (OTLP) *(D-CTMR-FIXTURE — DECIDED: ONE fixture, static route metadata, NO writer)*

**DECISION: ONE new OTLP fixture** (the 59/62/63/70 single-OTLP-dir precedent; the `Span.Attrs` seam is shared and P3 proved the Zipkin emit LIVE — no separate Zipkin dir, §2). Cloned from `0114-tracing-custom-tags-metadata` (itself from `0106`/`0087`) but **SIMPLER — NO Lua writer** (the ROUTE source is STATIC route config, unlike phase-70's dynamic Bucket). An H1 listener → HTTPFixedBody backend, HCM OTLP tracing provider → the driver-owned `test/helpers/otlptrace.Server` receiver, `random_sampling: 100`. The route entry (BOTH `envoy.yaml` + `envoy-go.yaml`, byte-identical) carries `metadata: {filter_metadata: {<ns>: {<key>: "<fixed-value>"}}}` (a FIXED cross-side-identical static value — both sides read the identical route config), and **TWO `metadata` custom_tags** proving both P-arms in ONE config:
- **`route_hit`** — `kind:{route:{}}`, `metadata_key{key:<ns>, path:[{key:<key>}]}`, `default_value:"<unused>"` → resolves to the static route value (P2/P4).
- **`route_default`** — `kind:{route:{}}`, `metadata_key{key:<ns>, path:[{key:"<absent-key>"}]}`, `default_value:"<fallback>"` → the path is absent → emits the `default_value` (P5).

**Cross-side EXACT value equality** (as STRONG as 0114's, WITHOUT a writer): both sides read the SAME static route metadata → the SAME resolved string, and the same `default_value`. The driver asserts, on EVERY span BOTH sides, by KEY (P2/S5 — intra-tag order is internal): `route_hit == "<fixed>"` AND `route_default == "<fallback>"` (key AND value), plus the `0087` baseline (span count > 0 — a 0-span pass is structurally impossible). Each an independent `Errorf`. `telemetry.sdk.*` resource + scope UNasserted cross-side (impl-specific, the 0114 precedent). Fixtures **116 → 117**.

**Deliberate breaks** (`-count=1`, full `TestDifferential/0115-...` selectors, confirm WHICH fired, `reference_differential_break_protocol_count1` / `reference_deliberate_break_wrong_assertion`): (a) change the static route value on one side → `route_hit` value-equality fires; (b) drop `route_default`'s default (empty) → the tag omits → the `route_default` presence assertion fires; (c) a wrong-namespace control on `route_hit` → falls to its default (fires the hit assertion). The PLAN substitutes a COMPILING equivalent for any non-compiling break (`reference_plan_break_instructions_dont_compile`) and confirms the ROUTE tag is SERVED (the span carries the static route value, not a vacuous default). The dispatch constraint (`reference_differential_fixture_dispatch_constraint`) holds: ONE dir = ONE cross-side runner branch (both custom_tags resolve on the SAME request).

## 9. Behavior-contract edit map — pinned *(all edits land at the IMPL, atomically with ADR-0293)*

*(Anchor re-derived at the IMPL: the tracing `custom_tags` `metadata` subsection of `docs/envoy-go/BEHAVIOR_CONTRACT.md`, where the `REQUEST` MetadataKind is described CONSUMED and `ROUTE`/`CLUSTER`/`HOST` are "reject loudly".)*

**B1 — flip the `ROUTE` line.** From "`ROUTE`/`CLUSTER`/`HOST` — reject loudly (departure)" to: the `metadata` type's **`ROUTE`** MetadataKind is CONSUMED — the matched route's static config metadata (`route.metadata.filter_metadata[ns]` namespace + `path` walk, over the landed `RouteMetadata()` accessor) is resolved, serialized to a STRING (string→raw; number/bool→scalar; struct/list→compact JSON — the `google.protobuf.Value` rendering, shared with REQUEST), and emitted as a `{tag, value}` span attribute on BOTH exporters; `default_value` emits when the path is absent/unresolvable and the default is non-empty, a present-empty value emits `""`, an unresolvable-with-empty-default omits (the `request_header` rule). The `CLUSTER`/`HOST`/unset-kind-A arms reject loudly (envoy-go-strict DEPARTURE — the reference boots them); the empty-namespace/empty-path/empty-segment/unset-kind-B rejects are PGV-PARITY (both reject). Two of the four MetadataKinds (`REQUEST`@70, `ROUTE`@71) are now consumed.

**B2 — the departures list.** Narrow the "non-`REQUEST` MetadataKinds" departure to "`CLUSTER`/`HOST` MetadataKinds + a fully-unset kind (sub-case A) reject where the reference boots"; keep the (documented boundary) non-string value serialization beyond the P3-pinned common types (number-precision edges + `NullValue`).

## 10. Test plan + task surface *(D-CTMR-SPLIT — DECISION: a SINGLE FLAT ROW)*

**DECISION: SINGLE FLAT ROW** (~9 tasks, comfortably under the ADR-0045 ~15 ceiling). The parse and resolve sit on the SAME landed `internal/tracing` engine; the route metadata is a landed read-only substrate (`RouteMetadata()`); the reject arms live in the same parse function; the seam add (S2) is one small method. No second subsystem can strand a leg. **ADR-0045 escape valve ARMABLE but UNCONSUMED**; ONE ADR (ADR-0293). Sketch (the PLAN decomposes; TDD):
- **T1** — `config.go`: `kindMetadataRoute` const + the ROUTE accept arm (§3.2) replacing the `:245-246` reject; CLUSTER/HOST/unset rejects UNCHANGED. `config_test.go`: accept ROUTE (namespace+path built) + reject CLUSTER/HOST/unset-A/unset-B/empty-namespace/empty-path/empty-segment (distinct substrings).
- **T2** — `resolve.go`: the `routeMetaLookup` 4th param + the `kindMetadataRoute` arm descending the FULL `MetaPath` (§3.3/§3.4). `resolve_test.go`: the ROUTE path-walk (single + multi-segment), the P4 serialization table (reused; string/number/bool/struct/list, `json.Compact`-compared), the P5 default/omit/present-empty matrix, the P6 unresolvable-segment→default, nil-`routeMetaLookup` tolerance, nil-`RouteMetadata` tolerance.
- **T3** — `internal/filter/http/chain.go`: add `func (c *FilterChain) RouteMetaLookup(ns string) (*structpb.Value, bool)` (S2) + the `structpb` import + a unit test (present ns → wrapped struct; absent ns → `(nil,false)`; nil `RouteMetadata` → `(nil,false)`).
- **T4** — `accesslog_emit.go`: thread `routeMetaLookup` to the three emit methods; update ALL 18 caller sites (§11 — `nil` at the 3 no-chain 404 sites, `chain.RouteMetaLookup` elsewhere). `span_emit_test.go`/`accesslog_emit_test.go` callers updated (they pass `nil` — no route-metadata tags configured — proving byte-stable no-route-metadata behavior).
- **T5** — `FuzzHCMConfigParse` seed repurpose (§7, S4) + dispatch-verification.
- **T6** — the `0115` fixture (config + driver + the static route metadata + the two custom_tags, NO writer) + deliberate breaks (§8).
- **T7** — BEHAVIOR_CONTRACT B1-B2 (§9).
- **T8** — six-gate + full 117-dir differential + `-race` (the `internal/tracing` + `internal/filter/hcm` + `internal/filter/http` packages) + the cycle guard (`go list -deps ./internal/tracing` — no filter-pkg edge) + `go mod tidy -diff` empty + envelope audit (`internal/xds`/`internal/tls`/`internal/boot`/`internal/listener`/`internal/bootstrap` BYTE-UNTOUCHED; production = `internal/tracing/{config.go,resolve.go}` + `internal/filter/http/chain.go` + `internal/filter/hcm/{accesslog_emit.go,connection.go,h2dispatch.go,h3dispatch.go}`).
- **T9** — ADR-0293 completed IN PLACE (§Decision/§Consequences) + ROADMAP row-71-done + the deferred-sentence narrow (roll the `ROUTE` MetadataKind OUT — §12) + STATE/router (stage-close).

Coverage properties, each an independent `Errorf`: the ROUTE accept + retained rejects; the path-walk (single/multi/unresolvable); the P4 serialization table; the P5 default/omit/present-empty matrix; the nil-`routeMetaLookup` + nil-`RouteMetadata` tolerance; the `*FilterChain.RouteMetaLookup()` accessor; the cross-side EXACT `0115` assertions; byte-stable-when-no-route-metadata-tags.

## 11. Edit-site roster *(RE-DERIVED at `fbc86063`)*

**Production — `internal/tracing/config.go`:** `parseCustomTags` `[REPLACE the :245-246 ROUTE reject with the accept arm]` · `customTagKind` `:53-58` `[ADD kindMetadataRoute]`. NO new import. **`internal/tracing/resolve.go`:** `ResolveCustomTags` `:26` `[ADD routeMetaLookup 4th param + kindMetadataRoute arm descending FULL MetaPath]`. NO new import. **`internal/filter/http/chain.go`:** `[ADD func (c *FilterChain) RouteMetaLookup — beside RouteMetadata :1156]` + `[ADD structpb import]`. **`internal/filter/hcm/accesslog_emit.go`:** `emitAccessLog`/`emitAccessLogH2`/`emitAccessLogH3` `:27`/`:87`/`:149` `[ADD routeMetaLookup param, pass to ResolveCustomTags :57/:118/:179]`. NO new import (structpb already present). **`internal/filter/hcm/connection.go`:** the 5 `emitAccessLog` callers `[:330 nil; :464/:597/:699/:777 chain.RouteMetaLookup]`. **`internal/filter/hcm/h2dispatch.go`:** the 6 `emitAccessLogH2` callers `[:313 nil; :396/:530/:577/:584/:613 chain.RouteMetaLookup]`. **`internal/filter/hcm/h3dispatch.go`:** the 7 `emitAccessLogH3` callers `[:130 nil; :210/:280/:341/:367/:373/:395 chain.RouteMetaLookup]`. *(The exact per-caller `chain` variable name is re-derived at the IMPL; the 3 no-chain 404 sites pass `nil` — provably never dereferenced, §3.5.)*

**Test/harness:** `config_test.go` / `resolve_test.go` `[ADD]` · `internal/filter/http/chain_test.go` `[ADD RouteMetaLookup test]` · `accesslog_emit_test.go` / `span_emit_test.go` `[UPDATE caller signatures — pass nil]` · `FuzzHCMConfigParse` seed (`fuzz_test.go:94-108`) `[REPURPOSE, dispatch-verified]` · `test/fixtures/0115-tracing-custom-tags-metadata-route/` (config + driver, NO Lua) `[ADD]`. **REUSED untouched:** `test/helpers/otlptrace` · `internal/dynamicmetadata` (ROUTE reads route config, not the Bucket) · `internal/filter/http/ratelimit`. **UNTOUCHED:** `internal/xds`, `internal/tls`, `internal/boot`, `internal/listener`, `internal/bootstrap`.

**Docs:** BEHAVIOR_CONTRACT per §9 `[IMPL]` · DECISIONS ADR-0293 §Context **AT THIS COMMIT** (§14), completion at the IMPL · ROADMAP/STATE/next-prompt `[STAGE-CLOSE]`.

## 12. Sentinel maintenance — a SENTENCE-NARROWING row, narrowed AT THE IMPL (the phase-57 precedent)

The `ROUTE` MetadataKind IS named in the live Observability `candidates:` sentence (as one of the non-`REQUEST` MetadataKinds deferred at phase 70). **Per the verified phase-57 graphite precedent, NO sentence is narrowed at ANY pre-IMPL stage.** So the sentence narrows the `ROUTE` MetadataKind OUT at the phase-71 IMPL row-done edit, NOT before; afterward the sentence STAYS a live `candidates:` match (the `CLUSTER`/`HOST` MetadataKinds + `spawn_upstream_span`/`http_service`/force-trace + the `ssl` family remain — `reference_sentinel_deferred_sentence_live_vs_historical`; keep EXACTLY ONE live Observability match, HTTP/3 + xDS untouched, three total). Check (1) keeps printing `NOT DONE: row 71` until the IMPL six-gate; checks (2)+(3) unchanged (`grep -cE 'remaining deferred \(not-yet-chartered\) candidates:'` → 3; `NEVER OPENED: gRPC/Runtime/WASM`) ⇒ the sentinel does NOT fire. **Nothing is owed at this SPEC.**

## 13. Deferred items *(the BRAINSTORM §8 roster carried MINUS this row's pickup)*

Carried unchanged: the `CLUSTER`/`HOST` MetadataKinds (rejected loudly this row — `CLUSTER` the `upstream_cluster` framework gap, `HOST` the reduced `envoy.lb` subset) · `spawn_upstream_span` · `http_service` · force-trace · the `ssl` stat family (ADR-0286 C3) · the 4 EMPTY built-in span attributes · a SIXTH stats sink (hystrix/wasm) · the RBAC `require=false` fixture rider · DataSource `environment_variable` (D-ENV-HARNESS-gated) · `watched_directory`/SDS rotation · the xDS + HTTP/3 families · the never-opened gRPC/Runtime/WASM families. **REMOVED: tracing `custom_tags` `metadata` (`ROUTE` kind) — THIS pickup.**

**Newly deferred at THIS row:** nothing new (the `CLUSTER`/`HOST` MetadataKinds were already deferred at phase 70; this row narrows the triple to the pair).

**Memory update owed at the IMPL:** the `*FilterChain.RouteMetaLookup()` seam — the ROUTE analogue of phase-70's `DynamicMetadata()` accessor; a consumer at the HCM emit layer wraps `RouteMetadata().GetFilterMetadata()[ns]` via `structpb.NewStructValue` to feed the shared `descend` walk (extends `reference_filterchain_dynamicmetadata_accessor`). And: the ROUTE resolve descends the FULL `MetaPath` (the REQUEST `[1:]` slice is a Bucket-pre-keying artifact, not a general metadata rule).

## 14. ADR continuity — the ADR-0293 §Context DRAFT (anchored here; full entry at the phase-71 IMPL)

*(The canonical entry is in `docs/envoy-go/DECISIONS.md`, appended at this SPEC commit — the tail flips ADR-0292 → ADR-0293 there; the IMPL completes the entry IN PLACE. This blockquote reproduces its prose; the STATUS-banner + `### Context` heading wrapper follows the DECISIONS file convention.)*

> **ADR-0293 — tracing `custom_tags` `metadata` type, `ROUTE` MetadataKind only (the SEVENTEENTH Observability-family row + the FIFTH tracing `custom_tags` row + the SECOND `metadata` MetadataKind after `REQUEST`/70/ADR-0292; lifts the `custom_tags metadata route kind unsupported` reject at `internal/tracing/config.go:245-246` and resolves a `ROUTE`-kind route-config metadata value out of the matched route's STATIC `metadata.filter_metadata[ns]` — over the LANDED `FilterChain.RouteMetadata() *corev3.Metadata` accessor [phase 24.2 / ADR-0165] — walk the `MetadataKey.path`, serialize the resolved `structpb.Value` to a STRING [reusing the phase-70 `descend`/`structpbValueToString` VERBATIM], emit as a `{tag, value}` span attribute via the shared `ResolveCustomTags`→`Span.Attrs []KV` seam on BOTH the OTLP and Zipkin exporters; the `CLUSTER`/`HOST`/unset-kind arms reject loudly with distinct substrings [an envoy-go-strict DEPARTURE — the reference BOOTS them]; a SINGLE FLAT ROW — FLIPS ROW 71 `done`; the Observability family STAYS OPEN; +0 stats/packages/modules/fuzzers/BackendKinds, new exports confined to `internal/tracing` + ONE new `func (c *FilterChain) RouteMetaLookup()` accessor on `internal/filter/http`; the `internal/xds` zero-new-symbol discipline UNTOUCHED).**
>
> **§Context (drafted at the phase-71 SPEC per ADR-0044).** Phase 46 built the HCM-native tracing engine (the per-request ingress SERVER `Span` with built-in STRING attributes over BOTH the OTLP and Zipkin transports through the shared `Span.Attrs []KV` seam, ADR-0260/0261). Phases 59/62/63 added the first three `custom_tags` SOURCE types (`literal` ADR-0277, `request_header` ADR-0283, `environment` ADR-0284). Phase 70 (ADR-0292) added the FOURTH and LAST `CustomTag` SOURCE type — the `metadata` type — scoped to the `REQUEST` MetadataKind (the per-request dynamic-metadata `Bucket`), leaving `ROUTE`/`CLUSTER`/`HOST`/unset-kind as loud strict-rejects. Phase 71 is the FIFTH tracing `custom_tags` row and the SECOND `metadata` MetadataKind. Six discriminating live-probe arms (`envoyproxy/envoy:contrib-v1.37.2`, fresh container per arm, a STATIC route-config metadata source served — NO writer needed, unlike phase 70's Lua REQUEST arm — a standalone host OTLP-gRPC receiver + a raw-TCP Zipkin dumper, decode-ran asserted) pinned: the reference BOOTS a `ROUTE` (and `CLUSTER`, `HOST`) metadata custom tag while PGV boot-rejecting an empty `metadata_key.key`/`path`/segment — so envoy-go's `ROUTE` reject is a genuine departure to lift and the `CLUSTER`/`HOST` rejects stay real departures, while the empty-structural rejects are parity (P1); the reference emits the `ROUTE` metadata custom tag as `{key = the LITERAL `tag` name, value = the serialized static-route metadata value as a STRING}`, APPENDED AFTER the built-ins on the OTLP span (in an internal iteration order, so assertions are key-based) and in the Zipkin v2 JSON `tags` map (P2/P3); the `structpb.Value`→string rendering is string→raw, number→canonical decimal, bool→`true`/`false`, struct/list→compact JSON — IDENTICAL to the phase-70 REQUEST table (P4); the `default_value` emits when the path is absent/unresolvable and the default is non-empty, a PRESENT-but-EMPTY value emits `""` and does NOT fall to the default, an unresolvable-with-empty-default OMITS — the `request_header` rule (P5); a multi-segment `MetadataKey.path` walks nested `StructValue` fields and an unresolvable segment falls to the default (P6). The landed substrate: `FilterChain.RouteMetadata()` (the `ROUTE`-kind source, seeded at the three dispatch sites), the phase-70 `descend`/`structpbValueToString` path-walk + serializer (reused verbatim), and the `Span.Attrs`/`ResolveCustomTags`/both-exporter seams.
>
> **The design (§Decision anticipated).** The `parseCustomTags` ROUTE arm (replacing the `:245-246` reject) accepts `kind==ROUTE` (validating a non-empty namespace + non-empty path + non-empty segments — PGV parity) into a `CustomTagSpec` reusing `MetaNamespace`/`MetaPath`/`DefaultValue`/`HasDefault` (the `request_header` idiom) + a new `kindMetadataRoute` const, and leaves the `CLUSTER`/`HOST`/unset-kind rejects loud with distinct substrings (envoy-go-strict). `ResolveCustomTags` grows a nil-tolerant FOURTH param `routeMetaLookup func(ns)(*structpb.Value,bool)` (distinct from the REQUEST two-arg `metaLookup`) + a `kindMetadataRoute` arm that descends the FULL `MetaPath` (not `[1:]` — the REQUEST slice is a Bucket-pre-keying artifact) via the reused `descend` and serializes via the reused `structpbValueToString`. The per-request route metadata reaches the emit sites via a NEW `func (c *FilterChain) RouteMetaLookup(ns string) (*structpb.Value, bool)` method (wrapping `RouteMetadata().GetFilterMetadata()[ns]` via `structpb.NewStructValue`, centralizing the `structpb` import to `chain.go`) threaded as `chain.RouteMetaLookup` at the 15 span-capable `accesslog_emit.go` callers (H1/H2/H3) and `nil` at the 3 no-chain 404 sites (provably never reached — the span block is `traceDecision`-gated). Envelopes: +0 packages, +0 go.mod modules (`corev3`/`structpb`/`type/metadata/v3`/`protojson` resolve at existing directs), +0 stats (a span attribute registers none), +0 fuzzers (a repurposed `FuzzHCMConfigParse` seed — a valid ROUTE accept + a CLUSTER reject — dispatch-verified to reach the metadata arm), +0 BackendKinds (the `otlptrace` receiver is driver-owned, `0115` reuses `HTTPFixedBody`, NO Lua writer); functional production edits in `internal/tracing/{config.go,resolve.go}` + `internal/filter/http/chain.go` + `internal/filter/hcm/{accesslog_emit.go,connection.go,h2dispatch.go,h3dispatch.go}`; new exports confined to `internal/tracing` + the one `*FilterChain.RouteMetaLookup()` accessor — the `internal/xds` discipline UNTOUCHED; `internal/xds`/`internal/tls`/`internal/boot`/`internal/listener`/`internal/bootstrap` byte-untouched. Proven by ONE new OTLP differential `0115-tracing-custom-tags-metadata-route` (a static route-metadata block + two `ROUTE` metadata custom_tags — one resolving to the static value, one falling to `default_value` — asserted cross-side EXACT key+value, NO writer needed since both sides read identical static route config; fixtures 116 → 117) + the config/resolve unit matrices (the P4 serialization table + the P5 default/omit matrix + the ROUTE path-walk). A SINGLE FLAT ROW (the phase-59/62/63/70 `custom_tags`-source precedent — parse + resolve on one landed engine; the ADR-0045 valve is armable but unconsumed). The Observability family STAYS OPEN (the `CLUSTER`/`HOST` MetadataKinds + `spawn_upstream_span`/`http_service`/force-trace + the `ssl` stat family remain).
>
> *(§Decision + §Consequences land at the phase-71 IMPL.)*

## 15. Exit — counts + expectations at SPEC-DONE

**Counts at this SPEC commit (docs-only apart from the DECISIONS append):** fixtures **116** · fuzzers **55** · stat surface **1201** · BackendKind **38** · go.mod modules **2** · **DECISIONS tail flips ADR-0292 → ADR-0293 AT THIS COMMIT** (the §Context draft append IS the tail flip; next-free becomes ADR-0294; the router's "re-derive, never trust" tail check must expect `## ADR-0293`).

**Anticipated at the phase-71 IMPL:** fixtures **116 → 117** (`0115-tracing-custom-tags-metadata-route`) · stat surface **1201 (+0)** · fuzzers **55 (+0)** · BackendKind **38 (+0)** · go.mod **2 (+0)** · ZERO new packages · new exports confined to `internal/tracing` + `func (c *FilterChain) RouteMetaLookup()` on `internal/filter/http` — the `internal/xds` discipline UNTOUCHED · functional production edits in `internal/tracing/{config.go,resolve.go}` + `internal/filter/http/chain.go` + `internal/filter/hcm/{accesslog_emit.go,connection.go,h2dispatch.go,h3dispatch.go}` · `internal/xds`/`internal/tls`/`internal/boot`/`internal/listener`/`internal/bootstrap` BYTE-UNTOUCHED · ADR-0293 completed IN PLACE · ROADMAP narrows the `ROUTE` MetadataKind OUT at the row-done edit.

**Sentinel:** re-verified expectation — check (1) keeps printing `NOT DONE: row 71`; checks (2)+(3) unchanged; **no deferred-sentence edit at any pre-IMPL stage** (§12); no `stop` file.

**Probe hygiene:** SIX fresh-container probe arms (P1..P6) ran THIS SPEC (the D-CTMR docket was genuinely owed — the BRAINSTORM ran none); all containers removed + the receivers stopped; the worktree contains only this SPEC + the DECISIONS append at commit time.

**Next → the phase-71 PLAN.** Its inheritance: the SINGLE FLAT ROW ~9-task spine (§10); the ROUTE accept arm cloning REQUEST (§3.2); the `kindMetadataRoute` resolve arm reusing `descend`/`structpbValueToString` and descending the FULL `MetaPath` (§3.3, S1); the `request_header` default rule (§3.4); the NEW `*FilterChain.RouteMetaLookup()` accessor + the `routeMetaLookup` thread at all 18 callers with `nil` at the 3 no-chain sites (§3.5, S2); the ONE OTLP fixture with two static-route custom_tags asserted cross-side EXACT, NO writer (§8); the fuzz-seed repurpose (§7, S4); the pinned §9 BC edit map. **RE-DERIVE this document; do not execute it.** Where it cites, go look; where it claims control flow, walk the call graph; default to REFUTED.

## 16. Adversarial-pass record

TWO independent verifiers ran against the draft in PRIVATE scratch before landing (`reference_parallel_subagents_private_scratch`).

**V1 — code-claims re-derivation + a DECISIVE by-execution probe of the load-bearing S1 seam.** V1 independently re-derived every load-bearing anchor at `fbc86063` (all HELD exact: the ROUTE reject `:245-246` + REQUEST accept `:228-244` + the `k := md.GetKind()` bind `:224` + the unset-A `:226-227`/unset-B `:251-252` split; the `customTagKind` block `:53-58` + `kindMetadataRoute` slot; `ResolveCustomTags` `:26` 3-param shape; `descend` `:100-114` + `structpbValueToString` `:123-140` verbatim-reusable; `RouteMetadata()` `:1156` + `DynamicMetadata()` `:1022` + `SetRouteMetadata` `:1150` wired at the three dispatch sites `connection.go:433`/`h2dispatch.go:375`/`h3dispatch.go:195`; the 18-caller roster 5+6+7 with the 3 nil sites `:330`/`:313`/`:130`; the collision greps `kindMetadataRoute`/`routeMetaLookup`/`RouteMetaLookup` → 0; the +0-module envelope with `protobuf v1.36.x` a DIRECT require) and **RAN a standalone Go program proving the S1 full-path-descent equivalence:** `descend(structpb.NewStructValue(nsStruct), ["outer","inner"])` yields `deep` == REQUEST's `descend(metaLookup(ns,"outer"), ["inner"])` yields `deep` (EQUAL); a single-segment path resolves; an unresolvable second (and first) segment returns `false` → default/omit; `structpb.NewStructValue` has signature `func(*structpb.Struct) *structpb.Value`. V1 also confirmed `GetFilterMetadata()` is nil-receiver-safe (the `RouteMetaLookup` body is panic-free on nil `RouteMetadata`), `structpb` is ABSENT from `chain.go` (the S2 import add is real), and the `withMetaTags` `meta_bad` seed is a MetadataKey-less ROUTE tag (the S4 repurpose is real). **ZERO SEVERE, ZERO MODERATE; ONE MINOR** — §5 pinned a version-fragile vendored `struct.pb.go` line for `NewStructValue` (folded: the line cite dropped, the version-stable signature retained). Verdict: **BUILDABLE as written.**

**V2 — process/consistency/SPEC-coverage.** All ten stage-close mechanics PASS: row 71 stays `in-progress` (ROADMAP untouched; the diff is exactly SPEC.md + DECISIONS.md + STATE.md + next-prompt.txt); the DECISIONS tail flip ADR-0292 → ADR-0293 is §Context-ONLY with a `STATUS: PROPOSED` banner (`grep -c '^## ADR-0293'` → 1, `## ADR-0294` → 0); no deferred sentence narrowed (3 `candidates:` sentences, the ROUTE MetadataKind still named); the sentinel does NOT fire (`NOT DONE: row 71` / 3 / `NEVER OPENED: gRPC/Runtime/WASM`; no `stop`); STATE §Current is a singleton with `lifecycle-state: 2` + `next-free ADR: ADR-0294` + EXACTLY 5 lineage bullets (newest phase-71 SPEC, oldest phase-70 SPEC, the phase-70 BRAINSTORM dropped); **the router rolled to the phase-71 PLAN, NOT the IMPL** (the four-stage lifecycle — phase 70 had distinct PLAN and IMPL commits — the CRITICAL check); counts consistent across all four docs (116 now / 117 anticipated, never stated as current); the S1–S5 sharpenings consistently reflected; no cited-but-unwritten obligation (the phase-69 `TestNoNewStat` defect class avoided — §7 adds no stat guard); format faithful to the phase-70 SPEC. **ZERO SEVERE, ZERO MODERATE, ZERO MINOR.**

The design direction — a `ROUTE`-kind metadata custom tag on the landed static-route-config metadata (`FilterChain.RouteMetadata()`), reusing the phase-70 `descend`/`structpbValueToString` verbatim, a NEW `routeMetaLookup` 4th param + a `RouteMetaLookup` method, single flat row — is unchanged from the BRAINSTORM.
