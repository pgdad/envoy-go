# Phase 35 — `load-balancer-random`

**Status:** SPEC authored (lifecycle-state 1 → 2 at this commit; the D-R1..D-R6 empirical pins EXECUTED live against the contrib reference Envoy — a RANDOM-cluster held-conn distribution probe on a docker bridge network + a 5-variant `--mode validate` matrix + a full `/stats` name-set diff RANDOM-vs-ROUND_ROBIN — + go-control-plane `/envoy` v1.32.4 bindings + upstream Envoy v1.37.2 source; AMEND-R1..R6 recorded; the ADR-0234 §Context DRAFT anchored in SPEC §13; ready for the phase-35 PLAN per `superpowers:writing-plans`). PLAN / PROGRESS artifacts still pending; land when this phase enters subsequent lifecycle states.

**What this phase is.** Phase 35 is the **SECOND Load-balancing-family row** — the family STAYS OPEN (it opened at phase 34; a flat top-level row per ADR-0106; ROADMAP row 35, depends-on 34). It lands `Cluster.LbPolicy RANDOM` (`envoy.config.cluster.v3` — enum value 3, already in the pinned core `/envoy v1.32.4` module; **ZERO new go.mod dep**, D-R1) — the project's **third LB policy** (after `roundRobin` [02] and `leastRequest` [34]). Unlike phase 34, phase 35 builds NO framework seam: it REUSES the phase-34 **ADR-0232 LB acquire/release seam** UNCHANGED — `randomLB` implements the existing unexported `loadBalancer` interface and returns the shared `noopRelease` (the seam's FIRST reuse, validating its "the non-keyed RANDOM drops in at zero cost" §Consequences claim — the family analogue of thrift-33's clean reuse of the §9 ADR-0230 seam). 6 family candidates remain after 35 {ring_hash, maglev, subset LB, locality-weighted LB, priority load balancing, panic thresholds}.

**The envelope (BRAINSTORM Q1, LEAN).** `RANDOM` accepted by `cluster.Manager` (ONE new switch case — bare construction, NO config parse; the lb_policy reject TEXT extends `… ROUND_ROBIN, LEAST_REQUEST` → `…, RANDOM` — the ONE deliberate byte-stable-reject change; blast radius pinned at D-R2: three sites, NO fixture pins it) + a STATELESS `randomLB` picking a uniformly-random endpoint per `Pick()` (`rng() % n`, one draw, INSENSITIVE to load — the upstream v1.37.2 `RandomLoadBalancer::peekOrChoose` mirror, D-R5) that REUSES the ADR-0232 seam UNCHANGED (returns `noopRelease`; no `Pick`-signature change, no consumer touch — D-R6). The RNG is the EXISTING package-private `newPCGRNG` helper (the `leastRequest` posture, called directly with no extraction — D-R5). DEFERRED: the `Cluster.load_balancing_policy` extension point (RANDOM's locality-weighting config), locality/panic/health-filtering, all other policies (RING_HASH/MAGLEV rejected with the NEW byte-stable text — the reference accepts them, a recorded departure; the hash policies additionally need the ADR-0232 `Pick(hashInput)` widening), any seam change.

**The six empirical pins (this SPEC session, D-R1..D-R6 executed live — SPEC §1.1):** **AMEND-R1** the v1.32.4 surface re-pinned (enum 3; NO `Cluster.RandomLbConfig` message → no config-parse arm, no PGV gate; the `lb_config` oneof has no `random` member → a stray member under RANDOM is silently ignored; ZERO new dep); **AMEND-R2** the 5-variant validate matrix (RANDOM/RING_HASH/MAGLEV accept on the reference; the mismatched-oneof strays accept silently) + the 3-site blast radius + NO boot-reject dir; **AMEND-R3** RANDOM stays UNIFORM under held-conn load CONFIRMED LIVE (3 bursts 16–25 around mean 21.3; leading host rotates randomly — the contrapositive of `0059`'s skew); **AMEND-R4** ZERO stat-name delta (surface STAYS 1116 — empty name-set diff 455==455); the StatsAsserter set pinned (cx/membership cross-equal; `upstream_rq_total` per-side); **AMEND-R5** the RANDOM source semantics (one uniform draw, NO active-count consult; healthy-set filtering upstream → all-hosts boundary; `newPCGRNG` already shared-ready; empty-set parity); **AMEND-R6** the seam REUSES unchanged (~45–60 prod LoC → single flat row, NO escape valve; single ADR-0234; ADR-0024 unamended).

**The differential proof (family-level proof-shape novelties, now ESTABLISHED).** ONE new fixture `0060-lb-random` (cross-side; chain `[tcp_proxy]` both sides; the 3 `TCPEcho` backends REUSED — **NO new BackendKind**, tail stays 33): the REUSED `0059` hold-4 + burst-60 stimulus with the assertion INVERTED — a **band-based per-side `AssertDistribution`** asserting RANDOM STAYS uniform (conservation + per-host floor `>= 12` + per-host ceiling `<= 32` — the SECOND band-based use; per-side because RANDOM is RNG-driven on both sides, NEVER cross-side-exact) DESPITE the held-conn load (the contrapositive of least_request) + a cross-side `StatsAsserter` prong; **NO new fuzzer** (no wire decode — fuzzers stay 42); every band leg proven live via deliberate-breaks with `-count=1` (consult-the-counters / pin-to-endpoints[0] / dropped-Inc).

**Masters (read first, in order):**

1. [`./BRAINSTORM.md`](./BRAINSTORM.md) — **the phase-35 charter.** The Q0/Q1/Q-seam/Q-split decisions; the v1.32.4 enum-3 + no-config-message verification (§2.1); the seam-reuse rationale (§1.7 / §3.1); the anti-skew differential design (§2.5/§6); the anticipated ADR (§7); the §10 D-R1..D-R6 pins this SPEC executed.
2. [`./SPEC.md`](./SPEC.md) — **the authoritative phase SPEC.** The empirical-finding-driven scope (§1.1, AMEND-R1..R6); the single-row split disposition (§3.0); the policy + seam-reuse design (§3); the proto roster + the oneof (§5); the reject matrix (§6); the zero-delta stat surface (§7); the `0060` anti-skew band-arm design (§8); the ~4–7-task PLAN spine (§10); the D-R1..D-R6 empirical-pin block (§11); the ADR-0234 §Context DRAFT (§13).
3. [`../../ENVOY_TARGET.md`](../../ENVOY_TARGET.md) — the current pin (`envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0f…`, ADR-0227).
4. `docs/envoy-go/DECISIONS.md` — ADR-0232 (the LB acquire/release seam — REUSED unchanged; read the §Consequences "the non-keyed RANDOM drops in at zero cost" claim random consummates) + ADR-0233 (the least_request policy — the RNG posture random reuses) + ADR-0024 (the per-cluster LB-state scope — UNAMENDED) + ADR-0106/0045/0044/0052/0080/0060. (ADR-0234 §Context is a DRAFT in SPEC §13 — the full entry lands at the 35 IMPL per ADR-0044.)
5. `internal/cluster/{loadbalancer,leastrequest,manager,cluster}.go` (the reused seam + `noopRelease` + `newPCGRNG` + the lb_policy switch + the reject text) + `internal/cluster/manager_test.go:320` (the `TestManager_Error_UnsupportedLBPolicy` retarget) + `test/differential/fixture/fixture.go` (the `DistributionAsserter`) + `test/fixtures/0059-lb-least-request/` (the stimulus `0060` transposes — the assertion INVERTED).

**Scope at phase 35** (per the SPEC + ROADMAP row 35):

- (a) The **ADR-0234 policy** — the `randomLB` type (`random.go`; stateless uniform pick per AMEND-R5; the REUSED `newPCGRNG` RNG; the seam-reuse `noopRelease`); manager acceptance (the bare `case clusterv3.Cluster_RANDOM`); the NEW lb-policy reject text + the `manager_test.go` retarget.
- (b) The **`0060-lb-random`** fixture (the anti-skew band arm + the stats prong) — fixtures 61 → 62.
- (c) **The ADR-0234 body** + the BEHAVIOR_CONTRACT 35 bundle (stat surface UNCHANGED at 1116) + STATE/ROADMAP advance + the row-35 `in-progress → done` flip at the IMPL six-gate (a flat family row — NO parent rollup).
- ZERO seam change, ZERO new package, ZERO new go.mod dep, ZERO config-parse arm, ZERO PGV gate.

**Counts (at IMPL):** fixtures 61 → 62; fuzzers 42 → 42 (deliberate); stat surface 1116 → 1116 (deliberate — the second zero-delta phase); BackendKind tail 33 → 33 (deliberate); DECISIONS.md tail **ADR-0233 STAYS at THIS SPEC commit** (the ADR-0234 §Context is a DRAFT in SPEC §13; the full entry + body land at the IMPL per ADR-0044; next-free ADR-0234).

**Predecessor:** Phase 34 done (`least_request`; the Load-balancing family OPENED; the ADR-0232 seam built — the asset phase 35 reuses).

**Successor:** the next Load-balancing-family row (TBD via brainstorm; 6 candidates remain after 35 — the hash policies RING_HASH/MAGLEV are the seam-EXTENDING ones; the rest are zero-churn or modifier rows).
