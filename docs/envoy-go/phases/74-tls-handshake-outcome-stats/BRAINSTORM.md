# Phase 74 Brainstorm — downstream TLS handshake-outcome stats (`ssl.handshake` / `ssl.fail_verify_error` / `ssl.fail_verify_no_cert`) (the TWENTIETH Observability-family row *(chain ordinal — §2.1 records the inherited off-by-one)*; the FIRST row to LEAVE the tracing `custom_tags` lineage since phase 59, and the FIRST row in a long streak to BREAK the +0-stat envelope; registers THREE fixed-name counters at listener scope under the EXISTING `"listener." + normalizeAddr(rt.addr) + "."` prefix (`internal/listener/manager.go:352`) and classifies the handshake error ALREADY BOUND at `manager.go:1178`; **it does NOT land `ssl.ciphers.*` / `ssl.versions.*`** — those SPLIT OFF and stay deferred, and the split is itself a finding; +3 stats / +0 packages / +0 modules / +0 fuzzers / **+0 fixtures** — `0111` is EXTENDED)

**Status: BRAINSTORM. Docs-only. ZERO production `.go`. Row 74 registered `in-progress` at this commit per the ROADMAP §Schema invariant.**

---

## 1. Mission and scope confirmation (74 — the row that RETIRES a 9-row-old ADR-adjacent adjective)

### 1.1 What phase 74 delivers as a self-contained whole (three fixed-name downstream handshake-outcome counters)

Phase 65 (ADR-0286) live-probed the reference at `envoyproxy/envoy:contrib-v1.37.2` and observed a downstream TLS handshake-outcome stat family that envoy-go does not emit AT ALL. That observation was recorded as a **NAMED COVERAGE BOUNDARY** and left unclosed for nine rows. Phase 74 closes the cheap three-quarters of it:

- **`ssl.handshake`** — successful downstream TLS handshakes.
- **`ssl.fail_verify_error`** — a client certificate WAS presented and verification FAILED.
- **`ssl.fail_verify_no_cert`** — NO client certificate was presented where one was required.

All three register at **listener scope**, i.e. under the prefix the listener already owns: `"listener." + normalizeAddr(rt.addr) + "."` — RE-DERIVED at tip as `internal/listener/manager.go:352`, inside `registerListenerMetrics` (`:351-355`), which today registers exactly two names (`downstream_cx_total` `:353`, `downstream_cx_active` `:354`).

The classification point is a handshake error value that **already exists in a bound variable**. RE-DERIVED at tip, `internal/listener/manager.go:1176-1181`:

```go
if selected.tlsCfg != nil {
	tlsConn := stdtls.Server(pkConn, selected.tlsCfg)
	if err := tlsConn.HandshakeContext(ctx); err != nil {
		log.Printf("listener %q: handshake: %v", rt.name, err)
		_ = pkConn.Close()
		return
	}
```

`err` is bound at `:1178`. The success arm is the fall-through at `:1183` (`dispatchConn = tlsConn`). The receiver `rt` is the same `*listenerRuntime` that already carries `downstreamCxTotal *stats.Counter` and `downstreamCxActive *stats.Gauge` (`manager.go:175-176`). **Nothing has to travel anywhere** — three more fields on `rt`, three more lines in `registerListenerMetrics`, one classifier call at the error site and one `Inc()` on the success fall-through.

This is a complete, useful, differential-provable capability: the FIRST observability of client-certificate verification outcomes in envoy-go, on a proxy that has shipped mandatory mTLS since phase 16.

### 1.2 What phase 74 does NOT deliver (forward to §8)

- **`ssl.ciphers.*` / `ssl.versions.*` / `ssl.curves.*` — the family SPLITS and the second half stays deferred.** §2.5 records the two blockers (a charset guard that PANICS, and an IANA-vs-OpenSSL name mismatch that makes a cross-side EXACT assertion infeasible). **⇒ ADR-0286 C3's boundary as written CONFLATES a cheap half with a blocked half; splitting it is part of this row's contribution.**
- The `upstream_cluster` span tag — **the RUNNER-UP, and this session's richest rejected entry.** §2.1, §8, §11.
- The pick-time `picked`-propagation row (the BEHAVIOR_CONTRACT `:690` departure). §2.1, §8.
- `access_log[].filter` · `hcm.access_log_options` · the `stdout`/`stderr` loggers · `stats_flush_on_admin` — all re-costed this session and all still deferred, several with materially better evidence than they had. §2.1, §8.
- `hcm.merge_slashes` · route-level header manipulation · `RouteAction.prefix_rewrite`/`host_rewrite_*` · HCM `server_name`/`via` — NEWLY SURFACED this session, none chartered. §2.1, §8.
- All tracing remainders (`spawn_upstream_span` / OTel `http_service` / `sampler` / `resource_detectors` / `verbose` / force-trace). §8.

### 1.3 Phase-done as the TWENTIETH Observability-family row landing (family STAYS OPEN)

Row 74 is the twentieth Observability-family row *(the CHAIN ordinal — row 69 carries no ordinal word, so the chain runs one short of the mechanical count of 21; kept as-is deliberately, §2.1)*. It is the **FIRST Observability row since phase 59 that is not a tracing `custom_tags` capability** — phase 73's own §1.3 warned that the `metadata`-MetadataKind line of cheap follow-ons was RETIRED and that the next roller would have to leave the lineage. It did.

After it lands the family STAYS OPEN: the `ssl` ciphers/versions half, `spawn_upstream_span`, `http_service`, `sampler`, `resource_detectors`, `verbose`, force-trace, `access_log[].filter`, `access_log_options`, the `stdout`/`stderr` loggers, `stats_flush_on_admin`, the pick-time row and the `upstream_cluster` span tag all remain deferred candidates.

### 1.4 ADR-0045 split readiness — anticipated a SINGLE FLAT ROW (escape-valve armable)

Anticipated a SINGLE FLAT ROW of **~8 tasks** (§2.7 sketches the spine; the PLAN decomposes). The one thing that could force a split is D-TLSHS-REGSCOPE (§2.2) turning up a registration posture that requires touching the QUIC listen path, which is a structurally different runtime (`internal/listener/quic.go`, §2.6). Under the anticipated posture it stays flat.

### 1.5 Seed-stub alignment + package placement — ALL edits in EXISTING files, ZERO new packages

Every production edit lands in ONE file: `internal/listener/manager.go` (three fields on `listenerRuntime`, three registrations in `registerListenerMetrics`, a `classifyHandshakeErr` helper + its two call points in `serveConnection`). Plus `internal/listener/manager_test.go`, the EXISTING fixture `test/fixtures/0111-tls-cvc-empty-dynamic-fallback/driver/driver.go`, and `docs/`. **NO new package. NO new fixture directory. NO new YAML.**

**Blast-radius note:** `internal/listener` has not been a stat-surface-changing package since phase 06.1. The envelope audit for this row is a **SHAPE pin, not a BYTE-UNTOUCHED pin**, for `manager.go` alone; every other production package is anticipated BYTE-UNTOUCHED (§10).

### 1.6 No prebrainstorm-notes branch

No off-master prebrainstorm-notes branch applies (`reference_phase_11_local_ratelimit_prebrainstorm` is `local_ratelimit`-only).

### 1.7 Phase 74's relationship to the existing seams — a stat ADDITION whose only real question is CLASSIFICATION, and that question is ANSWERED

Every previous row in this lineage had to *transport* something to a seam. This one does not. The error is bound; the stats scope exists; the receiver is in hand. **The row's whole risk was concentrated in ONE question — can Go's server-side handshake error DISCRIMINATE "no cert" from "bad cert"? — and that question was answered BY EXECUTION this session, twice, independently.** §2.3.

---

## 2. Design decisions

### 2.1 Row + subject confirmation: the Observability family continues with the downstream TLS handshake-outcome stats *(SELF-PICKED per the standing directive → phase 74 row registered)*

The FIRST decision, made AUTONOMOUSLY (no human pick) per the 2026-07-12 standing directive. Picked as the **smallest defensible ROW-SIZED candidate** after INVESTIGATING each candidate's size against source THIS session (SIX agents at tip `de2d7737` — four independent read-only cost assessors on disjoint remits plus TWO adversarial verifiers; several ran live Docker probes; §11). Row 74 registers `in-progress` AT this BRAINSTORM commit per the ROADMAP §Schema invariant.

**Why it is smallest-defensible:** the substrate is landed on four axes — (a) the stats scope + registration function already exist and already register two names at exactly the right prefix; (b) the handshake error is already bound at the classification point; (c) the reference half of the evidence is ALREADY IN THE LANDED RECORD, live-probed at phase 65 (§2.4), so no new probe is owed for the stat NAMES; (d) the fixture that drives all three arms **already exists and already passes** (§2.6). It costs **+0 fixtures**.

#### ⚠️ THE HEADLINE — a 9-ROW-OLD DEFERRAL ADJECTIVE REFUTED BY EXECUTION

`docs/envoy-go/DECISIONS.md:16908` (ADR-0286 §Consequences **C3**) records this family as a NAMED COVERAGE BOUNDARY and says, VERBATIM at tip:

> *"A PRE-EXISTING framework gap phase 65 neither opens nor closes — adding a stat family is a **framework-surgery row of its own** and would blow the +0-stat envelope."*

**⚠️ FIRST, A CITE CORRECTION THE INCOMING FRAMING GOT WRONG, and it matters for how much authority the adjective carried.** The incoming brief attributed a SECOND claim to C3 — that the `fail_verify_error`-vs-`fail_verify_no_cert` split *"needs an opaque `crypto/tls` handshake-error callback wired into every per-chain `*stdtls.Config`."* **That sentence is NOT in ADR-0286 C3, and is not in DECISIONS.md at all.** RE-DERIVED by grep at tip: it lives in **BRAINSTORM prose, three rows deep** — `docs/envoy-go/phases/70-tracing-custom-tags-metadata/BRAINSTORM.md:70` (its fullest form, naming `VerifyPeerCertificate`/`GetConfigForClient`), copied to `.../71-.../BRAINSTORM.md:62`, and again to `.../72-.../BRAINSTORM.md:124` where it was upgraded to *"framework surgery **CONFIRMED**"*. So the blocking half of the deferral was never an ADR clause — **it was a BRAINSTORM adjective that acquired the authority of an ADR by sitting next to one across three rows.** That is `reference_code_comment_not_evidence` in its documentary form, and recording the misattribution is part of the finding.

**BOTH halves are now REFUTED, and the callback half was refuted BY EXECUTION — independently, by TWO agents, on separate Go toolchains.** The discrimination criterion was stated BEFORE the run (`reference_probe_must_discriminate`): *if Go's server-side `Handshake()` error distinguishes the two arms, the claim is refuted; if identical, it holds.* The probe used a real ECDSA CA + leaves, `RequireAndVerifyClientCert`, and **forced client-cert send via `GetClientCertificate`** — mandatory per `reference_go_client_cert_withholding`, since an untrusted-cert arm without forced-send is a VACUOUS green. Result:

```
=== A: no client cert ===
  err   = tls: client didn't provide a certificate
  type  = *errors.errorString
  errors.As(*tls.CertificateVerificationError) = false
=== B: untrusted client cert ===
  err   = tls: failed to verify certificate: x509: certificate signed by unknown authority
  type  = *tls.CertificateVerificationError
  errors.As(*tls.CertificateVerificationError) = true
```

`errors.As(err, &*tls.CertificateVerificationError)` is an **exact, total discriminator**, applied to the `err` value ALREADY BOUND at `manager.go:1178`. **No `VerifyPeerCertificate` wiring, no per-chain `*stdtls.Config` surgery, no opaque callback.** The second agent re-ran it independently at **both TLS 1.2 and TLS 1.3** and confirmed the server-side `HandshakeContext` errors **SYNCHRONOUSLY** on both arms — so this row does **NOT** inherit the TLS-1.3 half-RTT client-side workaround that fixtures `0108`/`0111` needed.

**Frame this as the pattern, not the incident** (`reference_deferred_candidate_cost_restale`): a deferral's cost adjective must be RE-DERIVED, not re-read. **This is now the FOURTH consecutive row where doing so materially changed the pick** (72: HOST reachable at the seam; 73: CLUSTER reachable without a `routeAction` method; 74: the handshake split needs no callback) — and this adjective had gone **NINE rows** (phase 65 → 74) without being re-tested, longer than any of the others.

**Rejected alternatives (recorded per the standing directive; each RE-DERIVED/SIZED against source this session — §11):**

- **The `upstream_cluster` span tag — THE RUNNER-UP, and this roster's richest entry. RE-FLAGGED as the strongest FUTURE pick.** Status: accepted-and-emitted-**EMPTY**. `internal/tracing/span.go:77-78` unconditionally emits `upstream_cluster` AND `upstream_cluster.name` from `SpanInputs.UpstreamCluster` (`span.go:29`); the three emit sites hard-code `""` — `internal/filter/hcm/accesslog_emit.go:51`/`:112`/`:173`, each carrying a `// not available at this seam` comment (RE-DERIVED: `:51`'s full text continues *"; Task 11 differential is UNasserted on this field"*, `:112`/`:173`'s *"; UNasserted in differential"* — the incoming quote was a truncated prefix). The builder half is ALREADY complete (`internal/tracing/span_test.go:109-113` pins both keys against a non-empty input). The gap dates to phase **46.1b** — ~28 rows — and `docs/envoy-go/BEHAVIOR_CONTRACT.md:704` explicitly invites the row, VERBATIM at tip: *"A coverage boundary; revisit if/when the framework plumbs upstream-cluster identity to stream-completion."*
  - ⚠️ **A LANDED SPEC CLAIM REFUTED:** `docs/envoy-go/phases/73-tracing-custom-tags-metadata-cluster/SPEC.md:375` (§13 item 6) says the tag *"remains a SEPARATE unchartered feature"* and that *"this row does NOT bring it closer."* **That is FALSE** — phase 73's own `Endpoint` retention pattern is exactly what makes a cluster name near-free to carry (`reference_code_comment_not_evidence` / `reference_a_drift_correction_is_itself_a_claim`).
  - The naive endpoint-carried shape **BUILDS AND PASSES** (EXECUTED, adversarial verifier, private scratch): `gofmt` silent · `go build`/`go vet` 0 · golangci-lint 0 · **70/70 packages ok, ZERO tests red** — and all **10** tracing differential fixtures PASS (`0087`, `0088`, `0102`, `0105`, `0106`, `0107`, `0114`, `0115`, `0116`, `0117`), with cross-side value parity EXECUTED (`ref` and `subj` both `STRING("c_backend")` ×12). ⚠️ **The roster is TEN drivers, not nine — RE-DERIVED mechanically at tip:** nine use `assertAttrPresent` (`0087`, `0102`, `0105`, `0106`, `0107`, `0114`, `0115`, `0116`, `0117`) and `0088` pins the same two keys via `assertTagPresent` (`test/fixtures/0088-tracing-zipkin/driver/driver.go:458-459`). The `0088` Zipkin tag-count hazard is **VACUOUS** — envoy-go's encoder drops tags by KEY only (`internal/tracing/zipkin.go:87-92`, RE-DERIVED: the `continue` is gated on `kv.Key == "node_id" || kv.Key == "zone"`), never by value.
  - ⚠️ **BUT THE GATE IS WRONG, PROVEN BY EXECUTION.** The reference's built-in `upstream_cluster` is **SELECTION-gated**, not pick-gated — **the opposite of the phase-73 `metadata` CLUSTER-kind tag, which the project proved IS pick-gated** (`reference_cluster_tag_gated_on_pick_not_route`). A live probe (fresh container, bridge net, `envoy.filters.http.fault` 100% abort so the route selects a cluster but the router never dials) returned `upstream_cluster='svc'` on a **503 `FI` local reply where no host was picked**; the control arm `/direct` (`direct_response`, no cluster) returned `None`. A second probe independently showed the reference carries the name on the connect-refused `UF` arm (`'c_dead'`, 15 tags) and the circuit-breaker `UO` arm (`'c_cb'`, 15 tags), while the naive endpoint-carried subject emitted `''` on both.
  - ⚠️ **AND THEN THAT WAS PARTLY REFUTED TOO — this is the roster's best content.** A **+3-line** variant was BUILT and PROBED: `func (c *Cluster) NameOnlyEndpoint() Endpoint { return Endpoint{clusterName: c.name} }`, swapped in as the initializer at `internal/filter/http/router/router.go:534` and `router_h2.go:74` in place of `picked := cluster.Endpoint{}` (both anchors RE-DERIVED EXACT at tip). Because `upstreamHostString` gates on `ep.Host == ""` (`accesslog_emit.go:277-279`) and `IsZero()` is `Host=="" && Port==0` (`cluster.go:113`), an Endpoint carrying ONLY a cluster name is **still `IsZero()`** — so `%UPSTREAM_HOST%`, the OTLP `UPSTREAM_HOST` operator, the gRPC `UpstreamRemoteAddress`, and `MetaLookup`/`ClusterMetaLookup` ALL keep their existing answers. Result: `gofmt`/build/vet clean, **70/70 ok with ZERO test edits**, all 10 tracing fixtures PASS, and cross-side **PARITY ACHIEVED** on both the `UF` (`'c_dead'`) and `UO` (`'c_cb'`) arms. **This is NOT the `picked = ep` hoist that `BEHAVIOR_CONTRACT.md:690` deliberately scoped out** — that one flips three access-log consumers and breaks landed unit tests; this one provably does neither.
  - **Why it still LOSES to row 74:** even in the ALT shape a residual remains. The **5 span-capable zero-endpoint emit sites are `SendLocalReply`/`chain.LocalReplyDone()` branches that pass a LITERAL `cluster.Endpoint{}` and never pass through `doH1ClusterAction` at all** — so a filter short-circuit (fault abort, ext_authz deny, ratelimit 429) that selected a cluster still emits `""` where the reference emits the name. **Emit-caller partition RE-DERIVED at tip by exhaustive grep — 18 sites, and the enumeration matches exactly:** **6 pre-decision** (`connection.go:330`, `:464`, `h2dispatch.go:313`, `:396`, `h3dispatch.go:130`, `:210` — nil `traceDecision`, can NEVER emit a span; corroborated by the landed comments at `connection.go:325-327`, `:459-460`, `h2dispatch.go:306-307`, `:164-166`) / **5 span-capable zero-endpoint** (`connection.go:597`, `:699`, `h2dispatch.go:530`, `h3dispatch.go:280`, `:341`) / **7 real-`picked`** (`connection.go:777`, `h2dispatch.go:577`, `:584`, `:613`, `h3dispatch.go:367`, `:373`, `:395`). *(The partition's nil-`traceDecision` half is RE-DERIVED from call-site argument shape + landed comments; it was NOT independently executed.)* So the row is either **~6 tasks that ADDS a named departure**, or **~10 tasks that removes one but leaves the filter-short-circuit class** — against row 74's ~8 tasks with NO residual.
  - Also a MODERATE pre-existing **presence** divergence: on a no-cluster `direct_response` span the reference OMITS both keys (12 tags) while envoy-go emits `""` (14 tags) — neither created nor closed by any of these shapes, but a future value-equality assertion on such an arm would fail on PRESENCE, not value. (Executed on **Zipkin only**; **OTLP on that arm is NOT VERIFIED**.) Reference `UF`/`UO` spans also carry an `error: "true"` tag envoy-go never emits — a separate pre-existing gap.
  - ⚠️ **A DISCRIMINATING BREAK nobody had named, for whoever takes this:** `extractEndpoints(la, clusterName)` — RE-DERIVED at tip as `internal/cluster/manager.go:875`, sole non-test call site `:433`. `la.GetClusterName()` is a **DIFFERENT proto field** from the cluster's `name` (grep at tip: `GetClusterName()` has **ZERO** occurrences in `internal/cluster/`), and they are EQUAL in every landed fixture, so a crossed lookup would stay GREEN. That is the row's best break and it needs a fixture where the two differ.

- **Pick-time `picked`-propagation (the `BEHAVIOR_CONTRACT.md:690` departure)** — every anchor CONFIRMED at tip: `picked := cluster.Endpoint{}` at `router.go:534`/`router_h2.go:74`, assigned at `router.go:592` (`picked = pooled.Endpoint()`) / `router_h2.go:153` (`picked = ep`); `AcquireH1` returns a NON-ZERO `ep` alongside both the overflow error (`internal/cluster/cluster.go:744-745`) and the dial error (`:752-753`). The production fix was measured at **2 lines** by the cost agent, proven by build+test, flipping EXACTLY three landed assertions (`router_test.go` `TestH1ClusterAction_DialFailureReturns503`, `TestH1ClusterAction_AccessLogInputs_DialFailure`, `TestDoH1ClusterAction_ConnPoolOverflow503`) while `TestDoH1ClusterAction_CircuitBreakerOverflow503` **stays GREEN** — a built-in negative control proving the fix is precisely scoped. ⚠️ **A DISCREPANCY recorded rather than resolved (`reference_a_drift_correction_is_itself_a_claim`):** the LANDED `BEHAVIOR_CONTRACT.md:690` calls the same change *"a ~4-line hoist of `picked = ep`"*. Two-vs-four is not adjudicated here; the SPEC of whichever row takes it must measure at ITS tip. ⚠️ The prior cite `cluster.go:720-722` is **STALE** and was already corrected at phase 73 to `:745` — CONFIRMED at tip; `BEHAVIOR_CONTRACT.md:690` now carries `:745` correctly. **REJECTED because it is not row-sized as a capability**: 2 production lines is a 3-task change wearing a row costume, the rest is probe budget; `router_h2_test.go` has ZERO `picked` assertions so the H2 half is entirely unpinned; and the **pool-overflow arm is REFERENCE-UNPRODUCIBLE** (`reference_max_connections_soft_breaker` — the reference PENDS rather than shedding; `BEHAVIOR_CONTRACT.md:690` independently records pool-overflow as *"the SOLE unprobed arm"*), so one arm can never be cross-side proven. Take it as a 2-task prelude inside whichever future row next touches that seam. ⚠️ Also record: an earlier controller hypothesis that this row would SHRINK the `upstream_cluster` residual from 5 sites to 1 was **REFUTED** (§11.4).

- **`hcm.merge_slashes` / `normalize_path` / `path_with_escaped_slashes_action`** — envoy-go has **NO path normalization at all** (RE-DERIVED: `grep -rn 'merge_slashes\|normalize_path\|MergeSlashes\|NormalizePath\|path_with_escaped_slashes' internal/ --include='*.go'` ⇒ **ZERO output**), and the knobs are known proto fields so the `DiscardUnknown:false` pass at `internal/bootstrap/bootstrap.go:576` does NOT catch them — CONFIRMED BY EXECUTION (`configuration OK` with all three set, vs `unknown field "bogus_unknown_field"` on the rename control). Both halves execution-proven. ⚠️ **But its "SMALL 6-7 / one pure string transform" adjective is REFUTED BY EXECUTION**: the reference rewrites the **FORWARDED `:path`**, not merely the matching path — with `merge_slashes: true` the backend received `RequestURI="/api/x/y"` and the span `http.url` likewise. envoy-go carries TWO disjoint path representations: matching reads `req.URL.Path` (`internal/filter/hcm/route.go:128`) but upstream forwarding reads `req.URL.RequestURI()` via `req.Write` (`internal/filter/http/router/router.go:602`) on H1/H3 and the COMPLETELY SEPARATE `H2Request.Path` on H2 (`internal/filter/hcm/h2/client.go:609`, populated at `h2/stream.go:338`). Four mutation points × three codecs. ⚠️ **And its blast radius contains a PRE-EXISTING H2 ROUTING BUG:** `internal/filter/hcm/h2/stream.go:440` uses `url.Parse` rather than `url.ParseRequestURI`, so a `:path` of `//foo///bar` yields `URL.Path == "///bar"` with `foo` silently absorbed as authority — **H2 route matching for `//`-prefixed paths ALREADY diverges from H1/H3 today.** Re-costed **10-13**; cannot land as a clean single row without fixing that first. Deferred, with the H2 bug recorded as a finding in its own right.

- **`access_log[].filter`** — the premise CONFIRMED (`GetFilter()` ⇒ **0** non-test hits repo-wide; sole parse site `internal/bootstrap/bootstrap.go:937-938` reads only `GetTypedConfig()`). **Both halves now EXECUTION-proven cross-side, closing the evidence gap phase 73 explicitly left open** (phase-73 BRAINSTORM §2.1 admitted *"what was EXECUTED is the envoy-go side ONLY … the divergence is very well evidenced but strictly INFERRED end-to-end"*): with `filter: {status_code_filter EQ 404}` over two requests (200 + 404), **envoy-go wrote 2 lines and the reference wrote 1 — the 404 only**; and `filter: {}` **boot-REJECTS on the reference** (verbatim: `AccessLogValidationError.Filter: … field: "filter_specifier", reason: is required`) while envoy-go boots clean. The rename control fired (`unknown field "filterZZZ"`), proving the accept is genuine and not a skipped subtree. ⚠️ **But the design is contested and the size claim REFUTED**: the sink interface is `Submit(r any)` (`internal/accesslog/accesslog.go:18-21`, RE-DERIVED EXACT), **not** `Submit(*Record)`; only **4 of 13** `AccessLogFilter` types are evaluable from `accesslog.Record` (`status_code`, `duration`, `and`, `or`) so the row is mostly a REJECT ROSTER, and the remaining nine (`header`, `traceable`, `metadata`, `runtime`, …) have their inputs at the EMIT site, meaning a decorator-at-`Submit` placement FORECLOSES the natural extension path. Sinks are one global flat slice (`internal/filter/hcm/config.go:114`, assigned `:363`) built at `cmd/envoy-go/main.go:115` and passed through six hops with NO per-entry structure. The `headerCaptureSink` non-promotion hazard is CONFIRMED and is exactly one non-test site (`internal/filter/hcm/config.go:403`, a comma-ok `s.(headerCaptureSink)` assert — `reference_conn_wrap_method_no_promote`), plus two more transparency obligations: `Close()` forwarding (`cmd/envoy-go/main.go:187`) and mongoproxy holding a CONCRETE `*accesslog.AsyncFileSink` (`internal/filter/network/mongoproxy/filter.go:83`) that bypasses the interface entirely. Re-costed **10-13**. Deferred — but note the phase-73 *"split resolves AGAINST the candidate"* adjudication was made on INFERRED evidence that is now DIRECT, so **that particular framing is RETIRED** even though the row still loses on size.

- **`hcm.access_log_options`** (field 56) — `GetAccessLogOptions`/`GetFlushAccessLogOnNewRequest`/`GetAccessLogFlushInterval`/`GetFlushLogOnTunnel` ⇒ **0** hits. EXECUTED both sides: `flush_access_log_on_new_request: true` ⇒ reference wrote **4** lines (2 start-entries + 2 end-entries), envoy-go **2**; the reference's start entry carries **response code `0`** and zero sizes with the same x-request-id. ⚠️ **Its "plausibly cheaper than `access_log[].filter`" adjective is REFUTED**: all three emit helpers guard `if statusCode == 0 || len(f.accessLog) == 0 { return }` (`accesslog_emit.go:59`/`:120`/`:181`), so the existing helpers CANNOT be reused for a status-0 start entry — a new emit path is owed in triplicate, i.e. it pays exactly the dispatch-function tax the alternatives avoid. Plus two DEPRECATED top-level twins (HCM fields 54/55) equally inert and needing their own disposition. Re-costed **9-11**. Deferred.

- **`stdout`/`stderr` access loggers** — ⚠️ **the prior cite named the WRONG FILE**: there is no `internal/xds/bootstrap.go`; the fallthrough is in `internal/bootstrap/bootstrap.go` — RE-DERIVED EXACT at tip: guard `:952` (`if tc.GetTypeUrl() != fileAccessLogTypeURL {`), comment `:953-954`, `return nil` `:955`. The line numbers were right in the wrong file — *"exactly how a wrong cite ships: it half-verifies"* (`feedback_brief_citations_not_evidence`). ⚠️ **The recorded blocker is HALF WRONG and the REAL blocker is on the other side**: `docker logs <cid> 2>/dev/null` returns the reference's access-log lines CLEAN (Docker demuxes fd1/fd2 for free), `ReferenceProxy` already retains the container handle (`test/differential/harness.go:98`) and the harness ALREADY calls `c.Logs()` on the boot-reject path (`:455`), and `github.com/docker/docker` is a DIRECT dependency (`go.mod:7`) so `pkg/stdcopy` needs no go.mod change. **The unrecorded blocker is the SUBJECT side**: `StartSubjectProxy` takes `cmd.StdoutPipe()` (`harness.go:253`) and hands it to `readyListenerAddrs` (`:266`), whose loop RETURNS the instant it reads `envoy-go ready` (the function at `:63`, the terminal check at `:73` — ⚠️ the incoming `:67-85` span is approximate; the function header is `:63`) — nothing drains the pipe afterward, so subject stdout is unreadable by any asserter and would BLOCK the subject process once ~64 KiB accumulates; and `cmd.Stderr = os.Stderr` (⚠️ **`:258`, not the incoming `:257`**) means subject stderr is never captured at all. Also live: `bootstrap.go:947` promoted `tcp_grpc` to a loud reject while the comment at `:953` still lists it beside `stdout` as silently ignored. Production cost genuinely small (~4 tasks) but it owes a real differential-HARNESS change. Re-costed **7-9**. Deferred.

- **`stats_flush_on_admin`** — the **53-call-site `admin.New`** figure is the ONE prior cost adjective that SURVIVED re-derivation, and it was established by compile-error census, not grep: adding a 7th parameter and compiling with `-gcflags=all=-e` (plain `go vet` truncates to 2 errors and would have given a badly wrong answer) yielded **exactly 53** `not enough arguments` sites — 18 `admin_test.go`, 10 `drain_test.go`, 9 `serverinfo_test.go`, 7 `clusters_test.go`, 5 `listeners_test.go`, 2 `configdump_test.go`, 1 `stats_test.go`, 1 `cmd/envoy-go/main.go`. *(The controller did NOT re-run the compile census; an independent constructor-call grep over `internal/admin/` + `main.go` returns **53**, which corroborates the TOTAL but not the per-file split.)* ⚠️ Qualification the prior costing omitted: **52 of 53 are TEST sites; production is ONE** (`cmd/envoy-go/main.go:333`), so a setter reduces true churn to a single call site — the 53 is the cost of NOT using a setter. The reject to lift is `internal/bootstrap/bootstrap.go:606-608` (RE-DERIVED EXACT); `internal/statssink/flusher.go:46` `flushOnce()` is ALREADY a standalone one-shot; the fuzz-seed inversion at `internal/bootstrap/statssink_fuzz_test.go:130-139` is CONFIRMED present. ⚠️ Decisive against it: its cross-side assertion is a **timing-shaped NEGATIVE** (nothing flushed during a quiet window, then a batch after `GET /stats`) — the flakiest shape in this harness, with no gauge to poll — and `stats_flush_on_admin` is in a **oneof** with `stats_flush_interval` *(proto claim, NOT VERIFIED at tip)*, so the negative must be framed against the 5s default. **MODERATE 8-10.** Deferred.

- **The rest, re-confirmed and deferred:** `spawn_upstream_span` (boot-REJECT at `internal/tracing/config.go:137-138`, RE-DERIVED EXACT; LARGE 12-15 — `BuildServerSpan` is the only builder and hard-codes a 16-attribute SERVER shape; a CLIENT span needs a second model, an upstream-leg timing seam that does not exist, parent/child linkage and a two-span export path. ⚠️ By reflection the field is `*wrapperspb.BoolValue`, so the `!= nil` guard **rejects an explicit `spawn_upstream_span: false`** too — a semantically-no-op config is boot-refused; `reference_protojson_wrapper_scalar_not_object`) · OTel `http_service` (`config.go:336-337`, a whole protobuf-over-HTTP transport, MOD-LARGE 10-12) · `sampler` (`:342-343`) and `resource_detectors` (`:339-340`) — `TypedExtensionConfig` extension points each needing a type-URL registry plus ≥1 concrete impl, and `sampler` must interpose on the landed `Decide`/`DecideWithContext` precedence (`internal/tracing/decision.go:72` onward) pinned by the `0087` sampling assertions; MODERATE 9-11 each · tracing `verbose` (`config.go:126-127`, a plain `bool` so no explicit-false trap; `Span` has NO events/logs field and adding one ripples `toProto`, `zipkin.go` and every receiver-side assertion; MODERATE 9-11) · force-trace (the ONLY genuine silent-ignore in the tracing set — ZERO `x-envoy-force-trace` handling repo-wide, `Decide` honors `X-Client-Trace-Id` only at `decision.go:88` — but LARGEST 12-15, blocked on envoy-go having NO internal-vs-external request classification at all, plus edge header sanitization; the unused `ServiceForced` SampleClass at `decision.go:29` and its stats arm at `internal/tracing/stats.go:87` already exist) · a SIXTH stats sink (still UNAVAILABLE, not merely large — the only unconsumed `stats_sinks[]` extension is `wasm`) · response-flags plumbing (`SpanInputs.ResponseFlags` hard-coded `"-"` at `accesslog_emit.go:53`/`:114`/`:175`; good SUBSTRATE, not a capability) · all xDS candidates (the "no persistent stream" finding stands — RE-CONFIRMED MECHANICALLY: `grep -n "for {" internal/xds/stream.go` returns NOTHING, exit 1; everything beyond the SDS envelope is blocked behind the `dynamic_resources` boot-reject) · all HTTP/3 + QUIC candidates (`HTTPExpectations` is TCP-only, so QUIC cross-side assertion is per-side-only) · the never-opened families — **gRPC** gated on an HCM response-trailer framework row (`RunDecodeTrailers`/`RunEncodeTrailers` still have ZERO production callers), **Runtime** genuinely unbuilt but lighting up ZERO existing consumers (all ~10 runtime-adjacent sites are themselves parse-rejects or documented ignores), and ⚠️ **WASM remains a ROADMAP BOOKKEEPING ARTIFACT** — `internal/wasm/` is a substantially-built wazero host and rows 25/25.1/25.2/25.3 are all `done`, so `NEVER OPENED: WASM` describes built work. **Deliberately NOT acted on at this docs-only row**, exactly as phases 72 and 73 decided, because the §12 stage-close mechanics depend on that sentinel output. Flagged again for a future single deliberate correction.

- **⚠️ NEWLY SURFACED this session — recorded, none chartered:**
  - ⚠️ **A swallowed-panic BOOT HANG.** Two HCM listeners sharing a `stat_prefix` makes envoy-go hang at boot with ZERO output and never exit. Diagnosed by SIGQUIT stack dump: `internal/stats/registry.go:107` panics `stats: duplicate metric registration: %q` (RE-DERIVED EXACT), raised from `internal/filter/hcm/config.go:358` (`registry.NewCounter(prefix + "downstream_rq_total")` — RE-DERIVED EXACT) via `parseFilterWithCtx`, and **SWALLOWED** by `main.main.func3()` at `cmd/envoy-go/main.go:299` — a deferred `<-flusherDone` channel receive that blocks forever during panic unwinding, so the panic message never reaches stderr and the process neither crashes nor boots. A direct hazard to ANY future multi-listener fixture and a nasty silent-flake source.
  - **The global access-log sink flattening**, now PROVEN BY EXECUTION: two listeners each with their own `access_log` file ⇒ **both files received both requests**. `parseAccessLogConfigs` (`internal/bootstrap/bootstrap.go:895`) appends across all listeners into one slice; `cmd/envoy-go/main.go:115` builds one flat `[]accesslog.Sink`; the listener manager hands the SAME slice to every listener. ⚠️ **DOWNGRADED from blocker to fixture-design constraint** — a filter travels WITH its sink, so per-entry semantics stay intact and a single-listener fixture sidesteps it; all six landed access-log fixtures are single-listener already.
  - **HCM response-identity headers `server_name` / `server_header_transformation` / `via`** — accepted-and-silently-ignored, EXECUTED both sides: with `server_name: my-proxy, via: myvia` the reference returned `server: my-proxy` + `via: myvia` and forwarded `via` upstream, while envoy-go returned `Server: BaseHTTP/0.6 Python/3.12.3` and no `via` anywhere. `serverHeader()` is a hardcoded literal in TWO packages (`internal/filter/hcm/codec.go:17` and `internal/filter/http/router/router.go:46`, both `return "envoy"` — RE-DERIVED EXACT) across 8 call sites; `via` has **ZERO** occurrences repo-wide. ⚠️ **Bonus defect at DEFAULT config, independent of the knob:** the reference **OVERWRITES** an upstream `Server` header while envoy-go only adds it when absent — the `hasServer` guard at `codec.go:79`/`:91`/`:100`, the same shape at `h2dispatch.go:673`/`:684-685`/`:692` *(⚠️ the incoming `h2dispatch.go:684` anchors the `if ln == "server" {` test; the add-if-absent branch is `:692`)* and at `h3dispatch.go:52` (`if h.Get("Server") == ""`). envoy-go's default is effectively `APPEND_IF_ABSENT` where the reference's is `OVERWRITE`. Masked because the harness's Go backends emit no `Server`. MODERATE 9-11 for the trio, SMALL ~7 for a `server_name`+`server_header_transformation` slice.
  - **Route-level header manipulation** — only `Route.response_headers_to_add` is read (`internal/filter/hcm/config.go:573`) and it is threaded ONLY into `buildDirectResponseAction` (`:1054`, applied `:1072`); the `Route_Route` arm (`:669`, dispatching to `buildRouterActionWithVH` at `:670`/defined `:711`) **DROPS it**. `request_headers_to_add` and both `*_to_remove` are unread at all three levels (RE-DERIVED: `GetRequestHeadersToAdd`/`GetRequestHeadersToRemove`/`GetResponseHeadersToRemove` have ZERO route-config consumers; the only `GetResponseHeadersToAdd` hits outside `config.go:573` are inside the ratelimit filter, an unrelated consumer). Only `OVERWRITE_IF_EXISTS_OR_ADD` is implemented (admitted at `internal/filter/hcm/actions.go:66-72`, comment TRUE at tip). EXECUTED both sides: the reference added `x-route-req`/`x-rc-req` upstream, REMOVED `x-drop-me`, and set `x-route-resp`/`x-rc-resp` on the response; envoy-go did none of it. Full parity LARGE 12+; a defensible SMALL ~7-8 slice exists (`request_headers_to_remove` at all three levels + extending `response_headers_to_add` to the routed arm). `echobackend` + existing driver `echoHeaders` helpers make the fixture nearly free — **no new BackendKind**.
  - **`RouteAction.prefix_rewrite` / `host_rewrite_literal` / `auto_host_rewrite`** — **ZERO** occurrences repo-wide (RE-DERIVED). ⚠️ **`docs/envoy-go/phases/04-http-1.1/SPEC.md:41` claims these "all error" and that is STALE AND WRONG at tip** — RE-DERIVED VERBATIM, the line lists *"route-level `host_rewrite_*`, `auto_host_rewrite`, route-level `timeout` / `retry_policy` / `hash_policy` / `request_headers_to_add` / `response_headers_to_add` / `prefix_rewrite` / `regex_rewrite` — all error"*, and several of those (weighted_clusters, retry_policy, response_headers_to_add) have since LANDED while `RouteAction` never grew rejects for the rewrites at all. `RouteMatch` kept its phase-04 rejects (`internal/filter/hcm/config.go:627` onward); `RouteAction` did not. EXECUTED: reference with `prefix_rewrite: "/v2"` alone rewrote the upstream path; `host_rewrite_literal` rewrote the upstream `Host`; envoy-go did neither. Extra parity item found by probe: when rewriting, the reference ALSO injects `x-envoy-original-path` / `x-envoy-original-host`. SMALL-MODERATE 8-10.
  - **Default-config header divergence** — the reference injects `x-forwarded-proto`, `x-request-id` and `x-envoy-expected-rq-timeout-ms` on EVERY upstream request; envoy-go injects NONE (`internal/filter/http/router/router.go:683` says so verbatim — *"The router does NOT inject x-envoy-\*, x-forwarded-\*, or x-request-id headers"* — and the probe confirms; `X-Request-Id` is stamped only when a tracing provider is configured, `internal/filter/hcm/connection.go:546`). Live, unrecorded, not an ignored-field row.
  - Also unread and named for completeness: `HealthCheck`'s 18 sibling interval fields (`parseHealthChecks` at `internal/cluster/health.go:415`) · `OutlierDetection`'s 4 (`internal/cluster/outlier.go:52`'s *"parse-accepted-and-ignored"* comment is TRUE at tip) · `Bootstrap.header_prefix` · `Listener.bind_to_port`/`socket_options`/`traffic_direction`/`tcp_backlog_size`/`connection_balance_config` · `DownstreamTlsContext.require_sni`/`session_timeout`/session-resumption knobs (RE-DERIVED: `GetRequireSni`/`GetSessionTimeout` ⇒ ZERO hits — ⚠️ **adjacent to row 74; a future row here must not collide with the `ssl.*` scope**) · `Cluster.common_http_protocol_options`/`upstream_connection_options`/`track_cluster_stats`/the `dns_*` family · `RouteAction.request_mirror_policies` (LARGE 12+) · `hcm.forward_client_cert_details` (XFCC) · `Route.decorator` (span-name override, tracing-adjacent) · `quic_options.idle_timeout`/`crypto_handshake_timeout` (`internal/listener/manager.go:360-361`'s accepted-and-ignored comment is TRUE at tip).

- **⚠️ ROADMAP BOOKKEEPING — the inherited ordinal artifact, NOT acted on.** The Observability-family CHAIN ordinal remains ONE LOW against a mechanical count (row 69 carries no ordinal word, so 70/71/72/73 each ran one short and 74 inherits it: this row is the TWENTIETH by chain, the twenty-first mechanically). **Phase 74 KEEPS the chain ordinal deliberately** — renumbering 74 alone would make it inconsistent with four landed `done` rows. Recorded so the eventual correction is one deliberate edit rather than a rediscovery. Every "TWENTIETH" in this document is the CHAIN ordinal.

### 2.2 Scope: THREE fixed-name counters at LISTENER scope; the dynamic names SPLIT OFF *(self-answered shape; SPEC pins — D-TLSHS-REGSCOPE)*

Phase 74 registers exactly three names under the existing listener prefix. **The one genuinely open registration question is UNCONDITIONAL-per-listener vs TLS-CHAINS-ONLY**, and it determines whether the surface delta is a flat **+3** or a **dynamic** family:

- **Unconditional** (register on every listener, alongside `downstream_cx_total`) ⇒ a plaintext listener carries three permanently-zero `ssl.*` counters. Simple; matches the existing `registerListenerMetrics` shape exactly; makes the surface delta a clean static +3.
- **TLS-chains-only** (register only when at least one selected chain carries a `transport_socket`) ⇒ no dead names, but the registration becomes conditional on chain-build state and the surface delta becomes config-dependent, which the project's stat-surface accounting has no precedent for at listener scope.

**Anticipated: UNCONDITIONAL**, on the +3-static grounds. **D-TLSHS-REGSCOPE decides, and it must PROBE what the reference does** — specifically whether a plaintext-only reference listener exposes zero-valued `ssl.*` counters in `/stats`. That probe is cheap and discriminating and the SPEC owes it.

### 2.3 The classifier: `classifyHandshakeErr(err) → {ok, verifyError, noCert, other}` *(EXECUTION-answered; SPEC pins the table — D-TLSHS-CLASSIFY)*

A single table-driven helper in `internal/listener`:

- `err == nil` ⇒ **`ok`** ⇒ `ssl.handshake`.
- `errors.As(err, &*tls.CertificateVerificationError)` **true** ⇒ **`verifyError`** ⇒ `ssl.fail_verify_error`.
- the bare no-cert error ⇒ **`noCert`** ⇒ `ssl.fail_verify_no_cert`.
- everything else (ALPN mismatch, protocol error, EOF, ctx cancel, unknown SNI) ⇒ **`other`** ⇒ **no counter** at this row. ⚠️ Naming `other` explicitly, and asserting that it increments NOTHING, is what keeps the row from silently over-counting; a break that collapses `other` into `fail_verify_error` must fire.

⚠️ **THE FRAGILITY, and the task that guards it.** The no-cert arm is a bare `errors.New("tls: client didn't provide a certificate")` — `*errors.errorString`, **NO exported sentinel**, EXECUTED and typed above. Matching it requires a string comparison. **The test for that arm MUST construct its error by running a LIVE in-process handshake, never by hand-writing the string** — a hand-written string tests nothing and would sail through a Go toolchain that reworded the message. Written that way, the test IS the tripwire that fires when the stdlib changes. **The ADR must record this as a named fragility, not bury it.**

### 2.4 The reference half is ALREADY in the landed record — and the record is RICHER than the incoming framing said

`docs/envoy-go/DECISIONS.md:16890` (ADR-0286 §Context) records phase-65's SPEC §11 live probes against `envoyproxy/envoy:contrib-v1.37.2`: *"arm 1 CA-signed → 200; arm 2 wrong-CA → `ssl.fail_verify_error`; arm 3 no-cert → `ssl.fail_verify_no_cert`."* The same three names appear at `docs/envoy-go/BEHAVIOR_CONTRACT.md:916` and the boundary itself at `:928`. **So the reference emits exactly these names and the arms are already EXECUTED.**

⚠️ **A CORRECTION TO THE INCOMING FRAMING, and it is in the row's favour.** The incoming brief listed the reference's exact SCOPE/spelling as **NOT VERIFIED**. RE-DERIVED at tip, it is **partly in the landed record already**:

- `docs/envoy-go/phases/65-xds-sds-validation-context/SPEC.md:270` carries a VERBATIM probe line: **`listener.0.0.0.0_10000.ssl.fail_verify_error: 1`**. ⇒ the reference's scope IS `listener.<normalized addr>.`, which is byte-for-byte the prefix `registerListenerMetrics` already builds at `manager.go:352`. The `:278` line does the same for `ssl.fail_verify_no_cert`.
- `.../65-.../SPEC.md:282` records the **full observed roster**: `ssl.handshake: 1` · `ssl.fail_verify_error: 1` · `ssl.fail_verify_no_cert: 1` · `ssl.ciphers.TLS_AES_256_GCM_SHA384: 1` · `ssl.versions.TLSv1.3: 1` · `ssl.certificate.validation_ca.expiration_unix_time_seconds`. **⇒ a FOURTH `ssl.` name exists (a per-secret cert-expiry GAUGE) that no prior framing named** — explicitly OUT of scope here, recorded so it is not rediscovered.
- **`ssl.handshake: 1` across a probe of THREE arms (one success, two rejects) STRONGLY IMPLIES successes-only, not attempts.** Marked as a doc-sourced INFERENCE, not a verified fact.

⚠️ **What remains a SPEC-stage obligation, stated plainly:** all of the above is **DOC-SOURCED from phase 65, not re-executed at this tip**, and the reference version may have moved. The SPEC must RE-PROBE: (i) the exact scope + spelling at `contrib-v1.37.2`; (ii) whether `ssl.handshake` counts successes only or all attempts (a 2-arm probe discriminates); (iii) whether a plaintext listener exposes the names at all (D-TLSHS-REGSCOPE). **Nothing in this section may be cited as a re-executed observation.**

### 2.5 ⚠️ THE FAMILY SPLITS — and the second half is genuinely blocked (this is a finding, record it as one)

`ssl.ciphers.*` / `ssl.versions.*` (and `ssl.curves.*`) are DYNAMIC, wire-derived stat names, and two things bite:

1. **Charset.** `stats.NamePattern` = `` `^[a-zA-Z_]([a-zA-Z0-9_.]*[a-zA-Z0-9_])?$` `` (`internal/stats/registry.go:48`, RE-DERIVED VERBATIM) — **no hyphen**. Probed: `ssl.ciphers.ECDHE-RSA-AES128-GCM-SHA256` (the Envoy/OpenSSL TLS1.2 form) ⇒ `IsValidName=false` ⇒ `checkName` **PANICS** (the panic path is `register`/`getOrRegister` → `r.checkName(name)`, `registry.go:102`/`:178`). `ssl.ciphers.TLS_AES_128_GCM_SHA256` (TLS1.3 form) and `ssl.versions.TLSv1.2` are valid. This is the landed `reference_dynamic_stat_name_charset_guard` hazard.
2. **Name mismatch.** Go's `tls.CipherSuiteName` yields IANA forms (`TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256`); Envoy yields OpenSSL forms (`ECDHE-RSA-AES128-GCM-SHA256`). A cross-side EXACT assertion is **INFEASIBLE** without a hand-maintained mapping table — and the mapped names then trip the charset guard.

**NOT a blocker: lifecycle.** Post-`Freeze()` registration is explicitly permitted — `NewCounterIfAbsent` (`internal/stats/registry.go:161`) and `NewGaugeIfAbsent` (`:208`) both route through `getOrRegister` (`:177-188`), whose doc comment states *"PERMITTED post-Freeze by design (ADR-0117)"*. ⚠️ *(Cite correction: the incoming `registry.go:177-186` names the `getOrRegister` BODY, not the two `*IfAbsent` functions; those are at `:161` and `:208`.)* So lazy per-cipher counters are structurally fine. **The blocker is NAMING, not lifecycle.**

⇒ **ADR-0286 C3's boundary as written CONFLATES a cheap half with a blocked half. Splitting it is part of this row's contribution**, and the surviving ciphers/versions boundary must be RESTATED at `BEHAVIOR_CONTRACT.md:928` rather than deleted.

### 2.6 Fixture posture: +0 new fixtures — `0111` is EXTENDED with a `StatsAsserter` *(SPEC confirms — D-TLSHS-FIXTURE)*

`test/fixtures/0111-tls-cvc-empty-dynamic-fallback/` **already drives all three arms against ONE `require_client_certificate: true` listener** — RE-DERIVED at tip: `envoy-go.yaml:70` `require_client_certificate: true`; `README.md:73` `## The three arms (the observable)`; the arms are trusted-accept / forced-send-untrusted-reject / none-reject, with the forced-send installed at `driver/driver.go:520` (`cfg.GetClientCertificate = …`) exactly as `reference_go_client_cert_withholding` requires. The driver is a self-contained PKI generator (`driver.go` — `mustCA` `:193`, `mustLeaf` `:220`, `mustClientCert` `:246`), `BackendCount()` returns **1** (`:293`, satisfying `reference_differential_backendcount_min_one`), and it is registered at `test/differential/runner_test.go:138`.

**It has NO `StatsAsserter` today** — RE-DERIVED: zero `AssertStats|StatsAsserter` hits in that driver. And the fixture's own docs name the reason as a COVERAGE BOUNDARY, VERBATIM at `README.md:161-162`:

> *"**No `ssl.*` stats** — envoy-go emits none, so a verdict `StatsAsserter` is infeasible (inherits PLAN-65 C3); a pre-existing framework gap."*

**⇒ Row 74 RETIRES a boundary the fixture itself confesses.** The row ADDS a `fixture.StatsAsserter` (the interface at `test/differential/fixture/fixture.go:75`) to an EXISTING fixture — **no new YAML, no new fixture directory, fixtures stay 119**. The corresponding note at `expectations.yaml:178` must be retired in the same commit.

⚠️ **Two fixture obligations.** (i) The asserter must be a **cross-side DELTA** on the three counters, not an absolute count (`reference_panic_counter_differential_delta_assertion`), and it must scrape both admin addresses the runner already passes. (ii) ⚠️ **The reference-side names are the phase-65 spellings and the reference-side arm counts are NOT yet re-probed** — if D-TLSHS-SCOPE finds the reference has moved, the asserter must go subject-side-only for the moved names rather than ship a wrong cross-side pin.

### 2.7 Anticipated task spine (~8; the PLAN decomposes)

1. Register the three counters in `registerListenerMetrics`; **decide + pin D-TLSHS-REGSCOPE** (§2.2). 2. `classifyHandshakeErr`, table-driven, with the **live-handshake-constructed** no-cert case (§2.3). 3/4/5. Success / wrong-CA / no-cert increment tests — these three form a DISCRIMINATING cross-product, and **a break collapsing the classifier to one bucket must fire in exactly two of the three** (`reference_probe_must_discriminate`; `reference_deliberate_break_wrong_assertion` — confirm WHICH assertion fired). 6. **Scope boundary:** the QUIC/HTTP-3 listen path is a STRUCTURALLY SEPARATE runtime — `internal/listener/quic.go` has its own `quicAcceptLoop` (`:104`) and `serveQUICConnection` (`:120`), and `serveConnection` (`manager.go:1113`) is never reached from it — so pin that it neither panics nor miscounts, and NAME it a coverage boundary. 7. The `0111` `StatsAsserter` + retiring its two self-confessed boundary notes. 8. Docs: `BEHAVIOR_CONTRACT.md:928` retire the boundary for the three fixed names and **RESTATE the surviving ciphers/versions boundary**; the ROADMAP deferred-sentence narrow (⚠️ **at the IMPL only**); ADR-0296.

⚠️ **A `TestNoNewStat`-style registration guard is OWED** — this is the first stat-surface-changing row in a long streak, and the precedent is `internal/statssink/registration_test.go:26` `TestNoNewStat_RegistrationGuard` (RE-DERIVED EXACT). Here it inverts: the guard must assert **EXACTLY THREE** new names appear at listener scope and that a plaintext listener's set matches whatever D-TLSHS-REGSCOPE decided.

### 2.8 Stat surface hypothesis: **+3** *(the first non-zero delta in this lineage)*

Anticipated stat surface **1201 → 1204**. ⚠️ **This BREAKS the long +0-stat streak deliberately** — that streak was never a goal, it was a property of the rows that happened to be cheapest. ADR-0286 C3 cited "would blow the +0-stat envelope" as a reason to defer; **an envelope is not an argument, and the row is worth three names.** Say so in the ADR rather than apologising for it.

⚠️ **Charset is NOT at risk for these three** — `ssl.handshake` / `ssl.fail_verify_error` / `ssl.fail_verify_no_cert` are FIXED, hyphen-free, and pass `NamePattern` by inspection. The charset hazard applies ONLY to the deferred dynamic half (§2.5). The `normalizeAddr(rt.addr)` segment is already guarded today by the two landed listener counters.

---

## 3. Framework-survey result — a stat addition + one classifier; ZERO new packages/modules

### 3.1 Framework: no seam at all

No new interface, no new package-level type, no new exported symbol outside `internal/listener` (and plausibly none inside it — `classifyHandshakeErr` is unexported). Three `*stats.Counter` fields on `listenerRuntime`, three `r.NewCounter(prefix + …)` lines, one helper, two call points. **This is the smallest production seam of anything assessed this session.**

### 3.2 NEW packages: NONE

All production edits land in `internal/listener/manager.go`. `internal/stats`, `internal/tls`, `internal/xds`, `internal/boot`, `internal/bootstrap` are anticipated BYTE-UNTOUCHED. NO new package, so `reference_new_subpackage_pulls_transitive_module` does not bite — still re-check `git diff go.mod` after tidy at the IMPL.

### 3.3 go.mod modules: NONE

The classifier needs `errors` and `crypto/tls` — `crypto/tls` is ALREADY imported by `manager.go` (aliased `stdtls`, used at `:1177`). `errors` may or may not already be imported there; **either way an import LINE is not a go.mod MODULE**, so `+0 modules` holds. `go mod tidy -diff` anticipated EMPTY; the tracked go.mod-modules lineage figure stays **2** (per STATE.md this is the phase-61.2 count — `quic-go` direct + `qpack` indirect — **NOT a repo total**; the single `go.mod` requires **67** modules, controller-verified at tip).

### 3.4 REUSES

- **phase-06.1** the listener-scope stats chassis: `registerListenerMetrics` (`manager.go:351-355`), the `"listener." + normalizeAddr(rt.addr) + "."` prefix (`:352`), the `*stats.Counter` field pattern (`:175-176`).
- **phase-07.2** the unified pre/post-handshake dispatch: `serveConnection` (`manager.go:1113`) and its per-chain `*stdtls.Config` handshake block (`:1176-1182`) — the row adds nothing to the control flow, only two counter increments.
- **phase-65/66/67/68** the mTLS substrate: `require_client_certificate` → `ClientAuth` → the three observable handshake outcomes, and the `0111` fixture that already drives all three.
- **ADR-0117** post-`Freeze()` `*IfAbsent` registration — NOT needed by this row (all three names are static, pre-Freeze) but named because §2.5's deferred half depends on it.
- **`internal/statssink/registration_test.go:26`** as the registration-guard shape precedent (inverted, §2.7).

---

## 4. Bootstrap-level applicability — NONE; this is a LISTENER-RUNTIME row

Unlike every Observability row since 59, phase 74 consumes **no new config field at all**. It adds observability for behavior that is ALREADY fully implemented and ALREADY config-driven (`require_client_certificate` + a trust anchor, static or SDS-delivered). There is no parse arm, no reject to lift, no `DiscardUnknown` interaction, no fuzz-seed classification to flip.

**One consequence worth stating plainly: this row is a pure OBSERVABILITY addition, not a knob-lift and not a bug-fix.** Nothing behaves differently after it lands; three previously-invisible outcomes become countable. That is the honest case for it, and it is a strong one — the project has shipped mandatory mTLS since phase 16 with **zero** verification-outcome observability, and `0111`'s own README says so.

---

## 5. Stat surface hypothesis — **+3** (74): 1201 → 1204

Three fixed names at listener scope. See §2.2 (registration posture, which determines whether the delta is flat) and §2.8 (why breaking the +0 streak is correct). Charset-safe by inspection; the charset hazard belongs to the DEFERRED dynamic half (§2.5).

---

## 6. Anticipated edit sites (SPEC RE-DERIVES each at tip — a BRAINSTORM cite is not evidence, `feedback_brief_citations_not_evidence`)

All line numbers below were RE-OPENED at tip `de2d7737` by the controller while writing this document (§11.3 records what needed correcting).

- `internal/listener/manager.go:175-176` — THREE new `*stats.Counter` fields on `listenerRuntime`, appended beside `downstreamCxTotal`/`downstreamCxActive`.
- `internal/listener/manager.go:351-355` — three `r.NewCounter(prefix + "ssl.…")` lines inside `registerListenerMetrics`, using the EXISTING `prefix` built at `:352`. **Registration posture per D-TLSHS-REGSCOPE (§2.2).**
- `internal/listener/manager.go` (new, package-private) — `classifyHandshakeErr(err error) handshakeOutcome` (§2.3). Anticipated ZERO new go.mod modules; possibly ONE new import line (`errors`).
- `internal/listener/manager.go:1178` — the error arm: classify `err`, Inc the matching counter, then the EXISTING log + close + return, byte-order preserved.
- `internal/listener/manager.go:1183` — the success fall-through (`dispatchConn = tlsConn`): Inc `ssl.handshake`. ⚠️ **The plaintext path (`selected.tlsCfg == nil`) must Inc NOTHING** — a break that moves the success Inc outside the `if` must fire.
- `internal/listener/manager_test.go` — the classifier table (with the **live-handshake-constructed** no-cert case, §2.3), the three increment tests, the plaintext-no-Inc test, and the registration guard (§2.7). ⚠️ **`t.Errorf` per property, `t.Fatalf` only for a broken precondition** (`reference_fatalf_makes_assertions_unreachable`).
- `internal/listener/quic.go` — **BYTE-UNTOUCHED**; the coverage-boundary task (spine item 6) only PINS that the QUIC runtime (`quicAcceptLoop` `:104`, `serveQUICConnection` `:120`) neither panics nor miscounts, and names the boundary in docs.
- `test/fixtures/0111-tls-cvc-empty-dynamic-fallback/driver/driver.go` — implement `fixture.StatsAsserter` (`test/differential/fixture/fixture.go:75`); a cross-side DELTA assertion over the three counters. NO YAML change, NO new dir.
- `test/fixtures/0111-tls-cvc-empty-dynamic-fallback/README.md:161-162` and `expectations.yaml:178` — retire the two self-confessed *"No `ssl.*` stats … a verdict `StatsAsserter` is therefore INFEASIBLE"* notes. ⚠️ **These are the row's proof that it closed something real; leaving them stale would be the `reference_code_comment_not_evidence` class recurring in a fixture.**
- `docs/envoy-go/BEHAVIOR_CONTRACT.md:928` — retire the boundary for the three fixed names **and RESTATE the surviving `ssl.ciphers.*`/`ssl.versions.*` boundary with its two blockers** (§2.5). ⚠️ Also check `:916`, which currently narrates the reference's three arms as a phase-65 probe; it may want a "and envoy-go now emits these" clause.
- `docs/envoy-go/DECISIONS.md` — **ADR-0296** (§Context at the SPEC per ADR-0044; §Decision + §Consequences IN PLACE at the IMPL). ⚠️ The ADR must record (a) the C3 misattribution (§2.1), (b) the classifier fragility (§2.3), (c) the family split (§2.5), (d) why breaking the +0-stat streak is correct (§2.8).
- `docs/envoy-go/ROADMAP.md` — the deferred-sentence narrow at the **IMPL only**. RE-DERIVED at tip, the live Observability clause reads *"the downstream TLS handshake-outcome `ssl` stat family (handshake/fail_verify_error/fail_verify_no_cert/ciphers/versions — a NAMED COVERAGE BOUNDARY recorded at phase 65 per ADR-0286 §Consequences C3 …) + tracing `spawn_upstream_span`/`http_service`/force-trace."* ⇒ **the clause SHORTENS (three of five sub-names roll out, `ciphers`/`versions` survive) rather than being removed**, the sentence keeps its `candidates:` marker and still carries ≥1 candidate, so **check (2) STAYS 3**. Verified by reading the sentence, not assumed — this is exactly the hazard phase 73 flagged for itself and then had to re-adjudicate at its SPEC.

---

## 7. BRAINSTORM-time open questions to the SPEC (the D-TLSHS-* docket)

Marked **[PROBE]** where a live arm against `envoyproxy/envoy:contrib-v1.37.2` is REQUIRED, **[CODE]** where a read-only re-derivation suffices. Probe hygiene carries forward: a fresh container per arm (`feedback_probe_fresh_container_per_arm`), a bridge network (`reference_docker_probe_bridge_network`), the host-gateway literal plus `dns_lookup_family: V4_ONLY` (`reference_host_gateway_ip_docker_desktop`), and every arm must DISCRIMINATE (`reference_probe_must_discriminate`).

- **D-TLSHS-SCOPE** **[PROBE]** — the reference's exact scope + spelling at THIS tip. *Anticipated: `listener.<normalized addr>.ssl.<name>`, per the landed `phases/65-.../SPEC.md:270` line — but that is DOC-SOURCED and must be re-executed (§2.4).*
- **D-TLSHS-SEMANTICS** **[PROBE]** — does `ssl.handshake` count SUCCESSES only or ALL attempts? A 2-arm probe (1 success + 1 reject, fresh container) discriminates. *Anticipated: successes only, inferred from the landed 3-arm roster showing `ssl.handshake: 1` (§2.4).*
- **D-TLSHS-REGSCOPE** **[PROBE + CODE]** — unconditional-per-listener vs TLS-chains-only registration (§2.2). The PROBE half: does a PLAINTEXT reference listener expose zero-valued `ssl.*` in `/stats`? *Anticipated: unconditional.* **This decides whether the surface delta is a flat +3.**
- **D-TLSHS-CLASSIFY** **[CODE]** — the `{ok, verifyError, noCert, other}` table, and specifically that `other` increments NOTHING. The `errors.As` discriminator is EXECUTION-established (§2.1) but the SPEC must re-run it at ITS toolchain and pin the exact no-cert string. **The no-cert test case must be built from a LIVE handshake (§2.3).**
- **D-TLSHS-ALERTMAP** **[PROBE]** — ⚠️ **NOT VERIFIED and genuinely open:** do Go's two error shapes map cleanly onto the reference's TLS alerts (**48 `unknown_ca`** for the untrusted-cert arm vs **116 `certificate_required`** for the no-cert arm)? The session's probe was **Go-server vs Go-client**; nothing has confirmed that reference-Envoy's bucket assignment agrees with Go's error taxonomy on every input. A `VerifyClientCertIfGiven` listener, an expired-but-trusted cert, and a cert with a bad SAN are three inputs where they might diverge.
- **D-TLSHS-VERIFYIFGIVEN** **[PROBE + CODE]** — at `require_client_certificate: false` (phase 67's `VerifyClientCertIfGiven`), does the reference count a no-cert connection as `ssl.handshake` (it succeeds) and nothing else? ⚠️ `reference_go_client_cert_withholding` warns that a nil pool + `VerifyClientCertIfGiven` falls back to SYSTEM ROOTS — a probe here must not be vacuous.
- **D-TLSHS-QUIC** **[CODE]** — pin that the QUIC/H3 listen path (`internal/listener/quic.go`) is untouched, neither panics nor miscounts, and NAME it a coverage boundary. ⚠️ QUIC handshakes are mandatory-TLS1.3 and DO produce handshake outcomes the reference plausibly counts; leaving them uncounted is a DEPARTURE that must be written down, not merely omitted.
- **D-TLSHS-FIXTURE** **[CODE]** — extend `0111` with a cross-side DELTA `StatsAsserter` over the three counters; retire its two self-confessed boundary notes. **+0 fixtures (119).** Pin the full-subtest `-run` selector (`reference_differential_run_selector`) and run breaks with `-count=1` AFTER committing (`reference_differential_break_protocol_count1`, `reference_break_protocol_commit_first`).
- **D-TLSHS-SPLIT** **[CODE]** — the `ciphers`/`versions` half stays DEFERRED with its two blockers recorded (§2.5), and the BEHAVIOR_CONTRACT boundary is RESTATED rather than deleted.
- **D-TLSHS-GUARD** **[CODE]** — the inverted registration guard (§2.7): EXACTLY three new listener-scope names.
- **D-TLSHS-ROADMAP** **[CODE]** — confirm the deferred-sentence narrow SHORTENS rather than removes (§6, §12) and that check (2) stays **3**.

---

## 8. What phase 74 does NOT deliver (forward)

- **`ssl.ciphers.*` / `ssl.versions.*` / `ssl.curves.*`** — SPLIT OFF and DEFERRED with two named blockers (charset PANIC; IANA-vs-OpenSSL name mismatch making cross-side EXACT assertion infeasible). Lifecycle is NOT a blocker (§2.5). Plus the fourth observed name, `ssl.certificate.<secret>.expiration_unix_time_seconds`, a per-secret gauge nobody had named (§2.4).
- **The `upstream_cluster` span tag** — the RUNNER-UP and the strongest FUTURE pick, now with a `+3-line` `NameOnlyEndpoint` variant BUILT and cross-side PARITY-PROVEN on the `UF`/`UO` arms, the SELECTION-vs-PICK gate EXECUTED, the 18-site emit partition re-derived (6/5/7), the 5-site local-reply residual named, and a DISCRIMINATING break identified (`extractEndpoints`'s `la.GetClusterName()` vs `clusterName`). §2.1, §11.
- **The pick-time `picked`-propagation row** — 2-vs-4 production lines CONTESTED (§2.1); pool-overflow REFERENCE-UNPRODUCIBLE; take it as a 2-task prelude, not a row.
- **`hcm.merge_slashes`** — re-costed **10-13**, and it carries a **PRE-EXISTING H2 ROUTING BUG** (`h2/stream.go:440` `url.Parse`) that must be fixed first. §2.1.
- **`access_log[].filter`** — now EXECUTION-proven cross-side on BOTH halves (closing phase 73's admitted gap), but re-costed **10-13** on a contested design. §2.1.
- **`hcm.access_log_options`** (re-costed 9-11; its "cheaper" adjective REFUTED) · **`stdout`/`stderr` loggers** (re-costed 7-9; the real blocker is the SUBJECT-side undrained pipe) · **`stats_flush_on_admin`** (8-10; timing-shaped negative). §2.1.
- **NEWLY SURFACED, none chartered:** the swallowed-panic BOOT HANG · the access-log sink flattening (downgraded to a fixture-design constraint) · HCM `server_name`/`server_header_transformation`/`via` + the default-config `Server` OVERWRITE-vs-APPEND defect · route-level header manipulation · `RouteAction.prefix_rewrite`/`host_rewrite_*` (+ the STALE phase-04 SPEC claim) · the default-config `x-forwarded-proto`/`x-request-id`/`x-envoy-expected-rq-timeout-ms` divergence. §2.1.
- All tracing remainders / a sixth stats sink (**unavailable**, not deferred) / response-flags plumbing / all xDS / all HTTP-3 / the never-opened families (gRPC, Runtime, and the WASM bookkeeping artifact). §2.1.

---

## 9. ADR-0045 split readiness + ADR roster

Anticipated a **SINGLE FLAT ROW** of ~8 tasks (ADR-0045 valve armable-but-unconsumed). The production diff is confined to one file; the task count is dominated by the three discriminating increment tests plus the boundary pin and the fixture extension.

**ONE ADR anticipated: ADR-0296.** Re-derived MECHANICALLY in the worktree: `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1` ⇒ `## ADR-0295`, and `grep -c '^## ADR-0296'` ⇒ **0**, so next-free is **`ADR-0296`**. ADR-0295 is COMPLETE. ADR-0296's §Context drafts at the **SPEC** per ADR-0044 (the DECISIONS tail flips ADR-0295 → ADR-0296 AT that commit); §Decision + §Consequences complete **IN PLACE at the IMPL**, no renumber; next-free after it is ADR-0297. **DECISIONS.md is UNTOUCHED at this BRAINSTORM.**

No new ADR beyond ADR-0296 is anticipated. ⚠️ **ADR-0296 has one obligation its siblings did not:** it must record that **ADR-0286 C3's deferral rested partly on a claim C3 never made** (§2.1) — a correction ACROSS ADRs, which the project has not had to write before. It belongs in §Consequences, phrased as a documentary finding, not as a criticism of phase 65 (whose own text was accurate; the drift happened downstream in BRAINSTORM prose).

---

## 10. Envelope + counts (anticipated at the phase-74 IMPL; docs-only at this BRAINSTORM)

- ZERO new production packages. Production edits in **ONE** existing file.
- **+3 stats → 1204.** The first non-zero stat delta in this lineage (§2.8).
- +0 fixtures → **119** (`0111` EXTENDED, not cloned). +0 fuzzers → **55**. +0 BackendKinds → **38**. +0 go.mod modules → **2** (the tracked lineage figure).
- Blast radius: `internal/listener/manager.go` is SHAPE-pinned (not BYTE-UNTOUCHED). `internal/listener/quic.go`, `internal/stats`, `internal/tls`, `internal/xds`, `internal/boot`, `internal/bootstrap`, `internal/tracing`, `internal/filter/**`, `internal/cluster`, `validate/` all anticipated **BYTE-UNTOUCHED** — verify by sha256, not by reading (the phase-72/73 precedent).
- Layering check: `internal/listener` already imports `internal/stats`; no new edge. Re-run `go list -deps` (no `...`) at the IMPL as DISCIPLINE, not as a cycle check.
- DECISIONS tail **ADR-0295 → ADR-0296** (at the SPEC; next-free ADR-0297).

**Counts UNCHANGED at this BRAINSTORM (docs-only; ALL re-run MECHANICALLY in the worktree at this close, using the CANONICAL commands recorded in STATE.md):** fixtures **119** (`ls -d test/fixtures/[0-9]*/ | wc -l` ⇒ 119; numeric tail `0117-tracing-custom-tags-metadata-cluster`) · fuzzers **55** (`grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l` ⇒ 55) · stat surface **1201** (BEHAVIOR_CONTRACT doc count; NO mechanical command) · BackendKind **38** (`H2GoawayResponder`, `test/differential/fixture/fixture.go:614`) · DECISIONS tail **ADR-0295** COMPLETE (next-free **ADR-0296**) · go.mod modules **2** (the phase-61.2 lineage figure; the single `go.mod` requires **67**, controller-verified).

---

## 11. Sized-against-source — the cost derivations (SIX agents at tip `de2d7737`)

**SIX agents ran at tip `de2d7737`:** four independent read-only cost assessors on disjoint remits (access-log family · tracing remainders · pick-time + operational tooling · a repo-wide accepted-and-silently-ignored sweep) plus **TWO adversarial verifiers** — one attacking the RUNNER-UP by building it end-to-end, one attacking the RANKING. Several ran live Docker probes against `envoyproxy/envoy:contrib-v1.37.2`.

- **`tls-handshake-outcome-stats` (the pick) — SMALL ~8.** The inherited deferral is REFUTED on both halves, one of them BY EXECUTION twice on separate toolchains (§2.1). The substrate is landed on four axes; the fixture already exists and already drives all three arms; the reference half is already in the record. The ONE genuinely new cost versus its predecessors is the **+3 stat surface**, which brings a registration-guard obligation (§2.7) and a registration-posture decision (§2.2).
- **`upstream_cluster` span tag — the RUNNER-UP, ~6 (with a named departure) or ~10 (without).** Two shapes BUILT and PASSING; the reference gate PROBED and found SELECTION-gated; the ALT `NameOnlyEndpoint` variant reaching cross-side PARITY on `UF`/`UO` for 3 lines. **Loses on the 5-site local-reply residual, NOT on cost.** §2.1, §11.4.
- **Pick-time `picked`-propagation — 3 tasks wearing a row costume.** Deferred as a prelude. §2.1.
- **`hcm.merge_slashes` — RE-COSTED 10-13** (adjective REFUTED by execution; carries a pre-existing H2 routing bug). §2.1.
- **`access_log[].filter` — RE-COSTED 10-13**, but its evidence upgraded from INFERRED to DIRECT on both sides. §2.1.
- **`hcm.access_log_options` — RE-COSTED 9-11** (its "cheaper" adjective REFUTED by the `statusCode == 0` guard). §2.1.
- **`stdout`/`stderr` loggers — RE-COSTED 7-9** (wrong-file cite corrected; the real blocker relocated to the subject side). §2.1.
- **`stats_flush_on_admin` — MODERATE 8-10.** The one prior adjective (53 sites) that SURVIVED, with a qualification the prior costing omitted (52 of 53 are tests). §2.1.
- **Route headers / `prefix_rewrite` / `server_name`+`via` — 7 to 12+**, all newly surfaced, all with EXECUTED cross-side divergences. §2.1.
- **Everything else** — unchanged from phase 73's adjudication, re-confirmed. §2.1.

### 11.1 What was and was NOT verified by execution

**VERIFIED BY EXECUTION this session:** the `errors.As(*tls.CertificateVerificationError)` handshake discriminator (**twice, independently, at TLS 1.2 and TLS 1.3**, with forced client-cert send and the discrimination criterion stated before the run) · the `upstream_cluster` naive AND `NameOnlyEndpoint` variants building and passing 70/70 packages + all 10 tracing fixtures, with cross-side value parity · the reference's SELECTION-gated `upstream_cluster` on the `FI`/`UF`/`UO` arms plus the `direct_response` control · the `access_log[].filter` and `access_log_options` cross-side line-count deltas (2-vs-1, 2-vs-4) and the `filter: {}` reference boot-reject · the `merge_slashes` FORWARDED-path rewrite · the `server_name`/`via` and route-header cross-side divergences · the `prefix_rewrite`/`host_rewrite_literal` rewrites · the 53-site `admin.New` compile census (`-gcflags=all=-e`) · the access-log sink flattening (two files, both got both requests) · the swallowed-panic boot hang (SIGQUIT stack dump).

**NOT VERIFIED, stated plainly:**

- **The reference's `ssl.*` scope, spelling and semantics AT THIS TIP.** §2.4 shows more is in the landed record than the incoming framing claimed — including a verbatim `listener.0.0.0.0_10000.ssl.fail_verify_error: 1` line and the full 6-name roster — but **all of it is DOC-SOURCED from phase 65 and NONE of it was re-executed this session.** A SPEC obligation.
- **Whether `ssl.handshake` counts successes only or all attempts** — inferred (1 across a 3-arm probe), not probed.
- **Whether Go's two error shapes map onto the reference's TLS alerts** (48 `unknown_ca` vs 116 `certificate_required`) — the probe was **Go-server vs Go-client**. D-TLSHS-ALERTMAP.
- **`ssl.ciphers.*` / `ssl.versions.*` feasibility beyond the two known blockers** — the charset result is from a direct `IsValidName` probe; the cross-side mapping infeasibility is REASONED, not probed.
- **⚠️ NO ROW WAS BUILT FOR ROW 74 ITSELF.** The `ssl` seam analysis is a **READ-ONLY derivation plus an ISOLATED stdlib probe** that ran entirely outside this repository's build. **NO probe has confirmed the row compiles in-tree, that the counters register without colliding, or that the classifier fires from `serveConnection`.** Nothing in this document may be cited as execution evidence for the row's buildability. That is the SPEC's and the PLAN's job. *(Phase 73's BRAINSTORM made the same admission and it held; phase 72's carried a scratch build and this one does not.)*
- The QUIC path's handshake-outcome behavior on the reference — entirely unprobed (D-TLSHS-QUIC).

### 11.2 Controller re-derivation of the agents' load-bearing claims

Per `feedback_brief_citations_not_evidence`, the controller RE-OPENED every cite the pick rests on rather than trusting any report. ⚠️ Per `reference_verification_table_launders_wrong_cites`, note that a HOLDS verdict here means *the controller opened that file at that line at tip `de2d7737`* — nothing more.

| claim | controller result |
|---|---|
| synchronous handshake; `err` bound at `manager.go:1178` | **HOLDS** — `:1176` guard, `:1177` `stdtls.Server`, `:1178` `HandshakeContext`, success fall-through `:1183` |
| the two landed listener-scope stat fields | **HOLDS** — `manager.go:175-176` |
| registration + prefix | **HOLDS** — `registerListenerMetrics` `:351`, `prefix := "listener." + normalizeAddr(rt.addr) + "."` `:352`, counters `:353`/`:354` |
| `NamePattern` has no hyphen | **HOLDS** — `internal/stats/registry.go:48`, verbatim |
| duplicate-registration panic text | **HOLDS** — `registry.go:107` |
| ADR-0286 §Context / §Consequences C3 | **HOLDS** — `DECISIONS.md:16890` / `:16908` |
| BEHAVIOR_CONTRACT three names + boundary | **HOLDS** — `:916` / `:928` |
| `0111` drives three arms at `require_client_certificate: true`, has NO `StatsAsserter` | **HOLDS** — `envoy-go.yaml:70`, `README.md:73`, forced-send `driver.go:520`, `BackendCount` `:293`, zero `AssertStats` hits; **and the fixture's OWN README `:161-162` names the gap** |
| `fixture.StatsAsserter` | **HOLDS** — `test/differential/fixture/fixture.go:75` |
| the registration-guard precedent | **HOLDS** — `internal/statssink/registration_test.go:26` |
| ZERO `ssl.*` stats in production | **HOLDS, METHOD CORRECTED** — see §11.3 item 4 |
| `ADR-0286 C3` also claims a `crypto/tls` callback is needed | **REFUTED** — see §11.3 item 1 |

**Two things the controller found independently:**

1. **The Lua `UpstreamCluster()` gap is a THIRD consumer of the `upstream_cluster` framework gap — and it is STRUCTURALLY UNFIXABLE by an endpoint-carried name.** `internal/filter/http/lua/bridge.go:1391-1393` and `:1552-1553` both `return ""`; the adapter holds only `DecoderFilterCallbacks` + `*filter` (`bridge.go:1299-1306`) and runs at DECODE time, **before any pick**. This **CONFIRMS rather than inflates** the runner-up's one-consumer scoping — a `NameOnlyEndpoint` variant would not reach Lua.
2. **`accesslog.Record` has NO `UpstreamCluster` field at all** (grep ⇒ 0), so the access-log half of that gap is a separate, larger problem. Corroborates phase 73's own correction.

### 11.3 Cite drift found and corrected

1. ⚠️ **THE BIG ONE — the `crypto/tls` handshake-error-callback claim is NOT in ADR-0286 C3.** RE-DERIVED: `DECISIONS.md:16908` contains only the *"framework-surgery row of its own … blow the +0-stat envelope"* half. The callback claim lives in **BRAINSTORM prose**: `phases/70-…/BRAINSTORM.md:70` (originating, naming `VerifyPeerCertificate`/`GetConfigForClient`), copied to `phases/71-…/BRAINSTORM.md:62`, and upgraded to *"framework surgery **CONFIRMED**"* at `phases/72-…/BRAINSTORM.md:124`. **A BRAINSTORM adjective acquired ADR authority by proximity across three rows.** Recorded as a documentary finding, not silently fixed.
2. **`internal/xds/bootstrap.go` does not exist.** The `stdout`/`stderr` fall-through is `internal/bootstrap/bootstrap.go` — guard `:952`, comment `:953-954`, `return nil` `:955`. The line numbers were right in the wrong file.
3. **`registry.go:177-186` names `getOrRegister`, not the two `*IfAbsent` functions.** `NewCounterIfAbsent` is `:161`; `NewGaugeIfAbsent` is `:208`; `getOrRegister` spans `:177-188` and carries the ADR-0117 post-Freeze comment at `:175-176`.
4. **"the only `ssl` hits in `internal/` are Lua filenames/comments" needs a WORD-BOUNDARY grep to be true.** A naive `grep -rn 'ssl' internal/` matches **`acce`ssl`og`** on hundreds of lines. With `grep -roE '\bssl\b'` the hits are exactly the Lua bridge's `ssl` method surface (`lua/ssl.go`, `lua/connection.go`, `hcm/connection.go:103`/`:374`, `filter/http/chain.go`, `filter/http/callbacks.go`, `wasm/abi_callbacks.go`) — **ZERO stat names**. The CLAIM survives; the METHOD did not, and a future roller repeating the naive grep would get a badly wrong answer.
5. **`harness.go:257` → `:258`** for `cmd.Stderr = os.Stderr`; and `readyListenerAddrs` is defined at **`:63`** (terminal check `:73`), not the cited `:67-85` span.
6. **`extractEndpoints` is at `internal/cluster/manager.go:875`, not `:863`** — phase 73's IMPL moved it +12. The sole non-test call site `:433` is UNCHANGED. *(`reference_parallel_stream_mints_fresh_drift`: a landed IMPL shifts anchors; re-derive rosters at the current tip.)*
7. **`h2dispatch.go:684` is the `if ln == "server" {` test, not the add-if-absent branch** — that is `:692`, with the flag declared `:673` and set `:685`. A valid anchor, an imprecise one.
8. **`accesslog_emit.go`'s `// not available at this seam` comments are LONGER than quoted** — `:51` continues *"; Task 11 differential is UNasserted on this field"*, `:112`/`:173` *"; UNasserted in differential"*. Quoting a prefix as if it were the whole comment is how a "verbatim" quote drifts (`reference_quoting_is_not_executing`).
9. **`BEHAVIOR_CONTRACT.md:690` says the pick-time fix is "a ~4-line hoist" where the cost agent measured 2 production lines.** NOT adjudicated — recorded as a CONTESTED count per `reference_a_drift_correction_is_itself_a_claim`; whichever row takes it measures at its own tip.
10. **The incoming framing under-stated the landed record on `ssl` SCOPE.** It listed the scope as NOT VERIFIED; `phases/65-…/SPEC.md:270`/`:278`/`:282` carry verbatim listener-scoped probe output and a 6-name roster. Corrected UPWARD in §2.4, and still marked DOC-SOURCED.

### 11.4 Verifier fold — corrections to THIS SESSION'S OWN earlier reasoning

Recorded honestly, because the project's review discipline exists to catch exactly this class and dropping it quietly would defeat the purpose.

1. **⚠️ REFUTED BY RE-DERIVATION — the controller's own mid-session hypothesis that the pick-time `picked`-propagation row would SHRINK the `upstream_cluster` residual from 5 sites to 1, creating an ordering dependency between the two candidates.** It closes **ZERO of the 5**. The 5 span-capable zero-endpoint sites (`connection.go:597`, `:699`, `h2dispatch.go:530`, `h3dispatch.go:280`, `:341`) are `SendLocalReply`/`chain.LocalReplyDone()` branches that pass a **LITERAL `cluster.Endpoint{}`** and never enter `doH1ClusterAction`/`doH2ClusterAction` at all — the only functions the hoist touches. There is no ordering dependency. The hypothesis was plausible, cheap to check, and wrong.
2. **⚠️ PARTLY REFUTED — the FIRST adversarial verdict on the runner-up.** It downgraded the `upstream_cluster` row on the grounds that a FAITHFUL version must pay a **61-call-site thread**. The SECOND verifier's **`NameOnlyEndpoint`** variant reached cross-side PARITY on the acquire-failure classes (`UF` `'c_dead'`, `UO` `'c_cb'`) **for 3 lines, with ZERO test edits and 70/70 packages green**. ⇒ **the runner-up's rejection rests on the LOCAL-REPLY RESIDUAL, not on the threading tax.** Recording the superseded reason matters: a future roller reading only the first verdict would defer the row for a cost that does not exist.
3. **⚠️ The incoming brief's own C3 attribution was wrong** (§11.3 item 1). The controller inherited it, checked it, and found the quoted sentence absent from DECISIONS.md entirely. This is the *"a brief's citations are not evidence"* rule firing on the brief that cites that rule.
4. **⚠️ The `ssl`-grep method was wrong even though its conclusion was right** (§11.3 item 4). A claim can be TRUE and its stated derivation UNSOUND; only the derivation is reusable, so the unsound one had to be replaced rather than left standing.

**What NO ONE could check, stated plainly:** **no probe of any kind was run against the reference for row 74's own subject this session.** Every D-TLSHS-* PROBE item is open, including the two that decide the fixture's assertability (SCOPE, SEMANTICS) and the one that could invalidate the classifier's cross-side meaning (ALERTMAP). And **no build of row 74 exists**, in-tree or in scratch (§11.1).

---

## 12. Stage-close mechanics (this BRAINSTORM; the CONTROLLER executes these, not this document)

- **Row 74 registered `in-progress`** in ROADMAP §Schema at the stage-close commit (a new subject re-opens sentinel check (1)). Flips `done` at the phase-74 IMPL six-gate (ADR-0106, the SOLE leg — a SINGLE FLAT ROW, `reference_roadmap_split_phase_row_done`).
- **ROADMAP takes a SECOND edit at the same commit: a phase-74 charter sentence APPENDED to the `### Observability family` heading paragraph** (`ROADMAP.md:199` is the heading; the family paragraph is `:203`) — naming the row, its **+3-stat** envelope, the REFUTED 9-row-old adjective and its MISATTRIBUTION, the family SPLIT, the newly-surfaced deferred candidates, and that the family STAYS OPEN. Declared here because declared mechanics must match performed mechanics — the phase-70/72/73 paired-edit precedent. The edit is ADDITIONS ONLY and the live Observability **deferred sentence is BYTE-UNCHANGED**; sentinel check (2) must still return exactly **3**.
- **NO deferred sentence is narrowed at ANY pre-IMPL stage** (the phase-57 precedent) — narrowing early breaks sentinel check (2)'s meaning. The `ssl` clause **SHORTENS** (three sub-names roll out; `ciphers`/`versions` SURVIVE) at the **phase-74 IMPL only**, and check (2) must **STAY 3**. ⚠️ Unlike phase 73's hazard, this one was checked at the BRAINSTORM by reading the live sentence (§6): the clause is the FIRST of two `+`-joined members and its parenthetical retains two sub-names, so the sentence stays well-formed with ≥1 candidate. **Re-confirm at the IMPL anyway; a read is not a diff.**
- **DECISIONS UNTOUCHED** (ADR-0296 §Context drafts at the SPEC per ADR-0044).
- **STATE §Current rolled IN PLACE** (lifecycle DONE → 1; lineage re-capped **with its PREAMBLE updated** — the ADR-0288 rule; a stale preamble was a REAL defect caught during phase 72). Never prepend a new block above §Current.
- **next-prompt.txt rolled to the phase-74 SPEC** (TRACKED despite `.gitignore`; edit it in the stage worktree — `reference_next_prompt_tracked_despite_gitignore`).
- **Sentinel re-run MECHANICALLY TWICE** — once in the worktree at close, once on LANDED master after the squash-push (does NOT fire; `stop` NOT created): (1) prints `NOT DONE: row 74` (the registration re-opens check (1)); (2) `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:'` ⇒ **3**; (3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`. Checks (2)+(3) print ⇒ abundant work remains. (Check (3)'s `WASM` line is the bookkeeping artifact documented in §2.1 — left AS-IS.)
- **The adversarial record is FOLDED, not deferred** (§11.4). This stage's corrections to its own reasoning — the refuted ordering hypothesis, the partly-refuted first verdict, the inherited C3 misattribution, the unsound `ssl` grep — are folded at their points of use with the ledger in §11.4, per the obligation phase 73's §12 established.
- **Next → the phase-74 SPEC** (the D-TLSHS-* live-probe arms against `envoyproxy/envoy:contrib-v1.37.2` — **SCOPE, SEMANTICS, REGSCOPE and ALERTMAP are the four that matter**; RE-DERIVE every §6 edit site at the SPEC tip; RE-RUN the `errors.As` discriminator at the SPEC's own toolchain; draft ADR-0296 §Context including the C3 documentary correction).
