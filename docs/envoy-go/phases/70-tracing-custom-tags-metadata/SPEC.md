# SPEC 70 — tracing `custom_tags` `metadata` type, `REQUEST` MetadataKind only (the SIXTEENTH Observability-family row + the FOURTH tracing `custom_tags` row + the LAST `CustomTag` source type; lifts the `custom_tags metadata type unsupported` reject at `internal/tracing/config.go:214-215` and resolves a `REQUEST`-kind dynamic-metadata value out of the LANDED `internal/dynamicmetadata.Bucket` onto the ingress span via the shared `ResolveCustomTags`→`Span.Attrs` seam [both OTLP + Zipkin]; `ROUTE`/`CLUSTER`/`HOST`/unset-kind reject loudly with distinct substrings [envoy-go-strict, ADR-0080]; a SINGLE FLAT ROW; ONE new OTLP fixture `0114`)

> **Stage:** SPEC (lifecycle-state 1 → 2). Docs-only — ZERO production `.go`, apart from the ADR-0292 §Context append to `DECISIONS.md`. Worktree `.worktrees/phase-70-spec`, branch `phase-70-tracing-custom-tags-metadata-spec`, tip `9338507a` (the phase-70 BRAINSTORM squash), per `feedback_git_worktrees`.
>
> **Row 70 STAYS `in-progress`** — the IMPL flips it `done` at its six-gate (ADR-0106, the SOLE leg — a SINGLE FLAT ROW, §10). **ADR-0292's §Context DRAFTS HERE** (ADR-0044-as-used); §Decision/§Consequences land at the IMPL IN PLACE. **The DECISIONS tail flips ADR-0291 → ADR-0292 AT THIS SPEC COMMIT** (§14) — the §Context append IS the tail flip; next-free becomes ADR-0293.
>
> **Evidence base — code RE-DERIVED at `9338507a` this SPEC** (an independent read-only re-derivation agent re-opened EVERY anchor in BRAINSTORM §6/§11 + the go-control-plane `CustomTag_Metadata`/`MetadataKey`/`MetadataKind` proto types + the PGV `.validate.go` rules + the fuzz host + the collision greps + the load-bearing `Bucket`-seam threading) **+ SIX EXECUTED live-probe arms this SPEC** (the D-CTM docket — `envoyproxy/envoy:contrib-v1.37.2`, fresh container per arm, a Lua-set dynamic-metadata WRITER served-this-arm, a standalone host OTLP-gRPC + Zipkin-HTTP receiver, decode-actually-ran asserted per arm — 6 numbered conclusions "P1..P6"). Claims below are attributed **EXECUTED** (a P-arm conclusion, named) or **RE-DERIVED** (a declaration reading with `file:line` at `9338507a`); a reading is never presented as a probe. Unlike SPEC-69 (which resolved on the BRAINSTORM's already-run Dossier P), **the phase-70 BRAINSTORM ran NO probes — the D-CTM wire/serialize/default/pathwalk/reject arms were genuinely OWED at this SPEC and were EXECUTED here.** **RE-DERIVE this document; do not execute it** (`feedback_brief_citations_not_evidence`, `reference_quoting_is_not_executing`).
>
> **Baselines re-verified mechanically at `9338507a`:** fixtures **115** (`ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0113-stats-sink-otlp-knobs` ⇒ next fixture `0114`; tracing/OTLP fixtures TEMPLATE all ports — no in-container port to pick, C5) · fuzzers **55** (`grep -rn '^func Fuzz' --include='*.go' internal/ test/ | wc -l`) · DECISIONS tail **ADR-0291** (COMPLETE; next-free **ADR-0292**; `grep -c '^## ADR-0292'` → 0, FREE) · BackendKind tail **38** · stat surface **1201** · go.mod modules **2** (lineage figure; `structpb` + `type/metadata/v3` already resolve at the direct `go-control-plane/envoy v1.32.4` and the transitive `google.golang.org/protobuf`).
>
> **Sentinel expectation at this stage:** check (1) prints `NOT DONE: row 70` (registered `in-progress` at the BRAINSTORM stage-close); checks (2)+(3) unchanged (three live `candidates:` sentences; `NEVER OPENED: gRPC/Runtime/WASM`). **No deferred sentence is edited at ANY pre-IMPL stage of this row** — `tracing custom_tags (`metadata`)` stays in the live Observability `candidates:` sentence and rolls OUT at the phase-70 IMPL row-done edit, per the verified phase-57 graphite precedent (§12).

---

## 1. Purpose / Mission

Phase 46 built the HCM-native tracing engine — the per-request ingress SERVER `Span` with its built-in STRING attributes, exported over BOTH the OTLP (`OTLPExporter`) and Zipkin (`ZipkinExporter`) transports through the shared provider-neutral `Span.Attrs []KV` seam (ADR-0260/0261). Phases 59/62/63 added the first three `custom_tags` SOURCE types — `literal` (ADR-0277), `request_header` (ADR-0283), `environment` (ADR-0284) — each lifting one strict-reject arm in `parseCustomTags` and resolving per-request into `Span.Attrs` via the ordered, first-wins-deduped `[]CustomTagSpec` + `ResolveCustomTags(specs, headerLookup) []KV` pipeline threaded at the three `accesslog_emit.go` emit sites (H1/H2/H3). Phase 64 (ADR-0285) added the `max_path_tag_length` numeric knob.

Phase 70 lands the **FOURTH tracing `custom_tags` row + the LAST `CustomTag` SOURCE type**: the **`metadata`** type, scoped to the **`REQUEST`** `MetadataKind`. It lifts the wholesale `custom_tags metadata type unsupported` reject (`config.go:214-215`) and resolves an operator-configured `{tag, metadata:{kind:REQUEST, metadata_key:{key:<ns>, path:[<seg>...]}, default_value:<fb>}}` out of the request's **dynamic filter metadata** — envoy-go's landed `internal/dynamicmetadata.Bucket` (`Get(filterName, key) (*structpb.Value, bool)`, written in production by the Lua filter's `dynamicMetadata():set` and the mongo-proxy filter) — walks the `MetadataKey.path`, serializes the resolved `structpb.Value` to a STRING, and emits it as a `{tag, value}` span attribute on BOTH exporters. The other three MetadataKinds (`ROUTE`/`CLUSTER`/`HOST`) **and** an unset kind reject loudly with distinct substrings (envoy-go-strict, ADR-0080 — the reference ACCEPTS them, so this is a real DEPARTURE, proved by P6). It composes on FULLY-LANDED substrate: **ZERO new packages, +0 go.mod modules, +0 stats, +0 BackendKinds, +0 fuzzers** (a `FuzzHCMConfigParse` seed). New exported symbols are confined to `internal/tracing` (the `ResolveCustomTags` signature grows a `metaLookup` param; the `CustomTagSpec` struct grows fields) **plus ONE new accessor `func (c *FilterChain) DynamicMetadata()` on `internal/filter/http`** (the load-bearing SPEC correction — C1). The `internal/xds` zero-new-symbol discipline is UNTOUCHED (xds not touched).

### 1.1 BRAINSTORM anticipations — what HELD, what this SPEC CORRECTS *(the drift ledger; drift found is the point)*

**HELD (re-derived / EXECUTED-probed this session):** the `metadata` reject at `config.go:214-215` + the `parseCustomTags` switch (`:194-218`) + the `CustomTagSpec` struct (`:63-71`) + the `customTagKind` constants (`:52-56`) + the empty-tag reject (`:190-192`) + first-wins dedup (`:219-223`) — RE-DERIVED exact. The go-control-plane v1.32.4 proto shape (D-CTM-PROTO): `CustomTag_Metadata{Kind *MetadataKind; MetadataKey *MetadataKey; DefaultValue string}` (`custom_tag.pb.go:326`), `MetadataKind` oneof (`GetRequest`/`GetRoute`/`GetCluster`/`GetHost`, `metadata.pb.go:164-185`; each an empty marker msg), `MetadataKey{Key; Path []*MetadataKey_PathSegment}` (`:47`), `MetadataKey_PathSegment` KEY-ONLY (`GetKey`, `:276`) — RE-DERIVED exact, matching the BRAINSTORM. **D-CTM-WIRE / D-CTM-ZIPKIN-WIRE HELD (P1/P2):** the reference emits `{key = the LITERAL `tag` name (NOT namespaced), value = the serialized string}`, APPENDED AFTER all built-ins on the OTLP span, and as the same key/value in the Zipkin v2 JSON `tags` map — i.e. the EXISTING `Span.Attrs` upsert seam already produces exactly this shape. The `REQUEST`-only scope with loud rejects, +0 stats, the Lua-filter cross-side-deterministic WRITER, the fuzz-SEED posture, and the SINGLE FLAT ROW all HELD. `ResolveCustomTags`/`Span.Attrs`/`KV`/both exporters are the correct seams.

**CORRECTED — each flagged where it lands:**

| # | BRAINSTORM/router said | Corrected to | § |
|---|---|---|---|
| **C1** | **D-CTM-BUCKET-SEAM:** the per-request `Bucket` is reachable via `chain.DynamicMetadata()` (`chain.go:837`/`:963`) — thread it to the emit (BRAINSTORM §2.5/§6) | **The `:837`/`:963` accessors are on `decoderCB`/`encoderCB`, NOT on `*FilterChain`.** `grep 'func (c *FilterChain) DynamicMetadata'` → **ZERO**; `*FilterChain` has only `SetDynamicMetadata` (`:1014`) + the unexported `dynamicMetadata` field (`:274`). **DECISION: ADD a new accessor `func (c *FilterChain) DynamicMetadata() *dynamicmetadata.Bucket { return c.dynamicMetadata }` to `chain.go`** — a NEW exported symbol on `internal/filter/http` (mirroring the two callback accessors). Nil-tolerant (`Bucket.Get` is nil-receiver-safe, `dynamicmetadata.go:33`); timing-safe (`defer chain.Destroy()` runs AFTER the final emit). This is the one load-bearing envelope correction: the row adds one export OUTSIDE `internal/tracing`. | §3.5 |
| **C2** | **D-CTM-DEFAULT:** the `default`/omit semantics follow the `environment` omit-iff-resolved-empty precedent (present-empty OMITS, `resolve.go:36-52`) (BRAINSTORM §2.3) | **The metadata semantics follow the `request_header` pattern, NOT `environment` (P4).** P4-iii: a PRESENT-but-EMPTY metadata value (`set(...,"")`) EMITS `""` and does NOT fall to the default. P4-i: absent + non-empty default → emit default. P4-ii: absent + empty/omitted default → OMIT. This is EXACTLY `kindRequestHeader`'s rule (`resolve.go:23-35`, `HasDefault = DefaultValue != ""`): "resolve if the path resolves (even to `""`), else default if non-empty, else omit". So the spec sets `HasDefault = DefaultValue != ""` and the resolve arm mirrors `kindRequestHeader`, NOT `kindEnvironment`. | §3.4 |
| **C3** | (unmentioned) — the path-walk was to be freshly written (BRAINSTORM §2.3 D-CTM-PATHWALK) | **A landed precedent exists: `internal/filter/http/ratelimit/descriptors.go` `resolveMetadataValue` (`:1057`) + `descendStructpbValue` (`:1116`)** already resolve a `MetadataKey`→`Bucket` for the DYNAMIC source (`filterName = mk.GetKey()`, `Bucket.Get(ns, path[0])`, descend `path[1:]` via `GetStructValue().Fields`, terminal `Value_StringValue`). The tracing resolve arm CLONES this descent SHAPE (NOT an import — `internal/tracing` must not depend on a filter package; a ~15-line local walk), but DIVERGES on the terminal: ratelimit requires a STRING terminal, tracing must SERIALIZE non-string too (C4). | §3.3 |
| **C4** | **D-CTM-VALUE-SERIALIZE:** pin the STRING case; "name non-string as a documented boundary if non-trivial" (BRAINSTORM §2.3) | **Non-string is WELL-DEFINED and implemented, not deferred (P3).** The reference renders the resolved `google.protobuf.Value` as: string→raw (unquoted); number→canonical decimal (`42`, `3.14`); bool→`true`/`false`; struct/list→COMPACT JSON (`{"a":"b"}`, `["x","y","z"]`). **DECISION: implement to MATCH** (deferring would force either a silent divergence — ADR-0080 violation — or a runtime tag-drop, since the value is DYNAMIC and cannot be parse-rejected). String is special-cased to `GetStringValue()`; non-string via `protojson.Marshal` + `json.Compact` (strip the detrand whitespace to match the reference's compact form — `reference_detrand_race_catches_protojson_value_substring`). The FIXTURE proves the STRING case cross-side; non-string is UNIT-tested against the P3 table (not cross-side fixture-asserted), with number-precision edges + `NullValue` named as documented boundaries. | §3.3 |
| **C5** | **D-CTM-FIXTURE:** pick the next free in-container listener port (BRAINSTORM §2.6/router) | **REFUTED — no port to pick.** The tracing/OTLP fixtures (`0102`/`0105`/`0106`/`0087`) TEMPLATE every port (`{{.AdminPort}}`/`{{.ListenerPort}}`/`{{.BackendPort}}`/`{{.OTLPPort}}`) via the harness. A cloned metadata fixture inherits the templated ports; there is no hardcoded `l_test port_value` to increment. Drop the "next free port" line. | §8 |
| **C6** | **D-CTM-REJECT:** the reject substrings + "envoy-go's own reject vs the reference's PGV boot-reject" (BRAINSTORM §2.4) | **Sharpened by P6 + the PGV `.validate.go` re-derivation.** The reference does NOT PGV-boot-reject an UNSET kind (no `required` rule) nor an ABSENT `metadata_key` (nil-guarded) — it BOOTS all of `route`/`cluster`/`host`/unset-kind (P6). So envoy-go's `ROUTE`/`CLUSTER`/`HOST`/unset-kind reject is an envoy-go-strict **DEPARTURE** (real — the reference boots where envoy-go rejects). But the reference PGV DOES boot-reject an empty `metadata_key.key` (`min_len 1`) and an empty `path` (`min_items 1`) — so envoy-go's empty-namespace/empty-path reject is **PARITY** (both reject). Distinguish the two in BC + the ADR. | §3.6 |

### 1.2 SPEC-time verification record

The read-only re-derivation agent (tree at `9338507a`) re-opened all BRAINSTORM §6/§11 anchors + the proto/PGV/fuzz/collision anchors + traced the `Bucket`-seam threading across all 18 emit call sites: **one design-material correction (C1 — the missing `*FilterChain` accessor, an added export outside `internal/tracing`); four sharpenings (C2 default-semantics, C3 the ratelimit precedent, C4 non-string serialization, C6 the PGV departure/parity split); one refutation (C5 the port premise).** No claim was left on an un-re-derived citation. The six live-probe arms (P1..P6) were EXECUTED against `contrib-v1.37.2` with a served-this-arm Lua writer and a decode-ran assert per arm; every SPEC behavioral assumption HELD (tag key = literal name; value = serialized string appended after built-ins; both exporters; default emitted when absent+non-empty; omit when unresolvable+empty-default; present-empty emits `""`; multi-segment path-walk works; non-REQUEST kinds boot on the reference). **Adversarial-pass record (TWO independent verifiers ran against the draft in PRIVATE scratch before landing, `reference_parallel_subagents_private_scratch`):** **V1 — code-claims re-derivation + a DECISIVE by-execution probe of the load-bearing C4 serialization claim.** V1 independently re-derived every load-bearing claim at tip and RAN a standalone Go program marshaling a `*structpb.Value` for each P3 type via `protojson.Marshal` + `json.Compact`: string→`"x"` WITH quotes (so string MUST be special-cased to `GetStringValue()`); `42`→`42`; `3.14`→`3.14`; `true`→`true`; `{a:b}`→`{"a":"b"}`; list raw→`["x", "y", "z"]` (SPACES after commas) → `json.Compact` → `["x","y","z"]` — so **`json.Compact` is genuinely load-bearing and the P3 table matches exactly** (C4 CONFIRMED by execution). V1 also CONFIRMED C1 (the missing `*FilterChain` accessor; the proposed one is buildable, nil-safe, no Destroy race — dispatch is single-goroutine and Destroy is deferred), C2 (the `request_header` present-empty-emits-`""` rule, `resolve.go:23-35`), C3 (the ratelimit clone + the filter-free cycle guard — `go list -deps ./internal/tracing` has no filter edge), C6 (the PGV parity/departure split), and the +0-module envelope (`structpb`/`protojson`/`type/metadata/v3` all already in `internal/tracing`'s dep closure). **ZERO SEVERE, ZERO MODERATE; two MINOR** — the caller-site count was off by one (18 sites, not 17; H2 has 6 not 5 — the line-lists were complete, only the summary count words drifted) and one illustrative-pseudocode line was not valid Go; both folded in here. **V2 — process/consistency/adjudication:** all nine stage-close mechanics PASS (row 70 stays `in-progress`; the DECISIONS tail flip to ADR-0292 PROPOSED with §Context-only; no deferred sentence narrowed [3 `candidates:` sentences, the metadata candidate still present]; the sentinel does NOT fire; STATE.md §Current rolled IN PLACE with singleton pointers + five lineage bullets; the router rolled to the PLAN; the envelope docs-only; the counts consistent across all four docs; the C1 correction consistently reflected with the envelope exception acknowledged everywhere). **ZERO FAILs.** No SEVERE or MODERATE across either pass; the design direction — a `REQUEST`-kind metadata custom tag on the landed dynamic-metadata `Bucket`, single flat row — is unchanged from the BRAINSTORM.

---

## 2. Non-purposes (deferred; per BRAINSTORM §1.2 + §8)

NOT the `ROUTE`/`CLUSTER`/`HOST` MetadataKinds (rejected loudly — §3.6; their per-object route/cluster/host metadata is not exposed at the span-emit seam, and only the `REQUEST` dynamic-metadata `Bucket` is landed). NOT any other tracing follow-on (`spawn_upstream_span` / `http_service` / force-trace — each larger, all loud-rejects today). NOT a change to `max_path_tag_length` (orthogonal, untouched). NOT the 4 EMPTY built-in span attributes (`upstream_cluster`/`node_id`/`zone`/`peer.address`, the `reference_tracing_upstream_cluster_framework_gap` framework gap — a `REQUEST`-kind metadata tag is populated from the SEPARATE, landed per-request `Bucket`; do NOT conflate). NOT the `ssl` handshake-outcome stat family (framework surgery, ADR-0286 C3). NOT any new provider/transport/stat/package/module. NOT a full non-string-value serialization guarantee beyond the P3-pinned common types (number-precision edges + `NullValue` are documented boundaries — §3.3). NOT a separate Zipkin fixture (the `Span.Attrs` seam is shared and the OTLP fixture + unit tests prove both exporters — §8, the 59/62/63 single-OTLP-dir precedent).

---

## 3. The change — the D-CTM-* docket disposed one-for-one

### 3.1 D-CTM-SCOPE / D-CTM-PROTO — the `REQUEST`-only slice on the exact v1.32.4 proto *(RE-DERIVED)*

`CustomTag.GetMetadata()` (`custom_tag.pb.go:113`) returns `*CustomTag_Metadata{Kind *metadatav3.MetadataKind, MetadataKey *metadatav3.MetadataKey, DefaultValue string}`. Kind detection: `md.GetKind() == nil` → unset (reject); else `md.GetRequest() != nil` → REQUEST (accept); `GetRoute`/`GetCluster`/`GetHost` non-nil → the three departures (reject). `MetadataKey.GetKey()` is the `Bucket` namespace; `MetadataKey.GetPath()` is `[]*MetadataKey_PathSegment`, each KEY-ONLY (`GetKey()` — the sole oneof arm; `metadata.pb.go:287`). The import `metadatav3 "github.com/envoyproxy/go-control-plane/envoy/type/metadata/v3"` is ALREADY the repo convention (`internal/filter/http/ratelimit/*`); `config.go` gains this named import beside the existing `tracingv3` (`config.go:12`). All in the direct `go-control-plane/envoy v1.32.4` — **+0 go.mod modules**.

### 3.2 D-CTM-CONFIG-SEAM — extend `CustomTagSpec`; a `kindMetadata` accept + three-plus-one reject arms *(RE-DERIVED)*

`config.go`: add `kindMetadata` to the `customTagKind` iota block (`:52-56`), and extend `CustomTagSpec` (`:63-71`) with `MetaNamespace string` (= `metadata_key.key`) + `MetaPath []string` (the extracted segment keys; `path` has `min_items 1` so `MetaPath[0]` always exists) — reusing `DefaultValue` + `HasDefault` (set `HasDefault = DefaultValue != ""`, the `kindRequestHeader` idiom, C2). The `parseCustomTags` metadata arm (replacing `:214-215`):
- `md := ct.GetMetadata()`; on `md.GetKind()`:
  - **unset** (`GetKind() == nil`) → reject `tracing: custom_tags metadata kind required`.
  - **REQUEST** (`md.GetRequest() != nil`) → validate `mk := md.GetMetadataKey()`: `mk.GetKey() == ""` → reject `tracing: custom_tags metadata tag %q empty namespace`; `len(mk.GetPath()) == 0` → reject `tracing: custom_tags metadata tag %q empty path`; each `seg.GetKey() == ""` → reject `tracing: custom_tags metadata tag %q empty path segment`. Then build `CustomTagSpec{Key: tag, Kind: kindMetadata, MetaNamespace: mk.GetKey(), MetaPath: [seg.GetKey()...], DefaultValue: md.GetDefaultValue(), HasDefault: md.GetDefaultValue() != ""}`.
  - **ROUTE** → reject `tracing: custom_tags metadata route kind unsupported`.
  - **CLUSTER** → reject `tracing: custom_tags metadata cluster kind unsupported`.
  - **HOST** → reject `tracing: custom_tags metadata host kind unsupported`.
All substrings ADR-0080-distinct. The empty-namespace/empty-path/empty-segment rejects are PGV-PARITY (the reference boot-rejects them too, C6); the ROUTE/CLUSTER/HOST/unset-kind rejects are envoy-go-strict DEPARTURES (the reference boots them, C6/P6). First-wins dedup (`:219-223`) is UNCHANGED (a later same-key metadata tag drops after structural validation).

### 3.3 D-CTM-PATHWALK + D-CTM-VALUE-SERIALIZE — the `kindMetadata` resolve arm *(RE-DERIVED C3; EXECUTED P3/P5)*

`resolve.go`: `ResolveCustomTags` grows a nil-tolerant `metaLookup func(ns, key string) (*structpb.Value, bool)` third parameter (mirroring `headerLookup`'s nil-tolerance) + a `kindMetadata` arm:
```
var v *structpb.Value; var ok bool
if metaLookup != nil { v, ok = metaLookup(spec.MetaNamespace, spec.MetaPath[0]) }  // nil-tolerant, like headerLookup
if ok { v, ok = descend(v, spec.MetaPath[1:]) }       // nested StructValue walk (C3 clone)
if ok {
    if str, emit := structpbValueToString(v); emit {  // string→raw; non-string→compact JSON (C4)
        out = append(out, KV{Key: spec.Key, Str: str}); continue
    }
}
if spec.HasDefault { out = append(out, KV{Key: spec.Key, Str: spec.DefaultValue}) } // else omit
```
- **`descend(v, segs)`** (the C3 clone of `descendStructpbValue`, `[]string` variant): for each seg, `st := v.GetStructValue(); if st==nil → (nil,false)`; `v = st.Fields[seg]; if v==nil → (nil,false)`; return `(v, true)`. An unresolvable segment (P5 `[outer, nope]`) → `false` → treated as absent → default/omit.
- **`structpbValueToString(v)`** (C4): `*structpb.Value_StringValue` → `k.StringValue` (raw, even `""` → `("", true)`, the present-empty EMIT of P4-iii); `*structpb.Value_NullValue` → `("", false)` (a documented boundary — untested by P3; treat as unresolvable → default/omit); else `protojson.Marshal(v)` then `json.Compact` → `(compact, true)` (number `42`/`3.14`, bool `true`/`false`, struct/list compact JSON — P3; number-precision edges are a documented boundary).
The present-empty `""` EMITS (P4-iii, C2); absent/unresolvable → `HasDefault` default else omit (P4-i/ii). `resolve.go` gains the `structpb` + `protojson` + `encoding/json` imports (all existing-module).

### 3.4 D-CTM-DEFAULT — the `request_header` default rule, NOT `environment` *(EXECUTED P4; the C2 correction)*

Pinned by P4 (§1.1 C2): resolve-if-resolved (present-empty `""` emits `""`); else `default_value` if non-empty; else omit. `HasDefault = DefaultValue != ""` at parse; the resolve arm structurally mirrors `kindRequestHeader` (`resolve.go:23-35`), NOT `kindEnvironment`'s omit-iff-resolved-empty. A `resolve_test.go` matrix pins all four P4 arms + the P5 unresolvable-path→default edge, each an independent `Errorf` (`reference_fatalf_makes_assertions_unreachable`).

### 3.5 D-CTM-BUCKET-SEAM — a NEW `*FilterChain.DynamicMetadata()` accessor + a `metaLookup` thread at the emit sites *(the C1 correction; RE-DERIVED across all 18 call sites)*

The three emit methods `emitAccessLog`/`emitAccessLogH2`/`emitAccessLogH3` (`accesslog_emit.go`, `ResolveCustomTags` at `:55`/`:116`/`:177`) grow a nil-tolerant `metaLookup func(ns, key string) (*structpb.Value, bool)` param passed straight to `ResolveCustomTags`. Each caller builds it from `chain.DynamicMetadata().Get` (the NEW accessor, C1 — a nil-safe method value). **KEY SIMPLIFIER (re-derived):** the span block fires ONLY when `traceDecision != nil && traceDecision.Sample` (`:28`/`:87`/`:149`), and EVERY no-chain caller (the 404-no-match sites `connection.go:330` / `h2dispatch.go:313` / `h3dispatch.go:130`, which return/synthesize BEFORE the `chain` is built) passes `traceDecision == nil` — so `ResolveCustomTags` is provably NEVER reached there; those three sites pass `nil` for `metaLookup`. Every span-capable caller has `chain` in scope (built at `connection.go:350` / `h2dispatch.go:327` / `h3dispatch.go:148`). The 15 chain-in-scope callers pass `chain.DynamicMetadata().Get`; the 3 no-chain callers pass `nil`. `accesslog_emit.go` gains a `structpb` import (the param type). This leaks NO `*Bucket` type through the emit signatures (only `*structpb.Value`, already transitively present) and reuses the landed `headerLookup` threading idiom. The full caller roster (§11) is applied MECHANICALLY at the IMPL.

### 3.6 D-CTM-REJECT — the departure/parity split *(EXECUTED P6; RE-DERIVED the PGV `.validate.go`)*

P6 (via `--mode validate` on `contrib-v1.37.2`): `kind:{route:{}}` / `{cluster:{}}` / `{host:{}}` / kind-OMITTED all **BOOT-OK** on the reference (kind is not PGV-`required`) — so envoy-go's loud reject of ROUTE/CLUSTER/HOST/unset-kind is a real envoy-go-strict DEPARTURE (ADR-0080). Empty `metadata_key.key` → reference PGV-REJECT (`min_len 1`); empty/omitted `path` → reference PGV-REJECT (`min_items 1`) — so envoy-go's empty-namespace/empty-path reject is PARITY. (An entirely-ABSENT `metadata_key` is NOT PGV-rejected by the reference — nil-guarded — but yields `key==""` for envoy-go, which rejects it under the empty-namespace arm; a minor strict departure on that one shape, named in BC.) BEHAVIOR_CONTRACT records the two categories distinctly. `config_test.go` pins each reject substring; the departures are SUBJECT unit tests (the reference boots them, so they are NOT cross-side-informative — no fixture arm).

---

## 4. Framework primitives — 0 new packages, 0 new go.mod modules

No new seam, applier, resource type, or wire shape. **REUSES verbatim:** the phase-46 `Span`/`Span.Attrs []KV`/`KV` (`span.go:11-17`)/`BuildServerSpan`/`upsertAttr` + BOTH exporters (one `Attrs` change covers OTLP + Zipkin) · the phase-59/62/63 `parseCustomTags` (the parse home + first-wins dedup) + `ResolveCustomTags` (the resolve home) + the three `accesslog_emit.go` emit sites · the landed `internal/dynamicmetadata.Bucket.Get` (the `REQUEST`-kind source; nil-tolerant) · the Lua filter (`lua/metadata.go` `dynamicMetadataSet`, `:309-317`) as the cross-side-deterministic fixture WRITER · the ratelimit `descendStructpbValue` SHAPE (`descriptors.go:1116`, CLONED not imported — layering) · `FuzzHCMConfigParse` (the fuzz host). **NEW exports:** `func (c *FilterChain) DynamicMetadata()` on `internal/filter/http` (C1) + the `ResolveCustomTags` signature growth + `CustomTagSpec` fields + the `kindMetadata` const + the `descend`/`structpbValueToString` helpers (all in `internal/tracing`). **+0 go.mod modules:** `structpb` is already an `internal/dynamicmetadata` dep; `type/metadata/v3` + `protojson` resolve at existing directs; `go mod tidy -diff` anticipated EMPTY (re-checked at the IMPL, `reference_new_subpackage_pulls_transitive_module`). **Cycle guard STANDS:** `internal/tracing` gains NO import of a filter package (the `descend` clone keeps it self-contained); `internal/filter/http` already imports `dynamicmetadata`; re-verify `go list -deps ./internal/tracing` (no `...`) at the IMPL.

## 5. Identifier hygiene *(collision checks — RE-DERIVED at `9338507a`)*

`.go` grep repo-wide → **ZERO hits** for `kindMetadata` / `MetaNamespace` / `MetaPath` / `metaLookup` / `structpbValueToString` (the `descend` name is PLAN-chosen to avoid the ratelimit `descendStructpbValue`, which is a DIFFERENT package — no collision, but the PLAN re-greps its final name, `reference_spec_drafted_identifier_collision_check`). `metadatav3` is the ESTABLISHED alias (ratelimit) — adopt it, no collision (distinct package from `tracingv3`). `func (c *FilterChain) DynamicMetadata` → ZERO (only `SetDynamicMetadata` exists, C1). Fixture `0114-tracing-custom-tags-metadata` does not exist (tail `0113`); no port to claim (C5).

## 6. Reject roster — added / retained

**No existing reject is retired or flipped.** The wholesale `metadata type unsupported` reject (`:214-215`) is REPLACED by the accept-REQUEST + four distinct-substring rejects (§3.2/§3.6): metadata-kind-required (unset), metadata-{route,cluster,host}-kind-unsupported (DEPARTURES), and the empty-namespace / empty-path / empty-path-segment structural rejects (PARITY). All SUBJECT unit tests (the reference boots the departures, so they are not cross-side-informative). `literal`/`request_header`/`environment` arms + the empty-tag + typeless rejects are UNCHANGED. Roster byte-diffed at the IMPL (`reference_lifted_reject_hidden_enforcement` — confirm the lifted `metadata` reject silently enforced nothing else; it did not — it was a pure wholesale-unsupported guard).

## 7. Stat surface **+0** · Fuzz **+0 fuzzers, SEED only, dispatch-verified** *(D-CTM-STATS / D-CTM-FUZZSEED)*

**Stats: +0; surface stays 1201.** A span attribute is emitted on the wire, not registered. No `TestNoNewStat` guard is added (tracing has no per-tag stat surface; the phase-59/62/63 custom_tags rows added none).

**Fuzz: +0 fuzzers; count stays 55.** The metadata accept + four reject arms are exercised by a SEED to `FuzzHCMConfigParse` (`fuzz_test.go:28` — there is NO dedicated `internal/tracing` config fuzzer, confirmed): an `mkHCM`-built HCM with a tracing block carrying an ACCEPTED `REQUEST` metadata tag (`Kind:MetadataKind_Request_`, `MetadataKey{Key, Path:[{Key}]}`) + a REJECTED `ROUTE`-kind tag, marshaled to Any, `f.Add(typeURL, value)` — the phase-59/62/63 seed pattern (`withCustomTags`/`withReqHeaderTags`/`withEnvTags`). **Dispatch-verify (the named trap):** confirm the seed REACHES the `parseCustomTags` metadata arm (not an earlier provider/typeURL reject) — the custom_tags loop runs BEFORE the provider check (`config.go:122` precedes `:130`), so a well-formed HCM tracing block reaches it. Reconcile `grep -rn '^func Fuzz' internal/ test/ | wc -l` == 55 BEFORE and AFTER (`reference_fuzzer_count_docs_drift`).

## 8. Differential fixture — ONE new `0114-tracing-custom-tags-metadata` (OTLP) *(D-CTM-FIXTURE — DECIDED: ONE fixture, TWO custom_tags in one config)*

**DECISION: ONE new OTLP fixture** (the 59/62/63 single-OTLP-dir precedent; the `Span.Attrs` seam is shared so the OTLP fixture + unit tests prove BOTH exporters — no separate Zipkin dir, §2). Cloned from `0106-tracing-custom-tags-environment` (itself from `0087-tracing-otlp`): an H1 listener → HTTPFixedBody backend, HCM OTLP tracing provider → the driver-owned `test/helpers/otlptrace.Server` receiver, `random_sampling: 100`. Added: a **Lua http_filter** (BEFORE the router, on BOTH `envoy.yaml` + `envoy-go.yaml`) whose `envoy_on_request` calls `handle:streamInfo():dynamicMetadata():set("<ns>", "<key>", "<fixed-string>")` (a FIXED cross-side-identical value — envoy-go's `lua/metadata.go` and the reference `envoy.filters.http.lua` share the `set(namespace,key,value)` API, F8), and **TWO `metadata` custom_tags** proving both P-arms in ONE config:
- **`meta_hit`** — `metadata_key{key:<ns>, path:[{key:<key>}]}`, `default_value:"<unused>"` → resolves to the Lua-set fixed string (P1).
- **`meta_default`** — `metadata_key{key:<ns>, path:[{key:"<unset-key>"}]}`, `default_value:"<fallback>"` → the path is unset → emits the `default_value` (P4-i).

**Cross-side EXACT value equality** (STRONGER than 0106's key-only): both sides run the SAME Lua script → the SAME `Bucket` write → the SAME resolved string, and the same `default_value`. The driver asserts, on EVERY span BOTH sides: `meta_hit == "<fixed>"` AND `meta_default == "<fallback>"` (key AND value), plus the `0087` baseline (span count > 0 — a 0-span pass is structurally impossible). Each an independent `Errorf`. `telemetry.sdk.*` resource + scope UNasserted cross-side (impl-specific, the 0106 precedent). Fixtures **115 → 116**.

**Deliberate breaks** (`-count=1`, full `TestDifferential/0114-...` selectors, confirm WHICH fired, `reference_differential_break_protocol_count1` / `reference_deliberate_break_wrong_assertion`): (a) change the Lua-set value on one side → `meta_hit` value-equality fires; (b) drop `meta_default`'s default (empty) → the tag omits → the `meta_default` presence assertion fires; (c) a wrong-namespace control → `meta_hit` falls to its default (fires the hit assertion). The PLAN substitutes a COMPILING equivalent for any non-compiling break (`reference_plan_break_instructions_dont_compile`) and confirms the WRITER served-this-arm (the span must carry the Lua value, not a vacuous default). The dispatch constraint (`reference_differential_fixture_dispatch_constraint`) holds: ONE dir = ONE cross-side runner branch (both custom_tags resolve on the SAME request).

## 9. Behavior-contract edit map — pinned *(all edits land at the IMPL, atomically with ADR-0292)*

*(Anchor re-derived at the IMPL: the tracing `custom_tags` subsection of `docs/envoy-go/BEHAVIOR_CONTRACT.md`, where `literal`/`request_header`/`environment` are described and `metadata` is currently listed "unsupported (rejected)".)*

**B1 — flip the `metadata` line.** From "`metadata` — unsupported (rejected)" to: the `metadata` type is CONSUMED for the **`REQUEST`** MetadataKind — the named dynamic-metadata value (`metadata_key.key` namespace + `path` walk) is resolved out of the per-request `Bucket`, serialized to a STRING (string→raw; number/bool→scalar; struct/list→compact JSON — the `google.protobuf.Value` rendering), and emitted as a `{tag, value}` span attribute on BOTH exporters (appended after the built-ins on OTLP; in the `tags` map on Zipkin); `default_value` emits when the path is absent/unresolvable and the default is non-empty, a present-empty value emits `""`, an unresolvable-with-empty-default omits (the `request_header` rule). The `ROUTE`/`CLUSTER`/`HOST`/unset-kind arms reject loudly (envoy-go-strict DEPARTURE — the reference boots them); the empty-namespace/empty-path/empty-segment rejects are PGV-PARITY (both reject). This completes the four `custom_tags` SOURCE types.

**B2 — the departures list.** Add the two categories: (departure) non-`REQUEST` MetadataKinds + unset-kind reject where the reference boots; (documented boundary) non-string value serialization beyond the P3-pinned common types (number-precision edges + `NullValue`).

## 10. Test plan + task surface *(D-CTM-SPLIT — DECISION: a SINGLE FLAT ROW)*

**DECISION: SINGLE FLAT ROW** (~10 tasks, comfortably under the ADR-0045 ~15 ceiling). The parse and resolve sit on the SAME landed `internal/tracing` engine; the `Bucket` is a landed read-only substrate; the reject arms live in the same parse function; the seam correction (C1) is one small accessor. No second subsystem can strand a leg. **ADR-0045 escape valve ARMABLE but UNCONSUMED**; ONE ADR (ADR-0292). Sketch (the PLAN decomposes; TDD):
- **T1** — `config.go`: `kindMetadata` const + the `CustomTagSpec` extension (`MetaNamespace`/`MetaPath`) + the metadata accept + four reject arms (§3.2/§3.6). `config_test.go`: accept REQUEST + reject ROUTE/CLUSTER/HOST/unset-kind/empty-namespace/empty-path/empty-segment (distinct substrings).
- **T2** — `resolve.go`: the `metaLookup` param + the `kindMetadata` arm + `descend` + `structpbValueToString` (§3.3/§3.4). `resolve_test.go`: the path-walk (single + multi-segment), the P3 serialization table (string/number/bool/struct/list, `json.Compact`-compared), the P4 default/omit matrix, the P5 unresolvable-path→default, nil-`metaLookup` tolerance.
- **T3** — `internal/filter/http/chain.go`: add `func (c *FilterChain) DynamicMetadata() *dynamicmetadata.Bucket` (C1) + a unit test (returns the live bucket; nil-tolerant).
- **T4** — `accesslog_emit.go`: thread `metaLookup` to the three emit methods; update ALL 18 caller sites (§11 — `nil` at the 3 no-chain 404 sites, `chain.DynamicMetadata().Get` elsewhere). `span_emit_test.go`/`accesslog_emit_test.go` callers updated (they pass `nil` — no metadata tags configured — proving byte-stable no-metadata behavior).
- **T5** — `FuzzHCMConfigParse` seed + dispatch-verification (§7).
- **T6** — the `0114` fixture (config + driver + Lua writer + the two custom_tags) + deliberate breaks (§8).
- **T7** — BEHAVIOR_CONTRACT B1-B2 (§9).
- **T8** — six-gate + full 116-dir differential + `-race` (the `internal/tracing` + `internal/filter/hcm` packages) + the cycle guard (`go list -deps ./internal/tracing` — no filter-pkg edge) + `go mod tidy -diff` empty + envelope audit (`internal/xds`/`internal/tls`/`internal/boot`/`internal/listener`/`internal/bootstrap` BYTE-UNTOUCHED; production = `internal/tracing/{config.go,resolve.go}` + `internal/filter/http/chain.go` + `internal/filter/hcm/{accesslog_emit.go,connection.go,h2dispatch.go,h3dispatch.go}`).
- **T9** — ADR-0292 completed IN PLACE (§Decision/§Consequences) + ROADMAP row-70-done + the deferred-sentence narrow (roll `custom_tags (`metadata`)` OUT — §12) + STATE/router (stage-close).

Coverage properties, each an independent `Errorf`: the REQUEST accept + four rejects; the path-walk (single/multi/unresolvable); the P3 serialization table; the P4 default/omit/present-empty matrix; the nil-`metaLookup` tolerance; the `*FilterChain.DynamicMetadata()` accessor; the cross-side EXACT `0114` assertions; byte-stable-when-no-metadata-tags.

## 11. Edit-site roster *(RE-DERIVED at `9338507a`)*

**Production — `internal/tracing/config.go`:** `parseCustomTags` `:194-218` `[REPLACE the :214-215 metadata reject with the accept+4-reject arms]` · `customTagKind` `:52-56` `[ADD kindMetadata]` · `CustomTagSpec` `:63-71` `[ADD MetaNamespace/MetaPath]` · imports `[ADD metadatav3]`. **`internal/tracing/resolve.go`:** `ResolveCustomTags` `:14` `[ADD metaLookup param + kindMetadata arm + descend + structpbValueToString]` · imports `[ADD structpb, protojson, encoding/json]`. **`internal/filter/http/chain.go`:** `[ADD func (c *FilterChain) DynamicMetadata() — beside SetDynamicMetadata :1014]`. **`internal/filter/hcm/accesslog_emit.go`:** `emitAccessLog`/`emitAccessLogH2`/`emitAccessLogH3` `[ADD metaLookup param, pass to ResolveCustomTags :55/:116/:177]` · imports `[ADD structpb]`. **`internal/filter/hcm/connection.go`:** the 5 `emitAccessLog` callers `[:330 nil; :464/:597/:699/:777 chain.DynamicMetadata().Get]`. **`internal/filter/hcm/h2dispatch.go`:** the 6 `c.f.emitAccessLogH2` callers `[:313 nil; :396/:530/:577/:584/:613 chain.DynamicMetadata().Get]`. **`internal/filter/hcm/h3dispatch.go`:** the 7 `f.emitAccessLogH3` callers `[:130 nil; :210/:280/:341/:367/:373/:395 chain.DynamicMetadata().Get]`. *(The exact per-caller `chain` variable name is re-derived at the IMPL; the no-chain 404 sites pass `nil` — provably never dereferenced, §3.5.)*

**Test/harness:** `config_test.go` / `resolve_test.go` `[ADD]` · `internal/filter/http/chain_test.go` `[ADD accessor test]` · `accesslog_emit_test.go` / `span_emit_test.go` `[UPDATE caller signatures — pass nil]` · `FuzzHCMConfigParse` seed (`fuzz_test.go:28`) `[ADD, dispatch-verified]` · `test/fixtures/0114-tracing-custom-tags-metadata/` (config + driver + Lua) `[ADD]`. **REUSED untouched:** `test/helpers/otlptrace` · `internal/dynamicmetadata` · `internal/filter/http/lua`. **UNTOUCHED:** `internal/xds`, `internal/tls`, `internal/boot`, `internal/listener`, `internal/bootstrap`.

**Docs:** BEHAVIOR_CONTRACT per §9 `[IMPL]` · DECISIONS ADR-0292 §Context **AT THIS COMMIT** (§14), completion at the IMPL · ROADMAP/STATE/next-prompt `[STAGE-CLOSE]`.

## 12. Sentinel maintenance — a SENTENCE-NARROWING row, narrowed AT THE IMPL (the phase-57 precedent)

`tracing custom_tags (`metadata`)` IS named in the live Observability `candidates:` sentence. **Per the verified phase-57 graphite precedent, NO sentence is narrowed at ANY pre-IMPL stage.** So the sentence rolls `tracing custom_tags (`metadata`)/` OUT at the phase-70 IMPL row-done edit, NOT before; afterward the sentence STAYS a live `candidates:` match (the `ssl` family + `spawn_upstream_span`/`http_service`/force-trace remain, and the three non-`REQUEST` MetadataKinds become a NEW deferred entry — `reference_sentinel_deferred_sentence_live_vs_historical`; keep EXACTLY ONE live Observability match, HTTP/3 + xDS untouched, three total). Check (1) keeps printing `NOT DONE: row 70` until the IMPL six-gate; checks (2)+(3) unchanged (`grep -cE 'remaining deferred \(not-yet-chartered\) candidates:'` → 3; `NEVER OPENED: gRPC/Runtime/WASM`) ⇒ the sentinel does NOT fire. **Nothing is owed at this SPEC.**

## 13. Deferred items *(the BRAINSTORM §8 roster carried MINUS this row's pickup)*

Carried unchanged: the three non-`REQUEST` MetadataKinds (`ROUTE`/`CLUSTER`/`HOST` — rejected loudly this row) · `spawn_upstream_span` · `http_service` · force-trace · the `ssl` stat family (ADR-0286 C3) · the 4 EMPTY built-in span attributes (`reference_tracing_upstream_cluster_framework_gap`) · a SIXTH stats sink (hystrix/wasm) · the RBAC `require=false` fixture rider · DataSource `environment_variable` (D-ENV-HARNESS-gated) · `watched_directory`/SDS rotation · the xDS + HTTP/3 families · the never-opened gRPC/Runtime/WASM families. **REMOVED: tracing `custom_tags` `metadata` (`REQUEST` kind) — THIS pickup.**

**Newly deferred at THIS row:** the three non-`REQUEST` MetadataKinds (per-object metadata, not exposed at the span-emit seam) · non-string metadata-value serialization beyond the P3-pinned common types (number-precision edges + `NullValue` — documented boundaries, §3.3).

**Memory update owed at the IMPL:** the `*FilterChain.DynamicMetadata()` gap — a `chain.DynamicMetadata()` accessor is on the CALLBACK types (`decoderCB`/`encoderCB`), NOT on `*FilterChain`; a consumer at the HCM dispatch layer must ADD one (the BRAINSTORM mis-cited it as existing — `feedback_brief_citations_not_evidence`). And: the tracing `metadata` value→string serialization needs `json.Compact` after `protojson.Marshal` to strip the detrand whitespace and match the reference's compact form (`reference_detrand_race_catches_protojson_value_substring` extended to a value-emission site).

## 14. ADR continuity — the ADR-0292 §Context DRAFT (anchored here; full entry at the phase-70 IMPL)

*(The canonical entry is in `docs/envoy-go/DECISIONS.md`, appended at this SPEC commit — the tail flips ADR-0291 → ADR-0292 there; the IMPL completes the entry IN PLACE. This blockquote reproduces its prose; the STATUS-banner + `### Context` heading wrapper follows the DECISIONS file convention.)*

> **ADR-0292 — tracing `custom_tags` `metadata` type, `REQUEST` MetadataKind only (the SIXTEENTH Observability-family row + the FOURTH tracing `custom_tags` row + the LAST `CustomTag` SOURCE type after `literal`/59/ADR-0277, `request_header`/62/ADR-0283, `environment`/63/ADR-0284; lifts the `custom_tags metadata type unsupported` reject at `internal/tracing/config.go:214-215` and resolves a `REQUEST`-kind dynamic-metadata value out of the LANDED `internal/dynamicmetadata.Bucket` — walk the `MetadataKey.path`, serialize the resolved `structpb.Value` to a STRING [string→raw; number/bool→scalar; struct/list→compact JSON, the `google.protobuf.Value` rendering], emit as a `{tag, value}` span attribute via the shared `ResolveCustomTags`→`Span.Attrs []KV` seam on BOTH the OTLP and Zipkin exporters; the `ROUTE`/`CLUSTER`/`HOST`/unset-kind arms reject loudly with distinct substrings [an envoy-go-strict DEPARTURE — the reference BOOTS them]; a SINGLE FLAT ROW — FLIPS ROW 70 `done`; the Observability family STAYS OPEN; +0 stats/packages/modules/fuzzers/BackendKinds, new exports confined to `internal/tracing` + ONE new `func (c *FilterChain) DynamicMetadata()` accessor on `internal/filter/http`; the `internal/xds` zero-new-symbol discipline UNTOUCHED).**
>
> **§Context (drafted at the phase-70 SPEC per ADR-0044).** Phase 46 built the HCM-native tracing engine (the per-request ingress SERVER `Span` with built-in STRING attributes over BOTH the OTLP and Zipkin transports through the shared `Span.Attrs []KV` seam, ADR-0260/0261). Phases 59/62/63 added the first three `custom_tags` SOURCE types (`literal` ADR-0277, `request_header` ADR-0283, `environment` ADR-0284) — each lifting one `parseCustomTags` strict-reject arm and resolving per-request into `Span.Attrs` via the ordered first-wins-deduped `[]CustomTagSpec` + `ResolveCustomTags` pipeline threaded at the three `accesslog_emit.go` emit sites (H1/H2/H3). The `metadata` type stayed the SOLE loud strict-reject. Phase 70 is the FOURTH tracing `custom_tags` row and consumes the LAST `CustomTag` SOURCE type. Six discriminating live-probe arms (`envoyproxy/envoy:contrib-v1.37.2`, fresh container per arm, a Lua-set dynamic-metadata WRITER served-this-arm, a standalone host OTLP-gRPC + Zipkin-HTTP receiver, decode-ran asserted) pinned: the reference emits the metadata custom tag as `{key = the LITERAL `tag` name, value = the serialized dynamic-metadata value as a STRING}`, APPENDED AFTER the built-ins on the OTLP span and in the Zipkin v2 JSON `tags` map (P1/P2); the `structpb.Value`→string rendering is string→raw, number→canonical decimal, bool→`true`/`false`, struct/list→compact JSON (P3); the `default_value` emits when the path is absent/unresolvable and the default is non-empty, a PRESENT-but-EMPTY value emits `""` and does NOT fall to the default, an unresolvable-with-empty-default OMITS — the `request_header` rule, NOT the `environment` omit-on-empty rule (P4); a multi-segment `MetadataKey.path` walks nested `StructValue` fields and an unresolvable segment falls to the default (P5); and the reference ACCEPTS (boots) the `ROUTE`/`CLUSTER`/`HOST`/unset-kind metadata tags — so envoy-go's REQUEST-only reject is a genuine departure — while PGV boot-rejects an empty `metadata_key.key`/empty `path` (a parity reject) (P6). The landed substrate: `internal/dynamicmetadata.Bucket.Get` (the `REQUEST`-kind source), the Lua filter as the cross-side-deterministic writer, the ratelimit `descendStructpbValue` as the path-walk shape to clone, and the `Span.Attrs`/`ResolveCustomTags`/both-exporter seams.
>
> **The design (§Decision anticipated).** The `parseCustomTags` metadata arm (replacing the `:214-215` wholesale reject) accepts `kind==REQUEST` (validating a non-empty namespace + non-empty path + non-empty segments — PGV parity) into a `CustomTagSpec` extended with `MetaNamespace`/`MetaPath` (reusing `DefaultValue`/`HasDefault = DefaultValue != ""`, the `request_header` idiom) + a new `kindMetadata` const, and rejects `ROUTE`/`CLUSTER`/`HOST`/unset-kind loudly with distinct substrings (envoy-go-strict). `ResolveCustomTags` grows a nil-tolerant `metaLookup func(ns,key)(*structpb.Value,bool)` param + a `kindMetadata` arm that walks the path (a local `descend` over `GetStructValue().Fields`, cloned from the ratelimit shape — NOT imported, to keep `internal/tracing` filter-free) and serializes via `structpbValueToString` (string→`GetStringValue()`; else `protojson.Marshal` + `json.Compact` to match the reference's compact form and strip the detrand whitespace). The per-request `Bucket` reaches the emit sites via a NEW `func (c *FilterChain) DynamicMetadata()` accessor (the existing `:837`/`:963` accessors are on the callback types, not `*FilterChain`) threaded as `chain.DynamicMetadata().Get` at the 15 span-capable `accesslog_emit.go` callers (H1/H2/H3) and `nil` at the 3 no-chain 404 sites (provably never reached — the span block is `traceDecision`-gated). Envelopes: +0 packages, +0 go.mod modules (`structpb`/`type/metadata/v3`/`protojson` resolve at existing directs), +0 stats (a span attribute registers none), +0 fuzzers (a `FuzzHCMConfigParse` seed, dispatch-verified to reach the metadata arm), +0 BackendKinds (the Lua writer + `otlptrace` receiver are existing/driver-owned); functional production edits in `internal/tracing/{config.go,resolve.go}` + `internal/filter/http/chain.go` + `internal/filter/hcm/{accesslog_emit.go,connection.go,h2dispatch.go,h3dispatch.go}`; new exports confined to `internal/tracing` + the one `*FilterChain.DynamicMetadata()` accessor — the `internal/xds` discipline UNTOUCHED; `internal/xds`/`internal/tls`/`internal/boot`/`internal/listener`/`internal/bootstrap` byte-untouched. Proven by ONE new OTLP differential `0114-tracing-custom-tags-metadata` (a Lua-set FIXED-string writer + two metadata custom_tags — one resolving to the Lua value, one falling to `default_value` — asserted cross-side EXACT key+value, STRONGER than the 0106 key-only environment fixture; fixtures 115 → 116) + the config/resolve unit matrices (the P3 serialization table + the P4 default/omit matrix + the path-walk). A SINGLE FLAT ROW (the phase-59/62/63 `custom_tags`-source precedent — parse + resolve on one landed engine; the ADR-0045 valve is armable but unconsumed). The Observability family STAYS OPEN (the three non-`REQUEST` MetadataKinds + `spawn_upstream_span`/`http_service`/force-trace + the `ssl` stat family remain).
>
> *(§Decision + §Consequences land at the phase-70 IMPL.)*

## 15. Exit — counts + expectations at SPEC-DONE

**Counts at this SPEC commit (docs-only apart from the DECISIONS append):** fixtures **115** · fuzzers **55** · stat surface **1201** · BackendKind **38** · go.mod modules **2** · **DECISIONS tail flips ADR-0291 → ADR-0292 AT THIS COMMIT** (the §Context draft append IS the tail flip; next-free becomes ADR-0293; the router's "re-derive, never trust" tail check must expect `## ADR-0292`).

**Anticipated at the phase-70 IMPL:** fixtures **115 → 116** (`0114-tracing-custom-tags-metadata`) · stat surface **1201 (+0)** · fuzzers **55 (+0)** · BackendKind **38 (+0)** · go.mod **2 (+0)** · ZERO new packages · new exports confined to `internal/tracing` + `func (c *FilterChain) DynamicMetadata()` on `internal/filter/http` — the `internal/xds` discipline UNTOUCHED · functional production edits in `internal/tracing/{config.go,resolve.go}` + `internal/filter/http/chain.go` + `internal/filter/hcm/{accesslog_emit.go,connection.go,h2dispatch.go,h3dispatch.go}` · `internal/xds`/`internal/tls`/`internal/boot`/`internal/listener`/`internal/bootstrap` BYTE-UNTOUCHED · ADR-0292 completed IN PLACE · ROADMAP narrows `custom_tags (`metadata`)` OUT at the row-done edit.

**Sentinel:** re-verified expectation — check (1) keeps printing `NOT DONE: row 70`; checks (2)+(3) unchanged; **no deferred-sentence edit at any pre-IMPL stage** (§12); no `stop` file.

**Probe hygiene:** SIX fresh-container probe arms (P1..P6) ran THIS SPEC (the D-CTM docket was genuinely owed — the BRAINSTORM ran none); all containers removed + the receiver stopped; the worktree contains only this SPEC + the DECISIONS append at commit time.

**Next → the phase-70 PLAN.** Its inheritance: the SINGLE FLAT ROW ~10-task spine (§10); the metadata accept + four distinct rejects (§3.2/§3.6); the `kindMetadata` resolve arm with the `descend` clone + the `structpbValueToString` serializer (string→raw / non-string→`protojson`+`json.Compact`, §3.3); the `request_header` default rule NOT `environment` (§3.4, C2); the NEW `*FilterChain.DynamicMetadata()` accessor + the `metaLookup` thread at all 18 callers with `nil` at the 3 no-chain sites (§3.5, C1); the ONE OTLP fixture with two custom_tags asserted cross-side EXACT (§8); the fuzz-seed dispatch-verification (§7); the pinned §9 BC edit map. **RE-DERIVE this document; do not execute it.** Where it cites, go look; where it claims control flow, walk the call graph; default to REFUTED.
