# PROGRESS 70 — tracing `custom_tags` `metadata` type, `REQUEST` MetadataKind only

> Live task ledger for the phase-70 IMPL. The PLAN (`PLAN.md`) is the spine; this file records what ACTUALLY happened per task — red-first verbatims, WHICH break assertion fired (and any that did NOT), substitutions, the T5 dispatch-verify trap result, and the six-gate evidence. Populated at the IMPL, not the PLAN.

## Stage pointer

- **PLAN done** (2026-07-21) — the 9-task SINGLE-FLAT-ROW TDD spine (T1–T9) landed; the SPEC §11 anchors RE-DERIVED at `2dd6b2d0` (**ZERO unacknowledged drift — every anchor exact**; the tip is one docs-only commit ahead of the SPEC's cited `9338507a`, no line shifted) via a read-only re-derivation agent that also extracted the three clone skeletons (the ratelimit `descendStructpbValue` path-walk `descriptors.go:1116`, the `kindRequestHeader` default-rule arm `resolve.go:23-35`, the `withEnvTags` fuzz seed `fuzz_test.go:66-75`) + the RD-LUA sharpening (the Lua-writer clone source is `0027-http-lua-full-bridge`, NOT `0106` which has no Lua) + the C1 confirmation (`func (c *FilterChain) DynamicMetadata()` absent; `:837`/`:963` on `decoderCB`/`encoderCB`) + the 18-caller map (5+6+7; exactly 3 no-chain-404 nil sites; `chain` var name uniform). **Adversarial verification (PLAN §1.2 — record below.)** Fresh worktree off master `2dd6b2d0`, branch `phase-70-tracing-custom-tags-metadata-plan`. Row 70 STAYS `in-progress`; DECISIONS UNTOUCHED at the PLAN (ADR-0292 stays PROPOSED); NO deferred sentence narrowed at the PLAN (rolls OUT at the IMPL row-done, the phase-57 precedent).

## Adversarial-pass record (PLAN §1.2)

*(TWO independent verifiers ran against the draft in PRIVATE scratch before landing — `reference_parallel_subagents_private_scratch`; the real repo left untouched, no worktrees registered.)*

- **V1 — code-claims re-derivation + compile probes.** ONE SEVERE (folded), ZERO MODERATE, ZERO MINOR. **SEVERE:** the `parseCustomTags` metadata skeleton (§1.1) + the SPEC §3.1 shorthand called `md.GetRequest()`/`GetRoute()`/`GetCluster()`/`GetHost()` on `*tracingv3.CustomTag_Metadata`, which has ONLY `GetKind()`/`GetMetadataKey()`/`GetDefaultValue()` (`custom_tag.pb.go:373-387`); the kind-getters live on `*metadatav3.MetadataKind` (`metadata.pb.go:164-185`). Compile-proven both directions. CORRECTED in §1.1 (bind `k := md.GetKind()`, branch on `k.GetRequest()`) + the T1 Consumes/Step-3 warnings. All else CONFIRMED: the descend/serializer/accessor/fuzz-seed skeletons build; the 18-caller map exact (3 nil sites; h3:210 a chain site); the request_header default rule; +0 modules.
- **V2 — process/consistency/SPEC-coverage.** ZERO SEVERE, ZERO MODERATE, TWO MINOR (both folded: the V1/V2 records were placeholders — now populated; BackendKind 38 carried from SPEC, re-run mechanically at T8). All checks PASS: SPEC §10/§11 coverage complete (ZERO cited-but-unwritten — the phase-69 TestNoNewStat defect class avoided; SPEC §7 says NO such guard for tracing); stage-close mechanics correct (row in-progress; DECISIONS untouched; ADR-0292 completed IN PLACE at the IMPL; no premature sentence-narrow); counts mechanically consistent; sentinel does NOT fire; envelope + break-protocol + task-split consistent; format faithful to phase-69.

## Task ledger (filled at the IMPL)

| Task | Status | Commit | Red-first / breaks / notes |
|---|---|---|---|
| T1 config parse (kindMetadata + accept + 4 rejects) | pending | — | Red: `kindMetadata undefined`. Accept REQUEST; reject ROUTE/CLUSTER/HOST/unset-kind (DEPARTURE) + empty ns/path/segment (PARITY). Breaks A/B/C. |
| T2 resolve arm (metaLookup + descend + structpbValueToString) | pending | — | Red: `ResolveCustomTags` arity. The request_header default rule (present-empty EMITS ""); descend clones the ratelimit shape (NOT imported); the P3 serialization table (json.Compact load-bearing). Breaks D/E/F/G; -race. |
| T3 *FilterChain.DynamicMetadata() accessor | pending | — | Red: `DynamicMetadata undefined`. Beside SetDynamicMetadata :1014; nil-tolerant via Bucket.Get. Break H. |
| T4 metaLookup thread (18 callers) | pending | — | Red: arity mismatch. chain.DynamicMetadata().Get at 15 sites, nil at 3 no-chain-404 sites (:330/:313/:130). New metadata-span test. Breaks I/J; -race. |
| T5 fuzz seed + dispatch-verify | pending | — | withMetaTags REQUEST-accept + ROUTE-reject seed; dispatch-verify reaches the parseCustomTags metadata arm (config.go:122 < :130); count 55→55. |
| T6 fixture 0114 (OTLP) | pending | — | 0106 chassis + 0027 Lua writer; two custom_tags (meta_hit→Lua value, meta_default→default_value) cross-side EXACT key+value; breaks K/L/M; fixtures 115→116. |
| T7 BEHAVIOR_CONTRACT B1–B2 | pending | — | B1 flip the metadata line to CONSUMED-for-REQUEST; B2 the departures. Docs-only. |
| T8 verify (six-gate + envelope) | pending | — | gofmt/vet/build/tidy-diff/lint · FULL 116-dir differential · cycle guard (no filter edge) · -race · counts · envelope audit. ROW 70 FLIPS `done`. |
| T9 ADR-0292 + close | pending | — | ADR-0292 COMPLETE IN PLACE (§Decision + §Consequences; tail stays ADR-0292, next-free ADR-0293). ROADMAP row 70 → `done` + custom_tags (metadata) narrowed OUT. STATE/PROGRESS/router; sentinel re-run (does NOT fire); memory updates. |

## Findings carried from the PLAN (RE-DERIVED at `2dd6b2d0`; RE-VERIFY at the IMPL tip)

- **RD-EXACT:** every SPEC §11 anchor exact at `2dd6b2d0` — no line-boundary correction to carry (the SPEC's cites adopted verbatim).
- **RD-KIND:** `customTagKind` `config.go:52-56` = kindLiteral/kindRequestHeader/kindEnvironment; `kindMetadata` appends at iota==3. `CustomTagSpec` `:63-71` += `MetaNamespace string`/`MetaPath []string`, REUSE `DefaultValue`/`HasDefault`.
- **RD-REJECT:** the `metadata` reject is `config.go:214-215` (`"tracing: custom_tags metadata type unsupported"`) — the whole `case ct.GetMetadata()!=nil` is REPLACED. Empty-tag `:190-192` + first-wins dedup `:219-223` UNCHANGED.
- **RD-DEFAULT:** clone `kindRequestHeader` (`resolve.go:23-35`: present-empty EMITS "" via `KV{Key,""}`), NOT `kindEnvironment` (`:36-52`: omit-iff-resolved-empty). `HasDefault = DefaultValue != ""` (C2).
- **RD-DESCEND:** `descendStructpbValue` (`descriptors.go:1116`, `[]*MetadataKey_PathSegment`→string, non-string terminal → `("",false)`) is the CLONE SHAPE; the tracing `descend` is a `[]string` variant + a serializing terminal; `internal/tracing` gains NO filter import (a ~12-line local; cycle guard filter-free).
- **RD-SERIALIZE:** `structpbValueToString` — string→`GetStringValue()` (raw, incl. ""); `NullValue`→`("",false)` boundary; else `protojson.Marshal`+`json.Compact` (list `["x", "y", "z"]`→`["x","y","z"]`; `json.Compact` load-bearing, V1-executed at the SPEC). `resolve.go` imports only `"os"` → += structpb/protojson/encoding/json/bytes (all existing-module).
- **RD-ACCESSOR (C1):** `func (c *FilterChain) DynamicMetadata()` ABSENT (grep → 0); `SetDynamicMetadata` `chain.go:1014`; field `dynamicMetadata *dynamicmetadata.Bucket` `:274`; the `:837`/`:963` accessors on `*decoderCB`/`*encoderCB` (return `d.c.dynamicMetadata`/`e.c.dynamicMetadata`), NOT `*FilterChain`. ADD the getter beside `:1014` (import `:19` already present).
- **RD-BUCKET:** `dynamicmetadata.go:33` `Get(filterName, key string) (*structpb.Value, bool)` nil-receiver-safe (`if b==nil||b.m==nil { return nil,false }`) ⇒ `chain.DynamicMetadata().Get` safe on a nil bucket.
- **RD-CALLERS:** 18 callers exact (5 connection.go + 6 h2dispatch.go + 7 h3dispatch.go); the 3 nil sites are `connection.go:330`/`h2dispatch.go:313`/`h3dispatch.go:130` (BEFORE the chain built, `traceDecision` nil PRE-Decide); the 15 chain-in-scope sites pass `chain.DynamicMetadata().Get` (var name `chain` uniform, built `:350`/`:327`/`:148`). Subtlety: `h3dispatch.go:210` has `chain` but `traceDecision` still nil (Decide `:247`) — valid-but-never-reached, NOT a nil site. `accesslog_emit.go` LACKS `structpb` — ADD it.
- **RD-FUZZ:** `FuzzHCMConfigParse` `internal/filter/hcm/fuzz_test.go:28`; clone the `withEnvTags` seed (`:66-75`); dispatch CONFIRMED — `config.go:122` (`parseCustomTags`) precedes `:130` (`provider required`). Count 55→55 (a seed is +0). Add `metadatav3` import.
- **RD-LUA:** the fixture CHASSIS clones from `0106-tracing-custom-tags-environment` (OTLP tracing, all ports templated) but `0106` has NO Lua; the Lua `dynamicMetadata():set` writer clones from `0027-http-lua-full-bridge` (`scripts/e_dynamic_metadata.lua` + the `envoy.filters.http.lua` stanza).
- **RD-PORTS (C5):** all ports templated (`{{.AdminPort}}`/`{{.ListenerPort}}`/`{{.BackendPort}}`/`{{.OTLPPort}}`) — no hardcoded port to pick; next fixture `0114`.
- **RD-IDENT:** `kindMetadata`/`MetaNamespace`/`MetaPath`/`metaLookup`/`structpbValueToString`/`descend` all collision-free repo-wide (`descend*` = only ratelimit's `descendStructpbValue`, a different package). `metadatav3` is the established ratelimit alias.
- **RD-MOD:** +0 go.mod modules — `structpb` (dynamicmetadata dep), `type/metadata/v3` (ratelimit direct), `protojson`/`encoding/json`/`bytes` (std/existing) all resolve; re-check `git diff go.mod` after tidy at T8.
