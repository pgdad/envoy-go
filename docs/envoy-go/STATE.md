# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `22-http-filter-lua` (parent row in-progress; sub-phase `22.1-http-filter-lua-vm-and-headers-bridge` done at master tip `c986419`; sub-phase `22.2-http-filter-lua-full-bridge` done at master tip `46183a4`; sub-phase `22.3-http-filter-lua-multi-script-and-per-route` **PLAN done at this commit** — awaiting 22.3 IMPL). Phase `22-http-filter-lua` parent BRAINSTORM done at parent BRAINSTORM commit + parent SPEC done at `41ccee7`; 22.1 lifecycle traversed BRAINSTORM (parent) → SPEC at `a7021aa` → PLAN at `02d745a` → IMPL at `d30f131` → done. Phase `22.2` lifecycle traversed BRAINSTORM at `6ad3064` → SPEC at `0d6463e` → PLAN at `269dee1` → IMPL at `46183a4` → done. Phase `22.3` lifecycle traverses 2 → 3 at THIS commit per BOOTSTRAP §4.1 invariant 1 (BRAINSTORM done at `0929b3d`; SPEC done at `e72af4c`; PLAN done at THIS commit; awaiting IMPL). **22.3 PLAN done at 2026-05-21** — authored `docs/envoy-go/phases/22.3-http-filter-lua-multi-script-and-per-route/PLAN.md` (7-task breakdown realizing the SPEC §15 acceptance checklist; reviewed + approved via the `superpowers:writing-plans` plan-document-reviewer loop) via the `superpowers:writing-plans`-scoped-to-PLAN session per the 22.1 + 22.2 SPEC→PLAN two-session split precedent. The PLAN resolved the 3 residual PLAN-time D-questions: **D-P1** per-route override `*Chunk` storage = option (b') compile-with-cache-hit at bind, memoized by the resolved `*LuaPerRoute` pointer in a NEW `cc.perRouteChunks` map (keeps the `func(proto.Message) error` validator signature unchanged); **D-P2** fixture-0028 boot-reject roster = (g) `source_codes[name]` DataSource-failure + (h) per-route `source_code` DataSource-failure CROSS-SIDE, (f) key-empty REFERENCE-LESS subject-only (envoy-go-strict-defensive); **D-P3** = 6 net-new config-load arm-groups (key-empty + source_codes-value-gauntlet + 3 PGV-mirror per-route arms + per-route-source_code-gauntlet; arms 3+7 DROPPED per BRAINSTORM decision #2 + AMEND-22.3-1; fuzzer-surfaced arms carry to the IMPL Task 4 per the 22.1 Task-11 precedent). **R6** *LState-pool gate disposition: anticipated WEAK-default STANDS (per-route resolution is O(1) lookup, no new per-stream VM construction); conditional ADR-0194 fires only if the IMPL benchmark crosses 1ms. **ADR-0045 split-gate: STAY SINGLE-PHASE** (7 tasks ≪ 25; ~310-520 production LoC ≪ 1500). A load-bearing dispatch insight surfaced + captured in the PLAN: the lua filter builds the per-stream VM ONCE at DecodeHeaders (running the per-route-resolved chunk, defining BOTH hooks), so EncodeHeaders' guard must change from `f.cc.chunk == nil` to `f.vm == nil` (else a per-route override on a default-less listener wrongly skips `envoy_on_response`). **NO production code lands at PLAN** — only PLAN.md + STATE/README updates. Predecessor: **22.3 SPEC done at 2026-05-21** — authored `docs/envoy-go/phases/22.3-http-filter-lua-multi-script-and-per-route/SPEC.md` (16-section authoritative SPEC) via the `superpowers:brainstorming`-scoped-to-SPEC session per the 22.1 + 22.2 BRAINSTORM→SPEC two-session split precedent. The SPEC ran the ADR-0004 §11 empirical-pin scrape (5 pins: v1.32.4 proto roster + upstream v1.37.2 `source_codes`/`LuaPerRoute` dispatch + gopher-lua compile/chunk-sharing + local per-route mechanism/jwt_authn precedent/BootRejectFixture + fuzzer count) resolving the 4 D-questions IN-SESSION (D1 compile-cache → content-hash reuse; D2 chunk-binding → per-stream O(1) lookup atop HCM-build-time-validated per-route config; D3 disabled → early-return both hooks; D4 → boot-reject scenarios for the genuine config-load arms). **The scrape surfaced AMEND-22.3-1: it REFUTED 22.3 BRAINSTORM decision #3 + parent SPEC §6.3 arm 7** — upstream Envoy lua silently no-ops a dangling per-route `name` at request time (`perLuaCodeSetup()` → `nullptr` → `LUA_REFNIL`), and the jwt_authn precedent the BRAINSTORM cited uses a split-semantic (ADR-0153) whose PER-ROUTE half is runtime-resolve, NOT config-load fail-fast. **User-confirmed disposition (this session): upstream-parity silent no-op** — DROPS the dangling-name PARSE-REJECT arm 7, 0 net-new BEHAVIOR_CONTRACT departure records, no HCM cross-resolution plumbing. The other 7 LOCKED BRAINSTORM decisions STAND (1 combined NEW ADR-0193 + ADR-0125 §(xiv) AMENDMENT + no-reserved-name [arm 3 dropped] + fixture-0028 5-tier cross-side + `FuzzLuaPerRouteConfig` [30→31] + WEAK HOLD escape-valve at conditional ADR-0194 + single-phase). **0 net-new stats** (SHARED-vacuous; stat count STAYS 107). **ADR-0193 §Context draft anchored at THIS SPEC commit** per ADR-0044 (DECISIONS.md tail advances ADR-0192 → ADR-0193 §Context; next-free ADR-0194). 3 residual PLAN-time D-questions (D-P1 per-route `*Chunk` storage wiring + D-P2 fixture boot-reject cross-side-vs-subject-only roster + D-P3 config-load arm enumeration).
- **phase-directory:** `docs/envoy-go/phases/22-http-filter-lua/` (parent — BRAINSTORM.md + SPEC.md authored at parent SPEC commit; parent phase row stays in-progress until 22.3 phase-done per parent SPEC §1 closure pattern). Sub-phase directories: `docs/envoy-go/phases/22.1-http-filter-lua-vm-and-headers-bridge/` (SPEC.md + PLAN.md + PROGRESS.md + REVIEW.md from 22.1 lifecycle; LANDED at master tip `c986419`) + `docs/envoy-go/phases/22.2-http-filter-lua-full-bridge/` (SPEC.md + PLAN.md + PROGRESS.md + REVIEW.md + BRAINSTORM.md from 22.2 lifecycle; LANDED at master tip `46183a4`) + `22.3-http-filter-lua-multi-script-and-per-route/BRAINSTORM.md` **authored at THIS commit** (12-section dialogue: §1 mission/scope + §2 per-topic design decisions [12 sub-sections] + §3 framework-survey [0 new primitives] + §4 per-route 9th-canonical detail + §5 stat surface [0 net-new] + §6 fixture-0028 + §7 anticipated ADRs + §8 deferred items + §9 cross-references + §10 D-questions + §11 lessons applied + §12 closeout) + `22.3-http-filter-lua-multi-script-and-per-route/SPEC.md` **authored at THIS commit** (16-section authoritative SPEC: §1 mission + §1.1 empirical-finding-driven scope [AMEND-22.3-1] + §2 non-purposes + §3 framework-survey [0 new primitives] + §4 9th-canonical per-route shape + §5 proto roster [SourceCodes + LuaPerRoute CONSUMED] + §6 PARSE-REJECT roster [arms 3+7 dropped] + §7 stat surface [0 net-new] + §8 fixture-0028 + §9 behavior-contract delta + §10 deferred + §11 5-pin empirical block + §12 PLAN D-questions + §13 RATIFIED-PENDING + §14 BEHAVIOR_CONTRACT bundle + §15 IMPL acceptance + §16 ADR-0193 §Context anchor) + `22.3-http-filter-lua-multi-script-and-per-route/README.md` (updated at THIS commit — Status now "22.3 SPEC done at 2026-05-21; awaiting 22.3 PLAN") + `22.3-http-filter-lua-multi-script-and-per-route/BRAINSTORM.md` (UNCHANGED from `0929b3d`).
- **lifecycle-state:** `phase 22.3 PLAN done; awaiting 22.3 IMPL`. ROADMAP row `22.3` STAYS `in-progress` at this commit per ADR-0106 (already flipped `planned → in-progress` at the 22.3 BRAINSTORM-done commit `0929b3d`; PLAN-done does not change row status per BOOTSTRAP §4.1 invariant 3 — matches the 22.1/22.2 PLAN-done precedent). Parent row `22` STAYS `in-progress` until 22.3 phase-done; there are no further sub-rows (22.3 is the final sub-phase; parent row 22 closes at 22.3 IMPL phase-done — the §9 family drops from 4 remaining rows to 3). **DECISIONS.md tail UNCHANGED at ADR-0193 §Context draft** at THIS PLAN commit (no ADR landing at PLAN per ADR-0044 — the §Decision + §Consequences body + the ADR-0125 §(xiv) AMENDMENT body land at 22.3 IMPL atomic-landing Task). ADR-0188 + ADR-0189 + ADR-0190 + ADR-0191 + ADR-0192 + ADR-0193 §Context + ADR-0177 + ADR-0125 §(xiv) UNCHANGED at this commit. Next-free **ADR-0194** (UNCHANGED; conditional consumption only if the §13-R6 *LState-pool gate trips at the 22.3 IMPL benchmark). **22.3 PLAN authored at THIS commit** as `docs/envoy-go/phases/22.3-http-filter-lua-multi-script-and-per-route/PLAN.md` (7-task breakdown matching the 22.1 + 22.2 PLAN structural template; resolves D-P1/D-P2/D-P3 + R6 + the ADR-0045 split-gate STAY-SINGLE-PHASE disposition; reviewed + approved). **NO production code lands at PLAN** per ADR-0044 — only PLAN.md + STATE/README updates.
- **next-skill:** `superpowers:executing-plans` / `superpowers:subagent-driven-development` (scoped to 22.3 IMPL per SKILL_ROUTING state-3 entry "Phase in ROADMAP, directory exists, SPEC.md + PLAN.md exist, IMPL not done → superpowers:executing-plans / superpowers:subagent-driven-development scoped to THIS phase → output: production code + tests + fixture + ADR bodies + PROGRESS.md + REVIEW.md" — the PLAN→IMPL transition per the phase-22.1 + phase-22.2 precedent at master tips `d30f131` + `46183a4`; subagent-driven preferred per project memory `feedback_execution_style.md`). Cold-start scope for the next session: read STATE.md (this file) + `docs/envoy-go/ROADMAP.md` (row 22.1 done; row 22.2 done; row 22.3 in-progress; row 22 in-progress) + `docs/envoy-go/phases/22.3-http-filter-lua-multi-script-and-per-route/PLAN.md` (THIS commit's 7-task breakdown — the load-bearing IMPL input: Task 0 precondition-verify + Tasks 1-6 [SourceCodes consume / 3-arm validator / 3-tier dispatch + decode-encode wiring / FuzzLuaPerRouteConfig+R6 benchmark / fixture-0028 / atomic landing + parent-row-22 closure]; D-P1/D-P2/D-P3 + R6 resolutions; the deferred-const test-sweep note at Task 2) + `docs/envoy-go/phases/22.3-http-filter-lua-multi-script-and-per-route/SPEC.md` (the 16-section authoritative SPEC — §3.3 file roster + §6 PARSE-REJECT roster + §8 fixture-0028 + §11 empirical closures + AMEND-22.3-1 + §15 IMPL acceptance checklist) + `docs/envoy-go/phases/22.3-http-filter-lua-multi-script-and-per-route/BRAINSTORM.md` (the 8 LOCKED decisions; decision #3 REVISED per AMEND-22.3-1) + `docs/envoy-go/phases/22.2-http-filter-lua-full-bridge/{PLAN.md,PROGRESS.md,REVIEW.md}` (predecessor sub-phase IMPL structural precedent) + `docs/envoy-go/phases/22.1-http-filter-lua-vm-and-headers-bridge/{PLAN.md,PROGRESS.md}` (predecessor sub-phase IMPL precedent) + DECISIONS.md tail (ADR-0193 §Context draft — §Decision+§Consequences land at IMPL; next-free ADR-0194; ADR-0125 §(xiv) anticipation paragraph; ADR-0045 split-gate; ADR-0044 §Decision-body-at-IMPL discipline) + BEHAVIOR_CONTRACT.md `### envoy.filters.http.lua` (22.1 + 22.2 subsections + the `#### Phase 22.2 forward-pointer notes` 22.3-anticipated bullets; 22.3 adds 0 departure records per §9). The 22.3 IMPL session creates a fresh worktree per project memory `feedback_git_worktrees.md`: `git worktree add /home/esa/git/envoy-go/.worktrees/phase-22.3-http-filter-lua-multi-script-and-per-route-impl -b phase-22.3-http-filter-lua-multi-script-and-per-route-impl <22.3-PLAN-tip-SHA>` per the phase-22.1-IMPL + phase-22.2-IMPL sub-phase-IMPL worktree precedent; subagent-driven per-task execution per `feedback_execution_style.md`. The 22.3 IMPL squash-merge CLOSES ROADMAP row 22.3 AND parent row 22 (final sub-phase) + lands ADR-0193 full body + the ADR-0125 §(xiv) AMENDMENT.
- **last-commit:** `1efd4de`. Phase 22.3 PLAN squash-merge of branch `phase-22.3-http-filter-lua-multi-script-and-per-route-plan` from worktree `.worktrees/phase-22.3-http-filter-lua-multi-script-and-per-route-plan/` lands as the squash-merge commit on master per the phase-09..22.2 PLAN squash-merge convention. This placeholder is backfilled by the SHA-fill follow-up commit per the phase-09..22.2 convention: `phase 22.3 PLAN follow-up: STATE.md SHA-fill (TBD → 1efd4de post-squash)`. Predecessor master tip: `6c3c648` — `phase 22.3 SPEC follow-up: STATE.md SHA-fill (TBD → e72af4c post-squash)`. Pre-predecessor: `e72af4c` — `Squash merge phase-22.3-http-filter-lua-multi-script-and-per-route-spec`.
- **last-updated:** 2026-05-21
- **next-free ADR:** `ADR-0194` — UNCHANGED at this 22.3 PLAN commit (NO ADR landing at PLAN per ADR-0044; the ADR-0193 §Context draft anchored at the SPEC commit `e72af4c` STANDS — its §Decision + §Consequences body + the ADR-0125 §(xiv) AMENDMENT body land at the 22.3 IMPL atomic-landing Task). ADR-0193 = NEW combined `internal/filter/http/lua/` 22.3 multi-script + per-route surface (`Lua.SourceCodes` named-script map consume + `LuaPerRoute` 9th-canonical 3-arm oneof + per-route 3-tier dispatch + **upstream-parity dangling-`name` silent no-op per AMEND-22.3-1** [REVISES the BRAINSTORM HCM-build-time-PARSE-REJECT disposition] + no-reserved-name disposition + fixture-0028 + `FuzzLuaPerRouteConfig`; ONE ADR per the 22.1 one-package-shape-ADR economy ADR-0189). Anticipated remaining 22.3 IMPL ADR landings: **1 IN-PLACE AMENDMENT** on `ADR-0125 §(xiv)` (canonical-per-route roster grows 8 → 9; the 9th canonical specification [3-arm hybrid] + SHARED stat-discipline + 9-shape roster table + lua-row first-use citation; NO new ADR number; lands at 22.3 IMPL final Task; the AMENDMENT-anticipation paragraph at parent SPEC commit STANDS UNCHANGED) + **1 conditional ADR-0194** (escape-valve slot per the WEAK HOLD — fires only if the §13-R6 *LState-pool gate trips at the 22.3 IMPL benchmark Task; LIKELY STAYS UNCONSUMED — the SPEC §11 scrape confirmed per-route resolution is an O(1) `Resolve` + content-hash cache lookup, not a new per-stream VM construction). **ADR-0188 EXPLICIT API-REVISION ALLOWANCE clause STAYS scoped to consumer-#2** (UNCHANGED at 22.3 — per-route + multi-script are dispatch-surface extensions, not `internal/lua/` VM-API revisions). Phase-22.3's WEAK HOLD escape-valve buffer at THIS SPEC commit: 0-1 anticipated consumption (ADR-0194 conditional); re-evaluates at 22.3 IMPL benchmark Task.

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
