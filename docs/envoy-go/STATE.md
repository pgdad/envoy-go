# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `05.2-upstream-h2`
- **phase-directory:** `docs/envoy-go/phases/05.2-upstream-h2/` — currently a placeholder created at the planner-time split of phase 05 (per ADR-0045 / commit `2162509`); contains `README.md` only (the illustrative scope-sketch landed at split time; see that file for anticipated 05.2 surface). The next session's `superpowers:brainstorming` run produces `SPEC.md` here per ADR-0004 autonomous mode. The sibling `docs/envoy-go/phases/05.1-downstream-h2/` is now closed read-only history (state-6 reached at this commit; ROADMAP row `05.1` status is `done`). The parent `docs/envoy-go/phases/05-http-2/` retains its `SPEC.md` (`612cdea`) as the master design document for both sub-phases per ADR-0045 and phase-05 SPEC §11.1; that directory remains closed read-only history at the parent level (no PLAN/PROGRESS/REVIEW will land under it; those land under the sub-phase directories). The parent ROADMAP row `05-http-2` stays `in-progress` until 05.2 reaches `done`.
- **lifecycle-state:** `0` — phase 05.2 exists in ROADMAP (row at `05.2 | upstream-h2 | 05.1 | planned`) but its phase-directory holds only the split-time placeholder `README.md`. Per BOOTSTRAP §5 the next transition is state 0 → 1 via `superpowers:brainstorming`, which (a) creates `SPEC.md` under the phase-directory and (b) leaves the ROADMAP row at `planned` until the planning session enters and flips it to `in-progress` (per BOOTSTRAP §4.1 invariant 3, the directory exists *only* when the phase enters `in-progress` — but here the placeholder pre-existed at split time per ADR-0045, an explicit deviation; brainstorming proceeds without flipping ROADMAP). The state-6 phase-done commit for 05.1 landed on this commit on branch `phase/05.1-downstream-h2-finish`, fast-forwarded into `master` per ADR-0003. The 05.1 worktrees (`.worktrees/phase-05.1-downstream-h2-*`, including `…-review-followup`) are retained per the established repo convention (visible across phases 00–04 — none have been swept), available for future ad-hoc cleanup as a separate session.
- **next-skill:** `superpowers:brainstorming` — produces `docs/envoy-go/phases/05.2-upstream-h2/SPEC.md` per ADR-0004 autonomous-mode adaptation. Inputs: phase-05 master SPEC at `docs/envoy-go/phases/05-http-2/SPEC.md` (`612cdea`), the placeholder scope-sketch in this directory's `README.md`, the 05.1 REVIEW carry-forwards enumerated in `next-skill-scope` below, and the closed-history 05.1 phase artefacts (`SPEC.md`, `PLAN.md`, `PROGRESS.md`, `REVIEW.md`) for cross-reference. SPEC review loop is the `spec-document-reviewer` subagent per ADR-0004 (up to three iterations; on non-approval after three, set `lifecycle-state: blocked` and exit per ADR-0004 §3).
- **next-skill-scope:** Draft `docs/envoy-go/phases/05.2-upstream-h2/SPEC.md` covering the upstream-H2 surface as enumerated in the placeholder `README.md`, plus the following 05.1 REVIEW carry-forwards as explicit SPEC §3 inputs (per REVIEW.md `d69446a` Recommendation Path A). **Flow-control discipline ADR (anticipated ADR-0055 — next free after ADR-0054 landed at `fd18c0d`; planner verifies the next-free at SPEC/PLAN write time per ADR-0004):** bundles I-1 (outbound `MaxFrameSize` chunking on `ServerConn.writeData`), I-2 (per-stream send-window enforcement on outgoing DATA), I-3 (inbound WINDOW_UPDATE emission and per-stream `recvW` enforcement), M-3 (`writeData` dead `if taken <= 0` recovery branch — fix is to lift `waitFor`+`reserve` into a single mutex-guarded `reserveBlocking` operation), M-7 (`recvW` dead-field disposition — kept-and-consumed under I-3, deleted otherwise), M-9 (WINDOW_UPDATE delta overflow bounds-check on both `serverStream.recvWindowUpdate` and `ServerConn.onWindowUpdate`). The ADR documents flow-control discipline for the from-scratch H2 codec end-to-end and extends `BEHAVIOR_CONTRACT.md ## HTTP/2` with the threshold language. **Other carry-forward dispositions per REVIEW.md:** M-4 (`readClientPreface` ctx-awareness — defer to phase 06 or 07 listener-manager-level fix per REVIEW), M-5 (extract `translateFramerErr` helper — cosmetic, fold into the I-1/I-2/I-3 ADR or land as a small commit), M-8 (`excludedSubsections` `//nolint:unused` slice — promote to const or move to `CONFORMANCE_PINS.md` doc-prose), M-10 (SETTINGS_TIMEOUT — flag for 05.2 or 06; not a 05.2 hard requirement), M-11 (`recvData` state-check before `reqBody` append — one-line reorder, fold into the flow-control ADR's stream-state hardening), M-12 (`closedStreams` map unbounded growth — cap at last 1024; long-lived-conn concern). **Discovered-during-batch coverage gap carry-forward:** integration test for the monotonic-id-reuse rejection branch at `internal/filter/hcm/h2/conn.go:308-319` (only the even-id branch is covered by `TestServerConn_GOAWAYOnProtocolError_EvenStreamID`). Land in 05.2's PLAN as a small test-addition task; the rejection branch itself is correct and tested at unit level, only the integration coverage is missing. **Optional FU-7 future-tightening (elide the empty trailing DATA frame when `bodyText == ""` in `directResponseAction.writeH2`):** out-of-scope for both 05.1 and 05.2 unless the 05.2 brainstorming session finds an upstream-H2 alignment reason to fold it in. **Non-inputs (already closed in 05.1's follow-up batch):** I-4 + M-1 / M-2 / M-6 / M-13 / M-14 / M-15 / M-16 / M-17 — REVIEW.md §Recommendation Path A wording confirms these need no SPEC §3 mention.
- **last-commit:** this commit — `phase 05.1: downstream HTTP/2` per BOOTSTRAP §5.3, on branch `phase/05.1-downstream-h2-finish`, branched from master `3bb1bb9` per ADR-0003. Updates ROADMAP.md row `05.1-downstream-h2` from `in-progress` to `done` (parent row `05-http-2` stays `in-progress`; row `05.2-upstream-h2` stays `planned`) and advances STATE.md to phase 05.2-upstream-h2 lifecycle-state 0. Master is fast-forwarded to this commit per ADR-0003.
- **last-updated:** 2026-04-26

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
