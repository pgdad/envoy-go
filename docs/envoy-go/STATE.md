# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `13-http-filter-buffer` — phase 13 brainstorm landed at this commit. ROADMAP row 13 added with status `planned` per BOOTSTRAP_PROMPT.md §4.1 invariant 3 + §5 lifecycle-state-0 → 1 transition. Phase 13 is the SIXTH §9 family-row to enter the lifecycle (after cors @ 07.1 done, fault @ 09 done, header_mutation @ 10 done, local_ratelimit @ 11 done, csrf @ 12 done); the §9 family heading at ROADMAP line 56 stays unchanged per ADR-0106(c). Filter selection (`buffer`) was the brainstormer's choice per ADR-0106(b) + the user's interactive Q1+Q2 dialogue (alternatives surfaced were Path B Runtime + hot restart infra phase, plus §9 family-children bandwidth_limit / rbac / others; user picked Path A → buffer).
- **phase-directory:** `docs/envoy-go/phases/13-http-filter-buffer/` — contains `BRAINSTORM.md` (~556 lines) at this commit. SPEC.md, PLAN.md, PROGRESS.md, REVIEW.md authored over phase lifecycle (states 2 → 6). The BRAINSTORM commits 8 design Decisions across §2 (filter package layout / extension-registry registration / 1-consumed-0-deferred MVP envelope + parse-time `max_request_bytes ≤ 1 MiB` validation / cap-layering with ADR-0076 / per-route disabled-OR-override (5th canonical shape) / body-counting + 413-trigger algorithm / stat surface 29→31 / 413 wire shape reuse from ADR-0076), 5 differential-fixture scenarios at §6 (body-fits, CL fast-fail, streaming-overflow, per-route disabled, per-route tighter override), 4 anticipated ADRs at §7 (ADR-0125..ADR-0128), 2 inline-deferral items at §8 (`max_request_bytes > 1 MiB` envoy-go-only parse-time rejection; `per_connection_buffer_limit_bytes` / `per_request_buffer_limit_bytes` silent-ignore inherited from ADR-0076), and 11 empirical-pin obligations at §9 (§11.P1..§11.P11 to be resolved IN-SESSION at SPEC time per ADR-0004). NO sibling-stub was authored per ADR-0106(b). NO off-master prebrainstorm-notes branch was authored.
- **lifecycle-state:** `phase 13 brainstormed; SPEC.md pending` — phase 13 BRAINSTORM.md authored at this commit; SPEC.md not yet authored. Lifecycle-state-1 → 2 transition (BOOTSTRAP_PROMPT.md §5) is the next session's responsibility. Phase 12's six gates remain green at parent commit `a782fc9`; this commit is docs-only (no code changes; no test re-validation needed).
- **next-skill:** `superpowers:writing-plans` per BOOTSTRAP_PROMPT.md §5 lifecycle-state-2 entry condition (SPEC.md exists, PLAN.md does not — phase 13 enters this state once SPEC.md is authored; the routing convention from phases 09/10/11/12 is that writing-plans is called once for SPEC drafting, then again for PLAN drafting based on the SPEC). The SPEC author resolves the 11 §9 empirical-pin obligations IN-SESSION against reference Envoy v1.37.2 per ADR-0004.
- **next-skill-scope:** Cold-start scope for the phase 13 SPEC-authoring session. Read first: `docs/envoy-go/phases/13-http-filter-buffer/BRAINSTORM.md` (the authoritative design doc — every load-bearing decision lives there per D-3.4); `docs/envoy-go/ROADMAP.md` row 13 (now `planned`); `docs/envoy-go/BEHAVIOR_CONTRACT.md ## HTTP filter chain ### envoy.filters.http.csrf` subsection (the just-shipped phase 12 contract — phase 13 mirrors its structural shape with simplifications); `docs/envoy-go/DECISIONS.md` ADR-0076 (framework body cap + 413 wire shape — the load-bearing primitive phase-13 layers above; cap-layering rationale captured at ADR-0126 anticipated) + ADR-0073 / ADR-0117 / ADR-0124 (per-route precedents — phase 13 codifies the 5th canonical shape at ADR-0125 anticipated) + ADR-0120..ADR-0124 (the 5 phase-12 ADRs — phase 13 numbers ADR-0125..ADR-0128 next-free); `internal/filter/http/chain.go` lines 16-31 (the framework's hardcoded `filterBufferLimitBytes = 1 << 20` cap + `localReply413Body = "Payload Too Large"` 17-byte constant — phase-13's body-counting algorithm reuses these verbatim); `docs/envoy-go/phases/12-http-filter-csrf/SPEC.md` + `phases/12-http-filter-csrf/PROGRESS.md` (the most-recent phase artefacts — phase 13 BRAINSTORM is structurally derived from phase-12's). The 11 empirical pins at BRAINSTORM §9 (§11.P1..§11.P11) are the SPEC author's IN-SESSION obligation per ADR-0004; the empirical-pin scrape evidence lands verbatim in SPEC §11. Highest-priority pins for SPEC-time scrape: §11.P1 (reference Envoy's parse-time disposition on `max_request_bytes > 1 MiB`); §11.P3 (`BufferPerRoute` oneof violation); §11.P4 (per-route `disabled: true` bypass discipline + connection-level cap interaction); §11.P6 (Content-Length fast-fail discipline at `DecodeHeaders` time); §11.P11 (empty-body POST disposition — does Envoy increment `request_buffered`?).
- **last-commit:** `6cf412e` — `phase 13 brainstorm: ROADMAP row 13 + STATE.md flip`. Adds ROADMAP row 13 with status `planned`; authors `docs/envoy-go/phases/13-http-filter-buffer/BRAINSTORM.md` (~556 lines) capturing the 4-question interactive design dialogue + 8 design Decisions + 5 differential-fixture scenarios + 4 anticipated ADRs (ADR-0125..ADR-0128) + 2 inline-deferral items + 11 empirical-pin obligations. Docs-only commit; no code changes; phase 12's six gates remain green at parent commit `a782fc9`.
- **last-updated:** 2026-05-08

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
