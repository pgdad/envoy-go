# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

> **This file is a POINTER, not an archive** — `BOOTSTRAP_PROMPT.md` §4: *"STATE.md — pointer: active phase, last commit, next action."* It carries the live pointer, the live counts, and a SHORT recent lineage. **Full stage-by-stage history lives in `STATE_HISTORY.md`** (archived at ADR-0288; **nothing was deleted** — the pre-move file is preserved there verbatim, byte-for-byte).
>
> **⚠️ THE RULE ADR-0288 EXISTS TO ENFORCE: there is EXACTLY ONE live `next-skill:`, `lifecycle-state:`, `next-free ADR:`, and count block IN THIS FILE, and they live in §Current pointer directly below.** Before that move, ~330 lines of prepended backlog had buried TWO fossilised "current state" blocks whose `next-skill:` still read *"the 42.2 hedging BRAINSTORM"* and *"the 46.2 SPEC"* while the live phase was **66** — so `grep 'next-skill:' STATE.md` returned **two stale hits and ZERO live ones**, and `grep 'fuzzers:'` returned **48** and **42** when the true count was **55**. **A session may trust a grep of this file again. Keep it that way: when you advance the pointer, EDIT §Current pointer IN PLACE — never prepend a new block above it.**

---

## Current pointer

*(The ONLY live "what next" in this repo. Update IN PLACE at every stage close.)*

- **active-phase:** `phase 67 (tls-require-client-cert-false) BRAINSTORM done` (2026-07-17)
- **phase-directory:** `docs/envoy-go/phases/67-tls-require-client-cert-false/` (`BRAINSTORM.md` present; row 67 is `in-progress`)
- **lifecycle-state:** `1` (BRAINSTORM done). Row 67 registered `in-progress` at this stage-close commit (ROADMAP §Schema invariant); it flips `done` at the phase-67 IMPL six-gate (ADR-0106, the SOLE leg — no split anticipated).
- **next-skill:** **the phase-67 SPEC** — which must **RE-DERIVE the BRAINSTORM rather than execute it** (`feedback_brief_citations_not_evidence`). It OWES: the **D-RCCF-FETCHFAIL-POSTURE reconciliation** (BC:900 / ADR-0286 D-SDSVC-FETCHTIMEOUT / `internal/tls/config.go:116` all record the reference as serve-anyway on validation-context fetch failure — a SERVER-CERT-probe extrapolation from SPEC-60 §11 arm B — where this session's validation-context probe pinned init-hold-then-FAIL-CLOSED; reconcile by a discriminating server-cert-vs-validation-context fetch-failure probe BEFORE drafting ADR-0289 §Context, never a silent rewrite) · the **D-RCCF-ANCHORLESS-VC-FALSE** reference probe (the ONE unprobed cell: require=false/absent + a `validation_context` present WITHOUT `trusted_ca`) · the **D-RCCF-QUIC** final in/out call (provisionally OUT) · **re-witnessing one pinned reference cell as a probe-session control** (V2's honest limit — the inline/CVC handshake cells rest on this session's 21-arm run) · the **ADR-0289 §Context draft** per ADR-0044-as-used (the citation drift is flagged at BRAINSTORM §7). Honor `feedback_git_worktrees` · `feedback_subagents_no_push` · `feedback_push_to_origin`.
- **last-commit:** the phase-67 BRAINSTORM squash (`git log --grep 'phase 67'`; locate by SUBJECT, never by position). Prior: the phase-66 IMPL squash (`git log --grep 'phase 66'`).
- **last-updated:** 2026-07-17 (phase-67 BRAINSTORM done)
- **next-free ADR:** **`ADR-0289`** — DECISIONS.md tail is **ADR-0288** (re-derived this session). **A BRAINSTORM anchors no ADR and reserves nothing** — ADR-0289 is the anticipated number; the phase-67 SPEC drafts ADR-0289 §Context per ADR-0044.

---

## Project counts at this commit

*(Live. Re-verify at session start — `git fetch` first — and update here at every stage close. Canonical commands inline.)*

- **active differential fixtures:** **111** — `ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0109-xds-sds-combined-validation-context`.
- **fuzzers:** **55** — `grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`. Phase 66 added SEEDS only (a new `downstream-sds` dispatch side in `FuzzTLSContextParse`) ⇒ **+0**.
- **stat surface:** **1201** — BEHAVIOR_CONTRACT doc count. **There is NO mechanical counting command.** Phase 66 was **+0**.
- **BackendKind tail:** **38** (`H2GoawayResponder`).
- **DECISIONS.md tail:** **ADR-0288** (next-free **ADR-0289**) — `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1`. ADR-0287 was COMPLETED in place at the phase-66 IMPL (the tail did NOT flip).
- **go.mod modules:** the tracked lineage figure is **2** (the phase-61.2 count: `quic-go` direct + `qpack` indirect) — **NOT a repo total**; the single `go.mod` requires **67** modules. Phase 66 was **+0**.
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` (see `ENVOY_TARGET.md`).
- **conformance:** h2spec 53/53; proxy-wasm all 10 families PASS.

---

## Recent lineage

*(The last FIVE stages only — enough for the router's "prior-phase lineage" read. Everything earlier is in `STATE_HISTORY.md`. These bullets are carried over VERBATIM; the newest is the phase-67 BRAINSTORM this pointer reflects.)*

- **prior active-phase:** `phase 67 (tls-require-client-cert-false) BRAINSTORM done` (2026-07-17) — the roller's AUTONOMOUS self-pick (no human pick per the 2026-07-12 standing directive) opens the FIFTH xDS-family row (lifecycle-state 0 → 1; docs-only, fresh worktree off master `0d4d4041`, branch `phase-67-tls-require-client-cert-false-brainstorm`, worktree `.worktrees/phase-67-brainstorm`, per `feedback_git_worktrees`). **Subject:** verify-if-presented mTLS — honor `require_client_certificate: false`/absent via `ClientAuth = VerifyClientCertIfGiven` across ALL THREE validation-source shapes (fully-inline / SDS-VC / CVC) at the TCP apply points; the three anchor arms hoist OUT of the require gate (the SDS validation-context fetch fires at boot regardless of require, matching the reference's un-gated fetch), and **the phase-66 E3 reject RETIRES atomically with the lift** (its ONLY enforcement was this row's gap — re-derived, `reference_lifted_reject_hidden_enforcement`). **Why this pick:** the ONLY candidate simultaneously (i) pre-sized SMALL in a landed doc (SPEC-66 §13: *"needs `VerifyClientCertIfGiven` + a fetch-gate restructure"*; §3.10: *"an apply-point reshape, not a branch"*), (ii) fully substrate-reusing (nothing is built — a gate is moved; ONE production function, `NewDownstreamConfig`, ZERO new packages/modules/`internal/xds` symbols; `internal/boot`/`validate/` untouched), and (iii) already reference-PINNED — and the candidate ADR-0287 §Consequences ITSELF chartered (*"Lifting `require == false` belongs in a row that fixes ALL THREE paths together"*). Runner-up: the empty-dynamic Design B fallback; full rejected roster in BRAINSTORM §2.1. **EXECUTED evidence (three parallel input agents, each in PRIVATE scratch):** a **21-fresh-container-arm reference cross-product** ({inline, sdsvc, cvc} × {false, ABSENT} × {trusted, untrusted, none} + a require=true CVC control; arm-unique secret names + a served-this-arm assert in every SDS arm) — verify-if-presented LIVE on all three shapes; **ABSENT ≡ false** (all 9 twins outcome- and stat-identical; the ONLY cell that moves on the flip is `none`); a **Go `crypto/tls` cross-product** — `VerifyClientCertIfGiven` + a real pool matches the reference CELL-FOR-CELL with **byte-identical wire alerts (48 unknown_ca / 116 certificate_required)**; client-observed failure STRINGS differ per side ⇒ fixture pins stay PER-SIDE; **fetch-gating arms** — the reference does NOT gate the SDS fetch on require, init-holds on fetch failure (workers un-started, port UNBOUND in the container netns — never treat a docker-proxy accept as listener liveness) then **FAILS CLOSED** after `initial_fetch_timeout`, identical at require=true/false, no inline-default-CA rescue under CVC ⇒ envoy-go's un-gated fetch makes `require=false`+SDS boot-FAIL-capable — a **NEW ADR-0280-family departure instance** the SPEC must NAME. **The discriminating-probe discipline PAID twice** (`reference_probe_must_discriminate`): the polite-vs-forced client control exposed a **silent cert-withholding vacuous-green trap** (a Go client withholds a cert whose issuer is absent from the acceptable-CA list — `0110`'s driver MUST force-send via `GetClientCertificate`), and the nil-pool arm pinned the WORST-direction hazard (`VerifyClientCertIfGiven` + nil `ClientCAs` falls back to the SYSTEM pool — trusted clients REJECTED, anonymous ACCEPTED) ⇒ **D-RCCF-NILPOOL-GUARD** (the construction order makes the state unrepresentable; assignment-adjacency). **Adversarially verified before landing: THREE independent verifiers (V1 code-claims re-derivation / V2 probe reproduction with fresh PKI + fresh containers / V3 process-consistency) found FIFTEEN distinct defects, ZERO severe — all corrected in place.** The load-bearing finding: a **probe-vs-landed-docs DRIFT** — BC:900 + ADR-0286 D-SDSVC-FETCHTIMEOUT + `config.go:116` record the reference as serve-anyway on validation-context fetch failure (a SERVER-CERT-probe extrapolation from SPEC-60 §11 arm B) where this session's probe pinned init-hold-then-FAIL-CLOSED ⇒ the NAMED SPEC obligation **D-RCCF-FETCHFAIL-POSTURE** (reconcile BEFORE drafting ADR-0289 §Context, ideally by a discriminating server-cert-fetch-failure probe — never a silent rewrite). QUIC client-auth provisionally OUT (`NewQUICDownstreamConfig` never reads the field; final call at the SPEC, **D-RCCF-QUIC**); ONE unprobed cell (require=false + anchor-less inline VC, **D-RCCF-ANCHORLESS-VC-FALSE**) sent to the SPEC; a require=false+RBAC/principal fixture arm NEWLY deferred (§2.11). Fixture: anticipated ONE new **`0110-tls-require-client-cert-false`** (the `0109` chassis + a third arm + the forced-send driver; three-arm cross-side verdict — no-cert→ok is the discriminator vs require=true, untrusted→rejected proves the anchor LIVE). **Envelope:** ZERO new packages / modules / `internal/xds` symbols; +0 stats; anticipated a SINGLE FLAT ROW (~8–12 tasks; ADR-0045 valve armable, no split anticipated). **Row 67 registered `in-progress`** at this stage-close commit per the ROADMAP §Schema invariant; flips `done` at the phase-67 IMPL six-gate (the SOLE leg, ADR-0106). **NO deferred sentence is narrowed at ANY stage** (a §8-tier pickup; the phase-64/66 precedent). **Counts UNCHANGED at this BRAINSTORM (docs-only; count commands re-run MECHANICALLY in the worktree at this close):** stat surface **1201** · fixtures **111** (numeric tail `0109-xds-sds-combined-validation-context`) · fuzzers **55** (`internal/` scope) · BackendKind **38** · DECISIONS tail **ADR-0288** (next-free **ADR-0289**) · go.mod modules **2** (lineage figure; the single `go.mod` requires 67). **Sentinel re-run MECHANICALLY at this close (does NOT fire; `stop` NOT created):** (1) now prints `NOT DONE: row 67` (the registration re-opens check (1) — the loop is held open by ALL THREE checks again); (2) prints THREE live "candidates:" sentences (`grep -cE 'remaining deferred \(not-yet-chartered\) candidates:'` ⇒ **3** — the command, not the adjective); (3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`. **Next → the phase-67 SPEC** (RE-DERIVE the BRAINSTORM rather than execute it; the owed roster is in §Current pointer's next-skill line).

- **prior active-phase:** `phase 66 (xds-sds-combined-validation-context) IMPL done` (2026-07-16) — lifecycle-state 3 → DONE; **ROW 66 FLIPS `in-progress` → `done`** at this IMPL six-gate (ADR-0106, the SOLE leg — `reference_roadmap_split_phase_row_done`). Worktree `.worktrees/phase-66-impl`, branch `phase-66-impl`; subagent-driven (`feedback_execution_style`), each task committing LOCALLY only (`feedback_subagents_no_push`), squashed + pushed by the controller at stage-close. **COMPLETES ADR-0287 IN PLACE** — §Decision + §Consequences added to the SPEC-drafted §Context (ADR-0044); the DECISIONS tail did NOT flip (stays **ADR-0288**; next-free **ADR-0289**). **The deliverable: Design A (pool substitution).** The `commonTLSContextToConfig` `combined_validation_context is not supported in phase 03` reject is LIFTED for DOWNSTREAM + live-provider + `require_client_certificate: true` ONLY (upstream structurally dead behind `NewUpstreamConfig`'s earlier refusal; QUIC + nil-provider keep the BYTE-IDENTICAL substring, ADR-0080), and the SDS-delivered `*x509.CertPool` is installed as `ClientCAs`, winning OUTRIGHT — `default_validation_context.trusted_ca` is NEVER read. **NO `proto.Merge`/`proto.Clone`** (unbuildable — `parseValidationSecret` returns a pool, so no message crosses the seam); the substitution is justified by an **equivalence theorem on FOUR premises** (only `trusted_ca` honored / the `DataSource` specifier oneof REPLACES on merge, OBSERVED / a successful fetch guarantees the specifier is set / **P4: the four inline sub-field rejects RE-POINTED at `default_validation_context`, code-enforced** — an EXECUTED counterexample shows that without P4 a CVC default carrying `match_typed_subject_alt_names: evil.com` would boot a listener authenticating any CA_X-signed client with NO SAN check). **E1/E2** reject the two PGV-`required` halves (envoy-go runs NO PGV) AFTER the retained gate; **E2's reachability is POSITION-scoped** (it fires when any OTHER bootstrap position contributes an SDS count). **E3** rejects CVC + `require_client_certificate` false/absent in `NewDownstreamConfig` (the only apply-point that sees the `DownstreamTlsContext` field) — a LOUD, NAMED divergence, landed ATOMICALLY with the lift in T1 (without it the lift converts a boot-FAIL into a silently unauthenticated listener, `ClientAuth == NoClientCert` — CONFIRMED by deliberate break, twice). `internal/boot`'s `NewSDSProvider` pre-scan grows a THIRD arm counting the CVC's inner SDS half (pure-inline CVC skipped ⇒ `seen == 0` ⇒ the retained reject; `seen > 1` compose-two stays DEFERRED). **`internal/xds`, `parseValidationSecret`, `FetchInitialValidationContext`, and `test/helpers/sdsserver` are BYTE-UNTOUCHED — ZERO new symbols in `internal/xds`, the row's defining property; the 60.2 cycle guard HOLDS.** +1 fixture `0109-xds-sds-combined-validation-context` (served CA_X BEATS inline CA_Y — REPLACE not union, refuting Design C at the differential level; `structuralCheck` RE-DEMONSTRATED, `CompareBytes` live at byte offset 5, a served-this-arm assert ADDED). The three stale/false `internal/tls/config.go` comments are fixed with **PER-ARM nil-provider rosters** — the T8 ROSTER ADJUDICATION: the router conflated two arms' counts; re-derivation by execution gives the CVC gate THREE production consumers, the plain SDS-VC gate TWO (a count is only correct WITH its scope). **Verify (controller-run on the frozen HEAD `3f600cbc`):** six-gate ALL GREEN (`gofmt` silent · `go vet` 0 · `go build` 0 · `go mod tidy -diff` EMPTY · `git diff master -- go.mod go.sum` NO DIFF · `golangci-lint run ./...` 0); cycle guard HOLDS (`go list -deps ./internal/xds` → `internal/stats` + `internal/xds` ONLY); `-race` ok (tls 1.126s / boot 1.054s, no `init_fetch_timeout` flake recurrence); the FULL **111-dir** differential `ok  github.com/pgdad/envoy-go/test/differential  380.044s` EXIT 0 (110 pre-existing byte-stable, `0109` new+green). **Exit counts:** fixtures **110 → 111** (`0109`) / stat surface **1201 (+0)** / fuzzers **55 (+0, seeds)** / BackendKind tail **38 (+0)** / +0 packages / go.mod modules **2 (+0)** / DECISIONS tail **ADR-0288** (ADR-0287 COMPLETED in place). **Sentinel re-run MECHANICALLY (does NOT fire; `stop` NOT created):** (1) prints **NOTHING** (row 66 is `done`); (2) prints **THREE** live "candidates:" sentences (HTTP/3, xDS, Observability — UNCHANGED by this row); (3) prints **THREE** never-opened families (gRPC/Runtime/WASM). Checks (2)+(3) print ⇒ abundant work remains. **Next → the phase-67 BRAINSTORM** (the roller SELF-PICKS the subject per the 2026-07-12 standing directive).

- **prior active-phase:** `phase 66 (xds-sds-combined-validation-context) PLAN done` (2026-07-16) — lifecycle-state 2 → 3; docs-only, worktree `.worktrees/phase-66-plan`, branch `phase-66-plan`, off master `2c5802ee`. **CORRECTED SIX SPEC-66 DEFECTS, one SEVERE and SECURITY-RELEVANT (C1):** the row's headline `require_client_certificate: true` scope is **UNENFORCEABLE** at the SPEC's apply-point — `commonTLSContextToConfig` takes a `*CommonTlsContext` and the field lives on `DownstreamTlsContext` — and since the CVC arm is **unconditional today**, the row as specified would convert a **loud boot-FAIL into a silently unauthenticated listener** (`ClientCAs` nil, `ClientAuth == NoClientCert`). Fix: **E3**, an explicit reject in `NewDownstreamConfig`, landing **atomically with the lift in T1** (F5). C2: E1/E2's **order** vs the retained gate was unspecified and every order breaks something ⇒ gate FIRST; **E2 is reachable ONLY multi-listener** (the pre-scan is GLOBAL) — refuting both the SPEC and a verifier. C1b: the CVC envelope has FOUR fields, two unguarded. C3: **THREE** nil-provider consumers (+2 exported test-only ctors), not two. C4: DECISIONS tail is **ADR-0288**; ADR-0287 already exists ⇒ the IMPL **completes it in place**, no tail flip. C5: §9 has seven items, §11 said six. **THIS PLAN'S OWN first draft carried NINE defects found by an adversarial pass** (headline claims all SURVIVED, several confirmed by execution) — incl. **F4** (the apply-point skipped `ParseSDSConfig` ⇒ a malformed `sds_config` silently accepted + the `tls: ` fuzz invariant broken), **F2** (T2's RED-first was VACUOUS), **F5**, **F3**. **The lesson: knowing a failure mode by name does not prevent committing it** — each was made by an author who had just written the memory's name into the document. **9 tasks, NO split** (ADR-0045 escape-valve armable, unconsumed). Counts UNCHANGED: fixtures **110** · fuzzers **55** · stat **1201** · go.mod **2** (repo 67). **NEXT → the phase-66 IMPL.**

- **prior active-phase:** `phase 66 (xds-sds-combined-validation-context) SPEC done` (2026-07-16) — lifecycle-state 1 → 2; docs-only (NO production `.go`; the §3.3 `proto.Merge` probe + the §3.7 `validate` probe were THROWAWAY dirs DELETED before commit), worktree `.worktrees/phase-66-spec`, branch `phase-66-spec`, off master `b9f5a1c8`, per `feedback_git_worktrees`. **ROW 66 STAYS `in-progress`** (the IMPL flips it `done` at its six-gate, ADR-0106). **ADR-0287 §Context DRAFTED** at this SPEC per ADR-0044 (§Decision/§Consequences at the IMPL); DECISIONS tail **ADR-0286 → ADR-0287**, next-free **ADR-0288**. **⚠️ THE SPEC OVERTURNED THE BRAINSTORM'S CENTRAL IMPLEMENTATION PLAN, BY EXECUTION.** The BRAINSTORM's planned `proto.Clone` + `proto.Merge` apply-point is **STRUCTURALLY IMPOSSIBLE**: `internal/xds`'s `SecretProvider.FetchInitialValidationContext` returns an **`*x509.CertPool`**, and `parseValidationSecret` consumes-and-discards the `CertificateValidationContext` message — **no message crosses the seam, so `proto.Merge` has no operand**. The BRAINSTORM ran `proto.Merge` exactly as it told itself to, and still planned unbuildable code, because it **CITED the seam's return type**. **Design ADOPTED: pool substitution** (the SDS pool wins; `default_validation_context.trusted_ca` is not read) — justified by an **EQUIVALENCE THEOREM** (SPEC §3.2) proven from executed/re-derived premises and **adversarially attacked without a counterexample**: envoy-go honors ONLY `trusted_ca`; `proto.Merge` recurses into it as a message while its `specifier` **oneof** REPLACES (OBSERVED); a successful fetch **guarantees** that specifier is set (`dataSourceBytes` errors otherwise); ⇒ merged pool ≡ SDS pool exactly, and the hybrid-`DataSource` artifact (`watched_directory`) is inert. **Premise P5 is a COINCIDENCE, not a guarantee** — `main.go` passes the same `filepath.Dir(*cfgPath)` to both `NewSDSProvider` and `boot.Construct`, and `dataSourceBytes`/`loadDataSource` are arm-for-arm identical; **if either diverges the theorem is SILENTLY falsified**. **Rejected: CA-pool union** (REFUTED by live probe — the reference accepts the SDS CA and rejects the default's with `UNKNOWN_CA`; a union accepts both) and **a message-returning seam** (exact fidelity, but +1–2 `internal/xds` symbols + relocating `parseValidationSecret`'s rejects — the lift path if the departure is later closed). **⇒ D-COMBVC-HYBRID-DS (the BRAINSTORM's "sharpest risk") and D-COMBVC-CLONE BOTH DISSOLVE** — no merge, no hybrid, no clone guard. **ONE NAMED DEPARTURE (PROBED):** a served VC with no usable `trusted_ca` ⇒ the reference ACKs, falls back to the **DEFAULT CA**, and **SERVES TRAFFIC**; envoy-go **boot-FAILs** (ADR-0280 family). Siblings `trusted_ca:{}` / `{filename:""}` are **PGV-NACKed** by the reference ⇒ traffic agrees, lifecycle diverges. **Never phrase it as "envoy-go rejects where the reference rejects" — false for the empty case.** **NEW OBLIGATIONS the BRAINSTORM never named:** **BOTH** CVC halves are **PGV-`required`** (D-COMBVC-PUREINLINE **REFUTED** — its "legal per the proto — verified" was false) while **envoy-go runs NO PGV repo-wide** ⇒ **TWO new explicit rejects (E1/E2)**; and the four inline sub-field rejects are **BYPASSED** under CVC (the oneof nils `GetValidationContext()`) ⇒ **re-point at `default_validation_context`, each shown RED first**. **D-COMBVC-REQUIRE-FALSE DISPOSED EXPLICITLY** (not by silence): the reference **HONORS** the anchor at `require==false` (verify-if-presented, PROBED); envoy-go silently ignores it on **all three** paths ⇒ **SCOPED OUT as a NAMED BOUNDARY**, carried to §13. **⚠️ THE SPEC'S OWN FIRST DRAFT CARRIED FOUR DEFECTS — ONE SEVERE — all found by an adversarial pass, all recorded in SPEC §1.2 rather than hidden, every one in a section where it was MOST confident it had already caught the error.** **SEVERE:** *"QUIC is the CVC guard's ONLY live consumer"* is **FALSE** — **`validate.Bootstrap` passes a nil provider too**, so `side=="downstream" && provider==nil` is reachable on an **ORDINARY TCP listener** (proven by EXECUTING `validate.BootstrapFile`). It was **inherited from a FALSE LANDED CODE COMMENT** in the very doc region the SPEC audited for stale comments — and a THIRD comment in the same file contradicts it. **It was bound verbatim for DECISIONS.md.** ⇒ the gate MUST keep the `provider != nil` conjunct (TWO consumers), and a new boundary is named: the reference's `--mode validate` **ACCEPTS** a valid CVC config while envoy-go's `validate` path **REJECTS** it. **Also:** "silently boots with no trust anchor" was a **false control-flow claim** (it boot-FAILs loudly); the `BoolValue` probe was **VACUOUS** (one non-discriminating input carrying a negative conclusion — `BoolValue` IS extensionally OR in all four combos); and the §5.3 roster claimed "re-enumerated in full" while carrying **8 of 15** fields (now enumerated **by reflection**). **Counts UNCHANGED (docs-only, re-verified this session, `git fetch` first):** fixtures **110** · fuzzers **55** · stat surface **1201** · BackendKind **38** · go.mod modules **2** (repo total **67**). **Anticipated at IMPL:** fixtures **110 → 111** (`0109-xds-sds-combined-validation-context`) · stat **1201 (+0)** · fuzz **55 (+0)** · ZERO new packages · **ZERO new symbols in `internal/xds`** (preserved by the theorem, NOT by the BRAINSTORM's reasoning). **The deferred sentence is UNCHANGED by this row at EVERY stage** (`combined_validation_context` was never in one — a §8-tier pickup; the phase-64 precedent). **Sentinel re-run MECHANICALLY — does NOT fire; `stop` NOT created.** **NEXT → the phase-66 PLAN** — which must NOT write `proto.Merge`/`proto.Clone` (the BRAINSTORM's §6 still instructs them and they do not compile), must show the four bypassed rejects RED first, and must **RE-DERIVE this SPEC rather than execute it** (`feedback_brief_citations_not_evidence` — and now: **a landed CODE COMMENT is not evidence either**).

- **prior active-phase:** `phase 66 (xds-sds-combined-validation-context) BRAINSTORM done` (2026-07-16) — the roller's AUTONOMOUS self-pick (no human pick per the 2026-07-12 standing directive) opens the FOURTH xDS-family row (lifecycle-state 0 → 1; docs-only, fresh worktree off master `d545810a`, branch `phase-66-xds-sds-combined-validation-context`, worktree `.worktrees/phase-66-brainstorm`, per `feedback_git_worktrees`). **Subject:** `combined_validation_context` — lift the `commonTLSContextToConfig` `combined_validation_context is not supported in phase 03` reject (DOWNSTREAM + live-provider + `require_client_certificate: true` ONLY; upstream + QUIC keep the BYTE-IDENTICAL substring, ADR-0080) and MERGE the SDS-delivered `CertificateValidationContext` phase 65 landed onto the inline `default_validation_context`, installed as the downstream mTLS `ClientCAs`. **Why this pick:** the SMALLEST candidate sized this session — ~4 edit sites across `internal/tls/config.go` + `internal/boot/boot.go`, and **ZERO new symbols in `internal/xds`** (CVC is a LISTENER-CONFIG shape, not a SERVED-SECRET shape, so the served `tls.v3.Secret` wire is byte-identical to `0108`'s and the whole xds chain + `parseValidationSecret` + `FetchInitialValidationContext` + `test/helpers/sdsserver` are UNTOUCHED; ~95% of `0108`'s driver is reusable) — AND the candidate phase 65 ITSELF named as *"a natural follow-on ONCE plain SDS-`validation_context` lands"*, a precondition row 65 now satisfies. **⚠️ The router's PROVISIONAL pick (`xds-sds-upstream-server-cert`) was OVERTURNED ON EVIDENCE** (BRAINSTORM §2.1.1 — the correct outcome per the router's own instruction, not a deviation): it is blocked by a **VALUE-level constructibility cycle** — `boot.NewSDSProvider` needs `grpcclient.New(cm)` needs the very `*cluster.Manager` that `buildCluster`/`NewUpstreamConfig` is constructing, so the provider CANNOT exist when the upstream TLS config is built — plus a listeners-only pre-scan and an eager `upstreamCfg`/`extractH2Mode` coupling. It needs a boot-model reshape (two-pass cluster build or lazy per-Dial TLS) and is NOT smallest. **The import graph is FINE** (`cluster` already reaches `xds` transitively), which is exactly why `reference_xds_config_seam_transitive_cycle_guard` — a TYPE-level guard — did not catch it; the mechanism is recorded in §2.1.1 so the next roller does not re-derive it. **Rejected (BRAINSTORM §2.1):** upstream SDS (above) / DataSource `environment_variable` (blocked on the D-ENV-HARNESS seam SPEC-63 already re-derived and DECLINED; the value-carrying PEM arm cannot use `0106`'s key-presence trick) / HTTP/3 `QuicProtocolOptions` (no reject to lift — the knobs are silently ignored; ~1 of 9 maps cleanly to quic-go; the `0104` observable does not move when they change; genuine proof needs a QUIC transport-param inspector no test has) / tracing `custom_tags` `metadata` (~19 call-site threading, ≥3 pre-chain, a signature change, a probable package extraction) / SDS rotation / the compose-two edge / the `ssl` stat family (framework surgery) / CDS/EDS/LDS/RDS/ADS/Delta/RTDS/`google_grpc` / opening gRPC-Runtime-WASM. **Central design decisions (SPEC to RUN, not cite):** established by EXECUTING `proto.Merge` — `trusted_ca` is a **message**, so Merge **recursively merges** it (NOT the proto's "singular override" bullet), and the default's non-oneof sub-fields (`watched_directory` — inert in envoy-go, LIVE in the reference) SURVIVE a dynamic override into a **hybrid `DataSource`** (D-COMBVC-HYBRID-DS, the sharpest risk); Merge **mutates dst**, which points into the parsed bootstrap ⇒ a `proto.Clone` guard is MANDATORY (D-COMBVC-CLONE); CVC + `require_client_certificate` false/absent is silently IGNORED, inherited from phase 65 (D-COMBVC-REQUIRE-FALSE); the four inline sub-field rejects are BYPASSED under the CVC oneof (D-COMBVC-REJECT-REPOINT); `NewSDSProvider`'s pre-scan does not match the CVC wrapper and returns a silent `(nil, nil)` (D-COMBVC-PRESCAN). **⚠️ This BRAINSTORM was ADVERSARIALLY VERIFIED before landing and ELEVEN defects were found IN ITS OWN FIRST DRAFT** — all corrected; the lesson is banked at §11: **quoting is not executing.** The severe defects all traced to ONE omission (it quoted `proto.Merge`'s docs instead of RUNNING it), plus a silently-TRUNCATED "verbatim" proto quote that had manufactured a false central risk, and a DROPPED phase-65 §8 carry-forward. **A BRAINSTORM is not evidence either.** Fixture: anticipated ONE new `0109-xds-sds-combined-validation-context`. **Envelope:** ZERO new packages / ZERO new modules / ZERO new symbols in `internal/xds` / +0 stats; anticipated a SINGLE FLAT ROW (~6–9 tasks, ADR-0045 escape-valve armable but no split anticipated). **Row 66 registered `in-progress`** at this BRAINSTORM commit per the ROADMAP §Schema invariant; it flips `done` at the phase-66 IMPL six-gate (the SOLE leg — NO parent rollup, ADR-0106). **This row does NOT narrow any deferred sentence** — `combined_validation_context` was never in one (a §8-tier pickup; the phase-64 precedent, whose BRAINSTORM `7d724423` left the Observability sentence byte-identical). **Counts UNCHANGED at this BRAINSTORM (docs-only; re-verified against master tip `d545810a`, `git fetch` first):** stat surface **1201** · fixtures **110** · fuzzers **55** · BackendKind **38** · DECISIONS tail **ADR-0286** (next-free **ADR-0287**) · go.mod modules **2**. **Sentinel re-checked MECHANICALLY (does NOT fire):** (1) now prints `NOT DONE: row 66` (row 66 registered `in-progress` — check (1) BLOCKS `stop` again, having been silent at the phase-65 IMPL); (2) prints THREE live "candidates:" sentences (HTTP/3 `:175`, xDS `:185`, Observability `:193` — UNCHANGED by this row); (3) prints `NEVER OPENED: gRPC`, `Runtime`, `WASM`. Any of (1)/(2)/(3) printing ⇒ the sentinel does NOT fire; no `stop` file created. **Next → the phase-66 SPEC** (which must RUN `proto.Merge`/`proto.Clone` against real messages and pin D-COMBVC-HYBRID-DS + D-COMBVC-CLONE from OBSERVED behavior, then live-probe the reference for the hybrid-`DataSource` question and dispose D-COMBVC-REQUIRE-FALSE explicitly rather than by silence; D-COMBVC-REFMERGE is a cheap confirmation, NOT a gate).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
