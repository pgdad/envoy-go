# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `11-http-filter-local-ratelimit` — SPEC.md landed at this commit (ROADMAP row 11 added with status `in-progress` per `BOOTSTRAP_PROMPT.md` §4.1 invariant 3; phase directory `docs/envoy-go/phases/11-http-filter-local-ratelimit/` now contains `BRAINSTORM.md` + `SPEC.md`). Phase 11 is the FOURTH §9 family-row to land (after cors @ 07.1 done, fault @ 09 done, header_mutation @ 10 done); SPEC commit advances the row through the `in-progress` window that closes at the phase-done commit. Per ADR-0106 flat-top-level-rows discipline, phase 11 stays a top-level row, NOT a sub-phase of any §9 parent.
- **phase-directory:** `docs/envoy-go/phases/11-http-filter-local-ratelimit/` — contains `BRAINSTORM.md` (605 lines; commits `59e1be2` + `6ad8d8a` on branch `phase-11-http-filter-local-ratelimit-brainstorm`) + `SPEC.md` (1225 lines authored this session; mirrors phase 10's 16-section structure adapted for phase 11; spec-document-reviewer Approved on iteration 2 after 3 mechanical cross-reference fixes — see commit body for fix list). PLAN.md, PROGRESS.md, REVIEW.md follow in subsequent sessions per the phase 09 / 10 precedent.
- **lifecycle-state:** `spec-complete` — SPEC.md is the single output of this session (project's adapted lifecycle-state 2 → 3 advanced this session by authoring SPEC.md + executing all 8 §11 empirical pins IN-SESSION against reference Envoy v1.37.2 + landing the spec-document-reviewer approval + adding ROADMAP row 11 with status in-progress, mirroring phase 09 + 10's SPEC-merge pattern). The §11 empirical-pin block landed THREE amendments to BRAINSTORM hypotheses + TWO forward-carrying findings. Amendments: §11.5 stat-name shape — the Prometheus label is `envoy_local_http_ratelimit_prefix` (filter-specific tag-extractor), NOT `envoy_http_conn_manager_prefix` as BRAINSTORM Decision 6 hypothesized; §11.3 server-header value — Envoy emits `server: envoy` (NOT `envoy-go` as BRAINSTORM §1.1 item 8 hypothesized), and envoy-go's existing `serverHeader()` already returns literal `"envoy"` so NO code change is needed; §11.7 refill-timing tolerance narrowed from BRAINSTORM-hypothesized ±20ms to empirically-confirmed ±10ms (boundary is sharp at ≤5ms granularity in reference Envoy). Forward-carrying findings: §11.2c — `fill_interval` 50ms minimum is a FILTER-INTERNAL Envoy check (error path `source/server/config_validation/server.cc:76`; bare error `local rate limit token bucket fill timer must be >= 50ms`), NOT a PGV constraint; §11.2/§11.4 — `filter_enabled` + `filter_enforced` `RuntimeFractionalPercent` fields default to 0% (off), so fixture configs MUST set both to 100% on BOTH the reference Envoy and envoy-go sides for differential equivalence to hold (envoy-go silent-ignores the configured 100% but its always-on behavior matches the explicit 100%). ADR list collapsed 7 → 6 (ADR-0114..ADR-0119; ADR-0120 omnibus dropped per §8.1 — its 14-field deferral content folds inline into §13.1 BEHAVIOR_CONTRACT subsection + §13.5 forward-pointer notes). Differential fixture 0013 stays at 4 scenarios (no scenario-drop unlike phase 10's 5→4). Stat surface 22→26 names. The next session's input is SPEC.md.
- **next-skill:** `superpowers:writing-plans` per ADR-0005 routing — the next session's WORK is to author PLAN.md (per the phase 09 + 10 precedent: BRAINSTORM → SPEC → PLAN → impl → review). Per the user's persistent preference for subagent-driven over inline execution recorded in `~/.claude/projects/-home-esa-git-envoy-go/memory/MEMORY.md`, the PLAN-authoring session may run inline (PLAN.md is collaborative drafting; the framework + filter + fixture + ADRs surface is well-bounded by SPEC.md so inline drafting is natural) while the subsequent PLAN-execution session is subagent-driven per `superpowers:subagent-driven-development`.
- **next-skill-scope:** Cold-start scope for the PLAN-authoring session. Read first: `docs/envoy-go/phases/11-http-filter-local-ratelimit/SPEC.md` (full; 1225 lines; this session's output); `docs/envoy-go/phases/11-http-filter-local-ratelimit/BRAINSTORM.md` (full; 605 lines); `docs/envoy-go/phases/10-http-filter-header-mutation/PLAN.md` (most recent structural precedent for PLAN.md shape — task-by-task decomposition matching `BOOTSTRAP_PROMPT.md` §5 + 10's task count; mirror its preamble + per-task shape); secondarily `docs/envoy-go/phases/09-http-filter-fault/PLAN.md`. Read `docs/envoy-go/DECISIONS.md` ADR-0004 / ADR-0005 / ADR-0040 / ADR-0072 / ADR-0073 / ADR-0074 / ADR-0100 / ADR-0102 / ADR-0103 / ADR-0106 / ADR-0108..ADR-0113. Read `internal/filter/http/perroute.go` (existing 3-tier `Resolve`; phase 11 reuses; NO `ResolveAllTiers` invocation); `internal/filter/http/fault/fault.go` (SendLocalReply + StopIteration precedent at line 321); `internal/filter/hcm/codec.go` (`serverHeader()` returning literal `"envoy"`); `internal/admin/stats.go` (existing tag-extractor registration site — relevant to SPEC §12 deferred decision 1); `cmd/envoy-go/main.go:111–116` (registration site; phase 11 adds one line after `header_mutation` registration). The session emits PLAN.md only (lifecycle-state 3 → 4 in the project's adapted lifecycle); the spec is fixed and the planner decomposes it into ~12–16 tasks per SPEC's §1.4 estimate. PLAN must settle SPEC's §12 deferred decisions D1 (tag-extractor registration site), D2 (filter-callback wiring hook), D3 (PGV plumbing mechanism), D4 (scenario-3 retry-with-deadline option), D5 (test-only clock injection — default: skip). Branch: spawn next worktree `phase-11-http-filter-local-ratelimit-plan` off this SPEC tip per the user's worktree-per-stage preference (B in this session's continuation dialogue).
- **last-commit:** `TBD` — `phase 11 spec: http-filter-local-ratelimit [SPEC.md; ROADMAP row 11 → in-progress]`. Lands `SPEC.md` (1225 lines) for phase 11 (`http-filter-local-ratelimit`); appends ROADMAP row 11 with status `in-progress` (row did not exist at BRAINSTORM commit — added directly with in-progress per BRAINSTORM §10 prescription); advances STATE.md lifecycle 2 → 3; anticipates ADRs ADR-0114..ADR-0119 (6 ADRs per SPEC §8 — one fewer than BRAINSTORM anticipated, per §8.1 consolidation: ADR-0120 omnibus deferral folds inline into §13.1 + §13.5). All 8 BRAINSTORM §9 empirical pins resolved IN-SESSION per ADR-0004 against Envoy v1.37.2 image SHA `c5e8a68e52f4d4697a9adb280dbe415d77fedf1257e183dcb86205bd438f18bd` (probe date 2026-05-05). Spec-document-reviewer Approved on iteration 2 after 3 mechanical cross-reference fixes (lingering ADR-0120 citations rephrased to inline forward-pointers; §5.5 stat_prefix `foo`→`qux` to match §7.1 + §7.3; §1.1 safe-prefix list completed to 5-value form `foo`/`bar`/`baz`/`qux`/`strict`). The §9 family heading at `ROADMAP.md` line 56 stays unchanged per ADR-0106(c). The SPEC worktree branch `phase-11-http-filter-local-ratelimit-spec` (branched from `phase-11-http-filter-local-ratelimit-brainstorm` tip `6ad8d8a`, NOT from master per user-selected option B in the session continuation dialogue) carries this commit; ff-merge into master is deferred until phase-done close per the worktree-per-stage stacking pattern. SHA filled in a follow-up commit per the phase-04..10 SHA-fill convention.
- **last-updated:** 2026-05-05

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
