# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** (none) — phase 16 fully closed at this commit (state-6). Phase 16 stayed the NINTH §9 family-row (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11, csrf @ 12, buffer @ 13, compressor @ 14, bandwidth_limit @ 15) and is the FIRST §9 row since phase 14 to introduce non-zero framework deltas — TWO new primitives landed: (i) TLS-principal accessor `DecoderFilterCallbacks.DownstreamPrincipal() []string` anchored at ADR-0144 (Task 6 — `callbacks.go` interface + `chain.go` plumbing + `hcm/connection.go` + `hcm/h2dispatch.go` TLS state extraction + Group 7 tests landed); (ii) matcher-engine evaluator at new top-level `internal/matcher/` package anchored at ADR-0142 (Task 3 — NEW package + `Matcher` type + `New(tree, supportedActionTypes)` + `Evaluate(MatchContext)` + parse-time PARSE-REJECT for unknown terminal TypeURLs landed). PLAN ran single-row (NOT split via ADR-0045) per phase-13/14/15 LoC-borderline precedent; production LoC ~2778 (rbac.go 1191 + evaluator.go 854 + doc.go 148 = 2193 LoC rbac; matcher.go 510 + doc.go 75 = 585 LoC matcher) above SPEC-time ~1500 estimate but stayed coherent. 7 anticipated ADRs landed (ADR-0140..ADR-0146) per the per-ADR Lands-in-task map + 1 UNANTICIPATED ADR-0147 surfaced at Task 13 follow-up per ADR-0044 ADR-on-impl escape-valve (phase-03 TLS-layer `require_client_certificate=true` blanket-rejection LIFTED scoped to well-formed mTLS configs to enable fixture 0018 scenario 6 mTLS-principal exercise) + ADR-0125 §(xii) amendment paragraph authored at Task 10 per planner-time decision 14. Fixture 0018 (8 scenarios incl. mTLS scenario 6) landed; 19/19 differential fixtures green at phase-done. 20th fuzzer `FuzzRBACConfigParse` landed at Task 11. BEHAVIOR_CONTRACT 6-edit bundle landed at Task 15 (§13.1 `### envoy.filters.http.rbac` AFTER bandwidth_limit per planner-time decision 19 + §13.2 stat-table 60→64 names extension + §13.3 fixture 0018 Equivalence Matrix row + §13.4 Phase 16 forward-pointer notes + §13.5 NEW `## HTTPFilterCallbacks` umbrella with `### DownstreamPrincipal accessor` + §13.6 NEW `## Matcher engine framework primitive` top-level section). 6 phase-done gates verified at Task 15.
- **phase-directory:** `docs/envoy-go/phases/16-http-filter-rbac/` — BRAINSTORM.md (683 LoC) + SPEC.md (2004 LoC) + PLAN.md (758 LoC) + PROGRESS.md (1373 LoC; 16-task ledger) + REVIEW.md (297 LoC) all landed. Phase fully closed.
- **lifecycle-state:** `phase 16 done; REVIEW landed; awaiting next brainstorm` — state-6 entry per ADR-0005 §Decision 4. All 16 PLAN tasks executed (Task 1 PROGRESS preamble through Task 16 REVIEW.md). 6 phase-done gates verified GREEN at Task 15: Gate A (build/vet/lint clean) + Gate B (race tests clean) + Gate C (h2spec 53/53 PASS) + Gate D (20 fuzzers green at 30s each) + Gate E (19 differential fixtures 0000-0018 PASS) + Gate F (BEHAVIOR_CONTRACT §13.1-§13.6 populated). REVIEW.md landed at Task 16. Squash-merge to master at `a0bb191`. Next session is a BRAINSTORM session for the next §9 family-row (TBD by ROADMAP).
- **next-skill:** (none — phase 16 complete; awaiting next brainstorm session). The next session is a BRAINSTORM session (`superpowers:brainstorming`) for the next §9 family-row (TBD by ROADMAP).
- **next-skill-scope:** Cold-start scope for the next BRAINSTORM session. The next §9 family-row's identity is TBD by ROADMAP at brainstorm-time. Read first: `docs/envoy-go/ROADMAP.md` (post-phase-16 status + next-family-row candidates); `docs/envoy-go/phases/16-http-filter-rbac/REVIEW.md` (phase-16 retrospective + 15 deferred items / open follow-ups for future-phase consideration: `:path` injection unit-test gap, scenarios 4+5 destination_port/direct_remote_ip canonical end-to-end deferral, Prometheus tag-extractor structural-close, dead-code perSideScrape+mutex in driver.go, SafeRegex per-call compilation tech-debt, rbac_test.go file split, shadow access-log integration, response_code_details framework primitive, LOG-action access_log_hint primitive, CEL three-field condition evaluation, Principal_Custom v1.32.4 binding-absent workaround, log.Printf in resolvePerRouteConfig, track_per_rule_stats N-cap foot-gun, Principal_Set/Permission_Set recursion-depth foot-gun, SPEC §13.2 line 1842 retains hypothesis form); recent §9 BRAINSTORM precedents (`docs/envoy-go/phases/15-http-filter-bandwidth-limit/BRAINSTORM.md` + `docs/envoy-go/phases/14-http-filter-compressor/BRAINSTORM.md`).
- **last-commit:** `a0bb191` — `Squash merge phase-16-http-filter-rbac-impl`. Squashed 22 worktree commits (16 PLAN-task commits + 6 code-review follow-ups: Task 1 stylistic, Task 3 misspell, Task 4 lint + RangeMatch strict-parse, Task 6 doc-comment accuracy, Task 8 doc-comment + ADR-0145 Prometheus trade-off, Task 14 gofmt). Lands ~13159 net LoC across 45 files: rbac filter package + matcher framework primitive + framework deltas (callbacks.go DownstreamPrincipal interface + HCM TLS-state extraction + `:path` pseudo-header injection symmetric with `:method`/`:authority`) + fixture 0018 (8 scenarios incl. mTLS scenario 6) + 6-edit BEHAVIOR_CONTRACT + 7 anticipated ADRs (ADR-0140..ADR-0146) + 1 unanticipated ADR-0147 (mTLS-lift escape-valve) + ADR-0125 §(xii) amendment + ROADMAP row 16 in-progress→done + STATE.md state-4→state-5 advance + PROGRESS.md 16-task ledger + REVIEW.md retrospective. Predecessor on master: `948f6c4` `phase 16 plan follow-up: STATE.md SHA-fill (TBD → 40f030b post-squash)`. This `phase 16 impl follow-up` commit is the SHA-fill follow-up (TBD-task-15 → `a0bb191` post-squash) + state-5 → state-6 advance per phase-13/14/15 close pattern.
- **last-updated:** 2026-05-12
- **next-free ADR:** `ADR-0148` — phase-16 impl landed 7 anticipated ADRs (ADR-0140..ADR-0146) + 1 unanticipated ADR-0147 (Task 13 follow-up TLS-layer mTLS-lift per ADR-0044 escape-valve) + ADR-0125 §(xii) amendment paragraph at Task 10. Next-free advanced past ADR-0147 to ADR-0148. Future §9 family-row brainstorm sessions anchor against this next-free.

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
