# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `phase 32.1 (network-filter-redis-upstream-pool-seam-and-codec) IMPL done` (2026-06-08, this commit) — **the phase-32.1 IMPL COMPLETE: all 16 TDD tasks landed (`docs/envoy-go/phases/32.1-network-filter-redis-upstream-pool-seam-and-codec/PLAN.md` + `PROGRESS.md`) — the upstream-pool seam (`UpstreamConn`/`UpstreamDialFunc` in `internal/filter/network/upstreampool.go`), the RESP codec (request + reply decode + encode, `resp.go`), the `redisproxy` package foundation (10th built-in, config parse, byte-stable rejects, PING/AUTH local-reply, 10-name EAGER stat roster, the `Handle` pump), the `TCPRedisResponder` BackendKind (value 32), the `0055-redis-roundtrip` cross-side + `0056-redis-boot-reject` fixtures, the NEW `GET /stats` flat admin endpoint (the EIGHTH admin endpoint, required for the `StatsAsserter`; +10 stats accounted there), and the ADR-0230 §Decision/§Consequences body + the ADR-0229 §Decision/§Consequences 32.1 half landed in place (per ADR-0044). Counts advanced: stat surface 536 → **546**, differential fixtures 56 → **58**, fuzzers **40** (unchanged), BackendKind tail **32**, DECISIONS tail **ADR-0230**. Six gates GREEN (build / vet / golangci-lint / `-race -short` / the full 58-dir differential `ok` / h2spec 53/53 + proxy-wasm 10/10). **Next → the 32.2 SPEC** (`network-filter-redis-commands-and-stats`, `superpowers:writing-plans`). ROADMAP sub-row 32.1 is now `done` (parent row 32 STAYS `in-progress` until the 32.2 ROLLUP); sub-row 32.2 `planned`. PLAN-RECORD (preserved, 32.1 PLAN history): the SPEC §14 spine was decomposed into 16 bite-sized TDD tasks; ADR-0045 split-gate FINAL re-check at PLAN — 16 ≤ ~25 tasks / ~940-1420 ≤ ~1500 LoC → NO split, the 32.1a/32.1b escape-valve stayed UNCONSUMED; plan-document-reviewer APPROVED (one Task-10 test-code defect fixed in the loop). SPEC-RECORD (preserved): the phase-32.1 SUB-PHASE SPEC INHERITS the parent SPEC (the §5 proto / §6 reject / §7 stat / §8 fixture rosters + the §11 D32-1..D32-8 empirical-pin block; NO new docker probes) and pins the upstream connection-pool / cluster-routing seam's exact exported surface + multiplexing model (D-P32-3 — one-upstream-conn-per-downstream-conn; lazy dial on the first proxied command; a SYNCHRONOUS single-flight pump = depth-1 FIFO/positional pop-front; the seam consumes a DIAL CLOSURE → `internal/filter/network` gains NO `internal/cluster` import; the shared pool + deep queue + two-goroutine pipelining DEFERRED), the RESP partial-frame STREAMING-READER reassembly model (D-P32-4 — the terminal owns the conn, block-read a `bufio.Reader`, NO private high-water buffer, contrasted with the mongo/kafka observer model), the EAGER fixed-roster creation posture + the 32.1 10-name subset (D-P32-1 — 6 downstream counters + 4 gauges; stat surface 536 → 546), the boot-reject fixture arms (D-P32-5 — `0056` carries the `stat_prefix` arm only; the rest unit-armed), the 32.1 local-reply subset PING + AUTH (D-P32-6); the unknown-catch_all-cluster TOLERANCE (vs tcp_proxy's reject), the flat-`/stats` StatsAsserter (the `redis.` Prometheus arm defers to 32.2) + the downstream-RESP byte-equivalence prong (the §9 FIRST). It ANCHORS the ADR-0230 §Context (the upstream-pool seam; DECISIONS tail ADR-0229 → ADR-0230; the §Decision/§Consequences body LANDED at the 32.1 IMPL per ADR-0044). ~16-task spine; ~940-1420 prod LoC → fit the ADR-0045 gate (32.1a/32.1b escape-valve UNCONSUMED). At the 32.1 SPEC commit ROADMAP sub-row 32.1 flipped `planned → in-progress` (parent row 32 STAYED `in-progress`); spec-document-reviewer APPROVED. At THIS 32.1 IMPL commit sub-row 32.1 is now `done`. PARENT-SPEC RECORD (preserved):** the phase-32 `redis_proxy` PARENT SPEC COMPLETE; the D32-1..D32-8 empirical pins EXECUTED live (a real `redis:7` backend on a docker bridge network behind the contrib reference redis_proxy — round-trip CONFIRMED ran) + go-control-plane `/envoy` v1.32.4 + upstream Envoy v1.37.2 source; AMEND-R1..R9 recorded; the ADR-0229 §Context anchored; spec-document-reviewer APPROVED first iteration.** SPEC findings (SPEC §1.1 AMEND block): **D32-1 CONFIRMED** (`@type` carries `extensions.`; redis_proxy is CORE `/envoy v1.32.4` → **ZERO new go.mod dep**, the OPPOSITE of kafka_broker's `/contrib`); **D32-2 RESOLVED + REFUTES** (the per-command roster is **EAGER + table-bounded ~180 commands** — NOT the BRAINSTORM-hypothesized DYNAMIC; the fixed roster = 15 names under `redis.<sp>.` [6 counters + 4 GAUGES `ALL_REDIS_PROXY_STATS` + 2 `splitter.*` + 3 `REDIS_CLUSTER_STATS`]; `upstream_cx_*`/`upstream_rq_*` live under the existing CLUSTER scope `cluster.<name>.*`; the Prometheus arm is **LABEL-HOISTED** `envoy_redis_<leaf>{envoy_redis_prefix="<sp>"}` — the mongo `.rbac.` shape, NOT kafka inline; `enable_command_stats` gates neither presence nor increment in v1.37.2); **D32-3 RESOLVED → ADR-0230** (the seam: route→catch_all→cluster→host; ONE client per host multiplexed; FIFO positional pop-front correlation — RESP has no correlation id; envoy-go MVP may use one-conn-per-downstream); **D32-4 RESOLVED + REFINES** (`stat_prefix` + `settings` + `settings.op_timeout` ALL PGV-REQUIRED — `settings` is MANDATORY, not the BRAINSTORM "minimum subset" — + a RUNTIME `cannot configure a redis-proxy without any upstream` reject; unknown cluster refs tolerated at validate; deferred fields parse-accept); **D32-5 RESOLVED** (RESP framing verbatim; per-command reply bytes captured — the downstream RESPONSE bytes are GENERATED by redis_proxy → the §9-FIRST byte-equivalence prong); **D32-6 RESOLVED** (PING/AUTH [+ECHO/TIME/QUIT/HELLO] answered LOCALLY, zero upstream traffic; count `downstream_rq_total` but no `command.*`); **D32-7 RESOLVED** (per-command segment wire-derived but table-bounded eager → `IsValidName` satisfied by construction if the eager-table posture is adopted [the kafka posture] — the 32.2-SPEC pin); **D32-8 RESOLVED** (NO close-direction counters → close-direction-framework-zero-touch; ~1100-1550 prod LoC → the 2-way split holds). **Preserved (the BRAINSTORM record):** Phase 32 lands `envoy.filters.network.redis_proxy` — the project's **FIRST terminal routing proxy** (unlike every prior §9 row — echo/sni_cluster/zookeeper_proxy/mongo_proxy/kafka_broker, all observational sniffers on a `[filter, tcp_proxy]` chain — redis_proxy IS the connection terminator: parses RESP, routes commands to an upstream cluster, and pools/pipelines upstream connections itself; there is NO passive-observe MVP). The BRAINSTORM (`docs/envoy-go/phases/32-network-filter-redis-proxy/BRAINSTORM.md`, 12 §§ mirroring the mongo-29 parent pre-split template) settled via a multi-question dialogue: **Q0 subject** = `redis_proxy` (`envoy.filters.network.redis_proxy`, proto `envoy.extensions.filters.network.redis_proxy.v3.RedisProxy` — anticipated CORE `/envoy v1.32.4`, NOT `/contrib`; D32-1 confirms) chosen over `thrift_proxy` from {redis, thrift} (RESP far leaner than Thrift's transport×protocol matrix + method-routing + nested sub-filter chain); **Q1 envelope** = a SINGLE-ROUTE-TERMINAL MVP (RESP codec `+`/`-`/`:`/`$`/`*` + inline; `catch_all_route` to ONE upstream cluster; a basic per-host upstream connection pool with FIFO/positional request↔reply correlation — RESP carries no correlation id; the core single-key command set; per-command/downstream/upstream stats under `redis.<stat_prefix>.` via a new `redis.` prom arm + the `IsValidName` charset guard; sharding/redirection/`prefix_routes`-multi-cluster/multi-key-fragmentation/AUTH/faults/mirroring/`read_policy`/`custom_commands`/full-`ConnPoolSettings` DEFERRED, parse-accept-vs-reject per field pinned at SPEC D32-4; histograms deferred ADR-0060); **Q-split** = a 2-way FEATURE-PROGRESSIVE seam-first pre-split (the project's SIXTH BRAINSTORM-time pre-split after 22/25/26/28/29) — parent row 32 + **32.1** (`network-filter-redis-upstream-pool-seam-and-codec` — the upstream connection-pool / cluster-routing FRAMEWORK SEAM + the RESP codec + the redisproxy foundation + the 10th built-in + the `TCPRedisResponder` BackendKind + a minimal catch_all round-trip via `0055-redis-roundtrip`) + **32.2** (`network-filter-redis-commands-and-stats` — the full command set + the stat roster + the `redis.` arm + the differential command matrix + the 41st fuzzer `FuzzRESPDecode` + the parent-row-32 ROLLUP); **Q-pool-seam** = the new seam lands framework-level in `internal/filter/network/` but REDIS-SCOPED (YAGNI; thrift reuses/extends it later), reusing the `cluster.Manager` host-resolution + TCP dial path, built on `TerminalFilter.Handle`. Self-answered per §9 precedent: ONE new framework seam (the upstream connection-pool — the framework's SIXTH structural extension, ADR-0230) + one NEW filter package (`redisproxy`) + ZERO new go.mod deps (anticipated; redis_proxy is core `/envoy`); positional (not id-keyed) reply correlation; the differential proof is downstream-RESP-RESPONSE byte-equivalence PLUS cross-side `StatsAsserter` (the FIRST §9 row whose downstream response bytes are load-bearing — redis_proxy GENERATES them); `upstream_cx_*` pooling stats pinned per-side where the reference diverges; a NEW BackendKind (`TCPRedisResponder`, anticipated value 32); the 41st fuzzer `FuzzRESPDecode`; 2 ADRs anticipated — **ADR-0229** (the filter + parent umbrella) + **ADR-0230** (the upstream-pool seam); next-free at master tip is ADR-0229. ROADMAP parent row 32 registered `in-progress` with sub-phases `32.1, 32.2` + sub-rows 32.1/32.2 `planned` (per ADR-0106(d) — the family flat-row discipline preserves ADR-0045's split-gate WITHIN a single filter-row, the zookeeper-28/mongo-29 in-row split precedent; the §9 heading text UNTOUCHED per ADR-0106(c)) + the §9 candidate-roster paragraph refined (redis IN-PROGRESS/landing; {thrift} remains after phase 32). The static decisions + the empirical pins requiring evidence (D32-1..D32-8) are deferred to the parent SPEC, executed live against the contrib reference Envoy (`envoyproxy/envoy:contrib-v1.37.2`, ADR-0227) per `reference_docker_probe_bridge_network`. Counts UNCHANGED at the BRAINSTORM (fixtures 56 / fuzzers 40 / stat surface 536 / BackendKind tail 31; DECISIONS tail ADR-0228, next-free ADR-0229); the surface/fixture/fuzzer/BackendKind counts + the DECISIONS tail advance at the SPEC/IMPL sub-phases. **Earlier (phase 31 IMPL / phase 31 CLOSED, preserved):** the phase-31 `kafka_broker` IMPL COMPLETE — the FIFTH §9 row; a new `internal/filter/network/kafkabroker/` package (header-only both-direction Kafka sniffer at FULL per-API-key counter parity; the 9th built-in; consumer #3 of the ADR-0221 WriteFilter seam; the project's FIRST `/contrib v1.32.4` consumer); the EAGER 176-counter roster (stat surface 360 → 536); the `kafka.` INLINE prom arm; the `TCPKafkaResponder` BackendKind 31; the `0053`/`0054` fixtures; the 40th fuzzer `FuzzKafkaDecode`; ADR-0228 §Decision/§Consequences bodies landed in-place; six gates GREEN; ROADMAP row 31 `in-progress → done` (flat §9 row). The full prior detail (the 17-task spine; AMEND-K1..K7; D-PLAN-1/2/3; the phase-31 BRAINSTORM/SPEC/PLAN history; phase 30 the contrib pin-refresh; the phase-29 mongo_proxy record) is the durable on-disk record in the phase-31 + phase-30 + phase-29 directories.
- **phase-directory:** `docs/envoy-go/phases/32.1-network-filter-redis-upstream-pool-seam-and-codec/` (**SPEC.md + PLAN.md** — the durable phase-32.1 sub-phase SPEC + the 16-task PLAN landed; the PROGRESS.md artifact follows at the 32.1 IMPL session [Task 1 creates it], per the 29.1/29.2/29.3 per-sub-phase-dir precedent). The parent `docs/envoy-go/phases/32-network-filter-redis-proxy/` (**BRAINSTORM.md + SPEC.md + README.md** — the durable phase-32 parent-SPEC record; the 32.2 sub-phase SPEC/PLAN/PROGRESS follow in their own `32.2-…/` dir). The phase-31 directory `docs/envoy-go/phases/31-network-filter-kafka-broker/` (BRAINSTORM.md + SPEC.md + README.md + PLAN.md + PROGRESS.md — the durable phase-31 IMPL record). The phase-30 + phase-29 directories stay the durable phase-30/29 record.
- **lifecycle-state:** `phase 32.1 IMPL done — SKILL_ROUTING → the 32.2 SPEC (superpowers:writing-plans)` — the phase-32.1 IMPL is COMPLETE (`docs/envoy-go/phases/32.1-network-filter-redis-upstream-pool-seam-and-codec/PLAN.md` + `PROGRESS.md`; all 16 TDD tasks landed; six gates GREEN incl. the full 58-dir differential `ok`). Per the per-sub-phase precedent (22.x/25.x/26.x/28.x/29.x — after the sub-phase IMPL the next session runs the NEXT sub-phase's SPEC), the next step is the **32.2 SPEC** (`network-filter-redis-commands-and-stats`, `superpowers:writing-plans` per `feedback_execution_style` — see the `next-skill` bullet + `next-prompt.txt` for the full 32.2-SPEC scope). **Earlier (32.1 IMPL, DONE):** the SPEC §14 16-task spine executed end-to-end — Task 1 the baselines/anchors gate → the `redisproxy` skeleton+TypeURL → `config.go` parse → byte-stable rejects → `resp.go` request/reply decode+encode → `upstreampool.go` seam → `commands.go` PING/AUTH → `stats.go` 10-name roster → `filter.go` Handle pump → 10th built-in+bootstrap → `TCPRedisResponder` → `0055`/`0056` fixtures → the NEW `GET /stats` flat admin endpoint (the eighth admin endpoint, a Task-14 addition required for the `StatsAsserter`) → the completion bundle. The 32.1 IMPL filled the ADR-0230 §Decision/§Consequences body + the ADR-0229 §Decision/§Consequences (32.1 half, status PARTIAL) in place per ADR-0044; advanced counts (fixtures 56→**58**, fuzzers **40** unchanged, stat surface 536→**546** [+10: 6 downstream counters + 4 gauges], BackendKind 31→**32**; R9). Phases 29 + 30 + 31 are CLOSED. ROADMAP: parent row 32 `in-progress`; sub-row 32.1 `done` (flipped `in-progress → done` at THIS 32.1 IMPL commit per the 26.x/28.x/29.x precedent); sub-row 32.2 `planned`; rows 26/27/28/29/30/31 `done`. The §9 Network-filters family stays OPEN (1 candidate {thrift} remains after phase 32).
- **next-skill:** **`superpowers:writing-plans`** — scoped to the **32.2 SPEC** (`network-filter-redis-commands-and-stats`), the SECOND sub-phase of the parent phase-32 (`redis_proxy`) row, building ON the as-built 32.1 seam + codec foundation. The 32.2 SPEC must pin: the **full single-key command set** (core commands routed via `catch_all` — GET/SET/DEL/EXISTS/EXPIRE/TTL/TYPE/INCR/DECR/APPEND/GETSET/MGET/MSET/SADD/SCARD/SMEMBERS/LPUSH/LLEN/LRANGE + unknown-command forwarding; AUTH/PING/ECHO/TIME/QUIT/HELLO stay 32.1 local-reply; multi-key fragmentation [MGET/MSET sharding], `enable_redirection`, faults, mirroring DEFERRED — parse-accept, behavior-deferred); the **per-command + downstream + upstream stat roster** under `redis.<stat_prefix>.` (EAGER + table-bounded per D32-2 — the exact `ALL_REDIS_PROXY_STATS` slot names [total/success/error per command] + the 2 `splitter.*` + the 3 `REDIS_CLUSTER_STATS` names if 32.2-scoped; ~180 command names × 3 slots ≈ ~540 counters, stat surface **546 → ~1091**; the `stats.IsValidName` charset guard satisfied BY CONSTRUCTION for the table-bounded names); the new **`redis.` LABEL-HOISTED Prometheus tag-extractor arm** at `internal/stats/name.go` (`envoy_redis_<leaf>{envoy_redis_prefix="<sp>"}` — the mongo `.rbac.` HOIST shape, NOT the kafka INLINE arm; D32-2/AMEND-R2 pinned this at the parent SPEC); the **4 gauge inc/dec** call sites (the 4 gauges were REGISTERED at 32.1 but their inc/dec paths are 32.2 scope); the **differential command matrix** (extending `0055-redis-roundtrip` with a command spread + error/edge arms; the `TCPRedisResponder` FIFO/positional choreography per arm; the `StatsAsserter` names per arm; one-dir-one-runner-branch per `reference_differential_fixture_dispatch_constraint`); the **41st fuzzer `FuzzRESPDecode`** (in `redisproxy`; the `resp.go` decode path — no-panic / no-mutation / bounded-buffer); the BEHAVIOR_CONTRACT 32.2 bundle + the **parent-row-32 ROLLUP** (parent row 32 `in-progress → done` ATOMICALLY with sub-row 32.2 `planned → done` per the 18/19/22/24/25/26/28/29 precedent); the **ADR-0229 §Decision/§Consequences 32.2 half** (completing the PARTIAL body → ACCEPTED); the **ADR-0045 split-gate re-check at the 32.2 PLAN** (anticipated LoC + task count; confirm NO further split); D-question dispositions at 32.2 (likely no new docker probes — D32-2 resolved; confirm the `redis.` arm shape + the exact `ALL_REDIS_PROXY_STATS` slot names + the gauge inc/dec call sites in the upstream Envoy source). Honor `feedback_execution_style` (the 32.2 IMPL runs subagent-driven; subagents commit LOCAL-ONLY per `feedback_subagents_no_push`, the controller squash-merges + pushes at stage-close per `feedback_push_to_origin`; per-task `gofmt -l` + `golangci-lint run` per `feedback_pertask_gofmt_lint`; deliberate-break checks use `go test -count=1` per `reference_differential_break_protocol_count1`). Work in a git worktree (`feedback_git_worktrees`). Read first (per `next-prompt.txt`): `docs/envoy-go/phases/32.1-network-filter-redis-upstream-pool-seam-and-codec/SPEC.md` (THE 32.1 SPEC — the codec §3.5, the seam §4 [D-P32-3 + the ADR-0230 §Context], the stat roster §7, the fixtures §8, the 32.1 D-questions §12.2 — durable anchors 32.2 inherits) + `docs/envoy-go/phases/32-network-filter-redis-proxy/SPEC.md` (THE PARENT SPEC — the §5 proto / §6 reject / §7 stat / §8 fixture rosters + the §11 D32-1..D32-8 block; D32-2 [EAGER table-bounded per-command roster; 15 fixed names; `redis.` LABEL-HOISTED] + D32-4 [parse-accept-vs-reject field map] are the key 32.2 anchors) + `docs/envoy-go/phases/32.1-…/PLAN.md` + `PROGRESS.md` (the 32.1 IMPL as-built task log — the deviations, the six-gate record, the Task-14 unplanned `GET /stats` endpoint note, the Task-16 BEHAVIOR_CONTRACT "eighth endpoint" note) + `docs/envoy-go/STATE.md` (this file) + `docs/envoy-go/ROADMAP.md` (parent row 32 `in-progress`; sub-row 32.1 `done`; sub-row 32.2 `planned`) + `docs/envoy-go/DECISIONS.md` ADR-0229 (PARTIAL — the 32.1 half landed; the 32.2 half lands THIS SPEC/IMPL) + ADR-0230 (ACCEPTED; the bodies are the authoritative seam record) + ADR-0044 / ADR-0079 / ADR-0080 / ADR-0052 / ADR-0045 / ADR-0106 / ADR-0060 / ADR-0219 / ADR-0223 / ADR-0227 + the phase-29.2 SPEC/PLAN/PROGRESS (the per-sub-phase SPEC/IMPL lifecycle precedent — the BRAINSTORM-to-ROLLUP shape) + `internal/filter/network/redisproxy/` (the as-built 32.1 source: filter.go + config.go + resp.go + commands.go + stats.go) + `internal/filter/network/upstreampool.go` (the seam) + `internal/stats/registry.go` (`NewCounterIfAbsent`/`NewGaugeIfAbsent`/`IsValidName`) + `internal/stats/name.go` (the kafka `kafka.` INLINE arm = the 32.2 contrast; the mongo `.rbac.` hoist = the 32.2 shape for `redis.`) + `test/differential/fixture/fixture.go` (the `StatsAsserter` + `TCPRedisResponder` BackendKind 32) + `test/fixtures/0055-redis-roundtrip/` (the as-built cross-side fixture the 32.2 command matrix extends) + project memories (`reference_network_filter_typeurl_extensions`, `reference_wire_format_both_sides_see_same_bytes`, `reference_close_direction_framework_gap`, `reference_dynamic_stat_name_charset_guard`, `reference_differential_reference_parses_full_message`, `reference_differential_asserter_dispatch`, `reference_differential_fixture_dispatch_constraint`, `reference_differential_break_protocol_count1`, `feedback_execution_style`, `feedback_git_worktrees`, `feedback_subagents_no_push`, `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`). The controller squash-merges + pushes at stage-close (`feedback_push_to_origin`).
- **last-commit:** the phase-32.1 IMPL squash `fa58bc9` (`phase 32.1 IMPL: network-filter-redis-upstream-pool-seam-and-codec — the redis_proxy TERMINAL routing proxy on the ADR-0230 upstream-pool seam; 16-task PLAN executed subagent-driven; six-gate GREEN`) — squash-merged from `phase-32.1-network-filter-redis-upstream-pool-seam-and-codec-impl` + pushed to origin/master by the controller per `feedback_push_to_origin` (subagents committed LOCAL-ONLY per `feedback_subagents_no_push`); the IMPL branch was deleted and the worktree removed at stage-close. This docs-only SHA-fill trails it (confirm via `git log --oneline -1`). Substantive predecessor on master: the phase-32.1 PLAN squash `a28f593` (the 16-task PLAN + ROADMAP sub-row-32.1 PLAN-DONE note + STATE/next-prompt advance to the 32.1-IMPL cold-start); earlier the phase-32.1 SPEC squash `b7f01a4`; the phase-32 PARENT SPEC squash `0f7888d`; the phase-32 BRAINSTORM squash `f473c4e`; the phase-31 IMPL squash `928287a` (`phase 31 IMPL: kafka_broker network filter — 9th built-in header-only both-direction sniffer + full per-API-key counter parity`). Earlier substantive predecessors on master: the phase-31 PLAN squash `12f7cac`; the phase-31 SPEC squash `6104036`; the phase-31 BRAINSTORM squash `78d5f36`; the phase-30 IMPL squash `2d24f34` (phase 30 CLOSED).
- **last-updated:** 2026-06-08
- **next-free ADR:** `ADR-0231` (DECISIONS.md tail is now **ADR-0230** — the upstream connection-pool / cluster-routing framework seam, **§Context anchored at THIS 32.1 SPEC** per the BRAINSTORM §7, with §Decision/§Consequences body deferred to the 32.1 IMPL per ADR-0044). ADR-0229 (the redis_proxy filter + parent-row-32 umbrella) §Context was anchored at the parent SPEC; its §Decision/§Consequences bodies land across the 32.1/32.2 IMPL. So next-free after the 32.1 SPEC is **ADR-0231**; next-free after phase 32 phase-done ≈ ADR-0231 (no further ADR anticipated; each sub-phase re-checks). The ADR-0209 escape-valve reserve STANDS-UNCONSUMED.

---

## Project counts at this commit

- **active differential fixtures:** **58** (tail `0056-redis-boot-reject`). Canonical recipe `ls -d test/fixtures/[0-9]* | wc -l` = 58. Advanced at the phase-32.1 IMPL (+2: `0055-redis-roundtrip` cross-side [extended at 32.2] + `0056-redis-boot-reject`).
- **fuzzers:** **40** (canonical recipe `grep -rh "^func Fuzz" $(find ./internal -name fuzz_test.go) | wc -l`; tail `FuzzKafkaDecode`). UNCHANGED at the phase-32.1 IMPL; advances to 41 at the phase-32.2 IMPL (`FuzzRESPDecode`).
- **stat surface:** **546** (BEHAVIOR_CONTRACT doc count). Advanced at the phase-32.1 IMPL (+10 fixed — 6 downstream counters + 4 gauges; D-P32-1 EAGER); then advances to ~546 + 5 fixed + the EAGER per-command roster (~180 × 3 ≈ 540) ≈ ~1091 at the phase-32.2 IMPL (the `command.<cmd>.latency` histograms + `*_fault` counters DEFERRED, NOT counted).
- **BackendKind tail:** **32** (`TCPRedisResponder`). Advanced at the phase-32.1 IMPL.
- **DECISIONS.md tail:** **ADR-0230** (next-free **ADR-0231**) — the upstream connection-pool / cluster-routing framework seam; **§Decision/§Consequences body LANDED at the 32.1 IMPL** (status ACCEPTED). ADR-0229 (the redis_proxy filter + parent-row-32 umbrella) §Context anchored at the parent SPEC; its §Decision/§Consequences 32.1 half LANDED at the 32.1 IMPL (status PARTIAL — 32.2 half deferred). Next-free ADR-0231 (unchanged; no new ADR consumed at 32.1 IMPL). Was ADR-0228 at the phase-31 IMPL close.
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227, superseding ADR-0008).
- **conformance:** h2spec 53/53; proxy-wasm all 10 families PASS (image-independent; UNAFFECTED by phase 32 — touches no HTTP/h2/proxy-wasm path).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
