# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

> **This file is a POINTER, not an archive** — `BOOTSTRAP_PROMPT.md` §4: *"STATE.md — pointer: active phase, last commit, next action."* It carries the live pointer, the live counts, and a SHORT recent lineage. **Full stage-by-stage history lives in `STATE_HISTORY.md`** (archived at ADR-0288; **nothing was deleted** — the pre-move file is preserved there verbatim, byte-for-byte).
>
> **⚠️ THE RULE ADR-0288 EXISTS TO ENFORCE: there is EXACTLY ONE live `next-skill:`, `lifecycle-state:`, `next-free ADR:`, and count block IN THIS FILE, and they live in §Current pointer directly below.** Before that move, ~330 lines of prepended backlog had buried TWO fossilised "current state" blocks whose `next-skill:` still read *"the 42.2 hedging BRAINSTORM"* and *"the 46.2 SPEC"* while the live phase was **66** — so `grep 'next-skill:' STATE.md` returned **two stale hits and ZERO live ones**, and `grep 'fuzzers:'` returned **48** and **42** when the true count was **55**. **A session may trust a grep of this file again. Keep it that way: when you advance the pointer, EDIT §Current pointer IN PLACE — never prepend a new block above it.**

---

## Current pointer

*(The ONLY live "what next" in this repo. Update IN PLACE at every stage close.)*

- **active-phase:** `phase 77 (runtime-static-layer) BRAINSTORM done` (2026-07-26) — **ROW 77 REGISTERED `in-progress`** at `ROADMAP.md:139`, **RE-OPENING sentinel check (1)** after its third-ever silent close (verified post-edit: check (1) prints `NOT DONE: row 77`). ⚠️ **THIS WAS THE FIRST REGISTRATION IN THE PROJECT'S HISTORY PERFORMED FROM A STATE WHERE EVERY CHARTERED ROW WAS ALREADY `done`** — there was no in-flight row to pattern-match against. **THE FIRST Runtime-+-hot-restart-family ROW — THE FAMILY OPENS**, which legitimately clears one of check (3)'s three blockers (post-edit, check (3) prints `NEVER OPENED: gRPC` and `NEVER OPENED: WASM` only). **THE PICK, SELF-PICKED per the 2026-07-12 standing directive:** a bootstrap `layered_runtime` **static-layer** consumer. **Today envoy-go boot-REJECTS a config the reference accepts and serves** (`internal/bootstrap/bootstrap.go:568-569` — a raw-YAML generic-map pre-check firing on mere key presence, BEFORE `protojson.Unmarshal`, so lifting it lets the message unmarshal where ADR-0016's `DiscardUnknown:false` already covers unknown subfields ⇒ only the SEMANTIC arms need hand-writing). The row lifts that reject **for the `static_layer` arm ONLY** and lands an `internal/runtime` `Snapshot` (recursive Struct→dotted-path flattening at unbounded depth, distinct-key UNION across layers, precedence-collapsed) **in the phase-00 placeholder directory that ALREADY EXISTS** ⇒ **+0 new packages**, `go list -deps ./internal/runtime` a clean leaf. **+2 stats** (`runtime.num_keys`/`runtime.num_layers`, both verified against `NamePattern`), **+1 fixture** (`0118`), everything else +0. ⚠️ **THE SEMANTICS ARE PINNED BY MEASUREMENT ACROSS ELEVEN LIVE REFERENCE ARMS, NOT BY UPSTREAM PROSE:** `num_keys` is the size of the flattened-leaf UNION (a key in two layers counts **ONCE** — and a single-layer config CANNOT discriminate union from per-layer sum, so the 2-layer overlap arm is load-bearing and must be in the fixture); a `{numerator,denominator}` Struct **TERMINATES** flattening and counts as exactly one; an empty-string value still counts; and `num_keys == len(entries)` / `num_layers == len(layers)` held in **every** arm. ⚠️ **AN OFF-BY-ONE TRAP ISOLATED RATHER THAN REASONED ABOUT:** `layered_runtime` present with ZERO declared layers reads `num_layers: 1` with `layers: [""]`, shape-independent across three spellings — and a **200-vs-503 cross-product with BOTH controls** proves that phantom layer is a synthesized, **WRITABLE ADMIN** layer (`POST /runtime_modify` ⇒ 200 there; ⇒ 503 `No admin layer specified` both when `layered_runtime` is absent AND when one static layer is declared, the latter being the decisive negative) ⇒ **the honest "empty" arm is `layered_runtime` absent entirely, NEVER `layers: []`**. ⚠️ **THE FIXTURE MUST NOT GO THROUGH `ProbeAdmin`** — `compareAdminResponses` (`runner_test.go:1394`) compares the body **BYTE-EXACT**, while the reference `/runtime` body is **doubly non-deterministic** (JSON key order randomized **PER REQUEST** — three distinct md5s with an identical sort-keys md5; the Struct debug-string prefix randomized **PER PROCESS**, flipping across a `docker restart`) ⇒ assert **STATS** via the existing `fixture.StatsAsserter` seam over `/stats/prometheus` on a **NAMED SUBSET** (the phase-75 `0110` Shape-A precedent); both randomizations contaminate only the body, never the gauge values. Both gauge NAMES were measured present in all eleven arms **including the `layered_runtime`-absent arm (value 0)** ⇒ **no name-presence divergence**. **A Runtime-family row claiming the FIRST family ordinal; ONE new ADR-0299 anticipated.** *(Historical, retained: **phase 76 (lb-ring-hash-spread-margin) IMPL done** — **ROW 76 FLIPPED `in-progress` → `done`** at the T9 six-gate (ADR-0106, the SOLE leg — **a SINGLE FLAT ROW**, no split legs). ⚠️ **ROW 76 WAS THE LAST NON-`done` CHARTERED ROW, so sentinel check (1) IS NOW SILENT — for the first time since the phase-75 IMPL, and the ROADMAP now carries NO non-`done` row at all. ⇒ THE NEXT PHASE MUST CHARTER A NEW ROW; there is no banked mid-lifecycle work left.** The sentinel **STILL does NOT fire** — checks (2) and (3) both print — and **`stop` was NOT created.** ⚠️ **Check (1)'s silence was GUARDED against a false negative**: silence could also mean row 76 fell into the regex blind spot, so the check-(1) regex was applied to `:138` alone and returns `| 76 | lb-ring-hash-spread-margin | 75 | done ` — **the silence is genuine.** **THE ROW DELIVERED:** `0061-lb-ring-hash`'s spread assertion now carries a DERIVED margin — `sourceIPs` **4 → 16** (`totalConns` 64 → 256, derived), a seeded collapse-rate **MEASUREMENT** with a stacked non-vacuity CONTROL in `internal/cluster/ringhash_test.go`, a **`go/parser` LINKAGE gate** closing the single-edit drift case, two silently-vacuous unit tests rescued, and every prose site the fix falsifies swept. **+0 stats (1205) / +0 fixtures (119) / +0 fuzzers (55) / +0 BackendKinds (38) / +0 modules (2) / +0 PRODUCTION `.go` FILES / +0 production imports / ZERO new packages / ZERO new exported symbols** — the first chartered row in this lineage whose production-code delta is exactly nothing, asserted BY COMMAND per-package. ⚠️ **SEVENTEEN PLAN/ADR CLAIMS REFUTED BY EXECUTION, and TWO of them are GATE COMMANDS — the SIXTH and SEVENTH broken gates this lineage has recorded.** **G-A: PLAN `:1159-1163`'s G9 form is NOT symbol-scoped and reads RED on a correctly-implemented tree** — the `0061` driver package exports **zero** top-level symbols, so its whole `go doc -all` output is package-doc PROSE, and this row's own legitimate comment rescale surfaces as three diff lines, exit 1. **Not a fail-open — a FALSE POSITIVE**, which is worse in a specific way: it would either stop a close script or, over time, teach a future session to suppress the gate. **G-B: PLAN `:1155`'s wrong-baseline caveat NAMES THE WRONG SHA** — it warns against baselining on the phase-75 **tip** (`c57b98b8`), but that is an **ANCESTOR** of master and returns **0**, i.e. harmless; the dangerous sha is the phase-75 **pre-IMPL router** commit (`9f5d667b`), which returns **2**. ⚠️ **A session following the caveat literally would guard against the SAFE sha and walk into the unsafe one.** **BREAK α FIRED THE SPREAD LEG AND ONLY IT** — `runner_test.go:1293: distribution: subject spread: only 1 backend(s) nonzero, want >= 2 (ring collapsed?)`, byte-identical to the expected string, from **`driver.go:307`** (⚠️ **NOT the PLAN's `:295` — T5's comment sweep moved all five legs ~+12**); affinity, conservation and length all SURVIVED as predicted ⇒ neither vacuous nor mis-targeted; restore ⇒ `ok … 4.670s`, a green FOLLOWING an observed red. ⚠️ **BUT α IS NOT ISOLATING: the unit package catches it by TWO tests of 404, not the PLAN's ONE of 403** — `TestRingHash_DistinctKeysSpread` **and this row's own new measurement**, which fires **BOTH** legs at rate **1.00000**. **This row's own T1 moved the denominator the PLAN's finding was stated against.** ⚠️ **AND A DEFECT IN THE CONTROL LEG'S OWN DIAGNOSTIC PROSE, surfaced by α**: the message says *"a rate of 0 HERE means the ring is no longer being redrawn"*, but under α the control goes red at rate **1.0** — the **opposite pole**. The assertion is correct; **the text that explains it is not**, and a diagnostic string is exactly the kind of prose no test can falsify. Recorded, deliberately NOT fixed. **β-SINGLE-EDIT DID NOT FIRE (exit 0) WHILE T4's LINKAGE GATE WENT RED ON THE SAME TREE** — that pairing is T4's entire justification and is now discharged by execution rather than argued. **ALL SIXTEEN BREAKS EXECUTED at this tip.** **G6: 119/119 PASS / 0 FAIL / 0 SKIP, `ok … 409.759s`**, `comm -3` fixture-set vs subtest-set EMPTY. **G7 `-race`: a REAL failure on the FIRST attempt** — `0082-grpc-access-log-buffering` **PANICKED** at `bind: address already in use` on an ephemeral port, inside `ReferenceBootstrap` (`runner_test.go:200`) **BEFORE any assertion executed**, aborting the binary at 84 of 119 subtests; **`0082` is byte-untouched by this row and isolate-green 3/3 under `-race`** ⇒ **startup class, not a regression**. ⚠️ **NOT on this stage's hazard roster by name — the FIFTH consecutive stage at which that has held** (its sibling `0083` fired the byte-identical signature at phase 75). ⚠️ **AND THE BACKGROUND-TASK NOTIFICATION REPORTED "exit code 0" — that was the WRAPPER shell's status, not `go test`'s; the logged `EXIT=1` was the real one. A harness's exit code is not the command's exit code.** Clean `-race` re-run: **119/119 PASS, ZERO data races, zero panics, `ok … 400.595s`.** ⚠️ **THE RUNTIME QUESTION IS DISCHARGED AS "BELOW RESOLUTION", NOT AS A NUMBER** — measured with a same-session control, 3 warm runs per arm: K=16 **4.627 s** vs K=4 **4.523 s** ⇒ **+0.104 s (+2.3%)**, which is **SMALLER than the K=16 arm's own spread (0.19 s)** — and ⚠️ **the SIGN FLIPPED against the PLAN's own same-session measurement (−1.2%)**. **Two same-session controls disagreeing in SIGN is the strongest available evidence that the effect is below measurement resolution.** SPEC §3.6's **+4.5% stays REFUTED**; do not carry a signed delta forward in either direction. A Load-balancing-family **MAINTENANCE** row — ⚠️ **on a family DECLARED CLOSED at row 54** (`ROADMAP.md:116`), so it claims **NO family ordinal**; the right reason the heading paragraph stays unamended is *"maintenance rows do not extend a charter"*.)*
- **phase-directory:** `docs/envoy-go/phases/77-runtime-static-layer/` (`BRAINSTORM.md` only at this stage). Prior: `docs/envoy-go/phases/76-lb-ring-hash-spread-margin/` (`BRAINSTORM.md` + `SPEC.md` + `PLAN.md` + `PROGRESS.md`, the last carrying the full `# IMPL record`)
- **lifecycle-state:** **`1`** (BRAINSTORM done. **ROW 77 REGISTERED `in-progress`; check (1) RE-OPENED.** Next -> **the phase-77 SPEC**, which settles the **D-RSL-*** docket — and ⚠️ **D-RSL-EMPTY is the load-bearing item**: what envoy-go does with `layered_runtime` present-but-zero-declared-layers, where the reference synthesizes a WRITABLE ADMIN layer and reports `num_layers: 1`. The BRAINSTORM recommends **PARSE-REJECTING** that case rather than matching a layer envoy-go cannot write to, but the SPEC must settle it **BY EXECUTION**, not by reasoning.) ⚠️ **THREE SENTINEL FINDINGS FROM THIS STAGE THAT CHANGE HOW THE LOOP'S OWN CONTROL MECHANISM IS READ.** **(i) CHECK (2) IS FAIL-UNSAFE, AND IT WAS FAILING UNSAFE AT THIS TIP.** It matched **only** the long phrase, so the **Operational-tooling** family's genuinely un-chartered remainder — three items recorded in the **SHORT** form, in the *same paragraph* that reads `**The family STAYS OPEN**` — was **INVISIBLE** to it. Unlike check (3), which fails **SAFE** by construction, and unlike check (1)'s four-miss blind spot, which is currently **harmless** (all four missed rows are `done`), **this blind spot is LIVE**: had the visible sentences ever been emptied and the families opened, `stop` would have fired with real un-chartered work on the books — **a false "done" ends the project early**. **FIXED AT THE MATCHER, NOT AT THE PROSE** — broadened in `next-prompt.txt` with **three** negative controls observed (a `candidates were:` recap ⇒ silent, a short-form-only line ⇒ 1, an empty file ⇒ silent), which fixes the **CLASS** and immediately surfaces the **INSTANCE** with no ROADMAP prose edit; normalising that paragraph is left as a named follow-up. **(ii) THE DOCUMENTED TRAP'S STATED MECHANISM IS REFUTED WHILE THE TRAP IS REAL** — a naive `grep -c 'candidates:'` does inflate, but **NOT** via the `candidates were:` recaps, because that string does not contain the substring `candidates:` **at all** and is itself invisible to the naive count; the inflation is **ROADMAP table rows carrying BRAINSTORM prose**. **(iii) THE MEASURED RECORD: CHECK (2) HAS NEVER GONE DOWN.** Replaying the sentinel over every ROADMAP commit shows the sentence count has been the same in **every** commit since 2026-07-12, across ~17 phases, with history `0 -> 1 -> 3` and **not one decrease** — and **phase 74 landed three `ssl.*` names while making its OWN sentence LONGER** (its family clause SPLIT into a dynamic half plus a new `connection_error` clause), while **phase 73 predicted a clause REMOVAL and got a clause REPLACEMENT** because the tracing backlog held more items than the clause named (it still does — six follow-ons where the clause names three). ⇒ **the candidates sentence is a WINDOW onto a larger deferred backlog, not an inventory of it.** ⚠️ **AND THE ROUTER'S OWN SELECTION CALCULUS IS REFUTED AS ARITHMETIC:** *"a check-(3)-clearing candidate is worth **strictly more**"* does not hold, because opening a family **MINTS** a check-(2) sentence — the item count went **3 never-opened + 3 sentences -> 2 + 4, i.e. 6 -> 6, NET ZERO**. What survives is strictly weaker and is the actual reason to take the pick: **opening a family is the ONLY way that family's work becomes enumerable at all** — a prerequisite, not a shortcut. ⚠️ **THE SINGLE MOST CONSEQUENTIAL FACT AT THIS TIP: sentinel check (1) IS NOW SILENT AND THE ROADMAP CARRIES NO NON-`done` ROW.** The project is held open by checks **(2)** and **(3)** ALONE — three families never opened and three families still carrying deferred-candidate sentences. ⇒ **a candidate that clears a check-(3) blocker is now worth strictly more than one that does not**, which is a real change in the selection calculus and NOT merely a restatement of the standing directive's *"smallest defensible candidate first"*. ⚠️ **THE ROW WAS EXECUTED END-TO-END AND REFUTED SEVENTEEN CLAIMS, TWO OF THEM GATE COMMANDS.** The lineage's recorded broken-gate count moves from five to **seven**: **G-A** — the PLAN's own G9 `go doc -all` form is **not symbol-scoped** and reads **RED on a correctly-implemented tree** (the `0061` driver package exports **zero** top-level symbols, so its whole doc output is package PROSE and this row's legitimate comment rescale surfaces as diff lines); **not a fail-open but a FALSE POSITIVE**, which is worse in a specific way — it would either stop a close script or teach a future session to suppress the gate. **G-B** — the PLAN's wrong-baseline caveat **names the wrong sha**: baselining on the phase-75 **tip** (`c57b98b8`) returns **0** because it is an **ANCESTOR** of master and is therefore **harmless**, while the dangerous sha is the phase-75 **pre-IMPL router** commit (`9f5d667b`), which returns **2**. **A session following the caveat literally would guard against the SAFE sha and walk into the unsafe one.** ⚠️ **A THIRD METHODOLOGICAL FINDING, from the harness rather than the code:** the background-task notification for the first `-race` run reported **"exit code 0"** — that is the **WRAPPER shell's** status, not `go test`'s; the logged `EXIT=1` was the real one, and the run had in fact **panicked**. **A harness's exit code is not the command's exit code.** ⚠️ **BREAK α IS NOT ISOLATING** — the unit package catches it by **TWO tests of 404**, not the PLAN's **ONE of 403**, because **this row's own T1 added the second one**; a finding stated against a denominator that the same row then moves is stale by construction. ⚠️ **AND THE MEASUREMENT'S OWN DIAGNOSTIC PROSE IS WRONG IN A WAY NO TEST CAN CATCH**: the CONTROL leg's message says *"a rate of 0 HERE means the ring is no longer being redrawn"*, but under α it goes red at rate **1.0**, the opposite pole. **The assertion is correct; the sentence explaining it is not.** Recorded, deliberately not fixed.
- **next-skill:** **the phase-77 SPEC** — settle the **D-RSL-*** docket, and ⚠️ **EXECUTE each item rather than reasoning about it** (the phase-76 lesson: its docket was settled by execution on every item, and that is precisely what refuted its own headline claims). The docket: **D-RSL-EMPTY** (⚠️ **the load-bearing one** — envoy-go's posture on `layered_runtime` present-but-zero-declared-layers, where the reference synthesizes a **WRITABLE ADMIN layer** and reports `num_layers: 1`; the BRAINSTORM recommends **PARSE-REJECT** over matching a layer envoy-go cannot write to, and rejects "leave it undefined" in advance because an undefined arm is a latent divergence) · **D-RSL-STATS** (**+2** as hypothesised, or **+9** full reference name-parity with seven permanently-zero names? The reference emits NINE `runtime.*` names in **every** arm, and phase 41's *parse-and-register-emit-0-but-DEFER* is a real precedent — but the project asserts NAMED SUBSETS cross-side, never the full set, so +2 creates no name-set divergence) · **D-RSL-NUMKEYS-DEEP** (do duplicate leaf paths from different nesting shapes in ONE layer — `a.b: 1` alongside `a: {b: 2}` — collide, error, or double-count? **NOT PROBED. OWED.**) · **D-RSL-EMPTYSTR** (in a two-layer overlap, does a key set to `""` in the overriding layer clear or shadow the lower value? ⚠️ **`""` is ALSO the positional "not defined here" sentinel, so the `/runtime` body CANNOT discriminate** — this needs a `final_value` probe, not a body read) · **D-RSL-REJECT** (confirm the reject roster arm-by-arm with byte-stable wording, and ⚠️ **do not ASSUME ADR-0016 covers unknown subfields once the pre-check is lifted — EXECUTE it**) · **D-RSL-SPLIT** · **D-RSL-ADR**. ⚠️ **THE COST FIGURE IS A FLOOR, NOT AN ESTIMATE: 9-11 tasks, and the recorded ~10-14 is SUPERSEDED for a reason that matters more than the number** — the recorded figure is a **document read, i.e. a CARRIED claim**, while 9-11 is a fresh re-costing that found the groundwork already laid (the package directory exists, `bootstrapv3`/`structpb` are already imported by production code, `FuzzBootstrapLoad` already exists). ⚠️ **But the fresh figure ALSO under-counted**: it did not price the bootstrap reject roster, which is this row's real hidden cost — **three unsupported oneof arms** (`rtds_layer` mattering most, since silently ignoring it means a config asking for DYNAMIC runtime quietly gets STATIC values), **four semantic arms the reference enforces via PGV that envoy-go must hand-write** (empty `name`, missing `layer_specifier`, duplicate layer name, >1 admin layer), and **two loader-time value arms** (list-valued and null-valued entries ⇒ `Invalid runtime entry value for <key>`, each reproducing alone). All must land **ATOMICALLY** with the lift. ⚠️ **Today's message is an inline `fmt.Errorf`, NOT a named constant**, so the row CONVERTS as well as adds. ⚠️ **`TestLoad_RejectsLayeredRuntime` must be REPLACED, not extended** — it asserts only that the error contains `layered_runtime` and, unlike its `dynamic_resources` sibling, does **not** assert the `"bootstrap: "` prefix. **BANKED as the recommended phase-78 opening: `fault.abort.grpc_status` at ~7-9 tasks / +0 on EVERY envelope axis** — its current state is *worse* than "ignored" (`abortEnabled` is set only inside the `HttpStatus` type switch, so a `grpc_status` variant fires **no abort at all** and the request reaches the backend), and a 4-arm codec×content-type probe found the reference branches on the request **`content-type`** not the codec, returns HTTP **200** in every arm, and emits `grpc-status`/`grpc-message` as response **HEADERS not trailers** — which **RETIRES the framework blocker** that inflated its inherited ~10-13. ⚠️ **It does NOT open the gRPC family, and the PROVENANCE of the three-stage claim that it does is now LOCATED**: `ROADMAP.md`'s row-09 summary carries `(gRPC family)` as a **ROUTING LABEL** in a list whose sibling entry labels runtime keys the same way — **reading a filing label as a charter is the error**, and the clinching evidence is the probe itself, since a feature exercisable over plain HTTP/1.1 with no gRPC client, backend, or framing anywhere cannot open the gRPC family.
- **last-commit:** the phase-77 BRAINSTORM squash (`git log --grep 'phase 77'`; **locate by SUBJECT, never by position** — `reference_next_prompt_tracked_despite_gitignore`). Prior: the phase-77 router commit `f48975c4` (this stage's base), the phase-76 IMPL squash, the phase-76 router commit `921bc148`, the phase-76 PLAN squash `9b0077b9`, the phase-76 SPEC squash `98c27fc9`.
- **last-updated:** 2026-07-26 (phase-77 BRAINSTORM done. Lifecycle-state **DONE -> 1**; **ROW 77 REGISTERED `in-progress`**, RE-OPENING sentinel check (1); **`DECISIONS.md` BYTE-UNTOUCHED**, normal at a BRAINSTORM. Fresh worktree `/home/esa/git/envoy-go-wt-p77bs` off master `f48975c4`, branch `phase-77-brainstorm`. Docs-only: **ZERO production `.go`, ZERO test `.go`**; exactly THREE files edited (`ROADMAP.md`, `STATE.md`, `next-prompt.txt`) plus the new `BRAINSTORM.md`. **FOUR read-only re-costing agents at tip `f48975c4` on disjoint remits with PRIVATE scratch** — two of them running **LIVE reference probe fleets** against `envoyproxy/envoy:contrib-v1.37.2` (image id verified against the `ENVOY_TARGET.md` pin, fresh container per arm, bridge network, published ports) — plus controller re-derivation of every load-bearing claim. **THE HEADLINE — THE ROUTER'S OWN SELECTION CALCULUS IS REFUTED AS ARITHMETIC.** *"A candidate that clears a check-(3) blocker is worth **strictly more**"* does not hold: opening a family **MINTS** a check-(2) sentence, so the sentinel item count went **3 never-opened + 3 sentences -> 2 + 4 = 6 -> 6, NET ZERO**. What survives is strictly weaker, and is the actual reason to take the pick: **opening a family is the ONLY way that family's work becomes enumerable at all** — a prerequisite, not a shortcut. **THE SECOND HEADLINE — CHECK (2) IS FAIL-UNSAFE AND WAS FAILING UNSAFE AT THIS TIP.** It matched **only** the long phrase, so the **Operational-tooling** family's genuinely un-chartered remainder — three items recorded in the **SHORT** form in the *same paragraph* that reads `**The family STAYS OPEN**` — was **INVISIBLE**. Check (3) fails **SAFE** by construction and check (1)'s four-miss blind spot is currently **harmless** (all four missed rows are `done`), but **this one is LIVE**: had the visible sentences been emptied and the families opened, `stop` would have fired with real work on the books, and **a false "done" ends the project early**. **FIXED AT THE MATCHER RATHER THAN THE PROSE** — broadened in `next-prompt.txt`, fixing the **CLASS** and surfacing the **INSTANCE** with no ROADMAP prose edit, with **three** negative controls observed. ⚠️ **The documented trap's stated MECHANISM is ALSO refuted while the trap is real:** a naive `grep -c 'candidates:'` does inflate, but **NOT** via the `candidates were:` recaps — that string does not contain the substring `candidates:` **at all** and is itself invisible to the naive count; the inflation is **table rows carrying BRAINSTORM prose**. **THE THIRD HEADLINE — THE MEASURED RECORD, obtained by replaying the sentinel over EVERY ROADMAP commit rather than by reasoning: the sentence count has NEVER gone down.** Same in every commit since 2026-07-12 across ~17 phases, history `0 -> 1 -> 3`, **not one decrease** — and **phase 74 landed three `ssl.*` names while making its OWN sentence LONGER** (its family clause SPLIT into a dynamic half plus a new `connection_error` clause), while **phase 73 predicted a clause REMOVAL and got a clause REPLACEMENT** because the tracing backlog held more items than the clause named (it still does — six follow-ons where the clause names three). ⇒ **the candidates sentence is a WINDOW onto a larger backlog, not an inventory of it**, which is what makes rejecting a check-(2) narrowing row an evidence-based call rather than a preference. **FOUR INHERITED COSTS REFUTED, IN BOTH DIRECTIONS:** `fault.abort.grpc_status` **DOWN** to ~7-9 (framework blocker retired by probe) · `Listener.stat_prefix` **UP** to 10-12 — ⚠️ **a live probe found the reference MERGES two listeners sharing a prefix into ONE aggregated scope**, both addresses gone and the counter **SUMMED**, where envoy-go's registry **PANICS on duplicate registration** ⇒ a naive implementation crashes at boot on a config the reference serves; its `envoy_listener_address`-carries-a-non-address "risk" is **REFUTED as PARITY** (the reference does exactly that) and its "swallowed-panic boot-hang" mechanism refuted too (**no `recover()` anywhere** in the boot path — a loud crash, not a hang) · `upstream_cluster`'s cost DRIVER refuted hard, **~20-25 lines / 3 files, not ~85-100 / ~18** (phase 73 already built the seam and `extractEndpoints` already receives the cluster name), premise still UNVERIFIED pending one probe, and its Lua misattribution is **TEN sites, not five** · `ssl.connection_error` **UP** to ~12-15 with its framing refuted **in direction** — the reference's counter is BoringSSL **`SSL_ERROR_SSL`, a PROTOCOL error explicitly NOT `SSL_ERROR_SYSCALL`**, so mid-handshake RST / clean close / zero-byte close all increment **0** while garbage bytes and a TLS-1.1-only client increment 1 ⇒ **`syscall.ECONNRESET` is needed not to make it FIRE but to stop it OVER-firing**, the deny-list needs **three** predicates and is **OPEN** (the positive population cannot be typed in Go — mismatch/malformed-DER/version-mismatch all arrive as bare `*errors.errorString`), and ⚠️ **`tls.RecordHeaderError` is returned BY VALUE**, so a predicate drafted against a pointer **compiles and never matches**. **THE gRPC FAMILY CANNOT BE OPENED DEFENSIBLY AT THIS TIP:** its two cleanly-charter-satisfying candidates (`grpc_http1_reverse_bridge`, `grpc_web`) are blocked on the SAME seam — **`RunEncodeTrailers` has ZERO production callers** against 51 `RunDecodeHeaders`, its only call site anywhere being a test — at 16-22+ tasks; `grpc_stats` at ~12-14 is **none of the charter's four named subjects** and carries a **remote-client-triggerable stat-name PANIC** (with `stats_for_all_methods: true` the reference derives names from the client-controlled `:path`, and an observed real name fails `NamePattern` on a hyphen where `getOrRegister` panics). **A MAINTENANCE BUNDLE was costed and REJECTED as the pick though it is the cheapest defensible anything here** — six items at 1-2 tasks each with **ZERO production lines**, all genuinely owed, none touching check (2) or (3): the **10-site** Lua misattribution · `0062`/`0063`'s false *"DETERMINISTIC/EXACT — not a σ-band"* adjective (⚠️ **`:299` CONFIRMED, the SPEC-76 `:300` REFUTED**, and ✅ **negative-controlled** — `0064-lb-subset` carries the same adjective and is **correct**, so the false surface is exactly those two) · `0061/driver/driver_test.go`'s **exactly TWO** provably-vacuous arms, now named (`CollapseBitesSpread` trips conservation, `ReferenceConservation` trips *subject* conservation) · the rate-1.0 CONTROL diagnostic · `0013`'s 10 ms band (⚠️ **the verdict string is written INTO the compared byte stream**, so a jitter blip surfaces as a cross-side **byte divergence**) · `0059`'s empirical margin, **UPGRADED to near-owed** because its `concentrationMin` is **below the project's own 4-5σ bar** while an exact **Binomial** margin IS derivable (⚠️ **no σ numeral is recorded — two independent conversions disagreed on convention, and a contested count gets no number**). ⚠️ **TWO NEW DOCUMENTARY DEFECTS FOUND WHILE RE-DERIVING, both named as deferrals rather than folded in silently.** **(1) The documented PUBLIC IMPORT PATH DOES NOT EXIST** — `ROADMAP.md` and `BEHAVIOR_CONTRACT.md` document the public package under a module path that is **not** this module's (`head -1 go.mod` disagrees, and the live import in `cmd/envoy-go/main.go` and `internal/boot/boot.go` uses the real one); it occurs **20** times across FOUR docs, so **a session copying the documented path writes code that does not compile**. **(2) `BEHAVIOR_CONTRACT.md:1857`'s structural evidence for the settled QUIC `ssl.*` PARITY determination cites TWO line ranges that have BOTH drifted +20** — one of them now lands on the bind-failure unwind block, which does not support the claim at all — and ⚠️ **the cheap mechanical gate CANNOT catch it: an out-of-range check over every Go cite in that file printed NOTHING on a tree carrying three stale cites, because a +20 drift stays IN range.** **A range gate is a FALSE NEGATIVE for anchor drift; only content-anchoring fixes it** — which makes this the **EIGHTH** recorded broken gate. **TWO of the seven existing broken gates were RE-EXECUTED at this tip and BOTH reproduce:** `gofmt -l` **exits 0 while printing the unformatted file** (any `&&`-chained gate is **INERT**; the output-gated form goes RED correctly), and **G-A's `go doc -all` raw-diff false positive** (the `0061` driver emits **51** raw lines but only **1** symbol-scoped line, so any legitimate comment edit reads RED on a correct tree). ⚠️ **A CONTROLLER SELF-CORRECTION, recorded rather than quietly amended:** the first next-free-reference-port derivation printed **10485**, which was a **grep artifact** — `104[0-9][0-9]` matched digits inside unrelated prose in two fixtures. The real `104xx` maximum in use is **10449**, so **10450** stands. **A probe must discriminate.** ⚠️ **A count that is GLOB-DEPENDENT is stated with its command, not as a fact** — the controller's count of fixtures defining an `AssertStats` method disagreed with an agent's under a different glob, and **neither is quoted as "the" count**. **Counts UNCHANGED at this docs-only stage:** fixtures **119** (next-free `0118`) · fuzzers **55** · stat surface **1205** (DOCUMENTARY — no mechanical command; ⚠️ a re-costing pass established a mechanical count is only **PARTIALLY** constructible, for three independent structural reasons — most registration sites build non-literal names, **table fan-out** means one `statroster.New` hit expands to hundreds of names across five callers, and some names are invisible to **any** regex because the registration function is passed as a **method value**, needing `go/types` rather than grep; ⚠️ **`NewHistogram` does not exist and never has**, so a row greping for it reports a meaningless zero, and a defensible count row is **8-11 tasks**, not the small row it sounds like) · BackendKind tail **38** (⚠️ a TAIL; the file declares **39** constants, 0-38 — do **NOT** "fix" 38 to 39) · modules **2** (⚠️ the phase-61.2 lineage figure — **NOT** a repo total; the single `go.mod` requires **67**) · internal packages **73** · DECISIONS tail **ADR-0298 COMPLETE** (next-free **ADR-0299**) · next-free reference port **10450** (⚠️ **NOT** fixture-index aligned). **Sentinel re-run MECHANICALLY by the controller AFTER the ROADMAP edit landed (does NOT fire; `stop` NOT created):** **(1) RE-OPENED** — prints `NOT DONE: row 77`, and ⚠️ **the new row was checked against check (1)'s blind spot BY EXECUTION before being trusted**: the regex applied to the prospective row line in isolation MATCHES and the awk arm fires, with a **negative control** (the same line reading `done`) printing **nothing** · **(2) 3 -> 4** under the old matcher and **5** under the broadened one, and ⚠️ **BOTH increases are CORRECT — the ledger got more accurate, not worse; do NOT "fix" either count down** · **(3)** now prints `NEVER OPENED: gRPC` and `NEVER OPENED: WASM` only — **`Runtime` is legitimately GONE**, because the row genuinely delivers the charter's first clause; the marker phrase is a **legitimate USE**, not the gaming MENTION that fired twice in one commit at the phase-76 BRAINSTORM. ⚠️ **check (1)'s blind spot RE-DERIVED POST-EDIT: 109 data rows / 105 matched / still exactly FOUR misses** — em-dash in an "after" cell, a DOT in a slug, and two LETTER-suffixed ids; all four `done`, and **row 77 is among the MATCHED**. ⚠️ **The Bash cwd reset fired AGAIN — EIGHT consecutive sessions** — so every git command used `git -C <abs-worktree-path>`. `next-prompt.txt` rolled to the **phase-77 SPEC** (**TRACKED despite `.gitignore`**; edited in this worktree).)
- **next-free ADR:** **`ADR-0299`** — DECISIONS.md tail is now **ADR-0298** (STATUS **COMPLETE**). `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1` -> `## ADR-0298`; `grep -c '^## ADR-0299'` -> **0**. ⚠️ **`ADR-0299` is CLAIMED by row 77 but does NOT yet exist** — `DECISIONS.md` is **BYTE-UNTOUCHED** at this BRAINSTORM (normal for the stage), so the tail is still ADR-0298 and next-free is still ADR-0299. Its §Context is drafted at the **phase-77 SPEC** per **ADR-0044-as-used** (⚠️ **ADR-0044 itself does NOT contain that discipline**), with §Decision + §Consequences appended IN PLACE at the IMPL **after the RETAINED italic footer**, mirroring ADR-0295/0296/0297/0298. ⚠️ **It will claim the FIRST Runtime-family ordinal** — unlike ADR-0298, which correctly claimed NONE because the Load-balancing family had closed at row 54 and *maintenance rows do not extend a charter*. ⚠️ **ADR-0089 stays BYTE-UNTOUCHED and that is deliberate**: it defers `/runtime` and `POST /runtime_modify` to this family, but row 77 lands **neither** endpoint, so the un-defer is owed only by whichever later row does. ⚠️ **Write NO whole-file grep count into it** — that species self-falsified in ADR-0296 ¶3 and ADR-0297 ¶7 **and** ¶9, and at the phase-76 BRAINSTORM it escalated from a wrong NUMBER to a **flipped termination-sentinel check**. **ADR-0298 — *"a probabilistic differential-fixture assertion carries a DERIVED margin and is verified by a MEASURED RATE, never by a pass-count"*** — §Context (11 paragraphs) drafted at the phase-76 SPEC per ADR-0044-**as-used**; **§Decision + §Consequences APPENDED IN PLACE at the phase-76 IMPL after the RETAINED italic footer**, mirroring ADR-0295/0296/0297. **No renumber, no new ADR, and NO `---` separator** (the file's last `^---$` is still at `:17020`). Block shape controller-re-derived at this close: **1** `### Context` / **1** `### Decision` / **1** `### Consequences` / **1** retained italic footer / **0** `^## ADR-0299`. ⚠️ **FOUR of its OWN §Context paragraphs were CORRECTED IN PLACE at the IMPL** (¶1, ¶5, ¶7, ¶11) — the ADR-0297 ¶7/¶9 precedent, each correction **leading with what survives** and none of them changing a decision the ADR records. ⚠️ **It carries NO whole-file grep count** — that species self-falsified in ADR-0296 ¶3, ADR-0297 ¶7 AND ¶9, and at the phase-76 BRAINSTORM it escalated from a wrong NUMBER to a **flipped termination-sentinel check**. Every count in it is line-scoped or stated with no number. ⚠️ **It claims NO family ordinal** — the Load-balancing family closed at row 54, and **maintenance rows do not extend a charter**.

---

## Project counts at this commit

*(Live. Re-verify at session start — `git fetch` first — and update here at every stage close. Canonical commands inline.)*

⚠️ **ALL counts below were re-run MECHANICALLY in the stage worktree at this phase-76 IMPL close with the canonical commands, EACH WITH ITS NEGATIVE CONTROL OBSERVED — never copied forward.** **Phase 76 landed +0 on EVERY axis, including +0 PRODUCTION `.go` FILES** — the first chartered row in this lineage whose production-code delta is exactly nothing, and the ANTICIPATED envelope held on all of them. The row's `.go` delta is **three TEST-side files** (`internal/cluster/ringhash_test.go` edited, `test/fixtures/0061-lb-ring-hash/driver/driver_test.go` edited, `.../linkage_test.go` NEW), plus three fixture-doc files and four project docs. ⚠️ **The stat-surface total 1205 is DOC-SOURCED and was NOT re-derived mechanically** — there is no mechanical command, and the chain is known to be discontinuous in **TWO** places (the recorded `1200 -> 1201`, and `Phase 46.1b` closing at 1198 while `Phase 47.1` opens at 1200 — an unattributed +2). **Only the +0 DELTA is asserted, and that argument is STRUCTURAL rather than a `statssink` result:** zero production `.go` bytes changed (G8) and zero byte mismatches over the 375-file production roster (G14), so no `NewCounter`/`NewGauge` call site can have moved; the five `TestNoNewStat*` guards corroborate at 5x PASS. ⚠️ **A future phase should COUNT the surface rather than trust the chain — a +0 row is a cheap place to do it, and phase 76 declined to charter that, so it is now a NAMED maintenance deferral rather than an idle observation.**

- **active differential fixtures:** **119** — ⚠️ **Phase 76 landed +0: `0061-lb-ring-hash` was EDITED, not added** (`sourceIPs` 4 -> 16 plus a NEW `driver/linkage_test.go` inside the EXISTING fixture directory — no new YAML, no new directory, no new BackendKind, no new port), so the count STAYS **119** and `0118` remains FREE. NC observed: `mkdir .../0118-fake` ⇒ 120. — `ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0117-tracing-custom-tags-metadata-cluster` (phase 73). **Phase 75 landed +0**: `0110-tls-require-client-cert-false` was **EXTENDED** with a cross-side `fixture.StatsAsserter` (NO new YAML, NO new directory, NO new BackendKind, NO new port), so the count STAYS **119** and `0118` remains FREE. ⚠️ **Next-free REFERENCE port is `10450`, NOT `10118`** — ports are NOT fixture-index aligned. **The fixture now asserts what its own README confessed was infeasible, and BOTH sides were EXECUTED**: `ssl.handshake=2 ssl.no_certificate=1 ssl.fail_verify_error=1 ssl.fail_verify_no_cert=0`, `downstream_cx_total=3`, **identical on reference and subject**, with the discriminating asymmetry (`handshake=2` vs `no_certificate=1`) observed per side. Shape A over `/stats/prometheus`, keyed on the metric NAME with `envoy_listener_address` IGNORED — the NAMES are byte-identical cross-side while the labels differ (`0.0.0.0_10446` reference vs **`___20016`** subject: ⚠️ **the subject binds the IPv6 WILDCARD**). ⚠️ **`0111` was NOT given a value assertion, deliberately** — it runs `require_client_certificate: true` on both sides, so a success-path no-cert annotation reads **0 on every arm, structurally**, and a value pin would document a **vacuous `0 == 0`** as coverage; its PROSE gained the name in its NAMED-UNASSERTED list instead, because **both `0111` passages are CLOSED ENUMERATIONS** and a name in neither list reads as ASSERTED. **`0111`'s driver is byte-untouched, and its named-subset check was EXECUTED for the first time at this IMPL: PASS.** ⚠️ `var _ fixture.StatsAsserter` is a **TRIPWIRE, not the dispatch mechanism** — `0110` is only the **THIRD** fixture repo-wide to carry it, while **82** define an `AssertStats` method.
- **fuzzers:** **55** — **Phase 76 landed +0** (it consumes no new config field, so there is no parse arm to fuzz). NC observed: appending `func FuzzProbeOnly` ⇒ 56. — `grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`. **Phase 75 landed +0** — the row consumes no new config field at all, so there is no parse arm and no fuzz-seed classification to flip (`reference_fuzzer_count_docs_drift`: an `f.Add` seed is not a fuzzer).
- **stat surface:** **1205** — BEHAVIOR_CONTRACT doc count. **There is NO mechanical counting command** (the contract says so itself); enforcement is per-phase `TestNoNewStat*` guards asserting a delta of ZERO, never the absolute total. **Phase 75 LANDED +1 (1204 -> 1205)** — `listener.<normalized-addr>.ssl.no_certificate`, the FOURTH listener-scope `ssl.*` name and the FIRST **SUCCESS-PATH** handshake annotation (⚠️ **NOT a synonym for `ssl.fail_verify_no_cert`, and the mis-mapping is wrong in BOTH directions** — one annotates an ACCEPTED handshake, the other books a REJECTED one; they are mutually exclusive on every connection). ⚠️ **The delta is a NAME-surface delta, NOT a flat per-deployment one:** registration rides the block that ALREADY gates on `rt.tlsMode`, so a **plaintext-only** deployment gains **ZERO** names, while a **QUIC** listener gains it permanently zero — **PARITY** with the reference, and structural (`Manager.Start` launches no accept loop for `kindQUIC`, so the Inc site is unreachable for a **name-independent** reason). ⚠️ **QUIC was NOT re-probed at phase 75** — the parity claim is INHERITED from phase 74's family-level probe, and that inheritance is stated in BEHAVIOR_CONTRACT rather than presented as a measurement. Ledger updated: `1204` -> `1205` at `BEHAVIOR_CONTRACT.md:831`/`:847`, plus a `**Phase 75 - 1204 -> 1205 (+1)**` line after the tail entry — **the ONLY line-adding edit in the file** (`5744 -> 5746`), so every by-line citation into the TLS subsection stays valid. ⚠️ **The new entry RECORDS BOTH ledger gaps and BACK-FILLS NEITHER** (fabricating a line would be inventing a record). Gate figures at this close: `grep -c '1204'` => **2** · `grep -c '1205'` => **3** · `grep -cE '^#{2,4} .*ADR-[0-9]+.*ADR-[0-9]+'` => **17** (was 16 — the `:1849` heading joined them, on the `:785` precedent). ⚠️ **`internal/statssink`'s four *"stays 1200 / 1196"* prose sites, stale since phase 49, are RECORDED-NOT-FIXED** — that package is on this phase's byte-untouched sha256 roster.
- **BackendKind tail:** **38** — **Phase 76 landed +0** (`0061` uses the in-process `TCPEcho` branch and implements neither `BackendKindAware` nor `PerHostBackendKind`). NC observed: appending `ZZProbeKind = 39` ⇒ tail 39. — (`H2GoawayResponder`, `test/differential/fixture/fixture.go:614`). **Phase 75 landed +0** — `0110` was extended, not cloned, and it implements neither `BackendKindAware` nor `PerHostBackendKind`, so the runner takes the in-process `TCPEcho` branch. ⚠️ **This is a TAIL VALUE, not a count** — the file declares **39** constants (values 0-38, `TCPEcho = 0`). The lineage figure **38** is correct as written; do NOT 'fix' it to 39.
- **DECISIONS.md tail:** **ADR-0298** (next-free **ADR-0299**) — `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1` -> `## ADR-0298`; `grep -c '^## ADR-0299'` ⇒ **0**. **ADR-0298 is COMPLETE as of the phase-76 IMPL** — §Context drafted at the phase-76 SPEC, §Decision + §Consequences APPENDED IN PLACE after the RETAINED italic footer, **no renumber and NO `---` separator** (the file's last `^---$` is still at `:17020`). Block shape controller-re-derived at this close: **1** `### Context` / **1** `### Decision` / **1** `### Consequences` / **1** retained footer. ⚠️ **FOUR of its OWN §Context paragraphs were CORRECTED IN PLACE** (¶1's `:295` -> `:307`, ¶5's `-count ~= 81` -> **83**, ¶7's *"~71 collapses"* -> **74/2000**, ¶11's *"needs no edit"* -> REFUTED) on the ADR-0297 ¶7/¶9 precedent, each **leading with what survives**; none changes a decision the ADR records. ⚠️ **It carries NO whole-file grep count and claims NO family ordinal** — the Load-balancing family closed at row 54, and **maintenance rows do not extend a charter**. *(Historical, retained: **ADR-0297 is COMPLETE** — §Context drafted at the phase-75 SPEC (ADR-0044-**as-used**; ⚠️ ADR-0044 does NOT actually contain that discipline), §Decision + §Consequences appended IN PLACE at the phase-75 IMPL after the **RETAINED** footer, mirroring ADR-0295/0296. Block shape verified at this close: **1** `### Context` / **1** `### Decision` / **1** `### Consequences` / **1** retained footer; `grep -c '^## ADR-0298'` => **0**. **No renumber, no new ADR.** ⚠️ **Its OWN §Context ¶7 and ¶9 were CORRECTED IN PLACE while completing it** — ¶7's **self-falsifying whole-file grep** (the same species the phase-74 IMPL had just fixed in ADR-0296 ¶3, reproduced one ADR later) is restated with **NO number** and the searched token deliberately **NOT spelled**, because every restatement mints another counter-example; and ¶9's *"SELF vs OTHER-ADR"* form rule is replaced by **graft SCALE** after the n=7 population was re-enumerated first-hand. ⚠️ **¶9 ALSO said *"the only two instances in the file"* — which this row's own blockquote makes three** — so a pure append would have shipped a SECOND self-falsifying count in the commit that fixed the first. **The prescription has now outlived THREE wrong justifications: family, self-vs-other, scale.** ⚠️ **`ADR-0296` is NO LONGER byte-untouched** — its §Decision (g) carries the phase-75 correction as an INDENTED blockquote (`grep -c '^  > '` **2 -> 3**), **LEADING WITH WHAT SURVIVES** (no such counter was owed *in phase 74*, whose sole stat-asserting fixture is `require=true` on both sides — **the deferral was RIGHT**) and claiming **NO ORDINAL**.)*
- **go.mod modules:** **Phase 76 landed +0** — `go mod tidy -diff` EMPTY and `git diff master -- go.mod go.sum` EMPTY; ⚠️ **the row's TEST-side import delta is +8 and that is CORRECT** (`ringhash_test.go` gains `fmt`/`math`/`mathrand "math/rand/v2"`, `linkage_test.go` adds `go/ast`/`go/parser`/`go/token`/`strconv`/`testing`) — all stdlib, all test-side, and **an import LINE is not a go.mod MODULE**; there is no new sub-package, so `reference_new_subpackage_pulls_transitive_module` does not bite. NC observed: a fake `require` ⇒ 67 -> 68. The tracked lineage figure is **2** (the phase-61.2 count: `quic-go` direct + `qpack` indirect) — **NOT a repo total**; the single `go.mod` requires **67** (18 direct + 49 indirect). **Phase 75 landed +0** — `go mod tidy -diff` EMPTY and `git diff master -- go.mod go.sum` EMPTY. The row needed `crypto/tls` and `internal/stats`, both ALREADY imported by `internal/listener/manager.go`; **an import LINE is not a go.mod MODULE**, and there is no new sub-package, so `reference_new_subpackage_pulls_transitive_module` does not bite. ⚠️ **TEST-side imports grew by exactly four** (`log`, `math`, `net/http`, `strconv` in `0110`'s driver) — all stdlib, all test-side, **not a production-envelope violation**.
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` (see `ENVOY_TARGET.md`).
- **conformance:** h2spec 53/53; proxy-wasm all 10 families PASS.

---

## Recent lineage

*(The last FIVE stages only — enough for the router's "prior-phase lineage" read. Everything earlier is in `STATE_HISTORY.md`. These bullets are carried over VERBATIM; the newest mirrors §Current (**the phase-77 BRAINSTORM done** — the stage that OPENED the Runtime + hot restart family and RE-OPENED sentinel check (1) after its third-ever silent close, and that found the sentinel's OWN check (2) **FAILING UNSAFE** at this tip while refuting the router's *"strictly more"* selection calculus as arithmetic), and the oldest (**phase-75 IMPL done**) was dropped as this list re-capped at five. The five now read: phase-77 BRAINSTORM · phase-76 IMPL · phase-76 PLAN · phase-76 SPEC · phase-76 BRAINSTORM.)*

- **prior active-phase:** `phase 77 (runtime-static-layer) BRAINSTORM done` (2026-07-26) — lifecycle-state **DONE -> 1**; **ROW 77 REGISTERED `in-progress`** at `ROADMAP.md:139`, **RE-OPENING sentinel check (1)** after its third-ever silent close; **`DECISIONS.md` BYTE-UNTOUCHED**. Fresh worktree `/home/esa/git/envoy-go-wt-p77bs` off master `f48975c4`, branch `phase-77-brainstorm`. Docs-only: **ZERO production `.go`, ZERO test `.go`**; exactly THREE files edited (`ROADMAP.md`, `STATE.md`, `next-prompt.txt`) plus the new `BRAINSTORM.md`. **FOUR read-only re-costing agents at tip `f48975c4` on disjoint remits with PRIVATE scratch** — two running **LIVE reference probe fleets** against `envoyproxy/envoy:contrib-v1.37.2` (image id verified against the pin, fresh container per arm, bridge network) — plus controller re-derivation of every load-bearing claim. ⚠️ **THIS WAS THE FIRST REGISTRATION IN THE PROJECT'S HISTORY PERFORMED FROM A STATE WHERE EVERY CHARTERED ROW WAS ALREADY `done`.** **THE PICK, SELF-PICKED per the 2026-07-12 standing directive: THE FIRST Runtime-+-hot-restart-family ROW — THE FAMILY OPENS** — a bootstrap `layered_runtime` **static-layer** consumer. **Today envoy-go boot-REJECTS a config the reference accepts and serves** (`internal/bootstrap/bootstrap.go:568-569`, a raw-YAML generic-map pre-check firing on mere key presence BEFORE `protojson.Unmarshal` — so lifting it lets the message unmarshal where ADR-0016 already covers unknown subfields, and only the SEMANTIC arms need hand-writing). It lands an `internal/runtime` `Snapshot` **in the phase-00 placeholder directory that ALREADY EXISTS** ⇒ **+0 new packages**, `go list -deps` a clean leaf. Anticipated: **+2 stats (1205 -> 1207) / +1 fixture (`0118`) / +0 fuzzers (a corpus seed for the EXISTING `FuzzBootstrapLoad` is not a new `func Fuzz`) / +0 BackendKinds / +0 modules / +0 new PUBLIC surface**; ONE new **ADR-0299** claiming the FIRST family ordinal. **THE HEADLINE — THE ROUTER'S OWN SELECTION CALCULUS IS REFUTED AS ARITHMETIC.** *"A check-(3)-clearing candidate is worth **strictly more**"* does not hold: opening a family **MINTS** a check-(2) sentence, so the item count went **3 + 3 -> 2 + 4 = 6 -> 6, NET ZERO**. What survives is strictly weaker and is the real reason to take the pick: **opening a family is the ONLY way that family's work becomes enumerable at all.** **THE SECOND HEADLINE — CHECK (2) IS FAIL-UNSAFE AND WAS FAILING UNSAFE AT THIS TIP:** it matched only the long phrase, so the **Operational-tooling** family's genuinely un-chartered remainder, recorded in the **SHORT** form in the same paragraph that says the family STAYS OPEN, was **INVISIBLE**. Check (3) fails **SAFE** by construction and check (1)'s four-miss blind spot is **harmless** (all four missed rows `done`), but **this one is LIVE** — `stop` could have fired with real work on the books, and **a false "done" ends the project early**. **FIXED AT THE MATCHER, NOT THE PROSE** (broadened in `next-prompt.txt`, three negative controls observed) — fixing the CLASS and surfacing the INSTANCE with no ROADMAP prose edit. ⚠️ **The documented trap's stated MECHANISM is ALSO refuted while the trap is real:** the naive count inflates via **table rows carrying BRAINSTORM prose**, NOT via the `candidates were:` recaps, which do not contain the searched substring at all. **THE THIRD HEADLINE — THE MEASURED RECORD, from replaying the sentinel over EVERY ROADMAP commit: the sentence count has NEVER gone down** (unchanged in every commit since 2026-07-12 across ~17 phases, history `0 -> 1 -> 3`), and **phase 74 landed three `ssl.*` names while making its OWN sentence LONGER**, while **phase 73 predicted a clause REMOVAL and got a clause REPLACEMENT** because the backlog held more items than the clause named. ⇒ **the sentence is a WINDOW onto a larger backlog, not an inventory of it** — which is what makes rejecting a check-(2) narrowing row evidence-based rather than a preference. **THE SEMANTICS ARE PINNED BY MEASUREMENT ACROSS ELEVEN LIVE ARMS:** `num_keys` is the flattened-leaf **UNION** (a key in two layers counts **ONCE** — and a single-layer config **cannot** discriminate union from per-layer sum, so the 2-layer overlap arm is load-bearing); a `{numerator,denominator}` Struct **TERMINATES** flattening and counts as one; an empty-string value still counts; `num_keys == len(entries)` and `num_layers == len(layers)` held in **every** arm. ⚠️ **AN OFF-BY-ONE TRAP ISOLATED RATHER THAN REASONED ABOUT:** `layered_runtime` present with ZERO declared layers reads `num_layers: 1` with `layers: [""]`, shape-independent across three spellings, and a **200-vs-503 cross-product with BOTH controls** proves that phantom layer is a synthesized **WRITABLE ADMIN** layer ⇒ **the honest "empty" arm is `layered_runtime` absent entirely, NEVER `layers: []`**; D-RSL-EMPTY settles envoy-go's posture, with PARSE-REJECT recommended. ⚠️ **THE FIXTURE MUST NOT GO THROUGH `ProbeAdmin`** — `compareAdminResponses` compares the body **BYTE-EXACT** while the reference `/runtime` body is **doubly non-deterministic** (JSON key order randomized **PER REQUEST**; the Struct debug-string prefix **PER PROCESS**, flipping across a `docker restart`) ⇒ assert **STATS** via the existing `StatsAsserter` seam on a **NAMED SUBSET**; both randomizations contaminate only the body, never the gauge values, and both gauge NAMES were measured present in all eleven arms **including the absent-config arm (value 0)** ⇒ no name-presence divergence. **FOUR INHERITED COSTS REFUTED, IN BOTH DIRECTIONS:** `fault.abort.grpc_status` **DOWN to ~7-9 / +0 on every axis** (its state is *worse* than "ignored" — a `grpc_status` variant fires **no abort at all**; and the reference emits `grpc-status`/`grpc-message` as response **HEADERS not trailers**, retiring the framework blocker) — **BANKED as the recommended phase-78 opening**, and ⚠️ **the PROVENANCE of the three-stage claim that it opens the gRPC family is now LOCATED: row 09's own summary carries `(gRPC family)` as a ROUTING LABEL — reading a filing label as a charter is the error** · `Listener.stat_prefix` **UP to 10-12** (⚠️ the reference **MERGES** two listeners sharing a prefix into ONE scope with the counter **SUMMED**, where envoy-go's registry **PANICS on duplicate registration**; its label "risk" is **PARITY** and its "boot-hang" mechanism refuted — **no `recover()` anywhere**) · `upstream_cluster`'s cost driver refuted hard (**~20-25 lines / 3 files, not ~85-100 / ~18**), premise still UNVERIFIED, Lua misattribution **TEN sites not five** · `ssl.connection_error` **UP to ~12-15**, framing refuted **in direction** — the counter is **`SSL_ERROR_SSL`, NOT `SSL_ERROR_SYSCALL`**, so RST / clean close / zero-byte close all increment **0** ⇒ `syscall.ECONNRESET` is needed to stop it **OVER**-firing, the deny-list needs **three** predicates and is **OPEN**, and ⚠️ **`tls.RecordHeaderError` is returned BY VALUE** so a pointer-drafted predicate **compiles and never matches**. **THE gRPC FAMILY CANNOT BE OPENED DEFENSIBLY HERE** — its two charter-satisfying candidates are blocked on the SAME seam (**`RunEncodeTrailers` has ZERO production callers**) at 16-22+, and `grpc_stats` at ~12-14 is none of the charter's four subjects and carries a **client-triggerable stat-name PANIC**. ⚠️ **TWO NEW DOCUMENTARY DEFECTS, named as deferrals rather than folded in:** the documented **public import path does not exist** (20 occurrences across four docs under a module path `go.mod` disagrees with ⇒ a session copying it writes non-compiling code), and **`BEHAVIOR_CONTRACT.md:1857`'s structural evidence for a settled PARITY determination cites two ranges that have BOTH drifted +20**, one now landing on unrelated code — ⚠️ **and the cheap gate CANNOT catch it: an out-of-range check printed NOTHING on a tree with three stale cites, because a +20 drift stays IN range. A range gate is a FALSE NEGATIVE for anchor drift** ⇒ the **EIGHTH** recorded broken gate. **TWO of the seven existing broken gates RE-EXECUTED here and BOTH reproduce:** `gofmt -l` **exits 0 while printing the unformatted file**, and **G-A's raw `go doc -all` false positive** (51 raw lines vs **1** symbol-scoped). ⚠️ **A CONTROLLER SELF-CORRECTION:** the first next-free-port derivation printed a wrong figure from a **grep artifact** (`104[0-9][0-9]` matching digits inside unrelated prose); the real maximum in use is **10449** ⇒ **10450** stands. **A probe must discriminate.** **Counts UNCHANGED:** fixtures **119** · fuzzers **55** · stat surface **1205** (DOCUMENTARY; ⚠️ a mechanical count is only **PARTIALLY** constructible — non-literal names, `statroster` **table fan-out**, and registration functions passed as **method values** invisible to any regex; **`NewHistogram` has never existed**; a defensible count row is **8-11 tasks**) · BackendKind tail **38** · modules **2** · internal packages **73** · DECISIONS tail **ADR-0298 COMPLETE**. **Sentinel re-run MECHANICALLY AFTER the ROADMAP edit (does NOT fire; `stop` NOT created):** **(1) RE-OPENED** — `NOT DONE: row 77`, and ⚠️ **checked against the blind spot BY EXECUTION first**, with a **negative control** (the same line reading `done`) printing nothing · **(2) 3 -> 4** old matcher, **5** broadened — ⚠️ **both increases are CORRECT; do NOT "fix" either down** · **(3)** `NEVER OPENED: gRPC` / `NEVER OPENED: WASM` only — **`Runtime` legitimately GONE**, a **legitimate USE** of the marker, not the gaming MENTION that fired twice in one commit at the phase-76 BRAINSTORM. ⚠️ **check (1)'s blind spot RE-DERIVED POST-EDIT: 109 rows / 105 matched / still exactly FOUR misses**, all `done`, with **row 77 among the MATCHED**. ⚠️ **The Bash cwd reset fired again — EIGHT consecutive sessions.** **Next -> the phase-77 SPEC** (settle the D-RSL-* docket BY EXECUTION; ⚠️ **the 9-11 cost is a FLOOR** — it does not yet price the reject roster).

- **prior active-phase:** `phase 76 (lb-ring-hash-spread-margin) IMPL done` (2026-07-26) — lifecycle-state **3 -> DONE**; **ROW 76 FLIPPED `in-progress` -> `done`** at the T9 six-gate (ADR-0106, the SOLE leg — a SINGLE FLAT ROW, `reference_roadmap_split_phase_row_done`); **ADR-0298 PROPOSED -> COMPLETE in place, no renumber.** Fresh worktree `/home/esa/git/envoy-go-wt-p76impl` off master `921bc148`, branch `phase-76-impl`; **SEVEN subagent dispatches over the T1-T9 spine -> 8 local commits -> ONE squash.** T4 and T7 ran CONCURRENTLY on disjoint file sets ⇒ ⚠️ **commit order is NOT task order**, and every dispatch staged by **explicit path** — the PLAN's own Step-5 commands say `git commit -am`, which would have swept in a sibling agent's uncommitted work. **THE ROW DELIVERED:** `0061-lb-ring-hash`'s spread assertion now carries a DERIVED margin — `sourceIPs` **4 -> 16**, a seeded collapse-rate **MEASUREMENT** with a stacked K=4 non-vacuity CONTROL, a **`go/parser` LINKAGE gate**, two silently-vacuous unit tests rescued, and every prose site the fix falsifies swept. **+0 stats (1205) / +0 fixtures (119) / +0 fuzzers (55) / +0 BackendKinds (38) / +0 modules (2) / +0 PRODUCTION `.go` FILES / +0 production imports / ZERO new packages / ZERO new exported symbols** — asserted BY COMMAND, per-package. ⚠️ **SEVENTEEN CLAIMS REFUTED, TWO OF THEM GATE COMMANDS — the lineage's broken-gate count goes FIVE -> SEVEN.** **G-A: the PLAN's own G9 `go doc -all` form is NOT symbol-scoped and reads RED on a correctly-implemented tree** — the `0061` driver package exports **zero** top-level symbols, so its entire doc output is package PROSE and this row's legitimate comment rescale surfaces as three diff lines. **Not a fail-open but a FALSE POSITIVE**, which is worse in a specific way: it would either stop a close script or teach a future session to suppress the gate. **G-B: the PLAN's wrong-baseline caveat NAMES THE WRONG SHA** — the phase-75 **tip** (`c57b98b8`) is an **ANCESTOR** of master and returns **0**, i.e. harmless; the dangerous sha is the phase-75 **pre-IMPL router** commit (`9f5d667b`), returning **2**. **A session following it literally would guard the SAFE sha and walk into the unsafe one.** **ALL SIXTEEN BREAKS EXECUTED at this tip.** **Break α fired `driver.go:307`** — the spread leg and ONLY it, byte-identical to the expected string, affinity/conservation/length all surviving (⚠️ **the PLAN's `:295` is the PRE-T5 anchor — this row's own comment sweep moved all five legs ~+12; a stage's anchors drift under its OWN edits**); restore ⇒ `ok … 4.670s`, a green FOLLOWING an observed red. ⚠️ **BUT α IS NOT ISOLATING: the unit package catches it by TWO tests of 404, not the PLAN's ONE of 403 — because THIS ROW'S OWN T1 added the second.** A finding stated against a denominator the same row then moves is stale by construction. ⚠️ **AND THE MEASUREMENT'S CONTROL-LEG DIAGNOSTIC PROSE IS WRONG IN A WAY NO TEST CAN CATCH:** it says *"a rate of 0 HERE means the ring is no longer being redrawn"*, but under α it goes red at rate **1.0**, the opposite pole. **The assertion is correct; the sentence explaining it is not.** Recorded, deliberately not fixed. **β-single-edit DID NOT FIRE (exit 0) while T4's linkage gate went RED on the SAME tree** — the pairing that justifies T4, discharged by execution rather than argued. **G6 full differential: 119/119 PASS / 0 FAIL / 0 SKIP, `ok … 409.759s`**, `comm -3` fixture-set vs subtest-set EMPTY, first attempt, no flake, **`0061` did not fire a spread failure**; ⚠️ **NO cross-session runtime delta claimed from that absolute.** **G7 `-race` FAILED on the FIRST attempt and was classified on evidence:** `0082-grpc-access-log-buffering` **PANICKED** at `bind: address already in use` on an ephemeral port inside `ReferenceBootstrap` (`runner_test.go:200`) **BEFORE any assertion executed**, aborting the binary at **84 of 119** subtests; `0082` **byte-untouched** by this row and **isolate-green 3/3 under `-race`** ⇒ **startup class**. ⚠️ **NOT on this stage's roster by name — the FIFTH consecutive stage at which that has held.** ⚠️ **The background-task notification reported "exit code 0"; the real `go test` status was 1 — a harness's exit code is not the command's.** Clean re-run **119/119 PASS, ZERO data races, zero panics, `ok … 400.595s`.** ⚠️ **The startup flake firing MORE readily under `-race` is REPRODUCED, a third data point.** **THE RUNTIME QUESTION DISCHARGED AS "BELOW RESOLUTION", NOT AS A NUMBER:** same-session control, 3 warm runs/arm — K=16 **4.627 s** vs K=4 **4.523 s** ⇒ **+0.104 s (+2.3%)**, **smaller than the K=16 arm's own spread (0.19 s)**, and ⚠️ **the SIGN FLIPPED against the PLAN's own same-session measurement (−1.2%)**. **Two same-session controls disagreeing in SIGN is the strongest available evidence the effect is below measurement resolution.** SPEC §3.6's **+4.5% stays REFUTED**; carry no signed delta forward. **G14 roster production-only 375 files / 0 mismatches / 0 missing**, intersection with the 10-file EDIT roster **EMPTY** — the phase-75 roster was NOT copied forward, so the phase-73 SEVERE-1 is not reproduced; **NC observed on BOTH legs**, the MISSING leg mattering because a deletion otherwise reads as *"no mismatch"*. **ADR-0298 completed IN PLACE and FOUR of its OWN §Context paragraphs CORRECTED**, each leading with what survives: **¶1**'s `:295` -> `:307` (⚠️ **a line cite drafted at the SPEC, for a file the IMPL then edits ABOVE it, is stale BY CONSTRUCTION**) · **¶5**'s `-count ~= 81` -> **83** (94.6% vs the 95% claimed; ⚠️ **the error FLATTERS the ADR's own conclusion**) · **¶7**'s *"~71 collapses"* -> **74/2000** · **¶11**'s *"needs no edit"* -> ⚠️ ***"the bound still holds"* and *"the text needs no edit"* are DIFFERENT claims.** **Row 76's FOUR stale BRAINSTORM-era claims all corrected.** **Sentinel re-run MECHANICALLY AFTER the ROADMAP edit (does NOT fire; `stop` NOT created):** **(1) SILENT** — ⚠️ **guarded against a false negative** by applying the check-(1) regex to `:138` alone, which returns the `done` row ⇒ **the silence is genuine** · (2) => **3** · (3) `NEVER OPENED: gRPC/Runtime/WASM`. ⚠️ **check (1)'s blind spot RE-DERIVED: 108 / 104 / FOUR misses**, all `done`. ⚠️ **The Bash cwd reset fired again — SEVEN consecutive sessions.** **Next -> the phase-77 BRAINSTORM, which must CHARTER A NEW ROW** — row 76 was the last, and with check (1) silent the project is held open by checks (2) and (3) alone.

- **prior active-phase:** `phase 76 (lb-ring-hash-spread-margin) PLAN done` (2026-07-26) — lifecycle **2 -> 3**; **ROW 76 STAYS `in-progress`**; **ROADMAP and DECISIONS BYTE-UNTOUCHED**. Fresh worktree `/home/esa/git/envoy-go-wt-p76plan` off master `98c27fc9`, branch `phase-76-plan`. File set: the new `PLAN.md` (a **NINE-task TDD spine T1-T9** with a **SIXTEEN-break roster**) + the new `PROGRESS.md` + `STATE.md` + `next-prompt.txt`; **ZERO production `.go`, ZERO test `.go` in the PLAN COMMIT.** **FIVE agents on disjoint remits, PRIVATE scratch, SEPARATE worktrees**, plus controller re-derivation of every load-bearing claim. **THIS PLAN EXECUTED MOST OF THE ROW, and THIRTEEN of the SIXTEEN breaks RAN here.** **THE HEADLINE — THE SPEC'S OWN LINKAGE GATE IS BROKEN IN BOTH DIRECTIONS AT ONCE.** It **FAILS OPEN** on empty captures (`[ "" = "" ]` is TRUE ⇒ **exit 0 on a tree where NEITHER constant exists** — rename, move or delete all read green; controller-reproduced) **AND FAILS CLOSED** on a correctly-synced tree (`grep -c 'collapseFixtureK'` ⇒ **2**, the second match being the measurement test's **OWN DOC COMMENT** ⇒ `DESYNC: sourceIPs=16 collapseFixtureK=16`, exit 1). ⚠️ **The document that warns *"a green gate is evidence only if you have seen it go red"* shipped a gate whose red arm was never fired.** REPLACED by a **`go/parser` Go test** in the fixture's own package — structurally immune (the parser sees declarations, not comments), **+0 exported symbols** — proved over the **FULL 2x2 cross-product** plus the empty-capture arm; ⚠️ **the (4,4)-passes arm is the load-bearing one**, since it proves the gate READS the test-side value rather than hardcoding 16. **TWO MORE BROKEN GATES, BOTH NEW TO THE LINEAGE:** ⚠️ **`go doc -all <pkgA> <pkgB>`'s fail-open is specific to the `./` PREFIX** — a BARE import path exits **1** (`doc: no symbol …`), a `./`-prefixed one exits **0** silently dropping the second argument **even when it names a directory that does not exist** ⇒ **NARROWS** `reference_gate_command_negative_control`; and ⚠️ **`gofmt -l` NEVER exits non-zero** (observed exit 0 while printing the unformatted file) ⇒ any gate chaining it with `&&` is **INERT — gate on OUTPUT**. Plus a **THIRD, unrecorded `impblock` defect**: it matches only `^import \($` and so emits ZERO lines for the two single-line `import "testing"` files — **blind to 2 of this row's 3 `.go` files**. ⚠️ **The phase-75 sha256 roster CANNOT be copied forward** — `internal/cluster/**` was byte-gated there and this row EDITS `internal/cluster/ringhash_test.go` (`comm -12` returns exactly that file); phase 76's roster is production-only (**375 files**), intersection with the EDIT roster **EMPTY**. **THE SECOND HEADLINE — THE NAIVE RESCALE MANUFACTURES A THIRD VACUITY, AND NO DOCUMENT SAW IT:** `{20,28,16}` x4 = `{80,112,64}`, **all three multiples of 16** ⇒ the tuple passes affinity, falls through to spread, satisfies `err == nil`, and prints `--- PASS` while testing a different leg — **the one test that was HEALTHY would have been silently broken.** `{20,108,128}` used instead. ⚠️ **The SPEC's vacuity MECHANISM is two-thirds right** — affinity is evaluated INSIDE the per-element loop (`:284`), BEFORE conservation (`:291`), so `_ScatterBitesAffinity` and `_SubjectConservation` were **never vacuous**; the COUNT is right, the stated REASON over-generalises. **EXECUTED:** the measurement BUILT + GREEN (`CONTROL K=4: 74/2000 rate=0.03700`, `MEASURED K=16: 0/2000`, 0.61 s) · **γ-SHARP FIRED** — frozen ports drove **CONTROL RED while MEASURED stayed GREEN at 0/2000**, so the measured leg's **NULL-RESULT status is MEASURED, not argued** · γ-restore green **after an observed red** · **β two-edit FIRED** (35x over the bar) · ⚠️ **β-single-edit DID NOT FIRE, as declared** (exit 0; `grep -rn '\bsourceIPs\b' --include='*.go' .` ⇒ **3 hits, all in the 0061 driver**) ⇒ **a two-edit β proves only the UNIT TEST's constant load-bearing** · `sourceIPs` 4->16 + the rescale, **6/6 PASS with every leg re-proved BY MEASUREMENT** · **six anti-vacuity breaks, one per test, each firing exactly ONE red** · ⚠️ **the OWED `127.0.0.12`-`.17` probe DISCHARGED: 16/16 binds PASS**, requested `LocalAddr` == observed on every arm (a read-then-write backend, pre-empting the `io.Copy` self-splice artifact that made the SPEC's first probe report ten bogus `i/o timeout`s) · the law over **2e5 real ring draws per K** with a random-key discriminator · the **full differential at MASTER: 119/119 PASS, 408.8 s**, `0061` 3.42 s, first attempt, no flake · `BEHAVIOR_CONTRACT:1326` verified in scratch (**5746 -> 5746 lines**, `diff` exactly `1326c1326`, `% 16 == 0` intact) · every six-gate negative control observed RED. ⚠️ **`3^(1-K)` IS A CONSERVATIVE UPPER BOUND** — p̂/analytic **0.949** at K=4, **0.689** at K=8, while a random-key arm recovers 1.010/1.104 ⇒ the deficit belongs to the specific `xxHash64` key set. ⚠️ **K=16 IS NOT MEASURED AND CANNOT BE AT THAT SCALE** (expected count 0.014) — **7.0e-8 is analytic/extrapolated.** ⚠️ **CONTROL is 74/2000, not 71.** **SPEC §10 CARRIES FOUR ANCHOR ERRORS** (`README:58`->**`:57`**; **`:114`/`:135` MISSING**, `:135` with TWO `64`s; `driver_test.go` **15** sites not 8, two of them **executable `t.Fatal` strings**), **SPEC §4's collision claim is FALSE for `allSame`** (3 live Go hits in `package router`; helper renamed `collapseAllSame`), **its import prediction is refuted** (`fmt`/`math`/`math/rand/v2`, and `ringhash_test.go:3` is a single-line import needing a 1->N conversion), and **`0062`/`0063`'s σ-band comment is `:299`, not `:300`**. ⚠️ **AN AGENT DISAGREEMENT THE CONTROLLER RULED ON:** one agent said the *"DETERMINISTIC/EXACT — not a σ-band"* sites need no edit; **ruled against it on the text** — the adjective governs the compound *"affinity + SPREAD"* while only the parenthetical is affinity-scoped, and those sites carry **none** of the stale numerals, which is exactly why a numeral-keyed sweep cannot reach them. ⚠️ **A CONTROLLER SELF-CORRECTION, recorded not quietly amended:** this stage's first STATE draft wrote the three ADR-0288 field-name tokens verbatim while reporting their counts — **and the act of recording them changed them**, the ADR-0296 ¶3 / ADR-0297 ¶7 species reproduced one stage later. The numbers are now stated as a property with no numerals. **Counts UNCHANGED:** fixtures **119** · fuzzers **55** · stat surface **1205** (DOCUMENTARY) · BackendKind tail **38** · modules **2** · DECISIONS tail **ADR-0298 PROPOSED** (next-free **ADR-0299**). **This row narrows NOTHING** ⇒ check (2) **STAYS 3 through the IMPL.** **Sentinel re-run MECHANICALLY (does NOT fire; `stop` NOT created):** (1) `NOT DONE: row 76` · (2) => **3** · (3) `NEVER OPENED: gRPC/Runtime/WASM`. ⚠️ **check (1)'s blind spot RE-DERIVED: 108 / 104 / FOUR misses**, all `done`. **Next -> the phase-76 IMPL** (break α, the full suite at K=16, `-race`, the prose sweeps, `BEHAVIOR_CONTRACT:1326`, ADR-0298 completion, row 76 -> `done`).

- **prior active-phase:** `phase 76 (lb-ring-hash-spread-margin) SPEC done` (2026-07-25) — lifecycle-state **1 -> 2**; **ROW 76 STAYS `in-progress`**; **ROADMAP BYTE-UNTOUCHED**. Fresh worktree `/home/esa/git/envoy-go-wt-p76spec` off master `6ef436ac`, branch `phase-76-spec`. File set: `DECISIONS.md` (ADR-0298 §Context) + `STATE.md` + the new `SPEC.md` + `next-prompt.txt`; **ZERO production `.go`, ZERO test `.go` in the SPEC commit.** **FOUR agents on disjoint remits, PRIVATE scratch, SEPARATE worktrees**, plus controller re-derivation of every load-bearing claim. **THE HEADLINE — *"THE EXECUTABLE DELTA IS ONE INTEGER"* IS FALSE.** `test/fixtures/0061-lb-ring-hash/driver/driver_test.go` (6 tests) hard-codes 64-summing tuples and is named in **NO** phase-76 document. At the raised constant `_Affinity` **FAILS**; **`_CollapseBitesSpread` and `_ReferenceConservation` go SILENTLY VACUOUS**, tripping subject-conservation before their own leg while still printing `--- PASS` — the first is the unit test for **this row's own assertion**. ⚠️ **THREE confirmations blind in the same direction**: a file-scoped `grep` over `driver.go`, a `git diff --stat` reading `1 file changed`, and a green `go test ./test/differential/`. ⇒ **a change-set measure is not a build measure.** All six tests re-proved BY MEASUREMENT to fire their own leg after rescaling to 256. **THE SECOND HEADLINE — THE CONSTANT WAS BELOW THE PROJECT'S OWN BAR: 10 -> 16.** K=10 is **3.89σ** against a recorded **4-5σ**; `0062-lb-ring-hash-http` and `0063-lb-maglev` fixed the byte-identical defect (same root cause, same `3·(1/3)^N`, same original N=4) at **16** = 5.27σ, `0062/README.md:57-63` calling it *"past the 5σ-equivalent flake-free margin"*. **`0061` was left behind; this row finishes a phase-36.2 migration.** **CONTROLLER SELF-CORRECTION, recorded:** the SPEC first derived 10 from a policy it invented — right arithmetic, wrong premise ⇒ **grep for a sibling that already derived one.** **DOCKET RESOLVED BY EXECUTION:** CXTOTAL — `upstream_cx_total` **256 on BOTH sides**, reference `upstream_rq_total` **256**, subject **0**, read via a **discriminating break** not a green PASS; gauges 1026/342/342 + `membership_total`=3 + `cx_active`=0 positively observed unmoved. LOOPBACK — **10/10** for `.2`-`.11` (⚠️ `.12`-`.17` UNPROBED — OWED). RUNTIME — **+0.158 s (+4.5%)** vs a SAME-SESSION control; the cross-session baseline reads **-0.002 s**, an artifact ⇒ **quote deltas against same-session controls.** VERIFY — measurement BUILT and RUN (0.62 s), its **vacuity MEASURED** (freezing the ports leaves the measured leg GREEN at `0/2000` while the control leg goes RED), the law measured over **1 000 000** trials with p̂ slightly BELOW `3^(1-K)` ⇒ **a CONSERVATIVE UPPER BOUND**; seed checked against 12 alternatives. SWEEP — `0061` is the **ONLY** underived reachable probabilistic margin in 119 fixtures. ADR — **YES**. ⚠️ **BREAK β IS NOT EXECUTABLE AS SPECIFIED** — `collapseFixtureK` and `sourceIPs` sit in different packages with no mechanical link, so reverting the fixture ALONE leaves the measurement **GREEN** (executed). β is a **TWO-EDIT** break and proves only the unit test's constant load-bearing; a **negative- AND positive-controlled linkage gate** closes the single-edit case. ⚠️ **`BEHAVIOR_CONTRACT.md:1326` IS owed an edit** (four `64`s + one `127.0.0.2..5`, one line, the only such line) — refuting the BRAINSTORM's *"no behavior changes ⇒ no contract edit"* default: **the contract documents the fixture's WORKLOAD too.** `% 16 == 0` must STAY. ⚠️ **An OPPOSITE-DIRECTION false claim in FOUR places** (*"DETERMINISTIC/EXACT — not a σ-band"* — true of affinity, FALSE of spread), structurally invisible to a numeral-keyed sweep. ⚠️ **The Load-balancing family was declared CLOSED at row 54** ⇒ row 76 and ADR-0298 claim **NO family ordinal**; the BRAINSTORM's *"the family is already open"* is a mis-description. **Counts UNCHANGED:** fixtures **119** · fuzzers **55** · stat surface **1205** (DOCUMENTARY) · BackendKind **38** · modules **2** · DECISIONS tail **ADR-0298 PROPOSED** (next-free **ADR-0299**). **This row narrows NOTHING, ever** — no `candidates:` sentence names a load-balancing candidate ⇒ check (2) **STAYS 3 through the IMPL.** **Sentinel re-run MECHANICALLY (does NOT fire; `stop` NOT created):** (1) `NOT DONE: row 76` · (2) => **3** · (3) `NEVER OPENED: gRPC/Runtime/WASM`. ⚠️ **check (1)'s blind spot RE-DERIVED: 108 / 104 / FOUR misses**, all `done`. **Next -> the phase-76 PLAN.**

- **prior active-phase:** `phase 76 (lb-ring-hash-spread-margin) BRAINSTORM done` (2026-07-25) — lifecycle-state **DONE -> 1**; **ROW 76 REGISTERED `in-progress`** at `ROADMAP.md:138`, RE-OPENING sentinel check (1) after its second-ever silent close. Fresh worktree `/home/esa/git/envoy-go-wt-p76bs` off master `c57b98b8`, branch `phase-76-brainstorm`. Docs-only: **ZERO production `.go`, ZERO test `.go`**; exactly THREE files edited (`ROADMAP.md`, `STATE.md`, `next-prompt.txt`) plus the new `BRAINSTORM.md`; **DECISIONS.md BYTE-UNTOUCHED**. **FOUR read-only re-costing agents at tip `c57b98b8` on disjoint remits with PRIVATE scratch** — one LIVE 23-arm reference fleet, one Go-toolchain handshake-probe fleet, two repo re-derivation — plus controller re-derivation of every load-bearing claim. **THE PICK, SELF-PICKED per the 2026-07-12 standing directive (smallest defensible candidate first):** give `0061-lb-ring-hash`'s spread assertion a **DERIVED statistical margin** — `sourceIPs` **4 -> 10** — plus a **seeded collapse-rate MEASUREMENT** in `internal/cluster/ringhash_test.go`, plus the prose sweep the fix falsifies. ~5-7 tasks, **+0 on every envelope axis including +0 PRODUCTION `.go` FILES**. **THE HEADLINE — THE INHERITED RECOMMENDATION'S PRIMARY JUSTIFICATION IS REFUTED BY ONE `grep -c`.** `fault.abort.grpc_status` rode three stages and two documents on the claim that it is *"the ONLY identified candidate that clears a sentinel check-(3) blocker — it opens the **gRPC** family"*. **It does not**: the family charters bridge · gRPC-Web · gRPC-JSON transcoding · interop conformance (`ROADMAP.md:187-189`) and a fault-injection knob delivers NONE of them; a row that cleared check (3) by writing the marker phrase into its own summary would be gaming the one failure mode a fail-SAFE check cannot detect. Its cost ALSO re-derived **10-13, not 7-9**. **THE DIAGNOSIS, DERIVED THREE INDEPENDENT WAYS THAT ALL AGREE:** the governing variable is **K = distinct hash keys = `sourceIPs` = 4**, NOT the 64 connections — `HashSourceIP` (`internal/cluster/hash.go:133`) **STRIPS THE PORT**, so 16 connections from one source IP are **ONE** key; and the ring is keyed on the endpoint address **INCLUDING its OS-ephemeral port** (`ringhash.go:88-91` + `runner_test.go:272`), so it is a **fresh random 3-way partition on EVERY run**. ⇒ **P = 3^(1−K) = 3.7% at K=4** — analytic **0.0370**, controller Monte Carlo (200 000 trials/K) **0.0365**, an agent's independent replica (60 000 trials/K, real `xxHash64`, sanity-checked against the fixture's own 1026/342 ring gauge) **0.0349**. At **K=10 -> 5.1e-5 (~730×)**. **`0062` needs NOTHING** — identical assertion text, but `hashValues = 16` ⇒ P ≈ 7e-8, **~500 000× safer**; the ENTIRE delta between a fixture that flaked three times and one that never has is **K**. ⚠️ **THE FLAKE'S OWN CERTIFICATION IS STATISTICALLY INVALID:** `0061/README.md:143-148` certifies *"overwhelmingly stable"* on **`-count=20` -> 20/20 PASS** and calls the ring *"fixed"* — both false, and `(1−0.0365)^20 ≈ 0.48`, so the check was **MORE LIKELY THAN NOT to pass even if the assertion were exactly as broken as it in fact is.** ⇒ **A PASS-COUNT IS NOT A MARGIN**, hence the deliverable is a MEASUREMENT. ⚠️ **THIS ROW COMMITTED THE EXACT FAILURE MODE IT DOCUMENTS, TWICE, IN ONE COMMIT.** The first draft quoted check (3)'s marker phrase inside the sentence explaining why quoting it would be gaming — and check (3) came back with **gRPC silently GONE**; the second occurrence quoted **the command and its result**, a claim its own landing falsifies (the ADR-0296 ¶3 / ADR-0297 ¶7 / ¶9 species, **escalated from a wrong NUMBER to a flipped TERMINATION SENTINEL**). Both removed; the token is now **deliberately NOT spelled anywhere in `ROADMAP.md`**. ⇒ **never write a sentinel's own matcher string into a file the sentinel greps** (`grep` cannot tell mention from use), and **RE-RUN every sentinel check AFTER the stage's own edits land** — the pre-edit run was clean and meaningless. **RIVALS RE-COSTED AT THIS TIP, none carried:** `Listener.stat_prefix` ~7-10 (blast radius a RED HERRING; reference behavior already landed at `BEHAVIOR_CONTRACT.md:1859`) · `upstream_cluster` ~7-9 but ~85-100 lines / ~18 files, its central premise **UNVERIFIED**, the inherited break CONFIRMED non-existent (`grep -c 'GetClusterName()' internal/cluster/manager.go` -> **0**), and five fixtures mis-attributing the gap to a **Lua bridge** that 0087 does not configure · `ssl.connection_error` **~10-13, NOT cheaper** — its recorded predicate is **INCOMPLETE** (an RST peer yields `*net.OpError`, for which BOTH `errors.Is(io.EOF)` and `errors.Is(io.ErrUnexpectedEOF)` are FALSE), needing `syscall.ECONNRESET` ⇒ **+1 PRODUCTION import**, and its roster is **INERT** for this leaf · **Runtime family ~10-14 — the only evaluated candidate that GENUINELY clears a check-(3) blocker**, with SIX landed ADR-anchored `runtime_key` PARSE-REJECT arms forward-pointing at it (ADR-0187 `DECISIONS.md:11280`, ADR-0195 `:12413`). **METHODOLOGICAL FOLD:** the session OPENED treating `fault.abort.grpc_status` as presumptive because three documents agreed — **agreement across documents is what a CARRIED claim looks like, not a VERIFIED one**; all three cites trace to ONE unexecuted assertion. **Sentinel re-run MECHANICALLY TWICE (worktree AND landed master post-push; does NOT fire; `stop` NOT created):** (1) `NOT DONE: row 76` · (2) => **3** · (3) `NEVER OPENED: gRPC/Runtime/WASM`. ⚠️ **check (1)'s blind spot RE-DERIVED: 108 data rows (`:31`-`:138`) / 104 matched / FOUR misses**, all four `done`. **Next -> the phase-76 SPEC** (settle the D-RHSM-* docket; ⚠️ **EXECUTE** D-RHSM-CXTOTAL rather than reasoning about it).




## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
