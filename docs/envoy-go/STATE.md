# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

> **This file is a POINTER, not an archive** — `BOOTSTRAP_PROMPT.md` §4: *"STATE.md — pointer: active phase, last commit, next action."* It carries the live pointer, the live counts, and a SHORT recent lineage. **Full stage-by-stage history lives in `STATE_HISTORY.md`** (archived at ADR-0288; **nothing was deleted** — the pre-move file is preserved there verbatim, byte-for-byte).
>
> **⚠️ THE RULE ADR-0288 EXISTS TO ENFORCE: there is EXACTLY ONE live `next-skill:`, `lifecycle-state:`, `next-free ADR:`, and count block IN THIS FILE, and they live in §Current pointer directly below.** Before that move, ~330 lines of prepended backlog had buried TWO fossilised "current state" blocks whose `next-skill:` still read *"the 42.2 hedging BRAINSTORM"* and *"the 46.2 SPEC"* while the live phase was **66** — so `grep 'next-skill:' STATE.md` returned **two stale hits and ZERO live ones**, and `grep 'fuzzers:'` returned **48** and **42** when the true count was **55**. **A session may trust a grep of this file again. Keep it that way: when you advance the pointer, EDIT §Current pointer IN PLACE — never prepend a new block above it.**

---

## Current pointer

*(The ONLY live "what next" in this repo. Update IN PLACE at every stage close.)*

- **active-phase:** `phase 75 (tls-no-certificate-stat) PLAN done` (2026-07-25) — **ROW 75 STAYS `in-progress`**; **ROADMAP and DECISIONS are BYTE-UNTOUCHED** at a PLAN, so **sentinel check (1) still prints `NOT DONE: row 75`** and goes silent only at the phase-75 IMPL. ⚠️ **THIS PLAN EXECUTED MOST OF THE ROW.** SPEC §16 closed with *"NO BUILD OF ROW 75 EXISTS"*; that is no longer true — three private build worktrees compiled the row, ran the unit suite AND the FULL differential green, and executed **EIGHT of the TEN breaks**.
- **phase-directory:** `docs/envoy-go/phases/75-tls-no-certificate-stat/` (`BRAINSTORM.md` + `SPEC.md` + `PLAN.md` + `PROGRESS.md`; the last two are this stage's phase-directory delta in full)
- **lifecycle-state:** `3` (PLAN done; IMPL next). **Subject:** `tls-no-certificate-stat` — ONE fixed name, **`listener.<normalized-addr>.ssl.no_certificate`**, a **SUCCESS-PATH annotation** counting *"this COMPLETED handshake presented no client certificate"* (⚠️ **NOT** a synonym for `ssl.fail_verify_no_cert` — wrong in BOTH directions), registered inside the block `registerListenerMetrics` ALREADY gates on `rt.tlsMode` and Inc'd on the success fall-through phase 74 left beside `rt.sslHandshake.Inc()`. **The predicate is PINNED: `len(tlsConn.ConnectionState().PeerCertificates) == 0` ALONE, NO client-auth term** — and it is now **PROVEN load-bearing** by Break F, which adds the refuted mode term and fires the positive arm. Envelope: **+1 stat (1204 -> 1205)** · **+0 fixtures (119** — `0110-tls-require-client-cert-false` EXTENDED with a cross-side `fixture.StatsAsserter`**)** · +0 fuzzers (**55**) · +0 BackendKinds (**38**) · +0 modules (**2**) · **+0 production imports** (CONFIRMED BY BUILD in three worktrees) · ZERO new packages · ZERO new exported symbols; ONE ADR (**ADR-0297**, §Context landed at the SPEC).
- **next-skill:** **the phase-75 IMPL** — execute `PLAN.md`'s **11-task TDD spine (T1-T11)** and its **TEN-break roster, of which EIGHT already RAN at the PLAN**. ⚠️⚠️ **READ `PLAN.md` §1.3 FIRST — IT REFUTES BOTH THE SPEC AND THE ROUTER ON THIS ROW'S CENTRAL CLAIM.** SPEC §10.1 and router item 5(a) both state the new positive unit arm *IS* the discriminating break for the pinned predicate. **It is not.** Break D (deleting the `len(...)==0` guard) fires **`TestServeConnection_SSLHandshakeIncrements`** — a **PHASE-74** test — through `assertSSLCrossProduct`'s NEGATIVE half, while **the positive arm PASSES**; and **Break D′** (the same edit with `sslLeafRoster` left at three leaves) leaves the **ENTIRE package GREEN (`ok … 3.233s`)**. ⇒ **the roster extension is the SOLE guard on the row's central pinned decision, not accommodation for the new arm** — and the positive arm and the roster extension guard DIFFERENT defects, neither substituting for the other. Controller-reproduced end-to-end. **THE HEADLINE RESULT: the FIRST subject-side measurement exists and the two sides AGREE EXACTLY** — `0110` scrapes `ssl.handshake=2 ssl.no_certificate=1 ssl.fail_verify_error=1 ssl.fail_verify_no_cert=0 (downstream_cx_total=3)` on **BOTH** sides, with the discriminating asymmetry (`handshake=2` vs `no_certificate=1`) observed per side, and the **FULL differential suite `ok … 399.675s`, exit 0, no flake**. ⚠️ **T1 must order the GUARD FIRST** — two `reflect.DeepEqual` exact-set pins (`manager_test.go`, `quic_test.go`) go red at the production change alone, so landing registration first ends the task on a red tree. ⚠️ **A stale `-run` selector on a UNIT test prints `ok … [no tests to run]` and EXITS 0** — re-derive every selector after T1's rename. ⚠️ **The "delete the registration" break must be `-run` ISOLATED** or its SIGSEGV names no test and destroys its own evidence. ⚠️ **The fast-failure `ssl.*` suppression is REFERENCE-ONLY** — envoy-go accounts `ssl.*` at step (6), strictly before the step-(7) upstream dial. ⚠️ **The ABSENT check does NOT catch a deleted registration on a counter that IS Inc'd** (the crash kills the run first); it guards a registered-but-unscraped name and the `want: 0` row. ⚠️ **ADR-0297's OWN ¶7 and ¶9 are DEFECTIVE and T11 corrects them in place** — ¶7's grep claim is **self-falsifying** (the same species the phase-74 IMPL had just fixed in ADR-0296 ¶3), and ¶9's *"SELF vs OTHER-ADR (n=4)"* form rule is refuted by an n=7 population in which `:17211` is a phase-73 correction to a DIFFERENT landed ADR rendered INLINE; the surviving discriminator is **graft SCALE**, and the indented prescription has now outlived THREE wrong justifications. ⚠️ **SPEC §9's *"no two-ADR heading precedent exists"* is REFUTED — there are 16**, and `:785` is the exact later-phase-extends shape, so the `:1849` heading IS extended. ⚠️ **BEHAVIOR_CONTRACT edits APPEND IN PLACE** — `:1849`-`:1859` are cited by line from four documents; the ledger line after `:5002` must be the ONLY line-adding edit. ⚠️ **`0111`'s value rosters stay at THREE** (its `require=true` shape would make a value pin a vacuous `0 == 0`) **but its PROSE must change** — both passages are CLOSED ENUMERATIONS, so a name in neither list reads as asserted; **the controller REVERSED itself on this and recorded the reversal.** ⚠️ **Do NOT touch `handshakeOutcome`**; **do NOT add a nil guard at the Inc site** (it would make Break B vacuous). Row 75 flips `done` at the T10/T11 six-gate; the deferred-sentence narrow lands at the IMPL only and must keep **ZERO interior periods**. Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-pushes (`feedback_push_to_origin`); **use `git -C <abs-worktree-path>` everywhere and tripwire `pwd` + branch + commit count before any commit or gate run** (`reference_bash_cwd_reset_commits_to_main` — **it fired live in BOTH the phase-75 BRAINSTORM and this PLAN's session**).
- **last-commit:** the phase-75 PLAN squash (`git log --grep 'phase 75'`; **locate by SUBJECT, never by position** — `reference_next_prompt_tracked_despite_gitignore`). Prior: the phase-75 router commit `cedd2f27` (this stage's base), the phase-75 SPEC squash `2b5eff0a`, the phase-75 BRAINSTORM squash `e822f1ad`, the phase-74 IMPL squash `f8f6cd44`.
- **last-updated:** 2026-07-25 (phase-75 PLAN done. Lifecycle-state **2 -> 3**; **ROW 75 STAYS `in-progress`**; **ROADMAP and DECISIONS BYTE-UNTOUCHED**. Fresh worktree `/home/esa/git/envoy-go-wt-p75plan` off master `cedd2f27`, branch `phase-75-plan`. Docs-only: **ZERO production `.go`**, **EXACTLY TWO files in the phase-directory delta** — the new `PLAN.md` and `PROGRESS.md`. **SEVEN agents at tip `cedd2f27` on disjoint remits — FOUR read-only re-derivation agents and THREE BUILD-BY-EXECUTION agents in SEPARATE git worktrees** (`phase-75-plan-v1/v2/v3`, private scratch, local commits only, never pushed), plus controller re-verification of every load-bearing correction. **THE HEADLINE — THE SPEC AND THE ROUTER ARE BOTH WRONG ABOUT WHERE THIS ROW'S DISCRIMINATING BREAK LIVES, AND IT TOOK BUILDING THE ROW TO SEE IT.** Both state the new positive unit arm *IS* the discriminating break for the pinned `len(PeerCertificates)==0` predicate. **Break D fires the PHASE-74 mTLS arm instead** (`ssl.no_certificate = 1, want 0 — only [handshake] may move on this arm`), through `assertSSLCrossProduct`'s NEGATIVE half, **while the positive arm PASSES** — an unconditional Inc still leaves the counter at 1 on the arm that wants 1, so the positive half is structurally blind to it. **And Break D′, the decisive stacked control** (same edit, `sslLeafRoster` reverted to three leaves) leaves the **ENTIRE package GREEN, `ok … 3.233s`** ⇒ **the one-line roster extension is the SOLE guard on the row's central pinned decision.** Reproduced by the controller end-to-end. ⇒ the positive arm catches *never-Inc'd* (Break E) and *mode-gated* (Break F); the roster extension catches *unconditional* (Break D); **neither substitutes for the other and the row needs BOTH.** **THE SECOND HEADLINE — THE FIRST SUBJECT-SIDE MEASUREMENT EXISTS, AND THE SIDES AGREE EXACTLY:** `0110` gained a cross-side `StatsAsserter` and was RUN against the live reference — `ssl.handshake=2 ssl.no_certificate=1 ssl.fail_verify_error=1 ssl.fail_verify_no_cert=0 (downstream_cx_total=3)` on **BOTH** sides, `envoy_listener_ssl_no_certificate{envoy_listener_address="0.0.0.0_10446"} 1` vs `…{envoy_listener_address="___20016"} 1` (metric NAMES byte-identical, addresses differing ONLY in the label), the discriminating asymmetry observed per side, and the **FULL differential suite `ok 399.675s`, exit 0, no flake**. ⚠️ **The subject binds the IPv6 WILDCARD** — `___<port>`, not `0_0_0_0_<port>`; the agent wrote it wrongly and caught itself by execution. **FOURTEEN SPEC/router claims REFUTED. Beyond the headline:** the fast-failure `ssl.*` suppression is **REFERENCE-ONLY** (envoy-go accounts `ssl.*` at step 6, before the step-7 upstream dial; subject numbers BYTE-IDENTICAL under a refused upstream) · the **ABSENT check does not guard what SPEC §8.2 says** (deleting the registration of an Inc'd counter SIGSEGVs the subject subprocess and the run dies at `structuralCheck` before `AssertStats`) · **the tree goes RED at the production change** via two `reflect.DeepEqual` exact-set pins, so T1 orders the guard FIRST · **a stale `-run` on a UNIT test prints `ok … [no tests to run]` and EXITS 0**, a NEW SHAPE of `reference_differential_run_selector` · the "delete the registration" break **destroys its own evidence** full-package · `registerListenerMetrics`' doc comment starts at **`:351`**, not `:358` · the three `assertSSLCrossProduct` "callers" the SPEC cites are **test-func DECLARATION lines** (real sites `:4508/:4539/:4557`) · the pointer assertions use **`t.Error`**, not `t.Errorf` · `pollCounter`/`counterValue` live in **`quic_test.go`** · `mkDownstreamTSInline` takes **`string`** while the PKI holds `[]byte`, so **the SPEC's call does not compile** · the rename touches **THREE** sites incl. a cross-reference in a test the router said needs nothing · **SPEC §9's *"no two-ADR heading precedent exists"* is REFUTED — there are 16**, `:785` being the exact later-phase-extends shape · **check (1)'s blind-spot figure is 107/103/4, not 106/102/4**, and its *provenance* claim is false (106/102 is the PRE-BRAINSTORM number, invalidated by row 75's own addition in the very commit that claimed to re-derive it) · *"three-fifths"* was **already wrong at the phase-74 tip, self-inconsistently within phase 74's own edits** (`:928`, same phase, enumerates FOUR surviving families ⇒ 3+4=7) so it is REMOVED, not bumped · the stat-surface ledger is discontinuous in **TWO** places (`1198 -> 1200` is unrecorded, additional to `1200 -> 1201`) · `registry.go` has **FIVE** panic sites, not three — **a correction of this PLAN's own agent** · and **ADR-0297's OWN ¶7 and ¶9 are DEFECTIVE**: ¶7's grep claim is **SELF-FALSIFYING** (`VERIFYIFGIVEN` now returns 2 lines / 3 occurrences, one of them ¶7 itself — **the same species the phase-74 IMPL had just fixed in ADR-0296 ¶3, reproduced one ADR later**) and ¶9's *"SELF vs OTHER-ADR (n=4)"* rule is refuted by an **n=7** population with a THIRD form in which **`:17211` is a phase-73 correction to a DIFFERENT landed ADR rendered INLINE**; the surviving discriminator is **graft SCALE**, and the indented prescription has now outlived **THREE** wrong justifications (family, self-vs-other, scale). **A CONTROLLER DECISION THAT REVERSED ITSELF, RECORDED NOT QUIETLY AMENDED:** the `0111` question was first decided ADD-to-the-rosters, then reversed to prose-only once two later findings landed — the ABSENT check does not catch a deleted registration on an Inc'd counter, and **only `0110` of the NINE TLS-downstream fixtures is `require=false`**, so a `0111` value pin would document a vacuous `0 == 0` while `0110` carries a discriminating non-zero. **`0111`'s prose still MUST change** — both passages are CLOSED ENUMERATIONS. **Counts UNCHANGED (docs-only; re-run MECHANICALLY in the worktree at this close):** fixtures **119** / fuzzers **55** / stat surface **1204** / BackendKind **38** (39 constants, 0-38) / modules **2** (go.mod requires **67**) / DECISIONS tail **ADR-0297** PROPOSED (next-free **ADR-0298**; shape Context 1 / Decision 0 / Consequences 0 / footer 1). **Sentinel re-run MECHANICALLY TWICE (worktree AND landed master post-push; does NOT fire; `stop` NOT created):** (1) `NOT DONE: row 75` · (2) => **3** · (3) `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`. `next-prompt.txt` rolled to the phase-75 IMPL (**TRACKED despite `.gitignore`**; edited in this worktree).)
- **next-free ADR:** **`ADR-0298`** — DECISIONS.md tail is **ADR-0297** (STATUS **PROPOSED — §Context drafted at the phase-75 SPEC** per ADR-0044-**as-used**; §Decision + §Consequences append IN PLACE at the phase-75 IMPL, **no renumber, no new ADR**). `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1` -> `## ADR-0297`; `grep -c '^## ADR-0298'` -> **0**. Block shape re-verified at this close: **1** `### Context`, **0** `### Decision`, **0** `### Consequences`, **1** `*(§Decision + §Consequences land at the phase-75 IMPL.)*` footer (at `:17350`); heading at `:17322`, §Context carrying **ELEVEN** paragraphs ¶1-¶11 — **more than SPEC §14's eight-item list implies, so the IMPL must not duplicate them.** ⚠️ **ADR-0296 is BYTE-UNTOUCHED at this commit** — the §Decision (g) correction blockquote lands at the phase-75 **IMPL** as an indented `  > [CORRECTED at phase 75/ADR-0297: ...]` (leading bytes `0x20 0x20 0x3E 0x20`, `od -c`-verified byte-identical across `:16901` and `:16910`, the ONLY two instances), and **inserting it shifts `:17310 -> :17312` and `:17314 -> :17316`, the latter cited from `ROADMAP.md:137` and `STATE.md:48`** — both fixed in the same task. ⚠️ **ADR-0297's own ¶7 and ¶9 are corrected IN PLACE at the IMPL** (see next-skill).

---

## Project counts at this commit

*(Live. Re-verify at session start — `git fetch` first — and update here at every stage close. Canonical commands inline.)*

⚠️ **ALL counts below were re-run MECHANICALLY in the stage worktree at this phase-75 SPEC close with the canonical commands — never copied forward.** This is a **docs-only** stage: **ZERO production `.go`**, so **every count is UNCHANGED** from the phase-74 IMPL close. The phase-75 deltas below remain **ANTICIPATED, not landed**: +1 stat (1204 -> 1205), +0 fixtures (`0110` EXTENDED), +0 fuzzers, +0 BackendKinds, +0 modules, +0 production imports, ZERO new packages, ZERO new exported symbols. ⚠️ **TEST-side imports WILL grow and that is PERMITTED** — `0110`'s asserter needs `log`, `math`, `net/http`, `strconv`; the +0 claim is a **PRODUCTION** claim and the two categories must be audited SEPARATELY. ⚠️ **The stat-surface total 1204 is DOC-SOURCED and was NOT re-derived mechanically at this SPEC** — there is no mechanical command, and ADR-0296's own ledger entry records an unattributed `+1` gap and asks a future phase to COUNT rather than trust the chain. The **DELTA (+1)** is asserted with confidence; the **TOTAL** is not.

- **active differential fixtures:** **119** — `ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0117-tracing-custom-tags-metadata-cluster` (phase 73). **Phase 74 landed +0**: `0111-tls-cvc-empty-dynamic-fallback` was **EXTENDED** with a cross-side `fixture.StatsAsserter` (NO new YAML, NO new directory, NO new BackendKind, NO new port), so the count STAYS **119** and `0118` / reference port `10118` remain FREE. ⚠️ **The fixture now asserts what its own README confessed was infeasible, and the REFERENCE side was EXECUTED**: `handshake=1 fail_verify_error=1 fail_verify_no_cert=1`, `downstream_cx_total=3`, matching the subject EXACTLY — the sanctioned subject-only fallback was NOT needed and was never taken. Shape A over `/stats/prometheus`, keyed on the metric NAME with `envoy_listener_address` IGNORED (both sides hoist the address into a LABEL, so the NAME is cross-side identical even though the reference keeps dots and envoy-go underscores them). ⚠️ **`var _ fixture.StatsAsserter = (*edfDriver)(nil)` is a TRIPWIRE, not the dispatch mechanism** — PROVEN by Break G' run stacked on a FAILING assertion: with the method renamed the fixture stayed GREEN and the driver's log lines vanished entirely (`AssertStats` silently never ran), while with the name restored and the `var _` still commented out it went RED.
- **fuzzers:** **55** — `grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`. **Phase 74 landed +0** — it consumes no new config field at all, so there is no parse arm and no fuzz-seed classification to flip (`reference_fuzzer_count_docs_drift`: an `f.Add` seed is not a fuzzer).
- **stat surface:** **1204** — BEHAVIOR_CONTRACT doc count. **There is NO mechanical counting command** (the contract says so itself); enforcement is per-phase `TestNoNewStat*` guards asserting a delta of ZERO, never the absolute total. **Phase 74 LANDED +3 (1201 -> 1204)** — `ssl.handshake` / `ssl.fail_verify_error` / `ssl.fail_verify_no_cert` at listener scope, all three FIXED and hyphen-free, the FIRST non-zero stat delta in this lineage. ⚠️ **The delta is a NAME-surface delta, NOT a flat per-deployment one**: registration is TLS-CHAINS-ONLY (gated on `rt.tlsMode`), so a **plaintext-only** deployment gains **ZERO** names — pinned by `TestListenerMetrics_PlaintextListenerRegistersNoSSLNames` — while a **QUIC** listener DOES gain all three and they stay permanently zero, which is **PARITY** with the reference (probed at the SPEC, pinned at T3), not an anomaly. The registration guard asserts the **exact NAME SET**, not a cardinality — Break E' demonstrated the counterfactual live: a misspelled name still yields **length 3**, so the landed `statssink` `countMetrics` precedent would have stayed GREEN. Ledger updated: `1201` -> `1204` at `BEHAVIOR_CONTRACT.md:831`/`:847`, the THREE **narrative** stale `1200`s corrected, the other TEN `\b1200\b` hits **deliberately left alone as historically correct**, and a `**Phase 74 — 1201 -> 1204 (+3)**` line added after the ledger tail. ⚠️ **The missing `1200 -> 1201` step is RECORDED as an unattributed gap, NOT invented** — fabricating a `Phase 56.1` line would have been inventing a record. Gate figures at this close: `grep -c '1204'` => **3** · `grep -c '1201'` => **1** (the NEW ledger line ONLY) · `awk 'NR<4980' | grep -c '1201'` => **0** · `grep -cE '\b1200\b'` => **10** (was 13).
- **BackendKind tail:** **38** (`H2GoawayResponder`, `test/differential/fixture/fixture.go:614`). **Phase 74 landed +0** — `0111` was extended, not cloned, and its driver is a self-contained PKI generator with `BackendCount() == 1`. ⚠️ **This is a TAIL VALUE, not a count** — the file declares **39** constants (values 0–38, `TCPEcho = 0`). The lineage figure **38** is correct as written; do NOT 'fix' it to 39.
- **DECISIONS.md tail:** **ADR-0296** (next-free **ADR-0297**) — `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1`. **ADR-0296 is COMPLETE** — §Context drafted at the phase-74 SPEC (ADR-0044-**as-used**; ⚠️ ADR-0044 does NOT actually contain that discipline), §Decision + §Consequences appended IN PLACE at the phase-74 IMPL after the **RETAINED** footer, mirroring **ADR-0295**. Block shape verified at this close: **1** `### Context`, **1** `### Decision`, **1** `### Consequences`; `grep -c '^## ADR-0297'` => **0**. **No renumber, no new ADR.** Its TWO internal defects were fixed while completing it: the STATUS line's `see §Context ¶6` mis-pointer (the ADR-0044 finding is at **¶4(i)**; ¶6 is the classifier paragraph) and **¶3's SELF-REFUTING grep** — a count of 0 that the SPEC's own §Context append had made 1, the sole hit being that very paragraph. ⚠️ **`ADR-0286` is NO LONGER byte-untouched**: it now carries the phase-74 C3 correction as an INDENTED `> [CORRECTED at phase 74/ADR-0296: ...]` blockquote after `:16908`, its two-space indentation `cat -A`-verified byte-identical to the `:16901` precedent. **The discriminator for that FORM is the ADR FAMILY, not the phase gap** — `:17209` is a later-phase-corrects-earlier-ADR case that uses the INLINE form because it stays within one family.
- **go.mod modules:** the tracked lineage figure is **2** (the phase-61.2 count: `quic-go` direct + `qpack` indirect) — **NOT a repo total**; the single `go.mod` requires **67** (18 direct + 49 indirect). **Phase 74 landed +0** — `go mod tidy -diff` EMPTY and `git diff master -- go.mod go.sum` EMPTY. The classifier needed `errors` and `crypto/tls`, both ALREADY imported by `internal/listener/manager.go`; **an import LINE is not a go.mod MODULE**, and there is no new sub-package, so `reference_new_subpackage_pulls_transitive_module` does not bite.
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` (see `ENVOY_TARGET.md`).
- **conformance:** h2spec 53/53; proxy-wasm all 10 families PASS.

---

## Recent lineage

*(The last FIVE stages only — enough for the router's "prior-phase lineage" read. Everything earlier is in `STATE_HISTORY.md`. These bullets are carried over VERBATIM; the newest mirrors §Current (**the phase-75 PLAN done** — the stage that REFUTED both the SPEC and the router on where this row's discriminating break lives, produced the FIRST subject-side measurement of the counter with the two sides agreeing EXACTLY, and found ADR-0297's OWN ¶7 and ¶9 defective — ¶7 reproducing, one ADR later, the very self-refuting-grep species the phase-74 IMPL had just fixed in ADR-0296 ¶3), and the oldest (**phase-74 SPEC done**) was dropped as this list re-capped at five. The five now read: phase-75 PLAN · phase-75 SPEC · phase-75 BRAINSTORM · phase-74 IMPL · phase-74 PLAN.)*

- **prior active-phase:** `phase 75 (tls-no-certificate-stat) PLAN done` (2026-07-25) — phase-75 PLAN done. Lifecycle-state **2 -> 3**; **ROW 75 STAYS `in-progress`**; **ROADMAP and DECISIONS BYTE-UNTOUCHED**. Fresh worktree `/home/esa/git/envoy-go-wt-p75plan` off master `cedd2f27`, branch `phase-75-plan`. Docs-only: **ZERO production `.go`**, **EXACTLY TWO files in the phase-directory delta** — the new `PLAN.md` (an **11-task TDD spine T1-T11** with a **TEN-break roster**) and `PROGRESS.md`. **SEVEN agents at tip `cedd2f27` on disjoint remits — FOUR read-only re-derivation agents and THREE BUILD-BY-EXECUTION agents in SEPARATE git worktrees** (`phase-75-plan-v1/v2/v3`, private scratch, local commits only, never pushed), plus controller re-verification of every load-bearing correction. ⚠️ **THIS PLAN EXECUTED MOST OF THE ROW: SPEC §16's closing *"NO BUILD OF ROW 75 EXISTS"* is no longer true**, and **EIGHT of the TEN breaks RAN here.** **THE HEADLINE — THE SPEC AND THE ROUTER ARE BOTH WRONG ABOUT WHERE THIS ROW'S DISCRIMINATING BREAK LIVES.** Both state the new positive unit arm *IS* the discriminating break for the pinned `len(PeerCertificates)==0` predicate. **Break D fires the PHASE-74 mTLS arm instead** — `ssl.no_certificate = 1, want 0 — only [handshake] may move on this arm`, via `assertSSLCrossProduct`'s NEGATIVE half — **while the positive arm PASSES**, because an unconditional Inc still leaves the counter at 1 on the arm that wants 1. **Break D′, the decisive stacked control** (same edit, `sslLeafRoster` reverted to three leaves) leaves the **ENTIRE package GREEN, `ok … 3.233s`** ⇒ **the one-line roster extension is the SOLE guard on the row's central pinned decision.** Controller-reproduced end-to-end. ⇒ positive arm catches *never-Inc'd* and *mode-gated*; roster extension catches *unconditional*; **the row needs BOTH.** **THE SECOND HEADLINE — THE FIRST SUBJECT-SIDE MEASUREMENT, AND EXACT CROSS-SIDE AGREEMENT:** `0110` gained a cross-side `StatsAsserter` and RAN against the live reference — `ssl.handshake=2 ssl.no_certificate=1 ssl.fail_verify_error=1 ssl.fail_verify_no_cert=0 (downstream_cx_total=3)` on **BOTH** sides, metric NAMES byte-identical with the address differing ONLY in the label (`0.0.0.0_10446` vs `___20016` — ⚠️ the subject binds the **IPv6 wildcard**, caught by execution), and the **FULL differential suite `ok 399.675s`, exit 0, no flake**. **FOURTEEN SPEC/router claims REFUTED**, incl.: the fast-failure `ssl.*` suppression is **REFERENCE-ONLY** (envoy-go accounts `ssl.*` at step 6, before the step-7 upstream dial) · the **ABSENT check does not guard what SPEC §8.2 says** (deleting an Inc'd counter's registration SIGSEGVs the subject and the run dies at `structuralCheck` before `AssertStats`) · **the tree goes RED at the production change** via two `reflect.DeepEqual` exact-set pins, so T1 orders the guard FIRST · **a stale `-run` on a UNIT test prints `ok … [no tests to run]` and EXITS 0** (a NEW SHAPE of `reference_differential_run_selector`) · the registration-delete break **destroys its own evidence** full-package · `registerListenerMetrics`' doc starts at **`:351`** not `:358` · the SPEC's three `assertSSLCrossProduct` "callers" are **test-func DECLARATION lines** · `t.Error` not `t.Errorf` · `mkDownstreamTSInline` takes **`string`** so the SPEC's call **does not compile** · **SPEC §9's *"no two-ADR heading precedent exists"* is REFUTED — there are 16** · **check (1)'s blind spot is 107/103/4, not 106/102/4**, and its provenance claim is false · *"three-fifths"* was **already wrong at the phase-74 tip** so it is REMOVED not bumped · the ledger is discontinuous in **TWO** places · `registry.go` has **FIVE** panic sites, not three (**a correction of this PLAN's own agent**) · and **ADR-0297's OWN ¶7 and ¶9 are DEFECTIVE** — ¶7's grep claim is **SELF-FALSIFYING**, **reproducing one ADR later the exact species the phase-74 IMPL had just fixed in ADR-0296 ¶3**, and ¶9's *"SELF vs OTHER-ADR (n=4)"* rule is refuted by an **n=7** population where `:17211` is a phase-73 correction to a DIFFERENT landed ADR rendered INLINE; the surviving discriminator is **graft SCALE** and the indented prescription has now outlived **THREE** wrong justifications. **A CONTROLLER DECISION THAT REVERSED ITSELF, RECORDED NOT QUIETLY AMENDED:** the `0111` question was decided ADD-to-rosters, then reversed to prose-only once two later findings landed — **only `0110` of the NINE TLS-downstream fixtures is `require=false`**, so a `0111` value pin would document a vacuous `0 == 0`. **Counts UNCHANGED:** fixtures **119** / fuzzers **55** / stat surface **1204** / BackendKind **38** / modules **2** / DECISIONS tail **ADR-0297** PROPOSED. **Sentinel re-run MECHANICALLY TWICE (worktree AND landed master post-push; does NOT fire; `stop` NOT created):** (1) `NOT DONE: row 75` · (2) => **3** · (3) `NEVER OPENED: gRPC/Runtime/WASM`. `next-prompt.txt` rolled to the phase-75 IMPL (**TRACKED despite `.gitignore`**).

- **prior active-phase:** `phase 75 (tls-no-certificate-stat) SPEC done` (2026-07-25) — phase-75 SPEC done. Lifecycle-state **1 -> 2**; **ROW 75 STAYS `in-progress`** and the ROADMAP is **BYTE-UNTOUCHED**. Fresh worktree `.worktrees/phase-75-spec` off master `e822f1ad`, branch `phase-75-spec`. Docs-only: **ZERO production `.go`**, **EXACTLY TWO files in the delta** — the new `SPEC.md` and the `DECISIONS.md` ADR-0297 §Context append (a PURE APPEND, **30 insertions / 0 deletions**, verified by `git diff --numstat`; `ADR-0286` and `ADR-0296` BYTE-UNTOUCHED). **FIVE agents at tip `e822f1ad` on disjoint remits — TWO live-reference probe fleets** (disjoint port ranges 10600-10639 / 10640-10679, container prefixes `p75s-`/`p75f-`, PRIVATE scratch per `reference_parallel_subagents_private_scratch`) **and THREE read-only re-derivation agents** pinned to the stage worktree. **THE HEADLINE — THE ONE DOCKET ITEM THE BRAINSTORM CALLED *"GENUINELY OPEN"* IS RESOLVED AT THE WIRE.** D-TLSNC-SEMANTICS is **(a) UNCONDITIONAL**: a `DownstreamTlsContext` carrying ONLY `tls_certificates` — no `validation_context`, no `require_client_certificate` — driven by a no-cert client yields `ssl.handshake: 1` **AND `ssl.no_certificate: 1`**, reproduced in TWO fresh containers, and an `openssl s_client -msg` trace shows the server's ENTIRE flight with **ZERO** `CertificateRequest`: **the client was never asked, and the counter fired anyway.** Confirmed on an HCM `direct_response` stack with `clusters: []` (so `tcp_proxy` is not implicated), at TLS 1.2 AND 1.3, and — **without coordination** — by the SECOND fleet, whose control listener used the identical shape (`grep -c 'validation_context'` over both its bootstraps ⇒ **0**, controller-verified). ⇒ **the Inc gate is `len(tlsConn.ConnectionState().PeerCertificates) == 0` ALONE.** **THE REST OF THE DOCKET, ALL BY EXECUTION:** FAILPATH — `no_certificate` read **0** across SIX failure arms, and the sharpest drove a SUCCESS first on the SAME scope (moving `ssl.handshake` 0->1, proving the counters are wired there) then the failure, so *"an all-zero block proves nothing"* was addressed THREE independent ways; it also sharpens the semantics to *"**COMPLETED** handshake AND no peer cert"*. CERTPRESENT — **0**, corroborated by `ssl.sigalgs.*`, which appears ONLY when a cert was presented. SCOPE — **ABSENT**, not present-and-zero, on a plaintext scope, verified **PER NAME** by a two-listener single-process discriminator, and meaningful only because the reference registers these FIXED names **EAGERLY at listener-config time** (the DYNAMIC families behave oppositely). FIXTURE — Shape A SOUND on `0110` (five independent lines of evidence) and the predicted numbers **CONFIRMED reference-side by replaying `0110`'s exact three-arm structure**: `handshake=2` / **`no_certificate=1`** / `fail_verify_error=1` / `fail_verify_no_cert=0` / `downstream_cx_total=3`. ⚠️ **Arms 1 and 3 DISCRIMINATE — `no_certificate` is 1, NOT 2.** **THE BRAINSTORM'S DEAD-UPSTREAM HAZARD IS CHARACTERISED, AND IT IS NOT WHAT IT LOOKED LIKE: it is NOT deadness, it is FAST FAILURE.** `tcp_proxy` dials the upstream at ACCEPT time, before any TLS byte (a bare zero-byte connection still moves `upstream_cx_total`), so the connect and the handshake run CONCURRENTLY; an instantly-refused connect tears the downstream connection down MID-handshake and the whole family stays silent, while a **BLACKHOLE** upstream (`192.0.2.1:81`, `connect_timeout: 10s`) — equally dead but SLOW — loses the race and fires everything normally. ⚠️ **THE CONTROLLER CAUGHT A PROBE FLEET FAILING THE ROW'S OWN DISCIPLINE:** the fixture fleet reported a DETERMINISTIC SEQUENCING mechanism from four arms at `127.0.0.1:1`, all instant-refusal, all consistent with BOTH readings — `grep -rl '192.0.2\|blackhole\|connect_timeout: 10'` over its scratch returns **NOTHING**. `reference_probe_must_discriminate`, applied to a fleet's own experimental design rather than to a document. ⚠️ **COROLLARY: `downstream_cx_total > 0` is NOT a sufficient decode-ran guard for an `ssl.*` assertion** — it increments even when the family is wholly suppressed. `0110` is **NOT BLOCKING** (live in-process `TCPEcho` on both sides, structurally mandatory for green via `wantObservable`). **FOUR PRIOR CLAIMS REFUTED BY RE-DERIVATION:** ⚠️ **the correction-FORM discriminator is NOT the ADR FAMILY** — `DECISIONS.md:16901` is ADR-0289 (FIFTH xDS row) correcting ADR-0286 (THIRD xDS row), **same family, INDENTED form**; the derivable rule is **SELF vs OTHER-ADR** (indented for another ADR, n=2 at `:16901`/`:16910`; inline for an ADR correcting ITSELF at its own IMPL, n=2 both on `:17213`) — the prescribed form SURVIVES on a different rule · **`:17209` is NOT a correction at all** (it is ADR-0294's `Documented boundaries` bullet) · **`BEHAVIOR_CONTRACT.md:831`/`:847` are NOT the `### Stat surface` ledger** (heading `:4950`, entries `:4954-5002`); they are the two narrative bare-total restatements, and **BOTH sets are load-bearing** — phase 75 appends after `:5002` AND bumps `:831`/`:847` 1204 -> 1205 · the ledger arrow is **`->` U+2192** with the bold running THROUGH the parenthetical to the colon (⚠️ the form is NOT uniform in the file; match the TAIL entry) · the `0110` arm cite `:362-373` points at a **doc comment** (real arms `:400-435`), `ProbeAdmin` is `:554`, the README bullet `:160-163`. **THREE GUARDS THE BRAINSTORM'S ROSTER MISSED** — `assertSSLCrossProduct` (`manager_test.go:4474-4488`, a hard-coded 3-leaf NEGATIVE roster consumed by all three increment tests), `TestServeConnection_PlaintextListenerIncrementsNoSSL` (`:4568`), and `0111`'s cross-side `AssertStats` (THREE hard-coded rosters). ⚠️ **And every landed increment test asserts the counter does NOT move; NONE would assert that it DOES** — a registered-but-never-Inc'd field passes every unit guard, so the row owes a POSITIVE arm (SPEC §10.1). **ADR-0044's misattribution is now MEASURED: cited 532 times across 504 lines, 134 as *"ADR-0044 ADR-on-impl convention"*, with ZERO supporting tokens in its own block** (`:1419-1462`, titled *"BEHAVIOR_CONTRACT HTTP/1.1 subsection"*) — a convention with NO written ADR, stated most fully at `:8534`, a reference NOTE inside a phase-18 ADR. **`ADR-0044-as-used` is the house form.** **A FOURTH stale-confession site found OUTSIDE `0110`:** `0108-xds-sds-validation-context/expectations.yaml:104-106` (prose only, no assertion) — RECORDED, NOT chartered. **The phantom `B5` is traced to its ORIGIN:** a phase-74 PLAN edit-bundle label (`SPEC.md:304`, `PLAN.md:133`/`:896`) pre-authored VERBATIM into the two production comments by `PLAN.md:325`/`:684` and propagated onward into `DECISIONS.md`/`STATE.md`. ⚠️ **A CORRECTION TO THIS SESSION'S OWN REASONING, recorded not quietly amended:** the controller re-derived the contested `BEHAVIOR_CONTRACT.md:962` offset with `grep -bo`, read **630**, and called the BRAINSTORM's **627** *"off by 3"*. **That was a UNIT error** — 627 is the 0-based CHARACTER index, 630 the 0-based BYTE offset, and the line has multibyte chars. **The BRAINSTORM's figure HOLDS**, and this lineage has now produced TWO failed corrections against that single anchor. ⚠️ **NO BUILD OF ROW 75 EXISTS, in-tree or in scratch** — field, registration, predicate, guards, the new helper and the `0110` asserter are ALL specified and NONE compiled; every §3 figure is a **reference-side** measurement and **the subject side has never emitted this counter**. ⚠️ **The absolute total 1204 was NOT re-derived mechanically** (no such command exists; ADR-0296's own ledger asks a future phase to COUNT). **Counts UNCHANGED (docs-only; re-run MECHANICALLY in the worktree at this close):** fixtures **119** / fuzzers **55** / stat surface **1204** / BackendKind **38** / modules **2**; `go build ./...` and `go vet` clean; `0110` and `0111` both PASS at `-count=1`. **Sentinel re-run MECHANICALLY TWICE (worktree AND landed master post-push; does NOT fire; `stop` NOT created):** (1) `NOT DONE: row 75` · (2) => **3** · (3) `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`. `next-prompt.txt` rolled to the phase-75 PLAN (**TRACKED despite `.gitignore`**; edited in this worktree).

- **prior active-phase:** `phase 75 (tls-no-certificate-stat) BRAINSTORM done` (2026-07-24) — phase-75 BRAINSTORM done. Lifecycle-state **DONE -> 1**; **ROW 75 REGISTERED `in-progress`**, RE-OPENING sentinel check (1). Fresh worktree `.worktrees/phase-75-brainstorm` off master `f8f6cd44`, branch `phase-75-brainstorm`. Docs-only: **ZERO production `.go`**, exactly THREE files edited (`ROADMAP.md`, `STATE.md`, `next-prompt.txt`) plus the new `BRAINSTORM.md`; **DECISIONS.md BYTE-UNTOUCHED**. **FIVE read-only re-costing agents at tip `f8f6cd44` on disjoint remits** — two running LIVE reference containers, three running Go-toolchain probes. **THE HEADLINE — THE ADR THAT DEFERRED THIS ROW DEFERRED IT ON A CLAIM THAT WAS FALSE WHEN WRITTEN, AND ITS SUPPORTING CITATION POINTS AT NOTHING.** ADR-0296 §Decision (g) (`DECISIONS.md:17308`, landed ONE DAY earlier) says *"envoy-go does not implement `require_client_certificate: false` (verify-if-present) at all — §Context ¶7 / `VERIFYIFGIVEN` is explicitly OUT OF SCOPE"*. **Both halves fail mechanically.** Phase 67 (**row 67 `done`**) landed it across ALL THREE validation-source shapes (`internal/tls/config.go:79-84`, documented at `:60-68`), with fixture `0110` driving it since phase 67; and `grep -n 'VERIFYIFGIVEN' DECISIONS.md` returns **exactly ONE hit — `:17308`, the citing sentence ITSELF** — while §Context ¶7 (`:17274`) is about the four-family split and contains **ZERO** `require_client_certificate`. ⚠️ **THIRD internal mis-pointer in ADR-0296**, and §Decision (g) OPENS by noting SPEC §13.1's hygiene claim was false for exactly this item, then reproduces the defect one level down. The narrower claim (not owed *in phase 74*) SURVIVES; **the reason given does not.** **SECOND REASON THE PICK IS RIGHT:** `0110/README.md:160-161` still confesses *"No `ssl.*` stats — envoy-go emits none"*, **FALSE since the phase-74 IMPL** — the same stale confession phase 74 retired in `0111`. ⚠️ The bullet **BUNDLES** a retirable half with a STILL-LIVE `/listeners` guard, so the edit **SPLITS** it. **TWO RIVALS MATERIALLY CHANGED BY RE-DERIVATION: (1)** `ssl.connection_error`'s *"membership is UNENUMERATED"* blocker is **REFUTED BY EXECUTION** (18 Go arms + 16 LIVE reference arms, every round control-validated): the reference books it for every **decodable but rejected** handshake and books **NOTHING** when the peer left without a complete ClientHello. But the finding **RAISED** the cost — a naive catch-all OVER-COUNTS the EOF class (`io.EOF`/`io.ErrUnexpectedEOF`), the highest-frequency class in production, so a PREDICATE is required and `manager_test.go:4294`'s landed `{"io.EOF", io.EOF, outcomeOther}` row **IS** that over-count — and it **cannot ride `0111`**, forcing a NEW fixture. Deferred on COST (~9-11), blocker RETIRED, boundary written down. **(2)** The phase-74 RUNNER-UP `upstream_cluster` span tag is **NO LONGER BLOCKED** — a **route-resolved** name is in scope at 4 of the 5 zero-endpoint sites (all five anchors EXACT; 6/5/7 partition CONFIRMED), and SELECTION-gating is precisely what makes it correct. ⚠️ **The DISCRIMINATING break phase 74 handed forward DOES NOT EXIST** — `extractEndpoints` (`internal/cluster/manager.go:875`) makes **ZERO** `GetClusterName()` calls; the name is a PARAMETER used only in five `fmt.Errorf` messages, so the break would have been **VACUOUS**. **RECOMMENDED NEXT OPENING: `fault.abort.grpc_status`** (7-9 tasks) — the ONLY identified candidate that clears a sentinel check-(3) blocker; escapes the ADR-0058 trailer gap because a gRPC error local reply is Trailers-Only/headers-only, which envoy-go already emits (`extproc/check.go:518-530`). ONE probe owed. **FURTHER CORRECTIONS, all controller-verified:** `registry.go:107` is the DUPLICATE panic, the INVALID-NAME panic is **`:117`** · **there is NO `B5` step in BEHAVIOR_CONTRACT** (all eight hits are `AMEND-B5`/Wasm; the departure is at `:1855`), yet *"BEHAVIOR_CONTRACT B5"* **leaked into TWO LANDED PRODUCTION CODE COMMENTS** (`internal/listener/manager.go:392`, `:1265`) plus `DECISIONS.md:17304`/`:17314` and `STATE.md` — PLAN-internal numbering became a permanent cross-reference pointing at nothing · **the FOUR-family deferral is WRONG IN BOTH DIRECTIONS**: `ssl.versions.*` has **NEITHER** blocker (Envoy's `TLSv1.2` passes `NamePattern`; the trip is in **Go's** `VersionName` = `"TLS 1.2"`, failing on a **SPACE** not a hyphen) while `ssl.sigalgs.*` is **STRUCTURALLY UNIMPLEMENTABLE** (`testingOnlyPeerSignatureAlgorithm` is UNEXPORTED), and `ConnectionState.CurveID` **IS** exported at go1.26.5 · **next-free reference port is `10450`, NOT `10118`** (max allocated **10449**; ports are NOT fixture-index aligned) · a **PRE-EXISTING H2 ROUTING BUG PROVEN BY EXECUTION** (`internal/filter/hcm/h2/stream.go:440` `url.Parse` truncates `u.Path`; `"//foo/bar"` routes as `/bar` via `route.go:128`) · `layered_runtime` is a hand-written **REJECT** (`bootstrap.go:568-569`), not a silent ignore. ⚠️ **ONE CONTESTED CLAIM RESOLVED AGAINST AN AGENT:** an agent reported ADR-0296's `BEHAVIOR_CONTRACT.md:962` anchor as FALSE; **that refutation is itself WRONG** — the token sits at character offset **627** of line 962, whole-file `grep -c` returns **1**, and a second agent quoted the same text. **The `:962` anchor STANDS.** Recorded because **a drift CORRECTION is itself a claim** — and this one failed. ⚠️ **NO probe was run against the reference for THIS ROW'S OWN SUBJECT and NO build of row 75 exists** — the `ssl.no_certificate` semantics are DOC-SOURCED, not re-executed (BRAINSTORM §11.1 says so explicitly). **ROADMAP edits proven MECHANICALLY: exactly 2 insertions / 1 deletion**; the family-paragraph edit is a **PURE APPEND** (old is a byte-prefix of new, **+1255** chars) and the deferred sentence is **BYTE-IDENTICAL** across the diff, still terminating at `force-trace.` so the `[^.]*\.` regex still binds. **NO deferred sentence narrowed** (phase-57 precedent); the narrow lands at the IMPL only. **Counts UNCHANGED (docs-only; re-run MECHANICALLY in the worktree at this close):** fixtures **119** · fuzzers **55** · stat surface **1204** · BackendKind **38** · modules **2** · DECISIONS tail **ADR-0296** COMPLETE (next-free **ADR-0297**). **Sentinel re-run MECHANICALLY TWICE (worktree AND landed master post-push; does NOT fire; `stop` NOT created):** (1) prints **`NOT DONE: row 75`** — the registration RE-OPENS check (1) after its first-ever silent close; (2) => **3**; (3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`. ⚠️ **check (1)'s blind spot RE-DERIVED, not copied: 106 data rows, 102 matched, FOUR misses** — `00` (em-dash "after" column), `04` (DOT in the slug `http-1.1`), `28.1a` and `28.1b` (LETTER suffix). All four are `done`. `next-prompt.txt` rolled to the phase-75 SPEC (**TRACKED despite `.gitignore`**; edited in this worktree).

- **prior active-phase:** `phase 74 (tls-handshake-outcome-stats) IMPL done` (2026-07-24) — lifecycle-state 3 -> **DONE**; **ROW 74 FLIPS `in-progress` -> `done`** at the T8/T9 six-gate (ADR-0106, the SOLE leg — a SINGLE FLAT ROW, `reference_roadmap_split_phase_row_done`). Fresh worktree `.worktrees/phase-74-impl` off master `f1221a4a` (the phase-74 PLAN squash), branch `phase-74-impl`; **NINE subagent-driven tasks -> nine local commits -> ONE squash**, per `feedback_execution_style` / `feedback_subagents_no_push` / `feedback_push_to_origin`. **THE ROW DELIVERED:** `listener.<normalized-addr>.ssl.handshake` / `.ssl.fail_verify_error` / `.ssl.fail_verify_no_cert`, registered inside `registerListenerMetrics` **gated on `rt.tlsMode` ALONE — NO kind check** (this row is that field's FIRST production consumer), and Inc'd at the two points inside `serveConnection`'s EXISTING `if selected.tlsCfg != nil` block, with **NO nil guards** — because the three pointers are non-nil exactly when `rt.tlsMode`, and the two predicates are EQUIVALENT by the mixed-TLS/plaintext-chain reject. **+3 stats (1201 -> 1204, the FIRST non-zero stat delta in this lineage) / +0 fixtures (119) / +0 fuzzers (55) / +0 BackendKinds (38) / +0 modules (2) / +0 PRODUCTION imports / ZERO new packages / ZERO new exported symbols** — the last two asserted BY COMMAND (an extracted import-block diff; a `go doc -all` set-diff, both IDENTICAL), not by inspection. **THE HEADLINE — THE ROW'S CENTRAL CROSS-SIDE CLAIM IS NOW EXECUTED, NOT DERIVED.** Fixture `0111-tls-cvc-empty-dynamic-fallback` gained the `fixture.StatsAsserter` its **own README confessed was *infeasible***, and **the REFERENCE emitted `handshake=1 fail_verify_error=1 fail_verify_no_cert=1` with `downstream_cx_total=3` — matching the subject EXACTLY.** The sanctioned subject-only fallback was NOT needed and was never taken. Shape A over `/stats/prometheus`, keyed on the metric NAME with `envoy_listener_address` IGNORED, the ABSENT check SEPARATE from the value check with a `continue` (an unregistered counter would read `0 == 0` and pass VACUOUSLY). **ALL TWELVE BREAKS RAN, EVERY ONE WITH ITS PREDICTED OUTCOME.** **Break F — the one declared *must not fire* — did NOT fire**, and stayed green even with the const mutated, which is the demonstration that a hand-written string is self-consistent and therefore worthless; **Break F'** (the same mutation against the LIVE construction) fired exactly the two live rows, completing the cross-product. **Break D — the row's DISTINGUISHING break, ADDING the refuted `rt.kind != kindQUIC` gate** — fired, and **the FIRST and discriminating assertion was (1), the `reflect.DeepEqual` NAME SET**, not (4)'s zero check; the `driveH3` precondition SUCCEEDED, so the break did not fire by killing the drive. **Break E reproduced V1-M2 EXACTLY**: `TestListenerMetrics_GateMatchesInc` fired **exclusively** through its three counter-POINTER assertions while the `tlsMode`/`tlsCfg` build-time predicates printed NOTHING — the pointer half is load-bearing and the predicate-only version would have stayed green. **Break E'** demonstrated the counterfactual live: a misspelled name still yields **length 3**, so the landed `statssink` `countMetrics` cardinality precedent **would have been GREEN**. **Break B** fired in EXACTLY TWO of three with **both halves of each cross-product** firing. **Break C confirmed F3 verbatim** — a nil `*stats.Counter`.Inc is a **PROCESS CRASH** in the `serveConnection` goroutine, stack `stats.(*Counter).Inc` (`counter.go:22`) under `listener.(*listenerRuntime).serveConnection`, `created by ...acceptLoop`, with no `recover()` anywhere in `internal/listener/`; **C' was NOT needed** — the panic frame IS the Inc site. **Break H proved the `0111` RD3 forced-send disclaimer INVERTS at the counter layer** (subject `fail_verify_error` -> 0, `fail_verify_no_cert` -> 2, while `CompareBytes` stayed GREEN), which is why the prose revision was owed rather than optional. ⚠️ **ONE SUBSTITUTION, and its rationale is itself a claim: the PLAN's literal Break G' was VACUOUS** — renaming the method while leaving `want` at the PASSING value makes green consistent with BOTH *"never ran"* and *"ran and passed"*; **the PLAN's own parenthetical for G' applies recursively to G' itself.** Re-run STACKED ON Break G (a FAILING assertion) it discriminates cleanly: broken-and-renamed => **GREEN, the finding** (the driver's log lines vanished entirely — `AssertStats` silently never ran); broken-and-named => RED. ⇒ **`var _ fixture.StatsAsserter` is a TRIPWIRE, not the dispatch mechanism.** **SIX-GATE FULLY GREEN**: `gofmt -l .` silent · `go vet ./...` · `go build ./...` · `go mod tidy -diff` EMPTY · `git diff master -- go.mod go.sum` EMPTY · `golangci-lint run ./...` · **the FULL differential `ok 402.512s`, 119 subtests / 119 PASS / 0 SKIP / 0 FAIL**, with the fixture-directory set and the subtest set `comm -3`-verified EQUAL. `go test ./... -race` green; `go list -deps ./internal/listener` **439 packages both sides, diff IDENTICAL — no new edge**; the BYTE-UNTOUCHED roster **569 files, 0 mismatches**, its intersection with the EDIT roster **EMPTY**. **ELEVEN PLAN/SPEC CLAIMS REFUTED BY EXECUTION** (full ledger in `PROGRESS.md` §IMPL record). The three that matter: **(1)** the PLAN's own check-(1) BLIND-SPOT figure was **STALE** — it recorded *"104 rows, 102 matched, TWO misses"*; re-derived at the IMPL tip it is **106 rows, 102 matched, FOUR misses**, the PLAN having dropped `28.1a`/`28.1b` **which the phase-73 close already knew about** — the exact failure the "RE-DERIVE, never copy" instruction on that figure exists to prevent. **(2)** The PLAN's own **"+0 production imports" gate COMMAND is unreliable**: it returns **3 false-positive hits** (T5's `helpText` map literals) and exits 0, so anyone gating on the exit code reads a PASS as a FAIL; the decisive checks are the hunk headers and a direct extracted-import-block diff. **(3)** T2's predicted red was **UNREACHABLE IN ONE STEP** — the adversarial pass's own pointer assertions reference fields Step 2 creates, so the binary fails to COMPILE and no assertion runs; a **two-stage red** was executed rather than recording a build failure as if it were the assertion red. Also found: a **FIFTH** stale claim in the row-74 ROADMAP cell (a whole-file grep now returning 3, all three hits being sentences refuting the phrase), a **THIRD** stale comment in the PRODUCTION file (a dangling `AllocatesTwoMetricsPerListener` cross-reference the rename would have orphaned), a **FOURTH** RD3 site (`0111/driver.go:416-420`), and ~180 lines of intra-row anchor drift (⚠️ **the T2/T4 commit messages carry the PLAN's STALE `:516-525`/`:639`/`:1178` anchors verbatim, as instructed — recorded, not rewritten**). **TWO FRESH HAZARDS**: `io.Copy(conn, conn)` is unusable as an in-test echo backend (`*net.TCPConn` implements `io.ReaderFrom`, so the socket splices into itself and the failure surfaces **at the BACKEND**, reading as a proxy bug — proven with a direct-to-backend probe before blaming the proxy); and T1's PKI presents a **LEAF-ONLY** client chain **deliberately** (appending the issuing CA, as the `0111` driver does, would make `len(UnverifiedCertificates)==2` and fire T1's own `want 1`). **ONE `-race` failure, classified PRE-EXISTING ON EVIDENCE RATHER THAN ASSERTION**: `internal/boot TestSDSEndToEnd_FetchFailure_BootFailsClosed` — isolate-green 3/3, file byte-identical to master, and **REPRODUCED ON MASTER 3/20 with the byte-identical error string** under 48 CPU burners on 32 cores. ⚠️ It **WIDENS** `reference_sds_init_fetch_timeout_dial_budget_flake` rather than matching it: a DIFFERENT test in a DIFFERENT package sharing the same 200 ms-budget mechanism, now with the reproducible recipe the original entry explicitly lacked. **ADR-0296 COMPLETED IN PLACE** (PROPOSED -> COMPLETE; §Decision + §Consequences after the RETAINED footer, mirroring ADR-0295; `### Decision` 0 before / 1 after; tail stays ADR-0296, next-free ADR-0297) with its TWO internal defects fixed — the ¶6 -> ¶4(i) mis-pointer and **¶3's SELF-REFUTING grep**, a count of 0 that the SPEC's own append had made 1, the sole hit being that very paragraph. **ADR-0286 §Consequences C3 CORRECTED** via an INDENTED blockquote, `cat -A`-verified byte-identical in indentation to the `:16901` precedent, phrased as a **documentary finding, NOT** as criticism of phase 65 — recording that *"a framework-surgery row of its own"* is REFUTED (an inline add to ONE function gated on a field that already existed), that the *"opaque handshake-error callback"* half **was never in C3 at all**, and closing with **what STILL STANDS**: a **FOUR**-family `ciphers`/`versions`/`curves`/`sigalgs` boundary on its own two blockers, with **LIFECYCLE EXPLICITLY NOT one of them**. **The ROADMAP narrow REPLACED THE PROSE, not just three names** (`:204`, not `:202`): *"envoy-go emits ZERO such stats"* had gone FALSE and *"a framework-surgery row, NOT an inline add"* was the very adjective this row refuted; `curves`/`sigalgs` ADDED; **check (2) STAYS 3** and the sentence still terminates at `force-trace.` so the `[^.]*\.` regex still binds. **Sentinel re-run MECHANICALLY TWICE (worktree AND landed master post-push): does NOT fire; `stop` NOT created** — (1) **SILENT**, as predicted at the PLAN and confirmed here (row 74 was the last non-`done` chartered row); (2) => **3**; (3) `NEVER OPENED: gRPC/Runtime/WASM`. **Next -> the phase-75 BRAINSTORM** (the roller SELF-PICKS; `ssl.connection_error` is the cheapest follow-on this row NAMED, but its membership is UNENUMERATED and that cost must be RE-DERIVED, not re-read).

- **prior active-phase:** `phase 74 (tls-handshake-outcome-stats) PLAN done` (2026-07-24) — lifecycle-state 2 -> 3; docs-only (`PLAN.md` + `PROGRESS.md`, the ONLY two files in the squash's phase-directory delta — **ZERO production `.go`, DECISIONS and ROADMAP BYTE-UNTOUCHED**, row 74 STAYS `in-progress`). Fresh worktree `.worktrees/phase-74-plan` off master `ab13fc19`, branch `phase-74-plan`. **NO PARALLEL STREAM** — `git diff --stat f5a38c40 HEAD -- '*.go' go.mod go.sum test/` EMPTY. Six agents: FOUR read-only re-derivation agents on disjoint remits plus TWO adversarial verifiers in PRIVATE scratch (V1 building T1-T4 **BY EXECUTION** and running their breaks; V2 re-running ~120 anchors). **The 9-task TDD spine (T1-T9) with TWELVE breaks** (the SPEC's A-G plus PLAN-added C'/E'/F'/G'/H), **D — ADDING the refuted `rt.kind != kindQUIC` gate — the DISTINGUISHING one.** ⚠️ **TWELVE SPEC claims REFUTED.** The load-bearing one: **`SPEC.md:1`, `:315` and `:320` still carry the SPEC's OWN REFUTED first QUIC reading against its authoritative §3.4/§16** — a PLAN copying §10 item 2 verbatim would have implemented the wrong gate AND made Break D unfireable — and **a FOURTH stale site sits outside `SPEC.md`, at `ROADMAP.md:136`**, the very cell the IMPL opens. Also: the SPEC's ctx-override bound (*"ZERO `context.WithTimeout` in production"*) is **FALSE** (`listenerfilter/pipeline.go:43`) though its conclusion survives on the stronger ground that `Pipeline.Run` returns only an `error`; `checkName` panics at `registry.go:117` not `:107`; `errors` is at `manager.go:7` not `:6`; the SN3 impl is `name.go:83-92` not `:85-93`; `quic_test.go:92-97` pins only the COUNTER half; and **the SPEC's `grep => 0` is now `1`, made self-refuting by its own §Context append.** **TEN new findings, THREE SEVERE**: `internal/listener/` has **NO mTLS PKI** in its test corpus; `mkDownstreamTSRequireClientCert` **boot-REJECTS**; and a nil `*stats.Counter`.Inc **PANICS in a goroutine with no `recover()`** ⇒ Break C is a PROCESS CRASH. **The adversarial pass changed SIX instructions**: `net.Pipe()` **DEADLOCKS** silently to a 45s timeout; **Break F FIRED at its original scope** (the one break declared *must not fire*); `GateMatchesInc` **stayed GREEN under Break E**; T7's own `grep '1201' => 0` gate was **unsatisfiable by T7's own instruction**; Break H **would have killed the trusted arm so `AssertStats` never ran**; and **§1.4 had self-certified a completed adversarial pass over a `PROGRESS.md` that did not exist**, having deleted phase-73's guardrail against exactly that — restored verbatim. Sentinel re-run MECHANICALLY TWICE (worktree + landed master post-push): does NOT fire; `stop` NOT created. Counts UNCHANGED. **Next -> the phase-74 IMPL** (row 74 flips `done` at its six-gate; ADR-0296 completes IN PLACE; the ROADMAP narrow REPLACES THE PROSE and check (2) STAYS 3).


## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
