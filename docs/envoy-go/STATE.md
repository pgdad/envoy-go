# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `22-http-filter-lua` (parent row in-progress; sub-phase `22.1-http-filter-lua-vm-and-headers-bridge` done at master tip `c986419`; sub-phase `22.2-http-filter-lua-full-bridge` **BRAINSTORM done at this commit** — awaiting 22.2 SPEC; sub-phase `22.3-http-filter-lua-multi-script-and-per-route` planned). Phase `22-http-filter-lua` parent BRAINSTORM done at parent BRAINSTORM commit + parent SPEC done at `41ccee7`; 22.1 lifecycle traversed BRAINSTORM (parent) → SPEC at `a7021aa` → PLAN at `02d745a` → IMPL at `d30f131` → done. Phase `22.2` lifecycle traverses 0 → 1 at THIS commit per BOOTSTRAP §4.1 invariant 1 (BRAINSTORM done; awaiting SPEC). **22.2 BRAINSTORM done at 2026-05-18** via a 14-question dialogue (Q1 body bridge full upstream parity coroutine+zero-copy+both methods + Q2 trailers mirror headers + Q3 metadata+dynamic-metadata both at 22.2 — BREAKS cross-phase dynamic-metadata deferral discipline + Q4 connection full upstream SSL surface + Q5 httpCall full upstream parity cluster+sync/async + Q6 crypto 6 upstream methods in-package + Q7 fileBytes + timestamp full upstream parity + Q8 streamInfo-full 7 methods including filter-state in-package + Q9 pragmatic-middle EXTRACT internal/dynamicmetadata + Q10 strict-scope NEW ADR-0191 for internal/lua extensions + Q11 pragmatic-middle 5-6 stat counters + Q12 single mixed-mode fixture-0027 + Q13 WEAK HOLD escape-valve + Q14 stay single-phase let PLAN-stage gate fire if needed).
- **phase-directory:** `docs/envoy-go/phases/22-http-filter-lua/` (parent — BRAINSTORM.md + SPEC.md authored at parent SPEC commit; parent phase row stays in-progress until 22.3 phase-done per parent SPEC §1 closure pattern). Sub-phase directories: `docs/envoy-go/phases/22.1-http-filter-lua-vm-and-headers-bridge/` (SPEC.md + PLAN.md + PROGRESS.md + REVIEW.md from 22.1 lifecycle; LANDED at master tip `c986419`) + `22.2-http-filter-lua-full-bridge/BRAINSTORM.md` **authored at THIS commit** (14-Q dialogue + 12 §2 numbered sub-sections + framework-survey + per-route disposition + stat surface + fixture strategy + anticipated-ADR roster + deferred items + 7 D-questions carry-forward) + `22.2-http-filter-lua-full-bridge/README.md` (updated at THIS commit — Status now "BRAINSTORM done; awaiting 22.2 SPEC") + `22.3-http-filter-lua-multi-script-and-per-route/README.md` (placeholder; awaits 22.3 BRAINSTORM).
- **lifecycle-state:** `phase 22.2 BRAINSTORM done; awaiting 22.2 SPEC`. ROADMAP row `22.2` flips `planned → in-progress` at this commit with the per-cell BRAINSTORM-done annotation per ADR-0106 (14-Q dialogue + maximal envelope D per Q1-Q8 + cross-phase dynamic-metadata deferral break per Q3 + pragmatic-middle extraction posture per Q9 + strict-scope ADR-0188 allowance per Q10 + ~5-6 stat counter delta per Q11 + single mixed-mode fixture-0027 per Q12 + WEAK HOLD escape-valve per Q13 + single-phase scope-shape per Q14). Parent row `22` STAYS `in-progress` until 22.3 phase-done; sub-row `22.3` STAYS `planned`. DECISIONS.md tail UNCHANGED at ADR-0189 (NO new ADR consumption at this BRAINSTORM commit per ADR-0044 §Context-draft discipline — BRAINSTORM authors anticipate but do not consume ADR numbers). ADR-0188 + ADR-0189 + ADR-0125 §(xiv) UNCHANGED at this commit. Next-free **ADR-0190** UNCHANGED. **22.2 BRAINSTORM authored at THIS commit** as `docs/envoy-go/phases/22.2-http-filter-lua-full-bridge/BRAINSTORM.md` (~700 LoC; 14-Q dialogue across 12 §2 numbered sub-sections matching parent 22 BRAINSTORM + phase-21 BRAINSTORM precedent shape; surfaces 7 D-questions for 22.2 SPEC; documents the anticipated 22.2 IMPL ADR roster ADR-0190/0191/0192 + in-place AMEND on ADR-0177 + conditional ADR-0193 per WEAK HOLD). **NO new code lands at BRAINSTORM** per ADR-0044 §Context-draft discipline; ADR roster UNCHANGED at ADR-0189; next-free ADR-0190 UNCHANGED.
- **next-skill:** `superpowers:brainstorming` (scoped to 22.2 SPEC per SKILL_ROUTING state-1 entry "Phase in ROADMAP, directory does not exist → create + superpowers:brainstorming scoped to THIS phase → output: SPEC.md" — the 22.2 directory exists with BRAINSTORM.md + README.md; the SKILL_ROUTING state-1 entry's "create directory" precondition is already satisfied; the SPEC-authoring session uses the brainstorming skill scoped to 22.2 SPEC matching the 22.1 SPEC session precedent at master tip `a7021aa`). Cold-start scope for the next session: read STATE.md (this file) + `docs/envoy-go/ROADMAP.md` (row 22.1 done; row 22.2 in-progress at this commit; row 22.3 planned; row 22 in-progress) + `docs/envoy-go/phases/22.2-http-filter-lua-full-bridge/BRAINSTORM.md` (THIS commit's 14-Q dialogue) + `docs/envoy-go/phases/22.1-http-filter-lua-vm-and-headers-bridge/REVIEW.md` (predecessor sub-phase REVIEW + 24-item acceptance + handoff state) + `docs/envoy-go/phases/22-http-filter-lua/SPEC.md` (parent SPEC §10 forward-pointers + §11 7-pin empirical-pin block + §12 D-questions framework) + DECISIONS.md tail (ADR-0188 + ADR-0189 full bodies; ADR-0190 still next-free; ADR-0125 §(xiv) anticipation paragraph; ADR-0177 next-free for in-place AMEND at 22.2 IMPL per Q5 + Q9) + BEHAVIOR_CONTRACT.md `### envoy.filters.http.lua` (22.1's contract subsection — 22.2 SPEC anticipates extending). The 22.2 SPEC session creates a fresh worktree per project memory `feedback_git_worktrees.md`: `git worktree add /home/esa/git/envoy-go/.worktrees/phase-22.2-http-filter-lua-full-bridge-spec -b phase-22.2-http-filter-lua-full-bridge-spec <22.2-BRAINSTORM-tip-SHA>` per the phase-22.1-SPEC + phase-19.1-SPEC + phase-18.1-SPEC sub-phase-SPEC worktree precedent.
- **last-commit:** `6ad3064`. Phase 22.2 BRAINSTORM squash-merge of branch `phase-22.2-http-filter-lua-full-bridge-brainstorm` from worktree `.worktrees/phase-22.2-http-filter-lua-full-bridge-brainstorm/` landed as `6ad3064` on master per the phase-09..21 + phase-18.1 + phase-19.1 + phase-22.1 squash-merge convention. Predecessor master tip was `c986419` — `phase 22.1 IMPL follow-up: STATE.md SHA-fill (TBD → d30f131 post-squash)`. Pre-predecessor was `d30f131` — `Squash merge phase-22.1-http-filter-lua-vm-and-headers-bridge-impl`. This SHA-fill follow-up commit backfills the placeholder per the phase-09..21 convention: `phase 22.2 BRAINSTORM follow-up: STATE.md SHA-fill (TBD → 6ad3064 post-squash)`.
- **last-updated:** 2026-05-18
- **next-free ADR:** `ADR-0190` — UNCHANGED at this 22.2 BRAINSTORM commit (NO ADR consumption per ADR-0044 §Context-draft discipline; BRAINSTORM authors anticipate but do not consume ADR numbers). Anticipated 22.2 IMPL ADR landings (settled at 22.2 BRAINSTORM per Q9 + Q10 + Q13): **3 NEW ADRs** (ADR-0190 = NEW `internal/dynamicmetadata/` framework primitive per Q9 + Q3 — cross-phase dynamic-metadata deferral break + per-stream `*Bucket` accessor; ADR-0191 = NEW `internal/lua/` 22.2 API extensions per Q10 strict scope — coroutine yield/resume + body-bridge buffer seam at consumer-#1-scope-expansion; ADR-0192 = NEW `internal/filter/http/lua/` 22.2 package shape extensions — full bridge surface delta + filter-state in-package + crypto in-package + connection-SSL in-package + fixture-0027 mixed-mode discipline + 5-6 envoy-go-strict departure records) + **1 in-place AMEND** on `ADR-0177` (per Q5 + Q9 — `internal/httpclient/` cluster-based dispatch path for lua's httpCall consumer-#1; NO new ADR number consumed per ADR-0044 in-place edit discipline; matches phase-17 → phase-18 ADR-0149 → ADR-0150 AMEND precedent) + **1 conditional ADR-0193** (escape-valve slot per Q13 WEAK HOLD — fires only at one of: R6 *LState-pool gate if body+coroutine pushes per-stream construction over 1ms; body-buffer-seam-with-ADR-0128 separation; connection-SSL bridge integration with phase-03; httpCall coroutine integration with cluster manager). Anticipated 22.3 IMPL ADR landings UNCHANGED from 22.1 IMPL state (settled at 22.3 BRAINSTORM): ~1-2 NEW ADRs (NEW 9th canonical per-route shape ADR + per-route 3-tier dispatch) + 1 IN-PLACE AMENDMENT body landing at ADR-0125 §(xiv). **ADR-0188 EXPLICIT API-REVISION ALLOWANCE clause STAYS scoped to consumer-#2** per Q10 strict scope; 22.2's consumer-#1-scope-expansion revisions land under NEW ADR-0191 instead of in-place AMEND on ADR-0188. Phase-22.2's WEAK HOLD escape-valve buffer at THIS BRAINSTORM commit: 0-1 anticipated consumption (ADR-0193 conditional); re-evaluates at 22.2 IMPL atomic-landing Task per Q13.

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
