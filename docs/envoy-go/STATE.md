# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `phase 29.1 IMPL done` (2026-06-04, this commit) — **phase 29.1 (`network-filter-mongo-wire-and-requests`) sub-phase IMPL COMPLETE at 2026-06-04** — all 17 PLAN tasks landed + green; the six-gate is GREEN LIVE (`go build`/`go vet`/`golangci-lint` clean; `-race -short` PASS; the FULL **52-dir** differential suite byte-exact PASS incl. the 50-dir R1 back-compat / §4 zero-touch proof; h2spec 53/53; proxy-wasm 10/10). The `internal/filter/network/mongoproxy/` package request side LANDED: TypeURL via `proto.MessageName` (`envoy.extensions.filters.network.mongo_proxy.v3.MongoProxy`), 5-field config parse + FaultDelay PGV arms (oneof-required + `fixed_delay` gt-0s + denominator-defined) + the commands remembered-set + alias normalization, the in-house little-endian 14-type BSON parser (`bson.go`), the request-side wire decoder (5 opcodes body-decoded; Reply/CommandReply recognized-not-decoded until 29.2; OP_MSG/Update/Delete/Msg → `decoding_error`; sniffing-off-on-error for the connection lifetime AMEND-B6), the EAGER 23-stat roster (22 counters + the `op_query_active` gauge under `mongo.<stat_prefix>.`; D-P1 eager-at-config-parse; `delays_injected` PLURAL pinned by `TestStatRoster_MatchesUpstreamMacro`), the dynamic `cmd.*`/`collection.*`/callsite helpers, the per-connection active-query list (written 29.1, consumed 29.2), the 8th `builtins.RegisterBuiltins` built-in + the `mongo_proxy/v3` bootstrap blank-import, and the `mongo.` FOUR-RULE tag-extractor arm at `internal/stats/name.go` (AMEND-C1 multi-label hoist; labels SORTED LOCALLY → `prom.go` BYTE-UNTOUCHED, D-S29.1-5). **USER-APPROVED IMPL deviation (Task-8):** a `stats.IsValidName` guard around the 3 wire-derived dynamic-stat increments (skip un-nameable segments upstream would emit; fixed counters + active-query append always run) — fuzz-validated no-panic (`FuzzMongoDecode`, 60 s / 16 M execs, zero crashers) + recorded as a BEHAVIOR_CONTRACT coverage boundary at Task 17. Fixtures `0049-mongo-requests` (9 arms, cross-side label-aware StatsAsserter, the existing `TCPSink` backend BackendKind 28 per AMEND-C4; `cx_destroy_*` PRESENCE-ONLY per AMEND-C2; `$comment` callsite double-count AMEND-C3; R4 deliberate-break with `-count=1`) + `0050-mongo-boot-reject` (the `stat_prefix`-required PARSE-REJECT) GREEN cross-side; the 39th fuzzer `FuzzMongoDecode`. The Task-17 completion bundle (this commit): BEHAVIOR_CONTRACT `### envoy.filters.network.mongo_proxy` subsection + the 337→360 extension paragraph; ADR-0224 §Decision/§Consequences body in-place (DECISIONS tail STAYS ADR-0226 — no new ADR number); STATE/ROADMAP advance; next-prompt.txt rewritten for the 29.2-SPEC cold-start. **Earlier (29.1 PLAN, preserved):** the 29.1 PLAN (`docs/envoy-go/phases/29.1-network-filter-mongo-wire-and-requests/PLAN.md`, 3033 lines, 17 bite-sized TDD tasks) decomposes the SPEC §10 task spine into write-failing-test → run → implement → run → commit steps with complete Go code blocks, exact file paths, run commands + expected outputs, and per-task `gofmt -l` + `golangci-lint`. **The FINAL ADR-0045 gate-check PASSED: 17 tasks / ~1070–1470 production LoC → NO split** (both axes under the gate; the pre-authorized 29.1a/29.1b axis STANDS-UNCONSUMED). PLAN-time dispositions: stats.go REORDERED before codec.go (green-compiling `config → stats → decoder → glue` dependency order, the 28.1 precedent); the active-query list lives ON THE DECODER (not the filter — keeps the codec unit-testable; the zookeeper `requestsByXid`-on-decoder precedent; resolves the §3.7 representation question); D-S29.1-2 (BSON ordered-element `bsonDoc` + `asInt64`), D-S29.1-4 (`chainConsumed` high-water adapted verbatim), D-S29.1-5 (the mongo name.go arm SORTS its own labels locally → `prom.go` UNTOUCHED — verified `writeMetricLine` emits slice-order with no sort + SN4 prepends, so a global sort would break existing goldens), D-S29.1-6 (decoding_error single-increment in the decoder) all resolved at PLAN. plan-document-reviewer APPROVED (first iteration; 3 advisories, 2 folded: Task-7 vacuous-totalAppended test fixed + Task-4 `parseBSON` trailing-bytes check tightened; the 3rd — Task-15 cumulative-accounting placeholders — is correctly IMPL-owned). **Earlier (29.1 SPEC, preserved):** the 29.1 SPEC (`docs/envoy-go/phases/29.1-network-filter-mongo-wire-and-requests/SPEC.md`, 16 §§ + 2 appendices, mirroring the 28.1 first-sub-phase precedent) INHERITS the parent SPEC §5/§6/§7/§8/§11/§13 pins and refines them into the per-Task surface: the `internal/filter/network/mongoproxy/` file split (doc/mongoproxy/config/bson/codec/stats/filter.go), the 5-field config parse + FaultDelay PGV arms + the commands set with alias normalization, the 14-type BSON parser, the request-side codec (5 opcodes body-decoded; Reply/CommandReply recognized-not-decoded until 29.2; sniffing-off-on-error), the EAGER 23-stat roster (**D-P1 RESOLVED: eager-at-config-parse**; boot-window departure recorded), the dynamic cmd/collection/callsite name helpers, the active-query list (written 29.1, consumed 29.2), the 8th built-in + bootstrap blank-import, fixtures `0049-mongo-requests` (9 arms, label-aware StatsAsserter, the existing `TCPSink` backend) + `0050-mongo-boot-reject`, the 39th fuzzer `FuzzMongoDecode`, the ~17-task spine + ADR-0045 gate re-check (~1070–1470 production LoC; fits; pre-authorized 29.1a/29.1b split axis if the PLAN trips). **The D-P2 dynamic-stat live re-probe was EXECUTED at this session** (the parent §11.1 caveat CLOSED): a working docker-bridge-network probe (the prior failure was a Docker-Desktop `--network host` artifact) + the upstream `well_known_names.cc` source pin produced **4 new 29.1 AMENDs (C1..C4)**: **AMEND-C1** — the dynamic-stat Prometheus form is FULLY label-hoisted (`envoy_mongo_cmd_total{envoy_mongo_cmd="…"}`, `envoy_mongo_collection_query_*{envoy_mongo_collection="…"}`, callsite carries 3 labels; values NEVER appear in metric names) → the `name.go` arm is a FOUR-RULE tag extractor (**D-P2 + D-P3 RESOLVED**; the parent's "dynamic segments stay in the name" anticipation REFUTED); **AMEND-C2** — `cx_destroy_*_with_active_rq` increments on the REFERENCE during 29.1 fixtures (unanswered queries at connection close) → presence-only assertions at 29.1, value parity at 29.2; **AMEND-C3** — callsite queries DOUBLE-COUNT into both the collection and callsite families; **AMEND-C4** — the `0049` backend is the EXISTING `TCPSink` (BackendKind 28; silent — the 28.1 lesson re-applied); NO new BackendKind at 29.1 (the mongo responder, anticipated value 30, is 29.2's). **D-P8 RESOLVED: yes** — `0049` arm 2 exercises `commands: ["isMaster"]` + the `unknown_command` fallback on a dedicated listener. The D-P5 unit-arm half (the 3 FaultDelay PGV reject arms as code + unit tests) is scheduled at 29.1; the fixture-arm half stays for the 29.3 SPEC. spec-document-reviewer APPROVED (first iteration; 2 advisory notes folded as no-action). ROADMAP sub-row 29.1 flipped `planned → in-progress` at this commit + the 29.1 row's stale `.mongo.` inline-prefix text corrected in place to the ADR-0218 tag-extractor shape (the parent-§16-scheduled AMEND-B2 edit). **Earlier (phase 29 parent SPEC, preserved):** the parent SPEC (squash `ab376d8`) resolved all twelve D29-1..D29-12 pins against Envoy v1.37.2 with 13 AMENDs (B1..B13) — the 7-opcode LEGACY-ONLY envelope (OP_MSG → `decoding_error`), the `mongo.<stat_prefix>.` scope, the 22+1 roster (`delays_injected` plural), per-connection creation upstream, builtin-remembering `commands`, the FaultDelay PGV arms, the timing-bearing access log (no fixture dir), the AMEND-B13 seam reframing — and formalized the 3-way split + the ADR-0224/0225/0226 §Context drafts. **Phase 28 (zookeeper_proxy, ADR-0221/0222/0223) COMPLETE and CANONICAL.**
- **phase-directory:** `docs/envoy-go/phases/29.1-network-filter-mongo-wire-and-requests/` (SPEC.md + PLAN.md at this commit; the IMPL produces the code + fixtures + PROGRESS.md). The parent directory `29-network-filter-mongo-proxy/` (BRAINSTORM.md + the parent SPEC) stays the durable phase-29 record.
- **lifecycle-state:** `phase 29.1 IMPL done (SKILL_ROUTING state-1-for-29.2; next = the 29.2 SPEC)` — the 29.1 IMPL (this commit) landed all 17 PLAN tasks + the completion bundle + the GREEN six-gate. The next lifecycle transition opens the **29.2 SPEC** (`network-filter-mongo-responses-and-correlation`): the phase-29 parent BRAINSTORM already drafted the ADR-0225 §Context, so the sub-phase SPEC is authored via `superpowers:writing-plans` (the 28.2-SPEC-after-28.1-IMPL precedent — a fresh sub-phase directory + SPEC, NOT a fresh brainstorm). ROADMAP: parent row 29 STAYS `in-progress` (the ROLLUP is 29.3's); sub-row 29.1 `in-progress → done` (2026-06-04, this commit); 29.2/29.3 STAY `planned`.
- **next-skill:** `superpowers:writing-plans` (scoped to **the 29.2 SPEC** — author `docs/envoy-go/phases/29.2-network-filter-mongo-responses-and-correlation/SPEC.md`: the OP_REPLY/OP_COMMANDREPLY response decoder in `OnWrite` + reply-flag counters; requestID↔responseTo correlation consuming the 29.1 active-query list [first-match-erase] + the ADR-0223 per-connection mutex; the `op_query_active` gauge inc/dec [the project's first differentially-mirrored gauge]; `cx_destroy_local/remote_with_active_rq` value parity [parent D-P4 close-direction]; `emit_dynamic_metadata` [the ADR-0217 Bucket]; fixture `0051-mongo-responses` + the `TCPMongoResponder` BackendKind [anticipated value 30]; the ADR-0225 §Decision/§Consequences body at 29.2). Per `feedback_git_worktrees` work in a git worktree; commit LOCAL-ONLY, the controller squash-merges + pushes at stage-close [`feedback_push_to_origin`]. Counts at 29.1 IMPL phase-done: fuzzers **39** (`FuzzMongoDecode`), fixtures **52** [tail `0050-mongo-boot-reject`], stat surface **360** [+23]; DECISIONS.md tail **ADR-0226** (next-free **ADR-0227**; the ADR-0224 §Decision/§Consequences body landed in-place at this commit — no new number consumed).
- **last-commit:** `5ef5215` — `phase 29.1 IMPL: mongo_proxy request-side wire+BSON decode + 23-stat eager roster + 0049/0050 differential — 17-task spine green, six-gate PASS [ADR-0224]` (squash-merged from `phase-29.1-network-filter-mongo-wire-and-requests-impl`, 23 task/followup commits → one squash; pushed to origin/master). Substantive predecessor on master: the 29.1-PLAN squash `89c194a` (PLAN.md + STATE.md + next-prompt.txt); before that the 29.1-SPEC squash `7574219`.
- **last-updated:** 2026-06-04
- **next-free ADR:** `ADR-0227` (DECISIONS.md tail stays **ADR-0226**; NO ADR number consumed at the 29.1 IMPL — the ADR-0224 §Decision/§Consequences body landed IN-PLACE per ADR-0044; the ADR-0225/0226 §Context drafts already exist and their bodies land at 29.2/29.3; next-free after phase-29 phase-done stays ≈ **ADR-0227**). ADR-0209 escape-valve reserve carried from the §9 family STANDS-UNCONSUMED.

---

## Project counts at this commit

- **active differential fixtures:** **52** (tail `0050-mongo-boot-reject`; +`0049-mongo-requests` + `0050-mongo-boot-reject` at 29.1 IMPL). Canonical recipe `ls -d test/fixtures/[0-9]* | wc -l` = 52. Anticipated at phase-29 phase-done: **54** (`0051` at 29.2; `0052` at 29.3; NO access-log dir per AMEND-B10).
- **fuzzers:** **39** (canonical recipe `grep -rh "^func Fuzz" $(find ./internal -name fuzz_test.go) | wc -l`; +`FuzzMongoDecode` at 29.1 IMPL). Anticipated at phase-29 phase-done: **39** (40 only if D-P6 resolves to a separate response fuzzer).
- **stat surface:** **360** (BEHAVIOR_CONTRACT doc count; +23 mongo fixed roster — 22 counters + the `op_query_active` gauge — created eagerly per 29.1 SPEC §7.2 / D-P1). The response/fault increments at 29.2/29.3 add +0 creation.
- **DECISIONS.md tail:** **ADR-0226** (next-free **ADR-0227**) — unchanged at this commit (the ADR-0224 §Decision/§Consequences body landed in-place; no new number).
- **conformance:** h2spec 53/53; proxy-wasm all 10 families PASS (as of the 29.1 six-gate, 2026-06-04).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
