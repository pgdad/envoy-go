# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `08.1-admin-endpoints`
- **phase-directory:** `docs/envoy-go/phases/08.1-admin-endpoints/` — created by THIS commit on branch `phase-08.1-admin-endpoints-spec`; contains `SPEC.md` (the full sub-phase SPEC; lifecycle-state 1 → 2 deliverable). Sibling directories `docs/envoy-go/phases/08-admin-api-and-drain/` (parent master SPEC `SPEC.md` + the lifecycle-state-1 `BRAINSTORM.md` from master `29c9497`) and `docs/envoy-go/phases/08.2-graceful-drain/` (sibling SPEC stub `README.md`) also created by THIS commit. Phase 07 (parent + sub-phases 07.1 + 07.2), phases 04, 05 (+ 05.1, 05.2), and 06 (+ 06.1, 06.2) remain closed read-only history.
- **lifecycle-state:** `2` — phase 08.1 SPEC drafting session complete; lifecycle-state 1 → 2 transition lands. The SPEC distills BRAINSTORM §§2–10 into formal contract language and **executes the seven BRAINSTORM §2.7 empirical-pin obligations IN-SESSION** against reference Envoy v1.37.2 (`docker run envoyproxy/envoy:v1.37.2 -c <fixture>`) per ADR-0004's hard-gate discipline. Resolved: (1) `/config_dump` JSON MarshalOptions = `{Multiline: true, Indent: " ", UseProtoNames: true, EmitUnpopulated: true}` — pinned by §11.1 verbatim scrape (1-space indent, snake_case, emit zero-valued); top-level body shape `{"configs": [<Any>, <Any>, <Any>]}` with three sub-envelopes (Bootstrap, Listeners, Clusters) in this exact order; (2) `/clusters` text format = 10 cluster-level lines (`observability_name`, 4×`default_priority::*` circuit-breaker constants, 4×`high_priority::*` circuit-breaker constants, `added_via_api::false`) + 18 per-endpoint lines (`cx_active`, `cx_connect_fail`, `cx_total`, `rq_active`, `rq_error`, `rq_success`, `rq_timeout`, `rq_total`, `hostname::`, `health_flags::healthy`, `weight::1`, `region::`, `zone::`, `sub_zone::`, `canary::false`, `priority::0`, `success_rate::-1`, `local_origin_success_rate::-1`); envoy-go MVP emits Envoy's full unconditional line set with default constants for non-modeled fields (circuit breakers, AHC, locality, success_rate, weight); (3) `/listeners` text format = exactly one line per listener `<name>::<bind_addr>`, alphabetical-by-name; (4) `/server_info` JSON shape = `{version, state, uptime_current_epoch, uptime_all_epochs, hot_restart_version, command_line_options{...}, node{...}}`; `state` enum string `"LIVE"` post-MarkReady; `uptime_*` rendered as `durationpb` strings (`"1s"`, `"30s"` — protojson default); upstream Envoy auto-populates `node.user_agent_name`, `node.user_agent_build_version`, `node.extensions[]` (~3800 lines) and emits ~40 `command_line_options` fields + `hot_restart_version: "11.104"` — envoy-go-go emits empty `node` (from bootstrap), partial `command_line_options{config_path: <path>}`, `hot_restart_version: "disabled"` — all per-field allow-listed; (5) framing = `transfer-encoding: chunked` upstream on ALL six admin endpoints (envoy-go emits `Content-Length`; documented as a structural framing deviation extending phase-01's `/ready` precedent — differential harness's existing dechunk preprocessor covers all six); (6) header set = lowercase 6-header set `{content-type, cache-control: no-cache, max-age=0, x-content-type-options: nosniff, date, server: envoy, transfer-encoding: chunked}` consistent across all four new endpoints + `/ready` + `/stats/prometheus`; (7) pre-MarkReady `state` value = unobservable for static-resources bootstraps in v1.37.2 (Envoy log shows `cm init: all clusters initialized` followed by `starting workers` followed by `starting main dispatch loop` within ~3ms of admin server bind; even a `STRICT_DNS` cluster with unresolvable hostname does NOT block init); envoy-go-go emits `state: "PRE_INITIALIZING"` pre-MarkReady (mathematically complete, mirrors ADR-0015's documented-but-test-irrelevant precedent for `/ready`); `INITIALIZING` enum value not modeled (no xDS init phase that survives admin-bind in upstream Envoy either). Edge-case sub-pin findings: (8) Envoy does NOT enforce method discrimination — POST/PUT/DELETE on read-only endpoints return 200 with the same body as GET (envoy-go matches Envoy parity; no method check); (9) trailing slash (`/config_dump/`, etc.) returns 404 in upstream Envoy with the admin help page body — envoy-go inherits Go stdlib `http.ServeMux`'s `404 page not found` body (status code matches; body diverges; allow-listed for trailing-slash behavior); (10) `?format=json` query-param on `/clusters` and `/listeners` returns Envoy's JSON-form responses — out of MVP per ADR-0089 deferral list. Seven new ADRs anticipated (ADR-0084..ADR-0090) per SPEC §8: planner-time-split application, admin-mux reuse, `/config_dump` shape, `/clusters` + `/listeners` shape, `/server_info` shape + state-enum coverage, deferral list, no-ACL security posture. ROADMAP.md updated: row `08` → `in-progress` with sub-phases `08.1, 08.2`; row `08.1` (depends-on `07`) → `in-progress`; row `08.2` (depends-on `08.1`) → `planned`. Per BOOTSTRAP §5 the next transition is state 2 → 3 via `superpowers:writing-plans` (PLAN.md authoring for 08.1).
- **next-skill:** `superpowers:writing-plans` — PLAN.md authoring for phase 08.1. Inputs: this commit's `docs/envoy-go/phases/08.1-admin-endpoints/SPEC.md` (authoritative contract — the planner translates §4 deliverables into bite-sized TDD tasks per the writing-plans skill template), this commit's `docs/envoy-go/phases/08-admin-api-and-drain/SPEC.md` (parent master SPEC — the cross-cutting discipline), `docs/envoy-go/phases/08-admin-api-and-drain/BRAINSTORM.md` (background design context the SPEC distills), `BOOTSTRAP_PROMPT.md` §§5–7 (lifecycle + phase-done gates), `SKILL_ROUTING.md`, the closed-history `docs/envoy-go/phases/07.1-http-filter-framework/PLAN.md` and `docs/envoy-go/phases/07.2-listener-chain-completion/PLAN.md` as structural precedent (task-numbering conventions, TDD step layout, PROGRESS-preamble ADR-anchor convention), the existing `internal/admin/admin.go` + `internal/admin/admin_test.go` + `internal/admin/prometheus.go` code (the constructor + mux + `*stats.Registry` threading patterns the PLAN extends), and the existing `internal/cluster/manager.go` + `internal/listener/manager.go` (the snapshot-accessor patterns the PLAN extends).
- **next-skill-scope:** Lifecycle-state 2 → 3 PLAN drafting session deliverables: (1) `docs/envoy-go/phases/08.1-admin-endpoints/PLAN.md` — full implementation plan derived from this commit's SPEC.md §§4–7 deliverables + §11 empirical pins. The PLAN should structure tasks roughly: T1 PROGRESS preamble (ADR-0084 + ADR-0085 land); T2 widen `internal/admin.New` constructor (ADR-0085); T3 `internal/cluster.Manager.Clusters()` snapshot accessor + tests; T4 `internal/admin/headers.go` + `internal/admin/version.go` shared helpers; T5 `internal/admin/configdump.go` + tests (ADR-0086); T6 `internal/admin/clusters.go` + tests (ADR-0087); T7 `internal/admin/listeners.go` + tests (ADR-0087); T8 `internal/admin/serverinfo.go` + tests (ADR-0088); T9 `cmd/envoy-go/main.go` admin.New call-site update; T10 mux registration in `admin.Server.Start()`; T11 concurrent-scrape race test; T12 fuzzer `FuzzConfigDumpFormat` (ADR-0018 30s budget); T13 differential fixture `0009-admin-config-dump` (envoy.yaml + envoy-go.yaml + driver + backends + expectations.yaml + runner.go registration); T14 `BEHAVIOR_CONTRACT.md` umbrella restructure + four new equivalence-matrix rows (ADR-0089 + ADR-0090 land in PROGRESS preamble at this task per ADR-0052); T15 six-gate verification sweep + REVIEW.md authoring + phase-done commit. **Anticipated PLAN size: ~15 tasks + ~600–800 LoC production + ~250 LoC fixture/driver — well within ADR-0045 thresholds (~25 tasks, ~1500 LoC), confirming the planner-time split decision.** Settle SPEC §12 deferred decisions at PLAN time (BuildVersionString format, http.Server.WriteTimeout widening to 30s, server.live gauge interaction, pre-MarkReady scrape gating, etc.). After PLAN, advance STATE to lifecycle-state 3 with `next-skill: superpowers:executing-plans` (or `superpowers:subagent-driven-development` per the `Prefer subagent-driven execution` user-memory preference) and `active-phase` remains `08.1-admin-endpoints`. Worktree for the next session: branch from this SPEC-commit's master fast-forward HEAD per ADR-0003 + per-phase-worktree convention (`.worktrees/phase-08.1-admin-endpoints-plan`).
- **last-commit:** `TBD` — `phase 08.1: SPEC + parent 08 SPEC + 08.2 sibling stub [empirical-pin block resolved against ENVOY_TARGET v1.37.2]`, on branch `phase-08.1-admin-endpoints-spec`. Lands (a) `docs/envoy-go/phases/08-admin-api-and-drain/SPEC.md` — parent master SPEC mirroring 06 + 07 parent SPECs, ~6 sections + metadata block; (b) `docs/envoy-go/phases/08.1-admin-endpoints/SPEC.md` — full sub-phase SPEC, 16 sections + metadata block, including the §11 verbatim empirical-pin block resolved IN-SESSION against `envoyproxy/envoy:v1.37.2` (`sha256:c5e8a68e52f4d4697a9adb280dbe415d77fedf1257e183dcb86205bd438f18bd`; server-build SHA `5afe27fb338b16d5bb06b3a7198bcd581b4e3dee` confirmed via `/server_info`'s `version` field); (c) `docs/envoy-go/phases/08.2-graceful-drain/README.md` — sibling SPEC stub mirroring 06.2 + 07.2 stubs; (d) ROADMAP.md update — row `08` flips `planned → in-progress` with `sub-phases: 08.1, 08.2`; new row `08.1` (depends-on `07`) `in-progress`; new row `08.2` (depends-on `08.1`) `planned`; (e) STATE.md rewrite advancing to lifecycle-state 2 with `next-skill: superpowers:writing-plans`. Bundles parent SPEC + sub-phase SPEC + sibling stub + ROADMAP + STATE in one commit per the phase-06.1 `bundling` precedent (cleaner than splitting into three commits). No code changes in 08.1 SPEC commit (per BOOTSTRAP §5 lifecycle state 2 — SPEC drafting only); code lands at lifecycle-state 3. SHA filled in a follow-up commit per the phase-04..07.2 SHA-fill convention.
- **last-updated:** 2026-05-02

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
