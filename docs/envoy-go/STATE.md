# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `24.2-global-ratelimit-perroute-and-headers` — **24.2 PLAN DONE 2026-05-23 (this commit); awaiting the 24.2 IMPL** (next session, skill `superpowers:subagent-driven-development` per project memory `feedback_execution_style.md` to execute `docs/envoy-go/phases/24.2-global-ratelimit-perroute-and-headers/PLAN.md` over fresh `general-purpose` subagents — one per Task with a two-stage review between Tasks per `superpowers:subagent-driven-development`). 24.2 is the SECOND (and final) sub-phase of the phase-24 PLAN-time ADR-0045 split per ADR-0201. **24.1 IMPL DONE 2026-05-23** (squash-merge `a4fdc75`; SHA-fill `dbd2d3b`) shipped the **core decision path** of `envoy.filters.http.ratelimit` (the package skeleton + DELTA-1 `RateLimitClient` + DELTA-2 HCM route-table exposure + 5 CORE descriptor actions + OK/OVER_LIMIT/error dispositions + cluster-scoped 4-counter stats `cluster.<rls>.ratelimit[.<stat_prefix>].{ok,error,over_limit,failure_mode_allowed}` 110→114 + FULL §5 PARSE-REJECT roster (15→18 departures) + 33rd fuzzer + fixtures 33→35 via `0032`(a/b/c/d-core/e/h) + `0033`(boot-reject `domain`-empty) + 3 ADR landings ADR-0197[core] + ADR-0198 + ADR-0200 + **D-RL1 RAW-PROTO SEED CONFIRMED** + **19 HTTP filters wired**). **24.2 PLAN at this commit:** `docs/envoy-go/phases/24.2-global-ratelimit-perroute-and-headers/PLAN.md` authored from the 24.2 SPEC.md + the parent master SPEC, plan-document-reviewer-APPROVED on the first iteration. **9 tasks** (Pre-Task 0 + Tasks 1-8); 455 PLAN.md LoC; well below both the 25-task split-gate + the ~1500 PLAN-LoC soft-gate. ADR-0045 split-gate re-verification at this PLAN session: 24.2 anticipated production LoC ~480-820 (descriptors.go extension + encode.go + headers.go + compiled_perroute.go + dispositions.go/decode_headers.go/HCM updates) — well under the ~1500 trigger — confirms single sub-phase landing (no further nested split per ADR-0106). **9-task decomposition:** Pre-Task 0 (PROGRESS preamble + 12 preconditions); Task 1 (`descriptors.go` — 5 remaining actions `source_cluster`/`masked_remote_address`/`metadata`/`query_parameters`/`query_parameter_value_match` + AMEND-11 key defaults `query_param` SINGULAR + `metadata` accessor; ADR-0202 escape-valve target #1 wired to the `streamInfo().DynamicMetadata()` vs ROUTE_ENTRY accessor surface per D-RL8); Task 2 (`stage` multi-stage bucketing per parent §4.4); Task 3 (`compiled_perroute.go` `RateLimitPerRoute` 10th-canonical TPFC compile + **ADR-0199 FULL §Decision + §Consequences body** + **ADR-0125 §(xv) AMENDMENT 9 → 10 paragraph anchored in ADR-0199**); Task 4 (Axis-B `vh_rate_limits` cross-tier composition table + legacy `include_vh_rate_limits` force-include per parent §4.3 + AMEND-5); Task 5 (`encode.go` + `headers.go` — X-RateLimit DRAFT_VERSION_03 emission on all dispositions when enabled + MIN-status across multi-descriptor + `;w=`/`;name=` quota suffix + unit→seconds; **ADR-0197 IN-PLACE §Decision amendment** for the X-RateLimit + remaining-actions slice per ADR-0052; ADR-0202 escape-valve target #2 wired to the D-RL9 X-RateLimit byte-edge `headers_test.go` byte-pin against captured upstream headers); Task 6 (differential fixture EXTENSIONS to the EXISTING `0032-http-ratelimit/` — scenarios (f) `vh_inclusion` cross-side + (g) `x_ratelimit_headers` cross-side + scenario (d) `descriptor_actions` EXTENDED to all 10 actions; NO new fixture dir; the fake `RateLimitService` script extension at `test/helpers/ratelimitgrpc/` adds per-descriptor `statuses[]` for scenario g); Task 7 (fuzzer corpus extension — `FuzzRateLimitConfigParse` gets ~10-20 new seeds covering the remaining actions + `RateLimitPerRoute` + `stage` boundaries + legacy `include_vh_rate_limits`; NO new fuzzer); Task 8 (atomic landing — BEHAVIOR_CONTRACT completion bundle per D-RL17 + STATE re-advance + **ROADMAP row 24.2 `planned → done` AND parent row 24 `in-progress → done` in the SAME commit per the 18/19/22 ROLLUP precedent (D-RL18)** + REVIEW.md + verification of the FULL parent §15 acceptance UNION closing 24.1 partial items 7/9/15/16 + new items 12/14/17/18). PLAN hypothesis: **ADR-0202 stays UNCONSUMED at 24.2 phase-done** (HOLD-with-known-risk); fires only if Task 1's `metadata`-accessor surface OR Task 5's X-RateLimit byte-edge force a divergence. **Counts at 24.2 phase-done (when it lands):** fixture dirs stay 35 (Task 6 extends `0032` in-place); fuzzers stay 33 (Task 7 corpus extension only); HTTP filters stay 19; stats stay 114 (per-route `domain` is descriptor-tier per AMEND-1); BEHAVIOR_CONTRACT departures stay 18 (the 24.1 `disable_key`/`extension`/`dynamic_metadata` cover the surface; `override_option` accepted-but-INERT is upstream-parity, not a departure); canonical-per-route roster grows **9 → 10** (the FIRST roster growth since phase-22.3 / ADR-0193, ending the phase-23 + phase-24.1 REUSE-by-absence skip streak). Phase 23 (`admission_control`) DONE and CANONICAL — DO NOT reopen.
- **phase-directory:** `docs/envoy-go/phases/24.1-global-ratelimit-core-and-route-table/` (SPEC.md + PLAN.md + PROGRESS.md + REVIEW.md ALL landed). Sibling `docs/envoy-go/phases/24.2-global-ratelimit-perroute-and-headers/` (SPEC.md + **PLAN.md ALL landed at this commit**; PROGRESS.md + REVIEW.md land at 24.2 IMPL Pre-Task 0 + Task 8 respectively — next-skill `superpowers:subagent-driven-development`). Parent `docs/envoy-go/phases/24-http-filter-global-ratelimit/` (BRAINSTORM.md + the RETAINED parent master SPEC.md, annotated with the split-fired note). Phases 23 + 24.1 directories are complete; 24.2 directory has SPEC + PLAN at this commit.
- **lifecycle-state:** `phase 24.2 PLAN done + plan-document-reviewer-APPROVED; awaiting 24.2 IMPL` (SKILL_ROUTING state 3 — PLAN.md exists; implementation incomplete). ROADMAP row `24` STAYS the PARENT (`in-progress`, `sub-phases = 24.1, 24.2`); row `24.1` STAYS `done` (per 24.1 IMPL squash); row `24.2` STAYS `planned` with the **PLAN-authored + plan-document-reviewer-APPROVED 2026-05-23 per-cell annotation appended at this commit** (the row flips `planned → done` at 24.2 IMPL phase-done together with the parent row 24 rollup per D-RL18).
- **next-skill:** `superpowers:subagent-driven-development` (to execute the 9-task `docs/envoy-go/phases/24.2-global-ratelimit-perroute-and-headers/PLAN.md` over fresh `general-purpose` subagents — one per Task with two-stage review between Tasks per the skill + project memory `feedback_execution_style.md`). The IMPL session: creates a fresh worktree `phase-24.2-global-ratelimit-perroute-and-headers-impl` branched off this PLAN-stage-close master tip; runs Pre-Task 0 (PROGRESS preamble + 12 preconditions verifying the post-24.1 baseline intact); dispatches Tasks 1 → 2 → 3 → 4 → 5 → 6 → 7 → 8 in order (Tasks 1 + 3 are file-disjoint and CAN parallelize but D-P2 runs sequential for two-stage review); records D-RL8 + D-RL9 byte-confirmation outcomes at Tasks 1 + 5; lands the 6 phase-done gates GREEN at Task 8 (35/35 differential incl. extended `0032`; 33 fuzzers with extended `FuzzRateLimitConfigParse` corpus; 53/53 h2spec at ADR-0051 v1.32.4 pin; clean go build/vet/lint/race); squash-merges the IMPL branch to master per the phase-09..23 IMPL-stage-close pattern + pushes to origin per `feedback_push_to_origin.md`.
- **last-commit:** `TBD-24.2-PLAN-SQUASH` (squash-merge of branch `phase-24.2-global-ratelimit-perroute-and-headers-plan` from worktree `.worktrees/phase-24.2-global-ratelimit-perroute-and-headers-plan/` — branch commits collapsed into one squash: 24.2 PLAN.md authoring + STATE.md re-advance + ROADMAP.md row 24.2 per-cell PLAN-done annotation + next-prompt.txt rewrite for 24.2 IMPL cold-start). Filled in by the post-`wt-merge` STATE.md SHA-fill follow-up commit (the phase-09..23 + 24.1 PLAN stage-close precedent — `64078a3` was the 24.1 analogue). Predecessor master tip: `1d3293b` — `next-prompt.txt: repoint master-tip references to 349b7a1 (actual HEAD)`.
- **last-updated:** 2026-05-23
- **next-free ADR:** `ADR-0202` (UNCONSUMED at 24.2 PLAN session; reserves as the conditional escape-valve at 24.2 IMPL Task 1 D-RL8 metadata-accessor surface OR Task 5 D-RL9 X-RateLimit byte-edge per the PLAN's `## ADRs introduced/landed by this plan` block). DECISIONS.md tail at `ADR-0201` (UNCHANGED — the 24.2 PLAN session does not anchor new §Context drafts; ADR-0199's §Context stays anchored at the parent SPEC commit; the 24.2 IMPL Task 3 lands ADR-0199 §Decision + §Consequences FULL body + the ADR-0125 §(xv) AMENDMENT 9 → 10 paragraph anchored in ADR-0199; the 24.2 IMPL Task 5 lands the ADR-0197 IN-PLACE §Decision amendment for the X-RateLimit + remaining-actions slice per ADR-0052). **D-hypothesis (unchanged at PLAN-stage-close):** `ADR-0202` stays UNCONSUMED at phase-24 phase-done — HOLD-with-known-risk; the conditional escape-valve anchor passes through to 24.2 IMPL Tasks 1 + 5.

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
