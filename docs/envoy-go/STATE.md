# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `06.1-stats-prometheus`
- **phase-directory:** `docs/envoy-go/phases/06.1-stats-prometheus/` — contains `SPEC.md` (at `ad46930`, frozen), `PLAN.md` (at `343b198`), `PROGRESS.md` (full Task-1-through-Task-15 history + L4 FAIL block at `1f94b74` + gate-(d) fix block at `79be6b0` + L4 PASSED re-verification block at `1ed6cd0` + L4 REVIEW-followup re-verification block written by THIS commit), and `REVIEW.md` (`59d86f2` — verdict APPROVED WITH FOLLOW-UPS, 0 Critical / 2 Important / 12 Minor; SHA-fill at `d21d50b`). Parent `docs/envoy-go/phases/06-observability-baseline/` carries `BRAINSTORM.md` (`d271fd7`) + parent `SPEC.md` (`ad46930`). Sibling `docs/envoy-go/phases/06.2-access-log/` retains the placeholder `README.md` (`ad46930`). Phase 05 closed history unchanged.
- **lifecycle-state:** `4` — REVIEW-followup batch complete; M-1 closed at `caa58e5` (boundary guard at `buildCluster` + 6-case `TestNewManager_ClusterNameInvalidChars` regression test mirroring `TestParseFilter_StatPrefixInvalidChars` at `internal/filter/hcm/config_test.go:221`; inherits ADR-0065 by reference per Consequences (d) — no new ADR) and `665c879` (golangci-lint misspell fixup `minimised → minimized` on the test docstring). REVIEW Importants I-1 (SPEC §14 line 715 file-path mismatch) and I-2 (ROADMAP row 06.1 `planned`) are closed without code/doc changes on this branch (I-1 by REVIEW + PROGRESS Task 11 deviation #1 as-corrigendum; I-2 by the next-session phase-done commit's natural `planned → done` row flip). Gates (b)/(c)/(d)/(e) re-run GREEN at HEAD `665c879` per the L4 REVIEW-followup re-verification block written by THIS commit (gate (a) implied by gate (b)'s six-fixture sweep including `0005-prometheus-stats`; gate (f) closed by REVIEW.md at `59d86f2`). Boundary greps clean: ADR-0046 (4 hits in 4 of 5 allowed files; `hpack.go` legitimately omits per ADR-0054), ADR-0048 (`client.go` present), ADR-0059 (`internal/stats` stdlib-only, no third-party imports). Per BOOTSTRAP §5 the next transition is state 4 → 5 via a STATE-only commit confirming the verification verdict, mirroring 05.2 `5c0f3cc`.
- **next-skill:** *(no skill — pure STATE transition commit ahead.)* The next commit is a STATE.md-only edit promoting `lifecycle-state: 4` → `5` mirroring 05.2 `5c0f3cc` (no code, test, or doc changes; no ADR; verification verdict already recorded in PROGRESS.md by THIS commit). After STATE → 5 lands, the lifecycle-state-6 phase-done commit lands in the next session with the BOOTSTRAP §5.3 commit-message format `phase 06.1: stats-prometheus [ADR-0059, ADR-0060, ADR-0061, ADR-0062, ADR-0063, ADR-0064, ADR-0065]` (seven ADRs total — six anticipated + ADR-0065 the gate-(d) fix bonus); the phase-done commit body MUST cite REVIEW.md's verdict APPROVED WITH FOLLOW-UPS, explicitly note the I-1 PROGRESS-as-corrigendum acknowledgment (M-9 fix at `h2dispatch.go` not `router_action.go`), and document the I-2 one-step `planned → done` row flip resolution + the 12-Minor carry-forward to the L4 review-followup batch (separate post-phase-done branch). STATE then advances to lifecycle-state 0 between-phases with `active-phase: 06.2-access-log`, `next-skill: superpowers:brainstorming`.
- **next-skill-scope:** STATE → 5 commit deliverable (no skill invocation): edit `docs/envoy-go/STATE.md` to set `lifecycle-state: 5`, update prose to reflect "verification re-run green, ready for phase-done commit at lifecycle-state 6", update `next-skill` to *(none — phase-done commit is the next session's work)* or to the conventional placeholder cited in the 05.2 `5c0f3cc` precedent, update `last-commit` to TBD-then-SHA-fill per the phase-02..05.2 convention, update `last-updated`. Per ADR-0003 the `phase/06.1-stats-prometheus-review-followup` branch fast-forwards into `master` at the next-session phase-done commit (or a merge commit, planner's choice — the 05.2 `0c01ed6` precedent used a fast-forward). ROADMAP edits land at the phase-done commit (`row 06.1: planned → done` direct one-step flip per REVIEW I-2 disposition + 05.2 `0c01ed6` precedent; row 06 stays `in-progress` until 06.2's phase-done; row 06.2 stays `planned`).
- **last-commit:** `TBD` — `phase 06.1 review-followup: gates (b)/(c)/(d)/(e) re-run green — PROGRESS verification-block addendum + STATE → lifecycle-state 4`, on branch `phase/06.1-stats-prometheus-review-followup`. Re-verifies SPEC §3 phase-done gates (b)/(c)/(d)/(e) at HEAD `665c879` (end of the 2-commit M-1 closure batch — `caa58e5` + `665c879`). Gate (a) is implied by gate (b) since fixture `0005-prometheus-stats` is one of the six fixtures swept and PASSES; gate (f) was satisfied by REVIEW.md at `59d86f2`. Adds the L4 REVIEW-followup re-verification block at the tail of `docs/envoy-go/phases/06.1-stats-prometheus/PROGRESS.md` (verbatim outputs of every gate command run from this followup-branch tip) and updates STATE → lifecycle-state 4 with a description of all closure dispositions. No production code, test, or fixture file changed; this is a pure verification + state-transition commit per the 05.2 `f774d4f` precedent. ROADMAP unchanged (row 06.1 stays `planned` until the phase-done commit; the next-session phase-done commit will flip directly to `done` per the 05.2 one-step precedent). SHA-fill follow-up per the phase-02..05.2 convention.
- **last-updated:** 2026-04-28

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
