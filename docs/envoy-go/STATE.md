# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `<next-§9-family-row>` — phase 15 complete as of this commit (lifecycle-state-5 phase-done; awaiting next planning). Next phase is the next §9 HTTP filters family-row (remaining unbrainstormed members: jwt_authn, rbac, ext_authz, ext_proc, oauth2, lua, wasm, adaptive concurrency, admission control, global rate limit, …); determined by the next brainstorm session consulting the §9 heading + phase 15 just-shipped artefacts per ADR-0106(e). Phase 15 shipped `envoy.filters.http.bandwidth_limit` — EIGHTH §9 family-row (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11, csrf @ 12, buffer @ 13, compressor @ 14); 5 ADRs (ADR-0135..ADR-0139) + ADR-0125 §(xi) amendment; 18 differential fixtures (0000-0017) + 19 fuzzers green; 6 gates all pass.
- **phase-directory:** `docs/envoy-go/phases/15-http-filter-bandwidth-limit/` — COMPLETE through Task 16. Contains BRAINSTORM.md + SPEC.md + PLAN.md + PROGRESS.md (Tasks 1-16 documented) + REVIEW.md (208 LoC; all 12 SPEC §15 acceptance claims verified — 10 PASS + 2 PARTIAL documented in BEHAVIOR_CONTRACT §13.4). Lifecycle-state-6 reached.
- **lifecycle-state:** `phase 15 done; awaiting next planning` — phase 15 BRAINSTORM.md authored (squash-merged at `fa125f2`; SHA-fill at `e7a26ef`), SPEC.md authored (squash-merged at `49e0361`; SHA-fill at `cd45af0`), PLAN.md authored (squash-merged at `a5c5ec9`; SHA-fill at `36c91c9`), implementation Tasks 1-16 landed in-worktree, squash-merged to master at `c1361d3` (this commit's SHA-fill predecessor). All 6 phase-done gates green (Gate A build/vet/lint clean; Gate B race-test all packages PASS; Gate C h2spec 53/53 PASS at ADR-0051 pin; Gate D 19 fuzzers green at 30s budget each; Gate E 18 differential fixtures 0000-0017 PASS; Gate F BEHAVIOR_CONTRACT.md populated per SPEC §13.1-§13.5 — bandwidth_limit subsection inserted AFTER `### envoy.filters.http.compressor` per planner-time decision 16 + 14 active stat-table rows + 2 deferred-histogram allow-list rows + equivalence-matrix row 0017 + Phase 15 forward-pointer notes subsection + Timing tolerances ±70ms per-side entry + Twin-series filter discipline phase-15 histogram extension). ROADMAP row 15 flipped `in-progress → done` at this commit with date `2026-05-11`. ADR roster: ADR-0135 + ADR-0136 (Task 2 — package + types + factory + compiledConfig + 14-stat `filterStats`), ADR-0137 (Task 3 — kbps-per-tick `throttleDuration` + Path B-async + wire-shape divergence + non-endStream DataContinue HCM-deadlock-avoidance fix per Task-14 finding (d)), ADR-0139 (Task 7 — per-route INDEPENDENT-stats wiring + `newFilterStatsIfAbsent` post-Freeze idempotent registration), ADR-0138 (Task 8 — 14-stat surface + namespace + Prometheus inline-prefix + histogram twin-series allow-list). Plus ADR-0125 §(xi) amendment paragraph (landed at SPEC commit) documenting the NEW 6th canonical per-route pattern (bare-message-via-TPFC + code-level-required-`limit_kbps`-at-per-route). **Task-14 empirical findings flowed into BEHAVIOR_CONTRACT §13.4 + §13.5:** (a) cross-side wall-clock divergence > ±70ms for initial-burst-capacity bodies refuting SPEC §11.P9(c); per-side discipline adopted; (b) `*_enabled` + `*_enforced` `counterModePerSideExact` mode (initial-burst-discount divergence); (c) 6 gauges not asserted (transient/noisy); (d) non-endStream `DataContinue` HCM synchronous-dispatch deadlock-avoidance fix (mirrors phase-13 buffer precedent).
- **next-skill:** (none — phase 15 complete; next session brainstorms the next §9 family-row per ROADMAP)
- **next-skill-scope:** N/A — phase 15 closed at lifecycle-state-6. Next session opens a fresh brainstorm worktree for the next §9 HTTP filters family-row (remaining unbrainstormed members: jwt_authn, rbac, ext_authz, ext_proc, oauth2, lua, wasm, adaptive concurrency, admission control, global rate limit, …) per ADR-0106(e) consulting the §9 heading + phase 15 just-shipped artefacts.
- **last-commit:** `c1361d3` — `Squash merge phase-15-http-filter-bandwidth-limit-impl`. Squashed 36 worktree commits across 16 PLAN tasks (16 task-landing + 16 PROGRESS-SHA-fill follow-ups + 4 review/fix-pass commits). Lands the phase-15 production code (~645 LoC bandwidthlimit.go + ~63 LoC bucket.go + ~85 LoC doc.go + ~2052 LoC test + ~144 LoC fuzz_test), differential fixture 0017 (driver.go + envoy.yaml + envoy-go.yaml + expectations.yaml + README.md), boot registration (cmd/envoy-go/main.go +2 LoC; fixture.go +17 LoC; runner_test.go +19 LoC), 5 ADRs (ADR-0135..ADR-0139), BEHAVIOR_CONTRACT 5-edit bundle (§13.1 + §13.2 + §13.3 + §13.4 + §13.5 + §242 twin-series extension), ROADMAP row 15 done, STATE.md advance, PROGRESS.md (Tasks 1-16), REVIEW.md (208 LoC; 10 PASS + 2 PARTIAL claims). Plus 2 missing-pair helper additions documented as ZERO-framework-delta-preserving: `internal/stats/registry.go` `NewGaugeIfAbsent` (Task 7; +33 LoC) and `internal/stats/name.go` bandwidth_limit inline-prefix detection (Task 8; +40 LoC; mirrors phase-11 SN9). Total +6764/-10 across 23 files. 6 phase-done gates green at squash predecessor `4048ee4`.
- **last-updated:** 2026-05-12
- **next-free ADR:** `ADR-0140` (phase-15 ADR roster ended at ADR-0139 inclusive; advances by 1 for the next phase). The phase-15 ADR roster stayed at the SPEC-time-anticipated 5-ADR count (ADR-0135..ADR-0139); NO impl-time-added ADRs via ADR-0044 escape valve (unlike phase 14 which added ADR-0134 at Task-14 follow-up). Plus ADR-0125 §(xi) in-place amendment paragraph (landed at SPEC commit; does not advance the ADR counter).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
