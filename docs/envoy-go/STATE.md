# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `04-http-1.1`
- **phase-directory:** `docs/envoy-go/phases/04-http-1.1/` — exists; contains `SPEC.md` (drafted in commit `59e6f25`, reviewer-approved per ADR-0004) and `PLAN.md` (drafted in commit `82a3cf6` and approved by the plan-document-reviewer subagent in the same session per ADR-0005; verdict APPROVED with three advisory recommendations and zero blocking issues; coverage cross-check: SPEC §3 gates a/b/c/d/e/f all addressed; SPEC §4 deliverables all mapped to tasks; SPEC §10 14 deferred decisions all settled in §"Settled SPEC §10 deferred decisions"; SPEC §11 risks all mitigated; SPEC §12 phase-03 REVIEW carryovers all triaged; spec-review advisory items (i)-(iv) all addressed). `PROGRESS.md`, `REVIEW.md` do not yet exist. Phase 03's directory at `docs/envoy-go/phases/03-tls/` is closed and read-only history.
- **lifecycle-state:** `3` — `SPEC.md` and `PLAN.md` exist, reviewer-approved. Per `BOOTSTRAP_PROMPT.md` §5 state 3, the next session invokes the executor skill (`superpowers:subagent-driven-development` per user's standing preference + ADR-0005 default). Per the §5 state-2 GATE (re-checked at state-3 entry): PLAN.md has 17 tasks (well below ~25 split-gate threshold) and ~2400 LoC estimate (above ~1500 LoC literal threshold, but the §"Scope check" section in PLAN.md argues single-phase shipping per atomic-claim cohesion + no clean half-fixture seam + mid-execution split valve preserved + HTTPS sub-phase deferral is not a current-phase split — same shape phase 03 used). PLAN.md introduces eight ADRs (ADR-0037..ADR-0044) mapped to first-use commit landing tasks per phase-02/03 precedent. ADR tail verified at PLAN-write time as ADR-0036 (`docs/envoy-go/DECISIONS.md:1146`). ROADMAP row 04 unchanged (`in-progress`; advances to `done` at state 6 per ADR-0005).
- **next-skill:** `superpowers:subagent-driven-development`
- **next-skill-scope:** Execute `docs/envoy-go/phases/04-http-1.1/PLAN.md` task-by-task per ADR-0005 (autonomous-planning adaptation defaults the executor to subagent-driven execution + the user's standing preference for subagent-driven execution over inline). Per the PLAN.md §"Execution preconditions" block, the next session must run from a **fresh worktree on `phase/04-http-1.1-impl` cut off `master`** (NOT `phase/04-http-1.1-plan` and NOT `phase/04-http-1.1-spec`). Recommended worktree path: `.worktrees/phase-04-http-1.1-impl`. STATE.md's `last-commit` at cold-start must equal master = `phase/04-http-1.1-plan` HEAD = `82a3cf6` (this PLAN-drafting commit) plus this STATE-update commit (fast-forwarded into master in the same session per ADR-0003). The session executes Tasks 1..17 in order, each landing as one atomic commit (with PROGRESS SHA-fill follow-up commits per phase-02/03 precedent), checking each task's acceptance gate before proceeding. Per BOOTSTRAP §5 state-2 GATE re-check at execution time: if any single task's sub-steps blow up past ~10–15 items once contact with reality reveals complexity (likely candidates: Task 7 `config.go` typed_config parse with three landing ADRs, Task 15 fixture 0003 end-to-end integration), the executor splits per §6.2 with a deviation ADR. Plan-time advisory recommendations from the plan-document-reviewer subagent (advisory only, do not block but worth quoting for the executor's awareness): (1) Task 5 ADR-0039's local-reply status mapping (503 on `Cluster.Dial` err, 502 on `req.Write`/`http.ReadResponse` err) deserves a one-line BEHAVIOR_CONTRACT cross-reference in the ADR-0044 entry under "Asserted equivalence" so a future REVIEWer sees the mapping is intentional; (2) the SPEC §10 #6 settled-on (b) handcrafted-bufio HTTP echo backend diverges from SPEC's recommended (a) `http.Server` — the rationale (symmetry with phase-02 TCP echo, no `http.Server.Shutdown` plumbing) should be quoted in PROGRESS at Task 14 commit so the REVIEW session does not re-litigate; (3) Task 17 Step 7's four serial fuzz targets (bootstrap, tcpproxy, tls, hcm) at 30s each = ~2 minutes wall time — document the serial expectation in PROGRESS so the executor does not assume a hung test. None are blockers. Work from a fresh worktree per "always use git worktrees" project convention. Master and `phase/04-http-1.1-plan` share HEAD at this state-3 entry commit per ADR-0003 (fast-forward applied in the same session as PLAN.md commit + STATE state-3 commit).
- **last-commit:** 82a3cf6
- **last-updated:** 2026-04-25

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
