# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

> **This file is a POINTER, not an archive** — `BOOTSTRAP_PROMPT.md` §4: *"STATE.md — pointer: active phase, last commit, next action."* It carries the live pointer, the live counts, and a SHORT recent lineage. **Full stage-by-stage history lives in `STATE_HISTORY.md`** (archived at ADR-0288; **nothing was deleted** — the pre-move file is preserved there verbatim, byte-for-byte).
>
> **⚠️ THE RULE ADR-0288 EXISTS TO ENFORCE: there is EXACTLY ONE live `next-skill:`, `lifecycle-state:`, `next-free ADR:`, and count block IN THIS FILE, and they live in §Current pointer directly below.** Before that move, ~330 lines of prepended backlog had buried TWO fossilised "current state" blocks whose `next-skill:` still read *"the 42.2 hedging BRAINSTORM"* and *"the 46.2 SPEC"* while the live phase was **66** — so `grep 'next-skill:' STATE.md` returned **two stale hits and ZERO live ones**, and `grep 'fuzzers:'` returned **48** and **42** when the true count was **55**. **A session may trust a grep of this file again. Keep it that way: when you advance the pointer, EDIT §Current pointer IN PLACE — never prepend a new block above it.**

---

## Current pointer

*(The ONLY live "what next" in this repo. Update IN PLACE at every stage close.)*

- **active-phase:** `phase 81 (stats-name-charset-guards) BRAINSTORM done` (2026-07-31) — lifecycle-state **DONE -> 1**; **ROW 81 REGISTERED `in-progress`** at `ROADMAP.md:143`; sentinel `want` bumped **112 -> 113** in the SAME commit. **SELF-PICKED** per the 2026-07-12 standing directive (smallest defensible candidate first, with **severity as the tie-break**); four costing agents ran in parallel and every cost was re-derived at this tip. ⚠️ **THE HEADLINE: a config-triggered, REQUEST-TIME, un-recovered process crash, PROVED BY EXECUTION at this stage with a firing NC** — `internal/rbac/perpolicy.go:25` lazily registers `<base>.policy.<policyName>.<suffix>` on the first matching request, guarding `policyName` for EMPTINESS ONLY, and `internal/stats/registry.go:115-119` PANICS; an idiomatic `allow-admins` plus `track_per_rule_stats: true` boots GREEN and kills the process on first traffic. The row closes the **EIGHT** unguarded token sources across six packages; the empty-segment (`a..b`) hole is **EXPLICITLY OUT OF SCOPE**, deferred to a named successor. `ROADMAP.md` **228 -> 229 lines / 112 -> 113 data rows**, `STATE_HISTORY.md` **430 -> 432**; `BEHAVIOR_CONTRACT.md` and `DECISIONS.md` **BYTE-UNTOUCHED**. Base master `4f8e159c`, branch `phase-81-brainstorm`.
- **phase-directory:** `docs/envoy-go/phases/81-stats-name-charset-guards/` (`BRAINSTORM.md` + `PROGRESS.md`). Prior: `docs/envoy-go/phases/80-stats-sds-projection/`
- **lifecycle-state:** **`1`** (phase 81 BRAINSTORM done; **row 81 `in-progress`**). ⚠️ **Sentinel re-run MECHANICALLY at this stage with FIRING negative controls: it does NOT fire and `stop` was NOT created** (`ls stop` => `No such file or directory`). (1) at `want=113` prints **`NOT DONE: row 81`** — correct, the row is `in-progress`; NCs all fired: `want=112` => `GATE FAIL: examined 113 data rows, expected 112` (proving the bump was REQUIRED), row 81 doctored to `done` => **SILENT**, and row 62 doctored on that same copy => `NOT DONE: row 62` (so the check still discriminates when silent). (2) **FIVE**, UNCHANGED at `:191 :201 :211 :217 :225` — ⚠️ **THIS ROW NARROWS NOTHING, STATED AND NOT FORECAST; the twenty-eighth consecutive phase at which it did not go down** (a one-arm strip moves **5 -> 4, NOT 5 -> 0**). (3) `NEVER OPENED: gRPC`, `NEVER OPENED: WASM` (NC: invented slug fires; registered `Observability` correctly silent). Input measured **228 lines / 112 data rows / 13** bare `candidates:` hits pre-edit. ⚠️ **THE LEAK CHECK WAS LIVE AND PASSED:** row 81's cell carries **0** deferred-candidate phrases and exactly one family slug, `Observability-family row`, already registered **52x** elsewhere — and the by-mention silencing was **REPRODUCED LIVE** on a doctored copy (injecting `gRPC-family row` made `gRPC` STOP printing while `WASM` kept printing). ⚠️ **Row-81 well-formedness: the DISJUNCTION was required** — over all 113 rows ARM-A flags **57/69 only** and ARM-B flags **78 only**; row 81 is flagged by neither.
- **next-skill:** **a phase-81 SPEC.** ⚠️ **It must dispose D-81-HELPER FIRST** — a shared `internal/stats` helper makes the **full 120-fixture differential AND an explicit h2spec run mandatory** for every later stage of this row (~400-430 s per green attempt, `-race` a SECOND run), while per-package inlining keeps the gate per-package. Also open at the SPEC: D-81-WORDING (eight byte-stable reject messages), D-81-F-SITE (the request-time source cannot return an error to a caller — move the guard to config-compile time, or degrade the emission to a skip), and D-81-SANITIZE (⚠️ **a landed doctrinal contradiction**: phase 80 records sanitizing as *"FORECLOSED by ADR-0065"* while `internal/listener/manager.go:352` `normalizeAddr` **sanitizes**).
- **last-commit:** the phase-81 BRAINSTORM squash (`git log --grep 'phase 81'`; **locate by SUBJECT, never by position** — `reference_next_prompt_tracked_despite_gitignore`). Base master `4f8e159c`.
- **last-updated:** 2026-07-31 (phase-81 BRAINSTORM done). ⚠️ **THE LINEAGE EVICTION RULE WAS HONORED:** §Recent lineage was at its five-entry cap, so **`phase 79 (stats-prometheus-projection) PLAN done`** — re-derived as OLDEST at this tip — was moved **VERBATIM** into `STATE_HISTORY.md` (**430 -> 432**), its absence verified BEFORE the move (0 hits) with a **firing NC** on the phase-79 SPEC sibling (1 hit), and the migrated body confirmed byte-identical afterwards with a firing NC on a doctored body. ⚠️ **The known `phase 77 PLAN done` archive gap was re-checked, CONFIRMED still absent from BOTH files, and NOT widened.**
- **next-free ADR:** **`ADR-0303`** — `DECISIONS.md` tail is **ADR-0302**, STATUS **COMPLETE**; **301** `^## ADR-` headings, ids **0001-0302** with exactly one gap at **ADR-0209** and zero duplicates (*"301 ADRs"* and *"tail ADR-0302"* are both true and must not be conflated). `grep -c '^## ADR-0303'` => **0** (NC: `^## ADR-0302` => **1**). ⚠️ **A BRAINSTORM authors NO ADR** — ADR-0303's §Context lands at the SPEC and its §Decision/§Consequences at the IMPL, per ADR-0044-as-used. `DECISIONS.md` is **BYTE-UNTOUCHED** this stage.

---

## Project counts at this commit

*(Live. Re-verify at session start — `git fetch` first — and update here at every stage close. Canonical commands inline.)*

⚠️ **ALL counts below were re-run MECHANICALLY in the stage worktree at this phase-76 IMPL close with the canonical commands, EACH WITH ITS NEGATIVE CONTROL OBSERVED — never copied forward.** **Phase 76 landed +0 on EVERY axis, including +0 PRODUCTION `.go` FILES** — the first chartered row in this lineage whose production-code delta is exactly nothing, and the ANTICIPATED envelope held on all of them. The row's `.go` delta is **three TEST-side files** (`internal/cluster/ringhash_test.go` edited, `test/fixtures/0061-lb-ring-hash/driver/driver_test.go` edited, `.../linkage_test.go` NEW), plus three fixture-doc files and four project docs. ⚠️ **The stat-surface total 1205 is DOC-SOURCED and was NOT re-derived mechanically** — there is no mechanical command, and the chain is known to be discontinuous in **TWO** places (the recorded `1200 -> 1201`, and `Phase 46.1b` closing at 1198 while `Phase 47.1` opens at 1200 — an unattributed +2). **Only the +0 DELTA is asserted, and that argument is STRUCTURAL rather than a `statssink` result:** zero production `.go` bytes changed (G8) and zero byte mismatches over the 375-file production roster (G14), so no `NewCounter`/`NewGauge` call site can have moved; the five `TestNoNewStat*` guards corroborate at 5x PASS. ⚠️ **A future phase should COUNT the surface rather than trust the chain — a +0 row is a cheap place to do it, and phase 76 declined to charter that, so it is now a NAMED maintenance deferral rather than an idle observation.**

- **active differential fixtures:** **119** — ⚠️ **Phase 76 landed +0: `0061-lb-ring-hash` was EDITED, not added** (`sourceIPs` 4 -> 16 plus a NEW `driver/linkage_test.go` inside the EXISTING fixture directory — no new YAML, no new directory, no new BackendKind, no new port), so the count STAYS **119** and `0118` remains FREE. NC observed: `mkdir .../0118-fake` ⇒ 120. — `ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0117-tracing-custom-tags-metadata-cluster` (phase 73). **Phase 75 landed +0**: `0110-tls-require-client-cert-false` was **EXTENDED** with a cross-side `fixture.StatsAsserter` (NO new YAML, NO new directory, NO new BackendKind, NO new port), so the count STAYS **119** and `0118` remains FREE. ⚠️ **Next-free REFERENCE port is `10450`, NOT `10118`** — ports are NOT fixture-index aligned. **The fixture now asserts what its own README confessed was infeasible, and BOTH sides were EXECUTED**: `ssl.handshake=2 ssl.no_certificate=1 ssl.fail_verify_error=1 ssl.fail_verify_no_cert=0`, `downstream_cx_total=3`, **identical on reference and subject**, with the discriminating asymmetry (`handshake=2` vs `no_certificate=1`) observed per side. Shape A over `/stats/prometheus`, keyed on the metric NAME with `envoy_listener_address` IGNORED — the NAMES are byte-identical cross-side while the labels differ (`0.0.0.0_10446` reference vs **`___20016`** subject: ⚠️ **the subject binds the IPv6 WILDCARD**). ⚠️ **`0111` was NOT given a value assertion, deliberately** — it runs `require_client_certificate: true` on both sides, so a success-path no-cert annotation reads **0 on every arm, structurally**, and a value pin would document a **vacuous `0 == 0`** as coverage; its PROSE gained the name in its NAMED-UNASSERTED list instead, because **both `0111` passages are CLOSED ENUMERATIONS** and a name in neither list reads as ASSERTED. **`0111`'s driver is byte-untouched, and its named-subset check was EXECUTED for the first time at this IMPL: PASS.** ⚠️ `var _ fixture.StatsAsserter` is a **TRIPWIRE, not the dispatch mechanism** — `0110` is only the **THIRD** fixture repo-wide to carry it, while **82** define an `AssertStats` method.
- **fuzzers:** **55** — **Phase 76 landed +0** (it consumes no new config field, so there is no parse arm to fuzz). NC observed: appending `func FuzzProbeOnly` ⇒ 56. — `grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`. **Phase 75 landed +0** — the row consumes no new config field at all, so there is no parse arm and no fuzz-seed classification to flip (`reference_fuzzer_count_docs_drift`: an `f.Add` seed is not a fuzzer).
- **stat surface:** **1205** — BEHAVIOR_CONTRACT doc count. **There is NO mechanical counting command** (the contract says so itself); enforcement is per-phase `TestNoNewStat*` guards asserting a delta of ZERO, never the absolute total. **Phase 75 LANDED +1 (1204 -> 1205)** — `listener.<normalized-addr>.ssl.no_certificate`, the FOURTH listener-scope `ssl.*` name and the FIRST **SUCCESS-PATH** handshake annotation (⚠️ **NOT a synonym for `ssl.fail_verify_no_cert`, and the mis-mapping is wrong in BOTH directions** — one annotates an ACCEPTED handshake, the other books a REJECTED one; they are mutually exclusive on every connection). ⚠️ **The delta is a NAME-surface delta, NOT a flat per-deployment one:** registration rides the block that ALREADY gates on `rt.tlsMode`, so a **plaintext-only** deployment gains **ZERO** names, while a **QUIC** listener gains it permanently zero — **PARITY** with the reference, and structural (`Manager.Start` launches no accept loop for `kindQUIC`, so the Inc site is unreachable for a **name-independent** reason). ⚠️ **QUIC was NOT re-probed at phase 75** — the parity claim is INHERITED from phase 74's family-level probe, and that inheritance is stated in BEHAVIOR_CONTRACT rather than presented as a measurement. Ledger updated: `1204` -> `1205` at `BEHAVIOR_CONTRACT.md:831`/`:847`, plus a `**Phase 75 - 1204 -> 1205 (+1)**` line after the tail entry — **the ONLY line-adding edit in the file** (`5744 -> 5746`), so every by-line citation into the TLS subsection stays valid. ⚠️ **The new entry RECORDS BOTH ledger gaps and BACK-FILLS NEITHER** (fabricating a line would be inventing a record). Gate figures at this close: `grep -c '1204'` => **2** · `grep -c '1205'` => **3** · `grep -cE '^#{2,4} .*ADR-[0-9]+.*ADR-[0-9]+'` => **17** (was 16 — the `:1849` heading joined them, on the `:785` precedent). ⚠️ **`internal/statssink`'s four *"stays 1200 / 1196"* prose sites, stale since phase 49, are RECORDED-NOT-FIXED** — that package is on this phase's byte-untouched sha256 roster.
- **BackendKind tail:** **38** — **Phase 76 landed +0** (`0061` uses the in-process `TCPEcho` branch and implements neither `BackendKindAware` nor `PerHostBackendKind`). NC observed: appending `ZZProbeKind = 39` ⇒ tail 39. — (`H2GoawayResponder`, `test/differential/fixture/fixture.go:614`). **Phase 75 landed +0** — `0110` was extended, not cloned, and it implements neither `BackendKindAware` nor `PerHostBackendKind`, so the runner takes the in-process `TCPEcho` branch. ⚠️ **This is a TAIL VALUE, not a count** — the file declares **39** constants (values 0-38, `TCPEcho = 0`). The lineage figure **38** is correct as written; do NOT 'fix' it to 39.
- **DECISIONS.md tail:** **ADR-0298** (next-free **ADR-0299**) — `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1` -> `## ADR-0298`; `grep -c '^## ADR-0299'` ⇒ **0**. **ADR-0298 is COMPLETE as of the phase-76 IMPL** — §Context drafted at the phase-76 SPEC, §Decision + §Consequences APPENDED IN PLACE after the RETAINED italic footer, **no renumber and NO `---` separator** (the file's last `^---$` is still at `:17020`). Block shape controller-re-derived at this close: **1** `### Context` / **1** `### Decision` / **1** `### Consequences` / **1** retained footer. ⚠️ **FOUR of its OWN §Context paragraphs were CORRECTED IN PLACE** (¶1's `:295` -> `:307`, ¶5's `-count ~= 81` -> **83**, ¶7's *"~71 collapses"* -> **74/2000**, ¶11's *"needs no edit"* -> REFUTED) on the ADR-0297 ¶7/¶9 precedent, each **leading with what survives**; none changes a decision the ADR records. ⚠️ **It carries NO whole-file grep count and claims NO family ordinal** — the Load-balancing family closed at row 54, and **maintenance rows do not extend a charter**. *(Historical, retained: **ADR-0297 is COMPLETE** — §Context drafted at the phase-75 SPEC (ADR-0044-**as-used**; ⚠️ ADR-0044 does NOT actually contain that discipline), §Decision + §Consequences appended IN PLACE at the phase-75 IMPL after the **RETAINED** footer, mirroring ADR-0295/0296. Block shape verified at this close: **1** `### Context` / **1** `### Decision` / **1** `### Consequences` / **1** retained footer; `grep -c '^## ADR-0298'` => **0**. **No renumber, no new ADR.** ⚠️ **Its OWN §Context ¶7 and ¶9 were CORRECTED IN PLACE while completing it** — ¶7's **self-falsifying whole-file grep** (the same species the phase-74 IMPL had just fixed in ADR-0296 ¶3, reproduced one ADR later) is restated with **NO number** and the searched token deliberately **NOT spelled**, because every restatement mints another counter-example; and ¶9's *"SELF vs OTHER-ADR"* form rule is replaced by **graft SCALE** after the n=7 population was re-enumerated first-hand. ⚠️ **¶9 ALSO said *"the only two instances in the file"* — which this row's own blockquote makes three** — so a pure append would have shipped a SECOND self-falsifying count in the commit that fixed the first. **The prescription has now outlived THREE wrong justifications: family, self-vs-other, scale.** ⚠️ **`ADR-0296` is NO LONGER byte-untouched** — its §Decision (g) carries the phase-75 correction as an INDENTED blockquote (`grep -c '^  > '` **2 -> 3**), **LEADING WITH WHAT SURVIVES** (no such counter was owed *in phase 74*, whose sole stat-asserting fixture is `require=true` on both sides — **the deferral was RIGHT**) and claiming **NO ORDINAL**.)*
- **go.mod modules:** **Phase 76 landed +0** — `go mod tidy -diff` EMPTY and `git diff master -- go.mod go.sum` EMPTY; ⚠️ **the row's TEST-side import delta is +8 and that is CORRECT** (`ringhash_test.go` gains `fmt`/`math`/`mathrand "math/rand/v2"`, `linkage_test.go` adds `go/ast`/`go/parser`/`go/token`/`strconv`/`testing`) — all stdlib, all test-side, and **an import LINE is not a go.mod MODULE**; there is no new sub-package, so `reference_new_subpackage_pulls_transitive_module` does not bite. NC observed: a fake `require` ⇒ 67 -> 68. The tracked lineage figure is **2** (the phase-61.2 count: `quic-go` direct + `qpack` indirect) — **NOT a repo total**; the single `go.mod` requires **67** (18 direct + 49 indirect). **Phase 75 landed +0** — `go mod tidy -diff` EMPTY and `git diff master -- go.mod go.sum` EMPTY. The row needed `crypto/tls` and `internal/stats`, both ALREADY imported by `internal/listener/manager.go`; **an import LINE is not a go.mod MODULE**, and there is no new sub-package, so `reference_new_subpackage_pulls_transitive_module` does not bite. ⚠️ **TEST-side imports grew by exactly four** (`log`, `math`, `net/http`, `strconv` in `0110`'s driver) — all stdlib, all test-side, **not a production-envelope violation**.
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` (see `ENVOY_TARGET.md`).
- **conformance:** h2spec 53/53; proxy-wasm all 10 families PASS.

---

## Recent lineage

*(A SHORT recent lineage, capped at FIVE prior entries by ADR-0288. Everything earlier is in `STATE_HISTORY.md`. §Current carries the newest stage (**the phase-80 IMPL** — the stage that landed the `sds.` first-dot label-hoisting arm and the boot-boundary secret-name reject, flipped ROW 80 `done`, completed ADR-0302 in place and fixed ADR-0299's three-row-stale STATUS word). The five below are the FIVE PRIOR stages, newest first. ⚠️ **THIS SENTENCE IS ITSELF A DRIFT SURFACE AND HAS BEEN WRONG BEFORE:** it named the phase-80 BRAINSTORM as newest right through the phase-80 SPEC, which rolled §Current but not this note. **A rolling document's own descriptive prose must be rolled with it.** ⚠️ **A close that adds a bullet here MUST evict the oldest to `STATE_HISTORY.md` VERBATIM, and MUST verify the evictee is ABSENT from the archive beforehand with a FIRING negative control on a sibling that IS present.** ⚠️ **RE-DERIVE WHICH BULLET IS OLDEST AT THE TIP — never inherit the name from a plan**, because the plan's own eviction has already moved its named target once.)*

- **prior active-phase:** `phase 80 (stats-sds-projection) IMPL done` (2026-07-30) — lifecycle-state **3 -> DONE**; **ROW 80 FLIPPED `in-progress` -> `done`; PHASE 80 WAS CLOSED.** The `sds.` SN1 hoist plus a boot-boundary reject landed GREEN (full 120-fixture differential **120/120 in 402 s**, h2spec **53/53**, `-race` clean, stat-surface delta **+0** proven by call-site enumeration). `DECISIONS.md` **17692 -> 17724** (ADR-0302 PROPOSED -> COMPLETE, plus the ADR-0299 rider), `BEHAVIOR_CONTRACT.md` **5822 -> 5868**, `STATE_HISTORY.md` **428 -> 430**. ⚠️ **The PLAN estimated ~640 net `.go` and the row realized 1636 — the lineage's FIRST realized ~1500-LoC crossing**, every bucket over with the errors COMPOUNDING (fixture bucket 4.1×). Base master `28aa342d`, branch `phase80-impl`. **THIRTEEN tasks executed; SIXTEEN PLAN claims refuted, four load-bearing.**

- **prior active-phase:** `phase 80 (stats-sds-projection) PLAN done` (2026-07-30) — lifecycle-state **2 -> 3**; **ROW 80 STAYS `in-progress`** and `ROADMAP.md`, `BEHAVIOR_CONTRACT.md` and `DECISIONS.md` are **BYTE-UNTOUCHED** at this stage (the PLAN adds no row; sentinel `want` stays **112**). Docs-only: ZERO production `.go`, ZERO test `.go` committed. **Base master `47b9b378`** — ⚠️ **NOT the `f2dd994a` the router named**: `47b9b378` is the router's own roll commit on top of it, and branching off `f2dd994a` would have silently discarded it (`git diff --name-only f2dd994a..47b9b378` ⇒ `next-prompt.txt` ONLY, so every doc count survived). Branch `phase80-plan`. **FOUR probe agents on disjoint remits**, each in its own DETACHED worktree with PRIVATE scratch and a port band in **42000-42299** — clear of BOTH reserved bands (`20000-31007` subject blocks AND `11000-14999` backends, new at `f2dd994a`) and the static fixture ports — plus controller re-derivation. Zero commits, zero pushes by any agent; every experimental edit reverted BY EXPLICIT PATH with sha256 byte-identity verified **4/4** and independently re-confirmed by the controller. ⚠️ **THE PLAN'S OWN HEADLINES:** the SPEC's §7.2 fixture VERDICT was **RED ON ARRIVAL on three of five names**; its *"T5 folds into T4"* is not a fold but a **FORCED ATOMIC MERGE** (so the enumeration is **13 not 14** and T4 must NOT split); and its `855 x 1.75` split arithmetic **mixes two bases** (a realized-over-PLAN multiplier applied to a SPEC estimate), so *"ON the ~1500 line"* is not a derived result. **BAND 11-14, BUDGET 13.** ⚠️ **The reject's SITE was pinned BEFORE the SDS dial** — placed after it, the dial error MASKS the name reject. ⚠️ **A WRONG FIGURE WAS ALREADY BAKED INTO A LANDED ADR** (ADR-0302 §Context ¶11's *"forty-five files"*) and was flagged as landable at the IMPL precisely because the STATUS was still `PROPOSED`.

- **prior active-phase:** `phase 80 (stats-sds-projection) SPEC done` (2026-07-29) — lifecycle-state **1 -> 2**; **ROW 80 STAYED `in-progress`**, `ROADMAP.md` and `BEHAVIOR_CONTRACT.md` BYTE-UNTOUCHED, sentinel `want` stayed **112**. Docs-only, FIVE files: `SPEC.md` (NEW) + `DECISIONS.md` (**ADR-0302 §Context**, 17658 -> 17692, 301 headings) + `STATE.md` + `STATE_HISTORY.md` (**422 -> 426**) + `next-prompt.txt`. Base master `53855de0`, branch `phase-80-spec`. **FOUR investigation agents on disjoint remits.** ⚠️ **THE DESIGN FORK DISSOLVED:** first-dot is BOTH what the live reference does (three discriminating arms plus the pinned binary's own tag-extractor literal `^sds\.((<TAG_VALUE>)\.).+`) AND the file's own convention (all four dynamic-token arms use first-dot; last-dot needs a `strings.LastIndex` found NOWHERE in `ExtractTags`) ⇒ **D-SDS-1 = SN1-shaped, FIRST-DOT, single-segment, non-greedy**, label `envoy_xds_resource_name`, and there is **no fidelity-versus-correctness tension to adjudicate**. ⚠️ **BUT THE BRAINSTORM'S RULE WAS INCOMPLETE:** the reference applies a NAME-FORMATION pre-step (`://`/`:/`/`:` -> `_`, then TrimRight of ALL trailing dots) that envoy-go's naive `fmt.Sprintf` does not, so `trailing_dot.` builds `sds.trailing_dot..update_success` — which **`NamePattern` ACCEPTS**, because it is a CHARSET guard and not a well-formedness guard. ⚠️ **D-SDS-2 = a boot-boundary REJECT in `boot.NewSDSProvider`, NOT in `ParseSDSConfig`** — the SPEC's own first draft had that wrong: `ParseSDSConfig` has FOUR non-test call sites, three in `internal/tls/config.go` wrapping into a DIFFERENT error chain, none of which registers a stat. ⚠️ **THE REFERENCE ACCEPTS A HYPHENATED SECRET** and serves `envoy_sds_update_success{envoy_xds_resource_name="server-cert"} 1` with the hyphen `od`-verified verbatim in the label value ⇒ the silent skip is a **genuine conformance divergence** AND the chosen reject is **itself a documented envoy-go-strict DEPARTURE**. ⚠️ **THE EXISTING GATES ARE NEARLY BLIND** — the whole of `./internal/stats/... ./internal/statssink/...` fails EXACTLY ONE test under an experimental arm, and **ALL FOUR** phase-79 OTLP byte goldens are UNMOVED because `goldenRoster`'s 13 entries carry **no `sds.*` name** ⇒ **broken-gate shape SEVENTEEN: a golden ROSTER that omits the family under test makes EVERY cell of an otherwise-correct cross-product vacuous.** ⚠️ **THE FIXTURE HOME IS `0110`, NOT `0103`** (the SPEC's other self-correction) — but `0110`'s `scrapeProm` keys by metric name and DISCARDS the label set, so it is not free reuse. ⚠️ **AND THE FIXTURE CANNOT DISCRIMINATE THE FIRST-DOT FORK** — all four corpus secrets are DOT-FREE, so a dedicated unit test (G6) is required or the row's central decision ships unguarded. **A two-secret config is a hard boot reject** (exit 1, 0 B stdout, `seen` counted across ALL listeners) ⇒ **5 per process, and a two-secret fixture is NOT constructible.** `incNil` COMPLETE (95-byte audit, `partial == 0`, 31 rejected bytes). Nine BRAINSTORM claims refuted; **SPEC band 12-15, budget 13**.

- **prior active-phase:** `phase 80 (stats-sds-projection) BRAINSTORM done` (2026-07-29) — lifecycle-state **DONE -> 1**; **ROW 80 REGISTERED `in-progress`** at `ROADMAP.md:142`, **RE-OPENING sentinel check (1)** after its fifth-ever silent close; ROADMAP **227 -> 228 lines / 111 -> 112 data rows**, sentinel `want` bumped **111 -> 112** in the SAME commit and **rehearsed with controls BEFORE the edit**. **`DECISIONS.md` and `BEHAVIOR_CONTRACT.md` BYTE-UNTOUCHED.** Docs-only: ZERO production `.go`, ZERO test `.go`. Base master `80cafb7e`, branch `phase-80-brainstorm`. **FOUR investigation agents on disjoint remits**, each in its own detached worktree with PRIVATE scratch and a private port band **outside 20000-31007** (A1 41000-41099 with docker, A2 41100-41199, A3 41200-41299, A4 none), plus controller re-derivation. **SELF-PICKED per the 2026-07-12 standing directive: the `sds.` Prometheus-projection gap PLUS its registration-time silent-skip twin** — the last remaining arm of the departure rows 77 and 79 narrowed. ⚠️ **THE ID IS 80, NOT `79.1`, AND THAT IS AN ADJUDICATION AGAINST FOUR LANDED DOCUMENTS**: BOOTSTRAP §6.2's split machinery (parent `in-progress` + a split ADR) was never invoked, phase 79 crossed no §6.1 threshold and closed `done` with an EMPTY sub-phases field, **no sub-phase ROW has been created since 32.2** (all 31 dotted ids ≤ 32.2; the last 47 rows are flat), and **when this project DOES split the legs live in the parent's sub-phases PROSE with NO row of their own** (rows 40/42/43) ⇒ `79.1` would need either REOPENING closed row 79 or a row shape no row in the file has. **DEFECT REPRODUCED: 20/20 dropped against a discriminating 8/8 control**; end-to-end PRESENT on flat `/stats` (35 lines) and ABSENT on `/stats/prometheus` (86 lines, **0** `envoy_sds`). ⚠️ **THE DENOMINATOR IS 20 PER CORPUS AND 5 PER PROCESS** — `boot.NewSDSProvider`'s `seen > 1` guard admits ONE secret per boot, so a fixture assertion expecting 20 is unsatisfiable; `0024`'s two `path_config_source` secrets are EXCLUDED (including them gives 6x5=30 and would coincidentally 'confirm' the banked 30 for the wrong reason). ⚠️ **THE HOISTING SHAPE IS CONFIRMED AGAINST THE LIVE PINNED REFERENCE and the contract's own owed re-pin is DISCHARGED** — key `envoy_xds_resource_name` (a plausible-but-wrong key greps **0** against **66**), two distinct secret names in one dump, negative-controlled against a known-hoisting and a known-byte-mirror family. **This INVERTS the phase-79 result, where all three probed roots byte-mirrored.** ⚠️ **AND THE REFERENCE HOIST IS SINGLE-SEGMENT AND NON-GREEDY** — secret `alpha.beta.gamma` renders `envoy_sds_beta_gamma_update_success{envoy_xds_resource_name="alpha"}`, so the arm is SN1-shaped and the SPEC must adjudicate a fork in which **REFERENCE FIDELITY and apparent correctness DISAGREE** (A2 reasoned last-dot is correct without probing the reference; A1 supplied the fact and inverted it). ⚠️ **THE SECOND HALF IS OPERATOR-FACING: A HYPHEN.** `NamePattern` rejects 31 printable bytes incl. `-` and `/`, so `server-cert`, `my-tls-cert` and any SPIFFE URI silently skip all five counters — live boot on `server-cert` gives a green listener, working TLS, **5 of 36 stats gone and ZERO bytes of stderr**. ⚠️ **THE TWO HALVES ARE COUPLED: the phase-79 skip log is an INVERTED signal** that fires when the names exist and goes SILENT when they do not, so landing the projection arm ALONE would destroy the only accidental diagnostic in either direction. ⚠️ **A LANDED ADR CONSEQUENCE IS OVER-ATTRIBUTED** — ADR-0300 §Consequences (iii) credits phase 78 with making this candidate's boot panic visible; probed at **FIVE** varied insertion points per §Consequences (ii), every `defer` in `main()` is strictly AFTER the registration call, so the panic unwinds an **EMPTY defer chain** and visibility never depended on phase 78 at this site. ⚠️ **A FALSE DRIFT-CORRECTION REFUTED AND NOT INHERITED** — the banked claim that the phase-79 SPEC swapped the secret-to-fixture attribution is ITSELF WRONG: `0110` is `rccf_validation_ca`, `0111` is `edf_validation_ca`, evidenced by **executable Go in both drivers** with zero cross-occurrences ⇒ **`SPEC.md:114` was RIGHT** and the correction had propagated into four documents plus the stage brief. ⚠️ **AND THE FINDING WORTH MORE THAN THE ROW: A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`** — `grep` is a shell function execing ugrep with `--ignore-files`, `.gitignore:2` lists the file, and it is TRACKED anyway ⇒ `grep -rn 'want=111'` returns **8 hits / 6 files** while `command grep -rn` returns **11 / 7**. **`git check-ignore` reassures you WRONGLY** (git exempts tracked files). Found INDEPENDENTLY by the controller and by A4. **Band 9-12, budget 11**; stat surface delta ANTICIPATED **+0** (projection moves, not size).

- **prior active-phase:** `phase 79 (stats-prometheus-projection) IMPL done` (2026-07-29) — lifecycle-state **3 -> DONE**; **ROW 79 FLIPPED `in-progress` -> `done`; PHASE 79 CLOSED**; `want` STAYED **111**. `DECISIONS.md` **17628 -> 17658** (ADR-0301 PROPOSED -> COMPLETE, appended IN PLACE, no renumber, no `---`); `BEHAVIOR_CONTRACT.md` **5762 -> 5822**; `STATE_HISTORY.md` **416 -> 418**. Base master `22aa4dd6`, branch `phase79-impl`. **SEVEN investigation agents** on disjoint remits. **DELIVERED:** three SN5-shaped byte-mirror arms (`runtime.` `access_logs.` `tracing.`) so **TEN** registered names begin projecting; the terminal `ExtractTags` rejection now enumerates BOTH acceptor species; the silent skip becomes ONE aggregated log line per call with format string AND separator pinned as constants; `helpText` **15 -> 25** behind TWO guards; the `0118` absence pin CONVERTED to a LABEL-AWARE parity assertion; a four-consumer sink byte gate proven by a tag-hoisting NC at TWO injection sites. **+0 on every count axis.** ⚠️ **THE HEADLINE — THE ROW'S OWN FIX WAS STALE ON ARRIVAL AND EVERY GATE STAYED GREEN:** T1 landed the three arms first, T2 then wrote a terminal message enumerating the pre-row NINE top-level segments while TWELVE were live, T3 swept `nine` into four fixture files, T6 pinned the wrong text BYTE-EXACT — the very defect the row exists to repair, reproduced ONE GENERATION ON INSIDE the repair. Found by THREE investigations, each by EXECUTION, none by review. ⇒ **A BYTE-STABILITY GUARD WHOSE GOLDEN IS HAND-WRITTEN SHARES THE AUTHOR'S MISTAKE**; the row therefore ships `internal/stats/segmentcount_test.go`, deriving BOTH sides from `ExtractTags`' **AST**. ⚠️ **SIX STATEMENTS THE ROW'S OWN LANDING FALSIFIED** ⇒ a roster derived at the branch point is invalid the moment a sibling task lands; prefer SYMBOL anchors. ⚠️ **FOUR PLAN CLAIMS REFUTED BY EXECUTION** (arm iota unreproducible under a DERIVED roster; ceiling mechanism (b); the `envoy.yaml` site; `bootstrap.go:621-622`). ⚠️ **BROKEN-GATE SHAPE FIFTEEN CONFIRMED LIVE** — the OTLP `(F,F)` cell is byte-identical under the hoisting NC, so a golden on the default knobs PASSES on the wrong tree, while `(T,F)` SHRINKS. **Gates:** full 120-fixture differential **119 PASS + 1 FAIL**, the red being `0084-otlp-access-log` on `bind: address already in use` BEFORE any assertion, fixture UNTOUCHED, isolate-re-run **3x PASS** ⇒ the INDEXED STARTUP FLAKE classified ON EVIDENCE; `./internal/...` **70 ok**; `-race` clean; `gofmt -l` empty; `golangci-lint` 0; `go vet` 0; `go.mod`/`go.sum` 0 bytes.



## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
