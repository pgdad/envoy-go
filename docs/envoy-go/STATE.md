# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

> **This file is a POINTER, not an archive** — `BOOTSTRAP_PROMPT.md` §4: *"STATE.md — pointer: active phase, last commit, next action."* It carries the live pointer, the live counts, and a SHORT recent lineage. **Full stage-by-stage history lives in `STATE_HISTORY.md`** (archived at ADR-0288; **nothing was deleted** — the pre-move file is preserved there verbatim, byte-for-byte).
>
> **⚠️ THE RULE ADR-0288 EXISTS TO ENFORCE: there is EXACTLY ONE live `next-skill:`, `lifecycle-state:`, `next-free ADR:`, and count block IN THIS FILE, and they live in §Current pointer directly below.** Before that move, ~330 lines of prepended backlog had buried TWO fossilised "current state" blocks whose `next-skill:` still read *"the 42.2 hedging BRAINSTORM"* and *"the 46.2 SPEC"* while the live phase was **66** — so `grep 'next-skill:' STATE.md` returned **two stale hits and ZERO live ones**, and `grep 'fuzzers:'` returned **48** and **42** when the true count was **55**. **A session may trust a grep of this file again. Keep it that way: when you advance the pointer, EDIT §Current pointer IN PLACE — never prepend a new block above it.**

---

## Current pointer

*(The ONLY live "what next" in this repo. Update IN PLACE at every stage close.)*

- **active-phase:** `phase 76 (lb-ring-hash-spread-margin) PLAN done` (2026-07-26) — **ROW 76 STAYS `in-progress`**; sentinel check (1) stays live (`NOT DONE: row 76`) until the phase-76 IMPL. The sentinel **STILL does NOT fire** (all three checks print) and **`stop` was NOT created.** A Load-balancing-family **MAINTENANCE** row — ⚠️ **on a family DECLARED CLOSED at row 54** (`ROADMAP.md:116`, *"EIGHTH and FINAL Load-balancing-family row"*), so it claims **NO family ordinal** and the heading paragraph is NOT amended (the BRAINSTORM's *"the family is already open"* is a mis-description; the right reason is *"maintenance rows do not extend a charter"*). **ROADMAP BYTE-UNTOUCHED** (the phase-75 `2b5eff0a` precedent); `DECISIONS.md` carries the **ADR-0298 §Context** append.
- **phase-directory:** `docs/envoy-go/phases/76-lb-ring-hash-spread-margin/` (`BRAINSTORM.md` + `SPEC.md` + the new `PLAN.md` + `PROGRESS.md`)
- **lifecycle-state:** **`3`** (PLAN done. Next -> the phase-76 IMPL). ⚠️ **THE PLAN REFUTED THE SPEC'S OWN LINKAGE GATE — IN BOTH DIRECTIONS AT ONCE.** SPEC §3.3.1's grep-equality gate (`SPEC.md:225-227`) **FAILS OPEN**: `[ "$a" = "$b" ]` with both captures empty is TRUE in `test(1)`, so a rename, a file move or a deletion of either constant exits **0** (controller-executed: `SPEC FORM: exit=0 GREEN with a='' b=''`). It **ALSO FAILS CLOSED**: `grep -c 'collapseFixtureK'` returns **2**, the second match being the measurement test's **own doc comment**, so on a genuinely synced tree (16 vs 16) it printed `DESYNC: sourceIPs=16 collapseFixtureK=16` and exited 1. **The document that warns *"a green gate is evidence only if you have seen it go red"* shipped a gate whose red arm was never fired.** It is REPLACED by a `go/parser` Go test in the fixture's own package (structurally immune — the parser sees declarations, not comments; **+0 exported symbols**), proved over the **FULL 2×2 cross-product** plus the empty-capture arm; ⚠️ **the (4,4)-passes arm is the load-bearing one** — it proves the gate READS the test-side value rather than hardcoding 16. **TWO MORE GATES BROKEN, both NEW to this lineage:** ⚠️ **`go doc -all <pkgA> <pkgB>`'s fail-open is specific to the `./` PREFIX** — bare import paths exit **1** with `doc: no symbol …`, while `./`-prefixed exit **0** silently dropping the second argument **even when it names a directory that does not exist** (phase 75 pinned the `./` form) — this NARROWS `reference_gate_command_negative_control`; and ⚠️ **`gofmt -l` NEVER exits non-zero** (observed `exit=0` while printing the unformatted file), so any six-gate chaining it with `&&` is **INERT — gate on OUTPUT**. Plus a **THIRD, unrecorded defect in phase 75's `impblock`**: its awk matches only `^import \($`, emitting ZERO lines for `driver_test.go:3` and `ringhash_test.go:3` (both single-line `import "testing"`) ⇒ **structurally blind to 2 of phase 76's 3 `.go` files**; basename normalisation alone does not fix it. ⚠️ **AND THE PHASE-75 sha256 ROSTER CANNOT BE COPIED FORWARD** — `internal/cluster/**` was byte-gated at phase 75 and phase 76 **EDITS `internal/cluster/ringhash_test.go`** (`comm -12` returns exactly that file); copying the glob reproduces the phase-73 SEVERE-1. ⚠️ **THE SPEC REFUTED BOTH OF THE BRAINSTORM'S HEADLINE SCOPING CLAIMS, BY EXECUTION.** **(1) *"The executable delta is ONE INTEGER"* is FALSE.** `test/fixtures/0061-lb-ring-hash/driver/driver_test.go` exists (6 tests), hard-codes 64-summing tuples, and is named in **NO** phase-76 document (`grep -c 'driver_test' BRAINSTORM.md` ⇒ **0**). At the raised constant `TestAssertDistribution_Affinity` **FAILS**, and — worse — **`_CollapseBitesSpread` and `_ReferenceConservation` go SILENTLY VACUOUS**, tripping the *subject-conservation* leg before reaching their own while still printing `--- PASS`; the first of those is the unit test for **this row's own assertion**. ⚠️ **THREE independent "confirmations" were blind in the same direction** — a file-scoped `grep` over `driver.go` (cannot see a sibling file), a `git diff --stat` reading `1 file changed` (measures what was EDITED, never what was BROKEN), and a green `go test ./test/differential/` (never compiles the driver package's `_test.go`). ⇒ **a change-set measure is not a build measure.** **(2) `sourceIPs = 10` is BELOW the project's own recorded bar; the constant is revised to 16.** `reference_differential_band_sigma_margin` records **4-5σ**; K=10 is **3.89σ**. Siblings **`0062-lb-ring-hash-http` and `0063-lb-maglev` fixed the BYTE-IDENTICAL defect — same root cause, same `3·(1/3)^N` law, same original N=4 — by setting their key count to 16** (`0062/driver/driver.go:91`, `0063/driver/driver.go:91`), justified at `0062/README.md:57-63` as *"past the 5σ-equivalent flake-free margin"*. **`0061` was simply left behind; phase 76 finishes a phase-36.2 migration rather than establishing a policy.** **Subject:** `lb-ring-hash-spread-margin` — give `0061-lb-ring-hash`'s spread assertion (*"only N backend(s) nonzero, want >= 2 (ring collapsed?)"*, `test/fixtures/0061-lb-ring-hash/driver/driver.go:294-296`) a **DERIVED statistical margin**: `sourceIPs` **4 -> 16** at `driver/driver.go:78` (⚠️ **16, NOT the BRAINSTORM's 10** — see the lifecycle-state note above) (`totalConns` is DERIVED and tracks automatically), plus a **seeded collapse-rate MEASUREMENT** in `internal/cluster/ringhash_test.go`, plus the prose sweep the fix falsifies. **THE DIAGNOSIS, DERIVED THREE INDEPENDENT WAYS THAT ALL AGREE:** the governing variable is **K = distinct hash keys = `sourceIPs` = 4**, NOT the 64 connections — `tcp_proxy`'s `source_ip` policy resolves through `cluster.HashSourceIP` (`internal/cluster/hash.go:133`), which **STRIPS THE PORT**, so 16 connections from one source IP are **ONE** key. Meanwhile the ring is keyed on the endpoint address **INCLUDING its OS-ephemeral port** (`internal/cluster/ringhash.go:88-91` + `test/differential/runner_test.go:272`), so it is a **fresh random 3-way partition on EVERY run**. ⇒ **P(collapse) = 3·(1/3)^K = 3^(1−K) = 3.7% at K=4**; analytic **0.0370**, controller Monte Carlo (200 000 trials/K, fixed seed, model built from the CODE) **0.0365**, an agent's independent replica (60 000 trials/K, real `xxHash64`, sanity-checked against the fixture's own 1026/342 ring-size gauge) **0.0349**. At **K=16 -> 7.0e-8 (~530 000×)**; K=10 would have been 5.1e-5. ⚠️ **`burstPerIP` MUST NOT MOVE** — it is the AFFINITY leg's discriminating modulus (`driver.go:284`). **Anticipated envelope: +0 stats (1205) / +0 fixtures (119 — `0061` EDITED) / +0 fuzzers (55) / +0 BackendKinds (38) / +0 modules (2) / +0 PRODUCTION `.go` FILES / +0 imports / ZERO new packages / ZERO new exported symbols**; ADR-0298 anticipated but CONDITIONAL (**D-RHSM-ADR**). ~5-7 tasks — the smallest row the project has chartered.
- **next-skill:** **the phase-76 IMPL** — execute PLAN 76's **NINE-task TDD spine (T1-T9)** with its **SIXTEEN-break roster**, of which **THIRTEEN ALREADY RAN at the PLAN**. ⚠️ **THE ROW IS LARGELY BUILT ALREADY, IN PROBE WORKTREES** (`phase-76-plan-v1` `10f60552`, `phase-76-plan-v2` `5feb0609`; local commits, **never pushed**, **NOT in the PLAN commit**): the measurement test, the `go/parser` linkage gate, `sourceIPs = 16`, and the rescaled `driver_test.go` are all COMPILED and RUN. **The IMPL RE-DERIVES and RE-LANDS at its own tip; it does NOT cherry-pick the probes** (`reference_a_drift_correction_is_itself_a_claim`). **BREAKS ALREADY FIRED:** β two-edit (`collapse rate 0.03700 (74/2000) >= bar 0.001`, a 35x margin) · **γ-SHARP** — freezing the ephemeral ports drove the **CONTROL leg RED while the MEASURED leg stayed GREEN at `0/2000`**, which is the MEASURED proof that the measured leg is a **NULL RESULT standing alone** · γ-restore (green AFTER an observed red) · the linkage **2x2 cross-product all four arms** plus the empty-capture arm · **six anti-vacuity breaks, one per unit test, each firing exactly ONE red test**. ⚠️ **β-SINGLE-EDIT IS A DECLARED MUST-NOT-FIRE AND IT DID NOT FIRE** (fixture at 4, `collapseFixtureK` at 16 => `--- PASS`, exit 0): `grep -rn '\bsourceIPs\b' --include='*.go' .` => **3 hits, all in the 0061 driver** — the two constants are mechanically decoupled, so **a two-edit β proves only the UNIT TEST's constant load-bearing.** ⚠️ **ALL FOUR ITEMS SPEC §13 CARRIED AS *NOT EXECUTED* ARE NOW DISCHARGED.** **Break α FIRED THE SPREAD LEG** — `runner_test.go:1293: distribution: subject spread: only 1 backend(s) nonzero, want >= 2 (ring collapsed?)`, byte-identical to the expected string, from **`driver.go:295`, the FOURTH of five sequential legs**; affinity and conservation are UPSTREAM in the same function and did **NOT** fire (counts collapse to `[256,0,0]`, `256 % 16 == 0` so affinity survives and the sum is unchanged so conservation survives) ⇒ **neither vacuous nor mis-targeted**; restore ⇒ isolated `--- PASS (3.67s)`, **a green FOLLOWING an observed red**. ⚠️ **NEW FINDING: the unit package DOES catch break α — by exactly ONE test out of 403.** `TestRingHash_DistinctKeysSpread` fires while **8 of the 9 `TestRingHash_*` tests PASS under a TOTAL ring collapse**, including `SameKeySameEndpoint` and `WrapAround` ⇒ **the differential fixture is a genuine SECOND guard, not redundant coverage**, and the unit-level guard is a single point of failure. **FULL DIFFERENTIAL AT K=16: 119/119 PASS / 0 FAIL / 0 SKIP, `ok … 402.326s`**, `0061` subtest **3.30 s** — **−6.47 s (−1.58%)** against the same-day pre-fix master baseline (119/119, 408.8 s), and ⚠️ **NO hazard fired at all**, `0061` included. **`-race` over `./test/differential/`: ZERO data races**; two failures on fixtures **unrelated to this row** (`0009`, `0084`), both at `waitTCPDial` readiness **BEFORE any assertion executed**, **both isolate-green** ⇒ startup class. ⚠️ **NEITHER was on this stage's own hazard roster by name — the FOURTH consecutive stage at which that has held**, and ⚠️ **the startup flake fires MORE readily under `-race`** (2 hits with, 0 without, same session). ⚠️ **SPEC §3.6's RUNTIME FIGURE IS REFUTED:** both arms measured in ONE session, 3 warm runs each — K=16 **4.410 s** wall vs K=4 **4.463 s** ⇒ **−0.053 s (−1.2%)**, **negative and smaller than the K=4 arm's own spread** ⇒ **NO measurable cost from K=4 -> K=16, and NOT a speedup either. Do not carry +0.158 s (+4.5%) forward.** ⚠️ **The SPEC's methodological RULE survives its own NUMBER** — *quote the delta against a same-session control* is precisely what produced the different answer. **WHAT REMAINS FOR THE IMPL:** the prose sweeps (T5/T6) · `BEHAVIOR_CONTRACT:1326` (T7) · ADR-0298 completion · the row-76 flip with its four stale-claim corrections (T9) · and re-running the thirteen already-fired breaks at the IMPL's OWN tip. ⚠️ **`127.0.0.12`-`.17` ARE NO LONGER OWED — DISCHARGED at this PLAN: 16/16 binds PASS**, every requested `LocalAddr` == observed, full round-trip per arm. ⚠️ **`3^(1-K)` is a CONSERVATIVE UPPER BOUND, not "the" probability** — measured over 2e5 real ring draws, p̂/analytic is **0.949** at K=4 and **0.689** at K=8 (a random-key discriminator arm recovers 1.010/1.104, so the deficit belongs to the specific `xxHash64` key set); ⚠️ **K=16 is NOT measured and cannot be at that scale** (expected count 0.014) — **7.0e-8 is analytic/extrapolated.** ⚠️ **The CONTROL count is 74/2000, not the SPEC's 71.** ⚠️ **The naive x4 rescale of the scatter tuple manufactures a THIRD vacuity** (`{20,28,16}x4 = {80,112,64}`, all multiples of 16) — `{20,108,128}` is used instead. ⚠️ **SPEC §10 carries FOUR anchor errors** (`README:58`->`:57`; `:114` and `:135` MISSING; `driver_test.go` is **15** sites not 8, two of them executable `t.Fatal` strings) and **SPEC §4's collision claim is FALSE for `allSame`** (3 live Go hits in `package router`). **NEGATIVE-CONTROL EVERY GATE** — five gates in this lineage are now recorded broken across both directions, **three of them found here**. Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-pushes (`feedback_push_to_origin`); **use `git -C <abs-worktree-path>` everywhere and tripwire `pwd` + branch + commit count before any commit or gate run** (`reference_bash_cwd_reset_commits_to_main` — ⚠️ **it fired LIVE again throughout this PLAN**).
- **last-commit:** the phase-76 PLAN squash (`git log --grep 'phase 76'`; **locate by SUBJECT, never by position** — `reference_next_prompt_tracked_despite_gitignore`). Prior: the phase-76 SPEC squash `98c27fc9` (this stage's base), the phase-76 router commit `6ef436ac`, the phase-76 BRAINSTORM squash `6c68a351`, the phase-75 IMPL squash `c57b98b8`.
- **last-updated:** 2026-07-26 (phase-76 PLAN done. Lifecycle-state **2 -> 3**; **ROW 76 STAYS `in-progress`**; **ROADMAP and DECISIONS BYTE-UNTOUCHED** — ADR-0298 completes at the IMPL. Fresh worktree `/home/esa/git/envoy-go-wt-p76plan` off master `98c27fc9`, branch `phase-76-plan`. **File set: the new `PLAN.md` + the new `PROGRESS.md` + `STATE.md` + `next-prompt.txt`** — ZERO production `.go`, ZERO test `.go` **in the PLAN COMMIT** (the build work lives in probe worktrees `phase-76-plan-v1`/`-v2`, committed locally, never pushed). **FIVE agents on disjoint remits with PRIVATE scratch and SEPARATE worktrees** — one read-only roster re-derivation, one building and adversarially breaking the measurement, one executing the fixture rescale and the leg proofs, one auditing gates/ADR/contract/format, one running break α and the full suite at K=16 — plus controller re-derivation of every load-bearing claim. **THE HEADLINE — THE SPEC'S OWN LINKAGE GATE IS BROKEN IN BOTH DIRECTIONS AT ONCE, AND THE SPEC IS THE DOCUMENT THAT WARNS ABOUT EXACTLY THIS.** It **FAILS OPEN** on empty captures (`[ "" = "" ]` is TRUE ⇒ exit 0 on a tree where NEITHER constant exists — a rename, a move or a deletion reads green; controller-reproduced) and **FAILS CLOSED** on a correctly-synced tree (`grep -c 'collapseFixtureK'` ⇒ **2**, the second match being the measurement test's **OWN DOC COMMENT** — `reference_sentinel_matcher_string_self_clears`, live in a gate rather than a sentinel). REPLACED by a **`go/parser` Go test** in the fixture's own package: structurally immune (the parser sees declarations, not comments), **+0 exported symbols**, proved over the **FULL 2x2 cross-product** — ⚠️ **the (4,4)-passes arm is the load-bearing one**, since it proves the gate READS the test-side value rather than hardcoding 16 — plus the empty-capture arm the SPEC never ran. **TWO MORE BROKEN GATES, BOTH NEW:** ⚠️ **`go doc -all <pkgA> <pkgB>`'s fail-open is specific to the `./` PREFIX** — a BARE import path exits **1** with `doc: no symbol …`, while a `./`-prefixed one exits **0** silently dropping the second argument **even when it names a directory that does not exist** ⇒ this **NARROWS** `reference_gate_command_negative_control`; and ⚠️ **`gofmt -l` NEVER exits non-zero** (observed exit 0 while printing the unformatted file) ⇒ any six-gate chaining it with `&&` is **INERT**. Plus a **THIRD, previously unrecorded defect in phase 75's `impblock`** — it matches only `^import \($`, so it emits ZERO lines for the two single-line `import "testing"` files and is **structurally blind to 2 of phase 76's 3 `.go` files**. ⚠️ **THE PHASE-75 sha256 ROSTER CANNOT BE COPIED FORWARD** — `internal/cluster/**` was byte-gated there and this row EDITS `internal/cluster/ringhash_test.go`; phase 76's roster is production-only (**375 files**) and its intersection with the EDIT roster is EMPTY. **THE SECOND HEADLINE — THE NAIVE RESCALE MANUFACTURES A THIRD VACUITY, AND NO DOCUMENT SAW IT.** `_ScatterBitesAffinity`'s `{20,28,16}` scaled x4 is `{80,112,64}` — **all three multiples of 16** ⇒ the tuple PASSES affinity, falls through to spread, still satisfies `err == nil`, and prints `--- PASS` while testing a different leg: **the one test that was HEALTHY would have been silently broken.** `{20,108,128}` used instead (sum 256, `20 mod 16 = 4`, `108 mod 16 = 12`). ⚠️ **The SPEC's vacuity MECHANISM is only two-thirds right** — affinity is evaluated INSIDE the per-element loop (`driver.go:284`), BEFORE conservation (`:291`), so `_ScatterBitesAffinity` and `_SubjectConservation` were **never vacuous**; the SPEC's COUNT (exactly two, exactly the two it named) is right, its stated REASON over-generalises. **EXECUTED AT THIS PLAN:** the measurement BUILT and GREEN (`CONTROL K=4: 74/2000 rate=0.03700` · `MEASURED K=16: 0/2000`, 0.61 s) · **break γ-SHARP FIRED** (frozen ports ⇒ **CONTROL RED, MEASURED still GREEN at 0/2000** — the measured leg's null-result status **MEASURED, not argued**) · γ-restore green after an observed red · **break β two-edit FIRED** (35x over the bar) · **β-single-edit DID NOT FIRE, as declared** (exit 0; `grep -rn '\bsourceIPs\b' --include='*.go' .` ⇒ **3 hits, all in the 0061 driver**) · `sourceIPs` 4->16 + the `driver_test.go` rescale, **6/6 PASS with each leg re-proved BY MEASUREMENT** · **six anti-vacuity breaks, one per test, each firing exactly ONE red** · ⚠️ **the OWED `127.0.0.12`-`.17` bind probe DISCHARGED: 16/16 PASS**, requested `LocalAddr` == observed on every arm · the collapse law over **2e5 real ring draws per K** with a random-key discriminator arm · the **full differential at MASTER: 119/119 PASS, 408.8 s**, `0061` subtest 3.42 s, first attempt, no flake, `comm -3` fixture-set vs subtest-set EMPTY · `BEHAVIOR_CONTRACT:1326` verified in scratch (**5746 -> 5746 lines, `diff` exactly `1326c1326`**, `% 16 == 0` intact) · every six-gate negative control observed RED one at a time. ⚠️ **`3^(1-K)` IS A CONSERVATIVE UPPER BOUND, NOT "THE" PROBABILITY** — p̂/analytic **0.949** at K=4 and **0.689** at K=8 over 2e5 real draws, while a random-key arm recovers 1.010/1.104 ⇒ the deficit belongs to the specific `xxHash64` key set, not the builder. ⚠️ **K=16 IS NOT MEASURED AND CANNOT BE AT THAT SCALE** (expected count 0.014; `0/200000` bounds p̂ ≲ 1.5e-5) — **7.0e-8 is analytic/extrapolated and must be stated as such.** ⚠️ **The CONTROL count is 74/2000, not the SPEC's 71/2000.** **SPEC §10 CARRIES FOUR ANCHOR ERRORS** — `README:58` is really **`:57`** (`:58` has no numeral at all), **`:114` and `:135` are MISSING** (`:135` carries TWO `64`s), and `driver_test.go` is **15 sites not 8**, ⚠️ two of them **executable `t.Fatal` STRING LITERALS**; **SPEC §4's collision claim is FALSE for `allSame`** (4 hits, 3 live Go in `package router` — safe by package scoping, but the stated evidence is wrong; helper renamed `collapseAllSame`); **SPEC §4's import prediction is refuted** (`fmt`/`math`/`math/rand/v2`, and `ringhash_test.go:3` is a single-line import needing a 1->N structural conversion); **`0062`/`0063`'s σ-band comment is at `:299`, not `:300`**. ⚠️ **AN AGENT DISAGREEMENT THE CONTROLLER RULED ON, RECORDED NOT DROPPED:** one agent reported the *"DETERMINISTIC/EXACT — not a σ-band"* sites need NO edit. **Ruled against it on the text** — the adjective governs the compound *"affinity + SPREAD"* while only the parenthetical is affinity-scoped, and spread is exactly the probabilistic leg this row fixes; the sites carry **none** of the stale numerals, which is why a numeral-keyed sweep cannot reach them. ⚠️ **`ROADMAP.md:138` (row 76) is BRAINSTORM-era and carries FOUR stale claims** the SPEC refuted (`4 -> 10`, `K=10 -> 5.1e-5`, β-as-single-edit *"THE PROOF"*, *"THE EXECUTABLE DELTA IS ONE INTEGER"*) — the IMPL's row-flip owes them. ⚠️ **The router's *"the ADR-0288 singleton greps return 2"* holds for only ONE of the three field-name tokens** — re-derived by execution at this PLAN, and the other two return MORE than 2 because §Recent's historical recaps also carry them. **The numbers are deliberately NOT written here: this line lives in the file being counted, so recording them would change them** — that is the ADR-0296 ¶3 / ADR-0297 ¶7 self-falsifying-count species, and it fired against this very sentence in draft. Re-run the greps yourself. The invariant that DOES hold is `STATE.md:7`'s own wording: exactly one **live** instance, in §Current. **NEVER "fix" any of these counts down** — the first hit is always the RULE at `:7`, so a close script taking `head -1` reads the rule, not the value. **Counts UNCHANGED, re-run MECHANICALLY in the worktree:** fixtures **119** · fuzzers **55** · stat surface **1205** (⚠️ DOCUMENTARY — no mechanical command, rides TWO recorded ledger gaps) · BackendKind tail **38** (⚠️ a TAIL VALUE; the file declares **39** constants, 0-38) · modules **2** (the single `go.mod` requires **67** = 18 direct + 49 indirect) · DECISIONS tail **ADR-0298 PROPOSED** (next-free **ADR-0299**). **NO deferred sentence narrowed — and this row narrows NOTHING, ever**: check (2) **STAYS 3 through the IMPL**. **Sentinel re-run MECHANICALLY (does NOT fire; `stop` NOT created):** (1) `NOT DONE: row 76` · (2) => **3** · (3) `NEVER OPENED: gRPC/Runtime/WASM`. ⚠️ **check (1)'s blind spot RE-DERIVED INDEPENDENTLY AGAIN: 108 data rows (`:31`-`:138`) / 104 matched / FOUR misses** — `00` (em-dash), `04` (DOT in slug), `28.1a`, `28.1b` (LETTER suffix); all four `done`. `next-prompt.txt` rolled to the **phase-76 IMPL** (**TRACKED despite `.gitignore`**; edited in this worktree).)
- **next-free ADR:** **`ADR-0299`** — DECISIONS.md tail is now **ADR-0298** (STATUS **PROPOSED**). `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1` -> `## ADR-0298`; `grep -c '^## ADR-0299'` -> **0**. **ADR-0298 — *"a probabilistic differential-fixture assertion carries a DERIVED margin and is verified by a MEASURED RATE, never by a pass-count"*** — §Context (**11 paragraphs**) drafted at THIS SPEC per ADR-0044-**as-used**; §Decision + §Consequences append IN PLACE at the phase-76 IMPL, **no renumber**. Block shape verified at this close: **1** `### Context` / **0** `### Decision` / **0** `### Consequences` / **1** retained italic footer / **0** `---` separators. **Precedent for a PROCESS ADR is established, not novel** — ADR-0045 (phase-split discipline), ADR-0106 (family-expansion shape), ADR-0288 (*"a navigation/correctness move, NOT a phase"*). ⚠️ **It carries NO whole-file grep count** — that species self-falsified in ADR-0296 ¶3, ADR-0297 ¶7 AND ¶9, and at the phase-76 BRAINSTORM it escalated from a wrong NUMBER to a **flipped termination-sentinel check**. Every count in it is line-scoped or stated with no number. ⚠️ **It claims NO family ordinal** (the family closed at row 54).

---

## Project counts at this commit

*(Live. Re-verify at session start — `git fetch` first — and update here at every stage close. Canonical commands inline.)*

⚠️ **ALL counts below were re-run MECHANICALLY in the stage worktree at this phase-76 BRAINSTORM close with the canonical commands — never copied forward.** **Phase 76 is docs-only at this stage: ZERO production `.go`, ZERO test `.go`, so every count is UNCHANGED from the phase-75 IMPL close.** The row's ANTICIPATED deltas at its own IMPL are **+0 across every axis, including +0 PRODUCTION `.go` FILES** — it is the first chartered row in this lineage whose production-code delta is expected to be exactly nothing. ⚠️ **The stat-surface total 1205 is DOC-SOURCED and was NOT re-derived mechanically** — there is no mechanical command, and the chain is known to be discontinuous in **TWO** places (the recorded `1200 -> 1201`, and `Phase 46.1b` closing at 1198 while `Phase 47.1` opens at 1200 — an unattributed +2). A future phase should COUNT rather than trust the chain; a **+0** row is a cheap place to do it, though phase 76 does not charter that.

- **active differential fixtures:** **119** — `ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0117-tracing-custom-tags-metadata-cluster` (phase 73). **Phase 75 landed +0**: `0110-tls-require-client-cert-false` was **EXTENDED** with a cross-side `fixture.StatsAsserter` (NO new YAML, NO new directory, NO new BackendKind, NO new port), so the count STAYS **119** and `0118` remains FREE. ⚠️ **Next-free REFERENCE port is `10450`, NOT `10118`** — ports are NOT fixture-index aligned. **The fixture now asserts what its own README confessed was infeasible, and BOTH sides were EXECUTED**: `ssl.handshake=2 ssl.no_certificate=1 ssl.fail_verify_error=1 ssl.fail_verify_no_cert=0`, `downstream_cx_total=3`, **identical on reference and subject**, with the discriminating asymmetry (`handshake=2` vs `no_certificate=1`) observed per side. Shape A over `/stats/prometheus`, keyed on the metric NAME with `envoy_listener_address` IGNORED — the NAMES are byte-identical cross-side while the labels differ (`0.0.0.0_10446` reference vs **`___20016`** subject: ⚠️ **the subject binds the IPv6 WILDCARD**). ⚠️ **`0111` was NOT given a value assertion, deliberately** — it runs `require_client_certificate: true` on both sides, so a success-path no-cert annotation reads **0 on every arm, structurally**, and a value pin would document a **vacuous `0 == 0`** as coverage; its PROSE gained the name in its NAMED-UNASSERTED list instead, because **both `0111` passages are CLOSED ENUMERATIONS** and a name in neither list reads as ASSERTED. **`0111`'s driver is byte-untouched, and its named-subset check was EXECUTED for the first time at this IMPL: PASS.** ⚠️ `var _ fixture.StatsAsserter` is a **TRIPWIRE, not the dispatch mechanism** — `0110` is only the **THIRD** fixture repo-wide to carry it, while **82** define an `AssertStats` method.
- **fuzzers:** **55** — `grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`. **Phase 75 landed +0** — the row consumes no new config field at all, so there is no parse arm and no fuzz-seed classification to flip (`reference_fuzzer_count_docs_drift`: an `f.Add` seed is not a fuzzer).
- **stat surface:** **1205** — BEHAVIOR_CONTRACT doc count. **There is NO mechanical counting command** (the contract says so itself); enforcement is per-phase `TestNoNewStat*` guards asserting a delta of ZERO, never the absolute total. **Phase 75 LANDED +1 (1204 -> 1205)** — `listener.<normalized-addr>.ssl.no_certificate`, the FOURTH listener-scope `ssl.*` name and the FIRST **SUCCESS-PATH** handshake annotation (⚠️ **NOT a synonym for `ssl.fail_verify_no_cert`, and the mis-mapping is wrong in BOTH directions** — one annotates an ACCEPTED handshake, the other books a REJECTED one; they are mutually exclusive on every connection). ⚠️ **The delta is a NAME-surface delta, NOT a flat per-deployment one:** registration rides the block that ALREADY gates on `rt.tlsMode`, so a **plaintext-only** deployment gains **ZERO** names, while a **QUIC** listener gains it permanently zero — **PARITY** with the reference, and structural (`Manager.Start` launches no accept loop for `kindQUIC`, so the Inc site is unreachable for a **name-independent** reason). ⚠️ **QUIC was NOT re-probed at phase 75** — the parity claim is INHERITED from phase 74's family-level probe, and that inheritance is stated in BEHAVIOR_CONTRACT rather than presented as a measurement. Ledger updated: `1204` -> `1205` at `BEHAVIOR_CONTRACT.md:831`/`:847`, plus a `**Phase 75 - 1204 -> 1205 (+1)**` line after the tail entry — **the ONLY line-adding edit in the file** (`5744 -> 5746`), so every by-line citation into the TLS subsection stays valid. ⚠️ **The new entry RECORDS BOTH ledger gaps and BACK-FILLS NEITHER** (fabricating a line would be inventing a record). Gate figures at this close: `grep -c '1204'` => **2** · `grep -c '1205'` => **3** · `grep -cE '^#{2,4} .*ADR-[0-9]+.*ADR-[0-9]+'` => **17** (was 16 — the `:1849` heading joined them, on the `:785` precedent). ⚠️ **`internal/statssink`'s four *"stays 1200 / 1196"* prose sites, stale since phase 49, are RECORDED-NOT-FIXED** — that package is on this phase's byte-untouched sha256 roster.
- **BackendKind tail:** **38** (`H2GoawayResponder`, `test/differential/fixture/fixture.go:614`). **Phase 75 landed +0** — `0110` was extended, not cloned, and it implements neither `BackendKindAware` nor `PerHostBackendKind`, so the runner takes the in-process `TCPEcho` branch. ⚠️ **This is a TAIL VALUE, not a count** — the file declares **39** constants (values 0-38, `TCPEcho = 0`). The lineage figure **38** is correct as written; do NOT 'fix' it to 39.
- **DECISIONS.md tail:** **ADR-0297** (next-free **ADR-0298**) — `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1`. **ADR-0297 is COMPLETE** — §Context drafted at the phase-75 SPEC (ADR-0044-**as-used**; ⚠️ ADR-0044 does NOT actually contain that discipline), §Decision + §Consequences appended IN PLACE at the phase-75 IMPL after the **RETAINED** footer, mirroring ADR-0295/0296. Block shape verified at this close: **1** `### Context` / **1** `### Decision` / **1** `### Consequences` / **1** retained footer; `grep -c '^## ADR-0298'` => **0**. **No renumber, no new ADR.** ⚠️ **Its OWN §Context ¶7 and ¶9 were CORRECTED IN PLACE while completing it** — ¶7's **self-falsifying whole-file grep** (the same species the phase-74 IMPL had just fixed in ADR-0296 ¶3, reproduced one ADR later) is restated with **NO number** and the searched token deliberately **NOT spelled**, because every restatement mints another counter-example; and ¶9's *"SELF vs OTHER-ADR"* form rule is replaced by **graft SCALE** after the n=7 population was re-enumerated first-hand. ⚠️ **¶9 ALSO said *"the only two instances in the file"* — which this row's own blockquote makes three** — so a pure append would have shipped a SECOND self-falsifying count in the commit that fixed the first. **The prescription has now outlived THREE wrong justifications: family, self-vs-other, scale.** ⚠️ **`ADR-0296` is NO LONGER byte-untouched** — its §Decision (g) carries the phase-75 correction as an INDENTED blockquote (`grep -c '^  > '` **2 -> 3**), **LEADING WITH WHAT SURVIVES** (no such counter was owed *in phase 74*, whose sole stat-asserting fixture is `require=true` on both sides — **the deferral was RIGHT**) and claiming **NO ORDINAL**.
- **go.mod modules:** the tracked lineage figure is **2** (the phase-61.2 count: `quic-go` direct + `qpack` indirect) — **NOT a repo total**; the single `go.mod` requires **67** (18 direct + 49 indirect). **Phase 75 landed +0** — `go mod tidy -diff` EMPTY and `git diff master -- go.mod go.sum` EMPTY. The row needed `crypto/tls` and `internal/stats`, both ALREADY imported by `internal/listener/manager.go`; **an import LINE is not a go.mod MODULE**, and there is no new sub-package, so `reference_new_subpackage_pulls_transitive_module` does not bite. ⚠️ **TEST-side imports grew by exactly four** (`log`, `math`, `net/http`, `strconv` in `0110`'s driver) — all stdlib, all test-side, **not a production-envelope violation**.
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` (see `ENVOY_TARGET.md`).
- **conformance:** h2spec 53/53; proxy-wasm all 10 families PASS.

---

## Recent lineage

*(The last FIVE stages only — enough for the router's "prior-phase lineage" read. Everything earlier is in `STATE_HISTORY.md`. These bullets are carried over VERBATIM; the newest mirrors §Current (**the phase-76 PLAN done** — the stage that found the SPEC's OWN linkage gate broken in BOTH directions at once, two further gates broken in ways new to this lineage, and a naive rescale that would have silently converted the one healthy unit test into a broken one), and the oldest (**phase-75 SPEC done**) was dropped as this list re-capped at five. The five now read: phase-76 PLAN · phase-76 SPEC · phase-76 BRAINSTORM · phase-75 IMPL · phase-75 PLAN.)*

- **prior active-phase:** `phase 76 (lb-ring-hash-spread-margin) PLAN done` (2026-07-26) — lifecycle **2 -> 3**; **ROW 76 STAYS `in-progress`**; **ROADMAP and DECISIONS BYTE-UNTOUCHED**. Fresh worktree `/home/esa/git/envoy-go-wt-p76plan` off master `98c27fc9`, branch `phase-76-plan`. File set: the new `PLAN.md` (a **NINE-task TDD spine T1-T9** with a **SIXTEEN-break roster**) + the new `PROGRESS.md` + `STATE.md` + `next-prompt.txt`; **ZERO production `.go`, ZERO test `.go` in the PLAN COMMIT.** **FIVE agents on disjoint remits, PRIVATE scratch, SEPARATE worktrees**, plus controller re-derivation of every load-bearing claim. **THIS PLAN EXECUTED MOST OF THE ROW, and THIRTEEN of the SIXTEEN breaks RAN here.** **THE HEADLINE — THE SPEC'S OWN LINKAGE GATE IS BROKEN IN BOTH DIRECTIONS AT ONCE.** It **FAILS OPEN** on empty captures (`[ "" = "" ]` is TRUE ⇒ **exit 0 on a tree where NEITHER constant exists** — rename, move or delete all read green; controller-reproduced) **AND FAILS CLOSED** on a correctly-synced tree (`grep -c 'collapseFixtureK'` ⇒ **2**, the second match being the measurement test's **OWN DOC COMMENT** ⇒ `DESYNC: sourceIPs=16 collapseFixtureK=16`, exit 1). ⚠️ **The document that warns *"a green gate is evidence only if you have seen it go red"* shipped a gate whose red arm was never fired.** REPLACED by a **`go/parser` Go test** in the fixture's own package — structurally immune (the parser sees declarations, not comments), **+0 exported symbols** — proved over the **FULL 2x2 cross-product** plus the empty-capture arm; ⚠️ **the (4,4)-passes arm is the load-bearing one**, since it proves the gate READS the test-side value rather than hardcoding 16. **TWO MORE BROKEN GATES, BOTH NEW TO THE LINEAGE:** ⚠️ **`go doc -all <pkgA> <pkgB>`'s fail-open is specific to the `./` PREFIX** — a BARE import path exits **1** (`doc: no symbol …`), a `./`-prefixed one exits **0** silently dropping the second argument **even when it names a directory that does not exist** ⇒ **NARROWS** `reference_gate_command_negative_control`; and ⚠️ **`gofmt -l` NEVER exits non-zero** (observed exit 0 while printing the unformatted file) ⇒ any gate chaining it with `&&` is **INERT — gate on OUTPUT**. Plus a **THIRD, unrecorded `impblock` defect**: it matches only `^import \($` and so emits ZERO lines for the two single-line `import "testing"` files — **blind to 2 of this row's 3 `.go` files**. ⚠️ **The phase-75 sha256 roster CANNOT be copied forward** — `internal/cluster/**` was byte-gated there and this row EDITS `internal/cluster/ringhash_test.go` (`comm -12` returns exactly that file); phase 76's roster is production-only (**375 files**), intersection with the EDIT roster **EMPTY**. **THE SECOND HEADLINE — THE NAIVE RESCALE MANUFACTURES A THIRD VACUITY, AND NO DOCUMENT SAW IT:** `{20,28,16}` x4 = `{80,112,64}`, **all three multiples of 16** ⇒ the tuple passes affinity, falls through to spread, satisfies `err == nil`, and prints `--- PASS` while testing a different leg — **the one test that was HEALTHY would have been silently broken.** `{20,108,128}` used instead. ⚠️ **The SPEC's vacuity MECHANISM is two-thirds right** — affinity is evaluated INSIDE the per-element loop (`:284`), BEFORE conservation (`:291`), so `_ScatterBitesAffinity` and `_SubjectConservation` were **never vacuous**; the COUNT is right, the stated REASON over-generalises. **EXECUTED:** the measurement BUILT + GREEN (`CONTROL K=4: 74/2000 rate=0.03700`, `MEASURED K=16: 0/2000`, 0.61 s) · **γ-SHARP FIRED** — frozen ports drove **CONTROL RED while MEASURED stayed GREEN at 0/2000**, so the measured leg's **NULL-RESULT status is MEASURED, not argued** · γ-restore green **after an observed red** · **β two-edit FIRED** (35x over the bar) · ⚠️ **β-single-edit DID NOT FIRE, as declared** (exit 0; `grep -rn '\bsourceIPs\b' --include='*.go' .` ⇒ **3 hits, all in the 0061 driver**) ⇒ **a two-edit β proves only the UNIT TEST's constant load-bearing** · `sourceIPs` 4->16 + the rescale, **6/6 PASS with every leg re-proved BY MEASUREMENT** · **six anti-vacuity breaks, one per test, each firing exactly ONE red** · ⚠️ **the OWED `127.0.0.12`-`.17` probe DISCHARGED: 16/16 binds PASS**, requested `LocalAddr` == observed on every arm (a read-then-write backend, pre-empting the `io.Copy` self-splice artifact that made the SPEC's first probe report ten bogus `i/o timeout`s) · the law over **2e5 real ring draws per K** with a random-key discriminator · the **full differential at MASTER: 119/119 PASS, 408.8 s**, `0061` 3.42 s, first attempt, no flake · `BEHAVIOR_CONTRACT:1326` verified in scratch (**5746 -> 5746 lines**, `diff` exactly `1326c1326`, `% 16 == 0` intact) · every six-gate negative control observed RED. ⚠️ **`3^(1-K)` IS A CONSERVATIVE UPPER BOUND** — p̂/analytic **0.949** at K=4, **0.689** at K=8, while a random-key arm recovers 1.010/1.104 ⇒ the deficit belongs to the specific `xxHash64` key set. ⚠️ **K=16 IS NOT MEASURED AND CANNOT BE AT THAT SCALE** (expected count 0.014) — **7.0e-8 is analytic/extrapolated.** ⚠️ **CONTROL is 74/2000, not 71.** **SPEC §10 CARRIES FOUR ANCHOR ERRORS** (`README:58`->**`:57`**; **`:114`/`:135` MISSING**, `:135` with TWO `64`s; `driver_test.go` **15** sites not 8, two of them **executable `t.Fatal` strings**), **SPEC §4's collision claim is FALSE for `allSame`** (3 live Go hits in `package router`; helper renamed `collapseAllSame`), **its import prediction is refuted** (`fmt`/`math`/`math/rand/v2`, and `ringhash_test.go:3` is a single-line import needing a 1->N conversion), and **`0062`/`0063`'s σ-band comment is `:299`, not `:300`**. ⚠️ **AN AGENT DISAGREEMENT THE CONTROLLER RULED ON:** one agent said the *"DETERMINISTIC/EXACT — not a σ-band"* sites need no edit; **ruled against it on the text** — the adjective governs the compound *"affinity + SPREAD"* while only the parenthetical is affinity-scoped, and those sites carry **none** of the stale numerals, which is exactly why a numeral-keyed sweep cannot reach them. ⚠️ **A CONTROLLER SELF-CORRECTION, recorded not quietly amended:** this stage's first STATE draft wrote the three ADR-0288 field-name tokens verbatim while reporting their counts — **and the act of recording them changed them**, the ADR-0296 ¶3 / ADR-0297 ¶7 species reproduced one stage later. The numbers are now stated as a property with no numerals. **Counts UNCHANGED:** fixtures **119** · fuzzers **55** · stat surface **1205** (DOCUMENTARY) · BackendKind tail **38** · modules **2** · DECISIONS tail **ADR-0298 PROPOSED** (next-free **ADR-0299**). **This row narrows NOTHING** ⇒ check (2) **STAYS 3 through the IMPL.** **Sentinel re-run MECHANICALLY (does NOT fire; `stop` NOT created):** (1) `NOT DONE: row 76` · (2) => **3** · (3) `NEVER OPENED: gRPC/Runtime/WASM`. ⚠️ **check (1)'s blind spot RE-DERIVED: 108 / 104 / FOUR misses**, all `done`. **Next -> the phase-76 IMPL** (break α, the full suite at K=16, `-race`, the prose sweeps, `BEHAVIOR_CONTRACT:1326`, ADR-0298 completion, row 76 -> `done`).

- **prior active-phase:** `phase 76 (lb-ring-hash-spread-margin) SPEC done` (2026-07-25) — lifecycle-state **1 -> 2**; **ROW 76 STAYS `in-progress`**; **ROADMAP BYTE-UNTOUCHED**. Fresh worktree `/home/esa/git/envoy-go-wt-p76spec` off master `6ef436ac`, branch `phase-76-spec`. File set: `DECISIONS.md` (ADR-0298 §Context) + `STATE.md` + the new `SPEC.md` + `next-prompt.txt`; **ZERO production `.go`, ZERO test `.go` in the SPEC commit.** **FOUR agents on disjoint remits, PRIVATE scratch, SEPARATE worktrees**, plus controller re-derivation of every load-bearing claim. **THE HEADLINE — *"THE EXECUTABLE DELTA IS ONE INTEGER"* IS FALSE.** `test/fixtures/0061-lb-ring-hash/driver/driver_test.go` (6 tests) hard-codes 64-summing tuples and is named in **NO** phase-76 document. At the raised constant `_Affinity` **FAILS**; **`_CollapseBitesSpread` and `_ReferenceConservation` go SILENTLY VACUOUS**, tripping subject-conservation before their own leg while still printing `--- PASS` — the first is the unit test for **this row's own assertion**. ⚠️ **THREE confirmations blind in the same direction**: a file-scoped `grep` over `driver.go`, a `git diff --stat` reading `1 file changed`, and a green `go test ./test/differential/`. ⇒ **a change-set measure is not a build measure.** All six tests re-proved BY MEASUREMENT to fire their own leg after rescaling to 256. **THE SECOND HEADLINE — THE CONSTANT WAS BELOW THE PROJECT'S OWN BAR: 10 -> 16.** K=10 is **3.89σ** against a recorded **4-5σ**; `0062-lb-ring-hash-http` and `0063-lb-maglev` fixed the byte-identical defect (same root cause, same `3·(1/3)^N`, same original N=4) at **16** = 5.27σ, `0062/README.md:57-63` calling it *"past the 5σ-equivalent flake-free margin"*. **`0061` was left behind; this row finishes a phase-36.2 migration.** **CONTROLLER SELF-CORRECTION, recorded:** the SPEC first derived 10 from a policy it invented — right arithmetic, wrong premise ⇒ **grep for a sibling that already derived one.** **DOCKET RESOLVED BY EXECUTION:** CXTOTAL — `upstream_cx_total` **256 on BOTH sides**, reference `upstream_rq_total` **256**, subject **0**, read via a **discriminating break** not a green PASS; gauges 1026/342/342 + `membership_total`=3 + `cx_active`=0 positively observed unmoved. LOOPBACK — **10/10** for `.2`-`.11` (⚠️ `.12`-`.17` UNPROBED — OWED). RUNTIME — **+0.158 s (+4.5%)** vs a SAME-SESSION control; the cross-session baseline reads **-0.002 s**, an artifact ⇒ **quote deltas against same-session controls.** VERIFY — measurement BUILT and RUN (0.62 s), its **vacuity MEASURED** (freezing the ports leaves the measured leg GREEN at `0/2000` while the control leg goes RED), the law measured over **1 000 000** trials with p̂ slightly BELOW `3^(1-K)` ⇒ **a CONSERVATIVE UPPER BOUND**; seed checked against 12 alternatives. SWEEP — `0061` is the **ONLY** underived reachable probabilistic margin in 119 fixtures. ADR — **YES**. ⚠️ **BREAK β IS NOT EXECUTABLE AS SPECIFIED** — `collapseFixtureK` and `sourceIPs` sit in different packages with no mechanical link, so reverting the fixture ALONE leaves the measurement **GREEN** (executed). β is a **TWO-EDIT** break and proves only the unit test's constant load-bearing; a **negative- AND positive-controlled linkage gate** closes the single-edit case. ⚠️ **`BEHAVIOR_CONTRACT.md:1326` IS owed an edit** (four `64`s + one `127.0.0.2..5`, one line, the only such line) — refuting the BRAINSTORM's *"no behavior changes ⇒ no contract edit"* default: **the contract documents the fixture's WORKLOAD too.** `% 16 == 0` must STAY. ⚠️ **An OPPOSITE-DIRECTION false claim in FOUR places** (*"DETERMINISTIC/EXACT — not a σ-band"* — true of affinity, FALSE of spread), structurally invisible to a numeral-keyed sweep. ⚠️ **The Load-balancing family was declared CLOSED at row 54** ⇒ row 76 and ADR-0298 claim **NO family ordinal**; the BRAINSTORM's *"the family is already open"* is a mis-description. **Counts UNCHANGED:** fixtures **119** · fuzzers **55** · stat surface **1205** (DOCUMENTARY) · BackendKind **38** · modules **2** · DECISIONS tail **ADR-0298 PROPOSED** (next-free **ADR-0299**). **This row narrows NOTHING, ever** — no `candidates:` sentence names a load-balancing candidate ⇒ check (2) **STAYS 3 through the IMPL.** **Sentinel re-run MECHANICALLY (does NOT fire; `stop` NOT created):** (1) `NOT DONE: row 76` · (2) => **3** · (3) `NEVER OPENED: gRPC/Runtime/WASM`. ⚠️ **check (1)'s blind spot RE-DERIVED: 108 / 104 / FOUR misses**, all `done`. **Next -> the phase-76 PLAN.**

- **prior active-phase:** `phase 76 (lb-ring-hash-spread-margin) BRAINSTORM done` (2026-07-25) — lifecycle-state **DONE -> 1**; **ROW 76 REGISTERED `in-progress`** at `ROADMAP.md:138`, RE-OPENING sentinel check (1) after its second-ever silent close. Fresh worktree `/home/esa/git/envoy-go-wt-p76bs` off master `c57b98b8`, branch `phase-76-brainstorm`. Docs-only: **ZERO production `.go`, ZERO test `.go`**; exactly THREE files edited (`ROADMAP.md`, `STATE.md`, `next-prompt.txt`) plus the new `BRAINSTORM.md`; **DECISIONS.md BYTE-UNTOUCHED**. **FOUR read-only re-costing agents at tip `c57b98b8` on disjoint remits with PRIVATE scratch** — one LIVE 23-arm reference fleet, one Go-toolchain handshake-probe fleet, two repo re-derivation — plus controller re-derivation of every load-bearing claim. **THE PICK, SELF-PICKED per the 2026-07-12 standing directive (smallest defensible candidate first):** give `0061-lb-ring-hash`'s spread assertion a **DERIVED statistical margin** — `sourceIPs` **4 -> 10** — plus a **seeded collapse-rate MEASUREMENT** in `internal/cluster/ringhash_test.go`, plus the prose sweep the fix falsifies. ~5-7 tasks, **+0 on every envelope axis including +0 PRODUCTION `.go` FILES**. **THE HEADLINE — THE INHERITED RECOMMENDATION'S PRIMARY JUSTIFICATION IS REFUTED BY ONE `grep -c`.** `fault.abort.grpc_status` rode three stages and two documents on the claim that it is *"the ONLY identified candidate that clears a sentinel check-(3) blocker — it opens the **gRPC** family"*. **It does not**: the family charters bridge · gRPC-Web · gRPC-JSON transcoding · interop conformance (`ROADMAP.md:187-189`) and a fault-injection knob delivers NONE of them; a row that cleared check (3) by writing the marker phrase into its own summary would be gaming the one failure mode a fail-SAFE check cannot detect. Its cost ALSO re-derived **10-13, not 7-9**. **THE DIAGNOSIS, DERIVED THREE INDEPENDENT WAYS THAT ALL AGREE:** the governing variable is **K = distinct hash keys = `sourceIPs` = 4**, NOT the 64 connections — `HashSourceIP` (`internal/cluster/hash.go:133`) **STRIPS THE PORT**, so 16 connections from one source IP are **ONE** key; and the ring is keyed on the endpoint address **INCLUDING its OS-ephemeral port** (`ringhash.go:88-91` + `runner_test.go:272`), so it is a **fresh random 3-way partition on EVERY run**. ⇒ **P = 3^(1−K) = 3.7% at K=4** — analytic **0.0370**, controller Monte Carlo (200 000 trials/K) **0.0365**, an agent's independent replica (60 000 trials/K, real `xxHash64`, sanity-checked against the fixture's own 1026/342 ring gauge) **0.0349**. At **K=10 -> 5.1e-5 (~730×)**. **`0062` needs NOTHING** — identical assertion text, but `hashValues = 16` ⇒ P ≈ 7e-8, **~500 000× safer**; the ENTIRE delta between a fixture that flaked three times and one that never has is **K**. ⚠️ **THE FLAKE'S OWN CERTIFICATION IS STATISTICALLY INVALID:** `0061/README.md:143-148` certifies *"overwhelmingly stable"* on **`-count=20` -> 20/20 PASS** and calls the ring *"fixed"* — both false, and `(1−0.0365)^20 ≈ 0.48`, so the check was **MORE LIKELY THAN NOT to pass even if the assertion were exactly as broken as it in fact is.** ⇒ **A PASS-COUNT IS NOT A MARGIN**, hence the deliverable is a MEASUREMENT. ⚠️ **THIS ROW COMMITTED THE EXACT FAILURE MODE IT DOCUMENTS, TWICE, IN ONE COMMIT.** The first draft quoted check (3)'s marker phrase inside the sentence explaining why quoting it would be gaming — and check (3) came back with **gRPC silently GONE**; the second occurrence quoted **the command and its result**, a claim its own landing falsifies (the ADR-0296 ¶3 / ADR-0297 ¶7 / ¶9 species, **escalated from a wrong NUMBER to a flipped TERMINATION SENTINEL**). Both removed; the token is now **deliberately NOT spelled anywhere in `ROADMAP.md`**. ⇒ **never write a sentinel's own matcher string into a file the sentinel greps** (`grep` cannot tell mention from use), and **RE-RUN every sentinel check AFTER the stage's own edits land** — the pre-edit run was clean and meaningless. **RIVALS RE-COSTED AT THIS TIP, none carried:** `Listener.stat_prefix` ~7-10 (blast radius a RED HERRING; reference behavior already landed at `BEHAVIOR_CONTRACT.md:1859`) · `upstream_cluster` ~7-9 but ~85-100 lines / ~18 files, its central premise **UNVERIFIED**, the inherited break CONFIRMED non-existent (`grep -c 'GetClusterName()' internal/cluster/manager.go` -> **0**), and five fixtures mis-attributing the gap to a **Lua bridge** that 0087 does not configure · `ssl.connection_error` **~10-13, NOT cheaper** — its recorded predicate is **INCOMPLETE** (an RST peer yields `*net.OpError`, for which BOTH `errors.Is(io.EOF)` and `errors.Is(io.ErrUnexpectedEOF)` are FALSE), needing `syscall.ECONNRESET` ⇒ **+1 PRODUCTION import**, and its roster is **INERT** for this leaf · **Runtime family ~10-14 — the only evaluated candidate that GENUINELY clears a check-(3) blocker**, with SIX landed ADR-anchored `runtime_key` PARSE-REJECT arms forward-pointing at it (ADR-0187 `DECISIONS.md:11280`, ADR-0195 `:12413`). **METHODOLOGICAL FOLD:** the session OPENED treating `fault.abort.grpc_status` as presumptive because three documents agreed — **agreement across documents is what a CARRIED claim looks like, not a VERIFIED one**; all three cites trace to ONE unexecuted assertion. **Sentinel re-run MECHANICALLY TWICE (worktree AND landed master post-push; does NOT fire; `stop` NOT created):** (1) `NOT DONE: row 76` · (2) => **3** · (3) `NEVER OPENED: gRPC/Runtime/WASM`. ⚠️ **check (1)'s blind spot RE-DERIVED: 108 data rows (`:31`-`:138`) / 104 matched / FOUR misses**, all four `done`. **Next -> the phase-76 SPEC** (settle the D-RHSM-* docket; ⚠️ **EXECUTE** D-RHSM-CXTOTAL rather than reasoning about it).

- **prior active-phase:** `phase 75 (tls-no-certificate-stat) IMPL done` (2026-07-25) — lifecycle-state 3 -> **DONE**; **ROW 75 FLIPS `in-progress` -> `done`** at the T10/T11 six-gate (ADR-0106, the SOLE leg — a SINGLE FLAT ROW, `reference_roadmap_split_phase_row_done`). Fresh worktree `/home/esa/git/envoy-go-wt-p75impl` off master `9f5d667b` (the phase-75 router commit), branch `phase-75-impl`; **EIGHT subagent dispatches over the 11-task spine -> 20 local commits -> ONE squash**, per `feedback_execution_style` / `feedback_subagents_no_push` / `feedback_push_to_origin`. **THE ROW DELIVERED:** `listener.<normalized-addr>.ssl.no_certificate`, a **SUCCESS-PATH annotation** registered inside the block that **ALREADY** gated on `rt.tlsMode` (**no new gate, no new classifier, no new registration function**) and Inc'd on the success fall-through under **`len(tlsConn.ConnectionState().PeerCertificates) == 0` ALONE, NO client-auth term**. **+1 stat (1204 -> 1205) / +0 fixtures (119) / +0 fuzzers (55) / +0 BackendKinds (38) / +0 modules (2) / +0 PRODUCTION imports / ZERO new packages / ZERO new exported symbols / ZERO new dependency edges** — the last three asserted BY COMMAND, per-package. **ALL TEN BREAKS EXECUTED; every one matched its PLAN-recorded outcome.** **THE HEADLINE REPRODUCED AT THE IMPL TIP:** Break D (unconditional Inc) fired **`TestServeConnection_SSLHandshakeIncrements`** — a **PHASE-74** test — through `assertSSLCrossProduct`'s NEGATIVE half **while the new positive arm PASSED**, and **Break D' (the same edit with `sslLeafRoster` reverted to three leaves) left the ENTIRE package GREEN** ⇒ **the one-line roster extension is the SOLE guard on this row's central pinned decision**, with D' additionally showing that the two exact-set spelling pins stay green throughout because they pin REGISTRATION, never INCREMENT. **Breaks E and F fired the byte-identical assertion at the identical line** — distinguishable by the EDIT, not the OUTPUT, and **recorded as such rather than claimed as separately discriminated.** Break C fired **exactly one line** with the three build-time predicates **SILENT** (phase-74 V1-M2 reproduced). **Break G STAYED GREEN** — the silent-staleness finding — and G''s restore is what makes G's green mean *"ran and passed"* rather than *"never ran"*. **Break I-b PASSED VACUOUSLY**, which is what proves the ABSENT check NECESSARY rather than merely firing. Break J's rename went GREEN **with the driver's log lines VANISHING**; with `var _` kept it is a COMPILE ERROR. **THE CROSS-SIDE MEASUREMENT RE-EXECUTED LIVE:** `0110` scraped `ssl.handshake=2 ssl.no_certificate=1 ssl.fail_verify_error=1 ssl.fail_verify_no_cert=0 (downstream_cx_total=3)` on **BOTH** sides, NAMES byte-identical with the address differing ONLY in the label (`0.0.0.0_10446` vs **`___20016`** — the subject binds the **IPv6 wildcard**), and the asserter was observed firing **inside the full suite**, which a green fixture alone cannot distinguish from one that never dispatched. **`0111`'s named-subset check was EXECUTED for the FIRST time (the PLAN verified it only by code read): PASS**, driver byte-untouched. **SIX-GATE FULLY GREEN**; **FULL DIFFERENTIAL `ok 396.517s`, 119/119 PASS / 0 SKIP / 0 FAIL** with the fixture-directory set and the subtest set `comm -3`-verified EQUAL; **`-race` over `./...` INCLUDING `./test/differential/` — discharging the PLAN's own open item — reported ZERO data races**; sha256 BYTE-UNTOUCHED roster **592 files / 0 mismatches / 0 missing**, its intersection with the mechanically-derived **13-file** EDIT roster **EMPTY**. **TWO FLAKES, BOTH CLASSIFIED ON EVIDENCE, NEITHER ABSORBED:** `0083-grpc-access-log-headers` failed a first full run at `bind: address already in use` **before any assertion** (startup class; isolate-green, `internal/filter/**` sha256-untouched, immediate full re-run 119/119); and **`0061-lb-ring-hash`'s spread assertion fired under `-race` — its THIRD recorded occurrence** (`internal/cluster/**` sha256-verified byte-untouched; isolate-green **4/4** under `-race`) ⇒ structurally not this row, **but the margin fix is now genuinely OWED** as a Load-balancing-family maintenance item. ⚠️ **NEITHER was on the PLAN's roster of six known flakes — a stage brief's flake list is not the index.** **FOURTEEN PLAN/SPEC CLAIMS REFUTED BY EXECUTION, and TWO of them are the PLAN's own gate COMMANDS:** the **import gate FAILS CLOSED** (`impblock` prints `FILENAME`, so a `/tmp` baseline vs an `internal/` HEAD differs on EVERY line — 31 removed / 31 added, **exit 1, on a +0-import tree**; the PLAN's own negative-control transcript prints no path prefix, proving it validated a **different, basename-normalised** command) — **RD-IMPGATE existed to replace a gate that failed OPEN, and its replacement failed CLOSED** · **`go doc -all <pkgA> <pkgB>` FAILS OPEN** (the second argument is read as a SYMBOL; only `<pkgA>` is emitted, **exit 0, empty stderr**) — it would have gone green over `internal/stats` **without reading it**, in the task whose second production file lives there · the PLAN's `git stash` baseline is **VACUOUS** on a clean worktree (it stashes nothing, so the diff compares HEAD to HEAD) · the PLAN's EDIT roster names **4** test files while the real delta is **13** · **RD-0110-STALE's *"exactly THREE stale sites, no fourth"* is REFUTED** — a **FOURTH** sits at `0110/expectations.yaml`'s *"(no StatsAsserter)"* parenthetical, **falsified by this row's own T5**, and structurally invisible to a sweep that greps the STALE vocabulary · **T9 found a PLAN MISS its own roster could not see** — a comment saying *"keeping **both** Inc points"*, **already wrong before this row** (phase 74 landed three) and invisible to a *"three"*-shaped grep · **RD-BC-TOTALS' scratch figure contradicts the PLAN's own inline expectation** (`grep -c '1204'` goes 3 -> **2**, not 3 -> 1), and T8 Step 3's *"expect `1201` UNCHANGED"* is contradicted by T8 Step 2's own mandate to record the gap — **two numbers inside one PLAN disagreeing** · `ProbeAdmin` is **`:558`**, not the `:554` with which the PLAN corrected the cell's `:552` — **T5 grew the driver and re-staled the correction** · the row-75 cell carried **FIVE** stale claims, not four · `manager_test.go:4466`/`:4491-4492` **do not exist at this tip** (T3's variadic rewrite replaced both with roster-relative language correct at ANY family size) · `wantNames` is `:230-234` not `:231-235` · `0111/README.md`'s bullet is `:168-175` not `:167-174`, **one PLAN error propagated into TWO tasks** · and **anchor drift ran +1 to +168, NON-MONOTONIC**, so a single "+N" correction would have mis-targeted two of three regions. **ADR-0297 COMPLETED IN PLACE** (PROPOSED -> COMPLETE; §Decision + §Consequences after the RETAINED footer; tail stays ADR-0297, next-free ADR-0298) **and CORRECTED**: ¶7's **SELF-FALSIFYING grep** restated with **NO number** and the searched token deliberately **NOT spelled** (every restatement mints another counter-example — post-edit it occurs **exactly once** in the file), and ¶9's *"SELF vs OTHER-ADR"* rule replaced by **graft SCALE** after the **n=7** population was re-enumerated first-hand (`:17211` sits inside ADR-0294 and is a phase-**73** correction to a DIFFERENT landed ADR rendered **INLINE**). ⚠️ **¶9 ALSO carried *"the only two instances in the file"*, which THIS ROW'S OWN blockquote makes three** — so a pure append with ¶9 untouched would have shipped a **SECOND self-falsifying count in the very commit that fixed the first.** **The prescription has now outlived THREE wrong justifications: family, self-vs-other, scale.** **ADR-0296 §Decision (g) CORRECTED** via an INDENTED blockquote (`grep -c '^  > '` **2 -> 3**, leading bytes `od -c`-verified identical to both precedents), **LEADING WITH WHAT SURVIVES** — no such counter was owed *in phase 74*, whose sole stat-asserting fixture is `require=true` on both sides, **so the deferral was RIGHT and phase 74's row correctly scoped** — phrased as a documentary finding claiming **NO ORDINAL**. **BEHAVIOR_CONTRACT: eleven IN-PLACE rewrites + ONE ledger line** (`5744 -> 5746`, the only line-adding edit), every one of `:1849/:1851/:1853/:1855/:1857/:1859/:5002` **UNMOVED**; `:962` **`cmp`-verified BYTE-IDENTICAL** with its offsets re-derived (**627 CHARACTER / 630 BYTE**, 1002 chars / 1007 bytes) — **a FOURTH challenge survived**; the `:1849` heading **EXTENDED** on the `:785` precedent (**SPEC §9's *"no two-ADR heading precedent exists"* REFUTED — 16, now 17**); **`three-fifths` REMOVED rather than bumped** and restated as an ENUMERATION so no fourth wrong denominator was minted; **BOTH ledger gaps recorded, NEITHER back-filled.** **The ROADMAP narrow edits the RETIRED clause, not the candidates list** — check (2) **STAYS 3** by both forms, terminator still `force-trace.`, **interior periods 0 before and after**, verified in a `/tmp` scratch copy first and then on the real file. **Sentinel re-run MECHANICALLY TWICE (worktree AND landed master post-push): does NOT fire; `stop` NOT created** — (1) **SILENT**, as predicted at the PLAN and confirmed here (row 75 was the last non-`done` chartered row); (2) => **3**; (3) `NEVER OPENED: gRPC/Runtime/WASM`. ⚠️ **check (1)'s blind spot RE-DERIVED INDEPENDENTLY: 107 data rows (`:31`-`:137`) / 103 matched / FOUR misses**, all four `done`. **Next -> the phase-76 BRAINSTORM** (the roller SELF-PICKS; `fault.abort.grpc_status` is the recommended opening — the only identified candidate that clears a sentinel check-(3) blocker; ONE probe owed).

- **prior active-phase:** `phase 75 (tls-no-certificate-stat) PLAN done` (2026-07-25) — phase-75 PLAN done. Lifecycle-state **2 -> 3**; **ROW 75 STAYS `in-progress`**; **ROADMAP and DECISIONS BYTE-UNTOUCHED**. Fresh worktree `/home/esa/git/envoy-go-wt-p75plan` off master `cedd2f27`, branch `phase-75-plan`. Docs-only: **ZERO production `.go`**, **EXACTLY TWO files in the phase-directory delta** — the new `PLAN.md` (an **11-task TDD spine T1-T11** with a **TEN-break roster**) and `PROGRESS.md`. **SEVEN agents at tip `cedd2f27` on disjoint remits — FOUR read-only re-derivation agents and THREE BUILD-BY-EXECUTION agents in SEPARATE git worktrees** (`phase-75-plan-v1/v2/v3`, private scratch, local commits only, never pushed), plus controller re-verification of every load-bearing correction. ⚠️ **THIS PLAN EXECUTED MOST OF THE ROW: SPEC §16's closing *"NO BUILD OF ROW 75 EXISTS"* is no longer true**, and **EIGHT of the TEN breaks RAN here.** **THE HEADLINE — THE SPEC AND THE ROUTER ARE BOTH WRONG ABOUT WHERE THIS ROW'S DISCRIMINATING BREAK LIVES.** Both state the new positive unit arm *IS* the discriminating break for the pinned `len(PeerCertificates)==0` predicate. **Break D fires the PHASE-74 mTLS arm instead** — `ssl.no_certificate = 1, want 0 — only [handshake] may move on this arm`, via `assertSSLCrossProduct`'s NEGATIVE half — **while the positive arm PASSES**, because an unconditional Inc still leaves the counter at 1 on the arm that wants 1. **Break D′, the decisive stacked control** (same edit, `sslLeafRoster` reverted to three leaves) leaves the **ENTIRE package GREEN, `ok … 3.233s`** ⇒ **the one-line roster extension is the SOLE guard on the row's central pinned decision.** Controller-reproduced end-to-end. ⇒ positive arm catches *never-Inc'd* and *mode-gated*; roster extension catches *unconditional*; **the row needs BOTH.** **THE SECOND HEADLINE — THE FIRST SUBJECT-SIDE MEASUREMENT, AND EXACT CROSS-SIDE AGREEMENT:** `0110` gained a cross-side `StatsAsserter` and RAN against the live reference — `ssl.handshake=2 ssl.no_certificate=1 ssl.fail_verify_error=1 ssl.fail_verify_no_cert=0 (downstream_cx_total=3)` on **BOTH** sides, metric NAMES byte-identical with the address differing ONLY in the label (`0.0.0.0_10446` vs `___20016` — ⚠️ the subject binds the **IPv6 wildcard**, caught by execution), and the **FULL differential suite `ok 399.675s`, exit 0, no flake**. **FOURTEEN SPEC/router claims REFUTED**, incl.: the fast-failure `ssl.*` suppression is **REFERENCE-ONLY** (envoy-go accounts `ssl.*` at step 6, before the step-7 upstream dial) · the **ABSENT check does not guard what SPEC §8.2 says** (deleting an Inc'd counter's registration SIGSEGVs the subject and the run dies at `structuralCheck` before `AssertStats`) · **the tree goes RED at the production change** via two `reflect.DeepEqual` exact-set pins, so T1 orders the guard FIRST · **a stale `-run` on a UNIT test prints `ok … [no tests to run]` and EXITS 0** (a NEW SHAPE of `reference_differential_run_selector`) · the registration-delete break **destroys its own evidence** full-package · `registerListenerMetrics`' doc starts at **`:351`** not `:358` · the SPEC's three `assertSSLCrossProduct` "callers" are **test-func DECLARATION lines** · `t.Error` not `t.Errorf` · `mkDownstreamTSInline` takes **`string`** so the SPEC's call **does not compile** · **SPEC §9's *"no two-ADR heading precedent exists"* is REFUTED — there are 16** · **check (1)'s blind spot is 107/103/4, not 106/102/4**, and its provenance claim is false · *"three-fifths"* was **already wrong at the phase-74 tip** so it is REMOVED not bumped · the ledger is discontinuous in **TWO** places · `registry.go` has **FIVE** panic sites, not three (**a correction of this PLAN's own agent**) · and **ADR-0297's OWN ¶7 and ¶9 are DEFECTIVE** — ¶7's grep claim is **SELF-FALSIFYING**, **reproducing one ADR later the exact species the phase-74 IMPL had just fixed in ADR-0296 ¶3**, and ¶9's *"SELF vs OTHER-ADR (n=4)"* rule is refuted by an **n=7** population where `:17211` is a phase-73 correction to a DIFFERENT landed ADR rendered INLINE; the surviving discriminator is **graft SCALE** and the indented prescription has now outlived **THREE** wrong justifications. **A CONTROLLER DECISION THAT REVERSED ITSELF, RECORDED NOT QUIETLY AMENDED:** the `0111` question was decided ADD-to-rosters, then reversed to prose-only once two later findings landed — **only `0110` of the NINE TLS-downstream fixtures is `require=false`**, so a `0111` value pin would document a vacuous `0 == 0`. **Counts UNCHANGED:** fixtures **119** / fuzzers **55** / stat surface **1204** / BackendKind **38** / modules **2** / DECISIONS tail **ADR-0297** PROPOSED. **Sentinel re-run MECHANICALLY TWICE (worktree AND landed master post-push; does NOT fire; `stop` NOT created):** (1) `NOT DONE: row 75` · (2) => **3** · (3) `NEVER OPENED: gRPC/Runtime/WASM`. `next-prompt.txt` rolled to the phase-75 IMPL (**TRACKED despite `.gitignore`**).




## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
