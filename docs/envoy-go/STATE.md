# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

> **This file is a POINTER, not an archive** — `BOOTSTRAP_PROMPT.md` §4: *"STATE.md — pointer: active phase, last commit, next action."* It carries the live pointer, the live counts, and a SHORT recent lineage. **Full stage-by-stage history lives in `STATE_HISTORY.md`** (archived at ADR-0288; **nothing was deleted** — the pre-move file is preserved there verbatim, byte-for-byte).
>
> **⚠️ THE RULE ADR-0288 EXISTS TO ENFORCE: there is EXACTLY ONE live `next-skill:`, `lifecycle-state:`, `next-free ADR:`, and count block IN THIS FILE, and they live in §Current pointer directly below.** Before that move, ~330 lines of prepended backlog had buried TWO fossilised "current state" blocks whose `next-skill:` still read *"the 42.2 hedging BRAINSTORM"* and *"the 46.2 SPEC"* while the live phase was **66** — so `grep 'next-skill:' STATE.md` returned **two stale hits and ZERO live ones**, and `grep 'fuzzers:'` returned **48** and **42** when the true count was **55**. **A session may trust a grep of this file again. Keep it that way: when you advance the pointer, EDIT §Current pointer IN PLACE — never prepend a new block above it.**

---

## Current pointer

*(The ONLY live "what next" in this repo. Update IN PLACE at every stage close.)*

- **active-phase:** `phase 70 (tracing-custom-tags-metadata) BRAINSTORM done` (2026-07-21)
- **phase-directory:** `docs/envoy-go/phases/70-tracing-custom-tags-metadata/` (`BRAINSTORM.md` present; **row 70 is `in-progress`** — registered at the phase-70 BRAINSTORM commit per the §Schema invariant; flips `done` at the phase-70 IMPL six-gate)
- **lifecycle-state:** `1` (BRAINSTORM landed). **Row 70 registered `in-progress`** at this BRAINSTORM commit (§Schema; flips `done` at the phase-70 IMPL six-gate, the SOLE leg — ADR-0106). Subject: the tracing `custom_tags` `metadata` type, `REQUEST` MetadataKind only — the FOURTH tracing `custom_tags` row and the LAST `CustomTag` source type, over the landed `internal/dynamicmetadata.Bucket`. The Observability family STAYS OPEN. **Next → the phase-70 SPEC** (RE-DERIVE the BRAINSTORM; run the D-CTM-* probe arms).
- **next-skill:** **the phase-70 SPEC** — RE-DERIVE the BRAINSTORM at tip (re-open every §6 edit site + the go-control-plane `CustomTag_Metadata`/`MetadataKey`/`MetadataKind` proto types + the relevant PGV lines), run the D-CTM-* live-probe arms against `envoyproxy/envoy:contrib-v1.37.2` (D-CTM-WIRE / D-CTM-ZIPKIN-WIRE / D-CTM-VALUE-SERIALIZE / D-CTM-DEFAULT / D-CTM-PATHWALK / D-CTM-REJECT — fresh container per arm, a Lua-set metadata writer served-this-arm), pin D-CTM-CONFIG-SEAM + D-CTM-BUCKET-SEAM + D-CTM-FIXTURE, and DRAFT ADR-0292 §Context (the DECISIONS tail flips **ADR-0291 → ADR-0292** at the SPEC commit per ADR-0044). Fresh worktree off master (`feedback_git_worktrees`); docs-only apart from the DECISIONS §Context append. Row 70 STAYS `in-progress`; NO deferred sentence narrowed at the SPEC (rolls OUT at the IMPL, phase-57 precedent). **Scope confirmed at the BRAINSTORM:** `REQUEST` kind accepted; `ROUTE`/`CLUSTER`/`HOST` reject loudly (envoy-go-strict); +0 stats/packages/modules anticipated; ONE new OTLP fixture (a Lua-set positive arm + a `default_value` arm; a Zipkin dir weighed).
- **last-commit:** the phase-70 BRAINSTORM squash (`git log --grep 'phase 70'`; locate by SUBJECT, never by position). Prior: the phase-69 IMPL squash + its docs router-roll.
- **last-updated:** 2026-07-21 (phase-70 BRAINSTORM done — **row 70 registered `in-progress`**; `BRAINSTORM.md` written; the `environment` first-pick RE-DERIVED STALE at tip and corrected to `metadata` — the sole remaining `CustomTag` type; the full rejected roster adjudicated in §2.1; docs-only, ZERO production `.go`)
- **next-free ADR:** **`ADR-0292`** — DECISIONS.md tail is **ADR-0291** (COMPLETE; UNTOUCHED at this BRAINSTORM). ADR-0292 §Context drafts at the phase-70 SPEC per ADR-0044 (the tail flips ADR-0291 → ADR-0292 there); §Decision + §Consequences land at the phase-70 IMPL IN PLACE. `grep -c '^## ADR-0291'` → 1, `grep -c '^## ADR-0292'` → 0.

---

## Project counts at this commit

*(Live. Re-verify at session start — `git fetch` first — and update here at every stage close. Canonical commands inline.)*

- **active differential fixtures:** **115** — `ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0113-stats-sink-otlp-knobs` (`0112-stats-sink-otlp`@10448 + `0113-stats-sink-otlp-knobs`@10449 added at phase 69).
- **fuzzers:** **55** — `grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`. Phase 66 added SEEDS only (a new `downstream-sds` dispatch side in `FuzzTLSContextParse`) ⇒ **+0**.
- **stat surface:** **1201** — BEHAVIOR_CONTRACT doc count. **There is NO mechanical counting command.** Phase 66 was **+0**.
- **BackendKind tail:** **38** (`H2GoawayResponder`).
- **DECISIONS.md tail:** **ADR-0291** (next-free **ADR-0292**) — `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1`. ADR-0291 is **COMPLETE** — §Context anchored at the phase-69 SPEC (the tail flipped ADR-0290 → ADR-0291 at that commit per ADR-0044); §Decision + §Consequences landed at the phase-69 IMPL IN PLACE (STATUS banner COMPLETE; no new ADR, no renumber).
- **go.mod modules:** the tracked lineage figure is **2** (the phase-61.2 count: `quic-go` direct + `qpack` indirect) — **NOT a repo total**; the single `go.mod` requires **67** modules. Phase 66 was **+0**.
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` (see `ENVOY_TARGET.md`).
- **conformance:** h2spec 53/53; proxy-wasm all 10 families PASS.

---

## Recent lineage

*(The last FIVE stages only — enough for the router's "prior-phase lineage" read. Everything earlier is in `STATE_HISTORY.md`. These bullets are carried over VERBATIM; the newest mirrors §Current (the phase-70 BRAINSTORM done), and the oldest (phase-68 IMPL done) was dropped as this list re-capped at five.)*

- **prior active-phase:** `phase 70 (tracing-custom-tags-metadata) BRAINSTORM done` (2026-07-21) — lifecycle-state 0 → 1; the roller's AUTONOMOUS self-pick (no human pick per the 2026-07-12 standing directive) opens the next Observability-family row; docs-only, ZERO production `.go`; fresh worktree `.worktrees/phase-70-brainstorm` off master `33ed4c05`, branch `phase-70-tracing-custom-tags-metadata-brainstorm`, per `feedback_git_worktrees`. **Subject:** `tracing-custom-tags-metadata` — the tracing `custom_tags` `metadata` type scoped to the `REQUEST` MetadataKind, the SIXTEENTH Observability-family row + the FOURTH tracing `custom_tags` row + the LAST `CustomTag` source type (after literal @ 59, request_header @ 62, environment @ 63): lifts the `custom_tags metadata type unsupported` reject (`internal/tracing/config.go:214-215`) and resolves a `REQUEST`-kind dynamic-metadata value onto the ingress span over the LANDED `internal/dynamicmetadata.Bucket` (`Get(filterName, key)`), emitted on BOTH the OTLP + Zipkin exporters via the phase-46 `Span.Attrs []KV` seam; the `ROUTE`/`CLUSTER`/`HOST` kinds reject loudly with distinct substrings (envoy-go-strict, ADR-0080). **Why this pick (the FULL rejected roster adjudicated in BRAINSTORM §2.1):** the roller's FIRST pick — the `environment` type — was RE-DERIVED STALE at tip by a DIRECT READ (`config.go:208-213` shows `environment` + `request_header` already landed at phase 62/63; `reference_deferred_candidate_cost_restale`), so the sole remaining `CustomTag` type is `metadata`; the rivals: the `ssl` handshake-outcome stat family (ADR-0286 C3 framework-surgery + a NEW stat surface breaking the +0-stat streak; the verify-failure taxonomy needs a per-chain TLS callback) · `spawn_upstream_span` (a second span) · `http_service` (a new OTLP-over-HTTP transport) · force-trace (internal-request detection unbuilt) · a 6th stats sink (hystrix admin-streaming / wasm runtime — off the landed Flusher/Submit substrate) · the RBAC `require=false` fixture arm (engine complete ⇒ a RIDER, not row-sized) · DataSource `environment_variable` (SPEC-63 D-ENV-HARNESS blocked) · `watched_directory` (live per-chain TLS reconfig unbuilt) · HTTP-3 + xDS candidates · opening a never-opened family. **The landed substrate (re-derived this session):** the phase-59/62/63 `parseCustomTags`/`ResolveCustomTags`/`Span.Attrs` pipeline + `internal/dynamicmetadata.Bucket.Get` + the Lua filter (`internal/filter/http/lua/metadata.go:315` `Bucket.Set`) as the cross-side-deterministic fixture WRITER for the positive arm. **Envelope:** ZERO new production packages (all edits in `internal/tracing` + `internal/filter/hcm`, both existing; `internal/dynamicmetadata` CONSUMED not modified) / +0 stats (a span attribute, not a stat) / +0 fuzzers (a SEED to `FuzzHCMConfigParse`) / +0 BackendKinds / +0 go.mod modules (`structpb` already imported by `internal/dynamicmetadata`); ONE new OTLP fixture anticipated (a Lua-set positive arm + a `default_value` arm; a Zipkin dir weighed) ⇒ fixtures 115 → 116 (or 117). **BRAINSTORM-time open questions to the SPEC:** D-CTM-SCOPE · D-CTM-PROTO · D-CTM-WIRE · D-CTM-ZIPKIN-WIRE · D-CTM-VALUE-SERIALIZE · D-CTM-DEFAULT · D-CTM-PATHWALK · D-CTM-CONFIG-SEAM · D-CTM-BUCKET-SEAM · D-CTM-REJECT · D-CTM-FIXTURE/D-CTM-FIXTURE-SOURCE · D-CTM-FUZZSEED · D-CTM-SPLIT (SINGLE FLAT ROW anticipated, ~10–13 tasks; ADR-0045 armable). **Row 70 registered `in-progress`** at this stage-close commit per the ROADMAP §Schema invariant; flips `done` at the phase-70 IMPL six-gate (ADR-0106, the SOLE leg). **This pickup IS named in the live Observability `candidates:` sentence (`tracing custom_tags (\`metadata\`)`) — NO sentence is narrowed at ANY pre-IMPL stage (the phase-57 precedent); it rolls OUT at the phase-70 IMPL row-done edit.** **Counts UNCHANGED at this BRAINSTORM (docs-only; re-run MECHANICALLY in the worktree at this close):** fixtures **115** (numeric tail `0113-stats-sink-otlp-knobs`) · fuzzers **55** · stat surface **1201** · BackendKind **38** · DECISIONS tail **ADR-0291** (COMPLETE; next-free **ADR-0292**) · go.mod modules **2**. **Sentinel re-run MECHANICALLY at this close (does NOT fire; `stop` NOT created):** (1) prints `NOT DONE: row 70` (the registration re-opens check (1)); (2) prints THREE live "candidates:" sentences (`grep -cE 'remaining deferred \(not-yet-chartered\) candidates:'` ⇒ **3**); (3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`. **Next → the phase-70 SPEC** (RE-DERIVE the BRAINSTORM rather than execute it; the D-CTM-* probe arms + the ADR-0292 §Context draft; the owed roster is in §Current pointer's next-skill line).

- **prior active-phase:** `phase 69 (stats-sink-otlp) IMPL done` (2026-07-20) — lifecycle-state 3 → DONE; **ROW 69 FLIPS `in-progress` → `done`** at this IMPL six-gate (ADR-0106, the SOLE leg — a SINGLE FLAT ROW, `reference_roadmap_split_phase_row_done`). Fresh worktree `.worktrees/phase-69-impl` off master `b49b99b0`, branch `phase-69-stats-sink-otlp-impl`; subagent-driven (`feedback_execution_style`) — one implementer per task + a per-task adversarial reviewer, each committing LOCALLY only (`feedback_subagents_no_push`), controller squash-push at close (`feedback_push_to_origin`). **The 11-task TDD spine landed:** T1 (`742c0c29`) grpcclient `OTLPMetricsClient` (unary `NewOTLPLogsClient` clone over `colmetricspb`, `Export` narrowed to `error`) · T2 (`43cc7a28`) `internal/statssink/otlp.go` `OTLPMetricsSink` + the `otlpMetricsClient` seam (statssink stays grpcclient-free) + the mapping (Counter→cumulative monotonic `Sum`/Gauge→`Gauge`, `telemetry.sdk.*` resource, present-but-empty scope, nil→TRUE INDEPENDENT knobs via `stats.ExtractTags`, `prefix`, `deltaState.apply` in the writer→DELTA + startTime chaining, correct-ns) + the UNARY bounded-channel writer + `TestNoNewStat_OTLPRegistrationGuard` · T3 (`cb61075c`) the `bootstrap.go` parse arm (`openTelemetrySinkTypeURL` descriptor blank-import; `parseOpenTelemetrySinkConfig`; the fifth dispatch arm, reject roster 4→5; nil→TRUE wrappers; histogram reject; the FREE version-skew reject via `DiscardUnknown:false`) — **RE-DERIVATION FINDING: the pinned OTLP `SinkConfig`/`GrpcService` have NO `transport_api_version` field ⇒ that reject is the FREE strict-layer reject too, NOT hand-written** · T4 (`59e30925`) `main.go` gate+sub-loop · T5 (`1ec08034`) `test/helpers/otlpmetrics` receiver ((name, sorted-attrs)-keyed order-insensitive) · T6 (`133dac4a`) fixture `0112`@10448 (default) — **EXTENDED the T5 receiver ADDITIVELY (`Datapoints()`/`ExportCount()`) to disambiguate the app cluster from the sink's OWN cluster under tag extraction (a cross-task discovery from the T5 review)**; breaks L/M/N + the order-insensitive NON-break control (which caught + fixed a latent driver flake) · T7 (`cd447fac`) fixture `0113`@10449 (knobs: deltas+prefix+both-false) — the delta-SUM stability barrier proven load-bearing (the O+P masking pair); `otlp.go` byte-restored · T8 (`fc127900`) fuzz seed + dispatch-verify (the descriptor-blank-import trap confirmed; count 55→55) · T9 (`c00c77ad`) BC B1–B2 (new OTLP section + the sibling-reject example swapped OFF OTLP-metrics to `envoy.stat_sinks.hystrix`) · T10 six-gate ALL GREEN · T11 ADR-0291 COMPLETED IN PLACE + this roll. **Every task RE-DERIVED its anchors at the IMPL tip (a PLAN is not evidence); each got a per-task adversarial review (SPEC ✅ / quality Approved), all folded findings minor except the two cross-task carries (empty-scope, receiver-collision) which were resolved.** **Six-gate evidence:** gofmt SILENT · vet 0 · build 0 · `go mod tidy -diff` EMPTY + `go.mod`/`go.sum` NO DIFF vs master (+0 modules) · golangci-lint 0 · the FULL **115-dir** differential `ok 386.265s` EXIT 0 · cycle guard `go list -deps ./internal/statssink` = `internal/stats`+`internal/statssink` ONLY (no grpcclient edge) · `-race` green on statssink/grpcclient/bootstrap/otlpmetrics · envelope audit: 4 functional production files (`internal/statssink/otlp.go` new + `internal/bootstrap/bootstrap.go` + `internal/grpcclient/grpcclient.go` + `cmd/envoy-go/main.go`), `internal/xds`/`internal/tls`/`internal/boot`/`internal/listener` + the landed `internal/statssink/{sink,delta,label,mapping,flusher}.go` BYTE-UNTOUCHED. **Exit counts:** fixtures **113 → 115** (tail `0113-stats-sink-otlp-knobs`) / fuzzers **55 (+0)** / stat surface **1201 (+0)** / BackendKind **38 (+0)** / DECISIONS tail **ADR-0291** COMPLETE (next-free **ADR-0292**) / go.mod modules **2 (+0)**. **Sentinel re-run MECHANICALLY (does NOT fire; `stop` NOT created):** (1) prints NOTHING (row 69 `done` — every chartered row `done`); (2) prints THREE `candidates:` sentences (`grep -cE 'remaining deferred \(not-yet-chartered\) candidates:'` ⇒ **3**); (3) prints `NEVER OPENED: gRPC/Runtime/WASM`. Checks (2)+(3) print ⇒ abundant work remains. **Next → the phase-70 BRAINSTORM** (the roller SELF-PICKS per the 2026-07-12 standing directive).

- **prior active-phase:** `phase 69 (stats-sink-otlp) PLAN done` (2026-07-20) — lifecycle-state 2 → 3; docs-only (`PLAN.md` + `PROGRESS.md`, the ONLY two files in the squash's phase-directory delta — ZERO production `.go`, DECISIONS UNTOUCHED); fresh worktree off master `0ea2ec20` (== the phase-69 SPEC squash; NO parallel stream — the production tree is byte-identical to what the SPEC re-derived), branch `phase-69-stats-sink-otlp-plan`, per `feedback_git_worktrees`; subagent-driven: a read-only re-derivation agent re-opened EVERY SPEC §11 anchor at `0ea2ec20` (**ZERO unacknowledged drift — every anchor EXACT**, unlike phase 68's RD-LINES boundary drift) + extracted the exact clone skeletons (the `NewOTLPLogsClient` stanza, the `parseMetricsServiceConfig` reject template, the streaming-vs-unary mapping site `sink.go:198-202`, the six-getter `SinkConfig` descriptor, the `otlptrace` receiver) → the 11-task TDD spine (T1–T11) drafted → TWO independent adversarial verifiers in PRIVATE scratch (`reference_parallel_subagents_private_scratch`) → an amendment. **Row 69 STAYS `in-progress`** (flips at the IMPL six-gate). **The deliverable: an 11-task SINGLE-FLAT-ROW spine** — T1 grpcclient `OTLPMetricsClient` · T2 `internal/statssink/otlp.go` `OTLPMetricsSink` + seam + mapping + the UNARY writer + `TestNoNewStat_OTLPRegistrationGuard` · T3 `bootstrap.go` parse arm (descriptor blank-import; fifth dispatch arm; reject 4→5; nil→TRUE wrappers; histogram reject; free version-skew reject) · T4 `main.go` gate+sub-loop · T5 `test/helpers/otlpmetrics` receiver · T6/T7 fixtures `0112`/`0113` + breaks · T8 fuzz seed + dispatch-verify · T9 BC B1–B2 · T10 six-gate/full-115-dir/-race/cycle-guard/envelope-audit · T11 ADR-0291 IN PLACE + row-done + sentence-narrow + STATE/router. **Adversarial verification (PLAN §1.2):** **V1 — code-claims + a DECISIVE by-execution probe of the load-bearing RD-SKEW claim** (cloned into scratch; RAN a `protojson.UnmarshalOptions{DiscardUnknown:false}` unmarshal of a bootstrap carrying `resource_detectors` on the `open_telemetry` `SinkConfig` Any → ERROR `unknown field "resource_detectors"` INSIDE the Any; flipped to `true` → silently dropped; WITHOUT the blank import → `unable to resolve "…SinkConfig": not found`): **the free version-skew reject HOLDS and DISCRIMINATES, the descriptor-blank-import dispatch trap is real.** V1 also re-confirmed the grpcclient clone shape, the `colmetricspb.MetricsServiceClient` collision, `snapshot` emitting exactly ONE Metric/ZERO labels per family, the RD-STREAMING mapping site, the six-getter `SinkConfig`, the main.go wiring, and the `otlptrace` receiver API — **ZERO SEVERE, ZERO MODERATE, four MINOR folded** (RD-SEAM test-only-import precision; the RD-WRAPPER BoolValue wording; the dispatch-trap error text). **V2 — process/consistency/SPEC-coverage:** all stage-close mechanics PASS (row stays `in-progress`; ADR untouched at the PLAN; counts/sentinel/identifiers re-run; envelope internally consistent; break protocol complete; format faithful to phase 68) — **ONE SEVERE folded** (the SPEC §7 `TestNoNewStat_OTLPRegistrationGuard`, the one-per-sink +0-stats house guard, was cited by T10 but written by NO task; CORRECTED — added T2 Step 5b + the `registration_test.go` File-structure entry + the Self-review mapping) + two MINOR. **The design direction — a fifth `stats_sinks[]` consumer on landed substrate, single flat row — is unchanged; only the dropped registration guard was a real gap, now closed.** **Counts UNCHANGED at this PLAN (docs-only; re-run MECHANICALLY):** fixtures **113** · fuzzers **55** · stat surface **1201** · BackendKind **38** · DECISIONS tail **ADR-0291** (next-free ADR-0292; completed IN PLACE at the IMPL, no flip) · go.mod modules **2**. **Sentinel re-run MECHANICALLY (does NOT fire; `stop` NOT created):** (1) prints `NOT DONE: row 69`; (2) `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:'` ⇒ **3**; (3) prints `NEVER OPENED: gRPC/Runtime/WASM`. **Next → the phase-69 IMPL** (execute T1–T11; RE-DERIVE each task's anchors at the IMPL tip).

- **prior active-phase:** `phase 69 (stats-sink-otlp) SPEC done` (2026-07-19) — lifecycle-state 1 → 2; docs-only (`SPEC.md` + the ADR-0291 §Context append; ZERO production `.go`); fresh worktree off master `7a293a06` (the phase-69 BRAINSTORM squash; NO parallel stream), branch `phase-69-stats-sink-otlp-spec`, per `feedback_git_worktrees`; subagent-driven — an independent re-derivation agent re-opened EVERY BRAINSTORM §6/§11 anchor at tip + resolved the code-gated dockets (the reject roster / the `SinkConfig` six-field-plus-wrapper layout / the `DiscardUnknown` posture / the fuzz dispatch / the grpcclient stanza / the collisions), then the D-OTLPM docket was DISPOSED one-for-one. **Row 69 STAYS `in-progress`** (flips at the IMPL six-gate). **ADR-0291 §Context DRAFTED per ADR-0044 — the DECISIONS tail flips ADR-0290 → ADR-0291 AT THIS SPEC COMMIT** (next-free **ADR-0292**; the IMPL completes ADR-0291 IN PLACE, no further flip). **Headline resolutions:** **D-OTLPM-SKEW RESOLVED, INVERTING the BRAINSTORM's silent-ignore lean** — envoy-go parses the whole bootstrap with `protojson.UnmarshalOptions{DiscardUnknown:false}` (`bootstrap.go:510`), so the v1.37.2-only `resource_detectors`/`custom_metric_conversions` (absent from the pinned v1.32.4 `SinkConfig`) are boot-REJECTED FOR FREE once the descriptor is blank-imported — a named version-skew departure, ZERO new detect-and-reject code (NOT "silently ignored by `UnmarshalTo`" as BRAINSTORM §2.2/§8 supposed). **D-OTLPM-HISTO → STRICT-REJECT** `report_histograms_as_deltas: true` (the metrics_service `histogram_emit_mode` precedent, ADR-0080; accept-inert named as the alternative). **D-OTLPM-SPLIT → SINGLE FLAT ROW** (~11 tasks; the phase-48/49/57 sink precedent — the substrate is landed so the knobs are parameter-threading; ADR-0045 valve armable-but-unconsumed). **D-OTLPM-FIXTURE → TWO** (`0112-stats-sink-otlp`@10448 default cumulative + `0113-stats-sink-otlp-knobs`@10449 deltas+prefix+both-false with the delta-SUM stability barrier; fixtures 113 → 115). D-OTLPM-STARTTIME → per-side structural (subject ns-magnitude + cumulative-constant; reference unasserted; NEVER cross-side equality). D-OTLPM-DUPNAME → `(name, sorted-attrs)` order-insensitive keying. D-OTLPM-NODE → envoy-go's own `telemetry.sdk.{name=envoy-go,language=go,version}` triple (keys cross-side, values per-side). D-OTLPM-FUZZSEED → +0, a seed on the `open_telemetry` `@type` dispatch-verified to reach the new arm (the descriptor-blank-import trap named). RIDER → the require=false RBAC/principal fixture arm STAYS DEFERRED (off-subject, not ≤1-task). Also pinned: the two `*wrapperspb.BoolValue` knobs default TRUE (read `w==nil || w.GetValue()`, INVERTING the metrics_service `.GetValue()` template — `reference_protojson_wrapper_scalar_not_object`); the UNARY-Export bounded-channel writer (the OTLP-logs precedent, NOT the streaming metrics_service lifecycle); the grpcclient wrapper named `OTLPMetricsClient` to disambiguate from `colmetricspb.MetricsServiceClient`. **NO fresh reference probe this SPEC** (the BRAINSTORM's Dossier P discriminated every behavioral gate this session; no docket hinges on unverified reference behavior — stated honestly, not to skip rigor). **Adversarially verified before landing.** **Counts UNCHANGED at this SPEC (docs-only apart from the DECISIONS append; re-run MECHANICALLY):** fixtures **113** · fuzzers **55** · stat surface **1201** · BackendKind **38** · go.mod modules **2** — the ONLY moved count is the DECISIONS tail (**ADR-0290 → ADR-0291**; next-free **ADR-0292**). **Sentinel re-run MECHANICALLY at this close (does NOT fire; `stop` NOT created):** (1) prints `NOT DONE: row 69`; (2) prints THREE live "candidates:" sentences (`grep -cE 'remaining deferred \(not-yet-chartered\) candidates:'` ⇒ **3**); (3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`. **Next → the phase-69 PLAN** (RE-DERIVE the SPEC; the owed roster is in §Current pointer's next-skill line).

- **prior active-phase:** `phase 69 (stats-sink-otlp) BRAINSTORM done` (2026-07-19) — the roller's AUTONOMOUS self-pick (no human pick per the 2026-07-12 standing directive) opens the next Observability-family row (lifecycle-state 0 → 1; docs-only, ZERO production `.go`; both input dossiers lived in PRIVATE scratch OUTSIDE the repo — nothing to delete in-tree; fresh worktree off master `c7f029e6`, branch `phase-69-stats-sink-otlp-brainstorm`, per `feedback_git_worktrees`). **Subject:** `stats-sink-otlp` — the OpenTelemetry OTLP metrics stats sink `envoy.stat_sinks.open_telemetry`, the FIFTH `stats_sinks[]` consumer (after metrics_service/statsd/dog_statsd/graphite_statsd): per `stats_flush_interval` tick, ONE unary `Export` over a cluster-dialed gRPC client, Counter → monotonic Sum / Gauge → Gauge, tag-extracted names + tags-as-attributes as the TRUE default. **Why this pick (the FULL rejected roster adjudicated in BRAINSTORM §2.1):** the twice-deferred "loses on SIZE" adjective re-derived STALE at tip (`reference_deferred_candidate_cost_restale`) — every hard part is landed substrate (the phase-47 Sink/Flusher subsystem, the ADR-0263 `deltaState`, the ADR-0264 `ExtractTags`/`labelMapper`, the descriptor-derived TypeURL dispatch + sibling strict-reject, the grpcclient unary-OTLP client precedent, `go.opentelemetry.io/proto/otlp v1.0.0` DIRECT with `collector/metrics/v1` vendored ⇒ +0 modules anticipated); the rivals: SDS rotation = initial-fetch-only provider + static per-chain `tls.Config` ⇒ a multi-leg live-reconfig row (`watched_directory` first) · DataSource `environment_variable` D-ENV-HARNESS-gated · tracing `metadata` plumbing unbuilt · the tiny riders not row-sized (the require=false RBAC/principal fixture arm named the best RIDER) · QuicProtocolOptions near-zero differential value · upstream SDS VALUE-cycle blocked (restated, not re-derived) · the `ssl` family framework-surgery (ADR-0286 C3). **EXECUTED evidence (Dossier P, `envoyproxy/envoy:contrib-v1.37.2`, fresh container per arm, a throwaway per-arm unary-Export Go receiver with served-this-arm asserts — 16 numbered conclusions):** gRPC-only transport (PGV boot-reject when `protocol_specifier` absent) · ONE unary Export per flush, 1 resourceMetrics/1 scopeMetrics/empty scope · `telemetry.sdk.*` resource attrs only · used-only where used ≠ non-zero and the set GROWS · **the two BoolValue knobs DOC-DEFAULT TRUE are REAL** (baseline = tag-extracted names + tags-as-attributes; the both-false arm the discriminating direction) · same-name stats = SEPARATE Metric entries keyed by attributes · histograms → Histogram with 19 default bounds (envoy-go has NONE, ADR-0060 — a NAMED COVERAGE BOUNDARY; the checked phase-47 precedent `bootstrap.go:596` REFUTED the accept-inert lean — D-OTLPM-HISTO leans REJECT) · default CUMULATIVE; `report_counters_as_deltas` → DELTA with `isMonotonic` retained + startTime chaining; the two delta knobs INDEPENDENT · `prefix.` composes · **the reference's factor-1000 µs `startTimeUnixNano` bug is NOT cloned** (per-side structural assertion, never cross-side startTime equality) · fail-open + retry-per-flush, no sink-health stat (⇒ +0 stats) · the sink cluster's own stats are exported (feedback loop; assert application-cluster subsets) · the Docker Desktop host.docker.internal AAAA/V4_ONLY probe pin carried to SPEC probes (P-16). **Envelope:** ZERO new production packages (`internal/statssink/otlp.go` new FILE + one `internal/bootstrap` parse arm + a ~40-line `internal/grpcclient` stanza + one `cmd/envoy-go` build-loop arm; new TEST-ONLY `test/helpers/otlpmetrics`); new exports ONLY in the existing statssink/grpcclient rosters — the `internal/xds` zero-new-symbol discipline UNTOUCHED; the v1.32.4-pinned `SinkConfig` has SIX fields, the v1.37.2-only `resource_detectors`/`custom_metric_conversions` a NAMED version-skew boundary. **BRAINSTORM-time open questions to the SPEC:** D-OTLPM-HISTO · D-OTLPM-SKEW · D-OTLPM-STARTTIME · D-OTLPM-DUPNAME · D-OTLPM-NODE · D-OTLPM-SPLIT (BY-CONCERN 2-leg anticipated, collapse-to-flat argued honestly; the ADR-0045 valve arms at the PLAN) · D-OTLPM-FIXTURE (`0112-stats-sink-otlp`, port 10448; fixtures 113 → 114–115 RANGE) · D-OTLPM-FUZZSEED · the RIDER question. **Adversarially verified before landing** (an independent code-claims + probe-capture + adjudication verifier: zero SEVERE, one MODERATE §8-roster-carry + three MINOR corrections folded in; design direction unchanged). **Row 69 registered `in-progress`** at this stage-close commit per the ROADMAP §Schema invariant; flips `done` at the phase-69 IMPL six-gate (ADR-0106; per-leg if D-OTLPM-SPLIT lands legs). **This pickup IS named in the live Observability `candidates:` sentence (ROADMAP:198) — NO sentence is narrowed at ANY pre-IMPL stage (the verified phase-57 graphite precedent: in the sentence at the BRAINSTORM `d2fe54df`, rolled OUT at the IMPL `2825ca90`); it rolls OUT at the phase-69 IMPL row-done edit.** **Counts UNCHANGED at this BRAINSTORM (docs-only; count commands re-run MECHANICALLY in the worktree at this close):** fixtures **113** (numeric tail `0111-tls-cvc-empty-dynamic-fallback`) · fuzzers **55** (`internal/` scope) · stat surface **1201** (BEHAVIOR_CONTRACT doc count) · BackendKind **38** · DECISIONS tail **ADR-0290** (next-free **ADR-0291**) · go.mod modules **2**. **Sentinel re-run MECHANICALLY at this close (does NOT fire; `stop` NOT created):** (1) now prints `NOT DONE: row 69` (the registration re-opens check (1)); (2) prints THREE live "candidates:" sentences (`grep -cE 'remaining deferred \(not-yet-chartered\) candidates:'` ⇒ **3**); (3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`. **Next → the phase-69 SPEC** (RE-DERIVE the BRAINSTORM rather than execute it; the owed roster is in §Current pointer's next-skill line).

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
