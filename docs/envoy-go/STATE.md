# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `06.1-stats-prometheus`
- **phase-directory:** `docs/envoy-go/phases/06.1-stats-prometheus/` — contains `SPEC.md` (at `ad46930`, frozen), `PLAN.md` (at `be99b42`), and `PROGRESS.md` (Task 15 closing entry at `58d4ec9` plus the lifecycle-state 4 verifier verification block written by THIS commit). Parent `docs/envoy-go/phases/06-observability-baseline/` carries `BRAINSTORM.md` (`d271fd7`) + parent `SPEC.md` (`ad46930`). Sibling `docs/envoy-go/phases/06.2-access-log/` retains the placeholder `README.md` (`ad46930`). Phase 05 closed history unchanged.
- **lifecycle-state:** `3` — verification ran but did NOT pass. The verifier session on `phase/06.1-stats-prometheus-verify` (worktree `.worktrees/phase-06.1-stats-prometheus-verify`, branched from impl-branch tip `42e2650` per ADR-0003) re-ran every BOOTSTRAP §7.5 gate from a fresh session and surfaced a real defect: gate (d) FAIL on a fresh fuzz run of `FuzzHCMConfigParse` at the ADR-0018 30s budget — a crasher in 5 seconds via `internal/filter/hcm/config.go:164` (HCM stat-name construction from a user-controlled `stat_prefix` without validation; `stats.Registry.NewCounter` → `Registry.checkName` panics on a `stat_prefix` containing a space). Gate (e) part 2 (`go test -race ./...`) inherits the failure on `internal/filter/hcm` because Go's fuzz-testdata persistence saves the minimised seed (`9ba19570cf17f59f`) and unit-test mode replays it as a regression case. Gates (a) fixture-0005 differential PASS, (b) all 5 pre-existing fixtures PASS, (c) h2spec 53/53 PASS at the pinned summerwind SHA, (e) part 1 build/vet/lint clean, ADR-0059 boundary grep clean, 17 stat-emit call-site grep clean, and 6 of 7 fuzzers (BootstrapLoad, PromTextFormat, TcpProxyFilter, FrameStream, HPACKDecode, TLSContextParse) PASS at the 30s budget. Gate (f) REVIEW.md remains the lifecycle-state 6 deliverable. Per BOOTSTRAP §5 deviation rule (`Unexpected state → superpowers:systematic-debugging FIRST`) and §5.2 review-feedback re-entry analogue, lifecycle-state regresses 4 → 3 to TDD-fix the panic. Per BOOTSTRAP §5.3, the eventual phase-done commit will name all 06.1 ADRs landed plus any new ADR introduced by the fix. ROADMAP rows unchanged (06.1 stays `in-progress`; 06 stays `in-progress`; 06.2 stays `planned`). The verifier did NOT commit the auto-generated seed file (`internal/filter/hcm/testdata/fuzz/FuzzHCMConfigParse/9ba19570cf17f59f`) per the 05.2 verifier role contract (verifier commit changes only PROGRESS.md + STATE.md); the seed bytes are quoted verbatim in the PROGRESS.md verification block for the fix branch's reproduction.
- **next-skill:** `superpowers:systematic-debugging` — root-cause and TDD-fix the `FuzzHCMConfigParse` crasher. Inputs: this verification commit's `docs/envoy-go/phases/06.1-stats-prometheus/PROGRESS.md` "Verification (lifecycle-state 4) — FAILED" block (the authoritative bug report + verbatim panic trace + minimised seed bytes), `internal/filter/hcm/config.go:135-170` (the HCM filter constructor that calls `registry.NewCounter` with the unvalidated `stat_prefix`), `internal/stats/registry.go:60-101` (`NewCounter`/`checkName`/`nameRE`), `internal/filter/hcm/fuzz_test.go:25-42` (the fuzz target's contract: "no panic; every error message is hcm:-prefixed"), `docs/envoy-go/phases/06.1-stats-prometheus/SPEC.md` §6 + §10.1 (Rule SN1 substitution semantics), `docs/envoy-go/DECISIONS.md` ADR-0061 (the SN1-SN8 flattening rules ADR), `BOOTSTRAP_PROMPT.md` §§5-7 (lifecycle + phase-done gates), `superpowers:test-driven-development` (the per-task TDD discipline — write the regression test first, observe RED, fix, observe GREEN), `superpowers:systematic-debugging`'s `defense-in-depth.md` (recommended for the secondary `Registry.NewCounter` panic-vs-error question).
- **next-skill-scope:** Lifecycle-state 3 → re-verification session: TDD-fix the `FuzzHCMConfigParse` crasher and re-run all six BOOTSTRAP §7.5 gates from a fresh worktree. Worktree: `.worktrees/phase-06.1-stats-prometheus-impl-followup-gate-d` (or equivalent name) branched from this verify commit's HEAD per ADR-0003 + per-phase-worktree convention. Two fix-shape candidates (the fix branch chooses; ADR-0065 if non-obvious): (1) Reject configs with non-Prometheus-safe `stat_prefix` at HCM parse time, returning an `hcm: invalid stat_prefix: <prefix>` error — matches the fuzz target's contract that every error must be `hcm:`-prefixed. (2) Sanitise `stat_prefix` per Rule SN1's invalid-character substitution before constructing the counter name — e.g., map non-`[a-zA-Z0-9_.]` to `_`. Trade-off: changes the observable stat name set vs upstream Envoy when the fixture-side `stat_prefix` happens to contain such chars; phase 06.1 SPEC §10.1 anchors Rule SN1 against a specific reference scrape, so a SN1-extension ADR would be appropriate. Independently consider whether `stats.Registry.NewCounter` should return an error rather than panic on invalid names (defense-in-depth): a panic on user-input-derived names is fundamentally a contract violation regardless of the HCM-side caller's input validation. TDD shape: (1) write a regression test in `internal/filter/hcm/config_test.go` that constructs an HCM config with a `stat_prefix` containing a space and asserts on the chosen error/sanitisation behaviour — the test must FAIL pre-fix; (2) implement the chosen fix; (3) re-run the unit test (must PASS); (4) re-run `go test -fuzz=FuzzHCMConfigParse -fuzztime=30s ./internal/filter/hcm/` (must PASS clean); (5) re-run all six BOOTSTRAP §7.5 gates (a) through (e) part 2 in a fresh verify worktree to advance STATE to lifecycle-state 5 with `next-skill: superpowers:requesting-code-review`. The fix branch should also delete any auto-generated seed file `internal/filter/hcm/testdata/fuzz/FuzzHCMConfigParse/9ba19570cf17f59f` that may regenerate during fuzzing (the regression test in `config_test.go` is the durable artifact). Carry-forward consideration: the L4 review-followup session's 26 Minors remain queued for the eventual REVIEW phase; the gate-(d) fix should be a focused single-issue branch, not bundled with Minors triage.
- **last-commit:** `1f94b74` — `phase 06.1: lifecycle-state 4 verification — gate (d) FAIL on FuzzHCMConfigParse; STATE → 3`, on branch `phase/06.1-stats-prometheus-verify`. Adds the lifecycle-state 4 verifier verification block at the tail of `docs/envoy-go/phases/06.1-stats-prometheus/PROGRESS.md` (verbatim outputs of every gate command run from a fresh-session worktree branched from impl-branch tip `42e2650`) and updates STATE → lifecycle-state 3 with `next-skill: superpowers:systematic-debugging`. No production code, test, or fixture file changed; this is a pure verification + state-transition commit per the 05.2 verifier-commit role contract (`b34bd99`). The auto-generated seed file `internal/filter/hcm/testdata/fuzz/FuzzHCMConfigParse/9ba19570cf17f59f` was deleted from the verifier's worktree before this commit per the verifier role contract — its bytes are quoted in the PROGRESS.md verification block. ROADMAP unchanged (rows 06.1 + 06 stay `in-progress`; row 06.2 stays `planned`). SHA-fill follow-up per the phase-02..05.2 convention.
- **last-updated:** 2026-04-27

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
