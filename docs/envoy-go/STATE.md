# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `05.1-downstream-h2`
- **phase-directory:** `docs/envoy-go/phases/05.1-downstream-h2/` — exists (created by this lifecycle-transition commit per `BOOTSTRAP_PROMPT.md` §6.2 step 2), contents = `README.md` placeholder only; no `SPEC.md` yet. The sibling `docs/envoy-go/phases/05.2-upstream-h2/` was created in the same commit, also placeholder-only. The parent `docs/envoy-go/phases/05-http-2/` retains its `SPEC.md` (`612cdea`) as the master design document for both sub-phases per ADR-0045 and phase-05 SPEC §11.1; that directory is now closed read-only history at the parent level (no PLAN/PROGRESS/REVIEW will land under it; those land under the sub-phase directories).
- **lifecycle-state:** `1` — phase 05.1 is in `ROADMAP.md` (status=`planned`, depends-on=`04`); the directory exists but `SPEC.md` does not. Per `BOOTSTRAP_PROMPT.md` §5 state 1, the next session invokes `superpowers:brainstorming` scoped to phase 05.1 and produces `phases/05.1-downstream-h2/SPEC.md`. The state-1 procedure's "create directory" step is degenerate (the directory already exists from this commit's split per §6.2 step 2); the brainstorming session proceeds directly to SPEC drafting per ADR-0004's autonomous mode. The brainstorming session also flips ROADMAP row 05.1 `planned` → `in-progress` at directory-already-exists time (the corrected pattern from phase 05's commit `8d18320`).
- **next-skill:** `superpowers:brainstorming`
- **next-skill-scope:** Brainstorm phase 05.1 (`downstream-h2`) per ADR-0004's autonomous mode. The master design source is `docs/envoy-go/phases/05-http-2/SPEC.md` (`612cdea`); the 05.1 narrowed scope is enumerated in `docs/envoy-go/phases/05.1-downstream-h2/README.md` (placeholder, written by this commit) and is also captured in ADR-0045's "Decision" section. 05.1 covers: the full server-side H2 codec sub-package under `internal/filter/hcm/h2/` (errors, preface, framer, hpack, settings, flow, stream, conn — but NOT `client.go`, which is 05.2); HCM ALPN dispatch in `internal/filter/hcm/filter.go`; `codec_type: HTTP2` in `internal/filter/hcm/config.go`; `listenerCtx` plumbing from `internal/listener/manager.go`; codec-neutral `directResponseAction.body()` + `writeH2` adapter (per phase-05 SPEC §5.5) — required in 05.1 because the h2spec gate exercises `direct_response`; `cmd/envoy-go --allow-h2c` test-only flag (per phase-05 SPEC §4.4 ADR-Z); `cmd/envoy-go/main_test.go` h2 bootstrap variant; `test/conformance/h2spec/` (h2spec.go threshold list + h2spec_test.go testcontainers driver); NEW `docs/envoy-go/CONFORMANCE_PINS.md` pinning `summerwind/h2spec` by tag + SHA256; `BEHAVIOR_CONTRACT.md ## HTTP/2` SUBSECTION SCAFFOLD (asserted: `:status` + decoded-body equivalence on direct_response 2xx via h2spec; not asserted: wire-byte framing — full details in 05.2 once fixture 0004 lands); fuzz targets `FuzzFrameStream` + `FuzzHPACKDecode`; phase-04 REVIEW Minor carry-forward triage (per phase-05 SPEC §12 + ADR-X). Anticipated 05.1 ADRs (sequential numbering after ADR-0045 — planner verifies next-free at write time): ADR-P codec source (x/net/http2.Framer + hpack); ADR-Q from-scratch ServerConn; ADR-S server SETTINGS defaults; ADR-T BEHAVIOR_CONTRACT scaffold; ADR-U h2spec scope + threshold + pin; ADR-V ALPN dispatch wiring; ADR-Z `--allow-h2c`; ADR-X phase-04 REVIEW carry-forward triage. EXPLICITLY OUT of 05.1 (deferred to 05.2): `internal/filter/hcm/h2/client.go` ClientConn; `internal/cluster/dial_h2.go` + `Cluster.UseH2()` + `internal/cluster/manager.go` HttpProtocolOptions parsing; blank import for `upstreams/http/v3` in `internal/cluster/cluster.go`; `routerActionH2` in `internal/filter/hcm/actions.go`; fixture `test/fixtures/0004-h2-routing/` (full PKI gen + backends + driver + YAMLs + expectations + README); `test/helpers/h2.go`; `test/differential/runner_test.go` blank-import for the new fixture; ADR-R per-request-fresh-upstream-H2; ADR-W closes-ADR-0035-H2-leg; ADR-Y trailers. Phase-05 SPEC §11.1's split-by-surface recommendation is the authoritative scope split; ADR-0045 ratifies it. Per ADR-0003 + the project's per-phase-worktree convention (see prior `phase/0X-Y-spec`/`-plan`/`-impl` branch series), brainstorming runs from a fresh `phase/05.1-downstream-h2-spec` worktree branched from the master tip after this commit's fast-forward; the current `.worktrees/phase-05-http-2-plan` is closed-history at this state transition. Per ADR-0004 step 3 the SPEC is reviewed by the `spec-document-reviewer` subagent (≤3 iterations or escalate to `blocked` per ADR-0004 step 3).
- **last-commit:** `<filled-in-on-commit>` — the planner-time split commit on branch `phase/05-http-2-plan` (sole commit on that branch; no PLAN.md was written, the §6.2 split short-circuits the PLAN per `BOOTSTRAP_PROMPT.md` §5 state 2 GATE). Master is fast-forwarded to this tip per ADR-0003.
- **last-updated:** 2026-04-25

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
