# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `phase 26.3 PLAN done; awaiting 26.3 IMPL` (2026-05-31, this commit) — **phase 26.3 (`network-filter-rbac`) PLAN DONE at 2026-05-31**. The **15-task** bite-sized TDD PLAN (`docs/envoy-go/phases/26.3-network-filter-rbac/PLAN.md`) decomposes the 26.3 SPEC §10 spine: Task 1 first-action baselines/anchors gate; Tasks 2-3 the `internal/rbac/` engine MOVE (evaluator surface → compilers/`Evaluate`/matcher-bridge/`PerPolicyCounters`, re-exported); Task 4 the input-capability `Profile` (`ProfileHTTP`/`ProfileL4`) + HTTP-only-arm reject; Task 5 consumer-#1 HTTP-rbac migration (aliased import, `ProfileHTTP`, engine types); Task 6 the R4 differential re-verification gate; Task 7 the `NoFlush` close distinction (F3); Tasks 8-12 the `rbac_network` package (parse+`NewFactory(reg)`+4 static counters → L4 `EvalContext` → OnData decision [allow→Continue / deny→rcd+NoFlush-close; OnNewConnection Continue no-op] → shadow-metadata writes + dynamicmetadata doc generalization → 5th-built-in registration + boot smoke); Task 13 the 36th fuzzer; Task 14 the +2 differential fixtures (cross-side allow/deny/shadow StatsAsserter R-M-LIVE + boot-reject); Task 15 the BEHAVIOR_CONTRACT bundle + ADR-0216/0217/0218 §Decision/§Consequences bodies + the parent-row-26 ROLLUP + six-gate. **plan-document-reviewer APPROVED** (first iteration, Status: Approved; one advisory [Task-4 matcher-path HTTP-only-arm reachability — already hedged by the SPEC §3.4 + Task-4 NOTE] + two recommendations [Task-12 `reg.Lookup` accessor; line-anchor drift caught by the Task-1 re-pin gate] FOLDED IN at `edf93c2`). **ADR-0045 split-gate RE-CHECKED at PLAN time: NO split** (15 tasks / ~630-890 net-new LoC — the ~790 moved engine LoC is mechanical relocation re-verified byte-exact by the phase-16 fixtures [R4], NOT counted as churn — both axes within the ~25-task / ~1500-LoC gate; D-P1 confirmed). **PLAN-time D-questions resolved:** D-26.3-1 (enum `Profile` + `profile.permits(armKind)` at the HTTP-only arms in `buildOnePermission`/`buildOnePrincipal`; rules-path is the guaranteed reject target — the matcher-path carries no rbac-permission/principal leaf so its HTTP-only reject is vacuously satisfied, recorded at IMPL); D-26.3-3 (`networkrbac.NewFactory(reg)` closure-captures `*stats.Registry` in `RegisterBuiltins`, NOT a `FactoryCtx` field — preserving the ADR-0215 import-light invariant); D-26.3-7 (the `incPolicy`/`sync.Map` per-policy lazy-allocation moves to the engine as `rbac.PerPolicyCounters`; the four NAMED base counters + their registration + the `trackPerRuleStats` decision stay per-consumer; network passes false — F2). Docs only, no code change. Next: 26.3 IMPL. **Earlier (SPEC stage, preserved):** the per-sub-phase 26.3 SPEC (16 §§ + 2 appendices) — the THIRD/FINAL sub-phase of the phase-26 3-way pre-split — INHERITS the parent SPEC §5/§6/§7/§8/§11/§12/§13 rosters + refines the `rbac_network` per-Task surface against the AS-BUILT 26.1/26.2 framework (the `network.Connection` L4 accessors `LocalAddr`/`RemoteAddr`/`RequestedServerName`/`DownstreamPrincipals`; `ReadFilterCallbacks.DynamicMetadata()`; the `SetResponseCodeDetails` rcd sink; the `prefixConn` mixed-chain handover; `builtins.RegisterBuiltins`) + the phase-16 RBAC engine. **Three deliverables pinned:** (i) the shared `internal/rbac/` engine EXTRACTION (the abstract `EvalContext` + the ~23 permission/principal evaluators + the rules/matcher compilers + the matcher bridge + the per-policy machinery MOVED ~790 LoC + re-exported, gaining an input-capability `Profile` [`ProfileHTTP`/`ProfileL4`]; HTTP rbac = consumer #1, byte-exact re-verified by its phase-16 fixtures — R4; `rbac_network` = consumer #2) — ADR-0216; (ii) the FIRST production WRITE through the connection-scoped `*dynamicmetadata.Bucket` (the rbac shadow pair `shadow_engine_result`/`shadow_effective_policy_id` under namespace `envoy.filters.network.rbac`; `internal/dynamicmetadata/` reused at connection scope, doc generalized in place) — ADR-0217; (iii) the `rbac_network` filter (decision-in-`OnData` per AMEND-A8 + the sticky-halt constraint; allow → `Continue` → terminal [the FIRST production mixed read→terminal chain via `prefixConn` — R-M-LIVE]; enforced-deny → `rbac_deny_close` rcd + `Close(NoFlush)` [the F3 NoFlush framework touchpoint]; shadow → metadata + stats; the four `<stat_prefix>.rbac.*` counters [132→136]; `delay_deny` + HTTP-only-matcher PARSE-REJECT; CEL silent-ignore) — ADR-0218. **D-questions resolved at SPEC:** D-P1 (ONE sub-phase, net-new basis ~630-890 LoC / ~16-22 tasks — no 26.3a/26.3b split); D-P4 (CEL `condition`/`checked_condition`/`cel_config` SILENT-IGNORED by the phase-16 engine per ADR-0040 — `rbac_network` MIRRORS the silent-ignore; the parent §6.3 `condition-cel-unsupported` reject arm RETRACTED — the SPEC's one substantive refinement); D-P5 (network RBAC proto has NO `track_per_rule_stats` → `rbac_network` emits ONLY the 4 static counters; the engine's per-policy machinery stays dormant for consumer #2). **ADR-0216 + ADR-0217 + ADR-0218 §Context DRAFTED** into DECISIONS.md (tail ADR-0215 → **ADR-0218**; next-free **ADR-0219**; §Decision/§Consequences bodies land at 26.3 IMPL per ADR-0044). **spec-document-reviewer APPROVED** (first iteration, no CRITICAL/MAJOR; all four flagged resolutions independently verified against as-built code; 2 advisory recs already covered by SPEC deferral notes). Counts unchanged at SPEC (deltas land at IMPL): fuzzers 35 [→36], fixtures 44 [→46], stats 132 [→136]. ROADMAP sub-row 26.3 `planned → in-progress` at THIS SPEC commit (ADR-0106); parent row 26 STAYS `in-progress` (flips at 26.3 phase-done per the 18/19/22/24/25 ROLLUP). Next: 26.3 PLAN. **Earlier (phase 26.2 IMPL, preserved):** phase 26.2 (`network-filter-registry-migration`) IMPL DONE at 2026-05-30. The 12-task TDD PLAN executed via `superpowers:subagent-driven-development` (fresh subagent per task, two-stage review, local-only commits; controller squash-merges + pushes at stage-close). **LANDED:** the NEW `network.TerminalFilter` connection-takeover seam (`Handle(ctx, net.Conn)`) alongside the 26.1 `ReadFilter`/`OnData` model + a SEALED `NetworkFilter` marker via the exported `network.Marker` embeddable (`FilterInstanceFactory` generalized to `func() NetworkFilter`); the extended `network.FactoryCtx` (HasTLS/AllowH2C/ListenerPrincipal/NodeServiceCluster; heavy singletons closure-captured); the unified `[read-filter*, terminal-filter?]` chain runner (classify → read-prefix + optional trailing terminal; `TerminalReady()`/`HandleTerminal()` + the buffered-prefix `prefixConn` handover); `tcpproxy.NewNetworkFactory` + `hcm.NewNetworkFactory` adapters (Handle UNTOUCHED → byte-exact parity intrinsic); the unified `buildNetworkChainFactory` (NEW `network-filter-terminal-not-last` + `network-filter-multiple-terminals` chain-shape boot-rejects; the 26.1 `network-filter-mixed-chain-unsupported` reject LIFTED — mixed read→terminal chains now expressible, first PROD consumer rbac_network at 26.3); `serveConnection` step-7 unified into ONE `serveNetworkChain` (pure-read / pure-terminal / mixed); the hardcoded `manager.go` `filterRegistry`/`filterConstructor`/`filterHandler`/`buildTerminalFilter`/`listenerCtx` RETIRED + `netReg` intrinsic; the `internal/filter/network/builtins.RegisterBuiltins` seam; main.go boot-wired. The byte-stable unknown-type reject wording stays `"%s: unknown filter type_url %q"` (ADR-0080). **SIX GATES GREEN LIVE at Task 12** (run honestly, outputs quoted in PROGRESS.md): build/vet/lint/`-race -short` clean; differential R3 back-compat byte-exact (`0000-tcp-echo`/`0001-tcp-proxy-rr`/`0002-tls-tcp` tcp_proxy + `0003-http11-routing`/`0004-h2-routing` HCM + `0040`/`0041`/`0042` network-filter — ALL green; the sole differential non-green was a KNOWN environmental port-bind flake on the `0036-http-wasm-body-and-advanced` HTTP wasm fixture — `bind: address already in use` on a random ephemeral port — which PASSES on retry, NOT a migration regression); conformance 10/10 + h2spec 53/53 re-run LIVE (HCM now dispatches through the migrated path — NOT asserted-unaffected); fuzzers 35; fixtures 44; stat surface 132 — all +0 (migration is stat/fixture/fuzzer-NEUTRAL). **ADR-0215 §Decision/§Consequences bodies landed in place** (DECISIONS.md tail STAYS ADR-0215; no new ADR number consumed; next-free → ADR-0216). **BEHAVIOR_CONTRACT 26.2 framework-update block** added (TerminalFilter seam; `[read*, terminal?]` chain shape; unified single-dispatch; mixed-chain LIFT + new chain-shape rejects; the `filters[N]:` build-error index segment as an intended consequence). **Deferred cleanup recorded:** the now-dead `httpClient *httpclient.Client` pass-through param on `NewManagerWithBaseDirAndAllowH2C`/`buildListenerRuntimeWithCtx` (HCM gets httpClient via `builtins.Deps.HTTPClient`; removing it ripples cross-file → deferred per byte-exact-parity/avoid-churn discipline). The §9 Network-filters family stays OPEN (parent row 26 `in-progress` — flips at 26.3 per the 18/19/22/24/25 ROLLUP precedent; sub-row 26.2 `in-progress → done` at this phase-done commit; 26.3 `planned`). Next: 26.3 SPEC. **Earlier (PLAN stage, preserved):** phase 26.2 PLAN DONE at 2026-05-30 (squash `9a61788`) — the 12-task TDD PLAN; plan-document-reviewer APPROVED (1 MAJOR Task-7 unknown-type-url self-contradiction RESOLVED by deferring the unified unknown-type reject + 2-value collapse + `buildTerminalFilter` deletion to Task 10; 3 MINOR folded); ADR-0045 split-gate NO split; PLAN-time D-questions D-26.2-1..7 resolved (sealed `Marker`; FactoryCtx 4 primitives; `prefixConn` at 26.2; `builtins.RegisterBuiltins` import-cycle-forced placement). **Earlier (SPEC stage, preserved):** phase 26.2 SPEC DONE at 2026-05-30 (squash `7c19957`) — the per-sub-phase SPEC (16 §§) INHERITS the parent SPEC §5/§6/§7/§8/§11/§12/§13 rosters; RESOLVES the parent §3.2 terminal-fit D-question via the `TerminalFilter` seam over an OnData rewrite (REJECTED: HCM H1 driver + H2 ServerConn own the conn); ADR-0215 §Context drafted; spec-document-reviewer APPROVED. **Earlier (phase 26.1, preserved):** phase 26.1 (`network-filter-chain-framework-and-echo`) IMPL DONE at 2026-05-30 — the NEW `internal/filter/network/` read-filter chain framework (two-value `Status`; `ReadFilter`/`ReadFilterCallbacks`/`Connection`/`CloseType`; drainable `Buffer`; freeze-after-boot `*Registry`; per-connection runtime context owning the reused `*dynamicmetadata.Bucket` + RCD sink) + `echo` + `direct_response` first consumers + `manager.go` dual-dispatch (NOW retired at 26.2) + main.go boot-wiring + the 35th fuzzer + fixtures 0040/0041/0042. Echo TypeURL empirically corrected to the `extensions.` form. One CRITICAL sticky-halt bug found + fixed (echo multi-read re-iteration). ADR-0213 + ADR-0214 §Decision/§Consequences landed in place. Phases 00–25 + phase-26 parent SPEC + phase-26.1 + phase-26.2 DONE and CANONICAL.
- **phase-directory:** `docs/envoy-go/phases/26.3-network-filter-rbac/` (SPEC.md + PLAN.md — DONE; PROGRESS.md lands at the 26.3 IMPL session). The phase-26.2 directory (SPEC.md + PLAN.md + PROGRESS.md — DONE), the phase-26.1 directory (SPEC.md + PLAN.md + PROGRESS.md — DONE), and the parent directory `docs/envoy-go/phases/26-network-filter-chain-and-rbac/` (BRAINSTORM.md + parent SPEC.md — DONE) stay canonical.
- **lifecycle-state:** `phase 26.3 PLAN done (SKILL_ROUTING state 2 → 3; PLAN.md exists, implementation incomplete); awaiting 26.3 IMPL` (the 15-task TDD PLAN authored via `superpowers:writing-plans` + plan-document-reviewer APPROVED + ADR-0045 split-gate re-checked NO split + PLAN-time D-26.3-1/-3/-7 resolved + STATE/ROADMAP advanced + squash-merged + pushed). ROADMAP sub-row 26.3 STAYS `in-progress` (it flipped `planned → in-progress` at the SPEC commit per ADR-0106; a PLAN-done does NOT flip it — only phase-done does). Parent row 26 STAYS `in-progress`; it flips `in-progress → done` ATOMICALLY with sub-row 26.3 at 26.3 phase-done via the ROLLUP precedent (18/19/22/24/25). The next lifecycle transition is the 26.3 IMPL cold-start (SKILL_ROUTING state 3 → `superpowers:subagent-driven-development` over the PLAN's 15 TDD tasks).
- **next-skill:** `superpowers:subagent-driven-development` (scoped to the **26.3 IMPL** — executing the 15-task TDD PLAN at `docs/envoy-go/phases/26.3-network-filter-rbac/PLAN.md`, per `feedback_execution_style`: fresh subagent per task + two-stage review; subagents commit LOCAL-ONLY [`feedback_subagents_no_push`]; the controller squash-merges + pushes at stage-close). Task spine: 1 baselines gate → 2-3 engine MOVE → 4 `Profile` → 5 consumer-#1 migration → 6 R4 gate → 7 `NoFlush` → 8-12 `rbac_network` (parse/counters → L4 ctx → OnData → shadow writes → registration) → 13 fuzzer → 14 fixtures → 15 contract+ADR bodies+ROLLUP+six-gate. At 26.3 IMPL phase-done: fuzzers 35→**36**, fixtures 44→**46**, stat surface 132→**136**; DECISIONS.md tail STAYS **ADR-0218** (the §Decision/§Consequences bodies land in place per ADR-0044, no new number); ROADMAP sub-row 26.3 `in-progress → done` + **parent row 26 `in-progress → done` ATOMICALLY** (the 18/19/22/24/25 ROLLUP); the §9 Network-filters family stays OPEN (6 candidates remain: redis/mongo/kafka_broker/thrift/zookeeper/sni_cluster).
- **last-commit:** `<26.3-PLAN-squash>` — `phase 26.3 PLAN: bite-sized TDD decomposition of the 26.3 SPEC §10 spine [ADR-0216/0217/0218]` (the 15-task PLAN + plan-document-reviewer advisories + STATE/ROADMAP advance + next-prompt rewrite; squash-merged from `phase-26.3-network-filter-rbac-plan`; the SHA is filled by the immediately-following next-prompt master-tip repoint commit, per the 26.3-SPEC `ef22eb7`←`c4e5f20` precedent). Predecessor: `ef22eb7` (the 26.3 SPEC squash). origin/master in sync after push.
- **last-updated:** 2026-05-31
- **next-free ADR:** `ADR-0219` (the 26.3 SPEC DRAFTED ADR-0216 + ADR-0217 + ADR-0218 §Context into DECISIONS.md — tail advances ADR-0215 → **ADR-0218**; §Decision/§Consequences bodies land at 26.3 IMPL per ADR-0044, no new number consumed). After phase-26 phase-done (all of 0213..0218 with bodies): next-free STAYS **ADR-0219**. ADR-0209 escape-valve reserve carried from the §9 family STANDS-UNCONSUMED.

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
