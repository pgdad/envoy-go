# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

> **This file is a POINTER, not an archive** — `BOOTSTRAP_PROMPT.md` §4: *"STATE.md — pointer: active phase, last commit, next action."* It carries the live pointer, the live counts, and a SHORT recent lineage. **Full stage-by-stage history lives in `STATE_HISTORY.md`** (archived at ADR-0288; **nothing was deleted** — the pre-move file is preserved there verbatim, byte-for-byte).
>
> **⚠️ THE RULE ADR-0288 EXISTS TO ENFORCE: there is EXACTLY ONE live `next-skill:`, `lifecycle-state:`, `next-free ADR:`, and count block IN THIS FILE, and they live in §Current pointer directly below.** Before that move, ~330 lines of prepended backlog had buried TWO fossilised "current state" blocks whose `next-skill:` still read *"the 42.2 hedging BRAINSTORM"* and *"the 46.2 SPEC"* while the live phase was **66** — so `grep 'next-skill:' STATE.md` returned **two stale hits and ZERO live ones**, and `grep 'fuzzers:'` returned **48** and **42** when the true count was **55**. **A session may trust a grep of this file again. Keep it that way: when you advance the pointer, EDIT §Current pointer IN PLACE — never prepend a new block above it.**

---

## Current pointer

*(The ONLY live "what next" in this repo. Update IN PLACE at every stage close.)*

- **active-phase:** `phase 94 (tls-connection-error-stat) PLAN done` (2026-09-03) — lifecycle-state **2 -> 3**. `PLAN.md` created (**1662** lines), an **18-task** TDD spine discharging all EIGHT items of `SPEC.md` §16. **ROW 94 STAYS `in-progress`**; `want` STAYS **126**; `ROADMAP.md`, `BEHAVIOR_CONTRACT.md` and `DECISIONS.md` are **BYTE-UNTOUCHED** — that scope is MEASURED across three precedent PLAN commits (`90010c4c`, `bae5e24d`, `acedfd2b`), not inferred. The stage REFUTED **SIXTEEN** claims by execution. ⚠️ **TWO ARE LOAD-BEARING:** (i) the SPEC's `sslLeafRoster` 2x2 **cell D is REFUTED** — removing the predicate is INVISIBLE at BOTH roster sizes, because the guard lives inside `case outcomeOther:` while the two tests it names classify to `outcomeVerifyError`/`outcomeNoCert` and never enter that case; the SPEC's `+2 RED` reproduces only under an Inc-SITE HOIST, so §8 NC 1 MISLABELS a relocation as a predicate removal, and **the roster guards the Inc SITE, never the PREDICATE**; (ii) the unit layer **already reaches `outcomeOther`** via `TestNewManager_LiveHandshake_ALPNNegotiationFailure_Aborts`, so the Inc FIRES TODAY unasserted and the fixture is NOT the only possible instrument.
- **phase-directory:** `docs/envoy-go/phases/94-tls-connection-error-stat/` — `BRAINSTORM.md` (**412** lines) + `SPEC.md` (**500**) + `PLAN.md` (**1662**). ⚠️ **No `PROGRESS.md`, no `REVIEW.md` — a STANDING DEPARTURE, NAMED NOT CLAIMED.** This follows the phase-93 PLAN posture; the 75 and 77 PLANs DID create `PROGRESS.md`, so the condition was EVALUATED at this tip rather than inherited. The IMPL creates `PROGRESS.md` per the lifecycle.
- **lifecycle-state:** **3** (row 94 `in-progress`; `PLAN.md` exists, implementation incomplete ⇒ BOOTSTRAP §5 state 3 ⇒ the next session runs the **IMPL**). ⚠️ **SENTINEL RE-RUN MECHANICALLY AT THIS STAGE'S OWN TIP, ACTUAL OUTPUT:** (1) **`NOT DONE: row 94`, ONE line** — correct and expected while row 94 is open · (2) **SIX**, at `:204 :210 :216 :226 :232 :240`, **all six per-line md5 BYTE-IDENTICAL** to the SPEC's record (⚠️ the digest is METHOD-SENSITIVE: `sed -n 'Np' f | md5sum`, trailing newline INCLUDED; `tr -d '\n'` mismatches all six) · (3) **SILENT**. NC-A **TWO**, NC-B **TWO**, NC-C fired, NC-D **96 / 68**; the check-(2) positive control drives it to **0**. ⇒ **THE SENTINEL DOES NOT FIRE**; `stop` evaluated and deliberately NOT created. ⚠️ **THE MARGIN IS TWO** — and it returns to ONE the moment row 94 flips `done`.
- **next-skill:** **RUN THE PHASE-94 IMPL** (state 3 -> DONE). Execute the 18 tasks at `PLAN.md` §5 via `superpowers:subagent-driven-development`. ⚠️ **The three highest-risk tasks are T5** (the counter table — the ONLY thing that makes the predicate falsifiable, since the roster provably cannot), **T10** (boot both sides on the `tls_params` config BEFORE writing any arm — MEASURED to work at this PLAN, but re-run it at your own tip) and **T12** (the five drive arms, for which NO precedent exists in this tree — it is also the split-gate watch point). ⚠️ **The PLAN's self-review caught THREE would-be compile errors in its own draft** (`gaugeValue` does not exist; `Gauge` has `Load()` not `Value()`; `startOneWayTLSListener` takes a `handshakeTestPKI` and returns two values, not three) — treat every 'existing helper' cited anywhere as a claim to re-verify.
- **last-commit:** ⚠️ **TAKE THE TIP FROM `git rev-parse master`, NEVER from a subject grep and NEVER from a count quoted anywhere.** ⚠️ **RUN *BOTH* LOCATE FORMS AND RECONCILE THE SETS** — slug-anchored `--grep '^phase 94 (tls-connection-error-stat)'` and loose `--grep '^phase 94'`. **NO COUNT IS QUOTED HERE, DELIBERATELY — BOTH FORMS ROT**, and a slugless correction commit is invisible to the slug form.
- **last-updated:** 2026-09-03 (phase-94 **PLAN done**; **ROW 94 STAYS `in-progress`**, `want` STAYS **126**, `ROADMAP.md` STAYS **244** lines and BYTE-UNTOUCHED, `BEHAVIOR_CONTRACT.md` STAYS **5987** and BYTE-UNTOUCHED, `DECISIONS.md` STAYS **18834** and BYTE-UNTOUCHED with tail **ADR-0316** and the house `PROPOSED` guard STILL ARMED — the IMPL disarms it).
- **next-free ADR:** **`ADR-0317`**, UNCHANGED — this PLAN consumed no ADR number, and that is now MEASURED, not asserted: `DECISIONS.md` is untouched at ALL THREE precedent PLAN commits. TAIL-derived (`grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1` -> `## ADR-0316`; `grep -c '^## ADR-0317'` ⇒ **0**). ⚠️ **NEVER derive from the heading count** — the id space is SPARSE with exactly one gap at `ADR-0209`, so headings+1 reads **316**, an id already TAKEN.

---

## Project counts at this commit

*(⚠️ **NOT LIVE — THIS BLOCK IS A PHASE-76-ERA SNAPSHOT, AND IT SAID "Live" FOR SIXTEEN PHASES.** Corrected at the phase-93 BRAINSTORM. The bullets below are RETAINED VERBATIM for their historical detail and their negative controls; **their headline figures are stale and are superseded by the correction table immediately below.** The live pointer is §Current, above.)*

⚠️ **CORRECTION LANDED AT THE PHASE-93 BRAINSTORM (2026-08-29).** The bullets below were re-run mechanically at the **phase-76 IMPL close** — sixteen phases ago — under a header that read *"(Live. Re-verify at session start …)"*. **Three of their figures were re-derived at `40cf6766` and are superseded here; each command is given so the next session need not trust this line either:** **active differential fixtures 119 -> `121`** (`ls -d test/fixtures/*/ | wc -l`; tail `0119-grpc-unary-trailers`, **`0120` FREE** — ⚠️ the plausible `grep -cE '^[0-9]{4}-'` form reads **119** because it drops `0007a`/`0007b`) · **fuzzers 55 -> `56` targets across `48` FILES** (`git grep -c '^func Fuzz' -- '*.go'`; the occurrence form agrees — ⚠️ **it went stale INSIDE row 92's own commit**, which added `FuzzValidateResponseHeaderBlock` while the row's ROADMAP text still anticipates `+0`) · **DECISIONS.md tail ADR-0298 -> `ADR-0314`, next-free `ADR-0315`** (TAIL-derived; headings+1 COLLIDES at the `0209` gap). ⚠️ **THE STAT SURFACE IS DELIBERATELY NOT RENUMBERED.** Three different absolutes are live in this tree at this one tip — this block's, the phase-92 `PLAN.md`'s, and the `BEHAVIOR_CONTRACT.md` ledger tail's — and the contract warns of itself that a mechanical re-derivation *"should expect the re-derived figure to disagree."* Per `reference_a_drift_correction_is_itself_a_claim`, **on a contested count: NO NUMBER.** Enforcement remains the per-phase `TestNoNewStat*` delta guards, never an absolute. ⚠️ **AND `406` / `406 -> 407` MUST NOT BE RESTATED ANYWHERE** — refuted at the phase-92 PLAN; `grep -c '406' BEHAVIOR_CONTRACT.md` reads **0**. **The remaining bullets (BackendKind tail 38, go.mod, reference pin, conformance) were re-verified at `40cf6766` and still hold.**

- **active differential fixtures:** **119** — ⚠️ **Phase 76 landed +0: `0061-lb-ring-hash` was EDITED, not added** (`sourceIPs` 4 -> 16 plus a NEW `driver/linkage_test.go` inside the EXISTING fixture directory — no new YAML, no new directory, no new BackendKind, no new port), so the count STAYS **119** and `0118` remains FREE. NC observed: `mkdir .../0118-fake` ⇒ 120. — `ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0117-tracing-custom-tags-metadata-cluster` (phase 73). **Phase 75 landed +0**: `0110-tls-require-client-cert-false` was **EXTENDED** with a cross-side `fixture.StatsAsserter` (NO new YAML, NO new directory, NO new BackendKind, NO new port), so the count STAYS **119** and `0118` remains FREE. ⚠️ **Next-free REFERENCE port is `10450`, NOT `10118`** — ports are NOT fixture-index aligned. **The fixture now asserts what its own README confessed was infeasible, and BOTH sides were EXECUTED**: `ssl.handshake=2 ssl.no_certificate=1 ssl.fail_verify_error=1 ssl.fail_verify_no_cert=0`, `downstream_cx_total=3`, **identical on reference and subject**, with the discriminating asymmetry (`handshake=2` vs `no_certificate=1`) observed per side. Shape A over `/stats/prometheus`, keyed on the metric NAME with `envoy_listener_address` IGNORED — the NAMES are byte-identical cross-side while the labels differ (`0.0.0.0_10446` reference vs **`___20016`** subject: ⚠️ **the subject binds the IPv6 WILDCARD**). ⚠️ **`0111` was NOT given a value assertion, deliberately** — it runs `require_client_certificate: true` on both sides, so a success-path no-cert annotation reads **0 on every arm, structurally**, and a value pin would document a **vacuous `0 == 0`** as coverage; its PROSE gained the name in its NAMED-UNASSERTED list instead, because **both `0111` passages are CLOSED ENUMERATIONS** and a name in neither list reads as ASSERTED. **`0111`'s driver is byte-untouched, and its named-subset check was EXECUTED for the first time at this IMPL: PASS.** ⚠️ `var _ fixture.StatsAsserter` is a **TRIPWIRE, not the dispatch mechanism** — `0110` is only the **THIRD** fixture repo-wide to carry it, while **82** define an `AssertStats` method.
- **fuzzers:** **55** — **Phase 76 landed +0** (it consumes no new config field, so there is no parse arm to fuzz). NC observed: appending `func FuzzProbeOnly` ⇒ 56. — `grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`. **Phase 75 landed +0** — the row consumes no new config field at all, so there is no parse arm and no fuzz-seed classification to flip (`reference_fuzzer_count_docs_drift`: an `f.Add` seed is not a fuzzer).
- **stat surface:** **1205** — BEHAVIOR_CONTRACT doc count. **There is NO mechanical counting command** (the contract says so itself); enforcement is per-phase `TestNoNewStat*` guards asserting a delta of ZERO, never the absolute total. **Phase 75 LANDED +1 (1204 -> 1205)** — `listener.<normalized-addr>.ssl.no_certificate`, the FOURTH listener-scope `ssl.*` name and the FIRST **SUCCESS-PATH** handshake annotation (⚠️ **NOT a synonym for `ssl.fail_verify_no_cert`, and the mis-mapping is wrong in BOTH directions** — one annotates an ACCEPTED handshake, the other books a REJECTED one; they are mutually exclusive on every connection). ⚠️ **The delta is a NAME-surface delta, NOT a flat per-deployment one:** registration rides the block that ALREADY gates on `rt.tlsMode`, so a **plaintext-only** deployment gains **ZERO** names, while a **QUIC** listener gains it permanently zero — **PARITY** with the reference, and structural (`Manager.Start` launches no accept loop for `kindQUIC`, so the Inc site is unreachable for a **name-independent** reason). ⚠️ **QUIC was NOT re-probed at phase 75** — the parity claim is INHERITED from phase 74's family-level probe, and that inheritance is stated in BEHAVIOR_CONTRACT rather than presented as a measurement. Ledger updated: `1204` -> `1205` at `BEHAVIOR_CONTRACT.md:831`/`:847`, plus a `**Phase 75 - 1204 -> 1205 (+1)**` line after the tail entry — **the ONLY line-adding edit in the file** (`5744 -> 5746`), so every by-line citation into the TLS subsection stays valid. ⚠️ **The new entry RECORDS BOTH ledger gaps and BACK-FILLS NEITHER** (fabricating a line would be inventing a record). Gate figures at this close: `grep -c '1204'` => **2** · `grep -c '1205'` => **3** · `grep -cE '^#{2,4} .*ADR-[0-9]+.*ADR-[0-9]+'` => **17** (was 16 — the `:1849` heading joined them, on the `:785` precedent). ⚠️ **`internal/statssink`'s four *"stays 1200 / 1196"* prose sites, stale since phase 49, are RECORDED-NOT-FIXED** — that package is on this phase's byte-untouched sha256 roster.
- **BackendKind tail:** **38** — **Phase 76 landed +0** (`0061` uses the in-process `TCPEcho` branch and implements neither `BackendKindAware` nor `PerHostBackendKind`). NC observed: appending `ZZProbeKind = 39` ⇒ tail 39. — (`H2GoawayResponder`, `test/differential/fixture/fixture.go:614`). **Phase 75 landed +0** — `0110` was extended, not cloned, and it implements neither `BackendKindAware` nor `PerHostBackendKind`, so the runner takes the in-process `TCPEcho` branch. ⚠️ **This is a TAIL VALUE, not a count** — the file declares **39** constants (values 0-38, `TCPEcho = 0`). The lineage figure **38** is correct as written; do NOT 'fix' it to 39.
- **DECISIONS.md tail:** **ADR-0298** (next-free **ADR-0299**) — `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1` -> `## ADR-0298`; `grep -c '^## ADR-0299'` ⇒ **0**. **ADR-0298 is COMPLETE as of the phase-76 IMPL** — §Context drafted at the phase-76 SPEC, §Decision + §Consequences APPENDED IN PLACE after the RETAINED italic footer, **no renumber and NO `---` separator** (the file's last `^---$` is still at `:17020`). Block shape controller-re-derived at this close: **1** `### Context` / **1** `### Decision` / **1** `### Consequences` / **1** retained footer. ⚠️ **FOUR of its OWN §Context paragraphs were CORRECTED IN PLACE** (¶1's `:295` -> `:307`, ¶5's `-count ~= 81` -> **83**, ¶7's *"~71 collapses"* -> **74/2000**, ¶11's *"needs no edit"* -> REFUTED) on the ADR-0297 ¶7/¶9 precedent, each **leading with what survives**; none changes a decision the ADR records. ⚠️ **It carries NO whole-file grep count and claims NO family ordinal** — the Load-balancing family closed at row 54, and **maintenance rows do not extend a charter**. *(Historical, retained: **ADR-0297 is COMPLETE** — §Context drafted at the phase-75 SPEC (ADR-0044-**as-used**; ⚠️ ADR-0044 does NOT actually contain that discipline), §Decision + §Consequences appended IN PLACE at the phase-75 IMPL after the **RETAINED** footer, mirroring ADR-0295/0296. Block shape verified at this close: **1** `### Context` / **1** `### Decision` / **1** `### Consequences` / **1** retained footer; `grep -c '^## ADR-0298'` => **0**. **No renumber, no new ADR.** ⚠️ **Its OWN §Context ¶7 and ¶9 were CORRECTED IN PLACE while completing it** — ¶7's **self-falsifying whole-file grep** (the same species the phase-74 IMPL had just fixed in ADR-0296 ¶3, reproduced one ADR later) is restated with **NO number** and the searched token deliberately **NOT spelled**, because every restatement mints another counter-example; and ¶9's *"SELF vs OTHER-ADR"* form rule is replaced by **graft SCALE** after the n=7 population was re-enumerated first-hand. ⚠️ **¶9 ALSO said *"the only two instances in the file"* — which this row's own blockquote makes three** — so a pure append would have shipped a SECOND self-falsifying count in the commit that fixed the first. **The prescription has now outlived THREE wrong justifications: family, self-vs-other, scale.** ⚠️ **`ADR-0296` is NO LONGER byte-untouched** — its §Decision (g) carries the phase-75 correction as an INDENTED blockquote (`grep -c '^  > '` **2 -> 3**), **LEADING WITH WHAT SURVIVES** (no such counter was owed *in phase 74*, whose sole stat-asserting fixture is `require=true` on both sides — **the deferral was RIGHT**) and claiming **NO ORDINAL**.)*
- **go.mod modules:** **Phase 76 landed +0** — `go mod tidy -diff` EMPTY and `git diff master -- go.mod go.sum` EMPTY; ⚠️ **the row's TEST-side import delta is +8 and that is CORRECT** (`ringhash_test.go` gains `fmt`/`math`/`mathrand "math/rand/v2"`, `linkage_test.go` adds `go/ast`/`go/parser`/`go/token`/`strconv`/`testing`) — all stdlib, all test-side, and **an import LINE is not a go.mod MODULE**; there is no new sub-package, so `reference_new_subpackage_pulls_transitive_module` does not bite. NC observed: a fake `require` ⇒ 67 -> 68. The tracked lineage figure is **2** (the phase-61.2 count: `quic-go` direct + `qpack` indirect) — **NOT a repo total**; the single `go.mod` requires **67** (18 direct + 49 indirect). **Phase 75 landed +0** — `go mod tidy -diff` EMPTY and `git diff master -- go.mod go.sum` EMPTY. The row needed `crypto/tls` and `internal/stats`, both ALREADY imported by `internal/listener/manager.go`; **an import LINE is not a go.mod MODULE**, and there is no new sub-package, so `reference_new_subpackage_pulls_transitive_module` does not bite. ⚠️ **TEST-side imports grew by exactly four** (`log`, `math`, `net/http`, `strconv` in `0110`'s driver) — all stdlib, all test-side, **not a production-envelope violation**.
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` (see `ENVOY_TARGET.md`).
- **conformance:** h2spec **`95 tests, 94 passed, 1 skipped, 0 failed`** (the skip is 6.9.2/2, invariant) — the CORRECTED 95-case strict scope under dotted selectors, measured at the phase-85 IMPL close 2026-08-10 (ADR-0307; the historical 53/53 records measured only 53 of the 95 under nine silently no-op slash-form selectors, 2026-04-25 -> 2026-08-10); proxy-wasm all 10 families PASS.

---

## Recent lineage

*(A SHORT recent lineage, capped at FIVE prior entries by ADR-0288. Everything earlier is in `STATE_HISTORY.md`. §Current carries the newest stage — **the phase-94 PLAN**, an 18-task TDD spine that refuted SIXTEEN inherited claims by execution, two of them load-bearing enough to change the plan's own shape. The entry displaced from the tail of this list at this close was the **OPENING stage of the phase preceding the current one**; it is archived verbatim in `STATE_HISTORY.md` and is deliberately NOT named here, because a preamble that SPELLS the evictee's label makes a naive grep of this file read 1 and defeats the eviction check. ⚠️ **A DIRECT DATE READ WAS PERFORMED AT THIS CLOSE AND FOUND A TIE — `2026-09-01` x2, `2026-08-31`, `2026-08-29` x2 — so the oldest was NOT unique**, unlike the preceding close, which found uniqueness and said so. The tie was broken by LIFECYCLE ORDER within the shared date (this list is newest-first; a BRAINSTORM precedes its own phase's SPEC, so the TAIL entry is the older). ⚠️ **THAT IS FOUR DIFFERENT SHAPES IN FOUR CONSECUTIVE CLOSES. INHERIT NONE; READ THE DATES YOURSELF.**)

- **prior active-phase:** `phase 94 (tls-connection-error-stat) SPEC done` (2026-09-02) — lifecycle-state **1 -> 2**. `SPEC.md` created (**500** lines), discharging all EIGHT items of `BRAINSTORM.md` §10. **ADR-0316 §Context DRAFTED** (`DECISIONS.md` **18804 -> 18834**, tail **ADR-0315 -> ADR-0316**, next-free **ADR-0317**; `^---$` **STAYS 216**), and the strict house `PROPOSED` guard **RE-ARMED 0 -> 1** at `DECISIONS.md:18808`, resolved to `## ADR-0316` by a BACKWARD heading search. **ROW 94 STAYED `in-progress`**; `ROADMAP.md` and `BEHAVIOR_CONTRACT.md` **BYTE-UNTOUCHED**. The stage REFUTED **TWELVE** inherited claims by execution, three of them against its own agents — including that the BRAINSTORM's §7.2 verdict table is a CATEGORY ERROR that derived its one wrong row by the very message-text matching the design forbids.

- **prior active-phase:** `phase 94 (tls-connection-error-stat) BRAINSTORM done` (2026-09-01) — lifecycle-state **DONE -> 1**. **PHASE 94 OPENED BY SELF-PICK** per the 2026-07-12 standing directive; there was no banked mid-lifecycle work and no human pick was solicited. **ROW 94 REGISTERED `in-progress`** at `ROADMAP.md:156` — an **ADD, not a flip**, so the denominator MOVES: `want` **125 -> 126** and `ROADMAP.md` **243 -> 244**. Row field-count **NF=8 under BOTH the naive and the escape-aware form, COUNTED BEFORE AND AFTER INSTALLING** (the summary is a comma list carrying **no pipe character at all**; total `|` chars in the row = **7**, the delimiters only). ⚠️ **A POSITIVE CONTROL WAS RUN ON THE FIELD-COUNT INSTRUMENT ITSELF**: a deliberately-malformed draft carrying `(a|b)` reads **NF=9 under BOTH forms while check (1) stays SILENT about it** — and the precise mechanism is newly stated: a pipe in the SUMMARY (field 6) leaves `$5` intact so STATUS parsing is unaffected, whereas a pipe in the TITLE would corrupt `$5` and change the failure mode entirely. **Subject:** give `outcomeOther` a predicate-gated `Inc()` landing `listener.<normalized-addr>.ssl.connection_error`, the FIFTH listener-scope TLS counter, closing the NAMED DEPARTURE at `BEHAVIOR_CONTRACT.md:1971`. **THE BLOCKING UNKNOWN OF FIVE PRIOR STAGES IS NOW MEASURED**: twelve arms on the live pinned image (digest `7edd5b0fd763…` **verified against `ENVOY_TARGET.md` before any arm was trusted**), a two-listener **set reconciliation**, a positive control and a discriminating negative control — **the reference increments `ssl.connection_error` ONLY on a BoringSSL SSL protocol error and books NOTHING for a transport EOF or reset.** ⚠️ **ELEVEN CLAIMS REFUTED, and the largest is that the reference side was `unprobed`: PHASE 77 MEASURED IT on 2026-07-26 and the result NEVER LEFT ITS BRAINSTORM** — `SSL_ERROR_SSL` reads **0** in `DECISIONS.md` and **0** in `BEHAVIOR_CONTRACT.md`, so phase 93 rejected the candidate for lacking evidence that already existed. ⚠️ **The `~4-6 production lines` figure is REFUTED — measured 35-55 added / 12-22 removed across the same 2 files**, because four landed prose comments become false and `internal/stats/name.go` carries an **unguarded `Of the 30 entries` count inside the file it counts** (verified: exactly 30, and `len(helpText)` is asserted NOWHERE). ⚠️ **The test-pin surface spans FOUR files the inherited estimate never named, including TWO exact-set `reflect.DeepEqual` pins and one trap whose own comment predicts this exact change staying SILENTLY GREEN.** ⚠️ **The new name PREPENDS, not appends** (`LC_ALL=C`: `c` sorts first of five), so phase 75's append precedent does not transfer. ⚠️ **Phase 77's `cannot be typed in Go` blocker FALLS BY INVERSION** — the positive population is open and untypeable, but its transport COMPLEMENT is closed and every member is `errors.Is`-able. **A NEW FIXTURE IS FORCED** (`0120`, verified free; no existing arm anywhere produces an `outcomeOther`). ⚠️ **CHARTERED AS NARROWING ONLY** — check (2) keys on the PHRASE, not the clause, proven by this stage's own positive control, so this row moves the sentinel by **NOTHING**.

- **prior active-phase:** `phase 93 (h2-local-reply-content-length) IMPL done` (2026-09-01) — lifecycle-state **3 -> DONE**. **PHASE 93 CLOSED; ROW 93 FLIPPED `in-progress` -> `done`** at `ROADMAP.md:155` (a FLIP, not an ADD — `want` STAYS **125**, `ROADMAP.md` STAYS **243** lines, and the row field-counts **NF=8 under BOTH the naive and the escape-aware form, COUNTED BEFORE AND AFTER the edit**). ⚠️ **THE FIRST INSTALL OF THAT ROW READ NF=11 UNDER BOTH FORMS** — the IMPL summary embedded a regex alternation whose THREE `|` characters are field separators; escaping them to `\|` fixed only the escape-aware form (8) and left the naive form at **11**, so the alternation was rewritten as a COMMA LIST carrying no pipe at all. ⚠️ **`reference_markdown_row_unescaped_pipe_passes_gate` FIRED FOR REAL: check (1) passed at every one of those three states.** Only the field count caught it. **THE FIX LANDED AT THE COMPOSER**: `h2LocalReplyHeaders(bodyLen int)` emits `Content-Length` after `Content-Type`; seven call sites, four pass `0`, three pass `len(bad502Body)`=12. **MEASURED COSTS, EVERY ONE REFUTING A PREDECESSOR ESTIMATE**: production **`+14/-10`** (PLAN `+15/-10`), recompute pin **`+147/-0`** (SPEC `~+60`, 2.45x), router unit test **`+104/-0`** (SPEC `~+117`), `driver.go` **`+313/-69`** and `driver_test.go` **`+216/-45`** (SPEC `~+300/-44` and `~+57/-10`), README **`+11/-9`**, `BEHAVIOR_CONTRACT.md` **`+21/-0`** (PLAN `~+3/-0`, 7x), `DECISIONS.md` **`+131/-1`**. ⚠️ **A FALSE CLAIM WAS CAUGHT BEFORE SHIPPING: *"the wire bytes are unchanged"* IS FALSE** — `writeH2Reply` overrides a `content-length` ONLY when one is already present and has NO synthesize-when-absent arm (unlike `server`/`date`), pinned by the new `absent_stays_absent` NEGATIVE CONTROL which passes on the UNMODIFIED tree; the H/2 502 previously reached the peer with NO `content-length` and now carries one. **The VALUE is invariant; the PRESENCE is not.** ⚠️ **The PLAN's ZERO-CHURN claim is about the cross-side TRANSCRIPT, is TRUE, and is a DIFFERENT OBJECT** (it records only `status=`/`illegal=`) — **the two were being conflated.** ⚠️ **A PLAN GATE WAS ARITHMETICALLY IMPOSSIBLE AND BLIND**: `want 9` counts a doc mention with no open paren; measured **8 BEFORE and 8 AFTER**, INVARIANT across the fix. ⚠️ **THE PLAN'S *"FAIL=6 BEFORE"* BASELINE WAS MEASURED IN THE WRONG STATE** — `driver_test.go` is a PURE COMPARATOR, so its rows redden only AFTER the constant flips; the genuinely-red pre-fix gate is the DIFFERENTIAL, which failed with EXACTLY ONE error line. **INSTRUMENT UPGRADED, NOT DELETED**: three-state `declared` (absent / duplicated / parsed; a DUPLICATE yields not-OK, NEVER the first value), `declared == delivered` per side and relaxed on NEITHER, body pins ref **87** / subj **12** RE-MEASURED LIVE, plus a ROSTER BARRIER. **SEVEN negative controls each neutralized ALONE + THREE live Docker positive controls**; unit table **10 -> 18 rows**. ⚠️ **THE 503/504 BLIND SPOT IS OPEN, NOT CLOSED.** **ADR-0315 COMPLETED IN PLACE**; house `PROPOSED` guard **DISARMED 1 -> 0** at `:18650`, decoy at `:14866` still **1**.

- **prior active-phase:** `phase 93 (h2-local-reply-content-length) PLAN done` (2026-08-31) — lifecycle-state **2 -> 3**. `PLAN.md` created (**1031 lines**), discharging all EIGHT items of `SPEC.md` §14. **ROW 93 STAYS `in-progress`; `ROADMAP.md` is BYTE-UNTOUCHED** (sha256 `82baac19fa2c1c13…` identical across the stage; a `--numstat` of NOTHING is the evidence) and sentinel `want` STAYS **125**. **A PLAN ADDS NO ADR:** tail STAYS **ADR-0315**, next-free STAYS **ADR-0316**, `^---$` STAYS **216**, `^## ADR-` STAYS **314**, and the house `PROPOSED` guard STAYS **ARMED at 1** (verified BY LINE AND BY ADR — `:18650` under `## ADR-0315`; the `^**Status:** PROPOSED` decoy at `:14866` under `## ADR-0231` also reads 1 and is byte-untouched). **ZERO production `.go` bytes.** ⚠️ **THE STAGE REFUTED TWELVE INHERITED CLAIMS, FIVE OF THEM THE SPEC'S OWN MEASURED FIGURES, AND DISCHARGED THE TWO ITEMS THE SPEC COULD NOT MEASURE.** **§14 item 3 DISCHARGED: `p93WantRefBodyLen = 87` is CONFIRMED by live Docker measurement on all five arms** (`declared=87 bodyLen=87`), and `p93WantSubjBodyLen = 12` is **measured**, not merely derived — it was a CLAIM until this stage. **§14 item 2 DISCHARGED: the cross-side byte transcript has ZERO churn**, proven by sha256 (420 B, `c9739f62…`, both sides, pre- and post-fix) — and structurally, **there is NO stored baseline anywhere in `test/`** to re-baseline (`golden|baseline|snapshot` reads **0**) and `expectations.yaml` is **prose, not machine-evaluated** (ADR-0019). ⚠️ **SPEC §6.1's `~+60` RECOMPUTE-PIN ESTIMATE IS REFUTED — MEASURED 147 LINES, ~2.45×**, from a compiling file whose NC-1 and isolating NC-2b both fire. ⚠️ **SPEC §4's hcm baseline `323` IS REFUTED — it is `322`**, measured on the clean tip AND with the fix (the fix touches no hcm test); router **129** CONFIRMED exactly. ⚠️ **SPEC §6's *"18-row table"* IS NOT REPRODUCIBLE — the `AssertDistribution` table is TEN rows**; §8's `10 -> 18` is the correct reading, and the SPEC's underlying claim (the table is synthetic, not a production gate) survives intact. ⚠️ **TWO GATES THE SPEC NEVER NAMES MUST BE RE-BASELINED** — `driver.go` `+1/-1` and `driver_test.go` `+8/-8` — **and ONE OF THOSE ROWS IS A NEGATIVE CONTROL THAT MUST INVERT, NOT SHIFT**, or it silently stops discriminating. ⚠️ **THE EMPTY-VS-ZERO TRAP IS LIVE ON THIS FIXTURE**: pre-fix the subject's declared value is **`<absent>`, not `0`**, so `declared` must be a three-state observation. ⚠️ **TWO SPEC "DRIFT-PROOF" ANCHORS ARE NOT UNIQUE** (`val = strconv.Itoa(len(body))` matches 2 production lines, `chain.RunEncodeHeaders` matches 3) and `directResponseAction.body()` is cited without its package — it is **`internal/filter/hcm/actions.go`**, the mirror image of the SPEC's own `hcm/chain.go` warning. **Production cost MEASURED `+15 / -10`** (the SPEC's `+10/-8` fix-alone figure CONFIRMED byte-exact; its `~+12/-10` combined estimate refuted — the cheapest honest de-rot is `+13/-10`). **§14 item 6 DECIDED: the H/3 comment de-rot is BANKED** — it changes no behaviour and is wrong independently of this row.

- **prior active-phase:** `phase 93 (h2-local-reply-content-length) SPEC done` (2026-08-29) — lifecycle-state **1 -> 2**. `SPEC.md` created (**409 lines**); **ADR-0315 §Context DRAFTED** (`DECISIONS.md` **18646 -> 18674**, tail **ADR-0315**), and the house `PROPOSED` guard armed **0 -> 1**, NOT 1 -> 2 as its router instructed — the two forms are different regexes and only a UNION reads 1 -> 2. **ZERO production `.go` bytes; `ROADMAP.md` BYTE-UNTOUCHED.** ⚠️ **THE STAGE REVERSED THE BRAINSTORM'S CENTRAL PICK** — `h2LocalReplyHeaders(bodyLen int)`, NOT a parameterless placeholder: the carrier is read by `chain.RunEncodeHeaders` ~26 lines BEFORE `writeH2Reply` corrects it, so a placeholder ships a SELF-CONTRADICTORY ACCESS-LOG RECORD on all three 502 sites unconditionally. It also corrected the charter's load-bearing `ADR-0085` attribution (the doctrine is ratified in **ADR-0155**, which merely cites ADR-0085), refuted the "two contradictory ratified rules" claim, and named the instrument's blind spot rather than claiming it closed. ⚠️ **The phase-93 PLAN later refuted FIVE of this stage's own measured figures** — the `~+60` recompute pin (real: **147**), the hcm baseline `323` (real: **322**), the *"18-row table"* (real: **10**), the `~+12/-10` combined patch (real floor: **+13/-10**), and two "drift-proof" anchors that are NOT unique.




## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
