# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `07.2-listener-chain-completion`
- **phase-directory:** `docs/envoy-go/phases/07.2-listener-chain-completion/` — now contains `README.md` (sibling-stub seeded at the 07.1 SPEC commit `ee45aba`; read-only history), `SPEC.md` (1101 lines; drafted at master `bb5f437`; read-only history), and the full `PLAN.md` (2850 lines; drafted at this commit per ADR-0005 autonomous planning; plan-document-reviewer subagent approved at iteration 1 with three minor issues addressed in-place — `chainSpecificityRank` lifecycle clarified to deletion at Task 9 step 7 since chainmatch.go's `sniSpecificityRank` is the only live copy post-refactor; Task 14 step 2 pseudo-code's `DriveAlternate` error-check added; Architecture text's `AlternateConfigDriver` enumeration completed to 5 methods matching Task 13 step 1). Parent `docs/envoy-go/phases/07-filter-chain-framework/` carries `BRAINSTORM.md` (688 lines, `da28039`) + parent master `SPEC.md` (113 lines, `ee45aba`) — both read-only history. Sibling 07.1 directory at `docs/envoy-go/phases/07.1-http-filter-framework/` is closed read-only history. Phases 04, 05 (+ 05.1, 05.2), and 06 (+ 06.1, 06.2) remain closed read-only history.
- **lifecycle-state:** `4` — Implementation complete, not verified. All 17 PLAN tasks landed on `phase/07.2-listener-chain-completion-impl`; PROGRESS.md SHA-fills consolidated into THIS follow-up per ADR-0017 small-mechanical-fixes (one consolidation rather than 17 individual SHA-fill commits). Next session executes `superpowers:verification-before-completion` per `BOOTSTRAP_PROMPT.md` §5 step 4.
- **next-skill:** `superpowers:verification-before-completion` — phase 07.2 verification session (state 4 → 5 per `BOOTSTRAP_PROMPT.md` §5 step 4). Re-runs the local six-gate sweep against HEAD with command output quoted into PROGRESS.md as a `## Verification` entry; on all gates green, advances STATE to lifecycle-state 5 with `next-skill: superpowers:requesting-code-review`. The verification session does NOT modify production code — only PROGRESS.md (and STATE.md / ROADMAP.md as the lifecycle ratchet permits).
- **next-skill-scope:** Lifecycle-state 4 → 5 verification session deliverable: re-execute the closing six-gate sweep against HEAD on `phase/07.2-listener-chain-completion-impl` and quote ALL command outputs into a `## Verification` entry in `docs/envoy-go/phases/07.2-listener-chain-completion/PROGRESS.md`. The gates per ADR-0001 + SPEC §15 + the Task-17 sweep precedent: (a) `go build ./...` clean; (b) `go vet ./...` clean; (c) `golangci-lint run ./...` clean; (d) `go test -race -count=1 -short ./...` all packages PASS; (e) the differential suite for the phase's feature surface — fixtures 0000 through 0008 (including 0007a + 0007b) — at the ENVOY_TARGET pin (run via `make differential` or the equivalent `go test ./test/differential -run=...` invocation; quote the per-fixture PASS/FAIL line); (f) h2spec conformance 53/53 PASS at the ADR-0051 image pin (`summerwind/h2spec@sha256:5f4a65c30cae8569558ced048b4bfe0dcf01a221e36767ae504ccd8348a7aeb0`); (g) all 10 fuzzers run at `-fuzztime=30s` each (the 9 inherited + the new `FuzzFilterChainMatch` from Task 6) — zero crashers, zero persisted corpus failures. On all gates green, the verification session lands a single follow-up commit advancing STATE.md to lifecycle-state 5 with `next-skill: superpowers:requesting-code-review` and recording the gate outputs in PROGRESS.md `## Verification` entry. The REVIEW session (lifecycle 5 → 6) lands the phase-done commit (rows 07.2 + parent 07 → `done` AT THE SAME COMMIT per parent SPEC §5; STATE advances to phase-08 lifecycle-state 0 with `next-skill: superpowers:brainstorming`).
- **last-commit:** `TBD` — `phase 07.2 follow-up: PROGRESS SHA-fills (Tasks 1-17) + STATE → lifecycle-state 4`. This consolidated SHA-fill follow-up lands all 17 task-commit SHA fills in PROGRESS.md (Tasks 1-4, 6-8, 10-17 each take a single SHA; Tasks 5 and 9 take main + review-driven follow-up SHAs per the inter-task review iteration that occurred during implementation) + this STATE.md rewrite to advance to lifecycle-state 4 with `next-skill: superpowers:verification-before-completion`. Per ADR-0017 small-mechanical-fixes: consolidating 17 individual SHA-fill commits into one is the disciplined approach — the fills are pure mechanical reference-resolution with no semantic content. The verification session may SHA-fill this `last-commit: TBD` literal in its own follow-up per the established phase-04..07.1 convention if needed.
- **last-updated:** 2026-05-02

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
