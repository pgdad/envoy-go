# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `08.1-admin-endpoints`
- **phase-directory:** `docs/envoy-go/phases/08-admin-api-and-drain/` — created by THIS commit on branch `phase-08-admin-api-and-drain-brainstorm`; contains `BRAINSTORM.md` (autonomous-brainstorm artifact per ADR-0004). Sub-phase directories `docs/envoy-go/phases/08.1-admin-endpoints/` and `docs/envoy-go/phases/08.2-graceful-drain/` will be created by the next-session SPEC drafting. Parent directory will hold the eventual master `SPEC.md` summarizing both sub-phases (mirrors `docs/envoy-go/phases/06-observability-baseline/` + `docs/envoy-go/phases/07-filter-chain-framework/`). Phase 07 (parent + sub-phases 07.1 + 07.2), phases 04, 05 (+ 05.1, 05.2), and 06 (+ 06.1, 06.2) remain closed read-only history.
- **lifecycle-state:** `1` — phase 08 brainstorm session complete; planner-time-split decision per ADR-0045 lands phase 08 → 08.1 (`admin-endpoints`) + 08.2 (`graceful-drain`). 08.1 is the immediate next focus (four read-only admin endpoints: `GET /config_dump` + `GET /clusters` + `GET /listeners` + `GET /server_info`; reuses the existing phase-01 admin HTTP/1.1 mux; constructor-widening pattern threads `*bootstrap.Bootstrap` + `*cluster.Manager` + `*listener.Manager` into `admin.New()`); 08.2 (graceful drain: drain-state machine in new `internal/drain/` package + SIGTERM-handler upgrade in `cmd/envoy-go/main.go` + `POST /drain_listeners` admin endpoint + `/ready` DRAINING-state extension partially superseding ADR-0015 + `/server_info` `state` field transitions + `listener.Manager.Drain` + `cluster.Manager.Drain`) receives a sibling SPEC stub at the same SPEC-drafting session. Brainstorm captured in `docs/envoy-go/phases/08-admin-api-and-drain/BRAINSTORM.md` settling seven cardinal decisions (Decisions A–G): (A) admin-endpoint MVP surface = exactly four new read-only endpoints; the seeded ROADMAP-row's six-endpoint summary is exhaustive (`/stats/prometheus` already at 06.1; `/ready` already at 01); deferral list per ADR-0040 enumerates 15+ Envoy admin endpoints not in scope; (B) reuse phase-01 HTTP/1.1 admin mux — no new transport, no new bind, no new server; constructor-widening per LBP-1 discipline (mirrors 06.1's `*stats.Registry` + 07.1's `*HTTPRegistry`); (C) `/config_dump` body = `application/json` via `protojson` over `*adminv3.ConfigDump` envelope with three sub-envelopes (`BootstrapConfigDump` + `ListenersConfigDump` + `ClustersConfigDump`); static-only (no dynamic_* arrays since no xDS); MarshalOptions tunables empirically pinned at SPEC time; (D) `/clusters` and `/listeners` body = `text/plain; charset=UTF-8` (Envoy text mode; JSON form deferred per ADR-0040); MVP-emitted line set is a structurally-required subset of Envoy's unconditional output; deferred lines (locality tags, success rates, hot-path counters envoy-go does not yet emit) accommodated via tolerant equivalence-comparator; (E) `/server_info` `state` enum values in 08.1 = `LIVE` + `PRE_INITIALIZING` only (DRAINING + INITIALIZING deferred to 08.2 / never); MVP field set documented; (F) admin endpoint deferral list per ADR-0040 (15+ items: `?format=json` query-param, `?resource=` filtering, `RoutesConfigDump`, `SecretsConfigDump`, `ScopedRoutesConfigDump`, `EndpointsConfigDump`, `POST /reset_counters`, `/runtime`, `/certs`, `/memory`, `/heap_dump`, `POST /quitquitquit`, `POST /healthcheck/*`, `/logging`, `POST /reopen_logs`, `/listeners/<name>/*` sub-routes, `/init_dump`); (G) no-ACL admin-endpoint security posture (operator firewall responsibility; mirrors Envoy default; future security-hardening phase may add). Seven ADRs anticipated for 08.1 starting at ADR-0084 (next-free since 07.2 closed at ADR-0083). Combined 08.1 + 08.2 LoC estimate ~1100–1600 production + ~400–600 fixture/driver crosses both ADR-0045 thresholds (~25 tasks, ~1500 LoC) — same surface-driven split discipline as 06 → 06.1/06.2 + 07 → 07.1/07.2. Per BOOTSTRAP §5 the next transition is state 1 → 2 via `superpowers:writing-plans` (SPEC.md authoring for phase-08 parent + 08.1 sub-phase + 08.2 stub). BRAINSTORM §2.7 documents seven empirical-pin obligations the SPEC session must execute against reference Envoy v1.37.2 (`/config_dump` JSON shape via protojson MarshalOptions; `/clusters` text format byte layout; `/listeners` text format byte layout; `/server_info` JSON field set + state-enum value pre-MarkReady; `Content-Length` vs `transfer-encoding: chunked` framing per endpoint; per-endpoint header set; pre-MarkReady `state` value `PRE_INITIALIZING` vs `INITIALIZING`) — these are unresolvable from documentation alone, mirroring the 06.1 SN4 + 07.1 §2.6 four-pin pattern.
- **next-skill:** `superpowers:writing-plans` — SPEC.md authoring for phase 08.1. Inputs: this commit's `docs/envoy-go/phases/08-admin-api-and-drain/BRAINSTORM.md` (authoritative design source — see `## 11. Hand-off to writing-plans`), `BOOTSTRAP_PROMPT.md` §§5–6 (lifecycle + planner-time split contract), `SKILL_ROUTING.md`, the closed-history `docs/envoy-go/phases/07.1-http-filter-framework/SPEC.md` and `docs/envoy-go/phases/07.2-listener-chain-completion/SPEC.md` as structural precedent (header layout, §-numbering conventions, acceptance-bullet shape, empirical-pin verbatim subsections), `docs/envoy-go/phases/07-filter-chain-framework/SPEC.md` and `docs/envoy-go/phases/06-observability-baseline/SPEC.md` as parent-master-SPEC precedent, `docs/envoy-go/BEHAVIOR_CONTRACT.md` (the existing `## Admin API — /ready` section to be restructured into a `## Admin API` umbrella per BRAINSTORM §8), `docs/envoy-go/ENVOY_TARGET.md` (Envoy v1.37.2 pin — required for the seven BRAINSTORM §2.7 empirical-pin obligations), and the existing `internal/admin/admin.go` + `internal/admin/prometheus.go` code (the mux + `*stats.Registry` threading patterns the 08.1 SPEC extends).
- **next-skill-scope:** Lifecycle-state 1 → 2 SPEC drafting session deliverables: (1) `docs/envoy-go/phases/08-admin-api-and-drain/SPEC.md` — parent master SPEC summarizing both 08.1 + 08.2 scopes (mirrors `docs/envoy-go/phases/06-observability-baseline/SPEC.md` + `docs/envoy-go/phases/07-filter-chain-framework/SPEC.md`); (2) `docs/envoy-go/phases/08.1-admin-endpoints/SPEC.md` — full sub-phase SPEC derived from BRAINSTORM §§2–10; **must empirically pin the seven §2.7 obligations** by running reference Envoy v1.37.2 and copying output verbatim: (a) `/config_dump` JSON shape (boot Envoy with the §2.6 fixture bootstrap, scrape `/config_dump`, copy first 50 lines verbatim, derive protojson MarshalOptions: `Multiline`, `Indent`, `UseProtoNames`, `EmitUnpopulated`); (b) `/clusters` text format byte layout (which `<cluster>::<key>::<value>` triples Envoy emits unconditionally vs. conditionally); (c) `/listeners` text format byte layout (does Envoy emit only `<name>::<addr>` per listener, or additional fields); (d) `/server_info` JSON field set + state-enum value (which `state` enum value pre-MarkReady — `PRE_INITIALIZING` vs `INITIALIZING`; whether `command_line_options` is `{}` when no flags set; `version` string format; `hot_restart_version` value when hot restart not configured; `uptime_*` units); (e) `Content-Length` vs `transfer-encoding: chunked` framing per endpoint (phase 01 documented `/ready`'s framing deviation; verify whether the four new endpoints use chunked or fixed-length); (f) per-endpoint header set (`Cache-Control`, `X-Content-Type-Options`, `Server`, `Date` — match `/ready`'s header set or differ?); (g) edge-case empirical pins (HEAD request behavior, trailing-slash dispatch, non-GET method behavior); (3) `docs/envoy-go/phases/08.2-graceful-drain/README.md` — sibling SPEC stub citing parent SPEC + forward-looking 08.2 scope notes (BRAINSTORM §1 split-table + §3 stub is sufficient seed); (4) ROADMAP.md split: row `08 | admin-api-and-drain | 07 | planned` becomes parent `08 | admin-api-and-drain | 07 | in-progress | 08.1, 08.2 | ...` with rows `08.1 | admin-endpoints | 07 | planned | | ...` and `08.2 | graceful-drain | 08.1 | planned | | ...` (08.2's prereq is 08.1 because 08.2's `/ready` DRAINING-state extension and `/server_info` state-field transition both consume the admin-endpoint scaffold that 08.1 lands — see BRAINSTORM §1). After SPEC, advance STATE to lifecycle-state 2 with `next-skill: superpowers:writing-plans` (PLAN.md authoring) and `active-phase` remains `08.1-admin-endpoints`. Worktree for the next session: branch from this brainstorm-closure commit's master fast-forward HEAD per ADR-0003 + per-phase-worktree convention (`.worktrees/phase-08.1-admin-endpoints-spec`).
- **last-commit:** `TBD` — `phase 08: brainstorm — admin-api-and-drain split into 08.1 (admin-endpoints) + 08.2 (graceful-drain) per ADR-0045`, on branch `phase-08-admin-api-and-drain-brainstorm`. Lands `docs/envoy-go/phases/08-admin-api-and-drain/BRAINSTORM.md` capturing the full design for 08.1 plus a forward-looking 08.2 stub + this STATE.md rewrite advancing to phase 08.1 lifecycle-state 1 with `next-skill: superpowers:writing-plans`. Bundles BRAINSTORM + STATE in one commit per the phase-06 `d271fd7` precedent (cleaner than the phase-07 split into `da28039` + `9c3752b`). No code changes; pure scaffold + state transition. SHA filled in a follow-up commit per the phase-04..07.2 SHA-fill convention.
- **last-updated:** 2026-05-02

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
