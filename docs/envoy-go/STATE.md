# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `22-http-filter-lua` (parent row in-progress; sub-phases `22.1-http-filter-lua-vm-and-headers-bridge` + `22.2-http-filter-lua-full-bridge` + `22.3-http-filter-lua-multi-script-and-per-route` planned per BRAINSTORM Q11 long-prefix slugs + Q12 pre-create-all directories). Phase `22-http-filter-lua` **parent BRAINSTORM done at 2026-05-18** via a 12-question dialogue (Q1 MVP envelope — `D. Full upstream parity (SourceCodes map + 3-arm per-route oneof)` chosen from A/B/C/D; Q2 phase-split — `Pre-split now: 22.1 (envelope B) + 22.2 (envelope C delta) + 22.3 (multi-script + per-route)` chosen from 2-way/3-way/single-phase/other; Q3 Lua VM library — `github.com/yuin/gopher-lua (pure-Go Lua 5.1)` chosen from gopher-lua/aarzilli-golua/Shopify-go-lua; Q4 framework-primitive trigger — `NEW internal/lua/ framework primitive at first consumer (extract NOW)` chosen from in-package-with-trigger/extract-NOW/in-package-no-trigger; Q5 DataSource scope — `In-package; honor all 4 arms (Filename + InlineBytes + InlineString + EnvironmentVariable); PARSE-REJECT WatchedDirectory` chosen from in-package-all-arms/in-package-inline-only/NEW-internal-datasource-primitive/InlineString-only; Q6 phase 22.1 bridge-API scope — `Pragmatic-middle: minimal + :streamInfo() subset + request_handle:respond()` chosen from minimal/pragmatic-middle/full-streamInfo-and-connection; Q7 per-route shape — `NEW 9th canonical: 3-arm hybrid (disabled-bool + string-reference-delegation + DataSource-wholesale-override)` chosen from NEW-9th/COMPOUND-mapping/EXTENSION-of-5th; Q8 phase 22.1 stat surface — `Upstream + envoy-go-strict extensions: errors + executions + respond_calls (3 counters; 99 → 102)` chosen from upstream-exact/upstream-plus-extensions/defer-to-SPEC; Q9 differential-fixture strategy — `Full cross-side byte-exact for ALL 22.1 scenarios` chosen from partial-cross-side/full-cross-side/REFERENCE-LESS/defer-to-SPEC; Q10 ADR-0044 escape-valve hypothesis — `WEAK HOLD: 0-1 extra ADRs at 22.1 IMPL (one-slot escape-valve consumption)` chosen from STRONG-HOLD/WEAK-HOLD/BREAK/defer-to-SPEC; Q11 sub-phase slug convention — `22.1-http-filter-lua-vm-and-headers-bridge / 22.2-http-filter-lua-full-bridge / 22.3-http-filter-lua-multi-script-and-per-route (long-prefix style)` chosen from long-prefix/short-prefix/lifecycle-style; Q12 sub-phase directory pre-creation — `Pre-create all 4 directories (parent + 22.1 + 22.2 + 22.3) with placeholder READMEs at this BRAINSTORM` chosen from pre-create-all/pre-create-parent-only). **4 NEW ROADMAP rows registered** at this BRAINSTORM per ADR-0106: parent row `22 | http-filter-lua | 21 | in-progress | 22.1, 22.2, 22.3 | …` + sub-rows `22.1 | http-filter-lua-vm-and-headers-bridge | 21 | planned | …` + `22.2 | http-filter-lua-full-bridge | 22.1 | planned | …` + `22.3 | http-filter-lua-multi-script-and-per-route | 22.2 | planned | …`. **4 phase directories pre-created** at this BRAINSTORM per Q12: `docs/envoy-go/phases/22-http-filter-lua/` (parent — BRAINSTORM.md authored at this commit) + `22.1-http-filter-lua-vm-and-headers-bridge/README.md` + `22.2-http-filter-lua-full-bridge/README.md` + `22.3-http-filter-lua-multi-script-and-per-route/README.md` (each sub-phase README is descriptive of anticipated scope, not prescriptive of design). **FIRST §9 family-row to pre-split THREE-way at BRAINSTORM time** (prior splits at phases 05/06/07/08/18/19 all 2-way + all settled at SPEC time or PLAN time). **ENDS the phase-21 ZERO-NEW-framework-primitive streak** by introducing a NEW `internal/lua/` framework primitive at 22.1 first-consumer per Q4 EXTRACT-NOW decision — first §9 row since phase 17 (jwt_authn at `internal/jwks/` + `internal/jwt/`) to introduce a NEW framework primitive of substantial scope. **ENDS the FOUR-CONSECUTIVE ADR-0125-skip streak** (phases 18/19/20/21 all skipped) by amending ADR-0125's canonical-pattern roster from 8 → 9 entries at phase 22.3 IMPL per Q7 NEW 9th canonical (3-arm hybrid: disabled-bool + string-reference-delegation + DataSource-wholesale-override). **FIRST §9 row to introduce a third-party Lua VM dependency** in envoy-go: gopher-lua (pure-Go Lua 5.1; MIT-licensed; matches upstream Envoy's LuaJIT 5.1 dialect; NO CGO). The full envelope D delivers across 3 sub-phases: 22.1 = NEW `internal/lua/` + NEW `internal/filter/http/lua/` + DefaultSourceCode + 4-arm DataSource + pragmatic-middle bridge (headers + log + streamInfo subset + respond) + 3-counter stat surface (errors + executions + respond_calls; 99 → 102) + 28th fuzzer FuzzLuaConfigParse + differential fixture 0026 full cross-side byte-exact for ALL 7 scenarios + ADR-0188 (internal/lua/ primitive) + ADR-0189 (lua filter package shape); 22.2 = full bridge delta (body + bodyChunks + trailers + metadata + connection + httpCall + crypto + base64 + sha + fileBytes + timestamp + full streamInfo) + httpCall counters + fixture 0027 partial cross-side / REFERENCE-LESS fallback; 22.3 = SourceCodes + LuaPerRoute 3-arm oneof + NEW 9th canonical per-route shape ADR + ADR-0125 IN-PLACE AMENDMENT (roster 8 → 9) + per-route 3-tier dispatch + fixture 0028 cross-side byte-exact for deterministic scenarios. **D-hypothesis per Q10 WEAK HOLD at 22.1 IMPL**: 2 anticipated ADRs (ADR-0188 + ADR-0189) land cleanly; 0-1 escape-valve slot consumption from the phase-21 STRENGTHENED two-slot buffer (ZERO-slot buffer post-22.1-IMPL). Named risk surfaces (3): gopher-lua sandbox discipline + per-worker VM scoping + gopher-lua-vs-LuaJIT observable divergence at fixture-0026. **Phase 22 BRAINSTORM is READY FOR SQUASH-MERGE TO MASTER** per project memory `feedback_git_worktrees.md` + ADR-0005 §Decision 4. NO new code lands at BRAINSTORM (per ADR-0044 §Context-draft discipline — BRAINSTORM authors anticipate but do not consume ADR numbers); ADR roster unchanged at ADR-0187 tail.
- **phase-directory:** `docs/envoy-go/phases/22-http-filter-lua/` (parent — BRAINSTORM.md authored at this commit; parent SPEC.md to be authored at next session per phase-18 + phase-19 parent-row precedent). Sub-phase directories pre-created per Q12 with placeholder README.md each: `docs/envoy-go/phases/22.1-http-filter-lua-vm-and-headers-bridge/README.md` + `docs/envoy-go/phases/22.2-http-filter-lua-full-bridge/README.md` + `docs/envoy-go/phases/22.3-http-filter-lua-multi-script-and-per-route/README.md`. Each sub-phase READMEs points back to the parent BRAINSTORM + lists the anticipated artefacts (SPEC.md + PLAN.md + PROGRESS.md + REVIEW.md authored at each sub-phase's own dedicated session-set per phase-18.1 + 18.2 + 19.1 + 19.2 precedent).
- **lifecycle-state:** `phase 22.1 SPEC done; awaiting 22.1 PLAN`. ROADMAP row `22.1` is `in-progress` at this commit with the per-cell SPEC-done annotation (per ADR-0106 ROADMAP per-cell update at SPEC milestone); parent row `22` stays `in-progress`; sub-rows `22.2` + `22.3` stay `planned`. DECISIONS.md tail UNCHANGED at **ADR-0189** at this 22.1 SPEC commit (no NEW ADR §Context anchors consumed; ADR-0188 + ADR-0189 §Context blocks already anchored at parent SPEC commit `41ccee7` per parent SPEC §4.1 + §4.4 + ADR-0044 — §Decision+§Consequences body lands at 22.1 IMPL Lands-in-Task per ADR-0044). ADR-0125 §(xiv) AMENDMENT-anticipation paragraph UNCHANGED at this 22.1 SPEC commit (anchored at parent SPEC commit; AMENDMENT body lands at 22.3 IMPL final Task). Next-free **ADR-0190** UNCHANGED (carried forward as the 22.1 IMPL escape-valve slot per BRAINSTORM Q10 + parent SPEC §1.2 WEAK HOLD STANDS — anticipated 0-1 consumption from the `*LState`-pool benchmark surface per parent SPEC §13-R6 RATIFIED-PENDING). **Phase 22.1 SPEC authored at THIS commit** at `docs/envoy-go/phases/22.1-http-filter-lua-vm-and-headers-bridge/SPEC.md` — 15 sections + 2 appendices following the phase-18.1 + 19.1 sub-phase-SPEC precedent (Mission + Non-purposes + Framework primitive INCLUDING refined `internal/lua/` API signatures vs parent §4.1 sketch + compiledConfig + code shapes + Per-route discipline + 16-task IMPL breakdown across 5 tiers + PARSE-REJECT roster (cross-ref parent §6) + Stat surface (cross-ref parent §7) + Differential fixture-0026 (cross-ref parent §8 + verbatim 7 `.lua` source contents) + Behavior-contract delta + SPEC-time empirical pins (D5 + D7 resolved IN-SESSION) + SPEC-time D-questions (D1 + D3 carry-forward) + RATIFIED-PENDING items (§13-R3 REFINED + §13-R4 CLOSED) + BEHAVIOR_CONTRACT.md edit bundle + Test surface + 24-item acceptance checklist). **D5 CLOSED IN-SESSION**: 28th-fuzzer count CONFIRMED via project-wide grep — 27 unique fuzzers in main tree (`FuzzAccessLogFormat` + `FuzzAdaptiveConcurrencyConfigParse` + `FuzzBandwidthLimitConfigParse` + `FuzzBootstrapLoad` + `FuzzBufferConfigParse` + `FuzzCheckResponseMapping` + `FuzzCompressorConfigParse` + `FuzzConfigDumpFormat` + `FuzzCsrfPolicyConfigParse` + `FuzzDrainTransitions` + `FuzzExtAuthzConfigParse` + `FuzzExtProcConfigParse` + `FuzzFaultConfigParse` + `FuzzFilterChainMatch` + `FuzzFilterChainParse` + `FuzzFrameStream` + `FuzzHCMConfigParse` + `FuzzHeaderMutationConfigParse` + `FuzzHPACKDecode` + `FuzzJwtAuthnConfigParse` + `FuzzLocalRateLimitConfigParse` + `FuzzOAuth2ConfigParse` + `FuzzProcessingResponseMapping` + `FuzzPromTextFormat` + `FuzzRBACConfigParse` + `FuzzTcpProxyFilter` + `FuzzTLSContextParse` = 27); `FuzzLuaConfigParse` will be the 28th. **D7 CLOSED IN-SESSION (REFUTES BRAINSTORM hypothesis)**: envoy-go headers-map type EMPIRICALLY-DETERMINED as `net/http.Header` (Go `map[string][]string`; unordered per Go map semantics); the codebase explicitly acknowledges at `internal/filter/http/types.go:86-90` ("the unordered http.Header map cannot preserve the SPEC §11.2 verbatim 6-header order on the wire (Go map iteration is non-deterministic; net/http's Header.Write emits keys alphabetically sorted)"). Scenario (f) cross-side determinism STILL HOLDS via count-only assertion. NEW bridge `__pairs` metamethod discipline RATIFIED: snapshot to alphabetically-sorted slice + iterate by integer index (matches `net/http.Header.Write` precedent; closes per-run map-iteration non-determinism for script-author debugging). **D1 + D3 carry forward**: D1 (default_source_code absent vs no-op + script-missing-required-hooks disposition) closes at IMPL Task 2 first action via upstream Envoy v1.37.2 scrape against `config.cc::createFilterFactoryFromProtoTyped` + `lua_filter.cc::Filter` constructor; D3 (scenario (e) `:logInfo()` cross-side assertion shape) closes at 22.1 PLAN session per parent §12-D3 + §11.7.7 RECOMMENDED option (a) anticipated. **`internal/lua/` API refined vs parent §4.1 sketch**: function-option `VMOption` pattern (was sealed interface); `Run(chunk) + HasGlobalFunc(name) + CallGlobal(name, args...)` split (was `Run(chunk, hooks ...HookFn)` blob); `State() *lua.LState` escape-hatch exposed for userdata setup; `WithPanicHandler` (renamed from `WithPanicRecovery`) + `WithBasePrintSink` (renamed from `WithStderrSink`) naming. **`internal/lua/` file split RATIFIED**: 3 production files (vm.go + sandbox.go + compile.go) + 3 test files mirroring compact primitive-package precedent at `internal/jwks/` + `internal/httpclient/`. **`internal/filter/http/lua/` file split STANDS unchanged from parent §4.4**: 8 production + 5 test files. **16-task IMPL breakdown across 5 tiers**: Tier A scaffold (Tasks 1-5: package skeletons + compiled_config + datasource + compile cache + VM lifecycle/sandbox); Tier B bridge (Tasks 6-9: headers + log + streamInfo + respond+hooks); Tier C stats+tests (Tasks 10-12: stats+boot-registration + fuzzer + race tests); Tier D fixture (Tasks 13-15: BackendKind+BootRejectFixture infra + scripts/ subdirectory + wording-pinning); Tier E atomic landing (Task 16: BEHAVIOR_CONTRACT.md 7-edit bundle + ADR-0188+0189 §Decision+§Consequences body + STATE.md re-advance + ROADMAP row 22.1 IMPL-done annotation). **24-item acceptance checklist**: parent §16 18 items + 6 sub-phase-specific extensions (D5+D7 resolutions recorded; D1 closure at Task 2; D3 closure at PLAN session; per-task PROGRESS.md entry shape; REVIEW.md per `superpowers:requesting-code-review`). **§13-R3 RATIFIED-PENDING REFINED**: from "verify envoy-go's headers-map is insertion-ordered" (parent anticipation) to "bridge `__pairs` metamethod snapshots alphabetically + walks by integer index" (lands at IMPL Task 6 + verified at `bridge_test.go` cross-run-determinism test). **§13-R4 CLOSED at this SPEC commit** (28th-fuzzer count CONFIRMED). **§13-R6 STANDS**: `*LState`-pool benchmark gates 22.1 IMPL escape-valve fire at ADR-0190. Phase 22.1 SPEC is **READY FOR SQUASH-MERGE TO MASTER** per project memory `feedback_git_worktrees.md` + ADR-0005 §Decision 4. NO new code lands at SPEC (per ADR-0044 §Context-draft discipline + this 22.1 SPEC anchors NO NEW ADRs); ADR roster UNCHANGED at ADR-0189 tail.
- **next-skill:** `superpowers:writing-plans` (scoped to **22.1 PLAN authoring** per the phase-09..21 + phase-18.1 + phase-19.1 PLAN precedent — the 22.1 PLAN at `docs/envoy-go/phases/22.1-http-filter-lua-vm-and-headers-bridge/PLAN.md` consumes the 16-task IMPL breakdown from this SPEC §6 + concretizes per-task subagent dispatch shape per project memory `feedback_execution_style.md` + per-task PROGRESS.md entry shape + per-task acceptance criteria refinement + closes D3 per parent §12-D3 + §11.7.7 RECOMMENDED option (a)). Cold-start scope for the next session: read STATE.md (this file) + `docs/envoy-go/ROADMAP.md` (row 22.1 with SPEC-done per-cell annotation) + `docs/envoy-go/phases/22.1-http-filter-lua-vm-and-headers-bridge/SPEC.md` (load-bearing 22.1 SPEC; 15 sections + 2 appendices) + `docs/envoy-go/phases/22-http-filter-lua/SPEC.md` (parent SPEC; 16 sections inherited by the 22.1 SPEC) + `docs/envoy-go/phases/22-http-filter-lua/BRAINSTORM.md` (parent 12-Q dialogue) + DECISIONS.md (ADR-0188 + ADR-0189 §Context drafts + ADR-0125 §(xiv) AMENDMENT-anticipation paragraph) + phase-21 PLAN.md (most-recent PLAN precedent for sub-phase-less IMPL Task ordering shape).
- **last-commit:** `a7021aa`. Phase 22.1 SPEC squash-merge of branch `phase-22.1-http-filter-lua-vm-and-headers-bridge-spec` from worktree `.worktrees/phase-22.1-http-filter-lua-vm-and-headers-bridge-spec/` landed as `a7021aa` on master per the phase-09..21 squash-merge convention. Predecessor master tip was `49cc7cd` — `perf(cluster+router): add upstream HTTP/1.1 keep-alive conn pool` (out-of-doctrine perf commit per explicit /goal user override; orthogonal to phase-22.1 docs scope; cluster-pool re-baselining + deferred pool-config ADR remain TODO for a follow-up phase per the commit body). Pre-predecessor was `e180c39` — `phase 22 parent SPEC follow-up: STATE.md SHA-fill (TBD → 41ccee7 post-squash)`. This SHA-fill follow-up commit backfills the placeholder per the phase-09..21 convention: `phase 22.1 SPEC follow-up: STATE.md SHA-fill (TBD → a7021aa post-squash)`.
- **last-updated:** 2026-05-18
- **next-free ADR:** `ADR-0190` — UNCHANGED at this 22.1 SPEC commit (NO new §Context anchors consumed; ADR-0188 + ADR-0189 §Context anchors already landed at parent SPEC commit `41ccee7`). Anticipated 22.1 IMPL ADR landings per BRAINSTORM Q10 WEAK HOLD + parent SPEC §1.2 STANDS UNCHANGED: ADR-0188 + ADR-0189 §Decision+§Consequences body land cleanly at 22.1 IMPL Task 16 per ADR-0044 in-place edit discipline; 0-1 escape-valve slot consumption at ADR-0190 from the `*LState`-pool benchmark surface per parent SPEC §13-R6 (if 22.1 IMPL Task 12 benchmark surfaces per-stream `*LState` construction cost > 1ms unacceptable overhead). NO escape-valve from scenario (g) — the `BootRejectFixture` interface lives inside ADR-0189 §Decision body per parent SPEC §13-R1 + this 22.1 SPEC §13; does NOT consume ADR-0190. Anticipated 22.2 IMPL ADRs (settled at 22.2 BRAINSTORM): ~2-4 NEW ADRs (full-bridge-API shape + httpCall dispatcher + body-buffering interaction with ADR-0128 + dynamic-metadata-bridge deferral). Anticipated 22.3 IMPL ADRs (settled at 22.3 BRAINSTORM): ~1-2 NEW ADRs (NEW 9th canonical per-route shape ADR + per-route 3-tier dispatch) + 1 IN-PLACE AMENDMENT body landing at ADR-0125 §(xiv) (anticipation-paragraph anchored at parent SPEC commit per parent SPEC §4.5). Phase-21's STRENGTHENED two-slot buffer at ADR-0188 + ADR-0189 fully collapses at 22.1 IMPL phase-done (ZERO-slot buffer post-22.1-IMPL absorbs the 0-1 escape-valve consumption; the buffer-discipline re-evaluates at 22.2 BRAINSTORM).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
