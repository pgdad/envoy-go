# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

> **This file is a POINTER, not an archive** — `BOOTSTRAP_PROMPT.md` §4: *"STATE.md — pointer: active phase, last commit, next action."* It carries the live pointer, the live counts, and a SHORT recent lineage. **Full stage-by-stage history lives in `STATE_HISTORY.md`** (archived at ADR-0288; **nothing was deleted** — the pre-move file is preserved there verbatim, byte-for-byte).
>
> **⚠️ THE RULE ADR-0288 EXISTS TO ENFORCE: there is EXACTLY ONE live `next-skill:`, `lifecycle-state:`, `next-free ADR:`, and count block IN THIS FILE, and they live in §Current pointer directly below.** Before that move, ~330 lines of prepended backlog had buried TWO fossilised "current state" blocks whose `next-skill:` still read *"the 42.2 hedging BRAINSTORM"* and *"the 46.2 SPEC"* while the live phase was **66** — so `grep 'next-skill:' STATE.md` returned **two stale hits and ZERO live ones**, and `grep 'fuzzers:'` returned **48** and **42** when the true count was **55**. **A session may trust a grep of this file again. Keep it that way: when you advance the pointer, EDIT §Current pointer IN PLACE — never prepend a new block above it.**

---

## Current pointer

*(The ONLY live "what next" in this repo. Update IN PLACE at every stage close.)*

- **active-phase:** `phase 66 (xds-sds-combined-validation-context) SPEC done` (2026-07-16)
- **phase-directory:** `docs/envoy-go/phases/66-xds-sds-combined-validation-context/` (`BRAINSTORM.md` + `SPEC.md` present; `PLAN.md` next)
- **lifecycle-state:** `2` (SPEC done) → next `3` (PLAN done). Row 66 STAYS `in-progress`; it flips `done` only at its IMPL six-gate (ADR-0106).
- **next-skill:** **the phase-66 PLAN** — `superpowers:writing-plans`, per `next-prompt.txt`. Design A (pool substitution) is ADOPTED and its equivalence theorem proven (SPEC §3.2): the PLAN must NOT re-open the seam and must NOT write `proto.Merge`/`proto.Clone` (the BRAINSTORM's §6 still instructs them and they **do not compile** — SPEC §3.1). Honor `feedback_git_worktrees` · `feedback_subagents_no_push` · `feedback_push_to_origin`.
- **last-commit:** `a1fe295a` — the phase-66 SPEC (`git log --grep 'phase 66.*SPEC'`). *(ADR-0288's archival move lands directly on top.)*
- **last-updated:** 2026-07-16 (phase-66 SPEC done; STATE.md compacted per ADR-0288)
- **next-free ADR:** **`ADR-0289`** — DECISIONS.md tail is **ADR-0288** (this archival move). **ADR-0287** (phase-66 `combined_validation_context`) carries its **§Context ONLY**; its §Decision/§Consequences land IN PLACE at the phase-66 IMPL per ADR-0044.

---

## Project counts at this commit

*(Live. Re-verify at session start — `git fetch` first — and update here at every stage close. Canonical commands inline.)*

- **active differential fixtures:** **110** — `ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0108-xds-sds-validation-context`. Anticipated **111** at the phase-66 IMPL (`0109-xds-sds-combined-validation-context`).
- **fuzzers:** **55** — `grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`. Phase 66 adds SEEDS only (`FuzzTLSContextParse`) ⇒ **+0** anticipated.
- **stat surface:** **1201** — BEHAVIOR_CONTRACT doc count. **There is NO mechanical counting command.** Phase 66 anticipates **+0**.
- **BackendKind tail:** **38** (`H2GoawayResponder`).
- **DECISIONS.md tail:** **ADR-0288** (next-free **ADR-0289**) — `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1`.
- **go.mod modules:** the tracked lineage figure is **2** (the phase-61.2 count: `quic-go` direct + `qpack` indirect) — **NOT a repo total**; the single `go.mod` requires **67** modules. Phase 66 anticipates **+0**.
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` (see `ENVOY_TARGET.md`).
- **conformance:** h2spec 53/53; proxy-wasm all 10 families PASS.

---

## Recent lineage

*(The last FIVE stages only — enough for the router's "prior-phase lineage" read. Everything earlier is in `STATE_HISTORY.md`. These bullets are carried over VERBATIM; the newest is the phase-66 SPEC this pointer reflects.)*

- **prior active-phase:** `phase 66 (xds-sds-combined-validation-context) SPEC done` (2026-07-16) — lifecycle-state 1 → 2; docs-only (NO production `.go`; the §3.3 `proto.Merge` probe + the §3.7 `validate` probe were THROWAWAY dirs DELETED before commit), worktree `.worktrees/phase-66-spec`, branch `phase-66-spec`, off master `b9f5a1c8`, per `feedback_git_worktrees`. **ROW 66 STAYS `in-progress`** (the IMPL flips it `done` at its six-gate, ADR-0106). **ADR-0287 §Context DRAFTED** at this SPEC per ADR-0044 (§Decision/§Consequences at the IMPL); DECISIONS tail **ADR-0286 → ADR-0287**, next-free **ADR-0288**. **⚠️ THE SPEC OVERTURNED THE BRAINSTORM'S CENTRAL IMPLEMENTATION PLAN, BY EXECUTION.** The BRAINSTORM's planned `proto.Clone` + `proto.Merge` apply-point is **STRUCTURALLY IMPOSSIBLE**: `internal/xds`'s `SecretProvider.FetchInitialValidationContext` returns an **`*x509.CertPool`**, and `parseValidationSecret` consumes-and-discards the `CertificateValidationContext` message — **no message crosses the seam, so `proto.Merge` has no operand**. The BRAINSTORM ran `proto.Merge` exactly as it told itself to, and still planned unbuildable code, because it **CITED the seam's return type**. **Design ADOPTED: pool substitution** (the SDS pool wins; `default_validation_context.trusted_ca` is not read) — justified by an **EQUIVALENCE THEOREM** (SPEC §3.2) proven from executed/re-derived premises and **adversarially attacked without a counterexample**: envoy-go honors ONLY `trusted_ca`; `proto.Merge` recurses into it as a message while its `specifier` **oneof** REPLACES (OBSERVED); a successful fetch **guarantees** that specifier is set (`dataSourceBytes` errors otherwise); ⇒ merged pool ≡ SDS pool exactly, and the hybrid-`DataSource` artifact (`watched_directory`) is inert. **Premise P5 is a COINCIDENCE, not a guarantee** — `main.go` passes the same `filepath.Dir(*cfgPath)` to both `NewSDSProvider` and `boot.Construct`, and `dataSourceBytes`/`loadDataSource` are arm-for-arm identical; **if either diverges the theorem is SILENTLY falsified**. **Rejected: CA-pool union** (REFUTED by live probe — the reference accepts the SDS CA and rejects the default's with `UNKNOWN_CA`; a union accepts both) and **a message-returning seam** (exact fidelity, but +1–2 `internal/xds` symbols + relocating `parseValidationSecret`'s rejects — the lift path if the departure is later closed). **⇒ D-COMBVC-HYBRID-DS (the BRAINSTORM's "sharpest risk") and D-COMBVC-CLONE BOTH DISSOLVE** — no merge, no hybrid, no clone guard. **ONE NAMED DEPARTURE (PROBED):** a served VC with no usable `trusted_ca` ⇒ the reference ACKs, falls back to the **DEFAULT CA**, and **SERVES TRAFFIC**; envoy-go **boot-FAILs** (ADR-0280 family). Siblings `trusted_ca:{}` / `{filename:""}` are **PGV-NACKed** by the reference ⇒ traffic agrees, lifecycle diverges. **Never phrase it as "envoy-go rejects where the reference rejects" — false for the empty case.** **NEW OBLIGATIONS the BRAINSTORM never named:** **BOTH** CVC halves are **PGV-`required`** (D-COMBVC-PUREINLINE **REFUTED** — its "legal per the proto — verified" was false) while **envoy-go runs NO PGV repo-wide** ⇒ **TWO new explicit rejects (E1/E2)**; and the four inline sub-field rejects are **BYPASSED** under CVC (the oneof nils `GetValidationContext()`) ⇒ **re-point at `default_validation_context`, each shown RED first**. **D-COMBVC-REQUIRE-FALSE DISPOSED EXPLICITLY** (not by silence): the reference **HONORS** the anchor at `require==false` (verify-if-presented, PROBED); envoy-go silently ignores it on **all three** paths ⇒ **SCOPED OUT as a NAMED BOUNDARY**, carried to §13. **⚠️ THE SPEC'S OWN FIRST DRAFT CARRIED FOUR DEFECTS — ONE SEVERE — all found by an adversarial pass, all recorded in SPEC §1.2 rather than hidden, every one in a section where it was MOST confident it had already caught the error.** **SEVERE:** *"QUIC is the CVC guard's ONLY live consumer"* is **FALSE** — **`validate.Bootstrap` passes a nil provider too**, so `side=="downstream" && provider==nil` is reachable on an **ORDINARY TCP listener** (proven by EXECUTING `validate.BootstrapFile`). It was **inherited from a FALSE LANDED CODE COMMENT** in the very doc region the SPEC audited for stale comments — and a THIRD comment in the same file contradicts it. **It was bound verbatim for DECISIONS.md.** ⇒ the gate MUST keep the `provider != nil` conjunct (TWO consumers), and a new boundary is named: the reference's `--mode validate` **ACCEPTS** a valid CVC config while envoy-go's `validate` path **REJECTS** it. **Also:** "silently boots with no trust anchor" was a **false control-flow claim** (it boot-FAILs loudly); the `BoolValue` probe was **VACUOUS** (one non-discriminating input carrying a negative conclusion — `BoolValue` IS extensionally OR in all four combos); and the §5.3 roster claimed "re-enumerated in full" while carrying **8 of 15** fields (now enumerated **by reflection**). **Counts UNCHANGED (docs-only, re-verified this session, `git fetch` first):** fixtures **110** · fuzzers **55** · stat surface **1201** · BackendKind **38** · go.mod modules **2** (repo total **67**). **Anticipated at IMPL:** fixtures **110 → 111** (`0109-xds-sds-combined-validation-context`) · stat **1201 (+0)** · fuzz **55 (+0)** · ZERO new packages · **ZERO new symbols in `internal/xds`** (preserved by the theorem, NOT by the BRAINSTORM's reasoning). **The deferred sentence is UNCHANGED by this row at EVERY stage** (`combined_validation_context` was never in one — a §8-tier pickup; the phase-64 precedent). **Sentinel re-run MECHANICALLY — does NOT fire; `stop` NOT created.** **NEXT → the phase-66 PLAN** — which must NOT write `proto.Merge`/`proto.Clone` (the BRAINSTORM's §6 still instructs them and they do not compile), must show the four bypassed rejects RED first, and must **RE-DERIVE this SPEC rather than execute it** (`feedback_brief_citations_not_evidence` — and now: **a landed CODE COMMENT is not evidence either**).

- **prior active-phase:** `phase 66 (xds-sds-combined-validation-context) BRAINSTORM done` (2026-07-16) — the roller's AUTONOMOUS self-pick (no human pick per the 2026-07-12 standing directive) opens the FOURTH xDS-family row (lifecycle-state 0 → 1; docs-only, fresh worktree off master `d545810a`, branch `phase-66-xds-sds-combined-validation-context`, worktree `.worktrees/phase-66-brainstorm`, per `feedback_git_worktrees`). **Subject:** `combined_validation_context` — lift the `commonTLSContextToConfig` `combined_validation_context is not supported in phase 03` reject (DOWNSTREAM + live-provider + `require_client_certificate: true` ONLY; upstream + QUIC keep the BYTE-IDENTICAL substring, ADR-0080) and MERGE the SDS-delivered `CertificateValidationContext` phase 65 landed onto the inline `default_validation_context`, installed as the downstream mTLS `ClientCAs`. **Why this pick:** the SMALLEST candidate sized this session — ~4 edit sites across `internal/tls/config.go` + `internal/boot/boot.go`, and **ZERO new symbols in `internal/xds`** (CVC is a LISTENER-CONFIG shape, not a SERVED-SECRET shape, so the served `tls.v3.Secret` wire is byte-identical to `0108`'s and the whole xds chain + `parseValidationSecret` + `FetchInitialValidationContext` + `test/helpers/sdsserver` are UNTOUCHED; ~95% of `0108`'s driver is reusable) — AND the candidate phase 65 ITSELF named as *"a natural follow-on ONCE plain SDS-`validation_context` lands"*, a precondition row 65 now satisfies. **⚠️ The router's PROVISIONAL pick (`xds-sds-upstream-server-cert`) was OVERTURNED ON EVIDENCE** (BRAINSTORM §2.1.1 — the correct outcome per the router's own instruction, not a deviation): it is blocked by a **VALUE-level constructibility cycle** — `boot.NewSDSProvider` needs `grpcclient.New(cm)` needs the very `*cluster.Manager` that `buildCluster`/`NewUpstreamConfig` is constructing, so the provider CANNOT exist when the upstream TLS config is built — plus a listeners-only pre-scan and an eager `upstreamCfg`/`extractH2Mode` coupling. It needs a boot-model reshape (two-pass cluster build or lazy per-Dial TLS) and is NOT smallest. **The import graph is FINE** (`cluster` already reaches `xds` transitively), which is exactly why `reference_xds_config_seam_transitive_cycle_guard` — a TYPE-level guard — did not catch it; the mechanism is recorded in §2.1.1 so the next roller does not re-derive it. **Rejected (BRAINSTORM §2.1):** upstream SDS (above) / DataSource `environment_variable` (blocked on the D-ENV-HARNESS seam SPEC-63 already re-derived and DECLINED; the value-carrying PEM arm cannot use `0106`'s key-presence trick) / HTTP/3 `QuicProtocolOptions` (no reject to lift — the knobs are silently ignored; ~1 of 9 maps cleanly to quic-go; the `0104` observable does not move when they change; genuine proof needs a QUIC transport-param inspector no test has) / tracing `custom_tags` `metadata` (~19 call-site threading, ≥3 pre-chain, a signature change, a probable package extraction) / SDS rotation / the compose-two edge / the `ssl` stat family (framework surgery) / CDS/EDS/LDS/RDS/ADS/Delta/RTDS/`google_grpc` / opening gRPC-Runtime-WASM. **Central design decisions (SPEC to RUN, not cite):** established by EXECUTING `proto.Merge` — `trusted_ca` is a **message**, so Merge **recursively merges** it (NOT the proto's "singular override" bullet), and the default's non-oneof sub-fields (`watched_directory` — inert in envoy-go, LIVE in the reference) SURVIVE a dynamic override into a **hybrid `DataSource`** (D-COMBVC-HYBRID-DS, the sharpest risk); Merge **mutates dst**, which points into the parsed bootstrap ⇒ a `proto.Clone` guard is MANDATORY (D-COMBVC-CLONE); CVC + `require_client_certificate` false/absent is silently IGNORED, inherited from phase 65 (D-COMBVC-REQUIRE-FALSE); the four inline sub-field rejects are BYPASSED under the CVC oneof (D-COMBVC-REJECT-REPOINT); `NewSDSProvider`'s pre-scan does not match the CVC wrapper and returns a silent `(nil, nil)` (D-COMBVC-PRESCAN). **⚠️ This BRAINSTORM was ADVERSARIALLY VERIFIED before landing and ELEVEN defects were found IN ITS OWN FIRST DRAFT** — all corrected; the lesson is banked at §11: **quoting is not executing.** The severe defects all traced to ONE omission (it quoted `proto.Merge`'s docs instead of RUNNING it), plus a silently-TRUNCATED "verbatim" proto quote that had manufactured a false central risk, and a DROPPED phase-65 §8 carry-forward. **A BRAINSTORM is not evidence either.** Fixture: anticipated ONE new `0109-xds-sds-combined-validation-context`. **Envelope:** ZERO new packages / ZERO new modules / ZERO new symbols in `internal/xds` / +0 stats; anticipated a SINGLE FLAT ROW (~6–9 tasks, ADR-0045 escape-valve armable but no split anticipated). **Row 66 registered `in-progress`** at this BRAINSTORM commit per the ROADMAP §Schema invariant; it flips `done` at the phase-66 IMPL six-gate (the SOLE leg — NO parent rollup, ADR-0106). **This row does NOT narrow any deferred sentence** — `combined_validation_context` was never in one (a §8-tier pickup; the phase-64 precedent, whose BRAINSTORM `7d724423` left the Observability sentence byte-identical). **Counts UNCHANGED at this BRAINSTORM (docs-only; re-verified against master tip `d545810a`, `git fetch` first):** stat surface **1201** · fixtures **110** · fuzzers **55** · BackendKind **38** · DECISIONS tail **ADR-0286** (next-free **ADR-0287**) · go.mod modules **2**. **Sentinel re-checked MECHANICALLY (does NOT fire):** (1) now prints `NOT DONE: row 66` (row 66 registered `in-progress` — check (1) BLOCKS `stop` again, having been silent at the phase-65 IMPL); (2) prints THREE live "candidates:" sentences (HTTP/3 `:175`, xDS `:185`, Observability `:193` — UNCHANGED by this row); (3) prints `NEVER OPENED: gRPC`, `Runtime`, `WASM`. Any of (1)/(2)/(3) printing ⇒ the sentinel does NOT fire; no `stop` file created. **Next → the phase-66 SPEC** (which must RUN `proto.Merge`/`proto.Clone` against real messages and pin D-COMBVC-HYBRID-DS + D-COMBVC-CLONE from OBSERVED behavior, then live-probe the reference for the hybrid-`DataSource` question and dispose D-COMBVC-REQUIRE-FALSE explicitly rather than by silence; D-COMBVC-REFMERGE is a cheap confirmation, NOT a gate).

- **prior active-phase:** `phase 65 (xds-sds-validation-context) IMPL done` (2026-07-16) — lifecycle-state 3 → DONE; **ROW 65 FLIPS `in-progress` → `done`** at this IMPL six-gate (ADR-0106, the SOLE leg — `reference_roadmap_split_phase_row_done`). Worktree `.worktrees/phase-65-impl`, branch `phase-65-xds-sds-validation-context-impl`, off master `be419023`, per `feedback_git_worktrees`; subagent-driven T1→T11 (`feedback_execution_style`), each task committing LOCALLY only (`feedback_subagents_no_push`), squashed + pushed by the controller at stage-close. **ANCHORS ADR-0286** (§Decision/§Consequences landed in `DECISIONS.md`; §Context was drafted at the SPEC, SPEC §13, per ADR-0044) — DECISIONS tail **ADR-0285 → ADR-0286** (next-free **ADR-0287**). **The deliverable:** the THIRD xDS-family row + the SECOND SDS resource type — the downstream `validation_context_sds_secret_config` reject is LIFTED (`internal/tls/config.go:227-228`, downstream+provider ONLY; upstream + QUIC keep the BYTE-IDENTICAL substring, ADR-0080) and the knob HONORED: a PARALLEL `internal/xds` chain returning `*x509.CertPool` (`parseValidationSecret` + the CVC-reject roster / `applyValidationResponse` + `fetchValidationSecret`, SHARING `errValidation` + `secretTypeURL()` — D1: the `tls.v3.Secret` type URL is the SAME for both oneof arms, so NO new type URL and `sdsserver`'s generic derivation was untouched / `FetchInitialValidationContext` + the `SecretProvider` interface method + the `fakeProvider` impl in ONE commit) fetches the CA over the LANDED phase-60.2 SotW stream, and `NewDownstreamConfig`'s `require_client_certificate` block installs it as `cfg.ClientCAs` + `RequireAndVerifyClientCert` — gated `require==true`, so the deferred `require==false` case performs NO boot-time fetch (INERT, not merely ignored). `internal/boot`'s `NewSDSProvider` pre-scan detects the validation arm (`seen++` on BOTH arms ⇒ compose-two trips the LANDED `seen>1` deferral). The landed `*stdtls.Certificate` chain is **BYTE-UNTOUCHED** ⇒ `0103` structurally cannot regress; the **60.2 cycle guard HOLDS** (`go list -deps ./internal/xds`, no `...` → `internal/stats` + `internal/xds` ONLY; `internal/tls` ABSENT); the **ADR-0280 boot-FAIL DEPARTURE** extends unchanged to this resource type. **THREE PLAN-time corrections changed the shipped design — C3:** `ssl.fail_verify_error` DOES NOT EXIST in envoy-go (ZERO `ssl.*` stats repo-wide), so SPEC §8's negative-arm `StatsAsserter` was INFEASIBLE; REPLACED by a cross-side accept/reject CONTRAST (strictly STRONGER — it proves the anchor AND cross-side agreement), and the missing handshake-outcome family is a **NAMED COVERAGE BOUNDARY** now ADDED to the Observability deferred sentence. **C2:** the arm-5 flip would have been VACUOUS — **PROVEN at IMPL**: the pre-65 input dies at `ParseSDSConfig` with `tls: downstream: xds: sds: SdsSecretConfig sds_config is required` and never reaches the provider; the input was REBUILT with a full `sds_config`. **D2:** an IN-MEMORY 5-artifact PKI (neither clone-parent's model — `0103` commits PEMs, `0018` generates+gitignores); nothing touches disk. **THE RESIDUAL RISK RESOLVED:** `0108`'s `structuralCheck(side, out)` at the end of `driveSide` (a Drive error → the runner's `t.Fatalf("ref/subj drive: …")`, so a symmetric break fails loudly AND names the side) is what makes the fixture non-vacuous — **DEMONSTRATED, not assumed**: with the check DISABLED, break (1) (serve a DIFFERENT CA) ships **PASS** (both sides emit `good=REJECTED err=handshake-or-roundtrip-failed` + `bad=ACCEPTED` and compare EQUAL) — a pure-`CompareBytes` fixture would have shipped GREEN on a completely broken trust anchor. Honest consequence: the structural check strictly SUBSUMES `CompareBytes` here. **FIVE IMPL-time findings the PLAN did not anticipate** (`feedback_brief_citations_not_evidence` — **ELEVEN** PLAN defects total across T1-T10; the final two are a class the first tally missed: **break INSTRUCTIONS that do not compile** — T1's `GetTlsCertificate()` swap lacks the accessors, T2's "drop the `%w: `" leaves a `go vet`-rejected two-arg/one-verb `fmt.Errorf` — which show red that LOOKS like a fired assertion but proves nothing; banked as `reference_plan_break_instructions_dont_compile`): the `side != "downstream"` guard half is **DEAD** (`validation_context_type` is a oneof ⇒ `NewUpstreamConfig` refuses EARLIER with its own trusted_ca message; **QUIC is the guard's ONLY live consumer, via the `provider == nil` half**) — the PLAN's uncited prose asserted the OPPOSITE and that false claim reached a SHIPPED code comment, corrected in T5-fix `af55ac9e`; `NewDownstreamConfig`'s `provider == nil` guard is UNREACHABLE dead code (retained as documented defense-in-depth); the `errValidation`-before-`ctx.Err()` ordering is **UNTESTED** (T3 break 2 did NOT fire — no test creates a validation failure racing a live deadline; recorded HONESTLY, no coverage claimed); the `tls: downstream: %w` wrap is NOT fuzz-covered (the seed hits the `commonTLSContextToConfig` reject first; unit tests carry it). **A `-race` failure observed ONCE is PRE-EXISTING, NOT a phase-65 regression:** `TestProvider_FetchInitialCertificate_Timeout` (`init_fetch_timeout = 0, want 1`) — the test body is BYTE-IDENTICAL to master, master and branch each pass 3/3, 6 further saturated runs passed, could NOT be force-reproduced; mechanism: the 200ms budget covers BOTH the dial and the recv, so a missed budget returns an `open stream` error → `incUpdateFailure` → the counter stays 0 while every other assertion stays green. Phase 65's validation tests use a FAKE opener (no dial) and are immune. The master test was NOT "fixed" in this row. **Verify (controller-run on the frozen HEAD `af55ac9e`):** six-gate ALL GREEN (`gofmt` silent · `go vet` 0 · `go build` 0 · `go mod tidy -diff` EMPTY · `git diff master -- go.mod go.sum` EMPTY · `golangci-lint run ./...` 0); cycle guard HOLDS; the FULL **110-dir** differential `ok  375.884s` EXIT 0 (109 pre-existing byte-stable — phase 65 LIFTS a reject, it cannot change a passing fixture's bytes — plus the new `0108`). **Exit counts (re-derived on the landed tree):** stat surface **1201 (+0)** (BEHAVIOR_CONTRACT's authoritative figure UNCHANGED + ZERO production stat-registration call sites added — the only `RegisterSDSStats` additions in the diff are in `internal/xds/provider_test.go`) / fixtures **109 → 110** (`0108-xds-sds-validation-context`) / fuzzers **55 (+0, seeds)** / BackendKind tail **38 (+0)** (`test/differential/fixture/fixture.go` untouched — the `sdsserver` is DRIVER-owned) / +0 packages / go.mod modules **2 (+0)** / DECISIONS tail **ADR-0286**. **Sentinel re-run MECHANICALLY (does NOT fire; `stop` NOT created):** (1) now prints **NOTHING** (row 65 is `done`); (2) prints **THREE** live "candidates:" sentences (HTTP/3 `:175`, xDS `:185` **NARROWED** `SDS validation_context/upstream SDS` → `upstream SDS (server-cert + validation_context)`, Observability `:193` **EXTENDED** with the `ssl` family); (3) prints **THREE** never-opened families (gRPC/Runtime/WASM). Checks (2)+(3) print ⇒ abundant work remains. **Next → the phase-66 BRAINSTORM** (the roller SELF-PICKS the subject per the 2026-07-12 standing directive; the pick + rejected alternatives are recorded in the BRAINSTORM itself).

- **prior active-phase:** `phase 65 (xds-sds-validation-context) PLAN done` (2026-07-16) — lifecycle-state 2 → 3; docs-only (NO production `.go`), worktree `.worktrees/phase-65-plan`, branch `phase-65-xds-sds-validation-context-plan`, off master `be419023`, per `feedback_git_worktrees`. **The deliverables:** `docs/envoy-go/phases/65-xds-sds-validation-context/{PLAN,PROGRESS}.md` — an 11-task TDD spine decomposing SPEC §10 (T1 `parseValidationSecret` + the CVC-reject roster / T2 `fetchValidationSecret`+`applyValidationResponse` / T3 `FetchInitialValidationContext` + the `SecretProvider` method **+ the `fakeProvider` impl in the SAME commit** / T4 the reject-lift no-op / T5 the `ClientCAs` apply-point / T6 the boot pre-scan / T7 `sdsserver.WithValidationContext` / T8 fixture `0108` / T9 fuzz seeds / T10 BEHAVIOR_CONTRACT / T11 verify+ADR-0286+row-done+roll). **Every SPEC §12 `file:line` RE-DERIVED from source by three independent readers** (`feedback_brief_citations_not_evidence`) — mostly EXACT, but **FIVE substantive defects found and CORRECTED**: **C1** `ParseSDSConfig` has **14** reject arms, not the SPEC's "9" (`internal/xds/config.go:22-79`; reused verbatim, no build impact); **C2 🔴** the T5 arm-5 test-flip would be **VACUOUS** — `config_test.go:936-957` passes an `SdsSecretConfig` with **NO `sds_config`**, which (once routed through `ParseSDSConfig`) fires `sds_config is required` and NEVER reaches the provider ⇒ the input must be REBUILT with a full `sds_config` (the phase's highest-risk trap); **C3 🔴** **`ssl.fail_verify_error` DOES NOT EXIST in envoy-go** (grep-confirmed: ZERO `ssl.*` stats in `internal/`+`cmd/`; the only listener-scope counter is `downstream_cx_total`, `manager.go:353`) — the SPEC observed it on the REFERENCE (§11 arm 2) and assumed parity, so the §8 negative-arm `StatsAsserter` is **INFEASIBLE**; REPLACED by a driver-observed NORMALIZED accept/reject CONTRAST, which is CROSS-SIDE and therefore strictly STRONGER than a subject-only stat, and the missing `ssl.*` handshake-outcome family is recorded as a NAMED COVERAGE BOUNDARY (`reference_close_direction_framework_gap`) + a new Observability deferred candidate at T11 (+0 stats HELD); **C4** the two clone-parents use **INCOMPATIBLE PKI models** (`0103` COMMITS PEMs, `0018` GENERATES at `init()` + `.gitignore`s) and `0018`'s DTC (`envoy-go.yaml:164-174`) is `filename:`-based, NOT inline — resolved by **D2** (generate all 5 artifacts IN-MEMORY in `ensure()`, **no `pki/` dir**, server cert injected `inline_string` into both yamls ⇒ no `HostMount`/mount); **C5** `NewQUICDownstreamConfig` is `:90-113` (`:87-88` is prose) and its `:108` nil-provider call makes the `|| provider == nil` guard **CORRECTNESS, not defensiveness** (QUIC reaches the arm with `side=="downstream"`), the inline-CVC block closes at `:246`, `loadDataSource` is `datasource.go:20`, `stream_test.go`/`provider_test.go` span `:55-179`/`:59-160` (the SPEC's `:166`/`:137` are where the LAST test BEGINS), and "ALL test fakes" is exactly **ONE** (`fakeProvider`, `config_test.go:796-806`; two implementers repo-wide). **All five new identifiers RE-GREPPED collision-FREE**; the **60.2 cycle guard VERIFIED live** (`go list -deps ./internal/xds` → `internal/stats` + `internal/xds` only). **Residual risk carried into the IMPL:** T8's "serve a wrong CA" / "sign client_bad with the served CA" breaks are **SYMMETRIC** and will NOT fire `CompareBytes` (both sides change together) — `0108` needs an in-driver STRUCTURAL check or it proves only cross-side agreement, NOT that the served CA is the anchor. **Counts UNCHANGED at this PLAN (docs-only):** stat surface **1201** / fixtures **109** / fuzzers **55** / BackendKind **38** / DECISIONS tail **ADR-0285** (next-free **ADR-0286**) / modules **2**. Row 65 STAYS `in-progress` (flips `done` at the IMPL six-gate, ADR-0106). **Sentinel re-checked MECHANICALLY (does NOT fire; no `stop`):** (1) `NOT DONE: row 65`; (2) THREE live "candidates:" sentences (HTTP/3 `:175`, xDS `:185`, Observability `:193`); (3) THREE never-opened families (gRPC/Runtime/WASM). **Next → the phase-65 IMPL.**

- **prior active-phase:** `phase 65 (xds-sds-validation-context) SPEC done` (2026-07-15) — lifecycle-state 1 → 2; docs-only (NO production `.go`; the §11 live probe built a THROWAWAY dir DELETED before commit), worktree `.worktrees/phase-65-spec`, branch `phase-65-xds-sds-validation-context-spec`, off master `df43d940`, per `feedback_git_worktrees`. **The deliverable:** `docs/envoy-go/phases/65-xds-sds-validation-context/SPEC.md` (14 sections) DISPOSING every D-SDSVC-* question via LIVE probes against `envoyproxy/envoy:contrib-v1.37.2` (`reference_probe_fresh_container_per_arm`; the throwaway `probe65` container + certs + file-SDS config DELETED — docs-only, `git status` clean). **Live-probe findings (§11) — EVERY anticipation HELD (no ADR-0044 flip):** D-SDSVC-REFSERVE — the reference SERVES + APPLIES an SDS `validation_context` as the mTLS client-cert trust anchor (arm 1: a CA-signed client cert → **200 `OK-MTLS`** + `ssl.handshake:1`; arm 2: a wrong/un-served-CA cert → REJECTED `unknown ca` + `ssl.fail_verify_error:1`; arm 3: no cert → REJECTED `certificate required` + `ssl.fail_verify_no_cert:1` — three DISTINCT outcomes prove verification is LIVE, not vacuous), provability CONFIRMED; D-SDSVC-RESOURCE — the reference's OWN `config_dump` holds the secret under `dynamic_active_secrets` as `Secret{name, validation_context: CertificateValidationContext{trusted_ca: {inline_bytes}}}` (the `Secret_ValidationContext` oneof, proto field 4 — the DYNAMIC SDS machinery, NOT static); D-SDSVC-REQUIRE-SCOPE — `require_client_certificate: true` + an SDS `validation_context` = MANDATORY mTLS (arm 3), the phase-65 scope (mirrors phase 16); `false` (verify-if-present) DEFERS and is INERT (consistent with the inline `validation_context`-without-`require` precedent); D-SDSVC-FETCHTIMEOUT — DERIVED (the reference serve-anyway was probed at SPEC-60 arm B; the `validation_context` fetch reuses the IDENTICAL `FetchInitial*` timeout machinery + the boot-FAIL DEPARTURE, ADR-0280, unchanged); D-SDSVC-CVC-REJECT — RE-DERIVED the inline CVC reject roster (`config.go:234-245`) mirrored into the applier with `xds: sds:`-prefixed distinct substrings. **The probe used FILE-based SDS** (`path_config_source`) to ISOLATE the transport-independent secret-APPLICATION semantics (the gRPC SotW transport is the already-landed phase-60.2 substrate, probed at SPEC-60); the `dynamic_active_secrets` config_dump confirms the dynamic SDS code path ran. **Design decisions:** D-SDSVC-PROVIDER = **Option A (parallel chain, LEAN)** — `parseValidationSecret`/`applyValidationResponse`/`fetchValidationSecret`/`FetchInitialValidationContext(...) (*x509.CertPool, error)`, leaving the landed `*stdtls.Certificate` chain UNTOUCHED (zero regression to the passing `0103`); all four names GREP-collision-free in `internal/xds`+`internal/tls` (`reference_spec_drafted_identifier_collision_check`). D-SDSVC-APPLY-POINT = **fetch + install in `NewDownstreamConfig`'s `require_client_certificate` block** (gated on `require==true` — no wasteful boot fetch for the inert `false` case), `commonTLSContextToConfig` only no-ops the reject for the downstream+provider path (upstream/QUIC keep the byte-identical reject via the `side != "downstream" || provider == nil` guard) — a REFINEMENT over BRAINSTORM §2.6 (the FETCH moves INTO `NewDownstreamConfig` rather than being threaded OUT, because `require_client_certificate` lives on the `DownstreamTlsContext` visible only there). D-SDSVC-BOOT-SCAN (the load-bearing re-read finding) = **extend `boot.NewSDSProvider`'s pre-scan** (`boot.go:138`, which today sees ONLY `tls_certificate_sds_secret_configs`) to also detect a downstream `validation_context_sds_secret_config`, so a provider is built when a listener uses SDS for the validation_context; a context using SDS for BOTH secrets is the deferred compose-two edge (the single-secret provider model + the `seen>1` guard, `boot.go:147-148`, REJECTS it). D-SDSVC-STATS = **+0 counter TYPES** (reuse the 5-counter `SDSStats` scoped to the validation secret → a new DYNAMIC `sds.<validation-secret>.*` scope, the phase-60.2 convention); the verify outcome surfaces via the EXISTING `ssl.*` counters. D-SDSVC-FIXTURE = **ONE new `0108-xds-sds-validation-context`** (RE-DERIVED next-free; `0107` numeric tail), cloned from `0103` (driver-owned `sdsserver`, extended with a `WithValidationContext` Option) × `0018` (the client-cert mechanism — the driver builds its OWN stdlib `tls.Config{Certificates: LoadX509KeyPair(...)}` per `0018` scenario-6; NO runner API exists and `helpers.TLSServedLeaf` can't present a client cert); positive cross-side request-outcome equality + a wrong-CA negative sub-assertion (a subject-side `ssl.fail_verify_error` `StatsAsserter`); break-prove by serving a WRONG CA (`-count=1`, `-run 'TestDifferential/0108-…'`, confirm WHICH assertion fires). D-SDSVC-FUZZSEED = SEEDS to `FuzzDiscoveryResponseParse` (`xds/fuzz_test.go:71`) + `FuzzTLSContextParse` (`tls/fuzz_test.go:24`), count STAYS **55**. D-SDSVC-SPLIT = a **SINGLE FLAT ROW** (~11 tasks, §10; the ADR-0045 65.1/65.2 escape-valve documented ARMABLE but UNCONSUMED — the substrate is landed). The **60.2 cycle guard STANDS** (`internal/xds` must NOT import `internal/tls` — the CA-pool build is DUPLICATED in `internal/xds`, `go list -deps` verified at IMPL). **ADR-0286 §Context DRAFTED** (SPEC §13; §Decision/§Consequences at the IMPL per ADR-0044 — DECISIONS tail STAYS **ADR-0285** at this SPEC, ADR-0286 reserved-not-entered). **Counts UNCHANGED at this SPEC (docs-only; re-verified against master tip `df43d940`):** stat surface **1201** / fixtures **109** (numeric tail `0107`) / fuzzers **55** / BackendKind **38** / DECISIONS tail **ADR-0285** (next-free **ADR-0286**) / go.mod modules **2**. **Anticipated at the phase-65 IMPL:** the `internal/xds` applier/stream/provider arms + the `internal/tls` reject-lift + `ClientCAs` apply-point + the `internal/boot` pre-scan + the `sdsserver` `WithValidationContext` + the `0108` fixture + the fuzz seeds + BEHAVIOR_CONTRACT + ADR-0286 body; stat surface **1201 (+0)** / fixtures **109 → 110** (`0108`) / fuzzers **55 (+0, seeds)** / BackendKind **38 (+0)** / DECISIONS tail **ADR-0285 → ADR-0286** (next-free **ADR-0287**) / new packages **0** / new modules **0**; ROADMAP row 65 → `done` (the SOLE leg — ADR-0106). **Sentinel re-checked MECHANICALLY at this SPEC (does NOT fire):** (1) row 65 STAYS `in-progress` (flips `done` only at the IMPL six-gate) → check (1) prints `NOT DONE: row 65`; (2) THREE live "candidates:" sentences (HTTP/3, xDS, Observability) — the xDS sentence UNCHANGED at this SPEC (narrowed at the IMPL per the phase-63 convention); (3) THREE never-opened families (gRPC/Runtime/WASM); Operational-tooling also STAYS OPEN. Each independently blocks `stop`; no `stop` file created. **Next → the phase-65 PLAN** (decompose SPEC §10's ~11-task TDD spine; re-derive every §12 edit site against the master tip; ADR-0045 single-flat-row; PROGRESS scaffolded).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
