# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `06.2-access-log`
- **phase-directory:** `docs/envoy-go/phases/06.2-access-log/` — exists. SPEC.md committed at `4062c65` + `7bbf4a2` reviewer-fixes. THIS commit lands `PLAN.md` (16 numbered tasks distilling SPEC §4 / §5 / §6 / §7 / §11 / §15; lifecycle-state-2 → 3 deliverable per BOOTSTRAP §5 state 2 → 3). Phase 06.1 remains closed read-only history; the parent `docs/envoy-go/phases/06-observability-baseline/` retains its `BRAINSTORM.md` + parent `SPEC.md` as the master design document (still open until 06.2 phase-done flips parent row 06). ROADMAP row 06.2 stays `in-progress` (no row flip at PLAN commit per BOOTSTRAP §4.1 invariant 3); row 06 stays `in-progress`; row 06.1 stays `done`.
- **lifecycle-state:** `3` — SPEC.md AND PLAN.md committed; implementation has not started. PLAN.md (~2280 lines, 16 tasks + Refinement + Post-plan handoff + Plan review loop sections) was authored autonomously per ADR-0005's adapted plan-review discipline (the harness has no Task-subagent-dispatch tool available; plan-document-reviewer ran as an inline self-review against the reviewer-template's checklist — completeness / spec-alignment / task-decomposition / buildability — and approved at iteration 1; the constraint and substitution are recorded inline in the PLAN commit's body). The PLAN settles all 8 SPEC §12 deferred decisions plus 3 additional planner-internal items, anchors all 4 anticipated ADRs (ADR-0066..ADR-0069) to first-use commits per topical-vs-commit-order non-monotonic mapping (Tasks 2 / 5 / 7 / 15), reserves the empirical-format-pin scrape at Task 3 step 3 (filling the SPEC §11 `<<TBD: pinned at PLAN Task N — empirical scrape>>` placeholder against reference Envoy v1.37.2 — per Decision H + `## Settled SPEC §12 deferred decisions` #9), codifies the §6.2 split-gate decision as one-sub-phase-no-split (16 tasks under the 25-task primary signal; ~3000 LoC at the soft 1500 OR-leg with established phase-04 / 05.1 / 05.2 / 06.1 precedent), and adopts the 06.1 REVIEW M-8 polling-loop pattern prophylactically at Task 15 step 7 (does NOT close M-8 against fixture 0005 — that lands in a 06.1 review-followup batch). Per BOOTSTRAP §5 step 3, the next session is PLAN execution; lifecycle-state advances to 4 at Task 16's commit (the implementation session's last commit per the 05.1/05.2/06.1 convention).
- **next-skill:** `superpowers:subagent-driven-development` — phase 06.2 PLAN.md execution session (lifecycle-state 3 → 4). Per ADR-0005 §4 + the user's persistent preference for subagent-driven execution recorded in MEMORY.md, the executor uses `superpowers:subagent-driven-development` (one fresh subagent per PLAN task with two-stage review checkpoints) over the inline `superpowers:executing-plans` alternative; the PLAN's task structure (TDD red/green/commit per task with precondition / artifact / acceptance / verification command fields) is shaped for that execution mode. Inputs the executor should consider: (a) the PLAN's `## Execution preconditions` 12-item checklist (Task 1 step 1 verifies each; if any fails, stop and follow the precondition's "if fails" guidance); (b) the PLAN's `## ADRs introduced by this plan` map (ADR-0066 at Task 2; ADR-0069 at Task 5; ADR-0067 at Task 7; ADR-0068 at Task 15; non-monotonic commit-time ordering permitted per SPEC §8 + 05.2/06.1 precedents); (c) the PLAN's `## Settled SPEC §12 deferred decisions` (8 SPEC items + 3 planner-internal items); (d) the SHA-fill follow-up convention from the `## Refinement` section (one tiny commit per task with the just-landed SHA; phase-02..06.1 precedent); (e) the per-task acceptance checks as the green-build gate (D-3.6); (f) Task 16's six-gate sweep + BEHAVIOR_CONTRACT in-place edit + STATE → state-4 advancement as the implementation session's exit; (g) the post-plan-handoff note that the parent ROADMAP row 06 (observability-baseline) flips `in-progress → done` AT THE SAME phase-done commit as row 06.2 → done (mirroring the 05/05.1/05.2 closure pattern per parent SPEC §5).
- **next-skill-scope:** Phase 06.2 PLAN execution session (lifecycle-state 3 → 4). Per ADR-0005 §4 + BOOTSTRAP §5 state 3: the executor creates worktree `.worktrees/phase-06.2-access-log-impl` on branch `phase/06.2-access-log-impl` (branched from this commit's master fast-forward HEAD per ADR-0003 + per-phase-worktree convention), invokes `superpowers:subagent-driven-development`, executes Tasks 1–16 in order with TDD discipline (D-3.1: tests first per task; the PLAN's red/green/commit step shape is mandatory). Each task produces one logical commit; SHA-fill follow-up convention applies. The 15-operator default-format catalog is at SPEC §6; the empirical-format-pin scrape executes at Task 3 step 3 against `envoyproxy/envoy:v1.37.2` at the ENVOY_TARGET.md SHA (filling SPEC §11's TBD placeholder + mirrored at Task 16 into `BEHAVIOR_CONTRACT.md ## Access log field mapping`); the differential fixture 0006 lands at Task 15 (per-record three-tier equivalence per ADR-0068; 5-request workload per SPEC §7.2; polling-loop drain at 25ms/5s per Decision G; STATIC subject vs. STRICT_DNS reference per ADR-0010; reference invoked with `--concurrency 1` per ADR-0028); the closing acceptance checklist is SPEC §15 (16 bullets covering all four ADRs landed, no-third-party-access-log-library grep clean, four emit-hook sites grep-verifiable, BEHAVIOR_CONTRACT in-place edit landed, fuzz target FuzzAccessLogFormat at the 30s ADR-0018 budget, h2spec UNCHANGED at 53/53 PASS, no fixture 0000–0005 regression). On Task 16 commit, STATE.md advances to lifecycle-state 4; the verification session (lifecycle 4 → 5) runs gate (a)–(e) re-verification per BOOTSTRAP §7.5; the REVIEW session (state 5 → 6) writes REVIEW.md and commits the phase-done with subject `phase 06.2: phase-done — access-log lands; ROADMAP rows 06.2 + 06 → done [ADR-0066, ADR-0067, ADR-0068, ADR-0069]` (parent row 06 closes AT THE SAME COMMIT per parent SPEC §5).
- **last-commit:** `TBD` — `phase 06.2: PLAN drafted (autonomous plan-review per ADR-0005)`, on branch `phase/06.2-access-log-plan`. Writes `docs/envoy-go/phases/06.2-access-log/PLAN.md` (~2280 lines, 16 numbered tasks + Refinement + Post-plan handoff + Plan review loop sections; structurally mirrors the 06.1 PLAN.md and 05.2 PLAN.md precedents) and updates this STATE.md `lifecycle-state` `2 → 3` with `next-skill` set to `superpowers:subagent-driven-development` per ADR-0005 §4. Per the 06.1 anchored-ADR convention, ADR-0066..ADR-0069 land at PLAN-execution time (Tasks 2 / 5 / 7 / 15) — not at this commit. SHA-fill follow-up per phase-02..06.1 convention: a follow-up tiny commit `phase 06.2 follow-up: STATE.md SHA-fill for PLAN commit (TBD → <SHA>)` replaces this `TBD` with the actual SHA after the master fast-forward.
- **last-updated:** 2026-04-29

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
