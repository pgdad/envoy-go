# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `to-be-determined-at-next-session` (placeholder — the user picks the next-phase identity at the next session's cold start; per ROADMAP §9 HTTP-filters family closure trail there are 5 remaining §9 family-rows to land — `lua` / `wasm` / `adaptive_concurrency` / `admission_control` / `global rate limit` — plus other top-level rows; user direction settles which row lands next). Phase `20-http-filter-oauth2` **IMPL done at 2026-05-17**. **14 tasks landed at IMPL** (Tasks 1-14 per the 14-task PLAN at `docs/envoy-go/phases/20-http-filter-oauth2/PLAN.md`) **+ 1 follow-up commit** (Task 12 follow-up `6396eab` wiring `handleCallback` auth-code POST + `applyTokenEndpointResponse` full disposition matrix per §4.5 + §4.7 + AMEND-3 + 9 new tests — closed the wire-up gap that surfaced at Task 12). **9 NEW ADRs §Decision + §Consequences full bodies anchored at per-Task Lands-in-Tasks** per ADR-0044: ADR-0177 `internal/httpclient/` framework primitive at Task 2a `c0a058c` + ADR-0178 `internal/sdsfile/` framework primitive at Task 3 `cf36070` + ADR-0179 oauth2 HMAC composition (5-input newline-joined; dual-encoding read) at Task 4 `cbed2d1` + ADR-0180 oauth2 state-machine + deny-path 302+401 only no-500 + listener-scoped-only enforcement at Task 5 `43b1282` + ADR-0181 oauth2 cookie envelope + 6-counter stat surface (86 → 92 names) at Task 6 `c503cca` + ADR-0182 NEW filter-local AES-256-CBC token-encryption helper at Task 7 `d7e159b` + ADR-0183 refresh-token rotation timing + race-vs-rotation discipline at Task 8 `df33e4b` (with Task 8 follow-up `f9f007a` D15-literal revert of an Item-B call-site split) + ADR-0184 sign-out flow at Task 9 `21cda4c` + ADR-0185 token_endpoint POST body templates at Task 10 `ac47edc`. **2 IN-PLACE §Decision AMENDMENT bodies anchored** at Task 2 sub-commits: ADR-0150 jwks Fetcher refactor at Task 2b `e01573d` (Fetcher refactored to consume `*httpclient.Client`) + ADR-0159 extauthz httpAuthClient refactor at Task 2c `ea56c29` + ADR-0159 §Future Work CLOSURE-AT-PHASE-20 paragraph (**FIRST §9 family-row to CLOSE a prior-phase load-bearing forward-pointer** — the ADR-0159 §Future Work third-outbound-HTTP-consumer trigger closed at phase 20 per Q2 EXTRACT NOW + ADR-0177 introduction). **Task 11** `02590c7` full filter integration in `oauth2.go` + `compiled_config.go` + boot-registration at alphabetical line 135 between `localratelimit` + `rbac` per D19. **Task 12** `24e3df9` differential fixture `0024-http-oauth2` + NEW `test/helpers/oauthbackend/` test-helper (~643 LoC; `Scripted{Authz,Token}Responses` + `ValidCookieEnvelope` + `TamperedStateCookie` helpers; stdlib `net/http/httptest`-based) + 26th fuzzer `FuzzOAuth2ConfigParse` (~30 corpus seeds vs D7 ~60-seed target — fuzzer-corpus expansion is a documented future-work item). **Task 13** `2af6f12` BEHAVIOR_CONTRACT.md 10-edit bundle per SPEC §13 (atomic landing per ADR-0052; +265 / -2 LoC). **Task 14** (this commit `<TBD — SHA-fill follow-up after squash-merge>`) 6-gate phase-done verification + STATE.md re-advance + ROADMAP row 20 flip `in-progress → done` + REVIEW.md authored. **All 6 phase-done gates GREEN at Task 14:** Gate A `go build ./...` clean (exit 0); Gate B `go vet ./...` + `golangci-lint run` clean (both exit 0); Gate C `go test -race -count=1 ./...` clean repo-wide (re-run once for the pre-existing port-bind flake at fixture-0020 — `l_test_b:46566 bind: address already in use` per the Task 9 reviewer-recorded pre-existing flake class); Gate D `go test -count=1 ./test/differential/ -run 'TestDifferential'` 25/25 fixtures GREEN (0000-0024 incl. NEW 0024-http-oauth2); Gate E 26th fuzzer `FuzzOAuth2ConfigParse` clean at 30s (3,620,991 execs at 21k-167k/sec; 82 new interesting / 401 total) + 3 spot-checks clean (`FuzzExtProcConfigParse` + `FuzzBootstrapLoad` + `FuzzHCMConfigParse`); Gate F h2spec 53/53 PASS at ADR-0051 pin. **SPEC §15 18-item acceptance checklist: 17 GREEN + 1 GREEN-WITH-DOCUMENTED-EXCEPTIONS** (item 7 fixture-0024 9-scenario coverage GREEN at the 7-of-11 landed-subset boundary per PLAN Task 12 scope decision REFERENCE-LESS subject-only structural fixture; the 4 deferred scenarios (d refresh-token rotation; g token_endpoint 5xx → 302; h token_endpoint 4xx → 401; i `disable_token_encryption=true`) are documented future-work items in fixture-0024 README + REVIEW.md §6). **Cross-package regression matrix per SPEC §12 item C8 GREEN**: fixture-0019 (jwt_authn) + fixture-0020 (ext_authz HTTP-mode) GREEN post-refactor at Task 2b + 2c regression checks; fixture-0021 (ext_authz gRPC-mode) untouched. **THIRD CONSECUTIVE §9 row to skip ADR-0125 amendment** (REUSE-by-absence stronger form than 5th-canonical REUSE — proto absence of `OAuth2PerRoute` in v1.32.4/v1.37.x enforces listener-scoped-only via HCM-parse-time PARSE-REJECT per §5.2; `grep -cE '^\*\*\(xiv\)\*\*' docs/envoy-go/DECISIONS.md` returns 0). **Stat surface 86 → 92 names** (6-counter wire-exact: `oauth_unauthorized_rq` / `oauth_failure` / `oauth_passthrough` / `oauth_success` / `oauth_refreshtoken_success` / `oauth_refreshtoken_failure`; `signout_completed` + `cookie_decrypt_failure` ABSENT per S5 + AMEND-4 + §20.P11 RATIFIED-AS-ABSENT). **Recognized future-work items** (forward-pointers carried to future phases — NONE blocking phase-done; all in REVIEW.md §6): (i) fixture-0024 cross-side reference-Envoy promotion (currently REFERENCE-LESS subject-only structural per Task 12 scope decision — full cross-side differential deferred to a future fixture-extension task); (ii) 2-listener vs SPEC §7.2 3-listener topology — `l_test_b` `disable_token_encryption=true` scenario (i) blocked by go-control-plane v1.32.4 proto field absence (closes after the go-control-plane v1.37.x bump); (iii) fuzzer corpus seed count ~30 vs D7 target ~60 (additive corpus expansion deferred); (iv) HMAC `domain` empty-string subtlety discovered at Task 12 follow-up (host-only-default preserves invariant; operators with non-empty `cookie_domain` may see HMAC mismatch — documented in BEHAVIOR_CONTRACT §13.D Phase 20 forward-pointer notes); (v) v1.32.4 vs v1.37.x go-control-plane proto bump for 4 missing fields (`disable_token_encryption`, `Partitioned` cookie attribute, etc.); (vi) ~6 reviewer observations (sdsfile lifecycle comment; secret_file PARSE-REJECT swallowed; JSON fall-through; 700-LoC compiled_config split candidate; etc.). **D11 hypothesis HELD across all 14 tasks** — `ADR-0186` stays UNCONSUMED at phase-20 IMPL phase-done (`grep -cE '^## ADR-0186' docs/envoy-go/DECISIONS.md` returns 0). Phase 20 is **READY FOR SQUASH-MERGE TO MASTER** per project memory `feedback_git_worktrees.md` + ADR-0005 §Decision 4. Total fuzzer count is 26 (`grep -rE '^func Fuzz' --include='*.go' . | sort -u | wc -l` = 26).
- **phase-directory:** `docs/envoy-go/phases/20-http-filter-oauth2/` (BRAINSTORM.md 489 lines; SPEC.md 831 LoC; PLAN.md ~1332 LoC; PROGRESS.md ~2900 LoC after Task 14 append; REVIEW.md ~350 LoC authored at Task 14; ROADMAP row 20 single-row per ADR-0045 single-row decision settled at SPEC commit `4df55be`).
- **lifecycle-state:** `phase 20 IMPL done; awaiting next-phase identification`. ROADMAP row `20` is `done` at this commit (per-cell IMPL-done annotation appended documenting the 14-task IMPL landing + 1 follow-up commit + the 9 NEW ADRs + 2 IN-PLACE AMENDMENTs + the ADR-0159 forward-pointer CLOSURE-AT-PHASE-20 milestone + the 6-gate verbatim outputs + the SPEC §15 18-item acceptance + the recognized future-work items). DECISIONS.md tail at **ADR-0185** (9 NEW ADRs ADR-0177..ADR-0185 + 2 IN-PLACE AMENDMENTs to ADR-0150 + ADR-0159 landed at phase 20 IMPL); next-free `ADR-0186` (UNCHANGED — D11 hypothesis HELD; no additional ADR consumed at IMPL).
- **next-skill:** `superpowers:brainstorming` (the next-phase initial skill per SKILL_ROUTING.md state 0; the user identifies the next-phase target at cold start). Alternative if the user redirects to a non-§9 lane: any of the standard cold-start skills per `SKILL_ROUTING.md`. Cold-start scope for the next session: read STATE.md (this file) + `docs/envoy-go/ROADMAP.md` (5 remaining §9 family-rows: `lua` / `wasm` / `adaptive_concurrency` / `admission_control` / `global rate limit`; plus other top-level rows on the broader roadmap) + `docs/envoy-go/phases/20-http-filter-oauth2/REVIEW.md` (the phase-20 IMPL-done reviewer artefact — load-bearing for the squash-merge commit message body + for understanding the carried-forward future-work items inherited into the next phase) + `docs/envoy-go/phases/20-http-filter-oauth2/PROGRESS.md` (Task 14 entry — the 6-gate verbatim outputs + SPEC §15 18-item closure checklist + D11 final hypothesis status + known limitations).
- **last-commit:** `<TBD — SHA-fill follow-up after squash-merge>`. Phase 20 IMPL-done commit on worktree branch `phase-20-http-filter-oauth2-impl` at worktree `.worktrees/phase-20-http-filter-oauth2-impl/`. Per phase-09..19.2 convention, the SHA-fill follow-up commit lands AFTER the squash-merge to master with message shape `phase 20 IMPL follow-up: STATE.md SHA-fill (TBD → <squash-sha> post-squash)`. Predecessor master tip is `ad9780f` — `Squash merge phase-20-http-filter-oauth2-plan` (the phase-20 PLAN-done squash-merge from worktree `.worktrees/phase-20-http-filter-oauth2-plan/`).
- **last-updated:** 2026-05-17
- **next-free ADR:** `ADR-0186` — UNCHANGED from SPEC + PLAN. **D11 hypothesis HELD across all 14 IMPL tasks** — NO additional ADR consumed at phase-20 IMPL phase-done. Most-likely escape-valve surfaces hypothesized at PLAN time all closed via in-place §Decision body wording at existing ADRs (ADR-0182 PKCS#7 padding semantics; ADR-0178 fsnotify event-debounce edge-cases; ADR-0185 `urlEncode` charset edge-cases) — NONE fired as new ADR landings. The IMPL-time discoveries that surfaced at execution (auth-code POST wire-up gap at Task 11 closed by Task 12 follow-up; HMAC `domain` empty-string subtlety at Task 12 follow-up; fixture-0024 cross-side scope reduction at Task 12 to REFERENCE-LESS subject-only structural; 2-listener topology lock-in at Task 12 per go-control-plane v1.32.4 field absence; 30-seed fuzzer corpus at Task 12 vs ~60 target) were all resolved as scope-reductions + in-place §Consequences refresh + forward-pointer documentation in the BEHAVIOR_CONTRACT.md §13.D Phase 20 forward-pointer notes subsection, NOT new ADRs.

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
