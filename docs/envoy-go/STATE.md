# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `phase 29.3 PLAN done` (2026-06-06, this commit) — **phase 29.3 (`network-filter-mongo-fault-delay-and-access-log`) sub-phase PLAN COMPLETE at 2026-06-06** — the 29.3 PLAN (`docs/envoy-go/phases/29.3-network-filter-mongo-fault-delay-and-access-log/PLAN.md`, **12 bite-sized TDD tasks**, SEAM-FIRST) decomposes the SPEC §12 task spine into write-failing-test → run → implement → run → commit steps with complete Go code blocks, exact file paths, run commands + expected outputs, per-task `gofmt -l` + `golangci-lint`, and the `-count=1` deliberate-break protocol (the Task-4 seam `-race` break + the Task-11 `0052` R4 break). The task spine: T1 baselines/anchors gate (53/39/360/ADR-0226/BackendKind-30); T2 `chainRuntime` halt-state sync (the `MayHalt` construction-time `haltable` fast-path gate + halt `sync.Mutex`/`sync.Cond` + the `resumeReady` missed-wakeup guard) + a SYNTHETIC halting test filter + the never-halting byte-identity proof; T3 `ContinueReading` async-active third context (pre-handoff resume reaches `TerminalReady`); T4 `replayRead`+`readChainConn.Read` post-handoff withhold-until-resume (the pinned `replayHeld` park-state) + the `-race -count=N` R-HALT seam test + the deliberate-break; T5 the fault-delay roll+duration decision in the decoder (`maybeInjectDelay`/`takePendingDelay`; deterministic 100%; the `delayPending` re-entrancy guard); T6 the fault-delay timer in the filter (`MayHalt`; `time.AfterFunc`+`onDelayTimer`+`ContinueReading`; `delays_injected` at ARM; StopIteration-while-pending; `OnDestroy` cancel) + the timer↔destroy `-race`; T7 the `internal/accesslog` pluggable `Formatter func(any)` seam (default `Default` → HCM byte-identical) + the mongo JSON formatter + goldens; T8 the mongo access-log sink (gated on `cfg.accessLog`) + per-message emission both directions; T9 the `DrainState` accessor (`Draining()`/`CloseDirection()` on `ReadFilterCallbacks`, one folded ripple; the GUARDED `SetDrainState(rt.dm)` typed-nil fix) + `cx_drain_close` (reply-completion list-empty + FlushWrite); T10 the close-direction recording (`tcp_proxy` pump-EOF-first → `chainRuntime.closeDir`; the EXPLICIT `SetCloseDirection` forwarding methods on `prefixconn.go`+`writeconn.go` — the embedded `net.Conn` interface does NOT auto-promote) + the `OnDestroy` direction-keyed `cx_destroy_*` (D-P4 CLOSED); T11 `0052-mongo-fault-delay` cross-side (pre/post-handoff delay arms; the no-delay non-perturbation R1 arm; `cx_drain_close` driven-or-PRESENCE per D-S29.3-8; the `cx_destroy_*` VALUE-parity arms; the R4 break) + README; T12 the completion bundle (ADR-0226 §Decision/§Consequences body in place + the BEHAVIOR_CONTRACT 29.3 bundle incl. the framework async-halt/resume subsection + STATE/ROADMAP **with the parent-row-29 ROLLUP ATOMICALLY** + next-prompt + the six-gate). **The FINAL ADR-0045 gate-check PASSED: 12 tasks / ~420–760 production LoC → NO split** (both axes well under the gate; the pre-authorized split axis STANDS-UNCONSUMED). PLAN-time D-resolutions: **D-S29.3-1** halt primitive = halt mutex+cond behind the construction-time `haltable` gate (`MayHalt` opt-in; `resumeReady` closes the missed-wakeup race; exact primitive IMPL-refinable per `-race`); **D-S29.3-2** async-resume handoff = the dispatcher BLOCKS at the hold so resume re-enters via the SAME goroutine — `serveNetworkChain`/`handleTerminal` UNCHANGED; **D-S29.3-3** = `Draining()`+`CloseDirection()` on `ReadFilterCallbacks` (the drain decider a narrow structural `DrainState` interface, NOT a `*drain.Manager` leak); **D-S29.3-4** = a typed `Formatter func(any)` + a mongo record + a mongo formatter (HTTP `Default` UNCHANGED); **D-S29.3-5** = the parent six callbacks transcribed to the AS-BUILT five request decoders; **D-S29.3-6** = decoder decides+`takePendingDelay`, filter owns the timer+`cb`, `delayPending atomic.Bool` guard; **D-S29.3-7** = the deferred `Connection.Close` subsumes the zero-ms timer; **D-S29.3-8** = admin `/drain_listeners` with a PRESENCE+unit-value FALLBACK (the unit value proof is load-bearing). **plan-document-reviewer APPROVED (2 iterations; the first found 2 blocking issues — the typed-nil `*drain.Manager` panic + the `SetCloseDirection` non-promoting-through-`net.Conn`-interface-wraps silently-dead recording — both folded; the second APPROVED with 2 cosmetic advisories also folded).** ROADMAP UNCHANGED at this commit (sub-row 29.3 STAYS `in-progress` through PLAN+IMPL; parent row 29 STAYS `in-progress` — the ROLLUP is the 29.3 IMPL's). **Earlier (29.3 SPEC, preserved):** **phase 29.3 sub-phase SPEC COMPLETE at 2026-06-05** — the THIRD/FINAL phase-29 sub-phase SPEC (`docs/envoy-go/phases/29.3-network-filter-mongo-fault-delay-and-access-log/SPEC.md` + README; 14 §§ + 2 appendices, mirroring the 29.1/29.2 sub-phase SPECs) landed; **spec-document-reviewer APPROVED (first iteration; 5 advisories — items 1/2/5 folded [the §8.5 non-literal-quote fix; the parent §11.6 six-callback fault-eval pin inherited verbatim — decodeQuery/Insert/GetMore/KillCursors/Command/CommandReply NOT decodeReply; the pre-handoff close-direction no-differential-coverage completeness boundary]; items 3/4 forward-carry to PLAN/IMPL — the `0052` drain-trigger vehicle + the async-resume terminal-handoff mechanism).** The SPEC EXECUTES the parent §3.2/§4.1/§7/§8.4/§11.6/§11.7/§11.8/§11.10 + ADR-0226 §Context into the per-Task surface — FIVE deliverables: (1) the **async halt/resume seam** (the framework's FIFTH structural extension — ACTIVE async `ContinueReading` from a `time.AfterFunc` goroutine advancing past the halting filter + cross-goroutine safety via a per-`chainRuntime` halt mutex behind an atomic FAST-PATH [never-halting chains BYTE-IDENTICAL — R1] + post-handoff withhold-until-resume in `replayRead`/`readChainConn.Read` lifting the 28.1b §3.5 boundary for HALT purposes only; the post-handoff path is LOAD-BEARING — steady-state mongo decode runs post-handoff via `replayRead`); (2) **fault-delay injection** (the parsed `delay` consumed; per-decoded-request-message eval at the six-callback entry set + a re-entrancy guard; `rollPercent` deterministic at 100%; `time.AfterFunc` → `ContinueReading` on fire; **`delays_injected` at timer-ARM**; StopIteration-while-pending; timer cancel on destroy; the decoder decides/the filter owns the timer+`cb`); (3) the **mongo access log** (the parsed `access_log` consumed; one JSON line per decoded message both directions `{time,message,upstream_host}`; a pluggable formatter seam on `internal/accesslog` AsyncFileSink [default `Default` → HCM BYTE-IDENTICAL]; timing-bearing → unit goldens + a coverage boundary, NO fixture dir); (4) **`cx_drain_close`** (reply-completion drain close — list-empties + drain-active → increment + `Connection().Close(FlushWrite)`; a minimal drain-state accessor threads the phase-08.2 `*drain.Manager` [at the listener `rt.dm`, NOT yet reachable from a network filter] to the network filter callbacks); (5) the **close-direction seam D-P4 CLOSED** (which `tcp_proxy` pump EOF'd first → `chainRuntime.closeDirection` → a callbacks accessor → the `OnDestroy` direction-keyed `cx_destroy_local/remote_with_active_rq` VALUE-parity increment — the 29.2 presence-only boundary resolved). **29.3 is the framework-SURGERY sub-phase** (contrast 29.1/29.2 framework-ZERO-touch): the ONE consolidated ripple (ADR-0219) — the halt seam already opens `chain.go`/`readconn.go`/`tcp_proxy`-pump, so the close-direction + drain-state accessors ripple HERE. SPEC-time D-resolutions: **D-P12** (halt-state sync) RESOLVED at the SEMANTIC level (the mutex+atomic-fast-path+withhold-until-resume; the exact primitive [Mutex+Cond vs channel] is IMPL D-S29.3-1); **D-P7** (access-log formatter seam) RESOLVED (pluggable formatter on AsyncFileSink + a mongo carrier — D-S29.3-4); **D-P5** (delay-PGV fixture arms + header_delay) RESOLVED (reject arms unit-test-only; `header_delay` parse-accept-no-delay). Fixture **`0052-mongo-fault-delay`** (cross-side; deterministic 100% arms; the pre/post-handoff delay arms; the no-delay non-perturbation R1 arm; `cx_drain_close`; the `cx_destroy_*` VALUE-parity arms; timing NEVER compared); fuzzers STAY 39 (no new fuzzer — a seam `-race` test instead); stat surface STAYS 360 (+0 — `delays_injected`/`cx_drain_close`/`cx_destroy_*` go increment-active); BackendKind 30 reused; ADR-0045 split-gate ~420–760 production LoC / ~12–16 tasks → NO split (the seam lands seam-first GREEN before any mongo consumer wires in). 8 additive D-questions (D-S29.3-1..8) carried to PLAN/IMPL. ROADMAP sub-row 29.3 flipped `planned → in-progress` at this commit (parent row 29 STAYS `in-progress` — the ROLLUP is the 29.3 IMPL's) + the stale `delay_injected` singular corrected to `delays_injected` (AMEND-B3). The ADR-0226 §Context (the 29.3 charter) already exists; NO §Context AMEND needed at this SPEC (the 29.2 D-P4 re-scope already amended ADR-0225; the 29.3 charter stands as drafted); its §Decision/§Consequences body lands at the 29.3 IMPL (DECISIONS tail STAYS ADR-0226 — no new number). **Earlier (29.2 IMPL, preserved):** **phase 29.2 (`network-filter-mongo-responses-and-correlation`) sub-phase IMPL COMPLETE at 2026-06-04** — all 11 PLAN tasks (T1→T11) landed + reviewed + green. The `internal/filter/network/mongoproxy/` package gained the RESPONSE side + correlation + the gauge at ZERO framework touch + **+0 stat creation** (all 23 stats created eagerly at 29.1). LANDED: the response decoder in `OnWrite` (`decodeOnWrite`/`nextWriteMessage`/`decodeResponseMessage` feeding a private write-side `writeBuf` with NO high-water mark — the 28.2 fresh-per-Write Buffer asymmetry; OP_REPLY(1) + OP_COMMANDREPLY(2011) bodies decoded; any other opcode on the response stream → `decoding_error`); the 5 response counters increment-active (`op_reply` + `op_reply_cursor_not_found` [0x01] + `op_reply_query_failure` [0x02] + `op_reply_valid_cursor` [cursorID≠0] + `op_command_reply`; charge ONLY after a fully successful body decode); requestID↔responseTo correlation (`takeQuery` first-match-erase; only OP_QUERY created entries; uncorrelated replies charge fixed counters only; OP_COMMANDREPLY non-correlating); the `op_query_active` GAUGE — the project's FIRST differentially-mirrored gauge (Inc at OP_QUERY decode, Dec at correlated reply outside the lock, Add(-n) residual drain at `onDestroy`; nets to 0 per connection; proven cross-side at quiesced points answered→0, the unanswered→1-while-open unit-covered); the per-connection `sync.Mutex` (ADR-0223, narrowed to EXACTLY `dec.queries`; copy-out-under-lock; gauge/counter math outside; `sniffing` → `atomic.Bool` with a CAS-based at-most-once `decoding_error`, D-S29.2-4 — proven by a `-race -count=5` concurrent test + the R9 deliberate-break); `emit_dynamic_metadata` emission (D-S29.2-3 single-Set StructValue model — `Bucket.Set("envoy.filters.network.mongo_proxy", "operations", sv)`; per-pass overwrite-clear; ZERO `internal/dynamicmetadata/` change; differential-invisible → unit-test proof); the 39th fuzzer `FuzzMongoDecode` EXTENDED to feed both `decodeOnData` + `decodeOnWrite` (D-P6 — no 40th); fixture `0051-mongo-responses` (cross-side; the NEW `TCPMongoResponder` BackendKind **30**; 6 arms; the gauge quiesced-point arms answered→0; `cx_destroy_*` PRESENCE-ONLY; the R4 deliberate-break recorded — live-verified totals op_query=7/op_reply=5/op_command=1/op_command_reply=1/decoding_error=1/the 3 flag counters=1 each/op_query_active==0 at rest). **D-P4 COVERAGE BOUNDARY:** `cx_destroy_local/remote_with_active_rq` get NO increment at 29.2 (exist-at-zero, presence-only in `0051`); the close-direction accessor + value parity DEFER to 29.3 (the framework records close TYPE not DIRECTION — `reference_close_direction_framework_gap`). The dynamic-HISTOGRAM families stay DEFERRED (ADR-0060 — recorded here). The ADR-0225 §Decision/§Consequences body landed IN-PLACE (DECISIONS tail STAYS ADR-0226 — no new ADR number). **The six-gate is GREEN** (`go build`/`go vet`/`golangci-lint` clean; `-race -short` PASS; the 3 mongo differential fixtures `0049`/`0050`/`0051` byte-exact PASS cross-side; conformance asserted-unaffected — with the noted pre-existing HTTP-fixture startup flake reproduced on master). **Earlier (29.2 PLAN, preserved):** phase 29.2 sub-phase PLAN COMPLETE at 2026-06-04 — the 29.2 PLAN (`docs/envoy-go/phases/29.2-network-filter-mongo-responses-and-correlation/PLAN.md`, **11 bite-sized TDD tasks**) decomposes the SPEC §12 spine into write-failing-test → run → implement → run → commit steps with complete Go code blocks, exact file paths, run commands + expected outputs, per-task `gofmt -l` + `golangci-lint`, and the `-count=1` deliberate-break protocol. **plan-document-reviewer APPROVED (first iteration; 3 advisories — 2 folded [the cosmetic 0051-driver-LoC note + a post-error `dynMeta` re-Set advisory note in Task 8]; the 3rd, the `newTestDecoder` dead-line, is a 29.1 artifact the existing helper comment already covers — no-action).** Task spine: T1 baselines/anchors gate (52/39/360/ADR-0226/BackendKind-30-next-free); T2 `decoder` `writeBuf`+`mu`+`dynMeta` fields + `appendQuery` (gauge `Inc` under the lock); T3 `decodeOnWrite`/`nextWriteMessage`/`decodeResponseMessage` framing+dispatch + **`sniffing bool` → `atomic.Bool`** with a **CAS-based at-most-once `decoding_error`** (D-S29.2-4 RESOLVED at PLAN); T4 OP_REPLY/OP_COMMANDREPLY body decode + the 5 counters; T5 `takeQuery` first-match-erase + gauge `Dec` on a hit; T6 `OnDestroy` residual-drain teardown (snapshot-count-then-`Add`; D-S29.2-5 RESOLVED); T7 OnWrite glue + the `-race -count=5` concurrent test + the R9 mutex deliberate-break; T8 `emit_dynamic_metadata` single-Set StructValue Bucket emission (decoder accumulates / filter emits; D-S29.2-3 RESOLVED — zero `internal/dynamicmetadata/` change); T9 the 39th fuzzer EXTENDED to both directions (no 40th); T10 `TCPMongoResponder` BackendKind **30** + `acceptMongoResponder` runner arm; T11 `0051-mongo-responses` driver cross-side GREEN (gauge quiesced-point arms; `cx_destroy_*` presence-only; the R4 break) + the atomic completion bundle (ADR-0225 §Decision/§Consequences body + the BEHAVIOR_CONTRACT 29.2 bundle + STATE/ROADMAP + the six-gate). **The ADR-0045 FINAL gate-check PASSED: 11 tasks / ~330–540 production LoC → NO split** (both axes well under the gate; the 28.2 single-sub-phase precedent). **Earlier (29.2 SPEC, preserved):** phase 29.2 sub-phase SPEC COMPLETE at 2026-06-04 — the 29.2 SPEC (`docs/envoy-go/phases/29.2-network-filter-mongo-responses-and-correlation/SPEC.md`, 16 §§ + 2 appendices) + README landed; **spec-document-reviewer APPROVED (first iteration; 5 advisories — items 1/2/4 folded [the cx_destroy roster-table supersession note + the non-empty-list cx_destroy qualification + the sniffing-flag race-clean wording]; items 3/5 no-action).** The SPEC EXECUTES the parent §3.2/§7/§11.4/§11.5/§11.9/§11.10 + ADR-0225 §Context into the per-Task surface: the **OP_REPLY(1)/OP_COMMANDREPLY(2011) response decoder in `OnWrite`** (replaces the 29.1 no-op stub; a write-side private `writeBuf` with NO high-water mark — the fresh-per-Write Buffer asymmetry, the 28.2 pin) + the 5 response counters (`op_reply` + `op_reply_cursor_not_found` [0x01] + `op_reply_query_failure` [0x02] + `op_reply_valid_cursor` [cursorID≠0] + `op_command_reply`); **requestID↔responseTo correlation** consuming the 29.1 active-query list (first-match-erase; only OP_QUERY created entries; uncorrelated → fixed counters only) under the **ADR-0223 per-connection `sync.Mutex`** (narrowed to the ONE list; copy-out-under-lock, gauge/counters outside, pre-handoff locks too); the **`op_query_active` GAUGE** inc/dec (the project's FIRST differentially-mirrored gauge — inc at OP_QUERY decode, dec at correlated reply AND at connection-destroy teardown; atomic-outside-the-lock); **`emit_dynamic_metadata`** emission onto the ADR-0217 Bucket (the single-Set StructValue model; collection→ops; per-pass overwrite-clear; differential-invisible → unit-test proof); fixture **`0051-mongo-responses`** (cross-side; the NEW `TCPMongoResponder` BackendKind **30**; the gauge asserted at quiesced points — answered→0/unanswered→1; `cx_destroy_*` PRESENCE-ONLY) + the **39th fuzzer EXTENDED** to the response opcodes (D-P6 — direction-agnostic decoder → NO 40th). **D-P4 (close direction) RESOLVED at this SPEC: COVERAGE BOUNDARY (user-confirmed 2026-06-04).** The framework exposes no close DIRECTION to `OnDestroy` (the post-handoff close lives in `tcp_proxy`'s pump goroutines via EOF, unrecorded) → threading it requires a tcp_proxy + chain.go touch → kept OUT of 29.2 (framework-ZERO-touch, the 28.2 precedent); `cx_destroy_local/remote_with_active_rq` stay exist-at-zero/presence-only (the close-direction accessor + the direction-keyed increment + VALUE parity DEFER to 29.3, where ADR-0226's halt seam already opens the pump/terminal area — ADR-0219 one-ripple). Recorded as a **one-line in-place AMEND on ADR-0225's §Context** at this commit (no new ADR number; the ADR-0223-at-28.2-SPEC precedent — it SUPERSEDES the parent §7.2 "Incremented 29.2" + §3.1 "cx_destroy lands 29.2" roster cells for the two counters) + a BEHAVIOR_CONTRACT coverage boundary at the 29.2 IMPL. Also RESOLVED: D-P6 (extend the 39th), D-P9 (driver-waits-for-reply-round-trip gauge quiesced points), D-P10 (StatsAsserter gauge support works as-is — the `0049` `scrapeMongoStats`/`scrapeTypeLine` helpers), D-P11 (metadata unit-test-only); 29.2-additive D-S29.2-1..5 deferred to PLAN/IMPL. ADR-0045 split-gate: ~330–540 production LoC / ~11 tasks → NO split (the 28.2 single-sub-phase precedent). ROADMAP sub-row 29.2 flipped `planned → in-progress` at this commit + the stale BRAINSTORM-time row text corrected in place (the D-P4 resolution + the fuzzer-extend + fixture 53 + TCPMongoResponder 30). **Earlier (29.1 IMPL, preserved):** phase 29.1 (`network-filter-mongo-wire-and-requests`) sub-phase IMPL COMPLETE at 2026-06-04 — all 17 PLAN tasks landed + green; the six-gate is GREEN LIVE (`go build`/`go vet`/`golangci-lint` clean; `-race -short` PASS; the FULL **52-dir** differential suite byte-exact PASS incl. the 50-dir R1 back-compat / §4 zero-touch proof; h2spec 53/53; proxy-wasm 10/10). The `internal/filter/network/mongoproxy/` package request side LANDED: TypeURL via `proto.MessageName` (`envoy.extensions.filters.network.mongo_proxy.v3.MongoProxy`), 5-field config parse + FaultDelay PGV arms (oneof-required + `fixed_delay` gt-0s + denominator-defined) + the commands remembered-set + alias normalization, the in-house little-endian 14-type BSON parser (`bson.go`), the request-side wire decoder (5 opcodes body-decoded; Reply/CommandReply recognized-not-decoded until 29.2; OP_MSG/Update/Delete/Msg → `decoding_error`; sniffing-off-on-error for the connection lifetime AMEND-B6), the EAGER 23-stat roster (22 counters + the `op_query_active` gauge under `mongo.<stat_prefix>.`; D-P1 eager-at-config-parse; `delays_injected` PLURAL pinned by `TestStatRoster_MatchesUpstreamMacro`), the dynamic `cmd.*`/`collection.*`/callsite helpers, the per-connection active-query list (written 29.1, consumed 29.2), the 8th `builtins.RegisterBuiltins` built-in + the `mongo_proxy/v3` bootstrap blank-import, and the `mongo.` FOUR-RULE tag-extractor arm at `internal/stats/name.go` (AMEND-C1 multi-label hoist; labels SORTED LOCALLY → `prom.go` BYTE-UNTOUCHED, D-S29.1-5). **USER-APPROVED IMPL deviation (Task-8):** a `stats.IsValidName` guard around the 3 wire-derived dynamic-stat increments (skip un-nameable segments upstream would emit; fixed counters + active-query append always run) — fuzz-validated no-panic (`FuzzMongoDecode`, 60 s / 16 M execs, zero crashers) + recorded as a BEHAVIOR_CONTRACT coverage boundary at Task 17. Fixtures `0049-mongo-requests` (9 arms, cross-side label-aware StatsAsserter, the existing `TCPSink` backend BackendKind 28 per AMEND-C4; `cx_destroy_*` PRESENCE-ONLY per AMEND-C2; `$comment` callsite double-count AMEND-C3; R4 deliberate-break with `-count=1`) + `0050-mongo-boot-reject` (the `stat_prefix`-required PARSE-REJECT) GREEN cross-side; the 39th fuzzer `FuzzMongoDecode`. The Task-17 completion bundle (this commit): BEHAVIOR_CONTRACT `### envoy.filters.network.mongo_proxy` subsection + the 337→360 extension paragraph; ADR-0224 §Decision/§Consequences body in-place (DECISIONS tail STAYS ADR-0226 — no new ADR number); STATE/ROADMAP advance; next-prompt.txt rewritten for the 29.2-SPEC cold-start. **Earlier (29.1 PLAN, preserved):** the 29.1 PLAN (`docs/envoy-go/phases/29.1-network-filter-mongo-wire-and-requests/PLAN.md`, 3033 lines, 17 bite-sized TDD tasks) decomposes the SPEC §10 task spine into write-failing-test → run → implement → run → commit steps with complete Go code blocks, exact file paths, run commands + expected outputs, and per-task `gofmt -l` + `golangci-lint`. **The FINAL ADR-0045 gate-check PASSED: 17 tasks / ~1070–1470 production LoC → NO split** (both axes under the gate; the pre-authorized 29.1a/29.1b axis STANDS-UNCONSUMED). PLAN-time dispositions: stats.go REORDERED before codec.go (green-compiling `config → stats → decoder → glue` dependency order, the 28.1 precedent); the active-query list lives ON THE DECODER (not the filter — keeps the codec unit-testable; the zookeeper `requestsByXid`-on-decoder precedent; resolves the §3.7 representation question); D-S29.1-2 (BSON ordered-element `bsonDoc` + `asInt64`), D-S29.1-4 (`chainConsumed` high-water adapted verbatim), D-S29.1-5 (the mongo name.go arm SORTS its own labels locally → `prom.go` UNTOUCHED — verified `writeMetricLine` emits slice-order with no sort + SN4 prepends, so a global sort would break existing goldens), D-S29.1-6 (decoding_error single-increment in the decoder) all resolved at PLAN. plan-document-reviewer APPROVED (first iteration; 3 advisories, 2 folded: Task-7 vacuous-totalAppended test fixed + Task-4 `parseBSON` trailing-bytes check tightened; the 3rd — Task-15 cumulative-accounting placeholders — is correctly IMPL-owned). **Earlier (29.1 SPEC, preserved):** the 29.1 SPEC (`docs/envoy-go/phases/29.1-network-filter-mongo-wire-and-requests/SPEC.md`, 16 §§ + 2 appendices, mirroring the 28.1 first-sub-phase precedent) INHERITS the parent SPEC §5/§6/§7/§8/§11/§13 pins and refines them into the per-Task surface: the `internal/filter/network/mongoproxy/` file split (doc/mongoproxy/config/bson/codec/stats/filter.go), the 5-field config parse + FaultDelay PGV arms + the commands set with alias normalization, the 14-type BSON parser, the request-side codec (5 opcodes body-decoded; Reply/CommandReply recognized-not-decoded until 29.2; sniffing-off-on-error), the EAGER 23-stat roster (**D-P1 RESOLVED: eager-at-config-parse**; boot-window departure recorded), the dynamic cmd/collection/callsite name helpers, the active-query list (written 29.1, consumed 29.2), the 8th built-in + bootstrap blank-import, fixtures `0049-mongo-requests` (9 arms, label-aware StatsAsserter, the existing `TCPSink` backend) + `0050-mongo-boot-reject`, the 39th fuzzer `FuzzMongoDecode`, the ~17-task spine + ADR-0045 gate re-check (~1070–1470 production LoC; fits; pre-authorized 29.1a/29.1b split axis if the PLAN trips). **The D-P2 dynamic-stat live re-probe was EXECUTED at this session** (the parent §11.1 caveat CLOSED): a working docker-bridge-network probe (the prior failure was a Docker-Desktop `--network host` artifact) + the upstream `well_known_names.cc` source pin produced **4 new 29.1 AMENDs (C1..C4)**: **AMEND-C1** — the dynamic-stat Prometheus form is FULLY label-hoisted (`envoy_mongo_cmd_total{envoy_mongo_cmd="…"}`, `envoy_mongo_collection_query_*{envoy_mongo_collection="…"}`, callsite carries 3 labels; values NEVER appear in metric names) → the `name.go` arm is a FOUR-RULE tag extractor (**D-P2 + D-P3 RESOLVED**; the parent's "dynamic segments stay in the name" anticipation REFUTED); **AMEND-C2** — `cx_destroy_*_with_active_rq` increments on the REFERENCE during 29.1 fixtures (unanswered queries at connection close) → presence-only assertions at 29.1, value parity at 29.2; **AMEND-C3** — callsite queries DOUBLE-COUNT into both the collection and callsite families; **AMEND-C4** — the `0049` backend is the EXISTING `TCPSink` (BackendKind 28; silent — the 28.1 lesson re-applied); NO new BackendKind at 29.1 (the mongo responder, anticipated value 30, is 29.2's). **D-P8 RESOLVED: yes** — `0049` arm 2 exercises `commands: ["isMaster"]` + the `unknown_command` fallback on a dedicated listener. The D-P5 unit-arm half (the 3 FaultDelay PGV reject arms as code + unit tests) is scheduled at 29.1; the fixture-arm half stays for the 29.3 SPEC. spec-document-reviewer APPROVED (first iteration; 2 advisory notes folded as no-action). ROADMAP sub-row 29.1 flipped `planned → in-progress` at this commit + the 29.1 row's stale `.mongo.` inline-prefix text corrected in place to the ADR-0218 tag-extractor shape (the parent-§16-scheduled AMEND-B2 edit). **Earlier (phase 29 parent SPEC, preserved):** the parent SPEC (squash `ab376d8`) resolved all twelve D29-1..D29-12 pins against Envoy v1.37.2 with 13 AMENDs (B1..B13) — the 7-opcode LEGACY-ONLY envelope (OP_MSG → `decoding_error`), the `mongo.<stat_prefix>.` scope, the 22+1 roster (`delays_injected` plural), per-connection creation upstream, builtin-remembering `commands`, the FaultDelay PGV arms, the timing-bearing access log (no fixture dir), the AMEND-B13 seam reframing — and formalized the 3-way split + the ADR-0224/0225/0226 §Context drafts. **Phase 28 (zookeeper_proxy, ADR-0221/0222/0223) COMPLETE and CANONICAL.**
- **phase-directory:** `docs/envoy-go/phases/29.3-network-filter-mongo-fault-delay-and-access-log/` (SPEC.md + README.md + **PLAN.md** at this commit; PROGRESS.md lands at the IMPL). The 29.2 directory (`29.2-network-filter-mongo-responses-and-correlation/` — SPEC.md + PLAN.md + README.md + PROGRESS.md) + the 29.1 directory (`29.1-network-filter-mongo-wire-and-requests/` — SPEC.md + PLAN.md + PROGRESS.md) + the parent directory (`29-network-filter-mongo-proxy/` — BRAINSTORM.md + the parent SPEC) stay the durable phase-29 record.
- **lifecycle-state:** `phase 29.3 PLAN done (SKILL_ROUTING → 29.3 IMPL)` — the 29.3 PLAN (this commit) is landed + reviewed (plan-document-reviewer APPROVED) + green-baseline. The next lifecycle transition (SKILL_ROUTING: SPEC.md + PLAN.md exist for 29.3 + no PROGRESS.md → EXECUTE the 29.3 PLAN) opens the **29.3 IMPL**. ROADMAP: parent row 29 STAYS `in-progress` (the ROLLUP is the 29.3 IMPL's — lands at IMPL Task 12); sub-row 29.3 STAYS `in-progress` (through PLAN+IMPL); sub-rows 29.2/29.1 `done`.
- **next-skill:** the **29.3 IMPL execution path** (`superpowers:subagent-driven-development` per `feedback_execution_style` — SPEC.md + PLAN.md exist, no PROGRESS.md → execute the PLAN task-by-task; a fresh subagent per task + two-stage review). Execute the 12 bite-sized TDD tasks (`docs/envoy-go/phases/29.3-network-filter-mongo-fault-delay-and-access-log/PLAN.md`) SEAM-FIRST (T2–T4 land the async halt/resume seam GREEN with its `-race` test BEFORE T5+ wires the mongo consumer in). Subagents commit LOCAL-ONLY (`feedback_subagents_no_push`); the controller squash-merges + pushes at stage-close (`feedback_push_to_origin`); work in a git worktree (`feedback_git_worktrees`); each task runs `gofmt -l` + `golangci-lint` on touched packages (`feedback_pertask_gofmt_lint`); the Task-4 seam `-race` break + the Task-11 `0052` R4 break run with `-count=1` (`reference_differential_break_protocol_count1`). IMPL Task 12 lands the ADR-0226 §Decision/§Consequences body IN-PLACE + the BEHAVIOR_CONTRACT 29.3 bundle + **the parent-row-29 ROLLUP** (parent + sub-row 29.3 → `done` ATOMICALLY) + the six-gate (full 54-dir suite + h2spec 53/53 + proxy-wasm 10/10). The PLAN's risk register (read first): the seam is genuine concurrency (TDD with the synthetic `haltingFilter`); the `SetCloseDirection` non-promoting trap (the forwarding methods + `TestCloseDirectionThroughWrapStack` are MANDATORY); the typed-nil `*drain.Manager` guard; `cx_drain_close` cross-side is best-effort (the unit value proof is load-bearing). Counts at 29.3 PLAN done (unchanged): fixtures **53** (tail `0051-mongo-responses`; → 54 at IMPL with `0052`), fuzzers **39** (no new fuzzer at 29.3 — a seam `-race` test), stat surface **360** (+0 at 29.3), BackendKind tail **30** (`TCPMongoResponder` reused); DECISIONS.md tail **ADR-0226** (next-free **ADR-0227**).
- **last-commit:** `e8805e4` — the phase-29.3 PLAN squash (`phase 29.3 PLAN: async halt/resume seam + fault-delay + access log + cx_drain_close + close-direction (12 bite-sized TDD tasks; plan-document-reviewer APPROVED)`; squash-merged from `phase-29.3-network-filter-mongo-fault-delay-and-access-log-plan`; pushed to origin/master). [Controller fills the squash SHA post-merge.] Substantive predecessor on master: the 29.3-SPEC squash `0415153`; before that the 29.2-IMPL squash `620a3d0`; the 29.2-PLAN squash `0783776`; the 29.2-SPEC squash `13907f1`; the 29.1-IMPL squash `5ef5215`; the 29.1-PLAN squash `89c194a`; the 29.1-SPEC squash `7574219`.
- **last-updated:** 2026-06-06
- **next-free ADR:** `ADR-0227` (DECISIONS.md tail stays **ADR-0226**; NO ADR number consumed at the 29.3 PLAN — the ADR-0226 §Context draft already exists; its §Decision/§Consequences body lands IN-PLACE at the 29.3 IMPL per ADR-0044; next-free after phase-29 phase-done stays ≈ **ADR-0227**). ADR-0209 escape-valve reserve carried from the §9 family STANDS-UNCONSUMED.

---

## Project counts at this commit

- **active differential fixtures:** **53** (tail `0051-mongo-responses`; +`0051-mongo-responses` at 29.2 IMPL). Canonical recipe `ls -d test/fixtures/[0-9]* | wc -l` = 53. Anticipated at phase-29 phase-done: **54** (`0052` at 29.3; NO access-log dir per AMEND-B10).
- **fuzzers:** **39** (canonical recipe `grep -rh "^func Fuzz" $(find ./internal -name fuzz_test.go) | wc -l`; `FuzzMongoDecode` EXTENDED to both directions at 29.2 IMPL — D-P6, NO 40th). Anticipated at phase-29 phase-done: **39**.
- **stat surface:** **360** (BEHAVIOR_CONTRACT doc count; +23 mongo fixed roster — 22 counters + the `op_query_active` gauge — created eagerly per 29.1 SPEC §7.2 / D-P1). The 29.2 response increments + the gauge inc/dec/drain are +0 creation (increment-wiring only); the 29.3 fault/drain increments add +0 creation.
- **BackendKind tail:** **30** (`TCPMongoResponder`; +1 at 29.2 IMPL — `test/differential/fixture/fixture.go`).
- **DECISIONS.md tail:** **ADR-0226** (next-free **ADR-0227**) — unchanged at this commit (the ADR-0225 §Decision/§Consequences body landed in-place; no new number).
- **conformance:** h2spec 53/53; proxy-wasm all 10 families PASS (as of the 29.2 six-gate, 2026-06-04).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
