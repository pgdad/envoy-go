# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `to-be-determined-at-next-session` (placeholder — the user picks the next-phase identity at the next session's cold start; per ROADMAP §9 HTTP-filters family closure trail there are 4 remaining §9 family-rows to land — `lua` / `wasm` / `admission_control` / `global rate limit` — plus other top-level rows; user direction settles which row lands next). Phase `21-http-filter-adaptive-concurrency` **IMPL done at 2026-05-18**. **14 tasks landed at IMPL** (Tasks 1-14 per the 14-task PLAN at `docs/envoy-go/phases/21-http-filter-adaptive-concurrency/PLAN.md` — commits Task 1 `9eb493b` + SHA-fill `e3a8e13`; Task 2 `5a0a720` + SHA-fill `6e243b2`; Task 3 `84e317f` + SHA-fill `7aef3e5`; Task 4 `4a8f9c4` + SHA-fill `6ab6009`; Task 5 `d1dd3d7` + SHA-fill `2b6e3bc`; Task 6 `07d18c5` + SHA-fill `4a7e902`; Task 7 `498f566` + SHA-fill `3f942d8`; Task 8 `a7f2ffd` + SHA-fill `d9c399b`; Task 9 `5304aec` + SHA-fill `093b13c`; Task 10 `9d80820` + SHA-fill `7987bee`; Task 11 `ef6ac63` + SHA-fill `4dd0c4b`; Task 12 `02b96a2` + SHA-fill `9f29fc9`; Task 13 `75a64a5` + SHA-fill `fe3afc0`; Task 14 this commit). **2 NEW ADRs §Decision + §Consequences full bodies anchored at per-Task Lands-in-Tasks** per ADR-0044: ADR-0186 Gradient-1 controller state machine + in-package Clock seam (NOT framework primitive) + FAKE-TIME differential strategy + sorted-slice percentile aggregation (NOT CircllHist) + line-cited algorithmic lemmata against `gradient_controller.cc` + `fixed_value` PARSE-REJECT at Task 3 `84e317f`; ADR-0187 RTDS `enabled.RuntimeFeatureFlag` deferral PARSE-REJECT (static-default honored; `runtime_key != ""` triggers HCM-parse-time PARSE-REJECT with forward-pointer to the future Runtime/RTDS family phase) + `enabled` empty-default OFF semantics per AMEND-4 at Task 2 `5a0a720`. **1 IN-PLACE §Decision AMENDMENT body anchored** at Task 4 `4a8f9c4`: ADR-0059 §Decision AMENDMENT (float-valued gauge int64 encoding convention; NEW `internal/stats/conv.go` `BoolToInt` helper + comment-only `gauge.go` cross-reference; NO signature change to `*stats.Gauge`; ns for time-typed; ×1000 for ratio-typed; 0/1 for bool-typed). The AMENDMENT body REPLACES the SPEC-commit ANTICIPATION paragraph in-place per ADR-0044 in-place edit discipline. **All 6 phase-done gates GREEN at Task 14:** Gate A `go build ./...` clean (exit 0); Gate B `go vet ./...` + `golangci-lint run` clean (both exit 0); Gate C `go test -race -count=1 ./...` clean repo-wide (one-time fixture-infrastructure flake on first run — identical class to phase-19.2 + phase-20 PROGRESS precedents at fixture 0023 + 0012; full retry clean — `grep -cE "^FAIL|^--- FAIL" /tmp/race-full.log` → 0; all packages ok); Gate D `go test -count=1 -timeout=15m ./test/differential/ -run 'TestDifferential'` 27/27 fixture directories GREEN (26 pre-existing + new 0025-http-adaptive-concurrency) in 94.7s; Gate E 27th fuzzer `FuzzAdaptiveConcurrencyConfigParse` clean at 30s (2,212,203 execs at 33,551/sec; 19 new interesting / 240 total; 0 panics) + 3 representative pre-existing fuzzers spot-checked clean at Task 1 cold-start preconditions (`FuzzExtProcConfigParse` + `FuzzBootstrapLoad` + `FuzzOAuth2ConfigParse`); Gate F h2spec one-time flake on first run (likely Docker container startup timing) + retry clean 53/53 PASS at ADR-0051 pin (substantive equivalent of the SPEC §7.4-named `make test-h2spec` per the Makefile-absence carry-over from phase-20 PROGRESS Gate-F precedent). **SPEC §15 18-item acceptance checklist all GREEN** (6 gates + fixture-0025 4-scenario coverage + 7-name stat-surface byte-exact + Gradient-1 algorithmic-fidelity 7 Layer-A test families + 14-arm PARSE-REJECT roster + byte-exact 503 wire shape at scenario (b) cross-side + 2 NEW ADRs + 1 IN-PLACE AMENDMENT + 7-edit BEHAVIOR_CONTRACT bundle + DECISIONS + STATE + ROADMAP advance + end-to-end audit trail). **Cross-package regression matrix per SPEC §12 item C8 GREEN**: `internal/stats/` post-ADR-0059 §Decision AMENDMENT clean under `-race` at Task 4 + Task 12; all 16 HTTP filter packages clean under `-race` at Task 12; 27/27 differential fixtures GREEN at Task 12 + Task 14 Gate D. **LEANEST framework-delta §9 row to date** — ZERO new `internal/` framework primitives (only in-package `Clock` seam at `internal/filter/http/adaptive_concurrency/clock.go` + in-place `ADR-0059` §Decision AMENDMENT body landed at `internal/stats/`); 7 framework REUSES per SPEC §3.3 (stats Counter+Gauge / HTTPRegistry / per-request filter interface / HCM-parse-time PARSE-REJECT / REUSE-by-absence per-route enforcement / fuzzer-corpus framework / differential-fixture framework). **FOURTH CONSECUTIVE §9 row to skip ADR-0125 amendment** (REUSE-by-absence per SPEC §5.4 — no `AdaptiveConcurrencyPerRoute` proto message in v1.32.4 or v1.37.x; ADR-0125 roster stays at 8 entries; 5th-canonical consumer roster stays unchanged at FOUR consumers; `grep -cE '^\*\*\(xiv\)\*\*' docs/envoy-go/DECISIONS.md` returns 0 — same as post-phase-20). **27th fuzzer landed at Task 8** (`FuzzAdaptiveConcurrencyConfigParse`; ~29 corpus seeds per D6; clean at 30s — 2,212,203 execs / 19 new interesting / 240 total; 0 panics). **Stat surface 92 → 99 names** (7-name roster — 1 counter `rq_blocked` + 6 gauges `concurrency_limit` / `gradient` / `burst_queue_size` / `sample_rtt_msecs` / `min_rtt_msecs` / `min_rtt_calculation_active` under the upstream-byte-exact `http.<HCM_stat_prefix>.adaptive_concurrency.gradient_controller.<stat>` prefix template). **FOURTEENTH §9 family-row** (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11, csrf @ 12, buffer @ 13, compressor @ 14, bandwidth_limit @ 15, rbac @ 16, jwt_authn @ 17, ext_authz @ 18 with its 18.1+18.2 split, ext_proc @ 19 with its 19.1+19.2 split, oauth2 @ 20). **Notable IMPL-time findings** (forward-pointers carried; NONE blocking phase-done; all in REVIEW.md §6 + per-Task PROGRESS entries): (i) **v1.32.4 proto-binding limitation discovered at Task 2** — the `fixed_value` PARSE-REJECT arm 13 is structurally unreachable in v1.32.4 because the `MinimumRTTCalculationParams.fixed_value` field does not exist at the v1.32.4 proto-binding; byte-stable wording preserved (the arm exists in compiled_config.go as documented unreachable code with a TODO referencing the future proto-bump phase); the runtime test for arm 13 is skipped with a documented `t.Skip("v1.32.4 proto-binding lacks fixed_value field; arm 13 unreachable")` per Task 2 PROGRESS entry; forward-pointer to future proto-bump phase recorded in BEHAVIOR_CONTRACT.md §13.D Phase 21 forward-pointer notes; (ii) **fixture-0025 scope-deviation at Task 10** — single-directory + REFERENCE-LESS deviation from PLAN §10 4-sub-directory full-cross-side per phase-20 oauth2 precedent (the planned (a) parse_ok + (b) overflow_503 cross-side + (c) stat_surface + (d) pass_through_when_disabled landed as a single fixture-0025 directory with subject-only structural assertions); AMEND-6 cross-side byte-exact for scenario (b) DEFERRED as RATIFIED-PENDING-FUTURE-CROSS-SIDE-EXTENSION; the 503 + 25-byte body `"reached concurrency limit"` + `content-type: text/plain` + `content-length: 25` are all asserted envoy-go-side per the filter `decode_headers.go` 503-emission code path; (iii) **D8 HOLDING + 0 impl-time-unanticipated ADRs fired across all 14 tasks** — ADR-0188 + ADR-0189 stay UNCONSUMED at phase-21 IMPL phase-done; STRENGTHENED two-slot buffer per SPEC §10 D carried forward to phase 22. **Phase 21 is READY FOR SQUASH-MERGE TO MASTER** per project memory `feedback_git_worktrees.md` + ADR-0005 §Decision 4. Total fuzzer count is 27 (`grep -rE '^func Fuzz' --include='*.go' .` returns 27 unique names).
- **phase-directory:** `docs/envoy-go/phases/21-http-filter-adaptive-concurrency/` (BRAINSTORM.md authored at commit `cad1153` + SPEC.md authored at commit `49ba034` + PLAN.md authored at commit `ede4ac2` + PROGRESS.md ~1295 lines pre-Task-14 + Task 14 final entry append + REVIEW.md authored at Task 14; ROADMAP row 21 single-row per ADR-0045 single-row decision settled at SPEC commit `49ba034` + reconfirmed at PLAN commit `ede4ac2`).
- **lifecycle-state:** `phase 21 IMPL done; awaiting next-phase identification`. ROADMAP row `21` is `done` at this commit (per-cell IMPL-done annotation appended documenting the 14-task IMPL landing + 2 NEW ADRs + 1 IN-PLACE AMENDMENT + ZERO new framework primitives + the 6-gate verbatim outputs + SPEC §15 18-item acceptance summary + notable IMPL-time findings + the FOURTH CONSECUTIVE REUSE-by-absence milestone). DECISIONS.md tail at **ADR-0187** (2 NEW ADRs ADR-0186 + ADR-0187 + 1 IN-PLACE AMENDMENT to ADR-0059 landed at phase 21 IMPL); next-free **ADR-0188** (UNCHANGED — D8 hypothesis HELD; STRENGTHENED two-slot buffer with ADR-0189 also UNCONSUMED carries forward to phase 22).
- **next-skill:** `superpowers:brainstorming` (the next-phase initial skill per SKILL_ROUTING.md state 0; the user identifies the next-phase target at cold start). Alternative if the user redirects to a non-§9 lane: any of the standard cold-start skills per `SKILL_ROUTING.md`. Cold-start scope for the next session: read STATE.md (this file) + `docs/envoy-go/ROADMAP.md` (4 remaining §9 family-rows: `lua` / `wasm` / `admission_control` / `global rate limit`; plus other top-level rows on the broader roadmap) + `docs/envoy-go/phases/21-http-filter-adaptive-concurrency/REVIEW.md` (the phase-21 IMPL-done reviewer artefact — load-bearing for the squash-merge commit message body + for understanding the carried-forward future-work items inherited into the next phase) + `docs/envoy-go/phases/21-http-filter-adaptive-concurrency/PROGRESS.md` (Task 14 entry — the 6-gate verbatim outputs + SPEC §15 18-item closure checklist + D8 final hypothesis status + known limitations).
- **last-commit:** `473e970`. Phase 21 IMPL-done squash-merge of branch `phase-21-http-filter-adaptive-concurrency-impl` from worktree `.worktrees/phase-21-http-filter-adaptive-concurrency-impl/` is the user's manual step after this Task 14 commit lands (per the phase-09..20 squash-merge convention). Predecessor master tip was `3aa87e8` — `phase 21 PLAN follow-up: STATE.md SHA-fill (TBD → ede4ac2 post-squash)`. This SHA-fill placeholder will be backfilled per the phase-09..20 convention: `phase 21 IMPL follow-up: STATE.md SHA-fill (TBD → <SHA> post-squash)`.
- **last-updated:** 2026-05-18
- **next-free ADR:** `ADR-0188` — UNCHANGED from SPEC + PLAN. **D8 hypothesis HELD across all 14 IMPL tasks** — NO additional ADR consumed at phase-21 IMPL phase-done. The IMPL-time discoveries that surfaced at execution (v1.32.4 proto-binding limitation at Task 2 making arm 13 unreachable; fixture-0025 single-directory + REFERENCE-LESS scope-deviation at Task 10) were both resolved as documented-unreachable-arm + scope-reduction + forward-pointer documentation in BEHAVIOR_CONTRACT.md §13.D Phase 21 forward-pointer notes + REVIEW.md §6, NOT new ADRs. STRENGTHENED two-slot buffer per SPEC §10 D: ADR-0188 + ADR-0189 both stay UNCONSUMED at phase-21 IMPL phase-done; the next-phase escape-valve invariant is preserved with TWO slots of buffer (the BRAINSTORM-anticipated ADR-0188 candidate collapsed at SPEC time per AMEND-7; the STRENGTHENED two-slot buffer per SPEC §10 D adds ADR-0189 as the second buffer slot — both carry forward unconsumed into phase 22).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
