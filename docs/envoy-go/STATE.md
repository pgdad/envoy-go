# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `20-http-filter-oauth2` — phase `20-http-filter-oauth2` BRAINSTORM done at 2026-05-17 via a 10-question interactive dialogue (Q1 MVP envelope — Standard OAuth 2.0 sign-in + refresh + sign-out, NO id_token / NO PKCE / NO end_session_endpoint / NO cookie_configs / NO BASIC_AUTH; Q2 `internal/httpclient/` generalization trigger — EXTRACT NOW per the ADR-0159 §Future Work third-consumer trigger (oauth2 is the third outbound-HTTP consumer after `internal/jwks/Fetcher` at phase-17 + `extauthz/check.go`'s `httpAuthClient` at phase-18.1); Q3 SDS posture — filesystem-path SDS only, other ConfigSource variants PARSE-REJECT envoy-go-strict; Q4 token-encryption envelope — Proto-faithful AES-GCM default-on, honor `disable_token_encryption=true` to skip; Q5 per-route discipline — No per-route support, listener-scoped only per the v1.37.x oauth2 proto's absence of an `OAuth2PerRoute` message; Q6 ADR-0045 split-readiness leaning — MEDIUM-HIGH (LoC envelope between phase-17 jwt_authn 3855 single-row and phase-19 ext_proc 5000+ split; SPEC author makes the final call after the empirical-pin scrape closes the LoC envelope); Q7 filesystem-SDS location — NEW shared `internal/sdsfile/` package (~80-120 LoC primitive; anticipates future filters needing the same shape); Q8 SDS reload posture — fsnotify-based hot-reload with atomic-swap (~80 LoC extra; bundled into ADR-0178); Q9 cookie HMAC scheme — HMAC-SHA256(hmac_secret, host + BearerToken + OauthExpires) reference-Envoy default composition + host-binding rationale + `subtle.ConstantTimeCompare` constant-time discipline; Q10 token-endpoint POST body format under URL_ENCODED_BODY — `client_secret_post` in body, NOT `client_secret_basic` (BASIC_AUTH out of envelope per Q1)). Section A (scope + framework primitives + REUSES) + Section B (ADR roster B1 + stat surface B2 + fixture envelope B3 + deny-path wire shape B4 + boot-registration ordering B5 + deferred-items list B6 + cross-phase carry-forward B7 + open questions B8 + prior-phase lessons B9) design synthesis settled at this BRAINSTORM commit. **FIVE framework primitives** committed: (1) NEW top-level `internal/httpclient/` package per Q2 (~150-250 LoC; ADR-0177; closes the ADR-0159 §Future Work forward-pointer load-bearing); (2) NEW top-level `internal/sdsfile/` package per Q7 + Q8 (~160-200 LoC; ADR-0178); (3) NEW filter-local AES-GCM token-encryption helper at `oauth2/tokens.go` per Q4 (~150-200 LoC; ADR-0182); (4) IN-PLACE ADR-0150 §Decision AMENDMENT (jwks `Fetcher` refactored to consume `internal/httpclient/` per ADR-0044 in-place edit discipline); (5) IN-PLACE ADR-0159 §Decision AMENDMENT (extauthz `httpAuthClient` refactored to consume `internal/httpclient/` per ADR-0044; closes the ADR-0159 §Future Work forward-pointer). **THIRD CONSECUTIVE §9 family-row to skip the ADR-0125 amendment** (after phase 18 per ADR-0163 + phase 19 per ADR-0173 both REUSED the 5th canonical; phase 20's REUSE-by-absence is a stronger form — there is no per-route surface at all per Q5 listener-scoped-only enforcement; the absence itself is the lesson). Closes the ADR-0159 §Future Work forward-pointer ("third outbound-HTTP consumer triggers `internal/httpclient/` extraction") — the FIRST §9 family-row to CLOSE a phase-N-1 forward-pointer load-bearing. Phase 20 SPEC pending next session (lifecycle-state 1 → 2 per SKILL_ROUTING) — anticipates ~7-9 ADRs (ADR-0177..ADR-0185 per BRAINSTORM §7) with §Context drafts at SPEC + §Decision + §Consequences bodies at IMPL per ADR-0044; plus the TWO IN-PLACE §Decision AMENDMENTs to ADR-0150 + ADR-0159 per ADR-0044 (not new ADR numbers). Stat surface hypothesis 86 → ~94 names (~8 HCM-rooted SN2-reuse counters; claim approximate — SPEC-time empirical pin §20.P8 confirms; the `cookie_decrypt_failure` counter is an envoy-go-strict departure from Envoy reference flagged for ADR-0080 discipline review at SPEC time per §20.P11). Differential fixture `0024-http-oauth2` (~6-8 scenarios per BRAINSTORM §6) + NEW `test/helpers/oauthbackend/` (in-process OAuth 2.0 authorization-server backend; stdlib `net/http/httptest`-based) + 26th fuzzer `FuzzOAuth2ConfigParse` (current count 25 → 26). ROADMAP row `20` already registered `in-progress` at this BRAINSTORM commit (explicit user-direction departure from the phase-09..19.2 ROADMAP-row-add-at-SPEC convention; sub-phase rows `20.1` / `20.2` may appear at SPEC commit if the ADR-0045 split fires per Q6 MEDIUM-HIGH leaning).
- **phase-directory:** `docs/envoy-go/phases/20-http-filter-oauth2/` (BRAINSTORM.md authored this session at 489 lines; SPEC.md authored next session at lifecycle-state 1 → 2 per SKILL_ROUTING; PLAN.md + PROGRESS.md + REVIEW.md follow at subsequent lifecycle transitions; if the ADR-0045 split fires per Q6 MEDIUM-HIGH leaning, sibling sub-phase directories `docs/envoy-go/phases/20.1-http-filter-oauth2-<slug>/` + `docs/envoy-go/phases/20.2-http-filter-oauth2-<slug>/` get created at the SPEC commit per the phase-18 + phase-19 split-by-feature-class precedent).
- **lifecycle-state:** `phase 20 BRAINSTORM done; phase 20 SPEC pending (next session lifecycle-state 1 → 2 per SKILL_ROUTING)`. ROADMAP row `20` is `in-progress` at this commit (registered AT this BRAINSTORM commit per the explicit user-direction departure from the phase-09..19.2 ROADMAP-row-add-at-SPEC convention; sub-phases column empty pending the SPEC author's ADR-0045 split disposition). DECISIONS.md tail still at ADR-0176; next-free ADR `ADR-0177` stays UNCONSUMED at this BRAINSTORM commit (BRAINSTORM consumes 0 ADRs per ADR-0044 — ADRs anchor at SPEC + IMPL). Phase-19 parent + sub-phases 19.1 + 19.2 all `done` at master tip `c2c0f27` (the phase 19.2 IMPL follow-up STATE.md SHA-fill commit `phase 19.2 IMPL follow-up: STATE.md SHA-fill (TBD → 1ddb661 post-squash)`) — the immediate predecessor on master for this BRAINSTORM commit.
- **next-skill:** `superpowers:brainstorming` (scoped to THIS phase; outputs SPEC.md per SKILL_ROUTING.md state 1 — `superpowers:brainstorming` re-invoked for SPEC authoring per the phase-09..19.2 cadence convention). Cold-start scope for the phase-20 SPEC session: read STATE.md (this file); `docs/envoy-go/phases/20-http-filter-oauth2/BRAINSTORM.md` (load-bearing input — the 10-question Q1-Q10 settled decisions + the Section A + Section B design synthesis + the 12 §10 BRAINSTORM-time open questions for SPEC-time resolution); `docs/envoy-go/ROADMAP.md` row 20 (just-added `in-progress`; the row's verbose description captures the MVP envelope + the FIVE framework primitives + the FIVE framework REUSES + the per-route REUSE-by-absence + the MEDIUM-HIGH split-readiness + the stat surface hypothesis + the differential fixture envelope + the anticipated ADR roster); `docs/envoy-go/BEHAVIOR_CONTRACT.md` §13.4 phase-18.1 forward-pointer notes (the ADR-0159 closure surface — the third-consumer trigger Q2 EXTRACT NOW closes load-bearing) + the JWKS / JWT verifier framework primitive sections (anticipated reuse for the future id_token-enabling phase; phase-20 itself does NOT consume); `docs/envoy-go/DECISIONS.md` ADR-0150 + ADR-0159 (the §Decision AMENDMENT-recipient surfaces for the in-place edit per ADR-0044) + ADR-0125 (no amendment surface — REUSE-by-absence per Q5) + ADR-0044 (in-place edit discipline) + ADR-0045 (split-by-surface discipline; MEDIUM-HIGH leaning per Q6) + ADR-0106 (flat §9 family-row convention); v1.37.x oauth2 proto reference at `/home/esa/go/pkg/mod/github.com/envoyproxy/go-control-plane/envoy@v1.37.0/extensions/filters/http/oauth2/v3/oauth.pb.go` (the canonical proto file for the §10 empirical-pin scrape). The SPEC session's defining IN-SESSION obligations per BRAINSTORM §12: resolve all 12 §10 empirical pins via reference Envoy v1.37.2 scrape per ADR-0004; land §1.1 amendment-block channel entries for each pin's RATIFIED / REFUTED / PARTIAL / REFINED disposition (~6-10 amendments); anchor the 7-9 anticipated ADRs (ADR-0177..ADR-0185 per BRAINSTORM §7) with §Context drafts; record the TWO IN-PLACE §Decision AMENDMENTs to ADR-0150 + ADR-0159 per ADR-0044; confirm the REUSE-by-absence per-route classification at ADR-0180 (or analogous anchor); define the ~6-8-scenario differential fixture (`0024-http-oauth2`) with per-scenario expectations YAML; confirm-or-refute the ADR-0045 split-by-surface release-valve decision per Q6 MEDIUM-HIGH leaning; assign the RATIFIED-PENDING pin closures for §20.P5 (AES-GCM nonce-derivation) + §20.P7 (listener-scoped enforcement point) + §20.P8 (stat surface) + §20.P10 (token_endpoint body-format ordering) per phase-16 §10 lesson (c). Post-SPEC next-skill: `superpowers:writing-plans` for the phase-20 PLAN.md authoring session (lifecycle-state 2 → 3) — OR, if the SPEC author invokes the ADR-0045 split, the SPEC session exits with the sub-phase rows registered and the first sub-phase's PLAN as the next-skill.
- **last-commit:** `760b441` — `Squash merge phase-20-http-filter-oauth2-brainstorm`. Phase-done squash-merge of branch `phase-20-http-filter-oauth2-brainstorm` from worktree `.worktrees/phase-20-http-filter-oauth2-brainstorm/` (single worktree commit `118d2ac` bundled into the squash; 3 files / +497 / -7). This SHA-fill follow-up commit per the phase-09..19.2 convention: `phase 20 BRAINSTORM follow-up: STATE.md SHA-fill (TBD → 760b441 post-squash)`.
- **last-updated:** 2026-05-17
- **next-free ADR:** `ADR-0177` — UNCHANGED from the phase-19.2 IMPL phase-done state. BRAINSTORM consumes 0 ADRs per ADR-0044 (ADRs anchor at SPEC + IMPL — the BRAINSTORM identifies the anticipated roster without consuming numbers). ~7-9 ADRs (ADR-0177..ADR-0185) are anticipated for the phase-20 SPEC + IMPL stages per BRAINSTORM §7: ADR-0177 `internal/httpclient/` framework primitive (closes ADR-0159 §Future Work forward-pointer; AMENDS ADR-0150 + ADR-0159 in-place per ADR-0044) + ADR-0178 `internal/sdsfile/` filesystem-path Secret reader with fsnotify hot-reload (Q7 + Q8 bundled) + ADR-0179 oauth2 HMAC cookie composition canonicalization (Q9) + ADR-0180 oauth2 three-flow state-machine + 302-redirect / 401 / 500 deny-path wire shape + listener-scoped-only enforcement (Q5) + ADR-0181 oauth2 cookie envelope (5-of-7 CookieNames consumed; 2 deferred with PKCE) + Set-Cookie attribute discipline + ADR-0182 AES-GCM token-encryption scheme (Q4) + ADR-0183 refresh-token rotation timing + `default_refresh_token_expires_in` semantics + race-vs-rotation discipline + ADR-0184 sign-out flow + `signout_path` handling + cookie clearing + `deny_redirect_matcher` integration + ADR-0185 (likely) token_endpoint POST body format under URL_ENCODED_BODY `client_secret_post` field-ordering canonicalization (Q10). Plus the TWO IN-PLACE §Decision AMENDMENTs to ADR-0150 + ADR-0159 per ADR-0044 (not new ADR numbers). ADR-0044 escape-valve held in reserve at IMPL-time for ~0-2 impl-time-unanticipated ADRs per BRAINSTORM §7 (most-likely surfaces: AES-GCM nonce-vs-deterministic choice if §20.P5 surfaces a divergence; HMAC composition byte-exact ordering against reference Envoy if §20.P4 surfaces additional fields; listener-scoped-only enforcement point if §20.P7 surfaces a need for both HCM parse-time AND `oauth2.New` factory-time rejection).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
