# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `22-http-filter-lua` (parent row in-progress; sub-phase `22.1-http-filter-lua-vm-and-headers-bridge` done at master tip `c986419`; sub-phase `22.2-http-filter-lua-full-bridge` done at master tip `46183a4`; sub-phase `22.3-http-filter-lua-multi-script-and-per-route` **BRAINSTORM done at this commit** — awaiting 22.3 SPEC). Phase `22-http-filter-lua` parent BRAINSTORM done at parent BRAINSTORM commit + parent SPEC done at `41ccee7`; 22.1 lifecycle traversed BRAINSTORM (parent) → SPEC at `a7021aa` → PLAN at `02d745a` → IMPL at `d30f131` → done. Phase `22.2` lifecycle traversed BRAINSTORM at `6ad3064` → SPEC at `0d6463e` → PLAN at `269dee1` → IMPL at `46183a4` → done. Phase `22.3` lifecycle traverses 0 → 1 at THIS commit per BOOTSTRAP §4.1 invariant 1 (BRAINSTORM done; awaiting SPEC). **22.3 BRAINSTORM done at 2026-05-21** via an 8-decision dialogue settling the genuinely-open 22.3 questions the parent left to this session (parent BRAINSTORM Q7 + parent SPEC §3.1/§4.5/§5.2/§6.3 pre-resolved the 9th-canonical classification + the LuaPerRoute proto roster + the ~20 forward-pointer PARSE-REJECT arms; this BRAINSTORM settles: **1 combined NEW ADR-0193** for the SourceCodes multi-script activation + the LuaPerRoute 9th-canonical 3-tier dispatch (one cohesive coupled surface — per-route `name` delegates INTO `SourceCodes`) + **no-reserved-name discipline** (drops parent SPEC §6.3 forward-pointer arm 3; matches upstream — `default_source_code`+`source_codes` independent proto fields) + **HCM-build-time dangling-reference cross-resolution** (per-route `name` referencing an absent `SourceCodes` key PARSE-REJECTs at HCM filter-chain build time; fail-fast; mirrors phase-17 jwt_authn 8th-canonical `provider_name` discipline) + **fixture-0028 5-tier cross-side byte-exact** (listener-default / per-route-name→named / per-route-source_code-override / per-route-disabled / multi-script-selection) + **+1 NEW fuzzer `FuzzLuaPerRouteConfig`** (SourceCodes parse folds into existing `FuzzLuaConfigParse`; 30→31) + **WEAK HOLD D-hypothesis** (1 combined ADR-0193 + ADR-0125 §(xiv) AMENDMENT clean + 0-1 escape-valve at conditional ADR-0194 — fires only if per-route `*Chunk` lookup pushes per-stream construction over the 1ms R6 gate; likely STAYS WEAK-default since per-route resolution is an O(1) dispatch-table lookup, not a new per-stream VM construction) + **single-phase 22.3** (PLAN fires the ADR-0045 split-gate only if estimates exceed; surfaces too coupled for a clean BRAINSTORM-time split axis). **`source_code` per-route arm honors the FULL 4-arm DataSource roster** + WatchedDirectory PARSE-REJECT (forced by proto type identity with `DefaultSourceCode`). **0 net-new stats** (SHARED-vacuous per the 9th canonical SHARED stat-discipline; stat count STAYS 107). 4 D-questions (D1 compile-cache keying + D2 per-route chunk-binding + D3 disabled hook-skip semantics + D4 fixture boot-reject additions) carry forward to 22.3 SPEC.
- **phase-directory:** `docs/envoy-go/phases/22-http-filter-lua/` (parent — BRAINSTORM.md + SPEC.md authored at parent SPEC commit; parent phase row stays in-progress until 22.3 phase-done per parent SPEC §1 closure pattern). Sub-phase directories: `docs/envoy-go/phases/22.1-http-filter-lua-vm-and-headers-bridge/` (SPEC.md + PLAN.md + PROGRESS.md + REVIEW.md from 22.1 lifecycle; LANDED at master tip `c986419`) + `docs/envoy-go/phases/22.2-http-filter-lua-full-bridge/` (SPEC.md + PLAN.md + PROGRESS.md + REVIEW.md + BRAINSTORM.md from 22.2 lifecycle; LANDED at master tip `46183a4`) + `22.3-http-filter-lua-multi-script-and-per-route/BRAINSTORM.md` **authored at THIS commit** (12-section dialogue: §1 mission/scope + §2 per-topic design decisions [12 sub-sections] + §3 framework-survey [0 new primitives] + §4 per-route 9th-canonical detail + §5 stat surface [0 net-new] + §6 fixture-0028 + §7 anticipated ADRs + §8 deferred items + §9 cross-references + §10 D-questions + §11 lessons applied + §12 closeout) + `22.3-http-filter-lua-multi-script-and-per-route/README.md` (updated at THIS commit — Status now "22.3 BRAINSTORM done at 2026-05-21; awaiting 22.3 SPEC").
- **lifecycle-state:** `phase 22.3 BRAINSTORM done; awaiting 22.3 SPEC`. ROADMAP row `22.3` flips `planned → in-progress` at this commit per ADR-0106 (matches the 22.2 BRAINSTORM-done ROADMAP precedent at `6ad3064` — row flipped `planned → in-progress` at BRAINSTORM-done). Parent row `22` STAYS `in-progress` until 22.3 phase-done; there are no further sub-rows (22.3 is the final sub-phase; parent row 22 closes at 22.3 IMPL phase-done). DECISIONS.md tail UNCHANGED at ADR-0192 (NO new ADR consumption at this BRAINSTORM commit per ADR-0044 §Context-draft discipline — BRAINSTORM authors anticipate but do not consume ADR numbers). ADR-0188 + ADR-0189 + ADR-0190 + ADR-0191 + ADR-0192 + ADR-0177 + ADR-0125 §(xiv) UNCHANGED at this commit. Next-free **ADR-0193** UNCHANGED. **22.3 BRAINSTORM authored at THIS commit** as `docs/envoy-go/phases/22.3-http-filter-lua-multi-script-and-per-route/BRAINSTORM.md` (12-section dialogue matching the 22.2 BRAINSTORM structural template; surfaces 4 D-questions for 22.3 SPEC; documents the anticipated 22.3 IMPL ADR roster: 1 combined NEW ADR-0193 + 1 IN-PLACE AMENDMENT on ADR-0125 §(xiv) + conditional ADR-0194 per WEAK HOLD). **NO new code lands at BRAINSTORM** per ADR-0044 §Context-draft discipline; ADR roster UNCHANGED at ADR-0192; next-free ADR-0193 UNCHANGED.
- **next-skill:** `superpowers:brainstorming` (scoped to 22.3 SPEC per SKILL_ROUTING state-1 entry "Phase in ROADMAP, directory exists, SPEC.md does not exist → superpowers:brainstorming scoped to THIS phase → output: SPEC.md" — the 22.3 directory exists with BRAINSTORM.md + README.md; the SPEC-authoring session uses the brainstorming skill scoped to 22.3 SPEC matching the 22.1 + 22.2 SPEC session precedent at master tips `a7021aa` + `0d6463e`). Cold-start scope for the next session: read STATE.md (this file) + `docs/envoy-go/ROADMAP.md` (row 22.1 done; row 22.2 done; row 22.3 in-progress at this commit; row 22 in-progress) + `docs/envoy-go/phases/22.3-http-filter-lua-multi-script-and-per-route/BRAINSTORM.md` (THIS commit's 12-section dialogue — load-bearing for the 8 settled decisions + the 4 D-questions) + `docs/envoy-go/phases/22.2-http-filter-lua-full-bridge/REVIEW.md` (predecessor sub-phase REVIEW + §8 22.3 BRAINSTORM scope hand-off + 25-item acceptance) + `docs/envoy-go/phases/22.2-http-filter-lua-full-bridge/{SPEC.md,PLAN.md}` (predecessor sub-phase SPEC/PLAN structural precedent) + `docs/envoy-go/phases/22.1-http-filter-lua-vm-and-headers-bridge/{SPEC.md,PLAN.md,REVIEW.md}` (predecessor sub-phase precedent — the 22.1 SPEC §11 empirical-pin pattern is the template for the 22.3 SPEC §11 scrape) + `docs/envoy-go/phases/22-http-filter-lua/SPEC.md` (parent SPEC §3.1 surface-mapping + §4.5 ADR-0125 §(xiv) AMENDMENT-anticipation + §5.2 LuaPerRoute proto roster + §6.3 22.3 forward-pointer PARSE-REJECT arms + §10 forward-pointers + §11 empirical-pin block) + DECISIONS.md tail (ADR-0188 + ADR-0189 + ADR-0190 + ADR-0191 + ADR-0192 full bodies; ADR-0193 still next-free; ADR-0125 §(xiv) anticipation paragraph; ADR-0154 SHARED-stat discipline) + BEHAVIOR_CONTRACT.md `### envoy.filters.http.lua` (22.1 + 22.2 contract subsections — 22.3 SPEC anticipates extending with multi-script + per-route detail + ADR-0125 §(xiv) cross-reference). The 22.3 SPEC session creates a fresh worktree per project memory `feedback_git_worktrees.md`: `git worktree add /home/esa/git/envoy-go/.worktrees/phase-22.3-http-filter-lua-multi-script-and-per-route-spec -b phase-22.3-http-filter-lua-multi-script-and-per-route-spec <22.3-BRAINSTORM-tip-SHA>` per the phase-22.1-SPEC + phase-22.2-SPEC sub-phase-SPEC worktree precedent.
- **last-commit:** `TBD` placeholder for the 22.3 BRAINSTORM squash-merge SHA (filled in the post-squash SHA-fill follow-up commit per the phase-09..22.2 convention — `phase 22.3 BRAINSTORM follow-up: STATE.md SHA-fill (TBD → <squash-SHA> post-squash)`). Predecessor master tip: `2821f0d` — `phase 22.2 IMPL follow-up: STATE.md SHA-fill (TBD → 46183a4 post-squash)`. Pre-predecessor: `46183a4` — `Squash merge phase-22.2-http-filter-lua-full-bridge-impl`.
- **last-updated:** 2026-05-21
- **next-free ADR:** `ADR-0193` — UNCHANGED at this 22.3 BRAINSTORM commit (NO ADR consumption per ADR-0044 §Context-draft discipline; BRAINSTORM authors anticipate but do not consume ADR numbers). Anticipated 22.3 IMPL ADR landings (settled at this 22.3 BRAINSTORM per Q1 + Q7): **1 combined NEW ADR-0193** (`internal/filter/http/lua/` 22.3 multi-script + per-route surface — `Lua.SourceCodes` named-script map consume + `LuaPerRoute` 9th-canonical 3-arm oneof + per-route 3-tier dispatch + HCM-build-time dangling-reference cross-resolution + no-reserved-name disposition + fixture-0028 + `FuzzLuaPerRouteConfig`; the SourceCodes + LuaPerRoute surfaces are combined into ONE ADR because they are tightly coupled — per-route `name` delegates INTO `SourceCodes` — per the 22.1 one-package-shape-ADR economy precedent ADR-0189) + **1 IN-PLACE AMENDMENT** on `ADR-0125 §(xiv)` (canonical-per-route roster grows 8 → 9; adds the 9th canonical specification [3-arm hybrid: disabled-bool + string-reference-delegation + DataSource-wholesale-override] + the SHARED stat-discipline + the structural-distinctness rationale + the 9-shape roster table + the lua-row first-use citation; NO new ADR number consumed per ADR-0044 in-place edit discipline; lands at 22.3 IMPL final Task; the AMENDMENT-anticipation paragraph at parent SPEC commit STANDS UNCHANGED) + **1 conditional ADR-0194** (escape-valve slot per Q7 WEAK HOLD — fires only if the R6 *LState-pool gate trips at the 22.3 IMPL benchmark Task [per-route `*Chunk` lookup + per-route resolution push per-stream construction over the 1ms threshold]; LIKELY STAYS UNCONSUMED — per-route resolution is an O(1) dispatch-table lookup, not a new per-stream VM construction; the R6 escape-valve slot shifts from ADR-0193 [now the combined multi-script + per-route ADR] to ADR-0194 per the 22.2 REVIEW.md §5 R6 disposition carry-forward). **ADR-0188 EXPLICIT API-REVISION ALLOWANCE clause STAYS scoped to consumer-#2** (UNCHANGED at 22.3 — 22.3 is still consumer-#1; per-route + multi-script are dispatch-surface extensions, not `internal/lua/` VM-API revisions). Phase-22.3's WEAK HOLD escape-valve buffer at THIS BRAINSTORM commit: 0-1 anticipated consumption (ADR-0194 conditional); re-evaluates at 22.3 IMPL benchmark Task per Q7.

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
