# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `15-http-filter-bandwidth-limit` — phase 15 SPEC.md authored at this commit (state-2 → state-3 transition per ADR-0005). Phase 15 is the EIGHTH §9 family-row (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11, csrf @ 12, buffer @ 13, compressor @ 14) and the FIRST §9 row since phase-12 csrf to introduce **ZERO framework deltas** — composes against phase-09 fault async-resume (`time.AfterFunc` + `cb.ContinueDecoding/Encoding`) + phase-13 ADR-0128 decode-side body-buffering + phase-14 ADR-0131 encode-side `OverwriteBody` (anticipated NOT invoked). MVP envelope post-SPEC §1.1 amendments (**10 authored: 5 structural + 4 field-bookkeeping + 1 operational-foot-gun**): 4 consumed (`stat_prefix` PGV `min_len=1`, `enable_mode` all 4 values DISABLED/REQUEST/RESPONSE/REQUEST_AND_RESPONSE, `limit_kbps` UInt64Value **KiB/s units NOT kbps** per §1.1 amendment 6 + filter-internal-REQUIRED-at-per-route, `fill_interval` Duration default 50ms PGV `[20ms, 1s]`) + 3 silent-ignored (`runtime_enabled`, `enable_response_trailers`, `response_trailer_prefix`); Path B-async body algorithm with **kbps-per-tick throttle math** (`chunk_size = limit_kbps × 1024 × fill_interval_seconds`; `throttle = ceil(body/chunk_size) × fill_interval`) per ADR-0137 + §1.1 amendment 6 + §11.P15. Per-route uses **same bare `BandwidthLimit` proto via TPFC** (NO `BandwidthLimitPerRoute` wrapper exists in Envoy v1.37.2 per §11.P1; SPEC introduces a NEW canonical per-route pattern adjacent to ADR-0117 4th canonical via ADR-0125 §(xi) amendment as the 6th canonical entry). Per-route stats INDEPENDENT per ADR-0117 + §11.P4 + ADR-0139. Stat surface 46 → **60 names** (14 active = 8 counters + 6 gauges; namespace `<stat_prefix>.http_bandwidth_limit.<counter>` underscore-infix NOT HCM-rooted; Prometheus inlines stat_prefix into base name; NO new SN10 rule per §11.P10) + 2 unconditional Envoy histograms allow-listed via twin-series-filter divergence-window. Differential fixture `0017-http-bandwidth-limit` (6 scenarios; ±70ms wall-clock tolerance per §11.P9); 19th fuzzer `FuzzBandwidthLimitConfigParse`. Operational foot-gun (listener-level missing `limit_kbps` + active `enable_mode` → runtime hang) is byte-equivalent across both proxies per §1.1 amendment 10.
- **phase-directory:** `docs/envoy-go/phases/15-http-filter-bandwidth-limit/` — BRAINSTORM.md (509 LoC; PRESERVED VERBATIM per D-3.5) + SPEC.md authored (~1800 LoC; §§1-15 covering envelope + 10 §1.1 amendments + §11 verbatim empirical-pin evidence + §13 BEHAVIOR_CONTRACT patches + §14 testing strategy + §15 acceptance checklist). PLAN.md / PROGRESS.md / REVIEW.md NOT yet authored — those are subsequent lifecycle-state transitions (state-3 → 4 → 5 → 6) per ADR-0005 §Decision 4 session-split discipline.
- **lifecycle-state:** `phase 15 SPEC.md authored; PLAN.md pending` — state-2 → state-3 transition at this SPEC commit. Next session authors PLAN.md per the phase 09/10/11/12/13/14 precedent. SPEC's anticipated 5 ADRs (ADR-0135..ADR-0139) per §8 are roster-staged at SPEC commit; ADR §Decision bodies land at impl-time per ADR-0044 ADR-on-impl convention. ADR-0125 in-place §(xi) amendment paragraph LANDED at this SPEC commit per phase-13 ADR-0127-v2 + phase-14 ADR-0125 §(viii)-(x) in-place-update precedent.
- **next-skill:** `superpowers:writing-plans` per BOOTSTRAP_PROMPT.md §5 lifecycle-state-3 entry condition (PLAN.md does not exist — phase 15 enters this state at this commit; the routing convention from phases 09/10/11/12/13/14 is that `superpowers:writing-plans` is called once for SPEC drafting, then again for PLAN drafting based on the SPEC).
- **next-skill-scope:** Cold-start scope for the phase 15 PLAN-authoring session. Read first: `docs/envoy-go/phases/15-http-filter-bandwidth-limit/SPEC.md` (~1800 LoC; the authoritative design doc post-§1.1 10-amendment refutation block + §11 verbatim empirical evidence; SPEC §6 code shapes + §7 fixture topology + §8 ADR roster + §14 testing strategy + §15 acceptance checklist are the load-bearing inputs to PLAN). Read also: `docs/envoy-go/phases/14-http-filter-compressor/PLAN.md` + `docs/envoy-go/phases/13-http-filter-buffer/PLAN.md` (PLAN structural templates — task decomposition + per-task TDD cycles + per-task ADR landings). `docs/envoy-go/phases/15-http-filter-bandwidth-limit/BRAINSTORM.md` (509 LoC; PRESERVED VERBATIM per D-3.5; §§1-11 are the pre-§11-empirical-pin design sketch; load-bearing decisions still cite BRAINSTORM but SPEC §1.1 amendments record the SPEC-time refutations). `docs/envoy-go/DECISIONS.md` ADR-0117 (per-route INDEPENDENT-stats precedent inherited verbatim by phase-15), ADR-0125 §(xi) (the amendment paragraph landed at this SPEC commit codifies the NEW canonical pattern phase-15 introduces), ADR-0128 + ADR-0131 (framework primitives phase-15 composes), ADR-0044 (SPEC-time ADR anticipation discipline). `docs/envoy-go/phases/11-http-filter-local-ratelimit/SPEC.md` + `internal/filter/http/localratelimit/local_ratelimit.go` (the algorithmic precedent — phase-15 mirrors the `factoryState` + `sync.Map` lazy-cache + `resolvePerRouteConfig` + `newFilterStatsIfAbsent` shape verbatim with bandwidth-throttle semantics). PLAN sequences ~14-18 tasks per SPEC §1.4 estimate; each task lands a focused TDD red/green commit. Anticipated task chain: package skeleton + `New` factory + `compiledConfig` + `parsePerRoute` (Tasks 1-2); per-stream `*filter` struct + `bucket.go` throttle helper + `DecodeHeaders`/`EncodeHeaders` (Tasks 3-4); `DecodeData`/`EncodeData` kbps-per-tick algorithm + timer arming + counter increments (Tasks 5-6); `OnDestroy` + timer cleanup + Stop-races-Fire pending-gauge accounting (Task 7); per-route INDEPENDENT-stats lazy-cache + `resolvePerRouteConfig` (Task 8); 14-stat `filterStats` + `newFilterStats` / `newFilterStatsIfAbsent` (Task 9); boot registration in `cmd/envoy-go/main.go` (Task 10); differential fixture `0017-http-bandwidth-limit` (6 scenarios + driver + expectations.yaml + README; Tasks 11-13); BEHAVIOR_CONTRACT.md edits §13.1-§13.5 + 5 ADRs ADR-0135..ADR-0139 (Tasks 14-16); review carry-forwards (Tasks 17-18). PLAN author confirms the 5 anticipated ADRs at PLAN-time per ADR-0044; revises count if needed.
- **last-commit:** `<TBD>` — phase 15 SPEC squash-merge commit on master, post-`wt-merge` from worktree `phase-15-http-filter-bandwidth-limit-spec`. Fills via the SHA-fill follow-up commit per phase-11/12/13/14 SPEC precedent. The SPEC-stage worktree branch carries: SPEC.md authored at `docs/envoy-go/phases/15-http-filter-bandwidth-limit/SPEC.md`; ADR-0125 §(xi) in-place amendment paragraph in `docs/envoy-go/DECISIONS.md`; STATE.md advance (this file); ROADMAP.md row 15 status `planned → in-progress` + summary revision. Master tail before this SPEC commit: `e7a26ef` (phase 15 brainstorm follow-up STATE.md SHA-fill) → `fa125f2` (phase 15 brainstorm squash-merge) → `f4ce582` (phase 14 lifecycle-state-6) → `a3895b1` (phase 14 impl follow-up) → `9df9a29` (phase 14 impl squash-merge) → preceding phase-14 commits.
- **last-updated:** 2026-05-11
- **next-free ADR:** `ADR-0135` (unchanged from phase 14 STATE.md SHA-fill; phase-15 SPEC anticipates ADR-0135..ADR-0139 per §8 but does NOT author §Decision bodies at SPEC time — only the roster + ADR-0125 §(xi) in-place amendment paragraph land at this SPEC commit). The 5 anticipated ADRs land at phase-15 impl-time tasks per ADR-0044 ADR-on-impl convention. After phase-15 impl-time closes, `next-free ADR` advances to `ADR-0140`.

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
