# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

> **This file is a POINTER, not an archive** — `BOOTSTRAP_PROMPT.md` §4: *"STATE.md — pointer: active phase, last commit, next action."* It carries the live pointer, the live counts, and a SHORT recent lineage. **Full stage-by-stage history lives in `STATE_HISTORY.md`** (archived at ADR-0288; **nothing was deleted** — the pre-move file is preserved there verbatim, byte-for-byte).
>
> **⚠️ THE RULE ADR-0288 EXISTS TO ENFORCE: there is EXACTLY ONE live `next-skill:`, `lifecycle-state:`, `next-free ADR:`, and count block IN THIS FILE, and they live in §Current pointer directly below.** Before that move, ~330 lines of prepended backlog had buried TWO fossilised "current state" blocks whose `next-skill:` still read *"the 42.2 hedging BRAINSTORM"* and *"the 46.2 SPEC"* while the live phase was **66** — so `grep 'next-skill:' STATE.md` returned **two stale hits and ZERO live ones**, and `grep 'fuzzers:'` returned **48** and **42** when the true count was **55**. **A session may trust a grep of this file again. Keep it that way: when you advance the pointer, EDIT §Current pointer IN PLACE — never prepend a new block above it.**

---

## Current pointer

*(The ONLY live "what next" in this repo. Update IN PLACE at every stage close.)*

- **active-phase:** `phase 97 (quic-chain-selection-order) BRAINSTORM done` (2026-09-09) — lifecycle-state **DONE -> 1**. **PHASE 97 OPENED BY SELF-PICK** under the 2026-07-12 standing directive, with no human consulted and no banked mid-lifecycle work to advance first; the pick AND the fully-adjudicated rejected roster are in `BRAINSTORM.md` §1 and §4. **ROW 97 ADDED `in-progress`** — an **ADD, not a flip**, so the denominator MOVED: `want` **128 -> 129**, `ROADMAP.md` **246 -> 247**, row 97 at file line **159**. **THE SUBJECT: on a QUIC/HTTP-3 listener carrying BOTH a `default_filter_chain` AND a universally-eligible empty-match `filter_chains[0]`, envoy-go serves from the DEFAULT chain and leaves the eligible indexed chain at ZERO requests — exactly the alternative ADR-0080 §Alternatives (A) enumerates and REJECTS, and which §Decision 1 and §Decision 2 forbid, both empirically pinned to Envoy v1.37.2.** REPRODUCED BY EXECUTION on TWO shapes, both validating rc=0 and booting clean: a plaintext default chain, and one carrying its OWN QUIC transport socket — the second being the parity-relevant arm, since phase 96 already measured that the reference REJECTS a no-`transport_socket` default chain, so the first alone would merely re-find the banked D2-QUICTS. Both read `DEFAULT_CHAIN` **1** / `INDEXED_CHAIN` **0**; the plaintext arm ALSO cross-wires provenance within one connection, TLS from `filter_chains[0]` and filters from the default slot. **ROOT CAUSE: `internal/listener/quic.go`'s two chain accessors use DIFFERENT predicates and NEITHER calls the mandated algorithm** — the TCP path selects with `listenerfilter.SelectChain` over an ORDERED `chainSpecs`; QUIC reaches around it into an unordered map, behind a comment asserting a one-chain precondition NOTHING enforces.
- **phase-directory:** `docs/envoy-go/phases/97-quic-chain-selection-order/` — `BRAINSTORM.md` (**396**), re-derived by `wc -l` at the PUBLISHING tip in the SAME commit as the edits that moved it, never copied forward, and the `ROADMAP.md` citation of that figure was re-synced AFTER §8.6 was appended (it read 377 before). Phase dirs **137 -> 138**. No `SPEC.md`, `PLAN.md` or `PROGRESS.md` yet — this is a BRAINSTORM close.
- **lifecycle-state:** **1** (row 97 reads `in-progress` at `ROADMAP.md:159`; BOOTSTRAP §5 state 1 ⇒ the next session runs the **SPEC**). ⚠️ **ONE STAGE PER SESSION.**
- **next-skill:** **RUN THE PHASE-97 SPEC** (state 1 -> 2). ⚠️ **The termination sentinel was evaluated MECHANICALLY before AND after this stage's `ROADMAP.md` edit and DOES NOT FIRE** — check (1) went SILENT -> **ONE** line (`NOT DONE: row 97`, this row being registered), check (2) stayed **SIX** with every window shifted **+1** to `:207 :213 :219 :229 :235 :243` and all six per-line md5s BYTE-IDENTICAL, check (3) stayed SILENT. **The SPEC's BLOCKING UNKNOWN is `BRAINSTORM.md` §10 item 1: does the pinned reference ACCEPT a two-chain QUIC listener at all, and WHICH chain does it serve?** That measurement chooses the repair shape and must precede it. ⚠️ **A cost floor of ONE production file, `+7 / -10`, was measured by a built, run and reverted prototype and is a LOWER BOUND** — and the whole `internal/listener` package is GREEN both before and after it, which is a COVERAGE finding, not a clean bill.
- **last-commit:** ⚠️ **TAKE THE TIP FROM `git rev-parse master`, NEVER from a subject grep and NEVER from a count quoted anywhere.** ⚠️ **RUN *BOTH* LOCATE FORMS AND RECONCILE THE SETS** — `git log --grep '^phase 97 (quic-chain-selection-order)'` (the SLUG form) and `git log --grep '^phase 97'` (the LOOSE form). **NO COUNT IS QUOTED HERE, DELIBERATELY — both forms rot, and a SLUGLESS correction commit is invisible to the anchored form** (the live witness is `963d472d`, a phase-92 PLAN correction). This BRAINSTORM landed as ONE squashed commit off `5fe3c460`.
- **last-updated:** 2026-09-09 (phase-97 **BRAINSTORM done**). Files touched, matching the FIVE-file BRAINSTORM scope MEASURED by `git show --numstat` across the phase-94, -95 and -96 BRAINSTORM commits (`b75b0e2d` `7f568db2` `e99e9cb6`), each of which touched exactly these five and nothing else — `ROADMAP.md` (**+1 row**), `STATE.md` (in place), `STATE_HISTORY.md` (**562 -> 564**, raw delta **+2**: a blank line PLUS the entry line), the new `BRAINSTORM.md`, and `next-prompt.txt` (`git add -f`). **`DECISIONS.md` and `BEHAVIOR_CONTRACT.md` are BYTE-UNTOUCHED**, and ZERO production `.go` bytes: the prototype was reverted and the revert PROVEN by `sha256sum -c` on `internal/listener/quic.go` plus an empty `--untracked-files=all` status.
- **next-free ADR:** **`ADR-0319`**, UNCHANGED — a BRAINSTORM drafts no ADR; the phase-97 ADR lands at the SPEC. TAIL-derived (`grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1` -> `## ADR-0318`; `grep -c '^## ADR-0319'` ⇒ **0**). ⚠️ **NEVER derive from the heading count** — the id space is sparse at the `0209` gap. `DECISIONS.md` **19050** lines, `^---$` **216**, `^## ADR-` **317**, bare `^## ` **325** — all four UNCHANGED across this stage. ⚠️ **The house `PROPOSED` guard reads 0 and that is its RESTING STATE, not a break**; a SPEC drafting a new §Context RE-ARMS it.

---

## Project counts at this commit

*(⚠️ **NOT LIVE — THIS BLOCK IS A PHASE-76-ERA SNAPSHOT, AND IT SAID "Live" FOR SIXTEEN PHASES.** Corrected at the phase-93 BRAINSTORM. The bullets below are RETAINED VERBATIM for their historical detail and their negative controls; **their headline figures are stale and are superseded by the correction table immediately below.** The live pointer is §Current, above.)*

⚠️ **CORRECTION LANDED AT THE PHASE-93 BRAINSTORM (2026-08-29).** The bullets below were re-run mechanically at the **phase-76 IMPL close** — sixteen phases ago — under a header that read *"(Live. Re-verify at session start …)"*. **Three of their figures were re-derived at `40cf6766` and are superseded here; each command is given so the next session need not trust this line either:** **active differential fixtures 119 -> `121`** (`ls -d test/fixtures/*/ | wc -l`; tail `0119-grpc-unary-trailers`, **`0120` FREE** — ⚠️ the plausible `grep -cE '^[0-9]{4}-'` form reads **119** because it drops `0007a`/`0007b`) · **fuzzers 55 -> `56` targets across `48` FILES** (`git grep -c '^func Fuzz' -- '*.go'`; the occurrence form agrees — ⚠️ **it went stale INSIDE row 92's own commit**, which added `FuzzValidateResponseHeaderBlock` while the row's ROADMAP text still anticipates `+0`) · **DECISIONS.md tail ADR-0298 -> `ADR-0314`, next-free `ADR-0315`** (TAIL-derived; headings+1 COLLIDES at the `0209` gap). ⚠️ **THE STAT SURFACE IS DELIBERATELY NOT RENUMBERED.** Three different absolutes are live in this tree at this one tip — this block's, the phase-92 `PLAN.md`'s, and the `BEHAVIOR_CONTRACT.md` ledger tail's — and the contract warns of itself that a mechanical re-derivation *"should expect the re-derived figure to disagree."* Per `reference_a_drift_correction_is_itself_a_claim`, **on a contested count: NO NUMBER.** Enforcement remains the per-phase `TestNoNewStat*` delta guards, never an absolute. ⚠️ **AND `406` / `406 -> 407` MUST NOT BE RESTATED ANYWHERE** — refuted at the phase-92 PLAN; `grep -c '406' BEHAVIOR_CONTRACT.md` reads **0**. **The remaining bullets (BackendKind tail 38, go.mod, reference pin, conformance) were re-verified at `40cf6766` and still hold.**

- **active differential fixtures:** **119** — ⚠️ **Phase 76 landed +0: `0061-lb-ring-hash` was EDITED, not added** (`sourceIPs` 4 -> 16 plus a NEW `driver/linkage_test.go` inside the EXISTING fixture directory — no new YAML, no new directory, no new BackendKind, no new port), so the count STAYS **119** and `0118` remains FREE. NC observed: `mkdir .../0118-fake` ⇒ 120. — `ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0117-tracing-custom-tags-metadata-cluster` (phase 73). **Phase 75 landed +0**: `0110-tls-require-client-cert-false` was **EXTENDED** with a cross-side `fixture.StatsAsserter` (NO new YAML, NO new directory, NO new BackendKind, NO new port), so the count STAYS **119** and `0118` remains FREE. ⚠️ **Next-free REFERENCE port is `10450`, NOT `10118`** — ports are NOT fixture-index aligned. **The fixture now asserts what its own README confessed was infeasible, and BOTH sides were EXECUTED**: `ssl.handshake=2 ssl.no_certificate=1 ssl.fail_verify_error=1 ssl.fail_verify_no_cert=0`, `downstream_cx_total=3`, **identical on reference and subject**, with the discriminating asymmetry (`handshake=2` vs `no_certificate=1`) observed per side. Shape A over `/stats/prometheus`, keyed on the metric NAME with `envoy_listener_address` IGNORED — the NAMES are byte-identical cross-side while the labels differ (`0.0.0.0_10446` reference vs **`___20016`** subject: ⚠️ **the subject binds the IPv6 WILDCARD**). ⚠️ **`0111` was NOT given a value assertion, deliberately** — it runs `require_client_certificate: true` on both sides, so a success-path no-cert annotation reads **0 on every arm, structurally**, and a value pin would document a **vacuous `0 == 0`** as coverage; its PROSE gained the name in its NAMED-UNASSERTED list instead, because **both `0111` passages are CLOSED ENUMERATIONS** and a name in neither list reads as ASSERTED. **`0111`'s driver is byte-untouched, and its named-subset check was EXECUTED for the first time at this IMPL: PASS.** ⚠️ `var _ fixture.StatsAsserter` is a **TRIPWIRE, not the dispatch mechanism** — `0110` is only the **THIRD** fixture repo-wide to carry it, while **82** define an `AssertStats` method.
- **fuzzers:** **55** — **Phase 76 landed +0** (it consumes no new config field, so there is no parse arm to fuzz). NC observed: appending `func FuzzProbeOnly` ⇒ 56. — `grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`. **Phase 75 landed +0** — the row consumes no new config field at all, so there is no parse arm and no fuzz-seed classification to flip (`reference_fuzzer_count_docs_drift`: an `f.Add` seed is not a fuzzer).
- **stat surface:** **1205** — BEHAVIOR_CONTRACT doc count. **There is NO mechanical counting command** (the contract says so itself); enforcement is per-phase `TestNoNewStat*` guards asserting a delta of ZERO, never the absolute total. **Phase 75 LANDED +1 (1204 -> 1205)** — `listener.<normalized-addr>.ssl.no_certificate`, the FOURTH listener-scope `ssl.*` name and the FIRST **SUCCESS-PATH** handshake annotation (⚠️ **NOT a synonym for `ssl.fail_verify_no_cert`, and the mis-mapping is wrong in BOTH directions** — one annotates an ACCEPTED handshake, the other books a REJECTED one; they are mutually exclusive on every connection). ⚠️ **The delta is a NAME-surface delta, NOT a flat per-deployment one:** registration rides the block that ALREADY gates on `rt.tlsMode`, so a **plaintext-only** deployment gains **ZERO** names, while a **QUIC** listener gains it permanently zero — **PARITY** with the reference, and structural (`Manager.Start` launches no accept loop for `kindQUIC`, so the Inc site is unreachable for a **name-independent** reason). ⚠️ **QUIC was NOT re-probed at phase 75** — the parity claim is INHERITED from phase 74's family-level probe, and that inheritance is stated in BEHAVIOR_CONTRACT rather than presented as a measurement. Ledger updated: `1204` -> `1205` at `BEHAVIOR_CONTRACT.md:831`/`:847`, plus a `**Phase 75 - 1204 -> 1205 (+1)**` line after the tail entry — **the ONLY line-adding edit in the file** (`5744 -> 5746`), so every by-line citation into the TLS subsection stays valid. ⚠️ **The new entry RECORDS BOTH ledger gaps and BACK-FILLS NEITHER** (fabricating a line would be inventing a record). Gate figures at this close: `grep -c '1204'` => **2** · `grep -c '1205'` => **3** · `grep -cE '^#{2,4} .*ADR-[0-9]+.*ADR-[0-9]+'` => **17** (was 16 — the `:1849` heading joined them, on the `:785` precedent). ⚠️ **`internal/statssink`'s four *"stays 1200 / 1196"* prose sites, stale since phase 49, are RECORDED-NOT-FIXED** — that package is on this phase's byte-untouched sha256 roster.
- **BackendKind tail:** **38** — **Phase 76 landed +0** (`0061` uses the in-process `TCPEcho` branch and implements neither `BackendKindAware` nor `PerHostBackendKind`). NC observed: appending `ZZProbeKind = 39` ⇒ tail 39. — (`H2GoawayResponder`, `test/differential/fixture/fixture.go:614`). **Phase 75 landed +0** — `0110` was extended, not cloned, and it implements neither `BackendKindAware` nor `PerHostBackendKind`, so the runner takes the in-process `TCPEcho` branch. ⚠️ **This is a TAIL VALUE, not a count** — the file declares **39** constants (values 0-38, `TCPEcho = 0`). The lineage figure **38** is correct as written; do NOT 'fix' it to 39.
- **DECISIONS.md tail:** **ADR-0298** (next-free **ADR-0299**) — `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1` -> `## ADR-0298`; `grep -c '^## ADR-0299'` ⇒ **0**. **ADR-0298 is COMPLETE as of the phase-76 IMPL** — §Context drafted at the phase-76 SPEC, §Decision + §Consequences APPENDED IN PLACE after the RETAINED italic footer, **no renumber and NO `---` separator** (the file's last `^---$` is still at `:17020`). Block shape controller-re-derived at this close: **1** `### Context` / **1** `### Decision` / **1** `### Consequences` / **1** retained footer. ⚠️ **FOUR of its OWN §Context paragraphs were CORRECTED IN PLACE** (¶1's `:295` -> `:307`, ¶5's `-count ~= 81` -> **83**, ¶7's *"~71 collapses"* -> **74/2000**, ¶11's *"needs no edit"* -> REFUTED) on the ADR-0297 ¶7/¶9 precedent, each **leading with what survives**; none changes a decision the ADR records. ⚠️ **It carries NO whole-file grep count and claims NO family ordinal** — the Load-balancing family closed at row 54, and **maintenance rows do not extend a charter**. *(Historical, retained: **ADR-0297 is COMPLETE** — §Context drafted at the phase-75 SPEC (ADR-0044-**as-used**; ⚠️ ADR-0044 does NOT actually contain that discipline), §Decision + §Consequences appended IN PLACE at the phase-75 IMPL after the **RETAINED** footer, mirroring ADR-0295/0296. Block shape verified at this close: **1** `### Context` / **1** `### Decision` / **1** `### Consequences` / **1** retained footer; `grep -c '^## ADR-0298'` => **0**. **No renumber, no new ADR.** ⚠️ **Its OWN §Context ¶7 and ¶9 were CORRECTED IN PLACE while completing it** — ¶7's **self-falsifying whole-file grep** (the same species the phase-74 IMPL had just fixed in ADR-0296 ¶3, reproduced one ADR later) is restated with **NO number** and the searched token deliberately **NOT spelled**, because every restatement mints another counter-example; and ¶9's *"SELF vs OTHER-ADR"* form rule is replaced by **graft SCALE** after the n=7 population was re-enumerated first-hand. ⚠️ **¶9 ALSO said *"the only two instances in the file"* — which this row's own blockquote makes three** — so a pure append would have shipped a SECOND self-falsifying count in the commit that fixed the first. **The prescription has now outlived THREE wrong justifications: family, self-vs-other, scale.** ⚠️ **`ADR-0296` is NO LONGER byte-untouched** — its §Decision (g) carries the phase-75 correction as an INDENTED blockquote (`grep -c '^  > '` **2 -> 3**), **LEADING WITH WHAT SURVIVES** (no such counter was owed *in phase 74*, whose sole stat-asserting fixture is `require=true` on both sides — **the deferral was RIGHT**) and claiming **NO ORDINAL**.)*
- **go.mod modules:** **Phase 76 landed +0** — `go mod tidy -diff` EMPTY and `git diff master -- go.mod go.sum` EMPTY; ⚠️ **the row's TEST-side import delta is +8 and that is CORRECT** (`ringhash_test.go` gains `fmt`/`math`/`mathrand "math/rand/v2"`, `linkage_test.go` adds `go/ast`/`go/parser`/`go/token`/`strconv`/`testing`) — all stdlib, all test-side, and **an import LINE is not a go.mod MODULE**; there is no new sub-package, so `reference_new_subpackage_pulls_transitive_module` does not bite. NC observed: a fake `require` ⇒ 67 -> 68. The tracked lineage figure is **2** (the phase-61.2 count: `quic-go` direct + `qpack` indirect) — **NOT a repo total**; the single `go.mod` requires **67** (18 direct + 49 indirect). **Phase 75 landed +0** — `go mod tidy -diff` EMPTY and `git diff master -- go.mod go.sum` EMPTY. The row needed `crypto/tls` and `internal/stats`, both ALREADY imported by `internal/listener/manager.go`; **an import LINE is not a go.mod MODULE**, and there is no new sub-package, so `reference_new_subpackage_pulls_transitive_module` does not bite. ⚠️ **TEST-side imports grew by exactly four** (`log`, `math`, `net/http`, `strconv` in `0110`'s driver) — all stdlib, all test-side, **not a production-envelope violation**.
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` (see `ENVOY_TARGET.md`).
- **conformance:** h2spec **`95 tests, 94 passed, 1 skipped, 0 failed`** (the skip is 6.9.2/2, invariant) — the CORRECTED 95-case strict scope under dotted selectors, measured at the phase-85 IMPL close 2026-08-10 (ADR-0307; the historical 53/53 records measured only 53 of the 95 under nine silently no-op slash-form selectors, 2026-04-25 -> 2026-08-10); proxy-wasm all 10 families PASS.

---

## Recent lineage

*(A SHORT recent lineage, capped at FIVE prior entries by ADR-0288. Everything earlier is in `STATE_HISTORY.md`. §Current carries the newest stage — **the phase-97 BRAINSTORM**, which opened the row on a QUIC chain-selection inversion measured live on two shapes. The oldest entry below has been archived to make room; ⚠️ **the date read could not pick it alone — FOUR of the five pre-roll entries shared the oldest date, so the fallback was LIST POSITION, the tail** (method note 26), and this time position and date AGREED, which is a coincidence of this tip and not a rule. The eviction was verified with the LABEL-BOUND PAIR on BOTH files, plus a fabricated-label NC reading zero in both and a positive control on a sibling label that IS present in the archive; ⚠️ **the bare forms answer nothing — both read FIVE here and are invariant under which entry is evicted.**)*

- **prior active-phase:** `phase 96 (listener-default-chain-tlsmode) IMPL done` (2026-09-08) — lifecycle-state **3 -> DONE**. **ROW 96 FLIPPED `in-progress` -> `done`** (a FLIP, not an ADD: `want` STAYS **128** and `ROADMAP.md` STAYS **246** lines). All **19** `PLAN.md` §5 tasks landed IN ORDER, and the ordering was load-bearing: Tasks 1-5 landed and RAN at the UN-FIXED TIP — both crash pins aborting the binary with the anchored gate `^panic:|DATA RACE|SIGSEGV` reading **2** and the trace naming the SUCCESS-path `rt.sslHandshake.Inc()`, with the POINTER assertions observed to fire BEFORE the abort — because that tip IS the negative control for NC roster rows 1-3 and Task 6 consumes it. **THE ROW IS ONE PRODUCTION LINE** in `internal/listener/manager.go`, `+1 / -1`: the sole `tlsMode:` write now covers the `default_filter_chain` slot as well as the `filter_chains[]` accumulator, closing a remotely-triggerable process SIGSEGV and a QUIC stat-registration divergence together. Shape (B), nil-guarding the five Inc sites, was REJECTED as AFFIRMATIVELY WRONG rather than merely larger — the reference emits the SAME listener-scope `ssl.*` name set for a default-chain-only TLS listener, so a nil-guard trades a crash for a permanent silent observability divergence. **`0121-listener-default-chain-tls`** lands at reference port **10127** (fixtures 122 -> **123**), the FIRST fixture in this tree whose listener carries NO `filter_chains[]` at all. **ADR-0318 COMPLETED IN PLACE** (§Decision + §Consequences after the RETAINED italic footer; STATUS `PROPOSED` -> `ACCEPTED`; **the house guard is DISARMED**; no renumber, no `---`). **SIX CLAIMS REFUTED BY EXECUTION** — see `PROGRESS.md` §5 and §9.

- **prior active-phase:** `phase 96 (listener-default-chain-tlsmode) PLAN done` (2026-09-08) — lifecycle-state **2 -> 3**. `PLAN.md` created (**1666** lines), a **19-task** TDD spine ordered so Tasks 1-5 land and RUN at the UN-FIXED TIP because that tip IS the negative control for NC roster rows 1-3. The BOOTSTRAP §6.1 split gate was EVALUATED AND NOT TRIPPED; ⚠️ **its first and heaviest ground was later REFUTED at the IMPL by measurement** — the row came in at **+1725** on the accounting that compared it to `0a985a35`'s **+1635**, i.e. LARGER, not smaller ([[reference_measured_prototype_is_a_lower_bound]], fifteenth consecutive row). **FIFTEEN claims refuted by execution**, five of them inside the two `SPEC.md` sections the PLAN was told to trust verbatim: §10 shipped **EIGHT false figures at its own publishing commit**, the false-invariant occurrence set was **TEN and not nine** (`ROADMAP.md:136`, which the SPEC's own exclusion rule structurally could not reach), `TestListenerMetrics_GateMatchesInc` has NO table and NO shared assertion body, arm (f) is RED regardless of the fix unless it calls `mgr.Start(ctx)`, and the byte-untouched and edit rosters INTERSECT at `manager.go` alone where BOTH obvious guards fail against correct code. `0121`'s expectation map was MEASURED on both sides at N=1 and N=3; **drive count N = 3**, because 1 cannot discriminate a per-connection counter from a fire-once one.

- **prior active-phase:** `phase 96 (listener-default-chain-tlsmode) SPEC done` (2026-09-07) — lifecycle-state **1 -> 2**. `SPEC.md` created, discharging all NINE owed items of `BRAINSTORM.md` §10 and **REFUTING TEN** claims by execution. **THE BLOCKING UNKNOWN IS ANSWERED:** the pinned reference DOES emit the listener-scope `ssl.*` family for a listener whose only TLS is its `default_filter_chain`, and the name set is IDENTICAL to the set it emits when TLS lives in `filter_chains[0]` — measured on BOTH crashing shapes with a plaintext negative control reading ZERO, twice independently (a measurement agent, then the controller from its own configs on a disjoint port band). ⇒ **FIX SHAPE (A), widening `tlsMode`, IS THE PARITY-CORRECT REPAIR; shape (B), nil-guarding the Inc sites, is AFFIRMATIVELY WRONG** — it would leave a TLS-serving listener reporting ZERO `ssl.*` names. **ROW 96 STAYS `in-progress`**; `ROADMAP.md` and `BEHAVIOR_CONTRACT.md` are **BYTE-UNTOUCHED**, a scope MEASURED across FOUR consecutive SPEC commits (`13ad0aa0` `975e527e` `307f2e3d` `9ed6a620`) rather than inferred from wording.

- **prior active-phase:** `phase 96 (listener-default-chain-tlsmode) BRAINSTORM done` (2026-09-07) — lifecycle-state **DONE -> 1**. **PHASE 96 OPENED BY SELF-PICK** under the 2026-07-12 standing directive, with no banked mid-lifecycle work to advance first. **ROW 96 ADDED `in-progress`** — an **ADD, not a flip**, so the denominator MOVED: `want` **127 -> 128**, `ROADMAP.md` **245 -> 246**, row 96 at file line **158**. **THE SUBJECT: a TLS `default_filter_chain` on a listener whose `filter_chains[]` are plaintext or absent leaves `tlsMode` FALSE, so the five `ssl.*` counters are never registered, while the per-connection Inc guard is a DIFFERENT predicate that is TRUE — the first completed downstream TLS handshake SIGSEGVs the PROCESS, remotely, with no `recover()` on the path.** REPRODUCED BY EXECUTION on TWO distinct shapes, so the `len(chains) == 0` guard is NOT the boundary. **ROOT CAUSE: `anyTLS` was correct as a mixed-TLS cross-check accumulator and phase 74 REUSED it as the stat-registration gate, inheriting an invariant ADR-0080 §Decision 3 had already repealed at phase 07.2.** ⚠️ **THE ROUTER NAMED THE RIGHT FILE AND THE WRONG MEMBER** — enumerating the whole divergence set first found TEN divergences of which EIGHT are DELIBERATE, and only TWO unexplained. **THIRTEEN claims REFUTED by execution**, three of them in documents the router told the session to trust, including the router itself: row 95 asserts `internal/tls/config.go` landed at 642 while the file measures **666** at the very commit that wrote the sentence, and the router's `BEHAVIOR_CONTRACT.md:1944` figure is a BYTE count where its published sibling is a CHAR count.

- **prior active-phase:** `phase 95 (tls-alpn-mismatch-fallback) IMPL done` (2026-09-07) — lifecycle-state **3 -> DONE**. **PHASE 95 CLOSED; ROW 95 FLIPPED `in-progress` -> `done`** (a FLIP, not an ADD: `want` STAYED **127** and `ROADMAP.md` STAYED **245**). An ALPN-mismatching downstream client is now SERVED instead of aborted, via a TCP-only `GetConfigForClient` at the ENTRY of `NewDownstreamConfig` returning a per-handshake `Clone()` with `NextProtos = nil`. **ITS OWN FINAL WHOLE-BRANCH REVIEW REFUTED ITS CENTRAL SAFETY CLAIM, AND THE SCOPED RE-REVIEW OF THAT FIX THEN REFUTED THE FIX'S CLAIM** — F1: *"TCP-only, never in `NewQUICDownstreamConfig`"* was TRUE OF THE SYMBOL and FALSE OF THE PATH, because `manager.go` builds downstream TLS at TWO sites and only the `filter_chains[]` loop branched on `kind`; F-A: the mirroring fix also WIDENS acceptance, which *"pure config parity"* omitted. ⚠️ **TWO OF ITS FIGURES ARE FALSE AT ITS OWN PUBLISHING TIP AND WERE CAUGHT AT THE PHASE-96 BRAINSTORM** — its row asserts `internal/tls/config.go` landed at 642 while the file measures **666** at `0f3b98b3`, the very commit that wrote the sentence; and its `BEHAVIOR_CONTRACT.md:1944` figure disagrees with the router's, the measured value being **1841 bytes / 1830 chars** with the published sibling `:1971` figure being a CHAR count where this one is BYTES.




## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
