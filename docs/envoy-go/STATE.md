# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `23-http-filter-admission-control` **(BRAINSTORM done at this commit; lifecycle-state 1 → awaiting SPEC)**. Phase `22-http-filter-lua` parent + all three sub-phases (22.1 + 22.2 + 22.3) DONE and CANONICAL at predecessor master tip `032510a` — DO NOT reopen. Phase 23 is a NEW top-level §9 family-row (`envoy.filters.http.admission_control`), the SIXTEENTH §9 HTTP-filters-family row, opened at this commit via `superpowers:brainstorming`. **BRAINSTORM done at 2026-05-21** — a 2-question user dialogue settled the two consequential decisions (Q1 MVP envelope: FULL operator surface minus RTDS; Q2 probabilistic-rejection seam: inline `Rand` seam + deterministic-regime differential fixtures) plus 5 precedent-settled defaults (inline `Clock` seam; REUSE-by-absence per-route; byte-exact 3-counter stat surface; single-phase landing; subject-only deterministic-regime + two-directory differential taxonomy). BRAINSTORM.md authored at `docs/envoy-go/phases/23-http-filter-admission-control/BRAINSTORM.md`.
- **phase-directory:** `docs/envoy-go/phases/23-http-filter-admission-control/` (BRAINSTORM.md authored at this commit). SPEC.md + PLAN.md + PROGRESS.md + REVIEW.md to follow in later lifecycle sessions per the phase-09..22 single-filter precedent.
- **lifecycle-state:** `phase 23 BRAINSTORM done; awaiting SPEC` (SKILL_ROUTING state 1 → 2). ROADMAP row `23` added at this commit with status `in-progress` (the §9 heading expands into a flat top-level row when the phase enters in-progress, per ADR-0106 + the ROADMAP §Invariants). The §9 HTTP-filters family stands at 3 remaining rows (`wasm`, `admission_control`, `global rate limit`); it closes to **2 remaining (`wasm`, `global rate limit`) once phase-23 IMPL lands** — at BRAINSTORM the row is in-progress, not yet done.
- **next-skill:** `superpowers:writing-plans` **scoped to SPEC authoring** for phase 23 (per SKILL_ROUTING state 1 → 2 + the phase-09..22 precedent where the post-BRAINSTORM session authors `SPEC.md`). The SPEC session creates a fresh worktree per project memory `feedback_git_worktrees.md` (branch `phase-23-http-filter-admission-control-spec` off the post-brainstorm master tip) and executes the BRAINSTORM §10 empirical-pin obligations IN-SESSION against reference Envoy v1.37.2 per ADR-0004: **D1** stat-roster (`rq_rejected`/`rq_success`/`rq_error`) + `http.<HCM_stat_prefix>.admission_control.<stat>` prefix-template pin; **D2** forced-reject cross-side promotion (P_reject clamp-to-1.0 RNG-independence); **D3** `admission_control.cc` `P_reject` formula + clamp/gate-ordering line-citation depth; **D4** 503 reject local-reply body/header byte-pin. Cold-start scope for the SPEC session: read STATE.md (this file) + the phase-23 BRAINSTORM.md + `docs/envoy-go/ROADMAP.md` row 23 + the phase-21 SPEC.md (nearest single-filter algorithmic precedent) + DECISIONS.md tail (next-free ADR-0194; ADR-0044 §Context-draft discipline for the anticipated ADR-0194 + ADR-0195) + BEHAVIOR_CONTRACT.md (the per-filter departure-record convention; phase 23 anticipates a NEW `### envoy.filters.http.admission_control` subsection with the RTDS-departure record). **Subagent-driven execution (project memory `feedback_execution_style.md`) applies at PLAN/IMPL, not at the SPEC-authoring or BRAINSTORM stages.**
- **last-commit:** `3040a6b`. Phase 23 BRAINSTORM squash-merge of branch `phase-23-http-filter-admission-control-brainstorm` from worktree `.worktrees/phase-23-http-filter-admission-control-brainstorm/` landed as squash-merge commit `3040a6b` on master per the phase-09..22 BRAINSTORM squash-merge convention; this placeholder was backfilled by the SHA-fill follow-up commit per convention. Predecessor master tip: `032510a` — `phase 22.3 IMPL follow-up: STATE.md SHA-fill (TBD → c999312 post-squash)`.
- **last-updated:** 2026-05-21
- **next-free ADR:** `ADR-0194` — UNCHANGED at this BRAINSTORM commit (BRAINSTORM authors no ADR bodies per ADR-0004/ADR-0044; §Context drafts + §Decision/§Consequences bodies land at SPEC/IMPL). Phase 23 ANTICIPATES consuming **ADR-0194** (admission-control algorithm + package shape + inline `Clock`/`Rand` seams + deterministic-regime differential strategy) + **ADR-0195** (RTDS `runtime_key` deferral PARSE-REJECT); next-free advances 0194 → 0196 at IMPL. **ADR-0196 hypothesized UNCONSUMED at phase-23 phase-done** (D11-style HOLD-with-known-risk: a surprise upstream stat name / a 4th counter / a clamp-boundary float edge could force consumption). ADR-0193 full body + ADR-0125 §(xiv) amended remain the DECISIONS.md tail UNCHANGED at this commit.

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
