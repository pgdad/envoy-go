# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `<unset — next session resolves>` — phase 12 done at this commit. ROADMAP row 12 status flipped `in-progress → done` per BOOTSTRAP_PROMPT.md §4.1 invariant 3. Phase 12 was the FIFTH §9 family-row to land (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11); the §9 family heading at ROADMAP line 56 stays unchanged per ADR-0106. The next §9 family-child (compression / jwt_authn / rbac / ext_authz / ext_proc / oauth2 / buffer / lua / wasm / adaptive_concurrency / admission_control / bandwidth_limit — csrf is now landed and is no longer a candidate) is the brainstormer's choice.
- **phase-directory:** `docs/envoy-go/phases/12-http-filter-csrf/` — contains `BRAINSTORM.md` (499 lines) + `SPEC.md` (1597 lines) + `PLAN.md` (~2475 lines) + `PROGRESS.md` (extensive narrative log spanning Tasks 1-12) + `REVIEW.md` (lands at Task 13 commit). Phase 12 produced ~280 LoC production code in `internal/filter/http/csrf/` (filter impl: New factory + DecodeHeaders + filterStats + per-route helpers in single-file `csrf.go` per planner-time decision PLAN-6) + ~450 LoC unit tests covering all 6 SPEC §14.1 test groups + 40 LoC fuzzer (`FuzzCsrfPolicyConfigParse` — sixteenth fuzzer overall) + ~870 LoC fixture (envoy.yaml + envoy-go.yaml + backend + driver + expectations + README) + 5 ADRs (ADR-0120..ADR-0124) + an 8-line load-bearing HCM deviation (`:authority` injection in `internal/filter/hcm/connection.go` H1 path + `internal/filter/hcm/h2dispatch.go` H2 path; mirrors existing `:method` injection from Phase-07.1 Task 18; PLAN line 2189 anticipated this failure mode (a) — see PROGRESS.md Task 11 + Task 12 entries for the full retrospective). All 12 implementation tasks completed (Task 13 REVIEW.md follows at Task 13 commit). NO new framework primitive (thinnest §9 family-row to date — no FactoryCtx field, no HTTPRegistry method, no PerRouteConfig accessor, no ADR-0073 amendment, no SN flattening rule, no new tag-extractor pattern).
- **lifecycle-state:** `awaiting next planning` — phase 12 fully closed; six gates green at this commit (build/vet/lint clean; race tests pass — all packages, 0 fails; h2spec 53/53 PASS unchanged at the ADR-0051 pin; 16 fuzzers green at 30s budget — `FuzzCsrfPolicyConfigParse` re-validated at 2.2M execs / 30s clean alongside the 15 pre-existing fuzzers; all 14 differential fixtures 0000-0014 PASS with 0014-http-csrf green on first attempt; BEHAVIOR_CONTRACT.md populated with §13.1 NEW `### envoy.filters.http.csrf` subsection + §13.2 26→29 stat-table extension + §13.3 equivalence-matrix new row + §13.4 phase-12 forward-pointer notes).
- **next-skill:** `superpowers:brainstorming` per ADR-0106 (next §9 family-child cold-starts from the §9 heading + just-shipped phase-12 artefacts; no sibling-stub was authored).
- **next-skill-scope:** Cold-start scope for the next §9 family-child brainstorm session. Read first: `docs/envoy-go/ROADMAP.md` row 12 (now `done`) + the §9 family list at line 58 — csrf is now landed; remaining candidates include compression, jwt_authn, rbac, ext_authz, ext_proc, oauth2, buffer, lua, wasm, adaptive_concurrency, admission_control, bandwidth_limit; `docs/envoy-go/BEHAVIOR_CONTRACT.md ## HTTP filter chain ### envoy.filters.http.csrf` subsection (the just-shipped contract; line 1093) + `## Forward-pointer notes ### Phase 12 forward-pointer notes` (the deferred-field roadmap); `docs/envoy-go/DECISIONS.md` ADR-0120..ADR-0124 (the 5 phase-12 ADRs); `docs/envoy-go/phases/12-http-filter-csrf/REVIEW.md` (lands at Task 13 commit). Per ADR-0106 + the user's persistent preference recorded in `~/.claude/projects/-home-esa-git-envoy-go/memory/MEMORY.md`: the next family-child is the brainstormer's choice from the §9 family list. Phase 12's per-route stats SHARED with listener-level pattern (codified at ADR-0124) is the canonical reference for any future data-only per-route filter with HCM-scoped stats; phase 11's INDEPENDENT-stats pattern (ADR-0117) remains the canonical reference for stateful per-route filters. Phase 12's Task-11 HCM `:authority` injection deviation (`internal/filter/hcm/connection.go` + `internal/filter/hcm/h2dispatch.go`) is a load-bearing carry-forward — any future HTTP filter that consumes `:authority` from `req.Header` benefits from this fix; reverting the change would re-introduce the empty-target-origin bug that fixture 0014 surfaced. PLAN line 2189 anticipated this failure mode (a); REVIEW.md (Task 13) lands the formal retrospective.
- **last-commit:** `TBD` — `phase 12: http-filter-csrf [ADR-0120, ADR-0121, ADR-0122, ADR-0123, ADR-0124]`. Lands envoy.filters.http.csrf under the 07.1 framework as the FIFTH §9 family-row (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11). All six phase-done gates green. Framework deltas: NONE (thinnest §9 family-row to date — no FactoryCtx field, no HTTPRegistry method, no PerRouteConfig accessor, no ADR-0073 amendment, no SN flattening rule, no new tag-extractor pattern). Production-code carry-forward: 8-line `:authority` HCM injection at `internal/filter/hcm/connection.go` + `internal/filter/hcm/h2dispatch.go` (Task 11 deviation; mirrors existing `:method` injection; PLAN line 2189 anticipated). Differential fixture 0014-http-csrf green (6 scenarios: same-origin allowed / cross-origin rejected / additional_origins host:port match / missing source-origin rejected / Referer fallback / per-route wholesale-override with shared stats; GET passthrough unit-only). Stats: 3 new counters per HCM stat_prefix (26→29 stat-table extension); reuses existing `envoy_http_conn_manager_prefix` Prometheus tag-extraction (Rule SN2). SHA-fill follow-up commit per phase-04..11 SHA-fill convention.
- **last-updated:** 2026-05-08

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
