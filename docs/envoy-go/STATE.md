# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

> **This file is a POINTER, not an archive** — `BOOTSTRAP_PROMPT.md` §4: *"STATE.md — pointer: active phase, last commit, next action."* It carries the live pointer, the live counts, and a SHORT recent lineage. **Full stage-by-stage history lives in `STATE_HISTORY.md`** (archived at ADR-0288; **nothing was deleted** — the pre-move file is preserved there verbatim, byte-for-byte).
>
> **⚠️ THE RULE ADR-0288 EXISTS TO ENFORCE: there is EXACTLY ONE live `next-skill:`, `lifecycle-state:`, `next-free ADR:`, and count block IN THIS FILE, and they live in §Current pointer directly below.** Before that move, ~330 lines of prepended backlog had buried TWO fossilised "current state" blocks whose `next-skill:` still read *"the 42.2 hedging BRAINSTORM"* and *"the 46.2 SPEC"* while the live phase was **66** — so `grep 'next-skill:' STATE.md` returned **two stale hits and ZERO live ones**, and `grep 'fuzzers:'` returned **48** and **42** when the true count was **55**. **A session may trust a grep of this file again. Keep it that way: when you advance the pointer, EDIT §Current pointer IN PLACE — never prepend a new block above it.**

---

## Current pointer

*(The ONLY live "what next" in this repo. Update IN PLACE at every stage close.)*

- **active-phase:** `phase 81 (stats-name-charset-guards) IMPL done` (2026-07-31) — lifecycle-state **3 -> DONE**; **ROW 81 FLIPPED `in-progress` -> `done`; THE PHASE IS CLOSED.** Sentinel `want` STAYS **113**. Base master **`2a910ef2`**, taken from `git rev-parse master` at session start and not from any SHA quoted in the router; branch `phase-81-impl`. **FIVE implementation agents on disjoint package sets**, each in its own worktree off that base, committing locally only; the controller re-derived every load-bearing claim itself and gated every integration on `git apply --check` first. **All five reported `git status --porcelain` = 0 lines**, controller-re-confirmed; zero pushes, zero stray branches, zero docker containers created. ⚠️ **THE ROW'S CENTRAL DECISION WAS RE-DERIVED INDEPENDENTLY AND HELD EXACTLY:** the controller re-ran the segment-position cross-product inside `internal/stats` before dispatching anything — **LEADING disagrees on 1 of 9, INTERIOR on 5 of 9**, interior set exactly `0policy` `policy.` `.policy` `9` `""` — reproducing the PLAN to the token, with every dependent value confirmed in the same run (`a..b` true, `compressor..gzip.total` true, `tenant-foo`/`my-plugin`/`guest-side-config` false, mongo assembled `1abc` true against bare false). **All nine guards therefore probe the ASSEMBLED name.** ⚠️ **WHAT DID NOT SURVIVE IS THE PLAN'S ACCOUNT OF *WHY* SEVERAL CONSTRAINTS HOLD — the conclusions all stand, five of their stated reasons do not**, each caught by executing the counterfactual: relocating guard C into `newFilterStats` does **NOT** redden `my-plugin` (that site never calls it — the VACUOUS relocation), and the struct-literal roster is **FIVE** sites not two, the two omitted ones being exactly what decides the question; the prescribed compressor break arm is a **VACUOUS BREAK MODE** that cannot fire and was substituted; **D-81-EMPTY is decorative at every INTERIOR source** (proved three ways) **while at zookeeper the pre-existing required-check ORDER is genuinely load-bearing**, since an empty prefix there assembles to a LEADING dot; and F2's log-capture shape is **NOT** precedent-free (`internal/stats/promskip_test.go` is an exact one) while the mongo codec tests cited as its precedent carry **no log capture at all**. ⚠️ **THE ATOMICITY ARGUMENT BECAME AN OBSERVATION:** with F1 gutted, the per-route test did not panic — it failed on the LOG assertion, having captured **F2 catching exactly what F1 dropped**. Break arms produced **real un-recovered process panics on THREE independent paths** (rbac, with the DOUBLED `rbac` segment confirmed; wasm per-route dispatch; compressor `New`). ⚠️ **COST: REALIZED 2229 net `.go` against a PLAN budget of ~850 — 3.07x, CROSSING BOOTSTRAP §6.1's ~1500 trigger. RECORDED AS A FINDING, NOT RETROACTIVELY SPLIT** (§6.1 gates the PLAN's written estimate, which tripped neither threshold); the lineage's **SECOND** realized crossing and the **FIRST** where the PLAN's estimate was itself measured-basis — nine prototypes built and run — so **a measured prototype is a LOWER BOUND on the shipped artifact, not an estimate of it**. TWELVE tasks enumerated, TWELVE executed; the §6.1 third trigger did NOT fire. **Gates: full differential 120/120 in 402 s with `INNER_EXIT=0` and `comm -3` EMPTY** (⚠️ **on the THIRD launch — two prior runs ABORTED on the driver-owned receiver port race, at `0085` and `0083`, while the harness notification reported success against `INNER_EXIT=1`, the third consecutive instance of that lie**); h2spec **106 passed / 0 failed / 0 skipped** with the extractor negative-controlled; `go test ./cmd/envoy-go/` ok; `-race` ok on all nine packages; `gofmt -l` EMPTY output; `go vet` 0; `golangci-lint` 0 **with a FIRING injected-misspelling control on the merged tree**; `go.mod`/`go.sum` 0 bytes; **stat surface +0 by CALL-SITE ENUMERATION with its INPUT MEASURE asserted** (476-line production diff, matcher NC'd) at an unmoved **208 code sites / 36 production files**. File deltas: `DECISIONS.md` **17766 -> 17796** (ADR-0303 PROPOSED -> COMPLETE), `BEHAVIOR_CONTRACT.md` **5868 -> 5870**, `STATE_HISTORY.md` **436 -> 438**, `ROADMAP.md` 229 lines UNCHANGED in length.
- **phase-directory:** `docs/envoy-go/phases/81-stats-name-charset-guards/` (`BRAINSTORM.md` + `SPEC.md` + `PLAN.md` + `PROGRESS.md`). Prior: `docs/envoy-go/phases/80-stats-sds-projection/`
- **lifecycle-state:** **`DONE`** (phase 81 closed; **row 81 `done`**). ⚠️ **Sentinel re-run MECHANICALLY AFTER the flip: it does NOT fire and `stop` was NOT created** (`ls stop` => `No such file or directory`). (1) at `want=113` is now **SILENT** — ⚠️ **the one stage where silence is indistinguishable from a broken check, so the doctored-copy NC was MANDATORY and was run: row 62 flipped `in-progress` on a scratch copy => `NOT DONE: row 62`, with the NC VERIFIED TO HAVE LANDED by printing the doctored status field before trusting the result** (the precise failure the PLAN hit when its `sed` missed the real column spacing); denominator NC at `want=112` => `GATE FAIL: examined 113 data rows, expected 112`. (2) **FIVE**, UNCHANGED at `:191 :201 :211 :217 :225` — ⚠️ **THIS ROW NARROWS NOTHING, STATED AND NOT FORECAST; the THIRTY-FIRST consecutive phase at which it did not go down.** (3) `NEVER OPENED: gRPC`, `NEVER OPENED: WASM`, NC on an invented slug fires. Checks (2) and (3) both print, so the sentinel does not fire. Input measured **229 lines / 113 data rows**. ⚠️ **THE LEAK CHECK WAS ARMED HERE AND PASSED** — row 81's cell alone carries **0** matcher phrases and prints only the already-registered `Observability-family row`, with the extractor negative-controlled by injecting a second slug into a scratch copy. Row well-formedness unchanged: **NF=8**, flagged by neither arm of the disjunction gate.
- **next-skill:** **a phase-82 BRAINSTORM.** ⚠️ **SELF-PICK per the 2026-07-12 standing directive** — smallest defensible candidate first, severity as tie-break, recording the pick and the rejected alternatives with costs re-derived at that tip. There is **no banked mid-lifecycle work**: phase 81 is closed and every chartered row is `done`. The named candidates this row leaves are listed under §Deferred in `PROGRESS.md`; the strongest by severity is the **interior empty-segment hole** (slug `stats-name-empty-segment-guards`) — ⚠️ **but it is BLOCKED on ADR-0132 §Decision (v)** and the successor must carve out the compressor or supersede that ADR, so a pick must dispose that first. ⚠️ **A cheaper and wholly unblocked candidate exists and is currently untracked anywhere: the driver-owned receiver port race**, which aborts the whole differential binary and cost THIS session two full launches.
- **last-commit:** the phase-81 IMPL squash. ⚠️ **LOCATE BY SUBJECT, NEVER BY POSITION**, and ⚠️ **the bare `git log --grep 'phase 81'` is CONTAMINATED** because router commit BODIES discuss the phase. **Use `git log --grep '^phase 81 (stats-name-charset-guards)' --format='%h %s'`**, which returns **4** at this tip (IMPL, PLAN, SPEC, BRAINSTORM); take the NEWEST (`| head -1`) and verify the count. Base master `2a910ef2`.
- **last-updated:** 2026-07-31 (phase-81 IMPL done; **PHASE CLOSED**). ⚠️ **THE LINEAGE EVICTION RULE WAS HONORED:** §Recent was at its five-entry cap, so **`phase 80 (stats-sds-projection) SPEC done`** — **RE-DERIVED as OLDEST at this tip, not inherited** — was moved **VERBATIM** into `STATE_HISTORY.md` (**436 -> 438**), its absence verified BEFORE the move (**0** hits) with **FIRING NCs on three phase-79 siblings** (**1** each), the migrated body confirmed **byte-identical** afterwards by `sha256sum`, and ⚠️ **the identity check ITSELF negative-controlled** — a deliberately doctored copy correctly mismatched. ⚠️ **The §Recent intro prose was ROLLED WITH THE LIST.** ⚠️ **The archive-gap total remains NOT REPRODUCIBLE, so no number is carried**; and ⚠️ **the tolerant-anchor bullet count is 168, not the PLAN's 167** — that figure was taken at 434 lines, before the PLAN's own append.
- **next-free ADR:** **`ADR-0304`** — `DECISIONS.md` tail is **ADR-0303**, STATUS **COMPLETE as of this IMPL**: §Decision + §Consequences **APPENDED IN PLACE after the RETAINED italic footer, with the STATUS word flipped in the SAME commit**, no renumber and **no `---` separator**. Invariants re-derived at this close: **302** `^## ADR-` headings, ids **0001-0303** with exactly one gap at **ADR-0209** and zero duplicates; `grep -c '^## ADR-0304'` => **0** (NC: `^## ADR-0303` => **1**); STATUS census **15 COMPLETE + 1 PROPOSED -> 16 COMPLETE**; retained italic footers **NINE** (`ADR-0294..0300, 0302, 0303`; **ADR-0301 carries none**); `^---$` **216** and unchanged. ⚠️ **The recurrence guard — *a block whose STATUS reads `PROPOSED` must carry no `### Decision` heading* — is SILENT on the landed tree AND FIRES on a doctored copy of the POST-flip state, so its silence is meaningful.** ⚠️ **The block carries NO whole-file grep count**, avoiding the ADR-0296/0302 self-falsifying species by enumerating per site. The id<->phase map holds: ADR-NNNN -> phase NNNN - 222, so **ADR-0303 -> phase 81**.

---

## Project counts at this commit

*(Live. Re-verify at session start — `git fetch` first — and update here at every stage close. Canonical commands inline.)*

⚠️ **ALL counts below were re-run MECHANICALLY in the stage worktree at this phase-76 IMPL close with the canonical commands, EACH WITH ITS NEGATIVE CONTROL OBSERVED — never copied forward.** **Phase 76 landed +0 on EVERY axis, including +0 PRODUCTION `.go` FILES** — the first chartered row in this lineage whose production-code delta is exactly nothing, and the ANTICIPATED envelope held on all of them. The row's `.go` delta is **three TEST-side files** (`internal/cluster/ringhash_test.go` edited, `test/fixtures/0061-lb-ring-hash/driver/driver_test.go` edited, `.../linkage_test.go` NEW), plus three fixture-doc files and four project docs. ⚠️ **The stat-surface total 1205 is DOC-SOURCED and was NOT re-derived mechanically** — there is no mechanical command, and the chain is known to be discontinuous in **TWO** places (the recorded `1200 -> 1201`, and `Phase 46.1b` closing at 1198 while `Phase 47.1` opens at 1200 — an unattributed +2). **Only the +0 DELTA is asserted, and that argument is STRUCTURAL rather than a `statssink` result:** zero production `.go` bytes changed (G8) and zero byte mismatches over the 375-file production roster (G14), so no `NewCounter`/`NewGauge` call site can have moved; the five `TestNoNewStat*` guards corroborate at 5x PASS. ⚠️ **A future phase should COUNT the surface rather than trust the chain — a +0 row is a cheap place to do it, and phase 76 declined to charter that, so it is now a NAMED maintenance deferral rather than an idle observation.**

- **active differential fixtures:** **119** — ⚠️ **Phase 76 landed +0: `0061-lb-ring-hash` was EDITED, not added** (`sourceIPs` 4 -> 16 plus a NEW `driver/linkage_test.go` inside the EXISTING fixture directory — no new YAML, no new directory, no new BackendKind, no new port), so the count STAYS **119** and `0118` remains FREE. NC observed: `mkdir .../0118-fake` ⇒ 120. — `ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0117-tracing-custom-tags-metadata-cluster` (phase 73). **Phase 75 landed +0**: `0110-tls-require-client-cert-false` was **EXTENDED** with a cross-side `fixture.StatsAsserter` (NO new YAML, NO new directory, NO new BackendKind, NO new port), so the count STAYS **119** and `0118` remains FREE. ⚠️ **Next-free REFERENCE port is `10450`, NOT `10118`** — ports are NOT fixture-index aligned. **The fixture now asserts what its own README confessed was infeasible, and BOTH sides were EXECUTED**: `ssl.handshake=2 ssl.no_certificate=1 ssl.fail_verify_error=1 ssl.fail_verify_no_cert=0`, `downstream_cx_total=3`, **identical on reference and subject**, with the discriminating asymmetry (`handshake=2` vs `no_certificate=1`) observed per side. Shape A over `/stats/prometheus`, keyed on the metric NAME with `envoy_listener_address` IGNORED — the NAMES are byte-identical cross-side while the labels differ (`0.0.0.0_10446` reference vs **`___20016`** subject: ⚠️ **the subject binds the IPv6 WILDCARD**). ⚠️ **`0111` was NOT given a value assertion, deliberately** — it runs `require_client_certificate: true` on both sides, so a success-path no-cert annotation reads **0 on every arm, structurally**, and a value pin would document a **vacuous `0 == 0`** as coverage; its PROSE gained the name in its NAMED-UNASSERTED list instead, because **both `0111` passages are CLOSED ENUMERATIONS** and a name in neither list reads as ASSERTED. **`0111`'s driver is byte-untouched, and its named-subset check was EXECUTED for the first time at this IMPL: PASS.** ⚠️ `var _ fixture.StatsAsserter` is a **TRIPWIRE, not the dispatch mechanism** — `0110` is only the **THIRD** fixture repo-wide to carry it, while **82** define an `AssertStats` method.
- **fuzzers:** **55** — **Phase 76 landed +0** (it consumes no new config field, so there is no parse arm to fuzz). NC observed: appending `func FuzzProbeOnly` ⇒ 56. — `grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`. **Phase 75 landed +0** — the row consumes no new config field at all, so there is no parse arm and no fuzz-seed classification to flip (`reference_fuzzer_count_docs_drift`: an `f.Add` seed is not a fuzzer).
- **stat surface:** **1205** — BEHAVIOR_CONTRACT doc count. **There is NO mechanical counting command** (the contract says so itself); enforcement is per-phase `TestNoNewStat*` guards asserting a delta of ZERO, never the absolute total. **Phase 75 LANDED +1 (1204 -> 1205)** — `listener.<normalized-addr>.ssl.no_certificate`, the FOURTH listener-scope `ssl.*` name and the FIRST **SUCCESS-PATH** handshake annotation (⚠️ **NOT a synonym for `ssl.fail_verify_no_cert`, and the mis-mapping is wrong in BOTH directions** — one annotates an ACCEPTED handshake, the other books a REJECTED one; they are mutually exclusive on every connection). ⚠️ **The delta is a NAME-surface delta, NOT a flat per-deployment one:** registration rides the block that ALREADY gates on `rt.tlsMode`, so a **plaintext-only** deployment gains **ZERO** names, while a **QUIC** listener gains it permanently zero — **PARITY** with the reference, and structural (`Manager.Start` launches no accept loop for `kindQUIC`, so the Inc site is unreachable for a **name-independent** reason). ⚠️ **QUIC was NOT re-probed at phase 75** — the parity claim is INHERITED from phase 74's family-level probe, and that inheritance is stated in BEHAVIOR_CONTRACT rather than presented as a measurement. Ledger updated: `1204` -> `1205` at `BEHAVIOR_CONTRACT.md:831`/`:847`, plus a `**Phase 75 - 1204 -> 1205 (+1)**` line after the tail entry — **the ONLY line-adding edit in the file** (`5744 -> 5746`), so every by-line citation into the TLS subsection stays valid. ⚠️ **The new entry RECORDS BOTH ledger gaps and BACK-FILLS NEITHER** (fabricating a line would be inventing a record). Gate figures at this close: `grep -c '1204'` => **2** · `grep -c '1205'` => **3** · `grep -cE '^#{2,4} .*ADR-[0-9]+.*ADR-[0-9]+'` => **17** (was 16 — the `:1849` heading joined them, on the `:785` precedent). ⚠️ **`internal/statssink`'s four *"stays 1200 / 1196"* prose sites, stale since phase 49, are RECORDED-NOT-FIXED** — that package is on this phase's byte-untouched sha256 roster.
- **BackendKind tail:** **38** — **Phase 76 landed +0** (`0061` uses the in-process `TCPEcho` branch and implements neither `BackendKindAware` nor `PerHostBackendKind`). NC observed: appending `ZZProbeKind = 39` ⇒ tail 39. — (`H2GoawayResponder`, `test/differential/fixture/fixture.go:614`). **Phase 75 landed +0** — `0110` was extended, not cloned, and it implements neither `BackendKindAware` nor `PerHostBackendKind`, so the runner takes the in-process `TCPEcho` branch. ⚠️ **This is a TAIL VALUE, not a count** — the file declares **39** constants (values 0-38, `TCPEcho = 0`). The lineage figure **38** is correct as written; do NOT 'fix' it to 39.
- **DECISIONS.md tail:** **ADR-0298** (next-free **ADR-0299**) — `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1` -> `## ADR-0298`; `grep -c '^## ADR-0299'` ⇒ **0**. **ADR-0298 is COMPLETE as of the phase-76 IMPL** — §Context drafted at the phase-76 SPEC, §Decision + §Consequences APPENDED IN PLACE after the RETAINED italic footer, **no renumber and NO `---` separator** (the file's last `^---$` is still at `:17020`). Block shape controller-re-derived at this close: **1** `### Context` / **1** `### Decision` / **1** `### Consequences` / **1** retained footer. ⚠️ **FOUR of its OWN §Context paragraphs were CORRECTED IN PLACE** (¶1's `:295` -> `:307`, ¶5's `-count ~= 81` -> **83**, ¶7's *"~71 collapses"* -> **74/2000**, ¶11's *"needs no edit"* -> REFUTED) on the ADR-0297 ¶7/¶9 precedent, each **leading with what survives**; none changes a decision the ADR records. ⚠️ **It carries NO whole-file grep count and claims NO family ordinal** — the Load-balancing family closed at row 54, and **maintenance rows do not extend a charter**. *(Historical, retained: **ADR-0297 is COMPLETE** — §Context drafted at the phase-75 SPEC (ADR-0044-**as-used**; ⚠️ ADR-0044 does NOT actually contain that discipline), §Decision + §Consequences appended IN PLACE at the phase-75 IMPL after the **RETAINED** footer, mirroring ADR-0295/0296. Block shape verified at this close: **1** `### Context` / **1** `### Decision` / **1** `### Consequences` / **1** retained footer; `grep -c '^## ADR-0298'` => **0**. **No renumber, no new ADR.** ⚠️ **Its OWN §Context ¶7 and ¶9 were CORRECTED IN PLACE while completing it** — ¶7's **self-falsifying whole-file grep** (the same species the phase-74 IMPL had just fixed in ADR-0296 ¶3, reproduced one ADR later) is restated with **NO number** and the searched token deliberately **NOT spelled**, because every restatement mints another counter-example; and ¶9's *"SELF vs OTHER-ADR"* form rule is replaced by **graft SCALE** after the n=7 population was re-enumerated first-hand. ⚠️ **¶9 ALSO said *"the only two instances in the file"* — which this row's own blockquote makes three** — so a pure append would have shipped a SECOND self-falsifying count in the commit that fixed the first. **The prescription has now outlived THREE wrong justifications: family, self-vs-other, scale.** ⚠️ **`ADR-0296` is NO LONGER byte-untouched** — its §Decision (g) carries the phase-75 correction as an INDENTED blockquote (`grep -c '^  > '` **2 -> 3**), **LEADING WITH WHAT SURVIVES** (no such counter was owed *in phase 74*, whose sole stat-asserting fixture is `require=true` on both sides — **the deferral was RIGHT**) and claiming **NO ORDINAL**.)*
- **go.mod modules:** **Phase 76 landed +0** — `go mod tidy -diff` EMPTY and `git diff master -- go.mod go.sum` EMPTY; ⚠️ **the row's TEST-side import delta is +8 and that is CORRECT** (`ringhash_test.go` gains `fmt`/`math`/`mathrand "math/rand/v2"`, `linkage_test.go` adds `go/ast`/`go/parser`/`go/token`/`strconv`/`testing`) — all stdlib, all test-side, and **an import LINE is not a go.mod MODULE**; there is no new sub-package, so `reference_new_subpackage_pulls_transitive_module` does not bite. NC observed: a fake `require` ⇒ 67 -> 68. The tracked lineage figure is **2** (the phase-61.2 count: `quic-go` direct + `qpack` indirect) — **NOT a repo total**; the single `go.mod` requires **67** (18 direct + 49 indirect). **Phase 75 landed +0** — `go mod tidy -diff` EMPTY and `git diff master -- go.mod go.sum` EMPTY. The row needed `crypto/tls` and `internal/stats`, both ALREADY imported by `internal/listener/manager.go`; **an import LINE is not a go.mod MODULE**, and there is no new sub-package, so `reference_new_subpackage_pulls_transitive_module` does not bite. ⚠️ **TEST-side imports grew by exactly four** (`log`, `math`, `net/http`, `strconv` in `0110`'s driver) — all stdlib, all test-side, **not a production-envelope violation**.
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` (see `ENVOY_TARGET.md`).
- **conformance:** h2spec 53/53; proxy-wasm all 10 families PASS.

---

## Recent lineage

*(A SHORT recent lineage, capped at FIVE prior entries by ADR-0288. Everything earlier is in `STATE_HISTORY.md`. §Current carries the newest stage (**the phase-81 IMPL** — the stage that CLOSED row 81, re-derived the segment-position rule independently and found it exact, refuted five of the PLAN's *reasons* while every one of its conclusions held, turned the F1/F2 atomicity argument into a live observation, and realized **2229 net `.go` against a ~850 budget** — the lineage's SECOND crossing of the ~1500 trigger and the first over a MEASURED-basis estimate). The five below are the FIVE PRIOR stages, newest first. ⚠️ **THIS SENTENCE IS ITSELF A DRIFT SURFACE AND HAS BEEN WRONG BEFORE:** it named the phase-80 BRAINSTORM as newest right through the phase-80 SPEC, which rolled §Current but not this note. **A rolling document's own descriptive prose must be rolled with it.** ⚠️ **A close that adds a bullet here MUST evict the oldest to `STATE_HISTORY.md` VERBATIM, and MUST verify the evictee is ABSENT from the archive beforehand with a FIRING negative control on a sibling that IS present** — and ⚠️ **verify the identity check ITSELF discriminates**, by confirming a deliberately-doctored copy mismatches. ⚠️ **RE-DERIVE WHICH BULLET IS OLDEST AT THE TIP — never inherit the name from a plan**, because the plan's own eviction has already moved its named target once.)*

- **prior active-phase:** `phase 81 (stats-name-charset-guards) PLAN done` (2026-07-31) — lifecycle-state **2 -> 3**; **ROW 81 STAYS `in-progress`**, `ROADMAP.md`, `BEHAVIOR_CONTRACT.md` and `DECISIONS.md` **BYTE-UNTOUCHED**, sentinel `want` STAYS **113**. Base master **`82e425ac`** — taken from `git rev-parse master` at session start; ⚠️ at this tip the SPEC squash **IS** the master tip, so the recurring "the router's quoted SHA sits below the tip" hazard did not bite, and it was still re-derived rather than assumed. Branch `phase-81-plan`. Docs-only: **ZERO production `.go`, ZERO test `.go` committed**. **FIVE investigation agents on disjoint remits**, each in its own DETACHED worktree with private scratch and a private port band inside `42000-42499`; the controller re-derived every load-bearing claim itself (**five briefs re-derived, four corrected**). ⚠️ **THE HEADLINE: THE BARE-VS-ASSEMBLED QUESTION IS DECIDED BY SEGMENT POSITION, AND THE SPEC MEASURED IT AT THE ONE POSITION WHERE THE ANSWER IS DEGENERATE.** SPEC §2.8 measures `network/rbac` — where the operator token **LEADS** the assembled name — concludes *"MIS-ACCEPTS = 0, OVER-REJECTS = 1"*, and §3 promotes that template row-wide. Controller-executed cross-product over 9 tokens: **LEADING position disagrees on 1; INTERIOR position disagrees on 5** (`0policy`, `policy.`, `.policy`, `9`, `""`). ⚠️ **EIGHT OF THE NINE SOURCES ARE INTERIOR** — only B (zookeeper) is leading — and **three agents hit this independently on three different sources** (A1 on mongo, A2 on F1 with 5/19, A3 on wasm/compressor/rbac with three RED draft arms). **DECISION: ASSEMBLED AT ALL NINE**, per ADR-0065 §Consequences (b) (`DECISIONS.md:2379`, verbatim-verified): a bare probe at an interior position would **boot-reject four config shapes that today boot, serve, and register a perfectly valid counter** — a regression in an availability row — and would make **F1 and F2 disagree**, since F2 can only probe the assembled key. Two consequences recorded as **deliberate**: the guards inherit the interior-empty-segment hole by construction (SPEC §13.1, now measured on four sources, with **ACCEPT-pins** left in the corpus as a failing-first anchor for the successor), and mongo ends up more permissive than its three bare-guarded siblings. ⚠️ **SECOND HEADLINE: D-81-EMPTY'S PREMISE COLLAPSES.** A3's three-arm cross-product: unconditional **BARE** ⇒ rbac **FAIL (26 top-level)**, wasm/compressor `ok`; unconditional **ASSEMBLED** ⇒ all `ok`; skip-if-empty ⇒ all `ok`. Under the assembled template an empty token yields a **valid** name at C/G, and **D/E never see an empty segment at all** because `namespacePrefix("")` substitutes the literal `"rbac"` (controller-verified `rbac.go:507`). **§8 reasoned about a BARE token and applied the conclusion to an ASSEMBLED template**; **four of its seven cited tests never reach a production entry point**, and its H row is miscited (**ONE** bare `RateLimit{}`, in `fuzz_test.go:380`). Skip-if-empty is **KEPT for legibility, NOT load-bearing**; **A and B omit it** (empty already rejected upstream). ⚠️ **THIRD HEADLINE: A SECOND CORPUS RED AND AN ABORTING DIFFERENTIAL.** `wasm/abi_callbacks_test.go:1453` sets `&compiledConfig{pluginName: "my-plugin"}` — the source-C field itself — **AMBER, hard-red the moment the guard relocates**, which §7's shared-driver disposition makes tempting. And A4's first full differential **ABORTED at fixture 84 of 119** on `0083`'s own ALS-receiver port race, **35 fixtures silently never ran**, while the notification reported *"exit code 0"* against `INNER_EXIT=1`. ⚠️ **§7's single shared table is NOT CONSTRUCTIBLE** — five of six guard targets are package-private across five packages, and F1/F2 sit in different packages, so the audit is **~7 per-package drivers**. **COST: MEASURED, not modelled — 725 net `.go`, BELOW the SPEC's own band floor of 850** (F1 **178** confirms SPEC's 174; **F2 96 refutes its ~50**; D/E **38** each vs a ~75 model). **BAND 750-1000, BUDGET ~850, TWELVE tasks; NEITHER §6.1 trigger fires** (2.1× on tasks, 1.5-2.0× on LoC). **NO SPLIT.**

- **prior active-phase:** `phase 81 (stats-name-charset-guards) SPEC done` (2026-07-31) — lifecycle-state **1 -> 2**; **ROW 81 STAYS `in-progress`**, `ROADMAP.md` and `BEHAVIOR_CONTRACT.md` **BYTE-UNTOUCHED**, sentinel `want` STAYS **113**. Base master **`aab596e4`** — taken from `git rev-parse master` at session start, **NOT** from the BRAINSTORM squash `3816a17f`, which is two router-only commits below the tip (branching off it would have silently discarded them). Branch `phase-81-spec`. Docs-only: **ZERO production `.go`, ZERO test `.go`**. **FIVE investigation agents on disjoint remits**, each in its own DETACHED worktree with private scratch and a private port band inside `42000-42499`; the controller re-derived every load-bearing claim rather than adopting a brief. ⚠️ **THE HEADLINE: THE COUNT IS NINE, NOT EIGHT — source F is TWO structurally independent sources.** **F1** (rules engine, `for name := range r.GetPolicies()`) is enumerable at boot; **F2 (matcher engine) is NOT** — `BuildMatcherEngine` does **only** a TypeURL allowlist (`matcher.New(m, []string{actionTypeURL})`) and `Evaluate` returns `action.GetName()` read out of the matcher tree's terminal `Action` proto at **REQUEST TIME**, with **nothing enumerating those names at boot**. **PROVED BY EXECUTION with a firing NC** — `allow_admins` registers cleanly, while `allow-admins` **BOOTS ACCEPTED** (`BuildMatcherEngine err=nil`) and then `PerPolicyCounters.Inc` **PANICS** on `"http.myhcm.rbac.policy.allow-admins.allowed"` ⇒ **the BRAINSTORM's boot-only remedy covers F1 ONLY.** ⚠️ **THE REFERENCE ACCEPTS THE HYPHEN VERBATIM** — controller-run against `envoyproxy/envoy:contrib-v1.37.2`, three arms plus a discriminating no-track control: it boots, serves 200, and emits `envoy_http_rbac_policy_allowed{envoy_rbac_policy_name="allow-admins",…} 1`, **hoisting the policy name into a LABEL** (label values have no charset to violate — that is why upstream never needed a guard) while envoy-go **FLATTENS** it into the metric name and has no `rbac_policy_name` extraction at all ⇒ **the real gap is PROJECTION, not charset, and any reject here is an envoy-go-strict DEPARTURE, not a fix.** ⚠️ **THE CORPUS DOES NOT SURVIVE** — BRAINSTORM §4.2's *"no existing fixture should red"* is narrowly true and materially wrong: one **proven hard RED** at `internal/filter/http/ratelimit/compiled_config_test.go:574`/`:616-617` (`tenant-foo`, `IsValidName` = false), and it is **INVISIBLE TO THE DIFFERENTIAL** because source H has **ZERO** fixture coverage (`0032`/`0033` set only the HCM `stat_prefix`). **DISPOSITIONS — four D's plus TWO NEW FORKS:** D-81-HELPER **INLINE** (⚠️ **the tradeoff DOES NOT EXIST** — `go list -deps ./cmd/envoy-go` is **560** packages and contains **8 of 8**, all target packages *and* `internal/stats`, so both designs owe the **identical** differential + h2spec gate; the whole measured asymmetry is **11.09 s** of extra unit tests) · D-81-F-SITE **TWO PARTS** (gated boot reject at `http/rbac buildCompiledConfig` + skip-and-log backstop at `PerPolicyCounters.Inc`) · D-81-SANITIZE **DISSOLVES ON SCOPE** (ADR-0065's rejection is a **TWO-LIMB CONJUNCTION**; `normalizeAddr` **fails limb (ii)** because its input is captured POST-BIND, and ADR-0065 §Decision cites it **APPROVINGLY** ⇒ **ADR-0302 §Consequences (vii) item 5 is PARTIALLY REFUTED**; reject anyway — measured doctrine ratio **1 sanitizer : 15 rejecters**) · D-81-WORDING **eight byte-stable wordings, collision-checked** · **D-81-DEPTH (NEW FORK)** **table-driven-shared** (worth ~450-500 net lines; left undecided the PLAN defaults to the phase-80 shape and the row crosses) · **D-81-EMPTY (NEW FORK)** **skip-if-empty** (otherwise **seven** tests red and **ADR-0132 §Decision (v)** is contradicted). **COST: band ~850-1200 net `.go`, budget ~1000, ~14 tasks; NEITHER §6.1 trigger fires** (**1.8×** margin on tasks, **1.25-1.75×** on LoC) ⚠️ **and the router's split axis is REFUTED** — the empty-segment retrofit is already OUT of scope, so re-badging it `81.2` is not a split and would park **row 81 `in-progress` behind it**, re-coupling the request-time availability fix to work it was deliberately decoupled from. File deltas: `DECISIONS.md` gains **ADR-0303 §Context** only (tail moves **ADR-0302 -> ADR-0303**, next-free **ADR-0304**); `STATE_HISTORY.md` **432 -> 434**.

- **prior active-phase:** `phase 81 (stats-name-charset-guards) BRAINSTORM done` (2026-07-31) — lifecycle-state **DONE -> 1**; **ROW 81 REGISTERED `in-progress`** at `ROADMAP.md:143`; sentinel `want` bumped **112 -> 113** in the SAME commit. **SELF-PICKED** per the 2026-07-12 standing directive (smallest defensible candidate first, with **severity as the tie-break**); **four costing agents in parallel**, and every cost was re-derived at that tip. Base master `4f8e159c`, branch `phase-81-brainstorm`. Docs-only: ZERO production `.go`, ZERO test `.go`. ⚠️ **THE HEADLINE: a config-triggered, REQUEST-TIME, un-recovered process crash, PROVED BY EXECUTION at that stage with a firing NC** — `internal/rbac/perpolicy.go:25` lazily registers `<base>.policy.<policyName>.<suffix>` on the first matching request, guarding `policyName` for EMPTINESS ONLY, and `internal/stats/registry.go:115-119` PANICS; an idiomatic `allow-admins` plus `track_per_rule_stats: true` boots GREEN and kills the process on first traffic, while the `allow_admins` NC arm registers cleanly. The row closes the **EIGHT** unguarded token sources across six packages — ⚠️ **the phase-81 SPEC subsequently made that NINE across SEVEN packages** — and the empty-segment (`a..b`) hole is **EXPLICITLY OUT OF SCOPE**, deferred to a named successor. `ROADMAP.md` **228 -> 229 lines / 112 -> 113 data rows**, `STATE_HISTORY.md` **430 -> 432**; `BEHAVIOR_CONTRACT.md` and `DECISIONS.md` **BYTE-UNTOUCHED**. Sentinel re-run mechanically: (1) silent at `want=112` with a doctored-copy NC firing, (2) **FIVE** UNCHANGED (the twenty-eighth consecutive phase at which it did not go down), (3) `NEVER OPENED: gRPC`/`WASM`; the **leak check PASSED** with the by-mention silencing **REPRODUCED LIVE** on a doctored copy. ⚠️ **FIVE LOAD-BEARING ROUTER CLAIMS REFUTED BY EXECUTION:** `ROADMAP.md:76` **never declined a WASM row** (a category error — `:76` kills the *rider*, while `:218` is a separate `### WASM host family` heading, so a standalone WASM row is live and defensible) · `--mode validate`'s defect runs the **OPPOSITE** way, **false-REJECT not false-accept**, 3/3 on live passing fixtures `0103`/`0108`/`0109` where the pinned reference answers `configuration OK` · *"the five existing rejects"* is wrong by **3×** — there are **FIFTEEN** production `IsValidName` guard sites · the failure mode is a **direct `panic`, not a nil counter** (`checkName` panics; registration never returns nil) · and the prescribed differential bind-retry **cannot work**, because all 26 backend arms `go run`, so `cmd.Start()` returns nil and the collision surfaces later at the child's `Listen`. Bookkeeping findings: the archive gap is **57, not 58** (two extractor traps cleared); the obvious archive guard **SELF-CLEARS** by matching `STATE.md`'s own confession prose; CI is a **shallow clone** (`actions/checkout@v6`, depth 1) so no `git log`-derived expectation set can work; naive `NF==8` throws **15 false positives**; and a landed memory was **CORRECTED** — only `git grep -c` is silent on zero, GNU/ugrep always print.

- **prior active-phase:** `phase 80 (stats-sds-projection) IMPL done` (2026-07-30) — lifecycle-state **3 -> DONE**; **ROW 80 FLIPPED `in-progress` -> `done`; PHASE 80 WAS CLOSED.** The `sds.` SN1 hoist plus a boot-boundary reject landed GREEN (full 120-fixture differential **120/120 in 402 s**, h2spec **53/53**, `-race` clean, stat-surface delta **+0** proven by call-site enumeration). `DECISIONS.md` **17692 -> 17724** (ADR-0302 PROPOSED -> COMPLETE, plus the ADR-0299 rider), `BEHAVIOR_CONTRACT.md` **5822 -> 5868**, `STATE_HISTORY.md` **428 -> 430**. ⚠️ **The PLAN estimated ~640 net `.go` and the row realized 1636 — the lineage's FIRST realized ~1500-LoC crossing**, every bucket over with the errors COMPOUNDING (fixture bucket 4.1×). Base master `28aa342d`, branch `phase80-impl`. **THIRTEEN tasks executed; SIXTEEN PLAN claims refuted, four load-bearing.**

- **prior active-phase:** `phase 80 (stats-sds-projection) PLAN done` (2026-07-30) — lifecycle-state **2 -> 3**; **ROW 80 STAYS `in-progress`** and `ROADMAP.md`, `BEHAVIOR_CONTRACT.md` and `DECISIONS.md` are **BYTE-UNTOUCHED** at this stage (the PLAN adds no row; sentinel `want` stays **112**). Docs-only: ZERO production `.go`, ZERO test `.go` committed. **Base master `47b9b378`** — ⚠️ **NOT the `f2dd994a` the router named**: `47b9b378` is the router's own roll commit on top of it, and branching off `f2dd994a` would have silently discarded it (`git diff --name-only f2dd994a..47b9b378` ⇒ `next-prompt.txt` ONLY, so every doc count survived). Branch `phase80-plan`. **FOUR probe agents on disjoint remits**, each in its own DETACHED worktree with PRIVATE scratch and a port band in **42000-42299** — clear of BOTH reserved bands (`20000-31007` subject blocks AND `11000-14999` backends, new at `f2dd994a`) and the static fixture ports — plus controller re-derivation. Zero commits, zero pushes by any agent; every experimental edit reverted BY EXPLICIT PATH with sha256 byte-identity verified **4/4** and independently re-confirmed by the controller. ⚠️ **THE PLAN'S OWN HEADLINES:** the SPEC's §7.2 fixture VERDICT was **RED ON ARRIVAL on three of five names**; its *"T5 folds into T4"* is not a fold but a **FORCED ATOMIC MERGE** (so the enumeration is **13 not 14** and T4 must NOT split); and its `855 x 1.75` split arithmetic **mixes two bases** (a realized-over-PLAN multiplier applied to a SPEC estimate), so *"ON the ~1500 line"* is not a derived result. **BAND 11-14, BUDGET 13.** ⚠️ **The reject's SITE was pinned BEFORE the SDS dial** — placed after it, the dial error MASKS the name reject. ⚠️ **A WRONG FIGURE WAS ALREADY BAKED INTO A LANDED ADR** (ADR-0302 §Context ¶11's *"forty-five files"*) and was flagged as landable at the IMPL precisely because the STATUS was still `PROPOSED`.


## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
