# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `12-http-filter-csrf` — phase 12 PLAN.md authored at this commit. ROADMAP row 12 stays `in-progress` (flipped at SPEC commit `a305b86`). Phase 12 is the FIFTH §9 family-row in the in-progress lifecycle (after cors @ 07.1 done, fault @ 09 done, header_mutation @ 10 done, local_ratelimit @ 11 done); the §9 family heading at ROADMAP line 56 stays unchanged per ADR-0106(c). The PLAN settles all 4 SPEC §12 deferred decisions plus 5 PLAN-emerging decisions (per §"Planner-time deferred-decision resolution"). Notable resolutions: D1 + PLAN-2 — csrf is the FIRST §9 production filter to be structurally decoder-only (`HTTPFilter` value sets `Decoder: f, Encoder: nil`); D3 — envoy-go-own-wording errors per phase 11 ADR-0115 precedent (NOT verbatim Envoy PGV envelope); D4 + PLAN-5 — per-route stats SHARED via `buildPerRouteRuntime(perRoute, listenerStats)` helper at request time (option (b); the shared-stats invariant is structurally explicit in the helper signature); PLAN-6 — single-file `csrf.go` (no `origin.go` split since helpers are tightly coupled); PLAN-7 — single-listener fixture topology (one listener `l_main` + two routes; fits existing `fixture.Driver` contract; saves driver complexity vs phase 11's 4-listener topology); PLAN-8 — synthetic `http://` prefix for target URL parsing (NO framework extension; the prefix is stripped via `hostAndPort()` per §11.3 amendment so byte-equivalence with reference Envoy is preserved). All 5 anticipated ADRs (ADR-0120..ADR-0124) anchored at first-use tasks per ADR-0044 ADR-on-impl convention. NO ADR-0073 amendment paragraph + NO ADR-0061 amendment + NO new SN flattening rule (notable simplifications from phase 10/11; csrf is the structurally-thinnest §9 family-row to date).
- **phase-directory:** `docs/envoy-go/phases/12-http-filter-csrf/` — contains `BRAINSTORM.md` (499 lines; predecessor commits `7fd9213` + `ba58c7e` + `399532c` + `c2e7559` + `bb29bb0`) + `SPEC.md` (1597 lines; commits `a305b86` + `fb4d582`) + `PLAN.md` (~2475 lines at this commit) at this commit. PROGRESS.md authored at impl-stage Task 1; REVIEW.md authored at phase-done close (Task 13). The PLAN commits to 13 tasks (Task 1 precondition + PROGRESS preamble; Tasks 2-4 production code + 6 unit-test groups + ADRs 0120-0124; Task 5 fuzzer; Task 6 main.go register; Tasks 7-11 fixture infrastructure + driver; Task 12 BEHAVIOR_CONTRACT 4-edit bundle + ROADMAP flip + 6-gate verification + STATE advance + phase-done commit; Task 13 REVIEW.md), well under ADR-0045's 25-task threshold. Production code estimate ~283 LoC (csrf.go ~280 + main.go +3) — well under the ~1500 LoC threshold. NO sibling-stub authored per ADR-0106(b). NO off-master prebrainstorm-notes branch (UNLIKE phase 11; phase 12 cold-started fresh per ADR-0106(e)).
- **lifecycle-state:** `phase 12 PLAN authored; impl pending` — phase 12 PLAN.md authored at this commit; lifecycle-state-2 → 3 transition complete per BOOTSTRAP_PROMPT.md §5. Lifecycle-state-3 (impl execution) is the next session's responsibility. The plan-document-reviewer subagent dispatched at this commit returned **Approved** with only advisory recommendations (no blocking issues); the recommendations (CompareBytes sketch expansion in Task 11, IPv6 edge-case test in Group 5, h2spec command citation in Task 12 step 8) are noted but not gating. Phase 11's six gates remain green at master tip `0f3a710` (parent of this PLAN's worktree branch); this commit is docs-only (no code changes; no test re-validation needed).
- **next-skill:** `superpowers:subagent-driven-development` (recommended per ADR-0005 + the user's persistent preference for subagent-driven execution recorded in `~/.claude/projects/-home-esa-git-envoy-go/memory/MEMORY.md`) OR `superpowers:executing-plans` (alternative for inline batch execution). The 13-task PLAN routes per the standard task-by-task discipline; the impl session dispatches a fresh subagent per task with PROGRESS.md as the per-task append target.
- **next-skill-scope:** Cold-start scope for the phase 12 impl-execution session. Read first: `docs/envoy-go/phases/12-http-filter-csrf/PLAN.md` (the authoritative task list + step-by-step recipes; 13 tasks; ~2475 lines); `docs/envoy-go/phases/12-http-filter-csrf/SPEC.md` (the authoritative design + empirical-pin doc — every PLAN task traces to one or more SPEC sections); `docs/envoy-go/phases/12-http-filter-csrf/BRAINSTORM.md` (consulted only for design-rationale provenance — SPEC §1.1 amendments supersede); `docs/envoy-go/ROADMAP.md` row 12 (status `in-progress`); `docs/envoy-go/BEHAVIOR_CONTRACT.md ## HTTP filter chain ### envoy.filters.http.local_ratelimit` subsection at line 1008 (insertion anchor for the new csrf subsection); `docs/envoy-go/DECISIONS.md` ADR-0073 (no third amendment) + ADR-0101 (parse-time-drop StringMatcher discipline phase 12 inherits verbatim) + ADR-0117 (phase 11's stateful-per-route precedent — phase 12 explicitly DIVERGES with data-only-shared-stats per ADR-0124) + ADR-0114..ADR-0119 (the 6 phase-11 ADRs); `docs/envoy-go/phases/11-http-filter-local-ratelimit/PLAN.md` + `PROGRESS.md` (the most-recent phase artefacts — phase 12 implementation cadence mirrors these). The impl session executes Task 1's 16-precondition check FIRST; on green, proceeds task-by-task per the PLAN's checkbox steps. Worktree expected at `.worktrees/phase-12-http-filter-csrf-impl` (branched from master tip per ADR-0003 + the user's git-worktree preference). ADRs ADR-0120..ADR-0124 land at first-use commits per the per-task ADR table in PLAN's `## ADRs introduced by this plan` section.
- **last-commit:** `b903ab2` — `phase 12 PLAN: http-filter-csrf [13 tasks; plan-document-reviewer Approved]`. Authors `docs/envoy-go/phases/12-http-filter-csrf/PLAN.md` (~2475 lines) capturing the 13-task decomposition + 9 planner-time-decision resolutions (4 SPEC §12 + 5 PLAN-emerging) + 5-ADR anchor table (ADR-0120..ADR-0124 land at Tasks 2/2/3/3/4 per ADR-0044) + 16-precondition cold-start check + Refinement + Post-plan handoff sections. plan-document-reviewer Approved at iter-1 with only advisory recommendations. ROADMAP row 12 status unchanged (`in-progress`; flipped at SPEC commit). Docs-only commit; no code changes; phase 11's six gates remain green at parent commit `0f3a710`.
- **last-updated:** 2026-05-07

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
