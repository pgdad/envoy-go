# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `05.2-upstream-h2`
- **phase-directory:** `docs/envoy-go/phases/05.2-upstream-h2/` — contains `README.md`, `SPEC.md` (`dacf4b7`), `PLAN.md` (`4c6b6bb`), and `PROGRESS.md` (Tasks 1-15 landed; Task 15 executor verification block at `bd75c88`; lifecycle-state 4 verifier verification block written by this commit). REVIEW.md will land in the REVIEW session at lifecycle-state 6. The sibling `docs/envoy-go/phases/05.1-downstream-h2/` remains closed read-only history (ROADMAP row `05.1` status `done`). The parent `docs/envoy-go/phases/05-http-2/` retains its `SPEC.md` (`612cdea`) as the master design document. The parent ROADMAP row `05-http-2` stays `in-progress` until 05.2 reaches `done`; the 05.2 phase-done commit will close both rows on the same commit per 05.2 SPEC §4.4 + PLAN Task 15's "Refinement" note.
- **lifecycle-state:** `5` — phase 05.2 verification complete; the verifier session on `phase/05.2-upstream-h2-verify` (worktree `.worktrees/phase-05.2-upstream-h2-verify`, branched from master tip `7a37608` per ADR-0003) re-ran all six SPEC §3 phase-done gates from a fresh session per BOOTSTRAP §7.5: gates (a) fixture-0004 differential PASS, (b) all 5 differential fixtures PASS, (c) h2spec 53/53 PASS at the pinned summerwind SHA, (d) all 6 fuzzers PASS at 30s budget with empty `git status --porcelain` after each, (e) `go build`/`go vet`/`golangci-lint`/`go test -race ./...` all clean + ADR-0046 boundary grep empty modulo the 5 allowed files + ADR-0048 client.go present + forbidden-runtime-imports clean modulo doc.go text. Gate (f) REVIEW.md remains the lifecycle-state 6 deliverable. Per BOOTSTRAP §5 the next transition is state 5 → 6 via `superpowers:requesting-code-review`.
- **next-skill:** `superpowers:requesting-code-review` — drive the lifecycle-state 6 REVIEW deliverable: write `docs/envoy-go/phases/05.2-upstream-h2/REVIEW.md` with Critical/Important/Minor findings against the 05.2 deliverables (SPEC §4.1-§4.4, PLAN tasks 1-15 outcomes, all 4 ADRs landed in 05.2 — ADR-0055/0056/0057/0058), the 05.1 carry-forward dispositions matrix (PROGRESS Task 15 lines 1062-1090), and the 05.2-introduced REVIEW Minor candidates (PROGRESS Task 15 lines 1092-1095: the `TestClientConn_RoundTrip_PeerDataAfterEndStream` deadline-extension fix and the ADR-numbering non-monotonicity 0055→0056→0058→0057). Inputs: this verification commit, `BOOTSTRAP_PROMPT.md` §5 state 6, `SKILL_ROUTING.md`, the closed-history 05.1 REVIEW (`docs/envoy-go/phases/05.1-downstream-h2/REVIEW.md`) as structural precedent. Reviewer branches a fresh worktree `.worktrees/phase-05.2-upstream-h2-review` from master tip per ADR-0003 + per-phase-worktree convention.
- **next-skill-scope:** Lifecycle-state 6 REVIEW session deliverables: (1) write `REVIEW.md` to the phase directory with structured Critical/Important/Minor findings (mirror the 05.1 REVIEW shape); (2) on REVIEW approval (no Critical findings; Important findings either resolved in REVIEW-followup or scope-deferred with explicit rationale per BOOTSTRAP §5 step 6), the SAME commit (or REVIEW-followup commit chain) is the phase-done commit and flips ROADMAP row 05.2 status `in-progress` → `done` AND parent row 05 status `in-progress` → `done` per 05.2 SPEC §4.4 + PLAN Task 15's "Refinement" note (closes both rows together because 05.2 is the terminal sub-phase of 05). At the phase-done commit, advance STATE to lifecycle-state 6 (or directly to `state 0` for phase 06 entry per BOOTSTRAP §5's step 6 → step 1 transition); also update STATE's `active-phase` to `06-access-log-stats` (or whatever phase 06's directory name is when brainstormed). Re-entry: any Critical or Important finding the REVIEW raises bounces the lifecycle back to state 3 per BOOTSTRAP §5.2 review-feedback re-entry; cleanup commits land on `phase/05.2-upstream-h2-impl-followup` (or a new `-review-followup` branch per the 05.1 precedent) before re-entering verification.
- **last-commit:** TBD-this-commit — `phase 05.2: lifecycle-state 4 verification — all five executable gates (a/b/c/d/e) green; gate (f) deferred to REVIEW; STATE → 5`, on branch `phase/05.2-upstream-h2-verify`. Adds the lifecycle-state 4 verification block at the tail of `docs/envoy-go/phases/05.2-upstream-h2/PROGRESS.md` (verbatim outputs of every gate command run from a fresh-session worktree branched from master tip `7a37608`) and updates STATE → lifecycle-state 5 with `next-skill: superpowers:requesting-code-review`. No production code, test, or fixture file changed; this is a pure verification + state-transition commit per the 05.1 verifier-commit precedent. ROADMAP unchanged (rows 05.2 + 05 stay `in-progress`; phase-done commit at lifecycle-state 6 will flip both).
- **last-updated:** 2026-04-26

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
