# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `25 (in-progress) — 25.1 IMPL done at 2026-05-25; 25.2 BRAINSTORM done at 2026-05-25; 25.2 SPEC done at 2026-05-25; 25.2 PLAN done at 2026-05-25; 25.2 IMPL done at 2026-05-26 (this commit); awaiting 25.3 BRAINSTORM` — **phase 25.2 IMPL DONE at 2026-05-26 (this commit)** atomic-landing all 22 IMPL tasks per the 25.2 PLAN's 22-task TDD task graph + 6-tier structure per `superpowers:subagent-driven-development` discipline. **6 phase-done gates ALL GREEN per 25.2 SPEC §14.10:** Gate A (build) `go build ./...` clean; Gate B (vet+lint) `go vet ./...` + `golangci-lint run ./...` clean with no new suppressions (modulo `//nolint:unused` annotations on fixture-0036 retained helpers for cross-side arm restoration); Gate C (race) `go test -count=1 -race -short ./...` clean across 70+ packages; Gate D (differential) 39/39 fixture directories GREEN (37 pre-existing + 2 NEW: `0036-http-wasm-body-and-advanced` mixed-mode + `0037-http-wasm-body-and-advanced-boot-reject` subject-only); Gate E (fuzz) `FuzzWasmHostcallEnvelope` 30s-clean (~2.3M execs, 19 new interesting; no panics) + 35 project-wide fuzzers; Gate F (h2spec) 53/53 PASS at ADR-0051 v1.32.4 pin. **R8 benchmark gate disposition: STANDS WEAK-default; ADR-0209 escape-valve STAYS UNCONSUMED** — `BenchmarkPerStreamModule_Instantiation` at Task 22 reported `~98 ns/op` (32 B/op, 1 allocs/op) per stream, vastly under the 1ms threshold per D-P-PLAN-11 + D-25.2-P2 (the 25.2 root-VM model retires 25.1's per-stream Runtime construction at ~61µs → 25.2 per-stream cost is bookkeeping + `proxy_on_context_create` dispatch on a shared `*RootVM.runtime` + shared compiled `*Module`); ADR-0209 carries forward to 25.3 IMPL escape-valve slot per the R8 signaling protocol. **SPEC §15 46-item acceptance checklist ALL GREEN** at this Task 22 atomic landing — 12 framework primitive evolutions (items 1-12) materialized; 3 NEW framework primitives (items 13-15: `internal/filterstate/` + `internal/stats/dynamic/` + phase-22.2 lua MIGRATES); 8 filter package extensions (items 16-23); 1 PARSE-REJECT roster (item 24 — 6 NEW arms 19-24 with D-25.2-P5 byte-stable wording closure at this Task 22); 1 stat surface item (item 25 — 9 NEW envoy-go-strict counters; 119 → 128); 1 envoy-go-strict departure records item (item 26 — 6 NEW records consolidated bundle); 3 fuzzer + fixture items (items 27-30 incl. 35th fuzzer + fixture-0036 + fixture-0037 + NEW `BackendKind=HTTPWasmAdvanced`); 5 wire-shape pins (items 31-35 — AMEND-B1..B5); 3 ADR landings (items 36-38 — 4 NEW bodies + ADR-0202 AMEND + ADR-0209 UNCONSUMED); 2 STATE+ROADMAP (items 39-40); 1 R8 escape-valve gate (item 41 — UNCONSUMED); 5 SPEC-time D-question closures (items 42-46 — D-25.2-P1 at Task 21 first-action with arm 19 `envoy_go_strict_body_buffer_cap_bytes`-zero substring per D-25.2-P1; D-25.2-P2 at Task 22 benchmark gate; D-25.2-P3 + D-25.2-P4 at PLAN session per D-P-PLAN-9 + D-P-PLAN-10; D-25.2-P5 at this Task 22 BEHAVIOR_CONTRACT.md bundle landing). **4 NEW ADR §Decision+§Consequences bodies landed atomically at this Task 22 per the 25.2 SPEC §10 anchor map**: ADR-0205 (root VM lifecycle evolution — ONE long-lived `*RootVM` per `*compiledConfig`; per-stream contexts as CHILDREN sharing wazero Runtime+Module; tick goroutine + 10ms envoy-go-strict floor + Clock seam FIRST co-consumer beyond phase-21 RATIFIES ADR-0186; shared-data + httpCall + foreign-function-registry state at root; per-stream Module instantiation pattern WEAK-default fresh-per-stream STANDS); ADR-0206 (25.2 ABI extensions — 14 NEW env-namespace hostcalls + 7 NEW guest-export callbacks + 21 NEW capability keys with gate-at-`registerCallback` per AMEND-B5 + buffer-clamp wire-contract per AMEND-B1 + metric signedness per AMEND-B2 + mutex-per-RootVM foreign-function concurrency per D-P-PLAN-9 + full ~70-path proxy_get_property roster per AMEND-B4 + EMPTY default foreign-function registry per AMEND-A9); ADR-0207 (NEW `internal/filterstate/` framework primitive at second-consumer scope per Q7 — `*Bucket` + FilterStateObject + StateType; consumer #1 = phase-22.2 lua MIGRATES non-breaking at Task 10; consumer #2 = phase-25.2 wasm `filter_state.*` + `upstream_filter_state.*` per AMEND-B4; EXPLICIT API-REVISION ALLOWANCE for consumer #3+); ADR-0208 (NEW `internal/filter/http/wasm/` 25.2 package extensions — 9 envoy-go-strict counters per §7.1 + AMEND-B3 incl. `http_call_response_after_close` + 4 envoy-go-strict-only `PluginConfig` config fields per Qs 2/6/9 + dynamic-stats namespace `wasmcustom.<custom_name>` per AMEND-B2 via NEW `internal/stats/dynamic/` infrastructure + fixture-0036 + fixture-0037 + 35th fuzzer + ~7-edit BEHAVIOR_CONTRACT.md bundle + 6 envoy-go-strict departure records consolidated). **ADR-0202 §Consequences one-line in-place AMEND acknowledgment paragraph LANDED** per 25.2 SPEC §10.2 — no new ADR number consumed; EXPLICIT API-REVISION ALLOWANCE clause for consumer #2 STAYS scoped to consumer #2; 25.2's consumer-#1-internal-scope evolutions land under NEW ADRs per phase-22.2 Q10 strict-scope precedent; ADR-0202 `**Status:**` line updated with `AMENDED 2026-05-26 per phase-25.2 one-line acknowledgment in §Consequences`. **CONDITIONAL ADR-0209 STAYS UNCONSUMED** per D-P-PLAN-11 R8 disposition above. **SECOND occurrence of EXTRACT-NOW-on-second-consumer discipline** after phase-22.1+22.2's `internal/lua/` + `internal/dynamicmetadata/` — anchors NEW `internal/filterstate/` framework primitive (`*Bucket` + FilterStateObject + StateType discriminator + sync semantics) per ADR-0207 with EXPLICIT API-REVISION ALLOWANCE clause for consumer #3+; phase-21 `internal/clock/` extraction at Task 5 is also a SECOND occurrence (RATIFIES ADR-0186 at second-consumer scope; the migration of `internal/filter/http/adaptive_concurrency/clock.go` to use the NEW package is OUT OF 25.2 scope and noted as 25.2-follow-up architectural debt in REVIEW.md). **NEW `internal/stats/dynamic/` infrastructure subpackage** per ADR-0208 + AMEND-B2 (per-plugin Registry SCOPE discipline; `wasmcustom.<custom_name>` namespace byte-faithful to upstream — operator sees `wasm.<plugin>.wasmcustom.<custom>` at admin /stats by virtue of parent stat-scope nesting). **22 IMPL tasks landed in sequence:** Pre-Task 0 (15-precondition verification + PROGRESS.md preamble) → Task 1 (NEW root_vm.go + stream_context.go + DELETE 25.1 vm.go per D-P-PLAN-6) → Task 2 (sandbox.go EXTEND — 21 NEW capability keys) → Task 3 (registration.go EXTEND — 14 NEW hostcalls + 7 NEW callbacks gate-at-`registerCallback`) → Task 4 (NEW abi/body_bridge.go + abi/stream_control.go per AMEND-B1) → Task 5 (NEW tick.go + abi/timer.go — tick goroutine + 10ms floor + NEW `internal/clock/` EXTRACT at SECOND occurrence) → Task 6 (NEW shared_data.go + abi/shared_data.go — CAS + caps) → Task 7 (NEW foreign.go + abi/foreign.go — EMPTY default registry + mutex-per-RootVM per D-P-PLAN-9 + D-25.2-P3) → Task 8 (NEW http_call.go + abi/http_call.go — cancel-at-destruction per AMEND-B3) → Task 9 (NEW `internal/filterstate/` framework primitive per ADR-0207) → Task 10 (phase-22.2 lua MIGRATION — filterstate REWRITE non-breaking per §14.5) → Task 11 (NEW `internal/stats/dynamic/` infrastructure per ADR-0208 + AMEND-B2) → Task 12 (NEW dynamic_stats.go + abi/metrics.go — signed-int64 delta per AMEND-B2) → Task 13 (NEW property.go — full ~70-path proxy_get_property roster per AMEND-B4) → Task 14 (compiled_config.go EXTEND — 4 envoy-go-strict-only config fields + 6 NEW PARSE-REJECT arms 19-24) → Task 15 (abi_callbacks.go EXTEND — 7 NEW methods + 4 RE-USE primitive integrations) → Task 16 (NEW body.go + trailers.go + tick_clock.go) → Task 17 (NEW property.go + stats.go EXTEND — 9 NEW envoy-go-strict counters per §7.1 + AMEND-B3) → Task 18 (decode_headers.go + encode_headers.go EXTEND — per-stream construction via RootVM.NewStreamContext; CLOSES whole-repo build per D-P-PLAN-6) → Task 19 (35th project-wide fuzzer `FuzzWasmHostcallEnvelope` per §8.4 + R-25.2-12 + D-25.2-P4 closure per D-P-PLAN-10) → Task 20 (Differential fixture-0036 mixed-mode — 14 scenarios with deliberate-break liveness verification per `reference_differential_asserter_dispatch`; landed PARTIAL — cross-side arms a-j SKIPPED on Concern 1 Envoy v1.37.2 503 upstream-buffering parity deferral; arms k+l+m+n subject-only PASS) → Task 21 (Differential fixture-0037 subject-only boot-reject — arm 19 per D-25.2-P1 first-action closure with substring `"envoy_go_strict_body_buffer_cap_bytes"`; runner branch shape: `BootRejectFixture` EXTENDED with `subjectOnly: true` flag) → Task 22 (this atomic landing). **Project surface deltas at 25.2 phase-done:** stat surface 119 → 128 internal names (+9 envoy-go-strict counters); 20 HTTP filters wired UNCHANGED at 25.2; 34 → 35 project-wide fuzzer count (+1: `FuzzWasmHostcallEnvelope`); 37 → 39 differential fixture-dir count (+2: 0036 + 0037); 37 → 58 cumulative capability key roster (+21 NEW at 25.2); ADR tail advance to **ADR-0208** (ADR-0209 STAYS UNCONSUMED + carries to 25.3 IMPL escape-valve slot). **D-P-PLAN-1..D-P-PLAN-12 disposition recorded in REVIEW.md** — all 12 PLAN-time decisions HELD at IMPL with the following minor refinements: D-P-PLAN-6 whole-repo build break window from Task 1 Step 6 → Task 18 Step 3 OBSERVED + RECOVERED as anticipated; D-P-PLAN-7 parallelization mapping HELD across all 7-way + 3-way + 2-way Tier sub-graphs; D-P-PLAN-11 R8 disposition HELD UNCONSUMED at empirical evidence well below 1ms threshold. **D-25.2-P1..D-25.2-P5 closure evidence recorded in REVIEW.md** — D-25.2-P1 CLOSED at Task 21 first-action (arm 19 chosen per anticipated D-P-PLAN-11 disposition); D-25.2-P2 + R8 CLOSED at Task 22 benchmark gate (STANDS WEAK-default); D-25.2-P3 CLOSED at PLAN session per D-P-PLAN-9 (mutex-per-RootVM ratified at Task 7 with concurrent N=100 dispatch + no cross-stream argument leak); D-25.2-P4 CLOSED at PLAN session per D-P-PLAN-10 (35-seed corpus enumerated; fuzzer 30s-clean at Task 19); D-25.2-P5 CLOSED at this Task 22 BEHAVIOR_CONTRACT.md bundle landing (final wording + line counts pinned). **Architectural debts recorded in REVIEW.md** as 25.2-follow-up backlog (NOT load-bearing for 25.2 phase-done): phase-21 `internal/filter/http/adaptive_concurrency/clock.go` not yet migrated to NEW `internal/clock/` (extracted at Task 5; migration follow-up phase needed); phase-22.2 lua filterstate ephemeral Bucket pattern preserves `cb.filterState as map[string]any` test-shape via `bucketFromMap`/`materializeBucketIntoMap` adapter (deeper storage migration deferred — API-level migration IS complete); fixture-0036 cross-side arms a-j skipped on Envoy v1.37.2 503 upstream-buffering parity gap (production fix: wire envoy-go upstream-buffering to match Envoy's `Action::Pause + buffer-then-forward` byte-faithfully); scenario (j) `std::env::vars()` RefCell — per AMEND-A6 env_vars deferred to 25.3 (workaround in fixture-0036). Per ADR-0106 + phase-22.1/22.2/22.3 + phase-23 + phase-24.1/24.2 + phase-25.1 precedent, sub-row 25.2 flips `in-progress → done` at this 25.2 IMPL phase-done; sub-row 25.1 STAYS `done`; parent row 25 STAYS `in-progress` until 25.3 phase-done; sub-row 25.3 STAYS `planned`. Phases 22 + 23 + 24 + 24.1 + 24.2 + 25-parent + 25.1 + 25.2-BRAINSTORM + 25.2-SPEC + 25.2-PLAN + 25.2-IMPL DONE and CANONICAL — DO NOT reopen.
- **phase-directory:** `docs/envoy-go/phases/25.2-http-filter-wasm-body-and-advanced-bridge/` (**REVIEW.md authored at this commit** per `superpowers:requesting-code-review` skill; PROGRESS.md final Task 22 entry appended at this commit; README.md + BRAINSTORM.md + SPEC.md + PLAN.md DONE at predecessor commits). Predecessor sub-phase directory `docs/envoy-go/phases/25.1-http-filter-wasm-runtime-and-headers-bridge/` (README.md + SPEC.md + PLAN.md + PROGRESS.md + REVIEW.md DONE at predecessor commits). Parent directory `docs/envoy-go/phases/25-http-filter-wasm/` (BRAINSTORM.md + SPEC.md DONE at predecessor commits). Sibling sub-phase directory `25.3-…/` carries pre-created README.md forward-pointer.
- **lifecycle-state:** `phase 25.2 IMPL done; awaiting 25.3 BRAINSTORM (or 25.3 SPEC if BRAINSTORM-skip)` (SKILL_ROUTING state 4 → state 1 transition for sub-row 25.3 — 25.2 IMPL atomic-landing complete + all 6 phase-done gates green + 4 NEW ADR §Decision+§Consequences bodies anchored + ADR-0202 §Consequences AMEND acknowledgment landed + ADR-0209 STAYS UNCONSUMED + REVIEW.md authored per `superpowers:requesting-code-review`). ROADMAP row 25 STAYS `in-progress`; sub-row 25.1 STAYS `done`; sub-row 25.2 flips `in-progress → done` at this 25.2 IMPL phase-done per ADR-0106 per-cell IMPL-anchored lifecycle annotation; sub-row 25.3 STAYS `planned`. Parent row 25 flips `in-progress → done` at 25.3's phase-done.
- **next-skill:** `superpowers:brainstorming` (scoped to **25.3 BRAINSTORM**) per the per-sub-phase BRAINSTORM-before-SPEC discipline + the parent §10.3 + 25.1 SPEC §10.3 anticipated 25.3 ADRs roster + the parent BRAINSTORM-settled-enough threshold. 25.3 BRAINSTORM scope per the 25.2 BEHAVIOR_CONTRACT.md `### Phase 25.2 forward-pointer notes` hand-off list: per-route `typed_per_filter_config` 5th-canonical REUSE-by-absence per AMEND-A3 (anticipated NO §(xvi) AMENDMENT — ADR-0125 STAYS at 10 canonicals); multi-plugin VM-sharing semantics (`vm_id`-keyed VM reuse + plugin-context isolation); `VmConfig.environment_variables` activation (lifts the 25.2 PARSE-REJECT for `environment_variables` per AMEND-A6 deferral); `failure_policy = FAIL_RELOAD` activation + `fail_open` deprecated mapping + Group-C `vm_reload*` counter triplet; conformance harness seed at `test/conformance/proxy-wasm/` per AMEND-A8 (starting threshold 62.5% — 10/16 test families per `proxy-wasm-cpp-host@da3ce05d:test/` empirical pin); ADR-0207 EXPLICIT API-REVISION ALLOWANCE for consumer #3+ (rbac filter-state read; ext_authz filter-state inject; ext_proc filter-state pass-through; new filter families); ADR-0209 R8 escape-valve carries forward to 25.3 IMPL slot — may re-evaluate against per-route + multi-plugin VM-sharing surface (pooled-Module vs shared-Module-with-mutex-serialization decision if benchmark threshold trips at 25.3 IMPL). Alternative: `superpowers:writing-plans` if 25.3 BRAINSTORM is skipped per parent-BRAINSTORM-settled-enough pattern (the parent §10.3 anticipated ADRs roster + 25.1 + 25.2's empirical evidence may suffice; skip judgment at session-start time).
- **last-commit:** `<TBD-25.2-IMPL-SQUASH>` — `Squash merge phase-25.2-http-filter-wasm-body-and-advanced-bridge-impl`. The phase-25.2 IMPL squash-merge commit lands the 22-task IMPL atomic-landing per the 25.2 PLAN's 22-task TDD task graph + 6-tier structure + STATE.md re-advance + ROADMAP row 25.2 flips `in-progress → done` per ADR-0106 per-cell IMPL-anchored lifecycle annotation + REVIEW.md authored per `superpowers:requesting-code-review` + 4 NEW ADR §Decision+§Consequences bodies (ADR-0205+0206+0207+0208) + ADR-0202 §Consequences one-line AMEND acknowledgment + ADR-0209 STAYS UNCONSUMED. SHA filled in at stage-close follow-up commit per the phase-22.2 + phase-22.3 + phase-23 + phase-24.1 + phase-24.2 + phase-25-parent + phase-25.1-SPEC + phase-25.1-PLAN + phase-25.1-IMPL + phase-25.2-BRAINSTORM + phase-25.2-SPEC + phase-25.2-PLAN stage-close SHA-fill precedent. Predecessor master tip (pre-25.2-IMPL): `defdc9b` — `Squash merge phase-25.2-http-filter-wasm-body-and-advanced-bridge-plan`.
- **last-updated:** 2026-05-26
- **next-free ADR:** `ADR-0209` UNCHANGED (4 NEW ADR §Decision+§Consequences bodies landed at this commit — ADR-0205 + ADR-0206 + ADR-0207 + ADR-0208 — but the §Context drafts already existed at the 25.2 SPEC commit `f0eae39`, so DECISIONS.md tail STAYS at ADR-0208; ADR-0202 §Consequences AMEND acknowledgment consumes NO new ADR number per 25.2 SPEC §10.2). ADR-0209 reserved at the 25.2 SPEC commit per STRENGTHENED-WEAK-HOLD-with-1-slot per Q10 — STAYS UNCONSUMED at 25.2 IMPL phase-done (R8 benchmark gate disposition WEAK-default per Task 22 `BenchmarkPerStreamModule_Instantiation = ~98 ns/op` << 1ms threshold); carries forward to 25.3 IMPL escape-valve slot per the R8 signaling protocol. Anticipated next-free after 25.3 phase-done: **`ADR-0212`** if all 25.3 reserves UNCONSUMED (matching the parent SPEC §1.2 + BRAINSTORM §7.4 estimate of ADR-0202..ADR-0213 across the family — minus 1 if the 25.3 escape-valve reserve goes UNCONSUMED, plus 1 if ADR-0209 fires at 25.3); 25.3 anticipated ADRs ADR-0209 (or +1) per-route 5th-canonical REUSE-by-absence EXPLICIT-NO-NEW-CANONICAL classification per AMEND-A3 + ADR-0210 (or +1) multi-plugin VM-sharing semantics + ADR-0211 (or +1) conformance harness pin + escape-valve ADR-0212 (or +1) reserve for any 25.3-IMPL-time-unanticipated surface. **D-hypothesis disposition: STRENGTHENED-WEAK-HOLD-with-1-slot-buffer HOLDS** at this 25.2 IMPL phase-done — 4 anticipated NEW ADR §Decision+§Consequences bodies landed cleanly at Task 22; 0 escape-valve slot consumption from the R8 benchmark surface per D-P-PLAN-11.

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
