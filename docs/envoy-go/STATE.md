# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `03-tls`
- **phase-directory:** `docs/envoy-go/phases/03-tls/` — exists; contains `SPEC.md` (consistent with ADR-0030's TLS_AUTO mapping per the I-3 fix in commit `98cc35b`), `PLAN.md`, `PROGRESS.md` (16 task entries + two verification blocks: the original state-4 sweep at `71068a2` and the post-REVIEW-follow-ups state-4 sweep at `cbfe275`), and `REVIEW.md` (committed `d45c467` — verdict APPROVED WITH FOLLOW-UPS, 0 Critical, 4 Important, 8 Minor; the four Important items I-1..I-4 are landed in `98cc35b` and the post-fix gate sweep is now committed in `cbfe275`). All artifacts on branch `phase/03-tls-impl` under `.worktrees/phase-03-tls-impl`. Master remains at `aff6fbf` and is currently 4 commits behind impl (`d45c467`, `98cc35b`, `1351a01`, `cbfe275`).
- **lifecycle-state:** `5` — verified, post-REVIEW-follow-ups. Every SPEC §3 / BOOTSTRAP §7.5 phase-done gate that is in scope at this state was re-run from a fresh session by `superpowers:verification-before-completion` and quoted verbatim into PROGRESS.md (commit `cbfe275`): (a) `0002-tls-tcp` PASS 1.14s; (b) `0000-tcp-echo` PASS 1.08s + `0001-tcp-proxy-rr` PASS 1.12s (regression-clean across ADR-0032/0033/0034 and the I-1..I-4 follow-ups); (c) N/A — phase 03 ships no conformance suites; (d) `FuzzTLSContextParse` PASS 31.037s + `FuzzBootstrapLoad` PASS 31.076s + `FuzzTcpProxyFilter` PASS 31.046s, all at ADR-0018's 30s budget — no crashes, no `testdata/fuzz/` seed-corpus drift (in fact no `testdata/fuzz/` directories exist anywhere in the repo, per the budget-only ADR-0018 discipline); (e) `go build`, `go vet`, `golangci-lint run`, `go test -count=1 -timeout=10m ./...` all exit 0; (e′) `0002-tls-tcp/pki/gen` re-run produces a byte-identical PEM tree (`git diff --exit-code pki/` exit 0). Differential suite ran against pinned reference Envoy `envoyproxy/envoy@sha256:c5e8a68e52f4d4697a9adb280dbe415d77fedf1257e183dcb86205bd438f18bd` (`v1.37.2`, per `ENVOY_TARGET.md` / ADR-0008). HEAD at verification time was `938273e` (the state-4 STATE.md commit; the verified code is `1351a01`'s tree, since `938273e` is a pure STATE.md edit on top of it). State-machine flow this session: state 4 (impl complete inclusive of REVIEW.md follow-ups, awaiting fresh re-verification) → state 5 via verification (one forward move this session per §5.1). No deviations. No new ADRs. The 8 Minor REVIEW findings (M-1..M-8) remain deferred to phase 04+ triage. (f) `REVIEW.md` approval is the gate that distinguishes state 5 from state 6 and is the entire scope of state 5 — `REVIEW.md` (`d45c467`) is already approved with the four follow-ups now landed and re-verified; whether that constitutes "state 5 done" or warrants a confirming addendum is the §5.2 judgment call described in `next-skill-scope` below.
- **next-skill:** `superpowers:finishing-a-development-branch`
- **next-skill-scope:** Resolve the BOOTSTRAP §5 state-5 → state-6 judgment call, then execute state-6 finalisation. The judgment: `REVIEW.md` (`d45c467`) returned **APPROVED WITH FOLLOW-UPS** (0 Critical / 4 Important / 8 Minor). The four Important items (I-1..I-4) landed in `98cc35b` and the post-fix gate sweep is now committed in `cbfe275`. By BOOTSTRAP §5's state-detection rule ("for `REVIEW.md`, its approval status"), an approved REVIEW satisfies state 5 and the natural transition is state 6. Two paths exist; the next session picks one:
    - **Path B (recommended): treat REVIEW.md as fully satisfied → finalise.** Justification: (i) all four Important items are landed and re-verified green; (ii) I-1 / I-3 / I-4 are doc/comment/dead-code edits and I-2 is an error-string fix shipped with a TDD red→green regression test (`TestNewManager_MultiChain_NoSNIMatch` `strings.HasPrefix(err.Error(), "listener:")` assertion); none touched runtime semantics; (iii) the BOOTSTRAP §5.2 re-entry rule says "if issues → back to step 3 until REVIEW.md approved" — REVIEW.md is approved, no fresh review is mandated; (iv) the reviewer's own recommendation in `REVIEW.md` even suggested the four items could ship as the first commit of phase 04, an explicitly weaker bar than landing them inside phase 03 as we did. Under Path B, invoke `superpowers:finishing-a-development-branch` to (1) compose the state-6 phase-done commit per §5.3 with format `phase 03: <title> [ADR-0029, ADR-0030, ADR-0031, ADR-0032, ADR-0033, ADR-0034, ADR-0035, ADR-0036]` (eight ADRs landed during impl per `PROGRESS.md`), summary 1–3 sentences, `Differential surface:` row listing `0000-tcp-echo`/`0001-tcp-proxy-rr`/`0002-tls-tcp` all green, `Conformance:` row stating "N/A — phase 03 ships none"; (2) flip `ROADMAP.md` row for phase 03 from in-progress to `done`; (3) advance `STATE.md` to `lifecycle-state: 6` then immediately to phase 04's state 0/1 (consult `ROADMAP.md` and `BOOTSTRAP_PROMPT.md` §8 for the next phase title/scope); (4) fast-forward `master` to the impl tip per ADR-0003 so master and `phase/03-tls-impl` share HEAD again. Note that finalisation is multiple commits and may exceed §5.1's "one forward state move per session" — the §5.3 phase-done commit IS the state-6 deliverable, so it's all part of one state transition.
    - **Path A (alternative): confirming addendum review.** Justification: strict reading of "state 5 → `superpowers:requesting-code-review`" from §5 of BOOTSTRAP. If the next session prefers this, invoke `superpowers:requesting-code-review` against the diff `d45c467..cbfe275` (which is exactly the I-1..I-4 follow-ups + the verification block) with scope narrowed to "do these four fixes match what REVIEW.md recommended". Expected outcome is APPROVED with no new findings since I-1..I-4 are literal text changes to specific lines REVIEW.md called out. After the addendum REVIEW lands, proceed to Path B's state-6 steps (§5.3 commit, ROADMAP flip, STATE advance, master FF).
   Either path: work from the impl worktree (`/home/esa/git/envoy-go/.worktrees/phase-03-tls-impl`). The 8 Minor REVIEW findings (M-1..M-8) carry forward to phase 04+ triage in the same form as phase-02's REVIEW Minors 5/7 carried into phase 03 — no fix is required at state 6.
- **last-commit:** cbfe275
- **last-updated:** 2026-04-24

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
