# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `phase 26.1 PLAN done; awaiting 26.1 IMPL at 2026-05-30 (this commit)` — **phase 26.1 (`network-filter-chain-framework-and-echo`) PLAN DONE at 2026-05-30 (this commit)**. The PLAN (`docs/envoy-go/phases/26.1-network-filter-chain-framework-and-echo/PLAN.md`, ~1400 lines, **17 bite-sized TDD tasks**) decomposes the 26.1 SPEC §10 task spine; plan-document-reviewer APPROVED after 2 iterations (the fixture tasks 14/15/16 were rewritten from a fabricated harness model to the REAL `fixture.Driver`/`harness.BootRejectFixture` API + `init()`-registration + `runner_test.go` blank-import; the fuzzer-count gate was hardened to `git ls-files`; the `NewManagerWithBaseDirAndAllowH2C` ctor-caller blast radius — admin tests + manager_test — was added; the direct_response RCD sink was pinned in Task 6). **ADR-0045 split-gate CHECKED: NO split** (17 tasks / ~850-1020 net-new LoC, well within ~25 tasks / ~1500 LoC). **PLAN-time D-questions RESOLVED:** D-P26.1-1 = drainable `*Buffer`; D-P26.1-2 = INLINE the DataSource 4-arm switch in `directresponse` modeled on `internal/tls/datasource.go:loadDataSource(ds, baseDir)` (NO shared `internal/datasource` package exists) + REFINE `network.FactoryCtx` to carry `BaseDir` (SPEC §3.1 said empty; direct_response's Filename arm needs boot-time baseDir for byte-stable boot-reject parity). D-P26.1-3/4/5 remain IMPL-time (byte-stable wording / boot-reject arm / close-detection + RCD storage), scoped into PLAN Tasks 8/9/11/16. **Earlier (SPEC stage, unchanged):** phase 26.1 SPEC DONE at 2026-05-30. The §9 Network-filters family stays OPEN (parent row 26 `in-progress`). The 26.1 SPEC (`docs/envoy-go/phases/26.1-network-filter-chain-framework-and-echo/SPEC.md`, ~430 lines) was authored per the phase-22.1/25.1 per-sub-phase-SPEC precedent: it INHERITS the phase-26 parent SPEC's §5 proto roster + §6 PARSE-REJECT roster + §7 stat surface + §8 fixture taxonomy + §11 empirical-pin block + §12 D-questions + §13 RATIFIED-PENDING items and refines per-Task-level surface only (it does NOT re-execute the empirical pins). It anchors: (1) the NEW `internal/filter/network/` read-filter chain framework — §3.1 production API signatures (two-value `Status` enum; `ReadFilter` interface with `OnNewConnection`/`OnData`/`SetReadFilterCallbacks`/`OnDestroy`; `ReadFilterCallbacks` with `Connection()`/`ContinueReading()`/`DynamicMetadata() *dynamicmetadata.Bucket`; the drainable `*Buffer`; the `CloseType` enum `FlushWrite`/`NoFlush`; the two-step factory; the freeze-after-boot `*Registry`), §3.3 chain-runner iteration semantics, §3.4 per-connection runtime context owning the REUSED `*dynamicmetadata.Bucket` (parent AMEND-A5); (2) `echo` + `direct_response` first-consumer code shapes (§4); (3) the §3.5 **dual-dispatch** in `manager.go` (D-P2 RESOLVED: the new read-filter chain path lands FULLY at 26.1 ALONGSIDE the untouched `tcp_proxy`/HCM terminal-filter path — a `chainInfo.netChainFactory` field + a build-time pre-check on `filters[0].type_url` + a `serveConnection` dual-branch + a new `serveReadFilterChain` read loop); (4) the §3.6 `cmd/envoy-go/main.go` boot-wiring (new `netReg *network.Registry` constructor arg). D-S1 baselines VERIFIED at this SPEC commit (§11.1): fuzzers **34** (35th lands at 26.1 IMPL), differential fixtures **41** / numbering tail **0039** (26.1 lands 0040/0041/0042 → 44), stat surface **132** (stays 132), DECISIONS.md tail **ADR-0214**. spec-document-reviewer APPROVED (no load-bearing issues; 4 advisory nits folded in — line-ref +1 fixes, dynamicmetadata cite harmonized, `KnownTypeURLs`-mirrors-http-registry note, response-code-details storage-site D-question). Phases 00–25 + phase-26 parent SPEC + phase-26.1 SPEC DONE and CANONICAL.
- **phase-directory:** `docs/envoy-go/phases/26.1-network-filter-chain-framework-and-echo/` (SPEC.md + PLAN.md authored — DONE; PROGRESS.md / REVIEW.md land at the 26.1 IMPL / REVIEW sessions). The parent directory `docs/envoy-go/phases/26-network-filter-chain-and-rbac/` (BRAINSTORM.md + parent SPEC.md — both DONE) stays canonical. Sub-phase directories `26.2-network-filter-registry-migration/` + `26.3-network-filter-rbac/` are created at each sub-phase's SPEC entry per the per-sub-phase convention (git does not track empty dirs).
- **lifecycle-state:** `phase 26.1 PLAN done (SKILL_ROUTING state 2 → state 3 for sub-row 26.1); awaiting 26.1 IMPL` (SKILL_ROUTING state 3 for sub-phase 26.1 — 26.1 SPEC.md + PLAN.md exist; implementation incomplete). The 26.1 IMPL session is the next lifecycle transition (state 3 → state 4) for sub-row 26.1. ROADMAP sub-row 26.1 STAYS `in-progress` (it flipped `planned → in-progress` at the SPEC commit per ADR-0106; it flips `in-progress → done` at 26.1 IMPL phase-done). Parent row 26 STAYS `in-progress`; it flips `in-progress → done` at 26.3 phase-done via the ROLLUP precedent (18/19/22/24/25).
- **next-skill:** `superpowers:subagent-driven-development` (scoped to **26.1 IMPL** — execute the 17-task PLAN, fresh subagent per task, two-stage review between tasks, per the `feedback_execution_style` memory; subagents commit local-only per `feedback_subagents_no_push`, controller pushes at stage-close). The IMPL Task-1 is a HARD GATE: re-pin the D-S1 baselines (fuzzers 34, fixtures 41 / tail 0039, stats 132, ADR-0214) via `git ls-files` + re-pin the `manager.go` line numbers before asserting deltas. After 26.1 IMPL → verification → review per SKILL_ROUTING states 4/5.
- **last-commit:** `f1172a7` — `Squash merge phase-26.1-network-filter-chain-framework-and-echo-plan`. The phase-26.1-PLAN squash-merge commit lands `docs/envoy-go/phases/26.1-network-filter-chain-framework-and-echo/PLAN.md` + this STATE.md advance + the next-prompt.txt rewrite for the 26.1 IMPL cold-start. SHA corrected from a wrong stage-close value (`9252873`, never existed) to the actual squash SHA `f1172a7` at the 26.1-PLAN-review-fix follow-up commit. Predecessor: `ea77585` (phase-26.1-SPEC squash; tip-repoint trail `50ede88`/`37d8be1`/`1428eab`).
- **last-updated:** 2026-05-30
- **next-free ADR:** `ADR-0215` (UNCHANGED at the 26.1 SPEC — a per-sub-phase SPEC consumes NO new ADR number; ADR-0213 + ADR-0214 §Context drafts already landed at the parent SPEC, and their §Decision/§Consequences bodies land at 26.1 IMPL per ADR-0044; DECISIONS.md tail STAYS **ADR-0214**). Provisional remaining family span (drafted at their own sub-phase SPECs): 26.2 ADR-0215 (tcp_proxy/HCM migration + hardcoded-registry retirement); 26.3 ADR-0216 (`internal/rbac/` extraction) + ADR-0217 (connection-metadata sink as `internal/dynamicmetadata/` connection-scoped reuse) + ADR-0218 (`rbac_network` filter). Anticipated next-free after phase-26 phase-done: `ADR-0219`. ADR-0209 escape-valve reserve carried from the §9 family STANDS-UNCONSUMED.

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
