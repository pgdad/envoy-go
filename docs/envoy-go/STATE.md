# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `05.1-downstream-h2`
- **phase-directory:** `docs/envoy-go/phases/05.1-downstream-h2/` — contains `README.md` (placeholder, written at split time per ADR-0045 / commit `2162509`), `SPEC.md` (drafted by brainstorming per ADR-0004 autonomous mode; commit `4b45941`), `PLAN.md` (drafted by writing-plans per ADR-0005 autonomous mode; commit `634bcb6`; 3331 lines, 16 tasks, 8 new ADRs), and `PROGRESS.md` (executor-appended through Tasks 1–16 with final SHA-fills at `b416664`; the prior verification-block at `df85f85` records the gate-(e) bounce; the Task 17 entry at `9aa557a` records the state-3 follow-up cleanup and the re-verification of all five non-deferred SPEC §3 gates green at HEAD `65d2574`; the new `## Verification (lifecycle-state 5)` block at `2237e6e` records the second mandatory verification-before-completion run at HEAD `2cf3458` with all five non-deferred gates GREEN, lint=0 holding, h2spec 53/53 holding, six fuzzers PASS at 30s budget — promoting lifecycle-state 4 → 5). The sibling `docs/envoy-go/phases/05.2-upstream-h2/` is unchanged from the split commit (placeholder `README.md` only). The parent `docs/envoy-go/phases/05-http-2/` retains its `SPEC.md` (`612cdea`) as the master design document for both sub-phases per ADR-0045 and phase-05 SPEC §11.1; that directory remains closed read-only history at the parent level (no PLAN/PROGRESS/REVIEW will land under it; those land under the sub-phase directories).
- **lifecycle-state:** `5` — promoted from 4 by this verification-before-completion session, branched from STATE.md commit `2cf3458` (master tip per ADR-0003) into `.worktrees/phase-05.1-downstream-h2-verify-2/` on branch `phase/05.1-downstream-h2-verify-2`. Five of six SPEC §3 gates re-verified GREEN at HEAD `2cf3458` from a fresh terminal: (a) vacuously green per ADR-0045 (no new differential fixture in 05.1); (b) `0000-tcp-echo` / `0001-tcp-proxy-rr` / `0002-tls-tcp` / `0003-http11-routing` all PASS, plus `go test ./...` and `go test -race ./...` clean across all 15 packages with tests (no data races); (c) h2spec 53/53 PASS at the ADR-0051 pinned `summerwind/h2spec@sha256:5f4a65c30cae8569558ced048b4bfe0dcf01a221e36767ae504ccd8348a7aeb0` (per-section breakdown identical to Task 17's `9aa557a` run: sections 3.5 / 4.1 / 4.2 / 4.3 / 5.1 / 5.1.1 / 5.1.2 / 5.3.1 / 5.4.1 / 5.5 / 7 / 8.1 / 8.1.2 / 8.1.2.1 / 8.1.2.2 / 8.1.2.3 / 8.1.2.6 / 8.2); (d) all six fuzzers PASS at 30s budget per ADR-0018 with `git status --porcelain` empty after each — `FuzzBootstrapLoad` 782,549 execs, `FuzzTcpProxyFilter` 3,694,680 execs, `FuzzTLSContextParse` 5,188,444 execs, `FuzzHCMConfigParse` 3,400,923 execs, `FuzzFrameStream` 13,432,505 execs, `FuzzHPACKDecode` 2,030,062 execs (no testdata/fuzz pollution; corpus discipline preserved); (e) `go build` / `go vet` clean, ADR-0046 boundary grep clean (3 prod hits in `conn.go` / `framer.go` / `settings.go` — all in the 5 allowed files; `hpack.go` uses the http2/hpack sub-package; `stream.go` has no direct import), ADR-0048 `client.go` absence verified, **`golangci-lint run ./...` exits 0 with zero issues** (lint=0 invariant from Task 17's cleanup holds at the new HEAD). Gate (f) — `REVIEW.md` — remains state-5→state-6 work (`superpowers:requesting-code-review`), the next session's responsibility per BOOTSTRAP §5.
- **next-skill:** `superpowers:requesting-code-review` (state-4→state-5 promotion was this verification session at `2237e6e`; gate (f) is the state-5→state-6 transition per BOOTSTRAP §5). The review session should branch a fresh `phase/05.1-downstream-h2-review` (or similar) worktree from this STATE.md commit per ADR-0003; the verify-2 worktree at `.worktrees/phase-05.1-downstream-h2-verify-2` is closed-history at this state transition (its work landed in `2237e6e` + this STATE.md commit).
- **next-skill-scope:** Author `docs/envoy-go/phases/05.1-downstream-h2/REVIEW.md` per `superpowers:requesting-code-review`, scoped to the entire phase 05.1 surface — code under `internal/filter/hcm/h2/`, the ALPN dispatcher in `internal/filter/hcm/filter.go`, codec-neutral `directResponseAction` factoring in `internal/filter/hcm/actions.go`, the `--allow-h2c` CLI flag plumbing, the new `test/conformance/h2spec/` driver, the `BEHAVIOR_CONTRACT.md` `## HTTP/2` SCAFFOLD subsection, the new `docs/envoy-go/CONFORMANCE_PINS.md` sibling pin file, and the eight new ADRs (ADR-0046 through ADR-0053). Reading order for the review session: this STATE.md, `phases/05.1-downstream-h2/SPEC.md` (full), `phases/05.1-downstream-h2/PLAN.md` (full), `phases/05.1-downstream-h2/PROGRESS.md` (full — Tasks 1–16, verification at `df85f85`, Task 17 cleanup at `9aa557a`, verification-state-5 at `2237e6e`), `BOOTSTRAP_PROMPT.md` §5 state-5, `BEHAVIOR_CONTRACT.md` `## HTTP/2`, `DECISIONS.md` ADR-0042 / ADR-0044 / ADR-0045 / ADR-0046 / ADR-0047 / ADR-0048 / ADR-0049 / ADR-0050 / ADR-0051 / ADR-0052 / ADR-0053. Surface for the reviewers' explicit attention: (i) the Task 17 "Carry-forward observation" — `hpackBlocked bool` at `internal/filter/hcm/h2/conn.go:37` is dead code paired with the deleted `hpackBlockSID`, never written `true`, the read at `conn.go:236` always observes `false`; the guard block at `conn.go:227-250` is unreachable; ordering enforcement is owned by `golang.org/x/net/http2.Framer.checkFrameOrder` per ADR-0046; this is the recommended REVIEW.md venue for the carry-forward; (ii) the BEHAVIOR_CONTRACT `## HTTP/2` SCAFFOLD's documented "Does not yet apply to" boundary against 05.2 deliverables (routed-to-upstream H2 + fixture 0004); (iii) the phase-04 REVIEW Minor carry-forward triage (M-2/M-4/M-5/M-6/M-7) per ADR-0053, all dispositions textual/cosmetic + a forward-looking "phase-06-must-consume" tag. If the review approves, lifecycle-state advances to 6 and the next session writes the final phase commit and ROADMAP status update per BOOTSTRAP §5 state-6 / §5.3 commit-message format. If the review surfaces issues, the lifecycle re-enters at state 3 (REVIEW re-entry, NOT state 4 — per BOOTSTRAP §5.2).
- **last-commit:** `2237e6e` — the `## Verification (lifecycle-state 5)` PROGRESS append on branch `phase/05.1-downstream-h2-verify-2`, +188 lines, ONLY-PROGRESS.md change (no code, test, fixture, or STATE.md change). This STATE.md commit (the lifecycle-state 4→5 promotion commit on the same branch) is the 2nd commit on `phase/05.1-downstream-h2-verify-2`. Master is fast-forwarded to this STATE.md commit per ADR-0003.
- **last-updated:** 2026-04-26

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
