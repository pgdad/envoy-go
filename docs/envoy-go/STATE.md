# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `06.1-stats-prometheus`
- **phase-directory:** `docs/envoy-go/phases/06.1-stats-prometheus/` — contains `SPEC.md` (at `ad46930`, frozen), `PLAN.md` (at `343b198`), `PROGRESS.md` (full Task-1-through-Task-15 history + L4 FAIL block at `1f94b74` + gate-(d) fix block at `79be6b0` + L4 PASSED re-verification block at `1ed6cd0` + L4 REVIEW-followup re-verification block at `5ddef1b`), and `REVIEW.md` (`59d86f2` — verdict APPROVED WITH FOLLOW-UPS, 0 Critical / 2 Important / 12 Minor; SHA-fill at `d21d50b`). Parent `docs/envoy-go/phases/06-observability-baseline/` carries `BRAINSTORM.md` (`d271fd7`) + parent `SPEC.md` (`ad46930`). Sibling `docs/envoy-go/phases/06.2-access-log/` retains the placeholder `README.md` (`ad46930`). Phase 05 closed history unchanged.
- **lifecycle-state:** `5` — REVIEW-followup batch complete and re-verified; SPEC §3 phase-done gates (b)/(c)/(d)/(e) re-run green at HEAD `665c879` per the L4 REVIEW-followup re-verification block at `5ddef1b`. The REVIEW (`59d86f2` / SHA-fill `d21d50b`) returned APPROVED WITH FOLLOW-UPS with zero Critical, two Important (I-1 SPEC §14 line 715 file-path mismatch — M-9 fix landed at `h2dispatch.go` not `router_action.go`; I-2 ROADMAP row 06.1 still `planned`), and twelve Minor findings; the followup batch on `phase/06.1-stats-prometheus-review-followup` closed M-1 (cluster-name latent vulnerability — symmetric to ADR-0065's HCM `stat_prefix` defect; explicitly forward-flagged in ADR-0065 Consequences (d)) across two commits — `caa58e5` (boundary guard at `buildCluster` + 6-case `TestNewManager_ClusterNameInvalidChars` regression test mirroring `TestParseFilter_StatPrefixInvalidChars` at `internal/filter/hcm/config_test.go:221`; inherits ADR-0065 by reference, no new ADR) and `665c879` (golangci-lint misspell fixup `minimised → minimized` on the test docstring); `5ddef1b` re-verified gates (b)/(c)/(d)/(e) green and lifted STATE to lifecycle-state 4. REVIEW Importants I-1 and I-2 closed without code/doc changes on this branch (I-1 by REVIEW + PROGRESS Task 11 deviation #1 as-corrigendum; I-2 by the next-session phase-done commit's natural `planned → done` row flip per the 05.2 `0c01ed6` one-step precedent). The 12 collapsed Minors carry forward to the L4 review-followup batch (separate post-phase-done branch). Gate (f) closed by REVIEW.md `59d86f2` + the PROGRESS verification block addendum. Per BOOTSTRAP §5 the next transition is state 5 → 6 via the phase-done commit which flips ROADMAP row 06.1 to `done`.
- **next-skill:** `superpowers:subagent-driven-development` — drive the lifecycle-state 5 → 6 phase-done commit. Required edits in a single commit: (1) flip `docs/envoy-go/ROADMAP.md` row 06.1 status `planned` → `done` (one-step per the 05.2 `0c01ed6` precedent + REVIEW I-2 disposition; this also closes I-2 by removing the SPEC §4.4-vs-ROADMAP mismatch); (2) update `docs/envoy-go/STATE.md` `lifecycle-state` to `6` (or directly to `0` for phase 06.2 entry per BOOTSTRAP §5 step 6 → step 1 transition) with `active-phase` advanced to `06.2-access-log` per ROADMAP row 06.2's title; (3) preserve row 06 status `in-progress` (parent row only flips at 06.2's phase-done — see ROADMAP row 06.2's "Closes the parent row `06` at its phase-done commit" note; this mirrors the 05/05.1/05.2 closure pattern); (4) preserve row 06.2 status `planned`. The commit subject MUST name every ADR introduced in 06.1 per BOOTSTRAP §5.3: `phase 06.1: phase-done — stats-prometheus lands; ROADMAP row 06.1 → done [ADR-0059, ADR-0060, ADR-0061, ADR-0062, ADR-0063, ADR-0064, ADR-0065]` (seven ADRs total — ADR-0059 through ADR-0064 anticipated by SPEC §8 + ADR-0065 the gate-(d) fix bonus per the gate-(d) fix block at `79be6b0`). The commit body MUST cite REVIEW.md's verdict APPROVED WITH FOLLOW-UPS, explicitly note the I-1 PROGRESS-as-corrigendum acknowledgment (the M-9 fix at `internal/filter/hcm/h2dispatch.go` + `h2dispatch_test.go` not at `internal/filter/hcm/h2/router_action.go` + `router_action_test.go` per PROGRESS Task 11 deviation #1; SPEC §14 line 715 stays as-written per SPEC immutability), document the I-2 one-step `planned → done` resolution, summarise the 06.1 differential surface (fixture `0005-prometheus-stats` non-vacuous gate (a); 13 unique Prometheus names; per-counter delta-equality + per-gauge snapshot-equality), and acknowledge the 12-Minor carry-forward to the L4 review-followup batch.
- **next-skill-scope:** Single phase-done commit on this branch: flip ROADMAP row 06.1, update STATE.md `lifecycle-state` 5 → 6 (or 5 → 6 → 0 per BOOTSTRAP §5 step 6 → step 1), advance `active-phase` to `06.2-access-log`, and write the commit message naming all seven 06.1 ADRs (ADR-0059/0060/0061/0062/0063/0064/0065) per BOOTSTRAP §5.3. After this commit, phase 06.1 is done and phase 06.2 brainstorm is the next session's lifecycle-state 0/1 cold-start (`next-skill: superpowers:brainstorming` per BOOTSTRAP §5 step 1; worktree convention `.worktrees/phase-06.2-access-log-brainstorm` branched from the phase-done commit's HEAD per ADR-0003 + per-phase-worktree convention). Phase 06.2's brainstorm session inherits: (a) the four ADR-0058 / 05.2 carry-forwards (M-4 readClientPreface ctx-awareness, M-10 SETTINGS_TIMEOUT absent, M-12 closedStreams map unbounded, the seven 05.2 REVIEW Minors); (b) the 06.1 12-Minor carry-forward queue from the L4 review-followup batch (logged in REVIEW.md §Findings/Minor — M-2 through M-12 cosmetic / observability minors plus the post-phase-done batch coverage); (c) the M-1 closure precedent from this followup branch (the cluster-name guard pattern is now established; future filter / extension authors that introduce metrics derived from user input must validate at their input boundary using `stats.IsValidName` per ADR-0065 Consequences (e)); (d) the post-06.1 fuzzer count of 7 (`FuzzPromTextFormat` is now a phase-done invariant). Per ADR-0003 the followup branch + phase-done branch fast-forward into `master` at the phase-done commit. Re-entry: this is the terminal lifecycle-state pair (5 → 6) for phase 06.1; no further state-3 re-entry is anticipated.
- **last-commit:** `TBD` — `phase 06.1 review-followup: STATE.md → lifecycle-state 5 (3 → 4 → 5)`, on branch `phase/06.1-stats-prometheus-review-followup`. Promotes lifecycle-state 4 → 5 (re-verification at HEAD `665c879` captured in the PROGRESS verification block addendum at `5ddef1b` — all five non-deferred SPEC §3 gates GREEN; gate (a) implied by gate (b)'s six-fixture sweep including `0005-prometheus-stats`; gate (f) closed by REVIEW.md `59d86f2`). No code or PROGRESS edits; STATE-only commit mirroring the 05.2 `5c0f3cc` precedent. The next commit is the state-6 phase-done commit per BOOTSTRAP §5.3, naming ADR-0059/0060/0061/0062/0063/0064/0065 plus ROADMAP row 06.1 → done. SHA-fill follow-up per phase-02..05.2 convention.
- **last-updated:** 2026-04-28

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
