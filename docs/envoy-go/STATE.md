# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `04-http-1.1`
- **phase-directory:** `docs/envoy-go/phases/04-http-1.1/` — exists; contains `SPEC.md`, `PLAN.md`, `PROGRESS.md` (18 task entries plus TWO verification blocks: the original "## Verification (lifecycle-state 4)" sweep at `7649a19` for the pre-follow-ups surface HEAD `13d0373`, and a new "## Verification (lifecycle-state 4 — post-follow-ups)" sweep at `c315cb9` for the post-follow-ups surface HEAD `b24578a`; Task 18 commit `671a059` is the substantive change between the two), and `REVIEW.md` (committed `04527eb` — verdict APPROVED WITH FOLLOW-UPS, 0 Critical, 4 Important, 7 Minor; the four Importants plus M-1 are landed in `671a059` and the post-fix gate sweep is committed in `c315cb9`; the original APPROVED verdict stands — no new REVIEW.md is required since the follow-ups address exactly what it prescribed, mirroring the in-phase precedent set by phase 03's `98cc35b` + `cbfe275` pair). All artifacts on branch `phase/04-http-1.1-impl` under `.worktrees/phase-04-http-1.1-impl`. Phase 03's directory at `docs/envoy-go/phases/03-tls/` is closed and read-only history. Master remains at `aff6fbf` and is currently behind impl by every phase-04 commit.
- **lifecycle-state:** `5` — verified, post-REVIEW-follow-ups. Every SPEC §3 phase-done gate that is in scope at this state was re-run from a fresh session by `superpowers:verification-before-completion` and quoted verbatim into PROGRESS.md (commit `c315cb9`): (a) `TestDifferential/0003-http11-routing` PASS 1.22s — phase-04 fixture green post-follow-ups, ADR-0044 runner-vs-contract claim still upheld since the 0003 driver was not modified by Task 18 (its body-equivalence partition stays `/health` byte-equal, `/api/v1/N` status-only, `/missing/N` status-only); (b) `TestDifferential/0000-tcp-echo` PASS 1.49s, `TestDifferential/0001-tcp-proxy-rr` PASS 1.19s, `TestDifferential/0002-tls-tcp` PASS 1.20s — pre-existing fixtures regression-free across the I-4 cluster-error-text changes; (c) N/A — phase 04 ships no conformance suites; (d) `FuzzHCMConfigParse` PASS 31.065s, `FuzzBootstrapLoad` PASS 31.088s, `FuzzTcpProxyFilter` PASS 31.056s, `FuzzTLSContextParse` PASS 31.058s — all four at ADR-0018's 30s budget, no crashers, no `testdata/fuzz/` seed-corpus drift (in fact no `testdata/fuzz/` directories exist anywhere in the repo); (e) `go build ./...`, `go vet ./...`, `golangci-lint run ./...`, `go test -count=1 -timeout=5m ./internal/... ./test/helpers ./test/fixtures/... ./cmd/...` all exit 0 / all-OK. Explicit re-confirmations per the verifier instruction: I-1's new `TestRunConnection_UpstreamConnectionCloseClosesDownstream` regression test passes (witnessing that `routerAction.do` returns `errCloseAfterAction` on `resp.Close == true`); I-4's four `TestManager_Error_*` cases (StrictDNS / LogicalDNS / EDS / OriginalDST) all pass against the new "only STATIC clusters supported" error text. Differential suite ran against pinned reference Envoy `envoyproxy/envoy@sha256:c5e8a68e52f4d4697a9adb280dbe415d77fedf1257e183dcb86205bd438f18bd` (v1.37.2, per `ENVOY_TARGET.md` / ADR-0008). HEAD at verification time was `b24578a` (the state-4 STATE.md commit; the verified code is `671a059`'s tree, since `b24578a` is a pure STATE.md edit on top of it; the current HEAD `c315cb9` is the verification block append on top of that — also a pure docs commit). State-machine flow this session: state 4 (impl complete inclusive of REVIEW.md follow-ups, awaiting fresh re-verification) → state 5 via verification (one forward move this session per §5.1). No deviations. No new ADRs. The 6 remaining Minor REVIEW findings (M-2..M-7) carry forward to future-phase REVIEW triage. (f) `REVIEW.md` approval is the gate that distinguishes state 5 from state 6 and is the entire scope of state 5 — `REVIEW.md` (`04527eb`) is already approved with the four Important follow-ups + M-1 now landed and re-verified; whether that constitutes "state 5 done" or warrants a confirming addendum is the §5.2 judgment call described in `next-skill-scope` below.
- **next-skill:** `superpowers:finishing-a-development-branch`
- **next-skill-scope:** Resolve the BOOTSTRAP §5 state-5 → state-6 judgment call, then execute state-6 finalisation. The judgment: `REVIEW.md` (`04527eb`) returned **APPROVED WITH FOLLOW-UPS** (0 Critical / 4 Important / 7 Minor). The four Important items (I-1..I-4) plus M-1 landed in `671a059` and the post-fix gate sweep is now committed in `c315cb9`. By BOOTSTRAP §5's state-detection rule ("for `REVIEW.md`, its approval status"), an approved REVIEW satisfies state 5 and the natural transition is state 6. Two paths exist; the next session picks one:
    - **Path B (recommended): treat REVIEW.md as fully satisfied → finalise.** Justification: (i) all four Important items plus M-1 are landed and re-verified green; (ii) I-1 / I-2 ship with TDD red→green regression tests, I-4 ships with tests-red-then-prod-green discipline, I-3 is a timing-window propagation that's not TDD-able per phase-03 `98cc35b` precedent, and M-1 is a one-line orphan deletion in DECISIONS.md; none touched runtime semantics observable to fixture 0003; (iii) the BOOTSTRAP §5.2 re-entry rule says "if issues → back to step 3 until REVIEW.md approved" — REVIEW.md is approved, no fresh review is mandated; (iv) the project precedent is phase 03's `98cc35b` + `cbfe275` pair, where I-1..I-4 + verification block landed in-phase under the same APPROVED verdict and proceeded directly to state 6. Under Path B, invoke `superpowers:finishing-a-development-branch` to (1) compose the state-6 phase-done commit per §5.3 with format `phase 04: HTTP/1.1 [ADR-0037, ADR-0038, ADR-0039, ADR-0040, ADR-0041, ADR-0042, ADR-0043, ADR-0044]` (eight ADRs landed during impl per `PROGRESS.md`), summary 1–3 sentences, `Differential surface:` row listing `0000-tcp-echo` / `0001-tcp-proxy-rr` / `0002-tls-tcp` / `0003-http11-routing` all green, `Conformance:` row stating "N/A — phase 04 ships none"; (2) flip `ROADMAP.md` row 04 from `in-progress` to `done`; (3) advance `STATE.md` `active-phase` to `05-http-2`, `phase-directory` to `docs/envoy-go/phases/05-http-2/` (does not yet exist; created at next state-1), `lifecycle-state` to `1` (per §4.1 invariant 3, since phase 05's row already exists in ROADMAP from bootstrap), `next-skill` to `superpowers:brainstorming`; (4) fast-forward `master` to the impl tip per ADR-0003 so master and `phase/04-http-1.1-impl` share HEAD again. Note that finalisation is multiple commits and may exceed §5.1's "one forward state move per session" — the §5.3 phase-done commit IS the state-6 deliverable, so it's all part of one state transition.
    - **Path A (alternative): confirming addendum review.** Justification: strict reading of "state 5 → `superpowers:requesting-code-review`" from §5 of BOOTSTRAP. If the next session prefers this, invoke `superpowers:requesting-code-review` against the diff `04527eb..b24578a` (which is exactly the I-1..I-4 + M-1 follow-ups + the state-4 STATE.md flip; `c315cb9` is a pure verification-block append) with scope narrowed to "do these five fixes match what REVIEW.md recommended". Expected outcome is APPROVED with no new findings since I-1..I-4 + M-1 are literal text/test changes to specific lines REVIEW.md called out. After the addendum REVIEW lands, proceed to Path B's state-6 steps (§5.3 commit, ROADMAP flip, STATE advance, master FF).
   Either path: work from the impl worktree (`/home/esa/git/envoy-go/.worktrees/phase-04-http-1.1-impl`). The 6 Minor REVIEW findings (M-2..M-7) carry forward to future-phase REVIEW triage in the same form as phase-03's REVIEW Minors (M-1..M-8) carried into phase 04 — no fix is required at state 6.
- **last-commit:** c315cb9
- **last-updated:** 2026-04-25

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
