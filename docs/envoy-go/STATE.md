# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `13-http-filter-buffer` — phase 13 SPEC.md authored at this commit per BOOTSTRAP_PROMPT.md §5 lifecycle-state-1 → 2 transition (skill `superpowers:writing-plans` per ADR-0005, routed through SPEC-authoring step first per the phase 09/10/11/12 precedent). The SPEC mirrors phase 12's structural template section-for-section, adapted for phase 13's surface (1-field listener proto + per-route disabled-OR-override oneof + body-counting + 413-trigger algorithm + zero filter-specific counters). ROADMAP row 13 flips `planned → in-progress` at this commit per BOOTSTRAP_PROMPT.md §4.1 invariant 3. Phase 13 is the SIXTH §9 family-row to reach lifecycle-state-2 (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11, csrf @ 12); the §9 family heading at ROADMAP line 56 stays unchanged per ADR-0106(c).
- **phase-directory:** `docs/envoy-go/phases/13-http-filter-buffer/` — contains BRAINSTORM.md (~798 lines after §12 amendment + advisory-rec follow-up; §§1-11 PRESERVED VERBATIM per D-3.5; §12 supersedes §§1.1, 2.5, 2.6, 2.7, 2.8, 6, 7, 8, 11) + SPEC.md (authored at this commit; ~1343 lines after the §1.1 amendment 5 prose correction + §5.7 scenario-5 walkthrough insertion + advisory polish per the spec-document-reviewer pass). PLAN.md, PROGRESS.md, REVIEW.md authored over remaining phase lifecycle (states 3 → 6). The SPEC commits **3 ADRs** (ADR-0125, ADR-0126, ADR-0127 v2) per §8; ADR-0128 retired per BRAINSTORM §12.7 + SPEC §1.1 amendment 5 (no new stat-table entries; the reviewer-flagged `downstream_rq_too_large` citation issue carried in by the prior STATE.md `§unresolved` row was RESOLVED at this commit via option (a) prose correction landed inline in SPEC §1.1 amendment 5 + §11.5 — `downstream_rq_too_large` and `downstream_rq_completed` are Envoy-only HCM counters NOT in envoy-go's 29-name allow-list, filtered out per the existing twin-series-discipline; envoy-go-side observable is `downstream_rq_4xx` which IS in the 29-name table per phase 06.1). The §11 empirical-pin block executed in the SPEC drafting session ratifies the §12.3 disposition table for all 11 pins (P3, P4, P7, P8, P9, P10, P11 verbatim ratified; P1, P2, P5, P6 ratified with §12 amendment carry-through; 0 new divergences requiring SPEC-level Decision changes beyond the §1.1 amendment 5 prose correction). Differential fixture `0015-http-buffer` 6-request matrix per §7.1 (5 BRAINSTORM §6.2 scenarios + 1 cross-cutting CL-injection assertion per BRAINSTORM §12.6); empirical evidence for the `maybeAddContentLength` chunked → fixed-CL conversion at §11.8-CL is load-bearing for fixture scenario 6.
- **lifecycle-state:** `phase 13 SPEC.md authored; PLAN.md pending` — phase 13 BRAINSTORM.md authored at parent commit `6cf412e`, amended via §12 at parent commit `3915338`, advisory-rec follow-up at parent commit `37d4dfa`, follow-up STATE.md update at parent commit `812d234`. SPEC.md authored at this commit per BOOTSTRAP_PROMPT.md §5 step 1 → step 2 transition + ADR-0004 (autonomous-brainstorming adaptation) + ADR-0005 (autonomous-planning adaptation). Spec-document-reviewer dispatched per ADR-0004 §Decision 3 + writing-plans skill review-loop discipline; verdict **Approved with minor issues found** (1 of max 3 review iterations); 2 issues addressed inline (§1 `filterStats` ghost-removed; §5.7 scenario-5 walkthrough added with §5.8-§5.11 renumbering); 2 advisory polish items applied (§11.9.B → §11.9 P9.B cross-ref normalization; §12 D2 wording correction). Lifecycle-state-2 → 3 transition (BOOTSTRAP_PROMPT.md §5; PLAN.md authored via `superpowers:writing-plans` per ADR-0005) is the next session's responsibility. Phase 12's six gates remain green at base commit `a782fc9`; this commit chain stays docs-only (no code changes; no test re-validation needed).
- **next-skill:** `superpowers:writing-plans` per BOOTSTRAP_PROMPT.md §5 lifecycle-state-2 entry condition (SPEC.md exists at `docs/envoy-go/phases/13-http-filter-buffer/SPEC.md`, PLAN.md does not). The PLAN author follows the phase 09/10/11/12 precedent: parses SPEC §4 (deliverables) + §6 (per-component contracts) + §14 (testing strategy) into ~10-14 PLAN tasks per SPEC §1.4 envelope (well below ADR-0045's 1500 LoC / 25 task split-trigger), runs the plan-document-reviewer subagent loop per ADR-0005 §Decision 1, lands PLAN.md, flips lifecycle-state to `phase 13 PLAN.md authored; implementation incomplete`, sets next-skill to `superpowers:subagent-driven-development` per ADR-0005 §Decision 4 (user's standing preference per project memory).
- **next-skill-scope:** Cold-start scope for the phase 13 PLAN-authoring session. Read first: `docs/envoy-go/phases/13-http-filter-buffer/SPEC.md` (the authoritative input; especially §4 deliverables, §6 per-component contracts, §11 empirical-pin block for context, §12 deferred decisions D1-D4 for PLAN-author-resolved items, §14 testing strategy, §15 acceptance checklist); `docs/envoy-go/phases/13-http-filter-buffer/BRAINSTORM.md` §§1-12 (every load-bearing decision lives there per D-3.4; §12 supersedes affected §1-§11 portions); `docs/envoy-go/phases/12-http-filter-csrf/PLAN.md` (most-recent §9 family-row PLAN; structural template) + `docs/envoy-go/phases/12-http-filter-csrf/PROGRESS.md` (most-recent phase artefact for cross-reference). Highest-leverage SPEC sections for the PLAN author: §4.1 file inventory (drives the per-task scaffolding); §6.4-§6.7 contract bodies (drives the per-task TDD shape); §7.1 fixture matrix (drives the fixture-task sequencing); §8 ADR roster (3 ADRs to land at impl-time; ADR-0125 + ADR-0126 + ADR-0127 v2); §11.5 + §11.8 + §11.8-CL (the load-bearing empirical pins for the body-counting + maybeAddContentLength surface); §13 BEHAVIOR_CONTRACT 4-edit bundle (drives the phase-done Gate F task). Anticipated PLAN.md size: ~700-900 lines / ~10-14 tasks per SPEC §1.4 + phase 12 PLAN's task-count precedent (phase 12 landed at 12 tasks per phase 12 PROGRESS.md).
- **last-commit:** `f5d38fa` — `phase 13: SPEC.md authored; ROADMAP row 13 → in-progress; lifecycle-state-1 → 2 transition`. Authors `docs/envoy-go/phases/13-http-filter-buffer/SPEC.md` (~1343 lines) per BOOTSTRAP_PROMPT.md §5 step 1 → step 2 transition + ADR-0004. Mirrors phase 12's SPEC structural template section-for-section (§§1-15); resolves the §unresolved counter-name issue carried in by the prior STATE.md row via option (a) prose correction landed inline in SPEC §1.1 amendment 5 + §11.5 (`downstream_rq_too_large` is Envoy-only; envoy-go emits `downstream_rq_4xx` per the in-table 29-name HCM counter set from phase 06.1; both Envoy-only `downstream_rq_too_large` and `downstream_rq_completed` are filtered out per the twin-series-discipline allow-list). Spec-document-reviewer dispatched per ADR-0004 §Decision 3; verdict Approved with minor issues found; 2 issues addressed inline + 2 advisory polish items applied (1 of max 3 review iterations). ROADMAP row 13 flipped `planned → in-progress` per BOOTSTRAP_PROMPT.md §4.1 invariant 3. STATE.md updated. Anticipated: 3 ADRs land at phase-done commit (ADR-0125 package shape + per-route disabled-OR-override 5th canonical discipline; ADR-0126 max_request_bytes ≤ 1 MiB envoy-go-only parse-time validation + cap-layering rationale; ADR-0127 v2 body-counting algorithm + maybeAddContentLength mirror + reuse of framework SendLocalReply 413 wire shape + 100-Continue addendum). ADR-0128 stays retired per BRAINSTORM §12.7 + SPEC §1.1 amendment 5. Differential fixture `0015-http-buffer` 6-request matrix per SPEC §7.1; FuzzBufferConfigParse is the 17th fuzzer in the repo per SPEC §14.3. Docs-only commit; no code changes; phase 12's six gates remain green at base commit `a782fc9`.
- **last-updated:** 2026-05-09

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
