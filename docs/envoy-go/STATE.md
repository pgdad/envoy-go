# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `05.2-upstream-h2`
- **phase-directory:** `docs/envoy-go/phases/05.2-upstream-h2/` — now contains `README.md` (the illustrative scope-sketch from the planner-time split per ADR-0045), `SPEC.md` (drafted at `dacf4b7` by `superpowers:brainstorming` per ADR-0004 autonomous mode), AND `PLAN.md` (this commit, drafted by `superpowers:writing-plans` in adapted-autonomous-mode per ADR-0005; `plan-document-reviewer` subagent approved at iteration 1 of the up-to-3 budget with three advisory recommendations applied). PROGRESS.md + REVIEW.md will land in subsequent sessions. The sibling `docs/envoy-go/phases/05.1-downstream-h2/` remains closed read-only history (ROADMAP row `05.1` status `done`). The parent `docs/envoy-go/phases/05-http-2/` retains its `SPEC.md` (`612cdea`) as the master design document; that directory remains closed read-only history at the parent level. The parent ROADMAP row `05-http-2` stays `in-progress` until 05.2 reaches `done`; the 05.2 phase-done commit will close both rows on the same commit per the 05.2 SPEC §4.4 + 05.2 PLAN Task 15.
- **lifecycle-state:** `3` — phase 05.2's `SPEC.md` AND `PLAN.md` exist; PROGRESS.md does not. Per BOOTSTRAP §5 the next transition is state 3 → 4 via `superpowers:subagent-driven-development` (the default executor per ADR-0005 §4; the implementing session may override only with an ADR), which executes the 15 PLAN tasks task-by-task in a fresh worktree on `phase/05.2-upstream-h2-impl` (per the PLAN's `## Execution preconditions`); each task lands one or more commits with PROGRESS.md entries quoting command outputs verbatim per the phase-02/03/04/05.1 precedent. Task 15 (closing sweep) advances STATE to lifecycle-state 4.
- **next-skill:** `superpowers:subagent-driven-development` — executes `docs/envoy-go/phases/05.2-upstream-h2/PLAN.md` task-by-task per ADR-0005 §4. Inputs: this commit's `PLAN.md`, the SPEC.md at `dacf4b7`, the master phase-05 SPEC at `612cdea`, the closed-history 05.1 phase artefacts (especially 05.1's PROGRESS.md as the verbatim-output precedent for verification blocks). The implementer runs Task 1's preconditions check first; on any precondition failure, invoke `superpowers:systematic-debugging` per the PLAN's "Execution preconditions" guidance.
- **next-skill-scope:** Execute the 15 PLAN tasks: Task 1 (preconditions + PROGRESS preamble); Tasks 2-5 (ADR-0055 sequence — `reserveBlocking` / `translateFramerErr` prereqs, then I-1+I-2 outbound chunking, then I-3+M-9 receive-side + WINDOW_UPDATE bounds-check, then M-11 reorder + ADR-0055 lands); Task 6 (05.1 carry-forward — monotonic-id-reuse integration test + M-8 cleanup); Tasks 7-8 (`internal/filter/hcm/h2/client.go` skeleton + RoundTrip body); Task 9 (`Cluster.UseH2()` + `internal/cluster/dial_h2.go` + ADR-0056); Task 10 (`internal/cluster/manager.go` HttpProtocolOptions parsing + `internal/bootstrap/bootstrap.go` blank import); Task 11 (`routerActionH2` action + filter-build variant selection + `h2dispatch.go` adapter + `h2.Action` widening + ADR-0058); Task 12 (`test/helpers/h2.go` H2RoundTrip helper); Task 13 (fixture 0004 PKI + backends + bootstraps + expectations + harness `BackendKind: HTTPSH2`); Task 14 (fixture 0004 driver + runner blank-import + ADR-0057 closes ADR-0035 H2 leg); Task 15 (BEHAVIOR_CONTRACT in-place edit per ADR-0052 + all-gates green local sweep + STATE → lifecycle-state 4). The four ADRs land at ADR-0055 (Task 5), ADR-0056 (Task 9), ADR-0058 (Task 11), ADR-0057 (Task 14) — non-monotonic by topical-vs-first-use-order tradeoff documented in PLAN's "ADRs introduced by this plan" section. PROGRESS.md is append-only; SHA-fill follows the phase-02/03/04/05.1 convention. ROADMAP row 05.2 → `done` AND parent row 05 → `done` flip at the phase-done commit (REVIEW session, lifecycle-state 6), NOT at Task 15.
- **last-commit:** this commit — `phase 05.2: PLAN drafted (autonomous writing-plans per ADR-0005; plan-document-reviewer subagent approved at iteration 1)`, on branch `phase/05.2-upstream-h2-plan`, branched from master `dacf4b7` per ADR-0003. Lands `docs/envoy-go/phases/05.2-upstream-h2/PLAN.md` (~3000 lines) and advances STATE.md to phase 05.2-upstream-h2 lifecycle-state 3. ROADMAP rows are unchanged (row 05.2 stays `in-progress` per the SPEC commit; row 05 parent stays `in-progress`; row 05.1 stays `done`). Master is fast-forwarded to this commit per ADR-0003.
- **last-updated:** 2026-04-26

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
