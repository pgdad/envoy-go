# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `08.2-graceful-drain`
- **phase-directory:** `docs/envoy-go/phases/08.2-graceful-drain/` — contains `BRAINSTORM.md` (lifecycle-state-1 artefact at `e7b64ac`), `README.md` (sibling SPEC stub from 08.1 SPEC commit `1f85b07`; **read-only history**), `SPEC.md` (lifecycle-state 1 → 2 deliverable at `546b08a` on branch `phase-08.2-graceful-drain-spec`; **read-only history** as of THIS PLAN commit), and **NEW** `PLAN.md` (the lifecycle-state 2 → 3 deliverable; landed at `72ddc23` on branch `phase-08.2-graceful-drain-plan`). Parent ROADMAP row `08` STAYS `in-progress` (flips at 08.2 phase-done per parent SPEC §5 — the BOOTSTRAP_PROMPT.md §8 MVP-trunk-close commit). Sibling sub-phase `08.1-admin-endpoints` (closed at parent commit `70e6a65`) is read-only history. All earlier phases (00–07.2) remain closed read-only history.
- **lifecycle-state:** `3` — phase 08.2 PLAN drafting session complete; lifecycle-state 2 → 3 transition lands. The PLAN translates SPEC §§4–7 deliverables + §11 empirical pins into 13 bite-sized TDD tasks (T1..T13) per the writing-plans skill template + the 08.1 PLAN structural precedent (~3012 lines, mirrors the 08.1 PLAN dialect — front-matter (REQUIRED SUB-SKILL line, project-context paragraph, Goal, Architecture, Tech Stack, Scope check, File structure table, Planner-time deferred-decision resolution settling SPEC §12 nine items + a tenth nil-dm policy item, ADRs-introduced-by-this-plan table mapping ADR-0091..ADR-0099 to lands-in-task anchors, 15 Execution preconditions) + 13 task bodies with embedded test source, exact run commands, expected output, HEREDOC commit messages, **Files**/**Precondition**/**Artifact**/**Acceptance**/**Anchored** blocks, checkbox `- [ ]` steps + Refinement + Post-plan handoff closing sections). Plan-document-reviewer subagent dispatched per writing-skill review loop, flagged three real issues — (1) ADR-0096 anchor cross-reference said Task 6 (placeholder slot) instead of Task 4 (cluster-side anchor), (2) blank-import target file disambiguation needed (PLAN-correct `runner_test.go` vs SPEC-erratum `runner.go`), (3) nil-dm 500-vs-200 policy referenced from Task 7 but not enumerated in the planner-time-decision resolution section — all three patched in-session and re-verified APPROVED by the reviewer on second pass. The PLAN settles SPEC §12 in full (ten items including the planner-time-discovered nil-dm policy: `POST /drain_listeners` with `nil` drain manager returns `500` with body `drain manager not configured\n`). Task topology: T1 PROGRESS preamble (no production code; reproduces planner-time-decision resolution); T2 `internal/drain/` package (Manager + 9 unit tests + FuzzDrainTransitions); T3 `internal/admin.New` constructor widening to 7-param (LBP-1 fifth application; deliberately leaves `cmd/envoy-go` broken until T11 mirroring 08.1 Task-5 → Task-10 broken-window discipline); T4 `internal/cluster.Cluster.closePool()` + `Cluster.Manager.Drain()` (anchors ADR-0096; consolidates into T6 — T6 is a documented no-op placeholder slot for numerical alignment with STATE.md projection); T5 `internal/listener.Manager.Drain()` + Accept-loop fast-path + N-1 doc-comment carry-forward + filterRegistry closure widen with `_ = dm` discards (anchors ADR-0094); T6 placeholder (skip); T7 `internal/admin/drain.go` POST handler + 405 method discrimination (anchors ADR-0093; in-place amends ADR-0090); T8 `/ready` + `/server_info` DRAINING extensions (anchors ADR-0097 + ADR-0098; in-place amends ADR-0015 + ADR-0088); T9 HCM filter Inc/Dec + constructor widen + listener closure thread-through (realizes ADR-0096); T10 TCP-proxy filter Inc/Dec + constructor widen + listener closure thread-through (realizes ADR-0096); T11 `cmd/envoy-go/main.go` SIGTERM-handler upgrade + drainMgr alloc + threading (anchors ADR-0092 + ADR-0095); T12 differential fixture `0010-graceful-drain` (envoy.yaml + envoy-go.yaml + slow-streaming Go HTTP backend on :18001 + driver.go with admin-trigger + SIGTERM-trigger paths + expectations.yaml + runner_test.go blank-import); T13 BEHAVIOR_CONTRACT restructure + ADR-0099 + ADR-0089 in-place amend + six-gate verification + REVIEW.md + phase-done commit (closes 08.2 ROADMAP row + parent 08 row simultaneously per parent SPEC §5 — the BOOTSTRAP_PROMPT.md §8 MVP-trunk-close commit). Anticipated phase-done size: ~600 LoC production + ~880 LoC tests + ~670 LoC fixture YAML/Go + ~800 LoC docs = comfortably under ADR-0045 thresholds (production-only ~600 LoC vs ~1500 cap; 13 tasks vs ~25 cap). Per BOOTSTRAP §5 the next transition is state 3 → 4 via `superpowers:subagent-driven-development` (impl execution).
- **next-skill:** `superpowers:subagent-driven-development` — implementation execution per the user's persistent execution-style preference recorded in `~/.claude/projects/-home-esa-git-envoy-go/memory/MEMORY.md` (`feedback_execution_style.md`: "user always wants subagent-driven over inline execution for plans"); fallback `superpowers:executing-plans` if the executor opts for inline batched execution. Inputs: this commit's `docs/envoy-go/phases/08.2-graceful-drain/PLAN.md` (authoritative — the executor walks the 13 tasks T1..T13 in order, with task-6 skipped per the placeholder-slot annotation; each task ends in a `git commit` step with HEREDOC commit message), `docs/envoy-go/phases/08.2-graceful-drain/SPEC.md` (the source-of-truth contract for body shapes / status codes / header sets / ADR scope; consult when a task body's expected-output assertion needs disambiguation), `docs/envoy-go/phases/08.2-graceful-drain/BRAINSTORM.md` (background "why"; consult only when SPEC needs disambiguation), `docs/envoy-go/phases/08-admin-api-and-drain/SPEC.md` (parent master SPEC — at T13 the executor closes parent row `08` SIMULTANEOUSLY with `08.2` per parent §5), `docs/envoy-go/phases/08.1-admin-endpoints/{PLAN.md, PROGRESS.md, REVIEW.md}` (just-closed sibling — structural precedent for PROGRESS append-only entry style + REVIEW carry-forward dispositions; the LBP-1 fourth-application call sites in `internal/admin/admin_test.go` lines 13/57/83/115/121/137/158 are the call sites whose seventh `nil` arg the constructor-widening at T3 introduces), `BOOTSTRAP_PROMPT.md` §§5–8 (lifecycle + phase-done six-gate + MVP-trunk close), `SKILL_ROUTING.md`, the closed-history `docs/envoy-go/phases/06.1-stats-prometheus/PLAN.md` and `docs/envoy-go/phases/06.2-access-log/PLAN.md` and `docs/envoy-go/phases/07.1-http-filter-framework/PLAN.md` and `docs/envoy-go/phases/07.2-listener-chain-completion/PLAN.md` and `docs/envoy-go/phases/08.1-admin-endpoints/PLAN.md` (executor-side dialect references for similar TDD task layouts), and the existing code at `internal/admin/{admin,admin_test,serverinfo,serverinfo_test}.go` + `internal/listener/manager.go` + `internal/cluster/{manager,cluster}.go` + `internal/filter/hcm/{config,filter,connection,h2dispatch}.go` + `internal/filter/tcpproxy/filter.go` + `cmd/envoy-go/main.go` + `test/fixtures/0009-admin-config-dump/` + `test/differential/runner_test.go` (the targets the PLAN extends).
- **next-skill-scope:** Lifecycle-state 3 → 4 implementation execution session deliverables: (1) the 12 production tasks T1, T2, T3, T4, T5, T7, T8, T9, T10, T11, T12, T13 executed in order (T6 is a documented no-op placeholder skipped per PLAN line 1280); (2) `docs/envoy-go/phases/08.2-graceful-drain/PROGRESS.md` (NEW; append-only log; one entry per task with verbatim command outputs); (3) ~600 LoC production Go + ~880 LoC tests across `internal/drain/`, `internal/admin/`, `internal/listener/`, `internal/cluster/`, `internal/filter/hcm/`, `internal/filter/tcpproxy/`, `cmd/envoy-go/main.go`; (4) ~670 LoC fixture YAML/Go in `test/fixtures/0010-graceful-drain/`; (5) `BEHAVIOR_CONTRACT.md` in-place patch per SPEC §13 (lands at T13 per ADR-0052); (6) nine new ADRs ADR-0091..ADR-0099 in `DECISIONS.md` per ADR-0044 ADR-on-impl convention (each ADR lands at the task that anchors its first-use commit per the PLAN's "ADRs introduced by this plan" table at ~line 138); (7) five in-place amendments to ADR-0015, ADR-0085, ADR-0088, ADR-0089, ADR-0090 (land at T13 alongside BEHAVIOR_CONTRACT restructure per ADR-0089 consequence (b) pattern); (8) `ROADMAP.md` row `08.2` `in-progress → done` flip + parent row `08` `in-progress → done` flip SIMULTANEOUSLY at T13 (the BOOTSTRAP_PROMPT.md §8 MVP-trunk-close commit); (9) `REVIEW.md` (NEW; lifecycle artefact authored at T13 per the requesting-code-review skill — the 08-family REVIEW covers 08.1 + 08.2 jointly per parent §5); (10) `STATE.md` advance to lifecycle-state 4 then 5 then to MVP-trunk-closed `awaiting next planning` per BOOTSTRAP §5 (executor lands the state advances incrementally per the prior-phase pattern). After phase-done, the project enters BOOTSTRAP §9 feature-family expansion mode; `next-skill: superpowers:brainstorming` against the §9 family list (the planner of the next session selects the family). **Worktree for the next session:** branch from THIS PLAN-commit's master fast-forward HEAD per ADR-0003 + per-phase-worktree convention: `git worktree add .worktrees/phase-08.2-graceful-drain-impl -b phase-08.2-graceful-drain-impl master` then `cd` into it. Plan-time deferred-decision resolution (10 items) reproduced verbatim in PLAN §"Planner-time deferred-decision resolution" + PROGRESS preamble (Task 1) per the PLAN's self-contained-PROGRESS discipline; the executor consults that section for any nil-dm / FuzzDrainTransitions ship/skip / boot-order / closePool-shape decisions rather than re-deriving from the SPEC.
- **last-commit:** `72ddc23` — `phase 08.2: PLAN drafted (13-task implementation plan)`, on branch `phase-08.2-graceful-drain-plan`. Lands `docs/envoy-go/phases/08.2-graceful-drain/PLAN.md` (the lifecycle-state 2 → 3 deliverable; ~3012 lines) AND THIS STATE.md advance. PLAN.md mirrors the 08.1 PLAN structural precedent (front-matter + Scope-check + File-structure table + Planner-time-decision resolution + ADRs-introduced table + Execution-preconditions block + 13 task bodies + Refinement + Post-plan-handoff + Glossary + References). PLAN settles SPEC §12 deferred decisions in full plus a tenth planner-time-discovered item (nil-dm 500-vs-200 policy). The plan-doc-reviewer subagent dispatched per writing-plans skill flagged three issues on first pass (ADR-0096 lands-in-task wrong-task cross-reference; runner_test.go vs runner.go disambiguation; missing nil-dm planner-time-decision item) — all three patched in-session and APPROVED on second pass. No code changes in 08.2 PLAN commit (per BOOTSTRAP §5 lifecycle state 3 — PLAN drafting only); code lands at lifecycle-state 4 per `superpowers:subagent-driven-development`. SHA-fill follow-up commit lands immediately after this PLAN commit per the per-phase SHA-fill convention (replaces `TBD` with the actual SHA).
- **last-updated:** 2026-05-02

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
