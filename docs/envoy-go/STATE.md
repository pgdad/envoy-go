# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

> **This file is a POINTER, not an archive** — `BOOTSTRAP_PROMPT.md` §4: *"STATE.md — pointer: active phase, last commit, next action."* It carries the live pointer, the live counts, and a SHORT recent lineage. **Full stage-by-stage history lives in `STATE_HISTORY.md`** (archived at ADR-0288; **nothing was deleted** — the pre-move file is preserved there verbatim, byte-for-byte).
>
> **⚠️ THE RULE ADR-0288 EXISTS TO ENFORCE: there is EXACTLY ONE live `next-skill:`, `lifecycle-state:`, `next-free ADR:`, and count block IN THIS FILE, and they live in §Current pointer directly below.** Before that move, ~330 lines of prepended backlog had buried TWO fossilised "current state" blocks whose `next-skill:` still read *"the 42.2 hedging BRAINSTORM"* and *"the 46.2 SPEC"* while the live phase was **66** — so `grep 'next-skill:' STATE.md` returned **two stale hits and ZERO live ones**, and `grep 'fuzzers:'` returned **48** and **42** when the true count was **55**. **A session may trust a grep of this file again. Keep it that way: when you advance the pointer, EDIT §Current pointer IN PLACE — never prepend a new block above it.**

---

## Current pointer

*(The ONLY live "what next" in this repo. Update IN PLACE at every stage close.)*

- **active-phase:** `phase 68 (tls-cvc-empty-dynamic-fallback) BRAINSTORM done` (2026-07-18)
- **phase-directory:** `docs/envoy-go/phases/68-tls-cvc-empty-dynamic-fallback/` (`BRAINSTORM.md` present; row 68 is `in-progress`)
- **lifecycle-state:** `1` (BRAINSTORM done). **Row 68 registered `in-progress`** at the phase-68 BRAINSTORM stage-close per the ROADMAP §Schema invariant (`depends-on 67`); it flips `done` at the phase-68 IMPL six-gate (ADR-0106, the SOLE leg unless split — a SINGLE FLAT ROW anticipated, ~8–9 tasks, ADR-0045 valve armable).
- **next-skill:** **the phase-68 SPEC** — RE-DERIVE the BRAINSTORM rather than execute it (`feedback_brief_citations_not_evidence`). **Subject (SELF-PICKED + CONFIRMED by EXECUTED probe this BRAINSTORM): `tls-cvc-empty-dynamic-fallback`** — honor the reference's `combined_validation_context` MERGE semantics on an ACKed-but-EMPTY dynamic half: the CVC arm of `NewDownstreamConfig`, on an ACK-succeeded-but-empty dynamic fetch, falls back to `default_validation_context.trusted_ca` (via `loadTrustedCAPool` + the phase-67 `installPool`) and SERVES, where envoy-go today boot-FAILS. On the honored surface (only `trusted_ca` — P1) the reference `MergeFrom` reduces EXACTLY to "dynamic `trusted_ca` if present, else default's" — NO `proto.Merge` (ADR-0287's "message-returning seam" pre-characterization refuted, DECISIONS.md:16975). **The SPEC owes:** (1) **D-EDF-CLASSIFY** — the row's ONE growth risk: coarse `errValidation` export vs a narrow `errEmptyValidationContext` sentinel; RUN a **served-CORRUPT-`trusted_ca` discriminating probe** (not run at the BRAINSTORM — Dossier A limit) to decide (does the reference NACK corrupt, or merge-and-drop? — run THREE arms incl. `trusted_ca:{inline_bytes:""}`); lean NARROW. (2) **D-EDF-STREAMPOSTURE** — the RESIDUAL divergence: envoy-go TODAY classifies the empty resource as `errValidation` → NACK + `sds.update_rejected` where the reference cleanly ACKs (`update_success`); after the fallback the LISTENER matches but the SDS-stream posture diverges. Decide: reclassify-as-ACK (larger) vs NAME the departure (smaller — the traffic surface already matches); lean the latter, but PICK EXPLICITLY. (3) **D-EDF-FETCHSHAPE** (classified-error-then-fallback vs a `(nil, empty=true, nil)` variant — prefer the former, phase-67 nil-pool guard byte-intact). (4) **D-EDF-PGV** (add an explicit reject for a CVC missing `default_validation_context`, matching the reference's PGV-only boot-reject — lean ADD). (5) **D-EDF-EMPTYBOTH-REQUIRE** (empty-both + require=true → the RETAINED envoy-go-STRICT reject, the phase-67 require-needs-an-anchor invariant). (6) **D-EDF-FIXTURE** `0111` + **D-EDF-FUZZSEED** (dispatch-verify to avoid the phase-66/67 vacuity trap). **Envelope anticipated:** ONE functionally-edited production file (`internal/tls/config.go`) + **ONE new `internal/xds` exported symbol** (the classifier — the FIRST break of the recent zero-new-symbol streak, DECISIONS.md:16973, stated LOUDLY; cycle guard STANDS, TYPE-level); `internal/boot`/`validate/`/`internal/listener` UNTOUCHED; CVC-ONLY; +0 packages/modules/stats/fuzzers/BackendKinds; fixtures 112 → 113. ADR-0290 §Context drafts at the SPEC per ADR-0044 (the DECISIONS tail flips ADR-0289 → ADR-0290 at the SPEC commit; the IMPL completes it in place). Open a FRESH worktree off master (`feedback_git_worktrees`); **RE-DERIVE — do not trust this pointer's cites** (the three input dossiers lived in PRIVATE scratch OUTSIDE the repo and are not committed).
- **last-commit:** the phase-68 BRAINSTORM squash (`git log --grep 'phase 68'`; locate by SUBJECT, never by position). Prior: the phase-67 IMPL squash.
- **last-updated:** 2026-07-18 (phase-68 BRAINSTORM done — row 68 `in-progress`)
- **next-free ADR:** **`ADR-0290`** — DECISIONS.md tail is **ADR-0289** (next-free ADR-0290 is FREE; the phase-68 SPEC drafts ADR-0290 §Context, flipping the tail ADR-0289 → ADR-0290 at that SPEC commit; the IMPL completes it in place, no further flip).

---

## Project counts at this commit

*(Live. Re-verify at session start — `git fetch` first — and update here at every stage close. Canonical commands inline.)*

- **active differential fixtures:** **112** — `ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0110-tls-require-client-cert-false`.
- **fuzzers:** **55** — `grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`. Phase 66 added SEEDS only (a new `downstream-sds` dispatch side in `FuzzTLSContextParse`) ⇒ **+0**.
- **stat surface:** **1201** — BEHAVIOR_CONTRACT doc count. **There is NO mechanical counting command.** Phase 66 was **+0**.
- **BackendKind tail:** **38** (`H2GoawayResponder`).
- **DECISIONS.md tail:** **ADR-0289** (next-free **ADR-0290**) — `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1`. ADR-0289 was COMPLETED IN PLACE at the phase-67 IMPL (§Decision + §Consequences added; no further flip).
- **go.mod modules:** the tracked lineage figure is **2** (the phase-61.2 count: `quic-go` direct + `qpack` indirect) — **NOT a repo total**; the single `go.mod` requires **67** modules. Phase 66 was **+0**.
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` (see `ENVOY_TARGET.md`).
- **conformance:** h2spec 53/53; proxy-wasm all 10 families PASS.

---

## Recent lineage

*(The last FIVE stages only — enough for the router's "prior-phase lineage" read. Everything earlier is in `STATE_HISTORY.md`. These bullets are carried over VERBATIM; the newest is the phase-68 BRAINSTORM this pointer reflects.)*

- **prior active-phase:** `phase 68 (tls-cvc-empty-dynamic-fallback) BRAINSTORM done` (2026-07-18) — the roller's AUTONOMOUS self-pick (no human pick per the 2026-07-12 standing directive) opens the SIXTH xDS-family row (lifecycle-state 0 → 1; docs-only, ZERO production `.go`; the three input dossiers lived in PRIVATE scratch OUTSIDE the repo — nothing to delete in-tree; fresh worktree off master `92cd1647`, branch `phase-68-empty-dynamic-fallback-brainstorm`, per `feedback_git_worktrees`). **Subject:** `tls-cvc-empty-dynamic-fallback` — complete the `combined_validation_context` MERGE semantics on an ACKed-but-EMPTY dynamic half: the CVC arm of `NewDownstreamConfig`, on an ACK-succeeded-but-empty dynamic fetch, falls back to `default_validation_context.trusted_ca` (via `loadTrustedCAPool` + the phase-67 `installPool`) and SERVES, where envoy-go today boot-FAILS. **Why this pick (the FULL rejected roster ADJUDICATED in BRAINSTORM §2.1 — not the phase-67 one-liners):** the direct pickup of "Design B", the phase-66 runner-up filed as an ADR-0280-family departure (SPEC-66:267,509) and phase-67 re-deferred (SPEC-67:356); it is the SMALLEST candidate that is ALSO crisply differentially-testable (a three-arm cross-side verdict) AND already reference-confirmed — the smaller missing-`default:`-arm loses on differential value (a low-value reject-add on a `[#not-implemented-hide:]` field), OTLP-metrics loses on size (a NEW multi-leg sink — the strongest FUTURE pick, narrows ROADMAP:198). **On the honored surface (only `trusted_ca` — P1) the reference `MergeFrom` reduces EXACTLY to "dynamic `trusted_ca` if present, else default's"** — the fallback IS the honored-surface projection of the merge; NO `proto.Merge` (ADR-0287's "message-returning seam" pre-characterization, DECISIONS.md:16975, refuted by re-derivation — the phase-66 unbuildable-`proto.Merge` lesson applied). **EXECUTED evidence (Dossier A, `envoyproxy/envoy:v1.37.2`, fresh container per arm, a throwaway SotW SDS sibling-container with a served-this-arm assert, forced-send client certs):** empty-served → **falls back to CA_A + SERVES** (arm 1: `update_success=1`, LIVE, CA_A accept / CA_B reject alert 48 / no-cert reject at require=true) · timeout/unreachable → **fail-closed, NO fallback** (arm 3: `init_fetch_timeout=1` — the discriminating signal is ACK/`update_success` vs `init_fetch_timeout`; envoy-go needs a classification signal) · empty-BOTH / plain-SDS-VC-empty → **serve UNAUTHENTICATED, require=true SILENTLY unenforced** (arms 4a/5) · missing `default_validation_context` → **PGV boot-reject** (arm 4b — PGV-only; envoy-go runs none ⇒ a deliberate-divergence decision, `reference_pgv_forecloses_go_hazard`) · require × fallback → verify-if-presented against the FALLBACK anchor, CA_B still REJECTED under forced-send (a REAL verify, not a vacuous accept). **Constructibility (Dossier B):** ONE production function (`internal/tls/config.go` CVC error branch) + **ONE new `internal/xds` exported symbol** (a classifier — `errValidation` is unexported today, stream.go:33) — the FIRST break of the recent zero-new-symbol streak (DECISIONS.md:16973), stated LOUDLY; the cycle guard STANDS (the export adds no edge; TYPE-level); the P1–P5 theorem EXTENDS (P3 restated — the default DOES contribute on the empty branch; P4 NOT re-introduced — the four sub-field rejects already re-pointed at `default_validation_context`, config.go:442-461, gate the fallback); CVC-ONLY (plain SDS-VC empty stays boot-FAIL, a RETAINED departure). **BRAINSTORM-time open questions to the SPEC:** D-EDF-CLASSIFY (coarse `errValidation` vs a narrow `errEmptyValidationContext` — the ONE growth risk; RUN a served-CORRUPT-`trusted_ca` discriminator with THREE arms incl. `trusted_ca:{inline_bytes:""}`; lean NARROW) · D-EDF-STREAMPOSTURE (envoy-go NACKs the empty resource where the reference cleanly ACKs — a RESIDUAL divergence after the fallback; reclassify-as-ACK vs name-the-departure, lean the latter) · D-EDF-FETCHSHAPE · D-EDF-PGV · D-EDF-EMPTYBOTH-REQUIRE · D-EDF-FIXTURE `0111` · D-EDF-FUZZSEED. **Adversarially verified before landing** (an independent code-claims + probe + adjudication verifier). Anticipated: fixtures **112 → 113** (`0111-tls-cvc-empty-dynamic-fallback`) / stat surface **1201 (+0)** / fuzzers **55 (+0 anticipated)** / BackendKind **38 (+0)** / DECISIONS tail **ADR-0289 → ADR-0290** (at the SPEC) / ZERO new packages/modules / **ONE new `internal/xds` exported symbol**. **Envelope:** ~8–9 tasks, a SINGLE FLAT ROW anticipated (ADR-0045 valve armable, no split). **Row 68 registered `in-progress`** at this stage-close commit per the ROADMAP §Schema invariant; flips `done` at the phase-68 IMPL six-gate (the SOLE leg, ADR-0106). **NO deferred sentence is narrowed at ANY stage** (empty-dynamic/Design B is in NO live `candidates:` sentence — a §13-tier pickup; the phase-64/66/67 precedent). **Counts UNCHANGED at this BRAINSTORM (docs-only; count commands re-run MECHANICALLY in the worktree at this close):** stat surface **1201** · fixtures **112** (numeric tail `0110-tls-require-client-cert-false`) · fuzzers **55** (`internal/` scope) · BackendKind **38** · DECISIONS tail **ADR-0289** (next-free **ADR-0290**) · go.mod modules **2**. **Sentinel re-run MECHANICALLY at this close (does NOT fire; `stop` NOT created):** (1) now prints `NOT DONE: row 68` (the registration re-opens check (1)); (2) prints THREE live "candidates:" sentences (`grep -cE 'remaining deferred \(not-yet-chartered\) candidates:'` ⇒ **3**); (3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`. **Next → the phase-68 SPEC** (RE-DERIVE the BRAINSTORM rather than execute it; the owed roster is in §Current pointer's next-skill line).

- **prior active-phase:** `phase 67 (tls-require-client-cert-false) IMPL done` (2026-07-18) — lifecycle-state 3 → DONE; **ROW 67 FLIPS `in-progress` → `done`** at this IMPL six-gate (ADR-0106, the SOLE leg — `reference_roadmap_split_phase_row_done`). Worktree `.worktrees/phase-67-impl`, branch `phase-67-tls-require-client-cert-false-impl`; subagent-driven (`feedback_execution_style`), each task committing LOCALLY only (`feedback_subagents_no_push`), controller squash-push at close (`feedback_push_to_origin`). **The 9-task TDD spine landed:** T1 ATOMIC hoist — `NewDownstreamConfig` maps `ClientAuth` three-way across FOUR cells (true+anchor→`RequireAndVerifyClientCert` · false/absent+anchor→`VerifyClientCertIfGiven` · false/absent+no-anchor→`NoClientCert` · true+no-anchor→the RETAINED `requires validation_context.trusted_ca` reject) via a shared `installPool`/`clientAuthFor` closure with the nil-pool guard (assignment-adjacency); E3 RETIRED atomically with its flip roster; the P1–P5 theorem block moved INTACT; retained rejects byte-diffed · T2 the mapping cross-product units + the interface-pinned `TestVerifyIfGiven_NilPool_Unconstructible` (Break D its sole liveness proof) + the require=false `fakeProvider{vcErr}` fetch-failure arm · T3 four fuzz seeds on the CORRECT dispatch sides (+0 fuzzers; the `"downstream"`-side SDS-seed vacuity trap avoided, counter-checked) · T4/T5 fixture `0110-tls-require-client-cert-false` (CVC-primary at require=false, three-arm verdict trusted→ok+echo / untrusted→rejected / no-cert→ok+echo the discriminator, FORCED-SEND `GetClientCertificate` driver — Break B caught the polite-withhold vacuous green, 0109's require=true polite green the control; the structuralCheck triplet + per-side pin + served-this-arm breaks all fired) · T6 the comment sweeps (B11/B16/B17/B18/B19 verbatim + the SDS-VC leading-comment M1 + nil-pool test-goroutine M2 carries + the 0109 enumerated set; non-drift counterfactuals preserved) · T7 BC B1–B10/B13–B14 verbatim + the FOUR-site TEST_GAP sweep (B15) [+ a controller B6-consistency fixup] · T8 six-gate ALL GREEN (gofmt silent · vet 0 · build 0 · `go mod tidy -diff` EMPTY · `git diff master -- go.mod go.sum` NO DIFF · `golangci-lint run ./...` 0) + full **112-dir** differential (`ok  github.com/pgdad/envoy-go/test/differential  375.345s`, EXIT 0, 0 FAIL) + `-race` ok + the `internal/xds` cycle guard (`internal/stats` + `internal/xds` ONLY) + envelope audit (production edits = `internal/tls/config.go` + the ONE chartered `internal/xds/provider.go` comment; `boot.go`/`listener`/`validate` BYTE-UNTOUCHED) · T9 ADR-0289 COMPLETED IN PLACE (§Decision + §Consequences) + the B12 bracketed annotation on the ADR-0286 D-SDSVC-FETCHTIMEOUT bullet. **Task-scoped adversarial reviews** (T1/T2/T4 opus reviewers — SPEC ✅, the 0110 non-vacuity space walked and every arm proven load-bearing). **Exit counts:** fixtures **111 → 112** (`0110`) / stat surface **1201 (+0)** / fuzzers **55 (+0)** / BackendKind tail **38 (+0)** / DECISIONS tail **ADR-0289** (completed in place; next-free **ADR-0290**) / go.mod modules **2 (+0)**. **Sentinel re-run MECHANICALLY (does NOT fire; `stop` NOT created):** (1) prints **NOTHING** (row 67 is `done`); (2) prints **THREE** live "candidates:" sentences (HTTP/3, xDS, Observability); (3) prints **THREE** never-opened families (gRPC/Runtime/WASM). Checks (2)+(3) print ⇒ abundant work remains. **Next → the phase-68 BRAINSTORM** (the roller SELF-PICKS `xds-sds-upstream-server-cert` per the 2026-07-12 standing directive).

- **prior active-phase:** `phase 67 (tls-require-client-cert-false) PLAN done` (2026-07-17) — lifecycle-state 2 → 3; docs-only (`PLAN.md` + `PROGRESS.md`, the ONLY two files in the squash's phase-directory delta), fresh worktree off master `a15f4fca` (which includes a post-SPEC PARALLEL work stream: five new test files, one CI line, a TEST_GAP_ANALYSIS append — landed by another session and verified green before this stage), branch `phase-67-tls-require-client-cert-false-plan`, per `feedback_git_worktrees`; subagent-driven (`feedback_execution_style`): one re-derivation input agent → a drafting agent → THREE independent adversarial verifiers → an amendment agent, each verifier in PRIVATE scratch (`reference_parallel_subagents_private_scratch`). **ROW 67 STAYS `in-progress`** (flips at the IMPL six-gate). **The deliverable: a 9-task TDD spine (T1–T9)** decomposing SPEC §10 — **T1 ATOMIC** (the hoist + three-way-across-FOUR-cells `ClientAuth` + E3 retirement + the FULL flip roster in ONE commit; sketch-T1/T2 merged per the phase-66 F5 lesson) · T2 the mapping cross-product units + the interface-pinned `TestVerifyIfGiven_NilPool_Unconstructible` + a require=false `fakeProvider{vcErr}` fetch-failure arm (added by the adversarial pass, M9) · T3 fuzz seeds on the CORRECT dispatch sides (+0 fuzzers; the `"downstream"`-side SDS-seed vacuity trap named) · T4 fixture `0110` (CVC-primary, three-arm verdict, FORCED-SEND `GetClientCertificate` driver, port 10446, fixtures 111 → 112) · T5 the 0110 break protocol (Break B polite-swap + the structuralCheck triplet + per-side-pin + served-this-arm breaks) · T6 the five comment rewrites (B11/B16/B17/B18/B19) · T7 BC B1–B10/B13–B15 + the FOUR-site TEST_GAP sweep (targeted by CLAIM + grep, never line) · T8 verify (six-gate, cycle guard, full 112-dir differential, `-race`, counts, envelope audit) · T9 ADR-0289 COMPLETED IN PLACE + B12 (the DECISIONS:16899 bracketed annotation) + the full-repo zero-LIVING-drift confirmation with its expected-hits classification table + row-67 `done` flip + ROADMAP/STATE/router/sentinel + controller squash-push. **FOUR deliberate breaks pre-verified COMPILING at the input stage** (A mapping-swap · B polite-mode swap — its withholding physics reproduced from scratch by a verifier · C CVC-install inversion · D nil-pool-guard deletion) under a **BINDING MECHANISM-CONDITIONALITY rule** (verbatim status holds ONLY under the shared `installPool`/`clientAuthFor` closure; per-arm adjacency converts A to one-swap-per-arm and C/D to IMPL-recompiled equivalents). **PARALLEL-STREAM ABSORPTIONS stated loudly (A1–A6)**: the TEST_GAP B15 sweep MOVED AND GREW to FOUR sites (the append also inserted 4 lines at the file top — that is what moved the anchors); **B19 NEWLY CHARTERED** — a test-comment-only carve-out in `boot_sds_e2e_test.go:518-521` (the stale "records the reference-side ambiguity" sentence; the SPEC resolved that ambiguity) inside SPEC §15's `internal/boot` BYTE-UNTOUCHED pin, à la B16, envelope invariant unweakened; T1's stays-green roster gains the three new SDS e2e tripwires; the `:43`/`:540` "unpopulated trust store" phrases are envoy-go COUNTERFACTUALS the sweep must NOT touch (over-sweep = under-sweep). **Adversarially verified before landing: THREE independent verifiers (PV1 code-claims BY EXECUTION — built the hoist from the PLAN's own sketch in scratch and PROVED the flip roster complete [the untouched suite fails exactly and only the three roster tests], applied + executed every break, reproduced Break B's physics with fresh PKI · PV2 spine-logic — order-soundness simulated per task, the corrupt-CA two-edit over-determination verified by code-path walk · PV3 process — decomposition mapped 1:1, sentinel re-RUN) found 0 SEVERE · 5 distinct MODERATE · ~10 minor — ALL corrected in the amendment**, the sharpest: Break C's e2e firing set was WRONG (the accept subtest PASSES under the break — corrected by execution), T6's post-sweep "zero LIVING drift" confirmation could not pass as written (split T6-scoped/T9-full with a classification table), and the break verbatim-status conditionality was silent. Honest limits recorded: 0109's standing differential green and the 1201 stat surface carried, not re-run; T2 test 4 (anchorless) is a liveness-unproven regression pin per the phase-66 F2 discipline. **Counts UNCHANGED at this PLAN (docs-only; re-run MECHANICALLY at this close):** fixtures **111** (tail `0109-xds-sds-combined-validation-context`) · fuzzers **55** · stat surface **1201** · BackendKind **38** · DECISIONS tail **ADR-0289** (next-free **ADR-0290**; completed in place at the IMPL, no flip) · go.mod modules **2** (lineage figure; the single `go.mod` requires 67). **Sentinel re-run MECHANICALLY at this close (does NOT fire; `stop` NOT created):** (1) prints `NOT DONE: row 67`; (2) prints THREE live "candidates:" sentences (`grep -cE 'remaining deferred \(not-yet-chartered\) candidates:'` ⇒ **3**); (3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`. **Next → the phase-67 IMPL** (execute T1–T9; RE-DERIVE the PLAN's anchors per task — `feedback_brief_citations_not_evidence`; the owed roster is in §Current pointer's next-skill line).

- **prior active-phase:** `phase 67 (tls-require-client-cert-false) SPEC done` (2026-07-17) — lifecycle-state 1 → 2; docs-only (ZERO production `.go`; every probe harness lived in an agent-PRIVATE scratch OUTSIDE the repo — nothing to delete in-tree), fresh worktree off master `facb0faa`, branch `phase-67-tls-require-client-cert-false-spec`, per `feedback_git_worktrees`; subagent-driven end-to-end (`feedback_execution_style`, `reference_parallel_subagents_private_scratch`): three parallel INPUT agents (A live reference probes / B mechanical code re-derivation / C docs-process extracts) → a drafting agent → THREE independent adversarial verifiers → an amendment agent. **ROW 67 STAYS `in-progress`** (flips at the IMPL six-gate). **ADR-0289 §Context DRAFTED per ADR-0044 — the DECISIONS tail flips ADR-0288 → ADR-0289 at THIS SPEC commit (next-free ADR-0290; the IMPL completes ADR-0289 IN PLACE, no further flip).** **Headline resolutions, all EXECUTED-evidence-backed:** (1) **D-RCCF-FETCHFAIL-POSTURE RESOLVED by a discriminating 2×2 probe** ({server-cert, validation-context} × {silent, unreachable}, fresh container per arm, digest-pinned `envoyproxy/envoy:v1.37.2`): the reference's SDS fetch-failure posture is **UNIFORM per resource type** — init-hold (`/ready` INITIALIZING, port UNBOUND in the container netns; the docker-proxy accept-then-reset trap re-confirmed) → workers start + port binds at `initial_fetch_timeout` → **fail closed PER-CONNECTION** (`downstream_context_secrets_not_ready` increments per attempt; zero `ssl.*` movement; no TLS alert). "Serves" was only ever true of the SOCKET, never of TLS: SPEC-60 §11 arm B's RAW observation is CONSISTENT with this, and ADR-0280 + BC:886 are DRIFT-FREE — **the drift is the phase-65-era "serves anyway / unpopulated trust store" wording, and the adversarial pass (V1, the pass's one SEVERE) expanded the roster from three to FIVE living sites** (BC:900 · the ADR-0286 D-SDSVC-FETCHTIMEOUT bullet · `config.go:115-117` · `internal/xds/provider.go:91-93` · `config_test.go:999`), corrected at the IMPL with replacement wording pinned VERBATIM (SPEC §9 B1–B18; the ADR-0286 bullet by bracketed annotation, NEVER silently; provider.go:91-93 is the ONE chartered doc-comment-only correction inside the otherwise zero-symbols/zero-functional-change `internal/xds` envelope — the false-completeness alternative was refused; phase-65 STAGE DOCS stay as immutable history). (2) **D-RCCF-ANCHORLESS-VC-FALSE probed (9 cells)**: the reference treats `validation_context: {}` EXACTLY like no validation config at EVERY require value — **`require_client_certificate: true` with an anchorless VC is SILENTLY INEFFECTIVE on the reference (NO CertificateRequest sent — discriminated against a CertReq-present control with a force-transmitting client)** ⇒ envoy-go's retained `:179-181` reject is a **NEW named envoy-go-STRICT departure**, and require=false/absent + anchorless VC boots `NoClientCert`. (3) The ClientAuth mapping is **three-way across FOUR cells** (the V3 catch: require=true + no-anchor = the retained boot reject, NOT NoClientCert — the draft's headline table had the false cell and it was mirrored into the ADR draft; both corrected). (4) **Fixture `0110` is CVC-primary** (the E3-retired shape end-to-end + the un-gated fetch LIVE via the served-this-arm assert + pool substitution at require=false, in ONE three-arm verdict; SDS-VC-primary considered and rejected) with the **forced-send `GetClientCertificate` driver MANDATE** — the open question CLOSED: 0108 (driver.go:418) and 0109 (driver.go:449) are both POLITE and relied on withholding+require=true (verdict-correct there; vacuous-green at require=false). (5) **+0 fuzzers dispatch-verified** (SDS-shape require=false seeds MUST ride `"downstream-sds"` or die vacuously at the retained nil-provider gates `:381`/`:398` — the phase-66 T5 trap avoided by re-derivation, not by memory). (6) **D-RCCF-QUIC final call: OUT** (the BC boundary wording is EXTENDED to name the QUIC client-auth gap; no QUIC probe owed at OUT). (7) **The nil-pool guard is pinned against the `xds.SecretProvider` INTERFACE** (the interface permits `(nil, nil)` and `fakeProvider` does it; assignment-adjacency + an unconstructibility test — no config × provider behavior may yield `VerifyClientCertIfGiven` with nil `ClientCAs`). (8) **E3 retires atomically** with a flip roster BIGGER than the substring grep. **Adversarially verified before landing: THREE independent verifiers (V1 code-claims re-derivation — every anchor re-run · V2 probe reproduction — fresh PKI, self-written configs, own silent-SDS server, image digest MATCHED, the two most surprising pins reproduced cell-for-cell, ZERO refuting defects · V3 process/consistency — all 15 docket items mapped one-for-one, ADR mechanics byte-checked against the ADR-0287 precedent, §14 mirror paragraph-identical) found 13 distinct defects — 1 SEVERE, 5 MODERATE, the rest minor/trivial — ALL corrected in the amendment commit; V2's honest limits recorded (the UNREACHABLE probe column and 5 of 9 anchorless cells rest on this session's input-probe run).** **Counts UNCHANGED at this SPEC (docs-only; re-run MECHANICALLY in the worktree at this close):** fixtures **111** (numeric tail `0109-xds-sds-combined-validation-context`) · fuzzers **55** (`internal/` scope) · stat surface **1201** · BackendKind **38** · go.mod modules **2** (lineage figure; the single `go.mod` requires 67) — the ONLY moved count is the DECISIONS tail (**ADR-0288 → ADR-0289**, the §Context draft). **Sentinel re-run MECHANICALLY at this close (does NOT fire; `stop` NOT created):** (1) prints `NOT DONE: row 67`; (2) prints THREE live "candidates:" sentences (`grep -cE 'remaining deferred \(not-yet-chartered\) candidates:'` ⇒ **3** — the command, not the adjective); (3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`. **Next → the phase-67 PLAN** (RE-DERIVE the SPEC rather than execute it; the owed roster is in §Current pointer's next-skill line).

- **prior active-phase:** `phase 67 (tls-require-client-cert-false) BRAINSTORM done` (2026-07-17) — the roller's AUTONOMOUS self-pick (no human pick per the 2026-07-12 standing directive) opens the FIFTH xDS-family row (lifecycle-state 0 → 1; docs-only, fresh worktree off master `0d4d4041`, branch `phase-67-tls-require-client-cert-false-brainstorm`, worktree `.worktrees/phase-67-brainstorm`, per `feedback_git_worktrees`). **Subject:** verify-if-presented mTLS — honor `require_client_certificate: false`/absent via `ClientAuth = VerifyClientCertIfGiven` across ALL THREE validation-source shapes (fully-inline / SDS-VC / CVC) at the TCP apply points; the three anchor arms hoist OUT of the require gate (the SDS validation-context fetch fires at boot regardless of require, matching the reference's un-gated fetch), and **the phase-66 E3 reject RETIRES atomically with the lift** (its ONLY enforcement was this row's gap — re-derived, `reference_lifted_reject_hidden_enforcement`). **Why this pick:** the ONLY candidate simultaneously (i) pre-sized SMALL in a landed doc (SPEC-66 §13: *"needs `VerifyClientCertIfGiven` + a fetch-gate restructure"*; §3.10: *"an apply-point reshape, not a branch"*), (ii) fully substrate-reusing (nothing is built — a gate is moved; ONE production function, `NewDownstreamConfig`, ZERO new packages/modules/`internal/xds` symbols; `internal/boot`/`validate/` untouched), and (iii) already reference-PINNED — and the candidate ADR-0287 §Consequences ITSELF chartered (*"Lifting `require == false` belongs in a row that fixes ALL THREE paths together"*). Runner-up: the empty-dynamic Design B fallback; full rejected roster in BRAINSTORM §2.1. **EXECUTED evidence (three parallel input agents, each in PRIVATE scratch):** a **21-fresh-container-arm reference cross-product** ({inline, sdsvc, cvc} × {false, ABSENT} × {trusted, untrusted, none} + a require=true CVC control; arm-unique secret names + a served-this-arm assert in every SDS arm) — verify-if-presented LIVE on all three shapes; **ABSENT ≡ false** (all 9 twins outcome- and stat-identical; the ONLY cell that moves on the flip is `none`); a **Go `crypto/tls` cross-product** — `VerifyClientCertIfGiven` + a real pool matches the reference CELL-FOR-CELL with **byte-identical wire alerts (48 unknown_ca / 116 certificate_required)**; client-observed failure STRINGS differ per side ⇒ fixture pins stay PER-SIDE; **fetch-gating arms** — the reference does NOT gate the SDS fetch on require, init-holds on fetch failure (workers un-started, port UNBOUND in the container netns — never treat a docker-proxy accept as listener liveness) then **FAILS CLOSED** after `initial_fetch_timeout`, identical at require=true/false, no inline-default-CA rescue under CVC ⇒ envoy-go's un-gated fetch makes `require=false`+SDS boot-FAIL-capable — a **NEW ADR-0280-family departure instance** the SPEC must NAME. **The discriminating-probe discipline PAID twice** (`reference_probe_must_discriminate`): the polite-vs-forced client control exposed a **silent cert-withholding vacuous-green trap** (a Go client withholds a cert whose issuer is absent from the acceptable-CA list — `0110`'s driver MUST force-send via `GetClientCertificate`), and the nil-pool arm pinned the WORST-direction hazard (`VerifyClientCertIfGiven` + nil `ClientCAs` falls back to the SYSTEM pool — trusted clients REJECTED, anonymous ACCEPTED) ⇒ **D-RCCF-NILPOOL-GUARD** (the construction order makes the state unrepresentable; assignment-adjacency). **Adversarially verified before landing: THREE independent verifiers (V1 code-claims re-derivation / V2 probe reproduction with fresh PKI + fresh containers / V3 process-consistency) found FIFTEEN distinct defects, ZERO severe — all corrected in place.** The load-bearing finding: a **probe-vs-landed-docs DRIFT** — BC:900 + ADR-0286 D-SDSVC-FETCHTIMEOUT + `config.go:116` record the reference as serve-anyway on validation-context fetch failure (a SERVER-CERT-probe extrapolation from SPEC-60 §11 arm B) where this session's probe pinned init-hold-then-FAIL-CLOSED ⇒ the NAMED SPEC obligation **D-RCCF-FETCHFAIL-POSTURE** (reconcile BEFORE drafting ADR-0289 §Context, ideally by a discriminating server-cert-fetch-failure probe — never a silent rewrite). QUIC client-auth provisionally OUT (`NewQUICDownstreamConfig` never reads the field; final call at the SPEC, **D-RCCF-QUIC**); ONE unprobed cell (require=false + anchor-less inline VC, **D-RCCF-ANCHORLESS-VC-FALSE**) sent to the SPEC; a require=false+RBAC/principal fixture arm NEWLY deferred (§2.11). Fixture: anticipated ONE new **`0110-tls-require-client-cert-false`** (the `0109` chassis + a third arm + the forced-send driver; three-arm cross-side verdict — no-cert→ok is the discriminator vs require=true, untrusted→rejected proves the anchor LIVE). **Envelope:** ZERO new packages / modules / `internal/xds` symbols; +0 stats; anticipated a SINGLE FLAT ROW (~8–12 tasks; ADR-0045 valve armable, no split anticipated). **Row 67 registered `in-progress`** at this stage-close commit per the ROADMAP §Schema invariant; flips `done` at the phase-67 IMPL six-gate (the SOLE leg, ADR-0106). **NO deferred sentence is narrowed at ANY stage** (a §8-tier pickup; the phase-64/66 precedent). **Counts UNCHANGED at this BRAINSTORM (docs-only; count commands re-run MECHANICALLY in the worktree at this close):** stat surface **1201** · fixtures **111** (numeric tail `0109-xds-sds-combined-validation-context`) · fuzzers **55** (`internal/` scope) · BackendKind **38** · DECISIONS tail **ADR-0288** (next-free **ADR-0289**) · go.mod modules **2** (lineage figure; the single `go.mod` requires 67). **Sentinel re-run MECHANICALLY at this close (does NOT fire; `stop` NOT created):** (1) now prints `NOT DONE: row 67` (the registration re-opens check (1) — the loop is held open by ALL THREE checks again); (2) prints THREE live "candidates:" sentences (`grep -cE 'remaining deferred \(not-yet-chartered\) candidates:'` ⇒ **3** — the command, not the adjective); (3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`. **Next → the phase-67 SPEC** (RE-DERIVE the BRAINSTORM rather than execute it; the owed roster is in §Current pointer's next-skill line).

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
