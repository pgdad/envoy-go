# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

> **This file is a POINTER, not an archive** — `BOOTSTRAP_PROMPT.md` §4: *"STATE.md — pointer: active phase, last commit, next action."* It carries the live pointer, the live counts, and a SHORT recent lineage. **Full stage-by-stage history lives in `STATE_HISTORY.md`** (archived at ADR-0288; **nothing was deleted** — the pre-move file is preserved there verbatim, byte-for-byte).
>
> **⚠️ THE RULE ADR-0288 EXISTS TO ENFORCE: there is EXACTLY ONE live `next-skill:`, `lifecycle-state:`, `next-free ADR:`, and count block IN THIS FILE, and they live in §Current pointer directly below.** Before that move, ~330 lines of prepended backlog had buried TWO fossilised "current state" blocks whose `next-skill:` still read *"the 42.2 hedging BRAINSTORM"* and *"the 46.2 SPEC"* while the live phase was **66** — so `grep 'next-skill:' STATE.md` returned **two stale hits and ZERO live ones**, and `grep 'fuzzers:'` returned **48** and **42** when the true count was **55**. **A session may trust a grep of this file again. Keep it that way: when you advance the pointer, EDIT §Current pointer IN PLACE — never prepend a new block above it.**

---

## Current pointer

*(The ONLY live "what next" in this repo. Update IN PLACE at every stage close.)*

- **active-phase:** `phase 73 (tracing-custom-tags-metadata-cluster) IMPL done` (2026-07-24) — **row 73 `done`**; the phase is COMPLETE
- **phase-directory:** `docs/envoy-go/phases/73-tracing-custom-tags-metadata-cluster/` (`BRAINSTORM.md` + `SPEC.md` + `PLAN.md` + `PROGRESS.md` — all four landed; `PROGRESS.md`'s Status/Commit columns are POPULATED at this IMPL close. **Row 73 is `done`** — it flipped at the phase-73 IMPL six-gate per ADR-0106, the SOLE leg, a SINGLE FLAT ROW)
- **lifecycle-state:** `DONE` (the phase-73 IMPL landed; **row 73 `done`**; the four-stage lifecycle BRAINSTORM → SPEC → PLAN → IMPL is COMPLETE for phase 73 — the IMPL was the LAST stage). Subject as landed: the tracing `custom_tags` `metadata` type for the **`CLUSTER`** MetadataKind — the SEVENTH tracing `custom_tags` capability and the **FOURTH and LAST** of the four `metadata` MetadataKinds, so **this row CLOSED the quartet** (`REQUEST`@70, `ROUTE`@71, `HOST`@72, `CLUSTER`@73) and **no MetadataKind rejects any more**. It lifted the `tracing: custom_tags metadata tag %q cluster kind unsupported` reject and resolves the OWNING CLUSTER's static `metadata.filter_metadata[ns]` (walked by `MetadataKey.path`, FULL path) onto the ingress SERVER span via the shared `ResolveCustomTags` → `Span.Attrs []KV` seam on BOTH exporters; `descend`/`structpbValueToString` reused VERBATIM, proven by sha256. **SEAM A (endpoint-carried) landed as decided** — a second aliasing `clusterFilterMetadata` field + a nil-safe `ClusterMetaLookup` accessor on `cluster.Endpoint`, populated by a LOOP in `buildCluster` placed IMMEDIATELY after the `extractEndpoints` err guard and BEFORE ALL LB CONSTRUCTION, reached as `picked.ClusterMetaLookup` at the three `ResolveCustomTags` call sites. `unsafe.Sizeof(Endpoint{})` **112 → 120** MEASURED; **ZERO new imports anywhere**; the SEVEN-file sha256 envelope HOLDS incl. `actions.go`/`route.go`; all 18 emit callers BYTE-UNTOUCHED. **The departure roster as landed is TWO reject ARMS across THREE config spellings** — the fully-unset `kind` (two spellings) and the newly discovered entirely-absent `metadata_key` — plus TWO documented error-string collisions, NEITHER split. The pick-time departure gained its FIFTH consumer at the SAME breadth as phase 72's HOST tag, with the **circuit-breaker arm now EXECUTED-PARITY on the reference** (reference EXECUTED, envoy-go RE-DERIVED — not cross-side probed) and **pool-overflow alone unprobed**.
- **next-skill:** **the phase-74 BRAINSTORM** — the roller **SELF-PICKS** the subject per the 2026-07-12 standing directive (smallest defensible ROW-SIZED candidate first; record the pick AND the fully-adjudicated rejected roster in the BRAINSTORM). The sentinel does **NOT** fire: ⚠️ **check (1) now prints NOTHING because row 73 was the LAST non-`done` row — that is the NORMAL post-IMPL state and is NEVER on its own a termination condition**; checks (2) ⇒ **3** and (3) ⇒ `NEVER OPENED: gRPC/Runtime/WASM` still print, so `stop` was NOT created. **The Observability family STAYS OPEN**; after this row's narrow the live `candidates:` sentence carries FOUR candidates (the `ssl` stat family + `spawn_upstream_span` + `http_service` + force-trace). ⚠️ **Do NOT carry forward the four claims this row refuted** (the ROADMAP row-73 cell's three and the ADR-0295 heading's two over-claims — all corrected at this IMPL). ⚠️ **NEW at this IMPL — check (1)'s blind spot is BIGGER than recorded and has a THIRD cause:** re-derived MECHANICALLY here, check (1) sees **101 of 105** table rows; the four it cannot see are `00` (an em-dash "after" column), `04` (the DOT in the slug `http-1.1`), and — **not previously recorded** — `28.1a`/`28.1b` (a LETTER SUFFIX in the number field, which `[0-9.]+` cannot match). The prior "2 of 103" figure came from a set-difference whose own baseline regex ALSO excluded the letter-suffixed rows. All four are `done`, so there is no current impact, but check (1) is LESS exhaustive than documented. **Deferred items this row surfaced and DELIBERATELY did not add to the `candidates:` sentence** (they live in BEHAVIOR_CONTRACT B2 + ADR-0295 §Consequences instead, so nothing is lost — the omission is recorded, not silent): the absent-`metadata_key` DEPARTURE and the two error-string COLLISIONS. Also live: the pick-time `picked`-propagation row (pool-overflow the sole unprobed arm), `access_log[].filter` (the strongest FUTURE pick, its divergence PROVEN BY EXECUTION), `hcm.access_log_options`, the `stdout`/`stderr` access loggers, `stats_flush_on_admin`, and the never-opened families. **Live hazards to carry:** the PRE-EXISTING `internal/cluster` `-race` flake (`outlier_test.go:766` — isolate-re-run, do NOT re-classify; it did NOT reproduce at this row's gate), the full-suite startup flake, the SDS init-fetch dial-budget flake, and TWO newly-observed UNINDEXED load flakes (`internal/httpclient TestOptions_ZeroValue_NoOpDefaults`, `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery`) — isolate-green, NOT root-caused. Fresh worktree off master (`feedback_git_worktrees`); subagent-driven (`feedback_execution_style`); subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`); **use `git -C <abs-worktree-path>` everywhere and tripwire `pwd` + branch + commit count before any commit or gate run** (`reference_bash_cwd_reset_commits_to_main`).
- **last-commit:** the phase-73 IMPL squash (`git log --grep 'phase 73'`; locate by SUBJECT, never by position — `reference_next_prompt_tracked_despite_gitignore`). Prior: the phase-73 PLAN squash; before it the phase-73 SPEC squash and the phase-73 BRAINSTORM squash + its router follow-up `d51d33dc`.
- **last-updated:** 2026-07-24 (phase-73 IMPL done. **ROW 73 FLIPS `in-progress` → `done`** at the six-gate (ADR-0106, the SOLE leg — a SINGLE FLAT ROW); **ADR-0295 COMPLETED IN PLACE** (§Decision + §Consequences APPENDED after the retained `*(§Decision + §Consequences land at the phase-73 IMPL.)*` footer — tail stays **ADR-0295**, no renumber, no new ADR, exactly ONE `### Decision` heading inside the block: 0 before, 1 after). Fresh worktree `.worktrees/phase-73-impl` off master `bea4178c` (the phase-73 PLAN squash), branch `phase-73-tracing-custom-tags-metadata-cluster-impl`; subagent-driven, each subagent committing LOCALLY only, controller squash-push at close. Seven task commits `f6f3cede`/`1e74f996`/`91f354d8`/`97428088`/`ba92316c`/`849632c0`/`b9225e08` + this docs close. **Six-gate ALL GREEN:** gofmt SILENT / vet 0 / build 0 / `go mod tidy -diff` EMPTY + `go.mod`/`go.sum` NO DIFF vs master (+0 modules — **ZERO new imports anywhere**) / golangci-lint 0 / **the FULL 119-dir differential `ok 410.259s` EXIT 0, ZERO failures** with `0117` confirmed to run BY NAME / the layering check EMPTY (⚠️ a DISCIPLINE check, NOT a cycle check) / `-race` green on all three touched packages / **exactly ONE new exported symbol** by AST set-diff. **THREE PLAN instructions were REFUTED BY EXECUTION and are recorded rather than quietly fixed:** T4's `span_emit_test.go ⇒ 21` caller-count gate is UNSATISFIABLE BY CONSTRUCTION (landed 13+**29**=42 — the SAME defect class the PLAN had diagnosed twice for sha256 gates and then re-committed one level down; the intended invariant was verified in a STRONGER form); T1's +23 lines invalidated a landed BEHAVIOR_CONTRACT `.go:NNN` cite (`cluster.go:722` → `:745`), corrected at T7 with a sweep finding no others stale; and **Break B needs TWO breaks at TWO LAYERS**, since test 1(e) hand-constructs the `Endpoint` and never calls `buildCluster`. **NO break failed to fire at any task.** The FOUR stale claims were corrected, not propagated (the ROADMAP row-73 cell's three + the ADR-0295 HEADING's two over-claims, the heading having been the SOLE surviving carrier repo-wide); ADR-0294 `:17209` gained a bracketed `[CORRECTED at the phase-73 IMPL: …]` note per its own §Context precedent. The deferred sentence NARROWED at this row-done edit (phase-57 precedent), `count == 1` re-verified first: check (2) STAYS **3**, FOUR candidates remain. STATE §Current rolled IN PLACE (lifecycle 3 → DONE; lineage re-capped at five WITH its preamble updated); `next-prompt.txt` rolled to the phase-74 BRAINSTORM. Sentinel re-run MECHANICALLY: it does NOT fire; `stop` NOT created. Counts re-run MECHANICALLY in the worktree.)
- **next-free ADR:** **`ADR-0296`** — DECISIONS.md tail is **ADR-0295** (STATUS **COMPLETE** — §Context drafted at the phase-73 SPEC per ADR-0044; §Decision + §Consequences APPENDED IN PLACE at the phase-73 IMPL, no renumber, no new ADR). `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1` → `## ADR-0295`; `grep -c '^## ADR-0296'` → 0.

---

## Project counts at this commit

*(Live. Re-verify at session start — `git fetch` first — and update here at every stage close. Canonical commands inline.)*

- **active differential fixtures:** **119** — `ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0117-tracing-custom-tags-metadata-cluster` (added at phase 73 — the OTLP CLUSTER-metadata-custom_tags fixture: **TWO clusters** carrying DIFFERENT static `metadata.filter_metadata` in an ARBITRARY non-`envoy.lb` namespace behind two path-prefix routes onto the **SAME single backend**, so it DISCRIMINATES *selected cluster* from *only cluster*; spans partitioned per-path by **`http.url`**, two CLUSTER custom_tags cross-side EXACT key+value, STRING values only, NO writer; reference port 10117).
- **fuzzers:** **55** — `grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`. Phase 73 added a `meta_cluster_ok` `withMetaTags` seed AND repointed `meta_bad` to `Kind: &metadatav3.MetadataKind{}` ⇒ **+0** (an `f.Add` seed is not a fuzzer — `reference_fuzzer_count_docs_drift`).
- **stat surface:** **1201** — BEHAVIOR_CONTRACT doc count. **There is NO mechanical counting command.** Phase 73 was **+0** (a span attribute registers no stat; tracing has no per-tag stat surface, so NO `TestNoNewStat` guard is owed — and none was manufactured).
- **BackendKind tail:** **38** (`H2GoawayResponder`). Phase 73 was **+0** (`0117` reuses `HTTPFixedBody`; the `otlptrace` receiver is driver-owned; NO writer).
- **DECISIONS.md tail:** **ADR-0295** (next-free **ADR-0296**) — `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1`. **ADR-0295 is COMPLETE** — §Context drafted at the phase-73 SPEC per ADR-0044 (the tail flipped ADR-0294 -> ADR-0295 AT that commit; the §Context append IS the tail flip), §Decision + §Consequences APPENDED IN PLACE at the phase-73 IMPL after the RETAINED hand-off footer, no renumber and no new ADR. Verified at this IMPL close: 1 `### Context`, **1** `### Decision`, **1** `### Consequences` inside the block (0 `### Decision` before the append). ADR-0294/0293/0292 stay COMPLETE.
- **go.mod modules:** the tracked lineage figure is **2** (the phase-61.2 count: `quic-go` direct + `qpack` indirect) — **NOT a repo total**; the single `go.mod` requires **67** modules. Phase 73 was **+0** (`go mod tidy -diff` EMPTY; `go.mod`/`go.sum` byte-unchanged vs master — **ZERO new imports anywhere**, unlike phase 72's one).
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` (see `ENVOY_TARGET.md`).
- **conformance:** h2spec 53/53; proxy-wasm all 10 families PASS.

---

## Recent lineage

*(The last FIVE stages only — enough for the router's "prior-phase lineage" read. Everything earlier is in `STATE_HISTORY.md`. These bullets are carried over VERBATIM; the newest mirrors §Current (**the phase-73 IMPL done** — the stage that CLOSED the `metadata` MetadataKind quartet and had THREE of its own PLAN's instructions refuted by execution), and the oldest (**phase-72 PLAN done**) was dropped as this list re-capped at five. The five now read: phase-73 IMPL · phase-73 PLAN · phase-73 SPEC · phase-73 BRAINSTORM · phase-72 IMPL.)*

- **prior active-phase:** `phase 73 (tracing-custom-tags-metadata-cluster) IMPL done` (2026-07-24) — lifecycle-state 3 → DONE; **ROW 73 FLIPS `in-progress` → `done`** at this IMPL six-gate (ADR-0106, the SOLE leg — a SINGLE FLAT ROW, `reference_roadmap_split_phase_row_done`); **ADR-0295 COMPLETED IN PLACE** (§Decision + §Consequences APPENDED after the RETAINED hand-off footer; tail stays **ADR-0295**, no renumber, no new ADR; next-free **ADR-0296**). Fresh worktree `.worktrees/phase-73-impl` off master `bea4178c` (the phase-73 PLAN squash), branch `phase-73-tracing-custom-tags-metadata-cluster-impl`; subagent-driven (`feedback_execution_style`) — one implementer per task + a per-task adversarial reviewer, each committing LOCALLY only (`feedback_subagents_no_push`), controller squash-push at close. Each task RE-DERIVED its anchors at the IMPL tip (a PLAN is not evidence either). **The 9-task spine landed** (each red-first → green → break-verified): **T1** (`f6f3cede`) `internal/cluster` — a SECOND unexported `Endpoint.clusterFilterMetadata map[string]*structpb.Struct` field ALIASING `c.GetMetadata().GetFilterMetadata()` + a nil-safe `ClusterMetaLookup(ns)` accessor beside phase-72's `MetaLookup`, populated by a LOOP in `buildCluster` placed IMMEDIATELY after the `extractEndpoints` err guard and **BEFORE ALL LB CONSTRUCTION**; ALIASING proved MECHANICALLY (a brand-NEW namespace mutated post-`buildCluster` is visible through the accessor ⇒ the map HEADER is shared, not just its values); `unsafe.Sizeof(Endpoint{})` **112 → 120** MEASURED; **ZERO new imports anywhere**; `extractEndpoints`'s signature + `manager_test.go` BYTE-UNCHANGED and `manager.go`'s diff PURELY ADDITIVE (zero `-` lines). **Break D — THE ROW'S DISTINGUISHING BREAK — FIRED WITH EXACT DISCRIMINATION:** moving the loop after `newSubsetLB` failed `SubsetChild` and `SubsetDefaultFallback` while `RoundRobinPathStaysGreen`, populate-live, aliasing, projection and pool-HIT provenance ALL stayed green — the placement constraint is enforced by execution, not by comment; and the PLAN's warning that Break D is VACUOUS through `Cluster.PickEndpoint()` was RE-CONFIRMED LIVE at this tip (a subset-configured `PickEndpoint` returned `ok=true` under the break), so test 3 drives the package-internal `subsetLB.Pick(…, hasMatch=true)`. Breaks A/B/C also fired · **T2** (`1e74f996`) `kindMetadataCluster` APPENDED at **iota == 6**, EXECUTION-proven — the pre-known red was EXACTLY TWO `/cluster` subtests and its output PRINTED **`Kind:6`** — plus the CLUSTER accept arm (a byte-clone of the HOST accept, `MetaPath` FULL) replacing the `:268-269` reject, **the LAST kind-specific reject**; gofmt re-aligned exactly `:58`-`:61` as predicted; the stale comments narrowed incl. `:185` AND `:190`, whose "ADR-0080-distinct" claims go FALSE because `:233` and `:288` now COLLIDE on `kind required`; breaks E/F/G/H/H2 all fired (F fired BOTH properties independently; G fired exactly the two iota pins and nothing else) · **T3** (`91f354d8`) the nil-tolerant one-arg `clusterMetaLookup` **6th** param (the 3rd/4th/5th lookups UNTOUCHED) + a `kindMetadataCluster` arm descending the **FULL** `MetaPath`, so three of the four kinds now walk the full path; `descend`/`structpbValueToString` reuse proven by sha256 and the landed HOST arm byte-identical at its shifted span with exactly ONE removed line; 49 `resolve_test.go` callers += trailing `nil` (raw grep 50; `:107` a `t.Errorf` literal, gated by LINE); breaks I/J/K/K2 fired, **Break I's REQUEST/ROUTE/HOST half staying ENTIRELY green ⇒ the four arms are not entangled**; the agent ADDED **K2b** (crossing inside the production arms rather than at a test call site, because K2a as written edits the very test that catches it) — both fire, compiler silent both ways · **T4** (`97428088`) `picked.ClusterMetaLookup` at `accesslog_emit.go:57`/`:118`/`:179` and NOWHERE else — **THREE production lines**; **the SEVEN-file sha256 envelope HOLDS** (`connection.go`, `h2dispatch.go`, `h3dispatch.go`, `chain.go`, `subset.go`, **`actions.go`**, **`route.go`** — the last two precisely what the REJECTED route-carried seam would have touched, so the seam decision is honoured MECHANICALLY) plus `manager_test.go`, and all three emit SIGNATURES and all **18** emit callers are BYTE-UNTOUCHED; breaks L/M/M2 fired, **M2 the one that mattered — the compiler AND `go vet` AND `golangci-lint` were ALL SILENT on a 5th/6th argument crossing, and the distinctness test fired with the CROSSING SIGNATURE (values transposed: `dual_host="from-cluster"`, `dual_cluster="from-host"`), which is what identifies a crossing rather than a mere omission** · **T5** (`ba92316c`) an ADDITIVE `meta_cluster_ok` seed + the MANDATORY `meta_bad` REPOINT to `Kind: &metadatav3.MetadataKind{}` (present kind, oneof unset — the `:288` PARITY arm), mandatory because as-written the seed carried NO `MetadataKey` and would post-lift reject SILENTLY on `empty namespace` — itself the newly-found absent-`metadata_key` DEPARTURE; **dispatch-verified with THREE hypotheses stated BEFORE the run and the arms TAGGED SEPARATELY** (`:233`/`:288` now COLLIDE on `kind required`, so the error text alone would launder the result), printing `PROBE ARM=CLUSTER_ACCEPT tag=meta_cluster_ok` / `PROBE ARM=DEFAULT_UNSET_KIND tag=meta_bad` with `meta_bad` still LAST; **two hypotheses REFUTED by that run**; +0 fuzzers · **T6** (`849632c0`) the OTLP fixture **`0117-tracing-custom-tags-metadata-cluster`** — TWO clusters with different `metadata.filter_metadata` behind `prefix: /a` and `prefix: /b` onto the SAME single backend, `BackendCount` 1, zero LB spread. **The partition mechanism is `http.url` (PRIMARY); the named batch-poll fallback was NOT needed** — it renders BYTE-IDENTICALLY on both sides (`"http://trace.example/a"` / `".../b"`) across all 24 spans, partitioning 6/6 per side with ZERO unclassified (the driver still matches by SUFFIX, since `0116` declined to pin `http.url` by value), and the partition sizes are `Fatalf` PRECONDITIONS so a broken partition can never make the value assertions dead code. **Breaks N/O/P/Q/R ALL fired, with Q — the SELECTION break — firing on BOTH paths (24 firings) ⇒ the fixture DISCRIMINATES *selected* from *only*, the exact limitation `0116` had to confess**, and R making BOTH sides fall to default ⇒ CLUSTER ≠ HOST cross-side; no substitutions needed. The `upstream_cluster` framework gap was re-confirmed LIVE in the same dump (reference `"c_backend_a"`/`"c_backend_b"`, envoy-go `""`), which is why it could not serve as the discriminator; fixtures 118 → 119 · **T7** (`b9225e08`) BEHAVIOR_CONTRACT **B1-B5** — four hunks in a 5730-line file, line count unchanged, exactly ONE new `upstream_cluster` mention (inside B3(i), reference-side phrasing) · **T8** the six-gate · **T9** ADR-0295 IN PLACE + row-73-`done` + the deferred-sentence narrow + this roll. **Six-gate ALL GREEN:** gofmt SILENT / vet 0 / build 0 / `go mod tidy -diff` EMPTY + `go.mod`/`go.sum` NO DIFF vs master (**+0 modules — ZERO new imports anywhere**) / golangci-lint 0 / **the FULL 119-dir differential `ok 410.259s` EXIT 0, ZERO failures**, `0117` CONFIRMED to run BY NAME / the layering check EMPTY — ⚠️ **a DISCIPLINE check, NOT a cycle check** / `-race` green on all three touched packages, the PRE-EXISTING `outlier_test.go:766` flake NOT reproducing / **exactly ONE new exported symbol** by AST set-diff (`internal/cluster` 58 → 59; `internal/tracing` 49 → 49 with only the `ResolveCustomTags` signature growing; `internal/filter/hcm` 6 → 6) / the 18-caller roster re-derived **5+6+7** with the gated regex / the fixture tree exactly 5 files, NO `scripts/` / `git status` and `git clean -nd` EMPTY / nothing changed outside `internal/`, `test/`, `docs/`. **THREE PLAN INSTRUCTIONS WERE REFUTED BY EXECUTION, recorded rather than quietly fixed:** (1) **T4's `span_emit_test.go ⇒ 21` caller-count gate is UNSATISFIABLE BY CONSTRUCTION** — every T4 test drives through `emitAccessLog`/`H2`/`H3`, so each new test IS a new caller (landed 13 + **29** = 42, not 34); **this is the SAME defect class the PLAN diagnosed TWICE for sha256 gates and then re-committed one level down** when it downgraded sha256 → caller-count, and the invariant actually intended was verified in a STRONGER form (the 21 pre-existing callers byte-identical AT UNCHANGED LINE NUMBERS, the first 779 lines hashing identically to master, ZERO deletion lines in both emit test files). (2) **T1 invalidated a landed BEHAVIOR_CONTRACT citation the PLAN never flagged** — the B2 pool-overflow sentence cited `cluster.go:722`; T1's +23 lines shifted it to `:745`, corrected in place at T7, with a full sweep of every `.go:NNN` cite in that region finding no others stale. (3) **Break B needs TWO breaks at TWO LAYERS, not one** — test 1(e) hand-constructs the `Endpoint` and never calls `buildCluster`, so a loop-level wrong-source break fires the BUILD-level assertions while `TestEndpoint_ClusterMetaLookup` stays GREEN; only an ACCESSOR-level wrong source fires 1(e). **NO break failed to fire at any task.** **FOUR stale claims corrected at T9, none propagated:** the ROADMAP row-73 cell's three (the narrow-hazard sentence — the narrow SHORTENS the clause, leaving FOUR candidates with check (2) STAYING 3; the "only the two unset-kind arms remain" clause; the single-cluster `0117` description) **plus the ADR-0295 HEADING's two over-claims**, which the SPEC's own §16 asserted were already fixed and were not — the body had been corrected and the heading had not, making it the SOLE surviving carrier repo-wide. ADR-0294 `:17209` gained a bracketed `[CORRECTED at the phase-73 IMPL: …]` note per ADR-0294's OWN §Context precedent, retiring its refuted "CLUSTER RE-PAYS the 18-caller threading tax" close. **Counts at exit (re-run MECHANICALLY):** fixtures **118 → 119** (tail `0117-tracing-custom-tags-metadata-cluster`) / fuzzers **55 (+0)** / stat surface **1201 (+0)** / BackendKind **38 (+0)** / go.mod modules **2 (+0)** / **ZERO new packages** / DECISIONS tail **ADR-0295** COMPLETE (next-free **ADR-0296**). **The Observability family STAYS OPEN** — the `CLUSTER` MetadataKind rolled OUT of the live `candidates:` sentence at this row-done edit (phase-57 precedent), **CLOSING the quartet**: all four `metadata` MetadataKinds are consumed and no MetadataKind rejects. ⚠️ **The two deferred items this row newly surfaced are DELIBERATELY NOT in the `candidates:` sentence** — the absent-`metadata_key` DEPARTURE and the two error-string COLLISIONS live in BEHAVIOR_CONTRACT B2 + ADR-0295 §Consequences, so nothing is lost, but the omission is recorded rather than silent. **Sentinel re-run MECHANICALLY (does NOT fire; `stop` NOT created):** (1) prints **NOTHING** — row 73 was the LAST non-`done` row, the NORMAL post-IMPL state and NEVER on its own a termination condition; (2) ⇒ **3**; (3) prints `NEVER OPENED: gRPC/Runtime/WASM`. ⚠️ **NEW: check (1)'s blind spot is FOUR of 105 rows, not two of 103, and has a THIRD cause** — `28.1a`/`28.1b` carry a LETTER SUFFIX the `[0-9.]+` number field cannot match, and the prior figure came from a set-difference whose own baseline regex ALSO excluded them; all four blind rows are `done`, so no current impact. **Next → the phase-74 BRAINSTORM** (the roller SELF-PICKS per the 2026-07-12 standing directive).

- **prior active-phase:** `phase 73 (tracing-custom-tags-metadata-cluster) PLAN done` (2026-07-24) — lifecycle-state 2 → 3; docs-only (`PLAN.md` + `PROGRESS.md`, the ONLY two files in the squash's phase-directory delta — **ZERO production `.go`, DECISIONS and ROADMAP UNTOUCHED**, row 73 STAYS `in-progress`, ADR-0295 stays PROPOSED); fresh worktree `.worktrees/phase-73-plan` off master `79f96455` (== the phase-73 SPEC squash; **NO parallel stream** — `git diff --stat d51d33dc 79f96455 -- '*.go' go.mod go.sum test/` EMPTY, so the production tree was byte-identical to what the SPEC re-derived), branch `phase-73-tracing-custom-tags-metadata-cluster-plan`, per `feedback_git_worktrees`; subagent-driven. The stage produced the NINE-task single-flat-row TDD spine (T1-T9), each task red-first → green → break-verified with its own pre-known red set and exit gate, plus **EIGHTEEN pinned breaks (A-R)**. **TWO read-only agents on disjoint remits RE-DERIVED every SPEC §3/§9/§10/§11/§12/§14 anchor at the PLAN tip: ZERO DRIFT and ZERO REFUTED on every CODE anchor**, plus NINE new findings (F1-F9) — three stale-claim carriers the SPEC leaves unassigned, two `old_string` uniqueness hazards, and one that verified the populate-placement hazard's MECHANISM rather than inferring it. **THE HEADLINE — THE ADVERSARIAL PASS REFUTED THE PLAN'S OWN INSTRUCTIONS TWICE, AND ONE OF THOSE REFUTATIONS CAME FROM EXECUTION.** V1 built T1→T4 from the PLAN's skeletons pasted VERBATIM in private scratch and found (a) **`manager_test.go` was scheduled for four test additions while sha256-gated BYTE-UNTOUCHED in six places** — an unsatisfiable gate, and the identical defect the PLAN had already diagnosed for the emit test files without applying it to itself (T1's tests now land in a NEW `cluster_meta_test.go`; the gate is KEPT); and (b) **Break D — the row's DISTINGUISHING break — stays GREEN through `Cluster.PickEndpoint()`**, because that path passes `hasMatch=false` so `subsetLB.Pick` never consults `s.subsets` and falls through to a fallback that ALIASES the original slice (test 3 is now pinned on the package-internal `subsetLB.Pick(…, hasMatch=true)`, zero new exported symbols). V2 independently REFUTED the PLAN's claim that sentinel check (1) does not go silent at this row's IMPL close — **row 73 is the LAST non-`done` row**, so it does, and that is the NORMAL post-IMPL state, never on its own a termination condition. V1 also CONFIRMED the row's headline claims BY EXECUTION: the seven-file sha256 envelope, ZERO new imports and +0 modules, `sizeof(Endpoint)` 112 → 120, exactly ONE new exported symbol, T4's three-error entry state (the `ResolveCustomTags` arity, **NOT** the EMIT-signature arity), the 49-caller `resolve_test.go` transform, and that Breaks K2/M2 COMPILE CLEAN when the adjacent identically-typed lookup args are swapped — proving the distinctness tests are load-bearing rather than decorative. **ONE SEVERE, TWO MODERATE, ONE MAJOR-process and TEN MINOR findings, all folded** (PLAN §1.4); the controller re-verified every load-bearing correction at the tip before folding and found one further defect itself (a FOUR-vs-three copy-site mismatch). **Explicitly NOT verified, and recorded as such so the IMPL inherits no false confidence:** nothing reference-side (no Docker, no probes — every reference/PGV claim rests on the SPEC's own probe record), T5-T7 and T9 unexecuted, Breaks N/O/P/Q/R untested, and **T6 Step 1's `http.url` span-partition reconnaissance unaddressed — the IMPL's first live risk**, since `upstream_cluster` is unusable as a discriminator (envoy-go emits it EMPTY). Sentinel re-run MECHANICALLY TWICE (worktree + landed master post-push): does NOT fire; `stop` NOT created. Counts UNCHANGED.

- **prior active-phase:** `phase 73 (tracing-custom-tags-metadata-cluster) SPEC done` (2026-07-24) — lifecycle-state 1 → 2; docs-only, exactly TWO files in the squash delta (`SPEC.md` new + the ADR-0295 §Context append to `DECISIONS.md`) — **ZERO production `.go`, ROADMAP UNTOUCHED**; fresh worktree `.worktrees/phase-73-spec` off master `d51d33dc` (the phase-73 BRAINSTORM squash + its router follow-up), branch `phase-73-tracing-custom-tags-metadata-cluster-spec`, per `feedback_git_worktrees`; subagent-driven — SIX agents (three read-only: anchor re-derivation · a BY-EXECUTION costing agent that BUILT seam A end-to-end and priced seam B + the hybrid by execution · a repo-wide stale-comment and governance-anchor sweep) plus **three live-probe fleets on disjoint port ranges and container prefixes running 21 EXECUTED arms** against `contrib-v1.37.2` (a FRESH container per arm, bridge networks, the host-gateway literal + `dns_lookup_family: V4_ONLY`, a `literal` positive control in every bootstrap, every arm's discrimination stated BEFORE the run). **THE HEADLINE — D-CTMC-SEAM RESOLVED, AND THE PROBE INVERTED THE BRAINSTORM'S ANTICIPATION.** The BRAINSTORM expected the reference to resolve a CLUSTER tag at ROUTE-SELECTION time, which would have favoured the route-carried seam on fidelity. **It does not: the reference gates a CLUSTER tag on the PICKED UPSTREAM HOST, exactly as it gates a HOST tag, the two kinds moving in lockstep across every failure class and differing only in which metadata block is read.** Cluster SELECTED but NO host picked — zero endpoints (`UH`), DNS-empty (`UH`), all-zero circuit breaker (`UO`) — falls to `default_value` **while `upstream_cluster` sits on the very same span**; host PICKED but connect REFUSED (`UF`) **RESOLVES**, reproduced by two independent fleets on two bootstraps; weighted routes resolve **per-request in a perfect 1:1** with the span's own `upstream_cluster` across 20 requests. **⇒ DECIDED: SEAM A (endpoint-carried)** — a second aliasing `clusterFilterMetadata` field + a nil-safe `ClusterMetaLookup` accessor on `cluster.Endpoint`, reached as `picked.ClusterMetaLookup`; **it is the FAITHFUL seam, not the cheap compromise.** Seam B was rejected on FIDELITY first and cost second: it would MANUFACTURE a divergence, does NOT avoid the `internal/cluster` work (`cluster.Cluster` retains no metadata at all — EXECUTED), re-pays a **52-site** thread (18 production + 34 emit test, enumerated by compile error), needs an H2 carrier (`entry` is provably out of scope at all 6 `h2dispatch.go` emit sites and IN scope at all 12 H1/H3 sites) and leaves a weighted-route hole against a reference that resolves weighted routes perfectly. **The HYBRID was PRICED then refuted BY COMPILE ERROR**, not assumed away (H2's emit helper takes an `h2.H2Request`; `routeTable.match` takes `*http.Request`). A **THIRD seam** (`f.clusters *cluster.Manager`, in scope at the emit helpers) was named and dismissed so the docket closes. **A STRICTLY CHEAPER SEAM-A FORM WAS BUILT AND ADOPTED:** populate in `buildCluster` AFTER the `extractEndpoints` call rather than via a third parameter — leaving `extractEndpoints`'s signature AND `manager_test.go` byte-identical and adding **ZERO new imports anywhere** (the briefed form drags 10 test callers and a `structpb` import) ⇒ 6 files, ~+128/−58. ⚠️ **Placement is load-bearing and is pinned:** the loop must sit IMMEDIATELY after the `err != nil` guard at `manager.go:433-436` and BEFORE any LB construction — after `:472` it would silently zero the field in the subset/locality/priority LBs while round-robin stayed green. **A SECOND HEADLINE — the departure roster is NOT what every prior document said.** It is **TWO reject ARMS across THREE config spellings**, and the second arm is **NEWLY DISCOVERED and never before recorded**: an **entirely-absent `metadata_key`** is ACCEPTED by the reference (validate exit 0 AND a real boot emitting `default_value`) while envoy-go rejects it `empty namespace` — invisible until now because envoy-go emits ONE identical string across a PARITY case (present-but-empty) and a DEPARTURE case (omitted), the reference distinguishing them by message PRESENCE. **A THIRD — the `meta_bad` fuzz repoint is mandatory for a DIFFERENT reason than projected, and the BRAINSTORM's proposed target was the WRONG one:** the seed carries no `MetadataKey`, so post-lift it keeps rejecting **SILENTLY** on `empty namespace` with nothing going red; and the proposed nil-`kind` target is itself a DEPARTURE arm. The correct PARITY target is a PRESENT `MetadataKind` with its oneof unset — arm-DISCRIMINATED both sides. **BY EXECUTION:** gofmt silent · `go build`/`go vet` 0 · `go mod tidy -diff` EMPTY (+0 modules) · golangci-lint 0 · the layering grep EMPTY · the full 75-package suite failing **EXACTLY** the two predicted `/cluster` subtests with the failure printing **`Kind:6`** (⇒ iota==6 proven, not inferred) · `unsafe.Sizeof(Endpoint)` **112 → 120** · exactly ONE new exported symbol · **sha256 proof that SEVEN production files are byte-identical** including `actions.go` and `route.go`, precisely what the rejected seam would have touched · and endpoint provenance EXECUTED (one populating literal repo-wide, no reconstruction path, and a pool MISS→idle→HIT test that PASSES **and DISCRIMINATES**). Serialization: **ZERO CLUSTER-specific difference** ⇒ `structpbValueToString` reuses VERBATIM; three pre-existing boundaries re-confirmed and one **re-quantified far lower** — the ~6-significant-digit scalar rendering bites from `1000000`→`1e+06`; struct key order arbitrary across three fresh processes; and intra-custom-tag emit order arbitrary **ACROSS PROCESSES**, so assertions must be key-based. Zipkin byte-identical to OTLP across the whole value matrix except the empty-string drop, proven GENERIC by the `node_id`/`zone` built-ins in the same payload. Fixture `0117` DECIDED with **TWO clusters behind two path-prefix routes onto the SAME single backend** (deterministic, zero LB spread, `BackendCount` 1) so it **DISCRIMINATES selected-from-only** rather than confessing that limit the way `0116` had to; four breaks incl. the selection break. **ADVERSARIALLY VERIFIED before the squash-push by TWO independent verifiers in PRIVATE scratch on disjoint remits, and BOTH FOUND REAL DEFECTS — ZERO SEVERE, TWELVE MODERATE, ELEVEN MINOR, all folded at the point of use (§16), with TWO findings reached INDEPENDENTLY by both.** V1 (code + by-execution) BUILT the row from the SPEC's own skeletons and reproduced every code-level claim, while catching: the SPEC claiming seam A *"reproduces the reference EXACTLY"* on arms its OWN table marks boot-rejected/unclassified — **an over-claim that had ALREADY LANDED in ADR-0295 §Context** and is now corrected in both (seam B manufactures **ONE** expressible divergence, not three); the pick-gate refutation being called *"triple-sourced"* when **two of the three arms share an empty-host-set confound** and only the circuit-breaker arm DISCRIMINATES; the under-specified populate-loop placement above; a three-ways-wrong `Endpoint{` literal census; a **T4 sha256 gate that was unexecutable by construction** for the two emit test files that same task ADDS tests to; and an unnamed third seam. V2 (process/consistency/stage-close) independently re-ran every mechanical check — envelope docs-only, ROADMAP byte-untouched, check (2) ⇒ 3 **verified by APPLYING the pinned narrow in a scratch copy**, the ADR block shape exact, all 29 cited memory names existing, all five drift corrections RE-DERIVED CORRECT — while catching: a **dangling `See §16.1 below.` pointing at a section that did not exist** (the cited-but-unwritten class recurring inside the very section written to prevent it); the **"THREE, not two" departure count mixing spellings with arms**, already propagated into four documents; §9's B1 and B2 **editing the same physical line** with B2 unanchored and B1 calling "still-true" a clause the SPEC's own finding falsifies; **NO memory-write obligation declared** while two landed memories carry claims this stage refuted; and the ROADMAP row-73 cell carrying THREE stale claims where only one was assigned. V2 also CORRECTED the ADR-0288 singleton check itself: the greps return **2**, not 1, because `STATE.md:7` is the rule statement — a stage "fixing" it to 1 would delete the rule. **Row 73 STAYS `in-progress`** (flips `done` at the phase-73 IMPL six-gate, ADR-0106, the SOLE leg); **NO deferred sentence narrowed** (phase-57 precedent) — `git diff master..HEAD -- docs/envoy-go/ROADMAP.md` EMPTY; DECISIONS tail flipped ADR-0294 → **ADR-0295** (PROPOSED, §Context only — 1 `### Context` / 0 `### Decision` / 0 `### Consequences`, so the IMPL's APPEND is sound; next-free **ADR-0296**). **Counts UNCHANGED apart from the tail (re-run MECHANICALLY):** fixtures **118** · fuzzers **55** · stat surface **1201** · BackendKind **38** · go.mod modules **2** · port `10117` FREE. **Sentinel re-run MECHANICALLY (does NOT fire; `stop` NOT created):** (1) prints `NOT DONE: row 73`; (2) ⇒ **3**; (3) prints `NEVER OPENED: gRPC/Runtime/WASM`. **Next → the phase-73 PLAN** (decompose the NINE-task spine; RE-DERIVE every anchor at the PLAN tip).

- **prior active-phase:** `phase 73 (tracing-custom-tags-metadata-cluster) BRAINSTORM done` (2026-07-23) — lifecycle-state DONE → 1; the roller's AUTONOMOUS self-pick (no human pick per the 2026-07-12 standing directive) continues the Observability family; docs-only, ZERO production `.go`; fresh worktree `.worktrees/phase-73-brainstorm` off master `5aaadf13` (the phase-72 IMPL squash + its router follow-up), branch `phase-73-brainstorm`, per `feedback_git_worktrees`. **Subject:** `tracing-custom-tags-metadata-cluster` — the tracing `custom_tags` `metadata` type for the **`CLUSTER`** MetadataKind, the NINETEENTH Observability-family row *(chain ordinal)*, the SEVENTH tracing `custom_tags` capability and the **FOURTH and LAST** of the four `metadata` MetadataKinds ⇒ **this row CLOSES the quartet**. It lifts the `custom_tags metadata tag %q cluster kind unsupported` reject (`internal/tracing/config.go:268-269`) and resolves the SELECTED CLUSTER's static `metadata.filter_metadata[ns]` onto the ingress SERVER span via the shared `ResolveCustomTags` → `Span.Attrs []KV` seam on BOTH exporters; afterwards only the two unset-kind arms still reject (`config.go:232-233` DEPARTURE / `:287-288` PARITY). **THE DECISIVE FINDING — a STALE-COST CORRECTION, scoped down after adversarial review:** `CLUSTER`'s inherited *"MODERATE ~11-13 tasks, RE-PAYS the 18-caller thread"* adjective is FALSE. The `router.go:255` fact is TRUE; the cost consequence drawn from it is not. Both agents converged on the SOURCE half — the selected cluster IS reachable at the emit seam without a `routeAction` interface method — but **only ONE seam also avoids the 18-caller thread.** **Agent A's seam A (endpoint-carried)** — `extractEndpoints` has exactly ONE non-test call site (`manager.go:433`) with the `*clusterv3.Cluster` in scope (`:429`), so cluster metadata rides phase 72's OWN `Endpoint` retention pattern (`picked.ClusterMetaLookup`), **zero 18-caller thread**, and the `weightedClusterRouteAction` problem DISSOLVES. **Agent B's seam B (route-carried)** — `clusterRouteAction` holds a live `*cluster.Cluster` (the field at `actions.go:183`) with `entry` in scope at all 5 H1 (`dispatchRequest` `connection.go:312-780`, bound `:319`) and all 7 H3 (`h3dispatch.go:125`) emit CALL SITES, the H2 carrier already templated (`h2dispatch.go:158`/`:250`/`:375`) — **but `entry` does not exist inside the emit helpers, so seam B RE-PAYS the full phase-70/71 tax.** A draft asserting that BOTH seams avoided it was refuted INDEPENDENTLY by BOTH adversarial verifiers before landing (BRAINSTORM §11.4), one by building the row, the other by reading the document against its own §6/§10 — so the cost correction is SINGLE-SOURCED and the row is SMALL **on seam A**. ⚠️ **The seams are NOT behaviorally equivalent ⇒ D-CTMC-SEAM is the LOAD-BEARING SPEC question:** seam A INHERITS the B2 zero-`Endpoint` departure and inherits it WIDER than HOST does (the cluster is selected BEFORE any endpoint is tried); seam B does not, at the price of an H2 carrier field + a weighted-route hole (`router_weighted.go:119`). **Decide on PROBED reference behavior — connect failure / no-route 404 / weighted route — not diff size**; a hybrid is admissible. **A SECOND correction, to a LANDED CODE COMMENT** (`reference_code_comment_not_evidence`): `accesslog_emit.go:51`/`:112`/`:173`'s `UpstreamCluster: "", // not available at this seam` is TRUE of the parameter list, FALSE of reachability one layer up — a comment with the authority of proximity that had been discouraging a cheap row; it also sits on `tracing.SpanInputs`, NOT the access-log record. **The `upstream_cluster` span TAG stays a SEPARATE unchartered feature.** **The rivals (fully adjudicated in BRAINSTORM §2.1/§11):** `access_log[].filter` — the two agents SPLIT on size (SMALL ~8-9 vs MODERATE ~9-11) and **a split resolves AGAINST the candidate** (the phase-72 precedent), though its divergence is now **PROVEN BY EXECUTION** (identical config boots clean with `filter`, fails `unknown field` with a renamed arm ⇒ a KNOWN field ACCEPTED and SILENTLY IGNORED) and it is RE-FLAGGED the strongest FUTURE pick, with the global access-log sink flattening (`hcm/config.go:363`) and the `headerCaptureSink` non-promotion hazard both on record, plus **13** reject arms not 12 (PGV makes `filter_specifier` required) and `runtime_key` PROVEN a non-blocker (`layered_runtime` is boot-rejected, so both sides fall to `default_value` ⇒ guaranteed parity) · the pick-time `picked`-propagation row (SMALL ~6-8, both agents agreeing; both independently found conn-pool overflow returns a NON-ZERO `ep` at `cluster.go:720-722`, forking the row's scope — deferred as PROBE-dominated, the reference's per-class attribution rule still unprobed) · `stats_flush_on_admin` (MODERATE — three subsystems, a **53-call-site** `admin.New` to route around with a setter, a timing-shaped NEGATIVE assertion, and a fuzz seed whose classification INVERTS) · QUIC `QuicProtocolOptions` **RE-COSTED UPWARD to LARGE ~11-12** (the only candidate where harness work exceeds production work) · the `Runtime` family (MODERATE ~9 — genuinely unbuilt, but it lights up **ZERO** existing consumers: all ~10 runtime-adjacent sites are themselves parse-rejects or documented ignores) · xDS (the "no persistent stream" finding RE-CONFIRMED MECHANICALLY — `grep -n "for {" internal/xds/stream.go` returns NOTHING; smallest rows are inside the SDS envelope ~7-8, everything else blocked behind the `dynamic_resources` boot-reject) · `spawn_upstream_span`/`http_service`/`sampler`/`resource_detectors`/`verbose`/force-trace/the `ssl` stat family/a 6th sink — all unchanged from phase 72's adjudication. **NEWLY NAMED deferred candidates from this session's sweep:** **`hcm.access_log_options`** (field 56, **ZERO** production consumers, controller-verified — the same accepted-and-silently-ignored class, plausibly cheaper; deferred on EVIDENCE DEPTH, only one agent assessed it) and the **`stdout`/`stderr` access loggers** (`bootstrap.go:952-956` falls through to `return nil`; found INDEPENDENTLY by both agents, both hitting the same provability blocker — the reference's stdout returns via `docker logs`, so the landed `ReferenceLogMounter` chassis does not apply). Also DIAGNOSED but NOT chartered: **the global access-log sink flattening is a live divergence in its own right** — two listeners with different access-log files write every request to BOTH, masked because all six access-log fixtures carry exactly one entry. **Envelope anticipated:** ZERO new packages, +0 stats (**1201**) / +0 fuzzers (**55**, a seed add + the MANDATORY `meta_bad` repoint) / +0 BackendKinds (**38**) / +0 go.mod modules (**2**), ONE new OTLP fixture (`0117-tracing-custom-tags-metadata-cluster`, STRING values only, NO writer) ⇒ fixtures **118 → 119**; ONE ADR (**ADR-0295**, §Context at the SPEC per ADR-0044). ⚠️ **Unlike phase 72's, this BRAINSTORM did NOT build the row in scratch** — the seam analysis is a READ-ONLY derivation and NO probe has confirmed either seam compiles; nothing here is execution evidence for buildability (§11.1 says so explicitly). **Row 73 registered `in-progress`** at this stage-close commit per the ROADMAP §Schema invariant (plus a phase-73 charter sentence APPENDED to the `### Observability family` heading paragraph — the phase-70/72 paired-edit precedent). **NO deferred sentence narrowed at ANY pre-IMPL stage** (phase-57 precedent) — proven MECHANICALLY: the ROADMAP diff is exactly **2 insertions / 1 deletion**, the family-paragraph edit is a **PURE APPEND** (the old line is a byte-prefix of the new, +2567 chars), and the deferred sentence is **BYTE-IDENTICAL** across the diff. ⚠️ A hazard flagged for the IMPL: `CLUSTER` is the LAST item in its clause, so its roll-out REMOVES the clause rather than shortening it — confirm the sentence still parses and still carries ≥1 candidate or check (2) changes count. **DECISIONS.md UNTOUCHED.** **Counts UNCHANGED at this BRAINSTORM (docs-only; re-run MECHANICALLY in the worktree at this close, canonical commands):** fixtures **118** (`ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0116-tracing-custom-tags-metadata-host`) · fuzzers **55** (`grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`) · stat surface **1201** (BEHAVIOR_CONTRACT doc count; NO mechanical command) · BackendKind **38** · DECISIONS tail **ADR-0294** COMPLETE (next-free **ADR-0295**) · go.mod modules **2** (the phase-61.2 lineage figure; the single `go.mod` requires **67**). A CONTESTED fixture count was reported by agent A (116/117/118 from three commands) and RESOLVED rather than silently picked: the canonical command gives **118**; 116 excludes the suffixed siblings `0007a`/`0007b`, and 117 is the PRE-phase-72 figure. **Sentinel re-run MECHANICALLY at this close (does NOT fire; `stop` NOT created):** (1) prints `NOT DONE: row 73` (the registration re-opens check (1)); (2) `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:'` ⇒ **3**; (3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`. Checks (2)+(3) print ⇒ abundant work remains. **Next → the phase-73 SPEC** (the D-CTMC-* live-probe arms; RESOLVE D-CTMC-SEAM on probed behavior; RE-DERIVE every §6 edit site at the SPEC tip; draft ADR-0295 §Context).

- **prior active-phase:** `phase 72 (tracing-custom-tags-metadata-host) IMPL done` (2026-07-23) — lifecycle-state 3 → DONE; **ROW 72 FLIPS `in-progress` → `done`** at this IMPL six-gate (ADR-0106, the SOLE leg — a SINGLE FLAT ROW, `reference_roadmap_split_phase_row_done`); ADR-0294 COMPLETED IN PLACE. Fresh worktree `.worktrees/phase-72-impl` off master `b1e467e8` (the phase-72 PLAN squash), branch `phase-72-tracing-custom-tags-metadata-host-impl`; subagent-driven (`feedback_execution_style`) — one implementer per task + a per-task adversarial reviewer, each committing LOCALLY only (`feedback_subagents_no_push`), controller squash-push at close (`feedback_push_to_origin`). Each task RE-DERIVED its anchors at the IMPL tip (a PLAN is not evidence either). **The 9-task TDD spine landed** (each red-first → green → break-verified → adversarially reviewed): **T1** (`74a1d4bb`+`bbe1ab50`) `internal/cluster` — an unexported `Endpoint.filterMetadata map[string]*structpb.Struct` field ALIASING the parsed `lbe.GetMetadata().GetFilterMetadata()` + an exported nil-safe `MetaLookup(ns)` accessor, populated at `manager.go:884` BESIDE the byte-unchanged phase-38 `envoy.lb` scalar projection (`:883`/`:754`/`subset.go` byte-identical AND unshifted; ALIASING proved mechanically; sizeof 104 → 112 B; ONE new import; breaks A/B/C/D fired, C asserting on the ACCESSOR'S OWN return per F5b) · **T2** (`2aa98e45`+`afdee55f`) `config.go` — `kindMetadataHost` APPENDED at **iota==5** (confirmed BY EXECUTION; the pre-known red was EXACTLY TWO `/host` subtests printing `Kind:5`) + a HOST accept arm that is a verified byte-clone of the ROUTE arm (one differing line) replacing the `:267-268` reject, ZERO new imports, the nine stale comments narrowed, `grep -rn "host kind unsupported"` ⇒ **0 hits**, `CLUSTER` + BOTH unset arms verified STILL rejecting; breaks E/F/G/H fired · **T3** (`f2c73ad3`+`fc55ea2f`) `resolve.go` — the nil-tolerant one-arg `hostMetaLookup` **5th** param + a `kindMetadataHost` arm descending the **FULL** `MetaPath` (NOT `[1:]`), `descend`/`structpbValueToString` reused VERBATIM (reuse proven by sha256), 32 `resolve_test.go` callers += trailing `nil` (raw grep 33; `:106` is a string literal); break I (the crux) fired FOUR HOST test funcs while EVERY REQUEST and ROUTE subtest stayed GREEN · **T4** (`ef77640c`) **the row's distinguishing task** — `picked.MetaLookup` appended at `accesslog_emit.go:57`/`:118`/`:179` and NOWHERE else. **The 18-caller threading tax phases 70/71 each paid is SKIPPED** (`picked cluster.Endpoint` was already the 4th emit param), so **the three emit SIGNATURES + all 18 emit callers + the 29 existing emit test callers + `chain.go` + `subset.go` are BYTE-UNTOUCHED, CONFIRMED BY SHA256**; live per-site H1/H2/H3 span tests + a first-class zero-`picked` test (the **5** span-capable zero sites, not the raw 11-of-18); breaks L/M fired per-site · **T5** (`fc16d396`+`0a94cf28`) a PURELY ADDITIVE `meta_host_ok` fuzz seed (**`meta_bad` STAYS on CLUSTER**) + the stale `fuzz_test.go:92-93` comment narrow with the adjacent dispatch-order sentence byte-unchanged; **dispatch-verified TWICE by two agents** (+0 fuzzers) · **T6** (`a1d99561`+`b35d6a22`) the OTLP fixture `0116-tracing-custom-tags-metadata-host` on the `0115` chassis with the metadata block RELOCATED onto `lb_endpoints[0].metadata` in BOTH YAMLs, STRING values only, NO writer — **it ANSWERED the row's genuinely new cross-side question: the REFERENCE resolves an ARBITRARY (non-`envoy.lb`) `filter_metadata` namespace for a HOST-kind tag**, all **12 reference spans** carrying the endpoint value rather than the default; breaks N/O/P/Q ALL fired, **P the anti-vacuity control** and O discriminating OMIT from empty-string; fixtures 117 → 118 · **T7** (`ea41e55c`+`efd726b6`) BEHAVIOR_CONTRACT B1-B4 · **T8** the six-gate · **T9** ADR-0294 IN PLACE + row-72-`done` + the deferred-sentence narrow + this roll. **Six-gate ALL GREEN:** gofmt SILENT / vet 0 / build 0 / `go mod tidy -diff` EMPTY + `go.mod`/`go.sum` NO DIFF vs master (+0 modules) / golangci-lint 0 / **the FULL 118-dir differential `ok 406.088s` EXIT 0, ZERO failures** / the layering check EMPTY — ⚠️ **a DISCIPLINE check, NOT a cycle check** (`internal/cluster` carries no `internal/tracing` edge either, so a direct import would build clean; the refuted "cycle guard" justification was CORRECTED in ADR-0294 §Context at T9) / `-race` green on `internal/cluster` (**NEWLY load-bearing — the FIRST row in this lineage to touch it**), `internal/tracing`, `internal/filter/hcm` / **a seven-section envelope audit with ZERO findings** (19 files all classified; the `internal/cluster` SHAPE pin holding; the cost claim confirmed by hash; **exactly TWO new exported symbols** by AST symbol-set diff). **The NAMED departure (B2), scoped EXACTLY:** the reference resolves a HOST tag at LB-PICK time and emits the picked endpoint's metadata even when the upstream connect then fails, while envoy-go carries the ZERO `Endpoint` on every upstream-**ACQUIRE** failure sub-path and falls to `default_value`/omit — EXECUTED-proven for the connect-failure class ONLY (pool-overflow + circuit-breaker UNPROBED on the reference). ⚠️ **Post-ACQUIRE failures (the write / read-response 502s) carry the REAL endpoint and resolve normally: PARITY, not departure** — an over-broad claim CAUGHT AND RETRACTED in review; the 404-no-match arm (no span at all) and the zero-endpoint arm (boot-rejected) have NO envoy-go counterpart and are NOT written as parity. PRE-EXISTING; scoped out as a future row. **Adversarial review found real defects at EVERY task**, all fixed — most consequentially that retraction, plus a doc comment conflating the nil-lookup mechanism with the ZERO-`Endpoint` mechanism (a method value on the zero `Endpoint` is NON-nil) and a duplicate test deleted as scope creep. **Exit counts (re-run MECHANICALLY):** fixtures **117 → 118** (tail `0116-tracing-custom-tags-metadata-host`) / fuzzers **55 (+0)** / stat surface **1201 (+0)** / BackendKind **38 (+0)** / go.mod modules **2 (+0)** / ZERO new packages / DECISIONS tail **ADR-0294** COMPLETE (next-free **ADR-0295**). **The Observability family STAYS OPEN** — the `HOST` MetadataKind rolled OUT of the live `candidates:` sentence at this row-done edit (phase-57 precedent), leaving **`CLUSTER` as the LAST unconsumed `metadata` MetadataKind**; three of the four (`REQUEST`@70, `ROUTE`@71, `HOST`@72) are now consumed. **Sentinel re-run MECHANICALLY (does NOT fire; `stop` NOT created):** (1) prints **NOTHING** (row 72 `done` — every chartered row `done`); (2) `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:'` ⇒ **3**; (3) prints `NEVER OPENED: gRPC/Runtime/WASM`. Checks (2)+(3) print ⇒ abundant work remains. **Next → the phase-73 BRAINSTORM** (the roller SELF-PICKS per the 2026-07-12 standing directive).

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
