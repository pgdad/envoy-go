# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `phase 26 parent SPEC done; Network-filters family OPEN at 2026-05-30 (this commit)` — **phase 26 (`network-filter-chain-and-rbac`) PARENT SPEC DONE at 2026-05-30 (this commit)**. The §9 Network-filters family stays OPEN (first new §9 family since the HTTP-filters family closed at 25.3). The parent SPEC (`docs/envoy-go/phases/26-network-filter-chain-and-rbac/SPEC.md`, ~430 lines) executed the BRAINSTORM §10 D1–D8 empirical pins IN-SESSION against Envoy v1.37.2 + go-control-plane v1.32.4 (parallel-subagent fan-out per the phase-25 SPEC precedent), formalized the 3-way split surface-mapping (§3.1), and anchored the ADR §Context drafts. **SPEC-BLOCKING D4 RESOLVED:** the connection-metadata sink REUSES `internal/dynamicmetadata/` at connection scope (NOT a new package) — the scope-agnostic `Bucket` matches the upstream `(namespace→key→Value)` shape 1:1; `ReadFilterCallbacks.DynamicMetadata() *dynamicmetadata.Bucket` is shaped at 26.1 so 26.3 needs no callbacks revision (AMEND-A5). Other key empirical refinements: direct_response message is **`Config`** not `DirectResponse` (AMEND-A1); echo is an empty message w/ vacuous PGV (AMEND-A2); rbac stat roster = exactly 4 counters `allowed`/`denied`/`shadow_allowed`/`shadow_denied`, prefix `<stat_prefix>.rbac.<counter>`, `stat_prefix` PGV-required (AMEND-A3); HTTP-only matchers silently never-match upstream so envoy-go PARSE-REJECT is a DEPARTURE (AMEND-A4); HTTP rbac emits NO metadata today so consumer-#1 re-verification is engine-correctness only (AMEND-A6); rbac decision in `OnData` + enforced-deny = connection close, not metadata (AMEND-A7/A8); `delay_deny` PARSE-REJECT (AMEND-A9); `rules`+`matcher` dual-path support (AMEND-A10); engine extraction is mechanical — phase-16 `evalContext` already abstract w/ L4 stubs (AMEND-A11); master-tip fuzzer count = **34** not 35 (REFUTES BRAINSTORM hypothesis → 35th/36th → 36 at family-done). spec-document-reviewer APPROVED (1 load-bearing fuzzer-count fix + 2 advisories folded in). Phases 00–25 + phase-26 BRAINSTORM DONE and CANONICAL.
- **phase-directory:** `docs/envoy-go/phases/26-network-filter-chain-and-rbac/` (BRAINSTORM.md + **SPEC.md authored at this commit** — both DONE; PLAN.md / PROGRESS.md / REVIEW.md are per-sub-phase artifacts, not parent-level). Sub-phase directories `26.1-network-filter-chain-framework-and-echo/` + `26.2-network-filter-registry-migration/` + `26.3-network-filter-rbac/` are created at each sub-phase's SPEC entry per the per-sub-phase convention (git does not track empty dirs).
- **lifecycle-state:** `phase 26 parent SPEC done (lifecycle-state 1 → 2 for the parent row); awaiting per-sub-phase 26.1 SPEC authoring` (SKILL_ROUTING state 1 for sub-phase 26.1 — parent SPEC.md exists; the 26.1 sub-phase directory + 26.1/SPEC.md do NOT yet exist). Per ADR-0106 + the phase-22/25 precedent, the per-sub-phase 26.1 SPEC session is the next lifecycle-state-1 → state-2 transition for sub-row 26.1. ROADMAP row 26 STAYS `in-progress`; sub-row 26.1 STAYS `planned` until the 26.1 SPEC session opens its directory + lands SPEC.md. Parent row 26 flips `in-progress → done` at 26.3 phase-done via the ROLLUP precedent (18/19/22/24/25).
- **next-skill:** `superpowers:writing-plans` (scoped to **26.1 SPEC authoring** at `docs/envoy-go/phases/26.1-network-filter-chain-framework-and-echo/SPEC.md` per the phase-22.1/25.1 per-sub-phase SPEC precedent — the next session rewrites the next-prompt for 26.1 SPEC cold-start). The 26.1 SPEC inherits the parent SPEC's §5 proto roster + §6 PARSE-REJECT roster + §7 stat surface + §8 fixture taxonomy + §11 empirical-pin block + §12 D-questions + §13 RATIFIED-PENDING items; it refines per-Task-level surface only. A 26.1 BRAINSTORM is NOT needed (the parent BRAINSTORM + parent SPEC settled the design — Q1-Q6 locked, AMEND-A1..A11 anchored, ADR-0213+0214 §Context drafted). After 26.1 SPEC → 26.1 PLAN → 26.1 IMPL via `superpowers:subagent-driven-development` per `feedback_execution_style` memory.
- **last-commit:** `TBD-26-PARENT-SPEC-SQUASH` — `phase 26 parent SPEC: network-filter-chain-and-rbac [ADR-0213, ADR-0214 §Context drafts]`. The phase-26 parent-SPEC squash-merge commit lands `docs/envoy-go/phases/26-network-filter-chain-and-rbac/SPEC.md` + the ADR-0213 + ADR-0214 §Context drafts in DECISIONS.md + this STATE.md advance. SHA to be filled at the stage-close follow-up commit (`TBD-26-PARENT-SPEC-SQUASH` → actual) per the phase-22.x..25.x stage-close SHA-fill precedent. Predecessor: `77d6a01` (phase-26 BRAINSTORM squash; SHA-filled at `6319671`); tip-repoint trail `6f68d81` / `d0c63d2`.
- **last-updated:** 2026-05-30
- **next-free ADR:** `ADR-0215` (ADR-0213 + ADR-0214 §Context drafts CONSUMED at this parent-SPEC commit; DECISIONS.md tail advances to **ADR-0214**). The 26.1 ADRs (ADR-0213 chain framework + ADR-0214 registry + boot-wiring) have §Context drafts in DECISIONS.md now; §Decision/§Consequences bodies land at 26.1 IMPL per ADR-0044. Provisional remaining family span (drafted at their own sub-phase SPECs): 26.2 ADR-0215 (tcp_proxy/HCM migration + hardcoded-registry retirement); 26.3 ADR-0216 (`internal/rbac/` extraction) + ADR-0217 (connection-metadata sink as `internal/dynamicmetadata/` connection-scoped reuse) + ADR-0218 (`rbac_network` filter). Anticipated next-free after phase-26 phase-done: `ADR-0219`. ADR-0209 escape-valve reserve carried from the §9 family STANDS-UNCONSUMED.

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
