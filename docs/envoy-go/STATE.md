# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `05.1-downstream-h2`
- **phase-directory:** `docs/envoy-go/phases/05.1-downstream-h2/` — contains `README.md` (placeholder, written at split time per ADR-0045 / commit `2162509`), `SPEC.md` (drafted by brainstorming per ADR-0004 autonomous mode; commit `4b45941`), `PLAN.md` (drafted by writing-plans per ADR-0005 autonomous mode; commit `634bcb6`; 3331 lines, 16 tasks, 8 new ADRs), and `PROGRESS.md` (executor-appended through Tasks 1–16; final SHA-fills at `b416664`). The sibling `docs/envoy-go/phases/05.2-upstream-h2/` is unchanged from the split commit (placeholder `README.md` only). The parent `docs/envoy-go/phases/05-http-2/` retains its `SPEC.md` (`612cdea`) as the master design document for both sub-phases per ADR-0045 and phase-05 SPEC §11.1; that directory remains closed read-only history at the parent level (no PLAN/PROGRESS/REVIEW will land under it; those land under the sub-phase directories).
- **lifecycle-state:** `4` — phase 05.1's 16 PLAN tasks are landed on `phase/05.1-downstream-h2-impl` (37 commits between `5bd2cf4` and `b416664`); ADRs 0046..0053 are appended to `DECISIONS.md` (verified: `grep '^## ADR-' docs/envoy-go/DECISIONS.md | tail -1` returns `## ADR-0053:`). `internal/filter/hcm/h2/` carries the nine production source files enumerated in SPEC §4.1 (`doc.go`, `errors.go`, `preface.go`, `framer.go`, `hpack.go`, `flow.go`, `settings.go`, `stream.go`, `conn.go` — verified `! ls internal/filter/hcm/h2/client.go` per ADR-0048). `internal/filter/hcm/h2dispatch.go` is the one-way `hcm → h2` adapter (Settled #10). `internal/filter/hcm/actions.go` carries the codec-neutral `directResponseAction` factoring (`body()` + `writeH1` + `writeH2`; field rename `body` → `bodyText` per Settled #9). `internal/filter/hcm/filter.go` carries the `Filter.Handle` ALPN dispatch (defensive `tlsConn.HandshakeContext(ctx)` per SPEC §11.6). `cmd/envoy-go/main.go` carries the `--allow-h2c` flag (ADR-0049). `internal/listener/manager.go` carries `NewManagerWithBaseDirAndAllowH2C` + `listenerCtx`. `cmd/envoy-go/main_test.go` carries `TestEnvoyGoBinary_H2Smoke`. `test/conformance/h2spec/` carries the project's first non-vacuous conformance gate (53/53 h2spec tests pass at the ADR-0051 threshold). `docs/envoy-go/CONFORMANCE_PINS.md` carries the `summerwind/h2spec` SHA256 pin. `docs/envoy-go/BEHAVIOR_CONTRACT.md` carries the new `## HTTP/2` SCAFFOLD subsection + 5 new Header allow-list rows per ADR-0052. Per `BOOTSTRAP_PROMPT.md` §5 state 4, the next session invokes `superpowers:verification-before-completion` to re-run all gates from a fresh session and quote the outputs into PROGRESS. Task 16's local-sweep PROGRESS entry (commits `fb921ba` + `6c1099f` + `b416664`) is the executor's verification preview: gate (a) VACUOUS per ADR-0045 (pre-existing fixtures 0000/0001/0002/0003 all green); gate (b) `go test -race ./...` green (two data races discovered + fixed during the sweep — `e806f17` for the ServerConn.emitGoaway/framer write-ordering race + the framer_test concurrent-write helper); gate (c) NEWLY NON-VACUOUS — h2spec 53/53 PASS over sections 3/4/5/6-ex-6.6/7/8; gate (d) all 6 fuzzers (FuzzBootstrapLoad / FuzzTcpProxyFilter / FuzzTLSContextParse / FuzzHCMConfigParse / FuzzFrameStream / FuzzHPACKDecode) clean for 30s with no testdata/fuzz/ pollution (ADR-0018); gate (e) `go vet` clean + ADR-0046 boundary grep clean (3 production hits in `h2/framer.go`/`h2/settings.go`/`h2/conn.go`, all in the 5 allowed files; `h2/hpack.go` uses `golang.org/x/net/http2/hpack` sub-package not the main `http2` package; `h2/stream.go` has no direct import) + ADR-0048 `client.go` absence verified. **Gate (e) lint partial:** `golangci-lint run` reports 118 pre-existing issues confirmed to predate Task 16 (verified at `8d81be9` and earlier) — errcheck on `defer conn.Close()` in test files, misspellings throughout `h2/`, gofmt alignment, unused fields. Task 16 net-reduced the count by 3. The verification-before-completion session re-runs `golangci-lint`; if the count is still non-zero, that session bounces to state-3 (REVIEW re-entry) for cleanup or substantive triage rather than papering over with allow-lists. Gate (f) is state-5 work (`superpowers:requesting-code-review`).
- **next-skill:** `superpowers:verification-before-completion`
- **next-skill-scope:** Re-run all six phase-done gates per SPEC §3 / SPEC §7.5 from a FRESH session (cold-start path), quoting every command output verbatim into a NEW PROGRESS section appended to `phases/05.1-downstream-h2/PROGRESS.md`. The verification session does NOT re-execute Task 16's local sweep results; it generates an independent verification record. Per ADR-0003 + the per-phase-worktree convention, verification runs from a fresh `phase/05.1-downstream-h2-verify` worktree branched from master tip after this STATE.md commit's fast-forward; the current `.worktrees/phase-05.1-downstream-h2-impl` is closed-history at this state transition. Six gates: (a) `go test ./test/differential/... -timeout=12m` — VACUOUS per ADR-0045 (no new differential fixture in 05.1) but pre-existing fixtures 0000/0001/0002/0003 must remain green; (b) `go test -race ./...` — every package passes, no data races; (c) `go test ./test/conformance/h2spec/... -timeout=5m -v` — h2spec sections 3/4/5/6-ex-6.6/7/8 report `failed=0` (53/53 expected at the ADR-0051 pin); (d) `go test -run=^FuzzXxx$ -fuzz=^FuzzXxx$ -fuzztime=30s` for each of the six fuzzers — clean for the 30-second CI budget per ADR-0018, with `git status --porcelain` empty after each (no `testdata/fuzz/` pollution); (e) `go vet ./...` + `golangci-lint run ./...` + ADR-0046 boundary grep + ADR-0048 `client.go` absence — all clean. **Open lint debt:** the 118 pre-existing issues at impl-branch tip will need triage (fix in-line OR document as deferred-to-state-5-REVIEW with explicit ADR if scope creep). If lint cleanup balloons past ~30 minutes, the verification session sets `lifecycle-state: 3` with a `block-reason` and a fix-up plan, per BOOTSTRAP_PROMPT.md §5 step 5 re-entry pattern (REVIEW issues → step 3, not step 4). Gate (f) — `REVIEW.md` — is state-5 work (`superpowers:requesting-code-review`), not run by the verification session. Reading order for the verification session (cold-start Step C): `phases/05.1-downstream-h2/SPEC.md`, `phases/05.1-downstream-h2/PLAN.md`, `phases/05.1-downstream-h2/PROGRESS.md` (Tasks 1–16 + the local-sweep block), `phases/04-http-1.1/PROGRESS.md` (the verification-block shape phase-04 used post-state-4).
- **last-commit:** `b416664` — the final Task-16 SHA-fill on branch `phase/05.1-downstream-h2-impl` (last of 37 commits between `5bd2cf4` and tip). This STATE.md commit (the lifecycle-transition commit that promotes lifecycle-state from 3 to 4 and points `last-commit` at `b416664`) is the 38th and final commit on the `phase/05.1-downstream-h2-impl` branch. Master is fast-forwarded to this STATE.md commit per ADR-0003.
- **last-updated:** 2026-04-25

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
