# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `06.1-stats-prometheus`
- **phase-directory:** `docs/envoy-go/phases/06.1-stats-prometheus/` — contains `SPEC.md` (at `ad46930`, frozen), `PLAN.md` (at `343b198`), `PROGRESS.md` (full Task-1-through-Task-15 history + L4 FAIL block at `1f94b74` + gate-(d) fix block at `79be6b0` + L4 PASSED re-verification block at `1ed6cd0`; Path-A state-3 fix block + verification-block addendum to be appended on this followup branch), and `REVIEW.md` (`59d86f2` — verdict APPROVED WITH FOLLOW-UPS, 0 Critical / 2 Important / 12 Minor; SHA-fill at `d21d50b`). Parent `docs/envoy-go/phases/06-observability-baseline/` carries `BRAINSTORM.md` (`d271fd7`) + parent `SPEC.md` (`ad46930`). Sibling `docs/envoy-go/phases/06.2-access-log/` retains the placeholder `README.md` (`ad46930`). Phase 05 closed history unchanged.
- **lifecycle-state:** `3` — Path-A REVIEW-followup re-entry per BOOTSTRAP §5.2 review-feedback semantics. THIS commit on `phase/06.1-stats-prometheus-review-followup` (worktree `.worktrees/phase-06.1-stats-prometheus-review-followup`, branched from REVIEW SHA-fill tip `d21d50b` per ADR-0003) explicitly regresses STATE from lifecycle-state 5 (REVIEW APPROVED WITH FOLLOW-UPS at `59d86f2` / `d21d50b`) back to state 3, mirroring 05.2 `c6a0541`. The followup batch is scoped to **M-1 only** per REVIEW.md `## Recommendation` Path A: add `stats.IsValidName` guard at `cluster.NewManager`'s cluster-name boundary (~10-15 LoC) + 5-6-case regression test mirroring `TestParseFilter_StatPrefixInvalidChars` (`internal/filter/hcm/config_test.go:221`). REVIEW Important findings I-1 (SPEC §14 line 715 file-path mismatch — M-9 fix landed at `h2dispatch.go` not `router_action.go`; closed by REVIEW + PROGRESS Task 11 deviation #1 as-corrigendum, no commit needed) and I-2 (ROADMAP row 06.1 still `planned` after SPEC §4.4 claimed flip; closed at the lifecycle-state-6 phase-done commit's natural ROADMAP edit by `planned → done` direct flip per the 05.2 `0c01ed6` one-step precedent) require no code/doc changes on this followup branch. The 12 collapsed Minors carry forward to the L4 review-followup batch (separate post-phase-done branch); no Minor closure on this branch.
- **next-skill:** `superpowers:test-driven-development` — single-task TDD cycle for M-1: (1) RED — add `TestNewManager_ClusterNameInvalidChars` table-driven test at `internal/cluster/manager_test.go` covering 5-6 invalid-char cases (mirror `TestParseFilter_StatPrefixInvalidChars`'s pattern: literal space, dash, colon, slash, dollar, plus the empirically-anchored `"0000000000 0"` SP-at-index-10 shape) — assert `NewManager` returns an error matching `"invalid cluster name"`; verify test fails. (2) GREEN — at `internal/cluster/manager.go` `buildCluster` (or at `registerClusterMetrics`'s call site in `NewManagerWithBaseDir`) add a guard `if !stats.IsValidName("cluster." + name + ".upstream_rq_total") { return nil, fmt.Errorf("cluster: %q: invalid cluster name (must contain only ASCII letters, digits, underscore, or dot, and form a valid metric-name segment)", name) }` mirroring HCM `parseFilterWithCtx`'s ADR-0065 guard at `internal/filter/hcm/config.go:143`. Verify test passes. (3) REFACTOR — none expected; pattern inherits ADR-0065's shape by reference (no new ADR per ADR-0065 Consequences (d)). After M-1 lands, advance lifecycle: re-run gates (b)/(c)/(d)/(e) (gate (a) remains GREEN at fixture 0005 — not re-run separately; gate (f) closed by PROGRESS verification-block addendum), append PROGRESS verification-block addendum mirroring 05.2 `f774d4f` (verbatim outputs of every gate command from this followup-branch tip), update STATE → lifecycle-state 4. Then a final commit promotes STATE → lifecycle-state 5 mirroring 05.2 `5c0f3cc`. The lifecycle-state-6 phase-done commit lands in the next session.
- **next-skill-scope:** M-1 fix commit deliverable: `internal/cluster/manager.go` boundary guard (+~5 LoC; placement choice — at `buildCluster`'s name-extraction block immediately after the empty-name check at `manager.go:117-119`, OR at the head of `registerClusterMetrics` at `manager.go:96` as a defensive secondary check; planner picks at edit time per the symmetry-with-HCM-`parseFilterWithCtx` argument — recommendation: at `buildCluster` so the error surfaces at the same parse-time boundary as cluster-type, lb_policy, and load_assignment validations) plus `internal/cluster/manager_test.go` regression test (~25 LoC for a 5-6-case `t.Run`-loop mirroring `TestParseFilter_StatPrefixInvalidChars`'s `cases := []string{...}` shape). Listener already safe via `normalizeAddr` (per ADR-0065's analysis at `manager.go:196-198`) — NOT a target of this fix. Subsequent commits: (i) gates re-run + PROGRESS verification-block addendum + STATE → 4 (single commit, mirrors 05.2 `f774d4f`); (ii) STATE → 5 (single commit, mirrors 05.2 `5c0f3cc`). Per ADR-0003 + BOOTSTRAP §5 step 6, the phase-done commit lands in the next session at lifecycle-state 6 with ROADMAP row 06.1 `planned → done` (one-step per the 05.2 precedent + REVIEW I-2 disposition), and STATE then advances to lifecycle-state 0 between-phases with `active-phase: 06.2-access-log`, `next-skill: superpowers:brainstorming`.
- **last-commit:** `TBD` — `phase 06.1 review-followup: STATE.md → review-followup re-entry (state 5 → 3 per BOOTSTRAP §5.2)`, on branch `phase/06.1-stats-prometheus-review-followup`. STATE.md-only edit (this commit); no code, test, or doc changes. The followup batch closes M-1 (Path A per REVIEW.md `## Recommendation`); I-1 closed by REVIEW-as-corrigendum (no commit); I-2 closed at the next session's phase-done commit's ROADMAP edit. ROADMAP unchanged at this commit. SHA-fill follow-up per the phase-02..05.2 convention.
- **last-updated:** 2026-04-28

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
