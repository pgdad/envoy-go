# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

> **This file is a POINTER, not an archive** — `BOOTSTRAP_PROMPT.md` §4: *"STATE.md — pointer: active phase, last commit, next action."* It carries the live pointer, the live counts, and a SHORT recent lineage. **Full stage-by-stage history lives in `STATE_HISTORY.md`** (archived at ADR-0288; **nothing was deleted** — the pre-move file is preserved there verbatim, byte-for-byte).
>
> **⚠️ THE RULE ADR-0288 EXISTS TO ENFORCE: there is EXACTLY ONE live `next-skill:`, `lifecycle-state:`, `next-free ADR:`, and count block IN THIS FILE, and they live in §Current pointer directly below.** Before that move, ~330 lines of prepended backlog had buried TWO fossilised "current state" blocks whose `next-skill:` still read *"the 42.2 hedging BRAINSTORM"* and *"the 46.2 SPEC"* while the live phase was **66** — so `grep 'next-skill:' STATE.md` returned **two stale hits and ZERO live ones**, and `grep 'fuzzers:'` returned **48** and **42** when the true count was **55**. **A session may trust a grep of this file again. Keep it that way: when you advance the pointer, EDIT §Current pointer IN PLACE — never prepend a new block above it.**

---

## Current pointer

*(The ONLY live "what next" in this repo. Update IN PLACE at every stage close.)*

- **active-phase:** `phase 79 (stats-prometheus-projection) SPEC done` (2026-07-28) — lifecycle-state **1 -> 2**; **ROW 79 STAYS `in-progress`** and **`ROADMAP.md` is BYTE-UNTOUCHED at this stage** (the sentinel's `want` STAYS **111**). `BEHAVIOR_CONTRACT.md` BYTE-UNTOUCHED. Fresh worktree off master `316ba895`, branch `phase-79-spec`. Docs-only: **ZERO production `.go`, ZERO test `.go` committed**. **FOUR investigation agents on disjoint remits**, each in its own detached worktree with PRIVATE scratch and a private port band, plus controller re-derivation. ⚠️ **D-SPP-1 — the BRAINSTORM's "largest unmeasured risk" — IS CLOSED IN THE ROW'S FAVOUR BY EXECUTION AGAINST A LIVE PINNED REFERENCE** (six arms, fresh container per arm, bridge network, torn down BY NAME, teardown proof EMPTY): `runtime.`, `access_logs.` and `tracing.` **ALL THREE BYTE-MIRROR**, zero labels, middle segment = the SINK TYPE (proven by grep COUNT — the distinctive operator-chosen names appear ZERO times in dumps of 828-1159 lines). ⇒ **the row ships THREE arms, not two; nothing moves to 79.1 on this account.** ⚠️ **FIVE FURTHER BRAINSTORM CLAIMS REFUTED, EACH BY EXECUTION:** (i) **the D-SPP-3 counter form DEADLOCKS** — `Registry.Walk` holds `RLock` for the whole iteration while `getOrRegister` takes `Lock`, and Go's `RWMutex` is not reentrant; proven with a goroutine trace, so D-SPP-3 resolves to the **LOG** and `+0` becomes UNCONDITIONAL; (ii) **the `:350` error-string repair is ITSELF WRONG** — there are **NINE** recognized top-level segments, not five (`mongo. kafka. redis. thrift.` are `default`-branch `CutPrefix` detectors), so the naive `+wasm.` edit ships a FIFTH generation of a wrong list; (iii) **`116 B` is really `114 B`** (the probe registered `server.live` as a counter; production registers a **gauge**); (iv) **the `~:100-110` switch anchor is off by ~50 lines** (`switch {` is at **`:50`**; `:100-110` is inside the `wasm.` comment block); (v) **the near-miss control `http.<sp>.tracing.spans_sent` is a FABRICATED name**, registered nowhere. ⚠️ **AND TWO CLAIMS THE BRAINSTORM NEVER MADE:** `BEHAVIOR_CONTRACT.md:5020` is a **MANDATORY** edit on the `+0` path and was absent from the edit-site table entirely; and **the differential suite is structurally BLIND to two of the three arms** (10 fixtures configure `stats_sinks`, **0 of 10** configure a tracer or a gRPC/OTel access logger), so the sink audit must be a **UNIT-level** byte gate, not a fixture gate. **`0118`'s liveness PROVEN by the full red-then-green cycle** before any edit; **D-SPP-5 = CONVERT, not delete** (no generic prometheus comparison exists in the runner). ⚠️ **BAND RE-DERIVED AND THE BRAINSTORM'S 9-11 IS NOT ADOPTED: 10-12, budget 12** — derived by removing the now-discharged D-SPP-1 probe task, and its two ceiling contingencies, from an independent agent's 11-13 roster. **Stat surface +0 (1207 -> 1207), UNCONDITIONAL.** **`PROGRESS.md` NOT created — it is born at the PLAN.**
- **phase-directory:** `docs/envoy-go/phases/79-stats-prometheus-projection/` (`BRAINSTORM.md` + `SPEC.md` — ⚠️ **no `PROGRESS.md`; it is born at the PLAN**). Prior: `docs/envoy-go/phases/78-boot-panic-visibility/`
- **lifecycle-state:** **`2`** (phase 79 SPEC done; **row 79 still `in-progress`**). ⚠️ **Sentinel check (1) is LIVE** — re-run mechanically at this tip it prints `NOT DONE: row 79` with **no `GATE FAIL` at `want=111`**; negative controls `want=109`, `want=110` and `want=112` each fired `GATE FAIL: examined 111 data rows, expected <want>`. **`stop` was NOT created and MUST NOT be**: (2) still prints FIVE candidate sentences at `:189 :199 :209 :215 :223` — **UNCHANGED; this row narrows NOTHING, stated rather than forecast** — and (3) still prints `NEVER OPENED: gRPC` and `NEVER OPENED: WASM` (NC: an invented slug fired). Input measured at **227 lines / 1 003 291 bytes / 13** bare `candidates:` hits. Next -> **a phase-79 PLAN**.
- **next-skill:** **a phase-79 PLAN on `stats-prometheus-projection`** — it derives tasks from `SPEC.md` §9's **10-12 (budget 12)** roster and **creates `PROGRESS.md`**. ⚠️ **RE-DERIVE THE BAND RATHER THAN INHERIT IT** (`reference_deferred_candidate_cost_restale`): phase 78's PLAN refuted its own SPEC's band in BOTH directions, and this SPEC already refused the BRAINSTORM's. ⚠️ **The whole D-SPP docket is DISPOSED** — D-SPP-1 three byte-mirror arms (measured against a live reference) · D-SPP-2 recorded for 79.1 · D-SPP-3 the **LOG**, aggregated one line per scrape, `sync.Once` REJECTED on executed grounds · D-SPP-4 **yes**, with the **NINE**-segment enumeration plus a byte-stable guard · D-SPP-5 **CONVERT** the `0118` pin. ⚠️ **The PLAN must carry the two SPEC-time design artifacts that were BUILT rather than sketched**: the observability stacked control (four assertions, three negative controls, two of which trip DIFFERENT legs) and the **TWO** `helpText` reverse guards (a four-arm matrix proves one is insufficient — a typo copy-pasted into the golden list defeats the key-set guard and only the projection-driven guard catches it). ⚠️ **`scrapeProm` is LABEL-BLIND** and the parity flip must not use a raw-line comparison. ⚠️ **The full 120-fixture differential is MANDATORY** (`internal/stats` links into `cmd/envoy-go`, built at THREE sites; h2spec is a FOURTH consumer not covered by `./test/differential/`).
- **last-commit:** the phase-79 SPEC squash (`git log --grep 'phase 79'`; **locate by SUBJECT, never by position** — `reference_next_prompt_tracked_despite_gitignore`). Prior: the phase-79 BRAINSTORM squash `316ba895`; the phase-78 IMPL squash `3a4c8cfa`.
- **last-updated:** 2026-07-28 (phase-79 SPEC done; **ROW 79 STILL OPEN**). **Gates ACTUALLY run at this stage — docs-only, so NO Go gates were run by the controller and ZERO `.go` files were committed**, though the four agents executed Go builds, package tests, `-race`, a live dockerized reference probe and a `0118` differential red-then-green **inside their own worktrees**, each finishing `git status --porcelain` EMPTY. All THREE sentinel checks re-run with FIRING negative controls; counts re-derived with firing negative controls (fixtures **120** / probe dir ⇒ 121 · fuzzers **55** / appended `func Fuzz` ⇒ 56 · blank imports **120** / naive `^\t_ ` ⇒ 126 · internal packages **73** · ROADMAP **227 lines / 111 data rows** · next-free reference port **10119** / NC `10118` ⇒ 5 hits). `DECISIONS.md` **17596 -> 17628** (ADR-0301 §Context); `BEHAVIOR_CONTRACT.md` **5762**, BYTE-UNTOUCHED; `ROADMAP.md` **227**, BYTE-UNTOUCHED. ⚠️ **§Project counts below REMAINS STALE ON FOUR AXES and is again deliberately NOT repaired** — fixtures 119 (real **120**), stat surface 1205 (lineage **1207**), DECISIONS tail ADR-0298 (real **ADR-0301**), and `:33`'s next-free-reference-port claim (refuted by `0118/driver/driver.go:26-35`; next-free is **10119**). Repairing a count by editing the sentence that states it is how the ADR-0296/0297 species starts.
- **next-free ADR:** **`ADR-0302`** — `DECISIONS.md` tail is now **ADR-0301**, STATUS **PROPOSED**: §Context drafted at this SPEC in the **NEW** block form, verified mechanically at this close — heading em-dash is **U+2014** (`e2 80 94`, byte-identical to ADR-0300's), **1** `> **STATUS:**` blockquote, **1** `### Context`, **0** `### Decision`/`### Consequences`, **0** `---` separators, **13** §Context paragraphs, blank line before the heading. `grep -c '^## ADR-0302'` ⇒ **0**. §Decision + §Consequences append **IN PLACE** at the phase-79 IMPL (**ADR-0044-as-used**; ⚠️ ADR-0044 does not itself contain that discipline). ⚠️ **THE DOCUMENTED ADR RANGE-EXTRACTION HAZARD IS REFUTED AT THIS TIP** — `^## ADR-0107` matches **1** line (`:4304`), `:4858` begins `##` at byte 0, and **zero** lines in the file begin space-then-`#`. `^## ADR-` is a safe anchor. *(The one number with no heading is **ADR-0209**, a deliberately unconsumed reserve; `## ADR-0127 v2:` at `:5973` is the sole heading matching neither form.)*

---

## Project counts at this commit

*(Live. Re-verify at session start — `git fetch` first — and update here at every stage close. Canonical commands inline.)*

⚠️ **ALL counts below were re-run MECHANICALLY in the stage worktree at this phase-76 IMPL close with the canonical commands, EACH WITH ITS NEGATIVE CONTROL OBSERVED — never copied forward.** **Phase 76 landed +0 on EVERY axis, including +0 PRODUCTION `.go` FILES** — the first chartered row in this lineage whose production-code delta is exactly nothing, and the ANTICIPATED envelope held on all of them. The row's `.go` delta is **three TEST-side files** (`internal/cluster/ringhash_test.go` edited, `test/fixtures/0061-lb-ring-hash/driver/driver_test.go` edited, `.../linkage_test.go` NEW), plus three fixture-doc files and four project docs. ⚠️ **The stat-surface total 1205 is DOC-SOURCED and was NOT re-derived mechanically** — there is no mechanical command, and the chain is known to be discontinuous in **TWO** places (the recorded `1200 -> 1201`, and `Phase 46.1b` closing at 1198 while `Phase 47.1` opens at 1200 — an unattributed +2). **Only the +0 DELTA is asserted, and that argument is STRUCTURAL rather than a `statssink` result:** zero production `.go` bytes changed (G8) and zero byte mismatches over the 375-file production roster (G14), so no `NewCounter`/`NewGauge` call site can have moved; the five `TestNoNewStat*` guards corroborate at 5x PASS. ⚠️ **A future phase should COUNT the surface rather than trust the chain — a +0 row is a cheap place to do it, and phase 76 declined to charter that, so it is now a NAMED maintenance deferral rather than an idle observation.**

- **active differential fixtures:** **119** — ⚠️ **Phase 76 landed +0: `0061-lb-ring-hash` was EDITED, not added** (`sourceIPs` 4 -> 16 plus a NEW `driver/linkage_test.go` inside the EXISTING fixture directory — no new YAML, no new directory, no new BackendKind, no new port), so the count STAYS **119** and `0118` remains FREE. NC observed: `mkdir .../0118-fake` ⇒ 120. — `ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0117-tracing-custom-tags-metadata-cluster` (phase 73). **Phase 75 landed +0**: `0110-tls-require-client-cert-false` was **EXTENDED** with a cross-side `fixture.StatsAsserter` (NO new YAML, NO new directory, NO new BackendKind, NO new port), so the count STAYS **119** and `0118` remains FREE. ⚠️ **Next-free REFERENCE port is `10450`, NOT `10118`** — ports are NOT fixture-index aligned. **The fixture now asserts what its own README confessed was infeasible, and BOTH sides were EXECUTED**: `ssl.handshake=2 ssl.no_certificate=1 ssl.fail_verify_error=1 ssl.fail_verify_no_cert=0`, `downstream_cx_total=3`, **identical on reference and subject**, with the discriminating asymmetry (`handshake=2` vs `no_certificate=1`) observed per side. Shape A over `/stats/prometheus`, keyed on the metric NAME with `envoy_listener_address` IGNORED — the NAMES are byte-identical cross-side while the labels differ (`0.0.0.0_10446` reference vs **`___20016`** subject: ⚠️ **the subject binds the IPv6 WILDCARD**). ⚠️ **`0111` was NOT given a value assertion, deliberately** — it runs `require_client_certificate: true` on both sides, so a success-path no-cert annotation reads **0 on every arm, structurally**, and a value pin would document a **vacuous `0 == 0`** as coverage; its PROSE gained the name in its NAMED-UNASSERTED list instead, because **both `0111` passages are CLOSED ENUMERATIONS** and a name in neither list reads as ASSERTED. **`0111`'s driver is byte-untouched, and its named-subset check was EXECUTED for the first time at this IMPL: PASS.** ⚠️ `var _ fixture.StatsAsserter` is a **TRIPWIRE, not the dispatch mechanism** — `0110` is only the **THIRD** fixture repo-wide to carry it, while **82** define an `AssertStats` method.
- **fuzzers:** **55** — **Phase 76 landed +0** (it consumes no new config field, so there is no parse arm to fuzz). NC observed: appending `func FuzzProbeOnly` ⇒ 56. — `grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`. **Phase 75 landed +0** — the row consumes no new config field at all, so there is no parse arm and no fuzz-seed classification to flip (`reference_fuzzer_count_docs_drift`: an `f.Add` seed is not a fuzzer).
- **stat surface:** **1205** — BEHAVIOR_CONTRACT doc count. **There is NO mechanical counting command** (the contract says so itself); enforcement is per-phase `TestNoNewStat*` guards asserting a delta of ZERO, never the absolute total. **Phase 75 LANDED +1 (1204 -> 1205)** — `listener.<normalized-addr>.ssl.no_certificate`, the FOURTH listener-scope `ssl.*` name and the FIRST **SUCCESS-PATH** handshake annotation (⚠️ **NOT a synonym for `ssl.fail_verify_no_cert`, and the mis-mapping is wrong in BOTH directions** — one annotates an ACCEPTED handshake, the other books a REJECTED one; they are mutually exclusive on every connection). ⚠️ **The delta is a NAME-surface delta, NOT a flat per-deployment one:** registration rides the block that ALREADY gates on `rt.tlsMode`, so a **plaintext-only** deployment gains **ZERO** names, while a **QUIC** listener gains it permanently zero — **PARITY** with the reference, and structural (`Manager.Start` launches no accept loop for `kindQUIC`, so the Inc site is unreachable for a **name-independent** reason). ⚠️ **QUIC was NOT re-probed at phase 75** — the parity claim is INHERITED from phase 74's family-level probe, and that inheritance is stated in BEHAVIOR_CONTRACT rather than presented as a measurement. Ledger updated: `1204` -> `1205` at `BEHAVIOR_CONTRACT.md:831`/`:847`, plus a `**Phase 75 - 1204 -> 1205 (+1)**` line after the tail entry — **the ONLY line-adding edit in the file** (`5744 -> 5746`), so every by-line citation into the TLS subsection stays valid. ⚠️ **The new entry RECORDS BOTH ledger gaps and BACK-FILLS NEITHER** (fabricating a line would be inventing a record). Gate figures at this close: `grep -c '1204'` => **2** · `grep -c '1205'` => **3** · `grep -cE '^#{2,4} .*ADR-[0-9]+.*ADR-[0-9]+'` => **17** (was 16 — the `:1849` heading joined them, on the `:785` precedent). ⚠️ **`internal/statssink`'s four *"stays 1200 / 1196"* prose sites, stale since phase 49, are RECORDED-NOT-FIXED** — that package is on this phase's byte-untouched sha256 roster.
- **BackendKind tail:** **38** — **Phase 76 landed +0** (`0061` uses the in-process `TCPEcho` branch and implements neither `BackendKindAware` nor `PerHostBackendKind`). NC observed: appending `ZZProbeKind = 39` ⇒ tail 39. — (`H2GoawayResponder`, `test/differential/fixture/fixture.go:614`). **Phase 75 landed +0** — `0110` was extended, not cloned, and it implements neither `BackendKindAware` nor `PerHostBackendKind`, so the runner takes the in-process `TCPEcho` branch. ⚠️ **This is a TAIL VALUE, not a count** — the file declares **39** constants (values 0-38, `TCPEcho = 0`). The lineage figure **38** is correct as written; do NOT 'fix' it to 39.
- **DECISIONS.md tail:** **ADR-0298** (next-free **ADR-0299**) — `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1` -> `## ADR-0298`; `grep -c '^## ADR-0299'` ⇒ **0**. **ADR-0298 is COMPLETE as of the phase-76 IMPL** — §Context drafted at the phase-76 SPEC, §Decision + §Consequences APPENDED IN PLACE after the RETAINED italic footer, **no renumber and NO `---` separator** (the file's last `^---$` is still at `:17020`). Block shape controller-re-derived at this close: **1** `### Context` / **1** `### Decision` / **1** `### Consequences` / **1** retained footer. ⚠️ **FOUR of its OWN §Context paragraphs were CORRECTED IN PLACE** (¶1's `:295` -> `:307`, ¶5's `-count ~= 81` -> **83**, ¶7's *"~71 collapses"* -> **74/2000**, ¶11's *"needs no edit"* -> REFUTED) on the ADR-0297 ¶7/¶9 precedent, each **leading with what survives**; none changes a decision the ADR records. ⚠️ **It carries NO whole-file grep count and claims NO family ordinal** — the Load-balancing family closed at row 54, and **maintenance rows do not extend a charter**. *(Historical, retained: **ADR-0297 is COMPLETE** — §Context drafted at the phase-75 SPEC (ADR-0044-**as-used**; ⚠️ ADR-0044 does NOT actually contain that discipline), §Decision + §Consequences appended IN PLACE at the phase-75 IMPL after the **RETAINED** footer, mirroring ADR-0295/0296. Block shape verified at this close: **1** `### Context` / **1** `### Decision` / **1** `### Consequences` / **1** retained footer; `grep -c '^## ADR-0298'` => **0**. **No renumber, no new ADR.** ⚠️ **Its OWN §Context ¶7 and ¶9 were CORRECTED IN PLACE while completing it** — ¶7's **self-falsifying whole-file grep** (the same species the phase-74 IMPL had just fixed in ADR-0296 ¶3, reproduced one ADR later) is restated with **NO number** and the searched token deliberately **NOT spelled**, because every restatement mints another counter-example; and ¶9's *"SELF vs OTHER-ADR"* form rule is replaced by **graft SCALE** after the n=7 population was re-enumerated first-hand. ⚠️ **¶9 ALSO said *"the only two instances in the file"* — which this row's own blockquote makes three** — so a pure append would have shipped a SECOND self-falsifying count in the commit that fixed the first. **The prescription has now outlived THREE wrong justifications: family, self-vs-other, scale.** ⚠️ **`ADR-0296` is NO LONGER byte-untouched** — its §Decision (g) carries the phase-75 correction as an INDENTED blockquote (`grep -c '^  > '` **2 -> 3**), **LEADING WITH WHAT SURVIVES** (no such counter was owed *in phase 74*, whose sole stat-asserting fixture is `require=true` on both sides — **the deferral was RIGHT**) and claiming **NO ORDINAL**.)*
- **go.mod modules:** **Phase 76 landed +0** — `go mod tidy -diff` EMPTY and `git diff master -- go.mod go.sum` EMPTY; ⚠️ **the row's TEST-side import delta is +8 and that is CORRECT** (`ringhash_test.go` gains `fmt`/`math`/`mathrand "math/rand/v2"`, `linkage_test.go` adds `go/ast`/`go/parser`/`go/token`/`strconv`/`testing`) — all stdlib, all test-side, and **an import LINE is not a go.mod MODULE**; there is no new sub-package, so `reference_new_subpackage_pulls_transitive_module` does not bite. NC observed: a fake `require` ⇒ 67 -> 68. The tracked lineage figure is **2** (the phase-61.2 count: `quic-go` direct + `qpack` indirect) — **NOT a repo total**; the single `go.mod` requires **67** (18 direct + 49 indirect). **Phase 75 landed +0** — `go mod tidy -diff` EMPTY and `git diff master -- go.mod go.sum` EMPTY. The row needed `crypto/tls` and `internal/stats`, both ALREADY imported by `internal/listener/manager.go`; **an import LINE is not a go.mod MODULE**, and there is no new sub-package, so `reference_new_subpackage_pulls_transitive_module` does not bite. ⚠️ **TEST-side imports grew by exactly four** (`log`, `math`, `net/http`, `strconv` in `0110`'s driver) — all stdlib, all test-side, **not a production-envelope violation**.
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` (see `ENVOY_TARGET.md`).
- **conformance:** h2spec 53/53; proxy-wasm all 10 families PASS.

---

## Recent lineage

*(A SHORT recent lineage, capped at FIVE prior entries by ADR-0288. Everything earlier is in `STATE_HISTORY.md`. §Current carries the newest stage (**the phase-79 BRAINSTORM** — the stage that SELF-PICKED the `/stats/prometheus` projection-completeness gap, REPRODUCED the thirty-name silent drop BY EXECUTION against a discriminating 16/16 + 4/4 control, and REFUTED THREE INHERITED CLAIMS: the sink blast-radius, the *"four top-level prefixes"* figure, and the WASM row-summary rider) and — unlike the phase-78 closes — carries **NO** retained-historical entry at all: both phase-78 retained bullets (**PLAN** and **SPEC**) were MOVED to `STATE_HISTORY.md` at this close rather than deleted. The prior entries below now read, newest first: **phase-78 IMPL · phase-78 BRAINSTORM · phase-77 IMPL · phase-77 SPEC · phase-77 BRAINSTORM**; **phase-76 IMPL** was EVICTED to `STATE_HISTORY.md` as the list re-capped. ⚠️ **ONE PRE-EXISTING BOOKKEEPING GAP, STILL RECORDED RATHER THAN SILENTLY FIXED:** there has never been a `phase-77 PLAN` lineage entry. ⚠️ **AND ONE PARTLY RESOLVED BY THIS CLOSE:** the previous text noted that this heading said FIVE while the file actually carried SEVEN stages; with the two retained bullets moved out it now carries **SIX** (§Current plus five prior entries), and the "five" is EXACT for the prior list. Neither is repaired by editing the sentence that states it, because that is how the ADR-0296/0297 self-falsifying species starts.)*

- **prior active-phase:** `phase 79 (stats-prometheus-projection) BRAINSTORM done` (2026-07-28) — lifecycle-state **4 -> 1**; **ROW 79 REGISTERED `in-progress`** at `ROADMAP.md:141`, **RE-OPENING sentinel check (1)** after its fifth-ever silent close; ROADMAP **227 lines / 111 data rows**, sentinel `want` bumped **110 -> 111** in the SAME commit and rehearsed with controls BEFORE the edit. **`DECISIONS.md` and `BEHAVIOR_CONTRACT.md` BYTE-UNTOUCHED.** Docs-only: ZERO production `.go`, ZERO test `.go`. Four investigation agents on disjoint remits. **SELF-PICKED per the 2026-07-12 standing directive: the `/stats/prometheus` PROJECTION-COMPLETENESS gap — THIRTY live registered stat names SILENTLY DROPPED** by `internal/stats.ExtractTags` (an allow-list `switch`) plus `WriteProm` (a bare `return`). **REPRODUCED BY EXECUTION: 30/30 DROPPED against a discriminating 16/16 + 4/4 control.** **THREE INHERITED CLAIMS REFUTED:** the sink blast-radius (byte-mirror leaves all four other sinks BYTE-IDENTICAL ⇒ the sink leg is an AUDIT, not a migration); *"FOUR top-level prefixes"* (there are FIVE live arms, and `name.go:350`'s error string is where the wrong number came from); and the WASM row-summary rider, DECLINED on the merits. ⚠️ **THE PHASE-79 SPEC SUBSEQUENTLY REFUTED FOUR MORE OF THIS STAGE'S OWN CLAIMS** — the `116 B` figure (really **114**; its probe registered `server.live` as a counter where production registers a gauge), the `~:100-110` switch anchor (really **`:50`**), the near-miss control name `http.<sp>.tracing.spans_sent` (**FABRICATED — registered nowhere**), and the FIVE-arm repair itself (**there are NINE recognized top-level segments**). **SPEC band 9-11 was NOT adopted.**

- **prior active-phase:** `phase 78 (boot-panic-visibility) IMPL done` (2026-07-27) — lifecycle-state **3 -> 4**; **ROW 78 FLIPPED `in-progress` -> `done`**; **PHASE 78 IS CLOSED.** `ROADMAP.md` row 78 edited (`want` STAYS **110** — this row added no row; **all three sentinel checks re-run AFTER the edit** and no matcher string leaked, verified with three firing negative controls). `BEHAVIOR_CONTRACT.md` **5750 -> 5762**; `DECISIONS.md` **17560 -> 17596** (ADR-0300 PROPOSED -> COMPLETE, §Decision + §Consequences appended IN PLACE after the retained italic footer, **no renumber, no `---`**). Production: `cmd/envoy-go/main.go` ONLY (44 insertions / 24 deletions); test: `cmd/envoy-go/main_test.go` ONLY; `go.mod`/`go.sum` byte-clean; stat surface **+0**, argued STRUCTURALLY. Four worktrees off master (stage `phase78-impl`, docs `phase78-t7`/`phase78-t8` merged in, a DETACHED gates worktree at the T5 tip so the break arms could not corrupt the 414 s differential run), subagent-driven, subagents committed locally only, controller squash-pushed at close.

- **prior active-phase:** `phase 78 (boot-panic-visibility) BRAINSTORM done` (2026-07-27) — lifecycle-state **DONE -> 1**; **ROW 78 REGISTERED `in-progress`** at `ROADMAP.md:140`, **RE-OPENING sentinel check (1)** after its fourth-ever silent close; **`DECISIONS.md` and `BEHAVIOR_CONTRACT.md` BYTE-UNTOUCHED**. Fresh worktree `/home/esa/git/envoy-go-wt-p78bs` off master `4d7f63c2`, branch `phase-78-brainstorm`. Docs-only: **ZERO production `.go`, ZERO test `.go`**. **FOUR investigation agents on disjoint remits with PRIVATE scratch and PRIVATE worktrees**, plus controller re-derivation of every load-bearing claim. **THE PICK, SELF-PICKED per the 2026-07-12 standing directive (smallest defensible candidate first): BOOT-PANIC VISIBILITY — an Operational-tooling-family MAINTENANCE row claiming NO family ordinal.** ⚠️ **THE DEFECT, REPRODUCED BY THE CONTROLLER IN THREE ARMS: `cmd/envoy-go/main.go:298-303` registers a shutdown `defer` blocking on `<-flusherDone`, a channel closed ONLY at `:368`/`:370` — so ANY panic in that ~70-line boot window unwinds into a defer that waits FOREVER, and the process HANGS with ZERO BYTES OF OUTPUT.** Measured: duplicate `stat_prefix` ⇒ normal boot **EXIT=124, 0 bytes**; the **SAME config** under `--mode validate` ⇒ **EXIT=2 with the panic PRINTED** (`stats: duplicate metric registration`, `internal/stats/registry.go:107`); **negative control** (prefix renamed) boots cleanly (`envoy-go ready`). **Arm 1 vs arm 3 is the decisive pair — same config, same panic, printed on one entry path and swallowed on the other — which isolates the cause to the defer, not the panic and not the config.** ⚠️ **The window is NOT stat-specific** (it spans `boot.Construct`/`admin.New`/`lm.Start`/`bs.Stats.Freeze`; `registry.go` carries FIVE `panic(` sites). Fix = a **defer RELOCATION**, ~8 lines moved, plus a black-box guard whose **failing baseline is ALREADY EXECUTED**. ⚠️ **THE ORDERING IS FORCED BY EVIDENCE: this row is a PREREQUISITE for the banked projection row**, whose endgame (registration-time validation) is a BOOT PANIC — today a silent hang. ⚠️ **THE HEADLINE FINDING THE STAGE BANKED RATHER THAN TOOK: the `/stats/prometheus` PROJECTION-COMPLETENESS gap is THIRTY names across SIX families, and TWENTY-EIGHT of them PRE-DATE phase 77 by up to eleven months** — so the router's *"runtime prometheus fix"* framing is **REFUTED**; phase 77 was the SIXTH family to hit it and the FIRST to notice. Audited by two agreeing methods (293 registration sites; 4145 distinct names over 107 bootable fixtures, 271 end-to-end confirmations, ZERO mispredictions either way). ⚠️ **The gap was documented in code at phase 32.1** (`internal/admin/stats.go:12-22`) **and never enumerated.** Recorded as a NAMED ROADMAP deferral so the audit survives. ⚠️ **TWO CONTROLLER SELF-CORRECTIONS:** a hand-invented probe INPUT (`listener.0.0.0.0_10000.…`) manufactured a phantom SN3 defect — the shape never occurs, `normalizeAddr` pre-strips dots — **a probe's INPUT is a claim too**; and an **EMPTY probe output read as a CONFIRMING result** (0 `# HELP` lines from a container that never started: `id: n` is YAML-1.1 boolean `false`) — **a zero-match grep and an empty input are indistinguishable unless you measure the input.** ⚠️ **A NEW SHARED-RESOURCE HAZARD, found by causing it:** an agent's `docker ps | grep | xargs docker kill` killed THREE SIBLING AGENTS' containers — tear down **by container NAME**, never by ancestor/image filter.

- **prior active-phase:** `phase 77 (runtime-static-layer) IMPL done` (2026-07-27) — lifecycle-state **3 -> DONE**; **ROW 77 FLIPPED `in-progress` -> `done`** at the T12 six-gate (ADR-0106, the SOLE leg — a SINGLE FLAT ROW, `reference_roadmap_split_phase_row_done`); **ADR-0299 PROPOSED -> COMPLETE in place, no renumber, NO `---` separator.** Fresh worktree `/home/esa/git/envoy-go-wt-p77impl` off master `9003de01`, branch `phase-77-impl`; **FIVE subagent dispatches over the T1-T12 spine -> 10 local commits -> ONE squash**, two dispatches running CONCURRENTLY on disjoint file sets with explicit-path staging. **THE ROW DELIVERED:** the phase-01 wholesale `layered_runtime` boot-reject is **PARTIALLY LIFTED — the `static_layer` arm ONLY** — behind `internal/runtime.Snapshot` (recursive `Struct` -> dotted-path flattening, **TWO** termination branches, distinct-key **UNION** across layers), with a **NINE-arm hand-written reject roster landed ATOMICALLY** in the same commit as the lift and **both** guarding test sites. **+2 stats (1205 -> 1207) / +1 fixture (`0118-runtime-static-layer`, 119 -> 120) / +0 fuzzers (55) / +0 BackendKinds (tail 38) / +0 go.mod modules / +0 new packages (73) / +0 new PUBLIC surface (`validate` stays 2)** — each asserted BY COMMAND with its negative control observed. ⚠️ **THE HEADLINE — A REAL DEFECT THE ROW EXPOSED AND DELIBERATELY DID NOT FIX: the two new gauges are served on `/stats` and SILENTLY DROPPED from `/stats/prometheus`.** Found because the fixture-as-specified came back RED, then root-caused rather than assertion-adjusted. The values are **registered and correct**; the Prometheus **RENDERER** discards them — `internal/stats.ExtractTags` dispatches on a fixed set of recognized top-level segments (`cluster.`/`http.`/`listener.`/`server.`/`wasm.` + the SN9 default) and errors on `runtime.num_keys`, and `internal/stats.WriteProm` **silently `return`s** on that error under a comment reading *"should not occur"*. **Nothing logs and nothing errors**, which is why it passed registration, the whole unit suite, `go vet` and `golangci-lint` alike. Corroborated TWO ways: the live cross-side probe AND a direct read of both call sites. ⚠️ **Row 77 is the FIRST row to register a stat under a top-level segment the projection does not know**; ⚠️ **whether any PRE-EXISTING name is also dropped is UNVERIFIED and is reported as neither confirmed nor refuted.** NOT fixed here — `internal/stats` is on this row's byte-untouched roster, is governed by ADR-0061 and carries its own name-mapping tests. ⚠️ **But the deferral is ENFORCED, not prose:** `0118` reads its VALUES from flat `/stats` (sound cross-side — both names carry no address and no dynamic segment) and separately PINS the exposition asymmetry, so **the day the projection learns `runtime.`, `0118` goes RED on purpose.** ⚠️ **THE SECOND HEADLINE — FOUR OF THE PLAN'S OWN CLAIMS WERE REFUTED BY EXECUTION.** **(1)** The declared **must-stay-green** for the fixture under the PAIR-MATCH break is FALSE: the shipped arm carries `numerator` **ALONE**, so a pair-matching flattener recurses it and the total goes 6 -> **8**; the fixture went RED, correctly. **The error was reading a CONFIG counterfactual (varied input, correct code) as an IMPLEMENTATION counterfactual (fixed input, broken code)** — a reference probe can never perform the second, because you cannot install a broken flattener in the reference binary. ⚠️ **The sign was benign — the fixture is a STRONGER detector than it was credited with — and that is luck, not design.** The case-INSENSITIVE half of the same claim HOLDS (fixture green under U5, measured). **§1.1's architectural conclusion is UNTOUCHED**: the nested-vs-empty-struct collision at 4 is unchanged, so the roster still belongs at the unit layer. **(2)** The stale `bootstrap.go:499` cite occurs **TWICE** — ADR-0278 **and ADR-0280**, which re-uses its frame; fixing only the named one would have left it live. **(3)** `manager.go:1078-1082` likewise occurs **twice** in its paragraph. **(4)** Post-edit `grep -c '1205'` is **2**, not the predicted 1 — phase 75's own ledger line legitimately ENDS at 1205. ⚠️ **BROKEN-GATE COUNT: ELEVEN -> THIRTEEN, both found by execution here.** **TWELFTH — the PLAN's OWN G9 recipe is VACUOUS**: `go test > full.log` **without `-v`** yields a **2-line** log, so `grep -c -- '--- PASS'` and `grep -c -- '--- FAIL'` BOTH return **0** on a fully green run — a "0 FAIL" reading from it proves nothing. **THIRTEENTH — G3's roster is desynced BY CONSTRUCTION against a DELETED file**: built from the tip's `ls-files` alone it cannot represent `internal/runtime/doc.go`, which this row deleted, so the DESYNC leg fires on a CORRECT tree; the roster must span BOTH trees. **ALL BREAKS EXECUTED.** U1-U6 each fired its own subtest (**U2 and U3 both total 4 with DIFFERENT key sets — §1.1's central claim confirmed live by the same run that refuted its U6 corollary**) · R1-R10 each reddened its own roster row with the other nine green, **arm 4 / arm 5 proved non-short-circuiting in BOTH directions** · R-lift reddened the accept test · S1-S5 all fired, with **S3/S4 confirming a count-only stat guard PASSES a build whose two stat names are BOTH WRONG** · F1-F5 all fired, **F4 leaving the suite otherwise GREEN**, and **F1 observed the vacuity live** (`subj num_keys=0`, which a single-value lookup would have read as `0 == 0`). ⚠️ **F3's PREDICTED GUARD IS THE WRONG ONE** — `[no tests to run]` appears only when the fixture DIRECTORY is also absent; with the directory present the runner `t.Skipf`s and the bare `-run` form exits **0 with `ok`**, so it is the **AssertStats** guard that catches a missing blank import in the realistic case. **`FuzzBootstrapLoad` gained the prefix invariant it only CLAIMED** (it discarded both returns): **661,639 execs, ZERO violations, no crasher**, NEGCTRL red. **G9 full suite: 120/120 subtests present, `comm -3` EMPTY, 119 PASS / 1 FAIL**, the failure being `0084-otlp-access-log` on `bind: address already in use` — **classified as the INDEXED startup flake ON EVIDENCE**: it fails BEFORE any assertion on an EPHEMERAL port, **the same fixture PASSED at the same tip 7 minutes earlier**, isolate-re-run **3x PASS**, and `0084` is untouched by this row. ⚠️ **`0061-lb-ring-hash` PASSED — no finding there.** ⚠️ **The Bash cwd reset fired AGAIN — the ELEVENTH consecutive session**, and twice on plain read-only `cd && ls`; every git command used `git -C <abs-worktree-path>`.

- **prior active-phase:** `phase 77 (runtime-static-layer) SPEC done` (2026-07-26) — lifecycle-state **1 -> 2**; **ROW 77 STAYS `in-progress`**; **`ROADMAP.md` and `BEHAVIOR_CONTRACT.md` BYTE-UNTOUCHED**; **`DECISIONS.md` gains ADR-0299 §Context** (tail ADR-0298 -> **ADR-0299 PROPOSED**, next-free **ADR-0300**). Fresh worktree `/home/esa/git/envoy-go-wt-77spec` off master `638ef32a`, branch `phase-77-spec`. Docs-only: **ZERO production `.go`, ZERO test `.go` in the SPEC commit**. **FOUR agents on disjoint remits with PRIVATE scratch** — two LIVE reference probe fleets against `envoyproxy/envoy:contrib-v1.37.2` (image id `7edd5b0fd763` verified against the `ENVOY_TARGET.md` pin, fresh container per arm, bridge network, published ports), one Go-execution agent in its own throwaway worktree, one read-only survey — plus controller re-derivation of every load-bearing claim. **THE D-RSL-* DOCKET IS SETTLED BY EXECUTION ON EVERY ITEM, AND EXECUTION REFUTED TWENTY-THREE CLAIMS (R1-R23).** ⚠️ **THE HEADLINE — THE REJECT ROSTER'S FOUNDING PREMISE IS REFUTED AND THE ROW'S FRAMING INVERTS.** Every prior document calls `disk_layer`/`admin_layer`/`rtds_layer` *"unsupported oneof arms"* in a framing that presupposes the reference rejects them. **The reference ACCEPTS ALL THREE** — `--mode validate` rc=**0** and live boot `/ready`=200 for each; `disk_layer` with a real directory actually **loads keys**; `rtds_layer` returns rc=0 once a `node` block and a static non-EDS cluster are present. ⚠️ **The briefed "rtds fails validate" result is an ARTIFACT** — the first failure was `node 'id' and 'cluster' are required` (a **node** error) and the second was a **missing-cluster** error; **neither was ever a runtime-layer rejection**. ⇒ **envoy-go rejecting those three is a DELIBERATE DEPARTURE, not parity**, and the SPEC records it as one. The row fixes a reject-what-the-reference-serves defect for ONE arm and **deliberately preserves it for THREE** — correct, because silently ignoring `rtds_layer` means **a config asking for DYNAMIC runtime quietly gets STATIC values**, but a SPEC claiming parity here would have documented a departure as a fix. ⚠️ **THE SECOND HEADLINE — THE FLATTENING RULE THE LINEAGE RECORDS IS WRONG, AND AN IMPLEMENTER CODING IT WOULD HAVE SHIPPED A WRONG PARSER.** *"A Struct carrying `{numerator, denominator}` terminates flattening"* is true of that instance and wrong three ways, each killed by a named arm (15 arms, 3x each): **either name ALONE terminates** · the test is **purely LEXICAL and CASE-SENSITIVE** (`{Numerator, Denominator}` **recurses**) · **the VALUES are never inspected** (`{numerator: "notanumber", denominator: NOTANENUM}` boots cleanly). **Field count is irrelevant** — one-field `{foo: 1}` **recurses** while one-field `{numerator: 25}` **terminates**; that pair is the discriminator. ⚠️ **An implementation that parses a FractionalPercent here REJECTS configs the reference ACCEPTS.** ⚠️ **AND A SECOND TERMINATION BRANCH NO DOCUMENT RECORDED: THE EMPTY STRUCT** — `e: {f: {}}` ⇒ **1** key `e.f`, `e: {}` ⇒ **1** key `e`. **The inherited three-arm pin set contains no empty leaf and would have passed against an implementation that emits ZERO there** ⇒ a fourth fixture arm was added. **THE DOCKET, item by item:** **D-RSL-EMPTY -> PARSE-REJECT**, and for a better reason than the BRAINSTORM's aesthetic one — a `runtime_modify` cross-product plus a post-write re-read proves the synthesized layer is a **genuinely FUNCTIONING admin layer** (`num_keys` **0 -> 1 -> 3**, `admin_overrides_active` 0 -> 1), so **present-with-zero-declared-layers IS an IMPLICIT admin-layer request**, and rejecting the explicit form while accepting the implicit one would be incoherent; ⚠️ the honest cost is stated rather than buried — this **re-introduces the row's own defect class in one narrow arm** · **D-RSL-STATS -> +2**, and ⚠️ **the +9 alternative DOES NOT EXIST AS DESCRIBED**: the brief called the other seven *"permanently zero"*, but `runtime.load_success` and `runtime.override_dir_not_exists` measure **1 in EVERY arm** including the absent arm, so parity would require implementing snapshot-load accounting and override-dir probing · **D-RSL-NUMKEYS-DEEP -> COLLIDE into ONE key**, no error, no double-count — ⚠️ **but the within-layer WINNER IS NON-DETERMINISTIC**, ~40/60 across **18 fresh processes** with identical config bytes, so **a single-run probe reports clean last-declared-wins as an ARTIFACT**; cross-layer collisions ARE deterministic (later layer, 4/4) · **D-RSL-EMPTYSTR -> `""` FALLS THROUGH**: it neither shadows nor deletes — `final_value` stays `"lower"` while the key is **still stored and still counted**; ⚠️ the warned trap is worse than described, since `layer_values` renders a present-but-empty key **byte-identically to absent** (`["",""]`) ⇒ **the `/runtime` body can NEVER discriminate presence from emptiness** · **D-RSL-REJECT -> a NINE-arm roster**, ⚠️ with the tenth (**>1 admin layer**) **DROPPED AS UNREACHABLE** (envoy-go rejects `admin_layer` outright, so a second one can never be reached — shipping it would be an untestable roster row) · **D-RSL-SPLIT -> a SINGLE FLAT ROW at 11-13 tasks** (the BRAINSTORM's 9-11 was a declared FLOOR; this is the re-derivation it asked for) · **D-RSL-ADR -> YES, ADR-0299** · **D-RSL-PKG (NEW at this SPEC) -> `internal/runtime` imports `structpb` and NOTHING ELSE**, tightened from the BRAINSTORM's three-import permission because `go list -deps ./internal/runtime` returns **exactly one line** — a zero-dependency leaf, not even stdlib. **ADR-0016 EXECUTED, not assumed: CONFIRMED for unknown FIELD NAMES at any proto-typed depth (with a vacuity control that PASSED), and NOT a substitute for semantic validation** — `disk_layer`/`admin_layer`/`rtds_layer`, an **unset oneof** (`GetLayerSpecifier()` a **nil INTERFACE**, needing an explicit `case nil:`), **duplicate layer names**, list values and null values **all unmarshal cleanly**. ⚠️ **`reference_protojson_null_decodes_to_nil` DOES NOT APPLY here** — `k.null: null` yields a **non-nil** `*structpb.Value` of kind `Value_NullValue`; only `AsInterface()` is nil, and an implementer walking `GetFields()` **will see the key**. ⚠️ **A SECOND TEST SITE IN A SECOND PACKAGE THAT NO PHASE-77 DOCUMENT NAMES: `validate/validate_test.go:65-79`** — the public-package sibling of `internal/bootstrap/bootstrap_test.go:82-96`. **BOTH** use the fixture `name: static_layer` + `static_layer: {}`, **exactly the arm this row legalizes**, so **BOTH flip to `err == nil` and die at `t.Fatal`.** ⚠️ **Of the FOUR tests guarding these two rejects, exactly ONE asserts the `"bootstrap: "` prefix**, and ⚠️ **`FuzzBootstrapLoad` does NOT assert the invariant its own comment states** (`_, _ = Load(...)` — a **panic-only** guard), so it cannot be cited as a prefix gate. ⚠️ **`internal/bootstrap` has ZERO named reject constants and ZERO `ByteStable` tests** — all **47** arms are inline `fmt.Errorf` — so the *"per the landed discipline"* instruction points at a discipline landed in **filter** packages; this row **introduces** it here, scoped to its own arms. ⚠️ **The reference's error wording is NOT byte-assertable for TWO classes** — every PGV message carries a proto DebugString whose marker **rotates** (`goo.gle/debugstr`/`debugonly`/`debugproto`) and the unknown-field message varies in whitespace **and** in its `near L:C (offset N)` numbers across runs of the SAME file; **the randomization leaks into BOOT-ERROR text too**. Only the **three hand-written** loader messages are byte-stable 3/3. ⚠️ **PGV enforces only TWO of the four "PGV arms"** — duplicate-name and >1-admin-layer are **hand-written loader checks**. ⚠️ **THE PRECISION HAZARD IS INVERTED AND NON-MONOTONIC:** the obvious guess (large ints lose precision through a double) is **REFUTED** — `9007199254740993` renders **EXACTLY**, because out-of-int32 values are stored as `string_value`; the loss hits **small in-int32-range** integers via 6-significant-digit `%g` (`2147483647` -> `'2.14748e+09'` while the LARGER `2147483648` is exact), and the rendering is **NON-INJECTIVE** (`1000000` and `1000001` both -> `'1e+06'`). **Floats are safe; integers are not.** ⇒ **a FOURTH independent reason the fixture asserts STATS ONLY.** ⚠️ **THE FIXTURE'S PORT IS `10118`, NOT `10450`** — the router's 10450 is a correct max+1 figure (max in use **10449**) but ports are **family-banded** and the dominant, most-recent convention is `10<fixture index>` (**0114->10114, 0115->10115, 0116->10116, 0117->10117**); 10450 is the TLS/SDS band and this is not a TLS fixture. ⚠️ **A fixture needs a THIRD registration gate no phase-77 document mentions — a BLANK IMPORT in `runner_test.go`** (currently **119**); without it `init()` never runs, the registry lookup misses at `:192`, and the runner **`t.Skipf`s SILENTLY GREEN**. ⚠️ **`expectations.yaml` is 100% comment prose read by ZERO Go code** (ADR-0019). ⚠️ **The `StatsAsserter` dispatch is a SILENT type assertion with no `else`** — a signature typo makes the whole leg vanish green. ⚠️ **ADR-0278's heading is `DECISIONS.md:16670`, NOT the `:16672` carried by the BRAINSTORM AND by this file** (`:16672` is the `**§Context.**` body line). ⚠️ **There is NO `Phase 76` stat-ledger entry** — the chain runs `46.1b -> 47.1 -> 51 -> 74 -> 75`, so the Phase 77 line follows **Phase 75 at `:5004`** directly; `1205` occurs at **three** lines (`:831`, `:847`, `:5004`) and ⚠️ **`:847` also carries a stale *"115-dir"* against an actual 119**. ⚠️ **A FOURTH documentary site nobody named: `DECISIONS.md:16430`**, where ADR-0268 records `validate/validate_test.go`'s reject-arm roster **including `layered_runtime`** — a live test roster inside an ADR that the rewrite falsifies. **A Runtime-family row claiming the FIRST family ordinal.** *(Historical, retained: **phase 77 BRAINSTORM done** — the stage that OPENED the Runtime + hot restart family and RE-OPENED sentinel check (1) after its third-ever silent close, self-picked per the 2026-07-12 standing directive from a state where **every chartered row was already `done`** (the first such registration in the project's history); it found the sentinel's **OWN check (2) FAILING UNSAFE** (matching only the long phrase, so the Operational-tooling family's three SHORT-form un-chartered candidates were **INVISIBLE** — fixed **at the MATCHER, not the prose**, with three negative controls) and **REFUTED the router's *"strictly more"* selection calculus as arithmetic** (opening a family **MINTS** a check-(2) sentence: **3+3 -> 2+4 = 6 -> 6, NET ZERO**; what survives is strictly weaker — *opening a family is the ONLY way that family's work becomes enumerable at all*), plus the measured record that **check (2) HAS NEVER GONE DOWN** across ~17 phases.)*





## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
