# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `<next-§9-family-row>` — phase 15 complete as of this commit (lifecycle-state-5 phase-done; awaiting next planning). Next phase is the next §9 HTTP filters family-row (remaining unbrainstormed members: jwt_authn, rbac, ext_authz, ext_proc, oauth2, lua, wasm, adaptive concurrency, admission control, global rate limit, …); determined by the next brainstorm session consulting the §9 heading + phase 15 just-shipped artefacts per ADR-0106(e). Phase 15 shipped `envoy.filters.http.bandwidth_limit` — EIGHTH §9 family-row (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11, csrf @ 12, buffer @ 13, compressor @ 14); 5 ADRs (ADR-0135..ADR-0139) + ADR-0125 §(xi) amendment; 18 differential fixtures (0000-0017) + 19 fuzzers green; 6 gates all pass.
- **phase-directory:** `docs/envoy-go/phases/15-http-filter-bandwidth-limit/` — COMPLETE through phase-done (Task 15). Contains BRAINSTORM.md + SPEC.md + PLAN.md + PROGRESS.md (Tasks 1-15 documented). REVIEW.md is the remaining Task 16 (lifecycle-state-5 → 6 transition per ADR-0005 §Decision 4); the phase-done commit (this one) closes lifecycle-state-4 → 5.
- **lifecycle-state:** `phase 15 done; awaiting next planning` — phase 15 BRAINSTORM.md authored (squash-merged at `fa125f2`; SHA-fill at `e7a26ef`), SPEC.md authored (squash-merged at `49e0361`; SHA-fill at `cd45af0`), PLAN.md authored (squash-merged at `a5c5ec9`; SHA-fill at `36c91c9`), implementation Tasks 1-14 landed in-worktree, phase-done Task 15 at this commit, REVIEW.md at Task 16 pending. All 6 phase-done gates green (Gate A build/vet/lint clean; Gate B race-test all packages PASS; Gate C h2spec 53/53 PASS at ADR-0051 pin; Gate D 19 fuzzers green at 30s budget each; Gate E 18 differential fixtures 0000-0017 PASS; Gate F BEHAVIOR_CONTRACT.md populated per SPEC §13.1-§13.5 — bandwidth_limit subsection inserted AFTER `### envoy.filters.http.compressor` per planner-time decision 16 + 14 active stat-table rows + 2 deferred-histogram allow-list rows + equivalence-matrix row 0017 + Phase 15 forward-pointer notes subsection + Timing tolerances ±70ms per-side entry + Twin-series filter discipline phase-15 histogram extension). ROADMAP row 15 flipped `in-progress → done` at this commit with date `2026-05-11`. ADR roster: ADR-0135 + ADR-0136 (Task 2 — package + types + factory + compiledConfig + 14-stat `filterStats`), ADR-0137 (Task 3 — kbps-per-tick `throttleDuration` + Path B-async + wire-shape divergence + non-endStream DataContinue HCM-deadlock-avoidance fix per Task-14 finding (d)), ADR-0139 (Task 7 — per-route INDEPENDENT-stats wiring + `newFilterStatsIfAbsent` post-Freeze idempotent registration), ADR-0138 (Task 8 — 14-stat surface + namespace + Prometheus inline-prefix + histogram twin-series allow-list). Plus ADR-0125 §(xi) amendment paragraph (landed at SPEC commit) documenting the NEW 6th canonical per-route pattern (bare-message-via-TPFC + code-level-required-`limit_kbps`-at-per-route). **Task-14 empirical findings flowed into BEHAVIOR_CONTRACT §13.4 + §13.5:** (a) cross-side wall-clock divergence > ±70ms for initial-burst-capacity bodies refuting SPEC §11.P9(c); per-side discipline adopted; (b) `*_enabled` + `*_enforced` `counterModePerSideExact` mode (initial-burst-discount divergence); (c) 6 gauges not asserted (transient/noisy); (d) non-endStream `DataContinue` HCM synchronous-dispatch deadlock-avoidance fix (mirrors phase-13 buffer precedent).
- **next-skill:** `superpowers:requesting-code-review` — per BOOTSTRAP_PROMPT.md §5 lifecycle-state-5 entry condition (phase-done reached, REVIEW.md pending). Task 16 per PLAN authors `REVIEW.md` covering the 6-axis self-review: (1) ROADMAP row alignment, (2) ADR roster freshness (ADR-0135..ADR-0139 + ADR-0125 amendment §(xi)), (3) BEHAVIOR_CONTRACT 5-edit bundle alignment with SPEC §13.1-§13.5 (including Task-14 finding integration into §13.4 + §13.5), (4) per-side wall-clock + per-side counter discipline honesty (Task-14 4-finding integration), (5) PLAN-deviation honesty (ZERO new ADRs added impl-time — SPEC roster intact; Task-14 algorithmic divergence from SPEC §6.5 wording documented at ADR-0137), (6) 6-gate verification reproducibility. After REVIEW.md, the worktree merges back to master via the wt-merge skill per project memory + ADR-0003.
- **next-skill-scope:** Cold-start scope for the Task 16 REVIEW.md session. Read first: `docs/envoy-go/phases/15-http-filter-bandwidth-limit/PLAN.md` Task 16 for verbatim REVIEW.md authoring contract; `docs/envoy-go/phases/15-http-filter-bandwidth-limit/PROGRESS.md` Tasks 1-15 (entries log every impl decision + ADR anchor + gate output for the reviewer's traceback); `docs/envoy-go/phases/15-http-filter-bandwidth-limit/SPEC.md` §13 + §15 (acceptance checklist); `docs/envoy-go/BEHAVIOR_CONTRACT.md` lines 36 (equivalence-matrix row 15) + 1416 (bandwidth_limit subsection) + 1854 (Phase 15 forward-pointer notes) + 271 (twin-series filter discipline phase-15 extension) + ~358 (Timing tolerances ±70ms per-side); `docs/envoy-go/ROADMAP.md` row 15 (status `done` + date `2026-05-11`); `docs/envoy-go/DECISIONS.md` ADR-0125 §(xi) amendment + ADR-0135..ADR-0139. REVIEW.md follows the phase-14 REVIEW.md structural template (6-axis self-review + reviewer-instruction block + cross-reference to PROGRESS.md anchors). On REVIEW.md merge, run `wt-merge` to squash-merge `phase-15-http-filter-bandwidth-limit-impl` back to master + push to origin per project memory `feedback_push_to_origin.md`.
- **last-commit:** `<TBD>` — phase 15 phase-done commit (this commit). Lands: (a) BEHAVIOR_CONTRACT.md 5-edit bundle per SPEC §13.1-§13.5 (Gate F — `### envoy.filters.http.bandwidth_limit` subsection inserted AFTER `### envoy.filters.http.compressor` per planner-time decision 16; 14 active stat-table rows + 2 deferred-histogram allow-list rows; equivalence-matrix row for fixture 0017; Phase 15 forward-pointer notes subsection integrating 4 Task-14 impl-time findings; Timing tolerances ±70ms per-side entry; Twin-series filter discipline phase-15 histogram extension); (b) ROADMAP.md row 15 `in-progress → done` (date `2026-05-11`); (c) STATE.md `phase 15 done; awaiting next planning` (this file); (d) PROGRESS.md Task 15 entry with verbatim 6-gate outputs. All 6 gates green per Task 15 verbatim outputs.
- **last-updated:** 2026-05-11
- **next-free ADR:** `ADR-0140` (phase-15 ADR roster ended at ADR-0139 inclusive; advances by 1 for the next phase). The phase-15 ADR roster stayed at the SPEC-time-anticipated 5-ADR count (ADR-0135..ADR-0139); NO impl-time-added ADRs via ADR-0044 escape valve (unlike phase 14 which added ADR-0134 at Task-14 follow-up). Plus ADR-0125 §(xi) in-place amendment paragraph (landed at SPEC commit; does not advance the ADR counter).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
