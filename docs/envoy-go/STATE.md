# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `10-http-filter-header-mutation` — PLAN.md landed at this commit (phase directory `docs/envoy-go/phases/10-http-filter-header-mutation/` now contains `BRAINSTORM.md` + `SPEC.md` + `PLAN.md`). ROADMAP row 10 status remains `in-progress` (PLAN.md is an internal lifecycle artifact; row status flips only at SPEC and at phase-done per `BOOTSTRAP_PROMPT.md` §4.1 invariant 3). Phase 10 remains the THIRD §9 family-row to land (after cors @ 07.1 done, fault @ 09 done); the in-progress window closes at the phase-done commit. Per ADR-0106 flat-top-level-rows discipline, phase 10 stays a top-level row, NOT a sub-phase of any §9 parent.
- **phase-directory:** `docs/envoy-go/phases/10-http-filter-header-mutation/` — contains `BRAINSTORM.md` + `SPEC.md` + `PLAN.md` at this commit (3737-line PLAN — comparable to phase-09's 3159-line PLAN — with 18 tasks decomposing the SPEC into bite-sized TDD steps; plan-document-reviewer approved on first iteration per the writing-plans skill review loop). PROGRESS.md + REVIEW.md follow at PLAN-execution + REVIEW sessions per the phase-09 precedent.
- **lifecycle-state:** `plan-complete` — PLAN.md is the single output of this session (project's adapted lifecycle-state 3 → 4 advanced this session by authoring PLAN.md + landing the plan-document-reviewer approval). The PLAN settles all 8 SPEC §12 deferred decisions explicitly + 3 PLAN-emerging decisions: (1) `RequestRouteConfigsAllTiers` callback DECODER-ONLY (mirrors cors precedent at cors.go:163; OVERRIDES SPEC default of "BOTH symmetric"); (2) per-request cache SKIP; (3) per-route protected-header validation EAGER via new `HTTPRegistry.RegisterPerRouteValidator` hook + `BuildPerRouteConfig` signature widening (50 LoC framework delta); (4) compiledMutationOp value-typed; (5) protected-header predicate prefix-on-`:` + case-insensitive equality on `host`; (6) fuzzer SHIP; (7) race-detector cycle test ADD; (8) applyOps KEEP unexported. PLAN-emerging: (9) per-route-validator integration test fan-out table-driven per tier; (10) fixture path corrected to `test/fixtures/0012-...` per SPEC §4.3 erratum (mirrors 09 PLAN's identical erratum); (11) BackendKind enum value `HTTPHeaderMutation BackendKind = 9`. The 6 anticipated ADRs ADR-0108..ADR-0113 are anchored to lands-in-task per ADR-0044 ADR-on-impl convention. ADR-0073 amendment paragraph (in-place edit per ADR-0089 (b)) lands at Task 7 alongside ADR-0110. The next session's input is PLAN.md.
- **next-skill:** `superpowers:subagent-driven-development` per ADR-0005 routing + the user's persistent preference for subagent-driven over inline execution recorded in `~/.claude/projects/-home-esa-git-envoy-go/memory/MEMORY.md`. The next session's WORK is to execute the 18-task PLAN against an isolated worktree (per the "Always use git worktrees" memory entry — the IMPL stage MUST run in `.worktrees/phase-10-http-filter-header-mutation-impl` branched from master tip post-PLAN-SHA-fill per ADR-0003).
- **next-skill-scope:** Cold-start scope for the PLAN-execution session. Read first: `docs/envoy-go/phases/10-http-filter-header-mutation/PLAN.md` (full; 3737 lines; this session's output) — every task is self-contained with embedded source code, embedded tests, exact commands, and Anchored: footer. Read `docs/envoy-go/phases/10-http-filter-header-mutation/SPEC.md` (full; 1348 lines) ONLY when a task references a SPEC §-anchor for additional context. Read `docs/envoy-go/phases/09-http-filter-fault/PROGRESS.md` (the structural precedent for PROGRESS.md append-only entries) at Task 1. The PLAN's "Execution preconditions" block enumerates 16 verification steps the cold-start MUST run before Task 1; if any fail, the PROGRESS.md preamble records the deviation. Tasks 1–18 land in order; ADRs land at the task that anchors each first-use commit (ADR-0108/0109/0111 at Task 5; ADR-0110 + ADR-0073 amendment at Task 7; ADR-0112 + ADR-0113 at Task 16). Task 17 lands the phase-done commit; Task 18 lands REVIEW.md.
- **last-commit:** `<TBD — this commit>` — `phase 10 plan: http-filter-header-mutation [PLAN.md; 18 tasks settling SPEC §12 + 3 PLAN-emerging decisions]`. Lands `PLAN.md` (3737 lines) for phase 10 (`http-filter-header-mutation`); decomposes the SPEC into 18 bite-sized TDD tasks; settles all 8 SPEC §12 deferred decisions + 3 PLAN-emerging decisions (callback DECODER-ONLY mirroring cors precedent; per-route validator EAGER via new HTTPRegistry hook; fixture path corrected per SPEC §4.3 erratum). Plan-document-reviewer approved on first iteration with three advisory recommendations (multi-route validator test, prefer `//go:embed` for fixture YAML templates, anchor SPEC patches by section heading not line number — all advisory, none blocking, deferred to implementer's tactical choice at Tasks 4 / 13 / 16). 6 ADRs anticipated (ADR-0108..ADR-0113) with lands-in-task anchors. Inline ADR-0073 amendment paragraph lands at Task 7 alongside ADR-0110 per ADR-0089 (b). PLAN authored inline per STATE.md prior guidance (PLAN-authoring as collaborative drafting; the framework + filter + fixture + ADRs surface is well-bounded by SPEC.md). SHA-fill follow-up commit per the phase-04..09 SHA-fill convention. Per user preference, master pushed to origin after commit.
- **last-updated:** 2026-05-04

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
