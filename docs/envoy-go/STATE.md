# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `11-http-filter-local-ratelimit` — PLAN.md landed at this commit; phase directory `docs/envoy-go/phases/11-http-filter-local-ratelimit/` now contains `BRAINSTORM.md` + `SPEC.md` + `PLAN.md`. ROADMAP row 11 stays `in-progress` (no transition at PLAN commit per phase 09 / 10 precedent — `in-progress` activates at SPEC commit and closes at phase-done commit). Per ADR-0106 flat-top-level-rows discipline, phase 11 stays a top-level row.
- **phase-directory:** `docs/envoy-go/phases/11-http-filter-local-ratelimit/` — contains `BRAINSTORM.md` (605 lines) + `SPEC.md` (1225 lines) + `PLAN.md` (3900 lines authored this session via subagent draft + 1 fix-up pass + 2 reviewer iterations; mirrors phase 10 PLAN's preamble + per-task shape adapted for phase 11; plan-document-reviewer Approved on iteration 3 after the fix-up pass resolved 5 issues + iteration-2 narrow fix to two stale recursive-`New` test descriptions in the file-structure table — see commit body for fix list). PROGRESS.md, REVIEW.md follow during impl session per the phase 09 / 10 precedent.
- **lifecycle-state:** `plan-complete` — PLAN.md is the single output of this session (project's adapted lifecycle-state 3 → 4 advanced by authoring PLAN.md + landing the plan-document-reviewer approval). PLAN settles SPEC §12's 5 deferred decisions plus 4 PLAN-emerging planner-time decisions (9 total per the PROGRESS.md preamble template). Two LEGITIMATE divergences from SPEC (flagged in PLAN preamble's planner-time-decisions block): (a) **Tag-extractor registration site (D1):** PLAN extends `internal/stats/name.go`'s `flattenToProm` switch with new Rule SN9 — corrects SPEC §6.6 + §12 D1 mis-direction toward `internal/admin/stats.go` (which does NOT exist; tag-extraction lives in `internal/stats/name.go`). (b) **Per-route counter framework extension (NEW):** PLAN adds a small `internal/stats.Registry.NewCounterIfAbsent(name) *Counter` method (~30 LoC) permitting idempotent post-Freeze counter registration — needed because `BuildPerRouteConfig` does NOT recursively call any registered filter's `New` (verified via reading `internal/filter/http/perroute.go:63-85`), so per-route `*runtimeConfig` instances are built lazily by a request-time `sync.Map` cache miss path; the per-route stat names are data-driven (operator-chosen `stat_prefix`), arriving at HCM-build time which is post-Freeze. Codified as ADR-0117 (= ADR-0073 amendment paragraph) + ADR-0061 amendment paragraph in ADR-0118. SPEC §1.4's "NO new framework primitive required" claim is corrected at this PLAN — the framework delta is small (~30 LoC) and well-bounded. Third planner-time correction (PLAN decision 8): **fixture topology** — PLAN uses 4 pre-configured listeners (`l_s1`, `l_s2`, `l_s3`, `l_per_route`) instead of SPEC §7.1's two-listener `l_basic` + `l_per_route` layout. Rationale: SPEC §7.1's two-listener layout assumed per-scenario teardown was free, but the existing `fixture.Driver` interface only supports ONE `DriveSubject`/`DriveReference` invocation per fixture; reusing the existing `fixture.MultiListenerDriver` interface (added at phase 07.2 / fixture 0008; reused by phase 10 fixture 0012) avoids any framework extension and runs all 4 scenarios in one Drive call. The next session's input is PLAN.md.
- **next-skill:** `superpowers:subagent-driven-development` per the user's persistent preference (recorded in `~/.claude/projects/-home-esa-git-envoy-go/memory/MEMORY.md`: "always wants subagent-driven over inline execution for plans"). PLAN.md is the input; per-task subagent dispatch with two-stage review per the skill's discipline.
- **next-skill-scope:** Cold-start scope for the PLAN-execution session. Read first: `docs/envoy-go/phases/11-http-filter-local-ratelimit/PLAN.md` (full; 3900 lines; this session's output) — settles all 9 planner-time decisions + anchors ADRs ADR-0114..ADR-0119 + ADR-0073 amendment + ADR-0061 amendment at specific tasks per ADR-0044 ADR-on-impl convention. Secondary reads: `SPEC.md` (1225 lines; load-bearing for design); `BRAINSTORM.md` (605 lines; context). Read `docs/envoy-go/DECISIONS.md` ADR-0004 / ADR-0044 / ADR-0040 / ADR-0061 / ADR-0072 / ADR-0073 / ADR-0074 / ADR-0100 / ADR-0102 / ADR-0103 / ADR-0106 / ADR-0108..ADR-0113. Read `internal/filter/http/perroute.go:63-85` (`BuildPerRouteConfig` — does NOT call `New`); `internal/filter/http/fault/fault.go:321` (SendLocalReply + StopIteration precedent); `internal/filter/hcm/codec.go:17` (`serverHeader()` literal `"envoy"`); `internal/stats/name.go` (`flattenToProm` switch — Task 6 extends with SN9); `internal/stats/registry.go:75-121` (NewCounter + Freeze discipline — Task 5 adds NewCounterIfAbsent); `cmd/envoy-go/main.go:111–122` (registration site after header_mutation; phase 11 adds 1 line at Task 7); `test/fixtures/0012-http-header-mutation/driver/driver.go` (MultiListenerDriver precedent — Task 13 mirrors). The session emits Tasks 1–16's commits (lifecycle-state 4 → 5 in the project's adapted lifecycle). Branch: spawn next worktree `phase-11-http-filter-local-ratelimit-impl` off this PLAN tip per the user's worktree-per-stage preference.
- **last-commit:** `TBD` — `phase 11 plan: http-filter-local-ratelimit [PLAN.md; lifecycle 3 → 4 plan-complete]`. Lands `PLAN.md` (3900 lines) for phase 11 (`http-filter-local-ratelimit`); 16 tasks per the SPEC §1.4 12–16 estimate (mid-bound; the file split into `bucket.go` + `local_ratelimit.go` adds Task 3; closing tasks split into Tasks 14/15/16 per phase 10 precedent). PLAN settles SPEC §12 deferred decisions D1 (tag-extractor → SN9 in `internal/stats/name.go`) + D2 (`SetDecoderCallbacks` + `SetEncoderCallbacks` per cors+fault+header_mutation precedent) + D3 (six explicit PGV checks in `New` factory — verbatim Envoy error string for `fill_interval >= 50ms`) + D4 (scenario 3 ±10ms simple `time.Sleep`; retry-with-deadline reserved as fallback) + D5 (clock injection SKIP per SPEC default). Plus 4 PLAN-emerging decisions: PLAN-6 file-split (bucket.go + local_ratelimit.go); PLAN-7 race-detector test (`TestTokenBucket_ConcurrentTryConsume` 64 goroutines × 100 iter under -race); PLAN-8 fixture topology (4 pre-configured listeners replace SPEC §7.1's reconfiguration-with-teardown); PLAN-9 `BackendKind` name (`HTTPLocalRateLimit BackendKind = 10`). Plan-document-reviewer Approved on iteration 3 after fix-up pass (5 issues resolved: preamble vs Task 5 contradiction on per-route mechanism reconciled to lazy-cache narrative; Task 5 stream-of-consciousness exploration replaced with Settled approach + Considered alternatives; file-structure table added rows for `internal/stats/registry.go` + `_test.go`; Task 13 framework gap resolved by 4-listener topology with existing `MultiListenerDriver`; Task 4 `filterStats` field type settled on `*stats.Counter`) + iteration-3 narrow fix (two stale recursive-`New` test descriptions in line 67's `local_ratelimit_test.go` cell renamed to lazy-cache equivalents). The PLAN worktree branch `phase-11-http-filter-local-ratelimit-plan` is branched from the SPEC worktree tip `47b624b` (NOT from master) per the user-selected worktree-per-stage stacking pattern. SHA-fill in a follow-up commit per the phase-04..10 convention.
- **last-updated:** 2026-05-05

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
