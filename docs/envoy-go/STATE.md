# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `05-http-2`
- **phase-directory:** `docs/envoy-go/phases/05-http-2/` — exists, with `SPEC.md` (`612cdea` — drafted by `superpowers:brainstorming` in the autonomous mode of ADR-0004; APPROVED on iteration 1 by the `spec-document-reviewer` subagent of ADR-0004 step 3, advisory-only recommendations, two of which were addressed inline before the SPEC commit — §5.4 ALPN-dispatch dead-branch collapse and §4.4 ADR-Q symbol-name tightening; the two remaining advisory items are planner-scope per ADR-0004 step 4). Phase 04's directory at `docs/envoy-go/phases/04-http-1.1/` is closed read-only history (see prior STATE.md entry at `1542102` for its full inventory).
- **lifecycle-state:** `2` — `SPEC.md` exists (commit `612cdea`); `PLAN.md` does **not** yet exist. Per `BOOTSTRAP_PROMPT.md` §5 state 2, the next session invokes `superpowers:writing-plans` scoped to phase 05 and produces `phases/05-http-2/PLAN.md`. The state-2 entry GATE is the planner's responsibility: if `PLAN.md` exceeds ~25 tasks OR ~1500 LoC estimated, the planner must split phase 05 into sub-phases per `BOOTSTRAP_PROMPT.md` §6 and exit. The SPEC explicitly anticipates this in §11.1 with three plausible split axes; the recommended axis is "split by surface" (05.1 = downstream H2 + h2spec; 05.2 = upstream H2 + fixture 0004 + ADR-0035 H2-leg closure). At SPEC-stage entry the ROADMAP row 05 was flipped `planned` → `in-progress` in commit `8d18320` per `BOOTSTRAP_PROMPT.md` §4.1 invariant 3 (corrected pattern; phase-04 deferred this flip to state-2 entry, phase-05 does it at directory-creation time as STATE.md instructed the prior session — and as the phase-00 fix-up commit `33a3b89` retroactively prescribed). No further ROADMAP change is required at this state transition.
- **next-skill:** `superpowers:writing-plans`
- **next-skill-scope:** Plan phase 05 from `phases/05-http-2/SPEC.md` (`612cdea`). The SPEC's §13 acceptance checklist is the planner's source-of-truth for what `PLAN.md` must arrange to be deliverable. Concrete plan-time deliverables enumerated in SPEC §4 (4.1 new production code under `internal/filter/hcm/h2/` + `internal/cluster/dial_h2.go`; 4.2 changed code in HCM + cluster + listener manager; 4.3 new harness — fixture `0004-h2-routing` with full PKI, `test/conformance/h2spec/` for the project's first non-vacuous gate-(c) wiring, `test/helpers/h2.go`; 4.4 docs — new `BEHAVIOR_CONTRACT.md` `## HTTP/2` subsection per ADR-T, NEW file `docs/envoy-go/CONFORMANCE_PINS.md` for the `summerwind/h2spec` SHA pin, ten new ADRs ADR-P..ADR-Z to land at planner-assigned numbers ADR-0045..ADR-0054). Plan-time decisions explicitly deferred to the planner, listed in SPEC §10: streaming-body-vs-wait-for-END_STREAM dispatch (SPEC prescribes wait-for-END_STREAM); direct-response codec-neutral factoring vs duplicate writers (SPEC prescribes neutral factoring); H2Request/H2Response shape vs stdlib re-use (SPEC recommends stdlib for action surface, internal types for codec); CONFORMANCE_PINS.md vs Go-const for the pin (SPEC prescribes the doc file); `--allow-h2c` mechanism (CLI flag vs env var vs build tag); `routerActionH2`/`routerAction` interface vs concrete switch; per-cluster RR counter scope; H2-server pseudo-header ordering (RFC-mandated, planner notes only); concrete ADR numbers ADR-0045..0054 (planner verifies the next-free); structured-vs-heredoc `expectations.yaml` for fixture 0004 (SPEC prescribes heredoc per phase-04 M-6 carry-forward triage in §12). Phase-04 REVIEW Minor carry-forwards M-2/M-4/M-5/M-6/M-7 are dispositioned in SPEC §12 and formally land in ADR-X (M-7 carries a "phase-06-must-consume" tag for `Filter.statPrefix`). Splitting risk: per SPEC §11.1, phase 05 is the largest phase to date and likely trips the §6 plan-size gate; the planner must apply the gate and either ship a single PLAN or split. If splitting, ROADMAP row 05 becomes a parent row with `sub-phases: 05.1, 05.2[, 05.3]` per the recommended "split by surface" axis (or another axis if the planner judges otherwise — SPEC §11.1 enumerates three). The phase-05 SPEC remains the master design document under any split; sub-phase SPECs redistribute its §4 deliverables. Work from a fresh `phase/05-http-2-plan` worktree per the project's per-phase-worktree convention (the existing `.worktrees/phase-05-http-2-spec` is closed-history at this state transition; future plan/impl worktrees branch from the master tip after this commit's fast-forward per ADR-0003).
- **last-commit:** `612cdea` — the phase-05 SPEC.md commit on branch `phase/05-http-2-spec` (the `8d18320` ROADMAP-flip commit immediately precedes it on the same branch). Master is fast-forwarded to this tip in this lifecycle-transition commit per ADR-0003.
- **last-updated:** 2026-04-25

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
