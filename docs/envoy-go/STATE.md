# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `phase 25.3 IMPL done; §9 HTTP-filters family CLOSED at 2026-05-29 (this commit)` — **phase 25.3 IMPL DONE at 2026-05-29 (this commit)** atomic-landing all 15 tasks + the §9 HTTP-filters family closure ROLLUP (parent row 25 `in-progress → done` ATOMICALLY with sub-row 25.3 `in-progress → done`; 0 remaining §9 HTTP-filters rows). Six-gate phase-done evidence ALL GREEN per ADR-0052: Gate A `go build ./...` clean; Gate B `go vet ./...` + `golangci-lint run` clean; Gate C `go test -race -short ./...` green (stable across 3 whole-module runs); Gate D differential 41/41 GREEN (incl. NEW 0038 + 0039); Gate E 35 fuzzers (no 36th — D-25.3-6 FOLD); Gate F h2spec 53/53 GREEN + NEW proxy-wasm conformance 10/10 GREEN. Counts: stat surface 132 (+4: vm_reload triplet + env_vars_cap_exceeded); fixtures 41; fuzzers 35; conformance 10/10; ADR tail 0212 (3 NEW bodies LANDED ADR-0210/0211/0212 + ADR-0205 §Consequences AMEND + ADR-0186 §Consequences RATIFIES); next-free ADR-0213 (ADR-0209 + ADR-0213 escape-valve reserves STAND-UNCONSUMED per R8 STANDS-WEAK-default — all 3 R8 benchmarks << 1ms). 4 production bugs surfaced by the differential + fixed (BUG-1 frozen-registry per-route stats → NewCounterIfAbsent; BUG-2 reload triplet [consequence of BUG-3/4]; BUG-3 guest-trap Close cascade → StreamContext.trapped guard; BUG-4 ReloadDispatch-before-context-create reorder) — see REVIEW.md. **Historical (now-completed) 25.x sub-phase trail:** 25.1 IMPL done at 2026-05-25; 25.2 BRAINSTORM/SPEC/PLAN done at 2026-05-25; 25.2 IMPL done at 2026-05-26; 25.3 BRAINSTORM done at 2026-05-28; 25.3 SPEC done at 2026-05-28; 25.3 PLAN done at 2026-05-29. The §9 HTTP-filters family CLOSES at this 25.3 IMPL phase-done (parent row 25 + sub-row 25.3 BOTH flip `in-progress → done` ATOMICALLY per the 18/19/22/24 ROLLUP precedent; 0 remaining §9 HTTP-filters rows). Phases 22 + 23 + 24 + 24.1 + 24.2 + 25-parent + 25.1 + 25.2 (all stages) + 25.3 (all stages) DONE and CANONICAL — DO NOT reopen.
- **phase-directory:** `docs/envoy-go/phases/25.3-http-filter-wasm-perroute-and-conformance/` (README.md + BRAINSTORM.md + SPEC.md + PLAN.md + PROGRESS.md + **REVIEW.md authored at this 25.3 IMPL commit** — all 5+1 DONE). Predecessor sub-phase directories `docs/envoy-go/phases/25.2-...` + `25.1-...` (all 5+1 DONE). Parent directory `docs/envoy-go/phases/25-http-filter-wasm/` (BRAINSTORM.md + SPEC.md DONE at predecessor commits).
- **lifecycle-state:** `phase 25.3 IMPL done; §9 HTTP-filters family CLOSED` (SKILL_ROUTING state 4 → state 5 transition for sub-row 25.3 — 25.3 IMPL atomic-landing per `superpowers:subagent-driven-development`; six-gate evidence ALL GREEN; 3 NEW ADR bodies LANDED + 2 in-place amendments; STATE.md re-advanced + ROADMAP ROLLUP at this commit). **ROADMAP ROLLUP at this commit per ADR-0106 + the 18/19/22/24 precedent:** parent row 25 `in-progress → done` ATOMICALLY with sub-row 25.3 `in-progress → done`; sub-rows 25.1 + 25.2 STAY `done`. The §9 HTTP-filters family CLOSES to **0 remaining rows** (phase 25 was the EIGHTEENTH and FINAL §9 HTTP-filters row).
- **next-skill:** `superpowers:brainstorming` per BOOTSTRAP §5 state-0/state-1 routing (the §9 HTTP-filters family is now CLOSED → no in-progress phase remains; the next un-started §9-line family is brainstormed as its own phase when it enters `in-progress`). The next family per `BOOTSTRAP_PROMPT.md §9` ordering after the HTTP-filters family is the **Network filters family** (redis, mongo, kafka_broker, thrift, zookeeper, echo, direct_response, sni_cluster, rbac network) — to be added/refined as a ROADMAP row at the next BRAINSTORM. (Note: the broader §9 WASM-host family — cluster-specifier-wasm, access-logger-wasm, network-filter-wasm, WasmService — is a separate multi-phase sub-project per §9; it consumes the `internal/wasm/` primitive at multi-consumer scope and carries the ADR-0202 EXPLICIT API-REVISION ALLOWANCE for consumer #2 + the 6 deferred proxy-wasm conformance families.)
- **last-commit:** `TBD-25.3-IMPL-SQUASH` — `phase-25.3 Task 15: ADR-0210/0211/0212 bodies + BEHAVIOR_CONTRACT bundle + R8 benchmarks; STATE/ROADMAP ROLLUP — sub-row 25.3 in-progress->done + parent row 25 in-progress->done; §9 HTTP-filters family CLOSED`. The phase-25.3 IMPL squash-merge commit lands all 15 tasks + the 3 NEW ADR §Decision/§Consequences bodies (ADR-0210/0211/0212) + ADR-0205 §Consequences AMEND + ADR-0186 §Consequences RATIFIES + the BEHAVIOR_CONTRACT 128→132 bundle (2 NEW departures #8 reload-floor + #9 env_vars-cap; env_vars_cap allocate-only note; 6-deferred-conformance-families roster) + R8 benchmarks (all << 1ms; ADR-0209 + ADR-0213 reserves UNCONSUMED) + the ROADMAP ROLLUP (row 25.3 + row 25 BOTH `in-progress → done`). SHA to be filled at the stage-close follow-up commit (`TBD-25.3-IMPL-SQUASH` → actual) per the phase-22.x..25.2 stage-close SHA-fill precedent. Predecessor: `1aec3fc` — `Squash merge phase-25.3-http-filter-wasm-perroute-and-conformance-plan`.
- **last-updated:** 2026-05-29
- **next-free ADR:** `ADR-0213` at this 25.3 IMPL commit (3 NEW §Context drafts CONSUMED with §Decision+§Consequences bodies: ADR-0210 + ADR-0211 + ADR-0212; DECISIONS.md tail STAYS at ADR-0212). ADR-0209 (carried R8 escape-valve slot) + ADR-0213 (NEW reserve) STAND-UNCONSUMED — R8 STANDS-WEAK-default per all 3 benchmarks (`BenchmarkPerStreamModule_Instantiation` ~102 ns/op + `BenchmarkPerStreamPluginContextLookup` ~103 ns/op + `BenchmarkPerRouteResolve` ~0.2 ns/op; all << 1ms). next-free STAYS `ADR-0213`. **Historical anticipation block (superseded by the actual landing above):** ADR-0209 (carried from 25.2 as the R8 escape-valve slot) + ADR-0213 (NEW reserve for any 25.3-IMPL-time-unanticipated surface) form the STRENGTHENED-WEAK-HOLD-with-1-slot-buffer escape-valve pair per Q6 + 25.3 SPEC §10.5; anticipated BOTH UNCONSUMED at 25.3 IMPL phase-done. The D-25.3-2 BUNDLE disposition (Q1+Q2+Q3 under ONE ADR-0211) means only 3 (not 4) ADR numbers consumed at SPEC. **Anticipated 25.3 ADR consumption at 25.3 IMPL phase-done:** 3 NEW §Decision+§Consequences bodies (ADR-0210 EXPLICIT-NO-NEW-CANONICAL classification per AMEND-A3 ABSENCE-DEFINITIVE; ADR-0211 multi-plugin VM-sharing per Q1 + reload state machine per Q2 + env_vars activation per Q3 BUNDLED [SPEC may split per D-25.3-2]; ADR-0212 conformance harness seed per Q7 + pin SHA `proxy-wasm-cpp-host@da3ce05d`) + 1 in-place AMEND acknowledgment paragraph at ADR-0205 §Consequences (registry-keyed-by-`(vm_id, code_hash, runtime)` refinement per Q1; no new ADR number consumed) + ADR-0213 escape-valve reserve UNCONSUMED (STRENGTHENED-WEAK-HOLD-with-1-slot-buffer). **Anticipated next-free after 25.3 phase-done:** `ADR-0213` if all reserves UNCONSUMED; `ADR-0214` if ADR-0209 fires at 25.3 IMPL (pooled-Module vs shared-Module-with-mutex-serialization decision per parent §13-R8 signaling protocol per Q6 anticipated UNCONSUMED); both reserves consumed gives `ADR-0215` (unlikely; STRENGTHENED-WEAK-HOLD-with-1-slot-buffer per BRAINSTORM §7.3). **§9 HTTP-filters family closure at 25.3 IMPL phase-done:** ADR tail anticipated `ADR-0202..ADR-0212` (3 NEW ADR slots + 2 reserves UNCONSUMED matches parent §10.6 forward-projection `ADR-0205` reserve at 25.2 UNCONSUMED + `ADR-0213` reserve at 25.3 UNCONSUMED). **D-hypothesis disposition: STRENGTHENED-WEAK-HOLD-with-1-slot-buffer HOLDS** at this 25.3 BRAINSTORM commit — 3 anticipated NEW ADR §Context drafts will anchor cleanly at 25.3 SPEC commit; 0 escape-valve slot consumption anticipated from the R8 benchmark surface per Q6 + 10,000× margin at 25.2 phase-done.

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
