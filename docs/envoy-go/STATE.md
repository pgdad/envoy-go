# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `phase 39 (upstream-health-check) SPEC done` (2026-06-14) — the §11 D-HC1..D-HC8 empirical pins EXECUTED IN-SESSION live against `contrib-v1.37.2` (a Docker bridge-network probe: a STRICT_DNS cluster over a live nginx + a dead unbound host with `health_checks: [http{path:/}]` + `--mode validate` reject probes + a single-dead-host panic probe). **SPEC-firmed (AMEND-HC1..HC8):** (HC1) first-check-immediate — a host becomes healthy on its FIRST success / unhealthy on its FIRST failure (thresholds gate only SUBSEQUENT transitions); `/clusters` health_flags `healthy`/`/failed_active_hc`; the dead host excluded from `membership_healthy` + LB rotation. (HC2) the `HealthCheck` PGV rejects pinned byte-stable: `field: "health_checker", reason: is required` / `HttpHealthCheckValidationError.Path: value length must be at least 1 characters` / `HealthCheckValidationError.{Interval,Timeout,UnhealthyThreshold}: value is required`; the TCP checker PARSE-ACCEPTS at the reference → envoy-go 39.1 DEPARTURE-rejects tcp/grpc checkers (lifted at 39.2); gRPC needs cluster H2; `expected_statuses` default 200. (HC3) interval/timeout/thresholds PGV-required (NO defaults); `no_traffic_interval` default 60s; first check at startup. (HC4) the full reference stat set pinned [8 `health_check.*` + `membership_{change,healthy,degraded,excluded,total}` + `lb_healthy_panic` + `update_no_rebuild`]; **envoy-go MVP +7 → stat surface 1132** [`health_check.{attempt,success,failure,network_failure}` counters + `health_check.healthy` gauge + `membership_healthy` gauge + `lb_healthy_panic` counter; emitted only on clusters with `health_checks` → existing fixtures unaffected]; the deferred-feature stats {passive_failure/verify_cluster/degraded/membership_change/degraded/excluded/update_no_rebuild} are recorded departures. (HC5) `healthy_panic_threshold` default 50%, **strict `<`** (exactly 50% does NOT panic — verified `lb_healthy_panic: 0` at 1-of-2 healthy; a single-dead-host cluster 503'd + `lb_healthy_panic` 0→1); panic routes to ALL hosts → `0066` uses 2-live/1-dead (66%) to assert FILTERING not panic. (HC6) the unhealthy host is filtered from rotation (6/6 requests served by the live host, no 503); behavior-neutral when all healthy. (HC7) `0066-health-check-http` = 2 live + 1 dead, poll `/stats` to convergence then 100%-live assertion; single 39.1 leg (~14–20 tasks). (HC8) `go mod tidy -diff` EMPTY → ZERO new module; oneof fields http=8/tcp=9/grpc=11; `google.golang.org/grpc v1.70.0` ALREADY a direct dep → 39.2 gRPC uses `grpc_health_v1` directly. The **ADR-0242 + ADR-0243 §Context drafts ANCHORED** in SPEC §13 (DECISIONS tail STAYS ADR-0241; bodies at the 39.1 IMPL per ADR-0044). spec-document-reviewer APPROVED. Row 39 STAYS `in-progress` at this SPEC commit. ALL counts UNCHANGED (stat surface 1125 / fixtures 67 / fuzzers 42 / BackendKind 33 / DECISIONS tail ADR-0241, next-free ADR-0242). ALSO superseded — the prior BRAINSTORM-done note:

- **prior active-phase:** `phase 39 (upstream-health-check) BRAINSTORM done` (2026-06-14) — the FIRST Upstream-robustness-family row and the first NEW feature family opened since §9 (Network filters) closed at phase 33 (phases 34–38 were all Load-balancing-family rows). Charters `Cluster.health_checks` (`[]core.config.core.v3.HealthCheck`, proto field 8) — **active** upstream health checking (periodic outbound probes that mark hosts healthy/unhealthy) over an HTTP + TCP + gRPC checker roster — plus the project's FIRST per-host **health-state dimension** and the health-aware LB pick that makes all six LB constructs route only to healthy hosts (with a panic-threshold fallback). The family **keystone**: the host health-state primitive (i) unblocks the open Load-balancing family's 3 health-gated candidates {locality-weighted LB, priority load balancing, panic thresholds}, and (ii) is the substrate the sibling rows {outlier detection, circuit breakers, retries + hedging, connection pooling} compose with. **Q-decisions:** (Q0) NEW family = Upstream robustness (chosen over xDS/gRPC/HTTP3 — the foundational reliability layer that unblocks the health-gated LB candidates); (Q0b) first sub-project = active health checks (the keystone — establishes the health boundary; chosen over the cleaner-to-differential retries/circuit-breakers); (Q1) checker roster = HTTP + TCP + gRPC, split by codec — **39.1** = the keystone (host health-state dimension + the cluster-level checker runtime [the project's FIRST background runtime — `Manager.StartHealthChecks(ctx)` boot start + `Manager.Drain()` cancel/wait] + the HTTP probe codec + the health-aware LB across all six constructs) + the `0066-health-check-http` differential; **39.2** = the TCP + gRPC checkers reusing the 39.1 substrate; (Q2) health-aware LB = **Approach A: pick-time filter + panic fallback** (LB built once over all hosts; `Pick` skips unhealthy + re-picks; ring_hash/maglev walk-to-next-healthy preserving key stability; `healthy_panic_threshold` default 50% fallback-to-all), with the health view **build-time-injected** (host health is CLUSTER state, NOT a per-request `Pick` widening — the deliberate CONTRAST with the ADR-0235 hash-key / ADR-0239 subset-match ctx-carry pick-inputs; the `Pick` signature + exported `Cluster` surface stay BYTE-STABLE; behavior-neutral when all healthy → every existing fixture stays byte-identical). A NEW `health_checks` parse-reject surface (envoy-go's own byte-stable rejects; D-HC2). Differential `0066-health-check-http` (a cluster with a LIVE + a DEAD unbound host; POLL `/stats` until the dead host is unhealthy on BOTH sides [no fixed sleep], THEN assert 100% of traffic on the live host + cross-side `health_check.*`/`membership_*` parity; 3 `-count=1` deliberate breaks; ≥20-run flake-free). **2 ADRs anticipated:** ADR-0242 (the host health-state dimension + the active-HC runtime) + ADR-0243 (the health-aware LB pick — Approach A); the phase-36/38 two-ADR shape; §Context at SPEC, bodies at IMPL per ADR-0044. Anticipated moves at the 39.1 IMPL: fixtures 67 → 68 (`0066`), DECISIONS tail ADR-0241 → ADR-0243 (next-free ADR-0244), stat surface 1125 → ~1131 (~+6 `health_check.*` + `membership_healthy`; D-HC4 pins), fuzzers 42 → 42, BackendKind tail 33 → 33; ZERO new packages + ZERO new go.mod modules at 39.1. **ROADMAP row 39 registers `in-progress`** (a flat family row WITH a pre-authorized 39.1/39.2 by-codec split; it flips to `done` per-leg at each IMPL six-gate — NO parent rollup per ADR-0106). ALL counts UNCHANGED at this BRAINSTORM commit (stat surface 1125 / fixtures 67 / fuzzers 42 / BackendKind 33 / DECISIONS tail ADR-0241, next-free ADR-0242). ALSO superseded — the prior IMPL-done note:

- **prior active-phase:** `phase 38.2 (weighted-clusters) IMPL done` (2026-06-14) — the 10-task TDD PLAN executed end-to-end subagent-driven, the six-gate GREEN. As built: **ADR-0241** — the **weighted-cluster routing primitive** (`RouteAction.weighted_clusters`) + the `ClusterWeight.metadata_match` merge composition. The project's **FIRST per-request cluster-SELECTION primitive** (the SECOND `RouteAction.ClusterSpecifier` arm; every prior route resolves a SINGLE cluster at config-build). A route fans across N weighted clusters, ONE chosen per request by integer weight — weighted-RANDOM via a per-action `weightedSelector` (cumulative weights + a `newPCGRNG()` draw in `[0, Σweight)` + a cumulative walk — the FIRST use of the `randomLB` per-request RNG model OUTSIDE the cluster LB). `total_weight` is DEPRECATED upstream and IGNORED (the client uses the SUM). Each `ClusterWeight.metadata_match` MERGES over the route-level `RouteAction.metadata_match` (entry precedence; merged ONCE at config-build per entry; rides the EXISTING ADR-0239 `cluster.WithSubsetMatch` ctx seam UNCHANGED — NO `loadBalancer.Pick` change, NO exported-cluster change — the 38.1 compose). New types: `weightedSelector` + `H1/H2WeightedClusterAction` in `internal/filter/http/router/router_weighted.go` + `weightedClusterRouteAction` bridge in `internal/filter/hcm/actions.go` + `buildWeightedRouterAction`/`mergeRouteSubsetMatch`/`buildWeightedBridge` in `internal/filter/hcm/config.go`. SIX producer parse-reject arms (empty clusters / name-required / cluster_header-unsupported / weight-required / sum-0 / dangling — all unit-level; NO boot-reject fixture). Differential `0065-weighted-clusters` (per-side weight-distribution band over 3 clusters at ~4.5σ margins + a metadata_match-composition arm + cross-side conservation; 3 `-count=1` deliberate breaks [swap-weights / drop-cluster / drop-compose]; 20/20 flake-free). ZERO new stat surface (surface 1125 → 1125 — the FIRST LB-family leg to add NO stat surface). The ADR-0241 §Decision/§Consequences bodies landed IN-PLACE per ADR-0044. As-built counts: fixtures **66 → 67** (`0065`); DECISIONS tail **ADR-0240 → ADR-0241** (next-free ADR-0242); stat surface 1125 → **1125** (ZERO new); fuzzers 42 (UNCHANGED); BackendKind tail 33 (UNCHANGED). ALSO superseded — the prior PLAN-done note:

- **prior active-phase:** `phase 38.2 (weighted-clusters) PLAN done` (2026-06-14) — the PLAN (`superpowers:writing-plans`; `docs/envoy-go/phases/38.2-weighted-clusters/PLAN.md`) decomposes SPEC §10 into **10 bite-sized TDD tasks**. The FINAL ADR-0045 split-gate re-check → **NO FURTHER SPLIT** [~290 prod LoC / 10 tasks]. The §12 D-WC-IMPL-1..D-WC-IMPL-5 questions RESOLVED [file placement = `router_weighted.go` + hcm; `cluster_header` REJECTS / header-mutation SILENT-IGNORE; `0065` constants = weights {50,30,20}/N=500/~4.5σ bands; composition FOLDED into the weighted route; property test FOLDED into Task 2]. plan-document-reviewer APPROVED (4 advisory fixes applied). ALL counts UNCHANGED at this PLAN (stat 1125 / fixtures 66 / fuzzers 42 / BackendKind 33 / DECISIONS tail ADR-0240, next-free ADR-0241). ALSO superseded — the prior SPEC-done note:

- **prior active-phase:** `phase 38.2 (weighted-clusters) SPEC done` (2026-06-14) — the SPEC for the **pre-authorized SECOND leg of the phase-38 by-plane split** (the 36.1/36.2 precedent; `docs/envoy-go/phases/38.2-weighted-clusters/SPEC.md`). The §11 D-WC1..D-WC6 empirical pins EXECUTED live; ADR-0241 §Context draft anchored; spec-document-reviewer APPROVED. ALL counts UNCHANGED at this SPEC. ALSO superseded — the prior IMPL-done note:

- **prior active-phase:** `phase 38.1 (load-balancer-subset) IMPL done` (2026-06-14) — the 14-task TDD PLAN executed end-to-end subagent-driven, the six-gate GREEN. As built: the **subset LB wrapper** `Cluster.lb_subset_config` (`envoy.config.cluster.v3`, proto field 22) — the project's FIFTH LB construct and the family's FIRST WRAPPER (a wrap-after-switch, NOT a sixth `case`). **ADR-0239** (the SECOND pick-input seam extension — `loadBalancer.Pick(hashKey, hasHash)` → `Pick(hashKey, hasHash, match, hasMatch)`, the five leaf policies absorbing it behaviour-neutrally, only `subsetLB` consuming it; the byte-stable OPTION-C ctx-carried `cluster.WithSubsetMatch`; + the ROUTE-STATIC `RouteAction.metadata_match["envoy.lb"]` producer — `parseRouteSubsetMatch` in hcm/config.go → `clusterRouteAction.subsetMatch`, threaded VERBATIM per request, NO per-request fold) + **ADR-0240** (the `subsetLB` policy in `internal/cluster/subset.go` — enumerate subsets per selector × distinct `envoy.lb` value-tuple, a `buildLeafLB` child per subset [all five children exercisable], EXACT key-set resolution + the 3-way cluster-level fallback {NO_FALLBACK/ANY_ENDPOINT/DEFAULT_SUBSET}; the NEW `Endpoint.Metadata` dimension — the FIRST per-endpoint dimension beyond `Host:Port`, `Addr()` unchanged). The **4 `lb_subsets_*` stats** (`active` gauge + `created`/`selected`/`fallback` counters; `active`/`created` Set/Add at build to the ×1 distinct-subset count — NOT the reference's 33× artifact; `selected`/`fallback` the FIRST stats a `loadBalancer` Inc's from its own `Pick` path, request-counted + cross-side-equal). Differential `0064-lb-subset` (the static-config NAT-transparent TRUE cross-side subset-affinity SET-membership arm + an ANY_ENDPOINT fallback arm + cross-side byte-equiv + a cross-side `StatsAsserter`; 3 `-count=1` deliberate breaks; 20/20 flake-free). ZERO new cluster reject arm (CLUSTER_PROVIDED pre-covered; the only new reject is the producer-side `router-metadata-match-nonscalar`). The ADR-0239/0240 §Decision/§Consequences bodies landed IN-PLACE per ADR-0044. ALSO superseded — the prior PLAN-done active-phase note:

- **prior active-phase:** `phase 38 (load-balancer-subset) PLAN done` (2026-06-13) — the phase-38.1 PLAN is COMPLETE (`superpowers:writing-plans`; `docs/envoy-go/phases/38-load-balancer-subset/PLAN.md` — SPEC §10 decomposed into **14 bite-sized TDD tasks** [Task 1 baselines/anchors gate + PROGRESS.md; 2 `SubsetMatch`/`SubsetValue`/`valueEqual`/`key()` + `ScalarsFromStruct`; 3 `Endpoint.Metadata` + `extractEndpoints` capture; 4 the seam-input widening — interface + 5 leaves + `WithSubsetMatch`/`subsetMatchFrom` + the 3 funnels; 5 the `buildLeafLB` extraction; 6 `subsetLB` enumeration + build; 7 `subsetLB.Pick`; 8 manager `parseLbSubsetConfig` + wrap-after-switch; 9 the 4 `lb_subsets_*` registrations + counter injection; 10 the HTTP `metadata_match` producer; 11 the `0064-lb-subset` fixture; 12 deliberate-break liveness + flake; 13 full differential re-verify gate; 14 the completion bundle]; the FINAL ADR-0045 split-gate re-check → **NO FURTHER SPLIT within 38.1** [~300–450 prod LoC / 14 tasks — both axes under the `> ~25 tasks OR > ~1500 LoC` gate]; the §12 D-S38-1..D-S38-6 questions RESOLVED [the property test FOLDED into Tasks 6/7; the SPEC's verify/completion split into Tasks 13/14 — the phase-35/36/37 precedent]; plan-document-reviewer APPROVED first pass [3 advisory snippet-accuracy fixes applied — `Counter.Add(uint64)`/`Counter.Load()` not `.Value()`/the real `doH1ClusterAction`+`doH2ClusterAction` dispatch names + the `routerAction`/`routerActionH2` structs + the 3 `hcm/actions.go` call sites :213/:236/:249]). The phase-38 SPEC (prior commit `f52a8ea`) executed the §11 D-SS1..D-SS7 empirical pins live + anchored the ADR-0239/0240 §Context drafts + CONSUMED the FINAL 38.1/38.2 split. **SPEC-firmed deltas vs the BRAINSTORM:** the parse-reject surface is **EMPTY** — the only reference reject (`lb_subset_config` + `lb_policy: CLUSTER_PROVIDED` → `cluster: LB policy CLUSTER_PROVIDED cannot be combined with lb_subset_config`) is ALREADY covered by envoy-go's pre-existing unsupported-policy gate, so ZERO new reject arm and the `Cluster.LbPolicy` supported-list + `TestManager_Error_UnsupportedLBPolicy` are UNTOUCHED (PGV imposes nothing beyond 3 enum-range checks; empty keys / empty selectors / duplicate selectors / fallback-default mismatches ALL accept); `envoy.lb` value-equality is double-valued for numbers (int 1 ≡ 1.0 — no canonicalization) with an exact key-set match; stat surface anticipated **1121 → 1125** (+4 `lb_subsets_*`: `active` gauge + `created`/`selected`/`fallback` counters — the reference's `active`/`created` carry a **33× magnitude artifact** envoy-go does NOT mirror, pinning the ×1 distinct-subset count; only `selected`/`fallback` [request-counted] cross-side-equal in `0064`); the `lb_subsets_selected`/`fallback` counters are the FIRST stats a `loadBalancer` Inc's from its own `Pick` path (injected at register; ADR-0024 noted, not amended). **ROADMAP row 38 STAYS `in-progress`** (it flips at the 38.1 IMPL six-gate — a flat family row, NO parent rollup per ADR-0106). Charters the project's **FIFTH LB construct** and the family's **FIRST wrapper**: `Cluster.lb_subset_config` (`envoy.config.cluster.v3`) — **NOT a `Cluster.LbPolicy` enum value** (unlike the five incumbents roundRobin/leastRequest/randomLB/ringHashLB/maglevLB) but a wrapper triggered by the presence of `lb_subset_config`, that selects a subset of endpoints matching the route's `metadata_match` and **delegates to the cluster's `lb_policy` as the child policy run within that subset**. Picked from the 4-candidate roster (Q0) as **the only candidate decoupled from the absent health-checking boundary** (the other three — locality-weighted / priority / panic — are health-gated and belong after the Upstream-robustness family). As chartered: a `subsetLB` wrapper (`internal/cluster/subset.go` — enumerate subsets per selector × distinct endpoint-metadata value-tuples; a child `loadBalancer` per subset built by the existing policy factory — **ALL FIVE child policies supported** [Q1=A], the hashKey passing through to a hash-policy child; resolve the route match → subset → delegate `child.Pick`; the cluster-level fallback {NO_FALLBACK/ANY_ENDPOINT/DEFAULT_SUBSET} + `default_subset`) over a NEW `Endpoint.Metadata` dimension (the `envoy.lb` namespace captured at `extractEndpoints`, `manager.go:533` — the FIRST per-endpoint dimension beyond `Host:Port`) + a SECOND LB pick-seam input (the resolved subset match widening `Pick(hashKey, hasHash)` → `Pick(hashKey, hasHash, match, hasMatch)` + `WithSubsetMatch`/`subsetMatchFrom`, the ADR-0235 hash-key STYLE TEMPLATE) + a NEW HTTP producer plane (`RouteAction.metadata_match["envoy.lb"]` attached to `clusterRouteAction` — `actions.go:201`). The manager wraps-after-switch (subset wraps the `lb_policy` child — NOT a new `case`). **NO lb_policy reject-text change** (subset is not an enum value — the phase-34/35/36/37 doubly-hit reject-text-retarget lineage BREAKS; the reject surface shifts to `lb_subset_config` validation, D-SS1). **Pre-authorized 38.1/38.2 by-plane split (Q2/Q2b — the phase-36 valve):** 38.1 = subset LB on the HTTP `RouteAction.metadata_match` plane (this leg); 38.2 = `RouteAction.weighted_clusters` routing + `WeightedCluster.ClusterWeight.metadata_match` (a NEW per-request weighted-cluster-selection primitive — the router resolves a SINGLE cluster per route today; the brainstorm surfaced this hidden prerequisite). Differential `0064-lb-subset` (HTTP routes each carrying a `metadata_match` pinned to a subset over `envoy.lb`-tagged endpoints): subset affinity (every served backend ∈ the expected subset, BOTH sides — the SET-membership property; the `metadata_match` is STATIC config → NAT-transparent → TRUE cross-side) + a fallback arm + cross-side byte-equiv + a cross-side `StatsAsserter` (the `lb_subsets_*` stats). 2 ADRs anticipated: **ADR-0239** (the subset-match seam-input extension + the `RouteAction.metadata_match` producer — the seam-BUILD half, parallel to ADR-0235) + **ADR-0240** (the subset LB policy + the `Endpoint.Metadata` dimension — the policy half, parallel to ADR-0236/0238); the phase-36 two-ADR shape (contrast phases 35/37's single-ADR reuse); §Context at SPEC, bodies at IMPL per ADR-0044. Anticipated moves at the 38.1 IMPL: stat surface **1121 → ~1125** (~+4 `lb_subsets_*` stats — D-SS4 pins), fixtures **65 → 66** (`0064`; NO boot-reject dir anticipated — the parse rejects land unit-level), fuzzers UNCHANGED **42** (no wire decode — a subset-resolution property test is unit-level), BackendKind tail UNCHANGED **33** (`0064` reuses the `0063` HTTP backends), ZERO new packages + ZERO new go.mod deps. **The Load-balancing family stays OPEN** — **3** candidates remain after 38 {locality-weighted LB, priority load balancing, panic thresholds} (all health-gated). ALL counts UNCHANGED at this BRAINSTORM commit.
- **phase-directory:** `docs/envoy-go/phases/39-upstream-health-check/` (**BRAINSTORM.md + SPEC.md + README.md** — the durable phase-39 record: BRAINSTORM [the Q0/Q0b/Q1/Q2 decisions; the §2.1 proto verification; the §1.7/§2.3/§2.4 architecture; the §1.4/§2.2 split; the §2.6 reject surface; the §2.5/§6 differential; the §7 ADRs; the §10 D-HC1..D-HC8 pins] + SPEC [the §1.1 AMEND-HC1..HC8 block; the §3 architecture; the §6 PARSE-REJECT roster; the §7 stat surface +7; the §8 `0066` poll-to-convergence differential; the §11 D-HC1..D-HC8 pins EXECUTED LIVE; the §13 ADR-0242/0243 §Context drafts] + the phase-overview README [status 1 → 2]; the 39.1 PLAN.md + PROGRESS.md follow). **Superseded (preserved):** `docs/envoy-go/phases/38.2-weighted-clusters/` (**SPEC.md + PLAN.md + PROGRESS.md + README.md** — the durable phase-38.2 IMPL record: SPEC [the §3 architecture; the §6 SIX-arm reject roster; the §7 zero-stat finding; the §8 `0065` per-side-band differential design; the §11 D-WC1..D-WC6 empirical pins; the §13 ADR-0241 §Context draft] + PLAN [SPEC §10 → 10 bite-sized TDD tasks; the ADR-0045 NO-FURTHER-SPLIT re-check; the D-WC-IMPL-1..5 resolutions; plan-document-reviewer APPROVED] + PROGRESS [the 10-task IMPL record + the six-gate evidence] + the phase-overview README [status 3 → 4]). **The phase-38 parent dir** `docs/envoy-go/phases/38-load-balancer-subset/` (**BRAINSTORM.md + README.md** — the durable phase-38 BRAINSTORM charter [the Q0/Q1/Q2 decisions; the v1.32.4 `Cluster.LbSubsetConfig` + `LbEndpoint.metadata` [field 3] + `RouteAction.metadata_match` [field 4] + `core.Metadata.filter_metadata["envoy.lb"]` verification §2.1; the wrapper-not-an-enum-value architecture §1.7/§2.2/§3.1; the 38.1/38.2 by-plane split §1.4/§2.3; the NO-reject-text-change twist §2.6; the static-config NAT-transparent cross-side subset-affinity differential design §2.5/§6; the 2 anticipated ADRs §7; the §10 D-SS1..D-SS7 empirical pins the SPEC executes] + the phase-overview README [status 0 → 1]; the SPEC.md follows at the SPEC session, then the 38.1 PLAN.md + PROGRESS.md, per the per-sub-phase-dir precedent [36.1/36.2]). **Superseded (preserved):** `docs/envoy-go/phases/37-load-balancer-maglev/` (**BRAINSTORM.md + SPEC.md + PLAN.md + PROGRESS.md + README.md** — the durable phase-37 IMPL record [the maglev policy; ADR-0238]). The phase-36 dirs `docs/envoy-go/phases/36-load-balancer-ring-hash/` + `36.2-load-balancer-ring-hash-http/` (the durable phase-36 parent + 36.2 sub-phase record). The phase-35 directory `docs/envoy-go/phases/35-load-balancer-random/` + the phase-34 directory `docs/envoy-go/phases/34-load-balancer-least-request/` (the durable phase-35/34 records). The phase-31/32/33 network-filter directories + the phase-29/30 directories stay the durable prior-phase record.
- **lifecycle-state:** `phase 39 SPEC done — the §11 D-HC1..D-HC8 pins EXECUTED live against contrib-v1.37.2 (AMEND-HC1..HC8); the ADR-0242/0243 §Context drafts anchored; the 39.1/39.2 split CONFIRMED. Next: the phase-39 PLAN (decompose SPEC §10 into ~14–20 bite-sized TDD tasks; the FINAL ADR-0045 split-gate re-check).`. The phase-39 SPEC is COMPLETE: the durable record is `docs/envoy-go/phases/39-upstream-health-check/SPEC.md` + BRAINSTORM.md + README.md. The phase-38.2 IMPL durable record stays `docs/envoy-go/phases/38.2-weighted-clusters/`.
- **next-skill:** the phase-39 PLAN (`superpowers:writing-plans` — decompose SPEC §10 [the ~14–20-task spine] into bite-sized TDD tasks; resolve the §12 D-S39-1..D-S39-6 PLAN/IMPL questions; the FINAL ADR-0045 split-gate re-check [single 39.1 leg vs a state+runtime / health-aware-LB sub-split]; plan-document-reviewer gate).
- **last-commit:** the phase-39 SPEC-DONE bundle — `docs/envoy-go/phases/39-upstream-health-check/SPEC.md` (the §11 D-HC1..D-HC8 live pins + AMEND-HC1..HC8 + the §13 ADR-0242/0243 §Context drafts) + README.md (status 1 → 2) + STATE (this block) + ROADMAP (row 39 SPEC-DONE note). ALL counts UNCHANGED: stat surface **1125**; fixtures **67**; fuzzers **42**; BackendKind tail **33**; DECISIONS tail **ADR-0241** (next-free ADR-0242).
- **last-updated:** 2026-06-14 (phase-39 SPEC done)
- **next-free ADR:** `ADR-0242` (DECISIONS.md tail STAYS **ADR-0241**; the phase-39 BRAINSTORM anticipates ADR-0242 [the host health-state dimension + the active-HC runtime] + ADR-0243 [the health-aware LB pick], §Context drafts at the SPEC, bodies at the 39.1 IMPL per ADR-0044 — next-free after phase 39 ≈ ADR-0244).

---

## Project counts at this commit

- **active differential fixtures:** **67** (tail `0065-weighted-clusters`). Canonical recipe `ls -d test/fixtures/[0-9]* | wc -l` = 67. As built at the 38.2 IMPL (66 → 67 — `0065-weighted-clusters`, the per-side weight-distribution band + metadata_match-composition arm + cross-side conservation differential; NO boot-reject dir — all SIX producer rejects are unit-level; the distribution is probabilistic → no boot-reject fixture).
- **fuzzers:** **42** (canonical recipe `grep -rh "^func Fuzz" $(find ./internal -name fuzz_test.go) | wc -l`). UNCHANGED at the 38.2 IMPL (+0 — the SIXTH no-fuzzer LB phase; weighted selection decodes no untrusted wire bytes; a selection-distribution property test is unit-level, FOLDED into `router_weighted_test.go`).
- **stat surface:** **1125** (BEHAVIOR_CONTRACT doc count). UNCHANGED at the 38.2 IMPL (+0 — ZERO new stat names; the FIRST LB-family leg to add NO stat surface; weighted selection is observable via the pre-existing per-cluster `upstream_rq_*`). As built at the 38.1 IMPL (1121 → 1125 — +4 `lb_subsets_*` stats). See the BEHAVIOR_CONTRACT "Phase 38.1 extension — 1121 → 1125" + "Phase 38.2 — stat surface STAYS 1125" blocks.
- **BackendKind tail:** **33** (`TCPThriftResponder` — seq_id-agnostic canned framed-binary REPLY). UNCHANGED at the 38.2 IMPL (+0 — `0065` reuses the `0063`/`0064` `HTTPEcho`).
- **DECISIONS.md tail:** **ADR-0241** (next-free **ADR-0242**) — **ADR-0241** (the weighted-cluster routing primitive + the `ClusterWeight.metadata_match` merge composition) landed at the phase-38.2 IMPL (§Context promoted from the SPEC §13 draft, §Decision/§Consequences bodies IN-PLACE per ADR-0044).
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227, superseding ADR-0008).
- **conformance:** h2spec 53/53; proxy-wasm all 10 families PASS — asserted-unaffected by change-scope construction at the 38.2 IMPL (38.2 touches ONLY `internal/filter/http/router/router_weighted.go` [new] + `internal/filter/hcm/{actions.go, config.go}` [the `buildRouterAction` weighted arm] + the `0065` fixture + docs — NO HTTP/2 framing or proxy-wasm path; the wire path is byte-identical when no `weighted_clusters` route is configured; the full 67-dir differential is the real guard).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
