# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `22-http-filter-lua` (parent row in-progress; sub-phase `22.1-http-filter-lua-vm-and-headers-bridge` **done at this commit**; sub-phases `22.2-http-filter-lua-full-bridge` + `22.3-http-filter-lua-multi-script-and-per-route` planned per BRAINSTORM Q11 long-prefix slugs + Q12 pre-create-all directories). Phase `22-http-filter-lua` **parent BRAINSTORM done at 2026-05-18** via a 12-question dialogue (Q1 envelope D + Q2 3-way pre-split + Q3 gopher-lua v1.1.2 + Q4 EXTRACT-NOW + Q5 4-arm DataSource + Q6 pragmatic-middle bridge + Q7 NEW 9th canonical + Q8 3-counter stat surface + Q9 full cross-side byte-exact + Q10 WEAK HOLD escape-valve + Q11 long-prefix slug + Q12 pre-create all dirs). **4 ROADMAP rows** registered at BRAINSTORM per ADR-0106 (parent `22` + sub-rows `22.1` + `22.2` + `22.3`); parent SPEC anchored at commit `41ccee7` (16 sections; 12 AMENDs catalog; ADR-0188 + ADR-0189 §Context drafts + ADR-0125 §(xiv) AMENDMENT-anticipation paragraph); 22.1 SPEC anchored at `a7021aa` (15 sections + 2 appendices; D5 + D7 CLOSED in-session; D1 + D3 carry-forward); 22.1 PLAN anchored at `02d745a` (16-task IMPL execution plan; 10 PLAN-time decisions D3 + D-P1..D-P10 locked); 22.1 IMPL anchored at **THIS commit** (16 tasks landed; 2 NEW ADR §Decision + §Consequences body landings at Task 16 — NO ADR-0190 per D-P10 R6 STANDS WEAK-default).
- **phase-directory:** `docs/envoy-go/phases/22-http-filter-lua/` (parent — BRAINSTORM.md + SPEC.md authored at parent SPEC commit; parent phase row stays in-progress until 22.3 phase-done per parent SPEC §1 closure pattern). Sub-phase directories: `docs/envoy-go/phases/22.1-http-filter-lua-vm-and-headers-bridge/` (SPEC.md + PLAN.md + PROGRESS.md (16 task entries + Pre-Task 0 preamble) + REVIEW.md authored at THIS commit) + `22.2-http-filter-lua-full-bridge/README.md` (placeholder; awaits 22.2 BRAINSTORM) + `22.3-http-filter-lua-multi-script-and-per-route/README.md` (placeholder; awaits 22.3 BRAINSTORM).
- **lifecycle-state:** `phase 22.1 IMPL done; awaiting 22.2 SPEC`. ROADMAP row `22.1` flips `in-progress → done` at this commit with the per-cell IMPL-done annotation per ADR-0106 (16-task IMPL landing + 6-gate outputs + FIFTEENTH §9 family-row milestone + FIRST §9 row to introduce third-party Lua VM dependency (gopher-lua v1.1.2) + NEW `internal/lua/` framework primitive milestone + 22.1 SPEC §15.2 24-item acceptance + D1/D3/D5/D7 disposition record + R6 STANDS WEAK-default); parent row `22` STAYS `in-progress` until 22.3 phase-done; sub-rows `22.2` + `22.3` STAY `planned`. DECISIONS.md tail **ADVANCES to ADR-0189 with §Decision + §Consequences body landings** at this commit per ADR-0044 in-place edit discipline (§Context blocks already anchored at parent SPEC commit `41ccee7`; ADR-0188 + ADR-0189 §Decision + §Consequences bodies REPLACE the SPEC-commit placeholders at this commit). ADR-0125 §(xiv) AMENDMENT-anticipation paragraph UNCHANGED at this commit (anchored at parent SPEC commit; AMENDMENT body lands at 22.3 IMPL final Task). Next-free **ADR-0190** UNCHANGED (R6 STANDS WEAK-default; ADR-0190 NOT CONSUMED; carries forward to 22.2 BRAINSTORM as the 22.2 IMPL escape-valve slot per D-P10 R6 codification). **Phase 22.1 IMPL authored at THIS commit**: 16 Tasks landed across 5 tiers (Tier A scaffold Tasks 1-5; Tier B bridge Tasks 6-9; Tier C stats+tests Tasks 10-12; Tier D fixture Tasks 13-15; Tier E atomic landing Task 16) per 22.1 PLAN §Tier-A..Tier-E. **All 6 phase-done gates GREEN**: Gate A build clean; Gate B vet + lint clean (after fixing pre-existing gofmt drift at `internal/cluster/cluster.go:50` from commit `49cc7cd` as out-of-scope housekeeping — 1-line fix); Gate C race clean on unit packages (`internal/...` + `test/conformance/...` + `test/helpers/...`); Gate D differential 28/28 fixture directories GREEN (full cross-side suite ran in 72.88s; includes new `0026-http-lua-headers-bridge` GREEN with 7 scenarios: 6 wire-interactive (a)-(f) cross-side `CompareBytes` byte-exact + scenario (g) substring-match `"script load error"` via NEW `BootRejectFixture` per §13-R1); Gate E fuzz `FuzzLuaConfigParse` 30s baseline clean (no panics; ~120k execs/sec; 28-fuzzer project-wide count confirmed via `grep -h '^func Fuzz' | sort -u | wc -l = 28`); Gate F h2spec 53/53 PASS at ADR-0051 v1.32.4 pin (envoy-go-side conformance gate; 0.55s). **22.1 SPEC §15.2 24-item acceptance checklist all GREEN** (items 1-18 from parent §16 verbatim; items 19-24 sub-phase-specific extensions). **D1 REFUTED at IMPL Task 2** (upstream Envoy v1.37.2 source scrape against `lua_filter.cc:1455-1485` FilterConfig constructor + `:140-181` PerLuaCodeSetup constructor + `config.cc:14-26` createFilterFactoryFromProtoTyped — both arms 5 + 17 silent no-op; reserved wording constants pinned in source with `//nolint:unused` for future policy-bump migration per phase-21 `parseRejectFixedValueDeferred` reserved-constant precedent). **D3 CLOSED at PLAN session** (scenario (e) stat-counter `executions` delta IS the "Lua ran" assertion per parent §11.7.7 option (a); cumulative-after-Drive `AssertStats` cross-side compare verified at Task 14 driver impl). **D5 + D7 CLOSED at 22.1 SPEC §11** (D5: 28th-fuzzer count CONFIRMED; D7: envoy-go headers-map is unordered `net/http.Header` + bridge `__pairs` alphabetical-snapshot RATIFIED). **D-P10 R6 STANDS WEAK-default** (`ns/op = 69865` ~70µs/stream << 1ms threshold per parent §13-R6 + 22.1 PLAN D-P10; ADR-0190 NOT consumed; carries forward to 22.2 BRAINSTORM). **2 NEW PARSE-REJECT arms surfaced by Task 11 fuzzer**: arm 19 (`Lua.stat_prefix` invalid chars per `stats.IsValidName` regex pre-check — mirrors `hcm/config.go:209` + `cluster/manager.go:205` precedent) + arm 9-extension (`Filename` DataSource 16 MiB cap via `io.LimitReader` — defense vs `/dev/full`-class infinite-read OOM-kill); both fixed inline at Task 11 per ADR-0018 fuzzer-discipline + regression seeds preserved at `internal/filter/http/lua/testdata/fuzz/FuzzLuaConfigParse/`. **BEHAVIOR_CONTRACT.md 7-edit bundle landed atomically** at Task 16 per ADR-0052 + parent §14 + 22.1 SPEC §14 (NEW `### envoy.filters.http.lua` subsection ~330 LoC; stat-table 99 → 102 extension + extension summary paragraph; 3 envoy-go-strict departure records integrated; NEW `### Phase 22.1 forward-pointer notes` subsection; per-route-canonical cross-reference caption update + paragraph). **REVIEW.md authored** at `docs/envoy-go/phases/22.1-http-filter-lua-vm-and-headers-bridge/REVIEW.md` per `superpowers:requesting-code-review` per phase-21 IMPL precedent. **FIFTEENTH §9 family-row landed.** **FIRST §9 row to introduce third-party Lua VM dependency** (gopher-lua v1.1.2 pure-Go Lua 5.1 MIT-licensed NO CGO). **ENDS the phase-21 ZERO-NEW-framework-primitive streak** by introducing `internal/lua/` at first consumer per BRAINSTORM Q4 EXTRACT-NOW + ADR-0188 EXPLICIT API-REVISION ALLOWANCE for consumer #2. **17 HTTP filters wired post-22.1** (was 16). **102 stat names post-22.1** (was 99). **28 fuzzers post-22.1** (was 27). **28 differential fixture directories post-22.1** (was 27). Phase 22.1 IMPL is **READY FOR SQUASH-MERGE TO MASTER** per project memory `feedback_git_worktrees.md` + ADR-0005 §Decision 4. ADR roster ADVANCES to ADR-0189 §Decision + §Consequences body landings; next-free ADR-0190 UNCHANGED.
- **next-skill:** `superpowers:brainstorming` (scoped to 22.2 BRAINSTORM — full bridge surface delta per parent §10 forward-pointers + per BRAINSTORM Q2 PRE-SPLIT + ADR-0106 per-sub-phase BRAINSTORM convention). Cold-start scope for the next session: read STATE.md (this file) + `docs/envoy-go/ROADMAP.md` (row 22.1 with IMPL-done annotation; row 22.2 planned; row 22 in-progress) + `docs/envoy-go/phases/22.1-http-filter-lua-vm-and-headers-bridge/REVIEW.md` (this IMPL's REVIEW + 24-item acceptance) + `docs/envoy-go/phases/22.1-http-filter-lua-vm-and-headers-bridge/PROGRESS.md` (16 task entries — most relevant: Task 11 fuzzer 2 arms + Task 12 R6 STANDS + Task 14+15 fixture-0026 GREEN) + `docs/envoy-go/phases/22-http-filter-lua/SPEC.md` (parent SPEC; §10 forward-pointers describe 22.2 anticipated scope) + `docs/envoy-go/phases/22-http-filter-lua/BRAINSTORM.md` (Q6 pragmatic-middle 22.1 vs full 22.2 bridge dialogue) + DECISIONS.md tail (ADR-0188 + ADR-0189 full bodies; ADR-0190 still next-free; ADR-0125 §(xiv) anticipation) + BEHAVIOR_CONTRACT.md `### envoy.filters.http.lua` + `### Phase 22.1 forward-pointer notes` (22.2 BRAINSTORM scope hand-off bullets). The 22.2 BRAINSTORM session creates a fresh worktree per project memory `feedback_git_worktrees.md`: `git worktree add /home/esa/git/envoy-go/.worktrees/phase-22.2-http-filter-lua-full-bridge-brainstorm -b phase-22.2-http-filter-lua-full-bridge-brainstorm <22.1-IMPL-tip-SHA>`.
- **last-commit:** `<TBD — filled in by SHA-fill follow-up after squash-merge>`. Phase 22.1 IMPL squash-merge of branch `phase-22.1-http-filter-lua-vm-and-headers-bridge-impl` from worktree `.worktrees/phase-22.1-http-filter-lua-vm-and-headers-bridge-impl/` lands as `<TBD>` on master per the phase-09..21 + phase-18.1 + phase-19.1 squash-merge convention. Predecessor master tip was `6d3b487` — `phase 22.1 PLAN follow-up: STATE.md SHA-fill (TBD → 02d745a post-squash)`. Pre-predecessor was `02d745a` — `Squash merge phase-22.1-http-filter-lua-vm-and-headers-bridge-plan`. A SHA-fill follow-up commit backfills the placeholder per the phase-09..21 convention: `phase 22.1 IMPL follow-up: STATE.md SHA-fill (TBD → <squash-SHA> post-squash)`.
- **last-updated:** 2026-05-18
- **next-free ADR:** `ADR-0190` — UNCHANGED at this 22.1 IMPL commit (R6 STANDS WEAK-default at Task 12; ADR-0190 NOT CONSUMED; carries forward to 22.2 BRAINSTORM as the 22.2 IMPL escape-valve slot per parent §13-R6 + 22.1 PLAN D-P10 codification + 22.1 PROGRESS Task 12 disposition). ADR-0188 + ADR-0189 §Decision + §Consequences body landings ADVANCE the DECISIONS.md tail (§Context blocks unchanged from parent SPEC commit `41ccee7`); both ADRs are now fully landed per ADR-0044 in-place edit discipline. Anticipated 22.2 IMPL ADR landings (settled at 22.2 BRAINSTORM): ~2-4 NEW ADRs (full-bridge-API shape + httpCall dispatcher consumer-#1 of `internal/httpclient/` per phase-20 ADR-0177 + body-buffering interaction with ADR-0128 + dynamic-metadata-bridge deferral) + 1 conditional ADR-0190 (`*LState`-pool escape-valve — fires only if 22.2 body/trailer bridge surface crosses 1ms per-stream threshold). Anticipated 22.3 IMPL ADR landings (settled at 22.3 BRAINSTORM): ~1-2 NEW ADRs (NEW 9th canonical per-route shape ADR + per-route 3-tier dispatch) + 1 IN-PLACE AMENDMENT body landing at ADR-0125 §(xiv) (anticipation-paragraph anchored at parent SPEC commit; AMENDMENT body lands at 22.3 IMPL final Task per ADR-0044). Phase-21's STRENGTHENED two-slot buffer at ADR-0188 + ADR-0189 fully collapses at THIS 22.1 IMPL phase-done (ZERO-slot buffer post-22.1-IMPL — the 0-1 anticipated escape-valve consumption DID NOT fire per D-P10 R6 STANDS; the buffer-discipline re-evaluates at 22.2 BRAINSTORM per BRAINSTORM Q10 + parent §1.2 STRENGTHENED-VARIANT discussion).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
