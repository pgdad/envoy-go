# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `21-http-filter-adaptive-concurrency`. Phase 21 PLAN landed at this commit (1154-LoC PLAN.md at `docs/envoy-go/phases/21-http-filter-adaptive-concurrency/PLAN.md`; structure mirrors phase-20 PLAN's 14-task envelope). **Single-row landing CONFIRMED at PLAN time per ADR-0045 re-evaluation** (PLAN GATE per `superpowers:writing-plans` state-2 check: 14 tasks well under 25-task split-gate; LoC envelope ~1370-1830 production+fixture+AMENDMENT straddles ~1500 at the upper estimate but natural split axes don't carve cleanly per SPEC §3.5 + PLAN §Scope-check — controller state machine + FAKE-TIME tests + percentile aggregation are tightly coupled; framework delta is ZERO; 4 fixture scenarios co-located). **14-task PLAN decomposition** anticipated (Task 1 PROGRESS preamble + 15-precondition verification; Task 2 compiled_config + PARSE-REJECT roster + ADR-0187 §Decision/§Consequences anchor; Task 3 controller.go + clock.go state machine + ADR-0186 §Decision/§Consequences anchor; Task 4 internal/stats/conv.go NEW + gauge.go comment-only + stats.go + ADR-0059 §Decision AMENDMENT body landing; Task 5 decode_headers + 503 wire shape per AMEND-6; Task 6 encode_complete + filter.go per-stream + OnDestroy token-release per D11; Task 7 percentile.go sorted-slice quantile; Task 8 fuzz_test.go 27th fuzzer + corpus seeds; Task 9 full filter integration + boot-registration at cmd/envoy-go/main.go line 125; Task 10 differential fixture 0025-http-adaptive-concurrency 4-scenario; Task 11 ADR final-state alignment + DECISIONS.md cross-reference audit; Task 12 cross-package regression matrix verification; Task 13 BEHAVIOR_CONTRACT.md 7-edit bundle + ADR-0052 atomic landing; Task 14 six-gate phase-done verification A/B/C/D/E/F + STATE.md re-advance + ROADMAP row 21 flip + REVIEW.md). **18 planner-time decisions D1-D18 settled** (D1 Task 3+7 separate-Tasks for parallel-dispatch; D2 14-arm PARSE-REJECT byte-stable wording table; D3 race-test surface — TestController_ConcurrentForwardingDecision + TestController_FAKE_TIME_TimerOrdering; D4 cross-package regression cmd per ADR-0059 AMENDMENT; D5 stat-name compile-time guard via const-decls + table-driven test; D6 corpus seed roster ~30 seeds; D7 boot-registration line 125 alphabetical; D8 ADR escape-valve hypothesis HOLDING — ADR-0188 + ADR-0189 stay UNCONSUMED at IMPL phase-done; D9 fakeClock API shape; D10 sorted-slice quantile edge cases; D11 OnDestroy token-release symmetry; D12 task-graph parallel-dispatch opportunity at Tasks 2+4+7; D13 single-listener fixture topology; D14 wire-shape A1-A4 fixture-0025 coverage; D15 library-behavioral B5+B6+B7 unit+race coverage; D16 cross-phase C8 regression closure; D17 concurrencyLimit atomic.Uint32; D18 *rand.Rand per-controller-source fixed-seed). **Parallel-dispatch opportunity at Tasks 2+4+7** per D12 (three agents can run concurrently on disjoint files; all depend only on Task 1). **Sequential bottleneck at Task 3 → Tasks 5 → 6 → 9 → 10 → 11 → 12 → 13 → 14** (controller materialization + filter integration + fixture + BEHAVIOR_CONTRACT bundle + six-gate verification are the critical path). **Single-row landing settled per ADR-0045** (NOT split into 21.1+21.2 — the natural split axes don't carve cleanly per BRAINSTORM §1.4 + SPEC §3.5; LoC envelope re-estimated post-empirical-scrape at ~1370-1830, slightly above BRAINSTORM's ~1200-1500 due to AMEND-2 algorithmic-invariant additions but well below the ADR-0045 split-gate). **FOURTEENTH §9 family-row** (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11, csrf @ 12, buffer @ 13, compressor @ 14, bandwidth_limit @ 15, rbac @ 16, jwt_authn @ 17, ext_authz @ 18 with its 18.1+18.2 split, ext_proc @ 19 with its 19.1+19.2 split, oauth2 @ 20). **LEANEST framework-delta §9 row to date** — ZERO new `internal/` primitives + ONE in-place §Decision AMENDMENT-anticipation paragraph on ADR-0059 per D1 resolution (the BRAINSTORM-anticipated ADR-0188 candidate COLLAPSES); 7 framework REUSES per SPEC §3.3 (stats Counter+Gauge / HTTPRegistry / per-request filter interface / HCM-parse-time PARSE-REJECT / REUSE-by-absence per-route enforcement (**FOURTH CONSECUTIVE §9 row to skip ADR-0125 amendment**) / fuzzer-corpus framework / differential-fixture framework). **5 §21.P empirical pins ALL CLOSED at SPEC time** via parallel-subagent fan-out against v1.37.2 reference Envoy (disposition matrix: 1 RATIFIED + 3 PARTIAL + 1 RESOLVED-to-AMENDMENT): §21.P1 RATIFIED (503-overflow cross-side promotion D2 — trap sound; CAS arrival-order; 503 body byte-exact `"reached concurrency limit"` 25 bytes); §21.P2 PARTIAL (algorithmic invariants D3 — gradient formula + new-limit RATIFIED; minRTT-aggregation + jitter + 5-consec-trigger + first-tick AMENDs); §21.P3 PARTIAL (stat surface D4 — 3 stat-name renames `*_msecs` + `min_rtt_calculation_active`; RTT in MILLISECONDS upstream / envoy-go-strict departure to nanoseconds; template simplified — no `<config_stat_prefix>` segment); §21.P4 PARTIAL (PARSE-REJECT + defaults + `enabled` semantics — proto field rename `concurrency_limit_calculation_params`→`concurrency_limit_params`; `enabled` defaults OFF when absent REFUTES BRAINSTORM §2.1; `min_rtt_calc_params.interval` not proto-required with `fixed_value` alternative; 5 additional PGV PARSE-REJECT arms; ConcurrencyControllerConfig oneof solo RATIFIED); §21.P5 RESOLVED-to-AMENDMENT (D1 float-gauge convention anchor → IN-PLACE §Decision AMENDMENT on ADR-0059, NOT new ADR-0188). **§1.1 AMEND-1..AMEND-7 amendment block cataloged** (AMEND-1 proto-schema corrections C1-C4: field rename + proto-file consolidation + interval validation + fixed_value PARSE-REJECT; AMEND-2 algorithmic-invariant corrections C1-C4: percentile-not-MIN + jitter-to-next-interval + 5-consec-trigger + first-tick; AMEND-3 stat-name+encoding+template corrections C1-C5: 3 renames + template simplification + RTT-ms-vs-ns envoy-go-strict + gradient ×1000 + Accumulate-import-mode deferral; AMEND-4 `enabled` empty-default OFF refutation; AMEND-5 PARSE-REJECT roster expansion 5 additional arms; AMEND-6 503-overflow cross-side byte-exact promotion RATIFIED; AMEND-7 D1 resolution — ADR-0188-candidate COLLAPSES). **2 NEW ADRs landed at SPEC commit** (§Context drafts per ADR-0044): ADR-0186 Gradient-1 controller state machine + inline Clock seam (NOT framework primitive) + FAKE-TIME differential strategy + sorted-slice percentile aggregation (NOT CircllHist) + line-cited algorithmic lemmata against `gradient_controller.cc` + `fixed_value` PARSE-REJECT; ADR-0187 RTDS `enabled.RuntimeFeatureFlag` deferral PARSE-REJECT + `enabled` empty-default OFF semantics per AMEND-4. **1 IN-PLACE §Decision AMENDMENT-anticipation paragraph at ADR-0059** per ADR-0044 (AMENDMENT body lands at IMPL Task 4 — float-valued gauge int64 encoding convention; ns for time-typed; ×1000 for ratio-typed; 0/1 for bool-typed; NEW `internal/stats/conv.go` `boolToInt` helper). **FOURTH CONSECUTIVE §9 row to skip ADR-0125 amendment** (REUSE-by-absence; no `AdaptiveConcurrencyPerRoute` proto message in v1.32.4 or v1.37.x). Stat surface 92 → 99 names per AMEND-3 (7-name roster; 1 counter `rq_blocked` + 6 gauges `concurrency_limit` / `gradient` / `burst_queue_size` / `sample_rtt_msecs` / `min_rtt_msecs` / `min_rtt_calculation_active` under the upstream-byte-exact `http.<HCM_stat_prefix>.adaptive_concurrency.gradient_controller.<stat>` prefix template). Differential fixture `0025-http-adaptive-concurrency` per SPEC §7: 4 scenario directories ((a) parse_ok REFERENCE-LESS + (b) overflow_503 **PARTIAL CROSS-SIDE BYTE-EXACT per AMEND-6** + (c) stat_surface REFERENCE-LESS + (d) pass_through_when_disabled REFERENCE-LESS) + single-listener topology + 27th fuzzer `FuzzAdaptiveConcurrencyConfigParse`. Six-gate checklist (A/B/C/D/E/F) per SPEC §7.4 + SPEC §14.5 + cross-package regression matrix per SPEC §12 item C8 (post-ADR-0059 AMENDMENT). SPEC §15 18-item acceptance checklist for the IMPL phase-done reviewer. BEHAVIOR_CONTRACT 7-edit bundle anticipated per SPEC §13 (NEW `### envoy.filters.http.adaptive_concurrency` subsection + stat-table 92→99 extension + ADR-0059 §Decision AMENDMENT cross-reference + 2 envoy-go-strict departure records (RTT-ms-vs-ns + sorted-slice-vs-CircllHist) + NEW `### Phase 21 forward-pointer notes` subsection + Per-route canonical patterns cross-reference table caption update). 8 RATIFIED-PENDING-IMPL-TIME items deferred to IMPL per SPEC §12 (4 wire-shape byte-confirmations + 3 library-behavioral + 1 cross-phase regression). ADR-0044 escape-valve in reserve for ~0-2 impl-time-unanticipated ADRs at IMPL. **D11-style hypothesis STRENGTHENED at SPEC commit**: ADR-0188 + ADR-0189 stay UNCONSUMED at phase-21 phase-done (next-free escape valve carried forward TWO slots to phase 22; the D1 resolution collapsed the BRAINSTORM-anticipated ADR-0188 candidate, so the escape-valve buffer is now 2 slots wide instead of 1; HOLD-with-known-risk per ADR-0044 escape-valve hypothesis). Next-free ADR post-SPEC commit: **ADR-0188** (only 2 NEW consumed: ADR-0186 + ADR-0187; ADR-0188-candidate collapsed per AMEND-7).
- **phase-directory:** `docs/envoy-go/phases/21-http-filter-adaptive-concurrency/` (BRAINSTORM.md authored at commit `cad1153` + SPEC.md authored at commit `49ba034` + PLAN.md authored at this commit; PROGRESS.md + REVIEW.md to be authored at the IMPL phase per BOOTSTRAP §5).
- **lifecycle-state:** `phase 21 PLAN done; awaiting IMPL`. ROADMAP row `21` remains `in-progress` at this commit (sub-phases column empty per the SINGLE-ROW disposition CONFIRMED at PLAN time; status will flip to `done` at the phase-21 IMPL phase-done commit per BOOTSTRAP §5 step 6 + per the 14-task PLAN's Task 14 acceptance). DECISIONS.md tail UNCHANGED at **ADR-0187** (PLAN consumes NO ADRs per ADR-0044 — §Decision + §Consequences full bodies anchor at per-Task IMPL lands: ADR-0186 at Task 3 + ADR-0187 at Task 2; ADR-0059 §Decision AMENDMENT body anchors at Task 4 — the AMENDMENT-anticipation paragraph anchored at the SPEC commit is REPLACED with the final AMENDMENT body wording at IMPL Task 4 per ADR-0044 in-place edit discipline).
- **next-skill:** `superpowers:subagent-driven-development` (recommended per project memory `feedback_execution_style.md` — fresh subagent per task; reviews between tasks; exploits the parallel-dispatch opportunity at Tasks 2+4+7 per PLAN D12). The next session is the phase-21 IMPL session (lifecycle-state 3 → 6 traversing per BOOTSTRAP §5). Alternative: `superpowers:executing-plans` (inline batch execution with checkpoints). Cold-start scope for the next session: read STATE.md (this file) + `docs/envoy-go/phases/21-http-filter-adaptive-concurrency/PLAN.md` (the PLAN authored at this commit; load-bearing for the 14-task envelope + the 18 planner-time D-question slate + the per-Task File-structure table + the ADR landing roster + the task-graph parallelization opportunity + the 15-precondition execution checklist + the squash-merge handoff discipline) + `docs/envoy-go/phases/21-http-filter-adaptive-concurrency/SPEC.md` (per-Task SPEC §-references throughout the PLAN; load-bearing for the §6 code shapes + the §11 §21.P pin disposition matrix + the §12 RATIFIED-PENDING-IMPL-TIME items + the §15 18-item acceptance checklist + the AMEND-1..AMEND-7 amendment block) + project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (`feedback_execution_style.md` — subagent-driven IMPL preference; `feedback_git_worktrees.md` — always-worktrees for the IMPL worktree; `feedback_push_to_origin.md` — always-push after the clean local merge of the IMPL squash commit on master).
- **last-commit:** `<TBD — SHA-fill follow-up after squash-merge>` — to be replaced with the actual squash-commit SHA after the phase-21 PLAN squash-merge of the worktree branch `phase-21-http-filter-adaptive-concurrency-plan`. Phase 21 PLAN landed via worktree `.worktrees/phase-21-http-filter-adaptive-concurrency-plan/` on branch `phase-21-http-filter-adaptive-concurrency-plan` per project memory `feedback_git_worktrees.md` (1 worktree commit bundled into the squash: PLAN.md + STATE.md re-advance + ROADMAP row 21 PLAN-done annotation). SHA-fill follow-up commit per the phase-09..20 convention: `phase 21 PLAN follow-up: STATE.md SHA-fill (TBD → <squash SHA> post-squash)`. Predecessor master tip was `3f0f768` — `phase 21 SPEC follow-up: STATE.md SHA-fill (TBD → 49ba034 post-squash)`.
- **last-updated:** 2026-05-18
- **next-free ADR:** `ADR-0188` — UNCHANGED from the SPEC commit (PLAN consumes NO ADRs per ADR-0044 §Context-draft discipline; ADR-0186 + ADR-0187 §Context drafts already anchored at the SPEC commit `49ba034`; ADR-0059 §Decision AMENDMENT-anticipation paragraph already anchored at the SPEC commit). The BRAINSTORM-anticipated ADR-0188 candidate (float-valued gauge encoding convention) COLLAPSED at SPEC commit per D1 resolution → IN-PLACE §Decision AMENDMENT on ADR-0059 (NOT a new ADR; no number consumed). D11-style hypothesis HOLDING at PLAN commit per PLAN D8: ADR-0188 + ADR-0189 stay UNCONSUMED at phase-21 IMPL phase-done (HOLD-with-known-risk per ADR-0044 escape-valve hypothesis; 2-slot escape-valve buffer absorbs one impl-time-unanticipated ADR without breaking the next-phase escape-valve invariant; if at IMPL time a surprise lands warranting a new ADR, it is ADR-0188 + the D8 hypothesis is recorded as falsified in PROGRESS.md).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
