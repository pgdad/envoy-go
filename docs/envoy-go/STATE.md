# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

> **This file is a POINTER, not an archive** — `BOOTSTRAP_PROMPT.md` §4: *"STATE.md — pointer: active phase, last commit, next action."* It carries the live pointer, the live counts, and a SHORT recent lineage. **Full stage-by-stage history lives in `STATE_HISTORY.md`** (archived at ADR-0288; **nothing was deleted** — the pre-move file is preserved there verbatim, byte-for-byte).
>
> **⚠️ THE RULE ADR-0288 EXISTS TO ENFORCE: there is EXACTLY ONE live `next-skill:`, `lifecycle-state:`, `next-free ADR:`, and count block IN THIS FILE, and they live in §Current pointer directly below.** Before that move, ~330 lines of prepended backlog had buried TWO fossilised "current state" blocks whose `next-skill:` still read *"the 42.2 hedging BRAINSTORM"* and *"the 46.2 SPEC"* while the live phase was **66** — so `grep 'next-skill:' STATE.md` returned **two stale hits and ZERO live ones**, and `grep 'fuzzers:'` returned **48** and **42** when the true count was **55**. **A session may trust a grep of this file again. Keep it that way: when you advance the pointer, EDIT §Current pointer IN PLACE — never prepend a new block above it.**

---

## Current pointer

*(The ONLY live "what next" in this repo. Update IN PLACE at every stage close.)*

- **active-phase:** `phase 74 (tls-handshake-outcome-stats) PLAN done` (2026-07-24) — **row 74 STAYS `in-progress`** (registered at the BRAINSTORM per the ROADMAP §Schema invariant `:21`; it flips `done` only at the phase-74 IMPL six-gate)
- **phase-directory:** `docs/envoy-go/phases/74-tls-handshake-outcome-stats/` (`BRAINSTORM.md` + `SPEC.md` + **`PLAN.md`** + **`PROGRESS.md`** at this stage — the IMPL adds no new file, it POPULATES `PROGRESS.md`'s §IMPL record)
- **lifecycle-state:** `3` (the phase-74 PLAN landed; **row 74 STAYS `in-progress`**; the four-stage lifecycle has ONE stage left for phase 74 — IMPL). **Subject unchanged:** `tls-handshake-outcome-stats` — `ssl.handshake` / `ssl.fail_verify_error` / `ssl.fail_verify_no_cert` at listener scope, +3 stats / +0 fixtures / +0 packages / +0 modules / +0 fuzzers / **+0 PRODUCTION imports** / ZERO new exported symbols. **The 9-task TDD spine (T1-T9) is pinned in `PLAN.md`**, each task red-first -> green -> break-verified, with **TWELVE breaks** (SPEC's A-G plus PLAN-added C'/E'/F'/G'/H), of which **D — ADDING the refuted `rt.kind != kindQUIC` gate — is the DISTINGUISHING one.** ⚠️ **THE GATE IS `rt.tlsMode` ALONE, NO KIND CHECK** — `SPEC.md:1`, `:315` and `:320` still stale-carry the SPEC's own REFUTED first reading against its authoritative §3.4/§16 and ADR-0296 §Context ¶8(ii) (`DECISIONS.md:17274`); a PLAN that copied SPEC §10 item 2 verbatim would have implemented the wrong gate AND made Break D unfireable. **A FOURTH stale site sits OUTSIDE `SPEC.md`, at `ROADMAP.md:136`** — the very cell T9 opens — which still calls uncounted QUIC handshakes *"a DEPARTURE that must be written down"*; it is SHARED PARITY. **TWELVE SPEC claims were REFUTED at the PLAN and TEN new findings (F1-F10) minted, THREE of them SEVERE**: `internal/listener/`'s test corpus has **NO mTLS PKI** (a CA cert but no CA key, no client cert) so a new runtime PKI generator is required; `mkDownstreamTSRequireClientCert` (`manager_test.go:644`) **boot-REJECTS** and is not reusable; and a nil `*stats.Counter`.Inc **PANICS** in the `serveConnection` GOROUTINE with **no `recover()` anywhere in `internal/listener/`**, so Break C's observable is a PROCESS CRASH, not an assertion failure — which is why the registration gate and the Inc guard being equivalent (by the `:516-525` mixed-chain reject) earns its own named test. **The adversarial pass RAN and changed SIX instructions:** `net.Pipe()` **DEADLOCKS** (the live-handshake helper needs a loopback TCP pair — it hung to a 45s timeout with no failing assertion); **Break F FIRED at its original scope**, i.e. the one break declared *must not fire* would have raised a false stage-stop; `TestListenerMetrics_GateMatchesInc` **stayed GREEN under Break E** until it asserted the counter pointers themselves; T7's own gate (`grep '1201' => 0`) was **unsatisfiable by T7's own instruction**; Break H **would have killed the trusted arm so `AssertStats` never ran**; and PLAN §1.4 had **self-certified a completed adversarial pass over a `PROGRESS.md` that did not exist**. **+0 fixtures (119)** — `0111-tls-cvc-empty-dynamic-fallback` is EXTENDED with a cross-side `StatsAsserter`; `var _ fixture.StatsAsserter = (*edfDriver)(nil)` is MANDATORY (dispatch is a SILENT type assertion; compiler, vet AND lint are all silent) and the ABSENT check must be SEPARATE from the value check with a `continue` (a counter that fails to register reads `0 == 0` and passes VACUOUSLY).
- **next-skill:** **the phase-74 IMPL** — execute the 9-task spine in `PLAN.md` (T1 classifier -> T2 fields+gate+NAME-SET guard -> T3 QUIC test -> T4 the two Inc points -> T5 `helpText` -> T6 the `0111` asserter -> T7 BEHAVIOR_CONTRACT B1-B8 -> T8 six-gate -> T9 ADR-0296 + ADR-0286 C3 + ROADMAP + close). **ROW 74 FLIPS `in-progress` -> `done` at the T8/T9 six-gate** (ADR-0106, the SOLE leg — a SINGLE FLAT ROW, `reference_roadmap_split_phase_row_done`). **ADR-0296 COMPLETES IN PLACE** — append `### Decision` + `### Consequences` after the **RETAINED** `:17280` footer, mirroring **ADR-0295** (`:17229`->`:17231`->`:17243`), **NOT** ADR-0286; verify `### Decision` inside the block reads **0 before / 1 after**; flip `:17256` PROPOSED -> COMPLETE; no renumber, tail stays ADR-0296, next-free ADR-0297. ⚠️ **THE IMPL INHERITS A PARTIAL BUILD, NOT A FULL ONE.** V1 built T1-T4 in PRIVATE scratch and ran their breaks — that evidence is recorded in `PROGRESS.md` §What V1 CONFIRMED and does NOT need re-deriving — but **T5-T9 were NEVER executed, the `0111` `AssertStats` has still never been written or run, Breaks G/G'/H are untested, nothing reference-side was executed at this stage, and the T2 rename was never exercised in `manager_test.go` in place.** ⚠️ **RE-DERIVE every anchor at the IMPL tip anyway** (`feedback_brief_citations_not_evidence`, `reference_parallel_stream_mints_fresh_drift`) — a PLAN is not evidence either. ⚠️ **RD-GREP0: the SPEC's `grep -c 'VerifyPeerCertificate\|handshake-error callback'` => 0 is now 1**, the sole hit being the ADR-0296 paragraph asserting it is 0 — **do NOT read 1 as drift**; scope it with `awk 'NR>=16888 && NR<16930'`. ⚠️ **NO deferred sentence is narrowed before the IMPL**; at the IMPL the `ssl` clause on **`ROADMAP.md:204`** (NOT `:202`) SHORTENS, must **ADD `curves`/`sigalgs`** and must **REPLACE THE PROSE** (*"envoy-go emits ZERO such stats"* goes FALSE and *"a framework-surgery row, NOT an inline add"* is the very adjective this row refutes), and check (2) must **STAY 3**. Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`); **use `git -C <abs-worktree-path>` everywhere and tripwire `pwd` + branch + commit count before any commit or gate run** (`reference_bash_cwd_reset_commits_to_main`).
- **last-commit:** the phase-74 PLAN squash (`git log --grep 'phase 74'`; **locate by SUBJECT, never by position** — `reference_next_prompt_tracked_despite_gitignore`). Prior: the phase-74 SPEC squash `ab13fc19` (this stage's base), the phase-74 BRAINSTORM squash + its router follow-up `f5a38c40`, the phase-73 IMPL squash `de2d7737`, the phase-73 PLAN squash `bea4178c`.
- **last-updated:** 2026-07-24 (phase-74 PLAN done. Lifecycle-state 2 -> **3**; **row 74 STAYS `in-progress`** — **ROADMAP and DECISIONS BYTE-UNTOUCHED at a PLAN**. Fresh worktree `.worktrees/phase-74-plan` off master `ab13fc19` (the phase-74 SPEC squash), branch `phase-74-plan`. Docs-only: **`PLAN.md` + `PROGRESS.md` are the ONLY two files in the phase-directory delta** — ZERO production `.go`. **NO PARALLEL STREAM**: `git diff --stat f5a38c40 HEAD -- '*.go' go.mod go.sum test/` is **EMPTY**, so the production tree is byte-identical to what the SPEC re-derived and §1 is a full independent re-verification. Six agents: four read-only re-derivation agents on disjoint remits (listener/stats/statssink+collisions · the ctx-override call-graph trace + the stdlib block · the differential fixture framework + `0111` + the precedent scrapers · the four docs files + the phase-73 templates) and two adversarial verifiers in PRIVATE scratch, V1 building T1-T4 BY EXECUTION and V2 re-running ~120 anchors. V2's verdict: *"every one of the PLAN's twelve claimed corrections of the SPEC holds ... that earned trust is what makes S1 disqualifying"* — S1 being that §1.4 had self-certified an adversarial pass over a file that did not yet exist; the phase-73 guardrail sentence forbidding exactly that has been RESTORED verbatim. `next-prompt.txt` rolled to the phase-74 IMPL (**TRACKED despite `.gitignore`**; edited in this worktree). **Sentinel re-run MECHANICALLY TWICE at this close (worktree AND landed master post-push; does NOT fire; `stop` NOT created):** (1) prints **`NOT DONE: row 74`**; (2) => **3**; (3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`. ⚠️ Check (1) will go **SILENT** at the phase-74 IMPL close — row 74 is the ONLY non-`done` chartered row, CONFIRMED BY EXECUTION in a scratch copy; checks (2) and (3) still print, so the sentinel still does NOT fire.)
- **next-free ADR:** **`ADR-0297`** — DECISIONS.md tail is **ADR-0296** (STATUS **PROPOSED** — §Context drafted at the phase-74 SPEC, ADR-0044-**as-used**; §Decision + §Consequences append IN PLACE at the phase-74 IMPL, no renumber, no new ADR). **DECISIONS is BYTE-UNTOUCHED at this PLAN.** `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1` -> `## ADR-0296`; `grep -c '^## ADR-0297'` -> 0. Block shape re-verified at this close: block `:17254`-`:17280` (EOF), **1** `### Context` (`:17258`), **0** `### Decision`, **0** `### Consequences`, footer `:17280`. ⚠️ **TWO internal defects the IMPL must fix while completing it**: `:17256`'s STATUS points at *"§Context ¶6"* for the ADR-0044 misattribution, which is actually at **¶4(i) (`:17266`)**; and ¶3 (`:17264`) asserts a grep count of **0** that its own landing made **1**.

---

## Project counts at this commit

*(Live. Re-verify at session start — `git fetch` first — and update here at every stage close. Canonical commands inline.)*

⚠️ **ALL counts below are UNCHANGED at this docs-only phase-74 PLAN** apart from the DECISIONS tail, and were re-run MECHANICALLY in the stage worktree with the canonical commands. The phase-74 deltas noted per line are **ANTICIPATIONS — NOT YET LANDED**; they land (or are corrected) at the phase-74 IMPL. **Anticipated envelope: +3 stats (1201 -> 1204 — the first non-zero stat delta in this lineage) · +0 fixtures (119) · +0 fuzzers (55) · +0 BackendKinds (38) · +0 go.mod modules (2) · +0 IMPORTS · ZERO new packages · ZERO new exported symbols · DECISIONS tail STAYS ADR-0296 (BYTE-UNTOUCHED at a PLAN).**

- **active differential fixtures:** **119** — `ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0117-tracing-custom-tags-metadata-cluster` (added at phase 73 — the OTLP CLUSTER-metadata-custom_tags fixture: **TWO clusters** carrying DIFFERENT static `metadata.filter_metadata` in an ARBITRARY non-`envoy.lb` namespace behind two path-prefix routes onto the **SAME single backend**, so it DISCRIMINATES *selected cluster* from *only cluster*; spans partitioned per-path by **`http.url`**, two CLUSTER custom_tags cross-side EXACT key+value, STRING values only, NO writer; reference port 10117). **Phase 74 anticipates +0** — `0111-tls-cvc-empty-dynamic-fallback` is EXTENDED with a cross-side DELTA `fixture.StatsAsserter` (NO new YAML, NO new directory), so the count STAYS **119** (`0118` and reference port `10118` remain FREE). NOT YET LANDED.
- **fuzzers:** **55** — `grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`. Phase 73 added a `meta_cluster_ok` `withMetaTags` seed AND repointed `meta_bad` to `Kind: &metadatav3.MetadataKind{}` ⇒ **+0** (an `f.Add` seed is not a fuzzer — `reference_fuzzer_count_docs_drift`). **Phase 74 anticipates +0** — it consumes no new config field at all, so there is no parse arm and no fuzz-seed classification to flip. NOT YET LANDED.
- **stat surface:** **1201** — BEHAVIOR_CONTRACT doc count. **There is NO mechanical counting command** (the contract says so itself, e.g. `BEHAVIOR_CONTRACT.md:4942`); enforcement is per-phase `TestNoNewStat*` guards asserting a delta of ZERO, never the absolute total. ⚠️ **Phase 74 anticipates +3 -> 1204** — `ssl.handshake` / `ssl.fail_verify_error` / `ssl.fail_verify_no_cert` at listener scope, all three FIXED and hyphen-free; **charset EXECUTED, not asserted**: `IsValidName` returned true for all three FULL names in BOTH the reference and envoy-go spellings AND for the IPv6 form, `NewCounter` succeeded for all three, and SN3 flattening was executed clean on what are the **first three-dot listener names** (`ExtractTags` ⇒ residual `listener.ssl.handshake` + label `{envoy_listener_address …}`). ⚠️ **The delta is a NAME-surface delta, NOT a flat per-deployment one** — D-TLSHS-REGSCOPE resolved TLS-CHAINS-ONLY (the BRAINSTORM anticipated unconditional), so a **plaintext-only** deployment gains ZERO names; a **QUIC** listener DOES gain all three and they stay permanently zero, which is PARITY with the reference (probed), not an anomaly. ⚠️ **The IMPL edit map is bigger than "1201 -> 1204"**: `1201` appears at exactly TWO lines (`BEHAVIOR_CONTRACT.md:831`, `:847`), there is **NO recorded `1200 -> 1201` ledger step**, and THREE stale `1200`s survive at `:1429`/`:1463`/`:1495`. An INVERTED registration guard is OWED and **must assert a NAME SET, not a cardinality** — the landed precedent (`internal/statssink/registration_test.go:26`, one of FIVE guards in that one file) counts via `reg.Walk` and never inspects `m.Name()`, so a count-only inversion would pass with all three names misspelled. **NOT YET LANDED.**
- **BackendKind tail:** **38** (`H2GoawayResponder`). Phase 73 was **+0** (`0117` reuses `HTTPFixedBody`; the `otlptrace` receiver is driver-owned; NO writer). **Phase 74 anticipates +0** — `0111` is extended, not cloned, and its driver is a self-contained PKI generator with `BackendCount()` == 1. NOT YET LANDED.
- **DECISIONS.md tail:** **ADR-0296** (next-free **ADR-0297**) — `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1`. **ADR-0296 is PROPOSED** — §Context drafted at THIS phase-74 SPEC (ADR-0044-**as-used**; ⚠️ SPEC §1.1 D1 records that ADR-0044 does NOT actually contain that discipline), and the §Context append **IS** the tail flip ADR-0295 -> ADR-0296. Block shape verified at this close: **1** `### Context`, **0** `### Decision`, **0** `### Consequences`, plus the `> **STATUS: PROPOSED …**` blockquote and the `*(§Decision + §Consequences land at the phase-74 IMPL.)*` footer — so the IMPL's in-place APPEND is sound. ADR-0295/0294/0293/0292 stay COMPLETE. The append is a **PURE APPEND** (28 insertions, ZERO deletion lines) and **`ADR-0286` is BYTE-UNTOUCHED** (sha256-verified) — ⚠️ **its C3 correction lands at the phase-74 IMPL, not here**, as an INDENTED `> [CORRECTED at phase 74/ADR-0296: …]` blockquote per the `DECISIONS.md:16901` precedent.
- **go.mod modules:** the tracked lineage figure is **2** (the phase-61.2 count: `quic-go` direct + `qpack` indirect) — **NOT a repo total**; the single `go.mod` requires **67** modules. Phase 73 was **+0** (`go mod tidy -diff` EMPTY; `go.mod`/`go.sum` byte-unchanged vs master — **ZERO new imports anywhere**, unlike phase 72's one). **Phase 74 anticipates +0** — the classifier needs `errors` and `crypto/tls`, and `crypto/tls` is ALREADY imported by `internal/listener/manager.go` (aliased `stdtls`); **an import LINE is not a go.mod MODULE**, and there is no new sub-package, so `reference_new_subpackage_pulls_transitive_module` does not bite. NOT YET LANDED.
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` (see `ENVOY_TARGET.md`).
- **conformance:** h2spec 53/53; proxy-wasm all 10 families PASS.

---

## Recent lineage

*(The last FIVE stages only — enough for the router's "prior-phase lineage" read. Everything earlier is in `STATE_HISTORY.md`. These bullets are carried over VERBATIM; the newest mirrors §Current (**the phase-74 PLAN done** — the stage that decomposed the 9-task spine, REFUTED TWELVE SPEC claims and minted TEN findings, and whose adversarial pass changed SIX of its own instructions including catching that it had self-certified that very pass over a file which did not exist), and the oldest (**phase-73 SPEC done**) was dropped as this list re-capped at five. The five now read: phase-74 PLAN · phase-74 SPEC · phase-74 BRAINSTORM · phase-73 IMPL · phase-73 PLAN.)*

- **prior active-phase:** `phase 74 (tls-handshake-outcome-stats) PLAN done` (2026-07-24) — lifecycle-state 2 -> 3; docs-only (`PLAN.md` + `PROGRESS.md`, the ONLY two files in the squash's phase-directory delta — **ZERO production `.go`, DECISIONS and ROADMAP BYTE-UNTOUCHED**, row 74 STAYS `in-progress`). Fresh worktree `.worktrees/phase-74-plan` off master `ab13fc19`, branch `phase-74-plan`. **NO PARALLEL STREAM** — `git diff --stat f5a38c40 HEAD -- '*.go' go.mod go.sum test/` EMPTY. Six agents: FOUR read-only re-derivation agents on disjoint remits plus TWO adversarial verifiers in PRIVATE scratch (V1 building T1-T4 **BY EXECUTION** and running their breaks; V2 re-running ~120 anchors). **The 9-task TDD spine (T1-T9) with TWELVE breaks** (the SPEC's A-G plus PLAN-added C'/E'/F'/G'/H), **D — ADDING the refuted `rt.kind != kindQUIC` gate — the DISTINGUISHING one.** ⚠️ **TWELVE SPEC claims REFUTED.** The load-bearing one: **`SPEC.md:1`, `:315` and `:320` still carry the SPEC's OWN REFUTED first QUIC reading against its authoritative §3.4/§16** — a PLAN copying §10 item 2 verbatim would have implemented the wrong gate AND made Break D unfireable — and **a FOURTH stale site sits outside `SPEC.md`, at `ROADMAP.md:136`**, the very cell the IMPL opens. Also: the SPEC's ctx-override bound (*"ZERO `context.WithTimeout` in production"*) is **FALSE** (`listenerfilter/pipeline.go:43`) though its conclusion survives on the stronger ground that `Pipeline.Run` returns only an `error`; `checkName` panics at `registry.go:117` not `:107`; `errors` is at `manager.go:7` not `:6`; the SN3 impl is `name.go:83-92` not `:85-93`; `quic_test.go:92-97` pins only the COUNTER half; and **the SPEC's `grep => 0` is now `1`, made self-refuting by its own §Context append.** **TEN new findings, THREE SEVERE**: `internal/listener/` has **NO mTLS PKI** in its test corpus; `mkDownstreamTSRequireClientCert` **boot-REJECTS**; and a nil `*stats.Counter`.Inc **PANICS in a goroutine with no `recover()`** ⇒ Break C is a PROCESS CRASH. **The adversarial pass changed SIX instructions**: `net.Pipe()` **DEADLOCKS** silently to a 45s timeout; **Break F FIRED at its original scope** (the one break declared *must not fire*); `GateMatchesInc` **stayed GREEN under Break E**; T7's own `grep '1201' => 0` gate was **unsatisfiable by T7's own instruction**; Break H **would have killed the trusted arm so `AssertStats` never ran**; and **§1.4 had self-certified a completed adversarial pass over a `PROGRESS.md` that did not exist**, having deleted phase-73's guardrail against exactly that — restored verbatim. Sentinel re-run MECHANICALLY TWICE (worktree + landed master post-push): does NOT fire; `stop` NOT created. Counts UNCHANGED. **Next -> the phase-74 IMPL** (row 74 flips `done` at its six-gate; ADR-0296 completes IN PLACE; the ROADMAP narrow REPLACES THE PROSE and check (2) STAYS 3).

- **prior active-phase:** `phase 74 (tls-handshake-outcome-stats) SPEC done` (2026-07-24) — lifecycle-state 1 -> 2; docs-only, **EXACTLY TWO files in the squash delta** (the new `SPEC.md` + the ADR-0296 §Context append to `DECISIONS.md`) — **ZERO production `.go`, ROADMAP BYTE-UNTOUCHED**, row 74 STAYS `in-progress`; fresh worktree `.worktrees/phase-74-spec` off master `f5a38c40`, branch `phase-74-tls-handshake-outcome-stats-spec`, per `feedback_git_worktrees`; subagent-driven — **EIGHT agents**, of which FOUR were live-probe fleets against `envoyproxy/envoy:contrib-v1.37.2` on DISJOINT port ranges and container prefixes running **~35 EXECUTED arms** (fresh container per arm, bridge networks, a positive control in every bootstrap, every arm's discrimination criterion stated BEFORE the run), plus a Go-toolchain execution agent, two read-only re-derivation agents and a governance agent. **THE HEADLINE — THE BRAINSTORM'S CENTRAL REGISTRATION ANTICIPATION IS REFUTED, AND THE REFUTATION MADE THE ROW CHEAPER.** D-TLSHS-REGSCOPE anticipated *unconditional* registration ⇒ a flat +3. **The reference registers `listener.<addr>.ssl.*` TLS-CHAINS-ONLY.** The zero-visibility confound was eliminated FIRST — plain `/stats` demonstrably renders zero-valued names (429 lines vs 85 for `?usedonly`; 92 zero-valued `listener.*` counters on the plaintext container) — so an ABSENT name means genuinely NOT REGISTERED; then settled by ONE container / TWO listeners / ONE scrape, both driven with green positive controls: the TLS listener carried the full `ssl.*` block INCLUDING its zero members, the plaintext listener carried **ZERO** while carrying 15+ other zero-valued names in the same scope. envoy-go MATCHES it, and **the gate ALREADY EXISTS as a field** — `listenerRuntime.tlsMode` (`manager.go:145`, set `:639`), whose FIRST production consumer this row becomes, unambiguous because envoy-go REJECTS mixed TLS+plaintext chains on one listener (`:516-525`). ⇒ the +3 is a NAME-surface delta; a plaintext-only deployment gains ZERO names. **A SECOND HEADLINE — THE SPEC REVERSED ITS OWN DECISION BEFORE LANDING, and recorded the reversal rather than replacing it quietly.** D-TLSHS-QUIC was FIRST resolved to gate QUIC out, on the reasoning that *"a permanently-zero counter is a false measurement while an absent one is an honest gap."* A probe run specifically to settle it **REFUTED that on the facts**: the reference registers the full `ssl.*` family on a QUIC listener AT BOOT and **NEVER moves it** — `ssl.handshake: 0` after FIVE confirmed HTTP/3 connections, and `connection_error: 0` on a failure arm where the TCP comparator **in the same process and the same scrape** fired. ⇒ envoy-go doing the same is **EXACT PARITY**, and gating QUIC out would have been the DEPARTURE. The gate is `rt.tlsMode` **alone**, provably sufficient because `startQUIC` HARD-ERRORS without a TLS config (`quic.go:33-36`) so every QUIC listener that boots has `tlsMode == true`; `quic.go` stays BYTE-UNTOUCHED. The argument was sound in the abstract, cheap to check, and wrong — **the row's own thesis turned on the SPEC itself.** **RESOLVED BY EXECUTION:** `ssl.handshake` counts **SUCCESSES ONLY** (1-success + 1-reject: `cx_total` 2, `handshake` 1) · the two error taxonomies **AGREE on EVERY cert-shaped input**, including the two flagged as most likely to diverge — EXPIRED (alert 45) and wrong-EKU (alert 43) both book `fail_verify_error` AND both yield `*tls.CertificateVerificationError` · ⚠️ **the ALERT CODE IS NOT THE BUCKET KEY**, PROVEN BY CONSTRUCTION (five alerts 116/48/45/43/40 collapse onto two buckets; the same no-cert input forced to TLS 1.2 emits 40 not 116 yet still books `fail_verify_no_cert`), so an alert-derived implementation would mis-bucket EVERY TLS 1.2 connection and the shape-based `errors.As` design is INDEPENDENTLY VALIDATED — reinforced by `tls.AlertError` being UNREACHABLE on a non-QUIC server conn (`conn.go:1598` sits inside `if c.quic != nil`) · and at THIS session's `go1.26.5`, `errors.As(*tls.CertificateVerificationError)` is an EXACT, TOTAL discriminator at BOTH TLS versions with **NO** non-cert shape producing a CVE across TEN shapes ⇒ no over-count; non-vacuity PROVEN (text differs AND `UnverifiedCertificates` len 1), the no-forced-send control DEGRADED into the no-cert arm (confirming `reference_go_client_cert_withholding` live), and synchronicity PROVEN BY ORDERING (a 300 ms client stall held `HandshakeContext` 301/302 ms) ⇒ the row does NOT inherit the `0108`/`0111` half-RTT workaround. **THE ROW'S ONE NEW DEPARTURE:** `other` increments NOTHING while the reference books non-cert failures under **`ssl.connection_error`** (EXECUTED: TLS-1.0-only and plaintext-to-TLS-port both landed there, DISJOINT from the cert path). The row STAYS at three names because only TWO of N non-cert modes were driven and the bucket's membership is **UNENUMERATED** — a counter whose membership cannot be enumerated cannot be cross-side asserted honestly; `ssl.connection_error` is recorded as the cheapest identified follow-on. **THE PHASE-65 ROSTER IS WRONG ON ONE NAME:** `ssl.certificate.validation_ca.expiration_unix_time_seconds` **DOES NOT EXIST** at this pin — the reference emits two differently-shaped gauges, and the real surface is **FIFTEEN eager names + FOUR dynamic families** (`ciphers`/`versions`/**`curves`**/**`sigalgs`**, the last two never previously named) ⇒ the deferred split is a FOUR-family split. ⚠️ **`ssl.no_certificate` is a SUCCESS-PATH annotation, not a failure counter** — established independently by THREE fleets and already present in the LANDED record at `BEHAVIOR_CONTRACT.md:962`, which no prior phase-74 document had noticed; conflating it with `fail_verify_no_cert` is wrong in BOTH directions. **FIVE BRAINSTORM cites REFUTED** (and since `git diff --stat de2d7737 HEAD` touches NO production `.go`, each was wrong **WHEN WRITTEN**, not drift): `quicAcceptLoop` is `quic.go:88` not `:104` · `errors` is ALREADY imported (`manager.go:6`) ⇒ **+0 imports** · the stat SPELLING — `normalizeAddr` underscores DOTS too, so envoy-go emits `listener.0_0_0_0_10000.…` where the reference emits `listener.0.0.0.0_10000.…`, a DELIBERATE documented divergence (`manager.go:326-332`) that **decides the fixture asserter's shape** · the QUIC framing · and the ROADMAP anchors (`:199`->`:200`, `:203`->`:202`). **EIGHT DOCUMENTARY FINDINGS (D1-D8), TWO of them the row's own theme one level up:** **ADR-0044 does NOT contain the §Context-draft discipline the entire project cites it for** — it is titled *"BEHAVIOR_CONTRACT HTTP/1.1 subsection"* and a mechanical scan of its block returns ZERO such language; the convention accrued purely by downstream citation, and phase 73 had already begun hedging to *"ADR-0044-as-used"*, which this SPEC adopts throughout — and **the "first cross-ADR correction" claim is REFUTED by THREE precedents**, one of them (`DECISIONS.md:16901`, a phase-67/ADR-0289 correction) already sitting INSIDE ADR-0286 itself, which also fixes the FORM: the INDENTED `> [CORRECTED at phase NN/ADR-NNNN: …]` blockquote, not the inline variant. Also found: the stat-surface ledger HOLE (no recorded `1200 -> 1201`; three stale `1200`s at `:1429`/`:1463`/`:1495`) · TWO stale comments in the very file the row edits (`manager.go:172-174`, `:345`) · and `manager_test.go:1928` `TestListenerManager_AllocatesTwoMetricsPerListener`, whose NAME says *"exactly the 2"* while its BODY only asserts PRESENCE ⇒ **+3 yields NO RED from it** while its name silently goes false. **FIXTURE POSTURE DECIDED:** `0111` EXTENDED with a cross-side `fixture.StatsAsserter` scraping **`/stats/prometheus`, NOT flat `/stats`** — the two sides normalize the listener address differently but BOTH hoist it into a LABEL, leaving the metric NAME cross-side identical (the `0005` precedent), which also avoids plumbing the runner-allocated subject port the driver currently discards. **Shape A (scrape once, absolute counts)** chosen and JUSTIFIED rather than assumed: nothing pre-moves `l_edf`'s `ssl.*` (traced exhaustively — reference readiness polls admin `9901` not the TLS port, subject readiness parses a stdout sentinel, `waitTCPDial`'s ~12 call sites all target BACKEND ports, and a subject retry uses a FRESH process so stats reset). ⚠️ `var _ fixture.StatsAsserter = (*edfDriver)(nil)` is MANDATORY — dispatch is a SILENT type assertion and compiler, `go vet` AND `golangci-lint` are all silent — and **the ABSENT check must be SEPARATE from the value check**, since a counter that fails to register reads `0 == 0` and passes VACUOUSLY. ⚠️ The retirable boundary claim lives at **FOUR** sites, not the BRAINSTORM's two, **each BUNDLING a retirable half with a still-live `/listeners` guard**; and the RD3 forced-send disclaimer **INVERTS** at the stats layer — forced-send becomes exactly the discriminator between `fail_verify_error` and `fail_verify_no_cert`. **+0 fixtures (119).** **ADR-0296 PROPOSED** (§Context only — **1** `### Context`, **0** `### Decision`, **0** `### Consequences`, verified; the tail flip ADR-0295 -> ADR-0296 IS the §Context append; next-free **ADR-0297**), a **PURE APPEND** (28 insertions, ZERO deletions) with **`ADR-0286` BYTE-UNTOUCHED** (sha256-verified) — its C3 correction lands at the **IMPL**, matching both precedents. ⚠️ **NO build of row 74 exists, in-tree or in scratch** — the classifier, the gate, the guard and the asserter are all SPECIFIED and NONE is COMPILED; nothing in SPEC §3 may be cited as execution evidence for buildability. **NO deferred sentence narrowed** (phase-57 precedent) — but the narrow was VERIFIED MECHANICALLY by applying it in a SCRATCH copy: check (2) **STAYS 3** and the `candidates:` marker survives, ⚠️ **while the surviving PROSE becomes self-contradicting** (*"envoy-go emits ZERO such stats"* goes FALSE and *"a framework-surgery row, NOT an inline add"* is the very adjective the row refutes), so **the IMPL must replace the prose, not just delete three names.** **Counts UNCHANGED** apart from the tail: fixtures **119** · fuzzers **55** · stat surface **1201** · BackendKind **38** · modules **2**. **Sentinel re-run MECHANICALLY TWICE (worktree + landed master): does NOT fire; `stop` NOT created** — (1) `NOT DONE: row 74`; (2) ⇒ **3**; (3) `NEVER OPENED: gRPC/Runtime/WASM`. **Next -> the phase-74 PLAN** (decompose the ~9-task single flat row; RE-DERIVE every anchor at the PLAN tip; pin the SEVEN breaks A-G, of which **D** — ADDING the refuted `kind != kindQUIC` gate — is the DISTINGUISHING one).

- **prior active-phase:** `phase 74 (tls-handshake-outcome-stats) BRAINSTORM done` (2026-07-24) — lifecycle-state DONE → 1; the roller's AUTONOMOUS self-pick (no human pick, per the 2026-07-12 standing directive) continues the Observability family; docs-only, ZERO production `.go`, exactly THREE files edited (`ROADMAP.md`, `STATE.md`, `next-prompt.txt`) plus the new BRAINSTORM; fresh worktree `.worktrees/phase-74-brainstorm` off master `de2d7737` (the phase-73 IMPL squash), branch `phase-74-brainstorm`, per `feedback_git_worktrees`. **Subject:** `tls-handshake-outcome-stats` — the three FIXED-NAME downstream TLS handshake-outcome counters `ssl.handshake` / `ssl.fail_verify_error` / `ssl.fail_verify_no_cert` at **listener scope**, under the prefix `registerListenerMetrics` ALREADY builds (`"listener." + normalizeAddr(rt.addr) + "."`, `internal/listener/manager.go:352`, today registering exactly two names), classifying the synchronous handshake error **ALREADY BOUND** at `manager.go:1178` with the success arm at the `:1183` fall-through. The **TWENTIETH** Observability-family row *(chain ordinal — the inherited off-by-one is KEPT deliberately, as phase 73 kept it; renumbering 74 alone would make it inconsistent with four landed `done` rows)*, the **FIRST since phase 59 that is NOT a tracing `custom_tags` capability** — phase 73's own §1.3 warned the `metadata`-MetadataKind line of cheap follow-ons was RETIRED and the next roller would have to leave the lineage; it did — and the FIRST in a long streak to BREAK the +0-stat envelope. **Every production edit lands in ONE file**: three `*stats.Counter` fields beside `downstreamCxTotal`/`downstreamCxActive` (`manager.go:175-176`), three registrations, one unexported `classifyHandshakeErr`, two call points. **THE HEADLINE — A 9-ROW-OLD DEFERRAL ADJECTIVE REFUTED BY EXECUTION.** ADR-0286 §Consequences **C3** (`DECISIONS.md:16908`) calls this family *"a framework-surgery row of its own"* that *"would blow the +0-stat envelope"*; **both halves are refuted.** Nothing has to travel anywhere — the handshake error is a plain SYNCHRONOUS return value at a site where the listener-scope stats handles are ALREADY in scope on the same `*listenerRuntime` receiver — and `errors.As(err, &*tls.CertificateVerificationError)` is an **EXACT, TOTAL discriminator** between the two failure arms (untrusted cert ⇒ `*tls.CertificateVerificationError`; no cert ⇒ a bare `*errors.errorString`, `tls: client didn't provide a certificate`). **PROVEN BY EXECUTION TWICE, INDEPENDENTLY, on separate Go toolchains and at BOTH TLS 1.2 and TLS 1.3**, with the discrimination criterion stated BEFORE the run (`reference_probe_must_discriminate`) and the client cert FORCE-SENT via `GetClientCertificate` (mandatory per `reference_go_client_cert_withholding` — an untrusted-cert arm without forced-send is a VACUOUS green); both arms error SYNCHRONOUSLY, so the row does NOT inherit the TLS-1.3 half-RTT client-side workaround `0108`/`0111` needed. ⇒ **the FOURTH consecutive row whose pick was materially changed by RE-DERIVING a deferral's cost adjective instead of re-reading it** (`reference_deferred_candidate_cost_restale`), and this one had gone NINE rows (65 → 74) untested. **THE DOCUMENTARY FINDING, recorded rather than silently fixed:** the OTHER half of the inherited blocker — an *"opaque `crypto/tls` handshake-error callback wired into every per-chain `*stdtls.Config`"* — **is NOT in ADR-0286 at all**; `grep -c 'VerifyPeerCertificate\|handshake-error callback' docs/envoy-go/DECISIONS.md` ⇒ **0**. It originated in BRAINSTORM prose at `phases/70-tracing-custom-tags-metadata/BRAINSTORM.md:70` (its fullest form, naming `VerifyPeerCertificate`/`GetConfigForClient`), was copied to `phases/71-…/BRAINSTORM.md:62`, and was upgraded to *"framework surgery **CONFIRMED**"* at `phases/72-…/BRAINSTORM.md:124` — **a BRAINSTORM adjective that acquired the authority of an ADR by sitting next to one across three rows** (`reference_code_comment_not_evidence` in its documentary form). ADR-0296 owes a correction ACROSS ADRs — a first for the project — phrased as a documentary finding, not criticism of phase 65, whose own text was accurate. **THE FAMILY SPLITS, and the split is itself a contribution:** `ssl.ciphers.*`/`ssl.versions.*`/`ssl.curves.*` SPLIT OFF and STAY DEFERRED on two named blockers — (1) **charset**: `stats.NamePattern` (`internal/stats/registry.go:48`) bans the hyphen, so the OpenSSL cipher form fails `IsValidName` and `checkName` **PANICS** (`registry.go:107`, the landed `reference_dynamic_stat_name_charset_guard` hazard); (2) **name mismatch**: Go's `tls.CipherSuiteName` yields IANA forms while Envoy yields OpenSSL forms, so a cross-side EXACT assertion is INFEASIBLE without a hand-maintained table whose mapped names then trip blocker (1). **Lifecycle is explicitly NOT a blocker** (`NewCounterIfAbsent`/`NewGaugeIfAbsent` at `registry.go:161`/`:208`, ADR-0117 post-`Freeze()` by design) — **the blocker is NAMING**; the surviving boundary is RESTATED at `BEHAVIOR_CONTRACT.md:928`, not deleted. **The RUNNER-UP was the `upstream_cluster` span tag, DEFERRED ON A RESIDUAL DIVERGENCE RATHER THAN ON COST:** a **+3-line** `NameOnlyEndpoint` variant was BUILT and PROBED (gofmt/build/vet/lint clean, **70/70 packages green with ZERO test edits**, all **10** tracing differential fixtures passing, cross-side **PARITY ACHIEVED** on the connect-refused `UF` and circuit-breaker `UO` arms), which **PARTLY REFUTED the FIRST adversarial verdict's "61-call-site thread" grounds**; it loses because the reference's built-in tag is **SELECTION-gated, not pick-gated** (probed live: `upstream_cluster='svc'` on a 503 `FI` local reply where no host was picked, `/direct` control ⇒ `None`) — **the OPPOSITE of the phase-73 CLUSTER `metadata` tag** (`reference_cluster_tag_gated_on_pick_not_route`) — leaving **5 span-capable zero-endpoint emit sites** (`connection.go:597`, `:699`, `h2dispatch.go:530`, `h3dispatch.go:280`, `:341`) that pass a LITERAL `cluster.Endpoint{}` through `SendLocalReply`/`chain.LocalReplyDone()` and never enter `doH1ClusterAction`. The 18-site emit-caller partition was RE-DERIVED exhaustively at tip as **6 pre-decision / 5 span-capable zero-endpoint / 7 real-`picked`**, and a DISCRIMINATING break nobody had named was identified for whoever takes it (`extractEndpoints`'s `la.GetClusterName()` vs the cluster's own `name` — `internal/cluster/manager.go:875`, sole non-test call site `:433` — equal in every landed fixture, so a crossed lookup stays GREEN). ⚠️ `phases/73-…/SPEC.md:375`'s claim that this row *"does NOT bring it closer"* is **FALSE**: phase 73's `Endpoint` retention pattern is exactly what makes a cluster name near-free to carry. **Rivals re-costed against source and all deferred:** pick-time `picked`-propagation (2-vs-4 lines CONTESTED against `BEHAVIOR_CONTRACT.md:690`; pool-overflow REFERENCE-UNPRODUCIBLE — a 2-task prelude, not a row) · `hcm.merge_slashes` (**10-13**; its "one pure string transform" adjective REFUTED BY EXECUTION — the reference rewrites the FORWARDED `:path` across four mutation points × three codecs — **and its blast radius contains a PRE-EXISTING H2 ROUTING BUG**: `internal/filter/hcm/h2/stream.go:440` uses `url.Parse` not `url.ParseRequestURI`, so H2 route matching for `//`-prefixed paths ALREADY diverges from H1/H3 today) · `access_log[].filter` (**10-13**; evidence upgraded INFERRED → DIRECT on both sides, so phase 73's *"split resolves AGAINST the candidate"* framing is RETIRED even though the row still loses on size) · `hcm.access_log_options` (**9-11**; its "plausibly cheaper" adjective REFUTED by the `statusCode == 0` guard in all three emit helpers) · the `stdout`/`stderr` loggers (**7-9**; the prior cite named a FILE THAT DOES NOT EXIST — the fallthrough is `internal/bootstrap/bootstrap.go:952-955` — and the REAL blocker is the SUBJECT side's undrained `cmd.StdoutPipe()`) · `stats_flush_on_admin` (**8-10**; the 53-call-site `admin.New` figure is the ONE prior adjective that SURVIVED, by compile census with `-gcflags=all=-e`, though 52 of 53 are TEST sites). **NEWLY SURFACED, NONE chartered:** a **swallowed-panic BOOT HANG** (two HCM listeners sharing a `stat_prefix` ⇒ `internal/stats/registry.go:107` panics from `internal/filter/hcm/config.go:358`, SWALLOWED by the deferred `<-flusherDone` at `cmd/envoy-go/main.go:299`, so the process neither crashes nor boots — a direct hazard to any future multi-listener fixture) · the access-log sink flattening PROVEN BY EXECUTION and DOWNGRADED to a fixture-design constraint · HCM `server_name`/`via` + a default-config `Server` OVERWRITE-vs-APPEND defect · route-level header manipulation · `RouteAction.prefix_rewrite`/`host_rewrite_*` (whose "all error" claim at `phases/04-http-1.1/SPEC.md:41` is STALE AND WRONG at tip) · the default-config `x-forwarded-proto`/`x-request-id`/`x-envoy-expected-rq-timeout-ms` injection divergence. **Envelope anticipated:** **+3 stats (1201 → 1204)** — the first non-zero delta in this lineage; breaking the +0 streak is CORRECT, since an envelope is not an argument — +0 fixtures (**119**; `test/fixtures/0111-tls-cvc-empty-dynamic-fallback/` EXTENDED with a cross-side DELTA `fixture.StatsAsserter`, since it ALREADY drives all three arms against one `require_client_certificate: true` listener and its own `README.md:162-163` confesses *"No `ssl.*` stats … a verdict `StatsAsserter` is infeasible"* — **so the row RETIRES a boundary the fixture itself confesses**), +0 fuzzers (**55**), +0 BackendKinds (**38**), +0 go.mod modules (**2**), **ZERO new packages**; ONE ADR (**ADR-0296**, §Context at the SPEC per ADR-0044). A SINGLE FLAT ROW of ~8 tasks anticipated, with an INVERTED `TestNoNewStat`-style registration guard OWED (`internal/statssink/registration_test.go:26`). ⚠️ **NO probe of any kind was run against the reference for this row's own subject, and NO build of the row exists, in-tree or in scratch** — the `ssl` seam analysis is a READ-ONLY derivation plus an ISOLATED stdlib probe run entirely outside this repository's build; nothing in the BRAINSTORM may be cited as execution evidence for buildability (§11.1 says so explicitly). ⚠️ **The reference's `ssl.*` scope/spelling/semantics are DOC-SOURCED from phase 65, NOT re-executed** — `phases/65-xds-sds-validation-context/SPEC.md:270`/`:278` carry VERBATIM `listener.0.0.0.0_10000.ssl.fail_verify_error: 1` and `…ssl.fail_verify_no_cert: 1`, and `:282` a full SIX-name roster including a FOURTH name nobody had flagged (`ssl.certificate.validation_ca.expiration_unix_time_seconds`, a per-secret gauge, explicitly out of scope). **Row 74 registered `in-progress`** at this stage-close commit per the ROADMAP §Schema invariant (plus a phase-74 charter sentence APPENDED to the `### Observability family` heading paragraph — the phase-70/72/73 paired-edit precedent). **NO deferred sentence narrowed at ANY pre-IMPL stage** (phase-57 precedent) — proven MECHANICALLY: the ROADMAP diff is exactly **2 insertions / 1 deletion**, the family-paragraph edit is a **PURE APPEND** (the old line is a byte-prefix of the new, **+4410** chars), and the deferred sentence is **BYTE-IDENTICAL** across the diff. The `ssl` clause SHORTENS (three of five sub-names roll out; `ciphers`/`versions` SURVIVE) at the **phase-74 IMPL only**, and check (2) must STAY **3** — checked HERE by READING the live sentence (the clause is the FIRST of two `+`-joined members and its parenthetical retains two sub-names); **re-confirm at the IMPL anyway, because a read is not a diff.** **DECISIONS.md BYTE-UNTOUCHED.** **Counts UNCHANGED at this BRAINSTORM (docs-only; re-run MECHANICALLY in the worktree at this close with the canonical commands):** fixtures **119** (`ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0117-tracing-custom-tags-metadata-cluster`) · fuzzers **55** (`grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`) · stat surface **1201** (BEHAVIOR_CONTRACT doc count; NO mechanical command) · BackendKind **38** · DECISIONS tail **ADR-0295** COMPLETE (next-free **ADR-0296**) · go.mod modules **2** (the phase-61.2 lineage figure; the single `go.mod` requires **67**). **Sentinel re-run MECHANICALLY at this close (does NOT fire; `stop` NOT created):** (1) prints **`NOT DONE: row 74`** — the registration RE-OPENS check (1) after its first-ever silent close; (2) `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:'` ⇒ **3**; (3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM` (the `WASM` line remains the ROADMAP bookkeeping artifact, deliberately LEFT AS-IS). All three print ⇒ abundant work remains. **Next → the phase-74 SPEC** (the D-TLSHS-* live-probe arms against `envoyproxy/envoy:contrib-v1.37.2` — **SCOPE, SEMANTICS, REGSCOPE and ALERTMAP are the four that matter**; RE-DERIVE every §6 edit site at the SPEC tip; RE-RUN the `errors.As` discriminator at the SPEC's own toolchain; decide the QUIC/H3 coverage boundary; draft ADR-0296 §Context incl. the C3 documentary correction).

- **prior active-phase:** `phase 73 (tracing-custom-tags-metadata-cluster) IMPL done` (2026-07-24) — lifecycle-state 3 → DONE; **ROW 73 FLIPS `in-progress` → `done`** at this IMPL six-gate (ADR-0106, the SOLE leg — a SINGLE FLAT ROW, `reference_roadmap_split_phase_row_done`); **ADR-0295 COMPLETED IN PLACE** (§Decision + §Consequences APPENDED after the RETAINED hand-off footer; tail stays **ADR-0295**, no renumber, no new ADR; next-free **ADR-0296**). Fresh worktree `.worktrees/phase-73-impl` off master `bea4178c` (the phase-73 PLAN squash), branch `phase-73-tracing-custom-tags-metadata-cluster-impl`; subagent-driven (`feedback_execution_style`) — one implementer per task + a per-task adversarial reviewer, each committing LOCALLY only (`feedback_subagents_no_push`), controller squash-push at close. Each task RE-DERIVED its anchors at the IMPL tip (a PLAN is not evidence either). **The 9-task spine landed** (each red-first → green → break-verified): **T1** (`f6f3cede`) `internal/cluster` — a SECOND unexported `Endpoint.clusterFilterMetadata map[string]*structpb.Struct` field ALIASING `c.GetMetadata().GetFilterMetadata()` + a nil-safe `ClusterMetaLookup(ns)` accessor beside phase-72's `MetaLookup`, populated by a LOOP in `buildCluster` placed IMMEDIATELY after the `extractEndpoints` err guard and **BEFORE ALL LB CONSTRUCTION**; ALIASING proved MECHANICALLY (a brand-NEW namespace mutated post-`buildCluster` is visible through the accessor ⇒ the map HEADER is shared, not just its values); `unsafe.Sizeof(Endpoint{})` **112 → 120** MEASURED; **ZERO new imports anywhere**; `extractEndpoints`'s signature + `manager_test.go` BYTE-UNCHANGED and `manager.go`'s diff PURELY ADDITIVE (zero `-` lines). **Break D — THE ROW'S DISTINGUISHING BREAK — FIRED WITH EXACT DISCRIMINATION:** moving the loop after `newSubsetLB` failed `SubsetChild` and `SubsetDefaultFallback` while `RoundRobinPathStaysGreen`, populate-live, aliasing, projection and pool-HIT provenance ALL stayed green — the placement constraint is enforced by execution, not by comment; and the PLAN's warning that Break D is VACUOUS through `Cluster.PickEndpoint()` was RE-CONFIRMED LIVE at this tip (a subset-configured `PickEndpoint` returned `ok=true` under the break), so test 3 drives the package-internal `subsetLB.Pick(…, hasMatch=true)`. Breaks A/B/C also fired · **T2** (`1e74f996`) `kindMetadataCluster` APPENDED at **iota == 6**, EXECUTION-proven — the pre-known red was EXACTLY TWO `/cluster` subtests and its output PRINTED **`Kind:6`** — plus the CLUSTER accept arm (a byte-clone of the HOST accept, `MetaPath` FULL) replacing the `:268-269` reject, **the LAST kind-specific reject**; gofmt re-aligned exactly `:58`-`:61` as predicted; the stale comments narrowed incl. `:185` AND `:190`, whose "ADR-0080-distinct" claims go FALSE because `:233` and `:288` now COLLIDE on `kind required`; breaks E/F/G/H/H2 all fired (F fired BOTH properties independently; G fired exactly the two iota pins and nothing else) · **T3** (`91f354d8`) the nil-tolerant one-arg `clusterMetaLookup` **6th** param (the 3rd/4th/5th lookups UNTOUCHED) + a `kindMetadataCluster` arm descending the **FULL** `MetaPath`, so three of the four kinds now walk the full path; `descend`/`structpbValueToString` reuse proven by sha256 and the landed HOST arm byte-identical at its shifted span with exactly ONE removed line; 49 `resolve_test.go` callers += trailing `nil` (raw grep 50; `:107` a `t.Errorf` literal, gated by LINE); breaks I/J/K/K2 fired, **Break I's REQUEST/ROUTE/HOST half staying ENTIRELY green ⇒ the four arms are not entangled**; the agent ADDED **K2b** (crossing inside the production arms rather than at a test call site, because K2a as written edits the very test that catches it) — both fire, compiler silent both ways · **T4** (`97428088`) `picked.ClusterMetaLookup` at `accesslog_emit.go:57`/`:118`/`:179` and NOWHERE else — **THREE production lines**; **the SEVEN-file sha256 envelope HOLDS** (`connection.go`, `h2dispatch.go`, `h3dispatch.go`, `chain.go`, `subset.go`, **`actions.go`**, **`route.go`** — the last two precisely what the REJECTED route-carried seam would have touched, so the seam decision is honoured MECHANICALLY) plus `manager_test.go`, and all three emit SIGNATURES and all **18** emit callers are BYTE-UNTOUCHED; breaks L/M/M2 fired, **M2 the one that mattered — the compiler AND `go vet` AND `golangci-lint` were ALL SILENT on a 5th/6th argument crossing, and the distinctness test fired with the CROSSING SIGNATURE (values transposed: `dual_host="from-cluster"`, `dual_cluster="from-host"`), which is what identifies a crossing rather than a mere omission** · **T5** (`ba92316c`) an ADDITIVE `meta_cluster_ok` seed + the MANDATORY `meta_bad` REPOINT to `Kind: &metadatav3.MetadataKind{}` (present kind, oneof unset — the `:288` PARITY arm), mandatory because as-written the seed carried NO `MetadataKey` and would post-lift reject SILENTLY on `empty namespace` — itself the newly-found absent-`metadata_key` DEPARTURE; **dispatch-verified with THREE hypotheses stated BEFORE the run and the arms TAGGED SEPARATELY** (`:233`/`:288` now COLLIDE on `kind required`, so the error text alone would launder the result), printing `PROBE ARM=CLUSTER_ACCEPT tag=meta_cluster_ok` / `PROBE ARM=DEFAULT_UNSET_KIND tag=meta_bad` with `meta_bad` still LAST; **two hypotheses REFUTED by that run**; +0 fuzzers · **T6** (`849632c0`) the OTLP fixture **`0117-tracing-custom-tags-metadata-cluster`** — TWO clusters with different `metadata.filter_metadata` behind `prefix: /a` and `prefix: /b` onto the SAME single backend, `BackendCount` 1, zero LB spread. **The partition mechanism is `http.url` (PRIMARY); the named batch-poll fallback was NOT needed** — it renders BYTE-IDENTICALLY on both sides (`"http://trace.example/a"` / `".../b"`) across all 24 spans, partitioning 6/6 per side with ZERO unclassified (the driver still matches by SUFFIX, since `0116` declined to pin `http.url` by value), and the partition sizes are `Fatalf` PRECONDITIONS so a broken partition can never make the value assertions dead code. **Breaks N/O/P/Q/R ALL fired, with Q — the SELECTION break — firing on BOTH paths (24 firings) ⇒ the fixture DISCRIMINATES *selected* from *only*, the exact limitation `0116` had to confess**, and R making BOTH sides fall to default ⇒ CLUSTER ≠ HOST cross-side; no substitutions needed. The `upstream_cluster` framework gap was re-confirmed LIVE in the same dump (reference `"c_backend_a"`/`"c_backend_b"`, envoy-go `""`), which is why it could not serve as the discriminator; fixtures 118 → 119 · **T7** (`b9225e08`) BEHAVIOR_CONTRACT **B1-B5** — four hunks in a 5730-line file, line count unchanged, exactly ONE new `upstream_cluster` mention (inside B3(i), reference-side phrasing) · **T8** the six-gate · **T9** ADR-0295 IN PLACE + row-73-`done` + the deferred-sentence narrow + this roll. **Six-gate ALL GREEN:** gofmt SILENT / vet 0 / build 0 / `go mod tidy -diff` EMPTY + `go.mod`/`go.sum` NO DIFF vs master (**+0 modules — ZERO new imports anywhere**) / golangci-lint 0 / **the FULL 119-dir differential `ok 410.259s` EXIT 0, ZERO failures**, `0117` CONFIRMED to run BY NAME / the layering check EMPTY — ⚠️ **a DISCIPLINE check, NOT a cycle check** / `-race` green on all three touched packages, the PRE-EXISTING `outlier_test.go:766` flake NOT reproducing / **exactly ONE new exported symbol** by AST set-diff (`internal/cluster` 58 → 59; `internal/tracing` 49 → 49 with only the `ResolveCustomTags` signature growing; `internal/filter/hcm` 6 → 6) / the 18-caller roster re-derived **5+6+7** with the gated regex / the fixture tree exactly 5 files, NO `scripts/` / `git status` and `git clean -nd` EMPTY / nothing changed outside `internal/`, `test/`, `docs/`. **THREE PLAN INSTRUCTIONS WERE REFUTED BY EXECUTION, recorded rather than quietly fixed:** (1) **T4's `span_emit_test.go ⇒ 21` caller-count gate is UNSATISFIABLE BY CONSTRUCTION** — every T4 test drives through `emitAccessLog`/`H2`/`H3`, so each new test IS a new caller (landed 13 + **29** = 42, not 34); **this is the SAME defect class the PLAN diagnosed TWICE for sha256 gates and then re-committed one level down** when it downgraded sha256 → caller-count, and the invariant actually intended was verified in a STRONGER form (the 21 pre-existing callers byte-identical AT UNCHANGED LINE NUMBERS, the first 779 lines hashing identically to master, ZERO deletion lines in both emit test files). (2) **T1 invalidated a landed BEHAVIOR_CONTRACT citation the PLAN never flagged** — the B2 pool-overflow sentence cited `cluster.go:722`; T1's +23 lines shifted it to `:745`, corrected in place at T7, with a full sweep of every `.go:NNN` cite in that region finding no others stale. (3) **Break B needs TWO breaks at TWO LAYERS, not one** — test 1(e) hand-constructs the `Endpoint` and never calls `buildCluster`, so a loop-level wrong-source break fires the BUILD-level assertions while `TestEndpoint_ClusterMetaLookup` stays GREEN; only an ACCESSOR-level wrong source fires 1(e). **NO break failed to fire at any task.** **FOUR stale claims corrected at T9, none propagated:** the ROADMAP row-73 cell's three (the narrow-hazard sentence — the narrow SHORTENS the clause, leaving FOUR candidates with check (2) STAYING 3; the "only the two unset-kind arms remain" clause; the single-cluster `0117` description) **plus the ADR-0295 HEADING's two over-claims**, which the SPEC's own §16 asserted were already fixed and were not — the body had been corrected and the heading had not, making it the SOLE surviving carrier repo-wide. ADR-0294 `:17209` gained a bracketed `[CORRECTED at the phase-73 IMPL: …]` note per ADR-0294's OWN §Context precedent, retiring its refuted "CLUSTER RE-PAYS the 18-caller threading tax" close. **Counts at exit (re-run MECHANICALLY):** fixtures **118 → 119** (tail `0117-tracing-custom-tags-metadata-cluster`) / fuzzers **55 (+0)** / stat surface **1201 (+0)** / BackendKind **38 (+0)** / go.mod modules **2 (+0)** / **ZERO new packages** / DECISIONS tail **ADR-0295** COMPLETE (next-free **ADR-0296**). **The Observability family STAYS OPEN** — the `CLUSTER` MetadataKind rolled OUT of the live `candidates:` sentence at this row-done edit (phase-57 precedent), **CLOSING the quartet**: all four `metadata` MetadataKinds are consumed and no MetadataKind rejects. ⚠️ **The two deferred items this row newly surfaced are DELIBERATELY NOT in the `candidates:` sentence** — the absent-`metadata_key` DEPARTURE and the two error-string COLLISIONS live in BEHAVIOR_CONTRACT B2 + ADR-0295 §Consequences, so nothing is lost, but the omission is recorded rather than silent. **Sentinel re-run MECHANICALLY (does NOT fire; `stop` NOT created):** (1) prints **NOTHING** — row 73 was the LAST non-`done` row, the NORMAL post-IMPL state and NEVER on its own a termination condition; (2) ⇒ **3**; (3) prints `NEVER OPENED: gRPC/Runtime/WASM`. ⚠️ **NEW: check (1)'s blind spot is FOUR of 105 rows, not two of 103, and has a THIRD cause** — `28.1a`/`28.1b` carry a LETTER SUFFIX the `[0-9.]+` number field cannot match, and the prior figure came from a set-difference whose own baseline regex ALSO excluded them; all four blind rows are `done`, so no current impact. **Next → the phase-74 BRAINSTORM** (the roller SELF-PICKS per the 2026-07-12 standing directive).

- **prior active-phase:** `phase 73 (tracing-custom-tags-metadata-cluster) PLAN done` (2026-07-24) — lifecycle-state 2 → 3; docs-only (`PLAN.md` + `PROGRESS.md`, the ONLY two files in the squash's phase-directory delta — **ZERO production `.go`, DECISIONS and ROADMAP UNTOUCHED**, row 73 STAYS `in-progress`, ADR-0295 stays PROPOSED); fresh worktree `.worktrees/phase-73-plan` off master `79f96455` (== the phase-73 SPEC squash; **NO parallel stream** — `git diff --stat d51d33dc 79f96455 -- '*.go' go.mod go.sum test/` EMPTY, so the production tree was byte-identical to what the SPEC re-derived), branch `phase-73-tracing-custom-tags-metadata-cluster-plan`, per `feedback_git_worktrees`; subagent-driven. The stage produced the NINE-task single-flat-row TDD spine (T1-T9), each task red-first → green → break-verified with its own pre-known red set and exit gate, plus **EIGHTEEN pinned breaks (A-R)**. **TWO read-only agents on disjoint remits RE-DERIVED every SPEC §3/§9/§10/§11/§12/§14 anchor at the PLAN tip: ZERO DRIFT and ZERO REFUTED on every CODE anchor**, plus NINE new findings (F1-F9) — three stale-claim carriers the SPEC leaves unassigned, two `old_string` uniqueness hazards, and one that verified the populate-placement hazard's MECHANISM rather than inferring it. **THE HEADLINE — THE ADVERSARIAL PASS REFUTED THE PLAN'S OWN INSTRUCTIONS TWICE, AND ONE OF THOSE REFUTATIONS CAME FROM EXECUTION.** V1 built T1→T4 from the PLAN's skeletons pasted VERBATIM in private scratch and found (a) **`manager_test.go` was scheduled for four test additions while sha256-gated BYTE-UNTOUCHED in six places** — an unsatisfiable gate, and the identical defect the PLAN had already diagnosed for the emit test files without applying it to itself (T1's tests now land in a NEW `cluster_meta_test.go`; the gate is KEPT); and (b) **Break D — the row's DISTINGUISHING break — stays GREEN through `Cluster.PickEndpoint()`**, because that path passes `hasMatch=false` so `subsetLB.Pick` never consults `s.subsets` and falls through to a fallback that ALIASES the original slice (test 3 is now pinned on the package-internal `subsetLB.Pick(…, hasMatch=true)`, zero new exported symbols). V2 independently REFUTED the PLAN's claim that sentinel check (1) does not go silent at this row's IMPL close — **row 73 is the LAST non-`done` row**, so it does, and that is the NORMAL post-IMPL state, never on its own a termination condition. V1 also CONFIRMED the row's headline claims BY EXECUTION: the seven-file sha256 envelope, ZERO new imports and +0 modules, `sizeof(Endpoint)` 112 → 120, exactly ONE new exported symbol, T4's three-error entry state (the `ResolveCustomTags` arity, **NOT** the EMIT-signature arity), the 49-caller `resolve_test.go` transform, and that Breaks K2/M2 COMPILE CLEAN when the adjacent identically-typed lookup args are swapped — proving the distinctness tests are load-bearing rather than decorative. **ONE SEVERE, TWO MODERATE, ONE MAJOR-process and TEN MINOR findings, all folded** (PLAN §1.4); the controller re-verified every load-bearing correction at the tip before folding and found one further defect itself (a FOUR-vs-three copy-site mismatch). **Explicitly NOT verified, and recorded as such so the IMPL inherits no false confidence:** nothing reference-side (no Docker, no probes — every reference/PGV claim rests on the SPEC's own probe record), T5-T7 and T9 unexecuted, Breaks N/O/P/Q/R untested, and **T6 Step 1's `http.url` span-partition reconnaissance unaddressed — the IMPL's first live risk**, since `upstream_cluster` is unusable as a discriminator (envoy-go emits it EMPTY). Sentinel re-run MECHANICALLY TWICE (worktree + landed master post-push): does NOT fire; `stop` NOT created. Counts UNCHANGED.

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
