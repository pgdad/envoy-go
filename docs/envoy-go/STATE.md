# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `04-http-1.1`
- **phase-directory:** `docs/envoy-go/phases/04-http-1.1/` — exists; contains `SPEC.md` (drafted in commit `59e6f25` and approved by the spec-document-reviewer subagent in the same session per ADR-0004; verdict APPROVED with advisory recommendations only — no blocking issues; coverage cross-check: BOOTSTRAP §8 row-04 elements all ✓; phase-03 REVIEW M-1..M-8 carryovers all ✓ triaged in §12; §3 gates a/b/c/d/e/f all ✓ specified). `PLAN.md`, `PROGRESS.md`, `REVIEW.md` do not yet exist. Phase 03's directory at `docs/envoy-go/phases/03-tls/` is closed and read-only history.
- **lifecycle-state:** `2` — `SPEC.md` exists, reviewer-approved, `PLAN.md` does not. Per `BOOTSTRAP_PROMPT.md` §5 state 2, the next session invokes `superpowers:writing-plans` (in adapted autonomous mode per ADR-0005 — no Execution Handoff question, plan-document-reviewer subagent loop retained, plan location override to `docs/envoy-go/phases/NN-slug/PLAN.md`) and produces `PLAN.md`. Per §5 state-2 GATE, if the resulting `PLAN.md` exceeds ~25 tasks or ~1500 LoC estimated, the session splits per §6 instead. ROADMAP row 04 status flipped `planned` → `in-progress` (this commit) per §4.1 invariant 3 (a phase enters `in-progress` once its phase-directory exists; the directory was created at SPEC drafting and the row flips at the state-2 entry commit, mirroring phase-03's `df445a5` pattern). Phase 04 work to date: SPEC drafted at `59e6f25` covering 503 lines / 34 headings; eight anticipated ADRs labelled H–O (numeric ADR-0037..ADR-0044 expected, planner verifies at PLAN time); fixture `0003-http11-routing` design captured in §4.3 / §5.8 / §5.10 with 27 requests/proxy distributed across three routes; phase-03 REVIEW Minors triaged in §12. Note on prior-STATE M-1..M-8 fabrication: master STATE.md at commit `230fef6` contained an M-1..M-8 description that did not match the actual phase-03 REVIEW.md (`d45c467`) Minor list — that fabrication is now silently corrected (this STATE block does not re-cite the wrong list); SPEC §12 cites the actual REVIEW.md content.
- **next-skill:** `superpowers:writing-plans`
- **next-skill-scope:** Draft `docs/envoy-go/phases/04-http-1.1/PLAN.md` from `SPEC.md` per ADR-0005 (autonomous-planning adaptation): no Execution Handoff question, no human interaction; the plan-document-reviewer subagent gate is retained; the location override is to the project layout; default executor preference is `superpowers:subagent-driven-development` (set as `next-skill` once PLAN approved, per ADR-0005 + the user's standing preference for subagent-driven execution). Convert SPEC §4 deliverables into ~10–20 ordered tasks with explicit dependency arrows, per-task acceptance criteria (build / test / fixture-gate), red→green TDD checkpoints, ADR-N landing site for each anticipated ADR (H–O), and a §"ADRs introduced by this plan" header that pre-commits the H→numeric mapping (anticipated ADR-0037..ADR-0044 — verify at PLAN time that DECISIONS.md tail is still ADR-0036). Settle SPEC §10's 14 deferred decisions as PLAN-time choices: recommended (a)-style picks for #1–#3 (route-match interface, action interface, sentinel-error close-after-action signal); land #4 as ADR-M (HTTPExpectations interface extension — reusable in phase 05) unless the planner judges runner-side change too invasive; #5 = explicit catch-all 404 (matches Envoy's typical config posture); #6 = stdlib `http.Server` for in-process HTTP echo backends (test fixtures, not subjects); #7 = fixed in-code allow-list; #8 = `time.Now().UTC().Format(http.TimeFormat)` per response (no caching at phase 04); #9 = `string` field on `Filter`; #10 = keep everything inside `internal/filter/hcm/` and amend the `internal/http/doc.go` placeholder to point at hcm; #11 = verify ADR-0036 is the current tail (it is, per DECISIONS.md line 1146), assign ADR-0037..ADR-0044; #12 = reaffirm `Server: envoy` (no superseder of ADR-0014); #13 = `Content-Type: text/plain` for both direct_response and local replies; #14 = HTTP echo body `backend-<idx>:<n>` per §5.10. The reviewer-subagent advisory items (per the `Notes for the planner` block of the spec-review output) the planner should address: (i) verify phase-03 REVIEW Important findings I-1..I-4 actually landed in commits `98cc35b`/`cbfe275` via `git log --oneline -- internal/tls/params.go internal/listener/manager.go test/fixtures/0002-tls-tcp/driver/driver.go` — if any I-N is unfixed, add as PLAN.md Task 0; (ii) decide whether `internal/http/` placeholder stays / is amended / removed at PLAN time; (iii) record whether a phase-04.x HTTPS sub-phase is in scope (the HTTPS deferral target in §2 is "phase 04.x or 05.x or a dedicated HTTPS-fixture sub-phase" — planner picks); (iv) the `HttpConnectionManager` proto field set in go-control-plane may differ slightly from what SPEC §9 enumerates; planner verifies against the proto at PLAN time. Work from a fresh worktree per "always use git worktrees" project convention: `phase/04-http-1.1-plan` off this commit's tip. Master and `phase/04-http-1.1-spec` share HEAD at this commit per ADR-0003 (fast-forward applied in the same session).
- **last-commit:** 59e6f25 (the phase-04 SPEC.md drafting commit on `phase/04-http-1.1-spec`; the state-2 entry commit that lands ROADMAP row-04 flip + this STATE block fast-forwards master in the same session — the next session opens at master = `phase/04-http-1.1-spec` HEAD = state-2-entry SHA).
- **last-updated:** 2026-04-25

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
