# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `13-http-filter-buffer` — phase 13 PLAN.md authored at this commit per BOOTSTRAP_PROMPT.md §5 lifecycle-state-2 → 3 transition (skill `superpowers:writing-plans` per ADR-0005). The PLAN mirrors phase 12's structural template section-for-section, adapted for phase 13's surface: 13 tasks (Task 1 PROGRESS preamble + Task 2 package skeleton + parsePerRoute [ADR-0125, ADR-0126] + Task 3 DecodeHeaders body [ADR-0127 v2] + Task 4 DecodeData + maybeAddContentLength + DecodeTrailers + Group 4-6 tests + Task 5 fuzzer + Task 6 main.go register + Task 7 fixture infrastructure + Task 8 backend JSON-echo + Task 9 envoy.yaml/envoy-go.yaml + Task 10 expectations.yaml/README.md + Task 11 driver 6-scenario + Task 12 BEHAVIOR_CONTRACT 4-edit + 6-gate phase-done + Task 13 REVIEW.md). ROADMAP row 13 stays `in-progress` (ROADMAP flip is per `## Task 12` at phase-done; PLAN landing does NOT change ROADMAP status). Phase 13 is the SIXTH §9 family-row to reach lifecycle-state-3 (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11, csrf @ 12); the §9 family heading at ROADMAP line 56 stays unchanged per ADR-0106(c).
- **phase-directory:** `docs/envoy-go/phases/13-http-filter-buffer/` — contains BRAINSTORM.md (~798 lines after §12 amendment + advisory-rec follow-up; §§1-11 PRESERVED VERBATIM per D-3.5; §12 supersedes §§1.1, 2.5, 2.6, 2.7, 2.8, 6, 7, 8, 11) + SPEC.md (~1343 lines; resolves the §unresolved counter-name issue via option (a) inline) + PLAN.md (authored at this commit; ~2046 lines; 13 tasks). PROGRESS.md, REVIEW.md authored over remaining phase lifecycle (states 4 → 6). The PLAN settles all 4 SPEC §12 deferred decisions (D1 SetDecoderCallbacks; D2 single-file `buffer.go`; D3 envoy-go-own-wording validation errors; D4 backend JSON-echo for fixture scenario 6 CL-injection assertion) plus 7 PLAN-emerging items (D5 decoder-only `HTTPFilter` value; D6 single-listener-3-routes fixture topology; D7 `HTTPBuffer BackendKind = 12`; D8 chunked body via `req.TransferEncoding`; D9 backend lowercase canonical headers in JSON echo; D10 Go stdlib transparent 100-Continue; D11 `effectiveMax` + `accumulated` field types `uint32`). Plan-document-reviewer dispatched per ADR-0005 §Decision 1; verdict **Approved** (1 of max 3 review iterations); 1 advisory recommendation applied inline at Task 11 (allow-list filter-out mechanism clarification — runner enforces automatically per phase 06.1 §11.5 + phase 11/12 precedent). Differential fixture `0015-http-buffer` 6-request matrix per SPEC §7.1; FuzzBufferConfigParse is the 17th fuzzer in the repo per SPEC §14.3. The 3 ADRs (ADR-0125, ADR-0126, ADR-0127 v2) anchor at impl-time per ADR-0044 (ADR-0125 + ADR-0126 at Task 2; ADR-0127 v2 at Task 3); ADR-0128 stays retired.
- **lifecycle-state:** `phase 13 PLAN.md authored; implementation incomplete` — phase 13 BRAINSTORM.md authored at parent commit `6cf412e`, amended via §12 at parent commit `3915338`, advisory-rec follow-up at parent commit `37d4dfa`, follow-up STATE.md update at parent commit `812d234`. SPEC.md authored at parent commit `f5d38fa` (SHA-fill follow-up at `6e39444`). PLAN.md authored at this commit per BOOTSTRAP_PROMPT.md §5 step 2 → step 3 transition + ADR-0005 (autonomous-planning adaptation). Plan-document-reviewer dispatched per ADR-0005 §Decision 1 + writing-plans skill review-loop discipline; verdict **Approved** (no blocking issues; 1 advisory rec applied inline at Task 11; 1 advisory rec on Task 6 line numbers already mitigated by precondition 15). Lifecycle-state-3 → 4 transition (BOOTSTRAP_PROMPT.md §5; impl via `superpowers:subagent-driven-development` per ADR-0005 §Decision 4 + project memory user preference) is the next session's responsibility. Phase 12's six gates remain green at base commit `a782fc9`; this commit chain stays docs-only (no code changes; no test re-validation needed).
- **next-skill:** `superpowers:subagent-driven-development` per ADR-0005 §Decision 4 + project memory user preference (`feedback_execution_style.md`: "user always wants subagent-driven over inline execution for plans"). The impl session follows the phase 09/10/11/12 precedent: spawns a fresh impl worktree at `.worktrees/phase-13-http-filter-buffer-impl` branched off the PLAN tip; Task 1 verifies the 16 preconditions; Tasks 2-13 land per the PLAN's task headers + step-by-step bodies; per-task subagent dispatch with two-stage review between tasks per the subagent-driven-development discipline; phase-done at Task 12 (6-gate verification + ROADMAP row 13 done + STATE.md advance to `awaiting next planning`); REVIEW at Task 13 closes the row.
- **next-skill-scope:** Cold-start scope for the phase 13 impl session. Read first: `docs/envoy-go/phases/13-http-filter-buffer/PLAN.md` (the authoritative input; 13 tasks; ~2046 lines; the per-task headers + steps + commit messages are all enumerated); `docs/envoy-go/phases/13-http-filter-buffer/SPEC.md` (per-component contract bodies §6.1-§6.8 are the load-bearing test surface; §7.1 6-scenario fixture matrix; §11 empirical-pin transcripts for cross-reference; §13 BEHAVIOR_CONTRACT 4-edit bundle for Gate F at Task 12; §15 acceptance checklist for Task 13 REVIEW); `docs/envoy-go/phases/13-http-filter-buffer/BRAINSTORM.md` §12 amendment for context; `docs/envoy-go/phases/12-http-filter-csrf/PROGRESS.md` (most-recent impl precedent — task-by-task verbatim outputs, ADR landing pattern, per-task follow-up convention for code-review-issue fix-ups). Highest-leverage PLAN sections: `## Execution preconditions` (16-precondition cold-start verification — must all pass); `## Planner-time deferred-decision resolution` (the 11 settled decisions reproduce in PROGRESS.md preamble at Task 1); `## ADRs introduced by this plan` (anchor schedule + 11 cross-reference items); Task 2-4 step-3 code blocks (verbatim Go code for the filter implementation; ~280-330 LoC `buffer.go`); Task 11 step 1 (driver 6-scenario orchestration with the §11.8-CL load-bearing CL-injection assertion in scenario 6); Task 12 step 4 (verbatim 6-gate verification commands). Anticipated impl wallclock per phase 12 PROGRESS precedent: ~6-12 hours of focused work spread across the 13 tasks; ~2-3 ADRs each take ~30-45 min to author per ADR-0001 7-section template.
- **last-commit:** `a8bd93c` — `phase 13: PLAN.md authored; lifecycle-state-2 → 3 transition`. Authors `docs/envoy-go/phases/13-http-filter-buffer/PLAN.md` (~2046 lines, 13 tasks) per BOOTSTRAP_PROMPT.md §5 step 2 → step 3 transition + ADR-0005. Mirrors phase 12's PLAN structural template section-for-section (Scope check / File structure / Planner-time deferred-decision resolution / ADRs introduced / Execution preconditions / 13 task headers); settles 4 SPEC §12 deferred decisions (D1-D4) plus 7 PLAN-emerging items per the planner-time-decision discipline. Plan-document-reviewer dispatched per ADR-0005 §Decision 1; verdict Approved (no blocking issues); 1 advisory rec applied inline at Task 11 (allow-list filter-out mechanism clarification); 1 advisory rec on Task 6 fixed-line-numbers already mitigated by precondition 15. ROADMAP row 13 stays `in-progress` (the ROADMAP flip is per Task 12 at phase-done time, NOT at this PLAN-landing commit). The 3 ADRs ADR-0125 + ADR-0126 + ADR-0127 v2 are NOT landed at this commit per ADR-0044 ADR-on-impl convention (they anchor at Task 2 + Task 3 of the impl session). Docs-only commit; no code changes; phase 12's six gates remain green at base commit `a782fc9`.
- **last-updated:** 2026-05-09

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
