# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `16-http-filter-rbac` — phase 16 PLAN.md authored at this commit. Phase 16 stays the NINTH §9 family-row (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11, csrf @ 12, buffer @ 13, compressor @ 14, bandwidth_limit @ 15) and the FIRST §9 row since phase 14 to introduce non-zero framework deltas — TWO new primitives confirmed at SPEC time + carried forward by PLAN at dedicated impl-time tasks: (i) TLS-principal accessor `DecoderFilterCallbacks.DownstreamPrincipal() []string` anchored at ADR-0144 (Lands-in-task: Task 6 — `callbacks.go` interface + `chain.go` plumbing + `hcm/connection.go` + `hcm/h2dispatch.go` TLS state extraction + Group 7 tests); (ii) matcher-engine evaluator at new top-level `internal/matcher/` package anchored at ADR-0142 (Lands-in-task: Task 3 — NEW package + `Matcher` type + `New(tree, supportedActionTypes)` + `Evaluate(MatchContext)` + parse-time PARSE-REJECT for unknown terminal TypeURLs). PLAN re-evaluated ADR-0045 split-gate per SPEC §1.1 deferral + §11.Q1 resolution: production LoC estimate **~1208-1608 LoC borderline crossing the 1500 high-end** (rbac.go ~350-450 + evaluator.go ~650-850 + doc.go ~30 + internal/matcher/{doc,matcher}.go ~175-275 + framework deltas ~70 + main.go +3); task count **16 well under 25**; **PLAN ships SINGLE-ROW** per phase-13/14/15 borderline precedent (task count is load-bearing trigger + splitting fragments a single coherent filter + framework primitives belong with their consumers). All 7 anticipated ADRs (ADR-0140..ADR-0146) anchored to impl-time tasks per ADR-0044 + ADR-0125 §(xii) amendment paragraph anchored at Task 10 (planner-time decision 14 — NOT pre-landed at SPEC commit despite SPEC §5.4 prose claim; PLAN re-anchors at impl-time per ADR-0044 generalization + phase-13 ADR-0127-v2 in-place-update-at-impl-time precedent). Per-ADR Lands-in-task map: ADR-0140 + ADR-0141 → Task 2 (package skeleton + types + factory + DecodeHeaders STUB); ADR-0142 → Task 3; ADR-0143 → Task 4 (Permission) + Task 5 (Principal finalized); ADR-0144 → Task 6; ADR-0145 → Task 8; ADR-0146 → Task 9; ADR-0125 §(xii) → Task 10. PLAN session committed 19 planner-time decisions resolving SPEC §12's 12-item deferral list + 7 PLAN-emerging decisions including: D13 decoder-only HTTPFilter shape per SPEC §1 item 5 + ADR-0140 §Decision (iv); D14 ADR-0125 §(xii) at impl-time-Task 10 (the key disposition); D15 single-row at LoC-borderline per phase-13/14/15 precedent; D16 Group 9 stats-namespace integration tests at Task 8; D17 `internal/matcher.MatchContext` initial accessor set scoped to RBAC's canonical surface; D18 counter-delta byte-equivalence assertion convention from phase-13/14/15; D19 BEHAVIOR_CONTRACT.md §13.1 insertion AFTER bandwidth_limit line 1416 per landing-chronological precedent.
- **phase-directory:** `docs/envoy-go/phases/16-http-filter-rbac/` — BRAINSTORM.md (683 LoC) + **SPEC.md (2004 LoC; 12 §1.1 amendments authored; all 18 §11 pins resolved IN-SESSION against Envoy v1.37.2)** + **PLAN.md (758 LoC; 16 tasks; 7 ADRs anchored + ADR-0125 §(xii) at Task 10; 19 planner-time decisions resolved)** authored. PROGRESS.md / REVIEW.md NOT yet authored — those are subsequent lifecycle-state transitions (state-4 → state-5) per ADR-0005 §Decision 4 session-split discipline (PROGRESS.md authored as Task 1 of impl session; REVIEW.md authored as Task 16 of impl session).
- **lifecycle-state:** `phase 16 PLAN.md authored; impl pending` — state-3 exit. Next session executes PLAN.md task-by-task per the phase 09/10/11/12/13/14/15 PLAN→impl precedent. PLAN session ran `superpowers:writing-plans` skill + plan-document-reviewer subagent (1 iteration → Approved). Per the user's persistent preference for subagent-driven execution + always-worktrees rule, the impl session is expected to use `superpowers:subagent-driven-development` in a fresh worktree branched off the PLAN tip per ADR-0003.
- **next-skill:** `superpowers:executing-plans` per BOOTSTRAP_PROMPT.md §5 lifecycle-state-4 entry condition (PLAN.md exists; impl pending). Per the user's persistent subagent-driven preference recorded at `~/.claude/projects/-home-esa-git-envoy-go/memory/feedback_execution_style.md`, the impl session SHOULD use `superpowers:subagent-driven-development` (NOT inline `superpowers:executing-plans`). The phase-16 PLAN's 16 tasks include explicit TDD red/green discipline per task + per-task ADR-anchor verification + 17 execution preconditions verified at Task 1 cold-start.
- **next-skill-scope:** Cold-start scope for the phase 16 impl session. Read first: `docs/envoy-go/phases/16-http-filter-rbac/PLAN.md` (758 LoC; the authoritative impl shape — every load-bearing decision lives there; 16 tasks with TDD discipline + per-task ADR-anchor mapping + 19 planner-time-resolved decisions); `docs/envoy-go/phases/16-http-filter-rbac/SPEC.md` (2004 LoC; the authoritative design doc — PLAN cross-references SPEC §6 for compiledConfig + code shapes / §7 for fixture 0018 + §13 for BEHAVIOR_CONTRACT patches / §14 for 8 unit-test groups / §15 for 13-item acceptance checklist); `docs/envoy-go/DECISIONS.md` ADR-0044 (ADR-on-impl convention); ADR-0117 + ADR-0139 (per-route INDEPENDENT-stats precedent that ADR-0145 inherits verbatim); ADR-0125 (canonical per-route patterns + §(xii) amendment lands at impl-time Task 10); ADR-0128 + ADR-0131 (framework-primitive introduction precedent — phase-16 introduces TWO new primitives ADR-0142 + ADR-0144 mirroring this discipline). **Highest-priority impl-time tasks** per the PLAN: Task 1 (PROGRESS.md preamble + 17 preconditions verification — worktree branch / master tail / toolchain / DECISIONS.md tail at ADR-0139 / SPEC SHA / pristine tree / fixtures green / fuzzers green / Envoy image pinned / proto packages present / `internal/{filter/http/rbac,matcher}/` directories absent / fixture.HTTPRbac absent / `cmd/envoy-go/main.go` registers exactly 10 filters); Task 2 (rbac package skeleton — doc.go + rbac.go with types + factory + DecodeHeaders body + filterStats 4-base-counter struct + parsePerRoute + resolvePerRouteConfig + buildCompiledConfig + buildCompiledRulesEngine + buildCompiledMatcherEngine STUB + buildCompiledPerRoute + newFilterStats + newFilterStatsIfAbsent + Group 1 + Group 2 tests + ADR-0140 + ADR-0141 authored); Task 3 (NEW `internal/matcher/` framework primitive + ADR-0142 + rbac.go's buildCompiledMatcherEngine STUB replaced with real call); Task 4 (evaluator.go Permission Large 11 + AND/OR/NOT recursive combinators + 3 deferred PARSE-REJECT + Group 3 tests + ADR-0143 partial); Task 5 (evaluator.go Principal Large 11 + prinAuthenticated three-case algorithm per §6.6 + 3 deferred PARSE-REJECT including the NEW `Principal_Custom` per §1.1 amendment 7 + Group 4 tests + ADR-0143 finalized); Task 6 (TLS-principal accessor framework primitive: `callbacks.go` interface + `chain.go` plumbing + `hcm/connection.go` + `hcm/h2dispatch.go` TLS state extraction + `chain_test.go` probe-filter integration tests + Group 7 tests + ADR-0144); Task 7 (DecodeHeaders body finalization — dual-engine dispatch + SendLocalReply 403 wire shape + Group 5 + Group 6 tests); Task 8 (stat surface finalization — newFilterStats + newFilterStatsIfAbsent + per-route INDEPENDENT-stats wiring + Group 9 stats integration tests + ADR-0145 + impl-time empirical scrape of reference Envoy v1.37.2 stat namespace to ratify-or-amend SN2-reuse hypothesis); Task 9 (shadow + LOG-partial + track_per_rule_stats per-policy emission discipline finalization + ADR-0146); Task 10 (ADR-0125 §(xii) amendment paragraph authoring — verbatim from SPEC §5.4 per planner-time decision 14); Task 11 (main.go register + fixture infra + FuzzRBACConfigParse 20th fuzzer); Task 12 (Fixture 0018 driver — 8 scenarios incl. mTLS scenario 6); Task 13 (Fixture 0018 YAMLs + mTLS PKI gen — fresh fixture-CA + server cert + client cert with URI SAN `spiffe://example.com/admin` at fixture-load time); Task 14 (Fixture 0018 expectations + README + end-to-end differential pass → 19 fixtures green including 0018); Task 15 (BEHAVIOR_CONTRACT.md 6-edit bundle per SPEC §13 + ROADMAP row 16 in-progress→done flip + STATE.md advance + 6-gate phase-done verification); Task 16 (REVIEW.md). **Anticipated production LoC: ~1208-1608 LoC**; tests + fixture + docs bring total to ~4780-5410 LoC. 6 phase-done gates (build/vet/lint clean + race-test clean + h2spec 53/53 + 20 fuzzers green + 19 differential fixtures + BEHAVIOR_CONTRACT 6 patches) verified at Task 15.
- **last-commit:** `40f030b` — `Squash merge phase-16-http-filter-rbac-plan`. Squashed 1 worktree commit (`304b133` `phase 16 plan: http-filter-rbac [PLAN.md authored + STATE.md advance — 16 tasks + 7-ADR roster anchored + ADR-0125 §(xii) at impl-time Task 10]`). Lands the phase-16 PLAN.md (758 LoC; 16 tasks + 7-ADR roster + 19 planner-time decisions) + STATE.md advance state-3 exit. Predecessor on master: `cedf29a` `phase 16 spec follow-up: STATE.md SHA-fill (TBD → 3159811 post-squash)`. Phase-16 impl session begins from this master tip in a new impl-stage worktree per ADR-0005 §Decision 4 separate-sessions discipline.
- **last-updated:** 2026-05-12
- **next-free ADR:** `ADR-0140` (unchanged from phase 16 SPEC STATE.md). Per ADR-0044 ADR-on-impl convention, phase-16 PLAN anchors ADR-0140..ADR-0146 (7 ADRs) at impl-time anchor tasks per the per-ADR table in PLAN's `## ADRs introduced by this plan` § + ADR-0125 §(xii) amendment paragraph at impl-time Task 10. The impl session at each anchor task authors the ADR §Context/Decision/Consequences body. After phase 16 phase-done (after impl + REVIEW lands much later), next-free advances to `ADR-0147` (or higher if Task 14-style follow-up surfaces an unanticipated ADR mirroring phase-14 ADR-0134 pattern).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
