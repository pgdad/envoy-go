# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `phase 28.1b SPEC done (next = 28.1b PLAN)` (2026-06-02, this commit) — **phase 28.1b (`network-filter-read-seam-and-zookeeper-requests-proof`), the second/closing half of the user-approved ADR-0045 28.1a/28.1b invoked split, has its SPEC authored + reviewed + committed.** The 28.1b SPEC (`docs/envoy-go/phases/28.1b-network-filter-read-seam-and-zookeeper-requests-proof/SPEC.md`) designs: (a) the **read-side seam** — a `readChainConn` conn-wrap (innermost: `writeChainConn(prefixConn(readChainConn(conn)))`) re-feeding every post-terminal-handoff socket read through the read-filter chain via a new `chainRuntime.replayRead` (all read filters, chain order, Status ignored = observational, buffer drained after each pass = bounded memory); (b) the **decoder-feed re-base** (D-28.1b-1) — `Buffer.TotalAppended()` (a monotonic appended-bytes counter) replaces the physical-buffer-length basis of the zookeeperproxy decoder's high-water mark, making the feed correct under runtime drains while staying byte-identical on every pre-handoff/never-drained execution (the 28.1 SPEC §4.5 contract is explicitly REDESIGNED, not silently broken); (c) the **R1 wrap predicate** (D-28.1b-2) — readChainConn installs IFF `len(writeFilters) > 0` (the writeChainConn predicate, shared: both seams wrap together; every existing production chain has zero write filters → unwrapped → 47-dir back-compat preserved structurally); (d) the **observational boundaries** (post-handoff Status/Close/ContinueReading have no effect — documented BEHAVIOR_CONTRACT boundaries) + the **concurrency pins** (replay runs on tcp_proxy's downstream-pump goroutine; the 28.1b race surface is EMPTY because OnWrite is a no-op; **LOAD-BEARING 28.2 forward-pointer:** the response decoder in OnWrite will race the replay-path request decoder on the correlation structures → the 28.2 SPEC/ADR-0223 MUST add synchronization); (e) the proof + completion surface — 0046 re-enable + green + R4, 0047, the ADR-0221/0222 bodies (ADR-0221 §Context gained a one-line §AMEND at this commit extending its scope to BOTH conn-wrap seam halves; NO new ADR number), the BEHAVIOR_CONTRACT 136 → 337 roll, ROADMAP sub-row-only advance (the parent-row-28 rollup stays 28.2's — D-28.1b-4 ratified the default). ROADMAP sub-row 28.1b flipped `planned → in-progress` at this commit (ADR-0106).
- **phase-directory:** `docs/envoy-go/phases/28.1b-network-filter-read-seam-and-zookeeper-requests-proof/` (README.md + SPEC.md at this commit; PLAN.md/PROGRESS.md/REVIEW.md land at their lifecycle states) — the D-28.1b §6.4 ratified fresh-directory default (the 25.x/26.x per-sub-phase precedent). The 28.1 directory (`docs/envoy-go/phases/28.1-network-filter-write-seam-and-zookeeper-requests/`) is the durable, unmodified record of 28.1a (its SPEC/PLAN/PROGRESS are NOT extended further). The parent directory `docs/envoy-go/phases/28-network-filter-zookeeper-proxy/` (BRAINSTORM.md + parent SPEC.md) stays canonical; the 28.2 sub-phase directory is created at the 28.2 SPEC session.
- **lifecycle-state:** `phase 28.1b SPEC done (next = 28.1b PLAN)` — SKILL_ROUTING state 2 (SPEC.md exists, PLAN.md does not). The 28.1b SPEC's §10 task spine is ~10 tasks / ~80–100 production LoC (split-gate check §10.1: no further split).
- **next-skill:** `superpowers:writing-plans` (scoped to **the 28.1b PLAN** — `docs/envoy-go/phases/28.1b-network-filter-read-seam-and-zookeeper-requests-proof/PLAN.md`, authored from the 28.1b SPEC §10 task spine: Buffer.TotalAppended → decoder re-base → readconn.go → chain.go replayRead/wrap → race test → 0046 re-enable+green+R4 → 0047 → completion bundle → six-gate). Then `superpowers:subagent-driven-development` (28.1b IMPL).
- **last-commit:** _fill at squash_ — `phase 28.1b SPEC: read-side seam (readChainConn + replayRead + Buffer.TotalAppended re-base, R1 wrap predicate) + 0046/0047 proof surface + 28.1 completion-bundle re-scope; ROADMAP 28.1b planned→in-progress; ADR-0221 §AMEND (both-directions seam scope) [ADR-0221,ADR-0222]`. Substantive predecessor on master: the 28.1a-IMPL squash `8703aeb`.
- **last-updated:** 2026-06-02
- **next-free ADR:** `ADR-0224` (UNCHANGED at this 28.1b-SPEC commit — DECISIONS.md tail STAYS **ADR-0223**; the read-side seam consumes NO new ADR number: it folds into ADR-0221 per the D-28.1b-3 resolution, recorded as a one-line in-place §AMEND on ADR-0221 §Context at this commit [the ADR-0045 §AMEND precedent]; the ADR-0221 (both halves) + ADR-0222 §Decision/§Consequences bodies land IN PLACE at the 28.1b IMPL completion task, the ADR-0223 body at the 28.2 IMPL — and ADR-0223 MUST carry the 28.1b SPEC §3.6 correlation-structure synchronization forward-pointer; no further phase-28 ADR numbers anticipated).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
