# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `23-http-filter-admission-control` **(SPEC done at this commit; lifecycle-state 2 → awaiting PLAN)**. Phase `22-http-filter-lua` parent + all three sub-phases (22.1 + 22.2 + 22.3) DONE and CANONICAL at predecessor master tip `032510a` — DO NOT reopen. Phase 23 is a NEW top-level §9 family-row (`envoy.filters.http.admission_control`), the SIXTEENTH §9 HTTP-filters-family row. **BRAINSTORM done at 2026-05-21** (squash-merge `3040a6b`); **SPEC done at 2026-05-21** at this commit via `superpowers:writing-plans` scoped to SPEC authoring. The SPEC executed the BRAINSTORM §10 empirical pins (D1-D4 + 1 follow-up) IN-SESSION against reference Envoy v1.37.2 per ADR-0004 (parallel-subagent fan-out vs the v1.37.2 C++ source on GitHub + the v1.32.4 go-control-plane proto bindings), producing an 11-entry AMEND catalog with **5 substantive REFUTATIONS** of the BRAINSTORM: AMEND-1 (formula — `aggression` is an EXPONENT `^(1/aggression)` floored to 1.0, `sr_threshold` DIVIDES successes, NOT a separate gate); AMEND-2 (reject decision is INTEGER-MODULO `(1e4·P) > (r%1e4)`, so the inline `Rand` seam is `Uint64()` NOT `Float64()`; P=0 ⇒ never reject ⇒ all-admit cross-side; forced-reject byte-exact needs ≥10000 primed failures ⇒ stays subject-only structural); AMEND-3 (third counter `rq_failure` NOT `rq_error`; 3 counters, no gauges); AMEND-4 (`enabled`-absent ⇒ ENABLED — OPPOSITE of phase-21 adaptive_concurrency's absent⇒OFF); AMEND-7 (503 reject body is EMPTY, `response_code_details = "denied_by_admission_control"`); AMEND-8 (boot-reject is `sr_threshold < 1.0%`, NOT the BRAINSTORM's `sr_threshold > 100%` which upstream clamps at runtime). SPEC.md authored at `docs/envoy-go/phases/23-http-filter-admission-control/SPEC.md` (16 §-numbered sections + §15 acceptance checklist). ADR-0194 + ADR-0195 §Context drafts appended to `DECISIONS.md` at this commit per ADR-0044 §Context-draft discipline.
- **phase-directory:** `docs/envoy-go/phases/23-http-filter-admission-control/` (BRAINSTORM.md + SPEC.md authored). PLAN.md + PROGRESS.md + REVIEW.md to follow in later lifecycle sessions per the phase-09..22 single-filter precedent.
- **lifecycle-state:** `phase 23 SPEC done; awaiting PLAN` (SKILL_ROUTING state 2 → 3). ROADMAP row `23` stays `in-progress` (per-cell narrative updated at this SPEC commit with the SPEC-done annotation; status flips to `done` only at IMPL phase-done with all 6 gates GREEN). The §9 HTTP-filters family stands at 3 remaining rows (`wasm`, `admission_control`, `global rate limit`); it closes to **2 remaining (`wasm`, `global rate limit`) once phase-23 IMPL lands**.
- **next-skill:** `superpowers:writing-plans` **scoped to PLAN authoring** for phase 23 (per SKILL_ROUTING state 2 → 3 + the phase-09..22 precedent where the post-SPEC session authors `PLAN.md`). The PLAN session creates a fresh worktree per project memory `feedback_git_worktrees.md` (branch `phase-23-http-filter-admission-control-plan` off the post-SPEC master tip) and decomposes SPEC.md into bite-sized TDD tasks. **ADR-0045 split-gate check at PLAN time:** the SPEC settled SINGLE-ROW landing (~1000-1400 LoC prod, well under the gate); the PLAN author re-evaluates only if task-decomposition LoC drifts past ~1500 LoC OR ~25 tasks. **Subagent-driven execution (project memory `feedback_execution_style.md`) applies at PLAN/IMPL** — so the PLAN's executor session (state 3 → IMPL) uses `superpowers:subagent-driven-development`. Cold-start scope for the PLAN session: read STATE.md (this file) + the phase-23 SPEC.md (the authoritative input — §4 algorithm + §5 PARSE-REJECT roster + §6 code shapes + §7 fixtures + §11 empirical pins + §15 acceptance checklist) + the phase-23 BRAINSTORM.md + DECISIONS.md tail (ADR-0194 + ADR-0195 §Context stubs — §Decision/§Consequences bodies land at the PLAN's IMPL Lands-in-Tasks) + the phase-21 PLAN.md (nearest single-filter algorithmic PLAN precedent).
- **last-commit:** `a64ee71`. Phase 23 SPEC squash-merge of branch `phase-23-http-filter-admission-control-spec` from worktree `.worktrees/phase-23-http-filter-admission-control-spec/` landed as squash-merge commit `a64ee71` on master per the phase-09..22 SPEC squash-merge convention; this placeholder was backfilled by this SHA-fill follow-up commit per convention. Predecessor master tip: `0bc26f5` — `phase 23 BRAINSTORM follow-up: STATE.md SHA-fill (TBD → 3040a6b post-squash)`.
- **last-updated:** 2026-05-21
- **next-free ADR:** `ADR-0196` — ADVANCED from `ADR-0194` at this SPEC commit (2 numbers consumed: **ADR-0194** admission-control algorithm + package shape + inline `Rand`/`Clock` seams + deterministic-regime differential strategy; **ADR-0195** RTDS `runtime_key` deferral PARSE-REJECT + enabled-absent-ENABLED). Both §Context drafts appended to `DECISIONS.md` at this commit per ADR-0044 §Context-draft discipline; **§Decision + §Consequences bodies land at phase-23 IMPL** (ADR-0194 at the controller+filter materialization Task; ADR-0195 at the compiled_config+PARSE-REJECT roster Task). **ZERO in-place §Decision AMENDMENTs + ZERO ADR-0125 amendments** at phase 23 (REUSE-by-absence per-route; FIRST ADR-0125-skip since phase-22's roster amendment; canonical-per-route roster STAYS 9). **ADR-0196 hypothesized UNCONSUMED at phase-23 phase-done** (D-style HOLD-with-known-risk: a surprise upstream stat name / a 4th counter / a clamp-boundary float edge at IMPL could force consumption). DECISIONS.md tail now `ADR-0195` (§Context stub); ADR-0193 full body + ADR-0125 §(xiv) amended remain UNCHANGED.

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
