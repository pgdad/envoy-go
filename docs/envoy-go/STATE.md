# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `07.1-http-filter-framework`
- **phase-directory:** `docs/envoy-go/phases/07.1-http-filter-framework/` — now contains `SPEC.md` (965 lines, `f2dd659`), `PLAN.md` (3062 lines, `0377571`), AND `PROGRESS.md` (the implementation session's append-only log: 23 task entries + per-task SHA-fill follow-ups + per-task code-review-loop follow-up blocks). Sub-phase 07.2 directory carries an unchanged `README.md` stub. Parent `docs/envoy-go/phases/07-filter-chain-framework/` carries `BRAINSTORM.md` (688 lines, `da28039`) + parent master `SPEC.md` (113 lines, `ee45aba`) — both read-only history. Phase 06 (parent + sub-phases 06.1 + 06.2) remains closed read-only history.
- **lifecycle-state:** `4` — implementation complete (Tasks 1–23 landed on branch `phase/07.1-http-filter-framework-impl`), awaiting independent verification. Implementation session ran the full closing six-gate sweep at Task 23 with all gates green: gate (a) 9/9 differential fixtures PASS (0000–0006 + 0007a-cors + 0007b-iteration-probe); gate (b) H2 dispatch covered via 0004 + chain integration tests; gate (c) h2spec 53/53 PASS at the ADR-0051 pin (`summerwind/h2spec@sha256:5f4a65c30cae8569558ced048b4bfe0dcf01a221e36767ae504ccd8348a7aeb0`); gate (d) 9 fuzzers (8 prior + 1 new `FuzzFilterChainParse` mode-discriminator-unified) clean for 30s each, 0 crashers; gate (e) `go vet` + `golangci-lint` + `go test -race` all clean; gate (f) defers to REVIEW session. Boundary grep zero matches (no third-party filter-chain library); empirical-pin grep confirms BEHAVIOR_CONTRACT.md `## HTTP filter chain` carries verbatim §11.1/§11.2/§11.3/§11.4 blocks. ADR-0070..ADR-0076 all landed at first-use tasks. Notable transit-period adaptations: Task 22's structural fixture surfaced two latent H1 dispatch bugs from Task 15 (body-bytes-not-restored on `req.Body` after `RunDecodeData` drain; missing `LocalReplyDone()` check after body loop) — both fixed inline at Task 22. Task 18's review-loop deferred I-3 (Action 4-tuple bytesSent leak; encode-chain dormancy) was closed at Task 19's bundled work (Action 3-tuple + ActionResponse.Headers → OrderedHeaders + writeH1Reply/writeH2Reply collapse + cors actual-request encode-side ordered-append-with-alphabetical-net-new-keys). Per BOOTSTRAP §5 + the PLAN's Post-plan handoff section, the next transition is state 4 → 5 via `superpowers:verification-before-completion` (independent verification session re-runs all six gates).
- **next-skill:** `superpowers:verification-before-completion` — independent verification session for phase 07.1 (state 4 → 5 per `BOOTSTRAP_PROMPT.md` §5 step 4). Inputs: this commit's STATE.md + the impl branch (`phase/07.1-http-filter-framework-impl`) at HEAD `762bed7` (Task 23 SHA-fill commit) + the closing-task PROGRESS.md entry quoting all six gates' command outputs verbatim. The verification session re-runs gates (a) through (e) independently (gate (f) is REVIEW session's territory) per BOOTSTRAP §7.5 against the impl branch HEAD. On all-gates-PASS the session advances STATE to lifecycle-state 5 with `next-skill: superpowers:requesting-code-review`. On any gate FAIL the session sets STATE back to lifecycle-state 3 with `next-skill: superpowers:subagent-driven-development` (or `superpowers:systematic-debugging` for unexpected failures), records `block-reason`, and exits. The verification session creates worktree `.worktrees/phase-07.1-http-filter-framework-verify` on branch `phase/07.1-http-filter-framework-verify` per ADR-0003.
- **next-skill-scope:** Lifecycle-state 4 → 5 verification session deliverable: independent re-run of the six-gate sweep against impl-branch HEAD `762bed7`; PROGRESS.md append-only continuation with the verify-session's verbatim gate outputs (mirroring the phase-04..06.2 verify-session PROGRESS pattern); STATE.md advance to lifecycle-state 5 + `next-skill: superpowers:requesting-code-review` with `last-commit` updated to the verify-session's SHA. NO code changes in the verify session — gate failures escalate to a re-impl loop (state 4 → 3) rather than fix-in-place. The REVIEW session (state 5 → 6) then writes REVIEW.md + the phase-done commit that flips ROADMAP row 07.1 → done (parent row 07 STAYS in-progress per the 05/05.1/05.2 + 06/06.1/06.2 closure pattern; the parent only flips to done at 07.2's phase-done) AND advances STATE to phase 07.2 (`active-phase: 07.2-listener-chain-completion`; `lifecycle-state: 1`; `next-skill: superpowers:brainstorming`) AT THE SAME COMMIT. The L4 review-followup batch closing 11 Minors from phase 06.2 REVIEW.md remains a separate post-phase-done branch — NOT 07.1's scope; can run in parallel.
- **last-commit:** `4c17d5f` — `phase 07.1: STATE.md → lifecycle-state 4 (impl complete, awaiting verification)`. This impl-session-exit STATE.md commit advances the lifecycle from 3 → 4 + sets next-skill to `superpowers:verification-before-completion` + records last-commit pointer at this commit's SHA per the phase-02..06.2 SHA-fill precedent. Lands on top of `762bed7` (Task 23 SHA-fill) which itself sits on top of `801dab0` (Task 23 BEHAVIOR_CONTRACT edit + closing-six-gate-sweep). Implementation session totals: 60+ commits across Tasks 1–23 on branch `phase/07.1-http-filter-framework-impl` (each task lands one substantive commit + one SHA-fill commit + zero-or-more review-loop fix commits per the phase-02..06.2 SHA-fill convention); 7 new ADRs ADR-0070..ADR-0076 in DECISIONS.md; BEHAVIOR_CONTRACT.md extended with NEW `## HTTP filter chain` top-level section (~280 lines) + amendments to `## HTTP/1.1` + `## HTTP/2` for ADR-0040/ADR-0042 supersession + new row in `## Equivalence Matrix`; new framework files in `internal/filter/http/` (chain.go, perroute.go, registry.go, types.go, callbacks.go, chain_shape.go, fuzz_test.go) + 4 new packages under it (router/, cors/, envoygotest/, envoygotest/proto/); HCM rewired (config.go, filter.go, connection.go, h2dispatch.go, codec.go, route.go, actions.go); listener manager threaded with `*HTTPRegistry`; cmd/envoy-go/main.go boot wiring + bootstrap.go cors/v3 blank-import; 2 new differential fixtures (test/fixtures/0007a-cors/ + test/fixtures/0007b-iteration-probe/). Pre-existing fixtures `0000-0006` remained green throughout (modulo the transient `0004-h2-routing` red between Tasks 12–15 explicitly permitted by PLAN's Refinement).
- **last-updated:** 2026-05-01

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
