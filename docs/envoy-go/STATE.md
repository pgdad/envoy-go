# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `10-http-filter-header-mutation` — SPEC.md landed at this commit (ROADMAP row 10 status `planned → in-progress` per `BOOTSTRAP_PROMPT.md` §4.1 invariant 3; phase directory `docs/envoy-go/phases/10-http-filter-header-mutation/` now contains `BRAINSTORM.md` + `SPEC.md`). Phase 10 remains the THIRD §9 family-row to land (after cors @ 07.1 done, fault @ 09 done); SPEC commit advances the row through the `in-progress` window that closes at the phase-done commit. Per ADR-0106 flat-top-level-rows discipline, phase 10 stays a top-level row, NOT a sub-phase of any §9 parent.
- **phase-directory:** `docs/envoy-go/phases/10-http-filter-header-mutation/` — contains `BRAINSTORM.md` + `SPEC.md` at this commit (1348-line SPEC mirroring phase-09's 1305-line §1..§16 structure; spec-document-reviewer approved on first iteration per ADR-0004 review loop). PLAN.md, PROGRESS.md, REVIEW.md follow in subsequent sessions per the phase-09 precedent.
- **lifecycle-state:** `spec-complete` — SPEC.md is the single output of this session (project's adapted lifecycle-state 2 → 3 advanced this session by authoring SPEC.md + executing all five §11 empirical pins IN-SESSION against reference Envoy v1.37.2 + landing the spec-document-reviewer approval + flipping ROADMAP row 10 to in-progress, mirroring phase 09's SPEC-merge pattern). The §11 empirical-pin block landed two amendments to BRAINSTORM hypotheses: §11.1 protected-header set is **CONFIG-LOAD-TIME enforcement** (NOT silent runtime no-op as BRAINSTORM Decision 11 hypothesized — MAJOR SURPRISE); §11.3 zero-stats CONFIRMED (no revision, but ADR-0114 dropped per §8.1 consolidation since "no stats" needs no standalone ADR). Differential fixture scenarios collapsed 5 → 4 (protected-header scenario migrates to unit-test territory per §11.1); ADR list collapsed 7 → 6 (ADR-0108..ADR-0113). The next session's input is SPEC.md.
- **next-skill:** `superpowers:writing-plans` per ADR-0005 routing — the next session's WORK is to author PLAN.md (per the phase 09 precedent: BRAINSTORM → SPEC → PLAN → impl → review). Per the user's persistent preference for subagent-driven over inline execution recorded in `~/.claude/projects/-home-esa-git-envoy-go/memory/MEMORY.md`, the PLAN-authoring session may run inline (PLAN.md is collaborative drafting; the framework + filter + fixture + ADRs surface is well-bounded by SPEC.md so inline drafting is natural) while the subsequent PLAN-execution session is subagent-driven per `superpowers:subagent-driven-development`.
- **next-skill-scope:** Cold-start scope for the PLAN-authoring session. Read first: `docs/envoy-go/phases/10-http-filter-header-mutation/SPEC.md` (full; 1348 lines; this session's output); `docs/envoy-go/phases/10-http-filter-header-mutation/BRAINSTORM.md` (full); `docs/envoy-go/phases/09-http-filter-fault/PLAN.md` (structural precedent for PLAN.md shape — task-by-task decomposition matching BOOTSTRAP_PROMPT.md §5 + 09's 25-ish task count; mirror its preamble + per-task shape). Read `docs/envoy-go/DECISIONS.md` ADR-0004 / ADR-0005 / ADR-0072 / ADR-0073 / ADR-0074 / ADR-0100 / ADR-0101 / ADR-0106. Read `internal/filter/http/perroute.go` (existing `Resolve`; phase 10 adds `ResolveAllTiers` sibling per SPEC §6.7) + `internal/filter/http/callbacks.go` (existing `RequestRouteConfig`; phase 10 adds `RequestRouteConfigsAllTiers` + symmetric encoder method per SPEC §4.2 / §12 deferred decision 1) + `internal/filter/http/chain.go` (per-stream state machine that wires the new callback) + `internal/filter/http/fault/fault.go` (package-shape + parser precedent per SPEC §6.1) + `cmd/envoy-go/main.go:111–116` (registration site; phase 10 adds one line). The session emits PLAN.md only (lifecycle-state 3 → 4 in the project's adapted lifecycle); the spec is fixed and the planner decomposes it into ~12–18 tasks per SPEC's §1.4 estimate. PLAN must settle SPEC's §12 deferred decisions 1, 2, 3 explicitly (callback symmetry, per-request cache, per-route protected-header validation lifecycle) and note the planner's choice on §8.1 ADR consolidation (default: keep ADR-0108..ADR-0113 separate per the 09 SPEC §8.1 precedent).
- **last-commit:** `f339c12` — `phase 10 spec: http-filter-header-mutation [SPEC.md; ROADMAP row 10 → in-progress]`. Lands `SPEC.md` (1348 lines) for phase 10 (`http-filter-header-mutation`); flips ROADMAP row 10 status `planned → in-progress` with a SPEC-revised summary; advances STATE.md lifecycle 2 → 3; anticipates ADRs ADR-0108..ADR-0113 (6 ADRs per SPEC §8 — one fewer than BRAINSTORM anticipated, per §8.1 consolidation). All five §9 BRAINSTORM empirical pins resolved IN-SESSION per ADR-0004 against Envoy v1.37.2 image SHA `c5e8a68e52f4d4697a9adb280dbe415d77fedf1257e183dcb86205bd438f18bd` (probe date 2026-05-04). Spec-document-reviewer approved on first iteration per ADR-0004 review loop. The §9 family heading at `ROADMAP.md` line 56 stays unchanged per ADR-0106(c). The SPEC worktree branch `phase-10-http-filter-header-mutation-spec` (branched from master tip `71b6401`) ff-merges into master; SHA filled in a follow-up commit per the phase-04..09 SHA-fill convention. Per user preference, master pushed to origin after merge.
- **last-updated:** 2026-05-04

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
