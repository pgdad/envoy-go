# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `phase 36.1 (load-balancer-ring-hash) IMPL done` (2026-06-12, this commit) — the phase-36.1 {ring_hash} IMPL is COMPLETE (the seam EXTENSION ADR-0235 + the `ringHashLB` Ketama policy ADR-0236 + the hand-rolled `xxHash64`/`murmurHash2` + the manager two-layer gate + the 3 `ring_hash_lb.*` gauges + the tcp_proxy `source_ip` plane + the `0061-lb-ring-hash` subject-side affinity differential; the 10-task TDD PLAN executed subagent-driven, six-gate GREEN — see `docs/envoy-go/phases/36-load-balancer-ring-hash/PROGRESS.md`). **As-built deltas:** stat surface **1116 → 1119** (the 3 MIRRORED `cluster.<name>.ring_hash_lb.{size,min_hashes_per_host,max_hashes_per_host}` gauges — the FIRST non-zero LB-stat delta; cross-side-exact 1026/342/342 for the default 3-host cluster); fixtures **62 → 63** (`0061-lb-ring-hash`, tail); DECISIONS.md tail **ADR-0234 → ADR-0236** (the seam-extension ADR-0235 + the policy ADR-0236, both PROPOSED → ACCEPTED in-place per ADR-0044); TWO new exported symbols (`cluster.WithHashKey` + `cluster.HashSourceIP`, both additive — the exported `Cluster` surface byte-stable); fuzzers UNCHANGED at **42** (no wire decode); BackendKind tail UNCHANGED at **33** (`0061` reuses `TCPEcho`); ZERO new packages + ZERO new go.mod deps (`go mod tidy -diff` EMPTY). The seam's FIRST extension (the ADR-0232 PICK-INPUT half — the ctx-carried hash key); the project's FOURTH LB policy + FIRST consistent-hashing policy; the Load-balancing family's THIRD row, STAYS OPEN. Cross-side host IDENTITY INFEASIBLE in the harness (Docker source-IP NAT + per-side ring layouts) → the differential asserts PER-SIDE affinity (the `count % 16 == 0` modular invariant) + spread + cross-side byte-equivalence + cross-equal stats (AMEND-RH8).
- **phase-directory:** `docs/envoy-go/phases/36-load-balancer-ring-hash/` (**BRAINSTORM.md + SPEC.md + PLAN.md + README.md** — PLAN.md + the README.md status advance (2 → 3) landed THIS commit; the 36.1/36.2 split is CONSUMED → the 36.1 PROGRESS.md follows at the 36.1 IMPL session, then the 36.2 sub-phase dir per the 32.1/32.2 per-sub-phase-dir precedent). **Superseded (the phase-35 phase-directory, preserved):** `docs/envoy-go/phases/35-load-balancer-random/` (**BRAINSTORM.md + SPEC.md + README.md + PLAN.md** — PLAN.md landed THIS commit (README.md status advanced 2 → 3); the PROGRESS.md follows at the IMPL session [Task 1 creates it], per the kafka-31/thrift-33/least_request-34 single-flat-row per-phase-dir precedent). The phase-34 directory `docs/envoy-go/phases/34-load-balancer-least-request/` (**BRAINSTORM.md + SPEC.md + README.md + PLAN.md + PROGRESS.md** — the durable phase-34 IMPL record). The phase-33 directory `docs/envoy-go/phases/33-network-filter-thrift-proxy/` (**BRAINSTORM.md + SPEC.md + README.md + PLAN.md + PROGRESS.md** — the durable phase-33 IMPL record). The phase-32 dirs `docs/envoy-go/phases/32-network-filter-redis-proxy/` + `32.1-…/` + `32.2-network-filter-redis-commands-and-stats/` (**SPEC.md** — the durable phase-32.2 sub-phase SPEC; the PLAN.md follows at the 32.2 PLAN session, the PROGRESS.md at the 32.2 IMPL session [Task 1 creates it], per the 29.1/29.2/29.3 per-sub-phase-dir precedent). The phase-32.1 dir `docs/envoy-go/phases/32.1-network-filter-redis-upstream-pool-seam-and-codec/` (**SPEC.md + PLAN.md + PROGRESS.md** — the durable phase-32.1 IMPL record). The parent `docs/envoy-go/phases/32-network-filter-redis-proxy/` (**BRAINSTORM.md + SPEC.md + README.md** — the durable phase-32 parent-SPEC record). The phase-31 directory `docs/envoy-go/phases/31-network-filter-kafka-broker/` (BRAINSTORM.md + SPEC.md + README.md + PLAN.md + PROGRESS.md — the durable phase-31 IMPL record). The phase-30 + phase-29 directories stay the durable phase-30/29 record.
- **lifecycle-state:** `phase 36.1 IMPL done — next: the 36.2 SPEC (superpowers:brainstorming/writing-plans as routed — the HTTP route hash_policy plane: header xxHash64 + connection-properties source_ip, the rotl64(prev,1)^new combine + terminal short-circuit; then its own PLAN → IMPL adding 0062-lb-ring-hash-http, a NAT-transparent TRUE cross-side affinity differential)`. The 36.1/36.2 by-plane split (ADR-0045 CONSUMED) lands 36.1 first (this IMPL); 36.2 is the second producer plane against the ALREADY-LANDED ADR-0235 seam (it may surface a small route-hash ADR or amend ADR-0236; it adds 0 new stat names). The controller squash-merges this 36.1 IMPL + pushes at stage-close.
- **next-skill:** the **36.2 SPEC** (the HTTP route `hash_policy` plane — `superpowers:brainstorming` then `superpowers:writing-plans` per the family-row routing; SPEC §3.0 / §8.2 / §13 carry the 36.2 design surface: the header + connection-properties source_ip subset, the `rotl64(prev,1)^new` multi-policy combine, the `terminal` short-circuit, cookie/query/filter_state DEPARTURE-rejected, the `0062-lb-ring-hash-http` cross-side affinity differential).
- **last-commit:** the phase-36.1 {ring_hash} IMPL completion bundle (Task 10 — `docs/envoy-go/BEHAVIOR_CONTRACT.md` ring_hash delta [surface 1116→1119; the new `### Load balancer — ring_hash (RING_HASH)` subsection; the reject-text + deferred-candidate updates]; `docs/envoy-go/DECISIONS.md` ADR-0235 [seam extension] + ADR-0236 [ring_hash policy] full entries; STATE/ROADMAP row 36.1 done [fixtures 62→63]); the prior 9 tasks landed the seam + policy + tcp plane + the `0061` differential + the six-gate evidence.
- **last-updated:** 2026-06-12 (phase-36.1 IMPL done)
- **next-free ADR:** `ADR-0237` (DECISIONS.md tail advanced **ADR-0234 → ADR-0236** at the phase-36.1 IMPL — ADR-0235 [the LB hash-key seam extension] + ADR-0236 [the `ring_hash` policy], both ACCEPTED in-place per ADR-0044; the 36.2 leg may consume ADR-0237 for a route-hash ADR or amend ADR-0236).

---

## Project counts at this commit

- **active differential fixtures:** **63** (tail `0061-lb-ring-hash`). Canonical recipe `ls -d test/fixtures/[0-9]* | wc -l` = 63. Advanced at the phase-36.1 IMPL (+1 — `0061-lb-ring-hash`, the tcp source_ip subject-side affinity+spread differential; 36.2 will add `0062-lb-ring-hash-http`).
- **fuzzers:** **42** (canonical recipe `grep -rh "^func Fuzz" $(find ./internal -name fuzz_test.go) | wc -l`; tail `FuzzThriftDecode`). UNCHANGED at the phase-36.1 IMPL (+0 — the THIRD no-fuzzer phase; ring_hash decodes no untrusted wire bytes — the hash key derives from a source IP / parsed value, not a wire frame; a ring-lookup property test is unit-level).
- **stat surface:** **1119** (BEHAVIOR_CONTRACT doc count). Advanced at the phase-36.1 IMPL (1116 → **1119**, +3 — the 3 MIRRORED `cluster.<name>.ring_hash_lb.{size,min_hashes_per_host,max_hashes_per_host}` GAUGES; the FIRST non-zero LB-stat delta — least_request-34 + random-35 were both zero-delta phases; cross-side-exact, keyed only on ring-config + host count, NOT endpoint addresses; registered ONLY on the RING_HASH path; the DOC count is the source-of-truth, no programmatic golden).
- **BackendKind tail:** **33** (`TCPThriftResponder` — seq_id-agnostic canned framed-binary REPLY). UNCHANGED at the phase-36.1 IMPL (+0 — `0061` reuses `TCPEcho` 0; an LB phase exercises WHERE conns land, not a new backend).
- **DECISIONS.md tail:** **ADR-0236** (next-free **ADR-0237**) — TWO ADRs landed at the phase-36.1 IMPL: **ADR-0235** (the LB hash-key seam extension — the ADR-0232 PICK-INPUT half; the ctx-carried hash key; the seam's FIRST extension) + **ADR-0236** (the `ring_hash` load-balancing policy — the Ketama consistent-hash ring; the project's FOURTH LB policy + FIRST consistent-hashing policy), both PROPOSED → ACCEPTED in-place per ADR-0044 (the redis-32 / least_request-34 two-ADR shape, since the seam is EXTENDED not merely reused).
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227, superseding ADR-0008).
- **conformance:** h2spec 53/53; proxy-wasm all 10 families PASS (image-independent; UNAFFECTED by phase 36.1 — touches only `internal/cluster` [the LB seam + ring_hash policy] + `internal/filter/tcpproxy` [the source_ip plane]; no HTTP/h2/proxy-wasm path).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
