# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `19-http-filter-ext-proc` — phase 19 BRAINSTORM session COMPLETE (state 0 → 1 transition per ADR-0005 §Decision 4). BRAINSTORM.md authored at `docs/envoy-go/phases/19-http-filter-ext-proc/BRAINSTORM.md` (490 lines; 12 sections mirroring phase-18 BRAINSTORM section-for-section). The 6 user-driven design decisions settled in-session: Q1 family-child = `ext_proc` (TWELFTH §9 family-row; 6 remaining: `oauth2` / `lua` / `wasm` / `adaptive_concurrency` / `admission_control` / `global_ratelimit`); Q2 processing-mode envelope = Headers + BUFFERED body (STREAMED + BUFFERED_PARTIAL + FULL_DUPLEX_STREAMED PARSE-REJECT envoy-go-strict); Q3 service-mode envelope = Both proto-faithful (`grpc_service` bidi-stream + `http_service` JSON-transcoded-headers-only per the proto's HttpService constraint); Q4 mutation faithfulness = Full proto-faithful (`CommonResponse.header_mutation` + `body_mutation` + `clear_route_cache` + `status` CONTINUE/CONTINUE_AND_REPLACE + `ImmediateResponse` multi-stage deny + `mutation_rules` gating); Q5 mode-override = Full proto-faithful (`allow_mode_override` + `allowed_override_modes` + mid-stream re-eval + `max_message_timeout` bounding); Q6 per-route = 5th canonical REUSE + SHARED stats (NO ADR-0125 amendment — SECOND consecutive §9 row to REUSE the 5th canonical after phase 18 per ADR-0163). spec-document-reviewer subagent APPROVED the BRAINSTORM.md on first iteration (proto faithfulness, framework-reuse claims, ADR anchoring, lifecycle anchors, structural fidelity all verified). The next session is the phase-19 SPEC session (lifecycle-state 1 → 2) per `superpowers:brainstorming` scoped to SPEC authoring per the phase-09..18.2 convention.
- **phase-directory:** `docs/envoy-go/phases/19-http-filter-ext-proc/` (BRAINSTORM.md only at this commit; SPEC.md authored at the next session).
- **lifecycle-state:** `phase 19 BRAINSTORM done; phase 19 SPEC pending` — phase 19 BRAINSTORM is complete; the next session is the SPEC session. NO row 19 exists on ROADMAP yet — the SPEC session adds it per the phase-09..18.2 ROADMAP-row-add convention (rows registered at SPEC time once the empirical scrape confirms the envelope). The §9 family-row continuation is implicit per ADR-0106. Anticipated 7-9 ADRs at SPEC + IMPL time (ADR-0167..ADR-0173 + up to ADR-0175 if conditional empirical pins §19.P11 + §19.P12 fire); 13 empirical pins (§19.P1..§19.P13) enumerated for SPEC-time resolution per ADR-0004; ADR-0045 split-readiness HIGH (three split candidates surveyed — split-by-feature-class / split-by-service-mode / single-row — SPEC author chooses).
- **next-skill:** `superpowers:brainstorming` — phase 19 BRAINSTORM is done. The next session is a SPEC session (scoped to SPEC authoring per the phase-09..18.2 convention — re-invokes `superpowers:brainstorming` scoped to SPEC.md output, NOT a separate `superpowers:writing-spec` skill). Cold-start scope for the SPEC session: read STATE.md (this file); `docs/envoy-go/phases/19-http-filter-ext-proc/BRAINSTORM.md` (the 12-section design envelope); `docs/envoy-go/ROADMAP.md` (no row 19 yet — SPEC session adds it); `docs/envoy-go/MISSION.md`; `docs/envoy-go/BOOTSTRAP_PROMPT.md` §9 (the HTTP-filters family roster); `docs/envoy-go/DECISIONS.md` (tail at ADR-0166; next-free ADR-0167; the 11 ADRs anchored at phase-18 family — ADR-0156..ADR-0166 — provide the §9 framework precedents); `docs/envoy-go/BEHAVIOR_CONTRACT.md` (11 HTTP filters documented through phase 18.2; HCM stat-table at 77 names); `docs/envoy-go/SKILL_ROUTING.md`; the proto surface at `~/go/pkg/mod/github.com/envoyproxy/go-control-plane/envoy@v1.32.4/extensions/filters/http/ext_proc/v3/` and `~/go/pkg/mod/github.com/envoyproxy/go-control-plane/envoy@v1.32.4/service/ext_proc/v3/`. The SPEC session's defining in-session obligations (per BRAINSTORM §12): resolve all 13 §10 empirical pins via reference Envoy v1.37.2 scrape; anchor the 7-9 anticipated ADRs with §Context drafts; confirm or refute the ADR-0045 split-by-surface decision; define the ~8-10-scenario differential fixture; assign RATIFIED-PENDING pin closures for §19.P4 + §19.P11 + §19.P12 + §19.P8; add ROADMAP row 19.
- **last-commit:** `5ec0d67` — `Squash merge phase-19-http-filter-ext-proc-brainstorm`. The phase-19 BRAINSTORM worktree `phase-19-http-filter-ext-proc-brainstorm` (branched off master tip `d54d924` — `phase 18.2 IMPL follow-up: STATE.md SHA-fill (TBD → 42ad788 post-squash)`) carried the BRAINSTORM.md + STATE.md advance as a single in-worktree commit; squash-merged to master at `5ec0d67` as one BRAINSTORM commit + this SHA-fill follow-up commit per the phase-09..18.2 precedent.
- **last-updated:** 2026-05-15
- **next-free ADR:** `ADR-0167` — UNCHANGED from prior STATE. BRAINSTORM sessions do NOT consume ADR numbers per the ADR-0044 ADR-on-impl convention (ADR §Context drafts MAY land at SPEC time; full §Decision+§Consequences bodies land at IMPL time). ADR-0167 will be the phase-19 layout ADR landing at SPEC §Context-draft and IMPL §Decision+§Consequences. The anticipated phase-19 ADR roster (per BRAINSTORM §7): ADR-0167 phase-19 layout + selection rationale; ADR-0168 dual-mode service envelope + http_service proto-constraint; ADR-0169 `*ProcessorClient` bidi-stream wrapper (EXTENDS ADR-0158 — same package, alongside unary `*AuthClient`); ADR-0170 ProcessingRequest/Response JSON codec for http_service; ADR-0171 ProcessingMode + mode-override state-machine discipline; ADR-0172 CommonResponse mutation + ImmediateResponse multi-stage deny discipline; ADR-0173 per-route 5th-canonical REUSE + SHARED stats (NO ADR-0125 amendment); ADR-0174 (CONDITIONAL — fires only if §19.P12 confirms encode-side callback gap) symmetric `EncoderFilterCallbacks` extension; ADR-0175 (CONDITIONAL — fires only if §19.P11 confirms encode-side body-buffering primitive needed) encode-side body-buffering primitive.

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
