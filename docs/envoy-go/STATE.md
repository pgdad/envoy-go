# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `06.2-access-log`
- **phase-directory:** `docs/envoy-go/phases/06.2-access-log/` — does not yet exist beyond the placeholder `README.md` (`ad46930`). The directory's brainstorm-time `BRAINSTORM.md` / `SPEC.md` are authored at phase-06.2 brainstorm time (the next session). Phase 06.1 is closed; `docs/envoy-go/phases/06.1-stats-prometheus/` joins prior phases as closed read-only history. The parent `docs/envoy-go/phases/06-observability-baseline/` retains its `BRAINSTORM.md` + parent `SPEC.md` as the master design document (still open until 06.2 phase-done flips parent row 06). ROADMAP row 06.1 is `done` after this commit; row 06 stays `in-progress`; row 06.2 stays `planned`.
- **lifecycle-state:** `6` — phase 06.1 complete; ROADMAP row 06.1 flipped to `done` by this commit per 05.2 `0c01ed6` one-step precedent + REVIEW I-2 disposition. The L4 REVIEW-followup branch on `phase/06.1-stats-prometheus-review-followup` closed M-1 (cluster-name latent vulnerability — symmetric to ADR-0065's HCM stat_prefix defect; inherits ADR-0065 by reference, no new ADR) across two commits — `caa58e5` (boundary guard at `cluster.NewManager` + 6-case `TestNewManager_ClusterNameInvalidChars` regression test) and `665c879` (golangci-lint misspell fixup); `5ddef1b` re-verified gates (b)/(c)/(d)/(e) GREEN at HEAD `665c879`; `46b3435` promoted STATE to lifecycle-state 5; this commit is the lifecycle-state 6 phase-done commit. REVIEW Importants closed: I-1 by REVIEW + PROGRESS Task 11 deviation #1 as-corrigendum (no commit needed; SPEC immutability preserved); I-2 by this commit's ROADMAP edit. Twelve Minors carry forward to the L4 review-followup batch (separate post-phase-done branch). Per BOOTSTRAP §5 step 6 → step 1, the next session is phase-06.2 brainstorm and lifecycle-state resets to `0`-equivalent (cold-start for the new phase).
- **next-skill:** `superpowers:brainstorming` — phase 06.2 (`access-log`) brainstorm. Per ROADMAP row 06.2: "Internal `accesslog` package (file sink, Envoy default format formatter, async writer); HCM access-log emit hooks; fixture `0006-access-log`; populates `BEHAVIOR_CONTRACT.md ## Access log field mapping`. Closes the parent row `06` at its phase-done commit (mirrors 05 / 05.1 / 05.2 closure pattern)." Inputs the brainstorm should consider: (a) the four ADR-0058 / 05.2 carry-forwards (M-4 readClientPreface ctx-awareness, M-10 SETTINGS_TIMEOUT absent, M-12 closedStreams map unbounded, the seven 05.2 REVIEW Minors); (b) the 06.1 12-Minor carry-forward queue (M-2 through M-12 plus the post-phase-done batch coverage — see 06.1 REVIEW.md §Findings/Minor); (c) the M-1 closure precedent — the `stats.IsValidName` boundary-validation pattern is now established for any future filter / extension that derives metric names from user input (per ADR-0065 Consequences (e)); (d) the post-06.1 fuzzer count of 7 (`FuzzPromTextFormat` is now a phase-done invariant); (e) the 06.1 BEHAVIOR_CONTRACT additions (## Stat-name mapping rules SN1–SN8) co-exist with the upcoming ## Access log field mapping section per BEHAVIOR_CONTRACT skeleton.
- **next-skill-scope:** Phase 06.2 brainstorm session (lifecycle-state 0 → 1 cold-start). Per ADR-0006 + BOOTSTRAP §5: brainstorm with `superpowers:brainstorming` to scope phase 06.2's deliverables (internal `accesslog` package + Envoy default format + file sink + HCM emit hooks per ROADMAP row 06.2), then advance to lifecycle-state 1 (`superpowers:writing-plans` for SPEC.md authoring) and 2 (PLAN.md authoring). Worktree for the next session: branch from this commit's HEAD per ADR-0003 + per-phase-worktree convention (`.worktrees/phase-06.2-access-log-brainstorm`). Phase 06.2 is the terminal sub-phase of phase 06; its phase-done commit will flip BOTH ROADMAP row 06.2 AND parent row 06 → done together (the 05.2 precedent for terminal-sub-phase parent-flip).
- **last-commit:** `TBD` — `phase 06.1: phase-done — stats-prometheus lands; ROADMAP row 06.1 → done [ADR-0059, ADR-0060, ADR-0061, ADR-0062, ADR-0063, ADR-0064, ADR-0065]`, on branch `phase/06.1-stats-prometheus-review-followup`. Flips ROADMAP row 06.1 status `planned → done` (one-step per the 05.2 `0c01ed6` precedent + REVIEW I-2 disposition). Updates STATE.md `lifecycle-state` 5 → 6 with `active-phase` advanced to `06.2-access-log` and `next-skill` set to `superpowers:brainstorming` per BOOTSTRAP §5 step 6 → step 1 transition. Names all seven ADRs introduced in 06.1 per BOOTSTRAP §5.3: ADR-0059 + ADR-0060 (companion: stats-store architecture + histograms-deferred; landed at Task 2 in commit `9417235`), ADR-0061 + ADR-0064 (companion: SN1–SN8 flattening rules + stats_config.stats_tags hardcoded; landed at Task 4 in commit `7f45a4d`), ADR-0063 (per-endpoint cluster stats not emitted; landed at Task 8 in commit `a6f8b94`), ADR-0062 (differential equivalence shape; landed at Task 14 in commit `a32212b`), ADR-0065 (metric-name-input validation at boundary; landed at gate-(d) fix `79be6b0`). Subject-line ADR ordering follows numerical sequence (the project's standard subject-line convention) rather than commit-time order, mirroring 05.2's documented topical-vs-commit-order rationale. SHA-fill follow-up per phase-02..05.2 convention.
- **last-updated:** 2026-04-28

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
