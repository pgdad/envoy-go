# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `phase 28.1a IMPL done (ADR-0045 split invoked, user-approved)` (2026-06-02, this commit) — **phase 28.1 (`network-filter-write-seam-and-zookeeper-requests`) was invoked-split at IMPL Task 16 into 28.1a + 28.1b per the user-approved ADR-0045 split of 2026-06-02 (DECISIONS.md ADR-0045 §AMEND).** 28.1a (DONE this commit) landed PLAN Tasks 1–15 + the Task-16 0046 driver (committed but DISABLED): (a) the `network.WriteFilter` seam — `WriteFilter`/`WriteFilterCallbacks` interfaces, chain classification (a filter may be `ReadFilter`, `WriteFilter`, or both), write chain dispatched REVERSED, delivery via the `writeChainConn` downstream-conn wrapper handed to `handleTerminal` (no `TerminalFilter.Handle` signature change; `tcp_proxy`/HCM untouched; ZERO-write-filter chains UNWRAPPED = R1 back-compat preserved); ADR-0213 allowance consumed (ADR-0221 — body lands at 28.1b); (b) the complete `internal/filter/network/zookeeperproxy/` REQUEST package — 9-field config parse + PARSE-REJECT arms, the 201-counter eager stat roster + dynamic auth-scheme counters (unit-proven, NOT cross-side-proven), the shallow request decoder with the D-S28.1-1 min-length table + the per-connection xid→opcode correlation map, both-directions filter glue (ADR-0222 — body lands at 28.1b); (c) the 7th built-in + `zookeeper_proxy/v3` bootstrap blank-import; (d) the `.zookeeper.` `name.go` Prometheus arm; (e) the 37th fuzzer (found + fixed 2 real bugs); (f) the `TCPSink` `BackendKind` + the `0046-zookeeper-requests` driver, committed but DISABLED (blank-import commented out) pending the 28.1b read seam. **Root cause that forced the split:** the Task-16 0046 fixture proved `manager.go serveNetworkChain`'s read loop exits PERMANENTLY at terminal handoff (`TerminalReady()` → `HandleTerminal()` → return), so zookeeper_proxy only decodes the FIRST frame per connection vs reference Envoy re-iterating read filters forever → cross-side `*_rq` divergence on multi-frame connections. The 28.1 SPEC designed the WriteFilter seam but not the symmetric read-side seam. All gates green on the 28.1a scope (PROGRESS.md). The split AXIS differs from the §10.1 BRAINSTORM-time pre-authorized axis (a SEAM-COMPLETENESS axis, not write-vs-response); the user's 2026-06-02 approval governs.
- **phase-directory:** `docs/envoy-go/phases/28.1-network-filter-write-seam-and-zookeeper-requests/` (SPEC.md + PLAN.md + PROGRESS.md — PROGRESS.md carries the 28.1a closure entry this commit). The 28.1 directory CONTINUES to serve both 28.1a (done) and 28.1b: per the ADR-0045 / 22.x / 25.x / 26.x sub-phase-directory precedent a NEW sub-phase directory is created at the sub-phase's SPEC session — the 28.1b SPEC session decides whether to create `docs/envoy-go/phases/28.1b-…/` (with a README naming the 28.1 SPEC as master) or to keep authoring under the existing 28.1 directory (the invoked-split-of-a-sub-phase shape is novel; the 25.x/26.x sub-phases each had their own directory created at their SPEC, so a fresh `28.1b-…` directory is the expected default). The parent directory `docs/envoy-go/phases/28-network-filter-zookeeper-proxy/` (BRAINSTORM.md + parent SPEC.md) stays canonical; the 28.2 sub-phase directory is created at the 28.2 SPEC session.
- **lifecycle-state:** `phase 28.1a IMPL done (next = 28.1b SPEC)` — 28.1a reached IMPL-done on the invoked-split scope (PLAN Tasks 1–15 + the disabled 0046 driver; all gates green per PROGRESS.md). The NEXT session authors the **28.1b SPEC** (the read-side seam design + the re-scoped fixture/completion surface) per SKILL_ROUTING state 1 (a new sub-phase needs a SPEC before a PLAN). ADR-0045 §AMEND records the split invocation in place (no new ADR number consumed).
- **next-skill:** `superpowers:brainstorming` (scoped to **the 28.1b SPEC** — per SKILL_ROUTING state 1 [phase in ROADMAP, sub-phase directory does not yet exist → `superpowers:brainstorming` scoped to THIS sub-phase → output SPEC.md], mirroring how the 28.1 SPEC itself was authored by `superpowers:brainstorming` per the 22.1/25.1/26.1 per-sub-phase precedent). The 28.1b SPEC designs: the read-side seam (`readChainConn`-equivalent re-feeding post-handoff reads through the read chain — the PoC proved this achieves exact cross-side parity; R1-preserved by wrapping ONLY chains with ≥1 write filter; must honor the decoder/chain-buffer interplay — the SPEC §4.5 high-water mark assumes a CUMULATIVE buffer); 0046 re-enabled + green + R4 deliberate-break; 0047-zookeeper-boot-reject; the completion bundle (ADR-0221/0222 §Decision/§Consequences bodies; BEHAVIOR_CONTRACT 28.1 bundle incl. stat table 136 → 337; ROADMAP rollup decision). Then `superpowers:writing-plans` (28.1b PLAN) → `subagent-driven-development` (28.1b IMPL).
- **last-commit:** the 28.1a-closure squash (SHA filled by the post-squash fill commit on master) — `phase 28.1a completion bundle: STATE/ROADMAP/next-prompt for the ADR-0045 28.1a/28.1b split — 28.1a done (seam + zookeeperproxy request package + 37th fuzzer); 28.1b SPEC next (read seam + fixtures + completion)` (squash-merged from `phase-28.1-network-filter-write-seam-and-zookeeper-requests-impl`; the controller fills this SHA at the post-squash fill commit per the established fill-at-squash convention). Substantive predecessor on master: the 28.1-PLAN squash `29f9d38`.
- **last-updated:** 2026-06-02
- **next-free ADR:** `ADR-0224` (UNCHANGED at this 28.1a-closure commit — DECISIONS.md tail STAYS **ADR-0223**; the ADR-0221/0222/0223 §Context drafts landed at the parent SPEC per ADR-0044; NO new ADR number is consumed by the 28.1a/28.1b split [recorded as a one-line in-place §AMEND on ADR-0045 per the 25.2 IMPL Task 22 AMEND-acknowledgment precedent]; the ADR-0221 + ADR-0222 §Decision/§Consequences bodies land IN PLACE at the 28.1b completion bundle, the ADR-0223 body at the 28.2 IMPL; no further phase-28 ADR numbers anticipated).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
