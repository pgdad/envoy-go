# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `07.2-listener-chain-completion`
- **phase-directory:** `docs/envoy-go/phases/07.2-listener-chain-completion/` — currently contains a single `README.md` (68 lines, `ee45aba`) sibling-stub seeded at the 07.1 SPEC commit per the 06.2 / 05.2 stub precedent. The full 07.2 SPEC is authored at this sub-phase's lifecycle-state 1 → 2 transition. Parent `docs/envoy-go/phases/07-filter-chain-framework/` carries `BRAINSTORM.md` (688 lines, `da28039`) + parent master `SPEC.md` (113 lines, `ee45aba`) — both read-only history. Sibling 07.1 directory at `docs/envoy-go/phases/07.1-http-filter-framework/` is now closed read-only history (SPEC.md / PLAN.md / PROGRESS.md / REVIEW.md). Phases 04, 05 (+ 05.1, 05.2), and 06 (+ 06.1, 06.2) remain closed read-only history.
- **lifecycle-state:** `1` — SPEC has not yet been authored. The next session opens a `superpowers:brainstorming` (autonomous mode per ADR-0004) session against the parent phase-07 BRAINSTORM (`docs/envoy-go/phases/07-filter-chain-framework/BRAINSTORM.md` §1's split-table for the 07.2 carve-out + the rest of the BRAINSTORM for cross-cutting context) and the 07.2 sibling-stub README (`docs/envoy-go/phases/07.2-listener-chain-completion/README.md`); deliverable is the full 07.2 SPEC at `docs/envoy-go/phases/07.2-listener-chain-completion/SPEC.md` per the established 06.2 + 05.2 SPEC-shape precedent.
- **next-skill:** `superpowers:brainstorming` — phase 07.2 BRAINSTORM-then-SPEC session (state 1 → 2 per `BOOTSTRAP_PROMPT.md` §5 step 1). Autonomous brainstorming per ADR-0004 (the parent phase-07 BRAINSTORM at `da28039` already settled the 07.1+07.2 split + the 07.2 scope at brainstorm-close; a fresh autonomous-brainstorming session re-reads that BRAINSTORM and the sibling-stub README, then produces the 07.2 SPEC). The session writes `docs/envoy-go/phases/07.2-listener-chain-completion/SPEC.md` (per the 04 / 05.1 / 05.2 / 06.1 / 06.2 / 07.1 SPEC.md precedent), then advances STATE to lifecycle-state 2 (or 3 if the SPEC commit lands plan-document-reviewer-approved-PLAN-stub straight away — the project's choice).
- **next-skill-scope:** Lifecycle-state 1 → 2 SPEC-drafting session deliverable: full 07.2 SPEC drafted per the established 06.2 / 05.2 SPEC-shape (1 Purpose, 2 Non-purposes, 3 Phase-done gates, 4 Deliverables, 5 Architecture, 6+ Component-specific design, N-1 Empirical-pin obligations [if any], N Acceptance checklist), covering: (a) `listener_filters` framework — a from-scratch listener-filter dispatch pipeline that runs BEFORE HCM on each accepted downstream connection; the first concrete listener filters (likely `envoy.filters.listener.tls_inspector` for ClientHello-SNI extraction + possibly `envoy.filters.listener.original_dst`); (b) `FilterChainMatch` fields beyond SNI — `destination_port`, `prefix_ranges`, `source_prefix_ranges`, `source_ports`, `application_protocols` (ALPN; potentially supersedes ADR-0050), `transport_protocol` (`raw_buffer` vs `tls`); (c) `Listener.default_filter_chain` — the fallback chain (phase-03 errors at parse if set; 07.2 honors it). The 07.2 SPEC should: enumerate which parts of ADR-0033 (Phase-03 filter-chain subset) stay vs are superseded; identify the new 07.2-scoped fixture(s) (the BRAINSTORM does NOT pre-pin a fixture shape — the SPEC author decides); identify any empirical-pin obligations against reference Envoy v1.37.2 (chain-match precedence rules + `default_filter_chain` selection semantics are likely candidates for empirical pinning per the 06.1 / 06.2 / 07.1 SPEC-time-pin precedent); and assign anticipated ADR numbers (next-free at SPEC time per ADR-0004 — current tail is ADR-0076 from 07.1 so 07.2's first ADR is ADR-0077). The session also lands the ROADMAP edit (row 07.2 `planned → in-progress` per the 06.2 / 07.1 SPEC-commit pattern; row 07 stays `in-progress` AND its parent-row closure happens at 07.2's own phase-done commit per the 05/05.1/05.2 + 06/06.1/06.2 + 07/07.1 closure pattern). Per the 07.1 REVIEW.md I-3 finding: this 07.2 SPEC commit MUST atomically flip the row 07.2 to `in-progress` (do NOT leave it at `planned` through the impl session — the 07.1 SPEC commit's failure to flip its own row to `in-progress` is the precedent NOT to repeat).
- **last-commit:** `TBD` — `phase 07.1: phase-done — http-filter-framework lands; ROADMAP row 07.1 → done [ADR-0070, ADR-0071, ADR-0072, ADR-0073, ADR-0074, ADR-0075, ADR-0076]`. This phase-done commit lands `docs/envoy-go/phases/07.1-http-filter-framework/REVIEW.md` (309 lines; APPROVED WITH FOLLOW-UPS verdict; 0 Critical / 3 Important / 9 Minor findings; classifies + dispositions every finding) + `docs/envoy-go/ROADMAP.md` row 07.1 status `planned → done` (parent row 07 STAYS `in-progress` per the precedent — flips only at 07.2's phase-done) + this STATE.md rewrite to advance to phase 07.2 lifecycle-state 1 with `next-skill: superpowers:brainstorming`. Per ADR-0003: master FF'd to this phase-done commit's SHA-fill follow-up SHA. Per the 07.1 REVIEW I-3 finding: row 07.1 transitions `planned → done` directly (skipping the visibly-`in-progress` intermediate) — the 07.1 SPEC commit failed to flip its own row per the 06.2 SPEC-commit precedent; this phase-done commit closes the drift naturally. SHA filled in a follow-up commit per the phase-04..06.2 + 07.1 SHA-fill convention.
- **last-updated:** 2026-05-02

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
