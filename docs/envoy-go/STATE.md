# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `25 (in-progress) — 25.1 IMPL done at 2026-05-25; 25.2 BRAINSTORM done at 2026-05-25; 25.2 SPEC done at 2026-05-25 (this commit); awaiting 25.2 PLAN` — **phase 25.2 SPEC DONE at 2026-05-25 (this commit)** authoring the full advanced-bridge surface delta SPEC on top of 25.1's headers-only foundation per `superpowers:writing-plans` skill discipline (scoped to per-sub-phase SPEC authoring). 1658-line SPEC.md transcribes the 11 BRAINSTORM Q-decisions into formal SPEC shape across 15 sections + 2 appendices (mission summary + §1.1 5-AMEND empirical-finding catalog AMEND-B1..B5 + §2 25.2-specific non-purposes + §3 framework primitive evolutions (1 NEW package-level + 1 NEW infrastructure subpackage + 3 internal/wasm/ API evolutions + 7 REUSES + 1 MIGRATES) + §4 compiledConfig extensions + §5 35-37 hostcalls + 20 callbacks at 25.2 + §6 6 NEW PARSE-REJECT arms cumulative 18→24 + §7 9 NEW envoy-go-strict counters cumulative 119→128 + §8 fixture-0036 mixed-mode 12-14 scenarios + fixture-0037 subject-only boot-reject + §9 behavior-contract delta ~6 NEW departure records + §10 4 NEW ADR §Context drafts + ADR-0202 one-line in-place AMEND + ADR-0209 reserve + §11 5-pin empirical-pin block D-25.2-1..D-25.2-5 resolved IN-SESSION via parallel-subagent fan-out per ADR-0004 + §12 5 D-question carry-forwards D-25.2-P1..P5 + §13 R-25.2-1..R-25.2-12 RATIFIED-PENDING-IMPL + ~7-edit BEHAVIOR_CONTRACT bundle anticipation + §14 9-layer test surface + §15 46-item 25.2 IMPL acceptance checklist). 5 §11 empirical-pins resolved IN-SESSION via parallel-subagent fan-out against `proxy-wasm/spec@main:abi-versions/v0.2.1/README.md` + `proxy-wasm/proxy-wasm-cpp-host@da3ce05d` + `envoyproxy/envoy@v1.37.2` + `proxy-wasm/proxy-wasm-rust-sdk@v0.2.4` per ADR-0004 hard-gate discipline: AMEND-B1 buffer-clamp REFINES (cpp-host clamps silently on overflow vs spec README BAD_ARGUMENT text — envoy-go MUST mirror cpp-host); AMEND-B2 metric signedness + namespace REFINES (proxy_increment_metric delta is SIGNED int64 not unsigned; dynamic-stats namespace is `wasmcustom.<custom_name>` ONLY without plugin prefix — per-plugin isolation via per-plugin Registry scope); AMEND-B3 httpCall cancel-at-destruction CONFIRMS + RECOMMENDS new envoy-go-strict counter `http_call_response_after_close` (RAISES BRAINSTORM Q9 8-counter tally to 9 — project stat count 119→128 vs BRAINSTORM 127); AMEND-B4 property roster SUBSTANTIVE REFINEMENT (~10 dispatched roots + 4 direct tokens; ~70 sub-paths vs BRAINSTORM Q7 ~25; `xds.*` consolidates listener+route+cluster metadata; `upstream_filter_state` is DISTINCT root co-equal to filter_state); AMEND-B5 capability gate-at-registerCallback STRUCTURAL REFINEMENT (env-namespace gating happens at registerCallback time in wasm.cc:176-189 NOT at exports.cc call sites — denied hostcalls NOT registered on wazero Runtime; 21 NEW capability keys → post-25.2 cumulative 58 keys). 4 NEW ADR §Context drafts anchored at DECISIONS.md per ADR-0044 (ADR-0205 root VM lifecycle evolution per Q3; ADR-0206 25.2 ABI extensions per AMEND-B1+B2+B5+A9 + foreign-function registration interface; ADR-0207 NEW internal/filterstate/ framework primitive EXTRACT-NOW at second-consumer scope per Q7 with phase-22.2 lua MIGRATES non-breaking + upstream_filter_state distinct root per AMEND-B4; ADR-0208 internal/filter/http/wasm/ 25.2 package extensions + 9 envoy-go-strict counters + 4 envoy-go-strict-only config fields + dynamic-stats namespace + mixed-mode fixture-0036 + subject-only fixture-0037 + ~7-edit BEHAVIOR_CONTRACT bundle + 35th fuzzer + per-plugin Registry SCOPE discipline per AMEND-B2). ADR-0209 reserved at this commit per STRENGTHENED-WEAK-HOLD-with-1-slot per Q10 — likely candidate per-stream Module instantiation R8 escape-valve if 25.2 IMPL benchmark > 1ms per D-25.2-P2 + parent §13-R8 carry-forward (anticipated UNCONSUMED — 25.2 root-VM model retires per-stream Runtime construction so per-stream cost shrinks vs 25.1's 61µs WELL UNDER 1ms threshold). ADR-0202 one-line in-place AMEND acknowledgment paragraph at §Consequences anchored at 25.2 IMPL final Task per ADR-0044 in-place edit discipline (no new ADR number consumed; phase-22.2 Q10 strict-scope precedent). ADR-0125 STAYS at 10 canonicals UNCHANGED per AMEND-A3 inherited from parent SPEC. D-S2 RATIFIED at this 25.2 SPEC commit per phase-25.1 D-S1 precedent: 34-fuzzer baseline CONFIRMED at master tip (`grep -c "^func Fuzz" $(find . -name 'fuzz_test.go')` = 34); `FuzzWasmHostcallEnvelope` is 35th — ADR-0206 §Decision body + 25.2 BEHAVIOR_CONTRACT.md §13.4 patch pin to 35 at 25.2 IMPL final Task; parent §13-R5-equivalent at 25.2 CLOSED. 5 D-25.2-P1..D-25.2-P5 D-questions settled at SPEC §12 with PLAN/IMPL-time anchors (D-P1 fixture-0037 single-arm finalization → IMPL Task NN first-action; D-P2 per-stream Module instantiation R8 → IMPL benchmark task; D-P3 foreign-function dispatch concurrency model → PLAN session (mutex-per-RootVM anticipated); D-P4 FuzzWasmHostcallEnvelope corpus seed roster → PLAN OR fuzzer task; D-P5 BEHAVIOR_CONTRACT bundle line-count finalization → IMPL final Task). spec-document-reviewer subagent disposition at this 25.2 SPEC commit: Approved (pending review). Per ADR-0106 + phase-22.1/22.2/22.3 + phase-23 + phase-24.1/24.2 + phase-25.1 precedent, the 25.2 PLAN session is the next lifecycle-state-2 → state-3 transition for sub-row 25.2. ROADMAP row 25 STAYS `in-progress`; sub-row 25.1 STAYS `done`; sub-row 25.2 FLIPS `planned → in-progress` AT THIS SPEC COMMIT per ADR-0106 per-cell SPEC-anchored lifecycle annotation; sub-row 25.3 STAYS `planned`. Parent row 25 flips `in-progress → done` at 25.3's phase-done. Original 25.2 BRAINSTORM narrative preserved for historical reference: phase 25.2 BRAINSTORM DONE at 2026-05-25 (predecessor commit `0589f85`; SHA-fill `e800769`) authoring the full advanced-bridge surface delta per `superpowers:brainstorming` skill discipline. 11 Q-decisions settled (Q1 body per-chunk + accumulating buffer upstream-parity; Q2 16 MiB body cap envoy-go-strict default + configurable; Q3 ONE long-lived `*RootVM` per `*compiledConfig` upstream-byte-faithful + per-stream contexts as CHILDREN sharing wazero Runtime+Module; Q4 BadArgument + envoy-go-strict counter for unknown cluster; Q5 per-root-VM tick goroutine + envoy-go-strict 10ms period floor + Clock seam first co-consumer beyond phase-21; Q6 shared-data caps 1 MiB value + 1024-entry envoy-go-strict + 2 config fields; Q7 full property surface + EXTRACT-NOW `internal/filterstate/` consumer-#2 primitive with phase-22.2 lua MIGRATES; Q8 single-listener mixed-mode fixture-0036 + 12-14 scenarios partitioned by assertion-class; Q9 8-counter envoy-go-strict tally + dynamic-stats namespace + 1024-entry cap; Q10 strict-scope ADR consumption + 4 NEW + 1 reserve = ADR-0205..ADR-0209 + ADR-0202 one-line in-place AMEND acknowledgment; Q11 stay single sub-phase 25.2 + PLAN-stage split-gate arbiter per phase-22.2 Q14 precedent). spec-document-reviewer subagent disposition at BRAINSTORM commit: Approved (no blocking issues; 4 advisory cosmetic recommendations applied in follow-up commit). **phase 25.1 IMPL DONE at 2026-05-25 (predecessor commit `feded64`; SHA-fill `de4f853`)** atomic-landing all 17 tasks + 6 phase-done gates GREEN (A build / B vet+lint / C race / D differential 37/37 fixture dirs / E fuzz `FuzzWasmConfigParse` 30s clean + 34 fuzzers / F h2spec 53/53 PASS at ADR-0051 v1.32.4 pin) + Task 17 `BenchmarkPerStreamVM_Construction_Headers` reported `ns/op = 61000` (~61µs/stream; 17566 iters at 144212 B/op + 712 allocs/op on AMD Ryzen 9 9950X3D) — **D-P4 R8 STANDS WEAK-default; ADR-0205 NOT consumed at 25.1 IMPL; REDIRECTED at 25.2 SPEC to anchor the root VM lifecycle evolution per Q10 — ADR-0209 takes the escape-valve reserve slot at 25.2**. 3 NEW ADR §Decision + §Consequences bodies landed at 25.1 IMPL (ADR-0202 NEW `internal/wasm/` framework primitive + ADR-0203 NEW `internal/filter/http/wasm/` package shape + ADR-0204 default-deny capability sandbox); §Context anchors at parent SPEC commit `2c1455d` per ADR-0044 in-place edit discipline. 6-edit BEHAVIOR_CONTRACT.md bundle landed at 25.1 IMPL per ADR-0052. Phases 22 + 23 + 24 + 24.1 + 24.2 + 25-parent + 25.1-SPEC + 25.1-PLAN + 25.1-IMPL + 25.2-BRAINSTORM + 25.2-SPEC DONE and CANONICAL — DO NOT reopen.
- **phase-directory:** `docs/envoy-go/phases/25.2-http-filter-wasm-body-and-advanced-bridge/` (**SPEC.md authored at this commit** per `superpowers:writing-plans` skill scoped to 25.2 SPEC; README.md + BRAINSTORM.md DONE at predecessor commits; PLAN.md + PROGRESS.md + REVIEW.md await subsequent lifecycle sessions). Predecessor sub-phase directory `docs/envoy-go/phases/25.1-http-filter-wasm-runtime-and-headers-bridge/` (README.md + SPEC.md + PLAN.md + PROGRESS.md + REVIEW.md DONE at predecessor commits). Parent directory `docs/envoy-go/phases/25-http-filter-wasm/` (BRAINSTORM.md + SPEC.md DONE at predecessor commits). Sibling sub-phase directory `25.3-…/` carries pre-created README.md forward-pointer.
- **lifecycle-state:** `phase 25.2 SPEC done; awaiting 25.2 PLAN` (SKILL_ROUTING state 2 → 3 transition for sub-row 25.2 — 25.2 SPEC authoring complete + spec-document-reviewer Approved + cosmetic post-review fixes applied (if any); per ADR-0106 + phase-22.1/22.2/22.3 + phase-23 + phase-24.1/24.2 + phase-25.1 precedent, the 25.2 PLAN session is the next lifecycle-state-2 → state-3 transition for sub-row 25.2). ROADMAP row 25 STAYS `in-progress`; sub-row 25.1 STAYS `done`; sub-row 25.2 FLIPS `planned → in-progress` AT THIS SPEC COMMIT per ADR-0106 per-cell SPEC-anchored lifecycle annotation; sub-row 25.3 STAYS `planned`. Parent row 25 flips `in-progress → done` at 25.3's phase-done.
- **next-skill:** `superpowers:writing-plans` (scoped to **25.2 PLAN authoring** per the phase-22.1/22.2/22.3 + phase-23 + phase-24.1/24.2 + phase-25.1 PLAN precedent — the 25.2 PLAN session does precise task-graph estimation against ADR-0045 split-gate (~25 tasks); if exceeded, splits 25.2 into 25.2.1 + 25.2.2 at PLAN time per Q11 BRAINSTORM-defers-to-PLAN pattern. PLAN settles D-25.2-P3 foreign-function dispatch concurrency model (mutex-per-RootVM anticipated) + D-25.2-P4 FuzzWasmHostcallEnvelope corpus seed roster + D-P-PLAN-x decisions for IMPL ordering / Task graph parallelization / per-Task acceptance criteria / TDD discipline / subagent dispatch. Per BRAINSTORM §1.4 + 25.2 SPEC §1 + §3 + §5 envelope: ~22-28 tasks + ~5,000-7,500 LIVE production LoC + ~6,000-9,000 TEST LoC at 25.2 IMPL — task-arm gate borderline; PLAN session is the arbiter. NO NEW ADRs consumed at the PLAN commit (DECISIONS.md tail STAYS at ADR-0208; ADR-0205+0206+0207+0208 §Decision+§Consequences bodies land at 25.2 IMPL final Task; CONDITIONAL ADR-0209 only if R8 escape-valve fires at IMPL benchmark Task per D-25.2-P2 — anticipated UNCONSUMED). No alternative skill at this transition (per-sub-phase SPEC → PLAN convention).
- **last-commit:** `f0eae39` — `Squash merge phase-25.2-http-filter-wasm-body-and-advanced-bridge-spec`. The phase-25.2 SPEC squash-merge commit landing the 1658-line SPEC.md + 4 NEW ADR §Context drafts (ADR-0205+0206+0207+0208) + STATE.md re-advance + ROADMAP row 25.2 flipped `planned → in-progress` per ADR-0106 per-cell SPEC-anchored lifecycle annotation + ADR-0052 atomic-landing discipline. SHA filled in at this stage-close follow-up commit per the phase-22.2 + phase-22.3 + phase-23 + phase-24.1 + phase-24.2 + phase-25-parent + phase-25.1-SPEC + phase-25.1-PLAN + phase-25.1-IMPL + phase-25.2-BRAINSTORM stage-close SHA-fill precedent. Predecessor master tip (pre-25.2-SPEC): `b4720ab` — `next-prompt.txt: repoint master-tip references to 404a16e (actual HEAD)`.
- **last-updated:** 2026-05-25
- **next-free ADR:** `ADR-0209` (4 NEW §Context drafts consumed at this 25.2 SPEC commit: ADR-0205 root VM lifecycle + ADR-0206 25.2 ABI extensions + ADR-0207 NEW `internal/filterstate/` framework primitive + ADR-0208 NEW `internal/filter/http/wasm/` 25.2 package extensions; DECISIONS.md tail advances from `ADR-0204` to `ADR-0208`). ADR-0209 reserved at this commit per STRENGTHENED-WEAK-HOLD-with-1-slot per Q10 — likely candidate per-stream Module instantiation R8 escape-valve if 25.2 IMPL `BenchmarkPerStreamModule_Instantiation > 1ms` per D-25.2-P2 + parent §13-R8 carry-forward (anticipated UNCONSUMED — the 25.2 root-VM model retires per-stream Runtime construction so per-stream cost shrinks vs 25.1's 61µs WELL UNDER 1ms threshold; ADR-0209 carries forward to 25.3 if unconsumed). **D-hypothesis disposition (RESOLVED at this 25.2 SPEC commit)**: STRENGTHENED-WEAK-HOLD-with-1-slot-buffer STANDS — 4 anticipated NEW ADRs land cleanly at 25.2 IMPL; 0-1 escape-valve slot consumption from the R8 benchmark surface. The probability is LOW (25.1 R8 observed 61µs/stream; the 25.2 root-VM model REDUCES per-stream cost further; new per-stream cost surfaces — body buffer alloc + property-tree lookups — are anticipated at low microseconds each). WEAK-HOLD STANDS. Anticipated next-free after 25.2 phase-done: **`ADR-0210`** if reserve UNCONSUMED (4 numbers consumed); **`ADR-0211`** if reserve consumed (5 numbers). Anticipated next-free after 25.3 phase-done: **`ADR-0213`** if all 25.3 reserves UNCONSUMED (matching parent SPEC §1.2 + BRAINSTORM §7.4 estimate of ADR-0202..ADR-0213 across the family); 25.3 anticipated ADRs ADR-0210 EXPLICIT-NO-NEW-CANONICAL per-route classification per AMEND-A3 + ADR-0211 multi-plugin VM-sharing semantics + ADR-0212 conformance harness pin + escape-valve ADR-0213.

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
