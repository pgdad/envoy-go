# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `18.1-ext-authz-http` — phase 18.1 PLAN session COMPLETE (state-3 entry per ADR-0005 §Decision 4). The PLAN session authored `docs/envoy-go/phases/18.1-ext-authz-http/PLAN.md` — a 15-task task-decomposed implementation plan turning the 18.1 SPEC into concrete IMPL tasks: Task 1 (preconditions + PROGRESS.md preamble), Tasks 2–9 (the `internal/filter/http/extauthz/` package — `doc.go` + `extauthz.go` skeleton + `compiledConfig` + `filterStats` [T2, ADR-0156/0157] → `check.go` HTTP-outbound auth-check [T3, ADR-0159] → `attributes.go` `AuthorizationRequest` builder + request-side header filtering [T4, ADR-0160] → bidirectional header-mutation discipline [T5, ADR-0161] → `with_request_body` ADR-0128 reuse [T6, ADR-0162] → per-route 5th-canonical REUSE + SHARED-stats [T7, ADR-0163] → stat surface finalization + §18.P6/§18.P7 RATIFIED-PENDING closures [T8] → `DecodeHeaders` dispatch + async-resume outbound-call leg + `OnDestroy` cancellation [T9]), Task 10 (boot-registration + 22nd fuzzer + NEW `test/helpers/extauthzhttp/` helper + fixture infrastructure), Tasks 11–13 (fixture `0020-http-ext-authz-http` — driver + bootstraps + expectations/README + end-to-end differential pass + §18.P11 RATIFIED-PENDING closure), Task 14 (BEHAVIOR_CONTRACT 6-patch bundle + ROADMAP row 18.1 `→ done` + STATE.md advance + 6-gate phase-done verification), Task 15 (REVIEW.md). The PLAN settled all 7 SPEC §12 deferred decisions (D1–D7) + 5 PLAN-emergent decisions (D8–D12); refined the §10 ADR Lands-in-Task hypotheses into the authoritative table (ADR-0156/0157→T2, 0159→T3, 0160→T4, 0161→T5, 0162→T6, 0163→T7; ADR-0044 escape-valve → ADR-0165 at T9 if it fires); assigned the 3 RATIFIED-PENDING-IMPL-TIME pin closures (§18.P6 + §18.P7 → Task 8 empirical scrape; §18.P11 deny-path header-ordering → Task 13 fixture-harness diff). A plan-document-reviewer subagent reviewed the PLAN and APPROVED it. ROADMAP rows `18` (`in-progress`, sub-phases 18.1/18.2), `18.1` (`in-progress`), `18.2` (`planned`) are unchanged at this PLAN commit (per ADR-0005 §Decision 4 PLAN sessions add no ROADMAP rows).
- **phase-directory:** `docs/envoy-go/phases/18-http-filter-ext-authz/` (BRAINSTORM.md + parent master SPEC.md) + `docs/envoy-go/phases/18.1-ext-authz-http/` (SPEC.md + PLAN.md landed; PROGRESS.md + REVIEW.md pending — authored during the IMPL session) + `docs/envoy-go/phases/18.2-ext-authz-grpc/` (sibling stub README.md; full SPEC.md pending — drafted at 18.2's lifecycle-state 1, after 18.1 is `done`).
- **lifecycle-state:** `phase 18.1 SPEC.md + PLAN.md authored; IMPL pending` — state-3 entry per ADR-0005 §Decision 4. The phase-18.1 PLAN session turned the 18.1 SPEC into a 15-task implementation plan, settled the §12 deferred decisions, refined the ADR anchor map, assigned the RATIFIED-PENDING pin closures, and advanced this STATE.md. The next session is the phase-18.1 IMPL session (`superpowers:subagent-driven-development` per project memory `feedback_execution_style.md`; ADR-0005 §Decision 4 — BRAINSTORM / SPEC / PLAN / IMPL / REVIEW are SEPARATE sessions) executing `docs/envoy-go/phases/18.1-ext-authz-http/PLAN.md` task-by-task.
- **next-skill:** `superpowers:subagent-driven-development` — the next session executes `docs/envoy-go/phases/18.1-ext-authz-http/PLAN.md` (15 tasks; fresh subagent per task + two-stage review) per project memory `feedback_execution_style.md` (subagent-driven preferred over inline `superpowers:executing-plans`). Per ADR-0005 §Decision 4 this is a SEPARATE session from PLAN.
- **next-skill-scope:** Cold-start scope for the next (phase-18.1 IMPL) session. Read first: STATE.md (this file); `docs/envoy-go/phases/18.1-ext-authz-http/PLAN.md` (THE execution document — the `## Execution preconditions` block's 17 checks, the 15-task list, the `## Planner-time deferred-decision resolution` D1–D12, the `## ADRs introduced by this plan` Lands-in-Task table, the `## File structure` decomposition); `docs/envoy-go/phases/18.1-ext-authz-http/SPEC.md` (the authoritative SPEC the PLAN derives from — §1 purpose + §3 framework survey/ADR-0159 disposition (b) + §4 deny-path wire shape + §5 per-route 5th-canonical-REUSE + §6 compiledConfig + code shapes + §7 differential fixture + §10 ADR anchor map + §12 deferred decisions + §13 BEHAVIOR_CONTRACT additions + §14 testing + §15 acceptance checklist); `docs/envoy-go/phases/18-http-filter-ext-authz/SPEC.md` (parent master SPEC — §4 cross-cutting decisions, §5 the 13-pin empirical-pin block, §6 the 12-amendment block, §7 the 9-ADR anchor map, §8 the parent phase-done gate); `docs/envoy-go/DECISIONS.md` (tail at ADR-0164; next-free ADR-0165; ADR-0156..ADR-0163 §Context drafts — §Decision + §Consequences land at each ADR's Lands-in-Task per ADR-0044; ADR-0164 full; ADR-0125 8-canonical roster — phase 18 REUSES the 5th, adds NONE; ADR-0044 ADR-on-impl convention; ADR-0128 decode-side body-buffering; ADR-0085 `SendLocalReply`; ADR-0072 extension-registry registration; ADR-0114 single-token directory; ADR-0100 §2.2 boot-registration ordering; ADR-0117 per-route `sync.Map` lazy-cache; ADR-0150 `internal/jwks/Fetcher` — composed-against); `docs/envoy-go/ROADMAP.md` (rows 18 `in-progress`, 18.1 `in-progress` → flips `done` at the 18.1 phase-done commit, 18.2 `planned`); `docs/envoy-go/BEHAVIOR_CONTRACT.md` (10 HTTP filters documented through phase 17; HCM stat-table at 71 names — 18.1 grows it to 77; the framework-primitive sections + the `## JWKS framework primitive` umbrella the ADR-0159 HTTP-outbound auth-check composes against) + `docs/envoy-go/SKILL_ROUTING.md` + `docs/envoy-go/ENVOY_TARGET.md` (reference Envoy v1.37.2 pin). 18.1 scope per the PLAN: NEW `internal/filter/http/extauthz/` package (`doc.go` + `extauthz.go` + `check.go` + `attributes.go` + `extauthz_test.go` + `fuzz_test.go`; HTTP service mode only — `grpc_service` PARSE-REJECTs in 18.1); the thin ext_authz-local HTTP-outbound auth-check (ADR-0159 disposition (b)); request-side header filtering via the top-level `ExtAuthz.allowed_headers`/`disallowed_headers`; HTTP-mode `AuthorizationResponse` allow/deny header extraction; `with_request_body` via the ADR-0128 reuse; per-route 5th-canonical REUSE + SHARED-stats + the 6-counter stat surface; the `failure_mode_allow`/`status_on_error` error posture; the async-resume outbound-call leg + per-request cancellable `context.Context`; boot-registration (alphabetical between `envoygotest` and `fault`); differential fixture `0020-http-ext-authz-http` (7 HTTP-mode scenarios) + 1 NEW shared test-helper `test/helpers/extauthzhttp/` (in-process scriptable HTTP auth server) + the 22nd fuzzer `FuzzExtAuthzConfigParse`. The IMPL session opens a fresh worktree `phase-18.1-ext-authz-http-impl` branched off master tip; squash-merges back at session close; pushes to origin without asking.
- **last-commit:** `9f786f7` — `Squash merge phase-18.1-ext-authz-http-plan`. The phase-18.1 PLAN worktree `phase-18.1-ext-authz-http-plan` (branched off master tip `312beec`, the phase-18 SPEC SHA-fill follow-up) carried 1 commit (`971cd72`); squash-merged to master as one PLAN commit `9f786f7` (2 files, +666/-7). This `phase 18.1 plan follow-up` commit is the SHA-fill follow-up (TBD-phase-18.1-plan-squash-merge → `9f786f7` post-squash) per the phase-09..17 PLAN-stage close pattern. Predecessor master tip: `312beec` (`phase 18 spec follow-up: STATE.md SHA-fill (TBD → 308e9b6 post-squash)`). Pushed to origin per the always-push-to-origin rule.
- **last-updated:** 2026-05-14
- **next-free ADR:** `ADR-0165` — UNCHANGED by the PLAN session (PLAN sessions anchor no ADRs). Phase 18 anchored 9 ADRs (ADR-0156..ADR-0164): ADR-0156..ADR-0163 are §Context-drafted at the SPEC commit (§Decision + §Consequences land at each ADR's Lands-in IMPL anchor-task per ADR-0044 — the PLAN's Lands-in-Task table is the authoritative anchor map); **ADR-0164** (the ADR-0045 split-application ADR) landed IN FULL at the SPEC commit. NO ADR-0125 amendment paragraph (ADR-0163 records the explicit no-amendment 5th-canonical-REUSE decision). The ADR-0044 escape-valve is held in reserve for ~1 impl-time-unanticipated ADR — most-likely the async-resume-after-`OnDestroy` race guard → `ADR-0165` at IMPL Task 9 if it fires. ADR tail is at `ADR-0164`; next-free is `ADR-0165`.

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
