# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `07-filter-chain-framework`
- **phase-directory:** `docs/envoy-go/phases/07-filter-chain-framework/` — does NOT exist yet (phase-07 brainstorm session creates it). Per BOOTSTRAP §8 phases 00–08 ordered seed: phase 07 is filter-chain-framework (iteration protocol, per-route config, extension registry; trivial pluggable filter covers all iteration states; framework fixtures green). Closes parent ROADMAP rows 06.2 + 06 → done at THIS phase-done commit (mirrors 05/05.1/05.2 closure pattern); phase 06 done = parent 06 done because 06.2 was the last sub-phase of 06 (06.1 closed first per its own phase-done).
- **lifecycle-state:** `6` — phase 06.2 complete (and phase 06 complete because 06.2 was the last sub-phase). The phase-06.2 access-log subsystem lands: a from-scratch `internal/accesslog/` package (322 LoC: `accesslog.go` Sink interface + Record struct, `format.go` Default formatter per the Envoy v1.37.2 default-format empirical pin with `\` + `"` + `\n` + `\r` escape catalog matching reference Envoy `AccessLogFormatUtils::escapeUtilityValue` + RFC 4180 CSV-style, `writer.go` AsyncFileSink with 1024-record buffered channel + drop-newest backpressure, `stats.go` server.accesslog_dropped counter wiring per ADR-0069 SN5; the package's only external dependency is the Go stdlib `bytes`/`strconv`/`strings`/`time`/`os`/`sync` — D-3.2 grep clean, no third-party access-log library); four HCM emit-deferral hook sites (`actions.go:106` H1 directResponse, `actions.go:158` H1 router, `actions.go:283` H2 router via `emitAccessLogH2`, `h2dispatch.go:96` H2 directResponse); `byteCounterWriter` adapter wrapping the H1 `bufio.Writer` for accurate `BYTES_SENT` accounting; `Cluster.Dial`/`Cluster.DialH2` return-tuple expansion to surface the picked endpoint for `UPSTREAM_HOST` operator (per Decision A); `internal/bootstrap/bootstrap.go` parses `access_log[]` with strict `log_format` rejection at parse boundary (ADR-0067 extending ADR-0065's boundary-validation pattern); `cmd/envoy-go/main.go` opens AsyncFileSinks + threads them into HCM Filter instances + defers Close; differential fixture `0006-access-log` with the per-record three-tier equivalence matrix per ADR-0068 (7 Tier-E + 3 Tier-F + 5 Tier-S = 15 operators; RESP-SVC-TIME demoted from Tier-E to Tier-S during Task 15 per Decision A — reference Envoy injects X-Envoy-Upstream-Service-Time but envoy-go does not); `FuzzAccessLogFormat` 8th fuzzer at the ADR-0018 30s budget; `BEHAVIOR_CONTRACT.md ## Access log field mapping` populated in-place per ADR-0052 with the 15-operator format + three-tier matrix + verbatim empirical-pin scrape from SPEC §11. Four ADRs landed (ADR-0066/0067/0068/0069); the gate-(d) verifier-detour at `503c8ee` → `3fe7fbf` → `d0e13ae` caught and fixed an `escape()` backslash-catalog gap that the impl session's -run-only sweep missed (reasonable no-ADR per the 06.1 79be6b0 precedent; SPEC §11's "per Envoy convention" was under-specified but consistent with the fix). REVIEW.md at `dbff215` returned APPROVED WITH FOLLOW-UPS (zero Critical, two Important — I-1 SPEC vs ADR-0068 tier-count drift accepted as corrigendum, I-2 BEHAVIOR_CONTRACT empirical-pin discipline drift closed at `9f7b68f` via verbatim re-paste; eleven Minor carry forward to a post-phase-done L4 review-followup batch). All five executable gates GREEN at THIS commit; gate (f) closed by REVIEW.md.
- **next-skill:** `superpowers:brainstorming` — phase 07 brainstorm session (lifecycle-state 6 → 1 transition per BOOTSTRAP §5 step 6 → step 1; mirrors 06.1 → 06.2 transition at ae8276b's STATE update). Phase 07 is filter-chain-framework per BOOTSTRAP §8 + ROADMAP row 07. Brainstorm explores: filter iteration protocol shape (per-filter-callback-return vs explicit-state-machine), per-route filter config (typed_config vs pluggable extension registry), the "trivial pluggable filter covers all iteration states" deliverable, framework fixture design.
- **next-skill-scope:** Create worktree `.worktrees/phase-07-filter-chain-framework-brainstorm` on branch `phase/07-filter-chain-framework-brainstorm` branched from this commit's master fast-forward HEAD per ADR-0003 + per-phase-worktree convention. Per BOOTSTRAP §5 step 1 + the 06.1 / 06.2 brainstorm precedents: explore the design space, settle 3-7 cardinal decisions, draft `docs/envoy-go/phases/07-filter-chain-framework/BRAINSTORM.md` with sections per ADR-0040 (Mission, Scope, Constraints, Decisions, Open Questions, Out-of-scope deferrals). Brainstorm output drives the SPEC session (state 1 → 2). The L4 review-followup batch closing 11 Minors from phase 06.2 REVIEW.md `## Concerns — Minor` is a separate post-phase-done branch — NOT phase 07's scope; can run in parallel with the phase-07 brainstorm.
- **last-commit:** `2c65fcc` — `phase 06.2: phase-done — access-log lands; ROADMAP rows 06.2 + 06 → done [ADR-0066, ADR-0067, ADR-0068, ADR-0069]`, on branch `phase/06.2-access-log-review-followup`. Flips ROADMAP rows 06.2 + 06 status `in-progress → done` (one-step `in-progress → done` flip; row 06 closes WITH 06.2 per the parent SPEC §5 closure pattern + the 05/05.1/05.2 precedent). Updates STATE.md `lifecycle-state` 5 → 6 with `active-phase` advanced to `07-filter-chain-framework` and `next-skill` set to `superpowers:brainstorming` per BOOTSTRAP §5 step 6 → step 1 transition. Names all four ADRs introduced in 06.2 per BOOTSTRAP §5.3: ADR-0066 (access-log architecture; Task 2 commit `76f3ecd`), ADR-0067 (reject log_format at parse; Task 7 commit `6949fce`), ADR-0068 (three-tier equivalence matrix for fixture-0006; Task 15 commit `085890d`), ADR-0069 (server.accesslog_dropped counter naming SN5; Task 5 commit `5278161`). Subject-line ADR ordering follows numerical sequence per the project's standard subject-line convention. SHA-fill follow-up per phase-02..06.1 convention.
- **last-updated:** 2026-04-30

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
