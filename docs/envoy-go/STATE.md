# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `14-http-filter-compressor` — phase 14 SPEC.md authored at this commit. ROADMAP row 14 status flipped `planned → in-progress` per BOOTSTRAP_PROMPT.md §4.1 invariant 3 + §5 lifecycle-state-2 → 3 transition. Phase 14 is the SEVENTH §9 family-row in the lifecycle (after cors @ 07.1 done, fault @ 09 done, header_mutation @ 10 done, local_ratelimit @ 11 done, csrf @ 12 done, buffer @ 13 done); the §9 family heading at ROADMAP line 56 stays unchanged per ADR-0106(c). The SPEC drafting session executed all 15 BRAINSTORM §9 empirical pins IN-SESSION against reference Envoy v1.37.2 per ADR-0004 — 9 pins ratified verbatim, 6 pins refuted in part or whole, driving 6 §1.1 amendment blocks (mirrors phase-12 csrf 4-amendment SPEC-time-correction precedent; extends rather than phase-13 buffer §12 brainstorm-amendment-cycle precedent). The §3 framework survey concluded Path A FIRES: phase 14 introduces `EncoderFilterCallbacks.OverwriteBody(b []byte)` symmetric to phase-13 ADR-0128's decode-side primitives (~20-25 LoC framework delta across `internal/filter/http/callbacks.go` + `chain.go` + `internal/filter/hcm/connection.go` + `h2dispatch.go`).
- **phase-directory:** `docs/envoy-go/phases/14-http-filter-compressor/` — contains `BRAINSTORM.md` (541 lines; pre-SPEC design sketch) + `SPEC.md` (this commit; ~2050 lines covering §1 mission/scope + §1.1 six amendment blocks + §1.2-§1.7 framework + §2 non-goals + §3 phase-done gates + §4 deliverables + §5 architecture + per-request flows + §6 per-component contracts + §7 differential fixture 0016 + §8 ADR roster + §9 sibling-stub + §10 acceptance review claims + §11 15-pin empirical block with verbatim probe transcripts + §12 deferred decisions + §13 BEHAVIOR_CONTRACT additions + §14 testing strategy + §15 acceptance checklist). PLAN.md, PROGRESS.md, REVIEW.md authored over phase lifecycle (states 3 → 6).
- **lifecycle-state:** `phase 14 SPEC.md authored; PLAN.md pending` — phase 14 SPEC.md authored at this commit; PLAN.md not yet authored. Lifecycle-state-2 → 3 transition (BOOTSTRAP_PROMPT.md §5) completed by this commit; lifecycle-state-3 → 4 (PLAN authoring) is the next session's responsibility per ADR-0005 §Decision 4 SPEC/PLAN session split. Phase 13's six gates remain green at parent commit `51b9ea6`; this commit is docs-only (no code changes; no test re-validation needed).
- **next-skill:** `superpowers:writing-plans` — per BOOTSTRAP_PROMPT.md §5 lifecycle-state-3 entry condition (SPEC.md exists, PLAN.md does not). Per ADR-0005 §Decision 4, PLAN authoring is its own inline-controller session separate from SPEC authoring; the next session reads SPEC.md as authoritative input + drafts PLAN.md with task breakdown / acceptance criteria / impl ordering.
- **next-skill-scope:** Cold-start scope for the phase 14 PLAN-authoring session. Read first: `docs/envoy-go/phases/14-http-filter-compressor/SPEC.md` (THE authoritative input — 2050 lines; every load-bearing decision lives there); `docs/envoy-go/phases/13-http-filter-buffer/PLAN.md` + `phases/12-http-filter-csrf/PLAN.md` (most-recent PLAN precedents — ~12-task PLANs with per-task TDD micro-decomposition); `docs/envoy-go/DECISIONS.md` ADR-0125 amendment + ADR-0129..ADR-0133 (the 5 new ADRs the SPEC drafted in their final form for impl-anchor scheduling); `docs/envoy-go/ROADMAP.md` row 14 (now `in-progress`). The PLAN author breaks the SPEC's ~14-18 task surface into discrete impl tasks with TDD test-first ordering; each task lands one ADR (ADR-on-impl convention per ADR-0044) OR contributes to a multi-ADR foundation (anticipated: Task 4 lands ADR-0131 framework primitive; Task 6 lands ADR-0132 stat surface; Task 11 lands ADR-0133 fixture discipline). The PLAN author MAY split the row per ADR-0045 if surface exceeds 1500 LoC / 25 tasks (SPEC's position: single-row at ~1640-1955 LoC / ~14-18 tasks; near but under threshold); natural split shape per SPEC §1.4 is 14.1 (listener-level + framework primitive + 4 listener-only fixture scenarios) and 14.2 (per-route + 2 per-route fixture scenarios + ETag-mutator + rmAE backend-echo).
- **last-commit:** `<TBD-pending-this-SPEC-commit>` — `phase 14 SPEC: http-filter-compressor [in-progress + SPEC.md authored + 6 §1.1 amendments + ADR-0125 amendment + ADR-0129..ADR-0133 + ROADMAP row 14 in-progress + STATE.md flip]`. Squash-merge of the SPEC-authoring chain on the `phase-14-http-filter-compressor-spec` worktree branch: (a) SPEC.md (~2050 lines) authored covering all 15 sections + 15 empirical pins resolved against reference Envoy v1.37.2; (b) ADR-0125 in-place amendment paragraph adding §(viii)-(x) per phase-13 ADR-0127-v2 precedent; (c) ADR-0129..ADR-0133 authored in final form for impl-anchor scheduling; (d) ROADMAP row 14 `planned → in-progress`; (e) STATE.md flipped from "phase 14 brainstormed; SPEC.md pending" to "phase 14 SPEC.md authored; PLAN.md pending". Docs-only commit; no code changes; phase 13's six gates remain green at parent commit `51b9ea6`.
- **last-updated:** 2026-05-10

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
