# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `phase 32.2 (network-filter-redis-commands-and-stats) IMPL done` (2026-06-09, this commit) — **the phase-32.2 IMPL COMPLETE** (`docs/envoy-go/phases/32.2-network-filter-redis-commands-and-stats/PROGRESS.md`): all 12 TDD tasks landed subagent-driven (Task 1 baselines gate → the static 180-command `supportedCommandList`/`supportedCommands` table → the `commandVerdict`/`classify` dispatch + the ECHO/TIME/QUIT/HELLO local-reply set + `decodeRequest` arg exposure → the EAGER 540 per-command + 2 splitter + 3 REDIS_CLUSTER_STATS counters → the 2 lifecycle gauges' inc/dec + `cx_protocol_error` → the `Handle`/`serveRequest` pump rewire → the `redis.` SINGLE-label hoist arm → `FuzzRESPDecode` [41st] → the `TCPRedisResponder` reply-table extension → the `0055` command-matrix arms + the `cx_active` held-open gauge arm → this completion bundle). The 5 as-built deviations (all faithful — the reference is the source of truth): (1) `commandsWithoutMandatoryArgs={hello}` (HELLO no-arg PROXIED); (2) `TestHandle_ProtocolErrorIncrements` uses `+bad` not `?bad` + 3 hardened pump tests; (3) **D-S32.2-2** — the UNKNOWN-command error APPENDS the request args (byte-exact cross-side `BOGUSCMD x` → `-ERR unknown command 'BOGUSCMD', with args beginning with: x\r\n`; the SPEC-anticipated empty suffix corrected live; NO name.go reconciliation needed); (4) **D-P32-9** — `cluster.redis_cluster.upstream_cx_total` PER-SIDE PIN (ref=1 pooled / subj=4 one-conn-per-downstream; `upstream_rq_total`==5 cross-side equal); (5) the 2 `*_bytes_buffered` gauges a SUBJECT-SIDE coverage boundary (subj==0 asserted; reference rx_bytes_buffered==14 from the held PING). Six gates GREEN (build / vet / golangci-lint / `-race -short` / the full 58-dir differential byte-exact PASS / h2spec 53/53 + proxy-wasm 10/10 asserted-UNAFFECTED — phase 32.2 touches no HTTP/h2/proxy-wasm path). The ADR-0229 §Decision/§Consequences 32.2 half completed IN-PLACE (PARTIAL → ACCEPTED per ADR-0044 — NO new ADR; DECISIONS tail STAYS ADR-0230). Counts advanced: stat surface 546 → **1091** (+545: 2 splitter + 3 cluster + 540 per-command), fuzzers 40 → **41** (`FuzzRESPDecode`), fixtures **58** (`0055` extended, no new dir), BackendKind tail **32** (reply-table extended). ROADMAP parent row 32 + sub-row 32.2 flip `in-progress → done` ATOMICALLY (the §9 in-row ROLLUP per ADR-0106(d)); the §9 Network-filters family stays OPEN — {thrift} remains the sole candidate. **Next → the phase-33 {thrift} BRAINSTORM** (`superpowers:brainstorming`). **Earlier (the phase-32.2 PLAN, preserved):** the SPEC §14 12-task TDD spine decomposed into bite-sized tasks; plan-document-reviewer APPROVED iteration 2 (the one blocking issue [Task 3 Step 5 missed the 7 `resp_test.go` `decodeRequest` callers] fixed in the loop); PLAN-time D-dispositions D-S32.2-1 (sorted-slice golden + lookup map) / D-S32.2-4 (`decodeRequest` `args [][]byte` exposure) / D-S32.2-5 (`commandVerdict.closeAfter` QUIT signal) / D-S32.2-6 (`/stats/prometheus` label-aware scrape) / D-S32.2-7 (`refHeld`/`subjHeld` held-open gauge); ADR-0045 split-gate FINAL re-check NO split (12 tasks / ~330-510 prod LoC; the 32.2a/32.2b escape-valve UNCONSUMED). **Earlier (the phase-32.2 SPEC, preserved):** the SECOND/final sub-phase of the phase-32 `redis_proxy` row, building ON the as-built 32.1 seam + codec + foundation. The SPEC pins the **full single-route command set** (the EAGER 180-command `command.<cmd>.{total,success,error}` roster routed via catch_all + the unknown→`splitter.unsupported_command` / bad-arity→`splitter.invalid_request` dispatch), the **+5 fixed counters** (2 `splitter.*` + 3 `REDIS_CLUSTER_STATS`), the **2 lifecycle gauges' inc/dec** (`downstream_cx_active` mirrored + `downstream_rq_active` quiesced; the 2 `*_bytes_buffered` gauges exist-at-0 — framework-buffer-managed-upstream coverage boundary, the D-P32-8 finding), the **`redis.` LABEL-HOISTED Prometheus arm** (`envoy_redis_<leaf>{envoy_redis_prefix="<sp>"}` — the mongo `.rbac.` single-label hoist), the **extended local-reply set** (ECHO/QUIT deterministic differential arms; TIME wall-clock non-deterministic unit-test-only; HELLO split — `>2`-args/`bad-proto` local errors incl. `HELLO 3`→NOPROTO, `HELLO 2`/no-arg PROXIED), the **differential command matrix** (extending `0055`, NO new dir) + the `TCPRedisResponder` reply-table extension, the **41st fuzzer `FuzzRESPDecode`**, the BEHAVIOR_CONTRACT 32.2 bundle + the **parent-row-32 ROLLUP** (at the 32.2 IMPL), and the **ADR-0229 §Decision/§Consequences 32.2 half** (completes PARTIAL → ACCEPTED at the 32.2 IMPL — NO new ADR number). Stat surface **546 → 1091** (+2 +3 +540; latency histograms + `*_fault` counters NOT counted per ADR-0060/faults). Resolves the parent's 32.2-owned D-questions: **D-P32-2** (the EXACT 180-command table — transcribed from upstream Envoy v1.37.2 `supported_commands.h` + `command_splitter_impl.cc`: simpleCommands 152 + eval 2 + object 1 + hashMultipleSumResult 4 + mget/mset/scan/info.shard 4 + randomShard 2 + transaction 5 + ClusterScope 10 = 180; FULL eager table; success-on-any-reply / error-on-transport-failure single-server semantics), **D-P32-6** (full local-reply extent), **D-P32-7** (IsValidName by construction — static table), **D-P32-8** (gauge design — cx_active held-open LIVE + the rest quiesced-zero; the 2 buffered gauges framework-buffer-managed-upstream), **D-P32-9** (one-conn-per-downstream per-side asymmetry — per-arm sequential conns hold equality; concurrent-downstream not exercised). ADR-0045 split-gate SPEC re-check: ~325-500 prod LoC / ~12-15 tasks → fits as ONE sub-phase (32.2a/32.2b escape-valve UNCONSUMED). NO new docker probes (the parent §11 live-probe block is authoritative; the only SPEC-time evidence was the upstream v1.37.2 C++ source transcription, §12). spec-document-reviewer APPROVED (2 non-blocking nits fixed in the loop). At THIS SPEC commit ROADMAP sub-row 32.2 flips `planned → in-progress` (parent row 32 STAYS `in-progress`; the ROLLUP is the 32.2 IMPL's) + the stale row-32.2 text corrected (DYNAMIC→EAGER, inline-vs-hoist→LABEL-HOISTED). Counts UNCHANGED at the SPEC (fixtures 58 / fuzzers 40 / stat surface 546 / BackendKind tail 32; DECISIONS tail ADR-0230). (The 32.2 PLAN — `superpowers:writing-plans` — is now DONE; see the active-phase header above. Counts STAY UNCHANGED at the PLAN; they advance at the 32.2 IMPL.) **Earlier (the phase-32.1 IMPL, preserved):** the phase-32.1 IMPL COMPLETE: all 16 TDD tasks landed (`docs/envoy-go/phases/32.1-network-filter-redis-upstream-pool-seam-and-codec/PLAN.md` + `PROGRESS.md`) — the upstream-pool seam (`UpstreamConn`/`UpstreamDialFunc` in `internal/filter/network/upstreampool.go`), the RESP codec (request + reply decode + encode, `resp.go`), the `redisproxy` package foundation (10th built-in, config parse, byte-stable rejects, PING/AUTH local-reply, 10-name EAGER stat roster, the `Handle` pump), the `TCPRedisResponder` BackendKind (value 32), the `0055-redis-roundtrip` cross-side + `0056-redis-boot-reject` fixtures, the NEW `GET /stats` flat admin endpoint (the EIGHTH admin endpoint, required for the `StatsAsserter`; +10 stats accounted there), and the ADR-0230 §Decision/§Consequences body + the ADR-0229 §Decision/§Consequences 32.1 half landed in place (per ADR-0044). Counts advanced: stat surface 536 → **546**, differential fixtures 56 → **58**, fuzzers **40** (unchanged), BackendKind tail **32**, DECISIONS tail **ADR-0230**. Six gates GREEN (build / vet / golangci-lint / `-race -short` / the full 58-dir differential `ok` / h2spec 53/53 + proxy-wasm 10/10). **Next → the 32.2 SPEC** (`network-filter-redis-commands-and-stats`, `superpowers:writing-plans`). ROADMAP sub-row 32.1 is now `done` (parent row 32 STAYS `in-progress` until the 32.2 ROLLUP); sub-row 32.2 `planned`. PLAN-RECORD (preserved, 32.1 PLAN history): the SPEC §14 spine was decomposed into 16 bite-sized TDD tasks; ADR-0045 split-gate FINAL re-check at PLAN — 16 ≤ ~25 tasks / ~940-1420 ≤ ~1500 LoC → NO split, the 32.1a/32.1b escape-valve stayed UNCONSUMED; plan-document-reviewer APPROVED (one Task-10 test-code defect fixed in the loop). SPEC-RECORD (preserved): the phase-32.1 SUB-PHASE SPEC INHERITS the parent SPEC (the §5 proto / §6 reject / §7 stat / §8 fixture rosters + the §11 D32-1..D32-8 empirical-pin block; NO new docker probes) and pins the upstream connection-pool / cluster-routing seam's exact exported surface + multiplexing model (D-P32-3 — one-upstream-conn-per-downstream-conn; lazy dial on the first proxied command; a SYNCHRONOUS single-flight pump = depth-1 FIFO/positional pop-front; the seam consumes a DIAL CLOSURE → `internal/filter/network` gains NO `internal/cluster` import; the shared pool + deep queue + two-goroutine pipelining DEFERRED), the RESP partial-frame STREAMING-READER reassembly model (D-P32-4 — the terminal owns the conn, block-read a `bufio.Reader`, NO private high-water buffer, contrasted with the mongo/kafka observer model), the EAGER fixed-roster creation posture + the 32.1 10-name subset (D-P32-1 — 6 downstream counters + 4 gauges; stat surface 536 → 546), the boot-reject fixture arms (D-P32-5 — `0056` carries the `stat_prefix` arm only; the rest unit-armed), the 32.1 local-reply subset PING + AUTH (D-P32-6); the unknown-catch_all-cluster TOLERANCE (vs tcp_proxy's reject), the flat-`/stats` StatsAsserter (the `redis.` Prometheus arm defers to 32.2) + the downstream-RESP byte-equivalence prong (the §9 FIRST). It ANCHORS the ADR-0230 §Context (the upstream-pool seam; DECISIONS tail ADR-0229 → ADR-0230; the §Decision/§Consequences body LANDED at the 32.1 IMPL per ADR-0044). ~16-task spine; ~940-1420 prod LoC → fit the ADR-0045 gate (32.1a/32.1b escape-valve UNCONSUMED). At the 32.1 SPEC commit ROADMAP sub-row 32.1 flipped `planned → in-progress` (parent row 32 STAYED `in-progress`); spec-document-reviewer APPROVED. At THIS 32.1 IMPL commit sub-row 32.1 is now `done`. PARENT-SPEC RECORD (preserved):** the phase-32 `redis_proxy` PARENT SPEC COMPLETE; the D32-1..D32-8 empirical pins EXECUTED live (a real `redis:7` backend on a docker bridge network behind the contrib reference redis_proxy — round-trip CONFIRMED ran) + go-control-plane `/envoy` v1.32.4 + upstream Envoy v1.37.2 source; AMEND-R1..R9 recorded; the ADR-0229 §Context anchored; spec-document-reviewer APPROVED first iteration.** SPEC findings (SPEC §1.1 AMEND block): **D32-1 CONFIRMED** (`@type` carries `extensions.`; redis_proxy is CORE `/envoy v1.32.4` → **ZERO new go.mod dep**, the OPPOSITE of kafka_broker's `/contrib`); **D32-2 RESOLVED + REFUTES** (the per-command roster is **EAGER + table-bounded ~180 commands** — NOT the BRAINSTORM-hypothesized DYNAMIC; the fixed roster = 15 names under `redis.<sp>.` [6 counters + 4 GAUGES `ALL_REDIS_PROXY_STATS` + 2 `splitter.*` + 3 `REDIS_CLUSTER_STATS`]; `upstream_cx_*`/`upstream_rq_*` live under the existing CLUSTER scope `cluster.<name>.*`; the Prometheus arm is **LABEL-HOISTED** `envoy_redis_<leaf>{envoy_redis_prefix="<sp>"}` — the mongo `.rbac.` shape, NOT kafka inline; `enable_command_stats` gates neither presence nor increment in v1.37.2); **D32-3 RESOLVED → ADR-0230** (the seam: route→catch_all→cluster→host; ONE client per host multiplexed; FIFO positional pop-front correlation — RESP has no correlation id; envoy-go MVP may use one-conn-per-downstream); **D32-4 RESOLVED + REFINES** (`stat_prefix` + `settings` + `settings.op_timeout` ALL PGV-REQUIRED — `settings` is MANDATORY, not the BRAINSTORM "minimum subset" — + a RUNTIME `cannot configure a redis-proxy without any upstream` reject; unknown cluster refs tolerated at validate; deferred fields parse-accept); **D32-5 RESOLVED** (RESP framing verbatim; per-command reply bytes captured — the downstream RESPONSE bytes are GENERATED by redis_proxy → the §9-FIRST byte-equivalence prong); **D32-6 RESOLVED** (PING/AUTH [+ECHO/TIME/QUIT/HELLO] answered LOCALLY, zero upstream traffic; count `downstream_rq_total` but no `command.*`); **D32-7 RESOLVED** (per-command segment wire-derived but table-bounded eager → `IsValidName` satisfied by construction if the eager-table posture is adopted [the kafka posture] — the 32.2-SPEC pin); **D32-8 RESOLVED** (NO close-direction counters → close-direction-framework-zero-touch; ~1100-1550 prod LoC → the 2-way split holds). **Preserved (the BRAINSTORM record):** Phase 32 lands `envoy.filters.network.redis_proxy` — the project's **FIRST terminal routing proxy** (unlike every prior §9 row — echo/sni_cluster/zookeeper_proxy/mongo_proxy/kafka_broker, all observational sniffers on a `[filter, tcp_proxy]` chain — redis_proxy IS the connection terminator: parses RESP, routes commands to an upstream cluster, and pools/pipelines upstream connections itself; there is NO passive-observe MVP). The BRAINSTORM (`docs/envoy-go/phases/32-network-filter-redis-proxy/BRAINSTORM.md`, 12 §§ mirroring the mongo-29 parent pre-split template) settled via a multi-question dialogue: **Q0 subject** = `redis_proxy` (`envoy.filters.network.redis_proxy`, proto `envoy.extensions.filters.network.redis_proxy.v3.RedisProxy` — anticipated CORE `/envoy v1.32.4`, NOT `/contrib`; D32-1 confirms) chosen over `thrift_proxy` from {redis, thrift} (RESP far leaner than Thrift's transport×protocol matrix + method-routing + nested sub-filter chain); **Q1 envelope** = a SINGLE-ROUTE-TERMINAL MVP (RESP codec `+`/`-`/`:`/`$`/`*` + inline; `catch_all_route` to ONE upstream cluster; a basic per-host upstream connection pool with FIFO/positional request↔reply correlation — RESP carries no correlation id; the core single-key command set; per-command/downstream/upstream stats under `redis.<stat_prefix>.` via a new `redis.` prom arm + the `IsValidName` charset guard; sharding/redirection/`prefix_routes`-multi-cluster/multi-key-fragmentation/AUTH/faults/mirroring/`read_policy`/`custom_commands`/full-`ConnPoolSettings` DEFERRED, parse-accept-vs-reject per field pinned at SPEC D32-4; histograms deferred ADR-0060); **Q-split** = a 2-way FEATURE-PROGRESSIVE seam-first pre-split (the project's SIXTH BRAINSTORM-time pre-split after 22/25/26/28/29) — parent row 32 + **32.1** (`network-filter-redis-upstream-pool-seam-and-codec` — the upstream connection-pool / cluster-routing FRAMEWORK SEAM + the RESP codec + the redisproxy foundation + the 10th built-in + the `TCPRedisResponder` BackendKind + a minimal catch_all round-trip via `0055-redis-roundtrip`) + **32.2** (`network-filter-redis-commands-and-stats` — the full command set + the stat roster + the `redis.` arm + the differential command matrix + the 41st fuzzer `FuzzRESPDecode` + the parent-row-32 ROLLUP); **Q-pool-seam** = the new seam lands framework-level in `internal/filter/network/` but REDIS-SCOPED (YAGNI; thrift reuses/extends it later), reusing the `cluster.Manager` host-resolution + TCP dial path, built on `TerminalFilter.Handle`. Self-answered per §9 precedent: ONE new framework seam (the upstream connection-pool — the framework's SIXTH structural extension, ADR-0230) + one NEW filter package (`redisproxy`) + ZERO new go.mod deps (anticipated; redis_proxy is core `/envoy`); positional (not id-keyed) reply correlation; the differential proof is downstream-RESP-RESPONSE byte-equivalence PLUS cross-side `StatsAsserter` (the FIRST §9 row whose downstream response bytes are load-bearing — redis_proxy GENERATES them); `upstream_cx_*` pooling stats pinned per-side where the reference diverges; a NEW BackendKind (`TCPRedisResponder`, anticipated value 32); the 41st fuzzer `FuzzRESPDecode`; 2 ADRs anticipated — **ADR-0229** (the filter + parent umbrella) + **ADR-0230** (the upstream-pool seam); next-free at master tip is ADR-0229. ROADMAP parent row 32 registered `in-progress` with sub-phases `32.1, 32.2` + sub-rows 32.1/32.2 `planned` (per ADR-0106(d) — the family flat-row discipline preserves ADR-0045's split-gate WITHIN a single filter-row, the zookeeper-28/mongo-29 in-row split precedent; the §9 heading text UNTOUCHED per ADR-0106(c)) + the §9 candidate-roster paragraph refined (redis IN-PROGRESS/landing; {thrift} remains after phase 32). The static decisions + the empirical pins requiring evidence (D32-1..D32-8) are deferred to the parent SPEC, executed live against the contrib reference Envoy (`envoyproxy/envoy:contrib-v1.37.2`, ADR-0227) per `reference_docker_probe_bridge_network`. Counts UNCHANGED at the BRAINSTORM (fixtures 56 / fuzzers 40 / stat surface 536 / BackendKind tail 31; DECISIONS tail ADR-0228, next-free ADR-0229); the surface/fixture/fuzzer/BackendKind counts + the DECISIONS tail advance at the SPEC/IMPL sub-phases. **Earlier (phase 31 IMPL / phase 31 CLOSED, preserved):** the phase-31 `kafka_broker` IMPL COMPLETE — the FIFTH §9 row; a new `internal/filter/network/kafkabroker/` package (header-only both-direction Kafka sniffer at FULL per-API-key counter parity; the 9th built-in; consumer #3 of the ADR-0221 WriteFilter seam; the project's FIRST `/contrib v1.32.4` consumer); the EAGER 176-counter roster (stat surface 360 → 536); the `kafka.` INLINE prom arm; the `TCPKafkaResponder` BackendKind 31; the `0053`/`0054` fixtures; the 40th fuzzer `FuzzKafkaDecode`; ADR-0228 §Decision/§Consequences bodies landed in-place; six gates GREEN; ROADMAP row 31 `in-progress → done` (flat §9 row). The full prior detail (the 17-task spine; AMEND-K1..K7; D-PLAN-1/2/3; the phase-31 BRAINSTORM/SPEC/PLAN history; phase 30 the contrib pin-refresh; the phase-29 mongo_proxy record) is the durable on-disk record in the phase-31 + phase-30 + phase-29 directories.
- **phase-directory:** `docs/envoy-go/phases/32.2-network-filter-redis-commands-and-stats/` (**SPEC.md** — the durable phase-32.2 sub-phase SPEC landed THIS commit; the PLAN.md follows at the 32.2 PLAN session, the PROGRESS.md at the 32.2 IMPL session [Task 1 creates it], per the 29.1/29.2/29.3 per-sub-phase-dir precedent). The phase-32.1 dir `docs/envoy-go/phases/32.1-network-filter-redis-upstream-pool-seam-and-codec/` (**SPEC.md + PLAN.md + PROGRESS.md** — the durable phase-32.1 IMPL record). The parent `docs/envoy-go/phases/32-network-filter-redis-proxy/` (**BRAINSTORM.md + SPEC.md + README.md** — the durable phase-32 parent-SPEC record). The phase-31 directory `docs/envoy-go/phases/31-network-filter-kafka-broker/` (BRAINSTORM.md + SPEC.md + README.md + PLAN.md + PROGRESS.md — the durable phase-31 IMPL record). The phase-30 + phase-29 directories stay the durable phase-30/29 record.
- **lifecycle-state:** `phase 32.2 IMPL done — SKILL_ROUTING → the next phase (the §9 family stays OPEN; {thrift} remains)` — the phase-32.2 IMPL is COMPLETE (`docs/envoy-go/phases/32.2-network-filter-redis-commands-and-stats/PROGRESS.md`; all 12 tasks landed subagent-driven; six gates GREEN; the 5 as-built deviations recorded). The phase-32 `redis_proxy` parent row is DONE (the parent-row-32 ROLLUP landed ATOMICALLY at this 32.2 IMPL — parent row 32 + sub-row 32.2 `in-progress → done` per ADR-0106(d) / the 18/19/22/24/25/26/28/29 precedent); the SIXTH §9 Network-filters-family row CLOSES. The §9 Network-filters family stays OPEN — **{thrift} remains the SOLE candidate** (the LAST §9 candidate; after it the family closes). The next step is the **phase-33 {thrift} network-filter BRAINSTORM** (`superpowers:brainstorming`) — thrift_proxy is a terminal-proxy surface (transport×protocol matrix + method-routing + a nested thrift-filter sub-chain) that REUSES the 32.1 upstream-pool seam (ADR-0230); like kafka_broker it is a `/contrib` extension (the contrib go-control-plane dep + the contrib reference image are already in place from phases 30/31). NO new ADR was consumed at the 32.2 IMPL (DECISIONS tail STAYS ADR-0230; ADR-0229 now ACCEPTED — its 32.2 half landed in-place; next-free ADR-0231). **Earlier (32.2 PLAN, preserved):** plan-document-reviewer APPROVED iteration 2 (the Task 3 Step 5 `resp_test.go` caller blocking issue fixed in the loop); the SPEC §14 spine decomposed into 12 bite-sized TDD tasks; ADR-0045 split-gate FINAL re-check NO split (the 32.2a/32.2b escape-valve UNCONSUMED). **Earlier (32.2 SPEC findings, preserved):** resolved D-P32-2 (the EXACT 180-command table, the success/error single-server semantics), D-P32-6 (full local-reply extent; TIME non-deterministic; HELLO split), D-P32-7 (IsValidName by construction), D-P32-8 (gauge design — 2 lifecycle gauges filter-driven, 2 buffered gauges framework-managed-upstream coverage boundary), D-P32-9 (one-conn-per-downstream per-side asymmetry). **Earlier (32.1 IMPL, DONE):** the SPEC §14 16-task spine executed end-to-end — Task 1 the baselines/anchors gate → the `redisproxy` skeleton+TypeURL → `config.go` parse → byte-stable rejects → `resp.go` request/reply decode+encode → `upstreampool.go` seam → `commands.go` PING/AUTH → `stats.go` 10-name roster → `filter.go` Handle pump → 10th built-in+bootstrap → `TCPRedisResponder` → `0055`/`0056` fixtures → the NEW `GET /stats` flat admin endpoint (the eighth admin endpoint, a Task-14 addition required for the `StatsAsserter`) → the completion bundle. The 32.1 IMPL filled the ADR-0230 §Decision/§Consequences body + the ADR-0229 §Decision/§Consequences (32.1 half, status PARTIAL) in place per ADR-0044; advanced counts (fixtures 56→**58**, fuzzers **40** unchanged, stat surface 536→**546** [+10: 6 downstream counters + 4 gauges], BackendKind 31→**32**; R9). Phases 29 + 30 + 31 are CLOSED. ROADMAP: parent row 32 `in-progress`; sub-row 32.1 `done` (flipped `in-progress → done` at THIS 32.1 IMPL commit per the 26.x/28.x/29.x precedent); sub-row 32.2 `planned`; rows 26/27/28/29/30/31 `done`. The §9 Network-filters family stays OPEN (1 candidate {thrift} remains after phase 32).
- **next-skill:** **`superpowers:brainstorming`** — scoped to the **phase-33 {thrift} network-filter** (`thrift_proxy`, `envoy.filters.network.thrift_proxy`), the LAST §9 Network-filters-family candidate (after it the family closes). Thrift is a terminal-proxy surface (a transport×protocol matrix — `framed`/`unframed`/`header` transports × `binary`/`compact`/`twitter` protocols — + method-routing + a nested thrift-filter sub-chain) that REUSES the 32.1 upstream-pool seam (ADR-0230 — the SIXTH structural extension, redis-scoped but generalizable; thrift is the anticipated second consumer). Like kafka_broker it is a `/contrib` extension → it consumes the existing `github.com/envoyproxy/go-control-plane/contrib` v1.32.4 dep (phase-31) + the `envoyproxy/envoy:contrib-v1.37.2` reference image (phase-30, ADR-0227) — both already in place, ZERO new infra. The BRAINSTORM settles Q0 (subject confirm = thrift_proxy) / Q1 (the envelope — transport+protocol scope, method-routing scope, the per-method/global stat roster, deferred features) / Q-split (single row vs a pre-split, per the redis-32 / kafka-31 precedent) / Q-seam (reuse-vs-extend the upstream-pool seam). Honor `feedback_execution_style` (the downstream IMPL runs subagent-driven) + `feedback_git_worktrees` (work in a worktree) + `feedback_push_to_origin` / `feedback_subagents_no_push` (the controller pushes at stage-close; subagents commit local-only). Read first: `docs/envoy-go/ROADMAP.md` §9 Network-filters family (parent rows 26-32 DONE; {thrift} the sole open candidate) + `docs/envoy-go/STATE.md` (this file) + `docs/envoy-go/DECISIONS.md` ADR-0230 (the upstream-pool seam — ACCEPTED; thrift's anticipated second consumer) + ADR-0229 (the redis_proxy as-built — the terminal-routing-proxy precedent) + ADR-0228 (kafka_broker — the `/contrib` precedent) + ADR-0227 (the contrib pin) + ADR-0106(d)/0045/0044/0052/0060 + the phase-32 as-built (`docs/envoy-go/phases/32-network-filter-redis-proxy/` parent SPEC + `32.1`/`32.2` SPEC/PLAN/PROGRESS — the seam + codec + command-set + roster + the `redis.` prom arm + the `0055`/`0056` fixtures + `FuzzRESPDecode`) + the phase-31 kafka_broker record (`docs/envoy-go/phases/31-network-filter-kafka-broker/` — the `/contrib` dep + the contrib reference image) + `internal/filter/network/upstreampool.go` (the seam thrift reuses) + `internal/filter/network/redisproxy/` (the terminal-proxy package shape) + `internal/stats/name.go` (the kafka INLINE / mongo MULTI-label / redis SINGLE-label-hoist prom-arm contrast — thrift picks one) + project memories (`reference_wire_format_both_sides_see_same_bytes`, `reference_differential_reference_parses_full_message`, `reference_close_direction_framework_gap`, `reference_dynamic_stat_name_charset_guard`, `reference_network_filter_typeurl_extensions`, `reference_envoy_contrib_image_tagging`, `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_subagents_no_push`, `feedback_pertask_gofmt_lint`).
- **last-commit:** the phase-32.2 IMPL squash (`phase 32.2 IMPL: network-filter-redis-commands-and-stats — the full command set + the 180-command roster + the 540 per-command/2 splitter/3 cluster counters + the 2 lifecycle gauges' inc/dec + the redis. SINGLE-label prom arm + FuzzRESPDecode (41st) + the 0055 command matrix + the parent-row-32 ATOMIC ROLLUP; 12-task PLAN; six-gate GREEN`) — squash-merged from `phase-32.2-network-filter-redis-commands-and-stats` + pushed to origin/master by the controller at stage-close per `feedback_push_to_origin`; the IMPL worktree removed at stage-close. Confirm the live tip via `git log --oneline -1`. Substantive predecessor on master: the phase-32.2 PLAN squash `dd340ff` (the docs-only SHA-fill `9eba8f6` trailed it); the phase-32.2 SPEC squash `8da7377` (the docs-only SHA-fill `e270db7` trailed it); the phase-32.1 IMPL squash `fa58bc9` (`phase 32.1 IMPL: network-filter-redis-upstream-pool-seam-and-codec — the redis_proxy TERMINAL routing proxy on the ADR-0230 upstream-pool seam; 16-task PLAN; six-gate GREEN`; the docs-only SHA-fill `823c5aa` trailed it); earlier the phase-32.1 PLAN squash `a28f593`; the phase-32.1 SPEC squash `b7f01a4`; the phase-32 PARENT SPEC squash `0f7888d`; the phase-32 BRAINSTORM squash `f473c4e`; the phase-31 IMPL squash `928287a`. Earlier substantive predecessors on master: the phase-32.2 SPEC squash `8da7377` (the docs-only SHA-fill `e270db7` trailed it); the phase-32.1 IMPL squash `fa58bc9` (`phase 32.1 IMPL: network-filter-redis-upstream-pool-seam-and-codec — the redis_proxy TERMINAL routing proxy on the ADR-0230 upstream-pool seam; 16-task PLAN; six-gate GREEN`; the docs-only SHA-fill `823c5aa` trailed it); earlier the phase-32.1 PLAN squash `a28f593`; the phase-32.1 SPEC squash `b7f01a4`; the phase-32 PARENT SPEC squash `0f7888d`; the phase-32 BRAINSTORM squash `f473c4e`; the phase-31 IMPL squash `928287a`. Earlier substantive predecessors on master: the phase-31 PLAN squash `12f7cac`; the phase-31 SPEC squash `6104036`; the phase-31 BRAINSTORM squash `78d5f36`; the phase-30 IMPL squash `2d24f34` (phase 30 CLOSED).
- **last-updated:** 2026-06-09
- **next-free ADR:** `ADR-0231` (DECISIONS.md tail STAYS **ADR-0230** — the upstream connection-pool / cluster-routing framework seam, ACCEPTED, §Decision/§Consequences body landed at the 32.1 IMPL). NO new ADR was consumed at the 32.2 IMPL — ADR-0229 (the redis_proxy filter + parent-row-32 umbrella) is now **ACCEPTED**: its 32.2 half (the full command set + the per-command/splitter/cluster roster + the `redis.` prom arm + the 2 lifecycle gauges' inc/dec + the differential command matrix + the 41st fuzzer + the parent-row-32 ROLLUP) completed IN-PLACE at the 32.2 IMPL (PARTIAL → ACCEPTED per ADR-0044) with NO new number. So next-free is **ADR-0231** (the phase-33 {thrift} BRAINSTORM/SPEC anticipates its first new ADR there). The ADR-0209 escape-valve reserve STANDS-UNCONSUMED.

---

## Project counts at this commit

- **active differential fixtures:** **58** (tail `0056-redis-boot-reject`). Canonical recipe `ls -d test/fixtures/[0-9]* | wc -l` = 58. UNCHANGED at the phase-32.2 IMPL (`0055-redis-roundtrip` EXTENDED in place with the command matrix + the held-open gauge arm — NO new dir). Advanced at the phase-32.1 IMPL (+2: `0055` + `0056`).
- **fuzzers:** **41** (canonical recipe `grep -rh "^func Fuzz" $(find ./internal -name fuzz_test.go) | wc -l`; tail `FuzzRESPDecode`). Advanced at the phase-32.2 IMPL (+1: `FuzzRESPDecode` — no-panic / no-mutation / bounded-allocation over `decodeRequest` + `decodeReply`).
- **stat surface:** **1091** (BEHAVIOR_CONTRACT doc count). Advanced at the phase-32.2 IMPL (+545: 2 `splitter.*` + 3 REDIS_CLUSTER_STATS + 180×3=540 per-command `command.<cmd>.{total,success,error}`; the 180 `command.<cmd>.latency` histograms + the `*_fault` counters DEFERRED per ADR-0060/faults, NOT counted). Was 546 at the phase-32.1 IMPL close (+10 fixed — 6 counters + 4 gauges).
- **BackendKind tail:** **32** (`TCPRedisResponder`). UNCHANGED at the phase-32.2 IMPL (reply-table extended — `$-1` GET-miss, `:1` INCR/DEL — NOT re-numbered). Advanced at the phase-32.1 IMPL.
- **DECISIONS.md tail:** **ADR-0230** (next-free **ADR-0231**) — the upstream connection-pool / cluster-routing framework seam; **§Decision/§Consequences body LANDED at the 32.1 IMPL** (status ACCEPTED). ADR-0229 (the redis_proxy filter + parent-row-32 umbrella) is now **ACCEPTED** — its §Decision/§Consequences 32.2 half completed IN-PLACE at the 32.2 IMPL (PARTIAL → ACCEPTED per ADR-0044); NO new ADR number consumed (tail STAYS ADR-0230; next-free ADR-0231 unchanged). Was ADR-0228 at the phase-31 IMPL close.
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227, superseding ADR-0008).
- **conformance:** h2spec 53/53; proxy-wasm all 10 families PASS (image-independent; UNAFFECTED by phase 32 — touches no HTTP/h2/proxy-wasm path).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
