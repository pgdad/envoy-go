# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `05-http-2`
- **phase-directory:** `docs/envoy-go/phases/05-http-2/` — does **not** yet exist (per `BOOTSTRAP_PROMPT.md` §4.1 invariant 3, a phase directory is created only at the moment a phase enters `in-progress`; the next session's first file-system act is `mkdir docs/envoy-go/phases/05-http-2/` and that act is also when ROADMAP row 05 flips from `planned` to `in-progress`). Phase 04's directory remains in place at `docs/envoy-go/phases/04-http-1.1/` with `SPEC.md`, `PLAN.md`, `PROGRESS.md` (18 task entries plus TWO verification blocks: `7649a19` for the pre-follow-ups state-4 sweep at HEAD `13d0373`, and `c315cb9` for the post-follow-ups state-4 sweep at HEAD `b24578a`), and `REVIEW.md` (`04527eb` — verdict APPROVED WITH FOLLOW-UPS, all four Important items I-1..I-4 plus the cleanest Minor M-1 landed in `671a059`, post-fix verification block in `c315cb9`); it is now closed and read-only history.
- **lifecycle-state:** `1` — phase 05 is in ROADMAP (`planned`, `depends-on: 04`) but its directory does not yet exist. Per `BOOTSTRAP_PROMPT.md` §5 state 1, the next session creates the phase directory, invokes `superpowers:brainstorming` scoped to phase 05, and produces `SPEC.md`. Phase 04 closed at lifecycle-state 6 in this transition: ROADMAP row 04 status flipped `in-progress` → `done` (this commit), `REVIEW.md` (`04527eb`) was APPROVED WITH FOLLOW-UPS with all four Importants plus M-1 landed in `671a059` and re-verified in `c315cb9`, and the §5.3 phase-done commit lands eight ADRs (ADR-0037..ADR-0044). The 5 remaining Minor REVIEW findings (M-2, M-4, M-5, M-6, M-7) are deferred to phase 05+ triage in the same form phase-03's REVIEW Minors carried into phase 04 — see `next-skill-scope` for the candidate hygiene list. M-3 (`closeAfterAction := false` dead branch) is naturally resolved by the I-1 fix in `671a059` — `routerAction.do` now returns `errCloseAfterAction` when the upstream signals close, so the variable is no longer dead.
- **next-skill:** `superpowers:brainstorming`
- **next-skill-scope:** Brainstorm phase 05 — `HTTP/2 downstream + upstream (low-level framer, own conn mgr)`. Per ROADMAP, the differential surface at phase 05 completion is a new HTTP/2 fixture green plus all pre-existing fixtures (`0000-tcp-echo`, `0001-tcp-proxy-rr`, `0002-tls-tcp`, `0003-http11-routing`) regression-clean, AND `h2spec` conformance above the declared threshold (the first phase to ship a conformance suite per BOOTSTRAP §7.3 / §8). Per BOOTSTRAP D-3.2, phase 05 may use `golang.org/x/net/http2` as a *low-level codec only* — never as a server runtime; the HTTP/2 connection manager and own-conn-mgr framing must be written from scratch on top of that codec. First file-system acts of the next session: (1) `mkdir docs/envoy-go/phases/05-http-2/`; (2) flip ROADMAP row 05 `planned` → `in-progress`; (3) invoke `superpowers:brainstorming` scoped to phase 05 to produce `phases/05-http-2/SPEC.md`. The brainstorming session should triage these phase-04 carry-forward Minor findings from `REVIEW.md` (`04527eb`) into the SPEC, absorb them into a doctrine sweep, or re-defer them explicitly: M-2 ADR-0043's `Doctrine: D-3.4, D-3.5` mismatched against the informal supersession qualifier (mechanical inconsistency with ADR-0034); M-4 listener-manager `Stop()`/`Listeners()` race (carry-forward from phase 03 M-2; phase 04 did not touch the lock surface); M-5 SPEC §7 failure-mode "close upstream" prose vs the `defer upstream.Close()` mechanism (cosmetic doc/code drift); M-6 fixture-0003 driver's heredoc YAML pattern (carry-forward of the phase-08 / observability-phase structured `expectations.yaml` plan per ADR-0019); M-7 `Filter.statPrefix` stored but never consumed in phase 04 (forward-look for phase-06 stats per ADR-0041 — phase-06 should hook the consumer). ALSO carry forward as candidates: REVIEW.md's "single most important context to surface to the phase-05 planner" — `fixture 0003 still does not differentially exercise upstream TLS` (per phase-03 ADR-0035; phase 04 explicitly did not try). Phase 05 (HTTP/2 — almost always over TLS) is the natural place to close that loop, either by extending `test/differential/harness.go` to spawn TLS-capable HTTP/2 backends or by introducing a naturally-HTTPS HTTP/2 fixture whose upstream dial validates `Cluster.Dial`'s TLS branch cross-proxy. Work from a fresh impl worktree per the project's per-phase-worktree convention (`phase/05-http-2-spec`, `phase/05-http-2-plan`, `phase/05-http-2-impl`); the existing `.worktrees/phase-04-http-1.1-impl` is now closed-history. Master and `phase/04-http-1.1-impl` share HEAD again at this commit per ADR-0003.
- **last-commit:** 1542102 (the lifecycle-state-5 STATE.md update on `phase/04-http-1.1-impl` that closed phase 04 verification post-REVIEW-follow-ups; the §5.3 phase-done commit lands directly on top of it and fast-forwards `master` to its tip per ADR-0003).
- **last-updated:** 2026-04-25

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
