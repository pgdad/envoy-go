# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `06.2-access-log`
- **phase-directory:** `docs/envoy-go/phases/06.2-access-log/` — exists. This commit deletes the placeholder `README.md` (`ad46930`) and writes the authoritative `SPEC.md` (lifecycle-state-1 → 2 deliverable). The placeholder's "this README is illustrative; the SPEC supersedes it" final paragraph executes at this commit. Phase 06.1 remains closed read-only history; the parent `docs/envoy-go/phases/06-observability-baseline/` retains its `BRAINSTORM.md` + parent `SPEC.md` as the master design document (still open until 06.2 phase-done flips parent row 06). ROADMAP row 06.2 is `in-progress` after this commit; row 06 stays `in-progress`; row 06.1 stays `done`.
- **lifecycle-state:** `2` — SPEC.md committed, PLAN.md does not exist. The brainstorm session held against master `9acfc0b` (06.1 phase-done SHA-fill) settled the access-log decisions A–M (per SPEC §1 + §11 + §12) — option B partial-with-`-` field coverage; drop-newest async-writer with 4096-cap channel and `server.accesslog_dropped` counter; option β `log_format` rejection at parse; 3-tier per-field equivalence matrix (8 Tier-E + 3 Tier-F + 4 Tier-S = 15 operators); `internal/accesslog/` package shape (Sink interface, Default formatter, AsyncFileSink, stats wiring); HCM emit hooks at the four request-finalization sites (`directResponseAction.do`, `routerAction.do`, `h2DirectResponseAdapter.WriteH2`, `routerActionH2.doH2`); fixture `0006-access-log` with 5 sequential GETs per side, polling-loop drain (25ms intervals, 5s deadline) closing 06.1 REVIEW M-8 prophylactically; FuzzAccessLogFormat as the eighth fuzzer at the 30s ADR-0018 budget; ADR-0066 (architecture), ADR-0067 (β rejection at parse), ADR-0068 (3-tier equivalence), ADR-0069 (counter naming) anticipated. Per BOOTSTRAP §5 step 2 → step 3, the next session is PLAN.md authoring; lifecycle-state will advance to 3 at the PLAN commit.
- **next-skill:** `superpowers:writing-plans` — phase 06.2 PLAN.md authoring. The PLAN distills SPEC §4 (Deliverables) and §5 (Architecture) into an ordered, numbered task list, each task with: precondition / artifact / acceptance / verification command. Per ADR-0005 (autonomous-plan-review precedent), the PLAN session runs autonomously and a plan-document-reviewer subagent loop runs before commit. Inputs the planner should consider: (a) the SPEC §4 inventory of new + changed files; (b) the SPEC §11 empirical default-format pin block (PLAN reserves a task for the empirical scrape against the v1.37.2 reference image, per Decision H); (c) the SPEC §6 15-operator catalog (the Default formatter implementation task pins format details to this catalog); (d) the SPEC §7 fixture 0006 task split (envoy.yaml + envoy-go.yaml + driver/driver.go + backends/main.go + expectations.yaml + README.md); (e) the SPEC §10 carry-forward dispositions (M-8 polling-loop pattern adopted prophylactically; the 12-Minor 06.1 batch is NOT 06.2's responsibility); (f) the SPEC §15 acceptance checklist as the PLAN's terminal-task verification anchor. The §6.2 split threshold (~25 tasks OR ~1500 LoC) is unlikely but plausible; if PLAN reveals creep, the natural cut is 06.2.1 (`accesslog` package + bootstrap parse) and 06.2.2 (HCM emit + fixture 0006) per SPEC §1.
- **next-skill-scope:** Phase 06.2 PLAN.md authoring session (lifecycle-state 2 → 3). Per ADR-0005 + BOOTSTRAP §5 state 2: drafting PLAN.md per the SPEC contract; plan-document-reviewer subagent loop; commit with subject `phase 06.2: PLAN drafted (autonomous plan-review per ADR-0005)`; SHA-fill follow-up. Worktree for the next session: branch from this commit's HEAD per ADR-0003 + per-phase-worktree convention (`.worktrees/phase-06.2-access-log-plan`).
- **last-commit:** `TBD` — `phase 06.2: SPEC drafted (autonomous brainstorming per ADR-0004)`, on branch `phase/06.2-access-log-brainstorm`. Writes `docs/envoy-go/phases/06.2-access-log/SPEC.md` (751 lines, 16 top-level sections, encoding decisions A–M from the brainstorm session). Deletes `docs/envoy-go/phases/06.2-access-log/README.md` (the placeholder; superseded per its own final-paragraph contract). Flips ROADMAP row 06.2 status `planned → in-progress`. Updates STATE.md `lifecycle-state` `6 → 2` with `active-phase` unchanged and `next-skill` set to `superpowers:writing-plans` per BOOTSTRAP §5 state 2 transition. Anticipates four ADRs (0066–0069) per SPEC §8; ADR landing happens at PLAN-execution time per the 06.1 anchored-ADR convention. SHA-fill follow-up per phase-02..06.1 convention.
- **last-updated:** 2026-04-29

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
