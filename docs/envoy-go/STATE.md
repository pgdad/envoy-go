# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `25.1 (in-progress) — SPEC done at 2026-05-24 (this commit); awaiting 25.1 PLAN` — **phase 25.1 SPEC DONE at 2026-05-24 (this commit)** authoring `docs/envoy-go/phases/25.1-http-filter-wasm-runtime-and-headers-bridge/SPEC.md` for the first sub-phase of the phase-25 BRAINSTORM-time 3-way pre-split. Per the phase-22.1 SPEC precedent + the cold-start prompt scope, the 25.1 SPEC author skipped a sub-phase BRAINSTORM (parent BRAINSTORM + parent SPEC settled enough decisions — Q1-Q9 locked + AMEND-A1..A9 anchored + parent §13 R1-R8 RATIFIED-PENDING-IMPL items scoped + parent §15 24-item acceptance checklist drafted) and proceeded DIRECTLY to SPEC authoring. The 25.1 SPEC details the per-Task-level surface: (a) `internal/wasm/` API signature refinements vs parent §4.1 sketch — function-option `VMOption` pattern + State() / HasGlobalFunc() / per-callback method split + WithPanicHandler + WithLogSink naming clarifications; (b) `internal/wasm/` production file split — 8 production files in package root (vm.go + sandbox.go + compile.go + registration.go + bytecode_util.go + pairs.go + wasi.go + doc.go) + 1 subdirectory `abi/` with 1 production file (types.go) + 8 test files; (c) 24-hostcall implementation per parent §4.2 + WASI shim custom-8-stub implementation per R4; (d) default-deny capability roster materialization at sandbox.go per parent §4.3 + SanitizationConfig accept-empty discipline; (e) `internal/filter/http/wasm/` package file split (8 production + 5 test files) + boot-registration alphabetical position (between `router` and `httpReg.Freeze()` — `wasm` sorts after `router` at master tip 19-entry roster); (f) byte-stable PARSE-REJECT wording finalization at IMPL Task 9 per D-P5 + `TestParseRejectConstants_ByteStable` table-driven test; (g) fixture-0034 7-scenario authoring discipline per parent §8.1 + §4.5 D6 guardrails + NEW `BackendKind=HTTPWasm` constant + vendored Rust-sourced `.wasm` bytecode + `StatsAsserter.AssertStats` discipline for scenario (e) per `reference_differential_asserter_dispatch`; (h) fixture-0035 single-arm boot-reject parity per D-P6 (anticipated arm 5 `vm-config-code-required`); (i) 6 SPEC-time D-questions (D-P1 WASI denial errno → Task 2; D-P2 module-init callback gating → Task 6; D-P3 ADR-0196 first co-consumer → Task 11 + R7; D-P4 wazero-VM-pool benchmark → Task 17 + R8 escape-valve gate; D-P5 PARSE-REJECT wording → Task 9; D-P6 boot-reject arm selection → Task 16). Plus D-S1 SUB-PIN closed at SPEC §11.1: **34-fuzzer count VERIFIED at this SPEC commit** via project-wide `grep -rh "^func Fuzz"` against master tip (33 confirmed; `FuzzWasmConfigParse` is 34th); R5 from parent §13.2 CLOSED. Task envelope: **17 tasks** (within parent §3.0's 17-19 estimate); anticipated LIVE LoC ~4,200-4,800 (within parent's ~3,650-5,360 envelope); ADR-0045 task-arm split-gate fits cleanly. **NO NEW ADRs consumed at this 25.1 SPEC commit** — DECISIONS.md tail STAYS at ADR-0204; ADR-0202+0203+0204 §Decision+§Consequences bodies land at 25.1 IMPL Task 17 atomic landing per ADR-0044; ADR-0205 STAYS as escape-valve reserve slot (CONDITIONAL consumption only if R8 wazero-VM-pool benchmark gate fires — anticipated UNCONSUMED per parent §1.2 hypothesis + phase-22.1 70µs analogous-benchmark precedent). Phases 23 + 24 + 24.1 + 24.2 + 25-parent DONE and CANONICAL — DO NOT reopen.
- **phase-directory:** `docs/envoy-go/phases/25.1-http-filter-wasm-runtime-and-headers-bridge/` (README.md pre-created at parent BRAINSTORM commit + **SPEC.md landed at this commit**; PLAN.md NOT yet authored — lands at the 25.1 PLAN session). Parent directory `docs/envoy-go/phases/25-http-filter-wasm/` (BRAINSTORM.md + SPEC.md DONE at predecessor commits). Sibling sub-phase directories `25.2-…/` + `25.3-…/` carry pre-created README.md forward-pointers.
- **lifecycle-state:** `phase 25.1 SPEC done (lifecycle-state 1 → 2 for sub-row 25.1); awaiting 25.1 PLAN authoring` (SKILL_ROUTING state 2 for sub-phase 25.1 — 25.1/SPEC.md exists; 25.1/PLAN.md does NOT yet exist). Per ADR-0106 + phase-22.1/22.2/22.3 precedent, the 25.1 PLAN session is the next lifecycle-state-2 → state-3 transition for sub-row 25.1. ROADMAP row 25 STAYS `in-progress`; sub-row 25.1 flips `planned → in-progress` at this 25.1 SPEC commit (per ADR-0106 per-cell SPEC-done annotation + the phase-22.1 SPEC commit precedent which similarly flipped row 22.1 to `in-progress` at SPEC-done).
- **next-skill:** `superpowers:writing-plans` (scoped to **25.1 PLAN authoring** at `docs/envoy-go/phases/25.1-http-filter-wasm-runtime-and-headers-bridge/PLAN.md` per the phase-22.1 + phase-22.2 + phase-22.3 + phase-23 + phase-24.1 + phase-24.2 per-sub-phase PLAN precedent — the 25.1 PLAN session translates the 25.1 SPEC §6 17-task structure into the bite-sized TDD task breakdown per the writing-plans skill discipline + applies the ADR-0045 §6 split-gate re-evaluation (task-arm fit STANDS at 17 tasks; LoC-arm above 1500 threshold but acceptable per EXTRACT-NOW-primitive-bring-up precedent from phase-22.1). After 25.1 PLAN lands, IMPL via `superpowers:subagent-driven-development` per `feedback_execution_style` project memory.
- **last-commit:** `TBD-25.1-SPEC` — `Squash merge phase-25.1-http-filter-wasm-runtime-and-headers-bridge-spec`. The phase-25.1 SPEC squash-merge commit landing 25.1/SPEC.md + this STATE.md update + ROADMAP per-cell SPEC-done annotation. SHA filled in at the stage-close follow-up commit per the phase-22.1 + phase-22.2 + phase-22.3 + phase-23 + phase-24.1 + phase-24.2 SPEC stage-close SHA-fill precedent. Predecessor master tip (pre-25.1-SPEC): `7f7a1e7` — `next-prompt.txt: repoint master-tip references to b91bd64 (actual HEAD)` (with predecessors `b91bd64` + `6a72d30` + `a1e4db6` parent-SPEC SHA-fill + cold-start rewrite for 25.1 SPEC + `2c1455d` parent-SPEC squash-merge).
- **last-updated:** 2026-05-24
- **next-free ADR:** `ADR-0205` (UNCHANGED at this 25.1 SPEC commit — NO NEW ADRs consumed; ADR-0202+0203+0204 §Decision+§Consequences bodies land at 25.1 IMPL Task 17 Lands-in-Task per ADR-0044 + parent §10.1 + this 25.1 SPEC Appendix B). DECISIONS.md tail STAYS at `ADR-0204`. **D-hypothesis disposition (phase-25 parent SPEC, INHERITED at this 25.1 SPEC commit)**: STRENGTHENED-WEAK-HOLD-with-1-slot-buffer — ADR-0205 reserve slot covers (a) wazero-VM-pool design escape-valve if 25.1 IMPL `BenchmarkPerStreamVM_Construction_Headers` exceeds 1ms threshold per parent SPEC D-P4 + R8 (LOW probability — phase-22.1 observed 70µs at the analogous benchmark) OR (b) any wazero/V8 sub-pin edge case the §4.5 D6 guardrails missed. WEAK-HOLD stands at this 25.1 SPEC commit. Anticipated next-free after 25.3 phase-done: `ADR-0213` (matching parent SPEC §1.2 + BRAINSTORM §7.4 estimate of ADR-0202..ADR-0213 across the family).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
