# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `17-http-filter-jwt-authn` — phase 17 IMPL session COMPLETE (state-4 entry per ADR-0005 §Decision 4). Phase 17 is the TENTH §9 HTTP-filters family-row (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11, csrf @ 12, buffer @ 13, compressor @ 14, bandwidth_limit @ 15, rbac @ 16). The 15-task phase-17 PLAN executed in full: new `internal/filter/http/jwtauthn/` package (`jwtauthn.go` 845 + `evaluator.go` 662 + `provider.go` 707 + `doc.go` 185 = 2399 LoC production) + TWO new cross-phase-reusable framework primitives — `internal/jwks/` (`jwks.go` 626 + `doc.go` 77 = 703 LoC; HTTP-outbound JWKS fetcher with thread-safe cache + 5s-before-TTL background refresh + fixed-1s failed-refetch) per ADR-0150, `internal/jwt/` (`jwt.go` 555 + `doc.go` 98 = 653 LoC; pure-Go-stdlib JWS/JWT parser + RS+ES signature verifier + claim validator) per ADR-0151 — + new `test/helpers/jwksbackend/` test-helper (127 LoC; in-process HTTP JWKS server). 8 ADRs anchored at impl-time per the Lands-in-Task table (ADR-0148 + ADR-0149 @ Task 2; ADR-0150 @ Task 3; ADR-0151 @ Task 4; ADR-0152 @ Task 5; ADR-0153 @ Task 7; ADR-0154 @ Task 8; ADR-0155 @ Task 9 — §Decision + §Consequences bodies authored at impl-time; §Context drafts landed at SPEC commit) + ADR-0125 §(xiii) amendment paragraph landed at SPEC commit (8th canonical per-route: string-reference-delegation). **ADR-0044 escape-valve NOT triggered** — 0 impl-time-unanticipated ADRs (the PLAN-anticipated TLS-trust-store / clear_route_cache / sync-primitive surfaces did not materialize; ADR roster ends at ADR-0155). §11.P7 RATIFIED-PENDING-IMPL-TIME-EMPIRICAL-SCRAPE closure (deferred from Task 8 to Task 13 per ADR-0154 §Decision (vi)) CLOSED RATIFIED — the Task-13 fixture-0019 scrape confirmed SN2-reuse verbatim (both reference Envoy v1.37.2 and envoy-go emit `envoy_http_jwt_authn_<counter>{envoy_http_conn_manager_prefix="..."}` identically; NO new SN10 rule). Differential fixture `0019-http-jwt-authn` (8 scenarios) + 21st fuzzer `FuzzJwtAuthnConfigParse` landed green. Two Task-13-discovered counter divergence-windows documented (`allowed` increments on the CORS-bypass + per-route-disabled passthrough paths on reference Envoy but not envoy-go MVP per SPEC §3 + §1.1 amendment 5; `jwt_cache_*` structurally unreachable under MVP per §8 deferral 8). BEHAVIOR_CONTRACT.md 6-edit bundle landed at the phase-done commit (§13.1 jwt_authn subsection — landing-chronological insertion per planner-time decision 14 fallback since BEHAVIOR_CONTRACT uses landing-chronological ordering; §13.2 stat-table 64→71; §13.3 fixture 0019 equivalence-matrix row; §13.4 Phase 17 forward-pointer notes; §13.7 NEW `## JWKS framework primitive` top-level section; §13.8 NEW `## JWT verifier framework primitive` top-level section). Phase-done six-gate verification (Gates A/B/C/D/E/F per BOOTSTRAP_PROMPT.md §7.5) passed at Task 14. REVIEW.md authored at Task 15. ROADMAP row 17 status `done` (2026-05-14).
- **phase-directory:** `docs/envoy-go/phases/17-http-filter-jwt-authn/` — BRAINSTORM.md (544 LoC) + SPEC.md (2395 LoC) + PLAN.md (730 LoC) + PROGRESS.md (15 task entries) + REVIEW.md all landed.
- **lifecycle-state:** `phase 17 done; awaiting next planning` — state-4 entry per ADR-0005 §Decision 4. The phase-17 IMPL session executed the 15-task PLAN per `superpowers:subagent-driven-development` / `superpowers:executing-plans` discipline (per-task TDD per `superpowers:test-driven-development`), passed the phase-done six-gate verification, and authored REVIEW.md. The next session is a BRAINSTORM session for the next §9 HTTP-filters family-row (ext_authz / ext_proc / oauth2 / lua / wasm / adaptive_concurrency / admission_control / global_ratelimit follow the same row-as-its-own-phase pattern per ADR-0106) per `superpowers:brainstorming` cadence — ROADMAP currently has no row 18, so the next planning session opens with a fresh §9 family-row selection.
- **next-skill:** `(none — phase complete)` — phase 17 is done. The next session is a BRAINSTORM session for the next §9 family-row per `superpowers:brainstorming`; the cold-start prompt for that session selects the next §9 filter from the ext_authz / ext_proc / oauth2 / lua / wasm / adaptive_concurrency / admission_control / global_ratelimit roster.
- **next-skill-scope:** Cold-start scope for the next BRAINSTORM session. Read first: STATE.md (this file); `docs/envoy-go/ROADMAP.md` (row 17 `done`; no row 18 yet — the BRAINSTORM session adds the next §9 family-row); `docs/envoy-go/MISSION.md` + `docs/envoy-go/DECISIONS.md` (tail at ADR-0155; next-free ADR-0156) + `docs/envoy-go/BEHAVIOR_CONTRACT.md` (10 HTTP filters documented through phase 17; HCM stat-table at 71 names; TWO framework-primitive sections — `## JWKS framework primitive` + `## JWT verifier framework primitive` — added at phase 17) + `docs/envoy-go/SKILL_ROUTING.md`; `docs/envoy-go/phases/17-http-filter-jwt-authn/REVIEW.md` (phase-17 end-of-phase review — deliverables roster + ADR dispositions + §15 acceptance-checklist verification + divergence-window enumeration). The phase-17 framework deltas available to future filters: `internal/jwks/Fetcher` (compose for ext_authz HTTP-mode + oauth2 token-endpoint flows) + `internal/jwt` (`Parse` + `Token.VerifySignature` + `Token.ValidateClaims` + `Token.PayloadClaim` — compose for jwt_claim_router + oauth2 token validation).
- **last-commit:** `3cee1d3` — `Squash merge phase-17-http-filter-jwt-authn-impl`. The phase-17 IMPL worktree `phase-17-http-filter-jwt-authn-impl` (branched off master tip `d225bb7`) carried 21 commits (15 task-landing commits Tasks 1–15 + 6 in-task follow-ups: Task 2 / 4 / 5 / 6 / 7 / 8 follow-ups); squash-merged to master as one IMPL commit `3cee1d3` (31 files, +14367/-39). This `phase 17 impl follow-up` commit is the SHA-fill follow-up (TBD-impl-squash-merge → `3cee1d3` post-squash) per the phase-09..16 impl-stage close pattern.
- **last-updated:** 2026-05-14
- **next-free ADR:** `ADR-0156` — phase-17 anchored ADR-0148..ADR-0155 §Context drafts at SPEC commit `9070490`; §Decision + §Consequences bodies authored at IMPL commits per ADR-0044 ADR-on-impl convention. ADR-0125 §(xiii) amendment paragraph landed in full at SPEC commit. **ADR-0044 escape-valve NOT triggered** — 0 impl-time-unanticipated ADRs in phase 17 (DIVERGES from the phase-13 ADR-0127-v2 + phase-14 ADR-0134 + phase-16 ADR-0147 ~1-unanticipated precedent; the PLAN-anticipated surfaces did not materialize). ADR tail at `ADR-0155`; next-free advances to `ADR-0156`.

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
