# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `06.1-stats-prometheus`
- **phase-directory:** `docs/envoy-go/phases/06.1-stats-prometheus/` — contains `SPEC.md` (at `ad46930`, frozen), `PLAN.md` (at `343b198`), `PROGRESS.md` (full Task-1-through-Task-15 history + L4 FAIL block at `1f94b74` + gate-(d) fix block at `79be6b0` + L4 PASSED re-verification block at `1ed6cd0`), and `REVIEW.md` (written by THIS commit; verdict APPROVED WITH FOLLOW-UPS — 0 Critical / 2 Important / 12 Minor; recommends Path A state-3 follow-up to close M-1 cluster-name guard before phase-done, or Path B advance directly to state 6 with carry-forward to the L4 review-followup batch). Parent `docs/envoy-go/phases/06-observability-baseline/` carries `BRAINSTORM.md` (`d271fd7`) + parent `SPEC.md` (`ad46930`). Sibling `docs/envoy-go/phases/06.2-access-log/` retains the placeholder `README.md` (`ad46930`). Phase 05 closed history unchanged.
- **lifecycle-state:** `5` — phase 06.1 verification complete + REVIEW complete (verdict APPROVED WITH FOLLOW-UPS at THIS commit, mirroring the 05.2 `b9810ad` precedent of keeping STATE at lifecycle-state 5 in the REVIEW commit and explicitly regressing to state 3 only when the Path-A follow-up batch begins). The review session at branch `phase/06.1-stats-prometheus-review` (worktree `.worktrees/phase-06.1-stats-prometheus-review`, branched from L4-PASSED verifier-2 tip `be3374b` per ADR-0003) cross-checked all 19 SPEC §14 acceptance bullets against on-disk evidence (PASS for 16; PARTIAL for 3 — bullets 9/15/17 carry literal-vs-precedent reconciliations that PROGRESS surfaces transparently), audited all seven 06.1 ADRs (ADR-0059..0065) for internal consistency + Lands-in-task pointers vs commit history, audited the BEHAVIOR_CONTRACT in-place edit against SPEC §10.1's empirical evidence (verbatim equal byte-for-byte; no drift), collapsed the 26-Minor triage queue from impl Tasks 8–14 to 12 substantively-distinct findings + the cluster-name carry-forward + one reviewer-observed item, and audited the phase-05.2 REVIEW carry-forward dispositions (M-9 RESOLVED-IN-06.1; M-4/M-10/M-12 correctly DEFERRED). Two Important findings, neither requiring state-3 re-entry: (I-1) SPEC §14 line 715 names file paths that don't exist — the M-9 fix landed at `internal/filter/hcm/h2dispatch.go` + `h2dispatch_test.go` not at `internal/filter/hcm/h2/router_action.go` + `router_action_test.go`; PROGRESS Task 11 deviation #1 is the corrigendum; reviewer recommends accept-with-PROGRESS-as-corrigendum (no commit needed); (I-2) ROADMAP row 06.1 is still `planned` despite SPEC §4.4 having claimed the SPEC commit flips it to `in-progress`; reviewer recommends close at the lifecycle-state-6 phase-done commit by flipping `planned → done` directly (functionally equivalent to the 05.1/05.2 two-step; the 05.2 phase-done at `0c01ed6` already used a one-step row flip). The most-impactful Minor is M-1 (cluster-name latent vulnerability at `internal/cluster/manager.go:97` — symmetric to the HCM `stat_prefix` defect that ADR-0065 closed; explicitly forward-flagged in ADR-0065 Consequences (d); the L4 verifier-1's `FuzzBootstrapLoad` 30s budget did not happen to discover a crasher but the latent vulnerability is real and is the natural Path-A follow-up scope). Per BOOTSTRAP §5.2 review-feedback re-entry, Path A bounces the lifecycle back to state 3 for the M-1 follow-up batch; Path B keeps lifecycle at 5 and lets the phase-done commit absorb all carry-forwards. **Path A is recommended** (closes the ADR-0065 Consequences (d) forward-flag now while context is fresh; mirrors the 05.1/05.2 default).
- **next-skill:** `superpowers:subagent-driven-development` — drive the REVIEW-followup batch on a new `phase/06.1-stats-prometheus-review-followup` branch (per the 05.2 `phase/05.2-upstream-h2-review-followup` precedent). The follow-up batch is scoped to **M-1 only** (the cluster-name `stats.IsValidName` guard at `cluster.NewManager`'s cluster-name boundary + 5-6-case regression test mirroring `TestParseFilter_StatPrefixInvalidChars`'s pattern; ~10-15 LoC; inherits ADR-0065's pattern by reference per ADR-0065 Consequences (d) — no new ADR). I-1 and I-2 do NOT require code/doc changes in the follow-up branch: I-1 is accepted-with-PROGRESS-as-corrigendum (SPEC immutability governs); I-2 is closed at the phase-done commit's natural ROADMAP row-flip diff. After the follow-up batch lands and gates (b)/(c)/(d)/(e) are re-run green (gate (a) remains GREEN at fixture 0005; gate (f) is closed by a PROGRESS verification block addendum on the follow-up branch), STATE advances to lifecycle-state 6 for the phase-done commit (which flips ROADMAP row 06.1 → `done` directly per the 05.2 precedent; row 06 stays `in-progress` until 06.2's phase-done; row 06.2 stays `planned`; STATE then advances to lifecycle-state 0 between-phases with `active-phase: 06.2-access-log`, `next-skill: superpowers:brainstorming`). The phase-done commit message follows BOOTSTRAP §5.3: `phase 06.1: stats-prometheus [ADR-0059, ADR-0060, ADR-0061, ADR-0062, ADR-0063, ADR-0064, ADR-0065]` (seven ADRs total — six anticipated + ADR-0065 the gate-(d) fix bonus) plus differential-surface + conformance summary. **If Path B is chosen instead**, this `next-skill` is replaced with the phase-done commit directly, branched from THIS commit's HEAD; M-1 then carries forward to the L4 review-followup batch as a separate post-phase-done branch alongside the 12 collapsed Minors.
- **next-skill-scope:** Path A (default, recommended). The follow-up branch `phase/06.1-stats-prometheus-review-followup` is created in a fresh worktree `.worktrees/phase-06.1-stats-prometheus-review-followup`, branched from THIS commit's HEAD per ADR-0003. The first commit on the followup branch explicitly regresses STATE to lifecycle-state 3 with a `STATE.md → review-followup re-entry (state 5 → 3 per BOOTSTRAP §5.2)`-style message (mirroring 05.2 `c6a0541`). The followup task is M-1 only: add `stats.IsValidName("cluster." + c.GetName() + ".upstream_rq_total")` boundary guard at `cluster.NewManager`'s cluster-name validation point, returning `fmt.Errorf("cluster: %q: invalid cluster name (...)", c.GetName())` on failure; add a regression test at `internal/cluster/manager_test.go` covering 5-6 invalid-char cases. The fix inherits ADR-0065's pattern by reference (Consequences (d) explicitly authorises the inheritance — no new ADR). After the M-1 fix commit lands, run gates (b)/(c)/(d)/(e) locally and append a PROGRESS verification block addendum (mirroring 05.2 `f774d4f`); update STATE.md to lifecycle-state 5 confirming verification still green (mirroring 05.2 `5c0f3cc`); then the next session writes the phase-done commit at lifecycle-state 6 (mirroring 05.2 `0c01ed6`). Per ADR-0003 the followup + phase-done branches fast-forward into `master` at the phase-done commit. The phase-done commit body MUST cite REVIEW.md's verdict, explicitly note the I-1 PROGRESS-as-corrigendum acknowledgment, and document the I-2 one-step `planned → done` row flip resolution. **Path B alternative**: skip the followup branch; the phase-done commit lands directly on THIS commit's HEAD; M-1 carries forward to the L4 review-followup batch (a separate post-phase-done branch), and the 12 collapsed Minors carry forward to the same batch.
- **last-commit:** `59d86f2` — `phase 06.1: REVIEW.md — APPROVED WITH FOLLOW-UPS (0 Critical / 2 Important / 12 Minor)`, on branch `phase/06.1-stats-prometheus-review`. The review session produced REVIEW.md per the 05.1/05.2 format precedent and cross-checked the full 06.1 surface against SPEC §14 acceptance bullets, BEHAVIOR_CONTRACT in-place edits, ADR-0059..0065 internal consistency, BOOTSTRAP §3 doctrine compliance (D-3.2/3.3/3.4/3.6/3.7), the impl-Tasks-8-14 26-Minor triage queue, and the phase-05.2 REVIEW carry-forward dispositions. Reviewer-commit surface change is two files (`STATE.md` + REVIEW.md); no production code, test, or fixture changes per the doc-only review-role contract. ROADMAP unchanged here (the phase-done commit at lifecycle-state 6 will flip row 06.1 → done in one step per the 05.2 precedent). SHA-fill follow-up per the phase-02..05.2 convention.
- **last-updated:** 2026-04-28

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
