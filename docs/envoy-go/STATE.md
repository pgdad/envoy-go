# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `13-http-filter-buffer` — phase 13 brainstorm landed at commit `6cf412e` and was post-landing AMENDED at this commit per the rare D-3.5 amendment route, in light of empirical findings against Envoy v1.37.2 that surfaced during the SPEC-authoring session 2026-05-09 and overturn the algorithmic core hypothesized at §2.6/§2.7/§2.8. ROADMAP row 13 stays `planned` (no flip; the amendment is docs-only). Phase 13 is the SIXTH §9 family-row to enter the lifecycle (after cors @ 07.1 done, fault @ 09 done, header_mutation @ 10 done, local_ratelimit @ 11 done, csrf @ 12 done); the §9 family heading at ROADMAP line 56 stays unchanged per ADR-0106(c). Filter selection (`buffer`) was the brainstormer's choice per ADR-0106(b) + the user's interactive Q1+Q2 dialogue.
- **phase-directory:** `docs/envoy-go/phases/13-http-filter-buffer/` — contains `BRAINSTORM.md` (~794 lines after §12 amendment; was ~556 at the brainstorm-landing commit). §§1-11 are PRESERVED VERBATIM per D-3.5 immutability of the original landing; §12 is the post-landing amendment that supersedes §§1.1, 2.5, 2.6, 2.7, 2.8, 6, 7, 8, and 11 for SPEC-drafting purposes. SPEC.md, PLAN.md, PROGRESS.md, REVIEW.md authored over phase lifecycle (states 2 → 6). The amended brainstorm now commits **8 → 8 design Decisions** (Decisions 5/6/7/8 superseded by §12.4 v2 + §12.5; Decisions 1/2/3/4 unchanged), **3 differential-fixture-scenario set + 1 cross-cutting CL-injection assertion = 6 fixture requests** (was 5; restructured via §12.6 — Content-Length fast-fail scenario retired since Envoy doesn't fast-fail), **3 anticipated ADRs at §12.7 (ADR-0125, ADR-0126, ADR-0127 v2)** (was 4; ADR-0128 retired since phase 13 contributes ZERO new stat-table entries), **2 inline-deferral items at §8** (unchanged), and **11 empirical-pin obligations at §9** (LARGELY RESOLVED in §12.3 disposition table; SPEC author re-runs each probe and records verbatim transcripts in SPEC §11 per ADR-0004 but no Decision-level questions remain open absent re-run divergence). NO sibling-stub was authored per ADR-0106(b). NO off-master prebrainstorm-notes branch was authored.
- **lifecycle-state:** `phase 13 brainstormed + amended; SPEC.md pending` — phase 13 BRAINSTORM.md authored at parent commit `6cf412e` and amended via §12 at this commit. SPEC.md not yet authored. Lifecycle-state-1 → 2 transition (BOOTSTRAP_PROMPT.md §5) is the next session's responsibility. Phase 12's six gates remain green at base commit `a782fc9`; this commit is docs-only (no code changes; no test re-validation needed).
- **next-skill:** `superpowers:writing-plans` per BOOTSTRAP_PROMPT.md §5 lifecycle-state-2 entry condition (SPEC.md exists, PLAN.md does not — phase 13 enters this state once SPEC.md is authored). The SPEC author re-runs the 11 §9 empirical-pin obligations IN-SESSION against reference Envoy v1.37.2 per ADR-0004 and records verbatim transcripts in SPEC §11; the §12.3 disposition table gives the expected outcomes verbatim, so the SPEC author validates rather than discovers.
- **next-skill-scope:** Cold-start scope for the phase 13 SPEC-authoring session. Read first: `docs/envoy-go/phases/13-http-filter-buffer/BRAINSTORM.md` §§1-12 (the §1-§11 design sketch + the §12 empirical amendment — every load-bearing decision lives there per D-3.4); `docs/envoy-go/ROADMAP.md` row 13 (status `planned`); `docs/envoy-go/BEHAVIOR_CONTRACT.md ## HTTP filter chain ### envoy.filters.http.csrf` subsection (the just-shipped phase 12 contract); `docs/envoy-go/DECISIONS.md` ADR-0076 (framework body cap + 413 wire shape — the safety-net cap that phase-13's filter-counts algorithm layers under) + ADR-0073 / ADR-0117 / ADR-0124 (per-route precedents — phase 13 codifies the 5th canonical shape at ADR-0125 anticipated) + ADR-0120..ADR-0124 (the 5 phase-12 ADRs — phase 13 numbers ADR-0125 + ADR-0126 + ADR-0127 v2 next-free; ADR-0128 retired per §12.7); `internal/filter/http/chain.go` lines 16-31 (the framework's hardcoded `filterBufferLimitBytes = 1 << 20` cap + `localReply413Body = "Payload Too Large"` 17-byte constant — phase-13's body-counting algorithm reuses these verbatim); `docs/envoy-go/phases/12-http-filter-csrf/SPEC.md` + `phases/12-http-filter-csrf/PROGRESS.md` (the most-recent phase artefacts). Highest-leverage parts of the §12 amendment for the SPEC author: §12.1 (source-of-truth code excerpts; load-bearing for ADR-0127 v2 wording); §12.3 (11-pin disposition table; the SPEC §11 re-run validates rather than discovers); §12.4 Decision 6 v2 (filter-counts algorithm + StopIteration on DecodeHeaders + maybeAddContentLength mirror; load-bearing for ADR-0127 v2); §12.5 (zero stat-table delta; ADR-0128 retirement); §12.6 (6-request fixture matrix). Anticipated SPEC.md size: ~1100-1300 lines (was ~1500-1700 hypothesized at brainstorm-landing time; the simpler stat surface + dropped CL fast-fail trim ~300 lines).
- **last-commit:** `<TBD>` — `phase 13 brainstorm amendment 1: §12 empirical re-frame [post-landing per D-3.5]`. Authors §12 of BRAINSTORM.md (~242 net lines added; total 794) capturing source-of-truth excerpts from `source/extensions/filters/http/buffer/buffer_filter.cc` at `v1.37.2`, an 11-pin empirical disposition table, re-framed Decisions 5/6/8 v2, retired Decision 7 (zero stat-table delta), retired ADR-0128, and a restructured 6-request fixture matrix. Docs-only commit; no code changes; phase 12's six gates remain green at base commit `a782fc9`.
- **last-updated:** 2026-05-09

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
