# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `05.1-downstream-h2`
- **phase-directory:** `docs/envoy-go/phases/05.1-downstream-h2/` — contains `README.md` (placeholder, written at split time per ADR-0045 / commit `2162509`), `SPEC.md` (drafted by brainstorming per ADR-0004 autonomous mode; commit `4b45941`), and `PLAN.md` (drafted by writing-plans per ADR-0005 autonomous mode; commit `634bcb6`; 3331 lines, 16 tasks, 8 new ADRs). The sibling `docs/envoy-go/phases/05.2-upstream-h2/` is unchanged from the split commit (placeholder `README.md` only). The parent `docs/envoy-go/phases/05-http-2/` retains its `SPEC.md` (`612cdea`) as the master design document for both sub-phases per ADR-0045 and phase-05 SPEC §11.1; that directory remains closed read-only history at the parent level (no PLAN/PROGRESS/REVIEW will land under it; those land under the sub-phase directories).
- **lifecycle-state:** `3` — phase 05.1's `PLAN.md` exists at `phases/05.1-downstream-h2/PLAN.md` (commit `634bcb6`); implementation is incomplete (no `internal/filter/hcm/h2/` sub-package yet, no `test/conformance/h2spec/`, no `CONFORMANCE_PINS.md`, no PROGRESS.md, no `BEHAVIOR_CONTRACT.md ## HTTP/2` subsection, ADRs 0046..0053 unwritten). Per `BOOTSTRAP_PROMPT.md` §5 state 3, the next session invokes `superpowers:subagent-driven-development` (per ADR-0005 step 4 default executor preference) on the 16 tasks in PLAN.md from a fresh `phase/05.1-downstream-h2-impl` worktree. The §6.1 split GATE was re-run at PLAN write time and held: 16 tasks (well under the 25-task threshold); ~2400 LoC (over the 1500-LoC threshold by ~60%, comparable to phase-04's accepted overshoot per `ddf41cd`'s scope check); per SPEC §11.1 the codec-direction axis is exhausted by ADR-0045, so a re-split would require exiting blocked rather than further sub-splitting. The PLAN's `## Scope check` section argues task-count-under-25 is the operative signal; the mid-execution split valve (`BOOTSTRAP_PROMPT.md` §6.1 secondary trigger) and a re-evaluation threshold ("if landed-LoC exceeds 3500 by end of Task 9 invoke systematic-debugging") are documented. The PLAN was reviewed by the `plan-document-reviewer` subagent per ADR-0005 step 1 and approved on iteration 1 (no issues found; five advisory recommendations returned, two substantive items applied in `634bcb6` itself: Settled #9 — `body string` field renamed to `bodyText` so SPEC §13's `body()` method name is reachable; Settled #10 — import-cycle resolution promoted to settled-decisions block, one-way `hcm → h2` import via `h2dispatch.go` adapter at Task 9; three advisory items recorded as non-blocking).
- **next-skill:** `superpowers:subagent-driven-development`
- **next-skill-scope:** Execute `phases/05.1-downstream-h2/PLAN.md` (commit `634bcb6`)'s 16 atomic tasks per ADR-0005 step 4 (subagent-driven-development is the default executor stance). The PLAN's `## Execution preconditions` block enumerates the cold-start checks (Task 1's responsibility); the executor verifies each before touching any production file. Eight new ADRs land at first-use commits during execution: ADR-0046 codec source (Task 4); ADR-0047 server settings defaults + ADR-0041 amendment (Task 7); ADR-0048 server connection manager from scratch (Task 8); ADR-0049 `--allow-h2c` CLI flag (Task 11); ADR-0050 ALPN dispatch wiring (Task 12); ADR-0051 h2spec threshold + pin (Task 15); ADR-0052 BEHAVIOR_CONTRACT scaffold + ADR-0053 phase-04 REVIEW carry-forward triage (Task 16). The executor verifies `grep '^## ADR-' docs/envoy-go/DECISIONS.md | tail -1` returns `## ADR-0045:` at cold-start (precondition 7); if higher, every task's ADR reference is renumbered before Task 1. Per ADR-0003 + the project's per-phase-worktree convention, execution runs from a fresh `phase/05.1-downstream-h2-impl` worktree branched from master tip after this commit's fast-forward; the current `.worktrees/phase-05.1-downstream-h2-plan` is closed-history at this state transition. Phase-done gates per SPEC §3 + 05.1 specialization: gate (a) **vacuous** (no new differential fixture in 05.1 per ADR-0045); gate (b) all phase-04 fixtures still green; gate (c) **NEWLY NON-VACUOUS** — h2spec sections 3/4/5/6-ex-6.6/7/8 report `failed=0`; gate (d) two new fuzz targets clean for the 30-second CI budget; gate (e) `go vet` + `golangci-lint run` + `go test ./...` clean + ADR-0046 boundary grep passes + `client.go` does not exist; gate (f) REVIEW.md approved by the post-impl `superpowers:requesting-code-review` session (state-5 work, not state-3).
- **last-commit:** `634bcb6` — the PLAN-draft commit on branch `phase/05.1-downstream-h2-plan` (added `docs/envoy-go/phases/05.1-downstream-h2/PLAN.md` 3331 lines). This STATE.md commit (the lifecycle-transition commit that promotes lifecycle-state from 2 to 3 and points `last-commit` at `634bcb6`) is the second and final commit on the `phase/05.1-downstream-h2-plan` branch. Master is fast-forwarded to this STATE.md commit per ADR-0003.
- **last-updated:** 2026-04-25

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
