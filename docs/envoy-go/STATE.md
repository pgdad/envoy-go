# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `15-http-filter-bandwidth-limit` — phase 15 PLAN.md authored at this commit (state-3 → state-4 transition per ADR-0005). Phase 15 is the EIGHTH §9 family-row (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11, csrf @ 12, buffer @ 13, compressor @ 14) and the FIRST §9 row since phase-12 csrf to introduce **ZERO framework deltas** — composes against phase-09 fault async-resume (`time.AfterFunc` + `cb.ContinueDecoding/Encoding`) + phase-13 ADR-0128 decode-side body-buffering + phase-14 ADR-0131 encode-side `OverwriteBody` (anticipated NOT invoked). MVP envelope per SPEC §1 + §1.1 (**10 amendments**): 4 consumed (`stat_prefix` PGV `min_len=1`, `enable_mode` all 4 values, `limit_kbps` UInt64Value **KiB/s units** per amendment 6 + filter-internal-REQUIRED-at-per-route, `fill_interval` Duration default 50ms PGV `[20ms, 1s]`) + 3 silent-ignored (`runtime_enabled`, `enable_response_trailers`, `response_trailer_prefix`); Path B-async body algorithm with **kbps-per-tick throttle math** per ADR-0137 + §1.1 amendment 6 + §11.P15. Per-route uses **same bare `BandwidthLimit` proto via TPFC** (NO `BandwidthLimitPerRoute` wrapper per §11.P1; NEW 6th canonical pattern adjacent to ADR-0117 4th canonical via ADR-0125 §(xi) amendment). Per-route stats INDEPENDENT per ADR-0117 + ADR-0139. Stat surface 46 → **60 names** (14 active = 8 counters + 6 gauges) + 2 unconditional Envoy histograms allow-listed via twin-series-filter. Differential fixture `0017-http-bandwidth-limit` (6 scenarios; ±70ms tolerance); 19th fuzzer `FuzzBandwidthLimitConfigParse`. PLAN.md sequences **16 TDD tasks** decomposing SPEC §6 code shapes + §7 fixture topology + §8 ADR roster + §14 testing strategy + §15 acceptance checklist; settles 16 planner-time decisions (8 SPEC §12 + 8 PLAN-emerging); ADR anchor schedule per ADR-0044 ADR-on-impl (ADR-0135 + ADR-0136 at Task 2; ADR-0137 at Task 3; ADR-0138 at Task 8; ADR-0139 at Task 7 — phase-13 buffer authored-at-impl-time convention, NOT phase-14 SPEC-time-pre-landing).
- **phase-directory:** `docs/envoy-go/phases/15-http-filter-bandwidth-limit/` — BRAINSTORM.md (509 LoC; PRESERVED VERBATIM per D-3.5) + SPEC.md authored (~1800 LoC; §§1-15 covering envelope + 10 §1.1 amendments + §11 verbatim empirical-pin evidence + §13 BEHAVIOR_CONTRACT patches + §14 testing strategy + §15 acceptance checklist) + PLAN.md authored at this commit (~1336 LoC; 16 TDD tasks + 16 planner-time decisions + 5-ADR anchor schedule + 17-precondition cold-start checklist). PROGRESS.md / REVIEW.md NOT yet authored — those are subsequent lifecycle-state transitions (state-4 → 5 → 6) per ADR-0005 §Decision 4 session-split discipline.
- **lifecycle-state:** `phase 15 PLAN.md authored; impl pending` — state-3 → state-4 transition at this PLAN commit. Next session executes the 16-task impl chain per `superpowers:subagent-driven-development` (per project memory `feedback_execution_style.md` always-subagent-driven preference). PLAN's 5-ADR anchor schedule (ADR-0135 + ADR-0136 at Task 2; ADR-0137 at Task 3; ADR-0138 at Task 8; ADR-0139 at Task 7) supersedes the SPEC §8 roster-only anticipation per ADR-0044 ADR-on-impl convention.
- **next-skill:** `superpowers:subagent-driven-development` per ADR-0005 §Decision 4 default-executor preference + project memory `feedback_execution_style.md` always-subagent-driven preference. The impl session dispatches a fresh subagent per task; reviews between tasks; the 16-task chain lands the phase-15 production code + fixture + ADRs + BEHAVIOR_CONTRACT patches + ROADMAP/STATE advances + REVIEW.md.
- **next-skill-scope:** Cold-start scope for the phase 15 impl session. Read first: `docs/envoy-go/phases/15-http-filter-bandwidth-limit/PLAN.md` (the authoritative impl-shape doc; 16-task TDD chain + per-task Files/Precondition/Artifact/Acceptance bullets + 16 planner-time-decision resolutions). Read also: `docs/envoy-go/phases/15-http-filter-bandwidth-limit/SPEC.md` (the authoritative design doc; §6 code shapes are the source-of-truth for each task's implementation; §11 empirical-pin block is the verbatim Envoy v1.37.2 evidence). `docs/envoy-go/phases/14-http-filter-compressor/PROGRESS.md` + `docs/envoy-go/phases/13-http-filter-buffer/PROGRESS.md` (impl-session PROGRESS.md structural precedents). `internal/filter/http/localratelimit/local_ratelimit.go` (the algorithmic precedent — phase-15 mirrors `factoryState` + `sync.Map` lazy-cache + `resolvePerRouteConfig` + `newFilterStatsIfAbsent` shape verbatim). `internal/filter/http/fault/fault.go` (the `time.AfterFunc` + `OnDestroy.Stop()` precedent; phase-15 reuses with the simpler SPEC §6.9 Stop-races-Fire pending-gauge discipline). 5 ADRs (ADR-0135..ADR-0139) authored at impl-time per ADR-0044 ADR-on-impl + phase-13 buffer convention (UNLIKE phase-14 compressor's SPEC-time-pre-landing). Worktree spawn: `git worktree add .worktrees/phase-15-http-filter-bandwidth-limit-impl -b phase-15-http-filter-bandwidth-limit-impl master` branched off master tip post-PLAN-SHA-fill.
- **last-commit:** `a5c5ec9` — `Squash merge phase-15-http-filter-bandwidth-limit-plan`. Squashed 1 worktree commit `cf9495a` (phase 15 PLAN: http-filter-bandwidth-limit [16 tasks; 16 planner-time decisions; 5-ADR anchor schedule]). The PLAN-stage commit carries: PLAN.md authored at `docs/envoy-go/phases/15-http-filter-bandwidth-limit/PLAN.md`; STATE.md advance (this file). ROADMAP.md row 15 stays `in-progress` (PLAN landing does not flip the row; only phase-done flips to `done`). Master tail: `a5c5ec9` (this squash-merge) → `cd45af0` (phase 15 SPEC follow-up STATE.md SHA-fill) → `49e0361` (phase 15 SPEC squash-merge) → `e7a26ef` (phase 15 brainstorm follow-up) → `fa125f2` (phase 15 brainstorm squash-merge) → `f4ce582` (phase 14 lifecycle-state-6) → preceding phase-14 commits.
- **last-updated:** 2026-05-11
- **next-free ADR:** `ADR-0135` (unchanged from phase 15 SPEC commit; phase-15 PLAN anchors ADR-0135..ADR-0139 at impl-time tasks per ADR-0044 ADR-on-impl convention; ADR-0125 §(xi) amendment paragraph LANDED at SPEC commit). After phase-15 impl-time closes, `next-free ADR` advances to `ADR-0140`.

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
