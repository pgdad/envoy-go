# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `19.2-http-filter-ext-proc-body` — phase 19.2 PLAN session COMPLETE at 2026-05-16 (state 2 → 3 transition per ADR-0005 §Decision 4 + SKILL_ROUTING §2). The 19.2 PLAN composed against the 19.2 SPEC (the load-bearing input — 458 lines / 15 sections / 16-item §15 acceptance checklist) + REUSED the 19.1 PLAN format precedent (per-Task Files / Precondition / Artifact / Acceptance / Steps with checkboxes / Commit block). **In-session PLAN-author resolutions** (12 D-series decisions per PLAN §"Planner-time deferred-decision resolution"): D1 `EncoderFilterCallbacks.BufferEncodedBody() []byte` method name LOCKED (mirrors `BufferedBody` decoder-side; verb-first naming convention parallels `OverwriteBody`); D2 `processFn` closure-capture extends existing `activeProcessingMode` per-direction struct with `bodyMode + bodyBuf` fields; D3 4-stage state machine SPLIT-BY-DIRECTION per 19.1 precedent (NOT single 4-stage enum); D4 per-message timer = SINGLE ROLLING TIMER per direction via `context.WithTimeout` cancel-and-rebuild (NOT per-stage — at-most-one-in-flight invariant); D5 body-stage attribute envelope = header-stage SUPERSET (adds `request.size`/`response.size` populated from `len(body)`; exact roster crystallizes at Task 9 fixture-harness scrape); D6 PARSE-REJECT error text for `streamed_response` LOCKED; D7 OnDestroy-during-body-stage-outbound discipline + chain-side overflow SYMMETRIC to decode-side `errDecodeBufferOverflow`; D8 4 ADR Lands-in-Task assignments — ADR-0175 §Decision + §Consequences at Task 2 (single-Task); ADR-0168 §Decision AMENDMENT at Task 3 + §Consequences refresh at Task 7; ADR-0171 §Decision AMENDMENT + §Consequences at Task 4; ADR-0172 §Decision AMENDMENT + §Consequences at Task 6; D9 BEHAVIOR_CONTRACT 7-edit bundle lands at Task 10 (single-Task closing bundle per the smaller-than-19.1 envelope); D10 NO new ADR numbers consumed at 19.2 IMPL — 19.1 D12 hypothesis REASSERTED (next-free `ADR-0177` stays unconsumed); D11 25-task / 1500-LoC PLAN gate ratified (11 tasks / ~830-1400 production LoC — well under both bounds; no further sub-phase split); D12 per-route 5th-canonical body-mode arm activation LOCKED (no ADR-0173 amendment fires at 19.2). The PLAN was reviewed via `plan-document-reviewer` subagent: **Approved** with 2 advisory recommendations (one folded in — D5 disposition clarification at Task 9). The next session is the phase-19.2 IMPL-execution session (lifecycle-state 3 → done) per `superpowers:subagent-driven-development` (project memory `feedback_execution_style.md`).
- **phase-directory:** `docs/envoy-go/phases/19-http-filter-ext-proc/` (BRAINSTORM.md + parent SPEC.md) + `docs/envoy-go/phases/19.1-http-filter-ext-proc-headers/` (SPEC.md + PLAN.md + PROGRESS.md + REVIEW.md) + `docs/envoy-go/phases/19.2-http-filter-ext-proc-body/` (README.md sibling stub + SPEC.md authored at 19.2 SPEC session + PLAN.md authored at this PLAN session — 735 lines / 11-task work-unit decomposition; PROGRESS.md + REVIEW.md pending at the next-session lifecycle-state 3 → done).
- **lifecycle-state:** `phase 19.2 PLAN done; phase 19.2 IMPL pending; phase 19 parent in-progress; phase 19.1 done` — the 19.2 PLAN is complete; the 19.2 sub-row STAYS `in-progress` at this commit (no ROADMAP row-status change at PLAN-done; only the last-touched date advances); the parent row 19 STAYS `in-progress` (closes at 19.2's phase-done AT THE SAME commit per parent SPEC §8 rollup discipline). ROADMAP row `19.1` is `done` (last-touched `2026-05-16`); row `19` (parent) is `in-progress`; row `19.2` is `in-progress` (last-touched `2026-05-16` — advanced at this PLAN commit; depends-on `19.1`). DECISIONS.md tail still at ADR-0176; next-free ADR `ADR-0177` stays unconsumed (D10 hypothesis HELD at 19.2 PLAN time). The 19.2 IMPL session executes the 11-task PLAN per `superpowers:subagent-driven-development`; each task lands at its per-Task Lands-in-Tasks per ADR-0044 (Task 2 lands ADR-0175 §Decision + §Consequences; Tasks 3 + 4 + 6 land ADR-0168 / ADR-0171 / ADR-0172 §Decision AMENDMENTS in-place; Task 7 refreshes ADR-0168 §Consequences with integration completeness). Anticipated 19.2 IMPL production code per PLAN §"Scope check": ~830-1400 LoC across `internal/filter/http/extproc/` body-stage extension + `internal/filter/http/{callbacks,chain}.go` ADR-0175 encode-side body-buffering primitive + fixture `0023-http-ext-proc-body` (6 scenarios; REUSES 19.1's `test/helpers/extprocgrpc/` UNMODIFIED) + 25th fuzzer `FuzzProcessingResponseMapping`.
- **next-skill:** `superpowers:subagent-driven-development` (to execute the 11-task PLAN at `docs/envoy-go/phases/19.2-http-filter-ext-proc-body/PLAN.md`). Cold-start scope for the 19.2 IMPL session: read STATE.md (this file); `docs/envoy-go/phases/19.2-http-filter-ext-proc-body/PLAN.md` (the load-bearing input — 735 lines / 11 tasks); `docs/envoy-go/phases/19.2-http-filter-ext-proc-body/SPEC.md` (the spec the PLAN composes against); `docs/envoy-go/phases/19.1-http-filter-ext-proc-headers/PROGRESS.md` (the 19.1 IMPL evidence trail + per-task discipline precedent); `docs/envoy-go/DECISIONS.md` (ADR-0175 §Context at parent SPEC commit `9cc1458`; lands at Task 2; ADR-0168/0171/0172 §Decision AMENDMENTS land in-place per ADR-0044 at Tasks 3/4/6); `docs/envoy-go/BEHAVIOR_CONTRACT.md` (12 HTTP filters; stat-table at 86 names; SPEC §13 7-edit bundle lands at Task 10); project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (subagent-driven IMPL + always-worktrees + always-push). The 19.2 IMPL session's defining IN-SESSION obligations: spawn a fresh worktree `phase-19.2-http-filter-ext-proc-body-impl` per the 12-precondition verification at Task 1; subagent-dispatch each of the 11 tasks per the PLAN's `## Task graph` (Tasks 2/3/5 parallelizable after Task 1; Task 4 after Task 3; Task 6 after Tasks 2+4; Task 7 wires 2-6; Tasks 8/9/10/11 sequential); the closing Task 11 REVIEW per `superpowers:requesting-code-review`; squash-merge to master closes BOTH row 19.2 AND parent row 19 AT THE SAME commit per parent SPEC §8 rollup discipline.
- **last-commit:** `<19.2 PLAN squash commit SHA — TBD before squash; SHA-fill follow-up after squash-merge>` — the 19.2 PLAN worktree `phase-19.2-http-filter-ext-proc-body-plan` (branched off master tip `b9d2b78` — `phase 19.2 SPEC follow-up: STATE.md SHA-fill (TBD → 954a570 post-squash)`) carries one PLAN commit (the 19.2 PLAN.md + STATE/ROADMAP advance); squash-merged to master + SHA-fill follow-up per the phase-09..19.1 precedent.
- **last-updated:** 2026-05-16
- **next-free ADR:** `ADR-0177` — UNCHANGED from the phase-19 SPEC commit + the phase-19.1 IMPL session + the phase-19.2 SPEC session. The phase-19.2 PLAN session consumed NO new ADR numbers (D10 hypothesis HELD at PLAN time — REASSERTS the 19.1 D12 hypothesis: ADR-0175 was reserved at the parent SPEC commit + the §Decision + §Consequences land at 19.2 IMPL Task 2 per ADR-0044; ADR-0168 / ADR-0171 / ADR-0172 §Decision AMENDMENTS are in-place edits to existing 19.1-anchored §Decision sections at Tasks 3 + 4 + 6 per ADR-0044, NOT new ADR consumptions). The 19.2 IMPL may consume ADR-0177 + ADR-0178 if 19.2-IMPL-unanticipated ADRs fire (most-likely surfaces per SPEC §10 + PLAN D10: buffered-body-release-vs-stream-reset interaction with the framework's encode-chain primitive; `body_mutation_rules` application to body bytes — both UNLIKELY per the SPEC's analysis); if fired, PROGRESS.md records D10 as FALSIFIED.

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
