# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `phase 36.2 (load-balancer-ring-hash-http) done` (2026-06-13, this commit) — the phase-36.2 IMPL is COMPLETE (the 8-task TDD PLAN executed subagent-driven, six-gate GREEN). Lands the HTTP router's `RouteAction.hash_policy` PRODUCER plane — the SECOND data plane feeding the ALREADY-LANDED ADR-0235 ctx-carried-hash-key seam + the ADR-0236 `ringHashLB` ring (after the tcp_proxy `source_ip` plane at 36.1; the CONSUMED 36.1/36.2 by-plane split is now CLOSED). As built: the ONE additive exported symbol `cluster.HashHeaderValues` (the seed-chained `XXH64` fold over byte-sorted header values; single-value collapses to `xxHash64([]byte(value))`) + `parseRouteHashPolicies` at the hcm config-build boundary (the proto-free `router.HashPolicy` descriptor; router stays proto-free) + the `applyHashKey` fold (`rotl64(prev,1)^new`, first-verbatim, nullopt-skip, `terminal` short-circuit) at the 4 router dial sites + the uniform ctx-carried `router.WithDownstreamRemoteAddr` + the `0062-lb-ring-hash-http` NAT-transparent TRUE cross-side affinity differential (N=16 X-Hash values × K=16 = 256 routed requests; the modular-invariant affinity BOTH sides + spread + cross-side byte-equiv + cross-equal stats incl. `upstream_rq_total`). SUPPORT `header` + `connection_properties.source_ip`; DEPARTURE-reject cookie/query_parameter/filter_state + regex_rewrite + source_ip-false connection_properties; PARITY-reject empty `header_name` (`value length must be at least 1 runes`). **All D-S362-1..7 resolved + landed; ADR-0045 NO SPLIT.** **Counts at the IMPL six-gate:** stat surface **1119** (UNCHANGED — ZERO new stat names; the FIRST LB producer reusing the prior plane's stat surface entirely); fixtures **63 → 64** (`0062-lb-ring-hash-http`); fuzzers **42** (UNCHANGED — no wire decode); BackendKind tail **33** (UNCHANGED — existing HTTP backends reused); DECISIONS tail **ADR-0236 → ADR-0237** (ACCEPTED in-place per ADR-0044). The exported `Cluster` surface stays byte-stable (+1 additive symbol); ZERO new packages + ZERO new go.mod deps (`go mod tidy -diff` EMPTY); h2spec 53/53 + proxy-wasm 10/10 re-ran GREEN (NOT zero-touch like 36.1 — 36.2 touches the H1/H2 router path; the full 64-dir differential is the real guard).
- **phase-directory:** `docs/envoy-go/phases/36.2-load-balancer-ring-hash-http/` (**SPEC.md + PLAN.md + PROGRESS.md** — the durable 36.2 sub-phase SPEC + the 8-task TDD PLAN + the IMPL progress ledger [the as-built record + the six-gate evidence], all landed per the 32.1/32.2/29.1 per-sub-phase-dir precedent; the 36.2 IMPL LANDED THIS commit). The parent `docs/envoy-go/phases/36-load-balancer-ring-hash/` (**BRAINSTORM.md + SPEC.md + PLAN.md + README.md + PROGRESS.md** — the durable phase-36 parent BRAINSTORM/SPEC [both planes] + the 36.1 IMPL record; the 36.1/36.2 split is CONSUMED [parent SPEC §3.0], 36.1 LANDED). **Superseded (the phase-35 phase-directory, preserved):** `docs/envoy-go/phases/35-load-balancer-random/` (**BRAINSTORM.md + SPEC.md + README.md + PLAN.md** — PLAN.md landed THIS commit (README.md status advanced 2 → 3); the PROGRESS.md follows at the IMPL session [Task 1 creates it], per the kafka-31/thrift-33/least_request-34 single-flat-row per-phase-dir precedent). The phase-34 directory `docs/envoy-go/phases/34-load-balancer-least-request/` (**BRAINSTORM.md + SPEC.md + README.md + PLAN.md + PROGRESS.md** — the durable phase-34 IMPL record). The phase-33 directory `docs/envoy-go/phases/33-network-filter-thrift-proxy/` (**BRAINSTORM.md + SPEC.md + README.md + PLAN.md + PROGRESS.md** — the durable phase-33 IMPL record). The phase-32 dirs `docs/envoy-go/phases/32-network-filter-redis-proxy/` + `32.1-…/` + `32.2-network-filter-redis-commands-and-stats/` (**SPEC.md** — the durable phase-32.2 sub-phase SPEC; the PLAN.md follows at the 32.2 PLAN session, the PROGRESS.md at the 32.2 IMPL session [Task 1 creates it], per the 29.1/29.2/29.3 per-sub-phase-dir precedent). The phase-32.1 dir `docs/envoy-go/phases/32.1-network-filter-redis-upstream-pool-seam-and-codec/` (**SPEC.md + PLAN.md + PROGRESS.md** — the durable phase-32.1 IMPL record). The parent `docs/envoy-go/phases/32-network-filter-redis-proxy/` (**BRAINSTORM.md + SPEC.md + README.md** — the durable phase-32 parent-SPEC record). The phase-31 directory `docs/envoy-go/phases/31-network-filter-kafka-broker/` (BRAINSTORM.md + SPEC.md + README.md + PLAN.md + PROGRESS.md — the durable phase-31 IMPL record). The phase-30 + phase-29 directories stay the durable phase-30/29 record.
- **lifecycle-state:** `phase 36.2 IMPL done — the Load-balancing family STAYS OPEN (5 candidates remain: maglev, subset LB, locality-weighted LB, priority load balancing, panic thresholds — maglev the cheap follow-on reusing the ADR-0235 seam + the ADR-0236 ring unchanged). Next: the controller squash-merges the 8 task commits to master + pushes (per feedback_subagents_no_push + feedback_push_to_origin); the next-prompt roll-forward routes the next cold-start past 36.2 (the next Load-balancing candidate, or the next ROADMAP family — superpowers:brainstorming)`. The 36.2 IMPL is COMPLETE: the durable record is `docs/envoy-go/phases/36.2-load-balancer-ring-hash-http/PROGRESS.md` (the as-built ledger + the six-gate evidence); the SPEC + PLAN remain the design + execution references.
- **next-skill:** the controller's **stage-close** (squash-merge the 8 LOCAL-ONLY task commits to master + push); then the next cold-start routes past 36.2 via the next-prompt roll-forward (the next Load-balancing candidate or the next ROADMAP family — `superpowers:brainstorming`).
- **last-commit:** the phase-36.2 IMPL completion bundle (ADR-0052 atomic landing) — BEHAVIOR_CONTRACT 36.2 HTTP-plane addendum (the ring_hash subsection extended) + the full **ADR-0237** (the HTTP route `hash_policy` producer plane; §Context promoted verbatim from SPEC §13; §Decision + §Consequences IN-PLACE per ADR-0044; tail ADR-0236 → ADR-0237) + STATE/ROADMAP row-36 advance (`36.1 done, 36.2 done`; flat family row, NO parent rollup per ADR-0106) + the final six-gate evidence in PROGRESS.md. The prior 7 LOCAL-ONLY commits are the 36.2 IMPL Tasks 1-7.
- **last-updated:** 2026-06-13 (phase-36.2 IMPL done)
- **next-free ADR:** `ADR-0238` (DECISIONS.md tail advanced **ADR-0236 → ADR-0237** at this 36.2 IMPL — ADR-0237 [the HTTP route `hash_policy` producer plane] ACCEPTED in-place per ADR-0044).

---

## Project counts at this commit

- **active differential fixtures:** **64** (tail `0062-lb-ring-hash-http`). Canonical recipe `ls -d test/fixtures/[0-9]* | wc -l` = 64. Advanced at the phase-36.2 IMPL (+1 — `0062-lb-ring-hash-http`, the HTTP route header-hash NAT-transparent TRUE cross-side affinity+spread differential; the SECOND ring_hash producer plane after `0061`'s tcp source_ip).
- **fuzzers:** **42** (canonical recipe `grep -rh "^func Fuzz" $(find ./internal -name fuzz_test.go) | wc -l`). UNCHANGED at the phase-36.2 IMPL (+0 — the HTTP `hash_policy` producer decodes no untrusted wire bytes; the hash key derives from a parsed header value / source IP, not a wire frame; a key-fold property test is unit-level).
- **stat surface:** **1119** (BEHAVIOR_CONTRACT doc count). UNCHANGED at the phase-36.2 IMPL (+0 — ZERO new stat names; the HTTP `hash_policy` plane reuses the 3 `ring_hash_lb.*` gauges [36.1] + the existing per-cluster `cluster.<name>.*` roster; the FIRST LB producer reusing the prior plane's stat surface entirely; the DOC count is the source-of-truth, no programmatic golden).
- **BackendKind tail:** **33** (`TCPThriftResponder` — seq_id-agnostic canned framed-binary REPLY). UNCHANGED at the phase-36.2 IMPL (+0 — `0062` reuses the existing HTTP backends; a producer phase exercises WHICH key picks the backend, not a new backend).
- **DECISIONS.md tail:** **ADR-0237** (next-free **ADR-0238**) — **ADR-0237** (the HTTP route `hash_policy` producer plane — the SECOND data plane feeding the ADR-0236 `ring_hash` key; the `header` + `connection_properties.source_ip` specifiers, the `rotl64(prev,1)^new` fold, the additive `cluster.HashHeaderValues`; PRODUCER-ONLY — NO seam/manager/stat change) landed at the phase-36.2 IMPL, §Context promoted verbatim from SPEC §13, §Decision + §Consequences IN-PLACE, PROPOSED → ACCEPTED per ADR-0044. The prior tail ADR-0236 (the `ring_hash` policy) + ADR-0235 (the seam extension) landed at the phase-36.1 IMPL.
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227, superseding ADR-0008).
- **conformance:** h2spec 53/53; proxy-wasm all 10 families PASS — RE-RAN GREEN at the phase-36.2 IMPL (NOT zero-touch like 36.1: 36.2 touches the H1/H2 router path [`applyHashKey` stuffs a ctx key before the dial], but the wire path is byte-identical when no `hash_policy` is configured [every conformance config]; the full 64-dir differential is the real guard).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
