# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `06.1-stats-prometheus`
- **phase-directory:** `docs/envoy-go/phases/06-observability-baseline/` — contains `BRAINSTORM.md` (brainstorm-close artifact at `d271fd7`, frozen) plus `SPEC.md` (parent master SPEC, this commit). Sub-phase directories `docs/envoy-go/phases/06.1-stats-prometheus/` (full SPEC.md, this commit) and `docs/envoy-go/phases/06.2-access-log/` (README.md sibling stub, this commit) created by this commit. The parent SPEC summarizes both sub-phases (mirrors `docs/envoy-go/phases/05-http-2/`). Phase 05 (parent + sub-phases 05.1 + 05.2) remains closed read-only history.
- **lifecycle-state:** `2` — phase 06.1 SPEC drafted; the SPEC-drafting session on `phase/06.1-stats-prometheus-spec` (worktree `.worktrees/phase-06.1-stats-prometheus-spec`, branched from `d0ba694` per ADR-0003) lands four artifacts: parent 06 SPEC (~103 LoC), 06.1 sub-phase SPEC (~727 LoC including the Rule SN4 empirical-verification fold-in), 06.2 sibling README stub (~38 LoC), and the ROADMAP row 06 split. Rule SN4 was empirically verified during this session against reference Envoy v1.37.2 (image SHA per `ENVOY_TARGET.md`; server-side version SHA `5afe27fb338b16d5bb06b3a7198bcd581b4e3dee`) — verified form: `_Nxx` digit stripped from metric name (ends in literal `_xx`), label name `envoy_response_code_class`, label value single class digit (`"2"`/`"3"`/`"4"`/`"5"`). Verbatim scrape evidence inline in `docs/envoy-go/phases/06.1-stats-prometheus/SPEC.md` §10.1. ROADMAP row 06 split per ADR-0045: parent `06 | observability-baseline | 05 | in-progress | 06.1, 06.2 | ...` plus children `06.1 | stats-prometheus | 05 | planned | | ...` and `06.2 | access-log | 06.1 | planned | | ...`. Per BOOTSTRAP §5 the next transition is state 2 → 3 via `superpowers:writing-plans` (PLAN.md authoring for 06.1).
- **next-skill:** `superpowers:writing-plans` — PLAN.md authoring for phase 06.1. Inputs: this commit's `docs/envoy-go/phases/06.1-stats-prometheus/SPEC.md` (the authoritative source for task decomposition), the parent `docs/envoy-go/phases/06-observability-baseline/SPEC.md` (cross-cutting context), the closed-history `docs/envoy-go/phases/05.1-downstream-h2/PLAN.md` and `docs/envoy-go/phases/05.2-upstream-h2/PLAN.md` as structural precedent (task-numbering convention, "ADRs introduced by this plan" section, refinement notes), `BOOTSTRAP_PROMPT.md` §§5-7 (lifecycle + plan contract). The PLAN authors task-by-task TDD-shaped steps spanning ~12-16 tasks (06.1 surface is moderate; 05.2 had 15 tasks for comparable scope).
- **next-skill-scope:** Lifecycle-state 2 → 3 PLAN.md drafting session deliverable: `docs/envoy-go/phases/06.1-stats-prometheus/PLAN.md` with task-by-task implementation breakdown faithful to the 06.1 SPEC. Each task is bite-sized (TDD-shaped: write failing test, run it, implement, run it, commit per `superpowers:writing-plans`). The PLAN must enumerate the six anticipated ADRs (06.1 SPEC §8 — anticipated ADR-0059..ADR-0064; planner re-verifies next-free against `DECISIONS.md` tail at PLAN write time per ADR-0004) and bind each to its anchoring task. The PLAN must surface as explicit task scope: (a) the `cluster.NewManager` / `cluster.NewManagerWithBaseDir` / `admin.New` / `listenerManager.New` signature changes (06.1 SPEC §1.6 + §14 acceptance bullet); (b) the M-9 piggy-back fix (~5 LoC + ~20 LoC test); (c) the `FuzzPromTextFormat` fuzzer at the 30s ADR-0018 budget; (d) the BEHAVIOR_CONTRACT.md `## Stat-name mapping` in-place edit at the phase-done commit per ADR-0052 + the new equivalence-matrix row. After PLAN, advance STATE to lifecycle-state 3 with `next-skill: superpowers:subagent-driven-development` (per the user's persistent preference for subagent-driven plan execution). Worktree for the PLAN-drafting session: branch from this SPEC commit's HEAD per ADR-0003 + per-phase-worktree convention (`.worktrees/phase-06.1-stats-prometheus-plan`).
- **last-commit:** `ad46930` — `phase 06.1: SPEC drafted (autonomous brainstorming per ADR-0004) + Rule SN4 empirically pinned against reference Envoy v1.37.2`, on branch `phase/06.1-stats-prometheus-spec`. Lands the parent-06 SPEC, the 06.1 sub-phase SPEC with verified Rule SN4 (verbatim scrape evidence in §10.1; server-side version SHA `5afe27fb338b16d5bb06b3a7198bcd581b4e3dee`), the 06.2 sibling README stub, the ROADMAP row 06 split, and updates this STATE.md to lifecycle-state 2. No code changes. SHA-fill follow-up per phase-02..05.2 convention.
- **last-updated:** 2026-04-27

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
