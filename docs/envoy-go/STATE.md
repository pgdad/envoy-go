# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `phase 37 (load-balancer-maglev) brainstorm done` (2026-06-13, this commit) — the phase-37 BRAINSTORM is COMPLETE (lifecycle-state 0 → 1; `superpowers:brainstorming`, interactive with the live human + the spec-document-reviewer gate PASS first pass). Scopes the project's **fifth LB policy** and its **second consistent-hashing policy**: `Cluster.LbPolicy MAGLEV` (`envoy.config.cluster.v3`, enum value 5) — **the cheap follow-on** ADR-0235/ADR-0236 §Consequences named. MAGLEV **REUSES the ADR-0235 hash-key seam (`Pick(hashKey uint64, hasHash bool)`) + BOTH ADR-0237 producer planes (tcp `source_ip` + HTTP route `hash_policy`) UNCHANGED** (contrast phase-36, which BUILT them), swapping the Ketama RING for a fixed-size MAGLEV lookup table behind the SAME `Pick` seam (a new `maglevLB` in `internal/cluster/maglev.go` mirroring `ringHashLB` — built once, immutable, injected-rng; `Pick` indexes `table[hashKey % M]`, no-hash → uniform random table index; returns `noopRelease`). The v1.32.4 surface VERIFIED at the BRAINSTORM: enum value 5, WITH a `Cluster.MaglevLbConfig` message [a SINGLE `table_size` field, `UInt64Value`, PGV `value must be less than or equal to 5000011`; **NO `hash_function` knob** — maglev always uses `xxHash64`]. **Key decision (Q1): a configured non-prime `table_size` is DEPARTURE-rejected** fail-fast at config-build (accept unset→default-65537 / any prime ≤ 5000011 / the cap 5000011; the reference accepts a composite verbatim — a RECORDED DEPARTURE; the permutation requires `M` prime for a terminating full-cycle fill — each `skip ∈ [1, M-1]` is coprime to a prime `M`). **1 ADR anticipated:** ADR-0238 (the policy ONLY — NO seam ADR [ADR-0235 reused], NO producer ADR [ADR-0237 reused]; the phase-35 single-ADR-on-reuse shape). **Proof-shape:** `0063-lb-maglev` — a header-key NAT-transparent TRUE cross-side affinity+spread differential (the `0062` convention reused verbatim, retargeted to a MAGLEV cluster). **SINGLE flat row, NO split** (the producers already exist — no second subsystem to couple; ~120–220 prod LoC / ~8–10 tasks, well under the ADR-0045 gate; the least_request-34/random-35 precedent). The §10 D-M1..D-M6 empirical pins are EXECUTED at the SPEC (live against `envoyproxy/envoy:contrib-v1.37.2` per `reference_docker_probe_bridge_network`). **Counts UNCHANGED at this BRAINSTORM** (stat surface 1119 / fixtures 64 / fuzzers 42 / BackendKind tail 33 / DECISIONS tail ADR-0237, next-free ADR-0238); anticipated moves at the IMPL: fixtures 64 → 65 (`0063`), DECISIONS tail → ADR-0238, stat surface 1119 → ~1121 (+2 anticipated `maglev_lb.*` gauges — `min_entries_per_host`/`max_entries_per_host`, no `size` gauge; D-M4 pins).
- **phase-directory:** `docs/envoy-go/phases/37-load-balancer-maglev/` (**BRAINSTORM.md + README.md** — the durable phase-37 BRAINSTORM charter [the Q0/Q1 decisions, the v1.32.4 enum-5 + `Cluster.MaglevLbConfig` verification, the seam-REUSE rationale, the `table_size` primality DEPARTURE, the header-key cross-side affinity+spread differential design, the single anticipated ADR, the §10 D-M1..D-M6 pins] + the phase-overview README; the SPEC.md follows at the phase-37 SPEC session, the PLAN.md at the PLAN session, the PROGRESS.md at the IMPL session [Task 1 creates it], per the single-flat-row per-phase-dir precedent [kafka-31/thrift-33/least_request-34/random-35]). **Superseded (preserved):** `docs/envoy-go/phases/36.2-load-balancer-ring-hash-http/` (**SPEC.md + PLAN.md + PROGRESS.md** — the durable 36.2 sub-phase SPEC + the 8-task TDD PLAN + the IMPL progress ledger [the as-built record + the six-gate evidence], all landed per the 32.1/32.2/29.1 per-sub-phase-dir precedent; the 36.2 IMPL LANDED THIS commit). The parent `docs/envoy-go/phases/36-load-balancer-ring-hash/` (**BRAINSTORM.md + SPEC.md + PLAN.md + README.md + PROGRESS.md** — the durable phase-36 parent BRAINSTORM/SPEC [both planes] + the 36.1 IMPL record; the 36.1/36.2 split is CONSUMED [parent SPEC §3.0], 36.1 LANDED). **Superseded (the phase-35 phase-directory, preserved):** `docs/envoy-go/phases/35-load-balancer-random/` (**BRAINSTORM.md + SPEC.md + README.md + PLAN.md** — PLAN.md landed THIS commit (README.md status advanced 2 → 3); the PROGRESS.md follows at the IMPL session [Task 1 creates it], per the kafka-31/thrift-33/least_request-34 single-flat-row per-phase-dir precedent). The phase-34 directory `docs/envoy-go/phases/34-load-balancer-least-request/` (**BRAINSTORM.md + SPEC.md + README.md + PLAN.md + PROGRESS.md** — the durable phase-34 IMPL record). The phase-33 directory `docs/envoy-go/phases/33-network-filter-thrift-proxy/` (**BRAINSTORM.md + SPEC.md + README.md + PLAN.md + PROGRESS.md** — the durable phase-33 IMPL record). The phase-32 dirs `docs/envoy-go/phases/32-network-filter-redis-proxy/` + `32.1-…/` + `32.2-network-filter-redis-commands-and-stats/` (**SPEC.md** — the durable phase-32.2 sub-phase SPEC; the PLAN.md follows at the 32.2 PLAN session, the PROGRESS.md at the 32.2 IMPL session [Task 1 creates it], per the 29.1/29.2/29.3 per-sub-phase-dir precedent). The phase-32.1 dir `docs/envoy-go/phases/32.1-network-filter-redis-upstream-pool-seam-and-codec/` (**SPEC.md + PLAN.md + PROGRESS.md** — the durable phase-32.1 IMPL record). The parent `docs/envoy-go/phases/32-network-filter-redis-proxy/` (**BRAINSTORM.md + SPEC.md + README.md** — the durable phase-32 parent-SPEC record). The phase-31 directory `docs/envoy-go/phases/31-network-filter-kafka-broker/` (BRAINSTORM.md + SPEC.md + README.md + PLAN.md + PROGRESS.md — the durable phase-31 IMPL record). The phase-30 + phase-29 directories stay the durable phase-30/29 record.
- **lifecycle-state:** `phase 37 BRAINSTORM done (lifecycle-state 0 → 1) — the Load-balancing family STAYS OPEN; maglev IN-PROGRESS at phase 37 (4 candidates remain after 37: subset LB, locality-weighted LB, priority load balancing, panic thresholds). Next: the phase-37 SPEC (superpowers:writing-plans scoped to SPEC authoring) — execute the §10 D-M1..D-M6 empirical pins live against the contrib reference Envoy (reference_docker_probe_bridge_network), anchor the ADR-0238 §Context draft, confirm the single-flat-row ADR-0045 disposition`. The phase-37 BRAINSTORM is COMPLETE: the durable record is `docs/envoy-go/phases/37-load-balancer-maglev/BRAINSTORM.md` (the design charter + the §10 SPEC-time empirical-pin obligations) + README.md (the phase overview).
- **next-skill:** the controller's **stage-close** (squash-merge the phase-37 BRAINSTORM worktree branch to master + push, per feedback_subagents_no_push + feedback_push_to_origin); then the next cold-start authors the phase-37 SPEC via the next-prompt roll-forward (`superpowers:writing-plans` scoped to SPEC authoring — the kafka-31/thrift-33/least_request-34/random-35/ring_hash-36 precedent).
- **last-commit:** the phase-37 BRAINSTORM registration bundle (lifecycle-state 0 → 1) — `docs/envoy-go/phases/37-load-balancer-maglev/BRAINSTORM.md` + README.md (NEW) + the ROADMAP row-37 registration (`in-progress`, flat family row, NO split) + the family-roster `ring_hash DONE` / `maglev IN-PROGRESS` advance + the STATE advance (this file) + the next-prompt roll-forward routing the next cold-start to the phase-37 SPEC. NO code change, NO ADR consumed (DECISIONS tail STAYS ADR-0237 at the BRAINSTORM; next-free ADR-0238 anchored at the SPEC, body at the IMPL per ADR-0044). ALL counts UNCHANGED.
- **last-updated:** 2026-06-13 (phase-37 BRAINSTORM done)
- **next-free ADR:** `ADR-0238` (DECISIONS.md tail STAYS **ADR-0237** at this phase-37 BRAINSTORM — no ADR consumed; ADR-0238 [the maglev policy] §Context anchored at the phase-37 SPEC, the full body lands at the phase-37 IMPL per ADR-0044).

---

## Project counts at this commit

- **active differential fixtures:** **64** (tail `0062-lb-ring-hash-http`). Canonical recipe `ls -d test/fixtures/[0-9]* | wc -l` = 64. Advanced at the phase-36.2 IMPL (+1 — `0062-lb-ring-hash-http`, the HTTP route header-hash NAT-transparent TRUE cross-side affinity+spread differential; the SECOND ring_hash producer plane after `0061`'s tcp source_ip). UNCHANGED at the phase-37 BRAINSTORM (+0; the `0063-lb-maglev` fixture lands at the phase-37 IMPL → 65).
- **fuzzers:** **42** (canonical recipe `grep -rh "^func Fuzz" $(find ./internal -name fuzz_test.go) | wc -l`). UNCHANGED at the phase-36.2 IMPL (+0 — the HTTP `hash_policy` producer decodes no untrusted wire bytes; the hash key derives from a parsed header value / source IP, not a wire frame; a key-fold property test is unit-level). UNCHANGED at the phase-37 BRAINSTORM (+0 anticipated — maglev decodes no wire bytes; a table-build/lookup property test is unit-level).
- **stat surface:** **1119** (BEHAVIOR_CONTRACT doc count). UNCHANGED at the phase-36.2 IMPL (+0 — ZERO new stat names; the HTTP `hash_policy` plane reuses the 3 `ring_hash_lb.*` gauges [36.1] + the existing per-cluster `cluster.<name>.*` roster; the FIRST LB producer reusing the prior plane's stat surface entirely; the DOC count is the source-of-truth, no programmatic golden). UNCHANGED at the phase-37 BRAINSTORM (+0; maglev is anticipated to add +2 `maglev_lb.*` gauges [`min_entries_per_host`/`max_entries_per_host`, no `size` gauge] at the IMPL → ~1121, D-M4 pins the exact surface).
- **BackendKind tail:** **33** (`TCPThriftResponder` — seq_id-agnostic canned framed-binary REPLY). UNCHANGED at the phase-36.2 IMPL (+0 — `0062` reuses the existing HTTP backends; a producer phase exercises WHICH key picks the backend, not a new backend). UNCHANGED at the phase-37 BRAINSTORM (+0 — `0063` reuses the `0062` HTTP backends; a policy-swap phase exercises WHICH key picks the backend, not a new backend).
- **DECISIONS.md tail:** **ADR-0237** (next-free **ADR-0238**) — **ADR-0237** (the HTTP route `hash_policy` producer plane — the SECOND data plane feeding the ADR-0236 `ring_hash` key; the `header` + `connection_properties.source_ip` specifiers, the `rotl64(prev,1)^new` fold, the additive `cluster.HashHeaderValues`; PRODUCER-ONLY — NO seam/manager/stat change) landed at the phase-36.2 IMPL, §Context promoted verbatim from SPEC §13, §Decision + §Consequences IN-PLACE, PROPOSED → ACCEPTED per ADR-0044. The prior tail ADR-0236 (the `ring_hash` policy) + ADR-0235 (the seam extension) landed at the phase-36.1 IMPL. STAYS ADR-0237 at the phase-37 BRAINSTORM (no ADR consumed; ADR-0238 [the maglev policy] §Context anchored at the phase-37 SPEC, the body at the phase-37 IMPL per ADR-0044).
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227, superseding ADR-0008).
- **conformance:** h2spec 53/53; proxy-wasm all 10 families PASS — RE-RAN GREEN at the phase-36.2 IMPL (NOT zero-touch like 36.1: 36.2 touches the H1/H2 router path [`applyHashKey` stuffs a ctx key before the dial], but the wire path is byte-identical when no `hash_policy` is configured [every conformance config]; the full 64-dir differential is the real guard).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
