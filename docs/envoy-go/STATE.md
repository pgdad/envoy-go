# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `phase 31 (network-filter-kafka-broker) IMPL done — phase 31 CLOSED` (2026-06-08, this commit) — **the phase-31 `kafka_broker` IMPL COMPLETE; the FIFTH §9 Network-filters-family row LANDED.** All 17 PLAN tasks landed: the new `internal/filter/network/kafkabroker/` package — a passive both-direction Kafka HEADER-ONLY observability sniffer at FULL per-API-key request/response counter parity (the 9th built-in network filter; consumer #3 of the ADR-0221 WriteFilter seam; the project's FIRST `github.com/envoyproxy/go-control-plane/contrib v1.32.4` consumer). Decode envelope: the request header (`api_key` INT16 / `api_version` INT16 / `correlation_id` INT32 / `client_id` NULLABLE_STRING / tagged-fields) decoded on the read goroutine in `OnData`; the response header (`correlation_id` INT32 ONLY, count-and-frame-skip) decoded on the write goroutine in `OnWrite`; the api_key recovered on the response side via a `correlation_id → (api_key, api_version)` per-connection map under an ADR-0223 mutex; the static 86-key Kafka 3.9.1 `api_key → (root, maxVersion)` table + the `flexibleVersions(api_key)` predicate (ApiVersions(18)-response special-case, immaterial to the count path); the EAGER 176-counter roster (86 `request.<root>_request` + 86 `response.<root>_response` + 4 fixed under `kafka.<stat_prefix>.`; the 86 response-duration histograms DEFERRED per ADR-0060); the `kafka.` INLINE Prometheus arm (EMPTY labels, NO hoist — AMEND-K2 REFUTED the BRAINSTORM label-hoist hypothesis); the `/contrib v1.32.4` dep (held by the bootstrap blank-import); the `TCPKafkaResponder` BackendKind 31; the `0053-kafka-requests` cross-side `StatsAsserter` + `0054-kafka-boot-reject` fixtures; the 40th fuzzer `FuzzKafkaDecode`. ADR-0228 §Decision/§Consequences bodies LANDED IN-PLACE (ADR-0044; DECISIONS tail STAYS ADR-0228, next-free → ADR-0229 — NO new number). Six gates GREEN: `go build ./...` clean; `go vet ./...` clean; `golangci-lint run` clean; `go test ./... -race -short` 81 ok pkgs / 0 FAIL; differential **56/56** byte-exact (`-count=1`, incl. 54 back-compat); conformance h2spec 53/53 + proxy-wasm 10/10 (image-independent + UNAFFECTED — phase 31 touches no HTTP/h2/proxy-wasm path). Counts advanced: stat surface **360 → 536** (+176), fixtures **54 → 56**, fuzzers **39 → 40**, BackendKind tail **30 → 31**. A NEW `*.failure` cross-side asymmetry (ref=2 / subj=1 broken-stream abandon-at-close, pinned to exact per-side values in `0053`; analogous to `reference_close_direction_framework_gap`) recorded as a BEHAVIOR_CONTRACT departure. The four active features parse-accepted-behavior-deferred (the broker-address-rewrite write-mutation = a future framework-surgery sub-phase); ZERO framework-seam extension (never-halting, byte-identical R1). ROADMAP row 31 `in-progress → done` (a flat §9 row, NO parent rollup); after phase 31 the §9 candidate count drops 3 → 2 ({redis, thrift} remain). The pre-authorized 31.1/31.2 split STANDS-UNCONSUMED. **Earlier (the phase-31 PLAN, preserved):** the phase-31 `kafka_broker` PLAN COMPLETE; `PLAN.md` authored from the SPEC as a 17-task TDD decomposition; the ADR-0045 split-gate RE-CHECKED at PLAN time (17 tasks / ~805–1200 production LoC — both axes UNDER the gate; the pre-authorized 31.1/31.2 split STAYS UNCONSUMED); plan-document-reviewer APPROVED (first iteration). The PLAN surfaces three PLAN-time design decisions on top of AMEND-K1..K7: D-PLAN-1 (the `*.unknown`-keyed-on-(api_key, api_version) routing needs a per-api-key `maxVersion` column added to the Appendix-B table; `api_version > maxVersion(key)` → unknown; minVersion treated as 0 — the deprecated-low-version → unknown sub-case a recorded coverage boundary); D-PLAN-2 (decode-exception posture is per-exception `*.failure` + decoder RESET + Continue, NOT mongo-style lifetime sniffing-off — matching upstream `broker/filter.cc` `decoder->reset()`); D-PLAN-3 (the request decoder reads+VALIDATES api_key/api_version/correlation_id/client_id/tagged-fields so a malformed `client_id` length → `request.failure`, registering correlation BEFORE the client_id attempt; the response decoder is count-and-frame-skip on `correlation_id` ONLY, making the AMEND-K5 ApiVersions(18) response special-case immaterial to the count path — implemented+table-tested for completeness). D-P3 (boot-reject fixture arms) RESOLVED: `0054` carries the `stat_prefix`-required arm only; the api_keys-range/nested-rule arms stay unit-test-only. D-P6 (tables hand-transcribed, byte-stable golden tests; no code-gen). Counts UNCHANGED at this PLAN (docs-only); they advance at the phase-31 IMPL. **Earlier (the phase-31 SPEC, preserved):** the phase-31 `kafka_broker` SPEC COMPLETE; the D31-1..D31-8 empirical pins EXECUTED LIVE; the ADR-0228 §Context anchored; spec-document-reviewer APPROVED (first iteration).** The SPEC (`docs/envoy-go/phases/31-network-filter-kafka-broker/SPEC.md` + README) is a SINGLE flat §9 row (directly executable; SPEC → PLAN → IMPL — NOT a parent pre-split), recording seven empirical amendments (AMEND-K1..K7) where the live findings refined/refuted the BRAINSTORM hypotheses: **K1** the per-key stat segment is the `_request`/`_response`-SUFFIXED message-name snake-case (`request.produce_request`, NOT `request.produce`); **K2** the Prometheus exposition is FULLY INLINED with EMPTY labels (`envoy_kafka_<sp>_<rest>{}`) — REFUTES the BRAINSTORM's label-hoist hypothesis (the `kafka.` `name.go` arm is the zookeeper INLINE shape, not the mongo label-hoist shape); **K3** the roster is EAGER + **176 counters** (86 request + 86 response per-key + 4 fixed; api_keys 71/72 telemetry-excluded → 86 per direction; the 86 response-duration histograms DEFERRED per ADR-0060) → stat surface **360 → 536**; **K4** `*.unknown` keyed on (api_key, api_version) (unknown VERSION of a known key also → unknown), `*.failure` partly header-reproducible (malformed header; response unregistered-correlation_id → `response.failure` earns a `0053` differential arm); **K5** header-version is a `flexibleVersions(api_key)` predicate (tagged fields iff `api_version ∈ flexibleVersions`) with the ApiVersions(18) response-header-suppression special-case (Kafka source 3.9.1); **K6** the only genuinely-new go.mod dep is `/contrib v1.32.4` (kafka_broker/v3 self-contained — cncf/xds/protoc-gen-validate/vtprotobuf/`/envoy` already in the closure), @type carries `extensions.`; **K7** the `IsValidName` charset guard is satisfied BY CONSTRUCTION (table-bounded api-key names; only `stat_prefix` needs the config-boundary guard). D31-6 confirmed NO close-direction counters (framework-zero-touch). The api-key roster (86 entries) + the flexibleVersions table are pinned in SPEC Appendices B/C. Counts advance at the phase-31 IMPL: fixtures 54 → 56 (`0053-kafka-requests` cross-side + `0054-kafka-boot-reject`); fuzzers 39 → 40 (`FuzzKafkaDecode`); stat surface 360 → 536 (+176 eager); BackendKind tail 30 → 31 (the correlation-id-echoing responder). **The phase-31 BRAINSTORM (earlier, preserved):** The BRAINSTORM (`docs/envoy-go/phases/31-network-filter-kafka-broker/BRAINSTORM.md`, 12 §§ mirroring the phase-29 §9-sniffer template) settled via a multi-question dialogue: **Q0** subject confirmation = `kafka_broker` (`envoy.filters.network.kafka_broker`), already SELECTED at the phase-30 BRAINSTORM — the new `internal/filter/network/kafkabroker/` package (the 9th network-filter built-in; consumer #3 of the ADR-0221 WriteFilter seam; the project's FIRST `/contrib` consumer); **Q1** envelope = HEADER-ONLY decode (request/response headers generic across all API keys — `api_key`/`api_version`/`correlation_id`/`client_id`/tagged-fields + the response `correlation_id`; 4-byte length prefix both) + FULL per-API-key `request.<key>`/`response.<key>` counter parity in BOTH directions (recovering the api_key on the response side via a `correlation_id`→`api_key` per-connection map under an ADR-0223-style mutex — the mongo-29.2 correlation precedent) + `request.unknown`/`request.failure` + `response.unknown`/`response.failure` under `kafka.<stat_prefix>.` (a new `kafka.` Prometheus tag-extractor arm + the `stats.IsValidName` charset guard); the four active features (`force_response_rewrite`/`id_based_broker_address_rewrite_spec`/`api_keys_allowed`/`api_keys_denied`) PARSE-ACCEPTED-behavior-DEFERRED (the mongo `header_delay` precedent — ⚠️ the broker-address-rewrite would MUTATE the write buffer = a NEW framework capability never built, flagged as a future surgery sub-phase); `request.failure`/`response.failure` a UNIT-TESTED coverage boundary (fixtures well-formed); histograms deferred (ADR-0060); **Q-split** = a SINGLE unsplit §9 row (header-only kafka is much LEANER than mongo — NO BSON parser, trivial response header, NO fault-delay/async-halt seam; anticipated ~600–1100 LoC / ~12–18 tasks, under the ADR-0045 gate) with a PRE-AUTHORIZED 31.1-request / 31.2-response split axis as the escape valve (the mongo-29.1 stands-unconsumed precedent); the phase-30 forward-pointer's optional `31.x` active-features sub-phase DROPS. Self-answered per §9 precedent: ZERO framework-seam touch (pure consumer of the as-built §9 machinery — the post-handoff read seam needs the filter to implement `WriteFilter` even where observation-only per `reference_network_chain_terminal_handoff_ends_ondata`; ZERO-touch on the ADR-0226 async halt/resume seam — never-halting, byte-identical R1); 1 NEW package `internal/filter/network/kafkabroker/`; 1 new go.mod dep `github.com/envoyproxy/go-control-plane/contrib v1.32.4` (the FIRST `/contrib` dep, added with its first consumer — an unused dep cannot survive `go mod tidy`); TypeURL via `proto.MessageName` on `kafka_brokerv3.KafkaBroker` (D31-1); cross-side `StatsAsserter` (UNBLOCKED by phase 30's contrib pin) over hermetic synthesized frames (`0053-kafka-requests` cross-side + possibly `0054-kafka-boot-reject`); a NEW BackendKind (a synthesized correlation-id-echoing TCP Kafka responder, anticipated value 31); the 40th fuzzer `FuzzKafkaDecode`; 1 ADR — **ADR-0228** (next-free; DECISIONS tail ADR-0227). The static `(api_key, api_version) → header-version` table + the api-key name roster + the exact stat roster + eager-vs-dynamic creation are EMPIRICAL pins at SPEC (D31-1..D31-8, live-probed against the contrib reference Envoy per `reference_docker_probe_bridge_network`). ROADMAP row 31 registered `in-progress` (flat §9 row, no sub-phases; depends-on 30) + the §9 candidate-roster paragraph refined (kafka_broker IN-PROGRESS/landing; {redis, thrift} remain after phase 31, both terminal-proxy surfaces — without touching the §9 heading text per ADR-0106 (c)). Counts were UNCHANGED at the BRAINSTORM (fixtures 54 / fuzzers 39 / stat surface 360 / BackendKind tail 30); at THIS SPEC the DECISIONS.md tail advances to **ADR-0228** (the §Context anchored per ADR-0044; next-free ADR-0229); the surface/fixture/fuzzer/BackendKind counts still advance at the phase-31 IMPL. **Earlier (phase 30 IMPL / phase 30 CLOSED, preserved):** **the phase-30 IMPL COMPLETE; the project's FIRST reference-image pin-refresh since ADR-0008 LANDED at ZERO production LoC.** The SHA-pinned differential reference image flipped to **`envoyproxy/envoy:contrib-v1.37.2`** @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (the contrib variant of the SAME upstream version — a behavioral SUPERSET; ADR-0227 supersedes ADR-0008), all 54 fixtures re-baselined byte-identical-PASS (ZERO divergence), conformance re-ran green (h2spec 53/53, proxy-wasm 10/10), the six-gate GREEN LIVE on the contrib pin, ROADMAP row 30 `done`. The full prior detail (the 6-task T1→T6 spine, the AMEND-D30 image-identity correction, the PLAN/SPEC/BRAINSTORM history, and the entire phase-29 mongo_proxy record) is the durable on-disk record in the phase-30 + phase-29 directories.
- **phase-directory:** `docs/envoy-go/phases/31-network-filter-kafka-broker/` (**BRAINSTORM.md + SPEC.md + README.md + PLAN.md** — the durable phase-31 PLAN record landed at this commit; PROGRESS.md follows at the phase-31 IMPL session). The phase-30 directory `docs/envoy-go/phases/30-pin-refresh-envoy-contrib/` (**BRAINSTORM.md + SPEC.md + README.md + PLAN.md + PROGRESS.md** — the durable phase-30 IMPL record). The phase-29 directories stay the durable phase-29 record: `docs/envoy-go/phases/29.3-network-filter-mongo-fault-delay-and-access-log/` (SPEC.md + README.md + PLAN.md + **PROGRESS.md** — the durable 29.3 IMPL record). The 29.2 directory (`29.2-network-filter-mongo-responses-and-correlation/` — SPEC.md + PLAN.md + README.md + PROGRESS.md) + the 29.1 directory (`29.1-network-filter-mongo-wire-and-requests/` — SPEC.md + PLAN.md + PROGRESS.md) + the parent directory (`29-network-filter-mongo-proxy/` — BRAINSTORM.md + the parent SPEC) stay the durable phase-29 record.
- **lifecycle-state:** `phase 31 IMPL done — phase 31 CLOSED — SKILL_ROUTING → the §9-next-row BRAINSTORM (superpowers:brainstorming)` — the phase-31 `kafka_broker` IMPL is COMPLETE (all 17 PLAN tasks landed; six gates GREEN; ROADMAP row 31 `in-progress → done`, a flat §9 row with NO parent rollup per ADR-0106). Per the project's IMPL→next-phase precedent, the next step is a fresh **BRAINSTORM** for the next §9 Network-filters-family row (`superpowers:brainstorming`, per `feedback_execution_style`) — candidates {redis, thrift}, both terminal-proxy surfaces (command routing + upstream pooling) needing routing/pooling seams of their own, each its own brainstorm-time risk assessment; they carry forward unbrainstormed. Phases 29 + 30 + 31 are CLOSED. ROADMAP: row 31 `done`; row 30 `done`; parent row 29 `done`; sub-rows 29.1/29.2/29.3 `done`; rows 26/27/28 `done`. The §9 Network-filters family stays OPEN (2 candidates remain).
- **next-skill:** **`superpowers:brainstorming`** — scoped to the **next §9 Network-filters-family-row BRAINSTORM** (`feedback_execution_style`). Phase 31 (`kafka_broker`) is CLOSED; the §9 family stays OPEN with **2 candidates {redis, thrift}** (both terminal-proxy surfaces — command routing + upstream pooling — needing routing/pooling seams of their own; each its own brainstorm-time risk assessment; they carry forward unbrainstormed). The next session opens a fresh BRAINSTORM (subject selection between redis/thrift + envelope + Q-split + the empirical-pin obligations the SPEC will execute) in a git worktree (`feedback_git_worktrees`). Read first: `docs/envoy-go/ROADMAP.md` (the §9 Network-filters family roster + the row-26..31 history) + `docs/envoy-go/STATE.md` (this file) + `docs/envoy-go/BEHAVIOR_CONTRACT.md` `### envoy.filters.network.*` subsections (echo/sni_cluster/zookeeper_proxy/mongo_proxy/kafka_broker — the §9-row precedents) + `docs/envoy-go/DECISIONS.md` ADR-0106 (§9 family flat-row discipline) + ADR-0045 (split-gate) + ADR-0221 (WriteFilter seam) + ADR-0226 (async halt/resume seam) + the phase-29/31 BRAINSTORM/SPEC/PLAN (the §9-row lifecycle precedents) + project memories (`reference_network_filter_typeurl_extensions`, `reference_wire_format_both_sides_see_same_bytes`, `reference_close_direction_framework_gap`, `reference_docker_probe_bridge_network`, `reference_dynamic_stat_name_charset_guard`, `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`). NOTE: redis/thrift are TERMINAL-proxy surfaces (unlike the prior §9 OBSERVATIONAL sniffers zookeeper/mongo/kafka) — the brainstorm must assess whether they need NEW framework capability (command routing / upstream connection pooling) the as-built §9 framework does not provide. The controller squash-merges + pushes at stage-close (`feedback_push_to_origin`).
- **last-commit:** the phase-31 IMPL Task 17 completion-bundle commit on the `phase-31-network-filter-kafka-broker-impl` branch (`phase 31 IMPL Task 17: completion bundle — BEHAVIOR_CONTRACT 31 (360->536), ADR-0228 body, STATE/ROADMAP row 31 done, six gates green` — the 17th of the 17 IMPL tasks; the controller squash-merges the phase-31 IMPL branch to master + pushes + fills the live SHA). The IMPL spine commits (LOCAL-ONLY, squashed at merge): Task 1 baselines/anchors gate → Task 2 `/contrib v1.32.4` dep + blank-import + skeleton/TypeURL → Task 3 config parse + PGV arms + IsValidName guard → Tasks 4–5 the api-key table + flexibleVersions predicate → Task 6 the EAGER 176-counter roster → Tasks 7–8 the request + response decoders + correlation map + `-race` → Task 9 the ReadFilter/WriteFilter glue → Task 10 the 9th built-in → Task 11 the `kafka.` INLINE prom arm → Task 12 the `TCPKafkaResponder` BackendKind 31 → Tasks 13–14 the `0053`/`0054` fixtures → Task 15 the 40th fuzzer → Task 16 the full differential re-verify (56/56) → Task 17 this completion bundle. Substantive predecessor on master: the phase-31 PLAN squash `12f7cac` (`phase 31 PLAN: kafka_broker network filter — 17-task TDD decomposition (header-only decode + 176-counter parity + /contrib v1.32.4); ADR-0045 split-gate UNDER (31.1/31.2 split unconsumed); D-PLAN-1/2/3; plan-document-reviewer APPROVED first iteration` — squash-merged from `phase-31-network-filter-kafka-broker-plan` + pushed by the controller; the docs-only SHA-fill follow-up `596fccd` trails it). Substantive predecessor on master: the phase-31 SPEC squash `6104036` (`phase 31 SPEC: kafka_broker — header-only decode + FULL per-API-key request/response counter parity; D31-1..D31-8 empirical pins executed live (AMEND-K1..K7); EAGER 176-counter roster (stat surface 360→536); INLINE prom arm (no label hoist); /contrib v1.32.4 dep; ADR-0228 §Context; spec-document-reviewer APPROVED` — the docs-only SHA-fill follow-up `c476db1` trails it). Substantive predecessor on master: the phase-31 BRAINSTORM squash `78d5f36` (`phase 31 BRAINSTORM: kafka_broker … single unsplit row (pre-authorized 31.1/31.2 split axis); ADR-0228 anticipated; ROADMAP row 31 registered in-progress` — the docs-only SHA-fill follow-ups `5222610`/`2f387b6` trail it). Substantive predecessor on master: the phase-30 IMPL squash `2d24f34` (a docs-only SHA-fill follow-up trails it) — the phase-30 IMPL squash (the completion-bundle commit `phase 30 T6: completion bundle — six-gate green on contrib pin; ROADMAP row 30 done; next-prompt → phase-31 kafka_broker BRAINSTORM` + the T2 pin-flip `f773079` + the T5 ADR-0227/contract `b02147d`; squash-merged from `phase-30-pin-refresh-envoy-contrib-impl`; pushed to origin/master by the controller). Substantive predecessor on master: the phase-30 PLAN squash `3420ed0` (the docs-only SHA-fill follow-up `e3994f1` trails it) — the phase-30 PLAN squash (`phase 30 PLAN: 6-task spine (T1 baselines → T2 flip ENVOY_TARGET.md → T3 54-fixture re-baseline → T4 conformance → T5 ADR-0227 body + BEHAVIOR_CONTRACT pins → T6 completion bundle); plan-document-reviewer APPROVED first iteration`; squash-merged from `phase-30-pin-refresh-envoy-contrib-plan`; pushed to origin/master. Substantive predecessor on master: the phase-30 SPEC squash `3a0207b` (`phase 30 SPEC: reference-image pin-refresh to envoyproxy/envoy:contrib-v1.37.2 (D30-1 AMEND-D30 corrected the image identity); 54-fixture re-baseline byte-identical-PASS; ADR-0227 §Context (supersedes ADR-0008)`; the docs-only SHA-fill follow-ups `9dbb736`/`8bc919c`/`7869b07` trail it). Substantive predecessor on master: the phase-30 BRAINSTORM squash `e2d566a` (`phase 30 BRAINSTORM: reference-image pin-refresh to envoy-contrib (infra prerequisite for the kafka_broker §9 row); ROADMAP row 30 registered in-progress`; the docs-only SHA-fill follow-up `b0f6ca3`/`b448178` trails it). Substantive predecessor on master: the phase-29.3 IMPL squash `d671263` (`phase 29.3 IMPL: async halt/resume seam + mongo fault-delay + access log + cx_drain_close + close-direction (D-P4 CLOSED); parent-row-29 ROLLUP` — the framework's FIFTH structural extension landed seam-first + the mongo consumers; squash-merged from `phase-29.3-network-filter-mongo-fault-delay-and-access-log-impl`; pushed to origin/master). Substantive predecessor on master: the 29.3-PLAN squash `e8805e4`; before that the 29.3-SPEC squash `0415153`; the 29.2-IMPL squash `620a3d0`; the 29.2-PLAN squash `0783776`; the 29.2-SPEC squash `13907f1`; the 29.1-IMPL squash `5ef5215`; the 29.1-PLAN squash `89c194a`; the 29.1-SPEC squash `7574219`.
- **last-updated:** 2026-06-08
- **next-free ADR:** `ADR-0229` (DECISIONS.md tail is **ADR-0228** — the `kafka_broker` filter; its §Context was anchored at the phase-31 SPEC per ADR-0044 and its §Decision/§Consequences bodies LANDED IN-PLACE at the phase-31 IMPL — NO new ADR number consumed). ADR-0209 escape-valve reserve carried from the §9 family STANDS-UNCONSUMED; the pre-authorized 31.1/31.2 split axis STANDS-UNCONSUMED.

---

## Project counts at this commit

- **active differential fixtures:** **56** (tail `0054-kafka-boot-reject`; +`0053-kafka-requests` cross-side `StatsAsserter` + `0054-kafka-boot-reject` at the phase-31 IMPL). Canonical recipe `ls -d test/fixtures/[0-9]* | wc -l` = 56.
- **fuzzers:** **40** (canonical recipe `grep -rh "^func Fuzz" $(find ./internal -name fuzz_test.go) | wc -l`; +`FuzzKafkaDecode` at the phase-31 IMPL — both decode directions: no-panic + no-mutation + bounded-buffer).
- **stat surface:** **536** (BEHAVIOR_CONTRACT doc count; +176 `kafka_broker` eager counters — 86 `request.<root>_request` + 86 `response.<root>_response` + 4 fixed — created eagerly at config-load under `kafka.<stat_prefix>.` per the phase-31 IMPL / AMEND-K3. The 86 response-duration histograms are DEFERRED per ADR-0060 and NOT counted, so the delta is +176 not +262).
- **BackendKind tail:** **31** (`TCPKafkaResponder`; the correlation-id-echoing Kafka responder added at the phase-31 IMPL).
- **DECISIONS.md tail:** **ADR-0228** (next-free **ADR-0229**) — the phase-31 `kafka_broker` filter; its §Context was anchored at the phase-31 SPEC per ADR-0044 and its §Decision/§Consequences bodies LANDED IN-PLACE at the phase-31 IMPL (NO new ADR number consumed; DECISIONS.md tail STAYS ADR-0228, next-free → ADR-0229).
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227, superseding ADR-0008; the contrib variant of upstream v1.37.2 — flipped at the phase-30 IMPL T2).
- **conformance:** h2spec 53/53; proxy-wasm all 10 families PASS (re-run LIVE at the phase-31 IMPL six-gate, 2026-06-08 — image-independent + asserted-UNAFFECTED, phase 31 touches no HTTP/h2/proxy-wasm path).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
