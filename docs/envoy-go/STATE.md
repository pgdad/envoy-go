# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `phase 36.1 (load-balancer-ring-hash) PLAN done` (2026-06-12, this commit) — the phase-36 {ring_hash} SPEC is COMPLETE (`docs/envoy-go/phases/36-load-balancer-ring-hash/SPEC.md` + README.md; the D-RH1..D-RH8 empirical pins executed IN-SESSION via parallel-subagent fan-out against the contrib reference Envoy + go-control-plane v1.32.4 + upstream v1.37.2 source; spec-document-reviewer GREEN). **SPEC findings that REFINE the BRAINSTORM:** (1) **NOT zero stat delta** — RING_HASH emits 3 new `cluster.<name>.ring_hash_lb.{size,min_hashes_per_host,max_hashes_per_host}` GAUGES → surface 1116 → **1119** at the 36.1 IMPL (MIRRORED for parity; cross-side-exact — keyed on ring-config + host count only; AMEND-RH4); (2) the PGV has **NO min≤max rule** (runtime-only) → the hand-rolled gate mirrors TWO layers (PGV `value must be less than or equal to 8388608` + runtime `ring hash: minimum_ring_size (N) > maximum_ring_size (M)`; AMEND-RH5); (3) cross-side host IDENTITY is **INFEASIBLE** in the harness (Docker source-IP NAT + per-side ring layouts over different endpoint address strings) → the differential asserts PER-SIDE deterministic affinity + spread + cross-side byte-equivalence + cross-equal stats (AMEND-RH8); (4) the seam is `Pick(hashKey uint64, hasHash bool)` + an exported additive `cluster.WithHashKey` ctx-carry, the exported `Cluster` surface BYTE-STABLE (AMEND-RH7); (5) xxHash64 is hand-rolled (~45 LoC; absent from go.mod — NOT a new dep). **The FINAL ADR-0045 decision: CONSUME the 36.1/36.2 by-plane split** (~490 prod LoC / ~16–20 tasks — under the gate, so discretionary; consumed on subsystem-coupling + fixture-asymmetry grounds): **36.1** = seam extension (ADR-0235) + `ringHashLB` + xxHash64 + manager + the 3 gauges + the tcp_proxy `source_ip` plane + `0061`; **36.2** = the HTTP route `hash_policy` plane (header + conn-props source_ip) + `0062` — each its own PLAN→IMPL. The BRAINSTORM was DESIGN-ONLY (DESIGN-ONLY; `docs/envoy-go/phases/36-load-balancer-ring-hash/BRAINSTORM.md`, the kafka-31/thrift-33/least_request-34/random-35 12-§ family-template mirrored). The **THIRD Load-balancing-family row** — the family STAYS OPEN; lands `Cluster.LbPolicy RING_HASH` (`envoy.config.cluster.v3`, enum value **2** — VERIFIED in the EXISTING core `/envoy v1.32.4` module cache WITH a `Cluster.RingHashLbConfig` message [`minimum_ring_size`/`maximum_ring_size` `UInt64Value` defaults 1024/8388608 + `hash_function` enum XX_HASH·MURMUR_HASH_2]; `type.v3.HashPolicy` [tcp_proxy element, field 11] + `RouteAction.HashPolicy` [HTTP element] also present; ZERO new go.mod dep), the project's **fourth LB policy** (after roundRobin [02] + leastRequest [34] + randomLB [35]) and its **first consistent-hashing policy**. The dialogue settled: **Q0 subject** = `ring_hash` (the natural primary-`LbPolicy`-enum completion — RING_HASH/MAGLEV remain among the primary enum values; subset/locality/priority/panic are orthogonal MODIFIERS, several gated by the absent health-checking boundary; picked as the policy that exercises the seam-EXTENSION ADR-0232 §Consequences explicitly anticipated); 5 candidates remain after 36 {maglev, subset LB, locality-weighted LB, priority load balancing, panic thresholds} — maglev the cheap follow-on reusing this seam. **Q1 envelope** = BOTH PLANES: `RING_HASH` accepted by `cluster.Manager` (`RingHashLbConfig` parse + HAND-ROLLED PGV gate + the reject TEXT extends `… ROUND_ROBIN, LEAST_REQUEST, RANDOM` → `…, RING_HASH` — a byte-stable-reject touchpoint, three-site blast radius at SPEC D-RH5 [`manager.go` + `manager_test.go` doubly-hit retarget RING_HASH → MAGLEV + `BEHAVIOR_CONTRACT.md`]) + a `ringHashLB` (a Ketama ring built once from the endpoint set, `Pick(hashKey)` binary-searches for the first ring position ≥ key, wrap; returns `noopRelease` — no per-pick state) + the hash key supplied by BOTH tcp_proxy (`hash_policy: source_ip`) AND the HTTP router (route-level `RouteAction.hash_policy`, header + connection-properties source-ip subset; cookie/query/filter-state DEFERRED); MAGLEV / the `load_balancing_policy` extension point / weighted-ring richness / locality / panic DEFERRED. **Q-seam** = EXTEND the ADR-0232 seam via a ctx-carried hash key — the unexported `loadBalancer.Pick` widens to receive an optional request-derived hash key (the PICK-INPUT half ADR-0232 §Consequences anticipated: "the hash-keyed RING_HASH/MAGLEV will additionally require widening `Pick` to receive a request-derived hash key … the next LB phase that lands a hash policy must plan for it"); the consumer computes it + carries it in `context.Context`, `cluster.go` extracts (`withHashKey`/`hashKeyFrom`) + threads it; the **exported `Cluster` surface stays BYTE-STABLE** (`Dial`/`AcquireH1` already take `ctx` — the OPTION-C discipline preserved, only the two hash-producing consumers [tcpproxy/router] churn; `PickEndpoint()` no-hash → random fallback); the 3 existing policies ignore the key behavior-neutrally (all 62 fixtures byte-identical); the RELEASE half is unused (ring_hash returns `noopRelease`). **Q-split** = a PRE-AUTHORIZED 36.1/36.2 by-plane split (36.1 seam-ext + ringHashLB + manager + tcp_proxy `source_ip` plane; 36.2 HTTP route `hash_policy` plane; ~330–630 prod LoC / ~16–22 tasks UNDER the ADR-0045 hard gate as a single row but couples two subsystems → the FINAL ADR-0045 re-check at SPEC/PLAN decides whether to CONSUME it [anticipated consumed; the phase-34 valve precedent]). Self-answered: the differential is the AFFINITY + SPREAD arm — `0061-lb-ring-hash` (tcp source-IP; 36.1) + `0062-lb-ring-hash-http` (HTTP header; 36.2) binding the client to multiple `127.0.0.x` source IPs (D-RH8) to assert same-key→same-backend (affinity, stable) + distinct-keys→different-backends (spread), PER SIDE [cross-side-exact host identity IFF D-RH2 proves byte-exact ring reproduction feasible; else per-side bands per `reference_differential_band_sigma_margin`] + a cross-side `StatsAsserter` prong; a possible ring-config boot-reject dir (D-RH1/D-RH5, a separate dir per the fixture-dispatch constraint); NO new BackendKind (tail STAYS 33; `TCPEcho` + existing HTTP backends reused) / anticipated NO new fuzzer (no wire decode; a ring-lookup property test is a unit-level candidate — fuzzers STAY 42) / anticipated ZERO stat-delta (surface STAYS 1116). **2 ADRs anticipated:** ADR-0235 (the LB hash-key seam extension — the PICK-INPUT half) + ADR-0236 (the ring_hash policy) — the phase-34 two-ADR (seam + policy) shape (contrast random's single-ADR reuse); §Context at SPEC, bodies at IMPL per ADR-0044; DECISIONS tail STAYS ADR-0234 at this brainstorm, next-free ADR-0235. The **D-RH1..D-RH8** empirical pins DEFER to the SPEC (live against `envoyproxy/envoy:contrib-v1.37.2` per `reference_docker_probe_bridge_network`). ROADMAP row 36 registers `in-progress` (flat family row + the pre-authorized 36.1/36.2 valve) at this BRAINSTORM commit; the Load-balancing family-heading gains a `ring_hash IN-PROGRESS` note. Counts UNCHANGED at this brainstorm (stat surface 1116 / fixtures 62 / fuzzers 42 / BackendKind tail 33 / DECISIONS tail ADR-0234); anticipated moves at the phase-36 IMPLs (the split CONSUMED — SPEC §3.0): fixtures 62 → 63 (36.1 `0061`) → 64 (36.2 `0062`; NO boot-reject dir — AMEND-RH5), DECISIONS tail → ADR-0235 + ADR-0236 (both at the 36.1 IMPL per ADR-0044), **stat surface 1116 → 1119 (36.1 — +3 `ring_hash_lb.*` gauges, the FIRST non-zero LB-stat delta; AMEND-RH4)**, fuzzers/BackendKind UNCHANGED (42/33). **Next → the 36.1 PLAN** (`superpowers:writing-plans` scoped to PLAN authoring — decompose SPEC §10's 36.1 spine [seam + `ringHashLB` + xxHash64 + manager + tcp_proxy `source_ip` plane + `0061`] into bite-sized TDD tasks; the ADR-0045 gate re-check per sub-phase; the 36.2 PLAN follows after the 36.1 IMPL). **Superseded (the phase-35 IMPL active-phase, preserved):** `phase 35 (load-balancer-random) done` (2026-06-11) — the phase-35 {random} IMPL is COMPLETE: the 8-task TDD plan (`docs/envoy-go/phases/35-load-balancer-random/PLAN.md` + the as-built record `…/PROGRESS.md`) executed end-to-end subagent-driven, landing the project's THIRD LB policy (after phase-02 ROUND_ROBIN + phase-34 LEAST_REQUEST) and the SECOND Load-balancing-family row. As built: the **ADR-0234** stateless uniform-pick `randomLB` (`internal/cluster/random.go`) — ONE draw `i := int(rng() % uint64(n))`, return `endpoints[i]`, NO active-count consult, NO tie-break (the v1.37.2 `RandomLoadBalancer::peekOrChoose` mirror; the contrast with least_request's P2C); the RNG is the REUSED package-private `newPCGRNG()` (mutex-guarded crypto-seeded `math/rand/v2` PCG, injectable via `newRandomWithRNG`); `randomLB` returns the shared `noopRelease` — the **FIRST reuse of the ADR-0232 LB acquire/release seam UNCHANGED** (ZERO seam churn, validating its "RANDOM drops in at zero cost" §Consequences claim; NO seam ADR — the thrift-33 single-ADR-on-reuse precedent). `cluster.Manager` accepts `RANDOM` via a BARE `case clusterv3.Cluster_RANDOM: newRandom(endpoints)` (NO config-parse arm — `Cluster.RandomLbConfig` does not exist; NO PGV gate) beside ROUND_ROBIN + LEAST_REQUEST; the reject text extends `… ROUND_ROBIN, LEAST_REQUEST` → `…, RANDOM` (the ONE byte-stable-reject change, three-site blast radius — `manager.go` + the doubly-hit-retargeted `TestManager_Error_UnsupportedLBPolicy` [`Cluster_RANDOM` → `Cluster_RING_HASH`] + BEHAVIOR_CONTRACT); a mismatched `lb_config` oneof under RANDOM is silently ignored (reference parity). Differential `0060-lb-random` — the REUSED `0059` hold-4/burst-60 skew stimulus over 3 reused `TCPEcho` backends with the assertion INVERTED to anti-skew (the SECOND band-based `AssertDistribution`: conservation `sum==64` + uniform-floor + uniform-ceiling, PER SIDE — RNG-driven, never cross-side-exact) + a cross-side `StatsAsserter` prong (`upstream_cx_total`/`membership_total`/quiesced-active cross-equal; `upstream_rq_total` per-side). **The 0060 anti-skew band was re-tuned at IMPL Task 6** from the SPEC's anticipated `{floor 12, ceiling 32}` (~2.5σ, observed to flake 2/20 on the natural multinomial tail) to **`{floor 6, ceiling 40}`** (~4–5σ, **60/60** flake-free — two `-count=20` batches + the 3 breaks; observed extremes 14–31) — WITHIN the PLAN's pinned PRINCIPLE {conservation + uniform-floor + uniform-ceiling}, an auditable empirical amendment to D-S35-3; the wider band STILL bites all three deliberate breaks. The **3 deliberate-break liveness proofs** (`-count=1`, go-test caching defeated): (i) `newRandom` → `newLeastRequest` (consult-the-counters) fails the uniform FLOOR; (ii) `rng() % n` → `0` (single-host pin) fails the uniform CEILING; (iii) a corrupt `StatsAsserter` want fails the stats prong — each applied → FAIL → `git restore` → diff-clean, never committed broken. **The deliberate firsts HELD (the SECOND zero-delta phase):** stat surface UNCHANGED at **1116** (zero-delta — RANDOM holds no registry-named state; live-confirmed empty `/stats` name-set diff RANDOM-vs-ROUND_ROBIN, 455 == 455), NO new fuzzer (fuzzers **42** — the SECOND no-fuzzer phase), NO new BackendKind (tail **33** — `0060` reuses `TCPEcho`); fixtures **61 → 62**. The **ADR-0234** §Decision/§Consequences body landed IN-PLACE per ADR-0044 (DECISIONS tail advances **ADR-0233 → ADR-0234**; next-free **ADR-0235**; ONE ADR — policy only, no seam ADR since reuse; ADR-0024 UNAMENDED). Six gates GREEN (build/vet/gofmt/lint; `-race -short`; the full **62-dir** differential byte-exact [61 prior unchanged through the new manager case + `0060` green]; h2spec 53/53 + proxy-wasm 10/10 unaffected; `go mod tidy` clean — the phase-35 PROGRESS.md §"Task 7" + §"Task 8"). **Next → the phase-35 phase-done routing** (the controller squash-merges the `phase-35-impl` worktree + pushes at stage-close; the Load-balancing family stays OPEN — **6** candidates remain after 35: ring_hash, maglev, subset LB, locality-weighted LB, priority load balancing, panic thresholds). **Superseded (the phase-35 PLAN active-phase, preserved):** `phase 35 (load-balancer-random) PLAN done` (2026-06-11) — the phase-35 {random} PLAN was COMPLETE (`docs/envoy-go/phases/35-load-balancer-random/PLAN.md`): the SPEC §10 spine decomposed into **8 bite-sized TDD tasks** (T1 baselines gate + PROGRESS.md / T2 the `randomLB` stateless type [`random.go`; one draw `rng()%n`; REUSES `newPCGRNG` verbatim; shared `noopRelease`; empty-set `errNoEndpoints` parity] / T3 manager acceptance [the bare `case clusterv3.Cluster_RANDOM` — NO config parse] + the NEW reject text `(…, RANDOM)` + the DOUBLY-hit retarget of `TestManager_Error_UnsupportedLBPolicy` [trigger `Cluster_RANDOM` → `Cluster_RING_HASH` + the substring extended to `ROUND_ROBIN, LEAST_REQUEST, RANDOM`] + the mismatched-oneof silent-ignore / T4 the in-process anti-skew integration test [RANDOM follows the draw, does NOT avoid a held endpoint — the contrapositive of least_request] + RANDOM boot smoke / T5 the `0060-lb-random` fixture [the REUSED `0059` hold-4/burst-60/drain stimulus with the assertion INVERTED — the SECOND band-based `AssertDistribution`: conservation sum==64 + per-host floor ≥12 + per-host ceiling ≤32, PER SIDE — + the cross/per-side `StatsAsserter` prong; reuses `TCPEcho` 0, NO new BackendKind] / T6 band tuning [≥20-run flake-free per side] + the 3 `-count=1` deliberate-break liveness proofs [consult-the-counters (floor leg, the canonical anti-skew break) / pin-to-endpoints[0] (floor+ceiling) / stats-prong] / T7 the full 62-dir differential re-verify + `-race -short` + h2spec 53/53 + proxy-wasm 10/10 unaffected / T8 the completion bundle [BEHAVIOR_CONTRACT random delta + the ADR-0234 full entry per ADR-0044 + STATE/ROADMAP row 35 `in-progress → done` + the six-gate evidence]). The **§12 D-S35-1..4 RESOLVED:** D-S35-1 file placement (`random.go` NEW sibling; the manager case + reject text in `manager.go`; the retarget in `manager_test.go` — the `leastrequest.go` precedent); D-S35-2 `newRandom` calls `newPCGRNG()` DIRECTLY and ACCEPTS the shared seed-error message (`"cluster: least_request: seed rng"`), NO wrapper (the path is reachable only on a `crypto/rand` failure — effectively unreachable; a wrapper would double-wrap or require editing `newPCGRNG`, which AMEND-R5 forbids; a recorded cosmetic note); D-S35-3 the band constants {K=4 held, S=60 burst, conservation sum==64, uniform floor 12 ≈2.5σ, uniform ceiling 32 ≈2.8σ} + the tuning protocol (≥20 flake-free runs per side + the 3 `-count=1` breaks); D-S35-4 a standalone deterministic-RNG anti-skew unit test (RANDOM does NOT avoid a held endpoint), distinct from the `0060` fixture. The **FINAL ADR-0045 split-gate re-check → NO SPLIT** (8 tasks ≤ ~25 / ~45–60 prod LoC ≤ ~1500 — both legs FAR under the gate by an order of magnitude; NO escape valve — there is no seam build to split off; the seam is reused unchanged). plan-document-reviewer **APPROVED** (all as-built line anchors + the `randomLB` code snippet + the doubly-hit-retarget motivation + the `seqRNG`/`eps` redeclaration-hazard handling verified against disk + the SPEC; two advisory nits — the `0059` port note + a receiver-name typo in a snippet — cleaned in the loop). Counts UNCHANGED at the PLAN (stat surface 1116 / fixtures 61, tail `0059` / fuzzers 42 / BackendKind tail 33 / DECISIONS tail ADR-0233); they advance at the phase-35 IMPL (fixtures 61 → 62 [`0060-lb-random`], DECISIONS tail → ADR-0234, stat surface/fuzzers/BackendKind UNCHANGED — the second zero-delta phase). **Next → the phase-35 IMPL** (`superpowers:subagent-driven-development` — execute the 8-task PLAN). **Superseded (the phase-35 SPEC active-phase, preserved):** `phase 35 (load-balancer-random) SPEC done` (2026-06-11) — the phase-35 {random} SPEC is COMPLETE (`docs/envoy-go/phases/35-load-balancer-random/SPEC.md` + README.md; the BRAINSTORM §10 **D-R1..D-R6** empirical pins EXECUTED IN-SESSION (parallel-subagent fan-out) against (1) the LIVE contrib reference Envoy `envoyproxy/envoy:contrib-v1.37.2` — a RANDOM-cluster held-conn distribution probe (hold-4 + burst-60 ×3) on a docker bridge network per `reference_docker_probe_bridge_network` + a 5-variant `--mode validate` matrix + a full `/stats` name-set diff RANDOM-vs-ROUND_ROBIN — (2) go-control-plane `/envoy` v1.32.4 bindings + `go mod tidy -diff` EMPTY (ZERO new dep), (3) upstream Envoy v1.37.2 source `source/extensions/load_balancing_policies/random/random_lb.{h,cc}` + the shared base). **AMEND-R1..R6 (all CONFIRMED — no refutations, the simplest family surface):** **R1** the v1.32.4 surface re-pinned (`Cluster_RANDOM` enum value 3; **NO `Cluster.RandomLbConfig` message** → NO config-parse arm, NO PGV gate; the `lb_config` oneof has FIVE members and NO `random` member → a stray `least_request_lb_config`/`ring_hash_lb_config` under RANDOM is a mismatched-but-valid oneof member, silently ignored; ZERO new go.mod dep); **R2** the 5-variant validate matrix (RANDOM accepts bare; the mismatched-oneof strays accept SILENTLY — the silent-ignore parity; RING_HASH/MAGLEV validate-accept → envoy-go's continued rejection a recorded departure) + the reject-text blast radius THREE sites (`manager.go:252` + `manager_test.go:320` `TestManager_Error_UnsupportedLBPolicy` [the DOUBLY-hit retarget — its trigger `Cluster_RANDOM` becomes accepted → retarget to RING_HASH/MAGLEV + the substring `"ROUND_ROBIN, LEAST_REQUEST"` → `"…, RANDOM"` at line 327] + `BEHAVIOR_CONTRACT.md:899`; NO fixture pins the text → NO boot-reject dir; fixtures 61 → 62 only); **R3 (SPEC-BLOCKING for the fixture stimulus)** RANDOM stays UNIFORM under held-conn load CONFIRMED LIVE (3 bursts {25,21,18}/{19,25,20}/{16,24,24}, mean 21.3, range 16–25 ≈ ±1.4σ; the LEADING host ROTATES randomly burst-to-burst — RNG-driven NOT load-driven; the contrapositive of the `0059` least_request skew; witnessed via `downstream_cx_rx_bytes_total: 192`); the `0060` band pinned (conservation `sum == 64` + per-host floor `>= 12` [~2.5σ — violated by a load-skewing policy or single-host pin] + per-host ceiling `<= 32` [~2.8σ — violated by single-host pin]); **R4** ZERO stat-name delta CONFIRMED (empty `/stats` name-set diff both directions, 455 == 455; surface STAYS 1116 — the SECOND zero-delta phase); the `0060` StatsAsserter set pinned (`upstream_cx_total`/`membership_total`/quiesced-active CROSS-EQUAL; `upstream_rq_total` PER-SIDE — the subject's tcpproxy never calls `IncUpstreamRqTotal`); **R5** the RANDOM semantics pinned from v1.37.2 source (`RandomLoadBalancer::peekOrChoose` — ONE draw `random_hash % hosts.size()`, NO active-count consultation, NO power-of-two loop, NO tie-break; healthy-host filtering UPSTREAM of the pick in the shared base → degenerates to all-hosts under envoy-go's no-health-check boundary); the RNG REUSE CONFIRMED shared-ready (`newPCGRNG()` already package-private at `leastrequest.go:63` — a new `random.go` calls it DIRECTLY, NO extraction; `newRandomWithRNG` mirrors `newLeastRequestWithRNG` minus `choiceCount`); the empty-set `errNoEndpoints` + `noopRelease` parity; **R6** the seam REUSES UNCHANGED (`randomLB` returns the shared `noopRelease`; NO `Pick`-signature change, NO interface change, NO `cluster.go` change, NO consumer touch — ADR-0232 §Consequences "the non-keyed RANDOM drops in at zero cost" consummated; ADR-0024 UNAMENDED — random holds no per-cluster counter state) + the ADR-0045 envelope re-check (~45–60 prod LoC / ~4–7 tasks — FAR under the gate; NO escape valve) + the single ADR-0234 (policy only — no seam ADR since reuse; the thrift-33 single-ADR-on-reuse precedent). The differential is `0060-lb-random` (cross-side; `[tcp_proxy]` both sides over a 3-endpoint RANDOM cluster; 3 REUSED `TCPEcho` backends; the REUSED `0059` hold-4 + burst-60 stimulus with the assertion INVERTED — the SECOND band-based `AssertDistribution` use asserting RANDOM STAYS uniform [conservation + uniform-floor 12 + uniform-ceiling 32, PER SIDE] DESPITE the load imbalance + the cross-side `StatsAsserter` prong; three deliberate-break liveness proofs with `-count=1` [consult-the-counters (floor leg, the canonical anti-skew break) / pin-to-endpoints[0] (floor + ceiling) / dropped-Inc (stats prong)]); NO boot-reject dir; NO new fuzzer (fuzzers STAY 42 — deliberate); NO new BackendKind (tail STAYS 33 — `TCPEcho` 0 reused). The **ADR-0234 §Context DRAFT anchored in SPEC §13** (per the phase-35 routing the DECISIONS tail **STAYS ADR-0233 at this SPEC** — counts UNCHANGED, contrast the phase-34 SPEC which seeded its TWO §Context entries into DECISIONS.md; phase 35 has a single reuse-case ADR with no seam-build to pre-document → the full ADR-0234 entry + §Decision/§Consequences body land at the IMPL per ADR-0044; next-free ADR-0234). spec-document-reviewer **APPROVED** (all line anchors + the `randomLB` code + the band math [σ≈3.77; floor 12 ≈2.47σ; ceiling 32 ≈2.83σ] + the doubly-hit retarget + the ADR-continuity divergence verified against disk; one non-blocking advisory — the pre-existing fuzzer-ledger nuance, reconciled at IMPL Task 1 via the canonical recipe). ALL counts UNCHANGED at this SPEC (stat surface 1116 / fixtures 61, tail `0059` / fuzzers 42 / BackendKind tail 33 / DECISIONS tail ADR-0233); anticipated moves at the phase-35 IMPL: fixtures 61 → 62 (`0060-lb-random`), DECISIONS tail → ADR-0234, stat surface/fuzzers/BackendKind UNCHANGED. **Next → the phase-35 PLAN** (`superpowers:writing-plans` — decompose the SPEC §10 ~4–7-task spine into bite-sized TDD tasks; resolve the §12 D-S35-1..4 questions; FINAL ADR-0045 split-gate re-check). **Superseded (the phase-35 BRAINSTORM active-phase, preserved):** `phase 35 (load-balancer-random) BRAINSTORM done` (2026-06-11) — the phase-35 {random} BRAINSTORM is COMPLETE (DESIGN-ONLY; `docs/envoy-go/phases/35-load-balancer-random/BRAINSTORM.md`, the phase-34 12-§ family template mirrored). The dialogue settled: **Q0 subject** = `random` (`Cluster.LbPolicy RANDOM`, enum value **3**, `envoy.config.cluster.v3` — VERIFIED in the EXISTING core `/envoy v1.32.4` module cache: **NO `Cluster.RandomLbConfig` message** — zero config knobs, simpler than least_request's `choice_count`; RANDOM's locality-weighting config lives only on the DEFERRED `load_balancing_policies.random.v3` extension-point proto; ZERO new go.mod dep), the SECOND Load-balancing-family row — the family STAYS OPEN; picked from the 7-candidate roster as the **zero-churn ADR-0232 seam drop-in** over the seam-EXTENDING hash policies (RING_HASH/MAGLEV additionally need the ADR-0232 `Pick(hashInput)` widening — DEFERRED); 6 candidates remain after 35 {ring_hash, maglev, subset LB, locality-weighted LB, priority load balancing, panic thresholds}. **Q1 envelope** = LEAN: `RANDOM` accepted by `cluster.Manager` (the lb_policy reject TEXT extends its supported-list `… ROUND_ROBIN, LEAST_REQUEST` → `…, RANDOM` — a byte-stable-reject touchpoint, THREE-site blast radius pinned at SPEC D-R2: `manager.go:252` + `manager_test.go:322` [the doubly-hit retarget — its trigger `Cluster_RANDOM` becomes accepted → retarget to RING_HASH/MAGLEV + the substring assertion] + `BEHAVIOR_CONTRACT.md:899`) + a stateless `randomLB` (uniform `rng() % n`, INSENSITIVE to load; the injectable mutex-guarded crypto-seeded `math/rand/v2` PCG REUSED from `leastRequest`); NO config-parse arm, NO PGV gate. **Q-seam** = REUSE the ADR-0232 seam UNCHANGED (`randomLB` implements the existing `loadBalancer` interface `Pick() (Endpoint, func(), error)` returning the shared `noopRelease` — ignores the active counters at zero cost, the FIRST reuse of the ADR-0232 seam, validating its "RANDOM drops in at zero cost" §Consequences claim); ZERO seam code, ZERO seam ADR. **Q-split** = a SINGLE FLAT ROW 35 (~40–90 prod LoC / ~4–7 tasks, FAR under the ADR-0045 gate — smaller than least_request; NO escape valve — no seam build to split off). Self-answered: the differential is the ANTI-SKEW arm — fixture `0060-lb-random` reusing the `0059` held-open-conn stimulus but asserting RANDOM STAYS uniform-band PER SIDE despite the load imbalance (the contrapositive of least_request; RNG-driven, never cross-side-exact; the EXISTING `DistributionAsserter`) + a cross-side `StatsAsserter` prong; **NO new fuzzer** (fuzzers STAY 42) + **NO new BackendKind** (tail STAYS 33; the `0001`/`0059` echo backends reused) + ZERO stat-delta (1116) — the phase-34 family-level firsts, now ESTABLISHED expectations. **1 ADR anticipated:** ADR-0234 (the policy ONLY — no seam ADR since reuse; the thrift-33 single-ADR-on-reuse precedent); §Context at SPEC, body at IMPL per ADR-0044; DECISIONS tail STAYS ADR-0233 at this brainstorm, next-free ADR-0234. ROADMAP row 35 registers `in-progress` (flat family row, no sub-phases) at this BRAINSTORM commit; the Load-balancing family-heading gains a `random IN-PROGRESS` note. The **D-R1..D-R6** empirical pins DEFER to the SPEC (live against `envoyproxy/envoy:contrib-v1.37.2` per `reference_docker_probe_bridge_network`). Counts UNCHANGED at this brainstorm (stat surface 1116 / fixtures 61 / fuzzers 42 / BackendKind tail 33 / DECISIONS tail ADR-0233); anticipated moves at the phase-35 IMPL: fixtures 61 → 62 (`0060-lb-random`), DECISIONS tail → ADR-0234, stat surface/fuzzers/BackendKind UNCHANGED. **Next → the phase-35 SPEC** (`superpowers:writing-plans` scoped to SPEC authoring — single flat row, the kafka-31/thrift-33/least_request-34 precedent; execute D-R1..D-R6; anchor the ADR-0234 §Context draft). **Superseded (the phase-34 IMPL active-phase, preserved):** `phase 34 (load-balancer-least-request) done` (2026-06-11) — the phase-34 IMPL is COMPLETE: the 9-task TDD plan (`docs/envoy-go/phases/34-load-balancer-least-request/PLAN.md`) executed end-to-end, landing the project's FIRST Load-balancing-family policy beyond ROUND_ROBIN. As built: the **ADR-0232** LB acquire/release seam — the unexported `internal/cluster` `loadBalancer` interface reshaped from `Pick() (Endpoint, error)` to `Pick() (Endpoint, func(), error)` (OPTION C / D-L7 — the EXPORTED `Cluster` surface BYTE-STABLE, ZERO consumer churn); the release composes into the existing ADR-0063 `connWithGauge` `sync.Once` Close hook (`Dial`/`AcquireH1` release-on-failure; pool-HIT + the exported `PickEndpoint()` release IMMEDIATELY); `roundRobin` adopts a no-op release (provably behavior-neutral — all 60 prior fixtures byte-identical). The **ADR-0233** `leastRequest` policy — an un-weighted power-of-two-choices LB mirroring upstream v1.37.2 EXACTLY (with-replacement sampling, strict `<` on the RAW per-endpoint `atomic.Int64` active count, first-drawn wins ties, no clamp at `choice_count >= n`), crypto-seeded mutex-guarded `math/rand/v2` PCG RNG (injectable via `newLeastRequestWithRNG`); `cluster.Manager` accepts `LEAST_REQUEST` beside ROUND_ROBIN with a hand-rolled `choice_count >= 2` PGV-parity gate, DEPARTURE-rejects `active_request_bias`/`slow_start_config`, silent-ignores a mismatched `lb_config` oneof (reference parity), and emits the NEW two-policy unsupported-text for RANDOM/RING_HASH/MAGLEV. Differential `0059-lb-least-request` (hold-4/burst-60 skew over 3 reused `TCPEcho` backends, cc=10) — the FIRST band-based `AssertDistribution` (conservation/starvation/concentration PER SIDE; 20/20 flake-free) + a cross-side `StatsAsserter` prong, with 3 deliberate-break liveness proofs under `-count=1`. **The deliberate FIRSTS:** ZERO stat-name delta (surface stays 1116 — the FIRST zero-delta phase), NO new fuzzer (the FIRST no-fuzzer phase since the fuzzing regime began), NO new BackendKind (tail stays 33). Six gates GREEN (build/vet/gofmt/lint; `-race -short`; 61-dir differential byte-exact; h2spec 53/53 + proxy-wasm 10/10 unaffected; `go mod tidy` clean — the phase-34 PROGRESS.md §"Task 8 — six-gate evidence" + §"Task 9"). DECISIONS.md tail **ADR-0233** (ADR-0232 + ADR-0233 §Decision/§Consequences bodies landed IN-PLACE per ADR-0044 — NO new number; next-free **ADR-0234**). **Next → the phase-34 phase-done routing** (the controller squash-merges + pushes at stage-close; the Load-balancing family stays OPEN — 7 policy candidates remain: random/ring_hash/maglev + subset-LB/locality-weighted/priority/panic-thresholds). **Superseded (the phase-34 PLAN active-phase, preserved):** `phase 34 (load-balancer-least-request) PLAN done` (2026-06-11, this commit) — the phase-34 PLAN is COMPLETE (`docs/envoy-go/phases/34-load-balancer-least-request/PLAN.md`): the SPEC §10 ~10-task spine decomposed into **9 bite-sized TDD tasks** (write-failing-test → run-fail → minimal-impl → run-pass → per-task `gofmt -l` + `golangci-lint` per `feedback_pertask_gofmt_lint` → LOCAL-ONLY commit per `feedback_subagents_no_push`). SPEC §10 Tasks 2+3 MERGED into PLAN Task 2 (the `loadBalancer.Pick` signature change is compiler-coupled to its `cluster.go` call sites — they must change in ONE commit or the package will not build; doing the final threading immediately avoids a throwaway behaviorally-identical intermediate); all other §10 tasks map 1:1. The **§12 D-questions RESOLVED:** **D-S34-1** the file split (the reshaped interface + `roundRobin` stay in `loadbalancer.go`; the `leastRequest` type in a NEW sibling `leastrequest.go`; `parseLeastRequestLbConfig` + `defaultChoiceCount` in `manager.go`); **D-S34-2** the production RNG (`newLeastRequest` crypto-seeds a `math/rand/v2` PCG once per construction, wrapped behind a **mutex-guarded `rng func() uint64`** — Pick is concurrent across downstream conns and `math/rand/v2.Rand` is NOT per-source concurrency-safe; the `crypto/rand` read error threads out of `newLeastRequest` → boot-fail; tests inject a deterministic sequence via `newLeastRequestWithRNG` — the upstream mock posture); **D-S34-3** the band constants (K=4 held / S=60 burst / `c1 <= 12` starvation / `c2 >= 16` concentration / `c1+c2+c3 == 64` conservation) + the tuning protocol (≥20 repeat runs flake-free PER SIDE + the three `-count=1` deliberate-break proofs: inverted-comparison [c2 leg] / no-op-release [c1 leg — the canonical deterministic break] / stats-prong); **D-S34-4** the held-conn establishment witness (write+read-echo per held conn before holding — `acceptEchoCounting` is a streaming echo); **D-S34-5** compose release into the existing `connWithGauge` `dec` closure (`dec: func() { c.upstreamCxActive.Dec(); release() }` — the struct UNCHANGED, its `sync.Once` guards both the gauge Dec and the LB release). The **FINAL ADR-0045 split-gate re-check → NO SPLIT** (9 tasks ≤ ~25 / ~155–255 prod LoC ≤ ~1500 — both legs FAR under the gate; the pre-authorized 34.1-seam/34.2-policy escape valve STAYS UNCONSUMED — the kafka-31/thrift-33 precedent). plan-document-reviewer **APPROVED** (all line anchors + code snippets verified against the as-built source + the v1.32.4 proto surface + the differential harness; one advisory test-narrative nit cleaned in the loop). Counts UNCHANGED at the PLAN (stat surface 1116 / fixtures 60, tail `0058` / fuzzers 42 / BackendKind tail 33 / DECISIONS tail ADR-0233); they advance at the phase-34 IMPL (fixtures 60 → 61 ONLY — `0059-lb-least-request`; stat surface / fuzzers / BackendKind UNCHANGED — the deliberate zero-delta / no-fuzzer / no-BackendKind firsts). **Next → the phase-34 IMPL** (`superpowers:subagent-driven-development` — execute the 9-task PLAN; the controller squash-merges + pushes at stage-close). **Superseded (the phase-34 SPEC active-phase, preserved):** `phase 34 (load-balancer-least-request) SPEC done` (2026-06-11) — the phase-34 SPEC is COMPLETE (`docs/envoy-go/phases/34-load-balancer-least-request/SPEC.md` + README.md; the BRAINSTORM §10 **D-L1..D-L7** empirical pins EXECUTED IN-SESSION (parallel-subagent fan-out) against (1) the LIVE contrib reference Envoy `envoyproxy/envoy:contrib-v1.37.2` — held-open-conn LB probes + per-host `/clusters` scrapes + full `/stats` name-set diffs on a docker bridge network per `reference_docker_probe_bridge_network` + a 10-variant `--mode validate` matrix — (2) go-control-plane `/envoy` v1.32.4 bindings + `go mod tidy -diff` EMPTY (ZERO new dep), (3) upstream Envoy v1.37.2 source `source/extensions/load_balancing_policies/least_request/`). **AMEND-L1..L7:** **L1** the three-knob surface re-pinned (`choice_count` PGV `>= 2` ONLY-when-set, string `value must be greater than or equal to 2`) + TWO sharpenings — the manager calls NO PGV today (the choice_count gate is HAND-ROLLED) and `least_request_lb_config` is an `lb_config` ONEOF member (field 37; mismatched-member disposition pinned); **L2 (SPEC-BLOCKING)** cx-as-rq CONFIRMED LIVE — an open TCP-proxied conn IS one active request (per-host `rq_active == cx_active` 1:1; tcp_proxy charges rq-per-cx; the per-endpoint counter counts CONNECTIONS, release at final conn close); **L3** FULL_SCAN DEFERRED with reference-side proof (even the v1.37.2 reference's own legacy surface unknown-field-rejects `enable_full_scan`; the legacy converter never sets `selection_method`); sampling is WITH replacement → `choice_count == N` NOT deterministic (live: the most-loaded host took 3/30 at cc=3) while `choice_count > N` is LEGAL (no clamp) + quasi-deterministic (live: 0/30 at cc=10 — the fixture band basis); the deterministic exact-count arm DROPPED; **L4** ZERO stat-name delta CONFIRMED (empty `/stats` name-set diff both directions; surface STAYS 1116 — the first zero-delta phase, deliberate); the `0059` StatsAsserter set pinned (`upstream_cx_total`/`membership_total`/quiesced-active CROSS-EQUAL; `upstream_rq_total` PER-SIDE — the subject's tcpproxy never calls `IncUpstreamRqTotal`); **L5** the accept/reject matrix pinned (LEAST_REQUEST bare/with-config accepted; cc 0/1 PGV-rejected; cc 100 accepted no-clamp; bias/slow_start reference-parse-accepted → envoy-go-strict DEPARTURE rejects when set [behavior-bearing — the thrift AMEND-T7 fail-fast lineage]; a mismatched lb_config oneof member silently ignored by the reference → envoy-go silent-ignore PARITY; RANDOM/RING_HASH reference-accepted → envoy-go's continued rejection a recorded departure) + the reject-text blast radius is THREE sites (`manager.go:216` + `TestManager_Error_NonRoundRobinLB` [DOUBLY hit — its trigger LEAST_REQUEST becomes accepted → retargets to RANDOM] + `BEHAVIOR_CONTRACT.md:897`; NO fixture pins the text → NO boot-reject dir; fixtures 60 → 61 only); **L6** the P2C semantics pinned from source (k draws `rng() % n` WITH replacement; strict `<` on RAW `rq_active`; first-drawn wins ties; no clamp; the weighted bias formula belongs to the EDF path which NEVER engages at equal weights — pure P2C for the MVP; crypto RNG upstream → the injectable-RNG unit posture; healthy-host filtering degenerates to all-hosts [no health checking — the Upstream-robustness family's territory]); **L7** the seam is **OPTION C** — reshape ONLY the unexported `loadBalancer` interface to `Pick() (Endpoint, release func(), error)`; the EXPORTED `Cluster` surface BYTE-STABLE, ZERO consumer churn (the release composes into the existing ADR-0063 `connWithGauge` `sync.Once` dec; release-before-error on dial/TLS failure; `PickEndpoint()` releases IMMEDIATELY [httpclient + the thriftproxy probe stay source-compatible, their load invisible — a documented coverage note]; `AcquireH1` pool-hit releases the fresh pick immediately under cx-as-rq [the pooled conn's dial-time hold persists to FINAL close]); ~155–255 prod LoC / ~10 tasks → the SINGLE FLAT ROW holds with margin (the 34.1/34.2 escape valve UNCONSUMED); ADR-0024 needs NO amendment (per-endpoint counters on the per-cluster LB instance = the same scope discipline; a one-line cross-ref in ADR-0232). The differential is `0059-lb-least-request` (cross-side; `[tcp_proxy]` both sides over a 3-endpoint LEAST_REQUEST cluster with `choice_count: 10`; 3 REUSED `TCPEcho` backends; a hold-4 + burst-60 workload; the FIRST band-based `AssertDistribution` use — conservation `sum == 64` + starvation `c1 <= 12` + concentration-shape `c2 >= 16`, asserted PER SIDE [RNG-driven, never cross-side-exact; the 0003 subject-only asymmetry precedent] + the cross-side `StatsAsserter` prong; three deliberate-break liveness proofs with `-count=1` [inverted-comparison / no-op-release / dropped-Inc]); NO boot-reject dir; NO new fuzzer (fuzzers STAY 42 — deliberate); NO new BackendKind (tail STAYS 33). The **ADR-0232** (the LB acquire/release seam) + **ADR-0233** (the least_request policy) **§Context drafts ANCHORED** (DECISIONS tail ADR-0231 → **ADR-0233**; next-free **ADR-0234**; §Decision/§Consequences bodies at the IMPL per ADR-0044). spec-document-reviewer APPROVED. Counts UNCHANGED at the SPEC except the DECISIONS tail (stat surface 1116 / fixtures 60, tail `0058` / fuzzers 42 / BackendKind tail 33); anticipated moves at the IMPL: fixtures 60 → 61 ONLY (stat surface / fuzzers / BackendKind UNCHANGED — the deliberate proof-shape firsts). **Superseded (the phase-34 BRAINSTORM active-phase, preserved):** `phase 34 (load-balancer-least-request) BRAINSTORM done` (2026-06-10) — the phase-34 BRAINSTORM is COMPLETE (DESIGN-ONLY; `docs/envoy-go/phases/34-load-balancer-least-request/BRAINSTORM.md`): the dialogue settled **Q0** (the **Load-balancing family OPENS** — the first new family since the §9 Network-filters closure at phase 33; first subject `least_request` = `Cluster.LbPolicy LEAST_REQUEST` + config message `Cluster.LeastRequestLbConfig` in `envoy.config.cluster.v3` — VERIFIED in the go module cache: the EXISTING core `/envoy v1.32.4` dep carries it [ZERO new go.mod dep] with EXACTLY three knobs — `choice_count` [`UInt32Value`, PGV ≥ 2 when set, default 2] + `active_request_bias` [`RuntimeDouble`] + `slow_start_config` — and **NO `enable_full_scan` / NO `selection_method`** on this surface [those live only in the `load_balancing_policies.least_request.v3` extension proto] → the Q1 conditional FULL_SCAN arm's proto leg FAILED; anticipated DEFERRED at D-L3) / **Q1** (LEAN envelope: `LEAST_REQUEST` accepted by `cluster.Manager` [the lb_policy reject TEXT changes — a byte-stable-reject touchpoint, blast radius pinned at SPEC D-L5] + an un-weighted P2C `leastRequest` LB [sample `choice_count` random hosts, pick the fewest-outstanding-active-requests host] + FULL_SCAN only-if-supported; `active_request_bias`/`slow_start`/locality interplay/panic thresholds/all other policies [RANDOM/RING_HASH/MAGLEV byte-stable-rejected] DEFERRED) / **Q-seam** (the `internal/cluster` `loadBalancer` interface — currently stateless `Pick() (Endpoint, error)`, sole impl `roundRobin` — extends to an ACQUIRE/RELEASE shape: Pick returns the endpoint plus a release handle [exact exported surface pinned at SPEC]; the LB owns per-endpoint atomic active counters; the dial path releases on conn close; `roundRobin` ignores the counters [behavior-neutral]; every Pick consumer touched mechanically — **the project's FIRST framework seam OUTSIDE `internal/filter/network`**; every future Load-balancing row reuses it, the ADR-0230 build-at-first-consumer logic) / **Q-split** (a SINGLE FLAT ROW 34, ~300–600 prod LoC / ~10–14 tasks under the ADR-0045 gate + a pre-authorized 34.1-seam/34.2-policy escape valve, expected unconsumed). Proof-shape novelties (family-level firsts, DELIBERATE): **NO new fuzzer** (no wire decode — the FIRST no-fuzzer phase since the fuzzing regime began; fuzzers STAY 42) + **NO new BackendKind** (the `0001-tcp-proxy-rr` TCP echo backends reused; tail STAYS 33); ONE fixture `0059-lb-least-request` anticipated — a slow/held-open-backend load-skew arm asserted via the EXISTING `DistributionAsserter` (`test/differential/fixture/fixture.go` ~line 57, the 0001/0003 precedent) with BAND semantics PER SIDE (RNG-driven — never cross-side-exact) + a cross-side `StatsAsserter` prong on cluster counters (+ the FULL_SCAN deterministic arm only if D-L3 revives it). The **D-L1..D-L7** empirical pins DEFER to the SPEC (live against `envoyproxy/envoy:contrib-v1.37.2` per `reference_docker_probe_bridge_network`): D-L1 the v1.32.4 proto/PGV re-pin, D-L2 what "active request" means for TCP-proxied conns (cx-as-rq? — held-open-conn probe), D-L3 FULL_SCAN availability + tie-break, D-L4 the stat-surface delta (anticipated ZERO new names), D-L5 the accept/reject matrix + the reject-text blast radius, D-L6 P2C sampling semantics (per-side/unit-level only), D-L7 the ADR-0045 envelope re-check + the exact seam surface. **2 ADRs anticipated:** ADR-0232 (the LB acquire/release seam) + ADR-0233 (the least_request policy) — §Context at SPEC, bodies at IMPL per ADR-0044. ROADMAP row 34 registered `in-progress` + the Load-balancing family marked OPEN (sibling rows NOT pre-populated per ADR-0106). Counts UNCHANGED at this brainstorm: stat surface **1116** / fixtures **60** (tail `0058-thrift-boot-reject`) / fuzzers **42** (tail `FuzzThriftDecode`) / BackendKind tail **33** / DECISIONS tail **ADR-0231** (next-free **ADR-0232**); anticipated moves at the IMPL: fixtures 60 → 61, DECISIONS tail → ADR-0233, stat surface/fuzzers/BackendKind UNCHANGED. **Superseded (the phase-33 IMPL active-phase, preserved):** `phase 33 (network-filter-thrift-proxy) done` (2026-06-10) — the phase-33 {thrift} IMPL COMPLETE (`docs/envoy-go/phases/33-network-filter-thrift-proxy/PROGRESS.md`): all 16 TDD tasks landed subagent-driven — the `thriftproxy` package (the 11th built-in; `thriftproxy.go` TypeURL+factory / `config.go` parse + `stat_prefix` PGV + transport/protocol DEPARTURE rejects / `route.go` the method-routing table / `thrift.go` the framed-binary message-begin codec + reply classifier + `encodeUnknownMethod` `AppException` encoder / `stats.go` the EAGER 25-name roster / `filter.go` the `TerminalFilter.Handle` pump over the REUSED ADR-0230 seam) + the `thrift.` SINGLE-label-hoist prom arm + the `TCPThriftResponder` BackendKind 33 + the `0057-thrift-roundtrip` cross-side + `0058-thrift-boot-reject` fixtures + the 42nd fuzzer `FuzzThriftDecode` + this completion bundle. **ZERO new go.mod dep** (thrift_proxy is CORE `/envoy v1.32.4` — D-T1) + **ZERO framework-seam extension** (the FIRST row to REUSE a prior framework seam — ADR-0230 — UNCHANGED, validating its redis-scoped YAGNI sizing; the seam's defensive cross-cluster re-dial branch made live-tested). Two empirical per-side findings PINNED at `0057` (the Task-12 fixture, both documented behavioral boundaries, NOT subject bugs): (1) the subject counts `request`/`request_call`/`request_passthrough` on a routing MISS (count-before-match in the pump → subj=3 across the 3-arm workload), while the C++ reference does NOT count `request*` on a miss (ref=2 — HIT-only) → asserted PER-SIDE (ref=2/subj=3, NOT cross-equal); (2) the subject's cluster package does NOT emit `upstream_cx_rx_bytes_total` (it reuses only `upstream_cx_total`/`upstream_rq_total`) — the decode-ran byte-witness is reference-side only (ref=73), the subject witness is `request_call > 0`; `upstream_cx_total` is per-side (ref=1 pooled / subj=2 one-conn-per-downstream), `upstream_rq_total == 2` cross-equal. The 2 close-direction counters + `downstream_response_drain_close` are created-but-NEVER-incremented (the `reference_close_direction_framework_gap` boundary, AMEND-T6/D-T8 — the reference DOES fire them on the live miss arm, asserted per-side). Six gates GREEN (build / vet / golangci-lint / `-race -short` / the full 60-dir differential byte-exact PASS / h2spec 53/53 + proxy-wasm 10/10 asserted-UNAFFECTED — phase 33 touches no HTTP/h2/proxy-wasm path). The ADR-0231 §Decision/§Consequences body completed IN-PLACE (PROPOSED → ACCEPTED per ADR-0044 — NO new ADR; DECISIONS tail STAYS **ADR-0231**, next-free ADR-0232). Counts advanced: stat surface 1091 → **1116** (+25: 24 counters + 1 gauge `request_active`), fixtures 58 → **60** (`0057` + `0058`), fuzzers 41 → **42** (`FuzzThriftDecode`), BackendKind tail 32 → **33** (`TCPThriftResponder`). ROADMAP row 33 flips `in-progress → done` (a flat §9 row — NO parent rollup per ADR-0106); **the §9 Network-filters family CLOSES (0 candidates remain).** **Superseded (the phase-33 PLAN active-phase, preserved):** `phase 33 (network-filter-thrift-proxy) PLAN done` — the phase-33 {thrift} PLAN COMPLETE (`docs/envoy-go/phases/33-network-filter-thrift-proxy/PLAN.md`): the SPEC §10 spine decomposed into **16 bite-sized TDD tasks** (write-failing-test → run-fail → minimal-impl → run-pass → per-task `gofmt -l` + `golangci-lint` → LOCAL-ONLY commit per `feedback_subagents_no_push`). The **ADR-0045 split-gate FINAL re-check at PLAN: NO SPLIT** (16 ≤ ~25 tasks / ~790–1275 ≤ ~1500 prod LoC → both legs under the gate; the pre-authorized 33.1/33.2 escape-valve STAYS UNCONSUMED — the kafka-31 precedent). The **SPEC §12 D-questions RESOLVED:** **D-S33-1** the `thriftproxy/` file split mirrors redisproxy exactly (`thriftproxy.go` TypeURL+factory / `config.go` / `route.go` / `thrift.go` codec+classifier+`AppException` encoder / `stats.go` / `filter.go` / `doc.go` / `fuzz_test.go` — NO separate `exception.go`); **D-S33-2** the `0057` reply-EXCEPTION arm LANDS as a fixture arm (a `TCPThriftResponder` exception mode) + the reply-ERROR arm unit-tested; **D-S33-3** `request_active` quiesced-to-0 post-workload (NO held-open arm); **D-S33-4** reply classification peeks the first result-struct field header (STOP/field-id-0 → `response_success`, field-id ≥ 1 → `response_error`); **D-S33-5** six `FuzzThriftDecode` seeds (valid CALL/REPLY + truncated + bad-magic + oversized-length + invalid-msgtype); **D-S33-6** the malformed-frame path counts `request_decoding_error` + SILENTLY CLOSES (NO local `ProtocolError` — a coverage-boundary departure; a distinct `errInvalidType` sentinel splits invalid-msgtype → `request_invalid_type`). plan-document-reviewer **APPROVED** (two advisory nits pre-corrected in the loop: the `UnknownMethod` exception frame-len literal `0x4b`=75; the `errInvalidType` sentinel reconciled in the Task-5 decode snippet). Counts UNCHANGED at the PLAN (stat surface 1091 / fixtures 58 / fuzzers 41 / BackendKind tail 32 / DECISIONS tail ADR-0231); they advance at the phase-33 IMPL (→ 1116 / 60 / 42 / 33). ROADMAP row 33 STAYS `in-progress` (flat §9 row — flips `→ done` at the IMPL six-gate, NO parent rollup). **Next → the phase-33 IMPL** (`superpowers:subagent-driven-development` — execute the 16-task PLAN; the controller squash-merges + pushes at stage-close). **Superseded (the phase-33 SPEC active-phase, preserved):** the phase-33 {thrift} SPEC COMPLETE (`docs/envoy-go/phases/33-network-filter-thrift-proxy/SPEC.md` + README.md; the BRAINSTORM §10 **D-T1..D-T9** empirical pins EXECUTED IN-SESSION (parallel-subagent fan-out) live against the contrib reference Envoy `envoyproxy/envoy:contrib-v1.37.2` + go-control-plane `/envoy` v1.32.4 + upstream v1.37.2 CORE source). A hand-built canned-REPLY Thrift backend on a docker bridge network confirmed the **framed×binary round-trip RAN** (`cluster.thriftcluster.upstream_cx_rx_bytes_total: 21`, `thrift.thriftprobe.request_call: 1`, `response_success: 1`) + a **routing-miss local `UnknownMethod` exception** (zero upstream dial). **AMEND-T1..T7:** **T1** TypeURL `…thrift_proxy.v3.ThriftProxy` (carries `extensions.`), CORE `/envoy v1.32.4` (where redis_proxy/v3 lives), **ZERO new go.mod dep** (the redis D32-1 posture — CONFIRMS the `/contrib`→core correction); **T2** the **framed×binary** pair + the round-trip ran live (the working YAML reusable verbatim; framed=4-byte BE frame-len, binary strict message-begin `0x8001 00 <msgtype>`+i32 name-len+name+i32 seq_id; payload_passthrough reads message-begin only; AUTO auto-detects framed-binary identically); **T3** the roster is EAGER+table-bounded = **24 counters + 1 GAUGE `request_active` + 1 HISTOGRAM `request_time_ms`** (REFUTES the BRAINSTORM: NO `response_business_exception`; +`request_passthrough`/`response_passthrough` + 5 router counters [`route_missing`/`unknown_cluster`/`no_healthy_upstream`/`upstream_rq_maintenance_mode`/`shadow_request_submit_failure`]); the prom arm is **SINGLE-label-HOIST** `envoy_thrift_<leaf>{envoy_thrift_prefix="<sp>"}` (the redis-32/mongo shape, NOT kafka inline); **stat surface 1091 → 1116 (+25** — 24 counters + 1 gauge; the histogram deferred ADR-0060); **T4** routing-MISS → local `UnknownMethod` EXCEPTION zero-upstream (the redis-PING analogue; decode-error → local `ProtocolError`) — SPEC-BLOCKING for fixture traffic; **T5** the reference REMAPS upstream `seq_id`→0 + restores downstream, correlation POSITIONAL not seq_id-keyed → the MVP passes seq_id through → downstream byte-equivalence holds (upstream seq_id a per-side boundary); **T6** the close-direction counters `cx_destroy_local/remote_with_active_rq` DO fire on the live miss path (`cx_destroy_local_with_active_rq: 1`) — SHARPER than redis-32; the MVP creates-but-never-increments them + keeps the conn open on a local reply → asserted PER-SIDE on the `0057` miss arm (the framework-gap coverage boundary, D-T8); **T7** only `stat_prefix` is a hard PGV gate (`value length must be at least 1 characters`), `route_config` NOT required, the un-chosen transport/protocol pairs are envoy-go-strict DEPARTURE rejects (the reference parse-accepts them → unit-tested, NOT a cross-side boot-reject). The **ADR-0231 §Context anchored** (DECISIONS tail ADR-0230 → ADR-0231; §Decision/§Consequences body at the IMPL per ADR-0044); single flat row HOLDS (D-T9a — ~790–1275 LoC / ~16–20 tasks, UNDER the ADR-0045 gate; the 33.1/33.2 escape-valve UNCONSUMED); the differential is `0057-thrift-roundtrip` (route-HIT round-trip + route-MISS local-exception; downstream byte-equivalence + `StatsAsserter`; the per-side cx_destroy boundary) + `0058-thrift-boot-reject` (missing stat_prefix) + a new `TCPThriftResponder` BackendKind (seq_id-agnostic canned framed-binary REPLY, value 33) + the 42nd fuzzer `FuzzThriftDecode`; spec-document-reviewer gate applies at this SPEC. Counts UNCHANGED at the SPEC (stat surface 1091 / fixtures 58 / fuzzers 41 / BackendKind tail 32); DECISIONS tail advances to **ADR-0231** at this SPEC commit (next-free **ADR-0232**); the surface/fixture/fuzzer/BackendKind counts advance at the phase-33 IMPL (stat surface → 1116, fixtures → 60, fuzzers → 42, BackendKind → 33). **Next → the phase-33 PLAN** (`superpowers:writing-plans` — decompose SPEC §10 into bite-sized TDD tasks; re-check the ADR-0045 gate). **Earlier (the phase-33 BRAINSTORM, preserved):** the phase-33 {thrift} BRAINSTORM COMPLETE (DESIGN-ONLY; `docs/envoy-go/phases/33-network-filter-thrift-proxy/BRAINSTORM.md`, the 12-§ redis-32/kafka-31 template). Settled via a 4-question dialogue: **Q0** subject confirm = `thrift_proxy` (`envoy.filters.network.thrift_proxy`, proto `envoy.extensions.filters.network.thrift_proxy.v3.ThriftProxy`), the SOLE remaining §9 candidate — the §9 Network-filters family CLOSES after phase 33 phase-done; **with a BRAINSTORM dep-framing CORRECTION** — thrift_proxy is a **CORE `/envoy v1.32.4` extension** (verified in the go module cache: `thrift_proxy/v3` under `go-control-plane/envoy@v1.32.4`, where `redis_proxy/v3` lives — NOT under `/contrib` where `kafka_broker/v3` lives), REFUTING the stale "`/contrib` like kafka_broker" framing in the prior next-prompt/STATE → anticipated ZERO new go.mod dep because the existing core `/envoy` dep already carries it (the redis D32-1 posture), boots on the contrib reference image (ADR-0227, a superset; thrift is core so it is in the standard image too). **Q1** envelope = a SINGLE-PAIR SINGLE-ROUTE-TERMINAL MVP (the Thrift message-begin codec for ONE transport×protocol pair under `payload_passthrough` — route on method name + message type + `seq_id`, skip the struct body — + a match-all/single-method `route_config` route to ONE cluster + a router-only nested thrift-filter sub-chain + the global `thrift.<stat_prefix>.request*/response*` stat roster + a new `thrift.` prom arm; the rest of the 3×3 transport×protocol matrix / full route_config richness / the non-router thrift_filters / full struct parsing / seq_id multiplexing / `max_requests_per_connection` / `access_log` deferred; the close-direction `cx_destroy_local/remote_with_active_rq` counters deferred as a coverage boundary per the §9 zero-touch AMEND-R9 posture; histograms deferred ADR-0060). **Q-seam** = REUSE the ADR-0230 upstream-pool seam UNCHANGED (one-conn-per-downstream, synchronous single-flight, positional/FIFO correlation; thrift's `seq_id` parse-validated but NOT demux-load-bearing for single-flight; the seq_id-keyed multiplexing / shared pool / deep queue / pipelined model stay DEFERRED — exactly the surface ADR-0230 names for "thrift / a latency-or-fan-out sub-phase") — thrift is ADR-0230's anticipated SECOND consumer; the FIRST reuse of a prior framework seam → framework-ZERO-touch, validating the seam's redis-scoped YAGNI sizing. **Q-split** = a SINGLE FLAT ROW 33 (the kafka-31 / sni_cluster-27 precedent — a §9 protocol filter with NO new framework seam; the seam-first driver of redis's 2-way split is gone; ~800–1300 prod LoC / ~15–22 tasks UNDER the ADR-0045 gate) + a PRE-AUTHORIZED 33.1-codec / 33.2-stats escape-valve split (unconsumed). The differential is the §9 SECOND row whose downstream RESPONSE bytes are load-bearing (after redis-32 — thrift_proxy generates them) + cross-side `StatsAsserter` + a new `TCPThriftResponder` BackendKind (synchronous/positional with a seq_id-echo, anticipated value 33) + the 42nd fuzzer `FuzzThriftDecode`. Anticipated 1 ADR — **ADR-0231** (the filter only; no parent-umbrella since no split, no seam ADR since reuse — contrast redis's 2). The empirical pins (D-T1 TypeURL+CORE-dep / D-T2 the transport×protocol pair + wire format / D-T3 the stat roster + prom-arm shape / D-T4 config PGV+boot-reject / D-T5 router/local-reply semantics / D-T6 seq_id echo + positional correlation / D-T7 IsValidName placement / D-T8 close-direction deferral / D-T9 the ADR-0045 envelope + per-side upstream_cx_* pinning) defer to the phase-33 SPEC, executed live against the contrib reference Envoy per `reference_docker_probe_bridge_network`. ROADMAP row 33 registers `in-progress` (flat §9 row, no sub-phases) at this BRAINSTORM commit; the §9 candidate-roster paragraph marks thrift IN-PROGRESS + records the closure-after-33 + the `/contrib`→core correction. Counts UNCHANGED at the BRAINSTORM (stat surface 1091 / fixtures 58 / fuzzers 41 / BackendKind tail 32 / DECISIONS tail ADR-0230, next-free ADR-0231); they advance at the phase-33 IMPL (fixtures → 59–60, fuzzers → 42, stat surface → 1091+roster, BackendKind → 33, DECISIONS tail → ADR-0231). **Next → the phase-33 SPEC** (`superpowers:writing-plans` scoped to SPEC authoring — single flat row, the kafka-31 precedent; executes D-T1..D-T9; anchors the ADR-0231 §Context). **Earlier (the phase-32.x records — the 32.2 IMPL/PLAN/SPEC + the 32.1 IMPL — COMPRESSED to a pointer):** the full phase-32 `redis_proxy` chain (the ADR-0230 upstream-pool seam + RESP codec at 32.1; the 180-command set + the 540-counter roster + the parent-row-32 ROLLUP at 32.2) lives in its durable on-disk phase directories `docs/envoy-go/phases/32-network-filter-redis-proxy/` + `32.1-network-filter-redis-upstream-pool-seam-and-codec/` + `32.2-network-filter-redis-commands-and-stats/` (+ ROADMAP rows 32/32.1/32.2). **Earlier (phase 31 IMPL / phase 31 CLOSED, preserved):** the phase-31 `kafka_broker` IMPL COMPLETE — the FIFTH §9 row; a new `internal/filter/network/kafkabroker/` package (header-only both-direction Kafka sniffer at FULL per-API-key counter parity; the 9th built-in; consumer #3 of the ADR-0221 WriteFilter seam; the project's FIRST `/contrib v1.32.4` consumer); the EAGER 176-counter roster (stat surface 360 → 536); the `kafka.` INLINE prom arm; the `TCPKafkaResponder` BackendKind 31; the `0053`/`0054` fixtures; the 40th fuzzer `FuzzKafkaDecode`; ADR-0228 §Decision/§Consequences bodies landed in-place; six gates GREEN; ROADMAP row 31 `in-progress → done` (flat §9 row). The full prior detail (the 17-task spine; AMEND-K1..K7; D-PLAN-1/2/3; the phase-31 BRAINSTORM/SPEC/PLAN history; phase 30 the contrib pin-refresh; the phase-29 mongo_proxy record) is the durable on-disk record in the phase-31 + phase-30 + phase-29 directories.
- **phase-directory:** `docs/envoy-go/phases/36-load-balancer-ring-hash/` (**BRAINSTORM.md + SPEC.md + PLAN.md + README.md** — PLAN.md + the README.md status advance (2 → 3) landed THIS commit; the 36.1/36.2 split is CONSUMED → the 36.1 PROGRESS.md follows at the 36.1 IMPL session, then the 36.2 sub-phase dir per the 32.1/32.2 per-sub-phase-dir precedent). **Superseded (the phase-35 phase-directory, preserved):** `docs/envoy-go/phases/35-load-balancer-random/` (**BRAINSTORM.md + SPEC.md + README.md + PLAN.md** — PLAN.md landed THIS commit (README.md status advanced 2 → 3); the PROGRESS.md follows at the IMPL session [Task 1 creates it], per the kafka-31/thrift-33/least_request-34 single-flat-row per-phase-dir precedent). The phase-34 directory `docs/envoy-go/phases/34-load-balancer-least-request/` (**BRAINSTORM.md + SPEC.md + README.md + PLAN.md + PROGRESS.md** — the durable phase-34 IMPL record). The phase-33 directory `docs/envoy-go/phases/33-network-filter-thrift-proxy/` (**BRAINSTORM.md + SPEC.md + README.md + PLAN.md + PROGRESS.md** — the durable phase-33 IMPL record). The phase-32 dirs `docs/envoy-go/phases/32-network-filter-redis-proxy/` + `32.1-…/` + `32.2-network-filter-redis-commands-and-stats/` (**SPEC.md** — the durable phase-32.2 sub-phase SPEC; the PLAN.md follows at the 32.2 PLAN session, the PROGRESS.md at the 32.2 IMPL session [Task 1 creates it], per the 29.1/29.2/29.3 per-sub-phase-dir precedent). The phase-32.1 dir `docs/envoy-go/phases/32.1-network-filter-redis-upstream-pool-seam-and-codec/` (**SPEC.md + PLAN.md + PROGRESS.md** — the durable phase-32.1 IMPL record). The parent `docs/envoy-go/phases/32-network-filter-redis-proxy/` (**BRAINSTORM.md + SPEC.md + README.md** — the durable phase-32 parent-SPEC record). The phase-31 directory `docs/envoy-go/phases/31-network-filter-kafka-broker/` (BRAINSTORM.md + SPEC.md + README.md + PLAN.md + PROGRESS.md — the durable phase-31 IMPL record). The phase-30 + phase-29 directories stay the durable phase-30/29 record.
- **lifecycle-state:** `phase 36.1 PLAN done — next: the 36.1 IMPL (superpowers:subagent-driven-development — execute the 10-task PLAN; the 36.1/36.2 split CONSUMED; the Load-balancing family stays OPEN; 5 policy candidates remain after 36)` — the phase-36 {ring_hash} BRAINSTORM is COMPLETE (`docs/envoy-go/phases/36-load-balancer-ring-hash/BRAINSTORM.md`); Q0 ring_hash / Q1 both planes / Q-seam EXTEND ADR-0232 via a ctx-carried hash key (exported `Cluster` surface byte-stable) / Q-split pre-authorized 36.1/36.2 by plane; 2 ADRs anticipated (ADR-0235 seam-ext + ADR-0236 policy); D-RH1..D-RH8 → SPEC; counts UNCHANGED at this brainstorm. **Superseded (the phase-35 IMPL lifecycle-state, preserved):** `phase 35 IMPL done — SKILL_ROUTING → the next-phase routing` — the phase-35 {random} IMPL is COMPLETE (`docs/envoy-go/phases/35-load-balancer-random/PROGRESS.md`; the 8-task TDD plan executed subagent-driven; the ADR-0234 stateless `randomLB` over the REUSED ADR-0232 seam landed; the `0060-lb-random` anti-skew fixture green with the IMPL-re-tuned `{6,40}` band + the 3 deliberate-break liveness proofs; six gates GREEN). ROADMAP row 35 flips `in-progress → done` at THIS phase-35 IMPL commit (a flat family row — NO parent rollup per ADR-0106; the Load-balancing family stays OPEN — 6 candidates remain: ring_hash, maglev, subset LB, locality-weighted LB, priority load balancing, panic thresholds). The DECISIONS tail advances **ADR-0233 → ADR-0234** at this IMPL (the §Decision/§Consequences body landed in-place per ADR-0044; next-free ADR-0235). The IMPL ran subagent-driven (`feedback_execution_style`) in the worktree `.worktrees/phase-35-impl` (`feedback_git_worktrees`); subagents committed LOCAL-ONLY (`feedback_subagents_no_push`); the controller squash-merges + pushes at stage-close (`feedback_push_to_origin`). **Superseded (the phase-35 PLAN lifecycle-state, preserved):** `phase 35 PLAN done — SKILL_ROUTING → the phase-35 IMPL (single flat row; NO escape valve; the Load-balancing family stays OPEN; 6 candidates remain after 35)` — the phase-35 {random} PLAN was COMPLETE (`docs/envoy-go/phases/35-load-balancer-random/PLAN.md`; the SPEC §10 spine decomposed into 8 bite-sized TDD tasks; the §12 D-S35-1..4 questions RESOLVED [D-S35-1 file split: `random.go` NEW sibling + the manager case/reject in `manager.go` + the retarget in `manager_test.go`; D-S35-2 `newRandom` calls `newPCGRNG` directly, accepts the shared seed-error message, NO wrapper; D-S35-3 band K=4/S=60/floor 12/ceiling 32/sum 64 + ≥20-run flake protocol + the 3 `-count=1` breaks; D-S35-4 a standalone deterministic-RNG anti-skew unit test]; the FINAL ADR-0045 split-gate re-check → NO SPLIT [8 tasks / ~45–60 prod LoC — both legs far under; NO escape valve]; plan-document-reviewer APPROVED — see the active-phase lead). ROADMAP row 35 STAYS `in-progress` (flat family row — it flips `in-progress → done` at the phase-35 IMPL six-gate, NO parent rollup per ADR-0106; the Load-balancing family stays OPEN, 6 candidates remain after 35). The next step is the **phase-35 IMPL** (`superpowers:subagent-driven-development` — execute the 8-task PLAN: T1 baselines/anchors gate + PROGRESS.md → T2 the `randomLB` stateless uniform-pick type (`random.go`; REUSES `newPCGRNG`; shared `noopRelease`) → T3 manager acceptance (the bare `case clusterv3.Cluster_RANDOM`) + the NEW reject text + the doubly-hit retarget of `TestManager_Error_UnsupportedLBPolicy` + mismatched-oneof silent-ignore → T4 the in-process anti-skew integration test + RANDOM boot smoke → T5 the `0060-lb-random` fixture (the SECOND band-based `AssertDistribution` — anti-skew conservation/floor 12/ceiling 32 + the cross/per-side `StatsAsserter`) → T6 band tuning + the 3 `-count=1` deliberate-break liveness proofs (consult-the-counters [floor leg] / pin-to-endpoints[0] / stats-prong) → T7 the full 62-dir differential re-verify + `-race -short` + conformance unaffected → T8 the completion bundle [BEHAVIOR_CONTRACT random delta + the ADR-0234 full entry (ADR-0044 in-place; tail → ADR-0234) + STATE/ROADMAP row 35 `in-progress → done` + six-gate evidence]). The IMPL runs subagent-driven (`feedback_execution_style`) in a worktree (`feedback_git_worktrees`); subagents commit LOCAL-ONLY (`feedback_subagents_no_push`); the controller squash-merges + pushes at stage-close (`feedback_push_to_origin`); per-task `gofmt -l` + `golangci-lint` (`feedback_pertask_gofmt_lint`); canonical paths pinned for worktree subagents (`feedback_subagent_worktree_path_targeting`). **Superseded (the phase-35 SPEC lifecycle-state, preserved):** `phase 35 SPEC done — SKILL_ROUTING → the phase-35 PLAN (single flat row; NO escape valve; the Load-balancing family stays OPEN; 6 candidates remain after 35)` — the phase-35 {random} SPEC is COMPLETE (the D-R1..D-R6 empirical pins executed live, AMEND-R1..R6 recorded [all CONFIRMED]; the ADR-0234 §Context DRAFT anchored in SPEC §13; the DECISIONS tail STAYS ADR-0233 at the SPEC per the phase-35 routing — the full ADR-0234 entry + body land at the IMPL per ADR-0044; spec-document-reviewer APPROVED). **Superseded (the phase-35 BRAINSTORM lifecycle-state, preserved):** `phase 35 BRAINSTORM done — SKILL_ROUTING → the phase-35 SPEC (single flat row; the Load-balancing family stays OPEN; 6 candidates remain after 35)` — the phase-35 {random} BRAINSTORM is COMPLETE (DESIGN-ONLY; `docs/envoy-go/phases/35-load-balancer-random/BRAINSTORM.md`; the 4-decision dialogue settled Q0 subject [`random`, the zero-churn ADR-0232 seam drop-in — enum value 3, NO config message] / Q1 LEAN envelope [manager-accept + stateless `randomLB` + the reject-text extension] / Q-seam [REUSE the ADR-0232 seam UNCHANGED — `noopRelease`, ZERO seam churn, ZERO seam ADR] / Q-split [single flat row, NO escape valve] + the anti-skew `0060-lb-random` differential — see the active-phase lead). ROADMAP row 35 registered `in-progress` (flat family row, no sub-phases — it flips `in-progress → done` at the phase-35 IMPL six-gate, NO parent rollup per ADR-0106); the Load-balancing family-heading gains a `random IN-PROGRESS` note (sibling rows NOT pre-populated). The next step is the **phase-35 SPEC** (`superpowers:writing-plans` scoped to SPEC authoring, the established cadence): execute the BRAINSTORM §10 **D-R1..D-R6** empirical pins IN-SESSION live against the contrib reference Envoy (`envoyproxy/envoy:contrib-v1.37.2`, ADR-0227) per `reference_docker_probe_bridge_network` (a shared bridge network + STRICT_DNS backend hostnames + verify traffic ran via `downstream_cx_rx_bytes_total > 0`); anchor the **ADR-0234** (the random policy) §Context draft (DECISIONS tail STAYS ADR-0233; the §Decision/§Consequences body lands at the IMPL per ADR-0044); the spec-document-reviewer gate applies; then PLAN → IMPL per the cadence. The downstream stages run subagent-driven (`feedback_execution_style`) in worktrees (`feedback_git_worktrees`); subagents commit LOCAL-ONLY (`feedback_subagents_no_push`); the controller squash-merges + pushes at stage-close (`feedback_push_to_origin`); per-task `gofmt -l` + `golangci-lint` (`feedback_pertask_gofmt_lint`); canonical paths pinned for worktree subagents (`feedback_subagent_worktree_path_targeting`). **Superseded (the phase-34 IMPL lifecycle-state, preserved):** `phase 34 IMPL done — SKILL_ROUTING → the next-phase routing (the Load-balancing family stays OPEN; 7 policy candidates remain)` — the phase-34 {least_request} IMPL is COMPLETE (`docs/envoy-go/phases/34-load-balancer-least-request/PROGRESS.md`; the 9-task TDD plan executed; the ADR-0232 acquire/release seam + the ADR-0233 P2C policy landed; six gates GREEN). ROADMAP row 34 flips `in-progress → done` at THIS phase-34 IMPL commit (a flat family row — NO parent rollup per ADR-0106; the Load-balancing family stays OPEN). **Superseded (the phase-34 PLAN lifecycle-state, preserved):** the phase-34 {least_request} PLAN is COMPLETE (`docs/envoy-go/phases/34-load-balancer-least-request/PLAN.md`; the SPEC §10 spine decomposed into 9 bite-sized TDD tasks [SPEC Tasks 2+3 merged — compiler-coupled]; the §12 D-S34-1..5 questions resolved; the ADR-0045 split-gate FINAL re-check → NO SPLIT, the 34.1/34.2 escape valve UNCONSUMED; plan-document-reviewer APPROVED — see the active-phase lead). ROADMAP row 34 STAYS `in-progress` (flat family row — it flips `in-progress → done` at the phase-34 IMPL six-gate, NO parent rollup per ADR-0106; the Load-balancing family stays OPEN, 7 candidates remain after 34). The next step is the **phase-34 IMPL** (`superpowers:subagent-driven-development` — execute the 9-task PLAN: T1 baselines/anchors gate + PROGRESS.md → T2 the `loadBalancer` seam reshape (interface + `roundRobin` no-op release + `cluster.go` release threading; ADR-0232 OPTION C; behavior-neutral) → T3 the `leastRequest` P2C type → T4 manager acceptance + `parseLeastRequestLbConfig` + the §6 reject matrix → T5 the in-process skew integration + boot smoke → T6 the `0059-lb-least-request` fixture (band `AssertDistribution` + cross/per-side `StatsAsserter`) → T7 band tuning + the 3 deliberate-break liveness proofs → T8 the full 61-dir differential re-verify + `-race -short` + conformance → T9 the completion bundle [BEHAVIOR_CONTRACT + ADR-0232/0233 bodies + STATE/ROADMAP row 34 `in-progress → done`]). The IMPL runs subagent-driven (`feedback_execution_style`) in a worktree (`feedback_git_worktrees`); subagents commit LOCAL-ONLY (`feedback_subagents_no_push`); the controller squash-merges + pushes at stage-close (`feedback_push_to_origin`); per-task `gofmt -l` + `golangci-lint` (`feedback_pertask_gofmt_lint`); canonical paths pinned for worktree subagents (`feedback_subagent_worktree_path_targeting`). **Superseded (the phase-34 SPEC lifecycle-state, preserved):** `phase 34 SPEC done — SKILL_ROUTING → the phase-34 PLAN (single flat row; the 34.1/34.2 escape valve unconsumed)` — the phase-34 {least_request} SPEC is COMPLETE (the D-L1..D-L7 pins executed live; AMEND-L1..L7 recorded; the ADR-0232 + ADR-0233 §Context drafts anchored [DECISIONS tail ADR-0231 → ADR-0233, next-free ADR-0234]; spec-document-reviewer APPROVED — see the active-phase lead). ROADMAP row 34 STAYS `in-progress` (flat family row — it flips `in-progress → done` at the phase-34 IMPL six-gate, NO parent rollup per ADR-0106; the Load-balancing family stays OPEN, 7 candidates remain after 34). The next step is the **phase-34 PLAN** (`superpowers:writing-plans` — decompose the SPEC §10 ~10-task spine into bite-sized TDD tasks; resolve the §12 D-S34-1..5 questions; FINAL ADR-0045 split-gate re-check). The downstream stages run subagent-driven (`feedback_execution_style`) in worktrees (`feedback_git_worktrees`); subagents commit LOCAL-ONLY (`feedback_subagents_no_push`); the controller squash-merges + pushes at stage-close (`feedback_push_to_origin`); per-task `gofmt -l` + `golangci-lint` (`feedback_pertask_gofmt_lint`); canonical paths pinned for worktree subagents (`feedback_subagent_worktree_path_targeting`). **Superseded (the phase-34 BRAINSTORM lifecycle-state, preserved):** `phase 34 BRAINSTORM done — SKILL_ROUTING → the phase-34 SPEC (single flat row; the Load-balancing family OPENS at phase 34)` — the phase-34 {least_request} BRAINSTORM is COMPLETE (DESIGN-ONLY; `docs/envoy-go/phases/34-load-balancer-least-request/BRAINSTORM.md`; the 4-question dialogue settled Q0 family+subject [the Load-balancing family OPENS; `least_request` first — with the v1.32.4 three-knob proto verification + the FULL_SCAN-knob-ABSENT finding] / Q1 LEAN envelope / Q-seam the LB acquire/release extension in `internal/cluster` [the first seam outside `internal/filter/network`] / Q-split single flat row + a pre-authorized 34.1/34.2 escape valve — see the active-phase lead). ROADMAP row 34 registered `in-progress` (flat family row, no sub-phases — it flips `in-progress → done` at the phase-34 IMPL six-gate, NO parent rollup per ADR-0106); the Load-balancing family marked OPEN (sibling rows NOT pre-populated). The next step is the **phase-34 SPEC** (`superpowers:writing-plans` scoped to SPEC authoring, the established cadence): execute the BRAINSTORM §10 **D-L1..D-L7** empirical pins IN-SESSION live against the contrib reference Envoy (`envoyproxy/envoy:contrib-v1.37.2`, ADR-0227) per the docker-probe discipline (`reference_docker_probe_bridge_network` — a shared bridge network + STRICT_DNS backend hostnames + verify traffic ran via `downstream_cx_rx_bytes_total > 0`); anchor the **ADR-0232** (seam) + **ADR-0233** (policy) §Context drafts (DECISIONS tail STAYS ADR-0231; the §Decision/§Consequences bodies land at the IMPL per ADR-0044); the spec-document-reviewer gate applies; then PLAN → IMPL per the cadence. The downstream stages run subagent-driven (`feedback_execution_style`) in worktrees (`feedback_git_worktrees`); subagents commit LOCAL-ONLY (`feedback_subagents_no_push`); the controller squash-merges + pushes at stage-close (`feedback_push_to_origin`); per-task `gofmt -l` + `golangci-lint` (`feedback_pertask_gofmt_lint`); canonical paths pinned for worktree subagents (`feedback_subagent_worktree_path_targeting`). **Superseded (the phase-33 IMPL lifecycle-state, preserved):** `phase 33 IMPL done — phase 33 CLOSED; the §9 Network-filters family CLOSES (0 candidates remain) — SKILL_ROUTING → the next phase is a NEW feature family/row (TBD; `superpowers:brainstorming`)` — the phase-33 {thrift} IMPL is COMPLETE (`docs/envoy-go/phases/33-network-filter-thrift-proxy/PROGRESS.md`; all 16 TDD tasks landed subagent-driven; six gates GREEN; the ADR-0231 §Decision/§Consequences body landed in-place per ADR-0044; the two Task-12 per-side findings + the close-direction coverage boundary recorded). ROADMAP row 33 is now `done` (flipped `in-progress → done` at THIS IMPL six-gate — a flat §9 row, NO parent rollup per ADR-0106). **The §9 Network-filters family CLOSES — thrift was the LAST candidate; 0 candidates remain.** The next step is a **NEW feature family/row** (the §9 Network-filters family is exhausted; the ROADMAP §§ list the remaining unopened families — Load balancing, Upstream robustness, HTTP/3 + QUIC, gRPC, etc.); the next subject is TBD via `superpowers:brainstorming`. The IMPL ran subagent-driven (`feedback_execution_style`) in a worktree (`feedback_git_worktrees`); subagents committed LOCAL-ONLY (`feedback_subagents_no_push`); the controller squash-merges + pushes at stage-close (`feedback_push_to_origin`). The DECISIONS tail STAYS **ADR-0231** at this IMPL commit (§Decision/§Consequences body landed in-place; next-free ADR-0232). **Superseded (the phase-33 PLAN lifecycle-state, preserved):** `phase 33 PLAN done — SKILL_ROUTING → the phase-33 IMPL (single flat row; the §9 family CLOSES after phase 33 phase-done)` — the phase-33 {thrift} PLAN is COMPLETE (`docs/envoy-go/phases/33-network-filter-thrift-proxy/PLAN.md`; the SPEC §10 spine decomposed into 16 bite-sized TDD tasks; the ADR-0045 split-gate FINAL re-check → NO SPLIT, the 33.1/33.2 escape-valve UNCONSUMED; the §12 D-questions D-S33-1..6 resolved — see the active-phase lead; plan-document-reviewer APPROVED). ROADMAP row 33 STAYS `in-progress` (flat §9 row, no sub-phases — it flips `in-progress → done` at the phase-33 IMPL six-gate, NO parent rollup). The next step is the **phase-33 IMPL** (`superpowers:subagent-driven-development` — execute the 16-task PLAN; Task 1 the baselines/anchors gate + PROGRESS.md → Task 2 the package skeleton + TypeURL + bootstrap blank-import → Task 3 config parse → Task 4 the route table → Task 5 the framed-binary decode → Task 6 the reply classifier + `UnknownMethod` encoder → Task 7 the 25-name roster → Task 8 the `Handle` pump → Task 9 the 11th built-in → Task 10 the `thrift.` prom arm → Task 11 `TCPThriftResponder` → Task 12 `0057` + Task 13 `0058` fixtures → Task 14 `FuzzThriftDecode` → Task 15 the full differential re-verify + deliberate-break liveness → Task 16 the completion bundle [BEHAVIOR_CONTRACT 1091→1116 + the ADR-0231 §Decision/§Consequences body + STATE/ROADMAP row 33 `in-progress → done` + six-gate evidence]). The IMPL runs subagent-driven (`feedback_execution_style`) in a worktree (`feedback_git_worktrees`); subagents commit LOCAL-ONLY (`feedback_subagents_no_push`); the controller squash-merges + pushes at stage-close (`feedback_push_to_origin`). After phase 33 phase-done, the §9 Network-filters family CLOSES (0 candidates remain). The DECISIONS tail STAYS **ADR-0231** at this PLAN commit (§Context anchored at the SPEC; the §Decision/§Consequences body lands at the IMPL per ADR-0044; next-free ADR-0232). **Superseded (the phase-33 SPEC lifecycle-state, preserved):** `phase 33 SPEC done — SKILL_ROUTING → the phase-33 PLAN (single flat row; the §9 family CLOSES after phase 33 phase-done)` — the phase-33 {thrift} SPEC was COMPLETE (the D-T1..D-T9 empirical pins executed live, AMEND-T1..T7 recorded; the ADR-0231 §Context anchored; spec-document-reviewer APPROVED). **Superseded (the phase-33 BRAINSTORM lifecycle-state, preserved):** `phase 33 BRAINSTORM done — SKILL_ROUTING → the phase-33 SPEC (single flat row; the §9 family CLOSES after phase 33 phase-done)` — the phase-33 {thrift} BRAINSTORM was COMPLETE (DESIGN-ONLY; `docs/envoy-go/phases/33-network-filter-thrift-proxy/BRAINSTORM.md`; the 4-question dialogue settled Q0 subject-confirm-with-the-core-`/envoy`-correction / Q1 single-pair single-route-terminal envelope / Q-seam REUSE the ADR-0230 seam unchanged / Q-split single flat row + a pre-authorized 33.1/33.2 escape-valve). **Superseded (the prior lifecycle-state, preserved):** `phase 32.2 IMPL done — SKILL_ROUTING → the next phase (the §9 family stays OPEN; {thrift} remains)` — the phase-32.2 IMPL is COMPLETE (`docs/envoy-go/phases/32.2-network-filter-redis-commands-and-stats/PROGRESS.md`; all 12 tasks landed subagent-driven; six gates GREEN; the 5 as-built deviations recorded). The phase-32 `redis_proxy` parent row is DONE (the parent-row-32 ROLLUP landed ATOMICALLY at this 32.2 IMPL — parent row 32 + sub-row 32.2 `in-progress → done` per ADR-0106(d) / the 18/19/22/24/25/26/28/29 precedent); the SIXTH §9 Network-filters-family row CLOSES. The §9 Network-filters family stays OPEN — **{thrift} remains the SOLE candidate** (the LAST §9 candidate; after it the family closes). The next step is the **phase-33 {thrift} network-filter BRAINSTORM** (`superpowers:brainstorming`) — thrift_proxy is a terminal-proxy surface (transport×protocol matrix + method-routing + a nested thrift-filter sub-chain) that REUSES the 32.1 upstream-pool seam (ADR-0230); like kafka_broker it is a `/contrib` extension (the contrib go-control-plane dep + the contrib reference image are already in place from phases 30/31). NO new ADR was consumed at the 32.2 IMPL (DECISIONS tail STAYS ADR-0230; ADR-0229 now ACCEPTED — its 32.2 half landed in-place; next-free ADR-0231). **Earlier (32.2 PLAN, preserved):** plan-document-reviewer APPROVED iteration 2 (the Task 3 Step 5 `resp_test.go` caller blocking issue fixed in the loop); the SPEC §14 spine decomposed into 12 bite-sized TDD tasks; ADR-0045 split-gate FINAL re-check NO split (the 32.2a/32.2b escape-valve UNCONSUMED). **Earlier (32.2 SPEC findings, preserved):** resolved D-P32-2 (the EXACT 180-command table, the success/error single-server semantics), D-P32-6 (full local-reply extent; TIME non-deterministic; HELLO split), D-P32-7 (IsValidName by construction), D-P32-8 (gauge design — 2 lifecycle gauges filter-driven, 2 buffered gauges framework-managed-upstream coverage boundary), D-P32-9 (one-conn-per-downstream per-side asymmetry). **Earlier (32.1 IMPL, DONE):** the SPEC §14 16-task spine executed end-to-end — Task 1 the baselines/anchors gate → the `redisproxy` skeleton+TypeURL → `config.go` parse → byte-stable rejects → `resp.go` request/reply decode+encode → `upstreampool.go` seam → `commands.go` PING/AUTH → `stats.go` 10-name roster → `filter.go` Handle pump → 10th built-in+bootstrap → `TCPRedisResponder` → `0055`/`0056` fixtures → the NEW `GET /stats` flat admin endpoint (the eighth admin endpoint, a Task-14 addition required for the `StatsAsserter`) → the completion bundle. The 32.1 IMPL filled the ADR-0230 §Decision/§Consequences body + the ADR-0229 §Decision/§Consequences (32.1 half, status PARTIAL) in place per ADR-0044; advanced counts (fixtures 56→**58**, fuzzers **40** unchanged, stat surface 536→**546** [+10: 6 downstream counters + 4 gauges], BackendKind 31→**32**; R9). Phases 29 + 30 + 31 are CLOSED. ROADMAP: parent row 32 `in-progress`; sub-row 32.1 `done` (flipped `in-progress → done` at THIS 32.1 IMPL commit per the 26.x/28.x/29.x precedent); sub-row 32.2 `planned`; rows 26/27/28/29/30/31 `done`. The §9 Network-filters family stays OPEN (1 candidate {thrift} remains after phase 32).
- **next-skill:** **`superpowers:subagent-driven-development`** for the **36.1 IMPL** (execute the 10-task `PLAN.md`; the per-task gofmt+lint + LOCAL-ONLY commits + controller squash/push discipline) for phase 36 (the kafka-31/thrift-33/least_request-34/random-35 precedent — execute the §10 D-RH1..D-RH8 empirical pins IN-SESSION against the contrib reference per `reference_docker_probe_bridge_network`; anchor the ADR-0235 + ADR-0236 §Context drafts; make the FINAL ADR-0045 36.1/36.2 split decision). The controller squash-merges the `phase-36-ring-hash-brainstorm` worktree + pushes at stage-close (this BRAINSTORM commit). **Superseded (the phase-35 next-skill, preserved):** **the next-phase routing** (`SKILL_ROUTING.md` / `BOOTSTRAP §5`) — the phase-35 {random} IMPL is COMPLETE; the controller squash-merges the `phase-35-impl` worktree + pushes at stage-close, then routes to the next Load-balancing-family phase (the family stays OPEN — **6** policy candidates remain after 35: ring_hash, maglev, subset LB, locality-weighted LB, priority load balancing, panic thresholds; the ADR-0232 seam is the reusable structural asset — random just consummated its FIRST reuse, and the hash policies ring_hash/maglev additionally need the ADR-0232 `Pick(hashInput)` widening). Honor `feedback_execution_style` (the downstream IMPL runs subagent-driven) + `feedback_git_worktrees` (work in a worktree) + `feedback_push_to_origin` / `feedback_subagents_no_push` (the controller pushes at stage-close; subagents commit local-only) + `feedback_pertask_gofmt_lint` + `feedback_subagent_worktree_path_targeting`. Read first: `docs/envoy-go/ROADMAP.md` (the Load-balancing family § + rows 34/35 DONE) + `docs/envoy-go/STATE.md` (this file) + `docs/envoy-go/DECISIONS.md` tail ADR-0234 (+ ADR-0232 the LB seam — the §Consequences "RANDOM drops in at zero cost" CONSUMMATED + the `Pick(hashInput)` widening note the hash policies need) + the BOOTSTRAP/SKILL_ROUTING docs for the family-selection cadence. **Superseded (the phase-35 SPEC/PLAN next-skill, preserved):** **`superpowers:writing-plans`** — scoped to the **phase-35 SPEC** (`load-balancer-random`, single flat row — the kafka-31/thrift-33/least_request-34 precedent): executed the BRAINSTORM §10 **D-R1..D-R6** empirical pins IN-SESSION (parallel-subagent fan-out) against the contrib reference Envoy (`envoyproxy/envoy:contrib-v1.37.2`, ADR-0227) per `reference_docker_probe_bridge_network`, and anchor the **ADR-0234** (the random policy) §Context draft (DECISIONS tail STAYS ADR-0233 → next-free ADR-0234; body at the IMPL per ADR-0044; NO seam ADR — the seam is reused unchanged); the spec-document-reviewer gate applies; counts stay UNCHANGED at the SPEC. The D-R pins: **D-R1** re-pin the v1.32.4 surface (RANDOM = enum 3, NO `Cluster.RandomLbConfig`) + clean `go mod tidy` (ZERO new dep) + the mismatched-oneof silent-ignore disposition / **D-R2** the accept/reject matrix + the THREE-site reject-text blast radius (`manager.go:252` + `manager_test.go:322` doubly-hit retarget RANDOM → RING_HASH/MAGLEV + `BEHAVIOR_CONTRACT.md:899`; confirm no fixture pins → no boot-reject dir) / **D-R3** confirm the reference's RANDOM stays UNIFORM under the `0059` held-conn load (the anti-skew core claim) + pin the `0060` band constants + the ≥20-run flake protocol + the deliberate-break / **D-R4** the zero stat-delta probe + the `0060` `StatsAsserter` set (anticipated identical to `0059`) / **D-R5** the uniform-sampling/RNG semantics (full-endpoint-set, no health filtering; REUSE the `leastRequest` injectable mutex-guarded crypto-seeded PCG — confirm `newPCGRNG` sharable; `errNoEndpoints` + `noopRelease` parity) / **D-R6** the ADR-0045 envelope re-check (~40–90 prod LoC / ~4–7 tasks, NO escape valve) + confirm the seam REUSES unchanged (no `Pick`-signature change, no consumer touch, ADR-0024 unamended) + the single ADR. Honor `feedback_execution_style` (the downstream IMPL runs subagent-driven) + `feedback_git_worktrees` (work in a worktree) + `feedback_push_to_origin` / `feedback_subagents_no_push` (the controller pushes at stage-close; subagents commit local-only) + `feedback_pertask_gofmt_lint` + `feedback_subagent_worktree_path_targeting`. Read first: `docs/envoy-go/phases/35-load-balancer-random/BRAINSTORM.md` (the worked record — the 12 §§ + the D-R1..D-R6 pins + the enum-3 / no-config-message verification) + `docs/envoy-go/ROADMAP.md` (the Load-balancing family § + row 35) + `docs/envoy-go/STATE.md` (this file) + `docs/envoy-go/DECISIONS.md` tail ADR-0233 + **ADR-0232** (the LB seam — READ the §Consequences "RANDOM drops in at zero cost" + the `Pick(hashInput)` widening note) + ADR-0233 (the least_request policy — the RNG posture random reuses) + ADR-0106/0045/0044/0052/0080/0024 + `docs/envoy-go/BEHAVIOR_CONTRACT.md:899` (the lb-policy reject-text pinner) + `internal/cluster/{loadbalancer,leastrequest,manager,cluster}.go` (the seam + the `noopRelease` + the `leastRequest` RNG helpers + the lb_policy switch ~`manager.go:234`/the reject text ~`manager.go:252`) + `internal/cluster/manager_test.go:320` (`TestManager_Error_UnsupportedLBPolicy` — the doubly-hit retarget) + `test/fixtures/0059-lb-least-request/` (the held-conn skew driver `0060` transposes — the contrapositive assertion) + `test/differential/fixture/fixture.go` (the `DistributionAsserter`) + project memories (`reference_docker_probe_bridge_network`, `reference_differential_break_protocol_count1`, `reference_differential_asserter_dispatch`, `reference_differential_run_selector`, `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_subagents_no_push`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_path_targeting`). **Superseded (the post-phase-34 next-phase-routing next-skill, preserved):** **the next-phase routing** (`SKILL_ROUTING.md` / `BOOTSTRAP §5`) — the phase-34 IMPL is COMPLETE; the controller squash-merges the `phase-34-impl` worktree + pushes at stage-close, then routes to the next Load-balancing-family phase (the family stays OPEN — 7 policy candidates remain: random/ring_hash/maglev + subset-LB/locality-weighted/priority/panic-thresholds; the ADR-0232 seam is the reusable structural asset). **Superseded (the phase-34 PLAN next-skill, preserved):** the phase-34 IMPL next-skill was **`superpowers:subagent-driven-development`** scoped to the IMPL: execute the 9-task PLAN (`docs/envoy-go/phases/34-load-balancer-least-request/PLAN.md`) task-by-task — a fresh subagent per task; TDD per task (write-failing-test → run-fail → minimal-impl → run-pass); per-task `gofmt -l` + `golangci-lint` on touched pkgs (`feedback_pertask_gofmt_lint`); subagents commit LOCAL-ONLY (`feedback_subagents_no_push`); the controller squash-merges + pushes at stage-close (`feedback_push_to_origin`); work in a worktree (`feedback_git_worktrees`); canonical paths pinned (`feedback_subagent_worktree_path_targeting`). The 9 tasks: **T1** baselines/anchors gate + PROGRESS.md / **T2** the `loadBalancer` seam reshape — `Pick() (Endpoint, func(), error)` + `roundRobin` no-op release + `cluster.go` release threading (`PickEndpoint` immediate-release; `Dial`/`AcquireH1` release-on-failure + the `connWithGauge` dec composition; pool-hit immediate-release) — ADR-0232 OPTION C, behavior-neutral (all 60 fixtures stay byte-exact; tested via a stub-LB counter-balance suite) / **T3** the `leastRequest` P2C type (`leastrequest.go` — with-replacement, strict `<`, first-drawn ties, no-clamp; mutex-guarded crypto-seeded injectable RNG; `sync.Once` release) / **T4** manager acceptance + `parseLeastRequestLbConfig` (hand-rolled `choice_count >= 2`; bias/slow_start DEPARTURE rejects; mismatched-oneof silent-ignore; the NEW lb-policy reject text `supported: ROUND_ROBIN, LEAST_REQUEST`; retarget `TestManager_Error_NonRoundRobinLB` → RANDOM) / **T5** the in-process skew integration test (held picks elevate counters; the heaviest endpoint stops being picked; `-count=1` inverted-comparison liveness) + LEAST_REQUEST boot smoke / **T6** the `0059-lb-least-request` fixture (hold-4/burst-60/drain over LEAST_REQUEST cc=10; the FIRST band-based `AssertDistribution` [conservation/starvation/concentration] + the cross/per-side `StatsAsserter`; reuses `TCPEcho` — NO new BackendKind) / **T7** band tuning (≥20 runs flake-free per side) + the 3 `-count=1` deliberate-break liveness proofs / **T8** the full 61-dir differential re-verify (60 prior byte-exact through the reshape + `0059` green) + `-race -short` + h2spec 53/53 + proxy-wasm 10/10 + `go mod tidy -diff` empty / **T9** the completion bundle (BEHAVIOR_CONTRACT least_request delta + the ADR-0232/0233 §Decision/§Consequences bodies in-place per ADR-0044 + STATE/ROADMAP row 34 `in-progress → done`). At the IMPL six-gate the counts advance: fixtures 60 → **61** ONLY (`0059`); stat surface STAYS **1116** (ZERO delta — the first zero-delta phase), fuzzers STAY **42** (NO new fuzzer — the first no-fuzzer phase), BackendKind STAYS **33** (NO new BackendKind); DECISIONS tail STAYS **ADR-0233** (bodies land in-place, NO new number). Read first: `docs/envoy-go/phases/34-load-balancer-least-request/PLAN.md` (the 9-task spine + the D-S34-1..5 resolutions + the ADR-0045 NO-SPLIT re-check) + the SPEC (§1.1 AMEND-L1..L7 + §11 the D-L pins — AUTHORITATIVE) + this STATE + DECISIONS ADR-0232/0233/0024/0063 + `internal/cluster/{loadbalancer,leastrequest[NEW],manager,cluster}.go` + `test/fixtures/0001-tcp-proxy-rr/driver/driver.go` (the harness shape `0059` transposes) + `test/fixtures/0057-thrift-roundtrip/driver/driver.go` (the `StatsAsserter`/`scrapeStats` pattern) + `test/differential/runner_test.go` (`acceptEchoCounting` ~1329 + the asserter hooks ~1102/1158) + project memories (`feedback_execution_style`, `feedback_git_worktrees`, `feedback_subagents_no_push`, `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_path_targeting`, `reference_differential_break_protocol_count1`, `reference_differential_asserter_dispatch`, `reference_differential_fixture_dispatch_constraint`, `reference_docker_probe_bridge_network`). **Superseded (the phase-34 PLAN next-skill, preserved):** **`superpowers:writing-plans`** — scoped to the **phase-34 PLAN** (`load-balancer-least-request`, single flat row): decompose the SPEC §10 ~10-task spine into bite-sized TDD tasks (write-failing-test → run-fail → minimal-impl → run-pass → per-task `gofmt -l` + `golangci-lint` per `feedback_pertask_gofmt_lint` → LOCAL-ONLY commit per `feedback_subagents_no_push`); resolve the SPEC §12 D-questions (D-S34-1 the file split [`leastrequest.go` sibling anticipated] / D-S34-2 the production RNG source + seeding [`math/rand/v2` PCG crypto-seeded anticipated; injectable `rng func() uint64`] / D-S34-3 the FINAL band constants [K=4/S=60/c1≤12/c2≥16 anticipated] + the ≥20-repeat-run flake protocol + the three `-count=1` deliberate-break proofs / D-S34-4 the held-conn establishment witness [write+read-echo per held conn] / D-S34-5 the `connWithGauge` release-composition shape [compose into the existing `dec` closure anticipated]); FINAL ADR-0045 split-gate re-check (~155–255 prod LoC / ~10 tasks → NO split anticipated; the pre-authorized 34.1-seam/34.2-policy escape valve stays available). plan-document-reviewer gate applies. Counts stay UNCHANGED at the PLAN (they move at the IMPL: fixtures 60 → 61 ONLY). Read first: `docs/envoy-go/phases/34-load-balancer-least-request/SPEC.md` (AUTHORITATIVE — §1.1 AMEND-L1..L7; §3 the OPTION-C seam + the `leastRequest` design; §5/§6 the proto + reject matrices; §7 the zero-delta stat surface + the StatsAsserter set; §8 the `0059` band-arm design; §10 the task spine; §11 the D-L pin record; Appendix A the live probe basis) + BRAINSTORM.md + ROADMAP row 34 + this STATE + DECISIONS ADR-0232/0233 (§Context, THIS SPEC commit) + ADR-0024 (cross-referenced, unamended) + ADR-0063 (the `connWithGauge` attach-point) + `internal/cluster/{loadbalancer,manager,cluster}.go` (the seam sites) + `test/fixtures/0001-tcp-proxy-rr/driver/driver.go` (the harness shape `0059` transposes) + `test/differential/runner_test.go` (`acceptEchoCounting` ~line 1329 + the DistributionAsserter hook ~line 1102) + project memories (`feedback_execution_style`, `feedback_git_worktrees`, `feedback_subagents_no_push`, `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_path_targeting`, `reference_differential_break_protocol_count1`, `reference_differential_asserter_dispatch`, `reference_differential_fixture_dispatch_constraint`). **Superseded (the phase-34 SPEC next-skill, preserved):** **`superpowers:writing-plans`** — scoped to the **phase-34 SPEC** (`load-balancer-least-request`, single flat row — the kafka-31/thrift-33 precedent): execute the BRAINSTORM §10 **D-L1..D-L7** empirical pins IN-SESSION (parallel-subagent fan-out) against the contrib reference Envoy (`envoyproxy/envoy:contrib-v1.37.2`, ADR-0227) per `reference_docker_probe_bridge_network`, and anchor the **ADR-0232** (the LB acquire/release seam) + **ADR-0233** (the least_request policy) §Context drafts (DECISIONS tail ADR-0231 → next-free ADR-0232; bodies at the IMPL per ADR-0044); the spec-document-reviewer gate applies; counts stay UNCHANGED at the SPEC. Read first: `docs/envoy-go/phases/34-load-balancer-least-request/BRAINSTORM.md` (the worked record — the 12 §§ + the D-L1..D-L7 pins + the v1.32.4 three-knob verification) + `docs/envoy-go/ROADMAP.md` (the Load-balancing family § + row 34) + `docs/envoy-go/STATE.md` (this file) + `docs/envoy-go/DECISIONS.md` tail ADR-0231 + ADR-0106/0045/0044/0052/0060 + **ADR-0024** (the per-cluster RR counter-scope ADR — the seam touches its territory; no amendment anticipated) + `docs/envoy-go/BEHAVIOR_CONTRACT.md` + `internal/cluster/{loadbalancer,manager,cluster}.go` (the seam site + the lb_policy guard ~`manager.go:216` + the `PickEndpoint`/`Dial`/`connWithGauge` funnel) + `test/differential/fixture/fixture.go` (the `DistributionAsserter`, ~line 57) + `test/fixtures/0001-tcp-proxy-rr/` (the echo backends + distribution-driver precedent) + project memories (`reference_docker_probe_bridge_network`, `reference_differential_break_protocol_count1`, `reference_differential_asserter_dispatch`, `reference_differential_fixture_dispatch_constraint`, `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_subagents_no_push`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_path_targeting`). **Superseded (the post-phase-33 family-selection next-skill, preserved):** **`superpowers:brainstorming`** — the §9 Network-filters family is now CLOSED (phase 33 `thrift_proxy` was the LAST candidate; 0 remain), so the NEXT phase opens a NEW feature family/row. The subject is TBD — the ROADMAP §§ enumerate the remaining unopened families (Load balancing [least_request/random/ring_hash/maglev/subset/locality-weighted/priority/panic-thresholds], Upstream robustness [active health checks / outlier detection / circuit breakers / retries+hedging / per-protocol pooling], HTTP/3 + QUIC, gRPC [bridge / gRPC-Web / gRPC-JSON transcoding]). Honor `feedback_execution_style` (the downstream IMPL runs subagent-driven) + `feedback_git_worktrees` (work in a worktree) + `feedback_push_to_origin` / `feedback_subagents_no_push` (the controller pushes at stage-close; subagents commit local-only) + `feedback_pertask_gofmt_lint`. Read first: `docs/envoy-go/ROADMAP.md` (the family §§ + the now-CLOSED §9 Network-filters family) + `docs/envoy-go/STATE.md` (this file) + the BOOTSTRAP/SKILL_ROUTING docs for the family-selection cadence. **Superseded (the phase-33 IMPL next-skill, preserved):** **`superpowers:subagent-driven-development`** — scoped to the **phase-33 {thrift} network-filter IMPL** (`thrift_proxy`, `envoy.filters.network.thrift_proxy`, proto `envoy.extensions.filters.network.thrift_proxy.v3.ThriftProxy`), a SINGLE FLAT ROW (the kafka-31 precedent — NO parent-SPEC/sub-phase split; the upstream-pool seam is REUSED, not built). EXECUTED the 16-task PLAN (`docs/envoy-go/phases/33-network-filter-thrift-proxy/PLAN.md`) task-by-task: a fresh subagent per task; TDD per task (write-failing-test → run-fail → minimal-impl → run-pass); per-task `gofmt -l` + `golangci-lint` on touched pkgs per `feedback_pertask_gofmt_lint`; subagents commit LOCAL-ONLY per `feedback_subagents_no_push`; the controller squash-merges + pushes at stage-close per `feedback_push_to_origin`; work in a worktree per `feedback_git_worktrees`. The 16 tasks (PLAN §): T1 baselines/anchors gate + PROGRESS.md / T2 package skeleton + TypeURL (`proto.MessageName` pin) + `thrift_proxy/v3` blank-import / T3 config parse (`stat_prefix` required + IsValidName + transport/protocol departure rejects + payload_passthrough) / T4 the `route_config` method-routing table / T5 framed-binary frame + message-begin decode / T6 reply classifier + `UnknownMethod` `AppException` encoder / T7 the EAGER 25-name roster / T8 the `TerminalFilter.Handle` pump / T9 the 11th built-in / T10 the `thrift.` SINGLE-label-hoist prom arm (`name.go`) / T11 `TCPThriftResponder` BackendKind 33 / T12 `0057-thrift-roundtrip` cross-side fixture (HIT + MISS + reply-EXCEPTION arms) / T13 `0058-thrift-boot-reject` / T14 the 42nd fuzzer `FuzzThriftDecode` / T15 full differential re-verify (60 dirs byte-exact) + deliberate-break liveness (`-count=1`) / T16 the completion bundle. At the IMPL six-gate the counts advance (stat surface 1091 → **1116**, fixtures 58 → **60**, fuzzers 41 → **42**, BackendKind tail 32 → **33**); the ADR-0231 §Decision/§Consequences body lands IN-PLACE (ADR-0044, NO new number); ROADMAP row 33 flips `in-progress → done` (flat §9 row — NO parent rollup) + the §9 Network-filters family CLOSES. Read first: `docs/envoy-go/phases/33-network-filter-thrift-proxy/PLAN.md` (the 16-task spine + the D-S33-1..6 resolutions + the ADR-0045 NO-SPLIT re-check) + the SPEC (§1.1 AMEND-T1..T7, §11 the D-T1..D-T9 pins + Appendix A the framed×binary wire format — AUTHORITATIVE) + ROADMAP row 33 + this STATE + DECISIONS ADR-0231/0230/0229 + `internal/filter/network/upstreampool.go` (the REUSED seam) + `internal/filter/network/redisproxy/{redisproxy,config,resp,stats,filter,commands}.go` (the package shape) + `internal/stats/name.go` (the `redis.` arm the `thrift.` arm mirrors, line 315) + `internal/filter/network/builtins/builtins.go` (the registration site) + `internal/bootstrap/bootstrap.go` (the blank-import block ~line 119) + `test/differential/fixture/fixture.go` (BackendKind tail 32 + `StatsAsserter`) + `test/fixtures/0055-redis-roundtrip/` (the cross-side fixture template) + project memories (`reference_close_direction_framework_gap`, `reference_wire_format_both_sides_see_same_bytes`, `reference_differential_reference_parses_full_message`, `reference_differential_break_protocol_count1`, `reference_differential_asserter_dispatch`, `reference_dynamic_stat_name_charset_guard`, `reference_network_filter_typeurl_extensions`, `reference_docker_probe_bridge_network`, `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_subagents_no_push`, `feedback_pertask_gofmt_lint`). **Superseded (the phase-33 PLAN next-skill, preserved):** `superpowers:writing-plans` scoped to the phase-33 PLAN — decomposed SPEC §10 into 16 bite-sized TDD tasks; re-checked the ADR-0045 split-gate (NO SPLIT); resolved the §12 D-questions (D-S33-1..6); plan-document-reviewer APPROVED (two advisory nits pre-corrected). **Superseded (the phase-33 SPEC next-skill, preserved):** `superpowers:writing-plans` scoped to the phase-33 SPEC — the SPEC executed the BRAINSTORM §10 **D-T1..D-T9** empirical pins IN-SESSION (parallel-subagent fan-out) against the contrib reference Envoy (`envoyproxy/envoy:contrib-v1.37.2`, ADR-0227) per `reference_docker_probe_bridge_network`: D-T1 (TypeURL via `proto.MessageName` + confirm the CORE `/envoy v1.32.4` module — anticipated `github.com/envoyproxy/go-control-plane/envoy/extensions/filters/network/thrift_proxy/v3`, NOT `/contrib` — + a clean `go mod tidy`, ZERO new dep), D-T2 (the transport×protocol PAIR the MVP decodes + the wire format — transport frame + message-begin [method name + message type + `seq_id`] + the struct-skip for `payload_passthrough`), D-T3 (the global `thrift.<stat_prefix>.request*/response*` stat roster + eager-vs-dynamic + the `thrift.` prom-arm shape — INLINE/MULTI-label/SINGLE-hoist), D-T4 (config PGV/parse arms + the `0058` boot-reject arm), D-T5 (router/local-reply semantics — does the reference answer anything locally?), D-T6 (the seq_id echo + the positional-correlation contract), D-T7 (IsValidName placement — satisfied-by-construction unless a per-route/method stat is wire-derived), D-T8 (the close-direction `cx_destroy_local/remote_with_active_rq` deferral as a coverage boundary), D-T9 (the ADR-0045 envelope re-check + the per-side `upstream_cx_*` pinning) — and anchors the **ADR-0231 §Context** (DECISIONS tail ADR-0230 → ADR-0231; the §Decision/§Consequences body lands at the IMPL per ADR-0044). The MVP envelope (BRAINSTORM Q1): a SINGLE-PAIR SINGLE-ROUTE-TERMINAL — ONE transport×protocol pair under `payload_passthrough` + a match-all/single-method `route_config` route to ONE cluster + a router-only nested sub-chain + the global request/response stats; the ADR-0230 upstream-pool seam REUSED UNCHANGED (Q-seam — one-conn-per-downstream synchronous single-flight, positional/FIFO; thrift is its anticipated SECOND consumer). Honor `feedback_execution_style` (the downstream IMPL runs subagent-driven) + `feedback_git_worktrees` (work in a worktree) + `feedback_push_to_origin` / `feedback_subagents_no_push` (the controller pushes at stage-close; subagents commit local-only) + `feedback_pertask_gofmt_lint`. Read first: `docs/envoy-go/phases/33-network-filter-thrift-proxy/BRAINSTORM.md` (this brainstorm — the 12 §§ + the D-T1..D-T9 pins) + `docs/envoy-go/ROADMAP.md` §9 Network-filters family (rows 26-32 DONE; row 33 `in-progress`; thrift the sole open candidate — the family closes after 33) + `docs/envoy-go/STATE.md` (this file) + `docs/envoy-go/DECISIONS.md` ADR-0230 (the upstream-pool seam — REUSED unchanged) + ADR-0229 (the redis_proxy as-built — the terminal-routing-proxy precedent) + ADR-0228 (kafka_broker — the single-flat-row precedent) + ADR-0227 (the contrib pin) + ADR-0106/0045/0044/0052/0060 + the phase-32 as-built (`docs/envoy-go/phases/32-network-filter-redis-proxy/` + `32.1`/`32.2` — the seam + codec + command-set + roster + the `redis.` prom arm + the `0055`/`0056` fixtures) + `internal/filter/network/upstreampool.go` (the seam thrift REUSES unchanged) + `internal/filter/network/redisproxy/` (the terminal-proxy package shape thrift mirrors) + `internal/stats/name.go` (the kafka INLINE / mongo MULTI-label / redis SINGLE-label-hoist prom-arm contrast — thrift picks one at SPEC) + `test/differential/fixture/fixture.go` (the `StatsAsserter` + the BackendKind enum, tail 32) + project memories (`reference_wire_format_both_sides_see_same_bytes`, `reference_differential_reference_parses_full_message`, `reference_close_direction_framework_gap`, `reference_dynamic_stat_name_charset_guard`, `reference_network_filter_typeurl_extensions`, `reference_docker_probe_bridge_network`, `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_subagents_no_push`, `feedback_pertask_gofmt_lint`).
- **last-commit:** the phase-36 {ring_hash} BRAINSTORM bundle (`docs/envoy-go/phases/36-load-balancer-ring-hash/BRAINSTORM.md` — the 12-§ family-template doc + the spec-document-reviewer loop; `docs/envoy-go/ROADMAP.md` row 36 registered `in-progress` + the Load-balancing family-heading `ring_hash IN-PROGRESS` note; `docs/envoy-go/STATE.md` this advance; `next-prompt.txt` rolled forward to the phase-36 SPEC). Counts UNCHANGED at this brainstorm. **Superseded (the phase-35 last-commit, preserved):** the phase-35 IMPL Task-8 completion bundle (`docs/envoy-go/BEHAVIOR_CONTRACT.md` the `### Load balancer — random (RANDOM)` §9 delta + the reject-text update + the deferral-list retirement [6 candidates]; `docs/envoy-go/DECISIONS.md` the full ADR-0234 entry — §Context promoted from SPEC §13 + the §Decision/§Consequences bodies landed IN-PLACE per ADR-0044, tail ADR-0233 → **ADR-0234**, next-free ADR-0235; `docs/envoy-go/STATE.md` this advance + the counts block fixtures 61 → 62 + DECISIONS tail → ADR-0234; `docs/envoy-go/ROADMAP.md` row 35 `in-progress → done`; `docs/envoy-go/phases/35-load-balancer-random/PROGRESS.md` finalized) — the ATOMIC LANDING per ADR-0052. Authored in the worktree `.worktrees/phase-35-impl` (branch `phase-35-impl`); Tasks 1-7 landed local-only ahead of it. Counts at phase-done: fixtures **62** / fuzzers **42** / stat surface **1116** (zero delta) / BackendKind **33** / DECISIONS tail **ADR-0234**. The controller squash-merges + pushes at stage-close per `feedback_push_to_origin`. Confirm the live tip via `git log --oneline -1`. **Superseded (the phase-35 BRAINSTORM last-commit, preserved):** the phase-35 BRAINSTORM bundle (`docs/envoy-go/phases/35-load-balancer-random/BRAINSTORM.md` NEW + `docs/envoy-go/ROADMAP.md` row 35 registered `in-progress` + the Load-balancing family-heading `random IN-PROGRESS` note + `docs/envoy-go/STATE.md` this advance + `next-prompt.txt` routed → the phase-35 SPEC) — authored in the worktree `.worktrees/phase-35-brainstorm` (branch `phase-35-load-balancer-random-brainstorm`), branched from the master tip `f3d32ab` (the docs-only routing tips `a450950`/`5815eae`/`f3d32ab` above the substantive phase-34 IMPL squash `b086137`). Counts UNCHANGED at the BRAINSTORM (stat surface 1116 / fixtures 61 / fuzzers 42 / BackendKind tail 33 / DECISIONS tail ADR-0233). The controller squash-merges + pushes at stage-close per `feedback_push_to_origin`. Confirm the live tip via `git log --oneline -1`. **Superseded (the phase-34 IMPL last-commit, preserved):** the phase-34 IMPL Task-9 completion bundle (`docs/envoy-go/BEHAVIOR_CONTRACT.md` the least_request §9 delta + the deferral-list retirement; `docs/envoy-go/DECISIONS.md` the ADR-0232 + ADR-0233 §Decision/§Consequences bodies landed IN-PLACE per ADR-0044; `docs/envoy-go/STATE.md` this advance; `docs/envoy-go/ROADMAP.md` row 34 `in-progress → done`; the phase-34 PROGRESS.md finalized) — authored in the worktree `.worktrees/phase-34-impl` (branch `phase-34-impl`); the controller squash-merges + pushes at stage-close. Counts at phase-done: fixtures **61** / fuzzers **42** / stat surface **1116** (zero delta) / BackendKind **33** / DECISIONS tail **ADR-0233**. **Superseded (the phase-34 PLAN last-commit, preserved):** the phase-34 PLAN bundle the phase-34 PLAN bundle (`docs/envoy-go/phases/34-load-balancer-least-request/PLAN.md` NEW + `docs/envoy-go/STATE.md` this advance + `docs/envoy-go/ROADMAP.md` the row-34 PLAN-done note + `next-prompt.txt` routed → the phase-34 IMPL) — authored in the worktree `.worktrees/phase-34-plan` (branch `phase-34-load-balancer-least-request-plan`), branched from the master tip `cde9d0c` (the docs-only routing chain `cde9d0c`/`63eeb7e`/`c4a778e` above the substantive phase-34 SPEC squash `3671167`). Counts UNCHANGED at the PLAN (stat surface 1116 / fixtures 60 / fuzzers 42 / BackendKind tail 33 / DECISIONS tail ADR-0233). plan-document-reviewer APPROVED. The controller squash-merges + pushes at stage-close per `feedback_push_to_origin`. Confirm the live tip via `git log --oneline -1`. **Superseded (the phase-34 SPEC last-commit, preserved):** the phase-34 SPEC bundle (`docs/envoy-go/phases/34-load-balancer-least-request/SPEC.md` NEW + README.md NEW + `docs/envoy-go/DECISIONS.md` the ADR-0232 + ADR-0233 §Context drafts [tail ADR-0231 → ADR-0233; next-free ADR-0234] + `docs/envoy-go/ROADMAP.md` the row-34 SPEC-done note + `docs/envoy-go/STATE.md` this advance + `next-prompt.txt` routed → the phase-34 PLAN) — authored in the worktree `.worktrees/phase-34-spec` (branch `phase-34-load-balancer-least-request-spec`), branched from the master tip `a463fe8` (the docs-only routing tips `a463fe8`/`8c0bd36` above the substantive phase-34 BRAINSTORM squash `87572c0`). Counts UNCHANGED at the SPEC except the DECISIONS tail. The controller squash-merges + pushes at stage-close per `feedback_push_to_origin`. Confirm the live tip via `git log --oneline -1`. **Superseded (the phase-34 BRAINSTORM last-commit, preserved):** the phase-34 BRAINSTORM bundle (`docs/envoy-go/phases/34-load-balancer-least-request/BRAINSTORM.md` NEW + `docs/envoy-go/ROADMAP.md` row 34 registered `in-progress` + the Load-balancing family-OPEN paragraph + `docs/envoy-go/STATE.md` this advance + `next-prompt.txt` routed → the phase-34 SPEC) — authored in the worktree `.worktrees/phase-34-brainstorm` (branch `phase-34-load-balancer-least-request-brainstorm`), branched from the master tip `e1da5b7` (the docs-only routing tips `e1da5b7`/`6721618` above the CI-fix merge `4252c1b` and the substantive phase-33 IMPL squash `97aefd6`). Counts UNCHANGED at the BRAINSTORM. The controller squash-merges + pushes at stage-close per `feedback_push_to_origin`. Confirm the live tip via `git log --oneline -1`. **Superseded (the phase-33 IMPL last-commit, preserved):** the phase-33 IMPL completion bundle (Task 16 — `docs/envoy-go/BEHAVIOR_CONTRACT.md` the thrift_proxy subsection + the 1091→1116 stat-mapping block + the two Task-12 per-side findings; `docs/envoy-go/DECISIONS.md` the ADR-0231 §Decision/§Consequences body in-place; `docs/envoy-go/STATE.md` this advance + counts; `docs/envoy-go/ROADMAP.md` row 33 `in-progress → done` + the §9 family CLOSURE; `docs/envoy-go/phases/33-network-filter-thrift-proxy/PROGRESS.md` the final record) — the ATOMIC LANDING per ADR-0052. Authored in the worktree `.worktrees/phase-33-network-filter-thrift-proxy-impl` (branch `phase-33-network-filter-thrift-proxy-impl`); Tasks 1-15 landed local-only ahead of it (17 commits ahead of master at Task 15). The controller squash-merges + pushes at stage-close per `feedback_push_to_origin`. Confirm the live tip via `git log --oneline -1`. **Superseded (the phase-33 PLAN last-commit, preserved):** the phase-33 PLAN (`docs/envoy-go/phases/33-network-filter-thrift-proxy/PLAN.md` + the ROADMAP row-33 PLAN-DONE note + the STATE advance) — authored in the worktree `.worktrees/phase-33-network-filter-thrift-proxy-plan` (branch `phase-33-network-filter-thrift-proxy-plan`), branched from the master tip `7eddedc` (the docs-only next-prompt routing commit `2c26034`/`7eddedc` trailing the phase-33 SPEC squash `7158b21`). The PLAN decomposes the SPEC §10 spine into 16 bite-sized TDD tasks; plan-document-reviewer APPROVED (two advisory nits pre-corrected); counts UNCHANGED at the PLAN. The controller squash-merges + pushes at stage-close per `feedback_push_to_origin`. Confirm the live tip via `git log --oneline -1`. Substantive predecessor on master: the phase-33 SPEC squash `7158b21` (`phase 33 SPEC: network-filter-thrift-proxy …`; the docs-only next-prompt SHA-fills `5c572ed`/`ec687e9`/`2c26034`/`7eddedc` trail it). Substantive predecessor on master: the phase-33 BRAINSTORM squash `23b5121` (`phase 33 BRAINSTORM: network-filter-thrift-proxy …`; the docs-only SHA-fills `8624213` / `337d3c2` trail it). Substantive predecessor on master: the phase-32.2 IMPL squash `e45f647` (`phase 32.2 IMPL: network-filter-redis-commands-and-stats …`; the docs-only SHA-fills `9baaff9` / `1659328` trail it). Substantive predecessor on master: the phase-32.2 PLAN squash `dd340ff` (the docs-only SHA-fill `9eba8f6` trailed it); the phase-32.2 SPEC squash `8da7377` (the docs-only SHA-fill `e270db7` trailed it); the phase-32.1 IMPL squash `fa58bc9` (`phase 32.1 IMPL: network-filter-redis-upstream-pool-seam-and-codec — the redis_proxy TERMINAL routing proxy on the ADR-0230 upstream-pool seam; 16-task PLAN; six-gate GREEN`; the docs-only SHA-fill `823c5aa` trailed it); earlier the phase-32.1 PLAN squash `a28f593`; the phase-32.1 SPEC squash `b7f01a4`; the phase-32 PARENT SPEC squash `0f7888d`; the phase-32 BRAINSTORM squash `f473c4e`; the phase-31 IMPL squash `928287a`. Earlier substantive predecessors on master: the phase-32.2 SPEC squash `8da7377` (the docs-only SHA-fill `e270db7` trailed it); the phase-32.1 IMPL squash `fa58bc9` (`phase 32.1 IMPL: network-filter-redis-upstream-pool-seam-and-codec — the redis_proxy TERMINAL routing proxy on the ADR-0230 upstream-pool seam; 16-task PLAN; six-gate GREEN`; the docs-only SHA-fill `823c5aa` trailed it); earlier the phase-32.1 PLAN squash `a28f593`; the phase-32.1 SPEC squash `b7f01a4`; the phase-32 PARENT SPEC squash `0f7888d`; the phase-32 BRAINSTORM squash `f473c4e`; the phase-31 IMPL squash `928287a`. Earlier substantive predecessors on master: the phase-31 PLAN squash `12f7cac`; the phase-31 SPEC squash `6104036`; the phase-31 BRAINSTORM squash `78d5f36`; the phase-30 IMPL squash `2d24f34` (phase 30 CLOSED).
- **last-updated:** 2026-06-11 (phase-36 BRAINSTORM done)
- **next-free ADR:** `ADR-0235` (UNCHANGED at this brainstorm — DECISIONS.md tail STAYS **ADR-0234**; the phase-36 BRAINSTORM anticipates **TWO** ADRs — **ADR-0235** (the LB hash-key seam extension — the PICK-INPUT-half `Pick(hashKey)` widening ADR-0232 §Consequences anticipated) + **ADR-0236** (the ring_hash policy) — the phase-34 two-ADR (seam + policy) shape; §Context anchored at the phase-36 SPEC, §Decision/§Consequences bodies at the IMPL per ADR-0044; the tail advances ADR-0234 → ADR-0236 across the phase-36 IMPL). **Superseded (the phase-35 IMPL record, preserved):** `ADR-0235` (DECISIONS.md tail **ADR-0234** [the `random` load-balancing policy — phase 35]: ACCEPTED, the §Context anchored at the phase-35 SPEC + the §Decision/§Consequences body landed IN-PLACE at THIS phase-35 IMPL per ADR-0044, the tail advancing ADR-0233 → **ADR-0234**; ONE ADR at phase 35 — the policy only, NO seam ADR since the ADR-0232 seam is REUSED unchanged [the thrift-33 single-ADR-on-reuse precedent], ADR-0024 UNAMENDED). The ADR-0209 escape-valve reserve STANDS-UNCONSUMED. **Superseded (the phase-34 IMPL record, preserved):** next-free `ADR-0234` (DECISIONS.md tail **ADR-0233**: **ADR-0232** [the LB acquire/release seam — the first framework seam outside `internal/filter/network`] + **ADR-0233** [the least_request policy], both **ACCEPTED** — §Context anchored at the phase-34 SPEC; the §Decision/§Consequences bodies landed IN-PLACE at THIS phase-34 IMPL per ADR-0044 with NO new number consumed [the tail STAYED ADR-0233 through the IMPL] — the kafka-31/redis-32/thrift-33 §Context-at-SPEC / body-at-IMPL precedent). TWO ADRs at phase 34 (the seam + the policy — the redis-32 two-ADR shape, contrast thrift-33's one). The ADR-0209 escape-valve reserve STANDS-UNCONSUMED. **Superseded (the phase-33 IMPL record, preserved):** next-free `ADR-0232` (tail ADR-0231, the `thrift_proxy` filter, ACCEPTED — its §Decision/§Consequences body landed in-place at the phase-33 IMPL; ADR-0230 [the upstream-pool seam] STAYS ACCEPTED, REUSED unchanged by phase 33 — its redis-scoped YAGNI sizing VALIDATED at its first reuse).

---

## Project counts at this commit

- **active differential fixtures:** **62** (tail `0060-lb-random`). Canonical recipe `ls -d test/fixtures/[0-9]* | wc -l` = 62. Advanced at the phase-35 IMPL (+1: `0060-lb-random` — the REUSED `0059` hold-4/burst-60 skew stimulus over 3 reused `TCPEcho` backends with the assertion INVERTED to anti-skew; the SECOND band-based `AssertDistribution` [conservation `sum==64` + uniform-floor 6 + uniform-ceiling 40, PER SIDE — the IMPL-re-tuned `{6,40}` band] + a cross-side `StatsAsserter` prong). Was 61 at the phase-34 IMPL close (+1: `0059-lb-least-request` — the hold-4/burst-60 skew workload, the FIRST band-based `AssertDistribution`). Was 60 at the phase-33 IMPL close (+2: `0057-thrift-roundtrip` + `0058-thrift-boot-reject`).
- **fuzzers:** **42** (canonical recipe `grep -rh "^func Fuzz" $(find ./internal -name fuzz_test.go) | wc -l`; tail `FuzzThriftDecode`). UNCHANGED at the phase-35 IMPL (+0 — random decodes no wire format; the SECOND no-fuzzer phase, ADR-0234). UNCHANGED at the phase-34 IMPL (+0 — the FIRST no-fuzzer phase, ADR-0233). Was 42 at the phase-33 IMPL close (+1: `FuzzThriftDecode`).
- **stat surface:** **1116** (BEHAVIOR_CONTRACT doc count). UNCHANGED at the phase-35 IMPL (+0 — random holds no registry-named state at all [no per-endpoint counters]; live-confirmed empty name-set diff RANDOM-vs-ROUND_ROBIN, 455 == 455; the SECOND zero-stat-delta phase, ADR-0234). UNCHANGED at the phase-34 IMPL (+0 — the least_request per-endpoint active counters are LB-internal `atomic.Int64` fields, NOT registry stat names; the FIRST zero-stat-delta phase, ADR-0233). Was 1116 at the phase-33 IMPL close (+25: 24 `thrift_proxy` counters + 1 gauge `request_active`; the `request_time_ms` histogram DEFERRED per ADR-0060). Was 1091 at the phase-32.2 IMPL close.
- **BackendKind tail:** **33** (`TCPThriftResponder` — seq_id-agnostic canned framed-binary REPLY). UNCHANGED at the phase-35 IMPL (+0 — `0060` reuses `TCPEcho`; the SECOND no-new-BackendKind §-LB phase, ADR-0234). UNCHANGED at the phase-34 IMPL (+0 — `0059` reuses `TCPEcho`; the FIRST no-new-BackendKind §-LB phase, ADR-0233). Advanced at the phase-33 IMPL.
- **DECISIONS.md tail:** **ADR-0234** (next-free **ADR-0235**) — **ADR-0234** (the `random` load-balancing policy), now **ACCEPTED**: its §Context anchored at the phase-35 SPEC + the §Decision/§Consequences body landed IN-PLACE at THIS phase-35 IMPL per ADR-0044 (the tail advancing ADR-0233 → ADR-0234). ONE ADR at phase 35 (the policy only — NO seam ADR since the ADR-0232 seam is REUSED unchanged; the thrift-33 single-ADR-on-reuse precedent, contrast phase-34's two). Prior tail: ADR-0233 (the least_request policy) + ADR-0232 (the LB acquire/release seam), both ACCEPTED — bodies landed at the phase-34 IMPL.
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227, superseding ADR-0008).
- **conformance:** h2spec 53/53; proxy-wasm all 10 families PASS (image-independent; UNAFFECTED by phase 33 — touches no HTTP/h2/proxy-wasm path).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
