# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `05.1-downstream-h2`
- **phase-directory:** `docs/envoy-go/phases/05.1-downstream-h2/` — contains `README.md` (placeholder, written at split time per ADR-0045 / commit `2162509`), `SPEC.md` (drafted by brainstorming per ADR-0004 autonomous mode; commit `4b45941`), `PLAN.md` (drafted by writing-plans per ADR-0005 autonomous mode; commit `634bcb6`; 3331 lines, 16 tasks, 8 new ADRs), `PROGRESS.md` (executor-appended through Tasks 1–16 with final SHA-fills at `b416664`; the prior verification-block at `df85f85` records the gate-(e) bounce; the Task 17 entry at `9aa557a` records the state-3 follow-up cleanup and the re-verification of all five non-deferred SPEC §3 gates green at HEAD `65d2574`; the `## Verification (lifecycle-state 5)` block at `2237e6e` records the second mandatory verification-before-completion run at HEAD `2cf3458` with all five non-deferred gates GREEN), and now `REVIEW.md` (drafted by `superpowers:requesting-code-review` per ADR-0006 autonomous-mode adaptation; commit `d69446a`; 240 lines; verdict APPROVED WITH FOLLOW-UPS — 0 Critical, 4 Important, 17 Minor; full text at `docs/envoy-go/phases/05.1-downstream-h2/REVIEW.md`). The sibling `docs/envoy-go/phases/05.2-upstream-h2/` is unchanged from the split commit (placeholder `README.md` only). The parent `docs/envoy-go/phases/05-http-2/` retains its `SPEC.md` (`612cdea`) as the master design document for both sub-phases per ADR-0045 and phase-05 SPEC §11.1; that directory remains closed read-only history at the parent level (no PLAN/PROGRESS/REVIEW will land under it; those land under the sub-phase directories).
- **lifecycle-state:** `3` — re-entered from 5 by this `superpowers:requesting-code-review` session per `BOOTSTRAP_PROMPT.md` §5.2 (review approved BUT surfaced changes requiring code/doc fixes — re-entry is to step 3, NOT step 4). Branched from STATE.md commit `536f353` (master tip per ADR-0003) into `.worktrees/phase-05.1-downstream-h2-review/` on branch `phase/05.1-downstream-h2-review`. The REVIEW.md verdict is APPROVED WITH FOLLOW-UPS: zero Critical findings; four Important findings (I-1/I-2/I-3 — `ServerConn.writeData` does not respect `MAX_FRAME_SIZE`, per-stream send window, or inbound `WINDOW_UPDATE` emission; all three are flow-control discipline gaps that are dormant in 05.1's bodyless-GET-only behaviour surface and are recommended for carry-forward to a single 05.2 ADR. I-4 — `CONFORMANCE_PINS.md` is missing the SPEC §13-required `## Refresh procedure` section, ~10–20 lines of prose); seventeen Minor findings (M-1 = the verification session's pre-flagged `hpackBlocked` dead-code carry-forward, confirmed correct — recommend deletion; M-2 / M-3 / M-15 are also dead-code or documentation-drift; M-4..M-14, M-16, M-17 are cosmetic / forward-looking). All five non-deferred SPEC §3 phase-done gates re-verified GREEN at HEAD `536f353` from the review worktree (gate (c) h2spec 53/53 PASS independently re-executed). 14/14 SPEC §13 acceptance bullets substantively satisfied with the single exception of bullet 7 (CONFORMANCE_PINS.md refresh procedure = I-4). Per BOOTSTRAP §5.2, REVIEW-surfaced changes re-enter the lifecycle at state 3, not state 4 — even when the verdict is "approved with follow-ups", the asymmetry preserves the discipline that the next session resumes implementation (and TDD, per `superpowers:test-driven-development`) rather than just re-verifying.
- **next-skill:** `superpowers:subagent-driven-development` (or `superpowers:executing-plans`) — to land the REVIEW follow-up batch per Path A of REVIEW.md §Recommendation. The follow-up session branches a fresh `phase/05.1-downstream-h2-review-followup` (or similar) worktree from this STATE.md commit per ADR-0003; the review worktree at `.worktrees/phase-05.1-downstream-h2-review/` is closed-history at this state transition (its work landed in `d69446a` REVIEW.md + this STATE.md commit). Test-driven where applicable (M-1 deletion has unit tests covering the unreachable guard branch — those tests should be deleted in the same commit; the verification session pre-flagged this); doc-only changes (I-4, M-13, M-15, M-16, M-17) need no test changes.
- **next-skill-scope:** Land a small REVIEW follow-up batch in `.worktrees/phase-05.1-downstream-h2-review-followup/` per ADR-0003. Required content (the doctrine-required subset that closes SPEC §13 acceptance and the verification session's pre-flagged carry-forward): (1) **I-4** — add a `## Refresh procedure` section to `docs/envoy-go/CONFORMANCE_PINS.md` mirroring `ENVOY_TARGET.md`'s discipline (steps to pull a candidate image; `docker inspect --format='{{index .RepoDigests 0}}'`; append a new pin row preserving the prior row for audit; re-run `go test -run TestH2Spec ./test/conformance/h2spec/` and confirm 53/53 PASS at the new digest; supersede prior row's "current" status with a "superseded by <digest>" annotation; commit as a dedicated pin-refresh phase per D-3.7); promote the "All pins are append-only" line into the procedure section so policy and procedure live together. (2) **M-1** — delete the `hpackBlocked bool` field at `internal/filter/hcm/h2/conn.go:37` and the unreachable guard block at `:227-240`; the framer enforces header-block ordering via `golang.org/x/net/http2.Framer.checkFrameOrder` per ADR-0046 (translation to `*Error` lives at `framer.go:76-83`); delete or repurpose the unit tests covering the dead guard branch. Recommended additional content (low-cost cleanups bundled in the same commit batch): (3) **M-2** — delete `validateClientStreamID` at `internal/filter/hcm/h2/stream.go:402-410` (marked Deprecated; production path inlines the validation in `conn.onHeaders`; only used by 2 unit tests at `stream_test.go:375`/`:391` — either delete those tests or move the helper into `_test.go`). (4) **M-15** — fix ADR-0046 prose drift at `DECISIONS.md:1545` (says `framer.go`/`hpack.go`/`settings.go`; should say `framer.go`/`settings.go`/`conn.go` — the actual production importers of root `golang.org/x/net/http2`; `hpack.go` imports the `http2/hpack` sub-package only). Per D-3.5, ADR-0046 is landed/append-only; the correction lands either as a superseding ADR (recommended) or as a textual correction justified at commit-message level — the follow-up session decides. (5) Optional: **M-6** (replace `err == context.DeadlineExceeded`/`context.Canceled` with `errors.Is`); **M-13** (tighten `BEHAVIOR_CONTRACT.md:285` body-bytes prose); **M-14** (audit H2 vs H1 no-match 404 body text alignment between `internal/filter/hcm/h2dispatch.go:41` and `connection.go`); **M-16** (smoke-only docstring tag on `cmd/envoy-go/main_test.go` `H2Smoke`); **M-17** (one-line ADR-0050 cross-ref comment in `connection.go` ALPN-h1 fall-through). Items deferred to phase 05.2: **I-1 / I-2 / I-3** form a coherent ADR-shaped unit on flow-control discipline (outbound `MaxFrameSize` chunking + per-stream send-window enforcement + inbound `WriteWindowUpdate` emission); 05.2 brainstorming session inherits this finding as a SPEC §3 input; ADR lands at PLAN-write time per ADR-0004 autonomous-numbering rule (likely ADR-0054). Items further deferred: **M-3** (writeData dead recovery branch — bundles with I-1/I-2 in 05.2's flow-control ADR); **M-7** (recvW dead today — consumed by I-3 fix in 05.2); **M-9** (window overflow bounds-check — bundles with I-1/I-2/I-3); **M-10** (SETTINGS_TIMEOUT — flag for 05.2 or 06); **M-11** (recvData write-before-state-check — one-line reorder, low priority); **M-12** (closedStreams unbounded growth — long-lived-connection observability concern, naturally lands in phase 06); **M-4** (preface ctx-awareness — phase 06 or 07); **M-5** (framer error-translation duplication — pure cosmetic); **M-8** (`excludedSubsections` doctrine-D-3.4 noise — pure cosmetic). After the follow-up batch lands, the next session re-runs the affected SPEC §3 gates (b)/(c)/(d)/(e) — gate (a) remains vacuous and gate (f) is closed by an updated REVIEW.md addendum or by the executor's PROGRESS verification block — promotes lifecycle-state 3 → 4 (impl complete) → 5 (re-verified) → 6 (final phase-done commit + ROADMAP row 05.1 → `done` per BOOTSTRAP §5 state-6). The phase-done commit message follows §5.3 format and names every introduced ADR (ADR-0046..ADR-0053 plus any superseding ADR landed in the follow-up batch). Per BOOTSTRAP §5.2 the asymmetry is deliberate: REVIEW-surfaced changes resume implementation (and TDD), not just re-verification — the follow-up session adds tests where they're appropriate (M-1 deletion is paired with deletion of its unit-test coverage of the unreachable guard branch; ADR-0046 correction has no test surface).
- **last-commit:** `d69446a` — the REVIEW.md draft on branch `phase/05.1-downstream-h2-review`, +240 lines, ONLY-REVIEW.md change (no code, test, fixture, or STATE.md change). This STATE.md commit (the lifecycle-state 5 → 3 re-entry commit on the same branch) is the 2nd commit on `phase/05.1-downstream-h2-review`. Master is fast-forwarded to this STATE.md commit per ADR-0003.
- **last-updated:** 2026-04-26

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
