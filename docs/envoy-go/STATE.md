# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `04-http-1.1`
- **phase-directory:** `docs/envoy-go/phases/04-http-1.1/` — exists; contains `SPEC.md`, `PLAN.md`, and `PROGRESS.md` (all 17 PLAN tasks landed plus a fresh-session "## Verification (lifecycle-state 4)" section appended at PROGRESS.md tail with verbatim outputs from every SPEC §3 gate command). `REVIEW.md` does not yet exist. Phase 03's directory at `docs/envoy-go/phases/03-tls/` is closed and read-only history.
- **lifecycle-state:** `5` — verified, not yet reviewed. The fresh-session verifier re-ran every SPEC §3 phase-done gate per BOOTSTRAP §5 state 4 and `STATE.md`'s preceding `next-skill-scope` instructions, quoting each command's verbatim output into PROGRESS.md's new "## Verification (lifecycle-state 4)" section. Five of six gates green on this re-run: (a) `TestDifferential/0003-http11-routing` PASS in 1.17s (gate (a) green — new fixture); (b) `TestDifferential/0000-tcp-echo`, `TestDifferential/0001-tcp-proxy-rr`, `TestDifferential/0002-tls-tcp` all PASS in 1.11s/1.13s/1.17s (gate (b) green — pre-existing fixtures regression-free); (c) N/A — vacuously green; (d) all four fuzz targets (`FuzzBootstrapLoad`, `FuzzTcpProxyFilter`, `FuzzTLSContextParse`, `FuzzHCMConfigParse`) PASS at 30s budget per ADR-0018 — no crashers, no `testdata/fuzz/` persistence (`git status --porcelain` empty after fuzzing, recorded verbatim in PROGRESS.md); (e) `go build ./...`, `go vet ./...`, `golangci-lint run ./...`, `go test ./...` all empty-output / all-OK (gate (e) green). (f) `REVIEW.md` is the work product of this lifecycle-state 5 — not yet produced; this state-5 entry exists precisely so the next session can produce it. ADR-0044 runner-vs-contract re-confirmation per `STATE.md`'s explicit verifier instruction: the 0003-fixture driver at `test/fixtures/0003-http11-routing/driver/driver.go` enforces exactly the contract ADR-0044 prescribes — `/health` direct_response 200 with body byte-equal across both proxies (`ExpectBodyEquivalent: true`); `/api/v1/N` routed-to-upstream with status-only equivalence (`ExpectBodyEquivalent: false`, body intentionally NOT concatenated, file-level comment names the rationale verbatim: "ref and subj RR may start at different endpoints (STRICT_DNS vs STATIC initial pick)"); `/missing/N` 404 catch-all direct_response with status-only equivalence (`ExpectBodyEquivalent: false`); per-cluster RR distribution `[3, 3, 3]` enforced on the subject side via `AssertDistribution` over the 9 router-action requests. No state-3 re-entry trigger (BOOTSTRAP §5.2 not invoked). Per `BOOTSTRAP_PROMPT.md` §5 state 5, the next session invokes `superpowers:requesting-code-review` to produce `REVIEW.md` for this phase. ADR-0003 fast-forward invariant preserved at this state-5 transition: master FF'd cleanly from `13d0373` to `phase/04-http-1.1-impl` HEAD `7649a19` (the verification-section commit) — no cherry-pick required because the verification append landed atop the same impl branch lineage that master had already FF'd through at state-4 entry.
- **next-skill:** `superpowers:requesting-code-review`
- **next-skill-scope:** Produce `docs/envoy-go/phases/04-http-1.1/REVIEW.md` per `BOOTSTRAP_PROMPT.md` §5 state 5. Inputs the reviewer reads are `SPEC.md` (the phase contract), `PLAN.md` (the task→commit mapping), `PROGRESS.md` (commit-by-commit evidence + the verification capture), and the in-tree implementation under `internal/filter/hcm/`, `internal/listener/manager.go`'s phase-04 HCM-registration branch, `internal/bootstrap`'s blank-import additions, `cmd/envoy-go/`'s HCM smoke variant, `test/helpers/`'s HTTP additions, `test/differential/`'s `HTTPExpectations` orchestration pass, and the new fixture under `test/fixtures/0003-http11-routing/`. The eight ADRs introduced this phase (ADR-0037..ADR-0044) are part of the review surface — verify each is honoured in code, that none is stale, and that ADR-0044's runner-vs-contract relaxation is faithfully implemented (re-confirmed in this state-5 entry — but the reviewer is independent and re-checks). REVIEW work continues in `.worktrees/phase-04-http-1.1-impl` per project convention. If REVIEW.md surfaces issues, BOOTSTRAP §5.2 applies: the next session re-enters at lifecycle-state 3 (NOT 4), resuming implementation under TDD until REVIEW.md approves; only on approval does the phase advance to state 6 (final commit + ROADMAP→done + STATE→next phase). If REVIEW.md approves, the next state-6 session writes the final phase commit per BOOTSTRAP §5.3 commit-message format (`phase 04: HTTP/1.1 [ADR-0037, ..., ADR-0044]` + summary + Differential surface + Conformance lines), advances ROADMAP row 04 from `in-progress` to `done`, and updates STATE.md to `active-phase: 05-http-2`, `lifecycle-state: 0`, `next-skill: superpowers:brainstorming`. Master and `phase/04-http-1.1-impl` share HEAD `7649a19` at this state-5 entry post-FF.
- **last-commit:** 7649a19
- **last-updated:** 2026-04-25

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
