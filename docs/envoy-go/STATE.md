# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

> **This file is a POINTER, not an archive** — `BOOTSTRAP_PROMPT.md` §4: *"STATE.md — pointer: active phase, last commit, next action."* It carries the live pointer, the live counts, and a SHORT recent lineage. **Full stage-by-stage history lives in `STATE_HISTORY.md`** (archived at ADR-0288; **nothing was deleted** — the pre-move file is preserved there verbatim, byte-for-byte).
>
> **⚠️ THE RULE ADR-0288 EXISTS TO ENFORCE: there is EXACTLY ONE live `next-skill:`, `lifecycle-state:`, `next-free ADR:`, and count block IN THIS FILE, and they live in §Current pointer directly below.** Before that move, ~330 lines of prepended backlog had buried TWO fossilised "current state" blocks whose `next-skill:` still read *"the 42.2 hedging BRAINSTORM"* and *"the 46.2 SPEC"* while the live phase was **66** — so `grep 'next-skill:' STATE.md` returned **two stale hits and ZERO live ones**, and `grep 'fuzzers:'` returned **48** and **42** when the true count was **55**. **A session may trust a grep of this file again. Keep it that way: when you advance the pointer, EDIT §Current pointer IN PLACE — never prepend a new block above it.**

---

## Current pointer

*(The ONLY live "what next" in this repo. Update IN PLACE at every stage close.)*

- **active-phase:** `phase 89 (h2-decode-filter-mutations) BRAINSTORM done` (2026-08-15) — lifecycle-state **DONE -> 1**. **PHASE 89 OPENED BY SELF-PICK** per the 2026-07-12 standing directive; no banked mid-lifecycle work existed and that was PROVEN, not assumed (all 120 rows `done`, check (1) SILENT at `want=120` ⇒ no row `in-progress`). **ROW 89 REGISTERED `in-progress`** at `ROADMAP.md:151` and the sentinel `want` bumped **120 -> 121 in the SAME commit** (the phase-84/85/86/87/88 precedent). Base master **`125f0714`**, branch `phase-89-h2-decode-filter-mutations-brainstorm`.
- **phase-directory:** `docs/envoy-go/phases/89-h2-decode-filter-mutations/` — `BRAINSTORM.md` + `PROGRESS.md` (no `SPEC.md`, no `PLAN.md` yet).
- **lifecycle-state:** **1** (row 89 in ROADMAP, phase directory created, BRAINSTORM landed; SPEC not yet written). ⚠️ **Sentinel re-run MECHANICALLY on BOTH sides of this stage's ROADMAP edit and it does NOT fire** — BEFORE `want=120`: 238 lines / 120 data rows, (1) **SILENT** (the SEVENTH silent reading in project history), (2) **SIX** at `:198 :204 :210 :220 :226 :234`, (3) **SILENT**; AFTER `want=121`: 239 / 121, (1) **`NOT DONE: row 89`** ALONE, (2) **SIX** at `:199 :205 :211 :221 :227 :235`, (3) **SILENT**. `ROADMAP.md` numstat **`1 0`** — strictly additive. **All four NCs fired on both sides.** `stop` NOT created. ⚠️ **THE WINDOW ANCHORS SHIFTED +1 — re-derive them; do not cite `:198 …` again.**
- **next-skill:** the **phase-89 SPEC** — `docs/envoy-go/phases/89-h2-decode-filter-mutations/SPEC.md`. Dispose the **nine** BRAINSTORM §7 questions BY EXECUTION. The load-bearing ones: **Q1** the reconciler shape decided by COMPILING PROTOTYPE (delta-against-snapshot vs full rebuild vs slice-native chain), settling whether `filter_http.ReconcileOrderedHeaders` (`h2dispatch.go:612`) is reusable verbatim on the decode side; **Q3** pseudo-header safety — whether a filter may legitimately mutate `:path`/`:authority`, which would route mutations to the H2Request SCALAR fields rather than to `.Headers`, and which is **the row's most likely hidden cost**; **Q6** the contract, where there is **NO false sentence to correct** but an honest **"NOT asserted: … H2 differential coverage"** carve-out to CLOSE. ⚠️ **The SPEC RE-ARMS the strict `^> **STATUS: PROPOSED` guard 0 -> 1** and drafts **ADR-0311**.
- **last-commit:** the **phase-89 BRAINSTORM** commit. ⚠️ **LOCATE BY SUBJECT, NEVER BY POSITION** — `git log --grep '^phase 89 (h2-decode-filter-mutations) BRAINSTORM' --format='%h %s' | head -1`; a bare `git log --grep 'phase 89'` is CONTAMINATED (`next-prompt.txt` and commit bodies discuss the phase). Base master `125f0714`; the stage landed as ONE squashed commit merged to master and pushed by the controller at close.
- **last-updated:** 2026-08-15 (phase-89 **BRAINSTORM done**; **row 89 `in-progress`**; `want` **121**; strict `PROPOSED` guard **0**, re-armed by the SPEC; next-free **`ADR-0311`** UNMOVED — a BRAINSTORM adds no ADR).
- **next-free ADR:** **`ADR-0311`** — ⚠️ **UNMOVED BY THIS STAGE: a BRAINSTORM drafts no ADR.** TAIL-derived (`grep -o '^## ADR-[0-9]*' docs/envoy-go/DECISIONS.md | tail -1` -> `## ADR-0310`; `grep -c '^## ADR-0311'` -> **0**). ⚠️ **NEVER derive from the heading count — headings = 309 and headings+1 COLLIDES at the ADR-0209 gap.** ⚠️ **A NEW ADR TAKES NO `---` SEPARATOR** (`^---$` = **216**). ⚠️ **CARRY NO WHOLE-FILE COUNT OF THE LOOSE `PROPOSED` MATCHER** — it reads **1** at `DECISIONS.md:14866`, an ADR-0231 (phase-33) entry in the older non-blockquote form; only the strict form is the guard.

---

## Project counts at this commit

*(Live. Re-verify at session start — `git fetch` first — and update here at every stage close. Canonical commands inline.)*

⚠️ **ALL counts below were re-run MECHANICALLY in the stage worktree at this phase-76 IMPL close with the canonical commands, EACH WITH ITS NEGATIVE CONTROL OBSERVED — never copied forward.** **Phase 76 landed +0 on EVERY axis, including +0 PRODUCTION `.go` FILES** — the first chartered row in this lineage whose production-code delta is exactly nothing, and the ANTICIPATED envelope held on all of them. The row's `.go` delta is **three TEST-side files** (`internal/cluster/ringhash_test.go` edited, `test/fixtures/0061-lb-ring-hash/driver/driver_test.go` edited, `.../linkage_test.go` NEW), plus three fixture-doc files and four project docs. ⚠️ **The stat-surface total 1205 is DOC-SOURCED and was NOT re-derived mechanically** — there is no mechanical command, and the chain is known to be discontinuous in **TWO** places (the recorded `1200 -> 1201`, and `Phase 46.1b` closing at 1198 while `Phase 47.1` opens at 1200 — an unattributed +2). **Only the +0 DELTA is asserted, and that argument is STRUCTURAL rather than a `statssink` result:** zero production `.go` bytes changed (G8) and zero byte mismatches over the 375-file production roster (G14), so no `NewCounter`/`NewGauge` call site can have moved; the five `TestNoNewStat*` guards corroborate at 5x PASS. ⚠️ **A future phase should COUNT the surface rather than trust the chain — a +0 row is a cheap place to do it, and phase 76 declined to charter that, so it is now a NAMED maintenance deferral rather than an idle observation.**

- **active differential fixtures:** **119** — ⚠️ **Phase 76 landed +0: `0061-lb-ring-hash` was EDITED, not added** (`sourceIPs` 4 -> 16 plus a NEW `driver/linkage_test.go` inside the EXISTING fixture directory — no new YAML, no new directory, no new BackendKind, no new port), so the count STAYS **119** and `0118` remains FREE. NC observed: `mkdir .../0118-fake` ⇒ 120. — `ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0117-tracing-custom-tags-metadata-cluster` (phase 73). **Phase 75 landed +0**: `0110-tls-require-client-cert-false` was **EXTENDED** with a cross-side `fixture.StatsAsserter` (NO new YAML, NO new directory, NO new BackendKind, NO new port), so the count STAYS **119** and `0118` remains FREE. ⚠️ **Next-free REFERENCE port is `10450`, NOT `10118`** — ports are NOT fixture-index aligned. **The fixture now asserts what its own README confessed was infeasible, and BOTH sides were EXECUTED**: `ssl.handshake=2 ssl.no_certificate=1 ssl.fail_verify_error=1 ssl.fail_verify_no_cert=0`, `downstream_cx_total=3`, **identical on reference and subject**, with the discriminating asymmetry (`handshake=2` vs `no_certificate=1`) observed per side. Shape A over `/stats/prometheus`, keyed on the metric NAME with `envoy_listener_address` IGNORED — the NAMES are byte-identical cross-side while the labels differ (`0.0.0.0_10446` reference vs **`___20016`** subject: ⚠️ **the subject binds the IPv6 WILDCARD**). ⚠️ **`0111` was NOT given a value assertion, deliberately** — it runs `require_client_certificate: true` on both sides, so a success-path no-cert annotation reads **0 on every arm, structurally**, and a value pin would document a **vacuous `0 == 0`** as coverage; its PROSE gained the name in its NAMED-UNASSERTED list instead, because **both `0111` passages are CLOSED ENUMERATIONS** and a name in neither list reads as ASSERTED. **`0111`'s driver is byte-untouched, and its named-subset check was EXECUTED for the first time at this IMPL: PASS.** ⚠️ `var _ fixture.StatsAsserter` is a **TRIPWIRE, not the dispatch mechanism** — `0110` is only the **THIRD** fixture repo-wide to carry it, while **82** define an `AssertStats` method.
- **fuzzers:** **55** — **Phase 76 landed +0** (it consumes no new config field, so there is no parse arm to fuzz). NC observed: appending `func FuzzProbeOnly` ⇒ 56. — `grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`. **Phase 75 landed +0** — the row consumes no new config field at all, so there is no parse arm and no fuzz-seed classification to flip (`reference_fuzzer_count_docs_drift`: an `f.Add` seed is not a fuzzer).
- **stat surface:** **1205** — BEHAVIOR_CONTRACT doc count. **There is NO mechanical counting command** (the contract says so itself); enforcement is per-phase `TestNoNewStat*` guards asserting a delta of ZERO, never the absolute total. **Phase 75 LANDED +1 (1204 -> 1205)** — `listener.<normalized-addr>.ssl.no_certificate`, the FOURTH listener-scope `ssl.*` name and the FIRST **SUCCESS-PATH** handshake annotation (⚠️ **NOT a synonym for `ssl.fail_verify_no_cert`, and the mis-mapping is wrong in BOTH directions** — one annotates an ACCEPTED handshake, the other books a REJECTED one; they are mutually exclusive on every connection). ⚠️ **The delta is a NAME-surface delta, NOT a flat per-deployment one:** registration rides the block that ALREADY gates on `rt.tlsMode`, so a **plaintext-only** deployment gains **ZERO** names, while a **QUIC** listener gains it permanently zero — **PARITY** with the reference, and structural (`Manager.Start` launches no accept loop for `kindQUIC`, so the Inc site is unreachable for a **name-independent** reason). ⚠️ **QUIC was NOT re-probed at phase 75** — the parity claim is INHERITED from phase 74's family-level probe, and that inheritance is stated in BEHAVIOR_CONTRACT rather than presented as a measurement. Ledger updated: `1204` -> `1205` at `BEHAVIOR_CONTRACT.md:831`/`:847`, plus a `**Phase 75 - 1204 -> 1205 (+1)**` line after the tail entry — **the ONLY line-adding edit in the file** (`5744 -> 5746`), so every by-line citation into the TLS subsection stays valid. ⚠️ **The new entry RECORDS BOTH ledger gaps and BACK-FILLS NEITHER** (fabricating a line would be inventing a record). Gate figures at this close: `grep -c '1204'` => **2** · `grep -c '1205'` => **3** · `grep -cE '^#{2,4} .*ADR-[0-9]+.*ADR-[0-9]+'` => **17** (was 16 — the `:1849` heading joined them, on the `:785` precedent). ⚠️ **`internal/statssink`'s four *"stays 1200 / 1196"* prose sites, stale since phase 49, are RECORDED-NOT-FIXED** — that package is on this phase's byte-untouched sha256 roster.
- **BackendKind tail:** **38** — **Phase 76 landed +0** (`0061` uses the in-process `TCPEcho` branch and implements neither `BackendKindAware` nor `PerHostBackendKind`). NC observed: appending `ZZProbeKind = 39` ⇒ tail 39. — (`H2GoawayResponder`, `test/differential/fixture/fixture.go:614`). **Phase 75 landed +0** — `0110` was extended, not cloned, and it implements neither `BackendKindAware` nor `PerHostBackendKind`, so the runner takes the in-process `TCPEcho` branch. ⚠️ **This is a TAIL VALUE, not a count** — the file declares **39** constants (values 0-38, `TCPEcho = 0`). The lineage figure **38** is correct as written; do NOT 'fix' it to 39.
- **DECISIONS.md tail:** **ADR-0298** (next-free **ADR-0299**) — `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1` -> `## ADR-0298`; `grep -c '^## ADR-0299'` ⇒ **0**. **ADR-0298 is COMPLETE as of the phase-76 IMPL** — §Context drafted at the phase-76 SPEC, §Decision + §Consequences APPENDED IN PLACE after the RETAINED italic footer, **no renumber and NO `---` separator** (the file's last `^---$` is still at `:17020`). Block shape controller-re-derived at this close: **1** `### Context` / **1** `### Decision` / **1** `### Consequences` / **1** retained footer. ⚠️ **FOUR of its OWN §Context paragraphs were CORRECTED IN PLACE** (¶1's `:295` -> `:307`, ¶5's `-count ~= 81` -> **83**, ¶7's *"~71 collapses"* -> **74/2000**, ¶11's *"needs no edit"* -> REFUTED) on the ADR-0297 ¶7/¶9 precedent, each **leading with what survives**; none changes a decision the ADR records. ⚠️ **It carries NO whole-file grep count and claims NO family ordinal** — the Load-balancing family closed at row 54, and **maintenance rows do not extend a charter**. *(Historical, retained: **ADR-0297 is COMPLETE** — §Context drafted at the phase-75 SPEC (ADR-0044-**as-used**; ⚠️ ADR-0044 does NOT actually contain that discipline), §Decision + §Consequences appended IN PLACE at the phase-75 IMPL after the **RETAINED** footer, mirroring ADR-0295/0296. Block shape verified at this close: **1** `### Context` / **1** `### Decision` / **1** `### Consequences` / **1** retained footer; `grep -c '^## ADR-0298'` => **0**. **No renumber, no new ADR.** ⚠️ **Its OWN §Context ¶7 and ¶9 were CORRECTED IN PLACE while completing it** — ¶7's **self-falsifying whole-file grep** (the same species the phase-74 IMPL had just fixed in ADR-0296 ¶3, reproduced one ADR later) is restated with **NO number** and the searched token deliberately **NOT spelled**, because every restatement mints another counter-example; and ¶9's *"SELF vs OTHER-ADR"* form rule is replaced by **graft SCALE** after the n=7 population was re-enumerated first-hand. ⚠️ **¶9 ALSO said *"the only two instances in the file"* — which this row's own blockquote makes three** — so a pure append would have shipped a SECOND self-falsifying count in the commit that fixed the first. **The prescription has now outlived THREE wrong justifications: family, self-vs-other, scale.** ⚠️ **`ADR-0296` is NO LONGER byte-untouched** — its §Decision (g) carries the phase-75 correction as an INDENTED blockquote (`grep -c '^  > '` **2 -> 3**), **LEADING WITH WHAT SURVIVES** (no such counter was owed *in phase 74*, whose sole stat-asserting fixture is `require=true` on both sides — **the deferral was RIGHT**) and claiming **NO ORDINAL**.)*
- **go.mod modules:** **Phase 76 landed +0** — `go mod tidy -diff` EMPTY and `git diff master -- go.mod go.sum` EMPTY; ⚠️ **the row's TEST-side import delta is +8 and that is CORRECT** (`ringhash_test.go` gains `fmt`/`math`/`mathrand "math/rand/v2"`, `linkage_test.go` adds `go/ast`/`go/parser`/`go/token`/`strconv`/`testing`) — all stdlib, all test-side, and **an import LINE is not a go.mod MODULE**; there is no new sub-package, so `reference_new_subpackage_pulls_transitive_module` does not bite. NC observed: a fake `require` ⇒ 67 -> 68. The tracked lineage figure is **2** (the phase-61.2 count: `quic-go` direct + `qpack` indirect) — **NOT a repo total**; the single `go.mod` requires **67** (18 direct + 49 indirect). **Phase 75 landed +0** — `go mod tidy -diff` EMPTY and `git diff master -- go.mod go.sum` EMPTY. The row needed `crypto/tls` and `internal/stats`, both ALREADY imported by `internal/listener/manager.go`; **an import LINE is not a go.mod MODULE**, and there is no new sub-package, so `reference_new_subpackage_pulls_transitive_module` does not bite. ⚠️ **TEST-side imports grew by exactly four** (`log`, `math`, `net/http`, `strconv` in `0110`'s driver) — all stdlib, all test-side, **not a production-envelope violation**.
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` (see `ENVOY_TARGET.md`).
- **conformance:** h2spec **`95 tests, 94 passed, 1 skipped, 0 failed`** (the skip is 6.9.2/2, invariant) — the CORRECTED 95-case strict scope under dotted selectors, measured at the phase-85 IMPL close 2026-08-10 (ADR-0307; the historical 53/53 records measured only 53 of the 95 under nine silently no-op slash-form selectors, 2026-04-25 -> 2026-08-10); proxy-wasm all 10 families PASS.

---

## Recent lineage

*(A SHORT recent lineage, capped at FIVE prior entries by ADR-0288. Everything earlier is in `STATE_HISTORY.md`. §Current carries the newest stage (**the phase-88 IMPL** — PHASE 88 CLOSED, row 88 `done`, the codec now reassembling CONTINUATION on read and splitting on write in both directions; a real defect in this row's OWN write contract found by its unit-test author and fixed before landing; the surviving-field set shown RUN-TO-RUN NON-DETERMINISTIC; the PLAN's entropy MECHANISM refuted while its conclusion stands; the discard shown to defeat trailer VALIDATION, not only delivery). The five below are the prior stages, newest first. ⚠️ **EVICTED AT THIS CLOSE:** `phase 87 (h2-double-slash-path-routing) SPEC done` (2026-08-13) — chosen by **LIST POSITION (the LAST entry)**, because a DIRECT READ of all five dates before removal gave **2026-08-14 x1 and 2026-08-13 x4**, i.e. the **FOUR-WAY TIE at the oldest date** the router predicted, unlike the phase-88 PLAN's close where the oldest was unique. It is now in `STATE_HISTORY.md`, appended strictly (496 -> 498, `git diff --numstat` = `2 0`, base a byte-exact prefix via `cmp`). ⚠️ **Its absence guard was found BROKEN BY ITS OWN NC and rebuilt before use — see §Current `last-updated`.**)*

- **prior active-phase:** `phase 88 (h2-continuation-frames) IMPL done` (2026-08-15) — lifecycle-state **3 -> DONE**; ⚠️ **PHASE 88 CLOSED, ROW 88 FLIPPED `done`** at `ROADMAP.md:150` (`ROADMAP.md` numstat **`2 2`** — the status cell plus the `:204` gRPC-window narrowing 11 candidates -> 10, both MEASURED on each side, check (2) staying SIX exactly as the PLAN predicted). The H/2 codec now REASSEMBLES CONTINUATION on read and SPLITS header blocks on write in BOTH directions; ADR-0310 COMPLETED (strict `PROPOSED` guard **1 -> 0**), two `BEHAVIOR_CONTRACT.md` bullets ADDED mid-file (**every by-line cite at/below the `## HTTP/2` list shifted +2 — CITE BY STRING**). Production **+284** net over 3 files against a ~+190-240 PLAN band. Base master `26041b1b`.

- **prior active-phase:** `phase 88 (h2-continuation-frames) PLAN done` (2026-08-14) — lifecycle-state **2 -> 3**; **ROW 88 STAYS `in-progress`** at `ROADMAP.md:150`, `ROADMAP.md` **BYTE-UNTOUCHED** (verified by EMPTY DIFF against master), sentinel `want` stays **120**, and the strict `^> **STATUS: PROPOSED` guard **STAYS ARMED at 1** — the phase-88 IMPL disarms it, not this stage. Base master **`fe023fb5`** (from `git rev-parse master`), branch `phase-88-plan`. Docs-only: ZERO production `.go`, ZERO test `.go`; `DECISIONS.md` and `BEHAVIOR_CONTRACT.md` **BYTE-UNTOUCHED** too. ⚠️ **SUBAGENT-DRIVEN RETURNS — a NAMED RETURN from a FIVE-STAGE inline departure**: four probe agents on disjoint detached worktrees, disjoint port bands 47490-47529, private scratch each, **each committing NOTHING and each proving its tree clean**; the controller ran the sentinel battery, the counts, the cross-agent reconciliation, and **re-derived every load-bearing agent claim by execution** — which caught a wrong reference-image cite (`v1.33.0`) inside this branch, refuted by the code path: `parseEnvoyTarget` reads `ENVOY_TARGET.md`, pinning `contrib-v1.37.2`, while `harness_test.go:21/28`'s `v1.34.0` is a synthetic string inside a parser unit test. ⚠️ **THE HEADLINE: THIS PLAN REFUTES ITS OWN SPEC ON Q3, AND THE ROW GOT MORE URGENT.** SPEC §6 concluded the client leg is *"silent-loss-only, masked by CONNECTION LIFECYCLE"* on the strength of three distinct upstream source ports; **MEASURED here the upstream connection is POOLED AND REUSED (`127.0.0.1:34754` for all three requests), so the masking does not exist and the damage is CUMULATIVE** — request 1 loses **84 of 300** headers silently, request 2 **HANGS** (`context deadline exceeded` after 8.004 s), request 3 gets **502** while envoy-go tears down the pooled upstream with `GOAWAY COMPRESSION_ERROR`, hitting unrelated concurrent traffic. **88.2 is not the optional leg.** ⚠️ **FOUR SPEC ARM PARAMETERS REFUTED — the control variable is the HPACK-ENCODED block size, not raw bytes**: G at 20000 B wrote `HEADERS len=16042` (one frame, all headers arrive), I at `?big=20000` did not split at high entropy, K at `?many=200` wrote `len=15175` and delivered 200/200 twice — three vacuous arms, re-run at parameters measured to split; **and entropy silently moves the split point** (a compressible `'B'x20000` pad DID split where a high-entropy one did not), so **every arm must pin the split by asserting WIRE FRAMES, never a byte count**. ⚠️ **THE LOSS IS PARTIAL, NOT TOTAL, so the SPEC's intended fixture assertion was a DECOY** — two independent seams agree (h2c `NHDR 4 -> 1` and 216/300; `0004` TLS-h2 at 32000 B shows the small sentinel ARRIVING with `padlen=0` both directions): fields encoded BEFORE the split survive, so **the load-bearing assertion is the LENGTH of the large field**, never sentinel presence and never *which* headers survive. ⚠️ **THE THIRD LEG IS TWO-SIDED AND ONLY ONE SIDE IS INDEPENDENTLY ANCHORABLE**: 88.3s (server/response write) IS red-anchored at the unmodified tip (`direct_response` + 40 `header_mutation` response mutations ⇒ `RAW-FRAME: HEADERS len=29408 END_HEADERS`, exceeding the advertised 16384 by 13024 bytes; conforming x/net peer gets `http2: frame too large`; 5915-byte control clean), while 88.3c (client/request write) is NOT — `h2dispatch.go:457` calls `SetH2Request` BEFORE the decode chain so filter mutations never reach the H2 upstream request (a **pre-existing framework property, not a phase-88 defect**), and a large downstream block is capped on read; corroborated on the upstream wire (`http2debug=2`: backend read `HEADERS len=35023`/`32022` against a peer advertising 16384). ⇒ **D-88-SEQ FROZEN: ONE ATOMIC IMPL COMMIT, TDD held inside, NO sub-phase rows minted** (`want` stays 120). **D-88-CODE FROZEN, BOTH CANDIDATES REFUTED:** the reference emits **`RST_STREAM INTERNAL_ERROR(2)`, stream-scoped, NO GOAWAY** (`http2.too_many_headers`, stat `http2.header_overflow`, 60 KiB `max_request_headers_kb`), measured 5x — neither `FRAME_SIZE_ERROR` nor `ENHANCE_YOUR_CALM`, and the real divergence is **SCOPE**; RULED **connection-scoped `ErrEnhanceYourCalm` at 16 MiB** because under the decided byte-accumulator a stream-scoped reject leaves the HPACK table permanently desynced — ⚠️ recorded as a **cost tradeoff, NOT an impossibility** (a decode-and-discard drain would achieve parity; filed as a deferred candidate), with **both divergences NAMED in the contract**. **D-88-STAT: no new stat, delta 0. D-88-LASTID: PINNED** — `LastStreamID` **1 -> 0** CONFIRMED, origin confirmed as x/net (`framer: connection-error code=1` both sides), pinned with a behavioural improvement the SPEC did not claim (**the tip still proxies the truncated request and returns 200 after the GOAWAY; the prototype does not**). ⚠️ **THE SPEC'S "DEFENCE IN DEPTH" INSTRUCTION IS REFUTED — the 16 MiB bound is REAL COVERAGE**: nil-accumulator and wrong-stream ARE unreachable (isolation proven — with `AllowIllegalReads` the byte-identical inputs both deliver cleanly, so `checkFrameOrder` is the sole gate), but the over-bound arm is **REACHABLE** (`frames=1025 accumulated=16793600 bound=16777216 firstErr=<nil>`; end-to-end **the tip absorbed a 19.6 MB flood with no error and still proxied the request**); ⚠️ **nothing upstream binds — envoy-go does NOT inherit x/net's CVE-2023-45288 mitigation** (it lives in `readMetaFrame`, never enabled) and `SETTINGS_MAX_HEADER_LIST_SIZE` is never advertised; ⚠️ **and the exposure runs the other way — the TIP is safe because discarding retains nothing, so THIS ROW'S ACCUMULATOR INTRODUCES the retention and the bound is the mitigation for a hazard the fix itself creates.** **FIXTURE BLOCKER RESOLVED, MEASURED: extend `0004` in place at `37 0` + `78 0` = 115 insertions, 0 deletions, 2 files, 0 new files** (controller re-derived from the saved patch), +0 on every count axis; option 2 REFUTED by execution (only `0012`/`0015`/`0005` read request headers and `grep -ln http2` over all three returns nothing). **The roster is 11 rows, not 4 (130 lines MEASURED)** — a hardcode-16384 break arm reddens ONLY `peer_max_larger`/`peer_max_max_legal` while **every SPEC row stays GREEN**; ⚠️ the zero-`MaxFrameSize` case is genuinely reachable (`driveHandshake` sends `fr.WriteSettings()` with no arguments) and **the policy is ALREADY LANDED TWICE** (`conn.go:778-783`, `client.go:854-858`) — **reuse the sibling constant, do not mint one.** **Cost: production MEASURED net ~+189** (read +122, write +67), band **~+190-240**, ⚠️ of which **94 of 222 read-leg insertions (42%) are pure code motion and ~95 are comments — genuinely new executable code is ~70-80 lines**; test band **~+400-580** (differential 115 and roster 130 both MEASURED). **Quoted as a FLOOR** with three newly-discovered un-enumerated classes NAMED: PRIORITY-flag on a split HEADERS (**entirely unprobed**), split trailers, and a split block on a concurrently-reset stream.

- **prior active-phase:** `phase 88 (h2-continuation-frames) SPEC done` (2026-08-14) — lifecycle-state **1 -> 2**; **ROW 88 STAYS `in-progress`** at `ROADMAP.md:150`, `ROADMAP.md` **BYTE-UNTOUCHED** (verified by EMPTY DIFF against master), sentinel `want` stays **120**. Base master **`0158192c`** (from `git rev-parse master`), branch `phase-88-spec`. Docs-only: ZERO production `.go`, ZERO test `.go`; `BEHAVIOR_CONTRACT.md` **BYTE-UNTOUCHED**; `DECISIONS.md` gains **ADR-0310 §Context** STATUS `PROPOSED` (strict guard **0 -> 1** — the live pointer the phase-88 IMPL disarms). ⚠️ **NAMED INLINE DEPARTURE, FIFTH CONSECUTIVE STAGE: no investigation agents — every probe INLINE by the controller**, centred on **TWO COMPILING PROTOTYPES in DETACHED worktrees** (`wt-88-proto-a` the accumulator, `wt-88-proto-b` the `ReadMetaHeaders` alternative; both DELETED WHOLE at close) so Q1 was decided by MEASUREMENT, not argument. **ALL EIGHT BRAINSTORM §7 QUESTIONS DISPOSED BY EXECUTION.** ⚠️ **THE HEADLINE: THE BRAINSTORM'S TWO-LEG STRUCTURE IS REFUTED — THERE ARE THREE LEGS, AND THE READ FIX ALONE IS A REGRESSION.** `WriteContinuation` appears **NOWHERE in `internal/`**: both header-write sites emit the whole encoded block as one `WriteHeaders(... EndHeaders: true)`, and x/net's `WriteHeaders` does NOT split. That gap is MASKED at the tip (the read side drops the headers before they can be re-emitted) and UNMASKED by the read fix — the padded-upstream-response arm turns `200`-with-silent-loss into a hard **`http2: frame too large`**; raising the probe client's `MaxReadFrameSize` to 1 MiB restores a full-fidelity 200, **proving envoy-go emitted ONE oversized HEADERS frame**. The request direction passed only because x/net's **Server** defaults to a 1 MiB read frame size while its **Transport** uses the 16 KiB spec default — **the write gap is DIRECTION-INDEPENDENT**. ⇒ revised **D-88-SEQ: 88.1 server read · 88.2 client read · 88.3 write (both directions)**, with the **MEASURED ordering constraint that 88.3 lands WITH OR BEFORE 88.2** (88.2 alone is a strictly worse user-visible outcome than the defect); 88.1 alone stays safe and shippable. **D-88-PRIM decided by prototype: the per-connection accumulator.** `Framer.ReadMetaHeaders` REFUTED on four grounds, three MEASURED — ⚠️ **it compiles cleanly with ZERO diagnostics and then HANGS** (`ReadFrame` starts returning `*http2.MetaHeadersFrame`, matching no arm of either `dispatchFrame` and falling through to the silently-ignore default; a scoped `-run TestServerConn` was `Terminated` at a 45 s timeout); it **MUTATES the decoder it is handed** (`SetEmitFunc`/`SetEmitEnabled`/`SetMaxStringLength`, and `decodeBlock` is built entirely on that emit callback); it **pre-empts envoy-go's own pseudo-header validation** with differently-worded x/net `StreamError`s in a family h2spec §8.1.2.1 covers 4/4 and row 87 just touched; blast radius **6 non-test sites + 37 test-side references** vs the accumulator's 3 production files — **and it does nothing for the write leg.** **COST MEASURED: net +150 production `.go` over THREE files** (`client.go` 136/91, `conn.go` 81/14, `framer.go` 38/0 — inflated on BOTH sides by pure code motion; IMPL band **~+150-190**), quoted as a **FLOOR** (`reference_measured_prototype_is_a_lower_bound`: three decode sites, two write sites, per-connection state on two structs — nothing about row 87's single-call-site escape transfers). Prototype A is GREEN on every BRAINSTORM arm (B `RST_STREAM PROTOCOL_ERROR` -> **200**; C header **restored**; D **200 both streams**, was `GOAWAY COMPRESSION_ERROR`; G header **PRESENT** at 20000/40000 B; I **full pad** at default client settings) with existing `internal/filter/hcm/...` suites green, `go.mod` +0 and stat-surface DELTA 0 both MEASURED on the prototype (`NewCounter(|NewGauge(` reads **406** in BOTH trees). ⚠️ **THREE FINDINGS CARRIED:** (a) **h2spec §6.10 CONTINUATION passes 6/6 AT THE TIP THAT DISCARDS EVERY CONTINUATION FRAME** — incl. *"Sends multiple CONTINUATION frames preceded by a HEADERS frame"* — with the suite reading `95 tests, 94 passed, 1 skipped, 0 failed` IDENTICALLY at tip and prototype: those tests assert the connection's REACTION, never that the fields arrived, so **a conformance section that NAMES the feature is fully green while the feature is entirely absent**; (b) **a probe that could not answer its own question** — the client-leg desync was first probed with a single 20 KB header, which **exceeds the 4096-byte HPACK dynamic table and is never indexed**, so dropping it cannot desync anything; re-probed with 200 small INDEXABLE headers the tip delivers **165 of 200** twice with no connection-fatal error, and three distinct upstream source ports show **envoy-go opens a FRESH upstream connection per request** ⇒ the client leg is **silent-loss-only, masked by CONNECTION LIFECYCLE rather than code safety** (recorded as a bounded measurement, NOT as "the client leg is safe"); (c) ⚠️ **A STALE SERVER MADE THE PROTOTYPE READ IDENTICAL TO THE TIP** — the first battery reproduced every tip failure, which looks exactly like "the fix does nothing"; the prototype binary had never started (`bind: address already in use` on the admin port, because a `pgrep -f 'a\|b'` alternation matched nothing and the tip still held it), discriminated by the server log plus a `strings` symbol assertion (**2** occurrences in the prototype binary, **0** in the tip). **Every later reading asserts the bound binary with `ss -ltnp` first**; a sibling instance (a probe edit that failed `go build`, leaving the OLD binary to produce a stale plausible reading) was caught the same way.

- **prior active-phase:** `phase 88 (h2-continuation-frames) BRAINSTORM done` (2026-08-13) — lifecycle-state **DONE -> 1**; **ROW 88 REGISTERED `in-progress`** at `ROADMAP.md:150` and the sentinel `want` bumped **119 -> 120 in the SAME commit** (the phase-84/85/86/87 precedent). Base master **`244e482f`** (from `git rev-parse master`), branch `phase-88-brainstorm`. Docs-only: ZERO production `.go`, ZERO test `.go`; `DECISIONS.md` and `BEHAVIOR_CONTRACT.md` **BYTE-UNTOUCHED** (a BRAINSTORM adds no ADR; next-free stays **ADR-0310**; strict `PROPOSED` guard stays **0** — the phase-88 SPEC re-arms it). **SELF-PICKED** per the 2026-07-12 standing directive — phase 87 CLOSED, all 119 rows `done`, check (1) SILENT at `want=119` before this stage (the FOURTH silent reading) and re-opened as exactly `NOT DONE: row 88` after. ⚠️ **NAMED DEPARTURE, FOURTH CONSECUTIVE STAGE: no investigation agents — every probe INLINE by the controller** (tip binary built with `-o` into scratch; a raw HEADERS/CONTINUATION h2c client `probe88` written into the worktree, built, and **DELETED at close with the tree proven clean**; a stock `x/net/http2.Transport`; an h2c echo backend; ONE reference container `b88-main`/`contrib-v1.37.2` torn down **BY NAME**, 0 remaining, with FOUR pre-existing containers from other work left UNTOUCHED; ports 47450/47451/47455/47470 transient only). **THE HEADLINE: envoy-go DISCARDS HTTP/2 CONTINUATION frames on BOTH the downstream and the upstream path.** `internal/filter/hcm/h2/conn.go`'s `dispatchFrame` (cite BY SYMBOL) returns `nil` for `*http2.ContinuationFrame` behind a comment whose two clauses are **BOTH FALSE** — `ReadMetaHeaders` is assigned only in `test/differential/runner_test.go`, never in `internal/`, so the arm IS reached in normal usage — while `onHeaders` decodes with `HeadersEnded()` and **never branches on it**, dispatching the PARTIAL field set and leaving the HPACK dynamic table permanently diverged from the peer's; `client.go` has **no `ContinuationFrame` case at all** (`:417`/`:428`). **REPRODUCED BY EXECUTION at `244e482f` with a GREEN POSITIVE CONTROL on both sides** (single-frame `GET /marked` -> 200 both): `:path` in the CONTINUATION -> subject **RST_STREAM PROTOCOL_ERROR** vs reference **200**; a header in the CONTINUATION -> subject **200 with the header SILENTLY GONE** (its own un-split control echoes it) vs reference **200 `SAW-X-PROBE`**; one split request then an ordinary un-split one -> subject **GOAWAY COMPRESSION_ERROR** — **the blast radius of one split request is the CONNECTION, not the request** — vs reference **200**; upstream-side, a padded RESPONSE header block -> **response headers silently dropped through envoy-go** while a direct-to-backend BYPASS CONTROL preserves them. ⚠️ **THE FACT THAT CHANGES THE PRIORITY, NEW AT THIS STAGE: the defect is reachable by ORDINARY CLIENTS, not just a raw frame writer** — a stock `x/net/http2.Transport` splits a header block past the default 16384-byte `SETTINGS_MAX_FRAME_SIZE`, and at 20000 B the subject answered **200 while dropping `accept-encoding`, `user-agent` AND the padding header**; a large cookie or JWT suffices. Every prior stage treated CONTINUATION as exotic. ⚠️ **A COST REDUCTION FOUND BY EXECUTION: the row does NOT owe an RFC 9113 §6.10 ordering state machine** — the subject's PROTOCOL_ERROR on an interleaved PING comes from **x/net's own `Framer.checkFrameOrder`** via `translateFramerErr` (subject log `framer: connection-error code=1`), NOT from envoy-go code; attributing it to envoy-go would repeat the `reference_code_comment_not_evidence` error the false comment already committed once. What is owed is narrower: accumulate fragments until `END_HEADERS`, decode once, per direction. **D-88-SEQ PROPOSED: a SPLIT phase, two legs — 88.1 downstream/server, 88.2 upstream/client** — both independently red-anchored, 88.1 alone shippable; **NO sub-phase ROWS minted at this stage** (the row-84 precedent is a split in PROSE), and minting them later moves `want` a SECOND time. ⚠️ **ONE OPEN QUESTION DELIBERATELY NOT CLAIMED:** whether the CLIENT leg also desyncs its HPACK decoder the way the server leg provably does — two consecutive large-response arms both succeeded, which is CONSISTENT WITH per-request upstream connections but does not establish it; the SPEC settles it by probe. Cost **ESTIMATED ~+60-140 net production `.go` over TWO files / ~+300-700 net test** — ⚠️ **AN ESTIMATE, NOT A MEASUREMENT; the SPEC MUST ENUMERATE BY COMPILING PROTOTYPE** (row 87 broke a ten-firing `reference_measured_prototype_is_a_lower_bound` streak only because it had ONE grep-confirmed call site; this row has THREE decode sites across TWO files plus per-connection state).

- **prior active-phase:** `phase 87 (h2-double-slash-path-routing) IMPL done` (2026-08-13) — lifecycle-state **3 -> DONE**; ⚠️ **PHASE 87 CLOSED, ROW 87 FLIPPED `done`** at `ROADMAP.md:149` — the ONLY `ROADMAP.md` edit (`git diff --numstat` = **`1 1`**, the status cell ALONE; the row's summary prose verified **BYTE-IDENTICAL by sha256** over the status-blanked line, the row-86 precedent), sentinel `want` **STAYS 119**, 237 lines / 119 data rows unchanged, and **the strict `^> \*\*STATUS: PROPOSED` guard DISARMED 1 -> 0** by completing ADR-0309. Base master **`55763717`**, branch `phase-87-impl`, ONE squashed commit. **THE HEADLINE: envoy-go's hand-rolled H2 downstream codec now parses the origin-form `:path` as a REQUEST-TARGET** — `buildRequest` swaps `url.Parse` -> `url.ParseRequestURI` behind a three-line `strings.IndexByte(path,'#')` fragment guard, so a leading `//` is path bytes rather than an authority: `//foo` routes to the `//foo` route instead of 404-ing on an empty path, and `//foo/bar` routes on the FULL path instead of being **silently mis-routed to `/bar`**. **TDD held INSIDE the one commit — both RED censuses observed with failure lines READ before the production edit** (T0 and T1 are BOTH prerequisites of T2; landing the ten-line fix first would have converted both censuses into unfalsifiable green-on-arrival claims). Unit: **8 of 13 rows RED, matching the PLAN's expected set EXACTLY**. Differential: `TestDifferential/0004-h2-routing` RED on the `//edge` **status** arm (`subj drive: //edge: status=404, want 200`), named subtest confirmed PRINTED. ⚠️ **METHOD — SUBAGENT-DRIVEN RETURNS, a NAMED RETURN from the three-stage inline departure:** four task agents on disjoint file sets, each with private scratch, each committing locally with EXPLICIT PATHSPECS; the controller did the production edit, the ROADMAP flip, the sentinel battery, the STATE roll, the gates and the squash, and **re-derived every load-bearing agent claim by execution** — which is how a wrong fixture-path cite minted inside this branch was caught pre-squash. ⚠️ **COST — THE FLOOR HELD, AND THAT IS THE FINDING: total `.go` +219 net (production +13, tests +206 — the POST-REVIEW figure, 29 lines above the pre-review count) INSIDE the PLAN's ~135-236 band, BREAKING ten consecutive `reference_measured_prototype_is_a_lower_bound` firings.** ADR-0309 §Consequences (iii) records WHY rather than declaring a trend: ONE grep-confirmed production call site, measured twice by a compiling test-green prototype, and a PLAN that had already absorbed this row's two late-added test classes. **Nothing there licenses treating a multi-file prototype's measurement as an estimate.** ⚠️ **THE FINAL ADVERSARIAL REVIEW FOUND THREE REAL COVERAGE GAPS, ALL FIXED BEFORE THE SQUASH.** (a) **D-87-FRAG's central decision — the guard sits BEFORE the parse — was pinned by ZERO of the thirteen rows** (both implementations built and run against the table: 0/13 discriminated); a `rootless_fragment` (`foo#frag`) row was added and NEGATIVE-CONTROLLED — with the guard moved below the parse that row ALONE reddens, 1 of 15, revert sha256-verified. (b) The contract claimed percent-escape `Path`/`RawPath` semantics were *asserted* by a table that had **no `%` row and asserted `RawPath` nowhere** (`reference_verification_table_launders_wrong_cites`): a `wantRawPath` field and a `/foo%2Fbar` row were added AND the bullet rewritten to state the cross-side/unit assertion SPLIT. (c) Two of this stage's own ADR sentences overclaimed and were scoped — *"every form the codec accepts today"* (the row's OWN anchors refute it; now *"accepts AND ROUTES CORRECTLY"*) and *"a `:path` that is not a valid request-target is now REJECTED"* (**opaque/scheme-form targets like `example.com:80` are STILL ABSORBED into the same silent 404 — verified by execution**; now scoped to the three named families). Its reverse hidden-enforcement hunt — anything depending on the ABSENCE of the two new rejects — came back **EMPTY after an enumerated search**. **Gates (run LAST against the FINAL tree):** differential **121/121 `INNER_EXIT=0`** anchored panic gate SILENT 382.3 s · `go test ./...` **rc=0, zero FAIL, 127 ok** · `-race` clean · gofmt/vet/golangci-lint clean · **h2spec `95 tests, 94 passed, 1 skipped, 0 failed` from this row's own run** (skip `6.9.2/2`; §8.1.2.1 Pseudo-Header Fields **4/4 with the new rejects live**) · fixtures **121 +0** · fuzzers **55/48 +0** · BackendKind tail **38 +0** · `go mod tidy -diff` EMPTY · **stat-surface DELTA 0 by the same command both sides (327/79), base in a disposable detached worktree removed with proof**. ⚠️ **TWO FLAKES, IDENTITIES CAPTURED BEFORE RE-RUNNING, NEITHER TOUCHING THIS ROW:** the differential's FIRST launch **ABORTED at fixture 89 of 121** (`0087-tracing-otlp`, OTLP receiver `0.0.0.0:35323` bind failure) — **32 fixtures NEVER RAN while the harness wrapper exited 0**, only the captured `INNER_EXIT=1` discriminating them; `0087` joins `0081` (reproduced at `0.0.0.0:35097`) as a NAMED member of the driver-owned receiver port race. And **`TestSDSEndToEnd_FetchFailure_BootFailsClosed/silent_SDS_server…`** (`boot_sds_e2e_test.go:551`, gRPC `DeadlineExceeded`) recurred **byte-for-byte identical to the phase-87 SPEC's record — a THIRD firing with a now-stable signature**; both cleared scoped.



## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
