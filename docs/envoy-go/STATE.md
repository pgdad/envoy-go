# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `06.1-stats-prometheus`
- **phase-directory:** `docs/envoy-go/phases/06.1-stats-prometheus/` — contains `SPEC.md` (at `ad46930`, frozen) plus `PLAN.md` (this commit). Parent `docs/envoy-go/phases/06-observability-baseline/` carries `BRAINSTORM.md` (`d271fd7`) + parent `SPEC.md` (`ad46930`). Sibling `docs/envoy-go/phases/06.2-access-log/` retains the placeholder `README.md` (`ad46930`). Phase 05 closed history unchanged.
- **lifecycle-state:** `3` — phase 06.1 PLAN drafted; the PLAN-drafting session on `phase/06.1-stats-prometheus-plan` (worktree `.worktrees/phase-06.1-stats-prometheus-plan`, branched from `be99b42` per ADR-0003) lands `docs/envoy-go/phases/06.1-stats-prometheus/PLAN.md` (~3463 LoC, 15 tasks). Task topology mirrors the 05.2 PLAN's 15-task shape: Task 1 preconditions + PROGRESS preamble; Tasks 2-5 build the `internal/stats` package (registry/counter/gauge/name/prom-writer) with TDD; Task 6 admin endpoint; Task 7 bootstrap Registry alloc; Tasks 8-12 stat-emit call site threading + boot wiring (cluster, listener, HCM, main.go, Freeze ordering); Task 13 `FuzzPromTextFormat`; Task 14 fixture `0005-prometheus-stats`; Task 15 BEHAVIOR_CONTRACT in-place edit + ROADMAP/STATE updates + closing all-gates sweep. Six ADRs anchored: ADR-0059 (Internal Stats Store) + ADR-0060 (Histograms deferred) bound to Task 2; ADR-0061 (SN1-SN8 flattening, Rule SN4 pinned) + ADR-0064 (`stats_config.stats_tags` not honored) bound to Task 4; ADR-0063 (per-endpoint cluster stats not emitted) bound to Task 8; ADR-0062 (differential equivalence shape for stats) bound to Task 14. ADR numbers re-verified next-free at PLAN-write per ADR-0004 against `DECISIONS.md` tail (ADR-0058 latest at `ad46930`). Net LoC estimate: ~1700 (within BOOTSTRAP §6.1 single-phase gate). Per BOOTSTRAP §5 the next transition is state 3 → 4 via `superpowers:subagent-driven-development` (06.1 implementation per the user's persistent subagent-driven-execution preference).
- **next-skill:** `superpowers:subagent-driven-development` — execute the 15 PLAN tasks via fresh-subagent-per-task with two-stage review per ADR-0005. Inputs: this commit's `docs/envoy-go/phases/06.1-stats-prometheus/PLAN.md` (the authoritative task decomposition), the 06.1 SPEC at `ad46930` (the design contract every task traces back to), the parent 06 SPEC at `ad46930` (cross-cutting context), `BOOTSTRAP_PROMPT.md` §§5-7 (lifecycle + differential test contract; §7.5 phase-done gates), `superpowers:test-driven-development` (the per-task TDD discipline), `docs/envoy-go/phases/05.2-upstream-h2/{PLAN.md,PROGRESS.md,REVIEW.md}` (the closed-history precedent for execution rhythm + PROGRESS conventions + REVIEW shape).
- **next-skill-scope:** Lifecycle-state 3 → 4 implementation session, **resume at Task 10**. Tasks 1–9 are landed and SHA-filled on branch `phase/06.1-stats-prometheus-impl`: T1 `04bf75d`; T2 `9417235` + `7ccdc73` (concurrency follow-up); T3 `474e8c7`; T4 `7f45a4d` (ADR-0061 + ADR-0064); T5 `18fbb41`; T6 `f4160b8` + `17da0e3` (`cmd/envoy-go/main.go` D-3.6 scaffolding) + `6a1f93c` (test-robustness REVIEW I-1 close); T7 `d1e2690`; T8 `a6f8b94` (ADR-0063 + 5 doctrine-D-3.6 cascade fixes in listener/HCM/tcpproxy test files); T9 `bdfc34b` (5 Minor code-quality findings carried forward to L4 review-followup — see Task-9 PROGRESS Notes). Tasks 10–15 remain: T10 listener Registry threading + 2-metric per-listener alloc + accept-loop hot path; T11 HCM per-instance metric alloc + dispatch-entry/response-class hot path + M-9 carry-forward log line; T12 `cmd/envoy-go/main.go` Registry threading (admin.New throwaway → `bs.Stats` swap) + `Freeze()` boot ordering; T13 `internal/stats/fuzz_test.go` `FuzzPromTextFormat` at 30s ADR-0018 budget; T14 differential fixture `0005-prometheus-stats` + ADR-0062; T15 BEHAVIOR_CONTRACT in-place edit + ROADMAP/STATE updates + closing all-gates sweep. Continue per `superpowers:subagent-driven-development` in worktree `.worktrees/phase-06.1-stats-prometheus-impl` (already exists; branch HEAD = `a68fdb5`); each task ends with a commit on the impl branch; SHA-fill follow-up commits land in the same impl branch. After lifecycle-state 4 (impl complete; PROGRESS Task 15 verification block landed; BEHAVIOR_CONTRACT in-place edits per ADR-0052), advance STATE to lifecycle-state 4 with `next-skill: superpowers:verification-before-completion` for the verification-session re-run of all six gates per BOOTSTRAP §7.5 (phase-04..05.2 precedent — verifier branches a fresh worktree from the impl-branch tip per ADR-0003). Three settled-decision-style PLAN deviations from this session worth carrying forward: (i) per doctrine D-3.6, every constructor-widening task in 06.1 must update `cmd/envoy-go/main.go`'s call site immediately (proper threading via `bs.Stats` once T7 landed it; throwaway `stats.NewRegistry()` only for the admin holdover until T12); (ii) cluster's `connWithGauge` wrapper introduced at T9 ripples into `internal/filter/tcpproxy/filter.go`'s `halfClose` interface check + a delegating `(*connWithGauge).CloseWrite()` method — same wrapper-cascade pattern likely surfaces at T10/T11 with listener/HCM hot paths; (iii) the SN-name regex from T2 (`^[a-zA-Z_]([a-zA-Z0-9_.]*[a-zA-Z0-9_])?$`) rejects hyphens, so test fixture names containing hyphens need either renaming or a sanitization shim like `mkTestCluster` introduced at T9 — T10's listener-side test fixtures should anticipate this.
- **last-commit:** `a68fdb5` — `phase 06.1 follow-up: PROGRESS SHA-fill for Task 9 (TBD → bdfc34b; 5 Minors carried to L4 review-followup)`, on branch `phase/06.1-stats-prometheus-impl`. SHA-fill of the Task 9 entry's `**Commits:**` line; the Notes paragraph also names the 5 Minor code-quality findings (M-1..M-5) carried forward to the lifecycle-state-4 review-followup per the Task-8 deferred-Minor precedent. The full task-by-task commit chain Task 1 → Task 9 (with each task's PROGRESS entry SHA-filled by its follow-up commit) is the durable record; no STATE-blocking issues encountered. Branch tip is `a68fdb5` at session exit. Resume next session at Task 10 per `next-skill-scope`.
- **last-updated:** 2026-04-27

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
