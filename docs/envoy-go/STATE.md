# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `14-http-filter-compressor` — phase 14 PLAN.md authored at this commit. ROADMAP row 14 status STAYS at `in-progress` (the planned → in-progress flip happened at the SPEC commit `073cb88`; the impl work happens before in-progress → done flip at phase-done commit per BOOTSTRAP_PROMPT.md §5). Phase 14 is the SEVENTH §9 family-row in the lifecycle (after cors @ 07.1 done, fault @ 09 done, header_mutation @ 10 done, local_ratelimit @ 11 done, csrf @ 12 done, buffer @ 13 done); the §9 family heading at ROADMAP line 56 stays unchanged per ADR-0106(c). The PLAN-authoring session lands a 16-task TDD-ordered implementation plan covering the 6 §1.1 amendments + 5 anticipated ADRs (ADR-0129..ADR-0133, ALL ALREADY LANDED at SPEC commit per ADR-0044 SPEC-time-anticipation discipline) + the 8 SPEC §12 deferred-decisions resolutions + 8 PLAN-emerging decisions; each task carries TDD test-first ordering + per-task ADR anchor + acceptance criteria + dependencies (typically linear; Task N+1 builds on Task N). Framework primitive `EncoderFilterCallbacks.OverwriteBody(b []byte)` lands at Task 4 (FIRST among impl tasks that consume it; per cold-start prompt Critical PLAN-time obligation 2 + ADR-0131 §Decision (vi) Lands-in-task field).
- **phase-directory:** `docs/envoy-go/phases/14-http-filter-compressor/` — contains `BRAINSTORM.md` (541 lines; pre-SPEC design sketch) + `SPEC.md` (~2050 lines; authoritative input) + `PLAN.md` (this commit; ~2800 lines covering scope + file-structure + 16 planner-time decisions + 5 ADR roster + 11 cross-references + 17 execution preconditions + 16 TDD-ordered impl tasks). PROGRESS.md, REVIEW.md authored over phase lifecycle (states 4 → 6).
- **lifecycle-state:** `phase 14 PLAN.md authored; impl pending` — phase 14 PLAN.md authored at this commit; implementation not yet started. Lifecycle-state-3 → 4 transition (BOOTSTRAP_PROMPT.md §5) completed by this commit; lifecycle-state-4 → 5 (impl-then-verify) is the next session's responsibility per ADR-0005 §Decision 4 + project memory `feedback_execution_style.md` always-subagent-driven preference. Phase 13's six gates remain green at master tip; this commit is docs-only (no code changes; no test re-validation needed).
- **next-skill:** `superpowers:subagent-driven-development` — per BOOTSTRAP_PROMPT.md §5 lifecycle-state-4 entry condition (PLAN.md exists, impl not started) + project memory `feedback_execution_style.md` always-subagent-driven preference + ADR-0005 §Decision 4 PLAN/impl session split. The impl session dispatches a fresh subagent per task per the subagent-driven-development skill; reviews between tasks; fast iteration. The PLAN's 16 tasks are sequenced linearly (Task N+1 builds on Task N) so subagent dispatch can proceed task-by-task with checkpoint commits.
- **next-skill-scope:** Cold-start scope for the phase 14 impl session. Read first: `docs/envoy-go/phases/14-http-filter-compressor/PLAN.md` (THE authoritative impl input — ~2800 lines; every TDD step + acceptance criterion + commit message lives there); `docs/envoy-go/phases/14-http-filter-compressor/SPEC.md` (cross-reference for SPEC §11 empirical pins + §6 component contracts + §13 BEHAVIOR_CONTRACT patches); `docs/envoy-go/DECISIONS.md` ADR-0125 amendment §(viii)-(x) + ADR-0129..ADR-0133 (the 5 new ADRs the SPEC drafted in their final form for impl-anchor scheduling — ALREADY landed; no re-authoring needed; impl-tasks reference via commit message + verify Lands-in-task field intact at HEAD). The impl session begins with Task 1 (precondition check + PROGRESS.md preamble) and proceeds through Task 16 (REVIEW.md per requesting-code-review skill). The 5 ADRs anchor at Tasks 2 (ADR-0129+ADR-0130) + 4 (ADR-0131) + 8 (ADR-0132) + 11 (ADR-0133) per per-ADR Lands-in-task fields. The PLAN's "## Execution preconditions" section enumerates 17 preconditions verified at Task 1 cold-start. The impl session opens a fresh impl-stage worktree `phase-14-http-filter-compressor-impl` branched off the master tip after the PLAN.md SHA-fill follow-up per ADR-0003 + project memory always-worktrees rule.
- **last-commit:** `<TBD>` — `Squash merge phase-14-http-filter-compressor-plan` (squash of `<TBD>` `phase 14 PLAN: http-filter-compressor [PLAN.md authored + 16 TDD tasks + 16 planner-time decisions + 5 ADRs anchored + STATE.md flip]`). Squash-merge of the PLAN-authoring chain on the `phase-14-http-filter-compressor-plan` worktree branch: (a) PLAN.md (~2800 lines) authored covering scope + file-structure + 16 planner-time decisions resolution + 5 ADR roster (all 5 ADRs ALREADY LANDED at SPEC commit per ADR-0044 SPEC-time-anticipation; PLAN documents the per-ADR Lands-in-task anchors) + 11 cross-references + 17 execution preconditions + 16 TDD-ordered impl tasks; (b) STATE.md flipped from "phase 14 SPEC.md authored; PLAN.md pending" to "phase 14 PLAN.md authored; impl pending"; (c) lifecycle-state-3 → 4 transition completed; (d) next-skill flipped from `superpowers:writing-plans` to `superpowers:subagent-driven-development`. Docs-only commit; no code changes; phase 13's six gates remain green at parent commit `2b262b8` (the SPEC SHA-fill follow-up).
- **last-updated:** 2026-05-10

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
