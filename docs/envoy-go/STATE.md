# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

> **This file is a POINTER, not an archive** — `BOOTSTRAP_PROMPT.md` §4: *"STATE.md — pointer: active phase, last commit, next action."* It carries the live pointer, the live counts, and a SHORT recent lineage. **Full stage-by-stage history lives in `STATE_HISTORY.md`** (archived at ADR-0288; **nothing was deleted** — the pre-move file is preserved there verbatim, byte-for-byte).
>
> **⚠️ THE RULE ADR-0288 EXISTS TO ENFORCE: there is EXACTLY ONE live `next-skill:`, `lifecycle-state:`, `next-free ADR:`, and count block IN THIS FILE, and they live in §Current pointer directly below.** Before that move, ~330 lines of prepended backlog had buried TWO fossilised "current state" blocks whose `next-skill:` still read *"the 42.2 hedging BRAINSTORM"* and *"the 46.2 SPEC"* while the live phase was **66** — so `grep 'next-skill:' STATE.md` returned **two stale hits and ZERO live ones**, and `grep 'fuzzers:'` returned **48** and **42** when the true count was **55**. **A session may trust a grep of this file again. Keep it that way: when you advance the pointer, EDIT §Current pointer IN PLACE — never prepend a new block above it.**

---

## Current pointer

*(The ONLY live "what next" in this repo. Update IN PLACE at every stage close.)*

- **active-phase:** `phase 68 (tls-cvc-empty-dynamic-fallback) IMPL done` (2026-07-19)
- **phase-directory:** `docs/envoy-go/phases/68-tls-cvc-empty-dynamic-fallback/` (`BRAINSTORM.md` + `SPEC.md` + `PLAN.md` + `PROGRESS.md` present; **row 68 is `done`** — flipped at the six-gate)
- **lifecycle-state:** `DONE`. **ROW 68 FLIPS `in-progress` → `done`** at this IMPL six-gate (ADR-0106, the SOLE leg — `reference_roadmap_split_phase_row_done`; a SINGLE FLAT ROW, 9 tasks T1–T9; the ADR-0045 escape valve stayed armable/UNCONSUMED, no split). The xDS family STAYS OPEN.
- **next-skill:** **the phase-69 BRAINSTORM** — the roller SELF-PICKS the next row per the 2026-07-12 standing directive (no human pick). Open a FRESH worktree off master (`feedback_git_worktrees`); subagent-driven (`feedback_execution_style`); commit LOCALLY only, controller squash-pushes at close (`feedback_subagents_no_push` / `feedback_push_to_origin`). **The phase-68 spine that landed (PROGRESS.md ledger):** **T1** (`f6c89787`) `internal/xds` — `ErrEmptyValidationContext` defined beside `errValidation` (`stream.go:43`), wrapped ONLY at the fully-absent-`trusted_ca` site in `parseValidationSecret`, dual-`%w` at `applyValidationResponse` (`stream.go:174`); the `applyResponse` twins + empty-resources wrap stay `errValidation`-only (RD-DUPSITE); `provider.go` BYTE-UNTOUCHED (NACK/`update_rejected` posture stands — D-EDF-STREAMPOSTURE (b)). **FINAL-REVIEW CORRECTION:** the sentinel gate landed as `GetTrustedCa() == nil || GetTrustedCa().GetSpecifier() == nil` and was NARROWED to `== nil` alone before merge — the second disjunct swept a present-but-specifier-unset `trusted_ca:{}` (S1b) into the ACK-and-fallback class, which the reference PGV-NACKs (`specifier … is required`, SPEC-66 §3.9(b)/ADR-0287); S1b now stays a boot-FAIL, its unit case moved to the no-sentinel test, and ADR-0290/BEHAVIOR_CONTRACT/STATE reconciled to ADR-0287. **T2** (`32eb2a4b`) `internal/tls/config.go:184-223` fallback branch — `errors.Is` sole gate → `default_validation_context.trusted_ca` via `loadTrustedCAPool`+`installPool`; empty-both → phase-67 no-anchor logic (byte-identical `:204` reject / `NoClientCert`); S2/S3/timeout stay boot-FAIL; P5 block byte-intact. **T3** (`b5d53c3f`) `sdsserver.WithEmptyValidationContext` (S1, distinct from `WithValidationContext(name,nil)`=S2). **T4** (`81b1691f`) fuzz `"downstream-sds-empty"` side + seed, dispatch-verified (+0 fuzzers). **T5** (`788bcc57`) fixture `0111` (port 10447, three-arm forced-send at require=true). **T6** (`22acb9f4`, empty) breaks — Break G union-pool fired the untrusted-arm upper-bound in BOTH send-modes. **T7** (`b0a9c6b4`) BEHAVIOR_CONTRACT B1–B4 (item 3(a) CLOSED, item 10 NARROWED). **T8** six-gate (controller-run at `b0a9c6b4`). **T9** ADR-0290 completed IN PLACE + this roll. **RE-DERIVE — do not trust this pointer's cites; where it cites, go look.**
- **last-commit:** the phase-68 IMPL squash (`git log --grep 'phase 68'`; locate by SUBJECT, never by position). Prior: the phase-68 PLAN squash.
- **last-updated:** 2026-07-19 (phase-68 IMPL done — row 68 flips `done` at the six-gate; DECISIONS tail ADR-0290 COMPLETE; fixtures 112 → 113)
- **next-free ADR:** **`ADR-0291`** — DECISIONS.md tail is **ADR-0290**, now COMPLETE (§Decision + §Consequences landed at the phase-68 IMPL IN PLACE, STATUS flipped to COMPLETE; no renumber, no further flip); next-free **ADR-0291** is FREE — `grep -c '^## ADR-0291'` → 0.

---

## Project counts at this commit

*(Live. Re-verify at session start — `git fetch` first — and update here at every stage close. Canonical commands inline.)*

- **active differential fixtures:** **113** — `ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0111-tls-cvc-empty-dynamic-fallback`.
- **fuzzers:** **55** — `grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`. Phase 66 added SEEDS only (a new `downstream-sds` dispatch side in `FuzzTLSContextParse`) ⇒ **+0**.
- **stat surface:** **1201** — BEHAVIOR_CONTRACT doc count. **There is NO mechanical counting command.** Phase 66 was **+0**.
- **BackendKind tail:** **38** (`H2GoawayResponder`).
- **DECISIONS.md tail:** **ADR-0290**, now **COMPLETE** (next-free **ADR-0291**) — `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1`. ADR-0290 §Context was DRAFTED at the phase-68 SPEC (the tail flipped ADR-0289 → ADR-0290 at that commit per ADR-0044); §Decision + §Consequences landed at the phase-68 IMPL IN PLACE and the STATUS banner flipped to COMPLETE (no renumber, no further flip). ADR-0289 is COMPLETE (phase-67 IMPL).
- **go.mod modules:** the tracked lineage figure is **2** (the phase-61.2 count: `quic-go` direct + `qpack` indirect) — **NOT a repo total**; the single `go.mod` requires **67** modules. Phase 66 was **+0**.
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` (see `ENVOY_TARGET.md`).
- **conformance:** h2spec 53/53; proxy-wasm all 10 families PASS.

---

## Recent lineage

*(The last FIVE stages only — enough for the router's "prior-phase lineage" read. Everything earlier is in `STATE_HISTORY.md`. These bullets are carried over VERBATIM; the newest mirrors §Current (the phase-68 IMPL done), and the oldest (phase-67 PLAN done) was dropped as this list re-capped at five.)*

- **prior active-phase:** `phase 68 (tls-cvc-empty-dynamic-fallback) IMPL done` (2026-07-19) — lifecycle-state 3 → DONE; **ROW 68 FLIPS `in-progress` → `done`** at this IMPL six-gate (ADR-0106, the SOLE leg — `reference_roadmap_split_phase_row_done`). Worktree `.worktrees/phase-68-impl`, branch `phase-68-tls-cvc-empty-dynamic-fallback-impl`; subagent-driven (`feedback_execution_style`), each task committing LOCALLY only (`feedback_subagents_no_push`), controller squash-push at close (`feedback_push_to_origin`). **The 9-task TDD spine landed:** T1 (`f6c89787`) `internal/xds` NARROW sentinel — `ErrEmptyValidationContext` beside `errValidation` (`stream.go:43`), wrapped ONLY at the fully-absent-`trusted_ca` site in `parseValidationSecret`, dual-`%w` at `applyValidationResponse` (`stream.go:174`); the `applyResponse` twins + empty-resources wrap stay `errValidation`-only (RD-DUPSITE); `provider.go` BYTE-UNTOUCHED (the NACK/`update_rejected` posture stands — D-EDF-STREAMPOSTURE option (b)); Breaks A/B fired the `errors.Is` + `==false` scope-pin assertions; **the gate was NARROWED at the final review from `|| GetSpecifier() == nil` to `== nil` alone — the specifier-unset `trusted_ca:{}` (S1b) is PGV-NACKed by the reference (ADR-0287) and stays a boot-FAIL, ADR-0290/BC/STATE reconciled** · T2 (`32eb2a4b`) `internal/tls/config.go:184-223` fallback branch — `errors.Is(err, xds.ErrEmptyValidationContext)` sole gate → `default_validation_context.trusted_ca` via `loadTrustedCAPool`+`installPool`; empty-both routes to the phase-67 no-anchor logic (byte-identical `:204` reject / `NoClientCert`); S2/S3/timeout stay boot-FAIL; the P5 block byte-intact; Breaks C/D/E fired · T3 (`b5d53c3f`) `sdsserver.WithEmptyValidationContext` (S1, distinct from `WithValidationContext(name,nil)`=S2; Break F fired the `GetTrustedCa()==nil` discriminator) · T4 (`81b1691f`) fuzz `"downstream-sds-empty"` side + provider mode + seed, all three dispatch-verify arms confirmed (+0 fuzzers, 55→55) · T5 (`788bcc57`) fixture `0111-tls-cvc-empty-dynamic-fallback` (port 10447, CVC `default=CA_A` + served EMPTY dynamic VC, three-arm forced-send at require=true: CA_A accept+echo / CA_B reject / no-cert reject) · T6 (`22acb9f4`, empty — tree byte-identical to `788bcc57`) breaks — **Break G (symmetric union-pool CA_A∪CA_B) FIRED the untrusted-arm upper-bound in BOTH send-modes**; the **bare-polite swap Break G2 was the EXPECTED non-fire, recorded** (RD3: forced-send NOT observably load-bearing at require=true, retained for meaning + symmetry); the CA-swap structuralCheck triplet + per-side pin + served-this-arm all fired · T7 (`b0a9c6b4`) BEHAVIOR_CONTRACT B1–B4 (item 3(a) CLOSED — envoy-go now MATCHES; item 10 NARROWED — the default read on exactly ONE branch) · T8 six-gate ALL GREEN (controller-run at `b0a9c6b4`) · T9 ADR-0290 COMPLETED IN PLACE (§Decision + §Consequences, STATUS → COMPLETE) + this roll. **Exit counts:** fixtures **112 → 113** (tail `0111-tls-cvc-empty-dynamic-fallback`) / fuzzers **55 (+0)** / stat surface **1201 (+0)** / BackendKind tail **38 (+0)** / DECISIONS tail **ADR-0290** (completed IN PLACE; next-free **ADR-0291**) / go.mod modules **2 (+0)** / **ONE new exported `internal/xds` symbol** (`ErrEmptyValidationContext` — the streak break, ADR-0287). **Six-gate evidence:** the full **113-dir** differential `ok  github.com/pgdad/envoy-go/test/differential  379.894s` EXIT 0, 0 FAIL, zero flakes; `-race` on internal/tls + internal/xds + sdsserver all ok; cycle guard `go list -deps ./internal/xds` ⇒ `internal/stats` + `internal/xds` ONLY; envelope audit: functional production = `internal/tls/config.go` + `internal/xds/{secret,stream}.go` ONLY, `provider.go`/`internal/boot`/`internal/listener`/`validate` BYTE-UNTOUCHED. **Sentinel re-run MECHANICALLY (does NOT fire; `stop` NOT created):** (1) now prints NOTHING (row 68 is `done`); (2) prints THREE live "candidates:" sentences (`grep -cE 'remaining deferred \(not-yet-chartered\) candidates:'` ⇒ **3**); (3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`. Checks (2)+(3) print ⇒ abundant work remains. **Next → the phase-69 BRAINSTORM** (the roller SELF-PICKS per the 2026-07-12 standing directive).

- **prior active-phase:** `phase 68 (tls-cvc-empty-dynamic-fallback) PLAN done` (2026-07-19) — lifecycle-state 2 → 3; docs-only (`PLAN.md` + `PROGRESS.md`, the ONLY two files in the squash's phase-directory delta — ZERO production `.go`); fresh worktree off master `fba6d385` (== the phase-68 SPEC squash; NO parallel stream), branch `phase-68-tls-cvc-empty-dynamic-fallback-plan`, per `feedback_git_worktrees`; subagent-driven (`feedback_execution_style`): RE-DERIVED the SPEC against the tree at `fba6d385` (config.go, xds/{stream,secret,provider}.go, sdsserver, fuzz_test, config_test, the 0110/0109 drivers) → drafted a 9-task TDD spine (T1–T9) → THREE independent adversarial verifiers in PRIVATE scratch (`reference_parallel_subagents_private_scratch`) → an amendment. **ROW 68 STAYS `in-progress`** (flips at the IMPL six-gate). **The deliverable: a 9-task spine** — T1 `internal/xds` NARROW sentinel + dual-`%w` · T2 `internal/tls/config.go` fallback branch (block `:183-198`) + empty-both routing · T3 `sdsserver.WithEmptyValidationContext` · T4 fuzz `"downstream-sds-empty"` side + seed (+0) · T5/T6 fixture `0111` (port 10447) + breaks · T7 BC B1–B4 · T8 verify (113-dir) · T9 ADR-0290 completed IN PLACE + close. **THREE material RE-DERIVATION findings recorded:** **RD-DUAL/RD-PROV** (the dual-`%w` chain reaches `config.go` with `provider.go` BYTE-UNTOUCHED — traced + execution-confirmed), **RD-P5** (the CVC arm does NOT move in phase 68 — no hoist — so the mandatory P5 block stays byte-intact in place; the SPEC's `[MOVE INTACT]` re-derives to byte-preserve), **RD3** (at `require_client_certificate: true` forced-send is NOT observably load-bearing — a bare polite swap does NOT flip the observable; the union-vs-replace hazard is caught in BOTH send-modes because a permissive pool advertises the extra CA; the untrusted ARM upper-bounds the pool via a symmetric union-pool break). **Adversarial verification (PLAN.md §1.2):** V1 code-claims BY EXECUTION (cloned into scratch, applied T1/T2/T3, built + ran — the CORE classification + fallback + `provider.go`-untouched + breaks A–F all CONFIRMED SOUND), V2 spine-logic (ordering + control-flow + fuzz-trap SOUND), V3 process (docket-complete, counts/sentinel/collisions confirmed). **ONE SEVERE found and CORRECTED** (V1+V2 agreed, controller reproduced with fresh PKI): the draft's RD3 *two-factor* forced-send break was UNSOUND — a permissive `CA_A∪CA_B` pool advertises CA_B so the polite client sends `client_B` too and the break FIRES instead of going vacuous-green; replaced with the union-pool UPPER-BOUND break + the recorded bare-polite non-fire; the memory lesson corrected. **ONE MODERATE** (RD-DUPSITE — the dual-`%w`/empty-resources strings each match TWO `stream.go` sites; target `applyValidationResponse`) + MINORs (T2 block range `:183-198`; Break C not an exact one-line deletion; test-3 valid default) folded in. **The production design, the classifier, and the shipping fixture were unaffected — only the break justification was wrong.** **Counts UNCHANGED at this PLAN (docs-only; re-run MECHANICALLY):** fixtures **112** · fuzzers **55** · stat surface **1201** · BackendKind **38** · DECISIONS tail **ADR-0290** (next-free ADR-0291; completed IN PLACE at the IMPL, no flip) · go.mod modules **2**. **Sentinel re-run MECHANICALLY (does NOT fire; `stop` NOT created):** (1) prints `NOT DONE: row 68`; (2) `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:'` ⇒ **3**; (3) prints `NEVER OPENED: gRPC/Runtime/WASM`. **Next → the phase-68 IMPL** (execute T1–T9; RE-DERIVE the PLAN's anchors per task).

- **prior active-phase:** `phase 68 (tls-cvc-empty-dynamic-fallback) SPEC done` (2026-07-18) — lifecycle-state 1 → 2; docs-only (SPEC.md + the ADR-0290 §Context append; every probe harness lived in a PRIVATE scratch OUTSIDE the repo — nothing to delete in-tree; fresh worktree off master `f8416afd`, branch `phase-68-tls-cvc-empty-dynamic-fallback-spec`, per `feedback_git_worktrees`). **Row 68 STAYS `in-progress`** (flips at the IMPL six-gate). **ADR-0290 §Context DRAFTED per ADR-0044 — the DECISIONS tail flips ADR-0289 → ADR-0290 AT THIS SPEC COMMIT** (next-free ADR-0291; the IMPL completes ADR-0290 IN PLACE, no further flip). **Headline resolutions, EXECUTED-evidence-backed:** (1) **D-EDF-CLASSIFY RESOLVED → NARROW (mandatory)** by a discriminating SPEC-time probe (`envoyproxy/envoy@sha256:7edd5b0f…453be8` = `contrib-v1.37.2`, fresh container + fresh SotW SDS process per arm, forced-send CA_A/CA_B/no-cert clients) AND an INDEPENDENT from-scratch reproduction (own PKI/SDS/config): the reference ACKs + falls back to `default_validation_context.trusted_ca` + SERVES ONLY on the specifier-unset shape (S1 — CA_A accepted, CA_B alert-48 rejected, no-cert rejected); a set-but-empty `trusted_ca:{inline_bytes:""}` (S2, "DataSource cannot be empty") and a corrupt PEM (S3, "Failed to load trusted CA certificates") are **NACKed WITHOUT fallback** → fail-closed per-connection. **The boot discriminator is ACK vs NACK (`update_success` vs `update_rejected`)** — NOT `init_fetch_timeout` (all arms responded promptly; S2/S3 bind then fail-closed via `downstream_context_secrets_not_ready`, they do NOT init-hold) — a sharpening of the BRAINSTORM. So envoy-go falls back ONLY on S1 via a new exported sentinel `xds.ErrEmptyValidationContext` (sited in `stream.go`, wrapped in `parseValidationSecret` at the specifier-unset site, preserved through `applyValidationResponse` by dual-`%w`). (2) **FIVE BRAINSTORM corrections:** C1 CLASSIFY resolved + discriminator sharpened · C2 the envelope edits TWO production files (`internal/tls/config.go` + `internal/xds/{secret,stream}.go`), not one · **C3 `test/helpers/sdsserver` IS TOUCHED** — a new `WithEmptyValidationContext` Option is required (`WithValidationContext(name,nil)` yields S2 not S1, empirically confirmed via a go-control-plane oneof round-trip probe) · **C4 D-EDF-PGV is a NON-ISSUE** — E1 (`config.go:427-429`) already boot-rejects a CVC missing `default_validation_context`, matching the reference's PGV boot-reject · C5 P4 is the load-bearing gating premise. (3) **D-EDF-STREAMPOSTURE → option (b)** — accept + NAME the residual S1-only NACK/`update_rejected` divergence (the differentially-asserted traffic surface already matches; on S2/S3 both sides NACK). (4) **D-EDF-FETCHSHAPE → (a)** classified-error-then-fallback; **D-EDF-EMPTYBOTH** → the phase-67 no-anchor routing (require=true reject / require=false `NoClientCert`), no new reject; **D-EDF-FIXTURE** `0111` (port 10447, three-arm forced-send, `default=CA_A` + served EMPTY dynamic VC); **D-EDF-FUZZSEED** +0 fuzzers (new dispatch side/provider mode, dispatch-verified). **Adversarially verified before landing** by THREE independent verifiers (V1 code-claims re-derivation — every `file:line`/trace re-run + the proto-oneof edge settled by execution · V2 process/consistency — docket/ADR/counts/sentinel all clean · V3 probe reproduction FROM SCRATCH — S1 ACK+fallback vs S2/S3 NACK+fail-closed CONFIRMED, no refutation): **ZERO SEVERE, ZERO MODERATE**, three MINOR precision notes (A1 sentinel sited in `stream.go` so no new import · A2 the wrap is `stream.go:164` · A3 scope the sentinel off the `empty resources` branch) folded into §1.1/§1.2/§3.2/§3.8; design direction unchanged. **Counts UNCHANGED at this SPEC (docs-only apart from the DECISIONS append; re-run MECHANICALLY):** fixtures **112** (tail `0110-tls-require-client-cert-false`) · fuzzers **55** · stat surface **1201** · BackendKind **38** · go.mod modules **2** — the ONLY moved count is the DECISIONS tail (**ADR-0289 → ADR-0290**, the §Context draft; next-free **ADR-0291**). **Sentinel re-run MECHANICALLY at this close (does NOT fire; `stop` NOT created):** (1) prints `NOT DONE: row 68`; (2) prints THREE live "candidates:" sentences (`grep -cE 'remaining deferred \(not-yet-chartered\) candidates:'` ⇒ **3**); (3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`. **Next → the phase-68 PLAN** (RE-DERIVE the SPEC; the owed roster is in §Current pointer's next-skill line).

- **prior active-phase:** `phase 68 (tls-cvc-empty-dynamic-fallback) BRAINSTORM done` (2026-07-18) — the roller's AUTONOMOUS self-pick (no human pick per the 2026-07-12 standing directive) opens the SIXTH xDS-family row (lifecycle-state 0 → 1; docs-only, ZERO production `.go`; the three input dossiers lived in PRIVATE scratch OUTSIDE the repo — nothing to delete in-tree; fresh worktree off master `92cd1647`, branch `phase-68-empty-dynamic-fallback-brainstorm`, per `feedback_git_worktrees`). **Subject:** `tls-cvc-empty-dynamic-fallback` — complete the `combined_validation_context` MERGE semantics on an ACKed-but-EMPTY dynamic half: the CVC arm of `NewDownstreamConfig`, on an ACK-succeeded-but-empty dynamic fetch, falls back to `default_validation_context.trusted_ca` (via `loadTrustedCAPool` + the phase-67 `installPool`) and SERVES, where envoy-go today boot-FAILS. **Why this pick (the FULL rejected roster ADJUDICATED in BRAINSTORM §2.1 — not the phase-67 one-liners):** the direct pickup of "Design B", the phase-66 runner-up filed as an ADR-0280-family departure (SPEC-66:267,509) and phase-67 re-deferred (SPEC-67:356); it is the SMALLEST candidate that is ALSO crisply differentially-testable (a three-arm cross-side verdict) AND already reference-confirmed — the smaller missing-`default:`-arm loses on differential value (a low-value reject-add on a `[#not-implemented-hide:]` field), OTLP-metrics loses on size (a NEW multi-leg sink — the strongest FUTURE pick, narrows ROADMAP:198). **On the honored surface (only `trusted_ca` — P1) the reference `MergeFrom` reduces EXACTLY to "dynamic `trusted_ca` if present, else default's"** — the fallback IS the honored-surface projection of the merge; NO `proto.Merge` (ADR-0287's "message-returning seam" pre-characterization, DECISIONS.md:16975, refuted by re-derivation — the phase-66 unbuildable-`proto.Merge` lesson applied). **EXECUTED evidence (Dossier A, `envoyproxy/envoy:v1.37.2`, fresh container per arm, a throwaway SotW SDS sibling-container with a served-this-arm assert, forced-send client certs):** empty-served → **falls back to CA_A + SERVES** (arm 1: `update_success=1`, LIVE, CA_A accept / CA_B reject alert 48 / no-cert reject at require=true) · timeout/unreachable → **fail-closed, NO fallback** (arm 3: `init_fetch_timeout=1` — the discriminating signal is ACK/`update_success` vs `init_fetch_timeout`; envoy-go needs a classification signal) · empty-BOTH / plain-SDS-VC-empty → **serve UNAUTHENTICATED, require=true SILENTLY unenforced** (arms 4a/5) · missing `default_validation_context` → **PGV boot-reject** (arm 4b — PGV-only; envoy-go runs none ⇒ a deliberate-divergence decision, `reference_pgv_forecloses_go_hazard`) · require × fallback → verify-if-presented against the FALLBACK anchor, CA_B still REJECTED under forced-send (a REAL verify, not a vacuous accept). **Constructibility (Dossier B):** ONE production function (`internal/tls/config.go` CVC error branch) + **ONE new `internal/xds` exported symbol** (a classifier — `errValidation` is unexported today, stream.go:33) — the FIRST break of the recent zero-new-symbol streak (DECISIONS.md:16973), stated LOUDLY; the cycle guard STANDS (the export adds no edge; TYPE-level); the P1–P5 theorem EXTENDS (P3 restated — the default DOES contribute on the empty branch; P4 NOT re-introduced — the four sub-field rejects already re-pointed at `default_validation_context`, config.go:442-461, gate the fallback); CVC-ONLY (plain SDS-VC empty stays boot-FAIL, a RETAINED departure). **BRAINSTORM-time open questions to the SPEC:** D-EDF-CLASSIFY (coarse `errValidation` vs a narrow `errEmptyValidationContext` — the ONE growth risk; RUN a served-CORRUPT-`trusted_ca` discriminator with THREE arms incl. `trusted_ca:{inline_bytes:""}`; lean NARROW) · D-EDF-STREAMPOSTURE (envoy-go NACKs the empty resource where the reference cleanly ACKs — a RESIDUAL divergence after the fallback; reclassify-as-ACK vs name-the-departure, lean the latter) · D-EDF-FETCHSHAPE · D-EDF-PGV · D-EDF-EMPTYBOTH-REQUIRE · D-EDF-FIXTURE `0111` · D-EDF-FUZZSEED. **Adversarially verified before landing** (an independent code-claims + probe + adjudication verifier). Anticipated: fixtures **112 → 113** (`0111-tls-cvc-empty-dynamic-fallback`) / stat surface **1201 (+0)** / fuzzers **55 (+0 anticipated)** / BackendKind **38 (+0)** / DECISIONS tail **ADR-0289 → ADR-0290** (at the SPEC) / ZERO new packages/modules / **ONE new `internal/xds` exported symbol**. **Envelope:** ~8–9 tasks, a SINGLE FLAT ROW anticipated (ADR-0045 valve armable, no split). **Row 68 registered `in-progress`** at this stage-close commit per the ROADMAP §Schema invariant; flips `done` at the phase-68 IMPL six-gate (the SOLE leg, ADR-0106). **NO deferred sentence is narrowed at ANY stage** (empty-dynamic/Design B is in NO live `candidates:` sentence — a §13-tier pickup; the phase-64/66/67 precedent). **Counts UNCHANGED at this BRAINSTORM (docs-only; count commands re-run MECHANICALLY in the worktree at this close):** stat surface **1201** · fixtures **112** (numeric tail `0110-tls-require-client-cert-false`) · fuzzers **55** (`internal/` scope) · BackendKind **38** · DECISIONS tail **ADR-0289** (next-free **ADR-0290**) · go.mod modules **2**. **Sentinel re-run MECHANICALLY at this close (does NOT fire; `stop` NOT created):** (1) now prints `NOT DONE: row 68` (the registration re-opens check (1)); (2) prints THREE live "candidates:" sentences (`grep -cE 'remaining deferred \(not-yet-chartered\) candidates:'` ⇒ **3**); (3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`. **Next → the phase-68 SPEC** (RE-DERIVE the BRAINSTORM rather than execute it; the owed roster is in §Current pointer's next-skill line).

- **prior active-phase:** `phase 67 (tls-require-client-cert-false) IMPL done` (2026-07-18) — lifecycle-state 3 → DONE; **ROW 67 FLIPS `in-progress` → `done`** at this IMPL six-gate (ADR-0106, the SOLE leg — `reference_roadmap_split_phase_row_done`). Worktree `.worktrees/phase-67-impl`, branch `phase-67-tls-require-client-cert-false-impl`; subagent-driven (`feedback_execution_style`), each task committing LOCALLY only (`feedback_subagents_no_push`), controller squash-push at close (`feedback_push_to_origin`). **The 9-task TDD spine landed:** T1 ATOMIC hoist — `NewDownstreamConfig` maps `ClientAuth` three-way across FOUR cells (true+anchor→`RequireAndVerifyClientCert` · false/absent+anchor→`VerifyClientCertIfGiven` · false/absent+no-anchor→`NoClientCert` · true+no-anchor→the RETAINED `requires validation_context.trusted_ca` reject) via a shared `installPool`/`clientAuthFor` closure with the nil-pool guard (assignment-adjacency); E3 RETIRED atomically with its flip roster; the P1–P5 theorem block moved INTACT; retained rejects byte-diffed · T2 the mapping cross-product units + the interface-pinned `TestVerifyIfGiven_NilPool_Unconstructible` (Break D its sole liveness proof) + the require=false `fakeProvider{vcErr}` fetch-failure arm · T3 four fuzz seeds on the CORRECT dispatch sides (+0 fuzzers; the `"downstream"`-side SDS-seed vacuity trap avoided, counter-checked) · T4/T5 fixture `0110-tls-require-client-cert-false` (CVC-primary at require=false, three-arm verdict trusted→ok+echo / untrusted→rejected / no-cert→ok+echo the discriminator, FORCED-SEND `GetClientCertificate` driver — Break B caught the polite-withhold vacuous green, 0109's require=true polite green the control; the structuralCheck triplet + per-side pin + served-this-arm breaks all fired) · T6 the comment sweeps (B11/B16/B17/B18/B19 verbatim + the SDS-VC leading-comment M1 + nil-pool test-goroutine M2 carries + the 0109 enumerated set; non-drift counterfactuals preserved) · T7 BC B1–B10/B13–B14 verbatim + the FOUR-site TEST_GAP sweep (B15) [+ a controller B6-consistency fixup] · T8 six-gate ALL GREEN (gofmt silent · vet 0 · build 0 · `go mod tidy -diff` EMPTY · `git diff master -- go.mod go.sum` NO DIFF · `golangci-lint run ./...` 0) + full **112-dir** differential (`ok  github.com/pgdad/envoy-go/test/differential  375.345s`, EXIT 0, 0 FAIL) + `-race` ok + the `internal/xds` cycle guard (`internal/stats` + `internal/xds` ONLY) + envelope audit (production edits = `internal/tls/config.go` + the ONE chartered `internal/xds/provider.go` comment; `boot.go`/`listener`/`validate` BYTE-UNTOUCHED) · T9 ADR-0289 COMPLETED IN PLACE (§Decision + §Consequences) + the B12 bracketed annotation on the ADR-0286 D-SDSVC-FETCHTIMEOUT bullet. **Task-scoped adversarial reviews** (T1/T2/T4 opus reviewers — SPEC ✅, the 0110 non-vacuity space walked and every arm proven load-bearing). **Exit counts:** fixtures **111 → 112** (`0110`) / stat surface **1201 (+0)** / fuzzers **55 (+0)** / BackendKind tail **38 (+0)** / DECISIONS tail **ADR-0289** (completed in place; next-free **ADR-0290**) / go.mod modules **2 (+0)**. **Sentinel re-run MECHANICALLY (does NOT fire; `stop` NOT created):** (1) prints **NOTHING** (row 67 is `done`); (2) prints **THREE** live "candidates:" sentences (HTTP/3, xDS, Observability); (3) prints **THREE** never-opened families (gRPC/Runtime/WASM). Checks (2)+(3) print ⇒ abundant work remains. **Next → the phase-68 BRAINSTORM** (the roller SELF-PICKS `xds-sds-upstream-server-cert` per the 2026-07-12 standing directive).

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
