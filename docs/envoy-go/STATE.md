# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

> **This file is a POINTER, not an archive** — `BOOTSTRAP_PROMPT.md` §4: *"STATE.md — pointer: active phase, last commit, next action."* It carries the live pointer, the live counts, and a SHORT recent lineage. **Full stage-by-stage history lives in `STATE_HISTORY.md`** (archived at ADR-0288; **nothing was deleted** — the pre-move file is preserved there verbatim, byte-for-byte).
>
> **⚠️ THE RULE ADR-0288 EXISTS TO ENFORCE: there is EXACTLY ONE live `next-skill:`, `lifecycle-state:`, `next-free ADR:`, and count block IN THIS FILE, and they live in §Current pointer directly below.** Before that move, ~330 lines of prepended backlog had buried TWO fossilised "current state" blocks whose `next-skill:` still read *"the 42.2 hedging BRAINSTORM"* and *"the 46.2 SPEC"* while the live phase was **66** — so `grep 'next-skill:' STATE.md` returned **two stale hits and ZERO live ones**, and `grep 'fuzzers:'` returned **48** and **42** when the true count was **55**. **A session may trust a grep of this file again. Keep it that way: when you advance the pointer, EDIT §Current pointer IN PLACE — never prepend a new block above it.**

---

## Current pointer

*(The ONLY live "what next" in this repo. Update IN PLACE at every stage close.)*

- **active-phase:** `phase 90 (h2-host-authority-normalization) IMPL done` (2026-08-22) — lifecycle-state **3 -> DONE**. **PHASE 90 CLOSED, ROW 90 FLIPPED `done`** at `ROADMAP.md:152`; `ROADMAP.md` stays **240 lines / 122 data rows** and sentinel `want` stays **122** (a row FLIP, not a row ADD), and the row's separately-false *"ROUTE-MATCHING input"* sentence is CORRECTED while its misleading `regular.Add` wording is left in place, NAMED rather than silently rewritten. ADR-0312 **COMPLETED IN PLACE** — §Decision (D-90-SCOPE / D-90-DUP / D-90-SKIP / D-90-TARGET / D-90-YAML) + §Consequences (i)-(xix), no renumber, NO new `---`; strict `^> **STATUS: PROPOSED` guard **1 -> 0**, `^---$` stays **216**, `^## ADR-` stays **311** (bare `^## ` **319** — 8 `## Amendment` headings; state the scope whenever the number is restated), tail stays **ADR-0312**, next-free stays **`ADR-0313`**; `DECISIONS.md` **18297 -> 18367**. `BEHAVIOR_CONTRACT.md` stays **5962** — the `:2034` RIDER landed **IN PLACE with ZERO line delta**. Base master `1f147262`, branch `phase-90-impl`, ONE squashed commit. **Cost re-measured at THIS publishing commit:** production **+48 / −0** in ONE file and ONE package · unit **+122 / −0** · differential driver **+354 / −4** · fixture backend **+7 / −0**; ⚠️ **`reference_measured_prototype_is_a_lower_bound` FIRES A FOURTH CONSECUTIVE TIME** (BRAINSTORM floor `+15/−1`, SPEC prototype `+34/−0`, PLAN prototype `+36/−0`), and the cause is again UNDER-ENUMERATION. Census **RED `rc=1` / 204 `=== RUN` / 199 `--- PASS` / 5 subtest FAIL -> GREEN `rc=0` / 204 RUN / 204 PASS / anchored FAIL 0** (199 + 5 = 204 = 204 reconciles the FULL denominator). Gates: `go test ./...` **rc=0**, set reconciliation **exact at 236 packages** (127 ok + 109 no-test-files), zero difference both directions; full differential **rc=0, 121 `=== RUN` / 121 `--- PASS` / 0 SKIP**, anchored panic gate **0**, and **`--- PASS: TestDifferential/0004-h2-routing` asserted EXPLICITLY** because `runner_test.go:200` `t.Skipf`s an unregistered fixture and no fixture-count gate exists anywhere in the tree. ⚠️ **THE HEADLINE: THE ROW DISCOVERED A NEW REFERENCE DIVERGENCE THAT KILLED ONE OF ITS OWN DIFFERENTIAL ARMS.** The pinned reference **REFUSES a DUPLICATED regular `host`** on the H/2 downstream leg — `http2.invalid.header.field`, bare connection close, **NO GOAWAY and NO RST_STREAM**, **by ARITY, NOT by VALUE**, and it holds with `:authority` also present — so first-occurrence-wins is **NOT DIFFERENTIABLE IN PRINCIPLE**: arm **P90-E was REMOVED** (it was RED at the UNBROKEN tip, divergence on the REFERENCE side at transcript offset 231), the differential roster is **THREE** arms (P90-P / P90-A / P90-B), the `0004` workload is **42** requests/side, and first-wins stays pinned at the **UNIT** layer by `TestAuthorityNormalization/E_dup_host_first_wins` — shown by counterfactual to be the **SOLE** first-wins discriminator in the tree. ⇒ **this is the MIRROR of H1-D: on H/1 the SUBJECT is the stricter side, on H/2 the REFERENCE is.** ⚠️ **SIX FURTHER PLAN/ADR CLAIMS REFUTED BY EXECUTION:** (1) **THE ADR'S OWN APPEND POINT DID NOT EXIST** — its STATUS line AND PLAN §8.3 both named a *"RETAINED italic footer"* that ADR-0312 did not carry (four sibling ADRs do; the SPEC omitted it); **the IMPL added it and then appended after it** · (2) PLAN §8.3 said **five** deferred follow-ons, §11/§13 say **six**, **the IMPL registered SEVEN** (the new one discovered by this row) · (3) PLAN §10.2's flake-causation exclusion was **UNEARNED for `internal/boot`** — `go list -deps -test ./internal/boot/ | grep -c 'filter/hcm/h2'` ⇒ **1**, it DOES link the patched package; only `./internal/xds/` reads **0** — replaced by a direct `panic()`-injection exclusion with a live positive control · (4) PLAN §10.3's stated MECHANISM is **FALSE**: it said *"this row MODIFIES writer #1"*, but writer #1 is **byte-for-byte unmodified** — the new `case "host":` diverts before `default:` ever runs — so the slice-only-writer gate still reads **6 for a different reason than predicted**, and a reviewer diffing that line would find nothing · (5) PLAN §7.5's **B2 prediction is REFUTED** — P90-P sends no `host`, so dropping `!authoritySeen` cannot change it under ANY input; B2 reddens **P90-A on the authority-VALUE axis**, and ⚠️ **B2 AS INSTRUCTED DOES NOT COMPILE** (`declared and not used: authoritySeen`), a failure that reddens BOTH runs with `RUN=0` and **reads exactly like a successful break** · (6) **`hostSeen`'s necessity is NARROWER than PLAN §9.1 states** — required for first-occurrence-wins (`hostField != ""` cannot express a latched-but-empty first value), **NOT** for the promote condition, where `!authoritySeen` alone suffices. ⚠️ **B1's differential evidence is HALF-OBSERVABLE** — the comparator reports only the FIRST divergence, so **P90-B's divergence under B1 is MASKED** and the unit roster carries that half; and **B3/B4 are BOTH DIFFERENTIALLY SILENT**, reddening the unit roster only. ⚠️ **TWO PLAUSIBLE `PROPOSED` GUARD FORMS BOTH READ 1 AT DIFFERENT ADRs** (`^> **STATUS: PROPOSED` at ADR-0312 `:18281` and a historical `^**Status:** PROPOSED` at ADR-0231 `:14866`) — **the 1 -> 0 disarm must be verified by LINE AND ADR, never by the count alone.** ⚠️ **A STANDING FINDING, NOT A FLAKE: `TestServerConn_TinyWindowDelivery` RECURRED — the SECOND post-fix recurrence** (phase-80 recorded it FIXED and stated *"a recurrence is now a FINDING, not a re-run"*; the first recurrence is at `STATE_HISTORY.md:486`). It cleared 5/5 isolated and 3/3 full-package and is causally excluded from row 90, but it is **flagged as a finding, not absorbed**.
- **phase-directory:** `docs/envoy-go/phases/90-h2-host-authority-normalization/` — `BRAINSTORM.md` + `SPEC.md` + `PLAN.md` + `PROGRESS.md` (294 -> 525, the IMPL entry appended **per task, T1-T9**). Phase dirs stay **131** — an IMPL adds no directory. No `REVIEW.md` (standing departure, `git ls-files | grep -c 'REVIEW\.md$'` ⇒ **37**, a FILE count; newest at `phases/25.3-…`, none since).
- **lifecycle-state:** **DONE**. ⚠️ **Sentinel re-run AFTER the row-90 flip. ACTUAL output recorded, not forecast:** (1) at `want=122` => **SILENT** — ⚠️ **the HEALTHY reading now that row 90 is `done`; it read `NOT DONE: row 90` while the row was open, and that flip IS the check** · (2) **SIX** deferred-candidate windows at `:200 :206 :212 :222 :228 :236` · (3) **SILENT**. ⇒ **ONE check blocks the sentinel; `stop` was NOT created** (verified absent at the git root AND in the stage worktree). **NCs FIRED:** NC-A returned to the **ONE-line** `NOT DONE: row 62` form — row 90's line is GONE, exactly as the flip predicts, and the two-line form observed at the PLAN is what a still-open row looks like · NC-B `want=121` => `GATE FAIL: examined 122 data rows, expected 121`.
- **next-skill:** **a NEW phase BRAINSTORM, SELF-PICKED** (`superpowers:brainstorming`, per BOOTSTRAP §5 state DONE -> 1). ⚠️ **The loop runs AUTONOMOUSLY per the 2026-07-12 standing directive: do NOT stop to ask which phase.** The roller **self-picks the smallest defensible candidate and RECORDS THE REJECTED ALTERNATIVES** in the BRAINSTORM, then registers the new row `in-progress` in `ROADMAP.md` with `want` bumped **122 -> 123 in the SAME commit** (a pure insertion; verify `git diff --numstat` reads `1 0`). ⚠️ **PROVE, do not assume, that no banked mid-lifecycle work exists** — re-run sentinel check (1) at `want=122` and require it SILENT before opening anything. **Seven deferred follow-ons are registered as named rows in ADR-0312 §Consequences (xviii)** and are the first candidate pool, in this row's own priority order: arm C (the authority **VALIDITY** reject — ⚠️ it opens with a **SHAPE** decision, stream-scope vs connection-scope, not a cost decision, and it also owns **D-90-DUP**) · the **duplicate regular-`host` reject** (NEW, discovered by this row) · **H1-B′** (HTTP/1.1 with no `Host` — the cheapest, zero new machinery) · **H1-D** (a NAMED DEPARTURE — parity would make envoy-go STOP rejecting something it correctly rejects) · the **D-89-HOST residue** (`h2ReconcileSkipKey`; ⚠️ ground 2 is RETIRED by this row, so option (b) is newly defensible) · the **H3 arm-A prediction** (⚠️ **READ, NEVER MEASURED — ZERO probes at every stage; it opens with a PROBE, not a fix**) · the **access-log / Zipkin-span axis** (⚠️ opens with **which fixture pays for the surface** — not one of the four H2-capable fixtures carries an access-log or tracing surface).
- **last-commit:** the **phase-90 IMPL** commit. ⚠️ **LOCATE BY SUBJECT, NEVER BY POSITION** — `git log --grep '^phase 90 (h2-host-authority-normalization) IMPL' --format='%h %s' | head -1`. The slug-anchored form is required because a bare grep is contaminated by `next-prompt.txt`'s own prose.
- **last-updated:** 2026-08-22 (phase-90 **IMPL done**; **PHASE 90 CLOSED**, row 90 flipped `done`; `want` stays **122**; strict `PROPOSED` guard **1 -> 0**; next-free **`ADR-0313`**).
- **next-free ADR:** **`ADR-0313`** — **UNMOVED: the IMPL COMPLETED ADR-0312 IN PLACE and added no ADR.** TAIL-derived (`grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1` -> `## ADR-0312`; `grep -c '^## ADR-0313'` => **0**). ⚠️ **NEVER derive from the heading count**, and ⚠️ **THE HEADING FIGURE IS SCOPED: `^## ADR-` reads 311 while bare `^## ` reads 319** — the extra **8** are `## Amendment (per phase …)` headings. Both are right; they measure different things, and headings+1 COLLIDES at the ADR-0209 gap regardless (ADR-0209 ABSENT, verified 0). ⚠️ **A NEW ADR TAKES NO `---` SEPARATOR** (`^---$` **216**, unmoved; `DECISIONS.md` **18367**). ⚠️ **CARRY NO WHOLE-FILE COUNT OF THE LOOSE `PROPOSED` MATCHER** — only the strict form is the guard, and it now reads **0**; ⚠️ **and the strict form is NOT self-locating: a historical `^**Status:** PROPOSED` at ADR-0231 `:14866` ALSO reads 1, so a disarm must be verified by LINE AND ADR, never by count.**

---

## Project counts at this commit

*(Live. Re-verify at session start — `git fetch` first — and update here at every stage close. Canonical commands inline.)*

⚠️ **ALL counts below were re-run MECHANICALLY in the stage worktree at this phase-76 IMPL close with the canonical commands, EACH WITH ITS NEGATIVE CONTROL OBSERVED — never copied forward.** **Phase 76 landed +0 on EVERY axis, including +0 PRODUCTION `.go` FILES** — the first chartered row in this lineage whose production-code delta is exactly nothing, and the ANTICIPATED envelope held on all of them. The row's `.go` delta is **three TEST-side files** (`internal/cluster/ringhash_test.go` edited, `test/fixtures/0061-lb-ring-hash/driver/driver_test.go` edited, `.../linkage_test.go` NEW), plus three fixture-doc files and four project docs. ⚠️ **The stat-surface total 1205 is DOC-SOURCED and was NOT re-derived mechanically** — there is no mechanical command, and the chain is known to be discontinuous in **TWO** places (the recorded `1200 -> 1201`, and `Phase 46.1b` closing at 1198 while `Phase 47.1` opens at 1200 — an unattributed +2). **Only the +0 DELTA is asserted, and that argument is STRUCTURAL rather than a `statssink` result:** zero production `.go` bytes changed (G8) and zero byte mismatches over the 375-file production roster (G14), so no `NewCounter`/`NewGauge` call site can have moved; the five `TestNoNewStat*` guards corroborate at 5x PASS. ⚠️ **A future phase should COUNT the surface rather than trust the chain — a +0 row is a cheap place to do it, and phase 76 declined to charter that, so it is now a NAMED maintenance deferral rather than an idle observation.**

- **active differential fixtures:** **119** — ⚠️ **Phase 76 landed +0: `0061-lb-ring-hash` was EDITED, not added** (`sourceIPs` 4 -> 16 plus a NEW `driver/linkage_test.go` inside the EXISTING fixture directory — no new YAML, no new directory, no new BackendKind, no new port), so the count STAYS **119** and `0118` remains FREE. NC observed: `mkdir .../0118-fake` ⇒ 120. — `ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0117-tracing-custom-tags-metadata-cluster` (phase 73). **Phase 75 landed +0**: `0110-tls-require-client-cert-false` was **EXTENDED** with a cross-side `fixture.StatsAsserter` (NO new YAML, NO new directory, NO new BackendKind, NO new port), so the count STAYS **119** and `0118` remains FREE. ⚠️ **Next-free REFERENCE port is `10450`, NOT `10118`** — ports are NOT fixture-index aligned. **The fixture now asserts what its own README confessed was infeasible, and BOTH sides were EXECUTED**: `ssl.handshake=2 ssl.no_certificate=1 ssl.fail_verify_error=1 ssl.fail_verify_no_cert=0`, `downstream_cx_total=3`, **identical on reference and subject**, with the discriminating asymmetry (`handshake=2` vs `no_certificate=1`) observed per side. Shape A over `/stats/prometheus`, keyed on the metric NAME with `envoy_listener_address` IGNORED — the NAMES are byte-identical cross-side while the labels differ (`0.0.0.0_10446` reference vs **`___20016`** subject: ⚠️ **the subject binds the IPv6 WILDCARD**). ⚠️ **`0111` was NOT given a value assertion, deliberately** — it runs `require_client_certificate: true` on both sides, so a success-path no-cert annotation reads **0 on every arm, structurally**, and a value pin would document a **vacuous `0 == 0`** as coverage; its PROSE gained the name in its NAMED-UNASSERTED list instead, because **both `0111` passages are CLOSED ENUMERATIONS** and a name in neither list reads as ASSERTED. **`0111`'s driver is byte-untouched, and its named-subset check was EXECUTED for the first time at this IMPL: PASS.** ⚠️ `var _ fixture.StatsAsserter` is a **TRIPWIRE, not the dispatch mechanism** — `0110` is only the **THIRD** fixture repo-wide to carry it, while **82** define an `AssertStats` method.
- **fuzzers:** **55** — **Phase 76 landed +0** (it consumes no new config field, so there is no parse arm to fuzz). NC observed: appending `func FuzzProbeOnly` ⇒ 56. — `grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`. **Phase 75 landed +0** — the row consumes no new config field at all, so there is no parse arm and no fuzz-seed classification to flip (`reference_fuzzer_count_docs_drift`: an `f.Add` seed is not a fuzzer).
- **stat surface:** **1205** — BEHAVIOR_CONTRACT doc count. **There is NO mechanical counting command** (the contract says so itself); enforcement is per-phase `TestNoNewStat*` guards asserting a delta of ZERO, never the absolute total. **Phase 75 LANDED +1 (1204 -> 1205)** — `listener.<normalized-addr>.ssl.no_certificate`, the FOURTH listener-scope `ssl.*` name and the FIRST **SUCCESS-PATH** handshake annotation (⚠️ **NOT a synonym for `ssl.fail_verify_no_cert`, and the mis-mapping is wrong in BOTH directions** — one annotates an ACCEPTED handshake, the other books a REJECTED one; they are mutually exclusive on every connection). ⚠️ **The delta is a NAME-surface delta, NOT a flat per-deployment one:** registration rides the block that ALREADY gates on `rt.tlsMode`, so a **plaintext-only** deployment gains **ZERO** names, while a **QUIC** listener gains it permanently zero — **PARITY** with the reference, and structural (`Manager.Start` launches no accept loop for `kindQUIC`, so the Inc site is unreachable for a **name-independent** reason). ⚠️ **QUIC was NOT re-probed at phase 75** — the parity claim is INHERITED from phase 74's family-level probe, and that inheritance is stated in BEHAVIOR_CONTRACT rather than presented as a measurement. Ledger updated: `1204` -> `1205` at `BEHAVIOR_CONTRACT.md:831`/`:847`, plus a `**Phase 75 - 1204 -> 1205 (+1)**` line after the tail entry — **the ONLY line-adding edit in the file** (`5744 -> 5746`), so every by-line citation into the TLS subsection stays valid. ⚠️ **The new entry RECORDS BOTH ledger gaps and BACK-FILLS NEITHER** (fabricating a line would be inventing a record). Gate figures at this close: `grep -c '1204'` => **2** · `grep -c '1205'` => **3** · `grep -cE '^#{2,4} .*ADR-[0-9]+.*ADR-[0-9]+'` => **17** (was 16 — the `:1849` heading joined them, on the `:785` precedent). ⚠️ **`internal/statssink`'s four *"stays 1200 / 1196"* prose sites, stale since phase 49, are RECORDED-NOT-FIXED** — that package is on this phase's byte-untouched sha256 roster.
- **BackendKind tail:** **38** — **Phase 76 landed +0** (`0061` uses the in-process `TCPEcho` branch and implements neither `BackendKindAware` nor `PerHostBackendKind`). NC observed: appending `ZZProbeKind = 39` ⇒ tail 39. — (`H2GoawayResponder`, `test/differential/fixture/fixture.go:614`). **Phase 75 landed +0** — `0110` was extended, not cloned, and it implements neither `BackendKindAware` nor `PerHostBackendKind`, so the runner takes the in-process `TCPEcho` branch. ⚠️ **This is a TAIL VALUE, not a count** — the file declares **39** constants (values 0-38, `TCPEcho = 0`). The lineage figure **38** is correct as written; do NOT 'fix' it to 39.
- **DECISIONS.md tail:** **ADR-0298** (next-free **ADR-0299**) — `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1` -> `## ADR-0298`; `grep -c '^## ADR-0299'` ⇒ **0**. **ADR-0298 is COMPLETE as of the phase-76 IMPL** — §Context drafted at the phase-76 SPEC, §Decision + §Consequences APPENDED IN PLACE after the RETAINED italic footer, **no renumber and NO `---` separator** (the file's last `^---$` is still at `:17020`). Block shape controller-re-derived at this close: **1** `### Context` / **1** `### Decision` / **1** `### Consequences` / **1** retained footer. ⚠️ **FOUR of its OWN §Context paragraphs were CORRECTED IN PLACE** (¶1's `:295` -> `:307`, ¶5's `-count ~= 81` -> **83**, ¶7's *"~71 collapses"* -> **74/2000**, ¶11's *"needs no edit"* -> REFUTED) on the ADR-0297 ¶7/¶9 precedent, each **leading with what survives**; none changes a decision the ADR records. ⚠️ **It carries NO whole-file grep count and claims NO family ordinal** — the Load-balancing family closed at row 54, and **maintenance rows do not extend a charter**. *(Historical, retained: **ADR-0297 is COMPLETE** — §Context drafted at the phase-75 SPEC (ADR-0044-**as-used**; ⚠️ ADR-0044 does NOT actually contain that discipline), §Decision + §Consequences appended IN PLACE at the phase-75 IMPL after the **RETAINED** footer, mirroring ADR-0295/0296. Block shape verified at this close: **1** `### Context` / **1** `### Decision` / **1** `### Consequences` / **1** retained footer; `grep -c '^## ADR-0298'` => **0**. **No renumber, no new ADR.** ⚠️ **Its OWN §Context ¶7 and ¶9 were CORRECTED IN PLACE while completing it** — ¶7's **self-falsifying whole-file grep** (the same species the phase-74 IMPL had just fixed in ADR-0296 ¶3, reproduced one ADR later) is restated with **NO number** and the searched token deliberately **NOT spelled**, because every restatement mints another counter-example; and ¶9's *"SELF vs OTHER-ADR"* form rule is replaced by **graft SCALE** after the n=7 population was re-enumerated first-hand. ⚠️ **¶9 ALSO said *"the only two instances in the file"* — which this row's own blockquote makes three** — so a pure append would have shipped a SECOND self-falsifying count in the commit that fixed the first. **The prescription has now outlived THREE wrong justifications: family, self-vs-other, scale.** ⚠️ **`ADR-0296` is NO LONGER byte-untouched** — its §Decision (g) carries the phase-75 correction as an INDENTED blockquote (`grep -c '^  > '` **2 -> 3**), **LEADING WITH WHAT SURVIVES** (no such counter was owed *in phase 74*, whose sole stat-asserting fixture is `require=true` on both sides — **the deferral was RIGHT**) and claiming **NO ORDINAL**.)*
- **go.mod modules:** **Phase 76 landed +0** — `go mod tidy -diff` EMPTY and `git diff master -- go.mod go.sum` EMPTY; ⚠️ **the row's TEST-side import delta is +8 and that is CORRECT** (`ringhash_test.go` gains `fmt`/`math`/`mathrand "math/rand/v2"`, `linkage_test.go` adds `go/ast`/`go/parser`/`go/token`/`strconv`/`testing`) — all stdlib, all test-side, and **an import LINE is not a go.mod MODULE**; there is no new sub-package, so `reference_new_subpackage_pulls_transitive_module` does not bite. NC observed: a fake `require` ⇒ 67 -> 68. The tracked lineage figure is **2** (the phase-61.2 count: `quic-go` direct + `qpack` indirect) — **NOT a repo total**; the single `go.mod` requires **67** (18 direct + 49 indirect). **Phase 75 landed +0** — `go mod tidy -diff` EMPTY and `git diff master -- go.mod go.sum` EMPTY. The row needed `crypto/tls` and `internal/stats`, both ALREADY imported by `internal/listener/manager.go`; **an import LINE is not a go.mod MODULE**, and there is no new sub-package, so `reference_new_subpackage_pulls_transitive_module` does not bite. ⚠️ **TEST-side imports grew by exactly four** (`log`, `math`, `net/http`, `strconv` in `0110`'s driver) — all stdlib, all test-side, **not a production-envelope violation**.
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` (see `ENVOY_TARGET.md`).
- **conformance:** h2spec **`95 tests, 94 passed, 1 skipped, 0 failed`** (the skip is 6.9.2/2, invariant) — the CORRECTED 95-case strict scope under dotted selectors, measured at the phase-85 IMPL close 2026-08-10 (ADR-0307; the historical 53/53 records measured only 53 of the 95 under nine silently no-op slash-form selectors, 2026-04-25 -> 2026-08-10); proxy-wasm all 10 families PASS.

---

## Recent lineage

*(A SHORT recent lineage, capped at FIVE prior entries by ADR-0288. Everything earlier is in `STATE_HISTORY.md`. §Current carries the newest stage (**the phase-90 IMPL** — PHASE 90 CLOSED, row 90 `done`; the H/2 downstream leg now promotes `host` on an ABSENT `:authority` and suppresses every regular `host` in every shape, and the IMPL DISCOVERED A NEW REFERENCE DIVERGENCE THAT KILLED ONE OF ITS OWN ARMS: the pinned reference REFUSES a DUPLICATED regular `host` by ARITY, not by value, with no GOAWAY and no RST_STREAM, so first-occurrence-wins is NOT DIFFERENTIABLE IN PRINCIPLE and arm P90-E was REMOVED). The five below are the prior stages, newest first. ⚠️ **EVICTED AT THIS CLOSE:** `phase 89 (h2-decode-filter-mutations) SPEC done` (2026-08-16) — a **DIRECT READ of all five dates** before removal gave **2026-08-19 x2, 2026-08-17 x1, 2026-08-16 x2**, i.e. a **TWO-WAY TIE at the oldest**, broken by **LAST LIST POSITION**. ⚠️ **THE TIE SHAPE DOES NOT CARRY** — the phase-90 PLAN faced a UNIQUE OLDEST and NO tie at all, the phase-90 SPEC a TWO-way, the phase-90 BRAINSTORM none, the phase-89 IMPL a TWO-way, the phase-88 IMPL a FOUR-way. **Read the dates yourself every time; never inherit the shape — and never determine the evictee by position-by-convention.** It is now in `STATE_HISTORY.md`, appended strictly as **ONE SINGLE INLINE LINE** (the `(evicted at …):**` label carries the moved bullet on the SAME line — a two-line append BREAKS the guard), `512 -> 514` with `git diff --numstat` = `2 0`; the **ANCHORED own-label absence guard** `^- \*\*prior active-phase:\*\*` read **163 BEFORE and 163 AFTER — DELTA 0** (⚠️ that form is STRUCTURALLY INCAPABLE of counting archive labels, because an evicted entry carries `(evicted at …)` BEFORE the colon; a reading of 164 would mean the WRONG SHAPE was appended), the loose form went **206 -> 207**, the evictee was verified **ABSENT (0)** before the append, and its NC positive control fired at **3** on `phase 88 (h2-continuation-frames) SPEC done`, which IS present. ⚠️ **Carry NO archive-label absolute — the long-carried "202" figure is NOT reproducible.**)*

- **prior active-phase:** `phase 90 (h2-host-authority-normalization) PLAN done` (2026-08-22) — lifecycle-state **2 -> 3**. **ROW 90 STAYS `in-progress`** at `ROADMAP.md:152` and `ROADMAP.md` is **BYTE-UNTOUCHED** (verified by EMPTY DIFF against master); sentinel `want` stays **122**. `BEHAVIOR_CONTRACT.md` (**5962**) and `DECISIONS.md` (**18297**) **BYTE-UNTOUCHED** too — **a PLAN adds no ADR**; next-free stays **`ADR-0313`** and the strict `^> **STATUS: PROPOSED` guard **STAYS ARMED at 1**, disarmed by the IMPL. Docs-only: **ZERO production `.go`, ZERO test `.go`** (gated by `git diff --stat c7fef29b -- '*.go'` ⇒ EMPTY). Base master `c7fef29b`, branch `phase-90-plan`, ONE squashed commit. `PLAN.md` **1039 lines**, **NINE tasks**. ⚠️ **THE HEADLINE: THIS PLAN REFUTES FIVE SPEC CLAIMS AND ONE OF ITS OWN MEASUREMENT AGENTS.** (1) **THERE IS NO `host` BRANCH IN `stream.go`** — `grep -in 'host'` returns exactly TWO lines, `:503`/`:507`, both the WRITE of `authority`; a regular `host` reaches the GENERIC `regular.Add` at `:472`, so ADR-0312 §Context ¶1's *"does `regular.Add` for `host`"* reads as if a site exists to modify — **the IMPL ADDS a branch**, which is why the prototype is `−0`. (2) ⚠️ **THE GUARD ASYMMETRY IS BY PROPERTY, NOT BY SYMBOL** — SPEC §9.3 is REFUTED: `if h.Name == "host" { continue }` unconditionally **inside `buildH2Request`** (the exact behaviour arm A introduces) leaves **rc=0, both packages ok — VACUOUS**; only `H2Request.Authority`'s VALUE is pinned anywhere, so the roster is rebuilt around **FOUR PROPERTIES**. (3) **COST +36/−0, NOT +34/−0**; `hostField` **6** not 7; and a **THIRD required symbol `hostSeen` (8)** the SPEC never names — `reference_measured_prototype_is_a_lower_bound` fires a THIRD time. (4) **THE FIXTURE CONSTRAINT IS OVER-BROAD** — `counts[idx]++` is at **exactly one line, `driver.go:308`**, so *"must not hit `/api`"* is false and a `p90*` path takes the documented `a5` fall-through ⇒ **BOTH `0004` YAMLs BYTE-UNTOUCHED**, no route edit, no `TestRenderBootstrap_*` update. (5) **"THE 69 WAS AN ARTIFACT" IS PARTIALLY REFUTED** — a 69 reading reproduced HERE came with **rc=1 and a real `--- FAIL`**, and a SECOND flake fired **3 of 8** full-suite runs; causation excluded MECHANICALLY (`go list -deps -test ./internal/xds/ | grep -c 'filter/hcm/h2'` ⇒ **0**). (6) ⚠️ **A MEASUREMENT AGENT WAS REFUTED BY THE CONTROLLER** — `x/net@v0.34.0/http2/server.go` deletes only `Trailer` (`:2341`) and has NO `Del("Host")`, so **SPEC §8.4's table is correct**. **D-90-DUP** (NEW): `:authority` has **no duplicate guard** while its three siblings do; `authoritySeen` tracks **PRESENCE ONLY** and a **stability-pin test** asserts the non-change. **D-90-TARGET**: the axis is `host`-presence + `r.Host`; a route assertion is FORBIDDEN and the access-log/Zipkin axis is REJECTED on a NEW measurement — **not one of the four H2-capable fixtures carries an access-log or tracing surface** — and is banked as follow-on (vi). **SPLIT GATE NOT TRIPPED**: 9 tasks ≤ ~25, ~+322-581 LoC ≤ ~1500.

- **prior active-phase:** `phase 90 (h2-host-authority-normalization) SPEC done` (2026-08-19) — lifecycle-state **1 -> 2**. **ROW 90 STAYS `in-progress`** at `ROADMAP.md:152` and `ROADMAP.md` is **BYTE-UNTOUCHED** (verified by EMPTY DIFF against master at stage start AND at close); sentinel `want` stays **122**. `BEHAVIOR_CONTRACT.md` **BYTE-UNTOUCHED** too (**5962**). `DECISIONS.md` gained **ADR-0312 §Context** (`18277 -> 18297`, headings **310 -> 311**, tail **ADR-0312**) and the strict `^> **STATUS: PROPOSED` guard is **RE-ARMED 0 -> 1**, disarmed by the IMPL; ⚠️ `^---$` **STAYS 216** — a new ADR takes NO separator. Docs-only: **ZERO production `.go`, ZERO test `.go`.** Base master `f15d4f4e`, branch `phase-90-spec`, ONE squashed commit. **ALL SEVEN BRAINSTORM §7 QUESTIONS DISPOSED BY EXECUTION.** **D-90-SCOPE** bounds the row to arms **A + B on the H/2 downstream leg only**: arm C (the validity reject) and **H1-B′** are DEFERRED as named follow-ons, **H1-D is a NAMED DEPARTURE** (envoy-go is the RFC 7230 §5.4-conformant side; parity costs ~5x the whole fix and rewrites the H1 framing seam, which the 12-row CWE-444 suite is provably blind to), and **H1-E is CLOSED as a NON-DIVERGENCE**. ⚠️ **THE HEADLINE: THE ROW'S OWN JUSTIFYING SENTENCE IS FALSE.** `ROADMAP.md` row 90 and BRAINSTORM §3 both claim an empty authority is a **ROUTE-MATCHING input**; routing is **path-only** (`(*routeTable).match` reads `req.URL.Path`, and the `routeMatch` interface is literally `matches(path string) bool` — it cannot see the request), non-`["*"]` `domains` and `match.headers` both **boot-reject**, and all three authority-reading filters (csrf, jwt_authn, oauth2) PREFER `:authority` and only fall back to `Host`. **The blast radius is OBSERVABILITY** — `%REQ(:AUTHORITY)%` and the **Zipkin span NAME**. **The IMPL corrects the row text.** ⚠️ **FIVE FURTHER REFUTATIONS BY EXECUTION:** the BRAINSTORM named the **WRONG SITE** for arm C (`buildRequest` can signal only **STREAM** scope; the connection-scoped site is `(*serverStream).recvHeaders`, whose error reaches the `Stream == 0` discriminator) · arm C's rule is authority **VALIDITY, not emptiness**, and `host` is validated **INDEPENDENTLY** (a *valid* `:authority` beside an *empty* `host` is torn down) · arm C's reaction is **CONFIG-DEPENDENT**, its default emits **ZERO bytes**, `http2.rx_messaging_error` is the classifier surviving both postures, and **NEITHER stat exists in the subject** (`downstream_cx_protocol_error` resolves to `redisproxy` alone) · **H1-B is MIS-ATTRIBUTED** — HTTP/1.0 *with* a valid `Host` still 426s, so the 426 is the **VERSION**, and the genuine arm **H1-B′** (HTTP/1.1, no `Host` ⇒ ref 400 / subj 200) was absent from the record · and the BRAINSTORM's provenance grep reads **1, not 0** (the conclusion survives — the match is the English word in *"acquired ADR authority"* — the measurement does not). **D-90-INSTRUMENT** is a raw-framer client: `helpers.H2RoundTrip` was refuted at the pinned source **and on a live listener**, where all three shapes returned **200 while sending something other than what was asked for**. **D-90-FIXTURE** extends `0004-h2-routing` IN PLACE (fixtures stay **121**, **`0120` STAYS UNCONSUMED**, blank imports **121**, BackendKind tail **38**); **D-90-BACKEND** adds `r.Host` and NAMES the one axis a `net/http` backend cannot recover at any price. **D-90-SKIP** leaves D-89-HOST's SKIP untouched but **RETIRES its ground 2**. Cost MEASURED at **+34 / −0** over ONE file — the BRAINSTORM's `+15/−1` floor reproduces as a *minimum* and is **overrun 2.3x**; `./internal/...` reads **70 ok / 0 FAIL** with it applied (the recorded "69" was a flake artifact), and ⚠️ **`buildRequest`'s authority is COMPLETELY UNPINNED** — corrupting it unconditionally leaves the whole tree green — so the unit roster MUST cover it specifically.

- **prior active-phase:** `phase 90 (h2-host-authority-normalization) BRAINSTORM done` (2026-08-19) — lifecycle-state **DONE -> 1**. **PHASE 90 OPENED BY SELF-PICK** per the 2026-07-12 standing directive; no banked mid-lifecycle work existed and that was PROVEN, not assumed (all 121 rows `done`, check (1) SILENT at `want=121`). **ROW 90 REGISTERED `in-progress`** at `ROADMAP.md:152` with `want` bumped **121 -> 122 in the SAME commit** (numstat `1 0`, a pure insertion; 239 -> 240 lines, 121 -> 122 data rows). The charter: **NORMALIZE `host` AND `:authority` ON THE H/2 DOWNSTREAM LEG so the upstream request carries exactly ONE authority.** REPRODUCED BY EXECUTION across three H/2 arms plus a DISCRIMINATING negative control that distinguishes `:authority` ABSENT from PRESENT-AND-EMPTY, and TWO further H1 arms — on one of which **the SUBJECT IS THE STRICTER SIDE**. Cost FLOOR **+15 / -1 over ONE file and TWO symbols**, measured by a prototype applied, run and reverted; **NO EXISTING TEST PINS THE DEFECT** (controller-verified, 69 packages green WITH the prototype). Base master `6b4bc7c0`, branch `phase-90-brainstorm`. Docs-only: ZERO production `.go`, ZERO test `.go`; `DECISIONS.md` and `BEHAVIOR_CONTRACT.md` **BYTE-UNTOUCHED**.

- **prior active-phase:** `phase 89 (h2-decode-filter-mutations) IMPL done` (2026-08-17) — lifecycle-state **3 -> DONE**. **PHASE 89 CLOSED, ROW 89 FLIPPED `done`** at `ROADMAP.md:151`; `ROADMAP.md` stays **239 lines / 121 data rows** and sentinel `want` stays **121** (a row FLIP, not a row ADD). ADR-0311 **COMPLETED IN PLACE** after the RETAINED italic footer — strict `^> **STATUS: PROPOSED` guard **1 -> 0**, `^---$` stays **216**, headings stay **310**, tail stays **ADR-0311**, next-free stays **`ADR-0312`**. `BEHAVIOR_CONTRACT.md` **5960 -> 5962**: the header_mutation carve-out closed **ROW-ANCHORED and IN PLACE with ZERO line delta**, gated on the RESIDUAL COUNT reading **2** (not 0 — a reading of 0 is the defect, and rows `:33`/`:34` were asserted still carrying the phrase), plus **TWO ADDED bullets** in `## HTTP/2` (line-adding — **cite by STRING; every by-line anchor at/below the `## HTTP/2` asserted list shifts +2**). Base master `404de01f`, branch `phase-89-impl`, ONE squashed commit.

- **prior active-phase:** `phase 89 (h2-decode-filter-mutations) PLAN done` (2026-08-16) — lifecycle-state **2 -> 3**. **ROW 89 STAYS `in-progress`** at `ROADMAP.md:151`, `ROADMAP.md` **BYTE-UNTOUCHED** (verified by EMPTY DIFF against master, 0 bytes, at stage start AND at close), sentinel `want` stays **121**. `DECISIONS.md` and `BEHAVIOR_CONTRACT.md` **BYTE-UNTOUCHED** too — **a PLAN adds no ADR**; next-free stays **`ADR-0312`** and the strict `^> **STATUS: PROPOSED` guard **STAYS ARMED at 1** (`DECISIONS.md:18212`), disarmed by the IMPL. Base master **`733f9830`**, branch `phase-89-plan`. Docs-only: ZERO production `.go`, ZERO test `.go`.


## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
