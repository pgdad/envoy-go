# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

> **This file is a POINTER, not an archive** — `BOOTSTRAP_PROMPT.md` §4: *"STATE.md — pointer: active phase, last commit, next action."* It carries the live pointer, the live counts, and a SHORT recent lineage. **Full stage-by-stage history lives in `STATE_HISTORY.md`** (archived at ADR-0288; **nothing was deleted** — the pre-move file is preserved there verbatim, byte-for-byte).
>
> **⚠️ THE RULE ADR-0288 EXISTS TO ENFORCE: there is EXACTLY ONE live `next-skill:`, `lifecycle-state:`, `next-free ADR:`, and count block IN THIS FILE, and they live in §Current pointer directly below.** Before that move, ~330 lines of prepended backlog had buried TWO fossilised "current state" blocks whose `next-skill:` still read *"the 42.2 hedging BRAINSTORM"* and *"the 46.2 SPEC"* while the live phase was **66** — so `grep 'next-skill:' STATE.md` returned **two stale hits and ZERO live ones**, and `grep 'fuzzers:'` returned **48** and **42** when the true count was **55**. **A session may trust a grep of this file again. Keep it that way: when you advance the pointer, EDIT §Current pointer IN PLACE — never prepend a new block above it.**

---

## Current pointer

*(The ONLY live "what next" in this repo. Update IN PLACE at every stage close.)*

- **active-phase:** `phase 75 (tls-no-certificate-stat) IMPL done` (2026-07-25) — **ROW 75 FLIPPED `in-progress` -> `done`** at the T10/T11 six-gate (ADR-0106, the SOLE leg — a SINGLE FLAT ROW, `reference_roadmap_split_phase_row_done`). ⚠️ **SENTINEL CHECK (1) IS NOW SILENT** — row 75 was the last non-`done` chartered row. **The sentinel STILL does NOT fire** (checks (2) and (3) both print) and **`stop` was NOT created.** ADR-0297 is **COMPLETE**.
- **phase-directory:** `docs/envoy-go/phases/75-tls-no-certificate-stat/` (`BRAINSTORM.md` + `SPEC.md` + `PLAN.md` + `PROGRESS.md`; only `PROGRESS.md` changed at this stage — the IMPL's delta is production/test/docs, not new phase documents)
- **lifecycle-state:** **`DONE`** (IMPL done; the row is closed. Next -> the phase-76 BRAINSTORM). **Subject:** `tls-no-certificate-stat` — ONE fixed name, **`listener.<normalized-addr>.ssl.no_certificate`**, a **SUCCESS-PATH annotation** counting *"this COMPLETED handshake presented no client certificate"* (⚠️ **NOT** a synonym for `ssl.fail_verify_no_cert` — wrong in BOTH directions), registered inside the block `registerListenerMetrics` ALREADY gates on `rt.tlsMode` and Inc'd on the success fall-through phase 74 left beside `rt.sslHandshake.Inc()`. **The predicate is PINNED: `len(tlsConn.ConnectionState().PeerCertificates) == 0` ALONE, NO client-auth term** — and it is now **PROVEN load-bearing** by Break F, which adds the refuted mode term and fires the positive arm. Envelope **LANDED AND MEASURED**: **+1 stat (1204 -> 1205)** · **+0 fixtures (119** — `0110-tls-require-client-cert-false` EXTENDED with a cross-side `fixture.StatsAsserter`**)** · +0 fuzzers (**55**) · +0 BackendKinds (**38**) · +0 modules (**2**) · **+0 PRODUCTION imports** (negative-controlled gate) · **+4 TEST imports, PERMITTED** (`log`, `math`, `net/http`, `strconv` in `0110`'s driver) · ZERO new packages · **ZERO new exported symbols** (`go doc -all` per-package, byte-identical) · **ZERO new dependency edges** (`go list -deps` 439≡439 listener, 67≡67 stats); ONE ADR (**ADR-0297**, COMPLETE).
- **next-skill:** **the phase-76 BRAINSTORM** — the roller SELF-PICKS per the standing directive. SPEC §13 / PLAN §5 record **`fault.abort.grpc_status`** as the recommended next opening (7-9 tasks; **the only identified candidate that clears a sentinel check-(3) blocker** — it opens the gRPC family; it escapes the ADR-0058 trailer gap because a gRPC error local reply is Trailers-Only/headers-only, which envoy-go already emits; **ONE probe owed**). Rivals, re-costed and carried: `ssl.connection_error` (~9-11 tasks — blocker RETIRED at the phase-75 BRAINSTORM but cost RAISED: a predicate is required or it OVER-COUNTS the EOF class, and it cannot ride `0111`) · the `upstream_cluster` span tag (no longer blocked) · `Listener.stat_prefix` · the Runtime family opening. ⚠️ **THE ROW-75 RESIDUE THE NEXT PHASE INHERITS, none chartered:** `ssl.no_certificate` on a **RESUMED TLS 1.3 session** was NEVER probed (`session_reused` 0 in every run — **the one scenario in which the pinned predicate could be wrong in production**) · `ssl.no_certificate` on **QUIC was never DRIVEN** (the parity argument is STRUCTURAL — `Manager.Start` launches no accept loop for `kindQUIC` — hence name-independent, but it is an argument, not a measurement) · the predicate was never cross-checked at `require_client_certificate: true` (`0109` not run) · **the stat-surface ABSOLUTE total has no mechanical command and rides TWO recorded ledger gaps** (the known `1200 -> 1201` and a second, `46.1b` closing at 1198 while `47.1` opens at 1200) — the **+1 DELTA** is asserted, the TOTAL is documentary · `internal/statssink`'s four *"stays 1200 / 1196"* prose sites, stale since **phase 49**, RECORDED-NOT-FIXED (that package is on the byte-untouched sha256 roster) · the phantom `B5` in two landed production comments. ⚠️ **A REAL FOLLOW-ON THE PROJECT NOW OWES:** `0061-lb-ring-hash`'s spread assertion flaked a **THIRD** time (phase-75 T10, under `-race`); `internal/cluster/**` was sha256-verified BYTE-UNTOUCHED and it was isolate-green 4/4, so it is not this row — but the margin fix is genuinely owed as a Load-balancing-family maintenance item (`reference_0061_ring_hash_spread_flake`). ⚠️ **TOOLING LESSONS THE NEXT PLAN MUST NOT INHERIT BLIND:** the phase-75 PLAN's own **import gate FAILS CLOSED** as written (`impblock` prints `FILENAME`, so a baseline under `/tmp` vs a HEAD under `internal/` differs on EVERY line — a 31-removed/31-added diff, exit 1, on a **+0-import tree**); its negative-control transcript proves it validated a **different, basename-normalised** command. And **`go doc -all <pkgA> <pkgB>` FAILS OPEN** — the second argument is read as a SYMBOL, so only `<pkgA>` is emitted, exit 0, empty stderr; the phase-75 gate would have gone green over `internal/stats` **without reading it**, in the very task whose second production file lives there. **Audit each package separately.** Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-pushes (`feedback_push_to_origin`); **use `git -C <abs-worktree-path>` everywhere and tripwire `pwd` + branch + commit count before any commit or gate run** (`reference_bash_cwd_reset_commits_to_main` — ⚠️ **it fired LIVE again at the phase-75 IMPL's T10, where `pwd` had silently reset to the MAIN checkout on `master` at task start**).
- **last-commit:** the phase-75 IMPL squash (`git log --grep 'phase 75'`; **locate by SUBJECT, never by position** — `reference_next_prompt_tracked_despite_gitignore`). Prior: the phase-75 router commit `9f5d667b` (this stage's base), the phase-75 PLAN squash `bae5e24d`, the phase-75 SPEC squash `2b5eff0a`, the phase-75 BRAINSTORM squash `e822f1ad`, the phase-74 IMPL squash `f8f6cd44`.
- **last-updated:** 2026-07-25 (phase-75 IMPL done. Lifecycle-state **3 -> DONE**; **ROW 75 FLIPS `in-progress` -> `done`**; **sentinel check (1) GOES SILENT** for the second time in the project's history. Fresh worktree `/home/esa/git/envoy-go-wt-p75impl` off master `9f5d667b`, branch `phase-75-impl`; **EIGHT subagent dispatches over the 11-task spine -> 20 local commits -> ONE squash**, per `feedback_execution_style` / `feedback_subagents_no_push` / `feedback_push_to_origin`. **THE ROW DELIVERED:** `listener.<normalized-addr>.ssl.no_certificate`, registered inside the block that **ALREADY** gated on `rt.tlsMode` (**no new gate, no new classifier, no new registration function**) and Inc'd on the success fall-through under **`len(tlsConn.ConnectionState().PeerCertificates) == 0` ALONE, with NO client-auth term**. **ALL TEN BREAKS EXECUTED; every one matched its PLAN-recorded outcome.** **THE HEADLINE REPRODUCED AT THE IMPL TIP:** Break D (unconditional Inc) fired **`TestServeConnection_SSLHandshakeIncrements`** — a **PHASE-74** test — through `assertSSLCrossProduct`'s NEGATIVE half, **while the new positive arm PASSED**; and **Break D' (the same edit with `sslLeafRoster` reverted to three leaves) left the ENTIRE package GREEN** ⇒ **the one-line roster extension really is the SOLE guard on this row's central pinned decision.** Break D' additionally showed T1's two exact-set spelling pins stay green throughout — they pin REGISTRATION, never INCREMENT. Breaks E and F fired the **byte-identical** assertion at the identical line and are distinguishable by the EDIT, not the OUTPUT — **recorded, not claimed as separately discriminated.** Break C fired **exactly one line** with the three build-time predicates **silent** (phase-74 V1-M2 reproduced). Break G **STAYED GREEN** (the silent-staleness finding) and G''s restore is what makes G's green mean *"ran and passed"* rather than *"never ran"*. Break I-b **PASSED VACUOUSLY**, which is what proves the ABSENT check NECESSARY rather than merely firing. Break J's rename went GREEN **with the driver's log lines VANISHING**, and with `var _` kept it is a COMPILE ERROR — the tripwire working. **THE CROSS-SIDE MEASUREMENT, RE-EXECUTED LIVE AT THIS TIP:** `0110` scraped `ssl.handshake=2 ssl.no_certificate=1 ssl.fail_verify_error=1 ssl.fail_verify_no_cert=0 (downstream_cx_total=3)` on **BOTH** sides, metric NAMES byte-identical with the address differing ONLY in the label (`0.0.0.0_10446` reference vs **`___20016`** subject — the subject binds the **IPv6 wildcard**), and the asserter was observed firing **inside the full suite**, which a green fixture alone cannot distinguish from one that never dispatched. **`0111`'s named-subset check was EXECUTED for the FIRST TIME (the PLAN verified it only by code read): PASS** — T1's extra name in the subject's scrape is invisible to a named-subset iteration, and `0111/driver/driver.go` is byte-untouched. **SIX-GATE FULLY GREEN**: `gofmt -l .` silent · `go vet ./...` · `go build ./...` · `go mod tidy -diff` EMPTY · `git diff master -- go.mod go.sum` EMPTY · `golangci-lint run ./...` silent. **FULL DIFFERENTIAL `ok 396.517s`, 119 subtests / 119 PASS / 0 SKIP / 0 FAIL**, the fixture-directory set and the subtest set `comm -3`-verified EQUAL. **`-race` over `./...` — INCLUDING `./test/differential/`, discharging the PLAN's own open item — reported ZERO data races.** sha256 BYTE-UNTOUCHED roster **592 files / 0 mismatches / 0 missing**, and its intersection with the mechanically-derived 13-file EDIT roster is **EMPTY**. **TWO FLAKES, BOTH CLASSIFIED ON EVIDENCE, NEITHER ABSORBED:** a first full-differential run failed `0083-grpc-access-log-headers` at `bind: address already in use` **before any assertion** (the startup-flake class; isolate-green, `internal/filter/**` sha256-untouched, and the immediate full re-run was 119/119); and **`0061-lb-ring-hash`'s spread assertion fired under `-race` — its THIRD recorded occurrence** (`internal/cluster/**` sha256-verified byte-untouched, isolate-green **4/4** under `-race`), so it is structurally not this row — **but the margin fix is now genuinely owed** and `reference_0061_ring_hash_spread_flake` was updated. ⚠️ **NEITHER was on the PLAN's roster of six known flakes — a stage brief's flake list is not the index.** **FOURTEEN PLAN/SPEC CLAIMS REFUTED BY EXECUTION AT THE IMPL, and TWO of them are the PLAN's own gate COMMANDS:** the **import gate FAILS CLOSED** (`impblock` prints `FILENAME`, so a `/tmp` baseline vs an `internal/` HEAD differs on every line — 31 removed / 31 added, exit 1, on a **+0-import tree**; the PLAN's negative-control transcript proves it validated a **different, basename-normalised** command) — RD-IMPGATE existed to replace a gate that failed OPEN, and its replacement failed CLOSED · **`go doc -all <pkgA> <pkgB>` FAILS OPEN** (the second argument is read as a SYMBOL; only `<pkgA>` is emitted, exit 0, empty stderr — the gate would have gone green over `internal/stats` **without reading it**, in the task whose second production file lives there) · the PLAN's `git stash` baseline is **VACUOUS** on a clean worktree · the PLAN's EDIT roster names **4** test files while the real delta is **13** · **RD-0110-STALE's *"exactly THREE stale sites, no fourth"* is REFUTED** — a **FOURTH** sits at `0110/expectations.yaml`'s *"(no StatsAsserter)"* parenthetical, **falsified by this row's own T5**, and invisible to a sweep that greps the STALE vocabulary · **T9 found a PLAN MISS its own roster could not see**: a comment saying *"keeping **both** Inc points"*, **already wrong before this row** (phase 74 landed three) and invisible to a *"three"*-shaped grep · **RD-BC-TOTALS' scratch figure contradicts the PLAN's own inline expectation** (`grep -c '1204'` goes 3 -> **2**, not 3 -> 1) and T8 Step 3's *"expect `1201` UNCHANGED"* is contradicted by T8 Step 2's own mandate to record the gap · `ProbeAdmin` is **`:558`**, not the `:554` with which the PLAN corrected the cell's `:552` — **T5 grew the driver and re-staled the correction** · the row-75 cell carried **FIVE** stale claims, not four · `manager_test.go:4466`/`:4491-4492` **do not exist at this tip** (T3's variadic rewrite replaced both with roster-relative language correct at ANY family size) · `wantNames` is `:230-234`, not `:231-235` · `0111/README.md`'s bullet is `:168-175`, not `:167-174` — **one PLAN error propagated into TWO tasks** · and **anchor drift ran +1 to +168, NON-MONOTONIC**, so a single "+N" correction would have mis-targeted two of three regions. **ADR-0297 COMPLETED IN PLACE** (PROPOSED -> COMPLETE; §Decision + §Consequences after the RETAINED footer, mirroring ADR-0295/0296; tail stays ADR-0297, next-free ADR-0298) **and CORRECTED**: **¶7's SELF-FALSIFYING grep** — restated with NO number and the searched token deliberately NOT spelled, because every restatement mints another counter-example — and **¶9's REFUTED form rule**, replaced with **graft SCALE** after the n=7 population was re-enumerated first-hand. ⚠️ **¶9's original text ALSO contained *"the only two instances in the file"*, which THIS ROW'S OWN blockquote makes three — so a pure append with ¶9 untouched would have shipped a SECOND self-falsifying count in the very commit that fixed the first.** Post-edit the searched token occurs **exactly once** in `DECISIONS.md`. **ADR-0296 §Decision (g) CORRECTED** via an INDENTED blockquote (`grep -c '^  > '` **2 -> 3**, leading bytes `od -c`-verified identical to both precedents), **LEADING WITH WHAT SURVIVES** — no such counter was owed *in phase 74*, whose sole stat-asserting fixture is `require=true` on both sides, **so the deferral was RIGHT and phase 74's row correctly scoped** — and phrased as a documentary finding claiming **NO ORDINAL**. **BEHAVIOR_CONTRACT: eleven IN-PLACE rewrites + ONE ledger line** (`5744 -> 5746`, the only line-adding edit), every one of `:1849/:1851/:1853/:1855/:1857/:1859/:5002` **UNMOVED** so the by-line citations from four documents stay valid; `:962` **`cmp`-verified BYTE-IDENTICAL** and its offsets re-derived (**627 CHARACTER / 630 BYTE**, 1002 chars / 1007 bytes) — **a fourth challenge survived**; the `:1849` heading **EXTENDED** on the `:785` precedent (**SPEC §9's *"no two-ADR heading precedent exists"* refuted — there are 16, now 17**); the **`three-fifths` ratio REMOVED, not bumped**, and restated as an ENUMERATION so no fourth wrong denominator was minted; and **BOTH ledger gaps recorded, NEITHER back-filled.** **The ROADMAP narrow edits the RETIRED clause, not the candidates list** — check (2) **STAYS 3** by both forms, the terminator is still `force-trace.`, and **interior periods are 0 before and after** (verified in a `/tmp` scratch copy first, then on the real file, `diff` empty). **Counts re-run MECHANICALLY at this close:** fixtures **119** · fuzzers **55** · stat surface **1204 -> 1205** (⚠️ **the +1 DELTA is asserted; the ABSOLUTE total is DOCUMENTARY, has NO mechanical command, and rides TWO recorded gaps** — it was NOT re-derived and is not presented as measured) · BackendKind **38** (39 constants, 0-38) · modules **2** (go.mod requires **67**) · DECISIONS tail **ADR-0297 COMPLETE** (next-free **ADR-0298**). **Sentinel re-run MECHANICALLY TWICE (worktree AND landed master post-push): does NOT fire; `stop` NOT created** — (1) **SILENT**, as predicted at the PLAN and confirmed here; (2) => **3**; (3) `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`. ⚠️ **check (1)'s blind spot RE-DERIVED INDEPENDENTLY, not copied: 107 data rows (`:31`-`:137`) / 103 matched / FOUR misses** — `00` (em-dash "after" column), `04` (DOT in the slug `http-1.1`), `28.1a`, `28.1b` (LETTER suffix); all four `done`. `next-prompt.txt` rolled to the **phase-76 BRAINSTORM** (**TRACKED despite `.gitignore`**; edited in this worktree).)
- **next-free ADR:** **`ADR-0298`** — DECISIONS.md tail is **ADR-0297** (STATUS **COMPLETE** — §Context drafted at the phase-75 SPEC per ADR-0044-**as-used**, §Decision + §Consequences appended IN PLACE at the phase-75 IMPL after the **RETAINED** footer, mirroring ADR-0295/0296; **no renumber, no new ADR**). `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1` -> `## ADR-0297`; `grep -c '^## ADR-0298'` -> **0**. Block shape re-verified at this close: **1** `### Context` (ELEVEN paragraphs ¶1-¶11, **of which ¶7 and ¶9 were CORRECTED IN PLACE at the IMPL**) · **1** `### Decision` (was 0) · **1** `### Consequences` (was 0) · **1** RETAINED footer; heading now at **`:17324`**. ⚠️ **`ADR-0296` is NO LONGER byte-untouched** — its §Decision (g) now carries the phase-75 correction as an INDENTED `  > [CORRECTED at phase 75/ADR-0297: ...]` blockquote after `:17308`, `od -c`-verified byte-identical in its leading bytes to the `:16901`/`:16910` precedents; `grep -c '^  > '` is **3** (was 2). **The insert shifted `:17310 -> :17312` and `:17314 -> :17316`** — RE-DERIVED post-insert, not assumed; the `ROADMAP.md:137` and `STATE.md` citations were fixed in the same stage, and the **frozen `SPEC.md`/`BRAINSTORM.md` were deliberately NOT rewritten** (no convention requires it; the lineage treats them as historical records).

---

## Project counts at this commit

*(Live. Re-verify at session start — `git fetch` first — and update here at every stage close. Canonical commands inline.)*

⚠️ **ALL counts below were re-run MECHANICALLY in the stage worktree at this phase-75 IMPL close with the canonical commands — never copied forward.** The phase-75 deltas are now **LANDED, not anticipated**: **+1 stat (1204 -> 1205)**, +0 fixtures (`0110` EXTENDED), +0 fuzzers, +0 BackendKinds, +0 modules, **+0 PRODUCTION imports**, ZERO new packages, ZERO new exported symbols, ZERO new dependency edges. ⚠️ **TEST-side imports GREW by exactly four and that is PERMITTED** — `0110`'s asserter gained `log`, `math`, `net/http`, `strconv`; the +0 claim is a **PRODUCTION** claim and **the two categories were audited SEPARATELY**. ⚠️ **The stat-surface total 1205 is DOC-SOURCED and was NOT re-derived mechanically** — there is no mechanical command, and the chain is now known to be discontinuous in **TWO** places (the recorded `1200 -> 1201`, and `Phase 46.1b` closing at 1198 while `Phase 47.1` opens at 1200 — an unattributed +2). The **DELTA (+1)** is asserted with confidence and is pinned by T1/T4's exact-set tests; the **TOTAL is documentary** and a future phase should COUNT rather than trust the chain.

- **active differential fixtures:** **119** — `ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0117-tracing-custom-tags-metadata-cluster` (phase 73). **Phase 75 landed +0**: `0110-tls-require-client-cert-false` was **EXTENDED** with a cross-side `fixture.StatsAsserter` (NO new YAML, NO new directory, NO new BackendKind, NO new port), so the count STAYS **119** and `0118` remains FREE. ⚠️ **Next-free REFERENCE port is `10450`, NOT `10118`** — ports are NOT fixture-index aligned. **The fixture now asserts what its own README confessed was infeasible, and BOTH sides were EXECUTED**: `ssl.handshake=2 ssl.no_certificate=1 ssl.fail_verify_error=1 ssl.fail_verify_no_cert=0`, `downstream_cx_total=3`, **identical on reference and subject**, with the discriminating asymmetry (`handshake=2` vs `no_certificate=1`) observed per side. Shape A over `/stats/prometheus`, keyed on the metric NAME with `envoy_listener_address` IGNORED — the NAMES are byte-identical cross-side while the labels differ (`0.0.0.0_10446` reference vs **`___20016`** subject: ⚠️ **the subject binds the IPv6 WILDCARD**). ⚠️ **`0111` was NOT given a value assertion, deliberately** — it runs `require_client_certificate: true` on both sides, so a success-path no-cert annotation reads **0 on every arm, structurally**, and a value pin would document a **vacuous `0 == 0`** as coverage; its PROSE gained the name in its NAMED-UNASSERTED list instead, because **both `0111` passages are CLOSED ENUMERATIONS** and a name in neither list reads as ASSERTED. **`0111`'s driver is byte-untouched, and its named-subset check was EXECUTED for the first time at this IMPL: PASS.** ⚠️ `var _ fixture.StatsAsserter` is a **TRIPWIRE, not the dispatch mechanism** — `0110` is only the **THIRD** fixture repo-wide to carry it, while **82** define an `AssertStats` method.
- **fuzzers:** **55** — `grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`. **Phase 75 landed +0** — the row consumes no new config field at all, so there is no parse arm and no fuzz-seed classification to flip (`reference_fuzzer_count_docs_drift`: an `f.Add` seed is not a fuzzer).
- **stat surface:** **1205** — BEHAVIOR_CONTRACT doc count. **There is NO mechanical counting command** (the contract says so itself); enforcement is per-phase `TestNoNewStat*` guards asserting a delta of ZERO, never the absolute total. **Phase 75 LANDED +1 (1204 -> 1205)** — `listener.<normalized-addr>.ssl.no_certificate`, the FOURTH listener-scope `ssl.*` name and the FIRST **SUCCESS-PATH** handshake annotation (⚠️ **NOT a synonym for `ssl.fail_verify_no_cert`, and the mis-mapping is wrong in BOTH directions** — one annotates an ACCEPTED handshake, the other books a REJECTED one; they are mutually exclusive on every connection). ⚠️ **The delta is a NAME-surface delta, NOT a flat per-deployment one:** registration rides the block that ALREADY gates on `rt.tlsMode`, so a **plaintext-only** deployment gains **ZERO** names, while a **QUIC** listener gains it permanently zero — **PARITY** with the reference, and structural (`Manager.Start` launches no accept loop for `kindQUIC`, so the Inc site is unreachable for a **name-independent** reason). ⚠️ **QUIC was NOT re-probed at phase 75** — the parity claim is INHERITED from phase 74's family-level probe, and that inheritance is stated in BEHAVIOR_CONTRACT rather than presented as a measurement. Ledger updated: `1204` -> `1205` at `BEHAVIOR_CONTRACT.md:831`/`:847`, plus a `**Phase 75 - 1204 -> 1205 (+1)**` line after the tail entry — **the ONLY line-adding edit in the file** (`5744 -> 5746`), so every by-line citation into the TLS subsection stays valid. ⚠️ **The new entry RECORDS BOTH ledger gaps and BACK-FILLS NEITHER** (fabricating a line would be inventing a record). Gate figures at this close: `grep -c '1204'` => **2** · `grep -c '1205'` => **3** · `grep -cE '^#{2,4} .*ADR-[0-9]+.*ADR-[0-9]+'` => **17** (was 16 — the `:1849` heading joined them, on the `:785` precedent). ⚠️ **`internal/statssink`'s four *"stays 1200 / 1196"* prose sites, stale since phase 49, are RECORDED-NOT-FIXED** — that package is on this phase's byte-untouched sha256 roster.
- **BackendKind tail:** **38** (`H2GoawayResponder`, `test/differential/fixture/fixture.go:614`). **Phase 75 landed +0** — `0110` was extended, not cloned, and it implements neither `BackendKindAware` nor `PerHostBackendKind`, so the runner takes the in-process `TCPEcho` branch. ⚠️ **This is a TAIL VALUE, not a count** — the file declares **39** constants (values 0-38, `TCPEcho = 0`). The lineage figure **38** is correct as written; do NOT 'fix' it to 39.
- **DECISIONS.md tail:** **ADR-0297** (next-free **ADR-0298**) — `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1`. **ADR-0297 is COMPLETE** — §Context drafted at the phase-75 SPEC (ADR-0044-**as-used**; ⚠️ ADR-0044 does NOT actually contain that discipline), §Decision + §Consequences appended IN PLACE at the phase-75 IMPL after the **RETAINED** footer, mirroring ADR-0295/0296. Block shape verified at this close: **1** `### Context` / **1** `### Decision` / **1** `### Consequences` / **1** retained footer; `grep -c '^## ADR-0298'` => **0**. **No renumber, no new ADR.** ⚠️ **Its OWN §Context ¶7 and ¶9 were CORRECTED IN PLACE while completing it** — ¶7's **self-falsifying whole-file grep** (the same species the phase-74 IMPL had just fixed in ADR-0296 ¶3, reproduced one ADR later) is restated with **NO number** and the searched token deliberately **NOT spelled**, because every restatement mints another counter-example; and ¶9's *"SELF vs OTHER-ADR"* form rule is replaced by **graft SCALE** after the n=7 population was re-enumerated first-hand. ⚠️ **¶9 ALSO said *"the only two instances in the file"* — which this row's own blockquote makes three** — so a pure append would have shipped a SECOND self-falsifying count in the commit that fixed the first. **The prescription has now outlived THREE wrong justifications: family, self-vs-other, scale.** ⚠️ **`ADR-0296` is NO LONGER byte-untouched** — its §Decision (g) carries the phase-75 correction as an INDENTED blockquote (`grep -c '^  > '` **2 -> 3**), **LEADING WITH WHAT SURVIVES** (no such counter was owed *in phase 74*, whose sole stat-asserting fixture is `require=true` on both sides — **the deferral was RIGHT**) and claiming **NO ORDINAL**.
- **go.mod modules:** the tracked lineage figure is **2** (the phase-61.2 count: `quic-go` direct + `qpack` indirect) — **NOT a repo total**; the single `go.mod` requires **67** (18 direct + 49 indirect). **Phase 75 landed +0** — `go mod tidy -diff` EMPTY and `git diff master -- go.mod go.sum` EMPTY. The row needed `crypto/tls` and `internal/stats`, both ALREADY imported by `internal/listener/manager.go`; **an import LINE is not a go.mod MODULE**, and there is no new sub-package, so `reference_new_subpackage_pulls_transitive_module` does not bite. ⚠️ **TEST-side imports grew by exactly four** (`log`, `math`, `net/http`, `strconv` in `0110`'s driver) — all stdlib, all test-side, **not a production-envelope violation**.
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` (see `ENVOY_TARGET.md`).
- **conformance:** h2spec 53/53; proxy-wasm all 10 families PASS.

---

## Recent lineage

*(The last FIVE stages only — enough for the router's "prior-phase lineage" read. Everything earlier is in `STATE_HISTORY.md`. These bullets are carried over VERBATIM; the newest mirrors §Current (**the phase-75 IMPL done** — the stage that CLOSED row 75, took sentinel check (1) SILENT, reproduced the PLAN's inverted-break headline at the IMPL tip, and refuted **TWO of the PLAN's own gate COMMANDS** — one failing CLOSED on a clean tree, the other failing OPEN over the very package it was meant to audit), and the oldest (**phase-74 PLAN done**) was dropped as this list re-capped at five. The five now read: phase-75 IMPL · phase-75 PLAN · phase-75 SPEC · phase-75 BRAINSTORM · phase-74 IMPL.)*

- **prior active-phase:** `phase 75 (tls-no-certificate-stat) IMPL done` (2026-07-25) — lifecycle-state 3 -> **DONE**; **ROW 75 FLIPS `in-progress` -> `done`** at the T10/T11 six-gate (ADR-0106, the SOLE leg — a SINGLE FLAT ROW, `reference_roadmap_split_phase_row_done`). Fresh worktree `/home/esa/git/envoy-go-wt-p75impl` off master `9f5d667b` (the phase-75 router commit), branch `phase-75-impl`; **EIGHT subagent dispatches over the 11-task spine -> 20 local commits -> ONE squash**, per `feedback_execution_style` / `feedback_subagents_no_push` / `feedback_push_to_origin`. **THE ROW DELIVERED:** `listener.<normalized-addr>.ssl.no_certificate`, a **SUCCESS-PATH annotation** registered inside the block that **ALREADY** gated on `rt.tlsMode` (**no new gate, no new classifier, no new registration function**) and Inc'd on the success fall-through under **`len(tlsConn.ConnectionState().PeerCertificates) == 0` ALONE, NO client-auth term**. **+1 stat (1204 -> 1205) / +0 fixtures (119) / +0 fuzzers (55) / +0 BackendKinds (38) / +0 modules (2) / +0 PRODUCTION imports / ZERO new packages / ZERO new exported symbols / ZERO new dependency edges** — the last three asserted BY COMMAND, per-package. **ALL TEN BREAKS EXECUTED; every one matched its PLAN-recorded outcome.** **THE HEADLINE REPRODUCED AT THE IMPL TIP:** Break D (unconditional Inc) fired **`TestServeConnection_SSLHandshakeIncrements`** — a **PHASE-74** test — through `assertSSLCrossProduct`'s NEGATIVE half **while the new positive arm PASSED**, and **Break D' (the same edit with `sslLeafRoster` reverted to three leaves) left the ENTIRE package GREEN** ⇒ **the one-line roster extension is the SOLE guard on this row's central pinned decision**, with D' additionally showing that the two exact-set spelling pins stay green throughout because they pin REGISTRATION, never INCREMENT. **Breaks E and F fired the byte-identical assertion at the identical line** — distinguishable by the EDIT, not the OUTPUT, and **recorded as such rather than claimed as separately discriminated.** Break C fired **exactly one line** with the three build-time predicates **SILENT** (phase-74 V1-M2 reproduced). **Break G STAYED GREEN** — the silent-staleness finding — and G''s restore is what makes G's green mean *"ran and passed"* rather than *"never ran"*. **Break I-b PASSED VACUOUSLY**, which is what proves the ABSENT check NECESSARY rather than merely firing. Break J's rename went GREEN **with the driver's log lines VANISHING**; with `var _` kept it is a COMPILE ERROR. **THE CROSS-SIDE MEASUREMENT RE-EXECUTED LIVE:** `0110` scraped `ssl.handshake=2 ssl.no_certificate=1 ssl.fail_verify_error=1 ssl.fail_verify_no_cert=0 (downstream_cx_total=3)` on **BOTH** sides, NAMES byte-identical with the address differing ONLY in the label (`0.0.0.0_10446` vs **`___20016`** — the subject binds the **IPv6 wildcard**), and the asserter was observed firing **inside the full suite**, which a green fixture alone cannot distinguish from one that never dispatched. **`0111`'s named-subset check was EXECUTED for the FIRST time (the PLAN verified it only by code read): PASS**, driver byte-untouched. **SIX-GATE FULLY GREEN**; **FULL DIFFERENTIAL `ok 396.517s`, 119/119 PASS / 0 SKIP / 0 FAIL** with the fixture-directory set and the subtest set `comm -3`-verified EQUAL; **`-race` over `./...` INCLUDING `./test/differential/` — discharging the PLAN's own open item — reported ZERO data races**; sha256 BYTE-UNTOUCHED roster **592 files / 0 mismatches / 0 missing**, its intersection with the mechanically-derived **13-file** EDIT roster **EMPTY**. **TWO FLAKES, BOTH CLASSIFIED ON EVIDENCE, NEITHER ABSORBED:** `0083-grpc-access-log-headers` failed a first full run at `bind: address already in use` **before any assertion** (startup class; isolate-green, `internal/filter/**` sha256-untouched, immediate full re-run 119/119); and **`0061-lb-ring-hash`'s spread assertion fired under `-race` — its THIRD recorded occurrence** (`internal/cluster/**` sha256-verified byte-untouched; isolate-green **4/4** under `-race`) ⇒ structurally not this row, **but the margin fix is now genuinely OWED** as a Load-balancing-family maintenance item. ⚠️ **NEITHER was on the PLAN's roster of six known flakes — a stage brief's flake list is not the index.** **FOURTEEN PLAN/SPEC CLAIMS REFUTED BY EXECUTION, and TWO of them are the PLAN's own gate COMMANDS:** the **import gate FAILS CLOSED** (`impblock` prints `FILENAME`, so a `/tmp` baseline vs an `internal/` HEAD differs on EVERY line — 31 removed / 31 added, **exit 1, on a +0-import tree**; the PLAN's own negative-control transcript prints no path prefix, proving it validated a **different, basename-normalised** command) — **RD-IMPGATE existed to replace a gate that failed OPEN, and its replacement failed CLOSED** · **`go doc -all <pkgA> <pkgB>` FAILS OPEN** (the second argument is read as a SYMBOL; only `<pkgA>` is emitted, **exit 0, empty stderr**) — it would have gone green over `internal/stats` **without reading it**, in the task whose second production file lives there · the PLAN's `git stash` baseline is **VACUOUS** on a clean worktree (it stashes nothing, so the diff compares HEAD to HEAD) · the PLAN's EDIT roster names **4** test files while the real delta is **13** · **RD-0110-STALE's *"exactly THREE stale sites, no fourth"* is REFUTED** — a **FOURTH** sits at `0110/expectations.yaml`'s *"(no StatsAsserter)"* parenthetical, **falsified by this row's own T5**, and structurally invisible to a sweep that greps the STALE vocabulary · **T9 found a PLAN MISS its own roster could not see** — a comment saying *"keeping **both** Inc points"*, **already wrong before this row** (phase 74 landed three) and invisible to a *"three"*-shaped grep · **RD-BC-TOTALS' scratch figure contradicts the PLAN's own inline expectation** (`grep -c '1204'` goes 3 -> **2**, not 3 -> 1), and T8 Step 3's *"expect `1201` UNCHANGED"* is contradicted by T8 Step 2's own mandate to record the gap — **two numbers inside one PLAN disagreeing** · `ProbeAdmin` is **`:558`**, not the `:554` with which the PLAN corrected the cell's `:552` — **T5 grew the driver and re-staled the correction** · the row-75 cell carried **FIVE** stale claims, not four · `manager_test.go:4466`/`:4491-4492` **do not exist at this tip** (T3's variadic rewrite replaced both with roster-relative language correct at ANY family size) · `wantNames` is `:230-234` not `:231-235` · `0111/README.md`'s bullet is `:168-175` not `:167-174`, **one PLAN error propagated into TWO tasks** · and **anchor drift ran +1 to +168, NON-MONOTONIC**, so a single "+N" correction would have mis-targeted two of three regions. **ADR-0297 COMPLETED IN PLACE** (PROPOSED -> COMPLETE; §Decision + §Consequences after the RETAINED footer; tail stays ADR-0297, next-free ADR-0298) **and CORRECTED**: ¶7's **SELF-FALSIFYING grep** restated with **NO number** and the searched token deliberately **NOT spelled** (every restatement mints another counter-example — post-edit it occurs **exactly once** in the file), and ¶9's *"SELF vs OTHER-ADR"* rule replaced by **graft SCALE** after the **n=7** population was re-enumerated first-hand (`:17211` sits inside ADR-0294 and is a phase-**73** correction to a DIFFERENT landed ADR rendered **INLINE**). ⚠️ **¶9 ALSO carried *"the only two instances in the file"*, which THIS ROW'S OWN blockquote makes three** — so a pure append with ¶9 untouched would have shipped a **SECOND self-falsifying count in the very commit that fixed the first.** **The prescription has now outlived THREE wrong justifications: family, self-vs-other, scale.** **ADR-0296 §Decision (g) CORRECTED** via an INDENTED blockquote (`grep -c '^  > '` **2 -> 3**, leading bytes `od -c`-verified identical to both precedents), **LEADING WITH WHAT SURVIVES** — no such counter was owed *in phase 74*, whose sole stat-asserting fixture is `require=true` on both sides, **so the deferral was RIGHT and phase 74's row correctly scoped** — phrased as a documentary finding claiming **NO ORDINAL**. **BEHAVIOR_CONTRACT: eleven IN-PLACE rewrites + ONE ledger line** (`5744 -> 5746`, the only line-adding edit), every one of `:1849/:1851/:1853/:1855/:1857/:1859/:5002` **UNMOVED**; `:962` **`cmp`-verified BYTE-IDENTICAL** with its offsets re-derived (**627 CHARACTER / 630 BYTE**, 1002 chars / 1007 bytes) — **a FOURTH challenge survived**; the `:1849` heading **EXTENDED** on the `:785` precedent (**SPEC §9's *"no two-ADR heading precedent exists"* REFUTED — 16, now 17**); **`three-fifths` REMOVED rather than bumped** and restated as an ENUMERATION so no fourth wrong denominator was minted; **BOTH ledger gaps recorded, NEITHER back-filled.** **The ROADMAP narrow edits the RETIRED clause, not the candidates list** — check (2) **STAYS 3** by both forms, terminator still `force-trace.`, **interior periods 0 before and after**, verified in a `/tmp` scratch copy first and then on the real file. **Sentinel re-run MECHANICALLY TWICE (worktree AND landed master post-push): does NOT fire; `stop` NOT created** — (1) **SILENT**, as predicted at the PLAN and confirmed here (row 75 was the last non-`done` chartered row); (2) => **3**; (3) `NEVER OPENED: gRPC/Runtime/WASM`. ⚠️ **check (1)'s blind spot RE-DERIVED INDEPENDENTLY: 107 data rows (`:31`-`:137`) / 103 matched / FOUR misses**, all four `done`. **Next -> the phase-76 BRAINSTORM** (the roller SELF-PICKS; `fault.abort.grpc_status` is the recommended opening — the only identified candidate that clears a sentinel check-(3) blocker; ONE probe owed).

- **prior active-phase:** `phase 75 (tls-no-certificate-stat) PLAN done` (2026-07-25) — phase-75 PLAN done. Lifecycle-state **2 -> 3**; **ROW 75 STAYS `in-progress`**; **ROADMAP and DECISIONS BYTE-UNTOUCHED**. Fresh worktree `/home/esa/git/envoy-go-wt-p75plan` off master `cedd2f27`, branch `phase-75-plan`. Docs-only: **ZERO production `.go`**, **EXACTLY TWO files in the phase-directory delta** — the new `PLAN.md` (an **11-task TDD spine T1-T11** with a **TEN-break roster**) and `PROGRESS.md`. **SEVEN agents at tip `cedd2f27` on disjoint remits — FOUR read-only re-derivation agents and THREE BUILD-BY-EXECUTION agents in SEPARATE git worktrees** (`phase-75-plan-v1/v2/v3`, private scratch, local commits only, never pushed), plus controller re-verification of every load-bearing correction. ⚠️ **THIS PLAN EXECUTED MOST OF THE ROW: SPEC §16's closing *"NO BUILD OF ROW 75 EXISTS"* is no longer true**, and **EIGHT of the TEN breaks RAN here.** **THE HEADLINE — THE SPEC AND THE ROUTER ARE BOTH WRONG ABOUT WHERE THIS ROW'S DISCRIMINATING BREAK LIVES.** Both state the new positive unit arm *IS* the discriminating break for the pinned `len(PeerCertificates)==0` predicate. **Break D fires the PHASE-74 mTLS arm instead** — `ssl.no_certificate = 1, want 0 — only [handshake] may move on this arm`, via `assertSSLCrossProduct`'s NEGATIVE half — **while the positive arm PASSES**, because an unconditional Inc still leaves the counter at 1 on the arm that wants 1. **Break D′, the decisive stacked control** (same edit, `sslLeafRoster` reverted to three leaves) leaves the **ENTIRE package GREEN, `ok … 3.233s`** ⇒ **the one-line roster extension is the SOLE guard on the row's central pinned decision.** Controller-reproduced end-to-end. ⇒ positive arm catches *never-Inc'd* and *mode-gated*; roster extension catches *unconditional*; **the row needs BOTH.** **THE SECOND HEADLINE — THE FIRST SUBJECT-SIDE MEASUREMENT, AND EXACT CROSS-SIDE AGREEMENT:** `0110` gained a cross-side `StatsAsserter` and RAN against the live reference — `ssl.handshake=2 ssl.no_certificate=1 ssl.fail_verify_error=1 ssl.fail_verify_no_cert=0 (downstream_cx_total=3)` on **BOTH** sides, metric NAMES byte-identical with the address differing ONLY in the label (`0.0.0.0_10446` vs `___20016` — ⚠️ the subject binds the **IPv6 wildcard**, caught by execution), and the **FULL differential suite `ok 399.675s`, exit 0, no flake**. **FOURTEEN SPEC/router claims REFUTED**, incl.: the fast-failure `ssl.*` suppression is **REFERENCE-ONLY** (envoy-go accounts `ssl.*` at step 6, before the step-7 upstream dial) · the **ABSENT check does not guard what SPEC §8.2 says** (deleting an Inc'd counter's registration SIGSEGVs the subject and the run dies at `structuralCheck` before `AssertStats`) · **the tree goes RED at the production change** via two `reflect.DeepEqual` exact-set pins, so T1 orders the guard FIRST · **a stale `-run` on a UNIT test prints `ok … [no tests to run]` and EXITS 0** (a NEW SHAPE of `reference_differential_run_selector`) · the registration-delete break **destroys its own evidence** full-package · `registerListenerMetrics`' doc starts at **`:351`** not `:358` · the SPEC's three `assertSSLCrossProduct` "callers" are **test-func DECLARATION lines** · `t.Error` not `t.Errorf` · `mkDownstreamTSInline` takes **`string`** so the SPEC's call **does not compile** · **SPEC §9's *"no two-ADR heading precedent exists"* is REFUTED — there are 16** · **check (1)'s blind spot is 107/103/4, not 106/102/4**, and its provenance claim is false · *"three-fifths"* was **already wrong at the phase-74 tip** so it is REMOVED not bumped · the ledger is discontinuous in **TWO** places · `registry.go` has **FIVE** panic sites, not three (**a correction of this PLAN's own agent**) · and **ADR-0297's OWN ¶7 and ¶9 are DEFECTIVE** — ¶7's grep claim is **SELF-FALSIFYING**, **reproducing one ADR later the exact species the phase-74 IMPL had just fixed in ADR-0296 ¶3**, and ¶9's *"SELF vs OTHER-ADR (n=4)"* rule is refuted by an **n=7** population where `:17211` is a phase-73 correction to a DIFFERENT landed ADR rendered INLINE; the surviving discriminator is **graft SCALE** and the indented prescription has now outlived **THREE** wrong justifications. **A CONTROLLER DECISION THAT REVERSED ITSELF, RECORDED NOT QUIETLY AMENDED:** the `0111` question was decided ADD-to-rosters, then reversed to prose-only once two later findings landed — **only `0110` of the NINE TLS-downstream fixtures is `require=false`**, so a `0111` value pin would document a vacuous `0 == 0`. **Counts UNCHANGED:** fixtures **119** / fuzzers **55** / stat surface **1204** / BackendKind **38** / modules **2** / DECISIONS tail **ADR-0297** PROPOSED. **Sentinel re-run MECHANICALLY TWICE (worktree AND landed master post-push; does NOT fire; `stop` NOT created):** (1) `NOT DONE: row 75` · (2) => **3** · (3) `NEVER OPENED: gRPC/Runtime/WASM`. `next-prompt.txt` rolled to the phase-75 IMPL (**TRACKED despite `.gitignore`**).

- **prior active-phase:** `phase 75 (tls-no-certificate-stat) SPEC done` (2026-07-25) — phase-75 SPEC done. Lifecycle-state **1 -> 2**; **ROW 75 STAYS `in-progress`** and the ROADMAP is **BYTE-UNTOUCHED**. Fresh worktree `.worktrees/phase-75-spec` off master `e822f1ad`, branch `phase-75-spec`. Docs-only: **ZERO production `.go`**, **EXACTLY TWO files in the delta** — the new `SPEC.md` and the `DECISIONS.md` ADR-0297 §Context append (a PURE APPEND, **30 insertions / 0 deletions**, verified by `git diff --numstat`; `ADR-0286` and `ADR-0296` BYTE-UNTOUCHED). **FIVE agents at tip `e822f1ad` on disjoint remits — TWO live-reference probe fleets** (disjoint port ranges 10600-10639 / 10640-10679, container prefixes `p75s-`/`p75f-`, PRIVATE scratch per `reference_parallel_subagents_private_scratch`) **and THREE read-only re-derivation agents** pinned to the stage worktree. **THE HEADLINE — THE ONE DOCKET ITEM THE BRAINSTORM CALLED *"GENUINELY OPEN"* IS RESOLVED AT THE WIRE.** D-TLSNC-SEMANTICS is **(a) UNCONDITIONAL**: a `DownstreamTlsContext` carrying ONLY `tls_certificates` — no `validation_context`, no `require_client_certificate` — driven by a no-cert client yields `ssl.handshake: 1` **AND `ssl.no_certificate: 1`**, reproduced in TWO fresh containers, and an `openssl s_client -msg` trace shows the server's ENTIRE flight with **ZERO** `CertificateRequest`: **the client was never asked, and the counter fired anyway.** Confirmed on an HCM `direct_response` stack with `clusters: []` (so `tcp_proxy` is not implicated), at TLS 1.2 AND 1.3, and — **without coordination** — by the SECOND fleet, whose control listener used the identical shape (`grep -c 'validation_context'` over both its bootstraps ⇒ **0**, controller-verified). ⇒ **the Inc gate is `len(tlsConn.ConnectionState().PeerCertificates) == 0` ALONE.** **THE REST OF THE DOCKET, ALL BY EXECUTION:** FAILPATH — `no_certificate` read **0** across SIX failure arms, and the sharpest drove a SUCCESS first on the SAME scope (moving `ssl.handshake` 0->1, proving the counters are wired there) then the failure, so *"an all-zero block proves nothing"* was addressed THREE independent ways; it also sharpens the semantics to *"**COMPLETED** handshake AND no peer cert"*. CERTPRESENT — **0**, corroborated by `ssl.sigalgs.*`, which appears ONLY when a cert was presented. SCOPE — **ABSENT**, not present-and-zero, on a plaintext scope, verified **PER NAME** by a two-listener single-process discriminator, and meaningful only because the reference registers these FIXED names **EAGERLY at listener-config time** (the DYNAMIC families behave oppositely). FIXTURE — Shape A SOUND on `0110` (five independent lines of evidence) and the predicted numbers **CONFIRMED reference-side by replaying `0110`'s exact three-arm structure**: `handshake=2` / **`no_certificate=1`** / `fail_verify_error=1` / `fail_verify_no_cert=0` / `downstream_cx_total=3`. ⚠️ **Arms 1 and 3 DISCRIMINATE — `no_certificate` is 1, NOT 2.** **THE BRAINSTORM'S DEAD-UPSTREAM HAZARD IS CHARACTERISED, AND IT IS NOT WHAT IT LOOKED LIKE: it is NOT deadness, it is FAST FAILURE.** `tcp_proxy` dials the upstream at ACCEPT time, before any TLS byte (a bare zero-byte connection still moves `upstream_cx_total`), so the connect and the handshake run CONCURRENTLY; an instantly-refused connect tears the downstream connection down MID-handshake and the whole family stays silent, while a **BLACKHOLE** upstream (`192.0.2.1:81`, `connect_timeout: 10s`) — equally dead but SLOW — loses the race and fires everything normally. ⚠️ **THE CONTROLLER CAUGHT A PROBE FLEET FAILING THE ROW'S OWN DISCIPLINE:** the fixture fleet reported a DETERMINISTIC SEQUENCING mechanism from four arms at `127.0.0.1:1`, all instant-refusal, all consistent with BOTH readings — `grep -rl '192.0.2\|blackhole\|connect_timeout: 10'` over its scratch returns **NOTHING**. `reference_probe_must_discriminate`, applied to a fleet's own experimental design rather than to a document. ⚠️ **COROLLARY: `downstream_cx_total > 0` is NOT a sufficient decode-ran guard for an `ssl.*` assertion** — it increments even when the family is wholly suppressed. `0110` is **NOT BLOCKING** (live in-process `TCPEcho` on both sides, structurally mandatory for green via `wantObservable`). **FOUR PRIOR CLAIMS REFUTED BY RE-DERIVATION:** ⚠️ **the correction-FORM discriminator is NOT the ADR FAMILY** — `DECISIONS.md:16901` is ADR-0289 (FIFTH xDS row) correcting ADR-0286 (THIRD xDS row), **same family, INDENTED form**; the derivable rule is **SELF vs OTHER-ADR** (indented for another ADR, n=2 at `:16901`/`:16910`; inline for an ADR correcting ITSELF at its own IMPL, n=2 both on `:17213`) — the prescribed form SURVIVES on a different rule · **`:17209` is NOT a correction at all** (it is ADR-0294's `Documented boundaries` bullet) · **`BEHAVIOR_CONTRACT.md:831`/`:847` are NOT the `### Stat surface` ledger** (heading `:4950`, entries `:4954-5002`); they are the two narrative bare-total restatements, and **BOTH sets are load-bearing** — phase 75 appends after `:5002` AND bumps `:831`/`:847` 1204 -> 1205 · the ledger arrow is **`->` U+2192** with the bold running THROUGH the parenthetical to the colon (⚠️ the form is NOT uniform in the file; match the TAIL entry) · the `0110` arm cite `:362-373` points at a **doc comment** (real arms `:400-435`), `ProbeAdmin` is `:554`, the README bullet `:160-163`. **THREE GUARDS THE BRAINSTORM'S ROSTER MISSED** — `assertSSLCrossProduct` (`manager_test.go:4474-4488`, a hard-coded 3-leaf NEGATIVE roster consumed by all three increment tests), `TestServeConnection_PlaintextListenerIncrementsNoSSL` (`:4568`), and `0111`'s cross-side `AssertStats` (THREE hard-coded rosters). ⚠️ **And every landed increment test asserts the counter does NOT move; NONE would assert that it DOES** — a registered-but-never-Inc'd field passes every unit guard, so the row owes a POSITIVE arm (SPEC §10.1). **ADR-0044's misattribution is now MEASURED: cited 532 times across 504 lines, 134 as *"ADR-0044 ADR-on-impl convention"*, with ZERO supporting tokens in its own block** (`:1419-1462`, titled *"BEHAVIOR_CONTRACT HTTP/1.1 subsection"*) — a convention with NO written ADR, stated most fully at `:8534`, a reference NOTE inside a phase-18 ADR. **`ADR-0044-as-used` is the house form.** **A FOURTH stale-confession site found OUTSIDE `0110`:** `0108-xds-sds-validation-context/expectations.yaml:104-106` (prose only, no assertion) — RECORDED, NOT chartered. **The phantom `B5` is traced to its ORIGIN:** a phase-74 PLAN edit-bundle label (`SPEC.md:304`, `PLAN.md:133`/`:896`) pre-authored VERBATIM into the two production comments by `PLAN.md:325`/`:684` and propagated onward into `DECISIONS.md`/`STATE.md`. ⚠️ **A CORRECTION TO THIS SESSION'S OWN REASONING, recorded not quietly amended:** the controller re-derived the contested `BEHAVIOR_CONTRACT.md:962` offset with `grep -bo`, read **630**, and called the BRAINSTORM's **627** *"off by 3"*. **That was a UNIT error** — 627 is the 0-based CHARACTER index, 630 the 0-based BYTE offset, and the line has multibyte chars. **The BRAINSTORM's figure HOLDS**, and this lineage has now produced TWO failed corrections against that single anchor. ⚠️ **NO BUILD OF ROW 75 EXISTS, in-tree or in scratch** — field, registration, predicate, guards, the new helper and the `0110` asserter are ALL specified and NONE compiled; every §3 figure is a **reference-side** measurement and **the subject side has never emitted this counter**. ⚠️ **The absolute total 1204 was NOT re-derived mechanically** (no such command exists; ADR-0296's own ledger asks a future phase to COUNT). **Counts UNCHANGED (docs-only; re-run MECHANICALLY in the worktree at this close):** fixtures **119** / fuzzers **55** / stat surface **1204** / BackendKind **38** / modules **2**; `go build ./...` and `go vet` clean; `0110` and `0111` both PASS at `-count=1`. **Sentinel re-run MECHANICALLY TWICE (worktree AND landed master post-push; does NOT fire; `stop` NOT created):** (1) `NOT DONE: row 75` · (2) => **3** · (3) `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`. `next-prompt.txt` rolled to the phase-75 PLAN (**TRACKED despite `.gitignore`**; edited in this worktree).

- **prior active-phase:** `phase 75 (tls-no-certificate-stat) BRAINSTORM done` (2026-07-24) — phase-75 BRAINSTORM done. Lifecycle-state **DONE -> 1**; **ROW 75 REGISTERED `in-progress`**, RE-OPENING sentinel check (1). Fresh worktree `.worktrees/phase-75-brainstorm` off master `f8f6cd44`, branch `phase-75-brainstorm`. Docs-only: **ZERO production `.go`**, exactly THREE files edited (`ROADMAP.md`, `STATE.md`, `next-prompt.txt`) plus the new `BRAINSTORM.md`; **DECISIONS.md BYTE-UNTOUCHED**. **FIVE read-only re-costing agents at tip `f8f6cd44` on disjoint remits** — two running LIVE reference containers, three running Go-toolchain probes. **THE HEADLINE — THE ADR THAT DEFERRED THIS ROW DEFERRED IT ON A CLAIM THAT WAS FALSE WHEN WRITTEN, AND ITS SUPPORTING CITATION POINTS AT NOTHING.** ADR-0296 §Decision (g) (`DECISIONS.md:17308`, landed ONE DAY earlier) says *"envoy-go does not implement `require_client_certificate: false` (verify-if-present) at all — §Context ¶7 / `VERIFYIFGIVEN` is explicitly OUT OF SCOPE"*. **Both halves fail mechanically.** Phase 67 (**row 67 `done`**) landed it across ALL THREE validation-source shapes (`internal/tls/config.go:79-84`, documented at `:60-68`), with fixture `0110` driving it since phase 67; and `grep -n 'VERIFYIFGIVEN' DECISIONS.md` returns **exactly ONE hit — `:17308`, the citing sentence ITSELF** — while §Context ¶7 (`:17274`) is about the four-family split and contains **ZERO** `require_client_certificate`. ⚠️ **THIRD internal mis-pointer in ADR-0296**, and §Decision (g) OPENS by noting SPEC §13.1's hygiene claim was false for exactly this item, then reproduces the defect one level down. The narrower claim (not owed *in phase 74*) SURVIVES; **the reason given does not.** **SECOND REASON THE PICK IS RIGHT:** `0110/README.md:160-161` still confesses *"No `ssl.*` stats — envoy-go emits none"*, **FALSE since the phase-74 IMPL** — the same stale confession phase 74 retired in `0111`. ⚠️ The bullet **BUNDLES** a retirable half with a STILL-LIVE `/listeners` guard, so the edit **SPLITS** it. **TWO RIVALS MATERIALLY CHANGED BY RE-DERIVATION: (1)** `ssl.connection_error`'s *"membership is UNENUMERATED"* blocker is **REFUTED BY EXECUTION** (18 Go arms + 16 LIVE reference arms, every round control-validated): the reference books it for every **decodable but rejected** handshake and books **NOTHING** when the peer left without a complete ClientHello. But the finding **RAISED** the cost — a naive catch-all OVER-COUNTS the EOF class (`io.EOF`/`io.ErrUnexpectedEOF`), the highest-frequency class in production, so a PREDICATE is required and `manager_test.go:4294`'s landed `{"io.EOF", io.EOF, outcomeOther}` row **IS** that over-count — and it **cannot ride `0111`**, forcing a NEW fixture. Deferred on COST (~9-11), blocker RETIRED, boundary written down. **(2)** The phase-74 RUNNER-UP `upstream_cluster` span tag is **NO LONGER BLOCKED** — a **route-resolved** name is in scope at 4 of the 5 zero-endpoint sites (all five anchors EXACT; 6/5/7 partition CONFIRMED), and SELECTION-gating is precisely what makes it correct. ⚠️ **The DISCRIMINATING break phase 74 handed forward DOES NOT EXIST** — `extractEndpoints` (`internal/cluster/manager.go:875`) makes **ZERO** `GetClusterName()` calls; the name is a PARAMETER used only in five `fmt.Errorf` messages, so the break would have been **VACUOUS**. **RECOMMENDED NEXT OPENING: `fault.abort.grpc_status`** (7-9 tasks) — the ONLY identified candidate that clears a sentinel check-(3) blocker; escapes the ADR-0058 trailer gap because a gRPC error local reply is Trailers-Only/headers-only, which envoy-go already emits (`extproc/check.go:518-530`). ONE probe owed. **FURTHER CORRECTIONS, all controller-verified:** `registry.go:107` is the DUPLICATE panic, the INVALID-NAME panic is **`:117`** · **there is NO `B5` step in BEHAVIOR_CONTRACT** (all eight hits are `AMEND-B5`/Wasm; the departure is at `:1855`), yet *"BEHAVIOR_CONTRACT B5"* **leaked into TWO LANDED PRODUCTION CODE COMMENTS** (`internal/listener/manager.go:392`, `:1265`) plus `DECISIONS.md:17304`/`:17316` (⚠️ **`:17314` at the time; SHIFTED to `:17316` by the phase-75 IMPL's ADR-0296 blockquote insert**) and `STATE.md` — PLAN-internal numbering became a permanent cross-reference pointing at nothing · **the FOUR-family deferral is WRONG IN BOTH DIRECTIONS**: `ssl.versions.*` has **NEITHER** blocker (Envoy's `TLSv1.2` passes `NamePattern`; the trip is in **Go's** `VersionName` = `"TLS 1.2"`, failing on a **SPACE** not a hyphen) while `ssl.sigalgs.*` is **STRUCTURALLY UNIMPLEMENTABLE** (`testingOnlyPeerSignatureAlgorithm` is UNEXPORTED), and `ConnectionState.CurveID` **IS** exported at go1.26.5 · **next-free reference port is `10450`, NOT `10118`** (max allocated **10449**; ports are NOT fixture-index aligned) · a **PRE-EXISTING H2 ROUTING BUG PROVEN BY EXECUTION** (`internal/filter/hcm/h2/stream.go:440` `url.Parse` truncates `u.Path`; `"//foo/bar"` routes as `/bar` via `route.go:128`) · `layered_runtime` is a hand-written **REJECT** (`bootstrap.go:568-569`), not a silent ignore. ⚠️ **ONE CONTESTED CLAIM RESOLVED AGAINST AN AGENT:** an agent reported ADR-0296's `BEHAVIOR_CONTRACT.md:962` anchor as FALSE; **that refutation is itself WRONG** — the token sits at character offset **627** of line 962, whole-file `grep -c` returns **1**, and a second agent quoted the same text. **The `:962` anchor STANDS.** Recorded because **a drift CORRECTION is itself a claim** — and this one failed. ⚠️ **NO probe was run against the reference for THIS ROW'S OWN SUBJECT and NO build of row 75 exists** — the `ssl.no_certificate` semantics are DOC-SOURCED, not re-executed (BRAINSTORM §11.1 says so explicitly). **ROADMAP edits proven MECHANICALLY: exactly 2 insertions / 1 deletion**; the family-paragraph edit is a **PURE APPEND** (old is a byte-prefix of new, **+1255** chars) and the deferred sentence is **BYTE-IDENTICAL** across the diff, still terminating at `force-trace.` so the `[^.]*\.` regex still binds. **NO deferred sentence narrowed** (phase-57 precedent); the narrow lands at the IMPL only. **Counts UNCHANGED (docs-only; re-run MECHANICALLY in the worktree at this close):** fixtures **119** · fuzzers **55** · stat surface **1204** · BackendKind **38** · modules **2** · DECISIONS tail **ADR-0296** COMPLETE (next-free **ADR-0297**). **Sentinel re-run MECHANICALLY TWICE (worktree AND landed master post-push; does NOT fire; `stop` NOT created):** (1) prints **`NOT DONE: row 75`** — the registration RE-OPENS check (1) after its first-ever silent close; (2) => **3**; (3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`. ⚠️ **check (1)'s blind spot RE-DERIVED, not copied: 106 data rows, 102 matched, FOUR misses** — `00` (em-dash "after" column), `04` (DOT in the slug `http-1.1`), `28.1a` and `28.1b` (LETTER suffix). All four are `done`. `next-prompt.txt` rolled to the phase-75 SPEC (**TRACKED despite `.gitignore`**; edited in this worktree).

- **prior active-phase:** `phase 74 (tls-handshake-outcome-stats) IMPL done` (2026-07-24) — lifecycle-state 3 -> **DONE**; **ROW 74 FLIPS `in-progress` -> `done`** at the T8/T9 six-gate (ADR-0106, the SOLE leg — a SINGLE FLAT ROW, `reference_roadmap_split_phase_row_done`). Fresh worktree `.worktrees/phase-74-impl` off master `f1221a4a` (the phase-74 PLAN squash), branch `phase-74-impl`; **NINE subagent-driven tasks -> nine local commits -> ONE squash**, per `feedback_execution_style` / `feedback_subagents_no_push` / `feedback_push_to_origin`. **THE ROW DELIVERED:** `listener.<normalized-addr>.ssl.handshake` / `.ssl.fail_verify_error` / `.ssl.fail_verify_no_cert`, registered inside `registerListenerMetrics` **gated on `rt.tlsMode` ALONE — NO kind check** (this row is that field's FIRST production consumer), and Inc'd at the two points inside `serveConnection`'s EXISTING `if selected.tlsCfg != nil` block, with **NO nil guards** — because the three pointers are non-nil exactly when `rt.tlsMode`, and the two predicates are EQUIVALENT by the mixed-TLS/plaintext-chain reject. **+3 stats (1201 -> 1204, the FIRST non-zero stat delta in this lineage) / +0 fixtures (119) / +0 fuzzers (55) / +0 BackendKinds (38) / +0 modules (2) / +0 PRODUCTION imports / ZERO new packages / ZERO new exported symbols** — the last two asserted BY COMMAND (an extracted import-block diff; a `go doc -all` set-diff, both IDENTICAL), not by inspection. **THE HEADLINE — THE ROW'S CENTRAL CROSS-SIDE CLAIM IS NOW EXECUTED, NOT DERIVED.** Fixture `0111-tls-cvc-empty-dynamic-fallback` gained the `fixture.StatsAsserter` its **own README confessed was *infeasible***, and **the REFERENCE emitted `handshake=1 fail_verify_error=1 fail_verify_no_cert=1` with `downstream_cx_total=3` — matching the subject EXACTLY.** The sanctioned subject-only fallback was NOT needed and was never taken. Shape A over `/stats/prometheus`, keyed on the metric NAME with `envoy_listener_address` IGNORED, the ABSENT check SEPARATE from the value check with a `continue` (an unregistered counter would read `0 == 0` and pass VACUOUSLY). **ALL TWELVE BREAKS RAN, EVERY ONE WITH ITS PREDICTED OUTCOME.** **Break F — the one declared *must not fire* — did NOT fire**, and stayed green even with the const mutated, which is the demonstration that a hand-written string is self-consistent and therefore worthless; **Break F'** (the same mutation against the LIVE construction) fired exactly the two live rows, completing the cross-product. **Break D — the row's DISTINGUISHING break, ADDING the refuted `rt.kind != kindQUIC` gate** — fired, and **the FIRST and discriminating assertion was (1), the `reflect.DeepEqual` NAME SET**, not (4)'s zero check; the `driveH3` precondition SUCCEEDED, so the break did not fire by killing the drive. **Break E reproduced V1-M2 EXACTLY**: `TestListenerMetrics_GateMatchesInc` fired **exclusively** through its three counter-POINTER assertions while the `tlsMode`/`tlsCfg` build-time predicates printed NOTHING — the pointer half is load-bearing and the predicate-only version would have stayed green. **Break E'** demonstrated the counterfactual live: a misspelled name still yields **length 3**, so the landed `statssink` `countMetrics` cardinality precedent **would have been GREEN**. **Break B** fired in EXACTLY TWO of three with **both halves of each cross-product** firing. **Break C confirmed F3 verbatim** — a nil `*stats.Counter`.Inc is a **PROCESS CRASH** in the `serveConnection` goroutine, stack `stats.(*Counter).Inc` (`counter.go:22`) under `listener.(*listenerRuntime).serveConnection`, `created by ...acceptLoop`, with no `recover()` anywhere in `internal/listener/`; **C' was NOT needed** — the panic frame IS the Inc site. **Break H proved the `0111` RD3 forced-send disclaimer INVERTS at the counter layer** (subject `fail_verify_error` -> 0, `fail_verify_no_cert` -> 2, while `CompareBytes` stayed GREEN), which is why the prose revision was owed rather than optional. ⚠️ **ONE SUBSTITUTION, and its rationale is itself a claim: the PLAN's literal Break G' was VACUOUS** — renaming the method while leaving `want` at the PASSING value makes green consistent with BOTH *"never ran"* and *"ran and passed"*; **the PLAN's own parenthetical for G' applies recursively to G' itself.** Re-run STACKED ON Break G (a FAILING assertion) it discriminates cleanly: broken-and-renamed => **GREEN, the finding** (the driver's log lines vanished entirely — `AssertStats` silently never ran); broken-and-named => RED. ⇒ **`var _ fixture.StatsAsserter` is a TRIPWIRE, not the dispatch mechanism.** **SIX-GATE FULLY GREEN**: `gofmt -l .` silent · `go vet ./...` · `go build ./...` · `go mod tidy -diff` EMPTY · `git diff master -- go.mod go.sum` EMPTY · `golangci-lint run ./...` · **the FULL differential `ok 402.512s`, 119 subtests / 119 PASS / 0 SKIP / 0 FAIL**, with the fixture-directory set and the subtest set `comm -3`-verified EQUAL. `go test ./... -race` green; `go list -deps ./internal/listener` **439 packages both sides, diff IDENTICAL — no new edge**; the BYTE-UNTOUCHED roster **569 files, 0 mismatches**, its intersection with the EDIT roster **EMPTY**. **ELEVEN PLAN/SPEC CLAIMS REFUTED BY EXECUTION** (full ledger in `PROGRESS.md` §IMPL record). The three that matter: **(1)** the PLAN's own check-(1) BLIND-SPOT figure was **STALE** — it recorded *"104 rows, 102 matched, TWO misses"*; re-derived at the IMPL tip it is **106 rows, 102 matched, FOUR misses**, the PLAN having dropped `28.1a`/`28.1b` **which the phase-73 close already knew about** — the exact failure the "RE-DERIVE, never copy" instruction on that figure exists to prevent. **(2)** The PLAN's own **"+0 production imports" gate COMMAND is unreliable**: it returns **3 false-positive hits** (T5's `helpText` map literals) and exits 0, so anyone gating on the exit code reads a PASS as a FAIL; the decisive checks are the hunk headers and a direct extracted-import-block diff. **(3)** T2's predicted red was **UNREACHABLE IN ONE STEP** — the adversarial pass's own pointer assertions reference fields Step 2 creates, so the binary fails to COMPILE and no assertion runs; a **two-stage red** was executed rather than recording a build failure as if it were the assertion red. Also found: a **FIFTH** stale claim in the row-74 ROADMAP cell (a whole-file grep now returning 3, all three hits being sentences refuting the phrase), a **THIRD** stale comment in the PRODUCTION file (a dangling `AllocatesTwoMetricsPerListener` cross-reference the rename would have orphaned), a **FOURTH** RD3 site (`0111/driver.go:416-420`), and ~180 lines of intra-row anchor drift (⚠️ **the T2/T4 commit messages carry the PLAN's STALE `:516-525`/`:639`/`:1178` anchors verbatim, as instructed — recorded, not rewritten**). **TWO FRESH HAZARDS**: `io.Copy(conn, conn)` is unusable as an in-test echo backend (`*net.TCPConn` implements `io.ReaderFrom`, so the socket splices into itself and the failure surfaces **at the BACKEND**, reading as a proxy bug — proven with a direct-to-backend probe before blaming the proxy); and T1's PKI presents a **LEAF-ONLY** client chain **deliberately** (appending the issuing CA, as the `0111` driver does, would make `len(UnverifiedCertificates)==2` and fire T1's own `want 1`). **ONE `-race` failure, classified PRE-EXISTING ON EVIDENCE RATHER THAN ASSERTION**: `internal/boot TestSDSEndToEnd_FetchFailure_BootFailsClosed` — isolate-green 3/3, file byte-identical to master, and **REPRODUCED ON MASTER 3/20 with the byte-identical error string** under 48 CPU burners on 32 cores. ⚠️ It **WIDENS** `reference_sds_init_fetch_timeout_dial_budget_flake` rather than matching it: a DIFFERENT test in a DIFFERENT package sharing the same 200 ms-budget mechanism, now with the reproducible recipe the original entry explicitly lacked. **ADR-0296 COMPLETED IN PLACE** (PROPOSED -> COMPLETE; §Decision + §Consequences after the RETAINED footer, mirroring ADR-0295; `### Decision` 0 before / 1 after; tail stays ADR-0296, next-free ADR-0297) with its TWO internal defects fixed — the ¶6 -> ¶4(i) mis-pointer and **¶3's SELF-REFUTING grep**, a count of 0 that the SPEC's own append had made 1, the sole hit being that very paragraph. **ADR-0286 §Consequences C3 CORRECTED** via an INDENTED blockquote, `cat -A`-verified byte-identical in indentation to the `:16901` precedent, phrased as a **documentary finding, NOT** as criticism of phase 65 — recording that *"a framework-surgery row of its own"* is REFUTED (an inline add to ONE function gated on a field that already existed), that the *"opaque handshake-error callback"* half **was never in C3 at all**, and closing with **what STILL STANDS**: a **FOUR**-family `ciphers`/`versions`/`curves`/`sigalgs` boundary on its own two blockers, with **LIFECYCLE EXPLICITLY NOT one of them**. **The ROADMAP narrow REPLACED THE PROSE, not just three names** (`:204`, not `:202`): *"envoy-go emits ZERO such stats"* had gone FALSE and *"a framework-surgery row, NOT an inline add"* was the very adjective this row refuted; `curves`/`sigalgs` ADDED; **check (2) STAYS 3** and the sentence still terminates at `force-trace.` so the `[^.]*\.` regex still binds. **Sentinel re-run MECHANICALLY TWICE (worktree AND landed master post-push): does NOT fire; `stop` NOT created** — (1) **SILENT**, as predicted at the PLAN and confirmed here (row 74 was the last non-`done` chartered row); (2) => **3**; (3) `NEVER OPENED: gRPC/Runtime/WASM`. **Next -> the phase-75 BRAINSTORM** (the roller SELF-PICKS; `ssl.connection_error` is the cheapest follow-on this row NAMED, but its membership is UNENUMERATED and that cost must be RE-DERIVED, not re-read).



## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
