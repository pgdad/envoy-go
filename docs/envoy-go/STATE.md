# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `24.2-global-ratelimit-perroute-and-headers` — **24.1 IMPL DONE 2026-05-23 (this commit); awaiting the 24.2 PLAN** (next session, skill `superpowers:writing-plans` to author `docs/envoy-go/phases/24.2-global-ratelimit-perroute-and-headers/PLAN.md` from the existing 24.2 SPEC.md). Phase 24.1 (the FIRST sub-phase of the phase-24 PLAN-time ADR-0045 split per ADR-0201) shipped the **core decision path** of `envoy.filters.http.ratelimit`: the NEW `internal/filter/http/ratelimit/` package (`TypeURL` + `New` + boot-registration alphabetical oauth2↔rbac, **19 HTTP filters wired**) + `compiledConfig`/`buildCompiledConfig` (AMEND-3 13-field roster + defaults/clamps) + **DELTA 1** `internal/grpcclient/ratelimit_client.go` `RateLimitClient` (THIRD ADR-0158 unary `AuthClient`-clone wrapper) + the descriptor engine for the **5 CORE actions** (`generic_key`, `request_headers`, `remote_address`, `destination_cluster`, `header_value_match`) + empty-action-drop discipline + `ShouldRateLimit` async dispatch (`StopIteration` + OnDestroy-cancel) + OK/OVER_LIMIT/error dispositions (429/AMEND-8 header order/`request_rate_limited` rc-details ABSENT-BY-API/`x-envoy-ratelimited`/gRPC 8 vs 14 / 500 fail-closed nullptr-mutate + `rate_limiter_error` rc-details ABSENT-BY-API / fail-open default) + the **cluster-scoped cross-namespace 4-counter stat surface** `cluster.<rls>.ratelimit[.<stat_prefix>].{ok,error,over_limit,failure_mode_allowed}` (110 → 114 — FIRST cluster-scoped filter-stats surface; LANDS the AMEND-10 pattern that ext_authz's `charge_cluster_response_stats` DEFERRED) + **DELTA 2** the HCM route-table `rate_limits` exposure (parse/retain/seed + the `RouteRateLimits()`/`VirtualHostRateLimits()` `DecoderFilterCallbacks` accessor pair via chain set-once-by-dispatch / read-via-accessor mirroring ADR-0165; HIGHEST-risk surface per parent §12 item 1) + the FULL §5 PARSE-REJECT roster (RATIFIED-from-PGV/config arms + the 3 envoy-go-strict departures `disable_key`/`extension`/`dynamic_metadata`; departures 15 → 18) + the 33rd fuzzer `FuzzRateLimitConfigParse` + the cross-side differential `0032-http-ratelimit`(a/b/c/d-core/e/h) + the boot-reject `0033-http-ratelimit-boot-reject` (`domain`-empty); **fixtures 33 → 35**. 3 ADR landings (ADR-0197[core] §Decision + §Consequences 24.1 slice @ Task 7; ADR-0198 FULL @ Task 5; ADR-0200 FULL @ Task 3). **D-RL1 byte-confirmation outcome: RAW-PROTO SEED CONFIRMED @ Task 5** — raw `[]*routev3.RateLimit` shape fit ADR-0165's set-once primitive without divergence. **ADR-0202 UNCONSUMED at 24.1 phase-done** — D-hypothesis HELD; next-free ADR stays at ADR-0202. ONE Task-7 follow-up CRITICAL fix (`ContinueDecoding`-after-`SendLocalReply` wake-up on OVER_LIMIT + fail-closed paths) + ONE Task-10 doc-fix follow-up (stale burst/restart references). All 6 phase-done gates GREEN at this Task 12 commit. The parent row `24` STAYS `in-progress` (closes at 24.2 phase-done). The **24.2** sub-phase row STAYS `planned`; its SPEC is already authored at `docs/envoy-go/phases/24.2-global-ratelimit-perroute-and-headers/SPEC.md` (anchored at the parent SPEC commit). Phase 23 (`admission_control`) DONE and CANONICAL — DO NOT reopen. **24.2 scope:** the remaining 5 descriptor actions (`source_cluster`, `masked_remote_address`, `metadata`, `query_parameters`, `query_parameter_value_match`) + the X-RateLimit DRAFT_VERSION_03 response headers (`x-ratelimit-limit/-remaining/-reset` with MIN-status + `;w=`/`;name=` quota suffix, emitted on all dispositions; `encode.go` + `headers.go`) + `RateLimitPerRoute` the NEW **10th canonical** (`vh_rate_limits` enum honored, `override_option` INERT, `rate_limits` Axis-A, `domain` override) + the **ADR-0125 roster amendment 9 → 10** (RE-AMENDS after phase-23's REUSE-by-absence skip) + the `stage` multi-stage bucketing path + the Axis-B `vh_rate_limits` cross-tier composition table (INCLUDE/IGNORE/OVERRIDE + legacy `include_vh_rate_limits` force-include) + scenarios (f) `vh_inclusion` + (g) `x_ratelimit_headers` to the existing `0032` + EXTENDS (d) with the remaining actions. **NO new fixture dir at 24.2** (stays 35). 24.2 lands ADR-0199 FULL + the X-RateLimit/remaining slice of ADR-0197 (in-place edit); escape-valve reserve ADR-0202 (still unconsumed unless 24.2 surfaces a divergence). The 24.2 phase-done commit ALSO flips the parent row 24 `in-progress → done` (rollup) + verifies the full parent §15 acceptance UNION; §9 family → **1 remaining row** (`wasm`) at 24.2 phase-done.
- **phase-directory:** `docs/envoy-go/phases/24.1-global-ratelimit-core-and-route-table/` (SPEC.md + PLAN.md + PROGRESS.md + REVIEW.md ALL landed). Sibling `docs/envoy-go/phases/24.2-global-ratelimit-perroute-and-headers/` (SPEC.md authored; `planned`, opens at this 24.1 phase-done commit — next-skill `superpowers:writing-plans`). Parent `docs/envoy-go/phases/24-http-filter-global-ratelimit/` (BRAINSTORM.md + the RETAINED parent master SPEC.md, annotated with the split-fired note). Phases 23 + 24.1 directories are complete.
- **lifecycle-state:** `phase 24.1 IMPL done; awaiting 24.2 PLAN` (SKILL_ROUTING state 2 — SPEC.md exists for 24.2, PLAN.md does not). ROADMAP row `24` is the PARENT (`in-progress`, `sub-phases = 24.1, 24.2`); row `24.1` flipped `in-progress → done` AT THIS COMMIT (date `2026-05-23`; per-cell IMPL-done annotation appended); row `24.2` stays `planned` (depends-on 24.1; opens now). The parent row `24` flips `in-progress → done` at 24.2's phase-done (rollup per the 18/19/22 precedent).
- **next-skill:** `superpowers:writing-plans` (to author the **24.2** PLAN.md from the existing `24.2-...-SPEC.md`: the remaining 5 actions + X-RateLimit DRAFT_VERSION_03 headers + `RateLimitPerRoute` 10th-canonical + ADR-0125 9→10 amendment body + `stage` + Axis-B `vh_rate_limits` + scenarios (f)+(g) + (d)-extension). The PLAN author applies the ADR-0045 split-gate verification (24.2 is BELOW the gate — single-row landing) + the §10 empirical-pin obligations IN-SESSION against reference Envoy v1.37.2 per ADR-0004 (any 24.2-specific items not already settled in the parent SPEC AMEND catalog).
- **last-commit:** `a4fdc75` (squash-merge of branch `phase-24.1-global-ratelimit-core-and-route-table-impl` from worktree `.worktrees/phase-24.1-global-ratelimit-core-and-route-table-impl/` — 15 IMPL-branch commits collapsed into one: Pre-Task 0 + Tasks 1-11 + Task-7 follow-up CRITICAL `ContinueDecoding`-after-`SendLocalReply` wake-up fix + Task-10 doc-fix follow-up + this Task 12 atomic-landing = 15 commits; 61 files / +11041 / -25 LoC). Predecessor branch tip pre-Task-12: `349e4c8` (Task 11 — differential fixture `0033-http-ratelimit-boot-reject`). Predecessor master tip: `e8a8881` — `next-prompt.txt: repoint master-tip references to 55f7620 (actual HEAD)`.
- **last-updated:** 2026-05-23
- **next-free ADR:** `ADR-0202` (UNCONSUMED at 24.1 phase-done — D-RL1 byte-confirmation RESOLVED as RAW-PROTO SEED CONFIRMED at Task 5; the escape valve did NOT fire). DECISIONS.md tail at `ADR-0201` (the PLAN-time ADR-0045 split ADR; full body landed at the 24.1 PLAN commit). The 3 phase-24.1 ADR bodies (ADR-0197[core] + ADR-0198 + ADR-0200) all landed at their per-Task Lands-in-Tasks per ADR-0044 (Tasks 7 / 5 / 3 respectively). ADR-0199 (the X-RateLimit + `RateLimitPerRoute` 10th-canonical body) stays anchored at the parent SPEC commit as §Context-only; §Decision + §Consequences body lands at 24.2 IMPL. **D-hypothesis (re-mapped, post-24.1):** `ADR-0202` stays UNCONSUMED at phase-24 phase-done — HOLD-with-known-risk; the 24.1 surface (the HIGHEST-risk DELTA-2 chain-seed type byte-confirmation per parent SPEC §12 item 1) was the predicted firing site and it DID NOT fire (the raw-proto seed fit ADR-0165 cleanly). The remaining D-hypothesis firing surfaces lie in 24.2: the X-RateLimit MIN-status byte-edge across multi-descriptor responses + the `metadata` action's dynamic-metadata accessor (the Lua-bridge-anchored `dynmd` accessor at phase-22.2 may need extension for the `metadata` action's dispatch). The conditional ADR-0202 anchor obligation passes through to 24.2 IMPL.

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
