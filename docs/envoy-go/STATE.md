# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `06.1-stats-prometheus`
- **phase-directory:** `docs/envoy-go/phases/06.1-stats-prometheus/` — contains `SPEC.md` (at `ad46930`, frozen), `PLAN.md` (at `be99b42`), and `PROGRESS.md` (this commit, Task 15 closing entry). Parent `docs/envoy-go/phases/06-observability-baseline/` carries `BRAINSTORM.md` (`d271fd7`) + parent `SPEC.md` (`ad46930`). Sibling `docs/envoy-go/phases/06.2-access-log/` retains the placeholder `README.md` (`ad46930`). Phase 05 closed history unchanged.
- **lifecycle-state:** `4` — implementation complete; verification not yet run. All 15 PLAN tasks are done and committed on branch `phase/06.1-stats-prometheus-impl`. Task 15 (this commit) lands the BEHAVIOR_CONTRACT in-place edit (`## Stat-name mapping` populated with SN1–SN8 rules + 17-name table + twin-series filter footnote per ADR-0061/SPEC §10.1/§6; `## Equivalence Matrix` row superseded per ADR-0062/SPEC §10.2) and the closing all-gates sweep (gates a/b/c/d/e green; gate f deferred to REVIEW session per BOOTSTRAP §5 step 6). Six ADRs landed across Tasks 2/4/8/14: ADR-0059 (Internal Stats Store architecture; Task 2 at `9417235`), ADR-0060 (Histograms deferred; Task 2 at `9417235`), ADR-0061 (SN1–SN8 flattening rules; Task 4 at `7f45a4d`), ADR-0064 (`stats_config.stats_tags` not honored; Task 4 at `7f45a4d`), ADR-0063 (per-endpoint cluster stats not emitted; Task 8 at `a6f8b94`), ADR-0062 (differential equivalence shape for stats; Task 14 at `a32212b`). ROADMAP row 06.1 stays `in-progress` at this commit; the phase-done commit at lifecycle-state 6 (REVIEW session) flips it to `done`. Carry-forward Minors from Tasks 8–14 (26 Minor findings total, all polish-level) await L4 review-followup triage.
- **next-skill:** `superpowers:verification-before-completion`
- **next-skill-scope:** Verify all six gates per BOOTSTRAP §7.5 / SPEC §3 against branch `phase/06.1-stats-prometheus-impl` tip. The verification session branches a fresh worktree from the impl-branch tip per ADR-0003. Gates to re-run: (a) `go test ./test/differential/ -run TestDifferential/0005 -v` — new fixture green; (b) `go test ./test/differential/ -run 'TestDifferential/(0000|0001|0002|0003|0004)' -v` — pre-existing fixtures still green; (c) `go test ./test/conformance/h2spec/ -v` — h2spec 53/53 at the ADR-0051 pin; (d) all 7 fuzz targets at 30s ADR-0018 budget (the 7th is new: `FuzzPromTextFormat` in `internal/stats/`); (e) `go vet ./...` + `golangci-lint run ./...` + `go test -race ./...` — vet/lint/test clean. ADR-0059 boundary grep: zero matches in go.mod for `github.com/prometheus`; zero matches in `internal/`/`cmd/envoy-go/` production code for third-party stats/prometheus/metrics/expvar imports (excluding `internal/stats` itself). 17-call-site grep: 2 listener (`downstreamCxTotal.Inc` + `downstreamCxActive.Inc/Dec` in `internal/listener/manager.go`), 5 HCM (`downstreamRqTotal.Inc()` in `connection.go`/`h2dispatch.go` + `downstreamStatusClassCounter().Inc()` for 4 class counters in `connection.go`/`h2dispatch.go`), 8 cluster (`IncUpstreamRqTotal` + `IncStatusClass` + `upstreamCxTotal.Inc` + `upstreamCxActive.Inc/Dec` + `membershipTotal.Set` in `internal/cluster/cluster.go`+`manager.go`), 1 server.live (`liveGauge.Set(1)` in `internal/admin/admin.go`). Gate (f) REVIEW.md is deferred to lifecycle-state 6 per BOOTSTRAP §5. Exit deliverable from the verification session: a single commit on master that advances STATE to lifecycle-state 5 and sets `next-skill: superpowers:requesting-code-review`, mirroring the phase-05.1/05.2 verification-session precedent. Carry-forward Minors from Tasks 8–14 (26 total): the verifier should note them in PROGRESS but not fix them — that is the L4 review-followup session's scope. The most impactful items are T14's M-3 (200ms hardcoded drain wait — potential CI flakiness) and T13's M-1 (empty-`{}` over-acceptance in FuzzPromTextFormat seed). The verifier should explicitly confirm that `go test -race ./...` includes `test/differential/` and that the differential fixture `0005-prometheus-stats` PASS is non-vacuous (i.e., the test actually contacts reference Envoy, not just a stub).
- **last-commit:** `58d4ec9` — Task 15 phase-done-shape commit (BEHAVIOR_CONTRACT in-place edit + STATE → lifecycle-state 4 + PROGRESS Task 15 closing entry), on branch `phase/06.1-stats-prometheus-impl`. The commit message names all six 06.1 ADRs (ADR-0059, ADR-0060, ADR-0061, ADR-0062, ADR-0063, ADR-0064) per BOOTSTRAP §5.3 phase-done-message-shape (mirroring 05.2's `bd75c88`). The commit body documents a 6-finding lint-fix sweep that gate (e) `golangci-lint run ./...` surfaced (gofmt × 2, revive × 1, misspell × 1, errcheck × 2 — all pre-existing, not introduced by Task 15). ROADMAP.md deliberately untouched per BOOTSTRAP §5 step 6 (REVIEW session's responsibility at lifecycle-state 6).
- **last-updated:** 2026-04-27

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
