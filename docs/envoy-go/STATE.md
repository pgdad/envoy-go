# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `05.2-upstream-h2`
- **phase-directory:** `docs/envoy-go/phases/05.2-upstream-h2/` — contains `README.md`, `SPEC.md` (`dacf4b7`), `PLAN.md` (`4c6b6bb`), `PROGRESS.md` (Tasks 1-15 landed; Task 15 executor verification block at `bd75c88`; lifecycle-state 4 verifier verification block at `b34bd99`; REVIEW-followup verification block addendum written by this commit), and `REVIEW.md` (`b9810ad` — APPROVED WITH FOLLOW-UPS, two Importants + ten Minors). The sibling `docs/envoy-go/phases/05.1-downstream-h2/` remains closed read-only history (ROADMAP row `05.1` status `done`). The parent `docs/envoy-go/phases/05-http-2/` retains its `SPEC.md` (`612cdea`) as the master design document. The parent ROADMAP row `05-http-2` stays `in-progress` until 05.2 reaches `done`; the 05.2 phase-done commit will close both rows on the same commit per 05.2 SPEC §4.4 + PLAN Task 15's "Refinement" note.
- **lifecycle-state:** `4` — REVIEW-followup batch complete; SPEC §3 phase-done gates (b)/(c)/(d)/(e) re-run green at HEAD of this commit. The REVIEW (`b9810ad`) returned APPROVED WITH FOLLOW-UPS with two Important findings (I-1 BEHAVIOR_CONTRACT.md rows 40-44 5-cell defect; I-2 routerActionH2.do H1-path observability gap) and ten Minor findings; the followup batch on `phase/05.2-upstream-h2-review-followup` (worktree `.worktrees/phase-05.2-upstream-h2-review-followup`, branched from `b9810ad` per ADR-0003) closed both Importants plus three optional Minors (M-2 / M-4 / M-7) across three commits: `d8fa1d8` (I-1 BEHAVIOR_CONTRACT 5-cell rewrite per ADR-0052 in-place edit), `1d57b31` (I-2 Path A — log + unit test for the H1-path defensive stub), `635f6a3` (M-2/M-4/M-7 cosmetics bundle). The remaining seven Minors are accepted as-is or carry forward to phase 06 per REVIEW.md Recommendation. Gate re-run results (verbatim outputs in PROGRESS.md REVIEW-followup verification block addendum): (b) 5/5 differential fixtures PASS in 6.84s including fixture 0004 at 1.80s; (c) h2spec 53/53 PASS at the unchanged ADR-0051 pin; (d) all 6 fuzzers PASS at 30s budget with empty `git status --porcelain` after each; (e) `go vet`/`golangci-lint`/`go test -race ./...` all clean + ADR-0046 boundary grep empty modulo the 5 allowed files + ADR-0048 client.go present + forbidden-runtime-imports clean modulo doc.go text. Per BOOTSTRAP §5.2 review-followup re-entry: this is the lifecycle-state 4 commit; the next commit promotes 4 → 5; the commit after that is the phase-done commit (state 5 → 6 + ROADMAP rows 05.2 + 05 → done together).
- **next-skill:** `subagent-driven-development` — drive the lifecycle-state 4 → 5 promotion (a STATE-only commit; mirrors 05.1 precedent at `3bb1bb9`) and then the lifecycle-state 5 → 6 phase-done commit (ROADMAP rows 05.2 + 05 flip; STATE.md → lifecycle-state 6 with `active-phase` advanced to phase 06 per BOOTSTRAP §5 step 6). The phase-done commit message MUST name every ADR introduced in 05.2 per BOOTSTRAP §5.3: ADR-0055, ADR-0056, ADR-0057, ADR-0058 (all four landed in 05.2; ADR-0045 belongs to the 05-split decision and was already done at the 05.1 phase-done commit `bc4fca4`).
- **next-skill-scope:** Two follow-up commits remain on this branch: (1) a STATE-only lifecycle-state 4 → 5 promotion commit, mirroring `3bb1bb9` from the 05.1 precedent — body explains that the REVIEW-followup verification block at this commit's HEAD (HEAD `635f6a3`, addendum at the next commit) is the gate (b)/(c)/(d)/(e) re-run; (2) the phase-done commit which flips ROADMAP row 05.2 `in-progress` → `done` AND parent row 05 `in-progress` → `done` together per 05.2 SPEC §4.4 + PLAN Task 15's "Refinement" note, and updates STATE.md `lifecycle-state` to `6` with `active-phase` advanced to whatever phase 06's directory name is (per ROADMAP row 06: `observability-baseline` — directory likely `06-observability-baseline`; `next-skill` becomes `superpowers:brainstorming` per BOOTSTRAP §5 step 1's cold-start state). Re-entry: any new Critical finding bounces back to state 3 per BOOTSTRAP §5.2; the followup branch stays unmerged per per-phase-worktree convention.
- **last-commit:** `TBD` — `phase 05.2 review-followup: gates (b)/(c)/(d)/(e) re-run green — PROGRESS verification-block addendum + STATE → lifecycle-state 4`, on branch `phase/05.2-upstream-h2-review-followup`. Appends the REVIEW-followup verification block addendum to `docs/envoy-go/phases/05.2-upstream-h2/PROGRESS.md` (verbatim gate outputs at HEAD `635f6a3`) and updates STATE → lifecycle-state 4. No production code, test, or fixture file changed; this is a pure verification + state-transition commit per the 05.1 verifier-addendum-commit precedent at `a6abf63`. ROADMAP unchanged (rows 05.2 + 05 stay `in-progress`; phase-done commit will flip both). This entry's SHA is filled in a follow-up commit per the phase-02..05.1 SHA-fill convention.
- **last-updated:** 2026-04-26

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
