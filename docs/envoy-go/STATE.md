# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `21-http-filter-adaptive-concurrency`. Phase 21 BRAINSTORM landed at this commit per the 5-question dialogue (Q1 scope ambition — Pragmatic middle: full Gradient-1 algorithmic surface with PARSE-REJECT for `enabled.runtime_key != ""` deferring the RTDS RuntimeFeatureFlag; Q2 fixture strategy — cross-side w/ FAKE-TIME injection via an inline Clock seam, two-layer test taxonomy: subject-only step-driven algorithmic-fidelity unit tests + a `0025-http-adaptive-concurrency` structural cross-side fixture; Q3 clock seam — INLINE Clock interface in `internal/filter/http/adaptive_concurrency/clock.go`, NO new `internal/clock/` framework primitive; Q4 phase split — single phase 21; Q5 stat surface — full upstream 7-name parity 1 counter + 6 gauges 92 → 99). **FOURTEENTH §9 family-row** (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11, csrf @ 12, buffer @ 13, compressor @ 14, bandwidth_limit @ 15, rbac @ 16, jwt_authn @ 17, ext_authz @ 18 with its 18.1+18.2 split, ext_proc @ 19 with its 19.1+19.2 split, oauth2 @ 20). **LEANEST framework-delta §9 row to date** — ZERO new `internal/` primitives + ZERO in-place AMENDMENTs; 7 framework REUSES (stats Counter+Gauge / HTTPRegistry / per-request filter interface / HCM-parse-time PARSE-REJECT / REUSE-by-absence per-route enforcement (**FOURTH CONSECUTIVE §9 row to skip ADR-0125 amendment**) / fuzzer-corpus framework / differential-fixture framework). 2-3 ADRs anticipated at SPEC (ADR-0186 Gradient-1 controller state machine + clock seam inline + FAKE-TIME differential strategy + sorted-slice percentile aggregation + gradient formula bit-exact citation; ADR-0187 RTDS `enabled.RuntimeFeatureFlag` deferral PARSE-REJECT; ADR-0188-candidate float-valued gauge encoding convention — anchored as a new framework convention OR as in-place §Decision body on a stats-family ADR, SPEC author decides). **D11-style hypothesis** per BRAINSTORM §7.4: ADR-0189 stays UNCONSUMED at phase-21 phase-done (carrying the next-free escape valve forward to phase 22). 4 SPEC-time D-questions deferred from BRAINSTORM §10 (D1 float-gauge encoding convention anchor; D2 503-overflow cross-side promotion candidate via the deterministic 2-concurrent-slow-requests trap; D3 `gradient_controller.cc` line-citation depth; D4 stat-prefix template byte-exact pin per §10 IN-SESSION verification against reference Envoy v1.37.2 per ADR-0004). 6-8 deferred items per BRAINSTORM §8 (RTDS runtime keying; cross-side byte-exact algorithmic parity; alternative ConcurrencyControllerConfig oneof arms; CircllHist/HDR Hist percentile aggregation upgrade; stat-prefix template byte-exact pin SPEC-time-closure; float-gauge encoding convention anchor; gradient_controller.cc line-citation depth; multi-listener topology variant). **LOW-MEDIUM split-readiness leaning** per BRAINSTORM §1.4 (anticipated ~1200-1500 LoC + ~14-16 tasks; the natural split axes don't carve cleanly because the controller state machine + FAKE-TIME tests are tightly coupled and the framework delta is zero); SPEC author re-evaluates if scope drifts past the ADR-0045 split-gate at SPEC time. Single-row landing anticipated; sub-phase rows `21.1` / `21.2` may appear at the SPEC commit if the ADR-0045 split fires at SPEC time.
- **phase-directory:** `docs/envoy-go/phases/21-http-filter-adaptive-concurrency/` (BRAINSTORM.md authored at this commit; SPEC.md + PLAN.md + PROGRESS.md + REVIEW.md to be authored at subsequent lifecycle-state transitions per BOOTSTRAP §5).
- **lifecycle-state:** `phase 21 BRAINSTORM done; awaiting SPEC`. ROADMAP row `21` is `in-progress` at this commit (sub-phases column empty; status will flip to `done` at the phase-21 IMPL phase-done commit per BOOTSTRAP §5 step 6 + ADR-0045 single-row landing per Q4 unless the SPEC author splits). DECISIONS.md tail UNCHANGED at **ADR-0185** (BRAINSTORM does NOT consume ADRs per ADR-0044; the 2-3 anticipated ADRs ADR-0186..ADR-0188 are §Context-drafted at the SPEC commit, §Decision + §Consequences full bodies anchored at per-Task IMPL lands).
- **next-skill:** `superpowers:writing-plans` (scoped to SPEC.md authoring per the phase-09..20 precedent — `writing-plans` is the project's standard SPEC-authoring skill, NOT `brainstorming`, which fired at this commit). The next session is the phase-21 SPEC-authoring session (lifecycle-state 1 → 2). Cold-start scope for the next session: read STATE.md (this file) + `docs/envoy-go/phases/21-http-filter-adaptive-concurrency/BRAINSTORM.md` (the BRAINSTORM authored at this commit; load-bearing for the 5 Q-decisions + the framework-survey result + the §8 deferred-items register + the §10 SPEC-time D-questions) + `docs/envoy-go/phases/20-http-filter-oauth2/SPEC.md` (the most-recent §9 SPEC for structural precedent; phase-21 SPEC mirrors phase-20 SPEC's 15-section structure) + `docs/envoy-go/ROADMAP.md` row 21 (BRAINSTORM-time row commit) + project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`.
- **last-commit:** `cad1153` — `Squash merge phase-21-http-filter-adaptive-concurrency-brainstorm`. Phase 21 BRAINSTORM landed via worktree `.worktrees/phase-21-http-filter-adaptive-concurrency-brainstorm/` on branch `phase-21-http-filter-adaptive-concurrency-brainstorm` per project memory `feedback_git_worktrees.md` (2 worktree commits bundled into the squash: `d9b0410` initial BRAINSTORM + `64d1ffb` reviewer-advisory follow-up). This SHA-fill follow-up commit per the phase-09..20 convention: `phase 21 BRAINSTORM follow-up: STATE.md SHA-fill (TBD → cad1153 post-squash)`. Predecessor master tip was `4deaa5c` — `phase 20 IMPL follow-up: STATE.md SHA-fill (TBD → da08dfc post-squash)`.
- **last-updated:** 2026-05-18
- **next-free ADR:** `ADR-0186` — UNCHANGED from post-phase-20-IMPL. BRAINSTORM does NOT consume ADRs per ADR-0044 (the 2-3 anticipated ADRs ADR-0186..ADR-0188 are §Context-drafted at the SPEC commit, §Decision + §Consequences full bodies anchored at per-Task IMPL lands per the ADR-0044 multi-stage discipline).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
