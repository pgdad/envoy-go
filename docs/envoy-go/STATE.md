# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `phase 26.2 PLAN-done; awaiting 26.2 IMPL` (2026-05-30, this commit) — **phase 26.2 (`network-filter-registry-migration`) PLAN DONE at 2026-05-30**. The 26.2 PLAN (`docs/envoy-go/phases/26.2-network-filter-registry-migration/PLAN.md`, ~1240 lines, **12 bite-sized TDD tasks**) decomposes the 26.2 SPEC §10 task spine (squash `9a61788`, docs only, no code change), authored via `superpowers:writing-plans`. plan-document-reviewer APPROVED: 1 MAJOR (a Task-7 self-contradiction on the unknown-type-url reject) → RESOLVED by keeping `buildNetChainFactory`'s 3-value `(factory, isNetChain, error)` signature + the index-0 netReg-miss fall-through THROUGH Task 7 (the old `buildTerminalFilter:628` path still owns the `unknown filter type_url` wording); the unified unknown-type reject + the 2-value collapse + `buildTerminalFilter` deletion all land together at Task 10 (every intermediate commit stays green). 3 MINOR/ADVISORY folded (the `prefixConn` test uses a `scriptedConn` helper not `net.Pipe().CloseWrite()`; the existing `newChainRuntime([]ReadFilter{…})` call sites stay `[]ReadFilter`, only NEW tests use `[]NetworkFilter`; all 7 Marker-embed test fakes enumerated). Re-confirmed ✅ no new contradiction. **ADR-0045 split-gate CHECKED: NO split** (12 tasks / ~500-900 net-new LoC, well within ~25 tasks / ~1500 LoC). **PLAN-time D-questions RESOLVED:** D-26.2-1 = SEALED `NetworkFilter` marker via the exported embeddable `network.Marker` (Go's cross-package interface-sealing idiom — an unexported `isNetworkFilter()` cannot be satisfied from outside `network`, so out-of-package filters embed the exported zero-size `Marker`; blast radius = `echoFilter`/`directresponse.filter` + 6 `chain_test.go` fakes + `noopFilter`); D-26.2-2 = `FactoryCtx` += `HasTLS`/`AllowH2C`/`ListenerPrincipal`/`NodeServiceCluster` (the four per-chain primitives; the heavy singletons cm/dm/stats/sinks/httpReg/httpClient closure-captured in the adapters); D-26.2-3 = LAND the buffered-prefix `prefixConn` handover at 26.2, unit-tested via a synthetic always-`Continue` read filter → recording terminal (R-M; first PROD consumer `rbac_network` 26.3); D-26.2-5 = a NEW `internal/filter/network/builtins` package `RegisterBuiltins(reg, Deps)` (placement is import-cycle-forced: imports echo/directresponse/tcpproxy/hcm/network, so NOT in `network`/`listener`/`cmd`; consumed by main.go + the thinner ctors + the admin/manager/main test callers). D-26.2-4 (`listenerCtx` full-retire — anticipated; confirm at IMPL Task 7) / D-26.2-6 (PARSE-REJECT byte-stable wording — IMPL Task 8/10) / D-26.2-7 (import-cycle audit via `go build ./...` — IMPL Tasks 5/6/9) remain IMPL-time. The §9 Network-filters family stays OPEN (parent row 26 `in-progress`; sub-row 26.2 `in-progress`; 26.3 `planned`). Next: 26.2 IMPL (`superpowers:subagent-driven-development` executing the 12-task PLAN). **Earlier (SPEC stage, preserved):** **phase 26.2 SPEC DONE at 2026-05-30** (squash `7c19957`). The per-sub-phase 26.2 SPEC (`docs/envoy-go/phases/26.2-network-filter-registry-migration/SPEC.md`, 16 §§) was authored per the 22.1/25.1/26.1 per-sub-phase-SPEC precedent: it INHERITS the phase-26 parent SPEC §5/§6/§7/§8/§11/§12/§13 rosters + refines per-Task surface. It RESOLVES the parent §3.2 terminal-fit D-question: `tcp_proxy` + HCM migrate onto a NEW `network.TerminalFilter` **connection-takeover seam** (`Handle(ctx, downstream net.Conn)` byte-identical to the retired `manager.go` `filterHandler` — `*tcpproxy.Filter` + `*hcm.Filter` satisfy it with ZERO method changes; their `Handle` loops are UNTOUCHED → byte-exact parity intrinsic) — chosen over an `OnData` rewrite (REJECTED: HCM's H1 driver + H2 ServerConn fully own the conn; severe re-architecture/parity risk). 26.2 scope: retire the hardcoded `filterRegistry`/`filterConstructor`/`filterHandler`/`buildTerminalFilter`; generalize the registry (`FilterInstanceFactory func() NetworkFilter` sealed marker = ReadFilter | TerminalFilter); extend `network.FactoryCtx` with per-chain primitives (HasTLS/AllowH2C/ListenerPrincipal/NodeServiceCluster — heavy boot singletons closure-captured in `main.go` registration adapters `tcpproxy.NewNetworkFactory`/`hcm.NewNetworkFactory` so `internal/filter/network` stays import-light, no cluster/stats/hcm imports); unify `serveConnection` step-7 into ONE `serveNetworkChain` branch; LIFT the `network-filter-mixed-chain-unsupported` 26.1-transitional reject (mixed `[read-filter*, terminal-filter?]` chains expressible — the buffered-prefix `prefixConn` handover shaped + unit-tested; FIRST production consumer = `rbac_network` 26.3, since echo halts + direct_response closes — neither `Continue`s to a terminal at 26.2, so the multi-chain DIFFERENTIAL fixture is DEFERRED to 26.3 + the capability is unit-tested at 26.2). Migration is stat/fixture/fuzzer-NEUTRAL: stat surface stays 132, fixtures stay 44 (R3 back-compat — the existing tcp_proxy/HCM differential fixtures + conformance 10/10 + h2spec 53/53 stay byte-exact green LIVE = the migration proof), fuzzers stay 35. **ADR-0215 §Context DRAFTED into DECISIONS.md** (tail ADR-0214 → ADR-0215; next-free → ADR-0216; §Decision/§Consequences land at 26.2 IMPL per ADR-0044). spec-document-reviewer APPROVED (no CRITICAL/MAJOR; every code claim verified true against the migration-target source — `tcpproxy.Filter.Handle`@69, `hcm.Filter.Handle`@66, `filterHandler`@46, the FactoryCtx import-light/closure-capture design, the echo-halt deferral reasoning, all baselines; 4 MINOR/ADVISORY polish items folded in). The §9 Network-filters family stays OPEN (parent row 26 `in-progress`; sub-row 26.2 `planned → in-progress` at this SPEC commit per ADR-0106; 26.3 `planned`). **Earlier (phase 26.1 IMPL, preserved):** phase 26.1 (`network-filter-chain-framework-and-echo`) IMPL DONE at 2026-05-30. The 17-task TDD PLAN executed via `superpowers:subagent-driven-development` (fresh subagent per task, two-stage review, local-only commits). LANDED: the NEW `internal/filter/network/` read-filter chain framework (two-value `Status`; `ReadFilter`/`ReadFilterCallbacks`/`Connection`/`CloseType`; drainable `Buffer`; freeze-after-boot `*Registry`; per-connection runtime context owning the reused `*dynamicmetadata.Bucket` + RCD sink); `echo` + `direct_response` first consumers; `manager.go` dual-dispatch (new read-filter chain path alongside the untouched `tcp_proxy`/HCM terminal path + the `network-filter-mixed-chain-unsupported` transitional boot-reject); `cmd/envoy-go/main.go` boot-wiring; the 35th fuzzer `FuzzNetworkFilterConfigParse`; differential fixtures 0040/0041/0042. **EMPIRICAL CORRECTION (echo TypeURL):** the SPEC/PLAN pinned echo's type URL as `…envoy.filters.network.echo.v3.Echo`, but the actual go-control-plane v1.32.4 proto full-name carries the `extensions.` segment — `…envoy.extensions.filters.network.echo.v3.Echo` (verified via `proto.MessageName` + boot smoke + fixture 0040 against upstream v1.37.2); `echo.go` + SPEC §3.6/§4.1 + the BEHAVIOR_CONTRACT echo subsection corrected at Task 17. **One CRITICAL bug found + fixed at IMPL (Task 11):** a sticky single-`halted` flag broke echo's multi-read (echoed only the first read); split into a sticky `connHalted` (OnNewConnection only) vs a non-sticky per-pass OnData stop with per-read re-iteration — regression-tested + proven by the multi-write 0040 fixture. Six gates GREEN at Task 17: build/vet/lint/`-race -short` clean; differential 0040/0041/0042 + R4 back-compat (0000-tcp-echo/0002-tls-tcp) byte-exact; fuzzers 35; fixtures 44; stat surface 132 (26.1 added 0 stats — `internal/filter/network/` registers no counter); conformance 10/10 + h2spec 53/53 ASSERTED-UNAFFECTED (26.1 touches no HTTP/proxy-wasm/h2 code path — SPEC §2.9). ADR-0213 + ADR-0214 §Decision/§Consequences bodies landed in place (DECISIONS.md tail STAYS ADR-0214; no new number consumed). The §9 Network-filters family stays OPEN (parent row 26 `in-progress`); 26.2 (tcp_proxy/HCM migration + hardcoded-registry retirement) is next. **Earlier (PLAN stage, preserved):** phase 26.1 PLAN DONE at 2026-05-30. The PLAN (`docs/envoy-go/phases/26.1-network-filter-chain-framework-and-echo/PLAN.md`, ~1400 lines, **17 bite-sized TDD tasks**) decomposes the 26.1 SPEC §10 task spine; plan-document-reviewer APPROVED after 2 iterations (the fixture tasks 14/15/16 were rewritten from a fabricated harness model to the REAL `fixture.Driver`/`harness.BootRejectFixture` API + `init()`-registration + `runner_test.go` blank-import; the fuzzer-count gate was hardened to `git ls-files`; the `NewManagerWithBaseDirAndAllowH2C` ctor-caller blast radius — admin tests + manager_test — was added; the direct_response RCD sink was pinned in Task 6). **ADR-0045 split-gate CHECKED: NO split** (17 tasks / ~850-1020 net-new LoC, well within ~25 tasks / ~1500 LoC). **PLAN-time D-questions RESOLVED:** D-P26.1-1 = drainable `*Buffer`; D-P26.1-2 = INLINE the DataSource 4-arm switch in `directresponse` modeled on `internal/tls/datasource.go:loadDataSource(ds, baseDir)` (NO shared `internal/datasource` package exists) + REFINE `network.FactoryCtx` to carry `BaseDir` (SPEC §3.1 said empty; direct_response's Filename arm needs boot-time baseDir for byte-stable boot-reject parity). D-P26.1-3/4/5 remain IMPL-time (byte-stable wording / boot-reject arm / close-detection + RCD storage), scoped into PLAN Tasks 8/9/11/16. **Earlier (SPEC stage, unchanged):** phase 26.1 SPEC DONE at 2026-05-30. The §9 Network-filters family stays OPEN (parent row 26 `in-progress`). The 26.1 SPEC (`docs/envoy-go/phases/26.1-network-filter-chain-framework-and-echo/SPEC.md`, ~430 lines) was authored per the phase-22.1/25.1 per-sub-phase-SPEC precedent: it INHERITS the phase-26 parent SPEC's §5 proto roster + §6 PARSE-REJECT roster + §7 stat surface + §8 fixture taxonomy + §11 empirical-pin block + §12 D-questions + §13 RATIFIED-PENDING items and refines per-Task-level surface only (it does NOT re-execute the empirical pins). It anchors: (1) the NEW `internal/filter/network/` read-filter chain framework — §3.1 production API signatures (two-value `Status` enum; `ReadFilter` interface with `OnNewConnection`/`OnData`/`SetReadFilterCallbacks`/`OnDestroy`; `ReadFilterCallbacks` with `Connection()`/`ContinueReading()`/`DynamicMetadata() *dynamicmetadata.Bucket`; the drainable `*Buffer`; the `CloseType` enum `FlushWrite`/`NoFlush`; the two-step factory; the freeze-after-boot `*Registry`), §3.3 chain-runner iteration semantics, §3.4 per-connection runtime context owning the REUSED `*dynamicmetadata.Bucket` (parent AMEND-A5); (2) `echo` + `direct_response` first-consumer code shapes (§4); (3) the §3.5 **dual-dispatch** in `manager.go` (D-P2 RESOLVED: the new read-filter chain path lands FULLY at 26.1 ALONGSIDE the untouched `tcp_proxy`/HCM terminal-filter path — a `chainInfo.netChainFactory` field + a build-time pre-check on `filters[0].type_url` + a `serveConnection` dual-branch + a new `serveReadFilterChain` read loop); (4) the §3.6 `cmd/envoy-go/main.go` boot-wiring (new `netReg *network.Registry` constructor arg). D-S1 baselines VERIFIED at this SPEC commit (§11.1): fuzzers **34** (35th lands at 26.1 IMPL), differential fixtures **41** / numbering tail **0039** (26.1 lands 0040/0041/0042 → 44), stat surface **132** (stays 132), DECISIONS.md tail **ADR-0214**. spec-document-reviewer APPROVED (no load-bearing issues; 4 advisory nits folded in — line-ref +1 fixes, dynamicmetadata cite harmonized, `KnownTypeURLs`-mirrors-http-registry note, response-code-details storage-site D-question). Phases 00–25 + phase-26 parent SPEC + phase-26.1 SPEC DONE and CANONICAL.
- **phase-directory:** `docs/envoy-go/phases/26.2-network-filter-registry-migration/` (SPEC.md + PLAN.md authored — DONE; PROGRESS.md / REVIEW.md land at the 26.2 IMPL / REVIEW sessions). The phase-26.1 directory (SPEC.md + PLAN.md + PROGRESS.md — DONE) + the parent directory `docs/envoy-go/phases/26-network-filter-chain-and-rbac/` (BRAINSTORM.md + parent SPEC.md — DONE) stay canonical. The `26.3-network-filter-rbac/` directory is created at the 26.3 SPEC entry per the per-sub-phase convention (git does not track empty dirs).
- **lifecycle-state:** `phase 26.2 PLAN-done (SKILL_ROUTING state 3 → awaiting 26.2 IMPL for sub-row 26.2); awaiting 26.2 IMPL` (the 26.2 PLAN.md authored + plan-document-reviewer-APPROVED + squash-merged). The next lifecycle transition is the 26.2 IMPL cold-start (`superpowers:subagent-driven-development` executing the 12-task PLAN task-by-task — fresh subagent per task, two-stage review, local-only commits; controller pushes at stage-close). ROADMAP sub-row 26.2 STAYS `in-progress` (it flipped `planned → in-progress` at the SPEC commit per ADR-0106; flips `in-progress → done` at 26.2 IMPL phase-done). Parent row 26 STAYS `in-progress`; it flips `in-progress → done` at 26.3 phase-done via the ROLLUP precedent (18/19/22/24/25).
- **next-skill:** `superpowers:subagent-driven-development` (scoped to the **26.2 IMPL** — execute the 12-task PLAN task-by-task: T1 baselines/anchor re-pin; T2 `TerminalFilter`+sealed `NetworkFilter` marker; T3 extend `FactoryCtx`; T4 `[]NetworkFilter` chain + terminal handoff + `prefixConn`; T5 `tcpproxy.NewNetworkFactory`; T6 `hcm.NewNetworkFactory`; T7 unified `buildNetworkChainFactory` (classify + `[read*,terminal?]` shape rejects + mixed-chain LIFT); T8 `serveConnection` step-7 → `serveNetworkChain`; T9 `RegisterBuiltins` seam; T10 netReg-required + caller re-wiring + DELETE old terminal path; T11 main.go boot-wiring; T12 BEHAVIOR_CONTRACT + ADR-0215 §Decision/§Consequences bodies + STATE/ROADMAP advance + six-gate (full differential R3 back-compat + conformance 10/10 + h2spec 53/53 run LIVE)). After 26.2 IMPL phase-done → 26.3 SPEC per SKILL_ROUTING.
- **last-commit:** `9a61788` — `Squash merge phase-26.2-network-filter-registry-migration-plan` (the 26.2 PLAN.md, 12-task TDD decomposition of the SPEC §10 spine; squash-merged from `phase/26.2-network-filter-registry-migration-plan`, 3 worktree commits; docs only, no code change). Predecessor: `7c19957` (the 26.2 SPEC squash). The STATE/next-prompt advance commit that follows trails this substantive squash by +1 (not chased).
- **last-updated:** 2026-05-30
- **next-free ADR:** `ADR-0216` (the 26.2 SPEC DRAFTED the **ADR-0215 §Context** into DECISIONS.md → tail advanced ADR-0214 → **ADR-0215**; next-free is now **ADR-0216**. ADR-0215's §Decision/§Consequences bodies are RATIFIED-PENDING-IMPL — they land IN PLACE at 26.2 IMPL per ADR-0044, so the tail STAYS ADR-0215 through 26.2 phase-done; no new number consumed at IMPL). Provisional remaining family span (drafted at their own sub-phase SPECs): 26.3 ADR-0216 (`internal/rbac/` extraction) + ADR-0217 (connection-metadata sink as `internal/dynamicmetadata/` connection-scoped reuse) + ADR-0218 (`rbac_network` filter). Anticipated next-free after phase-26 phase-done: `ADR-0219`. ADR-0209 escape-valve reserve carried from the §9 family STANDS-UNCONSUMED.

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
