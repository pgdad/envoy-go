# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `07.2-listener-chain-completion`
- **phase-directory:** `docs/envoy-go/phases/07.2-listener-chain-completion/` — now contains `README.md` (sibling-stub seeded at the 07.1 SPEC commit `ee45aba` per the 06.2 / 05.2 stub precedent — read-only history once the SPEC.md commits) and the full `SPEC.md` (1101 lines; drafted at this commit per ADR-0004 autonomous brainstorming; spec-document-reviewer subagent approved at iteration 2 after one revision pass that fixed three issues — ADR-0033 supersession enumeration single-sourced to §5.7, pipeline-deadline mechanism reconciled to per-pipeline shared per Decision N, fixture-0008 listener count committed to dual-listener-with-c4-variant per §7.4 + Decision G). Parent `docs/envoy-go/phases/07-filter-chain-framework/` carries `BRAINSTORM.md` (688 lines, `da28039`) + parent master `SPEC.md` (113 lines, `ee45aba`) — both read-only history. Sibling 07.1 directory at `docs/envoy-go/phases/07.1-http-filter-framework/` is closed read-only history (SPEC.md / PLAN.md / PROGRESS.md / REVIEW.md). Phases 04, 05 (+ 05.1, 05.2), and 06 (+ 06.1, 06.2) remain closed read-only history.
- **lifecycle-state:** `2` — SPEC drafted, reviewer-approved. Next session writes `PLAN.md` per `BOOTSTRAP_PROMPT.md` §5 step 2.
- **next-skill:** `superpowers:writing-plans` — phase 07.2 PLAN-drafting session (state 2 → 3 per `BOOTSTRAP_PROMPT.md` §5 step 2). Autonomous planning per ADR-0005 (no Execution Handoff question; PLAN landed at `docs/envoy-go/phases/07.2-listener-chain-completion/PLAN.md`; plan-document-reviewer subagent loop retained; on green review, STATE advances to lifecycle-state 3 with `next-skill = superpowers:subagent-driven-development` per ADR-0005's default-executor preference). The session reads SPEC.md (1101 lines) + parent SPEC + ADRs cited in SPEC §10 + the existing `internal/listener/` source surface to derive a numbered-task PLAN that ships the SPEC's deliverables in order. Per `BOOTSTRAP_PROMPT.md` §6.1 the plan-size gate is `~25 tasks OR ~1500 LoC`; if the PLAN exceeds either threshold the session splits 07.2 into sub-sub-phases per §6.2 (this is unlikely — 07.2's surface is bounded by §4 to ~1100-1500 LoC across 4 packages).
- **next-skill-scope:** Lifecycle-state 2 → 3 PLAN-drafting session deliverable: full 07.2 PLAN per the established 06.2 / 07.1 PLAN-shape (numbered tasks, each with TDD obligations + acceptance criteria + estimated LoC + dependency graph + cross-references to SPEC sections). Tasks ordered to land green-at-each-step: (a) listener-filter framework package (`internal/listener/listenerfilter/`) types + registry + pipeline + chain-match algorithm with unit tests; (b) `tls_inspector/` filter + parser + proto with unit tests; (c) `internal/listener/manager.go` refactor to thread the registry, parse the new `FilterChainMatch` dimensions, honor `default_filter_chain`, and dispatch listener filters; (d) bootstrap + `cmd/envoy-go/main.go` wiring (registry construction + freeze + thread-through); (e) fixture `0008-listener-chain-match` (dual-listener bootstrap + driver + 5-backend program + expectations.yaml + README); (f) `FuzzFilterChainMatch` fuzzer; (g) BEHAVIOR_CONTRACT.md in-place edits per ADR-0052 (new `## Listener filters` section + `## TCP proxy` "Does not yet apply to" amendment + `## TLS` "Scope boundaries" amendment); (h) seven ADR drafts (ADR-0077..ADR-0083) lands-in-task per SPEC §10 (planner re-verifies next-free numbers at PLAN-write time per ADR-0004); (i) ROADMAP.md row 07.2 + parent row 07 closure at the phase-done commit. Per ADR-0005: PLAN landed at `docs/envoy-go/phases/07.2-listener-chain-completion/PLAN.md`; plan-document-reviewer subagent loop retained (max 3 iterations; block on third-iteration failure). The PLAN-drafting commit's STATE update advances `lifecycle-state` to 3 + sets `next-skill: superpowers:subagent-driven-development` per ADR-0005's default-executor preference.
- **last-commit:** `TBD` — `phase 07.2: SPEC drafted (autonomous brainstorming per ADR-0004) + ROADMAP row 07.2 → in-progress`. This SPEC-drafting commit lands `docs/envoy-go/phases/07.2-listener-chain-completion/SPEC.md` (1101 lines; covers Purpose / Non-purposes / Phase-done gates / Deliverables / Architecture / Listener-filter framework surface / FilterChainMatch precedence / `default_filter_chain` semantics / Differential fixture / Anticipated ADRs (ADR-0077..ADR-0083) / Empirical-pin block (3 in-session pins resolved against reference Envoy v1.37.2 server SHA `5afe27fb338b16d5bb06b3a7198bcd581b4e3dee`; 1 carry-forward pin documented) / Out-of-scope / BEHAVIOR_CONTRACT.md additions / Deferred decisions (15 items A–O) / Testing strategy / Acceptance checklist / References) + ROADMAP.md row 07.2 status flip `planned → in-progress` (per the 07.1 REVIEW I-3 corrected pattern — atomic flip at the SPEC commit, NOT deferred to impl-time) + this STATE.md rewrite to advance to lifecycle-state 2 with `next-skill: superpowers:writing-plans`. Per ADR-0003: master FF'd to this SPEC-drafting commit's SHA-fill follow-up SHA. SHA filled in a follow-up commit per the phase-04..07.1 SHA-fill convention.
- **last-updated:** 2026-05-02

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
