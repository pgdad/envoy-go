# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `08-admin-api-and-drain`
- **phase-directory:** `docs/envoy-go/phases/08-admin-api-and-drain/` — does NOT yet exist; first creation lands at the phase-08 brainstorming session per `BOOTSTRAP_PROMPT.md` §5 state 1 transition (BRAINSTORM.md → SPEC.md scaffolding). Phase 08 row exists in `ROADMAP.md` as part of the BOOTSTRAP §8 MVP-trunk seed (`admin-api-and-drain`, depends-on `07`, status `planned`, summary "Minimum admin API (config_dump, stats, clusters, listeners, ready, server_info) + graceful drain. Admin + drain fixtures green."). Parent `docs/envoy-go/phases/07-filter-chain-framework/` (BRAINSTORM.md + parent SPEC.md), and sub-phase directories `07.1-http-filter-framework/` (SPEC + PLAN + PROGRESS + REVIEW) and `07.2-listener-chain-completion/` (SPEC + PLAN + PROGRESS + REVIEW) are now closed read-only history. Phases 04, 05 (+ 05.1, 05.2), and 06 (+ 06.1, 06.2) remain closed read-only history.
- **lifecycle-state:** `1` — Phase 08 row is in `ROADMAP.md` (seeded by BOOTSTRAP §8 MVP-trunk) but the phase directory does NOT exist yet. Next session opens a `superpowers:brainstorming` session against the BOOTSTRAP §8 phase-08 row + the seeded summary ("Minimum admin API (config_dump, stats, clusters, listeners, ready, server_info) + graceful drain") and produces the phase-08 BRAINSTORM and/or SPEC per the established lifecycle-state 1 → 2 transition (mirrors the 06.1, 06.2, 07, 07.1, 07.2 brainstorm-then-SPEC pattern).
- **next-skill:** `superpowers:brainstorming` — phase 08 brainstorming session (state 1 → 2 per `BOOTSTRAP_PROMPT.md` §5 step 1). Autonomous brainstorming per ADR-0004 (no live human-in-the-loop). Inputs: `BOOTSTRAP_PROMPT.md` §8 phase-08 row + ROADMAP row 08 + the cumulative MVP-trunk surface at this phase-done commit (admin endpoints already partially shipped at 06.1's `/stats/prometheus`, plus the existing `/ready` from phase 01, plus the listener/cluster machinery that `/config_dump` and `/listeners`/`/clusters` would expose). Deliverable: `docs/envoy-go/phases/08-admin-api-and-drain/BRAINSTORM.md` (or directly SPEC.md if no split is anticipated), settling 3-7 cardinal decisions, design-space trade-offs, anticipated ADR numbers (next-free at this commit is ADR-0084 since 07.2 closed at ADR-0083), and the phase's split-vs-monolith call.
- **next-skill-scope:** Lifecycle-state 1 → 2 brainstorming session deliverable: phase-08 BRAINSTORM.md per the 06 / 07 BRAINSTORM precedent (Mission, Scope, Constraints, Decisions, Open Questions, Out-of-scope deferrals per ADR-0040). Brainstorm-time exploration: (a) admin-endpoint surface scope (`/config_dump`, `/clusters`, `/listeners`, `/server_info`, `/ready` — `/stats/prometheus` already shipped at phase 06.1; `/ready` already shipped at phase 01); (b) graceful-drain semantics (drain-listener vs drain-connection-by-connection; SIGTERM behavior; new-connection-rejection during drain; in-flight request completion windows); (c) admin-server transport (re-use of existing HTTP/1.1 admin scaffolding from phase 01 vs new code path); (d) split-vs-monolith call (whether to split phase 08 into sub-phases 08.1/08.2 per ADR-0045 if the design space exceeds the BOOTSTRAP §6.1 25-task / 1500-LoC thresholds). The session settles those decisions, drafts BRAINSTORM.md, then either: (i) advances directly to lifecycle-state 2 (SPEC drafting) on the same branch if no split is needed, OR (ii) splits per BOOTSTRAP §6 if scope demands. ROADMAP row 08 stays `planned` at brainstorming time; the row flips to `in-progress` at the phase-08 SPEC commit per the 06.2 / 07.2 SPEC-commit precedent (and per the 07.1 REVIEW I-3 close-out — the SPEC commit MUST atomically flip the row to `in-progress`).
- **last-commit:** `01abdfe` — `phase 07.2: listener-chain-completion [ADR-0077, ADR-0078, ADR-0079, ADR-0080, ADR-0081, ADR-0082, ADR-0083]`. This phase-done commit lands `docs/envoy-go/phases/07.2-listener-chain-completion/REVIEW.md` (APPROVED with 10 carry-forward minors; zero Critical / zero Important) + `docs/envoy-go/phases/07.2-listener-chain-completion/PROGRESS.md` closing entry + ROADMAP rows 07.2 (`in-progress → done`) AND parent 07 (`in-progress → done`) flipped AT THE SAME COMMIT per parent SPEC §5 closure pattern (mirrors 05/05.1/05.2 + 06/06.1/06.2) + this STATE.md rewrite advancing to phase 08 lifecycle-state 1 with `next-skill: superpowers:brainstorming` per BOOTSTRAP §5 step 6 → step 1 transition. Per ADR-0003: master FF'd to this phase-done commit's SHA-fill follow-up SHA. SHA filled in a follow-up commit per the phase-04..07.1 SHA-fill convention.
- **last-updated:** 2026-05-02

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
