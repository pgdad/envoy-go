# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `20-http-filter-oauth2` — phase `20-http-filter-oauth2` **SPEC done at 2026-05-17** (predecessor BRAINSTORM done at 2026-05-17 with the 10-question Q1-Q10 settled context per `docs/envoy-go/phases/20-http-filter-oauth2/BRAINSTORM.md`). The SPEC session executed the §11 12-pin v1.37.2 empirical-pin scrape via parallel-subagent fan-out (disposition matrix: 6 RATIFIED + 4 REFUTED + 1 PARTIAL → SPEC-decided + 1 RATIFIED-AS-ABSENT): **§20.P1 RATIFIED** (zero-retry default at token_endpoint POST); **§20.P2 RATIFIED** (host-only cookie when `cookie_domain` empty); **§20.P3 PARTIAL → SPEC-decided** (POST callback PARSE-REJECT envoy-go-strict; GET-only matches upstream practice); **§20.P4 REFUTED** (5-input newline-joined HMAC `HMAC-SHA256(hmac_secret, StrJoin({domain, expires, token, id_token, refresh_token}, "\n"))` — NOT 3-input per BRAINSTORM Q9; id_token + refresh_token empty when absent); **§20.P5 REFUTED** (AES-256-CBC NOT AES-GCM per BRAINSTORM Q4; SHA-256(hmac_secret)[:32] key; random 16-byte IV prepended; PKCS#7 padding; Base64URL(IV ‖ CT) envelope); **§20.P6 RATIFIED** (`core.ConfigSource.PathConfigSource` oneof arm field 8 wraps `{path, watched_directory}`; deprecated `ConfigSource.path` field 1 PARSE-REJECT); **§20.P7 RATIFIED** (no `OAuth2PerRoute` message in v1.37.x proto; HCM-parse-time PARSE-REJECT); **§20.P8 REFUTED** (6 counters wire-exact upstream NOT 8 per BRAINSTORM §5 over-count: `oauth_unauthorized_rq` + `oauth_failure` + `oauth_passthrough` + `oauth_success` + `oauth_refreshtoken_success` + `oauth_refreshtoken_failure`; `signout_completed` + `cookie_decrypt_failure` ABSENT); **§20.P9 REFUTED** (401 body constant `"OAuth flow failed."` 18 bytes; NEVER 500 anywhere; token_endpoint failure → 302 OR 401); **§20.P10 RATIFIED** (byte-exact 4-field auth-code template `grant_type=authorization_code&code={0}&client_id={1}&client_secret={2}&redirect_uri={3}` MVP-gated per S3 + 3-field refresh-token template; `:/=&?` percent-encoded; spaces as %20); **§20.P11 RATIFIED-AS-ABSENT** (upstream decryption-failure fall-back returns ciphertext-as-plaintext; no `cookie_decrypt_failure` counter; envoy-go-strict departure flag CLOSED); **§20.P12 RATIFIED** (proto default `csrf_token_expires_in=600s` = 10 minutes; MVP uses proto-default fall-through). **5 SPEC-time questions settled**: S1 AES-256-CBC mirror upstream byte-exact (vs BRAINSTORM Q4 AES-GCM); S2 drop runtime gate (envoy-go has no runtime-features layer; `disable_token_encryption=false` proto-default is sufficient; upstream's `envoy.reloadable_features.oauth2_encrypt_tokens` reloadable-features gate NOT modeled); S3 gated PKCE template (4-field MVP / 5-field future); S4 dual-encoding HMAC (emit Base64; accept BOTH Base64 + HexBase64 on read; ~30 LoC extra); S5 6-counter wire-exact + ADR-0045 single-row decision (NOT split into 20.1+20.2 per BRAINSTORM-anticipated MEDIUM-HIGH lean — phase-17 jwt_authn 3855 single-row precedent applies; LoC envelope re-estimated post-empirical-scrape at ~3400-3900 — slightly tighter than BRAINSTORM's ~3500-4000 due to S1/S2/S5 simplifications). **§1.1 AMEND-1..AMEND-7 amendment block cataloged**: AMEND-1 Q4→AES-256-CBC algorithm swap (per §20.P5 REFUTED; ADR-0182 rewrites); AMEND-2 Q9→5-input HMAC composition (per §20.P4 REFUTED; dual-encoding read per S4; ADR-0179 anchors); AMEND-3 deny-path simplification (302+401 only no-500 anywhere; constant 401 body; `addFlowCookieDeletionHeaders` on 401-with-flow_id_; token_endpoint non-2xx retry-eligible → 302 challenge envoy-go-strict simplification from upstream `redirectToOAuthServer-retry`; decryption-failure fall-back per §20.P11; ADR-0180 anchors); AMEND-4 stat surface 86 → 92 names 6-counter wire-exact (drops `signout_completed` + `cookie_decrypt_failure`; ADR-0181 anchors; CLOSES §20.P11 as RATIFIED-AS-ABSENT); AMEND-5 PKCE-gated 4-field auth-code + 3-field refresh-token templates (spaces as %20; PercentEncoding charset `:/=&?`; ADR-0185 anchors); AMEND-6 BRAINSTORM field-locus corrections (C1 `cookie_domain` is on OAuth2Credentials field 5 NOT OAuth2Config; C2 `cookie_configs` is `*CookieConfigs` wrapper NOT raw map; C3 `forward_bearer_token` OAuth2Config field 7 MVP-CONSUMED); AMEND-7 per-route + new deferral (HCM-parse-time PARSE-REJECT for route-level placement; NEW deferred surface — `Partitioned` cookie attribute depends on `cookie_configs`). **9 NEW ADRs landed at SPEC commit** (§Context drafts per ADR-0044; §Decision + §Consequences bodies land at IMPL): ADR-0177 NEW `internal/httpclient/` framework primitive (Options + RetryPolicy + Client.Do; closes ADR-0159 §Future Work forward-pointer load-bearing — third-consumer trigger fired per Q2 EXTRACT NOW; 3 consumers at introduction time — jwks Fetcher post-ADR-0150 AMENDMENT + extauthz httpAuthClient post-ADR-0159 AMENDMENT + oauth2 token_endpoint POST NEW); ADR-0178 NEW `internal/sdsfile/` framework primitive (Watcher + fsnotify + atomic-swap; consumes `generic_secret.inline_string` only; PARSE-REJECT non-filesystem ConfigSource arms + deprecated path field + secret_file indirect-arm; ~100ms debounce; NEW go.mod dep `github.com/fsnotify/fsnotify`); ADR-0179 oauth2 HMAC cookie composition (5-input newline-joined per AMEND-2; dual-encoding read per S4; constant-time-compare); ADR-0180 oauth2 state-machine + deny-path 302+401 only no-500 per AMEND-3 + listener-scoped-only via HCM-parse-time PARSE-REJECT + explicit NO-ADR-0125-AMENDMENT REUSE-by-absence classification (THIRD CONSECUTIVE §9 row after phase 18 + phase 19); ADR-0181 oauth2 cookie envelope (5-of-7 CookieNames consumed) + 6-counter wire-exact stat surface (86 → 92 names per AMEND-4 + S5; CLOSES §20.P11 as RATIFIED-AS-ABSENT); ADR-0182 NEW filter-local AES-256-CBC token-encryption helper (per AMEND-1; `disable_token_encryption=true` skip-path; decryption-failure fall-back per AMEND-3); ADR-0183 refresh-token rotation timing + race-vs-rotation discipline (no per-stream serialization; latest Set-Cookie wins; counter increment one-per-event — refresh-failure → 302 challenge NOT also `oauth_failure`); ADR-0184 sign-out flow (`signout_path` handling + full envelope clearing Max-Age=0 + `deny_redirect_matcher` integration; category (c) 302; no separate `signout_completed` counter); ADR-0185 token_endpoint POST body templates per AMEND-5 (4-field auth-code MVP + 3-field refresh-token byte-exact; PKCE-gated 5th field for future; NEW `urlEncode` custom helper). **2 IN-PLACE §Decision AMENDMENT-anticipation paragraphs at ADR-0150 + ADR-0159 per ADR-0044** (AMENDMENT bodies + ADR-0159 §Future Work CLOSURE-AT-PHASE-20 paragraph land at IMPL Task 2 alongside ADR-0177 introduction; refactor delta ~40-60 LoC jwks + ~50-80 LoC extauthz). **FIRST §9 family-row to CLOSE a prior-phase load-bearing forward-pointer** (ADR-0159 §Future Work "third outbound-HTTP consumer triggers `internal/httpclient/` extraction" → closed at phase 20 per Q2 EXTRACT NOW + ADR-0177 introduction; structurally important demonstration that ADR-0044 §Future-Work forward-pointer-and-close discipline functions across phase boundaries per BRAINSTORM §11 Lesson (d)). **THIRD CONSECUTIVE §9 row to skip ADR-0125 amendment** (REUSE-by-absence stronger form than phase-18 + phase-19 5th-canonical REUSE — no per-route surface at all per §20.P7; listener-scoped-only enforcement via HCM-parse-time PARSE-REJECT per §5.2). Stat surface 86 → 92 names (NOT 94 per BRAINSTORM §5 over-count). 9-scenario differential fixture `0024-http-oauth2` per SPEC §7 (a sign_in_happy_path; b1+b2 cookie_passthrough; c pass_through_matcher; d refresh_token_rotation; e signout_flow; f bad_state_401; g token_endpoint_5xx_302; h token_endpoint_4xx_401; i disable_token_encryption_true) + 2-or-3-listener topology + NEW `test/helpers/oauthbackend/` package (~250-350 LoC; Scripted{Authz,Token}Responses + ValidCookieEnvelope + TamperedStateCookie helpers; stdlib `net/http/httptest`-based) + 26th fuzzer `FuzzOAuth2ConfigParse` (count 25 → 26 post-phase-20). Six-gate checklist (A build / B vet+lint / C race / D differential / E fuzz / F h2spec) + cross-package regression matrix (jwt_authn fixture 0019 + extauthz HTTP fixture 0020 stay GREEN post-refactor; extauthz gRPC fixture 0021 untouched). SPEC §15 18-item acceptance checklist for the IMPL phase-done reviewer. SPEC §13 BEHAVIOR_CONTRACT 10-edit bundle anticipated (NEW `### envoy.filters.http.oauth2` subsection ~250-350 LoC + NEW `## HTTP outbound framework primitive (per phase 20 ADR-0177)` umbrella + NEW `## Filesystem-SDS framework primitive (per phase 20 ADR-0178)` umbrella + stat-table 86→92 extension + ADR-0125 cross-reference paragraph + ADR-0159 forward-pointer CLOSURE paragraph + 2 envoy-go-strict departure records [token_endpoint non-2xx → 302 simplification; POST callback PARSE-REJECT] + NEW `### Phase 20 forward-pointer notes` subsection + ADR-0150 jwks REFACTORED-AT-PHASE-20 paragraph). 8 RATIFIED-PENDING-IMPL-TIME items deferred to IMPL per SPEC §12 (5 wire-shape byte-confirmations: 401 Content-Type + no-trailing-newline; Set-Cookie attribute byte-exact upstream defaults; state-cookie payload byte-exact shape; HCM SendLocalReply Content-Type default; urlEncode charset for non-ASCII bytes — plus 2 library-behavioral: AES-CBC PKCS#7 decrypt-failure semantics; fsnotify debounce window — plus 1 cross-phase regression: ADR-0150 + ADR-0159 refactor regression matrix). ADR-0044 escape-valve in reserve for ~0-2 impl-time-unanticipated ADRs (most-likely surfaces: AES-256-CBC PKCS#7 padding-oracle hardening; fsnotify event-debounce edge-cases; urlEncode charset edge-cases). Anticipated total LoC ~3400-3900 (filter proper ~2850-3110 + framework ~550-790 NEW + 2 in-place refactors). 16 Go files (10 production + 6 test) per SPEC §6.11.
- **phase-directory:** `docs/envoy-go/phases/20-http-filter-oauth2/` (BRAINSTORM.md at 489 lines from the prior session; SPEC.md authored at this session at ~831 LoC mirroring phase-19.1 SPEC structural precedent; PLAN.md + PROGRESS.md + REVIEW.md follow at subsequent lifecycle transitions; ROADMAP row 20 stays single-row per ADR-0045 single-row decision; no 20.1/20.2 sub-directories per the SPEC author's split disposition).
- **lifecycle-state:** `phase 20 SPEC done; phase 20 PLAN pending (next session lifecycle-state 2 → 3 per SKILL_ROUTING)`. ROADMAP row `20` stays `in-progress` at this commit (per-cell SPEC-done annotation added; status flips to `done` only at IMPL phase-done with all 6 gates GREEN per the SPEC §15 18-item acceptance checklist). DECISIONS.md tail advances to ADR-0185 (9 NEW ADRs consumed at SPEC: ADR-0177..ADR-0185 with §Context drafts only per ADR-0044; §Decision + §Consequences bodies land at IMPL); plus 2 IN-PLACE §Decision AMENDMENT-anticipation paragraphs at ADR-0150 + ADR-0159 (no NEW numbers consumed); next-free `ADR-0186`. Phase-19 parent + sub-phases 19.1 + 19.2 all `done` at the upstream-of-this-commit master tip `f3c3ecc` (the phase-20 BRAINSTORM follow-up STATE.md SHA-fill commit) — the immediate predecessor on master for this SPEC commit.
- **next-skill:** `superpowers:writing-plans` (scoped to THIS phase; outputs PLAN.md per SKILL_ROUTING.md state 3 — `superpowers:writing-plans` invoked for the PLAN-authoring session per the phase-09..19.2 cadence convention). Cold-start scope for the phase-20 PLAN session: read STATE.md (this file); `docs/envoy-go/phases/20-http-filter-oauth2/SPEC.md` (load-bearing input — the 15-section SPEC + the §1.1 AMEND-1..AMEND-7 amendment block + the §10 9-NEW-ADR + 2-IN-PLACE-AMENDMENT anchor map + the §11 12-pin disposition matrix + the §12 8-item RATIFIED-PENDING-IMPL-TIME list + the §13 10-edit BEHAVIOR_CONTRACT bundle + the §14 testing strategy + the §15 18-item acceptance checklist); `docs/envoy-go/phases/20-http-filter-oauth2/BRAINSTORM.md` (the predecessor 10-question Q1-Q10 settled context — historical reference); `docs/envoy-go/ROADMAP.md` row 20 (per-cell SPEC-done annotation; verbose row description captures the 9 NEW ADRs + 2 IN-PLACE AMENDMENTs + the 12-pin disposition matrix + the 5 S1-S5 settled questions + the 7 AMEND-N entries + the SPEC §15 18-item acceptance checklist); `docs/envoy-go/BEHAVIOR_CONTRACT.md` (the 10-edit bundle lands at IMPL phase-done per SPEC §13 — review the existing subsections that get amended: `### Phase 19.2 forward-pointer notes` for the `### Phase 20 forward-pointer notes` placement; `## HTTP outbound auth-check framework note (per phase 18.1 ADR-0159)` for the CLOSURE-AT-PHASE-20 paragraph placement; `## JWKS framework primitive (per phase 17 ADR-0150)` for the REFACTORED-AT-PHASE-20 paragraph placement; `## Per-route canonical patterns cross-reference` for the table-caption + phase-20 cross-reference paragraph; the 86-name stat table for the 86→92 extension); `docs/envoy-go/DECISIONS.md` ADR-0177..ADR-0185 §Context drafts (the 9 NEW ADRs landed at this SPEC commit; the PLAN session translates each §Context to per-Task Lands-in-Tasks per ADR-0044) + ADR-0150 + ADR-0159 §Decision AMENDMENT-anticipation paragraphs (the 2 IN-PLACE AMENDMENTs land at IMPL Task 2 per SPEC §10 B + §3.4 + §3.5); ADR-0044 (in-place edit discipline) + ADR-0045 (single-row decision settled at this SPEC) + ADR-0106 (flat §9 family-row convention) + ADR-0080 (envoy-go-strict discipline — 2 new departures recorded at SPEC §13). The PLAN session's defining obligations per SPEC §6.11 file layout + §10 ADR anchor map: produce a 10-12-task IMPL PLAN with per-task Lands-in-Tasks for each of the 9 NEW ADRs + 2 IN-PLACE AMENDMENTs + the 4 grouped IMPL deliverables (Task 1 PROGRESS preamble + 6-precondition verification; Task 2 NEW `internal/httpclient/` + paired ADR-0150 + ADR-0159 in-place refactors; Task 3 NEW `internal/sdsfile/`; Task 4 HMAC composition; Task 5 state-machine + deny-path + listener-scoped + dispatch; Task 6 cookie envelope + stats; Task 7 AES-256-CBC; Task 8 refresh-token rotation + race-tests; Task 9 sign-out flow; Task 10 token_endpoint POST templates + urlEncode helper; Task 11 ADR final-state alignment + DECISIONS edits; Task 12 differential fixture 0024 + test-helper + fuzzer; Task 13 BEHAVIOR_CONTRACT 10-edit bundle + SPEC §15 + §12 closures; Task 14 six-gate phase-done verification + STATE/ROADMAP advance). Post-PLAN next-skill: `superpowers:executing-plans` (or `superpowers:subagent-driven-development` per project memory `feedback_execution_style.md`) for the phase-20 IMPL session (lifecycle-state 3 → 6 — IMPL spans 3→4→5→6 per SKILL_ROUTING).
- **last-commit:** `4df55be` — `Squash merge phase-20-http-filter-oauth2-spec`. Phase-done squash-merge of branch `phase-20-http-filter-oauth2-spec` from worktree `.worktrees/phase-20-http-filter-oauth2-spec/` (single worktree commit `a746b16` bundled into the squash; 4 files / +1218 / -7). This SHA-fill follow-up commit per the phase-09..19.2 convention: `phase 20 SPEC follow-up: STATE.md SHA-fill (TBD → 4df55be post-squash)`.
- **last-updated:** 2026-05-17
- **next-free ADR:** `ADR-0186` — advanced from `ADR-0177` per the 9 NEW ADRs consumed at SPEC (ADR-0177 NEW `internal/httpclient/` + ADR-0178 NEW `internal/sdsfile/` + ADR-0179 HMAC composition + ADR-0180 state-machine + deny-path + listener-scoped + ADR-0181 cookie envelope + stat surface + ADR-0182 AES-256-CBC + ADR-0183 refresh-token rotation + ADR-0184 sign-out flow + ADR-0185 token_endpoint POST templates). §Context drafts anchor at this SPEC commit per ADR-0044; §Decision + §Consequences bodies land at IMPL per ADR-0044. Plus 2 IN-PLACE §Decision AMENDMENT-anticipation paragraphs at ADR-0150 + ADR-0159 (AMENDMENT bodies + ADR-0159 §Future Work CLOSURE-AT-PHASE-20 paragraph land at IMPL Task 2). ADR-0044 escape-valve held in reserve at IMPL-time for ~0-2 impl-time-unanticipated ADRs per SPEC §10 C (most-likely surfaces: AES-256-CBC PKCS#7 padding-oracle hardening if surfaces as load-bearing — low-probability per AMEND-3 fall-through semantics; fsnotify event-debounce semantics under multi-rapid-write races — low-probability per ~100ms debounce envelope + Go race-test coverage; `urlEncode` charset-edge-case if non-ASCII bytes appear in `client_secret` or `code` — low-probability per stdlib UTF-8 escaping at the helper).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
