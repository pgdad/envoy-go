# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `05.2-upstream-h2`
- **phase-directory:** `docs/envoy-go/phases/05.2-upstream-h2/` — now contains `README.md` (the illustrative scope-sketch from the planner-time split per ADR-0045) AND `SPEC.md` (this commit, drafted by `superpowers:brainstorming` per ADR-0004 autonomous mode; spec-document-reviewer subagent approved at iteration 2 of the up-to-3 budget). PLAN.md, PROGRESS.md, REVIEW.md will land in subsequent sessions. The sibling `docs/envoy-go/phases/05.1-downstream-h2/` remains closed read-only history (ROADMAP row `05.1` status `done`). The parent `docs/envoy-go/phases/05-http-2/` retains its `SPEC.md` (`612cdea`) as the master design document for both sub-phases per ADR-0045; that directory remains closed read-only history at the parent level (PLAN/PROGRESS/REVIEW land under the sub-phase directories). The parent ROADMAP row `05-http-2` stays `in-progress` until 05.2 reaches `done`; the 05.2 phase-done commit will close both rows on the same commit per the 05.2 SPEC §4.4.
- **lifecycle-state:** `2` — phase 05.2's `SPEC.md` exists, `PLAN.md` does not. Per BOOTSTRAP §5 the next transition is state 2 → 3 via `superpowers:writing-plans` (in adapted-autonomous-mode per ADR-0005), which (a) writes `PLAN.md` under this phase-directory at the project layout per ADR-0005 §2, (b) runs the `plan-document-reviewer` subagent loop (up to three iterations per ADR-0005 §3), (c) advances STATE.md to lifecycle-state 3 with `next-skill = superpowers:subagent-driven-development` per ADR-0005 §4. ROADMAP row `05.2-upstream-h2` was flipped `planned → in-progress` at this SPEC commit per BOOTSTRAP §4.1 invariant 3 (the corrected pattern from phase-05's `8d18320` and 05.1's SPEC commit; no ADR needed — the BOOTSTRAP invariant prescribes the timing).
- **next-skill:** `superpowers:writing-plans` — produces `docs/envoy-go/phases/05.2-upstream-h2/PLAN.md` per ADR-0005 autonomous-mode adaptation. Inputs: this commit's `SPEC.md` at `docs/envoy-go/phases/05.2-upstream-h2/SPEC.md`, the master phase-05 SPEC at `docs/envoy-go/phases/05-http-2/SPEC.md` (`612cdea`), the closed-history 05.1 phase artefacts (`SPEC.md`, `PLAN.md`, `PROGRESS.md`, `REVIEW.md`) for cross-reference (especially 05.1's PLAN.md as a precedent for task shape and TDD discipline). PLAN review loop is the `plan-document-reviewer` subagent per ADR-0005 (up to three iterations; on non-approval after three, set `lifecycle-state: blocked` and exit per ADR-0005 §3).
- **next-skill-scope:** Draft `docs/envoy-go/phases/05.2-upstream-h2/PLAN.md` covering the 05.2 SPEC's deliverables enumerated in §4 (new production code: `internal/filter/hcm/h2/client.go`, `internal/cluster/dial_h2.go`, `routerActionH2` in `actions.go`; changed production code: `cluster/cluster.go` + `cluster/manager.go` for `HttpProtocolOptions` parsing + `UseH2()` accessor, `bootstrap/bootstrap.go` blank import, the 05.1-codec-internal flow-control tightening per ADR-0055 in `flow.go`/`conn.go`/`stream.go`/`framer.go`/`settings.go`; new harness: fixture `0004-h2-routing/` in full + `test/helpers/h2.go`; documentation: in-place edit of `BEHAVIOR_CONTRACT.md ## HTTP/2` per ADR-0052 + new ADRs ADR-0055..ADR-0058 in `DECISIONS.md`). Per BOOTSTRAP §6 the planner runs the size estimate at PLAN write time; the SPEC §11.1 anticipates ~12–15 TDD tasks + ~1300–1700 LoC (likely under the ~25-task / ~1500-LoC gate; if over, the SPEC §11.1 recommends the split-by-ADR-scope axis: 05.2.1 = ADR-0055 only, 05.2.2 = rest). The planner verifies the next-free ADR number is ADR-0055 (based on `DECISIONS.md` tail at `bc4fca4` being ADR-0054). The planner ALSO consumes the 05.1 REVIEW carry-forwards as enumerated in the 05.2 SPEC §12: §12.1's eight items (I-1/I-2/I-3 + M-3/M-5/M-7/M-9/M-11) bundle into ADR-0055 per SPEC §1 #6 + §5.6; §12.2's deferred items (M-4/M-8/M-10/M-12) carry forward per per-finding disposition (M-8 folded into 05.2 PLAN as a small task per Path A wording; the rest deferred to later phases); §12.3's monotonic-id-reuse integration test lands as a small task in PLAN. Default executor preference per ADR-0005 §4 is `superpowers:subagent-driven-development`.
- **last-commit:** this commit — `phase 05.2: SPEC drafted (autonomous brainstorming per ADR-0004; spec-document-reviewer subagent approved at iteration 2)`, on branch `phase/05.2-upstream-h2-spec`, branched from master `bc4fca4` per ADR-0003. Lands `docs/envoy-go/phases/05.2-upstream-h2/SPEC.md` (~1100 lines), updates ROADMAP.md row `05.2-upstream-h2` from `planned` to `in-progress` (parent row `05-http-2` stays `in-progress`; row `05.1-downstream-h2` stays `done`), and advances STATE.md to phase 05.2-upstream-h2 lifecycle-state 2. Master is fast-forwarded to this commit per ADR-0003.
- **last-updated:** 2026-04-26

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
