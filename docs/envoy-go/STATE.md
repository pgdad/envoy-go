# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `22-http-filter-lua` (parent row in-progress; sub-phase `22.1-http-filter-lua-vm-and-headers-bridge` done at master tip `c986419`; sub-phase `22.2-http-filter-lua-full-bridge` done at master tip `46183a4`; sub-phase `22.3-http-filter-lua-multi-script-and-per-route` **SPEC done at this commit** — awaiting 22.3 PLAN). Phase `22-http-filter-lua` parent BRAINSTORM done at parent BRAINSTORM commit + parent SPEC done at `41ccee7`; 22.1 lifecycle traversed BRAINSTORM (parent) → SPEC at `a7021aa` → PLAN at `02d745a` → IMPL at `d30f131` → done. Phase `22.2` lifecycle traversed BRAINSTORM at `6ad3064` → SPEC at `0d6463e` → PLAN at `269dee1` → IMPL at `46183a4` → done. Phase `22.3` lifecycle traverses 1 → 2 at THIS commit per BOOTSTRAP §4.1 invariant 1 (BRAINSTORM done at `0929b3d`; SPEC done at THIS commit; awaiting PLAN). **22.3 SPEC done at 2026-05-21** — authored `docs/envoy-go/phases/22.3-http-filter-lua-multi-script-and-per-route/SPEC.md` (16-section authoritative SPEC) via the `superpowers:brainstorming`-scoped-to-SPEC session per the 22.1 + 22.2 BRAINSTORM→SPEC two-session split precedent. The SPEC ran the ADR-0004 §11 empirical-pin scrape (5 pins: v1.32.4 proto roster + upstream v1.37.2 `source_codes`/`LuaPerRoute` dispatch + gopher-lua compile/chunk-sharing + local per-route mechanism/jwt_authn precedent/BootRejectFixture + fuzzer count) resolving the 4 D-questions IN-SESSION (D1 compile-cache → content-hash reuse; D2 chunk-binding → per-stream O(1) lookup atop HCM-build-time-validated per-route config; D3 disabled → early-return both hooks; D4 → boot-reject scenarios for the genuine config-load arms). **The scrape surfaced AMEND-22.3-1: it REFUTED 22.3 BRAINSTORM decision #3 + parent SPEC §6.3 arm 7** — upstream Envoy lua silently no-ops a dangling per-route `name` at request time (`perLuaCodeSetup()` → `nullptr` → `LUA_REFNIL`), and the jwt_authn precedent the BRAINSTORM cited uses a split-semantic (ADR-0153) whose PER-ROUTE half is runtime-resolve, NOT config-load fail-fast. **User-confirmed disposition (this session): upstream-parity silent no-op** — DROPS the dangling-name PARSE-REJECT arm 7, 0 net-new BEHAVIOR_CONTRACT departure records, no HCM cross-resolution plumbing. The other 7 LOCKED BRAINSTORM decisions STAND (1 combined NEW ADR-0193 + ADR-0125 §(xiv) AMENDMENT + no-reserved-name [arm 3 dropped] + fixture-0028 5-tier cross-side + `FuzzLuaPerRouteConfig` [30→31] + WEAK HOLD escape-valve at conditional ADR-0194 + single-phase). **0 net-new stats** (SHARED-vacuous; stat count STAYS 107). **ADR-0193 §Context draft anchored at THIS SPEC commit** per ADR-0044 (DECISIONS.md tail advances ADR-0192 → ADR-0193 §Context; next-free ADR-0194). 3 residual PLAN-time D-questions (D-P1 per-route `*Chunk` storage wiring + D-P2 fixture boot-reject cross-side-vs-subject-only roster + D-P3 config-load arm enumeration).
- **phase-directory:** `docs/envoy-go/phases/22-http-filter-lua/` (parent — BRAINSTORM.md + SPEC.md authored at parent SPEC commit; parent phase row stays in-progress until 22.3 phase-done per parent SPEC §1 closure pattern). Sub-phase directories: `docs/envoy-go/phases/22.1-http-filter-lua-vm-and-headers-bridge/` (SPEC.md + PLAN.md + PROGRESS.md + REVIEW.md from 22.1 lifecycle; LANDED at master tip `c986419`) + `docs/envoy-go/phases/22.2-http-filter-lua-full-bridge/` (SPEC.md + PLAN.md + PROGRESS.md + REVIEW.md + BRAINSTORM.md from 22.2 lifecycle; LANDED at master tip `46183a4`) + `22.3-http-filter-lua-multi-script-and-per-route/BRAINSTORM.md` **authored at THIS commit** (12-section dialogue: §1 mission/scope + §2 per-topic design decisions [12 sub-sections] + §3 framework-survey [0 new primitives] + §4 per-route 9th-canonical detail + §5 stat surface [0 net-new] + §6 fixture-0028 + §7 anticipated ADRs + §8 deferred items + §9 cross-references + §10 D-questions + §11 lessons applied + §12 closeout) + `22.3-http-filter-lua-multi-script-and-per-route/SPEC.md` **authored at THIS commit** (16-section authoritative SPEC: §1 mission + §1.1 empirical-finding-driven scope [AMEND-22.3-1] + §2 non-purposes + §3 framework-survey [0 new primitives] + §4 9th-canonical per-route shape + §5 proto roster [SourceCodes + LuaPerRoute CONSUMED] + §6 PARSE-REJECT roster [arms 3+7 dropped] + §7 stat surface [0 net-new] + §8 fixture-0028 + §9 behavior-contract delta + §10 deferred + §11 5-pin empirical block + §12 PLAN D-questions + §13 RATIFIED-PENDING + §14 BEHAVIOR_CONTRACT bundle + §15 IMPL acceptance + §16 ADR-0193 §Context anchor) + `22.3-http-filter-lua-multi-script-and-per-route/README.md` (updated at THIS commit — Status now "22.3 SPEC done at 2026-05-21; awaiting 22.3 PLAN") + `22.3-http-filter-lua-multi-script-and-per-route/BRAINSTORM.md` (UNCHANGED from `0929b3d`).
- **lifecycle-state:** `phase 22.3 SPEC done; awaiting 22.3 PLAN`. ROADMAP row `22.3` STAYS `in-progress` at this commit per ADR-0106 (already flipped `planned → in-progress` at the 22.3 BRAINSTORM-done commit `0929b3d`; SPEC-done does not change row status per BOOTSTRAP §4.1 invariant 3 — matches the 22.1/22.2 SPEC-done precedent). Parent row `22` STAYS `in-progress` until 22.3 phase-done; there are no further sub-rows (22.3 is the final sub-phase; parent row 22 closes at 22.3 IMPL phase-done). **DECISIONS.md tail advances ADR-0192 → ADR-0193 §Context draft** at THIS SPEC commit per ADR-0044 §Context-draft discipline (1 number consumed; §Decision + §Consequences body lands at 22.3 IMPL). ADR-0188 + ADR-0189 + ADR-0190 + ADR-0191 + ADR-0192 + ADR-0177 + ADR-0125 §(xiv) UNCHANGED at this commit. Next-free **ADR-0194**. **22.3 SPEC authored at THIS commit** as `docs/envoy-go/phases/22.3-http-filter-lua-multi-script-and-per-route/SPEC.md` (16-section authoritative SPEC matching the 22.1 + 22.2 SPEC structural template; runs the ADR-0004 §11 5-pin empirical scrape; resolves D1-D4 IN-SESSION; surfaces AMEND-22.3-1 [dangling-name = upstream-parity silent no-op, user-confirmed]; anchors the ADR-0193 §Context draft). **NO production code lands at SPEC** per ADR-0044 — only SPEC.md + the ADR-0193 §Context draft + STATE/README updates.
- **next-skill:** `superpowers:writing-plans` (scoped to 22.3 PLAN per SKILL_ROUTING state-2 entry "Phase in ROADMAP, directory exists, SPEC.md exists, PLAN.md does not exist → superpowers:writing-plans scoped to THIS phase → output: PLAN.md" — the SPEC→PLAN transition per the phase-22.1 + phase-22.2 precedent at master tips `02d745a` + `269dee1`). Cold-start scope for the next session: read STATE.md (this file) + `docs/envoy-go/ROADMAP.md` (row 22.1 done; row 22.2 done; row 22.3 in-progress; row 22 in-progress) + `docs/envoy-go/phases/22.3-http-filter-lua-multi-script-and-per-route/SPEC.md` (THIS commit's 16-section authoritative SPEC — load-bearing for the PLAN: §3.3 file roster + §6 PARSE-REJECT roster + §8 fixture-0028 + §11 empirical closures + AMEND-22.3-1 + §12 PLAN D-questions [D-P1/D-P2/D-P3] + §13 RATIFIED-PENDING [R6 *LState-pool gate + R-P1/R-P2/R-fuzz/W3] + §15 IMPL acceptance checklist) + `docs/envoy-go/phases/22.3-http-filter-lua-multi-script-and-per-route/BRAINSTORM.md` (the 8 LOCKED decisions; decision #3 REVISED per AMEND-22.3-1) + `docs/envoy-go/phases/22.2-http-filter-lua-full-bridge/{PLAN.md,REVIEW.md}` (predecessor sub-phase PLAN structural precedent + §5 R6 disposition carry-forward) + `docs/envoy-go/phases/22.1-http-filter-lua-vm-and-headers-bridge/PLAN.md` (predecessor sub-phase PLAN precedent) + `docs/envoy-go/phases/22-http-filter-lua/SPEC.md` (parent SPEC §3.0 task/LoC estimate vs ADR-0045 split-gate) + DECISIONS.md tail (ADR-0193 §Context draft; next-free ADR-0194; ADR-0125 §(xiv) anticipation paragraph; ADR-0045 split-gate; ADR-0044 §Decision-body-at-IMPL discipline) + BEHAVIOR_CONTRACT.md `### envoy.filters.http.lua` (22.1 + 22.2 subsections; 22.3 adds 0 departure records per §9). The 22.3 PLAN session creates a fresh worktree per project memory `feedback_git_worktrees.md`: `git worktree add /home/esa/git/envoy-go/.worktrees/phase-22.3-http-filter-lua-multi-script-and-per-route-plan -b phase-22.3-http-filter-lua-multi-script-and-per-route-plan <22.3-SPEC-tip-SHA>` per the phase-22.1-PLAN + phase-22.2-PLAN sub-phase-PLAN worktree precedent. The PLAN session fires the ADR-0045 split-gate only if estimates exceed (~25 tasks / ~1500 LoC); 22.3 is the SMALLEST sub-phase (~600-900 LoC; 1 new production file `perroute.go`) so a single-phase 22.3 is anticipated.
- **last-commit:** `TBD` (post-squash SHA-fill). Phase 22.3 SPEC squash-merge of branch `phase-22.3-http-filter-lua-multi-script-and-per-route-spec` from worktree `.worktrees/phase-22.3-http-filter-lua-multi-script-and-per-route-spec/` lands as the squash-merge commit on master per the phase-09..22.2 SPEC squash-merge convention. The SHA-fill follow-up commit backfills this placeholder per the phase-09..22.2 convention: `phase 22.3 SPEC follow-up: STATE.md SHA-fill (TBD → <squash-SHA> post-squash)`. Predecessor master tip: `1ca1645` — `phase 22.3 BRAINSTORM follow-up: STATE.md SHA-fill (TBD → 0929b3d post-squash)`. Pre-predecessor: `0929b3d` — `Squash merge phase-22.3-http-filter-lua-multi-script-and-per-route-brainstorm`.
- **last-updated:** 2026-05-21
- **next-free ADR:** `ADR-0194` — at this 22.3 SPEC commit **ADR-0193 §Context draft anchored** per ADR-0044 §Context-draft discipline (1 number consumed; §Decision + §Consequences body lands at 22.3 IMPL atomic-landing Task). ADR-0193 = NEW combined `internal/filter/http/lua/` 22.3 multi-script + per-route surface (`Lua.SourceCodes` named-script map consume + `LuaPerRoute` 9th-canonical 3-arm oneof + per-route 3-tier dispatch + **upstream-parity dangling-`name` silent no-op per AMEND-22.3-1** [REVISES the BRAINSTORM HCM-build-time-PARSE-REJECT disposition] + no-reserved-name disposition + fixture-0028 + `FuzzLuaPerRouteConfig`; ONE ADR per the 22.1 one-package-shape-ADR economy ADR-0189). Anticipated remaining 22.3 IMPL ADR landings: **1 IN-PLACE AMENDMENT** on `ADR-0125 §(xiv)` (canonical-per-route roster grows 8 → 9; the 9th canonical specification [3-arm hybrid] + SHARED stat-discipline + 9-shape roster table + lua-row first-use citation; NO new ADR number; lands at 22.3 IMPL final Task; the AMENDMENT-anticipation paragraph at parent SPEC commit STANDS UNCHANGED) + **1 conditional ADR-0194** (escape-valve slot per the WEAK HOLD — fires only if the §13-R6 *LState-pool gate trips at the 22.3 IMPL benchmark Task; LIKELY STAYS UNCONSUMED — the SPEC §11 scrape confirmed per-route resolution is an O(1) `Resolve` + content-hash cache lookup, not a new per-stream VM construction). **ADR-0188 EXPLICIT API-REVISION ALLOWANCE clause STAYS scoped to consumer-#2** (UNCHANGED at 22.3 — per-route + multi-script are dispatch-surface extensions, not `internal/lua/` VM-API revisions). Phase-22.3's WEAK HOLD escape-valve buffer at THIS SPEC commit: 0-1 anticipated consumption (ADR-0194 conditional); re-evaluates at 22.3 IMPL benchmark Task.

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
