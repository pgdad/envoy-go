# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `<unset — next session resolves>` — phase 11 done at this commit. ROADMAP row 11 status flipped `in-progress → done` per BOOTSTRAP_PROMPT.md §4.1 invariant 3. Phase 11 was the FOURTH §9 family-row to land (after cors @ 07.1, fault @ 09, header_mutation @ 10); the §9 family heading at ROADMAP line 56 stays unchanged per ADR-0106. The next §9 family-child (compression / jwt_authn / rbac / etc — local_ratelimit is now landed and is no longer a candidate) is the brainstormer's choice.
- **phase-directory:** `docs/envoy-go/phases/11-http-filter-local-ratelimit/` — contains `BRAINSTORM.md` (605 lines) + `SPEC.md` (1225 lines) + `PLAN.md` (3900 lines) + `PROGRESS.md` (extensive narrative log spanning Tasks 1-15) + `REVIEW.md` (lands at Task 16 follow-up). Phase 11 produced ~700 LoC production code in `internal/filter/http/localratelimit/` (filter impl + tokenBucket primitive + lazy-cache per-route resolver) + ~430 LoC unit tests + 40 LoC fuzzer (`FuzzLocalRateLimitConfigParse` — fifteenth fuzzer overall) + ~770 LoC fixture (envoy.yaml + envoy-go.yaml + backend + driver + expectations + README) + framework deltas (~30 LoC `internal/stats.Registry.NewCounterIfAbsent` + ~30 LoC `internal/stats/name.go` Rule SN9) + 6 ADRs (ADR-0114..ADR-0119) + ADR-0073 amendment paragraph + ADR-0061 amendment paragraph. All 15 implementation tasks completed (Task 16 REVIEW.md follows at Task 16 commit).
- **lifecycle-state:** `awaiting next planning` — phase 11 fully closed; six gates green at this commit (build/vet/lint clean; race tests pass — all packages, 0 fails after one known-flake retry on the differential suite's port-allocation race; h2spec 53/53 PASS unchanged at the ADR-0051 pin; 15 fuzzers green at 30s budget — `FuzzLocalRateLimitConfigParse` re-validated at 6.3M execs / 30s clean; all 14 differential fixtures 0000-0013 PASS; BEHAVIOR_CONTRACT.md populated with §13.1 NEW `### envoy.filters.http.local_ratelimit` subsection + §13.2 22→26 stat-table extension + §13.3 timing-tolerances row + §13.4 equivalence-matrix row + §13.5 phase-11 forward-pointer notes).
- **next-skill:** `superpowers:brainstorming` per ADR-0106 (next §9 family-child cold-starts from the §9 heading + just-shipped phase-11 artefacts; no sibling-stub was authored).
- **next-skill-scope:** Cold-start scope for the next §9 family-child brainstorm session. Read first: `docs/envoy-go/ROADMAP.md` row 11 (now `done`) + the §9 family list at line 58 — local_ratelimit is now landed; remaining candidates include compression, jwt_authn, rbac, ext_authz, decompressor, etc.; `docs/envoy-go/BEHAVIOR_CONTRACT.md ## HTTP filter chain ### envoy.filters.http.local_ratelimit` subsection (the just-shipped contract); `docs/envoy-go/DECISIONS.md` ADR-0114..ADR-0119 (the 6 phase-11 ADRs) + ADR-0073 amendment paragraph (per ADR-0117) + ADR-0061 amendment paragraph (per ADR-0118 — Rule SN9 + `Registry.NewCounterIfAbsent`); `docs/envoy-go/phases/11-http-filter-local-ratelimit/REVIEW.md` (lands at Task 16 commit). Per ADR-0106 + the user's persistent preference recorded in `~/.claude/projects/-home-esa-git-envoy-go/memory/MEMORY.md`: the next family-child is the brainstormer's choice from §9 family list. Phase 11's IMPL-1 substitution finding (PROGRESS.md preamble — `LocalRateLimitPerRoute` proto absent upstream → use `*LocalRateLimit` directly) is the canonical reference for any future local-rate-limit-adjacent filter that might re-encounter the same proto-shape question. Phase 11's per-route lazy-cache + `NewCounterIfAbsent` mechanism (codified at ADR-0117) is the canonical reference for any future stateful per-route filter (e.g., a future `global_ratelimit` per-process bucket fallback if it lands).
- **last-commit:** TBD — `phase 11: http-filter-local-ratelimit [ADR-0114, ADR-0115, ADR-0116, ADR-0117, ADR-0118, ADR-0119]`. Lands envoy.filters.http.local_ratelimit under the 07.1 framework as the FOURTH §9 family-row (after cors @ 07.1, fault @ 09, header_mutation @ 10). All six phase-done gates green. Framework deltas: `internal/stats.Registry.NewCounterIfAbsent` (idempotent post-Freeze counter registration per ADR-0117 + ADR-0061 amendment); `internal/stats/name.go` Rule SN9 (filter-specific Prometheus tag-extractor `envoy_local_http_ratelimit_prefix` per ADR-0118). Differential fixture 0013-http-local-ratelimit green (4 scenarios: basic-allow / basic-rate-limited / refill-after-fill_interval ±10ms / per-route-override). Stats: 4 new counters per stat_prefix (22→26 stat-table extension). One IMPL-time correction settled at PROGRESS.md preamble IMPL-1: `LocalRateLimitPerRoute` proto absent upstream → all per-route TPFC entries use `*LocalRateLimit` directly (the SAME proto as listener-level configs); the lazy-cache key is `*LocalRateLimit` proto pointer. SHA-fill follow-up commit per phase-04..10 SHA-fill convention.
- **last-updated:** 2026-05-06

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
