# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `08.1-admin-endpoints`
- **phase-directory:** `docs/envoy-go/phases/08.1-admin-endpoints/` — already contained `SPEC.md` (lifecycle-state 1 → 2 deliverable from master `1f85b07`); THIS commit on branch `phase-08.1-admin-endpoints-plan` adds `PLAN.md` (the full implementation plan; lifecycle-state 2 → 3 deliverable). Sibling directories `docs/envoy-go/phases/08-admin-api-and-drain/` (parent master SPEC + lifecycle-state-1 BRAINSTORM) and `docs/envoy-go/phases/08.2-graceful-drain/` (sibling SPEC stub) unchanged. Phase 07 (parent + sub-phases 07.1 + 07.2), phases 04, 05 (+ 05.1, 05.2), and 06 (+ 06.1, 06.2) remain closed read-only history.
- **lifecycle-state:** `3` — phase 08.1 PLAN drafting session complete; lifecycle-state 2 → 3 transition lands. The PLAN distils SPEC §§4–7 deliverables + §11 empirical pins into 15 bite-sized TDD tasks (T1 PROGRESS preamble + ADR-0084; T2 `Bootstrap.ConfigPath` field; T3 `cluster.Manager.Clusters()` accessor + types; T4 `internal/admin/{headers,version}.go` shared helpers; T5 `admin.New` constructor widening + `bootTime` field + `WriteTimeout` 30s + four mux placeholder stubs + ADR-0085; T6 `configdump.go` + ADR-0086; T7 `clusters.go` + ADR-0087; T8 `listeners.go` (covered by ADR-0087); T9 `serverinfo.go` + ADR-0088; T10 `cmd/envoy-go/main.go` call-site update + `bs.ConfigPath` wiring; T11 four-endpoint smoke + method-discrimination Envoy-parity tests; T12 `TestAdminConcurrentScrapeRace` 100×4×1s; T13 `FuzzConfigDumpFormat` 30s short-budget per ADR-0018; T14 differential fixture `0009-admin-config-dump` + per-endpoint canonicalisation driver per planner-time decisions 7+8; T15 BEHAVIOR_CONTRACT umbrella restructure + ADR-0089 + ADR-0090 + six-gate verification sweep + REVIEW.md + phase-done commit + STATE/ROADMAP advance). The PLAN settles all seven SPEC §12 deferred decisions PLUS surfaces and resolves two planner-time-discovered gaps (decision 8 — per-endpoint cx_*/rq_* counter lines emit literal `0` since envoy-go has no per-endpoint stats per ADR-0063 deferral; the differential allow-list widens SPEC §7.1's ±1 tolerance to fully allow-list these 8 fields per endpoint; decision 9 — `Bootstrap.ConfigPath string` field added; `bootstrap.Load` signature NOT widened; caller sets `bs.ConfigPath = *cfgPath` post-Load). Net change estimate: ~1700 LoC production + ~1380 LoC tests + ~550 LoC fixture/driver + ~700 LoC docs = ~4330 LoC total — within ADR-0045's split-gate thresholds (15 tasks <25; production ~1700 LoC barely above the 1500 LoC limit; the parent-level ADR-0084 split via 08 → 08.1 + 08.2 already applied the gate, leaving 08.1 as a single coherent admin-endpoints sub-phase). Per BOOTSTRAP §5 the next transition is state 3 → 4 via `superpowers:subagent-driven-development` (per the user's persistent `Prefer subagent-driven execution` memory preference; alternative is `superpowers:executing-plans`).
- **next-skill:** `superpowers:subagent-driven-development` — implementation session for phase 08.1 (per the user's persistent `Prefer subagent-driven execution` memory preference recorded in `~/.claude/projects/-home-esa-git-envoy-go/memory/MEMORY.md`; alternative `superpowers:executing-plans` is also acceptable). Inputs: THIS commit's `docs/envoy-go/phases/08.1-admin-endpoints/PLAN.md` (the authoritative implementation source — every executor task traces to a PLAN task T1-T15); `docs/envoy-go/phases/08.1-admin-endpoints/SPEC.md` (the contract authority — every PLAN task anchors to one or more SPEC sections via the per-task "Anchored:" footer); `docs/envoy-go/phases/08-admin-api-and-drain/{SPEC.md, BRAINSTORM.md}` (parent master SPEC + master BRAINSTORM); `docs/envoy-go/phases/07.1-http-filter-framework/{PLAN.md, PROGRESS.md, REVIEW.md}` + `07.2-listener-chain-completion/{PLAN.md, PROGRESS.md, REVIEW.md}` (closed read-only history; the 07.1 + 07.2 PROGRESS.md files are the structural precedent for PROGRESS append-pattern, ADR-with-first-use-commit discipline, SHA-fill follow-up cadence); `BOOTSTRAP_PROMPT.md` §§5–7 (lifecycle + phase-done six-gate); `SKILL_ROUTING.md`; the existing `internal/admin/{admin.go, prometheus.go, admin_test.go}` (the constructor + mux + `*stats.Registry` patterns Tasks 5+11 extend), `internal/cluster/{manager.go, manager_test.go}` (the snapshot-accessor patterns Task 3 extends), `internal/bootstrap/{bootstrap.go, bootstrap_test.go}` (the field-addition pattern Task 2 extends), `cmd/envoy-go/{main.go, main_test.go}` (the boot-graph patterns Task 10 updates), `test/fixtures/0007a-cors/{driver,backends}/` + `test/differential/{runner_test.go, fixture/fixture.go}` (the differential-fixture patterns Task 14 mirrors).
- **next-skill-scope:** Lifecycle-state 3 → 4 implementation session deliverables: 15 PLAN tasks T1-T15 landed sequentially per the PLAN's task-by-task contract. Each task lands one commit; SHA-fill follow-up commits track the TBD-SHA discipline per the phase-04..07.2 convention. Seven new ADRs (ADR-0084..ADR-0090) land at their respective task anchors per the PLAN's `## ADRs introduced by this plan` table. Six-gate verification sweep at T15 per BOOTSTRAP §7.5 + SPEC §3. The phase-done commit subject is `phase 08.1: admin-endpoints [ADR-0084, ADR-0085, ADR-0086, ADR-0087, ADR-0088, ADR-0089, ADR-0090]` per BOOTSTRAP §5.3 commit-message-completeness; the body explicitly names the ROADMAP-row transition (`08.1 → done`) and that parent row `08` STAYS `in-progress` (closes at 08.2's phase-done per parent SPEC §5). After phase-done, advance STATE to phase 08.2 lifecycle-state 0 with `next-skill: superpowers:brainstorming` and `active-phase: 08.2-graceful-drain`. Worktree for the next session: branch from THIS PLAN-commit's master fast-forward HEAD per ADR-0003 + per-phase-worktree convention (`.worktrees/phase-08.1-admin-endpoints-impl`). Per the user's `Always use git worktrees` memory preference, the implementation session creates a fresh impl worktree (separate from this plan worktree) before Task 1.
- **last-commit:** `TBD` — `phase 08.1: PLAN drafted (15-task implementation plan)`, on branch `phase-08.1-admin-endpoints-plan`. Lands (a) `docs/envoy-go/phases/08.1-admin-endpoints/PLAN.md` — full 15-task implementation plan (~3540 lines) covering: ADRs introduced (ADR-0084..ADR-0090 anchor table); 15 execution preconditions; nine planner-time deferred-decision resolutions (settles all SPEC §12 items PLUS the two planner-time-discovered gaps for per-endpoint counter `0`-emission and `Bootstrap.ConfigPath` plumbing); 15 tasks T1-T15 with TDD-step granularity (failing test → implement → passing test → commit → SHA-fill); Refinement notes (intermediate `cmd/envoy-go` build breakage between Tasks 5-10; Task 14 canonicalisation iteration expectation; Task 15 REVIEW.md major-finding blocking discipline); Post-plan handoff (ROADMAP / STATE / BEHAVIOR_CONTRACT / DECISIONS post-08.1 state); References (SPEC + parent-SPEC + BRAINSTORM + sibling-stub + structural-precedent PLANs + BOOTSTRAP cross-refs + DECISIONS cross-refs + ENVOY_TARGET pin); (b) `docs/envoy-go/STATE.md` rewrite advancing to lifecycle-state 3 with `next-skill: superpowers:subagent-driven-development`. Bundles PLAN + STATE in one commit per the phase-04..07.2 PLAN-commit precedent. No code changes in 08.1 PLAN commit (per BOOTSTRAP §5 lifecycle state 3 — PLAN drafting only; code lands at lifecycle-state 4). SHA filled in a follow-up commit per the phase-04..07.2 SHA-fill convention.
- **last-updated:** 2026-05-02

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
