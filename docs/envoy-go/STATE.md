# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `04-http-1.1`
- **phase-directory:** `docs/envoy-go/phases/04-http-1.1/` — exists; contains `SPEC.md`, `PLAN.md`, and `PROGRESS.md` (all 17 PLAN tasks landed on branch `phase/04-http-1.1-impl` under `.worktrees/phase-04-http-1.1-impl`, atomic main commit + SHA-fill follow-up per phase-01/02/03 precedent, final Task-17 all-gates evidence quoted verbatim at PROGRESS.md `## Task 17` block). `REVIEW.md` does not yet exist. Phase 03's directory at `docs/envoy-go/phases/03-tls/` is closed and read-only history.
- **lifecycle-state:** `4` — implementation complete, not yet verified. PLAN.md's 17 tasks all landed per PLAN's task→commit mapping. Eight phase-04 ADRs landed sequentially as ADR-0037..ADR-0044 exactly per PLAN's first-use-commit budget (no deviation; ADR-numbering tail at state-4 entry is ADR-0044). Five of six SPEC §3 gates green on Task-17's all-gates green sweep (PROGRESS evidence verbatim from impl-worktree session): (a) fixture `0003-http11-routing` PASS — 27 requests per side, status equivalence, decoded body bytes byte-equal for `/health` direct_response 200 + 404 catch-all paths, per-cluster RR distribution `[3,3,3]` over the 9 router-action requests (route-match selection witnessed); ADR-0044 explicitly relaxes routed-to-upstream body-byte-equivalence (rationale: RR start-index divergence between STRICT_DNS reference and STATIC subject — both maintain `[3,3,3]` overall but request[i] may hit a different backend index on each side; this relaxation lives in ADR-0044's "Not asserted" section and is a documented divergence from SPEC §3 (a)'s prose, recorded in ADR Consequences as a phase-04 limitation that a future RR-start-index-synchronisation phase may tighten back); (b) fixtures `0000-tcp-echo`, `0001-tcp-proxy-rr`, `0002-tls-tcp` all regression-free (re-verified `go test -race ./...` clean); (c) N/A — phase 04 introduces no conformance suite (h2spec is phase 05, h3spec/grpc/proxy-wasm later); (d) `internal/filter/hcm.FuzzHCMConfigParse` clean at ADR-0018's 30s CI budget plus carryforward `FuzzBootstrapLoad` + `FuzzTcpProxyFilter` + `FuzzTLSContextParse` all clean (Task-17 outputs verbatim in PROGRESS.md; serial expectation ~2min wall noted); (e) `go vet ./...`, `golangci-lint run ./...`, `go test ./...` all clean. (f) deferred to state 5. Per `BOOTSTRAP_PROMPT.md` §5 state 4, the next session invokes `superpowers:verification-before-completion` to re-run every gate from a fresh session and quote outputs into a dedicated PROGRESS.md verification section. ADR-0003 fast-forward invariant preserved at this state-4 transition: master FF'd cleanly to `phase/04-http-1.1-impl` HEAD `24a9c88` (no cherry-pick required — in contrast to phase-03's `a6f218f` topology note — because the phase-04 planner-session STATE→3 commits all landed on master *before* the impl worktree was cut, so master remained a strict ancestor of impl throughout execution).
- **next-skill:** `superpowers:verification-before-completion`
- **next-skill-scope:** Re-run every SPEC §3 phase-done gate from a fresh session and quote each command's verbatim output into PROGRESS.md (BOOTSTRAP §5 state 4 names PROGRESS as the capture target). Commands: `go build ./...`; `go vet ./...`; `golangci-lint run ./...`; `go test ./...`; `go test ./internal/bootstrap/ -run=FuzzBootstrapLoad -fuzz=FuzzBootstrapLoad -fuzztime=30s`; `go test ./internal/filter/tcpproxy/ -run=FuzzTcpProxyFilter -fuzz=FuzzTcpProxyFilter -fuzztime=30s`; `go test ./internal/tls/ -run=FuzzTLSContextParse -fuzz=FuzzTLSContextParse -fuzztime=30s`; `go test ./internal/filter/hcm/ -run=FuzzHCMConfigParse -fuzz=FuzzHCMConfigParse -fuzztime=30s` (four serial fuzz targets, ~2min wall total); `go test ./test/differential/ -v -timeout=12m` (all four fixtures — 0000/0001/0002/0003 — must PASS). Docker + the Envoy v1.32.4 image (per ADR-0013) required for the differential suite. Master and `phase/04-http-1.1-impl` share HEAD `24a9c88` at this state-4 entry; verification work continues in `.worktrees/phase-04-http-1.1-impl` per the "always use git worktrees" project convention. The verifier should re-confirm ADR-0044's documented body-byte-equivalence relaxation (routed-to-upstream requests) is what the runner actually enforces — i.e., the runner asserts status + per-cluster `[3,3,3]` distribution + direct_response body equality but does NOT byte-compare routed-to-upstream response bodies request-by-request. If the verifier finds the runner asserts something stronger or weaker than ADR-0044 prescribes, that is a state-3-re-entry trigger (per BOOTSTRAP §5.2). Fuzz-seed corpus discipline (ADR-0018): the verifier re-run will discover new interesting inputs across all four targets — these MUST NOT be persisted under `testdata/fuzz/` unless a crasher is hit. After verification passes, advance STATE to lifecycle-state 5 with `next-skill: superpowers:requesting-code-review` per §5 state 5.
- **last-commit:** <will be SHA-filled in a follow-up commit>
- **last-updated:** 2026-04-25

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
