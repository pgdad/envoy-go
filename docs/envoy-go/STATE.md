# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `07.1-http-filter-framework`
- **phase-directory:** `docs/envoy-go/phases/07-filter-chain-framework/` — created by the phase-07 brainstorm commit (`da28039` on branch `phase/07-filter-chain-framework-brainstorm`); contains `BRAINSTORM.md` (688 lines). Sub-phase directories `docs/envoy-go/phases/07.1-http-filter-framework/` and `docs/envoy-go/phases/07.2-listener-chain-completion/` will be created by the next-session SPEC drafting. Parent directory will hold the eventual master `SPEC.md` summarizing both sub-phases (mirrors `docs/envoy-go/phases/06-observability-baseline/` + `docs/envoy-go/phases/05-http-2/`). Phase 06 (parent + sub-phases 06.1 + 06.2) remains closed read-only history.
- **lifecycle-state:** `1` — phase 07 brainstorm session complete; planner-time-split decision per ADR-0045 lands phase 07 → 07.1 (`http-filter-framework`) + 07.2 (`listener-chain-completion`). 07.1 is the immediate next focus (HTTP filter iteration protocol + extension registry + per-route config + `cors` + test-only `envoy_go_test` probe filter); 07.2 (listener_filters framework + FilterChainMatch fields beyond SNI + Listener.default_filter_chain) receives a sibling SPEC stub at the same SPEC-drafting session. Brainstorm captured in `docs/envoy-go/phases/07-filter-chain-framework/BRAINSTORM.md` settling seven cardinal decisions (Q1–Q7): (Q1) 07.1+07.2 split rationale (disjoint code surfaces: HCM-internal vs listener-scope; 07.1 unblocks BOOTSTRAP §9 HTTP-filters family); (Q2) `typed_per_filter_config` 3-tier merge (Route > VirtualHost > RouteConfiguration; most-specific-override, no field-merge); (Q3) Envoy-faithful iteration protocol with async-resume — narrow method set (`Decode/EncodeHeaders/Data/Trailers`), status enums settled (`Continue / StopIteration / StopIterationAndBuffer / StopIterationNoBuffer`), out-of-MVP variants documented (`ContinueAndDontEndStream`, watermark, `Encode1xxHeaders`, metadata frames); (Q4) `*HTTPRegistry` threaded constructor map with Freeze-after-boot invariant (mirrors `*stats.Registry` LBP-1; rejects `init()`-based global); (Q5) trivial filter set = real `envoy.filters.http.cors` (differential fixture `0007a`) + test-only `envoy.filters.http.envoy_go_test` probe (structural fixture `0007b`); (Q6) `sendLocalReply` enters encode chain at filter[len-1], first-call-wins via `sync.Once`; (Q7) body buffer cap = 1 MiB hardcoded constant `filterBufferLimitBytes`, 413 on decode-side overflow, connection reset on encode-side overflow, `per_connection_buffer_limit_bytes` + `per_request_buffer_limit_bytes` added to ADR-0041 silent-ignore set. Seven ADRs anticipated for 07.1; inline supersessions: ADR-0040 totally superseded, ADR-0042 partially superseded, ADR-0041 amended. Largest refactor in 07.1: ~600 LOC of `routerAction` + `routerActionH2` move from `internal/filter/hcm/actions.go` to a new `internal/filter/http/router/` package (test bodies byte-preserved). Per BOOTSTRAP §5 the next transition is state 1 → 2 via `superpowers:writing-plans` (SPEC.md authoring for phase-07 parent + 07.1 sub-phase + 07.2 stub). BRAINSTORM §2.6 documents four empirical-pin obligations the SPEC session must execute against reference Envoy v1.37.2 (filter ordering on encode side; cors filter response shape; 413 overflow response shape; sendLocalReply encode-chain entry filter index) — these are unresolvable from documentation alone, mirroring the 06.1 SN4 pin pattern.
- **next-skill:** `superpowers:writing-plans` — SPEC.md authoring for phase 07.1. Inputs: `da28039`'s `BRAINSTORM.md` (authoritative design source — see `## 10. Hand-off to writing-plans`), `BOOTSTRAP_PROMPT.md` §§5–6 (lifecycle + planner-time split contract), `SKILL_ROUTING.md`, the closed-history `docs/envoy-go/phases/06.1-stats-prometheus/SPEC.md` and `docs/envoy-go/phases/06.2-access-log/SPEC.md` as structural precedent (header layout, §-numbering conventions, acceptance-bullet shape, empirical-pin verbatim subsections), `docs/envoy-go/phases/06-observability-baseline/SPEC.md` as parent-master-SPEC precedent, `docs/envoy-go/BEHAVIOR_CONTRACT.md` (the new `## HTTP filter chain` section to be added per BRAINSTORM §7), `docs/envoy-go/ENVOY_TARGET.md` (Envoy v1.37.2 pin — required for the four BRAINSTORM §2.6 empirical-pin obligations), and the phase-04 `SPEC.md`'s router-as-direct-call sections that are partially superseded by ADR-0040/ADR-0042 supersession notes per BRAINSTORM §8.
- **next-skill-scope:** Lifecycle-state 1 → 2 SPEC drafting session deliverables: (1) `docs/envoy-go/phases/07-filter-chain-framework/SPEC.md` — parent master SPEC summarizing both 07.1 + 07.2 scopes (mirrors `docs/envoy-go/phases/06-observability-baseline/SPEC.md` + `docs/envoy-go/phases/05-http-2/SPEC.md`); (2) `docs/envoy-go/phases/07.1-http-filter-framework/SPEC.md` — full sub-phase SPEC derived from BRAINSTORM §§2–9; **must empirically pin the four §2.6 obligations** by running reference Envoy v1.37.2 and copying output verbatim: (a) filter declaration order on encode side (boot Envoy with `http_filters: [a, b, router]`, log decode/encode entry, observe encode order is `[router, b, a]`, pin verbatim), (b) cors filter response shape (exact `Access-Control-*` header set produced by Envoy's cors filter on preflight + actual-request responses; which headers, in what order, with what casing), (c) 413 overflow response shape (exact body, headers, status when `per_connection_buffer_limit_bytes` is exceeded), (d) local-reply encode-chain entry filter index (verify the actual on-wire entry-point filter index via Envoy source + behavioral observation); (3) `docs/envoy-go/phases/07.2-listener-chain-completion/README.md` — sibling SPEC stub citing parent SPEC + forward-looking 07.2 scope notes (BRAINSTORM §1 split-table is sufficient seed); (4) ROADMAP.md split: row `07 | filter-chain-framework | 06 | planned` becomes parent `07 | filter-chain-framework | 06 | in-progress | 07.1, 07.2 | ...` with rows `07.1 | http-filter-framework | 06 | planned | | ...` and `07.2 | listener-chain-completion | 07.1 | planned | | ...` (07.2's prereq is 07.1 only because 07.2 is independent of 07.1's HTTP-filter machinery but is sequenced after it for planner-time ordering — see BRAINSTORM §1). After SPEC, advance STATE to lifecycle-state 2 with `next-skill: superpowers:writing-plans` (PLAN.md authoring) and `active-phase` remains `07.1-http-filter-framework`. Worktree for the next session: branch from this brainstorm-closure commit's master fast-forward HEAD per ADR-0003 + per-phase-worktree convention (`.worktrees/phase-07.1-http-filter-framework-spec`). The L4 review-followup batch closing 11 Minors from phase 06.2 REVIEW.md `## Concerns — Minor` is a separate post-phase-done branch — NOT phase 07.1's scope; can run in parallel.
- **last-commit:** `da28039` — `phase 07: brainstorm — filter-chain-framework split into 07.1 + 07.2`, on branch `phase/07-filter-chain-framework-brainstorm`. Lands `docs/envoy-go/phases/07-filter-chain-framework/BRAINSTORM.md` (688 lines) capturing the full design for 07.1 plus a forward-looking 07.2 stub. STATE.md update is THIS follow-up commit (split from `da28039` because the brainstorm session committed BRAINSTORM.md without bundling the STATE advance — diverges from the phase-06 `d271fd7` precedent which bundled both in one commit). No code changes in either commit; pure scaffold + state transition.
- **last-updated:** 2026-04-30

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
