# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `19.2-http-filter-ext-proc-body` — phase 19.2 SPEC session COMPLETE at 2026-05-16 (state 1 → 2 transition per ADR-0005 §Decision 4). The 19.2 SPEC author chose direct-to-SPEC per the 18.2 precedent (the 19.1 IMPL's 8-ADR envelope settled most cross-cutting decisions; ADR-0175 §Context already at the parent SPEC commit; the 19.2 README stub already named the 19.2 scope verbatim). The 19.2 SPEC.md is 458 lines / 15 sections (mirroring the 18.2 SPEC structure) with a 16-item §15 acceptance checklist. **In-session SPEC-author scoping decisions:** (i) HOLD AT 9 counters / 86 stat-name BEHAVIOR_CONTRACT total — the 8 reference-Envoy additional counters from ADR-0173 §Consequences stay DEFERRED to 19.3+; (ii) DEFER ALL THREE ADR-0170 §Consequences envelope-content divergences (protojson whitespace non-determinism; reference Envoy `metadata_context:{}` + `protocol_config:{}` empty-message emission; writer-side `value`-vs-`raw_value` on header injection) — body-mode is orthogonal to envelope-content rendering, closure deferred; (iii) REASSIGN Carryforward M (`subject_local_certificate` TLS-fixture closure from 19.1 REVIEW §10) to the TLS-fixture phase, NOT 19.2. **D12 / D13 hypothesis HELD at SPEC**: no new ADR numbers consumed at 19.2 SPEC time (ADR-0175 was reserved at the parent SPEC commit; the 3 §Decision AMENDMENTS for ADR-0168 / ADR-0171 / ADR-0172 are in-place edits per ADR-0044 to be landed at 19.2 IMPL). The 19.2 SPEC settles 19.1 SPEC §12 deferred decision #7 (`CONTINUE_AND_REPLACE` handling — CONSUMED as combined header+body replacement at header stages with body-mode BUFFERED; TREATED AS CONTINUE at body stages — the 19.1 spurious-dispatch for header-stages-with-body-mode-NONE lifts at 19.2). The next session is the phase-19.2 PLAN-authoring session (lifecycle-state 2 → 3) per `superpowers:writing-plans`.
- **phase-directory:** `docs/envoy-go/phases/19-http-filter-ext-proc/` (BRAINSTORM.md + parent SPEC.md) + `docs/envoy-go/phases/19.1-http-filter-ext-proc-headers/` (SPEC.md + PLAN.md + PROGRESS.md + REVIEW.md authored at IMPL phase-done) + `docs/envoy-go/phases/19.2-http-filter-ext-proc-body/` (README.md sibling stub + full SPEC.md authored at this SPEC session; PLAN.md pending at the next-session lifecycle-state 2 → 3).
- **lifecycle-state:** `phase 19.2 SPEC done; phase 19.2 PLAN pending; phase 19 parent in-progress; phase 19.1 done` — the 19.2 SPEC is complete; the 19.2 sub-row is `in-progress` at this commit; the parent row 19 STAYS `in-progress` (closes at 19.2's phase-done AT THE SAME commit per parent SPEC §8 rollup discipline). ROADMAP row `19.1` is `done` (last-touched `2026-05-16`); row `19` (parent) is `in-progress`; row `19.2` is `in-progress` (last-touched `2026-05-16`; depends-on `19.1`). DECISIONS.md tail still at ADR-0176; next-free ADR `ADR-0177` stays unconsumed (D12 / D13 hypothesis HELD at 19.2 SPEC time). The 19.2 PLAN session breaks the 19.2 SPEC into per-Task work units per `superpowers:writing-plans`; the planner settles the per-Task Lands-in-Tasks for ADR-0175 §Decision + §Consequences + the 3 §Decision AMENDMENTS (ADR-0168 / ADR-0171 / ADR-0172). Anticipated 19.2 IMPL production code: ~1500-2500 LoC across `internal/filter/http/extproc/` body-stage extension (extproc.go body-mode arm activation + processor.go 4-stage state machine + check.go body-mode `applyProcessingResponse` arms + attributes.go body-stage attribute envelope) + `internal/filter/http/callbacks.go` + `chain.go` ADR-0175 encode-side body-buffering primitive (~150–300 LoC chain-side + ~30–50 LoC interface + ~150–250 LoC unit tests) + fixture `0023-http-ext-proc-body` (6 body-stage scenarios per SPEC §7.2; REUSES 19.1's `test/helpers/extprocgrpc/` UNMODIFIED) + 25th fuzzer `FuzzProcessingResponseMapping`.
- **next-skill:** `superpowers:writing-plans` (to author `docs/envoy-go/phases/19.2-http-filter-ext-proc-body/PLAN.md`). Cold-start scope for the 19.2 PLAN session: read STATE.md (this file); `docs/envoy-go/phases/19.2-http-filter-ext-proc-body/SPEC.md` (the load-bearing input — 15 sections + 16-item §15 acceptance checklist + 4-ADR-anchor map at §10); `docs/envoy-go/phases/19-http-filter-ext-proc/SPEC.md` (parent SPEC — §2 scope + §4 cross-cutting + §5 13-pin block (CLOSED post-19.1) + §7 10-ADR anchor map + §8 parent-rollup discipline + §11 19.2 scope subset); `docs/envoy-go/phases/19.1-http-filter-ext-proc-headers/SPEC.md` + `PLAN.md` + `PROGRESS.md` + `REVIEW.md` (the load-bearing 19.1 envelope — 19.2 IMPL extends without re-deriving); `docs/envoy-go/ROADMAP.md` (row `19.2` `in-progress` post this SPEC commit); `docs/envoy-go/DECISIONS.md` (tail at ADR-0176; ADR-0175 §Context at parent SPEC commit; §Decision + §Consequences land at 19.2 IMPL per ADR-0044; ADR-0168 / ADR-0171 / ADR-0172 §Decision AMENDMENTS land in-place per ADR-0044); `docs/envoy-go/BEHAVIOR_CONTRACT.md` (12 HTTP filters; stat-table at 86 names; ext_proc subsection ready for body-mode AMENDMENT per SPEC §13); project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (subagent-driven IMPL + always-worktrees + always-push). The 19.2 PLAN session's defining IN-SESSION obligations: break the SPEC's 4-ADR anchor scope into per-Task Lands-in-Tasks (anticipated ~10-15 tasks per the 19.1 precedent + the smaller 19.2 envelope); confirm or refine the 7 §12 deferred decisions; ratify task ordering (chain-side ADR-0175 primitive before extproc body-mode dispatch; processor.go state-machine extension before check.go body-mode `applyProcessingResponse` arms; fixture 0023 + 25th fuzzer at the IMPL tail per the 19.1 Task 13 + Task 14 precedent).
- **last-commit:** TBD — to be SHA-filled at the SPEC follow-up commit per the phase-09..19.1 squash-merge + SHA-fill precedent. The 19.2 SPEC worktree `phase-19.2-http-filter-ext-proc-body-spec` (branched off master tip `fc4970a` — `phase 19.1 IMPL follow-up: STATE.md SHA-fill (TBD → 95bb425 post-squash)`) carries this SPEC commit + STATE/ROADMAP advance; squash-merged to master at the post-squash SHA + SHA-fill follow-up commit per the phase-09..19.1 precedent.
- **last-updated:** 2026-05-16
- **next-free ADR:** `ADR-0177` — UNCHANGED from the phase-19 SPEC commit + the phase-19.1 IMPL session. The phase-19.2 SPEC session consumed NO new ADR numbers (D12 / D13 hypothesis HELD at SPEC time: ADR-0175 §Context was reserved at the parent SPEC commit + the §Decision + §Consequences land at 19.2 IMPL per ADR-0044; ADR-0168 / ADR-0171 / ADR-0172 §Decision AMENDMENTS are in-place edits to existing 19.1-anchored §Decision sections at 19.2 IMPL per ADR-0044, NOT new ADR consumptions). The 19.2 IMPL may consume ADR-0177 + ADR-0178 if 19.2-IMPL-unanticipated ADRs fire (most-likely surfaces per SPEC §10: buffered-body-release-vs-stream-reset interaction with the framework's encode-chain primitive; `body_mutation_rules` application to body bytes — both UNLIKELY per the SPEC's analysis).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
