# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `16-http-filter-rbac` — phase 16 SPEC.md authored at this commit. Phase 16 stays the NINTH §9 family-row (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11, csrf @ 12, buffer @ 13, compressor @ 14, bandwidth_limit @ 15) and the FIRST §9 row since phase 14 to introduce non-zero framework deltas — TWO new primitives confirmed at SPEC time: (i) TLS-principal accessor on `DecoderFilterCallbacks` (`DownstreamPrincipal() []string`) required by `Principal_Authenticated`, anchored at ADR-0144; (ii) matcher-engine evaluator at new top-level `internal/matcher/` package implementing `xds.type.matcher.v3.Matcher` generic match-tree primitive, anchored at ADR-0142 (location confirmed `internal/matcher/` per SPEC §3.2 + §11.2 carry-forward decision). MVP per SPEC §1 + §1.1 (**12 amendments authored at SPEC time** per §11 empirical-pin resolution): 7 outer fields consumed proto-faithful (Q2 BOTH engines `rules` + `matcher` + `shadow_rules` + `shadow_matcher` + `rules_stat_prefix` + `shadow_rules_stat_prefix` + `track_per_rule_stats`); silent-ignore set lives ONE LEVEL DEEPER inside `config.rbac.v3.RBAC` (`audit_logging_options` + Policy.condition + Policy.checked_condition + Policy.cel_config — THREE CEL fields not two per §1.1 amendment 6); Permission MVP Large 11+11 (Q3); action enum ALLOW + DENY + LOG-partial (Q4; LOG folds into `allowed` counter per §1.1 amendments 5 + 8 — no separate `logged` counter exists in Envoy); shadow + track_per_rule_stats proto-faithful (Q5); per-route NEW 7th canonical absent-implies-disabled-OR-wholesale-override per ADR-0125 §(xii) in-place amendment (Q6; structurally distinct from 5th canonical's explicit-disabled-bool + 6th canonical's bare-message-via-TPFC); CEL three-field silent-ignored (Q7); deny-path `SendLocalReply(403, "RBAC: access denied", {Content-Type: text/plain})` RATIFIED per §1.1 amendment 10 + §11.P5 (19 bytes ASCII body); response_code_details divergence-window per §1.1 amendment 11 + §8.12; stat surface 60→64 names (**4 base counters** allowed/denied/shadow_allowed/shadow_denied per §1.1 amendment 8 REFUTES BRAINSTORM 5-counter hypothesis; variable upward with `track_per_rule_stats: true` per-policy counter family); SN2-reuse namespace hypothesis pending impl-time empirical scrape confirmation per §1.1 amendment 9; INDEPENDENT per-route stats per ADR-0145 (mirrors phase-11 ADR-0117 + phase-15 ADR-0139 stateful-override-implies-INDEPENDENT precedent); Permission has 14 variants (3 deferred); **Principal has 14 variants (per §1.1 amendment 7 REFUTES BRAINSTORM 13)** — 3 deferred including the NEW `custom` TypedExtensionConfig variant; Principal_Authenticated nil-principal_name = any-authenticated-user per §1.1 amendment 12 + §11.P14; differential fixture `0018-http-rbac` (8 scenarios sketched + mTLS scenario 6 + per-route 7th-canonical disabled scenario 7 + per-route INDEPENDENT-stats scenario 8); 20th fuzzer `FuzzRBACConfigParse`. **ADR-0045 surface-split release valve DEFERRED to PLAN time** — SPEC stays single-row at ~2000 LoC SPEC + ~2000 LoC anticipated production code; mirrors phase-14 (2049 SPEC + ~1800 prod, single-row) + phase-15 (1787 SPEC + ~1600 prod, single-row) precedent; PLAN session re-evaluates per ADR-0045 trigger (~25 tasks OR ~1500 LoC).
- **phase-directory:** `docs/envoy-go/phases/16-http-filter-rbac/` — BRAINSTORM.md (683 LoC) + **SPEC.md (2004 LoC; 12 §1.1 amendments authored; all 18 §11 pins resolved IN-SESSION against Envoy v1.37.2)** authored. PLAN.md / PROGRESS.md / REVIEW.md NOT yet authored — those are subsequent lifecycle-state transitions (state-3 → 4 → 5) per ADR-0005 §Decision 4 session-split discipline.
- **lifecycle-state:** `phase 16 SPEC.md authored; PLAN.md pending` — state-2 exit. Next session authors PLAN.md per the phase 09/10/11/12/13/14/15 precedent. SPEC §11 captured the 18 empirical pin resolutions IN-SESSION per ADR-0004; SPEC §10 enumerates the 12 acceptance review claims; SPEC §15 the 13-item acceptance checklist. PLAN author splits the SPEC into tasks + re-evaluates ADR-0045 trigger (SPEC's position: single-row; PLAN may split if task count exceeds ~25 or production LoC estimate exceeds ~1500).
- **next-skill:** `superpowers:writing-plans` per BOOTSTRAP_PROMPT.md §5 lifecycle-state-3 entry condition (PLAN.md does not exist — phase 16 enters this state at this commit; the routing convention from phases 09/10/11/12/13/14/15 re-invokes `superpowers:writing-plans` for the PLAN drafting based on the SPEC). The PLAN author splits the SPEC §6 + §7 + §8 + §13 + §14 deliverables into tasks following TDD discipline + applies the ADR-0045 split-gate to confirm single-row-or-split.
- **next-skill-scope:** Cold-start scope for the phase 16 PLAN-authoring session. Read first: `docs/envoy-go/phases/16-http-filter-rbac/SPEC.md` (2004 LoC; the authoritative input — every load-bearing decision lives there; §1.1 amendments 1-12 capture the SPEC-time re-frames; §6 covers compiledConfig + code shapes including the dual-engine dispatch + Permission/Principal evaluator interfaces + Principal_Authenticated three-case algorithm; §7 covers the 0018 differential fixture's 8 scenarios; §8 covers the 7 ADRs anticipated + ADR-0125 §(xii) amendment; §13 covers the 6 BEHAVIOR_CONTRACT subsection patches; §14 covers the 8 unit-test groups); `docs/envoy-go/phases/16-http-filter-rbac/BRAINSTORM.md` (683 LoC; the predecessor design sketch; PRESERVED VERBATIM per D-3.5; the §1.1 amendments in SPEC supersede where they diverge); `docs/envoy-go/phases/15-http-filter-bandwidth-limit/PLAN.md` (load-bearing precedent for phase-16 PLAN structural template — task breakdown + ADR anchor mapping + impl-time-ADR-on-impl convention per ADR-0044); `docs/envoy-go/phases/14-http-filter-compressor/PLAN.md` (secondary precedent; the 6-ADR roster mapping per phase-14's larger ADR set; closer to phase-16's 7-ADR roster than phase-15's 5-ADR roster); `docs/envoy-go/DECISIONS.md` ADR-0044 (SPEC-time ADR anticipation discipline; ADR roster lands at impl-time anchor tasks per phase-13/15 convention; phase-14 SPEC-time-pre-landing of ADR-0129..ADR-0133 is the divergent precedent), ADR-0045 (split-gate at PLAN time; ~25 tasks OR ~1500 LoC), ADR-0117 + ADR-0139 (INDEPENDENT-stats precedent; phase-16 inherits via ADR-0145), ADR-0125 (canonical per-route patterns; phase-16 SPEC §(xii) amendment introduces the 7th canonical), ADR-0128 + ADR-0131 (framework-primitive introduction precedent; phase-16 introduces TWO new primitives ADR-0142 + ADR-0144 mirroring this discipline). Highest-priority PLAN-time tasks: Task 1 (worktree spawn + baseline verification); Task 2 (rbac package skeleton + boot registration + ADR-0140 anchor — mirrors phase-15 Task 2 + phase-14 Task 2); Task 3 (compiledConfig + parsing + ADR-0141 anchor); Task 4 (matcher-engine framework primitive at `internal/matcher/` + ADR-0142 anchor); Task 5 (Permission + Principal Large 11+11 evaluators + ADR-0143 anchor); Task 6 (TLS-principal accessor framework primitive at `internal/filter/http/callbacks.go` + ADR-0144 anchor); Task 7 (DecodeHeaders dual-engine dispatch + SendLocalReply 403); Task 8 (stat surface + per-route INDEPENDENT-stats + ADR-0145 anchor); Task 9 (shadow + LOG-partial + track_per_rule_stats + ADR-0146 anchor); Task 10 (ADR-0125 §(xii) in-place amendment paragraph); Task 11 (differential fixture 0018 + 8 scenarios including mTLS); Task 12 (20th fuzzer FuzzRBACConfigParse); Task 13 (BEHAVIOR_CONTRACT.md §13.1-§13.6 patches); Task 14 (Task-14-style follow-up for any unanticipated impl-time-revealed ADR or wire-shape pin amendment — mirrors phase-14 ADR-0134 pattern). Estimated 14-22 tasks; ~2000-2500 LoC production code.
- **last-commit:** `3159811` — `Squash merge phase-16-http-filter-rbac-spec`. Squashed 1 worktree commit (e2ea817 `phase 16 spec: http-filter-rbac [SPEC authored + ROADMAP row 16 planned → in-progress + STATE.md flip]`). Lands the phase-16 SPEC artefacts (SPEC.md 2004 LoC + STATE.md flip + ROADMAP row 16 status flip planned → in-progress). Predecessor on master: `b45c1eb` `phase 16 brainstorm follow-up: STATE.md SHA-fill (TBD → 38749ba post-squash)`. Phase-16 PLAN author begins from this master tip in a new PLAN-authoring worktree per ADR-0005 §Decision 4 separate-sessions discipline.
- **last-updated:** 2026-05-12
- **next-free ADR:** `ADR-0140` (unchanged from phase 16 BRAINSTORM STATE.md). Per ADR-0044 ADR-on-impl convention, phase-16 SPEC anticipates ADR-0140..ADR-0146 (7 ADRs — LARGEST §9-row roster to date) + ADR-0125 §(xii) in-place amendment paragraph; the ADR anchor decisions land at impl-time anchor tasks per phase-13/15 precedent (phase-14's SPEC-time-pre-landing of ADR-0129..ADR-0133 is the divergent precedent). PLAN.md table maps each anticipated ADR to its impl-time anchor task.

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
