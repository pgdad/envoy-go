# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `12-http-filter-csrf` — phase 12 SPEC.md authored at this commit. ROADMAP row 12 status flips `planned → in-progress` per BOOTSTRAP_PROMPT.md §4.1 invariant 3 + §5 lifecycle-state-1 → 2 transition. Phase 12 is the FIFTH §9 family-row in the in-progress lifecycle (after cors @ 07.1 done, fault @ 09 done, header_mutation @ 10 done, local_ratelimit @ 11 done); the §9 family heading at ROADMAP line 56 stays unchanged per ADR-0106(c). The SPEC empirically resolved all 11 BRAINSTORM §9 empirical-pin obligations IN-SESSION against reference Envoy v1.37.2 per ADR-0004; **two MAJOR amendments** to BRAINSTORM hypotheses landed at SPEC §1.1: (a) host:port-only comparison (no scheme equality, no normalization, `additional_origins` matches `host[:port]` form not full URL — operator footgun documented prominently); (b) `filter_enabled` is PGV-REQUIRED at parse-time (cannot silent-ignore the field; runtime still always-100%-active per the phase-11-style discipline). Plus **two MINOR amendments**: (c) origin-extraction trichotomy (Origin: null literal → empty NO Referer fallback; empty/absent → Referer fallback; non-parseable → verbatim string used as source); (d) per-route stats SHARED with listener-level (diverges from phase 11 local_ratelimit's independent-per-route-stats precedent — this is the FIRST production filter to demonstrate the "wholesale data-only override + shared stats" pattern). Plus three confirmations (no `shadow_request_invalid` counter; tag-extractor reuse `envoy_http_conn_manager_prefix` — NO new SN flattening rule; rejection wire shape `Invalid origin` + 4-header lowercase set).
- **phase-directory:** `docs/envoy-go/phases/12-http-filter-csrf/` — contains `BRAINSTORM.md` (499 lines; predecessor commits `7fd9213` + `ba58c7e` + `399532c` + `c2e7559` + `bb29bb0`) + `SPEC.md` (1597 lines after iter-1 reviewer-recs polish) at this commit. PLAN.md, PROGRESS.md, REVIEW.md authored over phase lifecycle (states 2 → 6). The SPEC commits to 8 line-items per §1 (mirroring BRAINSTORM §1.1's 8 in-scope items; §1.1 amendments reshape algorithms within Decisions 3, 4, 7 but do not add or remove primitives), 6 differential-fixture scenarios at §7.1 (BRAINSTORM §6.2 preserved unchanged: scenario 1 same-origin allowed; 2 cross-origin rejected; 3 additional_origins host:port match; 4 missing source-origin rejected; 5 Referer fallback; 7 per-route wholesale override with shared stats; scenario 6 GET passthrough unit-only), 5 anticipated ADRs at §8 (ADR-0120..ADR-0124; **no consolidation**; no ADR-0073 amendment paragraph; no ADR-0061 amendment), 3 inline-deferral items at §13.4 (`filter_enabled` percentage-gating Runtime-family-coupled; `shadow_enabled` shadow-mode-coupled; `additional_origins[].StringMatcher` non-exact variants dropped at PARSE time per ADR-0101 §3 verbatim), 4 SPEC-deferred decisions at §12 (D1 callback-wiring; D2 URL-parse semantics edge cases; D3 PGV-mirror error-message wording; D4 per-route stats wiring mechanism), and 11 empirical pins at §11 (§11.1..§11.11 — all resolved IN-SESSION per ADR-0004; mirrors phase 11 SPEC §11.1..§11.8 structure verbatim). NO sibling-stub authored per ADR-0106(b). NO off-master prebrainstorm-notes branch (UNLIKE phase 11; phase 12 cold-started fresh per ADR-0106(e)).
- **lifecycle-state:** `phase 12 SPEC authored; PLAN.md pending` — phase 12 SPEC.md authored at this commit; lifecycle-state-1 → 2 transition complete per BOOTSTRAP_PROMPT.md §5. Lifecycle-state-2 → 3 transition (PLAN.md authoring) is the next session's responsibility. Phase 11's six gates remain green at parent commit `0f3a710`; this commit is docs-only (no code changes; no test re-validation needed).
- **next-skill:** `superpowers:writing-plans` per BOOTSTRAP_PROMPT.md §5 lifecycle-state-2 entry condition (SPEC.md exists, PLAN.md does not). The PLAN author derives task decomposition from this SPEC + the BRAINSTORM (the SPEC is the authoritative input to PLAN per §10.1; the BRAINSTORM is consulted only for design-rationale provenance — SPEC §1.1 amendments supersede where they diverge from BRAINSTORM hypotheses).
- **next-skill-scope:** Cold-start scope for the phase 12 PLAN-authoring session. Read first: `docs/envoy-go/phases/12-http-filter-csrf/SPEC.md` (the authoritative design + empirical-pin doc — every load-bearing decision lives there per D-3.4); `docs/envoy-go/phases/12-http-filter-csrf/BRAINSTORM.md` (consulted for design-rationale provenance only — SPEC §1.1 amendments supersede); `docs/envoy-go/ROADMAP.md` row 12 (now `in-progress`); `docs/envoy-go/BEHAVIOR_CONTRACT.md ## HTTP filter chain ### envoy.filters.http.local_ratelimit` subsection (the just-shipped phase 11 contract — phase 12 mirrors its structural shape with simplifications: no token bucket, no LBP-1-adjacent declaration, no filter-specific tag-extractor); `docs/envoy-go/DECISIONS.md` ADR-0073 (wholesale-override per-route discipline; phase 12 reuses with NO amendment) + ADR-0101 (parse-time-drop StringMatcher discipline phase 12 inherits verbatim) + ADR-0110 + ADR-0117 (the two prior ADR-0073 amendments phase 12 inherits with no third) + ADR-0114..ADR-0119 (the 6 phase-11 ADRs — phase 12 numbers ADR-0120..ADR-0124 next-free); `docs/envoy-go/phases/11-http-filter-local-ratelimit/PLAN.md` + `PROGRESS.md` (the most-recent phase artefacts — phase 12 PLAN is structurally derived from these). The 4 SPEC-deferred decisions at §12 are the PLAN author's resolution scope. ADRs ADR-0120..ADR-0124 land during impl commits per the per-task SHA-fill discipline (NOT at PLAN commit, NOT at SPEC commit — phase 09 / 10 / 11 precedent).
- **last-commit:** `<TBD — SPEC commit SHA>` — `phase 12 SPEC: http-filter-csrf [empirical-pin amendments]`. Authors `docs/envoy-go/phases/12-http-filter-csrf/SPEC.md` (1597 lines) capturing the 11 empirical pins resolved IN-SESSION + 4 amendments to BRAINSTORM hypotheses (host:port-only comparison; origin-extraction trichotomy; `filter_enabled` PGV-required; per-route stats shared) + 3 confirmations + 5 anticipated ADRs + 4 SPEC-deferred decisions + 6 differential-fixture scenarios + 3 inline-deferral items + 4-edit BEHAVIOR_CONTRACT bundle (lands at phase-done commit, NOT at SPEC commit). Flips ROADMAP row 12 status `planned → in-progress` with summary updated to reflect §1.1 amendments. Docs-only commit; no code changes; phase 11's six gates remain green at parent commit `0f3a710`.
- **last-updated:** 2026-05-07

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
