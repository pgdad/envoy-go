# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `07.1-http-filter-framework`
- **phase-directory:** `docs/envoy-go/phases/07-filter-chain-framework/` — contains `BRAINSTORM.md` (688 lines, brainstorm-close artifact at `da28039`) plus the parent master `SPEC.md` (113 lines, this commit) summarizing both sub-phase scopes. Sub-phase directory `docs/envoy-go/phases/07.1-http-filter-framework/` contains `SPEC.md` (965 lines, this commit) — the full 07.1 sub-phase SPEC with all four §2.6 empirical-pin obligations resolved verbatim in §11 against reference Envoy v1.37.2 (server SHA `5afe27fb338b16d5bb06b3a7198bcd581b4e3dee`). Sub-phase directory `docs/envoy-go/phases/07.2-listener-chain-completion/` contains `README.md` (68 lines, this commit) — the sibling SPEC stub forward-looking to 07.2's own brainstorm + SPEC session. Phase 06 (parent + sub-phases 06.1 + 06.2) remains closed read-only history.
- **lifecycle-state:** `2` — SPEC drafted + reviewer-approved (inline self-review per ADR-0004 fallback when subagent dispatch is unavailable in nested-agent context; review applied the four-category rubric from `superpowers:brainstorming`'s `spec-document-reviewer-prompt.md` — Completeness / Consistency / Clarity / Scope / YAGNI; status: Approved with no Issues). All four BRAINSTORM §2.6 empirical-pin obligations resolved IN-SESSION (none carried forward to impl time): (#1) filter declaration order on encode side = reverse decl order, verified via `[lua_a, lua_b, router]` chain with Lua-filter logCritical decode/encode logging — observed encode order `lua_b → lua_a`; (#2) cors filter response shape verified for four request shapes (preflight allowed/disallowed origins; actual GET allowed/no-origin) — preflight allowed-origin emits `200 OK` (NOT `204 No Content` as BRAINSTORM hypothesized) plus 6 CORS headers in pinned order; preflight disallowed-origin passes through to router which emits `405 Method Not Allowed` (no 4xx synthesis from cors filter); actual GET allowed-origin gets 3 CORS response headers added on the upstream response; actual GET no-origin is a no-op; (#3) 413 overflow shape verified via Buffer filter `max_request_bytes=1024` with 2KB POST — verbatim response is `413 Payload Too Large` + `content-length: 17` + `content-type: text/plain` + `connection: close` + body `"Payload Too Large"` (17 bytes, no trailing newline); (#4) sendLocalReply encode-chain entry filter index verified via `[lua_a, lua_b, lua_c, router]` chain where `lua_b` calls Envoy's `respond` API — encode order observed `lua_c → lua_b → lua_a`, confirming entry at filter[len-1] of encode-side filter set + full encode chain runs in reverse. ROADMAP.md split landed: parent row `07` flipped `planned → in-progress` with sub-phases `07.1, 07.2`; row `07.1` added as `planned` with depends-on `06`; row `07.2` added as `planned` with depends-on `07.1`. Per BOOTSTRAP §5 + ADR-0005 the next transition is state 2 → 3 via `superpowers:writing-plans` (PLAN.md authoring for 07.1).
- **next-skill:** `superpowers:writing-plans` — PLAN.md authoring for phase 07.1 (state 2 → 3 per `BOOTSTRAP_PROMPT.md` §5 step 2 + ADR-0005). Inputs: this commit's `docs/envoy-go/phases/07.1-http-filter-framework/SPEC.md` (the authoritative design source; SPEC §15 acceptance checklist + §12 deferred-decisions list are the planner's primary scoping inputs), the parent `docs/envoy-go/phases/07-filter-chain-framework/SPEC.md`, the brainstorm-close `docs/envoy-go/phases/07-filter-chain-framework/BRAINSTORM.md`, the closed-history `docs/envoy-go/phases/06.1-stats-prometheus/PLAN.md` and `docs/envoy-go/phases/06.2-access-log/PLAN.md` as PLAN-shape precedent, `BOOTSTRAP_PROMPT.md` §§5–6 (lifecycle + plan-size split gate), `SKILL_ROUTING.md`, and `docs/envoy-go/DECISIONS.md` for ADR numbering (next-free is ADR-0070; the planner authors ADR-0070..ADR-0076 per SPEC §8). The seven anticipated ADRs are described with sufficient detail in SPEC §8 for PLAN-task allocation; the planner re-verifies next-free at write time per ADR-0004's autonomous-numbering rule.
- **next-skill-scope:** Lifecycle-state 2 → 3 PLAN drafting session deliverable: `docs/envoy-go/phases/07.1-http-filter-framework/PLAN.md` — task-by-task implementation plan derived from SPEC §4 (deliverables file inventory) + §13.1 (BEHAVIOR_CONTRACT additions) + §15 (acceptance checklist) + §8 (seven anticipated ADRs ADR-0070..ADR-0076). The §6.1 plan-size gate applies: if PLAN.md exceeds ~25 numbered tasks OR ~1500 LoC estimated net change, the planner splits 07.1 further into 07.1.1 / 07.1.2 / etc. SPEC §1 estimates the production-code surface at ~800-1200 LoC for the framework + ~150 LoC for cors + ~250 LoC for envoygotest + ~600 LoC of router-action migration (test bodies byte-preserved) + ~250 LoC of HCM dispatch wiring changes — total ~2050-2450 LoC. **The plan-size gate may trip**; the planner makes the split call per §6.1 + ADR-0045's planner-time-split discipline. After PLAN, advance STATE to lifecycle-state 3 with `next-skill: superpowers:subagent-driven-development` (per ADR-0005's default executor preference) and `active-phase` remains `07.1-http-filter-framework`. Worktree for the next session: branch from THIS SPEC commit's master fast-forward HEAD per ADR-0003 + per-phase-worktree convention. The L4 review-followup batch closing 11 Minors from phase 06.2 REVIEW.md remains a separate post-phase-done branch — NOT 07.1's scope; can run in parallel.
- **last-commit:** `ee45aba` — `phase 07.1: SPEC drafted (autonomous brainstorming per ADR-0004)`. Lands: `docs/envoy-go/phases/07-filter-chain-framework/SPEC.md` (parent master SPEC, 113 lines), `docs/envoy-go/phases/07.1-http-filter-framework/SPEC.md` (full sub-phase SPEC, 965 lines, with all four §11 empirical-pin blocks verbatim against reference Envoy v1.37.2), `docs/envoy-go/phases/07.2-listener-chain-completion/README.md` (sibling SPEC stub, 68 lines), the ROADMAP.md split (parent row 07 → in-progress + sub-phases column; rows 07.1 + 07.2 added as planned), and this STATE.md advance (lifecycle-state 1 → 2). No code changes in this commit; pure scaffold + state transition. Empirical-pin scaffolding lived at `/tmp/envoy-07.1-empirical/` during the SPEC session (not committed — those configs are SPEC-time evidence-gathering scaffolding, not fixture artifacts; the verbatim outputs are what's pinned).
- **last-updated:** 2026-04-30

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
