# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `07.2-listener-chain-completion`
- **phase-directory:** `docs/envoy-go/phases/07.2-listener-chain-completion/` — now contains `README.md` (sibling-stub seeded at the 07.1 SPEC commit `ee45aba`; read-only history), `SPEC.md` (1101 lines; drafted at master `bb5f437`; read-only history), and the full `PLAN.md` (2850 lines; drafted at this commit per ADR-0005 autonomous planning; plan-document-reviewer subagent approved at iteration 1 with three minor issues addressed in-place — `chainSpecificityRank` lifecycle clarified to deletion at Task 9 step 7 since chainmatch.go's `sniSpecificityRank` is the only live copy post-refactor; Task 14 step 2 pseudo-code's `DriveAlternate` error-check added; Architecture text's `AlternateConfigDriver` enumeration completed to 5 methods matching Task 13 step 1). Parent `docs/envoy-go/phases/07-filter-chain-framework/` carries `BRAINSTORM.md` (688 lines, `da28039`) + parent master `SPEC.md` (113 lines, `ee45aba`) — both read-only history. Sibling 07.1 directory at `docs/envoy-go/phases/07.1-http-filter-framework/` is closed read-only history. Phases 04, 05 (+ 05.1, 05.2), and 06 (+ 06.1, 06.2) remain closed read-only history.
- **lifecycle-state:** `3` — PLAN drafted, reviewer-approved. Next session executes `PLAN.md` per `BOOTSTRAP_PROMPT.md` §5 step 3.
- **next-skill:** `superpowers:subagent-driven-development` — phase 07.2 implementation session (state 3 → 4 per `BOOTSTRAP_PROMPT.md` §5 step 3). Per ADR-0005's default-executor preference (subagent-driven over inline) AND per the user's persistent preference recorded in MEMORY.md. The session creates worktree `.worktrees/phase-07.2-listener-chain-completion-impl` on branch `phase/07.2-listener-chain-completion-impl` (recommended per PLAN's `## Execution preconditions` #1) and dispatches one fresh subagent per task across the 17 numbered tasks per `superpowers:subagent-driven-development`. Each subagent reads PLAN.md task-N + SPEC.md cross-references + DECISIONS.md ADR-NNNN entries needed for that task; performs TDD per `superpowers:test-driven-development`; commits + SHA-fills; returns to the parent for inter-task review. On all 17 tasks complete, STATE advances to lifecycle-state 4 with `next-skill: superpowers:verification-before-completion`.
- **next-skill-scope:** Lifecycle-state 3 → 4 implementation session deliverable: 17 tasks committed in order on `phase/07.2-listener-chain-completion-impl`, each with its SHA-fill follow-up commit per the phase-02..07.1 SHA-fill convention. Tasks (per PLAN.md): T1 PROGRESS preamble + ADR-0077 + ADR-0083; T2 internal/listener/listenerfilter/{doc,types,callbacks}.go + ADR-0079; T3 registry.go; T4 pipeline.go + ADR-0082; T5 chainmatch.go + ADR-0080 + ADR-0081; T6 fuzz_test.go (FuzzFilterChainMatch — 10th fuzzer); T7 tls_inspector/parser.go; T8 tls_inspector/{doc,tls_inspector,proto}.go; T9 manager.go validateFilterChainMatch + default_filter_chain + listener_filters[] parsing + ADR-0078; T10 manager.go acceptLoop + serveConnection unified dispatch refactor; T11 cmd/envoy-go/main.go + internal/bootstrap/bootstrap.go boot wiring; T12 internal/listener/integration_test.go end-to-end accept-loop tests; T13 test/differential/fixture/fixture.go MultiListenerDriver + AlternateConfigDriver interfaces; T14 test/differential/runner_test.go branches; T15 test/fixtures/0008-listener-chain-match/ static files (configs + expectations + README + backends); T16 fixture-0008 driver + §11.4 carry-forward resolution per Decision K; T17 BEHAVIOR_CONTRACT in-place edit + closing six-gate sweep [all 7 ADRs]. Per ADR-0003: master FFs to this PLAN commit's SHA-fill follow-up SHA. The implementation session's exit advances STATE to lifecycle-state 4 (`next-skill: superpowers:verification-before-completion`); the verification session advances to 5 (`next-skill: superpowers:requesting-code-review`); the REVIEW session lands the phase-done commit (rows 07.2 + parent 07 → `done` AT THE SAME COMMIT per parent SPEC §5; STATE advances to phase-08 lifecycle-state 0 with `next-skill: superpowers:brainstorming`).
- **last-commit:** `3cf08d6` — `phase 07.2: PLAN drafted (autonomous planning per ADR-0005) + STATE → lifecycle-state 3`. This PLAN-drafting commit lands `docs/envoy-go/phases/07.2-listener-chain-completion/PLAN.md` (2850 lines; covers Goal / Architecture / Tech Stack / Scope check / ADRs introduced (7) / Settled SPEC §14 deferred decisions (15 A–O) / Carry-forward triage / Execution preconditions (14) / 17 numbered tasks with TDD steps + ADR-landing tasks (T1, T2, T4, T5, T9) / Refinement / Post-plan handoff / Plan review loop) + this STATE.md rewrite to advance to lifecycle-state 3 with `next-skill: superpowers:subagent-driven-development`. Per ADR-0003: master FF'd to this PLAN-drafting commit's SHA-fill follow-up SHA. SHA filled in a follow-up commit per the phase-04..07.1 SHA-fill convention.
- **last-updated:** 2026-05-02

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
