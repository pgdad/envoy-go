# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `02-tcp-proxy`
- **phase-directory:** `docs/envoy-go/phases/02-tcp-proxy/` — exists; contains `SPEC.md`, `PLAN.md`, `PROGRESS.md` (all 10 PLAN tasks landed plus a `## Verification` section with fresh-shell re-run evidence for every executable SPEC §3 gate), and `REVIEW.md` (landed in this session).
- **lifecycle-state:** `6` — REVIEW.md landed at commit `5c215f0` on branch `phase/02-tcp-proxy-impl` with verdict **APPROVED** (unconditional). Findings tally: 0 Critical, 0 Important, 8 Minor — all eight explicitly deferrable to phase 03+ per the reviewer's approval line, none blocking advancement. The reviewer dispatched via `superpowers:requesting-code-review` (general-purpose subagent, same dispatch model as phase-01) walked the commit range `013daa7..3da655d` (25 commits, ~3265 LoC, 33 files) and consumed SPEC.md, PLAN.md, PROGRESS.md (including the `## Verification` block), BEHAVIOR_CONTRACT.md's new `## TCP proxy` subsection, the seven new ADRs (0022–0028 — ADR-0022..0027 mapped 1:1 to SPEC §4.4's pre-committed ADRs A–F plus ADR-0028 as the mid-Task-10 determinism-fix ADR), and all production/test code under `internal/cluster/`, `internal/filter/tcpproxy/`, `internal/listener/`, `cmd/envoy-go/`, `test/helpers/`, `test/differential/`, `test/fixtures/0000-tcp-echo/driver/`, and `test/fixtures/0001-tcp-proxy-rr/`. Per SKILL_ROUTING state-5 scope the review is signal/intent-level: phase-done gates a–e evidence was cited from PROGRESS.md's `## Verification` block, not re-executed; reviewer spot-checks (`go build ./...`, ADR-count grep, file-existence walks) were clean. Axis-by-axis coverage: (1) PLAN.md fidelity — all 10 tasks landed per acceptance criteria with commits traceable through PROGRESS; (2) SPEC §12 acceptance checklist — all items PASS except the two intentionally-deferred-to-state-6 items (ROADMAP row 02 → `done` and STATE advance to phase 03); (3) SPEC doctrine items — all satisfied; (4) SPEC §3 gates a–e — all satisfied by code. The eight Minor findings (reviewer numbering) are: (1) ADR-0028 was unanticipated by SPEC §4.4's A–F map; (2) ADR-0028 bundles a determinism fix with an unrelated bug fix; (3) ADR physical-order drift carried forward from phase 01; (4) unused `ctx` parameter in `Filter.Handle`; (5) unbounded goroutine in `readyListenerAddrs`; (6) `""` sentinel in `FixtureDriver.Drive` is convention-not-type-enforced; (7) `expectations.yaml` carries prose rather than data; (8) missing cross-link from BEHAVIOR_CONTRACT `## TCP proxy` to ADR-0028. None block phase advancement. Per SKILL_ROUTING.md / BOOTSTRAP_PROMPT §5 state 6, the next session lands the phase-done close-out.
- **next-skill:** `superpowers:finishing-a-development-branch`
- **next-skill-scope:** Land the phase-02 close-out per SKILL_ROUTING.md state 6 + phase-00/01 precedent (see phase-01's `c1b32df` commit + the resulting master-side close-out commit for the exact shape). Concretely, the next session must: (i) merge branch `phase/02-tcp-proxy-impl` into `master` (preferring a single fast-forward — the branch is linear atop master at phase-02 open time; if master has advanced between now and the next session a rebase-then-ff is acceptable, a merge commit is not); (ii) in one atomic commit on `master` with message format `phase 02: COMPLETE — ROADMAP row 02 → done, STATE advanced to phase 03 (lifecycle-state 1)`, update `docs/envoy-go/ROADMAP.md` row 02 status → `done`, and update `docs/envoy-go/STATE.md` to set `active-phase: 03-tls`, `phase-directory: docs/envoy-go/phases/03-tls/` (directory creation deferred to that phase's state-1 session per SKILL_ROUTING step 1), `lifecycle-state: 1`, `next-skill: superpowers:brainstorming`, and a `next-skill-scope` that bounds brainstorming to phase 03 per ROADMAP row 03 ("Downstream TLS termination + upstream TLS origination + SNI. TLS TCP fixture green.") and phase-02 SPEC §9 deferrals. The close-out commit is the phase-done exit per BOOTSTRAP §5 state 6; no code changes land in it. Do not re-run gate commands — phase-02's green verification is captured in PROGRESS.md's `## Verification` block and the REVIEW approval at `5c215f0`. After the close-out commit lands, the worktrees `.worktrees/phase-02-tcp-proxy-impl`, `.worktrees/phase-02-tcp-proxy-plan`, and `.worktrees/phase-02-tcp-proxy-spec` can be removed as part of cleanup (their contents are preserved in master history via the merge); this is discretionary, not gated. If the merge surfaces a conflict (unexpected given the linear topology), stop and invoke `superpowers:systematic-debugging` per SKILL_ROUTING deviation clause. The eight Minor findings from `REVIEW.md` are NOT addressed in the close-out — they are explicitly deferred to phase 03+ per the reviewer's approval line.
- **last-commit:** 5c215f0
- **last-updated:** 2026-04-23

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
