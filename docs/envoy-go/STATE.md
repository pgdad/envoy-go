# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `07.2-listener-chain-completion`
- **phase-directory:** `docs/envoy-go/phases/07.2-listener-chain-completion/` — now contains `README.md` (sibling-stub seeded at the 07.1 SPEC commit `ee45aba`; read-only history), `SPEC.md` (1101 lines; drafted at master `bb5f437`; read-only history), and the full `PLAN.md` (2850 lines; drafted at this commit per ADR-0005 autonomous planning; plan-document-reviewer subagent approved at iteration 1 with three minor issues addressed in-place — `chainSpecificityRank` lifecycle clarified to deletion at Task 9 step 7 since chainmatch.go's `sniSpecificityRank` is the only live copy post-refactor; Task 14 step 2 pseudo-code's `DriveAlternate` error-check added; Architecture text's `AlternateConfigDriver` enumeration completed to 5 methods matching Task 13 step 1). Parent `docs/envoy-go/phases/07-filter-chain-framework/` carries `BRAINSTORM.md` (688 lines, `da28039`) + parent master `SPEC.md` (113 lines, `ee45aba`) — both read-only history. Sibling 07.1 directory at `docs/envoy-go/phases/07.1-http-filter-framework/` is closed read-only history. Phases 04, 05 (+ 05.1, 05.2), and 06 (+ 06.1, 06.2) remain closed read-only history.
- **lifecycle-state:** `5` — Verified, not reviewed. All six phase-done gates per `BOOTSTRAP_PROMPT.md` §7.5 re-ran fresh against HEAD `46e416c` on `phase/07.2-listener-chain-completion-impl` in this independent verification session per `superpowers:verification-before-completion` discipline (no trust of the Task 17 sweep — fresh evidence captured). All gates GREEN: (a) fixture 0008-listener-chain-match PASS; (b) all 9 pre-existing fixtures (0000-0007b) PASS; (c) h2spec 53/53 PASS at the ADR-0051 pin; (d) all 10 fuzzers PASS at `-fuzztime=30s` (9 inherited + new `FuzzFilterChainMatch`) with zero crashers; (e) `go build`, `go vet`, `golangci-lint run`, `go test -race -count=1 ./...` all clean across 30 packages with no DATA RACE. Gate (f) defers to the REVIEW session per BOOTSTRAP §5 step 6. Verbatim outputs quoted into PROGRESS.md `## Verification — six-gate sweep [BOOTSTRAP §7.5]` entry at this commit. Next session executes `superpowers:requesting-code-review` per `BOOTSTRAP_PROMPT.md` §5 step 5.
- **next-skill:** `superpowers:requesting-code-review` — phase 07.2 review session (state 5 → 6 per `BOOTSTRAP_PROMPT.md` §5 step 5). Produces `docs/envoy-go/phases/07.2-listener-chain-completion/REVIEW.md` against the SPEC + the implementation. On approval, the review session lands the phase-done commit (rows 07.2 + parent 07 → `done` AT THE SAME COMMIT per parent SPEC §5) and advances STATE to phase-08 lifecycle-state 0 with `next-skill: superpowers:brainstorming`. On issues, lifecycle re-enters at state 3 per BOOTSTRAP §5.2.
- **next-skill-scope:** Lifecycle-state 5 → 6 review session deliverable: produce `docs/envoy-go/phases/07.2-listener-chain-completion/REVIEW.md` per BOOTSTRAP §7.6, asserting the implementation against `SPEC.md` (every §3 phase-done gate, every §4 deliverable, every §5–§9 architectural surface, every §10 ADR commitment, every §11 empirical pin transcribed verbatim into BEHAVIOR_CONTRACT.md, every §16 boundary-grep). On approved REVIEW: the review session lands the **phase-done commit** with subject `phase 07.2: phase-done — listener-chain-completion lands; ROADMAP rows 07.2 + 07 → done [ADR-0077, ADR-0078, ADR-0079, ADR-0080, ADR-0081, ADR-0082, ADR-0083]` per SPEC §3 footer; the commit flips ROADMAP row 07.2 (`in-progress` → `done`) AND parent row 07 (`in-progress` → `done`) AT THE SAME COMMIT per parent SPEC §5; STATE.md advances to `active-phase: phase-08-...`, `lifecycle-state: 0`, `next-skill: superpowers:brainstorming`. On REVIEW issues: lifecycle re-enters at state 3 per BOOTSTRAP §5.2 and the implementer addresses findings before re-verification. The review session may also SHA-fill the `last-commit: TBD` literal below in the same phase-done commit (or a follow-up) per the established phase-04..07.1 convention.
- **last-commit:** `TBD` — `phase 07.2 verification: six-gate sweep all green + STATE → lifecycle-state 5`. This verification commit lands the `## Verification — six-gate sweep [BOOTSTRAP §7.5]` entry in PROGRESS.md (gates a-e fresh outputs quoted verbatim; gate f defers to REVIEW) and this STATE.md rewrite advancing lifecycle-state 4 → 5 with `next-skill: superpowers:requesting-code-review`. Per the established phase-04..07.1 convention, the `last-commit: TBD` literal is SHA-filled by the next follow-up commit (or by the REVIEW session as part of its own state ratchet).
- **last-updated:** 2026-05-02

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
