# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `06.1-stats-prometheus`
- **phase-directory:** `docs/envoy-go/phases/06.1-stats-prometheus/` — contains `SPEC.md` (at `ad46930`, frozen) plus `PLAN.md` (this commit). Parent `docs/envoy-go/phases/06-observability-baseline/` carries `BRAINSTORM.md` (`d271fd7`) + parent `SPEC.md` (`ad46930`). Sibling `docs/envoy-go/phases/06.2-access-log/` retains the placeholder `README.md` (`ad46930`). Phase 05 closed history unchanged.
- **lifecycle-state:** `3` — phase 06.1 PLAN drafted; the PLAN-drafting session on `phase/06.1-stats-prometheus-plan` (worktree `.worktrees/phase-06.1-stats-prometheus-plan`, branched from `be99b42` per ADR-0003) lands `docs/envoy-go/phases/06.1-stats-prometheus/PLAN.md` (~3463 LoC, 15 tasks). Task topology mirrors the 05.2 PLAN's 15-task shape: Task 1 preconditions + PROGRESS preamble; Tasks 2-5 build the `internal/stats` package (registry/counter/gauge/name/prom-writer) with TDD; Task 6 admin endpoint; Task 7 bootstrap Registry alloc; Tasks 8-12 stat-emit call site threading + boot wiring (cluster, listener, HCM, main.go, Freeze ordering); Task 13 `FuzzPromTextFormat`; Task 14 fixture `0005-prometheus-stats`; Task 15 BEHAVIOR_CONTRACT in-place edit + ROADMAP/STATE updates + closing all-gates sweep. Six ADRs anchored: ADR-0059 (Internal Stats Store) + ADR-0060 (Histograms deferred) bound to Task 2; ADR-0061 (SN1-SN8 flattening, Rule SN4 pinned) + ADR-0064 (`stats_config.stats_tags` not honored) bound to Task 4; ADR-0063 (per-endpoint cluster stats not emitted) bound to Task 8; ADR-0062 (differential equivalence shape for stats) bound to Task 14. ADR numbers re-verified next-free at PLAN-write per ADR-0004 against `DECISIONS.md` tail (ADR-0058 latest at `ad46930`). Net LoC estimate: ~1700 (within BOOTSTRAP §6.1 single-phase gate). Per BOOTSTRAP §5 the next transition is state 3 → 4 via `superpowers:subagent-driven-development` (06.1 implementation per the user's persistent subagent-driven-execution preference).
- **next-skill:** `superpowers:subagent-driven-development` — execute the 15 PLAN tasks via fresh-subagent-per-task with two-stage review per ADR-0005. Inputs: this commit's `docs/envoy-go/phases/06.1-stats-prometheus/PLAN.md` (the authoritative task decomposition), the 06.1 SPEC at `ad46930` (the design contract every task traces back to), the parent 06 SPEC at `ad46930` (cross-cutting context), `BOOTSTRAP_PROMPT.md` §§5-7 (lifecycle + differential test contract; §7.5 phase-done gates), `superpowers:test-driven-development` (the per-task TDD discipline), `docs/envoy-go/phases/05.2-upstream-h2/{PLAN.md,PROGRESS.md,REVIEW.md}` (the closed-history precedent for execution rhythm + PROGRESS conventions + REVIEW shape).
- **next-skill-scope:** Lifecycle-state 3 → 4 implementation session, **resume at Task 14**. Tasks 1–13 are landed and SHA-filled on branch `phase/06.1-stats-prometheus-impl`. Tasks 1–9 (prior session, exit at `a68fdb5`): T1 `04bf75d`; T2 `9417235` + `7ccdc73`; T3 `474e8c7`; T4 `7f45a4d` [ADR-0061 + ADR-0064]; T5 `18fbb41`; T6 `f4160b8` + `17da0e3` + `6a1f93c`; T7 `d1e2690`; T8 `a6f8b94` [ADR-0063]; T9 `bdfc34b` (5 Minors → L4). Tasks 10–13 (this session): T10 `5dc76ab` + `8ddd6c9` SHA-fill + `df46e24` (5 Minors → L4 — listener Registry threading, registration-at-Start deviation due to `:0` collision, pre-existing `rt.netLn` race fixed in scope, IPv6-bracket-aware `normalizeAddr`); T11 `438bd4f` + `e545113` SHA-fill + `e68020a` review-followup (rename `UpstreamRqTotalInc`→`IncUpstreamRqTotal` for verb-prefix consistency with `IncStatusClass`; deviation #6 added — exported `*Cluster` accessors required for cross-package Inc; 4 Minors → L4); T12 `a07b5dc` + `7f754d6` SHA-fill + `6139b11` review-followup (admin throwaway → `bs.Stats` swap; `bs.Stats.Freeze()` post-`MarkReady`; test assertion strengthened from PLAN's single-name `# HELP envoy_server_live` sketch to dual-name unification witness because the literal sketch falsely PASSes pre-Task-12; 2 Minors → L4); T13 `63bb098` + `e7b52db` SHA-fill + `103b81b` review-followup (FuzzPromTextFormat at 30s; embedded `labelValue` in synthetic metric name with `.`→`_` neutralization to avoid degenerate fuzz; `}`-aware `validatePromLine` because writer's `escapeLabelValue` only escapes `\`/`"`/`\n` not space; corpus regression `1d8483e640bf8347` saved from real fuzzer find on `.000 ` trailing-space input; all 7 fuzzers green at 30s budget — `FuzzFrameStream` had a transient first-run timeout under 32-worker race-detector pressure but re-run was authoritative; 5 Minors → L4). **Tasks 14–15 remain:** T14 differential fixture `test/differential/0005-prometheus-stats/` (envoy.yaml + envoy-go.yaml + driver) + runner registration + ADR-0062 (differential equivalence shape for stats: per-counter delta-equality + per-gauge snapshot-equality on the 17 metric names against reference Envoy v1.37.2 under a 5-request defined load); T15 BEHAVIOR_CONTRACT.md ## Stat-name mapping in-place edit per ADR-0061 + ROADMAP row 06.1 in-progress→done + STATE.md → lifecycle-state 4 with `next-skill: superpowers:verification-before-completion` + closing all-gates sweep (`go vet`, `go build`, `go test -race -count=1 ./...`, all 7 fuzzers @30s, differential fixtures 0000–0005 green, h2spec 53/53). Continue per `superpowers:subagent-driven-development` in worktree `.worktrees/phase-06.1-stats-prometheus-impl` (branch HEAD = `103b81b`); each task ends with a commit on the impl branch; SHA-fill follow-up commits land in the same impl branch; per Task-8/9/10/11/12/13 precedent, code-quality Minors carry to lifecycle-state-4 review-followup via a third per-task follow-up commit. After lifecycle-state 4 (Tasks 14+15 done; BEHAVIOR_CONTRACT edits landed; ROADMAP row 06.1 flipped done; PROGRESS Task 15 verification block captured), advance STATE to lifecycle-state 4 with `next-skill: superpowers:verification-before-completion` for the verification session re-run of all six gates per BOOTSTRAP §7.5 (phase-04..05.2 precedent — verifier branches a fresh worktree from the impl-branch tip per ADR-0003). Carry-forwards from this session that the next session should keep in mind: (i) the `routeAction.do` interface widened to `(int, error)` at T11 (Deviation #2) is consumed by `runConnection` for the response-class Inc — Task 14's fixture driver must respect the new signature when stubbing actions in any tests it adds; (ii) the `bs.Stats` Registry is now Frozen post-boot per T12 — Task 14's runner-side construction of subject envoy-go either uses the binary subprocess (preferred per T12 precedent) or constructs a non-Frozen Registry locally for harness-side metric collection; (iii) the BEHAVIOR_CONTRACT.md ## Stat-name mapping section is currently `_to be filled per-phase as needed._` (skeleton at line 50 of master `BEHAVIOR_CONTRACT.md` per the bootstrap-time skeleton) — Task 15's in-place edit replaces the skeleton with the full 17-name mapping table per SPEC §6 + ADR-0061 SN1-SN8 rules + Rule SN4 empirical pin from BRAINSTORM evidence. Total impl carryforwards across Tasks 8–13 to L4 review-followup: 5 + 5 + 5 + 4 + 2 + 5 = 26 Minor findings, all polish-level; the L4 review-followup session will triage and either land or accept-as-is per the 05.2 review-followup precedent.
- **last-commit:** `103b81b` — `phase 06.1 follow-up: Task 13 PROGRESS — 5 Minors carried to L4 review-followup`, on branch `phase/06.1-stats-prometheus-impl`. The Task-13 review-followup commit replaces a stale "no Minors carried forward" parenthetical in the PROGRESS Task-13 `**Commits:**` line with the 5-Minor enumeration the code-quality reviewer surfaced (M-1 empty-`{}` over-acceptance; M-2 `NaN`/`Inf` over-acceptance; M-3 FuzzFrameStream transient-flake observability; M-4 `matchingBraceEnd` naming nit; M-5 `validatePromLine` predicate-naming nit). Branch tip is `103b81b` at session exit. Resume next session at Task 14 per `next-skill-scope`. The full task-by-task commit chain Task 1 → Task 13 (each task: impl + SHA-fill + review-followup; latter on Tasks 8/9/10/11/12/13) is the durable record; no STATE-blocking issues encountered.
- **last-updated:** 2026-04-27

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
