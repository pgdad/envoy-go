# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `14-http-filter-compressor` — phase 14 brainstorm landed at this commit. ROADMAP row 14 added with status `planned` per BOOTSTRAP_PROMPT.md §4.1 invariant 3 + §5 lifecycle-state-0 → 1 transition. Phase 14 is the SEVENTH §9 family-row to enter the lifecycle (after cors @ 07.1 done, fault @ 09 done, header_mutation @ 10 done, local_ratelimit @ 11 done, csrf @ 12 done, buffer @ 13 done); the §9 family heading at ROADMAP line 56 stays unchanged per ADR-0106(c). Filter selection (`compressor` — `envoy.filters.http.compressor`, response-side, gzip-only) was the brainstormer's choice per ADR-0106(b) + the user's interactive 6-question dialogue (Q1 = `compression` chosen from `compression / rbac / bandwidth_limit / jwt_authn`; Q2 = `gzip-only` chosen from gzip / +brotli / +brotli+zstd / +zstd; Q3 = `response-only with request_direction_config silent-ignored`; Q4 = `Path B buffer-then-compress with documented wire-shape divergence`; Q5 = `full envelope; deprecated top-level mirrors silent-ignored`; Q6 = `full ~11 counters mirror Envoy`).
- **phase-directory:** `docs/envoy-go/phases/14-http-filter-compressor/` — contains `BRAINSTORM.md` (541 lines) at this commit. SPEC.md, PLAN.md, PROGRESS.md, REVIEW.md authored over phase lifecycle (states 2 → 6). The BRAINSTORM commits 8 design Decisions across §2 (filter package layout `compressor/` / extension-registry registration / 8-consumed-9-ignored MVP envelope + parse-rejection of unknown `compressor_library` TypeURL / codec-library Any-unmarshal-and-dispatch shape with Gzip compression-level mapping table / per-route `disabled`-OR-`overrides` SECOND row using ADR-0125 5th canonical / Path B buffer-then-compress body algorithm + wire-shape divergence with forward-pointer / 11-counter stat surface 29→40 + new SN10 Prometheus tag-extractor / Vary + Content-Encoding + Accept-Encoding parser semantics), 6 differential-fixture scenarios at §6 (allow-compress, skip-content-type, skip-content-length-too-small, skip-on-etag, per-route-disabled, per-route-override), 5 anticipated ADRs at §7 (ADR-0129..ADR-0133) + ADR-0125 amendment paragraph (in-place at SPEC time per phase-13 ADR-0127-v2 precedent), 8 inline-deferral items at §8 (brotli + zstd codec extensions / request-side compression / future `envoy.filters.http.decompressor` / chunked-encoded response wire shape / Gzip sub-knobs not in Go gzip / `choose_first` selection mode / `Compressor.runtime_enabled` + `response_direction_config.common_config.enabled` runtime gates / deprecated top-level mirrors), and 15 empirical-pin obligations at §9 (§11.P1..§11.P15 to be resolved IN-SESSION at SPEC time per ADR-0004). NO sibling-stub was authored per ADR-0106(b). NO off-master prebrainstorm-notes branch was authored.
- **lifecycle-state:** `phase 14 brainstormed; SPEC.md pending` — phase 14 BRAINSTORM.md authored at this commit; SPEC.md not yet authored. Lifecycle-state-1 → 2 transition (BOOTSTRAP_PROMPT.md §5) is the next session's responsibility. Phase 13's six gates remain green at parent commit `fd976db`; this commit is docs-only (no code changes; no test re-validation needed).
- **next-skill:** `superpowers:writing-plans` per BOOTSTRAP_PROMPT.md §5 lifecycle-state-2 entry condition (SPEC.md exists, PLAN.md does not — phase 14 enters this state once SPEC.md is authored; the routing convention from phases 09/10/11/12/13 is that writing-plans is called once for SPEC drafting, then again for PLAN drafting based on the SPEC). The SPEC author resolves the 15 §9 empirical-pin obligations IN-SESSION against reference Envoy v1.37.2 per ADR-0004.
- **next-skill-scope:** Cold-start scope for the phase 14 SPEC-authoring session. Read first: `docs/envoy-go/phases/14-http-filter-compressor/BRAINSTORM.md` (the authoritative design doc — every load-bearing decision lives there per D-3.4); `docs/envoy-go/ROADMAP.md` row 14 (now `planned`); `docs/envoy-go/BEHAVIOR_CONTRACT.md ## HTTP filter chain ### envoy.filters.http.buffer` subsection (the just-shipped phase 13 contract — phase 14 mirrors its structural shape with response-side adaptation) + `### Phase 13 forward-pointer notes` (ADR-0076 cap-promotion forward-pointer is NOT promoted by phase 14 since MVP is response-only; it remains open for a future row); `docs/envoy-go/DECISIONS.md` ADR-0125 (5th canonical disabled-OR-override per-route discipline — phase 14 SECOND row to use; amendment paragraph required at SPEC-time mirroring phase-13 ADR-0127-v2 in-place-update precedent) + ADR-0128 (phase 13 framework-deltas precedent — phase 14's §3 framework survey may add an `EncoderFilterCallbacks.OverwriteBody(b []byte)` primitive symmetric on encode side, OR find no delta needed if existing primitive is sufficient) + ADR-0118 SN9 Prometheus tag-extractor (phase 14 introduces SN10 next-after pattern) + ADR-0073 wholesale-override per-route precedent (phase 14 inherits) + ADR-0129..ADR-0133 next-free roster; `internal/filter/hcm/connection.go:467-475` (the encode-chain shape: one-shot full-body `RunEncodeData(ctx, resp.Body, true)` — phase 14 Path B operates within this constraint) + `internal/filter/hcm/codec.go:74-119` (`writeH1Reply` unconditional fixed-`Content-Length` rewrite at line 87-89 — phase 14 emits compressed-length CL; chunked-encoded output not supported → wire-shape divergence from Envoy documented at ADR-0131); `docs/envoy-go/phases/13-http-filter-buffer/SPEC.md` + `phases/13-http-filter-buffer/PROGRESS.md` (the most-recent phase artefacts — phase 14 BRAINSTORM is structurally derived from phase-13's). The 15 empirical pins at BRAINSTORM §9 (§11.P1..§11.P15) are the SPEC author's IN-SESSION obligation per ADR-0004; the empirical-pin scrape evidence lands verbatim in SPEC §11. Highest-priority pins for SPEC-time scrape: §11.P3 (`response_direction_config.common_config.enabled` PGV requirement — fixture configs MUST set explicitly to 100%/HUNDRED on Envoy side; mirrors phase-12 csrf P11), §11.P5 (stat namespace + codec-tag embedding — determines SN10 1-tag vs. 2-tag extraction), §11.P9 (wire shape on small responses — confirms or amends the Path-B wire-shape divergence-window), §11.P14 (gzip wire compatibility — confirms the decompressed-byte-exact differential-fixture discipline at ADR-0133).
- **last-commit:** `e8f75cf` — `phase 14 brainstorm: ROADMAP row 14 + STATE.md flip`. Adds ROADMAP row 14 with status `planned`; flips STATE.md from "awaiting next planning" (phase 13 closed) to "phase 14 brainstormed; SPEC.md pending" with active-phase pointer to phase 14 + next-skill rotation to `superpowers:writing-plans`. Parent commit `1158c30` (`phase 14: BRAINSTORM — envoy.filters.http.compressor (response-side, gzip-only)`) authored `docs/envoy-go/phases/14-http-filter-compressor/BRAINSTORM.md` (541 lines) capturing the 6-question interactive design dialogue + 8 design Decisions + 6 differential-fixture scenarios + 5 anticipated ADRs (ADR-0129..ADR-0133) + ADR-0125 amendment paragraph + 8 inline-deferral items + 15 empirical-pin obligations. Docs-only chain (BRAINSTORM + ROADMAP/STATE flip); no code changes; phase 13's six gates remain green at parent commit `fd976db`.
- **last-updated:** 2026-05-10

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
