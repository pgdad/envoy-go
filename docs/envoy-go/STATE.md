# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

> **This file is a POINTER, not an archive** — `BOOTSTRAP_PROMPT.md` §4: *"STATE.md — pointer: active phase, last commit, next action."* It carries the live pointer, the live counts, and a SHORT recent lineage. **Full stage-by-stage history lives in `STATE_HISTORY.md`** (archived at ADR-0288; **nothing was deleted** — the pre-move file is preserved there verbatim, byte-for-byte).
>
> **⚠️ THE RULE ADR-0288 EXISTS TO ENFORCE: there is EXACTLY ONE live `next-skill:`, `lifecycle-state:`, `next-free ADR:`, and count block IN THIS FILE, and they live in §Current pointer directly below.** Before that move, ~330 lines of prepended backlog had buried TWO fossilised "current state" blocks whose `next-skill:` still read *"the 42.2 hedging BRAINSTORM"* and *"the 46.2 SPEC"* while the live phase was **66** — so `grep 'next-skill:' STATE.md` returned **two stale hits and ZERO live ones**, and `grep 'fuzzers:'` returned **48** and **42** when the true count was **55**. **A session may trust a grep of this file again. Keep it that way: when you advance the pointer, EDIT §Current pointer IN PLACE — never prepend a new block above it.**

---

## Current pointer

*(The ONLY live "what next" in this repo. Update IN PLACE at every stage close.)*

- **active-phase:** `phase 100 (listener-filters-timeout-enforce) BRAINSTORM done` (2026-09-21) — lifecycle-state **DONE -> 1**. **PHASE 100 OPENED BY SELF-PICK** under the 2026-07-12 standing directive, no human consulted. **ROW 100 ADDED `in-progress`** at `ROADMAP.md:162` (an ADD, not a flip: `want` **131 -> 132**, `ROADMAP.md` **249 -> 250**). **THE SUBJECT: `listener_filters_timeout` IS NEVER ENFORCED** — on a TCP listener with `tls_inspector`, a client that sends nothing is held with NO deadline until the CLIENT acts (50 silent connections past a 1 s timeout: `downstream_cx_active` **50**, open fds **7 -> 57**), while the reference (by digest `7edd5b0fd763…`) times out AT the deadline, books `listener.<addr>.downstream_pre_cx_timeout`, and CLOSES under `continue_on_listener_filters_timeout: false` (FIN at **1000-1002 ms**) or falls through to chain selection under `true`. The phase-99 BRAINSTORM's next-in-line adjudication was **ADOPTED ONLY AFTER RE-MEASURING BOTH SIDES AT THIS TIP** (method note 69). **THIRTEEN claims refuted by execution**, among them: the drop under `false` is not unreachable but LAZY, timed by the client; the `[1s, 60s]` envelope is envoy-go's OWN (the reference enforces `0.5s`, `61s`, `120s`); **`0s` DISABLES the reference's timeout where envoy-go reads 15 s, so a repair that skips it MINTS a divergence**; the deadline-only repair is **RACY** (65 of 100 concurrent drops lost without a wall-clock check); the reference PARKS a silent connection under `true` for 90 s too, so "resource exhaustion" binds only under `false`; the phase-98 absence matcher's pathspec never matched `manager.go`; and `REVIEW_FINDINGS.md:185-187` had recorded the defect on 2026-07-07. **CHARTER: enforce the deadline, close under `false`, fall through as `raw_buffer` under `true`, book `downstream_pre_cx_timeout` (+1 NAME), make `0s` disable.** Floor **`23 6`** in `manager.go` only — a FLOOR.
- **phase-directory:** `docs/envoy-go/phases/100-listener-filters-timeout-enforce/` — `BRAINSTORM.md` (**514**), re-derived by `wc -l` in the publishing commit. Phase dirs **140 -> 141**. ⚠️ **A BRAINSTORM'S FILE SCOPE IS MEASURABLE:** this stage touches **5** paths — `ROADMAP.md` `1 0`, `STATE.md`, `STATE_HISTORY.md` `2 0`, the new `BRAINSTORM.md`, `next-prompt.txt` — the same path set as the phase-99 BRAINSTORM (`c9b9ecfc`). **Wording is not scope — `git show --numstat` it.**
- **lifecycle-state:** **1** (`BRAINSTORM.md` exists, `SPEC.md` does not; BOOTSTRAP §5 ⇒ the next session is at **state 1 -> 2**). ⚠️ **THE TERMINATION SENTINEL WAS RE-EVALUATED MECHANICALLY ON BOTH SIDES OF THIS STAGE'S ADD AND DOES NOT FIRE** — POST-ADD: check (1) **ONE** (`NOT DONE: row 100`), check (2) **SIX** at `:210 :216 :222 :232 :238 :246` with all six per-line md5s (trailing newline INCLUDED) byte-identical to the phase-99 close (the windows MOVED +1 and did not CHANGE), check (3) **SILENT**; NC-A **TWO** (`NOT DONE: row 62`, `NOT DONE: row 100`), NC-B **TWO** at `want=131` (`NOT DONE: row 100`, `GATE FAIL: examined 132 data rows, expected 131`), NC-C FIRED (residual 0), NC-D **96 / 68** under `--`, check-(2) positive control **6 substitutions ASSERTED, residual 0**, escape-aware malformed set exactly **{57, 69}**, row 100 **8 fields under BOTH forms**, fixture set **126 = 126** (split **102 `driver/` + 24 `inputs/`**), both `comm` directions EMPTY. PRE-ADD shapes (check (1) SILENT, NC-A ONE, NC-B ONE at `want=130`) measured first — **both sides, neither inherited.** **`stop` was evaluated and deliberately NOT created.** ⚠️⚠️ **THE MARGIN IS STILL ONE.**
- **next-skill:** **WRITE PHASE 100's `SPEC.md`** (`superpowers:brainstorming` scoped to this phase, BOOTSTRAP §5 state 1 -> 2), discharging all **NINE** owed items of `BRAINSTORM.md` §10 — ⚠️ **ONE STAGE PER SESSION.** The load-bearing ones: **choose the repair SHAPE against the measured race** (§0.5, §3.1 — gate it on the 50-connection `continue…: false` arm, run at least twice); **measure the subject under the chosen shape for the one-byte `true` arm** (the reference answers `400` AT the deadline); **specify the `0s` fold and re-point the zero → 15000 parse pin after reading its inputs**; **decide the close KIND or pin none** (the reference sends FIN for a silent peer, RST when a peeked byte is unread; the subject is unmeasured); **replace the vacuous abort assertion** (`manager_test.go:4036`, satisfied by its own client's read deadline); charter fixture `0125` with a silent-client Drive hook and NO `downstream_cx_total` pin on a drop arm (the reference counts it post-filter); draft **ADR-0322** §Context in the house `> **STATUS: PROPOSED` form, superseding ADR-0082's *honoured* and *zero → 15 s* clauses and leaving its envelope clause for the next row; predict the ledger delta **+1 NAME**.
- **last-commit:** ⚠️ **TAKE THE TIP FROM `git rev-parse master`, NEVER from a subject grep and NEVER from a count quoted anywhere.** ⚠️ **RUN *BOTH* LOCATE FORMS AND RECONCILE THE SETS** — `git log --grep '^phase 100 (listener-filters-timeout-enforce)'` (the SLUG form) and `git log --grep '^phase 100'` (the LOOSE form). **NO COUNT IS QUOTED HERE, DELIBERATELY — both forms rot.** ⚠️ **AND THE BLINDNESS IS IN THE STAGE-WORD FILTER, NOT THE LOOSE FORM**: `--grep '^phase 95 (.*) IMPL'` reads ZERO while `--grep '^phase 95'` DOES contain `0f3b98b3`. This BRAINSTORM landed as ONE squashed commit carrying **both the full slug AND the stage word**.
- **last-updated:** 2026-09-21 (phase-100 **BRAINSTORM done**). Fresh worktree off master `d5bc9153`, branch `wt-phase-100-brainstorm`, per `feedback_git_worktrees`; subagent-driven — a Docker-only REFERENCE agent (band `16400-16427`, containers `p100ref-*` torn down by name) and a Docker-free SUBJECT agent (band `16450-16499`, a throwaway worktree created and removed), the phase-98 split. **NONE OF THE SIX GATES RUN — A BRAINSTORM'S SCOPE, NOT AN OMISSION.** The one STANDING DEPARTURE is no `REVIEW.md` (NONE of 93-99); ⚠️ **`golangci-lint` (under `GOTOOLCHAIN=go1.26.2`) and gate (b) (ambient go1.27.1) are NOT departures — do not restate either.**
- **next-free ADR:** **`ADR-0322`** — **UNMOVED by this stage; the TAIL is `ADR-0321` and it is `ACCEPTED`.** ⚠️ **THE HOUSE GUARD `^> \*\*STATUS: PROPOSED` IS DISARMED — it reads EMPTY, and that is CORRECT, not a broken matcher**: proven by reading the tail ADR's own status line directly (`DECISIONS.md:19363`, `> **STATUS: ACCEPTED`). **The phase-100 SPEC's new tail ADR RE-ARMS it.** ⚠️ **THE ADR-0231 DECOY AT `:14866` STILL HITS under `^\*\*Status:\*\* PROPOSED` and is BYTE-UNTOUCHED (md5 unchanged against `515856da`) — reading only the decoy inverts the error.** `DECISIONS.md` **19487** lines, `^---$` **216**, `^## ADR-` **320**, bare `^## ` **328**, tail **ADR-0321** — all five re-derived at this close and UNMOVED (a BRAINSTORM touches no `DECISIONS.md` byte). ⚠️ **Derive next-free from the TAIL, never from the heading count** (the `0209` gap), and ⚠️ **no count of either `PROPOSED` matcher is written in any prose the grep matches.** ⚠️ **ADR-0082 (the timeout's governing ADR) is an OLD-FORM `**Status:** Accepted` ADR** — the SPEC supersedes its clauses in the new ADR, it does not rewrite ADR-0082's decision text.

---

## Project counts at this commit

*(⚠️ **NOT LIVE — THIS BLOCK IS A PHASE-76-ERA SNAPSHOT, AND IT SAID "Live" FOR SIXTEEN PHASES.** Corrected at the phase-93 BRAINSTORM. The bullets below are RETAINED VERBATIM for their historical detail and their negative controls; **their headline figures are stale and are superseded by the correction table immediately below.** The live pointer is §Current, above.)*

⚠️ **CORRECTION LANDED AT THE PHASE-93 BRAINSTORM (2026-08-29).** The bullets below were re-run mechanically at the **phase-76 IMPL close** — sixteen phases ago — under a header that read *"(Live. Re-verify at session start …)"*. **Three of their figures were re-derived at `40cf6766` and are superseded here; each command is given so the next session need not trust this line either:** **active differential fixtures 119 -> `121`** (`ls -d test/fixtures/*/ | wc -l`; tail `0119-grpc-unary-trailers`, **`0120` FREE** — ⚠️ the plausible `grep -cE '^[0-9]{4}-'` form reads **119** because it drops `0007a`/`0007b`) · **fuzzers 55 -> `56` targets across `48` FILES** (`git grep -c '^func Fuzz' -- '*.go'`; the occurrence form agrees — ⚠️ **it went stale INSIDE row 92's own commit**, which added `FuzzValidateResponseHeaderBlock` while the row's ROADMAP text still anticipates `+0`) · **DECISIONS.md tail ADR-0298 -> `ADR-0314`, next-free `ADR-0315`** (TAIL-derived; headings+1 COLLIDES at the `0209` gap). ⚠️ **THE STAT SURFACE IS DELIBERATELY NOT RENUMBERED.** Three different absolutes are live in this tree at this one tip — this block's, the phase-92 `PLAN.md`'s, and the `BEHAVIOR_CONTRACT.md` ledger tail's — and the contract warns of itself that a mechanical re-derivation *"should expect the re-derived figure to disagree."* Per `reference_a_drift_correction_is_itself_a_claim`, **on a contested count: NO NUMBER.** Enforcement remains the per-phase `TestNoNewStat*` delta guards, never an absolute. ⚠️ **AND `406` / `406 -> 407` MUST NOT BE RESTATED ANYWHERE** — refuted at the phase-92 PLAN; `grep -c '406' BEHAVIOR_CONTRACT.md` reads **0**. **The remaining bullets (BackendKind tail 38, go.mod, reference pin, conformance) were re-verified at `40cf6766` and still hold.**

- **active differential fixtures:** **119** — ⚠️ **Phase 76 landed +0: `0061-lb-ring-hash` was EDITED, not added** (`sourceIPs` 4 -> 16 plus a NEW `driver/linkage_test.go` inside the EXISTING fixture directory — no new YAML, no new directory, no new BackendKind, no new port), so the count STAYS **119** and `0118` remains FREE. NC observed: `mkdir .../0118-fake` ⇒ 120. — `ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0117-tracing-custom-tags-metadata-cluster` (phase 73). **Phase 75 landed +0**: `0110-tls-require-client-cert-false` was **EXTENDED** with a cross-side `fixture.StatsAsserter` (NO new YAML, NO new directory, NO new BackendKind, NO new port), so the count STAYS **119** and `0118` remains FREE. ⚠️ **Next-free REFERENCE port is `10450`, NOT `10118`** — ports are NOT fixture-index aligned. **The fixture now asserts what its own README confessed was infeasible, and BOTH sides were EXECUTED**: `ssl.handshake=2 ssl.no_certificate=1 ssl.fail_verify_error=1 ssl.fail_verify_no_cert=0`, `downstream_cx_total=3`, **identical on reference and subject**, with the discriminating asymmetry (`handshake=2` vs `no_certificate=1`) observed per side. Shape A over `/stats/prometheus`, keyed on the metric NAME with `envoy_listener_address` IGNORED — the NAMES are byte-identical cross-side while the labels differ (`0.0.0.0_10446` reference vs **`___20016`** subject: ⚠️ **the subject binds the IPv6 WILDCARD**). ⚠️ **`0111` was NOT given a value assertion, deliberately** — it runs `require_client_certificate: true` on both sides, so a success-path no-cert annotation reads **0 on every arm, structurally**, and a value pin would document a **vacuous `0 == 0`** as coverage; its PROSE gained the name in its NAMED-UNASSERTED list instead, because **both `0111` passages are CLOSED ENUMERATIONS** and a name in neither list reads as ASSERTED. **`0111`'s driver is byte-untouched, and its named-subset check was EXECUTED for the first time at this IMPL: PASS.** ⚠️ `var _ fixture.StatsAsserter` is a **TRIPWIRE, not the dispatch mechanism** — `0110` is only the **THIRD** fixture repo-wide to carry it, while **82** define an `AssertStats` method.
- **fuzzers:** **55** — **Phase 76 landed +0** (it consumes no new config field, so there is no parse arm to fuzz). NC observed: appending `func FuzzProbeOnly` ⇒ 56. — `grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`. **Phase 75 landed +0** — the row consumes no new config field at all, so there is no parse arm and no fuzz-seed classification to flip (`reference_fuzzer_count_docs_drift`: an `f.Add` seed is not a fuzzer).
- **stat surface:** **1205** — BEHAVIOR_CONTRACT doc count. **There is NO mechanical counting command** (the contract says so itself); enforcement is per-phase `TestNoNewStat*` guards asserting a delta of ZERO, never the absolute total. **Phase 75 LANDED +1 (1204 -> 1205)** — `listener.<normalized-addr>.ssl.no_certificate`, the FOURTH listener-scope `ssl.*` name and the FIRST **SUCCESS-PATH** handshake annotation (⚠️ **NOT a synonym for `ssl.fail_verify_no_cert`, and the mis-mapping is wrong in BOTH directions** — one annotates an ACCEPTED handshake, the other books a REJECTED one; they are mutually exclusive on every connection). ⚠️ **The delta is a NAME-surface delta, NOT a flat per-deployment one:** registration rides the block that ALREADY gates on `rt.tlsMode`, so a **plaintext-only** deployment gains **ZERO** names, while a **QUIC** listener gains it permanently zero — **PARITY** with the reference, and structural (`Manager.Start` launches no accept loop for `kindQUIC`, so the Inc site is unreachable for a **name-independent** reason). ⚠️ **QUIC was NOT re-probed at phase 75** — the parity claim is INHERITED from phase 74's family-level probe, and that inheritance is stated in BEHAVIOR_CONTRACT rather than presented as a measurement. Ledger updated: `1204` -> `1205` at `BEHAVIOR_CONTRACT.md:831`/`:847`, plus a `**Phase 75 - 1204 -> 1205 (+1)**` line after the tail entry — **the ONLY line-adding edit in the file** (`5744 -> 5746`), so every by-line citation into the TLS subsection stays valid. ⚠️ **The new entry RECORDS BOTH ledger gaps and BACK-FILLS NEITHER** (fabricating a line would be inventing a record). Gate figures at this close: `grep -c '1204'` => **2** · `grep -c '1205'` => **3** · `grep -cE '^#{2,4} .*ADR-[0-9]+.*ADR-[0-9]+'` => **17** (was 16 — the `:1849` heading joined them, on the `:785` precedent). ⚠️ **`internal/statssink`'s four *"stays 1200 / 1196"* prose sites, stale since phase 49, are RECORDED-NOT-FIXED** — that package is on this phase's byte-untouched sha256 roster.
- **BackendKind tail:** **38** — **Phase 76 landed +0** (`0061` uses the in-process `TCPEcho` branch and implements neither `BackendKindAware` nor `PerHostBackendKind`). NC observed: appending `ZZProbeKind = 39` ⇒ tail 39. — (`H2GoawayResponder`, `test/differential/fixture/fixture.go:614`). **Phase 75 landed +0** — `0110` was extended, not cloned, and it implements neither `BackendKindAware` nor `PerHostBackendKind`, so the runner takes the in-process `TCPEcho` branch. ⚠️ **This is a TAIL VALUE, not a count** — the file declares **39** constants (values 0-38, `TCPEcho = 0`). The lineage figure **38** is correct as written; do NOT 'fix' it to 39.
- **DECISIONS.md tail:** **ADR-0298** (next-free **ADR-0299**) — `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1` -> `## ADR-0298`; `grep -c '^## ADR-0299'` ⇒ **0**. **ADR-0298 is COMPLETE as of the phase-76 IMPL** — §Context drafted at the phase-76 SPEC, §Decision + §Consequences APPENDED IN PLACE after the RETAINED italic footer, **no renumber and NO `---` separator** (the file's last `^---$` is still at `:17020`). Block shape controller-re-derived at this close: **1** `### Context` / **1** `### Decision` / **1** `### Consequences` / **1** retained footer. ⚠️ **FOUR of its OWN §Context paragraphs were CORRECTED IN PLACE** (¶1's `:295` -> `:307`, ¶5's `-count ~= 81` -> **83**, ¶7's *"~71 collapses"* -> **74/2000**, ¶11's *"needs no edit"* -> REFUTED) on the ADR-0297 ¶7/¶9 precedent, each **leading with what survives**; none changes a decision the ADR records. ⚠️ **It carries NO whole-file grep count and claims NO family ordinal** — the Load-balancing family closed at row 54, and **maintenance rows do not extend a charter**. *(Historical, retained: **ADR-0297 is COMPLETE** — §Context drafted at the phase-75 SPEC (ADR-0044-**as-used**; ⚠️ ADR-0044 does NOT actually contain that discipline), §Decision + §Consequences appended IN PLACE at the phase-75 IMPL after the **RETAINED** footer, mirroring ADR-0295/0296. Block shape verified at this close: **1** `### Context` / **1** `### Decision` / **1** `### Consequences` / **1** retained footer; `grep -c '^## ADR-0298'` => **0**. **No renumber, no new ADR.** ⚠️ **Its OWN §Context ¶7 and ¶9 were CORRECTED IN PLACE while completing it** — ¶7's **self-falsifying whole-file grep** (the same species the phase-74 IMPL had just fixed in ADR-0296 ¶3, reproduced one ADR later) is restated with **NO number** and the searched token deliberately **NOT spelled**, because every restatement mints another counter-example; and ¶9's *"SELF vs OTHER-ADR"* form rule is replaced by **graft SCALE** after the n=7 population was re-enumerated first-hand. ⚠️ **¶9 ALSO said *"the only two instances in the file"* — which this row's own blockquote makes three** — so a pure append would have shipped a SECOND self-falsifying count in the commit that fixed the first. **The prescription has now outlived THREE wrong justifications: family, self-vs-other, scale.** ⚠️ **`ADR-0296` is NO LONGER byte-untouched** — its §Decision (g) carries the phase-75 correction as an INDENTED blockquote (`grep -c '^  > '` **2 -> 3**), **LEADING WITH WHAT SURVIVES** (no such counter was owed *in phase 74*, whose sole stat-asserting fixture is `require=true` on both sides — **the deferral was RIGHT**) and claiming **NO ORDINAL**.)*
- **go.mod modules:** **Phase 76 landed +0** — `go mod tidy -diff` EMPTY and `git diff master -- go.mod go.sum` EMPTY; ⚠️ **the row's TEST-side import delta is +8 and that is CORRECT** (`ringhash_test.go` gains `fmt`/`math`/`mathrand "math/rand/v2"`, `linkage_test.go` adds `go/ast`/`go/parser`/`go/token`/`strconv`/`testing`) — all stdlib, all test-side, and **an import LINE is not a go.mod MODULE**; there is no new sub-package, so `reference_new_subpackage_pulls_transitive_module` does not bite. NC observed: a fake `require` ⇒ 67 -> 68. The tracked lineage figure is **2** (the phase-61.2 count: `quic-go` direct + `qpack` indirect) — **NOT a repo total**; the single `go.mod` requires **67** (18 direct + 49 indirect). **Phase 75 landed +0** — `go mod tidy -diff` EMPTY and `git diff master -- go.mod go.sum` EMPTY. The row needed `crypto/tls` and `internal/stats`, both ALREADY imported by `internal/listener/manager.go`; **an import LINE is not a go.mod MODULE**, and there is no new sub-package, so `reference_new_subpackage_pulls_transitive_module` does not bite. ⚠️ **TEST-side imports grew by exactly four** (`log`, `math`, `net/http`, `strconv` in `0110`'s driver) — all stdlib, all test-side, **not a production-envelope violation**.
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` (see `ENVOY_TARGET.md`).
- **conformance:** h2spec **`95 tests, 94 passed, 1 skipped, 0 failed`** (the skip is 6.9.2/2, invariant) — the CORRECTED 95-case strict scope under dotted selectors, measured at the phase-85 IMPL close 2026-08-10 (ADR-0307; the historical 53/53 records measured only 53 of the 95 under nine silently no-op slash-form selectors, 2026-04-25 -> 2026-08-10); proxy-wasm all 10 families PASS.

---

## Recent lineage

*(A SHORT recent lineage, capped at FIVE prior entries by ADR-0288. Everything earlier is in `STATE_HISTORY.md`. §Current carries the newest stage — **the phase-100 BRAINSTORM**, which added row 100. The oldest entry below has been archived to make room; ⚠️ **THE DATE READ DID NOT PICK IT ALONE** — the MEASURED pre-roll histogram, INCLUDING the entry this close promoted from §Current, was **FOUR `2026-09-21`, TWO `2026-09-20`**, a TWO-WIDE tie at the tail, broken by LIST POSITION (the projection held; it was measured anyway). ⚠️ **The post-roll list reads FOUR `09-21`, ONE `09-20` — so the NEXT close's pre-roll tail is projected to be a UNIQUE `09-20` that a date read picks alone. That is a PROJECTION, not a measurement: measure the histogram anyway, INCLUDING the entry your close promotes.** The eviction was verified with the LABEL-BOUND PAIR on BOTH files — the evictee's own label going `STATE.md` **1 -> 0** and `STATE_HISTORY.md` **0 -> 1** — plus a fabricated-label NC reading zero in BOTH and a positive control on a label that IS present in the archive and absent from §Recent; ⚠️ **the bare forms answer nothing, and the archive's positive controls are SELF-INCREMENTING: name the LABEL, never the COUNT.**)*

- **prior active-phase:** `phase 99 (chain-match-sni-longest-suffix) IMPL done` (2026-09-21) — lifecycle-state **3 -> DONE**. **ROW 99 FLIPPED `in-progress` -> `done`** at `ROADMAP.md:161` (a FLIP, not an ADD: `want` STAYS **131** and `ROADMAP.md` STAYS **249** lines). All **19** `PLAN.md` §5 tasks landed IN ORDER, and the order was load-bearing: Tasks 1-9 RECORDED the un-fixed tip before Task 10 touched production code — fixture `0124` RED on exactly its four `a.b.foo.test` rows (reference green), the five-selector roster at **421** `=== RUN` (= 398 + 23 new) with **18** new FAIL lines. **SNI TIES NOW BREAK ON THE PATTERN THAT MATCHED — its rank (exact > `*.` suffix > `*`), then the LONGEST matching suffix — on TCP AND QUIC, in either declaration order.** Production set: `listenerfilter/chainmatch.go` **`42	39`** (`sniMatchedRank` replaces `sniSpecificityRank`; slot 2 of `breakTie`) plus COMMENT-ONLY `internal/listener/quic.go` **`4	4`**; **ZERO new production packages**. Fixture **`0124-listener-sni-longest-suffix`** is GREEN on both sides. **ADR-0321 COMPLETED IN PLACE and `ACCEPTED`**; `BEHAVIOR_CONTRACT.md` gained a `+0, UNCHANGED` ledger entry quoting no absolute. ✅ **THE TOOLCHAIN FOLD-IN IS DISCHARGED**: the compressor assertion re-pointed to "the level is PLUMBED" (`compressor_test.go` **`53	49`**, test-only), and `GOTOOLCHAIN=go1.26.2 golangci-lint run ./...` RUNS, rc 0, with a COMPILING planted control that fired `errcheck`, `revive` and `ineffassign`. **EIGHT claims refuted by execution** (listed in `next-prompt.txt` and `99/PROGRESS.md`), including gate (b)'s denominator (**241, not 240** — `0124` adds a `driver` AND a `pki/gen` package).

- **prior active-phase:** `phase 99 (chain-match-sni-longest-suffix) PLAN done` (2026-09-21) — lifecycle-state **2 -> 3**. `PLAN.md` created (**2414** lines), a **19-task** TDD spine discharging all EIGHT owed items of `SPEC.md` §15. **ROW 99 STAYS `in-progress`** (`want` **131**, `ROADMAP.md` **249**). 🔴 **EVERY CODE BLOCK IN ITS APPENDICES WAS BUILT, RUN AND REVERTED AT THIS STAGE**: the P2 edit (`42 39` in `chainmatch.go`, the SPEC's `26 23` was a floor), the 19-row unit file, the QUIC arm, the compressor re-point, AND fixture `0124` in full (~941 lines) — run against the reference by digest: **RED at the un-fixed tip on exactly the four `a.b.foo.test` rows, GREEN under P2 three times, reference green in all six runs.** **NINE findings against the SPEC, five outright refutations:** §11 row 3 (P1 ALSO reddens `O1`), §11 row 4 (the naive drop-length mutant DOES NOT COMPILE — a masked control), `quic.go:181-183` is `:182-185`, §5.2's "17 RED" counts output LINES (14 unit rows + 2 QUIC subtests fail at the tip), and the inverted patch ALSO reddens fixture `l_mixed`. 🔴 **P2's slot-2 insert shifts two LIVE `quic_test.go` cites into `chainmatch.go` by +6** — fixed in the same commit (Task 10). **Split gate evaluated by command: 19 tasks, max 6 sub-steps, ≈1254 LoC — NOT SPLIT.**

- **prior active-phase:** `phase 99 (chain-match-sni-longest-suffix) SPEC done` (2026-09-21) — lifecycle-state **1 -> 2**. `SPEC.md` created, discharging all NINE owed items of `BRAINSTORM.md` §10. **ROW 99 STAYS `in-progress`** (a SPEC neither adds nor flips a row: `want` STAYS **131**, `ROADMAP.md` STAYS **249**). 🔴 **THE BRAINSTORM's `22 0` PROTOTYPE IS REFUTED AS THE REPAIR — MEASURED ON BOTH SIDES:** the reference (by digest `7edd5b0fd763…`) serves the chain whose **MATCHED** pattern is most specific — exact, then the LONGEST matching `*.` suffix — on **TCP AND QUIC**, both declaration orders; a mixed set `["x.test","*.foo.test"]` LOSES `a.b.foo.test` to `["*.b.foo.test"]` (the RANK axis) and `["*.foo.test","*.c.b.foo.test"]` loses it too (the LENGTH axis). The prototype kept whole-set rank and still serves the mixed chain. **The chosen shape ranks the MATCHED pattern: `26 23` in `chainmatch.go`, agreeing with the reference on every arm both sides ran.** QUIC is broken at the tip too (`H3 error (0x0)`) — owed item 2 DISCHARGED BY MEASUREMENT, not deferral. **`+0` stat names CONFIRMED.** ⚠️ **ONE DECLARED served→closed change**: two chains whose best MATCHED pattern is the same string (a class the reference REFUSES at validate, `multiple filter chains with overlapping matching rules are defined`). ⚠️ **NEW, BANKED: `SelectChain`'s pairwise fold is ORDER-DEPENDENT** — an early tie masks a strictly better later chain (an ALPN tie hides a `server_names` chain; subject-only). **ELEVEN claims refuted by execution**, including a premise of the controller's own brief (`manager.go:chainSpecificityRank` was deleted at phase 07.2) and the controller's own first draft of the NC roster.

- **prior active-phase:** `phase 99 (chain-match-sni-longest-suffix) BRAINSTORM done` (2026-09-21) — lifecycle-state **DONE -> 1**. **PHASE 99 OPENED BY SELF-PICK** under the 2026-07-12 standing directive, no human consulted. **ROW 99 ADDED `in-progress`** at `ROADMAP.md:161` (an ADD, not a flip: `want` **130 -> 131**, `ROADMAP.md` **248 -> 249**). **THE SUBJECT: SNI LONGEST-SUFFIX PRECEDENCE** — with two chains `*.b.foo.test` and `*.foo.test` and SNI `a.b.foo.test`, the reference serves the LONGEST suffix in BOTH declaration orders while envoy-go's `SelectChain` returns `ErrAmbiguousChainMatch` and the TCP path **CLOSES THE CONNECTION — even with a `default_filter_chain` configured**, on a config that passes `-mode validate`. **RE-MEASURED at this tip on BOTH sides, end to end by served body**, reversal pair + matched negatives; the banked framing HELD (method note 69) and four facts no document had were added. Repair floor `22 0` in `chainmatch.go`; fix, un-fix and INVERTED patch are all green at 398 `=== RUN` — the suite is BLIND to the slot. **FOURTEEN claims refuted by execution**, six against the router or this file — 🔴 **including that `golangci-lint` CANNOT RUN: it runs under `GOTOOLCHAIN=go1.26.2`, rc=0, zero findings, positive control fired three linters; and gate (b)'s compressor RED is the SAME toolchain move** (passes under go1.26.2). Both are owed to THIS row's IMPL as a FOLD-IN (`BRAINSTORM.md` §10 item 8), not chartered as a row.

- **prior active-phase:** `phase 98 (chain-match-transport-protocol-reject) IMPL done` (2026-09-20) — lifecycle-state **3 -> DONE**. **ROW 98 FLIPPED `in-progress` -> `done`** at `ROADMAP.md:160` (a FLIP, not an ADD: `want` STAYS **130** and `ROADMAP.md` STAYS **248** lines). All **20** `PLAN.md` §6 tasks landed IN ORDER, and the ordering was load-bearing: Tasks 1-10 ran at the UN-FIXED TIP, where exactly **THREE** arms were RED — the parse arm, the QUIC arm, and **(s1), which STAYED RED through the reject-lift and turned green only at the STAMP**, so the two production edits are separately attributable. **THE PARSE GATE IS LIFTED AND A TCP CONNECTION NO LISTENER FILTER CLASSIFIED IS NOW `raw_buffer`.** ⚠️ **"ONE PRODUCTION FILE" IS TRUE OF THE BEHAVIOUR AND FALSE OF THE FILE COUNT — THE PRODUCTION SET IS THREE FILES:** `internal/listener/manager.go` **`4	9`** carries the behaviour change, while `listenerfilter/chainmatch.go` **`3	3`** and `listenerfilter/types.go` **`3	3`** are COMMENT-ONLY and deliberately line-count-NEUTRAL; `internal/listener/quic.go` is **BYTE-UNTOUCHED**. Fixture **`0123-listener-transport-protocol`** was SCORED for the first time at any tip and **all three listeners answered as chartered on BOTH sides** (`l_bogus` -> DEFAULT, `l_raw` -> **INDEXED**, `l_tls` -> DEFAULT, every counter `present=true`). **ADR-0320 COMPLETED IN PLACE and ACCEPTED**; the house `PROPOSED` guard is **DISARMED**. **EIGHTEEN claims refuted by execution**, including `expectations.yaml:162-163`, which CONTRADICTED `PLAN.md` §8 on NC roster row 5b and was **itself the wrong document** (a constant `"raw_buffer"` that passes `l_tls`), and `PLAN.md`'s own Task 12 code block, **NOT INSTALLABLE AS PRINTED** against the `4	9` budget its own gate enforces.





## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
