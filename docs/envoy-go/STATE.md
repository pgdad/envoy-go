# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `16-http-filter-rbac` — phase 16 BRAINSTORM.md authored at this commit. Phase 16 is the NINTH §9 family-row (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11, csrf @ 12, buffer @ 13, compressor @ 14, bandwidth_limit @ 15) and the FIRST §9 row since phase 14 to introduce non-zero framework deltas — TWO new primitives anticipated: (i) TLS-principal accessor on `DecoderFilterCallbacks` required by `Principal_Authenticated`, anchored at ADR-0144; (ii) matcher-engine evaluator at new `internal/matcher/` package (or SPEC-determined location) implementing `xds.type.matcher.v3.Matcher` generic match-tree primitive, anchored at ADR-0142. MVP per BRAINSTORM Q1-Q7: 7 fields consumed proto-faithful (Q2 BOTH engines `rules` + `matcher` + `shadow_rules` + `shadow_matcher` + `rules_stat_prefix` + `shadow_rules_stat_prefix` + `track_per_rule_stats`); Permission MVP Large 11+11 (Q3); action enum ALLOW + DENY + LOG-partial (Q4); shadow + track_per_rule_stats proto-faithful (Q5); per-route NEW 7th canonical absent-implies-disabled per ADR-0125 §(xii) hypothesis (Q6); CEL `condition` silent-ignored (Q7); deny-path `SendLocalReply(403, "RBAC: access denied", {Content-Type: text/plain})` hypothesis; stat surface 60→65+ names (5 base counters minimum: `allowed`, `denied`, `logged`, `shadow_allowed`, `shadow_denied`; variable upward with `track_per_rule_stats: true` 2N per-policy counters); INDEPENDENT per-route stats hypothesis (mirrors phase-11 ADR-0117 + phase-15 ADR-0139); differential fixture `0018-http-rbac` (8 scenarios sketched at brainstorm including a TLS-mTLS scenario for `Principal_Authenticated`); 20th fuzzer `FuzzRBACConfigParse`. **ADR-0045 surface-split release valve HIGHLY anticipated** — LoC estimate ~2200-3000+ above the 1500-LoC trigger; recommended split is 16.1 (rules-engine + Large 11+11 + per-route 7th canonical + 4-counter aggregate + TLS-principal framework delta) + 16.2 (matcher-engine + shadow + LOG-partial + track_per_rule_stats + per-rule counters + matcher-engine framework primitive). SPEC author confirms split-or-single-row at SPEC drafting.
- **phase-directory:** `docs/envoy-go/phases/16-http-filter-rbac/` — BRAINSTORM.md authored (683 LoC; 7 Q-dialogue decisions settled: Q1 family-child = rbac; Q2 engine = BOTH proto-faithful; Q3 Permission/Principal = Large 11+11; Q4 action = ALLOW+DENY+LOG-partial; Q5 shadow+track = both proto-faithful; Q6 per-route = NEW 7th canonical absent-implies-disabled; Q7 CEL = silent-ignored). SPEC.md / PLAN.md / PROGRESS.md / REVIEW.md NOT yet authored — those are subsequent lifecycle-state transitions (state-2 → 3 → 4 → 5) per ADR-0005 §Decision 4 session-split discipline.
- **lifecycle-state:** `phase 16 BRAINSTORM.md authored; SPEC.md pending` — state-1 exit. Next session authors SPEC.md per the phase 09/10/11/12/13/14/15 precedent. BRAINSTORM.md §9 enumerates 18 empirical-pin candidates (P1–P18) the SPEC author resolves IN-SESSION against reference Envoy v1.37.2 per ADR-0004 (CRITICAL pins: P1 RBACPerRoute proto shape; P3 matcher-engine terminal-action TypeURL set; P4 LOG-action exact behavior; P5 exact 403 wire shape; P9 per-route SHARED-vs-INDEPENDENT stats; P14 Principal_Authenticated full algorithm). BRAINSTORM.md §11 carries 10 open structural questions for the SPEC author (split decision, matcher-engine location, TLS-principal accessor API shape, INDEPENDENT-vs-SHARED posture, §12 amendment-cycle posture, filter-chain ordering, deprecated-field disposition, LOG counter semantics, track_per_rule_stats N-cap, matcher-engine terminal-action type set).
- **next-skill:** `superpowers:writing-plans` per BOOTSTRAP_PROMPT.md §5 lifecycle-state-2 entry condition (SPEC.md does not exist — phase 16 enters this state at this commit; the routing convention from phases 09/10/11/12/13/14/15 is that `superpowers:writing-plans` is called once for SPEC drafting, then again for PLAN drafting based on the SPEC). The SPEC author resolves the 18 §9 empirical-pin obligations IN-SESSION against reference Envoy v1.37.2 per ADR-0004.
- **next-skill-scope:** Cold-start scope for the phase 16 SPEC-authoring session. Read first: `docs/envoy-go/phases/16-http-filter-rbac/BRAINSTORM.md` (683 LoC; §§1–11; the authoritative design doc — every load-bearing decision lives there per D-3.4; §1.4 ADR-0045 split-by-engine-and-observability release-valve at 16.1+16.2 — HIGHLY anticipated; §9 eighteen empirical pins (P1–P18) to resolve IN-SESSION against Envoy v1.37.2 per ADR-0004; §11 ten structural carry-forwards); `docs/envoy-go/phases/15-http-filter-bandwidth-limit/SPEC.md` §1.1 amendment-block channel (load-bearing precedent for empirical-pin-driven SPEC-time amendments — phase 15 landed TEN amendments; phase 16 likely lands 5-15 analogously given the larger surface); `docs/envoy-go/phases/14-http-filter-compressor/SPEC.md` §1.1 amendment-block channel (phase 14 landed SIX amendments; secondary precedent); `docs/envoy-go/phases/13-http-filter-buffer/BRAINSTORM.md` §12 amendment cycle (release-valve precedent if a pin resolution reframes a brainstorm decision); `docs/envoy-go/DECISIONS.md` ADR-0125 (5+6 canonical per-route patterns + in-place-amendment precedent — phase 16 SPEC adds §(xii) paragraph as the 7th canonical entry), ADR-0117 + ADR-0139 (INDEPENDENT per-route stats precedent — phase 16 hypothesizes; §9.P9 confirms or amends), ADR-0128 + ADR-0131 (framework primitives reuse precedent — phase 16 ADDS TWO new primitives ADR-0142 + ADR-0144 mirroring this discipline), ADR-0118 (SN9 + SN-rule introduction precedent; phase-16 may introduce SN10 if §9.P7 demands), ADR-0106 (flat top-level rows + no-sibling-stub), ADR-0044 (SPEC-time ADR anticipation); `docs/envoy-go/BEHAVIOR_CONTRACT.md` `### Phase 15 forward-pointer notes` subsection for the framework-primitives precedent context. Highest-priority pins for SPEC-time scrape: §9.P1 (RBACPerRoute proto shape — determines ADR-0125 §(xii) amendment wording), §9.P3 (matcher-engine terminal-action TypeURL set — determines ADR-0142 supported-set + §8.8 deferral scope), §9.P4 (action: LOG exact behavior — determines `logged` counter semantics + dual-engine counter-emission per ADR-0145), §9.P5 (exact 403 wire shape — determines §4 SendLocalReply byte-discipline), §9.P9 (per-route SHARED-vs-INDEPENDENT — determines ADR-0145 framing), §9.P14 (Principal_Authenticated full algorithm — determines ADR-0144 accessor API surface).
- **last-commit:** `38749ba` — `Squash merge phase-16-http-filter-rbac-brainstorm`. Squashed 1 worktree commit (5b4deac `phase 16 brainstorm: http-filter-rbac [planned + BRAINSTORM authored + ROADMAP row 16 + STATE.md flip]`). Lands the phase-16 brainstorm artefacts (BRAINSTORM.md 683 LoC + STATE.md flip + ROADMAP row 16 added). Predecessor on master: `07f8d44` `chore: gitignore up.txt` (small unrelated chore committed before the squash-merge per user direction). Phase-16 SPEC author begins from this master tip in a new SPEC-authoring worktree per ADR-0005 §Decision 4 separate-sessions discipline.
- **last-updated:** 2026-05-12
- **next-free ADR:** `ADR-0140` (unchanged from phase 15 STATE.md SHA-fill; phase-16 BRAINSTORM does NOT author ADRs — ADR authoring is SPEC-time per ADR-0044). The phase-16 SPEC author anticipates ADR-0140..ADR-0146 (7 ADRs — LARGEST §9-row roster to date) plus an ADR-0125 in-place amendment paragraph §(xii) per BRAINSTORM §7.

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
