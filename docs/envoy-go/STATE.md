# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `18-http-filter-ext-authz` — phase 18 BRAINSTORM session COMPLETE (state-1 entry per ADR-0005 §Decision 4). Phase 18 is the ELEVENTH §9 HTTP-filters family-row (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11, csrf @ 12, buffer @ 13, compressor @ 14, bandwidth_limit @ 15, rbac @ 16, jwt_authn @ 17). The brainstorm session selected `ext_authz` from the 8-candidate remaining §9 roster and settled the MVP envelope via a 6-question live dialogue: **Both service modes proto-faithful** (`grpc_service` + `http_service`); **request body included** proto-faithful (`with_request_body` reusing the phase-13 ADR-0128 decode-side body-buffering primitive); **Full gRPC AttributeContext population** (source/destination Peers incl. `principal` via the phase-16 ADR-0144 `DownstreamPrincipal()` reuse; full `request.http`; `tls_session.sni`; `context_extensions`); **Full header-side mutation** (request-side filtering + allow-path upstream injection + deny-path downstream copying); **per-route 5th-canonical REUSE** (`ExtAuthzPerRoute{oneof{disabled(bool) | check_settings}}` maps onto the existing buffer/compressor disabled-OR-narrower-override sum-type; SHARED stats — FIRST §9 row to REUSE an ADR-0125 canonical rather than add one; NO ADR-0125 amendment paragraph). Phase 18 anticipates **TWO new framework primitives** (THIRD consecutive two-primitive §9 row): a gRPC-client outbound primitive at NEW `internal/grpcclient/` (envoy-go's FIRST gRPC infrastructure of any kind; ADR-0158) + an HTTP-outbound auth-check composing against the phase-17 `internal/jwks/Fetcher` outbound-HTTP structure (ADR-0159); plus TWO framework REUSES (ADR-0128 body-buffering + ADR-0144 `DownstreamPrincipal()`). **8 anticipated ADRs** (ADR-0156..ADR-0163; NO ADR-0125 amendment). **ADR-0045 split-readiness flagged HIGH** — the FIRST §9 row to flag HIGH rather than MODERATE (the two-transport MVP envelope is genuinely structurally bifurcated; the structurally clean cut is by service-mode — 18.1 = filter scaffold + HTTP mode, 18.2 = gRPC mode + the gRPC-client primitive); the SPEC author makes the single-row-vs-split call. BRAINSTORM.md enumerates 13 §10 empirical-pin obligations for the SPEC session to resolve IN-SESSION against reference Envoy v1.37.2 per ADR-0004. ROADMAP row 18 added with status `planned`.
- **phase-directory:** `docs/envoy-go/phases/18-http-filter-ext-authz/` — BRAINSTORM.md landed (this session); SPEC.md + PLAN.md + PROGRESS.md + REVIEW.md pending.
- **lifecycle-state:** `phase 18 BRAINSTORM.md authored; SPEC pending` — state-1 entry per ADR-0005 §Decision 4. The phase-18 BRAINSTORM session selected the §9 family-row WITH the user (Q1 = `ext_authz`), explored the design space via the Q2–Q6 dialogue, authored `docs/envoy-go/phases/18-http-filter-ext-authz/BRAINSTORM.md`, added ROADMAP row 18 (`planned`), and advanced this STATE.md. The next session is a SPEC session (`superpowers:brainstorming` SCOPED to SPEC authoring per the phase-09..17 cadence; ADR-0005 §Decision 4 — BRAINSTORM / SPEC / PLAN / IMPL / REVIEW are SEPARATE sessions) authoring `docs/envoy-go/phases/18-http-filter-ext-authz/SPEC.md`.
- **next-skill:** `superpowers:brainstorming` (SCOPED to SPEC authoring) — the next session authors `docs/envoy-go/phases/18-http-filter-ext-authz/SPEC.md` based on the phase-18 BRAINSTORM.md, and is responsible for resolving the 13 §10 empirical pins IN-SESSION against reference Envoy v1.37.2 per ADR-0004. Per ADR-0005 §Decision 4 this is a SEPARATE session from BRAINSTORM; it does NOT author PLAN.md (that is the PLAN session, next-skill `superpowers:writing-plans`).
- **next-skill-scope:** Cold-start scope for the next (phase-18 SPEC) session. Read first: STATE.md (this file); `docs/envoy-go/phases/18-http-filter-ext-authz/BRAINSTORM.md` (the phase-18 brainstorm — 6 design decisions + §3 framework-survey + §4 per-route 5th-canonical-reuse + §5 stat surface + §6 fixture envelope + §7 8-ADR roster + §8 deferrals + §10 the 13 empirical-pin obligations the SPEC must resolve + §11 lessons applied); `docs/envoy-go/ROADMAP.md` (row 18 `planned`); `docs/envoy-go/MISSION.md` + `docs/envoy-go/DECISIONS.md` (tail at ADR-0155; next-free ADR-0156; ADR-0125 8-canonical roster — phase 18 REUSES the 5th; ADR-0045 split-by-surface release valve; ADR-0044 ADR-on-impl convention; ADR-0128 decode-side body-buffering; ADR-0144 `DownstreamPrincipal()`; ADR-0150 `internal/jwks/Fetcher`) + `docs/envoy-go/BEHAVIOR_CONTRACT.md` (10 HTTP filters documented through phase 17; HCM stat-table at 71 names; the framework-primitive sections `## Matcher engine framework primitive` + `## JWKS framework primitive` + `## JWT verifier framework primitive` + the `## HTTPFilterCallbacks` umbrella) + `docs/envoy-go/SKILL_ROUTING.md` + `docs/envoy-go/ENVOY_TARGET.md` (reference Envoy v1.37.2 pin); a recent §9 SPEC.md for shape precedent (`docs/envoy-go/phases/17-http-filter-jwt-authn/SPEC.md` or `16-http-filter-rbac/SPEC.md`). Reusable framework surface available to ext_authz: `internal/jwks/Fetcher` outbound-HTTP `http.Client`/timeout/retry structure (compose for HTTP-mode auth-check), `DecoderFilterCallbacks.DownstreamPrincipal()` (compose for gRPC `AttributeContext.source.principal`), the phase-13 ADR-0128 decode-side body-buffering primitive (compose for `with_request_body`), the phase-09 async-resume primitive (`time.AfterFunc` + `cb.ContinueDecoding`; compose for the async outbound call leg). NOTE: envoy-go has ZERO gRPC infrastructure — the `grpc_service` mode requires a brand-new gRPC-client primitive built from scratch (`google.golang.org/grpc` dial + the `envoy.service.auth.v3.Authorization/Check` stub).
- **last-commit:** `TBD-brainstorm-squash-merge` — the phase-18 BRAINSTORM worktree `phase-18-http-filter-ext-authz-brainstorm` (branched off master tip `d56d51a`) is squash-merged to master as one BRAINSTORM commit at session end per the phase-09..17 brainstorm-stage precedent (`git merge --squash`). This field is SHA-filled by the post-squash follow-up commit on master (TBD-brainstorm-squash-merge → `<sha>` post-squash) per the phase-09..17 brainstorm-stage close pattern. Predecessor master tip: `d56d51a` (`phase 17 impl follow-up: STATE.md SHA-fill (TBD → 3cee1d3 post-squash)`).
- **last-updated:** 2026-05-14
- **next-free ADR:** `ADR-0156` — UNCHANGED by the phase-18 BRAINSTORM session (brainstorm sessions anticipate ADRs but do not anchor them — anchoring happens at SPEC §Context-draft + IMPL §Decision/§Consequences time per ADR-0044). Phase 18 anticipates 8 ADRs ADR-0156..ADR-0163 (§7 of the phase-18 BRAINSTORM.md) + NO ADR-0125 amendment paragraph (the per-route shape REUSES the existing 5th canonical — the FIRST §9 row since phase 13 to not extend the ADR-0125 roster). ADR tail remains at `ADR-0155`; next-free remains `ADR-0156`.

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
