# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `06.1-stats-prometheus`
- **phase-directory:** `docs/envoy-go/phases/06.1-stats-prometheus/` — contains `SPEC.md` (at `ad46930`, frozen), `PLAN.md` (at `343b198`), and `PROGRESS.md` (Task 15 closing entry at `58d4ec9`, the lifecycle-state 4 verifier FAIL block at `1f94b74`, the lifecycle-state 3 → 4 fix block at `79be6b0`, plus the lifecycle-state 4 → 5 re-verification PASSED block written by THIS commit). Parent `docs/envoy-go/phases/06-observability-baseline/` carries `BRAINSTORM.md` (`d271fd7`) + parent `SPEC.md` (`ad46930`). Sibling `docs/envoy-go/phases/06.2-access-log/` retains the placeholder `README.md` (`ad46930`). Phase 05 closed history unchanged.
- **lifecycle-state:** `5` — all six BOOTSTRAP §7.5 gates re-verified GREEN from a fresh-session worktree per the 05.2 verifier role contract; awaiting code review. The verify branch `phase/06.1-stats-prometheus-verify-2` (worktree `.worktrees/phase-06.1-stats-prometheus-verify-2`, branched from gate-(d) fix-branch tip `4982713` per ADR-0003) re-ran every gate at the documented thresholds and recorded verbatim outputs in PROGRESS.md's "Verification (lifecycle-state 4 — re-run after gate-(d) fix) — PASSED" block: (a) all 6 differential fixtures green (0000-tcp-echo, 0001-tcp-proxy-rr, 0002-tls-tcp, 0003-http11-routing, 0004-h2-routing, 0005-prometheus-stats; 8.82 s wall, reference-Envoy SHA `c5e8a68e…f18bd` matches `ENVOY_TARGET.md` pin), (b) implicit in (a) — 5 pre-existing fixtures (0000-0004) still green; no regression introduced by 06.1 stats-Registry threading or by ADR-0065's HCM boundary guard, (c) h2spec 53/53 PASS at the ADR-0051 pinned summerwind SHA `5f4a65c30cae8569558ced048b4bfe0dcf01a221e36767ae504ccd8348a7aeb0`, (d) all 7 fuzz targets PASS at the ADR-0018 30 s budget — `FuzzBootstrapLoad` (327 K execs, 2 new), `FuzzPromTextFormat` (26.2 M execs, 0 new), `FuzzTcpProxyFilter` (4.0 M execs, 1 new), `FuzzHCMConfigParse` (3.24 M execs, 1 new — the prior crasher target now CLEARED after ADR-0065 fix; 0 crashers), `FuzzFrameStream` (13.9 M execs, 5 new), `FuzzHPACKDecode` (1.9 M execs, 0 new), `FuzzTLSContextParse` (4.0 M execs, 11 new); 53.6 M total execs, 20 new-interesting, 0 crashers across 7 targets; `git status --porcelain` empty after each fuzz run (no auto-persisted seeds), (e) part 1 `go vet ./...` + `golangci-lint run ./...` + `go build ./...` all clean (zero output, exit 0); part 2 `go test -race ./...` clean across 18 ok test packages (`cmd/envoy-go`, `admin`, `bootstrap`, `cluster`, `filter/hcm`, `filter/hcm/h2`, `filter/tcpproxy`, `listener`, `stats`, `tls`, `conformance/h2spec`, `differential`, 5 fixture drivers, `helpers`), 0 FAIL, 0 DATA RACE warnings, (f) REVIEW.md is the lifecycle-state-6 deliverable (deferred). Per the 05.2 verifier role contract (`b34bd99`) and the prior failing verifier (`1f94b74`), this verifier commit changes ONLY `STATE.md` + `PROGRESS.md` — no production code, test, or fixture changes; no testdata/fuzz/ corpus changes; the inherited seed file `internal/stats/testdata/fuzz/FuzzPromTextFormat/1d8483e640bf8347` (committed earlier in the impl chain — verified tracked) is unchanged. Cluster-name latent vulnerability noted in `79be6b0`'s fix block remains queued for a separate follow-up branch (NOT verifier scope). L4 review-followup's 26 Minors from impl Tasks 8–14 remain queued for the eventual REVIEW phase. Per BOOTSTRAP §5 step 4 → step 5, the next session is the formal lifecycle-state-5 code review.
- **next-skill:** `superpowers:requesting-code-review` — produce `docs/envoy-go/phases/06.1-stats-prometheus/REVIEW.md` covering the full 06.1 surface area: 15 PLAN tasks across the impl branch (`04bf75d` → `58d4ec9`), the L4 verifier session's failure block (`1f94b74`), the gate-(d) fix branch's ADR-0065 work (`79be6b0` + SHA-fill `4982713`), and this re-verification block. Inputs: `docs/envoy-go/phases/06.1-stats-prometheus/SPEC.md` (frozen at `ad46930`), `PLAN.md` (frozen at `343b198`), `PROGRESS.md` (the full Task-1-through-Task-15 history + the L4 FAIL block + the gate-(d) fix block + the L4 PASSED re-run block landed by THIS commit), `docs/envoy-go/DECISIONS.md` ADR-0059..0065 (06.1's 7 ADRs in commit-add order: 0059, 0060, 0061, 0064, 0063, 0062, 0065 — non-monotonic per 05.2 precedent), `docs/envoy-go/BEHAVIOR_CONTRACT.md` (06.1 in-place edits at `58d4ec9` for stat-name mapping rules SN1-SN8 + the empirically-pinned Rule SN4 from `ad46930`), the production surface added by 06.1 (`internal/stats/registry.go`, `internal/stats/promtext.go`, `internal/admin/admin.go`'s `/stats/prometheus` endpoint + `server.live` gauge, `internal/cluster/cluster.go` + `internal/cluster/manager.go` Registry threading + 8 cluster metrics, `internal/listener/manager.go` Registry threading + 2 listener metrics, `internal/filter/hcm/config.go` + `internal/filter/hcm/connection.go` + `internal/filter/hcm/h2dispatch.go` HCM Inc wiring with the ADR-0065 boundary guard at `parseFilterWithCtx`, `internal/bootstrap` Bootstrap.Stats field + Load allocator), the new fixture `test/fixtures/0005-prometheus-stats/`, the new fuzz target `internal/stats.FuzzPromTextFormat`, and the new fuzz seed `FuzzPromTextFormat/1d8483e640bf8347`.
- **next-skill-scope:** Lifecycle-state 5 → 6 code-review session in a fresh worktree (`.worktrees/phase-06.1-stats-prometheus-review`, branched from THIS commit's HEAD per ADR-0003 + per-phase-worktree convention). The reviewer MUST: (1) produce `docs/envoy-go/phases/06.1-stats-prometheus/REVIEW.md` per the 05.1/05.2 review-format precedent (Important findings → blocking; Minor findings → carry-forward to L4-review-followup batch); (2) cross-check SPEC §4 acceptance bullets vs implementation evidence in PROGRESS.md; (3) cross-check ADR-0059..0065 internal consistency + each ADR's "Lands-in-task" pointer vs commit history; (4) cross-check BEHAVIOR_CONTRACT.md edits against the empirical-pin evidence in `ad46930`'s SPEC + the fixture-0005 expectations.yaml; (5) cross-check BOOTSTRAP §3 (D-3.x doctrine) compliance; (6) integrate the L4 review-followup's 26 Minors triage queue (Task 8 5 / Task 9 5 / Task 10 5 / Task 11 4 / Task 12 2 / Task 13 5 / Task 14 4 — listed in the impl Task 15 closing block) and the cluster-name carry-forward; (7) NOT touch the production surface — review is doc-only output. On reviewer approval (no Importants), STATE transitions to lifecycle-state 6 with `next-skill` set to whatever lands at the phase-done commit (`commit (message: "phase 06.1: <title> [ADR-NNNN,...]")` per BOOTSTRAP §5.3, ROADMAP rows 06.1 + 06 → done together if 06.1 completes the parent's deliverable set, or 06.1 → done while 06 stays `in-progress` until 06.2 lands). On reviewer Importants, STATE regresses to lifecycle-state 3 with `next-skill: superpowers:executing-plans` (resume implementation per BOOTSTRAP §5.2 — re-enter at step 3, not step 4).
- **last-commit:** `1ed6cd0` — `phase 06.1: lifecycle-state 4 verification — all six gates GREEN after gate-(d) fix; STATE → 5`, on branch `phase/06.1-stats-prometheus-verify-2`. The verifier session re-ran every BOOTSTRAP §7.5 gate from a fresh worktree per the 05.2 verifier role contract: gate (a) 6/6 fixtures, gate (b) implicit, gate (c) h2spec 53/53, gate (d) 7/7 fuzzers (53.6 M execs, 0 crashers; `FuzzHCMConfigParse` cleared after ADR-0065 fix), gate (e) part 1 vet/lint/build clean + part 2 race tests 18 ok / 0 FAIL / 0 DATA RACE, gate (f) deferred to L6. Verifier-commit surface change is two files (`STATE.md` + `PROGRESS.md`); production code, test, and fixture surfaces are untouched per role contract. ROADMAP unchanged (rows 06.1 + 06 stay `in-progress`; row 06.2 stays `planned`). SHA-fill follow-up per the phase-02..05.2 convention.
- **last-updated:** 2026-04-28

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
