# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `21-http-filter-adaptive-concurrency`. Phase 21 SPEC landed at this commit (15-section SPEC at `docs/envoy-go/phases/21-http-filter-adaptive-concurrency/SPEC.md`; ~839 LoC; structure mirrors phase-20 SPEC). **Single-row landing settled per ADR-0045** (NOT split into 21.1+21.2 — the natural split axes don't carve cleanly per BRAINSTORM §1.4 + SPEC §3.5; LoC envelope re-estimated post-empirical-scrape at ~1370-1830, slightly above BRAINSTORM's ~1200-1500 due to AMEND-2 algorithmic-invariant additions but well below the ADR-0045 split-gate). **FOURTEENTH §9 family-row** (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11, csrf @ 12, buffer @ 13, compressor @ 14, bandwidth_limit @ 15, rbac @ 16, jwt_authn @ 17, ext_authz @ 18 with its 18.1+18.2 split, ext_proc @ 19 with its 19.1+19.2 split, oauth2 @ 20). **LEANEST framework-delta §9 row to date** — ZERO new `internal/` primitives + ONE in-place §Decision AMENDMENT-anticipation paragraph on ADR-0059 per D1 resolution (the BRAINSTORM-anticipated ADR-0188 candidate COLLAPSES); 7 framework REUSES per SPEC §3.3 (stats Counter+Gauge / HTTPRegistry / per-request filter interface / HCM-parse-time PARSE-REJECT / REUSE-by-absence per-route enforcement (**FOURTH CONSECUTIVE §9 row to skip ADR-0125 amendment**) / fuzzer-corpus framework / differential-fixture framework). **5 §21.P empirical pins ALL CLOSED at SPEC time** via parallel-subagent fan-out against v1.37.2 reference Envoy (disposition matrix: 1 RATIFIED + 3 PARTIAL + 1 RESOLVED-to-AMENDMENT): §21.P1 RATIFIED (503-overflow cross-side promotion D2 — trap sound; CAS arrival-order; 503 body byte-exact `"reached concurrency limit"` 25 bytes); §21.P2 PARTIAL (algorithmic invariants D3 — gradient formula + new-limit RATIFIED; minRTT-aggregation + jitter + 5-consec-trigger + first-tick AMENDs); §21.P3 PARTIAL (stat surface D4 — 3 stat-name renames `*_msecs` + `min_rtt_calculation_active`; RTT in MILLISECONDS upstream / envoy-go-strict departure to nanoseconds; template simplified — no `<config_stat_prefix>` segment); §21.P4 PARTIAL (PARSE-REJECT + defaults + `enabled` semantics — proto field rename `concurrency_limit_calculation_params`→`concurrency_limit_params`; `enabled` defaults OFF when absent REFUTES BRAINSTORM §2.1; `min_rtt_calc_params.interval` not proto-required with `fixed_value` alternative; 5 additional PGV PARSE-REJECT arms; ConcurrencyControllerConfig oneof solo RATIFIED); §21.P5 RESOLVED-to-AMENDMENT (D1 float-gauge convention anchor → IN-PLACE §Decision AMENDMENT on ADR-0059, NOT new ADR-0188). **§1.1 AMEND-1..AMEND-7 amendment block cataloged** (AMEND-1 proto-schema corrections C1-C4: field rename + proto-file consolidation + interval validation + fixed_value PARSE-REJECT; AMEND-2 algorithmic-invariant corrections C1-C4: percentile-not-MIN + jitter-to-next-interval + 5-consec-trigger + first-tick; AMEND-3 stat-name+encoding+template corrections C1-C5: 3 renames + template simplification + RTT-ms-vs-ns envoy-go-strict + gradient ×1000 + Accumulate-import-mode deferral; AMEND-4 `enabled` empty-default OFF refutation; AMEND-5 PARSE-REJECT roster expansion 5 additional arms; AMEND-6 503-overflow cross-side byte-exact promotion RATIFIED; AMEND-7 D1 resolution — ADR-0188-candidate COLLAPSES). **2 NEW ADRs landed at SPEC commit** (§Context drafts per ADR-0044): ADR-0186 Gradient-1 controller state machine + inline Clock seam (NOT framework primitive) + FAKE-TIME differential strategy + sorted-slice percentile aggregation (NOT CircllHist) + line-cited algorithmic lemmata against `gradient_controller.cc` + `fixed_value` PARSE-REJECT; ADR-0187 RTDS `enabled.RuntimeFeatureFlag` deferral PARSE-REJECT + `enabled` empty-default OFF semantics per AMEND-4. **1 IN-PLACE §Decision AMENDMENT-anticipation paragraph at ADR-0059** per ADR-0044 (AMENDMENT body lands at IMPL Task 4 — float-valued gauge int64 encoding convention; ns for time-typed; ×1000 for ratio-typed; 0/1 for bool-typed; NEW `internal/stats/conv.go` `boolToInt` helper). **FOURTH CONSECUTIVE §9 row to skip ADR-0125 amendment** (REUSE-by-absence; no `AdaptiveConcurrencyPerRoute` proto message in v1.32.4 or v1.37.x). Stat surface 92 → 99 names per AMEND-3 (7-name roster; 1 counter `rq_blocked` + 6 gauges `concurrency_limit` / `gradient` / `burst_queue_size` / `sample_rtt_msecs` / `min_rtt_msecs` / `min_rtt_calculation_active` under the upstream-byte-exact `http.<HCM_stat_prefix>.adaptive_concurrency.gradient_controller.<stat>` prefix template). Differential fixture `0025-http-adaptive-concurrency` per SPEC §7: 4 scenario directories ((a) parse_ok REFERENCE-LESS + (b) overflow_503 **PARTIAL CROSS-SIDE BYTE-EXACT per AMEND-6** + (c) stat_surface REFERENCE-LESS + (d) pass_through_when_disabled REFERENCE-LESS) + single-listener topology + 27th fuzzer `FuzzAdaptiveConcurrencyConfigParse`. Six-gate checklist (A/B/C/D/E/F) per SPEC §7.4 + SPEC §14.5 + cross-package regression matrix per SPEC §12 item C8 (post-ADR-0059 AMENDMENT). SPEC §15 18-item acceptance checklist for the IMPL phase-done reviewer. BEHAVIOR_CONTRACT 7-edit bundle anticipated per SPEC §13 (NEW `### envoy.filters.http.adaptive_concurrency` subsection + stat-table 92→99 extension + ADR-0059 §Decision AMENDMENT cross-reference + 2 envoy-go-strict departure records (RTT-ms-vs-ns + sorted-slice-vs-CircllHist) + NEW `### Phase 21 forward-pointer notes` subsection + Per-route canonical patterns cross-reference table caption update). 8 RATIFIED-PENDING-IMPL-TIME items deferred to IMPL per SPEC §12 (4 wire-shape byte-confirmations + 3 library-behavioral + 1 cross-phase regression). ADR-0044 escape-valve in reserve for ~0-2 impl-time-unanticipated ADRs at IMPL. **D11-style hypothesis STRENGTHENED at SPEC commit**: ADR-0188 + ADR-0189 stay UNCONSUMED at phase-21 phase-done (next-free escape valve carried forward TWO slots to phase 22; the D1 resolution collapsed the BRAINSTORM-anticipated ADR-0188 candidate, so the escape-valve buffer is now 2 slots wide instead of 1; HOLD-with-known-risk per ADR-0044 escape-valve hypothesis). Next-free ADR post-SPEC commit: **ADR-0188** (only 2 NEW consumed: ADR-0186 + ADR-0187; ADR-0188-candidate collapsed per AMEND-7).
- **phase-directory:** `docs/envoy-go/phases/21-http-filter-adaptive-concurrency/` (BRAINSTORM.md authored at predecessor commit `cad1153` + SPEC.md authored at this commit; PLAN.md + PROGRESS.md + REVIEW.md to be authored at subsequent lifecycle-state transitions per BOOTSTRAP §5).
- **lifecycle-state:** `phase 21 SPEC done; awaiting PLAN`. ROADMAP row `21` is `in-progress` at this commit (sub-phases column empty per the SINGLE-ROW disposition settled at this SPEC commit; status will flip to `done` at the phase-21 IMPL phase-done commit per BOOTSTRAP §5 step 6). DECISIONS.md tail advanced to **ADR-0187** (2 NEW ADRs consumed at this SPEC commit per ADR-0044 §Context-draft discipline; §Decision + §Consequences full bodies anchor at per-Task IMPL lands: ADR-0186 at Task 3 + ADR-0187 at Task 2; ADR-0059 §Decision AMENDMENT body anchors at Task 4).
- **next-skill:** `superpowers:writing-plans` (scoped to PLAN.md authoring per the phase-09..20 precedent). The next session is the phase-21 PLAN-authoring session (lifecycle-state 2 → 3). Cold-start scope for the next session: read STATE.md (this file) + `docs/envoy-go/phases/21-http-filter-adaptive-concurrency/SPEC.md` (the SPEC authored at this commit; load-bearing for the 15-section authoritative input + the §11 §21.P pin disposition matrix + the §10 ADR anchor map + the §12 RATIFIED-PENDING-IMPL-TIME items + the §15 18-item acceptance checklist + the AMEND-1..AMEND-7 amendment block) + `docs/envoy-go/phases/21-http-filter-adaptive-concurrency/BRAINSTORM.md` (predecessor; 5 Q-decisions context) + `docs/envoy-go/phases/20-http-filter-oauth2/PLAN.md` (structural precedent; the most-recent §9 PLAN for 14-task envelope structure) + `docs/envoy-go/ROADMAP.md` row 21 (SPEC-done annotation commit) + project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`.
- **last-commit:** `49ba034` — `Squash merge phase-21-http-filter-adaptive-concurrency-spec`. Phase 21 SPEC landed via worktree `.worktrees/phase-21-http-filter-adaptive-concurrency-spec/` on branch `phase-21-http-filter-adaptive-concurrency-spec` per project memory `feedback_git_worktrees.md` (1 worktree commit `a4a5658` bundled into the squash: SPEC.md + ADR-0186/0187 §Context drafts + ADR-0059 §Decision AMENDMENT-anticipation + STATE.md re-advance + ROADMAP row 21 SPEC-done annotation). This SHA-fill follow-up commit per the phase-09..20 convention: `phase 21 SPEC follow-up: STATE.md SHA-fill (TBD → 49ba034 post-squash)`. Predecessor master tip was `68ea0c5` — `phase 21 BRAINSTORM follow-up: STATE.md SHA-fill (TBD → cad1153 post-squash)`.
- **last-updated:** 2026-05-18
- **next-free ADR:** `ADR-0188` — advanced from ADR-0186 (post-phase-20-IMPL) by 2 numbers consumed at this SPEC commit per ADR-0044 §Context-draft discipline (ADR-0186 controller state machine; ADR-0187 RTDS deferral PARSE-REJECT). The BRAINSTORM-anticipated ADR-0188 candidate (float-valued gauge encoding convention) COLLAPSED per D1 resolution → IN-PLACE §Decision AMENDMENT on ADR-0059 (NOT a new ADR; no number consumed). D11-style hypothesis STRENGTHENED at SPEC commit: ADR-0188 + ADR-0189 stay UNCONSUMED at phase-21 IMPL phase-done (HOLD-with-known-risk per ADR-0044 escape-valve hypothesis; 2-slot escape-valve buffer absorbs one impl-time-unanticipated ADR without breaking the next-phase escape-valve invariant).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
