# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `25 (parent; in-progress) — awaiting 25.1 SPEC` — **phase 25 parent SPEC DONE at 2026-05-24 (this commit)** authoring `docs/envoy-go/phases/25-http-filter-wasm/SPEC.md` for the FINAL §9 HTTP-filters-family row `wasm` (`envoy.extensions.filters.http.wasm.v3.Wasm`). The parent SPEC formalizes the 3-way split surface-mapping (§3) + executes the §10 D1-D9 empirical-pin obligations IN-SESSION per ADR-0004 via parallel-subagent fan-out against `envoyproxy/envoy@v1.37.2` C++ source + `go-control-plane@v1.32.4` proto bindings + `proxy-wasm/proxy-wasm-cpp-host@da3ce05d` + the proxy-wasm v0.2.1 spec README + `wazero/wazero@main` README + `proxy-wasm/proxy-wasm-rust-sdk@v0.2.4`. Generates 9 AMEND-block entries (§1.1) — 6 substantive REFUTATIONS + 2 CONFIRMS-with-refinement + 1 RATIFIES-with-extensions: **AMEND-A1** (wazero pin `v1.10.1` Go 1.23 floor; Apache-2.0 license correction — BRAINSTORM §2.3 "MIT-licensed" REFUTED; `wasm32-wasip1` target — BRAINSTORM §2.9 `wasm32-wasi` updated post-Rust-1.78-rename; proxy-wasm-rust-sdk `=0.2.4` pin); **AMEND-A2** (stat-roster STRUCTURAL REFUTATION — BRAINSTORM 5+2 flat `wasm.<vm_id>.<stat>` template wrong; actual upstream is TRI-GROUP `wasm.<runtime>.{created,active}` Group B + `wasm.<plugin_name>.{vm_reload*}` Group C + flat `wasm.remote_load_*` Group A; HCM-stats_prefix DROPPED; no `vm_id` discriminator); **AMEND-A3** (`WasmPerRoute` absence CONFIRMED — NO message in v1.32.4 binding OR v1.37.2 IDL; 5th-canonical REUSE-by-absence definitive; **ADR-0125 STAYS at 10 canonicals — NO §(xvi) amendment**; BRAINSTORM escape-valve to 11th canonical RETIRED); **AMEND-A4** (wazero-vs-V8 byte-exact CONFIRMS BRAINSTORM §2.7 with §4.5 D6 guardrails — wazero compiler-mode is WebAssembly Core 1.0+2.0 spectest-green; proxy-wasm ABI is wire-defined + host-owned; zero open wazero bugs on compiler-mode wasm-1.0 semantics); **AMEND-A5** (default-deny capability roster CONFIRMS BRAINSTORM §2.5 envoy-go-strict departure; ~80-key capability roster pinned; denial semantic `WasmResult::InternalFailure`=10 + integration error log + `hostcall_denied` counter); **AMEND-A6** (proxy-wasm v0.1.0+v0.2.0 PARSE-REJECT is envoy-go-strict-stricter departure NOT parity — upstream accepts ALL 3 ABI versions and version-dispatches; detection point: scan wasm export section for `proxy_abi_version_0_2_1` sentinel; BRAINSTORM "DEPRECATED upstream" framing REFUTED); **AMEND-A7** (WasmResult enum REFUTATION — 10 values with value-gaps at 5/9/11 NOT BRAINSTORM-hypothesized 13; `BadExpression`+`ResultMismatch`+`BrokenConnection` do NOT exist in v0.2.1; WasmBufferType value 8 is `FOREIGN_FUNCTION_ARGUMENTS` not `CallData`); **AMEND-A8** (conformance-suite source REFUTATION — `proxy-wasm/spec` ships NO test suite; source pinned to `proxy-wasm-cpp-host@da3ce05d` test/ with 10/16 test families = 62.5% starting pass-threshold; BRAINSTORM 100%-pass expectation REFINED); **AMEND-A9** (foreign-function disposition RATIFIES option (b) registration interface + EMPTY default registry + `WasmResult::NotFound`=1 for unregistered names — 0-vs-10-foreign-function envoy-go-strict departure at 25.2). Plus: 25.1 hostcall surface REFINED from BRAINSTORM ~17 to **24 hostcalls** (BRAINSTORM missed 7 mandatory WASI shims + `proxy_get_log_level` + `proxy_get_current_time_nanoseconds`); 25.1 callback surface REFINED to **13 callbacks** (BRAINSTORM 8 missed 5 mandatory module-init/allocator callbacks); 25.1 stat surface REFINED to **5 counters not 7** (114 → 119; not 114 → 121). **Per-sub-phase task/LoC envelope** (parent SPEC §3.0): 25.1 ~17-19 tasks ~3,650-5,360 LIVE LoC; 25.2 ~20-24 tasks ~2,730-4,250 LIVE LoC; 25.3 ~9-12 tasks ~1,300-2,350 LIVE LoC. Family-final ~46-55 tasks ~7,680-11,960 LIVE LoC (task-arm gate fits all 3 sub-phases under ADR-0045 ~25-task threshold; LoC-arm above ~1500 at 25.1 + 25.2 — acceptable per EXTRACT-NOW primitive-bring-up precedent from phase-22.1). **No further nested split needed** — task-arm gate fits all 3 sub-phases. Phase 25 parent SPEC anchors 3 §Context drafts at this commit (ADR-0202 NEW `internal/wasm/` framework primitive + EXPLICIT API-REVISION ALLOWANCE; ADR-0203 NEW `internal/filter/http/wasm/` package shape + 18-arm PARSE-REJECT roster + 5-counter envoy-go-strict stat surface; ADR-0204 default-deny capability sandbox); §Decision + §Consequences bodies land at 25.1 IMPL Lands-in-Task per ADR-0044. ADR-0202..ADR-0204 §Context bodies landed in DECISIONS.md at this commit. **STRENGTHENED-WEAK-HOLD with 1-slot-buffer** at 25.1 IMPL (vs BRAINSTORM 2-slot) — 6 SPEC-time-surface-risks closed at SPEC commit via AMEND-A1..A9; 1 residual buffer slot for wazero-VM-pool benchmark gate (D-P4 + R8). Phases 23 + 24 + 24.1 + 24.2 DONE and CANONICAL — DO NOT reopen.
- **phase-directory:** `docs/envoy-go/phases/25-http-filter-wasm/` (BRAINSTORM.md + **SPEC.md landed at this commit**; PLAN.md NOT yet authored — phase 25 is the parent row, no parent-row PLAN per the phase-22 + phase-24 parent-row precedent; per-sub-phase SPEC/PLAN sessions land at each sub-phase's own dedicated session). Sibling directories `docs/envoy-go/phases/25.1-…/` + `25.2-…/` + `25.3-…/` carry pre-created README.md forward-pointers per phase-22 precedent.
- **lifecycle-state:** `phase 25 parent SPEC done (lifecycle-state 1 → 2 for parent row); awaiting per-sub-phase 25.1 SPEC authoring` (SKILL_ROUTING state 1 for sub-phase 25.1 — parent SPEC.md exists; 25.1 sub-phase directory exists with README only; 25.1/SPEC.md does NOT yet exist). Per ADR-0106 + phase-22 precedent, the per-sub-phase 25.1 SPEC session is the next lifecycle-state-1 → state-2 transition for sub-row 25.1. ROADMAP row 25 STAYS `in-progress`; sub-row 25.1 STAYS `planned` until 25.1 SPEC session opens its directory + lands SPEC.md.
- **next-skill:** `superpowers:writing-plans` (scoped to **25.1 SPEC authoring** at `docs/envoy-go/phases/25.1-http-filter-wasm-runtime-and-headers-bridge/SPEC.md` per the phase 22.1 + phase 22.2 + phase 22.3 per-sub-phase SPEC precedent — the 25.1 SPEC inherits the parent SPEC's §6 PARSE-REJECT roster + §7 stat surface + §8 fixture-0034+0035 disposition + §11 empirical-pin block + §12 D-questions + §13 RATIFIED-PENDING-IMPL items + §14 BEHAVIOR_CONTRACT.md edit bundle; refines per-Task-level surface only). 25.1 BRAINSTORM may not be needed (the parent BRAINSTORM + parent SPEC settled enough decisions — Q1-Q9 locked, AMEND-A1..A9 anchored, §13 RATIFIED-PENDING items scoped, §15 24-item acceptance checklist drafted — that the 25.1 SPEC session may proceed directly to SPEC authoring per the next-prompt-permitted skip + phase-22.1 precedent). After 25.1 SPEC lands, PLAN authoring at 25.1 PLAN session; after PLAN lands, IMPL via `superpowers:subagent-driven-development` per `feedback_execution_style` project memory.
- **last-commit:** `2c1455d` — `Squash merge phase-25-http-filter-wasm-spec`. The phase-25 parent-SPEC squash-merge commit landing SPEC.md + 3 ADR §Context drafts in DECISIONS.md + this STATE.md update + ROADMAP per-cell SPEC-done annotation. SHA filled in at the stage-close follow-up commit per the phase-22 + phase-24 SPEC stage-close SHA-fill precedent. Predecessor master tip (pre-25-SPEC): `1f773cf` — `next-prompt.txt: repoint master-tip references to 29aa1db (actual HEAD)` (with predecessors `29aa1db` parent-SPEC-cold-start rewrite + `d7bc003` 25-BRAINSTORM SHA-fill + `c17cbf0` phase-25-BRAINSTORM squash-merge).
- **last-updated:** 2026-05-24
- **next-free ADR:** `ADR-0205` (CONSUMED at 25-parent-SPEC commit: ADR-0202 + ADR-0203 + ADR-0204 §Context drafts — bodies land at 25.1 IMPL Lands-in-Task per ADR-0044). DECISIONS.md tail at `ADR-0204` (3 NEW consumed: ADR-0202..ADR-0204). **D-hypothesis disposition (phase-25 parent SPEC)**: STRENGTHENED-WEAK-HOLD-with-1-slot-buffer — ADR-0205 reserve slot covers (a) wazero-VM-pool design escape-valve if 25.1 IMPL `BenchmarkPerStreamVM_Construction_Headers` exceeds 1ms threshold per parent SPEC D-P4 + R8 (LOW probability — phase-22.1 observed 70µs at the analogous benchmark) OR (b) any wazero/V8 sub-pin edge case the §4.5 D6 guardrails missed. WEAK-HOLD stands at this SPEC commit. Anticipated next-free after 25.3 phase-done: `ADR-0213` (matching BRAINSTORM §7.4 estimate of ADR-0202..ADR-0213 across the family).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
