# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `phase 29 BRAINSTORM done` (2026-06-02, this commit) — **phase 29 (`network-filter-mongo-proxy`) BRAINSTORM DONE at 2026-06-02** — the FOURTH §9 Network-filters-family row (a parent pre-split row per ADR-0106 + the phase-22/25/26/28 precedent — the project's FIFTH BRAINSTORM-time pre-split), brainstormed interactively via a 4-question dialogue: **Q0** subject = `mongo_proxy` chosen from the 4 remaining §9 candidates (the natural next per the ADR-0221 §Consequences anticipation — consumer #2 of the `network.WriteFilter` conn-wrap seam; a both-direction sniffer directly analogous to zookeeper_proxy; a core extension with no contrib-image risk; `redis`/`thrift` are large terminal-proxy surfaces; `kafka_broker` carries contrib-image risk); **Q1** scope = **FULL counter+gauge parity minus histograms** — both-direction op-code counters + per-command (`cmd.<cmd>.total`) + per-collection (`collection.<collection>.query.*`) counters via in-house BSON decode + the `op_query_active` GAUGE (the project's first differentially-mirrored gauge) + fault-delay injection + the mongo access log + dynamic metadata; the per-command/per-collection `reply_num_docs`/`reply_size`/`reply_time_ms` HISTOGRAM families stay DEFERRED per ADR-0060 (BEHAVIOR_CONTRACT coverage-boundary record); 3-way FEATURE-PROGRESSIVE pre-split; **Q2** the upstream-faithful **async halt/resume seam** — upstream mongo_proxy injects delay by returning `StopIteration` from `onData` while a delay timer pends and resumes via `read_callbacks_->continueReading()` on timer fire; **as-built correction discovered during the brainstorm:** `ReadFilterCallbacks.ContinueReading()` ALREADY EXISTS (26.1) but supports only OnNewConnection-halt resume + re-entrant in-OnData resume — the phase-29.3 seam is therefore an EXTENSION of existing machinery (OnData-halt persistence + cross-goroutine-safe `ContinueReading` + post-handoff read-halt honoring in `readChainConn`, lifting the 28.1b SPEC §3.5 observational boundary for halt purposes only; anticipated zero new exported API; `TerminalFilter.Handle` UNCHANGED); this is the ADR-0221 §Consequences "mongo fault-delay is the anticipated first halt consumer" closure pickup, REFINED — the halt is READ-side per upstream `onData` semantics, not the anticipated write-side; **Q3** hermetic synthesized-byte differential fixtures (NO real mongod — driver handshake/heartbeat traffic would break exact counter parity) + cross-side **`StatsAsserter`** counter+gauge parity as the LOAD-BEARING proof + deliberate-break liveness per fixture (`-count=1`) + DETERMINISTIC fault-delay arms (100%-probability fixed delay → `delay_injected` parity; timing never compared). `mongo_proxy` (`envoy.filters.network.mongo_proxy`, type `…filters.network.mongo_proxy.v3.MongoProxy` — bindings verified present in go-control-plane v1.32.4: `stat_prefix`/`access_log`/`delay`/`emit_dynamic_metadata`/`commands`) is a passive observability sniffer decoding the MongoDB legacy wire protocol (LITTLE-endian MsgHeader + BSON — in contrast to zookeeper's big-endian jute) in BOTH directions. **Sub-phase mapping:** **29.1** (`network-filter-mongo-wire-and-requests`) = the in-house BSON parser (package-internal `bson.go`; extract-at-second-consumer) + the wire decoder request side (OP_QUERY/OP_INSERT/OP_GET_MORE/OP_KILL_CURSORS) + request-side op/flag/shape counters + the dynamic `cmd.*`/`collection.*` families + the per-connection active-query list + config parse (5-field proto; `stat_prefix` PGV-required → boot-reject) + the 8th `builtins.RegisterBuiltins` built-in + bootstrap blank-import + the `.mongo.` Prometheus inline-prefix arm + fixtures `0049-mongo-requests`/`0050-mongo-boot-reject` + the 39th fuzzer (ADR-0224); **29.2** (`network-filter-mongo-responses-and-correlation`) = the OP_REPLY decoder in `OnWrite` + reply-flag counters + requestID↔responseTo correlation + the `op_query_active` gauge + `cx_destroy_local/remote_with_active_rq` + dynamic-metadata emission (ADR-0217 Bucket third production write) + fixture `0051-mongo-responses` (+ the 40th fuzzer per SPEC D29-12) (ADR-0225); **29.3** (`network-filter-mongo-fault-delay-and-access-log`) = the async halt/resume seam extension + fault-delay injection (FaultDelay proto; the phase-09 HTTP fault parse/eval precedent) + the mongo access log (reuses `internal/accesslog`) + `cx_drain_close` + fixture `0052-mongo-fault-delay` + the parent-row-29 ROLLUP + six-gate (ADR-0226). **Anticipated counts at family-row-done:** fixtures 50 → 54(-55); stat surface 337 → ~360–380 (SPEC pins the mirrored roster; histograms + runtime-key gating recorded as envoy-go-strict departures); fuzzers 38 → 39–40; ADRs ADR-0224/0225/0226 (next-free after ≈ ADR-0227). ADR-0045 split-gate: ~1650–2500 net-new LoC / ~36–48 tasks as one phase → the 3-way pre-split; each sub-phase re-checks at its PLAN. After phase-29 phase-done, **3** §9 candidates remain (redis/kafka_broker/thrift). BRAINSTORM.md authored at `docs/envoy-go/phases/29-network-filter-mongo-proxy/BRAINSTORM.md`; ROADMAP parent row 29 registered `in-progress` + sub-rows 29.1/29.2/29.3 `planned` per ADR-0106. Next: **29 parent SPEC** (`superpowers:writing-plans` scoped to parent-SPEC authoring per the 22/24/25/26/28 parent-row precedent; executes the §10 D29-1..D29-12 empirical pins IN-SESSION vs Envoy v1.37.2 per ADR-0004 — **D29-1 [the standard ref image ships mongo_proxy] is SPEC-BLOCKING for the Q3 strategy; D29-4 [does upstream v1.37.2 decode OP_MSG] is the largest decode-envelope unknown**; anchors the ADR-0224/0225/0226 §Context drafts). **Earlier (phase 28, preserved):** **Phase 28 (`network-filter-zookeeper-proxy`) is COMPLETE** (28.1a + 28.1b + 28.2 all done; parent row rolled at 2026-06-02). Sub-phase 28.2 landed: the unified per-connection decoder (`requestDecoder` → `decoder` rename + write-side `writeBuf` reassembly), the response dispatch table (5-row: connect/watch/control/data/unknown), the per-connection `sync.Mutex` (ADR-0221 forward-pointer DISCHARGED — ADR-0223 §Decision item 4; empirically proven load-bearing by the -race test), latency-threshold counters (`recordLatency`; `<=` INCLUSIVE; wire-opcode-keyed overrides; injectable-duration boundary tests), `OnWrite` glue (replaces the 28.1 no-op), the 38th fuzzer (`FuzzZookeeperResponseDecode` — 1.8M execs/30s clean), `TCPZKResponder BackendKind=29` (fixed 10ms delay; D-S28.2-2 triggers; D-S28.2-1 corrected 28-byte watch frame), fixture `0048-zookeeper-responses` (4 listeners / 8 arms / GREEN cross-side on the FIRST run; R4 both breaks recorded), ADR-0223 §Decision/§Consequences body + BEHAVIOR_CONTRACT 28.2 bundle. Phase 28 as a whole delivered: the `network.WriteFilter` seam (ADR-0221) + complete both-direction zookeeper_proxy counter parity (ADR-0222/0223) across 3 sub-phases (28.1a write-seam+request, 28.1b read-seam+cross-side-proof, 28.2 response+latency); six gates GREEN LIVE (50/50 differential; h2spec 53/53; proxy-wasm 10/10); THIRD §9 Network-filters-family row closed.
- **phase-directory:** `docs/envoy-go/phases/29-network-filter-mongo-proxy/` (BRAINSTORM.md — this commit; parent SPEC.md next). The phase-28 family directories (`28-network-filter-zookeeper-proxy/` + `28.1-network-filter-write-seam-and-zookeeper-requests/` + `28.1b-network-filter-read-seam-and-zookeeper-requests-proof/` + `28.2-network-filter-zookeeper-responses-and-latency/`) stay the durable records of their phases.
- **lifecycle-state:** `phase 29 BRAINSTORM done (SKILL_ROUTING state 1 → 2; next = parent SPEC)` — the phase-29 BRAINSTORM (4-question dialogue: Q0 `mongo_proxy` subject; Q1 full-counter+gauge-parity-minus-histograms envelope + 3-way pre-split [29.1 wire+BSON+requests / 29.2 responses+correlation / 29.3 async-halt-resume-seam+fault-delay+access-log]; Q2 the upstream-faithful async halt/resume seam as an EXTENSION of the as-built 26.1/28.1b machinery; Q3 synthesized-byte fixtures + cross-side `StatsAsserter` + deterministic 100%-probability fault-delay arms) authored at `docs/envoy-go/phases/29-network-filter-mongo-proxy/BRAINSTORM.md`. ROADMAP parent row 29 registered `in-progress` + sub-rows 29.1/29.2/29.3 `planned` at this commit (ADR-0106). The next lifecycle transition is the **phase-29 parent SPEC** (`superpowers:writing-plans` scoped to parent-SPEC authoring; the §10 D29-1..D29-12 empirical pins execute IN-SESSION vs Envoy v1.37.2). Phase 28 (parent + 28.1a/28.1b/28.2) is DONE and CANONICAL (preserved in the active-phase bullet above).
- **next-skill:** `superpowers:writing-plans` (scoped to **phase-29 parent SPEC authoring** — `docs/envoy-go/phases/29-network-filter-mongo-proxy/SPEC.md` per the 22/24/25/26/28 parent-row precedent; executes the BRAINSTORM §10 D29-1..D29-12 empirical pins IN-SESSION against Envoy v1.37.2 per ADR-0004 [parallel-subagent fan-out per the 25/26/27/28 SPEC precedent]; **D29-1 — confirm the standard `envoyproxy/envoy:v1.37.2` reference image ships `mongo_proxy` — is SPEC-BLOCKING for the Q3 cross-side strategy; D29-4 — does upstream decode OP_MSG — is the largest decode-envelope unknown**; anchors the ADR-0224 [mongo_proxy filter + BSON/wire decode + request side] + ADR-0225 [response side + correlation + gauge] + ADR-0226 [async halt/resume seam + fault delay + access log] §Context drafts; formalizes the 3-way split surface-mapping; ROADMAP sub-row 29.1 flips `planned → in-progress` at the 29.1 SPEC commit, NOT at the parent SPEC). Per `feedback_execution_style` the eventual IMPLs run subagent-driven; per `feedback_git_worktrees` work in a git worktree; subagents commit LOCAL-ONLY [`feedback_subagents_no_push`], the controller squash-merges + pushes at stage-close [`feedback_push_to_origin`]; per-task `gofmt -l` + `golangci-lint` on touched pkgs [`feedback_pertask_gofmt_lint`]. Counts at phase-29 BRAINSTORM (unchanged from phase-28 phase-done — docs only): fuzzers **38**, fixtures **50** [tail `0048-zookeeper-responses`], stat surface **337**; DECISIONS.md tail **ADR-0223** (next-free **ADR-0224** — anticipated to be consumed by the phase-29 roster ADR-0224/0225/0226).
- **last-commit:** `91b1be7` — `phase 29 BRAINSTORM: mongo_proxy — full counter+gauge parity + fault-delay + access log; async halt/resume seam; 3-way pre-split (29.1 wire+BSON+requests / 29.2 responses+correlation / 29.3 seam+fault-delay+access-log); ROADMAP rows 29/29.1/29.2/29.3 registered` (squash-merged from `phase-29-network-filter-mongo-proxy-brainstorm`; docs only — BRAINSTORM.md + ROADMAP rows + this STATE.md advance + next-prompt.txt). Substantive predecessor on master: the phase-28.2 IMPL squash `fde21c9` (trailed by the docs-only SHA-fill/repoint commits `3adc6a7`/`fd709a6`/`0a2cf8e`). origin/master in sync after push.
- **last-updated:** 2026-06-02
- **next-free ADR:** `ADR-0224` (DECISIONS.md tail STAYS **ADR-0223** — NO ADR consumed at the phase-29 BRAINSTORM; the phase-29 roster ADR-0224 [filter + BSON/wire decode + request side] / ADR-0225 [response side + correlation + gauge] / ADR-0226 [async halt/resume seam + fault delay + access log] §Context drafts land at the parent/sub-phase SPECs per ADR-0044; next-free after phase-29 phase-done ≈ **ADR-0227**, or ADR-0228 if the SPEC splits the seam into its own number). ADR-0209 escape-valve reserve carried from the §9 family STANDS-UNCONSUMED.

---

## Project counts at this commit

- **active differential fixtures:** **50** (tail `0048-zookeeper-responses`) — unchanged (docs only).
- **fuzzers:** **38** (canonical recipe `grep -rh "^func Fuzz" $(find ./internal -name fuzz_test.go) | wc -l`) — unchanged.
- **stat surface:** **337** (BEHAVIOR_CONTRACT doc count) — unchanged.
- **DECISIONS.md tail:** **ADR-0223** (next-free **ADR-0224**) — unchanged.
- **conformance:** h2spec 53/53; proxy-wasm all 10 families PASS (as of the 28.2 six-gate, 2026-06-02).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
