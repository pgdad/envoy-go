# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `06.2-access-log`
- **phase-directory:** `docs/envoy-go/phases/06.2-access-log/` — exists. SPEC.md committed at `4062c65` + `7bbf4a2` reviewer-fixes. PLAN.md committed at `16366f4`. Implementation session (Tasks 1–16) executed on branch `phase/06.2-access-log-impl`; closing local sweep at `a349f35` claimed gates (a)/(b)/(c)/(d)/(e) all GREEN. Verification session executed on branch `phase/06.2-access-log-verify` (this commit) per ADR-0003 + per-phase-worktree convention; **gate (d) FAILED** on `FuzzAccessLogFormat` at the ADR-0018 30 s budget. Lifecycle-state rolls back 4 → 3 per BOOTSTRAP §5 deviation rule (`Unexpected state → systematic-debugging FIRST`). Phase 06.1 remains closed read-only history; the parent `docs/envoy-go/phases/06-observability-baseline/` retains its `BRAINSTORM.md` + parent `SPEC.md` (still open until 06.2 phase-done flips parent row 06 at the eventual REVIEW session's phase-done commit per parent SPEC §5). ROADMAP rows 06 + 06.2 stay `in-progress`.
- **lifecycle-state:** `3` — implementation incomplete. The Task 16 closing-sweep entry in PROGRESS.md ran fuzzers with `-run <Name> -v` only (seed corpus PASS for all 8 fuzzers); it did NOT run `FuzzAccessLogFormat` for the ADR-0018 30 s budget that BOOTSTRAP §7.5 (d) ("any new fuzzer has run clean for its short-budget CI run") requires. A fresh-session 30 s fuzz run from `.worktrees/phase-06.2-access-log-verify` produced a crasher in 1.07 s: `internal/accesslog/format.go::escape()` (line 51) escapes `"` → `\"`, `\n` → `\n`, `\r` → `\r` but does NOT escape `\` → `\\`. Inputs whose quoted-field values contain a literal `\` produce output where the closing `"` field-delimiter is preceded by `\`, which round-trip un-escape readers (and the fuzzer's even-quote-count parseability invariant) interpret as an escaped quote — the closing delimiter is swallowed. Minimised seed: method=path=proto=authority=ua=`"0"`, upstream=`"\"` (single backslash). Gates (a)/(b)/(c)/(e) all PASS in the verifier worktree (fixture-0006 PASS 11.35 s; pre-existing 0000–0005 PASS 8.73 s; h2spec 53/53 PASS 2.15 s; `go vet` + `golangci-lint` + `go test -race ./...` clean for all 20 packages). Auto-generated seed file `internal/accesslog/testdata/fuzz/FuzzAccessLogFormat/1bdc705d534eee86` deleted from verifier worktree before this commit per the 05.2 verifier role contract (`b34bd99` — verifier commit changes only PROGRESS.md + STATE.md; production-code and test-corpus changes belong to the fix branch). Seed bytes quoted verbatim in PROGRESS.md verification block for the fix branch's reproduction.
- **next-skill:** `superpowers:systematic-debugging` — gate (d) FAIL is unexpected state per BOOTSTRAP §5 deviation rule. The bug is fully characterised in PROGRESS.md verification block (root cause: missing `\` escape in `internal/accesslog/format.go::escape()`; standard fix matches reference Envoy + RFC 4180 CSV-style escaping). The fix-branch session may proceed directly to the fix shape after a brief systematic-debugging pass confirms the characterisation; the formal entrypoint is still `superpowers:systematic-debugging` per the deviation-rule contract.
- **next-skill-scope:** Create worktree `.worktrees/phase-06.2-access-log-impl-followup-gate-d` on branch `phase/06.2-access-log-impl-followup-gate-d` branched from this verify commit's master fast-forward HEAD per ADR-0003 + per-phase-worktree convention. TDD-discipline (D-3.1) the fix: (1) RED — add a unit test in `internal/accesslog/format_test.go` reproducing the seed (e.g., `TestDefault_BackslashInQuotedField` with `UpstreamHost = "\\"`, asserting the closing `"` is preceded by `\\` not `\`); (2) GREEN — extend `escape()` in `internal/accesslog/format.go` to also escape `\` → `\\` (matches reference Envoy `AccessLogFormatUtils::escapeUtilityValue` + RFC 4180); (3) re-introduce the auto-save seed file at `internal/accesslog/testdata/fuzz/FuzzAccessLogFormat/1bdc705d534eee86` so Go fuzz auto-replays it permanently as a regression seed; (4) re-run gate (d) `go test -count=1 -fuzz=FuzzAccessLogFormat -fuzztime=30s ./internal/accesslog/` — must pass clean; (5) re-run gates (a)/(b)/(c)/(e) locally; (6) consider whether an ADR is warranted (the fix is an extension of the SPEC §11 escape catalog by 1 char to match reference Envoy — likely no ADR, just a SPEC §11 amend; mirror 06.1's ADR-0065 only if a non-obvious design tradeoff appears); (7) update PROGRESS.md with a fix entry; (8) advance STATE.md → lifecycle-state 4 with `next-skill: superpowers:verification-before-completion`. The next session after the fix is a verify-2 session (`phase/06.2-access-log-verify-2`, fresh worktree per the 05.1 + 06.1 verify-2 precedent) that re-runs all six gates fresh; on PASS, STATE advances 4 → 5 with `next-skill: superpowers:requesting-code-review`. ROADMAP rows 06 + 06.2 close at the eventual REVIEW session's phase-done commit per parent SPEC §5.
- **last-commit:** `503c8ee` — `phase 06.2: lifecycle-state 4 verification — gate (d) FAIL on FuzzAccessLogFormat; STATE → 3`, on branch `phase/06.2-access-log-verify`. Updates this STATE.md (lifecycle-state 4 → 3; next-skill verification-before-completion → systematic-debugging) and appends a `## Verification (lifecycle-state 4) — FAILED` block to PROGRESS.md (gates a/b/c/e PASS captures + gate-d FAIL analysis + verbatim seed bytes for fix-branch reproduction). No production-code or test-corpus changes per the 05.2 verifier role contract. SHA-fill follow-up per phase-02..06.1 convention: a follow-up tiny commit `phase 06.2 follow-up: STATE.md SHA-fill for verification commit (TBD → <SHA>)` replaces this `TBD` with the actual SHA after the master fast-forward.
- **last-updated:** 2026-04-30

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
