# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

> **This file is a POINTER, not an archive** — `BOOTSTRAP_PROMPT.md` §4: *"STATE.md — pointer: active phase, last commit, next action."* It carries the live pointer, the live counts, and a SHORT recent lineage. **Full stage-by-stage history lives in `STATE_HISTORY.md`** (archived at ADR-0288; **nothing was deleted** — the pre-move file is preserved there verbatim, byte-for-byte).
>
> **⚠️ THE RULE ADR-0288 EXISTS TO ENFORCE: there is EXACTLY ONE live `next-skill:`, `lifecycle-state:`, `next-free ADR:`, and count block IN THIS FILE, and they live in §Current pointer directly below.** Before that move, ~330 lines of prepended backlog had buried TWO fossilised "current state" blocks whose `next-skill:` still read *"the 42.2 hedging BRAINSTORM"* and *"the 46.2 SPEC"* while the live phase was **66** — so `grep 'next-skill:' STATE.md` returned **two stale hits and ZERO live ones**, and `grep 'fuzzers:'` returned **48** and **42** when the true count was **55**. **A session may trust a grep of this file again. Keep it that way: when you advance the pointer, EDIT §Current pointer IN PLACE — never prepend a new block above it.**

---

## Current pointer

*(The ONLY live "what next" in this repo. Update IN PLACE at every stage close.)*

- **active-phase:** `phase 92 (h2-response-header-validation) PLAN done` (2026-08-26) — lifecycle-state **2 -> 3**. **ROW 92 STAYS `in-progress`** at `ROADMAP.md:154`; `ROADMAP.md`, `DECISIONS.md`, `BEHAVIOR_CONTRACT.md`, `go.mod`, `go.sum` and **every `.go` file** are **BYTE-UNTOUCHED** (all verified by EMPTY DIFF against master; `git diff --name-only master -- '*.go'` => **0 files**); sentinel `want` STAYS **124**. `PLAN.md` created (**1479 lines**) in the existing phase dir; phase dirs STAY **133**. **A PLAN ADDS NO ADR** — tail STAYS **ADR-0314**, next-free STAYS **ADR-0315**, the strict `^> **STATUS: PROPOSED` guard STAYS **ARMED at 1** (`:18443`, under `## ADR-0314` at `:18441`; the ADR-0231 decoy at `:14866` also reads 1 and is byte-untouched), `^---$` STAYS **216**. ⚠️ **THE STAGE REFUTED TEN INHERITED CLAIMS, THREE OF THEM ITS OWN SPEC'S LOAD-BEARING FIGURES.** (1) **THE OPEN ARM IS CLOSED AND THE BROAD RULE SURVIVED** — the reference REJECTS an **identical** duplicate `content-length` with **502**, byte-identical to the differing-value arm, so the leg rejects ANY second `content-length`. (2) ⚠️ **THE `te` LEG HAD NEVER BEEN MEASURED IN THIS DIRECTION** and measuring it adds **TWO** shapes (`te: gzip` **502**, `te: ""` **502**, while `te: trailers` is **FORWARDED VERBATIM**) ⇒ **the divergence set is NINE shapes, not seven**. (3) **AN EIGHTH-CLASS SHAPE fell out and is scoped OUT in writing** — a SINGLE `content-length` with a wrong value gives reference **200-then-RST_STREAM** vs subject silent rewrite; **duplicate-NESS, not value-CORRECTNESS, trips the 502**, and it belongs to the banked `content-length`-rewrite candidate, now **FOUR-legged**. (4) **A THIRD FILE the SPEC did not know about** — `router_h2_trailers_test.go:528` is an **AST-audit golden** pinning the MULTISET of `ActionResponse` Status literals in `doH2ClusterAction`; it reddens on a correct change (`+2 / -1`), and a prototype that skipped the test run would have missed a mandatory file. (5) ⚠️ **THE VALIDATOR WOULD SHIP COMPLETELY UNGATED** — it moves **ZERO** of **655** tests, RUN SETS identical, so deleting its whole body would leave the suite green; **the unit table is the only thing that can fail**. (6) **COST SUPERSEDED: `+74`/`+77` -> `+174 / -1`** across THREE files in TWO packages, one compiling prototype, all gates on OUTPUT. (7) **THE COUNTER-NAMING PREMISE IS WRONG — `http2_tx_reset` exists in NO `.go` file**; envoy-go registers **DOTTED** names, so the reference leaf **`http2.rx_messaging_error`** is convention-native. (8) ⚠️ **`406` IS NOT THE STAT SURFACE AND IS NOT REPRODUCIBLE** — zero hits in `BEHAVIOR_CONTRACT.md` (whose ledger tail is **1207**), provenance an **unnamed grep**, and seven candidate commands read 143/324/401/402/403/404/699. (9) **AN UPPERCASE STAT NAME *PASSES* `IsValidName`** — a proposed NC was vacuous. (10) **The UNANCHORED `PROPOSED` count reads 3, not 2**, stale inside the ADR that documents it. ⚠️ **The SPEC's §3 symbol table survived 9/9 EXACT, ZERO DRIFT** — recorded because a clean re-derivation is a RESULT, not a reason to stop re-deriving.
- **phase-directory:** `docs/envoy-go/phases/92-h2-response-header-validation/` — `BRAINSTORM.md` (**627**) + `SPEC.md` (**586**) + `PLAN.md` (**1479**, NEW this stage). Phase dirs STAY **133**. No `PROGRESS.md` yet (the IMPL creates it); no `REVIEW.md` (standing departure).
- **lifecycle-state:** **3** (IMPL next). ⚠️ **THE SPLIT GATE WAS EVALUATED, NOT ASSUMED: 23 tasks vs ~25, ~760-975 net LoC vs ~1500 ⇒ NO SPLIT**, and three named mid-execution split triggers are written into `PLAN.md` §10.4 so the decision is not made under pressure. ⚠️ **Sentinel re-run MECHANICALLY at this tip. ACTUAL output, recorded not forecast:** (1) `want=124` -> **`NOT DONE: row 92`**, ONE line · (2) **SIX** at `:202 :208 :214 :224 :230 :238` · (3) **SILENT**. ⇒ **TWO checks block the sentinel; `stop` WAS EVALUATED AND DELIBERATELY NOT CREATED** (verified absent at the git root AND in the stage worktree). **All four NCs fired:** NC-A `NC LANDED? [ in-progress ]` **inspected FIRST**, then **TWO** lines `NOT DONE: row 62` + `NOT DONE: row 92`; NC-B **TWO** lines `NOT DONE: row 92` **AND** `GATE FAIL: examined 124 data rows, expected 123`; NC-C residual **2 -> 0** => `NEVER OPENED: gRPC`, WASM correctly silent; NC-D long **5** / short **1** / union **6**. Row 92 field-counts **NF=8 under BOTH forms**; malformed-row baseline **2** escape-aware (ids 57/69) and **17** naive — the forms **DISAGREE on row 57** (13 vs 9) but **AGREE on row 69** (10), so a gate must state which form it uses.
- **next-skill:** **the phase-92 IMPL** (`superpowers:subagent-driven-development`, or `superpowers:executing-plans` — BOOTSTRAP §5 **state 3**, lifecycle-state **3 -> DONE**). Work `PLAN.md` **T1 -> T23 in order**; TDD per `superpowers:test-driven-development`; append to `PROGRESS.md` per task. ⚠️ **THE ROW'S RED ANCHOR IS T2+T3** (the differential arm reddens on the UNMODIFIED subject) — the unit tables name symbols the row creates, so writing them first is a BUILD BREAK, which proves nothing. ⚠️ **ROW 92 FLIPS `done` AT THE IMPL, NOT BEFORE.**
- **last-commit:** the **phase-92 PLAN** commit. ⚠️ **LOCATE BY SUBJECT, NEVER BY POSITION** — `git log --grep '^phase 92 (h2-response-header-validation) PLAN' --format='%h %s'`. ⚠️ The BRAINSTORM landed **TWO** commits (a stage commit + a CORRECTION), so a slug-only grep returns several; take the NEWEST with `| head -1`.
- **last-updated:** 2026-08-26 (phase-92 **PLAN done**; row 92 UNCHANGED at `in-progress`, `want` STAYS **124**; **NO ADR added** — tail **ADR-0314**, next-free **ADR-0315**, strict `PROPOSED` guard **ARMED at 1**).
- **next-free ADR:** **`ADR-0315`**. TAIL-derived (`grep -oE '^## ADR-[0-9]+' ... | tail -1` -> `## ADR-0314`; `grep -c '^## ADR-0315'` => **0**; gap audit over `0001..0314` re-run at this tip: exactly ONE missing id, `0209`, ZERO duplicates). ⚠️ **NEVER derive from the heading count — the id space is SPARSE and headings+1 COLLIDES: it reads 314, the TAIL ITSELF, a TAKEN id.** ⚠️ **The strict guard's UNANCHORED form is fail-unsafe in the OVER-counting direction and IS NOW STALE IN THE RECORD: `grep -F '> **STATUS: PROPOSED'` reads **3**, not the recorded 2** — `:18342` and `:18371` are PROSE ABOUT the guard and `:18443` is the real one. **The anchor is load-bearing and the count inflates every time an ADR documents the form.**

---

## Project counts at this commit

*(Live. Re-verify at session start — `git fetch` first — and update here at every stage close. Canonical commands inline.)*

⚠️ **ALL counts below were re-run MECHANICALLY in the stage worktree at this phase-76 IMPL close with the canonical commands, EACH WITH ITS NEGATIVE CONTROL OBSERVED — never copied forward.** **Phase 76 landed +0 on EVERY axis, including +0 PRODUCTION `.go` FILES** — the first chartered row in this lineage whose production-code delta is exactly nothing, and the ANTICIPATED envelope held on all of them. The row's `.go` delta is **three TEST-side files** (`internal/cluster/ringhash_test.go` edited, `test/fixtures/0061-lb-ring-hash/driver/driver_test.go` edited, `.../linkage_test.go` NEW), plus three fixture-doc files and four project docs. ⚠️ **The stat-surface total 1205 is DOC-SOURCED and was NOT re-derived mechanically** — there is no mechanical command, and the chain is known to be discontinuous in **TWO** places (the recorded `1200 -> 1201`, and `Phase 46.1b` closing at 1198 while `Phase 47.1` opens at 1200 — an unattributed +2). **Only the +0 DELTA is asserted, and that argument is STRUCTURAL rather than a `statssink` result:** zero production `.go` bytes changed (G8) and zero byte mismatches over the 375-file production roster (G14), so no `NewCounter`/`NewGauge` call site can have moved; the five `TestNoNewStat*` guards corroborate at 5x PASS. ⚠️ **A future phase should COUNT the surface rather than trust the chain — a +0 row is a cheap place to do it, and phase 76 declined to charter that, so it is now a NAMED maintenance deferral rather than an idle observation.**

- **active differential fixtures:** **119** — ⚠️ **Phase 76 landed +0: `0061-lb-ring-hash` was EDITED, not added** (`sourceIPs` 4 -> 16 plus a NEW `driver/linkage_test.go` inside the EXISTING fixture directory — no new YAML, no new directory, no new BackendKind, no new port), so the count STAYS **119** and `0118` remains FREE. NC observed: `mkdir .../0118-fake` ⇒ 120. — `ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0117-tracing-custom-tags-metadata-cluster` (phase 73). **Phase 75 landed +0**: `0110-tls-require-client-cert-false` was **EXTENDED** with a cross-side `fixture.StatsAsserter` (NO new YAML, NO new directory, NO new BackendKind, NO new port), so the count STAYS **119** and `0118` remains FREE. ⚠️ **Next-free REFERENCE port is `10450`, NOT `10118`** — ports are NOT fixture-index aligned. **The fixture now asserts what its own README confessed was infeasible, and BOTH sides were EXECUTED**: `ssl.handshake=2 ssl.no_certificate=1 ssl.fail_verify_error=1 ssl.fail_verify_no_cert=0`, `downstream_cx_total=3`, **identical on reference and subject**, with the discriminating asymmetry (`handshake=2` vs `no_certificate=1`) observed per side. Shape A over `/stats/prometheus`, keyed on the metric NAME with `envoy_listener_address` IGNORED — the NAMES are byte-identical cross-side while the labels differ (`0.0.0.0_10446` reference vs **`___20016`** subject: ⚠️ **the subject binds the IPv6 WILDCARD**). ⚠️ **`0111` was NOT given a value assertion, deliberately** — it runs `require_client_certificate: true` on both sides, so a success-path no-cert annotation reads **0 on every arm, structurally**, and a value pin would document a **vacuous `0 == 0`** as coverage; its PROSE gained the name in its NAMED-UNASSERTED list instead, because **both `0111` passages are CLOSED ENUMERATIONS** and a name in neither list reads as ASSERTED. **`0111`'s driver is byte-untouched, and its named-subset check was EXECUTED for the first time at this IMPL: PASS.** ⚠️ `var _ fixture.StatsAsserter` is a **TRIPWIRE, not the dispatch mechanism** — `0110` is only the **THIRD** fixture repo-wide to carry it, while **82** define an `AssertStats` method.
- **fuzzers:** **55** — **Phase 76 landed +0** (it consumes no new config field, so there is no parse arm to fuzz). NC observed: appending `func FuzzProbeOnly` ⇒ 56. — `grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`. **Phase 75 landed +0** — the row consumes no new config field at all, so there is no parse arm and no fuzz-seed classification to flip (`reference_fuzzer_count_docs_drift`: an `f.Add` seed is not a fuzzer).
- **stat surface:** **1205** — BEHAVIOR_CONTRACT doc count. **There is NO mechanical counting command** (the contract says so itself); enforcement is per-phase `TestNoNewStat*` guards asserting a delta of ZERO, never the absolute total. **Phase 75 LANDED +1 (1204 -> 1205)** — `listener.<normalized-addr>.ssl.no_certificate`, the FOURTH listener-scope `ssl.*` name and the FIRST **SUCCESS-PATH** handshake annotation (⚠️ **NOT a synonym for `ssl.fail_verify_no_cert`, and the mis-mapping is wrong in BOTH directions** — one annotates an ACCEPTED handshake, the other books a REJECTED one; they are mutually exclusive on every connection). ⚠️ **The delta is a NAME-surface delta, NOT a flat per-deployment one:** registration rides the block that ALREADY gates on `rt.tlsMode`, so a **plaintext-only** deployment gains **ZERO** names, while a **QUIC** listener gains it permanently zero — **PARITY** with the reference, and structural (`Manager.Start` launches no accept loop for `kindQUIC`, so the Inc site is unreachable for a **name-independent** reason). ⚠️ **QUIC was NOT re-probed at phase 75** — the parity claim is INHERITED from phase 74's family-level probe, and that inheritance is stated in BEHAVIOR_CONTRACT rather than presented as a measurement. Ledger updated: `1204` -> `1205` at `BEHAVIOR_CONTRACT.md:831`/`:847`, plus a `**Phase 75 - 1204 -> 1205 (+1)**` line after the tail entry — **the ONLY line-adding edit in the file** (`5744 -> 5746`), so every by-line citation into the TLS subsection stays valid. ⚠️ **The new entry RECORDS BOTH ledger gaps and BACK-FILLS NEITHER** (fabricating a line would be inventing a record). Gate figures at this close: `grep -c '1204'` => **2** · `grep -c '1205'` => **3** · `grep -cE '^#{2,4} .*ADR-[0-9]+.*ADR-[0-9]+'` => **17** (was 16 — the `:1849` heading joined them, on the `:785` precedent). ⚠️ **`internal/statssink`'s four *"stays 1200 / 1196"* prose sites, stale since phase 49, are RECORDED-NOT-FIXED** — that package is on this phase's byte-untouched sha256 roster.
- **BackendKind tail:** **38** — **Phase 76 landed +0** (`0061` uses the in-process `TCPEcho` branch and implements neither `BackendKindAware` nor `PerHostBackendKind`). NC observed: appending `ZZProbeKind = 39` ⇒ tail 39. — (`H2GoawayResponder`, `test/differential/fixture/fixture.go:614`). **Phase 75 landed +0** — `0110` was extended, not cloned, and it implements neither `BackendKindAware` nor `PerHostBackendKind`, so the runner takes the in-process `TCPEcho` branch. ⚠️ **This is a TAIL VALUE, not a count** — the file declares **39** constants (values 0-38, `TCPEcho = 0`). The lineage figure **38** is correct as written; do NOT 'fix' it to 39.
- **DECISIONS.md tail:** **ADR-0298** (next-free **ADR-0299**) — `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1` -> `## ADR-0298`; `grep -c '^## ADR-0299'` ⇒ **0**. **ADR-0298 is COMPLETE as of the phase-76 IMPL** — §Context drafted at the phase-76 SPEC, §Decision + §Consequences APPENDED IN PLACE after the RETAINED italic footer, **no renumber and NO `---` separator** (the file's last `^---$` is still at `:17020`). Block shape controller-re-derived at this close: **1** `### Context` / **1** `### Decision` / **1** `### Consequences` / **1** retained footer. ⚠️ **FOUR of its OWN §Context paragraphs were CORRECTED IN PLACE** (¶1's `:295` -> `:307`, ¶5's `-count ~= 81` -> **83**, ¶7's *"~71 collapses"* -> **74/2000**, ¶11's *"needs no edit"* -> REFUTED) on the ADR-0297 ¶7/¶9 precedent, each **leading with what survives**; none changes a decision the ADR records. ⚠️ **It carries NO whole-file grep count and claims NO family ordinal** — the Load-balancing family closed at row 54, and **maintenance rows do not extend a charter**. *(Historical, retained: **ADR-0297 is COMPLETE** — §Context drafted at the phase-75 SPEC (ADR-0044-**as-used**; ⚠️ ADR-0044 does NOT actually contain that discipline), §Decision + §Consequences appended IN PLACE at the phase-75 IMPL after the **RETAINED** footer, mirroring ADR-0295/0296. Block shape verified at this close: **1** `### Context` / **1** `### Decision` / **1** `### Consequences` / **1** retained footer; `grep -c '^## ADR-0298'` => **0**. **No renumber, no new ADR.** ⚠️ **Its OWN §Context ¶7 and ¶9 were CORRECTED IN PLACE while completing it** — ¶7's **self-falsifying whole-file grep** (the same species the phase-74 IMPL had just fixed in ADR-0296 ¶3, reproduced one ADR later) is restated with **NO number** and the searched token deliberately **NOT spelled**, because every restatement mints another counter-example; and ¶9's *"SELF vs OTHER-ADR"* form rule is replaced by **graft SCALE** after the n=7 population was re-enumerated first-hand. ⚠️ **¶9 ALSO said *"the only two instances in the file"* — which this row's own blockquote makes three** — so a pure append would have shipped a SECOND self-falsifying count in the commit that fixed the first. **The prescription has now outlived THREE wrong justifications: family, self-vs-other, scale.** ⚠️ **`ADR-0296` is NO LONGER byte-untouched** — its §Decision (g) carries the phase-75 correction as an INDENTED blockquote (`grep -c '^  > '` **2 -> 3**), **LEADING WITH WHAT SURVIVES** (no such counter was owed *in phase 74*, whose sole stat-asserting fixture is `require=true` on both sides — **the deferral was RIGHT**) and claiming **NO ORDINAL**.)*
- **go.mod modules:** **Phase 76 landed +0** — `go mod tidy -diff` EMPTY and `git diff master -- go.mod go.sum` EMPTY; ⚠️ **the row's TEST-side import delta is +8 and that is CORRECT** (`ringhash_test.go` gains `fmt`/`math`/`mathrand "math/rand/v2"`, `linkage_test.go` adds `go/ast`/`go/parser`/`go/token`/`strconv`/`testing`) — all stdlib, all test-side, and **an import LINE is not a go.mod MODULE**; there is no new sub-package, so `reference_new_subpackage_pulls_transitive_module` does not bite. NC observed: a fake `require` ⇒ 67 -> 68. The tracked lineage figure is **2** (the phase-61.2 count: `quic-go` direct + `qpack` indirect) — **NOT a repo total**; the single `go.mod` requires **67** (18 direct + 49 indirect). **Phase 75 landed +0** — `go mod tidy -diff` EMPTY and `git diff master -- go.mod go.sum` EMPTY. The row needed `crypto/tls` and `internal/stats`, both ALREADY imported by `internal/listener/manager.go`; **an import LINE is not a go.mod MODULE**, and there is no new sub-package, so `reference_new_subpackage_pulls_transitive_module` does not bite. ⚠️ **TEST-side imports grew by exactly four** (`log`, `math`, `net/http`, `strconv` in `0110`'s driver) — all stdlib, all test-side, **not a production-envelope violation**.
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` (see `ENVOY_TARGET.md`).
- **conformance:** h2spec **`95 tests, 94 passed, 1 skipped, 0 failed`** (the skip is 6.9.2/2, invariant) — the CORRECTED 95-case strict scope under dotted selectors, measured at the phase-85 IMPL close 2026-08-10 (ADR-0307; the historical 53/53 records measured only 53 of the 95 under nine silently no-op slash-form selectors, 2026-04-25 -> 2026-08-10); proxy-wasm all 10 families PASS.

---

## Recent lineage

*(A SHORT recent lineage, capped at FIVE prior entries by ADR-0288. Everything earlier is in `STATE_HISTORY.md`. §Current carries the newest stage (**the phase-90 IMPL** — PHASE 90 CLOSED, row 90 `done`; the H/2 downstream leg now promotes `host` on an ABSENT `:authority` and suppresses every regular `host` in every shape, and the IMPL DISCOVERED A NEW REFERENCE DIVERGENCE THAT KILLED ONE OF ITS OWN ARMS: the pinned reference REFUSES a DUPLICATED regular `host` by ARITY, not by value, with no GOAWAY and no RST_STREAM, so first-occurrence-wins is NOT DIFFERENTIABLE IN PRINCIPLE and arm P90-E was REMOVED). The five below are the prior stages, newest first. ⚠️ **EVICTED AT THIS CLOSE:** `phase 89 (h2-decode-filter-mutations) SPEC done` (2026-08-16) — a **DIRECT READ of all five dates** before removal gave **2026-08-19 x2, 2026-08-17 x1, 2026-08-16 x2**, i.e. a **TWO-WAY TIE at the oldest**, broken by **LAST LIST POSITION**. ⚠️ **THE TIE SHAPE DOES NOT CARRY** — the phase-90 PLAN faced a UNIQUE OLDEST and NO tie at all, the phase-90 SPEC a TWO-way, the phase-90 BRAINSTORM none, the phase-89 IMPL a TWO-way, the phase-88 IMPL a FOUR-way. **Read the dates yourself every time; never inherit the shape — and never determine the evictee by position-by-convention.** It is now in `STATE_HISTORY.md`, appended strictly as **ONE SINGLE INLINE LINE** (the `(evicted at …):**` label carries the moved bullet on the SAME line — a two-line append BREAKS the guard), `512 -> 514` with `git diff --numstat` = `2 0`; the **ANCHORED own-label absence guard** `^- \*\*prior active-phase:\*\*` read **163 BEFORE and 163 AFTER — DELTA 0** (⚠️ that form is STRUCTURALLY INCAPABLE of counting archive labels, because an evicted entry carries `(evicted at …)` BEFORE the colon; a reading of 164 would mean the WRONG SHAPE was appended), the loose form went **206 -> 207**, the evictee was verified **ABSENT (0)** before the append, and its NC positive control fired at **3** on `phase 88 (h2-continuation-frames) SPEC done`, which IS present. ⚠️ **Carry NO archive-label absolute — the long-carried "202" figure is NOT reproducible.**)*

- **prior active-phase:** `phase 92 (h2-response-header-validation) SPEC done` (2026-08-25) — lifecycle-state **1 -> 2**. `SPEC.md` created (**586 lines**); **ADR-0314 §Context DRAFTED** (`DECISIONS.md` **18439 -> 18467**, `^## ADR-` **312 -> 313**, tail **ADR-0314**) with the RETAINED italic footer, and the strict `PROPOSED` guard RE-ARMED **0 -> 1**. **ZERO production `.go` bytes.** It settled D-92-POSTURE (**502, EVICTING, NOT RETRIABLE** — a THIRD `doH2ClusterAction` arm), D-92-VALIDATOR (**two independent functions**, not a mode flag), D-92-DIFF (**extend `0004`**, `+193 / -0`, not mint `0120`), D-92-FUZZ (**yes**) and D-92-1XX (**correct the wrong comment, 10/3**), and **REFUTED NINE claims including its OWN controller-side hypothesis** — the 502 arm's connection teardown is **PARITY**, while its **retry amplification** (3 attempts vs the reference's 1) is the real divergence the fix would introduce. ⚠️ It left **ONE OPEN ARM** (identical duplicate `content-length`) and **six items** the PLAN owed. ⚠️ **The phase-92 PLAN has since SUPERSEDED three of its figures** — `+74`/`+77` -> **`+174 / -1`**, the unsourced `406`, and the `http2_tx_reset` spelling — and measured the **`te` leg it shipped unmeasured**.

- **prior active-phase:** `phase 92 (h2-response-header-validation) BRAINSTORM done` (2026-08-25) — lifecycle-state **DONE -> 1**. **PHASE 92 OPENED BY SELF-PICK** per the 2026-07-12 standing directive, with no banked mid-lifecycle work, **PROVEN not assumed** (all 123 rows `done`, check (1) SILENT at `want=123`, NC-A the ONE-line `NOT DONE: row 62`). **ROW 92 REGISTERED `in-progress`** at `ROADMAP.md:154` in the SAME commit (`241 -> 242` lines / `123 -> 124` rows, `git diff --numstat` = **`1 0`**, a PURE INSERTION) and sentinel `want` **123 -> 124**; check-(2) anchors shifted +1 to `:202 :208 :214 :224 :230 :238` with the COUNT invariant at SIX and the **CONTENT byte-identical, MEASURED by per-line md5 with a negative control proving the comparator discriminates**. **THE CHARTER: on the HTTP/2 downstream leg, REJECT a response whose upstream header block carries a connection-specific or otherwise malformed field, instead of laundering it onto the downstream stream** — the ENCODE-direction mirror of `h2.IsIllegalH2RequestHeader`, which row 89 landed on the DECODE direction. **REPRODUCED BY EXECUTION at `47e44004`** with a raw-framer client holding ONE `hpack.Decoder` per connection and `ReadMetaHeaders` deliberately UNSET, a purpose-built raw h2c backend, positive controls FIRST and LAST, three byte-identical reproductions, and a **DIRECT-TO-BACKEND discriminating negative control** without which the finding is unfalsifiable: seven shapes — `connection`, `transfer-encoding`, `keep-alive`, `upgrade`, `proxy-connection`, an uppercase name, and a DUPLICATED `content-length` — are each **forwarded by envoy-go with 200** where the reference answers **502 `reset reason: protocol error`**, so envoy-go emits a response RFC 9113 §8.2.2 requires every conformant peer to reject. Cost floor **`+44 / -0`, ONE file and ONE symbol**, from a prototype BUILT, RUN and REVERTED (`sha256sum -c` => OK) and re-driven on the wire. ⚠️ **NO EXISTING TEST PINS IT IN EITHER DIRECTION** (655 RUN / 0 FAIL with the fix applied; `./internal/...` 70/70 ok) and that green is **proven non-vacuous by a REACHABILITY CONTROL** — a `panic()` at the site reddens `TestChainIntegration_H2_DirectResponseHappy` — so it is assertion blindness, not dead code.

- **prior active-phase:** `phase 91 (h2-framer-partial-frame-desync) IMPL done` (2026-08-24) — lifecycle-state **3 -> DONE**. **PHASE 91 CLOSED, ROW 91 FLIPPED `done`** at `ROADMAP.md:153` (a FLIP, not an ADD — `ROADMAP.md` stays **241 lines / 123 data rows**, sentinel `want` STAYS **123**, and the edited row field-counts **NF=8 under BOTH the escape-aware and naive forms**). **THE DEFECT IS FIXED AND THE FIX IS PROVEN UNDER THE CONTENTION THAT REPRODUCES IT:** interleaved (U,P,U,P,…) contended arms read **UNPATCHED 9 FAIL / 32** against **PATCHED 0 FAIL / 32**, with the unpatched arm independently at **9 / 36** earlier in the stage — so the base rate was stable and the machine was demonstrably still producing the hang during the window the patched arm was sampled. **ZERO third-mode iterations in either arm** (fast pole 0.01-0.05 s, slow pole 10.00-10.03 s = the test's own `frameReadTimeout`, i.e. a HANG not slowness); stall points scattered 438-810 of 1024. ⚠️ **The uncontended control is 0.01 s PASS on BOTH arms and proves NOTHING** — that reading absorbed this deadlock as a flake three times. ⚠️ **THE STAGE REFUTED FOUR MORE INHERITED CLAIMS BY EXECUTION.** (1) **h2spec 5.1.2/1 IS NOT A GATE for the burst-drain ordering guarantee** — with `tryReadFrame` ablated to `return nil, nil` it stayed at `95 tests, 94 passed, 1 skipped, 0 failed` **4 runs out of 4 on the unpatched tree and again on the patched tree**, the ablation CONFIRMED PRESENT IN THE COMPILED BINARY BY DISASSEMBLY; combined with the SPEC's 204/204 finding the guarantee was pinned by **ZERO tests at ANY layer**, so this row **BUILT** the missing gate rather than inherit one. (2) *"`framer.go` must end with ZERO `SetReadDeadline` sites"* is **FALSE AS STATED** — the frozen design puts the `aLongTimeAgo` stamp in `closeReader`, in that file; the landed posture is **zero of the SIX polling sites and exactly ONE stamp**. (3) `TestFramer_ReadFrameCtxCancel` **PASSES UNMODIFIED** — the predicted +2/−0 repair was unnecessary and **no edit was made**. (4) the leak is **delta 0 vs 40**, not the inherited *"delta 5 over 40"*, so the guard's slack is **2 from the noise floor** — the precedent's `+8` would have MASKED it.

- **prior active-phase:** `phase 91 (h2-framer-partial-frame-desync) PLAN done` (2026-08-23) — lifecycle-state **2 -> 3**. **ROW 91 STAYS `in-progress`** at `ROADMAP.md:153`; `ROADMAP.md`, `DECISIONS.md`, `BEHAVIOR_CONTRACT.md`, `go.mod`, `go.sum` and **every `.go` file** are **BYTE-UNTOUCHED** (all verified by EMPTY DIFF against master; `git diff --name-only master -- '*.go'` => **0 files**); sentinel `want` STAYS **123**. `PLAN.md` created (**1171 lines**) in the existing phase dir; phase dirs STAY **132**. **A PLAN ADDS NO ADR** — tail STAYS **ADR-0313**, next-free STAYS **ADR-0314**, the strict `^> **STATUS: PROPOSED` guard STAYS **ARMED at 1** (`:18371`, under `## ADR-0313` at `:18369`; the ADR-0231 decoy at `:14866` also reads 1 and is byte-untouched), `^---$` STAYS **216**. ⚠️ **THE STAGE REFUTED TEN INHERITED CLAIMS, TWO OF THEM LOAD-BEARING FOR WHERE THE MANDATED EDIT GOES.** (1) `conn.go:276-278` is **WRONG** — the drain block is **276-281**, and as cited it stops *before* the `io.Copy` drain it names. (2) `conn.go:311-314` is **WRONG** — the block is **311-317**, and ⚠️ **the SPEC CONTRADICTS ITSELF** (its §5.5 already writes `311-317`); that block also carries a fifth statement the other lacks, `flushPendingDispatch()` at **312**, directly load-bearing for join ordering. (3) `frame.go:545` for `checkFrameOrder` is **off by 2** — the declaration is `:543`. (4) `framer_test.go:330` names the *call site*; the test is `TestFramer_ReadFrameCtxCancel` at **:320**. (5) *"`frame.go:497-500` invalidates **unconditionally**"* is **OVERSTATED** — `:499` is guarded by `if fr.lastFrame != nil` at `:498`. (6) ⚠️ **`internal/cluster/hedge_test.go:353`, the SPEC's third `pollUntil`, DOES NOT EXIST** — the true path is `internal/filter/http/router/hedge_test.go:353`, a different package. (7) *"exactly TWO exported producing functions"* holds only for package-level `func`s — **`(*Framer).ReadFrame` at `frame.go:496` is an exported METHOD producing a `Frame`** (the D-91-ALIAS conclusion survives: it mints from the WIRE, never from a caller-supplied `[]byte`). (8) *"the parent package never drives a real `ServerConn` frame loop"* is **wrong as a code claim** — `filter_test.go:150/:161` -> `filter.go:113 Handle` -> `:125 runH2` -> `:189 h2.NewServerConn` -> `Run()`; the CONCLUSION survives on a sharper measured reason (that test closes the client before the preface, so `Run` returns at `conn.go:212-215` and **never reaches `startReader()`**). (9) the four H2-capable fixtures are the right SET on a **wrong reason** — `codec_type`'s proto zero value IS `AUTO`, so the **36** fixture dirs that never write it are also `AUTO`; **the real discriminator is the downstream ALPN `h2` offer**, which selects the same four. (10) the SPEC's contended fast pole *"~0.07 s"* does not reproduce — 5/5 uncontended isolated runs read **0.01 s** at this tip. ⚠️ **THE PLAN ALSO DISCHARGES ONE OF THE SPEC'S OWN DESIGN ARGUMENTS:** §5.5 A2's *"the reader steals the drain's bytes"* **cannot arise** under the frozen design (the reader exits on its first read error and closes the frame channel before any consumer can observe that error), so **the two inline drain joins are NO-OPS at this tip** — retained as a structural invariant and **said so** rather than implied. ⚠️ **AND `closeReader` IS signal + STAMP + join, not signal-and-join:** the stamp is the only thing that unblocks a reader parked in a blocking `read(2)`, and it is safe **only because** this fix deletes all **six** reader-side `SetReadDeadline` sites (`framer.go` 170/173/185/191/202/204 -> **zero**), which is exactly the race SPEC §5.4 objected to.

- **prior active-phase:** `phase 91 (h2-framer-partial-frame-desync) SPEC done` (2026-08-23) — lifecycle-state **1 -> 2**. **ROW 91 STAYS `in-progress`** at `ROADMAP.md:153` and `ROADMAP.md` is **BYTE-UNTOUCHED** (verified by EMPTY DIFF against master); sentinel `want` STAYS **123**. `SPEC.md` created (**623 lines**) in the existing phase dir; phase dirs STAY **132**. **ADR-0313 §Context DRAFTED** (`DECISIONS.md` **18367 -> 18387**, `^---$` **216 DELTA 0**, tail **ADR-0313**, next-free **ADR-0314**) and the strict `^> **STATUS: PROPOSED` guard RE-ARMED **0 -> 1**, verified BY LINE AND ADR (`:18371`, nearest preceding heading `## ADR-0313` at `:18369` — NOT the ADR-0231 decoy at `:14866`, which still reads 1 and is byte-untouched). **ZERO production `.go` bytes changed. `BEHAVIOR_CONTRACT.md` BYTE-UNTOUCHED (5962).** ⚠️ **THE STAGE REFUTED SEVEN INHERITED CLAIMS, THREE LOAD-BEARING FOR THE FIX SHAPE.** (1) *"A deep copy is impossible from outside `x/net`"* is **FALSE** — every aliasing type has an EXPORTED accessor and a probe proved the copy survives a clobber; the conclusion (unbuffered channel + release handshake) survives only on REBUILT grounds, namely that `frame.go` exports just TWO producing functions so a copy cannot be re-minted as an `http2.Frame`. (2) The retaining-type inventory is **SEVEN, not five** (`DataFrame.data`, `UnknownFrame.p` omitted). (3) `closeReader` needs **SEVEN wiring sites, not four**, and the fourth **DOES NOT EXIST** — `(*ClientConn).readLoop` has ZERO defers. (4) The differential departure's stated grounds (*"no reference-side counterpart"*) are **WRONG**: the observable IS reference-comparable; the **TRIGGER** is not harness-controllable (the subject subprocess is spawned with **no `cmd.Env`**). (5) `ROADMAP.md` cites `esalaine` **4 LINES / 7 OCCURRENCES** — the carried "FIVE" is wrong on both axes. (6) The ARM-A escape-aware guard reads **2** (rows 57/69), NOT 0 — a SPEC asserting `== 0` would fail. (7) A controller-caught near-miss: an agent's *"`git grep -c` under-matches `find` by one"* **DOES NOT REPRODUCE** (both read **71**, per-file diff EMPTY) and had already been written into the SPEC before being verified. ⚠️ **GAP 1 IS NOW PRICED AND GATEABLE:** the burst-drain / h2spec 5.1.2/1 ordering guarantee is pinned by ZERO tests — proven by GUTTING the drain (204/204 green 4/4, both ordering-adjacent tests 5/5), not by a grep — and h2spec `"http2/5.1.2": 1` is a PINNED suite whose image digest matches locally, already a per-push CI gate at ~9 s warm. ⚠️ **Its NEGATIVE CONTROL is the load-bearing half and is MANDATED at the IMPL.**


## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
