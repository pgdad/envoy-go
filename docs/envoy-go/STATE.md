# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

> **This file is a POINTER, not an archive** — `BOOTSTRAP_PROMPT.md` §4: *"STATE.md — pointer: active phase, last commit, next action."* It carries the live pointer, the live counts, and a SHORT recent lineage. **Full stage-by-stage history lives in `STATE_HISTORY.md`** (archived at ADR-0288; **nothing was deleted** — the pre-move file is preserved there verbatim, byte-for-byte).
>
> **⚠️ THE RULE ADR-0288 EXISTS TO ENFORCE: there is EXACTLY ONE live `next-skill:`, `lifecycle-state:`, `next-free ADR:`, and count block IN THIS FILE, and they live in §Current pointer directly below.** Before that move, ~330 lines of prepended backlog had buried TWO fossilised "current state" blocks whose `next-skill:` still read *"the 42.2 hedging BRAINSTORM"* and *"the 46.2 SPEC"* while the live phase was **66** — so `grep 'next-skill:' STATE.md` returned **two stale hits and ZERO live ones**, and `grep 'fuzzers:'` returned **48** and **42** when the true count was **55**. **A session may trust a grep of this file again. Keep it that way: when you advance the pointer, EDIT §Current pointer IN PLACE — never prepend a new block above it.**

---

## Current pointer

*(The ONLY live "what next" in this repo. Update IN PLACE at every stage close.)*

- **active-phase:** `phase 83 (wasm-pause-arm-leak) IMPL done` (2026-08-06) — lifecycle-state **3 -> DONE**; **ROW 83 FLIPPED `in-progress` -> `done`; THE PHASE WAS CLOSED.** Base master **`1c93c6ad`** (from `git rev-parse master`), branch `phase-83-impl`; sentinel `want` **STAYED 115** (the IMPL updates `status` IN PLACE per §Schema `:18`, so the `ROADMAP.md` diff is **exactly one line**, and a before/after count confirmed the check-(2) union, the `-family row` total and the data-row denominator all **INVARIANT** — no leak). **EIGHT task commits**, subagent-driven, each committing locally only; the order-constrained core (S3 guard **before** S1) ran sequentially in ONE worktree. ⚠️ **THE HEADLINE: THE ROW CLOSED A *LIVE* PRODUCTION LEAK THAT THE BRAINSTORM, THE SPEC AND THE PLAN ALL MISSED.** S8 was chartered as ONE park, the PLAN widened it to THREE, S2's task found a FOURTH — and the real roster is **ELEVEN `TerminateStream` arms across THREE status axes**, of which the one that matters is `EncodeHeaders`' captured-local-response arm parking `RunEncodeHeaders`, **an iterator all three HCM dispatchers drive**. Its own comment claimed the return *"prevents the response from continuing through the chain"*; measured, it **parked and did not return in 3 s**. It was found only because S8's remedy forced a census of the whole status surface instead of the one arm the charter named. ⚠️ **SECOND: THE §6.1 LoC TRIGGER IS CROSSED AGAINST A BAND THAT WAS ITSELF MEASURED, NOT ESTIMATED** — realized **+3536 net `.go`** against the PLAN's ~1950 budget, **~1.8x**. The PLAN had replaced the SPEC's analogue-scaled figure with measured prototypes precisely to stop this. ⚠️ **`reference_measured_prototype_is_a_lower_bound` HAS NOW FIRED ON FOUR CONSECUTIVE ROWS AND MEASURING DID NOT NEUTRALISE IT — the cause is under-ENUMERATION, not under-SCALING, and a prototype measures only the items someone thought to enumerate.** ⚠️ **THIRD: THE ROW SHIPS TWO UNGATED STATEMENTS AND NAMES BOTH** — the watchdog log-string correction (nothing in the package asserts any log string: 22 test files, zero log-capture idioms, against a firing control of 5 on the non-test side) and the `Add(1)`-before-`Store` ordering, which no probe catches. ⚠️ **FOURTH: THE PLAN'S OWN BREAK ROSTER WAS WRONG** — the guard-alone break **REDDENS at 400/400**, so the stacked arm is no longer stacked-by-necessity and `Stop()` is confirmed resource hygiene, not correctness. **THIRTY-SEVEN refutations.** **DISPOSITION: RECORDED, NOT RETRO-SPLIT** — the scope items are coupled by measurement and every split axis cuts a correctness constraint.
- **phase-directory:** `docs/envoy-go/phases/83-wasm-pause-arm-leak/` (`BRAINSTORM.md`, `SPEC.md`, `PLAN.md`, `PROGRESS.md` — **no `REVIEW.md`, the STANDING LINEAGE DEPARTURE**). Prior: `docs/envoy-go/phases/82-wasm-http-call-response-cache/`
- **lifecycle-state:** **`DONE`** (phase 83 IMPL done; **row 83 is `done` and the phase is CLOSED**). ⚠️ **Sentinel re-run MECHANICALLY at this tip, BEFORE and AFTER all edits: it does NOT fire and `stop` was NOT created** (`ls stop` => `No such file or directory`). Input measured **231 lines / 115 data rows** first, so an empty result could not read as a zero result. **(1) SILENT — every ROADMAP row is now `done`.** ⚠️ **THIS IS THE STAGE AT WHICH SILENCE BECAME INDISTINGUISHABLE FROM A BROKEN CHECK, so the doctored-copy NC stopped being optional and was run: row 62 doctored => `NOT DONE: row 62`, with `NC LANDED? [ in-progress ]` INSPECTED BEFORE the result was trusted.** **(2) FIVE — `:193 :203 :213 :219 :227`, UNCHANGED; the THIRTY-NINTH consecutive phase without a decrease. THIS ROW NARROWED NOTHING AND THAT IS STRUCTURAL** — the `### WASM host family` heading at `ROADMAP.md:221` owns none of the five anchors. **(3) `NEVER OPENED: gRPC` — ALONE.** ⇒ the condition is a CONJUNCTION and checks (2) and (3) still print, so **the sentinel does NOT fire.** ⚠️ **FIVE negative controls, ALL FIRED:** row 62 doctored; `want=114` => `GATE FAIL: examined 115 data rows, expected 114`; an invented slug fires while `WASM`/`HTTP-filters` correctly do not; check (2) **one-arm** strip moves **5 -> 4, NOT 5 -> 0**; both arms stripped => 0.
- **next-skill:** **the next phase's BRAINSTORM — SELF-PICK per the 2026-07-12 standing directive.** No banked mid-lifecycle work remains: phase 83 is CLOSED and no other row is `in-progress`. Record the pick and the rejected alternatives in the BRAINSTORM. ⚠️ **Carry forward the two findings this row could not close:** (a) the row named **two UNGATED corrections** — operator-facing watchdog log strings and the `Add(1)`-before-`Store` ordering — and a gate for the first is CONSTRUCTIBLE (`decode_headers.go` declares `var logf = log.Printf`, so it is swappable) but UNCHARTERED; (b) **a trailers-paused guest is BLIND for TWO independent reasons** — the header-map router sends the trailer map types to its default arm **and** the trailers dispatchers never capture the trailers at all — so **any future row must price BOTH seams**, since fixing the routing alone would still hand the guest an empty map.
- **last-commit:** the phase-83 IMPL squash. ⚠️ **LOCATE BY SUBJECT, NEVER BY POSITION**, and ⚠️ **a bare `git log --grep 'phase 83'` is CONTAMINATED** because router commit BODIES discuss the phase — the sentence warning about the grep satisfies the grep. **Use `git log --grep '^phase 83 (wasm-pause-arm-leak)' --format='%h %s'`** and take the NEWEST (`| head -1`), verifying the count first: it returned **3** before this stage (BRAINSTORM, SPEC, PLAN) and returns **4** after, with an invented slug at **0** and the completed phase-82 form at **4** as firing controls. Base master `1c93c6ad`.
- **last-updated:** 2026-08-06 (phase-83 IMPL done). ⚠️ **THE LINEAGE EVICTION RULE WAS HONORED:** §Recent was at its five-entry cap, so **`phase 82 (wasm-http-call-response-cache) SPEC done`** — RE-DERIVED as OLDEST at this tip by date order, not inherited — was moved to `STATE_HISTORY.md` **453 -> 455**, an edit verified **STRICTLY APPEND-ONLY** (2 insertions / 0 deletions **and** the base file confirmed a byte-exact PREFIX of the new one, not merely 'looks right'). ⚠️ **THE TOLERANT ARCHIVE-ABSENCE GUARD WAS USED, NOT THE FAIL-UNSAFE COLON-ANCHORED ONE**, which reads **163** where the tolerant form reads **176** — thirteen entries carry a parenthetical between the field name and the colon, and they are the most recent phases' evictions. Because the guard exists to prove an entry is ABSENT before appending, a false 'absent' **AUTHORIZES A DUPLICATE APPEND**. Run here at **0** for the target, with **THREE firing NCs on REAL targets**: a plain-label target at 1 on both forms, an ANNOTATED-label target at **1 tolerant / 0 old — the fail-unsafe miss reproduced exactly** — and an invented target at 0 on both. ⚠️ **The guard's regex is DESCRIBED rather than spelled inside the archived label, because a quoting entry defeats its own negated-asterisk class and SELF-CLEARS from the guard.** ⚠️ **A CONTROLLER SLIP WORTH KEEPING: the one-line `sed` written to display the §Recent roster mis-parsed one entry — a greedy backtick match printed `panic!` instead of the phase name — so the eviction target was confirmed by DIRECT READ instead. An extraction command is a claim too.**
- **next-free ADR:** **`ADR-0306`** — `DECISIONS.md` tail is **ADR-0305**, and **ADR-0305 is now COMPLETE**: §Decision + §Consequences **APPENDED IN PLACE after the RETAINED italic footer, no renumber and NO `---` separator**. ⚠️ **THE COMPLETING-STAGE INVARIANTS WERE ASSERTED, NOT 'byte-untouched':** `^## ADR-` headings **STAYED 304** · tail **STAYED ADR-0305**, next-free **STAYED ADR-0306** (derived from the TAIL — ⚠️ `next-free = headings + 1` COLLIDES because of the single id gap; **do NOT 'fix' it**) · `^---$` **STAYED 216** · STATUS census **STAYED 18** · retained italic footers **STAYED 11** · the strict guard `^> \*\*STATUS: PROPOSED` went **1 -> 0** · and the line count GREW **17892 -> 17926**, the one figure meant to move. ⚠️ **ADR-0305'S OWN NOW-STALE STATUS SENTENCE WAS CORRECTED IN PLACE** on the ADR-0297 ¶7/¶9 + ADR-0298 precedent, **leading with what survives**: the loose form IS a false-positive gate and the strict form IS the right one; only the arithmetic moved. ⚠️ **AND THE COMPLETION DID NOT RESTORE THE OLD FIGURE — IT CONVERTED THE BLOCK INTO ONE MORE FALSE POSITIVE**, because the correction narrates the word it counts; measured, the loose form still returns **5**, now false-positive in every hit. **NO live whole-file count of that pattern is carried anywhere, and that is the lesson rather than an omission.**

---

## Project counts at this commit

*(Live. Re-verify at session start — `git fetch` first — and update here at every stage close. Canonical commands inline.)*

⚠️ **ALL counts below were re-run MECHANICALLY in the stage worktree at this phase-76 IMPL close with the canonical commands, EACH WITH ITS NEGATIVE CONTROL OBSERVED — never copied forward.** **Phase 76 landed +0 on EVERY axis, including +0 PRODUCTION `.go` FILES** — the first chartered row in this lineage whose production-code delta is exactly nothing, and the ANTICIPATED envelope held on all of them. The row's `.go` delta is **three TEST-side files** (`internal/cluster/ringhash_test.go` edited, `test/fixtures/0061-lb-ring-hash/driver/driver_test.go` edited, `.../linkage_test.go` NEW), plus three fixture-doc files and four project docs. ⚠️ **The stat-surface total 1205 is DOC-SOURCED and was NOT re-derived mechanically** — there is no mechanical command, and the chain is known to be discontinuous in **TWO** places (the recorded `1200 -> 1201`, and `Phase 46.1b` closing at 1198 while `Phase 47.1` opens at 1200 — an unattributed +2). **Only the +0 DELTA is asserted, and that argument is STRUCTURAL rather than a `statssink` result:** zero production `.go` bytes changed (G8) and zero byte mismatches over the 375-file production roster (G14), so no `NewCounter`/`NewGauge` call site can have moved; the five `TestNoNewStat*` guards corroborate at 5x PASS. ⚠️ **A future phase should COUNT the surface rather than trust the chain — a +0 row is a cheap place to do it, and phase 76 declined to charter that, so it is now a NAMED maintenance deferral rather than an idle observation.**

- **active differential fixtures:** **119** — ⚠️ **Phase 76 landed +0: `0061-lb-ring-hash` was EDITED, not added** (`sourceIPs` 4 -> 16 plus a NEW `driver/linkage_test.go` inside the EXISTING fixture directory — no new YAML, no new directory, no new BackendKind, no new port), so the count STAYS **119** and `0118` remains FREE. NC observed: `mkdir .../0118-fake` ⇒ 120. — `ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0117-tracing-custom-tags-metadata-cluster` (phase 73). **Phase 75 landed +0**: `0110-tls-require-client-cert-false` was **EXTENDED** with a cross-side `fixture.StatsAsserter` (NO new YAML, NO new directory, NO new BackendKind, NO new port), so the count STAYS **119** and `0118` remains FREE. ⚠️ **Next-free REFERENCE port is `10450`, NOT `10118`** — ports are NOT fixture-index aligned. **The fixture now asserts what its own README confessed was infeasible, and BOTH sides were EXECUTED**: `ssl.handshake=2 ssl.no_certificate=1 ssl.fail_verify_error=1 ssl.fail_verify_no_cert=0`, `downstream_cx_total=3`, **identical on reference and subject**, with the discriminating asymmetry (`handshake=2` vs `no_certificate=1`) observed per side. Shape A over `/stats/prometheus`, keyed on the metric NAME with `envoy_listener_address` IGNORED — the NAMES are byte-identical cross-side while the labels differ (`0.0.0.0_10446` reference vs **`___20016`** subject: ⚠️ **the subject binds the IPv6 WILDCARD**). ⚠️ **`0111` was NOT given a value assertion, deliberately** — it runs `require_client_certificate: true` on both sides, so a success-path no-cert annotation reads **0 on every arm, structurally**, and a value pin would document a **vacuous `0 == 0`** as coverage; its PROSE gained the name in its NAMED-UNASSERTED list instead, because **both `0111` passages are CLOSED ENUMERATIONS** and a name in neither list reads as ASSERTED. **`0111`'s driver is byte-untouched, and its named-subset check was EXECUTED for the first time at this IMPL: PASS.** ⚠️ `var _ fixture.StatsAsserter` is a **TRIPWIRE, not the dispatch mechanism** — `0110` is only the **THIRD** fixture repo-wide to carry it, while **82** define an `AssertStats` method.
- **fuzzers:** **55** — **Phase 76 landed +0** (it consumes no new config field, so there is no parse arm to fuzz). NC observed: appending `func FuzzProbeOnly` ⇒ 56. — `grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`. **Phase 75 landed +0** — the row consumes no new config field at all, so there is no parse arm and no fuzz-seed classification to flip (`reference_fuzzer_count_docs_drift`: an `f.Add` seed is not a fuzzer).
- **stat surface:** **1205** — BEHAVIOR_CONTRACT doc count. **There is NO mechanical counting command** (the contract says so itself); enforcement is per-phase `TestNoNewStat*` guards asserting a delta of ZERO, never the absolute total. **Phase 75 LANDED +1 (1204 -> 1205)** — `listener.<normalized-addr>.ssl.no_certificate`, the FOURTH listener-scope `ssl.*` name and the FIRST **SUCCESS-PATH** handshake annotation (⚠️ **NOT a synonym for `ssl.fail_verify_no_cert`, and the mis-mapping is wrong in BOTH directions** — one annotates an ACCEPTED handshake, the other books a REJECTED one; they are mutually exclusive on every connection). ⚠️ **The delta is a NAME-surface delta, NOT a flat per-deployment one:** registration rides the block that ALREADY gates on `rt.tlsMode`, so a **plaintext-only** deployment gains **ZERO** names, while a **QUIC** listener gains it permanently zero — **PARITY** with the reference, and structural (`Manager.Start` launches no accept loop for `kindQUIC`, so the Inc site is unreachable for a **name-independent** reason). ⚠️ **QUIC was NOT re-probed at phase 75** — the parity claim is INHERITED from phase 74's family-level probe, and that inheritance is stated in BEHAVIOR_CONTRACT rather than presented as a measurement. Ledger updated: `1204` -> `1205` at `BEHAVIOR_CONTRACT.md:831`/`:847`, plus a `**Phase 75 - 1204 -> 1205 (+1)**` line after the tail entry — **the ONLY line-adding edit in the file** (`5744 -> 5746`), so every by-line citation into the TLS subsection stays valid. ⚠️ **The new entry RECORDS BOTH ledger gaps and BACK-FILLS NEITHER** (fabricating a line would be inventing a record). Gate figures at this close: `grep -c '1204'` => **2** · `grep -c '1205'` => **3** · `grep -cE '^#{2,4} .*ADR-[0-9]+.*ADR-[0-9]+'` => **17** (was 16 — the `:1849` heading joined them, on the `:785` precedent). ⚠️ **`internal/statssink`'s four *"stays 1200 / 1196"* prose sites, stale since phase 49, are RECORDED-NOT-FIXED** — that package is on this phase's byte-untouched sha256 roster.
- **BackendKind tail:** **38** — **Phase 76 landed +0** (`0061` uses the in-process `TCPEcho` branch and implements neither `BackendKindAware` nor `PerHostBackendKind`). NC observed: appending `ZZProbeKind = 39` ⇒ tail 39. — (`H2GoawayResponder`, `test/differential/fixture/fixture.go:614`). **Phase 75 landed +0** — `0110` was extended, not cloned, and it implements neither `BackendKindAware` nor `PerHostBackendKind`, so the runner takes the in-process `TCPEcho` branch. ⚠️ **This is a TAIL VALUE, not a count** — the file declares **39** constants (values 0-38, `TCPEcho = 0`). The lineage figure **38** is correct as written; do NOT 'fix' it to 39.
- **DECISIONS.md tail:** **ADR-0298** (next-free **ADR-0299**) — `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1` -> `## ADR-0298`; `grep -c '^## ADR-0299'` ⇒ **0**. **ADR-0298 is COMPLETE as of the phase-76 IMPL** — §Context drafted at the phase-76 SPEC, §Decision + §Consequences APPENDED IN PLACE after the RETAINED italic footer, **no renumber and NO `---` separator** (the file's last `^---$` is still at `:17020`). Block shape controller-re-derived at this close: **1** `### Context` / **1** `### Decision` / **1** `### Consequences` / **1** retained footer. ⚠️ **FOUR of its OWN §Context paragraphs were CORRECTED IN PLACE** (¶1's `:295` -> `:307`, ¶5's `-count ~= 81` -> **83**, ¶7's *"~71 collapses"* -> **74/2000**, ¶11's *"needs no edit"* -> REFUTED) on the ADR-0297 ¶7/¶9 precedent, each **leading with what survives**; none changes a decision the ADR records. ⚠️ **It carries NO whole-file grep count and claims NO family ordinal** — the Load-balancing family closed at row 54, and **maintenance rows do not extend a charter**. *(Historical, retained: **ADR-0297 is COMPLETE** — §Context drafted at the phase-75 SPEC (ADR-0044-**as-used**; ⚠️ ADR-0044 does NOT actually contain that discipline), §Decision + §Consequences appended IN PLACE at the phase-75 IMPL after the **RETAINED** footer, mirroring ADR-0295/0296. Block shape verified at this close: **1** `### Context` / **1** `### Decision` / **1** `### Consequences` / **1** retained footer; `grep -c '^## ADR-0298'` => **0**. **No renumber, no new ADR.** ⚠️ **Its OWN §Context ¶7 and ¶9 were CORRECTED IN PLACE while completing it** — ¶7's **self-falsifying whole-file grep** (the same species the phase-74 IMPL had just fixed in ADR-0296 ¶3, reproduced one ADR later) is restated with **NO number** and the searched token deliberately **NOT spelled**, because every restatement mints another counter-example; and ¶9's *"SELF vs OTHER-ADR"* form rule is replaced by **graft SCALE** after the n=7 population was re-enumerated first-hand. ⚠️ **¶9 ALSO said *"the only two instances in the file"* — which this row's own blockquote makes three** — so a pure append would have shipped a SECOND self-falsifying count in the commit that fixed the first. **The prescription has now outlived THREE wrong justifications: family, self-vs-other, scale.** ⚠️ **`ADR-0296` is NO LONGER byte-untouched** — its §Decision (g) carries the phase-75 correction as an INDENTED blockquote (`grep -c '^  > '` **2 -> 3**), **LEADING WITH WHAT SURVIVES** (no such counter was owed *in phase 74*, whose sole stat-asserting fixture is `require=true` on both sides — **the deferral was RIGHT**) and claiming **NO ORDINAL**.)*
- **go.mod modules:** **Phase 76 landed +0** — `go mod tidy -diff` EMPTY and `git diff master -- go.mod go.sum` EMPTY; ⚠️ **the row's TEST-side import delta is +8 and that is CORRECT** (`ringhash_test.go` gains `fmt`/`math`/`mathrand "math/rand/v2"`, `linkage_test.go` adds `go/ast`/`go/parser`/`go/token`/`strconv`/`testing`) — all stdlib, all test-side, and **an import LINE is not a go.mod MODULE**; there is no new sub-package, so `reference_new_subpackage_pulls_transitive_module` does not bite. NC observed: a fake `require` ⇒ 67 -> 68. The tracked lineage figure is **2** (the phase-61.2 count: `quic-go` direct + `qpack` indirect) — **NOT a repo total**; the single `go.mod` requires **67** (18 direct + 49 indirect). **Phase 75 landed +0** — `go mod tidy -diff` EMPTY and `git diff master -- go.mod go.sum` EMPTY. The row needed `crypto/tls` and `internal/stats`, both ALREADY imported by `internal/listener/manager.go`; **an import LINE is not a go.mod MODULE**, and there is no new sub-package, so `reference_new_subpackage_pulls_transitive_module` does not bite. ⚠️ **TEST-side imports grew by exactly four** (`log`, `math`, `net/http`, `strconv` in `0110`'s driver) — all stdlib, all test-side, **not a production-envelope violation**.
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` (see `ENVOY_TARGET.md`).
- **conformance:** h2spec 53/53; proxy-wasm all 10 families PASS.

---

## Recent lineage

*(A SHORT recent lineage, capped at FIVE prior entries by ADR-0288. Everything earlier is in `STATE_HISTORY.md`. §Current carries the newest stage (**the phase-83 IMPL** — the stage that closed the phase, and that found a LIVE production park in `RunEncodeHeaders` which no phase-83 document had named, widened S8 from one chartered arm to ELEVEN across three status axes, crossed §6.1 again at ~1.8x against a band the PLAN had already MEASURED, and named TWO corrections that ship UNGATED). The five below are the prior stages, newest first.)*

- **prior active-phase:** `phase 83 (wasm-pause-arm-leak) PLAN done` (2026-08-03) — lifecycle-state **2 -> 3**; **ROW 83 STAYS `in-progress`**, `ROADMAP.md`, `DECISIONS.md` and `BEHAVIOR_CONTRACT.md` **BYTE-UNTOUCHED**, sentinel `want` stays **115**, strict PROPOSED guard stays **1**. Base master **`e4563a49`** (from `git rev-parse master`), branch `phase-83-plan`. Docs-only: ZERO production `.go`, ZERO test `.go`. **FIVE investigation agents** in DETACHED worktrees with private scratch and port bands inside `43800-44299` — and unlike a review stage, **every one WROTE REAL CODE AND RAN IT**, because the SPEC's ~1070-line test figure was analogue-scaled and analogue-scaling is what made phases 81 and 82 overrun by 3.07x and 2.55x. ⚠️ **THE HEADLINE: THE §6.1 LoC TRIGGER IS CROSSED ON MEASUREMENT — ~1840-2080 net against ~1500 — AND IT CROSSED A BAND THIS SPEC HAD ALREADY REVISED UP 2.4x IN ANTICIPATION OF EXACTLY THIS.** Measured: S1 test **679** vs 371 (1.83x) · S2 **453** vs 333 (1.36x) · S3 **237** vs 195 (1.22x) · S4 re-inverted **280** vs 113 (2.48x) · S8 **197 + 14 in-place** vs 60 (3.3x) · production **~140-230** vs 63-90. ⚠️ **The finding is not "the estimate was low" but "the estimate was UNDER-ENUMERATED"** — the SPEC's §13 S1 row prices seven items while its own §7 mandates three more, measuring **190 lines / 28% of S1's test side priced at ZERO**, and S8's 3.3x is driven by **14 pre-existing assertions no estimate counted**. **DISPOSITION: RECORD, DO NOT RETRO-SPLIT** — the scope items are coupled by measurement (S5/S6 and the `pause.go` half of S1/S2/S3 **do not apply independently**), so every available split axis cuts through a correctness constraint. ⚠️ **SECOND: THE SPEC'S 299-vs-1 A/B IS PACER-DEPENDENT AND TWO AGENTS MEASURED OPPOSITE RESULTS FOR THE SAME CELL** (299 under `time.Sleep` at pace == watchdog == 50 us, **1** under a spin pacer at the same real spacing, **A=291/B=298 — green on both arms** at 0.2x); `time.Sleep` has a ~1 ms floor on this host. **NO NUMBER IS CARRIED.** ⚠️ **THIRD: S3 HAS NO LIVE DEFECT AND NO RED ANCHOR OF ITS OWN** — SPEC §1.2's "reachable today" is refuted, so **"BEFORE or WITH" collapses to WITH**. ⚠️ **FOURTH: THE SPEC'S §7 SNIPPET HAS A HOLE — `stopPauseTimer` does not bump the generation (400/400 late fires; 0/400 with the bump), and plugging it in 6 lines IS the stale-token filter-local fix.** **NINETEEN refutations, EIGHT load-bearing**, plus **TWO new broken-gate shapes (27, 28)**.

- **prior active-phase:** `phase 83 (wasm-pause-arm-leak) SPEC done` (2026-08-03) — lifecycle-state **1 -> 2**; **ROW 83 STAYS `in-progress`**, `ROADMAP.md` and `BEHAVIOR_CONTRACT.md` **BYTE-UNTOUCHED**, sentinel `want` stays **115**. Base master **`5d0892e4`** (from `git rev-parse master` — ⚠️ **not the `8a0126d2` the router's prose still names**), branch `phase-83-spec`. Docs-only: ZERO production `.go`, ZERO test `.go`. **FIVE investigation agents** on disjoint remits in DETACHED worktrees with private scratch and port bands inside `43400-43799`; every load-bearing claim controller-re-derived — **one agent claim did NOT survive**, and ⚠️ **one CONTROLLER hypothesis did not survive an agent's measurement** (SPEC §2.2). ⚠️ **THE HEADLINE: THE ROW'S DECLARED PRINCIPAL RISK MEASURES ZERO, BECAUSE NO GUEST IN THIS REPOSITORY REACHES THE BROKEN ARMS AT ALL — AND THE FIX AS CHARTERED IS INCORRECT.** Instrumenting `body.go:226` across five full `0036` runs yields **ZERO fires** with two firing positive controls; `0036` with the fix runs **19.25 s** against a 19.33-20.36 s baseline inside a 1.03 s variance, and a **10 s -> 1 ms** watchdog sweep passes at every value. Three measured reasons: (n) trips the cap **82 lines before** the arm; (a)/(b)/(c) Pause only on non-final chunks; and ⚠️ **they are not driven at all** (`driver.go:511-513` emits a constant skip token, so the fixture produces **one** body chunk). ⚠️ **This REFUTES the BRAINSTORM's load-bearing *"three vendored guests already do it"* — true of source, false of execution** ⇒ **the failing-first anchor must be SYNTHETIC and the differential is STRUCTURALLY BLIND to this row.** ⚠️ **AND `Stop()`-before-reassign, the BRAINSTORM's entire S3, is INSUFFICIENT:** over 300 superseded generations `Stop()` alone fires **299** spurious resumes vs **1** with a generation counter, and **`-race` reports nothing** — it is a logical race. ⚠️ **A SEVENTH unbounded park** (`body.go:282`, encode-side cap, no `localReplyDone` counterpart) sits in the file the row edits. **TWENTY-THREE refutations, NINE load-bearing**, including **BROKEN-GATE SHAPE 26** — a liveness barrier placed UPSTREAM of the gate it claims to prove, installed one row earlier *as the fix* for that same failure.

- **prior active-phase:** `phase 83 (wasm-pause-arm-leak) BRAINSTORM done` (2026-08-03) — lifecycle-state **DONE -> 1**; **ROW 83 REGISTERED `in-progress`** and the sentinel `want` bumped **114 -> 115** in the SAME commit. Base master **`8a0126d2`** (from `git rev-parse master`), branch `phase-83-brainstorm`. Docs-only: ZERO production `.go`, ZERO test `.go`. **SELF-PICKED** per the 2026-07-12 standing directive — no banked mid-lifecycle work existed at this tip. **FIVE investigation agents** on disjoint remits, each in its own DETACHED worktree with private scratch and a private port band inside `43000-43399`; every load-bearing claim controller-re-derived, and **two agent claims did NOT survive that re-derivation**. ⚠️ **THE HEADLINE: THE ROW THE PREVIOUS PHASE LANDED TO STOP A LEAK STOPPED IT IN TWO OF SIX ARMS, AND THE EVIDENCE THAT THE OTHER FOUR WERE SAFE WAS FALSIFIED BY THE COMMIT THAT WROTE IT.** Phase 82's S9 armed the watchdog and set the resume flag in the two HEADERS arms only; the four remaining `abi.ProxyActionPause` arms return statuses that **PARK** the goroutine (`chain.go:430/474/569/635`) while arming nothing — the S9 leak verbatim. Proven by execution: a synthetic Pause-from-body guest stayed parked **1 s past a 250 ms watchdog window**, `ContinueStream` returned `WasmResultOk` while resuming nothing, and the stream released only after the flag was hand-set. ⚠️ **The recorded mitigation *"ZERO guest crates call any resume hostcall"* is FALSE — the call was added by `fb93845f`, the phase-82 IMPL commit that wrote the claim** — and false a second way: reaching the leak needs only a **Pause**, which THREE vendored guests already do. ⚠️ **CONTROLLER-LEVEL SYNTHESIS NEITHER AGENT REPORTED ALONE: the LIVE blast radius is TWO arms on THREE protocol paths** — `Run*Trailers` has **ZERO** non-test call sites while `Run*Data` has **EIGHT** across `connection.go` (H1), `h2dispatch.go` (H2) and `h3dispatch.go` (H3). The trailers arms are LATENT and are fixed here anyway, because a measured in-tree prototype would activate them. **TWENTY-ONE refutations, EIGHT load-bearing**, including **BROKEN-GATE SHAPE 25** (a gate whose own docstring claims it is behavioral when its body is a tautology over package constants) and ⚠️ **the REFUTATION BY WORKING PROTOTYPE of the gRPC family's inherited HARD-BLOCKED verdict**.

- **prior active-phase:** `phase 82 (wasm-http-call-response-cache) IMPL done` (2026-08-03) — lifecycle-state **3 -> DONE**; **ROW 82 FLIPPED `in-progress` -> `done`; THE PHASE WAS CLOSED.** Base master **`c0be319b`**, branch `phase-82-impl`; sentinel `want` STAYED **114** (the IMPL updates `status` IN PLACE per §Schema `:18`, so the `ROADMAP.md` diff is exactly one line). **SIX agents**, each in its own worktree with private scratch and disjoint port bands inside `42400-42799`: three ran the sequential core in ONE worktree (the T1-T4-before-T5 order is a CORRECTNESS constraint — the vendored Rust SDK's `get_map` has no `NotFound` arm, so honoring Pause against an unwired cache converts a benign wrong header into a guest TRAP plus a poison cascade), two ran independent Rust/fixture streams in parallel DETACHED worktrees and **cherry-picked in clean, zero conflicts**, and one ran the break roster and **committed nothing**. ⚠️ **THE HEADLINE: THE COST CROSSED §6.1 AND IS RECORDED AS A FINDING, NOT RETRO-SPLIT — realized `+2675` net `.go` against a ~1050 budget, 2.55x, and across `:290`'s ~1500 trigger**, with the test scaffolding dominating exactly as the PLAN warned; **a MEASURED PROTOTYPE IS A LOWER BOUND, and this was the SECOND CONSECUTIVE ROW to demonstrate it.** **SEVENTEEN refutations, SIX load-bearing**, and **TWO GATE-COVERAGE FINDINGS that matter more than the greens**: removing the `:status` synthesis left **all three stats legs GREEN** and was caught by the cross-side byte comparison ALONE (**a counter proving a callback RAN cannot prove it returned the RIGHT ANSWER**), and the enum swap is caught by a real guest blob as well as by numeric literals, so the true discriminator is *the wire value originates outside the Go declaration*. Gates: full differential **120/120 in 388.961 s**, h2spec **53/53/0/0** (⚠️ ONE report, no reference arm — the PLAN's "106" does NOT reproduce), stat surface **+0** asserted two independent ways. ⚠️ **It named THREE deferrals of its own, and the phase-83 BRAINSTORM refuted the safety argument for the first of them.**

- **prior active-phase:** `phase 82 (wasm-http-call-response-cache) PLAN done` (2026-08-02) — lifecycle-state **2 -> 3**; **ROW 82 STAYED `in-progress`**, `ROADMAP.md`, `DECISIONS.md` and `BEHAVIOR_CONTRACT.md` **BYTE-UNTOUCHED**, sentinel `want` stayed **114**. Base master **`71fc86d7`**, branch `phase-82-plan`. Docs-only: ZERO production `.go`, ZERO test `.go`. FIVE investigation agents in DETACHED worktrees; **FOURTEEN refutations, SIX load-bearing.** ⚠️ **THE HEADLINE: THE SPEC'S DOMINANT COST ITEM WAS OVER-ESTIMATED ~15x, SO THE SPLIT AXIS DISSOLVED** — S1 was priced at 300-600 net and **MEASURED 30**, because **FOUR of the SIX** `ProxyActionPause` arms **already honored Pause**; only the two HEADERS arms were deferred, so *"stream control deferred since 25.2"* was FALSE as a blanket claim. Also: **S1 alone HANGS AND LEAKS** a connection + goroutine per request (NEW scope item S9, named by no prior document); **the SPEC's own replacement gate goes GREEN ON A TRAP** because the success counter fired before the guest call; **S5 silently opens a guest WRITE surface** (seven consumers of the map-type flag, four of them mutators); **the guest blob is byte-reproducible ONLY under rustc 1.94.0 + `log 0.4.30` and NEITHER pin was recorded**; and **the accessor fork had FOUR options, not two, with the SPEC's preferred one DOMINATED** (`ABICallbacks` is **21** methods, not 20 — the "20" traced to a landed WRONG comment). **The phase-82 IMPL confirmed the S1 census and the ordering constraint by execution, and refuted the h2spec baseline, the nolint claim, the blob-grep claim and the cap-denied figure.**



## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
