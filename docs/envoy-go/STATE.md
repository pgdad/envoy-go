# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `24-http-filter-global-ratelimit` — **SPEC done 2026-05-22 at this commit; awaiting PLAN** (next session, skill `superpowers:writing-plans` to author `PLAN.md`). Phase 24 is a NEW top-level §9 family-row (`envoy.filters.http.ratelimit`, the GLOBAL rate-limit filter), the SEVENTEENTH §9 HTTP-filters-family row. Phase `23-http-filter-admission-control` DONE and CANONICAL — DO NOT reopen. **Phase-24 SPEC outcome:** the §10 D1–D7 empirical pins resolved IN-SESSION against reference Envoy v1.37.2 + go-control-plane v1.32.4 (parallel-subagent fan-out per ADR-0004); an **11-entry AMEND catalog** (SPEC §1.1) with **6 substantive REFUTATIONS** of the BRAINSTORM. Load-bearing refutations: **AMEND-1** stats anchor at the **CLUSTER** scope `cluster.<rls_cluster>.ratelimit[.<stat_prefix>].<stat>` (NOT `http.<HCM_prefix>.`), 4 counters `ok`/`error`/`over_limit`/`failure_mode_allowed`, no `cluster_not_found`; **AMEND-2** there are NO `filter_enabled`/`filter_enforced` proto fields (hardcoded runtime keys `ratelimit.http_filter_enabled`/`http_filter_enforcing` honored-as-static; the route-level `disable_key` is the PARSE-REJECT surface); **AMEND-4** `RateLimitPerRoute.override_option` is INERT (`[#not-implemented-hide:]`); embedded `rate_limits` is an unconditional early-return (Axis A); NEW per-route `domain` field; **AMEND-6** descriptor `hits_addend` is `UInt64Value` (not bare uint32); Unit enum non-monotonic (WEEK=7) ⇒ fake must encode by proto NUMBER; **AMEND-9** DELTA-2 is NOT a `RequestRouteConfig()` reuse (TPFC is keyed by filter NAME; `rate_limits` are first-class Route/VirtualHost fields, unparsed today) — it is a genuinely NEW chain-seeded `DecoderFilterCallbacks` accessor pair plumbed like ADR-0165 (HIGH risk); **AMEND-10** the cluster-scoped surface is a novel cross-namespace write via `NewCounterIfAbsent` (the same pattern ext_authz's `charge_cluster_response_stats` DEFERRED) — phase 24 LANDS it. Stat count **110 → 114**. **LoC envelope ~1900–2700 (ABOVE the ADR-0045 ~1500 split-gate)** ⇒ split-readiness HIGH/CONFIRMED; per Q1 the SPEC is full-surface single-phase and the **PLAN author APPLIES the split-gate** (candidate axis 24.1 core decision path + DELTA-2 + PARSE-REJECTs / 24.2 remaining actions + X-RateLimit headers + RateLimitPerRoute + stage + vh-override, SPEC §16). Fixtures 33 → 35 (`0032-http-ratelimit` cross-side via a SHARED fake gRPC `RateLimitService` + `0033-http-ratelimit-boot-reject`, `domain`-empty); fuzzers 32 → 33 (`FuzzRateLimitConfigParse`); 19 HTTP filters wired post-phase-24. Cross-phase closure pickup: phase-11 local_ratelimit's descriptor-action / X-RateLimit+vh-policy / multi-stage deferral clusters LIFTED at phase 24. §9 family closes from 2 remaining rows to **1 remaining** (`wasm`) post-phase-24.
- **phase-directory:** `docs/envoy-go/phases/24-http-filter-global-ratelimit/` (BRAINSTORM.md + SPEC.md authored; PLAN.md + PROGRESS.md + REVIEW.md not yet authored). Phase 23 directory `docs/envoy-go/phases/23-http-filter-admission-control/` is complete (full IMPL lifecycle).
- **lifecycle-state:** `phase 24 SPEC done; awaiting PLAN` (SKILL_ROUTING state 2 → next is `superpowers:writing-plans` to author PLAN.md; the PLAN author APPLIES the ADR-0045 split-gate — LoC ~1900–2700 trips it; SPEC §16 candidate axis 24.1/24.2). ROADMAP row `24` STAYS `in-progress` (per-cell SPEC-done annotation added at this commit; stays `in-progress` until phase-24 IMPL phase-done — or becomes the parent row over 24.1/24.2 if the PLAN split fires). The §9 HTTP-filters family stands at **1 remaining row** post-phase-24 (`wasm`).
- **next-skill:** `superpowers:writing-plans` (to author `PLAN.md` per the phase-09..23 precedent; the PLAN author applies the ADR-0045 split-gate at lifecycle-state 2 → 3, SPEC §16).
- **last-commit:** `<<TBD-fill-post-spec-commit>>` (squash-merge of branch `phase-24-http-filter-global-ratelimit-spec` from worktree `.worktrees/phase-24-http-filter-global-ratelimit-spec/`; a SHA-fill follow-up backfills this placeholder per the phase-09..23 SPEC-stage close pattern). Predecessor master tip: `24a4dde` — `next-prompt.txt: repoint master-tip references to 53e14de (actual HEAD)`.
- **last-updated:** 2026-05-22
- **next-free ADR:** `ADR-0201` (4 NEW consumed at the phase-24 SPEC commit — ADR-0197 + ADR-0198 + ADR-0199 + ADR-0200 §Context drafts anchored at this SPEC commit per ADR-0044 §Context-draft discipline; §Decision + §Consequences bodies land at phase-24 IMPL). DECISIONS.md tail at `ADR-0200` (§Context-draft state; §Decision/§Consequences stubbed). ADR-0194 + ADR-0195 + ADR-0196 all CONSUMED (phase-23). **D-hypothesis:** ADR-0201 stays UNCONSUMED at phase-24 phase-done — HOLD-with-known-risk, LARGER surprise surface than phase-23 (DELTA-2 chain-seed + cross-namespace stat write + a PLAN-time split would re-map ADR consumption). Canonical-per-route roster STAYS 9 (the 9 → 10 amendment is ANCHORED in ADR-0199 §Context but LANDS at phase-24 IMPL).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
