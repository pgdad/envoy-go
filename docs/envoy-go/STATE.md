# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `23-http-filter-admission-control` **(PLAN done at this commit; lifecycle-state 3 → awaiting IMPL execution)**. Phase `22-http-filter-lua` parent + all three sub-phases DONE and CANONICAL at predecessor master tip `032510a` — DO NOT reopen. Phase 23 is a NEW top-level §9 family-row (`envoy.filters.http.admission_control`), the SIXTEENTH §9 HTTP-filters-family row. **BRAINSTORM done 2026-05-21** (squash-merge `3040a6b`); **SPEC done 2026-05-21** (squash-merge `a64ee71` + SHA-fill `ec68627`); **PLAN done 2026-05-22** at this commit via `superpowers:writing-plans` scoped to PLAN authoring. The PLAN decomposed `SPEC.md` into **12 bite-sized TDD tasks** at `docs/envoy-go/phases/23-http-filter-admission-control/PLAN.md`, ran the ADR-0045 split-gate check (12 tasks ≤ 25 AND ~1250-1800 LoC grand-total — natural split axes don't carve; **SINGLE-ROW landing CONFIRMED**), and passed the plan-document-reviewer subagent loop (**Approved on iteration 1**; all framework-grounding claims independently verified). The PLAN settles 10 planner-time decisions PD-1..PD-10, including **three SPEC-pseudocode-vs-reality gaps** surfaced by reading the real filter framework: **PD-1** the real factory signature `New(tc *anypb.Any, ctx FactoryCtx)` (NOT the SPEC §6 illustrative `New(message proto.Message)`); **PD-2.503** the framework `SendLocalReply` is 3-arg `(status, body, headers)` — the AMEND-7 `denied_by_admission_control` rc-details is NOT surfaceable through the API (ABSENT-by-API; byte-pin reduces to 503 + empty body + no headers); **PD-3** `DecoderFilterCallbacks` exposes NO `StreamInfo()`/`HealthCheck()` accessor — the AMEND-4 health-check gate arm is **NOT-MODELED** at MVP (decode-gate implements only `!enabled`; AMEND-11 health-check-not-recorded is vacuous; documented deferral + BEHAVIOR_CONTRACT note at IMPL Task 11; does NOT consume ADR-0196).
- **phase-directory:** `docs/envoy-go/phases/23-http-filter-admission-control/` (BRAINSTORM.md + SPEC.md + PLAN.md authored). PROGRESS.md + REVIEW.md to follow at the IMPL + verification/review lifecycle sessions per the phase-09..22 precedent.
- **lifecycle-state:** `phase 23 PLAN done; awaiting IMPL execution` (SKILL_ROUTING state 3). ROADMAP row `23` stays `in-progress` (status flips to `done` only at IMPL phase-done with all 6 gates GREEN). The §9 HTTP-filters family stands at 3 remaining rows (`wasm`, `admission_control`, `global rate limit`); it closes to **2 remaining (`wasm`, `global rate limit`) once phase-23 IMPL lands**.
- **next-skill:** `superpowers:subagent-driven-development` (the executor session per SKILL_ROUTING state 3 + ADR-0005 §Decision 4 default-executor preference + project memory `feedback_execution_style.md`). The IMPL session creates a fresh worktree per `feedback_git_worktrees.md` (branch `phase-23-http-filter-admission-control-impl` off this PLAN commit's master tip) and executes the 12 PLAN tasks task-by-task (TDD per `superpowers:test-driven-development` on every task; append to PROGRESS.md on each task completion). Task graph: Tasks 2+3 parallelize (then 7 once 2 lands); Tasks 4→5→6→8→9→10/11→12 are the critical path. ADR-0194 §Decision+§Consequences body lands at Task 4 (controller); ADR-0195 at Task 2 (compiled_config) per ADR-0044. Cold-start scope for the IMPL session: read STATE.md (this file) + the phase-23 PLAN.md (the authoritative task-by-task input — §Execution-preconditions + the 12 Tasks + the 10 PD decisions + the ADR-lands-in-Task table) + the phase-23 SPEC.md (the 11-AMEND implementation contract + §15 acceptance checklist) + DECISIONS.md tail (ADR-0194 + ADR-0195 §Context stubs) + the phase-21 PROGRESS.md/REVIEW.md (nearest single-filter IMPL precedent).
- **last-commit:** `af4a0fe` (this PLAN squash-merge of branch `phase-23-http-filter-admission-control-plan` from worktree `.worktrees/phase-23-http-filter-admission-control-plan/`; this placeholder was backfilled by this SHA-fill follow-up commit per the phase-09..22 PLAN squash-merge convention). Predecessor master tip: `9d6e876` — `next-prompt.txt: advance to post-phase-23-SPEC PLAN cold-start` (atop the SPEC SHA-fill `ec68627`).
- **last-updated:** 2026-05-22
- **next-free ADR:** `ADR-0196` — UNCHANGED at this PLAN commit (NO ADR numbers consumed at PLAN authoring; ADR-0194 + ADR-0195 §Decision+§Consequences bodies LAND at IMPL per ADR-0044, NOT at PLAN). **ZERO in-place §Decision AMENDMENTs + ZERO ADR-0125 amendments** at phase 23 (REUSE-by-absence per-route; FIRST ADR-0125-skip since phase-22; canonical-per-route roster STAYS 9). **ADR-0196 hypothesized UNCONSUMED at phase-23 phase-done** (D-style HOLD-with-known-risk per SPEC §10: a surprise upstream stat name / a 4th counter / a clamp-boundary float edge / a §Decision-level health-check rationale at IMPL could force consumption; the PD-3 health-check NOT-MODELED disposition is a deferral, NOT a §Decision-level ADR, so it does not pre-consume ADR-0196). DECISIONS.md tail at `ADR-0195` (§Context stub); ADR-0194 + ADR-0195 §Context drafts landed; ADR-0193 full body + ADR-0125 §(xiv) amended remain UNCHANGED.

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
