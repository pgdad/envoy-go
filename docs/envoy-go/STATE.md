# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

> **This file is a POINTER, not an archive** — `BOOTSTRAP_PROMPT.md` §4: *"STATE.md — pointer: active phase, last commit, next action."* It carries the live pointer, the live counts, and a SHORT recent lineage. **Full stage-by-stage history lives in `STATE_HISTORY.md`** (archived at ADR-0288; **nothing was deleted** — the pre-move file is preserved there verbatim, byte-for-byte).
>
> **⚠️ THE RULE ADR-0288 EXISTS TO ENFORCE: there is EXACTLY ONE live `next-skill:`, `lifecycle-state:`, `next-free ADR:`, and count block IN THIS FILE, and they live in §Current pointer directly below.** Before that move, ~330 lines of prepended backlog had buried TWO fossilised "current state" blocks whose `next-skill:` still read *"the 42.2 hedging BRAINSTORM"* and *"the 46.2 SPEC"* while the live phase was **66** — so `grep 'next-skill:' STATE.md` returned **two stale hits and ZERO live ones**, and `grep 'fuzzers:'` returned **48** and **42** when the true count was **55**. **A session may trust a grep of this file again. Keep it that way: when you advance the pointer, EDIT §Current pointer IN PLACE — never prepend a new block above it.**

---

## Current pointer

*(The ONLY live "what next" in this repo. Update IN PLACE at every stage close.)*

- **active-phase:** `phase 72 (tracing-custom-tags-metadata-host) IMPL done` (2026-07-23) — **row 72 `done`**
- **phase-directory:** `docs/envoy-go/phases/72-tracing-custom-tags-metadata-host/` (`BRAINSTORM.md` + `SPEC.md` + `PLAN.md` + `PROGRESS.md` present, the PROGRESS ledger fully populated T1-T8; **row 72 is `done`** — flipped `in-progress` → `done` at the phase-72 IMPL six-gate per ADR-0106, the SOLE leg, a SINGLE FLAT ROW)
- **lifecycle-state:** `DONE` (the phase-72 IMPL landed; **row 72 `done`**; the four-stage lifecycle BRAINSTORM → SPEC → PLAN → IMPL is complete for phase 72 — the next session opens a NEW phase at its BRAINSTORM). Subject: the tracing `custom_tags` `metadata` type for the **`HOST`** MetadataKind — the EIGHTEENTH Observability-family row, the SIXTH tracing `custom_tags` capability and the THIRD of the four `metadata` MetadataKinds (after `REQUEST`@70, `ROUTE`@71) — **LANDED**. It LIFTED the `tracing: custom_tags metadata tag %q host kind unsupported` reject (`internal/tracing/config.go:267-268`; `grep -rn "host kind unsupported"` ⇒ 0 hits) and resolves the SELECTED UPSTREAM ENDPOINT's metadata out of `lb_endpoints[].metadata.filter_metadata[ns]` — over a NEW unexported `Endpoint.filterMetadata` retention field ALIASING the parsed proto map + an exported nil-safe `MetaLookup(ns)` accessor on `cluster.Endpoint`, populated at `manager.go:884` BESIDE the byte-unchanged phase-38 `envoy.lb` scalar projection — walking the `MetadataKey.path` FULLY (NOT the REQUEST arm's `[1:]` slice), serialized to a STRING by the phase-70 `descend`/`structpbValueToString` reused VERBATIM, emitted as a `{tag,value}` span attribute via the shared `ResolveCustomTags`→`Span.Attrs []KV` seam on BOTH exporters. `CLUSTER` + both unset-kind arms still PARSE-REJECT loudly (envoy-go-strict, ADR-0080). **The row SKIPPED the 18-caller threading tax phases 70/71 each paid** — `picked cluster.Endpoint` was already the 4th param of all three `emitAccessLog*` helpers, so the closure is built LOCALLY at the three `ResolveCustomTags` call sites (`accesslog_emit.go:57`/`:118`/`:179`) and **the three emit signatures + all 18 emit callers + the 29 existing emit test callers + `chain.go` + `subset.go` are BYTE-UNTOUCHED, confirmed by sha256 at the envelope audit.** SINGLE FLAT ROW (NINE tasks, T1-T9; ADR-0045 valve armable-but-unconsumed); ONE new OTLP fixture `0116-tracing-custom-tags-metadata-host` (fixtures **117 → 118**), which answered the row's genuinely new cross-side question — **the REFERENCE resolves an ARBITRARY (non-`envoy.lb`) `filter_metadata` namespace for a HOST-kind tag** (12 reference spans carrying the endpoint value, not the default). ONE named departure (BEHAVIOR_CONTRACT B2), scoped EXACTLY: the reference resolves a HOST tag at LB-PICK time and emits the picked endpoint's metadata even when the upstream connect then fails, while envoy-go carries the ZERO `Endpoint` on every upstream-**ACQUIRE** failure sub-path and falls to `default_value`/omit — EXECUTED-proven for the connect-failure class ONLY (pool-overflow + circuit-breaker UNPROBED on the reference); ⚠️ **post-ACQUIRE failures (the write / read-response 502s) carry the REAL endpoint and resolve normally: PARITY, not departure** (an over-broad draft claim caught and RETRACTED in review), and the 404-no-match arm (no span at all) + the zero-endpoint arm (boot-rejected) have NO envoy-go counterpart and are NOT parity. PRE-EXISTING; deliberately scoped out as a future row.
- **next-skill:** **the phase-73 BRAINSTORM** — the roller **SELF-PICKS** the next subject (the 2026-07-12 standing autonomous directive; no human pick), smallest defensible ROW-SIZED candidate first, recording the pick AND the fully-adjudicated rejected roster in the BRAINSTORM. **RE-DERIVE every candidate's cost at the CURRENT tip — do NOT trust the adjectives below** (`reference_deferred_candidate_cost_restale`; phase 72 itself found `HOST` had been mis-costed MODERATE by BOTH predecessor BRAINSTORMs). **The candidate pool:** Observability (family OPEN) — **`CLUSTER` metadata**, the LAST unconsumed `metadata` MetadataKind, which CLOSES the quartet but **RE-PAYS the 18-caller thread** phase 72 skipped (no metadata field on `cluster.Cluster`; `router.go` publishes only `Picked() cluster.Endpoint`) · **the pick-time `picked`-propagation row this row's own departure named** (small — `ep` is already in scope at the acquire-failure returns — but it flips three access-log consumers, breaks landed unit tests, and NO differential fixture pins it; probe the reference's pool-overflow and circuit-breaker arms first, which phase 72 left UNPROBED) · **`access_log[].filter`** (a LIVE behavioural divergence — a KNOWN proto field the strict `DiscardUnknown:false` pass ACCEPTS with ZERO production `GetFilter()` consumers) · `stats_flush_on_admin` · `spawn_upstream_span` / OTel `http_service` / `sampler` / `resource_detectors` / tracing `verbose` / force-trace · the `ssl` handshake-outcome stat family (ADR-0286 C3 framework surgery) · xDS (family OPEN) — upstream SDS · CDS/EDS · LDS/RDS · ADS · Delta · `google_grpc` · HTTP/3 (family OPEN) · the never-opened `gRPC` and `Runtime` families (and the `WASM` ROADMAP **bookkeeping artifact** — `internal/wasm/` is substantially BUILT, so that sentinel line is a docs correction, NOT a work row). Carry forward the two live hazards: the PRE-EXISTING `internal/cluster` `-race` flake (`TestOutlierDetector_ConcurrentEjectExactlyOnce`, `outlier_test.go:766` — isolate-re-run, do NOT re-classify) and the cross-side constraint that tag-value assertions use **STRING values only** (`reference_structpb_tag_cross_side_string_only`). Fresh worktree off master (`feedback_git_worktrees`); subagent-driven (`feedback_execution_style`); subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`).
- **last-commit:** the phase-72 IMPL squash (`git log --grep 'phase 72'`; locate by SUBJECT, never by position — `reference_next_prompt_tracked_despite_gitignore`). Prior: the phase-72 PLAN squash.
- **last-updated:** 2026-07-23 (phase-72 IMPL done — the NINE-task spine T1-T9 landed, each red-first → green → break-verified → adversarially reviewed, every task RE-DERIVING its anchors at the IMPL tip. **ROW 72 FLIPS `in-progress` → `done`** at the six-gate (ADR-0106, the SOLE leg); ADR-0294 COMPLETED IN PLACE (tail stays ADR-0294, next-free ADR-0295); the live Observability `candidates:` sentence NARROWED `CLUSTER`/`HOST` → `CLUSTER`-only. **Six-gate ALL GREEN** — gofmt SILENT / vet 0 / build 0 / `go mod tidy -diff` EMPTY / golangci-lint 0 / the FULL **118-dir** differential `ok 406.088s` EXIT 0 / the layering check EMPTY (**a DISCIPLINE check, not a cycle check**) / `-race` green on `internal/cluster` (NEWLY load-bearing), `internal/tracing`, `internal/filter/hcm` / a seven-section envelope audit with **ZERO findings**. Adversarial review found real defects at EVERY task, all fixed — most consequentially the RETRACTION narrowing the departure to upstream-ACQUIRE failures only. STATE §Current rolled IN PLACE [lifecycle 3 -> DONE; lineage re-capped at five WITH its preamble updated]; next-prompt.txt rolled to the phase-73 BRAINSTORM. Counts re-run MECHANICALLY in the worktree: fixtures **117 → 118**, everything else +0.)
- **next-free ADR:** **`ADR-0295`** — DECISIONS.md tail is **ADR-0294** (STATUS **COMPLETE** — §Context drafted at the phase-72 SPEC per ADR-0044; §Decision + §Consequences APPENDED IN PLACE at the phase-72 IMPL, no renumber, no second `### Decision` heading). `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1` → `## ADR-0294`; `grep -c '^## ADR-0295'` → 0.

---

## Project counts at this commit

*(Live. Re-verify at session start — `git fetch` first — and update here at every stage close. Canonical commands inline.)*

- **active differential fixtures:** **118** — `ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0116-tracing-custom-tags-metadata-host` (added at phase 72 — the OTLP HOST-metadata-custom_tags fixture, a static `lb_endpoints[0].metadata.filter_metadata` block in an ARBITRARY non-`envoy.lb` namespace + two HOST custom_tags cross-side EXACT key+value, STRING values only, NO writer).
- **fuzzers:** **55** — `grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`. Phase 72 added a PURELY ADDITIVE `meta_host_ok` `withMetaTags` seed (`meta_bad` STAYS on CLUSTER) ⇒ **+0** (an `f.Add` seed is not a fuzzer — `reference_fuzzer_count_docs_drift`).
- **stat surface:** **1201** — BEHAVIOR_CONTRACT doc count. **There is NO mechanical counting command.** Phase 72 was **+0** (a span attribute registers no stat; tracing has no per-tag stat surface, so NO `TestNoNewStat` guard is owed — and none was manufactured).
- **BackendKind tail:** **38** (`H2GoawayResponder`). Phase 72 was **+0** (`0116` reuses `HTTPFixedBody`; the `otlptrace` receiver is driver-owned; NO writer).
- **DECISIONS.md tail:** **ADR-0294** (next-free **ADR-0295**) — `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1`. **ADR-0294 is COMPLETE** — §Context drafted at the phase-72 SPEC per ADR-0044 (the tail flipped ADR-0293 → ADR-0294 AT that commit), §Decision + §Consequences APPENDED IN PLACE at the phase-72 IMPL, no renumber and no new ADR. ADR-0293 and ADR-0292 stay COMPLETE.
- **go.mod modules:** the tracked lineage figure is **2** (the phase-61.2 count: `quic-go` direct + `qpack` indirect) — **NOT a repo total**; the single `go.mod` requires **67** modules. Phase 72 was **+0** (`go mod tidy -diff` EMPTY; `go.mod`/`go.sum` byte-unchanged vs master — the one new import, `structpb` into `internal/cluster/cluster.go`, resolves at an existing direct).
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` (see `ENVOY_TARGET.md`).
- **conformance:** h2spec 53/53; proxy-wasm all 10 families PASS.

---

## Recent lineage

*(The last FIVE stages only — enough for the router's "prior-phase lineage" read. Everything earlier is in `STATE_HISTORY.md`. These bullets are carried over VERBATIM; the newest mirrors §Current (**the phase-72 IMPL done** — the stage that flipped row 72 `done` and closed the four-stage lifecycle), and the oldest (**phase-71 PLAN done**) was dropped as this list re-capped at five. The five now read: phase-72 IMPL · phase-72 PLAN · phase-72 SPEC · phase-72 BRAINSTORM · phase-71 IMPL.)*

- **prior active-phase:** `phase 72 (tracing-custom-tags-metadata-host) IMPL done` (2026-07-23) — lifecycle-state 3 → DONE; **ROW 72 FLIPS `in-progress` → `done`** at this IMPL six-gate (ADR-0106, the SOLE leg — a SINGLE FLAT ROW, `reference_roadmap_split_phase_row_done`); ADR-0294 COMPLETED IN PLACE. Fresh worktree `.worktrees/phase-72-impl` off master `b1e467e8` (the phase-72 PLAN squash), branch `phase-72-tracing-custom-tags-metadata-host-impl`; subagent-driven (`feedback_execution_style`) — one implementer per task + a per-task adversarial reviewer, each committing LOCALLY only (`feedback_subagents_no_push`), controller squash-push at close (`feedback_push_to_origin`). Each task RE-DERIVED its anchors at the IMPL tip (a PLAN is not evidence either). **The 9-task TDD spine landed** (each red-first → green → break-verified → adversarially reviewed): **T1** (`74a1d4bb`+`bbe1ab50`) `internal/cluster` — an unexported `Endpoint.filterMetadata map[string]*structpb.Struct` field ALIASING the parsed `lbe.GetMetadata().GetFilterMetadata()` + an exported nil-safe `MetaLookup(ns)` accessor, populated at `manager.go:884` BESIDE the byte-unchanged phase-38 `envoy.lb` scalar projection (`:883`/`:754`/`subset.go` byte-identical AND unshifted; ALIASING proved mechanically; sizeof 104 → 112 B; ONE new import; breaks A/B/C/D fired, C asserting on the ACCESSOR'S OWN return per F5b) · **T2** (`2aa98e45`+`afdee55f`) `config.go` — `kindMetadataHost` APPENDED at **iota==5** (confirmed BY EXECUTION; the pre-known red was EXACTLY TWO `/host` subtests printing `Kind:5`) + a HOST accept arm that is a verified byte-clone of the ROUTE arm (one differing line) replacing the `:267-268` reject, ZERO new imports, the nine stale comments narrowed, `grep -rn "host kind unsupported"` ⇒ **0 hits**, `CLUSTER` + BOTH unset arms verified STILL rejecting; breaks E/F/G/H fired · **T3** (`f2c73ad3`+`fc55ea2f`) `resolve.go` — the nil-tolerant one-arg `hostMetaLookup` **5th** param + a `kindMetadataHost` arm descending the **FULL** `MetaPath` (NOT `[1:]`), `descend`/`structpbValueToString` reused VERBATIM (reuse proven by sha256), 32 `resolve_test.go` callers += trailing `nil` (raw grep 33; `:106` is a string literal); break I (the crux) fired FOUR HOST test funcs while EVERY REQUEST and ROUTE subtest stayed GREEN · **T4** (`ef77640c`) **the row's distinguishing task** — `picked.MetaLookup` appended at `accesslog_emit.go:57`/`:118`/`:179` and NOWHERE else. **The 18-caller threading tax phases 70/71 each paid is SKIPPED** (`picked cluster.Endpoint` was already the 4th emit param), so **the three emit SIGNATURES + all 18 emit callers + the 29 existing emit test callers + `chain.go` + `subset.go` are BYTE-UNTOUCHED, CONFIRMED BY SHA256**; live per-site H1/H2/H3 span tests + a first-class zero-`picked` test (the **5** span-capable zero sites, not the raw 11-of-18); breaks L/M fired per-site · **T5** (`fc16d396`+`0a94cf28`) a PURELY ADDITIVE `meta_host_ok` fuzz seed (**`meta_bad` STAYS on CLUSTER**) + the stale `fuzz_test.go:92-93` comment narrow with the adjacent dispatch-order sentence byte-unchanged; **dispatch-verified TWICE by two agents** (+0 fuzzers) · **T6** (`a1d99561`+`b35d6a22`) the OTLP fixture `0116-tracing-custom-tags-metadata-host` on the `0115` chassis with the metadata block RELOCATED onto `lb_endpoints[0].metadata` in BOTH YAMLs, STRING values only, NO writer — **it ANSWERED the row's genuinely new cross-side question: the REFERENCE resolves an ARBITRARY (non-`envoy.lb`) `filter_metadata` namespace for a HOST-kind tag**, all **12 reference spans** carrying the endpoint value rather than the default; breaks N/O/P/Q ALL fired, **P the anti-vacuity control** and O discriminating OMIT from empty-string; fixtures 117 → 118 · **T7** (`ea41e55c`+`efd726b6`) BEHAVIOR_CONTRACT B1-B4 · **T8** the six-gate · **T9** ADR-0294 IN PLACE + row-72-`done` + the deferred-sentence narrow + this roll. **Six-gate ALL GREEN:** gofmt SILENT / vet 0 / build 0 / `go mod tidy -diff` EMPTY + `go.mod`/`go.sum` NO DIFF vs master (+0 modules) / golangci-lint 0 / **the FULL 118-dir differential `ok 406.088s` EXIT 0, ZERO failures** / the layering check EMPTY — ⚠️ **a DISCIPLINE check, NOT a cycle check** (`internal/cluster` carries no `internal/tracing` edge either, so a direct import would build clean; the refuted "cycle guard" justification was CORRECTED in ADR-0294 §Context at T9) / `-race` green on `internal/cluster` (**NEWLY load-bearing — the FIRST row in this lineage to touch it**), `internal/tracing`, `internal/filter/hcm` / **a seven-section envelope audit with ZERO findings** (19 files all classified; the `internal/cluster` SHAPE pin holding; the cost claim confirmed by hash; **exactly TWO new exported symbols** by AST symbol-set diff). **The NAMED departure (B2), scoped EXACTLY:** the reference resolves a HOST tag at LB-PICK time and emits the picked endpoint's metadata even when the upstream connect then fails, while envoy-go carries the ZERO `Endpoint` on every upstream-**ACQUIRE** failure sub-path and falls to `default_value`/omit — EXECUTED-proven for the connect-failure class ONLY (pool-overflow + circuit-breaker UNPROBED on the reference). ⚠️ **Post-ACQUIRE failures (the write / read-response 502s) carry the REAL endpoint and resolve normally: PARITY, not departure** — an over-broad claim CAUGHT AND RETRACTED in review; the 404-no-match arm (no span at all) and the zero-endpoint arm (boot-rejected) have NO envoy-go counterpart and are NOT written as parity. PRE-EXISTING; scoped out as a future row. **Adversarial review found real defects at EVERY task**, all fixed — most consequentially that retraction, plus a doc comment conflating the nil-lookup mechanism with the ZERO-`Endpoint` mechanism (a method value on the zero `Endpoint` is NON-nil) and a duplicate test deleted as scope creep. **Exit counts (re-run MECHANICALLY):** fixtures **117 → 118** (tail `0116-tracing-custom-tags-metadata-host`) / fuzzers **55 (+0)** / stat surface **1201 (+0)** / BackendKind **38 (+0)** / go.mod modules **2 (+0)** / ZERO new packages / DECISIONS tail **ADR-0294** COMPLETE (next-free **ADR-0295**). **The Observability family STAYS OPEN** — the `HOST` MetadataKind rolled OUT of the live `candidates:` sentence at this row-done edit (phase-57 precedent), leaving **`CLUSTER` as the LAST unconsumed `metadata` MetadataKind**; three of the four (`REQUEST`@70, `ROUTE`@71, `HOST`@72) are now consumed. **Sentinel re-run MECHANICALLY (does NOT fire; `stop` NOT created):** (1) prints **NOTHING** (row 72 `done` — every chartered row `done`); (2) `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:'` ⇒ **3**; (3) prints `NEVER OPENED: gRPC/Runtime/WASM`. Checks (2)+(3) print ⇒ abundant work remains. **Next → the phase-73 BRAINSTORM** (the roller SELF-PICKS per the 2026-07-12 standing directive).

- **prior active-phase:** `phase 72 (tracing-custom-tags-metadata-host) PLAN done` (2026-07-23) — lifecycle-state 2 -> 3; docs-only (`PLAN.md` + `PROGRESS.md`, the ONLY two files in the squash's phase-directory delta — ZERO production `.go`, DECISIONS/ROADMAP UNTOUCHED); fresh worktree `.worktrees/phase-72-plan` off master `2a82cc7b` (== the phase-72 SPEC squash; **NO parallel stream** — `git diff --stat 0150b04c 2a82cc7b -- '*.go' go.mod go.sum test/` is EMPTY, so the production tree is byte-identical to what the SPEC re-derived), branch `phase-72-tracing-custom-tags-metadata-host-plan`, per `feedback_git_worktrees`; subagent-driven — TWO independent read-only re-derivation agents on disjoint remits re-opened every SPEC §3/§9/§11 anchor at tip. **RESULT: 68 of 69 anchors EXACT-HOLD; ONE MINOR drift (RD-LIT); ZERO SPEC claims refuted; FIVE NEW FINDINGS.** The findings matter more than the drift: **F1/F2 — THREE UNNAMED EDIT-SITE OBLIGATIONS** (two stale `config_test.go` doc comments at `:627-629`/`:754-757` and **seven stale production comment lines in four groups** in `config.go`, notably `:182` `// metadata CLUSTER/HOST/unset kinds reject LOUDLY`, all going FALSE the moment the accept lands — the class SPEC §7 pins for `fuzz_test.go` but misses elsewhere); **F4** — the landed **ADR-0294 §Context still carries the "(the cycle guard)" justification its own SPEC §16/M3 REFUTED** (re-executed: `internal/cluster` does not depend on `internal/tracing`, so a direct import would build clean — it is a self-imposed LAYERING rule), forwarded as a T9 obligation; **F5** — the SPEC's quoted HOST skeleton is **not gofmt-clean**, and `structpb.NewStructValue(nil)`'s non-nil return, while real, is **NOT observably load-bearing through `ResolveCustomTags`** (`descend`'s first hop returns `(nil,false)` without panicking and `MetaPath` is min-1 by parse validation) ⇒ T1's guard break must assert on the ACCESSOR'S OWN RETURN or it is VACUOUS. **The spine:** T1 the `Endpoint.filterMetadata` field + nil-safe `MetaLookup` + the `manager.go:884` populate (`:883`/`:754`/`subset.go` byte-unchanged; ONE new import) · T2 `kindMetadataHost` at **iota==5** + the HOST accept arm replacing the `:267-268` reject + the 9 stale comments · T3 the `hostMetaLookup` 5th param + the FULL-`MetaPath` resolve arm + 32 trailing-`nil` callers · T4 `picked.MetaLookup` at the THREE `ResolveCustomTags` call sites ONLY · T5 the purely-ADDITIVE `meta_host_ok` seed · T6 the `0116` OTLP fixture + breaks · T7 BC B1-B4 · T8 the six-gate + a SHAPE-pinned `internal/cluster` envelope audit · T9 ADR-0294 IN PLACE + row-done + the sentence-narrow + STATE/router. **ADVERSARIALLY VERIFIED before landing by TWO independent verifiers in PRIVATE scratch, and BOTH FOUND REAL DEFECTS — ZERO SEVERE, SEVEN MODERATE, TWELVE MINOR, all folded.** **V1 (code-claims + by-execution) BUILT the row from the PLAN's own skeletons** — `go build`/`vet` 0, `go mod tidy -diff` EMPTY, the layering check EMPTY, the predicted red set EXACTLY TWO (printing `Kind:5`), and **sha256 proof that the 18 emit callers, `chain.go`, `subset.go` and `accesslog_emit_test.go` are byte-identical to baseline ⇒ THE CENTRAL COST CLAIM CONFIRMED BY HASH** — while catching THREE MODERATE: **(M1) the PLAN's OWN RD-FUZZ "drift correction" was ITSELF WRONG** (`CLUSTER/HOST` is on `fuzz_test.go:92`, so the SPEC's `:92-93` was right and the "corrected" `:93-94` holds no such token — an `old_string` built on it would have missed the stale claim and garbled the adjacent CORRECT sentence) ⇒ reverted, drift tally 2 -> 1; **(M2)** T4's entry state and red classification were both false — after T3 the package does NOT compile, the draft having conflated the EMIT-SIGNATURE arity (genuinely unchanged, the row's real distinction from 70/71) with the `ResolveCustomTags` arity (which changes exactly as in 70/71); **(M3)** the `MetaLookup` doc-comment skeleton FAILED `golangci-lint` (`analogue` -> `misspell`, `locale: US`), which would have broken T1 Step 6 and T8 gate 5 on a verbatim paste. **V2 (process/consistency/stage-close) caught FOUR MODERATE** — a missing `PROGRESS.md` cited five times as a record channel; **§1.4/§1.5 asserting the adversarial record in the PAST TENSE over an empty placeholder while self-certifying immunity to that exact defect** (the phase-69 cited-but-unwritten class recurring, now collapsed into ONE populated section); a sentinel cross-reference pointing at §Task 9 (the **IMPL**'s close, where check (1) goes SILENT) which exposed that **the PLAN listed none of its OWN stage-close obligations** (a PLAN-close checklist added); and the `accesslog_emit_test.go` SPEC-roster departure silently inverted into a BYTE-UNTOUCHED gate rather than named. **Row 72 STAYS `in-progress`** (flips at the IMPL six-gate); DECISIONS UNTOUCHED (ADR-0294 stays PROPOSED — verified `grep -c '^### Decision'` ⇒ 0 inside the block, so T9's APPEND is sound); NO deferred sentence narrowed. **Counts UNCHANGED (docs-only; re-run MECHANICALLY):** fixtures **117** · fuzzers **55** · stat surface **1201** · BackendKind **38** · DECISIONS tail **ADR-0294** (PROPOSED; next-free ADR-0295) · go.mod modules **2**. **Sentinel re-run MECHANICALLY (does NOT fire; `stop` NOT created):** (1) prints `NOT DONE: row 72`; (2) ⇒ **3**; (3) prints `NEVER OPENED: gRPC/Runtime/WASM`. **Next -> the phase-72 IMPL** (execute T1-T9; RE-DERIVE each task's anchors at the IMPL tip).

- **prior active-phase:** `phase 72 (tracing-custom-tags-metadata-host) SPEC done` (2026-07-23) — lifecycle-state 1 → 2; docs-only (`SPEC.md` new + the ADR-0294 §Context append to `DECISIONS.md`; ZERO production `.go`, ROADMAP UNTOUCHED); fresh worktree `.worktrees/phase-72-spec` off master `0150b04c` (the phase-72 BRAINSTORM squash + its router follow-up; NO parallel stream — the production tree is byte-identical to what the SPEC re-derived), branch `phase-72-tracing-custom-tags-metadata-host-spec`, per `feedback_git_worktrees`; subagent-driven — a read-only re-derivation agent re-opened EVERY BRAINSTORM §6/§11 anchor at tip AND BUILT the row's full claimed shape in private scratch, plus THREE probe fleets running SEVEN discriminating live arms (P1-P7) against `contrib-v1.37.2`. **The `D-CTMH-*` docket disposed one-for-one.** HEADLINE FINDINGS: (P2, the load-bearing one) **`envoy.lb` is NOT privileged — ANY `filter_metadata` namespace is addressable**, so `cluster.Endpoint` must retain ALL namespaces ⇒ S1 decided a `filterMetadata map[string]*structpb.Struct` field aliasing `lbe.GetMetadata().GetFilterMetadata()` (ONE new import, zero new allocation, `unsafe.Sizeof` 104 → 112 B) beside the BYTE-UNCHANGED phase-38 projection; (P6 × the code, the row's most consequential finding) **the reference resolves a HOST tag at LOAD-BALANCER PICK TIME, not on upstream success** — on a connect-refused 503 `UF` it emitted the picked-but-unreachable endpoint's own metadata — while envoy-go carries the zero `Endpoint` on every failure sub-path ⇒ a NAMED DEPARTURE (B2). Adversarial review CAUGHT TWO FALSE parity claims in an earlier draft of that table: envoy-go emits NO SPAN AT ALL on a 404 no-match (PRE-Decide sites) and BOOT-REJECTS a zero-endpoint cluster, so neither is a HOST-tag parity. The departure is established as **PRE-EXISTING** (the access-log upstream-host field is already empty there, pinned by three unit tests and by NO differential fixture) and deliberately SCOPED OUT as its own future row; (P7) `LocalityLbEndpoints.metadata` contributes NOTHING to HOST-kind resolution ⇒ envoy-go retaining none is NOT a divergence, no departure owed; (P3/P5) the serializer reuses VERBATIM but three PRE-EXISTING boundaries are newly quantified — scalar-number precision, a different renderer for nested numbers, and an ARBITRARY reference key order for multi-key structs ⇒ **the `0116` fixture asserts STRING values only** — plus the reference's Zipkin serializer DROPS empty-string tag values where OTLP emits `""`. TWO BRAINSTORM premises found FALSE (S2): `accesslog_emit.go` ALREADY imports `structpb` (so no shape localizes an import), and the "two cheaper closure shapes" claim — MEASURED, all three candidates cost exactly 1 alloc/128 B and the pointer-receiver variant is strictly worse. The central cost claim EXECUTION-CONFIRMED: `go build ./...` EXIT 0, `go mod tidy -diff` EMPTY, the cycle guard clean, exactly the two predicted `config_test.go` `/host` failures across 70 packages, and sha256 proof that the 18 emit callers + 29 emit test callers + `chain.go` are BYTE-UNTOUCHED. A SINGLE FLAT ROW of NINE tasks (T1-T9) DECIDED (ADR-0045 valve armable-but-unconsumed — the load-bearing `internal/cluster` question turned up ZERO behavioral disturbance). One hazard carried forward: a PRE-EXISTING `internal/cluster` `-race` flake (`TestOutlierDetector_ConcurrentEjectExactlyOnce`) reproduced on the UNMODIFIED baseline. **Row 72 STAYS `in-progress`**; NO deferred sentence narrowed (phase-57 precedent); DECISIONS tail flipped ADR-0293 → **ADR-0294** (PROPOSED, §Context at the SPEC per ADR-0044; next-free ADR-0295); STATE §Current rolled IN PLACE; next-prompt.txt rolled to the phase-72 PLAN. Counts UNCHANGED apart from the tail flip.

- **prior active-phase:** `phase 72 (tracing-custom-tags-metadata-host) BRAINSTORM done` (2026-07-23) — lifecycle-state DONE → 1; the roller's AUTONOMOUS self-pick (no human pick per the 2026-07-12 standing directive) continues the Observability family; docs-only, ZERO production `.go`; fresh worktree `.worktrees/phase-72-brainstorm` off master `4238a4d3` (the phase-71 IMPL squash), branch `phase-72-brainstorm`, per `feedback_git_worktrees`. **Subject:** `tracing-custom-tags-metadata-host` — the tracing `custom_tags` `metadata` type for the **`HOST`** MetadataKind, the EIGHTEENTH Observability-family row + the SIXTH tracing `custom_tags` capability + the THIRD of the four `metadata` MetadataKinds (after `REQUEST`@70, `ROUTE`@71): lifts the `custom_tags metadata tag %q host kind unsupported` reject (`internal/tracing/config.go:267-268`) and resolves the SELECTED UPSTREAM ENDPOINT's metadata (`lb_endpoints[].metadata.filter_metadata[ns]`, walked by `MetadataKey.path`) onto the ingress SERVER span as a `{tag, value}` attribute on BOTH the OTLP and Zipkin exporters via the shared `ResolveCustomTags` → `Span.Attrs []KV` seam (phase 46); `CLUSTER` + the two unset-kind arms stay reject-loud with distinct substrings (envoy-go-strict DEPARTURE, ADR-0080 — the reference BOOTS them). **THE DECISIVE COST FINDING (§1.7/§2.1/§11, re-derived mechanically at tip `4238a4d3`): `HOST` SKIPS THE 18-CALLER THREADING TAX.** The three emit functions already take the selected endpoint as their 4th parameter (`internal/filter/hcm/accesslog_emit.go:27`/`:87`/`:149` — `picked cluster.Endpoint`, already consumed at `:72`/`:133`/`:194` via `upstreamHostString`) and the three `ResolveCustomTags` call sites (`:57`/`:118`/`:179`) sit INSIDE those same functions, so the `hostMetaLookup` closure is built LOCALLY and the 18 emit callers (5 `connection.go` + 6 `h2dispatch.go` + 7 `h3dispatch.go`, count re-derived) are BYTE-UNTOUCHED — the single largest task in BOTH predecessor rows is dropped entirely, the three emit SIGNATURES are unchanged, and `internal/filter/http/chain.go` is not touched at all (unlike 70's `DynamicMetadata()` and 71's `RouteMetaLookup()` adds). **Why this pick (the FULL rejected roster adjudicated in BRAINSTORM §2.1/§11; TWO independent read-only cost-assessment agents at tip `4238a4d3`):** `metadata`/`HOST` sits on landed substrate on FOUR axes — the phase-70 `descend` (`resolve.go:128`) + `structpbValueToString` (`:151`) reused VERBATIM; phase-71's `kindMetadataRoute` config accept arm (`config.go:248-264`) and resolve arm (`resolve.go:95-118`) as byte-level clone templates (the FULL `MetaPath`, NOT `[1:]` — `reference_route_metadata_resolve_full_metapath`); the source ALREADY AT THE SEAM; and the just-landed `0115` fixture chassis with the metadata block relocated from the route to `lb_endpoints[]`. **TWO STALE-COST CORRECTIONS recorded (`reference_deferred_candidate_cost_restale`):** (1) `HOST` had been costed MODERATE by both predecessor BRAINSTORMs on "only the reduced `envoy.lb` subset is in scope" — the FACT holds exactly (`cluster.go:46-49`; `manager.go:883`) and phase 71's BRAINSTORM `:55` even SAYS the picked `Endpoint.Metadata` "IS in scope at the seam" — what was never derived is its COST CONSEQUENCE (in-scope ⇒ no 18-caller thread); re-derived at tip `HOST` is SMALL. (2) The `CLUSTER` "LARGE because of the `upstream_cluster` framework gap" adjective is a CATEGORY ERROR, not merely stale: the gap is REAL (`accesslog_emit.go:51`/`:112`/`:173` hard-code `UpstreamCluster: ""`) but belongs to the `upstream_cluster` span TAG — a DIFFERENT feature; a `CLUSTER`-kind metadata tag needs the selected cluster's `metadata`, a DIFFERENT plumbing problem (**no claim that it is a *smaller* one** — both are blocked by the same missing seam, the selected cluster's identity at the emit seam; the correction is the CATEGORY, not the magnitude), so `CLUSTER` is MODERATE (~11–13 tasks) and rejected on SIZE, not impossibility. **The rivals:** `CLUSTER` metadata (MODERATE — no metadata field on `cluster.Cluster` `cluster.go:132`; `router.go:255` publishes only `Picked() cluster.Endpoint` ⇒ a `routeAction` method `route.go:52-59` + a `SetClusterMetadata`/`ClusterMetaLookup` pair RE-PAYING the 18-caller thread; plus `weightedClusterRouteAction`'s per-request in-closure selection `actions.go:234-245`; **the natural phase-73 candidate**, closing the quartet) · `spawn_upstream_span` (LARGE) · OTel `http_service`/`sampler`/`resource_detectors` (MODERATE each) · tracing `verbose` (MODERATE) · force-trace (LARGEST) · the `ssl` stat family (LARGE — ADR-0286 C3 framework surgery CONFIRMED, the one adjective that HELD) · a 6th stats sink (effectively VACUOUS — all five non-`wasm` `stats_sinks[]` extensions are landed; the only unconsumed one is `wasm`) · xDS reconnection-backoff (REJECTED as larger-than-it-looks — `initial_fetch_timeout` is ALREADY landed at `internal/xds/config.go:11-13`/`:74-75`, and `internal/xds/stream.go` has NO loop at all, just two single-shot fetchers ⇒ no persistent watch stream to reconnect TO) · QUIC `QuicProtocolOptions` (SMALL ~9–11 but LOSES on execution risk — ~2 tasks of NEW harness first, timing-flaky observables) · upstream SDS (LARGE + a boot-ORDER blocker) · CDS/EDS · LDS/RDS · ADS · Delta xDS · `google_grpc` · upstream H3/alt-svc/0-RTT/h3spec/QUIC robustness · the RBAC `require=false` rider · DataSource `environment_variable` · `watched_directory`/SDS rotation · opening a never-opened family (gRPC gated on an HCM response-trailer framework row — `RunDecodeTrailers` `chain.go:455` / `RunEncodeTrailers` `:622` have ZERO production callers; Runtime genuinely unbuilt — `internal/runtime/` = `doc.go` only; **WASM a ROADMAP BOOKKEEPING ARTIFACT** — `internal/wasm/` is 21,550 LOC of wazero host and rows 25/25.1/25.2/25.3 are all `done`, so `NEVER OPENED: WASM` describes substantially BUILT work; flagged for a future correction, deliberately NOT acted on at this docs-only row). **NEWLY NAMED deferred candidates from this session's sweep:** `access_log[].filter` (field 2 of `envoy.config.accesslog.v3.AccessLog`, a 13-arm oneof — a KNOWN proto field so the strict `DiscardUnknown:false` pass ACCEPTS it, yet a repo-wide `GetFilter()` grep returns ZERO production hits ⇒ a **LIVE BEHAVIORAL DIVERGENCE**, not merely an unimplemented knob; **flagged a strong FUTURE pick**) and `stats_flush_on_admin` (an explicit ADR-0080 strict-reject at `internal/bootstrap/bootstrap.go:606-607`). **The ONE genuine gap (D-CTMH-ENDPOINT-METADATA — the load-bearing SPEC question):** `cluster.Endpoint` retains only a LOSSY projection — the `envoy.lb` namespace ONLY, scalars ONLY (`cluster.go:46-49`, populated `manager.go:883` via `ScalarsFromStruct` `subset.go:263-285`, which routes `StructValue`/`ListValue`/`NullValue`/nil into a DISCARDED `nonScalar` return) — while a HOST tag must address a caller-chosen namespace AND walk a nested path; so the RAW per-namespace metadata must be RETAINED ALONGSIDE it (an added field + a one-line populate, the phase-38 projection BYTE-UNCHANGED; NO identity ripple — `Endpoint` is already non-comparable per its own `cluster.go:88-92` doc comment and every identity use is `Addr()`-keyed). **`internal/cluster` is phase 72's ONE genuinely new blast-radius surface vs phases 70/71** (mechanically re-derived — `git log --name-only e2912f6f~1..HEAD -- internal/` lists 13 files across 70+71 and `internal/cluster` appears ZERO times), so the IMPL envelope audit must pin the change's SHAPE rather than assert BYTE-UNTOUCHED. **Envelope:** ZERO new packages (edits in `internal/tracing` + `internal/cluster` + `internal/filter/hcm`, all existing; `internal/dynamicmetadata` and `internal/filter/http/chain.go` UNTOUCHED) / +0 stats / +0 fuzzers (a PURELY ADDITIVE `meta_host_ok` `FuzzHCMConfigParse` seed — NO `meta_bad` repoint, CLUSTER stays the live reject-arm seed, a second reason `HOST` beats `CLUSTER`) / +0 BackendKinds / +0 go.mod modules; ONE new OTLP fixture anticipated (`0116-tracing-custom-tags-metadata-host`, a static `lb_endpoints[].metadata` hit arm + a `default_value` arm, single-endpoint cluster, NO writer) ⇒ fixtures **117 → 118**. **BRAINSTORM-time open questions to the SPEC:** D-CTMH-SCOPE · D-CTMH-PROTO · D-CTMH-WIRE · D-CTMH-ZIPKIN-WIRE · D-CTMH-VALUE-SERIALIZE · D-CTMH-DEFAULT · D-CTMH-RESOLVE-SEAM · **D-CTMH-ENDPOINT-METADATA** · D-CTMH-CONFIG-KIND · D-CTMH-NOHOST (the zero-`picked` case — the one behavior with NO analogue in 70/71) · D-CTMH-REJECT · D-CTMH-FIXTURE/D-CTMH-FIXTURE-SOURCE · D-CTMH-FUZZSEED · D-CTMH-SPLIT (SINGLE FLAT ROW anticipated, ~9–11 tasks; ADR-0045 armable). **Row 72 registered `in-progress`** at this stage-close commit per the ROADMAP §Schema invariant (plus a phase-72 charter sentence appended to the §Observability-family heading paragraph); it flips `done` at the phase-72 IMPL six-gate (ADR-0106, the SOLE leg). **This pickup IS named in the live Observability `candidates:` sentence (the non-`REQUEST`/non-`ROUTE` MetadataKinds `CLUSTER`/`HOST`) — NO sentence is narrowed at ANY pre-IMPL stage (the phase-57 precedent); the `HOST` candidate rolls OUT, leaving `CLUSTER`-only, at the phase-72 IMPL row-done edit.** **DECISIONS.md UNTOUCHED** (ADR-0294 §Context drafts at the SPEC per ADR-0044; §Decision + §Consequences complete IN PLACE at the IMPL, no renumber). **Counts UNCHANGED at this BRAINSTORM (docs-only; re-run MECHANICALLY in the worktree at this close):** fixtures **117** (`ls -d test/fixtures/[0-9]*/ | wc -l` ⇒ 117; numeric tail `0115-tracing-custom-tags-metadata-route`) · fuzzers **55** (`grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l` ⇒ 55) · stat surface **1201** (BEHAVIOR_CONTRACT doc count; NO mechanical command) · BackendKind **38** (`H2GoawayResponder`) · DECISIONS tail **ADR-0293** (COMPLETE; next-free **ADR-0294** — `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1` ⇒ `## ADR-0293`) · go.mod modules **2** (the phase-61.2 lineage figure, NOT a repo total). **Sentinel re-run MECHANICALLY at this close (does NOT fire; `stop` NOT created):** (1) prints `NOT DONE: row 72` (the registration re-opens check (1)); (2) prints THREE live "candidates:" sentences (`grep -cE 'remaining deferred \(not-yet-chartered\) candidates:'` ⇒ **3**); (3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`. Checks (2)+(3) print ⇒ abundant work remains. **Next → the phase-72 SPEC** (RE-DERIVE the BRAINSTORM rather than execute it; the D-CTMH-* live-probe arms + the ADR-0294 §Context draft; the owed roster is in §Current pointer's next-skill line).

- **prior active-phase:** `phase 71 (tracing-custom-tags-metadata-route) IMPL done` (2026-07-22) — lifecycle-state 3 → DONE; **ROW 71 FLIPS `in-progress` → `done`** at this IMPL six-gate (ADR-0106, the SOLE leg — a SINGLE FLAT ROW, `reference_roadmap_split_phase_row_done`). Fresh worktree `.worktrees/phase-71-impl` off master `428d7728` (the phase-71 PLAN squash), branch `phase-71-tracing-custom-tags-metadata-route-impl`; subagent-driven (`feedback_execution_style`) — one implementer per task + a per-task adversarial reviewer, each committing LOCALLY only (`feedback_subagents_no_push`), controller squash-push at close (`feedback_push_to_origin`). Each task RE-DERIVED its anchors at the IMPL tip (a PLAN is not evidence) — the controller re-opened all SPEC §11 anchors and found **32/32 EXACT-HOLD, ZERO drift** (no correction needed; the phase-70 `k := md.GetKind()` receiver fact still held). **The 9-task TDD spine landed** (each red-first → green → break-verified → adversarially reviewed): T1 (`9f96354a`) `config.go` `kindMetadataRoute` + the ROUTE accept arm cloning REQUEST (MetaPath FULL, NO new import) replacing the `:245-246` `route kind unsupported` reject; CLUSTER/HOST/unset rejects UNCHANGED; breaks A/B/C fired (one stale phase-70 `"route"` reject subtest removed) · T2 (`4da435b2`) `resolve.go` the nil-tolerant one-arg `routeMetaLookup` 4th param + the `kindMetadataRoute` arm descending the FULL `MetaPath` (NOT `[1:]` — the REQUEST slice is a Bucket-pre-keying artifact); `descend`/`structpbValueToString` reused VERBATIM; the crux Break D (wrong slice) reproduced; `-race` clean · T3 (`3c0acfef`) the NEW `func (c *FilterChain) RouteMetaLookup()` accessor (the ROUTE analogue of `DynamicMetadata()`, wrapping `RouteMetadata().GetFilterMetadata()[ns]` via `structpb.NewStructValue`, nil-safe) + the `structpb` import to `chain.go` (an existing module, +0 go.mod); Break G discriminating · T4 (`040f67d4`) the `routeMetaLookup` thread at all 18 emit callers — `chain.RouteMetaLookup` APPENDED as the 4th arg at the 15 span-capable sites, a 2nd `nil` at exactly the 3 no-chain-404 sites (provably never reached — the `traceDecision` gate); the 3rd `metaLookup` arg UNTOUCHED; a live ROUTE-metadata-span test; `-race` clean · T5 (`8147ed9e`) the `withMetaTags` seed repurposed (add `meta_route_ok` valid ROUTE accept + repoint `meta_bad` Route→Cluster), dispatch-verified to reach the metadata arm (+0 fuzzers) · T6 (`3776a7e8`) the OTLP fixture `0115-tracing-custom-tags-metadata-route` (the `0114` chassis WITHOUT the Lua writer + a static route `metadata.filter_metadata` block + two ROUTE custom_tags — `route_hit`→the static value `v-route-0115`, `route_default`→`fallback-0115` — asserted cross-side EXACT key+value; breaks J/K/L all fired LIVE; fixtures 116→117) · T7 (`383cc974`) BEHAVIOR_CONTRACT B1–B2 · T8 the six-gate · T9 ADR-0293 COMPLETED IN PLACE + this roll. **Every task got a per-task adversarial review** (ZERO SEVERE/MODERATE across all; the T6 reviewer independently re-ran the `0115` differential LIVE → PASS). **Six-gate ALL GREEN:** gofmt SILENT · vet 0 · build 0 · `go mod tidy -diff` EMPTY + `go.mod`/`go.sum` NO DIFF vs master (+0 modules) · golangci-lint 0 · the FULL **117-dir** differential `ok 388.647s` EXIT 0 · cycle guard `go list -deps ./internal/tracing` = NO `internal/filter` edge (the reused `descend` keeps it filter-free) · `-race` green on `internal/tracing`/`internal/filter/http`/`internal/filter/hcm` · envelope audit: 7 functional production files (`internal/tracing/{config.go,resolve.go}` + `internal/filter/http/chain.go` + `internal/filter/hcm/{accesslog_emit.go,connection.go,h2dispatch.go,h3dispatch.go}`) + the `0115` fixture + a 1-line `runner_test.go` registration + BC, with `internal/xds`/`internal/tls`/`internal/boot`/`internal/listener`/`internal/bootstrap`/`validate/` + `internal/dynamicmetadata`/`ratelimit`/`lua` BYTE-UNTOUCHED. **Exit counts:** fixtures **116 → 117** (tail `0115-tracing-custom-tags-metadata-route`) / fuzzers **55 (+0)** / stat surface **1201 (+0)** / BackendKind **38 (+0)** / DECISIONS tail **ADR-0293** COMPLETE (next-free **ADR-0294**) / go.mod modules **2 (+0)**. **The Observability family STAYS OPEN** — the `ROUTE` MetadataKind rolled OUT of the live `candidates:` sentence at this row-done edit (phase-57 precedent); the `CLUSTER`/`HOST` MetadataKinds + `spawn_upstream_span`/`http_service`/force-trace + the `ssl` stat family remain. **Sentinel re-run MECHANICALLY (does NOT fire; `stop` NOT created):** (1) prints NOTHING (row 71 `done` — every chartered row `done`); (2) prints THREE `candidates:` sentences (`grep -cE 'remaining deferred \(not-yet-chartered\) candidates:'` ⇒ **3**); (3) prints `NEVER OPENED: gRPC/Runtime/WASM`. Checks (2)+(3) print ⇒ abundant work remains. **Next → the phase-72 BRAINSTORM** (the roller SELF-PICKS per the 2026-07-12 standing directive).


## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
