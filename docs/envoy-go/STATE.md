# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `06.1-stats-prometheus`
- **phase-directory:** `docs/envoy-go/phases/06.1-stats-prometheus/` — contains `SPEC.md` (at `ad46930`, frozen), `PLAN.md` (at `be99b42`), and `PROGRESS.md` (Task 15 closing entry at `58d4ec9`, the lifecycle-state 4 verifier FAIL block at `1f94b74`, plus the lifecycle-state 3 → 4 fix block written by THIS commit). Parent `docs/envoy-go/phases/06-observability-baseline/` carries `BRAINSTORM.md` (`d271fd7`) + parent `SPEC.md` (`ad46930`). Sibling `docs/envoy-go/phases/06.2-access-log/` retains the placeholder `README.md` (`ad46930`). Phase 05 closed history unchanged.
- **lifecycle-state:** `4` — gate-(d) crasher TDD-fixed; implementation complete; awaiting fresh-worktree formal verification. The fix branch `phase/06.1-stats-prometheus-impl-followup-gate-d` (worktree `.worktrees/phase-06.1-stats-prometheus-impl-followup-gate-d`, branched from verify-branch tip `6a053a4` per ADR-0003) added (a) `internal/stats.IsValidName(name string) bool` — read-only helper wrapping the same `nameRE` that `Registry.checkName` enforces, (b) a guard in `internal/filter/hcm/config.go`'s `parseFilterWithCtx` that calls `stats.IsValidName("http." + statPrefix + ".downstream_rq_total")` between the existing `stat_prefix` non-empty check and the route-config build, returning `hcm: invalid stat_prefix: %q (...)` on failure, (c) `TestParseFilter_StatPrefixInvalidChars` in `internal/filter/hcm/config_test.go` (6 cases, including the verbatim fuzz seed prefix `"0000000000 0"` — RED before fix, GREEN after), (d) `TestIsValidName` in `internal/stats/registry_test.go` covering the new helper, and (e) ADR-0065 in `DECISIONS.md` recording the validate-at-input-boundary pattern + the two rejected alternatives (sanitise-per-SN1 — would silently mutate the Rule-SN2 label value vs upstream Envoy + cause label-collision data-loss; convert NewCounter to error-return — would cascade to duplicate-name and post-Freeze paths, wider API change than needed). The fix branch's local sanity sweep passes all six gates: (a) fixture-0005 differential GREEN, (b) all 5 pre-existing fixtures GREEN, (c) h2spec 53/53 GREEN at the ADR-0051 pin, (d) `FuzzHCMConfigParse` 30 s GREEN (3.49 M execs, 19 new-interesting, 0 crashers) plus all 7 fuzzers GREEN at a 10 s sanity budget, (e) part 1 `go vet` + `golangci-lint` + `go build` clean and part 2 `go test -race ./...` clean across all 18 packages with no DATA RACE warnings, (f) REVIEW.md is the lifecycle-state-6 deliverable (deferred). Cluster-name carry-forward latent vulnerability noted in PROGRESS.md (`internal/cluster/manager.go:97` propagates `cluster.<name>` into eight metric names without validation; not gate-(d)-blocking; follow-up branch will add the same `stats.IsValidName` guard at `cluster.NewManager`); listener is already safe via `normalizeAddr`. Per BOOTSTRAP §5 step 3 → step 4, the next session is the formal lifecycle-state-4 verifier in a fresh worktree per the 05.2 verifier role contract (verifier commit changes only `STATE.md` + `PROGRESS.md`).
- **next-skill:** `superpowers:verification-before-completion` — re-run all six BOOTSTRAP §7.5 gates from a fresh-session worktree branched from THIS commit's HEAD. Inputs: this commit's `docs/envoy-go/phases/06.1-stats-prometheus/PROGRESS.md` "Lifecycle-state 3 — gate-(d) fix landed" block (the authoritative fix-evidence record + verbatim local sweep outputs), `docs/envoy-go/DECISIONS.md` ADR-0065 (the validate-at-input-boundary pattern), `internal/filter/hcm/config.go` (`parseFilterWithCtx` + the new guard), `internal/stats/registry.go` (`IsValidName` + the unchanged `Registry.checkName` panic discipline), `internal/filter/hcm/config_test.go` (`TestParseFilter_StatPrefixInvalidChars`), `internal/stats/registry_test.go` (`TestIsValidName`), `BOOTSTRAP_PROMPT.md` §7.5 (the six-gate definition of done), the prior verifier's "Verification (lifecycle-state 4) — FAILED" block at `1f94b74` (the role-contract precedent: verifier commit changes only PROGRESS.md + STATE.md, no production-test-corpus changes), and the 05.2 verifier role contract `b34bd99`.
- **next-skill-scope:** Lifecycle-state 4 → 5 re-verification session in a fresh worktree (`.worktrees/phase-06.1-stats-prometheus-verify-2`, branched from THIS commit's HEAD per ADR-0003 + per-phase-worktree convention). The verifier MUST: (1) re-run all six BOOTSTRAP §7.5 gates from a fresh session — (a) all 6 differential fixtures including 0005, (b) implicit in (a), (c) h2spec at the ADR-0051 pinned summerwind SHA, (d) all 7 fuzz targets at the ADR-0018 30 s budget each, (e) part 1 `go vet` + `golangci-lint run` + `go build ./...` clean and part 2 `go test -race ./...` clean across all packages, (f) REVIEW.md is the lifecycle-state-6 deliverable (deferred); (2) quote verbatim outputs of every gate command into a "Verification (lifecycle-state 4 — re-run after gate-(d) fix) — PASSED" block appended to PROGRESS.md; (3) on full-pass, transition STATE to lifecycle-state 5 with `next-skill: superpowers:requesting-code-review`; (4) on any failure, regress STATE to lifecycle-state 3 with the same `next-skill: superpowers:systematic-debugging` shape used by `1f94b74`; (5) per the 05.2 verifier role contract, the verifier commit changes ONLY `STATE.md` + `PROGRESS.md` — no production code, test, or fixture changes; any auto-generated fuzz seed file under `internal/filter/hcm/testdata/fuzz/` (or any other testdata path) MUST be deleted from the verifier's worktree before commit, with bytes quoted in PROGRESS.md if a new crasher surfaces. Carry-forward considerations for the verifier's PROGRESS.md write-up: (i) the cluster-name latent vulnerability noted in this commit's fix block is NOT a gate-(d) re-verify concern (it's a separate follow-up branch's scope); (ii) the L4 review-followup session's 26 Minors remain queued for the eventual REVIEW phase (not for the verifier).
- **last-commit:** `79be6b0` — `phase 06.1 fix: gate-(d) FuzzHCMConfigParse — validate stat_prefix at HCM parse boundary [ADR-0065]`, on branch `phase/06.1-stats-prometheus-impl-followup-gate-d`. Adds (a) `IsValidName` to `internal/stats/registry.go` + its test; (b) `stat_prefix` validation guard in `parseFilterWithCtx` returning `hcm: invalid stat_prefix: %q (...)` on failure; (c) `TestParseFilter_StatPrefixInvalidChars` regression test (RED before fix, GREEN after — covers the verbatim seed prefix `"0000000000 0"` plus 5 other invalid chars); (d) ADR-0065 in `DECISIONS.md` recording the validate-at-input-boundary pattern + the two rejected alternatives + the cluster-name carry-forward; (e) PROGRESS.md "Lifecycle-state 3 — gate-(d) fix landed" block with verbatim local-sweep outputs; (f) STATE.md transition 3 → 4 with `next-skill: superpowers:verification-before-completion`. Production-code surface change is two files (one new exported function + one new error-return guard); test surface change is two files (two new `Test*` functions); doc surface change is three files (DECISIONS.md ADR-0065, PROGRESS.md fix block, STATE.md). ROADMAP unchanged (rows 06.1 + 06 stay `in-progress`; row 06.2 stays `planned`). SHA-fill follow-up per the phase-02..05.2 convention.
- **last-updated:** 2026-04-28

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
