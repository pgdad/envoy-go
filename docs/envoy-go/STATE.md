# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `03-tls`
- **phase-directory:** `docs/envoy-go/phases/03-tls/` — exists; contains `SPEC.md` (now consistent with ADR-0030's TLS_AUTO mapping per the I-3 fix in commit `98cc35b`), `PLAN.md`, `PROGRESS.md` (16 task entries: original 15 + Task 16 state-3 re-entry from commit `1351a01`), and `REVIEW.md` (committed `d45c467` by the prior session — verdict APPROVED WITH FOLLOW-UPS, 0 Critical, 4 Important, 8 Minor; the four Important items are now landed in `98cc35b`). All artifacts on branch `phase/03-tls-impl` under `.worktrees/phase-03-tls-impl`. Master remains at `aff6fbf` and is currently 3 commits behind impl (`d45c467` REVIEW, `98cc35b` follow-ups, `1351a01` PROGRESS Task 16).
- **lifecycle-state:** `4` — implementation complete inclusive of REVIEW.md follow-ups; awaiting fresh state-4 re-verification. State machine flow this session: state 5 (REVIEW issued with 4 Important flags by prior session in `d45c467`) → re-entered state 3 per BOOTSTRAP §5.2 ("if issues → back to step 3"; user-confirmed Path A — strict §5.2 reading over the reviewer's own "ship as first commit of phase 04" recommendation) → state 3 work completed in `98cc35b` (REVIEW follow-ups I-1..I-4) and recorded as PROGRESS Task 16 in `1351a01` → state 3 → state 4 transition (one forward state move this session per §5.1). The four Important fixes were doc/comment/error-string/dead-code corrections only — no runtime semantics changed. I-1: `internal/tls/params.go` `mapTLSVersion(TLS_AUTO)` head-comment rewritten to match the actual `(0, nil)` no-op behaviour and ADR-0030. I-2: `internal/listener/manager.go:302` `GetConfigForClient` SNI-no-match error now begins `"listener: %q: ..."` (colon after package prefix), matching project-wide `<package>: ...` discipline; shipped with TDD red→green regression test (`TestNewManager_MultiChain_NoSNIMatch` extended with `strings.HasPrefix(err.Error(), "listener:")` assertion). I-3: SPEC §5.5 TLS_AUTO row now reads "no-op (treat as unset, per ADR-0030)" instead of "→ error". I-4: orphaned `prefix` variable + `_ = prefix` suppressor deleted from `test/fixtures/0002-tls-tcp/driver/driver.go`. Local gate sweep on the impl worktree from this session was clean (`go build`, `go vet`, `golangci-lint run`, `go test -count=1 -timeout=10m ./...` all exit 0; `internal/listener` 0.008s with new prefix assertion green; `internal/tls` 0.016s; `test/fixtures/0002-tls-tcp/driver` 0.002s; differential suite passed incidentally at 4.877s — strictly a state-4 gate, not substitute evidence for next session's required fresh-shell re-run). The 8 Minor REVIEW findings (M-1..M-8) carry forward to phase 04+ triage; no fix is committed in this re-entry. Reference Envoy pin remains `envoyproxy/envoy@sha256:c5e8a68e52f4d4697a9adb280dbe415d77fedf1257e183dcb86205bd438f18bd` (v1.37.2, per `ENVOY_TARGET.md` / ADR-0008).
- **next-skill:** `superpowers:verification-before-completion`
- **next-skill-scope:** Re-run every SPEC §3 / BOOTSTRAP §7.5 phase-done gate from a fresh session against impl tip `1351a01` (or HEAD as resolved at session-start) and append a fresh "Verification — lifecycle-state 4 → 5 (post-REVIEW-follow-ups)" block to `PROGRESS.md` quoting all command outputs verbatim. Required gates: (a) `0002-tls-tcp` differential fixture remains green after the I-4 fixture-driver edit (the `prefix` deletion changed only an unused local; no behaviour change expected); (b) regression-clean `0000-tcp-echo` and `0001-tcp-proxy-rr`; (c) N/A — phase 03 ships no conformance suites; (d) all three fuzzers (`FuzzTLSContextParse`, `FuzzBootstrapLoad`, `FuzzTcpProxyFilter`) at ADR-0018's 30-second budget each — no crashes, no `testdata/fuzz/` seed-corpus drift; (e) `go build`, `go vet`, `golangci-lint run`, `go test -count=1 -timeout=10m ./...` all exit 0; (e′) `0002-tls-tcp/pki/gen` re-run leaves `git diff --exit-code pki/` clean. After verification, the next session decides on the §5.2 re-review question: REVIEW.md (`d45c467`) was already APPROVED WITH FOLLOW-UPS and the four Important follow-ups are now landed in `98cc35b` — strict §5 ordering would call for a brief state-5 confirming addendum (a focused `superpowers:requesting-code-review` round verifying I-1..I-4 match the REVIEW.md recommendations); a more pragmatic reading treats the existing REVIEW as fully satisfied and skips directly to state 6. The orchestrator should make that judgment call after the state-4 verification block lands. State 6 finalisation (the phase-done commit per §5.3 with format `phase 03: <title> [ADR-0029,...,ADR-0036]`, `ROADMAP.md` status flip to `done`, master fast-forward to impl tip per ADR-0003, and STATE handoff to phase 04) follows in either case. Work from the impl worktree (`/home/esa/git/envoy-go/.worktrees/phase-03-tls-impl`).
- **last-commit:** 1351a01
- **last-updated:** 2026-04-24

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
