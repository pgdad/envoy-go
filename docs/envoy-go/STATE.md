# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

> **This file is a POINTER, not an archive** — `BOOTSTRAP_PROMPT.md` §4: *"STATE.md — pointer: active phase, last commit, next action."* It carries the live pointer, the live counts, and a SHORT recent lineage. **Full stage-by-stage history lives in `STATE_HISTORY.md`** (archived at ADR-0288; **nothing was deleted** — the pre-move file is preserved there verbatim, byte-for-byte).
>
> **⚠️ THE RULE ADR-0288 EXISTS TO ENFORCE: there is EXACTLY ONE live `next-skill:`, `lifecycle-state:`, `next-free ADR:`, and count block IN THIS FILE, and they live in §Current pointer directly below.** Before that move, ~330 lines of prepended backlog had buried TWO fossilised "current state" blocks whose `next-skill:` still read *"the 42.2 hedging BRAINSTORM"* and *"the 46.2 SPEC"* while the live phase was **66** — so `grep 'next-skill:' STATE.md` returned **two stale hits and ZERO live ones**, and `grep 'fuzzers:'` returned **48** and **42** when the true count was **55**. **A session may trust a grep of this file again. Keep it that way: when you advance the pointer, EDIT §Current pointer IN PLACE — never prepend a new block above it.**

---

## Current pointer

*(The ONLY live "what next" in this repo. Update IN PLACE at every stage close.)*

- **active-phase:** `phase 87 (h2-double-slash-path-routing) BRAINSTORM done` (2026-08-12) — lifecycle-state **DONE -> 1**; **ROW 87 REGISTERED `in-progress`** at `ROADMAP.md:149` and the sentinel `want` bumped **118 -> 119 in the SAME commit** (the phase-84/85/86 precedent). Base master **`638ef78a`** (from `git rev-parse master`), branch `phase-87-brainstorm`. Docs-only: ZERO production `.go`, ZERO test `.go`; `DECISIONS.md` and `BEHAVIOR_CONTRACT.md` **BYTE-UNTOUCHED** (a BRAINSTORM adds no ADR; next-free stays **ADR-0309**; strict `PROPOSED` guard stays **0** — the phase-87 SPEC re-arms it). **SELF-PICKED** per the 2026-07-12 standing directive — no banked mid-lifecycle work existed (phase 86 CLOSED, all 118 rows `done`, check (1) SILENT at `want=118` — the THIRD silent reading — before this stage re-opened it at row 87). ⚠️ **NAMED DEPARTURE: no investigation agents — probes INLINE by the controller** (tip binary built with `-o` into session scratch; four `curl --http2-prior-knowledge --path-as-is` executions over h2c; two `net/url` `go run` differentials; ports 47400-47402 TEMPLATE VALUES ONLY, nothing bound survives). **THE HEADLINE: envoy-go's hand-rolled H2 downstream codec MIS-PARSES a leading `//` in the origin-form `:path`.** `internal/filter/hcm/h2/stream.go`'s `buildRequest` parses `:path` with `url.Parse` — a generic RFC-3986 URI parser that reads a leading `//` as a network-path reference and peels the authority into `u.Host`, corrupting `u.Path`. REPRODUCED BY EXECUTION at `638ef78a` (positive control `GET /` -> `routed-ok` 200, so the miss is not vacuous): `GET //foo` -> **404** (empty path — routing miss); `GET //foo/bar` -> **SILENT MIS-ROUTE to `/bar`** (leading segment swallowed as authority; served against the WRONG path with no error); mid-path `/a//b` -> 200 (unaffected). ⚠️ **The defect is H2-SPECIFIC** — the IDENTICAL `//foo` over HTTP/1.1 returns `routed-ok` 200 (H1 is served by Go `net/http`; H3 delegates request-target parsing to the `quic-go`/`http3` library), so the blast radius is exactly ONE production site (the `url.Parse(path)` call in `buildRequest`). The fix primitive is `url.ParseRequestURI` (measured at this tip: preserves `//foo`, keeps asterisk-form `*`, splits the query) — to be settled at the SPEC by prototype. The two reproduced arms (404 + the mis-route) are the row's RED anchors; the mis-route's proof must assert the ROUTED PATH, not just the status. Cost floor re-derived **~2-10 net production + ~120-400 test `.go`** — a FLOOR (the SPEC must ENUMERATE by prototype, differential harness plumbing included; the tenth consecutive `reference_measured_prototype_is_a_lower_bound` firing closed the phase-86 lineage). **Sentinel after-state RECORDED, not predicted, BOTH SIDES of the registration** — BEFORE at `want=118`: (1) SILENT · (2) SIX `:196 :202 :208 :218 :224 :232` · (3) SILENT. AFTER at `want=119` (input **237 lines / 119 data rows**): (1) **`NOT DONE: row 87`** ALONE · (2) SIX shifted uniformly +1 to **`:197 :203 :209 :219 :225 :233`** (the single inserted row at `:149` sits above all six windows) · (3) SILENT ⇒ the conjunction does NOT fire, `stop` NOT created (verified absent at repo root AND stage worktree, both sides). All four NCs fired both sides (row-62 doctoring => AFTER `NOT DONE: row 62` AND `NOT DONE: row 87` with `NC LANDED? [ in-progress ]` inspected first; want=118 => `GATE FAIL: examined 119 data rows, expected 118`; check-(3) doctoring residual 2 -> 0 => `NEVER OPENED: gRPC` with WASM silent; check-(2) one-arm 5/1, union 6). Every leak axis INVARIANT (lines 236 -> 237 · rows 118 -> 119 · union 6 · `-family row` 95/67 · `gRPC-family row` 2 · `Operational-tooling-family row` 3; new-slug occurrences 2, both inside row 87's own line; ARM-A flags {119, 131} UNAFFECTED — those rows sit above the `:149` insertion, not re-run with the fragile escape-aware command).
- **phase-directory:** `docs/envoy-go/phases/87-h2-double-slash-path-routing/` — `BRAINSTORM.md`, `PROGRESS.md` (new). **Still no `REVIEW.md`, the STANDING LINEAGE DEPARTURE — 37 of 127 phase dirs carry one, none since 25.3.** Prior: `docs/envoy-go/phases/86-validate-sds-nil-provider/` (CLOSED).
- **lifecycle-state:** **1** (phase 87 BRAINSTORM done; row 87 `in-progress` at `ROADMAP.md:149`). ⚠️ **Sentinel re-run MECHANICALLY, BOTH SIDES of the registration — it does NOT fire and `stop` was NOT created** (details in the active-phase bullet): post-registration check (1) `NOT DONE: row 87` at `want=119`, check (2) SIX, check (3) SILENT — the conjunction fails, and a next stage EXISTS (`next-skill: SPEC for phase 87`).
- **next-skill:** **SPEC for phase 87** — dispose the seven BRAINSTORM §4 open questions BY EXECUTION, centered on a compiling test-green prototype of the `buildRequest` fix (Q1 — `url.ParseRequestURI` vs manual `&url.URL{}` construction; enumerate origin-form / asterisk-form `*` / the `#fragment` divergence) and the reference-container re-verification of the `//foo`/`//foo/bar` expectation (Q4, name `b87-*`, tear down BY NAME). ⚠️ **Q5 is the load-bearing COST question:** `fixture.HTTPExpectations` is H1-only (`reference_differential_http_expectations_tcp_only`), so an H2 arm needs `Drive` hooks, and injecting a literal `:path = //foo` (x/net `http2.Transport` derives `:path` from `req.URL.RequestURI()` and may normalize) may need a raw HEADERS-frame writer or a `curl --path-as-is` subprocess. Draft `ADR-0309 §Context` STATUS `PROPOSED` (re-arms the strict guard 0 -> 1); enumerate cost by prototype (the §4.1 floor is a FLOOR).
- **last-commit:** the **phase-87 BRAINSTORM** commit. ⚠️ **LOCATE BY SUBJECT, NEVER BY POSITION** — `git log --grep '^phase 87 (h2-double-slash-path-routing) BRAINSTORM' --format='%h %s' | head -1`; a bare `git log --grep 'phase 87'` is CONTAMINATED (router/commit bodies discuss the phase). Base master `638ef78a`, branch `phase-87-brainstorm`; the stage landed as ONE squashed commit merged to master and pushed by the controller at close.
- **last-updated:** 2026-08-12 (phase-87 **BRAINSTORM done**; row 87 `in-progress`; `want` **119**; NO ADR; strict `PROPOSED` guard stays **0**). ⚠️ **THE LINEAGE EVICTION RULE WAS HONORED:** §Recent was at its five-entry cap, so **`phase 85 (h2spec-selector-repair) PLAN done`** (2026-08-09) — the UNIQUE OLDEST by a DIRECT READ of the five dates (2026-08-12, 2026-08-11, 2026-08-10 x2, 2026-08-09 — NO tie) — was moved to `STATE_HISTORY.md` **482 -> 484**, verified **STRICTLY APPEND-ONLY** (`git diff --numstat` = `2 0` AND the base file a byte-exact **PREFIX** via `cmp`). ⚠️ **THE ARCHIVE-ABSENCE GUARD used the whole-file fixed-string form** (the archive's own bullet anchor is `- **prior active-phase (evicted…):**`, NOT the §Recent `- **prior active-phase:**` form, so the §Recent-style bullet anchor reads 0 on present entries and is non-discriminating here): the target read **0** pre-append, against controls **re-derived FROM THE FILE**: `phase 82 (wasm-http-call-response-cache) IMPL done` (**12** — DRIFTED UP AGAIN from 11; a control's own count is a LIVE FIGURE), `phase 83 (wasm-pause-arm-leak) PLAN done` (**10**, up from 9, same mechanism), and an invented label (**0** — non-discriminating on its own, recorded as such); POST-append the target reads **1**.
- **next-free ADR:** **`ADR-0309`** — a BRAINSTORM adds no ADR, so `DECISIONS.md` is BYTE-UNTOUCHED at this stage (18098 lines · **307** `^## ADR-` headings · tail `## ADR-0308` COMPLETE · strict `^> \*\*STATUS: PROPOSED` guard **0** · STATUS census **21** · `^---$` **216**; next-free derived from the TAIL, never the count — headings+1 COLLIDES at the ADR-0209 gap). The phase-87 SPEC opens **ADR-0309 §Context** STATUS `PROPOSED` (re-arming the strict guard 0 -> 1), the H2 request-target parse correction; the `BEHAVIOR_CONTRACT.md ## HTTP/2` origin-form-path parity rule rides that ADR per the ADR-0052 discipline. ⚠️ **CARRY NO WHOLE-FILE COUNT OF THE LOOSE `PROPOSED` MATCHER** — every hit is a false positive now that no block is live.

---

## Project counts at this commit

*(Live. Re-verify at session start — `git fetch` first — and update here at every stage close. Canonical commands inline.)*

⚠️ **ALL counts below were re-run MECHANICALLY in the stage worktree at this phase-76 IMPL close with the canonical commands, EACH WITH ITS NEGATIVE CONTROL OBSERVED — never copied forward.** **Phase 76 landed +0 on EVERY axis, including +0 PRODUCTION `.go` FILES** — the first chartered row in this lineage whose production-code delta is exactly nothing, and the ANTICIPATED envelope held on all of them. The row's `.go` delta is **three TEST-side files** (`internal/cluster/ringhash_test.go` edited, `test/fixtures/0061-lb-ring-hash/driver/driver_test.go` edited, `.../linkage_test.go` NEW), plus three fixture-doc files and four project docs. ⚠️ **The stat-surface total 1205 is DOC-SOURCED and was NOT re-derived mechanically** — there is no mechanical command, and the chain is known to be discontinuous in **TWO** places (the recorded `1200 -> 1201`, and `Phase 46.1b` closing at 1198 while `Phase 47.1` opens at 1200 — an unattributed +2). **Only the +0 DELTA is asserted, and that argument is STRUCTURAL rather than a `statssink` result:** zero production `.go` bytes changed (G8) and zero byte mismatches over the 375-file production roster (G14), so no `NewCounter`/`NewGauge` call site can have moved; the five `TestNoNewStat*` guards corroborate at 5x PASS. ⚠️ **A future phase should COUNT the surface rather than trust the chain — a +0 row is a cheap place to do it, and phase 76 declined to charter that, so it is now a NAMED maintenance deferral rather than an idle observation.**

- **active differential fixtures:** **119** — ⚠️ **Phase 76 landed +0: `0061-lb-ring-hash` was EDITED, not added** (`sourceIPs` 4 -> 16 plus a NEW `driver/linkage_test.go` inside the EXISTING fixture directory — no new YAML, no new directory, no new BackendKind, no new port), so the count STAYS **119** and `0118` remains FREE. NC observed: `mkdir .../0118-fake` ⇒ 120. — `ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0117-tracing-custom-tags-metadata-cluster` (phase 73). **Phase 75 landed +0**: `0110-tls-require-client-cert-false` was **EXTENDED** with a cross-side `fixture.StatsAsserter` (NO new YAML, NO new directory, NO new BackendKind, NO new port), so the count STAYS **119** and `0118` remains FREE. ⚠️ **Next-free REFERENCE port is `10450`, NOT `10118`** — ports are NOT fixture-index aligned. **The fixture now asserts what its own README confessed was infeasible, and BOTH sides were EXECUTED**: `ssl.handshake=2 ssl.no_certificate=1 ssl.fail_verify_error=1 ssl.fail_verify_no_cert=0`, `downstream_cx_total=3`, **identical on reference and subject**, with the discriminating asymmetry (`handshake=2` vs `no_certificate=1`) observed per side. Shape A over `/stats/prometheus`, keyed on the metric NAME with `envoy_listener_address` IGNORED — the NAMES are byte-identical cross-side while the labels differ (`0.0.0.0_10446` reference vs **`___20016`** subject: ⚠️ **the subject binds the IPv6 WILDCARD**). ⚠️ **`0111` was NOT given a value assertion, deliberately** — it runs `require_client_certificate: true` on both sides, so a success-path no-cert annotation reads **0 on every arm, structurally**, and a value pin would document a **vacuous `0 == 0`** as coverage; its PROSE gained the name in its NAMED-UNASSERTED list instead, because **both `0111` passages are CLOSED ENUMERATIONS** and a name in neither list reads as ASSERTED. **`0111`'s driver is byte-untouched, and its named-subset check was EXECUTED for the first time at this IMPL: PASS.** ⚠️ `var _ fixture.StatsAsserter` is a **TRIPWIRE, not the dispatch mechanism** — `0110` is only the **THIRD** fixture repo-wide to carry it, while **82** define an `AssertStats` method.
- **fuzzers:** **55** — **Phase 76 landed +0** (it consumes no new config field, so there is no parse arm to fuzz). NC observed: appending `func FuzzProbeOnly` ⇒ 56. — `grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`. **Phase 75 landed +0** — the row consumes no new config field at all, so there is no parse arm and no fuzz-seed classification to flip (`reference_fuzzer_count_docs_drift`: an `f.Add` seed is not a fuzzer).
- **stat surface:** **1205** — BEHAVIOR_CONTRACT doc count. **There is NO mechanical counting command** (the contract says so itself); enforcement is per-phase `TestNoNewStat*` guards asserting a delta of ZERO, never the absolute total. **Phase 75 LANDED +1 (1204 -> 1205)** — `listener.<normalized-addr>.ssl.no_certificate`, the FOURTH listener-scope `ssl.*` name and the FIRST **SUCCESS-PATH** handshake annotation (⚠️ **NOT a synonym for `ssl.fail_verify_no_cert`, and the mis-mapping is wrong in BOTH directions** — one annotates an ACCEPTED handshake, the other books a REJECTED one; they are mutually exclusive on every connection). ⚠️ **The delta is a NAME-surface delta, NOT a flat per-deployment one:** registration rides the block that ALREADY gates on `rt.tlsMode`, so a **plaintext-only** deployment gains **ZERO** names, while a **QUIC** listener gains it permanently zero — **PARITY** with the reference, and structural (`Manager.Start` launches no accept loop for `kindQUIC`, so the Inc site is unreachable for a **name-independent** reason). ⚠️ **QUIC was NOT re-probed at phase 75** — the parity claim is INHERITED from phase 74's family-level probe, and that inheritance is stated in BEHAVIOR_CONTRACT rather than presented as a measurement. Ledger updated: `1204` -> `1205` at `BEHAVIOR_CONTRACT.md:831`/`:847`, plus a `**Phase 75 - 1204 -> 1205 (+1)**` line after the tail entry — **the ONLY line-adding edit in the file** (`5744 -> 5746`), so every by-line citation into the TLS subsection stays valid. ⚠️ **The new entry RECORDS BOTH ledger gaps and BACK-FILLS NEITHER** (fabricating a line would be inventing a record). Gate figures at this close: `grep -c '1204'` => **2** · `grep -c '1205'` => **3** · `grep -cE '^#{2,4} .*ADR-[0-9]+.*ADR-[0-9]+'` => **17** (was 16 — the `:1849` heading joined them, on the `:785` precedent). ⚠️ **`internal/statssink`'s four *"stays 1200 / 1196"* prose sites, stale since phase 49, are RECORDED-NOT-FIXED** — that package is on this phase's byte-untouched sha256 roster.
- **BackendKind tail:** **38** — **Phase 76 landed +0** (`0061` uses the in-process `TCPEcho` branch and implements neither `BackendKindAware` nor `PerHostBackendKind`). NC observed: appending `ZZProbeKind = 39` ⇒ tail 39. — (`H2GoawayResponder`, `test/differential/fixture/fixture.go:614`). **Phase 75 landed +0** — `0110` was extended, not cloned, and it implements neither `BackendKindAware` nor `PerHostBackendKind`, so the runner takes the in-process `TCPEcho` branch. ⚠️ **This is a TAIL VALUE, not a count** — the file declares **39** constants (values 0-38, `TCPEcho = 0`). The lineage figure **38** is correct as written; do NOT 'fix' it to 39.
- **DECISIONS.md tail:** **ADR-0298** (next-free **ADR-0299**) — `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1` -> `## ADR-0298`; `grep -c '^## ADR-0299'` ⇒ **0**. **ADR-0298 is COMPLETE as of the phase-76 IMPL** — §Context drafted at the phase-76 SPEC, §Decision + §Consequences APPENDED IN PLACE after the RETAINED italic footer, **no renumber and NO `---` separator** (the file's last `^---$` is still at `:17020`). Block shape controller-re-derived at this close: **1** `### Context` / **1** `### Decision` / **1** `### Consequences` / **1** retained footer. ⚠️ **FOUR of its OWN §Context paragraphs were CORRECTED IN PLACE** (¶1's `:295` -> `:307`, ¶5's `-count ~= 81` -> **83**, ¶7's *"~71 collapses"* -> **74/2000**, ¶11's *"needs no edit"* -> REFUTED) on the ADR-0297 ¶7/¶9 precedent, each **leading with what survives**; none changes a decision the ADR records. ⚠️ **It carries NO whole-file grep count and claims NO family ordinal** — the Load-balancing family closed at row 54, and **maintenance rows do not extend a charter**. *(Historical, retained: **ADR-0297 is COMPLETE** — §Context drafted at the phase-75 SPEC (ADR-0044-**as-used**; ⚠️ ADR-0044 does NOT actually contain that discipline), §Decision + §Consequences appended IN PLACE at the phase-75 IMPL after the **RETAINED** footer, mirroring ADR-0295/0296. Block shape verified at this close: **1** `### Context` / **1** `### Decision` / **1** `### Consequences` / **1** retained footer; `grep -c '^## ADR-0298'` => **0**. **No renumber, no new ADR.** ⚠️ **Its OWN §Context ¶7 and ¶9 were CORRECTED IN PLACE while completing it** — ¶7's **self-falsifying whole-file grep** (the same species the phase-74 IMPL had just fixed in ADR-0296 ¶3, reproduced one ADR later) is restated with **NO number** and the searched token deliberately **NOT spelled**, because every restatement mints another counter-example; and ¶9's *"SELF vs OTHER-ADR"* form rule is replaced by **graft SCALE** after the n=7 population was re-enumerated first-hand. ⚠️ **¶9 ALSO said *"the only two instances in the file"* — which this row's own blockquote makes three** — so a pure append would have shipped a SECOND self-falsifying count in the commit that fixed the first. **The prescription has now outlived THREE wrong justifications: family, self-vs-other, scale.** ⚠️ **`ADR-0296` is NO LONGER byte-untouched** — its §Decision (g) carries the phase-75 correction as an INDENTED blockquote (`grep -c '^  > '` **2 -> 3**), **LEADING WITH WHAT SURVIVES** (no such counter was owed *in phase 74*, whose sole stat-asserting fixture is `require=true` on both sides — **the deferral was RIGHT**) and claiming **NO ORDINAL**.)*
- **go.mod modules:** **Phase 76 landed +0** — `go mod tidy -diff` EMPTY and `git diff master -- go.mod go.sum` EMPTY; ⚠️ **the row's TEST-side import delta is +8 and that is CORRECT** (`ringhash_test.go` gains `fmt`/`math`/`mathrand "math/rand/v2"`, `linkage_test.go` adds `go/ast`/`go/parser`/`go/token`/`strconv`/`testing`) — all stdlib, all test-side, and **an import LINE is not a go.mod MODULE**; there is no new sub-package, so `reference_new_subpackage_pulls_transitive_module` does not bite. NC observed: a fake `require` ⇒ 67 -> 68. The tracked lineage figure is **2** (the phase-61.2 count: `quic-go` direct + `qpack` indirect) — **NOT a repo total**; the single `go.mod` requires **67** (18 direct + 49 indirect). **Phase 75 landed +0** — `go mod tidy -diff` EMPTY and `git diff master -- go.mod go.sum` EMPTY. The row needed `crypto/tls` and `internal/stats`, both ALREADY imported by `internal/listener/manager.go`; **an import LINE is not a go.mod MODULE**, and there is no new sub-package, so `reference_new_subpackage_pulls_transitive_module` does not bite. ⚠️ **TEST-side imports grew by exactly four** (`log`, `math`, `net/http`, `strconv` in `0110`'s driver) — all stdlib, all test-side, **not a production-envelope violation**.
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` (see `ENVOY_TARGET.md`).
- **conformance:** h2spec **`95 tests, 94 passed, 1 skipped, 0 failed`** (the skip is 6.9.2/2, invariant) — the CORRECTED 95-case strict scope under dotted selectors, measured at the phase-85 IMPL close 2026-08-10 (ADR-0307; the historical 53/53 records measured only 53 of the 95 under nine silently no-op slash-form selectors, 2026-04-25 -> 2026-08-10); proxy-wasm all 10 families PASS.

---

## Recent lineage

*(A SHORT recent lineage, capped at FIVE prior entries by ADR-0288. Everything earlier is in `STATE_HISTORY.md`. §Current carries the newest stage (**the phase-87 BRAINSTORM** — the SELF-PICKED open of the H2 double-slash path-routing repair: row 87 registered `in-progress`, `want` bumped 118 -> 119, the defect reproduced by execution, check (1) re-opened to `NOT DONE: row 87`). The five below are the prior stages, newest first. ⚠️ **EVICTED AT THIS CLOSE:** `phase 85 (h2spec-selector-repair) PLAN done` (2026-08-09) — the UNIQUE OLDEST by a DIRECT READ of the five dates (2026-08-12, 2026-08-11, 2026-08-10 x2, 2026-08-09 — NO tie). It is now in `STATE_HISTORY.md`, appended strictly (482 -> 484, `git diff --numstat` = `2 0`, base a byte-exact prefix via `cmp`).)*

- **prior active-phase:** `phase 86 (validate-sds-nil-provider) IMPL done` (2026-08-12) — lifecycle-state **3 -> DONE**; ⚠️ **PHASE 86 CLOSED, ROW 86 FLIPPED `done`** at `ROADMAP.md:148` (the ONLY ROADMAP edit, `git diff --numstat` = `1 1`, `want` STAYS **118**, every leak axis INVARIANT by whole-file count: lines 236 · rows 118 · union 6 · `-family row` 95/67 · `gRPC-family row` 2 · `Operational-tooling-family row` 3; the Op-tooling "RTDS/SDS validate companion" sentence BYTE-UNTOUCHED). **Sentinel after-state RECORDED, not predicted: (1) SILENT — the THIRD silent reading in project history · (2) SIX at `:196 :202 :208 :218 :224 :232` · (3) SILENT ⇒ the conjunction does NOT fire, `stop` NOT created** (verified absent at repo root AND stage worktree, BOTH sides of the flip); all four NCs fired BOTH sides (row-62 doctoring => pre-flip `NOT DONE: row 62` AND `row 86` / post-flip `row 62` ALONE — the proof the silent side still LOOKS — with `NC LANDED? [ in-progress ]` inspected first; want=117 => `GATE FAIL: examined 118 data rows, expected 117`; check-(3) doctoring residual 2 -> 0 => `NEVER OPENED: gRPC` with WASM silent; check-(2) one-arm 5/1, union 6). Base master **`b2b5be7e`**, branch `phase-86-impl`, ONE squashed commit (D-86-SEQ held: lift + guards + guard-preservation NCs + docs + flip atomic). **THE HEADLINE: `--mode validate` now ACCEPTS all three SDS-bound downstream TLS shapes exactly where boot accepts them — validate accepts iff boot accepts, MODULO exactly the rejects that require I/O to discover (D-86-N5) — demonstrated by the full eleven-shape probe battery at THIS tip (accepts: control/A/B/C/n5 `configuration OK` exit 0; rejects: n1/n2/n4/n6/n7 message-TRANSITIONED to the boot strings, n3 already-parity) AND a per-shape parity-string diff vs normal-mode boot (core messages BYTE-IDENTICAL, both raw lines recorded per shape).** Landed production (+150 net `.go` over 5 files, INSIDE the PLAN's 117-165 band): `internal/xds/provider_nofetch.go` (the no-fetch sentinel; `IsNoFetch(nil)==false` pinned NC-8; two never-called defense-in-depth strings NC-9) · the `internal/boot` `newSDSProviderAndClient` split + `NewValidateSDSProvider` (the ENTIRE body moved — reviewer-verified 89-vs-89 lines with ONLY the seven return sites differing, parity by CODE-PATH; the never-dialed lazy client CLOSED, D-86-CONN — ⚠️ that Close is UNGATED, deleting it stays green, a named future-maintenance candidate) · three `internal/tls` `IsNoFetch` fetch-site skips + the `sdsCertPromised` interplay (the empty-cert reject's BYTES untouched, its gate gains `&& !sdsCertPromised`) · `validate/validate.go` threading. Tests +1116 net `.go` over 6 files — ⚠️ **OVERRAN the PLAN §7 ceiling (760) by 356: the TENTH consecutive `reference_measured_prototype_is_a_lower_bound` firing, RECORDED in ADR-0308 §Consequences (iii), not absorbed.** TDD held: eleven-shape RED census with failure lines read; accept arms RED with the sentinel's defense-in-depth fetch string (a DISCRIMINATING red); n3 green-on-arrival LABELED a regression pin; the three pins (base anchors `config_test.go:921/:1198/:1310` = tip `:966/:1402/:1514` after pure insertions) BYTE-UNTOUCHED, verified green AND by zero-deletion diff. **FIVE execution findings recorded:** (a) PLAN §6's n2 recipe was AMBIGUOUS — "two positions" means TWO SDS-bound CONTEXTS (the boot cross-context cap), not two entries in one list (which trips `ParseSDSConfig`'s DIFFERENT per-context string; census fix round 1); (b) **n3's WRAP format changed** — listener-wrapped pre-fix, bare pre-scan post-fix, frozen substring unchanged, true call-site parity only post-fix (ADR-0308 §Consequences vi); (c) ⚠️ **"validate never passes nil" is FALSE unscoped** — at seen==0 validate threads `(nil, nil)` exactly like boot, so the nil-consumer roster is TWO unconditional + ONE conditional (caught at the T5 review; scoped in every landed comment AND the contract text; the negative form is ON THE RECORD in ADR-0308); (d) three ADR-0308 citation defects caught pre-squash (a wrong `internal/xds/xdsgrpc` path for the Close pins — the SAME wrong path lives in SPEC-86/PLAN-86 prose, prior-stage records NOT rewritten, the ADR carries the correction; stale base-tip pin locators now dual-labeled; a RETIRED `require_client_certificate` gate nearly re-inscribed — ADR-0289 says the VC-SDS fetch is un-gated); (e) two `:518` locators minted stale WITHIN the branch, fixed by the final-review fix wave (cite-by-string now). Gates: differential **121/121 `INNER_EXIT=0`**, anchored panic gate silent, 392.3 s · `go test ./...` rc=0 (the close-verification re-run flaked `internal/boot` ONCE — identity uncaptured, a recorded lapse; cleared package x4 + scoped x5 + full-suite green run 2; likely SDS dial-budget class, UNCLASSED) · `-race` clean on the four production-touched packages · gofmt + golangci-lint clean on six · counts each with its NC observed: fixtures **121** +0 · fuzzers **55/48** +0 · BackendKind tail **38** +0 · `go mod tidy -diff` EMPTY · `go list -deps ./validate` edge set UNCHANGED (base-vs-tip sorted diff empty) · **stat-surface DELTA 0 by same-command census both sides (`NewCounter(|NewGauge(` form: 145/21 in BOTH trees)**. ⚠️ One probe caveat carried honestly: `validate_test.go`'s `deadPort` helper transiently binds an ephemeral loopback socket (closed, never dialed) — "validate binds nothing" is true of the PRODUCT, not of that one test helper. Method: **subagent-driven per `feedback_execution_style` — a NAMED RETURN from the 86-lineage inline departure** (6 implementers + 6 task reviewers + 4 scoped re-reviews + a gates agent + a most-capable-model final whole-branch review; 3 fix rounds + 1 final fix wave, ALL findings addressed or ledgered with rulings; the controller ran the sentinel battery itself and squashed).

- **prior active-phase:** `phase 86 (validate-sds-nil-provider) PLAN done` (2026-08-11) — lifecycle-state **2 -> 3**; **ROW 86 STAYS `in-progress`** at `ROADMAP.md:148`, `ROADMAP.md` **BYTE-UNTOUCHED** (verified by empty diff against master), sentinel `want` stays **118**. Base master **`23ad1232`** (from `git rev-parse master`), branch `phase-86-plan`. Docs-only: ZERO production `.go`, ZERO test `.go`; `DECISIONS.md` and `BEHAVIOR_CONTRACT.md` **BYTE-UNTOUCHED** (a PLAN adds no ADR; strict `PROPOSED` guard **STAYS 1** — the live pointer the phase-86 IMPL disarms). ⚠️ **NAMED DEPARTURE CONTINUES: no investigation agents — the PLAN's cheap probes ran INLINE by the controller** (tip binary built with `-o` into session scratch; SEVEN `--mode validate` executions — arms A/B/C + static control + n1/n3/n7; ports 47200-47203 TEMPLATE VALUES ONLY, nothing bound; no docker). **THE RED ANCHORS RE-PROVEN AT THIS TIP WITH FAILURE LINES READ:** arms A/B/C exit 1 with the three reject strings — each wrapped `listener: %q: filter_chains[%d]:`, so the IMPL's assertions are MANDATED to be substring (`strings.Contains`), never full-string — and the static control `configuration OK` exit 0; n1/n7 confirmed MASKED (they emit the arm-A string today — the message-transition RED side), n3 confirmed already-parity (the `DELTA_GRPC` boot string, SPEC §10.5's nuance). **NO SPEC CLAIM MOVED — recorded as a measurement, not praise.** **PLAN.md landed the full SPEC-§13 payload:** EIGHT ordered IMPL tasks under D-86-SEQ (ONE leg, one atomic commit: Task 0 eleven-shape RED census -> T1 `internal/xds/provider_nofetch.go` -> T2 the boot `newSDSProviderAndClient` split + `NewValidateSDSProvider` -> T3 the three tls fetch-site skips + the `:518` `sdsCertPromised` interplay -> T4 validate threading + validate_test/main_test arms -> T5 consumer-comment rewrites -> T6 docs (contract edits + ADR-0308 completion + the ROADMAP flip) -> T7 gates, TDD order held INSIDE the commit with every RED line read); **the guard-preservation NC roster** (the three pins `config_test.go:921/:1198/:1310` BYTE-UNTOUCHED — verified both by green runs AND by no-diff-hunk-overlap — plus NINE new NCs including the QUIC-wrapped SDS shapes NC-4/NC-5 and `IsNoFetch(nil)==false`); **the §6a-6d contract-edit TEXT drafted** (the `:1062` REVERSAL paragraph, the `:1050` THREE->TWO production-consumer edit, the `:1034` roster drop, the `:948-958` SDS-acceptance extension) + the ADR-0308 §Decision/§Consequences completion plan (strict guard 1 -> 0 at the IMPL; `^---$` stays 216); the frozen error-string constraint set (ZERO landed reject strings change; only the sentinel's two never-called defense-in-depth strings are new); the eleven probe-shape regeneration recipe (fixture-0103/0108/0109-derived, ports 47200-47299 template-only); **budget per-task enumerated ~117-165 net production / ~440-760 test `.go` ON TOP of the SPEC's ~110-160/~400-680 floor** — the enumeration's top end already exceeds the SPEC band on the test side, consistent with NINE consecutive `reference_measured_prototype_is_a_lower_bound` firings; overrun beyond is a RECORDABLE finding for ADR-0308 §Consequences. One form-dependence finding recorded: the carried ARM-A {119, 131} figure is NOT reproducible by ad-hoc field-count forms (`NF != 8` flags 17 rows) — it binds only with its escape-aware pipe-split command (BRAINSTORM-86 §6); a PLAN's binding invariance proof is the empty `ROADMAP.md` diff, verified at this close. **Sentinel after-state RECORDED, not predicted: (1) `NOT DONE: row 86` at `want=118` — the single expected line, denominator silent · (2) SIX at `:196 :202 :208 :218 :224 :232` · (3) SILENT ⇒ the conjunction does NOT fire, `stop` NOT created** (verified absent at repo root AND stage worktree); all four NCs fired (row-62 doctoring => `NOT DONE: row 62` AND `NOT DONE: row 86` with `NC LANDED? [ in-progress ]` inspected first; want=117 => `GATE FAIL: examined 118 data rows, expected 117`; check-(3) doctoring residual 2 -> 0 => `NEVER OPENED: gRPC` with WASM silent; check-(2) one-arm 5/1, union 6); leak axes INVARIANT (`-family row` 95/67 · `gRPC-family row` 2 · `Operational-tooling-family row` 3).

- **prior active-phase:** `phase 86 (validate-sds-nil-provider) SPEC done` (2026-08-10) — lifecycle-state **1 -> 2**; **ROW 86 STAYS `in-progress`** at `ROADMAP.md:148`, `ROADMAP.md` **BYTE-UNTOUCHED** (verified by empty diff against master), sentinel `want` stays **118**. Base master **`11f5a92c`** (from `git rev-parse master`), branch `phase-86-spec`. Docs-only: ZERO production `.go`, ZERO test `.go`. ⚠️ **NAMED DEPARTURE CONTINUES (BRAINSTORM-86 precedent): no investigation agents — every probe INLINE by the controller**, and this SPEC's centerpiece is **a COMPILING, TEST-GREEN PROTOTYPE of the chosen mechanism built in a DETACHED worktree** (`wt-86-proto` at the tip, diff captured to session scratch, worktree DELETED at close) — the cost was MEASURED, not estimated. **ALL SEVEN BRAINSTORM-§4 QUESTIONS DISPOSED (SPEC.md §1-§7):** **Q1 DECIDED — D-86-MECH, the no-fetch sentinel provider:** `xds.NoFetchProvider()`/`IsNoFetch()` (the discriminator is the SENTINEL, never `provider == nil`), `boot.NewValidateSDSProvider` reusing the ENTIRE pre-scan via the `newSDSProviderAndClient` split (parity by CODE-PATH — future arms inherited by construction; the never-dialed lazy client CLOSED, D-86-CONN), three fetch-site skips in `internal/tls` + the `:518` `sdsCertPromised` interplay (D-86-NOFETCH-CERT — UNENUMERATED at the BRAINSTORM, found only by executing the prototype). Prototype measured **gross +102/-14, net +88 production `.go` across 4 files**; ALL ELEVEN probe shapes behave correctly against it (3 arms + control ACCEPT; the 6 build-time negatives emit the BYTE-IDENTICAL boot strings; the dead-endpoint shape ACCEPTS — the modulo-fetch case); **six packages' full test suites green including the three existing nil-reject pins UNTOUCHED** (`config_test.go:921/:1198/:1310`). Option (b) mode-flag threading measured DEAD (~108 call sites over five signatures: 2+36+2+52+16); option (c) placeholder material REJECTED (fabricates state). **Q2 BY EXECUTION — the boot-parity surface is SIX build-time arms + ONE fetch-time class:** node, one-secret cap, ParseSDSConfig, secret-name charset, **cluster-existence AND cluster-H2-options (n7 — ABSENT from the BRAINSTORM enumeration, a fresh under-enumeration specimen)** — all 5-9 ms, I/O-free (`grpc.NewClient` lazy); the dead-endpoint shape is the ONLY exempt class (**D-86-N5: validate accepts iff boot accepts, modulo exactly the rejects that require I/O to discover**). **Q3 EXECUTED — the reference record is no longer carried:** `contrib-v1.37.2` validates all four arm/control shapes `configuration OK`; n1 (node absent) is a **BOTH-SIDES reject** (`TlsCertificateSdsApi: node 'id' and 'cluster' are required` — the reference RUNS SDS checks in validate mode without dialing); n2-n7 reference-accept = the boot path's PRE-EXISTING documented departures inherited by parity, **no new departure minted** (D-86-REF). **Q4 — the sweep found `BEHAVIOR_CONTRACT.md:1062` recording the OPPOSITE decision as permanent ("diverges, and stays that way"), its rationale REFUTED BY EXECUTION; ADR-0308 is the mandated vehicle for its REVERSAL** plus the `:1034`/`:1050` consumer-roster edits and the `:948-958` extension; the phase-60.2 no-DIAL decision SURVIVES; production comment rewrites enumerated (`config.go:344-346/:407-435/:447-452`, `manager.go:569-573`). **Q5** placement (tls arms + boot negatives + validate end-to-end + main_test CLI subprocess; RED anchors = the three arm rejects re-reproduced at THIS tip; NO differential fixture). **Q6** ZERO landed reject strings change (pins stay; boot-parity strings byte-identical by code-path reuse; only the sentinel's two never-called defense-in-depth strings are new). **Q7** each +0 re-checked POST-change: `go list -deps ./validate` already carries `internal/xds`+`xdsgrpc` (the carried claim now VERIFIED), `go mod tidy -diff` clean, fuzzers 55/48, fixtures/BackendKind +0, **stat-surface DELTA 0 by same-command census both sides (145/21 — a DIFFERENT counting form than the carried 208/36; no absolute corrected, only the delta asserted)**. ⚠️ **COST REFUTED UPWARD — the NINTH consecutive `reference_measured_prototype_is_a_lower_bound` firing:** IMPL budget **~110-160 net production + ~400-680 test `.go`** vs the BRAINSTORM's ~50-90/~150-300 floors (unenumerated: n7, the `:518` interplay, the consumer-comment rewrites, the client-close plumbing). **D-86-SEQ: ONE IMPL leg, one atomic commit** (lift + guards + guard-preservation tests together — `reference_lifted_reject_hidden_enforcement` engaged prospectively). **Sentinel after-state RECORDED, not predicted: (1) `NOT DONE: row 86` at `want=118` — the single expected line · (2) SIX at `:196 :202 :208 :218 :224 :232` · (3) SILENT ⇒ the conjunction does NOT fire, `stop` NOT created** (verified at repo root AND worktree); all four NCs fired (row-62 doctoring => `row 62` AND `row 86` with `NC LANDED? [ in-progress ]` inspected first; want=117 => `GATE FAIL: examined 118 data rows, expected 117`; check-(3) doctoring 2 -> 0 => `NEVER OPENED: gRPC` with WASM silent; check-(2) one-arm 5/1, union 6); leak axes INVARIANT (`-family row` 95/67 · `gRPC-family row` 2 · `Operational-tooling-family row` 3 · ARM-A flags {119, 131} only).

- **prior active-phase:** `phase 86 (validate-sds-nil-provider) BRAINSTORM done` (2026-08-10) — lifecycle-state **DONE -> 1**; **ROW 86 REGISTERED `in-progress`** at `ROADMAP.md:148` and the sentinel `want` bumped **117 -> 118 in the SAME commit** (the phase-84/85 precedent). Base master **`766d98ad`** (from `git rev-parse master`), branch `phase-86-brainstorm`. Docs-only: ZERO production `.go`, ZERO test `.go`. **SELF-PICKED** per the 2026-07-12 standing directive — no banked mid-lifecycle work existed (phase 85 CLOSED, all 117 prior rows `done`, check (1) SILENT — the second silent reading in project history — before this stage re-opened it at row 86). ⚠️ **NAMED DEPARTURE from the BRAINSTORM-84/85 three-agent pattern: the probes were run INLINE by the controller** (binary built at `766d98ad` with `-o` into session scratch; four `--mode validate` CLI executions; code reads and greps; NOTHING in the repo tree edited by any probe). ⚠️ **THE HEADLINE: the `--mode validate` SDS divergence is THREE-ARMED, not one-armed — ALL THREE ARMS REPRODUCED BY EXECUTION AT THIS TIP**: `tls_certificate_sds_secret_configs` => exit 1 `SDS-delivered certificate requires a live SDS provider (unavailable in this mode)` (`internal/tls/config.go:387-389`) · `validation_context_sds_secret_config` => exit 1 (`config.go:436-438`) · `combined_validation_context` => exit 1 (`config.go:453-455`) — all three gated on `provider == nil`, `validate/validate.go:48-49` threads a LITERAL nil into `boot.Construct`, while the ordinary boot path HONORS all three shapes (phases 60.2/65/66; `cmd/envoy-go/main.go:156` builds a live provider); the static-TLS positive control returned `configuration OK` exit 0, so the rejects are not vacuous. TWO findings beyond the carried record: **a pure reject-lift is wrong in BOTH directions** (UNDER-rejects without boot-parity — `boot.NewSDSProvider`'s pre-scan enforces the node arm-7 requirement, the one-secret cap, and the ParseSDSConfig arms, none of which validate runs; OVER-lifts if keyed on `provider == nil`, whose OTHER consumers — QUIC `manager.go:567` and the exported test-only constructors — must keep rejecting BYTE-IDENTICAL, `reference_lifted_reject_hidden_enforcement` engaged PROSPECTIVELY), and **both fetch sites must be skipped** (`config.go:390` + `NewDownstreamConfig`'s require_client_certificate block), preserving the phase-60.2 no-dial decision. Coverage hole MEASURED ZERO (no SDS test in `validate/validate_test.go` 432 lines or `cmd/envoy-go/main_test.go` 1502 lines); the three rejects are the row's RED anchors. Cost floor re-derived **~50-90 net production + ~150-300 test `.go`** (the carried one-arm ~30-40/60-120 REFUTED as a central estimate; eighth consecutive `reference_measured_prototype_is_a_lower_bound` firing — the SPEC must ENUMERATE by prototype). **Sentinel after-state RECORDED, not predicted: (1) `NOT DONE: row 86` — the single expected line while the phase is open · (2) SIX at `:196 :202 :208 :218 :224 :232` · (3) SILENT ⇒ the conjunction does NOT fire, `stop` NOT created** (verified absent at repo root AND stage worktree, before and after); all four NCs fired on BOTH sides of the edit (row-62 doctoring => before `NOT DONE: row 62` ALONE / after `row 62` AND `row 86`, with `NC LANDED? [ in-progress ]` inspected first; `want` off-by-one both directions => `GATE FAIL`; check-(3) doctoring 2 -> 0 => `NEVER OPENED: gRPC` with WASM silent; check-(2) one-arm 5/1, union 6); every leak axis INVARIANT (lines 235 -> 236 · rows 117 -> 118 · union 6 · `-family row` 95/67 · `gRPC-family row` 2 · `Operational-tooling-family row` 3 · ARM-A flags {119, 131} only — row 86 at `:148` clean; new slug occurrences 2, both inside row 86's own line).

- **prior active-phase:** `phase 85 (h2spec-selector-repair) IMPL done` (2026-08-10) — lifecycle-state **3 -> DONE**; ⚠️ **PHASE 85 CLOSED, ROW 85 FLIPPED `done`** at `ROADMAP.md:147` (the ONLY ROADMAP edit, `git diff --numstat` = `1 1`, `want` STAYS **117**, every leak axis INVARIANT by whole-file count: lines 235 · rows 117 · union 6 · `-family row` 95/67 · `gRPC-family row` 2 · `Operational-tooling-family row` 3 · ARM-A flags 119+131 only). **Sentinel after-state RECORDED, not predicted: (1) SILENT — the SECOND time in project history · (2) SIX at `:195 :201 :207 :217 :223 :231` · (3) SILENT ⇒ the conjunction does NOT fire, `stop` NOT created** (verified absent at repo root AND stage worktree); all four NCs fired post-flip (row-62 doctoring => `NOT DONE: row 62` ALONE with `NC LANDED? [ in-progress ]` inspected first; want=116 => `GATE FAIL: examined 117 data rows, expected 116`; check-(3) doctoring 2 -> 0 => `NEVER OPENED: gRPC` with WASM silent; check-(2) one-arm 5/1, union 6). Base master `4b427fe1`, branch `phase-85-impl`, ONE squashed commit (D-85-SEQ). **THE HEADLINE: THE H2SPEC GATE NOW RUNS ITS TRUE 95-CASE SCOPE AND IS GREEN — `95 tests, 94 passed, 1 skipped, 0 failed` (skip 6.9.2/2, invariant), run LAST as the commit's gate evidence — and the "NO ROW MAY CITE h2spec 53/53" caveat is RETIRED** (ADR-0307 §Consequences). Landed: production THREE files in `internal/filter/hcm/h2/` (shared `validateSetting` on BOTH read paths with validate-before-apply — ENABLE_PUSH ∈ {0,1}, MAX_FRAME_SIZE ∈ [2^14, 2^24-1] -> PROTOCOL_ERROR, IWS > 2^31-1 -> FLOW_CONTROL_ERROR as 3-line defense-in-depth behind x/net's parse-time guard; `readClientSettings` preserves the framer's own ConnectionError code via `translateFramerErr` reuse + `Run` step-3 own-code emission — the wrong-code HANDSHAKE defect fixed; the IWS=0 announced-flag quirk fix with seeding and the walk's effective-old sharing ONE computation; `window.adjust` single critical section; the §6.9.2 live-stream walk with the conn-level window untouched); harness (NINE selectors slash->dot, JUnit `ClassName`/`Error` parse fix — h2spec emits `<error>` never `<failure>` — the 31-entry `expectedSuites` roster summing to 95, three guard layers + the REVERSE layer-2 direction, the zero-case skip DELETED, the 6.6 comment rewritten); the ~13-line CI enrollment in the `differential` job; docs (ADR-0307 COMPLETE in place, `CONFORMANCE_PINS.md` append-only +103 with the 31-row corrected table, `BEHAVIOR_CONTRACT.md` net-zero `2 2` at `:2054`/`:2056`). TDD held: 11 arms re-proven RED at THIS tip with failure lines read (census re-verified by the task reviewer's own execution), 2 green-on-arrival regression pins labeled as pins, 3/3 guard NCs fired with correct layer attribution and restores sha256-verified. ⚠️ **COST OVERRAN THE BUDGET: realized net +1046 `.go`** (production +143, harness +79, tests +824 in TWO new files) vs the PLAN's ~760-900 — **the EIGHTH consecutive `reference_measured_prototype_is_a_lower_bound` firing**, cause again under-ENUMERATION (the IMPL's fuller test arms: 824 vs the prototype's 504-in-one-file). ⚠️ **TWO PLAN claims did not survive execution:** §8 item 7's "24 suites incl. six 6.x rows" pins-table instruction was STALE against the PLAN's own §1.4 (the landed table is 31 rows / 13 6.x rows, re-derived independently from the roster literal AND the captured XML); the ~760-900 budget was itself a lower bound. Gates: differential **121/121 `INNER_EXIT=0`**, 0 FAIL/SKIP, 0 `no driver registered`, anchored panic gate silent, 382.9 s · `go test ./...` rc=0 on run 2 (⚠️ run 1: `TestServerConn_TinyWindowDelivery` i/o-timeout at 10 s under full-suite load — a NEW unclassed flake, cleared scoped x5 + full-package x3 + full run 2; recorded, not fixed) · `-race` full h2 package ok · stat surface **+0 by call-site enumeration: ARM-1 silent with input measure 236, census 208/36 at base AND tip** · fixtures STAY 121 · fuzzers 55 · BackendKind tail 38 · go.mod +0.



## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
