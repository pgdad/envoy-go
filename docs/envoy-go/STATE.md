# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `24.1-global-ratelimit-core-and-route-table` — **PLAN-time SPLIT FIRED 2026-05-22 at this commit (ADR-0201); awaiting the 24.1 PLAN** (next session, skill `superpowers:writing-plans` to author `docs/envoy-go/phases/24.1-global-ratelimit-core-and-route-table/PLAN.md` + run the plan-document-reviewer loop). Phase 24 (`envoy.filters.http.ratelimit`, the GLOBAL rate-limit filter; SEVENTEENTH §9 row) is now the PARENT over **24.1** (core decision path + DELTA-2 + PARSE-REJECTs) + **24.2** (remaining actions + X-RateLimit headers + `RateLimitPerRoute` + stage + vh-Axis-B). Phase `23-http-filter-admission-control` DONE and CANONICAL — DO NOT reopen. **Why the split:** the parent SPEC §1.2/§3.4 post-empirical LoC envelope is **~1900–2700 (ABOVE the ADR-0045 ~1500 split-gate)**; per BRAINSTORM Q1 the split disposition was DEFERRED to PLAN time, and this PLAN session applied the state-2 GATE along the SPEC §16 core-path/remaining-surface axis. The FIRST **PLAN-time** split (vs phase-18/19 SPEC-time + phase-22 BRAINSTORM-time). **The parent SPEC empirical AMENDs are SETTLED** (the 11-entry AMEND-1..11 catalog: AMEND-1 cluster-scope stats `cluster.<rls>.ratelimit[.<stat_prefix>].{ok,error,over_limit,failure_mode_allowed}` 110→114; AMEND-2 no runtime proto fields; AMEND-4 inert `override_option`; AMEND-6 descriptor `hits_addend` `UInt64Value`; AMEND-9 DELTA-2 chain-seeded `RouteRateLimits()`/`VirtualHostRateLimits()` accessor pair HIGH-risk; AMEND-10 cross-namespace `NewCounterIfAbsent` write) — do NOT re-derive. **24.1 scope:** the package skeleton + boot-registration (19 HTTP filters) + `compiledConfig` + DELTA-1 `RateLimitClient` + the 5 CORE actions (`generic_key`/`request_headers`/`remote_address`/`destination_cluster`/`header_value_match`) + `ShouldRateLimit` dispatch + OK/OVER_LIMIT/error dispositions + the cluster-scoped 4-counter stat surface (110→114) + DELTA-2 route-table exposure + the FULL §5 PARSE-REJECT roster (departures 15→18) + the 33rd fuzzer + `0032`(a/b/c/d-core/e/h) + `0033` (fixtures 33→35). **24.2 (deferred, planned):** remaining 5 actions + X-RateLimit DRAFT_VERSION_03 + `RateLimitPerRoute` 10th-canonical + ADR-0125 9→10 + `stage` + Axis-B `vh_rate_limits` + `0032`(f/g). §9 family closes from 2 remaining rows to **1 remaining** (`wasm`) at 24.2 phase-done.
- **phase-directory:** `docs/envoy-go/phases/24.1-global-ratelimit-core-and-route-table/` (SPEC.md authored at this split commit; PLAN.md + PROGRESS.md + REVIEW.md not yet authored). Sibling `docs/envoy-go/phases/24.2-global-ratelimit-perroute-and-headers/` (SPEC.md authored; `planned`, opens after 24.1 phase-done). Parent `docs/envoy-go/phases/24-http-filter-global-ratelimit/` (BRAINSTORM.md + the RETAINED parent master SPEC.md, annotated with the split-fired note). Phase 23 directory is complete.
- **lifecycle-state:** `phase 24.1 SPEC done (carved at split); awaiting 24.1 PLAN` (SKILL_ROUTING state 2 → next is `superpowers:writing-plans` to author `24.1-.../PLAN.md` + the plan-document-reviewer loop ≤3 iterations). ROADMAP row `24` is the PARENT (`in-progress`, `sub-phases = 24.1, 24.2`); row `24.1` `in-progress` (ships first); row `24.2` `planned` (depends-on 24.1). The parent row `24` flips `in-progress → done` at 24.2's phase-done (rollup per the 18/19/22 precedent).
- **next-skill:** `superpowers:writing-plans` (to author the **24.1** `PLAN.md` from `docs/envoy-go/phases/24.1-global-ratelimit-core-and-route-table/SPEC.md`; decompose into bite-sized TDD tasks, anchor ADR-0197[core]/ADR-0198/ADR-0200 §Decision+§Consequences bodies to specific Tasks, run the plan-document-reviewer loop; subagent-driven execution applies at the 24.1 IMPL session, not at PLAN authoring).
- **last-commit:** `<<TBD-fill-post-split-commit>>` (squash-merge of branch `phase-24-http-filter-global-ratelimit-plan` from worktree `.worktrees/phase-24-http-filter-global-ratelimit-plan/` — the PLAN-time ADR-0045 split into 24.1 + 24.2; docs-only [parent SPEC annotation + 2 sub-phase SPEC slices + ROADMAP parent/sub-rows + ADR-0201 + STATE + next-prompt]; this SHA-fill follow-up backfills the placeholder per the phase-09..23 stage-close pattern). Predecessor master tip: `dc26d88` — `next-prompt.txt: repoint master-tip references to e834e18 (actual HEAD)`.
- **last-updated:** 2026-05-22
- **next-free ADR:** `ADR-0202` (ADR-0201 CONSUMED at this split commit as the ADR-0045 split-application ADR — the FIRST PLAN-time split, mirroring ADR-0164/ADR-0084; full body landed). DECISIONS.md tail at `ADR-0201`. The 4 phase-24 §Context drafts ADR-0197..0200 stay anchored at the parent SPEC commit (their `Lands-in` lines map ADR-0197 core→24.1 / headers→24.2; ADR-0198→24.1; ADR-0199→24.2; ADR-0200→24.1); §Decision + §Consequences bodies land at each sub-phase IMPL. **D-hypothesis (re-mapped):** `ADR-0202` stays UNCONSUMED at phase-24 phase-done — HOLD-with-known-risk, LARGER surprise surface than phase-23 (the highest-risk DELTA-2 chain-seed type per SPEC §12 item 1 could fire it at 24.1 IMPL; the X-RateLimit MIN-status byte-edge / `metadata`-action accessor at 24.2 IMPL). Canonical-per-route roster STAYS 9 (the 9 → 10 amendment is ANCHORED in ADR-0199 §Context but LANDS at 24.2 IMPL).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
