# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

> **This file is a POINTER, not an archive** — `BOOTSTRAP_PROMPT.md` §4: *"STATE.md — pointer: active phase, last commit, next action."* It carries the live pointer, the live counts, and a SHORT recent lineage. **Full stage-by-stage history lives in `STATE_HISTORY.md`** (archived at ADR-0288; **nothing was deleted** — the pre-move file is preserved there verbatim, byte-for-byte).
>
> **⚠️ THE RULE ADR-0288 EXISTS TO ENFORCE: there is EXACTLY ONE live `next-skill:`, `lifecycle-state:`, `next-free ADR:`, and count block IN THIS FILE, and they live in §Current pointer directly below.** Before that move, ~330 lines of prepended backlog had buried TWO fossilised "current state" blocks whose `next-skill:` still read *"the 42.2 hedging BRAINSTORM"* and *"the 46.2 SPEC"* while the live phase was **66** — so `grep 'next-skill:' STATE.md` returned **two stale hits and ZERO live ones**, and `grep 'fuzzers:'` returned **48** and **42** when the true count was **55**. **A session may trust a grep of this file again. Keep it that way: when you advance the pointer, EDIT §Current pointer IN PLACE — never prepend a new block above it.**

---

## Current pointer

*(The ONLY live "what next" in this repo. Update IN PLACE at every stage close.)*

- **active-phase:** `phase 71 (tracing-custom-tags-metadata-route) IMPL done` (2026-07-22) — **ROW 71 `done`**
- **phase-directory:** `docs/envoy-go/phases/71-tracing-custom-tags-metadata-route/` (`BRAINSTORM.md` + `SPEC.md` + `PLAN.md` + `PROGRESS.md` present; **row 71 is `done`** — flipped at the phase-71 IMPL six-gate, ADR-0106, the SOLE leg)
- **lifecycle-state:** `DONE` (IMPL landed; **row 71 `done`**). Subject: the tracing `custom_tags` `metadata` type for the **`ROUTE`** MetadataKind — the FIFTH tracing `custom_tags` capability and the SECOND `metadata` MetadataKind, over the LANDED `FilterChain.RouteMetadata() *corev3.Metadata` accessor (phase 24.2 / ADR-0165). The 9-task SINGLE-FLAT-ROW TDD spine (T1–T9) LANDED (each red-first → green → break-verified → per-task adversarial review, RE-DERIVING every anchor at the IMPL tip — 32/32 EXACT-HOLD, ZERO drift, no correction needed): T1 (`9f96354a`) `config.go` `kindMetadataRoute` + the ROUTE accept arm cloning REQUEST (MetaPath FULL, NO new import) replacing the `:245-246` reject · T2 (`4da435b2`) `resolve.go` the one-arg `routeMetaLookup` 4th param + the `kindMetadataRoute` arm descending the FULL `MetaPath` (`descend`/`structpbValueToString` reused VERBATIM) · T3 (`3c0acfef`) the NEW `func (c *FilterChain) RouteMetaLookup()` accessor + the `structpb` import to `chain.go` · T4 (`040f67d4`) the `routeMetaLookup` thread at all 18 callers (`chain.RouteMetaLookup` at 15, a 2nd `nil` at the 3 no-chain sites, the 3rd `metaLookup` untouched) + a live ROUTE-metadata-span test · T5 (`8147ed9e`) the `withMetaTags` seed repurpose, dispatch-verified · T6 (`3776a7e8`) the OTLP `0115` static-route-metadata fixture (NO writer, two ROUTE custom_tags cross-side EXACT; breaks J/K/L fired LIVE) · T7 (`383cc974`) BC B1–B2 · T8 the six-gate · T9 ADR-0293 IN PLACE + this roll. `CLUSTER`/`HOST`/unset-kind stay PARSE-REJECTED loudly (envoy-go-strict, ADR-0080). Two of the four `metadata` MetadataKinds (`REQUEST`@70, `ROUTE`@71) now consumed. The Observability family STAYS OPEN. **Next → the phase-72 BRAINSTORM.**
- **next-skill:** **the phase-72 BRAINSTORM** — the roller SELF-PICKS the next subject per the 2026-07-12 standing directive (smallest defensible ROW-SIZED candidate; record the pick + the rejected roster in the BRAINSTORM). The sentinel does NOT fire (checks (2)+(3) still print — abundant work remains; the Observability/HTTP-3/xDS families stay OPEN with live `candidates:` sentences, and gRPC/Runtime/WASM are never-opened). Fresh worktree off master (`feedback_git_worktrees`); subagent-driven (`feedback_execution_style`); subagents commit LOCALLY only (`feedback_subagents_no_push`), controller squash-push at close (`feedback_push_to_origin`). Candidate pool includes: the `CLUSTER`/`HOST` metadata MetadataKinds (the residual non-`REQUEST`/non-`ROUTE` kinds — `CLUSTER` the `upstream_cluster` framework gap, `HOST` the reduced `envoy.lb` subset), `spawn_upstream_span`/`http_service`/force-trace, the `ssl` stat family (ADR-0286 C3), a 6th stats sink, and the deferred xDS/HTTP-3 candidates + the never-opened gRPC/Runtime/WASM families.
- **last-commit:** the phase-71 IMPL squash (`git log --grep 'phase 71'`; locate by SUBJECT, never by position). Prior: the phase-71 PLAN squash.
- **last-updated:** 2026-07-22 (phase-71 IMPL done — the 9-task SINGLE-FLAT-ROW TDD spine landed subagent-driven [one implementer + a per-task adversarial reviewer per task], each RE-DERIVING its anchors at the IMPL tip [32/32 EXACT-HOLD, ZERO drift, no correction needed]; **ROW 71 FLIPPED `in-progress` → `done`** at the six-gate; ADR-0293 COMPLETED IN PLACE [§Decision + §Consequences, STATUS PROPOSED → COMPLETE, no renumber]; the `ROUTE` MetadataKind narrowed OUT of the live Observability `candidates:` sentence at the row-done edit; six-gate ALL GREEN incl. the FULL 117-dir differential `ok 388.647s`)
- **next-free ADR:** **`ADR-0294`** — DECISIONS.md tail is **ADR-0293** (STATUS **COMPLETE** — §Context/§Decision/§Consequences all landed; §Decision + §Consequences appended IN PLACE at the phase-71 IMPL, no renumber). `grep -c '^## ADR-0293'` → 1, `grep -c '^## ADR-0294'` → 0.

---

## Project counts at this commit

*(Live. Re-verify at session start — `git fetch` first — and update here at every stage close. Canonical commands inline.)*

- **active differential fixtures:** **117** — `ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0115-tracing-custom-tags-metadata-route` (added at phase 71 — the OTLP ROUTE-metadata-custom_tags fixture, a static route `metadata.filter_metadata` block + two ROUTE custom_tags cross-side EXACT key+value, NO writer).
- **fuzzers:** **55** — `grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`. Phase 71 REPURPOSED the `withMetaTags` seed (a valid ROUTE accept + a CLUSTER reject) ⇒ **+0**.
- **stat surface:** **1201** — BEHAVIOR_CONTRACT doc count. **There is NO mechanical counting command.** Phase 71 was **+0** (a span attribute registers no stat).
- **BackendKind tail:** **38** (`H2GoawayResponder`). Phase 71 was **+0** (`0115` reuses `HTTPFixedBody`; the `otlptrace` receiver is driver-owned; NO Lua writer).
- **DECISIONS.md tail:** **ADR-0293** (next-free **ADR-0294**) — `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1`. ADR-0293 is **COMPLETE** — §Context drafted at the phase-71 SPEC, §Decision + §Consequences appended IN PLACE at the phase-71 IMPL (no renumber). ADR-0292 stays COMPLETE.
- **go.mod modules:** the tracked lineage figure is **2** (the phase-61.2 count: `quic-go` direct + `qpack` indirect) — **NOT a repo total**; the single `go.mod` requires **67** modules. Phase 66 was **+0**.
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` (see `ENVOY_TARGET.md`).
- **conformance:** h2spec 53/53; proxy-wasm all 10 families PASS.

---

## Recent lineage

*(The last FIVE stages only — enough for the router's "prior-phase lineage" read. Everything earlier is in `STATE_HISTORY.md`. These bullets are carried over VERBATIM; the newest mirrors §Current (the phase-71 IMPL done), and the oldest (phase-70 PLAN done) was dropped as this list re-capped at five.)*

- **prior active-phase:** `phase 71 (tracing-custom-tags-metadata-route) IMPL done` (2026-07-22) — lifecycle-state 3 → DONE; **ROW 71 FLIPS `in-progress` → `done`** at this IMPL six-gate (ADR-0106, the SOLE leg — a SINGLE FLAT ROW, `reference_roadmap_split_phase_row_done`). Fresh worktree `.worktrees/phase-71-impl` off master `428d7728` (the phase-71 PLAN squash), branch `phase-71-tracing-custom-tags-metadata-route-impl`; subagent-driven (`feedback_execution_style`) — one implementer per task + a per-task adversarial reviewer, each committing LOCALLY only (`feedback_subagents_no_push`), controller squash-push at close (`feedback_push_to_origin`). Each task RE-DERIVED its anchors at the IMPL tip (a PLAN is not evidence) — the controller re-opened all SPEC §11 anchors and found **32/32 EXACT-HOLD, ZERO drift** (no correction needed; the phase-70 `k := md.GetKind()` receiver fact still held). **The 9-task TDD spine landed** (each red-first → green → break-verified → adversarially reviewed): T1 (`9f96354a`) `config.go` `kindMetadataRoute` + the ROUTE accept arm cloning REQUEST (MetaPath FULL, NO new import) replacing the `:245-246` `route kind unsupported` reject; CLUSTER/HOST/unset rejects UNCHANGED; breaks A/B/C fired (one stale phase-70 `"route"` reject subtest removed) · T2 (`4da435b2`) `resolve.go` the nil-tolerant one-arg `routeMetaLookup` 4th param + the `kindMetadataRoute` arm descending the FULL `MetaPath` (NOT `[1:]` — the REQUEST slice is a Bucket-pre-keying artifact); `descend`/`structpbValueToString` reused VERBATIM; the crux Break D (wrong slice) reproduced; `-race` clean · T3 (`3c0acfef`) the NEW `func (c *FilterChain) RouteMetaLookup()` accessor (the ROUTE analogue of `DynamicMetadata()`, wrapping `RouteMetadata().GetFilterMetadata()[ns]` via `structpb.NewStructValue`, nil-safe) + the `structpb` import to `chain.go` (an existing module, +0 go.mod); Break G discriminating · T4 (`040f67d4`) the `routeMetaLookup` thread at all 18 emit callers — `chain.RouteMetaLookup` APPENDED as the 4th arg at the 15 span-capable sites, a 2nd `nil` at exactly the 3 no-chain-404 sites (provably never reached — the `traceDecision` gate); the 3rd `metaLookup` arg UNTOUCHED; a live ROUTE-metadata-span test; `-race` clean · T5 (`8147ed9e`) the `withMetaTags` seed repurposed (add `meta_route_ok` valid ROUTE accept + repoint `meta_bad` Route→Cluster), dispatch-verified to reach the metadata arm (+0 fuzzers) · T6 (`3776a7e8`) the OTLP fixture `0115-tracing-custom-tags-metadata-route` (the `0114` chassis WITHOUT the Lua writer + a static route `metadata.filter_metadata` block + two ROUTE custom_tags — `route_hit`→the static value `v-route-0115`, `route_default`→`fallback-0115` — asserted cross-side EXACT key+value; breaks J/K/L all fired LIVE; fixtures 116→117) · T7 (`383cc974`) BEHAVIOR_CONTRACT B1–B2 · T8 the six-gate · T9 ADR-0293 COMPLETED IN PLACE + this roll. **Every task got a per-task adversarial review** (ZERO SEVERE/MODERATE across all; the T6 reviewer independently re-ran the `0115` differential LIVE → PASS). **Six-gate ALL GREEN:** gofmt SILENT · vet 0 · build 0 · `go mod tidy -diff` EMPTY + `go.mod`/`go.sum` NO DIFF vs master (+0 modules) · golangci-lint 0 · the FULL **117-dir** differential `ok 388.647s` EXIT 0 · cycle guard `go list -deps ./internal/tracing` = NO `internal/filter` edge (the reused `descend` keeps it filter-free) · `-race` green on `internal/tracing`/`internal/filter/http`/`internal/filter/hcm` · envelope audit: 7 functional production files (`internal/tracing/{config.go,resolve.go}` + `internal/filter/http/chain.go` + `internal/filter/hcm/{accesslog_emit.go,connection.go,h2dispatch.go,h3dispatch.go}`) + the `0115` fixture + a 1-line `runner_test.go` registration + BC, with `internal/xds`/`internal/tls`/`internal/boot`/`internal/listener`/`internal/bootstrap`/`validate/` + `internal/dynamicmetadata`/`ratelimit`/`lua` BYTE-UNTOUCHED. **Exit counts:** fixtures **116 → 117** (tail `0115-tracing-custom-tags-metadata-route`) / fuzzers **55 (+0)** / stat surface **1201 (+0)** / BackendKind **38 (+0)** / DECISIONS tail **ADR-0293** COMPLETE (next-free **ADR-0294**) / go.mod modules **2 (+0)**. **The Observability family STAYS OPEN** — the `ROUTE` MetadataKind rolled OUT of the live `candidates:` sentence at this row-done edit (phase-57 precedent); the `CLUSTER`/`HOST` MetadataKinds + `spawn_upstream_span`/`http_service`/force-trace + the `ssl` stat family remain. **Sentinel re-run MECHANICALLY (does NOT fire; `stop` NOT created):** (1) prints NOTHING (row 71 `done` — every chartered row `done`); (2) prints THREE `candidates:` sentences (`grep -cE 'remaining deferred \(not-yet-chartered\) candidates:'` ⇒ **3**); (3) prints `NEVER OPENED: gRPC/Runtime/WASM`. Checks (2)+(3) print ⇒ abundant work remains. **Next → the phase-72 BRAINSTORM** (the roller SELF-PICKS per the 2026-07-12 standing directive).

- **prior active-phase:** `phase 71 (tracing-custom-tags-metadata-route) PLAN done` (2026-07-22) — lifecycle-state 2 → 3; docs-only (`PLAN.md` + `PROGRESS.md`, the ONLY two files in the squash's phase-directory delta — ZERO production `.go`, DECISIONS/ROADMAP UNTOUCHED); fresh worktree `.worktrees/phase-71-plan` off master `60f12146` (== the phase-71 SPEC squash; NO parallel stream — the production tree is byte-identical to what the SPEC re-derived), branch `phase-71-tracing-custom-tags-metadata-route-plan`, per `feedback_git_worktrees`; subagent-driven: a read-only re-derivation agent re-opened EVERY SPEC §11 anchor at `60f12146` (**32/32 anchor groups EXACT-HOLD — ZERO unacknowledged drift**; the tip is one docs-only commit ahead of the SPEC's cited `fbc86063`, no line shifted) + confirmed the clone sources (the LANDED phase-70 REQUEST accept arm `config.go:228-244`, the `kindMetadata` REQUEST resolve arm `resolve.go:65-88`, `descend`/`structpbValueToString` `:100-114`/`:123-140` reused VERBATIM, the `withMetaTags` seed `fuzz_test.go:94-108` with `meta_bad` a MetadataKey-less ROUTE tag) + the S2 fact (`structpb` ABSENT from `chain.go`; the new `RouteMetaLookup` method ADDS it) + the 18-caller map (5+6+7; exactly 3 no-chain-404 nil sites `connection.go:330`/`h2dispatch.go:313`/`h3dispatch.go:130`; the 4th arg APPENDS, the 3rd `metaLookup` untouched) + RD-NOIMPORT-CFG (config.go adds ZERO imports) + RD-FIXTURE (the `0114` chassis has a Lua writer to DROP; `0115` uses static route metadata) → the 9-task SINGLE-FLAT-ROW TDD spine (T1–T9) drafted → TWO independent adversarial verifiers in PRIVATE scratch (`reference_parallel_subagents_private_scratch`) → the V1/V2 records folded. **Row 71 STAYS `in-progress`** (flips at the IMPL six-gate). **The deliverable: a 9-task SINGLE-FLAT-ROW spine** — T1 `config.go` `kindMetadataRoute` + the ROUTE accept arm cloning REQUEST replacing the `:245-246` reject (NO new import) · T2 `resolve.go` the one-arg `routeMetaLookup` 4th param + the `kindMetadataRoute` arm descending the FULL `MetaPath` (`descend`/`structpbValueToString` reused verbatim, NO new import) · T3 the NEW `func (c *FilterChain) RouteMetaLookup()` accessor + the `structpb` import to `chain.go` · T4 the `routeMetaLookup` thread at all 18 callers (`chain.RouteMetaLookup` APPENDED as the 4th arg at 15, a 2nd `nil` at the 3 no-chain sites) · T5 the `FuzzHCMConfigParse` seed repurpose (add `meta_route_ok` + repoint `meta_bad` Route→Cluster) · T6 the OTLP `0115` static-route-metadata fixture (NO writer) + breaks · T7 BC B1–B2 · T8 six-gate/full-117-dir/-race/cycle-guard/envelope-audit · T9 ADR-0293 IN PLACE + row-done + sentence-narrow + STATE/router. **Adversarial verification (PLAN §1.2):** **V1 — code-claims + by-execution compile probes** re-derived every load-bearing skeleton at tip and EXECUTION-CONFIRMED the pivotal S1 full-path-descent equivalence (`descend(structpb.NewStructValue(nsStruct), ["outer","inner"])` yields `"deep"` == REQUEST's `descend(metaLookup-result, ["inner"])` yields `"deep"`, EQUAL; single-segment resolves; unresolvable → false; `NewStructValue` signature confirmed) — **BUILDABLE as written, ZERO SEVERE/MODERATE/MINOR** (one informational heads-up: a naive `resolve_test.go` `ResolveCustomTags(` grep returns 21 vs the 20 real callers because line 106 is a string literal — already covered by the PLAN's re-grep instruction). **V2 — process/consistency/SPEC-coverage:** all eight stage-close mechanics PASS (row stays `in-progress`; DECISIONS UNTOUCHED at the PLAN; ADR-0293 completes IN PLACE at the IMPL, no renumber; counts/sentinel/identifiers re-run; envelope internally consistent; break protocol complete; format faithful to phase 70; ZERO cited-but-unwritten obligation — the phase-69 `TestNoNewStat` defect class avoided, SPEC §7 says NO stat guard for tracing) — **ZERO SEVERE, ZERO MODERATE, ZERO MINOR.** **The design direction — a `ROUTE`-kind metadata custom tag on the landed static-route-config metadata (`RouteMetadata()`), reusing the phase-70 `descend`/`structpbValueToString` verbatim, a NEW one-arg `routeMetaLookup` 4th param + a `RouteMetaLookup` method, single flat row of 9 tasks — is unchanged from the SPEC.** **Counts UNCHANGED at this PLAN (docs-only; re-run MECHANICALLY):** fixtures **116** · fuzzers **55** · stat surface **1201** · BackendKind **38** · DECISIONS tail **ADR-0293** (PROPOSED; next-free ADR-0294; completed IN PLACE at the IMPL, no flip) · go.mod modules **2**. **Sentinel re-run MECHANICALLY (does NOT fire; `stop` NOT created):** (1) prints `NOT DONE: row 71`; (2) `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:'` ⇒ **3**; (3) prints `NEVER OPENED: gRPC/Runtime/WASM`. **Next → the phase-71 IMPL** (execute T1–T9; RE-DERIVE each task's anchors at the IMPL tip).

- **prior active-phase:** `phase 71 (tracing-custom-tags-metadata-route) SPEC done` (2026-07-21) — lifecycle-state 1 → 2; docs-only (`SPEC.md` + the ADR-0293 §Context append; ZERO production `.go`); fresh worktree `.worktrees/phase-71-spec` off master `fbc86063` (the phase-71 BRAINSTORM squash; NO parallel stream — the production tree is byte-identical to what the SPEC re-derived), branch `phase-71-tracing-custom-tags-metadata-route-spec`, per `feedback_git_worktrees`; subagent-driven — an independent read-only re-derivation agent re-opened EVERY BRAINSTORM §6/§11 anchor at tip (the `config.go:245-246` ROUTE reject + the `:228-244` REQUEST accept to clone + `CustomTagSpec`/`customTagKind`, `resolve.go` `ResolveCustomTags`/`descend`/`structpbValueToString`, `chain.go:1156` `RouteMetadata()` + `:1022` `DynamicMetadata()`, the go-control-plane `CustomTag_Metadata`/`MetadataKey`/`MetadataKind` types + the PGV `.validate.go` rules + `structpb.NewStructValue`/`GetFilterMetadata`, the fuzz host, the collisions, the 18-site emit roster) + SIX EXECUTED live-probe arms (P1–P6) against `envoyproxy/envoy:contrib-v1.37.2` (fresh container per arm, a STATIC route-config metadata source served — **NO writer needed for ROUTE**, unlike phase 70's Lua REQUEST arm — a standalone host OTLP-gRPC receiver + a raw-TCP Zipkin dumper, decode-ran asserted per arm), then the D-CTMR docket was DISPOSED one-for-one. **Row 71 STAYS `in-progress`** (flips at the IMPL six-gate). **ADR-0293 §Context DRAFTED per ADR-0044 — the DECISIONS tail flips ADR-0292 → ADR-0293 AT THIS SPEC COMMIT** (next-free **ADR-0294**; the IMPL completes ADR-0293 IN PLACE, no further flip). **HEADLINE: every anchor HELD; NO code-blocking correction.** P1 — the reference BOOTS ROUTE/CLUSTER/HOST (`--mode validate` OK) while PGV-rejecting empty `metadata_key.key`/`path`/segment ⇒ lift ROUTE (real capability), keep CLUSTER/HOST as envoy-go-strict departures, the empty-structural rejects are parity. P2–P6 — every phase-70 wire/serialize/default/pathwalk rule REPRODUCES exactly for ROUTE (literal-tag-name key + serialized string appended after built-ins on OTLP + in the Zipkin `tags` map; the P3 serialization table; the `request_header` default rule present-empty EMITS `""`; nested path-walk + unresolvable-segment→default). **FIVE sharpenings (none code-blocking):** S1 — `routeMetaLookup` is a NEW nil-tolerant FOURTH param on `ResolveCustomTags` (ONE-arg `func(ns) (*structpb.Value, bool)`, distinct from the REQUEST two-arg `metaLookup`), descending the FULL `MetaPath` (the `[1:]` is a REQUEST-only Bucket-pre-keying artifact); S2 — `structpb` is ABSENT from the three dispatch files + `chain.go`, so a NEW `func (c *FilterChain) RouteMetaLookup(ns string) (*structpb.Value, bool)` method (wrapping `RouteMetadata().GetFilterMetadata()[ns]` via `structpb.NewStructValue`) centralizes the import to `chain.go` and threads as `chain.RouteMetaLookup` — the ROUTE analogue of phase-70's `DynamicMetadata()` add; S3 — the unset-kind reject splits (nil `*MetadataKind` = DEPARTURE [reference accepts, no `required` rule], present-empty-oneof = PARITY [PGV "value is required"]); S4 — the existing `withMetaTags` `meta_bad` ROUTE seed exercises the reject arm phase 71 REMOVES, so repurpose it (add a valid ROUTE accept `meta_route_ok` + repoint `meta_bad` to CLUSTER); S5 — intra-custom-tag order is INTERNAL iteration order (not config order), so assertions are key-based (envoy-go's `upsertAttr` already is). **D-CTMR dispositions:** D-CTMR-FIXTURE → ONE new OTLP `0115-tracing-custom-tags-metadata-route` (a static route-metadata block + two ROUTE custom_tags — one resolving to the static value, one falling to `default_value` — asserted cross-side EXACT key+value, NO writer since both sides read identical static route config; fixtures 116 → 117; no separate Zipkin dir — P3 proved the Zipkin emit LIVE); D-CTMR-FUZZSEED → +0 (the repurposed seed, dispatch-verified); D-CTMR-SPLIT → SINGLE FLAT ROW (~9 tasks, ADR-0045 valve armable-but-unconsumed). **Adversarially verified before landing** (TWO independent verifiers in PRIVATE scratch). **Counts UNCHANGED at this SPEC (docs-only apart from the DECISIONS append; re-run MECHANICALLY):** fixtures **116** · fuzzers **55** · stat surface **1201** · BackendKind **38** · go.mod modules **2** — the ONLY moved count is the DECISIONS tail (**ADR-0292 → ADR-0293**; next-free **ADR-0294**). **Sentinel re-run MECHANICALLY at this close (does NOT fire; `stop` NOT created):** (1) prints `NOT DONE: row 71`; (2) prints THREE live "candidates:" sentences (`grep -cE 'remaining deferred \(not-yet-chartered\) candidates:'` ⇒ **3**); (3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`. **Next → the phase-71 PLAN** (RE-DERIVE the SPEC; the owed roster is in §Current pointer's next-skill line).

- **prior active-phase:** `phase 71 (tracing-custom-tags-metadata-route) BRAINSTORM done` (2026-07-21) — lifecycle-state DONE → 1; the roller's AUTONOMOUS self-pick (no human pick per the 2026-07-12 standing directive) continues the Observability family; docs-only, ZERO production `.go`; fresh worktree `.worktrees/phase-71-brainstorm` off master `e2912f6f` (the phase-70 IMPL squash), branch `phase-71-tracing-custom-tags-metadata-route`, per `feedback_git_worktrees`. **Subject:** `tracing-custom-tags-metadata-route` — the tracing `custom_tags` `metadata` type for the **`ROUTE`** MetadataKind, the SEVENTEENTH Observability-family row + the FIFTH tracing `custom_tags` capability + the SECOND `metadata` MetadataKind (after `REQUEST` @ 70): lifts the `custom_tags metadata route kind unsupported` reject (`internal/tracing/config.go:245-246`) and resolves the matched route's STATIC config metadata (`route.metadata.filter_metadata[ns]`) onto the ingress span over the LANDED `FilterChain.RouteMetadata() *corev3.Metadata` accessor (`internal/filter/http/chain.go:1156`, phase 24.2 / ADR-0165), emitted on BOTH the OTLP + Zipkin exporters via the phase-46 `Span.Attrs []KV` seam; the `CLUSTER`/`HOST` kinds stay reject-loud with distinct substrings (envoy-go-strict, ADR-0080). **Why this pick (the FULL rejected roster adjudicated in BRAINSTORM §2.1; TWO independent read-only cost-assessment agents at tip `e2912f6f`):** `metadata`/`ROUTE` is SMALL — it clones the phase-70 `kindMetadata` resolve machinery (`descend`/`structpbValueToString`, reused VERBATIM; landed LAST SESSION, freshest substrate, lowest execution risk), the source `chain.RouteMetadata()` is landed + reachable at the emit sites, and the source is STATIC route config needing NO runtime writer (simpler than phase 70's Lua REQUEST arm). **STALE-COST CORRECTION:** the phase-70 BRAINSTORM lumped all three non-`REQUEST` MetadataKinds as "not exposed at the span-emit seam"; that is now STALE for `ROUTE` (`chain.RouteMetadata()` IS landed; `reference_deferred_candidate_cost_restale`). The rivals: `CLUSTER` metadata (the `upstream_cluster` framework gap — LARGE) · `HOST` metadata (only the reduced `envoy.lb` subset in scope — MODERATE) · reconnection-backoff (xDS — SMALL but THIN, initial-fetch-only ⇒ little to reconnect to) · upstream SDS (xDS — MODERATE, threads a provider across the `grpcclient→cluster→tls` cycle) · `http_service` (MODERATE, new HTTP-protobuf transport) · `spawn_upstream_span` (MOD-LARGE, new CLIENT-span model) · force-trace (LARGEST, unbuilt internal-request detection) · SDS rotation (LARGE, new mutable-cert seam) · the `ssl` stat family (ADR-0286 C3) · a 6th stats sink · the RBAC `require=false` rider · DataSource `environment_variable` (D-ENV-HARNESS blocked) · `watched_directory` · HTTP-3 candidates · opening a never-opened family. **The landed substrate (re-derived this session):** the phase-70 `descend`/`structpbValueToString` + the `CustomTagSpec.MetaNamespace`/`MetaPath`/`DefaultValue`/`HasDefault` fields + the four-kind reject switch (lift the ROUTE arm) + the `metaLookup`-threading pattern across 18 callers (mirror for `routeMetaLookup`) + `FilterChain.RouteMetadata()` (phase 24.2) as the `ROUTE` source. **The one genuine design question (D-CTMR-RESOLVE-SEAM):** the source-shape asymmetry — `REQUEST` reads `metaLookup(ns,key)→*structpb.Value` (Bucket pre-keys the first segment, descend `MetaPath[1:]`); `ROUTE` reads `*corev3.Metadata.FilterMetadata[ns]→*structpb.Struct` (namespace→struct, descend the FULL `MetaPath`). Anticipated seam: a new nil-tolerant `routeMetaLookup func(ns) (*structpb.Value, bool)` param (namespace struct wrapped as a `StructValue`), symmetric with `metaLookup`. **Envelope:** ZERO new production packages (edits in `internal/tracing` + `internal/filter/hcm`, both existing; `internal/dynamicmetadata` UNTOUCHED) / +0 stats / +0 fuzzers (a SEED extension to `FuzzHCMConfigParse`) / +0 BackendKinds / +0 go.mod modules (`corev3`/`metadatav3`/`structpb`/`protojson` all already imported); ONE new OTLP fixture anticipated (`0115`, a static-route-metadata positive arm + a `default_value` arm; NO writer) ⇒ fixtures **116 → 117**. **BRAINSTORM-time open questions to the SPEC:** D-CTMR-SCOPE · D-CTMR-PROTO · D-CTMR-WIRE · D-CTMR-ZIPKIN-WIRE · D-CTMR-VALUE-SERIALIZE · D-CTMR-DEFAULT · D-CTMR-RESOLVE-SEAM · D-CTMR-CONFIG-KIND · D-CTMR-BUCKET-SEAM · D-CTMR-REJECT · D-CTMR-FIXTURE/D-CTMR-FIXTURE-SOURCE · D-CTMR-FUZZSEED · D-CTMR-SPLIT (SINGLE FLAT ROW anticipated, ~8–10 tasks; ADR-0045 armable). **Row 71 registered `in-progress`** at this stage-close commit per the ROADMAP §Schema invariant; flips `done` at the phase-71 IMPL six-gate (ADR-0106, the SOLE leg). **This pickup IS named in the live Observability `candidates:` sentence (the non-`REQUEST` MetadataKinds `ROUTE`/...) — NO sentence is narrowed at ANY pre-IMPL stage (the phase-57 precedent); the ROUTE candidate rolls OUT at the phase-71 IMPL row-done edit.** **Counts UNCHANGED at this BRAINSTORM (docs-only; re-run MECHANICALLY in the worktree at this close):** fixtures **116** (numeric tail `0114-tracing-custom-tags-metadata`) · fuzzers **55** · stat surface **1201** · BackendKind **38** · DECISIONS tail **ADR-0292** (COMPLETE; next-free **ADR-0293**) · go.mod modules **2**. **Sentinel re-run MECHANICALLY at this close (does NOT fire; `stop` NOT created):** (1) prints `NOT DONE: row 71` (the registration re-opens check (1)); (2) prints THREE live "candidates:" sentences (`grep -cE 'remaining deferred \(not-yet-chartered\) candidates:'` ⇒ **3**); (3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`. **Next → the phase-71 SPEC** (RE-DERIVE the BRAINSTORM rather than execute it; the D-CTMR-* probe arms + the ADR-0293 §Context draft; the owed roster is in §Current pointer's next-skill line).

- **prior active-phase:** `phase 70 (tracing-custom-tags-metadata) IMPL done` (2026-07-21) — lifecycle-state 3 → DONE; **ROW 70 FLIPS `in-progress` → `done`** at this IMPL six-gate (ADR-0106, the SOLE leg — a SINGLE FLAT ROW, `reference_roadmap_split_phase_row_done`). Fresh worktree `.worktrees/phase-70-impl` off master `a80f2ef3` (the phase-70 PLAN squash), branch `phase-70-tracing-custom-tags-metadata-impl`; subagent-driven (`feedback_execution_style`) — one implementer per task + a per-task adversarial reviewer, each committing LOCALLY only (`feedback_subagents_no_push`), controller squash-push at close (`feedback_push_to_origin`). Each task RE-DERIVED its anchors at the IMPL tip (a PLAN is not evidence) — the §1 re-derivation ledger held EXACT (all identifiers free, all anchors at tip). **The 9-task TDD spine landed** (each red-first → green → break-verified → adversarially reviewed): T1 (`1752f0d4`) `config.go` `kindMetadata` + `CustomTagSpec.MetaNamespace`/`.MetaPath` + the REQUEST accept + four distinct-substring rejects (ROUTE/CLUSTER/HOST/unset-kind — envoy-go-strict DEPARTURE) replacing the `:214-215` reject — **the V1 SEVERE honored: bind `k := md.GetKind()`, branch on `k.GetRequest()`** · T2 (`a1720365`) `resolve.go` the nil-tolerant `metaLookup` param + `kindMetadata` arm (the `request_header` default rule — present-empty EMITS `""`) + a filter-free `descend` clone + `structpbValueToString` (string→raw / else `protojson.Marshal`+`json.Compact`, the `json.Compact` load-bearing for the list case) · T3 (`2fb8a513`) the NEW `func (c *FilterChain) DynamicMetadata()` accessor (the C1 correction — the `:837`/`:963` accessors were on `decoderCB`/`encoderCB`) · T4 (`15ad855d`) the `metaLookup` thread at all 18 emit callers — `chain.DynamicMetadata().Get` at the 15 span-capable sites, `nil` at exactly the 3 no-chain-404 sites (provably never reached — the `traceDecision` gate) + a live metadata-span test · T5 (`e44cd99c`) the `withMetaTags` seed into `FuzzHCMConfigParse`, dispatch-verified to reach the metadata arm (+0 fuzzers) · T6 (`e8b12c33`) the OTLP fixture `0114-tracing-custom-tags-metadata` (the `0106` chassis + a `0027`-style Lua `dynamicMetadata():set` writer + two custom_tags — `meta_hit`→the Lua value, `meta_default`→`default_value` — asserted cross-side EXACT key+value; breaks K/L/M all fired; fixtures 115→116) · T7 (`3cc73db9`) BEHAVIOR_CONTRACT B1–B2 · T8 the six-gate · T9 ADR-0292 COMPLETED IN PLACE + this roll. **Every task got a per-task adversarial review** (T1/T2/T4/T6 full-subagent — ZERO SEVERE/MODERATE across all; T1 two minor coverage gaps non-blocking; T3/T5/T7 verified inline). **Six-gate ALL GREEN:** gofmt SILENT · vet 0 · build 0 · `go mod tidy -diff` EMPTY + `go.mod`/`go.sum` NO DIFF vs master (+0 modules) · golangci-lint 0 · the FULL **116-dir** differential `ok 381.376s` EXIT 0 · cycle guard `go list -deps ./internal/tracing` = NO `internal/filter` edge (the `descend` clone keeps it filter-free) · `-race` green on `internal/tracing`/`internal/filter/http`/`internal/filter/hcm` · envelope audit: 7 functional production files (`internal/tracing/{config.go,resolve.go}` + `internal/filter/http/chain.go` + `internal/filter/hcm/{accesslog_emit.go,connection.go,h2dispatch.go,h3dispatch.go}`), `internal/xds`/`internal/tls`/`internal/boot`/`internal/listener`/`internal/bootstrap`/`validate/` + `internal/dynamicmetadata`/`internal/filter/http/ratelimit`/`internal/filter/http/lua` BYTE-UNTOUCHED. **Exit counts:** fixtures **115 → 116** (tail `0114-tracing-custom-tags-metadata`) / fuzzers **55 (+0)** / stat surface **1201 (+0)** / BackendKind **38 (+0)** / DECISIONS tail **ADR-0292** COMPLETE (next-free **ADR-0293**) / go.mod modules **2 (+0)**. **The Observability family STAYS OPEN** — `custom_tags (metadata)` for the `REQUEST` kind rolled OUT of the live `candidates:` sentence at this row-done edit (phase-57 precedent); the three non-`REQUEST` MetadataKinds became a NEW deferred entry, the `ssl` + `spawn_upstream_span`/`http_service`/force-trace candidates remain. **Sentinel re-run MECHANICALLY (does NOT fire; `stop` NOT created):** (1) prints NOTHING (row 70 `done` — every chartered row `done`); (2) prints THREE `candidates:` sentences (`grep -cE 'remaining deferred \(not-yet-chartered\) candidates:'` ⇒ **3**); (3) prints `NEVER OPENED: gRPC/Runtime/WASM`. Checks (2)+(3) print ⇒ abundant work remains. **Next → the phase-71 BRAINSTORM** (the roller SELF-PICKS per the 2026-07-12 standing directive).

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
