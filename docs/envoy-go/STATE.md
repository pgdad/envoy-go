# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

> **This file is a POINTER, not an archive** — `BOOTSTRAP_PROMPT.md` §4: *"STATE.md — pointer: active phase, last commit, next action."* It carries the live pointer, the live counts, and a SHORT recent lineage. **Full stage-by-stage history lives in `STATE_HISTORY.md`** (archived at ADR-0288; **nothing was deleted** — the pre-move file is preserved there verbatim, byte-for-byte).
>
> **⚠️ THE RULE ADR-0288 EXISTS TO ENFORCE: there is EXACTLY ONE live `next-skill:`, `lifecycle-state:`, `next-free ADR:`, and count block IN THIS FILE, and they live in §Current pointer directly below.** Before that move, ~330 lines of prepended backlog had buried TWO fossilised "current state" blocks whose `next-skill:` still read *"the 42.2 hedging BRAINSTORM"* and *"the 46.2 SPEC"* while the live phase was **66** — so `grep 'next-skill:' STATE.md` returned **two stale hits and ZERO live ones**, and `grep 'fuzzers:'` returned **48** and **42** when the true count was **55**. **A session may trust a grep of this file again. Keep it that way: when you advance the pointer, EDIT §Current pointer IN PLACE — never prepend a new block above it.**

---

## Current pointer

*(The ONLY live "what next" in this repo. Update IN PLACE at every stage close.)*

- **active-phase:** `phase 85 (h2spec-selector-repair) BRAINSTORM done` (2026-08-08) — lifecycle-state **DONE -> 1**; **ROW 85 REGISTERED `in-progress`** at `ROADMAP.md:147` and the sentinel `want` bumped **116 -> 117 in the SAME commit** (the phase-84 precedent, 115 -> 116). Base master **`84c16c65`** (from `git rev-parse master`), branch `phase-85-brainstorm`. Docs-only: ZERO production `.go`, ZERO test `.go`. **SELF-PICKED** per the 2026-07-12 standing directive — no banked mid-lifecycle work existed (phase 84 CLOSED, all 116 prior rows `done`, check (1) SILENT before this stage re-opened it). **THREE investigation agents** on disjoint remits (h2spec re-derivation BY EXECUTION in its own detached worktree, port band 46500-46599, removed and verified gone; the six family backlog paragraphs, read-only; the deferral sweep, read-only); every load-bearing claim controller-re-derived, and **two agent claims did NOT survive as stated** (BRAINSTORM §5.3). ⚠️ **THE HEADLINE: THE H2 CONFORMANCE GATE'S TRUE SCOPE WAS EXECUTED FOR THE FIRST TIME — 95 CASES, FOUR RED.** As committed the gate runs **53/53 green**; with the NINE malformed slash-form section-6 selectors (`test/conformance/h2spec/h2spec.go:22-37`; h2spec addresses sections with DOTTED numbers, an unmatched positional argument is a silent no-op) dot-corrected it runs **95 tests / 90 passed / 1 skipped / 4 FAILED** (section 6 alone **42/37/1/4**) — 42 strict cases, **44% of declared scope, silently unrun since 2026-04-25**. The four failures are **production RFC 9113 MUST violations**, causes read at the code sites: **6.5.2/1, 6.5.2/3, 6.5.2/4** — `onSettings` (`internal/filter/hcm/h2/conn.go:507-538`) applies every SETTINGS value with ZERO validation (no ENABLE_PUSH-in-{0,1}, no MAX_FRAME_SIZE bounds; want GOAWAY(PROTOCOL_ERROR)); **6.9.2/1** — live streams' send windows never adjusted on a SETTINGS_INITIAL_WINDOW_SIZE change. ⚠️ **The gap is PARTIAL — 6.5.2/2 and 6.9.2/3 PASS.** ⚠️ **TWO NEW GATE DEFECTS BEYOND THE SELECTORS:** the JUnit `<failure>` parse gap (`tc.Failure` parsed nil for all four failing cases — per-case failure identity invisible to the Go report, recoverable only from container logs) and ADR-0051's **VACUOUS** 6.6 PUSH_PROMISE exclusion (the pinned image ships NO 6.6 suite at all). ⚠️ **ALSO NEW: `h2/client.go`'s response read-loop is CONTINUATION-blind too** (no `ContinuationFrame` arm; `:428` decodes only the first fragment) — recorded for the deferred CONTINUATION row, which **h2spec 6.10 is MEASURED NOT to gate** (6/6 GREEN over the live `conn.go:255-259` discard). Row 85 is an **Operational-tooling-family MAINTENANCE row claiming NO family ordinal** (ADR-0298/ADR-0300 precedent); the Operational-tooling backlog sentence is **DELIBERATELY UNTOUCHED** — the candidate came from SPEC-84 §13 item 1 ("one coherent future row"), not from any family window, so nothing rolls out of any live sentence at row-done either. Rejected alternatives re-derived and recorded in BRAINSTORM §2: **candidate (i) is NOT A ROW** (all six check-(2) phrases sit in standalone family PROSE paragraphs naming ~42 distinct candidates; every retire-shape is doctrinally foreclosed as the self-clearing-matcher defect); the **CONTINUATION two-sided repair** (strongest product defect on the board, 2-4x this row, ungated by any conformance selector — the natural NEXT row); the **stat-surface recount** (cheapest, but repairs no gate — and the contradiction WIDENED: `STATE.md:33` says 1205 while ADR-0300/0301 headers say 1207, both live, both DOC-SOURCED); **`ssl.connection_error`** (+444 whole-`.go` VERIFIED floor — the backlog agent's "smallest" framing repeated the phase-75 production-only-vs-whole-`.go` category error and was REJECTED); `test/conformance/grpc/` (9/26 reachable); the `validate` nil-`sdsProvider` bug (still the best sub-row bug, still available); REVIEW.md restoration (process-not-product).
- **phase-directory:** `docs/envoy-go/phases/85-h2spec-selector-repair/` — NEW at this stage (`BRAINSTORM.md`, `PROGRESS.md`); **still no `REVIEW.md`, the STANDING LINEAGE DEPARTURE — 37 of 126 phase dirs carry one, none since 25.3.** Prior: `docs/envoy-go/phases/84-grpc-unary-response-trailers/` (CLOSED — five stage docs, both legs executed).
- **lifecycle-state:** **1** (BRAINSTORM done; SPEC next). ⚠️ **Sentinel RE-RUN MECHANICALLY at this stage, BEFORE and AFTER the ROADMAP edit — it does NOT fire and `stop` was NOT created** (`ls stop` => `No such file or directory`, both sides). BEFORE at `want=116`: (1) SILENT · (2) SIX `:194 :200 :206 :216 :222 :230` · (3) SILENT. AFTER at `want=117`: **(1) `NOT DONE: row 85` — CORRECT while the phase is open** (it goes silent again only at this row's IMPL flip) · **(2) SIX — anchors SHIFTED by the row insert to `:195 :201 :207 :217 :223 :231`, re-derived not predicted** · (3) SILENT. **All doctoring NCs fired on BOTH sides:** row-62 doctoring => `NOT DONE: row 62` (after-side: rows 62 AND 85), with `NC LANDED? [ in-progress ]` inspected before trusting the result; `want` off-by-one => `GATE FAIL: examined 116 data rows, expected 115` before / `examined 117 data rows, expected 116` after; check-(3) doctoring (residual occurrences confirmed **0** first) => `NEVER OPENED: gRPC` while WASM correctly stays silent; check-(2) one-arm strips => **6 -> 5** (short) / **6 -> 1** (long), never 6 -> 0. **LEAK CHECK as WHOLE-FILE BEFORE/AFTER counts, every hyphen-leading pattern `--`-guarded:** lines **234 -> 235** · data rows **116 -> 117** · check-(2) union **6 -> 6** · `-family row` **95 occurrences / 67 LINES -> 95 / 67** (row 85's `Operational-tooling-family MAINTENANCE row` phrasing deliberately does NOT match check (3)'s pattern — the rows-76/78 maintenance precedent) · `gRPC-family row` **2 -> 2** · `Operational-tooling-family row` **3 -> 3** · escape-aware ARM-A flags lines **119 and 131 ONLY** (row 85 at `:147` is NOT flagged) · new slug occurrences **2**, both inside row 85's own line. **No sentinel matcher string was written into `ROADMAP.md`.**
- **next-skill:** **SPEC — phase 85 (`h2spec-selector-repair`).** It owes: the SEVEN open questions of BRAINSTORM §4 — chiefly **Q1 CI enrollment** (docker is already present in the differential job; the container ran 3.77 s locally; flake posture UNMEASURED), **Q2 the reference container's own section-6 failure set** (SPEC-84's "disjoint set of three" is **UNVERIFIED at this tip** and is carried as a QUESTION, not a fact), and **Q3 the green-at-every-tip sequencing** (selectors + zero-case guard land RED unless the four production fixes land in the same leg — the SPEC must sequence so no intermediate tip has a red gate) — plus `ADR-0307` §Context drafted STATUS `PROPOSED` (next-free FROM THE TAIL), an **ENUMERATION, not a scaling**, of the row's cost against the §3.3 floor (~9 selector chars + ~15-25 harness + ~40-80 production + ~100-200 unit-test + ~25 docs + one ADR), and the "53/53" citation sweep (§4 Q7). ⚠️ **Every §3.3 figure is a LOWER BOUND** (`reference_measured_prototype_is_a_lower_bound` — six consecutive firings; 84.1 realized 1.50x/1.37x).
- **last-commit:** the **phase-85 BRAINSTORM** commit. ⚠️ **LOCATE BY SUBJECT, NEVER BY POSITION** — `git log --grep '^phase 85 (h2spec-selector-repair) BRAINSTORM' --format='%h %s'` and take the NEWEST (`| head -1`); a bare `git log --grep 'phase 85'` is CONTAMINATED because router commit BODIES discuss the phase. Base master `84c16c65`, branch `phase-85-brainstorm`; the stage lands as ONE squashed commit pushed by the controller at close.
- **last-updated:** 2026-08-08 (phase-85 **BRAINSTORM done**; row 85 registered `in-progress`; `want` 116 -> 117). ⚠️ **THE LINEAGE EVICTION RULE WAS HONORED:** §Recent was at its five-entry cap, so **`phase 84 (grpc-unary-response-trailers) BRAINSTORM done`** (2026-08-06) — the OLDEST by a DIRECT READ of the five dates (two entries shared 2026-08-06; ⚠️ **THE TIEBREAK IS STATED, NOT ASSUMED:** within one date the entry lowest in the file / earliest in lifecycle order is the oldest — BRAINSTORM-84 precedes SPEC-84 in lifecycle order and was confirmed LAST in §Recent by direct read before removal) — was moved to `STATE_HISTORY.md` **466 -> 468**, verified **STRICTLY APPEND-ONLY** (`git diff --numstat` = `2 0` AND the base file confirmed a byte-exact **PREFIX** of the new one via `cmp` against the HEAD blob). ⚠️ **THE ARCHIVE-ABSENCE GUARD USED THE ROBUST FORM** — bullet-anchored, allowing ANY run of characters before the backtick-quoted target, corroborated by a whole-file fixed-string search; **both read 0** on the eviction target pre-append, against THREE controls **re-derived FROM THE FILE at this close**: the still-present `phase 82 (wasm-http-call-response-cache) IMPL done` (**4/4** — ⚠️ DRIFTED UP AGAIN from the 3 at the 84.2 IMPL close, because every eviction entry that cites it adds a line: a control's own count is a LIVE FIGURE), `phase 83 (wasm-pause-arm-leak) PLAN done` (**2/2**, up from 1 by the same mechanism), and an invented label (**0/0** — non-discriminating on its own, recorded as such). The guard's pattern is **DESCRIBED** in the archived label, never spelled.
- **next-free ADR:** **`ADR-0307`** — UNCHANGED; `DECISIONS.md` and `BEHAVIOR_CONTRACT.md` are **BYTE-UNTOUCHED by the phase-85 BRAINSTORM** (a BRAINSTORM drafts no ADR; ADR-0307 §Context is drafted at the SPEC per convention, STATUS `PROPOSED`). The tail is still **`ADR-0306`, STATUS `COMPLETE`** (completed in place at the 84.1 IMPL). ⚠️ **DERIVE NEXT-FREE FROM THE TAIL** — `headings + 1` COLLIDES (yields 0306) because of the single gap at ADR-0209. Re-verified mechanically at this tip, not copied: `DECISIONS.md` **17990** lines · **305** `^## ADR-` headings with the tail reading `## ADR-0306` · `^## ADR-0307` = **0**. ⚠️ **CARRY NO WHOLE-FILE COUNT OF THE LOOSE `PROPOSED` MATCHER** — every hit is a false positive; the strict guard reads **0** at this tip and that is CORRECT (it arms 0 -> 1 when the phase-85 SPEC drafts ADR-0307, and disarms at the IMPL that completes it).

---

## Project counts at this commit

*(Live. Re-verify at session start — `git fetch` first — and update here at every stage close. Canonical commands inline.)*

⚠️ **ALL counts below were re-run MECHANICALLY in the stage worktree at this phase-76 IMPL close with the canonical commands, EACH WITH ITS NEGATIVE CONTROL OBSERVED — never copied forward.** **Phase 76 landed +0 on EVERY axis, including +0 PRODUCTION `.go` FILES** — the first chartered row in this lineage whose production-code delta is exactly nothing, and the ANTICIPATED envelope held on all of them. The row's `.go` delta is **three TEST-side files** (`internal/cluster/ringhash_test.go` edited, `test/fixtures/0061-lb-ring-hash/driver/driver_test.go` edited, `.../linkage_test.go` NEW), plus three fixture-doc files and four project docs. ⚠️ **The stat-surface total 1205 is DOC-SOURCED and was NOT re-derived mechanically** — there is no mechanical command, and the chain is known to be discontinuous in **TWO** places (the recorded `1200 -> 1201`, and `Phase 46.1b` closing at 1198 while `Phase 47.1` opens at 1200 — an unattributed +2). **Only the +0 DELTA is asserted, and that argument is STRUCTURAL rather than a `statssink` result:** zero production `.go` bytes changed (G8) and zero byte mismatches over the 375-file production roster (G14), so no `NewCounter`/`NewGauge` call site can have moved; the five `TestNoNewStat*` guards corroborate at 5x PASS. ⚠️ **A future phase should COUNT the surface rather than trust the chain — a +0 row is a cheap place to do it, and phase 76 declined to charter that, so it is now a NAMED maintenance deferral rather than an idle observation.**

- **active differential fixtures:** **119** — ⚠️ **Phase 76 landed +0: `0061-lb-ring-hash` was EDITED, not added** (`sourceIPs` 4 -> 16 plus a NEW `driver/linkage_test.go` inside the EXISTING fixture directory — no new YAML, no new directory, no new BackendKind, no new port), so the count STAYS **119** and `0118` remains FREE. NC observed: `mkdir .../0118-fake` ⇒ 120. — `ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0117-tracing-custom-tags-metadata-cluster` (phase 73). **Phase 75 landed +0**: `0110-tls-require-client-cert-false` was **EXTENDED** with a cross-side `fixture.StatsAsserter` (NO new YAML, NO new directory, NO new BackendKind, NO new port), so the count STAYS **119** and `0118` remains FREE. ⚠️ **Next-free REFERENCE port is `10450`, NOT `10118`** — ports are NOT fixture-index aligned. **The fixture now asserts what its own README confessed was infeasible, and BOTH sides were EXECUTED**: `ssl.handshake=2 ssl.no_certificate=1 ssl.fail_verify_error=1 ssl.fail_verify_no_cert=0`, `downstream_cx_total=3`, **identical on reference and subject**, with the discriminating asymmetry (`handshake=2` vs `no_certificate=1`) observed per side. Shape A over `/stats/prometheus`, keyed on the metric NAME with `envoy_listener_address` IGNORED — the NAMES are byte-identical cross-side while the labels differ (`0.0.0.0_10446` reference vs **`___20016`** subject: ⚠️ **the subject binds the IPv6 WILDCARD**). ⚠️ **`0111` was NOT given a value assertion, deliberately** — it runs `require_client_certificate: true` on both sides, so a success-path no-cert annotation reads **0 on every arm, structurally**, and a value pin would document a **vacuous `0 == 0`** as coverage; its PROSE gained the name in its NAMED-UNASSERTED list instead, because **both `0111` passages are CLOSED ENUMERATIONS** and a name in neither list reads as ASSERTED. **`0111`'s driver is byte-untouched, and its named-subset check was EXECUTED for the first time at this IMPL: PASS.** ⚠️ `var _ fixture.StatsAsserter` is a **TRIPWIRE, not the dispatch mechanism** — `0110` is only the **THIRD** fixture repo-wide to carry it, while **82** define an `AssertStats` method.
- **fuzzers:** **55** — **Phase 76 landed +0** (it consumes no new config field, so there is no parse arm to fuzz). NC observed: appending `func FuzzProbeOnly` ⇒ 56. — `grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`. **Phase 75 landed +0** — the row consumes no new config field at all, so there is no parse arm and no fuzz-seed classification to flip (`reference_fuzzer_count_docs_drift`: an `f.Add` seed is not a fuzzer).
- **stat surface:** **1205** — BEHAVIOR_CONTRACT doc count. **There is NO mechanical counting command** (the contract says so itself); enforcement is per-phase `TestNoNewStat*` guards asserting a delta of ZERO, never the absolute total. **Phase 75 LANDED +1 (1204 -> 1205)** — `listener.<normalized-addr>.ssl.no_certificate`, the FOURTH listener-scope `ssl.*` name and the FIRST **SUCCESS-PATH** handshake annotation (⚠️ **NOT a synonym for `ssl.fail_verify_no_cert`, and the mis-mapping is wrong in BOTH directions** — one annotates an ACCEPTED handshake, the other books a REJECTED one; they are mutually exclusive on every connection). ⚠️ **The delta is a NAME-surface delta, NOT a flat per-deployment one:** registration rides the block that ALREADY gates on `rt.tlsMode`, so a **plaintext-only** deployment gains **ZERO** names, while a **QUIC** listener gains it permanently zero — **PARITY** with the reference, and structural (`Manager.Start` launches no accept loop for `kindQUIC`, so the Inc site is unreachable for a **name-independent** reason). ⚠️ **QUIC was NOT re-probed at phase 75** — the parity claim is INHERITED from phase 74's family-level probe, and that inheritance is stated in BEHAVIOR_CONTRACT rather than presented as a measurement. Ledger updated: `1204` -> `1205` at `BEHAVIOR_CONTRACT.md:831`/`:847`, plus a `**Phase 75 - 1204 -> 1205 (+1)**` line after the tail entry — **the ONLY line-adding edit in the file** (`5744 -> 5746`), so every by-line citation into the TLS subsection stays valid. ⚠️ **The new entry RECORDS BOTH ledger gaps and BACK-FILLS NEITHER** (fabricating a line would be inventing a record). Gate figures at this close: `grep -c '1204'` => **2** · `grep -c '1205'` => **3** · `grep -cE '^#{2,4} .*ADR-[0-9]+.*ADR-[0-9]+'` => **17** (was 16 — the `:1849` heading joined them, on the `:785` precedent). ⚠️ **`internal/statssink`'s four *"stays 1200 / 1196"* prose sites, stale since phase 49, are RECORDED-NOT-FIXED** — that package is on this phase's byte-untouched sha256 roster.
- **BackendKind tail:** **38** — **Phase 76 landed +0** (`0061` uses the in-process `TCPEcho` branch and implements neither `BackendKindAware` nor `PerHostBackendKind`). NC observed: appending `ZZProbeKind = 39` ⇒ tail 39. — (`H2GoawayResponder`, `test/differential/fixture/fixture.go:614`). **Phase 75 landed +0** — `0110` was extended, not cloned, and it implements neither `BackendKindAware` nor `PerHostBackendKind`, so the runner takes the in-process `TCPEcho` branch. ⚠️ **This is a TAIL VALUE, not a count** — the file declares **39** constants (values 0-38, `TCPEcho = 0`). The lineage figure **38** is correct as written; do NOT 'fix' it to 39.
- **DECISIONS.md tail:** **ADR-0298** (next-free **ADR-0299**) — `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1` -> `## ADR-0298`; `grep -c '^## ADR-0299'` ⇒ **0**. **ADR-0298 is COMPLETE as of the phase-76 IMPL** — §Context drafted at the phase-76 SPEC, §Decision + §Consequences APPENDED IN PLACE after the RETAINED italic footer, **no renumber and NO `---` separator** (the file's last `^---$` is still at `:17020`). Block shape controller-re-derived at this close: **1** `### Context` / **1** `### Decision` / **1** `### Consequences` / **1** retained footer. ⚠️ **FOUR of its OWN §Context paragraphs were CORRECTED IN PLACE** (¶1's `:295` -> `:307`, ¶5's `-count ~= 81` -> **83**, ¶7's *"~71 collapses"* -> **74/2000**, ¶11's *"needs no edit"* -> REFUTED) on the ADR-0297 ¶7/¶9 precedent, each **leading with what survives**; none changes a decision the ADR records. ⚠️ **It carries NO whole-file grep count and claims NO family ordinal** — the Load-balancing family closed at row 54, and **maintenance rows do not extend a charter**. *(Historical, retained: **ADR-0297 is COMPLETE** — §Context drafted at the phase-75 SPEC (ADR-0044-**as-used**; ⚠️ ADR-0044 does NOT actually contain that discipline), §Decision + §Consequences appended IN PLACE at the phase-75 IMPL after the **RETAINED** footer, mirroring ADR-0295/0296. Block shape verified at this close: **1** `### Context` / **1** `### Decision` / **1** `### Consequences` / **1** retained footer; `grep -c '^## ADR-0298'` => **0**. **No renumber, no new ADR.** ⚠️ **Its OWN §Context ¶7 and ¶9 were CORRECTED IN PLACE while completing it** — ¶7's **self-falsifying whole-file grep** (the same species the phase-74 IMPL had just fixed in ADR-0296 ¶3, reproduced one ADR later) is restated with **NO number** and the searched token deliberately **NOT spelled**, because every restatement mints another counter-example; and ¶9's *"SELF vs OTHER-ADR"* form rule is replaced by **graft SCALE** after the n=7 population was re-enumerated first-hand. ⚠️ **¶9 ALSO said *"the only two instances in the file"* — which this row's own blockquote makes three** — so a pure append would have shipped a SECOND self-falsifying count in the commit that fixed the first. **The prescription has now outlived THREE wrong justifications: family, self-vs-other, scale.** ⚠️ **`ADR-0296` is NO LONGER byte-untouched** — its §Decision (g) carries the phase-75 correction as an INDENTED blockquote (`grep -c '^  > '` **2 -> 3**), **LEADING WITH WHAT SURVIVES** (no such counter was owed *in phase 74*, whose sole stat-asserting fixture is `require=true` on both sides — **the deferral was RIGHT**) and claiming **NO ORDINAL**.)*
- **go.mod modules:** **Phase 76 landed +0** — `go mod tidy -diff` EMPTY and `git diff master -- go.mod go.sum` EMPTY; ⚠️ **the row's TEST-side import delta is +8 and that is CORRECT** (`ringhash_test.go` gains `fmt`/`math`/`mathrand "math/rand/v2"`, `linkage_test.go` adds `go/ast`/`go/parser`/`go/token`/`strconv`/`testing`) — all stdlib, all test-side, and **an import LINE is not a go.mod MODULE**; there is no new sub-package, so `reference_new_subpackage_pulls_transitive_module` does not bite. NC observed: a fake `require` ⇒ 67 -> 68. The tracked lineage figure is **2** (the phase-61.2 count: `quic-go` direct + `qpack` indirect) — **NOT a repo total**; the single `go.mod` requires **67** (18 direct + 49 indirect). **Phase 75 landed +0** — `go mod tidy -diff` EMPTY and `git diff master -- go.mod go.sum` EMPTY. The row needed `crypto/tls` and `internal/stats`, both ALREADY imported by `internal/listener/manager.go`; **an import LINE is not a go.mod MODULE**, and there is no new sub-package, so `reference_new_subpackage_pulls_transitive_module` does not bite. ⚠️ **TEST-side imports grew by exactly four** (`log`, `math`, `net/http`, `strconv` in `0110`'s driver) — all stdlib, all test-side, **not a production-envelope violation**.
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` (see `ENVOY_TARGET.md`).
- **conformance:** h2spec 53/53; proxy-wasm all 10 families PASS.

---

## Recent lineage

*(A SHORT recent lineage, capped at FIVE prior entries by ADR-0288. Everything earlier is in `STATE_HISTORY.md`. §Current carries the newest stage (**the phase-85 BRAINSTORM** — the stage that SELF-PICKED row 85 `h2spec-selector-repair`, EXECUTED the h2spec gate's true 95-case scope for the first time — FOUR RED, all production RFC 9113 MUST violations — registered the row `in-progress`, and bumped the sentinel `want` 116 -> 117 in the same commit). The five below are the prior stages, newest first. ⚠️ **EVICTED AT THIS CLOSE:** `phase 84 (grpc-unary-response-trailers) BRAINSTORM done` (2026-08-06) — the OLDEST by a DIRECT READ of the five dates; two entries shared 2026-08-06 and the tiebreak used is *within one date, the entry lowest in the file / earliest in lifecycle order is the oldest*, which puts it before SPEC-84; it was confirmed LAST in this list by direct read before removal. It is now in `STATE_HISTORY.md`, appended strictly.)*

- **prior active-phase:** `phase 84 (grpc-unary-response-trailers) IMPL-84.2 done` (2026-08-08) — lifecycle-state **3 -> DONE for the 84.2 leg**; ⚠️ **PHASE 84 CLOSED, ROW 84 FLIPPED `done`** at `ff71318d` (ONE line, `git diff --numstat` = `1 1`, `want` STAYED 116, every leak axis INVARIANT); sentinel after-state RECORDED, not predicted: **(1) SILENT for the first time in the project's history · (2) SIX · (3) SILENT ⇒ the conjunction does NOT fire, `stop` NOT created.** Base master `35e3933e`, branch `phase-84-impl-84.2`; `DECISIONS.md`/`BEHAVIOR_CONTRACT.md` **BYTE-UNTOUCHED** (no ADR consumed; next-free stayed ADR-0307); **ZERO production `.go`.** Landed: fixture **`0119-grpc-unary-trailers`** with **FOUR arms** (`success`/`notfound`/`unimplemented`/**`plain`** — the trailer-less non-gRPC H2 GET that IS the D-84-ENDSTREAM gate), driver **669** lines over raw `x/net/http2` Framer + hpack (**first-of-kind among fixture drivers**, permitted at `runner_test.go:3311`), registration ONE blank-import line (`runner_test.go:146`), 0079's three PEMs sha256-asserted pair-by-pair. ⚠️ **HEADLINE: the FOURTH arm is the only reason the row's highest-leverage decision is measured** — break **F2** (variant B, plain arm DISABLED) went **vacuously GREEN**; **F2′** with the arm restored went **RED at offset 940**: the PLAN's blindness finding **REPRODUCED, not fixed**. Break roster re-derived at `b637f656`, all six arms: **F1** RED@**192** (subject transcript **0** `TRAILERS` lines vs reference **3**, measured) · **F3** RED@**192 byte-identical to F1** (the fixture localises the SYMPTOM, not the LAYER — as chartered; 84.1's unit arms own layer localisation) · **F4** SYMMETRIC **PASS** · **F5** `AssertStats` **GREEN** (`downstream_rq_2xx` = 4 measured under F1 via a nested NC) while `CompareBytes` **RED** — **broken-gate shape 31 LIVE on this fixture** · **F6** SKIP + exit 0 in 0.85 s. Canonicalization stayed **CLOSED at three, ZERO new rules forced** (the two name-drops absorb even wire order; the sort stays vacuous). The `AssertStats` floor **STRENGTHENED against the PLAN, a RECORDED review deviation** (I-1): subject `http.ingress_http.downstream_rq_2xx >= len(arms())` = **4**, not `>= 1` (READ-ERR is recorded INTO the transcript, so an identical two-sided failure yields byte-equal transcripts and a vacuous `CompareBytes` green). ⚠️ **PATH CORRECTION recorded: the F3 populate site is `internal/filter/http/router/router_h2.go:250`** — a task brief mis-inferred `internal/filter/hcm/` and the PLAN's own cite was path-less; only re-derivation caught it. Gates: **121/121 PASS `INNER_EXIT=0`, wall 391.189 s** on launch 2 (launch 1 was 120/121 on a NEW `0079-h2-multiplex-pool` flake — `held stream not served after drain`, NOT the port-race class); `go test ./...` clean on run 2 (run 1: NEW `internal/wasm` A4 flake, passes standalone — both NEW flakes cleared on re-run, neither in a documented class); `-race` clean on four packages; vet/gofmt/golangci 0. **Stat surface +0 by call-site enumeration (208/36), delta 0/0** — ARM-1 production-scoped diff **0** with the input measure asserted (raw diff 675, all 8 files under `test/`) and its NC firing 6; **NEVER via `TestNoNewStat*`** (proven blind by execution at the 84.1 PLAN).

- **prior active-phase:** `phase 84 (grpc-unary-response-trailers) PLAN-84.2 done` (2026-08-08) — lifecycle-state **2 -> 3 for the 84.2 leg**; ⚠️ **ROW 84 STAYS `in-progress`** — it flips `done` only at the 84.2 IMPL (`reference_roadmap_split_phase_row_done`); sentinel `want` STAYS **116**; `ROADMAP.md`, `DECISIONS.md` and `BEHAVIOR_CONTRACT.md` all **BYTE-UNTOUCHED** (no ADR consumed — ADR-0306, COMPLETE, already binds 84.2 at `:17948`/`:17982`/`:17986`). Base master **`546b453d`** (from `git rev-parse master`), branch `phase-84-plan-84.2`. Docs-only: ZERO production `.go`, ZERO test `.go`. **THREE investigation agents** on disjoint remits — A1 harness mechanics (read-only), A2 **a full working prototype of fixture `0119` BUILT AND RUN at this tip** in a DETACHED worktree (private port band `46000-46099`; finished `git status --porcelain` = 0 lines, production files sha256-matching base blobs), A3 cost + doc invariants. ⚠️ **THE HEADLINE: THE CHARTERED THREE-ARM FIXTURE IS BLIND TO THE ROW'S HIGHEST-LEVERAGE DECISION** — variant B (unconditional END_STREAM) injected at the landed `h2dispatch.go:751` site is **GREEN on all three mandated gRPC arms AND on all three existing downstream-ALPN-h2 fixtures** (every gRPC response carries trailers; Go's `x/net` client tolerates an empty trailing block); **a FOURTH, trailer-less plain-GET arm reddens it deterministically** (`first divergence at offset 940`) ⇒ **the PLAN mandates FOUR arms**. ⚠️ **SECOND: §6.1 INVERTS for this leg** — floor ≈790 / budget ≈1050-1200 / corpus-anchored ceiling ≈1370-1400 net `.go` vs ~1500, the FIRST non-crossing leg in five rows, stated WITH the overrun history (84.1 realized **+3414/+3124 net `.go` vs its ≈2280 budget, 1.50x/1.37x** — the SIXTH consecutive `reference_measured_prototype_is_a_lower_bound` firing). ⚠️ **THIRD: ZERO canonicalization iteration** — the prototype went GREEN on its FIRST run; the closed rule list is the two mandated name-drops (`x-envoy-upstream-service-time`, `date`) + a never-fired READ-ERR addr scrub; the sort is VACUOUS (re-confirmed); trailer blocks byte-exact cross-side including grpc-go's `grpc-message`-before-`grpc-status` order. The three deferred cross-side controls all EXECUTED: SYMMETRIC (same bogus line both sides — PASS), VACUITY (stats leg GREEN under a broken tree while `CompareBytes` reds — shape 31 LIVE on this fixture), LIVENESS (registration silent-green: `no driver registered` + SKIP + exit 0 in 0.81 s — now Task 2's RED). Baseline 120/120 `INNER_EXIT=0` in 389.4 s (attempt 1 ABORTED on the documented `0081` port race; no siblings). **THIRTEEN corrections, FIVE load-bearing** — among them the SPEC's YAML-mirror census REFUTED on `0004` (it SHIPS both mirrors; true census **76/76 of 120**; the no-mirrors decision survives on the corrected 3-of-4-analogues ground), and ⚠️ **an UNANCHORED panic-grep false-fires 14 times on a green log** — the anchored `^panic:|DATA RACE|SIGSEGV` form stays canonical.

- **prior active-phase:** `phase 84 (grpc-unary-response-trailers) IMPL-84.1 done` (2026-08-08) — lifecycle-state **3 -> DONE for the 84.1 leg**; **ROW 84 STAYED `in-progress`**, sentinel `want` STAYED **116**. ⚠️ **UNLIKE a docs-only stage, `DECISIONS.md` AND `BEHAVIOR_CONTRACT.md` were touched:** `ADR-0306` COMPLETED IN PLACE (STATUS `PROPOSED` -> `COMPLETE`, no renumber, no `---` separator, strict guard 1 -> 0 CORRECT, ¶4 *"ten"* -> **nine**), `BEHAVIOR_CONTRACT.md` RECONCILED (five falsified statements net-zero in place + one `## HTTP/2` tail append, **5900 -> 5955**, the ADR-0052 `:1821` vehicle). Base master **`764a530d`**, branch `phase-84-impl`, final tip **`87e67fe2`**, **TWENTY tasks subagent-driven**. ⚠️ **THE HEADLINE: THE FIVE-FILE SEAM LANDED AND A REAL SECURITY CONDUIT THE ROW ITSELF CREATED WAS CLOSED IN THE SAME LEG** — Task 13's uppercase-field-name bypass in `validateResponseTrailers` was LOAD-BEARING (RFC 9113 §8.2.1) and closed; request/response guard parity byte-identical A-Z. Production roster FIVE files (`h2/client.go` capture + **D-84-VALIDATE at CAPTURE time**, failing the stream via `h2.NewStreamError` -> RST_STREAM(INTERNAL_ERROR), NO `EvictH2ConnOnError`; `h2/stream.go` EXTRACT of the connection-specific set; `router/router.go` `ActionResponse.Trailers`; `router_h2.go` the ONE populate site; `h2dispatch.go` `writeH2Reply` at THREE call sites) + comment-only `http/chain.go:483`, plus ⚠️ **the PLAN's "retry.go no edits" REFUTED BY EXECUTION** (Task 5: `timedOut` misclassified the new `Status:0` producer as a per-try-timeout — 504 + err=nil MEASURED; fixed by EXTRACTING `perTryTimedOutH2` + an 8-row table). `host`/`trailer` pass through (reference-parity); the second-trailing-block rule DROPPED as dead-on-the-wire. Gates: **120/120 differential CLEAN at `87e67fe2`** (`INNER_EXIT=0`, 388.892 s), seam packages + `-race` green, gofmt/vet/golangci 0, **stat surface +0 by call-site enumeration (208/36), NOT `TestNoNewStat*` (proven blind by execution)**. `ROADMAP.md` row 84 `sub-phases` recorded IN PLACE (whole-file leak check run, every axis HELD).

- **prior active-phase:** `phase 84 (grpc-unary-response-trailers) PLAN-84.1 done` (2026-08-07) — lifecycle-state **2 -> 3**; **ROW 84 STAYS `in-progress`**, and `ROADMAP.md`, `DECISIONS.md` and `BEHAVIOR_CONTRACT.md` are all **BYTE-UNTOUCHED** (0 of 7 recent PLANs touched any of them, verified with firing NCs on the seven IMPLs and the seven SPECs). Sentinel `want` stays **116**; the strict `PROPOSED` guard on `ADR-0306` **STAYS ARMED AT 1** — a PLAN that "fixes" it to 0 destroys a live pointer. Base master **`c470cf03`** (from `git rev-parse master`), branch `phase-84-plan`. Docs-only: ZERO production `.go`, ZERO test `.go`. **FIVE investigation agents** on disjoint remits in DETACHED worktrees with private scratch and port bands inside `45300-45799`; **four of the five WROTE REAL CODE AND RAN IT** — two independent seam patches, a validation prototype with 22 unit cases, a live grpc-go RED-anchor probe, a live `contrib-v1.37.2` reference probe over a raw-framer upstream, a live h2spec run, an EXECUTED stat-guard blindness probe, and **SEVEN injected break arms**. ⚠️ **THE HEADLINE: §6.1 CROSSES, BUT NOT WHERE THE SPEC SAYS IT DOES.** The ≈1738 floor is a WHOLE-ROW figure including the 84.2 fixture, PKI and registration buckets; priced from LANDED buckets the **seam leg alone** is floor **≈1103** / **budget ≈2280** / ceiling **≈3376** — so **the row crosses at its floor and the LEG crosses at its BUDGET**, and §6.1 gates *"`PLAN.md` estimates"*, i.e. the budget, not the floor. **Three SPEC budget figures REFUTED:** production floor **46 -> 211** (no row in the population landed under 128 production lines, and a MEASURED but INCOMPLETE variant-A build is already **net +66**); unit-test ceiling **1400 -> 2926** (three of the four reference rows exceeded 1400; the population median is **1910**); `ADR-0306` **~66 -> +36…+70** (the block is already **29 lines landed**). ⚠️ **AND THE COMMENT-FRACTION CATEGORY ERROR — THE SPEC COMMITTED THE SPECIES IT DIAGNOSED ONE SECTION EARLIER:** 39.2% is a WHOLE-DIFF fraction dominated by a test bucket 5-8x the production bucket's size; the **production-only** fraction is **60.0 / 78.3 / 64.8 / 83.4%, median 71.6%**. ⚠️ **SECOND: THE RED ANCHOR IS CONFIRMED 3/3 AND THERE IS A SECOND ARM NO DOCUMENT NAMED** — the success unary fails `Internal: server closed the stream without sending trailers` and the **error** unary degrades **silently** to `Unknown desc = ` with an EMPTY message, a worse failure mode; both go GREEN under variant A, with a plain-H2 GET invariance control byte-stable across base/A/B. **The split is SOUND on this axis.** ⚠️ **THIRD: THE ROW'S HIGHEST-LEVERAGE DECISION IS INVISIBLE TO THE ENTIRE EXISTING TEST SURFACE** — two agents on disjoint remits measured it independently: variant B (unconditional END_STREAM) passes **every** existing unit test **and** every runtime probe, and injecting it reddens **zero** pre-existing tests. D-84-ENDSTREAM is **UNMEASURED, not merely ungated**, without the new 4-cell frame matrix — and its blast radius is refuted **~13x** (**35 of 120** fixtures mention `http2_protocol_options`, only **3** have a downstream ALPN-h2 listener, not 40). ⚠️ **FOURTH: THE REFERENCE REJECTS MALFORMED TRAILERS IN ITS UPSTREAM CODEC AND FORWARDS `host` AND `trailer` VERBATIM** (16 paths measured; `rx_messaging_error: 17` / `upstream_rq_tx_reset: 17` = exactly the reject rows) — **so the SPEC's RFC 9110 §6.5.1 field list would have made 84.2 RED on a CORRECT tree.** ⚠️ **FIFTH: A NUMBER IN THE LANDED `ADR-0306` IS WRONG — NINE slash-form selectors, not ten** (`http2/6/6` is a `//` comment), propagated into the SPEC, `STATE.md`, `next-prompt.txt` and the SPEC commit's own subject. **THIRTY-ONE refutations, THIRTEEN load-bearing**, plus **broken-gate shapes 34 (an NC run with a BROADER pattern than the gate it controls) and 35 (an ordered validation leg no wire sequence can reach)**.

- **prior active-phase:** `phase 84 (grpc-unary-response-trailers) SPEC done` (2026-08-06) — lifecycle-state **1 -> 2**; **ROW 84 STAYS `in-progress`**, `ROADMAP.md` and `BEHAVIOR_CONTRACT.md` **BYTE-UNTOUCHED**, sentinel `want` stays **116**. Base master **`e1c07208`** (from `git rev-parse master`), branch `phase-84-spec`. Docs-only: ZERO production `.go`, ZERO test `.go`. **FIVE investigation agents** on disjoint remits in DETACHED worktrees with private scratch and port bands inside `44800-45299`; every load-bearing claim controller-re-derived, and **three agent claims did NOT survive** — one of them a **refutation aimed at a claim the BRAINSTORM never made** (`reference_refutation_must_answer_the_claim_as_stated`). ⚠️ **THE HEADLINE: THE CONFORMANCE GATE THIS ROW WAS CHARTERED TO REASON ABOUT HAS NEVER RUN THE SECTION IN QUESTION — OR ANY OF RFC 9113 §6.** `http2/6/10` selects **zero** cases: h2spec addresses sections with **dotted** numbers, `h2spec.go:22-36` declares **ten** slash-form strings, and an unmatched positional argument is a **silent no-op**. Proven from inside the repo without running anything — the total decomposes as `3`=2 + `4`=9 + `5`=22 + `7`=2 + `8`=18 = **53**, and `CONFORMANCE_PINS.md`'s own audit table has listed the **18 suites actually observed since 2026-04-25 with ZERO 6.x rows, totalling 53**, while **ADR-0051 §2 asserts the gate runs `http2/6/1-6/5` and `http2/6/7-6/10`**. ⚠️ **The pin doc and the ADR have contradicted each other for ~80 phases and neither was noticed because both end in "53".** The correct selector yields **42 tests / 37 passed / 1 skipped / 4 FAILED** — four envoy-go-specific failures hidden the whole time, against a **disjoint** set of three on the reference; `assertThreshold` **cannot see a zero-case suite** (`if s.Tests == 0 { continue }`); and ⚠️ **the gate is not run in CI at all** (zero conformance refs in `.github/`; `TestH2Spec` skips under `-short`). ⇒ **NO ROW MAY CITE "h2spec 53/53" AS FRAME-LEVEL EVIDENCE — 44% of its declared scope is unrun.** ⚠️ **SECOND: Q4 INVERTS — ADR-0058 §Consequences ALREADY PRESCRIBES THIS ROW'S FIX VERBATIM** (*"widen `H2Response` with a `Trailers []hpack.HeaderField` field … emit a trailing HEADERS frame conditionally … only an additive field"*), so the row is a **FULFILMENT** and the verdict is **NARROW, not supersede**; and **ADR-0052 `:1821` — naming "gRPC in a gRPC-family phase" explicitly — makes ADR-0306 the MANDATED vehicle**, a constraint no prior document named. ⚠️ **THIRD: §6.1 CROSSES AT THE FLOOR — ≈1738 net `.go`, central ≈2290, against ~1500** — the BRAINSTORM's ~950-1200 is **below the floor** for three measured category errors (the ratio's denominator counts fixture drivers as production; all three rows in its median shipped ~no fixture while the one fixture-bearing row was excluded at **1.22**; comment fraction is **39.2% and rising**, not ~30%). **SO THIS SPEC SPLITS THE ROW 84.1/84.2** — the axis cuts **no** correctness constraint (unlike phase 83's), the precedent is the **phase-61 family opener which split AT ITS SPEC**, and ⚠️ **it costs NO ROADMAP ROW: phase 61's splitting SPEC touched exactly one file with `ROADMAP.md` byte-untouched, and zero dotted rows exist above 32.2.** **TWENTY-SIX refutations, ELEVEN load-bearing**, plus **broken-gate shapes 32 (a declared-but-unmatched gate selector) and 33 (a git pathspec ending in `/` reading a fail-unsafe zero)**.



## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
