# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `phase 36.2 (load-balancer-ring-hash-http) PLAN done` (2026-06-12, this commit) — the phase-36.2 PLAN is AUTHORED (`docs/envoy-go/phases/36.2-load-balancer-ring-hash-http/PLAN.md` — the SPEC §10 spine decomposed into **8 bite-sized TDD tasks**: (1) baselines/anchors gate + PROGRESS.md, (2) `cluster.HashHeaderValues` seed-chained `XXH64` fold, (3) `parseRouteHashPolicies` + the DEPARTURE-reject matrix + the descriptor wiring, (4) the `applyHashKey` fold + the 4 dial-site ctx rebinds + the uniform ctx-carried remote addr, (5) the `0062-lb-ring-hash-http` differential, (6) the deliberate-break liveness, (7) the full 64-dir re-verify + the six-gate + the HTTP-path-touch conformance rationale, (8) the completion bundle with ADR-0237). **All SPEC §12 D-questions RESOLVED:** D-S362-1 → an additive `cluster.HashHeaderValues`; D-S362-2 → the descriptor `router.HashPolicy` is router-owned but the proto-read `parseRouteHashPolicies` stays in `hcm/config.go` (router stays proto-free — a refinement vs the SPEC's loose "router-package parse helper"); D-S362-3 → a uniform ctx-carried remote addr for BOTH codecs (as-built finding: `*http.Request.RemoteAddr` is NOT populated on the HCM H1 codec path) + per-codec `headerVal` shims; D-S362-4 → implement the full multi-value seed-chained fold (the producer feeds single-value); D-S362-5 → pin the go-binding PGV `value length must be at least 1 runes`; D-S362-6 → the 36.1 aggregate-count modular invariant, BOTH sides (NAT-transparent header key); D-S362-7 → re-run-or-assert h2spec/proxy-wasm with the ctx-stuff-only rationale. **ADR-0045 split-gate re-check → NO SPLIT** (~150 prod LoC / 8 tasks). The plan-document-reviewer gate ran (APPROVED first pass; the one advisory — the H1 carry rides the live `ctx` into `RunAction`, not `SetRequestCtx` — folded in). **Counts UNCHANGED at the PLAN** (they advance at the 36.2 IMPL): stat surface **1119**; fixtures **63** (→ 64 at the IMPL); fuzzers **42**; BackendKind **33**; DECISIONS tail **ADR-0236** (next-free **ADR-0237**, lands at the IMPL per ADR-0044).
- **phase-directory:** `docs/envoy-go/phases/36.2-load-balancer-ring-hash-http/` (**SPEC.md + PLAN.md** — the durable 36.2 sub-phase SPEC + the 8-task TDD PLAN, both landed per the 32.1/32.2/29.1 per-sub-phase-dir precedent; PLAN.md landed THIS commit; the PROGRESS.md follows at the 36.2 IMPL session [Task 1 creates it]). The parent `docs/envoy-go/phases/36-load-balancer-ring-hash/` (**BRAINSTORM.md + SPEC.md + PLAN.md + README.md + PROGRESS.md** — the durable phase-36 parent BRAINSTORM/SPEC [both planes] + the 36.1 IMPL record; the 36.1/36.2 split is CONSUMED [parent SPEC §3.0], 36.1 LANDED). **Superseded (the phase-35 phase-directory, preserved):** `docs/envoy-go/phases/35-load-balancer-random/` (**BRAINSTORM.md + SPEC.md + README.md + PLAN.md** — PLAN.md landed THIS commit (README.md status advanced 2 → 3); the PROGRESS.md follows at the IMPL session [Task 1 creates it], per the kafka-31/thrift-33/least_request-34 single-flat-row per-phase-dir precedent). The phase-34 directory `docs/envoy-go/phases/34-load-balancer-least-request/` (**BRAINSTORM.md + SPEC.md + README.md + PLAN.md + PROGRESS.md** — the durable phase-34 IMPL record). The phase-33 directory `docs/envoy-go/phases/33-network-filter-thrift-proxy/` (**BRAINSTORM.md + SPEC.md + README.md + PLAN.md + PROGRESS.md** — the durable phase-33 IMPL record). The phase-32 dirs `docs/envoy-go/phases/32-network-filter-redis-proxy/` + `32.1-…/` + `32.2-network-filter-redis-commands-and-stats/` (**SPEC.md** — the durable phase-32.2 sub-phase SPEC; the PLAN.md follows at the 32.2 PLAN session, the PROGRESS.md at the 32.2 IMPL session [Task 1 creates it], per the 29.1/29.2/29.3 per-sub-phase-dir precedent). The phase-32.1 dir `docs/envoy-go/phases/32.1-network-filter-redis-upstream-pool-seam-and-codec/` (**SPEC.md + PLAN.md + PROGRESS.md** — the durable phase-32.1 IMPL record). The parent `docs/envoy-go/phases/32-network-filter-redis-proxy/` (**BRAINSTORM.md + SPEC.md + README.md** — the durable phase-32 parent-SPEC record). The phase-31 directory `docs/envoy-go/phases/31-network-filter-kafka-broker/` (BRAINSTORM.md + SPEC.md + README.md + PLAN.md + PROGRESS.md — the durable phase-31 IMPL record). The phase-30 + phase-29 directories stay the durable phase-30/29 record.
- **lifecycle-state:** `phase 36.2 PLAN done — next: the 36.2 IMPL (superpowers:subagent-driven-development — execute the 8-task TDD PLAN; fresh subagent per task, two-stage spec+code-quality review between tasks; work in a worktree; subagents commit LOCAL-ONLY, the controller squash-merges + pushes at stage-close)`. The PLAN (`docs/envoy-go/phases/36.2-load-balancer-ring-hash-http/PLAN.md`) is the IMPL input: the 8-task spine with complete code, exact file paths/line anchors, the §6 reject matrix, the `applyHashKey` fold + the 4 dial-site ctx rebinds + the ctx-carried remote addr, the `0062` fixture (the modular-invariant BOTH-side affinity proof), the deliberate-break liveness, the six-gate, and the ADR-0237 completion bundle. The SPEC (`…/SPEC.md`) remains the design reference. The IMPL advances the counts: fixtures 63 → 64 (`0062-lb-ring-hash-http`); DECISIONS tail ADR-0236 → ADR-0237; stat surface STAYS 1119; fuzzers/BackendKind unchanged.
- **next-skill:** the **36.2 IMPL** (`superpowers:subagent-driven-development` — execute the 8-task PLAN: (2) `cluster.HashHeaderValues`, (3) `parseRouteHashPolicies` + reject matrix + descriptor wiring, (4) the `applyHashKey` fold + the 4 dial-site rebinds + the uniform remote-addr carry, (5) the `0062-lb-ring-hash-http` differential, (6) the deliberate-breaks, (7) the full 64-dir re-verify + the HTTP-path-touch conformance rationale, (8) the ADR-0237 + STATE/ROADMAP completion bundle).
- **last-commit:** the phase-36.2 PLAN authoring bundle — the durable `docs/envoy-go/phases/36.2-load-balancer-ring-hash-http/PLAN.md` (the 8-task TDD decomposition; all D-S362-1..7 resolved; ADR-0045 re-check NO SPLIT; plan-document-reviewer APPROVED) + STATE/ROADMAP row-36 advance (the 36.2 PLAN authored; counts unchanged). The prior commit was the phase-36.2 SPEC authoring bundle.
- **last-updated:** 2026-06-12 (phase-36.2 PLAN done)
- **next-free ADR:** `ADR-0237` (DECISIONS.md tail STAYS **ADR-0236** at this PLAN — the ADR-0237 [the HTTP route `hash_policy` producer plane] §Context is a DRAFT in SPEC §13; the full entry [§Context + §Decision + §Consequences] lands at the 36.2 IMPL per ADR-0044).

---

## Project counts at this commit

- **active differential fixtures:** **63** (tail `0061-lb-ring-hash`). Canonical recipe `ls -d test/fixtures/[0-9]* | wc -l` = 63. Advanced at the phase-36.1 IMPL (+1 — `0061-lb-ring-hash`, the tcp source_ip subject-side affinity+spread differential; 36.2 will add `0062-lb-ring-hash-http`).
- **fuzzers:** **42** (canonical recipe `grep -rh "^func Fuzz" $(find ./internal -name fuzz_test.go) | wc -l`; tail `FuzzThriftDecode`). UNCHANGED at the phase-36.1 IMPL (+0 — the THIRD no-fuzzer phase; ring_hash decodes no untrusted wire bytes — the hash key derives from a source IP / parsed value, not a wire frame; a ring-lookup property test is unit-level).
- **stat surface:** **1119** (BEHAVIOR_CONTRACT doc count). Advanced at the phase-36.1 IMPL (1116 → **1119**, +3 — the 3 MIRRORED `cluster.<name>.ring_hash_lb.{size,min_hashes_per_host,max_hashes_per_host}` GAUGES; the FIRST non-zero LB-stat delta — least_request-34 + random-35 were both zero-delta phases; cross-side-exact, keyed only on ring-config + host count, NOT endpoint addresses; registered ONLY on the RING_HASH path; the DOC count is the source-of-truth, no programmatic golden).
- **BackendKind tail:** **33** (`TCPThriftResponder` — seq_id-agnostic canned framed-binary REPLY). UNCHANGED at the phase-36.1 IMPL (+0 — `0061` reuses `TCPEcho` 0; an LB phase exercises WHERE conns land, not a new backend).
- **DECISIONS.md tail:** **ADR-0236** (next-free **ADR-0237**) — TWO ADRs landed at the phase-36.1 IMPL: **ADR-0235** (the LB hash-key seam extension — the ADR-0232 PICK-INPUT half; the ctx-carried hash key; the seam's FIRST extension) + **ADR-0236** (the `ring_hash` load-balancing policy — the Ketama consistent-hash ring; the project's FOURTH LB policy + FIRST consistent-hashing policy), both PROPOSED → ACCEPTED in-place per ADR-0044 (the redis-32 / least_request-34 two-ADR shape, since the seam is EXTENDED not merely reused).
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227, superseding ADR-0008).
- **conformance:** h2spec 53/53; proxy-wasm all 10 families PASS (image-independent; UNAFFECTED by phase 36.1 — touches only `internal/cluster` [the LB seam + ring_hash policy] + `internal/filter/tcpproxy` [the source_ip plane]; no HTTP/h2/proxy-wasm path).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
