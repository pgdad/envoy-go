# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `04-http-1.1`
- **phase-directory:** `docs/envoy-go/phases/04-http-1.1/` — exists; contains `SPEC.md`, `PLAN.md`, `PROGRESS.md` (18 task entries: original 17 + Task 18 state-3 re-entry from commit `4093723`, plus a fresh-session "## Verification (lifecycle-state 4)" section between Tasks 17 and 18 with the original pre-follow-ups verbatim gate captures), and `REVIEW.md` (committed `04527eb` by the prior state-5 session — verdict APPROVED WITH FOLLOW-UPS, 0 Critical, 4 Important, 7 Minor; the four Important items plus M-1 are now landed in `671a059`). All artifacts on branch `phase/04-http-1.1-impl` under `.worktrees/phase-04-http-1.1-impl`. Phase 03's directory at `docs/envoy-go/phases/03-tls/` is closed and read-only history.
- **lifecycle-state:** `4` — implementation complete inclusive of REVIEW.md follow-ups; awaiting fresh state-4 re-verification. State machine flow this session: state 5 (REVIEW.md issued APPROVED WITH FOLLOW-UPS by prior session in `04527eb`) → re-entered state 3 per BOOTSTRAP §5.2 ("if issues → back to step 3"; project precedent is phase 03's `98cc35b` — land I-class follow-ups inside the phase rather than deferring to the next phase) → state 3 work completed in `671a059` (REVIEW follow-ups I-1..I-4 + M-1) and recorded as PROGRESS Task 18 in `4093723` → state 3 → state 4 transition (one forward state move this session per §5.1). The five fixes landed: I-1 `routerAction.do` now returns `errCloseAfterAction` when the upstream's response signals close (`resp.Close == true`), wiring the connection loop's existing `closeAfterAction` handling per SPEC §5.3; new TDD regression test `TestRunConnection_UpstreamConnectionCloseClosesDownstream` in `internal/filter/hcm/connection_test.go` (red→green captured in PROGRESS Task 18); existing `TestRouterAction_DoHappy` tightened to accept the sentinel return as non-failure. I-2 `TestRouterAction_DoCtxCancel` no longer silently discards `a.do`'s return — asserts nil to catch a future regression where ctx-cancel propagates instead of mapping to a 503 local reply per SPEC §7. I-3 `routerAction.do` now propagates `ctx.Deadline` (if any) to the upstream socket via `SetDeadline` before `req.Write` — closes the post-dial-stall window; comment-only / timing-dependent, not TDD-able without flaky tests, per phase-03 `98cc35b` precedent for I-3-class changes. I-4 `internal/cluster/manager.go` lines 83/86/89/140 stale "phase 02 …" qualifiers dropped per REVIEW prescription (constraint is project-wide; phase qualifier misled operators); `manager_test.go` assertions updated; line 115's "phase 03" qualifier is out of REVIEW scope and left unchanged; TDD discipline observed (tests-red-then-prod-green captured in PROGRESS Task 18). M-1 `docs/envoy-go/DECISIONS.md` orphan/duplicate "Lands in Task 13 …" tail line at file end deleted; ADR-0043's body already documents the informal supersession in prose at line 1399. The 6 remaining Minor REVIEW findings (M-2..M-7) carry forward to future-phase REVIEW triage; no fix is committed in this re-entry. Local gate sweep on the impl worktree from this session was clean (`go build`, `go vet`, `golangci-lint run`, `go test -count=1 -timeout=5m ./internal/... ./test/helpers ./test/fixtures/... ./cmd/...` all exit 0 / all-OK; verbatim outputs captured in PROGRESS Task 18). Differential, fuzz, and full state-4 verification re-run fully under state 4 (next session) — the local sweep here is state-3 evidence, not substitute evidence for the next session's required fresh-shell re-run. Reference Envoy pin remains `envoyproxy/envoy@sha256:c5e8a68e52f4d4697a9adb280dbe415d77fedf1257e183dcb86205bd438f18bd` (v1.37.2, per `ENVOY_TARGET.md` / ADR-0008).
- **next-skill:** `superpowers:verification-before-completion`
- **next-skill-scope:** Re-run every SPEC §3 phase-done gate that is in scope at state 4 from a fresh shell against impl tip `4093723` (or HEAD as resolved at session-start), and append a NEW "## Verification (lifecycle-state 4 — post-follow-ups)" block to `docs/envoy-go/phases/04-http-1.1/PROGRESS.md` (do NOT overwrite the existing pre-follow-ups verification block — that one captures the state of the tree before the I-1..I-4 + M-1 fixes; the new block captures the state after). Required gates: (a) `TestDifferential/0003-http11-routing` remains green after the I-1 / I-3 routerAction.do edits (the resp.Close check + ctx.Deadline propagation should not change observable behaviour against fixture 0003 — every fixture-0003 request already drives `Connection: close` from the test client, so the request-side close was already triggering loop exit); (b) regression-clean `0000-tcp-echo` / `0001-tcp-proxy-rr` / `0002-tls-tcp` (those fixtures don't exercise routerAction.do); (c) N/A — vacuously green; (d) all four fuzzers (`FuzzBootstrapLoad`, `FuzzTcpProxyFilter`, `FuzzTLSContextParse`, `FuzzHCMConfigParse`) at ADR-0018's 30-second budget — no crashes, no `testdata/fuzz/` seed-corpus drift; (e) `go build`, `go vet`, `golangci-lint run`, `go test -count=1 -timeout=5m ./internal/... ./test/helpers ./test/fixtures/... ./cmd/...` all exit 0 / all-OK. (f) deferred to state 5/6 per BOOTSTRAP §5 state 4. Work from the impl worktree (`/home/esa/git/envoy-go/.worktrees/phase-04-http-1.1-impl`). After verification, the next session decides on the §5.2 re-review question: REVIEW.md (`04527eb`) was already APPROVED WITH FOLLOW-UPS and the four Important + M-1 items are now landed in `671a059` — strict §5 ordering would call for a brief state-5 confirming addendum (a focused `superpowers:requesting-code-review` round verifying I-1..I-4 + M-1 match the REVIEW.md recommendations); a more pragmatic reading treats the existing REVIEW as fully satisfied and skips directly to state 6 finalisation. The orchestrator should make that judgment call after the state-4 verification block lands. State 6 finalisation (the phase-done commit per §5.3 with format `phase 04: HTTP/1.1 [ADR-0037, ..., ADR-0044]`, `ROADMAP.md` status flip to `done`, master fast-forward to impl tip per ADR-0003, and STATE handoff to phase 05) follows in either case.
- **last-commit:** 4093723
- **last-updated:** 2026-04-25

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
