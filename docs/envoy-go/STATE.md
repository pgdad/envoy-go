# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `<next-§9-family-row>` — phase 14 complete as of this STATE.md SHA-fill (lifecycle-state-6 → awaiting next planning). Next phase is the next §9 HTTP filters family-row (remaining unbrainstormed members: jwt_authn, rbac, ext_authz, ext_proc, oauth2, lua, wasm, adaptive concurrency, admission control, bandwidth limit, global rate limit); determined by the next brainstorm session consulting the §9 heading + phase 14 just-shipped artefacts per ADR-0106(e). Phase 14 shipped `envoy.filters.http.compressor` — SEVENTH §9 family-row (after cors @ 07.1, fault @ 09, header_mutation @ 10, local_ratelimit @ 11, csrf @ 12, buffer @ 13); 6 ADRs (ADR-0129..ADR-0134) + ADR-0125 amendment §(viii)-(x); 17 differential fixtures (0000-0016) + 18 fuzzers green; 6 gates all pass.
- **phase-directory:** `docs/envoy-go/phases/14-http-filter-compressor/` — COMPLETE. Contains BRAINSTORM.md + SPEC.md + PLAN.md + PROGRESS.md (Tasks 1-16 documented) + REVIEW.md (191 LoC per phase-13 structural template). The squash-merge commit `9df9a29` + STATE.md SHA-fill follow-up `a3895b1` close the impl-lifecycle transitions through state-6.
- **lifecycle-state:** `awaiting next planning` — phase 14 BRAINSTORM.md authored (squash-merged at `643294f`; SHA-fill at `51b9ea6`), SPEC.md authored (squash-merged at `073cb88`; SHA-fill at `2b262b8`), PLAN.md authored (squash-merged at `bdcb7c1`; SHA-fill at `3af5d3a`), implementation Tasks 1-15 landed in-session, REVIEW.md at Task 16, squash-merged at `9df9a29`. All 6 phase-done gates green (Gate A build/vet/lint clean; Gate B race-test all packages PASS; Gate C h2spec 53/53 PASS at ADR-0051 pin; Gate D 18 fuzzers green at 30s budget each; Gate E 17 differential fixtures 0000-0016 PASS in ~47s wallclock; Gate F BEHAVIOR_CONTRACT.md populated per SPEC §13.1-§13.4 — compressor subsection at ~line 1302 + 29→46 stat-name table extension + equivalence-matrix row 14 + Phase 14 forward-pointer notes subsection). ROADMAP row 14 flipped `in-progress → done` at the squash-merge with date `2026-05-10`. ADR roster: ADR-0129 + ADR-0130 (Task 2), ADR-0131 (Task 4), ADR-0132 (Task 8 — Lands-in-task SPEC-time error caught + corrected via 1-line clerical edit at Task 8 per ADR-0127-v2 in-place-update precedent), ADR-0133 (Task 11), ADR-0134 (Task 14 follow-up; impl-time addition per ADR-0044 escape-valve — unanticipated HCM `directResponseAction.response_headers_to_add` framework delta uncovered at fixture integration when scenario 2's image/png content-type-skip path failed because envoy-go's `directResponseAction.body()` hardcoded Content-Type: text/plain), ADR-0125 amendment §(viii)-(x) (in-place at SPEC commit per phase-13 ADR-0127-v2 in-place-update precedent).
- **next-skill:** `superpowers:brainstorming` — targeting the next §9 HTTP filters family-row (the family heading at ROADMAP line 56+ enumerates the remaining members; cold-start per ADR-0106(e) from the §9 heading + phase 14 just-shipped artefacts).
- **next-skill-scope:** Cold-start scope for the next brainstorm session. Read first: `docs/envoy-go/ROADMAP.md` §9 family heading (line 56+) for the family-row ordering; `docs/envoy-go/phases/14-http-filter-compressor/SPEC.md` §1.3 + §1.4 (family-expansion shape + ADR-0045 release valve) for cold-start discipline; `docs/envoy-go/phases/14-http-filter-compressor/REVIEW.md` (191 LoC; §1-§7 with phase 14 retrospective + 4-counter per-side empirical-divergence + ADR-0134 PLAN-deviation honesty + 6 lessons learned); `docs/envoy-go/BEHAVIOR_CONTRACT.md` `### Phase 14 forward-pointer notes` subsection (~line 1738+) for the deferred surface from phase 14 (8 BRAINSTORM §8 deferrals: brotli + zstd codec extensions; request-side compression via `request_direction_config` activation; future `envoy.filters.http.decompressor` filter; chunked-encoded response wire shape; Gzip codec sub-knobs not in Go gzip; `choose_first` first-acceptable selection mode; `runtime_enabled` + `enabled` runtime gates; deprecated top-level mirrors + ADR-0134 AppendAction broader support deferral + request_headers_to_add symmetric path deferral + 4-counter per-side empirical divergences); `docs/envoy-go/DECISIONS.md` ADR-0106 (flat top-level row; next-free number ADR-0135) for the row-expansion discipline.
- **last-commit:** `a3895b1` — `phase 14 impl follow-up: STATE.md SHA-fill (TBD → 9df9a29 post-squash)`. Master tail: `a3895b1` (this STATE.md SHA-fill) → `9df9a29` (squash-merge of phase-14-impl chain; lands all 33 worktree commits as one master commit: code + tests + fixture 0016 + echobackend helper + BEHAVIOR_CONTRACT 4-edit + ROADMAP done + STATE phase-done + REVIEW.md + 6-ADR roster) → `3af5d3a` (phase 14 PLAN follow-up STATE.md SHA-fill) → `bdcb7c1` (phase 14 PLAN squash-merge) → `2b262b8` (phase 14 SPEC follow-up STATE.md SHA-fill) → `073cb88` (phase 14 SPEC squash-merge) → preceding phase-13 commits.
- **last-updated:** 2026-05-10
- **next-free ADR:** `ADR-0135` (phase-14 ADR roster ended at ADR-0134 inclusive; advances by 1 for the next phase). The phase-14 ADR roster expanded from PLAN's 5 to a 6-ADR set due to the ADR-0134 escape-valve at Task 14 follow-up.

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
