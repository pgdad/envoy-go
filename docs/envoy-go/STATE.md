# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `05.1-downstream-h2`
- **phase-directory:** `docs/envoy-go/phases/05.1-downstream-h2/` — contains `README.md` (placeholder, written at split time per ADR-0045 / commit `2162509`), `SPEC.md` (drafted by brainstorming per ADR-0004 autonomous mode; commit `4b45941`), `PLAN.md` (drafted by writing-plans per ADR-0005 autonomous mode; commit `634bcb6`; 3331 lines, 16 tasks, 8 new ADRs), and `PROGRESS.md` (executor-appended through Tasks 1–16; final SHA-fills at `b416664`; verification-block appended at this STATE.md commit). The sibling `docs/envoy-go/phases/05.2-upstream-h2/` is unchanged from the split commit (placeholder `README.md` only). The parent `docs/envoy-go/phases/05-http-2/` retains its `SPEC.md` (`612cdea`) as the master design document for both sub-phases per ADR-0045 and phase-05 SPEC §11.1; that directory remains closed read-only history at the parent level (no PLAN/PROGRESS/REVIEW will land under it; those land under the sub-phase directories).
- **lifecycle-state:** `3` — bounced back from 4 by the verification-before-completion session per the gate-(e) prescription written into the prior STATE.md (`b61e61f`): "if the count is still non-zero, that session bounces to state-3 (REVIEW re-entry) for cleanup or substantive triage rather than papering over with allow-lists." Five of six SPEC §3 gates verified GREEN at HEAD `b61e61f` from `.worktrees/phase-05.1-downstream-h2-verify` (branched from master tip per ADR-0003): (a) vacuously green — no new differential fixture in 05.1 per ADR-0045; (b) `0000-tcp-echo` / `0001-tcp-proxy-rr` / `0002-tls-tcp` / `0003-http11-routing` all PASS, plus auxiliary `go test -race ./...` clean across every package; (c) h2spec 53/53 PASS over sections 3.5/4.1/4.2/4.3/5.1/5.1.1/5.1.2/5.3.1/5.4.1/5.5/7/8.1/8.1.2/8.1.2.1/8.1.2.2/8.1.2.3/8.1.2.6/8.2 at the ADR-0051 pin; (d) all six fuzzers PASS at 30s budget with `git status --porcelain` empty after each (no testdata/fuzz pollution); (e) `go build` / `go vet` / `go test ./...` clean, ADR-0046 boundary grep clean, ADR-0048 `client.go` absence verified — **but `golangci-lint run ./...` exits 1 with 38 issues** (down from STATE.md `b61e61f`'s preliminary 118 estimate; the executor's count likely included multi-line revive bodies — the canonical issue count at `b61e61f` is 38). Per-linter: 18 errcheck (17 test-file `defer Close()` + 1 production `internal/filter/hcm/filter.go:36` `defer downstream.Close()`), 12 misspell (British→American: `synthesise`/`synthesising`/`behaviour`/`signalled`/`Serialise`/`honour`/`honouring`/`honoured`/`cancelling`), 4 gofmt (alignment in `h2/conn.go` / `h2/fuzz_test.go` / `h2spec/h2spec.go` / `h2spec/h2spec_test.go`), 2 revive (`exported` doc-comment on `ErrNoError` const block in `h2/errors.go:11`; `package-comments` shape on `h2dispatch.go:1`), 2 unused (`hpackBlockSID uint32` field in `h2/conn.go:38`, paired with the used `hpackBlocked bool`; `func ctxErr(ctx, fallback) error` in `h2/framer.go:156` — both require code-reading judgement to confirm they are not 05.2 forward-look). Verification-block PROGRESS entry committed at this STATE.md commit; full gate outputs quoted verbatim. Gate (f) — `REVIEW.md` — remains state-5 work (`superpowers:requesting-code-review`), not run by the verification session.
- **block-reason:** Gate (e) lint count non-zero (38 issues at HEAD `b61e61f`). Cleanup deferred to state-3 follow-up per the verification block's fix-up plan in `phases/05.1-downstream-h2/PROGRESS.md` (the `## Verification (lifecycle-state 4)` block). 36/38 issues are mechanical (errcheck wrapper, gofmt, misspell sed, revive doc-comment); 2/38 (the two unused-symbol findings) require code-reading triage to confirm dead-vs-forward-look before deletion. Cleanup MUST avoid `//nolint` allow-list paper per the prior STATE.md's explicit guidance.
- **next-skill:** `superpowers:test-driven-development` (for the production-touching changes — the `internal/filter/hcm/filter.go:36` `errcheck` fix and the two `unused`-symbol deletions/justifications), wrapped under `superpowers:subagent-driven-development` to land the mechanical batches (gofmt, misspell, test-file errcheck) in parallel. Direct execution is also acceptable for the strictly-mechanical fixes (`gofmt -w` and the misspell sed). Per BOOTSTRAP_PROMPT.md §5 step 5 re-entry pattern: state-3 work continues on a `phase/05.1-downstream-h2-impl-followup` (or similar) worktree branched from this STATE.md commit; the verify worktree at `.worktrees/phase-05.1-downstream-h2-verify` is closed-history at this state transition.
- **next-skill-scope:** Land the gate-(e) lint cleanup as a single atomic state-3 follow-up sequence: (1) `gofmt -w` the four files (`h2/conn.go`, `h2/fuzz_test.go`, `h2spec/h2spec.go`, `h2spec/h2spec_test.go`); (2) British→American sed across the 12 misspell sites (production: `h2/conn.go`, `h2/framer.go`, `h2/stream.go`, `h2dispatch.go`; test: `h2/conn_test.go`, `h2/stream_test.go`); (3) wrap test-file `defer c.Close()` to `defer func() { _ = c.Close() }()` (or `t.Cleanup(...)`) across `framer_test.go` (12 sites), `settings_test.go` (4), `fuzz_test.go` (1); (4) wrap production `internal/filter/hcm/filter.go:36` `defer downstream.Close()` with the same idiom — explicitly drop the close error on connection-tear-down per phase-04 precedent in `internal/filter/hcm/connection.go`; (5) add a doc comment to the `ErrNoError`-anchored const block in `h2/errors.go` matching the revive `exported` rule shape; (6) refactor `h2dispatch.go`'s leading file-comment so the package-attached comment starts with `// Package hcm` (the existing prose can move into a separate non-package-attached comment block); (7) confirm `hpackBlockSID` is not 05.2 forward-look (cross-check PLAN.md, SPEC §5.3 / §5.8, and ADR-0048's boundary docs) — likely deletion; (8) confirm `func ctxErr` has no callers and is not test-helper-pending — likely deletion; for both, if deletion is wrong, carry an explicit deferral note + ADR justification rather than a bare `//nolint`. After the cleanup, re-run gate (e) `golangci-lint run ./...` from a fresh terminal in the impl-followup worktree; once the count is zero AND `go test -race ./...` AND `go test ./test/conformance/h2spec/...` AND the six fuzzers all remain green, append a small "Task 17 — gate-(e) lint cleanup" entry to `PROGRESS.md` quoting the new outputs; commit; FF master; advance `lifecycle-state` back to 4 (NOT 5; the verification re-run is the state-3→state-4 promotion, after which a SECOND `superpowers:verification-before-completion` session runs the full re-verification at the new HEAD). Reading order for the state-3 follow-up: this STATE.md, `phases/05.1-downstream-h2/PROGRESS.md` `## Verification (lifecycle-state 4)` block (the fix-up plan is canonical there), `phases/05.1-downstream-h2/SPEC.md` §3 + §5.5 + §5.8, `phases/05.1-downstream-h2/PLAN.md` (for any 05.2 forward-look hint on `hpackBlockSID` / `ctxErr`).
- **last-commit:** `b61e61f` — the prior STATE.md commit (lifecycle-state 3→4 promotion) on branch `phase/05.1-downstream-h2-impl`. This STATE.md commit (the verification-block + state-4→state-3 bounce commit on branch `phase/05.1-downstream-h2-verify`) is the 1st commit on the verify branch. Master is fast-forwarded to this STATE.md commit per ADR-0003.
- **last-updated:** 2026-04-26

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
