# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `25 (parent; in-progress) — awaiting 25.1 SPEC` — **phase 25 BRAINSTORM DONE at 2026-05-24 (this commit)** authoring `docs/envoy-go/phases/25-http-filter-wasm/BRAINSTORM.md` for the FINAL §9 HTTP-filters-family row `wasm` (`envoy.extensions.filters.http.wasm.v3.Wasm`). The BRAINSTORM session settled 9 user-decided Q-decisions + 3 post-synthesis confirmations: Q1 FULL upstream parity + 3-way PRE-SPLIT AT BRAINSTORM TIME (SECOND §9 row to pre-split at BRAINSTORM after phase-22 lua; sub-phases 25.1-http-filter-wasm-runtime-and-headers-bridge / 25.2-http-filter-wasm-body-and-advanced-bridge / 25.3-http-filter-wasm-perroute-and-conformance); Q2 `github.com/tetratelabs/wazero` (pure-Go WASM runtime; CNCF Sandbox; no CGO — matches phase-22 gopher-lua pure-Go precedent); Q3 implement proxy-wasm v0.2.1 host ABI IN-HOUSE at `internal/wasm/` (~40+ host functions; NO third-party proxy-wasm-go-host dep); Q4 envoy-go-strict DEFAULT-DENY capability roster (matches phase-22 lua sandbox precedent; upstream defaults to allow-all — envoy-go-strict departure); Q5 BRAINSTORM hypothesizes 5th-canonical REUSE per-route (no dedicated `WasmPerRoute` proto visible at brainstorm; SPEC empirically pins against v1.37.2 + go-control-plane v1.32.4 — if confirmed, ADR-0125 STAYS at 10 per phase-23 + phase-24.1 REUSE-by-absence precedent; if SPEC surfaces novel-shape proto, escalates to NEW 11th canonical + ADR-0125 amendment 10 → 11); Q6 FULL cross-side byte-exact across all 3 sub-phases (phase-22 lua precedent; mixed-mode fallback at 25.2 for non-deterministic httpCall/tick/metric-emission per ADR-0192); Q7 FEATURE-PROGRESSIVE split axis (phase-22 lua precedent — 25.1 primitive + headers; 25.2 body + advanced; 25.3 per-route + multi-plugin + conformance); Q8 upstream-parity 5 counters + envoy-go-strict 2 extensions `executions` + `hostcall_denied` at 25.1 (phase-22 lua precedent; project total 114 → ~121); Q9 PRE-BUILT VENDORED Rust-sourced `.wasm` files under `test/fixtures/wasm-plugins/` (no toolchain in CI; canonical proxy-wasm-rust-sdk ecosystem). Post-synthesis confirmations: sub-phase slugs OK; +6 fixture-dir delta (35 → 41 across the family) OK; Rust source language confirmed. **NEW `internal/wasm/` framework primitive at 25.1 first consumer** (SECOND occurrence of EXTRACT-NOW-at-first-consumer discipline after phase-22.1 `internal/lua/`; ENDS phase-24 zero-new-package-level-primitive disposition; EXPLICIT API-REVISION ALLOWANCE clause for consumer #2 — broader §9 WASM host family which consumes the primitive at multi-consumer scope: cluster-specifier-wasm + access-logger-wasm + network-filter-wasm + WasmService singleton plugins). **NEW go.mod direct dep `github.com/tetratelabs/wazero`** at 25.1 (SECOND VM-class third-party dep after phase-22 gopher-lua). 25.2 RE-CONSUMES phase-20 `internal/httpclient/` (ADR-0177 third-or-later co-consumer) for proxy_dispatch_http_call — validates the phase-20 framework extraction. 25.3 seeds `test/conformance/proxy-wasm/` per `BOOTSTRAP_PROMPT.md §7.3` anticipation. **Anticipated 8-12 ADRs across the family (ADR-0202 .. ADR-0213)**: 25.1 anchors ADR-0202 (NEW `internal/wasm/` framework primitive + wazero dep + API-revision-allowance) + ADR-0203 (NEW `internal/filter/http/wasm/` package shape + in-house proxy-wasm v0.2.1 host ABI implementation + 7-counter stat surface + fixture-0034 + test-bytecode-vendoring) + ADR-0204 (proxy-wasm capability-restriction default-deny + sandbox posture); WEAK-HOLD-with-2-slot-buffer prediction at 25.1 IMPL (larger surface than phase-22.1 justifies wider buffer). **Counts at family-row close (post-25.3 IMPL DONE hypothesized)**: 20 HTTP filters wired (19 → 20 at 25.1; STAYS at 20 across 25.2 + 25.3); 36 fuzzers (33 → 34 `FuzzWasmConfigParse` at 25.1; 34 → 35 `FuzzWasmHostcallEnvelope` at 25.2; 35 → 36 `FuzzWasmPerRouteConfig` at 25.3); 41 differential fixture dirs (35 → 37 at 25.1; 37 → 39 at 25.2; 39 → 41 at 25.3 — per `reference_differential_fixture_dispatch_constraint` one-dir = one-branch); ~125+ stats (depending on SPEC empirical pin; +7 at 25.1 + ~4 envoy-go-strict counters at 25.2 + open-ended plugin-defined dynamic-stats namespace at 25.2 not counted in static total); ~25-26 envoy-go-strict departures (+5-8 across the family for sandbox default-deny + `executions`/`hostcall_denied` + AsyncDataSource.Remote PARSE-REJECT + upstream-distinct runtime-name PARSE-REJECT + proxy-wasm v0.1.0 ABI PARSE-REJECT + 25.2 counters + foreign-function default-deny). After phase 25 phase-done: **§9 HTTP-filters family closes to 0 remaining rows** — phase 25 is the FINAL §9 HTTP-filters-family row. **ROADMAP rows added at this BRAINSTORM commit**: parent row 25 (`in-progress`, sub-phases `25.1, 25.2, 25.3`) + sub-rows 25.1 + 25.2 + 25.3 (all `planned`). Sub-phase directory READMEs pre-created at this commit per phase-22 precedent (`25-http-filter-wasm/BRAINSTORM.md` + `25.1-…/README.md` + `25.2-…/README.md` + `25.3-…/README.md`). Phases 23 + 24 + 24.1 + 24.2 DONE and CANONICAL — DO NOT reopen.
- **phase-directory:** `docs/envoy-go/phases/25-http-filter-wasm/` (BRAINSTORM.md landed at this commit; SPEC.md + PLAN.md NOT yet authored). Sibling directories `docs/envoy-go/phases/25.1-http-filter-wasm-runtime-and-headers-bridge/` + `docs/envoy-go/phases/25.2-http-filter-wasm-body-and-advanced-bridge/` + `docs/envoy-go/phases/25.3-http-filter-wasm-perroute-and-conformance/` pre-created with README.md forward-pointers per phase-22 precedent.
- **lifecycle-state:** `phase 25 BRAINSTORM done (lifecycle-state 0 → 1); awaiting parent SPEC authoring` (SKILL_ROUTING state 1 — phase 25 in ROADMAP, parent directory exists with BRAINSTORM.md, parent SPEC.md does NOT yet exist; sub-phase directories exist with README.md only). ROADMAP rows 25 (parent, `in-progress`) + 25.1 + 25.2 + 25.3 (all sub-phases, `planned`) registered AT THIS COMMIT. Per ADR-0106, the parent row registers as `in-progress` with sub-phases listed at this BRAINSTORM-DONE commit (matches phase-22 + phase-24 precedent of BRAINSTORM-time pre-split flipping the parent to `in-progress`).
- **next-skill:** `superpowers:writing-plans` (scoped to **parent SPEC authoring** at `docs/envoy-go/phases/25-http-filter-wasm/SPEC.md` per the phase 18 + phase 19 + phase 22 + phase 24 parent-row precedent — the parent SPEC formalizes the 3-way split surface-mapping + executes the §10 empirical-pin obligations IN-SESSION against reference Envoy v1.37.2 per ADR-0004). After parent SPEC lands, per-sub-phase SPEC sessions (25.1 / 25.2 / 25.3) follow; each sub-phase's SPEC lands at its own dedicated session per the phase 18.1 / 18.2 / 19.1 / 19.2 / 22.1 / 22.2 / 22.3 / 24.1 / 24.2 precedent. Sub-phase IMPL via `superpowers:subagent-driven-development` per `feedback_execution_style` project memory.
- **last-commit:** `c17cbf0` — `Squash merge phase-25-http-filter-wasm-brainstorm`. The phase-25 BRAINSTORM-DONE squash-merge commit landing the BRAINSTORM.md + 3 sub-phase READMEs + ROADMAP rows 25 + 25.1 + 25.2 + 25.3 + this STATE.md update (6 files / +481 / -7 LoC). SHA filled in at this stage-close follow-up commit per the phase-22 + phase-24 BRAINSTORM stage-close SHA-fill precedent (e.g. `1ca1645` 22.3 follow-up `TBD → 0929b3d`; `8466bab` 24 follow-up SHA-fill + next-prompt advance). Predecessor master tip (pre-25-BRAINSTORM): `1ab2014` — `next-prompt.txt: repoint master-tip references to 35f4c73 (actual HEAD)`.
- **last-updated:** 2026-05-24
- **next-free ADR:** `ADR-0202` (UNCONSUMED at 25-BRAINSTORM-done — D-hypothesis HELD across the entire phase-24 family-row at 24.2 phase-done; phase 25 BRAINSTORM does NOT consume any ADR — BRAINSTORM-time decisions live in the BRAINSTORM doc + ROADMAP row addition, NOT as ADR entries per ADR-0106). DECISIONS.md tail at `ADR-0201` (UNCHANGED — phase 25 BRAINSTORM lands no new ADR; ADR-0202 anticipated to anchor the NEW `internal/wasm/` framework primitive at 25.1 IMPL; full 8-12 ADR roster across the family at ADR-0202 .. ADR-0213 anticipated). **D-hypothesis disposition (phase-25 BRAINSTORM)**: `ADR-0202` stays UNCONSUMED — confirmed at BRAINSTORM phase-done (no ADR consumption at BRAINSTORM time per ADR-0106).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
