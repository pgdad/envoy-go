# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `05.1-downstream-h2`
- **phase-directory:** `docs/envoy-go/phases/05.1-downstream-h2/` — contains `README.md` (placeholder, written at split time per ADR-0045 / commit `2162509`), `SPEC.md` (drafted by brainstorming per ADR-0004 autonomous mode; commit `4b45941`), `PLAN.md` (drafted by writing-plans per ADR-0005 autonomous mode; commit `634bcb6`; 3331 lines, 16 tasks, 8 new ADRs), `PROGRESS.md` (executor-appended through Tasks 1–16 with final SHA-fills at `b416664`; the prior verification-block at `df85f85` records the gate-(e) bounce; the Task 17 entry at `9aa557a` records the state-3 follow-up cleanup and the re-verification of all five non-deferred SPEC §3 gates green at HEAD `65d2574`; the `## Verification (lifecycle-state 5)` block at `2237e6e` records the second mandatory verification-before-completion run at HEAD `2cf3458` with all five non-deferred gates GREEN; the `## Follow-up batch verification (lifecycle-state 3 re-entry from REVIEW)` block at `a6abf63` records the follow-up batch verification at HEAD `3fdc0a5` with all five non-deferred gates GREEN — see "follow-up batch" details below), `REVIEW.md` (drafted by `superpowers:requesting-code-review` per ADR-0006 autonomous-mode adaptation; commit `d69446a`; 240 lines; verdict APPROVED WITH FOLLOW-UPS — 0 Critical, 4 Important, 17 Minor; full text at `docs/envoy-go/phases/05.1-downstream-h2/REVIEW.md`). The sibling `docs/envoy-go/phases/05.2-upstream-h2/` is unchanged from the split commit (placeholder `README.md` only). The parent `docs/envoy-go/phases/05-http-2/` retains its `SPEC.md` (`612cdea`) as the master design document for both sub-phases per ADR-0045 and phase-05 SPEC §11.1; that directory remains closed read-only history at the parent level (no PLAN/PROGRESS/REVIEW will land under it; those land under the sub-phase directories).
- **lifecycle-state:** `5` — promoted from 3 → 4 → 5 by this `superpowers:subagent-driven-development` session. The follow-up batch (state 3 work, REVIEW.md `d69446a` Path A) landed in `.worktrees/phase-05.1-downstream-h2-review-followup/` on branch `phase/05.1-downstream-h2-review-followup` as 7 commits: `4944719` (I-4 — `CONFORMANCE_PINS.md ## Refresh procedure` mirroring `ENVOY_TARGET.md` per D-3.7) → `38ff587` (M-1 — `hpackBlocked` dead field + guard deleted; framer enforces header-block ordering via `golang.org/x/net/http2.Framer.checkFrameOrder`, translated to `*Error` at `framer.go:76-83`) → `851a260` (M-2 — Deprecated `validateClientStreamID` + the 2 dead unit tests deleted; production path inlines the validation in `conn.onHeaders`) → `fd18c0d` (M-15 — ADR-0054 supersedes ADR-0046 per D-3.5; corrects the file-list claim from `framer.go`/`hpack.go`/`settings.go` to `framer.go`/`settings.go`/`conn.go` — the actual root-`http2` importers; `hpack.go` imports `http2/hpack` only) → `3d00501` (M-6 — `errors.Is` for ctx errors in `fuzz_test.go`) → `b825078` (M-13 + M-16 + M-17 — `BEHAVIOR_CONTRACT.md` body-bytes prose tightening + `H2Smoke` smoke-only docstring tag + `connection.go` `runConnection` ADR-0050/SPEC §5.4 cross-ref) → `3fdc0a5` (M-14 — H2 no-match 404 body aligned to phase-04 H1 empty-body convention; `h2dispatch.go:41` `bodyText: "not found\n"` → `bodyText: ""`; `directResponseAction.writeH2` correctly emits HEADERS+empty-DATA-with-END_STREAM at 0 bytes which is RFC 9113 compliant). Each commit went through implementer + spec-compliance-reviewer + code-quality-reviewer subagent gates per `superpowers:subagent-driven-development`; all approved (FU-7 approved-with-notes acknowledging optional future tightening to elide the empty trailing DATA frame — out of scope for M-14). State 3 → 4 → 5 promotion landed in commit `a6abf63` (PROGRESS.md `## Follow-up batch verification` block). Re-verification at HEAD `3fdc0a5` against the follow-up worktree: gate (a) VACUOUS per ADR-0045; gate (b) all four pre-existing differential fixtures green (`go test ./test/differential/...` + auxiliary `go test -count=1 ./...` + `go test -race -count=1 ./...` all packages OK); gate (c) h2spec **53/53 PASS** at the same ADR-0051 pinned digest `summerwind/h2spec@sha256:5f4a65c30cae8569558ced048b4bfe0dcf01a221e36767ae504ccd8348a7aeb0`; gate (d) all six fuzzers PASS at the 30s ADR-0018 budget with `git status --porcelain` empty after each (no testdata/fuzz pollution); gate (e) `go build` / `go vet` / `golangci-lint run ./...` (v1.64.8) all return zero issues, ADR-0046 + ADR-0054 boundary grep returns 3 production hits in the 3 corrected files (`framer.go`/`settings.go`/`conn.go`), `client.go` absence verified, ADR list ends with `ADR-0054`. Gate (f) closed by REVIEW.md `d69446a` (the original APPROVED-WITH-FOLLOW-UPS verdict) PLUS the PROGRESS verification block at `a6abf63` per BOOTSTRAP §5.2 — the executor's PROGRESS verification block is the documented alternative to a formal REVIEW.md addendum, per REVIEW.md §Recommendation Path A wording. The follow-up batch closes 9 of REVIEW's 21 findings (I-4 + M-1 / M-2 / M-6 / M-13 / M-14 / M-15 / M-16 / M-17). The remaining 12 findings (I-1 / I-2 / I-3 + M-3 / M-4 / M-5 / M-7 / M-8 / M-9 / M-10 / M-11 / M-12) carry forward to phase 05.2 per REVIEW.md Recommendation Path A — I-1/I-2/I-3 form a coherent flow-control discipline ADR-shaped unit (likely ADR-0055 at 05.2 PLAN-write time per ADR-0004), with M-3/M-7/M-9 bundling into the same ADR.
- **next-skill:** `superpowers:finishing-a-development-branch` — drives the BOOTSTRAP §5 state-6 transition: write the final phase-done commit per §5.3 format (`phase 05.1: downstream HTTP/2 [ADR-0046, ADR-0047, ADR-0048, ADR-0049, ADR-0050, ADR-0051, ADR-0052, ADR-0053, ADR-0054]`), update `ROADMAP.md` row `05.1` from `in-progress` to `done` (parent row `05` stays `in-progress` until 05.2 lands), and advance STATE.md to point at phase `05.2-upstream-h2` (lifecycle-state 0 — phase exists in ROADMAP but its phase-directory is currently a placeholder, awaiting `superpowers:brainstorming` per BOOTSTRAP §5 state 0/1). Master tip is fast-forwarded to the phase-done commit per ADR-0003 at the close of state 6.
- **next-skill-scope:** Land the state-6 phase-done commit per BOOTSTRAP §5 / §5.3. Required artefacts: (1) ROADMAP.md row `05.1-downstream-h2` status flips `in-progress` → `done`; row `05-http-2` parent stays `in-progress` until 05.2 closes; row `05.2-upstream-h2` stays `planned`. (2) STATE.md fields advance: `active-phase: 05.2-upstream-h2`; `phase-directory: docs/envoy-go/phases/05.2-upstream-h2/` (currently placeholder `README.md` only — the brainstorming step creates `SPEC.md`); `lifecycle-state: 0`; `next-skill: superpowers:brainstorming` (state 0 → 1 transition); `next-skill-scope` enumerates the 05.1 carry-forwards that 05.2 inherits as SPEC inputs (I-1 / I-2 / I-3 flow-control discipline as a coherent ADR; M-3 / M-7 / M-9 bundle into same ADR; M-4 / M-5 / M-8 / M-10 / M-11 / M-12 dispositions per REVIEW.md). (3) Phase-done commit message follows §5.3 format verbatim: subject `phase 05.1: downstream HTTP/2 [ADR-0046, ADR-0047, ADR-0048, ADR-0049, ADR-0050, ADR-0051, ADR-0052, ADR-0053, ADR-0054]`; body 1–3 sentences naming the dataplane delivered (from-scratch H2 server codec sub-package + ALPN dispatcher + `directResponseAction` codec-neutral factoring + `--allow-h2c` test-only CLI flag + first non-vacuous conformance gate); `Differential surface:` line acknowledging gate (a) was vacuous in 05.1 with the routed-to-upstream H2 differential surface deferred to 05.2 + fixture 0004; `Conformance:` line names h2spec 53/53 PASS at the ADR-0051 pin. (4) Per ADR-0003, the state-6 commit lands on a fresh worktree branched from this STATE.md commit (e.g. `phase/05.1-downstream-h2-finish`), then master fast-forwards. The follow-up worktree at `.worktrees/phase-05.1-downstream-h2-review-followup/` becomes closed-history at the state-6 transition. (5) The discovered-during-batch coverage gap (no integration test for the monotonic-id-reuse rejection branch at `internal/filter/hcm/h2/conn.go:308-319`; only the even-id branch is covered by `TestServerConn_GOAWAYOnProtocolError_EvenStreamID`) is recorded in the PROGRESS follow-up block and is appropriate to either land as a small additional follow-up commit before state-6 OR carry forward into 05.2 — the state-6 session decides. The optional FU-7 future-tightening (elide the empty trailing DATA frame when `bodyText == ""`) is similarly out-of-scope for the state-6 commit.
- **last-commit:** `a6abf63` — the PROGRESS.md `## Follow-up batch verification (lifecycle-state 3 re-entry from REVIEW)` block on branch `phase/05.1-downstream-h2-review-followup`, +441 lines, ONLY-PROGRESS.md change. This is the 8th commit on `phase/05.1-downstream-h2-review-followup` (after `4944719`, `38ff587`, `851a260`, `fd18c0d`, `3d00501`, `b825078`, `3fdc0a5`). This STATE.md commit will be the 9th. Master is fast-forwarded to this STATE.md commit per ADR-0003.
- **last-updated:** 2026-04-26

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
