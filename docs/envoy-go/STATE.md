# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `05.1-downstream-h2`
- **phase-directory:** `docs/envoy-go/phases/05.1-downstream-h2/` — contains `README.md` (placeholder, written at split time per ADR-0045 / commit `2162509`), `SPEC.md` (drafted by brainstorming per ADR-0004 autonomous mode; commit `4b45941`), `PLAN.md` (drafted by writing-plans per ADR-0005 autonomous mode; commit `634bcb6`; 3331 lines, 16 tasks, 8 new ADRs), and `PROGRESS.md` (executor-appended through Tasks 1–16 with final SHA-fills at `b416664`; the prior verification-block at `df85f85` records the gate-(e) bounce; the Task 17 entry at `9aa557a` records the state-3 follow-up cleanup and the re-verification of all five non-deferred SPEC §3 gates green at HEAD `65d2574`). The sibling `docs/envoy-go/phases/05.2-upstream-h2/` is unchanged from the split commit (placeholder `README.md` only). The parent `docs/envoy-go/phases/05-http-2/` retains its `SPEC.md` (`612cdea`) as the master design document for both sub-phases per ADR-0045 and phase-05 SPEC §11.1; that directory remains closed read-only history at the parent level (no PLAN/PROGRESS/REVIEW will land under it; those land under the sub-phase directories).
- **lifecycle-state:** `4` — promoted back from 3 by the Task 17 state-3 follow-up session: gate-(e) lint cleanup landed in two cleanup commits on `phase/05.1-downstream-h2-impl-followup` (`9e23e77` mechanical sweep — 36 of 38 issues; `65d2574` unused-symbol triage — 2 of 38 issues, both deletions after code-reading confirmed neither `hpackBlockSID` nor `func ctxErr` is 05.2 forward-look). Five of six SPEC §3 gates re-verified GREEN at HEAD `65d2574` from `.worktrees/phase-05.1-downstream-h2-impl-followup` (branched from `df85f85` per ADR-0003): (a) vacuously green per ADR-0045; (b) `0000-tcp-echo` / `0001-tcp-proxy-rr` / `0002-tls-tcp` / `0003-http11-routing` all PASS, plus `go test -race ./...` clean; (c) h2spec 53/53 PASS at the ADR-0051 pin (sections 3.5 / 4.1 / 4.2 / 4.3 / 5.1 / 5.1.1 / 5.1.2 / 5.3.1 / 5.4.1 / 5.5 / 7 / 8.1 / 8.1.2 / 8.1.2.1 / 8.1.2.2 / 8.1.2.3 / 8.1.2.6 / 8.2); (d) all six fuzzers PASS at 30s budget with `git status --porcelain` empty after each — `FuzzBootstrapLoad` 786,306 execs, `FuzzTcpProxyFilter` 3,939,064 execs, `FuzzTLSContextParse` 5,774,736 execs, `FuzzHCMConfigParse` 3,146,466 execs, `FuzzFrameStream` 14,378,504 execs, `FuzzHPACKDecode` 2,292,547 execs; (e) `go build` / `go vet` / `go test ./...` clean, ADR-0046 boundary grep clean (3 prod hits in the 5 allowed files), ADR-0048 `client.go` absence verified, **`golangci-lint run ./...` exits 0 with zero issues** (down from 38 at `df85f85`). Gate (f) — `REVIEW.md` — remains state-5 work (`superpowers:requesting-code-review`), not run by Task 17. The Task 17 PROGRESS entry quotes all command outputs verbatim and includes a "Carry-forward observation" subsection noting that the `hpackBlocked bool` paired with the now-deleted `hpackBlockSID` is also dead code (never set to `true` anywhere; the read at `conn.go:236` always observes `false`; the actual CONTINUATION ordering enforcement is owned by `golang.org/x/net/http2.Framer.checkFrameOrder` per the deletion rationale). That observation is OUT OF SCOPE for Task 17 (the 38-issue gate-(e) cleanup is bounded scope per BOOTSTRAP §6.3); a future small commit may remove `hpackBlocked` and the `conn.go:227-250` guard block. The recommended venue for raising it is the upcoming gate-(f) `REVIEW.md`.
- **next-skill:** `superpowers:verification-before-completion` (state-3→state-4 promotion was Task 17; the second mandatory verification-before-completion session re-runs the full SPEC §3 gate sweep at the new HEAD `65d2574` per BOOTSTRAP §1 step E and §5 state-4). The verification session should branch a fresh `phase/05.1-downstream-h2-verify-2` (or similar) worktree from this STATE.md commit per ADR-0003; the impl-followup worktree at `.worktrees/phase-05.1-downstream-h2-impl-followup` is closed-history at this state transition.
- **next-skill-scope:** Re-run every SPEC §3 phase-done gate (a)–(e) from a fresh terminal in the new verify worktree, capturing each command's verbatim output into a fresh `## Verification (lifecycle-state 5)` block in `phases/05.1-downstream-h2/PROGRESS.md`. Expected: gates (a) vacuous per ADR-0045; (b) four pre-existing differential fixtures all PASS plus `go test -race ./...` clean; (c) h2spec 53/53 PASS at the ADR-0051 pin; (d) six fuzzers PASS at 30s budget per ADR-0018 with `git status --porcelain` empty after each (no testdata/fuzz pollution); (e) `go build` / `go vet` / `go test ./...` clean, ADR-0046 boundary grep clean, ADR-0048 `client.go` absence verified, `golangci-lint run ./...` exits 0 with zero issues. If all five non-deferred gates remain GREEN, the verification session promotes `lifecycle-state` to 5 and the next session invokes `superpowers:requesting-code-review` for gate (f). If any gate regresses (most likely concern: lint=0 invariant under tooling drift, OR a fuzzer turning up a new crasher in the 30s budget that did not appear in Task 17's run, OR a differential fixture flake), the verification session bounces back to lifecycle-state 3 with a fresh `block-reason` line and a fix-up plan in PROGRESS.md — same discipline as `df85f85`'s bounce. Do NOT introduce `//nolint` allow-list paper to mask any regression. Reading order for the verification session: this STATE.md, `phases/05.1-downstream-h2/PROGRESS.md` Task 17 entry (the canonical record of the cleanup), `phases/05.1-downstream-h2/SPEC.md` §3 (phase-done gates), `BOOTSTRAP_PROMPT.md` §1 step E + §5 state-4.
- **last-commit:** `9aa557a` — the Task 17 PROGRESS verification-block append on branch `phase/05.1-downstream-h2-impl-followup`, on top of the cleanup commits `9e23e77` (mechanical sweep) and `65d2574` (unused-symbol triage). This STATE.md commit (the lifecycle-state 3→4 promotion commit on the same branch) is the 4th commit on `phase/05.1-downstream-h2-impl-followup`. Master is fast-forwarded to this STATE.md commit per ADR-0003.
- **last-updated:** 2026-04-26

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
