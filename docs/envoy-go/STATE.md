# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `07.1-http-filter-framework`
- **phase-directory:** `docs/envoy-go/phases/07.1-http-filter-framework/` — now contains `SPEC.md` (965 lines, master `f2dd659` — the spec-reviewer-fixes follow-up HEAD; original SPEC commit at `ee45aba`) AND `PLAN.md` (3062 lines, this commit) — the task-by-task implementation plan covering 23 tasks across the iteration-protocol framework + extension registry + per-route 3-tier merge + chain state machine + cors filter + envoy_go_test probe filter + router-as-terminal-filter migration + HCM dispatch wiring (H1 + H2) + boot wiring + two new fixtures (0007a-cors differential, 0007b-iteration-probe structural) + closing six-gate sweep + BEHAVIOR_CONTRACT in-place edit. Sub-phase 07.2 directory carries an unchanged `README.md` stub. Parent `docs/envoy-go/phases/07-filter-chain-framework/` carries `BRAINSTORM.md` (688 lines, `da28039`) + parent master `SPEC.md` (113 lines, `ee45aba`) — both read-only history once drafted. Phase 06 (parent + sub-phases 06.1 + 06.2) remains closed read-only history.
- **lifecycle-state:** `3` — PLAN drafted + reviewer-approved (`Approved` with three advisory recommendations addressed in-place: (a) Task 5 → Task 7 transient compile divergence acknowledged + framed as deliberate; (b) Task 13 step 4 added explicitly to extend `FuzzFilterChainParse` to chain-shape adversarial inputs (closes Task 10's forward-reference); (c) Task 12 buildability gap codified in Refinement with two execution patterns documented). The plan-size gate (`BOOTSTRAP_PROMPT.md` §6.1) was evaluated: 23 tasks (under 25-task primary signal); ~5500 LoC effective (above the 1500-LoC OR-leg, but within the phase-04/05.1/05.2/06.1/06.2 precedent for one-sub-phase shipment when surface is structurally atomic). The Scope check argues against 07.1.1/07.1.2 split because the surface-axis split would create vacuous gate (a) on 07.1.1 (framework-only without filters or fixtures), exactly the BOOTSTRAP §6.3 anti-pattern. Mid-execution split valve: if cumulative LoC > 9000 by end of Task 20, invoke `superpowers:systematic-debugging`. Per BOOTSTRAP §5 + ADR-0005 the next transition is state 3 → 4 via `superpowers:subagent-driven-development` (PLAN execution; user's persistent preference per MEMORY.md is subagent-driven over inline-execution). All seven anticipated ADRs (ADR-0070..ADR-0076) have summaries with topic-to-task mappings in `## ADRs introduced by this plan` (commit-time ordering: T1→ADR-0070, T2→ADR-0071, T3→ADR-0072, T4→ADR-0073, T7→ADR-0075, T9→ADR-0076, T18→ADR-0074 — non-monotonic at the last entry, permitted per SPEC §8 + 05.2/06.1/06.2 precedents). All eight SPEC §12 deferred decisions are settled in `## Settled SPEC §12 deferred decisions`.
- **next-skill:** `superpowers:subagent-driven-development` — PLAN execution for phase 07.1 (state 3 → 4 per `BOOTSTRAP_PROMPT.md` §5 step 3 + ADR-0005's default executor preference + the user's persistent preference recorded in MEMORY.md). Inputs: this commit's `docs/envoy-go/phases/07.1-http-filter-framework/PLAN.md` (the authoritative task-by-task source; the PLAN's `## Execution preconditions` block is the executor's first-task gate at Task 1 step 1; the PLAN's `## ADRs introduced by this plan` topical-to-task mapping is the executor's authoring-order reference for ADR-0070..ADR-0076), `docs/envoy-go/phases/07.1-http-filter-framework/SPEC.md` (the design source the PLAN traces to), `docs/envoy-go/DECISIONS.md` (for ADR numbering verification — Task 1 step 1 confirms tail at ADR-0069), `docs/envoy-go/BEHAVIOR_CONTRACT.md` (the in-place-edit target at Task 23), `BOOTSTRAP_PROMPT.md` §5 + §7.5 (the six-gate phase-done definition Task 23 sweeps), `SKILL_ROUTING.md`, the closed-history 06.1 + 06.2 PLANs/PROGRESS.md as execution-shape precedent. The execution session creates worktree `.worktrees/phase-07.1-http-filter-framework-impl` on branch `phase/07.1-http-filter-framework-impl` per the PLAN's `## Execution preconditions` precondition #1 + ADR-0003.
- **next-skill-scope:** Lifecycle-state 3 → 4 PLAN execution session deliverable: production-code commits implementing PLAN.md's 23 tasks with TDD discipline per `superpowers:test-driven-development`; per-task SHA-fill follow-up commits per the phase-02..06.2 convention (Refinement codifies); PROGRESS.md append-only log quoting command outputs verbatim per SPEC §15 acceptance bullet 18 + the phase-04..06.2 PROGRESS-style precedents; BEHAVIOR_CONTRACT.md `## HTTP filter chain` in-place edit + amendments to `## HTTP/1.1` + `## HTTP/2` + Equivalence Matrix row at Task 23 (per ADR-0052; Refinement codifies the at-impl-session-last-commit timing); seven ADRs ADR-0070..ADR-0076 land at first-use tasks (T1, T2, T3, T4, T7, T9, T18) per the `## ADRs introduced by this plan` topical-to-task mapping. After Task 23 commits + SHA-fill, the verification session (state 4 → 5) re-runs all six gates per BOOTSTRAP §7.5 and advances STATE to lifecycle-state 5 with `next-skill: superpowers:requesting-code-review`. The REVIEW session (state 5 → 6) writes REVIEW.md + the phase-done commit that flips ROADMAP row 07.1 → done (parent row 07 STAYS in-progress per the 05/05.1/05.2 + 06/06.1/06.2 closure pattern; the parent only flips to done at 07.2's phase-done) AND advances STATE to phase 07.2 (`active-phase: 07.2-listener-chain-completion`; `lifecycle-state: 1`; `next-skill: superpowers:brainstorming`) AT THE SAME COMMIT. The L4 review-followup batch closing 11 Minors from phase 06.2 REVIEW.md remains a separate post-phase-done branch — NOT 07.1's scope; can run in parallel.
- **last-commit:** `TBD` — `phase 07.1: PLAN.md drafted + plan-document-reviewer approved (state 2 → 3)`. Lands: `docs/envoy-go/phases/07.1-http-filter-framework/PLAN.md` (3062 lines; full 23-task plan with TDD-step granularity, 7-ADR topical-to-task mapping, 8-deferred-decision settlement, scope-check argument for one-sub-phase shipment, Refinement section codifying SHA-fill / BEHAVIOR_CONTRACT / ROADMAP discipline, Post-plan handoff section codifying the verification + REVIEW session contracts, Plan review loop section codifying the reviewer scope) + this STATE.md advance (lifecycle-state 2 → 3). No code changes in this commit; pure plan-document + state transition. The SHA-fill follow-up `phase 07.1: STATE.md SHA-fill for PLAN commit` lands the actual SHA per the phase-02..06.2 SHA-fill precedent.
- **last-updated:** 2026-04-30

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
