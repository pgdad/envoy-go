# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `phase 38 (load-balancer-subset) BRAINSTORM done` (2026-06-13, this commit) — the phase-38 BRAINSTORM is COMPLETE (`superpowers:brainstorming`; `docs/envoy-go/phases/38-load-balancer-subset/BRAINSTORM.md` — the Q0/Q1/Q2 decisions; **ROADMAP row 38 registers `in-progress`** [a flat family row WITH a pre-authorized 38.1/38.2 by-plane split — NO parent rollup per ADR-0106]). Charters the project's **FIFTH LB construct** and the family's **FIRST wrapper**: `Cluster.lb_subset_config` (`envoy.config.cluster.v3`) — **NOT a `Cluster.LbPolicy` enum value** (unlike the five incumbents roundRobin/leastRequest/randomLB/ringHashLB/maglevLB) but a wrapper triggered by the presence of `lb_subset_config`, that selects a subset of endpoints matching the route's `metadata_match` and **delegates to the cluster's `lb_policy` as the child policy run within that subset**. Picked from the 4-candidate roster (Q0) as **the only candidate decoupled from the absent health-checking boundary** (the other three — locality-weighted / priority / panic — are health-gated and belong after the Upstream-robustness family). As chartered: a `subsetLB` wrapper (`internal/cluster/subset.go` — enumerate subsets per selector × distinct endpoint-metadata value-tuples; a child `loadBalancer` per subset built by the existing policy factory — **ALL FIVE child policies supported** [Q1=A], the hashKey passing through to a hash-policy child; resolve the route match → subset → delegate `child.Pick`; the cluster-level fallback {NO_FALLBACK/ANY_ENDPOINT/DEFAULT_SUBSET} + `default_subset`) over a NEW `Endpoint.Metadata` dimension (the `envoy.lb` namespace captured at `extractEndpoints`, `manager.go:533` — the FIRST per-endpoint dimension beyond `Host:Port`) + a SECOND LB pick-seam input (the resolved subset match widening `Pick(hashKey, hasHash)` → `Pick(hashKey, hasHash, match, hasMatch)` + `WithSubsetMatch`/`subsetMatchFrom`, the ADR-0235 hash-key STYLE TEMPLATE) + a NEW HTTP producer plane (`RouteAction.metadata_match["envoy.lb"]` attached to `clusterRouteAction` — `actions.go:201`). The manager wraps-after-switch (subset wraps the `lb_policy` child — NOT a new `case`). **NO lb_policy reject-text change** (subset is not an enum value — the phase-34/35/36/37 doubly-hit reject-text-retarget lineage BREAKS; the reject surface shifts to `lb_subset_config` validation, D-SS1). **Pre-authorized 38.1/38.2 by-plane split (Q2/Q2b — the phase-36 valve):** 38.1 = subset LB on the HTTP `RouteAction.metadata_match` plane (this leg); 38.2 = `RouteAction.weighted_clusters` routing + `WeightedCluster.ClusterWeight.metadata_match` (a NEW per-request weighted-cluster-selection primitive — the router resolves a SINGLE cluster per route today; the brainstorm surfaced this hidden prerequisite). Differential `0064-lb-subset` (HTTP routes each carrying a `metadata_match` pinned to a subset over `envoy.lb`-tagged endpoints): subset affinity (every served backend ∈ the expected subset, BOTH sides — the SET-membership property; the `metadata_match` is STATIC config → NAT-transparent → TRUE cross-side) + a fallback arm + cross-side byte-equiv + a cross-side `StatsAsserter` (the `lb_subsets_*` stats). 2 ADRs anticipated: **ADR-0239** (the subset-match seam-input extension + the `RouteAction.metadata_match` producer — the seam-BUILD half, parallel to ADR-0235) + **ADR-0240** (the subset LB policy + the `Endpoint.Metadata` dimension — the policy half, parallel to ADR-0236/0238); the phase-36 two-ADR shape (contrast phases 35/37's single-ADR reuse); §Context at SPEC, bodies at IMPL per ADR-0044. Anticipated moves at the 38.1 IMPL: stat surface **1121 → ~1125** (~+4 `lb_subsets_*` stats — D-SS4 pins), fixtures **65 → 66** (`0064`; NO boot-reject dir anticipated — the parse rejects land unit-level), fuzzers UNCHANGED **42** (no wire decode — a subset-resolution property test is unit-level), BackendKind tail UNCHANGED **33** (`0064` reuses the `0063` HTTP backends), ZERO new packages + ZERO new go.mod deps. **The Load-balancing family stays OPEN** — **3** candidates remain after 38 {locality-weighted LB, priority load balancing, panic thresholds} (all health-gated). ALL counts UNCHANGED at this BRAINSTORM commit.
- **phase-directory:** `docs/envoy-go/phases/38-load-balancer-subset/` (**BRAINSTORM.md + README.md** — the durable phase-38 BRAINSTORM charter [the Q0/Q1/Q2 decisions; the v1.32.4 `Cluster.LbSubsetConfig` + `LbEndpoint.metadata` [field 3] + `RouteAction.metadata_match` [field 4] + `core.Metadata.filter_metadata["envoy.lb"]` verification §2.1; the wrapper-not-an-enum-value architecture §1.7/§2.2/§3.1; the 38.1/38.2 by-plane split §1.4/§2.3; the NO-reject-text-change twist §2.6; the static-config NAT-transparent cross-side subset-affinity differential design §2.5/§6; the 2 anticipated ADRs §7; the §10 D-SS1..D-SS7 empirical pins the SPEC executes] + the phase-overview README [status 0 → 1]; the SPEC.md follows at the SPEC session, then the 38.1 PLAN.md + PROGRESS.md, per the per-sub-phase-dir precedent [36.1/36.2]). **Superseded (preserved):** `docs/envoy-go/phases/37-load-balancer-maglev/` (**BRAINSTORM.md + SPEC.md + PLAN.md + PROGRESS.md + README.md** — the durable phase-37 IMPL record [the maglev policy; ADR-0238]). The phase-36 dirs `docs/envoy-go/phases/36-load-balancer-ring-hash/` + `36.2-load-balancer-ring-hash-http/` (the durable phase-36 parent + 36.2 sub-phase record). The phase-35 directory `docs/envoy-go/phases/35-load-balancer-random/` + the phase-34 directory `docs/envoy-go/phases/34-load-balancer-least-request/` (the durable phase-35/34 records). The phase-31/32/33 network-filter directories + the phase-29/30 directories stay the durable prior-phase record.
- **lifecycle-state:** `phase 38 BRAINSTORM done — the Load-balancing family STAYS OPEN; subset LB chartered at phase 38 (3 candidates remain after 38: locality-weighted LB, priority load balancing, panic thresholds — all health-gated). Next: the phase-38 SPEC (superpowers:writing-plans scoped to SPEC authoring — execute the §10 D-SS1..D-SS7 empirical pins live against envoyproxy/envoy:contrib-v1.37.2 per reference_docker_probe_bridge_network; anchor the ADR-0239 + ADR-0240 §Context drafts; the FINAL 38.1/38.2 ADR-0045 split decision)`. The phase-38 BRAINSTORM is COMPLETE: the durable record is `docs/envoy-go/phases/38-load-balancer-subset/BRAINSTORM.md` (the Q0/Q1/Q2 + differential-proof decisions + the §10 D-SS1..D-SS7 pins) + README.md.
- **next-skill:** `superpowers:writing-plans` (scoped to SPEC authoring — the kafka-31/thrift-33/least_request-34/random-35/ring_hash-36/maglev-37 precedent): author `docs/envoy-go/phases/38-load-balancer-subset/SPEC.md`, executing the §10 D-SS1..D-SS7 empirical pins IN-SESSION against the contrib reference Envoy per ADR-0004/ADR-0227 (`reference_docker_probe_bridge_network`), anchoring the ADR-0239 + ADR-0240 §Context drafts, and confirming the 38.1/38.2 ADR-0045 split disposition. Then the 38.1 PLAN + IMPL, then 38.2.
- **last-commit:** the phase-38 BRAINSTORM-done bundle — `docs/envoy-go/phases/38-load-balancer-subset/BRAINSTORM.md` + README.md (NEW) + STATE/ROADMAP row 38 `in-progress` registration (a flat family row WITH a pre-authorized 38.1/38.2 by-plane split; the Load-balancing family heading gains the `subset IN-PROGRESS` note) + the next-prompt.txt roll-forward to the phase-38 SPEC. ALL counts UNCHANGED (a BRAINSTORM commit — stat surface 1121 / fixtures 65 / fuzzers 42 / BackendKind tail 33 / DECISIONS tail ADR-0238, next-free ADR-0239).
- **last-updated:** 2026-06-13 (phase-38 BRAINSTORM done)
- **next-free ADR:** `ADR-0239` (DECISIONS.md tail STAYS **ADR-0238** at this BRAINSTORM; phase 38 anticipates ADR-0239 [the subset-match seam-input + the `RouteAction.metadata_match` producer] + ADR-0240 [the subset LB policy + the `Endpoint.Metadata` dimension] — §Context drafts at the SPEC, bodies at the 38.1 IMPL per ADR-0044; next-free after phase 38 ≈ ADR-0241).

---

## Project counts at this commit

- **active differential fixtures:** **65** (tail `0063-lb-maglev`). Canonical recipe `ls -d test/fixtures/[0-9]* | wc -l` = 65. UNCHANGED at the phase-38 BRAINSTORM (a docs-only commit). Anticipated 65 → 66 at the 38.1 IMPL (`0064-lb-subset` — the static-config NAT-transparent cross-side subset-affinity differential; NO boot-reject dir anticipated — the `lb_subset_config` parse rejects land unit-level).
- **fuzzers:** **42** (canonical recipe `grep -rh "^func Fuzz" $(find ./internal -name fuzz_test.go) | wc -l`). UNCHANGED at the phase-38 BRAINSTORM. Anticipated UNCHANGED at the 38.1 IMPL (+0 — subset decodes no wire bytes; the subset-resolution property test is unit-level, FOLDED into `subset_test.go`).
- **stat surface:** **1121** (BEHAVIOR_CONTRACT doc count). UNCHANGED at the phase-38 BRAINSTORM. Anticipated 1121 → ~1125 at the 38.1 IMPL (~+4 `lb_subsets_*` stats — `lb_subsets_active` gauge + `lb_subsets_created`/`lb_subsets_selected`/`lb_subsets_fallback` counters; D-SS4 pins the exact set + count + types).
- **BackendKind tail:** **33** (`TCPThriftResponder` — seq_id-agnostic canned framed-binary REPLY). UNCHANGED at the phase-38 BRAINSTORM. Anticipated UNCHANGED at the 38.1 IMPL (+0 — `0064` reuses the `0063`/`0062` HTTP backends; subset adds an endpoint METADATA dimension carried in the bootstrap config, not a new backend).
- **DECISIONS.md tail:** **ADR-0238** (next-free **ADR-0239**) — **ADR-0238** (the `maglev` load-balancing policy) landed at the phase-37 IMPL. UNCHANGED at the phase-38 BRAINSTORM. Phase 38 anticipates ADR-0239 (the subset-match seam-input extension + the `RouteAction.metadata_match` producer) + ADR-0240 (the subset LB policy + the `Endpoint.Metadata` dimension) — both land at the 38.1 IMPL per ADR-0044 (DECISIONS tail → ADR-0240; next-free ADR-0241).
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227, superseding ADR-0008).
- **conformance:** h2spec 53/53; proxy-wasm all 10 families PASS — UNCHANGED at the phase-38 BRAINSTORM (a docs-only commit). At the 38.1 IMPL: asserted-unaffected by change-scope construction (subset touches `internal/cluster/*` + the HTTP route `metadata_match` parse + the `0064` fixture/docs — NO HTTP/2 or proxy-wasm path; the wire path is byte-identical when no `lb_subset_config` cluster is configured; the full 66-dir differential is the real guard).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
