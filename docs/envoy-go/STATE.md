# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `06.1-stats-prometheus`
- **phase-directory:** `docs/envoy-go/phases/06-observability-baseline/` — created by this brainstorm commit; contains `BRAINSTORM.md` (this commit). Sub-phase directories `docs/envoy-go/phases/06.1-stats-prometheus/` and `docs/envoy-go/phases/06.2-access-log/` will be created by the next-session SPEC drafting. Parent directory holds the eventual master `SPEC.md` summarizing both sub-phases (mirrors `docs/envoy-go/phases/05-http-2/`). Phase 05 (parent + sub-phases 05.1 + 05.2) remains closed read-only history.
- **lifecycle-state:** `1` — phase 06 brainstorm session complete; planner-time-split decision per ADR-0045 lands phase 06 → 06.1 (`stats-prometheus`) + 06.2 (`access-log`). 06.1 is the immediate next focus; 06.2 receives a sibling SPEC stub at the same SPEC-drafting session. Brainstorm captured in `docs/envoy-go/phases/06-observability-baseline/BRAINSTORM.md` covering: stats backend architecture (thin internal `internal/stats` package + Prometheus adapter; no third-party Prometheus dep) → 17 stat-name catalog (counters + gauges; histograms ADR-deferred) → carry-forward dispositions (M-9 bundled; M-4/M-10/M-12 deferred to dedicated H2-hardening sub-phase) → admin endpoint coverage (`/stats/prometheus` only; phase 08 owns rest of admin API) → differential equivalence claim (behavioral-delta assertion per fixture `0005-prometheus-stats`; no byte-exact whole-output) → 6 anticipated ADRs. Per BOOTSTRAP §5 the next transition is state 1 → 2 via `superpowers:writing-plans` (SPEC.md authoring for both phase-06 parent + 06.1 sub-phase + 06.2 stub). Brainstorm-review session surfaced two empirical-verification obligations (BRAINSTORM §2.3.1: exact `envoy_response_code_class` label form + `_Nxx` name-collapse semantics must be pinned at SPEC time against reference-Envoy v1.37.2 scrape; this is unresolvable from documentation alone) and one scope-warning (BRAINSTORM §3 note 6: existing `cluster.NewManager`/`admin.New` signatures change — call-site updates must be itemized in SPEC's "Files touched").
- **next-skill:** `superpowers:writing-plans` — SPEC.md authoring for phase 06.1. Inputs: this commit's `BRAINSTORM.md` (authoritative design source), `BOOTSTRAP_PROMPT.md` §§5-6 (lifecycle + planner-time split contract), `SKILL_ROUTING.md`, the closed-history `docs/envoy-go/phases/05.1-downstream-h2/SPEC.md` and `docs/envoy-go/phases/05.2-upstream-h2/SPEC.md` as structural precedent (header layout, §-numbering conventions, acceptance-bullet shape), `docs/envoy-go/phases/05-http-2/SPEC.md` as parent-master-SPEC precedent, `docs/envoy-go/BEHAVIOR_CONTRACT.md` (the `## Stat-name mapping` and `## Access log field mapping` empty placeholders to be filled by 06.1 / 06.2 respectively), `docs/envoy-go/ENVOY_TARGET.md` (Envoy v1.37.2 pin — required for empirical verification per BRAINSTORM §2.3.1).
- **next-skill-scope:** Lifecycle-state 1 → 2 SPEC drafting session deliverables: (1) `docs/envoy-go/phases/06-observability-baseline/SPEC.md` — parent master SPEC summarizing both 06.1 + 06.2 scopes (mirrors `docs/envoy-go/phases/05-http-2/SPEC.md`); (2) `docs/envoy-go/phases/06.1-stats-prometheus/SPEC.md` — full sub-phase SPEC derived from BRAINSTORM §§2-9; **must empirically pin Rule SN4 per BRAINSTORM §2.3.1** by running reference Envoy v1.37.2 and copying the `/stats/prometheus` output verbatim for `cluster.<n>.upstream_rq_<class>` stats; (3) `docs/envoy-go/phases/06.2-access-log/README.md` — sibling SPEC stub citing parent SPEC + forward-looking 06.2 scope notes; (4) ROADMAP.md split: row `06 | observability-baseline | 05 | planned` becomes parent `06 | observability-baseline | 05 | in-progress | 06.1, 06.2 | ...` with rows `06.1 | stats-prometheus | 05 | planned | | ...` and `06.2 | access-log | 06.1 | planned | | ...` (06.2 depends on 06.1 because the access-log filter wants to emit metrics about its own buffer pressure — optional but natural). After SPEC, advance STATE to lifecycle-state 2 with `next-skill: superpowers:writing-plans` (PLAN.md authoring) and `active-phase` remains `06.1-stats-prometheus`. Worktree for the next session: branch from this brainstorm commit's HEAD per ADR-0003 + per-phase-worktree convention (`.worktrees/phase-06.1-stats-prometheus-spec` or similar).
- **last-commit:** `d271fd7` — `phase 06: brainstorm — observability-baseline scoped + split into 06.1 (stats-prometheus) + 06.2 (access-log) per ADR-0045`, on branch `phase/06-observability-baseline-brainstorm`. Lands `docs/envoy-go/phases/06-observability-baseline/BRAINSTORM.md` capturing the full design for 06.1 plus a forward-looking 06.2 stub, and updates this STATE.md to lifecycle-state 1. No code changes. SHA-fill follow-up per phase-02..05.2 convention.
- **last-updated:** 2026-04-27

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
