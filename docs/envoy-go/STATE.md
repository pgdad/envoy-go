# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

> **This file is a POINTER, not an archive** — `BOOTSTRAP_PROMPT.md` §4: *"STATE.md — pointer: active phase, last commit, next action."* It carries the live pointer, the live counts, and a SHORT recent lineage. **Full stage-by-stage history lives in `STATE_HISTORY.md`** (archived at ADR-0288; **nothing was deleted** — the pre-move file is preserved there verbatim, byte-for-byte).
>
> **⚠️ THE RULE ADR-0288 EXISTS TO ENFORCE: there is EXACTLY ONE live `next-skill:`, `lifecycle-state:`, `next-free ADR:`, and count block IN THIS FILE, and they live in §Current pointer directly below.** Before that move, ~330 lines of prepended backlog had buried TWO fossilised "current state" blocks whose `next-skill:` still read *"the 42.2 hedging BRAINSTORM"* and *"the 46.2 SPEC"* while the live phase was **66** — so `grep 'next-skill:' STATE.md` returned **two stale hits and ZERO live ones**, and `grep 'fuzzers:'` returned **48** and **42** when the true count was **55**. **A session may trust a grep of this file again. Keep it that way: when you advance the pointer, EDIT §Current pointer IN PLACE — never prepend a new block above it.**

---

## Current pointer

*(The ONLY live "what next" in this repo. Update IN PLACE at every stage close.)*

- **active-phase:** `phase 95 (tls-alpn-mismatch-fallback) IMPL done` (2026-09-07) — lifecycle-state **3 -> DONE**. **PHASE 95 CLOSED; ROW 95 FLIPPED `in-progress` -> `done`** at `ROADMAP.md:157` — a **FLIP, not an ADD**, so `want` STAYS **127** and `ROADMAP.md` STAYS **245** lines. Row field-count **NF=8 under BOTH the naive and the escape-aware form, COUNTED BEFORE AND AFTER INSTALLING** (the appended summary carries **no pipe character at all** — the pipe was REWORDED AWAY, never escaped; row 95 still holds exactly **7** `|` chars, the delimiters only; escape-aware malformed rows STAY IDs **57** and **69** only, naive **17** / escape-aware **2**). **THE FALLBACK LANDED AT THE ENTRY OF `NewDownstreamConfig`** (`internal/tls/config.go` 582 -> **642**): a TCP-only `GetConfigForClient` returning a PER-HANDSHAKE `Clone()` with `NextProtos = nil` when the client's ALPN offer is non-empty and overlaps none of the chain's `alpn_protocols`, so a mismatching client now **COMPLETES the handshake with no protocol selected and is SERVED** — matching the pinned reference — instead of aborting with `no_application_protocol`. **`NewQUICDownstreamConfig` is BYTE-UNTOUCHED**; QUIC's RFC 9001 §8.1 rejection is PARITY, not a departure. ⚠️ **THE ENTRY ANCHOR IS LOAD-BEARING, NOT STYLISTIC — THE SPEC's FORECAST AUTHENTICATION BYPASS WAS REPRODUCED BY EXECUTION AND IS WORSE THAN STATED: the bypassed client is not merely admitted, it is SERVED.** NC 7 drove a client presenting **NO client certificate** through a `require_client_certificate: true` chain to a completed handshake **AND a full application round trip**, at BOTH TLS versions (`hsCompleted=true served=true certSent=false`, `no_certificate=1`/`handshake=1`/`connection_error=0`); the install anchor carries `NoClientCert`/`nil` while the handshake-time clone carries `RequireAndVerifyClientCert`/non-nil. ⚠️ **A handshake-completion probe answers the WRONG QUESTION** — the TLS1.3 REJECTED arm also reads `hsCompleted=true` — which is why the bypass needed a write-and-read probe. **18 tasks, THREE parallel streams on private worktrees, plus an unplanned Task 9b, and a DELIBERATE Task 17/18 REORDER**: the gates ran BEFORE the close, because the row-93 and row-94 precedents both put the six-gate figures INSIDE the row summary and the PLAN's literal order would have forced a FORECAST. **EIGHTEEN CLAIMS REFUTED BY EXECUTION** (BRAINSTORM 9, SPEC 11, PLAN 17, IMPL **EIGHTEEN**), plus a NINETEENTH found at the close. The load-bearing ones: **(1) PLAN Task 1 Step 2's THIRD PIN, AS LITERALLY WORDED, IS SATISFIABLE BY DOING NOTHING** — `ClientAuth`/`ClientCAs` are phase-67 outputs, green at the tip, green after the fix, **AND GREEN UNDER THE BYPASS BUG** (which corrupts the ALT and never the base); only an added `GetConfigForClient != nil` discriminates. **The FOURTH vacuous-pin cell of the phase and the FIRST found at the IMPL.** **(2) ⚠️ A DELETED `+0/+0` NEGATIVE-CONTROL ARM IS INVISIBLE TO EVERY GATE** — a patch silently dropped fixture `0120`'s `record("clean_fin", …)`, the DISCRIMINATING control exercising the predicate's `io.EOF` term, and `go vet`, `gofmt` and the fixture's OWN cross-side pins all stayed quiet because that arm contributes ZERO BY DESIGN; only a per-arm probe caught it, and the commits were rebuilt with the line restored and every measurement re-derived. **A control whose whole point is contributing zero cannot be guarded by a total.** **(3) SPEC §5.2's "rest 0" IS REFUTED IN PRACTICE** — the empty-map NC exhibits `no_certificate = 1`, so a `wantLeaves` written from the SPEC's figure goes RED AGAINST CORRECT CODE; **§5.3's "assert the client-certificate signature" instruction likewise produces a FALSE RED**, the version-invariant discriminator being the server-side counter TRIPLE. **(4) SPEC §5.3's "ONE LISTENER, two arms" IS UNSTATEABLE** alongside that triple — the control arm drives `ssl.handshake` to 1, so `handshake == 0` on the withheld-cert arm is impossible on a shared listener; per-arm listeners KEEP exact equality. Also refuted: the WEAK form of arm (b) would be GREEN at the un-fixed tip (**OBSERVED, not reasoned** — both variants measure `served=false` there); **PLAN §0.5's "TWO SITES ARE NOT HISTORY" IS FOUR**; a CASE-SENSITIVE sweep actually MISSED one (`The five` reads 2 vs 3); the line shift is **BANDED (+0/+8/+9/+14), not one constant**; **SEVEN citations were invalidated by this row's own doc-comment growth**, repaired and re-anchored on SYMBOLS; NC 7 found a detector the PLAN did not enumerate; SPEC §5.2's "six rows byte-for-byte untouched" is UNACHIEVABLE IN GO once the struct gains a field; the controller's brief MISCOUNTED the guarded non-sites as TEN (**it is NINE**) and offered `clientAuthFor`, a symbol that DOES NOT EXIST — both caught by subagents that greped first; a nonexistent package selector (`./internal/xds/sds/...`) failed LOUDLY FOR THE WRONG REASON, reading exactly like a test failure; and **the NINETEENTH, found while writing the close: the controller's own merged `--numstat` TOTAL read `+1355 / -93` while its own per-file table sums to `+1347 / -92`** — re-derived at the publishing tip rather than carried forward. **MEASURED COST, fifteen files `+1347 / -92`, `.go` nine files `+1038 / -56`** — ⚠️ PLAN §1.3 UNDERSHOT `.go` by ~28% (≈ +812) and PLAN §7's `config.go` 629 is STALE (**642**). ⚠️ **THE ROW TOUCHES TWO FILES BEYOND PLAN §4's DECLARED MAP** — `internal/xds/secret.go` and `internal/xds/secret_test.go`, both COMMENT-ONLY citation repairs: **DECLARED, NOT SILENT.** **SIX-GATE POSTURE, EVERY FIGURE FROM THIS SESSION'S OWN RUNS, DEPARTURES NAMED RATHER THAN COMPLIANCE CLAIMED**: differential **122/122 PASS, 0 FAIL, 0 SKIP**, RC=0, 406.690s, set reconciled BY NAME in BOTH directions with the extractor NC'd on a scratch copy and **the count-only form PROVEN VACUOUS** (122 under a rename) while both `comm` arms fired — and **`0120` is GREEN on its FIRST-EVER run with the fallback present**, having been built on a branch based on the un-fixed tip so its pre-fix RED could be measured; h2spec **95 tests, 94 passed, 1 skipped, 0 failed**; fuzzers **56 / 48 (+0)**; **anchored panic gate 0 AND PROVEN LIVE** (fired on a scratch `^panic:` and on a scratch `DATA RACE`, read 0 on a green run carrying a MID-LINE `panic:` mention); **the NINE guarded non-sites byte-untouched AND PROVEN LIVE** (a corrupted literal fired on exactly that row — ⚠️ their LINE numbers DRIFTED, `manager_test.go:1276`->`:1280` and `:1732`->`:1736`, so the guard is BY LITERAL); `gofmt -l` OUTPUT empty and `golangci-lint` v1.64.8 rc=0 on tls/listener/xds/`0120`-driver, US-locale misspell sweep clean; **no `REVIEW.md` — a STANDING DEPARTURE, NAMED not claimed.** ⚠️ **AND THE NON-DOCKER SWEEP IS A DEPARTURE WITH BOTH RUNS REPORTED, BECAUSE A GREEN RERUN CLEARS NOTHING**: run 1 read `PIPESTATUS[0]`=1 with anchored FAIL **8** over TWO failing packages (123 ok + 2 FAIL + 110 no-test = **235**, RUN=8731); run 2, same 235 packages with `-count=1`, read RC=0 / FAIL=0 / 125 ok + 110 no-test = **235**. The two are `TestSDSEndToEnd_FetchFailure_BootFailsClosed` (`internal/boot`) — a REGISTERED flake, 4/4 green in isolation at HEAD, ⚠️ **NOT structurally immune** since `internal/boot` DOES depend on `internal/tls` and `internal/listener`, though the install is gated on a non-empty `NextProtos` while that test drives an SDS fetch failure with no `alpn_protocols` — and `TestFramer_ReaderGoroutineDoesNotLeak` (`internal/filter/hcm/h2`), **NOT PREVIOUSLY REGISTERED and NEWLY REGISTERED HERE**, a client-preface `connection reset by peer` at `framer_leak_test.go:186` in a package `go list -deps -test` shows has NO dependency on `internal/tls`, `internal/listener` or `internal/xds` and that this row touches no file in, 3/3 green in isolation at HEAD ⇒ contention-sensitive under the parallel sweep. **ADR-0317 COMPLETED IN PLACE** after the RETAINED italic footer (no renumber, no `---`), and the house `PROPOSED` guard **DISARMED 1 -> 0**. ⚠️ **FINAL WHOLE-BRANCH REVIEW: APPROVED WITH FINDINGS — ONE Important, FOUR Minor, ALL FIVE CLOSED IN-BRANCH BEFORE THE SQUASH** (`PROGRESS.md` §8). **THE IMPORTANT ONE FALSIFIED THIS ROW'S OWN SAFETY CLAIM: "TCP-only" WAS TRUE OF THE SYMBOL AND FALSE OF THE PATH.** `NewQUICDownstreamConfig` really was byte-untouched, but `internal/listener/manager.go`'s `default_filter_chain` block called `NewDownstreamConfig` **UNCONDITIONALLY** — none of the `kind == kindQUIC` branch its `filter_chains[]` loop carries — and `manager.go` accepts a listener with ZERO `filter_chains[]` plus only a default chain, while `quicTLSConfig()` returns `defaultChain.tlsCfg` FIRST. So a QUIC listener whose default chain carried a PLAIN `DownstreamTlsContext` with `alpn_protocols` built through the TCP builder, got the fallback INSTALLED, and handed it to `quic.Listen` — `negotiateALPN`'s RFC 9001 §8.1 rejection went VACUOUS. **RUN RED FIRST** against the un-fixed code (`GetConfigForClient != nil`, `NextProtos=[h3]`), and the positive companion was red too — a properly QUIC-wrapped default chain could not build AT ALL. Fixed by **MIRRORING THE BRANCH THAT ALREADY EXISTED**, no new policy: the plain shape now boot-rejects with the existing `"unexpected quic transport_socket type_url"` error, the config-parity reject `filter_chains[]` already produced. ⚠️ **THAT "NO NEW POLICY" CLAIM IS INCOMPLETE AND THE SCOPED RE-REVIEW OF THE FIX WAVE CAUGHT IT (finding F-A; disclosed at `ADR-0317` D-ALPNFB-TCPONLY and `PROGRESS.md` §9).** The same branch also WIDENS acceptance: a `kindQUIC` listener whose only chain is a `default_filter_chain` wrapping `require_client_certificate: true` plus a `trusted_ca` used to boot-REJECT and now BUILDS — with `ClientAuth = NoClientCert` and `ClientCAs = nil`, because `NewQUICDownstreamConfig` never evaluates `require_client_certificate` at all (the PRE-EXISTING, DECIDED gap at `BEHAVIOR_CONTRACT.md` item 8(b) / D-RCCF-QUIC). **Nothing that previously shipped regresses** — the identical shape under `filter_chains[]` already behaved this way — but parity with a known gap INHERITS it into a shape that used to be refused, and the bare claim was the same symbol-vs-path error the row records at F1: true of the narrowing, false of the whole change. ⚠️ Also: the mirroring is PARTIAL — the mandatory-TLS reject was NOT mirrored, so a `transport_socket`-less QUIC `default_filter_chain` still builds and fails at Start instead of boot (finding F-E, pre-existing, BANKED not fixed). Phase-61 deferred item **M-FB1** is discharged incidentally. **ZERO pre-existing QUIC + `default_filter_chain` shapes** in any fixture or test, VERIFIED not trusted. `ADR-0317` §Context ¶4 and D-ALPNFB-TCPONLY **AMENDED IN PLACE** — correct and NOT an append-only violation, this being the row's OWN ADR in the same unsquashed branch — with all guards re-measured UNCHANGED: `^---$` **216**, `^## ADR-` **316**, bare `^## ` **324**, tail **ADR-0317**, house `PROPOSED` **0**. The four Minor findings: `STATE.md`'s stale `PROGRESS.md` count (F2, below), the scope-less `--numstat` figure (F3 — fifteen files `+1347 / -92` is the implementation range EXCLUDING this row's four close artefacts; the bare full-range command at `0cdb9050` reads nineteen files `+1829 / -103`), fixture `0120`'s "counters are BLIND" measurement RE-SCOPED to the SIX-arm shape it was taken on and RETAINED as the evidence for arm (vii) (F4 — at SEVEN arms the pins are 3/3 and the deletion IS caught, by arm (vii), still not by the counters), and the entry-guard prose (F5 — of THREE guards exactly ONE is live and NC-exercised; TWO are defensive and UNREACHABLE, code left alone, `SPEC.md` §4 frozen).
- **phase-directory:** `docs/envoy-go/phases/95-tls-alpn-mismatch-fallback/` — `BRAINSTORM.md` (**314**) + `SPEC.md` (**516**) + `PLAN.md` (**865**) + `PROGRESS.md` (**705**). Phase dirs STAY **136**. ⚠️ **THE `PROGRESS.md` FIGURE WAS FALSE AT THE TIP AND IS RE-DERIVED HERE** (final-review finding F2): it read **465** while `wc -l` read **469** — commit `0cdb9050` added `+6/-2` to `PROGRESS.md` and did not roll this file. A false PRESENT-TENSE count in the one file whose own preamble promises a session may trust a grep of it. It now reads **705**; re-derived by `wc -l` at the PUBLISHING tip, never copied. ⚠️ **THAT FIGURE WAS ROLLED TWICE, AND THE SECOND ROLL IS THE POINT**: the fix wave set it to **605** after appending `PROGRESS.md` §8, and the controller's own subsequent edits — the scoped re-review's §9 adjudication plus the post-fix differential re-run in gate (a) — moved it to **705**. A count in this file goes stale the instant anyone appends to the file it counts, which is exactly how F2 arose; it is therefore re-derived in the SAME commit as every edit that moves it, never in a later one. **`PROGRESS.md` created — the lifecycle's IMPL artefact. NO `REVIEW.md`** — a STANDING DEPARTURE, NAMED not claimed, matching the 93/94 posture.
- **lifecycle-state:** **DONE** (row 95 reads `done` at `ROADMAP.md:157`; BOOTSTRAP §5 state DONE ⇒ the next session **SELF-PICKS** the next subject per the 2026-07-12 standing directive and runs its **BRAINSTORM**). ⚠️ **SENTINEL RE-RUN MECHANICALLY AT THIS TIP, ACTUAL OUTPUT, BOTH BEFORE AND AFTER THE FLIP — AND THE NC SHAPES CHANGED ACROSS IT:** BEFORE, (1) `NOT DONE: row 95` ONE line at `want=127` · (2) **SIX** at `:205 :211 :217 :227 :233 :241` · (3) SILENT · NC-A **TWO** · NC-B **TWO**. AFTER, **(1) SILENT** · (2) **STILL SIX**, same six line numbers · (3) SILENT · **NC-A ONE** line (`NOT DONE: row 62`) · ⚠️ **NC-B ALSO DROPPED TO ONE** (`GATE FAIL: examined 127 data rows, expected 126`) — the router warned only about NC-A, but NC-B loses its `NOT DONE: row 95` line by the same mechanism. NC-C FIRED at 0 residual both times; NC-D **96 / 68** both times (with `--` before the pattern). Check-(2) positive control **residual 0 with 6 substitutions ASSERTED** both times — BOTH phrases substituted, because the longer does not contain the shorter and a one-phrase control reports a residual of 5 that reads like a finding. Per-line md5 of the six windows, **trailing newline INCLUDED**, **ALL SIX BYTE-IDENTICAL BEFORE AND AFTER**: `205 10d7807bf02d` · `211 4a92f7e62fc6` · `217 2a7eb298b9fd` · `227 242e53c6f7a3` · `233 b2680e6f4fbf` · `241 6caa1c3ce0e7`. ⇒ **THE SENTINEL DOES NOT FIRE**; `stop` EVALUATED and deliberately NOT created, verified absent at the git root and in this worktree. ⚠️ **NO CANDIDATE LINE WAS DELETED OR "TIDIED"** — deleting the last one ENDS THE PROJECT, and the history is `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases.
- **next-skill:** **SELF-PICK THE NEXT SUBJECT AND RUN ITS BRAINSTORM** (state DONE -> 1) per the 2026-07-12 standing directive — smallest defensible candidate first, recording the pick AND the rejected alternatives, with **no pause for a human pick**. There is **no banked mid-lifecycle work**: phase 95 is closed end to end. ⚠️ **THE RECOMMENDED PICK CHANGED AFTER THIS BULLET WAS FIRST WRITTEN, AND THE ROUTER AGREES WITH THE LATER FORM — `next-prompt.txt` §THE NEXT SUBJECT.** It read *"the most defensible banked pick is the DRIVER-OWNED RECEIVER PORT RACE"*; that pick's OWN stated trigger — *"if a full differential run aborts again"* — **DID NOT FIRE** at this close (two full 122/122 runs, both RC=0, no abort), and its cost is **~36 driver files** by the probe-and-rebind SHAPE (**not the 14 the memory note claims**). **The smaller and better-evidenced pick, MEASURED at this close rather than imagined, is the `default_filter_chain`-vs-`filter_chains[]` DIVERGENCE SET** — the class the final review's F1 opened and closed only ONE member of. The surviving member: the `filter_chains[]` loop rejects a QUIC chain with no `transport_socket` at config-parse and the default-chain block carries no such arm, so that shape BUILDS and fails later at Start instead. ⚠️ **NOT a safety hole** (`internal/listener/quic.go` catches the nil config before `quic.Listen`) — a deferred-reject / config-parity DEPARTURE. ⚠️ **AND NOT EVERY DIVERGENCE IS A DEFECT: the mixed-TLS cross-check is DELIBERATELY absent per ADR-0080.** ⚠️ **TWO DOCUMENTS DISAGREEING ABOUT THE NEXT PICK WOULD SEND A SESSION SOMEWHERE DIFFERENT DEPENDING ON WHICH IT READ FIRST; that is why this bullet is amended rather than left to be outvoted.** Two ALPN follow-ons are banked and deliberately NOT chartered: **no fixture asserts `NegotiatedProtocol` cross-side outside `0120`**, and **no fixture combines SDS with `alpn_protocols`** (recorded at ADR-0317 §Context ¶8 as ABSENT COVERAGE). ⚠️ **RE-DERIVE EVERY COUNT AT YOUR OWN TIP; INHERIT NOTHING FROM THIS BULLET.**
- **last-commit:** ⚠️ **TAKE THE TIP FROM `git rev-parse master`, NEVER from a subject grep and NEVER from a count quoted anywhere.** ⚠️ **RUN *BOTH* LOCATE FORMS AND RECONCILE THE SETS** — slug-anchored `--grep '^phase 95 (tls-alpn-mismatch-fallback)'` and loose `--grep '^phase 95'`; ⚠️ **a slugless CORRECTION commit is INVISIBLE to the anchored form.** ⚠️ **NO COUNT IS QUOTED HERE ON PURPOSE: BOTH FORMS ROT.** ⚠️ **A loose `--grep 'phase NN.*IMPL'` MATCHES SPEC AND PLAN COMMITS** whose subjects contain the word — anchor on `^phase NN (slug) STAGE:`.
- **last-updated:** 2026-09-07 (phase-95 **IMPL done**; row 95 FLIPPED `done`, `want` STAYS **127**, `ROADMAP.md` STAYS **245** lines — a FLIP, not an ADD. `DECISIONS.md` 18902 -> **18942** with `^---$` STAYS **216**, `^## ADR-` STAYS **316**, bare `^## ` STAYS **324**, tail STAYS **ADR-0317**, next-free STAYS **ADR-0318**, the house `PROPOSED` guard **DISARMED 1 -> 0** — verified BY LINE AND BY ADR via a BACKWARD heading search resolving to `18874 ## ADR-0317` — and the ADR-0231 decoy at `:14866` STAYS **1** under its own different matcher, byte-identical. `BEHAVIOR_CONTRACT.md` STAYS **5989** lines: the `:1944` redemption is **WITHIN-LINE**, 357 -> **1841** chars, with `:1961` and `:1971` md5-identical. `internal/tls/config.go` 582 -> **642**. `go.mod`/`go.sum` BYTE-UNTOUCHED (**67** require entries; ⚠️ the `grep -cE '^\s+[a-z0-9./-]+ v[0-9]'` form reads **62** — NAME THE FORM); fixtures STAY **122** (`0120` EXTENDED to seven arms; `0121` still FREE and this row did NOT take it), extractor reconciles **122 = 122** with both `comm` directions EMPTY and the extractor itself NC'd — ⚠️ **a count-only check is VACUOUS, the count stays 122 under a rename**; BackendKind tail STAYS **38**; fuzzers STAY **56 / 48**; `go list ./...` STAYS **237** (**235** excluding the two Docker drivers); phase dirs STAY **136**; `-family row` STAYS **96 / 68**. Stat surface **+0** — no new metric name, only WHICH of `ssl.handshake` / `ssl.connection_error` a mismatch arm increments; a DELTA never an absolute, and by the ledger's own convention a `+0` row earns NO chain entry. **`STATE_HISTORY.md` 552 -> 554 (+2, NOT +1 — the archive entry is a BLANK LINE plus the entry line)**; archive strict guard **163, DELTA 0**, parenthetical **63 -> 64**, loose **226 -> 227**, and `163 + 64 = 227` reconciles.)
- **next-free ADR:** **`ADR-0318`**. TAIL-derived (`grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1` -> `## ADR-0317`; `grep -c '^## ADR-0318'` ⇒ **0**). ⚠️ **NEVER derive from the heading count** — the id space is SPARSE with exactly one gap at `ADR-0209`, so headings+1 (**316**+1) collides with `ADR-0317`, which is TAKEN. ⚠️ **NEVER gate on the UNANCHORED `PROPOSED` form nor the middle-ground `^\*\*Status:\*\*.*PROPOSED` form** (which reads **23**); **NAME WHICH FORM ANY GATE USES**, and never conflate the strict house form (`^> \*\*STATUS: PROPOSED`, now **0**) with the ADR-0231 decoy (`^\*\*Status:\*\* PROPOSED`, still **1** at `:14866`) — a different matcher entirely.

---

## Project counts at this commit

*(⚠️ **NOT LIVE — THIS BLOCK IS A PHASE-76-ERA SNAPSHOT, AND IT SAID "Live" FOR SIXTEEN PHASES.** Corrected at the phase-93 BRAINSTORM. The bullets below are RETAINED VERBATIM for their historical detail and their negative controls; **their headline figures are stale and are superseded by the correction table immediately below.** The live pointer is §Current, above.)*

⚠️ **CORRECTION LANDED AT THE PHASE-93 BRAINSTORM (2026-08-29).** The bullets below were re-run mechanically at the **phase-76 IMPL close** — sixteen phases ago — under a header that read *"(Live. Re-verify at session start …)"*. **Three of their figures were re-derived at `40cf6766` and are superseded here; each command is given so the next session need not trust this line either:** **active differential fixtures 119 -> `121`** (`ls -d test/fixtures/*/ | wc -l`; tail `0119-grpc-unary-trailers`, **`0120` FREE** — ⚠️ the plausible `grep -cE '^[0-9]{4}-'` form reads **119** because it drops `0007a`/`0007b`) · **fuzzers 55 -> `56` targets across `48` FILES** (`git grep -c '^func Fuzz' -- '*.go'`; the occurrence form agrees — ⚠️ **it went stale INSIDE row 92's own commit**, which added `FuzzValidateResponseHeaderBlock` while the row's ROADMAP text still anticipates `+0`) · **DECISIONS.md tail ADR-0298 -> `ADR-0314`, next-free `ADR-0315`** (TAIL-derived; headings+1 COLLIDES at the `0209` gap). ⚠️ **THE STAT SURFACE IS DELIBERATELY NOT RENUMBERED.** Three different absolutes are live in this tree at this one tip — this block's, the phase-92 `PLAN.md`'s, and the `BEHAVIOR_CONTRACT.md` ledger tail's — and the contract warns of itself that a mechanical re-derivation *"should expect the re-derived figure to disagree."* Per `reference_a_drift_correction_is_itself_a_claim`, **on a contested count: NO NUMBER.** Enforcement remains the per-phase `TestNoNewStat*` delta guards, never an absolute. ⚠️ **AND `406` / `406 -> 407` MUST NOT BE RESTATED ANYWHERE** — refuted at the phase-92 PLAN; `grep -c '406' BEHAVIOR_CONTRACT.md` reads **0**. **The remaining bullets (BackendKind tail 38, go.mod, reference pin, conformance) were re-verified at `40cf6766` and still hold.**

- **active differential fixtures:** **119** — ⚠️ **Phase 76 landed +0: `0061-lb-ring-hash` was EDITED, not added** (`sourceIPs` 4 -> 16 plus a NEW `driver/linkage_test.go` inside the EXISTING fixture directory — no new YAML, no new directory, no new BackendKind, no new port), so the count STAYS **119** and `0118` remains FREE. NC observed: `mkdir .../0118-fake` ⇒ 120. — `ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0117-tracing-custom-tags-metadata-cluster` (phase 73). **Phase 75 landed +0**: `0110-tls-require-client-cert-false` was **EXTENDED** with a cross-side `fixture.StatsAsserter` (NO new YAML, NO new directory, NO new BackendKind, NO new port), so the count STAYS **119** and `0118` remains FREE. ⚠️ **Next-free REFERENCE port is `10450`, NOT `10118`** — ports are NOT fixture-index aligned. **The fixture now asserts what its own README confessed was infeasible, and BOTH sides were EXECUTED**: `ssl.handshake=2 ssl.no_certificate=1 ssl.fail_verify_error=1 ssl.fail_verify_no_cert=0`, `downstream_cx_total=3`, **identical on reference and subject**, with the discriminating asymmetry (`handshake=2` vs `no_certificate=1`) observed per side. Shape A over `/stats/prometheus`, keyed on the metric NAME with `envoy_listener_address` IGNORED — the NAMES are byte-identical cross-side while the labels differ (`0.0.0.0_10446` reference vs **`___20016`** subject: ⚠️ **the subject binds the IPv6 WILDCARD**). ⚠️ **`0111` was NOT given a value assertion, deliberately** — it runs `require_client_certificate: true` on both sides, so a success-path no-cert annotation reads **0 on every arm, structurally**, and a value pin would document a **vacuous `0 == 0`** as coverage; its PROSE gained the name in its NAMED-UNASSERTED list instead, because **both `0111` passages are CLOSED ENUMERATIONS** and a name in neither list reads as ASSERTED. **`0111`'s driver is byte-untouched, and its named-subset check was EXECUTED for the first time at this IMPL: PASS.** ⚠️ `var _ fixture.StatsAsserter` is a **TRIPWIRE, not the dispatch mechanism** — `0110` is only the **THIRD** fixture repo-wide to carry it, while **82** define an `AssertStats` method.
- **fuzzers:** **55** — **Phase 76 landed +0** (it consumes no new config field, so there is no parse arm to fuzz). NC observed: appending `func FuzzProbeOnly` ⇒ 56. — `grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`. **Phase 75 landed +0** — the row consumes no new config field at all, so there is no parse arm and no fuzz-seed classification to flip (`reference_fuzzer_count_docs_drift`: an `f.Add` seed is not a fuzzer).
- **stat surface:** **1205** — BEHAVIOR_CONTRACT doc count. **There is NO mechanical counting command** (the contract says so itself); enforcement is per-phase `TestNoNewStat*` guards asserting a delta of ZERO, never the absolute total. **Phase 75 LANDED +1 (1204 -> 1205)** — `listener.<normalized-addr>.ssl.no_certificate`, the FOURTH listener-scope `ssl.*` name and the FIRST **SUCCESS-PATH** handshake annotation (⚠️ **NOT a synonym for `ssl.fail_verify_no_cert`, and the mis-mapping is wrong in BOTH directions** — one annotates an ACCEPTED handshake, the other books a REJECTED one; they are mutually exclusive on every connection). ⚠️ **The delta is a NAME-surface delta, NOT a flat per-deployment one:** registration rides the block that ALREADY gates on `rt.tlsMode`, so a **plaintext-only** deployment gains **ZERO** names, while a **QUIC** listener gains it permanently zero — **PARITY** with the reference, and structural (`Manager.Start` launches no accept loop for `kindQUIC`, so the Inc site is unreachable for a **name-independent** reason). ⚠️ **QUIC was NOT re-probed at phase 75** — the parity claim is INHERITED from phase 74's family-level probe, and that inheritance is stated in BEHAVIOR_CONTRACT rather than presented as a measurement. Ledger updated: `1204` -> `1205` at `BEHAVIOR_CONTRACT.md:831`/`:847`, plus a `**Phase 75 - 1204 -> 1205 (+1)**` line after the tail entry — **the ONLY line-adding edit in the file** (`5744 -> 5746`), so every by-line citation into the TLS subsection stays valid. ⚠️ **The new entry RECORDS BOTH ledger gaps and BACK-FILLS NEITHER** (fabricating a line would be inventing a record). Gate figures at this close: `grep -c '1204'` => **2** · `grep -c '1205'` => **3** · `grep -cE '^#{2,4} .*ADR-[0-9]+.*ADR-[0-9]+'` => **17** (was 16 — the `:1849` heading joined them, on the `:785` precedent). ⚠️ **`internal/statssink`'s four *"stays 1200 / 1196"* prose sites, stale since phase 49, are RECORDED-NOT-FIXED** — that package is on this phase's byte-untouched sha256 roster.
- **BackendKind tail:** **38** — **Phase 76 landed +0** (`0061` uses the in-process `TCPEcho` branch and implements neither `BackendKindAware` nor `PerHostBackendKind`). NC observed: appending `ZZProbeKind = 39` ⇒ tail 39. — (`H2GoawayResponder`, `test/differential/fixture/fixture.go:614`). **Phase 75 landed +0** — `0110` was extended, not cloned, and it implements neither `BackendKindAware` nor `PerHostBackendKind`, so the runner takes the in-process `TCPEcho` branch. ⚠️ **This is a TAIL VALUE, not a count** — the file declares **39** constants (values 0-38, `TCPEcho = 0`). The lineage figure **38** is correct as written; do NOT 'fix' it to 39.
- **DECISIONS.md tail:** **ADR-0298** (next-free **ADR-0299**) — `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1` -> `## ADR-0298`; `grep -c '^## ADR-0299'` ⇒ **0**. **ADR-0298 is COMPLETE as of the phase-76 IMPL** — §Context drafted at the phase-76 SPEC, §Decision + §Consequences APPENDED IN PLACE after the RETAINED italic footer, **no renumber and NO `---` separator** (the file's last `^---$` is still at `:17020`). Block shape controller-re-derived at this close: **1** `### Context` / **1** `### Decision` / **1** `### Consequences` / **1** retained footer. ⚠️ **FOUR of its OWN §Context paragraphs were CORRECTED IN PLACE** (¶1's `:295` -> `:307`, ¶5's `-count ~= 81` -> **83**, ¶7's *"~71 collapses"* -> **74/2000**, ¶11's *"needs no edit"* -> REFUTED) on the ADR-0297 ¶7/¶9 precedent, each **leading with what survives**; none changes a decision the ADR records. ⚠️ **It carries NO whole-file grep count and claims NO family ordinal** — the Load-balancing family closed at row 54, and **maintenance rows do not extend a charter**. *(Historical, retained: **ADR-0297 is COMPLETE** — §Context drafted at the phase-75 SPEC (ADR-0044-**as-used**; ⚠️ ADR-0044 does NOT actually contain that discipline), §Decision + §Consequences appended IN PLACE at the phase-75 IMPL after the **RETAINED** footer, mirroring ADR-0295/0296. Block shape verified at this close: **1** `### Context` / **1** `### Decision` / **1** `### Consequences` / **1** retained footer; `grep -c '^## ADR-0298'` => **0**. **No renumber, no new ADR.** ⚠️ **Its OWN §Context ¶7 and ¶9 were CORRECTED IN PLACE while completing it** — ¶7's **self-falsifying whole-file grep** (the same species the phase-74 IMPL had just fixed in ADR-0296 ¶3, reproduced one ADR later) is restated with **NO number** and the searched token deliberately **NOT spelled**, because every restatement mints another counter-example; and ¶9's *"SELF vs OTHER-ADR"* form rule is replaced by **graft SCALE** after the n=7 population was re-enumerated first-hand. ⚠️ **¶9 ALSO said *"the only two instances in the file"* — which this row's own blockquote makes three** — so a pure append would have shipped a SECOND self-falsifying count in the commit that fixed the first. **The prescription has now outlived THREE wrong justifications: family, self-vs-other, scale.** ⚠️ **`ADR-0296` is NO LONGER byte-untouched** — its §Decision (g) carries the phase-75 correction as an INDENTED blockquote (`grep -c '^  > '` **2 -> 3**), **LEADING WITH WHAT SURVIVES** (no such counter was owed *in phase 74*, whose sole stat-asserting fixture is `require=true` on both sides — **the deferral was RIGHT**) and claiming **NO ORDINAL**.)*
- **go.mod modules:** **Phase 76 landed +0** — `go mod tidy -diff` EMPTY and `git diff master -- go.mod go.sum` EMPTY; ⚠️ **the row's TEST-side import delta is +8 and that is CORRECT** (`ringhash_test.go` gains `fmt`/`math`/`mathrand "math/rand/v2"`, `linkage_test.go` adds `go/ast`/`go/parser`/`go/token`/`strconv`/`testing`) — all stdlib, all test-side, and **an import LINE is not a go.mod MODULE**; there is no new sub-package, so `reference_new_subpackage_pulls_transitive_module` does not bite. NC observed: a fake `require` ⇒ 67 -> 68. The tracked lineage figure is **2** (the phase-61.2 count: `quic-go` direct + `qpack` indirect) — **NOT a repo total**; the single `go.mod` requires **67** (18 direct + 49 indirect). **Phase 75 landed +0** — `go mod tidy -diff` EMPTY and `git diff master -- go.mod go.sum` EMPTY. The row needed `crypto/tls` and `internal/stats`, both ALREADY imported by `internal/listener/manager.go`; **an import LINE is not a go.mod MODULE**, and there is no new sub-package, so `reference_new_subpackage_pulls_transitive_module` does not bite. ⚠️ **TEST-side imports grew by exactly four** (`log`, `math`, `net/http`, `strconv` in `0110`'s driver) — all stdlib, all test-side, **not a production-envelope violation**.
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` (see `ENVOY_TARGET.md`).
- **conformance:** h2spec **`95 tests, 94 passed, 1 skipped, 0 failed`** (the skip is 6.9.2/2, invariant) — the CORRECTED 95-case strict scope under dotted selectors, measured at the phase-85 IMPL close 2026-08-10 (ADR-0307; the historical 53/53 records measured only 53 of the 95 under nine silently no-op slash-form selectors, 2026-04-25 -> 2026-08-10); proxy-wasm all 10 families PASS.

---

## Recent lineage

*(A SHORT recent lineage, capped at FIVE prior entries by ADR-0288. Everything earlier is in `STATE_HISTORY.md`. §Current carries the newest stage — **the phase-95 IMPL**, which closed the phase, flipped row 95 `done`, refuted EIGHTEEN claims by execution plus a nineteenth found while writing the close, and reproduced the forecast authentication bypass by driving it rather than by reasoning about it. The entry displaced from the tail of this list at this close was the **SPEC stage of the phase BEFORE the current one**; it is archived verbatim in `STATE_HISTORY.md` and is deliberately NOT named here, because a preamble that SPELLS the evictee's label makes the eviction check match its own prose. ⚠️ **At this close the DATE READ AND THE LIST POSITION AGREE, for the second close running** — the five dates read `09-07, 09-06, 09-05, 09-03, 09-02`, five DISTINCT values, so there is no tie anywhere and the unique oldest IS the tail. **That agreement is a coincidence of this tip, not a rule: the tie sat in the MIDDLE two closes before this one and AT THE OLDEST POSITION one close before that, where a date read could not discriminate at all.** ⚠️ **AND THE BARE FORMS ANSWER NOTHING** — strict and naive both read FIVE here, and BOTH are invariant under which entry is evicted (five in, five out). **READ THE DATES AND THE POSITIONS, USE THE LABEL-BOUND PAIR ACROSS BOTH FILES — the evictee's own label moves `STATE.md` 1 -> 0 and `STATE_HISTORY.md` 0 -> 1, and a single count on either file alone is NOT the check — AND INHERIT NO SHAPE.**)

- **prior active-phase:** `phase 95 (tls-alpn-mismatch-fallback) PLAN done` (2026-09-07) — lifecycle-state **2 -> 3**. `PLAN.md` created (**865** lines), an **18-task** TDD spine discharging all NINE owed items of `SPEC.md` §15, with the task count **DERIVED here** (the SPEC deliberately quotes none) and the BOOTSTRAP §6.1 split gate **EVALUATED AND NOT TRIPPED** (18 vs ~25 tasks; ≈ **+812** `.go` lines, ≈ **+989** all-in, vs ~1500). **`ROADMAP.md`, `BEHAVIOR_CONTRACT.md` and `DECISIONS.md` are BYTE-UNTOUCHED** — a scope MEASURED across FOUR precedent PLAN commits (`db539e7d` `90010c4c` `bae5e24d` `acedfd2b`), not inferred from wording; **a PLAN adds no ADR**, so tail STAYS `ADR-0317`, next-free STAYS `ADR-0318`, and the house `PROPOSED` guard STAYS **ARMED**. **ZERO production `.go` bytes.** The stage REFUTED **SEVENTEEN** claims by execution, **four load-bearing, and three of those are pins that as specified would FAIL AGAINST A CORRECT IMPLEMENTATION OR COULD NEVER FAIL AT ALL.** (a) ⚠️ **`SPEC.md` §0.1's AUTHENTICATION BYPASS IS CONFIRMED BY EXECUTION AND IS WORSE THAN STATED: the bypassed client is not merely admitted, it is SERVED.** NC 7 was written deliberately — a client presenting **NO client certificate** completed the handshake AND got a full application round trip through a `require_client_certificate: true` chain, at BOTH TLS versions (`hsCompleted=true served=true negotiated="" certSent=false`, `no_certificate=1`/`handshake=1`/`connection_error=0`); instrumented at both instants, the install anchor carries `NoClientCert`/`nil` and the handshake-time clone carries `RequireAndVerifyClientCert`/non-nil. (b) 🔴 **`SPEC.md` §12 CELL 13 IS A VACUOUS GUARD** — removing `alpn_protocols` from the SUBJECT side leaves the fixture **GREEN** (both sides `3`/`2`), and removing it from the REFERENCE side is RED but byte-identical to the tip-bug red; **no arm can see the YAML edit through those two counters**, and the neutralised config is indistinguishable from the FIXED one. A measured replacement (an overlap arm pinning `NegotiatedProtocol` per side: subject `"h2"` -> `""`) is specified. (c) ⚠️ **§5.3's "assert the client-certificate signature" instruction produces a FALSE RED against the CORRECT code** — at TLS1.2 the client reads `remote error: tls: handshake failure` with **no `certificate` substring** (`handshake_server.go:966-971` sends `alertCertificateRequired` only at TLS1.3); the version-invariant discriminator is the server-side TRIPLE `fail_verify_no_cert==1` + `connection_error==0` + `handshake==0`. (d) ⚠️ **§5.2's "`handshake == 1`, rest 0" is REFUTED** — `startOneWayTLSListener` sends no `CertificateRequest`, so **every completing handshake also books `ssl.no_certificate`**; `wantLeaves` must be `{handshake:1, no_certificate:1}` or the map reddens on correct code. Also refuted: **§7's comment roster names ONE member of a NINE-member class**, and two missed sites (`tls_handshake_negative_test.go:27`, `manager_test.go:1086`) are **already-false present-tense claims this row makes WORSE by resurrecting the `GetConfigForClient` symbol**; an **unscoped grep for the return statement reads SEVEN and the seventh is the QUIC return the design forbids touching** (and "byte-identical" is false — `:220`/`:248` carry trailing comments, so a literal-line grep reads 2 or 5, never 6); the `panic()` reachability control fires on **2 of 4** drives, which **retires row 8's post-fix value** (only row 7 discriminates once the callback intercepts first); **cell 10 is INVISIBLE** unless §5.4 re-reads the base config AFTER driving the callback; a failing arm-(vi) drive makes `AssertStats` **dead code**; and `SPEC.md` §10's `STATE_HISTORY.md` **+1 forecast was +2**, contradicting the precedent immediately before it.

- **prior active-phase:** `phase 95 (tls-alpn-mismatch-fallback) SPEC done` (2026-09-07) — lifecycle-state **1 -> 2**. `SPEC.md` created (**516** lines), discharging all NINE owed items of `BRAINSTORM.md` §10 and **REFUTING ELEVEN** claims by execution. Three are load-bearing. (a) **The BRAINSTORM's per-chain PRE-BUILD of the alternate TLS config is an AUTHENTICATION BYPASS**: the mandated entry anchor sits at `internal/tls/config.go:57`, but `cfg.ClientCAs` and `cfg.ClientAuth` are assigned LATER at `:94-95`, so a build-time clone carries `NoClientCert` and would admit a mismatched-ALPN client to a `require_client_certificate` chain with NO cert verification — per-handshake `Clone()` is therefore the ONLY correct option, not a preference. **The phase-95 PLAN then CONFIRMED this by execution and found it worse: the bypassed client is SERVED a full application round trip.** (b) **A SPEC MAY NOT TOUCH `BEHAVIOR_CONTRACT.md`**, though the router's owed-list item 5 says it must: measured across FIVE consecutive SPEC commits (`975e527e` `13ad0aa0` `8f7bdaf0` `682b4734` `307f2e3d`), every one leaves it and `ROADMAP.md` BYTE-UNTOUCHED — the `:1944` edit is PINNED at §11 and LANDED by the IMPL. (c) **The BRAINSTORM's fixture arm (vi) cannot detect (a)** — a VALID client cert passes under both the correct code and the bug; the SPEC adds the WITHHELD-cert shape that discriminates. Also refuted: FOUR cited anchors off by exactly `+1` (the six returns are `128 195 220 229 248 258`, the QUIC return `:290`, the `NextProtos` append `:579` — `:580` is BLANK); `BEHAVIOR_CONTRACT.md:1944` is **357** chars, NOT the "enormous line" (that is `:1971`, **5098**); **FIVE** fixtures carry downstream `alpn_protocols`, not one (`0004` `0079` `0080` `0119` TCP, `0104` QUIC) — the no-regression conclusion SURVIVES and is now PROVABLE, since the tip ENFORCES overlap so every green fixture is by construction one the fallback no-ops on.

- **prior active-phase:** `phase 95 (tls-alpn-mismatch-fallback) BRAINSTORM done` (2026-09-06) — lifecycle-state **DONE -> 1**. **PHASE 95 OPENED BY SELF-PICK** per the 2026-07-12 standing directive; there was no banked mid-lifecycle work (PROVEN: all 126 rows `done`, check (1) SILENT at `want=126`, NC-A the one-line `NOT DONE: row 62`) and no human pick was solicited. **ROW 95 REGISTERED `in-progress`** at `ROADMAP.md:157` — an **ADD, not a flip**, so the denominator MOVES: `want` **126 -> 127** and `ROADMAP.md` **244 -> 245**. Row field-count **NF=8 under BOTH the naive and the escape-aware form, COUNTED BEFORE AND AFTER INSTALLING** (the summary carries **no pipe character at all**; total `|` chars in the row = **7**, the delimiters only). **THE SUBJECT: when a downstream TLS client offers ALPN values overlapping NONE of the chain's `alpn_protocols`, the pinned reference COMPLETES the handshake with no protocol selected and SERVES the connection (`ssl.handshake +1`, `ssl.connection_error +0`, HTTP 200 over HTTP/1.1 via the AUTO codec — MEASURED, six arms per side with a TLS-1.0 positive control per side), while envoy-go ABORTS with `no_application_protocol` and, since phase 94, books `ssl.connection_error +1`.** The tree records this as PARITY (`internal/listener/tls_handshake_negative_test.go:117-118`) — REFUTED by execution. ⚠️ **THE INHERITED CANDIDATE 1 — "the cheapest, a seventh unit-table row asserting the increment" — WAS WRONG IN KIND: asserting `+1` would have PINNED a live cross-side behaviour divergence as parity.** **COST FLOOR MEASURED by a built, run and reverted prototype: ONE production file (`internal/tls/config.go`, `+31/-0`), a `GetConfigForClient` fallback installed at the ENTRY of `NewDownstreamConfig` (⚠️ NOT at a `return` — the function has SIX return sites and the first placement landed in the wrong one and changed nothing); the prototype matches the reference on all six arms; with it in place exactly ONE test is red (`TestNewManager_LiveHandshake_ALPNNegotiationFailure_Aborts`, which pins the wrong side), the six-row counter table, its stacked control and the QUIC ALPN-mismatch test all green.** QUIC is deliberately untouched (RFC 9001 §8.1). Row lands +0 fixtures (`0120` gains a sixth arm), +0 stat names, +0 BackendKinds, +0 fuzzers, +0 modules. **Core-TLS / downstream-listener MAINTENANCE row claiming NO family ordinal** (rows 85-91 precedent). ⚠️ **NINE inherited claims REFUTED**, listed at `BRAINSTORM.md` §0 — including the memory note's port-race roster (FOURTEEN; measured **~36** driver files) and a measurement agent's next-free ADR ("0292 area"; TAIL-derived **ADR-0317**).

- **prior active-phase:** `phase 94 (tls-connection-error-stat) IMPL done` (2026-09-05) — lifecycle-state **3 -> DONE**. **PHASE 94 CLOSED; ROW 94 FLIPPED `in-progress` -> `done`** at `ROADMAP.md:156` (a **FLIP, not an ADD** — `want` STAYS **126**, `ROADMAP.md` STAYS **244** lines; rows 93 and 94 both re-counted **NF=8 under BOTH the naive and the escape-aware form, BEFORE AND AFTER**). `outcomeOther` now increments `listener.<normalized-addr>.ssl.connection_error` — the FIFTH listener-scope TLS counter — when `!isTransportHandshakeErr(err)`, a CLOSED transport COMPLEMENT matched by `errors.Is` ALONE with **NO message-text matching**. The named departure at `BEHAVIOR_CONTRACT.md:1971` is **CLOSED**. ⚠️ **The `handshakeOutcome` taxonomy STAYS FOUR while this scope's counters go to FIVE**, so `outcomeOther` is the ONLY outcome mapping to a counter CONDITIONALLY and the 1:1 reading is wrong in BOTH directions. **The TWENTY-FIFTH Observability-family row; the family STAYS OPEN.** 18 tasks, THREE parallel streams on private worktrees with Docker serialized to exactly ONE. **TWELVE claims REFUTED by execution, THREE of which invalidated text this stage had ALREADY COMMITTED** and were corrected in place — chiefly that **TWO predicate terms are DEFENSIVE and UNEXERCISED, not one** (`context.DeadlineExceeded` as well as `net.ErrClosed`; controller-verified independently).

- **prior active-phase:** `phase 94 (tls-connection-error-stat) PLAN done` (2026-09-03) — lifecycle-state **2 -> 3**. `PLAN.md` created (**1662** lines), an **18-task** TDD spine discharging all EIGHT items of `SPEC.md` §16. **ROW 94 STAYED `in-progress`**; `ROADMAP.md`, `BEHAVIOR_CONTRACT.md` and `DECISIONS.md` **BYTE-UNTOUCHED**, a scope MEASURED across three precedent PLAN commits rather than inferred. The stage REFUTED **SIXTEEN** claims by execution, two load-bearing: the SPEC's `sslLeafRoster` 2x2 **cell D is REFUTED** (removing the predicate is INVISIBLE at BOTH roster sizes, because the guard lives inside `case outcomeOther:` while the two tests it names never enter that case; the roster guards the **Inc SITE**, never the PREDICATE), and the unit layer **already reaches `outcomeOther`**, so the Inc fired unasserted and the fixture was never the only possible instrument. ⚠️ **The phase-94 IMPL then refuted TWELVE more, including THREE of this PLAN's own claims** — its `gofmt` replacement story (net churn is ZERO, not one line), two of its twelve "STABLE ANCHOR" rows, its Task-7 `four` gate (UNSATISFIABLE by the PLAN's own mandated text) and its Task-5 `=== RUN` denominator (**8**, not 9 — the stacked test was double-counted).




## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
