# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `phase 37 (load-balancer-maglev) IMPL done` (2026-06-13, this commit) — the phase-37 IMPL is COMPLETE (`superpowers:subagent-driven-development`; `docs/envoy-go/phases/37-load-balancer-maglev/PROGRESS.md` — the 10-task TDD PLAN executed end-to-end subagent-driven, six-gate GREEN; **ROADMAP row 37 flips `in-progress → done`** [a flat family row — NO parent rollup per ADR-0106]). Lands the project's **fifth LB policy** and its **second consistent-hashing policy**: `Cluster.LbPolicy MAGLEV` (`envoy.config.cluster.v3`, enum value 5) — **the cheap follow-on**, the seam's FIRST hash-policy REUSE. As built: the **ADR-0238** fixed-size Maglev lookup-table `maglevLB` (`internal/cluster/maglev.go` — endpoints sorted by `Addr()` ascending, per host `offset = xxHash64(addr)%M` + `skip = (xxHash64Seed(addr,1)%(M-1))+1`, the canonical populate loop until all M slots fill [terminates: M prime → skip coprime → full M-cycle]; `Pick` indexes `table[hashKey % M]`, no-hash → a uniform random table index; returns `noopRelease`; built once, immutable, lock-free; the standalone `isPrime` helper) over the **REUSED ADR-0235 hash-key seam (`Pick(hashKey uint64, hasHash bool)`) + BOTH ADR-0237 producer planes (tcp `source_ip` + HTTP route `hash_policy`) UNCHANGED** (the FIRST hash-policy reuse — ZERO seam churn, ZERO seam ADR, ZERO producer churn, ZERO producer ADR; ZERO new hash code [`xxHash64`/`xxHash64Seed` reused]; ADR-0024 UNAMENDED). `cluster.Manager` accepts `MAGLEV` via `case Cluster_MAGLEV` + `parseMaglevLbConfig` (default 65537 self-supplied + the HAND-ROLLED cap `≤ 5000011` THEN primality gate — both reference PARITY; mismatched-oneof silent-ignored) with the reject text extended `…, RING_HASH` → `…, RING_HASH, MAGLEV` (the doubly-hit-retargeted `TestManager_Error_UnsupportedLBPolicy` → CLUSTER_PROVIDED). The 2 MIRRORED `maglev_lb.{min,max}_entries_per_host` gauges (Set once at table build via a `*maglevLB` type-assert — MAGLEV-only; cross-side-exact `floor(M/N)`/`ceil(M/N)` = 21845/21846; NO `size` gauge). Differential `0063-lb-maglev` — an HTTP route header `hash_policy` feeding MAGLEV (the `0062` convention reused), N=16 `X-Hash` values × K=16 = 256 routed `GET /get` + 8 `/health`: the per-side modular invariant (`≡ 0 mod K` affinity, BOTH sides) + spread (`≥ 2`) + conservation (256) + cross-side `/health` byte-equiv + a cross-side `StatsAsserter` (the 2 gauges cross-equal + `upstream_rq_total` cross-equal); cross-side host identity NOT asserted (different tables — the `0062` posture); the 3 `-count=1` deliberate breaks (scatter-the-key → affinity; BUILD-level collapse → spread + both gauges; stats-drop → stats) STILL bite; 20/20 flake check. **⚠️ D-M5 (PARITY, not departure):** the `table_size` primality reject faithfully reproduces the reference's own `The table size of maglev must be prime number`. The ADR-0238 §Decision/§Consequences body landed IN-PLACE per ADR-0044 (DECISIONS tail advances **ADR-0237 → ADR-0238**; next-free **ADR-0239**). Six gates GREEN (build/vet/gofmt/lint; `-race -short`; 65-dir differential byte-exact [the 64 prior dirs unchanged through the new MAGLEV case]; h2spec 53/53 + proxy-wasm 10/10 asserted-unaffected by change-scope construction; `go mod tidy -diff` empty). As built: stat surface **1119 → 1121** (+2 `maglev_lb.*` gauges — the SECOND non-zero LB-stat delta), fixtures **64 → 65** (`0063`; NO boot-reject dir), fuzzers UNCHANGED **42** (the FOURTH no-fuzzer LB phase — the table property test FOLDED into `maglev_test.go`), BackendKind tail UNCHANGED **33** (`0063` reuses the `0062` `HTTPEcho`), ZERO new packages + ZERO new go.mod deps + ZERO new hash code + ZERO new exported symbols. **The Load-balancing family stays OPEN** — **4** candidates remain after 37 {subset LB, locality-weighted LB, priority load balancing, panic thresholds}.
- **phase-directory:** `docs/envoy-go/phases/37-load-balancer-maglev/` (**BRAINSTORM.md + SPEC.md + PLAN.md + README.md** — the durable phase-37 BRAINSTORM charter [the Q0/Q1 decisions, the v1.32.4 enum-5 + `Cluster.MaglevLbConfig` verification, the seam-REUSE rationale, the header-key cross-side affinity+spread differential design, the single anticipated ADR, the §10 D-M1..D-M6 pins; NOTE its "primality DEPARTURE" framing is SUPERSEDED by SPEC AMEND-M5 → PARITY] + the SPEC [the D-M1..D-M6 empirical-pin block §11 + the `maglevLB` algorithm §3 + the `0063` differential §8 + the ADR-0238 §Context DRAFT §13] + **the PLAN.md [the 10-task TDD decomposition; landed THIS commit]** + the phase-overview README [status advanced 2 → 3]; the PROGRESS.md follows at the IMPL session [Task 1 creates it], per the single-flat-row per-phase-dir precedent [kafka-31/thrift-33/least_request-34/random-35]). **Superseded (preserved):** `docs/envoy-go/phases/36.2-load-balancer-ring-hash-http/` (**SPEC.md + PLAN.md + PROGRESS.md** — the durable 36.2 sub-phase SPEC + the 8-task TDD PLAN + the IMPL progress ledger [the as-built record + the six-gate evidence], all landed per the 32.1/32.2/29.1 per-sub-phase-dir precedent; the 36.2 IMPL LANDED THIS commit). The parent `docs/envoy-go/phases/36-load-balancer-ring-hash/` (**BRAINSTORM.md + SPEC.md + PLAN.md + README.md + PROGRESS.md** — the durable phase-36 parent BRAINSTORM/SPEC [both planes] + the 36.1 IMPL record; the 36.1/36.2 split is CONSUMED [parent SPEC §3.0], 36.1 LANDED). **Superseded (the phase-35 phase-directory, preserved):** `docs/envoy-go/phases/35-load-balancer-random/` (**BRAINSTORM.md + SPEC.md + README.md + PLAN.md** — PLAN.md landed THIS commit (README.md status advanced 2 → 3); the PROGRESS.md follows at the IMPL session [Task 1 creates it], per the kafka-31/thrift-33/least_request-34 single-flat-row per-phase-dir precedent). The phase-34 directory `docs/envoy-go/phases/34-load-balancer-least-request/` (**BRAINSTORM.md + SPEC.md + README.md + PLAN.md + PROGRESS.md** — the durable phase-34 IMPL record). The phase-33 directory `docs/envoy-go/phases/33-network-filter-thrift-proxy/` (**BRAINSTORM.md + SPEC.md + README.md + PLAN.md + PROGRESS.md** — the durable phase-33 IMPL record). The phase-32 dirs `docs/envoy-go/phases/32-network-filter-redis-proxy/` + `32.1-…/` + `32.2-network-filter-redis-commands-and-stats/` (**SPEC.md** — the durable phase-32.2 sub-phase SPEC; the PLAN.md follows at the 32.2 PLAN session, the PROGRESS.md at the 32.2 IMPL session [Task 1 creates it], per the 29.1/29.2/29.3 per-sub-phase-dir precedent). The phase-32.1 dir `docs/envoy-go/phases/32.1-network-filter-redis-upstream-pool-seam-and-codec/` (**SPEC.md + PLAN.md + PROGRESS.md** — the durable phase-32.1 IMPL record). The parent `docs/envoy-go/phases/32-network-filter-redis-proxy/` (**BRAINSTORM.md + SPEC.md + README.md** — the durable phase-32 parent-SPEC record). The phase-31 directory `docs/envoy-go/phases/31-network-filter-kafka-broker/` (BRAINSTORM.md + SPEC.md + README.md + PLAN.md + PROGRESS.md — the durable phase-31 IMPL record). The phase-30 + phase-29 directories stay the durable phase-30/29 record.
- **lifecycle-state:** `phase 37 IMPL done — the Load-balancing family STAYS OPEN; maglev LANDED at phase 37 (4 candidates remain after 37: subset LB, locality-weighted LB, priority load balancing, panic thresholds). Next: the next Load-balancing candidate or the next ROADMAP family (superpowers:brainstorming — the Load-balancing FAMILY stays OPEN; pick the next candidate per the next-prompt's onward routing)`. The phase-37 IMPL is COMPLETE: the durable record is `docs/envoy-go/phases/37-load-balancer-maglev/PROGRESS.md` (the 10-task TDD ledger + the six-gate evidence) + PLAN.md (the 10-task TDD spine; the FINAL ADR-0045 re-check → NO SPLIT; the D-S37-1..5 resolutions; the plan-document-reviewer APPROVAL) + SPEC.md (the D-M1..D-M6 pins + the algorithm + the `0063` design + the ADR-0238 §Context DRAFT) + BRAINSTORM.md (the design charter) + README.md.
- **next-skill:** the controller's **stage-close** (the final six-gate re-run + squash-merge the phase-37 IMPL worktree branch to master + push, per feedback_subagents_no_push + feedback_push_to_origin); then the next cold-start brainstorms the next Load-balancing candidate or the next ROADMAP family via the next-prompt roll-forward (`superpowers:brainstorming`).
- **last-commit:** the phase-37 IMPL completion bundle (Task 10) — BEHAVIOR_CONTRACT maglev delta (the `### Load balancer — maglev (MAGLEV)` subsection + the 2 `maglev_lb.*` gauges in `## Stat-name mapping` + the reject-text/deferred-list updates + surface 1119 → 1121) + the full ADR-0238 entry (the maglev policy — §Context promoted from SPEC §13, §Decision + §Consequences IN-PLACE per ADR-0044; DECISIONS tail → ADR-0238) + STATE/ROADMAP row 37 `in-progress → done` (fixtures 64 → 65). The prior Tasks 1–9 landed `maglev.go` + the manager case/gate/gauges + the `0063` fixture + the six-gate verification. NO push (the controller pushes at stage-close).
- **last-updated:** 2026-06-13 (phase-37 IMPL done)
- **next-free ADR:** `ADR-0239` (DECISIONS.md tail advances **ADR-0237 → ADR-0238** at this phase-37 IMPL — ADR-0238 [the maglev policy] §Context promoted from the phase-37 SPEC §13 DRAFT, §Decision + §Consequences landed IN-PLACE per ADR-0044, PROPOSED → ACCEPTED).

---

## Project counts at this commit

- **active differential fixtures:** **65** (tail `0063-lb-maglev`). Canonical recipe `ls -d test/fixtures/[0-9]* | wc -l` = 65. Advanced at the phase-37 IMPL (+1 — `0063-lb-maglev`, the HTTP route header-hash NAT-transparent cross-side per-side affinity+spread MAGLEV differential; the `0062` convention reused, retargeted to a MAGLEV cluster; NO boot-reject dir — the cap + primality rejects land unit-level). The prior +1 was the phase-36.2 IMPL (`0062-lb-ring-hash-http`).
- **fuzzers:** **42** (canonical recipe `grep -rh "^func Fuzz" $(find ./internal -name fuzz_test.go) | wc -l`). UNCHANGED at the phase-37 IMPL (+0 — the FOURTH no-fuzzer LB phase; maglev decodes no wire bytes; the table-build/lookup property test is unit-level, FOLDED into `maglev_test.go` [D-S37-5]). UNCHANGED at the phase-36.2 IMPL (+0).
- **stat surface:** **1121** (BEHAVIOR_CONTRACT doc count). Advanced at the phase-37 IMPL (+2 — the 2 MIRRORED `maglev_lb.{min,max}_entries_per_host` gauges [Set once at table build via a `*maglevLB` type-assert — MAGLEV-only; cross-side-exact `floor(M/N)`/`ceil(M/N)` = 21845/21846; NO `size` gauge]; the SECOND non-zero LB-stat delta after ring_hash-36.1; the DOC count is the source-of-truth, no programmatic golden). The prior surface was 1119 (UNCHANGED through the phase-36.2 IMPL + the phase-37 BRAINSTORM/SPEC/PLAN; the +2 lands at this IMPL — D-M4 pinned the exact surface, Task 6 registered the gauges, Task 10 advances the doc count).
- **BackendKind tail:** **33** (`TCPThriftResponder` — seq_id-agnostic canned framed-binary REPLY). UNCHANGED at the phase-37 IMPL (+0 — `0063` reuses the `0062` `HTTPEcho`; a policy-swap phase exercises WHICH key picks the backend, not a new backend). UNCHANGED at the phase-36.2 IMPL (+0).
- **DECISIONS.md tail:** **ADR-0238** (next-free **ADR-0239**) — **ADR-0238** (the `maglev` load-balancing policy — `Cluster.LbPolicy MAGLEV` enum value 5; the fixed-size Maglev lookup-table `maglevLB` REUSING the ADR-0235 hash-key seam + BOTH ADR-0237 producer planes UNCHANGED [the seam's FIRST hash-policy reuse]; the cap + primality `table_size` gate [both reference PARITY]; the 2 `maglev_lb.*` gauges; the policy ONLY — NO seam ADR, NO producer ADR, the phase-35 single-ADR-on-reuse shape) landed at the phase-37 IMPL, §Context promoted verbatim from SPEC §13, §Decision + §Consequences IN-PLACE, PROPOSED → ACCEPTED per ADR-0044. The prior tail ADR-0237 (the HTTP route `hash_policy` producer plane) + ADR-0236 (the `ring_hash` policy) + ADR-0235 (the seam extension) landed at the phase-36.1/36.2 IMPLs.
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227, superseding ADR-0008).
- **conformance:** h2spec 53/53; proxy-wasm all 10 families PASS — asserted-unaffected at the phase-37 IMPL by change-scope construction (maglev touches only `internal/cluster/*` + the `0063` fixture/registration + docs — NO HTTP/2 or proxy-wasm path; the wire path is byte-identical when no `lb_policy: MAGLEV` cluster is configured; the full 65-dir differential is the real guard).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
