# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `phase 26 BRAINSTORM done; Network-filters family OPEN at 2026-05-29 (this commit)` — **phase 26 (`network-filter-chain-and-rbac`) BRAINSTORM DONE at 2026-05-29 (this commit)**. Opens the first new §9 feature family since the HTTP-filters family closed at 25.3. Phase 26 bootstraps the L4 network read-filter chain framework (the network-layer analogue of phase 07.1's HTTP filter framework) + lands the family's first 3 filters across a BRAINSTORM-time 3-way pre-split: **26.1** `network-filter-chain-framework-and-echo` (NEW `internal/filter/network/` read-filter chain — iteration protocol + `ReadFilterCallbacks` + per-connection buffering + freeze-after-boot registry + `echo` + `direct_response`); **26.2** `network-filter-registry-migration` (migrate `tcp_proxy`/HCM onto the new registry/chain; retire the hardcoded `manager.go` `filterRegistry`; back-compat via existing fixtures); **26.3** `network-filter-rbac` (`rbac_network` full upstream parity + extract shared `internal/rbac/` engine [HTTP rbac = consumer #1 migrated + re-verified, network = #2] + NEW connection-level dynamic-metadata sink). 6-question user dialogue settled: Q1 first-phase selection (chain framework + rbac_network); Q2 3-way pre-split (THIRD project occurrence after phase-22/25); Q3 sub-phase mapping; Q4 read-filter-only (write-filter deferred + API-revision allowance); Q5 extract shared `internal/rbac/`; Q6 full upstream parity. Self-answered per §9 precedent: full cross-side byte-exact fixtures; upstream-parity + envoy-go-strict stats; ZERO new third-party go.mod deps. Spec-document-reviewer APPROVED (2 advisory recs folded in: D4 connection-metadata-sink shape elevated to SPEC-BLOCKING; ADR numbers locked to a clean 0213–0218). **Predecessor §9 HTTP-filters family CLOSED at 25.3 (commit `57c7c4d` IMPL squash; SHA-fill `24c8fe1`) — DO NOT reopen.** Phases 00–25 (all stages incl. 25.1/25.2/25.3 + the parent-25 rollup) DONE and CANONICAL.
- **phase-directory:** `docs/envoy-go/phases/26-network-filter-chain-and-rbac/` (**BRAINSTORM.md authored at this commit** — DONE; SPEC.md / PLAN.md / PROGRESS.md / REVIEW.md pending the SPEC+ stages). Sub-phase directories `26.1-network-filter-chain-framework-and-echo/` + `26.2-network-filter-registry-migration/` + `26.3-network-filter-rbac/` are created at each sub-phase's SPEC entry per the per-sub-phase convention (git does not track empty dirs).
- **lifecycle-state:** `phase 26 BRAINSTORM done` (SKILL_ROUTING state 0/1 → state 1/2 transition — parent BRAINSTORM authored per `superpowers:brainstorming`; spec-document-reviewer APPROVED; ROADMAP rows registered at this commit per ADR-0106). **ROADMAP registration at this commit per ADR-0106:** parent row 26 `planned → in-progress` (sub-phases `26.1, 26.2, 26.3` listed); sub-rows 26.1 + 26.2 + 26.3 registered `planned`. Parent row 26 flips `in-progress → done` at 26.3 phase-done via the ROLLUP precedent (18/19/22/24/25).
- **next-skill:** `superpowers:writing-plans` per BOOTSTRAP §5 state-1/state-2 routing, scoped to **parent SPEC authoring** (`docs/envoy-go/phases/26-network-filter-chain-and-rbac/SPEC.md`) per the phase-22/24/25 parent-row precedent. The parent SPEC executes the BRAINSTORM §10 D1–D8 empirical-pin obligations IN-SESSION against reference Envoy v1.37.2 per ADR-0004 (D4 connection-metadata-sink shape is SPEC-BLOCKING), anchors the ADR-0213.. §Context drafts, and formalizes the 3-way split surface-mapping. Per-sub-phase SPECs (26.1/26.2/26.3) follow the parent SPEC.
- **last-commit:** `77d6a01` — `phase 26 BRAINSTORM: open Network-filters family — L4 read-filter chain framework + echo/direct_response/rbac_network (3-way pre-split)`. The phase-26 BRAINSTORM squash-merge commit lands `docs/envoy-go/phases/26-network-filter-chain-and-rbac/BRAINSTORM.md` + the 4 ROADMAP rows (parent 26 `in-progress` + 26.1/26.2/26.3 `planned`) + this STATE.md advance + the next-prompt.txt SPEC-cold-start rewrite. SHA to be filled at the stage-close follow-up commit (`77d6a01` → actual) per the phase-22.x..25.x stage-close SHA-fill precedent. Predecessor: `b0007a4` — `next-prompt.txt: repoint master-tip references to 3bda442 (actual HEAD)`.
- **last-updated:** 2026-05-29
- **next-free ADR:** `ADR-0213` (UNCHANGED — a BRAINSTORM consumes no ADR numbers; DECISIONS.md tail STAYS at ADR-0212). The parent SPEC anchors the phase-26 §Context drafts beginning at ADR-0213; anticipated family span ADR-0213 .. ~ADR-0218 (≈6 ADRs, locked clean at this BRAINSTORM per the reviewer rec): 26.1 ADR-0213 (chain framework) + ADR-0214 (registry + boot-wiring); 26.2 ADR-0215 (tcp_proxy/HCM migration + hardcoded-registry retirement); 26.3 ADR-0216 (`internal/rbac/` extraction) + ADR-0217 (connection-metadata sink) + ADR-0218 (`rbac_network` filter). Exact numbers + §Decision/§Consequences body timing pinned at SPEC + per-sub-phase IMPL per ADR-0044. ADR-0209 + ADR-0213 escape-valve reserves carried from the §9 family STAND-UNCONSUMED (R8 STANDS-WEAK-default).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
