# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `phase 28 parent SPEC done` (2026-06-01, this commit) — **phase 28 (`network-filter-zookeeper-proxy`) parent SPEC DONE at 2026-06-01** — the parent master SPEC (`docs/envoy-go/phases/28-network-filter-zookeeper-proxy/SPEC.md`, 16 §§ + 1 appendix) formalizes the BRAINSTORM's 4 Q-decisions + EXECUTES all ten §10 D28-1..D28-10 empirical pins IN-SESSION against Envoy v1.37.2 per ADR-0004 (parallel-subagent fan-out: a LIVE docker boot of the reference image with a `[zookeeper_proxy, tcp_proxy]` listener + admin `/stats` + `/stats/prometheus` probes with two stat_prefix values; go-control-plane v1.32.4 binding reads + `proto.MessageName` run in-session; upstream filter/decoder/filter-manager/connection source fetched at tag v1.37.2). **Pins RESOLVED (11 AMENDs):** **D28-1 (SPEC-BLOCKING) CONFIRMED** — the standard `envoyproxy/envoy:v1.37.2` image ships `zookeeper_proxy` as a core extension (validate OK; boots; the full eager counter roster live at 0) → the Q3 cross-side strategy STANDS; AMEND-A1 the stat scope is **`<stat_prefix>.zookeeper.<counter>`** (REFUTES the BRAINSTORM's `zookeeper.<stat_prefix>.*` hypothesis); AMEND-A2 the upstream roster is **201 EAGER macro counters** (4 plain + 28 `_rq` + 29 `_rq_bytes` + 28 `_decoder_error` + 28 `_resp` + 28 `_resp_bytes` + 28 `_resp_fast` + 28 `_resp_slow`; the four `enable_*` flags gate INCREMENTS never creation) → stat surface 136 → **337** (the largest single-filter addition; supersedes the BRAINSTORM's ~190–240 estimate); AMEND-A3 roster asymmetries (`connect_readonly_rq`/`_rq_bytes` rq-side only; NO `auth_rq` — auth requests counted via LAZY per-scheme `auth.<scheme>_rq` dynamic counters, mirrored via `NewCounterIfAbsent`; `auth_resp*` ARE in the macro); AMEND-A4 reference Prometheus exposition is FLAT (`envoy_<prefix>_zookeeper_<counter>{}`, EMPTY label set — probed live with two prefixes) → the `internal/stats/name.go` arm is the **ADR-0138 INLINE-PREFIX shape** (NOT the `.rbac.` tag-extractor the BRAINSTORM assumed); AMEND-A5 connect is distinguished by xid SNIFFING (xid 0; the full special-xid set is 0/−1/−2/−4/−8 incl. SetWatches −8); AMEND-A6 the wire `OpCodes` enum (26 values, gaps at 10/20/102, `Close=−11`, `SetAuth=100`, `AddWatch=106`…) ≠ the proto's 27-value contiguous enum → a proto→wire mapping table is required; AMEND-A7 TWO correlation structures (data map erase-on-lookup + control FIFO queues per repeating control xid; unknown response xid → `decoder_error`); AMEND-A8 the decoder owns its OWN partial-packet reassembly buffers + passthrough is UNCONDITIONAL (`Continue` always; never closes; never mutates the chain buffer); AMEND-A9 **dynamic metadata DEFERRED** (REFUTES the BRAINSTORM's anticipated-mirror — full metadata needs deep payload parsing [the bulk of upstream's ~1100-line decoder] and has zero observable surface in envoy-go; coverage-boundary record; the 28.1 decoder is SHALLOW [header + min-length validation], with the payload-malformed leniency recorded as an envoy-go-lenient departure); AMEND-A10 latency-threshold PGV (override `threshold` REQUIRED + gte 1ms; `opcode` defined_only; duplicate override → upstream config-load exception → 28.2 boot-reject parity arm; comparison `latency <= threshold` → fast, INCLUSIVE; overrides keyed by WIRE opcode); AMEND-A11 the write path is REVERSE/LIFO order (config `[A,B,C]` ⇒ write dispatch `C→B→A`) + StopIteration-on-write = the-write-does-not-proceed (NO resume mechanism upstream) + writes enter the chain at `ConnectionImpl::write` BEFORE the transport (justifying the `writeChainConn` conn-wrap delivery) + zookeeper registers via `addFilter` (ONE instance, both directions) + its `onWrite` ALWAYS returns Continue. **The 2-way split surface-mapping is formalized** (SPEC §3.1 table: 28.1 = the WriteFilter seam + the zookeeperproxy request side incl. FULL 201-counter eager creation [creation parity — response counters exist-at-zero until 28.2] + the 7th built-in + bootstrap blank-import + the name.go arm + fixtures `0046-zookeeper-requests`/`0047-zookeeper-boot-reject` + the 37th fuzzer + ADR-0221/0222 bodies; 28.2 = the response decoder in OnWrite + xid correlation + watch events + latency-threshold fast/slow counters + fixture `0048-zookeeper-responses` + the 38th fuzzer + ADR-0223 body + the parent-row-28 ROLLUP + six-gate). **ADR-0045 gate re-checked per sub-phase:** 28.2 fits (~600–900 LoC / ~10–14 tasks); **28.1 STRADDLES (~1150–1500 production LoC / ~16–20 tasks) → D-P1 pre-authorizes the 28.1a (seam + config + roster + 0047) / 28.1b (request decoder + 0046 + fuzzer) split axis if the 28.1 PLAN trips**. Nine SPEC-time D-questions (D-P1..D-P9) recorded for sub-phase SPEC/PLAN resolution. **ADR-0221 (the WriteFilter seam — CONSUMES the ADR-0213 API-revision allowance) + ADR-0222 (zookeeper_proxy request side) + ADR-0223 (response side + latency-threshold counters) §Context drafts LANDED in DECISIONS.md** (tail ADR-0220 → **ADR-0223**; next-free **ADR-0224**; §Decision/§Consequences bodies land at the respective IMPLs per ADR-0044; phase 28 drafts ALL THREE at the parent SPEC — vs the 26 precedent's first-sub-phase-only — because the 0223 §Context is fully determined by this session's pins). **spec-document-reviewer APPROVED** (first iteration, no blocking issues; every cited code anchor + proto fact + the 201-counter arithmetic + the upstream stats macro independently re-verified by the reviewer; 1 advisory [scope the R6 fuzzer-count recipe to `./internal`] FOLDED IN). Counts UNCHANGED at SPEC (docs only; deltas land at the IMPLs): fuzzers **36** [→37 at 28.1, →38 at 28.2], fixtures **47** [tail `0045-sni-cluster`; →49 at 28.1, →50 at 28.2], stat surface **136** [→337 at 28.1 creation]. ROADMAP rows UNCHANGED at this commit: parent row 28 STAYS `in-progress`; sub-rows 28.1/28.2 STAY `planned` (28.1 flips `planned → in-progress` at the **28.1 SPEC** commit per the 26.x precedent / ADR-0106). Next: **28.1 SPEC** (`superpowers:writing-plans` scoped to the 28.1 sub-phase SPEC). **Earlier (phase 28 BRAINSTORM, preserved):** **phase 28 (`network-filter-zookeeper-proxy`) BRAINSTORM DONE at 2026-06-01** — the THIRD §9 Network-filters-family row (a parent pre-split row per ADR-0106 + the phase-22/25/26 precedent — the project's FOURTH BRAINSTORM-time pre-split), brainstormed interactively via a 4-question dialogue: **Q0** subject = `zookeeper_proxy` chosen from the 5 remaining §9 candidates (smallest well-bounded; pure observability; motivates the deferred write seam; `kafka_broker` carries contrib-image risk; `redis`/`thrift` are large terminal surfaces; `mongo` is better attempted AFTER the write seam exists); **Q1** scope = **both-direction COUNTER parity** — requests at 28.1, responses + the deterministic `enable_latency_threshold_metrics` fast/slow counters at 28.2; the latency HISTOGRAM family (`*_response_latency`) stays DEFERRED per ADR-0060 (BEHAVIOR_CONTRACT coverage-boundary record); 2-way DIRECTION-PROGRESSIVE pre-split; **Q2** the upstream-faithful **`network.WriteFilter` seam** — `OnWrite(buf *Buffer, endStream bool) Status` + write chain in REVERSE chain order (Envoy parity) + delivery via the chain runtime wrapping the downstream `net.Conn` handed to `handleTerminal` (`writeChainConn`; `prefixConn` precedent) — `TerminalFilter.Handle` signature UNCHANGED, `tcp_proxy`/HCM untouched; CONSUMES the ADR-0213 API-revision allowance (26.1 Q4 defer-with-allowance → consume-at-consumer; zookeeper_proxy = consumer #1, mongo_proxy = anticipated #2); `StopIteration`-on-write pinned at SPEC (anticipated documented-unsupported); **Q3** hermetic synthesized-byte differential fixtures (NO real ZooKeeper server) + cross-side **`StatsAsserter`** per-opcode counter parity as the LOAD-BEARING proof (the filter never mutates bytes — a body differential is vacuous) + deliberate-break liveness per fixture. `zookeeper_proxy` (`envoy.filters.network.zookeeper_proxy`, type `…filters.network.zookeeper_proxy.v3.ZooKeeperProxy` — bindings verified present in go-control-plane v1.32.4 at brainstorm time) is a passive observability sniffer decoding the ZooKeeper client protocol (jute) in BOTH directions, emitting per-opcode counters (~27–30 opcodes per the proto's `LatencyThresholdOverride` opcode enum; SPEC pins the exact roster vs upstream's stat-macro list incl. watch_event/connect_readonly) + dynamic metadata (anticipated-mirror via the connection-scoped ADR-0217 `*dynamicmetadata.Bucket` — second production write; SPEC D28-6 pins). **Sub-phase mapping:** **28.1** (`network-filter-write-seam-and-zookeeper-requests`) = the WriteFilter seam (ADR-0221) + the `internal/filter/network/zookeeperproxy/` package request side — config parse (9-field proto; `stat_prefix` REQUIRED → boot-reject; `max_packet_bytes` default 1 MiB; the three `enable_per_opcode_*` opt-in flags; `access_log` upstream-`[#not-implemented-hide:]` posture per SPEC) + the request decoder (length-prefix framing, connect special-case, xid/opcode dispatch) + per-opcode `*_rq` counters + `decoder_error` + the per-connection xid→opcode tracking map + the 7th `builtins.RegisterBuiltins` built-in + the bootstrap blank-import + the prom tag-extractor arm (`.rbac.` precedent) + fixtures `0046-zookeeper-requests` (cross-side) / `0047-zookeeper-boot-reject` + the 37th fuzzer (ADR-0222); **28.2** (`network-filter-zookeeper-responses-and-latency`) = the response decoder in `OnWrite` (response framing, xid correlation, `*_resp` counters, watch-event xid −1, ping/auth xids −2/−4) + latency-threshold fast/slow COUNTERS (deterministic differential arms via extreme thresholds — 1 h → all fast, minimal → all slow) + fixture `0048-zookeeper-responses` + the parent-row-28 ROLLUP + six-gate (ADR-0223). **Anticipated counts at family-row-done:** fixtures 47 → 50; stat surface 136 → ~190–240 (the LARGEST single-filter stat addition in the project; SPEC pins the mirrored roster); fuzzers 36 → 37–38; ADRs ADR-0221/0222/0223 (next-free after ≈ ADR-0224). ADR-0045 split-gate: ~1150–1950 net-new LoC / ~22–30 tasks as one phase → the 2-way pre-split; each sub-phase re-checks at its PLAN. After phase-28 phase-done, **4** §9 candidates remain (redis/mongo/kafka_broker/thrift). BRAINSTORM.md authored at `docs/envoy-go/phases/28-network-filter-zookeeper-proxy/BRAINSTORM.md`; ROADMAP parent row 28 registered `in-progress` + sub-rows 28.1/28.2 `planned` per ADR-0106. Next: **28 parent SPEC** (`superpowers:writing-plans` scoped to parent-SPEC authoring per the 22/24/25/26 parent-row precedent; executes the §10 D28-1..D28-10 empirical pins IN-SESSION vs Envoy v1.37.2 per ADR-0004 — **D28-1 [the standard ref image ships zookeeper_proxy] is SPEC-BLOCKING for the Q3 strategy**; anchors the ADR-0221/0222/0223 §Context drafts). **Earlier (phase 27 IMPL, preserved):** **phase 27 (`network-filter-sni-cluster`) IMPL DONE at 2026-06-01** — the SECOND §9 Network-filters-family row (a flat top-level row per ADR-0106; phase 26 was the anomalous pre-split family-parent) executed via `superpowers:subagent-driven-development` over the 9-task TDD PLAN (fresh subagent per task, two-stage review, local-only commits; controller squash-merges + pushes at stage-close). **LANDED:** the connection-scoped upstream-cluster-override seam (ADR-0219) — the NARROW typed `chainRuntime.upstreamClusterOverride` field (Q2/YAGNI — NOT a general filter-state primitive) + `SetUpstreamCluster(name string)` ON the `ReadFilterCallbacks` interface (the essential-channel `DynamicMetadata()` precedent, NOT the `rcd` type-assert sink — compile-forced no-op stubs into the three out-of-package `fakeCallbacks` doubles) + the `UpstreamClusterOverride(ctx) (string, bool)` ctx accessor (NEW `internal/filter/network/upstreamcluster.go`; unexported `upstreamClusterKey{}` + `withUpstreamClusterOverride`) + `handleTerminal` wraps the call `ctx` iff an override is set (the `TerminalFilter.Handle(ctx, conn)` signature UNCHANGED — no HCM ripple); `tcp_proxy` made per-connection-cluster-resolving (`Filter.cluster *cluster.Cluster` → `cm *cluster.Manager` + boot-resolved `defaultCluster`; `Handle` resolves `eff := defaultCluster`, override present&non-empty → `cm.Get(override)` [miss → zero-byte downstream close, `tcpproxy: per-connection override cluster %q not found`], else → `defaultCluster`; the `tcpproxy: cluster %q not found` / `weighted_clusters` rejects byte-stable; no-override byte-exact). **The `sni_cluster` filter (ADR-0220):** NEW `internal/filter/network/snicluster/` config-less package (`TypeURL` via `proto.MessageName` — the `extensions.` segment; `New` accepts empty/absent `typed_config`, malformed → `sni_cluster: invalid typed_config: %w`; `OnNewConnection` reads `Connection().RequestedServerName()` → `SetUpstreamCluster(sni)` verbatim iff non-empty + `Continue` [MUST Continue — sticky-halt constraint]; `OnData` pass-through `Continue`; no-op `OnDestroy`); registered as the 6th `builtins.RegisterBuiltins` entry (D27-S2: `cmd/envoy-go/main.go` delegates wholly to `RegisterBuiltins` — single insertion point, no parallel edit; ADR-0072 order-neutral); the `internal/bootstrap/bootstrap.go` blank-import of the `sni_cluster/v3` proto (echo precedent — the `bootstrap.Load` path does not import `builtins`); the end-to-end override-routes / unknown-close integration tests HOMED in the `builtins` package test (the only place importing BOTH `network` + `tcpproxy` without a cycle, driving the full seam via `NewChainRuntime`→`OnNewConnection`→`OnData`→`HandleTerminal`); the `0045-sni-cluster` 3-arm cross-side TLS fixture (route SNI `foo.example.com` / empty-SNI-fallback / unknown-cluster-close — byte-exact vs Envoy v1.37.2; a `fixture.DistributionAsserter` per-backend accept counts `[1,1]` makes the route/fallback arms NON-VACUOUS [a broken override → `[0,2]` FIRES — proven by a recorded liveness-break]; the D27-S3 `closeOK` close-lifecycle normalization: ref Envoy closes PRE-TLS-handshake via tls_inspector, envoy-go extracts SNI from the completed `*tls.Conn` then closes POST-`tcp_proxy.Handle` — both zero application bytes). **SIX GATES GREEN LIVE at Task 9** (run honestly, outputs quoted in PROGRESS.md): `go build` (exit 0) / `go vet` (exit 0) / `golangci-lint run` (exit 0) / `go test ./... -race -short` (78 pkgs ok, 0 FAIL); the FULL 47-fixture differential suite byte-exact GREEN (47/47 PASS, 0 FAIL — incl. the 8 back-compat `tcp_proxy`/26.x dirs + the new `0045`); h2spec 53/53 + proxy-wasm conformance 10/10 re-run LIVE (asserted-unaffected — 27 touches no HTTP/h2/proxy-wasm path — but re-confirmed green since the harness was available). **ADR-0219 + ADR-0220 §Decision/§Consequences bodies landed in place** (DECISIONS.md tail STAYS **ADR-0220**; next-free STAYS **ADR-0221**; NO new ADR number consumed per ADR-0044). **BEHAVIOR_CONTRACT 27 bundle** added (the NEW `### envoy.filters.network.sni_cluster` subsection + the `tcp_proxy per-connection cluster resolution — 27 amendment` block + the `downstream_cx_*`-unmirrored coverage-boundary record + the narrow-override-vs-filter-state stand-in note). **KNOWN coverage boundary:** `tcp.<stat_prefix>.downstream_cx_no_route` (and the wider `downstream_cx_*` family) is a known-unmirrored upstream counter — envoy-go's `tcp_proxy` emits none; a pre-existing gap NOT introduced by phase 27 (D27-4/§7.2); the unknown-override behavior IS proven cross-side byte-exact (0045 arm 3), the stat absence is invisible to a body differential. ROADMAP row 27 `in-progress → done`. The §9 Network-filters family stays OPEN (5 candidates remain: redis/mongo/kafka_broker/thrift/zookeeper). Final counts: fixtures 46 → 47, stat surface 136 (+0), fuzzers 36 (+0). Next: phase-28 BRAINSTORM. **Earlier (PLAN, preserved):** **phase 27 (`network-filter-sni-cluster`) PLAN DONE at 2026-05-31** — the **9-task** bite-sized TDD PLAN (`docs/envoy-go/phases/27-network-filter-sni-cluster/PLAN.md`) decomposes the SPEC §10 spine: Task 1 first-action baselines/anchors gate (re-pin fixtures **46** [tail `0044`], stats **136**, fuzzers **36**, DECISIONS tail **ADR-0220** [next-free **ADR-0221**; NOTE: the SPEC §10 Task-1 row's "tail ADR-0218" was written PRE-SPEC-commit — the LIVE tail is ADR-0220], `proto.MessageName` + the as-built line anchors); Task 2 the `chainRuntime.upstreamClusterOverride` field + `SetUpstreamCluster` ON `ReadFilterCallbacks` + the concrete `*callbacks` impl (ADR-0219 writer seam); Task 3 the `UpstreamClusterOverride` ctx accessor (new `internal/filter/network/upstreamcluster.go`) + `handleTerminal` threads the override iff set (ADR-0219 reader seam); Task 4 `tcp_proxy` per-connection resolution (`Filter.cluster` → `cm` + `defaultCluster`; `Handle` override-then-fallback + unknown→close; the no-override back-compat sentinel stays in `tcpproxy/filter_test.go`); Task 5 the `internal/filter/network/snicluster/` config-less filter (`TypeURL` via `proto.MessageName`; `OnNewConnection` SNI→`SetUpstreamCluster`+`Continue` LIVE-asserted; `OnData` pass-through; no-op `OnDestroy`; ADR-0220); Task 6 register as the 6th built-in (`builtins.RegisterBuiltins` SINGLE insertion point — D27-S2 RESOLVED: main.go delegates wholly to `RegisterBuiltins`, NO parallel edit) + the override-routes / unknown-close END-TO-END integration tests (homed in the `builtins` package test — the only place that imports BOTH `network` + `tcpproxy` without a cycle, driving the FULL seam via `NewChainRuntime`→`OnNewConnection`→`OnData`→`HandleTerminal`); Task 7 the `0045-sni-cluster` 3-arm cross-side TLS fixture (route / empty-SNI-fallback / unknown-cluster-close; mirror `0043-network-rbac` driver shape; deliberate-break proof); Task 8 back-compat differential re-verify (the existing `tcp_proxy` fixtures byte-exact) + full suite 46→47; Task 9 the completion bundle (BEHAVIOR_CONTRACT 27 subsection + `tcp_proxy` amendment + ADR-0219/0220 §Decision/§Consequences bodies in-place per ADR-0044 + STATE/ROADMAP row 27 `in-progress → done` + six-gate). **plan-document-reviewer APPROVED** (first iteration, Status: Approved, no Issues Found; every line-anchor + code snippet + the import-cycle reasoning [`network`↛`tcpproxy`; `tcpproxy`→`network`; `builtins`→both] independently verified against as-built code; 3 advisory recs FOLDED IN — the mixed-chain `resumeIdx`-resets-to-0 dispatch-fidelity nuance [reach `TerminalReady()` via an `OnData` pass, not by calling `HandleTerminal` straight after `OnNewConnection`]; the non-reusable cross-package `mkClusterMgr` note; the tightening of Task-4's test-home-split prose). **ADR-0045 split-gate RE-CHECKED at PLAN time: NO split** (9 tasks / ~180–400 net-new LoC — both axes well under the ~25-task / ~1500-LoC gate; single flat phase 27). **PLAN-time D-questions resolved:** D27-S1 (the `0045` dir re-pinned at IMPL Task-1; fallback arm = a TLS connection with NO SNI on a `[sni_cluster, tcp_proxy]` chain — proves the empty-SNI no-op AND the configured-cluster fallback in one); D27-S2 (main.go delegates wholly to `RegisterBuiltins` — single insertion point, no parallel edit); D27-S3 (unknown-override close via the `Handle` early-`return` → deferred `downstream.Close()` FIN → zero-byte body parity; NO `SO_LINGER` on the terminal path); D27-S4 (fuzzer DEFER — echo-parity config-less → fuzzers stay 36). Docs only, no code change. Next: **27 IMPL** (`superpowers:subagent-driven-development` per `feedback_execution_style` — fresh subagent per task, two-stage review, local-only commits; controller squash-merges + pushes at stage-close). **Earlier (SPEC, preserved):** **phase 27 (`network-filter-sni-cluster`) SPEC DONE at 2026-05-31** — the SPEC (`docs/envoy-go/phases/27-network-filter-sni-cluster/SPEC.md`, 16 §§ + 1 appendix) refines the BRAINSTORM's 3 Q-decisions against the AS-BUILT 26.1/26.2/26.3 framework + EXECUTES all eight §10 D27-1..D27-8 empirical pins IN-SESSION against Envoy v1.37.2 (ADR-0004; `sni_cluster.cc` + `tcp_proxy.cc` fetched at tag v1.37.2 via parallel-subagent fan-out + `proto.MessageName` run in-session). **Pins RESOLVED:** D27-1 `SniCluster` EMPTY proto, `@type` = `type.googleapis.com/envoy.extensions.filters.network.sni_cluster.v3.SniCluster` (`proto.MessageName` — the `extensions.` segment); D27-2/D27-6 `sni_cluster.cc::onNewConnection()` reads `requestedServerName()`, sets `PerConnectionCluster` (key `"envoy.tcp_proxy.cluster"`) with the SNI VERBATIM (`Mutable`/`Connection`), returns `Continue`, `onData` no-op `Continue`; D27-5 `tcp_proxy`'s `getRegularRouteFromEntries` reads the key FIRST → overrides the configured cluster; absent → fallback; D27-4 unknown override cluster → `downstream_cx_no_route` + `NoFlush` close (the `downstream_cx_*` family NOT mirrored — envoy-go `tcp_proxy` emits none; surface +0); D27-3 (seam-shaping) `SetUpstreamCluster(name string)` ON the `ReadFilterCallbacks` interface (essential-channel — the `DynamicMetadata()` precedent, NOT the `rcd` type-assert sink) + a NARROW `chainRuntime.upstreamClusterOverride` field threaded to the terminal via the call `ctx` (`handleTerminal` wraps it; `network.UpstreamClusterOverride(ctx)`; `TerminalFilter.Handle` signature UNCHANGED — no HCM ripple); D27-7 no per-route surface; D27-8 single phase (~180–400 LoC / ~9–14 tasks, ADR-0045 LOW) + fuzzer DEFERRED (echo-parity config-less → fuzzers stay 36). `tcp_proxy` made per-connection-resolving (retain `*cluster.Manager` + boot-resolved `defaultCluster`; override-then-fallback at `Handle`; unknown→close; back-compat byte-exact via the EXISTING 0000/0001/0002 + 26.x fixtures). **ADR-0219 (override seam + tcp_proxy resolution) + ADR-0220 (sni_cluster filter) §Context DRAFTED into DECISIONS.md** (tail ADR-0218 → **ADR-0220**; next-free **ADR-0221**; §Decision/§Consequences bodies land at 27 IMPL per ADR-0044). **spec-document-reviewer APPROVED** (first iteration, no CRITICAL/MAJOR; every line-anchor + the ctx-threading path + the `tcp_proxy` struct change + the ADR drafts independently verified against as-built code; 2 advisory notes [the canonical fuzzer-count recipe; the redundant-by-design empty-override guards] FOLDED IN). Counts UNCHANGED at SPEC (deltas land at IMPL): fuzzers **36** [+0], fixtures **46** [→47: one `0045-sni-cluster` 3-arm cross-side TLS dir — route/fallback/unknown-cluster-close; NO boot-reject], stat surface **136** [+0]. ROADMAP row 27 STAYS `in-progress` (flips at 27 IMPL phase-done per ADR-0106). Next: **27 PLAN** (`superpowers:writing-plans` → `PLAN.md`; ADR-0045 split-gate re-check at PLAN time). **Earlier (BRAINSTORM, preserved):** **phase 27 (`network-filter-sni-cluster`) BRAINSTORM DONE at 2026-05-31** — the SECOND §9 Network-filters-family row (flat top-level row per ADR-0106; phase 26 was the anomalous pre-split family-parent), brainstormed interactively via a 3-question dialogue (Q1 full upstream parity, SINGLE phase — no pre-split; Q2 a NARROW typed per-connection cluster-override seam keyed by the upstream-canonical `envoy.tcp_proxy.cluster` — NO general connection-scoped filter-state primitive [extract-at-second-consumer/YAGNI; API-revision allowance], NO dynamic-metadata reuse [Envoy uses filter-state=control not metadata=observability]; Q3 all THREE behavior arms cross-side byte-exact proven). `sni_cluster` (`envoy.filters.network.sni_cluster`, type `…filters.network.sni_cluster.v3.SniCluster` — an EMPTY proto) is a config-less read-filter that in `OnNewConnection` reads the TLS SNI (`Connection.RequestedServerName()`) + publishes it VERBATIM as a per-connection upstream-cluster-override (then `Continue` — no sticky halt per `reference_network_read_filter_onnewconnection_halts`); the terminal `tcp_proxy` consumes it to OVERRIDE its configured cluster (empty/absent SNI → configured fallback; SNI naming an unknown cluster → connection close). **0 NEW packages:** a small override seam in `internal/filter/network/` (a `chainRuntime` field + a `ReadFilterCallbacks` writer accessor + the `TerminalFilter` reader threading) + a contained `internal/filter/tcpproxy/` per-connection-resolution change (retain `*cluster.Manager` + default; override-then-fallback at `Handle`; unknown→close; back-compat byte-exact via the EXISTING 0000/0001/0002 + 26.x fixtures) + the config-less `internal/filter/network/snicluster/` filter (6th built-in). **Anticipated ADR-0219** (the override seam + tcp_proxy per-connection resolution) **+ ADR-0220** (the `sni_cluster` filter); next-free after ≈ ADR-0221. **Anticipated counts at IMPL:** fixtures 46 → 47 (ONE cross-side multi-cluster TLS dir proving route/fallback/unknown-cluster-close; NO boot-reject dir — config-less); stat surface 136 (+0 — the filter has no stats; SPEC pins whether the unknown-cluster path touches an existing tcp_proxy no-route counter); fuzzers 36 (→37 ONLY if SPEC adds a low-value config-less-parse fuzzer). **ADR-0045 split-gate LOW** (~180–400 net-new LoC / ~10–16 tasks → single phase; PLAN re-checks). Self-answered per §9 precedent: full cross-side byte-exact differential; upstream-parity (zero) stat surface; zero new go.mod deps. BRAINSTORM.md authored at `docs/envoy-go/phases/27-network-filter-sni-cluster/BRAINSTORM.md`; ROADMAP row 27 registered `in-progress` per ADR-0106 (verified vs the phase-23 flat-row brainstorm precedent — a flat §9 row registers `in-progress` at its BRAINSTORM commit). After phase-27 phase-done, 5 §9 candidates remain (redis/mongo/kafka_broker/thrift/zookeeper). Next: **27 SPEC** (`superpowers:writing-plans` scoped to SPEC authoring; executes the §10 D27-1..D27-8 empirical-pin obligations IN-SESSION against Envoy v1.37.2 per ADR-0004; anchors the ADR-0219 + ADR-0220 §Context drafts). **Earlier (phase 26.3 IMPL, preserved):** **phase 26.3 (`network-filter-rbac`) IMPL DONE at 2026-05-31**, and **parent phase 26 (`network-filter-chain-and-rbac`) DONE** (the parent-row-26 ROLLUP, atomic with sub-row 26.3, per the 18/19/22/24/25 precedent). The 15-task TDD PLAN executed via `superpowers:subagent-driven-development` (fresh subagent per task, two-stage review, local-only commits; controller squash-merges + pushes at stage-close). **LANDED:** the NEW shared `internal/rbac/` engine EXTRACTION (the abstract `EvalContext` + the ~23 permission/principal evaluators + the rules/matcher compilers + the matcher bridge + `PerPolicyCounters`, MOVED + re-exported, gaining the input-capability `Profile` [`ProfileHTTP`/`ProfileL4`]) — HTTP rbac migrated as consumer #1 (byte-exact RE-VERIFIED at the R4 gate: fixture `0018-http-rbac` stays byte-exact green; NO HTTP-rbac behavior change), `rbac_network` = consumer #2; the input-capability `Profile` PARSE-REJECTing the four HTTP-only arms at compile (`ProfileL4`; byte-stable `rbac: <arm> is HTTP-only ...`); the FIRST production WRITE through the connection-scoped `*dynamicmetadata.Bucket` (the rbac shadow pair `shadow_engine_result`/`shadow_effective_policy_id` under namespace `envoy.filters.network.rbac`; ADR-0217 — `internal/dynamicmetadata/` reused at connection scope, doc generalized in place); the `rbac_network` filter (`ProfileL4` L4 input surface; decision-in-`OnData` + `OnNewConnection` Continue no-op per the sticky-halt constraint; enforced-deny = `rbac_deny_close` rcd + `Close(NoFlush)` [the F3 NoFlush framework touchpoint]; shadow metadata + the four `<stat_prefix>.rbac.{allowed,denied,shadow_allowed,shadow_denied}` counters; `delay_deny` + HTTP-only-matcher + stat_prefix-invalid + xDS-dynamic-policy PARSE-REJECTs; CEL silent-ignore inherited F1; the rules/matcher dual-path); the NEW `.rbac.` Prometheus tag-extractor arm at `internal/stats/name.go` (`<stat_prefix>.rbac.* → envoy_rbac_<suffix>{envoy_rbac_prefix=...}`); the 5th `builtins.RegisterBuiltins` registration; the 36th fuzzer `FuzzNetworkRBACConfigParse`; the +2 differential fixtures (`0043-network-rbac` cross-side allow/deny/shadow `StatsAsserter` — the FIRST LIVE production mixed read→terminal chain R-M-LIVE; `0044-network-rbac-boot-reject`). **SIX GATES GREEN LIVE at Task 15** (run honestly, outputs quoted in PROGRESS.md): build/vet/lint/`-race -short` clean; the FULL 46-fixture differential suite byte-exact GREEN (46/46 PASS, 0 FAIL, 0 SKIP — NO environmental flake this run; incl. the R4 `0018-http-rbac` + the +2 rbac dirs); h2spec 53/53 + proxy-wasm conformance 10/10 re-run LIVE (asserted-unaffected — 26.3 touches no HTTP/h2/proxy-wasm path — but re-confirmed green since the harness was available). **ADR-0216 + ADR-0217 + ADR-0218 §Decision/§Consequences bodies landed in place** (DECISIONS.md tail STAYS **ADR-0218**; next-free STAYS **ADR-0219**; NO new ADR number consumed per ADR-0044). **BEHAVIOR_CONTRACT 26.3 bundle** added (the `### envoy.extensions.filters.network.rbac` subsection; stat table 132 → 136; the envoy-go-strict departures; the prom tag-extractor; the D-26.3-4 SNI/mTLS differential-gap note). **KNOWN coverage boundary D-26.3-4:** the SNI/`requested_server_name` + mTLS `authenticated` arms are unit-tested but NOT cross-side differential-proven (the IP/port arms ARE — fixture 0043). ROADMAP sub-row 26.3 `in-progress → done` + **parent row 26 `in-progress → done` ATOMICALLY**; the §9 Network-filters family stays OPEN (6 candidates remain: redis/mongo/kafka_broker/thrift/zookeeper/sni_cluster). Next: phase-27 BRAINSTORM (DONE this commit — `sni_cluster` selected; see the active-phase lead above). **Earlier (PLAN stage, preserved):** **phase 26.3 (`network-filter-rbac`) PLAN DONE at 2026-05-31**. The **15-task** bite-sized TDD PLAN (`docs/envoy-go/phases/26.3-network-filter-rbac/PLAN.md`) decomposes the 26.3 SPEC §10 spine: Task 1 first-action baselines/anchors gate; Tasks 2-3 the `internal/rbac/` engine MOVE (evaluator surface → compilers/`Evaluate`/matcher-bridge/`PerPolicyCounters`, re-exported); Task 4 the input-capability `Profile` (`ProfileHTTP`/`ProfileL4`) + HTTP-only-arm reject; Task 5 consumer-#1 HTTP-rbac migration (aliased import, `ProfileHTTP`, engine types); Task 6 the R4 differential re-verification gate; Task 7 the `NoFlush` close distinction (F3); Tasks 8-12 the `rbac_network` package (parse+`NewFactory(reg)`+4 static counters → L4 `EvalContext` → OnData decision [allow→Continue / deny→rcd+NoFlush-close; OnNewConnection Continue no-op] → shadow-metadata writes + dynamicmetadata doc generalization → 5th-built-in registration + boot smoke); Task 13 the 36th fuzzer; Task 14 the +2 differential fixtures (cross-side allow/deny/shadow StatsAsserter R-M-LIVE + boot-reject); Task 15 the BEHAVIOR_CONTRACT bundle + ADR-0216/0217/0218 §Decision/§Consequences bodies + the parent-row-26 ROLLUP + six-gate. **plan-document-reviewer APPROVED** (first iteration, Status: Approved; one advisory [Task-4 matcher-path HTTP-only-arm reachability — already hedged by the SPEC §3.4 + Task-4 NOTE] + two recommendations [Task-12 `reg.Lookup` accessor; line-anchor drift caught by the Task-1 re-pin gate] FOLDED IN at `edf93c2`). **ADR-0045 split-gate RE-CHECKED at PLAN time: NO split** (15 tasks / ~630-890 net-new LoC — the ~790 moved engine LoC is mechanical relocation re-verified byte-exact by the phase-16 fixtures [R4], NOT counted as churn — both axes within the ~25-task / ~1500-LoC gate; D-P1 confirmed). **PLAN-time D-questions resolved:** D-26.3-1 (enum `Profile` + `profile.permits(armKind)` at the HTTP-only arms in `buildOnePermission`/`buildOnePrincipal`; rules-path is the guaranteed reject target — the matcher-path carries no rbac-permission/principal leaf so its HTTP-only reject is vacuously satisfied, recorded at IMPL); D-26.3-3 (`networkrbac.NewFactory(reg)` closure-captures `*stats.Registry` in `RegisterBuiltins`, NOT a `FactoryCtx` field — preserving the ADR-0215 import-light invariant); D-26.3-7 (the `incPolicy`/`sync.Map` per-policy lazy-allocation moves to the engine as `rbac.PerPolicyCounters`; the four NAMED base counters + their registration + the `trackPerRuleStats` decision stay per-consumer; network passes false — F2). Docs only, no code change. Next: 26.3 IMPL. **Earlier (SPEC stage, preserved):** the per-sub-phase 26.3 SPEC (16 §§ + 2 appendices) — the THIRD/FINAL sub-phase of the phase-26 3-way pre-split — INHERITS the parent SPEC §5/§6/§7/§8/§11/§12/§13 rosters + refines the `rbac_network` per-Task surface against the AS-BUILT 26.1/26.2 framework (the `network.Connection` L4 accessors `LocalAddr`/`RemoteAddr`/`RequestedServerName`/`DownstreamPrincipals`; `ReadFilterCallbacks.DynamicMetadata()`; the `SetResponseCodeDetails` rcd sink; the `prefixConn` mixed-chain handover; `builtins.RegisterBuiltins`) + the phase-16 RBAC engine. **Three deliverables pinned:** (i) the shared `internal/rbac/` engine EXTRACTION (the abstract `EvalContext` + the ~23 permission/principal evaluators + the rules/matcher compilers + the matcher bridge + the per-policy machinery MOVED ~790 LoC + re-exported, gaining an input-capability `Profile` [`ProfileHTTP`/`ProfileL4`]; HTTP rbac = consumer #1, byte-exact re-verified by its phase-16 fixtures — R4; `rbac_network` = consumer #2) — ADR-0216; (ii) the FIRST production WRITE through the connection-scoped `*dynamicmetadata.Bucket` (the rbac shadow pair `shadow_engine_result`/`shadow_effective_policy_id` under namespace `envoy.filters.network.rbac`; `internal/dynamicmetadata/` reused at connection scope, doc generalized in place) — ADR-0217; (iii) the `rbac_network` filter (decision-in-`OnData` per AMEND-A8 + the sticky-halt constraint; allow → `Continue` → terminal [the FIRST production mixed read→terminal chain via `prefixConn` — R-M-LIVE]; enforced-deny → `rbac_deny_close` rcd + `Close(NoFlush)` [the F3 NoFlush framework touchpoint]; shadow → metadata + stats; the four `<stat_prefix>.rbac.*` counters [132→136]; `delay_deny` + HTTP-only-matcher PARSE-REJECT; CEL silent-ignore) — ADR-0218. **D-questions resolved at SPEC:** D-P1 (ONE sub-phase, net-new basis ~630-890 LoC / ~16-22 tasks — no 26.3a/26.3b split); D-P4 (CEL `condition`/`checked_condition`/`cel_config` SILENT-IGNORED by the phase-16 engine per ADR-0040 — `rbac_network` MIRRORS the silent-ignore; the parent §6.3 `condition-cel-unsupported` reject arm RETRACTED — the SPEC's one substantive refinement); D-P5 (network RBAC proto has NO `track_per_rule_stats` → `rbac_network` emits ONLY the 4 static counters; the engine's per-policy machinery stays dormant for consumer #2). **ADR-0216 + ADR-0217 + ADR-0218 §Context DRAFTED** into DECISIONS.md (tail ADR-0215 → **ADR-0218**; next-free **ADR-0219**; §Decision/§Consequences bodies land at 26.3 IMPL per ADR-0044). **spec-document-reviewer APPROVED** (first iteration, no CRITICAL/MAJOR; all four flagged resolutions independently verified against as-built code; 2 advisory recs already covered by SPEC deferral notes). Counts unchanged at SPEC (deltas land at IMPL): fuzzers 35 [→36], fixtures 44 [→46], stats 132 [→136]. ROADMAP sub-row 26.3 `planned → in-progress` at THIS SPEC commit (ADR-0106); parent row 26 STAYS `in-progress` (flips at 26.3 phase-done per the 18/19/22/24/25 ROLLUP). Next: 26.3 PLAN. **Earlier (phase 26.2 IMPL, preserved):** phase 26.2 (`network-filter-registry-migration`) IMPL DONE at 2026-05-30. The 12-task TDD PLAN executed via `superpowers:subagent-driven-development` (fresh subagent per task, two-stage review, local-only commits; controller squash-merges + pushes at stage-close). **LANDED:** the NEW `network.TerminalFilter` connection-takeover seam (`Handle(ctx, net.Conn)`) alongside the 26.1 `ReadFilter`/`OnData` model + a SEALED `NetworkFilter` marker via the exported `network.Marker` embeddable (`FilterInstanceFactory` generalized to `func() NetworkFilter`); the extended `network.FactoryCtx` (HasTLS/AllowH2C/ListenerPrincipal/NodeServiceCluster; heavy singletons closure-captured); the unified `[read-filter*, terminal-filter?]` chain runner (classify → read-prefix + optional trailing terminal; `TerminalReady()`/`HandleTerminal()` + the buffered-prefix `prefixConn` handover); `tcpproxy.NewNetworkFactory` + `hcm.NewNetworkFactory` adapters (Handle UNTOUCHED → byte-exact parity intrinsic); the unified `buildNetworkChainFactory` (NEW `network-filter-terminal-not-last` + `network-filter-multiple-terminals` chain-shape boot-rejects; the 26.1 `network-filter-mixed-chain-unsupported` reject LIFTED — mixed read→terminal chains now expressible, first PROD consumer rbac_network at 26.3); `serveConnection` step-7 unified into ONE `serveNetworkChain` (pure-read / pure-terminal / mixed); the hardcoded `manager.go` `filterRegistry`/`filterConstructor`/`filterHandler`/`buildTerminalFilter`/`listenerCtx` RETIRED + `netReg` intrinsic; the `internal/filter/network/builtins.RegisterBuiltins` seam; main.go boot-wired. The byte-stable unknown-type reject wording stays `"%s: unknown filter type_url %q"` (ADR-0080). **SIX GATES GREEN LIVE at Task 12** (run honestly, outputs quoted in PROGRESS.md): build/vet/lint/`-race -short` clean; differential R3 back-compat byte-exact (`0000-tcp-echo`/`0001-tcp-proxy-rr`/`0002-tls-tcp` tcp_proxy + `0003-http11-routing`/`0004-h2-routing` HCM + `0040`/`0041`/`0042` network-filter — ALL green; the sole differential non-green was a KNOWN environmental port-bind flake on the `0036-http-wasm-body-and-advanced` HTTP wasm fixture — `bind: address already in use` on a random ephemeral port — which PASSES on retry, NOT a migration regression); conformance 10/10 + h2spec 53/53 re-run LIVE (HCM now dispatches through the migrated path — NOT asserted-unaffected); fuzzers 35; fixtures 44; stat surface 132 — all +0 (migration is stat/fixture/fuzzer-NEUTRAL). **ADR-0215 §Decision/§Consequences bodies landed in place** (DECISIONS.md tail STAYS ADR-0215; no new ADR number consumed; next-free → ADR-0216). **BEHAVIOR_CONTRACT 26.2 framework-update block** added (TerminalFilter seam; `[read*, terminal?]` chain shape; unified single-dispatch; mixed-chain LIFT + new chain-shape rejects; the `filters[N]:` build-error index segment as an intended consequence). **Deferred cleanup recorded:** the now-dead `httpClient *httpclient.Client` pass-through param on `NewManagerWithBaseDirAndAllowH2C`/`buildListenerRuntimeWithCtx` (HCM gets httpClient via `builtins.Deps.HTTPClient`; removing it ripples cross-file → deferred per byte-exact-parity/avoid-churn discipline). The §9 Network-filters family stays OPEN (parent row 26 `in-progress` — flips at 26.3 per the 18/19/22/24/25 ROLLUP precedent; sub-row 26.2 `in-progress → done` at this phase-done commit; 26.3 `planned`). Next: 26.3 SPEC. **Earlier (PLAN stage, preserved):** phase 26.2 PLAN DONE at 2026-05-30 (squash `9a61788`) — the 12-task TDD PLAN; plan-document-reviewer APPROVED (1 MAJOR Task-7 unknown-type-url self-contradiction RESOLVED by deferring the unified unknown-type reject + 2-value collapse + `buildTerminalFilter` deletion to Task 10; 3 MINOR folded); ADR-0045 split-gate NO split; PLAN-time D-questions D-26.2-1..7 resolved (sealed `Marker`; FactoryCtx 4 primitives; `prefixConn` at 26.2; `builtins.RegisterBuiltins` import-cycle-forced placement). **Earlier (SPEC stage, preserved):** phase 26.2 SPEC DONE at 2026-05-30 (squash `7c19957`) — the per-sub-phase SPEC (16 §§) INHERITS the parent SPEC §5/§6/§7/§8/§11/§12/§13 rosters; RESOLVES the parent §3.2 terminal-fit D-question via the `TerminalFilter` seam over an OnData rewrite (REJECTED: HCM H1 driver + H2 ServerConn own the conn); ADR-0215 §Context drafted; spec-document-reviewer APPROVED. **Earlier (phase 26.1, preserved):** phase 26.1 (`network-filter-chain-framework-and-echo`) IMPL DONE at 2026-05-30 — the NEW `internal/filter/network/` read-filter chain framework (two-value `Status`; `ReadFilter`/`ReadFilterCallbacks`/`Connection`/`CloseType`; drainable `Buffer`; freeze-after-boot `*Registry`; per-connection runtime context owning the reused `*dynamicmetadata.Bucket` + RCD sink) + `echo` + `direct_response` first consumers + `manager.go` dual-dispatch (NOW retired at 26.2) + main.go boot-wiring + the 35th fuzzer + fixtures 0040/0041/0042. Echo TypeURL empirically corrected to the `extensions.` form. One CRITICAL sticky-halt bug found + fixed (echo multi-read re-iteration). ADR-0213 + ADR-0214 §Decision/§Consequences landed in place. Phases 00–25 + phase-26 parent SPEC + phase-26.1 + phase-26.2 DONE and CANONICAL.
- **phase-directory:** `docs/envoy-go/phases/28-network-filter-zookeeper-proxy/` (BRAINSTORM.md + parent SPEC.md — SPEC.md this commit; the 28.1 sub-phase directory `28.1-network-filter-write-seam-and-zookeeper-requests/` is created at the 28.1 SPEC session). The phase-27 directory `docs/envoy-go/phases/27-network-filter-sni-cluster/` (BRAINSTORM.md + SPEC.md + PLAN.md + PROGRESS.md — DONE), the phase-26.3 directory `docs/envoy-go/phases/26.3-network-filter-rbac/` (SPEC.md + PLAN.md + PROGRESS.md — DONE), the phase-26.2 directory (SPEC.md + PLAN.md + PROGRESS.md — DONE), the phase-26.1 directory (SPEC.md + PLAN.md + PROGRESS.md — DONE), and the parent directory `docs/envoy-go/phases/26-network-filter-chain-and-rbac/` (BRAINSTORM.md + parent SPEC.md — DONE) stay canonical.
- **lifecycle-state:** `phase 28 parent SPEC done (next = 28.1 SPEC per the per-sub-phase precedent)` — the parent SPEC (16 §§ + 1 appendix; D28-1..D28-10 pins resolved; AMEND-A1..A11; ADR-0221/0222/0223 §Context drafted; spec-document-reviewer APPROVED first iteration, 1 advisory folded in) authored at `docs/envoy-go/phases/28-network-filter-zookeeper-proxy/SPEC.md`. ROADMAP rows UNCHANGED at this commit: parent row 28 `in-progress`; sub-rows 28.1/28.2 `planned` (28.1 flips `planned → in-progress` at the 28.1 SPEC commit per the 26.x precedent / ADR-0106). The next lifecycle transition is the **28.1 SPEC** (`superpowers:writing-plans` scoped to the 28.1 sub-phase SPEC — `network-filter-write-seam-and-zookeeper-requests`). Phase 27 + phase 26 (parent + 26.1/26.2/26.3) are DONE and CANONICAL (preserved in the active-phase bullet above).
- **next-skill:** `superpowers:writing-plans` (scoped to **the 28.1 sub-phase SPEC** — `docs/envoy-go/phases/28.1-network-filter-write-seam-and-zookeeper-requests/SPEC.md` per the 22.1/25.1/26.1 per-sub-phase precedent; INHERITS the parent SPEC §11 empirical pins [no re-scrape; re-pin only the as-built line anchors + count baselines at its first-action gate]; refines parent §3.2(28.1)/§4 into the per-deliverable surface [the WriteFilter seam ADR-0221 + the zookeeperproxy request side ADR-0222 + fixtures 0046/0047 + the 37th fuzzer]; resolves the parent D-questions 28.1 owns — D-P1 [split-gate re-check; pre-authorized 28.1a/28.1b axis] / D-P2 [decoder depth + interface composition] / D-P3 [read-filter writes — anticipated NO] / D-P5 [creation parity — anticipated all-201] / D-P7 [writeChainConn return — anticipated (len(p), nil)] / D-P8 [name.go arm posture — anticipated shape-based]; ROADMAP sub-row 28.1 flips `planned → in-progress` at the 28.1 SPEC commit). Per `feedback_execution_style` the eventual IMPLs run subagent-driven; per `feedback_git_worktrees` work in a git worktree; subagents commit LOCAL-ONLY [`feedback_subagents_no_push`], the controller squash-merges + pushes at stage-close [`feedback_push_to_origin`]; per-task `gofmt -l` + `golangci-lint` on touched pkgs [`feedback_pertask_gofmt_lint`]. Counts at the phase-28 parent SPEC (unchanged from BRAINSTORM — docs only): fuzzers **36**, fixtures **47** [tail `0045-sni-cluster`], stat surface **136**; DECISIONS.md tail **ADR-0223** (§Context drafts; next-free **ADR-0224**).
- **last-commit:** `a532159` — `phase 28 parent SPEC: zookeeper_proxy + WriteFilter seam — D28-1..D28-10 pins resolved vs Envoy v1.37.2; 11 AMENDs; 2-way split formalized; ADR-0221/0222/0223 §Context drafted [ADR-0221,ADR-0222,ADR-0223]` (squash-merged from `phase-28-network-filter-zookeeper-proxy-spec`; docs only — SPEC.md + the DECISIONS.md ADR drafts + this STATE.md advance + next-prompt.txt). Substantive predecessor on master: the phase-28 BRAINSTORM squash `a410659` (trailed by the docs-only repoints `3f3b43d`/`0ccd392`). origin/master in sync after push.
- **last-updated:** 2026-06-01
- **next-free ADR:** `ADR-0224` (DECISIONS.md tail advances **ADR-0220 → ADR-0223** at this parent-SPEC commit — the ADR-0221 [WriteFilter seam; consumes the ADR-0213 API-revision allowance] / ADR-0222 [zookeeper_proxy request side] / ADR-0223 [response side + latency-threshold counters] §Context drafts are ALL THREE landed here per ADR-0044; §Decision/§Consequences bodies land at the respective IMPLs — 0221/0222 at 28.1, 0223 at 28.2; no further phase-28 ADR numbers anticipated). ADR-0209 escape-valve reserve carried from the §9 family STANDS-UNCONSUMED.

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
