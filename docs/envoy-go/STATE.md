# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `22-http-filter-lua` **(parent row DONE at this commit — final sub-phase 22.3 IMPL done)**; sub-phase `22.1-http-filter-lua-vm-and-headers-bridge` done at master tip `c986419`; sub-phase `22.2-http-filter-lua-full-bridge` done at master tip `46183a4`; sub-phase `22.3-http-filter-lua-multi-script-and-per-route` **IMPL done at this commit** (closes parent row 22). Phase `22-http-filter-lua` parent BRAINSTORM done at parent BRAINSTORM commit + parent SPEC done at `41ccee7`; 22.1 lifecycle traversed BRAINSTORM (parent) → SPEC at `a7021aa` → PLAN at `02d745a` → IMPL at `d30f131` → done. Phase `22.2` lifecycle traversed BRAINSTORM at `6ad3064` → SPEC at `0d6463e` → PLAN at `269dee1` → IMPL at `46183a4` → done. Phase `22.3` lifecycle traversed BRAINSTORM done at `0929b3d` → SPEC done at `e72af4c` → PLAN done at `1efd4de` → **IMPL done at THIS commit** per BOOTSTRAP §4.1 invariant 1. **22.3 IMPL done at 2026-05-21** — 7-task landing (Task 0 precondition-verify + Tasks 1-6) realizing the SPEC §15 ~16-item acceptance checklist, subagent-driven per project memory `feedback_execution_style.md`. **CONSUME + DISPATCH delta: 0 new framework primitives, 0 net-new stats (count STAYS 107), 0 net-new bridge methods, 0 net-new BEHAVIOR_CONTRACT departure records (count STAYS 14 — all 22.3 dispositions upstream-parity).** Task 1 consumed `Lua.SourceCodes` into a `cc.sourceCodes map[string]*internallua.Chunk` content-hash registry (sorted-key iteration, dedup into the shared per-listener `CompileCache`) + the `source-codes-key-empty` arm (the arm-4 `source_codes`-deferred reject DROPPED). Task 2 landed NEW `internal/filter/http/lua/perroute.go` `parsePerRouteLua` real 3-arm validator (oneof-required / disabled-must-be-true / name-min-1 / source_code-gauntlet) REPLACING the arm-18 one-liner. Task 3 landed the per-route 3-tier dispatch `resolveDecodeScript` (default / name→registry-with-dangling-name-silent-no-op / source_code→memo / disabled→skip) + decode/encode wiring + the encode-guard fix (gate on `f.vm==nil`, the `f.cc.chunk==nil` clause DROPPED). **D-P1(b') REALIZED with a no-re-read correction:** the dispatch switches the `GetOverride()` oneof DIRECTLY (does NOT call `parsePerRouteLua` per stream) so the per-route `source_code` `Filename` DataSource is read ONCE per route via the `cc.perRouteChunks` memo, never re-read per stream — the PLAN's literal "call parsePerRouteLua at dispatch" wording was the error (a read-counting test surfaced it; see REVIEW.md + PROGRESS.md Task 3). Task 4 landed `FuzzLuaPerRouteConfig` (**fuzzer 30 → 31**) + `FuzzLuaConfigParse` source_codes corpus + `BenchmarkPerStream_PerRoute_Resolution`. **R6 disposition: anticipated WEAK-default STANDS** — resolution-only 10.46 ns/op (0 allocs) + per-stream 31.47 ns/op, both ~5 orders of magnitude under the 1ms gate; **conditional ADR-0194 does NOT fire; next-free ADR STAYS ADR-0194.** Task 5 landed TWO differential fixtures (**AUTHORIZED two-directory amendment, fixtures 29 → 31, NOT the pre-IMPL "30" anticipation**): `0028-http-lua-multi-script-and-per-route` (cross-side multi-listener, 5 per-route selection scenarios + dangling-name no-op) + `0029-http-lua-source-codes-boot-reject` (boot-reject, source_codes compile-error, common stderr substring `near '-'`) — the framework dispatches one branch per fixture directory (cross-side XOR boot-reject), so the PLAN's single-fixture "5 cross-side + 3 boot-reject" split into two dirs; subject-only (f) key-empty + (h) per-route source_code-failure remain covered at the unit layer. Task 6 (atomic landing) landed ADR-0193 §Decision + §Consequences + the ADR-0125 §(xiv) IN-PLACE AMENDMENT body (canonical roster 8 → 9) + the BEHAVIOR_CONTRACT.md edit bundle + doc.go + STATE/ROADMAP (parent-row-22 closure) + REVIEW.md. **AMEND-22.3-1: dangling per-route `name` is an upstream-parity SILENT NO-OP** (arms 3 + 7 DROPPED). All green gates GREEN at Task 6 (build/vet/lint clean; non-differential test suite ok; `-race ./internal/filter/http/lua/` clean; lua differential fixtures 0026/0027/0028/0029 GREEN in isolation — combined-run failures are the documented pre-existing `freeTCPPort` multi-listener port-allocation flake per 22.2 REVIEW §7.4, never a byte-divergence or compile defect; `cmd/envoy-go/main.go` + `test/differential/fixture/fixture.go` ZERO delta vs base `04eba88`). Predecessor: **22.3 PLAN done at `1efd4de`** (7-task breakdown; D-P1 b' / D-P2 two-dir / D-P3 6-arm-groups / R6 WEAK-default / ADR-0045 STAY-SINGLE-PHASE resolutions).
- **phase-directory:** `docs/envoy-go/phases/22-http-filter-lua/` (parent — BRAINSTORM.md + SPEC.md authored at parent SPEC commit; **parent phase row DONE at this commit** — the final sub-phase 22.3 IMPL closes it per parent SPEC §1 closure pattern). Sub-phase directories: `docs/envoy-go/phases/22.1-http-filter-lua-vm-and-headers-bridge/` (SPEC.md + PLAN.md + PROGRESS.md + REVIEW.md from 22.1 lifecycle; LANDED at master tip `c986419`) + `docs/envoy-go/phases/22.2-http-filter-lua-full-bridge/` (SPEC.md + PLAN.md + PROGRESS.md + REVIEW.md + BRAINSTORM.md from 22.2 lifecycle; LANDED at master tip `46183a4`) + `docs/envoy-go/phases/22.3-http-filter-lua-multi-script-and-per-route/` (BRAINSTORM.md at `0929b3d` + SPEC.md at `e72af4c` + PLAN.md at `1efd4de` + **PROGRESS.md + REVIEW.md authored at THIS IMPL commit**). REVIEW.md (authored at THIS commit per `superpowers:requesting-code-review`) covers the per-task review (Tasks 0-6) + the SPEC §15 ~16-item acceptance-checklist closure + the TWO PLAN divergences (Task 3 D-P1(b') dispatch correction + Task 5 two-directory fixture amendment). PROGRESS.md has the per-task evidence (Tasks 0-6) quoting command outputs per `superpowers:verification-before-completion`.
- **lifecycle-state:** `phase 22.3 IMPL done; phase 22 (parent) done; awaiting next-phase BRAINSTORM`. ROADMAP row `22.3` flips `in-progress → done` at THIS commit per ADR-0106 per-cell IMPL-done annotation (date 2026-05-21); **parent row `22` flips `in-progress → done` at THIS commit** (22.3 is the final sub-phase; all three sub-rows 22.1 + 22.2 + 22.3 done). The §9 HTTP-filters family closes from 4 remaining rows (post-phase-21) to 3 remaining: `wasm`, `admission_control`, `global rate limit`. **DECISIONS.md tail at ADR-0193 full body** (§Decision + §Consequences landed at THIS commit per ADR-0044 in-place edit discipline; §Context UNCHANGED from the SPEC anchor `e72af4c`) + the **ADR-0125 §(xiv) IN-PLACE AMENDMENT body landed** at THIS commit (canonical roster 8 → 9; no new ADR number). ADR-0188 + ADR-0189 + ADR-0190 + ADR-0191 + ADR-0192 + ADR-0177 UNCHANGED. **Conditional ADR-0194 NOT consumed** (R6 WEAK-default STANDS — `BenchmarkPerStream_PerRoute_Resolution` resolution-only 10.46 ns/op + per-stream 31.47 ns/op, both ~5 orders of magnitude under the 1ms gate). **Stat count STAYS 107. 17 HTTP filters UNCHANGED. BEHAVIOR_CONTRACT departure-record count STAYS 14** (3 from 22.1 + 11 from 22.2; 22.3 adds 0 — all upstream-parity). **Fuzzer count 30 → 31** (NEW `FuzzLuaPerRouteConfig`). **Differential fixtures 29 → 31** — NOTE: this is the AUTHORIZED two-directory amendment (`0028-http-lua-multi-script-and-per-route` cross-side + `0029-http-lua-source-codes-boot-reject` boot-reject), NOT the "30" the pre-IMPL STATE anticipated; the differential framework dispatches one branch per fixture directory (cross-side XOR boot-reject), so the SPEC's single-fixture "5 cross-side + 3 boot-reject" split into two dirs.
- **next-skill:** `superpowers:brainstorming` (scoped to the NEXT §9 phase per SKILL_ROUTING — parent row 22 is CLOSED; the §9 HTTP-filters family has 3 remaining rows `wasm` / `admission_control` / `global rate limit`, none yet brainstormed). Cold-start scope for the next session: read STATE.md (this file) + `docs/envoy-go/ROADMAP.md` (rows 22.1 + 22.2 + 22.3 done; parent row 22 done; the §9 family-expansion section §"HTTP filters family" for the 3 remaining rows) + `docs/envoy-go/BOOTSTRAP_PROMPT.md` §9 family-expansion discipline (each next filter becomes its own brainstormed phase; ADR-0106 flat top-level rows) + DECISIONS.md tail (ADR-0193 full body + ADR-0125 §(xiv) amended; next-free ADR-0194; ADR-0044 §Context-draft discipline for the next phase's anticipated ADRs) + the predecessor §9 filter precedents (`docs/envoy-go/phases/22.3-http-filter-lua-multi-script-and-per-route/{SPEC.md,PLAN.md,PROGRESS.md,REVIEW.md}` for the most-recent IMPL structural precedent) + BEHAVIOR_CONTRACT.md `### envoy.filters.http.lua` (the now-complete 22.1 + 22.2 + 22.3 subsections, for cross-filter pattern reference). The next-phase BRAINSTORM session creates a fresh worktree per project memory `feedback_git_worktrees.md`. **Per project memory `feedback_push_to_origin.md`: after the 22.3 IMPL clean squash-merge to master with tests green, push to origin without asking.**
- **last-commit:** `TBD → <squash-SHA> post-squash`. Phase 22.3 IMPL squash-merge of branch `phase-22.3-http-filter-lua-multi-script-and-per-route-impl` from worktree `.worktrees/phase-22.3-http-filter-lua-multi-script-and-per-route-impl/` lands as the squash-merge commit on master per the phase-09..22.2 IMPL squash-merge convention. This placeholder is backfilled by the SHA-fill follow-up commit per the phase-09..22.2 convention: `phase 22.3 IMPL follow-up: STATE.md SHA-fill (TBD → <squash-SHA> post-squash)` (the controller does the squash + SHA-fill). Predecessor master tip: `04eba88` — `phase 22.3 PLAN follow-up: STATE.md SHA-fill (TBD → 1efd4de post-squash)`. Pre-predecessor: `1efd4de` — `Squash merge phase-22.3-http-filter-lua-multi-script-and-per-route-plan`.
- **last-updated:** 2026-05-21
- **next-free ADR:** `ADR-0194` — UNCHANGED at this 22.3 IMPL commit. **R6 WEAK-default STANDS; the conditional ADR-0194 escape-valve was UNCONSUMED** — `BenchmarkPerStream_PerRoute_Resolution` resolution-only 10.46 ns/op (0 allocs) + per-stream 31.47 ns/op, both ~5 orders of magnitude under the 1ms (1,000,000 ns) gate; per-route resolution is an O(1) `Resolve` + content-hash + proto-pointer-memo lookup, not a new per-stream VM construction. **ADR-0193 §Decision + §Consequences body LANDED at THIS commit** (NEW combined `internal/filter/http/lua/` 22.3 multi-script + per-route surface: `Lua.SourceCodes` named-script content-hash registry consume + `LuaPerRoute` 9th-canonical 3-arm validator + per-route 3-tier dispatch with the D-P1(b') no-re-read proto-pointer memo + the encode-guard fix + **upstream-parity dangling-`name` silent no-op per AMEND-22.3-1** + no-reserved-name disposition + the D-P2 two-directory fixture split + R6 WEAK-default; ONE ADR per the 22.1 one-package-shape-ADR economy ADR-0189; §Context UNCHANGED from the SPEC anchor `e72af4c`). **ADR-0125 §(xiv) IN-PLACE AMENDMENT body LANDED at THIS commit** (canonical-per-route roster grows 8 → 9: the 9th canonical [3-arm hybrid: disabled-bool + string-reference-delegation + DataSource-wholesale-override] + SHARED stat-discipline + the 9-shape roster table + the lua-row first-use citation referencing ADR-0193; NO new ADR number). **ADR-0188 EXPLICIT API-REVISION ALLOWANCE clause STAYS scoped to consumer-#2** (UNCHANGED at 22.3 — per-route + multi-script are dispatch-surface extensions, not `internal/lua/` VM-API revisions). Parent row 22 CLOSED; no successor sub-phase to carry the WEAK HOLD escape-valve buffer — ADR-0194 STAYS next-free for the next §9 phase.

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
