# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `phase 36.2 (load-balancer-ring-hash-http) SPEC done` (2026-06-12, this commit) — the phase-36.2 SPEC is AUTHORED (the HTTP route `hash_policy` producer plane — the SECOND producer feeding the ALREADY-LANDED ring_hash seam ADR-0235 + policy ADR-0236; `docs/envoy-go/phases/36.2-load-balancer-ring-hash-http/SPEC.md`). The SPEC settles: SUPPORT `header` (`xxHash64(value)`) + `connection_properties.source_ip` (reuse the LANDED `cluster.HashSourceIP` — byte-identical to the tcp plane); DEPARTURE-reject `cookie`/`query_parameter`/`filter_state` + `regex_rewrite` + `source_ip==false` (the reference validate-ACCEPTS them → recorded departures, fail-fast at config-build); the multi-policy fold `rotl64(prev,1)^new` (first contributor verbatim, nullopt-skip, `terminal` short-circuit on a non-empty accumulator); the per-request compute threaded via the LANDED `cluster.WithHashKey` at the FOUR router dial sites (H1 `doH1ClusterAction`/`do`, H2 `doH2ClusterAction`/`doH2` — the helper is codec-agnostic: H1 `*http.Request` vs H2 `h2.H2Request` hpack scan + ctx-carried remote addr). **SPEC empirical-pin block (FRESH in-session 2026-06-12):** the `RouteAction.HashPolicy` roster vs the ACTUAL v1.32.4 module; the v1.37.2 source (combine + terminal + the header SEED-CHAINED `XXH64` fold — REFINING the parent's loose "xxHash64(sorted values)"); a 7-variant live `--mode validate` matrix; a live HTTP header-hash affinity/stats run (ZERO new stat names; `upstream_rq_total` cross-equal on the HTTP plane, unlike tcp). **Counts UNCHANGED at the SPEC** (they advance at the 36.2 IMPL): stat surface **1119** (the 36.2 plane adds 0 names); fixtures **63** (→ 64 at the IMPL [`0062-lb-ring-hash-http`], a NAT-transparent TRUE cross-side affinity differential); fuzzers **42**; BackendKind **33**; DECISIONS tail **ADR-0236** (the ADR-0237 §Context is a DRAFT in SPEC §13; the full entry lands at the 36.2 IMPL per ADR-0044). 36.2 is a PRODUCER-ONLY leg (NO LB policy / seam / manager / package / go.mod change; ≤1 additive `cluster.HashHeaderValues` helper). The spec-document-reviewer gate ran (PASS; the H1/H2 helper asymmetry + 3 precision fixes folded in).
- **phase-directory:** `docs/envoy-go/phases/36.2-load-balancer-ring-hash-http/` (**SPEC.md** — the durable 36.2 sub-phase SPEC, landed THIS commit per the 32.1/32.2/29.1 per-sub-phase-dir precedent; the PLAN.md follows at the 36.2 PLAN session, the PROGRESS.md at the 36.2 IMPL session [Task 1 creates it]). The parent `docs/envoy-go/phases/36-load-balancer-ring-hash/` (**BRAINSTORM.md + SPEC.md + PLAN.md + README.md + PROGRESS.md** — the durable phase-36 parent BRAINSTORM/SPEC [both planes] + the 36.1 IMPL record; the 36.1/36.2 split is CONSUMED [parent SPEC §3.0], 36.1 LANDED). **Superseded (the phase-35 phase-directory, preserved):** `docs/envoy-go/phases/35-load-balancer-random/` (**BRAINSTORM.md + SPEC.md + README.md + PLAN.md** — PLAN.md landed THIS commit (README.md status advanced 2 → 3); the PROGRESS.md follows at the IMPL session [Task 1 creates it], per the kafka-31/thrift-33/least_request-34 single-flat-row per-phase-dir precedent). The phase-34 directory `docs/envoy-go/phases/34-load-balancer-least-request/` (**BRAINSTORM.md + SPEC.md + README.md + PLAN.md + PROGRESS.md** — the durable phase-34 IMPL record). The phase-33 directory `docs/envoy-go/phases/33-network-filter-thrift-proxy/` (**BRAINSTORM.md + SPEC.md + README.md + PLAN.md + PROGRESS.md** — the durable phase-33 IMPL record). The phase-32 dirs `docs/envoy-go/phases/32-network-filter-redis-proxy/` + `32.1-…/` + `32.2-network-filter-redis-commands-and-stats/` (**SPEC.md** — the durable phase-32.2 sub-phase SPEC; the PLAN.md follows at the 32.2 PLAN session, the PROGRESS.md at the 32.2 IMPL session [Task 1 creates it], per the 29.1/29.2/29.3 per-sub-phase-dir precedent). The phase-32.1 dir `docs/envoy-go/phases/32.1-network-filter-redis-upstream-pool-seam-and-codec/` (**SPEC.md + PLAN.md + PROGRESS.md** — the durable phase-32.1 IMPL record). The parent `docs/envoy-go/phases/32-network-filter-redis-proxy/` (**BRAINSTORM.md + SPEC.md + README.md** — the durable phase-32 parent-SPEC record). The phase-31 directory `docs/envoy-go/phases/31-network-filter-kafka-broker/` (BRAINSTORM.md + SPEC.md + README.md + PLAN.md + PROGRESS.md — the durable phase-31 IMPL record). The phase-30 + phase-29 directories stay the durable phase-30/29 record.
- **lifecycle-state:** `phase 36.2 SPEC done — next: the 36.2 PLAN (superpowers:writing-plans — decompose SPEC §10's ~6–8-task spine; the ADR-0045 gate re-check, anticipated NO split [~150 prod LoC]); then its own IMPL adding 0062-lb-ring-hash-http`. The SPEC (`docs/envoy-go/phases/36.2-load-balancer-ring-hash-http/SPEC.md`) is the PLAN input: §3 the producer wiring (config-build parse + DEPARTURE-reject; the per-request `applyHashKey` fold at the 4 dial sites), §5 the proto roster, §6 the reject roster, §7 stats (0 new), §8 the `0062` fixture, §10 the task spine, §11 the empirical-pin block, §12 the PLAN/IMPL D-questions (the H1/H2 helper asymmetry D-S362-3; the header-hash exported surface D-S362-1; the conformance-gate disposition D-S362-7), §13 the ADR-0237 §Context draft. No separate 36.2 BRAINSTORM (the parent phase-36 BRAINSTORM covered both planes; 36.2 is producer wiring against a landed seam). The controller squash-merges this 36.2 SPEC + pushes at stage-close.
- **next-skill:** the **36.2 PLAN** (`superpowers:writing-plans` — the SPEC §10 spine into bite-sized TDD tasks: the header-hash helper, the config-build parse+reject, the per-request fold + the 4 dial-site `WithHashKey` rebinds, the `0062-lb-ring-hash-http` fixture, the deliberate-breaks, the full re-verify + the HTTP-path-touch conformance rationale, the completion bundle with ADR-0237).
- **last-commit:** the phase-36.2 SPEC authoring bundle — the durable `docs/envoy-go/phases/36.2-load-balancer-ring-hash-http/SPEC.md` (the HTTP route hash_policy producer plane; the FRESH in-session empirical-pin block + the spec-document-reviewer-gate fixes) + STATE/ROADMAP row-36 advance (the 36.2 SPEC authored; counts unchanged). The prior commit was the phase-36.1 IMPL completion bundle.
- **last-updated:** 2026-06-12 (phase-36.2 SPEC done)
- **next-free ADR:** `ADR-0237` (DECISIONS.md tail STAYS **ADR-0236** at this SPEC — the ADR-0237 [the HTTP route `hash_policy` producer plane] §Context is a DRAFT in SPEC §13; the full entry [§Context + §Decision + §Consequences] lands at the 36.2 IMPL per ADR-0044).

---

## Project counts at this commit

- **active differential fixtures:** **63** (tail `0061-lb-ring-hash`). Canonical recipe `ls -d test/fixtures/[0-9]* | wc -l` = 63. Advanced at the phase-36.1 IMPL (+1 — `0061-lb-ring-hash`, the tcp source_ip subject-side affinity+spread differential; 36.2 will add `0062-lb-ring-hash-http`).
- **fuzzers:** **42** (canonical recipe `grep -rh "^func Fuzz" $(find ./internal -name fuzz_test.go) | wc -l`; tail `FuzzThriftDecode`). UNCHANGED at the phase-36.1 IMPL (+0 — the THIRD no-fuzzer phase; ring_hash decodes no untrusted wire bytes — the hash key derives from a source IP / parsed value, not a wire frame; a ring-lookup property test is unit-level).
- **stat surface:** **1119** (BEHAVIOR_CONTRACT doc count). Advanced at the phase-36.1 IMPL (1116 → **1119**, +3 — the 3 MIRRORED `cluster.<name>.ring_hash_lb.{size,min_hashes_per_host,max_hashes_per_host}` GAUGES; the FIRST non-zero LB-stat delta — least_request-34 + random-35 were both zero-delta phases; cross-side-exact, keyed only on ring-config + host count, NOT endpoint addresses; registered ONLY on the RING_HASH path; the DOC count is the source-of-truth, no programmatic golden).
- **BackendKind tail:** **33** (`TCPThriftResponder` — seq_id-agnostic canned framed-binary REPLY). UNCHANGED at the phase-36.1 IMPL (+0 — `0061` reuses `TCPEcho` 0; an LB phase exercises WHERE conns land, not a new backend).
- **DECISIONS.md tail:** **ADR-0236** (next-free **ADR-0237**) — TWO ADRs landed at the phase-36.1 IMPL: **ADR-0235** (the LB hash-key seam extension — the ADR-0232 PICK-INPUT half; the ctx-carried hash key; the seam's FIRST extension) + **ADR-0236** (the `ring_hash` load-balancing policy — the Ketama consistent-hash ring; the project's FOURTH LB policy + FIRST consistent-hashing policy), both PROPOSED → ACCEPTED in-place per ADR-0044 (the redis-32 / least_request-34 two-ADR shape, since the seam is EXTENDED not merely reused).
- **reference pin:** `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227, superseding ADR-0008).
- **conformance:** h2spec 53/53; proxy-wasm all 10 families PASS (image-independent; UNAFFECTED by phase 36.1 — touches only `internal/cluster` [the LB seam + ring_hash policy] + `internal/filter/tcpproxy` [the source_ip plane]; no HTTP/h2/proxy-wasm path).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
