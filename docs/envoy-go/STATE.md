# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `05.1-downstream-h2`
- **phase-directory:** `docs/envoy-go/phases/05.1-downstream-h2/` — contains `README.md` (placeholder, written at split time per ADR-0045 / commit `2162509`) and `SPEC.md` (drafted by the brainstorming session per ADR-0004 autonomous mode; commit `4b45941`). The sibling `docs/envoy-go/phases/05.2-upstream-h2/` is unchanged from the split commit (placeholder `README.md` only). The parent `docs/envoy-go/phases/05-http-2/` retains its `SPEC.md` (`612cdea`) as the master design document for both sub-phases per ADR-0045 and phase-05 SPEC §11.1; that directory remains closed read-only history at the parent level (no PLAN/PROGRESS/REVIEW will land under it; those land under the sub-phase directories).
- **lifecycle-state:** `2` — phase 05.1's `SPEC.md` exists; `PLAN.md` does not. Per `BOOTSTRAP_PROMPT.md` §5 state 2, the next session invokes `superpowers:writing-plans` scoped to phase 05.1 and produces `phases/05.1-downstream-h2/PLAN.md`. The §5 / §6.1 split GATE (`PLAN.md > ~25 tasks OR > ~1500 LoC`) is anticipated **not** to trip per the SPEC's own §11.1 estimate (~12–15 TDD tasks, ~1700 LoC — under the task gate, at the LoC gate); if it does trip, the split-by-codec-direction axis is exhausted (the master phase-05 SPEC §11.1 already split the only meaningful axis per ADR-0045), so the planner must escalate to `blocked` per `BOOTSTRAP_PROMPT.md` §6 rather than re-split. The SPEC was reviewed by the `spec-document-reviewer` subagent per ADR-0004 step 3 and approved on iteration 2 (one issue found in iteration 1 — a contradiction on routerAction-on-H2 enforcement across SPEC §2.2 / §4.2 / §5.2 step 4c — fixed before iteration 2; iteration 2 returned Approved with one minor attribution-slip recommendation, also fixed in §5.2 step 4c referencing ADR-0045 instead of ADR-Q). ROADMAP row 05.1 was flipped `planned` → `in-progress` in the same commit as the SPEC draft per the corrected pattern from phase 05's `8d18320` (here applied at brainstorming time per the directory-already-exists case noted in this STATE.md's prior lifecycle-state-1 line).
- **next-skill:** `superpowers:writing-plans`
- **next-skill-scope:** Write `PLAN.md` for phase 05.1 against the SPEC at `docs/envoy-go/phases/05.1-downstream-h2/SPEC.md` (commit `4b45941`). The PLAN's eight new ADRs (the SPEC's lettered placeholders ADR-P/Q/S/T/U/V/Z/X) are assigned concrete numbers at PLAN write time per ADR-0045's "ADR numbering shift" consequence — next-free is `ADR-0046` (planner verifies at write time by inspecting the `DECISIONS.md` tail). The eight letters land at ADR-0046..ADR-0053 in the order they're authored. The PLAN re-runs the size estimate at write time and applies the §6.1 split GATE; per the SPEC's §11.1 risk analysis the gate is expected to hold but if it trips the planner exits blocked per `BOOTSTRAP_PROMPT.md` §6 (no further split axis is available — see "lifecycle-state" line above). Per ADR-0003 + the project's per-phase-worktree convention (see prior `phase/0X-Y-spec`/`-plan`/`-impl` branch series), planning runs from a fresh `phase/05.1-downstream-h2-plan` worktree branched from master tip after this commit's fast-forward; the current `.worktrees/phase-05.1-downstream-h2-spec` is closed-history at this state transition. Per ADR-0005 the PLAN is reviewed by the `plan-document-reviewer` subagent (≤3 iterations or escalate to `blocked` per ADR-0005 step 3). Reading order for the PLAN session (cold-start Step C): `phases/05.1-downstream-h2/SPEC.md` (the design source), `phases/05-http-2/SPEC.md` (the master SPEC the 05.1 SPEC slices — used to disambiguate edge cases the 05.1 SPEC defers to "the planner"), `phases/04-http-1.1/PLAN.md` (the most recent prior PLAN, for shape reference), `phases/04-http-1.1/REVIEW.md` (commit `04527eb`, source of the M-2/M-4/M-5/M-6/M-7 carry-forwards triaged in 05.1 SPEC §12).
- **last-commit:** `4b45941` — the SPEC-draft commit on branch `phase/05.1-downstream-h2-spec` (added `docs/envoy-go/phases/05.1-downstream-h2/SPEC.md` 524 lines; flipped `ROADMAP.md` row 05.1 `planned` → `in-progress`). This STATE.md commit (the lifecycle-transition commit that promotes lifecycle-state from 1 to 2 and points `last-commit` at `4b45941`) is the second and final commit on the `phase/05.1-downstream-h2-spec` branch. Master is fast-forwarded to this STATE.md commit per ADR-0003.
- **last-updated:** 2026-04-25

---

## Lifecycle cross-reference

See `SKILL_ROUTING.md` (and `BOOTSTRAP_PROMPT.md` §5) for the full state machine. The `lifecycle-state` field above maps to numbered states 0–6 of that machine. The `next-skill` field is the skill to invoke at the next session's Step D.

## Exit contract for every session

Before exiting, a session MUST:

1. Update `active-phase`, `phase-directory`, `lifecycle-state`, `next-skill`, `next-skill-scope`, `last-commit`, `last-updated` above to reflect the new state.
2. Ensure the repository has no uncommitted state-affecting changes (scaffold, SPEC, PLAN, PROGRESS, REVIEW, or code changes are all either committed or deliberately abandoned per §6 splitting).
3. If exiting due to `blocked` or an ADR'd deviation, set `lifecycle-state` to `blocked` and add a short `block-reason` line below the list.
