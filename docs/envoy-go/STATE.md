# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `(none — phase 18.2 + phase 18 both done at this commit; awaiting next §9 family-row brainstorm)` — phase 18.2 IMPL session COMPLETE (state-4 entry per ADR-0005 §Decision 4) AND parent row 18 closed at the SAME commit per parent SPEC §8 parent-rollup discipline. The 14-task phase-18.2 PLAN executed in full: Tasks 1–12 authored the `internal/grpcclient/` framework primitive + the gRPC-mode extensions to `internal/filter/http/extauthz/` + the 6 new `envoyhttp.DecoderFilterCallbacks` methods (per ADR-0165 — the ADR-0044 escape-valve fired at planner-time D3 + D12) + differential fixture `0021-http-ext-authz-grpc` (8 scenarios; three-listener topology; 22/22 differential fixtures green) + 23rd fuzzer `FuzzCheckResponseMapping` + ADR-0166 (unanticipated cluster-manager amendment landed as Task 11.5 fixup between Tasks 11 and 12 — plaintext h2c upstream relaxation; ADR-0044 escape-valve fired SECOND time at IMPL-fixup time); Task 13 (this commit) applied the BEHAVIOR_CONTRACT.md 8-patch bundle + flipped ROADMAP rows 18.2 AND 18 BOTH to `done` at the SAME commit + advanced STATE.md + ran the 6-gate phase-done verification (all 6 gates GREEN). Task 14 (REVIEW.md) is the final task of this IMPL session per `superpowers:requesting-code-review`. **5 ADRs anchored at 18.2 IMPL** (ADR-0158 full §Decision + §Consequences — gRPC-client framework primitive; ADR-0157 §Decision AMENDMENT in-place at Task 3 — `grpc_service` arm activation; ADR-0160 gRPC-mode portion at Task 5; ADR-0161 gRPC-mode portion at Task 6; **ADR-0165** callback-surface extension at Task 4; **ADR-0166** plaintext h2c upstream relaxation at Task 11.5). The next lifecycle session is the next §9 HTTP-filters family-row BRAINSTORM (the family list at `BOOTSTRAP_PROMPT.md` §9 enumerates the remaining HTTP filters — `ext_proc` / `oauth2` / `lua` / `wasm` / `adaptive concurrency` / `admission control` / `global rate limit` — each is its own coherent phase per ADR-0106).
- **phase-directory:** `docs/envoy-go/phases/18-http-filter-ext-authz/` (BRAINSTORM.md + parent master SPEC.md) + `docs/envoy-go/phases/18.1-ext-authz-http/` (SPEC.md + PLAN.md + PROGRESS.md + REVIEW.md; fully closed) + `docs/envoy-go/phases/18.2-ext-authz-grpc/` (SPEC.md + PLAN.md + PROGRESS.md; REVIEW.md authored at Task 14 — final task of this IMPL session).
- **lifecycle-state:** `phase 18.2 done; phase 18 done; phase <next> BRAINSTORM pending` — phase 18.2 IMPL is complete; all 6 phase-done gates passed; ROADMAP rows 18.2 AND 18 both flipped `done` AT THE SAME COMMIT per parent SPEC §8 parent-rollup discipline; next-free ADR is ADR-0167 (since BOTH ADR-0165 AND ADR-0166 fired at 18.2 IMPL — the ADR-0044 escape-valve fired TWICE in this phase). The next session is a BRAINSTORM session for the next §9 HTTP-filters family-row per `superpowers:brainstorming` cadence — ROADMAP currently has no row 19, so the next planning session opens with a fresh §9 family-row selection (the family-row to BRAINSTORM is the operator's choice from the remaining HTTP-filters list: `ext_proc` / `oauth2` / `lua` / `wasm` / `adaptive concurrency` / `admission control` / `global rate limit`).
- **next-skill:** `superpowers:brainstorming` — phase 18.2 + phase 18 are done. The next session is a BRAINSTORM session for the next §9 HTTP-filters family-row per `superpowers:brainstorming`; the cold-start prompt for that session selects the next HTTP-filter from the remaining roster. Cold-start scope for the next BRAINSTORM session: read STATE.md (this file); `docs/envoy-go/ROADMAP.md` (rows 18 + 18.2 + 18.1 all `done`; no row 19 yet — the BRAINSTORM session adds the next §9 family-row); `docs/envoy-go/MISSION.md`; `docs/envoy-go/BOOTSTRAP_PROMPT.md` §9 (the HTTP-filters family roster); `docs/envoy-go/DECISIONS.md` (tail at ADR-0166; next-free ADR-0167; the 11 ADRs anchored across the phase-18 family — ADR-0156..ADR-0166 — provide the §9 framework precedents); `docs/envoy-go/BEHAVIOR_CONTRACT.md` (11 HTTP filters documented through phase 18.2; HCM stat-table at 77 names; FOUR framework-primitive sections — `## HTTPFilterCallbacks` (now with 6 new methods per ADR-0165), `## Matcher engine framework primitive`, `## JWKS framework primitive`, `## JWT verifier framework primitive`, `## gRPC client framework primitive` (NEW at 18.2), `## HTTP outbound auth-check framework note`); `docs/envoy-go/SKILL_ROUTING.md`; `docs/envoy-go/phases/18.2-ext-authz-grpc/REVIEW.md` (phase-18.2 end-of-phase review — deliverables roster + ADR dispositions + §15 acceptance-checklist verification — authored at Task 14 of this IMPL session). The phase-18.2 framework deltas available to future filters: `internal/grpcclient/` (compose for ext_proc + global_ratelimit + future gRPC-family filters); the 6 new `DecoderFilterCallbacks` methods (`DownstreamRemoteAddr` / `DownstreamLocalAddr` / `DownstreamTLSServerName` / `DownstreamTLSPeerCertDER` / `DownstreamProtocol` / `ListenerPrincipal`; consume for ext_proc + global_ratelimit + any filter needing socket-address / TLS-connection / listener-cert introspection); the cluster-manager plaintext h2c upstream dial path (per ADR-0166; consume for any H2-upstream-only filter or gRPC-family filter wanting a plaintext upstream).
- **last-commit:** `<TBD — phase 18.2 Task 13 squash-merge SHA; filled by the SHA-fill follow-up commit per the phase-09..18.1 IMPL-stage close pattern>`. The phase-18.2 IMPL worktree `phase-18.2-ext-authz-grpc-impl` (branched off master tip `6226d11` — `phase 18.2 PLAN follow-up: STATE.md SHA-fill`) carries the 13 task commits (Tasks 1–13) + the Task-11.5 fixup commit set (5 sub-commits for Task 11.5 per the §11.5 fixup convention) + the various task review-fix follow-ups; squash-merged to master as one IMPL commit at this Task 13 commit + a SHA-fill follow-up commit per the phase-09..18.1 precedent.
- **last-updated:** 2026-05-15
- **next-free ADR:** `ADR-0167` — phase 18.2 IMPL landed 5 ADRs (ADR-0158 full body, ADR-0157 §Decision AMENDMENT, ADR-0160 gRPC-mode portion, ADR-0161 gRPC-mode portion, ADR-0165 (NEW), ADR-0166 (NEW)). **ADR-0044 escape-valve fired TWICE** at 18.2 — once at PLAN time (planner-time D3 + D12 → ADR-0165 callback-surface extension) + once at IMPL-fixup time (Task 11.5 → ADR-0166 plaintext h2c upstream relaxation; an unanticipated cluster-manager amendment surfaced by the fixture-0021 three-listener topology requirements). The ADR tail at this commit is `ADR-0166`; next-free advances to `ADR-0167`. ADR-0125 8-canonical roster UNCHANGED (phase 18 REUSES the 5th canonical; no amendment). The next phase opens with `next-free: ADR-0167`.

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
