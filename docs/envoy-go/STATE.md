# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `18.2-ext-authz-grpc` — phase 18.2 PLAN session COMPLETE (state-3 entry per ADR-0005 §Decision 4). This PLAN commit authored `docs/envoy-go/phases/18.2-ext-authz-grpc/PLAN.md` (the 14-task PLAN mirroring the phase-09..18.1 PLAN structural template; settles all 7 SPEC §12 deferred-decisions D1..D7 plus 6 PLAN-emerged decisions D8..D13). ROADMAP rows UNCHANGED at this commit (row 18.2 stays `in-progress` — flips `done` only at the IMPL phase-done commit AT THE SAME COMMIT as parent row 18 `done` per parent SPEC §8 rollup; row 18.1 stays `done`; parent row 18 stays `in-progress`). **ADR-0165 hypothesized to land at 18.2 IMPL Task 4** per planner-time decision D3 + D12 — the SPEC §13.5/§6.5/§6.6 "NO new callback method" claim is in direct conflict with SPEC §15 item 4 + §11.P4 RATIFICATION (populated `tls_session.sni` / `source.certificate` / socket addresses / `destination.principal`); the callback-surface extension is unavoidable; ADR-0044 escape-valve fires. The PLAN AMENDS SPEC §13.5 + §6.5 + §6.6 in-place at Task 4 alongside ADR-0165's authoring. Next-free ADR remains `ADR-0165` at this PLAN commit; bumps to `ADR-0166` at the IMPL Task 4 commit if the D12 hypothesis holds. The next session is the phase-18.2 IMPL session per project memory `feedback_execution_style.md`.
- **phase-directory:** `docs/envoy-go/phases/18-http-filter-ext-authz/` (BRAINSTORM.md + parent master SPEC.md) + `docs/envoy-go/phases/18.1-ext-authz-http/` (SPEC.md + PLAN.md + PROGRESS.md + REVIEW.md; fully closed) + `docs/envoy-go/phases/18.2-ext-authz-grpc/` (SPEC.md + PLAN.md drafted at this commit + the pre-existing README.md stub from the parent SPEC commit, now superseded; PROGRESS.md + REVIEW.md pending).
- **lifecycle-state:** `phase 18.2 PLAN done; phase 18.2 IMPL pending` — phase 18.2 PLAN is complete; ROADMAP rows unchanged at this commit; parent row `18` stays `in-progress` (closes AT THE SAME COMMIT as row 18.2 at IMPL phase-done per parent SPEC §8 rollup); next-free ADR is `ADR-0165` (CONDITIONAL: ADR-0165 lands at IMPL Task 4 per D12 → next-free becomes `ADR-0166`). The phase-18.2 IMPL session is the next lifecycle step (state-3 entry per ADR-0005 §Decision 4 + project memory `feedback_execution_style.md` → `superpowers:subagent-driven-development`).
- **next-skill:** `superpowers:subagent-driven-development` — the next session executes the 14-task PLAN at `docs/envoy-go/phases/18.2-ext-authz-grpc/PLAN.md`. The PLAN settles 13 planner-time decisions (D1..D13) so the implementer can proceed without re-deriving; each task carries a single-ADR-anchor (where applicable), file list, acceptance criteria, verification commands, and TDD-style "write failing test first" Step 1 ordering. Cold-start scope: read `docs/envoy-go/STATE.md` (this file) + `docs/envoy-go/phases/18.2-ext-authz-grpc/PLAN.md` (the authoritative task ledger) + `docs/envoy-go/phases/18.2-ext-authz-grpc/SPEC.md` (the cross-reference for the §1 + §3.1 + §6 + §7 + §10 + §13 + §15 anchors) + `docs/envoy-go/DECISIONS.md` (ADR-0158 §Context already at parent SPEC commit `308e9b6`; ADR-0157 in-place AMENDMENT at Task 3; ADR-0160/0161 in-place EXTENSION with gRPC-mode portion at Tasks 5/6; ADR-0165 CONDITIONAL at Task 4 per D12) + `docs/envoy-go/ROADMAP.md` (rows 18 + 18.2 both `in-progress`; close BOTH `done` AT THE SAME COMMIT at IMPL Task 13) + `docs/envoy-go/BEHAVIOR_CONTRACT.md` (8-edit bundle lands at IMPL Task 13 per SPEC §13).
- **last-commit:** `<TBD>` — `Squash merge phase-18.2-ext-authz-grpc-plan`. The phase-18.2 PLAN worktree `phase-18.2-ext-authz-grpc-plan` (branched off master tip `be18857`, the phase-18.2 SPEC SHA-fill follow-up) was squash-merged to master as one PLAN commit `<TBD>` (~2 files, ~+670 LoC). The squash combined ≥1 worktree commit — initial PLAN authoring + STATE advance + a reviewer-fix commit addressing the plan-document-reviewer pass (verdict APPROVED with 2 non-blocking issues + 2 advisory recommendations; the reviewer-fix folded the chain-primitive count consistency fix (5 → 6 across all surfaces) + the SPEC §13.5/§6.5/§6.6 triple-amendment expansion at Task 4 Step 9 + the Task 4 Step 0 pre-spike for listener-cert plumbing). NO BEHAVIOR_CONTRACT changes at this PLAN commit — the 8-edit bundle lands at 18.2 IMPL Task 13. NO new ADR numbers at this PLAN commit. Predecessor master tip: `be18857` (`phase 18.2 SPEC follow-up: STATE.md SHA-fill`). The post-squash SHA-fill follow-up commit (`<TBD>` → actual SHA) lands directly on master per the phase-09..18.1 PLAN-stage close pattern.
- **last-updated:** 2026-05-15
- **next-free ADR:** `ADR-0165` — phase 18.2 PLAN landed 0 new ADR numbers; ADR-0165 is HYPOTHESIZED to land at 18.2 IMPL Task 4 per planner-time decisions D3 + D12 (the callback-surface extension; the ADR-0044 escape-valve firing). ADR-0158 §Decision + §Consequences land at IMPL Task 3; ADR-0157 §Decision AMENDMENT lands at IMPL Task 3; ADR-0160 gRPC-mode portion lands at IMPL Task 5; ADR-0161 gRPC-mode portion lands at IMPL Task 6. If D12 is falsified at IMPL Task 4 (the implementer finds an alternative path that avoids the callback extension — unlikely per the PLAN's D3 rationale), ADR-0165 does NOT land + next-free stays at `ADR-0165`. ADR tail at master tip remains `ADR-0164`.

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
