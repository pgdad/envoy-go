# envoy-go State

This file is the single source of truth for "what next" (see `BOOTSTRAP_PROMPT.md` §4.1 invariant 1). Cold-start reads it first. A session must update it before exiting.

---

- **active-phase:** `phase 29 parent SPEC done` (2026-06-03, this commit) — **phase 29 (`network-filter-mongo-proxy`) parent SPEC DONE at 2026-06-03** — the parent master SPEC (`docs/envoy-go/phases/29-network-filter-mongo-proxy/SPEC.md`, 16 §§ + 1 appendix) formalizes the BRAINSTORM's 4 Q-decisions + EXECUTES all twelve §10 D29-1..D29-12 empirical pins IN-SESSION against Envoy v1.37.2 per ADR-0004 (parallel-subagent fan-out: a LIVE docker boot of the reference image with a `[mongo_proxy, tcp_proxy]` listener + admin `/stats` + `/stats/prometheus` probes with two stat_prefix values; go-control-plane v1.32.4 binding reads + `proto.MessageName` run in-session; upstream proxy/codec/bson/stats/utility/filter-manager/fault-config source fetched at tag v1.37.2; the as-built `internal/filter/network/` halt/resume machinery inventoried with file:line anchors). **Pins RESOLVED (13 AMENDs B1..B13):** **D29-1 (SPEC-BLOCKING) CONFIRMED** — the standard `envoyproxy/envoy:v1.37.2` image ships `mongo_proxy` as a core extension (validate OK; boots; the 23-stat roster live post-first-connection) → the Q3 cross-side strategy STANDS; **D29-4 RESOLVED LEGACY-ONLY** (the largest unknown) — upstream decodes EXACTLY 7 opcodes (Reply 1 / Insert 2002 / Query 2004 / GetMore 2005 / KillCursors 2007 / Command 2010 / CommandReply 2011); **OP_MSG(2013) + Update(2001) + Delete(2006) + Msg(1000) are NOT decoded** → `decoding_error`; AMEND-B1 stat scope CONFIRMED `mongo.<stat_prefix>.<counter>`; AMEND-B2 **Prometheus is TAG-EXTRACTED** (`envoy_mongo_<leaf>{envoy_mongo_prefix="<prefix>"}`) → the `name.go` arm is the **ADR-0218 tag-extractor shape, REFUTING the BRAINSTORM/ROADMAP `.mongo.` inline-prefix (ADR-0138) hypothesis** (the exact inverse of the phase-28 finding); AMEND-B3 the fixed roster is EXACTLY 22 counters + 1 gauge (`op_query_active`), **`delays_injected` PLURAL**, ZERO macro histograms → stat surface 337 → **360**; AMEND-B4 fixed stats are created per-CONNECTION upstream (reference admin shows nothing until the first downstream connection) → all differential assertions post-first-connection + creation posture = D-P1; AMEND-B5 the 7-opcode envelope; AMEND-B6 decode error → `sniffing_=false` → decode STOPS for the connection LIFETIME (≠ zookeeper's abandon-buffer model) → fresh connection per error fixture arm; AMEND-B7 `commands` = builtin-REMEMBERING (not gating; unlisted → `cmd.unknown_command.total`; default `{delete,insert,update}`; alias normalization incl. `ismaster`→`isMaster`, `find`→query-path); AMEND-B8 fault-delay semantics (per-decoded-request-message eval; re-entrancy guard; `delays_injected` at timer-ARM; StopIteration-while-pending per read; timer-fire → `continueReading()` resuming at the NEXT filter; cancel on close); AMEND-B9 FaultDelay PGV (the `fault_delay_secifier` oneof REQUIRED + `fixed_delay` gt 0s → 2 new boot-reject parity arms); AMEND-B10 the access log is timing-bearing JSON → differential log comparison NOT viable → unit-test + coverage-boundary fallback (NO access-log fixture dir; fixtures end at 54); AMEND-B11 dynamic-metadata shape (collection-keyed operation ListValues; insert/query only; cleared per decode pass; mirrorable onto the ADR-0217 Bucket; differential-invisible → unit-proof); AMEND-B12 `cx_destroy_local/remote_with_active_rq` needs close-DIRECTION — an as-built framework gap → D-P4 (29.2 seam-or-boundary); AMEND-B13 **the async halt/resume seam REFRAMED** — upstream has NO persistent filter-manager halt (the filter is re-invoked per read and keeps returning StopIteration); envoy-go's per-pass-stop ALREADY mirrors this pre-handoff; the three REAL extensions are (i) ACTIVE asynchronous `ContinueReading` (today's async call degenerates to a flag nothing consumes), (ii) cross-goroutine safety (timer goroutine vs read-loop/pump-A), (iii) post-handoff read-halt honoring (`replayRead` ignores Status + `readChainConn.Read` returns bytes regardless — the 28.1b §3.5 boundary lifted for halt purposes only). **3-way split surface-mapping formalized** (§3: 29.1 = BSON+wire+requests+roster+prom-arm+0049/0050+39th-fuzzer; 29.2 = OP_REPLY/OP_COMMANDREPLY+correlation+mutex+gauge+cx_destroy+metadata+0051; 29.3 = seam+fault-delay+access-log+cx_drain_close+0052+ROLLUP); all three sub-phases fit the ADR-0045 gate (§15). **ADR-0224 + ADR-0225 + ADR-0226 §Context drafts appended to DECISIONS.md** (tail ADR-0223 → ADR-0226; next-free → ADR-0227; §Decision/§Consequences bodies land at the respective IMPLs per ADR-0044). 12 SPEC-time D-questions (D-P1..D-P12) recorded for sub-phase SPEC/PLAN resolution. spec-document-reviewer APPROVED. **Earlier (phase 28, preserved):** **Phase 28 (`network-filter-zookeeper-proxy`) is COMPLETE** (28.1a + 28.1b + 28.2 all done; parent row rolled at 2026-06-02). Phase 28 as a whole delivered: the `network.WriteFilter` seam (ADR-0221) + complete both-direction zookeeper_proxy counter parity (ADR-0222/0223) across 3 sub-phases (28.1a write-seam+request, 28.1b read-seam+cross-side-proof, 28.2 response+latency+per-connection-mutex); six gates GREEN LIVE (50/50 differential; h2spec 53/53; proxy-wasm 10/10); THIRD §9 Network-filters-family row closed.
- **phase-directory:** `docs/envoy-go/phases/29-network-filter-mongo-proxy/` (BRAINSTORM.md + SPEC.md — the parent SPEC at this commit; the 29.1 sub-phase directory `29.1-network-filter-mongo-wire-and-requests/` is created at the 29.1 SPEC session). The phase-28 family directories stay the durable records of their phases.
- **lifecycle-state:** `phase 29 parent SPEC done (SKILL_ROUTING state 2-for-29.1; next = 29.1 SPEC)` — the parent SPEC (this commit) resolved all empirical pins + formalized the split + anchored the ADR drafts. The next lifecycle transition is the **29.1 sub-phase SPEC** (`superpowers:writing-plans` scoped to the 29.1 SPEC per the 22.1/25.1/26.1/28.1 per-sub-phase precedent — the next session authors `docs/envoy-go/phases/29.1-network-filter-mongo-wire-and-requests/SPEC.md`, NOT the 29.1 PLAN). ROADMAP sub-row 29.1 flips `planned → in-progress` at that 29.1 SPEC commit. Phase 28 (parent + 28.1a/28.1b/28.2) is DONE and CANONICAL.
- **next-skill:** `superpowers:writing-plans` (scoped to **the 29.1 sub-phase SPEC** — `docs/envoy-go/phases/29.1-network-filter-mongo-wire-and-requests/SPEC.md` per the 22.1/25.1/26.1/28.1 precedent; inherits the parent SPEC §11 empirical pins [no re-scraping needed beyond the D-P2 dynamic-stat live re-probe]; resolves the 29.1-scoped D-questions D-P1/D-P2/D-P3/D-P8 [+ unit-arms for D-P5]; pins the package file layout + the decoder/chain-buffer tracking shape + the fixture arm rosters + the canned-bytes mongo responder BackendKind; re-checks the ADR-0045 gate at PLAN). Per `feedback_execution_style` the eventual IMPLs run subagent-driven; per `feedback_git_worktrees` work in a git worktree; subagents commit LOCAL-ONLY [`feedback_subagents_no_push`], the controller squash-merges + pushes at stage-close [`feedback_push_to_origin`]; per-task `gofmt -l` + `golangci-lint` on touched pkgs [`feedback_pertask_gofmt_lint`]. Counts at the phase-29 parent SPEC (unchanged from the BRAINSTORM — docs only): fuzzers **38**, fixtures **50** [tail `0048-zookeeper-responses`], stat surface **337**; DECISIONS.md tail **ADR-0226** (the three phase-29 §Context drafts consumed 0224/0225/0226 at this commit; next-free **ADR-0227**).
- **last-commit:** `PARENT_SPEC_SQUASH_SHA` — `phase 29 parent SPEC: mongo_proxy + async halt/resume seam — D29-1..D29-12 pins resolved vs Envoy v1.37.2; 13 AMENDs; 3-way split formalized; ADR-0224/0225/0226 §Context drafted [ADR-0224,ADR-0225,ADR-0226]` (squash-merged from `phase-29-network-filter-mongo-proxy-spec`; docs only — SPEC.md + the DECISIONS.md ADR drafts + this STATE.md advance + next-prompt.txt). Substantive predecessor on master: the phase-29 BRAINSTORM squash `91b1be7` (trailed by the docs-only repoints `49127e7`/`386e6dd`). origin/master in sync after push.
- **last-updated:** 2026-06-03
- **next-free ADR:** `ADR-0227` (DECISIONS.md tail is now **ADR-0226** — the phase-29 roster ADR-0224 [mongo_proxy filter + BSON/wire decode + request side] / ADR-0225 [response side + correlation + gauge] / ADR-0226 [async halt/resume seam + fault delay + access log] §Context drafts landed at this parent-SPEC commit per ADR-0044; the §Decision/§Consequences bodies land at the respective sub-phase IMPLs; next-free after phase-29 phase-done stays ≈ **ADR-0227**). ADR-0209 escape-valve reserve carried from the §9 family STANDS-UNCONSUMED.

---

## Project counts at this commit

- **active differential fixtures:** **50** (tail `0048-zookeeper-responses`) — unchanged (docs only). Anticipated at phase-29 phase-done: **54** (`0049-mongo-requests` + `0050-mongo-boot-reject` at 29.1; `0051-mongo-responses` at 29.2; `0052-mongo-fault-delay` at 29.3; NO access-log dir per AMEND-B10).
- **fuzzers:** **38** (canonical recipe `grep -rh "^func Fuzz" $(find ./internal -name fuzz_test.go) | wc -l`) — unchanged. Anticipated at phase-done: **39** (40 only if D-P6 resolves to a separate response fuzzer).
- **stat surface:** **337** (BEHAVIOR_CONTRACT doc count) — unchanged. Anticipated at phase-done: **360** (+23: the exact mongo fixed roster per SPEC §7.2).
- **DECISIONS.md tail:** **ADR-0226** (next-free **ADR-0227**) — the three phase-29 §Context drafts landed at this commit.
- **conformance:** h2spec 53/53; proxy-wasm all 10 families PASS (as of the 28.2 six-gate, 2026-06-02).

---

## Lifecycle cross-reference

(unchanged — see SKILL_ROUTING.md and BOOTSTRAP §5.)

## Exit contract for every session

(unchanged.)
