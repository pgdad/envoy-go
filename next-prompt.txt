=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-CHECKED 2026-07-14 (after the phase-62 SPEC landed; row 62 STAYS `in-progress`, run MECHANICALLY) — it does NOT fire. Do NOT create `stop`.** State:
- (1) prints `NOT DONE: row 62` — the phase-62 row STAYS `in-progress` at its SPEC (a row flips `done` only at its IMPL six-gate, ADR-0106). At least one non-`done` row ⇒ the sentinel is blocked.
- (2) prints THREE live "candidates:" sentences — HTTP/3, xDS, and Observability ⇒ three families STAY OPEN. (`reference_sentinel_deferred_sentence_live_vs_historical`.)
- (3) prints `NEVER OPENED: gRPC`, `Runtime`, `WASM` ⇒ THREE families never opened. Operational-tooling also STAYS OPEN.

Any of (1)/(2)/(3) printing ⇒ the sentinel does NOT fire.

**STATUS AS OF 2026-07-14: the phase-62 SPEC LANDED; ROADMAP row 62 (`tracing-custom-tags-request-header`) STAYS `in-progress`.** The SPEC DISPOSED all eleven D-RH-* questions via LIVE probes against `envoyproxy/envoy:contrib-v1.37.2` (SPEC §11, fresh container per arm). **The empirical ground truth (pins the design):** (WIRE) `{key:<tag>, value:{stringValue:<header value>}}`; (MISSING) present→value / absent+default→default / absent+empty-default→**OMIT** (a NEW semantic vs `literal`); (MULTIVALUE) → the **FIRST** header value; (PRECEDENCE) custom overrides a colliding built-in AND — the LOAD-BEARING finding — among custom tags with the SAME key the reference keeps the **FIRST in config order** (Envoy's config-time map insert-if-absent), which CONTRADICTS the landed `upsertAttr` last-wins (SPEC §1.1 amendment); (REJECT) empty `request_header.name` PGV boot-rejects (parity) + the reference ACCEPTS `request_header`. **The pinned design (SPEC §3):** lift the reject (`config.go:155-156`); the static `TracingConfig.CustomTags []KV` becomes an ORDERED `[]CustomTagSpec` deduped FIRST-wins at parse; a per-request `ResolveCustomTags(specs, headerLookup) []KV` threaded at the THREE `accesslog_emit.go` call sites (H1 `:55`/H2 `:116`/H3 `:177`) via `reqHeaderLookupH1`/`reqHeaderLookupH2`; `BuildServerSpan`/`upsertAttr` UNCHANGED. ADR-0283 §Context is DRAFTED in SPEC §13. Per the STANDING DIRECTIVE, the lifecycle advances THIS phase to its PLAN.

==============================================

## What THIS session does (the phase-62 PLAN)

Perform the PLAN stage (lifecycle-state 2 → 3) for the phase-62 subject — the **tracing `request_header` custom_tag source arm** (Observability family). This is a DOCS-ONLY stage in a FRESH worktree off master (`feedback_git_worktrees`); NO production `.go` changes. It authors `docs/envoy-go/phases/62-tracing-custom-tags-request-header/PLAN.md` (+ a `PROGRESS.md` scaffold) — a MECHANICAL TDD decomposition (`superpowers:test-driven-development`, `superpowers:writing-plans`) of SPEC §10 into an ordered task spine, each task a red→green with a `-count=1` liveness break where an assertion is load-bearing (`reference_differential_break_protocol_count1`, `reference_deliberate_break_wrong_assertion`). NO new empirical question remains — the SPEC's live probes pinned everything; the PLAN is pure decomposition. It RE-DERIVES every SPEC §12 edit-site `file:line` against the master tip (`feedback_brief_citations_not_evidence`).

**The task spine (from SPEC §10 — the PLAN refines/orders):**
1. Config model + parse arm — the `CustomTagSpec`/`customTagKind` types; reshape `parseCustomTags` → `([]CustomTagSpec, error)` with the `request_header` accept arm + the empty-`name` reject + **first-wins dedup by tag key** (SPEC §3.1/§3.2); the `TracingConfig.CustomTags` field type change. Unit: accept a request_header; reject empty-name + unchanged arms; dedup keeps the FIRST of two same-key tags.
2. `ResolveCustomTags` (SPEC §3.3) — the per-request resolver (literal→static; request_header→first value / default / omit; nil-lookup→default/omit). Unit: the full matrix.
3. `span.go` — CONFIRM `BuildServerSpan`/`upsertAttr` need NO signature change (the resolver produces unique keys); a `span_test.go` case: a resolved request_header KV upserts over a colliding built-in.
4. Zipkin encoder unit test.
5. Call-site threading — `accesslog_emit.go:55`/`:116`/`:177` call `ResolveCustomTags(..., reqHeaderLookupH1/H2(...))`.
6. New OTLP fixture `0105-tracing-custom-tags-request-header` (RE-DERIVE the next-free number — `0104` is the tail; the driver SENDS `x-trace-user`; present-case cross-side, proven live).
7. `FuzzHCMConfigParse` seed (no new fuzzer; count stays 55).
8. BEHAVIOR_CONTRACT edits.
9. Verify — six-gate + the full 107-dir differential (byte-stable except `0105`).
10. ADR-0283 §Decision/§Consequences + STATE + ROADMAP (row 62 → `done`, ADR-0106; the LIVE deferred sentence NARROWS `custom_tags (request_header/environment/metadata)` → `(environment/metadata)` — re-run check-(2), EXACTLY ONE live match) + router roll.

Land it (commit + push per `feedback_push_to_origin`) and ROLL THIS FILE to the IMPL stage.

**Honor throughout (the discipline, unchanged):**
- **Lifecycle = one stage per session.** This session: the PLAN ONLY. After it lands: roll to the IMPL.
- **Process anchors:** ADR-0044 (ADR §Decision/§Consequences land at the IMPL) · ADR-0045 (single flat row — the SPEC confirmed ~11–13 tasks, escape-valve UNCONSUMED) · ADR-0106 (row 62 flips `done` at its IMPL, SOLE leg, no parent rollup) · `reference_roadmap_split_phase_row_done`.
- **`next-prompt.txt` IS TRACKED** (`reference_next_prompt_tracked_despite_gitignore`) — edit it inside the stage worktree and fold into the stage commit; locate commits by SUBJECT (`git log --grep`), never by position.
- **Worktree discipline** (`feedback_git_worktrees`, `feedback_subagent_worktree_detach`, `feedback_subagent_worktree_path_targeting`) — a PLAN is docs-only + light; keep worktree hygiene; VERIFY the main checkout stays clean (edit worktree-relative paths only). If subagent-driven, `feedback_execution_style` + `feedback_subagents_no_push`.
- **The tracing traps** (`reference_tracing_custom_tag_override_builtin` — the upsert-by-key seam; the SPEC found the custom-vs-custom rule is FIRST-wins (dedup at parse), NOT the last-wins upsert; `reference_tracing_upstream_cluster_framework_gap` — the 4 EMPTY built-ins are a SEPARATE gap; `reference_differential_fixture_dispatch_constraint`; `reference_fuzzer_count_docs_drift`; `reference_fatalf_makes_assertions_unreachable`; `reference_sentinel_deferred_sentence_live_vs_historical`).

**ADR reservation ledger (provisional; each finalized at its IMPL in landing order):** ADR-0277 = phase-59 tracing-custom-tags-literal (LANDED) · ADR-0278 = xDS 60.1 (LANDED) · ADR-0279 = HTTP/3 61.1 (LANDED) · ADR-0280 = xDS 60.2 (LANDED) · ADR-0281 = HTTP/3 61.2 (LANDED) · ADR-0282 = HTTP/3 61.3 (LANDED) · **ADR-0283 = phase-62 tracing-custom-tags-request-header** (§Context DRAFTED in SPEC §13; §Decision/§Consequences at the phase-62 IMPL) · next-free unreserved **ADR-0284**.

**Current counts (VERIFIED at the phase-62 SPEC against master tip `664fb2dd` — RE-VERIFY at session start, `git fetch` first):** fixtures **106** (`ls -d test/fixtures/[0-9]*/ | wc -l`; tail `0104-http3-downstream-get`) · fuzzers **55** · DECISIONS tail **ADR-0282** (next-free **ADR-0283**) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** · go.mod modules **2** (`quic-go` direct + `qpack` indirect). A PLAN is DOCS-ONLY ⇒ ALL counts UNCHANGED at this stage.

**Locating the phase-62 SPEC stage squash** (a router cannot name its own sha — identify by SUBJECT, never by position):
```sh
git log --oneline --grep='^phase 62 (tracing-custom-tags-request-header) SPEC' -1
```

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-62 PLAN is next; the SPEC landed, row 62 `in-progress`).
2. `docs/envoy-go/phases/62-tracing-custom-tags-request-header/SPEC.md` — the pinned design (§1.1 the first-wins-dedup amendment; §3 the change; §10 the task spine; §11 the live-probe results; §12 the edit-site roster; §13 the ADR-0283 §Context).
3. `docs/envoy-go/phases/62-tracing-custom-tags-request-header/BRAINSTORM.md` — the 12-section design (the D-RH-* origins, the rejected alternatives §2.1).
4. `docs/envoy-go/phases/59-tracing-custom-tags-literal/{PLAN,PROGRESS}.md` — the LANDED literal-row PLAN shape (the TDD spine template + the PROGRESS scaffold this row mirrors).
5. `docs/envoy-go/STATE.md` — the active-phase header (phase 62 SPEC done; NEXT = the phase-62 PLAN).
6. `docs/envoy-go/ROADMAP.md` — row 62 (with the SPEC-done annotation) + the Observability family prose.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the tracing `reference_*` traps + the differential-harness traps for the `0105` fixture.
