START a fresh session for envoy-go. **The phase-42.2b (hedging) IMPL is COMPLETE and LANDED on master, and ROW 42 (`retries-and-hedging`) is DONE.** 42.2b was the THIRD and FINAL leg of the three-leg Upstream-robustness row (42.1 retries + 42.2a `per_try_timeout` + 42.2b hedging) — all three legs have landed, so the split row FLIPS `in-progress → done` (ADR-0106 + `reference_roadmap_split_phase_row_done`; NO parent rollup). The next step is a **FRESH BRAINSTORM for a NEW subject** (the row-41-IMPL precedent: an IMPL-done that closes a row rolls `next-prompt.txt` to a fresh BRAINSTORM — NOT another stage of 42.2b, which is fully done). Confirm the latest master is the 42.2b IMPL completion commit via `git log --oneline -3` (a NEW sha — do NOT assume a literal value). Counts at tip: stat surface **1181** / fixtures **79** (tail `0077-hedge-on-per-try-timeout`) / fuzzers **42** / BackendKind tail **36** (`BlockingHoldResponder`) / DECISIONS tail **ADR-0251**, next-free **ADR-0252**.

## NEXT STEP — choose the next ROADMAP subject + run its BRAINSTORM (`superpowers:brainstorming`)

Row 42 is DONE. The next session picks the next subject and brainstorms it (the project's **BRAINSTORM-picks-the-subject** convention — the subject choice is deferred to the next session, NOT pre-committed here). Read `docs/envoy-go/ROADMAP.md` for the next-priority row + the §9 family headings (each heading expands into per-phase rows only when a phase under it enters `in-progress` — do NOT pre-populate rows for unbrainstormed phases).

- **The remaining Upstream-robustness-family candidate** is **{per-protocol connection pooling}** — where the phase-41-DEFERRED `max_connections` + `max_pending_requests` enforcement lands (the phase-41 SPEC narrowed circuit-breaking to the `max_requests` keystone; the cx/pending budgets need the connection-pool queue envoy-go's synchronous model lacks, and their stat names are already registered emit-0 for parity). It is a STRONG candidate (it composes on the phase-40/41/42 substrate + activates the dormant emit-0 stats), but the next session MAY pick a different next-priority subject — defer the actual choice to the BRAINSTORM dialogue.
- Whatever the subject: open it with `superpowers:brainstorming` (explore intent + forks BEFORE any spec/code), author the BRAINSTORM in the prevailing phase-shape (the phase-41/42 12-section shape), get it spec-document-reviewer-APPROVED, anchor the §10 D-* live-pin obligations for the SPEC (executed against `contrib-v1.37.2` per `reference_docker_probe_bridge_network`), and advance STATE/ROADMAP (lifecycle-state 0 → 1 if it opens a new family row).

## What just landed (phase 42.2b — DONE)

**ADR-0251** — hedging (`RouteAction.hedge_policy` field 27 / `VirtualHost` field-17 fallback): the project's FIRST concurrent-upstream-attempt control plane. A SEPARATE concurrent `hedgeExecutorH1`/`hedgeExecutorH2` (`internal/filter/http/router/hedge.go`, a SIBLING to `retry.go` — NO existing executor modified) dispatched via the `H{1,2}ClusterAction` (+ weighted) closure switch BEFORE the `if a.rp != nil` retry branch, gated by `(*HedgePolicy).TriggersConcurrency()`; a per-attempt HEDGE-TRIGGER timer launches a budget-counted hedge LEAVING the original in flight; a BUFFERED first-acceptable-wins collector returns the first non-retriable result + cancels/drains losers (`-race`-clean). The load-bearing invariants: **AMEND-H1** (a hedged per-try-timeout increments `upstream_rq_retry`, NEVER `upstream_rq_per_try_timeout` — STAYS 0 under hedge mode), **AMEND-H2** (`upstream_rq_retry_limit_exceeded` = "no more hedges", not a final failure), the **H1/H2 loser-cancel asymmetry** (a recorded departure — NO active H1-loser socket reset), **AMEND-H5** (NO new stat — surface STAYS 1181). The `0077-hedge-on-per-try-timeout` cross-side fixture (a single held `BlockingHoldResponder` host; poll-to-converge + `/__release` + assert `per_try_timeout==0`) proves it. Byte-identical when no triggering `hedge_policy`.

## Honor throughout (the discipline)

- **Lifecycle = one stage per session.** `feedback_git_worktrees` (a fresh worktree off master) · `feedback_subagents_no_push` (subagents commit LOCALLY; the controller squashes + pushes at stage-close) · `feedback_push_to_origin` (push after a clean squash on master with tests green) · `feedback_execution_style` (subagent-driven execution).
- **BRAINSTORM-specific:** the `superpowers:brainstorming` skill (explore intent + forks before any creative work); the BRAINSTORM PICKS THE SUBJECT (do NOT pre-commit the subject here); author the §10 D-* live-pin obligations for the downstream SPEC; defer the ADR §Context draft to the SPEC, the §Decision/§Consequences bodies to the IMPL (ADR-0044).
- **Process anchors:** ADR-0045 (the split-gate re-check at PLAN); ADR-0106 + `reference_roadmap_split_phase_row_done` (a split row flips `done` only when ALL legs land — as row 42 just did); ADR-0080 (strict-reject duplicates); `reference_docker_probe_bridge_network` (live reference-Envoy probes).

## Read first (in order)

1. THIS FILE — the cold-start router (the 42.2b IMPL is DONE + LANDED; row 42 is DONE; the next step is a FRESH BRAINSTORM for a NEW subject).
2. `docs/envoy-go/STATE.md` — active-phase `phase 42.2b (hedging) IMPL done`; the count figures (1181 / 79 / 42 / 36 / ADR-0251, next-free ADR-0252).
3. `docs/envoy-go/ROADMAP.md` — the next-priority row + the §9 family headings (row 42 now `done`; the Upstream-robustness family STAYS OPEN with 1 candidate left {per-protocol connection pooling}).
4. The `superpowers:brainstorming` skill — run it to pick + explore the next subject.
5. `docs/envoy-go/DECISIONS.md` (ADR-0249 retries + ADR-0250 per_try_timeout + ADR-0251 hedging; next-free ADR-0252) + `docs/envoy-go/BEHAVIOR_CONTRACT.md` (the `### Route — request recovery (retries)` subsection now carries the hedging sub-block; the stat-surface block is 1181).
6. `docs/envoy-go/phases/42.2b-hedging/README.md` — the just-completed phase summary (the masters reading order, the as-built counts, the cross-references).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `reference_roadmap_split_phase_row_done`, `feedback_subagents_no_push`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_execution_style`; AND — if the next subject is the {per-protocol connection pooling} candidate, which is itself a concurrent-upstream-attempt control plane — `reference_concurrent_attempt_downstream_class_assertion` (assert the DOWNSTREAM 2xx class, not the over-counting upstream class) + `reference_concurrency_differential_release_barrier` (the held `/__release` + poll-the-gauge model)).
