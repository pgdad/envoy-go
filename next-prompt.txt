START a fresh cold-start session for envoy-go — phase 26.1 (`network-filter-chain-framework-and-echo`) SPEC is DONE; author the 26.1 PLAN.

This is a per-sub-phase PLAN cold-start. **Phase 26.1 SPEC is DONE + squash-merged to master + pushed to origin** (SPEC squash `ea77585`; origin/master at the same after the stage-close follow-up). The 26.1 SPEC inherits the phase-26 parent SPEC's §5 proto roster + §6 PARSE-REJECT roster + §7 stat surface + §8 fixture taxonomy + §11 empirical-pin block + §12 D-questions + §13 RATIFIED items, and refines per-Task surface only. It anchors the NEW `internal/filter/network/` read-filter chain framework + `echo` + `direct_response` + the `manager.go` dual-dispatch + the `cmd/envoy-go/main.go` boot-wiring. STATE.md points at `phase 26.1 SPEC done; awaiting 26.1 PLAN`, next-skill `superpowers:writing-plans` (scoped to 26.1 PLAN), next-free `ADR-0215` (DECISIONS.md tail at ADR-0214 — the ADR-0213+0214 §Decision/§Consequences bodies land at 26.1 IMPL, NOT at the PLAN).

## What this session must do: author the phase-26.1 PLAN

Per SKILL_ROUTING state 2 (26.1 SPEC.md exists, PLAN.md does not), the next-skill is **`superpowers:writing-plans` scoped to 26.1 PLAN authoring** (`docs/envoy-go/phases/26.1-network-filter-chain-framework-and-echo/PLAN.md`). This is the bite-sized TDD task plan derived from the 26.1 SPEC §10 task spine (~14-18 tasks). The PLAN:
1. Decomposes the 26.1 SPEC §10 task spine into bite-sized, individually-committable, TDD-first tasks (write-the-failing-test → run-it-fails → minimal-impl → run-it-passes → commit), each with exact file paths + complete code/test sketches + exact commands + expected output, per the writing-plans skill.
2. Checks the ADR-0045 split-gate FIRST (the SPEC §15 confirms 26.1 fits: ~850-1020 net-new LoC / ~14-18 tasks, ZERO moved LoC — well within ~25 tasks / ~1500 LoC; NO split anticipated). If the PLAN exceeds the gate, split per SKILL_ROUTING state-2 GATE + update ROADMAP/STATE and stop.
3. Embeds the SPEC §11.1 D-S1 baselines as the IMPL Task-1 hard first-action gate (re-grep fuzzers 34 / fixtures 41 / tail 0039 / stats 132 before asserting deltas — the master tip may advance between SPEC and IMPL).
4. Resolves the 26.1 SPEC §12 D-questions that are PLAN-time (D-P26.1-1 buffer type; D-P26.1-2 DataSource resolution sharing); leaves D-P26.1-3/4/5 (byte-stable wording, boot-reject arm, close-detection + response-code-details storage) to IMPL.

A 26.1 BRAINSTORM is NOT needed (settled at the parent BRAINSTORM + parent SPEC + the 26.1 SPEC). A 26.1 SPEC re-author is NOT needed (DONE + reviewed + merged).

This session:
1. Reads STATE.md + the 26.1 SPEC + the parent SPEC (for the inherited rosters) + ROADMAP rows 26/26.1 + BOOTSTRAP_PROMPT.md §5 + SKILL_ROUTING.md to confirm the SPEC→PLAN routing (state 2).
2. Confirms baseline GREEN at master tip (`go build ./... && go vet ./... && golangci-lint run && go test -race -short ./...` + the differential/h2spec/conformance gates carried from 25.3 — the phase-26.1 SPEC is docs-only).
3. Invokes `superpowers:writing-plans` scoped to 26.1 PLAN authoring (in a worktree per the standing preference; the PLAN stage is doc-authoring, no production code).

## Phase 26.1 scope (settled at the 26.1 SPEC §1-§10; ADR-0213 + ADR-0214)

26.1 `network-filter-chain-framework-and-echo` lands the NEW L4 read-filter chain framework + the family's first two trivial filters:
- **NEW `internal/filter/network/`** (SPEC §3): `types.go` (two-value `Status` enum `Continue`/`StopIteration`; `ReadFilter` interface `OnNewConnection`/`OnData`/`SetReadFilterCallbacks`/`OnDestroy`; two-step factory `NetworkFilterFactory`+`FilterInstanceFactory`; `FactoryCtx{}`); `callbacks.go` (`ReadFilterCallbacks` with `Connection()`/`ContinueReading()`/`DynamicMetadata() *dynamicmetadata.Bucket`; `Connection` accessor `Write`/`Close`/`LocalAddr`/`RemoteAddr`/`RequestedServerName`/`DownstreamPrincipals`; `CloseType` enum `FlushWrite`/`NoFlush`); `buffer.go` (drainable `*Buffer`); `registry.go` (freeze-after-boot `*Registry` — mirrors `listenerfilter/registry.go` + `KnownTypeURLs` from `http/registry.go`); `chain.go` (per-connection chain runner + per-connection runtime context owning the REUSED `*dynamicmetadata.Bucket`). Per SPEC §3.4: REUSE `internal/dynamicmetadata/` at connection scope (NO new package, NO package code change at 26.1 — the doc-generalization + first write land at 26.3).
- **`echo`** (`internal/filter/network/echo/`, `envoy.filters.network.echo.v3.Echo`, EMPTY config) — `OnData` writes bytes back + drains + returns `StopIteration` (SPEC §4.1).
- **`direct_response`** (`internal/filter/network/directresponse/`, `envoy.extensions.filters.network.direct_response.v3.Config` — note `Config`, field `response` DataSource) — logic in `OnNewConnection`: write response + `endStream=true`, set ResponseCodeDetails `DirectResponse`, close `FlushWrite`, return `StopIteration` (SPEC §4.2); boot-reject on unset `response.specifier` (SPEC §6.1).
- **Dual-dispatch in `manager.go`** (SPEC §3.5; D-P2 RESOLVED — lands FULLY at 26.1): new `netReg *network.Registry` constructor arg; `chainInfo.netChainFactory` field; build-time pre-check on `filters[0].type_url` (resolves in `netReg` → new read-filter-chain path; else → existing `buildTerminalFilter`+`Handle` path UNTOUCHED); `serveConnection` step-7 (`manager.go:1005`) dual-branch + a new `serveReadFilterChain` read loop; `network-filter-mixed-chain-unsupported` 26.1-transitional reject (lifted at 26.2). `tcp_proxy`/HCM stay on the OLD path (migration is 26.2).
- **Boot-wiring in `cmd/envoy-go/main.go`** (SPEC §3.6): construct `*network.Registry` → Register echo + direct_response → Freeze → thread `netReg` into `listener.NewManagerWithBaseDirAndAllowH2C(...)` (mirrors the `lfReg` block @main.go:198-200).
- **Fixtures (+3)**: `0040-network-echo` cross-side + `0041-network-direct-response` cross-side + `0042-network-direct-response-boot-reject` boot-reject parity (SPEC §8; numbering 41 → 44). **Fuzzer (+1)**: the 35th `FuzzNetworkFilterConfigParse` (SPEC §11.1; master tip = 34).
- **Stats**: 0 net-new (stays 132). **ADR-0213 + ADR-0214 §Decision/§Consequences bodies land at 26.1 IMPL** (§Context already drafted at the parent SPEC; no new ADR number consumed at 26.1).

Read-filter-ONLY (write-filter deferred + API-revision allowance, ADR-0213). NO per-route surface (SPEC §2.4 / parent D6).

## Project state at master tip (`ea77585`)

- **Master tip:** `ea77585` (the phase-26.1-SPEC squash; the stage-close follow-up commit — STATE SHA-fill + this next-prompt rewrite — may advance the tip one further; run `git log --oneline -1` to confirm the actual tip; this repoint caveat is expected). Predecessor `89fc507` (phase-26 parent-SPEC squash; SHA-filled at `607cb74`; tip-repoint trail `073dd7e`/`9429983`). origin/master in sync after push.
- **§9 Network-filters family OPEN:** parent row 26 `in-progress`; sub-row 26.1 `in-progress` (SPEC done); 26.2/26.3 `planned`. 6 family candidates remain after phase 26 (redis, mongo, kafka_broker, thrift, zookeeper, sni_cluster).
- **Counts at master tip:** stat surface **132**; differential fixtures **41** (numbering tail **0039**); fuzzers **34**; conformance 10/10; h2spec 53/53; DECISIONS.md tail **ADR-0214**, next-free **ADR-0215**. Anticipated 26.1 deltas (at IMPL, NOT at PLAN): fixtures 41 → 44 (0040/0041/0042); fuzzers 34 → 35; stats unchanged at 132; ADR-0213+0214 §Decision/§Consequences bodies land.
- **Toolchain:** Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); reference Envoy v1.37.2 (ADR-0008); go-control-plane v1.32.4 (ADR-0008).
- **Six gates GREEN at master tip** (the phase-26.1 SPEC is docs-only; code is unchanged since 25.3 IMPL phase-done).

## Read first (in order)

1. THIS FILE — the phase-26.1 PLAN cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 26.1 SPEC done; awaiting 26.1 PLAN`; next-skill `superpowers:writing-plans` (26.1 PLAN); next-free `ADR-0215`; last-commit `ea77585`.
3. `docs/envoy-go/phases/26.1-network-filter-chain-framework-and-echo/SPEC.md` — the 26.1 SPEC (the §3.1 API signatures, §3.2 file split, §3.3 chain runner, §3.4 per-conn context, §3.5 dual-dispatch, §3.6 boot-wiring, §4 echo+direct_response, §6 PARSE-REJECT, §8 fixtures, §10 task spine, §11.1 D-S1 baselines, §12 D-questions, §13 RATIFIED items, §15 acceptance checklist). The PLAN decomposes §10.
4. `docs/envoy-go/phases/26-network-filter-chain-and-rbac/SPEC.md` — the PARENT SPEC (the inherited §5/§6/§7/§8/§11/§12/§13 rosters the 26.1 SPEC references).
5. `docs/envoy-go/ROADMAP.md` — rows 26 / 26.1 (26.1 now `in-progress`).
6. `BOOTSTRAP_PROMPT.md` §5 (state machine) + §4.1 STATE-update invariant + ADR-0004/0005 (autonomous PLAN adaptation) + ADR-0045 (split-gate).
7. `docs/envoy-go/SKILL_ROUTING.md` — confirm state-2 → `superpowers:writing-plans` (PLAN) for sub-row 26.1, including the > ~25-task / > ~1500-LoC split GATE.
8. The existing code the 26.1 PLAN tasks pin against: `internal/listener/listenerfilter/{registry,pipeline,types}.go` (the closest structural analogue + the registry-test mirror); `internal/filter/http/{types,registry,callbacks,chain}.go` (the filter-framework precedent); `internal/listener/manager.go` (the dual-dispatch target: `filterHandler`@42, `filterRegistry`@102, `buildTerminalFilter`@569 + call sites @444/@503, `chainInfo` struct @185, `NewManagerWithBaseDirAndAllowH2C`@302, `serveConnection` step-7 @1005); `internal/filter/tcpproxy/filter.go` (`Handle`@69 — stays untouched); `internal/dynamicmetadata/dynamicmetadata.go` (the REUSED sink); `cmd/envoy-go/main.go` (boot-wiring @125-213; the `lfReg` mirror @198-200).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — execution-style + worktree + push + subagents-no-push + differential-asserter-dispatch + fixture-dispatch-constraint memories all apply to 26.1.

After the 26.1 PLAN is authored + reviewed + committed + squash-merged + pushed, advance STATE.md per BOOTSTRAP §5 and rewrite next-prompt.txt for the 26.1 IMPL cold-start (`superpowers:subagent-driven-development` per `feedback_execution_style`).
