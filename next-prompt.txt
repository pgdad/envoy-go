=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, the active phase is fully landed, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action. (The sentinel does NOT apply right now — phase 51 is chartered, its PLAN is LANDED, and the next stage is its IMPL.)

**STATUS AS OF 2026-07-05: phase 51 (`bootstrap-validate-mode`) PLAN is LANDED; the NEXT stage is its IMPL.**

- The phase-51 PLAN landed: `docs/envoy-go/phases/51-bootstrap-validate-mode/PLAN-51.md` — a self-reviewed, fully-concrete **6-task TDD spine** (no placeholders; every step shows complete runnable code or exact shell commands). Row 51 stays `in-progress` (lifecycle-state 2 → 3). STATE.md active-phase header rolled to `phase 51 (bootstrap-validate-mode) PLAN done`. ROADMAP.md row 51 + the "Operational tooling family" section prose both rolled to reflect PLAN-done. **Re-verify with `git log --oneline -6` + `git status`; do NOT assume.**
- **The 6 tasks, in order (re-read `PLAN-51.md` in full before starting — do not work from this summary alone):**
  1. **Scaffolding** — `PROGRESS-51.md` + baseline counts (stat 1200 / fixtures 96 / fuzzers 52 / BackendKind 38 / ADR tail 0267) + the ADR-0045 NO-sub-split confirmation.
  2. **`internal/filter/http/builtins` extraction** — `RegisterBuiltins(reg *filter_http.HTTPRegistry)` with **NO `Deps` struct** (AMEND-VALIDATE-HTTPBUILTINS-NO-DEPS — confirmed no HTTP builtin factory captures a boot singleton at registration time); rewires `main.go`'s ~55-line inline `httpReg` block down to 3 lines. Gated on the FULL existing `cmd/envoy-go` test suite staying green (the byte-stability proof).
  3. **`internal/boot.Construct`/`NewTracingProvider` extraction** — the HIGHEST-RISK task. Per **AMEND-VALIDATE-DEPGRAPH**, `Construct` does **NOT** build the cluster manager (it takes `cm *cluster.Manager` as a caller-supplied parameter, since `sinks`/`tracingProvider` already depend on the `cm`-derived `grpcclient.Dialer` and must be built before `Construct` is callable). Relocates `maybeWrapLuaScriptLoadError` + its two consts + `tracesDialerAdapter` + `zipkinTransportAdapter` verbatim out of `main.go`. Gated on the **FULL 96-fixture differential suite** (`go test ./test/differential/... -run TestDifferential -count=1`) staying green — THIS is the byte-stability regression anchor proving the extraction changed nothing observable about any landed feature. Also gated on the full `cmd/envoy-go` suite + `-race`.
  4. **The public `validate` package** — `Bootstrap(io.Reader, baseDir string, allowH2C bool) error` + `BootstrapFile(path string) error`, the FIRST non-`internal/`/non-`cmd/`/non-`test/` package in this repo. Unit tests: 4 REUSED `internal/bootstrap` reject-arm cases (dynamic_resources, layered_runtime, YAML syntax error, empty document) + 3 NEW construction-boundary cases a bootstrap-parse-level test cannot catch (a nonexistent relative TLS cert filename failing inside `cluster.NewManagerWithBaseDir`; a syntactically invalid Lua script failing inside `listener.NewManagerWithBaseDirAndAllowH2C` and exercising the relocated `maybeWrapLuaScriptLoadError` wrap; an unregistered HTTP-filter name failing via the frozen `httpReg`'s resolution) + 1 positive-path fully-valid-config case.
  5. **`--mode validate` CLI flag** — `0` valid / `1` invalid / `2` usage error (unrecognized `--mode` or missing `-c`). **Calls `validate.Bootstrap` directly, NOT `BootstrapFile`**, so `-allow-h2c` continues to compose with `--mode validate`. One new CLI-subprocess test in `cmd/envoy-go/main_test.go` (good config → exit 0 + "configuration OK"; bad config [reuses Task 4's bad-TLS-cert-filename scenario] → exit 1 + stderr substring; unknown `--mode` → exit 2).
  6. **Docs completion + final six-gate** — ADR-0268 §Decision/§Consequences (its §Context was drafted at SPEC-51.md §12), a new `## Bootstrap config validation` BEHAVIOR_CONTRACT.md section + count-delta paragraph, the full-repo six-gate (`go build`, `gofmt -l`, `golangci-lint run ./...`, `go vet ./...`, `go test ./... -count=1`, the full differential `-race`, `go mod tidy -diff` empty, fuzzer-count reconcile == 52, fixture-count reconcile == 96), STATE.md/ROADMAP.md/PROGRESS-51.md all rolled to IMPL-done + **ROADMAP row 51 flips `done`**.
- **Global constraints carried from the PLAN (re-verify, do not assume):** counts at PLAN — stat **1200** / fixtures **96** / fuzzers **52** / BackendKind **38** / DECISIONS **ADR-0267** (next-free **ADR-0268**). Anticipated at IMPL exit: ALL counts UNCHANGED (+0 everywhere) except **THREE new packages** (`internal/boot`, `internal/filter/http/builtins`, `github.com/esalaine/envoy-go/validate`) and **ADR-0268** landing (next-free ADR-0269). **ZERO new go.mod modules** — `go mod tidy -diff` must stay EMPTY at every task.
- **D-VALIDATE-ALIAS-CONVENTION pinned:** keep `internal/filter/network/builtins` UNALIASED in both `main.go` and `internal/boot/boot.go`; alias the new sibling `httpbuiltins "github.com/esalaine/envoy-go/internal/filter/http/builtins"`.

=> The NEXT action is the **phase 51 (`bootstrap-validate-mode`) IMPL** (lifecycle-state 3 → 4, TERMINAL for this phase). NOT the termination sentinel. NOT a new BRAINSTORM/SPEC/PLAN.

==============================================

## The NEXT action — the phase 51 (`bootstrap-validate-mode`) IMPL

Per `feedback_git_worktrees` + `feedback_execution_style`: create a FRESH git worktree off `master` (via `superpowers:using-git-worktrees`), then execute `docs/envoy-go/phases/51-bootstrap-validate-mode/PLAN-51.md` task-by-task via `superpowers:subagent-driven-development` — one fresh subagent per task, controller review between tasks.

**Controller responsibilities throughout (per `feedback_subagent_autocommit_claudemd` — the global CLAUDE.md makes dispatched subagents auto-commit; do NOT fight this):**
- After EACH task's subagent finishes: verify the commit touches ONLY the files that task's PLAN entry names, verify tests were actually run (not just written) with `-v` output showing real PASS lines (not vacuous), verify the per-task gates (gofmt/lint/vet/build) are clean, clean up any stray leak files from a prematurely-started next task, and confirm the worktree branch is not detached (`feedback_subagent_worktree_detach`) and the MAIN checkout stayed clean (`feedback_subagent_worktree_path_targeting`).
- **Task 3 is the load-bearing gate of this whole phase** — its subagent MUST run the FULL 96-fixture differential suite (`go test ./test/differential/... -run TestDifferential -count=1`, not a `-run` subset) and it must show ALL 96 fixtures passing, byte-identical to pre-refactor behavior. If ANY fixture fails, this is a STOP-and-diagnose moment (`superpowers:systematic-debugging`) — do not proceed to Task 4 with a red differential suite.
- Subagents commit LOCALLY only (`feedback_subagents_no_push`) — the controller squashes (if desired) and pushes ONCE at stage-close, after Task 6's docs-completion commit, per `feedback_push_to_origin`.
- Re-run the FULL project verification suite yourself on the FINAL frozen HEAD before declaring the stage done (do not trust a subagent's self-report of gates passing without independently re-running them) — per `superpowers:verification-before-completion`.

**After Task 6 lands and the six-gate is independently re-verified GREEN on the frozen HEAD:**
- Merge the worktree branch back to `master` (or open/land a PR per whatever integration convention this project uses — check recent phase-50 IMPL's git history for the exact merge mechanics: `git log --oneline --all -20` to see if prior phases fast-forward-merged or PR'd).
- Push `master` to `origin` (controller action, not a subagent action).
- Roll `next-prompt.txt` forward per Task 6 Step 7's own instruction: **row 51 will be `done` with NO phase 52 chartered yet** — this is the "none chartered, awaiting a human pick" state (per the `3d10670d` precedent), NOT the all-work-complete termination state (a human still needs to pick the next feature/row). Do NOT create the `stop` sentinel at that point — leave `next-prompt.txt` explicitly awaiting a human pick, and do not invent a phase 52 charter yourself.

## Honor throughout (the discipline)
- **Lifecycle = one stage per session** — but IMPL is itself a multi-task subagent-driven session (this is the one stage where "session" spans the full 6-task execution, per the project's own IMPL-stage precedent at phases 44-50).
- **Re-verify, do not assume.** The PLAN's line-number citations were correct against commit `78dcd772`+the SPEC/PLAN commits — re-confirm HEAD hasn't drifted before Task 2 starts (`git log --oneline -6`).
- **Process anchors:** ADR-0044 (ADR §Context landed at SPEC-51.md §12; §Decision+§Consequences land HERE) · ADR-0045 (sub-split gate — PLAN confirmed NO sub-split; re-confirm if any task's real LoC balloons unexpectedly) · ADR-0106 (row 51 flips `done` at THIS IMPL, no parent rollup) · next-free **ADR-0269** (ADR-0268 lands at this IMPL).
- **No differential-specific lessons apply to NEW fixtures** (this phase creates none) — but `reference_full_suite_race_after_background_mutator` DOES apply (Task 3's `-race` gate must run the FULL `internal/boot`/`cmd/envoy-go` packages, not a subset, since `main.go` still starts background goroutines unrelated to this refactor that could interact with it).
- `feedback_execution_style` / `feedback_git_worktrees` / `feedback_subagents_no_push` / `feedback_subagent_autocommit_claudemd` / `feedback_pertask_gofmt_lint` / `feedback_push_to_origin` — ALL apply at this stage, per the PLAN header's own framing.

## Read first (in order)
1. THIS FILE — the cold-start router (phase 51 `bootstrap-validate-mode` PLAN LANDED; the next action is its IMPL).
2. `docs/envoy-go/phases/51-bootstrap-validate-mode/PLAN-51.md` — the FULL 6-task TDD spine (read in full, not just the Orientation section — every task has exact code/commands that must be followed precisely, not reinvented).
3. `docs/envoy-go/phases/51-bootstrap-validate-mode/SPEC-51.md` — the design rationale behind the PLAN's task boundaries (§3 the construction-boundary extraction, §10 the resolved D-VALIDATE-* pins, §12 the ADR-0268 §Context draft Task 6 extends).
4. `docs/envoy-go/STATE.md` — the active-phase header (`phase 51 (bootstrap-validate-mode) PLAN done`) + the recorded NEXT (`phase 51 IMPL`) + the counts (1200 / 96 / 52 / 38 / ADR-0267, next-free ADR-0268).
5. `docs/envoy-go/ROADMAP.md` — row 51 `in-progress` + the "Operational tooling family" section, both reflecting PLAN-done.
6. `cmd/envoy-go/main.go` fresh against HEAD (re-confirm every line-number citation in PLAN-51.md before Task 2 starts — files evolve) + `internal/filter/network/builtins/builtins.go` (the pattern Task 2's package mirrors where applicable) + `internal/bootstrap/bootstrap_test.go` (the `sampleBootstrap` const + reject-arm tests Task 4 reuses) + `cmd/envoy-go/main_test.go` (the subprocess-test convention Task 5 extends).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_execution_style`/`feedback_git_worktrees`/`feedback_subagents_no_push`/`feedback_subagent_autocommit_claudemd`/`feedback_subagent_worktree_detach`/`feedback_subagent_worktree_path_targeting`/`feedback_pertask_gofmt_lint`/`feedback_push_to_origin`/`reference_full_suite_race_after_background_mutator` all apply at this IMPL stage. No differential-fixture-specific memories apply (this phase creates none).
