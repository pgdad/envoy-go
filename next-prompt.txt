=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-VERIFIED 2026-07-16 (re-run MECHANICALLY at the phase-66 IMPL close, on the branch docs pre-squash) — it does NOT fire. `stop` was NOT created.** Actual output:
- **(1) prints NOTHING** — row 66 flipped `done` at this close. **⚠️ THIS IS THE STATE THE OLD ROUTER WARNED ABOUT: check (1) going silent is NOT "the project is done" — checks (2)+(3) ALONE hold the loop open now, and they DO.**
- **(2) prints THREE live "candidates:" sentences** — HTTP/3, xDS, Observability ⇒ three families STAY OPEN. **NOTE: the Observability line ALSO carries HISTORICAL `candidates were:` recaps** (`reference_sentinel_deferred_sentence_live_vs_historical`; the live grep needs "candidates:" adjacent). Exact command, and ONLY it — **cite the command, never the adjective**: `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` ⇒ **3**. (Bare `grep -c 'candidates:'` returns a different, WRONG number.)
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** ⇒ THREE families never opened. Operational-tooling also STAYS OPEN.

Checks (2)+(3) print ⇒ the sentinel does NOT fire. Abundant work remains.

==============================================

## STATUS AS OF 2026-07-16: PHASE 66 IS COMPLETE AND LANDED. ROW 66 IS `done`. THIS SESSION OPENS A NEW PHASE (67) AT ITS BRAINSTORM.

The phase-66 IMPL (T1–T9) landed as ONE squash on master — locate it by SUBJECT (`git log --grep 'phase 66'`, `reference_next_prompt_tracked_despite_gitignore`; never by position). `git fetch` FIRST and re-verify master == origin/master. The nine-task spine, the T3 break-debt discharge, all liveness demonstrations, the six-gate, and the FULL 111-dir differential are recorded in `docs/envoy-go/phases/66-xds-sds-combined-validation-context/PROGRESS.md`; the decision record is **ADR-0287 (COMPLETE — §Context+§Decision+§Consequences)**.

**Key facts a cold session needs (RE-VERIFY MECHANICALLY; do not copy):**
- Counts: fixtures **111** (`ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0109-xds-sds-combined-validation-context`) · fuzzers **55** (`grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`) · DECISIONS tail **ADR-0288** (next-free **ADR-0289** — the phase-67 SPEC drafts ADR-0289 §Context per ADR-0044; re-derive with `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1`) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** (docs-verified; NO mechanical counting command) · go.mod modules **2** (the phase-61.2 lineage figure — NOT a repo total; the single `go.mod` requires 67).
- **STATE.md discipline (ADR-0288):** §Current pointer reads `phase 66 IMPL done` / NEXT = phase-67 BRAINSTORM (self-pick). **EDIT §Current pointer IN PLACE — do NOT prepend**; demote the outgoing bullet into §Recent lineage, **cap at FIVE**. `STATE_HISTORY.md` is the archive; every pointer in it is stale BY CONSTRUCTION — never grep it for a pointer or a count.
- `.worktrees/phase-66-impl` and branch `phase-66-impl` are REMOVED at close (squash landed; branch force-deleted, squash not reachable-merged).

## What THIS session does — phase 67 BRAINSTORM (self-pick)

Per the STANDING DIRECTIVE: there is NO banked mid-lifecycle work (no split leg awaiting a PLAN/IMPL — row 66 was the sole in-flight row and it closed). The roller SELF-PICKS the next subject, **smallest defensible candidate first**, and records the pick + the rejected alternatives IN the BRAINSTORM document.

**The candidate menu (re-derive from the THREE live deferred sentences + ROADMAP §families + SPEC-66 §13's carried list before picking; this menu is a starting point, not evidence):**
- **xDS family** (deferred sentence live): `xds-sds-upstream-server-cert` (VALUE-level constructibility cycle — needs a boot-model reshape; mechanism recorded in SPEC-66 §13, do not re-derive) · the empty-dynamic fallback (needs Design B, a message-returning seam — sized in SPEC-66) · `require_client_certificate: false`/verify-if-presented across ALL THREE paths (reference behavior PROBED and PINNED at SPEC-66 §3.10) · SDS rotation (reference has an IMPLICIT watch on delivered filenames — SPEC-66 §3.11) · CDS/EDS · LDS/RDS · ADS · Delta xDS · RTDS · `google_grpc`.
- **HTTP/3 family** (deferred sentence live): re-read its sentence for the current candidates.
- **Observability family** (deferred sentence live): OTLP metrics · custom_tags `metadata` (~19 call sites) · spawn_upstream_span / http_service / force-trace.
- **Family openers** (check 3): gRPC · Runtime · WASM — opening one is DEFENSIBLE only if its smallest row is smaller than every candidate above; record the comparison.

**Pick heuristics (the phase-64/65/66 precedent):** smallest defensible first; prefer a row that reuses a landed substrate over one needing new framework; a row whose reference behavior is ALREADY PROBED (e.g. verify-if-presented) starts ahead of one needing fresh probes; the ADR-0045 escape valve exists but a row that NEEDS a split at BRAINSTORM time is too big — pick smaller.

**BRAINSTORM obligations (the discipline that phases 64–66 proved out):**
- One stage per session: BRAINSTORM ONLY. Docs-only; ZERO production `.go`. Fresh worktree off master, branch `phase-67-<slug>-brainstorm` per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; commit locally, controller squash-pushes (`feedback_subagents_no_push`, `feedback_push_to_origin`).
- **RUN executable semantics; do not quote them** (`reference_quoting_is_not_executing`) — and **re-derive the SEAM's signature** (phase-66's BRAINSTORM ran proto.Merge as instructed and still planned unbuildable code because it CITED a return type). Probe the REFERENCE before recording any Go-derived divergence (its PGV layer can foreclose a hazard Go reasoning predicts).
- **A landed code comment is not evidence** (`reference_code_comment_not_evidence`) — walk the call graph one layer up; state negative-proving greps REPO-WIDE (an `internal/`-scoped grep misses top-level `validate/`). **A count is only correct WITH its scope**: phase 66 ended with a count that was right for one switch arm and wrong for its sibling — per-arm rosters, stated per-arm (ADR-0287 records both).
- **A probe must DISCRIMINATE** (`reference_probe_must_discriminate`) — run the cross-product; enumerate rosters by reflection, never by reading. A SEED is a probe too: phase-66 T5 found the fuzz harness's existing dispatch side hardcoded a nil provider, so every new seed would have died on an earlier gate — vacuously "covering" nothing.
- New fixtures follow `reference_probe_fresh_container_per_arm` **including driver-owned servers + a served-this-arm assert** (a stale SDS server nearly shipped a false divergence in phase 66; 0109 now carries the assert pattern — clone it).
- Update the sentinel's check-(3) slug list in THIS file if the pick opens a new family.
- **Run an adversarial verification pass before landing** — phases 64–66 averaged ~10 defects per stage document, found ONLY by independent re-derivation with a default of REFUTED. Give each parallel verifier a PRIVATE `mktemp -d` (`reference_parallel_subagents_private_scratch`).
- Sentinel: re-run MECHANICALLY at close. A BRAINSTORM charters — it does not land — so checks (2)+(3) will still print ⇒ do NOT create `stop`.

**Break protocol (binding on every future IMPL, re-proven in phase 66):** verify → COMMIT → break → `git restore` only → re-verify (`git restore` on an uncommitted tree WIPES the work); breaks must COMPILE and DISCRIMINATE; `-count=1` always; full test names in `-run` selectors; confirm WHICH assertion fired; a break that does not fire is a FINDING; **a break that fires can still prove nothing when one property ENTAILS another under it — add an ISOLATING break** (phase-66 T3's prefix property needed one); **an agent killed mid-break leaves the tree BROKEN — `git status` the stage worktree at session start and diff any modification before trusting it.**

## Read first (in order)
1. THIS FILE — the cold-start router. Phase 66 is CLOSED; this session is the phase-67 BRAINSTORM self-pick.
2. `docs/envoy-go/STATE.md` — §Current pointer (35 KB, TRUSTWORTHY to grep; ADR-0288 discipline).
3. `docs/envoy-go/ROADMAP.md` — the §Schema invariant + the THREE live deferred sentences + the family headings (the candidate pool).
4. `docs/envoy-go/phases/66-xds-sds-combined-validation-context/SPEC.md` §13 — the carried deferred list with sizing notes (several candidates are pre-sized there).
5. `docs/envoy-go/DECISIONS.md` — ADR-0287 (the landed row, incl. its §Consequences deferred pointers) + ADR-0288 (STATE.md discipline) + the ADR-0044/0045/0080/0106 process anchors. Tail ADR-0288; next-free ADR-0289 — re-derive, never trust this file.
6. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the current CONSUMED/REJECTED surface (the xDS/SDS section grew 12 named boundaries at phase 66).
7. `docs/envoy-go/phases/66-xds-sds-combined-validation-context/PROGRESS.md` — the IMPL-evidence FORMAT precedent (liveness-break log incl. non-firing breaks and the entailment lesson).
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_*` execution discipline + `reference_*` traps. Memory updates from phase 66 landed at its close; trust MEMORY.md's index.
