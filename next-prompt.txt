START a fresh cold-start session for envoy-go — **phase 27 IMPL (`network-filter-sni-cluster`) is DONE + final-review APPROVED + squash-merged + pushed**. There is NO open phase. This session OPENS the **next phase** — operator's choice of subject; the most likely is a fresh **phase-28 BRAINSTORM** selecting the next **§9 Network-filters-family** candidate (5 remain: `redis` / `mongo` / `kafka_broker` / `thrift` / `zookeeper`), per SKILL_ROUTING (no SPEC/PLAN exists for any new phase → `superpowers:brainstorming`).

Phase 27 IMPL landed at squash `ab8098f` (pushed to origin/master): the 9-task TDD execution of the 27 PLAN via `superpowers:subagent-driven-development` (fresh subagent per task + two-stage review; subagents local-only; controller squash-merged + pushed). Final whole-branch review APPROVED (no Critical/Important; 2 minor non-blocking notes). What landed: (i) the connection-scoped upstream-cluster-override seam (ADR-0219) — the NARROW typed `chainRuntime.upstreamClusterOverride` field (Q2/YAGNI; NOT a general filter-state primitive) + `SetUpstreamCluster(name string)` ON the `ReadFilterCallbacks` interface + the exported `UpstreamClusterOverride(ctx) (string, bool)` reader (NEW `internal/filter/network/upstreamcluster.go`; the writer half stays unexported/framework-internal) + `handleTerminal` ctx-threading iff override set (the `TerminalFilter.Handle` signature UNCHANGED — no HCM ripple); (ii) `tcp_proxy` made per-connection-cluster-resolving (`Filter.cluster` → `cm *cluster.Manager` + boot-resolved `defaultCluster`; `Handle` override-then-fallback; unknown override → zero-byte downstream close `tcpproxy: per-connection override cluster %q not found`; existing rejects/logs byte-stable; no-override path byte-exact — back-compat fixtures green); (iii) the `sni_cluster` filter (ADR-0220): NEW `internal/filter/network/snicluster/` config-less package (`TypeURL` via `proto.MessageName` const+pinning-test; `OnNewConnection` SNI-verbatim→`SetUpstreamCluster`+`Continue` [sticky-halt-safe]; `OnData` pass-through; the SECOND production mixed read→terminal chain `[sni_cluster, tcp_proxy]`), registered as the 6th built-in (single `builtins.RegisterBuiltins` insertion — no main.go edit, D27-S2) + the `sni_cluster/v3` proto blank-import in `internal/bootstrap/bootstrap.go` (echo precedent). Plus the end-to-end override-routes/unknown-close integration tests in the `builtins` package (the only no-import-cycle home; FOO/FALLBACK distinct sentinels; liveness proven by two seam breaks) and the `0045-sni-cluster` 3-arm cross-side TLS differential fixture (route / empty-SNI-fallback / unknown-cluster-close; route/fallback arms made live via `DistributionAsserter` per-backend accept counts; `closeOK` normalization [ref pre-handshake vs subj post-termination close — both zero app bytes, D27-S3] documented; deliberate-break proven).

## What this session must do

There is no active phase. Per SKILL_ROUTING (no SPEC/PLAN for any new phase → state 0/1), the next lifecycle step is a fresh **phase BRAINSTORM** (`superpowers:brainstorming`) for the operator-chosen next phase. The strong default is the next §9 Network-filters-family candidate (the family stays OPEN; 5 remain). Confirm the subject with the operator if ambiguous; otherwise open a brainstorm for a §9 candidate. After BRAINSTORM → SPEC → PLAN → IMPL per the established per-phase lifecycle (`feedback_execution_style`: subagent-driven execution once a PLAN exists; subagents commit LOCAL-ONLY [`feedback_subagents_no_push`]; the controller squash-merges + pushes at stage-close). Work in a git worktree (`feedback_git_worktrees`); per-task `gofmt -l` + `golangci-lint` on touched pkgs (`feedback_pertask_gofmt_lint`).

## Project state at master tip

- **Master tip:** `9417274` (the `last-commit`-fill / next-prompt-rewrite commit, this repoint) — trails the substantive 27 IMPL squash `ab8098f`. Confirm the live tip via `git log --oneline -1`; any further next-prompt repoint advances the tip +1 (expected, not chased). STATE.md `last-commit` points at the substantive 27 IMPL squash `ab8098f`. origin/master in sync after push.
- **§9 Network-filters family:** parent row 26 DONE; 26.1 / 26.2 / 26.3 done; row 27 (`network-filter-sni-cluster`) **done**. The family stays OPEN — 5 candidates remain (`redis` / `mongo` / `kafka_broker` / `thrift` / `zookeeper`).
- **Counts at master tip:** stat surface **136** (+0 at 27); differential fixtures **47** (tail `0045-sni-cluster`); fuzzers **36** (+0 at 27, D27-S4 DEFER); conformance 10/10; h2spec 53/53; DECISIONS.md tail **ADR-0220** (bodies in place); next-free **ADR-0221** (ADR-0209 escape-valve reserve STANDS-UNCONSUMED).
- **Toolchain:** Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); reference Envoy v1.37.2 (ADR-0008); go-control-plane v1.32.4 (ADR-0008).
- **Known coverage boundaries (carried, non-blocking):** D-26.3-4 — the rbac_network SNI/mTLS L4 arms unit-tested but not cross-side differential-proven (carried from 26.3). Phase-27: the `tcp.<stat_prefix>.downstream_cx_no_route` family (and wider `downstream_cx_*`) is a known-unmirrored upstream counter (BEHAVIOR_CONTRACT coverage-boundary record); the narrow typed override is the envoy-go stand-in for Envoy's `envoy.tcp_proxy.cluster` filter-state key (no general filter-state primitive — Q2).

## Read first (in order)

1. THIS FILE — the next-phase cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 27 IMPL done`; next-skill `superpowers:brainstorming`; next-free `ADR-0221`; last-commit `ab8098f`.
3. `docs/envoy-go/ROADMAP.md` — rows 26/26.1/26.2/26.3/27 all `done`; the §9 family-open note + the 5 remaining candidates.
4. `docs/envoy-go/SKILL_ROUTING.md` + `BOOTSTRAP_PROMPT.md` §4/§5 — the per-phase lifecycle + the cold-start invariants.
5. `docs/envoy-go/phases/27-network-filter-sni-cluster/{SPEC.md,PLAN.md,PROGRESS.md}` + `docs/envoy-go/phases/26.3-network-filter-rbac/` — the two most recent §9 filters, for precedent on how a network filter is specced/planned/implemented (the `internal/filter/network/` framework, the `builtins.RegisterBuiltins` seam, the override seam if a future filter needs per-connection state, the differential-fixture conventions incl. `DistributionAsserter`).
6. `docs/envoy-go/DECISIONS.md` ADR-0213/0214/0215 (the §9 framework) + ADR-0216/0217/0218 (rbac family) + ADR-0219/0220 (the override seam + sni_cluster) + ADR-0106 (ROADMAP flip discipline) + ADR-0044 (in-place ADR-body discipline) + ADR-0045 (split-gate) + ADR-0052 (atomic doc bundle).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — execution-style + worktree + push + subagents-no-push + per-task gofmt/lint + the network-filter constraints (typeurl-extensions, onnewconnection-halts) + the differential dispatch/asserter constraints all carry forward to the next §9 filter.
