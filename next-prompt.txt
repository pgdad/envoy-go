START a fresh cold-start session for envoy-go — phase 26.1 (`network-filter-chain-framework-and-echo`) IMPL is DONE; author the **phase 26.2 SPEC** (`network-filter-registry-migration`).

This is a per-sub-phase SPEC cold-start. **Phase 26.1 IMPL is DONE + squash-merged to master + pushed to origin** (squash `3ee1955` — NEW `internal/filter/network/` L4 read-filter chain framework + `echo` + `direct_response` + `manager.go` dual-dispatch + `main.go` boot-wiring + ADR-0213/0214 §Decision/§Consequences bodies + BEHAVIOR_CONTRACT 26.1 bundle + 35th fuzzer + differential fixtures 0040/0041/0042). The 17-task TDD PLAN executed via `superpowers:subagent-driven-development` (fresh subagent per task, two-stage spec+quality review; one CRITICAL echo-multiread bug found+fixed at IMPL Task 11; one empirical SPEC correction — echo TypeURL carries the `extensions.` segment). Six gates GREEN; full differential suite byte-exact incl. R4 back-compat. STATE.md points at `phase 26.1 phase-done; awaiting 26.2 SPEC`, next-skill `superpowers:writing-plans` (scoped to the 26.2 per-sub-phase SPEC), next-free `ADR-0215`.

## What this session must do: author the phase-26.2 per-sub-phase SPEC

Per SKILL_ROUTING (sub-row 26.2 at state 1 — no 26.2 SPEC.md yet), the next-skill is **`superpowers:writing-plans`** scoped to authoring the **26.2 SPEC** (`docs/envoy-go/phases/26.2-network-filter-registry-migration/SPEC.md`), per the phase-22.1 / 25.1 per-sub-phase-SPEC-authoring precedent. The 26.2 SPEC INHERITS the phase-26 parent SPEC's §5 proto roster + §6 PARSE-REJECT roster + §7 stat surface + §8 fixture taxonomy + §11 empirical-pin block + §12 D-questions + §13 RATIFIED-PENDING items and refines per-Task-level surface only (it does NOT re-execute the parent empirical pins). It consumes **ADR-0215** (the §Context draft already landed at the parent SPEC; the 26.2 SPEC anchors the §Context refinement, with §Decision/§Consequences landing at 26.2 IMPL per ADR-0044). DECISIONS.md tail is **ADR-0214**; next-free **ADR-0215**.

## Phase 26.2 scope (settled at the parent SPEC + ROADMAP row 26.2; ADR-0215)

**`network-filter-registry-migration`** — migrate the two existing terminal network filters onto the NEW `internal/filter/network/` read-filter framework + registry that 26.1 landed, and RETIRE the hardcoded `manager.go` terminal-filter registry (`filterRegistry` / `buildTerminalFilter` / `filterHandler` / the `Handle` terminal path):
- **`tcp_proxy`** (`internal/filter/tcpproxy/`) — migrate to the `network.ReadFilter` interface + register in `*network.Registry` (currently dispatched via the hardcoded `filterRegistry` map + `Handle`). tcp_proxy is a terminal read filter (routes L4 to an upstream cluster) — assess whether it fits the read-filter `OnData` model or needs a terminal-filter extension to the framework (a parent-SPEC D-question to resolve at the 26.2 SPEC/PLAN).
- **HCM** (`internal/filter/hcm/`) — migrate the HTTP connection manager onto the same registry/dispatch path.
- **Retire the dual-dispatch's "old path":** 26.1 left the terminal-filter path UNTOUCHED alongside the new read-filter path (the `network-filter-mixed-chain-unsupported` 26.1-transitional boot-reject was explicitly "lifted at 26.2"). 26.2 unifies them — once tcp_proxy/HCM are network read filters, the dual-dispatch collapses to a single registry-driven path and the mixed-chain restriction lifts (mixed read+terminal chains become expressible).
- Likely framework extensions the migration surfaces (resolve at 26.2 SPEC): a terminal/write-filter or upstream-routing surface for tcp_proxy; how HCM's per-stream pipeline composes under the L4 read-filter chain; the `FactoryCtx` may need to carry the cluster manager / httpClient / stats registry the terminal filters consume (26.1's `FactoryCtx` carries only `BaseDir`).

This is a MIGRATION/refactor sub-phase (move existing behavior onto the new framework at byte-exact parity — R4 back-compat is paramount; the existing tcp_proxy/HCM differential fixtures + conformance + h2spec must stay byte-exact green). NO new operator-visible feature; stat surface unchanged at 132 (verify). The `rbac_network` filter + connection-metadata sink is 26.3 (consumes ADR-0216/0217/0218).

## Project state at master tip

- **Master tip:** `964fd26` (a next-prompt.txt repoint commit; trails the 26.1-IMPL stage-close `92af220` and the IMPL squash `3ee1955`). Confirm via `git log --oneline -1` — the commit landing any further next-prompt repoint will itself advance the tip +1, expected and not chased. origin/master in sync after push. STATE.md `last-commit` points at the substantive squash `3ee1955` (not the repoint tip).
- **§9 Network-filters family OPEN:** parent row 26 `in-progress`; sub-row 26.1 `done`; 26.2 `planned` (flips `planned → in-progress` at the 26.2 SPEC commit per ADR-0106); 26.3 `planned`. Parent row 26 flips `in-progress → done` at 26.3 phase-done (ROLLUP precedent 18/19/22/24/25).
- **Counts at master tip:** stat surface **132**; differential numbered-fixtures **44** (tail **0042**); fuzzers **35**; conformance 10/10; h2spec 53/53; DECISIONS.md tail **ADR-0214**, next-free **ADR-0215**. 26.2 is a migration: anticipated NO new fixtures/fuzzers/stats deltas (verify at the 26.2 SPEC §11 D-S1 baselines; the migration may ADD a mixed-chain fixture once the restriction lifts — a 26.2 SPEC decision).
- **Toolchain:** Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); reference Envoy v1.37.2 (ADR-0008); go-control-plane v1.32.4 (ADR-0008).

## Read first (in order)

1. THIS FILE — the phase-26.2 SPEC cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 26.1 phase-done; awaiting 26.2 SPEC`; next-skill `superpowers:writing-plans` (26.2 SPEC); next-free `ADR-0215`.
3. `docs/envoy-go/phases/26-network-filter-chain-and-rbac/SPEC.md` — the PARENT SPEC (the §5/§6/§7/§8/§11/§12/§13 rosters the 26.2 SPEC inherits; the §2/§3 sub-phase scope split; the AMEND-A1..A11 catalog; the ADR-0213..0218 §Context drafts — esp. ADR-0215 for the registry migration).
4. `docs/envoy-go/phases/26.1-network-filter-chain-framework-and-echo/SPEC.md` + `PLAN.md` — the per-sub-phase SPEC/PLAN PRECEDENT to mirror (structure, §11 empirical-pin discipline, §10 task-spine shape, §15 acceptance checklist) + the framework API the migration builds on.
5. `docs/envoy-go/phases/26.1-network-filter-chain-framework-and-echo/PROGRESS.md` — the 26.1 IMPL record (the as-built framework surface, the echo-TypeURL correction, the chain runtime per-socket-read re-iteration semantics, the dual-dispatch left additive for 26.2 to unify).
6. The MIGRATION TARGETS: `internal/filter/network/{types,callbacks,chain,registry}.go` (the framework to migrate onto); `internal/listener/manager.go` (the dual-dispatch + the hardcoded `filterRegistry`/`buildTerminalFilter`/`filterHandler`/`serveConnection` terminal path to retire/unify); `internal/filter/tcpproxy/filter.go` (`Handle` — the tcp_proxy terminal filter to migrate); `internal/filter/hcm/` (the HCM terminal filter to migrate).
7. `docs/envoy-go/ROADMAP.md` rows 26/26.2; `BOOTSTRAP_PROMPT.md` §5 (state machine) + §4.1 STATE invariant + ADR-0004/0005 (autonomous SPEC adaptation) + ADR-0044 (in-place ADR-body discipline) + ADR-0106 (ROADMAP flip-on-SPEC); `docs/envoy-go/SKILL_ROUTING.md` (confirm sub-row 26.2 state 1 → writing-plans for the SPEC).
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — execution-style + worktree + push + subagents-no-push + differential-asserter-dispatch + fixture-dispatch-constraint + network-factoryctx-basedir all apply.

After the 26.2 SPEC is authored + reviewed + squash-merged + pushed, advance STATE.md per BOOTSTRAP §5 (ROADMAP sub-row 26.2 `planned → in-progress` per ADR-0106; parent row 26 STAYS `in-progress`) and rewrite next-prompt.txt for the 26.2 PLAN cold-start (`superpowers:writing-plans` scoped to the 26.2 per-sub-phase PLAN that decomposes the 26.2 SPEC §10 task spine).
