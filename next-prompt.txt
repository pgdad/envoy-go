=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-RUN MECHANICALLY 2026-07-24 at the phase-73 IMPL close. It does NOT fire. `stop` was NOT created.** ACTUAL output, recorded rather than predicted:
- **(1) prints NOTHING.** ⚠️ **This is the FIRST close at which check (1) is silent, and it is EXPECTED: row 73 was the LAST non-`done` row, and it flipped at this IMPL's six-gate.** A silent check (1) is the **NORMAL post-IMPL state** and is **NEVER on its own a termination condition** — all THREE checks must be silent. Do not read it as "the project is finished".
- **(2) prints THREE live "candidates:" sentences** — HTTP/3, xDS, Observability. **Cite the command, never the adjective** (`reference_sentinel_deferred_sentence_live_vs_historical`: the live grep needs `candidates:` adjacent; `candidates **were**:` recaps are historical): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` => **3**. The Observability sentence NARROWED at this row-done edit (the phase-57 precedent — it narrows at the IMPL, at no earlier stage) and now carries **FOUR** candidates: the `ssl` stat family + `spawn_upstream_span` + `http_service` + force-trace.
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** => THREE families never opened. (The `WASM` line remains a ROADMAP **bookkeeping artifact** — `internal/wasm/` is a substantially-built wazero host and rows 25/25.1/25.2/25.3 are all `done` — deliberately LEFT AS-IS, since the stage-close mechanics depend on this output.)

Checks (2)+(3) print => the sentinel does NOT fire. Abundant work remains.

⚠️ **CHECK (1) HAS A KNOWN BLIND SPOT, and it is BIGGER than previously recorded — FOUR of 105 rows, for THREE distinct reasons** (PRE-EXISTING, no action owed; RE-DERIVED mechanically at the phase-73 IMPL by set-differencing every table row against the rows check (1) actually matches, NOT by inspecting the regex). Check (1) sees **101 of 105** rows. The four it cannot see:
- `| 00 | bootstrap | — | done |` — the field `[0-9.,  ]*` does not match an **em-dash** "after" column.
- `| 04 | http-1.1 | 03 | done |` — the slug field `[a-z0-9-]+` does not match the **dot** in `http-1.1`.
- `| 28.1a | … | done |` and `| 28.1b | … | done |` — **NEWLY FOUND at the phase-73 IMPL**: the number field `[0-9.]+` does not match a **LETTER SUFFIX**. The earlier "2 of 103" figure came from a set-difference whose OWN baseline regex also excluded these rows, so it under-counted its own blind spot.

All four are `done`, so there is no current impact — but check (1) is **not exhaustive**, and any future dotted slug, em-dash cell **or letter-suffixed row number** goes invisible too. That matters given how much of the stage-close mechanics leans on it.

==============================================


## STATUS AS OF 2026-07-24: THE PHASE-73 IMPL IS COMPLETE AND LANDED. ROW 73 IS `done`. PHASE 73 IS FINISHED. THIS SESSION PERFORMS THE PHASE-74 BRAINSTORM — AND THE ROLLER SELF-PICKS ITS SUBJECT.

**What the phase-73 IMPL landed** (`git log --grep 'phase 73'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): the tracing `custom_tags` `metadata` type for the **`CLUSTER`** MetadataKind — the **FOURTH and LAST** of the four kinds, so **the quartet is CLOSED and no MetadataKind rejects any more** (`REQUEST`@70, `ROUTE`@71, `HOST`@72, `CLUSTER`@73). A 9-task single-flat-row TDD spine: a second aliasing `Endpoint.clusterFilterMetadata` field + a nil-safe `ClusterMetaLookup` accessor on `cluster.Endpoint` populated by a placement-pinned loop in `buildCluster`; `kindMetadataCluster` at iota==6 + the CLUSTER accept arm; a nil-tolerant `clusterMetaLookup` 6th `ResolveCustomTags` param + a FULL-`MetaPath` resolve arm; `picked.ClusterMetaLookup` at the three emit call sites; a `meta_cluster_ok` fuzz seed + the mandatory `meta_bad` repoint; the two-cluster OTLP fixture `0117`; BEHAVIOR_CONTRACT B1–B5; the six-gate; ADR-0295 COMPLETED IN PLACE. **Six-gate ALL GREEN, the FULL 119-dir differential `ok 410.259s` EXIT 0 with ZERO failures. EVERY break fired — not one failed to fire.**

### What matters for the NEXT session, in order

1. **⚠️ THE SENTINEL DID NOT FIRE AND `stop` WAS NOT CREATED — despite check (1) going silent for the first time.** Row 73 was the last non-`done` row. Re-run all three checks yourself and record the ACTUAL output; do not predict it, and do not treat a silent check (1) as a finish line.

2. **⚠️ THREE OF THE PHASE-73 PLAN'S OWN INSTRUCTIONS WERE REFUTED BY EXECUTION.** They are recorded in `PROGRESS.md` and ADR-0295 §Consequences because each is a reusable defect class. **(a) A caller-count gate on a test file that the same task ADDS tests to is UNSATISFIABLE BY CONSTRUCTION** — every new emit test IS a new caller; phase 73 pinned 13+21=34 and landed 13+**29**=42. This is the SAME defect the PLAN had already diagnosed TWICE for *sha256* gates and then re-committed one level down when it downgraded the mechanism. **Gate the invariant, not a count your own task moves** (pre-existing callers byte-identical at unchanged line numbers + a leading-region hash + ZERO deletion lines). **(b) A production-line insertion silently invalidates prose citations elsewhere** — T1's +23 lines moved a landed BEHAVIOR_CONTRACT cite from `cluster.go:722` to `:745`; the obligation is the SWEEP, not the single fix. **(c) A break can need TWO breaks at TWO LAYERS** — a test that hand-constructs its input never reaches a build-time populate loop.

3. **⚠️ FOUR STALE CLAIMS WERE CORRECTED AT THE PHASE-73 IMPL. DO NOT RE-INTRODUCE ANY OF THEM.** The ROADMAP row-73 cell's three (the narrow REMOVES-vs-SHORTENS hazard — it SHORTENS, leaving FOUR candidates and check (2) STAYING 3; the "only the two unset-kind arms remain" clause — it is **TWO reject ARMS across THREE config spellings**; the single-cluster `0117` description — superseded by the TWO-CLUSTER decision), **plus the ADR-0295 HEADING's two over-claims**, which the SPEC's own §16 asserted were already fixed and were not. **A document's account of its own corrections is not evidence that they landed** (`reference_a_drift_correction_is_itself_a_claim`). Also retired: ADR-0294's *"`CLUSTER` RE-PAYS the 18-caller threading tax"* close, now carrying a bracketed correction — **a deferral's COST ADJECTIVE must be re-derived, not re-read**.

4. **The deferred roster the roller picks from.** After this row's narrow the live Observability `candidates:` sentence carries FOUR: the `ssl` stat family (ADR-0286 C3 framework surgery), `spawn_upstream_span`, `http_service`, force-trace. Beyond the sentence, and **named in ADR-0295 §Consequences / BEHAVIOR_CONTRACT B2 rather than in it — a DELIBERATE, recorded omission, not an oversight**: the **absent-`metadata_key` DEPARTURE** (the reference ACCEPTS a `metadata` tag with no `metadata_key` and emits `default_value`; envoy-go rejects `empty namespace`) and the **two error-string COLLISIONS** (`empty namespace` spans a PARITY and a DEPARTURE case; both surviving unset-kind arms now emit `kind required`) — splitting either changes landed error strings, ADR-0080 territory. Other live candidates carried from the phase-73 BRAINSTORM/SPEC: the **pick-time `picked`-propagation row** (its cost is now better known — a CLUSTER tag is the FIFTH consumer, the circuit-breaker arm is **EXECUTED-PARITY**, and **pool-overflow is the SOLE remaining unprobed arm**); **`access_log[].filter`** (RE-FLAGGED the strongest FUTURE pick — a KNOWN field ACCEPTED and SILENTLY IGNORED, proven by execution, with the global sink-flattening and `headerCaptureSink` non-promotion hazards on record); `hcm.access_log_options`; the `stdout`/`stderr` access loggers; `stats_flush_on_admin`; OTel `sampler`/`resource_detectors`; tracing `verbose`; a 6th stats sink (effectively vacuous — only `wasm` is unconsumed); all xDS and HTTP/3 candidates; and the never-opened families (gRPC is gated on an HCM response-trailer framework row; Runtime is genuinely unbuilt; **WASM is a bookkeeping artifact**).

5. **Live hazards — never reflex-classify any of these as a regression.** The PRE-EXISTING `internal/cluster` `-race` flake (`TestOutlierDetector_ConcurrentEjectExactlyOnce`, `outlier_test.go:766` — isolate-re-run, do NOT re-classify; it did NOT reproduce at phase 73's gate) · the full-suite startup flake (`subject ready: EOF` on an UNRELATED fixture) · the SDS init-fetch dial-budget flake · and **TWO newly-observed, still-UNINDEXED load flakes** in byte-identical packages, isolate-green and NOT root-caused: `internal/httpclient TestOptions_ZeroValue_NoOpDefaults` and `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery`.

**Counts at exit (re-run MECHANICALLY in the worktree; never copy them):** fixtures **119** (`ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0117-tracing-custom-tags-metadata-cluster`; `0118` and reference port `10118` both FREE) / fuzzers **55** (`grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`) / stat surface **1201** (BEHAVIOR_CONTRACT doc count; NO mechanical command) / BackendKind tail **38** / go.mod modules **2** (the phase-61.2 lineage figure; the single `go.mod` requires **67**) / DECISIONS tail **ADR-0295** COMPLETE (next-free **ADR-0296**) / ZERO new packages this row.

## What THIS session does — the phase-74 BRAINSTORM

One stage per session. **The roller SELF-PICKS the subject** per the 2026-07-12 standing directive: the smallest defensible ROW-SIZED candidate, established by independent read-only cost assessment at the CURRENT tip, over a **fully-adjudicated rejected roster** recorded in the BRAINSTORM. There is no banked mid-lifecycle work — phase 73 closed cleanly, so this opens a brand-new subject.

**The owed roster:**
1. **Re-derive the candidate costs AT TIP — do not re-read them.** `reference_deferred_candidate_cost_restale`: landed substrate SHRINKS a deferred row, and this has now shrunk the pick on three consecutive rows. Two of the last three headline cost adjectives were REFUTED by re-derivation. Where a prior BRAINSTORM assigned an adjective, go look at the code; where two agents SPLIT on a size, **the split resolves AGAINST the candidate** (the phase-72 precedent).
2. **A landed CODE COMMENT is not evidence** (`reference_code_comment_not_evidence`) — phase 73's predecessor found a comment that was TRUE of a parameter list and FALSE of reachability one layer up, and it had been discouraging a cheap row for several phases. Walk the call graph one layer UP.
3. **Register the new row `in-progress` at the BRAINSTORM stage-close commit** per the ROADMAP §Schema invariant; it flips `done` only at that phase's IMPL six-gate (ADR-0106, the SOLE leg for a single flat row). **NO deferred sentence is narrowed at ANY pre-IMPL stage** (the phase-57 precedent) — narrowing it early breaks check (2)'s meaning.
4. **Docs-only, ZERO production `.go`.** DECISIONS is UNTOUCHED at a BRAINSTORM (the ADR's §Context lands at the SPEC per ADR-0044). Re-run every count MECHANICALLY in the worktree.
5. **Close by re-running all three sentinel checks mechanically, TWICE** — in the stage worktree AND on LANDED master after the squash-push (the phase-72/73 precedent: a stage worktree can disagree with what actually landed, and the post-push re-run is the one that counts). **Create `stop` if and ONLY if all three print nothing.** Registering a new row will make check (1) print again.

**Stage-close mechanics (a BRAINSTORM):** STATE §Current rolled **IN PLACE** (lifecycle DONE → 1; the §Recent lineage re-capped at FIVE **with its PREAMBLE updated** — the ADR-0288 rule). ⚠️ **The ADR-0288 singleton greps return 2, not 1**, for each of `next-skill:` / `lifecycle-state:` / `next-free ADR:` — the second hit is `STATE.md:7`, the RULE STATEMENT itself. Verify with `grep -n`, and **never "fix" the count to 1**; that would delete the rule. `next-prompt.txt` rolled to the phase-74 SPEC.

Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`).

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. At the phase-72 close that put a stray commit on `master` in the MAIN checkout AND made a "final gates" run validate master's pre-row tree — GREEN while proving nothing. **Use `git -C <abs-worktree-path>` for every git command, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline) + §Project counts + the §Recent lineage (five bullets: phase-73 IMPL + phase-73 PLAN + phase-73 SPEC + phase-73 BRAINSTORM + phase-72 IMPL).
2. `docs/envoy-go/ROADMAP.md` — the §Schema invariant, the family headings, and the THREE live `candidates:` sentences. **Row 73 is `done`; every chartered row is `done`.**
3. `docs/envoy-go/phases/73-tracing-custom-tags-metadata-cluster/PROGRESS.md` — the phase-73 IMPL record: the per-task outcomes, the FULL break ledger (every break's actual firing assertion), the THREE PLAN refutations, and the re-derived check-(1) blind spot. **The most transferable content in the phase directory.**
4. `docs/envoy-go/phases/73-tracing-custom-tags-metadata-cluster/BRAINSTORM.md` §2.1/§11 and `SPEC.md` §13 — the fully-adjudicated rejected roster and the deferred-item ledger the next pick is chosen from. **Both are ONE row stale by construction: re-cost, do not re-read.**
5. `docs/envoy-go/DECISIONS.md` tail — **ADR-0295** (COMPLETE — the CLUSTER row, incl. the three PLAN refutations in §Consequences) + ADR-0294 · ADR-0044 · ADR-0045 · ADR-0080 · ADR-0106 · ADR-0288.
6. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the `custom_tags` paragraph (now CONSUMED for all four MetadataKinds) and the pick-time departure clause (B3), whose SOLE unprobed arm is pool-overflow.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — trust MEMORY.md's index. Load-bearing for a fresh pick: `reference_deferred_candidate_cost_restale`, `reference_code_comment_not_evidence`, `reference_a_drift_correction_is_itself_a_claim`, `reference_probe_must_discriminate`, `reference_lifted_reject_hidden_enforcement`, `reference_retention_field_populate_before_value_copy` (NEW at phase 73), `reference_endpoint_metalookup_host_metadata_seam`, `reference_host_tag_resolves_at_pick_time`, `reference_cluster_tag_gated_on_pick_not_route`, `reference_cluster_race_outlier_flake`, and **`reference_bash_cwd_reset_commits_to_main`**.
