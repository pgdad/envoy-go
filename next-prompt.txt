=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, the active phase is fully landed, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action. (The sentinel does NOT apply right now — see below.)

**STATUS AS OF 2026-07-06: phase 52 (`load-balancer-locality-weighted`) BRAINSTORM is LANDED (row 52 registers `in-progress`). The NEXT action is the phase-52 SPEC.**

- A human picked **locality-weighted LB** (`Cluster.CommonLbConfig.locality_weighted_lb_config`) as the SIXTH Load-balancing-family row, from the 3-candidate roster left after phase 38 {locality-weighted LB, priority load balancing, panic thresholds} — all three UNBLOCKED by the phase-39-through-43 Upstream-robustness family (active health checks + outlier detection + circuit breakers + retries + connection pooling).
- The BRAINSTORM (`docs/envoy-go/phases/52-load-balancer-locality-weighted/BRAINSTORM.md`, the 12-section phase-38 shape) settled, via a 4-question user dialogue: Q0 subject = locality-weighted LB; Q1 mechanism scope = `locality_weighted_lb_config` ONLY (the sibling `zone_aware_lb_config` oneof arm is OUT of scope and EXPLICITLY REJECTED when configured, not silently ignored); Q2 subset-LB composition = kept INDEPENDENT, deferred again (both `lb_subset_config` and `locality_weighted_lb_config` configured on one cluster is an explicit reject); Q3 child-policy scope = mirror `subsetLB`, ALL FIVE leaf policies via factory reuse; Q4 differential proof = BOTH a static weight-ratio arm AND a health-degradation shift arm (the load-bearing novel proof — this is the family's FIRST construct to consume health as a continuous weight, not a binary filter).
- Architecture: a `localityWeightedLB` wrapper needing **ZERO new `Pick` parameters** (unlike phase 38's subsetLB, which BUILT a seam pick-input) — it resolves locality selection entirely from per-cluster build-time state (two new `Endpoint` fields, `Locality`/`LocalityWeight`, mirroring phase 38's `Metadata` precedent) plus the EXISTING phase-39/40 `clusterHealth` state, consumed via a NEW per-locality health-fraction aggregation. Delegates to a per-locality child (built by the existing policy factory) unchanged. Cluster-wide panic reuses the EXISTING `inPanic` gate one layer higher. **1 ADR anticipated** (ADR-0269 — a single-ADR "reuse" phase, the phase-35/37 shape, since there's no seam to build). NO new producer plane (cluster-only construct — the first LB phase since round_robin with zero per-route surface).
- Counts UNCHANGED at this BRAINSTORM (stat 1200 / fixtures 96 / fuzzers 52 / BackendKind 38 / DECISIONS tail ADR-0268, next-free ADR-0269). §10 lists D-LW1..D-LW8 — the empirical pins the SPEC executes IN-SESSION against the contrib reference Envoy (`envoyproxy/envoy:contrib-v1.37.2`, ADR-0227) via `reference_docker_probe_bridge_network`. D-LW3 (the effective-weight degradation formula) and D-LW4 (the panic-mode interaction) are the two most load-bearing SPEC-BLOCKING live probes.
- STATE.md/ROADMAP.md/next-prompt.txt updated (docs-only, authored in worktree `.worktrees/phase-52-brainstorm` branch `phase-52-load-balancer-locality-weighted-brainstorm`; controller squashes + pushes at stage-close per `feedback_push_to_origin`).

=> The NEXT action is **the phase-52 SPEC** — author `docs/envoy-go/phases/52-load-balancer-locality-weighted/SPEC.md` (`superpowers:writing-plans` scoped to SPEC authoring, the kafka-31/thrift-33/least_request-34/random-35/ring_hash-36/maglev-37/subset-38 precedent), executing the §10 D-LW1..D-LW8 empirical pins IN-SESSION against the contrib reference Envoy per ADR-0004/ADR-0227 (`reference_docker_probe_bridge_network` — bridge network + STRICT_DNS backend hostnames + verify traffic ran via `downstream_cx_rx_bytes_total > 0`), anchoring the ADR-0269 §Context draft, and confirming the single-flat-row envelope (§1.4 of the BRAINSTORM; no pre-authorized split anticipated).

==============================================

## Read first (in order)
1. THIS FILE — the cold-start router.
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 52 BRAINSTORM done`) + history.
3. `docs/envoy-go/ROADMAP.md` — row 52 (`in-progress`) + the Load-balancing family narrative section (confirms the 3→2 candidate-count drop anticipated after phase 52).
4. `docs/envoy-go/phases/52-load-balancer-locality-weighted/BRAINSTORM.md` — the FULL 12-section brainstorm (§10 lists the exact D-LW1..D-LW8 empirical pins the SPEC must execute; §2 has every design decision + rationale; §11 lists prior-phase lessons applied).
5. `docs/envoy-go/phases/38-load-balancer-subset/{BRAINSTORM,SPEC}.md` — the most recent same-family phase, useful as a SPEC shape template (subset's `subset.go` wrapper-owns-children pattern is the direct structural precedent for this phase's `localityWeightedLB`).
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — re-check for any entries relevant to locality-weighted LB, health-check propagation warmup, or oneof-sibling-reject discipline.

## Honor throughout (the discipline, unchanged for whichever phase comes next)
- **Lifecycle = one stage per session** (BRAINSTORM → SPEC → PLAN → IMPL).
- **Subagent-driven execution** (`feedback_execution_style`) in a **fresh worktree** (`feedback_git_worktrees`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit, re-runs the full suite on the frozen HEAD, does any deliberate-break verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). Per-task gates: `gofmt -l` + `golangci-lint` + `go vet` + `go build ./...` (`feedback_pertask_gofmt_lint`).
- **Process anchors:** ADR-0106 (per-leg rows) · ADR-0044 (ADR §Decision+§Consequences land at IMPL) · ADR-0045 (sub-split soft gate) · next-free **ADR-0269**.
- Re-verify baselines fresh at cold-start — do NOT trust any count in this file without re-running the baseline commands (`ls -d test/fixtures/*/ | wc -l`, `grep -rh '^func Fuzz' --include='*.go' . | wc -l`, etc.) against current HEAD.
