=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **PHASE 90 (`h2-host-authority-normalization`) IS OPEN AT LIFECYCLE-STATE 1. ROW 90 IS `in-progress` AT `ROADMAP.md:152` AND `want` IS 122.** ⇒ **THERE IS BANKED MID-LIFECYCLE WORK. THIS SESSION WRITES THE PHASE-90 SPEC. DO NOT SELF-PICK A NEW SUBJECT.** ⚠️ **THE SENTINEL DOES NOT FIRE — check (1) prints `NOT DONE: row 90`, check (2) prints SIX, check (3) is SILENT. DO NOT create `stop`.** ⚠️ **MEASURE ALL THREE YOURSELF BEFORE DOING ANYTHING ELSE; do not take this file's word for it.**

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=122 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **CHECK (1) NOW HAS ITS OWN LIVENESS AGAIN — IT PRINTS `NOT DONE: row 90` WHILE PHASE 90 IS OPEN.** That is the HEALTHY reading, not a defect. Run BOTH doctoring NCs EVERY SESSION anyway:
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md.
```
⚠️ **THE NC-A SIGNATURE CHANGES SHAPE WITH THE BOARD STATE — THIS BURNED A DRAFT AT THE PHASE-90 BRAINSTORM.** On an ALL-`done` board it reads `NOT DONE: row 62` **ALONE**. **While row 90 is in-progress it reads `NOT DONE: row 62` AND `NOT DONE: row 90`, and BOTH lines are required** — row 62 proves the check is live, row 90 proves the row is still registered. **A reading of `row 62` alone now would mean THE ROW WAS LOST.** Do not "fix" the expectation to the older one-line shape.
```sh
# the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```
Also NC the DENOMINATOR (`want=121` on the real file must print `GATE FAIL: examined 122 data rows, expected 121`).

⚠️ **AND WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -oiE '-family row'` fails with `grep: amily row: No such file or directory` and the surrounding arithmetic prints `base=0 now=0 delta=0`, **which reads exactly like "no change".** **Always pass `--` before the pattern**, and remember the counts are form-dependent: **95 occurrences vs 67 LINES** at this tip (`grep -c` counts LINES). **A gate that reads zero on BOTH sides is not evidence of invariance.**

⚠️ **THE ARM-A MALFORMED-ROW FIGURE RECONCILES ONLY UNDER AN ESCAPE-AWARE FIELD COUNT — MEASURED AT THE PHASE-90 BRAINSTORM.** The malformed rows are ids **57** (`fields=9`) and **69** (`fields=10`), at lines **119** and **131**. ⚠️ **A NAIVE `awk -F'|'` `NF != 8` FORM READS *SEVENTEEN* ROWS**, because legitimately ESCAPED pipes (`\|`) in row summaries split as fields. **Strip `\|` BEFORE splitting.** The naive count is NOT a drift signal — do not "correct" the {57, 69} figure, and do not "fix" the rows.

**SENTINEL RUN MECHANICALLY AT THE PHASE-90 BRAINSTORM (2026-08-19), ON BOTH SIDES OF THE ROW ADD. IT DOES NOT FIRE; `stop` was NOT created** (verified absent at the git root AND in the stage worktree). ACTUAL output, recorded not predicted: **BEFORE:** 239 lines / 121 data rows — (1) **SILENT** at `want=121`, (2) SIX at `:199 :205 :211 :221 :227 :235`, (3) SILENT · **AFTER:** 240 lines / 122 data rows — (1) **`NOT DONE: row 90`**, (2) **SIX** at `:200 :206 :212 :222 :228 :236`, (3) SILENT.
- **ALL FOUR NCs FIRED ON THE POST-ADD FILE.** NC-A `NC LANDED? [ in-progress ]` inspected FIRST => `NOT DONE: row 62` **AND** `NOT DONE: row 90` (see the shape warning above) · NC-B `want=121` => `GATE FAIL: examined 122 data rows, expected 121` · NC-C residual **2 -> 0** confirmed first => `NEVER OPENED: gRPC`, WASM correctly silent · NC-D long **5** / short **1**, union **6** (a one-arm strip is NOT an NC for the union).
- ⚠️ **A DRAFT OF THE BRAINSTORM PREDICTED "(1) SILENT" AFTER THE ADD AND WAS WRONG.** An `in-progress` row MUST be named by check (1). The error was caught ONLY by running the gate. **RECORD, DO NOT FORECAST.**
**RE-VERIFIED AT THE PUSHED TIP `059c13c9` (2026-08-19), AFTER the merge and push — the sentinel and ALL FOUR NCs re-run mechanically, and EVERY count figure in this file re-derived.** (1) **`NOT DONE: row 90`** with the denominator asserted at 122 · (2) **SIX** at `:200 :206 :212 :222 :228 :236` · (3) **SILENT**. NC-A => `NOT DONE: row 62` **AND** `NOT DONE: row 90` · NC-B `want=121` => `GATE FAIL: examined 122 data rows, expected 121` · NC-C residual 2 -> 0 => `NEVER OPENED: gRPC`, WASM silent · NC-D long **5** / short **1** / union **6**. ⇒ **THE SENTINEL DOES NOT FIRE — TWO checks block it (row 90 in-progress AND six windows). `stop` WAS NOT CREATED** (verified absent at the git root). `master` and `origin/master` both `059c13c9`, `0 0`.

- Every leak axis at this tip: `-family row` **95 / 67** · `gRPC-family row` **2** · `Operational-tooling-family row` **3**. Row 90 `fields=8` (escape-aware, verified BEFORE installing).
- ⚠️ do NOT "fix" the six and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases; the candidate sentences are a WINDOW onto a larger deferred backlog, not an inventory of it. ⚠️ **THE INVENTORY IS ~59, NOT ~42** — the long-carried "~42" is the SENTENCE-VISIBLE subset only. **Cite ~42 only as "sentence-visible", never as the inventory.** ⚠️ **AND TWO WINDOW CANDIDATES ARE STALE — RE-VERIFIED BY EXECUTION AT THE PHASE-90 BRAINSTORM:** window `:222` (was `:221`) still lists the H2 `//`-path routing bug that **ROW 87 CLOSED** — `internal/filter/hcm/h2/stream.go` now uses `url.ParseRequestURI`, confirmed — and the `/stats/prometheus` projection gap that **ROWS 79 AND 80 CONSUMED** (both `done`, confirmed). **A candidate's presence in a window is NOT evidence it is still open.**

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (ugrep under the hood). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Reproduced at THIRTY-ONE separate tips.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **THE METHOD BINDS; carry no standing discrepancy figure and no standing absolute.** ⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS; print FILE NAMES, never a count.** ⚠️ **THE HARNESS `grep` STRIPS A LEADING `./` FROM PATHS** — measured at the phase-90 BRAINSTORM, where a `grep -v '^\./internal/'` filter matched NOTHING and silently read as "no files outside internal/". ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files; use `-v --no-index`, and `git status` is the discriminating check). ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **`grep -c` ON ZERO MATCHES IS TOOL-SPECIFIC** (only `git grep -c` prints nothing; all exit 1 — capture the value, never chain on the exit code). ⚠️ **`grep -c` COUNTS *LINES*, NOT *OCCURRENCES*.** ⚠️ **`\t` IN GNU ERE IS A LITERAL `t` — use `-P`.** ⚠️ **A `git grep` PATHSPEC ENDING IN `/` READS A FAIL-UNSAFE ZERO** — use the shell-glob form. ⚠️ **AN UNANCHORED PANIC-GREP FALSE-FIRES 14 TIMES ON A FULLY-GREEN DIFFERENTIAL LOG** — **the canonical gate is ANCHORED: `^panic:|DATA RACE|SIGSEGV`.** ⚠️ **AND `STATE.md` QUOTES ITS OWN MATCHERS:** an unanchored `grep -c 'next-skill:'` reads more than 1 on the unmodified file; the anchored `^- \*\*next-skill:\*\*` reads 1.

## ⚠️ A BARE `^func <name>` GREP IS A PREFIX MATCH — MEASURED ON THIS ROW'S OWN PICKED SYMBOL

The SPEC must prototype at `buildRequest`. **A bare prefix grep mis-scopes it across TWO packages:**
```sh
git grep -c '^func buildRequest'  -- 'internal/**/*.go' ':!*_test.go'   # reads 3  <-- TRAP
#   internal/filter/hcm/h2/stream.go:409   func buildRequest(...)              <-- the real one
#   internal/filter/http/extproc/attributes.go:141  buildRequestHeadersProcessingRequest(
#   internal/filter/http/extproc/attributes.go:308  buildRequestBodyProcessingRequest(
git grep -c '^func buildRequest(' -- 'internal/**/*.go' ':!*_test.go'   # reads 1  <-- CORRECT
```
**ANCHOR THE OPEN PAREN.** `^func buildH2Request(` likewise reads **1**. Negative-controlled: a fabricated
`^func buildRequestZZZ(` reads **0**, so the discriminating form is live and not vacuously matching.
⚠️ **This is the same species as `reference_symbol_assertion_needs_qualified_name`, but the contaminant here is a
LONGER SYMBOL IN A DIFFERENT PACKAGE, not a doc quote — a `':!docs'` pathspec would NOT have caught it.**

## ⚠️ A DRIFT CORRECTION IS ITSELF A CLAIM — ONE FIRED AND WAS REFUTED AT THE PHASE-90 BRAINSTORM

A probe reported the stat-surface figure **406** as stale, "tip measures 403". **THE CARRIED FIGURE WAS RIGHT.** The canonical command counts **OCCURRENCES REPO-WIDE** and reads **406** (405 in `internal/` + 1 in `cmd/envoy-go/main_test.go`); the **403** was a **LINE** count **scoped to `internal/`** — a different unit AND a different scope. **Before accepting any "figure X is stale" claim, reproduce BOTH the old and the new command and compare UNIT and SCOPE, not just the numbers.**

Two further counts are CONTESTED and NO NUMBER IS CARRIED:
- ⚠️ **`STATE_HISTORY.md`'s "archive labels 202" IS NOT REPRODUCIBLE.** Six plausible matcher forms at this tip give **203 / 163 / 205 / 166 / 165 / 209**; none is 202. The cause is visible — the positive control reads **3**, not 1, because entries are cross-referenced inside OTHER entries' prose. **Use the ANCHORED form `^- \*\*prior active-phase:\*\*` for the absence guard**, and note that writing an eviction NOTE naming the evictee makes a LOOSE grep read 1 in `STATE.md`.
- the loose `PROPOSED` matcher's whole-file count · the stat-surface DOC-side absolute (1205 vs 1207) · the zero-test `generic/*` suite count (13 vs 14).

## ⚠️ EVERY PORT BAND THIS LOOP HAS ASSIGNED SINCE PHASE 87 IS INSIDE THE KERNEL EPHEMERAL RANGE

`net.ipv4.ip_local_port_range = 32768 60999`. The bands handed out were 47410-47441, 47450-47559, 47560-47999 and 48000+ — **all inside it**. A phase-90 probe hit three `bind: address already in use` failures on ports `ss -ltn` reported FREE (held as TIME-WAIT or outbound sockets; docker's `-p` proxy does not set `SO_REUSEADDR`).

⚠️ **THIS IS ALSO THE MECHANISM BEHIND THE LONG-CARRIED "driver-owned receiver port race":** all three recorded ports — **35097, 35323, 42039** — are inside the ephemeral range, while the static fixture band (10000-19172) and the reserved bands (20000-31007, 11000-14999) are all **BELOW 32768**, which is exactly why those never race.

⇒ **BAND YOUR PROBE PORTS BELOW 32768.** `21000-24999` is clear of both the ephemeral range and every reserved band. **Check availability with `ss -tan` (ALL states), never `ss -ltn`** — the listener-only form reads a fail-unsafe "free". **A `bind: address already in use` in a 4xxxx band is NOT evidence a sibling session is squatting.**

## ⚠️ `INNER_EXIT` DOES NOT EXIST IN THIS REPO

The six-gate posture below has demanded for many phases that every differential launch capture **`INNER_EXIT`**, because *"the wrapper exits 0 even when the binary aborts mid-suite"*. **There is no wrapper.** `git grep -n 'INNER_EXIT' -- ':!docs'` returns **exactly one** hit — THIS FILE's instruction text — and `git ls-files | grep -icE '\.(sh|mk|bash)$|Makefile'` returns **0**. The suite is launched as a direct `go test ./test/differential/`, so **the outer process IS the inner process** and `PIPESTATUS[0]` is the only exit code there is. ⚠️ **`DECISIONS.md` nonetheless records `INNER_EXIT=0` as a measured gate reading at phase 87** — a value nothing in the tree emits. **THE HAZARD IS REAL; THE MECHANISM NAMED IS NOT.** Use instead: `PIPESTATUS[0]` · the terminal `ok  …/test/differential  <n>s` line · and a **SET RECONCILIATION** of the `=== RUN` set against the resolved-result set, empty in BOTH directions.

⚠️ **AND THE FIXTURE-COUNT REGEX IS DIGIT-BLIND:** `^ *--- PASS: TestDifferential/[0-9]{4}-[a-z0-9-]+ \(…\)$` reads **119, not 121**, because `0007a-cors` and `0007b-iteration-probe` carry LETTER suffixes — **a spurious two-fixture shortfall that reads exactly like a mid-suite abort.** And `grep -c '^--- PASS: TestDifferential/<fixture>$'` reads **0 on a GREEN log** — the subtest line is INDENTED four spaces AND carries a ` (9.14s)` duration suffix. **Correct form:** `grep -cE '^ *--- PASS: TestDifferential/<fixture>( \([0-9.]+s\))?$'`, negative-controlled against a fabricated fixture name reading 0.


==============================================


## STATUS AS OF 2026-08-19: PHASE 90 (`h2-host-authority-normalization`) **OPEN AT LIFECYCLE-STATE 1** — row 90 `in-progress` at `ROADMAP.md:152`, `want` **122**. **THIS SESSION WRITES THE PHASE-90 SPEC.**

**What the 90 BRAINSTORM landed (2026-08-19, base master `6b4bc7c0`, branch `phase-90-brainstorm`, ONE squashed commit).** Docs-only: **ZERO production `.go`, ZERO test `.go`.** `ROADMAP.md` numstat **`1 0`** (239 -> 240 lines, 121 -> 122 data rows). `docs/envoy-go/phases/90-h2-host-authority-normalization/` created with `BRAINSTORM.md` + `PROGRESS.md` (phase dirs **130 -> 131**). `STATE.md` rolled IN PLACE; `STATE_HISTORY.md` **506 -> 508** (numstat `2 0`). **`DECISIONS.md` and `BEHAVIOR_CONTRACT.md` BYTE-UNTOUCHED** — a BRAINSTORM adds no ADR; next-free stays **`ADR-0312`** and the strict `PROPOSED` guard **STAYS AT 0**. **THE SPEC RE-ARMS IT 0 -> 1.**

### THE CHARTER

**NORMALIZE `host` AND `:authority` ON THE HTTP/2 DOWNSTREAM LEG so the upstream request carries exactly ONE authority, as the reference does.** Reference rule, measured across four arms: `:authority` wins if non-empty; `host` is PROMOTED when `:authority` is absent; the regular `host` is NEVER forwarded; an explicitly-empty `:authority` is a CONNECTION ERROR.

**REPRODUCED BY EXECUTION at `6b4bc7c0`** with a raw-framer client and a raw-framer backend holding ONE `hpack.Decoder` PER CONNECTION (all four proxied arms on ONE pooled upstream conn, streams 1/3/5/7, all decoding cleanly — the connection-scoped-table hazard was EXERCISED, not dodged), positive control on every arm:

| arm | subject | reference |
|---|---|---|
| **P** (positive control) `:authority` only | `:authority=auth.example.com`, 5 fields | identical modulo Envoy's 3 added headers |
| **A** `:authority` + `host` | forwards **BOTH** | **DROPS** the regular `host` |
| **B** `host` only | `":authority"=""` **PRESENT-AND-EMPTY**, keeps `host` | **PROMOTES** `host`, drops it |
| **C** ⚠️ explicit `:authority: ""` + `host` | 200 | **TEARS THE CONNECTION DOWN** — client EOF, ZERO backend delivery, `downstream_cx_protocol_error: 1` |

⚠️ **THE NEGATIVE CONTROL IS LOAD-BEARING AND ARM B IS UNFALSIFIABLE WITHOUT IT.** The same client sent the same header sets DIRECT to the backend: `DIRECT-B` 5 fields (`:authority` ABSENT) vs `DIRECT-C` 6 fields (`":authority"=""` PRESENT). **The instrument DISCRIMINATES absent from present-but-empty** — a decode bug would look identical.

⚠️ **THE H1 LEG DIVERGES TOO, AND PHASE 89 ASSERTED ONLY THE H/2 LEG.** H1-P / H1-A / H1-E AGREE. **H1-B** (HTTP/1.0, no Host): subject 200 forwarding an EMPTY `Host:` vs reference **426 Upgrade Required**, zero delivery. **H1-D** (duplicate `Host:` ×2): subject **400**, zero delivery vs reference 200 comma-coalesced — ⚠️ **THE SUBJECT IS THE STRICTER SIDE, THE OPPOSITE POLARITY FROM A/B.** ⚠️ **H1-E REFUTES any "the reference guarantees a non-empty authority" generalization** — it forwards an empty `host` with a 200 on HTTP/1.1.

**MECHANISM:** `internal/filter/hcm/h2/stream.go` builds TWO things and both mishandle `host` — `buildH2Request` fills `Authority` from `:authority` ONLY and its `default:` arm appends `host` onto the ordered upstream carrier; `buildRequest` independently does `regular.Add` for `host` and sets `http.Request.Host` from the same empty `authority`, so **an empty authority is a ROUTE-MATCHING input, not merely a wire artifact.**

### ⚠️ WHAT THE SPEC OWES — SEVEN QUESTIONS, THREE LOAD-BEARING

**Q1 — SCOPE.** Which arms does row 90 close? A+B only (the +15 floor)? A+B+C? Both legs? **Decide by measurement, not symmetry.**

**Q2 — ⚠️ THE DIFFERENTIAL ARM CANNOT USE `helpers.H2RoundTrip`, PROVEN AT THE PINNED SOURCE.**
```
golang.org/x/net@v0.34.0  http2/transport.go:2162-2166
    if asciiEqualFold(k, "host") || asciiEqualFold(k, "content-length") {
        continue          <-- a client-set `host` header is SILENTLY DROPPED
    }
http2/transport.go:2146   f(":authority", host)   <-- ALWAYS synthesized from req.Host/req.URL.Host
```
`helpers.H2RoundTrip` (`test/helpers/h2.go:33`) builds an `*http.Request` and hands it to `cc.RoundTrip`, so it inherits both. **ALL THREE H/2 SHAPES ARE STRUCTURALLY INEXPRESSIBLE THROUGH IT** — the same class as the phase-89 PLAN's `order=` line, which was only refuted at IMPL. **CAUGHT AT BRAINSTORM THIS TIME. DO NOT WRITE AN ARM AGAINST `H2RoundTrip`.**
✅ **The instrument ALREADY EXISTS in-tree:** `test/fixtures/0119-grpc-unary-trailers/driver/driver.go` is a full raw-framer H/2 client — `http2.ClientPreface` at `:474`, `http2.NewFramer` at `:478`, `hpack.NewDecoder` at `:479`, `fr.WriteHeaders` at `:491`, and an EXPLICIT hand-built pseudo-header list including `{Name: ":authority", …}` at `:355-358`.

**Q3 — WHICH FIXTURE.** ⚠️ **THE H2-CAPABLE DOWNSTREAM SET IS *FOUR*, NOT ONE:** `0004-h2-routing`, `0079-h2-multiplex-pool`, `0080-h2-goaway-rotation`, `0119-grpc-unary-trailers`. Extend one, or mint `0120`? (`0120` is FREE and STAYS UNCONSUMED until the SPEC decides.)

**Q4 — arm C's reject.** Adopt the reference's connection-level teardown, or NAME A DEPARTURE? The reference reaction is a `downstream_cx_protocol_error`, not a 4xx.

**Q5 — H1-D polarity.** The subject is STRICTER. Parity would make envoy-go **stop rejecting** something it rejects today. A NAMED DEPARTURE may be the better answer.

**Q6 — routing blast radius.** `buildRequest` sets `http.Request.Host` from `authority`; promotion changes a ROUTE-MATCHING input. Which existing route tests cover host matching?

**Q7 — `h2ReconcileSkipKey`** currently DROPS a filter-written `host` (D-89-HOST: SKIP). Under a promotion model it arguably belongs on `Authority`. **UNRESOLVED.**

### COST — A MEASURED FLOOR, AND IT COVERS ARMS A AND B ONLY

**+15 / −1 production over ONE file and TWO symbols** — `buildH2Request` and `buildRequest` in `internal/filter/hcm/h2/stream.go` — measured by a prototype applied, run, and reverted with `sha256sum -c` proving the restore. ⚠️ **A MEASURED PROTOTYPE IS A LOWER BOUND.**

⚠️ **NO EXISTING TEST PINS THE DEFECT — CONTROLLER-VERIFIED.** Green baseline FIRST, then green WITH the prototype across `go test ./internal/... -count=1` at **69 packages ok**; the single failure (`TestAcquireH2Stream_PromoteSkipsDrainingConn`) went **6/6 green on retry** with the prototype still applied. **The row ships with ZERO inherited guard and MUST bring its own.** ⚠️ That flake fired **WITHOUT `-race`**, which WIDENS the recorded `internal/cluster` outlier class.

**NAMED under-enumeration, each a real cost:** `(*ClientConn).RoundTrip`'s unconditional `:authority` emit · `h2ReconcileSkipKey` (Q7) · `(*chainDispatchAction).WriteH2` · **arm C's reject is NOT in the floor** · the H1 leg entirely · **the H3 leg entirely UNPROBED** · differential fixture cost.

### REJECTED ALTERNATIVES — every cost re-derived at this tip by a built-run-and-reverted prototype

| candidate | production cost | files | pkgs | verdict |
|---|---|---|---|---|
| **`host`/`:authority`** | **+15 / −1** | **1** | **1** | **PICKED** |
| decode-side trailers | +37 / −4 | 3 | 1 | rejected — see below |
| ADR-0310 C1 drain | +161 / −31 | 3 | 1 | **DEFERRED — the best NEXT row** |
| ADR-0310 C2 `max_request_headers_kb` | +76 / −2 | 5 | **3** | deferred — **must not go before C1** |
| ADR-0310 C3 SETTINGS | +31 / −19 | 3 | 1 | **REJECTED — measured ANTI-parity** |

⚠️ **C3 IS NOT A PARITY GAP — THREE DOCUMENTS ARE WRONG.** Measured: envoy-go SETTINGS `NumSettings=6` (`0x3 0x4 0x5 0x2 0x1 0x9`), reference `NumSettings=4` (`0x1 0x8 0x3 0x4`) — **NEITHER advertises `0x6`**, and the reference still doesn't with `max_request_headers_kb: 96` explicitly set. A POSITIVE reading (a frame WAS received; `0x6` absent from a NON-EMPTY set). **ADR-0310 §Consequences (x), the phase-88 PLAN §10, and `BEHAVIOR_CONTRACT.md`'s `## HTTP/2` bullet all file it as deferred PARITY. Implementing it would CREATE a divergence.** It is also not a pure wire change: with `0x6` advertised, x/net-based clients — **including this repo's own differential drivers** — start REFUSING to send oversized requests (`transport.go:3118` stores `cc.peerMaxHeaderListSize`, enforced at `:2239`/`:2296`); envoy-go itself IGNORES a peer's `0x6` (qualified symbol `SettingMaxHeaderListSize` => **zero** hits repo-wide); and advertising alone is a wire-level lie (with `0x6=61440` advertised, a 100 KiB block still returned 200). **Defensible residue: a test pinning `0x6` ABSENT on BOTH sides plus a pin on the SETTINGS set — which is currently ENTIRELY UNPINNED (a 7th setting on the wire left all 197 `=== RUN` rows green).**

⚠️ **DECODE-SIDE TRAILERS — the forwarding fix is cheap but the USEFUL version is not.** Reproduced cleanly (subject `TRAILERS: <NONE>` vs reference `TRAILERS(wire): x-p90-trailer=t1`); +37/−4 over 3 files, green at `RUN=519 PASS=340 FAIL=0`. **BUT wiring `RunDecodeTrailers` does NOT make the trailer filter hook observable, and this was MEASURED:** with the prototype installed and the hook PROVABLY FIRING, Lua STILL logged `LUA-TRAILERS-NIL` — **emitted BEFORE the ENTER line**, because `lua/decode_headers.go` runs `envoy_on_request` as a coroutine at DecodeHeaders and `bodyChunks()` does not park until end-of-stream. The useful version additionally touches `lua/decode_headers.go`, `lua/body.go`, `lua/lua.go` — coroutine-suspension semantics. **A row shipping the +33 forwarding fix alone would deliver a hook no filter can observe.**

⚠️ **C1 NOTES A FUTURE ROW MUST NOT RE-LEARN:** the client leg is **FORCED, proven by compile error** (`client.go:454:49: a.buf undefined` — the accumulator is shared), reproducing phase-88's D-88-SEQ atomicity BY MEASUREMENT; and **the 11-row phase-88 continuation roster stays GREEN across the full rewrite**, so it is behaviour-anchored and gives a C1 implementation almost NO help. The mechanism is a **documented facility of the already-pinned dependency** (`x/net@v0.34.0` `hpack.Decoder.SetEmitEnabled`). ⚠️ **C2's enforcement point (`onHeaderBlock`, post-decode) is REPLACED by C1's incremental one — doing C2 first means writing code C1 deletes.** ⚠️ **And C2's stat would be INVISIBLE where this project asserts stats:** `http2.` is not a recognized `ExtractTags` root, so the counter is correct in `/stats` and **silently dropped** from `/stats/prometheus` (`stats: WriteProm skipped 1 registered metric name(s) with no recognized top-level segment: http2.header_overflow`) — a differential there would read 0 subject / 2 reference, **a false divergence produced by the RENDERER**. Fixing it is a new SN rule, i.e. an ADR-0061 amendment (13 roots -> 14).

### ⚠️ FIVE REFUTATIONS THE NEXT SESSION MUST NOT RE-LEARN

1. ⚠️ **THE PHASE-89 CODEC CENSUS IS PROSE-CONTAMINATED AND STRUCTURALLY BLIND.** It records `codec_type HTTP1 270 / AUTO 6 / HTTP3 3 / HTTP2 ZERO`. **Config-only over `test/fixtures/*/*.yaml` reads HTTP1 212 / AUTO 2 / HTTP2 0 / HTTP3 0** — the larger figures count README prose and Go comments. **And the YAML view is BLIND TO 46 OF 121 FIXTURES**, which carry no `envoy-go.yaml` and build config inside their Go driver (driver-side: AUTO 3 / HTTP1 50 / HTTP3 2). **The claim "no fixture has ever exercised downstream H2" is FALSE — the H2-capable set is FOUR.**
2. ⚠️ **THE "~64 KiB ENCODED BAND" IN ADR-0310 §Consequences (xi) AND `BEHAVIOR_CONTRACT.md` IS NOT REPRODUCIBLE.** Against the same pinned image with RFC-legal CONTINUATION framing, encoded **62170 / 66506 / 70170 / 100182 / 150586 / 400729 / 1000229 ALL** returned `RST_STREAM INTERNAL_ERROR(2)`, **stream-scoped**, booking `http2.header_overflow` every time. THREE independent framings tried; the band appeared in NONE, and **`COMPRESSION_ERROR(9)` was NEVER observed from the reference in any arm** (a single oversized HEADERS frame gave `GOAWAY FRAME_SIZE_ERROR(6)`). `continuation_test.go`'s `AcceptsPastReferenceLimit` justifies its 96 KiB by that band — **the justification does not survive re-measurement** (the 96 KiB figure itself is fine). **RECORDED, NOT FIXED.**
3. ⚠️ **`RunDecodeTrailers` "NO SEAM EXISTS" IS REFUTED.** `FilterChain.RunDecodeTrailers` (`chain.go:490`) is a **complete working seam with ZERO production callers**. Missing are CAPTURE (`serverStream` drops the fields), CARRIAGE (`H2Request` has no `Trailers`), EMISSION (`RoundTrip` writes no trailing block) — **three concrete absences, not an absent seam.**
4. ⚠️ **envoy-go FORWARDS H1 REQUEST TRAILERS UNGATED, WHERE THE REFERENCE DROPS THEM BY DEFAULT.** `enable_trailers` occurs **ZERO** times repo-wide; the reference needs it at **TWO** independent config sites (HCM **and** cluster `explicit_http_config`). An unrecorded, opposite-direction divergence — parity would make envoy-go **stop** doing something it does today.
5. ⚠️ **THE H1→H2-UPSTREAM 502 BLOCKER: THE RECORDED CITE NAMES THE WRONG SITE, AND THE BLOCKER IS TWO-LEGGED.** `connection.go:467` has NOT drifted but is the **injection** site (`rf.SetAction`); **selection** is at `connection.go:339`, and `actions.go :: (*clusterRouteAction).asRouterAction` memoizes `router.H1ClusterAction` with **no `UseH2()` consultation**. ⚠️ **`h3dispatch.go:139` has the SAME defect**, self-documented at `:138`. Mechanism caught on the wire: `CONN 2 BAD-PREFACE "POST /probe/h1 HTTP/1.1\r"` — envoy-go negotiates ALPN `h2`, then writes an HTTP/1.1 request line onto it. `(*Cluster).UseH2()` has **ZERO** consumers in `hcm/`.

### ⚠️ COUNTS AT THIS TIP — RE-DERIVE MECHANICALLY, NEVER COPY

- **`ROADMAP.md` 240 lines / 122 data rows**; row 90 `in-progress`, all others `done`; check-(2) anchors `:200 :206 :212 :222 :228 :236`; `-family row` **95 / 67**; `gRPC-family row` **2**; `Operational-tooling-family row` **3**; ARM-A malformed rows ids **57**/**69** at lines **119**/**131** (ESCAPE-AWARE form; naive reads 17).
- **`DECISIONS.md` 18277** · **310** headings · tail **ADR-0311** (COMPLETE) · next-free **ADR-0312** (TAIL-derived; `grep -c '^## ADR-0312'` => **0**; headings+1 COLLIDES at the ADR-0209 gap) · **strict `^> \*\*STATUS: PROPOSED` guard 0 — the SPEC RE-ARMS it 0 -> 1** · `^---$` **216**. ⚠️ **A NEW ADR TAKES NO `---` SEPARATOR.**
- **`BEHAVIOR_CONTRACT.md` 5962** — BYTE-UNTOUCHED this stage. ⚠️ **every by-line cite at or below the `## HTTP/2` asserted list has shifted +2 (phase 89) and +2 (phase 88) and +1 (phase 87). CITE BY STRING OR SYMBOL.**
- **`STATE.md` 63** · **`STATE_HISTORY.md` 508** (⚠️ **carry NO archive-label count — see the contested-counts section**) · **`BOOTSTRAP_PROMPT.md` 522** (repo ROOT) · phase dirs **131** · ⚠️ **`REVIEW.md` 37 is a FILE COUNT, not a line count** — `git ls-files | grep -c 'REVIEW\.md$'` => **37**, newest at `phases/25.3-…`; **no phase since 25.3 has written one — the standing REVIEW departure, NAMED not claimed.**
- **differential fixtures 121** at **`test/fixtures/`** (⚠️ **NOT `test/differential/fixtures/`, a plausible wrong path returning a SILENT `0`**); numeric tail `0119-grpc-unary-trailers`, next-free **`0120` STAYS UNCONSUMED**. · **fuzzers 55 / 48 files.** · **BackendKind tail 38** (`H2GoawayResponder BackendKind = 38` at `test/differential/fixture/fixture.go:614`). · **fixture blank imports 121** — the NARROWED form `grep -cP '^\t_ "[^"]*test/fixtures/'`; ⚠️ **the unnarrowed `grep -cP '^\t_ "'` reads 123 and is REFUTED.**
- **stat surface: the COMMAND binds, not the number.** `grep -ro --include='*.go' -e 'NewCounter(' -e 'NewGauge(' .` => **406** (405 `internal/` + 1 `cmd/envoy-go/main_test.go`). ⚠️ **A "403" claim is a LINE count scoped to `internal/` — different unit, different scope. NOT drift.**
- **The slice-only-writer gate reads 6 at this tip.** ⚠️ **NAMED BLIND AXES: element-level in-place mutation (`.Headers[i].Value = …`) and any writer outside `internal/filter/hcm/`.**
- **Module path is `github.com/pgdad/envoy-go`** — which CONFIRMS `ROADMAP.md`'s five `esalaine` cites as a live documentary defect.

### ⚠️ DOCUMENTARY DEFECTS — carried forward, still deliberately NOT fixed

⚠️ **ADR-0310 §Consequences (x)'s C3 "deferred parity" framing is REFUTED** (see above) · ⚠️ **ADR-0310 §Consequences (xi)'s "~64 KiB band" is NOT REPRODUCIBLE** · ⚠️ **`DECISIONS.md` records `INNER_EXIT=0` at phase 87** — a value nothing in the tree emits · ⚠️ **the PHANTOM `h2.parseHeadersForRequest` cited at `h2dispatch.go:462`/`:479`/`:491`** against `git grep -c '^func.*parseHeadersForRequest' -- 'internal/**/*.go'` ⇒ **0** (positive control `buildH2Request` ⇒ 1) · `internal/filter/http/types.go`'s FALSE *"per ADR-0071's filter API stability"* comment (⚠️ **write "rejected on measured blast radius — 33 production + 38 test files, ~101 mutation call sites". NEVER "per ADR-0071"**) · **the H1→H2 502 cite naming the wrong site, and its UNRECORDED H3 leg** · **the stale H1-trailers prose at `chain.go:477-489` and `connection.go:562-568`** · `header_mutation` rejecting `remove` of a protected header where the reference config-ACCEPTS it · `RunAction`'s unguarded H2 arm · `ADR-0051` §2 · `ADR-0058`'s dead `routerActionH2.doH2` location · `ADR-0057`'s "27 round-trips" (now **31**; ⚠️ `README.md:36`'s "first 27 requests" is CORRECT as a prefix) · **`ROADMAP.md` rows 57/69 malformed (ARM-A GUARD)** · **`STATE.md` §Project counts FROZEN at phase 76** (they self-contradict §Current — do not anchor on them) · `harness_test.go` port inventory STALE · `body.go` nolint INERT · xDS cycle guard NOT AUTOMATED · `wasm/doc.go:219` two errors · ROADMAP cites `esalaine` FIVE times · `rbac.go:50` token `F2` · root `PROGRESS.md` stray phase-32.1 doc · SPEC-86/PLAN-86 cite a nonexistent `internal/xds/xdsgrpc/...` path · `BEHAVIOR_CONTRACT.md`'s "no stdlib net/http parsing" sentence (RETAINED, ridden by ADR-0309) · the TWO riders citing the ADR-0052 mandate at a DRIFTED `:1821` · window `:222` carries TWO CLOSED candidates.

### ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + ALL FOUR NCs YOURSELF** and record ACTUAL output. **Check (1) prints `NOT DONE: row 90` while phase 90 is open — that is HEALTHY.** NC-A now needs BOTH `row 62` AND `row 90`.
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** The 90 BRAINSTORM refuted the phase-89 codec census, ADR-0310's C3 parity framing, ADR-0310's 64 KiB band, the "no seam exists" trailers claim, the H1→H2 cite, **and a drift CORRECTION that was itself wrong**. It also **caught its own §8 forecast by running the gate.** Do the same.
3. **LINE ANCHORS DRIFT; SYMBOL ANCHORS DO NOT — RE-VERIFY EVERY CITE AT YOUR TIP.** ⚠️ **And PATHSPEC-SCOPE every symbol assertion** — a bare `git grep` for a symbol whose signature a doc quotes is DOCS-CONTAMINATED.
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Still live: the **SDS dial-budget class** · the **driver-owned receiver port race** (⚠️ **now EXPLAINED — see the ephemeral-range section**) · `internal/cluster` outlier (⚠️ **fired at phase 90 WITHOUT `-race`, widening the class; 6/6 green on retry**) · `internal/httpclient` zero-value · the two 84.2-era flakes · `TestServerConn_TinyWindowDelivery` · the REFERENCE h2spec section-8 flip. **Capture `-v` output BEFORE re-running anything.**
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only). Foreign containers present throughout phase 90 and deliberately left untouched: `infallible_booth`, `crazy_kare`, `golink-ai`, `quizzical_goldstine`.
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`.
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL** (⚠️ **QUALIFIED AND PATHSPEC-SCOPED**), **and ASSERT THE BOUND BINARY with `ss -ltnp` before reading any probe result.** **`gofmt -l` never exits non-zero — gate on OUTPUT.** ⚠️ **`gofmt` REALIGNMENT CHANGES A MEASURED DIFF** — a phase-90 probe's cost moved `+35/−2` -> `+37/−4`; **a pre-`gofmt` cost figure UNDERSTATES.** ⚠️ **`go test` WITHOUT `-v` PRINTS ZERO `=== RUN`** — a census can read `RUN=0` beside `RC=0`, a VACUOUS GREEN. ⚠️ **A `-run` selector matching nothing prints `[no tests to run]` and EXITS 0.**
8. ⚠️ **`pgrep -f` / `pkill -f` MATCHES YOUR OWN SHELL AND KILLS THE TOOL CALL (exit 144)**, and a `pgrep -f 'a\|b'` ALTERNATION MATCHES NOTHING SILENTLY. ⚠️ **AT PHASE 90 A PROBE'S `pgrep -f 'envoy-go -c'` MATCHED AND KILLED A SIBLING PROBE'S PROCESS** (PID 2870478), whose log then read `signal received; initiating graceful drain` and whose next probe got `connection refused` — **a killed server is INDISTINGUISHABLE from a broken feature.** **Kill ONLY PIDs whose cmdline contains your OWN scratch path**, taken from `pgrep -laf` with the `bash -c` line filtered out. **If you run parallel probes, warn them.**
9. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-*`, `wt-*` or stage worktree should outlive its stage close** — expect only `master` (verify with `git worktree list`).
10. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** (a)/(b) differential **121 fixtures** + full `go test ./...`, gated on `PIPESTATUS[0]` and a **SET RECONCILIATION** (⚠️ **NOT `INNER_EXIT`, which does not exist**); (c) **h2spec cite ONLY from your own run — and note it is STRUCTURALLY incapable of anchoring any upstream-facing row: its harness configures `envoy.filters.http.router` ALONE with `direct_response` routes and never goes upstream**; grpc-conformance deferred in writing; proxy-wasm 10/16; (d) fuzzers 55/48; (e) the ANCHORED panic gate on every differential launch; (f) no REVIEW.md — standing departure.
11. ⚠️ **`-p` PUBLISHING WORKS; `--network host` IS WHAT DOES NOT** — `--network host` does not share the host netns here AND it SILENTLY IGNORES `-p`. **Host-gateway is `192.168.65.2`** (⚠️ **`getent hosts` resolves it IPv6-ONLY — use `getent ahostsv4`**). Both `-p` publishing and `--add-host=host.docker.internal:host-gateway` are working recipes. ⚠️ A **STRICT_DNS** cluster whose name does not resolve **blocks listener init entirely** — use STATIC, or `dns_lookup_family: V4_ONLY`, and **verify the listener is bound before believing any probe result.** ⚠️ **`clusters: []` is BOOT-REJECTED even when every route is a `direct_response`** — a dummy cluster is required. ⚠️ **envoy-go REJECTS `match.headers` outright.**
12. ⚠️ **A RAW-FRAMER PEER MUST HOLD ONE `hpack.Decoder` PER CONNECTION.** The HPACK dynamic table is CONNECTION-scoped; a fresh decoder per request decodes only the FIRST request on a pooled connection and then yields `invalid indexed representation index NN` with a **truncated field list — which reads exactly like "headers were lost"**. ⚠️ **`hpack.NewDecoder(4096, nil)` PANICS with SIGSEGV in `callEmit`** — a nil emit func is NOT treated as "discard"; pass `func(hpack.HeaderField) {}`. **And a raw-framer peer is MANDATORY for wire-shape questions** — a `net/http`+`h2c` peer normalizes case, hides pseudo-headers and destroys order. **Assert the peer RECEIVED something (n>0) BEFORE interpreting what it received.**
13. ⚠️ **`bash /dev/tcp` + `timeout cat <&3` INSIDE COMMAND SUBSTITUTION RETURNS SILENTLY EMPTY** — measured on four arms at phase 90, a false "no reply" that a Go raw-TCP client immediately disproved (real 502/400 responses). **Do not build an H1 probe on `/dev/tcp`.**

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 90 (h2-host-authority-normalization)' --format='%h %s'   # 1 at this tip: BRAINSTORM
git log --grep '^phase 89 (h2-decode-filter-mutations)' --format='%h %s'        # 4 — a CLOSED phase, use as a FIRING CONTROL
```
⚠️ **The count comments are LIVE FIGURES — VERIFY before relying.** Take the NEWEST line (`| head -1`).

Execution style: **subagent-driven per `feedback_execution_style`**. Subagents commit LOCALLY on the ONE stage branch with EXPLICIT PATHSPECS; the controller squashes (`feedback_subagents_no_push`, `feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** and tear down docker **BY NAME**. ⚠️ **BAND PRIVATE PORTS BELOW 32768** — see the ephemeral-range section; `21000-24999` is clear of both the ephemeral range and the reserved bands (10000-19172 static fixtures, 20000-31007, 11000-14999). **The 47xxx/48xxx bands used by phases 87-90 were all INSIDE the ephemeral range and MUST NOT be reused.** ⚠️ **ONE WORKTREE PER AGENT preferred; explicit-path staging if shared; never an unscoped `git restore`.** Per-task `gofmt` + `golangci-lint` on touched packages (⚠️ **misspell runs in locale US — sweep British spellings in `.go` comments BEFORE the lint gate**). ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch.**

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root — **observed repeatedly at every phase-88, -89 and -90 stage, and the harness ANNOUNCES it**. Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) before any commit or gate run. ⚠️ **Shell VARIABLES do not survive between Bash calls** — re-export in every command.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — `git status --porcelain` AND `git diff --stat master` must show ONLY your intended edits. For a patched TRACKED file: `sha256sum` BEFORE patching, `git checkout -- <path>` after, `sha256sum -c` the capture — ⚠️ **and COMMIT FIRST, because `git checkout --` restores from HEAD and will wipe uncommitted work.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads the phase-90 SPEC.** §Project counts SELF-CONTRADICT §Current — do not anchor on them (frozen at phase 76).
2. `docs/envoy-go/phases/90-h2-host-authority-normalization/BRAINSTORM.md` — **§2 the five reproduced arms and the discriminating negative control · §4 the rejected alternatives with every cost re-derived · §6 the cost FLOOR and its NAMED under-enumeration · §7 THE SEVEN QUESTIONS THIS SPEC OWES · §9 the nine findings not to re-learn.**
3. `docs/envoy-go/phases/90-h2-host-authority-normalization/PROGRESS.md` — the BRAINSTORM entry.
4. `docs/envoy-go/ROADMAP.md` row 90 at `:152`, and the six sentinel windows at `:200 :206 :212 :222 :228 :236`.
5. `BOOTSTRAP_PROMPT.md` §4-§7 (repo ROOT) — the lifecycle contract and the six-gate posture. **State 1 -> output `SPEC.md`.**

## What the SPEC owes
**DISPOSE OF ALL SEVEN §7 QUESTIONS BY EXECUTION**, not by argument — Q1 (scope), Q2 (the instrument; ⚠️ **`H2RoundTrip` is REFUTED — do not write an arm against it**), Q3 (which of the FOUR H2-capable fixtures, or mint `0120`), Q4 (arm C's reject: parity or NAMED DEPARTURE), Q5 (H1-D polarity — the subject is STRICTER), Q6 (route-matching blast radius), Q7 (`h2ReconcileSkipKey` vs D-89-HOST) · **FREEZE the D-90-* decisions** · **ENUMERATE THE COST BY COMPILING PROTOTYPE** (⚠️ `reference_measured_prototype_is_a_lower_bound` has fired on BOTH axes at the phase-88 IMPL and again at phase 89; the +15 floor covers arms A and B ONLY) · **DRAFT `ADR-0312` §Context and RE-ARM the strict `PROPOSED` guard 0 -> 1** · `ROADMAP.md` **BYTE-UNTOUCHED** (row 90 STAYS `in-progress`, `want` stays **122**) · `BEHAVIOR_CONTRACT.md` **BYTE-UNTOUCHED** · run the sentinel and all four NCs and record ACTUAL output · `PROGRESS.md` appended · `STATE.md` rolled IN PLACE (lifecycle-state **1 -> 2**) with the oldest §Recent entry evicted by a **DIRECT DATE READ** (⚠️ **the tie shape does NOT carry — the phase-90 BRAINSTORM faced NONE, the phase-89 IMPL a TWO-way, the PLAN none, the SPEC a TWO-way, the BRAINSTORM a THREE-way, phase-88 IMPL a FOUR-way**) · `next-prompt.txt` rolled (`git add -f`) · ONE squashed commit, merged and pushed.
