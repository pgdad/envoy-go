=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **PHASE 93 (`h2-local-reply-content-length`) IS CLOSED. ROW 93 READS `done` AT `ROADMAP.md:155`. THERE IS NO OPEN PHASE AND NO BANKED MID-LIFECYCLE WORK.** The phase directory holds `BRAINSTORM.md` (503) + `SPEC.md` (409) + `PLAN.md` (1031) and no `PROGRESS.md`. ⇒ **THIS SESSION SELF-PICKS A NEW SUBJECT AND RUNS ITS BRAINSTORM (lifecycle-state DONE -> 1).**

⚠️⚠️ **CHECK (2) IS NOW THE *SOLE* THING BLOCKING TERMINATION.** Checks (1) and (3) are BOTH SILENT at this tip — check (1) went silent the moment row 93 flipped `done`. **The margin is ONE.** ⚠️ **DO NOT "TIDY" A DEFERRED-CANDIDATE LINE — DELETING THE LAST ONE ENDS THE PROJECT.** ⚠️ **MEASURE ALL THREE YOURSELF BEFORE DOING ANYTHING ELSE; do not take this file's word for it.**

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE. This form parses FIELDS instead of matching a pattern, and
#     asserts the row DENOMINATOR so a deleted row is itself an error.
#     ⚠️ want IS 125 — row 93 was FLIPPED (not added) at the phase-93 IMPL, so
#     the denominator did NOT move; it last changed at the phase-93 BRAINSTORM.
awk -F'|' -v want=125 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **CHECK (1) IS NOW SILENT, SO ITS NCs ARE THE ONLY PROOF IT STILL WORKS.** Run BOTH doctoring NCs EVERY SESSION, because a check that prints nothing is indistinguishable from a broken one:
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md.
```
⚠️ **NC-A NOW READS EXACTLY *ONE* LINE** (`NOT DONE: row 62`) — it read TWO while row 93 was open and **COLLAPSED at the phase-93 IMPL's own flip.** ⚠️ **NC-B ALSO READS ONE** (`GATE FAIL: examined 125 data rows, expected 124`) — it too was TWO before the flip. **BOTH SHAPES CHANGED INSIDE ONE STAGE; never inherit a NC shape across a row flip — re-measure.**
```sh
# the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```
Also NC the DENOMINATOR (`want=124` on the real file).

⚠️ **AND WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -c '-family row'` fails (GNU: `grep: amily row: No such file or directory`) and the surrounding arithmetic prints `0`, **which reads exactly like "no change."** **Always pass `--` before the pattern**; with `--` occurrences read **95** and LINES read **67**. **`grep -c` counts LINES, NOT OCCURRENCES.** ⚠️ **AND `grep -c` PRINTS `0` *AND* EXITS 1, so `$(cmd || echo 0)` EMITS TWO ZEROS** — capture with `v=$(cmd || true)`.

⚠️ **THE MALFORMED-ROW FIGURE RECONCILES ONLY UNDER AN ESCAPE-AWARE FIELD COUNT.** Malformed rows are ids **57** (`NF=9`) and **69** (`NF=10`). ⚠️ **A NAIVE `awk -F'|'` `NF != 8` FORM READS SEVENTEEN.** **Strip `\|` BEFORE splitting.** The two forms **DISAGREE on row 57** but **AGREE on row 69**, so **any gate must state WHICH FORM it uses.**

⚠️ **AND DO NOT SUBSTITUTE A "REASONABLE EQUIVALENT" FOR CHECK (1)'s AWK.** A measurement agent once tried `NF>=8 && $2 ~ /^ *[0-9]+ *$/`: it printed **92**, not the true denominator. **Use the form above verbatim.**

**SENTINEL RUN MECHANICALLY AT THE PHASE-93 IMPL, BOTH BEFORE AND AFTER THE ROW-93 FLIP.** ACTUAL output, recorded not predicted.
**BEFORE the flip:** (1) `NOT DONE: row 93` — ONE line at `want=125` · (2) **SIX** at `:203 :209 :215 :225 :231 :239` · (3) SILENT; NC-A **TWO** lines, NC-B **TWO** lines, NC-C fired (WASM control **2**), NC-D long **5** / short **1** / union **6**.
**AFTER the flip:** **(1) SILENT** · (2) **STILL SIX at the same six lines, and all six per-line md5 BYTE-IDENTICAL** (`10d7807bf02d 4a92f7e62fc6 2a7eb298b9fd 4ad940205410 b2680e6f4fbf 6caa1c3ce0e7`) — **that digest match is the proof no deferred-candidate line was tidied** · (3) SILENT; **NC-A ONE** line, **NC-B ONE** line, NC-C fired with WASM control **2**, NC-D **5 / 1 / 6**.
⇒ **THE SENTINEL DOES NOT FIRE. `stop` WAS EVALUATED AND DELIBERATELY NOT CREATED** (verified absent at the git root and in every stage worktree).
⚠️ **THIS RUN DOES NOT CARRY FORWARD — run all three checks and all four NCs YOURSELF, at YOUR OWN tip.**

- ⚠️ **LIVE FIGURES RE-DERIVED AT THIS TIP:** `ROADMAP.md` **243** lines / **125** data rows / tail row **93**, **ALL 125 `done`** · `DECISIONS.md` **18804** · `^---$` **216** · `^## ADR-` **314** · bare `^## ` **322** (314 + 8 `## Amendment`) · tail **ADR-0315** · next-free **ADR-0316** (`^## ADR-0316` reads **0**) · house `PROPOSED` **0**, ADR-0231 decoy **1** at `:14866` · `BEHAVIOR_CONTRACT.md` **5987** · `STATE.md` **64** · `STATE_HISTORY.md` **538**, strict **163 (DELTA 0 across the IMPL)** / parenthetical **56** / loose **219** (**163 + 56 = 219 exactly**) · phase dirs **134** · fixtures **121** (tail `0119`, **`0120` FREE**) · fuzzers **56 targets / 48 FILES** · `go.mod` **67 REQUIRE ENTRIES** (**18 direct + 49 indirect**; two require blocks, `:5-24` and `:26-76`) — ⚠️ **67 IS THE REQUIRE COUNT, NOT A LINE COUNT, and the plausible `grep -cE '^\s+[a-z0-9./-]+ v[0-9]'` form reads 62** because it misses uppercase/underscore paths · BackendKind tail **38** (39 declared, values 0-38) · `-family row` **95 / 67**.
- ⚠️ **THE STAT SURFACE IS QUOTED AS A DELTA AND NEVER AS AN ABSOLUTE.** THREE different absolutes are live in this tree at one tip. Per `reference_a_drift_correction_is_itself_a_claim`, on a contested count: **NO NUMBER.** ⚠️ **AND THE REFUTED FIGURE MUST NOT BE SPELLED, IN EITHER ITS BARE OR ITS ARROW FORM — the phase-93 SPEC wrote a prohibition that QUOTED the token and thereby FALSIFIED ITSELF on its own grep.**
- ⚠️ do NOT "fix" the six in check (2) and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases. ⚠️ **Cite ~40 only as "sentence-visible", never as the inventory** — and **no gate may rest on a ` + ` split, in any form.**

==============================================

## STATUS AS OF 2026-09-01: PHASE 93 (`h2-local-reply-content-length`) **CLOSED**. **THIS SESSION SELF-PICKS THE NEXT SUBJECT AND RUNS ITS BRAINSTORM.**

**What the 93 IMPL landed.** The fix at the COMPOSER — `h2LocalReplyHeaders(bodyLen int)` (`router_h2.go`) emits `Content-Length` after `Content-Type`, Date-before-Server preserved. Seven call sites: four pass `0` (503/504), three pass `len(bad502Body)`=12 (502). Two NEW unit-test files, the differential instrument upgraded from an ARITY pin to a DECLARED-vs-DELIVERED pin, ADR-0315 completed in place, and row 93 flipped `done`.

**Cost, MEASURED by `--numstat` at the publishing tree (nine files):** `router_h2.go`+`retry.go` **`+14/-10`** · `h2dispatch_recompute_cl_test.go` NEW **`+147/-0`** · `router_h2_local_reply_cl_test.go` NEW **`+104/-0`** · `driver.go` **`+313/-69`** · `driver_test.go` **`+216/-45`** · `README.md` **`+11/-9`** · `DECISIONS.md` **`+131/-1`** · `BEHAVIOR_CONTRACT.md` **`+21/-0`** · `ROADMAP.md` row 93 flipped in place. `go.mod`/`go.sum` **BYTE-UNTOUCHED**.

### ⚠️ WHAT THE IMPL REFUTED — NINE CLAIMS, AND ONE WAS A FALSEHOOD ABOUT TO SHIP

1. ⚠️ **"THE WIRE BYTES ARE UNCHANGED" IS FALSE, AND IT WAS CAUGHT MID-STAGE IN A DRAFTED CONTRACT RIDER.** `writeH2Reply` overrides a `content-length` **only when the carrier already has one**; unlike `server` and `date` it has **NO synthesize-when-absent arm** — pinned by the new `absent_stays_absent` NEGATIVE CONTROL, which PASSES on the unmodified tree. So the H/2 502 previously reached the peer with **no `content-length` at all** and now carries one: a real change to the serialized response, and exactly why the fixture's subject arity went **0 -> 1**. **What is invariant is the VALUE (the recompute overrides whatever the composer passes), NOT the field's PRESENCE.** ⚠️ **THE PLAN'S SEPARATE ZERO-CHURN CLAIM IS ABOUT THE CROSS-SIDE *TRANSCRIPT*, IS TRUE, AND IS A DIFFERENT OBJECT** — that transcript records only `status=` and `illegal=` for the p92 arms and never carried `content-length`. **The two were being conflated. Keep them distinct.**
2. ⚠️ **A PLAN GATE WAS ARITHMETICALLY IMPOSSIBLE *AND* BLIND.** `git grep -c 'h2LocalReplyHeaders(' # want 9` claims `9 = 7 calls + def + doc mention`, but the doc mention has **NO OPEN PAREN**. MEASURED: the count is **8 BEFORE and 8 AFTER the fix** — **INVARIANT across the change it was meant to verify**, independently re-confirmed on an unfixed worktree. **The discriminating assertion is `git grep -n 'h2LocalReplyHeaders()' -- internal/` returning EMPTY.** ⇒ **A GATE WHOSE METRIC DOES NOT MOVE ACROSS THE FIX IS NOT A GATE — ask of every gate "what does this read on the OTHER side?"**
3. ⚠️ **THE PLAN'S PRESCRIBED "BEFORE" BASELINE WAS MEASURED IN THE WRONG STATE.** It instructed that the non-Docker sweep reads `FAIL=6` before the re-baseline. MEASURED at that point: the sweep is **GREEN for the driver package** (`FAIL=5`, an unrelated `internal/boot` SDS initial-fetch timing flake that passed 3/3 on re-run). `driver_test.go` is a **PURE COMPARATOR** whose rows read `p92WantSubjCLFields` from `driver.go`, so they can only redden **AFTER** the constant flips; `FAIL=6` reproduces exactly at that intermediate state. **The genuinely-red pre-fix gate is the DIFFERENTIAL**, which the sweep excludes — it failed with **exactly one error line**, itself proof the step-7 byte compare passed and only the step-8 pin fired.
4. ⚠️ **AN UNESCAPED `|` IN THE ROW-93 EDIT PASSED CHECK (1) AT THREE SEPARATE STATES.** The IMPL summary embedded a regex alternation; its THREE `|` characters made the row read **NF=11 under BOTH forms**. Escaping to `\|` fixed only the escape-aware form (8) and **left the naive form at 11**. The alternation was rewritten as a **COMMA LIST carrying no pipe at all**, giving **8 / 8**. ⚠️ **`reference_markdown_row_unescaped_pipe_passes_gate` FIRED FOR REAL — the sentinel was happy every time. COUNT THE FIELDS UNDER BOTH FORMS BEFORE AND AFTER ANY ROW EDIT.**
5. ⚠️ **THE PLAN'S ARCHIVE-EVICTION TIE DID NOT EXIST AT THE IMPL'S TIP.** The PLAN recorded `STATE.md:52` and `:54` both reading `2026-08-25` and broke the tie by lifecycle precedence. MEASURED at the IMPL by direct date read: `2026-08-29, 2026-08-29, 2026-08-26, 2026-08-26, 2026-08-25` — **a UNIQUE oldest, no tie-break required.** **Re-read the dates; never inherit an eviction rationale.**
6. ⚠️ **THE PLAN'S `+15/-10` PRODUCTION COST IS REFUTED — MEASURED `+14/-10`.** Its own line accounting could not reach 14 additions on `router_h2.go`.
7. ⚠️ **THE INSTRUMENT COST WAS UNDERSTATED BY BOTH PREDECESSORS** — SPEC `~+300/-44` and `~+57/-10`; MEASURED **`+313/-69`** and **`+216/-45`** (~3.8x on the unit table).
8. ⚠️ **THE `BEHAVIOR_CONTRACT` RIDER COST 7x THE PLAN'S ESTIMATE** (`~+3/-0` -> **`+21/-0`**), because it was written to the `phase 84.1` precedent rather than to the estimate.
9. ⚠️ **THE SPEC ENTRY'S OWN `PROPOSED`-FORM COUNTS WERE ALREADY STALE** — it landed "90 lines / 101 occurrences"; measured before the IMPL's edit, **91 / 105**, because the entry's own prose spellings are hits. **The IMPL therefore landed NO number for those forms.** ⚠️ **A COUNT OF A PATTERN, WRITTEN INTO A FILE THE PATTERN MATCHES, FALSIFIES ITSELF.**

### ⚠️ WHAT THE IMPL CONFIRMED BY EXECUTION — DO NOT RE-LITIGATE

- **PHASE 92'S PIN WORKED EXACTLY AS DESIGNED.** It predicted *"if envoy-go later gains a Content-Length on H/2 local replies … THESE PINS REDDEN and a human must consciously re-derive them."* That is precisely what happened, and the re-derivation **INVERTED the negative-control row rather than shifting it** — MEASURED: at the intermediate state the un-inverted row went RED, which is the silent-disarm the PLAN warned about, caught live.
- **The recompute pin's TWO NCs behave exactly as the PLAN predicted**: NC-1 (neutralise the recompute) reddens the two present-and-wrong rows and leaves `absent_stays_absent` GREEN; the ISOLATING NC-2b (synthesize-when-absent) reddens **exactly ONE row — the negative control**. **A coarse NC that reddens everything cannot show independence.**
- **The three-state `declared` observation is load-bearing**: pre-fix the subject reads `<absent>`, **not `0`**. A DUPLICATE yields `declaredOK = false`, **never "the first value"** (RFC 9113 §8.1.1) — pinned by a dedicated test whose duplicate rows carry a deliberately plausible first value, so a "take the first" implementation passes `declared == delivered` and **only that test catches it**.
- **The ROSTER BARRIER is mandatory and was NC'd**: neutralised, an empty observation slice satisfies every range-loop pin vacuously — reproduced exactly (`got 0 observations, want 5`, with the p93 pins correctly SILENT).
- **`p93WantRefBodyLen = 87` / `p93WantSubjBodyLen = 12` were RE-MEASURED LIVE over Docker at the IMPL**, by three positive-control break runs, not re-quoted from the PLAN.
- **§5.2's blind spot REPRODUCES EXACTLY** — `\b(503|504)\b` over `driver.go` returns exactly one line and it is a COMMENT. ⚠️ **THE INSTRUMENT IS STRUCTURALLY INCAPABLE OF SEEING THE BODY-LESS SITES. THAT GAP IS OPEN. DO NOT CLAIM IT CLOSED.**
- **There is NO stored baseline anywhere in `test/`** (`golden|baseline|snapshot` reads **0**) and `expectations.yaml` is PROSE, NOT MACHINE-EVALUATED (ADR-0019).

### ⚠️ BANKED CANDIDATES (the check-(2) inventory is SEPARATE — do not confuse them)
- **`ssl.connection_error`** — MEASURED ~4-6 production lines across 2 files, five `outcomeOther` rows already waiting. ⚠️ **THE BEST NEXT IN-WINDOW PICK.**
- **1xx interim responses** — DROP-AND-DELIVER; ~5-15 lines, one symbol.
- **The `Content-Encoding: gzip` over an UNCOMPRESSED body corruption** — H/2-only. ⚠️ **The phase-93 fix MAY have incidentally fixed it. NO CREDIT WAS CLAIMED and the reference-side comparison is UNMEASURED — verify before charter.**
- **The `h3dispatch.go` Rule B comment de-rot** — BANKED at the 93 PLAN, NAMED in ADR-0315 ¶9. Changes no behaviour. ⚠️ **`grep -c 'SetH2Action' internal/filter/hcm/h3dispatch.go` reads 0**, so no H/2 carrier reaches `writeH3Reply`. ⚠️ **`TestWriteH3Reply_EmptyBody` passes a NIL carrier — NOT a guard on the live path. Do not cite it as coverage.**
- **New arms driving the 503/504 classes** — the only way to make the instrument see the body-less sites.
- **The empty-body defect, BOTH legs** — ten sites. Reference bodies MEASURED: 87 / 167 / 19 / 24 B. ⚠️ **The contract ALREADY RELAXES local-reply body bytes** (`BEHAVIOR_CONTRACT.md:1993`) — a ratified relaxation, not an unnamed divergence. Adding a body emits a DATA frame where END_STREAM used to ride HEADERS.
- **The pooled-upstream-lifetime defect** — three sites, re-verified UNFIXED; reference side is a PREDICTION, three packages, probable SPLIT.
- **The H/1 no-`Host` divergence** — ⚠️ its `+90/-0` is a STALE PREDICTION.
- **`allocateOTLPPort`** — ⚠️ 26 driver files, not one site.
- **Counting the stat surface MECHANICALLY** — BLOCKED, not merely deferred.

## ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + ALL FOUR NCs YOURSELF** and record ACTUAL output. ⚠️ **CHECK (1) IS SILENT NOW, SO ITS NCs ARE THE ONLY EVIDENCE IT WORKS.**
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** 92 IMPL twelve · 93 BRAINSTORM eleven · 93 SPEC eleven · 93 PLAN twelve · **93 IMPL NINE, one of them a FALSEHOOD CAUGHT BEFORE SHIPPING.** Do the same, and record it when your agents refute you. ⚠️ **AND VERIFY IN BOTH DIRECTIONS: at the 93 IMPL an agent's report was doubted and proved RIGHT, and separately a controller grep read `0` only because the text was UPPERCASE — the instrument was blind, not the work.**
3. **LINE ANCHORS DRIFT; SYMBOL AND LITERAL-TEXT ANCHORS DO NOT — RE-VERIFY EVERY CITE AT YOUR TIP.** ⚠️ **PATHSPEC-SCOPE every symbol assertion.** ⚠️ **A "DRIFT-PROOF" ANCHOR IS ITSELF A CLAIM — CHECK IT FOR UNIQUENESS.** At the 93 IMPL, `val = strconv.Itoa(len(body))` matched TWO production lines and `return nil, 0, "dial: not a TLS connection"` matched TWO inside ONE file (`p92DriveArm` and `p90DriveArm`) — **a line-targeted edit would have silently corrupted the phase-90 driver.** ⚠️ **`internal/filter/hcm/chain.go` DOES NOT EXIST — it is `internal/filter/http/chain.go`** — but ⚠️ **`actions.go` IS in `hcm`.**
3b. ⚠️ **CHARACTER-CLASS AND ESCAPE SELECTORS ARE THE FAIL-UNSAFE FAMILY.** `grep -cE '^\s+[a-z0-9./-]+ v[0-9]'` on `go.mod` reads 62 not 67 · `^[0-9]{4}-` on fixtures reads 119, dropping `0007a`/`0007b` (use `ls -d test/fixtures/*/ | wc -l`) · ⚠️ **`\t` in a regex is TOOL-DEPENDENT: harness `grep` is ugrep 7.8.4, `/usr/bin/grep` is GNU 3.11. NAME THE TOOL.** ⚠️ **AND A CASE-SENSITIVE GREP IS BLIND TO UPPERCASE PROSE — use `-i` when auditing comments.**
3c. ⚠️ **`feedback_brief_citations_not_evidence` APPLIES TO INCIDENTAL FIGURES. Re-derive EVERY number you restate — including every number in THIS file.**
3d. ⚠️ **NO GATE MAY REST ON A ` + ` SPLIT.**
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Live: TWO SDS dial-budget flakes ⚠️ **plus `TestSDSEndToEnd_FetchFailure_BootFailsClosed`, which failed ONCE at the 93 IMPL with a `DeadlineExceeded` recv error instead of the initial-fetch-timeout wording and passed 3/3 on re-run** · the driver-owned receiver port race · `internal/httpclient` zero-value · the two 84.2-era flakes · the REFERENCE h2spec section-8 flip · `TestOutlierDetector_ConcurrentEjectExactlyOnce`. ⚠️ **`TestServerConn_TinyWindowDelivery` IS NOT A FLAKE — phase 91 FIXED a live production deadlock; a recurrence is a REGRESSION of row 91.**
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only). ⚠️ **THE FOREIGN-CONTAINER ROSTER IS NOT CLOSED AND CHANGED AGAIN BEFORE THE 93 IMPL.** Seen and LEFT UNTOUCHED: `curl-world-h3-1`, `curl-world-tls-1`, `curl-world-proxy-tls-1`, `curl-world-httpd-1`, `curl-world-httpbin-1`, `curl-world-quirks-1`, `curl-world-socks-1`, `curl-world-proxy-digest-1`, `curl-world-proxy-1`, `infallible_booth`, `crazy_kare`, `golink-ai`, `quizzical_goldstine`. ⚠️ **A `reaper_*` testcontainers Ryuk container is created BY THE DIFFERENTIAL ITSELF and is REUSED by later runs — leave it alone; it self-expires.** **Treat NO roster as stable.**
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`.
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL.** **`gofmt -l` never exits non-zero — gate on OUTPUT.** ⚠️ **`golangci-lint` DOES exit non-zero on findings — but STILL gate on OUTPUT.** ⚠️ **go test WITHOUT `-v` PRINTS ZERO `=== RUN`** — `RUN=0` beside `RC=0` is a VACUOUS GREEN. ⚠️ **A `-run` selector matching nothing prints `[no tests to run]` and EXITS 0.** ⚠️ **On `-v` output, unanchored `grep -c 'FAIL'` reads nonzero ON A FULLY GREEN TREE** — use `grep -cE '^(FAIL|--- FAIL)|^ *--- FAIL'`.
7b. ⚠️ **`grep -c` ON ZERO MATCHES PRINTS `0` AND EXITS 1** — capture (`v=$(… || true)`), NEVER chain with `&&`, and NEVER write `$(… || echo 0)`, which emits TWO zeros.
7c. ⚠️ **A GREEN TEST RUN IS NOT EVIDENCE A SITE IS EXERCISED — IT MAY BE DEAD CODE.** Prove it with a **`panic()` REACHABILITY CONTROL**.
7d. ⚠️ **NC EVERY NEW TEST, AND NC YOUR OWN FIXES** — a NC that is a BUILD BREAK proves nothing; **NEUTRALISE instead so the package still compiles.** ⚠️ **A literal revert of the 93 fix BUILD-BREAKS its own new test** — the IMPL dropped only the emitted field and kept the parameter.
7e. ⚠️ **A UNIT TABLE OVER A SHARED ASSERTION IS NOT A GATE ON PRODUCTION BEHAVIOUR** — and ⚠️ **`t.Helper()` IS WHAT SAVES IT**: it gives each failure a DISTINCT call-site line. **MEASURED at the 93 IMPL: `:76` and `:102`, two arms red in ONE run.**
7f. ⚠️ **A NC THAT LEAVES YOUR NEGATIVE CONTROL GREEN IS NOT EVIDENCE THAT CONTROL DOES ANY WORK** — you need a SECOND, ISOLATING NC.
7g. ⚠️ **ASSERT *WHICH* ERROR FIRED, AND ALSO WHICH DID NOT.** The 93 IMPL added a `wantErrAbsent` column beside `wantErrSubstr`: **asserting a pin fired says nothing about which pins did NOT.** ⚠️ **AND KEEP TABLE ROWS SINGLE-CAUSE** — a re-baseline turned one row into a compound-cause row that still passed, against the file's own doctrine; it was repaired.
8. ⚠️ **`pgrep -f` / `pkill -f` MATCHES YOUR OWN SHELL AND KILLS THE TOOL CALL (exit 144)**. **Kill ONLY PIDs you captured yourself.**
9. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-*`, `wt-*` or stage worktree should outlive its stage close** — expect only `master` (verify with `git worktree list`).
10. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** MEASURED AT THE 93 IMPL, every figure from that session's own runs: (a) differential **121/121 PASS, 0 FAIL, 0 SKIP**, fixture set reconciled BY NAME in BOTH directions; (b) non-Docker sweep **RC=0, FAIL=0, 125 ok + 109 no-test-files = 234**, gated on `PIPESTATUS[0]` and a SET RECONCILIATION (⚠️ **NOT `INNER_EXIT`, which DOES NOT EXIST**); (c) h2spec **`95 tests, 94 passed, 1 skipped, 0 failed`** — ⚠️ **h2spec is MEASURED BLIND to burst-drain ordering**; grpc-conformance deferred in writing; proxy-wasm **10 families measured** (NOT "10/16"); (d) fuzzers **56 / 48**; (e) the ANCHORED panic gate `^panic:|DATA RACE|SIGSEGV` on every differential launch, **0**; (f) no REVIEW.md — standing departure. ⚠️ **`-race` on the DIFFERENTIAL suite is VACUOUS** — the subject is an unraced subprocess.
11. ⚠️ **THE FIXTURE INVOCATION: `go test ./test/differential/ -run 'TestDifferential/0004-h2-routing' -count=1 -v 2>&1`.** ⚠️ **`-count=1` IS NOT OPTIONAL** — the harness builds envoy-go as a **SUBPROCESS**, so a router edit is **not a compile-time input** to that test binary and the cache serves a stale PASS. ⚠️ **Gate (a)'s failure mode is a SILENT PASS** — the runner `t.Skipf`s an unregistered fixture and **NO fixture-count gate exists anywhere in the tree**. **ASSERT THE FIXTURE SET BY NAME, BOTH DIRECTIONS.** ⚠️ **The full suite takes ~400s.**
11b. ⚠️ **`go test ./...` DRIVES DOCKER — AND THERE ARE **TWO** DOCKER DRIVERS, NOT ONE.** Besides `test/differential`, **`test/conformance/h2spec`** also drives Docker. **Exclude BOTH:** `go list ./... | grep -vE '/test/differential$|/test/conformance/h2spec$'`. Serialize Docker ownership across parallel agents.
12. ⚠️ **`-p` PUBLISHING WORKS; `--network host` IS WHAT DOES NOT.** ⚠️ A **STRICT_DNS** cluster whose name does not resolve **blocks listener init entirely** — prefer `STATIC` by IP. ⚠️ **`clusters: []` is BOOT-REJECTED.** ⚠️ **envoy-go REJECTS `match.headers`**, ⚠️ **refuses `codec_type: HTTP2` without TLS unless `--allow-h2c`**, ⚠️ **boots with `-c`, NOT `--config-path`**, and ⚠️ **`access_log[].log_format` is BOOT-REJECTED.** ⚠️ **`ENVOY_TARGET.md` is at `docs/envoy-go/ENVOY_TARGET.md`, NOT the repo root.** ⚠️ **A PROBE BACKEND MUST BIND `0.0.0.0`, NOT `127.0.0.1`.** ⚠️ **The pinned reference is `envoyproxy/envoy:contrib-v1.37.2`, digest `7edd5b0fd763…` — VERIFY IT MATCHES `ENVOY_TARGET.md` BEFORE TRUSTING A REFERENCE RUN.**
13. ⚠️ **A RAW-FRAMER PEER MUST HOLD ONE `hpack.Decoder` PER CONNECTION.** ⚠️ **Set `ReadMetaHeaders` to NIL when probing malformed shapes.** ⚠️ **`0004` is TLS+ALPN-h2, NOT h2c.**
14. ⚠️ **`bash /dev/tcp` + `timeout cat <&3` INSIDE COMMAND SUBSTITUTION RETURNS SILENTLY EMPTY.**
15. ⚠️ **GO DOES NOT FLAG UNUSED CONSTANTS.** Post-removal cleanup is grep-driven.
16. ⚠️ **`golangci-lint`'s misspell runs in locale US and HAS NOW FIRED ON THREE CONSECUTIVE STAGES' PROTOTYPES** (the SPEC's `favour`, the PLAN's `behaviour` x2, the IMPL's `Modelling`). **Sweep British spellings in `.go` comments BEFORE the gate.** ⚠️ **Markdown prose may use them freely.**
17. ⚠️ **A GREEN RUN OF A CONTENTION-SENSITIVE TEST PROVES NOTHING.** Phase 91's subject cleared 5/5 uncontended while failing 9/32 contended on the SAME binary.
18. ⚠️ **AN ABSENCE CLAIM NEEDS A POSITIVE CONTROL AND A DISCRIMINATING NEGATIVE CONTROL.**
19. ⚠️ **DERIVE next-free ADR FROM THE TAIL, NEVER FROM THE HEADING COUNT.** The id space is SPARSE (exactly one gap, `0209`) and **headings+1 reads 315 — a TAKEN id.**
20. ⚠️ **`reference_measured_prototype_is_a_lower_bound` HAS NOW FIRED EIGHT CONSECUTIVE ROWS, ALWAYS BY UNDER-ENUMERATING *FILES*.** At the 93 IMPL it fired again: the PLAN enumerated EIGHT stale README lines and a NINTH existed (`:151`), describing the very pin the row re-baselined. **ENUMERATE BY `git grep -l` ON THE LITERAL CLAIM TEXT, AND BY COMPILING PROTOTYPE.**

## ⚠️ THE `PROPOSED` GUARD — DISARMED, AND THE DECOY IS WHAT REMAINS

| form | now | where |
|---|---|---|
| `^> \*\*STATUS: PROPOSED` — **the house form** | **0** | DISARMED at the 93 IMPL; the line was `:18650` under `## ADR-0315` |
| `^\*\*Status:\*\* PROPOSED` — **the ADR-0231 decoy** | **1** | `:14866` under `## ADR-0231`, byte-untouched for many phases |
| union of both | **1** | — |

⚠️ **VERIFY BY LINE AND BY ADR, NEVER BY THE COUNT ALONE** — a session gating on the union or the decoy reads `1` and wrongly concludes an ADR is still open. ⚠️ **NEVER gate on the UNANCHORED form nor the middle-ground `^\*\*Status:\*\*.*PROPOSED` form.** ⚠️ **AND DO NOT QUOTE A COUNT FOR EITHER OF THOSE TWO FORMS: the phase-93 SPEC's landed figures for them were MEASURED STALE at the IMPL, because the entry's own prose spellings are hits.** This is `ADR-0312 §Consequences (x)`: the SPEC hit it in the ARM direction, **the IMPL hit it in the DISARM direction.**

## ⚠️ THE ARCHIVE GUARD

⚠️ **THE STRICT `163` IS NOT THE ENTRY COUNT.** Measured at the 93 IMPL close: loose **219** = strict **163** + parenthetical **56**, exactly. **The guard is SOUND as a DELTA-0 SHAPE check** (a correctly-shaped parenthetical append moves it by 0) **but the number is a SUBSET.** The forms disagree and **any gate must name WHICH it uses.**

⚠️ **THE ARCHIVE'S POSITIVE CONTROLS ARE SELF-INCREMENTING.** A control figure recorded in the file it measures is invalidated by the act of recording it. ⚠️ **THE IMPL'S ARCHIVE LINE THEREFORE NAMES NO CONTROL FIGURE AT ALL** — the simplest correct move. If you name one, you MUST disclose the +1-per-use. ⚠️ **The eviction check has the same trap:** if the §Recent preamble NAMES the evictee, a naive grep on `STATE.md` reads **1**. **The DISCRIMINATING form is the strict `^- \*\*prior active-phase:\*\* \`<label>\`` form.** ⚠️ **AT THE 93 IMPL BOTH forms read 0**, because the preamble was worded to describe the evictee without spelling its label — **do that.**

## ⚠️ PORT BANDING

`net.ipv4.ip_local_port_range = 32768 60999`. ⇒ **BAND YOUR PROBE PORTS BELOW 32768.** ⚠️ **The differential harness reserves `20000..31007` (`harness_test.go:293`)**, so `12000-19000` is the safe band for ad-hoc probes — a failure OUTSIDE the harness band is an **out-of-band** occurrence, NOT an in-band recurrence. **Check availability with `ss -tan` (ALL states), never `ss -ltn`.** ⚠️ **FIXTURE ports are FAMILY-BANDED as `10<index>` — but `10120` is TAKEN (fixture `0028`); free in-band is `10126-10129`.** ⚠️ **`0120` as a DIRECTORY is FREE.**

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (**ugrep 7.8.4**). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **THE METHOD BINDS; carry no standing discrepancy figure and no standing absolute.** ⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS; print FILE NAMES, never a count.** ⚠️ **THE HARNESS `grep` STRIPS A LEADING `./`.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files; use `-v --no-index`, and `git status` is the discriminating check). ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **A DISCREPANCY MAY FAIL TO MANIFEST** — absence of a discrepancy is not evidence the blindness is gone.

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 93 (h2-local-reply-content-length)' --format='%h %s'   # the SLUG form — a CLOSED phase, use as a FIRING CONTROL
git log --grep '^phase 93' --format='%h %s'                                   # the LOOSE form — RUN IT to CROSS-CHECK
```
⚠️ **RUN BOTH FORMS AND RECONCILE THE SETS. NO COUNT IS QUOTED HERE, DELIBERATELY — BOTH FORMS ROT.** **A `| head -1` on ONE form can silently return a NON-TIP commit. Take the tip from `git rev-parse master`.** ⚠️ **AND WHEN YOU LAND A CORRECTION, PUT THE FULL SLUG IN ITS SUBJECT.**

Execution style: **subagent-driven per `feedback_execution_style`**. Subagents commit LOCALLY on their own stage branch with EXPLICIT PATHSPECS; the controller merges and squashes (`feedback_subagents_no_push`, `feedback_push_to_origin`). **Pure MEASUREMENT agents should commit NOTHING and prove their tree clean.** ⚠️ **PARALLEL AGENTS IN ONE WORKTREE RACE THE GIT INDEX EVEN WITH DISJOINT PATHSPECS** — use **PER-AGENT WORKTREES ON THEIR OWN BRANCHES**, merged by the controller. **MEASURED AT THE 93 IMPL: five per-agent worktrees on disjoint files merged with ZERO conflicts.** ⚠️ **GIVE EACH PARALLEL AGENT ITS OWN PRIVATE SCRATCH DIR**, and **SERIALIZE DOCKER OWNERSHIP TO EXACTLY ONE AGENT AT A TIME.** ⚠️ **VERIFY WHAT A SUBAGENT REPORTS — BUT ALSO BELIEVE IT WHEN IT REFUTES YOU.** Per-task `gofmt` + `golangci-lint` on touched packages, **gated on OUTPUT**. ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root** — build with `-o` into scratch.

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root — **observed REPEATEDLY at the phase-93 IMPL, with the harness printing `Shell cwd was reset to /home/esa/git/envoy-go`**. Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` before any commit or gate run. ⚠️ **Shell VARIABLES do not survive between Bash calls** — re-export in every command. ⚠️ **AND AN UNQUOTED HEREDOC (`<<EOF`) COMMAND-SUBSTITUTES BACKTICKED TEXT** — use `<<'EOF'` and pass paths via the environment.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — `git status --porcelain` AND `git diff --stat master` must show ONLY your intended edits. For a patched TRACKED file: `sha256sum` BEFORE patching, `git checkout -- <path>` after, `sha256sum -c` the capture — ⚠️ **and COMMIT FIRST, because `git checkout --` restores from HEAD and will wipe uncommitted work.** ⚠️ **Also check the MAIN repo root, and `--untracked-files=all` to catch throwaway probe files.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads SELF-PICK + BRAINSTORM.** ⚠️ **`grep -c 'next-skill:'` reads 2: one is the ADR-0288 RULE STATEMENT matching its own grep, and the live pointer is the other. That is CORRECT, not a violation.** ⚠️ **§Project counts is a PHASE-76-ERA SNAPSHOT and SAYS SO.**
2. `docs/envoy-go/ROADMAP.md` — the deferred-candidate windows at `:203 :209 :215 :225 :231 :239` are **the check-(2) inventory and the ONLY remaining blocker.** Read row 93 (`:155`) for the closed row's full record.
3. `docs/envoy-go/DECISIONS.md` **ADR-0315** — the completed phase-93 decision. ⚠️ **Its §Context ¶2 records that the `content-length: 0` doctrine is ratified in **ADR-0155** (`:8187-:8381`), ATTRIBUTED THERE to ADR-0085 — the doctrine is NOT in `## ADR-0085` (`:3282-:3327`), which is *"Admin-mux reuse"* and returns ZERO matches. Do not restate the misattribution.**
4. `docs/envoy-go/BEHAVIOR_CONTRACT.md` tail — the phase-93 rider.

## What the next session owes
**SELF-PICK the next subject and RUN ITS BRAINSTORM** (state DONE -> 1), recording the pick AND the rejected alternatives · **RE-RUN the sentinel + all four NCs yourself** — ⚠️ **check (1) is SILENT now, so its NCs are the only proof it works, and NC-A/NC-B are ONE-line shapes** · **REGISTER the new row in `ROADMAP.md`** (`want` **125 -> 126**) — ⚠️ **COUNT THE ROW'S FIELDS UNDER *BOTH* THE NAIVE AND THE ESCAPE-AWARE FORM, BEFORE AND AFTER INSTALLING; an unescaped `|` PASSES check (1) and the phase-93 IMPL hit this for real** · **ANTICIPATE `ADR-0316`** (TAIL-derived; headings+1 COLLIDES at the `0209` gap) · **REFUTE YOUR PREDECESSOR BY EXECUTION** · `STATE.md` rolled IN PLACE with the oldest §Recent entry evicted by a **DIRECT DATE READ** (⚠️ **read the dates yourself; the 93 PLAN recorded a tie that did NOT exist one stage later**) and archived to `STATE_HISTORY.md` as **ONE INLINE LINE** in the PARENTHETICAL form, so the strict guard stays at **163, DELTA 0** · ⚠️ **AND ROLL THE §Recent PREAMBLE SENTENCE TOO — the guard is structurally blind to it** · ⚠️ **WORD THE PREAMBLE SO IT DOES NOT SPELL THE EVICTEE'S LABEL, or the naive eviction check reads 1** · ⚠️ **NAME NO POSITIVE-CONTROL FIGURE IN THE ARCHIVE LINE, or disclose that you have just incremented it** · `next-prompt.txt` rolled (`git add -f`) · ONE squashed commit, merged and pushed — ⚠️ **and if a late measurement invalidates something you already pushed, land a CLEARLY-LABELLED correction commit (WITH THE FULL SLUG IN ITS SUBJECT) rather than force-pushing or leaving it standing.**
