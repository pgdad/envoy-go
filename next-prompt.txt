=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

**SENTINEL RE-CHECKED 2026-07-08 — it does NOT fire. Do NOT create `stop`.** Evidence: (a) **ROW 55 IS `in-progress`** (lifecycle-state 2; the PLAN and IMPL remain); (b) the **Observability family** STAYS OPEN even after row 55 — `graphite` + OTLP-metrics sinks, tracing `custom_tags`/`spawn_upstream_span`/`http_service`/force-trace, the tap filter; (c) the **Operational-tooling family** STAYS OPEN — xDS-sourced dry-validation, an admin-API live-reload-and-validate endpoint, an RTDS/SDS validate companion; (d) FIVE families have never been opened — **HTTP/3 + QUIC**, **gRPC**, **xDS / dynamic config**, **Runtime + hot restart**, **WASM host**.

**STATUS AS OF 2026-07-08: phase 55 (`stats-sink-statsd-tcp`) SPEC is DONE and LANDED (`1eecd76e`, FF-merged to master + pushed). Row 55 `in-progress`, lifecycle-state 2.**

**THE NEXT ACTION: the phase-55 (`stats-sink-statsd-tcp`) PLAN.**

==============================================

## What is already DECIDED — do NOT re-litigate

The BRAINSTORM settled Q1–Q5 via user dialogue. The SPEC then executed the live probes and **AMENDED three of them**. The current, authoritative design is the **SPEC**, not the BRAINSTORM. Where they disagree, the SPEC wins.

- **Q1 writer shape → BOUNDED CHANNEL + WRITER GOROUTINE** (the `MetricsServiceSink`/ADR-0262 shape). Unchanged. `delta.apply` runs in the WRITER, not `Submit` (ADR-0263).
- **Q2 dial → NOT `Cluster.Dial`.** **AMEND-TCP-CXSTATS:** the reference's stats cluster shows `upstream_cx_total: 0` / `upstream_cx_active: 0`, and with `max_connections: 0` it still connects and flushes. A NEW narrow, exported `Cluster.DialSink(ctx) (net.Conn, error)` — LB pick + `connect_timeout` + upstream TLS; **NO permit, NO `upstream_cx_*`, no `connWithGauge` wrap**. `internal/statssink` still must NOT import `internal/cluster` (a `func(context.Context) (net.Conn, error)` seam; `main.go` closes over the manager).
- **Q3 error policy → RE-DECIDED.** **AMEND-TCP-RESUME:** the reference does NOT drop a failed flush; it ACCUMULATES unwritten snapshots and delivers all of them on reconnect (45 over a 30s outage, nothing lost or duplicated). The policy is **bounded accumulate + line-aligned resume**: on a write error, `conn.Close()`, `conn = nil`, and `pending = pending[bytes.LastIndexByte(pending[:n], '\n')+1:]`. Complete lines that landed are never re-sent; the straddling line — which the dead conn's stream parser discards at EOF — is re-sent whole. `pending` is BOUNDED by `maxPendingBytes` (drop-oldest + rate-limited log): a deliberate, documented departure from the reference's unbounded buffer.
- **Q4 differential → payload parity CROSS-SIDE + subject-exact transport assertions.** **AMEND-TCP-CONNCOUNT:** the reference opens 1 conn idle / **2** under load (conn #2 is `|ms`-only, the worker sink). envoy-go has no histograms (ADR-0060) so it can never open it — cross-side conn-count equality is infeasible **by the histogram boundary**. Subject asserts `ConnCount() == 1` AND `cluster.c_statsd.upstream_cx_total == 0`.
- **Q5 envelope → SINGLE FLAT ROW, ADR-0272.** The amendments grew it to **~250–330 prod LoC**. Escape valve re-armed: **if your decomposition exceeds ~14 tasks, revisit the ADR-0045 split gate.**
- **AMEND-TCP-NODE (new reject):** the reference refuses to boot a `tcp_cluster_name` sink unless BOTH `node.id` and `node.cluster` are set; the UDP sink boots with no node. envoy-go mirrors it byte-stably.
- **AMEND-TCP-USEDONLY:** the reference emits only USED stats, so the emitted line SET differs cross-side by construction. Assert NAMED SUBSETS only, never the whole set.

## What THIS session does (the PLAN, lifecycle-state 2 → 3)

1. Re-verify the cold-start baselines (below) against master tip `1eecd76e`. Do NOT trust any count in this file without re-running the commands.
2. Invoke `superpowers:writing-plans` and decompose **SPEC §10** (14 indicative tasks) into a bite-sized TDD spine. Apply the **plan-document-reviewer gate** (dispatch a review of the PLAN against the SPEC *and the actual as-built source* — every prior PLAN that skipped it leaked wrong line citations, wrong test snippets, or wrong import lists).
3. **Resolve the FIVE §12 D-questions:**
   - **D-TCP-CLOSE (LOAD-BEARING).** The race-free mechanism by which `Close` reaches the writer-goroutine-owned `conn` to unwedge a blocked `Write`. `cancel()` CANNOT interrupt a raw `net.Conn` (unlike a gRPC stream — this is precisely where the `MetricsServiceSink` precedent stops transferring). Candidates: an `atomic.Pointer[net.Conn]`, a ctx-watching closer goroutine, or relying solely on a per-write `SetWriteDeadline`. **Must be `-race`-PROVEN at IMPL, not argued in prose.**
   - **D-TCP-CONFIG-SHAPE.** A tagged-union `TCPClusterName string` on `StatsdSinkConfig` (one slice, one `main.go` loop, a two-arm branch) vs. a separate `Bootstrap.StatsdTCPSinkConfigs` slice (consistent with the landed three-separate-slices shape).
   - **D-TCP-PENDING-CAP.** The value of `maxPendingBytes`, and whether `dropOldestLines` drops whole LINES or whole SNAPSHOTS. The reference is unbounded, so there is NO value to mirror — this is an envoy-go choice, justify it.
   - **D-TCP-NODE-PLACEMENT.** Whether the node-required reject lives in `parseStatsdSinkConfig` (via `result.Proto.GetNode()`, so the landed `FuzzStatsdSinkConfigParse` covers it — **PREFERRED**) or at the `main.go` sink-build site. Verify `result.Proto` is populated at that point before committing to it.
   - **D-TCP-DIALNAME.** The exported name/signature of the unaccounted dial (`DialSink`? `DialUnaccounted`?) and whether it shares a private helper with `Dial`.
4. FINAL ADR-0045 split-gate re-check against the grown envelope. Write `docs/envoy-go/phases/55-stats-sink-statsd-tcp/PLAN.md`, advance `docs/envoy-go/STATE.md`, land it (commit + push), then **ROLL THIS FILE FORWARD to point at the phase-55 IMPL**. Row 55 STAYS `in-progress`.
5. NO new live probes are needed — the SPEC executed all seven empirical pins. If the PLAN nonetheless needs one, run it; do not guess.
6. PLAN is docs-only. Direct-on-master or a docs worktree; the fresh-worktree + subagent discipline is MANDATORY at IMPL.

## Three traps the PLAN must not walk into

- **`statsdrecv` is a DATAGRAM parser and this is now MEASURED, not hypothetical.** `ingest` (`statsdrecv.go:131`) splits a UDP datagram on `\n`, where every line is complete by construction. The probe's ~200 KB post-reconnect write arrived in `recv()` chunks capped at 65536 bytes, **splitting lines mid-token**. The TCP listener must read line-at-a-time through `bufio` with a carried remainder and DISCARD an incomplete trailing line at EOF — §3.5's no-duplication argument DEPENDS on that discard. Per `reference_line_parser_extension_delimiter_reuse`, trace one concrete split-read example byte-for-byte in the task body.
- **The `bootstrap.go:556-561` ordering constraint survives the lift with INVERTED meaning.** `GetAddress()` returns nil for BOTH a missing oneof AND a `tcp_cluster_name` arm, which is why the reject ran FIRST. After the lift the `tcp_cluster_name` arm must be **dispatched** before the nil-`address` reject fires, or a valid TCP config is rejected as "missing `statsd_specifier`". A regression test MUST pin this.
- **`TCPStatsdSink` is the statsd sinks' FIRST background mutator.** A `-run`-subset `-race` will MISS the class of failure it introduces (`reference_full_suite_race_after_background_mutator`). The IMPL merge gate runs a FULL-package `-race` on `internal/statssink`, `internal/cluster`, AND `test/differential`.

## Honor throughout (the discipline, unchanged)
- **Lifecycle = one stage per session.** This session: the phase-55 PLAN ONLY.
- **Subagent-driven execution** (`feedback_execution_style`) in a **fresh worktree** (`feedback_git_worktrees`) — mandatory at IMPL; subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit, re-runs gates on the frozen HEAD, does deliberate-break/liveness verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). Per-task gates: `gofmt -l` + `golangci-lint` + `go vet` + `go build ./...` (`feedback_pertask_gofmt_lint`).
- **Process anchors:** ADR-0106 · ADR-0044 (ADR §Decision+§Consequences at IMPL; §Context drafted at SPEC §13) · ADR-0045 (split soft gate) · ADR-0080 (byte-stable rejects) · ADR-0052 (atomic six-gate bundle) · ADR-0227 (reference `envoyproxy/envoy:contrib-v1.37.2`) · ADR-0262 (bounded-channel writer-goroutine sink) · ADR-0263 (delta in the WRITER) · ADR-0252 (the `max_connections` permit `DialSink` must NOT take) · next-free **ADR-0272**.
- Re-verify baselines fresh at cold-start against current HEAD:
  - `ls -d test/fixtures/[0-9][0-9][0-9][0-9]* | wc -l` → expect **99** (next fixture dir: `0098-*`)
  - `grep -rh '^func Fuzz' --include='*.go' --exclude-dir=.worktrees . | wc -l` → expect **52**
  - `grep -nE '^## ADR-0[0-9]+' docs/envoy-go/DECISIONS.md | tail -1` → expect **ADR-0271** (next-free **ADR-0272**)
  - `grep -n "BackendKind = " test/differential/fixture/fixture.go | tail -1` → expect **38**
  - stat surface → **1200**

## Read first (in order)
1. THIS FILE — the cold-start router (phase-55 PLAN is next).
2. `docs/envoy-go/phases/55-stats-sink-statsd-tcp/SPEC.md` — **THE authoritative design.** §3 the design; §6 the reject roster; §8 the `0098` fixture + FOUR deliberate breaks; §10 the 14 indicative tasks; §11 the executed empirical pins (evidence for every claim); §12 the five D-questions you must resolve; §13 the ADR-0272 §Context draft.
3. `docs/envoy-go/phases/55-stats-sink-statsd-tcp/BRAINSTORM.md` — the Q1–Q5 dialogue. **Superseded by the SPEC on Q2, Q3, and Q4's rationale.**
4. `docs/envoy-go/STATE.md` — the active-phase header (`phase 55 SPEC done`) + full history.
5. The as-built source: `internal/statssink/{statsd.go,udp.go,delta.go,flusher.go,sink.go}` (`emitStatsdLines` at `udp.go:58`; the `metricsClient` seam at `sink.go:29-32`; the `Close` shape at `sink.go:135-148`), `internal/cluster/cluster.go` (`Dial` at `:536`, `PickEndpoint` at `:506`, `connDec` at `:359-365`, `ConnectTimeout`/`UpstreamTLSConfig`), `internal/bootstrap/bootstrap.go` (the `:567-568` reject, `parseStatsdSinkConfig` at `:562`, the `:556-561` ordering comment, `StatsdSinkConfig` at `:297`), `cmd/envoy-go/main.go` (`cm` at `:105`, `minNode` at `:155`, sink loop `:194-232`, Flusher start `:308-315`), `test/helpers/statsdrecv/`, `test/fixtures/0092-stats-sink-statsd/`.
6. `internal/grpcclient/grpcclient.go:109-141` — the cluster-re-lookup-per-dial callback the `main.go` dial closure mirrors.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — especially `reference_full_suite_race_after_background_mutator`, `reference_line_parser_extension_delimiter_reuse`, `reference_delta_sink_differential_stability_barrier`, `reference_periodic_sink_differential_two_receivers`, `reference_host_gateway_ip_docker_desktop`, `reference_membership_total_vs_healthy_gauge`, `reference_differential_backendcount_min_one`, `reference_differential_run_selector`, `reference_differential_break_protocol_count1`, `reference_fuzzer_count_docs_drift`, and the `feedback_*` execution discipline.
