=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-VERIFIED 2026-07-17 (re-run MECHANICALLY at the phase-67 BRAINSTORM close, in the stage worktree AFTER the row-67 registration edit) — it does NOT fire. `stop` was NOT created.** Actual output:
- **(1) prints `NOT DONE: row 67`** — row 67 registered `in-progress` at this close, so check (1) BLOCKS `stop` again (it had gone silent when row 66 flipped `done`). The loop is held open by ALL THREE checks.
- **(2) prints THREE live "candidates:" sentences** — HTTP/3, xDS, Observability ⇒ three families STAY OPEN. **NOTE: the Observability line ALSO carries HISTORICAL `candidates were:` recaps** (`reference_sentinel_deferred_sentence_live_vs_historical`; the live grep needs "candidates:" adjacent). Exact command, and ONLY it — **cite the command, never the adjective**: `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` ⇒ **3**. (Bare `grep -c 'candidates:'` returns a different, WRONG number.)
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** ⇒ THREE families never opened. Operational-tooling also STAYS OPEN.

All three checks print ⇒ the sentinel does NOT fire. Abundant work remains.

==============================================

## STATUS AS OF 2026-07-17: THE PHASE-67 BRAINSTORM IS COMPLETE AND LANDED AT THIS CLOSE. ROW 67 IS `in-progress`. THIS SESSION PERFORMS THE PHASE-67 SPEC.

**What the BRAINSTORM landed** (`docs/envoy-go/phases/67-tls-require-client-cert-false/BRAINSTORM.md`; locate the squash by SUBJECT — `git log --grep 'phase 67'`, `reference_next_prompt_tracked_despite_gitignore`; never by position): the **FIFTH xDS-family row**, self-picked per the 2026-07-12 standing directive (runner-up: the empty-dynamic Design B fallback; full rejected roster in BRAINSTORM §2.1) — **verify-if-presented mTLS**: `require_client_certificate: false`/absent honored via `ClientAuth = VerifyClientCertIfGiven` across ALL THREE validation shapes (fully-inline / SDS-VC / CVC) at the TCP apply points; the anchor arms hoist OUT of the require gate (the un-gated SDS fetch is a NEW ADR-0280-family departure instance the SPEC must NAME); the phase-66 E3 reject RETIRES atomically with the lift; QUIC client-auth provisionally OUT; fixture `0110-tls-require-client-cert-false` anticipated (the 0109 chassis + a third arm + a forced-send driver). Evidence: a 21-fresh-container-arm reference cross-product + SDS fetch-gating arms + a Go `crypto/tls` cross-product with byte-identical wire-alert pins (48/116). **The adversarial record: THREE independent verifiers (code-claims re-derivation / probe reproduction with fresh PKI+containers / process-consistency) found FIFTEEN distinct defects, ZERO severe — all corrected in place.** The load-bearing finding is a probe-vs-landed-docs DRIFT on the reference's validation-context fetch-failure posture → the named SPEC obligation **D-RCCF-FETCHFAIL-POSTURE**.

**⚠️ PUSH (idempotent — evaluate, then act):** GitHub was UNREACHABLE for this entire session (network outage; the phase-66 squash AND this close may both be unpushed). FIRST ACTION: `git fetch`; if origin/master is BEHIND local master and the local tip is the phase-67 brainstorm squash, `git push origin master` per `feedback_push_to_origin`; if in sync, proceed; if REJECTED (diverged), STOP and reconcile — never rebase away landed squashes.

**Key facts a cold session needs (RE-VERIFY MECHANICALLY; do not copy):**
- Counts: fixtures **111** (`ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0109-xds-sds-combined-validation-context`) · fuzzers **55** (`grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`) · DECISIONS tail **ADR-0288** (next-free **ADR-0289** — the phase-67 SPEC drafts ADR-0289 §Context per ADR-0044-as-used; re-derive with `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1`) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** (docs-verified; NO mechanical counting command) · go.mod modules **2** (the phase-61.2 lineage figure — NOT a repo total; the single `go.mod` requires 67).
- **STATE.md discipline (ADR-0288):** §Current pointer reads `phase 67 BRAINSTORM done` / NEXT = the phase-67 SPEC. **EDIT §Current pointer IN PLACE — do NOT prepend**; demote the outgoing bullet into §Recent lineage, **cap at FIVE**. `STATE_HISTORY.md` is the archive; every pointer in it is stale BY CONSTRUCTION — never grep it for a pointer or a count.
- `.worktrees/phase-67-brainstorm` and branch `phase-67-tls-require-client-cert-false-brainstorm` are REMOVED at close (squash landed; locate the squash by SUBJECT).

## What THIS session does — phase 67 SPEC

One stage per session: SPEC ONLY. Docs-only plus THROWAWAY probe harnesses (deleted before commit — the SPEC-66 precedent). Fresh worktree off master, branch `phase-67-tls-require-client-cert-false-spec`, per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; commit locally, controller squash-pushes (`feedback_subagents_no_push`, `feedback_push_to_origin`).

**The SPEC must RE-DERIVE the BRAINSTORM rather than execute it** (`feedback_brief_citations_not_evidence` — line numbers in BRAINSTORM §6 are cited off master `0d4d4041` and WILL drift; symbols are the citation, D-RCCF-DOCSHAPE). The obligations it OWES:

- **D-RCCF-FETCHFAIL-POSTURE** — reconcile the per-resource-type reference fetch-failure posture BEFORE drafting ADR-0289 §Context: three landed sites (BEHAVIOR_CONTRACT:900, ADR-0286 D-SDSVC-FETCHTIMEOUT, `internal/tls/config.go:116`) record serve-anyway — a SERVER-CERT-probe extrapolation from SPEC-60 §11 arm B — where this session's validation-context probe pinned init-hold-then-FAIL-CLOSED. Decide whether the arm-B extrapolation was wrong or the postures genuinely differ per resource type, ideally by a **discriminating server-cert-fetch-failure probe**. NEVER a silent rewrite of the landed sites.
- **D-RCCF-ANCHORLESS-VC-FALSE** — probe the ONE unprobed cell (require=false/absent + a `validation_context` present WITHOUT `trusted_ca`, inline) before choosing reject vs NoClientCert-boot; fresh container per arm.
- **D-RCCF-QUIC** — the final in/out call (provisionally OUT; if IN, probe reference QUIC client-cert behavior first and size the pre-scan surgery honestly — `boot.go`'s pre-scan skips QUIC sockets).
- **Re-witness one pinned reference cell as a probe-session control** (V2's honest limit: the inline/CVC handshake cells rest on this session's 21-arm run).
- **The ADR-0289 §Context draft** per ADR-0044-as-used (the citation drift is flagged at BRAINSTORM §7; follow the usage, do not fix the attribution in this row).
- **Probes must DISCRIMINATE** (`reference_probe_must_discriminate`) — run the cross-product, never a single non-discriminating arm; **fresh container per arm + fresh driver-owned servers + a served-this-arm assert** (`feedback_probe_fresh_container_per_arm`, the phase-66 extension).
- **The `GetClientCertificate` forced-send driver trap binds ALL client-cert arms** — a polite Go client (`tls.Config.Certificates`) SILENTLY WITHHOLDS a cert whose issuer is absent from the server-advertised acceptable-CA list, turning an untrusted arm into a second none arm (vacuous green). `0110`'s driver MUST force-send; verify whether the `0108`/`0109` chassis already does or relied on same-CA politeness.
- **D-RCCF-NILPOOL-GUARD** — the nil-pool construction-order hazard: `VerifyClientCertIfGiven` + nil `ClientCAs` falls back to the SYSTEM root pool — the WORST direction (trusted clients REJECTED, anonymous ACCEPTED). Pin the construction order that makes the state unrepresentable (assignment-adjacency) + the unconstructibility test.
- **Per-side failure-string pins with byte-identical wire alerts 48/116** — client-observed failure STRINGS differ per side (OpenSSL vs Go), the wire alert bytes agree; pin per-side (`reference_differential_reference_parses_full_message`; `reference_wire_format_both_sides_see_same_bytes` is SATISFIED, never invoked as a deviation).
- **Stat surface +0, with the cross-side three-arm verdict CONTRAST per ADR-0286 C3** — envoy-go emits ZERO `ssl.*` stats; the accept/reject contrast (trusted→ok · untrusted→rejected · no-cert→ok) is the proof the anchor is LIVE, and the ssl-stat framework-surgery row is NOT a dependency.
- Also on the SPEC's docket (BRAINSTORM §10, ranked by blast radius): D-RCCF-FETCHGATE (pin the departure wording) · -E3-RETIRE (confirm E3 guards nothing else; lift+retirement ONE task; retained-reject roster byte-diffed) · -P-PREMISES (re-derive P1-P5 under the moved gate; the P5 comment survives) · -DRAGGED · -CERTPROVIDER · -FUZZSEED (re-derive the fuzz harness's provider dispatch first — a seed can be vacuous behind an earlier gate, the phase-66 T5 lesson) · -VALIDATE · -STATS (+0 confirm) · -SPLIT (single flat row ~8-12 tasks) · -DOCSHAPE · -FIXTURE (the shape split: which SDS shape is `0110`'s primary).
- **Run an adversarial verification pass before landing** — a PRIVATE `mktemp -d` per parallel verifier (`reference_parallel_subagents_private_scratch`); phases 64-67 averaged ~10-15 defects per stage document, found ONLY by independent re-derivation with a default of REFUTED.
- Sentinel: re-run MECHANICALLY at close. A SPEC completes nothing — check (1) keeps printing `NOT DONE: row 67` and checks (2)+(3) keep printing ⇒ do NOT create `stop`. No deferred-sentence edit at ANY stage (a §8-tier pickup; do not fabricate a narrow).

## Read first (in order)
1. `docs/envoy-go/phases/67-tls-require-client-cert-false/BRAINSTORM.md` — the chartered row; §10 is the SPEC's ranked docket; §2.3-2.5 carry the pinned probe tables (ClientAuth cross-product, fetch-gating, nil-pool); §6 the edit-site roster (re-derive, symbols not lines).
2. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY to grep; ADR-0288 discipline).
3. `docs/envoy-go/ROADMAP.md` — row 67 + the §Schema invariant + the THREE live deferred sentences.
4. `docs/envoy-go/DECISIONS.md` — ADR-0286 (incl. the D-SDSVC-FETCHTIMEOUT drift site) + ADR-0287 (E3's rationale — the reject this row retires) + the ADR-0044/0045/0080/0106 process anchors. Tail ADR-0288; next-free ADR-0289 — re-derive, never trust this file.
5. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the TLS/SDS surface incl. :900 (the drift site), :902/:928/:936 (the paragraphs this row retires/sharpens).
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_*` execution discipline + `reference_*` traps; trust MEMORY.md's index.
