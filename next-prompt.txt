=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, the active phase is fully landed, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action. (The sentinel does NOT apply right now — phase 48 is chartered, its BRAINSTORM + SPEC are LANDED, and the next stage is its PLAN.)

**STATUS AS OF 2026-06-30: phase 48 (`stats-sink-statsd`) SPEC is LANDED; the NEXT stage is its PLAN.**
- The phase-48 SPEC landed: `docs/envoy-go/phases/48-stats-sink-statsd/SPEC-48.md` (the 14-section shape mirroring `SPEC-47.1.md`; lifecycle-state 1 → 2; docs-only direct-on-master). The §11 D-SD-* live pins were EXECUTED IN-SESSION against `envoyproxy/envoy:contrib-v1.37.2`; a `spec-document-reviewer` pass APPROVED it (every `bootstrap.go`/`main.go`/`internal/statssink` citation EXACT against HEAD) after one precision fold. STATE.md active-phase header rolled to `phase 48 (stats-sink-statsd) SPEC done`. **Re-verify with `git log --oneline -6` + `git status`; do NOT assume.**
- **The LOAD-BEARING probe is RESOLVED — D-SD-DELTA → DELTA.** The reference statsd counter `|c` is a per-flush delta-since-last-flush (decode-ran proof: `cluster.backend.upstream_rq_total` = `7,0,0,0` across four flushes, SUM==K==7, last-seen 0); gauge `|g` is absolute. ⇒ the `StatsdSink` ALWAYS owns a sink-private `deltaState` (reusing the landed `internal/statssink/delta.go` `newDeltaState()`/`apply()` — NO config knob; intrinsic to statsd) and `0092` asserts the delta-SUM with a post-convergence stability barrier (`reference_delta_sink_differential_stability_barrier`), NOT `0089`'s absolute `value==K`. The other pins: D-SD-NAME (full dotted name, tags inlined, ZERO labels; join `prefix + "." + name`; default prefix `envoy`); D-SD-LINE (`<prefix>.<name>:<int>|c|g`, ONE line per UDP datagram, NOT newline-batched, integer value, no `@rate`/sign; `|ms` timers unreachable — no histograms, ADR-0060); D-SD-STATS (+0 self-stats, no sink cluster); D-SD-REJECT (reference PGV-rejects a missing `statsd_specifier` + a hostname address [REFERENCE-PARITY rejects]; permits `tcp_cluster_name` [envoy-go-STRICT rejects — UDP-only] + IGNORES `socket_address.protocol` [always UDP — accepted-and-ignored]).
- Counts at SPEC (re-verify, do NOT assume): stat surface **1200** (non-H2 **1196**) / fixtures **93** / fuzzers **50** / BackendKind **38** / DECISIONS **ADR-0264** (next-free **ADR-0265**).

=> The NEXT action is the **phase 48 (`stats-sink-statsd`) PLAN** (lifecycle-state 2 → 3). NOT the termination sentinel. NOT a new SPEC.

==============================================

## The NEXT action — the phase 48 (`stats-sink-statsd`) PLAN
Author `docs/envoy-go/phases/48-stats-sink-statsd/PLAN-48.md` (a TDD-task spine; `superpowers:writing-plans` — mirror a prior single-flat-row PLAN in git history, e.g. the 47.1 PLAN shape minus the multi-leg split). Docs-only direct-on-master (the family-row PLAN precedent). Lifecycle-state 2 → 3. The SPEC's §10 sketches ~8–11 tasks; the PLAN firms them into a TDD spine and RESOLVES the §12 D-SD-* PLAN questions. After it lands, a `plan-document-reviewer` pass (the 47.x precedent — verify every `bootstrap.go`/`main.go`/`internal/statssink` line citation EXACT against HEAD; the five high-risk areas covered).

**The §12 D-SD-* PLAN questions the PLAN must RESOLVE** (from SPEC-48 §12):
- **D-SD-SPLIT** — the FINAL ADR-0045 re-check with real LoC (a SINGLE FLAT ROW is anticipated; the escape-valve stays UNCONSUMED unless the PLAN surfaces unexpected size).
- **D-SD-CONFIG-HOME** — a parallel `result.StatsdSinkConfigs []StatsdSinkConfig` slice on `internal/bootstrap` (alongside `StatsSinkConfigs`) vs a unified sink-config interface list; the `main.go` two-loop build into the ONE `statsSinks []statssink.Sink` slice.
- **D-SD-LIFECYCLE-SHAPE** — the writer shape: a SYNCHRONOUS per-flush `Write` (RECOMMENDED — UDP `Write` never blocks on a peer; the `Flusher` calls `Submit` serially; no background mutator) vs a bounded-channel writer-goroutine for `MetricsServiceSink` symmetry. The drop-log rate-limit reuse (logged-not-counted).
- **D-SD-DELTA-REUSE** — reuse the landed `delta.go` `newDeltaState()`/`apply()` VERBATIM (same package, unexported) sink-private vs a statsd-local copy. The `apply` contract (counters→delta, non-counters→absolute) MATCHES the statsd `|c`/`|g` split exactly (confirmed at the SPEC).
- **D-SD-ADDR** — the `UDPAddress` carrier (a `host:port` string resolved at `NewStatsdSink` via `net.ResolveUDPAddr`, vs a pre-resolved `*net.UDPAddr` on the config struct); literal-IP-required (reference-parity) vs benign hostname resolution.
- **D-SD-RECEIVER-WIRING** — the `0092` UDP receiver reachability (shared bridge + STATIC IP, the §11 probe precedent); the `test/helpers/statsdrecv` poll/assert surface (`DeltaSum(name)`/`Gauge(name)`/`FlushCount()`/`Reset()`/`Addr()`).
- **D-SD-FUZZER** — `FuzzStatsdSinkConfigParse` lands at 48 (fuzzers 50 → 51; CONFIRMED at the SPEC §6); reconcile the documented-vs-actual `^func Fuzz` count (50 actual verified).
- **D-SD-STATS-FINAL** — confirm +0 via the existing `internal/statssink/registration_test.go` (no statsd self-counter).
- **D-SD-GAUGE-SUBSET** — whether `0092` adds a cross-side absolute-gauge subset assertion (e.g. `membership_healthy==1|g`) alongside the counter delta-SUM.

**Anticipated PLAN shape (~8–11 tasks; the PLAN firms):** PROGRESS scaffold + the ADR-0045 re-check (D-SD-SPLIT) → the `StatsdSinkConfig` struct + `StatsdSinkConfigs` field + `statsdSinkTypeURL` constant + the two-arm dispatch + `parseStatsdSinkConfig` + the strict-reject arms (`tcp_cluster_name` / sibling-TypeURL-naming-both / missing-specifier) [TDD] + `FuzzStatsdSinkConfigParse` → the `internal/statssink/statsd.go` `StatsdSink` (the `*net.UDPConn` dial + the `deltaState`-then-line mapping + `|c`-delta/`|g`-absolute/prefix-join formatting + synchronous `Submit` + idempotent `Close`) [TDD table-driven over a fake UDP listener; full-package `-race`] → the `main.go` statsd-sink build loop + the flusher-gate generalization (`len(StatsSinkConfigs)>0 || len(StatsdSinkConfigs)>0`) + the byte-stability guard → the driver-owned `test/helpers/statsdrecv` UDP receiver → the `0092-stats-sink-statsd` cross-side fixture (poll-to-converge, K requests, the delta-SUM + stability barrier) + the deliberate breaks ((a) absolute-not-delta MUST fail the barrier, (b) wrong prefix/type, (c) barrier-masks-(a)) + 20/20 flake + the FULL-package `-race` → the registration test (1200 → 1200, +0) → the full 94-dir differential + six-gate → ADR-0265 §Decision/§Consequences body + BEHAVIOR_CONTRACT + STATE/ROADMAP + the fuzzer-count reconcile. Anticipated counts at the IMPL: stat **1200** (+0) / fixtures **94** (`0092`) / fuzzers **51** (`FuzzStatsdSinkConfigParse`) / BackendKind **38** / DECISIONS **ADR-0265** (next-free ADR-0266); ZERO new packages, ZERO new go.mod modules. Row 48 STAYS `in-progress` (it flips `done` at the phase-48 IMPL six-gate — NO parent rollup per ADR-0106).

After the PLAN lands (+ plan-document-reviewer APPROVED), ROLL THIS FILE FORWARD to point at the phase-48 IMPL (mirror a prior phase's post-PLAN IMPL router in git history — e.g. the 47.x IMPL routers; subagent-driven in a FRESH WORKTREE).

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** The next stage is the phase-48 PLAN (author `PLAN-48.md`; docs-only direct-on-master). Lifecycle-state 2 → 3. After it, a `plan-document-reviewer` pass.
- **Subagent-driven execution** (`feedback_execution_style`) in a **fresh worktree** (`feedback_git_worktrees`) for the IMPL; subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit, re-runs the full suite on the frozen HEAD, does the deliberate-break verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). (BRAINSTORM/SPEC/PLAN are docs-only direct-on-master per the family-row precedent — no worktree needed.) Per-task gates: `gofmt -l` + `golangci-lint` on touched pkgs + `go vet` + `go build ./...` (`feedback_pertask_gofmt_lint`).
- **Process anchors:** ADR-0106 (per-leg rows) · ADR-0044 (ADR §Context-at-SPEC / §Decision+§Consequences-at-IMPL) · ADR-0045 (sub-split soft gate — escape-valve unconsumed) · ADR-0080 (strict-reject) · ADR-0064 (`stats_sinks[]` silent-ignore, now a SECOND consumer) · ADR-0060 (no histograms → no timers/`|ms`) · next-free **ADR-0265**.
- **Differential discipline** (carries to `0092`): `-run 'TestDifferential/0092'` NEVER bare (`reference_differential_run_selector`); `-count=1` on every break + `-race` run (`reference_differential_break_protocol_count1`); the full-suite `subject ready: EOF` startup flake is isolatable (`reference_differential_fullsuite_startup_flake`); the Docker bridge + decode-ran proof (`reference_docker_probe_bridge_network`); the driver-owned UDP receiver is NOT a BackendKind (`reference_differential_grpc_receiver_driver_owned`); the live reference is the wire truth (`reference_wire_format_both_sides_see_same_bytes`); periodic-sink two-receivers + hard-Close (`reference_periodic_sink_differential_two_receivers`); the delta-sink stability barrier (D-SD-DELTA CONFIRMED deltas — `reference_delta_sink_differential_stability_barrier`); the metrics_service probe boundary (`reference_stats_sink_metrics_service_probe`).

## Read first (in order)
1. THIS FILE — the cold-start router (phase 48 `stats-sink-statsd` SPEC LANDED; the next action is its PLAN).
2. `docs/envoy-go/phases/48-stats-sink-statsd/SPEC-48.md` — the frozen scope + the §11 D-SD-* live-probe pins (D-SD-DELTA → DELTA load-bearing) + the §12 D-SD-* PLAN questions the PLAN must RESOLVE + the §10 per-task sketch + the ADR-0265 §Context draft.
3. `docs/envoy-go/phases/48-stats-sink-statsd/BRAINSTORM.md` — the chartered scope (UDP-only / single flat row).
4. `docs/envoy-go/STATE.md` — the active-phase header (`phase 48 (stats-sink-statsd) SPEC done`) + the recorded NEXT (`phase 48 PLAN`) + the counts (1200 / 93 / 50 / 38 / ADR-0264).
5. `docs/envoy-go/ROADMAP.md` — row 48 `in-progress` (the table row + the Observability family note); the family STAYS OPEN.
6. `docs/envoy-go/phases/47-stats-sink-metrics-service/PLAN-47.1.md` (+ the 47.2a/47.2b PLANs) — the TDD-spine PLAN shape to mirror; the `deltaState` pattern landed at 47.2a (`internal/statssink/delta.go`).
7. `internal/statssink/{flusher,sink,mapping,delta}.go` + `internal/bootstrap/bootstrap.go` (`parseStatsSinks` `:415` + the `:430` sibling-reject + `StatsSinkConfig` `:274` + `metricsServiceTypeURL` `:214`) + `cmd/envoy-go/main.go` (`:189-201` the sink-slice + flusher build; `:373-377` `Start`) — the EXACT seams the PLAN cites.
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `reference_stats_sink_metrics_service_probe`, `reference_periodic_sink_differential_two_receivers`, `reference_delta_sink_differential_stability_barrier`, `reference_differential_grpc_receiver_driver_owned`, `reference_strict_reject_sibling_typeurl_gap`, `reference_docker_probe_bridge_network`, plus the differential-discipline references above).
