RESUME a fresh session for envoy-go (router last updated 2026-06-28; the **phase 46.2 (tracing Zipkin+B3) IMPL is DONE and LANDED** — squash `2e176d0b`, fast-forwarded onto master + pushed to origin; the worktree removed). The 46.2 leg was the **SECOND-and-FINAL chartered leg** of the by-exporter tracing split (46.1 core+OTLP [DONE, ADR-0260 CLOSED] / 46.2 Zipkin+B3 [DONE, **ADR-0261 ANCHORED**]). **ROW 46 (`tracing`) FLIPPED `in-progress` → `done`** (46.1 + 46.2 COMPLETE, per ADR-0106 + `reference_roadmap_split_phase_row_done`). **The Observability FAMILY STAYS OPEN** — stats sinks + the tap filter remain candidate future rows; there was NO family-close at 46.2 (phase-46 BRAINSTORM §1.3).

HEAD is the **46.2 IMPL squash `2e176d0b`** atop the 46.2 PLAN `477ed266`. **Re-read the shas with `git log --oneline -6`, do NOT assume.** Confirm `git status` clean (the untracked `.claude/` is session cruft, leave it) + master == origin/master.

## What the 46.2 IMPL landed (the 13-task TDD spine, built subagent-driven in a worktree; ALL within `internal/tracing` — ZERO new Go packages, ZERO new go.mod modules)
- `b3.go` — `ExtractB3` (single `b3` / multi `X-B3-*`) + `InjectB3` (multi-header `X-B3-*`) + `FuzzExtractB3`. **The incoming SPAN-id is our server span's parent** (the 4th `b3` field / `X-B3-ParentSpanId` is the caller's grandparent — accepted-but-ignored), per SPEC §11 D-TRACE-ZIPKIN-B3 (a correctness fix vs the plan's Task-2 sketch, settled against the live reference).
- `decision.go` — `DecideWithContext` lifts trace-context extraction to the HCM caller; `Decide` stays a thin `traceparent` wrapper (byte-stable — OTel/`0086`/`0087` unchanged).
- `zipkin.go` — the v2-JSON span encoder (`name`=request authority via the provider-neutral `Span.Authority`; OTLP keeps `"ingress"`; 14 STRING tags = OTLP-16 MINUS `node_id`/`zone`; MICROsecond timing; `shared`/`parentId` per `shared_span_context`) + `zipkinIdentity` + the `ZipkinExporter` (bounded channel + writer goroutine + size/interval/close buffer + v2-JSON POST over the `ZipkinTransport` seam [`httpclient.ClusterDispatch`] + retry-once + drop-newest + idempotent Close).
- `config.go` — `NewConfig` provider dispatch (OTel|Zipkin) + the `ZipkinConfig` parse arm + strict-rejects (`collector_endpoint_version`≠`HTTP_JSON` / `split_spans_for_request` / empty `collector_cluster`).
- `stats.go` — `RegisterZipkinCounters` (2 LAZY `tracing.zipkin.{spans_sent,spans_dropped}`).
- `exporter.go` — `ExporterFor(*TracingConfig)` provider dispatch + memoize + lazy register + the `HasCluster` `log.Fatalf` boot-reject (REFERENCE-PARITY — UNLIKE the permissive OTLP boot).
- HCM (`connection.go`/`h2dispatch.go`/`accesslog_emit.go`) — provider-aware extract/inject (B3 vs traceparent by `ProviderKind`) + the `Span.Authority` carry; no-tracing byte-stable.
- `cmd/envoy-go/main.go` — hoist `httpClient` + thread the `zipkinTransportAdapter`.
- `test/helpers/zipkincollector` — the driver-owned HTTP/JSON receiver.
- `0088-tracing-zipkin` — cross-side EXACT differential (poll-to-converge, stable v2-JSON subset + B3 continuation prong).

## Verification (controller-run on the frozen HEAD before squash/push)
Six-gate GREEN: `gofmt -l .` / `go vet ./...` / `go build ./...` / `golangci-lint run ./...` all clean; the full unit suite green; the **full 90-dir differential `ok 270.885s`** (clean — the earlier full-`./...` `subject ready: EOF` on the UNRELATED `0013-http-local-ratelimit` was the documented isolatable startup flake, isolate-re-run passed); full-package `-race` (`internal/tracing` + `internal/filter/hcm`) green. **0088 deliberate breaks — all 4 LIVE** (name→ingress / drop-continuation / kind→CLIENT / skip-export each FAIL, restored via `git restore`); **0088 flake-soak 20/20**. `go mod tidy -diff` EMPTY.

## Counts at 46.2 IMPL-done (re-verify, do NOT assume)
stat surface **1200** (non-H2 **1196**; +2 LAZY `tracing.zipkin.*`) / fixtures **90** (tail `0088-tracing-zipkin`) / fuzzers **49** (`FuzzExtractB3`) / BackendKind **38** (driver-owned receiver, unchanged) / DECISIONS **ADR-0261** (next-free **ADR-0262**).

## The NEXT action — a FRESH planning decision (pick the next phase/family + open its BRAINSTORM)
**ALL chartered numbered ROADMAP rows through row 46 are `done`.** There is no in-flight phase. The next action is to **choose the next phase and open it** (lifecycle-state 0 → 1, a BRAINSTORM — `superpowers:brainstorming` — docs-only direct-on-master per the family-row precedent). Cold-start MUST read `docs/envoy-go/STATE.md` (its recorded NEXT = "pick the next open ROADMAP row/family") + `docs/envoy-go/ROADMAP.md` (the family structure) FIRST, then decide WITH the user.
- **Candidate directions:** continue the still-OPEN **Observability family** (its remaining candidate rows are the **stats sinks** + the **tap filter**), OR open a NEW family. This is a Q0-style "which row next" decision for the BRAINSTORM — confirm the pick with the user before chartering.

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** The next stage is a BRAINSTORM (`superpowers:brainstorming`) for the chosen next phase. Lifecycle-state 0 → 1.
- **Subagent-driven execution** (`feedback_execution_style`) in a **fresh worktree** (`feedback_git_worktrees`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit, re-runs the full suite on the frozen HEAD, does the deliberate-break verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). Per-task gates: `gofmt -l` + `golangci-lint` on touched pkgs + `go vet` + `go build ./...` (`feedback_pertask_gofmt_lint`).
- **Process anchors:** ADR-0106 (per-leg rows) · ADR-0044 (ADR §Context-at-SPEC / §Decision+§Consequences-at-IMPL) · ADR-0045 (sub-split soft gate) · ADR-0080 (strict-reject) · next-free **ADR-0262**.
- **Differential discipline** (carries to any new fixture): `-run 'TestDifferential/<NNNN>'` NEVER bare; `-count=1` on every break + `-race` run; the full-suite `subject ready: EOF` startup flake is isolatable (`reference_differential_fullsuite_startup_flake`); the Docker bridge + decode-ran proof (`reference_docker_probe_bridge_network`); driver-owned receivers are NOT BackendKinds (`reference_differential_grpc_receiver_driver_owned`); the live reference is the wire truth (`reference_wire_format_both_sides_see_same_bytes`).

## Read first (in order)
1. THIS FILE — the cold-start router (the 46.2 IMPL is DONE + LANDED; row 46 `done`; the next action is a FRESH BRAINSTORM for the next phase/family).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 46.2 IMPL done`) + the recorded NEXT (pick the next open ROADMAP row/family) + the counts (1200 / 90 / 49 / 38 / ADR-0261).
3. `docs/envoy-go/ROADMAP.md` — row 46 (`tracing`, `done`) + the family structure (the Observability family STAYS OPEN; pick the next row/family).
4. `docs/envoy-go/DECISIONS.md` — **ADR-0261** (the 46.2 Zipkin+B3 anchor, §Decision/§Consequences landed) + next-free **ADR-0262**.
5. `docs/envoy-go/phases/46-tracing/` (PROGRESS-46.2.md + SPEC-46.2.md + PLAN-46.2.md) — the just-completed leg's record (the as-built design + the verification block), if the next phase touches tracing.
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_subagent_autocommit_claudemd`, `feedback_pertask_gofmt_lint`, `reference_roadmap_split_phase_row_done`, plus the differential-discipline references above).
