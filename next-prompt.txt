START a fresh cold-start session for envoy-go — **the phase 28.1b IMPL is DONE + squash-merged + pushed** (the controller did the squash; the squash SHA is recorded in `docs/envoy-go/STATE.md` last-commit). The active phase is **phase 28** (parent `in-progress`; sub-rows `28.1a` `done`, `28.1b` `done`, `28.2` `planned`). This session authors the **28.2 SPEC** — per SKILL_ROUTING **state 1** (the 28.2 sub-phase directory does not yet exist → `superpowers:brainstorming` scoped to the 28.2 SPEC, per the per-sub-phase precedent).

Phase 28 (`network-filter-zookeeper-proxy`) lands `zookeeper_proxy` at both-direction COUNTER parity. 28.1 was invoked-split (ADR-0045 §AMEND) into 28.1a (WriteFilter seam + the complete REQUEST package + the DISABLED `0046` driver; squash `8703aeb`) + 28.1b (the READ seam + `0046` cross-side proof + `0047` + the completion bundle; just squash-merged). **The phase-28.1 family is now complete except the 28.2 response side.** This session designs that response side.

## What this session must do

Author the **28.2 SPEC** via `superpowers:brainstorming` (self-driven per the §9 precedent; the subsequent PLAN executes subagent-driven). Create the 28.2 sub-phase directory `docs/envoy-go/phases/28.2-network-filter-zookeeper-responses-and-latency/` (README + SPEC). The 28.2 envelope (from the parent BRAINSTORM + ROADMAP row 28.2):

- (a) the **RESPONSE-side decoder in `OnWrite`** — response framing (xid + zxid + err), xid correlation against the 28.1-laid per-connection tracking map, per-opcode `*_resp` counters, watch-event handling (xid −1) + ping/auth special xids (−2/−4). The request package laid the correlation structures at 28.1 (written, NOT yet consumed — the R5 ratification); 28.2 consumes them.
- (b) **latency measurement** (request-timestamp → response) + the `enable_latency_threshold_metrics` fast/slow COUNTER surface (`*_resp_fast`/`*_resp_slow`; `default_latency_threshold` default 100 ms; `latency_threshold_overrides` per the proto's opcode enum) — counters ONLY; the `*_response_latency` HISTOGRAM family stays DEFERRED per ADR-0060 with a coverage-boundary record.
- (c) the per-opcode response-bytes opt-in counters (scope per SPEC).
- (d) fixture **`0048-zookeeper-responses`** (cross-side StatsAsserter `*_resp`/`watch_event`/threshold-counter parity; DETERMINISTIC threshold arms via extreme thresholds — 1 h → all fast, minimal → all slow — avoiding cross-side timing nondeterminism).
- (e) optionally the response-decoder fuzzer (38th; SPEC D28-10).
- (f) the BEHAVIOR_CONTRACT 28.2 bundle + **the parent-row-28 ROLLUP** (parent flips `in-progress → done` ATOMICALLY with this sub-row per the 18/19/22/24/25/26 precedent) + the six-gate.
- ADR-0223 (response side + latency-threshold counters) gets its §Decision/§Consequences body at the 28.2 IMPL.

## Load-bearing pins the 28.2 SPEC MUST carry

1. **The §3.6 synchronization obligation (load-bearing).** The 28.1b OnWrite was a pure no-op so the 28.1b race surface was empty — but 28.2's OnWrite response decoder will run on goroutine B (the upstream→downstream write pump) CONCURRENTLY with goroutine A's replay-path request decoder, and BOTH touch the per-connection correlation structures. **The 28.2 SPEC MUST add a per-connection `sync.Mutex` (or equivalent) guarding the correlation maps, and ADR-0223 MUST record it.** This is the explicit forward-pointer pinned in ADR-0221 §Consequences + the BEHAVIOR_CONTRACT conn-wrap-seam block at 28.1b — do not lose it.
2. **The deterministic-threshold differential discipline.** Latency counters need DETERMINISTIC thresholds to be cross-side assertable (extreme thresholds: 1 h → all fast / minimal → all slow). Cross-side timing equality is otherwise nondeterministic.
3. **The parent-row-28 rollup happens at 28.2 phase-done** (parent `in-progress → done` ATOMIC with sub-row 28.2 — the final-sub-phase precedent; 28.1b deliberately did NOT roll the parent).
4. **The correlation-consumption R5 ratification.** The two correlation structures were written at 28.1 but never consumed (laid down for the response side); 28.2 consumes them — ratify R5 at the 28.2 SPEC.
5. **Counts at 28.2 open:** active fixtures **49** (tail `0047`); fuzzers **37**; stat surface **337**; DECISIONS tail **ADR-0223** / next-free **ADR-0224**. After 28.2: fixtures → 50 (`0048`); fuzzers → 37–38; the 201-counter roster is already created eagerly (response-side counters exist-at-zero now — 28.2 wires their increments, no creation delta).

## Project state at master tip

- **Master tip:** the 28.1b-IMPL squash (see STATE.md `last-commit` for the SHA — the controller fills it at stage-close; a docs-only repoint trails the squash by +1 — confirm the live tip via `git log --oneline -1`). Substantive predecessors: the 28.1b-PLAN squash `eff30e8`, the 28.1a-IMPL squash `8703aeb`.
- **§9 Network-filters family:** rows 26 (parent + 26.1/26.2/26.3) + 27 DONE; row 28 `in-progress` (parent; 28.1a `done`, 28.1b `done`, 28.2 `planned`). After phase 28 phase-done, 4 family candidates remain (`redis`/`mongo`/`kafka_broker`/`thrift`).
- **Counts at master tip:** stat surface **337**; active differential fixtures **49** (tail `0047`); fuzzers **37**; conformance h2spec 53/53 + proxy-wasm 10/10; DECISIONS.md tail **ADR-0223** (next-free **ADR-0224**). ADR-0209 escape-valve reserve STANDS-UNCONSUMED.
- **Toolchain:** Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); reference Envoy v1.37.2 (ADR-0008); go-control-plane v1.32.4 (ADR-0008).
- **Known coverage boundaries (carried, non-blocking):** D-26.3-4; the `tcp_proxy` `downstream_cx_*` family unmirrored; histograms deferred (ADR-0060 — incl. the `*_response_latency` family 28.2 keeps deferred); zookeeper dynamic-metadata deferred (AMEND-A9) + shallow-decode leniency + terminal-originated-writes-only + the write-only-filter boot boundary (28.1 SPEC §7.5) + the post-handoff observational boundaries (28.1b SPEC §3.5; documented at 28.1b Task 9).

## Read first (in order)

1. THIS FILE — the 28.2-SPEC cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 28.1b IMPL done`; next-skill `superpowers:brainstorming` (the 28.2 SPEC); the squash SHA in last-commit.
3. `docs/envoy-go/phases/28-network-filter-zookeeper-proxy/SPEC.md` §3.1–§3.2 (the split surface-mapping table + the 28.2 scope boundary) + §11 (the D28-1..D28-10 empirical pins — correlation-structure shape, response framing, latency semantics) + `BRAINSTORM.md` (Q1 both-direction-counters; the 28.1/28.2 direction-progressive pre-split).
4. `docs/envoy-go/phases/28.1-network-filter-write-seam-and-zookeeper-requests/SPEC.md` + `PROGRESS.md` — the as-built request surface + the correlation structures laid for the response side.
5. `docs/envoy-go/phases/28.1b-network-filter-read-seam-and-zookeeper-requests-proof/SPEC.md` + `PROGRESS.md` — the read seam + §3.6 (THE synchronization obligation pin 28.2 must carry) + §3.5 observational boundaries.
6. `docs/envoy-go/DECISIONS.md` ADR-0221 (both seam halves + the §Consequences 28.2 forward-pointer) + ADR-0222 (the request-package body) + ADR-0223 §Context (the response-side draft — its §Decision/§Consequences body lands at the 28.2 IMPL and MUST add the per-connection mutex).
7. The as-built code 28.2 extends: `internal/filter/network/zookeeperproxy/` (`decoder.go` — request decoder + the correlation maps; `zookeeperproxy.go:OnWrite` — the current no-op to be replaced; `stats.go` — the 201-counter roster incl. the exist-at-zero `_resp*` families; `config.go` — the latency fields parsed-not-consumed); `internal/filter/network/chain.go` + `readconn.go` + `writeconn.go` (the conn-wrap seam both halves).
8. `docs/envoy-go/SKILL_ROUTING.md` (state 1 = no sub-phase dir → brainstorming) + the BOOTSTRAP §4/§5 cold-start invariants.
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — execution-style + worktree + push + subagents-no-push + per-task gofmt/lint + the network-filter constraints (incl. the network read-filter OnNewConnection-halts + the chain-terminal-handoff-ends-OnData notes) + the differential dispatch/asserter constraints all carry forward.
