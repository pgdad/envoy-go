START a fresh cold-start session for envoy-go — **the phase-41 circuit-breakers BRAINSTORM is COMPLETE and LANDED on master** (the THIRD Upstream-robustness-family row; the charter `docs/envoy-go/phases/41-circuit-breakers/BRAINSTORM.md` — 12-section phase-39/40 precedent, reviewer-APPROVED — + the STATE/ROADMAP/next-prompt roll, all docs-only; counts UNCHANGED). The subject is PICKED (circuit breakers = phase 41), the FOUR load-bearing design forks are SETTLED, and the design is written to disk. **The next lifecycle step is the phase-41 SPEC** — author `docs/envoy-go/phases/41-circuit-breakers/SPEC.md` directly (per the phase-39/40 precedent — NOT a re-brainstorm; the forks are settled): execute the §10 D-CB-PROTO/LIFECYCLE/STATS/REJECT/BACKEND/DIFFERENTIAL empirical pins IN-SESSION live against the contrib reference Envoy (`envoyproxy/envoy:contrib-v1.37.2`, ADR-0227) per `reference_docker_probe_bridge_network`, anchor the ADR-0248 §Context draft in SPEC §13, and re-run the FINAL ADR-0045 split-gate check (the BRAINSTORM pre-authorized NO split). Confirm the tip via `git log --oneline -5` (HEAD is the phase-41 BRAINSTORM docs commit — the `41-circuit-breakers/BRAINSTORM.md` charter + the STATE/ROADMAP/next-prompt roll — sitting on the substantive phase-40.3 IMPL squash `148f288`; NO substantive code has landed since `148f288` — the BRAINSTORM is docs-only). Counts UNCHANGED at this BRAINSTORM: stat surface **1149** / fixtures **75** / fuzzers **42** / BackendKind tail **35** (`HTTP503Responder`) / DECISIONS tail **ADR-0247**, next-free **ADR-0248**.

## What just landed (the phase-41 circuit-breakers BRAINSTORM — the durable record is `docs/envoy-go/phases/41-circuit-breakers/BRAINSTORM.md`; read that, not this summary)

The brainstorm charters **circuit breakers** = phase 41 — `Cluster.circuit_breakers` (`cluster.v3.CircuitBreakers`, `Cluster` field 10, VERIFIED in the EXISTING go-control-plane dep `envoy v1.32.4`, `config/cluster/v3/circuit_breaker.pb.go` — ZERO new go.mod module; NB the *reference Envoy binary* for live probes stays `contrib-v1.37.2` per ADR-0227 — two distinct version axes; the prior in-flight next-prompt's "v1.37.0" conflated them): per-`RoutingPriority` OVERFLOW budgets that, once exhausted, FAIL FAST with a 503 upstream-overflow (UO) + an `upstream_*_overflow` counter, never queued. The project's FIRST feature that REJECTS load + its FIRST cluster-level concurrency accounting. REUSES the phase-39/40 cluster-runtime lifecycle seams (the connection-acquire chokepoints `Cluster.Dial`[`cluster.go:284`]/`AcquireH1`[`:338`]/`DialH2`[`dial_h2.go:42`] + the per-request `RecordUpstreamResult` completion seam [`cluster.go:186`, ADR-0245]) — ADDS NO new router→cluster channel, only counter try-acquire/release hooks at points the code already passes through (the router admission point `router.go:610`/`router_h2.go:76` + the `connWithGauge` conn-close path; parse in `buildCluster` `manager.go:363` after `outlier_detection` `:416`; the `circuitBreaker` field on the `Cluster` struct `cluster.go:85`).

**The FOUR settled forks (the BRAINSTORM's load-bearing decisions):**
- **Fork 1 — SCOPE (the core overflow trio; SINGLE phase, NO ADR-0045 split** [~250–400 prod LoC / ~12–16 tasks]**):** ENFORCE `max_connections` + `max_pending_requests` + `max_requests` (the three budgets with live request/connection substrate). PARSE-and-register-emit-0-but-DEFER `max_retries`/`retry_budget` (no retry substrate — retries+hedging is a separate future family candidate), `max_connection_pools` (HTTP/2 multiplexed-pool semantics), `per_host_thresholds` — the AMEND-OD3-4 emit-0-for-parity shape.
- **Fork 2 — ENFORCEMENT ARCHITECTURE (→ ADR-0248):** a `circuitBreaker` struct on `Cluster` (alongside `health`/`outlier`) with per-priority atomic counters (`openConns`/`activeRequests`/`pendingRequests`) + the parsed budgets; NON-BLOCKING try-acquire/release (fail-fast, never queue) — `max_requests` try-acquire at router admission → release at `RecordUpstreamResult`; `max_connections` try-acquire at `Dial` → release at upstream-conn close; `max_pending_requests` binds in the admitted-but-no-connection window (saturated `max_connections`). On exhaustion → immediate 503-UO + the matching `upstream_*_overflow` counter; the `circuit_breakers.<priority>.*_open` GAUGE reads 1 at/over limit. NO background goroutine (synchronous atomic try-acquire — CONTRAST the phase-40.3 per-interval sweep goroutine).
- **Fork 3 — PER-PRIORITY + STAT SURFACE:** nothing routes by `RoutingPriority` yet → ENFORCE DEFAULT-only, but REGISTER BOTH `circuit_breakers.default.*` + `circuit_breakers.high.*` trees for surface parity (HIGH never binds, emits 0). Sketch: **+10** gauges (5 names × 2 priorities: `cx_open`/`cx_pool_open`/`rq_open`/`rq_pending_open`/`rq_retry_open`) + **~3–4** `upstream_*_overflow` cluster-level counters → **~1149 → ~1162**. `track_remaining`/`remaining_*` DEFERRED. The EXACT overflow-counter→budget mapping is a SPEC-time EMPIRICAL PIN vs `contrib-v1.37.2` over the docker bridge — NOT hard-committed in the BRAINSTORM.
- **Fork 4 — DIFFERENTIAL (deterministic concurrency):** a blocking backend (`BackendKind #36`, `BlockingHoldResponder`) + release barrier on a shared bridge-network backend both sides hit; fixture(s) `0074` (+ possibly `0075`): fill the budget with N held-open concurrent requests, fire the N+1th → assert cross-side **503 UO + `circuit_breakers.default.rq_open == 1`** + the overflow-counter delta, then release. Deterministic / flake-free over the 20-run gate (NO sleep/timing window — the `reference_differential_band_sigma_margin` lesson). Sketch: **+1–2 fixtures** (75 → 76/77), BackendKind 35→36, possibly **+1 fuzzer** (CB-config parse, 42→43). Exact fixture split (one multi-assert vs per-budget) pinned at SPEC.

**Anticipated ADRs:** ADR-0248 (enforcement architecture + per-priority surface), possibly ADR-0249 (the defer-enforcement posture for retry/pool/per-host budgets). **New parse-reject arms** (D-CB-REJECT): duplicate-priority `Thresholds` / budget-value edge cases / `per_host_thresholds` presence — byte-stable per ADR-0080, exact arms pinned at SPEC. **DEFERRED items (→ BRAINSTORM §8):** `max_retries`+`retry_budget` enforcement (blocked on the retries+hedging family), `max_connection_pools` (HTTP/2 multiplexed pools), `per_host_thresholds`, `track_remaining`/`remaining_*`, HIGH-priority binding (blocked on priority routing), a blocking/queuing pending model (41 is fail-fast only).

## The SPEC obligations (§10 D-CB-* — execute IN-SESSION at the 41 SPEC)

- **D-CB-PROTO** — the exact `CircuitBreakers`/`Thresholds`/`RetryBudget` field roster + types + PGV + defaults (the well-known `1024/1024/1024/3/∞`, `track_remaining=false`, retry-budget `20%/3`; `priority` DEFAULT=0/HIGH=1); `go mod tidy -diff` EMPTY.
- **D-CB-LIFECYCLE** — the try-acquire/release semantics: when `max_requests` counts (reject-then-no-count vs count-then-reject), the `max_pending_requests` window against envoy-go's synchronous-acquire model, whether `max_connections` counts pooled-idle H1 conns, the 503-UO emission point + whether the conn/request is fully un-made.
- **D-CB-STATS** — the exact `circuit_breakers.<priority>.*_open` gauge names + the `upstream_*_overflow` counter names + the overflow-counter→budget mapping from a live `/stats` scrape; whether both priority trees always emit; the `remaining_*` set under `track_remaining`.
- **D-CB-REJECT** — the envoy-go-strict reject arms (byte-stable per ADR-0080); whether a config-parse fuzzer is warranted (42 → 42 or 43).
- **D-CB-BACKEND** — the `BlockingHoldResponder` release mechanism + how the driver confirms N-in-flight deterministically.
- **D-CB-DIFFERENTIAL** — the fixture split (one multi-budget vs per-budget `0074`/`0075`) + the cross-side UO-flag + overflow-counter + gauge assertions, sleepless via the release barrier.

## Honor throughout (the SPEC discipline)

- **Lifecycle = one stage per session:** the SPEC next (author `SPEC.md` directly; execute D-CB-* live; anchor the ADR-0248 §Context; re-check the ADR-0045 split-gate — pre-authorized NO split). `feedback_git_worktrees` (any isolated work in a worktree) · `feedback_subagents_no_push` (task subagents commit locally only; the controller squashes + pushes at stage-close) · `feedback_push_to_origin` (after a clean local merge on master with tests green, push without asking).
- **ADR/process:** ADR-0044 (the §Context drafts at the SPEC, the §Decision/§Consequences bodies land IN-PLACE at the IMPL) · ADR-0045 (the split-gate) · ADR-0052 (the six-gate at the IMPL) · ADR-0080 (envoy-go's own byte-stable rejects) · ADR-0106 + `reference_roadmap_split_phase_row_done` (row 41 is a flat un-split family row — it flips `done` at its single IMPL six-gate; NO parent rollup; the family stays open) · ADR-0227 (`contrib-v1.37.2`).
- **At the SPEC:** `reference_docker_probe_bridge_network` (any live reference-Envoy probe runs over a shared Docker bridge network + a STRICT_DNS backend hostname; verify the request path ran — `upstream_rq_total > 0`) · `reference_proto_roster_extraction_digits` (digit-inclusive regexes when scraping the proto/stat roster).

## Read first (in order)

1. THIS FILE — the post-phase-41-BRAINSTORM cold-start (the BRAINSTORM is DONE + LANDED; the next step is the phase-41 SPEC — execute D-CB-* against `contrib-v1.37.2`, anchor ADR-0248 §Context).
2. `docs/envoy-go/phases/41-circuit-breakers/BRAINSTORM.md` — the durable phase-41 charter (the four settled forks, the architecture over the existing seams, the §5 stat surface, the §6 differential, the §7 ADRs, the §10 D-CB-* SPEC pins, the §8 deferred items). Read this, not the summary above.
3. `docs/envoy-go/STATE.md` — active-phase `phase 41 (circuit-breakers) BRAINSTORM done`; the count footer (1149 / 75 / 42 / 35 / ADR-0247, next-free ADR-0248); the lifecycle routing (next-skill = the 41 SPEC).
4. `docs/envoy-go/ROADMAP.md` — row 41 `in-progress` (the BRAINSTORM-DONE charter) + the §Upstream-robustness family heading (the family-progress note: active-HC DONE / outlier DONE / circuit breakers IN-PROGRESS; 2 candidates remain after 41).
5. `docs/envoy-go/DECISIONS.md` (the ADR-0245/0246/0247 outlier triad + the process ADRs; next-free ADR-0248) + `docs/envoy-go/BEHAVIOR_CONTRACT.md` (the `### Cluster` health subsections + the stat-surface block at 1149 — the 41 SPEC anticipates the `circuit_breakers.*` block).
6. The phase-40 family record `docs/envoy-go/phases/40.{1,2,3}-*` + `40-outlier-detection/` (the precedent shape for the 41 SPEC — the seam reuse, the emit-0-for-parity stat posture, the cross-side traffic/concurrency differential) + the phase-39 `39-upstream-health-check/` (the cluster-runtime lifecycle precedent).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`.
