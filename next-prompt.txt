=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. **There IS banked work at this tip** — phase 76 is BRAINSTORMED, SPEC'd **and PLANNED**, and row 76 is `in-progress` — so this session **ADVANCES IT** and performs the **phase-76 IMPL**. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-RUN MECHANICALLY 2026-07-26 at the phase-76 PLAN. It does NOT fire. `stop` was NOT created and MUST NOT be.** ⚠️ **Re-run it yourself anyway and record what YOU see** — a prior run's result is not evidence for the current tip. ACTUAL output, recorded rather than predicted:
- **(1) prints `NOT DONE: row 76`.** Live since the BRAINSTORM; it stays live until row 76 flips `done` at the phase-76 IMPL — **i.e. THIS session is the one that silences it.**
- **(2) prints THREE live "candidates:" sentences** — HTTP/3 (`:186`), xDS (`:196`), Observability (`:206`). **Cite the command, never the adjective** (`reference_sentinel_deferred_sentence_live_vs_historical`): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` => **3**. ⚠️ A naive `grep -c 'candidates:'` returns a larger number, and the `candidates were:` recaps inside the Observability sentence are HISTORICAL.
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** => THREE families never opened. (The `WASM` line remains a ROADMAP **bookkeeping artifact** — `internal/wasm/` is a substantially-built wazero host and rows 25/25.1/25.2/25.3 are all `done` — deliberately LEFT AS-IS.)

⚠️ **PHASE 76 NARROWS NOTHING, EVER — verified mechanically.** None of the three `candidates:` sentences names a load-balancing candidate (the only `ring`/`Load-balanc` substrings on those lines are `buffering`, `Load-bearing`, and a historical *"the Load-balancing family closed at phase 54"*). ⇒ **check (2) STAYS 3 at the IMPL.** Do not go looking for a narrow to land.

⚠️ **NEVER WRITE A SENTINEL'S OWN MATCHER STRING INTO A FILE THE SENTINEL GREPS.** This fired LIVE at the phase-76 BRAINSTORM, twice, in one commit: row 76's first draft quoted check (3)'s marker phrase for the gRPC family — *inside the sentence explaining why quoting it would be gaming the check* — and check (3) came back with **gRPC silently GONE**. **`grep` cannot tell a mention from a use.** ⚠️ **THE SAME SPECIES FIRED AGAIN AT THE PHASE-76 PLAN, in `STATE.md`:** the first draft of the close wrote the three ADR-0288 field-name tokens verbatim while reporting their grep counts, and **the act of recording the counts CHANGED them** (each +1). Caught and corrected; the counts are now stated as a property with no numerals. ⇒ **THE IMPL EDITS `ROADMAP.md` — the only phase-76 stage that does. Re-run every sentinel check AFTER your edits land, not only at session open.**

⚠️ **CHECK (1)'s BLIND SPOT — RE-DERIVE IT; NEVER COPY IT.** At this tip it is **108 data rows (`:31`-`:138`), 104 matched, FOUR misses** — `| 00 | bootstrap | — | done |` (the `[0-9.,  ]*` field does not match an **em-dash**), `| 04 | http-1.1 | 03 | done |` (the `[a-z0-9-]+` slug field does not match the **DOT**), and **`| 28.1a |` / `| 28.1b |`** (the `^\| [0-9.]+ \|` id field cannot match a **letter suffix**). All four are `done`, so there is no current impact. ⚠️ **This figure was recorded WRONG in two consecutive lineages** before being re-derived at the phase-75 IMPL (107/103/4), the phase-76 BRAINSTORM, the phase-76 SPEC and again, independently, at the phase-76 PLAN (108/104/4 each time). **Derive it yourself.**

==============================================


## STATUS AS OF 2026-07-26: PHASE 76 IS BRAINSTORMED, SPEC'D **AND PLANNED**. ROW 76 IS `in-progress`. THIS SESSION PERFORMS THE **PHASE-76 IMPL** — THE FINAL STAGE OF THE ROW.

**What the phase-76 PLAN landed** (`git log --grep 'phase 76'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): `docs/envoy-go/phases/76-lb-ring-hash-spread-margin/PLAN.md` — a **NINE-task TDD spine (T1-T9)** with a **SIXTEEN-break roster** — plus `PROGRESS.md`, plus the STATE roll. **ROADMAP and DECISIONS BYTE-UNTOUCHED; row 76 STAYS `in-progress`.** File set: the new `PLAN.md` + the new `PROGRESS.md` + `STATE.md` + `next-prompt.txt` — **ZERO production `.go`, ZERO test `.go` in the PLAN COMMIT.**

⚠️ **BUT THE ROW IS LARGELY BUILT ALREADY, IN PROBE WORKTREES.** The measurement test, the `go/parser` linkage gate, `sourceIPs = 16` and the rescaled `driver_test.go` are all **COMPILED, RUN and LOCALLY COMMITTED** at `phase-76-plan-v1` (`10f60552`) and `phase-76-plan-v2` (`5feb0609`). **Never pushed. NOT in the PLAN commit.** The IMPL should **re-derive rather than re-invent**, and must re-derive every anchor at its own tip (`reference_a_drift_correction_is_itself_a_claim`). **PLAN.md carries the verbatim, executed code for every one of them.**

### What matters for the NEXT session, in order

1. **⚠️ THE SENTINEL DID NOT FIRE AND `stop` WAS NOT CREATED. Re-run all three checks yourself and record the ACTUAL output; do not predict it.** ⚠️ **THIS IS THE STAGE THAT SILENCES CHECK (1)** — row 76 is the last non-`done` chartered row, so after the flip check (1) prints NOTHING. **Checks (2) and (3) will still print, so the sentinel STILL does not fire.** Create `stop` if and ONLY if ALL THREE print nothing.

2. **⚠️ THIS IS AN IMPL — execute the spine, do not re-litigate the PLAN.** PLAN §3 is T1-T9, each red-first → green → break-verified, each re-deriving its own anchors. **THIRTEEN of the sixteen breaks ALREADY RAN at the PLAN with their outcomes recorded verbatim** — re-run them at the IMPL tip and record what YOU see; a prior stage's transcript is not evidence for your tree.

3. **⚠️ THE FIVE BROKEN GATES. Phase 76 is a +0-PRODUCTION-FILE row, which is exactly when a broken gate goes unnoticed — and this lineage has now recorded FIVE, THREE of them found at the PLAN.**
   - **The phase-76 SPEC's OWN linkage gate fails BOTH ways.** **OPEN:** `[ "$a" = "$b" ]` with both captures empty is TRUE ⇒ **exit 0 on a tree where NEITHER constant exists** (rename/move/delete all read green). **CLOSED:** `grep -c 'collapseFixtureK'` ⇒ **2**, the second match being the measurement test's **own doc comment**, so a genuinely synced tree prints `DESYNC: sourceIPs=16 collapseFixtureK=16` and exits 1. **Use PLAN T4's `go/parser` Go test** (structurally immune; +0 exported symbols) and its hardened shell fallback (`^\s*` anchor + an empty-capture arm).
   - **`go doc -all <pkgA> <pkgB>`'s fail-open is specific to the `./` PREFIX** — this NARROWS the recorded memory. A **BARE** import path exits **1** with `doc: no symbol …`; a **`./`-prefixed** one exits **0** silently dropping the second argument, **even when it names a directory that does not exist**. Phase 75 pinned the `./` form. **Audit ONE package per invocation.**
   - **`gofmt -l` NEVER exits non-zero** — observed exit 0 while printing the unformatted file. Any gate chaining it with `&&` is **INERT**. Gate on OUTPUT: `[ "$(gofmt -l . | wc -l)" -eq 0 ]`.
   - **The import gate `impblock` has THREE defects, not one.** It fails CLOSED (prints `FILENAME`, so a `/tmp` baseline differs on every line — normalise to basenames) **and** it matches only `^import \($`, emitting ZERO lines for the two single-line `import "testing"` files ⇒ **blind to 2 of this row's 3 `.go` files.** PLAN T9 Step 4 carries a helper that handles both forms, with both arms observed.
   - ⚠️ **The phase-75 sha256 roster CANNOT be copied forward** — `internal/cluster/**` was byte-gated there and this row **EDITS `internal/cluster/ringhash_test.go`** (`comm -12` returns exactly that file). That would reproduce the phase-73 SEVERE-1. Phase 76's roster is **production-only, 375 files**, intersection with the EDIT roster **EMPTY**.

4. **⚠️ THE ROW IS NOW EXECUTED END-TO-END — ALL FOUR ITEMS SPEC §13 CARRIED AS *NOT EXECUTED* ARE DISCHARGED.** Re-run them at YOUR tip; a prior stage's transcript is not evidence for your tree.
   - **Break α FIRED THE SPREAD LEG.** `internal/cluster/ringhash.go:140`, `m := sort.Search(...)` → `m := 0` (⚠️ **NOT `:129`** the doc comment, **NOT `:142`** the legitimate wrap). Observed: `runner_test.go:1293: distribution: subject spread: only 1 backend(s) nonzero, want >= 2 (ring collapsed?)` — byte-identical to the expected string, from **`driver.go:295`, the FOURTH of five sequential legs**. **Affinity and conservation are UPSTREAM in the same function and did NOT fire** (counts collapse to `[256,0,0]`, `256 % 16 == 0` so affinity survives; the sum is unchanged so conservation survives) ⇒ **neither vacuous nor mis-targeted.** Restore ⇒ isolated `--- PASS (3.67s)`: a green FOLLOWING an observed red. ⚠️ **NEW: the unit package DOES catch break α — by exactly ONE test out of 403.** `TestRingHash_DistinctKeysSpread` fires while **8 of the 9 `TestRingHash_*` tests PASS under a TOTAL ring collapse** (including `SameKeySameEndpoint` and `WrapAround`) ⇒ **the differential fixture is a genuine SECOND guard, and the unit-level guard is a single point of failure.**
   - **The FULL differential suite at K=16: 119/119 PASS / 0 FAIL / 0 SKIP, `ok … 402.326s`**, `0061` subtest **3.30 s** — **−6.47 s (−1.58%)** against the same-day pre-fix master baseline (119/119, 408.8 s). ⚠️ **NO hazard fired at all**, `0061` included.
   - **`-race` over `./test/differential/`: ZERO data races.** Two failures on fixtures **unrelated to this row** (`0009-admin-config-dump`, `0084-otlp-access-log`), both at `waitTCPDial` readiness **BEFORE any assertion executed** (`runner_test.go:342`/`:360` are pre-drive gates, not assertion sites), **both isolate-green** ⇒ startup class. ⚠️ **NEITHER was on the PLAN's own hazard roster by name — the FOURTH consecutive stage at which that has held.** ⚠️ **The startup flake fires MORE readily under `-race`** (2 hits with, 0 without, same session).
   - ⚠️ **SPEC §3.6's RUNTIME FIGURE IS REFUTED.** Both arms measured in ONE session, 3 warm runs each: K=16 **4.410 s** wall vs K=4 **4.463 s** ⇒ **−0.053 s (−1.2%)** — **negative, and smaller than the K=4 arm's own spread** (one 4.59 s outlier drives the sign). ⇒ **NO measurable cost from K=4 → K=16, and NOT a speedup either. Do not carry +0.158 s (+4.5%) forward.** ⚠️ **The SPEC's methodological RULE survives its own NUMBER** — *quote the delta against a same-session control* is exactly what produced the different answer.
   - ⚠️ **`127.0.0.12`-`.17` are NO LONGER OWED — DISCHARGED at the PLAN: 16/16 binds PASS**, requested `LocalAddr` == observed on every arm.
   - **WHAT REMAINS:** the prose sweeps (T5/T6), `BEHAVIOR_CONTRACT:1326` (T7), ADR-0298 completion and the row-76 flip (T9).

5. **⚠️ THE BREAK ROSTER — SIXTEEN BREAKS, THIRTEEN ALREADY FIRED.**
   - **β is a TWO-EDIT break** (revert `sourceIPs` **and** `collapseFixtureK` to 4). **FIRED at the PLAN:** `collapse rate 0.03700 (74/2000) >= bar 0.001`, a 35× margin. ⚠️ **It proves only the UNIT TEST's constant is load-bearing — NOT the fixture's. Do not describe β as proving more than it does.**
   - **β-SINGLE-EDIT is a DECLARED MUST-NOT-FIRE and it DID NOT FIRE** (exit 0). `grep -rn '\bsourceIPs\b' --include='*.go' .` ⇒ **3 hits, all in the 0061 driver** — mechanically decoupled. **That green is the FINDING**, and it is what T4's linkage gate exists to close; show T4 RED on that same tree.
   - **γ-SHARP is the load-bearing anti-vacuity break and it FIRED:** freezing the ephemeral ports drove the **CONTROL leg RED while the MEASURED leg stayed GREEN at `0/2000`**. ⚠️ **At K=16, M=2000 the expected collapse count is 0.00014 ⇒ the measured leg is a NULL RESULT standing alone.** Any wording of the form *"the unit test proves the ring randomizes"* is true **only of the CONTROL leg**.
   - **Six anti-vacuity breaks, one per unit test, ALL SIX FIRED**, each producing exactly ONE red test.

6. **⚠️ A PASS-COUNT IS NOT A MARGIN — and `README.md:143-148` must be STRUCK, not re-measured.** It certifies the assertion *"overwhelmingly stable"* on `-count=20` → 20/20 and calls the ring *"fixed"*. **Both false.** The ring is keyed on the endpoint address **including its OS-ephemeral port**, so it is a fresh random 3-way partition every run; and `(1−0.0355)^20 ≈ 0.48`, so the check was **more likely than not to pass even if the assertion were exactly as broken as it in fact is.** 95% power would need `-count=81`. ⇒ **do NOT verify this row by re-running the fixture N times.**

7. **⚠️ THE THREE THINGS THE PLAN REFUTED THAT THE IMPL MUST NOT SILENTLY RE-INHERIT.**
   - **The naive ×4 rescale of the scatter tuple manufactures a THIRD vacuity.** `{20,28,16}×4 = {80,112,64}` — **all three multiples of 16** ⇒ the tuple passes affinity, falls through to spread, satisfies `err == nil`, and prints `--- PASS` while testing a different leg. **The one test that was HEALTHY would have been silently broken.** Use `{20,108,128}`.
   - **The SPEC's vacuity MECHANISM is only two-thirds right.** Affinity is evaluated **INSIDE** the per-element loop (`driver.go:284`), **BEFORE** conservation (`:291`) — so `_ScatterBitesAffinity` and `_SubjectConservation` were **never vacuous**. The COUNT is right (exactly two, exactly the two named); the stated REASON over-generalises. Conservation shadows only the legs that FOLLOW it.
   - **`3^(1−K)` is a CONSERVATIVE UPPER BOUND, not "the" probability.** Measured over 2×10⁵ real ring draws: p̂/analytic **0.949** at K=4, **0.689** at K=8, while a random-key discriminator arm recovers 1.010/1.104 ⇒ the deficit belongs to the specific `xxHash64` key set, not the builder. ⚠️ **K=16 is NOT measured and cannot be at that scale** (expected count 0.014; `0/200000` bounds p̂ ≲ 1.5e-5) — **7.0e-8 is analytic/extrapolated and must be stated as such.** ⚠️ **The CONTROL count is 74/2000, not the SPEC's 71.**

8. **⚠️ SPEC §10 CARRIES FOUR ANCHOR ERRORS, AND SPEC §4 TWO MORE.** Re-derived at the PLAN, all settled by command: **`README:58` is really `:57`** (`:58` has no numeral at all) · **`README:114` and `:135` are MISSING** from the roster, and `:135` carries **TWO** `64`s · **`driver_test.go` is 15 sites, not 8**, and ⚠️ **`:43`/`:52` are executable `t.Fatal` STRING LITERALS** while `:11` carries the file's only bare `4` · **SPEC §4's collision claim is FALSE for `allSame`** (4 hits, 3 live Go in `package router`; safe by package scoping, but the helper is renamed `collapseAllSame` so the check is clean rather than explained) · **SPEC §4's import prediction is refuted** — the real delta is `fmt`/`math`/`math/rand/v2`, and `ringhash_test.go:3` is a **single-line `import "testing"`** needing a 1→N structural conversion.

9. **⚠️ SWEEP FOR THE SHAPE OF THE OLD CLAIM, NOT ONLY ITS NUMERALS.** `0061/driver.go:270-271`, `expectations.yaml:21-22` and `README.md:54` assert the spread leg is *"DETERMINISTIC/EXACT — not a σ-band"*, which is **true of affinity and FALSE of spread**. ⚠️ **An agent argued at the PLAN that these need no edit; the controller ruled against it on the text** — the adjective governs the compound *"affinity + SPREAD"* while only the parenthetical is affinity-scoped. These sites contain **none** of the stale numerals and are structurally invisible to a numeral-keyed sweep — the phase-75 `0110/expectations.yaml` lesson. ⚠️ **`README.md:81`'s `multinomial(64, 1/3)` and `{0,16,32,48,64}` MUST also change** — SPEC §7's *"survives — do not delete"* invites leaving it byte-untouched; the **bound** survives (0.096% → 0.381%, still `< 1%`), the **numerals** do not. ⚠️ **`0062/driver.go:299` and `0063/driver.go:299` carry the same false adjective and are DELIBERATELY OUT OF SCOPE** (⚠️ **`:299`, not the SPEC's `:300`**); ADR-0298 ¶11 records the contradiction instead.

10. **⚠️ `ROADMAP.md:138` (row 76) IS BRAINSTORM-ERA AND CARRIES FOUR STALE CLAIMS** the SPEC refuted — `sourceIPs 4 → 10` · `K=10 → 5.1e-5` · β as a single-edit *"THE PROOF"* · *"THE EXECUTABLE DELTA IS ONE INTEGER … 7 hits, EVERY ONE A COMMENT"* (`grep -c '\b64\b' driver.go` ⇒ **8**, one of them CODE). The row-flip owes all four corrections — the phase-75 precedent, whose cell carried five.

11. **⚠️ `BEHAVIOR_CONTRACT.md:1326` — ONE line, IN PLACE, no line-count change.** Verified in scratch at the PLAN: **5746 → 5746 lines, `diff` exactly `1326c1326`**, chars 1791 → 1796. `127.0.0.2..5` → `..17`; **four** `64`s → `256`. ⚠️ **`% 16 == 0`, `16 conns each` and `all-16-or-0` all STAY** (`burstPerIP` unmoved) — and `256` contains no `16` substring, so a line-scoped `s/64/256/g` cannot collide. It is the **only** such line (`grep -n 'source IPs .127\|16 conns each'` ⇒ 1 hit).

12. **ADR-0298 completes IN PLACE, no renumber.** Mirror ADR-0297 (`DECISIONS.md:17324-17392`): rewrite `PROPOSED` → `COMPLETE` in the blockquote, **RETAIN the italic footer verbatim**, then append `### Decision (landed at the phase-76 IMPL)` (narrative opener → numbered task list with shas → lettered rulings) and `### Consequences (landed at the phase-76 IMPL)` (bulleted). **NO `---` separator** — the last `^---$` is at `:17020`. Tail stays ADR-0298; next-free stays **ADR-0299**. ⚠️ **ADR-0298 carries NO whole-file grep count** — that species self-falsified in ADR-0296 ¶3, ADR-0297 ¶7 AND ¶9, and escalated at the BRAINSTORM to a flipped sentinel check. ⚠️ **It claims NO family ordinal** — the Load-balancing family was declared **CLOSED at row 54** (`ROADMAP.md:116`; `grep -c 'Load-balancing-family row'` ⇒ **8**, the chain 34/35/36/37/38/52/53/54 complete). The right reason the heading stays unamended is *"maintenance rows do not extend a charter"*.

13. **⚠️ THE STAT SURFACE IS DOCUMENTARY — assert the DELTA, never the total.** `1205` has **NO mechanical command** and rides **TWO** recorded ledger gaps (`1200 → 1201`, and Phase 46.1b closing at 1198 while 47.1 opens at 1200). `grep -n '1205'` ⇒ **3 hits** (`:831`, `:847`, the `:5004` ledger tail) — **all three STAY; none needs editing, and phase 76 adds NO ledger line.** The +0 argument here is **structural**: zero production `.go` bytes change, so no `NewCounter`/`NewGauge` call site can move.

14. **The live hazards — never reflex-classify any of these as a regression.** The PRE-EXISTING `internal/cluster` `-race` flake (`TestOutlierDetector_ConcurrentEjectExactlyOnce` — ⚠️ it did **NOT** surface in FIVE green `-race` runs across the PLAN) · the full-suite startup flake (`subject ready: EOF`, and at phase 75 also `bind: address already in use` on an UNRELATED fixture, **failing BEFORE any assertion**) · `reference_sds_init_fetch_timeout_dial_budget_flake` · **`0061-lb-ring-hash` — ⚠️ THIS ROW'S OWN SUBJECT; a spread firing BEFORE the fix is EXPECTED at ~3.6%/run, and AFTER the fix it is a FINDING** · and TWO still-**UNINDEXED** load flakes: `internal/httpclient TestOptions_ZeroValue_NoOpDefaults` and `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery`. ⚠️ **A stage brief's flake list is not the index — NEITHER flake that fired at phase 75 was on the PLAN's roster of six.** Isolate-re-run, then state the classification and its evidence. ⚠️ **UNMEASURED until the IMPL: the differential-suite effect of a 4× larger workload** (256 connections, 16 bound source IPs per side) on runtime and dial budget.

15. **The rivals for the NEXT row — re-costed at the BRAINSTORM, NOT re-costed since. Re-derive before adopting** (`reference_deferred_candidate_cost_restale`): **the Runtime family opening ~10-14 — the only evaluated candidate that GENUINELY clears a check-(3) blocker**, with SIX landed ADR-anchored `runtime_key` PARSE-REJECT arms forward-pointing at it (ADR-0187 `DECISIONS.md:11280`, ADR-0195 `:12413`) · `fault.abort.grpc_status` ~10-13 (blocker RETIRED by execution, but ⚠️ **it does NOT open the gRPC family**) · `ssl.connection_error` ~10-13 (recorded predicate INCOMPLETE — an RST peer yields `*net.OpError`, needing `syscall.ECONNRESET` ⇒ **+1 PRODUCTION import**) · `upstream_cluster` ~7-9 but ~85-100 lines across ~18 files, central premise **UNVERIFIED** · `Listener.stat_prefix` ~7-10. **Maintenance deferrals named at the PLAN:** `0059`'s empirical-only margins (⚠️ **the same evidentiary shape as `0061`'s refuted 20/20**) · `0013`'s 10 ms band · the `0062`/`0063` σ-band adjective · a mechanical COUNT of the stat surface. ⚠️ **AGREEMENT ACROSS DOCUMENTS IS NOT EVIDENCE** — `fault.abort.grpc_status` rode three stages on one unexecuted assertion, refuted by a single `grep -c`. **A claim that has survived several stages is MORE suspect than one written yesterday, not less.**

16. **Counts at exit (re-run MECHANICALLY in the worktree; never copy them):** fixtures **119** (`ls -d test/fixtures/[0-9]*/ | wc -l`; `0061` is EDITED, not added) / fuzzers **55** (`grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`) / **stat surface 1205** (BEHAVIOR_CONTRACT doc count; **NO mechanical command** — item 13) / BackendKind tail **38** (⚠️ a TAIL VALUE — the file declares 39 constants, 0-38) / go.mod modules **2** (the lineage figure; the single `go.mod` requires **67** = 18 direct + 49 indirect) / DECISIONS tail **ADR-0298** (PROPOSED now, COMPLETE at this IMPL; next-free **ADR-0299**) / next-free reference port **10450** (⚠️ **NOT** `10118` — ports are NOT fixture-index aligned; phase 76 needs no new port).

## What THIS session does — the phase-76 IMPL

One stage per session. Execute `docs/envoy-go/phases/76-lb-ring-hash-spread-margin/PLAN.md`'s **T1-T9** spine, recording per-task ACTUAL results in `PROGRESS.md` under its `# IMPL record` heading (red-first verbatims, WHICH break fired and any that did NOT, every refuted claim, every gate's observed negative control). **ROW 76 FLIPS `in-progress` → `done`** at the T9 six-gate — **a SINGLE FLAT ROW**, no split legs (`reference_roadmap_split_phase_row_done`). ADR-0298 completes IN PLACE. **NO deferred sentence is narrowed** — check (2) must **STAY 3**.

**Stage-close mechanics (an IMPL):** STATE §Current rolled **IN PLACE** (lifecycle **3 → DONE**; §Recent re-capped at FIVE **with its PREAMBLE updated** — the ADR-0288 rule). ⚠️ **The ADR-0288 singleton greps do NOT all return the same number** — the rule statement is `STATE.md:7`, the FIRST hit, with the live value second, and §Recent's historical recaps add more; a close script taking `head -1` reads the RULE, not the value. Verify with `grep -n`, and **never "fix" a count down**. ⚠️ **Do NOT write the three field-name tokens verbatim into `STATE.md` while reporting their counts** — that fired at the PLAN and each count went +1. `next-prompt.txt` rolled to **the next phase's BRAINSTORM** (the roller SELF-PICKS per the standing directive; record the pick and the rejected alternatives).

Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** (`reference_parallel_subagents_private_scratch`). Per-task `gofmt` + `golangci-lint` on touched packages (`feedback_pertask_gofmt_lint`).

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. ⚠️ **THIS HAS NOW FIRED LIVE IN SIX CONSECUTIVE SESSIONS — including throughout the phase-76 PLAN, where the tool reported `Shell cwd was reset to /home/esa/git/envoy-go` inline on a majority of calls.** At the phase-75 IMPL's T10 `pwd` had silently reset to the MAIN checkout on `master` one step before a gate run; at the phase-72 close the same hazard put a stray commit on `master` AND made a "final gates" run validate master's pre-row tree — GREEN while proving nothing. **Use `git -C <abs-worktree-path>` for every git command, prefer absolute paths over `cd`, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline) + §Project counts + the §Recent lineage (five bullets: phase-76 PLAN + phase-76 SPEC + phase-76 BRAINSTORM + phase-75 IMPL + phase-75 PLAN).
2. `docs/envoy-go/phases/76-lb-ring-hash-spread-margin/PLAN.md` — **the whole document**, and especially **§1.1** (the five broken gates, three found at the PLAN), **§1.2** (the ×4 scatter trap), **§1.3** (the corrected vacuity mechanism), **§1.4** (SPEC §10's four anchor errors), **§1.5** (what was already executed), **§1.6** (`3^(1−K)` as an upper bound, and why K=16 is not measured), **§1.7** (⚠️ **the four SPEC §13 items DISCHARGED — break α's fired leg, the full suite at K=16, `-race`, and the REFUTED runtime cost**, plus the finding that the unit package guards break α by exactly ONE test out of 403), **§1.8** (the corrections owed forward, incl. the ROADMAP cell's four stale claims and the `STATE.md` singleton-grep trap), **§2** (global constraints), **§3** (the T1-T9 spine with verbatim executed code), **§4** (the sixteen-break map), and **§7** (live hazards).
3. `docs/envoy-go/phases/76-lb-ring-hash-spread-margin/PROGRESS.md` — the adversarial-pass record, the nineteen findings that changed the PLAN, the agent disagreement the controller ruled on, and the "still OWED" list. **Append your IMPL record under its `# IMPL record` heading.**
4. `docs/envoy-go/phases/76-lb-ring-hash-spread-margin/SPEC.md` — for the derivation (§3.2's σ table and the `0062`/`0063` house standard) and the docket, ⚠️ **but read it AGAINST PLAN §1.1/§1.4**: its linkage gate is broken both ways, its §10 roster has four errors, and its §4 identifier and import claims are refuted.
5. `test/fixtures/0061-lb-ring-hash/` in full — `driver/driver.go` (constants `:78-80`, the `net.IPv4(127,0,0,byte(2+s))` bind at `:196`, `AssertDistribution` `:276-306` with the **leg order** length `:277` → affinity `:284` → subject conservation `:291` → spread **`:295`** → reference conservation `:302`, and `:335`/`:367`), **`driver/driver_test.go`**, `expectations.yaml`, `README.md` (⚠️ **`:143-148` is the refuted flake-check paragraph**). Plus `internal/cluster/ringhash.go` (`:88-92`, `:129-132` doc, `:133` decl, **`:140` break α**, `:142` wrap), `internal/cluster/hash.go:121-135`, `internal/cluster/ringhash_test.go` (⚠️ `:3` is a single-line import; `_DistinctKeysSpread` `:50-64`; insertion point **after `:64`**), `internal/cluster/leastrequest_test.go:19` (⚠️ `eps(n)` builds a FROZEN ring — must NOT be used by the measurement), and `test/differential/runner_test.go:272`.
6. `docs/envoy-go/phases/75-tls-no-certificate-stat/PROGRESS.md` — the richest single source on how this lineage's stages fail: what every task ACTUALLY did, which break fired and how, the fourteen refuted claims, and the two broken gate commands.
7. `docs/envoy-go/DECISIONS.md` — **ADR-0298** (`:17394-17422`, §Context PROPOSED, eleven paragraphs — this IMPL completes it) and **ADR-0297** (`:17324-17392`) in full as the completed form to mirror; ADR-0044 (⚠️ it does NOT contain the §Context-draft discipline — use `ADR-0044-as-used`) · ADR-0045 · ADR-0106 · ADR-0288.
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Load-bearing for this IMPL: **`reference_0061_ring_hash_spread_flake`** and **`reference_differential_band_sigma_margin`** (this row's subject), `reference_gate_command_negative_control` (⚠️ **NARROWED at the PLAN — the `go doc` fail-open is `./`-prefix-specific**), `reference_sentinel_matcher_string_self_clears`, `reference_plan_schedules_edits_to_a_byte_gated_file`, `reference_fixture_workload_constant_desync`, `reference_ordered_assertion_legs_vacuous_on_constant_change`, `reference_change_set_measure_not_build_measure`, `reference_differential_break_protocol_count1`, `reference_break_protocol_commit_first`, `reference_deliberate_break_wrong_assertion`, `reference_liveness_break_needs_failing_baseline`, `reference_positive_arm_cannot_catch_overfiring`, `reference_fatalf_makes_assertions_unreachable`, `reference_probe_must_discriminate`, `reference_code_comment_not_evidence`, `reference_quoting_is_not_executing`, `feedback_brief_citations_not_evidence`, `reference_a_drift_correction_is_itself_a_claim`, `reference_next_prompt_tracked_despite_gitignore`, `reference_roadmap_split_phase_row_done`, `feedback_git_worktrees`, `feedback_execution_style`, and **`reference_bash_cwd_reset_commits_to_main`**.
