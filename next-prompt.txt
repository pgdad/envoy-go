START a fresh cold-start session for envoy-go — **the phase-40.2 (`outlier-detection-other-consecutive`: the gateway + local-origin consecutive detectors + the split accounting switch) SPEC is DONE, LANDED on master (commit `2919a3a`), and PUSHED to `origin/master`; the next lifecycle step is the phase-40.2 PLAN** (`superpowers:writing-plans` — decompose SPEC §10's ~10–14-task spine into bite-sized TDD tasks; resolve the §12 D-S40.2-1..7 questions; run the FINAL ADR-0045 split-gate re-check [anticipated NO FURTHER SPLIT — ~150–300 prod LoC]; plan-document-reviewer loop; land + push) OR a new subject. Confirm the tip via `git log --oneline -5` (the substantive master tip is the phase-40.2 SPEC `2919a3a`; the prior substantive tip is the phase-40.1 IMPL squash `bb4da7b`). Counts at the 40.2 SPEC (UNCHANGED — docs-only): stat surface **1137** / fixtures **71** / fuzzers **42** / BackendKind tail **35** (`HTTP503Responder`) / DECISIONS tail **ADR-0245**, next-free **ADR-0246**.

## What just landed (phase 40.2 SPEC — the durable record is `docs/envoy-go/phases/40.2-outlier-detection-other-consecutive/{SPEC,README}.md`; read those, not this summary)

The SPEC was authored DIRECTLY (the phase-40 BRAINSTORM is DONE for the whole family — it chartered all three legs). The §11 D-OD2-* empirical pins were EXECUTED IN-SESSION live against `envoyproxy/envoy:contrib-v1.37.2` per `reference_docker_probe_bridge_network` (a 3-cluster bridge-net probe: gateway `{2×200,1×503}`; local-origin split=true `{2×200,1×dead}`; local-origin split=false `{2×200,1×dead}`; `/stats` scrapes + `--mode validate` reject probes). spec-document-reviewer APPROVE-WITH-MINORS (the M1 gateway-no-eject-fall-through fix + m4/m5 applied).

**Charters (40.2 = the SECOND leg of the by-detector-class split):** `consecutive_gateway_failure` (eject on N consecutive gateway-class 5xx `{502,503,504}`) + `consecutive_local_origin_failure` (eject on N consecutive connect/reset failures, split=true only) + `split_external_local_origin_errors` (the accounting switch). Over the 40.1 substrate (ADR-0245) with NO router-seam re-design — it makes the 40.1-reserved `UpstreamResult.LocalOriginErr` field LIVE.

**SPEC-firmed (AMEND-OD2-1..6):**
- OD2-1: the gateway detector is active-by-default + detect-only-by-default (`enforcing_consecutive_gateway_failure` default 0); at 40.2 envoy-go now emits `ejections_detected_consecutive_gateway_failure` on the EXISTING `0069`'s 503s → the `0069` StatsAsserter is WIDENED to cross-assert it (a 40.2 task; `0069` routing/ejection UNCHANGED).
- OD2-2: gateway-checked-FIRST + the once-ejected counter reset (a 503 host ejects via gateway → `detected_consecutive_5xx` stays 0, live-pinned; a gateway-no-eject falls through so `consec5xx` ALSO advances — both counters advance on every 5xx, only an EJECTION clears them).
- OD2-3 (the load-bearing pin): the split accounting — split=false ⇒ a local-origin failure mapped to the 5xx/gateway detectors (the local-origin detector inactive); split=true ⇒ a dedicated local-origin detector ONLY + externals to the 5xx/gateway detectors.
- OD2-4: `LocalOriginErr` populated at the H1 acquire-503/write-502/read-502 + H2 dial-502/roundtrip-502 sites; the picked endpoint surfaced from the shared `Cluster.Dial`/`AcquireH1` connect-failure path (the ONE framework touch — today returns `Endpoint{}`); an LB-pick-failed (no host) ⇒ no per-host attribution (no seam call); the H2 ctx-cancel path is a downstream cancel (no seam call); the 40.1 SUCCESS sites UNCHANGED.
- OD2-5: stat surface **+4 → 1141** (`ejections_{detected,enforced}_consecutive_gateway_failure` + `ejections_{detected,enforced}_consecutive_local_origin_failure`; the 40.1 double-count EXTENDED); the legacy + statistical stats DEFERRED to 40.3.
- OD2-6: ZERO new BackendKind (`0070` gateway reuses `HTTP503Responder` 35; `0071` local-origin reuses the driver-side `allocDeadPort` dead-host mechanism — the `0066`/`0067` precedent).

Two new reject arms (`enforcing_consecutive_{gateway,local_origin}_failure > 100`; house wording at PLAN). Differentials `0070-outlier-detection-consecutive-gateway-failure` + `0071-outlier-detection-local-origin` (both cross-side traffic-driven ejection; the split=false local-origin-as-5xx arm a `record`-level unit test). **ADR-0246 §Context DRAFT anchored** in SPEC §13 (DECISIONS tail STAYS ADR-0245 at the SPEC per ADR-0044; §Decision/§Consequences land at the 40.2 IMPL).

## The phase-40.2 PLAN obligations (the next leg in the lifecycle)

Decompose SPEC §10's anticipated ~10–14-task TDD spine into bite-sized tasks (the per-task code + file structure; the 40.1 PLAN is the template — `docs/envoy-go/phases/40.1-outlier-detection-consecutive-5xx/PLAN.md`). Resolve the §12 D-S40.2-1..7 PLAN/IMPL questions: (1) the two house reject strings; (2) the `record` reset ordering replicating the `detected_consecutive_5xx == 0`-on-gateway-eject pin (gateway-first + 5xx-streak-reset-on-gateway-eject, or a host-already-ejected guard); (3) the signature for surfacing the picked endpoint on the shared `Cluster.Dial`/`AcquireH1` connect-failure path (widen `AcquireH1` to `(*PooledH1Conn, Endpoint, error)` mirroring `DialH2`, surface `ep` from `Dial` on the DialContext/handshake failures); (4) the local-origin classification boundary (the H1 write/read 502 post-connect-reset sites IN at the MVP); (5) `0070`/`0071` constants single-sourced (`reference_fixture_workload_constant_desync`) + the `0069` StatsAsserter gateway-detected widening; (6) `0070`+`0071` two dirs vs one folded dir (the `0067`/`0068` two-dir precedent); (7) the ADR-0045 final split-gate re-check (anticipated NO FURTHER SPLIT). plan-document-reviewer loop; land + push.

## Honor throughout (the lifecycle discipline)

- **Lifecycle = one stage per session:** the PLAN (`superpowers:writing-plans`). `feedback_git_worktrees` · `feedback_execution_style` (subagent-driven IMPL the session after) · `feedback_push_to_origin` · `feedback_subagents_no_push`.
- **IMPL-time discipline (the session after the 40.2 PLAN):** `reference_differential_run_selector` (`-run 'TestDifferential/<NNNN>'`) · `reference_differential_break_protocol_count1` (`-count=1`; the seam rides the SAME live `doH1/doH2ClusterAction` drivers, now ALSO at the connection-FAILURE sites) · `reference_docker_probe_bridge_network` · `reference_health_check_propagation_warmup` (the traffic-driven ejection gates on `ejections_active` convergence + warmup + delta counters) · `reference_fixture_workload_constant_desync` · `reference_percent_cap_cross_multiply` (the reused 40.1 cap) · `feedback_pertask_gofmt_lint`.
- **ADR/process:** ADR-0044 (§Context at SPEC [done]; body at IMPL) · ADR-0045 (split-gate re-check at the PLAN) · ADR-0052 (six-gate) · ADR-0080 (byte-stable rejects) · ADR-0106 + `reference_roadmap_split_phase_row_done` (row 40 flips `done` only when ALL THREE legs land — 40.2 is NOT the last; 40.3 [statistical detectors] remains) · ADR-0227 (`contrib-v1.37.2`).

## Read first (in order)

1. THIS FILE — the post-phase-40.2-SPEC cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 40.2 (outlier-detection-other-consecutive) SPEC done`; the count footer (1137 / 71 / 42 / 35 / ADR-0245, next-free ADR-0246); the lifecycle routing (next-skill = the 40.2 PLAN).
3. `docs/envoy-go/phases/40.2-outlier-detection-other-consecutive/SPEC.md` (the §1.1 AMEND-OD2-1..6 block; the §3 architecture — the parse/detector extension + the gateway/local-origin/split branch + the `LocalOriginErr` seam population + the picked-endpoint attribution; the §5 proto roster fields 10–14; the §6 two reject arms; the §7 stat surface +4; the §8 `0070`/`0071` differential design; the §11 D-OD2-* live-pin block; the §12 D-S40.2-1..7; the §13 ADR-0246 §Context draft) + `docs/envoy-go/phases/40.1-outlier-detection-consecutive-5xx/{SPEC,PLAN}.md` (the substrate + the PLAN template).
4. `internal/cluster/{outlier.go,health.go,cluster.go,manager.go,dial_h2.go}` (the `outlierConfig`/`parseOutlierDetection`/`outlierDetector.record` 40.2 extends; the `RecordUpstreamResult` seam + `UpstreamResult{StatusCode, LocalOriginErr}`; `AcquireH1` [returns `(*PooledH1Conn, error)` — needs widening] + `DialH2`/`Dial` [returns `Endpoint{}` on the connect-failure path]) + `internal/filter/http/router/{router.go,router_h2.go}` (the success sites :656/:101 AND the five connection-failure sites the 40.2 local-origin seam must populate — H1 :613/:639/:651, H2 :79/:97) + `test/fixtures/0069-outlier-detection-consecutive-5xx/` (the differential template + the StatsAsserter to widen) + `test/differential/` (the `allocDeadPort` dead-host mechanism in the 0066/0067 drivers; the `HTTP503Responder` BackendKind 35).
5. `docs/envoy-go/DECISIONS.md` (ADR-0245 [the 40.2-reused substrate] + the process ADRs) + `docs/envoy-go/BEHAVIOR_CONTRACT.md` (the `### Cluster — passive health (outlier detection)` subsection 40.2 extends).
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`.
