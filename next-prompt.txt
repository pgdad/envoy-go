=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **PHASE 87 (`h2-double-slash-path-routing`) IS MID-LIFECYCLE: BRAINSTORM done (2026-08-12), SPEC done (2026-08-13). ROW 87 IS `in-progress` AT `ROADMAP.md:149`, `want` IS 119.** ⇒ **THIS SESSION PERFORMS THE PHASE-87 PLAN.** `STATE.md`'s `next-skill:` reads `PLAN for phase 87` — that IS your instruction. Do NOT re-pick a subject. **The SPEC disposed ALL SEVEN BRAINSTORM-§4 questions BY EXECUTION** (SPEC.md §1-§8): the fix is **D-87-PRIM `url.ParseRequestURI` + D-87-FRAG a 3-line explicit fragment reject**, prototyped at **net +10 production `.go`, ONE file** (`stream.go`); both failure arms RED-at-tip / GREEN-at-prototype with the ROUTED PATH asserted by body; the reference EXECUTED (`b87-main`) — `//foo` -> 200, `//foo/bar` -> FULL-path routed, `/foo#frag` -> 400, rootless/bare-query -> connection teardown; **Q5's feared cost EVAPORATED** — x/net `http2.Transport` delivers a literal leading-`//` `:path` UNMODIFIED (measured through `test/helpers.H2CRoundTrip`), so **D-87-DIFF extends `0004-h2-routing`** (one `prefix: "//edge"` route both YAMLs + one appended drive-loop; NO raw-frame writer, NO subprocess, NO new fixture). **ADR-0309 §Context is APPENDED, STATUS `PROPOSED` — the strict guard is ARMED at 1 and the PLAN does NOT disarm it** (the IMPL completes the ADR). `ROADMAP.md` stays BYTE-UNTOUCHED at the PLAN (row 87 stays `in-progress`, `want` stays 119); the row flips `done` only at the phase-87 IMPL.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=119 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **`want` IS 119 AT THIS TIP; row 87 is `in-progress` (this is a PLAN, not a row-flip stage — `ROADMAP.md` stays BYTE-UNTOUCHED, `want` stays 119).** ⚠️ **AT THIS TIP check (1) prints EXACTLY `NOT DONE: row 87` (the single expected line while the phase is open). ANY OTHER row id appearing is a FINDING, and its ABSENCE (a SILENT check (1)) would mean row 87 was flipped early — also a FINDING at a PLAN.** ⚠️ **THE SENTINEL DOES NOT FIRE:** check (2) prints SIX (at `:197 :203 :209 :219 :225 :233` — re-derive, the anchors drift with every edit) and the condition is a CONJUNCTION. Check (3) is SILENT.

⚠️ **THE DOCTORING NCs REMAIN MANDATORY EVERY SESSION.** Both:
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md at want=119: expect "NOT DONE: row 62" AND "NOT DONE: row 87" (row 87 is in-progress this stage, so the un-doctored side already prints row 87 — the NC ADDS row 62)
```
```sh
# the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```

⚠️ **AND WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -oiE '-family row'` fails with `grep: amily row: No such file or directory` and the surrounding arithmetic prints `base=0 now=0 delta=0`, **which reads exactly like "no change".** Observed live at FOUR phase-84 stages. **Always pass `--` before the pattern**, and remember the counts are form-dependent: **95 occurrences vs 67 LINES** at this tip (`grep -c` counts LINES). **A gate that reads zero on BOTH sides is not evidence of invariance.** ⚠️ AND the carried **ARM-A {119, 131} figure binds ONLY with its escape-aware pipe-split command** (BRAINSTORM-86 §6) — ad-hoc field-count forms do NOT reproduce it (`NF != 8` flags 17 rows); do not "correct" the figure with a different form.

**SENTINEL RE-RUN MECHANICALLY AT THE PHASE-87 SPEC CLOSE (2026-08-13, base `d7fd87fd`), ONE SIDE — a SPEC edits no ROADMAP (byte-untouched verified by EMPTY DIFF against master). IT DOES NOT FIRE; `stop` was NOT created** (verified absent at repo root AND stage worktree). ACTUAL output, recorded not predicted: input measured **237 lines / 119 data rows** · **(1) `NOT DONE: row 87`** alone at `want=119` (denominator silent) · **(2) SIX `:197 :203 :209 :219 :225 :233`** (⚠️ do NOT "fix" the six and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases; the candidates sentences are a WINDOW onto a larger deferred backlog, not an inventory of it) · **(3) SILENT**. The condition is a CONJUNCTION ⇒ the sentinel does NOT fire — and `STATE.md`'s `next-skill:` reads `PLAN for phase 87`, i.e. a next stage EXISTS and the loop continues.
- **ALL FOUR NCs FIRED AT THIS CLOSE:** row-62 doctoring => `NOT DONE: row 62` AND `NOT DONE: row 87` — with `NC LANDED? [ in-progress ]` inspected first; `want` off-by-one (`want=118` => `GATE FAIL: examined 119 data rows, expected 118`, plus the expected `NOT DONE: row 87`); check-(3) doctoring (residual confirmed **2 -> 0** on the doctored copy first) => `NEVER OPENED: gRPC` while `WASM` correctly stayed silent; check-(2) one-arm counts **5** (long arm alone) / **1** (short arm alone), union **6** (a one-arm strip is NOT an NC for the union).
- ⚠️ **Re-run all three checks and both doctoring NCs yourself and record what YOU see** (ONE side — a PLAN edits no ROADMAP; verify byte-untouched by empty diff).

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (ugrep under the hood). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Reproduced at TWENTY separate tips.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **THE METHOD BINDS; carry no standing discrepancy figure and no standing absolute.** ⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS; print FILE NAMES, never a count.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files; use `-v --no-index`, and `git status` is the discriminating check). ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **`grep -c` ON ZERO MATCHES IS TOOL-SPECIFIC** (only `git grep -c` prints nothing; all exit 1 — capture the value, never chain on the exit code). ⚠️ **`grep -c` COUNTS *LINES*, NOT *OCCURRENCES*.** ⚠️ **`\t` IN GNU ERE IS A LITERAL `t`** — use `-P`. ⚠️ **A `git grep` PATHSPEC ENDING IN `/` READS A FAIL-UNSAFE ZERO** (shape 33) — use the shell-glob form. ⚠️ **AN UNANCHORED PANIC-GREP FALSE-FIRES 14 TIMES ON A FULLY-GREEN DIFFERENTIAL LOG** — **the canonical gate is ANCHORED: `^panic:|DATA RACE|SIGSEGV`. A gate that fires on a green log is worse than no gate.**

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`** with FOUR blind spots (an em-dash, a DOT in `http-1.1`, two LETTER-suffixed ids). ⚠️ **EXECUTED on scratch copies at the phase-77 PLAN: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** ⚠️ **DO NOT restore the regex form.** At this tip check (1) prints `NOT DONE: row 87` — **the row-62 doctoring NC is what proves the check still LOOKS beyond your own row**; any row id other than 87 appearing is a finding.

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

⚠️ **Run the union form, never one half of it.** ⚠️ **A ONE-ARM STRIP IS NOT AN NC** (6 -> 5 short / 6 -> 1 long; never 6 -> 0). ⚠️ **The documented trap's stated MECHANISM is refuted while the trap is real** — the inflation comes from ROADMAP table rows carrying BRAINSTORM prose. **Cite the command; do not re-inherit the explanation.** ⚠️ **The line numbers drift with edits — re-derive them; the current six are `:197 :203 :209 :219 :225 :233`.** ⚠️ **These six paragraphs remain the final sentinel blocker, and any edit near them is a sentinel-affecting edit.** All six are standalone family PROSE paragraphs (not rows), naming ~42 distinct candidates; every "retire the paragraphs" shape is doctrinally foreclosed (`reference_sentinel_matcher_string_self_clears`). ⚠️ Row 87's provenance is OUTSIDE the windows (the row-85/86 shape) — **nothing narrows in any window sentence at row-done.**


==============================================


## STATUS AS OF 2026-08-13: PHASE 87 (`h2-double-slash-path-routing`) SPEC DONE (row 87 `in-progress` at `ROADMAP.md:149`, `want` 119, base `d7fd87fd`). **THIS SESSION PERFORMS THE PHASE-87 PLAN** (`STATE.md`'s `next-skill:` reads `PLAN for phase 87`).

**What the 87 SPEC landed (2026-08-13, base `d7fd87fd`, branch `phase-87-spec`, ONE squashed commit).** Docs-only: ZERO production `.go`, ZERO test `.go` landed; `ROADMAP.md` and `BEHAVIOR_CONTRACT.md` BYTE-UNTOUCHED; **`DECISIONS.md` gains ADR-0309 §Context STATUS `PROPOSED`** (18098 -> 18114, numstat `16 0`, base a byte-exact prefix; headings 307 -> 308; tail ADR-0309; **strict `PROPOSED` guard ARMED 0 -> 1 — the live pointer the phase-87 IMPL disarms**; STATUS census 21 -> 22; `^---$` stays 216; next-free **ADR-0310**, TAIL-derived). ⚠️ NAMED DEPARTURE CONTINUED: no agents — probes INLINE (detached prototype worktree `wt-87-proto`, DELETED at close with tree-clean proof; ports 47410-47411/47420-47421 transient; docker `b87-main` torn down BY NAME, 0 remaining). **ALL SEVEN §4 QUESTIONS DISPOSED BY EXECUTION:**

- **Q1 (D-87-PRIM + D-87-FRAG):** `url.ParseRequestURI` replaces `url.Parse` in `buildRequest` (`internal/filter/hcm/h2/stream.go` — cite by SYMBOL; the line drifted `:440` -> `:465` across the lineage), PLUS `strings.IndexByte(path, '#') >= 0` -> `PROTOCOL_ERROR` `fragment in :path` (the row's ONLY new error string; decided by REFERENCE behavior — it 400s a fragment). **Prototype measured: `11 ins / 1 del` = NET +10 production `.go`, ONE file, gofmt clean.** 17-case `net/url` differential: byte-identical Path/RawPath/RawQuery on every accepted non-`//` form (incl. `/foo%2Fbar` escape semantics, `*`, absolute-form); `//foo`/`//`/`//foo/bar`/`//foo?x=1` REPAIRED; rootless `foo` + bare `?a=b` (today SILENT 404s) become `bad :path` PROTOCOL_ERRORs (reference tears the CONNECTION down on both — direction parity). Manual `&url.URL{}` REJECTED. CONNECT confirmed out of scope (empty/missing `:path` rejected upstream of the parse).
- **Q3:** both arms RED at tip / GREEN at prototype with the ROUTED PATH asserted by BODY (two-route config `prefix:"/bar"` -> `WRONG-bar` + catch-all -> `routed-ok`): tip `//foo` -> 404, `//foo/bar` -> **`WRONG-bar`** (the silent mis-route); prototype both -> `routed-ok` 200; `/bar` control unchanged both sides.
- **Q4 EXECUTED** (`b87-main`, contrib-v1.37.2, DEFAULT path handling): `//foo` -> 200, `//` -> 200, `//foo/bar` -> **FULL-path routed**, `//foo?x=1` -> 200, `/a//b` -> 200 — BRAINSTORM §3 CONFIRMED, no reshape. Raw HEADERS-frame battery (scratch x/net framer client, ~120 lines — curl/`http.NewRequest` cannot emit these): `/foo#frag` -> **400**; rootless/bare-query -> **connection teardown, zero frames**; `OPTIONS *` -> 404 (parity, unchanged by the fix). **D-87-REJECT-SHAPE:** envoy-go rejects at STREAM level (`RST_STREAM PROTOCOL_ERROR`, the codec's existing pseudo-header idiom) vs the reference's 400/conn-teardown — direction parity, NAMED wire-shape divergence, UNIT-level only (no differential reject arm chartered).
- **Q5 — THE FEARED COST EVAPORATED, MEASURED:** x/net `http2.Transport` delivers a literal leading-`//` `:path` UNMODIFIED — proven through the repo's own `test/helpers.H2CRoundTrip` against the tip binary (404/mis-route reproduced exactly as with `curl --path-as-is`). ⇒ **D-87-DIFF: EXTEND `0004-h2-routing`** — ONE route `match: { prefix: "//edge" }` -> `direct_response 200 "edge-ok"` in BOTH YAMLs (above the catch-all), ONE drive-loop APPENDED after the existing 27-request schedule (`GET //edge` expect 200/`edge-ok` concatenated into the transcript; `GET //edge/health` expect 200/`edge-ok` — pre-fix the subject fails BOTH arms: 404 AND the `/health` route's `OK\n` mis-route body). NO raw-frame writer, NO curl subprocess, NO new fixture/BackendKind/port; counts/distribution logic untouched. The raw framer is recorded as a measured FALLBACK only.
- **Q2:** query/RawQuery preserved (`/foo?a=b` identical both primitives; `//foo?x=1` splits post-fix; `RequestURI` stays the LITERAL `:path` — pinned). Zero-regression: `internal/filter/hcm/...` green · **`TestDifferential/0004-h2-routing` PASS against the prototype tree** (real run, named subtest printed) · full `go test ./...` green modulo the flake below.
- **Q6:** unit path-forms table beside `stream_test.go`'s existing `buildRequest` rejection table — nine accept rows (`/`, `/foo`, `/foo?a=b`, `//foo`, `//`, `//foo/bar`, `//foo?x=1`, `/a//b`, `*`) pinning `URL.Path`/`URL.RawQuery`/`RequestURI`-literal + four reject rows (`foo`, `?a=b`, `/foo#frag`, `/foo?a=b#frag`); the SPEC's 63-line probe (captured to scratch) is the seed; ~100-170 lines at the package's Table-B exact-message idiom. Fuzz +0 (`f.Add` seed OPTIONAL, a seed is not a fuzzer).
- **Q7 each re-derived +0:** fixtures **121** (tail `0119`, `0120` FREE) · fuzzers **55/48** · BackendKind tail **38** · go.mod diff EMPTY on the prototype · stat surface no call site in the diff (DELTA re-asserted at the IMPL). **Cost ENUMERATED: production MEASURED EXACTLY +10 net (IMPL band ~10-16 — grep re-confirms ONE non-test parse site in `internal/filter/hcm/`); test `.go` ~130-225 net per-file enumerated — a FLOOR** (ten-firing doctrine; named un-enumerated classes: exact-message pins at the Table-B idiom, reviewer-mandated arms). **D-87-SEQ: ONE IMPL leg, one atomic commit** (swap + fragment guard + unit arms + differential arms + docs + row flip together).

⚠️ **FLAKE FINDING at this SPEC — the 86-IMPL's uncaptured `internal/boot` failure RECURRED WITH IDENTITY CAPTURED:** `TestSDSEndToEnd_FetchFailure_BootFailsClosed/silent_SDS_server:_validation_context_fetch_times_out,_boot_fails` (`internal/boot/boot_sds_e2e_test.go:551`) — under full-suite load the fetch fails with the gRPC `DeadlineExceeded` recv error instead of the initial-fetch-timeout message the test asserts. Cleared scoped `-count=5` + full-package green. **SDS dial-budget class, now with a NAMED member.** Recurrence at the PLAN/IMPL is expected occasionally under full-suite load: capture `-v` output BEFORE re-running, then clear scoped.

### ⚠️ WHAT THE PLAN OWES (SPEC §9/§11 — every stage refutes its predecessor by execution)

1. **Task decomposition under D-87-SEQ** (ONE leg, one atomic commit; TDD held INSIDE): unit path-forms table RED -> `0004` `//edge` arms RED -> the +10 production edit -> GREEN both layers -> docs (ADR-0309 §Decision/§Consequences completion plan; the `BEHAVIOR_CONTRACT.md ## HTTP/2` origin-form parity rule TEXT drafted — the section heading is at `:2019`, the edit LANDS at the IMPL riding ADR-0309 per ADR-0052; ROADMAP row-87 flip `numstat 1 1`) -> gates LAST (differential 121 full suite with `INNER_EXIT`, anchored panic gate, h2spec 95-case from the IMPL's own run, `go test ./...`, `-race` on the h2 package, gofmt + golangci-lint misspell-US, every count with its NC).
2. **RED anchors RE-PROVEN BY EXECUTION at the PLAN tip with failure lines read** — the unit-table REDs (`//foo` -> `Path=""`, `//foo/bar` -> `Path="/bar"` against `url.Parse`) and the differential REDs (`//edge` -> 404 drive error; `//edge/health` -> `OK\n` mis-route body). The PLAN's probes are cheap (tip binary + curl/raw client; no docker needed — the reference record is banked in SPEC §4).
3. **Freeze the error-string constraint set:** `bad :path` REUSED for the two `ParseRequestURI` rejects (the wrapping `&Error{...Underlying: err}` carries `invalid URI for request` — decide and pin the exact assertion form); `fragment in :path` the ONLY NEW string; ZERO landed reject strings change.
4. **Budget per task ON TOP of the SPEC floor** (production ~10-16 measured; test ~130-225 enumerated — the top end is a FLOOR per the ten consecutive `reference_measured_prototype_is_a_lower_bound` firings; overrun beyond is a RECORDABLE finding for ADR-0309 §Consequences).
5. **The `0004` extension recipe pinned:** both YAML route tables + the rendered-bootstrap path (`renderBootstrap`/`SubjectConfig`), the drive-loop APPENDED (existing 27-request transcript prefix stays byte-identical), `expectations.yaml`/README/doc.go prose updates enumerated.

### ⚠️ CARRIED META-LESSONS (they apply to the PLAN's method)

1. **Every budget is a FLOOR** — ten consecutive `reference_measured_prototype_is_a_lower_bound` firings, cause UNDER-ENUMERATION every time. The SPEC enumerated per-file; the PLAN enumerates per-TASK and names what it cannot enumerate.
2. **CITE BY STRING/SYMBOL, NEVER BY LINE** — the `buildRequest` parse site drifted `:440` -> `:465` across the lineage; the 86 lineage minted TWO stale-locator defects inside its OWN branch. The contract section heading `## HTTP/2` is at `:2019` at THIS tip — re-derive at yours.
3. **A reproduced defect on one path is not proof of the others** — H1/H3 verified correct by separate code paths (H1 measured; H3 by code read `internal/filter/hcm/h3dispatch.go`); do NOT touch them at the IMPL.

### ⚠️ COUNTS AT THIS TIP — RE-DERIVE MECHANICALLY, NEVER COPY

- **differential fixtures 121** — numeric tail `0119-grpc-unary-trailers`, next-free `0120` (stays FREE — D-87-DIFF extends `0004`). H2 routing fixtures: `0004-h2-routing`, `0079-h2-multiplex-pool`, `0080-h2-goaway-rotation`.
- **`ROADMAP.md` 237 lines / 119 data rows** (row 87 `in-progress` at `:149`); check-(2) anchors `:197 :203 :209 :219 :225 :233`; `-family row` **95/67**; `gRPC-family row` **2**; `Operational-tooling-family row` **3**; ARM-A flags **{119, 131}** (escape-aware command ONLY).
- **`DECISIONS.md` 18114** lines · **308** headings · tail **ADR-0309 PROPOSED** (§Context only) · next-free **ADR-0310** (TAIL-derived) · **strict `PROPOSED` guard 1 — ARMED, do NOT disarm at the PLAN** · STATUS census **22** · `^---$` **216**.
- **`BEHAVIOR_CONTRACT.md` 5957** (BYTE-UNTOUCHED until the IMPL) · **`STATE.md` 64** · **`STATE_HISTORY.md` 486** · **`BOOTSTRAP_PROMPT.md` 522** · phase dirs **128**, `REVIEW.md` **37** (standing departure, named not claimed).
- **h2spec: cite `95 tests, 94 passed, 1 skipped, 0 failed` ONLY from your own tip's run** (skip 6.9.2/2 invariant).
- **fuzzers 55 / 48 files** · **BackendKind tail 38** over 39 declarations (do NOT "fix") · stat call sites: assert DELTAS only (state which counting form you ran; the 86/87 lineage measured `NewCounter(|NewGauge(` at 145/21).
- **phase-87 surface (re-derive at your tip):** defect site `buildRequest` in `internal/filter/hcm/h2/stream.go` (the `url.Parse(path)` call — ONE non-test site in `internal/filter/hcm/`, re-verified at the SPEC); unit tests `internal/filter/hcm/h2/stream_test.go` (the existing `buildRequest` rejection table is the idiom anchor); differential `test/fixtures/0004-h2-routing/` (`envoy.yaml`, `envoy-go.yaml`, `driver/driver.go` `drive()`, `expectations.yaml`) + `test/helpers/h2.go` (`H2RoundTrip` — NOT edited, it already delivers literal paths). H1 (`net/http`) and H3 (`internal/filter/hcm/h3dispatch.go`) are correct — do NOT touch.
- ⚠️ **STILL CONTESTED, NO NUMBER CARRIED:** the `STATE_HISTORY.md` archive-gap total · the loose `PROPOSED` matcher's whole-file count · the stat-surface absolute (1205 vs 1207 — WIDENED; only DELTAs asserted) · the zero-test `generic/*` suite count (13 vs 14).

### ⚠️ DOCUMENTARY DEFECTS — carried forward, still deliberately NOT fixed

**`ADR-0051` §2's false gate-scope sentence** corrected on the record by ADR-0307. **`ADR-0058`** cites `routerActionH2.doH2` at a dead location. **`ROADMAP.md` rows at lines 119/131 malformed** (ARM-A GUARD, not a fix). **`STATE.md` §Project counts FROZEN at phase 76.** **`harness_test.go:208` port inventory STALE.** **`body.go` nolint INERT** · **xDS cycle guard NOT AUTOMATED** · **`wasm/doc.go:219` two errors** · **ROADMAP cites `esalaine` FIVE times** · **`rbac.go:50` token `F2`** · **root `PROGRESS.md`** stray phase-32.1 doc · **SPEC-86/PLAN-86 prose cites the grpcclient Close pins at a nonexistent `internal/xds/xdsgrpc/...` path** (ADR-0308 §Decision (b) is the correction on the record).

### Candidates ledger (FUTURE picks; re-derive EVERY cost at your tip)

- **The CONTINUATION two-sided repair** — strongest KNOWN product defect (server discard at `internal/filter/hcm/h2/conn.go`'s `dispatchFrame` `*http2.ContinuationFrame` arm — cite by symbol + client blindness + the client-side SETTINGS gap in `h2/client.go`, all named in ADR-0307). h2spec MEASURED not to gate either side. Est. 2-4x row 85 (+1046 realized) — the natural next LARGE row; charter as a SPLIT phase.
- **The stat-surface recount** (1205-vs-1207 WIDENED; rides a future +0 row) · **`ssl.connection_error`** (+444 whole-`.go` floor) · **`test/conformance/grpc/`** (9/26 reachable) · **REVIEW.md restoration** (process-not-product) · **hygiene fold-ins** (`harness_test.go:208`, xDS cycle-guard) · **the D-86-CONN `client.Close` gate** (UNGATED invariant, ~10 lines, fold-in) · the **~42 candidates in the six family windows**.

### ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + BOTH DOCTORING NCs YOURSELF** — record ACTUAL output. A PLAN does NOT edit `ROADMAP.md` (ONE side; row 87 stays `in-progress`, `want` stays 119, check (1) prints `NOT DONE: row 87`); verify byte-untouched by empty diff against master.
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** The SPEC's RED anchors and measured decisions must be RE-PROVEN at the PLAN tip, not quoted (the PLAN-86 precedent: re-prove the REDs with failure lines read; no SPEC claim moved).
3. **LINE ANCHORS DRIFT; SYMBOL ANCHORS DO NOT — RE-VERIFY EVERY CITE AT YOUR TIP.**
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Still live: SDS dial-budget (TWO packages — **now with the NAMED member `TestSDSEndToEnd_FetchFailure_BootFailsClosed`, identity captured at the 87 SPEC**) · `internal/cluster` `-race` outlier · `internal/httpclient` zero-value · driver-receiver port race (roster 42; `0081` binds `0.0.0.0:42039`) · the two 84.2-era flakes (cleared, unclassed) · `TestServerConn_TinyWindowDelivery` (85-IMPL, cleared, unclassed) · the REFERENCE h2spec section-8 flip (reference-side only).
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only).
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`.
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL.** **A harness's exit code is not the command's.** **`gofmt -l` never exits non-zero — gate on OUTPUT.**
8. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-87-*` or stage worktree should outlive its stage close** — expect only `master` plus your own (verify with `git worktree list`).
9. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** (a)/(b) differential 121 fixtures, full suite with `INNER_EXIT`; (c) h2spec cite only from your own run; grpc-conformance deferred in writing; proxy-wasm 10/16; (d) fuzzers 55/48; (e) `INNER_EXIT` on every differential launch AND `go test ./...`; (f) no REVIEW.md — standing departure.

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 87 (h2-double-slash-path-routing)' --format='%h %s'  # 2 at this tip: BRAINSTORM + SPEC (the OPEN phase); becomes 3 after your PLAN
git log --grep '^phase 86 (validate-sds-nil-provider)' --format='%h %s'    # 4: a CLOSED phase, use it as a FIRING CONTROL
```
⚠️ **The count comments are LIVE FIGURES — VERIFY before relying.** Take the NEWEST line (`| head -1`).

Execution style: **subagent-driven per `feedback_execution_style` is the STANDING PREFERENCE**; the inline departure remains AVAILABLE for cheap-probe stages, and the 87 BRAINSTORM + SPEC both took it (inline probes; the SPEC's prototype in a DETACHED worktree, deleted with tree-clean proof) — NAME the choice either way. A PLAN's probes are cheap (tip binary + curl/raw client; no docker owed — the reference record is banked in SPEC §4). Subagents commit LOCALLY on the ONE stage branch; the controller squashes (`feedback_subagents_no_push`, `feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** and tear down docker **BY NAME**. ⚠️ **Band private ports outside BOTH `20000-31007` AND `11000-14999`, clear of the static fixture ports (10000-19172); `0081` binds `0.0.0.0:42039`.** The 87 SPEC used 47410-47411/47420-47421; the PLAN's probes band from **47430+**. ⚠️ **ONE WORKTREE PER AGENT preferred; explicit-path staging if shared; never an unscoped `git restore`.** Per-task `gofmt` + `golangci-lint` on touched packages (⚠️ misspell locale US). ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch.** ⚠️ **Docker BY NAME (`p87-*` if this stage needs any), `--rm`, verify gone.**

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root. Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — sha256 against the base blob, never by eye; work DETACHED so a failed revert cannot dirty a branch.

⚠️ **THE ARCHIVE-ABSENCE GUARD — USE THE WHOLE-FILE FIXED-STRING FORM** (`grep -cF`), NC'd with REAL targets re-derived FROM THE FILE first; a control's own count is a LIVE FIGURE: at the phase-87 SPEC close `phase 82 (wasm-http-call-response-cache) IMPL done` read **12** and `phase 83 (wasm-pause-arm-leak) PLAN done` read **10** (both UNCHANGED from the BRAINSTORM close — the first no-drift reading; still re-derive). ⚠️ **The §Recent-style bullet anchor `- **prior active-phase:**` reads 0 on ARCHIVED entries** (the archive wraps them `- **prior active-phase (evicted…):**`) — use the fixed-string form. **Verify STRICTLY APPEND-ONLY**: `git diff --numstat` = `N 0` AND base a byte-exact PREFIX (`cmp`). At the PLAN close §Current rolls SPEC-done -> PLAN-done and §Recent evicts the oldest per a DIRECT DATE READ at that tip (⚠️ expect a TWO-WAY tie at 2026-08-10 — `86 SPEC done` and `86 BRAINSTORM done` — resolved by LIST POSITION, the LAST entry, per the 86/87 precedent; CONFIRM by direct read before removal).

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads `PLAN for phase 87` — that IS your instruction.** §Project counts SELF-CONTRADICT §Current — do not anchor on them (frozen at phase 76).
2. `docs/envoy-go/phases/87-h2-double-slash-path-routing/SPEC.md` — all seven disposed questions, the D-87 decisions, the cost enumeration, §9's IMPL-leg sketch the PLAN decomposes.
3. `internal/filter/hcm/h2/stream.go` (`buildRequest` — cite by symbol) + `internal/filter/hcm/h2/stream_test.go` (the Table-B idiom the unit arms adopt) + `test/fixtures/0004-h2-routing/` (`driver/driver.go` `drive()`, both YAMLs — the D-87-DIFF extension surface).
4. `docs/envoy-go/ROADMAP.md` — row 87 at `:149` (BYTE-UNTOUCHED at the PLAN); the six family-window paragraphs (check-(2) anchors); confirm nothing narrows.

## What the PLAN owes
Ordered IMPL tasks under D-87-SEQ (ONE leg, one atomic commit, TDD inside) · RED anchors RE-PROVEN at the PLAN tip with failure lines read (unit REDs + the `0004` `//edge` differential REDs) · the contract `## HTTP/2` parity-rule TEXT drafted + the ADR-0309 completion plan (guard stays 1 at the PLAN; 1 -> 0 at the IMPL) · the error-string constraint set frozen (`bad :path` reused; `fragment in :path` the only new string) · the `0004` extension recipe pinned · budget per task on top of the SPEC floor · `ROADMAP.md` BYTE-UNTOUCHED (row 87 stays `in-progress`, `want` stays 119 — verify by empty diff) · the sentinel + all four NCs run (ONE side), recorded · STATE.md rolled IN PLACE (SPEC-done -> PLAN-done; §Recent eviction per DIRECT DATE READ — expect the 2026-08-10 two-way tie, list-position rule) · `next-prompt.txt` rolled to the phase-87 IMPL (`git add -f`) · ONE squashed commit, merged to master, pushed.
