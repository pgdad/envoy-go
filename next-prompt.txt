=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

**THERE IS BANKED MID-LIFECYCLE WORK AT THIS TIP.** Phase 83 (`wasm-pause-arm-leak`) was **BRAINSTORMED (2026-08-03)** and **SPEC'd (2026-08-03)**; **row 83 is `in-progress` and the phase is OPEN.** ⇒ **This session performs the phase-83 PLAN. Do NOT self-pick a new subject.**

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=115 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **CHECK (1) IS LOUD — IT PRINTS `NOT DONE: row 83`, BECAUSE ROW 83 IS `in-progress`.** It went silent for exactly one tip (the phase-82 IMPL). **It goes silent again only when the phase-83 IMPL flips row 83 to `done`.** ⚠️ **RUN THE DOCTORED-COPY NC ANYWAY.** It is cheap, and the moment check (1) is silent again its failure mode and its success mode become indistinguishable. The form that works (a `sed` targeting `done      ` does NOT — it misses the real `| done |` column spacing and prints nothing, which reads exactly like *"the check is blind"*):
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
```
At the phase-83 SPEC **FIVE** NCs were run and **all five fired**: row 62 doctored ⇒ `NOT DONE: row 62` (with `NC LANDED? [ in-progress ]` inspected first); `want=114` ⇒ `GATE FAIL: examined 115 data rows, expected 114`; an invented slug fires while `WASM`/`HTTP-filters` correctly do not; check (2) **one-arm** strip ⇒ **5 -> 4, NOT 5 -> 0**; both arms stripped ⇒ 0. **Run at least the row-62 and the `want` NCs. Prefer all five.**

⚠️ **`want=115` IS A LIVE FIGURE, NOT A CONSTANT.** It is the ROADMAP data-row count. **A row-adding phase MUST bump it in the same commit that adds the row.** ⚠️ **YOUR PLAN ADDS NO ROW, SO `want` STAYS 115** — a SPEC/PLAN does not touch `status`, and the IMPL updates it IN PLACE per §Schema `:18`.

Check (3) fails SAFE by construction. When you open a new family, add its row-summary slug to the list above.

**SENTINEL RE-RUN MECHANICALLY AT THE PHASE-83 SPEC, BOTH BEFORE AND AFTER ALL EDITS, WITH FIVE FIRING NEGATIVE CONTROLS. IT DOES NOT FIRE. `stop` was NOT created; `ls stop` => `No such file or directory`, and it MUST NOT be created while checks (2) and (3) are still live — which they are.** ⚠️ **Re-run it yourself anyway and record what YOU see.** ACTUAL output, recorded rather than predicted:
- **(1)** `NOT DONE: row 83` at `want=115`, denominator printed.
- **(2) FIVE — `:193 :203 :213 :219 :227`, UNCHANGED. The THIRTY-SEVENTH consecutive phase at which check (2) did not go down. STATED, not forecast.**
- **(3) `NEVER OPENED: gRPC` — ALONE.**
- Input measured at **231 lines / 115 data rows** before anything was written, so an empty result could not read as a zero result.

⚠️ **CHECK (2) CANNOT MOVE FOR A WASM ROW, AND THAT IS NOW STRUCTURAL RATHER THAN ASSERTED.** Each anchor was mapped to its owning `^### ` heading at the phase-83 SPEC: `:193` HTTP/3 + QUIC · `:203` xDS / dynamic config · `:213` Observability · `:219` Runtime + hot restart · `:227` Operational tooling. **The `### WASM host family` heading is at `:221` and owns NO check-(2) anchor.** There is nothing for a WASM row to narrow. **Do not forecast a decrease; state what your row narrows — here, nothing.**

⚠️ **THE LEAK CHECK GOES LIVE THE MOMENT YOU WRITE A ROADMAP ROW.** The rule: **NEVER WRITE A SENTINEL'S OWN MATCHER STRING INTO A FILE THE SENTINEL GREPS.** The sentinel greps `docs/envoy-go/ROADMAP.md` — writing these strings *here* (or in `SPEC.md` / `DECISIONS.md` / `STATE.md`) is safe; writing them *there* is not. ⚠️ **THIS WAS CONFIRMED BY POSITIVE CONTROL AT THE PHASE-83 SPEC, NOT ASSUMED:** the four matcher strings written into a scratch `SPEC.md` left checks (2) and (3) **UNCHANGED**, while concatenating the same four onto a `ROADMAP.md` copy moved check (2) **5 -> 7** and made check (3)'s `NEVER OPENED: gRPC` go **SILENT — a false PASS.**
⚠️ **AND THE TWO CLASSES ARE OPPOSITE.** `<slug>-family row` is check (3)'s **PASS** condition; `deferred candidates:` / `remaining deferred (not-yet-chartered) candidates:` are check (2)'s **FAIL** conditions. **One leak silences a real failure; the other manufactures one.**
⚠️ **The literal `WASM-family row` occurs TWICE in `ROADMAP.md` — at line 144 (row 82, field 7) and line 145 (row 83, field 7).** The heading is `### WASM host family` and the naive convention would produce `WASM-host-family row`, **which does NOT match** — that trap is unchanged.

⚠️ **ROW WELL-FORMEDNESS: THE GATE MUST BE A DISJUNCTION.** Re-run over all **115** rows at the phase-83 SPEC: **ARM-A (escape-aware, `pieces != 8` on a `|`-split) catches lines 119 and 131 ONLY; ARM-B (escape-aware trailing piece) catches line 140 ONLY.** ⚠️ **Naive `NF!=8` flags SEVENTEEN — `55 56 57 58 60 63 66 72 74 105 110 111 117 119 124 131 136`, i.e. 15 FALSE POSITIVES + 2 true — and MISSES line 140** (compensating defects cancel) ⇒ wrong in **both** directions. ⚠️ **BOTH ARMS WERE NC'd WITH THE DOCTORING VERIFIED TO HAVE LANDED, AND A FIRST NC ATTEMPT DID NOT LAND** (its `sub()` pattern never matched) — which would have read exactly like *"the arm is blind"*. **Inspect that your NC landed before trusting its result.**

⚠️ **A GATE-AUTHORING TRAP RECORDED RATHER THAN HIDDEN:** the phase-82 PLAN's first ARM-A used the wrong pipe-split threshold (`!=9` instead of `!=8`) and flagged **113 of 114 rows**. **A gate that fires on almost everything reads as THOROUGH and is WORTHLESS. Only the printed denominator exposed it.** Print your denominators.

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (ugrep under the hood). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Reproduced at THIRTEEN separate tips.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS** (wrapped prints bare paths, GNU prints `./`-prefixed). ⚠️ **Print FILE NAMES, never a count.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** — git exempts TRACKED files, so it exits 1 and reads as *"not ignored"*; `git check-ignore -v --no-index` prints the truth (`.gitignore:2:next-prompt.txt`), and `git status` is the discriminating check. ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** (`xargs` execs a binary; `command` is a shell keyword) — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **`grep -c` ON ZERO MATCHES IS TOOL-SPECIFIC**: only `git grep -c` prints nothing; GNU and the wrapper print `0`. Both exit 1, so an exit-code gate is wrong for either. ⚠️ **AND `grep -c` COUNTS *LINES*, NOT *OCCURRENCES***. ⚠️ **`\t` IN GNU ERE IS A LITERAL `t`** while the harness function normalizes it ⇒ the trap bites exactly where gates run and is invisible interactively. **Use `-P` unconditionally.**

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`** with FOUR blind spots (an em-dash, a DOT in `http-1.1`, two LETTER-suffixed ids). Every document from phase 75 forward called it *"currently HARMLESS."* ⚠️ **Nobody fired the unsafe arm until the phase-77 PLAN. EXECUTED on scratch copies: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** ⚠️ **DO NOT restore the regex form.**

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

⚠️ **Run the union form, never one half of it.** ⚠️ **A ONE-ARM STRIP IS NOT AN NC**: `sed 's/deferred candidates://'` moves the union **5 -> 4, not 5 -> 0**. ⚠️ **AND THIS WAS RE-CONFIRMED ACCIDENTALLY AND LIVE AT THE PHASE-83 SPEC** — a single-phrase matcher run while mapping anchors to families printed **ONE of the five**, because the long form `remaining deferred (not-yet-chartered) candidates:` does not contain the substring `deferred candidates:`. ⚠️ **The documented trap's stated MECHANISM is refuted while the trap is real** — the inflation comes from ROADMAP table rows carrying BRAINSTORM prose, not from `candidates were:` recaps. **Cite the command; do not re-inherit the explanation.**

⚠️ **CHECK (2) HAS NEVER GONE DOWN** across ~37 phases (history `0 -> 1 -> 3 -> 4 -> 5`) => **the candidates sentence is a WINDOW onto a larger deferred backlog, not an inventory of it.** ⚠️ **A stage that predicts a decrease is repeating the phase-73 error.**

⚠️ **`:213` IS NOT A SENTENCE ANCHOR.** It is a **45,959-char** line carrying SEVERAL deferred phrases; only ONE is the colon-terminated form the sentinel matches, at **character offset 14459 / BYTE offset 14608**. Citing *"the `:213` sentence"* without the offset is ambiguous. ⚠️ **And `:227` does not carry the long phrase at all** — SHORT form only, which is exactly the Operational-tooling blindness check (2) was broadened to fix.

==============================================


## STATUS AS OF 2026-08-03: PHASE 83 (`wasm-pause-arm-leak`) IS **OPEN**. ROW 83 IS `in-progress`. LIFECYCLE-STATE **2**. THIS SESSION PERFORMS THE **PLAN**.

**What the phase-83 SPEC landed.** Base master **`5d0892e4`** (from `git rev-parse master`), branch `phase-83-spec`. Docs-only: ZERO production `.go`, ZERO test `.go`. **FIVE investigation agents** on disjoint remits, each in its own DETACHED worktree with private scratch and disjoint port bands inside `43400-43799`; all five reported `git status --porcelain` = 0 lines and removed their own worktrees. `SPEC.md` NEW · `DECISIONS.md` **17858 -> 17892** (ADR-0305 §Context, STATUS **PROPOSED**) · `PROGRESS.md` +1 stage record · `STATE.md` rolled IN PLACE (63 -> 63) · `STATE_HISTORY.md` **448 -> 450** · `ROADMAP.md` and `BEHAVIOR_CONTRACT.md` **BYTE-UNTOUCHED**.

### ⚠️ THE HEADLINE: THE ROW'S DECLARED PRINCIPAL RISK MEASURES **ZERO**, BECAUSE **NO GUEST IN THIS REPOSITORY REACHES THE BROKEN ARMS AT ALL** — AND THE FIX AS CHARTERED IS **INCORRECT**

The defect is real: six production `abi.ProxyActionPause` arms, two flagged, **four return statuses that PARK the dispatch goroutine while arming nothing**. Census re-derived symbol-anchored, denominator stated (`^\s*case abi\.ProxyActionPause:` over non-test `.go` ⇒ exactly **6**), and **every BRAINSTORM line anchor is correct at this tip — no drift**: `decode_headers.go:269` **flagged** · `encode_headers.go:126` **flagged** · `body.go:226` **no** · `body.go:313` **no** · `trailers.go:142` **no** · `trailers.go:199` **no**.

⚠️ **BUT INSTRUMENTING `body.go:226` ACROSS FIVE FULL `0036` RUNS YIELDS ZERO FIRES**, with two firing positive controls. Three measured reasons: (n) trips the cap at `body.go:144` and returns at `:172`, **47 lines before dispatch at `:191` and 82 before the arm**; (a)/(b)/(c) Pause only inside `if !end_of_stream`; and ⚠️ **they are not driven at all** — `inputs/driver.go:511-513` emits `emitConstantSkipToken`, so the whole fixture produces **ONE** body chunk. ⚠️ **This REFUTES the BRAINSTORM's load-bearing *"three vendored guests already do it"* — TRUE OF SOURCE, FALSE OF EXECUTION.** ⇒ **the failing-first anchor must be SYNTHETIC and the differential is STRUCTURALLY BLIND to this row.**

⚠️ **AND THE NAIVE FIX IS WRONG.** `DecodeData` runs **once per chunk**, so `beginDecodePause` re-arms per chunk and the final-chunk `Continue` **neither clears the flag nor stops the timers**; orphans then fire on an **unparked** chain and latch a spurious token. ⚠️ **`Stop()`-before-reassign — the BRAINSTORM's ENTIRE S3 — IS INSUFFICIENT, proven by a controlled A/B:** over 300 superseded generations at a 50 µs watchdog, `Stop()` alone fires `ContinueDecoding` **299** times; `Stop()` + a generation counter fires **1**; single-pause NC exactly 1. ⚠️ **`-race` REPORTS NOTHING** — a logical race over `atomic.Bool` + `sync.Mutex`. **The gate must be a behavioral fire-count assertion.**

⚠️ **AND THE TIMER-OVERWRITE DEFECT IS REACHABLE TODAY, NOT LATENT** — the trigger is two `beginDecodePause` calls without an intervening `resumeDecode`, which the buffered-1 chain channel already permits with the two headers arms alone. The row widens reachability threefold; it does not create it.

⚠️ **A SEVENTH UNBOUNDED PARK, IN THE FILE THE ROW EDITS, THAT A CENSUS OF SIX DOES NOT COVER.** `body.go:282` — the encode-side body cap — returns `DataStopIterationNoBuffer` with **no `SendLocalReply`** (ADR-0071), the encode chain parks on exactly that status, and **there is no encode-side counterpart to the decode side's `localReplyDone` short-circuit at `chain.go:406-408`.** Its own comment is false twice: *"return StopAllIteration so the chain terminates the response early"* names a status it does not return and an outcome it does not produce. **SIX is right for GUEST Pause arms and wrong for a LEAK census.**

### ⚠️ WHAT THIS SESSION DOES — THE PLAN

Draft `PLAN.md` for phase 83, roll `STATE.md` IN PLACE, roll this file forward. **`ROADMAP.md`, `DECISIONS.md` and `BEHAVIOR_CONTRACT.md` all stay BYTE-UNTOUCHED and `want` stays 115.** ⚠️ **ADR-0305 is already drafted and STATUS `PROPOSED` — a PLAN adds NO ADR and must not touch `DECISIONS.md`.** ⚠️ **The recurrence guard `^> \*\*STATUS: PROPOSED` is currently 1 and must STAY 1** through the PLAN; the IMPL disarms it to 0. ⚠️ **Do NOT use the loose form `^> \*\*STATUS: .*PROPOSED`** — it returns **5**: **FOUR FALSE POSITIVES** (COMPLETE blocks at `:17466 :17534 :17600 :17662` that merely narrate the word) **plus ADR-0305 itself at `:17862`, which is the one TRUE positive.** The strict form returns exactly **1**, at `:17862`. ⚠️ **THE ROUTER'S OWN "returns 4, all four false positives" FIGURE WAS SELF-FALSIFIED BY ADR-0305's LANDING — a PROPOSED block matches the loose form too. This is the self-falsifying-count species firing on the very count that documents a false-positive gate.** ⚠️ **AND ADR-0305's OWN STATUS BLOCKQUOTE CARRIES THE SAME NOW-STALE "returns 4 … ALL FOUR ARE FALSE POSITIVES" SENTENCE.** The phase-83 IMPL, which already rewrites that line `PROPOSED -> COMPLETE`, **owes an in-place §Context/STATUS correction on the ADR-0297 ¶7/¶9 + ADR-0298 precedent** (four §Context paragraphs corrected in place while completing the block). **Lead with what survives: the gate IS a false-positive gate and the strict form IS the right one; only the arithmetic moved.**

**The PLAN owes FIVE things, per SPEC §17:**
1. ⚠️ **An explicit §6.1 re-evaluation against a MEASURED test side.** The SPEC's ~1070-line test figure is **analogue-scaled, not written** — the exact failure mode of phases 81 and 82. **The precedent if it crosses is RECORD, DO NOT RETRO-SPLIT.**
2. **A task ordering that lands the generation guard BEFORE or WITH S1/S2** — S1/S2 without it widen a live defect threefold. This is a CORRECTNESS constraint, not a preference.
3. **A disposition for the stale-token hazard** (D-83-STALETOKEN) — priced filter-local mitigation, or deferral BY NAME. **It must not surface at the IMPL.**
4. **A remedy choice for the encode-side cap park** (D-83-ENCODECAP) — non-parking status vs host-owned bound, **with the reference's behavior cited**. ⚠️ **NOT "arm the watchdog"** — it is a HOST-initiated park, no guest owes a resume, and the CAS model does not apply.
5. **A break roster whose arms are proven RED first** — the differential cannot go red on this row and the package's existing guard is vacuous.

**Scope as SPEC'd (NINE items, up from seven):** S1 flag+arm the two body arms **with arm-once + `Continue`-path disarm** · S2 flag+arm the two trailers arms · **S3 UPGRADED to `Stop()` + generation counter** · S4 rebuild the vacuous census guard · S5 rewrite the false comment surface (**ELEVEN sites, not three**) · S6 re-derive `defaultPauseWatchdog` · S7 de-hardcode the test port · **S8 NEW: bound the encode-side cap park** · **S9 NEW: dispose the stale-token hazard**.

**REVISED BAND: 950-1400 net `.go`, budget ~1150, 12-16 tasks** — up **~2.4-2.6x** from the chartered 350-600/~450. Production measures **28 insertions** for the prototype but **63-90 net** with the falsified 27-line `pause.go` doc block and the misattributing log strings; test side **≈1070** priced against measured sibling analogues, **≈93%** of the total — the THIRD consecutive row at which test scaffolding dominates (phase 81 **3.07x**, phase 82 **2.55x**). ⚠️ **The SPEC's own cross-check says 1135-1160 is CONSERVATIVE:** the stage's probe files total **478 lines** covering ~6 of 17 roster items ⇒ ~80 lines/item ⇒ **≈1360**. **1400 is the honest ceiling, not a pessimistic one.**

### ⚠️ THE FOUR OPEN QUESTIONS ARE ALL DISPOSED — DO NOT RE-LITIGATE THEM, BUT DO RE-DERIVE THEIR EVIDENCE

- **D-83-CAP (Q1) — ZERO RISK, BY MEASUREMENT.** `0036` with the fix: **19.25 s** vs a 19.33-20.36 s two-run baseline, inside a **1.03 s** variance, **zero watchdog fires**. A **10 s / 1 s / 250 ms / 50 ms / 1 ms** sweep passes at every value. Cross-product both ways: raising (n)'s cap 1024 -> 65536 makes the arm **FIRE** and the watchdog release at **exactly 10.0 s**; removing the fix makes the same probe park **15 s** until the driver's client gives up. ⚠️ **`0036` is the ONLY fixture repo-wide with a wasm body callback**, so a green there is evidence the row broke nothing, **NOT** evidence it works.
- **D-83-TRAILERS (Q2) — FLAG AND ARM.** Option B fails on its own terms: the current behavior is a **permanent unresumable park** (the guest's `proxy_continue_stream` CAS loses because the flag was never set), and ⚠️ **there is NO stream idle timeout anywhere in the tree** (`idle_timeout` over `internal/**/*.go` returns two *comments* only, with a firing control) — **bounded by nothing at all.** One line flips a RED probe GREEN, so an anchor exists despite zero production callers.
- **D-83-WATCHDOG (Q3) — 10 s STAYS, THE DERIVATION IS REBUILT.** ⚠️ **BOTH** stated constraints fall. The upper one was already known void; ⚠️ **the LOWER one is also unsound and no prior document says so** — `http_call.go:279-281` consults the 5 s `defaultHttpCallTimeout` **only when the guest passes 0**, the cited guest passes `Duration::from_secs(5)` explicitly, and guest timeouts are uncapped. Measured: the corpus arms the watchdog **exactly once**, for **747 µs**, released **by the guest**, against a predicted *"~10 s of added runtime"* that measures **0 s**. Real ceiling: the differential's **90 s per-fixture budget** (`test/differential/runner_test.go:237`), **not** the 15 s client timeout the comment cites.
- **D-83-LOGHOME (Q4) — ADR-0305 §Context, explicitly NOT chartered.** No priced shape has a failing-first anchor, and an ADR amendment is unavailable under ADR-0288's append-only rule.

### ⚠️ BROKEN-GATE COUNT IS **TWENTY-SIX** — ONE NEW SHAPE LANDED AT THE PHASE-83 SPEC

**THE TWENTY-SIXTH: A LIVENESS BARRIER PLACED UPSTREAM OF THE GATE IT CLAIMS TO PROVE.** `pause_test.go:73-76` and `:95-97` install the `executions` counter as the guard against cap-denied vacuity — *"a 0 means the capability gate short-circuited"*. But `decode_headers.go:199-201` increments `executions` **BEFORE** `CallProxyOnRequestHeaders`, and the capability gate lives **INSIDE** that call. Measured under zero capabilities: **`executions = 1`.** ⚠️ **IT IS GREEN ON EXACTLY THE FAILURE IT EXISTS TO CATCH — AND IT WAS INSTALLED AT PHASE-82 TASK 8 *AS THE FIX* FOR THAT SAME FAILURE** (`dispatch_test.go:645-647`). **A replacement gate inherited the defect it replaced, one row later.** ⚠️ **Use `f.streamCtx.HasGlobalFunc(...)` instead** — cap-gated at `internal/wasm/root_vm.go:906-916`, confirmed discriminating.

**Shape 25 is now PROVEN BY BREAK, and it is WIDER than recorded.** Flipping `body.go:227` to `DataContinue` leaves `TestFilter_Pause_CensusOfHonoredArms` green; flipping **ALL FOUR** frozen arms leaves the **ENTIRE PACKAGE** green at `ok 0.324s`. ⚠️ **NOTHING in `internal/filter/http/wasm/` catches all four frozen Pause dispositions being flipped to Continue.** ⚠️ **And the test contradicts itself internally, independently of the docstring** — its inline rationale at `:132-134` claims asserting the constants makes a disposition change *"a compile-or-assert failure at this site"*, which is false on its own terms. **Two false sentences fifteen lines apart in the same test.** A measured 113-line behavioral replacement goes RED on the four-arm break with **per-arm isolation** (each break trips exactly its own assertion) and GREEN on the clean tree.

The twenty-four carried forward, unchanged: a counter that proves a callback RAN cannot prove it returned the RIGHT ANSWER · a replacement gate that INHERITS the defect it replaces · a gate that FIRES ON ALMOST EVERY ROW reads as thorough and is worthless · a guard is a claim at TRANSCRIPTION time · two defects that CANCEL in the gate metric · an inert gate cell · a full-suite recipe without `-v` is VACUOUS · a sha256 roster desynced against a DELETED file · `gofmt -l` NEVER exits non-zero · `go doc -all <A> <B>` swallows arg2 · a `+0 exported symbols` gate over an EMPTY package reds on a CORRECT tree · a RANGE gate cannot detect anchor drift · a roster's naive `[ -f ] || continue` exits 0 on a DELETED file · a count-only stat guard PASSES a build with BOTH names wrong · a `-run` no-match exits 0 with `[no tests to run]` · a `--- PASS` tally over a package with sibling tests exceeds the fixture denominator · a stat-delta claim cannot be discharged by guards scoped to another package · a stderr-VOLUME assertion passes on the hang · `golangci-lint` misspell locale US · a harness's exit code is not the command's · a GOLDEN ROSTER that omits the family under test · a gate metric INVARIANT under the change it guards · a hand-written ENUM golden pinning WRONG wire values · a stat COUNT guard blind to a RENAME.

### What matters for the NEXT session, in order

1. **⚠️ RE-RUN ALL THREE SENTINEL CHECKS YOURSELF AND RECORD THE ACTUAL OUTPUT; DO NOT PREDICT IT.** Check (1) prints `NOT DONE: row 83` at `want=115` — **CORRECT and expected** while the phase is open. **Your PLAN adds no row: `want` STAYS 115 and `ROADMAP.md` stays BYTE-UNTOUCHED.**

2. **⚠️ EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION, NOT BY REVIEW.** The lineage is unbroken: phase-81 BRAINSTORM 14, SPEC 22, PLAN 14, IMPL 17; phase-82 BRAINSTORM 26, SPEC 23, PLAN 14, IMPL 17; phase-83 BRAINSTORM 21, **SPEC 23 (NINE load-bearing).** **Budget for finding more. A PLAN that refutes nothing has not looked.** ⚠️ **AND AN AGENT'S REFUTATION IS A CLAIM TOO** — at the phase-83 SPEC the controller **did not accept** an agent's correction of the guest-crate denominator (its glob required a `scripts/` path component and missed ten conformance crates; **35 is right as a repo total, 25 is the fixture-guest subset — both stand, neither corrects the other**). ⚠️ **AND THE CONTROLLER'S OWN HYPOTHESIS DID NOT SURVIVE EITHER** — it predicted zero fires and attributed them to (a)/(b)/(c) taking the `Continue` path, when the stronger fact is they are **not driven at all**. **Right conclusion, wrong mechanism, recorded as such.**

3. **⚠️ BEFORE ANY LINE-ADDING EDIT TO A HEAVILY-CITED DOC, ENUMERATE THE CITES YOUR INSERT WOULD SHIFT.** ⚠️ **AND DO NOT CARRY A WHOLE-FILE COUNT OF A PATTERN YOUR OWN PROSE SPELLS.** The `ROADMAP.md:<line>` cite count **self-falsified for the FOURTH time at this tip — inside the commit that named the species**: measured **117** at `8a0126d2`, **118** at `1a89031b`, **120** at `5d0892e4`, the last delta located to two more `ROADMAP.md:195` occurrences added by the very commit whose subject was *"…record the self-falsifying-count species for the THIRD time."* ⚠️ **AND IT FIRED A FIFTH TIME, IN THE PHASE-83 SPEC ITSELF, ON A DIFFERENT COUNT.** The SPEC wrote *"the loose form returns **4** and all four are FALSE POSITIVES"* — and then landed **ADR-0305 with STATUS `PROPOSED`**, which the loose form also matches, making it **5 (four false positives + one true)**. **A PROPOSED-status ADR is itself a loose-form hit, so any stage that drafts one invalidates that count by drafting it.** ⚠️ **This is the species' nastiest variant so far: the self-falsified number was the number DOCUMENTING A FALSE-POSITIVE GATE, so the error hid inside a warning about errors.** Corrected in this router; **ADR-0305's own STATUS blockquote still carries the stale sentence and the IMPL owes the in-place fix.**

**The lesson is not "recount harder".** The phase-83 SPEC and ADR-0305 therefore carry **NO** live whole-repo count of: `ROADMAP.md:<line>` cites · `BEHAVIOR_CONTRACT.md:<line>` cites · `dispatchHttpCallResponse` carriers (**classified enumeration instead**) · `allCallbacksNoOp` occurrences · `WASM-family row` occurrences. ⚠️ **Max cited line in `BEHAVIOR_CONTRACT.md` is 5078 against a 5900-line file, so a TAIL append shifts ZERO.**

4. **⚠️ LINE ANCHORS DRIFT; PHRASE AND SYMBOL ANCHORS DID NOT.** ⚠️ **`*RootVM.dispatchHttpCallResponse` DOES NOT EXIST** — the symbol is `(*RootVM).handleHttpCallResponse` (`internal/wasm/http_call.go:365`), and the wrong name rides **three landed `.go` code comments** (`abi_callbacks.go:1101`, `stream_context.go:249`, `:254`) plus two phase-25.2 documents **plus the phase-83 BRAINSTORM and this router** — which is exactly why the raw occurrence count must never be carried. ⚠️ `headerMapForType` is a METHOD with an `*abiCallbacks` receiver — grep the SYMBOL, not `func headerMapForType`. **RE-VERIFY EVERY `.go` AND `.md` LINE CITE BEFORE RELYING ON IT.**

5. **⚠️ THE GATES, WITH MEASURED BASELINES FROM THE PHASE-82 IMPL (a docs-only stage does NOT re-run them and must not claim them).** Full 120-fixture differential **120/120 in 388.961 s** · **`0036` alone 18.87 s** (⚠️ **the phase-83 SPEC measured 19.33-20.36 s on the subtest with ~1.03 s variance — use the ENVELOPE, not the point**) · h2spec **53 tests, 53 passed, 0 skipped, 0 failed** — ⚠️ **ONE report, NO reference arm; the phase-82 PLAN's "106 passed" DOES NOT REPRODUCE. State your own denominator.** · `go test ./cmd/envoy-go/` **8.974 s** · touched packages ~1 s. `-race` is a SECOND run. ⚠️ **CI runs the differential at `-timeout 20m`, job `timeout-minutes: 30`** (`.github/workflows/ci.yml:60,63`, re-verified), **`go-version: '1.23'` at THREE sites** (`:14 :39 :92`), **`golangci-lint-action@v6.5.2` with `version: v1.64.8`** (`:21-23`), and the **per-fixture budget is 90 s** (`test/differential/runner_test.go:237`).
    ```sh
    ( go test ./test/differential/ -count=1 -v > "$SCRATCH/full.log" 2>&1; echo "INNER_EXIT=$?" )
    grep -cE '^    --- PASS: TestDifferential/' "$SCRATCH/full.log"          # want 120
    grep -E  '^    --- (FAIL|SKIP): TestDifferential/' "$SCRATCH/full.log"   # want EMPTY
    grep -c  'no driver registered for fixture' "$SCRATCH/full.log"          # want 0
    grep -cE '^panic:|DATA RACE|SIGSEGV' "$SCRATCH/full.log"                 # want 0
    grep -o  'TestDifferential/[^ ]*' "$SCRATCH/full.log" | sed 's|TestDifferential/||' | sort -u \
      | comm -3 - <(ls -1 test/fixtures/ | grep -E '^[0-9]{4}[a-z]?-' | sort)  # want EMPTY
    ```
    ⚠️ The faithful dir predicate is `^[0-9]{4}[a-z]?-` — a bare `^[0-9]{4}-` gives **118** (it drops `0007a-cors` and `0007b-iteration-probe`). ⚠️ **`ls -d test/fixtures/[0-9]*/ | wc -l` and the faithful predicate BOTH give 120** — they do not diverge; the divergence is faithful-vs-bare.
    ⚠️ **`go test ./test/differential/...` (with `...`) matches TWO packages and BUFFERS `-v` output.** A run that looks hung with an empty log is normal — do not kill it.
    ⚠️ **Subtest `--- PASS` lines flush only when the PARENT test completes**, so a mid-run `grep -c '--- PASS'` legitimately reads **0**. Watch `=== RUN` instead.
    ⚠️ **THE RUN ABORTS, IT DOES NOT MERELY FAIL. CAPTURE `INNER_EXIT` AND THE PANIC ARM.** Budget ~3 launches.
    ⚠️ **`reference_differential_log_lacks_subject_stderr` IS REFUTED FOR THIS HARNESS** — `harness.go:258` sets `cmd.Stderr = os.Stderr` and a SECOND site at `:615` uses `io.MultiWriter`, so BOTH launch paths tee stderr. **Still true:** fixture `StatsAsserter` counter values print only inside `t.Errorf`, so a **green** run shows none of them.

6. **⚠️ THE SIX-GATE HAS A STANDING LINEAGE DEPARTURE — NAME IT, DO NOT CLAIM COMPLIANCE.** Gate **(f)** expects a `REVIEW.md`; **none since 25.3 — 87 of 124 phase dirs carry none** (37 carry one). Gate **(d)** is **VACUOUS for a row that adds none** — say "vacuous", not "green" (**55** fuzzers; ⚠️ **scoped to `-- '*.go'`, unrestricted gives 161**). Gate **(c)**: proxy-wasm is INHERITED unless you run it — ⚠️ **and when you do, STATE THE DENOMINATOR: 10 of the cpp-host's 16 files (62.5%), 6 deferred.** ⚠️ **A DOCS-ONLY PLAN OWES (a)-(f) ONLY AS A POSTURE STATEMENT, NOT AS A GREEN.** Normative cites re-verified at the phase-83 SPEC: **§6.1 `:285`**, `:289` ~25-task, `:290` ~1500-LoC, **`:291` BLANK**, `:292` third trigger; **§6.2 `:294`**; **§7.5 `:357`, gates `:360-365`, close `:367`**. ⚠️ **`/BOOTSTRAP_PROMPT.md` WITH A LEADING SLASH RESOLVES NOWHERE** — the file is `<repo>/BOOTSTRAP_PROMPT.md` (**522** lines). ⚠️ **`docs/superpowers/plans/` CONTAINS NO FILE OF THAT NAME** — the 1024-line copy is `2026-04-21-envoy-go-bootstrap-prompt.md`, and its offsets are **NOT a constant shift** (§6.1 Δ+197, §7.5 **Δ+228**). **Open the repo-root file.**

7. **⚠️ ADR + DOCUMENT CONVENTIONS.** **The DECISIONS tail is `ADR-0305`, STATUS `PROPOSED`, §Context-only (1 `### Context`, 0 `### Decision`, 0 `### Consequences`, 13 `**§Context ¶N` paragraphs). Next-free is `ADR-0306`. A PLAN ADDS NO ADR — leave `DECISIONS.md` BYTE-UNTOUCHED.** **304** `^## ADR-` headings, ids **0001-0305**, ⚠️ **exactly one gap at ADR-0209** — ⚠️ **the arithmetic is CONSISTENT *because of* the gap, so "next-free = headings + 1" gives ADR-0305 and COLLIDES. DO NOT "FIX" IT.** `DECISIONS.md` **17892** lines. STATUS census **18** (matcher `^> \*\*STATUS: `, a BLOCKQUOTE; ⚠️ `^\*\*STATUS:\*\*` matches NOTHING and reads as a false zero). Retained italic footers **11**. ⚠️ **`^---$` is 216 and must stay 216** — the last is at `:17020` trailing ADR-0288, so ADR-0289..0305, **seventeen** blocks, carry none. ⚠️ **CARRY NO WHOLE-FILE GREP COUNT IN AN ADR.** ⚠️ **ADR-0044 does NOT contain the §Context-at-SPEC discipline** — re-verified by token scan at the phase-83 SPEC (`IMPL` 0, `append` 0, `in place` 0, `§Context` 0, `§Decision` 0, `drafted` 0 over `:1419-1462`, NCs `HTTP/1.1` 6 / `allow-list` 5 firing); write **ADR-0044-as-used**. ⚠️ **NEW AT PHASE 83: `ADR-0106` DOES NOT CONTAIN A SOLE-LEG RULE EITHER** — `sole` **0**, `leg` **0**, `six-gate` **0** over the correctly-bounded block `:4788-4857` (input measured **70 lines / 1302 words**, NCs `filter` 32 / `family` 42 firing). Its subject is *"§9 HTTP filters family expansion shape"*, and it is **scoped to §9 HTTP filters**, so rows 74-81 cited it out of family while a WASM row is legitimately in scope. **Write ADR-0106-as-used.** ⚠️ **AND ADR-0304's OWN STATUS IS WRONG AT THE COMMIT THAT WROTE IT** — it asserts *"302 headings spanning ids 0001-0303"* while `71fc86d7` measures **303 / 0001-0304**; the block's own heading falsified it as it landed. **Recorded, not fixed — `DECISIONS.md` is append-only.**
   ⚠️ **`DECISIONS.md` HEADINGS ARE NOT IN NUMERIC ORDER.** `ADR-0107` is at `:4304`, `ADR-0105` at `:4582`, `ADR-0106` at `:4788`. **A range extraction of the form `awk '/^## ADR-NNNN/,/^## ADR-NNNN+1/'` SILENTLY RUNS TO EOF** — it did at the phase-83 SPEC, returning 13,071 lines, and **only the NC exposed it** (a `filter` count of 3776 for a 70-line block). **Bound the block by "the next `^## ADR-` heading of any id", and print the denominator.**

8. **⚠️ A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX — the THIRTIETH consecutive stage at which this held.** The index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. **Still live:** `reference_sds_init_fetch_timeout_dial_budget_flake` (**TWO packages**) · the pre-existing `internal/cluster` `-race` outlier (`TestOutlierDetector_ConcurrentEjectExactlyOnce`) · `internal/httpclient TestOptions_ZeroValue_NoOpDefaults` · `internal/filter/http/wasm` hardcodes port **42552**. ⚠️ **THE 42552 COLLISION FIRED LIVE AT THE PHASE-83 SPEC** — one agent's package run failed with `bind: address already in use` because a sibling agent held it, discriminated by re-running the identical build twice (`ok 0.323s`, `ok 0.326s`). **That is the S7 justification, OBSERVED rather than argued.** ⚠️ **The TIME-WAIT half of the claim remains REFUTED** — three consecutive `-count=1` runs passed with TIME-WAIT accumulating 1→2→3, because `net.Listen` sets `SO_REUSEADDR`. **Only a LIVE listener collides.** ⚠️ **S7's measured diffstat is 9 ins / 26 del, net −17** (minimal) or 20/30/net −10 (documented) — **the BRAINSTORM's 9/23/net −14 is WRONG and UNDERSTATES the reduction. Pick a variant and state it.**

9. **⚠️ CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE — and NEVER tear down a container this session did not create**, by name or otherwise. The phase-83 SPEC's five agents created **no named containers**; the reference Envoy containers are testcontainers-managed and self-terminated in every log. **No image/ancestor filter and no `prune` was used at any point.** ⚠️ **A trap worth carrying: `kill` on a `go run` parent leaves the COMPILED CHILD holding the socket.**
    ⚠️ **MACHINE-STATE CHANGES DELIBERATELY LEFT IN PLACE (from phase 82): `rustup toolchain 1.94.0-x86_64-unknown-linux-gnu` + its `wasm32-wasip1` target**, and a `1.96.0` toolchain from a probe. `rustup default` unchanged (`stable`). ⚠️ **The pinned toolchain is NOT on phase 83's critical path** — **no new Rust blob is needed.** ⚠️ **BUT THE BRAINSTORM'S SIGNATURE GENERALIZATION DOES NOT EXTEND TO S2:** `CallProxyOnRequestBody` is `(i32,i32,i32)->i32` like headers (`stream_context.go:180-184` vs `:205-209`, confirmed), but **`CallProxyOnRequestTrailers` takes TWO args** (`:227-231`, `(i32,i32)->i32`), so **S2 needs a NEW type-section entry** — the existing `fixTypeSection` type 1 does not cover it.

10. **Counts at this tip (re-run MECHANICALLY in your worktree; never copy them).** ⚠️ Use GNU `command grep` or `git grep` repo-wide — but ⚠️ **NOT `command grep` inside `xargs`.**
    fixtures **120** (next-free **0119**; naive `^[0-9]{4}-` gives **118**) / fuzzers **55** (⚠️ `-- '*.go'`-scoped; unrestricted 161) / internal packages **73** (⚠️ **`ls -d internal/*/` gives 29 — TOP-LEVEL DIRECTORIES, not packages**; cross-checked three ways, incl. `go list ./internal/...`) / phase dirs **124**, of which **37** carry `REVIEW.md` and **87** do not / **BackendKind tail 38** over **39** declarations (do NOT "fix" 38 to 39) / guest crates **35**, **36 blobs** — ⚠️ **the 35 splits into 25 FIXTURE guests (0034: 7, 0036: 14, 0038: 4) + 10 `test/conformance/proxy-wasm/sources/*` conformance crates, and the sourceless blob is `test/fixtures/0039-http-wasm-perroute-boot-reject/bytecode/probe.wasm` — NOT in 0036, whose sets are a clean 14/14/14** / **`DECISIONS.md` 17892**, **304** headings, tail **ADR-0305 PROPOSED**, next-free **ADR-0306**, retained footers **11**, `^---$` **216**, STATUS census **18**, strict PROPOSED guard **1** (⚠️ **loose form 5 — FOUR false positives + ADR-0305 itself; see the warning above**) / **`ROADMAP.md` 231 lines / 115 data rows** (⚠️ **"a numeric-id regex gives 113" is REGEX-DEPENDENT** — `^\| *[0-9]+(\.[0-9]+)? *\|` gives 113, the plainest `^\| *[0-9]+ *\|` gives **84**; **state which regex**) / **`BEHAVIOR_CONTRACT.md` 5900** (⚠️ **max cited line anywhere is 5078**) / **`STATE.md` 63** / **`STATE_HISTORY.md` 450** — ⚠️ **entries are 176 TOTAL bullet-anchored, of which 175 are `prior active-phase` and one is a head `active-phase` at `:23`; SAY WHICH DENOMINATOR.** ⚠️ **175/176 are the POST-append figures measured at `b4e3be14`; the 174/175 pair a prior document carries was measured PRE-append and is not a correction of these — the phase-83 SPEC evicted one entry, so BOTH halves moved +1.**
    ⚠️ **THE "NEXT-FREE REFERENCE PORT" CONTEST IS DISSOLVED, NOT RESOLVED.** Measured occupancy is **39 distinct ports in FAMILY BANDS** — `10000-10001`, `10012-10013`, `10131-10140`, `10443-10447`, then `12345 / 15000-15011 / 15104 / 18000-18002 / 18007 / 19000 / 19999`. **NEITHER `10119` NOR `10450` is bound**, so both prior figures are "free" and the choice is BAND-DRIVEN (`reference_differential_fixture_port_convention`).
    ⚠️ **STILL CONTESTED, SO NO NUMBER IS CARRIED:** the `STATE_HISTORY.md` archive-gap total · production `stats.IsValidName` GUARD SITES **25** vs a naive line-grep's **56** · the stat-surface total **1205**, which is DOC-SOURCED with a known-discontinuous chain (**only the DELTA is ever asserted**).
    ⚠️ **DO NOT SOURCE ANY COUNT FROM `STATE.md` §Project.** It is **TEN PHASES STALE** (self-declared *"at this phase-76 IMPL close"* while presented as *"(Live…)"*). **Anchor on §Current. Do NOT "fix" §Project.**

11. **⚠️ DOCUMENTARY DEFECTS — carried forward, still deliberately NOT fixed.**
    - ⚠️ **THE S5 SURFACE IS ELEVEN SITES, NOT THREE.** Beyond `pause.go:3-29`, `:65-70` and `decode_headers.go:256`, the phase-83 SPEC swept up: **`abi_callbacks.go:854-856`** (claims `ContinueStream` resumes after `DataStopIterationAndBuffer` — **precisely the arm that sets no flag, so it resumes nothing**; load-bearing) · **`body.go:222-224`** (claims the chain accumulates further chunks the guest then sees, when `chain.go:412-431` **parks**) · **`decode_headers.go:36-39`** (the file header, **stale since phase 82** updated the inline comment and left it) · `pause.go:54-55`, `:87-89`, `:112-117` · `wasm.go:230-232` · `abi_callbacks.go:705-706/711-712` (soft). ⚠️ **`pause.go:112-117`'s *"zero of the 35 guest crates return Action::Pause from proxy_on_response_headers"* is VERIFIED TRUE — do NOT "fix" it.**
    - ⚠️ **`pause.go:19-21`'s stated `0036` (n) dependency is INVERTED** — (n) depends on the decode-body arm **never being reached**, not on it *"pausing indefinitely by design"*. **It is the entire stated basis for freezing `body.go`/`trailers.go`, and it is false.** **NEW at phase 83.**
    - ⚠️ **The watchdog log strings MISATTRIBUTE the callback** — `pause.go:103-104` and `:128-129` hard-code *"from proxy_on_request_headers"*, captured verbatim for a **body** pause with the fix in place. Operator-facing. **NEW at phase 83.**
    - ⚠️ **`testLifecycleCapabilities` (`dispatch_test.go:113-125`) has BOTH body capabilities and NEITHER trailers capability** ⇒ any S2 test using `newTestCompiledConfigWithCaps` without `extraCaps` is **cap-denied and SILENTLY GREEN**. **NEW at phase 83.**
    - ⚠️ **`trailers.go`'s two Pause arms had ZERO test coverage of any kind** — all five tests in `trailers_test.go` (`:50 :78 :95 :117 :140`) exercise only the `nil streamCtx` / `nil cfg` pass-through. **NEW at phase 83.**
    - ⚠️ **A guest that Pauses from a trailers callback is BLIND** — `abi_callbacks.go:223-225` sends map types 1/3/4/5 to `default: return nil, false, false`; measured `type=0 ok=true`, `type=1 ok=false`, `type=3 ok=false`. S2 makes the park bounded, **not useful**. **NEW at phase 83.**
    - ⚠️ **`ContinueStream` CANNOT REPORT A LOST RESUME** — `abi_callbacks.go:887-904` discards `resumeDecode`'s boolean and returns `WasmResultOk` unconditionally, deliberately per `:880-882`; the guest is not re-entered after a Pause and no property or stat is exposed. **Indistinguishable between "redundant resume" and "resume dropped on the floor".**
    - ⚠️ **FOUR false non-test comments about the trailers seam, OUT of the wasm package and therefore OUT of S5's scope:** `hcm/connection.go:565` and `hcm/h2dispatch.go:503` both say *"the FilterChain does not yet expose a RunDecodeTrailers method (Task 18 will add it)"* — it has existed since Task 19 at `chain.go:455`; `router/router.go:284-285` claims a call that never happens; `connection.go:300` carries a softer fifth. ⚠️ **Do NOT fold these into a wasm row — `router.go:284-285` papers over the fact that `Run*Trailers` is DEAD production code.**
    - ⚠️ **`pause_test.go:435` and `:436` are BOTH `RunDecodeTrailers`**, not one of each, and they drive a synthetic `parkingProbeFilter`, not the wasm filter.
    - ⚠️ **`internal/wasm/doc.go:219` carries TWO errors** — *"ABICallbacks 13->20 methods"* against an AST-counted **21**, **and** it names `abi_callbacks.go` when the interface is declared at **`internal/wasm/registration.go:130`**.
    - ⚠️ **`internal/filter/http/wasm/root_abi_callbacks.go:43` names a type `allCallbacksNoOp` that DOES NOT EXIST.**
    - ⚠️ **`ROADMAP.md` cites `github.com/esalaine/envoy-go/…` FIVE times** (module is `github.com/pgdad/envoy-go`, `go.mod:1`; root cause ADR-0006 at `DECISIONS.md:142`). ⚠️ **`esalaine` occurs in ZERO `.go` files** — purely documentary.
    - ⚠️ **The header-map enum values are STILL WRONG in the append-only phase-25.1 SPEC record** (`docs/envoy-go/phases/25.1-.../SPEC.md:481-484`).
    - ⚠️ **All three cpp-host cites in `http_call.go`'s header comment are WRONG for v1.37.2**; **cancel-at-destruction is a DEPARTURE documented as PARITY**; **envoy-go passes the wrong `plugin_context_id`**; **there is NO cap on the http-call response body**.
    - ⚠️ **`internal/filter/http/wasm/trailers.go` is internally contradictory in three tenses** (`:10-12` Task 16, `:90-95` Task 16, `:100` Task 18) and ⚠️ **its documented `requestTrailers`/`responseTrailers` capture fields DO NOT EXIST** — the string occurs only in the comment, and `_ = trailers` at `:100`/`:165` discards the argument outright.
    - ⚠️ **`ROADMAP.md` row 82's summary carries TWO wrong-on-arrival claims** — INVARIANT-BLOCKED by §Schema `:18` ⇒ **recorded, not fixed**. Row 82's field 7 is **1998 chars UNTRIMMED / 1996 TRIMMED** — ⚠️ **the lineage figure 1998 is the UNTRIMMED convention; do not "correct" it.**
    - ⚠️ **`BEHAVIOR_CONTRACT.md:1967`** asserts a blocker its own successor phase KILLED (`ssl.connection_error`). **`DECISIONS.md` ADR-0301 §Decision (b)** says *"has failed twelve top-level detectors"* — PRESENT TENSE inside immutable ADR text, wrong since row 80 landed.
    - **`ROADMAP.md` malformation: three rows lose summary content in GFM render** (lines 119, 131, 140), INVARIANT-BLOCKED => **defensible as a GUARD with the DISJUNCTION gate, not a fix**. ⚠️ **There is NO doc-invariant test anywhere in this repo.** The reusable precedent is `test/differential/harness_test.go:123-137`'s `runtime.Caller(0)` -> repoRoot idiom.
    - **`STATE.md` §Project counts self-contradicts §Current and is FROZEN at the phase-76 IMPL close.** **Anchor on §Current. Do NOT "fix" §Project.**
    - **`BEHAVIOR_CONTRACT.md`'s `wasm.` arm is called a *"NEW SN9 flattening rule"*, which COLLIDES with ADR-0118's actual SN9.**
    - ⚠️ **The two `//nolint:gosec` directives row 80 added are INERT** — `.golangci.yml` is `disable-all: true` with **9** linters (`govet errcheck staticcheck unused ineffassign gofmt goimports misspell revive`), golangci-lint **v1.64.8**. ⚠️ **`gosec`, `nolintlint`, `depguard` and `forbidigo` are NONE of them** — relevant to the trap-arm logging-convention question. `misspell` locale **US**, ⚠️ **and it FIRED live at the phase-83 SPEC on a British spelling in a prototype comment.**
    - **`golangci-lint-action@v6.5.2` emits a Node-20 deprecation warning in CI.** Deliberately NOT bumped: v7+ requires golangci-lint **v2** while the repo pins **v1.64.8**. **A warning, not a failure.**
    - ⚠️ **`ROADMAP.md` §Schema `:18` says *"Sub-phases get their own rows"* while practice puts split legs in the parent's `sub-phases` PROSE.** **ZERO dotted rows exist above 32.2.** **Record the tension; do not relitigate.**
    - **Production-DEAD trailer code** in four filters · **`DECISIONS.md`'s wasm module-function roster** enumerates 24 keys, **8 of which do not exist in `internal/`** · **THREE live `freeTCPPort` definitions** (`cmd/envoy-go/main_test.go:180`, `test/conformance/h2spec/h2spec_test.go:219`, `test/differential/harness_test.go:246`+`:292`), and ⚠️ **none is the right idiom for an IN-PROCESS bind** — use `net.Listen("tcp","127.0.0.1:0")`, which has no race window at all. **Do NOT copy `freeTCPPort` for S7.**
    - ⚠️ **`internal/filter/network/rbac/rbac.go:50` contains the token `F2`** as an unrelated **phase-26 fork label**. **Do not use `F2` as a grep anchor in that package.**

12. **⚠️ HAZARDS THE PLAN INHERITS FROM THE SPEC, NAMED SO THEY CANNOT SURFACE AT THE IMPL.**
    - **The failing-first anchor is SYNTHETIC and must be proven RED before the fix.** No fixture and no vendored guest can produce one. `reference_liveness_break_needs_failing_baseline`: a green can also mean *"did not run"*.
    - **`-race` is NOT the gate for the generation guard** — it reports nothing. Use a fire-count assertion of the 300-generation shape with a single-pause NC at exactly 1.
    - **Plugin `config.name` must be process-unique** — `-count=N>1` on a test building a compiled config fails with *"…is duplicated across PluginConfig entries"*. **And the name feeds a stat scope** guarded by `^[a-zA-Z_]([a-zA-Z0-9_.]*[a-zA-Z0-9_])?$`, so `t.Run` names containing `-` or non-ASCII are rejected at config build (`reference_dynamic_stat_name_charset_guard`, fired live).
    - **The S4 replacement must be RE-INVERTED, not merely kept** — it asserts `decodePaused`/`encodePaused` stay **false** on the frozen arms, and **S1/S2 invert that contract**. Flip its assertions in the same commit or the row ships a self-contradicting gate.
    - **The S4 replacement does NOT cover** dispatch ordering/count, the park itself (it calls filter methods directly, not through `FilterChain`), or watchdog arming. **Chain-level park assertions are a separate, separately-priced item.**
    - **`gofmt -l` NEVER exits non-zero — gate on OUTPUT.** ⚠️ **An empty lint result is not evidence the linter looked** — inject a British spelling, confirm `misspell` fires, restore byte-identically.

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — take it from `git rev-parse master`, **NOT from a SHA quoted in this file**. ⚠️ **AND THE BARE SUBJECT-LOCATE GREP IS CONTAMINATED BY THIS FILE'S OWN PROSE ABOUT IT** — `git log` greps the whole commit message and router commits discuss the phase in their BODIES, so **the sentence warning about the grep satisfies the grep.** ⚠️ **USE THE SLUG-ANCHORED FORM**, which is stage-exact:
```sh
git log --grep '^phase 83 (wasm-pause-arm-leak)' --format='%h %s'   # 2 at this tip: BRAINSTORM, SPEC
```
Measured with both controls: an invented slug returns **0**, and the completed phase-82 form returns **4**. **Take the NEWEST line (`| head -1`) and verify the count before relying on it.** Subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** (`reference_parallel_subagents_private_scratch`) and they **must tear down docker containers BY NAME, never by an `ancestor`/image filter** (`reference_parallel_agents_shared_machine_namespaces`). ⚠️ **THERE ARE TWO RESERVED PORT BANDS — band private ports outside BOTH `20000-31007` (subject blocks) AND `11000-14999` (backends), and also clear of the static fixture ports `10000-10447` / `15000-15011` / `15104` / `18000-18007` / `19000` / `19999`.** ⚠️ **AND NOW ALSO CLEAR OF `42552`, which `internal/filter/http/wasm` hardcodes and which COLLIDED LIVE at the phase-83 SPEC.** The `42000-43799` band has been used safely (phases 80, 81, all four phase-82 stages, and the phase-83 BRAINSTORM and SPEC). ⚠️ **If two agents share ONE worktree they must stage by EXPLICIT PATH** and **must NOT run an unscoped `git restore`**. ⚠️ **PREFER ONE WORKTREE PER AGENT OFF A COMMON BASE** — **independent streams in DETACHED worktrees cherry-pick back CLEAN when their file sets are disjoint**, while an order-constrained core chain shares ONE worktree sequentially. Per-task `gofmt` + `golangci-lint` on touched packages (`feedback_pertask_gofmt_lint`). ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch, and note `-o` precedes the package path.**

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. ⚠️ **ASSUME IT FIRES EVERY SESSION — it has been treated as live for THIRTY-FOUR consecutive sessions and it FIRED, OBSERVED, SIX TIMES at the phase-83 SPEC** (`Shell cwd was reset to /home/esa/git/envoy-go`). **Use `git -C <abs-worktree-path>` for every git command, prefer absolute paths over `cd`, put any needed `cd` FIRST in a compound command, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — `git add` ALONE WILL NOT STAGE IT. USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN.** At the phase-83 SPEC five agents wrote throwaway `.go` probes, full S1/S2/S3 prototypes, a 113-line replacement gate, instrumented differential builds and a squatter-socket harness, ran them, reverted them, and each confirmed `git status --porcelain` = 0 lines. **Every agent worked in a DETACHED worktree precisely so a failed revert could not dirty a branch — copy that.**

⚠️ **AN ARCHIVE-ABSENCE GUARD ANCHORED ON A PHASE NAME SELF-CLEARS, AND IT DID AT THIS TIP.** `STATE.md`'s `last-updated` field NAMES the entry it just evicted, so `grep 'phase 81 … IMPL done' STATE.md` returns **1** and reads as *"the eviction did not happen"*. **Anchor on the BULLET** (`^- \*\*prior active-phase:\*\* \`<name>\``), which returns **0** correctly, and NC it against an entry that IS still present.

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline — ⚠️ **anchor greps on `^- **<field>`**; all seven fields verified singleton at the phase-83 SPEC close, and §Recent verified at its five-entry cap). ⚠️ **§Project counts SELF-CONTRADICTS §Current and is TEN PHASES STALE** — do not anchor on it and do not fix it.
2. `docs/envoy-go/phases/83-wasm-pause-arm-leak/SPEC.md` — **the whole document, but especially §1.1-§1.4 (the three central findings and the revised scope table), §3-§9 (the SEVEN disposed forks), §13 (cost + the §6.1 arithmetic), §14 (named-not-fixed) and §15 (the ten hazards carried into the PLAN).**
3. `docs/envoy-go/phases/83-wasm-pause-arm-leak/BRAINSTORM.md` — ⚠️ **READ IT AS A REFUTED DOCUMENT.** Its §4.1 questions are all disposed, its S3 remedy is refuted, its blast-radius framing is qualified, and its *"three vendored guests"* claim is false of execution. Its census and line anchors are CORRECT.
4. `docs/envoy-go/ROADMAP.md` — **§Schema first** (`:18` carries BOTH the update-in-place invariant AND *"Sub-phases get their own rows"* on ONE line), then the five deferred-candidate paragraphs at `:193 :203 :213 :219 :227`, then the family headings (⚠️ **`### WASM host family` is at `:221` and owns NO check-(2) anchor**). **You add NO row; `want` STAYS 115.**
5. `BOOTSTRAP_PROMPT.md` — **at the REPO ROOT, no leading slash.** §6.1 `:285`, §6.2 `:294`, §7.5 `:357-367`. ⚠️ **Not the 1024-line copy under `docs/superpowers/plans/`.**
6. `docs/envoy-go/DECISIONS.md` — **ADR-0305** (your row's own, STATUS PROPOSED — read its 13 §Context paragraphs; **do not edit it**) · **ADR-0288** (roll-IN-PLACE and the archive rule) · **ADR-0044-as-used** · **ADR-0106-as-used**.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — especially **`reference_measured_prototype_is_a_lower_bound`** (⚠️ **fired on TWO CONSECUTIVE ROWS, and this row's SPEC already revised the band ~2.4x BEFORE any code landed**), **`reference_probe_must_discriminate`**, **`reference_gate_command_negative_control`**, **`reference_replacement_gate_inherits_defect`** (⚠️ **fired LIVE as broken-gate shape 26**), **`reference_code_comment_not_evidence`**, **`reference_a_drift_correction_is_itself_a_claim`**, **`reference_empty_output_is_not_a_zero_result`**, **`reference_vacuous_break_modes`**, **`reference_liveness_break_needs_failing_baseline`**, **`reference_sentinel_matcher_string_self_clears`**, **`reference_dynamic_stat_name_charset_guard`**, **`feedback_brief_citations_not_evidence`**, `feedback_git_worktrees`, `feedback_execution_style`, and **`reference_bash_cwd_reset_commits_to_main`**.
