=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **PHASE 95 (`tls-alpn-mismatch-fallback`) IS OPEN AT LIFECYCLE-STATE 3. ROW 95 READS `in-progress` AT `ROADMAP.md:157`.** ⇒ **THIS SESSION RUNS THE PHASE-95 IMPL** (state 3 -> DONE). The spine is `docs/envoy-go/phases/95-tls-alpn-mismatch-fallback/PLAN.md` §5 — **EIGHTEEN tasks**.

⚠️ **THE MARGIN IS TWO.** Check (1) is NON-SILENT (`NOT DONE: row 95`, one line) and check (2) reads SIX. Check (3) is SILENT. ⚠️ **DELETING THE LAST DEFERRED-CANDIDATE LINE STILL ENDS THE PROJECT. DO NOT "TIDY" ONE.** NC-A and NC-B read **TWO** lines each at this tip. **MEASURE ALL THREE CHECKS AND ALL FOUR NCs YOURSELF BEFORE DOING ANYTHING ELSE.** ⚠️ **THE IMPL FLIPS ROW 95 `in-progress` -> `done`, SO CHECK (1) GOES SILENT AND *NC SHAPES CHANGE* — AFTER THE FLIP NC-A READS **ONE** LINE, NOT TWO. NEVER INHERIT AN NC SHAPE ACROSS A ROW FLIP.**

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. Parses FIELDS instead of
#     matching a pattern, and asserts the row DENOMINATOR so a deleted row is
#     itself an error.
#     ⚠️ want IS 127 and ROADMAP.md IS 245 lines at the phase-95 IMPL close.
#     Row 95 is now `done`, so check (1) is SILENT — it was NOT silent one close ago.
awk -F'|' -v want=127 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **CHECK (3) IS SILENT, AND A SILENT CHECK IS INDISTINGUISHABLE FROM A BROKEN ONE — THE NCs ARE THE ONLY EVIDENCE.** Run all four EVERY SESSION:
```sh
# NC-A — doctor a row to `in-progress`. ⚠️ READS *ONE* LINE NOW (row 95 is `done`, so the only
#        output is the doctored row 62). It read TWO before the phase-95 flip. MEASURED at the
#        phase-95 IMPL close, both before and after the flip.
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md with want=127.

# NC-B — the DENOMINATOR, want=126 on the real file. ⚠️ READS *ONE* LINE NOW —
#        `GATE FAIL: examined 127 data rows, expected 126` alone. It read TWO before the flip
#        (`NOT DONE: row 95` preceded it). ⚠️ BOTH NC-A AND NC-B CHANGED SHAPE ACROSS THE FLIP;
#        the phase-95 router forecast this and the IMPL confirmed it by execution on both sides.

# NC-C — the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```
⚠️ **AND RUN THE CHECK-(2) POSITIVE CONTROL — AND READ THE COMMAND BEFORE TRUSTING ITS RESULT.** The longer phrase does not CONTAIN the shorter one as a substring, so a control that substitutes only one phrase reports a residual of 5 and reads like a finding. Substitute BOTH and assert the substitution count:
```sh
sed -e 's/deferred candidates:/deferred candidatesXX/g' \
    -e 's/remaining deferred (not-yet-chartered) candidates:/remaining deferred (not-yet-chartered) candidatesXX/g' \
    docs/envoy-go/ROADMAP.md > "$SCRATCH/c2pc.md"
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' "$SCRATCH/c2pc.md" | wc -l  # want 0
grep -o 'candidatesXX' "$SCRATCH/c2pc.md" | wc -l                                                                # want 6 — PROVE it landed
```

⚠️ **WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -c '-family row'` fails (GNU: `grep: amily row: No such file or directory`, **rc=2**) and the surrounding arithmetic prints `0`, **which reads exactly like "no change."** **Always pass `--` before the pattern**; with `--` occurrences read **96** and LINES read **68**. **`grep -c` counts LINES, NOT OCCURRENCES,** and **`grep -c` on zero matches PRINTS `0` *AND* EXITS 1, so `$(cmd || echo 0)` EMITS TWO ZEROS** — capture with `v=$(cmd || true)`.

⚠️ **THE MALFORMED-ROW FIGURE RECONCILES ONLY UNDER AN ESCAPE-AWARE FIELD COUNT — AND THE ESCAPE-AWARE COMMAND ITSELF CAN BE BROKEN.** Malformed rows are **ROW IDs 57** and **69** (file lines 119 and 131). **Strip `\|` BEFORE splitting.** Row 57 reads **naive NF=13 / escape-aware NF=9**; row 69 reads **10 / 10**; rows 94 and 95 read **8 / 8**; naive malformed count **17**, escape-aware **2**. ⚠️ **The correct form is `sed 's/\\|//g' F | awk -F'|' '/^\| *[0-9]/ && NF!=8'` with NO file argument to awk** — passing the file makes awk IGNORE STDIN and print the NAIVE 17 under the escape-aware label. **CONFIRMED broken-when-misused at the phase-95 PLAN.** **Any gate must state WHICH FORM it uses and reconcile against the known baseline.**

⚠️ **AND DO NOT SUBSTITUTE A "REASONABLE EQUIVALENT" FOR CHECK (1)'s AWK.** `NF>=8 && $2 ~ /^ *[0-9]+ *$/` once printed **92**, not the true denominator. **Use the form above verbatim.**

**SENTINEL RUN MECHANICALLY AT THE PHASE-95 IMPL CLOSE, BOTH BEFORE AND AFTER THE ROW-95 FLIP, AND AGAIN AFTER THE FINAL-REVIEW FIX WAVE AND THE SCOPED RE-REVIEW ADJUDICATION.** ACTUAL output, recorded not predicted — **and unlike the PLAN's run this one is NOT identical across its own edits, because the flip moved check (1) and both denominators NCs:**
(1) **SILENT** (it read `NOT DONE: row 95` before the flip) · (2) **SIX** at `:205 :211 :217 :227 :233 :241` · (3) SILENT · NC-A **ONE** (was TWO) · NC-B **ONE** (was TWO) · NC-C fired (0 residual) · NC-D **96 / 68** · check-(2) positive control **6 -> 0 with 6 substitutions asserted**.
Per-line md5 of the six windows, **trailing newline INCLUDED** (`sed -n 'Np' f | md5sum`) — **ALL SIX BYTE-IDENTICAL to the phase-95 SPEC close**:
`205 10d7807bf02d` · `211 4a92f7e62fc6` · `217 2a7eb298b9fd` · `227 242e53c6f7a3` · `233 b2680e6f4fbf` · `241 6caa1c3ce0e7`
⇒ **THE SENTINEL DOES NOT FIRE. `stop` WAS EVALUATED AND DELIBERATELY NOT CREATED** (verified absent at the git root and in every stage worktree).
⚠️ **THE md5 DIGEST IS METHOD-SENSITIVE:** the six match ONLY with the **trailing newline included**. **State the method whenever you quote the digest.**
⚠️ **THIS RUN DOES NOT CARRY FORWARD — run all three checks, all four NCs *and* the check-(2) positive control YOURSELF, at YOUR OWN tip, BEFORE and AFTER the row-95 flip.**

⚠️⚠️ **NEVER RE-SPELL A SENTINEL MATCH PHRASE INSIDE A SENTINEL WINDOW** (the phase-94 rule: the sentence asserting the sentinel could not move MOVED IT, six -> seven, inside `:226`). **THE IMPL EDITS `ROADMAP.md`, SO THIS RULE IS LIVE THIS SESSION.**

- ⚠️ **LIVE FIGURES RE-DERIVED AT THIS TIP (the phase-95 IMPL close, AFTER the fix wave and the re-review adjudication):** `ROADMAP.md` **245** lines / **127** data rows / tail row **95** (**`done`**) · `DECISIONS.md` **18942** · `^---$` **216** · `^## ADR-` **316** · bare `^## ` **324** · tail **ADR-0317** · next-free **ADR-0318** (`^## ADR-0318` reads **0**) · house `PROPOSED` **ARMED**, at `DECISIONS.md:18876` under `## ADR-0317` (verified BY LINE AND BY ADR via a BACKWARD heading search) · ADR-0231 decoy **1** at `:14866` · middle-ground `^\*\*Status:\*\*.*PROPOSED` **23** (NEVER a gate) · `BEHAVIOR_CONTRACT.md` **5989**, `:1944` **357** chars / `:1961` **736** / `:1971` **5098** · `STATE.md` **65** (rolled IN PLACE, TWICE — the fix wave and then the adjudication, each re-deriving the `PROGRESS.md` count in its OWN commit) · `STATE_HISTORY.md` **554** (552 -> 554, **+2**), strict **163 (DELTA 0, as promised)** / parenthetical **63 -> 64** / loose **226 -> 227** (**163 + 64 = 227 exactly — RE-RECONCILED at this close under the NAMED anchored-occurrence forms below; four other plausible regexes read 0, 158, 224 and 227-as-LINES, so the form is the figure**) · phase dirs **136** · fixtures **122** (tail `0120-tls-connection-error`; **`0121` is the next free index — and THIS ROW DOES NOT TAKE IT**, it EXTENDS `0120`) · fixture extractor **122 = 122**, both `comm` directions EMPTY, split **98 `driver/` + 24 `inputs/`** · fuzzers **56 targets / 48 FILES** · `go.mod` **67 REQUIRE ENTRIES** (⚠️ the `grep -cE '^\s+[a-z0-9./-]+ v[0-9]'` form reads 62) · `go list ./...` **237** (**235** after excluding the two Docker drivers) · BackendKind tail **38** (`test/differential/fixture/fixture.go:614`) · `-family row` **96 / 68** · `internal/tls/config.go` **666** lines (582 -> 629 code, -> 642 comments, -> 666 after the fix wave's and the adjudication's comment corrections) · `./internal/tls/` + `./internal/listener/` + `./internal/xds/` **RC=0, RUN=414**, anchored FAIL **0**, panic **0** · full differential **122/122 PASS, RC=0, 399.93s**, dirs=imports=executed=**122** with `comm` EMPTY both directions.
- ⚠️ **THE STAT SURFACE IS QUOTED AS A DELTA AND NEVER AS AN ABSOLUTE.** Three different absolutes are live at one tip. **Phase 95 landed +0 names — and by the ledger's OWN stated convention at `BEHAVIOR_CONTRACT.md:5130`, a `+0` row earns NO chain entry; it took none.** The ledger is at `### Stat surface`, `:5071`; its chain tail is the phase-94 entry at `:5139`. **MEASURED at the SPEC and RE-CONFIRMED at the PLAN: no ledger row.**
- ⚠️ do NOT "fix" the six in check (2) and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases; **no gate may rest on a ` + ` split** (wrong on 3 of the 6 windows).

==============================================

## STATUS AS OF 2026-09-07: PHASE 95 (`tls-alpn-mismatch-fallback`) **DONE**. Lifecycle-state **3 -> DONE**; ROADMAP row 95 flipped `in-progress` -> `done`. **THE SENTINEL WAS RE-EVALUATED MECHANICALLY AFTER THE FLIP AND DOES NOT FIRE** — check (2) reads SIX. `stop` was NOT created. **THIS SESSION SELF-PICKS AND OPENS THE NEXT SUBJECT (a BRAINSTORM).**

**What phase 95 shipped, in one paragraph.** A downstream TLS client offering ALPN values overlapping NONE of the chain's `alpn_protocols` now **COMPLETES the handshake with no protocol selected and is SERVED**, matching the pinned reference, instead of aborting with `no_application_protocol`. The mechanism is a TCP-only `crypto/tls.GetConfigForClient` installed at the **ENTRY** of `internal/tls.NewDownstreamConfig`, returning a **PER-HANDSHAKE `Clone()`** with `NextProtos = nil`. The entry anchor is load-bearing, not stylistic: a build-time snapshot at the install site carries `ClientAuth = NoClientCert` / `ClientCAs = nil` because `installPool` writes them later, and serving that clone was PROVEN by execution to turn mandatory mTLS into anonymous access — the bypassed client was not merely admitted, it was **SERVED** a full application round trip.

### ⚠️ WHAT PHASE 95 REFUTED ABOUT ITSELF — READ THIS BEFORE TRUSTING ANY OF ITS DOCUMENTS

**The IMPL's own final whole-branch review refuted the IMPL's central safety claim, and the scoped re-review of that FIX then refuted the FIX's claim.** Recorded at `PROGRESS.md` §8 and §9, corrected in place at `ADR-0317`. The first two are the same error twice.

1. 🔴 **"TCP-only, never in `NewQUICDownstreamConfig`" was TRUE OF THE SYMBOL AND FALSE OF THE PATH.** The QUIC builder's body genuinely held ZERO occurrences of the installer — but `internal/listener/manager.go` builds downstream TLS at **TWO** sites, and only the `filter_chains[]` loop branched on `kind == kindQUIC`; the `default_filter_chain` block called `NewDownstreamConfig` **unconditionally**, `quicTLSConfig()` returns `rt.defaultChain.tlsCfg` FIRST, and a listener with zero `filter_chains[]` and only a default chain is accepted. A QUIC listener therefore DID reach the installer and `negotiateALPN`'s `if quic && len(serverProtos) != 0` **RFC 9001 §8.1 rejection went VACUOUS.** Fixed by mirroring the branch that already existed; pinned by `TestBuildListenerRuntime_QUICDefaultFilterChain_*`, **run RED against the un-fixed code before the fix landed.** ⚠️ **Eighteen task reviews and every in-task grep missed it. Enumerate a symbol's CALL SITES and check each caller's own branch — the callee's body is not the path.**
2. 🔴 **The fix's own "pure config parity, no new policy" was INCOMPLETE — the same branch WIDENS acceptance.** A `kindQUIC` listener whose only chain is a `default_filter_chain` wrapping `require_client_certificate: true` plus a `trusted_ca` used to boot-REJECT and now BUILDS, with `ClientAuth = NoClientCert` / `ClientCAs = nil`, because `NewQUICDownstreamConfig` never evaluates `require_client_certificate` at all. **Nothing that previously shipped regresses** — the identical shape under `filter_chains[]` already behaved this way (the DECIDED gap at `BEHAVIOR_CONTRACT.md` item 8(b) / D-RCCF-QUIC) — but parity with a known gap INHERITS it into a shape that used to be refused. **Disclosed, not fixed**; the QUIC client-auth gap stays out of scope.
3. ⚠️ **This row FALSIFIED a `BEHAVIOR_CONTRACT.md` sentence and did not notice.** Item 11's *"(A QUIC listener's `default_filter_chain` goes through plain `NewDownstreamConfig` and IS covered by the pre-scan)"* was true when written and false the moment the fix landed. **The row whose whole subject is a false present-tense comment minted one in the contract file itself.** Corrected within-line; the SDS pre-scan model is now UNIFORM, which strengthens its "benign and arguably load-bearing" reading rather than weakening it.
4. ⚠️ **A corrected claim survived in a THIRD copy.** The fixture's six-arm "the counters are BLIND" measurement was re-scoped in `driver.go` and `README.md` but left present-tense in `expectations.yaml` — including inside **arm (vii)'s own `role:` field**, claiming the subject-side deletion leaves the fixture GREEN when arm (vii) is exactly what catches it. **Reconcile a corrected claim across its whole occurrence SET, not the sites you happened to open.**
5. ⚠️ **A citation that did not hold.** `manager_test.go` cited *"`TestBuildListenerRuntime_QUICKind`'s sibling roster"*; that test is a POSITIVE build test with no roster and no reject arm. Re-derived: the `filter_chains[]` half of the parity is pinned **NOWHERE**, so dropping that branch leaves the package green. Replaced by that statement, not by another citation.

### ⚠️ WHAT PHASE 95 MEASURED THAT THE NEXT SESSION MUST NOT RE-DERIVE

- **Gates at the published tip:** differential **122/122 PASS, RC=0, 399.93s**, anchored FAIL 0, SKIP 0, panic 0, no abort, `0120` PASS 1.76s; dirs=imports=executed=**122**, `comm` EMPTY both directions. `./internal/tls/ ./internal/listener/ ./internal/xds/` **RC=0 RUN=414** FAIL 0 panic 0. h2spec exact **95/94/1/0**. Fuzzers **56 targets / 48 FILES**. gofmt silent, `go build ./...` ok, golangci-lint clean.
- ⚠️ **GATE (b) IS A NAMED DEPARTURE AND BOTH RUNS ARE REPORTED.** The non-Docker sweep was RED on run 1 (RC=1, anchored FAIL **8**, TWO failing packages) and GREEN on run 2 (RC=0, 125 ok + 110 no-test = **235**). **A green rerun clears nothing.** One failure is the registered `internal/boot` SDS dial-budget flake; the other is **NEW — registered below.**
- **The nine guarded non-site comment literals read 1 each (OK=9 BAD=0).** ⚠️ **The controller's own brief said TEN; the file holds NINE** — `manager.go:138` is NOT a guarded site because the row appends to it. A commit subject still carries the miscount; it is corrected in `PROGRESS.md`, not by rewriting history.
- **Fixture `0120-tls-connection-error` now has SEVEN arms** (port `10126` retained, no new fixture index taken). Pins `wantConnectionError = 3`, `wantHandshake = 3`, `wantNegotiatedProtocol = "h2"`. Arm (vi) `alpn_mismatch` is **tolerant/log-only**; arm (vii) `alpn_overlap` is **STRICT** and is the ONLY observable in the fixture that can see the `alpn_protocols` key at all.

### ⚠️ THE NEXT SUBJECT — SELF-PICKED, PER THE 2026-07-12 STANDING DIRECTIVE

**Smallest defensible candidate first, and this one was MEASURED at the phase-95 close rather than imagined: `default_filter_chain` DIVERGES FROM THE `filter_chains[]` LOOP, and F1 closed only ONE member of that class.** The remaining member: the loop carries a config-parity reject for a QUIC chain with NO `transport_socket` (`manager.go`, *"quic listener requires a transport_socket (mandatory TLS)"*), the `default_filter_chain` block carries **no such arm**, and `manager.go` accepts a listener with zero `filter_chains[]` and only a default chain — so that shape **BUILDS** where the same shape under `filter_chains[]` boot-rejects. ⚠️ **NOT a safety hole**: `internal/listener/quic.go` catches the nil config at **Start** (*"quic listener has no TLS config (mandatory TLS not built)"*) before `quic.Listen`. It is a **deferred-reject / config-parity DEPARTURE** — different phase, different message. Independently measured by the controller AND by the scoped re-reviewer, who agreed.

⚠️ **WHOEVER TAKES IT MUST ENUMERATE THE WHOLE DIVERGENCE SET BETWEEN THE TWO BLOCKS FIRST, AND MUST NOT ASSUME EVERY DIVERGENCE IS A DEFECT** — the mixed-TLS cross-check is **DELIBERATELY** absent from the default chain per ADR-0080 (*"default_filter_chain has an INDEPENDENT TLS posture"*), and that one is correct as it stands. **A BRAINSTORM that reports "N divergences, all defects" has not read ADR-0080.** ⚠️ **This is a self-pick, so the BRAINSTORM must record the pick AND the rejected alternatives**, and it must justify the pick against the other banked items below rather than inheriting this paragraph's recommendation.

### Banked, NOT chartered

- ⚠️ **THE DRIVER-OWNED RECEIVER PORT RACE — the runner-up, REJECTED FOR SIZE, COST MEASURED: ~36 driver files** (30 `panic` the binary via `panic(fmt.Sprintf("driver: start …`; four `fmt.Errorf` variants degrade to one failing subtest). Idiom: `net.Listen("tcp","127.0.0.1:0")` → read → `Close()` → re-bind `0.0.0.0:%d`, inside `ip_local_port_range` `32768 60999`, loopback probe for a wildcard bind, no retry (`harness_test.go:240-244` predicts it). Two fix shapes: a banded allocator in `test/differential/fixture/` + a 2-line swap per driver, or bind `0.0.0.0:0` and read the port back from each helper's `Addr()`. ⚠️ **The memory note said FOURTEEN — corrected to ~36.** **The most defensible NEXT pick if a full differential run aborts again — and note it did NOT recur at the phase-95 IMPL: two full 122/122 runs, both RC=0, no abort.** The trigger condition has therefore NOT fired; do not open this on the strength of the note alone.
- **`NegotiatedProtocol` CROSS-SIDE COVERAGE — NARROWED, NOT CLOSED.** ⚠️ **PARTIALLY CLOSED at the phase-95 IMPL**: fixture `0120`'s arm (vii) `alpn_overlap` now pins it (`wantNegotiatedProtocol = "h2"`). It remains the **ONLY** such assertion in 122 fixtures, so the class is narrowed, not closed. Not chartered.
- `0108`'s two *"emits NO `ssl.*` stats whatsoever"* confessions (false since phase 74) · no `len(helpText)` guard + two ungated prose counts in `internal/stats/name.go` · `0118:31`'s falsified *"TLS/SDS band"* · `0061-lb-ring-hash`'s σ-margin second occurrence · 1xx interim responses · the H/1 no-`Host` divergence · the pooled-upstream-lifetime defect · the other TEN fixed `ssl.*` names and FOUR dynamic families (blocked on NAMING) · **an SDS + `alpn_protocols` fixture — NO fixture combines them (`0103`, `0108`-`0111` carry SDS and no ALPN; the five ALPN fixtures carry no SDS), recorded at ADR-0317 §Context ¶8 as ABSENT COVERAGE, deliberately not chartered.**

## ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + ALL FOUR NCs + THE CHECK-(2) POSITIVE CONTROL YOURSELF** and record ACTUAL output, **BEFORE and AFTER the row-95 flip**. ⚠️ **NC SHAPES CHANGE ACROSS A ROW FLIP — NEVER INHERIT ONE.**
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** 93 IMPL nine · 94 BRAINSTORM eleven · 94 SPEC twelve · 94 PLAN sixteen · 94 IMPL twelve · 95 BRAINSTORM nine · 95 SPEC eleven · **95 PLAN SEVENTEEN**, four load-bearing, **THREE of them negative-control cells that as specified either could never fail or would fail against CORRECT code** · **95 IMPL — the COUNT is not the point and the SOURCE is: the IMPL's own FINAL WHOLE-BRANCH REVIEW refuted the IMPL's central safety claim, and the SCOPED RE-REVIEW OF THE FIX then refuted the FIX's own claim.** ⚠️ **EXPECT TO BE REFUTED BY YOUR OWN REVIEW SEAM, NOT ONLY BY YOUR SUCCESSOR** — and note that eighteen per-task reviews passed the defect through, because each was scoped to its own diff while the hole lived in a CALLER none of them opened. ⚠️ **AND WHEN TWO AGENTS CONTRADICT EACH OTHER, FIND THE VARIABLE RATHER THAN PICKING A WINNER** — at the phase-95 PLAN two agents disagreed about which program `grep` is, and **both were right in their own shell** (see 3j).
3. **LINE ANCHORS DRIFT; SYMBOL AND LITERAL-TEXT ANCHORS DO NOT — RE-VERIFY EVERY CITE AT YOUR TIP.** The phase-95 SPEC's §0.3/§0.4 anchor corrections were **ALL CONFIRMED** by two independent readers at the PLAN: six returns `128 195 220 229 248 258`, QUIC return `:290`, `NextProtos` append `:579`, `_Aborts` `121-171`, helper doc `85-87`, `alpnMatchAny` at `chainmatch.go:293`. ⚠️ **PATHSPEC-SCOPE every symbol assertion and every `sed` — AND RANGE-SCOPE anything inside `config.go`.** ⚠️ **`internal/filter/hcm/chain.go` DOES NOT EXIST — it is `internal/filter/http/chain.go`** — but **`actions.go` IS in `hcm`.**
3a. ⚠️ **AN ANCHOR CAN BE NON-UNIQUE, AND TWO WERE.** The return statement (7 hits, incl. the forbidden QUIC one) and `t.Fatalf("subj drive: %v", err)` (`:1277` AND `:2078`). **Anchor on the ENCLOSING SYMBOL.**
3b. ⚠️ **CHARACTER-CLASS AND ESCAPE SELECTORS ARE THE FAIL-UNSAFE FAMILY.** `^[0-9]{4}-` on fixtures reads 120, dropping `0007a`/`0007b` (use `ls -d test/fixtures/*/ | wc -l`) · **a CASE-SENSITIVE GREP IS BLIND TO UPPERCASE PROSE — use `-i` when auditing comments.**
3c. ⚠️ **`feedback_brief_citations_not_evidence` APPLIES TO INCIDENTAL FIGURES — including every number in THIS file, in `PLAN.md`, in `SPEC.md`, and in every AGENT REPORT.** At the phase-95 PLAN four measurement agents ran; every headline figure was re-derived by the controller before use, and **one agent cite (`runner_test.go:1281`) was wrong**.
3d. ⚠️ **NO GATE MAY REST ON A ` + ` SPLIT.**
3e. ⚠️ **A GATE COMMAND CAN MATCH PROSE INSTEAD OF CODE.** Anchor fixture-set extraction on the blank-import LINE:
```sh
extract () { grep -oE '^[[:space:]]*_ "github\.com/pgdad/envoy-go/test/fixtures/[^/]+/(driver|inputs)"$' "$1" \
  | sed -E 's#.*/test/fixtures/##; s#/(driver|inputs)"$##' | sort; }
```
**At this tip: dirs 122 = imports 122 (`test/differential/runner_test.go`), both `comm` directions EMPTY**, layout split **98 `driver/` + 24 `inputs/`**. **NC the extractor** (rename one import in a SCRATCH COPY — it must fire in BOTH `comm` directions). ⚠️ **A COUNT-ONLY CHECK IS VACUOUS: the import count stays 122 under a rename.**
3f. ⚠️ **"AT THE RETURN" IS NOT A PLACEMENT.** Anchor installs on the ENTRY of the path that must be covered and PROVE the install with an arm that reddens without it.
3g. ⚠️ **A "CHEAP ASSERTION GAP" CAN BE A BEHAVIOUR DIVERGENCE WEARING A DISGUISE.** Measure the reference for THAT ARM before asserting any cross-side stat.
3h. ⚠️ **THE STDLIB CAN COINCIDENTALLY MATCH THE REFERENCE ON ONE SHAPE.** Go's `http11fallback` makes exactly one non-overlapping ALPN shape succeed today; a table with only that shape proves nothing — **and once the fix lands, that shape stops discriminating at all** (the callback intercepts it first).
3i. ⚠️ **A LOOSE `--grep 'phase NN.*STAGE'` MATCHES THE WRONG STAGE** — subjects containing *"SPEC's"* or *"the PLAN"* match. **Anchor stage-commit locates on `^phase NN (slug) STAGE:`.**
3j. ⚠️ **`grep` IS A DIFFERENT PROGRAM IN A SUBAGENT SHELL THAN IN THE CONTROLLER SHELL.** In the CONTROLLER shell `grep` is a **function** resolving to **ugrep 7.8.4** which honours `.gitignore` and is therefore **BLIND to `next-prompt.txt`**; in a SUBAGENT shell `grep` is **`/usr/bin/grep` (GNU 3.11)** and sees it fine. **Both readings are correct; the variable is the SHELL, not the tree.** Every anchored-occurrence figure was cross-checked under BOTH tools and they AGREE exactly (`-family row` 96, archive 163/62/225) — **the divergence is confined to recursive, `.gitignore`-honouring sweeps.** In the controller shell use `command grep -rl`, `git grep`, or a direct path; ⚠️ **`command grep` DOES NOT SURVIVE `xargs`.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files). **NAME THE SHELL AS WELL AS THE TOOL.**
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Live: TWO SDS dial-budget flakes plus `TestSDSEndToEnd_FetchFailure_BootFailsClosed` · the driver-owned receiver port race (aborted a full run at the 94 IMPL) · `internal/httpclient` zero-value · the two 84.2-era flakes · the REFERENCE h2spec section-8 flip · `TestOutlierDetector_ConcurrentEjectExactlyOnce` · `0061-lb-ring-hash`'s σ-margin · `TestP83_StopPauseTimer_IsAuthoritativeAgainstAnEnteredClosure` (wasm, 1/400) · ⚠️ **NEW at the 95 IMPL: `internal/filter/hcm/h2 TestFramer_ReaderGoroutineDoesNotLeak`** — gate (b) run 1 RED, run 2 GREEN. **A goroutine-leak assertion failing under full-suite load is the shape a REAL leak also has**, so this is a REGISTRATION, not a clearance; it was NOT isolated. **Whoever next touches `internal/filter/hcm/h2` should isolate it before assuming it is noise** ([[reference_recurring_flake_may_be_production_bug]]). ⚠️ **`TestServerConn_TinyWindowDelivery` IS NOT A FLAKE** — a recurrence is a REGRESSION of row 91. ⚠️ **A GREEN RERUN CLEARS NOTHING.**
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only). ⚠️ **AT THE PHASE-95 PLAN A SIBLING `curl-world` SESSION STILL OWNED SEVENTEEN CONTAINERS holding `18080-19000`, up 42 hours.** ⚠️ **A `reaper_*` testcontainers Ryuk container is created BY THE DIFFERENTIAL ITSELF and is REUSED; leave it alone.** **Treat NO roster as stable.**
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`. ⚠️ **`INNER_EXIT` DOES NOT EXIST in this repo.**
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL, THEN DRIVE THE ARM.** At phase 95 a prototype built, booted and SERVED with its callback never installed. **`gofmt -l` never exits non-zero — gate on OUTPUT.** ⚠️ **go test WITHOUT `-v` PRINTS ZERO `=== RUN`** — `RUN=0` beside `RC=0` is a VACUOUS GREEN. ⚠️ **A `-run` selector matching nothing prints `[no tests to run]` and EXITS 0.** ⚠️ **On `-v` output, unanchored `grep -c 'FAIL'` reads nonzero ON A FULLY GREEN TREE** — use `grep -cE '^(FAIL|--- FAIL)|^ *--- FAIL'`.
7b. ⚠️ **`grep -c` ON ZERO MATCHES PRINTS `0` AND EXITS 1** — capture, NEVER chain with `&&`, NEVER `$(… || echo 0)`.
7c. ⚠️ **A GREEN TEST RUN IS NOT EVIDENCE A SITE IS EXERCISED** — prove it with a **`panic()` REACHABILITY CONTROL**. At the phase-95 PLAN it fired on **2 of 4** drives and the stack confirmed the `handshake_server.go:169` dispatch anchor. **There is NO `recover()` in non-test `internal/tls` or `internal/listener`, so a panic aborts the binary — gate on `grep -cE '^panic:'`.**
7d. ⚠️ **NC EVERY NEW TEST, AND NC YOUR OWN FIXES** — NEUTRALISE so the package still compiles; check the NC is EXECUTABLE.
7e. ⚠️ **A UNIT TABLE OVER A SHARED ASSERTION IS NOT A GATE ON PRODUCTION BEHAVIOUR** — `t.Helper()` gives each failure a DISTINCT call-site line.
7f. ⚠️ **A NC THAT LEAVES YOUR NEGATIVE CONTROL GREEN IS NOT EVIDENCE THAT CONTROL DOES ANY WORK.** **PROVEN AT THE PHASE-95 PLAN: cell 10 fires ONLY the base-config assertion; four sibling assertions stay green under the bug.**
7g. ⚠️ **ASSERT *WHICH* ERROR FIRED, AND ALSO WHICH DID NOT** — ⚠️ **BUT NOT VIA A CLIENT-VISIBLE ERROR STRING IF THE ALERT CODE IS VERSION-DEPENDENT.** TLS1.3 says `certificate required`; TLS1.2 says `handshake failure`. **Use the server-side counter triple.** ⚠️ **KEEP TABLE ROWS SINGLE-CAUSE.** ⚠️ **PREFER AN EXACT EQUALITY TO A FLOOR.**
7h. ⚠️ **A PROBE CLIENT CAN WITHHOLD THE VERY THING THE ARM EXISTS TO TEST** — force-send via `GetClientCertificate` and ASSERT (not log) that it fired. ⚠️ **`openssl s_client` answers "did the handshake complete", NOT "was the connection served"** — the phase-95 bypass is visible only because a Go probe sent `GET /` AFTER the handshake and got a reply. ⚠️ **A TEST TABLE CAN CARRY AN INVENTED INPUT NO PRODUCTION PATH PRODUCES** — build contexts with the package's OWN helpers.
7i. ⚠️ **A GATE THAT READS 0 HAS NOT BEEN SHOWN TO WORK.** Run every silent gate over an input KNOWN to trip it.
7j. ⚠️ **A PIN CAN FAIL AGAINST *CORRECT* CODE.** Two did at phase 95 (the TLS1.2 error string; the `wantLeaves` "rest 0" that ignores `ssl.no_certificate`). **Before writing an expectation map, MEASURE the leaf set a completing handshake actually books.**
8. ⚠️ **`pgrep -f` / `pkill -f` MATCHES YOUR OWN SHELL AND KILLS THE TOOL CALL (exit 144)**. **Kill ONLY PIDs you captured yourself at launch.** ⚠️ **`pgrep -la a b` ERRORS** — one pattern per call.
9. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-*`, `wt-*` or stage worktree should outlive its stage close** — expect only `master` (verify with `git worktree list`).
10. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** (a) differential — **122/122 expected**; (b) non-Docker sweep gated on `PIPESTATUS[0]` + a SET RECONCILIATION — **235 packages**; (c) h2spec `95 tests, 94 passed, 1 skipped, 0 failed`; (d) fuzzers **56 / 48**; (e) the ANCHORED panic gate `^panic:|DATA RACE|SIGSEGV`, **0** *and PROVEN LIVE*; (f) no REVIEW.md — standing departure. ⚠️ **`-race` on the DIFFERENTIAL suite is VACUOUS** — the subject is an unraced subprocess. **THE IMPL RUNS ALL SIX (Task 18).**
11. ⚠️ **THE DIFFERENTIAL'S FAILURE MODE IS A SILENT PASS.** `-count=1` IS NOT OPTIONAL (the subject is a SUBPROCESS; the cache serves a stale PASS). **ASSERT THE FIXTURE SET BY NAME, BOTH DIRECTIONS, AGAINST THE FIXTURES ON DISK.** The full suite takes ~400s.
11b. ⚠️ **`go test ./...` DRIVES DOCKER — AND THERE ARE TWO DOCKER DRIVERS.** Exclude BOTH: `go list ./... | grep -vE '/test/differential$|/test/conformance/h2spec$'`.
12. ⚠️ **`-p` PUBLISHING WORKS; `--network host` IS WHAT DOES NOT.** ⚠️ **`STRICT_DNS` + `host.docker.internal` + `--add-host=host.docker.internal:host-gateway` IS REQUIRED for a container-to-host cluster HERE** (VM-backed Docker; `docker0` DOES NOT EXIST on the host). ⚠️ **THE REFERENCE CONTAINER CANNOT READ `filename:` CERT PATHS** — use `inline_string:` — ⚠️ **AND INDENT THE PEM ONE LEVEL DEEPER THAN `inline_string:`**: a PEM indented shallower than its key ends the block scalar early and the reference reports `yaml-cpp: … illegal map value` at a line INSIDE the PEM. ⚠️ **A PROBE BACKEND MUST BIND `0.0.0.0`, NOT `127.0.0.1`.** ⚠️ **Keep PEM substitutions OUT of YAML comments.** ⚠️ **envoy-go REJECTS `match.headers`**, **refuses `codec_type: HTTP2` without TLS unless `--allow-h2c`**, **boots with `-c`, NOT `--config-path`**, **`access_log[].log_format` is BOOT-REJECTED**, **`TLSv1_0`/`TLSv1_1` in `tls_params` BOOT-REJECT** (`internal/tls/params.go`) — a bad-version arm is produced CLIENT-side. ⚠️ **AN OMITTED `clusters` KEY BOOT-REJECTS envoy-go** (`cluster manager: cluster: zero clusters in bootstrap`, exit 1) — the inherited *"`clusters: []` did not reproduce"* note concerns the EMPTY-LIST form, a DIFFERENT input. Give a probe subject a placeholder STATIC cluster. ⚠️ **`ENVOY_TARGET.md` is at `docs/envoy-go/ENVOY_TARGET.md`, NOT the repo root.** ⚠️ **The pinned reference is `envoyproxy/envoy:contrib-v1.37.2`, digest `7edd5b0fd763…` — PIN BY DIGEST, verify against `ENVOY_TARGET.md:3-4`** (VERIFIED IN USE at the phase-95 PLAN).
13. ⚠️ **A RAW-FRAMER PEER MUST HOLD ONE `hpack.Decoder` PER CONNECTION.** ⚠️ **`0004` IS *NOT* THE ONLY FIXTURE WITH DOWNSTREAM `alpn_protocols` — FIVE CARRY IT**: `0004-h2-routing` (`envoy.yaml:42` / `envoy-go.yaml:40`), `0079-h2-multiplex-pool/driver/driver.go:295`, `0080-h2-goaway-rotation/driver/driver.go:287`, `0119-grpc-unary-trailers/driver/driver.go:177` — all TCP, all `["h2","http/1.1"]` — plus `0104-http3-downstream-get/driver/driver.go:237` and `:307`, both `["h3"]` on QUIC. **None can regress under this row, and the proof needs no measurement: the tip ENFORCES overlap, so any fixture green today is by construction one whose clients overlap or send no ALPN — exactly the two cases the fallback no-ops on.**
14. ⚠️ **`bash /dev/tcp` + `timeout cat <&3` INSIDE COMMAND SUBSTITUTION RETURNS SILENTLY EMPTY** — use Go for socket probes.
15. ⚠️ **GO DOES NOT FLAG UNUSED CONSTANTS.** Post-removal cleanup is grep-driven.
16. ⚠️ **`golangci-lint`'s misspell runs in locale US.** **Sweep British spellings in `.go` comments BEFORE the gate.** Markdown prose may use them freely.
17. ⚠️ **A GREEN RUN OF A CONTENTION-SENSITIVE TEST PROVES NOTHING.**
18. ⚠️ **AN ABSENCE CLAIM NEEDS A POSITIVE CONTROL AND A DISCRIMINATING NEGATIVE CONTROL.**
19. ⚠️ **DERIVE next-free ADR FROM THE TAIL, NEVER FROM THE HEADING COUNT.** The id space is SPARSE (exactly one gap, `0209`) and headings+1 (**316**+1) collides with `ADR-0317`, which is TAKEN.
20. ⚠️ **`reference_measured_prototype_is_a_lower_bound` HAS FIRED THIRTEEN CONSECUTIVE ROWS.** At the phase-95 PLAN it fired by under-enumerating a COMMENT CLASS — `SPEC.md` §7 named ONE of NINE `GetConfigForClient` sites. **ENUMERATE BY GREPPING THE LITERAL SYMBOL OUTWARD, CASE-INSENSITIVELY, PATHSPEC-SCOPED, AND BY COMPILING.**
21. ⚠️ **A MEASUREMENT THAT GOVERNS A DEFERRAL DECISION MUST LAND IN A GOVERNING DOCUMENT.** The phase-95 reference measurement is `ADR-0317 §Context ¶2`; the `BEHAVIOR_CONTRACT.md:1944` extension is PINNED at `SPEC.md` §11 and **LANDS AT THIS IMPL (Task 16)**.
22. ⚠️ **A STAGE'S FILE SCOPE IS ITSELF MEASURABLE, AND THE ROUTER CAN BE WRONG ABOUT IT.** `git show --numstat` on the precedent stage settles it. **For an IMPL, measure against the phase-94 IMPL commit** — and measure MORE THAN ONE, because at the phase-95 SPEC this note's predecessor was refuted for naming too few. **An IMPL is the stage that DOES touch `ROADMAP.md`, `BEHAVIOR_CONTRACT.md` and `DECISIONS.md`.**
23. ⚠️ **`git diff --stat` IS A SUM, NOT ADDITIONS.** Cost figures come from `--numstat`.
24. ⚠️ **AN UNESCAPED `|` IN A ROADMAP ROW PASSES CHECK (1) AND SILENTLY BREAKS THE FIELD COUNT.** **LIVE THIS SESSION — the IMPL writes row 95's summary.** Count fields (want **8**) under BOTH forms, BEFORE and AFTER installing. **Reword a pipe AWAY rather than escaping it.**
25. ⚠️ **THE EVICTION CHECK IS A *PAIR*, AND THE BARE FORMS DO NOT ANSWER IT.** At the phase-95 PLAN close BOTH bare forms read **5** on `STATE.md` (the naive form read 6 one close earlier; the extra hit lived in the `last-updated` bullet and the SPEC roll removed it). **Both are invariant under which entry is evicted.** Only the LABEL-BOUND form discriminates, on BOTH files: the evictee's own label goes `STATE.md` **1 -> 0** and `STATE_HISTORY.md` **0 -> 1**. **VERIFIED IN BOTH DIRECTIONS at this close.**
26. ⚠️ **A DIRECT DATE READ CAN FAIL TO PICK THE EVICTEE.** The tie has moved in each of the last three closes: MIDDLE (`7964f620`), AT THE OLDEST POSITION (`abc309d7`), **ABSENT (this tip — five distinct dates, so date and position AGREE).** **That agreement is a coincidence of this tip, not a rule.** When a tie IS at the oldest position, fall back to LIST POSITION — the tail — never to a guess.

27. ⚠️ **A SYMBOL-SCOPED CLAIM IS NOT A PATH-SCOPED ONE.** `grep` proving a symbol absent from a function does NOT prove "this never runs on path X" — the CALLERS decide the path, and there can be more than one. Phase 95 asserted "never in `NewQUICDownstreamConfig`", was RIGHT about the body, and was WRONG about the path because `internal/listener/manager.go` has **TWO** downstream-TLS build sites and only one branched on `kind`. **Enumerate the call sites (`grep -rn '<Symbol>(' --include=*.go`) and check each caller's own branch.** Pin the claim through the accessor PRODUCTION uses, not the builder's return. ⚠️ **Pathspec scoping that keeps a diff clean is the SAME scoping that hides its callers.**
28. ⚠️ **A `+0/+0` NEGATIVE-CONTROL ARM CAN BE SILENTLY DELETED WITH EVERY GATE STAYING GREEN.** At the 95 IMPL a subagent dropped `record("clean_fin", …)` from fixture `0120`; `go vet`, `gofmt` and the fixture's own cross-side pins all stayed quiet, because that arm moves NO counter BY DESIGN. Only a per-arm probe caught it. **Diff the ARM ROSTER, not the counters** — `grep -o 'record("[a-z_]*"' driver.go | sort` at both commits, `comm` BOTH ways — and treat a shrinking roster as a finding even when every pin passes.
29. ⚠️ **A MULTI-INSERT LINE SHIFT IS BANDED, NOT A CONSTANT.** `internal/tls/config.go` 582 -> 642 shifted **+0** (<= old 21), **+8** (29-59), **+9** (60-270), **+14** (271-580). The controller handed a subagent a flat "+9"; the subagent re-derived and caught it. **Never hand a scalar shift for a multi-insert file — re-locate every cite by LITERAL TEXT (`grep -nF -- "<exact line>" <path>`).** Line numbers are derived data; the literal is the identity.
30. ⚠️ **A `go test` SELECTOR NAMING A PACKAGE THAT DOES NOT EXIST PRINTS `FAIL … [setup failed]` AND EXITS 1** — it reads exactly like a real red run and it MATCHES the anchored `^FAIL` gate. The controller chased `./internal/xds/sds/...` three times at the 95 IMPL; the package is **`./internal/boot`**. **Confirm a selector resolves (`go list ./path/...`) before believing a FAIL.** This is the mirror of the `-run` footgun, which fails the OTHER way (no match ⇒ `[no tests to run]`, **EXIT 0**). Between them a false red and a false green are each one typo away.
31. ⚠️ **RECONCILE A CORRECTED CLAIM ACROSS ITS WHOLE OCCURRENCE SET.** The 95 IMPL corrected a false measurement in `driver.go` and `README.md` and left a THIRD present-tense copy in `expectations.yaml` — inside the very arm that refutes it. **After correcting any claim, grep its distinctive phrase tree-wide and fix every hit, or state which hits you are deliberately leaving and why.**

## ⚠️ THE `PROPOSED` GUARD — CURRENTLY *DISARMED* (reads **0**); **THE NEXT SPEC RE-ARMS IT**

| form | now | where |
|---|---|---|
| `^> \*\*STATUS: PROPOSED` — **the house form** | **DISARMED, reads 0** | phase 95 flipped it `PROPOSED` -> `ACCEPTED` in place under `## ADR-0317`. ⚠️ **A ZERO HERE IS THE EXPECTED RESTING STATE, NOT EVIDENCE THE GUARD WORKS** — prove it live by appending a `PROPOSED` line to a SCRATCH COPY before you trust a 0. The next SPEC stage re-arms it 0 -> 1 when it drafts its own ADR. |
| `^\*\*Status:\*\* PROPOSED` — **the ADR-0231 decoy** | unchanged, reads 1 | `:14866` under `## ADR-0231`, byte-untouched |
| `^\*\*Status:\*\*.*PROPOSED` — **the middle ground** | reads **23** | ⚠️ **NEVER A GATE** |

⚠️ **VERIFY BY LINE AND BY ADR, NEVER BY THE COUNT ALONE** — resolve any hit to its enclosing ADR by a BACKWARD heading search (`awk 'NR<=<HITLINE> && /^## ADR-/ {h=$0; n=NR} END {print n, h}'`). ⚠️ **NEVER gate on the UNANCHORED form.** ⚠️ **AND DO NOT QUOTE A COUNT FOR EITHER GATE FORM IN PROSE THE GREP MATCHES** — the phase-93 SPEC falsified itself doing exactly that. **ADR-0317 IS COMPLETE.** As landed: heading + house STATUS line (`ACCEPTED`) + `### Context` (ELEVEN ¶s) + the retained italic footer + `### Decision` (SEVEN `D-ALPNFB-*` items) + `### Consequences` ((i)-(xi)); NO `---` separator, so `^---$` STAYS **216**, `^## ADR-` **316**, bare `^## ` **324**, tail **ADR-0317**, next-free **ADR-0318** (`^## ADR-0318` reads 0). ⚠️ **D-ALPNFB-TCPONLY CARRIES TWO IN-PLACE CORRECTIONS, BOTH WITHIN-LINE** — F1 (the claim was true of the SYMBOL, false of the PATH) and F-A (the fix also WIDENS acceptance, which "pure config parity" omitted). Amending a landed ADR in place was legitimate ONLY because ADR-0317 is that same unsquashed branch's OWN ADR; `DECISIONS.md` stays append-only for EARLIER phases.

## ⚠️ THE ARCHIVE GUARD

⚠️ **THE STRICT `163` IS NOT THE ENTRY COUNT.** Re-measured at this tip: loose **226** = strict **163** + parenthetical **63**, exactly — **the triple reconciles only under these ANCHORED-OCCURRENCE forms:**
```sh
H=docs/envoy-go/STATE_HISTORY.md
grep -oE '^- \*\*prior active-phase:\*\* ' $H | wc -l   # strict         163
grep -oE '^- \*\*prior active-phase \('    $H | wc -l   # parenthetical   63
grep -oE '^- \*\*prior active-phase'       $H | wc -l   # loose          226  = 163 + 63
```
⚠️ The unanchored `grep -c 'prior active-phase'` counts LINES and coincides by accident; the colon form is a FOURTH regex. **Name the form or the figure is meaningless.** ⚠️ **`grep -F` WITH A PATTERN STARTING `- ` HITS THE FLAG TRAP** — pass `--` first. **The guard is SOUND as a DELTA-0 SHAPE check** (a correctly-shaped parenthetical append moves the STRICT form by 0 — verified again at this close) **but the number is a SUBSET.**

⚠️ **THE ARCHIVE'S POSITIVE CONTROLS ARE SELF-INCREMENTING.** This stage's archive line names NO control figure. ⚠️ **AND THE RAW LINE DELTA IS `+2`, NOT `+1`** — the entry is a blank line PLUS the entry line. `SPEC.md` §10 forecast `+1` and its own commit landed `+2`, contradicting the precedent immediately before it. **This close: `STATE_HISTORY.md` 550 -> 552.** An IMPL close evicts the phase-94 SPEC entry (`2026-09-02`) if the five-entry cap still binds — but **READ, DO NOT PREDICT.**

## ⚠️ PORT BANDING

`net.ipv4.ip_local_port_range = 32768 60999`. ⇒ **BAND YOUR PROBE PORTS BELOW 32768.** ⚠️ **The differential harness reserves `20000..31007` (`harness_test.go:292-296`) and `11000..14999` (`:209-212`)**, so `12000-19000` is the ad-hoc band — ⚠️ **minus `18080-19000`, held by sibling `curl-world` containers.** **Check availability with `ss -tan` (ALL states), never `ss -ltn`.** ⚠️ **FOR A UNIT TABLE, USE PORT 0** — `connPair` and `startOneWayTLSListener` bind `127.0.0.1:0`. ⚠️ **`net.Pipe` DEADLOCKS a client-cert handshake — use a loopback TCP pair.** ⚠️ **FIXTURE ports follow the `10<index>` family band; `0120` holds `10126` and THIS ROW KEEPS IT.**

## ⚠️ A RECURSIVE `grep` IN THE *CONTROLLER* SHELL IS BLIND TO `next-prompt.txt`

See method note **3j** — this is SHELL-DEPENDENT and was mis-scoped by every prior roll. In the controller shell `grep` is a **function** (ugrep 7.8.4, `--ignore-files`, honours `.gitignore`); `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. `command grep -rl …` (GNU) sees it; direct-path access works. **In a SUBAGENT shell the bare `grep` is `/usr/bin/grep` and already sees it.** ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files).

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 95 (tls-alpn-mismatch-fallback)' --format='%h %s'   # the SLUG form
git log --grep '^phase 95' --format='%h %s'                                # the LOOSE form — RUN IT to CROSS-CHECK
```
⚠️ **RUN BOTH FORMS AND RECONCILE THE SETS. NO COUNT IS QUOTED HERE, DELIBERATELY — BOTH FORMS ROT.** ⚠️ **A `.*SPEC` / `.*PLAN` STYLE LOOSE GREP MATCHES THE WRONG STAGE** (method note 3i). **Take the tip from `git rev-parse master`.** ⚠️ **AND WHEN YOU LAND A CORRECTION, PUT THE FULL SLUG IN ITS SUBJECT.**

Execution style: **subagent-driven per `feedback_execution_style`**. Subagents commit LOCALLY on their own stage branch with EXPLICIT PATHSPECS; the controller merges and squashes (`feedback_subagents_no_push`, `feedback_push_to_origin`). **Pure MEASUREMENT agents should commit NOTHING and prove their tree clean.** ⚠️ **PARALLEL AGENTS IN ONE WORKTREE RACE THE GIT INDEX** — per-agent worktrees on their own branches, private scratch dirs, **Docker serialized to exactly ONE agent at a time** (four agents ran this way at the phase-95 PLAN). ⚠️ **VERIFY WHAT A SUBAGENT REPORTS — BUT ALSO BELIEVE IT WHEN IT REFUTES YOU.** ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root** — build with `-o` into scratch.

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root — **it fired repeatedly again at the phase-95 PLAN.** Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` before any commit. ⚠️ **Shell VARIABLES do not survive between Bash calls** — re-export in every command. ⚠️ **AN UNQUOTED HEREDOC (`<<EOF`) COMMAND-SUBSTITUTES BACKTICKED TEXT** — use `<<'EOF'`.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE OR PROTOTYPE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — `sha256sum` BEFORE patching, `git checkout -- <path>` after, `sha256sum -c` the capture — ⚠️ **and COMMIT FIRST if anything else is pending, because `git checkout --` restores from HEAD.** ⚠️ **Also check the MAIN repo root, and `--untracked-files=all`.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` now reads OPEN THE NEXT SUBJECT (BRAINSTORM).** ⚠️ **Its §Recent is FIVE entries under the ADR-0288 cap; the head now carries TWO entries dated `2026-09-07`, so THE NEXT CLOSE INHERITS A TIE — at the NEWEST position, where it is harmless, but READ it, do not assume it away.**
2. `docs/envoy-go/phases/95-tls-alpn-mismatch-fallback/PROGRESS.md` — **705 lines. THE CLOSING RECORD OF THE PRECEDENT ROW.** ⚠️ **§8 and §9 are the load-bearing halves**: §8 the final whole-branch review's five findings, §9 the scoped re-review's five. **Read §9 before trusting anything §1-§7 asserts about TCP-only-ness or config parity.**
3. `docs/envoy-go/DECISIONS.md` **ADR-0317** — COMPLETE (§Context eleven ¶s + §Decision seven `D-ALPNFB-*` + §Consequences (i)-(xi)), status **ACCEPTED**. ⚠️ **D-ALPNFB-TCPONLY carries TWO in-place corrections** (F1 and F-A). Next-free ADR is **ADR-0318**.
4. `internal/listener/manager.go` — **the two downstream-TLS build sites**: the `filter_chains[]` loop and the `default_filter_chain` block. ⚠️ **THIS IS THE NEXT SUBJECT'S PRIMARY SURFACE.** Read both against each other and against ADR-0080 before concluding anything is a defect.
5. `internal/listener/quic.go` — `quicTLSConfig()` (defaultChain FIRST), `quicChain()`, and the Start-time nil-TLS reject that makes the surviving divergence non-fatal.
6. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — item 8(b) / D-RCCF-QUIC (QUIC client-auth is entirely absent, DECIDED) and item 11 (the SDS pre-scan model, **corrected at the phase-95 close — it is now UNIFORM**).
7. `docs/envoy-go/phases/95-tls-alpn-mismatch-fallback/PLAN.md` — **865 lines**, the precedent IMPL spine (18 tasks); `SPEC.md` **516** ⚠️ **whose §12 cells 10 and 13, §5.2 `wantLeaves` and §5.3 error-string instruction were REFUTED and deliberately NOT implemented**, and whose `:217` still overstates the guard surface BY DESIGN (frozen prior-stage artefact).
8. ⚠️ **`BOOTSTRAP_PROMPT.md` IS AT THE REPO ROOT.** §5 the lifecycle state machine (appears VERBATIM TWICE, §5 and an appendix — **the second copy's offset is NON-constant; find it by heading, never by arithmetic**).

## What the next session owes
**OPEN THE NEXT SUBJECT — a BRAINSTORM (lifecycle DONE -> 1)** — ⚠️ **ONE STAGE PER SESSION.** The pick is a **SELF-PICK** under the 2026-07-12 standing directive: **no human is consulted, and the BRAINSTORM must record the pick AND the rejected alternatives.** The recommended subject is the `default_filter_chain`-vs-`filter_chains[]` divergence set measured at the phase-95 close (see §THE NEXT SUBJECT above) — ⚠️ **but that recommendation is EVIDENCE, NOT AN ORDER: re-derive the divergence set at YOUR tip and justify the pick against the banked list, or pick something else and say why.** · **REGISTER THE NEW ROW `in-progress` — that is an ADD, not a flip, so the denominator MOVES: `want` 127 -> 128 and `ROADMAP.md` 245 -> 246** · **count row fields (want 8) under BOTH the naive and the escape-aware form, BEFORE and AFTER installing, and carry NO unescaped `|` in the summary** · **RE-RUN the sentinel + all four NCs + the check-(2) positive control YOURSELF, BEFORE and AFTER the row ADD — ⚠️ the NC shapes CHANGE across a row change and they changed TWICE at phase 95** · **DRAFT the next ADR (ADR-0318) only at the SPEC stage, not here; the house `PROPOSED` guard currently reads 0 and a 0 is the RESTING STATE, not evidence the guard works — prove it live on a scratch copy** · **REFUTE YOUR PREDECESSOR BY EXECUTION** and record it — ⚠️ **and read §WHAT PHASE 95 REFUTED ABOUT ITSELF first: two of those five are the SAME error (a claim true of a symbol and false of a path), and the second was minted by the correction to the first** · `STATE.md` rolled IN PLACE with the oldest §Recent entry evicted — ⚠️ **read the DATES *and* the POSITIONS; there is a TIE at the NEWEST position now** — and archived to `STATE_HISTORY.md` as **ONE INLINE LINE** in the PARENTHETICAL form, **strict guard 163 DELTA 0**, raw delta **+2**, ⚠️ **name NO positive-control figure in the archive line** · **ROLL THE §Recent PREAMBLE SENTENCE TOO, without spelling the evictee's label** · ⚠️ **RE-DERIVE ANY LINE COUNT YOU QUOTE IN `STATE.md` IN THE SAME COMMIT AS THE EDIT THAT MOVES IT** — phase 95 shipped a stale one, had it caught by review, and then moved it AGAIN with the correction itself · `next-prompt.txt` rolled (`git add -f`) · ONE squashed commit, merged and pushed — ⚠️ **and if a late measurement invalidates something you already pushed, land a CLEARLY-LABELLED correction commit (WITH THE FULL SLUG IN ITS SUBJECT) rather than force-pushing or leaving it standing.**
