=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action. (The sentinel does NOT apply right now — phase 54 is chartered and mid-lifecycle, and Observability + Operational tooling both carry non-empty deferred-candidate lists.)

**STATUS AS OF 2026-07-08: phase 54 (`load-balancer-panic-threshold`) SPEC is DONE (row 54 STAYS `in-progress`, lifecycle-state 1 → 2). The NEXT action is the phase-54 PLAN.**

- **The phase:** `healthy_panic_threshold` as an independent construct — the EIGHTH and FINAL Load-balancing-family row (the family CLOSES at phase-done). Scope: the first-ever differential proof (`0097-lb-panic-threshold`) + FOUR evidence-driven corrections the SPEC's live probes pinned. The SPEC (`docs/envoy-go/phases/54-load-balancer-panic-threshold/SPEC.md`, squash `a59ca442`) resolved all six §10 pins (D-PT1..D-PT6) live against `envoyproxy/envoy:contrib-v1.37.2` and is the PLAN's input.
- **The SPEC's landed findings the PLAN decomposes (§1.1 AMEND-PT1..PT6, §3, §8, §11):**
  - **AMEND-PT1 (the marquee finding, NOT anticipated at BRAINSTORM):** the reference INTEGER-TRUNCATES (floors) the threshold — panic iff `100.0·healthy/total < floor(threshold_percent)`. envoy-go's float compare (`available/total < value/100`) OVER-panics at non-integer thresholds (`60.9%` @ `60%` → reference no-panic, envoy-go panics). FIX (§3.1): floor the value + compare via integer cross-multiply `100·avail < floor(value)·total` (strict `<`, ULP-safe per `reference_percent_cap_cross_multiply`; agrees with the old form at the integer thresholds every prior test used). Stored representation becomes an integer percent (§12 D-PT-STORE).
  - **AMEND-PT2 (D-PT1 iii):** the reference increments `lb_healthy_panic` ONCE per pick on both flat AND locality panic (delta==N). FIX (§3.2): REMOVE the redundant outer `panicInc()` at `locality.go:144` — the shared-health flat child's own `panicGate` provides the single increment (do NOT nil the flat child; that would suppress the increment AND change the zero-total-weight fallback). Unit-proven against the pinned delta==N.
  - **AMEND-PT3 (D-PT4):** the reference does NOT locally panic within a degraded locality → RETROFIT (§3.3): build per-locality children against panic-disabled `tierHealth` views; the flat fallback child KEEPS shared/panic-enabled health. **This ENTAILS a health-parameterized factory signature change** to `newLocalityWeightedLB(WithRNG)` + the `manager.go:513-514` closure (the phase-53 `priorityLeafFactory`/D-P-FACTORY precedent) — load-bearing, not optional. Subset analog needs NO change (a subset DOES panic per-subset — the crux asymmetry; `subsetLB`'s shared-health children already faithful).
  - **AMEND-PT4 (D-PT3):** the reference PGV-rejects `healthy_panic_threshold.value` outside `[0,100]` at boot → a byte-stable range reject (§3.4/§6; ADR-0080). MUST be a STANDALONE, UNCONDITIONAL check early in `buildCluster` — the three `parsePanicThreshold` sites are all health-guarded, so folding the reject there would miss a plain cluster (§12 D-PT-REJECT-PLACEMENT). `NaN` is config-unreachable (protojson rejects it).
  - **AMEND-PT5/PT6:** presence semantics already match (value 0 disables, absent=50%); stat surface ZERO delta (1200→1200); envelope ~150–260 prod LoC / ~9–11 tasks, NO split, NO fuzzer.
- **The PLAN session** runs `superpowers:writing-plans` (SPEC §10 → bite-sized TDD tasks, the phase-36/37/38/52/53 precedent), does the FINAL ADR-0045 split-gate re-check (anticipated NO split — §3.0), and RESOLVES the §12 PLAN/IMPL design questions: **D-PT-STORE** (integer-percent field on `clusterHealth`), **D-PT-REJECT-PLACEMENT** (standalone unconditional check — the fold-into-`parsePanicThreshold` option is INSUFFICIENT), **D-PT-TIERHEALTH-HOME** (relocate/share `tierHealth` + the mandatory factory signature change), **D-PT-UNIT-VS-DIFF** (the double-increment + retrofit stay unit-proven; no locality/subset differential dir). The plan-document-reviewer gate applies at the PLAN. NO live probes at the PLAN (the SPEC executed them all).
- **0097 fixture (SPEC §8.1):** ONE dir, ONE boot, THREE health-checked STATIC clusters × 5 hosts, SAME 2-of-5 degradation (60% healthy): A `{value:80}` PANICS (all 5 hosts incl 2 unhealthy `>0` offset-invariant; `lb_healthy_panic == N_A`), B ABSENT/50% NO panic (2 unhealthy `==0`; counter `==0`), C `{value:60.9}` NO panic (the integer-truncation discriminator). Three `-count=1` deliberate breaks (a: hardcode 50 → A stops panicking; b: skip degradation → A's panic asserts fail; c: revert the floor → C panics). BackendKind reuses the toggle-responder pattern (tail STAYS 38).

==============================================

## Honor throughout (the discipline, unchanged)
- **Lifecycle = one stage per session** (BRAINSTORM → SPEC → PLAN → IMPL). This session: the phase-54 PLAN ONLY; land it, then roll this file to the phase-54 IMPL.
- **Subagent-driven execution** (`feedback_execution_style`) in a **fresh worktree** (`feedback_git_worktrees`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit, re-runs gates on the frozen HEAD, does deliberate-break verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). Per-task gates: `gofmt -l` + `golangci-lint` + `go vet` + `go build ./...` (`feedback_pertask_gofmt_lint`). (For a docs-only stage like the PLAN, the "gate" is the plan-document-reviewer + internal consistency.)
- **Process anchors:** ADR-0106 (per-leg rows) · ADR-0044 (ADR §Decision+§Consequences land at IMPL) · ADR-0045 (split soft gate) · ADR-0080 (byte-stable rejects) · next-free **ADR-0271** (§Context DRAFT already anchored in SPEC §13; body lands at IMPL).
- Re-verify baselines fresh at cold-start — do NOT trust any count in this file without re-running the baseline commands (`ls -d test/fixtures/[0-9][0-9][0-9][0-9]* | wc -l` → expect 98, `grep -rh '^func Fuzz' --include='*.go' --exclude-dir=.worktrees . | wc -l` → expect 52, DECISIONS tail → ADR-0270, stat surface → 1200) against current HEAD.

## Read first (in order)
1. THIS FILE — the cold-start router (phase-54 PLAN is next).
2. `docs/envoy-go/phases/54-load-balancer-panic-threshold/SPEC.md` — the PLAN's direct input: §3 (the four corrections + exact source citations), §6 (the reject roster), §8 (the `0097` three-arm fixture + breaks), §10 (the ~9–11-task indicative spine), §11 (the D-PT1..D-PT6 live evidence), §12 (the D-PT-* PLAN questions to resolve), §13 (the ADR-0271 §Context DRAFT).
3. `docs/envoy-go/STATE.md` — the active-phase header (`phase 54 SPEC done`) + full history.
4. `docs/envoy-go/ROADMAP.md` — row 54 `in-progress` (the SPEC-firmed AMEND-PT1..PT6 note); the Load-balancing family section (CLOSES at phase-done).
5. `docs/envoy-go/phases/53-load-balancer-priority/PLAN.md` — the PLAN shape template (bite-sized TDD tasks, the FINAL ADR-0045 re-check, the §12-resolution style); its `tierHealth`/`priorityLeafFactory` are directly load-bearing for AMEND-PT3's factory change.
6. The as-built source the PLAN's tasks touch: `internal/cluster/health.go` (`inPanic`/`parsePanicThreshold`/`panicGate`/`clusterHealth`), `internal/cluster/locality.go` (`localityWeightedLB.Pick` :142-146 + the constructor + the `factory` closure), `internal/cluster/priority.go` (`tierHealth`), `internal/cluster/manager.go` (the three `parsePanicThreshold` sites :461/501/536, the `buildLeafLB` closures :478-479/:513-514, the reject-wording family `fmt.Errorf("cluster: %q: …")`).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — especially `reference_percent_cap_cross_multiply` (the AMEND-PT1 cross-multiply discipline), `reference_health_check_propagation_warmup` (the `0097` convergence+warmup gate), `reference_round_robin_offset_randomized`, `reference_fixture_workload_constant_desync`, `reference_differential_*`, `feedback_*`.
