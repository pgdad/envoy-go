RESUME a fresh session for envoy-go (router last updated 2026-06-26; the **phase 45 (`otlp-access-log`) BRAINSTORM is IN PROGRESS — design presented + the load-bearing decisions SETTLED via explicit user choices, but `BRAINSTORM-45.md` is NOT yet written and NOTHING has been committed this stage**). HEAD is UNCHANGED at the **`next-prompt.txt` tip-stamp `b349768c`**, which sits atop the **phase-44.3 IMPL squash `4abf2c49`** (subject `phase 44.3 (grpc-access-log header-capture) IMPL: make additional_{request,response}_headers_to_log LIVE — …`). Re-read the shas with `git log --oneline -14`, do NOT assume — any number of docs-only `next-prompt.txt:` commits may stack ATOP the IMPL squash (the stable anchor). BENEATH `4abf2c49` (~depth 3) is the pre-IMPL `next-prompt.txt:` run, then the **44.3 PLAN `e8c42d28`** (~depth 6), the **44.3 SPEC `6c0caafe`** (~depth 10), the **44.2 IMPL squash `c4055b20`** (~depth 13), down toward the 44.1 squashes + the phase-44 BRAINSTORM `81afdd59`. Counts are UNCHANGED from the 44.3 IMPL-done (this stage has committed nothing): stat surface **1189** (non-H2 **1185**) / fixtures **85** (`ls -d test/fixtures/[0-9][0-9][0-9][0-9]* | wc -l` == 85; tail `0083-grpc-access-log-headers`) / fuzzers **44** (`^func Fuzz` == 44) / BackendKind tail **38** / DECISIONS tail **ADR-0257**, next-free **ADR-0258**.

**ROW 44 (`grpc-access-log`) is `done`** (the 44.3 header-capture leg was the FINAL chartered leg of the by-concern 3-leg split; per-leg ADR-0106 + `reference_roadmap_split_phase_row_done`; NO parent rollup). The **Observability FAMILY STAYS OPEN** (OTLP access log [opening now] / OTel-Zipkin tracing / stats sinks / tap filter remain rows). **The active action is the SECOND Observability-family row — phase 45 (`otlp-access-log`) — whose BRAINSTORM is mid-flight from a prior session.** Resume `superpowers:brainstorming` from the design-approval gate: confirm the presented design with the user (the decisions below are SETTLED, from explicit AskUserQuestion answers — only the final assembled-design sign-off + the doc write remain), then write the BRAINSTORM in the house phase shape and run the spec-document-reviewer loop.

First confirm state via `git log --oneline -14` (expect HEAD at `b349768c` atop the 44.3 IMPL squash `4abf2c49`; counts as above; re-read the shas, do NOT assume).

## THE IN-FLIGHT BRAINSTORM (phase 45 `otlp-access-log`) — SETTLED DECISIONS (do NOT re-derive)
- **Subject** (user-confirmed): the **OTLP access logger** — a THIRD `accesslog.Sink` (`OTLPAccessLogSink`) alongside `AsyncFileSink` (06.2) + `GrpcAccessLogSink` (44), dialing the OTLP collector's **`opentelemetry.proto.collector.logs.v1.LogsService.Export`** (UNARY, batched — NOT the streaming `AccessLogService.StreamAccessLogs`) over the existing `grpcclient.Dialer`/cluster-TLS layer; each proto-agnostic `Record` → an OTLP `LogRecord` inside a batched `ExportLogsServiceRequest` (one `ResourceLogs`, N records).
- **Envelope ceiling** (user-chosen): built-in `LogRecord` mapping **PLUS a small command-operator engine** (`%REQ(x)%`/`%RESP(x)%`/`%RESPONSE_CODE%`/`%START_TIME%`/… substitution over the satisfiable subset of the 10-field `Record` + header maps) — the full-featured target.
- **Split** (user-chosen): **2-leg by concern.**
  - **Leg 45.1 — core export + built-ins + buffering** (ADR-0258): the NEW go.mod module + an `OTLPLogsClient` wrapper over `LogsServiceClient.Export` (mirrors `ALSClient`'s shape, UNARY) + the `OpenTelemetryAccessLogConfig` → `OTLPConfig` parse arm (LIFT OTLP out of the ADR-0041 silent-ignore set; strict-reject roster mirroring ALS — V2 transport / `google_grpc` / empty `envoy_grpc.cluster_name` / non-empty `body`/`attributes`/`resource_attributes` [deferred to 45.2] / `formatters` [out of scope]) + the `OTLPAccessLogSink` (channel + writer-goroutine + the REUSED buffer machinery: `buffer_size_bytes`/`buffer_flush_interval` → per-`Export` batch) + the built-in `Record→LogRecord` mapping (`TimeUnixNano`/`SeverityNumber`/Envoy built-in attributes; `disable_builtin_labels` honored) + the 2 process-global stats `access_logs.open_telemetry.{logs_written,logs_dropped}` + the driver-owned OTLP `LogsService` receiver test helper (NEW, mirrors `accessloggrpc`; NO new BackendKind) + a built-ins differential fixture.
  - **Leg 45.2 — the command-operator engine** (ADR-0259): the minimal `%…%` substituter over `body` (`AnyValue`) + `attributes`/`resource_attributes` (`KeyValueList`); `%REQ%`/`%RESP%` operators SUBSUME 44.3 header-capture (no separate header surface) + a templated-attributes fixture.
- **Row 45** registers `in-progress` at the BRAINSTORM commit; flips `done` at the 45.2 IMPL (final leg, ADR-0106); the Observability family STAYS OPEN.

## STRUCTURAL FINDINGS already verified in-session (carry forward; do NOT re-investigate)
- **The ONE departure:** OTLP needs `go.opentelemetry.io/proto/otlp` as the **FIRST new direct go.mod module since 44.x** (already in the module cache transitively — `@v1.0.0`; promote to direct). Provides `collector/logs/v1.LogsService` (`Export(ExportLogsServiceRequest)→ExportLogsServiceResponse`, UNARY), `logs/v1` (`LogRecord`/`ResourceLogs`/`ScopeLogs`), `common/v1` (`AnyValue`/`KeyValueList`). Envoy's own `OpenTelemetryAccessLogConfig` already imports `go.opentelemetry.io/proto/otlp/common/v1`. The resolved `go-control-plane/envoy v1.32.4` HAS the OTLP access-logger proto at `extensions/access_loggers/open_telemetry/v3/logs_service.pb.go` (config fields: `common_config` *CommonGrpcAccessLogConfig [1] / `body` *AnyValue [2] / `attributes` *KeyValueList [3] / `resource_attributes` *KeyValueList [4] / `disable_builtin_labels` bool [5] / `stat_prefix` string [6] / `formatters` []TypedExtensionConfig [7]). ZERO OTHER new modules; the sink lives in the existing `internal/accesslog` package.
- **No command-operator engine exists today.** `internal/accesslog/format.go` is a SINGLE hardcoded default-format emitter (`Default(*Record)`, 86 lines, 15-operator default), NOT a general `%…%` template parser — so 45.2 builds a genuinely new (small) subsystem; it can reuse format.go's operator→Record-field knowledge.
- **Reuse seams (mapped in-session, file:line):** `internal/accesslog/` — `Sink`{`Submit(any)`/`Close() error`} [`accesslog.go:18`]; `Record` 10 fields + `RequestHeaders`/`ResponseHeaders` maps [`accesslog.go:29`, proto-agnostic]; `GrpcAccessLogSink` channel+writer-goroutine+buffer+drop-newest+idempotent-Close template [`grpcsink.go:48`, ctor `:80`, `run()` `:166`]; `CaptureHeaders` [`capture.go:13`]; `buildHTTPAccessLogEntry` [`mapping.go:54`]. `internal/bootstrap/` — `ALSConfig` [`bootstrap.go:193`], `parseGrpcAccessLog` strict-reject arms [`:371`], `parseOneAccessLog` type-URL dispatch [`:323`], silent-ignore set (ADR-0041/0080). `internal/grpcclient/` — `Dialer.DialContext` [`grpcclient.go:106`], `ALSClient` (the wrapper shape `OTLPLogsClient` mirrors) [`:259`, ctor `:277`]. `internal/filter/hcm/` — `emitAccessLog`/`emitAccessLogH2` (generic `Submit`, UNCHANGED) [`accesslog_emit.go:19`/`:45`], `accessLog []accesslog.Sink` [`config.go:110`], `headerCaptureSink` union [`config.go:321`]. `cmd/envoy-go/main.go` — the dial-and-build pass-through [`:118`]. `test/helpers/accessloggrpc/` — the driver-owned receiver pattern (`Entries`/`Count`/`BatchSizes`/`Reset`) [`accessloggrpc.go:49`].
- **Anticipated counts:** at 45.1 IMPL — **+1 go.mod module** / stat **1189 → ~1191** (+2, SPEC-pinned — fixed `access_logs.open_telemetry.*` vs. `stat_prefix`-derived is `D-OTLP-STATS`) / fixtures **85 → 86** / fuzzers **44 → 45** (`FuzzParseOpenTelemetryAccessLogConfig`) / BackendKind **38** UNCHANGED (driver-owned receiver) / **ADR-0258**. At 45.2 IMPL — fixtures **→ 87** / fuzzers **→ 46** (operator-template parser) / **ADR-0259**.
- **SPEC-deferred `D-OTLP-*` (resolved at the 45.1 SPEC via live probe vs. `envoyproxy/envoy:contrib-v1.37.2` per `reference_docker_probe_bridge_network`):** `D-OTLP-BUILTINS` (exact built-in attribute set + `disable_builtin_labels`) · `D-OTLP-EXPORT-SHAPE` (unary `Export` batching + `ResourceLogs`/`ScopeLogs` nesting + node/Identifier placement) · `D-OTLP-STATS` (fixed vs. `stat_prefix`-derived; +2 vs. +N) · `D-OTLP-SEVERITY` (status-code → `SeverityNumber`) · `D-OTLP-RECEIVER` (driver-owned receiver wiring) · `D-OTLP-OPERATORS` (45.2 operator subset + unsupported-op: strict-reject vs. missing-value `-`) · `D-OTLP-SPLIT` (ADR-0045 re-check at each SPEC).

## NEXT STEP — finish the phase-45 BRAINSTORM
Resume `superpowers:brainstorming` from the design-approval gate: (1) confirm the assembled design with the user (2-leg boundary / `access_logs.open_telemetry.*` +2 / probe-deferral of the built-in set, per the 44.x rhythm); (2) write `docs/envoy-go/phases/45-otlp-access-log/BRAINSTORM-45.md` in the house 12-section phase-43/44 BRAINSTORM shape (a fresh worktree off master per `feedback_git_worktrees`, OR a docs-only direct-on-master commit as the 44 BRAINSTORM was); (3) run the spec-document-reviewer loop (verify EVERY load-bearing proto/code claim TRUE against the real source — esp. the OTLP proto field tags + the unary `Export` signature + the reuse-seam file:lines above); (4) register row 45 `in-progress` on ROADMAP + roll STATE.md; (5) push per `feedback_push_to_origin`. THEN roll this router to **the 45.1 SPEC**.

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** This stage is the phase-45 BRAINSTORM (lifecycle-state 0 → 1 for the new row). `feedback_git_worktrees` · `feedback_execution_style` (subagent-driven once a PLAN exists) · `feedback_push_to_origin`.
- **Process anchors:** ADR-0106 (per-leg roadmap rows; family expansion has no parent rollup) · ADR-0045 (the split-gate — re-checked at each SPEC) · ADR-0041/0080 (silent-ignore vs. strict-reject for access-log type-URLs) · ADR-0255 (44.1 ALS sink) · ADR-0256 (44.2 buffering) · ADR-0257 (44.3 header-capture) · next-free **ADR-0258** (45.1 OTLP core) then **ADR-0259** (45.2 operator engine).

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-45 OTLP BRAINSTORM is IN PROGRESS; decisions SETTLED; the next action is to FINISH the brainstorm — design sign-off → write `BRAINSTORM-45.md` → spec-review loop → roll to the 45.1 SPEC).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 44.3 (grpc-access-log) IMPL done`) + the recorded NEXT (the next Observability-family row) + the count figures.
3. `docs/envoy-go/ROADMAP.md` — row 44 (`grpc-access-log`, `done`) + the `### Observability family` heading (line ~158: gRPC ALS done, OTLP access log next, then tracing / stats sinks / tap).
4. `docs/envoy-go/DECISIONS.md` — ADR-0255/0256/0257 (the 44.x as-built ALS sink + buffering + header-capture reuse seams) + next-free **ADR-0258**.
5. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the `### Access log — gRPC Access Log Service (ALS) streaming sink` block (the as-built contract the OTLP sink extends; the stat-surface 1189 entry).
6. `docs/envoy-go/phases/44-grpc-access-log/` — SPEC/PROGRESS-44.1/44.2/44.3 (the as-built ALS subsystem the OTLP row reuses — the sink/buffer/parse/differential-receiver templates).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `reference_streaming_sink_differential_framing`, `reference_differential_grpc_receiver_driver_owned`, `reference_docker_probe_bridge_network`, `reference_fuzzer_count_docs_drift`, `feedback_execution_style`, `feedback_git_worktrees`, `feedback_subagents_no_push`, `feedback_pertask_gofmt_lint`).
