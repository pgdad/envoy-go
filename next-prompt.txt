START a fresh cold-start session for envoy-go — **the phase-29.2 sub-phase PLAN (`network-filter-mongo-responses-and-correlation`) is DONE + reviewer-APPROVED + squash-merged + pushed**. The active phase is **phase 29.2 → PLAN done** (parent row 29 STAYS `in-progress`; sub-row 29.1 `done`; 29.2 `in-progress`; 29.3 `planned`). This session opens the **29.2 IMPL** — executing `docs/envoy-go/phases/29.2-network-filter-mongo-responses-and-correlation/PLAN.md` (11 bite-sized TDD tasks) via **`superpowers:subagent-driven-development`** (per SKILL_ROUTING state 3: SPEC.md + PLAN.md exist → IMPL; per `feedback_execution_style` — a fresh subagent per task, two-stage review between tasks). Work in a git worktree (`feedback_git_worktrees`); subagents commit LOCAL-ONLY (`feedback_subagents_no_push`); the controller squash-merges + pushes at stage-close (`feedback_push_to_origin`).

The 29.2 PLAN landed at the **29.2-PLAN squash `0783776`**; a docs-only STATE/next-prompt SHA-fill commit trails it (the named live tip — confirm via `git log --oneline -1`). The substantive predecessor on master is the 29.2-SPEC squash `13907f1` (docs-only `d7a92a1`/`d2f5046` trail it); before that the 29.1-IMPL squash `5ef5215`; the 29.1-PLAN `89c194a`; the 29.1-SPEC `7574219`. The 29.2 PLAN is the IMPL's blueprint; the 29.2 SPEC + the as-built `internal/filter/network/mongoproxy/` package (29.1) + ADR-0225 §Context (incl. the 2026-06-04 D-P4 AMEND) are the pinned-behavior references. The ADR-0225 §Decision/§Consequences body lands IN PLACE at the IMPL final task (no new ADR number — DECISIONS tail STAYS ADR-0226, next-free ADR-0227).

## What the 29.2 IMPL builds (the PLAN's 11-task spine)

The PLAN turns the SPEC §3/§6/§7 surface into write-failing-test → run → implement → run → commit steps with COMPLETE Go code blocks, exact file paths, run commands + expected outputs, per-task `gofmt -l` + `golangci-lint`, and the `-count=1` deliberate-break protocol (`reference_differential_break_protocol_count1`). The tasks (green-compiling dependency order):

1. **T1** — first-action baselines/anchors gate: re-pin fixtures **52** (tail `0050`) + fuzzers **39** + stats **360** + DECISIONS tail **ADR-0226** (next-free **ADR-0227**) + BackendKind tail **29** (next-free **30**) + the SPEC §9.1 as-built anchors, against the live IMPL-session tip (R6). Create `PROGRESS.md`.
2. **T2** — `decoder` struct: add `writeBuf []byte` + `mu sync.Mutex` + `dynMeta map[string][]string`; the `appendQuery` helper (list append under `mu` + the gauge `Inc` — the list-size↔gauge invariant); replace the two 29.1 request-path append sites. [SPEC §3.5/§3.4]
3. **T3** — response framing + dispatch: `decodeOnWrite` + `nextWriteMessage` + `decodeResponseMessage` + the `opReply`/`opCommandReply` write-side dispatch arm + the write-side `decoding_error`/sniffing-off; **`sniffing bool` → `atomic.Bool`** + the **CAS-based at-most-once `decoderError`** (D-S29.2-4); the readBuf-release relocation (post-loop, so goroutine B never touches readBuf). [SPEC §3.1/§3.2]
4. **T4** — OP_REPLY + OP_COMMANDREPLY body decode + the 5 response counters (`op_reply` + `_cursor_not_found` [0x01] + `_query_failure` [0x02] + `_valid_cursor` [cursorID≠0] + `op_command_reply`). [SPEC §3.2]
5. **T5** — correlation: `takeQuery` (first-match-erase by `query.requestID == reply.responseTo` under `mu`) + the gauge `Dec` on a hit (outside the lock) + uncorrelated-miss handling. [SPEC §3.3/§3.4]
6. **T6** — `decoder.onDestroy` (snapshot-count drain under `mu`, `Add(-n)` outside; D-S29.2-5) + `filter.OnDestroy` wiring + the inc/dec lifecycle invariant. [SPEC §3.4]
7. **T7** — `OnWrite` glue (replace the no-op) + `TestDecoderConcurrentRequestResponseRace` (`-race -count=5`) + the **R9 mutex deliberate-break** (remove the `appendQuery`/`takeQuery` locks → DATA RACE with `-count=1`; revert). [SPEC §3.1/§3.5/R9]
8. **T8** — `emit_dynamic_metadata` emission (decoder accumulates `dynMeta`; the filter writes ONE StructValue via `Bucket.Set("envoy.filters.network.mongo_proxy", "operations", sv)` — the single-Set model, D-S29.2-3, zero `internal/dynamicmetadata/` change; per-pass clear; gated). [SPEC §3.7 / D-P11]
9. **T9** — the 39th fuzzer `FuzzMongoDecode` EXTENDED to feed BOTH `decodeOnData` + `decodeOnWrite` (D-P6 — NO 40th). [SPEC §7]
10. **T10** — `TCPMongoResponder` BackendKind **30** + the `acceptMongoResponder`/`mongoRespondLoop` runner arm (the `acceptZKResponder` sibling) emitting correlated OP_REPLY/OP_COMMANDREPLY frames (responseTo echoes requestID). [SPEC §6.1]
11. **T11** — `0051-mongo-responses` driver + cross-side GREEN all arms (the gauge quiesced-point arms answered→0/unanswered→1; `cx_destroy_*` PRESENCE-ONLY; the R4 deliberate-break with `-count=1`) + README; then the completion bundle: ADR-0225 §Decision/§Consequences body in-place + the BEHAVIOR_CONTRACT 29.2 bundle (§8) + STATE/ROADMAP sub-row 29.2 `in-progress → done` (parent row 29 STAYS `in-progress`) + next-prompt.txt for the **29.3-SPEC** cold-start + the six-gate (full 53-dir suite). [SPEC §6.2/§8/§13]

## Load-bearing PLAN pins the IMPL must honor

- **D-S29.2-4 (sniffing → `atomic.Bool` + CAS).** The PLAN converts the 29.1 `sniffing bool` to `atomic.Bool` and folds the at-most-once `decoding_error` charge into a single `CompareAndSwap(true, false)` in `decoderError()`. This keeps the per-connection `sync.Mutex` STRICTLY on `dec.queries` (cleaner than the SPEC §3.5 conservative mu-for-sniffing sketch). The `-race -count=5` test (T7) + the R9 deliberate-break are the proof.
- **The per-connection `sync.Mutex` (ADR-0223, narrowed to ONE list).** Guards EXACTLY `dec.queries` (3 lockers: `appendQuery`/`takeQuery`/`onDestroy`); entries copied out by value under the lock; gauge/counter math OUTSIDE it; the pre-handoff request path locks too (uniformity). Removing `mu` MUST trip `-race`.
- **No write-side high-water mark** — `writeChainConn.Write` allocates a fresh per-Write Buffer (the 28.2 asymmetry); `decodeOnWrite` appends `buf.Bytes()` directly to `writeBuf`.
- **The gauge is the project's FIRST differentially-mirrored gauge** — `Inc` at append, `Dec` at correlated reply, `Add(-n)` residual drain at destroy → 0 at connection end; the D-P9 quiesced-point assertion (answered→0, unanswered→1 while open, 0 post-close).
- **+0 stat creation; no new name.go arm; no new built-in; zero framework touch** — all 23 stats created eagerly at 29.1; the four-rule arm + the `0049` `scrapeMongoStats`/`scrapeTypeLine` helpers handle the reply counters + the gauge as-is. The framework (`internal/filter/network/` chain/readconn/writeconn/types/callbacks/terminal/registry, `manager.go`, `tcp_proxy`, HCM, `internal/stats/`, `internal/dynamicmetadata/`) is UNTOUCHED.
- **D-P4 COVERAGE BOUNDARY** — `cx_destroy_local/remote_with_active_rq` get NO increment at 29.2 (exist-at-zero, PRESENCE-ONLY in `0051`); the close-direction accessor + value parity DEFER to 29.3. Recorded as the BEHAVIOR_CONTRACT coverage boundary at the IMPL.
- **The 0051 arm-accounting table is GROUND TRUTH** — author it as a comment block above `driveProxy` (the `0049` discipline) matching the arms EXACTLY, then re-verify the `op_reply`/`op_query` totals LIVE cross-side. The IMPL-owned D-S29.2-1 (reply-frame byte minimums: empty OP_REPLY = 20 body bytes, empty OP_COMMANDREPLY = 10) is verified vs upstream at the responder + decoder tasks (`reference_wire_format_both_sides_see_same_bytes`). The cross-side fixture needs Docker on a shared bridge network (`reference_docker_probe_bridge_network`).

## What this session must do

1. Re-establish a worktree (`feedback_git_worktrees`); confirm the clean baseline (`go build ./...` + `go test ./internal/... -count=1`).
2. Run `superpowers:subagent-driven-development` to execute the 29.2 PLAN task-by-task (T1→T11): a fresh subagent per task carrying the PLAN's complete code blocks; two-stage review between tasks; per-task `gofmt -l` + `golangci-lint`; the `-count=1` deliberate-break protocol for the R9 mutex break + the T11 R4 fixture breaks. Subagents commit LOCAL-ONLY.
3. The T11 completion bundle lands the ADR-0225 §Decision/§Consequences body + the BEHAVIOR_CONTRACT 29.2 bundle + STATE/ROADMAP (sub-row 29.2 `in-progress → done`; parent row 29 STAYS `in-progress`) + next-prompt.txt for the **29.3-SPEC** cold-start + the six-gate (full 53-dir suite). Controller: squash-merge + fill the squash SHA into STATE/next-prompt + push.

## Project state at master tip

- **Master tip:** the 29.2-PLAN squash (confirm the live tip via `git log --oneline -1`; a docs-only SHA-fill commit may trail). Substantive predecessors: the 29.2-SPEC squash `13907f1`; the 29.1-IMPL squash `5ef5215`; the 29.1-PLAN `89c194a`; the 29.1-SPEC `7574219`. origin/master in sync after push.
- **§9 Network-filters family:** rows 26/27/28 DONE; row 29 `in-progress` (29.1 `done`; 29.2 `in-progress` — PLAN done; 29.3 `planned`).
- **Counts at master tip (UNCHANGED at the 29.2 PLAN — no code):** stat surface **360** (+0 at 29.2/29.3); differential fixtures **52** (tail `0050-mongo-boot-reject`; → 53 at the 29.2 IMPL, 54 at 29.3); fuzzers **39** (the 29.2 IMPL EXTENDS `FuzzMongoDecode`, no count change); BackendKind tail **29** (`TCPZKResponder`; → `TCPMongoResponder` 30 at the 29.2 IMPL); conformance h2spec 53/53 + proxy-wasm 10/10 (as of the 29.1 six-gate, 2026-06-04); DECISIONS.md tail **ADR-0226** (next-free **ADR-0227**; the ADR-0225 body lands at the 29.2 IMPL — no new number).
- **Toolchain:** Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); reference Envoy v1.37.2 (ADR-0008); go-control-plane v1.32.4 (ADR-0008).
- **Known coverage boundaries (carried, non-blocking):** the mongo D-P1 boot-window eager stat-creation departure (unobservable); the `stats.IsValidName` guard on un-nameable wire-derived dynamic segments (user-approved; fuzz-validated; carries unchanged — `reference_dynamic_stat_name_charset_guard`); the mongo dynamic-HISTOGRAM families deferred (ADR-0060 — RECORDED at the 29.2 IMPL); the **`cx_destroy_*` close-direction value parity DEFERRED to 29.3** (D-P4 — recorded at the 29.2 IMPL; `reference_close_direction_framework_gap`); the dynamic-metadata emission differential-invisible (AMEND-B11 — unit-test only); fault delay + access log + drain + `cx_drain_close` + `delays_injected` + mongo runtime-key gating + the mongo access log are 29.3's; `tcp_proxy` `downstream_cx_*` unmirrored; histograms deferred project-wide (ADR-0060).

## Read first (in order)

1. THIS FILE — the 29.2-IMPL cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 29.2 PLAN done`; next-skill `superpowers:subagent-driven-development` (the 29.2 IMPL); next-free `ADR-0227`.
3. `docs/envoy-go/phases/29.2-network-filter-mongo-responses-and-correlation/PLAN.md` — the IMPL's blueprint (11 tasks; complete Go code blocks; the PLAN-time D-question dispositions; the file structure + the ADR-0045 gate-check).
4. `docs/envoy-go/phases/29.2-network-filter-mongo-responses-and-correlation/SPEC.md` — the pinned behavior (esp. §3 the design; §6 the fixture + `TCPMongoResponder`; §7 the fuzzer; §10 the D-questions; §13 the test surface + the six-gate) + the sibling README.md.
5. `docs/envoy-go/DECISIONS.md` ADR-0225 (the §Context + the 2026-06-04 D-P4 AMEND — the charter; its §Decision/§Consequences body lands at the IMPL final task) + ADR-0224 (the 29.1 request side this builds on) + ADR-0223 (the per-connection-mutex + no-write-side-high-water-mark + copy-out-under-lock discipline) + ADR-0217 (the dynamic-metadata Bucket) + ADR-0221 (the write chain) + ADR-0044/0052/0060/0219.
6. `internal/filter/network/mongoproxy/` (the 29.1 package the PLAN extends in place — `codec.go` the response decoder joins; `filter.go` the OnWrite feed + the OnDestroy; `stats.go` the eager reply counters + the gauge; `bson.go` reused for reply-doc walks; `config.go` `emitDynamicMetadata` already parsed) + `internal/stats/gauge.go` + `internal/dynamicmetadata/dynamicmetadata.go` + `internal/filter/network/rbac/rbac.go` (the existing Bucket-producer pattern) + `test/differential/fixture/fixture.go` (the BackendKind roster — next-free 30) + `test/fixtures/0049-mongo-requests/driver/driver.go` (the `0051` driver template + the LE wire/BSON builders + the scrape helpers) + the `acceptZKResponder` block in `test/differential/runner_test.go` (the `acceptMongoResponder` plumbing template).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — execution-style + worktree + push + subagents-no-push + per-task gofmt/lint + the differential dispatch/asserter/break-protocol (`-count=1`) + wire-format-both-sides + the dynamic-stat charset guard + the close-direction framework-gap boundary all carry forward into the 29.2 IMPL.
