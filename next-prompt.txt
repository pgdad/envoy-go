START a fresh cold-start session for envoy-go — **the phase-29.1 sub-phase IMPL (`network-filter-mongo-wire-and-requests`) is DONE + the six-gate is GREEN + squash-merged + pushed**. The active phase is **phase 29.1 → done** (parent row 29 STAYS `in-progress`; 29.2/29.3 `planned`). This session opens the **29.2 SPEC** (`network-filter-mongo-responses-and-correlation`) — authoring `docs/envoy-go/phases/29.2-network-filter-mongo-responses-and-correlation/SPEC.md` via **`superpowers:writing-plans`** (per SKILL_ROUTING: the 29.1 IMPL closes → the 29.2 SPEC opens; the phase-29 parent BRAINSTORM already drafted the ADR-0225 §Context, so the sub-phase SPEC is authored, NOT re-brainstormed — the 28.2-SPEC-after-28.1-IMPL precedent). Work in a git worktree (`feedback_git_worktrees`); commit LOCAL-ONLY; the controller squash-merges + pushes at stage-close (`feedback_push_to_origin`).

The 29.1 IMPL landed at the squash SHA the controller fills post-merge (the substantive predecessor on master is the 29.1-PLAN squash `89c194a`; before that the 29.1-SPEC squash `7574219`). The phase-29 parent SPEC (`29-network-filter-mongo-proxy/SPEC.md`, squash `ab376d8`) + the 29.1 SPEC/PLAN are the pinned-behavior references the 29.2 SPEC builds on. ADR-0225 §Context is already in `DECISIONS.md` (the 29.2 charter); its §Decision/§Consequences body lands at the 29.2 IMPL.

## What 29.2 lands (the response side + correlation + the gauge — ADR-0225)

1. **The RESPONSE-side decoder in `OnWrite`** (the ADR-0221 write chain; mongo_proxy = consumer #2): OP_REPLY(1) decode (responseFlags + cursorID + numberReturned + reply documents) + the reply-flag counters `op_reply_cursor_not_found` [flag 0x01] / `op_reply_query_failure` [flag 0x02] / `op_reply_valid_cursor` [cursorID ≠ 0] + the base `op_reply`; OP_COMMANDREPLY(2011) → `op_command_reply`. These are the Reply(1)/CommandReply(2011) opcodes the 29.1 decoder RECOGNIZED-NOT-DECODED (valid envelope, consumed, NOT a `decoding_error`) — 29.2 wires their body decode + counters. The write-side private reassembly buffer is created WITH the response decoder (29.1's `OnWrite` was a pure no-op stub that did NOT buffer).
2. **requestID↔responseTo correlation** consuming the 29.1 per-connection active-query list (`dec.queries`): first-match-erase on `reply.responseTo == query.requestID`; ONLY OP_QUERY created entries at 29.1 (GetMore/Insert/KillCursors/Command did not — parent §11.4 item 7); uncorrelated replies charge ONLY the fixed `op_reply*` counters. The active-query list is now cross-goroutine state (pump A `OnData` writes; pump B `OnWrite` reads/erases) → the **ADR-0223 per-connection `sync.Mutex`** pattern applies (minimal critical section; entries COPIED OUT under the lock; counter/gauge math outside it). The 29.1 `filter.go` forward-pointer comment marks the mutex site.
3. **The `op_query_active` GAUGE inc/dec** — the project's FIRST differentially-mirrored gauge (the gauge ROW was created at 29.1; 29.2 wires the increments): `Inc` at OP_QUERY decode (the list-append site), `Dec` at a correlated reply AND at connection destroy (the list-teardown). The fixture asserts the gauge at QUIESCED points (all queries answered → 0 both sides; an unanswered-query arm → 1 both sides) — the gauge-value parity design the SPEC must pin.
4. **`cx_destroy_local_with_active_rq` / `cx_destroy_remote_with_active_rq` VALUE parity** (the 29.1 presence-only AMEND-C2 boundary CLOSED): close-direction-keyed increment at connection destroy when the active-query list is non-empty. The parent **D-P4** seam-or-boundary resolution: envoy-go's `OnDestroy()` carries no close direction today → either a minimal close-direction accessor (the ADR-0219 no-ripple discipline) OR a coverage boundary. The 29.2 SPEC RESOLVES D-P4.
5. **`emit_dynamic_metadata` emission** (parsed at 29.1, consumed here): per decode pass, a Struct keyed by collection name → ListValue of operation strings (`"insert"`/`"query"` in v1.37.2), cleared at the top of every pass, under namespace `envoy.filters.network.mongo_proxy` → the ADR-0217 Bucket (the THIRD production writer; `Set` + per-pass clear). Differential-invisible → unit-test proof + a BEHAVIOR_CONTRACT note (no fixture surface).
6. **Fixture `0051-mongo-responses`** (cross-side; the gauge asserted at quiesced points; the `cx_destroy_*` value-parity arms) + **the `TCPMongoResponder` BackendKind — anticipated value 30** (29.1 used the existing silent `TCPSink` = 28 per AMEND-C4; 29.2 needs a responder that emits mongo OP_REPLY frames — a NEW BackendKind, the 28.2 `TCPZKResponder` = 29 precedent). The response-decode fuzzer if SPEC D-P6 pins one (the 40th).
7. **The dynamic-HISTOGRAM coverage boundary (ADR-0060):** the per-command/per-collection `reply_num_docs`/`reply_size`/`reply_time_ms` histogram families stay DEFERRED — recorded as a BEHAVIOR_CONTRACT coverage boundary at 29.2 (the response side that would populate them).
8. **The BEHAVIOR_CONTRACT 29.2 bundle + the ADR-0225 §Decision/§Consequences body** in-place (no new ADR number — the §Context already exists; DECISIONS tail STAYS ADR-0226). Stat surface stays **360** (+0 creation — all 23 fixed stats were created eagerly at 29.1; 29.2 wires the response/gauge increments only).

## Load-bearing 29.1 outputs the 29.2 SPEC consumes

- **The active-query list (`dec.queries`)** — written at 29.1 (every OP_QUERY appends `{requestID, collection, command, callsite, start}`), NEVER read at 29.1 (R5). 29.2's correlation reads + erases it. The `start time.Time` was recorded at 29.1 (cheap; avoids a 29.2 struct revision) for the deferred latency.
- **The 23-stat EAGER roster** — `op_reply`/`op_reply_cursor_not_found`/`op_reply_query_failure`/`op_reply_valid_cursor`/`op_command_reply` + the `op_query_active` gauge all exist-at-zero from config-load (creation parity, D-P1) — 29.2 increment-wires them with NO new creation.
- **The `mongo.` four-rule name.go tag-extractor arm** — already handles the fixed reply counters (`envoy_mongo_op_reply{prefix}`) and the gauge `# TYPE … gauge` line; 29.2 adds no new name.go arm.
- **The both-directions filter glue** — `OnWrite` is a pure no-op stub at 29.1; 29.2 replaces the stub body with the response decoder feed (the write-side reassembly buffer) while keeping unconditional `Continue` (R3 extends to the write side).

## What this session must do

1. Re-establish a worktree (`feedback_git_worktrees`); confirm the clean baseline (`go build ./...` + `go test ./internal/... -count=1`).
2. Run `superpowers:writing-plans` to author the 29.2 SPEC: create `docs/envoy-go/phases/29.2-network-filter-mongo-responses-and-correlation/` (README + SPEC.md); pin the response-decode wire layout (parent §11.4/§11.5 OP_REPLY framing) + the correlation/mutex design + the gauge quiesced-point assertion strategy + the D-P4 close-direction resolution + the `emit_dynamic_metadata` namespace/keys + the `0051` fixture arms + the `TCPMongoResponder` BackendKind 30 + the ADR-0225 §Context cross-reference; run the spec-document-reviewer; fold advisories.
3. STATE.md advance (active-phase → `phase 29.2 SPEC done`; next-skill → the 29.2 PLAN per SKILL_ROUTING state 2); ROADMAP sub-row 29.2 `planned → in-progress`; rewrite next-prompt.txt for the **29.2-PLAN** cold-start; commit + squash-merge + push.

## Project state at master tip

- **Master tip:** the 29.1-IMPL squash (the controller fills the SHA post-merge; confirm the live tip via `git log --oneline -1`). STATE.md `last-commit` points at it once filled. Substantive predecessors: the 29.1-PLAN squash `89c194a`; the 29.1-SPEC squash `7574219`. origin/master in sync after push.
- **§9 Network-filters family:** rows 26/27/28 DONE; row 29 `in-progress` (29.1 `done`; 29.2/29.3 `planned`).
- **Counts at master tip:** stat surface **360** (+0 at 29.2/29.3); differential fixtures **52** (tail `0050-mongo-boot-reject`; → 54 at 29.2/29.3); fuzzers **39** (→ 39–40); conformance h2spec 53/53 + proxy-wasm 10/10 (as of the 29.1 six-gate, 2026-06-04); DECISIONS.md tail **ADR-0226** (next-free **ADR-0227**; the ADR-0225 body lands at the 29.2 IMPL — no new number).
- **Toolchain:** Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); reference Envoy v1.37.2 (ADR-0008); go-control-plane v1.32.4 (ADR-0008).
- **Known coverage boundaries (carried, non-blocking):** the mongo D-P1 boot-window stat-creation departure (eager vs upstream per-connection — unobservable to the differential); the `stats.IsValidName` guard on un-nameable wire-derived dynamic segments (user-approved; fuzz-validated; differential-unaffected); the mongo dynamic-HISTOGRAM families deferred (ADR-0060 — recorded at 29.2); fault delay + access log + drain + `cx_drain_close` + `delays_injected` are 29.3's; mongo runtime-key gating unmirrored (29.3 record); the mongo access log not differentially compared (AMEND-B10; 29.3 record); `tcp_proxy` `downstream_cx_*` unmirrored; histograms deferred project-wide (ADR-0060).

## Read first (in order)

1. THIS FILE — the 29.2-SPEC cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 29.1 IMPL done`; next-skill `superpowers:writing-plans` (the 29.2 SPEC); next-free `ADR-0227`.
3. `docs/envoy-go/DECISIONS.md` ADR-0225 §Context (the 29.2 charter; the §Decision/§Consequences body lands at the 29.2 IMPL) + ADR-0224 §Decision/§Consequences (the 29.1 request side this builds on — the active-query list + the eager roster + the four-rule arm) + ADR-0223 (the zookeeper per-connection-mutex precedent the correlation mutex mirrors) + ADR-0217 (the dynamic-metadata Bucket) + ADR-0044/0052/0060.
4. `docs/envoy-go/phases/29.1-network-filter-mongo-wire-and-requests/SPEC.md` + `PLAN.md` — the request-side design 29.2 extends (esp. §3.5 codec / §3.6 stats / §3.7 the active-query list + the 29.2 forward-pointers; §11.2 the D-P2 probe data; the Reply/CommandReply recognized-not-decoded posture).
5. `docs/envoy-go/phases/29-network-filter-mongo-proxy/SPEC.md` — the parent SPEC (§11.4/§11.5 the OP_REPLY wire layout the response decoder mirrors verbatim; §7 stat surface; AMEND-B11 dynamic metadata; AMEND-B12 close-direction; D29-9/D29-10/D29-12 the 29.2-owned pins).
6. `docs/envoy-go/SKILL_ROUTING.md` + the lifecycle docs — state 2 (writing the SPEC for a sub-phase whose directory does not yet exist; the parent BRAINSTORM drafted the §Context) + the split policy.
7. `internal/filter/network/mongoproxy/` (the 29.1 package — `codec.go` the response decoder extends; `filter.go` the `OnWrite` stub + the 29.2 mutex forward-pointer comment; `stats.go` the reply counters + the gauge; `bson.go` reused for reply-doc walks) + `internal/filter/network/zookeeperproxy/decoder.go` (the consumer-#1 response-decoder + per-connection-mutex precedent the 29.2 design mirrors).
8. `test/fixtures/0048-zookeeper-responses/` (the cross-side RESPONSE fixture + the `TCPZKResponder` BackendKind 29 precedent for `TCPMongoResponder` BackendKind 30) + `test/fixtures/0049-mongo-requests/` (the 29.1 request driver + the LE wire/BSON builders shared verbatim with `0051`) + `test/differential/fixture/fixture.go` (the BackendKind roster; StatsAsserter; MultiListenerDriver).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — execution-style + worktree + push + per-task gofmt/lint + the network-filter constraints + the differential dispatch/asserter/break-protocol (`-count=1`) + wire-format-both-sides + docker-probe-bridge-network all carry forward into the 29.2 SPEC.
