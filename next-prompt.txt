=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **THERE IS BANKED MID-LIFECYCLE WORK AT THIS TIP: PHASE 85 (`h2spec-selector-repair`) IS OPEN AT LIFECYCLE-STATE 1.** The phase-85 BRAINSTORM landed 2026-08-08 (SELF-PICKED; row 85 registered `in-progress` at `ROADMAP.md:147`; `want` bumped 116 -> 117 in the same commit). ⇒ **THIS SESSION PERFORMS THE PHASE-85 SPEC.** `STATE.md`'s `next-skill:` reads `SPEC — phase 85 (h2spec-selector-repair)` — that IS your instruction. The SPEC's obligations are enumerated in `BRAINSTORM.md` §4 and §10 (seven open questions; ADR-0307 §Context drafted STATUS `PROPOSED`; an ENUMERATION-not-a-scaling of cost; the "53/53" citation sweep).

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=117 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **`want` IS NOW 117** — the phase-85 BRAINSTORM added row 85 and bumped it 116 -> 117 in the same commit (precedent: 84's BRAINSTORM 115 -> 116). ⚠️ **AT THIS TIP CHECK (1) PRINTS `NOT DONE: row 85` — THAT IS THE CORRECT, EXPECTED OUTPUT WHILE PHASE 85 IS OPEN.** It goes silent again only at this row's IMPL flip. Check (2) prints **SIX** at the SHIFTED anchors **`:195 :201 :207 :217 :223 :231`** (the row insert moved every family paragraph down one line — re-derive them, they drift with every edit). Check (3) is SILENT. ⇒ the sentinel does NOT fire. ⚠️ **A SPEC CHANGES NONE OF THIS: it leaves `ROADMAP.md` BYTE-UNTOUCHED and `want` STAYS 117** (0 of the last 8 SPECs touched ROADMAP; SPEC-84 was verified byte-untouched with firing NCs). If your session's diff touches `ROADMAP.md` at all, that is a FINDING to justify, not a routine edit.

⚠️ **THE DOCTORING NCs REMAIN MANDATORY EVERY SESSION** — with check (3) silent and check (1) printing a single expected line, a broken check is nearly indistinguishable from a healthy one. Both:
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md at want=117: expect BOTH "NOT DONE: row 62" AND "NOT DONE: row 85"
```
```sh
# the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```

⚠️ **AND WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -oiE '-family row'` fails with `grep: amily row: No such file or directory` and the surrounding arithmetic prints `base=0 now=0 delta=0`, **which reads exactly like "no change".** Observed live at FOUR phase-84 stages. **Always pass `--` before the pattern**, and remember the counts are form-dependent: **95 occurrences vs 67 LINES** at this tip (`grep -c` counts LINES). ⚠️ **Row 85's summary deliberately reads `Operational-tooling-family MAINTENANCE row`, which does NOT match either the check-(3) pattern or `-family row`** (the rows-76/78 maintenance precedent) — so `-family row` stayed 95/67 across the row insert. **A gate that reads zero on BOTH sides is not evidence of invariance.**

**SENTINEL RE-RUN MECHANICALLY AT THE PHASE-85 BRAINSTORM CLOSE (2026-08-08, base `84c16c65`), BEFORE AND AFTER THE ROADMAP EDIT. IT DOES NOT FIRE; `stop` was NOT created** (`ls stop` => `No such file or directory`, both sides). ACTUAL output, recorded rather than predicted:
- BEFORE, at `want=116` on **234 lines / 116 data rows**: (1) SILENT · (2) SIX `:194 :200 :206 :216 :222 :230` · (3) SILENT.
- AFTER, at `want=117` on **235 lines / 117 data rows**: **(1) `NOT DONE: row 85`** — correct while the phase is open · **(2) SIX — `:195 :201 :207 :217 :223 :231`** (⚠️ do NOT "fix" it and do NOT forecast a decrease — it has NEVER gone down across ~40 phases, history `0 -> 1 -> 3 -> 4 -> 5 -> 6`: the candidates sentences are a WINDOW onto a larger deferred backlog, not an inventory of it) · **(3) SILENT.**
- **ALL NCs FIRED, BOTH SIDES:** row-62 doctoring => `NOT DONE: row 62` (after-side: rows **62 AND 85**), `NC LANDED? [ in-progress ]` inspected first; `want` off-by-one => `GATE FAIL: examined 116 data rows, expected 115` before / `examined 117 data rows, expected 116` after; check-(3) doctoring (residual confirmed **0** first) => `NEVER OPENED: gRPC` while `WASM` correctly stays silent; check-(2) one-arm strips => **6 -> 5** (short) / **6 -> 1** (long), **never 6 -> 0** (a one-arm strip is NOT an NC for the union).
- **LEAK CHECK, whole-file before/after counts:** lines 234 -> 235 · rows 116 -> 117 · check-(2) union 6 -> 6 · `-family row` 95/67 -> 95/67 · `gRPC-family row` 2 -> 2 · `Operational-tooling-family row` 3 -> 3 · escape-aware ARM-A flags **119 and 131 ONLY** (row 85 at `:147` NOT flagged).
- ⚠️ **Re-run all three checks and both doctoring NCs yourself and record what YOU see — before AND after any `ROADMAP.md` edit (a SPEC should make none).**

**RE-EVALUATED MECHANICALLY ON EXPLICIT HUMAN INSTRUCTION 2026-08-09 (post-85-BRAINSTORM, at tip `21a5a3c2` == `origin/master`). IT DOES NOT FIRE; `stop` was NOT created (`ls stop` => `No such file or directory`, checked at the repo root AND the worktree root).** ACTUAL output, recorded not predicted: input measured **235 lines / 117 data rows** first; **(1) `NOT DONE: row 85`** at `want=117` — the single EXPECTED line while phase 85 is open at lifecycle-state 1, with the denominator assertion printing nothing; **(2) SIX — `:195 :201 :207 :217 :223 :231`**; **(3) SILENT**. The condition is a CONJUNCTION and checks (1) and (2) print, so the sentinel does NOT fire — `STATE.md`'s `next-skill:` reads `SPEC — phase 85 (h2spec-selector-repair)`, i.e. a next stage EXISTS and the loop continues. All FOUR NCs fired: row-62 doctoring => `NOT DONE: row 62` AND `NOT DONE: row 85` (with `NC LANDED? [ in-progress ]` inspected first); `want=116` => `GATE FAIL: examined 117 data rows, expected 116`; check-(3) doctoring (residual `gRPC-family row` confirmed **0** first) => `NEVER OPENED: gRPC` while `WASM` correctly stayed silent; check-(2) one-arm strips => **6 -> 5** (short arm) / **6 -> 1** (long arm), never 6 -> 0. This file was already rolled to the **PHASE-85 SPEC** at the BRAINSTORM close and STAYS there — that is the next actionable stage. `want` STAYS **117** (check (1) goes silent again only at this row's IMPL flip); `ROADMAP.md` BYTE-UNTOUCHED by this re-evaluation.

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (ugrep under the hood). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Reproduced at SEVENTEEN separate tips.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **THE METHOD BINDS; carry no standing discrepancy figure and no standing absolute.** ⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS; print FILE NAMES, never a count.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files; use `-v --no-index`, and `git status` is the discriminating check). ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **`grep -c` ON ZERO MATCHES IS TOOL-SPECIFIC** (only `git grep -c` prints nothing; all exit 1 — capture the value, never chain on the exit code). ⚠️ **`grep -c` COUNTS *LINES*, NOT *OCCURRENCES*.** ⚠️ **`\t` IN GNU ERE IS A LITERAL `t`** — use `-P`. ⚠️ **A `git grep` PATHSPEC ENDING IN `/` READS A FAIL-UNSAFE ZERO** (shape 33) — use the shell-glob form. ⚠️ **AN UNANCHORED PANIC-GREP FALSE-FIRES 14 TIMES ON A FULLY-GREEN DIFFERENTIAL LOG** — **the canonical gate is ANCHORED: `^panic:|DATA RACE|SIGSEGV`. A gate that fires on a green log is worse than no gate.**

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`** with FOUR blind spots (an em-dash, a DOT in `http-1.1`, two LETTER-suffixed ids). ⚠️ **EXECUTED on scratch copies at the phase-77 PLAN: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** ⚠️ **DO NOT restore the regex form.** With row 85 open, check (1)'s single expected line is `NOT DONE: row 85` — **any OTHER row id appearing there is a finding.**

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

⚠️ **Run the union form, never one half of it.** ⚠️ **A ONE-ARM STRIP IS NOT AN NC** (6 -> 5 short / 6 -> 1 long; never 6 -> 0). ⚠️ **The documented trap's stated MECHANISM is refuted while the trap is real** — the inflation comes from ROADMAP table rows carrying BRAINSTORM prose. **Cite the command; do not re-inherit the explanation.** ⚠️ **The line numbers drift with edits — re-derive them; the current six are `:195 :201 :207 :217 :223 :231`.** ⚠️ **These six paragraphs remain the final sentinel blocker, and any edit near them is a sentinel-affecting edit.** The phase-85 BRAINSTORM classified them mechanically: all six are standalone family PROSE paragraphs (not rows), naming ~42 distinct candidates; every "retire the paragraphs" shape is doctrinally foreclosed (`reference_sentinel_matcher_string_self_clears`); the ONLY legitimate move they admit is chartering ONE real item — which is what row 85 does NOT do (its candidate came from SPEC-84 §13, OUTSIDE the windows; no window sentence is edited by this phase, at any stage).


==============================================


## STATUS AS OF 2026-08-08: PHASE 85 (`h2spec-selector-repair`) IS **OPEN AT LIFECYCLE-STATE 1** (BRAINSTORM done). ROW 85 IS **`in-progress`** at `ROADMAP.md:147`. **THIS SESSION PERFORMS THE PHASE-85 SPEC.**

**What the 85 BRAINSTORM landed (2026-08-08, base `84c16c65`, branch `phase-85-brainstorm`).** Docs-only, ZERO `.go`: `docs/envoy-go/phases/85-h2spec-selector-repair/BRAINSTORM.md` + `PROGRESS.md` (new), ROADMAP row 85 (ONE new data row, 234 -> 235 / 116 -> 117), this file's `want` 116 -> 117, `STATE.md` rolled IN PLACE (64 lines, seven singleton fields verified), `STATE_HISTORY.md` **466 -> 468** (BRAINSTORM-84 evicted at the five-entry cap; strictly append-only, numstat `2 0`, base a byte-exact PREFIX via `cmp`; absence guard 0/0 pre-append with THREE controls re-derived FROM THE FILE: phase-82-IMPL-done **4/4** — drifted UP from 3 again, a control's own count is a LIVE FIGURE — phase-83-PLAN-done **2/2**, invented label **0/0** non-discriminating). `DECISIONS.md` and `BEHAVIOR_CONTRACT.md` **BYTE-UNTOUCHED**; **no ADR consumed, next-free stays `ADR-0307` (from the tail).** THREE investigation agents (h2spec BY EXECUTION in a detached worktree, band 46500-46599, removed and verified; backlog paragraphs read-only; deferral sweep read-only); two agent claims did NOT survive as stated (BRAINSTORM §5.3).

### ⚠️ WHAT THE 85 BRAINSTORM PROVED — CARRY THESE, THEY ARE MEASUREMENTS

- ⚠️ **THE GATE WAS RUN BOTH WAYS AT `84c16c65`** (pinned `summerwind/h2spec@sha256:5f4a65…`): as committed **53/53 green in 3.77 s** (byte-for-byte the 18 suites of `CONFORMANCE_PINS.md:37-57`, zero 6.x); with the NINE slash-form section-6 strings dot-corrected (nine ONE-CHARACTER edits, measured sufficient) **95 tests / 90 passed / 1 skipped / 4 FAILED**, gate exits 1. Section 6 alone: **42/37/1/4**; the skipped case is 6.9.2/2. `--dryrun -S http2/6/10` prints ZERO case lines and exits 0; `-S http2/6.10` lists 6 CONTINUATION cases.
- ⚠️ **THE FOUR FAILURES ARE PRODUCTION RFC 9113 MUST VIOLATIONS**, causes read at the code sites: **6.5.2/1** (ENABLE_PUSH other than 0/1), **6.5.2/3** (MAX_FRAME_SIZE below 16384), **6.5.2/4** (above 2^24-1) — all want GOAWAY(PROTOCOL_ERROR), all get SETTINGS ACK, because `onSettings` (`internal/filter/hcm/h2/conn.go:507-538`) applies every value with ZERO validation; **6.9.2/1** — expected `DATA (length:1, flags:0x00)`, got `DATA (length:3, flags:0x01)`: live streams' send windows are never adjusted on a SETTINGS_INITIAL_WINDOW_SIZE change. ⚠️ **The gap is PARTIAL: 6.5.2/2 and 6.9.2/3 PASS** — the fix is three identifier checks + one window walk, not a rewrite. These are MUSTs: pinning them as accepted departures would need a departure ADR, and the BRAINSTORM does not propose that.
- ⚠️ **TWO GATE DEFECTS BEYOND THE SELECTORS, BOTH NEW:** (a) the JUnit `<failure>` parse gap — `tc.Failure` parsed nil for ALL FOUR failing cases, `assertThreshold`'s per-case `FAILED:` lines never printed, failing-case identity is currently recoverable only from container logs; (b) ADR-0051's 6.6 PUSH_PROMISE exclusion is **VACUOUS** — the pinned image ships NO 6.6 suite at all (the 8.2 probe covers client-sent PUSH_PROMISE and runs green).
- **Selector anatomy re-verified by direct read:** `thresholdSections` at `test/conformance/h2spec/h2spec.go:21-37`; NINE declared section-6 strings; line 30 is the `http2/6/6` comment — **"nine" is right, SPEC-84's "ten" (`SPEC.md:52,:457`) is wrong**, ADR-0306 ¶4 already corrected it; do not re-inherit "ten".
- **The zero-case blindness:** `if s.Tests == 0 { continue }` at `h2spec_test.go:310` — nothing anywhere asserts a declared selector produced >= 1 case. **CI:** zero h2spec/conformance refs under `.github/` (exit 1); `TestH2Spec` skips under `-short`; CI's test job runs `-short` and the differential job runs only `./test/differential/...`.
- ⚠️ **h2spec 6.10 is MEASURED NOT TO GATE the CONTINUATION discard** — 6/6 PASS with the defect live at `conn.go:255-259` (re-read byte-identical at this tip). And **NEW: `h2/client.go`'s response read-loop is CONTINUATION-blind too** (no `ContinuationFrame` arm in the frame switch; `:428` decodes only the first fragment) — the deferred CONTINUATION row is TWO-SIDED (`reference_one_sided_gate_for_a_two_sided_fix`). It stays deferred; this row does not fix it and its gate does not cover it.
- **Rejected alternatives, re-derived at this tip and recorded in BRAINSTORM §2 — do NOT re-litigate:** candidate (i) "the six backlog paragraphs" is **NOT A ROW** (prose windows; retire-shapes foreclosed); the CONTINUATION two-sided repair (~100-250 prod + 400-800 test, natural NEXT row); the stat-surface recount (cheapest, repairs no gate; ⚠️ **the contradiction WIDENED: `STATE.md:33` 1205 vs ADR-0300/0301 headers 1207, both live, both DOC-SOURCED**); `ssl.connection_error` (+444 whole-`.go` VERIFIED floor; the "smallest" framing repeated the phase-75 category error); `test/conformance/grpc/` (9/26 reachable); `validate` nil-`sdsProvider` (best sub-row bug, available); REVIEW.md restoration (process-not-product).

### ⚠️ WHAT THE SPEC OWES (BRAINSTORM §4 — SEVEN QUESTIONS, plus conventions)

1. **Q1 CI enrollment** — should the repaired gate run in CI (docker already in the differential job; container 3.77 s locally)? Flake posture UNMEASURED; measure or defer in writing.
2. **Q2 the reference's own section-6 failures** — SPEC-84 recorded "a disjoint set of three" on the reference; **UNVERIFIED at this tip, carried as a QUESTION not a fact.** Run the pinned `contrib-v1.37.2` on the corrected selectors; any case the reference ALSO fails needs an explicit fix-to-spec vs fix-to-reference decision.
3. **Q3 sequencing** — selectors + zero-case guard land RED unless the four production fixes land in the same leg. Sequence so NO intermediate tip has a red gate.
4. **Q4 6.9.2/1 blast radius** — the send-window delta walk touches live-stream flow control; enumerate which existing tests/fixtures exercise INITIAL_WINDOW_SIZE changes (suspected near zero — itself a finding); decide unit+h2spec vs a differential fixture.
5. **Q5 threshold structure** — the zero-case guard covers matching-nothing; it does NOT cover a suite silently dropping out of the declared list. Decide whether `assertThreshold` gets named-suite enumeration.
6. **Q6 the vacuous 6.6 exclusion** — delete or annotate; the ADR records the fact either way.
7. **Q7 the "53/53" citation sweep** — enumerate every doc citing h2spec 53/53 (ADR-0051, ADR-0052 `:1803`, `CONFORMANCE_PINS.md`, `BEHAVIOR_CONTRACT.md`, ROADMAP row summaries, `STATE.md`) and state which the row's ADR reconciles vs records. ⚠️ **ADR-0052 `:1821` makes an ADR the MANDATED vehicle for any `BEHAVIOR_CONTRACT.md` statement change.**
- **ADR-0307 §Context drafted STATUS `PROPOSED`** (a SPEC convention; the strict guard then correctly reads 1, ARMED, until the IMPL completes it). **Derive next-free FROM THE TAIL** (headings+1 collides at the ADR-0209 gap; tail is ADR-0306 COMPLETE).
- **Cost: ENUMERATE, do not scale.** Floor from the BRAINSTORM: ~9 selector chars + ~15-25 harness + ~40-80 production (`conn.go` only) + ~100-200 unit-test + ~25 docs + one ADR. ⚠️ **Every figure is a LOWER BOUND** — `reference_measured_prototype_is_a_lower_bound` fired on SIX consecutive rows (84.1 realized 1.50x/1.37x); the cause is under-ENUMERATION. The likeliest unpriced lines: Q2's reference run, Q4's flow-control coverage, and any `BEHAVIOR_CONTRACT.md` reconciliation Q7 forces.
- **A SPEC is docs-only**: ZERO production `.go`, ZERO test `.go`, `ROADMAP.md` BYTE-UNTOUCHED, `want` stays 117. Prototype/probe code lives in DETACHED worktrees and is reverted with sha256 proof (the 84.2-PLAN precedent).

### ⚠️ THE SIX-GATE STANDING POSTURE — NAME THE DEPARTURES, DO NOT CLAIM COMPLIANCE

**(a)/(b)** — a docs-only SPEC owes these only as POSTURE statements (zero `.go` changed means not-exercised, not green); the suite is **121** fixtures and this row anticipates **+0** (the differential surface is untouched — the h2spec gate is `test/conformance/`, not `test/differential/`). **(c)** — `test/conformance/grpc/` stays DEFERRED IN WRITING (SPEC-84 §4); proxy-wasm 10 of 16 families; ⚠️ **h2spec MUST be stated WITH the scope caveat until this row's IMPL lands: "NO ROW MAY CITE h2spec 53/53 AS FRAME-LEVEL EVIDENCE" — this row is the one that retires that caveat, and citing its own future repair as present-tense evidence is the exact defect it fixes.** **(d)** — say "vacuous" not "green" when nothing introduces a parser/codec/filter; fuzzers **55** in **48** files, all under `internal/`, zero under `test/` (the §7.4-location violation stays named). **(e)** — `INNER_EXIT` mandatory on every differential launch AND `go test ./...` (a SPEC typically runs neither; if a probe runs them, cite honestly). **(f)** — STANDING DEPARTURE, named not claimed: no `REVIEW.md`; **37 of 126** phase dirs carry one, none since 25.3. Normative cites: §6.1 `:285`, §6.2 `:294`, §7.3 `:346-351`, §7.5 `:357`, gates `:360-365`. ⚠️ **`/BOOTSTRAP_PROMPT.md` WITH A LEADING SLASH RESOLVES NOWHERE** — the file is `<repo>/BOOTSTRAP_PROMPT.md` (522 lines); the `docs/superpowers/plans/` copy is the divergent GENERATOR PLAN with a NON-constant offset.

**STAT SURFACE — the 85 BRAINSTORM landed +0** (docs-only; no enumeration owed for a stage with zero `.go`). The canonical measure remains **call-site enumeration (208/36 at the 84.2 close — cite 208/36, NEVER 208/84)**, re-derived at base AND tip when a stage touches `.go`; **NEVER via `TestNoNewStat*`** (proven blind by execution at the 84.1 PLAN). The absolute is **1207, DOC-SOURCED from `BEHAVIOR_CONTRACT.md`'s ledger tail** — ⚠️ **and the 1205-vs-1207 contradiction (`STATE.md:33` vs ADR-0300/0301 headers) is now RECORDED as WIDENED; only the DELTA is ever asserted.**

### ⚠️ ADR + DOCUMENT CONVENTIONS

**The DECISIONS tail is `ADR-0306`, STATUS `COMPLETE`. Next-free is `ADR-0307` — DERIVE FROM THE TAIL** (headings+1 COLLIDES at the single gap, ADR-0209). ⚠️ **The 85 BRAINSTORM consumed NO ADR and left `DECISIONS.md` BYTE-UNTOUCHED** (verified: `git diff` on the two files = 0 bytes). ⚠️ **Do NOT use the loose form `^> \*\*STATUS: .*PROPOSED`** — every hit is a false positive; the strict guard reads **0** at this tip and that is CORRECT (it arms 0 -> 1 when this session drafts ADR-0307 §Context — that 1 is then a LIVE POINTER a later stage must not "fix"). **Carry no whole-file count of the loose matcher.**

### ⚠️ COUNTS AT THIS TIP — RE-DERIVE MECHANICALLY, NEVER COPY

**HOUSE RULE: every number below is a LIVE FIGURE. Re-run it in your own worktree with its negative control observed.**

- **differential fixtures 121** — numeric tail `0119-grpc-unary-trailers`, next-free `0120`. TWO reference-port conventions COEXIST (`10<index>` for 0103+; sequential 19xxx for 0040-0102) — STATE THE CONVENTION YOU USE. **This row anticipates +0 fixtures.**
- **`ROADMAP.md` 235 lines / 117 data rows**; row 85 at `:147`; check-(2) anchors `:195 :201 :207 :217 :223 :231`; `-family row` **95 occurrences / 67 LINES**; `gRPC-family row` **2**; `Operational-tooling-family row` **3**.
- **`DECISIONS.md` 17990** lines · **305** `^## ADR-` headings · tail **`ADR-0306` COMPLETE** · `^## ADR-0307` = **0** · `^---$` **216** · STATUS census **19** (matcher `^> \*\*STATUS: `, a BLOCKQUOTE).
- **`BEHAVIOR_CONTRACT.md` 5955** · **`STATE.md` 64** (seven singleton fields verified at this close) · **`STATE_HISTORY.md` 468** · **`BOOTSTRAP_PROMPT.md` 522** at the repo root · phase dirs **126**, `REVIEW.md` **37**.
- **h2spec at this tip (BY EXECUTION):** broken selectors **53** cases · corrected **95** (§6: 42/37/1/4) · declared section-6 selector strings **9** + 1 comment · h2spec image pinned by sha256 in `h2spec_test.go`.
- **stat call sites 208 / 36 production files** (at the 84.2 close) · **fuzzers 55** in **48** files · **BackendKind tail 38** over **39** declarations (do NOT "fix").
- ⚠️ **STILL CONTESTED, NO NUMBER CARRIED:** the `STATE_HISTORY.md` archive-gap total · production `stats.IsValidName` guard sites · `allCallbacksNoOp` occurrences · the loose `PROPOSED` matcher's whole-file count · whether 84.1's realized net counts its committed probe (3414 vs 3124, neither adjudicated) · **the stat-surface absolute (1205 vs 1207 — WIDENED, see above).**

### ⚠️ DOCUMENTARY DEFECTS — carried forward, still deliberately NOT fixed

- ⚠️ **`ADR-0051` §2 is FACTUALLY WRONG** about the h2spec sections the gate runs (`DECISIONS.md:1747-1750`; append-only ⇒ RECORDED, NOT FIXED — **this row's ADR-0307 is the designated correction vehicle**); its PUSH_PROMISE rationale is moot twice over, and the 6.6 exclusion it defends is VACUOUS (no 6.6 suite exists in the pinned image). `ADR-0052 :1803` repeats the wrong scope claim.
- ⚠️ **`ADR-0058` cites `internal/filter/hcm/actions.go:routerActionH2.doH2` TWICE and the symbol is not there** — `routerActionH2` is at `router_h2.go:254`; the emit is `writeH2Reply` at `h2dispatch.go:720`. Do not propagate either.
- ⚠️ **`ROADMAP.md` malformation: rows at lines 119, 131, 140 lose summary content in GFM render** — re-verified at this close: ARM-A (escape-aware `pieces != 8`) flags **119 and 131 ONLY**; row 85 at `:147` is NOT flagged. Naive `NF != 8` is wrong in both directions; no ARM-B count is carried. INVARIANT-BLOCKED ⇒ a GUARD, not a fix.
- **`STATE.md` §Project counts SELF-CONTRADICT §Current and are FROZEN at phase 76** (fixtures 119; stat 1205; "next-free port 10450"). **Do not source from §Project; do not fix it.**
- ⚠️ **`test/differential/harness_test.go:208`'s port inventory is STALE** (true set reaches 10449+; claims 10000..10447).
- ⚠️ **The two `//nolint:gosec` directives in `body.go` are INERT** (`.golangci.yml` `disable-all: true` + 9 linters; misspell locale US) · ⚠️ **The xDS config-seam cycle guard is NOT AUTOMATED ANYWHERE** · ⚠️ **`internal/wasm/doc.go:219` carries TWO errors** · **`ROADMAP.md` cites `github.com/esalaine/envoy-go/…` FIVE times** (module is `github.com/pgdad/envoy-go`) · **`rbac.go:50` contains the token `F2`** as a phase-26 fork label — do not grep-anchor on `F2` in that package.

### ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS YOURSELF AND RECORD ACTUAL OUTPUT** — both doctoring NCs every time; check (1)'s expected line is exactly `NOT DONE: row 85` and nothing else.
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** Lineage: phase-84 B22/S26/P31/I-20-tasks/P13/I-four-arm-fixture; phase-85 **B: three agents, the gate run both ways, six load-bearing establishments, two agent claims rejected**. ⚠️ **A refutation is a claim too, and a controller/task BRIEF is a claim too** — the 85 BRAINSTORM rejected an agent's "smallest row" framing precisely because it repeated the phase-75 category error.
3. **BEFORE ANY LINE-ADDING EDIT TO A HEAVILY-CITED DOC, ENUMERATE THE CITES YOUR INSERT WOULD SHIFT** — and carry no whole-file count of a pattern your own prose spells.
4. **LINE ANCHORS DRIFT; SYMBOL ANCHORS DID NOT.** Re-verified at the 85 BRAINSTORM: `thresholdSections` **`h2spec.go:21-37`** (nine section-6 strings; comment at `:30`), the zero-case skip **`h2spec_test.go:310`**, the CONTINUATION discard **`conn.go:255-259`**, `onSettings` **`conn.go:507-538`**. **RE-VERIFY EVERY CITE AT YOUR TIP.**
5. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Still live: the SDS dial-budget flake (TWO packages) · the `internal/cluster` `-race` outlier · `internal/httpclient TestOptions_ZeroValue_NoOpDefaults` · the driver-receiver port race (roster 42; `0081` binds `0.0.0.0:42039`) · the TWO 84.2-era flakes (`0079` held-stream 502; `internal/wasm` A4), both cleared on re-run, neither classed — **a recurrence of either is worth recording.**
6. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only). The h2spec probe pattern from this BRAINSTORM: testcontainers pulls the pinned image by sha256; a detached worktree with the nine chars flipped is the cheap way to run the true scope — **revert with sha256 proof**.
7. **MACHINE-STATE DELIBERATELY IN PLACE (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-85-*` worktree should outlive its stage close — check `git worktree list`; expect only `master` plus your own.**
8. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`.
9. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL.**
10. **A DEFERRED CANDIDATE'S COST GOES STALE** — re-derive at the current tip; **a MEASURED PROTOTYPE IS A LOWER BOUND** (six consecutive firings; under-ENUMERATION, not under-scaling).

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — take it from `git rev-parse master`, **NOT from a SHA quoted in this file**. ⚠️ **THE BARE SUBJECT-LOCATE GREP IS CONTAMINATED BY THIS FILE'S OWN PROSE** — router commits discuss the phase in their BODIES. **USE THE SLUG-ANCHORED FORM**, stage-exact:
```sh
git log --grep '^phase 85 (h2spec-selector-repair)' --format='%h %s'   # 1 at this tip: the BRAINSTORM. An invented slug returns 0.
git log --grep '^phase 84 (grpc-unary-response-trailers)' --format='%h %s'  # 6 — a CLOSED phase, use it as a FIRING CONTROL
```
⚠️ **The count comments above are LIVE FIGURES that have gone stale in this file before — VERIFY before relying on them.** Take the NEWEST line (`| head -1`).

Subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** and tear down docker **BY NAME**. ⚠️ **Band private ports outside BOTH `20000-31007` (subject blocks) AND `11000-14999` (backends + subject admin), and clear of the static fixture ports (19 bands, 10000-19172).** The `42000-46099` band has been used safely (phases 80-84) — ⚠️ **but `0081`'s ALS receiver binds `0.0.0.0:42039` inside it**; the 85 BRAINSTORM's h2spec probe used `46500-46599` cleanly. ⚠️ **If two agents share ONE worktree they must stage by EXPLICIT PATH and never run an unscoped `git restore`; PREFER ONE WORKTREE PER AGENT.** Per-task `gofmt` + `golangci-lint` on touched packages. ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch.**

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root. **Observed live AGAIN at this stage (every compound command's trailing `pwd` printed the repo root).** Use `git -C <abs-worktree-path>` for every git command; put any needed `cd` FIRST in a compound command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — `git add` ALONE WILL NOT STAGE IT. USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — restoration verified by sha256 against the base blob, never by eye; injection-site occurrence asserted `== 1` before writing; work DETACHED so a failed revert cannot dirty a branch.

⚠️ **THE ARCHIVE-ABSENCE GUARD — USE THE ROBUST FORM.** Anchor on the bullet, allow ANY run of characters before the backtick-quoted target, corroborate with a whole-file fixed-string search. ⚠️ **NC IT WITH A *REAL* TARGET re-derived FROM THE FILE first** — an invented label reads 0 on every arm and looks exactly like agreement. ⚠️ **A CONTROL'S OWN COUNT IS A LIVE FIGURE:** `phase 82 … IMPL done` read 3 at the 84.2 IMPL close and **4** at the 85 BRAINSTORM close — each eviction entry citing it adds a line. **Re-derive it; never copy it.** ⚠️ **DESCRIBE the guard's regex in the archive entry label; never spell it.** ⚠️ **Verify the archive edit is STRICTLY APPEND-ONLY**: `git diff --numstat` = `N 0` AND the base file a byte-exact PREFIX (`cmp` the base blob against `head -<baselines>` of the new file).

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY** (seven singleton fields, each verified singleton at the 85 BRAINSTORM close; §Recent at its five-entry cap). ⚠️ **`next-skill:` reads `SPEC — phase 85 (h2spec-selector-repair)` — that IS your instruction.** ⚠️ **§Project counts SELF-CONTRADICT §Current — do not anchor on them, do not fix them.**
2. `docs/envoy-go/phases/85-h2spec-selector-repair/BRAINSTORM.md` — the charter: §3 scope, §4 the seven questions the SPEC owes, §5 what is already established BY EXECUTION (do not re-prove; do re-verify cites).
3. `docs/envoy-go/ROADMAP.md` — **§Schema first** (`:18`), then row 85 at `:147`, then the check-(2) lines at `:195 :201 :207 :217 :223 :231` (sentinel-sensitive; a SPEC touches NONE of this file).
4. `test/conformance/h2spec/h2spec.go` + `h2spec_test.go` — the selector list `:21-37`, the zero-case skip `:310`, the JUnit parse path (`assertThreshold` + testcontainers plumbing); and `internal/filter/hcm/h2/conn.go` — `onSettings` `:507-538`, the frame switch `:252-262`.
5. `BOOTSTRAP_PROMPT.md` — repo ROOT, no leading slash (522 lines). §6.1 `:285`, §6.2 `:294`, §7.3 `:346-351`, §7.5 `:357-367`, gates `:360-365`.
6. `docs/envoy-go/DECISIONS.md` — append-only; tail `ADR-0306` COMPLETE, next-free `ADR-0307` **derived from the tail**; ADR-0051 at `:1721`-ish (§2 the false scope claim, `:1747-1750`); ADR-0052 `:1803` (repeats it) and `:1821` (the contract-change vehicle). The SPEC drafts ADR-0307 §Context, STATUS `PROPOSED`.
7. `docs/envoy-go/CONFORMANCE_PINS.md` — the 18-suite audit table `:37-57` and the 2026-04-25 first-run record; the SPEC's Q7 sweep starts here.

## After the SPEC lands — the roll-forward
A SPEC is docs-only: `ROADMAP.md` BYTE-UNTOUCHED, `want` STAYS **117**, row 85 STAYS `in-progress`. Re-run the sentinel and record what you see: expect **(1) `NOT DONE: row 85`** (and NOTHING else), **(2) SIX** (re-derive the anchors), **(3) SILENT** ⇒ the sentinel does NOT fire and `stop` is NOT created. If the SPEC drafts ADR-0307 §Context, the strict `PROPOSED` guard arms **0 -> 1 — a LIVE POINTER, do not "fix" it**, and `DECISIONS.md` grows by the §Context block (enumerate the cites your insert shifts; the tail heading count 305 -> 306 and next-free becomes ADR-0308 FROM THE NEW TAIL only if a full ADR is added — a §Context-only draft under a new `## ADR-0307` heading DOES move the tail; record what you actually did). **Roll this file to point at the PLAN-85**, carrying: the sentinel state you actually observed with both doctoring NCs, the SPEC's answers to Q1-Q7 (especially the Q3 sequencing decision and the Q2 reference-side measurement), the re-derived counts, and the cost ENUMERATION the PLAN must refute by execution.
