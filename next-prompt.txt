=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, the active phase is fully landed, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action. (The sentinel does NOT apply right now — phase 50 is chartered, its BRAINSTORM + SPEC + PLAN are LANDED, and the next stage is its IMPL.)

**STATUS AS OF 2026-07-05: phase 50 (`stats-sink-dogstatsd-batching`) PLAN is LANDED; the NEXT stage is its IMPL.**
- The phase-50 PLAN landed: `docs/envoy-go/phases/50-stats-sink-dogstatsd-batching/PLAN-50.md` (an 8-task TDD spine mirroring `PLAN-49.md`'s shape — ONE fewer task than phase 49, since no new fuzzer / no new bootstrap-dispatch-arm are needed this row; lifecycle-state 2 → 3; docs-only direct-on-master). It RESOLVES every §11 D-DSDB-* PLAN question (see below). A `plan-document-reviewer` (general-purpose) pass VERIFIED every `bootstrap.go`/`main.go`/`internal/statssink`/`test/helpers/statsdrecv`/`internal/bootstrap/dogstatsd_fuzz_test.go` line citation EXACT against HEAD, independently re-derived the 13-call-site breaking-change claim via a repo-wide grep (confirmed exact, no missed site), and hand-traced the `appendLine`/`flush` packing algorithm for cap=0/oversized-line/boundary correctness (all sound) — after TWO non-blocking folds (a `0093` driver LoC citation off-by-one, `714`→`713`; a `registration_test.go` line-range off-by-one, `:79-98`→`:79-99`). STATE.md active-phase header rolled to `phase 50 (stats-sink-dogstatsd-batching) PLAN done`. **Re-verify with `git log --oneline -6` + `git status`; do NOT assume.**
- **A breaking-change gotcha was caught and resolved DURING this PLAN's own authoring** (before any test code was written against a stale signature): adding a third parameter to `NewDogStatsdSink` breaks ALL THIRTEEN existing call sites across the repo — verified EXACT via a repo-wide grep, not assumed: 11 in `internal/statssink/dogstatsd_test.go` (lines `21,43,68,88,130,178,209,230,249,266,282`), 1 in `internal/statssink/registration_test.go` (`:87`, inside `TestNoNewStat_DogStatsdRegistrationGuard`), 1 in `cmd/envoy-go/main.go` (`:222`). **Do NOT re-derive this list from scratch — re-verify the grep count matches 13 before starting Task 3, then fix the same-package (11+1) sites FIRST as Task 3's mechanical Step 1 (passing `0` for each, preserving every existing test's semantics unchanged), before writing any NEW batching test** — otherwise the package will not compile at intermediate steps.
- **The §11 D-DSDB-* PLAN questions are RESOLVED (do NOT re-litigate; the IMPL EXECUTES these):** D-DSDB-SPLIT → NO sub-split (single flat row, ~80–150 LoC, 8 tasks; escape-valve UNCONSUMED). D-DSDB-CAP → cap `200` + a ~160–170-char literal `backendName` constant (the `0093` `statPrefix="hcm_local"` stays SHORT/unchanged) so the `envoy.cluster_name`-tagged subset lines are ALWAYS forced alone while the `envoy.http_conn_manager_prefix`-tagged lines stay short and co-batch — **Task 6 Step 1 computes the EXACT byte lengths from the ACTUAL rendered lines before finalizing the fixture; if the hand-estimate is off, adjust `backendName`'s length, NOT the cap.** D-DSDB-STATS-FINAL → +0, confirmed via the EXISTING `TestNoNewStat_DogStatsdRegistrationGuard` (its call site gets the mechanical Task-3 fix, NOT a new test). D-DSDB-FUZZER-SEED → NO extra seed added (the existing `max_bytes_per_datagram: 512` seed already exercises the field, now via the accept path — fuzzers stay 52, confirmed at Task 2 Step 5). D-DSDB-BUFFER-TYPE → `strings.Builder` (matches `formatTagSuffix`'s existing convention in the same file).
- Counts at PLAN (re-verify, do NOT assume): stat surface **1200** (non-H2 **1196**) / fixtures **95** / fuzzers **52** / BackendKind **38** / DECISIONS **ADR-0266** (next-free **ADR-0267**).

=> The NEXT action is the **phase 50 (`stats-sink-dogstatsd-batching`) IMPL** (lifecycle-state 3 → done). NOT the termination sentinel. NOT a new PLAN.

==============================================

## The NEXT action — the phase 50 (`stats-sink-dogstatsd-batching`) IMPL
Execute `docs/envoy-go/phases/50-stats-sink-dogstatsd-batching/PLAN-50.md` end-to-end (the 8-task TDD spine) **subagent-driven** (`feedback_execution_style`) in a **FRESH git worktree off master** (`feedback_git_worktrees` · `superpowers:using-git-worktrees`). This is the FINAL stage of phase 50 — its six-gate FLIPS ROW 50 `done` (the sole leg — ADR-0106; NO parent rollup; the Observability family STAYS OPEN) and ANCHORS ADR-0267 §Decision/§Consequences (ADR-0044). Lifecycle-state 3 → done.

**Execution mechanics (the controller discipline — honor verbatim):**
- **Subagent-driven** (`superpowers:subagent-driven-development`): dispatch a FRESH subagent per task (or a small batch); the CONTROLLER reviews between tasks.
- **Fresh worktree off master** (`feedback_git_worktrees`): create the worktree BEFORE Task 1; all task work lands there; the controller verifies the MAIN checkout stays clean + the branch undetached after each task (`feedback_subagent_worktree_detach` · `_path_targeting`).
- **Subagents commit LOCALLY only** (`feedback_subagents_no_push`); the global CLAUDE.md makes them AUTO-COMMIT (`feedback_subagent_autocommit_claudemd`) — do NOT fight it. The CONTROLLER verifies each commit (correct fileset, real non-vacuous tests via `-v` + read assertions, gates green), cleans stray next-task leak files, re-runs the FULL suite on the FINAL frozen HEAD, does the deliberate-break verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_push_to_origin`).
- **Per-task gates** (`feedback_pertask_gofmt_lint`): `gofmt -l` (empty) + `golangci-lint run` on touched pkgs + `go vet` + `go build ./...`.
- **Task 3's own gotcha (folded into the PLAN by its own authoring — the 13-call-site breaking change):** fix ALL 11 `dogstatsd_test.go` + 1 `registration_test.go` `NewDogStatsdSink` call sites (third arg `0`) BEFORE writing any new batching test, so `internal/statssink` compiles at every intermediate step; `main.go`'s 13th call site is a SEPARATE package, fixed at Task 4.
- **The boundary operator is LOAD-BEARING — do NOT implement `>=`.** The comparison is `prospective > cap` (STRICT): a buffer landing EXACTLY at the cap after appending STILL FITS (SPEC-50 §1.1 AMEND-DSDB-BOUNDARY-CONFIRMED, live-proven both directions at cap=75/74). Task 3's exact-boundary unit test is the regression guard — it must FAIL if the operator is flipped to `>=`.
- **D-DSDB-CAP verification (Task 6):** before locking `0094`'s fixture constants, COMPUTE the actual rendered line lengths (a throwaway `len()`/`t.Logf` check) rather than trusting the PLAN's hand-estimated `~160–170` char `backendName` — adjust `backendName`'s length (not the cap) if the estimate is off, to preserve "HCM lines batch, cluster lines don't."
- **Differential discipline (the `0094` task — Task 6):** `-run 'TestDifferential/0094'` NEVER bare (`reference_differential_run_selector`); `-count=1` on EVERY break + `-race` run (`reference_differential_break_protocol_count1`); the full-suite `subject ready: EOF` startup flake is isolatable (`reference_differential_fullsuite_startup_flake`); the Docker bridge + decode-ran proof (`reference_docker_probe_bridge_network`); the extended driver-owned UDP receiver is NOT a new BackendKind (`reference_differential_grpc_receiver_driver_owned`); the live reference is the wire truth (`reference_wire_format_both_sides_see_same_bytes`); two per-side receivers + hard Close (`reference_periodic_sink_differential_two_receivers`); the delta-sink stability barrier (`reference_delta_sink_differential_stability_barrier`); the `hostGatewayIP` LOCAL duplicate reused verbatim from the `0093` driver, NOT an import of `differential.HostGatewayIP` (`reference_host_gateway_ip_docker_desktop`). The CONTROLLER runs the live differential + the two deliberate breaks ((a) force one-line-per-datagram — MUST fail the batching-occurred assertion; (b) silently drop an overflow line instead of flushing-then-appending — MUST undercount the delta-SUM) + the 20/20 flake-soak + the FULL-package `internal/statssink` `-race` ITSELF.
- **The full-package `-race`** (`reference_full_suite_race_after_background_mutator`): the `DogStatsdSink` remains synchronous (no new goroutine — buffering happens entirely within one `Submit` call), but the `Flusher` ticker remains a background mutator — run the `-race` gate over the FULL `internal/statssink` package.

Anticipated counts at the IMPL six-gate (the PLAN's Global Constraints): stat **1200** (+0 — transport-layer only) / fixtures **96** (`0094-stats-sink-dogstatsd-batching`) / fuzzers **52** (UNCHANGED — no new fuzzer this row) / BackendKind **38** (the extended driver-owned UDP receiver) / DECISIONS **ADR-0267** (next-free ADR-0268); ZERO new packages, ZERO new go.mod modules (`go mod tidy -diff` EMPTY). **Row 50 FLIPS `done`** at this six-gate.

After the IMPL lands (squashed + pushed; ADR-0267 body + BEHAVIOR_CONTRACT + STATE/ROADMAP [row 50 `done`] all committed), ROLL THIS FILE FORWARD: CHECK the FULL `docs/envoy-go/ROADMAP.md` for any remaining CHARTERED row that is NOT `done` — if one exists, roll to ITS next lifecycle stage; ELSE (no row is chartered-and-incomplete) the loop RE-OPENS in the "none chartered, awaiting a human pick" shape (the phase-48→49 / phase-49→50 precedent — deferred Observability candidates remain: `graphite`/OTLP-metrics sinks / the plain-statsd `tcp_cluster_name` transport / tracing `custom_tags`/`spawn_upstream_span`/`http_service`/force-trace / the tap filter — NONE of these are pre-chartered, a human must pick one to open the next BRAINSTORM) — do NOT invent/charter a new phase yourself. The TERMINATION SENTINEL applies ONLY if STATE.md's NEXT would otherwise show literally nothing left to do at ANY level (not merely "no phase currently chartered") — per the router's own long-standing invariant, "none chartered" is NOT the same condition as "all work complete," so mirror the phase-48-IMPL→row-48-done precedent (re-open awaiting a human pick) rather than the stop-file precedent, UNLESS a human has explicitly signaled the project itself is finished.

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** The next stage is the phase-50 IMPL (subagent-driven in a fresh worktree; FLIPS row 50 `done`). Lifecycle-state 3 → done.
- **Subagent-driven execution** (`feedback_execution_style`) in a **fresh worktree** (`feedback_git_worktrees`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit, re-runs the full suite on the frozen HEAD, does the deliberate-break verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). Per-task gates: `gofmt -l` + `golangci-lint` + `go vet` + `go build ./...` (`feedback_pertask_gofmt_lint`).
- **Process anchors:** ADR-0106 (per-leg rows — row 50 flips `done` at this IMPL) · ADR-0044 (ADR §Decision+§Consequences land at the IMPL) · ADR-0045 (sub-split soft gate — escape-valve UNCONSUMED, re-confirmed at PLAN Task 1) · ADR-0080 (strict-reject; this row LIFTS a prior strict-reject, the reverse direction) · ADR-0266 (the phase-49 dog_statsd sink this row extends) · next-free **ADR-0267** (ANCHORED at this IMPL).
- **Differential discipline** (carries to `0094`, and to re-running `0093` unaffected in the full suite): `-run 'TestDifferential/0094'` NEVER bare; `-count=1` on every break + `-race`; the Docker bridge + decode-ran proof; the driver-owned receiver EXTENSION is NOT a new BackendKind; the live reference is the wire truth; periodic-sink two-receivers + hard-Close; the delta-sink stability barrier; reuse the `0093` driver's LOCAL `hostGatewayIP` helper verbatim; DogStatsd tag order stays unsorted; the admin-interface wire-name collision (`0094` reuses `DeltaSumTagged`, unaffected by batching); `reference_full_suite_race_after_background_mutator`.

## Read first (in order)
1. THIS FILE — the cold-start router (phase 50 `stats-sink-dogstatsd-batching` PLAN LANDED; the next action is its IMPL).
2. `docs/envoy-go/phases/50-stats-sink-dogstatsd-batching/PLAN-50.md` — the 8-task TDD spine + the D-DSDB-* resolution block + the Global Constraints (counts) + the per-task code/tests/gates + the deliberate-break protocol + the Orientation "what already works" brief, INCLUDING the pinned `appendLine`/`flush` code block (READ THIS FIRST within the PLAN — it has the exact current line numbers for every seam).
3. `docs/envoy-go/phases/50-stats-sink-dogstatsd-batching/SPEC-50.md` — the frozen design + the §10 D-DSDB-* live-probe pins (D-DSDB-BOUNDARY LOAD-BEARING) + the ADR-0267 §Context draft (§12).
4. `docs/envoy-go/STATE.md` — the active-phase header (`phase 50 (stats-sink-dogstatsd-batching) PLAN done`) + the recorded NEXT (`phase 50 IMPL`) + the counts (1200 / 95 / 52 / 38 / ADR-0266).
5. `docs/envoy-go/ROADMAP.md` — row 50 `in-progress` (flips `done` at this IMPL); the Observability family note STAYS OPEN.
6. `docs/envoy-go/phases/49-stats-sink-dogstatsd/PLAN-49.md` (+ the phase-49 IMPL router precedent in STATE.md, commit `65130bbe`) — the subagent-driven IMPL controller shape to mirror.
7. `internal/statssink/dogstatsd.go` (`Submit`, the `s.write(line)` call site, `formatTagSuffix`, `write`, `Close`) + `internal/bootstrap/bootstrap.go` (`parseDogStatsdSinkConfig`, the strict-reject to delete, `DogStatsdSinkConfig`) + `cmd/envoy-go/main.go` (the dog_statsd build loop) + `test/helpers/statsdrecv/statsdrecv.go` (`ingest` — confirm the `\n`-split ALREADY works, do not touch it; the `tags`/`Tags` idiom to mirror for the two new accessors) + `test/fixtures/0093-stats-sink-dogstatsd/driver/driver.go` (the `0094` clone template, INCLUDING its local `hostGatewayIP` helper) — the EXACT seams the PLAN cites; re-read fresh (line numbers drift), do not trust citations without re-verifying against HEAD.
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `reference_periodic_sink_differential_two_receivers`, `reference_delta_sink_differential_stability_barrier`, `reference_differential_grpc_receiver_driver_owned`, `reference_docker_probe_bridge_network`, `reference_host_gateway_ip_docker_desktop`, `reference_dogstatsd_tag_order_unsorted`, `reference_admin_interface_wire_name_collision`, `reference_membership_total_vs_healthy_gauge`, plus the differential-discipline references above).
