RESUME a fresh session for envoy-go (router last updated 2026-06-26; the **phase 45 (`otlp-access-log`) BRAINSTORM is DONE and LANDED** — `docs/envoy-go/phases/45-otlp-access-log/BRAINSTORM.md` written in the 12-section phase-44 shape, spec-document-reviewer-APPROVED first-iteration, ROADMAP row 45 registered `in-progress`, STATE.md rolled, committed + pushed. **The next action is the phase 45.1 (core) SPEC.**). HEAD is at the **phase-45 BRAINSTORM commit** (subject `phase 45 (otlp-access-log) BRAINSTORM: …`), which sits atop the docs-only `next-prompt.txt:` checkpoint run, then the **phase-44.3 IMPL squash `4abf2c49`** (subject `phase 44.3 (grpc-access-log header-capture) IMPL: …`). Re-read the shas with `git log --oneline -14`, do NOT assume — any number of docs-only `next-prompt.txt:` commits may stack ATOP the BRAINSTORM commit (the stable anchor). BENEATH `4abf2c49` (~depth 3) is the pre-IMPL `next-prompt.txt:` run, then the **44.3 PLAN `e8c42d28`** (~depth 6), the **44.3 SPEC `6c0caafe`** (~depth 10), the **44.2 IMPL squash `c4055b20`** (~depth 13). Counts are UNCHANGED from the 44.3 IMPL-done (the BRAINSTORM is docs-only): stat surface **1189** (non-H2 **1185**) / fixtures **85** (`ls -d test/fixtures/[0-9][0-9][0-9][0-9]* | wc -l` == 85; tail `0083-grpc-access-log-headers`) / fuzzers **44** (`^func Fuzz` == 44) / BackendKind tail **38** / DECISIONS tail **ADR-0257**, next-free **ADR-0258**.

**ROW 44 (`grpc-access-log`) is `done`**; **ROW 45 (`otlp-access-log`) is `in-progress`** (registered at the BRAINSTORM commit; flips `done` at the 45.2 IMPL — the FINAL leg, per-leg ADR-0106 + `reference_roadmap_split_phase_row_done`). The **Observability FAMILY STAYS OPEN** (OTLP access log [active] / OTel-Zipkin tracing / stats sinks / tap filter remain rows). **The active action is the phase 45.1 (core) SPEC** — the FIRST leg of the by-concern 2-leg OTLP split. Invoke `superpowers:writing-plans` scoped to SPEC authoring (the SPEC stage precedes the PLAN): author `docs/envoy-go/phases/45-otlp-access-log/SPEC-45.1.md` in the house 14-section SPEC shape (mirror `SPEC-44.1.md`/`SPEC-44.2.md`), EXECUTE the §10 D-OTLP-* live-probe obligations IN-SESSION against the reference, anchor the ADR-0258 §Context draft, run the spec-document-reviewer loop, register the SPEC on ROADMAP/STATE, push. THEN roll this router to the 45.1 PLAN.

First confirm state via `git log --oneline -14` (expect HEAD at the phase-45 BRAINSTORM commit atop the `next-prompt.txt:` checkpoint run + the 44.3 IMPL squash `4abf2c49`; counts as above; re-read the shas, do NOT assume).

## THE CHARTERED PHASE-45 DESIGN (from the LANDED BRAINSTORM — do NOT re-derive; the SPEC pins the empirical D-OTLP-* questions)
- **Subject:** the **OTLP access logger** — a THIRD `accesslog.Sink` (`OTLPAccessLogSink`) alongside `AsyncFileSink` (06.2) + `GrpcAccessLogSink` (44), batching proto-agnostic `Record`s into OTLP `LogRecord`s inside a single `ExportLogsServiceRequest` (one `ResourceLogs`, N records) sent over the **UNARY** `opentelemetry.proto.collector.logs.v1.LogsService.Export` (NOT the streaming `StreamAccessLogs`) over the existing `grpcclient.Dialer`/cluster-TLS layer.
- **Envelope:** built-in `LogRecord` mapping PLUS a small `%…%` command-operator engine — the full-featured target.
- **Split:** BY-CONCERN 2-leg.
  - **Leg 45.1 — core export + built-ins + buffering** (ADR-0258): the NEW `go.opentelemetry.io/proto/otlp` direct go.mod module (transitive `@v1.0.0` → direct) + an `OTLPLogsClient` wrapper over `LogsServiceClient.Export` (mirrors `ALSClient`'s shape, UNARY) + the `OpenTelemetryAccessLogConfig` → `OTLPConfig` parse arm (LIFT OTLP out of the ADR-0041 silent-ignore set at `bootstrap.go:335`; strict-reject roster mirroring ALS — V2 transport / `google_grpc` / empty `envoy_grpc.cluster_name` [the SAME `CommonGrpcAccessLogConfig` arms `parseGrpcAccessLog` `:371` already runs] / `formatters` [out of scope] / non-empty `body`/`attributes`/`resource_attributes` [deferred to 45.2 — EXPLICIT per-sibling reject per `reference_strict_reject_sibling_typeurl_gap`]) + the `OTLPAccessLogSink` (channel + writer-goroutine + the REUSED 44.2 buffer machinery `grpcsink.go:166` → per-`Export` batch) + the built-in `Record→LogRecord` mapping (`TimeUnixNano`/`SeverityNumber`/Envoy built-in attributes; `disable_builtin_labels` honored) + the 2 process-global stats `access_logs.open_telemetry.{logs_written,logs_dropped}` + the driver-owned OTLP `LogsService` receiver test helper (NEW `test/helpers/otlplogs`, mirrors `accessloggrpc`; NO new BackendKind) + the `0084-otlp-access-log` built-ins differential.
  - **Leg 45.2 — the command-operator engine** (ADR-0259): the minimal `%…%` substituter over `body` (`AnyValue`) + `attributes`/`resource_attributes` (`KeyValueList`); `%REQ%`/`%RESP%` operators SUBSUME 44.3 header-capture (reuse `format.go`'s operator→`Record`-field knowledge + `capture.go:13` `CaptureHeaders`) + the `0085-otlp-access-log-attributes` differential.

## STRUCTURAL FINDINGS verified at the BRAINSTORM (carry forward; re-pin at the SPEC against the real source)
- **The ONE departure:** `go.opentelemetry.io/proto/otlp` is the FIRST new direct go.mod module since the 44.x stretch (already in the module cache transitively `@v1.0.0`; promote to direct). Provides `collector/logs/v1.LogsService` (`Export(ExportLogsServiceRequest)→ExportLogsServiceResponse`, UNARY — `collector/logs/v1/logs_service_grpc.pb.go:27`), `logs/v1` (`LogRecord`/`ResourceLogs`/`ScopeLogs`/`SeverityNumber`), `common/v1` (`AnyValue`/`KeyValueList`). ZERO OTHER new modules; the sink lives in the existing `internal/accesslog` package.
- **The OTLP config `common_config`(1) IS `envoy.extensions.access_loggers.grpc.v3.CommonGrpcAccessLogConfig`** — the SAME proto message gRPC ALS parses ⇒ the strict-reject roster (V2/`google_grpc`/empty-cluster) + the buffer-field handling (`buffer_size_bytes` default 16384, `buffer_flush_interval` default-1s panic-guard) are LITERALLY the reused `parseGrpcAccessLog` code path. The `OpenTelemetryAccessLogConfig` proto is at `go-control-plane/envoy@v1.32.4` `extensions/access_loggers/open_telemetry/v3/logs_service.pb.go` (fields `common_config`(1)/`body`(2) *AnyValue/`attributes`(3) *KeyValueList/`resource_attributes`(4) *KeyValueList/`disable_builtin_labels`(5) bool/`stat_prefix`(6) string/`formatters`(7) []TypedExtensionConfig).
- **No command-operator engine exists today.** `internal/accesslog/format.go:26` `Default(*Record)` is a SINGLE hardcoded 15-operator default-format emitter, NOT a general `%…%` parser — 45.2 builds a genuinely new (small) subsystem reusing format.go's operator→Record-field knowledge.
- **Reuse seams (BRAINSTORM-verified file:line):** `internal/accesslog/` — `Sink` [`accesslog.go:18`]; `Record` 10 fields + header maps [`:29`]; `GrpcAccessLogSink` struct [`grpcsink.go:48`], ctor [`:80`], `run()` writer-goroutine + buffer [`:166`]; `Default` [`format.go:26`]; `CaptureHeaders` [`capture.go:13`]; `buildHTTPAccessLogEntry` [`mapping.go:54`]. `internal/bootstrap/bootstrap.go` — `parseOneAccessLog` dispatch [`:323`], silent-ignore arm naming `open_telemetry` [`:335`/`:336`], `parseGrpcAccessLog` strict-reject arms [`:371`], `ALSConfig` [`:193`]. `internal/grpcclient/grpcclient.go` — `DialContext` [`:106`], `ALSClient` [`:259`], `NewALSClient` [`:277`], `StreamAccessLogs` [`:295`]. `internal/filter/hcm/accesslog_emit.go` — `emitAccessLog` [`:19`] / `emitAccessLogH2` [`:45`] (generic `Submit`, UNCHANGED). `cmd/envoy-go/main.go:129` — the ALS dial-and-build pass (add a sibling `bs.OTLPConfigs` pass). `test/helpers/accessloggrpc/accessloggrpc.go` — the driver-owned receiver pattern (`Server` `:49`, `New` `:73`, `StreamAccessLogs` `:137`, `Entries`/`Count`/`BatchSizes`/`MaxBatchSize`/`Reset`).
- **Anticipated counts:** at 45.1 IMPL — **+1 go.mod module** / stat **1189 → ~1191** (+2, SPEC-pinned — fixed `access_logs.open_telemetry.*` vs `stat_prefix`-derived is `D-OTLP-STATS`) / fixtures **85 → 86** (`0084`) / fuzzers **44 → 45** (`FuzzParseOpenTelemetryAccessLogConfig`) / BackendKind **38** UNCHANGED (driver-owned receiver) / **ADR-0258**. At 45.2 IMPL — fixtures **→ 87** (`0085`) / fuzzers **→ 46** / **ADR-0259**.

## SPEC-deferred `D-OTLP-*` (RESOLVE at the 45.1 SPEC via a live probe vs `envoyproxy/envoy:contrib-v1.37.2` per `reference_docker_probe_bridge_network`)
`D-OTLP-BUILTINS` (exact built-in attribute key set + `disable_builtin_labels` effect + the default `Body` content) · `D-OTLP-EXPORT-SHAPE` (unary `Export` batching + `ResourceLogs`/`ScopeLogs`/`Resource` nesting + node/`Identifier` placement + reconnect/drain) · `D-OTLP-STATS` (fixed `access_logs.open_telemetry.*` vs `stat_prefix`-derived; +2 vs +N) · `D-OTLP-SEVERITY` (status-code → `SeverityNumber`; whether `SeverityText` set) · `D-OTLP-RECEIVER` (driver-owned `otlplogs` receiver shape + h2c-vs-TLS) · `D-OTLP-OPERATORS` (45.2 operator subset + unsupported-op: strict-reject vs missing-value `-`) · `D-OTLP-SPLIT` (ADR-0045 re-check at each SPEC).

## NEXT STEP — the phase-45.1 (core) SPEC
Invoke `superpowers:writing-plans` (SPEC stage): (1) a fresh worktree off master per `feedback_git_worktrees` for any code-adjacent probe artifacts (the SPEC doc itself commits direct on master per the 44.x rhythm); (2) EXECUTE the §10 D-OTLP-{BUILTINS,EXPORT-SHAPE,STATS,SEVERITY,RECEIVER} live probes against `contrib-v1.37.2` (shared bridge + an OTLP `LogsService` receiver hostname reachable from BOTH containers; verify decode ran — the receiver received ≥1 `LogRecord` from the reference); (3) author `SPEC-45.1.md` in the 14-section SPEC-44.x shape, anchoring the ADR-0258 §Context draft; (4) run the spec-document-reviewer loop (verify EVERY load-bearing proto/code claim TRUE against the real source); (5) register the SPEC on ROADMAP + roll STATE.md; (6) push per `feedback_push_to_origin`. THEN roll this router to the 45.1 PLAN.

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** This stage is the phase-45.1 SPEC (lifecycle-state 1 → 2 for the 45.1 leg). `feedback_git_worktrees` · `feedback_execution_style` (subagent-driven once a PLAN exists) · `feedback_push_to_origin`.
- **Process anchors:** ADR-0106 (per-leg roadmap rows; family expansion has no parent rollup) · ADR-0045 (the split-gate — re-checked at each SPEC) · ADR-0041/0080 (silent-ignore vs strict-reject for access-log type-URLs) · ADR-0255 (44.1 ALS sink) · ADR-0256 (44.2 buffering) · ADR-0257 (44.3 header-capture) · next-free **ADR-0258** (45.1 OTLP core) then **ADR-0259** (45.2 operator engine).

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-45 BRAINSTORM is DONE; the next action is the 45.1 core SPEC — live-probe the D-OTLP-* questions → write `SPEC-45.1.md` → spec-review loop → roll to the 45.1 PLAN).
2. `docs/envoy-go/phases/45-otlp-access-log/BRAINSTORM.md` — the chartered design (the 2-leg split + the §10 D-OTLP-* SPEC obligations + the verified reuse seams).
3. `docs/envoy-go/STATE.md` — the active-phase header (`phase 45 (otlp-access-log) BRAINSTORM done`) + the recorded NEXT (the 45.1 SPEC).
4. `docs/envoy-go/ROADMAP.md` — row 45 (`otlp-access-log`, `in-progress`) + the `### Observability family` heading.
5. `docs/envoy-go/DECISIONS.md` — ADR-0255/0256/0257 (the 44.x as-built ALS sink + buffering + header-capture reuse seams) + next-free **ADR-0258**.
6. `docs/envoy-go/phases/44-grpc-access-log/` — SPEC-44.1/44.2/44.3 (the house SPEC shape the 45.1 SPEC mirrors) + the as-built ALS subsystem the OTLP row reuses.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `reference_streaming_sink_differential_framing`, `reference_differential_grpc_receiver_driver_owned`, `reference_docker_probe_bridge_network`, `reference_strict_reject_sibling_typeurl_gap`, `reference_fuzzer_count_docs_drift`, `feedback_execution_style`, `feedback_git_worktrees`, `feedback_subagents_no_push`, `feedback_pertask_gofmt_lint`).
