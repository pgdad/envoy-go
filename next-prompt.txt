=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

**THERE IS BANKED MID-LIFECYCLE WORK AT THIS TIP AND ADVANCING IT IS THE STAGE.** Phase 79 (`stats-prometheus-projection`) is **OPEN**: row 79 is `in-progress`, `BRAINSTORM.md` is landed, and `SPEC.md` / `PLAN.md` / `PROGRESS.md` do NOT exist. **This session performs the phase-79 SPEC.** Do NOT open a new subject.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=111 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **`want=111` IS A LIVE FIGURE, NOT A CONSTANT.** It is the ROADMAP data-row count. **A row-adding phase MUST bump it in the same commit that adds the row** — otherwise every later session gets `GATE FAIL: examined 112 data rows, expected 111`. That noise is deliberate: a wrong denominator must be loud, because a silent one is how check (1) failed in the first place. **The phase-79 BRAINSTORM bumped 110 -> 111 when it chartered row 79, and REHEARSED the bump with controls BEFORE making it** (`want=110` over the 111-row file fires `GATE FAIL`, confirming the bump was required rather than cosmetic). **The phase-79 SPEC adds NO row and MUST NOT touch `want`.**

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. When you open a new family, add its row-summary slug here.

**SENTINEL RE-RUN MECHANICALLY 2026-07-28 AT THE PHASE-79 BRAINSTORM — TWICE, at session open and again AFTER the `ROADMAP.md` row-79 insert landed. It does NOT fire. `stop` was NOT created and MUST NOT be.** ⚠️ **Re-run it yourself anyway and record what YOU see** — a prior run's result is not evidence for the current tip. ACTUAL output at the post-edit tip, recorded rather than predicted:
- **(1) `NOT DONE: row 79`.** ⚠️ **Check (1) is LIVE AGAIN** — it went silent at the phase-78 IMPL and row 79 re-opened it. **No `GATE FAIL` at `want=111`, so the new denominator holds.** ⇒ checks (2) and (3) are **no longer the only things blocking `stop`**.
- **(2) FIVE, UNCHANGED IN COUNT** — now at `:189`, `:199`, `:209`, `:215` (long form) and `:223` (the Operational-tooling short form). **Every line shifted +1 by the row-79 insert at `:141`. Row 79 narrowed NOTHING and that is STATED rather than forecast.**
- **(3) `NEVER OPENED: gRPC` and `NEVER OPENED: WASM`** — unchanged. ⚠️ **The phase-79 BRAINSTORM DECLINED the WASM rider on the merits (see item 3 below); this is a deliberate non-registration, not an oversight.**

Negative controls observed on all three at the BRAINSTORM: `want=109` AND `want=110` each ⇒ `GATE FAIL: examined 111 data rows, expected <want>` · an invented slug ⇒ `NEVER OPENED: ZZZ-nonexistent` · and the INPUT measured at **1 003 291 bytes / 13 `candidates:` hits** (against the sentinel's own narrower 5).

⚠️ **THE PHASE-79 BRAINSTORM EDITED `ROADMAP.md`, AND THE LEAK CHECK WAS RUN WITH ITS OWN NEGATIVE CONTROLS.** The new row-79 cell text was extracted to a scratch file and grepped for all three forbidden matcher strings ⇒ **0** on `deferred candidates:`, **0** on the long form, and **only the INTENTIONAL `Observability-family row`** among the eleven slugs; the same greps against a deliberately doctored copy ⇒ **1 / 1 / 1**. **The zeros are measured, not assumed.** ⚠️ **The leak check also caught a REAL near-miss and it was acted on:** the draft row quoted `ROADMAP.md:76` verbatim, which reproduced the `HTTP-filters-family row` slug inside the grepped file. Harmless to check (3) — that family IS legitimately opened — but gratuitous, so the ROADMAP row was PARAPHRASED and the verbatim quote kept in `BRAINSTORM.md`, which the sentinel does not grep. **`grep` cannot tell a mention from a use; the same species fired again inside the STATE.md roll, where a draft sentence describing the absence of a string CONTAINED that string.**

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`.** Every document from phase 75 forward recorded its blind spot — **109 data rows / 105 matched / FOUR misses** at an **em-dash** in an "after" cell, a **DOT** in the slug `http-1.1`, and two **LETTER-suffixed** ids `28.1a`/`28.1b` — and every one of them called it *"currently HARMLESS, since all four missed rows are `done`."*

⚠️ **Nobody fired the unsafe arm until the phase-77 PLAN. EXECUTED on scratch copies: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** A false "done" ends the project early. ⚠️ **DO NOT restore the regex form.**

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

The old check (2) matched ONLY the long phrase and could not see the **Operational-tooling** family's three genuinely un-chartered items, recorded in the **SHORT** form. Fixed at the phase-77 BRAINSTORM, at the MATCHER; negative controls all EXECUTED. ⚠️ **Re-confirmed at the phase-79 BRAINSTORM by accident:** an agent running only the long-phrase regex saw **FOUR** lines, not five — the `:223` short form is invisible to it. **Run the union form above, never one half of it.**

⚠️ **Normalising that Operational-tooling paragraph to the long form is a NAMED, DELIBERATELY-UNTAKEN follow-up.**

⚠️ **THE DOCUMENTED TRAP'S STATED MECHANISM IS REFUTED, WHILE THE TRAP IS REAL.** `reference_sentinel_deferred_sentence_live_vs_historical` says a naive `grep -c 'candidates:'` inflates *because of* the `candidates were:` recaps. It does inflate — but **not for that reason**: `candidates were:` does not contain that substring **at all**. The inflation comes from **ROADMAP table rows carrying BRAINSTORM prose**. **Cite the command; do not re-inherit the explanation.**

⚠️ **AND THE MEASURED RECORD: CHECK (2) HAS NEVER GONE DOWN** across ~22 phases (history `0 -> 1 -> 3 -> 4 -> 5`). **Phase 74 landed three `ssl.*` stat names and made its OWN sentence LONGER**; **phase 73 predicted a clause REMOVAL and got a clause REPLACEMENT**; **phase 77 opened a family and narrowed nothing**; **phase 78 narrowed nothing at ANY of its four stages**; **and phase 79's BRAINSTORM chartered a row straight out of the `:209` paragraph and STILL narrowed nothing** — the candidate sentence keeps its text until the row CLOSES. => **the candidates sentence is a WINDOW onto a larger deferred backlog, not an inventory of it.** ⚠️ **A stage that predicts a decrease here is repeating the phase-73 error. State what your row narrows — usually nothing — rather than forecasting.**

⚠️ **NEVER WRITE A SENTINEL'S OWN MATCHER STRING INTO A FILE THE SENTINEL GREPS.** The sentinel greps **`docs/envoy-go/ROADMAP.md`** — writing these strings *here* is safe; writing them *there* is not. **The phase-79 SPEC does NOT edit `ROADMAP.md` at all**, so this hazard is dormant for one stage; it re-arms at the IMPL, which flips row 79 to `done`.

⚠️ **THE SELECTION CALCULUS, CORRECTED AND STILL STANDING.** *"A candidate that clears a check-(3) blocker is worth **strictly more**"* does **not** hold: opening a family **MINTS** a check-(2) sentence, so phase 77's own arithmetic was **NET ZERO**. What survives: **opening a family is the ONLY way that family's work becomes enumerable at all — a prerequisite, not a shortcut.** Do not restore the stronger claim.

==============================================


## STATUS AS OF 2026-07-28: PHASE 79 IS **OPEN**. ROW 79 IS `in-progress`. THIS SESSION PERFORMS THE **PHASE-79 SPEC**.

**What the phase-79 BRAINSTORM landed** (`git log --grep 'phase 79'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): **FOUR files plus this one, and that is the complete BRAINSTORM file set** — `docs/envoy-go/phases/79-stats-prometheus-projection/BRAINSTORM.md` (NEW), `ROADMAP.md` (row 79 inserted at `:141`, `in-progress`, depends-on 78 — **227 lines / 111 data rows**), `STATE.md` (§Current rolled IN PLACE per ADR-0288; §Recent lineage was AT its five-cap so the phase-76 IMPL bullet was evicted), `STATE_HISTORY.md` (+6 lines). **`DECISIONS.md` and `BEHAVIOR_CONTRACT.md` BYTE-UNTOUCHED. ZERO production `.go`, ZERO test `.go`.** Four investigation agents on disjoint remits with private scratch and a private probe worktree, plus controller re-derivation.

### What matters for the NEXT session, in order

1. **⚠️ RE-RUN ALL THREE SENTINEL CHECKS YOURSELF AND RECORD THE ACTUAL OUTPUT; DO NOT PREDICT IT.** Create `stop` if and ONLY if all three print nothing. **Check (1) is LIVE again** (`NOT DONE: row 79`), so the sentinel cannot fire while phase 79 is open. `want` STAYS **111** — **the SPEC adds no row and must not touch it.**

2. **⚠️ THE SUBJECT IS FIXED AND THE STAGE IS THE SPEC. DO NOT RE-OPEN THE SEARCH.** Row 79 = `stats-prometheus-projection`, the **TWENTY-SECOND §9 Observability-family row**. Read `docs/envoy-go/phases/79-stats-prometheus-projection/BRAINSTORM.md` in full; it is the input. ⚠️ **But a BRAINSTORM's citations are not evidence** (`feedback_brief_citations_not_evidence`) — **re-derive every load-bearing figure at YOUR tip.** Phase 78's record is that **every stage found a load-bearing error in its predecessor, and each was found by EXECUTION, never by review.** Phase 79's BRAINSTORM already refuted three inherited claims the same way. **Budget for finding one more.**

   **THE DEFECT, reproduced by execution at the BRAINSTORM and re-runnable:** `internal/stats.ExtractTags` (`internal/stats/name.go:47`) is an allow-list `switch`; `WriteProm` (`internal/stats/prom.go:38-41`) does a bare `return` inside its `Walk` callback with **no log, no counter, no error, no partial line**. **30 live registered stat names tried / 30 DROPPED**, against a **discriminating** control of **16/16 PROJECTED** plus **4/4 near-miss PROJECTED**. Composition: `access_logs.` **4** · `tracing.` **4** · `sds.` **20** · `runtime.` **2**. Silent skip confirmed with actual bytes: a 5-metric registry emitted **116 B / exactly one metric line**, `WriteProm` returned `nil`. ⚠️ **The near-misses localise the defect to the ROOT SEGMENT alone** — the HCM-scoped `http.<sp>.tracing.spans_sent` PROJECTS while the tracer-scoped `tracing.opentelemetry.spans_sent` DROPS.

3. **⚠️ THE THREE REFUTATIONS THE BRAINSTORM LANDED — CARRY ALL THREE; A SPEC THAT INHERITS THE ORIGINALS WILL BE WRONG.**
   - **(i) THE BLAST-RADIUS CLAIM IS FALSE FOR A BYTE-MIRROR ARM.** The banked text says four further non-test `ExtractTags` consumers fall back to the full dotted name so an arm **changes their wire output**. Measured before/after on a scratch copy: `internal/statssink` label-map, dogstatsd, graphite and otlp are **BYTE-IDENTICAL**; only prometheus moves (absent -> 237 B). Mechanism: every fallback is `residual = fullname, labels = nil`, exactly what a byte-mirror arm returns, and `label.go:39` catches it on the **`len(labels)==0` disjunct**. ⇒ **the sink leg is an AUDIT, not a migration.** ⚠️ **This holds ONLY for byte-mirror. The `sds.` LABEL-HOISTING arm genuinely moves all four sinks — which is why it is deferred to 79.1.**
   - **(ii) "FOUR top-level prefixes" IS WRONG — there are FIVE live arms** (`cluster.`, `http.`, `listener.`, `server.`, `wasm.` at `name.go:51,61,83,93,97`), and the banked sentence contradicts its own opening clause. ⚠️ **The origin is a LIVE DEFECT: the terminal error at `name.go:350` reads `(want cluster.|http.|listener.|server.)` — it OMITS `wasm.`, stale since phase 25.1, and every one of the thirty drops emits it.** Anyone deriving the arm count from the error string gets 4. **Folded into scope as D-SPP-4.**
   - **(iii) THE WASM ROW-SUMMARY RIDER WAS DECLINED ON THE MERITS.** Every supporting figure re-derived TRUE (`internal/wasm/` 21 550 lines, rows 25/25.1/25.2/25.3 all `done`, fixtures `0034`-`0039`, a live `wasm.` arm, proxy-wasm conformance **EXECUTED 10/10**). **The characterization is false:** `ROADMAP.md:76` declares phase 25 the FINAL row of the §9 HTTP-filters family, and rows 25.x use "family" 23 times to declare it CLOSED. **The WASM work was landed INSIDE that family and used to close it**, so registering WASM is a doctrine adjudication against a landed closure statement, not a string edit — and writing the marker would silence check (3) **BY MENTION**. ⚠️ **Do not re-adopt it as cheap.**

4. **⚠️ THE D-SPP DOCKET THE SPEC MUST ANSWER** (BRAINSTORM §7):
   - **D-SPP-1 — THE ROW'S LARGEST UNMEASURED RISK. Is byte-mirror the right shape for `access_logs.` and `tracing.`?** Reference parity is measured for **`runtime.` ONLY** (`0118:145-152`, reference emitted `envoy_runtime_num_keys{} 6`). **No in-repo measurement exists for the other two.** The reference may hoist the sink-type segment (`grpc_access_log`, `zipkin`) as a **LABEL** rather than inlining it — which makes byte-mirror the WRONG shape, drags those arms into the tag-hoisting category, and **destroys the (i) no-op result for them**. ⚠️ **PIN IT AGAINST A LIVE DOCKERIZED REFERENCE: fresh container per arm** (`feedback_probe_fresh_container_per_arm`), **bridge network** (`reference_docker_probe_bridge_network`), **torn down BY NAME, never by an image/ancestor filter** (`reference_parallel_agents_shared_machine_namespaces`), **`contrib-` tag in the standard repo** (`reference_envoy_contrib_image_tagging`), **host-gateway literal IP for reference->host** (`reference_host_gateway_ip_docker_desktop`). **If either arm hoists, it moves to 79.1 and this row ships TWO arms, not three — say so rather than forcing byte-mirror.**
   - **D-SPP-2 — the `sds.` static/dynamic split, so the number cannot drift.** Statically **5** name templates (`internal/xds/stats.go:34-38`); the banked **20** is `5 x 4` distinct secrets. ⚠️ **The banked rule is under-specified and a naive re-derivation gets the right total for the WRONG REASON:** grepping `test/fixtures/**` returns **SIX** distinct SDS secret names — the four real ones plus `client_secret` and `hmac` from `0024-http-oauth2`, which do NOT contribute because they use `path_config_source` and are served by `internal/sdsfile.Watcher`, never the xds gRPC provider (`RegisterSDSStats` has exactly ONE non-test caller, `internal/boot/boot.go:201`). **The rule is "distinct secrets reaching `boot.go:201`", not "distinct secrets in the corpus"** — the corpus rule yields `6 x 5 = 30` and reads as confirmation of the 30-name total by coincidence. Applies to 79.1.
   - **D-SPP-3 — log or counter for the `WriteProm` skip?** BRAINSTORM hypothesis: **the LOG, holding stat surface +0**, and the argument is documentary — `prom.go:18-22` already CLAIMS the behavior is "log+ignore" and the code implements **neither**. ⚠️ **A counter makes the delta +1 and the ledger must move — AND carries a SELF-REFERENTIAL TRAP: the counter's own name must satisfy `ExtractTags` or it is silently skipped by the very code it instruments.** Name it under `server.` (SN5) if taken.
   - **D-SPP-4 — does the `name.go:350` error-string fix ride this row?** BRAINSTORM position: **yes**. Confirm no test asserts the current string byte-for-byte.
   - **D-SPP-5 — flip or delete the `0118` departure pin?** See item 5.

5. **⚠️ THE CLOSING GATE IS ALREADY BUILT, IS AN EXECUTING ASSERTION, AND GOES RED ON SUCCESS BY DESIGN.** `test/fixtures/0118-runtime-static-layer/driver/driver.go:272-276` carries a DEPARTURE PIN — *"The reference emits both gauges to /stats/prometheus; envoy-go emits NEITHER"* — backed by an **absence assertion** in `assertPrometheusExpositionDeparture` (`:290`), called unconditionally at `:266`, failing at `:306-314` with *"is NOW PRESENT … the phase-77 prometheus-exposition gap has been CLOSED. That is good news and this row is deliberately RED to force the follow-up"*. Pin block 1 is `:142-166` (the inherited `:154-166` is a near-miss). ⚠️ **A LIVENESS CAVEAT** (`reference_liveness_break_needs_failing_baseline`): **confirm `0118` is RED BEFORE editing the pin.** A green `0118` afterwards is ambiguous between "parity achieved, pin correctly flipped" and "the assertion did not run."

6. **⚠️ THE DEFAULT STATE IS UNGUARDED — MEASURED, NOT ASSUMED.** All three arms were added on a scratch copy and the real gates run: `go build ./...` clean · `go test ./internal/... -count=1` **ALL GREEN** · `go vet ./test/...` clean. **ZERO test or fixture edits were required.** The only negative assertions use `"unknown_top_segment.foo"` (`name_test.go:141`) and `"listener_manager.*"` (`:956`), neither colliding. ⇒ **the arm guards are work this row must CREATE, not tests it must keep passing.** ⚠️ **AND the `helpText` "completeness assertion" DOES NOT EXIST:** the map IS 15 entries (`name.go:458-476`) and `:209` IS a real line, but it sits in `TestHelpText_Coverage` (`:195`) iterating a **hand-listed 10-name slice** (`:196-206`). **There is no reverse direction** — `name_test.go:230-236` says so itself. **New `helpText` entries are unguarded by construction; a HELP line silently degrades to the bare metric name.** ⚠️ **The observability leg needs a NEGATIVE assertion with a stacked control** (one dropped + one projecting name, signal fires EXACTLY once) — `reference_positive_arm_cannot_catch_overfiring`.

7. **⚠️ FIVE MORE CITE REPAIRS THE BRAINSTORM LANDED — do not re-inherit the originals.** `internal/bootstrap/bootstrap.go:621,622` are **const declarations**; registration is `:752,753` · the prometheus consumer is **`flattenToProm` at `name.go:371`**, not `prom.go`; the full `ExtractTags` call-site set is **five** · fixtures **`0099` and `0100` assert NOTHING post-`ExtractTags`** — both carry zero `"envoy_` literals and deliberately scrape the flat `/stats`, and `test/helpers/statsdrecv/statsdrecv.go` is a generic parser keyed by the caller's `subsetNames` · **the banked six-fixture roster is an UNDERCOUNT with wrong members** — it misses `0091-stats-sink-metrics-service-labels` (`:164-166`, the ONLY fixture exercising `label.go`'s labelMapper), `0112-stats-sink-otlp` and `0113-stats-sink-otlp-knobs`; repo-wide **31** test files carry `"envoy_` literals · `name_test.go` is 959 lines / **55 `^func Test` but 56 top-level funcs** (helper `sameLabels` at `:762`), `^func Fuzz` = **0**.

8. **⚠️ THE `+0` STAT-SURFACE CLAIM IS CONDITIONAL AND CANNOT BE DISCHARGED BY THE `TestNoNewStat*` GUARDS.** Adding a `switch` arm is a **PROJECTION** change: the thirty names are already registered and already incrementing. **+0 holds unless D-SPP-3 chooses the counter, in which case it is +1 and the ledger moves.** All five guards live in `internal/statssink/registration_test.go` (`:26 :53 :81 :109 :137`), header `package statssink`, walking that package's own Registry — **none reaches `internal/stats`, and the blindness cuts BOTH ways (they would also miss a regression here).** **The available argument is STRUCTURAL: enumerate the diff's registration call sites and show the set is empty.**

9. **⚠️ THE BAND IS A STATED RE-SCOPE, NOT AN INHERITANCE — AND THE SPEC MUST RE-DERIVE IT.** **SPEC band 9-11, budget 11.** The banked candidate at `ROADMAP.md:209` says **11-14** for the FULL scope; the item-6 measurement argues **7-9**. 9-11 was chosen UP for two stated reasons: the measurement **excludes D-SPP-1's dockerized reference probing** (real, serial, container-per-arm), and cost calibration is **three-for-three at or above the SPEC ceiling** (76: 7-9 -> 9; 77: 11-13 -> 12; 78: 7-9 -> PLAN re-derived 10 -> shipped 10). ⚠️ **Phase 78's PLAN refuted its SPEC's band in BOTH directions** (`reference_deferred_candidate_cost_restale`). ⚠️ **Agreement between two agents who read the same document is NOT evidence.**

10. **SCOPE, split DELIBERATELY at the BRAINSTORM rather than at the ADR-0045 gate** (precedent 28.1a/b, 46.1b, 56.1/56.2). **IN:** three byte-mirror arms (`runtime.`, `access_logs.`, `tracing.` = 10 of the 30 names) + the stale `:350` error string + `helpText` and its prose count sentence at `name.go:448` + `name_test.go` arm guards + making `WriteProm`'s silent skip OBSERVABLE + flipping `0118`'s pin + the sink-side regression AUDIT. **OUT, to a banked row 79.1 (~7-9):** the `sds.` LABEL-HOISTING arm (20 names; `envoy_xds_resource_name`, measured ONCE — re-pin against a live dockerized reference) and the **registration-time-validation endgame**. ⚠️ **Per ADR-0300 §Consequences (ii), any injected-defer probing in that endgame MUST VARY THE INSERTION POINT across the pre- and post-anchor windows** — a single pre-anchor injection reports "caught" and conceals the genuinely uncovered case.

11. **⚠️ THE STANDING GATE HAZARDS — COUNT 14.** A full-suite recipe **WITHOUT `-v` is VACUOUS** · a sha256 roster built from one tree is **desynced BY CONSTRUCTION against a DELETED file** (span BOTH trees) · `gofmt -l` **NEVER exits non-zero** (gate on OUTPUT) · `go doc -all <A> <B>` **silently swallows arg2** · a **`+0 exported symbols`** gate over an EMPTY package goes RED on a CORRECT tree · a **RANGE** gate cannot detect anchor drift · a roster's naive `[ -f ] || continue` **exits 0 on a DELETED file** · a **count-only** stat guard **PASSES a build with BOTH names wrong** · a `-run` no-match **exits 0** with `[no tests to run]` · a `--- PASS` tally over a package with sibling tests **exceeds the fixture denominator** · a stat-delta claim **cannot be discharged by guards scoped to another package** · a **stderr-VOLUME** assertion **passes on the hang** · `golangci-lint` runs **`misspell` with `locale: US`**, so British spellings in `.go` comments FAIL — ⚠️ **and this row edits Go comments AND an error string, so it is LIVE again; do not paste SPEC prose into code** · **a harness's exit code is not the command's** — capture `INNER_EXIT`.

12. **⚠️ THE FULL 120-FIXTURE DIFFERENTIAL IS MANDATORY WHENEVER `cmd/envoy-go` OR THE HARNESS CHANGES — AND `internal/stats` LINKS INTO `cmd/envoy-go`, SO THIS ROW TRIPS IT.** Budget **~400-420 s per green attempt** (78: 120 / 414 s; 77: 120 / 403 s; 76: 119 / 409.8 s); a `-race` pass is a SECOND run, not a substitute. Run with `-count=1` AND `-v`, capture the INNER exit status, scope the tally to `TestDifferential/` and cross-check with `comm -3` — **the `comm -3` cross-check against the fixture-dir set is the load-bearing gate, not the raw count** (phase 78 measured **121** = 120 subtests + 1 parent; the oft-quoted 134 comes from an UNFILTERED package run). ⚠️ **AND THERE IS A THIRD BUILD SITE OF THIS BINARY**: `test/conformance/h2spec/h2spec_test.go:210` (`TestH2Spec`, entry `:30`), alongside `test/differential/harness.go:240` and `:594`. Do not exclude it silently.

13. **⚠️ ANCHORS AT THIS TIP** (re-derive in your own worktree; never copy): `DECISIONS.md` **17596** (tail **ADR-0300 COMPLETE**, next-free **ADR-0301**) · `ROADMAP.md` **227 lines / 111 data rows** · `BEHAVIOR_CONTRACT.md` **5762** · `STATE.md` §Current bullets at **`:15-21`**, closing `---` at **`:23`**. ⚠️ **`ROADMAP.md` and `STATE.md` BOTH MOVED at this BRAINSTORM, so every cite into them written before 2026-07-28 must be re-derived** — in particular **check (2)'s five lines are now `:189 :199 :209 :215 :223`**, and the banked candidate paragraph is now **`:209`, not `:208`**. ⚠️ **`:209` is a ~46 kB SINGLE LINE and its FIRST `projection` hit is an unrelated `envoy.lb`-scalars mention — the real candidate text begins near byte offset ~44 800 and the token there is UPPERCASE `PROJECTION`, so a case-sensitive grep finds only the decoy. Locate it POSITIONALLY.**

14. **⚠️ ADR + DOCUMENT CONVENTIONS FOR THIS ROW.** ADR-0301, **NEW form** — `## ADR-NNNN — <title>` (EM-DASH) + a single `> **STATUS:**` blockquote, **no `---` separator** — mirroring **ADR-0300** (`DECISIONS.md:17532`). §Context drafted at the SPEC; §Decision + §Consequences appended IN PLACE at the IMPL (**ADR-0044-as-used**; ⚠️ ADR-0044 does **not** itself contain that discipline). ⚠️ **Two ADR formats coexist** — old blocks (≤~0250) use `## ADR-NNNN: <title>` with plain `**Status:**` lines. ⚠️ **A RANGE-EXTRACTION HAZARD: `^## ADR-0107` matches NOTHING** — ADR-0106's block ends where ` ## ADR-0108` begins at `:4858`, **with a leading space**. An awk/sed range gate assuming contiguous space-free `^## ADR-` headings silently mis-bounds. ⚠️ **CITE THE SIX-GATE CORRECTLY: it is `BOOTSTRAP_PROMPT.md` §7.5, gates (a)-(f) at `:360-365`** (heading `:357`, closing sentence `:367` — the inherited `:357-366` is off by two), **at the REPO ROOT, WITH the `.md` extension**. **ADR-0106 does NOT define the six-gate** — its block contains **ZERO** occurrences of "six" (verified with two negative controls); its subject is the §9 family-expansion shape and the SOLE-leg property, and it is textually scoped to the HTTP-filters family, a scope the lineage has widened silently.

15. **⚠️ THE SPEC CREATES `SPEC.md` ONLY. `PROGRESS.md` IS BORN AT THE PLAN.** Verified across the whole project: `git ls-tree` at the phase-78 BRAINSTORM commit returns exactly one path, and `grep -rln '^# BRAINSTORM record' docs/envoy-go/phases/*/PROGRESS.md` ⇒ **0 files across 120 phase dirs**. The phase-78 BRAINSTORM's own `--stat` was four files. **The phase-79 router previously instructed opening a `PROGRESS.md` at the BRAINSTORM; that was WRONG and was not done.** The SPEC's file set is `SPEC.md` (NEW) + `DECISIONS.md` (ADR-0301 §Context) + `STATE.md` + `next-prompt.txt`. **`ROADMAP.md` stays BYTE-UNTOUCHED at the SPEC** (row 79 stays `in-progress`).

16. **⚠️ DOCUMENTARY DEFECTS, STILL NAMED AS DEFERRALS AND STILL DELIBERATELY NOT FIXED.**
    - ⚠️ **NEW, from the phase-79 BRAINSTORM: `ROADMAP.md`'s row 78 is the ONLY MALFORMED ROW of 111.** It has `NF=8` with **2 877 characters of IMPL summary sitting in field 8** and **no trailing `|`** (rows 138/139 both have `NF=8` with field 8 EMPTY; 110 of 111 rows end in `|`). GFM renders it as 7 cells against a 6-column header, so **the entire phase-78 IMPL summary is dropped from the rendered table.** **NOT FIXED because the §Schema invariant at `ROADMAP.md:18` is *"Rows are never deleted. Only `status` and `sub-phases` columns are updated in place"*** — repairing it means editing a closed row's `summary` cell. **Row 79 was written to the canonical shape and verified: 7 pipes, `NF=8` with field 8 empty, terminated with a pipe.**
    - **The documented PUBLIC IMPORT PATH DOES NOT EXIST.** Re-measured at this tip: **36 live occurrences across SEVEN files** (live = everything except the frozen archives under `docs/envoy-go/phases/**`) — `DECISIONS.md` **11**, root `PROGRESS.md` **8**, `ROADMAP.md` **7**, `next-prompt.txt` **4**, `STATE_HISTORY.md` **3**, `BEHAVIOR_CONTRACT.md` **2** (`:862`, `:5012`), `test/fixtures/0018-http-rbac/README.md` **1**. Repo-wide bare count **5392**, of which **5356 (99.33 %)** are frozen archives; the `/validate` form is **44**. ⚠️ **ZERO occurrences in compiled Go and `go build ./...` exits 0** — the tree builds green under the real path `github.com/pgdad/envoy-go`; this is documentary and prospective. ⚠️ **`DECISIONS.md:142` is `## ADR-0006: module path \`github.com/esalaine/envoy-go\`` — an ADR that DECIDES the wrong path, never superseded.** ⚠️ **BANKED FIGURE CORRECTED: "18 `ADR-0006` hits" is WRONG — at this tip it is 24 matching lines / 28 occurrences across 15 files** (the conclusion is unaffected). ⚠️ **AND A NEW OBSERVATION THAT CHANGES WHAT THE EDIT MEANS: all 8 root `PROGRESS.md` occurrences (`:85-91`, `:104`) are PASTED `go test` OUTPUT LINES** (`ok  github.com/esalaine/envoy-go/internal/admin (cached)`) — a recorded execution log under a path that does not exist, not prose asserting a path. **Rewriting recorded output is a different doctrinal act from correcting a statement.** ⚠️ **Fix by PATTERN `esalaine/envoy-go` (bare), NOT the `/validate` form.** **SPEC 8-10, expect 10-11.**
    - **A mechanical stat-surface count** to replace the documentary 1207. ⚠️ **Re-measured, and the rejection is UNDERSTATED rather than overstated: `214` registration sites -> 210** (`NewCounter` 113 + `NewGauge` 30 + `NewCounterIfAbsent` 62 + `NewGaugeIfAbsent` 5; no `NewHistogram`/`NewTextReadout` exist), **"the 17-row ledger" -> 27 rows** (`BEHAVIOR_CONTRACT.md:4962`, rows `:4966`-`:5018`, plus a 28th detached at `:805`), **11 literal sites yielding 10 unique names** — a ~0.8 % sample of 1207. **5** `statroster.New` fan-out sites confirmed (zookeeper alone **201** names); **2** method-value registrations confirmed invisible to any regex (`reg.NewCounter,` has no `(`). **The TWO-PARALLEL-TOTALS obstacle CONFIRMED** (`:795`, root cause `internal/cluster/manager.go:194-202`, 4 stats behind `if c.useH2`) — so 1207 is **config-conditional**. **The +3 unattributed gap CONFIRMED EXACTLY**: `:5008` 46.1b ends 1198; `:5010` 47.1 opens 1200 (**+2, no row**); `:5012` 51 ends 1200; `:5014` 74 opens 1201 (**+1, no row**). **8-11 tasks, pure meta-work.**
    - **Symmetric bind hardening** — ⚠️ **the banked scope is materially WRONG in the row's favour. `mustAllocatePort()` in "42 packages" -> actually 10** (drivers `0089, 0090, 0091, 0103, 0108-0113`); the racy helper is copy-pasted under FOUR names totalling ~15 definitions. ⚠️ **"fixture BACKENDS have no retry" is OVERBROAD:** `fixture.TCPEcho`/`HTTPEcho` — the default for nearly every fixture — `net.Listen` and **hold the listener open** for the test's lifetime (`runner_test.go:274-283`), so they cannot close-then-rebind race. **Only `fixture.HTTPSH2` (`:288-293`) does `freeTCPPort` -> `startHTTPSH2Backend` with `t.Fatalf` and no retry.** Anchors verified exact: `runner_test.go:237` = `90*time.Second`; `freeTCPPortBlock` at `harness_test.go:235` (bases 20000..31007); `startSubjectWithRetry` at `runner_test.go:219`, **sole caller**, three sites `:1214/:1377/:2066`; and the NOTE at **`:2215-2217`** (not `:2211-2218`): *"a bind-collision retry … must NEVER be added here — this path asserts that the start FAILS."* **Still rejected: it CANNOT be verified by a green suite run** (`0e9cc680` needed `-count=6`).
    - **Opening the gRPC family — HARD-BLOCKED, and the blocker now GENERALIZES.** `RunEncodeTrailers` has **exactly two non-test hits**, both in `internal/filter/http/chain.go` — `:621` (doc comment) and `:622` (definition); the call form `\.RunEncodeTrailers(` returns **0 non-test, 1 test**. `RunDecodeHeaders`: **178**. ⚠️ **NEW: the DECODE side is equally dead — `\.RunDecodeTrailers(` is also 0 non-test, 1 test. The entire trailers pair is unreachable from production code.** Both charter-satisfying candidates (`grpc_http1_reverse_bridge`, `grpc_web`) sit behind it: **16-22+ tasks.**
    - **UNRESOLVED:** the *"plus two more in the same file"* half of the `BEHAVIOR_CONTRACT.md` stale-cite claim (`internal/tls/config.go:272`, `internal/filter/http/chain.go:19` flagged, **not audited**).
    - `BEHAVIOR_CONTRACT.md:501` calls the `wasm.` arm a *"NEW SN9 flattening rule"*, which **COLLIDES with ADR-0118's actual SN9**.
    - **`ADR-0299`'s STATUS line still reads `PROPOSED`** although its §Decision and §Consequences landed at the phase-77 IMPL. **Recorded, not fixed.**
    - **`BEHAVIOR_CONTRACT.md` carries 14 `### Does not yet apply to` headings**, not 15.
    - ⚠️ **`STATE.md` §Project counts REMAINS STALE — now on FOUR axes.** Fixtures **119** (real 120) · stat surface **1205** (lineage 1207) · DECISIONS tail **ADR-0298** (real **ADR-0300**) · ⚠️ **NEW, found at the phase-79 BRAINSTORM: `STATE.md:33` asserts "Next-free REFERENCE port is `10450`, NOT `10118` — ports are NOT fixture-index aligned", which is REFUTED by the landed fixture's own driver.** `0118/driver/driver.go:26-35` reads *"Convention `10<fixture index>` — 0114->10114 … so 0118->10118. ⚠️ NOT 10450: that is the TLS/SDS band (0108-0113)"* and sets `refListenerPort = 10118`. The sentence is right about `10450` **for the TLS band** and wrong as a general claim. **Deliberately NOT repaired — repairing a count by editing the sentence that states it is how the ADR-0296/0297 species starts. Anchor on §Current instead, which IS live.**

17. **⚠️ A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX — the TWELFTH consecutive stage at which this held.** The index is `~/.claude/projects/-home-esa-git-envoy-go/memory/` (exactly four flake files). Live hazards: the full-suite startup flake (`subject ready: EOF` **and** `bind: address already in use`, both failing **BEFORE any assertion**, the latter as a **PANIC that can abort the whole binary**, firing **MORE readily under `-race`** and as the fixture count grows — now **120**; ⚠️ hardened at `0e9cc680` **for the SUBJECT PROXY ONLY**) · `reference_sds_init_fetch_timeout_dial_budget_flake` — ⚠️ **TWO packages: `internal/xds TestProvider_FetchInitialCertificate_Timeout` AND `internal/boot TestSDSEndToEnd_FetchFailure_BootFailsClosed`** · the PRE-EXISTING `internal/cluster` `-race` outlier flake — ⚠️ **name it: `TestOutlierDetector_ConcurrentEjectExactlyOnce`** · two still-**UNINDEXED** load flakes (`internal/httpclient TestOptions_ZeroValue_NoOpDefaults`, `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery`), carried as prose for an **eleventh** stage. **Isolate-re-run, then state the classification AND its evidence.** ⚠️ **`0061-lb-ring-hash` is NOT a live flake — a spread failure there is a FINDING.**

18. **Counts at this tip (re-run MECHANICALLY in your worktree; never copy them):** fixtures **120** (`ls -d test/fixtures/[0-9]*/ | wc -l`; the faithful `discoverFixtures` predicate is `^[0-9]{4}[a-z]?-`; next-free **0119**) / fuzzers **55** (⚠️ the `internal/`-vs-repo-wide pair is **NOT discriminating** — both return 55; use the append-a-`func Fuzz` control **on a scratch COPY**) / **stat surface 1207**, ⚠️ **DOCUMENTARY and CONFIG-CONDITIONAL — assert the DELTA** / **BackendKind tail 38** (⚠️ a TAIL VALUE — 39 constants, 0-38; **do NOT "fix" 38 to 39**) / go.mod modules **2** (⚠️ the phase-61.2 lineage figure — **NOT a repo total**; the single `go.mod` requires **67**; **do NOT "fix" 2 to 67**) / internal packages **73** / blank imports in `runner_test.go` **120**, ⚠️ **anchor on the FULL `^\t_ "github.com/pgdad/envoy-go/test/fixtures/` prefix — a naive `^\t_ ` gives 126** / DECISIONS tail **ADR-0300 COMPLETE** (next-free **ADR-0301**) / next-free reference port **10119** (⚠️ **the convention IS `10<fixture index>`** — `grep -rn '10119' test/` ⇒ 0 hits, negative control `10118` ⇒ 5).

## What THIS session does — the phase-79 SPEC on `stats-prometheus-projection`

One stage per session. Write `docs/envoy-go/phases/79-stats-prometheus-projection/SPEC.md`, draft **ADR-0301 §Context** in `DECISIONS.md` (NEW form, em-dash heading, single STATUS blockquote, no `---`), roll `STATE.md` §Current pointer **IN PLACE** (ADR-0288 — ⚠️ **§Recent lineage is AT its five-entry cap; a close that adds a bullet must evict the oldest to `STATE_HISTORY.md`**) and this file. **`ROADMAP.md` stays BYTE-UNTOUCHED. Do NOT create `PROGRESS.md`.**

⚠️ **ANSWER THE D-SPP DOCKET BY EXECUTION, NOT BY REASONING — AND D-SPP-1 IS THE ONE THAT WILL BITE.** Phase 78's SPEC established by execution that its BRAINSTORM's proposed fix was wrong; phase 79's BRAINSTORM refuted three inherited claims the same way. **A SPEC that only reasons will be refuted at the PLAN.** The single highest-value thing this stage can do is put a live dockerized reference in front of `access_logs.` and `tracing.` and find out whether byte-mirror is the right shape at all.

⚠️ **RE-DERIVE THE BAND** (`reference_deferred_candidate_cost_restale`). 9-11 is a re-scope with stated reasons (item 9), not a measurement. **If your re-derivation disagrees, the disagreement IS the SPEC's headline — say so rather than reconciling silently.**

Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** (`reference_parallel_subagents_private_scratch`), **BANDED PORTS** — ⚠️ **the band must sit OUTSIDE the differential harness's 20000-31007 allocation range** — and they **must tear down docker containers BY NAME, never by an `ancestor`/image filter** (`reference_parallel_agents_shared_machine_namespaces`). ⚠️ **If two agents share ONE worktree they must stage by EXPLICIT PATH; a `git commit -am` sweeps in a sibling's uncommitted work.** ⚠️ **AND THEY MUST NOT RUN AN UNSCOPED `git restore`.** Per-task `gofmt` + `golangci-lint` on touched packages (`feedback_pertask_gofmt_lint`). ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch.**

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. ⚠️ **THIS HAS NOW FIRED LIVE IN SIXTEEN CONSECUTIVE SESSIONS.** **Assume it fires every session.** **Use `git -C <abs-worktree-path>` for every git command, prefer absolute paths over `cd`, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — Current-pointer section (TRUSTWORTHY; ADR-0288 discipline — ⚠️ **anchor greps on `^- **<field>`**, not bare; all seven fields verified singleton at the phase-79 BRAINSTORM close, bullets at `:15-21`). ⚠️ **§Project counts is STALE ON FOUR AXES** (item 16) — do not anchor on it.
2. `docs/envoy-go/phases/79-stats-prometheus-projection/BRAINSTORM.md` — **the input to this stage.** §0 carries the three refutations; §7 is the D-SPP docket; §11.1 records the five rejected alternatives with the evidence that settled each; §11.4 is the ten cite repairs.
3. `docs/envoy-go/ROADMAP.md:141` — row 79 as chartered. ⚠️ **`:209` is the banked candidate paragraph, a ~46 kB single line — locate positionally, and note the token is UPPERCASE `PROJECTION`.**
4. `docs/envoy-go/phases/78-boot-panic-visibility/SPEC.md` — the shape a SPEC takes in this lineage, and how it records a refutation of its own BRAINSTORM.
5. `docs/envoy-go/DECISIONS.md` — **ADR-0300** (the NEW-form template; §Consequences (ii) carries the corrected injected-defer residual) · **ADR-0045** (the split gate: ~25 tasks / ~1500 LoC, structural not optional) · **ADR-0288** (`:17010`, the roll-IN-PLACE rule) · ADR-0044 (⚠️ it does **NOT** contain the Context-draft discipline — use `ADR-0044-as-used`) · **ADR-0106** (⚠️ the SOLE-leg property, **not** the six-gate) · **ADR-0264** (metrics_service label mapping, which `internal/statssink/label.go` implements).
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — especially **`reference_probe_must_discriminate`** (necessary but NOT sufficient), **`reference_independent_probes_can_share_a_blind_axis`**, **`reference_probe_input_is_a_claim`**, **`reference_stats_sink_emits_used_only`**, **`reference_listener_stat_scope_cross_side_divergence`**, **`feedback_probe_fresh_container_per_arm`**, **`reference_docker_probe_bridge_network`**, **`reference_positive_arm_cannot_catch_overfiring`**, **`reference_liveness_break_needs_failing_baseline`**, **`reference_gate_command_negative_control`**, **`reference_empty_output_is_not_a_zero_result`**, `reference_sample_is_not_an_audit`, `reference_code_comment_not_evidence`, `reference_a_drift_correction_is_itself_a_claim`, `reference_stale_cite_recurs_fix_by_pattern`, `reference_deferred_candidate_cost_restale`, `feedback_brief_citations_not_evidence`, `reference_next_prompt_tracked_despite_gitignore`, `feedback_git_worktrees`, `feedback_execution_style`, and **`reference_bash_cwd_reset_commits_to_main`**.
