=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-VERIFIED 2026-07-21 (re-run MECHANICALLY at the phase-70 BRAINSTORM close, in the stage worktree) — it does NOT fire. `stop` was NOT created.** Actual output:
- **(1) prints `NOT DONE: row 70`** — row 70 (`tracing-custom-tags-metadata`) was registered `in-progress` at this BRAINSTORM commit per the §Schema invariant; it flips `done` at the phase-70 IMPL six-gate. (The registration re-opens check (1) until the IMPL.)
- **(2) prints THREE live "candidates:" sentences** — HTTP/3, xDS, Observability ⇒ three families STAY OPEN. **Cite the command, never the adjective** (`reference_sentinel_deferred_sentence_live_vs_historical`): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` ⇒ **3**. (The Observability sentence stays live — the `custom_tags (metadata)` candidate is NARROWED OUT only at the phase-70 IMPL row-done edit; the `ssl` + `spawn_upstream_span`/`http_service`/force-trace candidates remain.)
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** ⇒ THREE families never opened.

All three checks print ⇒ the sentinel does NOT fire. Abundant work remains.

==============================================

## STATUS AS OF 2026-07-21: THE PHASE-70 BRAINSTORM IS COMPLETE AND LANDED. ROW 70 (`tracing-custom-tags-metadata`) IS `in-progress`. THIS SESSION PERFORMS THE PHASE-70 SPEC.

**What the phase-70 BRAINSTORM landed** (`git log --grep 'phase 70'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): docs-only (`docs/envoy-go/phases/70-tracing-custom-tags-metadata/BRAINSTORM.md` + the ROADMAP row-70 registration + the family CHARTERED sentence + STATE + this router-roll; ZERO production `.go`). Fresh worktree off master `33ed4c05`, branch `phase-70-tracing-custom-tags-metadata-brainstorm`, per `feedback_git_worktrees`.
- **Subject (the roller's AUTONOMOUS self-pick per the 2026-07-12 standing directive):** `tracing-custom-tags-metadata` — the tracing `custom_tags` `metadata` type scoped to the **`REQUEST`** MetadataKind, the SIXTEENTH Observability-family row + the FOURTH tracing `custom_tags` row + the **LAST `CustomTag` source type** (after literal @ 59, request_header @ 62, environment @ 63): lifts the `custom_tags metadata type unsupported` reject (`internal/tracing/config.go:214-215`) and resolves a `REQUEST`-kind dynamic-metadata value onto the ingress span over the LANDED `internal/dynamicmetadata.Bucket` (`Get(filterName, key)`), emitted on BOTH the OTLP + Zipkin exporters via the phase-46 `Span.Attrs []KV` seam; the `ROUTE`/`CLUSTER`/`HOST` kinds reject loudly with distinct substrings (envoy-go-strict, ADR-0080).
- **⚠️ RE-DERIVATION FINDING (the pick correction, sound):** the roller's FIRST pick — the `environment` type — was RE-DERIVED STALE at tip by a DIRECT READ of `internal/tracing/config.go:208-213`, which shows `environment` + `request_header` ALREADY LANDED (phase 62/63); the sole remaining `CustomTag` type is `metadata` (`reference_deferred_candidate_cost_restale` — landed rows shrink deferred candidates; the deferred sentence's `(metadata)` parenthetical was confirmed against source, not trusted alone). A scouting brief that attributed `environment` to "phase 62" was corrected against the git log (`environment` landed at phase **63**).
- **The FULL rejected roster (BRAINSTORM §2.1, each SIZED at tip this session):** the `ssl` handshake-outcome stat family (ADR-0286 C3 framework-surgery + a NEW stat surface breaking the +0-stat streak; the verify-failure taxonomy needs a per-chain TLS callback) · `spawn_upstream_span` (a second span) · `http_service` (a new OTLP-over-HTTP tracer transport) · force-trace (internal-request detection unbuilt) · a 6th stats sink (hystrix admin-streaming / wasm runtime — off the landed Flusher/Submit substrate) · the RBAC `require=false` fixture arm (engine complete ⇒ a RIDER, not row-sized) · DataSource `environment_variable` (SPEC-63 D-ENV-HARNESS blocked) · `watched_directory` (live per-chain TLS reconfig unbuilt) · HTTP-3 + xDS candidates · opening a never-opened family.
- **Landed substrate (re-derived):** the phase-59/62/63 `parseCustomTags`/`ResolveCustomTags`/`Span.Attrs` pipeline + `internal/dynamicmetadata.Bucket.Get` + the Lua filter (`internal/filter/http/lua/metadata.go:315` `Bucket.Set`) as the cross-side-deterministic fixture WRITER for the positive arm. **Envelope:** +0 stats/packages/fuzzers/BackendKinds/modules anticipated; ONE new OTLP fixture (a Lua-set positive arm + a `default_value` arm; a Zipkin dir weighed) ⇒ fixtures 115 → 116 (or 117).
- **BRAINSTORM-time open questions to the SPEC:** D-CTM-SCOPE · D-CTM-PROTO · D-CTM-WIRE · D-CTM-ZIPKIN-WIRE · D-CTM-VALUE-SERIALIZE · D-CTM-DEFAULT · D-CTM-PATHWALK · D-CTM-CONFIG-SEAM · D-CTM-BUCKET-SEAM · D-CTM-REJECT · D-CTM-FIXTURE/D-CTM-FIXTURE-SOURCE · D-CTM-FUZZSEED · D-CTM-SPLIT (SINGLE FLAT ROW anticipated, ~10–13 tasks; ADR-0045 armable).
- **Adversarially verified before landing** (an independent code-claims + roster-adjudication verifier). Row 70 registered `in-progress` at the stage-close commit per the §Schema invariant. NO deferred sentence narrowed at this pre-IMPL stage (phase-57 precedent). DECISIONS.md UNTOUCHED (ADR-0292 §Context drafts at the phase-70 SPEC per ADR-0044).
- **Counts UNCHANGED at this BRAINSTORM (docs-only; re-run mechanically):** fixtures **115** / fuzzers **55** / stat **1201** / BackendKind **38** / DECISIONS tail **ADR-0291** (COMPLETE; next-free **ADR-0292**) / go.mod modules **2**.

**⚠️ PUSH (idempotent — evaluate, then act):** FIRST ACTION: `git fetch`; if origin/master is BEHIND local master and the local tip is the phase-70 BRAINSTORM squash (`git log --grep 'phase 70'`, locate by SUBJECT), `git push origin master` per `feedback_push_to_origin`; if in sync, proceed; if REJECTED (diverged), STOP and reconcile — never rebase away landed squashes.

## What THIS session does — the phase-70 SPEC (RE-DERIVE the BRAINSTORM, do NOT execute it)

One stage per session: the SPEC. Fresh worktree off master per `feedback_git_worktrees`; docs-only APART from the ADR-0292 §Context append to DECISIONS.md. RE-DERIVE the BRAINSTORM at tip (an independent re-derivation agent re-opens EVERY §6 edit site + the go-control-plane `CustomTag_Metadata`/`MetadataKey`/`MetadataKind` proto types + the relevant PGV lines — `feedback_brief_citations_not_evidence`), then DISPOSE the D-CTM-* docket one-for-one. Run the live-probe arms against `envoyproxy/envoy:contrib-v1.37.2` (fresh container per arm, `reference_probe_fresh_container_per_arm`; a Lua-set metadata WRITER served-this-arm; verify the OTLP/Zipkin span decode ACTUALLY ran — `reference_docker_probe_bridge_network` / `reference_host_gateway_ip_docker_desktop`):
- **D-CTM-WIRE / D-CTM-ZIPKIN-WIRE** — the reference's metadata custom-tag wire form on the OTLP + Zipkin span (key = the `tag`, value = the serialized dynamic-metadata value as a STRING attribute; appended after the built-ins).
- **D-CTM-VALUE-SERIALIZE** — the `structpb.Value` → span-tag string rendering (pin the STRING case; name non-string as a documented boundary if non-trivial).
- **D-CTM-DEFAULT** — `default_value`/omit semantics (absent path vs present-but-empty value vs absent-with-empty-default; anticipated the `environment` omit-on-empty-resolved precedent — probe pins it).
- **D-CTM-PATHWALK** — the `MetadataKey.path` → `Bucket.Get(ns, path[0])` + nested-`StructValue` walk of `path[1:]` mapping (single-segment common case + a multi-segment probe).
- **D-CTM-REJECT** — the three unsupported-MetadataKind reject substrings (ADR-0080 distinct) + the empty-namespace/empty-path/unset-kind handling (envoy-go's own reject vs the reference's PGV); confirm (one probe arm) the reference ACCEPTS `ROUTE`/`CLUSTER`/`HOST` (the departure is real).

Also PIN (code-derived): **D-CTM-CONFIG-SEAM** (the `CustomTagSpec` `kindMetadata` extension — namespace + `[]string` path + reuse `DefaultValue`); **D-CTM-BUCKET-SEAM** (how the per-request `Bucket` reaches `ResolveCustomTags` at the three `accesslog_emit.go` call sites `:55` H1/`:116` H2/`:177` H3 — RE-DERIVE whether the per-protocol `chain` [built at `connection.go:350`/`h2dispatch.go:327`/`h3dispatch.go:148`; `chain.DynamicMetadata()`, `chain.go:837`/`:963`] is in scope at the emit or must be threaded; a nil-tolerant `metaLookup` param); **D-CTM-FIXTURE/D-CTM-FIXTURE-SOURCE** (ONE OTLP dir or +Zipkin; the Lua writer's cross-side wire-identity; a `default_value` arm — the dispatch constraint, `reference_differential_fixture_dispatch_constraint`); **D-CTM-FUZZSEED** (a SEED to `FuzzHCMConfigParse` `fuzz_test.go:28`, dispatch-verified to reach the `parseCustomTags` metadata arm; count STAYS 55, `reference_fuzzer_count_docs_drift`); **D-CTM-SPLIT** (SINGLE FLAT ROW anticipated, ~10–13 tasks — ADR-0045 armable).

**Stage-close mechanics:** DRAFT ADR-0292 §Context (the DECISIONS tail flips **ADR-0291 → ADR-0292** at the SPEC commit per ADR-0044; §Decision/§Consequences land at the IMPL IN PLACE). Row 70 STAYS `in-progress` (flips `done` at the phase-70 IMPL six-gate). NO deferred sentence narrowed at the SPEC (rolls OUT at the IMPL — the phase-57 precedent). Counts UNCHANGED apart from the DECISIONS tail (ADR-0291 → ADR-0292). Re-run the sentinel MECHANICALLY at close (it does NOT fire: (1) `NOT DONE: row 70`; (2) THREE `candidates:` sentences; (3) `NEVER OPENED: gRPC/Runtime/WASM`). Roll THIS file to the phase-70 PLAN. **Adversarially verify before landing** (`reference_parallel_subagents_private_scratch`).

## Read first (in order)
1. `docs/envoy-go/phases/70-tracing-custom-tags-metadata/BRAINSTORM.md` — the full pick + §6 edit-site roster + the D-CTM-* docket (§10).
2. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline; the next-skill line carries the SPEC's owed work).
3. `internal/tracing/{config.go,resolve.go}` + `internal/filter/hcm/accesslog_emit.go` + `internal/dynamicmetadata/dynamicmetadata.go` — RE-DERIVE the seam at tip (the metadata reject `config.go:214-215`, `CustomTagSpec` `config.go:63`, `ResolveCustomTags` `resolve.go:14`, the three call sites `:55`/`:116`/`:177`, `Bucket.Get` `dynamicmetadata.go:33`).
4. `docs/envoy-go/ROADMAP.md` — the Observability family paragraph + the three live `candidates:` sentences (locate by grep, not line) + §Schema (row-registration invariant).
5. `docs/envoy-go/DECISIONS.md` tail — ADR-0291 (COMPLETE), ADR-0286 (the `ssl` stat boundary, C3), ADR-0277/0283 (the prior `custom_tags` literal/request_header ADRs) + ADR-0044/0045/0106 (the ADR/split/valve discipline).
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — trust MEMORY.md's index; esp. `feedback_brief_citations_not_evidence`, `reference_probe_fresh_container_per_arm`, `reference_differential_fixture_dispatch_constraint`, `reference_tracing_upstream_cluster_framework_gap`, `reference_sentinel_deferred_sentence_live_vs_historical`.
