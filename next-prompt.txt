=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

**THERE IS BANKED MID-LIFECYCLE WORK AT THIS TIP.** Phase 78 (`boot-panic-visibility`) is **OPEN** — row 78 is `in-progress`, `BRAINSTORM.md` / `SPEC.md` / `PLAN.md` / `PROGRESS.md` are landed, `DECISIONS.md` carries **ADR-0300 §Context** (PROPOSED), and `ROADMAP.md` + `BEHAVIOR_CONTRACT.md` are BYTE-UNTOUCHED. **This session performs the phase-78 IMPL.** Do NOT open a new subject.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=110 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **`want=110` IS A LIVE FIGURE, NOT A CONSTANT.** It is the ROADMAP data-row count, bumped 109 -> 110 at the phase-78 BRAINSTORM in the same commit that added row 78. **A row-adding phase MUST bump it in the same commit that adds the row** — otherwise every later session gets `GATE FAIL: examined 111 data rows, expected 110`. That noise is deliberate: a wrong denominator must be loud, because a silent one is how check (1) failed in the first place. **The phase-78 IMPL adds NO row — it FLIPS row 78 to `done` — so `want` STAYS 110.**

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. When you open a new family, add its row-summary slug here.

**SENTINEL RE-RUN MECHANICALLY 2026-07-27 AT THE PHASE-78 PLAN — TWICE, by the controller at session open and by a read-only agent independently. It does NOT fire. `stop` was NOT created and MUST NOT be.** ⚠️ **Re-run it yourself anyway and record what YOU see** — a prior run's result is not evidence for the current tip. ACTUAL output, recorded rather than predicted:
- **(1) `NOT DONE: row 78`** — correct and intended; the row stays `in-progress` until THIS session's IMPL flips it. **No `GATE FAIL` printed**, so the denominator 110 holds.
- **(2) FIVE, UNCHANGED** — `:188`, `:198`, `:208`, `:214` (long form) and `:222` (the Operational-tooling short form). **This row narrows NOTHING and that is STATED rather than forecast.**
- **(3) `NEVER OPENED: gRPC` and `NEVER OPENED: WASM`** — unchanged.

Negative controls observed on all three: `want=109` ⇒ `GATE FAIL: examined 110 data rows, expected 109` · a scratch copy with row 77 flipped ⇒ `NOT DONE: row 77` as well · an invented slug ⇒ `NEVER OPENED: ZZZ-nonexistent` · and the INPUT measured at **994 044 bytes / 5 hits** against an empty control at **0 bytes / 0 hits** (`reference_empty_output_is_not_a_zero_result`).

⚠️ **`ROADMAP.md` was BYTE-UNTOUCHED at the phase-78 PLAN (sha256-verified against master), so no matcher string can have leaked.** That verification carried its own NEGATIVE CONTROL: the same comparison over `STATE.md` correctly reported MISMATCH, proving the gate is not inert. ⚠️ **THIS SESSION DOES EDIT `ROADMAP.md`** (row 78 -> `done`), so **re-run ALL THREE checks afterwards** and verify mechanically that no matcher string leaked.

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`.** Every document from phase 75 forward recorded its blind spot — **109 data rows / 105 matched / FOUR misses** at an **em-dash** in an "after" cell, a **DOT** in the slug `http-1.1`, and two **LETTER-suffixed** ids `28.1a`/`28.1b` — and every one of them called it *"currently HARMLESS, since all four missed rows are `done`."*

⚠️ **Nobody fired the unsafe arm until the phase-77 PLAN. EXECUTED on scratch copies: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** A false "done" ends the project early. ⚠️ **DO NOT restore the regex form.**

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

The old check (2) matched ONLY the long phrase and could not see the **Operational-tooling** family's three genuinely un-chartered items, recorded in the **SHORT** form. Fixed at the phase-77 BRAINSTORM, at the MATCHER; negative controls all EXECUTED.

⚠️ **Normalising that Operational-tooling paragraph to the long form is a NAMED, DELIBERATELY-UNTAKEN follow-up.**

⚠️ **THE DOCUMENTED TRAP'S STATED MECHANISM IS REFUTED, WHILE THE TRAP IS REAL.** `reference_sentinel_deferred_sentence_live_vs_historical` says a naive `grep -c 'candidates:'` inflates *because of* the `candidates were:` recaps. It does inflate — but **not for that reason**: `candidates were:` does not contain that substring **at all**. The inflation comes from **ROADMAP table rows carrying BRAINSTORM prose**. **Cite the command; do not re-inherit the explanation.**

⚠️ **AND THE MEASURED RECORD: CHECK (2) HAS NEVER GONE DOWN** across ~20 phases (history `0 -> 1 -> 3 -> 4 -> 5`). **Phase 74 landed three `ssl.*` stat names and made its OWN sentence LONGER**; **phase 73 predicted a clause REMOVAL and got a clause REPLACEMENT**; **phase 77 opened a family and narrowed nothing**; **phase 78's BRAINSTORM ADDED an item to an existing sentence, and its SPEC and PLAN each narrowed nothing.** => **the candidates sentence is a WINDOW onto a larger deferred backlog, not an inventory of it.** ⚠️ **A stage that predicts a decrease here is repeating the phase-73 error. State what your row narrows — usually nothing — rather than forecasting.**

⚠️ **NEVER WRITE A SENTINEL'S OWN MATCHER STRING INTO A FILE THE SENTINEL GREPS.** The sentinel greps **`docs/envoy-go/ROADMAP.md`** — writing these strings *here* is safe; writing them *there* is not. This fired LIVE at the phase-76 BRAINSTORM, twice in one commit. **`grep` cannot tell a mention from a use.** ⚠️ **THE PHASE-78 IMPL EDITS `ROADMAP.md`** — forbidden substrings in any new row-78 cell text are `deferred candidates:`, `remaining deferred (not-yet-chartered) candidates:`, and any `<Family>-family row` slug you do not intend to register.

⚠️ **THE SELECTION CALCULUS, CORRECTED AND STILL STANDING.** *"A candidate that clears a check-(3) blocker is worth **strictly more**"* does **not** hold: opening a family **MINTS** a check-(2) sentence, so phase 77's own arithmetic was **NET ZERO**. What survives: **opening a family is the ONLY way that family's work becomes enumerable at all — a prerequisite, not a shortcut.** Do not restore the stronger claim.

==============================================


## STATUS AS OF 2026-07-27: PHASE 78 IS **OPEN**. ROW 78 IS `in-progress`. THIS SESSION PERFORMS THE **PHASE-78 IMPL**.

**What the phase-78 PLAN landed** (`git log --grep 'phase 78'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): `docs/envoy-go/phases/78-boot-panic-visibility/PLAN.md` (a **TEN-task TDD spine T1-T10** with an **EIGHT-arm break roster**) + `PROGRESS.md` + the `STATE.md` roll. **`ROADMAP.md`, `BEHAVIOR_CONTRACT.md` and `DECISIONS.md` ALL BYTE-UNTOUCHED** (sha256-verified against master, with the gate's negative control observed on `STATE.md`). **Docs-only: ZERO production `.go`, ZERO test `.go` in the PLAN commit.** Four investigation agents on disjoint remits with private DETACHED worktrees, private scratch and banded ports, plus controller re-derivation of every load-bearing claim including an INDEPENDENT cross-product execution. **THE PLAN EXECUTED MOST OF THE ROW — the production change was built THREE TIMES INDEPENDENTLY, both guard layers are written and red-then-green'd, and SEVEN of the EIGHT break arms RAN THERE.**

### What matters for the NEXT session, in order

1. **⚠️ RE-RUN ALL THREE SENTINEL CHECKS YOURSELF AND RECORD THE ACTUAL OUTPUT; DO NOT PREDICT IT.** Create `stop` if and ONLY if all three print nothing. **Check (1) will print `NOT DONE: row 78` UNTIL your own T10 flips it** — after that it goes silent, and checks (2) and (3) are what keep `stop` from firing. `want` STAYS 110. **Re-run all three AFTER the `ROADMAP.md` edit lands.**

2. **⚠️ THE SINGLE MOST IMPORTANT THING TO CARRY FORWARD: THE FIX IS *NOT* "MOVE THE DEFER", AND THE SPEC'S OWN PERMITTED ALTERNATIVE IS RETRACTED.** SPEC §1.1 established by execution that relocating the `<-flusherDone` defer MOVES the hang rather than removing it. **The PLAN then found the SPEC contradicting itself**: SPEC §3.1 explicitly PERMITS a trailing sibling `defer cancel()` (*"the IMPL may take it if it reads better in situ"*) while SPEC §3.7 MANDATES a coverage guard asserting the `cancel` call is **inside the same function literal** as the receive. Two agents built the alternative independently — it is behaviourally CORRECT (`EXIT=2` in 0.0094 s) and **the SPEC's own mandated guard goes RED on it**. ⇒ **PLAN §1.1 RETRACTS the permission. The single-literal `cancel()`-FIRST form is the ONLY accepted shape.** The bounded-`select` alternative is refuted on its own terms: **5.012 s of dead wall-clock on every panic path.** ⚠️ **Do NOT take either alternative.**

3. **⚠️ A THIRD COUNTER-EXAMPLE IN THE R5 FAMILY: A stderr-NON-EMPTY ASSERTION PASSES ON THE HANGING TREE.** SPEC §1.1 leaves stderr as `—` for the hanging cell. Measured by the controller AND independently by an agent: the naive tree with a post-anchor panic and a stats sink emits **564 / 705 / 846 / 987 / 1122 / 1269 bytes of stderr** while hanging — growing with wall-clock, panic text **ABSENT** — and the composition is **pure statsd `write: connection refused` noise from the still-running flush ticker**. R5 proves output-only and exit-status-only each insufficient; this adds **output-VOLUME**. ⇒ **assert the panic TEXT, never a byte count.**

4. **⚠️ THE BEHAVIOURAL GUARD PASSES ON THE TREE THAT STILL HANGS — EXECUTED, not reasoned.** `TestMain_BootPanicIsVisible` returned `--- PASS` on the naive-relocation build. Its trigger panics PRE-anchor, and per SPEC R3/R8 **there is no config-reachable post-anchor panic** to drive a behavioural probe from. ⇒ **the structural coverage arm (T5) is LOAD-BEARING, not belt-and-braces**, and the behavioural red-then-green is NOT evidence the fix is complete. That is why T4 and T5 are separate tasks with separate reviewer gates.

5. **⚠️ ARM θ — THE MOST VALUABLE RESULT IN THE ROSTER, AND IT IS A NEGATIVE ONE.** On the FULLY-FIXED tree, `defer func() { <-make(chan struct{}) }()` injected before `boot.Construct` leaves **all three guard arms GREEN and the whole package 10/10 PASS while the binary wedges for 12 s at ZERO BYTES on BOTH streams** — quieter than the original defect, because the relocated defer's `cancel()` runs first and silences the flusher's error log. ⇒ **SPEC §3.4's detector is a METHOD, not a shipped test.** Say so in the ADR; do not try to fix it in this row.

6. **⚠️ THE BLAST RADIUS HAS A THIRD BUILD SITE NAMED IN NO PHASE-78 DOCUMENT BEFORE THE PLAN.** `test/conformance/h2spec/h2spec_test.go:210` (`TestH2Spec`, entry `:30`) also compiles and spawns `./cmd/envoy-go`, alongside `test/differential/harness.go:240` and `:594`. The project's conformance claim is **h2spec 53/53**. **T9 gates it.** If you exclude it, state the reason — do not exclude it silently.

7. **⚠️ THE FULL 120-FIXTURE DIFFERENTIAL IS MANDATORY.** Budget **~400-420 s per green attempt** (phase-77: 120 fixtures / 403 s; phase-76: 119 / 409.8 s); a `-race` pass is a SECOND run, not a substitute. Run with `-count=1` AND `-v`, capture the INNER exit status, and **scope the tally to `TestDifferential/` subtests then cross-check with `comm -3` against the fixture-dir set** — a raw `--- PASS` count exceeds 120 because the package declares 15 other top-level tests (phase 77 measured 134 for 120). ⚠️ **Without `-v` a green log is indistinguishable from a suite that ran nothing** — there is no pass tally and no per-fixture record; **failures ARE still visible**, which is the refinement the SPEC lacked. **Enumerate the TEN stats-sink fixtures individually** — `0089 0090 0091 0092 0093 0094 0098 0101 0112 0113` — and note **`0098-stats-sink-statsd-tcp` is the tree's ONLY stats-sink background mutator** and the send-on-closed-channel exposure.

8. **⚠️ COST: TEN, and RE-DERIVE IT AGAIN AT YOUR OWN TIP** (`reference_deferred_candidate_cost_restale`). The PLAN refuted the SPEC's 7-9 in BOTH directions: the **floor of 7 is unreachable** (the prose denominator is NINE candidate sites, not seven, across five separate regions, while T1's code diff is 7 added / 6 removed lines — folding them means a prose regression rides in on a green BUILD gate) and the **ceiling of 9 is exceeded** (T4 splits into T4+T5). ⚠️ **The SPEC's own calibration sentence is imprecise**: phase 76's ~5-7 was its BRAINSTORM; its SPEC already said 7-9 and its PLAN shipped 9 — the CEILING of its own band. The real pattern is two-for-two at the ceiling (76: 7-9 -> 9; 77: 11-13 -> 12). ⚠️ **Two agents independently said 8-10 and 9-10, but both read the same SPEC — AGREEMENT ACROSS DOCUMENTS IS NOT EVIDENCE.**

9. **⚠️ A NEW GATE HAZARD — BROKEN-GATE COUNT 13 -> 14: `.golangci.yml` enables `misspell` with `locale: US`.** An agent's first draft was flagged **3x** (`behavioural` x2, `CANCELLED`), **and the phase-78 SPEC uses "behavioural" throughout** ⇒ **do NOT paste SPEC or ADR prose into `.go` comments.** The standing thirteen: a full-suite recipe **WITHOUT `-v` is VACUOUS** · a sha256 roster built from one tree is **desynced BY CONSTRUCTION against a DELETED file** (span BOTH trees) · `gofmt -l` **NEVER exits non-zero** (gate on OUTPUT; the `&&` form is inert) · `go doc -all <A> <B>` **silently swallows arg2** · a **`+0 exported symbols`** gate over an EMPTY package goes RED on a CORRECT tree · a **RANGE** gate cannot detect anchor drift (use a content/symbol anchor) · a roster's naive `[ -f ] || continue` **exits 0 on a DELETED file** · a **count-only** stat guard **PASSES a build with BOTH names wrong** · a `-run` no-match **exits 0** with `[no tests to run]` · a `--- PASS` tally over a package with sibling tests **exceeds the fixture denominator** · a stat-delta claim **cannot be discharged by guards scoped to another package** · a **stderr-VOLUME** assertion **passes on the hang**. ⚠️ **A harness's exit code is not the command's.**

10. **⚠️ THE `+0` STAT-SURFACE CLAIM CANNOT BE DISCHARGED BY A `TestNoNewStat*` RUN.** All **five** live in `internal/statssink/registration_test.go` (`:26 :53 :81 :109 :137`) and guard **that package's** registration sites; **none applies to `cmd/envoy-go`**. The available argument is **STRUCTURAL** — `main()` contains no `NewCounter`/`NewGauge` call and the production diff is confined to it. Phase 76 used exactly this form. **State it as structural; do not cite the five guards as coverage.**

11. **⚠️ THE PROSE DENOMINATOR IS NINE CANDIDATE SITES, NOT SEVEN.** SPEC §3.8 misses **`:365-367`** — which sits PHYSICALLY ADJACENT to the relocation anchor and becomes `flusherDone`'s only remaining stale comment once `:289-297` is rewritten — and **`:396`**, which SURVIVES and must be RECORDED as checked so a later sweep does not "correct" a true comment (the R11 failure mode). Also: **`:198` is a CLARIFICATION, not a correction** — it carries no location claim and was never false, so a break written against it would be vacuous. Re-derived audit: **3 FALSE · 3 INCOMPLETE · 3 SURVIVE**, plus the pre-existing `:201` "both"-vs-five drift. ⚠️ **`:304-308`, `:112-113`, `:396` and `:93` stay BYTE-UNTOUCHED.**

12. **⚠️ THE BEHAVIOURAL GUARD'S CONJUNCTION, AND WHY EACH LEG EXISTS.** Leg 1 = the process did not hang, detected from `ctx.Err() == context.DeadlineExceeded`, **never from an exit code** — measured, a HEALTHY boot and a HUNG boot are **byte-identical on every status observable** (`ctx.Err()` = `context deadline exceeded`, `runErr` = `signal: killed`, `ExitCode()` = **-1** on both); they differ **only in stdout** (109 B of ready sentinels vs 0 B). ⇒ **leg 2 is insufficient through BLINDNESS, not satisfaction-by-success** — a sharpening of SPEC R5 — so leg 1's failure message must print `stdout.Len()` and its first line to discriminate its two red modes. Leg 1 is the **sole `t.Fatalf`**; legs 2/3/4 are `t.Errorf` (`reference_fatalf_makes_assertions_unreachable`), which break arm ζ proves load-bearing by firing **all four in one run**. ⚠️ **SIGKILL, never SIGTERM** — `exec.CommandContext`'s default cancel is `Process.Kill`; do not set `cmd.Cancel`. A SIGTERM cancels the server ctx, releasing the very wait that is hanging: measured, `EXIT=124` at 8.006 s **with the panic text PRESENT**, which reads as "printed, just slow".

13. **⚠️ THERE IS EXACTLY ONE CONFIG-REACHABLE IN-WINDOW PANIC TRIGGER AND NO FALLBACK** — two HCM chains on **DISTINCT** addresses sharing a `stat_prefix`, reaching `internal/stats/registry.go:107`. The same-address candidate is refuted (listener metrics register POST-bind ⇒ `bind: address already in use`, exit 1). ⚠️ The registration cite `internal/filter/hcm/config.go:352-358` **truncates**: `:352` derives the prefix; the block is `:358-362`; `:358` (`downstream_rq_total`) is the one that collides. Anchor the doc comment on the **message string and the counter name**, not only the lines. ⚠️ **THE EXPOSURE IS PANIC-GENERIC** — an inserted nil-deref hangs identically, so scoping anything to `panic(` call sites scopes it to the WRONG SET; label any `panic(` denominator structurally incomplete at the point of use.

14. **⚠️ ANCHORS DRIFT, AND THIS ROW MOVES ITS OWN.** At the PLAN tip: `DECISIONS.md` **17560** · `ROADMAP.md` **226** · `BEHAVIOR_CONTRACT.md` **5750** — all three BYTE-UNTOUCHED, so every existing cite holds. ⚠️ **But T1 and T2 move `main.go`'s line numbers under this row's own later tasks** — every task must re-derive its anchors at the tip it is editing. ⚠️ **SPEC §13's exit table says DECISIONS `17531` "at this close"; the true value is `17560`** (17531 is the pre-commit figure — `STATE.md` and this file both have it right). ⚠️ **`cmd/envoy-go` declares 8 test functions and FIVE helpers, not four.**

15. **⚠️ BEHAVIOR_CONTRACT §7 — THE ANCHORS AND THE FORBIDDEN CLAIM.** Insert after `:872`, before `:874`. ⚠️ **Anchor on the PARENTHESISED heading `### Does not yet apply to (bootstrap config validation)`** — the bare string occurs **15** times in the file. ⚠️ **DO NOT write "a boot failure is never silent"** — ADR-0300 §Context ¶11 forecloses it: `os.OpenFile` at `internal/accesslog/writer.go:56`, reached from `main.go:117`, blocks forever on a reader-less FIFO **before any defer is registered**. Scope the statement to *panics unwinding through `main`*. ⚠️ **DO NOT inherit `github.com/esalaine/envoy-go/validate` from the neighbouring `:862`** — the real module path is `github.com/pgdad/envoy-go`.

16. **⚠️ DOCUMENTARY DEFECTS, STILL NAMED AS DEFERRALS AND STILL DELIBERATELY NOT FIXED.**
    - **The documented PUBLIC IMPORT PATH DOES NOT EXIST.** **36 occurrences** of `esalaine/envoy-go/validate` repo-wide, including `BEHAVIOR_CONTRACT.md:862` (inside the very section this row extends) and four in `DECISIONS.md`. ⚠️ **`DECISIONS.md:142` is `## ADR-0006: module path \`github.com/esalaine/envoy-go\`` — an ADR that DECIDES the wrong path, never superseded.** ⚠️ **Fix by PATTERN `esalaine/envoy-go` (bare), NOT the `/validate` form** — ADR-0006 has no `/validate` suffix and is invisible to the narrower grep.
    - **A mechanical stat-surface count** to replace the documentary 1207 — **8-11 tasks**.
    - **UNRESOLVED:** the *"plus two more in the same file"* half of the `BEHAVIOR_CONTRACT.md` stale-cite claim (`internal/tls/config.go:272`, `internal/filter/http/chain.go:19` flagged, **not audited**).
    - `BEHAVIOR_CONTRACT.md:501` calls the `wasm.` arm a *"NEW SN9 flattening rule"*, which **COLLIDES with ADR-0118's actual SN9**; and `internal/stats/name.go:350`'s error string enumerates `(want cluster.|http.|listener.|server.)` — **already wrong**.
    - **The `/stats/prometheus` projection-completeness gap** (`ROADMAP.md:208`) — 30 names / 6 families / 28 pre-dating phase 77. **UNBLOCKED by row 78** once it lands.
    - ⚠️ **`STATE.md` §Project counts is STALE ON THREE AXES** — fixtures **119** (real 120; flagged), stat surface **1205** (lineage **1207**; NOT flagged), DECISIONS tail **ADR-0298** (real **ADR-0300**; NOT flagged). A session anchoring there gets the wrong tail ADR. **Recorded, not fixed.**
    - ⚠️ **A live PORT-CONVENTION CONTRADICTION**: this file says next-free reference port **10119** (family-banded `10<index>`); `STATE.md` §Project counts says *"`10450`, NOT `10118` — ports are NOT fixture-index aligned"*. Executed: `0117/envoy.yaml:56` ⇒ `10117`, `0118/envoy.yaml:90` ⇒ `10118` — **the index-aligned convention is what actually landed.** Row 78 adds no fixture, so this is informational.

17. **⚠️ A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX — the TENTH consecutive stage at which this held.** The index is `~/.claude/projects/-home-esa-git-envoy-go/memory/` (exactly four flake files). Live hazards: the full-suite startup flake (`subject ready: EOF` **and** `bind: address already in use`, both failing **BEFORE any assertion**, the latter as a **PANIC that can abort the whole binary**, firing **MORE readily under `-race`** and as the fixture count grows — now **120**; ⚠️ hardened at `0e9cc680`, so a post-hardening recurrence warrants suspicion, not acceptance) · `reference_sds_init_fetch_timeout_dial_budget_flake` — ⚠️ **TWO packages, which the SPEC does not say: `internal/xds TestProvider_FetchInitialCertificate_Timeout` AND `internal/boot TestSDSEndToEnd_FetchFailure_BootFailsClosed`, and `internal/boot` is the DIRECT CALLEE of the line this row edits (`main.go:316`)** · the PRE-EXISTING `internal/cluster` `-race` outlier flake — ⚠️ **name it: `TestOutlierDetector_ConcurrentEjectExactlyOnce`**, so an unrelated `internal/cluster` `-race` failure is not laundered through the exemption · two still-**UNINDEXED** load flakes (`internal/httpclient TestOptions_ZeroValue_NoOpDefaults`, `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery`), carried as prose for a **ninth** stage. **Isolate-re-run, then state the classification AND its evidence.** ⚠️ **`0061-lb-ring-hash` is NOT a live flake — a spread failure there is a FINDING.**

18. **Counts at this tip (re-run MECHANICALLY in your worktree; never copy them):** fixtures **120** (`ls -d test/fixtures/[0-9]*/ | wc -l`; ⚠️ the faithful `discoverFixtures` predicate is `^[0-9]{4}[a-z]?-`, verified against `runner_test.go:1461-1497` and NOT drifted; next-free **0119**) / fuzzers **55** (⚠️ the `internal/`-vs-repo-wide pair is **NOT discriminating** — both return 55; use the append-a-`func Fuzz` negative control) / **stat surface 1207**, ⚠️ **DOCUMENTARY — assert the DELTA** / **BackendKind tail 38** (⚠️ a TAIL VALUE — 39 constants, 0-38; **do NOT "fix" 38 to 39**) / go.mod modules **2** (⚠️ the phase-61.2 lineage figure — **NOT a repo total**; the single `go.mod` requires **67**; **do NOT "fix" 2 to 67**) / internal packages **73** / blank imports in `runner_test.go` **120**, ⚠️ **anchor on the FULL `^\t_ "github.com/pgdad/envoy-go/test/fixtures/` prefix — a naive `^\t_ ` gives 126** / DECISIONS tail **ADR-0300 PROPOSED** (next-free **ADR-0301**; block shape **1 Context / 0 Decision / 0 Consequences / 1 retained footer / 0 `---`**, and the footer is the **LAST LINE OF THE FILE**, so the append lands at EOF) / next-free reference port **10119**.

## What THIS session does — the phase-78 IMPL

One stage per session. Execute the **T1-T10** spine of `docs/envoy-go/phases/78-boot-panic-visibility/PLAN.md` red-first, append the `# IMPL record` half of `PROGRESS.md`, complete **ADR-0300 §Decision + §Consequences IN PLACE** after the retained italic footer (**no renumber, NO `---` separator**), land the `BEHAVIOR_CONTRACT.md` subsection, and **FLIP ROW 78 `in-progress` -> `done`** at the six-gate. Roll `STATE.md` §Current pointer **IN PLACE** (ADR-0288) and this file.

⚠️ **RE-RUN EVERY BREAK ARM AT YOUR OWN TIP.** Seven of the eight ran at the PLAN, but a prior stage's red is not this stage's red, and `main.go`'s line numbers move under T1/T2.

⚠️ **CITE THE SIX-GATE CORRECTLY: *"the six-gate (`BOOTSTRAP_PROMPT` §7.5, gates (a)-(f) at `:357-366`, at the REPO ROOT) … the SOLE leg (ADR-0106)"*.** **ADR-0106 does NOT define the six-gate** — its block (`DECISIONS.md:4788-4857`) contains **zero** occurrences of "six"; its subject is the §9 family-expansion shape and the SOLE-leg property.

⚠️ **`ADR-0089` stays BYTE-UNTOUCHED unless your row lands `/runtime` or `POST /runtime_modify`** — row 78 lands neither. **`internal/stats/**` stays BYTE-UNTOUCHED** — this row makes panics visible; it does not re-litigate which should exist.

Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** (`reference_parallel_subagents_private_scratch`), **BANDED PORTS**, and **must tear down docker containers BY NAME, never by an `ancestor`/image filter** (`reference_parallel_agents_shared_machine_namespaces`) — and ⚠️ **if two agents share ONE worktree they must stage by EXPLICIT PATH; a `git commit -am` sweeps in a sibling's uncommitted work.** ⚠️ **AND THEY MUST NOT RUN AN UNSCOPED `git restore`.** Per-task `gofmt` + `golangci-lint` on touched packages (`feedback_pertask_gofmt_lint`).

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. ⚠️ **THIS HAS NOW FIRED LIVE IN FOURTEEN CONSECUTIVE SESSIONS** — at this PLAN it fired twice on plain read-only command blocks. **Assume it fires every session.** **Use `git -C <abs-worktree-path>` for every git command, prefer absolute paths over `cd`, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — Current-pointer section (TRUSTWORTHY; ADR-0288 discipline — ⚠️ **anchor greps on `^- **<field>`**, not bare; all seven fields verified singleton at the phase-78 PLAN close) + the Recent lineage. ⚠️ **§Project counts is STALE ON THREE AXES** (item 16) — do not anchor on it.
2. `docs/envoy-go/phases/78-boot-panic-visibility/PLAN.md` — **the whole document**, especially **§1.1** (the SPEC's self-contradiction and the retraction), **§1.2** (the stderr-volume counter-example), **§1.3-§1.4** (the nine-site prose denominator), **§1.5** (the third build site), **§1.6** (the `misspell` gate), **§1.8** (the cost re-derivation), **§2** (Global constraints — every task's requirements implicitly include it), **Tasks 1-10**, **§4** (the break map), **§5** (the twelve gates with their negative controls), **§7** (deferrals) and **§8** (live hazards).
3. `docs/envoy-go/phases/78-boot-panic-visibility/PROGRESS.md` — the PLAN record half; append the IMPL record half.
4. `docs/envoy-go/phases/78-boot-panic-visibility/SPEC.md` — ⚠️ **read it AFTER the PLAN**, and read **§3.1's alternative (ii) as RETRACTED**, **§3.8's denominator as SEVEN-OF-NINE**, **§9's task band as SUPERSEDED (7-9 -> 10)**, and **§13's DECISIONS figure as WRONG (17531 -> 17560)**.
5. `cmd/envoy-go/main.go` — read `:186-210`, `:289-350`, `:355-397` FIRST-HAND before writing anything. `cmd/envoy-go/main_test.go` — note it is **`package main`**, not `package main_test`.
6. `docs/envoy-go/DECISIONS.md` — **ADR-0300 §Context** (the eleven paragraphs this IMPL must not contradict; ¶4 is the reusable core and ¶11 bounds the BEHAVIOR_CONTRACT wording) · **ADR-0045** (the split gate) · **ADR-0288** · ADR-0044 (⚠️ it does **NOT** contain the Context-draft discipline — use `ADR-0044-as-used`) · **ADR-0106** (⚠️ the SOLE-leg property, **not** the six-gate).
7. `docs/envoy-go/phases/77-runtime-static-layer/PROGRESS.md` and `docs/envoy-go/phases/76-lb-ring-hash-spread-margin/PROGRESS.md` — the shape of an IMPL record in this lineage.
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — especially **`reference_probe_must_discriminate`** (necessary but NOT sufficient), **`reference_independent_probes_can_share_a_blind_axis`**, **`reference_probe_input_is_a_claim`**, **`reference_timeout_exit_124_shared_by_healthy_and_hung`**, **`reference_liveness_break_needs_failing_baseline`**, **`reference_gate_command_negative_control`**, **`reference_deliberate_break_wrong_assertion`**, `reference_empty_output_is_not_a_zero_result`, `reference_fatalf_makes_assertions_unreachable`, `reference_harness_exit_code_is_not_command_exit_code`, `reference_a_drift_correction_is_itself_a_claim`, `reference_stale_cite_recurs_fix_by_pattern`, `reference_deferred_candidate_cost_restale`, `feedback_brief_citations_not_evidence`, `reference_next_prompt_tracked_despite_gitignore`, `feedback_git_worktrees`, `feedback_execution_style`, and **`reference_bash_cwd_reset_commits_to_main`**.
