=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **PHASE 86 (`validate-sds-nil-provider`) CLOSED AT THE IMPL (2026-08-12): ROW 86 IS `done` AT `ROADMAP.md:148`, ALL 118 ROWS ARE `done`, AND `want` IS 118.** ⇒ **THIS SESSION PERFORMS A SELF-PICKED BRAINSTORM** per the standing directive (no banked mid-lifecycle work exists). `STATE.md`'s `next-skill:` reads `BRAINSTORM — SELF-PICK` — that IS your instruction. The candidates ledger is in §Candidates below; re-derive EVERY cost at your tip. The BRAINSTORM registers the new row `in-progress` in `ROADMAP.md` AND bumps `want` 118 -> 119 in the SAME commit (the phase-84/85/86 precedent).

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=118 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **`want` IS 118 AT THIS TIP; the BRAINSTORM's row registration bumps it 118 -> 119 in the SAME commit as the new row.** ⚠️ **AT THIS TIP check (1) is SILENT — the THIRD silent reading in project history (after the row-84 and row-85 closes); after the registration it prints EXACTLY `NOT DONE: row <new>`. State both as measurements, run BOTH sides.** ANY OTHER row id appearing is a FINDING. ⚠️ **THE SENTINEL DOES NOT FIRE AT EITHER SIDE:** check (2) prints SIX (at `:196 :202 :208 :218 :224 :232` — re-derive, the anchors drift with every edit) and the condition is a CONJUNCTION. Check (3) is SILENT.

⚠️ **THE DOCTORING NCs REMAIN MANDATORY EVERY SESSION.** Both:
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md at want=118: pre-registration expect "NOT DONE: row 62" ALONE (the silent side still LOOKS); post-registration "NOT DONE: row 62" AND the new row
```
```sh
# the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```

⚠️ **AND WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -oiE '-family row'` fails with `grep: amily row: No such file or directory` and the surrounding arithmetic prints `base=0 now=0 delta=0`, **which reads exactly like "no change".** Observed live at FOUR phase-84 stages. **Always pass `--` before the pattern**, and remember the counts are form-dependent: **95 occurrences vs 67 LINES** at this tip (`grep -c` counts LINES). **A gate that reads zero on BOTH sides is not evidence of invariance.** ⚠️ AND the carried **ARM-A {119, 131} figure binds ONLY with its escape-aware pipe-split command** (BRAINSTORM-86 §6) — ad-hoc field-count forms do NOT reproduce it (`NF != 8` flags 17 rows); do not "correct" the figure with a different form.

**SENTINEL RE-RUN MECHANICALLY AT THE PHASE-86 IMPL CLOSE (2026-08-12, base `b2b5be7e`), BOTH SIDES OF THE ROW-86 FLIP. IT DOES NOT FIRE; `stop` was NOT created** (verified absent at repo root AND stage worktree, both sides). ACTUAL output, recorded not predicted: input measured **236 lines / 118 data rows** first; pre-flip **(1) `NOT DONE: row 86`** alone; post-flip **(1) SILENT — the THIRD silent reading**; **(2) SIX — `:196 :202 :208 :218 :224 :232`** both sides (⚠️ do NOT "fix" it and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases; the candidates sentences are a WINDOW onto a larger deferred backlog, not an inventory of it); **(3) SILENT** both sides. The condition is a CONJUNCTION ⇒ the sentinel does NOT fire — and `STATE.md`'s `next-skill:` reads `BRAINSTORM — SELF-PICK`, i.e. a next stage EXISTS and the loop continues.
- **ALL FOUR NCs FIRED AT THIS CLOSE, BOTH SIDES:** row-62 doctoring => pre-flip `NOT DONE: row 62` AND `NOT DONE: row 86` / post-flip `NOT DONE: row 62` ALONE — the proof the silent side still LOOKS — with `NC LANDED? [ in-progress ]` inspected first; `want=117` => `GATE FAIL: examined 118 data rows, expected 117`; check-(3) doctoring (residual confirmed **2 -> 0** on the doctored copy first) => `NEVER OPENED: gRPC` while `WASM` correctly stayed silent; check-(2) one-arm counts **5** (long arm alone) / **1** (short arm alone), union **6** (a one-arm strip is NOT an NC for the union).
- ⚠️ **Re-run all three checks and both doctoring NCs yourself, BOTH SIDES of your row registration, and record what YOU see.**

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (ugrep under the hood). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Reproduced at TWENTY separate tips.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **THE METHOD BINDS; carry no standing discrepancy figure and no standing absolute.** ⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS; print FILE NAMES, never a count.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files; use `-v --no-index`, and `git status` is the discriminating check). ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **`grep -c` ON ZERO MATCHES IS TOOL-SPECIFIC** (only `git grep -c` prints nothing; all exit 1 — capture the value, never chain on the exit code). ⚠️ **`grep -c` COUNTS *LINES*, NOT *OCCURRENCES*.** ⚠️ **`\t` IN GNU ERE IS A LITERAL `t`** — use `-P`. ⚠️ **A `git grep` PATHSPEC ENDING IN `/` READS A FAIL-UNSAFE ZERO** (shape 33) — use the shell-glob form. ⚠️ **AN UNANCHORED PANIC-GREP FALSE-FIRES 14 TIMES ON A FULLY-GREEN DIFFERENTIAL LOG** — **the canonical gate is ANCHORED: `^panic:|DATA RACE|SIGSEGV`. A gate that fires on a green log is worse than no gate.**

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`** with FOUR blind spots (an em-dash, a DOT in `http-1.1`, two LETTER-suffixed ids). ⚠️ **EXECUTED on scratch copies at the phase-77 PLAN: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** ⚠️ **DO NOT restore the regex form.** At this tip check (1) is SILENT — **the row-62 doctoring NC is what proves the silent side still LOOKS**; any row id other than your own new row appearing on either side is a finding.

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

⚠️ **Run the union form, never one half of it.** ⚠️ **A ONE-ARM STRIP IS NOT AN NC** (6 -> 5 short / 6 -> 1 long; never 6 -> 0). ⚠️ **The documented trap's stated MECHANISM is refuted while the trap is real** — the inflation comes from ROADMAP table rows carrying BRAINSTORM prose. **Cite the command; do not re-inherit the explanation.** ⚠️ **The line numbers drift with edits — re-derive them; the current six are `:196 :202 :208 :218 :224 :232`.** ⚠️ **These six paragraphs remain the final sentinel blocker, and any edit near them is a sentinel-affecting edit.** All six are standalone family PROSE paragraphs (not rows), naming ~42 distinct candidates; every "retire the paragraphs" shape is doctrinally foreclosed (`reference_sentinel_matcher_string_self_clears`). ⚠️ If the self-pick comes from OUTSIDE the windows (the row-85/86 shape), **nothing narrows in any window sentence at row-done**; if it comes from INSIDE a window, the narrowing rules of that window's family apply — read the window sentence FIRST.


==============================================


## STATUS AS OF 2026-08-12: PHASE 86 CLOSED AT THE IMPL (row 86 `done` at `ROADMAP.md:148`, ALL 118 ROWS DONE, base `b2b5be7e`). **THIS SESSION PERFORMS A SELF-PICKED BRAINSTORM** (the 2026-07-12 standing directive; no banked mid-lifecycle work).

**What the 86 IMPL landed (2026-08-12, base `b2b5be7e`, branch `phase-86-impl`, ONE squashed commit — D-86-SEQ held).** Production **+150 net `.go`** / 5 files (INSIDE the PLAN band): the `internal/xds` no-fetch sentinel (`NoFetchProvider()`/`IsNoFetch()`; `IsNoFetch(nil)==false`) · the `internal/boot` `newSDSProviderAndClient` split + `NewValidateSDSProvider` (ENTIRE body moved — 89-vs-89 lines, only the seven return sites differ; the never-dialed client CLOSED, D-86-CONN — ⚠️ UNGATED, deleting it stays green, a named candidate) · three `internal/tls` `IsNoFetch` fetch-site skips + the `sdsCertPromised` interplay (reject BYTES untouched) · `validate/validate.go` threading. Tests **+1116 net** / 6 files — ⚠️ **OVERRAN the 760 ceiling by 356, the TENTH consecutive `reference_measured_prototype_is_a_lower_bound` firing, recorded in ADR-0308 §Consequences (iii)**. Docs: the four contract edits (BC 5955 -> 5957; item-7 REVERSAL `:1064`, roster `:1052`, drop `:1036`, extension `:950-952`) · ADR-0308 COMPLETE (18066 -> 18098, strict guard 1 -> 0) · row 86 `done` (numstat `1 1`). **The parity contract demonstrated by execution:** all eleven shapes at the tip (accepts exit 0 `configuration OK`; n1/n2/n4/n6/n7 message-transitioned to the boot strings; n3 already-parity) + per-shape parity-string diff vs normal-mode boot, core messages BYTE-IDENTICAL. **Contract: validate accepts iff boot accepts, MODULO exactly the rejects that require I/O to discover (D-86-N5).**

### ⚠️ FIVE EXECUTION FINDINGS (the PLAN claims that did not survive — carry them)

1. **PLAN §6's n2 recipe was AMBIGUOUS:** "arm A + a second SDS position" means TWO SDS-bound CONTEXTS (two listeners/chains, one entry each — the boot cross-context cap `multiple SDS-bound downstream TLS contexts unsupported`); two entries in ONE list trips `xds.ParseSDSConfig`'s DIFFERENT per-context string (`MVP takes one, got 2`) — an n3-class already-parity shape. The census fix round rebuilt it.
2. **n3's WRAP format changed:** listener-wrapped pre-fix (fired at the tls layer) -> bare pre-scan string post-fix (`NewValidateSDSProvider` runs BEFORE `Construct`); the frozen substring never changed; true call-site parity exists only post-fix (ADR-0308 §Consequences vi).
3. ⚠️ **"validate never passes nil" is FALSE unscoped** — at seen==0 validate threads `(nil, nil)` exactly like boot; the nil-consumer roster is **TWO unconditional (QUIC hardcoded; main.go seen==0) + ONE conditional (validate at seen==0)**. Every landed comment and the contract text carry the scoped form; the negative form is on the record in ADR-0308.
4. **Three ADR-0308 citation defects were caught pre-squash by review** — ⚠️ including a wrong `internal/xds/xdsgrpc/...` path for the grpcclient Close pins **which ALSO lives in SPEC-86/PLAN-86 prose (prior-stage records NOT rewritten; ADR-0308 §Decision (b) carries the correct `internal/grpcclient/grpcclient_test.go:1911/:1982`)**; stale base-tip pin locators (now dual-labeled: base `:921/:1198/:1310` = tip `:966/:1402/:1514`); a RETIRED `require_client_certificate` gate nearly re-inscribed (ADR-0289: the VC-SDS fetch is un-gated).
5. **Two `:518` locators were minted stale WITHIN the branch** (config.go gained lines above the reject) — fixed by the final-review fix wave; cite that reject BY STRING (`no tls_certificates configured` in `commonTLSContextToConfig`), never by line. ⚠️ One probe caveat: `validate_test.go`'s `deadPort` helper transiently binds an ephemeral loopback socket (closed, never dialed) — "validate binds nothing" is true of the PRODUCT, not of that helper.

### ⚠️ COUNTS AT THIS TIP — RE-DERIVE MECHANICALLY, NEVER COPY

- **differential fixtures 121** — numeric tail `0119-grpc-unary-trailers`, next-free `0120`.
- **`ROADMAP.md` 236 lines / 118 data rows** (row 86 `done` at `:148`); check-(2) anchors `:196 :202 :208 :218 :224 :232`; `-family row` **95/67**; `gRPC-family row` **2**; `Operational-tooling-family row` **3**; ARM-A flags **{119, 131}** (escape-aware command ONLY).
- **`DECISIONS.md` 18098** lines · **307** headings · tail **ADR-0308 COMPLETE** · next-free **ADR-0309** (TAIL-derived) · strict `PROPOSED` guard **0** · STATUS census **21** · `^---$` **216**.
- **`BEHAVIOR_CONTRACT.md` 5957** · **`STATE.md` 64** · **`STATE_HISTORY.md` 482** · **`BOOTSTRAP_PROMPT.md` 522** · phase dirs **127**, `REVIEW.md` **37** (standing departure, named not claimed).
- **h2spec: cite `95 tests, 94 passed, 1 skipped, 0 failed` ONLY from your own tip's run** (skip 6.9.2/2 invariant).
- **fuzzers 55 / 48 files** · **BackendKind tail 38** over 39 declarations (do NOT "fix") · stat call sites: assert DELTAS only (two forms in circulation — 208/36 ARM-1 vs 145/21 `NewCounter(|NewGauge(` — NEITHER corrected; state which form you ran; the 86 IMPL measured 145/21 both sides, DELTA 0).
- **validate surface (post-86):** `validate/validate.go` **78** · `validate/validate_test.go` **836** · `cmd/envoy-go/main_test.go` **1628** · `internal/tls/config.go` **582** · `internal/tls/config_test.go` **2604** · `internal/boot/boot.go` **413** · `internal/boot/validate_sds_provider_test.go` **270** (NEW) · `internal/xds/provider_nofetch.go` **35** + test **32** (NEW).
- ⚠️ **STILL CONTESTED, NO NUMBER CARRIED:** the `STATE_HISTORY.md` archive-gap total · the loose `PROPOSED` matcher's whole-file count · the stat-surface absolute (1205 vs 1207 — WIDENED; only DELTAs asserted) · the zero-test `generic/*` suite count (13 vs 14).

### ⚠️ DOCUMENTARY DEFECTS — carried forward, still deliberately NOT fixed

**`ADR-0051` §2's false gate-scope sentence** corrected on the record by ADR-0307. **`ADR-0058`** cites `routerActionH2.doH2` at a dead location. **`ROADMAP.md` rows at lines 119/131 malformed** (ARM-A GUARD, not a fix). **`STATE.md` §Project counts FROZEN at phase 76.** **`harness_test.go:208` port inventory STALE.** **`body.go` nolint INERT** · **xDS cycle guard NOT AUTOMATED** · **`wasm/doc.go:219` two errors** · **ROADMAP cites `esalaine` FIVE times** · **`rbac.go:50` token `F2`** · **root `PROGRESS.md`** stray phase-32.1 doc · **NEW: SPEC-86/PLAN-86 prose cites the grpcclient Close pins at a nonexistent `internal/xds/xdsgrpc/...` path** (prior-stage records not rewritten; ADR-0308 §Decision (b) is the correction on the record). The phase-66-era `BEHAVIOR_CONTRACT.md:1062` reversal and the consumer-comment staleness are RESOLVED — landed at the 86 IMPL.

### Candidates ledger (for THIS session's self-pick; re-derive EVERY cost at your tip)

- **The CONTINUATION two-sided repair** — strongest KNOWN product defect (server discard `conn.go:255-259` + client blindness + the client-side SETTINGS gap `h2/client.go:376-407`, all named in ADR-0307). h2spec MEASURED not to gate either side. Est. 2-4x row 85 (+1046 realized) — the natural next LARGE row; charter as a SPLIT phase.
- **The stat-surface recount** (1205-vs-1207 WIDENED; rides a future +0 row) · **`ssl.connection_error`** (+444 whole-`.go` floor) · **`test/conformance/grpc/`** (9/26 reachable) · **REVIEW.md restoration** (process-not-product) · **hygiene fold-ins** (`harness_test.go:208`, xDS cycle-guard) · **NEW: the D-86-CONN `client.Close` gate** (UNGATED invariant; ~10 lines riding `validate_test.go`'s goroutine-count idiom — likely too thin alone, a fold-in candidate) · the **~42 candidates in the six family windows**.

### ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + BOTH DOCTORING NCs YOURSELF, BOTH SIDES OF YOUR ROW REGISTRATION** — record ACTUAL output.
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** Lineage 86 closed with FIVE findings (above) — the tenth consecutive lower-bound firing says: the SPEC must ENUMERATE by prototype, and every budget is a FLOOR.
3. **LINE ANCHORS DRIFT; SYMBOL ANCHORS DID NOT — RE-VERIFY EVERY CITE AT YOUR TIP.** The 86 lineage minted TWO stale-locator defects inside its OWN branch; cite by string/symbol where possible.
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Still live: SDS dial-budget (TWO packages) · `internal/cluster` `-race` outlier · `internal/httpclient` zero-value · driver-receiver port race (roster 42; `0081` binds `0.0.0.0:42039`) · the two 84.2-era flakes (cleared, unclassed) · `TestServerConn_TinyWindowDelivery` (85-IMPL, cleared, unclassed) · the REFERENCE h2spec section-8 flip (reference-side only). ⚠️ ONE new event at the 86-IMPL close-verification: `go test ./...` run 1 IN THE WORKTREE failed package `internal/boot` (⚠️ test identity NOT captured — a recorded process lapse; capture `-v` or a log BEFORE re-running), run 2 green 127/127, cleared package x4 + SDS-scoped x5; the gates battery's own full run was green. Consistent with the SDS dial-budget class — UNCLASSED until it recurs with an identity; recurrence in-band is a FINDING.
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only).
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`.
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL.** **A harness's exit code is not the command's.** **`gofmt -l` never exits non-zero — gate on OUTPUT.**
8. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-86-*` or stage worktree should outlive its stage close** — expect only `master` plus your own (verify with `git worktree list`).
9. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** (a)/(b) differential 121 fixtures, full suite with `INNER_EXIT`; (c) h2spec cite only from your own run; grpc-conformance deferred in writing; proxy-wasm 10/16; (d) fuzzers 55/48; (e) `INNER_EXIT` on every differential launch AND `go test ./...`; (f) no REVIEW.md — standing departure.

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 86 (validate-sds-nil-provider)' --format='%h %s'   # 4 at this tip: BRAINSTORM + SPEC + PLAN + IMPL — a CLOSED phase, use it as a FIRING CONTROL
git log --grep '^phase 85 (h2spec-selector-repair)' --format='%h %s'      # 4 — also CLOSED
```
⚠️ **The count comments are LIVE FIGURES — VERIFY before relying.** Take the NEWEST line (`| head -1`).

Execution style: **subagent-driven per `feedback_execution_style` is the STANDING PREFERENCE** — the 86 IMPL ran 6 implementers + 6 reviewers + scoped re-reviews and the reviews caught FIVE defect classes pre-squash (citation rot, an overclaiming comment, a dead assertion); the inline departure remains AVAILABLE for cheap-probe stages — NAME the choice either way. Subagents commit LOCALLY on the ONE stage branch; the controller squashes (`feedback_subagents_no_push`, `feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** and tear down docker **BY NAME**. ⚠️ **Band private ports outside BOTH `20000-31007` AND `11000-14999`, clear of the static fixture ports (10000-19172); `0081` binds `0.0.0.0:42039`.** The 86 lineage used 47000-47299 as TEMPLATE VALUES ONLY — a fresh BRAINSTORM's probes band from **47400+**. ⚠️ **ONE WORKTREE PER AGENT preferred; explicit-path staging if shared; never an unscoped `git restore`.** Per-task `gofmt` + `golangci-lint` on touched packages (⚠️ misspell locale US). ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch.** ⚠️ **Docker BY NAME (`b87-*` suggested for the next phase's probes), `--rm`, verify gone.**

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root. Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — sha256 against the base blob, never by eye; work DETACHED so a failed revert cannot dirty a branch.

⚠️ **THE ARCHIVE-ABSENCE GUARD — USE THE ROBUST FORM** (bullet-anchored + whole-file fixed-string, NC'd with a REAL target re-derived FROM THE FILE first; a control's own count is a LIVE FIGURE: `phase 82 … IMPL done` read **11** at this close, up from 10; `phase 83 … PLAN done` read **9**, up from 8). **Verify STRICTLY APPEND-ONLY**: `git diff --numstat` = `N 0` AND base a byte-exact PREFIX (`cmp`). The BRAINSTORM close rolls §Current to BRAINSTORM-done and §Recent evicts per the DIRECT DATE READ at that close (the five dates at THIS tip: 2026-08-11, 2026-08-10 x3, 2026-08-09 — a UNIQUE oldest, `phase 85 PLAN done`).

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads `BRAINSTORM — SELF-PICK` — that IS your instruction.** §Project counts SELF-CONTRADICT §Current — do not anchor on them (frozen at phase 76).
2. `docs/envoy-go/ROADMAP.md` — the six family-window paragraphs (check-(2) anchors) + the candidates they name; the row-table schema for registering the new row.
3. `docs/envoy-go/phases/86-validate-sds-nil-provider/PROGRESS.md` — the closed predecessor's findings (the five carried findings above, in full).
4. `docs/envoy-go/DECISIONS.md` — the ADR-0308 tail block (COMPLETE — the parity contract and the roster scoping are its permanent statements).

## What the BRAINSTORM owes
The pick + rejected alternatives WITH re-derived costs (BRAINSTORM-85/86 form) · the new row registered `in-progress` + `want` 118 -> 119 in the SAME commit · probes BY EXECUTION at the tip (name the departure if inline) · the sentinel + all four NCs BOTH SIDES of the registration, recorded · every leak axis invariant by whole-file count · STATE.md rolled IN PLACE (§Recent eviction per DIRECT DATE READ — the unique oldest at this tip is `phase 85 (h2spec-selector-repair) PLAN done`, 2026-08-09) · `next-prompt.txt` rolled to the new phase's SPEC (`git add -f`) · ONE squashed commit, merged to master, pushed.
