START a fresh cold-start session for envoy-go — **the phase-38.2 (`weighted-clusters`) IMPL is DONE, LANDED on master, and PUSHED to `origin/master`; the next lifecycle step is a fresh BRAINSTORM for the next subject** (`superpowers:brainstorming` — the next subject is chosen at the BRAINSTORM per ADR-0106; see "What's next" below). The substantive master tip is the phase-38.2 IMPL squash **`14053d1`** (`phase 38.2 IMPL: weighted-clusters (RouteAction.weighted_clusters routing + WeightedCluster.ClusterWeight.metadata_match) — the project's FIRST per-request cluster-SELECTION primitive`; the feature squash [router_weighted.go + the hcm producer/bridge + ADR-0241 + the `0065` fixture + the doc landing]; pushed to `origin/master`; confirm via `git log --oneline -5`). The prior substantive tips are the phase-38.2 PLAN-done bundle `d3227ba` and the phase-38.1 IMPL squash `4eedf0e` (subset LB). Counts MOVED at the 38.2 IMPL: **fixtures 66 → 67** (`0065-weighted-clusters`), **DECISIONS tail ADR-0240 → ADR-0241** (next-free **ADR-0242**); **stat surface 1125 UNCHANGED** (ZERO new — the FIRST LB-family leg to add NO stats), fuzzers 42 / BackendKind tail 33 UNCHANGED.

## What just landed (phase 38.2 IMPL — the durable record is PROGRESS.md + ADR-0241 + BEHAVIOR_CONTRACT, read those not this summary)

`RouteAction.weighted_clusters` routing — the project's FIRST per-request cluster-SELECTION primitive (the SECOND `RouteAction.ClusterSpecifier` arm) — + `WeightedCluster.ClusterWeight.metadata_match` (merged over the route-level `RouteAction.metadata_match`, entry precedence) composing with the 38.1 subset LB. Built end-to-end across 10 TDD tasks, subagent-driven (two-stage review per task + a final holistic review); the ADR-0052 six-gate GREEN; the full 67-dir differential GREEN. As built:
- **`internal/filter/http/router/router_weighted.go`** — the `weightedSelector` (cumulative weights + a per-action mutex-guarded `newWeightedRNG`, mirroring `cluster.newPCGRNG`) + `pick()` (first cumulative bucket > r; weight-0 never picked) + the `WeightedCluster` entry type + `H1/H2WeightedClusterAction` (pre-build N `*routerAction`/`*routerActionH2`, per-request `sel.pick()` → the EXISTING `doH1/H2ClusterAction` — NO forked dispatch, the randomLB model lifted to the router layer).
- **`internal/filter/hcm/actions.go`** — the `weightedClusterRouteAction` bridge (the `clusterRouteAction` sibling; holds the pre-built `h1`/`h2` closures; satisfies the UNCHANGED `routeAction` interface).
- **`internal/filter/hcm/config.go`** — `mergeRouteSubsetMatch` (route-level ⊕ entry, entry precedence; reuses `ScalarsFromStruct`/`NewSubsetMatch` + the 38.1 scalar reject) + the `buildRouterAction` `RouteAction_WeightedClusters` switch arm (the single-cluster arm preserved BYTE-STABLE) + `buildWeightedRouterAction` (the SIX producer parse-reject arms: empty-clusters/name-required/cluster_header-unsupported/weight-required/sum-0/dangling — envoy-go's OWN parse-reject, no PGV) + `buildWeightedBridge`.
- **ADR-0241** (ACCEPTED in-place per ADR-0044 — the weighted-cluster routing primitive + the `metadata_match` merge; ONE ADR, the ADR-0239 seam REUSED UNCHANGED, NO `loadBalancer.Pick` change, NO exported-cluster change).
- **`test/fixtures/0065-weighted-clusters`** — the cross-side PER-SIDE weight-distribution band (~4.5σ over the per-cluster `upstream_rq_total`; the `0060-lb-random` lineage) + a metadata_match-composition arm (the `c_sub` v2 endpoint starved to 0 — the merged match selects v1 within the chosen cluster) + cross-side conservation; 3 `-count=1` deliberate breaks (swap-weights/drop-cluster/drop-compose); 20/20 flake-free. ZERO new BackendKind/fuzzer/package/go.mod-dep.

## What's next — a fresh BRAINSTORM (`superpowers:brainstorming`)

The lifecycle returns to BRAINSTORM. Per ADR-0106 sibling rows are NOT pre-populated — the next subject is chosen at the BRAINSTORM. The Load-balancing family STAYS OPEN but its **3 remaining candidates {locality-weighted LB, priority load balancing, panic thresholds} are ALL gated by the absent health-checking boundary** — they CANNOT land until the Upstream-robustness family exists. So the next BRAINSTORM most likely opens a NEW family (the user picks). The unstarted families (roughly by size/impact; each MVP-scoped per MISSION §2.1):
1. **xDS / dynamic config** — the single largest gap (the whole project is STATIC bootstrap today: ADS, delta xDS, LDS/CDS/RDS/EDS/SDS/RTDS, reconnection, initial-fetch timeout).
2. **Upstream robustness** — active health checks (HTTP/TCP/gRPC) / outlier detection / circuit breakers / retries + hedging / per-protocol connection pooling — this UNBLOCKS the 3 health-gated LB candidates.
3. **HTTP/3 + QUIC** — quic-go transport, downstream H3 listener, upstream H3 cluster, the `h3spec` gate (only H1/H2 today).
4. **gRPC** — gRPC bridge, gRPC-Web, gRPC-JSON transcoding, interop conformance.
5. **Observability** (beyond the baseline Prometheus + file access log) — gRPC ALS, OTLP access log, OTel/Zipkin/Jaeger/Datadog/XRay tracing, stats sinks, tap filter.
6. **Runtime + hot restart** — the RTDS-driven runtime layer (note `WeightedCluster.runtime_key_prefix` + `random_value_specifier.header_name` were DEFERRED at 38.2 to this family); hot-restart / graceful-drain beyond phase 08's minimum.

The §9 Network-filters family is CLOSED (phases 26–33). The HTTP-filters family (09–25) shipped its curated set but is NOT formally closed. The WASM host family is partially satisfied (proxy-wasm HTTP-filter conformance via phase 25; a broader standalone host sub-project remains on the books).

## Read first (in order)

1. THIS FILE — the post-phase-38.2-IMPL cold-start (next: a fresh BRAINSTORM for the next subject).
2. `docs/envoy-go/STATE.md` — active-phase `phase 38.2 (weighted-clusters) IMPL done`; the counts (fixtures 67 / fuzzers 42 / stat surface 1125 / BackendKind 33 / DECISIONS tail ADR-0241, next-free ADR-0242); the lifecycle routing (→ the next BRAINSTORM).
3. `docs/envoy-go/ROADMAP.md` — row 38 (38.1 + 38.2 both `done`; the flat family row; the Load-balancing family STAYS OPEN — 3 health-gated candidates remain) + the §Feature Families section (the authoritative detail on the unstarted families above).
4. `docs/envoy-go/MISSION.md` (§2.1 the MVP-parity definition) + `docs/envoy-go/phases/38.2-weighted-clusters/` (PROGRESS.md + SPEC.md + PLAN.md + README.md — the durable phase-38.2 record) — for the lifecycle/format precedent the next BRAINSTORM follows.
5. `docs/envoy-go/DECISIONS.md` — the process ADRs the BRAINSTORM honors (ADR-0106 flat family row / ADR-0045 split-gate / ADR-0044 ADR-at-SPEC / ADR-0052 six-gate / ADR-0004 empirical pins / ADR-0227 contrib reference image) + ADR-0241 (the just-landed weighted-cluster primitive).
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_execution_style`, `feedback_git_worktrees`, `feedback_subagents_no_push` + `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_path_targeting`/`_detach`, and the differential references (surface them when the chosen subject reaches its IMPL).

## Honor throughout

- The lifecycle is BRAINSTORM → SPEC → PLAN → IMPL, each its own session. The next session is a fresh **BRAINSTORM** (`superpowers:brainstorming` — pick the next subject [most likely a new family]; the MVP charter; the framework survey; the anticipated ADRs/fixtures; the §empirical-pin questions the SPEC will execute).
- `feedback_git_worktrees` · `feedback_execution_style` (subagent-driven at the IMPL) · `feedback_subagents_no_push` · `feedback_push_to_origin` (the controller squash-merges + pushes at stage-close — the 38.2 IMPL landed as squash `14053d1`) · `feedback_pertask_gofmt_lint`.
- ADR-0004 (the empirical-pin discipline — EXECUTE at the SPEC) · ADR-0044 (ADR §Context at SPEC, bodies at IMPL in-place) · ADR-0045 (the split-gate) · ADR-0052 (the atomic-landing six-gate) · ADR-0080 (byte-stable parse-reject wording) · ADR-0106 (flat family row — NO parent rollup, sibling rows NOT pre-populated) · ADR-0227 (the contrib reference image `envoyproxy/envoy:contrib-v1.37.2`).
