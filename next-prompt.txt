=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-CHECKED 2026-07-13 (after the phase-61.3 PLAN landed, run MECHANICALLY) — it does NOT fire. Do NOT create `stop`.** State:
- (1) prints `NOT DONE: row 61` — ONE `in-progress` row. Row 61 `http3-downstream-listener` is the HTTP/3-family opener; legs 61.1 (QUIC/UDP listen substrate) + 61.2 (H3 codec + HCM adapter) are DONE, and leg 61.3 (cross-side differential + harness UDP/H3-client surgery) is now DECOMPOSED (its PLAN just landed) but NOT yet IMPLEMENTED. Row 61 flips `done` only at the 61.3 IMPL (ADR-0106). This ALONE blocks `stop`.
- (2) prints THREE live "candidates:" sentences — HTTP/3, xDS, and Observability ⇒ three families STAY OPEN. (`reference_sentinel_deferred_sentence_live_vs_historical`.)
- (3) prints `NEVER OPENED: gRPC`, `Runtime`, `WASM` ⇒ THREE families never opened. Operational-tooling also STAYS OPEN.

All three checks print ⇒ the sentinel does NOT fire.

**STATUS AS OF 2026-07-13: the phase-61.3 PLAN LANDED (a DOCS-ONLY stage — authored `PLAN-61.3.md`'s 9-task TDD spine + `PROGRESS-61.3.md` scaffold in a fresh worktree off master; NO production `.go` changes; row 61 STAYS `in-progress`; DECISIONS tail STAYS ADR-0281; ADR-0282 reserved for the 61.3 IMPL).** The NEXT IMPLEMENTABLE STAGE is the **phase-61.3 IMPL** (`http3-downstream-get-differential` leg 61.3 — execute `PLAN-61.3.md` task-by-task, subagent-driven; the FINAL leg — its six-gate FLIPS ROADMAP row 61 → `done`; lifecycle-state 3 for the 61.3 leg — a CODE stage adding the `0104-http3-downstream-get` cross-side fixture + the harness UDP/H3-client surgery + a small stdlib-only writeH3Reply fidelity pickup).

**What the 61.3 PLAN delivered (for CONTEXT; the code lands at the 61.3 IMPL — RE-DERIVE every `test/` `file:line` against the master tip at IMPL start, `git fetch` first):**
- `docs/envoy-go/phases/61-http3-downstream-listener/PLAN-61.3.md` — a 9-task TDD spine: (T2) `test/helpers/h3.go` `H3RoundTrip` (pinned-UDP-dial quic-go `http3.Transport` client — TEST-code quic-go, the production import-hygiene gate stays intact); (T3) harness UDP exposure (`ReferenceProxy.udpAddrs` + transport-aware `/udp` + `ListenerUDPAddr`, the TWO MAPPING starters); (T4) the reference contrib-Envoy H3 container DE-RISK (host→container UDP GET→200, non-vacuous — proven FIRST before the fixture builds on top); (T5) the runner `ReferenceListenerIsUDP` marker + UDP-addr dispatch to `DriveReference`; (T6) the stdlib-only writeH3Reply `server: envoy` + `content-length` fidelity pickup (the 61.2-deferred item, ADR-0281 §Consequences); (T7) the `0104-http3-downstream-get` cross-side fixture (driver + config templates BOTH sides + `AssertStats` named-subset + blank-import); (T8) the full cross-side run GREEN + per-assertion liveness breaks; (T9) BEHAVIOR_CONTRACT + ADR-0282 + **ROADMAP row 61 → `done`** + STATE + six-gate + sentinel re-check + router roll.
- `docs/envoy-go/phases/61-http3-downstream-listener/PROGRESS-61.3.md` — the baselines + task checklist + design pins + the TWO RE-DERIVATION CORRECTIONS + the carried 61.1/61.2 deferred-maintenance dispositions.
- STATE active-phase header flipped to `phase 61.3 PLAN done` (NEXT = the phase-61.3 IMPL); `next-prompt.txt` rolled (THIS file).

**TWO RE-DERIVATION CORRECTIONS the PLAN recorded (LOAD-BEARING for the 61.3 IMPL — `feedback_brief_citations_not_evidence`):**
1. **The fixture is `0104-http3-downstream-get`, NOT the SPEC/router `0102`.** `0102-tracing-custom-tags-literal` (phase 59) + `0103-xds-sds-server-cert` (phase 60.2) consumed 0102/0103 after the SPEC was written; the tail is `0103`, next free is `0104`. Fixtures **105 → 106**. RE-VERIFY at IMPL start (`ls -d test/fixtures/[0-9][0-9][0-9][0-9]* | sort | tail -3`); take the next free if a parallel row raced.
2. **`HTTPExpectations` is TCP-only and UNUSABLE for H3.** The runner enforces it with an internal HTTP/1-over-TCP client (`runner_test.go:1273-1291`), which cannot reach a QUIC/UDP listener. The `0104` driver asserts status in the Drive hooks (`H3RoundTrip` → error on non-200), body via `CompareBytes`, stats via `StatsAsserter.AssertStats` — NOT `HTTPExpectations`, NOT the dead `SubjectAsserter` (`reference_differential_asserter_dispatch`).

**The phase-61.3 IMPL scope (a CODE stage — execute `PLAN-61.3.md`'s 9-task spine subagent-driven; the FINAL leg — FLIPS row 61 `done`):**
- Fresh worktree off master (`.worktrees/phase-61.3-impl`, branch `phase-61-http3-downstream-get-differential-impl`, per `feedback_git_worktrees`); subagent-driven (`feedback_execution_style`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the controller squashes + pushes at stage-close (`feedback_push_to_origin`); per-task gofmt + golangci-lint (`feedback_pertask_gofmt_lint`); worktree hygiene (`feedback_subagent_worktree_detach` / `feedback_subagent_worktree_path_targeting`).
- The ONLY production change is stdlib-only (writeH3Reply `server`/`content-length`, T6 — the production quic-go import-hygiene gate stays intact: `go list -deps ./internal/filter/hcm | grep -i quic-go` prints NOTHING). Everything else is TEST infra (`test/helpers/h3.go`, `test/differential/harness.go`, `test/differential/runner_test.go`, `test/differential/fixture/fixture.go`) + the new fixture dir.
- LOAD-BEARING differential-harness traps: `reference_differential_asserter_dispatch` (cross-side → `AssertStats`, not `SubjectAsserter`), `reference_differential_run_selector` (`-run 'TestDifferential/0104-http3-downstream-get'`), `reference_differential_break_protocol_count1` + `reference_deliberate_break_wrong_assertion` (per-assertion `-count=1` breaks, confirm WHICH fires), `reference_docker_probe_bridge_network` + `reference_host_gateway_ip_docker_desktop` (host↔container UDP; verify decode RAN non-vacuously — the `0104` `direct_response` needs NO backend dial, so the only Docker concern is host→container UDP publishing of the reference QUIC listener), `reference_envoy_contrib_image_tagging` (`contrib-v1.37.2` in the standard `envoyproxy/envoy` repo), `reference_differential_backendcount_min_one` (`BackendCount() 1`), `reference_stats_sink_emits_used_only` (named-stat subset — the shared `downstream_rq_*` counters, NOT the http3-specific counters envoy-go omits), `reference_roadmap_split_phase_row_done` (row 61 flips `done` at THIS final-leg six-gate).
- **ROADMAP row 61 → `done`** at the 61.3 IMPL six-gate (ALL three legs land, ADR-0106); the HTTP/3 deferred sentence STAYS exactly one live "candidates:" match (the family STAYS OPEN, `reference_sentinel_deferred_sentence_live_vs_historical`). ANCHORS **ADR-0282** (or the documented fold into ADR-0281 — the IMPL decides).

**ADR reservation ledger (provisional; each finalized at its IMPL in landing order):** ADR-0277 = phase-59 (LANDED) · ADR-0278 = xDS 60.1 (LANDED) · ADR-0280 = xDS 60.2 (LANDED) · ADR-0279 = HTTP/3 61.1 (LANDED) · ADR-0281 = HTTP/3 61.2 codec-arm (LANDED) · **ADR-0282 = HTTP/3 61.3 harness/differential (provisional 61.3 anchor — finalized at the 61.3 IMPL; or folded into ADR-0281)** · next-free unreserved **ADR-0283**.

**Current counts (VERIFIED at the phase-61.3 PLAN session against master tip `eaef171a` — RE-VERIFY at session start, `git fetch` first):** fixtures **105** (tail `0103-xds-sds-server-cert`; 61.3 IMPL adds `0104-http3-downstream-get` → 106) · fuzzers **55** · DECISIONS tail **ADR-0281** (next-free **ADR-0282**) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** · go.mod modules **2** (`quic-go v0.54.1` direct + `qpack v0.5.1` indirect, both confined to `internal/listener` production deps; `test/helpers/h3.go` will add a TEST-only `http3.Transport` import at the IMPL). Phase-61.3 PLAN was DOCS-ONLY ⇒ ALL counts UNCHANGED at this PLAN. Anticipated at the 61.3 IMPL: fixtures **105 → 106** (`0104`); **ROADMAP row 61 → `done`**; stat surface **1201 (+0 recommended)**; go.mod modules **+0**; new production Go packages **+0**.

**Locating the phase-61.3 PLAN stage squash** (a router cannot name its own sha — identify by SUBJECT, never by position):
```sh
git log --oneline --grep='^phase 61.3 (http3-downstream-get-differential) PLAN' -1
```

**THE NEXT ACTION: the phase-61.3 IMPL (lifecycle-state 3 for the 61.3 leg — execute `PLAN-61.3.md` task-by-task, subagent-driven; the FINAL leg — FLIPS ROADMAP row 61 → `done`).** After the 61.3 IMPL lands, row 61 is `done` and the roller SELF-PICKS the next subject (smallest-defensible among the open/never-opened families — xDS/Observability/HTTP-3 deferred candidates, or the never-opened gRPC/Runtime/WASM families; record the pick + rejected alternatives in that stage's BRAINSTORM). The sentinel governs termination, not this list.

==============================================

## What THIS session does (the phase-61.3 IMPL)

Execute `docs/envoy-go/phases/61-http3-downstream-listener/PLAN-61.3.md` task-by-task (the 9-task TDD spine), subagent-driven (`feedback_execution_style`), in a FRESH worktree off master (`.worktrees/phase-61.3-impl`, branch `phase-61-http3-downstream-get-differential-impl`, per `feedback_git_worktrees`). This is a SINGLE STAGE (the IMPL — a CODE stage). It is the THIRD and FINAL leg of the 61.1/61.2/61.3 split — its six-gate **FLIPS ROADMAP row 61 `in-progress` → `done`** (ALL legs landed, ADR-0106 / `reference_roadmap_split_phase_row_done`). The HTTP/3 + QUIC FAMILY STAYS OPEN after phase-done.

**Deliverables (per PLAN-61.3):**
- `test/helpers/h3.go` — `H3RoundTrip` (T2). `test/differential/harness.go` — UDP exposure (T3). The reference-H3 container de-risk (T4). `test/differential/runner_test.go` + `fixture/fixture.go` — the `ReferenceListenerIsUDP` marker + UDP-addr dispatch (T5). `internal/filter/hcm/h3dispatch.go` — the stdlib-only `server`/`content-length` fidelity pickup (T6). `test/fixtures/0104-http3-downstream-get/` — the cross-side fixture (T7). The full cross-side run + per-assertion liveness breaks (T8).
- Docs (T9): BEHAVIOR_CONTRACT HTTP/3 cross-side-proven; ADR-0282 §Context/§Decision/§Consequences (or the fold into ADR-0281); **ROADMAP row 61 → `done`**; STATE; the six-gate; the mechanical sentinel re-check (it does NOT fire — three families open, three never-opened); `next-prompt.txt` rolled to the roller's next self-pick.
- `next-prompt.txt` — the router roll (folded into the IMPL squash; `reference_next_prompt_tracked_despite_gitignore`).

## Honor throughout (the discipline, unchanged)
- **Lifecycle = one stage per session.** This session: the phase-61.3 IMPL ONLY. After it lands: the roller's self-pick (the sentinel governs).
- **Process anchors:** ADR-0044 (ADR §Decision/§Consequences land at the IMPL) · ADR-0045 (the split escape-valve — CONSUMED for row 61) · ADR-0080 (distinct per-arm reject substrings) · ADR-0106 (row 61 flips `done` only when ALL legs land) · `reference_roadmap_split_phase_row_done`.
- **The differential harness traps (LOAD-BEARING for the 61.3 IMPL):** `reference_differential_fixture_dispatch_constraint` (one fixture dir = ONE runner branch; `0104` is cross-side only), `reference_differential_asserter_dispatch` (cross-side subject assertions in `StatsAsserter.AssertStats`, NOT the dead `SubjectAsserter`), `reference_differential_run_selector` (`-run 'TestDifferential/0104-http3-downstream-get'`), `reference_differential_break_protocol_count1` + `reference_deliberate_break_wrong_assertion` (per-assertion `-count=1` breaks; confirm WHICH fires), `reference_docker_probe_bridge_network` + `reference_host_gateway_ip_docker_desktop` (host↔container UDP; verify decode RAN), `reference_envoy_contrib_image_tagging` (`contrib-v1.37.2`), `reference_differential_backendcount_min_one` (`BackendCount() 1`), `reference_stats_sink_emits_used_only` (named-stat subset), `reference_fatalf_makes_assertions_unreachable` (`Errorf` per property in `AssertStats`).
- **`next-prompt.txt` IS TRACKED** (`reference_next_prompt_tracked_despite_gitignore`) — edit it inside the stage worktree and fold into the stage commit; locate commits by SUBJECT (`git log --grep`), never by position.
- **Import hygiene (production quic-go gate):** `test/helpers/h3.go` MAY import quic-go (TEST code); the production gate `go list -deps ./internal/filter/hcm | grep -i quic-go` MUST still print NOTHING (the T6 writeH3Reply fidelity pickup stays stdlib-only). Re-verify at the six-gate.
- **Deferred maintenance (carried; fold when the relevant file next reopens):** the phase-57 statssink notes F-1/F-2/F-3 (fold into the next `udp.go`/statssink-driver-touching row); the phase-60.2 final-review MINOR roll-up (T2/T5/T6/T7 — cosmetic/coverage, fold when the SDS files next reopen); the phase-61.1 review MINORS M6-1 (`quicAcceptLoop` no backoff), M-FB1 (`default_filter_chain` QUIC decode gap), M-FB2 (`quicTLSConfig`/`quicChain` map-nondeterminism) — RE-DEFERRED to a QUIC-robustness/SNI-multi-chain row (none exercised by the single-chain `0104`); the phase-61.2 review MINORS T5-M1 (runH3 encode-error path counter gap — WriteH2 parity), T5-M2 (POST-body test depth), T5-B1 (`SetDownstreamLocalAddr` nil — quic-go doesn't populate `LocalAddrContextKey`), T7-M1 (`quicChain`/`quicTLSConfig` divergence only under deferred multi-chain SNI). None is a security/resource/crash risk within the explicitly-deferred scope.

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-61.3 IMPL is next; legs 61.1 + 61.2 CLOSED, 61.3 DECOMPOSED).
2. `docs/envoy-go/phases/61-http3-downstream-listener/PLAN-61.3.md` — the 9-task TDD spine (RE-DERIVE every `test/` `file:line` against the master tip at IMPL start, `git fetch` first — the harness citations are load-bearing and the harness shifts across phases).
3. `docs/envoy-go/phases/61-http3-downstream-listener/PROGRESS-61.3.md` — the baselines + design pins + the TWO RE-DERIVATION CORRECTIONS + the carried deferred-maintenance dispositions.
4. `docs/envoy-go/phases/61-http3-downstream-listener/{PLAN-61.2,PROGRESS-61.2}.md` + `docs/envoy-go/DECISIONS.md` (ADR-0281) — the landed 61.2 codec-arm seams (`h3dispatch.go`/`writeH3Reply`/`emitAccessLogH3`) + the ADR-0281 §Consequences "what 61.3 must do" list.
5. `docs/envoy-go/phases/61-http3-downstream-listener/SPEC.md` — §3.5 / §8 (the differential + harness surgery), §7 (the stat surface — named subset). NOTE the SPEC's `0102` fixture name is STALE (→ `0104`) and its "three starters" is refined to TWO mapping starters (PLAN-61.3 corrections).
6. `docs/envoy-go/ROADMAP.md` — row 61 `in-progress` (the 61.3 IMPL flips it `done`); the LIVE HTTP/3 deferred sentence (line ~171). `docs/envoy-go/STATE.md` — the active-phase header (phase 61.3 PLAN done; NEXT = the phase-61.3 IMPL).
7. `test/differential/harness.go` (the two mapping starters `:107`/`:170`, `ReferenceProxy` `:97-101`, `ListenerAddr` `:227`, the ready-sentinel `:69`) + `test/differential/runner_test.go` (the cross-side path `:1140-1320`, the blank-import block `:26-130`) + `test/differential/fixture/fixture.go` (the Driver interface `:15-52`, `StatsAsserter` `:75`) + `test/helpers/h2.go` (`H2RoundTrip` `:33`, the pinned-dial model) + `test/fixtures/0003-http11-routing/driver/driver.go` (the fixture-driver template) + `test/fixtures/0067-health-check-tcp/driver/driver.go` (the `AssertStats`/`scrapeStats` model). RE-DERIVE the current `file:line` (the harness shifts).
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the `reference_*` differential-harness traps (esp. the Docker/bridge/host-gateway/contrib-image trio + the asserter-dispatch/run-selector/break-protocol/backendcount-min-one/stats-sink-emits-used-only set + `reference_roadmap_split_phase_row_done` + `reference_sentinel_deferred_sentence_live_vs_historical` + `reference_next_prompt_tracked_despite_gitignore`).
