START a fresh cold-start session for envoy-go — **the phase-29.1 sub-phase SPEC (`network-filter-mongo-wire-and-requests`) is DONE + spec-document-reviewer APPROVED + squash-merged + pushed**. The active phase is **phase 29.1** (`in-progress`; parent row 29 `in-progress`; 29.2/29.3 `planned`). This session authors the **29.1 PLAN** (`docs/envoy-go/phases/29.1-network-filter-mongo-wire-and-requests/PLAN.md`) — per SKILL_ROUTING state 2 (`superpowers:writing-plans` scoped to PLAN authoring: bite-sized TDD tasks with exact file paths, complete code, run commands, and expected outputs). The PLAN is the **FINAL ADR-0045 gate-check** (the SPEC-level check passed at ~1070–1470 production LoC / ~17 tasks; the pre-authorized split axis if the PLAN trips is 29.1a [SPEC §10 Tasks 1–11] / 29.1b [Tasks 12–17]).

The 29.1 SPEC landed at squash `7574219` (pushed to origin/master). It inherits the parent SPEC §11 pins verbatim and ADDS the **D-P2 live re-probe results (§11.2)** — read both documents; the SPEC's §3/§6/§7/§8 are the PLAN's blueprints. The load-bearing 29.1-SPEC pins:

- **AMEND-C1 (D-P2/D-P3 RESOLVED — the BIG one):** reference Envoy's dynamic mongo stats are FULLY label-hoisted in Prometheus: `mongo.<sp>.cmd.<cmd>.total` → `envoy_mongo_cmd_total{envoy_mongo_cmd="<cmd>",envoy_mongo_prefix="<sp>"}`; `collection.<c>.query.<leaf>` → `envoy_mongo_collection_query_<leaf>{envoy_mongo_collection="<c>",…}`; callsite stats carry THREE labels. Dynamic values NEVER appear in metric names. The `internal/stats/name.go` arm is a **FOUR-RULE tag extractor** (prefix/cmd/collection/callsite — mirroring upstream `well_known_names.cc` `addTokenized` rules, transcribed verbatim in SPEC §11.2). Labels emit in sorted key order; D-S29.1-5 checks whether `prom.go` multi-label rendering needs a ~3-line sort.
- **D-P1 RESOLVED: EAGER roster creation** at config parse (all 23 stats via `NewCounterIfAbsent`/`NewGaugeIfAbsent`); the boot-window departure vs upstream's per-connection creation is a BEHAVIOR_CONTRACT record, unobservable to the differential (post-first-connection assertions).
- **AMEND-C2:** `cx_destroy_*_with_active_rq` increments on the REFERENCE whenever a connection closes with an unanswered OP_QUERY (i.e. every query-bearing `0049` arm — the `TCPSink` backend never replies) → the `0049` assertions are PRESENCE-ONLY for that pair; value parity is 29.2's `0051`.
- **AMEND-C3:** `$comment` callsite queries DOUBLE-COUNT (both `collection.<c>.query.*` AND `collection.<c>.callsite.<cs>.query.*` increment).
- **AMEND-C4:** the `0049` backend is the EXISTING `TCPSink` (BackendKind 28; silent); NO new BackendKind at 29.1. The mongo responder kind (anticipated value 30) is 29.2's.
- **D-P8 RESOLVED:** `0049` arm 2 = a dedicated listener with `commands: ["isMaster"]` proving the remembered-list + `unknown_command` fallback semantics.
- **The OP_REPLY/OP_COMMANDREPLY 29.1 posture:** recognized-not-decoded (valid envelope, frame consumed, no counters); body decode + counters land at 29.2; the `0049` corpus contains no response-direction opcodes.
- **PARSE-REJECT arms (SPEC §6):** `mongo_proxy: stat_prefix is required` (the `0050` fixture arm) + the 3 FaultDelay PGV arms (oneof-required / fixed_delay-gt-0s / denominator) as code + unit tests only (fixture disposition = parent D-P5, resolved at the 29.3 SPEC).

## What this session must do

1. Author `docs/envoy-go/phases/29.1-network-filter-mongo-wire-and-requests/PLAN.md` via `superpowers:writing-plans`: decompose the SPEC §10 task spine (17 tasks) into bite-sized TDD steps (write-failing-test → run → implement → run → commit per task), with complete code blocks (the SPEC's §3.2/§3.7/§7.4 anticipated shapes are starting points), exact file paths, and per-task `gofmt -l` + `golangci-lint` steps (`feedback_pertask_gofmt_lint`).
2. Re-check the ADR-0045 gate against the final task count/LoC; if it trips, split per the pre-authorized 29.1a/29.1b axis (SPEC §10.1) — update ROADMAP/STATE accordingly and stop.
3. Run the plan-document-reviewer loop until APPROVED; fold advisories in.
4. STATE.md advance (active-phase → `phase 29.1 PLAN done`; next-skill → `superpowers:subagent-driven-development` for the 29.1 IMPL per `feedback_execution_style`); rewrite this next-prompt.txt for the 29.1-IMPL cold-start; commit + squash-merge + push (work in a worktree per `feedback_git_worktrees`; push per `feedback_push_to_origin`).

## Project state at master tip

- **Master tip:** the docs-only SHA-fill/repoint commits trailing the 29.1-SPEC squash `7574219` (confirm the live tip via `git log --oneline -1`). STATE.md `last-commit` points at the substantive squash. origin/master in sync after push.
- **§9 Network-filters family:** rows 26/27/28 DONE; row 29 `in-progress` (29.1 `in-progress` at the SPEC commit; 29.2/29.3 `planned`).
- **Counts at master tip (docs-only commits since the 28.2 IMPL):** stat surface **337** (→ 360 at 29.1 IMPL); differential fixtures **50** (tail `0048-zookeeper-responses`; → 52 at 29.1 IMPL); fuzzers **38** (→ 39 at 29.1 IMPL); conformance h2spec 53/53 + proxy-wasm 10/10 (as of the 28.2 six-gate); DECISIONS.md tail **ADR-0226** (next-free **ADR-0227**; the ADR-0224 §Decision/§Consequences body lands at the 29.1 IMPL — no new number).
- **Toolchain:** Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); reference Envoy v1.37.2 (ADR-0008); go-control-plane v1.32.4 (ADR-0008).
- **Known coverage boundaries (carried, non-blocking):** D-26.3-4 (rbac SNI/mTLS L4 arms); `tcp_proxy` `downstream_cx_*` unmirrored; histograms deferred project-wide (ADR-0060); zookeeper dynamic-metadata deferral + shallow-decode leniency + terminal-originated-writes-only + write-only-filter boot boundary; post-handoff observational boundaries (28.1b §3.5 — lifted for halt purposes only at 29.3); mongo runtime-key gating unmirrored (29.3 record); the mongo access log not differentially compared (AMEND-B10; 29.3 record).

## Read first (in order)

1. THIS FILE — the 29.1-PLAN cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 29.1 SPEC done`; next-skill `superpowers:writing-plans` (29.1 PLAN); next-free `ADR-0227`.
3. `docs/envoy-go/phases/29.1-network-filter-mongo-wire-and-requests/SPEC.md` — **THE document this PLAN decomposes** (esp. §3 package design, §6 reject arms, §7.4 the four-rule name.go arm, §8 fixture arms, §10 task spine, §11.2 the D-P2 probe data, §12.2 the D-S29.1-* open questions the PLAN/IMPL resolves).
4. `docs/envoy-go/phases/29-network-filter-mongo-proxy/SPEC.md` — the parent SPEC (§11.4 wire/BSON layouts the decoder mirrors verbatim; §5 proto roster; §7 stat surface).
5. `docs/envoy-go/phases/28.1-network-filter-write-seam-and-zookeeper-requests/PLAN.md` (3092 lines — the closest first-sub-phase network-filter PLAN precedent for task/step granularity; the 28.1b [1379-line] + 28.2 [2107-line] PLANs are also on disk).
6. `docs/envoy-go/SKILL_ROUTING.md` + `BOOTSTRAP_PROMPT.md` §5/§6 — the lifecycle + split policy.
7. `internal/filter/network/zookeeperproxy/` — the consumer-#1 package the PLAN's code blocks mirror (config.go PARSE-REJECT constants :148-155; stats.go rosterSuffixes :159-175; decoder.go chainConsumed :40-48; zookeeperproxy.go factory shape) + `internal/stats/name.go` (the `.rbac.` tag-extractor arm :226-241 — the label-promotion shape the mongo arm generalizes) + `internal/stats/prom.go` (multi-label rendering — D-S29.1-5).
8. `test/fixtures/0046-zookeeper-requests/` + `0047-zookeeper-boot-reject/` + `0048-zookeeper-responses/` — the fixture driver precedents (875/202/865 LoC); `test/differential/fixture/fixture.go` (TCPSink=28; StatsAsserter :75-77; MultiListenerDriver :599-616).
9. `docs/envoy-go/DECISIONS.md` ADR-0224 §Context (:14421 — the 29.1 charter) + ADR-0218 (tag-extractor precedent) + ADR-0044/0045/0052/0080 (doc/split/bundle/reject discipline).
10. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — execution-style + worktree + push + subagents-no-push + per-task gofmt/lint + the network-filter constraints (typeurl-extensions, onnewconnection-halts, terminal-handoff-ends-ondata) + the differential dispatch/asserter/break-protocol (`-count=1`) + wire-format-both-sides constraints all carry forward. (The new `docker-probe-bridge-network` memory — added at the 29.1 SPEC — only matters for the 29.2/29.3 SPEC live probes, NOT this PLAN session, which does no probing.)
