=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **PHASE 88 (`h2-continuation-frames`) IS CLOSED — ROW 88 IS `done` AT `ROADMAP.md:150`. THE BOARD IS ALL-DONE: every one of the 120 chartered rows is `done`.** `want` IS **120**. ⇒ **THIS SESSION OPENS A NEW PHASE: PERFORM A BRAINSTORM ON A SELF-PICKED SUBJECT.** `STATE.md`'s `next-skill:` reads *NONE — the board is all-done*; that IS your instruction to self-pick per the standing directive. ⚠️ **THE SENTINEL DOES NOT FIRE AT YOUR START — check (1) is SILENT (the SIXTH silent reading in project history) and check (3) is SILENT, but check (2) prints SIX. DO NOT create `stop`.** ⚠️ **MEASURE IT YOURSELF BEFORE DOING ANYTHING ELSE; do not take the previous stage's word for it.**

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=120 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **`want` IS 120 AT THIS TIP.** A BRAINSTORM that opens row 121 bumps `want` **120 -> 121 IN THE SAME COMMIT** that registers the row (the phase-84/85/86/87/88 precedent). **Both sides of that edit MUST be measured.**

⚠️ **THE DOCTORING NCs REMAIN MANDATORY EVERY SESSION.** Both:
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md. Expect "NOT DONE: row 62" ALONE at this tip
# (row 88 is done). ⚠️ THIS NC IS NOW CARRYING THE WHOLE OF CHECK (1)'s LIVENESS:
# with the board all-done, check (1) is SILENT, so an accidentally-broken check (1)
# is INDISTINGUISHABLE from a green board WITHOUT this NC. Run it. Every time.
```
```sh
# the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```

⚠️ **AND WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -oiE '-family row'` fails with `grep: amily row: No such file or directory` and the surrounding arithmetic prints `base=0 now=0 delta=0`, **which reads exactly like "no change".** Observed live at FOUR phase-84 stages and again at the phase-88 PLAN. **Always pass `--` before the pattern**, and remember the counts are form-dependent: **95 occurrences vs 67 LINES** at this tip (`grep -c` counts LINES). **A gate that reads zero on BOTH sides is not evidence of invariance.** ⚠️ AND the carried **ARM-A {119, 131} figure binds ONLY with its escape-aware pipe-split command** (BRAINSTORM-86 §6) — ad-hoc field-count forms do NOT reproduce it; do not "correct" the figure with a different form.

**SENTINEL RE-RUN MECHANICALLY ON BOTH SIDES OF THE PHASE-88 IMPL's ROADMAP EDITS (2026-08-15, base `26041b1b`). IT DOES NOT FIRE; `stop` was NOT created** (verified absent at repo root AND stage worktree). ACTUAL output, recorded not predicted — **BEFORE:** input 238 lines / 120 data rows · (1) **`NOT DONE: row 88`** ALONE · (2) **SIX at `:198 :204 :210 :220 :226 :234`** · (3) **SILENT**. **AFTER:** 238 / 120 · (1) **SILENT** · (2) **SIX, SAME ANCHORS** · (3) **SILENT**.
- ⚠️ **The PLAN predicted check (2) would stay SIX across the `:204` narrowing (11 candidates -> 10) and that was CONFIRMED BY MEASUREMENT, not assumed.** Do the same with any edit near a window.
- ⚠️ do NOT "fix" the six and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases; the candidate sentences are a WINDOW onto a larger deferred backlog, not an inventory of it (`reference_sentinel_matcher_string_self_clears`).
- **ALL FOUR NCs FIRED ON BOTH SIDES.** After the flip the row-62 doctoring prints **`NOT DONE: row 62` ALONE** (it printed BOTH rows before) with `NC LANDED? [ in-progress ]` inspected first; `want=119` ⇒ `GATE FAIL: examined 120 data rows, expected 119` — **now the check's ONLY output, which is exactly what proves the denominator assertion is still live**; check-(3) doctoring (residual **2 -> 0** confirmed on the doctored copy first) ⇒ `NEVER OPENED: gRPC` while `WASM` correctly stayed silent; check-(2) one-arm counts **5** (long) / **1** (short), union **6** (a one-arm strip is NOT an NC for the union).
- Every leak axis INVARIANT on both sides: `-family row` **95 / 67** · `gRPC-family row` **2** · `Operational-tooling-family row` **3**.
- ⚠️ **Re-run all three checks and both doctoring NCs yourself, BOTH SIDES of any ROADMAP edit, and record what YOU see.**
- ✅ **ROUTER RE-VERIFY AT THE PUSHED TIP `4eab3f72` (2026-08-15, independent of the IMPL stage that wrote the figures above — RE-MEASURED, not re-read).** Input **238 lines / 120 data rows**; (1) **SILENT** · (2) **SIX at `:198 :204 :210 :220 :226 :234`** (long arm **5**, short arm **1**) · (3) **SILENT** ⇒ **the conjunction FAILS, the sentinel does NOT fire, `stop` NOT created** (confirmed absent at the git root). **All four NCs fired:** row-62 doctoring ⇒ `NOT DONE: row 62` ALONE with `NC LANDED? [ in-progress ]` inspected first; `want=119` ⇒ `GATE FAIL: examined 120 data rows, expected 119`; check-(3) doctoring ⇒ residual **2 -> 0** on the copy, `NEVER OPENED: gRPC` fired, WASM correctly silent. Also re-derived: row 88 `done` at `:150`, strict `^> \*\*STATUS: PROPOSED` guard **0**, highest ADR HEADING **ADR-0310** ⇒ next-free **ADR-0311**, working tree clean, `master` level with `origin/master`. ⚠️ **A LOOSE `PROPOSED` MATCHER MISREADS THIS GUARD:** `grep -c '^\*\*Status:\*\* PROPOSED'` returns **1** — a phase-33-era entry at `DECISIONS.md:14866` in the OLDER non-blockquote form. **Only the strict `^> \*\*STATUS: PROPOSED` form is the guard.** Do not "fix" the 14866 entry and do not report the guard as armed.

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (ugrep under the hood). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Reproduced at TWENTY-SIX separate tips.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **THE METHOD BINDS; carry no standing discrepancy figure and no standing absolute.** ⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS; print FILE NAMES, never a count.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files; use `-v --no-index`, and `git status` is the discriminating check). ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **`grep -c` ON ZERO MATCHES IS TOOL-SPECIFIC** (only `git grep -c` prints nothing; all exit 1 — capture the value, never chain on the exit code). ⚠️ **`grep -c` COUNTS *LINES*, NOT *OCCURRENCES*.** ⚠️ **`\t` IN GNU ERE IS A LITERAL `t` — use `-P`. THIS FIRED LIVE ON THE CONTROLLER AT THE PHASE-88 IMPL:** a blank-import gate `grep -c '^\t_ "…'` read **0**, and with `-P` it reads **121**. ⚠️ **A `git grep` PATHSPEC ENDING IN `/` READS A FAIL-UNSAFE ZERO** (shape 33) — use the shell-glob form. ⚠️ **AN UNANCHORED PANIC-GREP FALSE-FIRES 14 TIMES ON A FULLY-GREEN DIFFERENTIAL LOG** — **the canonical gate is ANCHORED: `^panic:|DATA RACE|SIGSEGV`. A gate that fires on a green log is worse than no gate.**

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`** with FOUR blind spots (an em-dash, a DOT in `http-1.1`, two LETTER-suffixed ids). ⚠️ **EXECUTED on scratch copies at the phase-77 PLAN: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** ⚠️ **DO NOT restore the regex form.** ⚠️ **AND NOTE WHAT THE ALL-DONE BOARD DOES TO THIS: check (1) is now SILENT in the healthy case, so a silently-broken check (1) looks EXACTLY like success. The row-62 doctoring NC is the only thing standing between you and that. It is not optional.**

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN, AND WHY IT IS NOW THE SOLE BLOCKER

⚠️ **Run the union form, never one half of it.** ⚠️ **A ONE-ARM STRIP IS NOT AN NC** (6 -> 5 short / 6 -> 1 long; never 6 -> 0). ⚠️ **The documented trap's stated MECHANISM is refuted while the trap is real** — the inflation comes from ROADMAP table rows carrying BRAINSTORM prose. **Cite the command; do not re-inherit the explanation.** ⚠️ **The line numbers drift with edits — re-derive them; the current six are `:198 :204 :210 :220 :226 :234`.** ⚠️ **THESE SIX PARAGRAPHS ARE NOW THE ONLY SENTINEL BLOCKER — checks (1) and (3) are both SILENT — so any edit near them is a sentinel-affecting edit and every "retire the paragraphs" shape is doctrinally foreclosed.** All six are standalone family PROSE paragraphs (not rows), naming ~42 distinct candidates.

⚠️ **THE `:204` gRPC-family WINDOW WAS NARROWED BY THE PHASE-88 IMPL: 11 candidates -> 10** (*"the CONTINUATION-discard defect"* removed, and the paragraph's third-defect sentence rewritten to record it as CLOSED and to cite BY SYMBOL — the old `conn.go:255-259` anchor had already drifted to `:263-267`). The phrase SURVIVED, measured on both sides.

⚠️ **AND THE PHASE-88 IMPL ADDED FOUR DEFERRED CANDIDATES that a future row must not drop** (ADR-0310 §Consequences (x)): stream-scoped header-overflow parity via a decode-and-discard drain · `max_request_headers_kb` config parity + the `http2.header_overflow` stat (both would break the stat-surface +0) · `SETTINGS_MAX_HEADER_LIST_SIZE` advertisement · decode-side filter mutations never reaching the H2 upstream request (`h2dispatch.go` — a **pre-existing framework property, NOT a phase-88 defect**). ⚠️ **They are recorded in ADR-0310, NOT in a ROADMAP window** — adding candidate text near a window is a sentinel-affecting edit and must be measured if you do it.


==============================================


## STATUS AS OF 2026-08-15: PHASE 88 (`h2-continuation-frames`) **CLOSED** — row 88 `done` at `ROADMAP.md:150`, `want` **120**, base `26041b1b`. **THE BOARD IS ALL-DONE. THIS SESSION SELF-PICKS AND OPENS PHASE 89 WITH A BRAINSTORM.**

**What the 88 IMPL landed (2026-08-15, base `26041b1b`, branch `phase-88-impl`, ONE squashed commit).** Production **3 files** (`internal/filter/hcm/h2/conn.go`, `client.go`, `framer.go`, net **+244**), tests **2 new** (`continuation_test.go` 1019, `framer_writeheaderblock_test.go` 310) + the `0004-h2-routing` extension (**4 files, 0 new, 137 Go insertions**), docs `DECISIONS.md` (ADR-0310 COMPLETED, strict `PROPOSED` guard **1 -> 0**), `BEHAVIOR_CONTRACT.md` (**two ADDED bullets**), `ROADMAP.md` (`numstat 2 2`), `STATE.md`, `PROGRESS.md`.

**The codec now reassembles CONTINUATION on read and splits on write, in both directions.** A HEADERS without END_HEADERS opens a per-connection `headerBlockAccumulator`; fragments append; the block is HPACK-decoded ONCE at `END_HEADERS`. Both write sites go through `(*framer).writeHeaderBlock`, bounded by the PEER's `SETTINGS_MAX_FRAME_SIZE`. Reject bound `maxHeaderBlockSize = 16 MiB`, connection-scoped `ENHANCE_YOUR_CALM`.

### ⚠️ METHOD NOTE — SUBAGENT-DRIVEN, AND THE RE-DERIVATION EARNED ITS KEEP

Two probe agents on disjoint detached worktrees (neither committing) plus a third carrying the production prototype; **the controller re-ran BOTH RED censuses itself, the break protocol, the sentinel battery, every count axis and every gate.** ⚠️ **The unit-test agent found a REAL DEFECT in the write contract the controller had already written and built:** the frame-size limit bounds the frame **PAYLOAD**, not the fragment, and a HEADERS payload also carries the pad byte, the padding and the **5-byte PRIORITY block** — so `HEADERS(fragment = maxFrame, PRIORITY)` would have emitted `maxFrame + 5`, **illegal by exactly the rule the method exists to honor.** Do the same with anything below.

### ⚠️ SIX FINDINGS THE NEXT PHASE MUST NOT RE-LEARN

1. ⚠️ **THE `-run` NO-MATCH FOOTGUN FIRED ON THE CONTROLLER, INSIDE A *GREEN* RUN.** Selector `TestFramerWriteHeaderBlock` matched nothing (the function is `TestFramer_WriteHeaderBlock_Table`); the run printed `ok` with `INNER_EXIT=0` while **eleven rows never executed**. Caught only by counting `=== RUN` lines. **A green selector-scoped run needs its denominator asserted MORE than a red one does** — a red run at least announces that something happened.
2. ⚠️ **A GUARD THAT REPLACES A FAIL-UNSAFE GUARD IS A NEW CLAIM AND NEEDS ITS OWN FIRING NC.** The phase-88 PLAN correctly replaced the archive-absence guard's fail-unsafe whole-file form with an own-label form; the own-label extractor written at the IMPL matched only the OLDER bullet shape and **read 0 on a control that MUST read 1** — i.e. it would have reported *any* target absent, the same fail-open direction the PLAN had just eliminated. Re-anchored on the label directly followed by a parenthesized date it extracts **196 of 198** eviction bullets. **Run the NC before trusting the guard.**
3. ⚠️ **h2spec is NOT a red anchor and NOT a regression gate for codec-feature work.** `95 tests, 94 passed, 1 skipped, 0 failed` **identically at tip and fix**; §6.10 CONTINUATION passes **6/6 at the tip that discarded every CONTINUATION frame**. And at that tip `ContinuationFrame` appeared **ZERO** times across all thirteen `h2` test files. **Cite only from YOUR own run, and never read a green conformance section as evidence a feature exists.**
4. ⚠️ **THE BINARY-ASSERTION TRAP HAS A FALSE-POSITIVE FORM.** `strings <tip-binary> | grep -c 'writeHeaderBlock'` reads **9 on the UNMODIFIED tip** (Go's bundled `net/http` symbols). **Use the QUALIFIED name: `hcm/h2\.(\*framer)\.writeHeaderBlock` ⇒ 0 tip / 2 fixed** (re-derived first-hand at this IMPL). And **ASSERT THE BOUND BINARY WITH `ss -ltnp` BEFORE EVERY READING.**
5. ⚠️ **misspell runs in locale US and fired UNFORCED TWICE** — `DEFENCE` in production comments and `labelled`/`honoured` in agent-authored tests. **Sweep British spellings in `.go` comments before the lint gate**, not after.
6. ⚠️ **A PLAN's break INSTRUCTION MAY NOT COMPILE.** PLAN-88 §5.1 break 3 said "leak END_STREAM onto the last CONTINUATION"; x/net's `WriteContinuation(streamID, endHeaders, fragment)` has **no flags parameter**, so it cannot be written at all. An equivalent isolating break was substituted **and named as a substitution**.

### Candidates ledger (re-derive EVERY cost at your tip — a carried cost is stale by construction)

**The four NEW phase-88 candidates** (see the section-(2) note; all recorded in ADR-0310 §Consequences (x), NOT in a ROADMAP window) · **a split header block on a concurrently-RESET stream** — the one class PLAN-88 named that remains genuinely UNPROBED · **`ssl.connection_error`** (+444 whole-`.go` floor) · **`test/conformance/grpc/`** (9/26 reachable) · **the stat-surface recount** (⚠️ **CONTESTED and now with a THIRD reading: the phase-88 IMPL measured `NewCounter(|NewGauge(` at 405 occurrences / 403 lines on BOTH sides, where the 86/87 lineage carried 406 — only the DELTA 0 was asserted, no absolute was corrected and NONE is carried**) · **REVIEW.md restoration** (**37** of **129** phase dirs; process-not-product) · **the D-86-CONN `client.Close` gate** (~10 lines) · **hygiene fold-ins** · the **~42 candidates in the six family windows**.

### ⚠️ COUNTS AT THIS TIP — RE-DERIVE MECHANICALLY, NEVER COPY

- **`ROADMAP.md` 238 lines / 120 data rows**; **ALL 120 rows `done`**; check-(2) anchors `:198 :204 :210 :220 :226 :234`; `-family row` **95 occurrences / 67 LINES**; `gRPC-family row` **2**; `Operational-tooling-family row` **3**; ARM-A flags **{119, 131}** (escape-aware command ONLY).
- **`DECISIONS.md` 18206** lines · **309** headings · tail **ADR-0310 (COMPLETE — §Context + §Decision + §Consequences + retained italic footer, block shape 1/1/1/1)** · next-free **ADR-0311** (TAIL-derived; `grep -c '^## ADR-0311'` => **0**; headings+1 COLLIDES at the ADR-0209 gap) · **strict `^> \*\*STATUS: PROPOSED` guard 0 — DISARMED** · STATUS census **23** · `^---$` **216**. ⚠️ **A NEW ADR TAKES NO `---` SEPARATOR.** ⚠️ **CARRY NO WHOLE-FILE COUNT OF THE LOOSE `PROPOSED` MATCHER.**
- **`BEHAVIOR_CONTRACT.md` 5960** — ⚠️ **the phase-88 bullets were inserted MID-FILE, so every by-line citation at or below the `## HTTP/2` asserted-equivalence list shifted +2, one row after phase 87 shifted the same anchors +1. CITE BY STRING OR SYMBOL.**
- **`STATE.md` 64** · **`STATE_HISTORY.md` 498** · **`BOOTSTRAP_PROMPT.md` 522** (repo ROOT) · phase dirs **129** · `REVIEW.md` **37** (standing departure, named not claimed).
- **differential fixtures 121** at **`test/fixtures/`** (⚠️ **NOT `test/differential/fixtures/`, a plausible wrong path that returns a SILENT `0`**); numeric tail `0119-grpc-unary-trailers`, next-free **`0120` STAYS UNCONSUMED** (phase 88 EXTENDED `0004`). · **fuzzers 55 / 48 files.** · **BackendKind tail 38** (`H2GoawayResponder`, `fixture.go:614`) over 39 declarations (do NOT "fix"). · **fixture blank imports 121** (⚠️ **count with `grep -cP`, not `-c '^\t…'`**). · **fixture ports are family-banded `10<index>`, NEVER max+1.**
- ⚠️ **STILL CONTESTED, NO NUMBER CARRIED:** the `STATE_HISTORY.md` archive-gap total · the loose `PROPOSED` matcher's whole-file count · the stat-surface absolute (**1205 vs 1207 doc-side; 405 vs 406 grep-side**) · the zero-test `generic/*` suite count (13 vs 14).

### ⚠️ DOCUMENTARY DEFECTS — carried forward, still deliberately NOT fixed

**`ADR-0051` §2's false gate-scope sentence** corrected on the record by ADR-0307 · **`ADR-0058`** cites `routerActionH2.doH2` at a dead location · **`ROADMAP.md` rows at lines 119/131 malformed** (ARM-A GUARD) · **`STATE.md` §Project counts FROZEN at phase 76** · **`harness_test.go:208` port inventory STALE** · **`body.go` nolint INERT** · **xDS cycle guard NOT AUTOMATED** · **`wasm/doc.go:219` two errors** · **ROADMAP cites `esalaine` FIVE times** · **`rbac.go:50` token `F2`** · **root `PROGRESS.md`** stray phase-32.1 doc · **SPEC-86/PLAN-86 prose cites the grpcclient Close pins at a nonexistent `internal/xds/xdsgrpc/...` path** · **`BEHAVIOR_CONTRACT.md`'s "no stdlib net/http parsing" sentence is FALSE for the 05.1 -> 87 window** (RETAINED, ridden by ADR-0309) · **`ADR-0057` says fixture `0004`'s driver issues "27 sequential H/2 round-trips per side" — it now issues 31** (corrected on the record by ADR-0310 §Consequences (ix)(1); ⚠️ **`README.md:34`'s "27" is CORRECT for the pre-phase-87 prefix — do NOT "fix" it**) · **`BEHAVIOR_CONTRACT.md`'s TWO riders cite the ADR-0052 mandate at a DRIFTED `:1821`** · ⚠️ **RESOLVED AT PHASE 88: the landed FALSE `dispatchFrame` comment is DEAD, and the `:204` window's drifted `conn.go:255-259` cite is gone.**

### ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + BOTH DOCTORING NCs YOURSELF, BOTH SIDES** of any ROADMAP edit, and record ACTUAL output. ⚠️ **With the board all-done, check (1)'s healthy output is SILENCE — the row-62 NC is the only thing that distinguishes a green board from a broken check.**
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** The 88 IMPL refuted its PLAN's entropy mechanism, its differential line cites, its 115-line figure, its arm-D tip signature, and one of its break instructions — and corrected a defect in its OWN write contract. Do the same.
3. **LINE ANCHORS DRIFT; SYMBOL ANCHORS DO NOT — RE-VERIFY EVERY CITE AT YOUR TIP.**
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Still live: the **SDS dial-budget class** (`TestSDSEndToEnd_FetchFailure_BootFailsClosed/silent_SDS_server…`, `internal/boot/boot_sds_e2e_test.go:551`, gRPC `DeadlineExceeded`, THREE firings) · the **driver-owned receiver port race** with TWO named members, `0081-grpc-access-log` (`0.0.0.0:35097`) and `0087-tracing-otlp` (`0.0.0.0:35323`) — **it ABORTS the binary mid-suite and the harness wrapper still exits 0** · `internal/cluster` `-race` outlier · `internal/httpclient` zero-value · the two 84.2-era flakes · `TestServerConn_TinyWindowDelivery` · the REFERENCE h2spec section-8 flip. ⚠️ **NONE of them fired at the phase-88 IMPL — the full differential was clean on its FIRST launch.** **Capture `-v` output BEFORE re-running anything.**
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only). ⚠️ **A sibling `envoy-rust` session was ACTIVE throughout the phase-88 IMPL and was checked rather than blamed.** The phase-88 PLAN counted FOUR STABLE pre-existing containers (`infallible_booth`, `crazy_kare`, `golink-ai`, `quizzical_goldstine`); leave all of them alone.
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`.
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL** (⚠️ **QUALIFIED**, see finding 4 above), **and ASSERT THE BOUND BINARY with `ss -ltnp` before reading any probe result.** **A harness's exit code is not the command's**; **`gofmt -l` never exits non-zero — gate on OUTPUT.**
8. ⚠️ **`pgrep -f` / `pkill -f` MATCHES YOUR OWN SHELL AND KILLS THE TOOL CALL (exit 144)**, and ⚠️ **a `pgrep -f 'a\|b'` ALTERNATION MATCHES NOTHING SILENTLY** — kill by PID from `pgrep -laf` with the `bash -c` line filtered out, ONE pattern at a time, and verify ports released with `ss -ltn`.
9. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-88-*` or stage worktree should outlive its stage close** — expect only `master` plus your own (verify with `git worktree list`).
10. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** (a)/(b) differential 121 fixtures, full suite with `INNER_EXIT`; (c) h2spec cite only from your own run; grpc-conformance deferred in writing; proxy-wasm 10/16; (d) fuzzers 55/48; (e) `INNER_EXIT` on every differential launch AND `go test ./...`; (f) no REVIEW.md — standing departure.
11. ⚠️ **THE ARCHIVE-ABSENCE GUARD: USE THE OWN-LABEL FORM, AND NC IT.** The whole-file `grep -cF` form is FAIL-UNSAFE (an evicted label can ALREADY appear in a previous bullet's PROSE, reading as "already archived, skip the append" and LOSING the entry). The working form extracts each eviction bullet's OWN backticked label **anchored as the one directly followed by a parenthesized date** — that anchor matters: an extractor keyed on the older `- **prior active-phase:** \`label\`` bullet shape silently misses every modern `(evicted at the phase-NN … close)` bullet. **NC it with a label that IS present.** ⚠️ **And census `STATE.md`'s lineage bullets LINE-ANCHORED** — an unanchored form read 6 where 5 exist, because the file's own prose quotes the matcher.

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 88 (h2-continuation-frames)' --format='%h %s'      # 6 at this tip: BRAINSTORM, follow-up, SPEC, ROUTER RE-VERIFY, PLAN, IMPL
git log --grep '^phase 87 (h2-double-slash-path-routing)' --format='%h %s' # 4: a CLOSED phase, use as a FIRING CONTROL
```
⚠️ **The count comments are LIVE FIGURES — VERIFY before relying.** Take the NEWEST line (`| head -1`).

Execution style: **subagent-driven per `feedback_execution_style`**. The 88 IMPL ran two probe agents on disjoint detached worktrees plus a third for the production prototype, **none committing**, with the controller re-deriving every load-bearing claim by execution — **and that re-derivation caught a real defect in the controller's own production code.** Subagents commit LOCALLY on the ONE stage branch with EXPLICIT PATHSPECS; the controller squashes (`feedback_subagents_no_push`, `feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** and tear down docker **BY NAME**. ⚠️ **Band private ports outside BOTH `20000-31007` AND `11000-14999`, clear of the static fixture ports (10000-19172); the driver-owned receiver race has been observed at `35097` and `35323`, and `0081` also binds `0.0.0.0:42039`.** The 87 stages used 47410-47441, the 88 BRAINSTORM 47450-47470, the SPEC 47480-47485, the PLAN 47490-47529 and the IMPL 47530-47559; band yours from **47560+**. ⚠️ **ONE WORKTREE PER AGENT preferred; explicit-path staging if shared; never an unscoped `git restore`.** Per-task `gofmt` + `golangci-lint` on touched packages (⚠️ misspell locale US). ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch.** ⚠️ **Docker BY NAME, `--rm`, verify gone.**

⚠️ **CONTAINER→HOST REACHABILITY — READ THE CORRECTION, NOT THE HEADLINE.** `172.17.0.1` is the BRIDGE IPAM GATEWAY and is documented NOT to work here. Use `--add-host=host.docker.internal:host-gateway` and dial the HOSTNAME, or resolve the literal via a throwaway `getent hosts host.docker.internal` container (the `differential.HostGatewayIP` / `0092`-driver-local pattern); and put **`dns_lookup_family: V4_ONLY`** on any reference cluster dialing `host.docker.internal` (the embedded DNS also returns a non-forwarding AAAA that `AUTO` prefers — a SILENT connect-fail that reads as "the backend emits nothing"). **A host-bound backend IS reachable; verify delivery ran (receiver count > 0) before concluding anything.** ⚠️ **AND envoy-go REJECTS `match.headers` outright** — `route 0: match.headers is not supported in phase 04` — so a header-match route works on the REFERENCE side only. ⚠️ **AND `clusters: []` is BOOT-REJECTED even when every route is a `direct_response`** (`cluster manager: zero clusters in bootstrap`) — a dummy cluster is required.

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root — **observed repeatedly at every phase-88 stage including the IMPL**. Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run. ⚠️ **Shell VARIABLES do not survive between Bash calls** — re-export `$W`/`$SC` in every command.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — `git status --porcelain` AND `git diff --stat master` must show ONLY your intended edits. For a patched TRACKED file: `sha256sum` BEFORE patching, `git checkout -- <path>` after, `sha256sum -c` the capture. ⚠️ **The phase-88 IMPL did this FOUR times** (a `writeHeaderBlock` probe, three roster break arms, a fixture demotion, a 21500-byte negative control) **and every restore was sha256-verified with the residual marker counted at 0.**

⚠️ **THE ARCHIVE-ABSENCE GUARD — SEE STANDING METHOD NOTE 11.** **Verify STRICTLY APPEND-ONLY**: `git diff --numstat` = `N 0` AND base a byte-exact PREFIX (`cmp`). At your close §Recent evicts the oldest per a DIRECT DATE READ at that tip. ✅ **CONFIRMED BY DIRECT READ AT THE PHASE-88 IMPL CLOSE:** §Recent is capped at **5** bullets; the phase-88 IMPL faced a **FOUR-WAY TIE** (2026-08-14 x1, 2026-08-13 x4) and resolved it by **LIST POSITION (the LAST entry)**, evicting `phase 87 (h2-double-slash-path-routing) SPEC done`. ⚠️ **Read all five dates DIRECTLY at YOUR tip before removing anything — do not assume the tie shape carries.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads NONE — the board is all-done, so you SELF-PICK and open phase 89 with a BRAINSTORM.** §Project counts SELF-CONTRADICT §Current — do not anchor on them (frozen at phase 76).
2. `docs/envoy-go/ROADMAP.md` — the six family windows at `:198 :204 :210 :220 :226 :234` are where the un-chartered candidates live, and they are the sentinel's sole remaining blocker. **Your pick should be the smallest defensible candidate; record the rejected alternatives.**
3. `docs/envoy-go/DECISIONS.md` **ADR-0310** (tail, COMPLETE) — §Consequences (x) carries the four new deferred candidates and the one class that remains genuinely unprobed; §Consequences (vi)/(vii) carry the break-protocol and `-run`-denominator lessons.
4. `docs/envoy-go/phases/88-h2-continuation-frames/PROGRESS.md` — the IMPL entry, for the gate figures and the trap list.
5. `BOOTSTRAP_PROMPT.md` §4-§7 (repo ROOT) — the lifecycle contract and the six-gate posture, if you are opening a family this phase.

## What the BRAINSTORM owes
A **SELF-PICKED** subject with the rejected alternatives RECORDED · a new `docs/envoy-go/phases/89-<slug>/BRAINSTORM.md` + `PROGRESS.md` · **row 89 registered `in-progress`** in `ROADMAP.md` **and the sentinel `want` bumped 120 -> 121 IN THE SAME COMMIT** (the phase-84/85/86/87/88 precedent) · the sentinel + all four NCs **ON BOTH SIDES** of that edit, recorded as ACTUAL OUTPUT · `STATE.md` rolled IN PLACE (lifecycle-state **DONE -> 1**) with the oldest §Recent entry evicted by a DIRECT DATE READ · `next-prompt.txt` rolled (`git add -f`) · ONE squashed commit, merged and pushed. ⚠️ **A BRAINSTORM adds no ADR: next-free stays `ADR-0311` and the strict `PROPOSED` guard stays 0 — the SPEC re-arms it.**
