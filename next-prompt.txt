=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **PHASE 85 (`h2spec-selector-repair`) CLOSED AT THE 2026-08-10 IMPL — ROW 85 IS `done` AND NO BANKED MID-LIFECYCLE WORK REMAINS ANYWHERE ON THE BOARD.** All 117 chartered rows are `done`; check (1) is SILENT for only the SECOND time in project history — **AND THAT IS NOT THE SENTINEL FIRING: the condition is a CONJUNCTION and check (2) still prints SIX.** ⇒ **THIS SESSION PERFORMS A NEW-PHASE BRAINSTORM: SELF-PICK the subject per the standing directive** (smallest defensible candidate; record pick + rejected alternatives; register the new row and bump `want` 117 -> 118 IN THE SAME COMMIT — the phase-84/85 precedent). `STATE.md`'s `next-skill:` reads `BRAINSTORM — SELF-PICK` — that IS your instruction. Candidates on the board (§Candidates below; re-derive every cost at YOUR tip — `reference_deferred_candidate_cost_restale`).

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=117 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **`want` IS 117 AT THIS TIP** (all rows `done`). ⚠️ **AT THIS TIP CHECK (1) IS SILENT — the correct output with every row done; ANY `NOT DONE:` line appearing before your BRAINSTORM's own row registration is a FINDING.** Your BRAINSTORM registers a NEW row `in-progress` and bumps `want` 117 -> 118 in the SAME commit; after that edit check (1) prints exactly `NOT DONE: row <new>` — the expected line while the new phase is open. ⚠️ **THE SENTINEL DOES NOT FIRE AT THIS TIP:** check (2) prints SIX (at `:195 :201 :207 :217 :223 :231` pre-edit — re-derive, the anchors drift with every edit) and the condition is a CONJUNCTION. Check (3) is SILENT. The phase-85 IMPL close is the precedent: (1) SILENT · (2) SIX · (3) SILENT ⇒ does NOT fire, `stop` NOT created.

⚠️ **THE DOCTORING NCs REMAIN MANDATORY EVERY SESSION** — and doubly so while checks (1)+(3) are BOTH silent: a broken check is nearly indistinguishable from a healthy one. Both:
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md at want=117: expect "NOT DONE: row 62" ALONE
# (after your row registration at want=118: expect "NOT DONE: row 62" AND "NOT DONE: row <new>")
```
```sh
# the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```

⚠️ **AND WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -oiE '-family row'` fails with `grep: amily row: No such file or directory` and the surrounding arithmetic prints `base=0 now=0 delta=0`, **which reads exactly like "no change".** Observed live at FOUR phase-84 stages. **Always pass `--` before the pattern**, and remember the counts are form-dependent: **95 occurrences vs 67 LINES** at this tip (`grep -c` counts LINES). **A gate that reads zero on BOTH sides is not evidence of invariance.**

**SENTINEL RE-RUN MECHANICALLY AT THE PHASE-85 IMPL CLOSE (2026-08-10, base `4b427fe1`), BEFORE AND AFTER THE ROW-85 FLIP. IT DOES NOT FIRE; `stop` was NOT created** (`ls stop` => `No such file or directory`, repo root AND stage worktree). ACTUAL output, recorded not predicted: input measured **235 lines / 117 data rows** first; PRE-FLIP at `want=117`: **(1) `NOT DONE: row 85`** — the single expected line, denominator silent; **(2) SIX — `:195 :201 :207 :217 :223 :231`**; **(3) SILENT**. POST-FLIP: **(1) SILENT — the second time in project history**; **(2) SIX, same anchors** (⚠️ do NOT "fix" it and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases; the candidates sentences are a WINDOW onto a larger deferred backlog, not an inventory of it); **(3) SILENT**. The condition is a CONJUNCTION and check (2) prints ⇒ the sentinel does NOT fire.
- **ALL FOUR NCs FIRED ON BOTH SIDES OF THE FLIP:** row-62 doctoring => pre-flip `NOT DONE: row 62` AND `NOT DONE: row 85`, post-flip `NOT DONE: row 62` ALONE — with `NC LANDED? [ in-progress ]` inspected first; `want=116` => `GATE FAIL: examined 117 data rows, expected 116`; check-(3) doctoring (residual `gRPC-family row` confirmed **2 -> 0** on the doctored copy first) => `NEVER OPENED: gRPC` while `WASM` correctly stayed silent; check-(2) one-arm counts **5** (long arm alone) / **1** (short arm alone), union **6** (a one-arm strip is NOT an NC for the union).
- ⚠️ **Re-run all three checks and both doctoring NCs yourself and record what YOU see — BEFORE and AFTER your own row registration.**

**RE-EVALUATED MECHANICALLY ON EXPLICIT HUMAN INSTRUCTION 2026-08-10 (post-85-IMPL, at tip `83ebf029` == `origin/master`). IT DOES NOT FIRE; `stop` was NOT created (`ls stop` => `No such file or directory` at the repo root).** ACTUAL output, recorded not predicted: input measured **235 lines / 117 data rows** first; **(1) SILENT** at `want=117` — every chartered row is `done` and the denominator assertion printed nothing (the correct output at a between-phases tip; any `NOT DONE:` line here would have been a finding); **(2) SIX — `:195 :201 :207 :217 :223 :231`**; **(3) SILENT**. The condition is a CONJUNCTION and check (2) prints, so the sentinel does NOT fire — and `STATE.md`'s `next-skill:` reads `BRAINSTORM — SELF-PICK`, i.e. a next stage EXISTS and the loop continues. All FOUR NCs fired with checks (1)+(3) both silent (the state where a broken check is nearly indistinguishable from a healthy one — the NCs are the proof the checks still LOOK): row-62 doctoring => `NOT DONE: row 62` ALONE (with `NC LANDED? [ in-progress ]` inspected first); `want=116` => `GATE FAIL: examined 117 data rows, expected 116`; check-(3) doctoring (residual `gRPC-family row` confirmed **2 -> 0** on the doctored copy first) => `NEVER OPENED: gRPC` while `WASM` correctly stayed silent; check-(2) one-arm counts **5** (long arm alone) / **1** (short arm alone), union **6**, never 6 -> 0. This file was already rolled to the **post-85 SELF-PICKED BRAINSTORM** at the IMPL close and STAYS there — that is the next actionable stage. `want` STAYS **117** (the next BRAINSTORM bumps it to 118 in the same commit as its row registration); `ROADMAP.md` BYTE-UNTOUCHED by this re-evaluation (`-family row` re-read **95/67**, `gRPC-family row` **2**, `Operational-tooling-family row` **3**).

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (ugrep under the hood). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Reproduced at NINETEEN separate tips.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **THE METHOD BINDS; carry no standing discrepancy figure and no standing absolute.** ⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS; print FILE NAMES, never a count.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files; use `-v --no-index`, and `git status` is the discriminating check). ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **`grep -c` ON ZERO MATCHES IS TOOL-SPECIFIC** (only `git grep -c` prints nothing; all exit 1 — capture the value, never chain on the exit code). ⚠️ **`grep -c` COUNTS *LINES*, NOT *OCCURRENCES*.** ⚠️ **`\t` IN GNU ERE IS A LITERAL `t`** — use `-P`. ⚠️ **A `git grep` PATHSPEC ENDING IN `/` READS A FAIL-UNSAFE ZERO** (shape 33) — use the shell-glob form. ⚠️ **AN UNANCHORED PANIC-GREP FALSE-FIRES 14 TIMES ON A FULLY-GREEN DIFFERENTIAL LOG** — **the canonical gate is ANCHORED: `^panic:|DATA RACE|SIGSEGV`. A gate that fires on a green log is worse than no gate.**

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`** with FOUR blind spots (an em-dash, a DOT in `http-1.1`, two LETTER-suffixed ids). ⚠️ **EXECUTED on scratch copies at the phase-77 PLAN: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** ⚠️ **DO NOT restore the regex form.** At this tip check (1) is SILENT — **any row id appearing there before your own registration is a finding** — and the row-62 doctoring NC is what proves it still LOOKS.

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

⚠️ **Run the union form, never one half of it.** ⚠️ **A ONE-ARM STRIP IS NOT AN NC** (6 -> 5 short / 6 -> 1 long; never 6 -> 0). ⚠️ **The documented trap's stated MECHANISM is refuted while the trap is real** — the inflation comes from ROADMAP table rows carrying BRAINSTORM prose. **Cite the command; do not re-inherit the explanation.** ⚠️ **The line numbers drift with edits — re-derive them; the current six are `:195 :201 :207 :217 :223 :231`.** ⚠️ **These six paragraphs remain the final sentinel blocker, and any edit near them is a sentinel-affecting edit.** All six are standalone family PROSE paragraphs (not rows), naming ~42 distinct candidates; every "retire the paragraphs" shape is doctrinally foreclosed (`reference_sentinel_matcher_string_self_clears`). If your pick comes from a family window sentence, the row-done narrowing rule for that sentence applies at YOUR phase's close; a pick from outside the windows (the phase-85 shape: SPEC-84 §13) narrows nothing.


==============================================


## STATUS AS OF 2026-08-10: PHASE 85 CLOSED (row 85 `done` at the IMPL, base `4b427fe1`). ALL 117 ROWS `done`; NO BANKED WORK. **THIS SESSION SELF-PICKS A NEW SUBJECT AND PERFORMS ITS BRAINSTORM.**

**What the 85 IMPL landed (2026-08-10, base `4b427fe1`, branch `phase-85-impl`, ONE squashed commit).** THE HEADLINE: **the h2spec gate now runs its TRUE 95-case scope and is GREEN — `95 tests, 94 passed, 1 skipped, 0 failed` (skip 6.9.2/2, invariant)** — after 107 days of nine silently no-op slash-form selectors (2026-04-25 -> 2026-08-10). Production (3 files, `internal/filter/hcm/h2/`): shared `validateSetting` on BOTH read paths (validate-before-apply, two-pass); `readClientSettings` preserves the framer's own ConnectionError code via `translateFramerErr` reuse + `Run` step-3 own-code emission (the wrong-code HANDSHAKE defect fixed); the IWS=0 announced-flag quirk fix (`InitialWindowSizeAnnounced`, seeding + walk effective-old sharing ONE computation); `window.adjust` single-critical-section; the §6.9.2 live-stream walk (conn-level window untouched). Harness: nine dotted selectors, JUnit `ClassName`/`Error` parse fix, 31-entry `expectedSuites` roster (sums to 95), three guard layers + REVERSE layer-2 direction, zero-case skip DELETED, 6.6 comment rewritten. CI: the gate enrolled in the `differential` job (~13 lines). Docs: **ADR-0307 COMPLETE** (strict `PROPOSED` guard **1 -> 0, DISARMED — a new SPEC re-arms it**), `CONFORMANCE_PINS.md` +103 append-only (31-row corrected table + run record + reference-observation ¶), `BEHAVIOR_CONTRACT.md` net-zero `2 2` at `:2054`/`:2056`, `STATE.md:38` flipped (no longer a false line), row 85 flipped. TDD held: 11 arms re-proven RED at the IMPL tip (census re-executed by an independent reviewer), 2 green-on-arrival regression pins, 3/3 guard NCs fired + sha256 restores.

### ⚠️ WHAT THE IMPL MEASURED THAT ITS PREDECESSORS GOT WRONG — the stage-refutes-predecessor lineage held

- **PLAN §8 item 7's "24 suites incl. six 6.x rows" pins-table instruction was STALE against the PLAN's own §1.4** — the landed `CONFORMANCE_PINS.md` table is **31 rows / 13 `6.x` rows / Total 95**, re-derived independently from the roster literal AND the captured JUnit XML. A successor copying "24" from PLAN §8 will be wrong.
- **The PLAN's ~760-900 net-`.go` IMPL budget was itself a lower bound: realized net +1046** (production +143, harness +79, tests +824 in TWO new files) — **the EIGHTH consecutive `reference_measured_prototype_is_a_lower_bound` firing**, cause again under-ENUMERATION (the IMPL's fuller test arms: 824 vs the prototype's 504-in-one-file). Overrun RECORDED in ADR-0307 §Consequences, not silently absorbed.
- **A NEW unclassed flake:** `TestServerConn_TinyWindowDelivery` (`internal/filter/hcm/h2`, PRE-EXISTING test) hit a 10 s i/o-timeout ONCE under full-`./...` load; cleared scoped x5 + full-package x3 + full run 2. Mechanically disjoint from the walk (no mid-stream SETTINGS in that test). Recorded, not fixed; NOT yet in any documented flake class.

### ⚠️ COUNTS AT THIS TIP — RE-DERIVE MECHANICALLY, NEVER COPY

**HOUSE RULE: every number below is a LIVE FIGURE. Re-run it in your own worktree with its negative control observed.**

- **differential fixtures 121** — numeric tail `0119-grpc-unary-trailers`, next-free `0120`.
- **`ROADMAP.md` 235 lines / 117 data rows** (ALL `done`); check-(2) anchors `:195 :201 :207 :217 :223 :231`; `-family row` **95/67**; `gRPC-family row` **2**; `Operational-tooling-family row` **3**; ARM-A well-formedness flags **{119, 131} ONLY** (escape-aware pipe-split, field count != 8; row 85 at `:147` is clean). ⚠️ Your row registration must keep ALL of these except adding its own row (+1 line, +1 row, `want` -> 118).
- **`DECISIONS.md` 18050** lines · **306** `^## ADR-` headings · tail **`ADR-0307` COMPLETE** · `^## ADR-0308` = **0** · strict `PROPOSED` guard **0 — DISARMED and correct** (your SPEC's new ADR §Context re-arms it 0 -> 1). ⚠️ next-free ADR-0308 is DERIVED FROM THE TAIL (headings+1 COLLIDES at the ADR-0209 gap).
- **`BEHAVIOR_CONTRACT.md` 5955** · **`STATE.md` 64** · **`STATE_HISTORY.md` 474** · **`BOOTSTRAP_PROMPT.md` 522** at the repo ROOT · phase dirs **126**, `REVIEW.md` **37** (standing departure, named not claimed).
- **h2spec (BY EXECUTION at this IMPL): `95 tests, 94 passed, 1 skipped, 0 failed`** — ⚠️ **cite ONLY from your own tip's run**; the harness now guards itself (3 layers + reverse; NCs demonstrated). Reference (`contrib-v1.37.2`): 95/82/1/12 with a flaky section-8 twelfth slot — RECORD-only, never expectations (`CONFORMANCE_PINS.md`).
- **stat call sites 208 / 36 production files** (`\.New(Counter|Gauge)(IfAbsent)?\(` non-test, comment-filtered) · **fuzzers 55 / 48 files** · **BackendKind tail 38** over 39 declarations (do NOT "fix").
- ⚠️ **STILL CONTESTED, NO NUMBER CARRIED:** the `STATE_HISTORY.md` archive-gap total · the loose `PROPOSED` matcher's whole-file count · the stat-surface absolute (1205 vs 1207 — WIDENED; only DELTAs are asserted) · the zero-test `generic/*` suite count (13 vs 14).

### ⚠️ DOCUMENTARY DEFECTS — carried forward, still deliberately NOT fixed

- **`ADR-0051` §2's false gate-scope sentence** is now CORRECTED ON THE RECORD by ADR-0307 §Decision (the sentence itself stays in ADR-0051 as history — do not edit it). **`ADR-0058`** cites `routerActionH2.doH2` at a location that doesn't exist (`router_h2.go:254` / `h2dispatch.go:720` are real). **`ROADMAP.md` rows at lines 119/131 malformed** (ARM-A GUARD, not a fix). **`STATE.md` §Project counts FROZEN at phase 76** (do not source; do not fix) — `STATE.md:38` is FIXED as of this close and is no longer on this list. **`harness_test.go:208` port inventory STALE.** **`body.go` nolint directives INERT** · **xDS cycle guard NOT AUTOMATED** · **`wasm/doc.go:219` two errors** · **ROADMAP cites `esalaine` FIVE times** (module is `pgdad`) · **`rbac.go:50` token `F2`** (don't grep-anchor) · **root `PROGRESS.md`** stray phase-32.1 doc.

### Candidates for the SELF-PICK (record pick + rejections in the BRAINSTORM; re-derive EVERY cost at your tip)

- **The CONTINUATION two-sided repair** — the strongest KNOWN product defect on the board (server discard `conn.go:255-259` + client blindness), now with the client-side SETTINGS gap (`h2/client.go:376-407`: mid-connection zero validation and no walk REMAIN, plus the seeding quirk at `:192-195` now set-but-unread, while the HANDSHAKE read of the server's initial SETTINGS is INCIDENTALLY validated as of the 85 IMPL via the shared `readClientSettings` — RFC-correct but UNGATED, no client-side unit arm — so the symmetry is now PARTIAL, not exact, and the row owes the handshake arm too) NAMED beside it in ADR-0307. h2spec MEASURED not to gate either side (6.10 green over the discard; the client side has no conformance driver at all). Est. 2-4x row 85.
- **The stat-surface recount** — the 1205-vs-1207 contradiction WIDENED and both figures are DOC-SOURCED; a counting phase would replace the chain with a measurement. Cheapest candidate; repairs no gate.
- **The `validate` nil-`sdsProvider` bug** — still the best sub-row-sized production bug on the board.
- **`ssl.connection_error`** (+444 whole-`.go` VERIFIED floor) · **`test/conformance/grpc/`** (9/26 reachable) · **REVIEW.md restoration** (process-not-product) · the **~42 candidates in the six family windows** (`:195 :201 :207 :217 :223 :231`).

### ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + BOTH DOCTORING NCs YOURSELF, BEFORE AND AFTER YOUR ROW REGISTRATION** — record ACTUAL output.
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** Lineage 85: B (gate run both ways) · S (reference measured) · P (whole change set prototyped; four claims moved) · I (**the PLAN's own §8 "24-row" instruction and its budget both refuted by execution; a probe agent's inference about a cell it never ran is a CLAIM**).
3. **LINE ANCHORS DRIFT; SYMBOL ANCHORS DID NOT — RE-VERIFY EVERY CITE AT YOUR TIP.** The phase-85 production sites now include `validateSetting` (settings.go), `window.adjust` (flow.go), the walk in `onSettings` (conn.go), `InitialWindowSizeAnnounced` (settings.go struct + both set-sites).
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Still live: SDS dial-budget (TWO packages) · `internal/cluster` `-race` outlier · `internal/httpclient` zero-value · driver-receiver port race (roster 42; `0081` binds `0.0.0.0:42039`) · the two 84.2-era flakes (cleared, unclassed) · **`TestServerConn_TinyWindowDelivery` (NEW at 85-IMPL, cleared, unclassed)** · the REFERENCE h2spec section-8 flip (reference-side only; subject zero-variance).
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only).
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`.
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL.** **A harness's exit code is not the command's.** **`gofmt -l` never exits non-zero — gate on OUTPUT.**
8. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-85-*` or stage worktree should outlive its stage close** — expect only `master` plus your own.
9. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** (a)/(b) differential 121 fixtures, full suite with `INNER_EXIT`; (c) **the h2spec citation caveat is RETIRED — cite `95/94/1/0` only from YOUR OWN tip's run**; grpc-conformance deferred in writing; proxy-wasm 10/16; (d) fuzzers 55/48; (e) `INNER_EXIT` on every differential launch AND `go test ./...`; (f) no REVIEW.md — standing departure.

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 85 (h2spec-selector-repair)' --format='%h %s'   # 4 at this tip: B + S + P + IMPL. An invented slug returns 0.
git log --grep '^phase 84 (grpc-unary-response-trailers)' --format='%h %s'  # 6 — a CLOSED phase, use it as a FIRING CONTROL
```
⚠️ **The count comments are LIVE FIGURES — VERIFY before relying.** Take the NEWEST line (`| head -1`).

Subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** and tear down docker **BY NAME**. ⚠️ **Band private ports outside BOTH `20000-31007` AND `11000-14999`, clear of the static fixture ports (10000-19172); `0081` binds `0.0.0.0:42039`.** Phase-85 stages used `46500-46899`; the IMPL reserved `46900-46999` and USED NONE (the h2spec harness picks ephemeral ports) — **the next session should band from `47000-47099`.** ⚠️ **ONE WORKTREE PER AGENT preferred; explicit-path staging if shared; never an unscoped `git restore`.** Per-task `gofmt` + `golangci-lint` on touched packages (⚠️ misspell locale US). ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch.**

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root — **observed live AGAIN at this IMPL (every compound command's trailing check printed the repo root).** Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — sha256 against the base blob, never by eye; work DETACHED so a failed revert cannot dirty a branch.

⚠️ **THE ARCHIVE-ABSENCE GUARD — USE THE ROBUST FORM** (bullet-anchored + whole-file fixed-string, NC'd with a REAL target re-derived FROM THE FILE first; a control's own count is a LIVE FIGURE: phase-82-IMPL read **7** at this close, up from 6; phase-83-PLAN read **5**, up from 4). **Verify STRICTLY APPEND-ONLY**: `git diff --numstat` = `N 0` AND base a byte-exact PREFIX (`cmp`).

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads `BRAINSTORM — SELF-PICK` — that IS your instruction.** §Project counts SELF-CONTRADICT §Current — do not anchor on them (frozen at phase 76; `:38` is now correct).
2. `docs/envoy-go/ROADMAP.md` — §Schema first (`:18`); the six family-window paragraphs; the candidate ledger above.
3. `docs/envoy-go/DECISIONS.md` tail — ADR-0307 COMPLETE (the phase-85 record incl. the client-side gap and the CONTINUATION backlog); ADR-0298/0300 for the maintenance-row-no-ordinal precedent.
4. `docs/envoy-go/phases/85-h2spec-selector-repair/` — the closed phase's four stage docs, for the candidate provenance (SPEC §13, PLAN §1) and the BRAINSTORM's rejected-alternatives ledger to extend.
5. `docs/envoy-go/CONFORMANCE_PINS.md` — the corrected 95-case record and the refresh procedure (any future roster change rides it).

## After the BRAINSTORM lands — the roll-forward
The new row registers `in-progress`, `want` bumps 117 -> 118 in the SAME commit; check (1) prints exactly the new row's line; the sentinel still does NOT fire (check (2) prints SIX) — re-run everything and record ACTUAL output. **Roll this file to the new phase's SPEC**, carrying: the pick + rejected alternatives, the sentinel state you observed (both NCs, before AND after registration), the re-derived counts at your tip, and the phase-85 lineage notes above (they compress at your discretion but the REFUTED-claims ledger and the flake register survive verbatim).
