START a fresh cold-start session for envoy-go — **the phase-38.2 (`weighted-clusters`) PLAN is DONE; the next lifecycle step is the phase-38.2 IMPL** (`superpowers:subagent-driven-development` per `feedback_execution_style` — execute `docs/envoy-go/phases/38.2-weighted-clusters/PLAN.md` Tasks 1–10 in a fresh worktree; one fresh subagent per task + two-stage review + a final holistic review; Task 1 creates PROGRESS.md; subagents commit LOCAL-ONLY per `feedback_subagents_no_push`; the controller squash-merges + pushes at stage-close per `feedback_push_to_origin`; per-task `gofmt -l` + `golangci-lint` per `feedback_pertask_gofmt_lint`; the ADR-0052 six-gate at Task 10). The phase-38.2 PLAN is the pre-authorized SECOND leg of the phase-38 by-plane split (the 36.1/36.2 precedent). The substantive master tip is the phase-38.2 PLAN-done bundle **`d3227ba`** (`phase 38.2 PLAN: weighted-clusters — SPEC §10 decomposed into 10 bite-sized TDD tasks`; `docs/envoy-go/phases/38.2-weighted-clusters/PLAN.md` + STATE + ROADMAP row 38 + README; pushed to `origin/master`; confirm via `git log --oneline -5`). This paragraph is the docs-only SHA-fill that NAMES `d3227ba` the live tip (it sits directly above the substantive PLAN-done tip `d3227ba`). The prior substantive tips are the phase-38.2 SPEC-done bundle `90cf818` and the phase-38.1 IMPL squash `4eedf0e` (subset LB; LANDED + PUSHED). Counts UNCHANGED at the 38.2 PLAN: **stat surface 1125 / fixtures 66 / fuzzers 42 / BackendKind tail 33 / DECISIONS tail ADR-0240, next-free ADR-0241**.

## What just landed (phase 38.2 PLAN — the durable record is PLAN.md + SPEC.md, read those not this summary)

The PLAN decomposes SPEC §10 into **10 bite-sized TDD tasks** (code-complete: exact file paths, complete Go snippets matching the as-built signatures, exact commands + expected output). The FINAL ADR-0045 split-gate re-check → **NO FURTHER SPLIT** (~290 prod LoC / 10 tasks — both axes under the `> ~25 tasks OR > ~1500 LoC` gate). plan-document-reviewer APPROVED (4 advisory fixes applied). The 10 tasks:
1. **Baselines/anchors gate + PROGRESS.md** — re-confirm fixtures 66 / fuzzers 42 / stat surface 1125 / BackendKind 33 / DECISIONS tail ADR-0240 + re-pin the as-built anchors.
2. **`weightedSelector` + RNG + `pick()`** (NEW `internal/filter/http/router/router_weighted.go`) — cumulative weights + a per-action mutex-guarded `newWeightedRNG()` (mirrors `cluster.newPCGRNG` in-package, ~12 LoC) + `pick()` (r = rng()%Σweight, first cumulative bucket > r; weight-0 entries never picked) + the folded distribution property test.
3. **`mergeRouteSubsetMatch`** (`hcm/config.go`) — route-level ⊕ entry `envoy.lb` scalars, ENTRY precedence; reuses `ScalarsFromStruct`/`NewSubsetMatch` + the 38.1 non-scalar reject.
4. **`WeightedCluster` type + `H1/H2WeightedClusterAction`** (router_weighted.go) — pre-build N `*routerAction`, pick the index, dispatch via the EXISTING `doH1ClusterAction`/`doH2ClusterAction` (NO `loadBalancer.Pick` change).
5. **`weightedClusterRouteAction` bridge** (`hcm/actions.go`) — the `clusterRouteAction` sibling; satisfies `routeAction` {do/asRouterAction/asRouterActionH2}.
6. **`buildRouterAction` `RouteAction_WeightedClusters` switch arm + the SIX-arm reject roster + the FULL `buildWeightedRouterAction`/`buildWeightedBridge`** — the reject precedence is name → cluster_header-unsupported → weight → cluster-exists → sum-0 (aligned to SPEC §6).
7. **Accept-case test coverage** (no new prod code) — explicit-0 entry accepts / non-matching `total_weight` ignored / merge-precedence end-to-end.
8. **The `0065-weighted-clusters` fixture** — per-side weight-distribution band (weights {50,30,20}, N=500, per-backend ~4.5σ bands [200,300]/[104,196]/[60,140]) + the composition FOLDED into the weighted route (one weighted cluster is a subset cluster with `fallback_policy: ANY_ENDPOINT` + a `ClusterWeight.metadata_match{version:v1}` → its v2 endpoint backend3 starved to 0) + cross-side conservation (Σ per-cluster `upstream_rq_total` = N).
9. **Deliberate-break liveness** (`-count=1`) — swap-weights / drop-cluster / drop-compose-metadata_match + ≥20-run flake.
10. **Full differential re-verify + completion bundle** — the six-gate; BEHAVIOR_CONTRACT delta; ADR-0241 (§Context+§Decision+§Consequences, ACCEPTED in-place per ADR-0044 → DECISIONS tail ADR-0241); STATE/ROADMAP/README; fixtures 66 → 67.

The §12 D-WC-IMPL-1..5 are RESOLVED in the PLAN Pre-flight (file placement; the in-package RNG mirror; the deferred-field disposition [`cluster_header` REJECTS / header-mutation+typed_per_filter_config+host_rewrite SILENT-IGNORE]; the `0065` constants + band formula σ=√(N·p·(1−p)); the composition-folded design; the property-test placement).

## What the phase-38.2 IMPL does (the next step)

Execute PLAN.md Tasks 1–10 subagent-driven (one fresh subagent per task; two-stage review per task — a code-reviewer pass + the controller's own check; a final holistic review before the completion bundle). The as-built code 38.2 builds ON is UNCHANGED in shape: reuse `doH1/H2ClusterAction` + `parseRouteHashPolicies` + `ScalarsFromStruct`/`NewSubsetMatch` + `cluster.WithSubsetMatch` VERBATIM (NO `loadBalancer.Pick` change, NO exported-cluster change). NEW: `internal/filter/http/router/router_weighted.go` (the selector + the `WeightedCluster` type + the H1/H2 constructors) + the `weightedClusterRouteAction` bridge in `hcm/actions.go` + `buildWeightedRouterAction`/`mergeRouteSubsetMatch` in `hcm/config.go` + the `0065-weighted-clusters` fixture. ZERO new packages/go.mod-deps/stats/fuzzers/BackendKind (assert explicitly at Task 10). The single-cluster `RouteAction_Cluster` arm stays byte-stable (the 66 prior differential dirs are the guard). ADR-0004 was already executed at the SPEC — do NOT re-probe; the SPEC §11 D-WC pins are the empirical authority.

## Read first (in order)

1. THIS FILE — the post-phase-38.2-PLAN cold-start (next: the phase-38.2 IMPL).
2. `docs/envoy-go/STATE.md` — active-phase `phase 38.2 (weighted-clusters) PLAN done`; the counts (fixtures 66 / fuzzers 42 / stat surface 1125 / BackendKind 33 / DECISIONS tail ADR-0240, next-free ADR-0241); the lifecycle routing (→ the 38.2 IMPL).
3. `docs/envoy-go/phases/38.2-weighted-clusters/` — PLAN.md (the 10-task spine + the Pre-flight ADR-0045 re-check + the D-WC-IMPL resolutions + the File structure map) + SPEC.md (the §3 architecture + §5 proto roster + §6 reject roster + §7 zero-stat + §8 `0065` differential + §11 D-WC1..D-WC6 pins + §13 ADR-0241 draft) + README.md.
4. `docs/envoy-go/phases/38-load-balancer-subset/` — SPEC.md (§3.0 split) + PROGRESS.md (the 38.1 as-built subset LB + the ADR-0239 seam 38.2 reuses UNCHANGED).
5. `docs/envoy-go/DECISIONS.md` — ADR-0239 (the subset-match seam + producer — REUSED UNCHANGED) + ADR-0240 (the subset LB policy — the 38.2 composition target) + ADR-0237/0235 + the process ADRs (0044/0045/0052/0080/0106/0004/0227).
6. The 38.1 as-built code 38.2 builds ON: `internal/filter/hcm/{actions.go [clusterRouteAction], config.go [buildRouterAction + parseRouteSubsetMatch + parseRouteHashPolicies], route.go [routeAction iface]}` + `internal/filter/http/router/{router.go [H1ClusterAction + doH1ClusterAction + applyHashKey + routerAction], router_h2.go [H2ClusterAction + doH2ClusterAction + routerActionH2]}` + `internal/cluster/{subset.go [SubsetMatch/NewSubsetMatch/ScalarsFromStruct], cluster.go [WithSubsetMatch], random.go + leastrequest.go [newPCGRNG — the RNG model the selector mirrors]}` + `test/fixtures/{0060-lb-random [the per-side band precedent], 0064-lb-subset [the cross-side HTTP-route driver + the subset cluster shape]}/` + `test/differential/fixture/fixture.go [DistributionAsserter, StatsAsserter]` + the as-built test helpers `mkClusterManager`/`singleEndpointCluster`/`loopbackHTTPEcho` (the PLAN's tests extend these).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_execution_style` (subagent-driven IMPL), `feedback_git_worktrees`, `feedback_subagents_no_push` + `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_path_targeting`/`_detach`, and the differential references (`_break_protocol_count1`, `_run_selector` [use `-run 'TestDifferential/0065'`], `_band_sigma_margin` [the per-side weight-distribution σ-margins — load-bearing for `0065`], `_hash_key_cross_side_infeasible` [why the band is per-side], `_fixture_workload_constant_desync` [N/band constants synced], `_docker_probe_bridge_network`, `_asserter_dispatch`, `_fixture_dispatch_constraint`).

## Honor throughout

- The lifecycle is BRAINSTORM → SPEC → PLAN → IMPL, each its own session. The next session is the phase-38.2 **IMPL** (`superpowers:subagent-driven-development` — execute PLAN.md Tasks 1–10; the ADR-0052 six-gate at Task 10).
- `feedback_git_worktrees` (a fresh worktree for the IMPL) · `feedback_execution_style` (subagent-driven; one fresh subagent per task + two-stage review) · `feedback_subagents_no_push` (subagents commit local-only) · `feedback_push_to_origin` (the controller squash-merges + pushes at stage-close) · `feedback_pertask_gofmt_lint` · `feedback_subagent_worktree_path_targeting`/`_detach` (pin the canonical worktree path; GIT HYGIENE block on deliberate-break checks).
- ADR-0004 (empirical pins — already EXECUTED at the SPEC; the IMPL re-uses them) · ADR-0044 (ADR §Context at SPEC, body at IMPL in-place — ADR-0241 lands at Task 10) · ADR-0045 (the split-gate — re-checked at the PLAN, NO further split) · ADR-0052 (the atomic-landing six-gate) · ADR-0080 (byte-stable parse-reject wording) · ADR-0106 (flat family row — NO parent rollup) · ADR-0227 (the contrib reference image).

## After phase 38.2 — the longer horizon (see `ROADMAP.md` §Feature Families for the authoritative detail)

Forward-looking ONLY — the IMMEDIATE next step is the phase-38.2 IMPL above. After 38.2 the Load-balancing family has **3 candidates left** {locality-weighted LB, priority load balancing, panic thresholds} — **ALL gated by the absent health-checking boundary**, so they CANNOT land until the Upstream-robustness family exists. Per ADR-0106 sibling rows are NOT pre-populated; the next subject (a remaining LB candidate, or a NEW family) is chosen at the next BRAINSTORM. **Six families are essentially unstarted** (each opens with its own `superpowers:brainstorming`, MVP-scoped): xDS / dynamic config (the single largest gap — the whole project is STATIC bootstrap today); Upstream robustness (active health checks / outlier detection / circuit breakers / retries + hedging / connection pooling — UNBLOCKS the 3 health-gated LB candidates); HTTP/3 + QUIC; gRPC (bridge / gRPC-Web / transcoding); Observability (gRPC ALS / OTLP / tracing / stats sinks / tap); Runtime + hot restart (the RTDS-driven runtime layer — note `WeightedCluster.runtime_key_prefix` + `random_value_specifier.header_name` were DEFERRED at 38.2 to this family). The §9 Network-filters family is CLOSED (phases 26–33); the HTTP-filters family (09–25) shipped its curated set but is NOT formally closed; the WASM host family is partially satisfied (proxy-wasm HTTP-filter conformance via phase 25).
