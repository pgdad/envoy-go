START a fresh cold-start session for envoy-go — **the phase-36 (`load-balancer-ring-hash`) BRAINSTORM is DONE and LANDED on master; the next lifecycle step is the phase-36 SPEC** (`superpowers:writing-plans` scoped to SPEC authoring — the kafka-31/thrift-33/least_request-34/random-35 single-flat-row precedent). The substantive master tip is the phase-36 BRAINSTORM squash **`ffb3a61`** (`phase 36 BRAINSTORM: load-balancer-ring-hash — RING_HASH the FOURTH LB policy / FIRST consistent-hashing; EXTEND the ADR-0232 seam (ctx-carried hash key, exported surface byte-stable); BOTH planes (tcp source_ip + HTTP route hash_policy); pre-authorized 36.1/36.2 split; 2 ADRs (0235 seam + 0236 policy)`; pushed to `origin/master`; confirm via `git log --oneline -3`). It carries `docs/envoy-go/phases/36-load-balancer-ring-hash/BRAINSTORM.md` (the 12-§ family-template doc) + the ROADMAP row-36 `in-progress` registration with the `36.1, 36.2 (pre-authorized)` split column + the Load-balancing family-heading `ring_hash IN-PROGRESS` note + the STATE.md advance (active-phase → `phase 36 (load-balancer-ring-hash) BRAINSTORM done`). The literal live master tip is this docs-only `next-prompt.txt` roll-forward commit (the substantive BRAINSTORM squash `ffb3a61` is the parent; confirm via `git log --oneline -3`). Below the BRAINSTORM squash sits the phase-35 IMPL squash `4618841` (the `randomLB` policy + the FIRST ADR-0232 seam reuse) and its docs-only SHA-fill tips.

## What this session does — author the phase-36 SPEC

The phase-36 {ring_hash} BRAINSTORM is COMPLETE (DESIGN-ONLY). The next session authors `docs/envoy-go/phases/36-load-balancer-ring-hash/SPEC.md` (+ README.md) via `superpowers:writing-plans` scoped to SPEC authoring, executing the BRAINSTORM §10 **D-RH1..D-RH8** empirical pins IN-SESSION (parallel-subagent fan-out per the 25–35 SPEC precedent) live against (1) the contrib reference Envoy `envoyproxy/envoy:contrib-v1.37.2` (ADR-0227) per `reference_docker_probe_bridge_network` (bridge network + STRICT_DNS backend hostnames + verify traffic ran via `downstream_cx_rx_bytes_total > 0`), (2) go-control-plane `/envoy` v1.32.4 bindings (`go mod tidy -diff` — anticipated EMPTY), (3) upstream Envoy v1.37.2 source (`source/extensions/load_balancing_policies/ring_hash/`). The SPEC anchors the **ADR-0235 + ADR-0236 §Context drafts** and makes the **FINAL ADR-0045 36.1/36.2 split decision**.

## The BRAINSTORM-settled design (do NOT re-litigate — the SPEC pins the empirics)

- **Q0 subject** = `ring_hash` (`Cluster.LbPolicy RING_HASH`, enum value **2**, `envoy.config.cluster.v3` — VERIFIED in the v1.32.4 module cache WITH a `Cluster.RingHashLbConfig` message [`minimum_ring_size`/`maximum_ring_size` `UInt64Value` defaults 1024/8388608 + `hash_function` enum XX_HASH·MURMUR_HASH_2]; `type.v3.HashPolicy` [tcp_proxy element, field 11] + `RouteAction.HashPolicy` [HTTP element] also present; ZERO new go.mod dep). The project's FOURTH LB policy and its FIRST consistent-hashing policy. 5 candidates remain after 36 {maglev, subset LB, locality-weighted LB, priority load balancing, panic thresholds} — maglev the cheap follow-on reusing this seam.
- **Q1 envelope** = BOTH hash-input planes: tcp_proxy (`hash_policy: source_ip`) + the HTTP router (route-level `RouteAction.hash_policy`, header + connection-properties source-ip subset; cookie/query/filter-state DEFERRED). + `RING_HASH` accepted by `cluster.Manager` (`RingHashLbConfig` parse + HAND-ROLLED PGV gate + the reject TEXT extends `… ROUND_ROBIN, LEAST_REQUEST, RANDOM` → `…, RING_HASH`) + a `ringHashLB` (a Ketama ring built once from the endpoint set, `Pick(hashKey)` binary-searches for the first ring position ≥ key, wrap; returns `noopRelease`).
- **Q-seam** = **EXTEND** the ADR-0232 seam via a **ctx-carried hash key** (the PICK-INPUT-half widening ADR-0232 §Consequences explicitly anticipated). The unexported `loadBalancer.Pick` widens to receive an optional request-derived hash key; the consumer computes it + carries it in `context.Context`, `cluster.go` extracts (`withHashKey`/`hashKeyFrom`) + threads it; the **exported `Cluster` surface stays BYTE-STABLE** (`Dial`/`AcquireH1` already take `ctx` — the OPTION-C discipline preserved; only the two hash-producing consumers [tcpproxy/router] churn; `PickEndpoint()` no-hash → random fallback); the 3 existing policies ignore the key behavior-neutrally (all 62 fixtures byte-identical).
- **Q-split** = a **PRE-AUTHORIZED 36.1/36.2 by-plane split** (36.1 seam-ext + ringHashLB + manager + tcp_proxy `source_ip` plane; 36.2 HTTP route `hash_policy` plane; ~330–630 prod LoC / ~16–22 tasks UNDER the ADR-0045 hard gate as a single row but couples two subsystems → the FINAL ADR-0045 re-check at SPEC/PLAN decides whether to CONSUME it [anticipated consumed; the phase-34 valve precedent]).
- **Differential** = the AFFINITY + SPREAD arm — `0061-lb-ring-hash` (tcp source-IP; 36.1) + `0062-lb-ring-hash-http` (HTTP header; 36.2) binding the client to multiple `127.0.0.x` source IPs (D-RH8) to assert same-key→same-backend (affinity, stable) + distinct-keys→different-backends (spread), PER SIDE [cross-side-exact host identity IFF D-RH2 proves byte-exact ring reproduction feasible; else per-side bands per `reference_differential_band_sigma_margin`] + a cross-side `StatsAsserter` prong; a possible ring-config boot-reject dir (D-RH1/D-RH5, a separate dir per `reference_differential_fixture_dispatch_constraint`). NO new BackendKind (tail STAYS 33) / anticipated NO new fuzzer (fuzzers STAY 42) / anticipated ZERO stat-delta (surface STAYS 1116).

## The D-RH1..D-RH8 empirical pins the SPEC must EXECUTE (BRAINSTORM §10)

D-RH1 the v1.32.4 config surface (RingHashLbConfig fields/PGV bounds [min ≤ max ≤ 8388608, HAND-ROLLED] + `type.v3.HashPolicy` + `RouteAction.HashPolicy`; `go mod tidy` clean; the `ring_hash_lb_config` oneof-member silent-ignore parity) · D-RH2 *(SPEC-BLOCKING for the fixture assertion strength)* the EXACT Ketama ring-build algorithm + hash function + lookup direction — **decide whether byte-exact cross-side ring reproduction is FEASIBLE** (cross-side-exact host identity vs per-side affinity+spread bands) + the no-hash fallback · D-RH3 the per-plane hash-key COMPUTE (tcp source_ip → uint64; HTTP header/connection-properties → uint64) · D-RH4 the stat-surface delta (anticipated ZERO new names) + the 0061/0062 StatsAsserter set · D-RH5 the accept/reject matrix + the reject-text blast radius (the doubly-hit retarget RING_HASH → MAGLEV) + whether a ring-config PGV boot-reject dir is warranted · D-RH6 *(36.2 scope)* the HTTP route `hash_policy` key-source subset (header + source-ip land; cookie/query/filter-state defer) + the multi-policy combine · D-RH7 *(SPEC-BLOCKING for the seam + split)* the EXACT ctx-carry seam surface (the internal `Pick` signature, the context-key type, the helpers, the PickEndpoint no-hash fallback) + the ADR-0045 envelope re-check + the FINAL 36.1/36.2 split decision + ADR-0024 unamended + whether a `FuzzRingLookup` is warranted (anticipated NO) · D-RH8 *(SPEC-BLOCKING for the harness)* the multi-source-IP client-bind harness (can the differential client bind to multiple `127.0.0.x`?) + the affinity/spread band + the ≥20-run flake-free protocol.

## Counts at master tip (phase 36 BRAINSTORM done — confirm at the SPEC baseline)

- stat surface **1116** (UNCHANGED at the brainstorm; anticipated UNCHANGED at the phase-36 IMPL — D-RH4).
- differential fixtures **62** (tail `0060-lb-random`; `ls -d test/fixtures/[0-9]* | wc -l`). Anticipated → 64 (or 65 with a boot-reject dir) at the IMPL.
- fuzzers **42** (canonical ledger; tail `FuzzThriftDecode`). Anticipated UNCHANGED at the IMPL (no wire decode).
- BackendKind tail **33** (`TCPThriftResponder`). Anticipated UNCHANGED (`TCPEcho` + existing HTTP backends reused).
- DECISIONS tail **ADR-0234** (next-free **ADR-0235**). The phase-36 SPEC anchors the ADR-0235 (seam) + ADR-0236 (policy) §Context drafts; the tail advances ADR-0234 → ADR-0236 across the phase-36 IMPL per ADR-0044.
- reference pin: `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227).
- Toolchain: Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); go-control-plane `/envoy` v1.32.4 (ADR-0008).
- conformance h2spec 53/53 + proxy-wasm 10/10.

## Read first (in order)

1. THIS FILE — the post-phase-36-BRAINSTORM cold-start (next: the phase-36 SPEC).
2. `docs/envoy-go/phases/36-load-balancer-ring-hash/BRAINSTORM.md` — the settled design + the §10 D-RH1..D-RH8 pin obligations.
3. `docs/envoy-go/STATE.md` — active-phase `phase 36 (load-balancer-ring-hash) BRAINSTORM done`; the lifecycle → the phase-36 SPEC routing; the counts.
4. `docs/envoy-go/ROADMAP.md` — row 36 `in-progress` (flat family row + the 36.1/36.2 valve); the Load-balancing family-heading + the 5-candidate-after-36 roster.
5. `docs/envoy-go/DECISIONS.md` — **ADR-0232** (the LB seam — its §Consequences PICK-INPUT-half anticipation that phase 36 CONSUMMATES) + ADR-0233 (least_request) + ADR-0234 (random) + ADR-0024/0045/0044/0052/0080/0106/0060/0063.
6. `internal/cluster/{loadbalancer,leastrequest,random,manager,cluster}.go` — the as-built LB layer (the seam `Pick() (Endpoint, func(), error)` + `noopRelease`; the three policies; the lb_policy switch + reject text; the `Dial`/`AcquireH1`/`PickEndpoint` Pick funnel the SPEC widens to extract the ctx hash key) + the two hash-input consumers `internal/filter/tcpproxy/filter.go` + `internal/filter/http/router/router.go`.
7. The phase-34/35 phase directories (`docs/envoy-go/phases/34-load-balancer-least-request/`, `…/35-load-balancer-random/`) — the family-template + the as-built precedents.
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_execution_style`, `feedback_git_worktrees`, `feedback_subagents_no_push`, `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_path_targeting`, `reference_differential_break_protocol_count1`, `reference_differential_asserter_dispatch`, `reference_differential_run_selector`, `reference_differential_band_sigma_margin`, `reference_differential_fixture_dispatch_constraint`, `reference_docker_probe_bridge_network`.

## Honor throughout

- `feedback_execution_style` (subagent-driven) · `feedback_git_worktrees` (work in a worktree) · `feedback_subagents_no_push` (subagents commit LOCAL-ONLY; the controller squash-merges + pushes at stage-close) · `feedback_push_to_origin` (after a clean landing on master with tests green, push without asking) · `feedback_pertask_gofmt_lint` · `feedback_subagent_worktree_path_targeting`.
- ADR-0106 (flat family row per LB policy — NO parent rollup) · ADR-0045 (the split-gate — the FINAL 36.1/36.2 decision at SPEC/PLAN) · ADR-0044 (ADR bodies at the IMPL, in-place) · ADR-0052 (the atomic-landing six-gate) · ADR-0080 (byte-stable parse-reject wording) · ADR-0227 (the contrib reference image; live probes per `reference_docker_probe_bridge_network`).
- The lifecycle is BRAINSTORM → SPEC → PLAN → IMPL, each its own session. The next session is the phase-36 **SPEC** (`superpowers:writing-plans` scoped to SPEC authoring — execute D-RH1..D-RH8; anchor the ADR-0235 + ADR-0236 §Context; make the FINAL ADR-0045 36.1/36.2 split decision).
