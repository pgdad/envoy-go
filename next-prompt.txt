RESUME a fresh session for envoy-go (router last updated 2026-06-27; the **phase 45.2 (operator engine) PLAN is DONE and LANDED** — `docs/envoy-go/phases/45-otlp-access-log/PLAN-45.2.md` authored direct-on-master [docs-only], modeled on `PLAN-45.1.md`, a 10-task TDD spine; plan-document-reviewer loop run — **1 SHOULD-FIX found + FIXED** (an empty `kvlist_value`/`array_value` would COLLAPSE to a `stringValue` in the engine's `Eval` because the value-arm discriminator switched on slice-non-nil-ness — a type-changing cross-side bug; fixed by an explicit `otlpValueKind` tag on `OTLPValueTemplate` + `make([]T,0,len)` slice init + a regression unit row) + 2 cosmetic line-number nits — re-reviewed **APPROVED**. The §12 D-OTLP-2-* PLAN questions are RESOLVED in the PLAN's D-question block. **The next action is the phase 45.2 (operator engine) IMPL** — the FINAL leg of the by-concern 2-leg OTLP split; its six-gate FLIPS ROADMAP row 45 → `done`.). HEAD is at the **next-prompt.txt roll commit** atop the **STATE.md + PLAN-45.2 docs commit** atop the prior next-prompt roll `c533a9d9` + the prior roll `51869c92` + the **phase-45.2 SPEC `b4767c43`** + the prior roll `ac21a13c` + the **phase-45.1 IMPL squash `07439d1f`**. Re-read the shas with `git log --oneline -14`, do NOT assume — any number of docs-only `next-prompt.txt:` commits may stack ATOP the PLAN. Counts at PLAN-done (the exit invariant — re-verify, do NOT assume; UNCHANGED from the SPEC since the PLAN is docs-only): stat surface **1191** (H2 cluster; non-H2 **1187**) / fixtures **86** (`ls -d test/fixtures/[0-9][0-9][0-9][0-9]* | wc -l` == 86; tail `0084-otlp-access-log`) / fuzzers **45** (`grep -rh '^func Fuzz' --include='*.go' . | wc -l` == 45) / BackendKind tail **38** (UNCHANGED — the OTLP receiver is driver-owned) / DECISIONS tail **ADR-0258**, next-free **ADR-0259**.

**ROW 44 (`grpc-access-log`) is `done`**; **ROW 45 (`otlp-access-log`) is `in-progress`** (the 45.1 core leg + the 45.2 SPEC + the 45.2 PLAN all LANDED; the 45.2 IMPL is the FINAL leg — **row 45 flips `done` at the 45.2 IMPL** per per-leg ADR-0106 + `reference_roadmap_split_phase_row_done`). The **Observability FAMILY STAYS OPEN** (OTLP access log [active — 45.2 IMPL remains] / OTel-Zipkin tracing / stats sinks / tap filter remain rows). **The active action is the phase 45.2 (operator engine) IMPL** — execute the 10-task TDD spine in `PLAN-45.2.md` subagent-driven (`superpowers:subagent-driven-development` per `feedback_execution_style`) in a FRESH worktree off master (`feedback_git_worktrees`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the controller squashes + pushes at stage-close (`feedback_push_to_origin`). ANCHORS **ADR-0259**.

First confirm state via `git log --oneline -14` (expect HEAD at a next-prompt.txt roll over the STATE+PLAN-45.2 docs commit over the prior roll `c533a9d9` over the phase-45.2 SPEC `b4767c43` over the phase-45.1 IMPL squash `07439d1f`; counts as above; re-read the shas, do NOT assume).

## NEXT STEP — the phase-45.2 (operator engine) IMPL
This is an IMPL stage — subagent-driven in a fresh worktree off master (`feedback_git_worktrees` · `feedback_execution_style` · `feedback_subagents_no_push` · `feedback_subagent_worktree_detach` + `feedback_subagent_worktree_path_targeting` · `feedback_pertask_gofmt_lint`). The deliverable is the OTLP access-log operator engine built per `PLAN-45.2.md` (the 10-task spine + the Final-review/handoff section). The spine:
1. **T1** PROGRESS-45.2.md scaffold + baselines (86 fixtures / 45 fuzzers / 38 BackendKind / ADR-0258) + the ADR-0045 split re-check (D-OTLP-2-SPLIT-FINAL, ≈270 prod LoC, ONE FINAL leg).
2. **T2** the operator engine `internal/accesslog/otlpformat.go` (the registry of curated `Record`-mapped operators reusing `format.go`'s extractor semantics + `orDash`/`orEmptyDash` [no file-sink `escape()`] · `CompileOTLPTemplate` `%…%` scanner [`%%` literal, STRICT-REJECT unknown ops] · `CompileOTLPValue` `AnyValue`-tree walker [the EXPLICIT `otlpValueKind` tag — string/kvlist/array; STRICT-REJECT int/bool/double/bytes at any depth] · `ValidateOTLPValue` [resource_attributes type-only walk, NO operator scan — literal pass-through] · `Eval` [per-record substitute, switch on `kind`]) [TDD table-driven incl. the empty-`kvlist`/`array` round-trip regression row].
3. **T3** the operator-engine fuzzer `FuzzCompileOTLPValue` (D-OTLP-2-FUZZER; 45 → 46).
4. **T4** the config-parse LIFT (`internal/bootstrap`) — REMOVE the body/attributes/resource_attributes reject arms (`bootstrap.go:480-488`; the formatters reject `:489` STAYS); bootstrap COMPILES at parse via the `accesslog` helpers (the NEW acyclic `bootstrap → accesslog` edge, D-OTLP-2-COMPILE-SITE) + the value-type reject; the `OTLPConfig.{Body *accesslog.OTLPValueTemplate, Attributes []accesslog.OTLPAttrTemplate, ResourceAttributes []*commonpb.KeyValue}` fields + the `commonpb` import [TDD — FLIP the 45.1 reject-body/attributes/resource_attributes tests to ACCEPT + ADD reject-unknown-operator + reject-bad-value-type].
5. **T5** the `buildLogRecord`/`buildResource`/`buildExportRequest` extensions (body/attributes substitution + the resource_attributes append SURVIVING `disable_builtin_labels`, AMEND-OPS-5) [TDD table-driven incl. nested kvlist/array].
6. **T6** the sink template threading + the grown `NewOTLPAccessLogSink` signature (per-record `Eval` at buffer-append, the resource build at flush) [TDD, full-package `-race` per `reference_full_suite_race_after_background_mutator`].
7. **T7** the main wiring (thread `cfg.Body`/`cfg.Attributes`/`cfg.ResourceAttributes`).
8. **T8** the `0085-otlp-access-log-operators` differential (cross-side EXACT on the deterministic operators only — query-less path, AMEND-OPS-6; the templated body + nested-kvlist/array attributes + the static + literal-`%REQ(:AUTHORITY)%`-pass-through resource_attributes) + the `disable_builtin_labels`/unsupported-operator/bad-value-type SUBJECT unit tests (NOT extra fixture dirs — `reference_differential_fixture_dispatch_constraint`); use `-run 'TestDifferential/0085'` (`reference_differential_run_selector`).
9. **T9** `0085` deliberate breaks (`-count=1`, `reference_differential_break_protocol_count1`) + 20/20 flake + full-package `-race`.
10. **T10** full 87-dir + six-gate + ADR-0259 body (§Context drafted at SPEC §13 — promote PROPOSED → ACCEPTED) + BEHAVIOR_CONTRACT + STATE/ROADMAP (**row 45 → `done`**) + the fuzzer-count reconcile (`reference_fuzzer_count_docs_drift`, 45 → 46).
Then the Final-review/handoff: the controller SQUASHES the worktree branch into ONE atomic commit + verifies the main checkout clean + **pushes to origin** + removes the worktree; THEN roll this router to the next Observability-family BRAINSTORM (row 45 is now `done`; the family stays open).

Anticipated 45.2-IMPL counts: stat **1191 UNCHANGED** (the operator engine adds no stat) / fixtures **87** (`0085`) / fuzzers **46** / BackendKind **38** / DECISIONS **ADR-0259**; ZERO new packages + ZERO new go.mod modules (`go mod tidy -diff` EMPTY).

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** This stage is the phase-45.2 IMPL (subagent-driven in a fresh worktree per `feedback_execution_style` · `feedback_git_worktrees` · `feedback_subagents_no_push` [subagents commit locally; controller squashes + pushes at stage-close] · `feedback_subagent_worktree_detach` + `feedback_subagent_worktree_path_targeting` · `feedback_pertask_gofmt_lint` · `feedback_push_to_origin`).
- **Process anchors:** ADR-0106 (per-leg roadmap rows; row 45 flips `done` at THIS IMPL) · ADR-0045 (split-gate) · ADR-0041/0080 (silent-ignore vs strict-reject) · ADR-0016 (proto blank-import registry) · ADR-0255/0256/0257 (44.x as-built) · **ADR-0258** (45.1 OTLP core, LANDED) · next-free **ADR-0259** (45.2 operator engine — §Context drafted in `SPEC-45.2.md` §13; body lands at THIS IMPL).
- **45.1 as-built seams the 45.2 IMPL builds on (re-confirm line numbers — files evolve):** `internal/accesslog/format.go` (`Default()` + `orDash`/`orEmptyDash` — the operator SEMANTICS source, NOT a reusable engine) · `internal/accesslog/accesslog.go` (`Record` `:29` — the 10 scalar fields) · `internal/accesslog/otlpmapping.go` (`buildLogRecord` `:20` / `buildResource` `:28` / `buildExportRequest` `:46` — EXTEND) · `internal/accesslog/otlpsink.go` (`OTLPAccessLogSink` `:42`; `NewOTLPAccessLogSink` `:66`; `buildExportRequest` call `:158`; `buildLogRecord` `:188`; `proto.Size` `:190`) · `internal/bootstrap/bootstrap.go` (`OTLPConfig` `:217`; `parseOpenTelemetryAccessLog` `:475` — the reject arms `:480-488` to LIFT, `:489` formatters STAYS) · `internal/filter/hcm/accesslog_emit.go` (`:26` H1 query-strip; `:53` H2 query-kept — AMEND-OPS-6) · `test/helpers/otlplogs/otlplogs.go` (`Records()`+`ResourceAttributes()` — suffice as-is) · `test/fixtures/0084-otlp-access-log/` (the differential precedent to copy for `0085`).

## Read first (in order)
1. THIS FILE — the cold-start router (the 45.2 PLAN is DONE + committed; the next action is the 45.2 operator-engine IMPL — subagent-driven, fresh worktree).
2. `docs/envoy-go/phases/45-otlp-access-log/PLAN-45.2.md` — THE deliverable spine (the Orientation brief + the D-question resolutions + the 10-task TDD spine + the Final-review/handoff).
3. `docs/envoy-go/phases/45-otlp-access-log/SPEC-45.2.md` — the chartered operator-engine leg (the §11 D-OTLP-2-* live-probe PINS [AMEND-OPS-1..6] + the §3 design + the §13 ADR-0259 §Context draft).
4. `docs/envoy-go/phases/45-otlp-access-log/PROGRESS-45.1.md` + `PLAN-45.1.md` — the core-leg as-built (the seams the 45.2 IMPL extends + the house spine shape).
5. `docs/envoy-go/DECISIONS.md` — **ADR-0258** (the 45.1 as-built, LANDED) + next-free **ADR-0259**.
6. `docs/envoy-go/STATE.md` — the active-phase header (`phase 45.2 (otlp-access-log operator engine) PLAN done`) + the recorded NEXT (the 45.2 IMPL).
7. `docs/envoy-go/ROADMAP.md` — row 45 (`otlp-access-log`, `in-progress`) + the `### Observability family` heading.
8. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the `### Access log — OpenTelemetry (OTLP) access-log sink` subsection (the 45.1 contract 45.2 extends) + the stat-surface block (1191).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `reference_streaming_sink_differential_framing`, `reference_differential_grpc_receiver_driver_owned`, `reference_strict_reject_sibling_typeurl_gap`, `reference_differential_fixture_dispatch_constraint`, `reference_differential_run_selector`, `reference_differential_break_protocol_count1`, `reference_full_suite_race_after_background_mutator`, `reference_fuzzer_count_docs_drift`, `feedback_pertask_gofmt_lint`, `feedback_execution_style`, `feedback_git_worktrees`, `feedback_subagents_no_push`, `feedback_subagent_worktree_detach`, `feedback_subagent_worktree_path_targeting`).
