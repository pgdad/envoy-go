=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

**THERE IS BANKED WORK AT THIS TIP.** Phase 77 (`runtime-static-layer`) is BRAINSTORMED, **SPEC'd and PLAN'd**, and **row 77 is registered `in-progress`**. **This session performs the phase-77 IMPL** — it executes `docs/envoy-go/phases/77-runtime-static-layer/PLAN.md` task-by-task. Do NOT self-pick a new subject.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     REWRITTEN AT THE PHASE-77 PLAN. The old REGEX form was FAIL-UNSAFE -- see
#     the section-(1) note below. This form parses FIELDS instead of matching a
#     pattern, and asserts the row DENOMINATOR so a deleted row is itself an error.
awk -F'|' -v want=109 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form. See the section-(2) note below.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **`want=109` IS A LIVE FIGURE, NOT A CONSTANT.** It is the ROADMAP data-row count at the phase-77 PLAN tip. **A row-adding phase MUST bump it in the same commit that adds the row** — otherwise every later session gets `GATE FAIL: examined 110 data rows, expected 109`. That noise is deliberate: a wrong denominator must be loud, because a silent one is how check (1) failed in the first place.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-RUN MECHANICALLY 2026-07-27 at the phase-77 PLAN. It does NOT fire. `stop` was NOT created and MUST NOT be.** ⚠️ **Re-run it yourself anyway and record what YOU see** — a prior run's result is not evidence for the current tip. ACTUAL output, recorded rather than predicted:
- **(1) `NOT DONE: row 77`** — live since the BRAINSTORM; **it is silenced at the phase-77 IMPL**, which is precisely when a false "done" would matter most.
- **(2) FOUR under the old matcher, FIVE under the broadened one above.** ⚠️ **BOTH ARE CORRECT — do NOT "fix" either count down.** Sites: `:187`, `:197`, `:207`, `:213` (long form) and **`:221`** (the Operational-tooling short form).
- **(3) `NEVER OPENED: gRPC` and `NEVER OPENED: WASM`** — `Runtime` is legitimately GONE, cleared by row 77. (The `WASM` line remains a ROADMAP **bookkeeping artifact** — `internal/wasm/` is a substantially-built wazero host and rows 25/25.1/25.2/25.3 are all `done` — deliberately LEFT AS-IS.)

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`.** Every document from phase 75 forward recorded its blind spot — **109 data rows / 105 matched / FOUR misses** at `:31` (an **em-dash** in the "after" cell), `:35` (a **DOT** in the slug `http-1.1`), `:83`/`:84` (**LETTER-suffixed** ids `28.1a`/`28.1b`) — and every one of them called it *"currently HARMLESS, since all four missed rows are `done`."*

⚠️ **Nobody fired the unsafe arm until the phase-77 PLAN. EXECUTED on scratch copies:**

| arm | old check-(1) output |
|---|---|
| flip a normal-shaped row to `in-progress` | names it — RED |
| flip row `00` to `in-progress` | **INVISIBLE** |
| flip row `04` to `in-progress` | **INVISIBLE** |
| flip `28.1a` + `28.1b` to `in-progress` | **INVISIBLE** |
| **all four `in-progress`, row 77 `done`** | **(empty)** ⚠️ **`stop` WOULD BE CREATED with FOUR not-done rows on the books** |

**Check (3) fails safe by construction. Check (2)'s blind spot was found and fixed at the phase-77 BRAINSTORM. Check (1)'s is the one nobody re-derived — and it is the one that fails UNSAFE.** A false "done" ends the project early.

**The replacement above was armed on all five arms** (each of the four blind-spot rows named correctly; a deleted row ⇒ `GATE FAIL: examined 108 … expected 109`). **The fix is at the MATCHER, not at the prose** — no `ROADMAP.md` edit, so it surfaces the class and the instances at once.

⚠️ **DO NOT restore the regex form.** If you want to compare, run both — but the field-parsed one is the gate.

## ⚠️ THE SECTION-(2) NOTE — CHECK (2) WAS ALSO FAILING UNSAFE, AND WHAT ITS NUMBERS NOW MEAN

**The old check (2) matched ONLY the long phrase.** The **Operational-tooling** family records three genuinely un-chartered items — xDS-sourced dry-validation · an admin-API-exposed live-reload-and-validate endpoint · an RTDS/SDS validate companion — in the **SHORT** form, in the *same paragraph* that reads `**The family STAYS OPEN**`. The old matcher **could not see them**. Fixed at the phase-77 BRAINSTORM, at the MATCHER; negative controls all EXECUTED: a `candidates were:` recap line ⇒ **silent** · a short-form-only line ⇒ **1** · an empty file ⇒ **silent**.

⚠️ **Normalising that Operational-tooling paragraph to the long form is a NAMED, DELIBERATELY-UNTAKEN follow-up.**

⚠️ **THE DOCUMENTED TRAP'S STATED MECHANISM IS REFUTED, WHILE THE TRAP IS REAL.** `reference_sentinel_deferred_sentence_live_vs_historical` says a naive `grep -c 'candidates:'` inflates *because of* the `candidates were:` recaps. It does inflate — but **not for that reason**: `candidates were:` does not contain that substring **at all**. The inflation comes from **ROADMAP table rows carrying BRAINSTORM prose**. **Cite the command; do not re-inherit the explanation.**

⚠️ **AND THE MEASURED RECORD: CHECK (2) HAS NEVER GONE DOWN** across ~17 phases (history `0 → 1 → 3`, then `→ 4` when row 77 opened the Runtime family). **Phase 74 landed three `ssl.*` stat names and made its OWN sentence LONGER**; **phase 73 predicted a clause REMOVAL and got a clause REPLACEMENT.** ⇒ **the candidates sentence is a WINDOW onto a larger deferred backlog, not an inventory of it.**

⚠️ **NEVER WRITE A SENTINEL'S OWN MATCHER STRING INTO A FILE THE SENTINEL GREPS.** The sentinel greps **`docs/envoy-go/ROADMAP.md`** — writing these strings *here* is safe; writing them *there* is not. This fired LIVE at the phase-76 BRAINSTORM, twice in one commit. **`grep` cannot tell a mention from a use.** ⚠️ **The phase-77 IMPL EDITS `ROADMAP.md` (row 77 → `done`), so re-run ALL THREE checks AFTER your edits land.**

⚠️ **THE PRIOR ROUTER'S SELECTION CALCULUS IS REFUTED AS ARITHMETIC.** *"A candidate that clears a check-(3) blocker is worth **strictly more**"* does **not** hold: opening a family **MINTS** a check-(2) sentence, so the item count went **3 never-opened + 3 sentences → 2 + 4 = 6 → 6, NET ZERO.** What survives, and what actually justified the phase-77 pick: **opening a family is the ONLY way that family's work becomes enumerable at all — a prerequisite, not a shortcut.** Do not restore the stronger claim.

==============================================


## STATUS AS OF 2026-07-27: PHASE 77 IS **PLAN'd**. ROW 77 IS `in-progress`. THIS SESSION PERFORMS THE **PHASE-77 IMPL**.

**What the phase-77 PLAN landed** (`git log --grep 'phase 77'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): `docs/envoy-go/phases/77-runtime-static-layer/PLAN.md` (**2208 lines, TWELVE tasks**) and `PROGRESS.md`. File set: `PLAN.md` + `PROGRESS.md` + `STATE.md` + `next-prompt.txt`. **`ROADMAP.md`, `BEHAVIOR_CONTRACT.md` and `DECISIONS.md` ALL BYTE-UNTOUCHED; ZERO `.go` files changed.** Three agents on disjoint remits with PRIVATE scratch — one LIVE reference probe fleet (**43 fresh containers**), one Go-execution agent in a throwaway worktree restored to an EMPTY porcelain, one read-only gate/anchor agent — plus four read-only surveys and controller re-derivation. **THREE of the controller's OWN predictions were REFUTED.**

### What matters for the NEXT session, in order

1. **⚠️ RE-RUN ALL THREE SENTINEL CHECKS YOURSELF AND RECORD THE ACTUAL OUTPUT; DO NOT PREDICT IT.** Create `stop` if and ONLY if all three print nothing. **The IMPL EDITS `ROADMAP.md`, so run them TWICE — at session open and after the edit lands.** After row 77 flips, check (1) goes **SILENT**; checks (2) and (3) are what still block `stop`.

2. **⚠️ THE PLAN IS THE PRIMARY INPUT AND IT CORRECTS THE SPEC IN THREE PLACES.** Read `PLAN.md` §1 before anything else. The three: SPEC §8.3's discrimination instruction is **unachievable** (§1.1) · SPEC §5's collision check **missed a declaration** (§1.4) · SPEC §10 T7's guard class is **blind to renames** (§1.7). ⚠️ **A FOURTH "correction" was published and then WITHDRAWN** — see item 16; the PLAN's own §1.9 records it as a worked example of a refutation that answered the wrong question.

3. **⚠️ THE BREAK ROSTER IS SPLIT BY LAYER, AND THE FIXTURE IS NOT A BREAK DETECTOR.** `num_keys` cannot identify which arm broke — a broken recursion collapses the nested arm **and** the empty-struct arm at once (both ⇒ **4**), and two further counterfactuals (case-insensitive, pair-match) **do not move it at all**. Six discriminating breaks live in `internal/runtime/snapshot_test.go` on the **KEY SET**; the fixture keeps F1 (registration ABSENCE), F2 (TRANSPOSITION), F3/F4 (the two silent-green failures) and F5 (a wrong-`want` liveness proof). ⚠️ **U5 and U6 leave `num_keys` at 6 — the FIXTURE MUST STAY GREEN under them.** If it goes red, something else broke.

4. **⚠️ T4 IS ATOMIC AND MUST NOT BE SPLIT** (`reference_lifted_reject_hidden_enforcement`). The pre-check is a SINGLE guard in front of FOUR oneof arms; **all three siblings unmarshal `err = <nil>`** (EXECUTED). And **both** guarding tests — `internal/bootstrap/bootstrap_test.go:82-96` **and** `validate/validate_test.go:65-79` — use the fixture `name: static_layer` + `static_layer: {}`, **exactly the arm the row legalizes**, so both flip to `err == nil` and die at `t.Fatal`. Lift + roster + both test replacements = **ONE commit**.

5. **⚠️ THE REFERENCE-SIDE NUMBERS ARE MEASURED — `num_keys=6`, `num_layers=2`**, identical over 3 fresh boots, with four isolation arms summing exactly (1+2+1+2) and two controls bracketing the readout (P-single 1/1, P-baseline 0/0). Zero boot failures in 43 boots. Line shape: **no labels** — `envoy_runtime_num_keys{} 6`, each sample preceded by its own `# TYPE` line. ⚠️ **`runtime.load_success` and `runtime.override_dir_not_exists` read `1` even with NO `layered_runtime` block**, so **neither is a "a static layer loaded" precondition** — the separate ABSENT check is the honest substitute.

6. **⚠️ FOUR GO-SIDE FACTS THAT BITE IF DRAFTED FROM MEMORY.** The oneof wrapper names are **ASYMMETRIC** — `RuntimeLayer_StaticLayer` (no trailing underscore) vs `RuntimeLayer_DiskLayer_` / `_AdminLayer_` / `_RtdsLayer_` (trailing underscore), and the un-suffixed forms are the nested **MESSAGE** types ⇒ `impossible type switch case`. An **unset oneof is a nil INTERFACE** and takes `default`, so `case nil:` must be explicit or arm 4 mislabels it. **`layered_runtime: {}` and `{layers: []}` are INDISTINGUISHABLE after unmarshal** ⇒ arm 9 is ONE predicate, TWO test rows. A YAML `null` is a **non-nil** `*structpb.Value` ⇒ switch on `GetKind()`, never `v == nil`.

7. **⚠️ THE FLATTENING RULE IS LEXICAL AND HAS TWO TERMINATION BRANCHES.** `numerator` OR `denominator`, lowercase, exact, **case-sensitive**, **either alone**, **values never inspected**; plus **the EMPTY STRUCT is a counted LEAF**. ⚠️ **An implementation that parses a FractionalPercent here REJECTS configs the reference ACCEPTS.** Field count is irrelevant — `{foo: 1}` recurses while `{numerator: 25}` terminates.

8. **⚠️ THE BROKEN-GATE COUNT IS ELEVEN, THREE OF THEM FOUND AT THIS PLAN.** A sha256 roster's naive `[ -f ] || continue` **exits 0 on a DELETED file** · the landed `countMetrics` stat idiom is **blind to a rename** at constant count (proved: two in, two out, both names wrong ⇒ PASS) · a **`+0 exported symbols` gate over an EMPTY package goes RED on a CORRECT tree** (a stray newline against a 0-byte baseline). Plus one **sharpened**: the `go doc` fail-open is **not** *"`./` prefix"* (`./nonexistent` alone exits **1** — REFUTED); it is that **with a valid arg1, ANY arg2 is silently swallowed** (byte-length proof: runtime alone 285, runtime+bootstrap 285, bootstrap alone 19185). And the standing ones: `gofmt -l` **NEVER exits non-zero** (gate on OUTPUT) · a **RANGE** gate cannot detect anchor drift (content-anchor instead). ⚠️ **`internal/bootstrap/**`, `internal/runtime/**` and `validate/**` must NOT be byte-gated for this row** — the roster is **977** after the set-difference (universe 990 − edit 13). ⚠️ **Re-negative-control every gate AT THE IMPL TIP**; a control observed at the PLAN is a citation, not evidence.

9. **⚠️ A HARNESS'S EXIT CODE IS NOT THE COMMAND'S EXIT CODE.** At the phase-76 IMPL a background-task notification for a `-race` full suite reported *"exit code 0"*; that was the **WRAPPER shell's** status. The logged inner status was **1**, and the run had **PANICKED** at 84 of 119 subtests. **Capture the inner status explicitly** (`cmd; echo "EXIT=$?" >> log`) and **derive the PASS/FAIL tally from the log.**

10. **⚠️ THE FIXTURE'S THREE REGISTRATION GATES AND TWO SILENT-GREEN FAILURES.** `0118-runtime-static-layer` on port **`10118`** (family-banded `10<index>`, **not** 10450 — that is the TLS/SDS band and this is not a TLS fixture). The three gates: the `NNNN-…` directory · `RegisterFixture` with the name **equal to the directory name** · **a BLANK IMPORT in `runner_test.go`** (currently **119**, insert after `:144`). ⚠️ **A bare `-run 'TestDifferential/0118'` on a tree without the blank import prints `[no tests to run]` and EXITS 0** — use the guarded form in PLAN §4 G5. ⚠️ **The `StatsAsserter` dispatch is a SILENT type assertion with no `else`** (`runner_test.go:1347-1349`, **first addr = REFERENCE**) — a signature typo makes the leg **vanish green** while the compiler, `go vet` and `golangci-lint` all stay quiet, so grep the run for the driver's own `log.Printf` line to prove it RAN. ⚠️ **`BackendCount()` must be ≥1** (`runner_test.go:240-243`); `return 1` + default `TCPEcho`. ⚠️ **`fixture.TB` has NO `Logf`** — use `log.Printf`. ⚠️ **`expectations.yaml` is 100% comment prose read by ZERO Go code** (ADR-0019).

11. **⚠️ ASSERT THE DELTA, NOT THE TOTAL; AND THE NAME SET, NOT THE COUNT.** `BEHAVIOR_CONTRACT.md`'s ledger has **TWO** discontinuities and the `1200 → 1201` one is documented **nowhere in the file**, so the absolute `1205 → 1207` rides an unaudited gap. And a count-only stat guard passes a build with **both names wrong**.

12. **⚠️ ANCHORS DRIFT UNDER A STAGE'S OWN EDITS.** Re-derive every count and cite at the tip that reads it. ⚠️ **Self-falsifying counts remain the lineage's most persistent defect species** — never write a token into the file whose occurrences of that token you are counting. ⚠️ **The ADR-0288 singleton greps over `STATE.md` do NOT all return the same number** — anchor on `^- \*\*<field>`, not bare (verified at this PLAN: anchored **1** for all seven fields; bare ranges 1-8).

13. **⚠️ A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX — the SIXTH consecutive stage at which this held.** Live hazards: the full-suite startup flake (`subject ready: EOF` **and** `bind: address already in use`, both failing **BEFORE any assertion**, the latter as a **PANIC that aborts the whole binary**, firing **MORE readily under `-race`** and more readily as the fixture count grows — this row adds the 120th) · `reference_sds_init_fetch_timeout_dial_budget_flake` · the PRE-EXISTING `internal/cluster` `-race` outlier flake · two still-**UNINDEXED** load flakes (`internal/httpclient TestOptions_ZeroValue_NoOpDefaults`, `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery`). **Isolate-re-run, then state the classification AND its evidence.** ⚠️ **`0061-lb-ring-hash` is NO LONGER a live flake** — a spread failure there is now a **FINDING**.

14. **Counts at exit (re-run MECHANICALLY in the worktree; never copy them):** fixtures **119 → 120** (`ls -d test/fixtures/[0-9]*/ | wc -l`; ⚠️ that glob is **NOT** equivalent to `discoverFixtures` — the faithful predicate is `^[0-9]{4}[a-z]?-`) / fuzzers **55, +0** (a seed is not a `func Fuzz`) / **stat surface 1205 → 1207**, ⚠️ **assert the DELTA** / **BackendKind tail 38** (⚠️ a TAIL VALUE — the file declares **39** constants, 0-38; **do NOT "fix" 38 to 39**) / go.mod modules **2** (⚠️ the phase-61.2 lineage figure — **NOT a repo total**; the single `go.mod` requires **67** = 18 direct + 49 indirect in **TWO** `require` blocks; **do NOT "fix" 2 to 67**) / internal packages **73, +0** (`internal/runtime` already exists) / blank imports in `runner_test.go` **119 → 120** / DECISIONS tail **ADR-0299 PROPOSED → COMPLETE** (next-free **ADR-0300**) / next-free fixture index **0119**.

15. **⚠️ TWO DOCUMENTARY DEFECTS, NAMED AS DEFERRALS AND DELIBERATELY NOT FIXED.** Do not silently absorb either.
    - **The documented PUBLIC IMPORT PATH DOES NOT EXIST — and it is worse than a typo.** `head -1 go.mod` is `github.com/pgdad/envoy-go`; the docs say `github.com/esalaine/envoy-go/validate` on **20 lines / 24 occurrences** across four docs (DECISIONS **11**, ROADMAP **4**, STATE_HISTORY **3**, BEHAVIOR_CONTRACT **2**; STATE.md **0**). ⚠️ **`DECISIONS.md:142` is `## ADR-0006: module path github.com/esalaine/envoy-go` — an ADR that DECIDES the wrong path, never superseded.** A session copying it writes code that does not compile.
    - **The `1200 → 1201` stat-ledger step in `BEHAVIOR_CONTRACT.md`, documented NOWHERE in that file** (the `1198 → 1200` one is explained at `:732` but not in ledger form, so a ledger-form sweep cannot see it). A mechanical stat-surface count is owed — **8-11 tasks**, not the small row it sounds like.

16. **⚠️ `BEHAVIOR_CONTRACT.md:1857`'s TWO STALE CITES ARE REAL — AND THE PHASE-77 PLAN'S FIRST PASS WRONGLY "REFUTED" THEM.** The claim's `+20` is **the SIZE OF A DRIFT, not a token in the document**; the first probe grepped for the literal string `+20`, found only phase enumerations, and reported "no such anchor exists". **That was a category error and it was published before being caught.** RE-DERIVED by resolving each cite against the code it names: `internal/listener/manager.go:1078-1082`, cited as the accept-loop `kindQUIC` `continue`, is actually the **bind-failure UNWIND block** (real site **`:1098`**); `:1044-1054`, cited as the bind loop, is a **doc comment** (real site **`:1064`**). **Both drifted +20**, and the paragraph they support is a **settled, probe-backed QUIC PARITY determination** that explicitly warns against re-opening it. `quic.go:84-85` and `:109` in the same paragraph still resolve. **Fix with SYMBOL anchors** (T9 Step 4). ⚠️ The *"plus two more in the same file"* half is **UNVERIFIED** — do not report it either way. ⚠️ **THE TRANSFERABLE LESSON: a REFUTATION MUST ANSWER THE CLAIM AS STATED.** Grepping a literal token does not test a claim about drift. An agent's confident refutation, arriving with commands and outputs attached, is exactly as much a claim as the thing it refutes.

17. **⚠️ TWO STALE CITES THE IMPL DISPOSES OF (T10).** `DECISIONS.md:16430` — ADR-0268's **BODY** line (heading `:16418`) records `validate/validate_test.go`'s reject-arm roster *including `layered_runtime`*, which T4 falsifies. `DECISIONS.md:16672` — ADR-0278's **`**§Context.**` BODY** line (heading **`:16670`**, ⚠️ **not `:16672`**) cites **`bootstrap.go:499`** for the pre-check while `ROADMAP.md:139` cites `:568-569`; **the actual site is `:568-570`.** Replace with a **symbol anchor** so it cannot drift again. **Heading-vs-body is a live confusion class in this file's own cites: `grep -n '^## ADR-NNNN'` for a heading, and say "body line" when you mean one.**

18. **The BANKED next subject, for whenever phase 77 closes: `fault.abort.grpc_status`, ~7-9 tasks / +0 on EVERY envelope axis.** Its current state is **worse than "ignored"** — `abortEnabled` is set only inside the `HttpStatus` type switch, so a `grpc_status` variant fires **no abort at all** and the request reaches the backend. A 4-arm codec×content-type probe found the reference branches on the request **`content-type`** not the codec, returns HTTP **200** in every arm, and emits `grpc-status`/`grpc-message` as response **HEADERS not trailers** — which **RETIRES the framework blocker**. ⚠️ **It is an HTTP-filters-family row and does NOT open the gRPC family**; the provenance of the contrary claim is a **ROUTING LABEL** in row 09's summary, and **reading a filing label as a charter is the error.** ⚠️ `reference_deferred_candidate_cost_restale`: **re-derive its cost at the adopting tip.**

## What THIS session does — the phase-77 IMPL

One stage per session. **Execute `docs/envoy-go/phases/77-runtime-static-layer/PLAN.md`, task-by-task, red-first, T1 → T12.** Append the `# IMPL record` half to `PROGRESS.md`. Land ADR-0299's §Decision + §Consequences **IN PLACE after the RETAINED italic footer** (`DECISIONS.md:17494` at the PLAN tip; **no renumber, NO `---` separator** — the convention was abandoned and the file's last `^---$` is `:17020`, ~440 lines before ADR-0298 begins). Flip **row 77 (`ROADMAP.md:139`) → `done`**. Roll `STATE.md` and this file.

⚠️ **`ADR-0089` stays BYTE-UNTOUCHED and that is deliberate** — it defers `/runtime` (`DECISIONS.md:3550`) and `POST /runtime_modify` (`:3543`) to this family, and row 77 lands **neither**, so the un-defer is owed only by whichever later row does.

⚠️ **ADR-0299 carries NO whole-file grep count** — that species self-falsified in ADR-0296 ¶3 and ADR-0297 ¶7 **and** ¶9, and at the phase-76 BRAINSTORM it escalated from a wrong number to a flipped termination-sentinel check. Every count line-scoped or stated with no numeral. **§Consequences must record that the SPEC's own per-arm-count discrimination instruction is unachievable, and why the roster moved to the unit layer.**

⚠️ **THIS ROW NARROWS NOTHING.** The Runtime family's candidates sentence (`ROADMAP.md:213`) lists RTDS, the two admin endpoints, disk layer, admin layer, the six `runtime_key` arms, the silent-IGNORE knobs, hot restart and graceful drain. Row 77 delivers **none** of them — it delivers the static layer, which the sentence does not name. **Check (2) stays 4 (old matcher) / 5 (broadened) through the IMPL.**

Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** (`reference_parallel_subagents_private_scratch`) — and ⚠️ **if two agents share ONE worktree, they must stage by EXPLICIT PATH; a `git commit -am` sweeps in a sibling's uncommitted work.** Per-task `gofmt` + `golangci-lint` on touched packages (`feedback_pertask_gofmt_lint`).

⚠️ **A THROWAWAY WORKTREE IS LEFT ON DISK FROM THE PLAN STAGE: `/home/esa/git/envoy-go-wt-p77probe`** (branch `p77-probe-throwaway`, EMPTY porcelain, nothing committed, the `bootstrap.go:568-570` pre-check restored verbatim). **Remove it before starting** — `git -C /home/esa/git/envoy-go worktree remove /home/esa/git/envoy-go-wt-p77probe`.

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. ⚠️ **THIS HAS NOW FIRED LIVE IN TEN CONSECUTIVE SESSIONS** — observed at this PLAN as `Shell cwd was reset to /home/esa/git/envoy-go` immediately after a `cd` into scratch. **Use `git -C <abs-worktree-path>` for every git command, prefer absolute paths over `cd`, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline — ⚠️ **anchor greps on `^- **<field>`**, not bare) + §Project counts + the §Recent lineage.
2. `docs/envoy-go/phases/77-runtime-static-layer/PLAN.md` — **THE PRIMARY INPUT.** §1 the re-derivation ledger (**§1.1** the unachievable discrimination instruction · **§1.2** the fail-unsafe sentinel · **§1.3** the measured reference numbers · **§1.4**–**§1.10** the other refutations) · §2 global constraints · §3 the file structure and the byte-gate set-difference · **Tasks 1-12** · §4 the eleven gates with their observed controls · §5 deferred (**+ the STRUCK item**) · §6 hazards · §7 the self-review against the SPEC · §8 operative memories.
3. `docs/envoy-go/phases/77-runtime-static-layer/PROGRESS.md` — what was EXECUTED at the PLAN, the three refuted controller predictions, and the controller self-correction.
4. `docs/envoy-go/phases/77-runtime-static-layer/SPEC.md` — **the design of record, with four PLAN corrections.** §1.1 the refuted founding premise · §1.2 the R1-R23 drift ledger · §3.3 the measured semantics · §6 the nine-arm roster · §8 the fixture · §14 the ADR-0299 shape. ⚠️ **Treat §8.3's break-discrimination instruction, §5's collision result and §10 T7's guard class as SUPERSEDED by PLAN §1.** ⚠️ **§13 item 8 STANDS** — the PLAN's first-pass strike of it was withdrawn (item 16).
5. `docs/envoy-go/phases/76-lb-ring-hash-spread-margin/PROGRESS.md` — the shape of an IMPL record in this lineage.
6. `docs/envoy-go/DECISIONS.md` — **ADR-0299** (`:17464`, PROPOSED, at the tail) · **ADR-0016** `:387` (unknown-field rejection) · **ADR-0089** `:3514` (deferrals `:3543`/`:3550`; stays BYTE-UNTOUCHED) · **ADR-0106** `:4788` (the six-gate) · **ADR-0187** `:11280` + **ADR-0195** `:12413` (the six `runtime_key` arms) · **ADR-0268** `:16418` (roster body `:16430`) · **ADR-0278** `:16670` (⚠️ **not `:16672`**) · ADR-0044 (⚠️ it does **NOT** contain the §Context-draft discipline — use `ADR-0044-as-used`) · ADR-0045 `:1466` · ADR-0288 `:16983`.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — especially `reference_lifted_reject_hidden_enforcement`, `reference_probe_must_discriminate`, `reference_gate_command_negative_control`, `reference_ordered_assertion_legs_vacuous_on_constant_change`, `reference_fatalf_makes_assertions_unreachable`, `reference_fixture_tb_has_no_logf`, `reference_differential_run_selector`, `reference_differential_fixture_three_registration_gates`, `reference_differential_asserter_dispatch`, `reference_spec_drafted_identifier_collision_check`, `reference_harness_exit_code_is_not_command_exit_code`, `reference_quoting_is_not_executing`, `feedback_brief_citations_not_evidence`, `reference_next_prompt_tracked_despite_gitignore`, `feedback_git_worktrees`, `feedback_execution_style`, and **`reference_bash_cwd_reset_commits_to_main`**.
