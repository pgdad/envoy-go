RESUME a fresh session for envoy-go (router last updated 2026-06-28; the **phase 46.1b (span emission + OTLP export) IMPL is DONE and LANDED** — squash `eef54c41`, fast-forwarded onto master + pushed to origin; the 15-task TDD spine executed subagent-driven in a fresh worktree [now removed]; the six-gate GREEN incl. the full 89-dir differential [one transient startup-flake on an UNRELATED fixture, isolate-re-run GREEN per `reference_differential_fullsuite_startup_flake`]; the final code-reviewer APPROVED merge-ready; ADR-0260 §Decision/§Consequences body LANDED [CLOSES the 46.1 leg]). **46.1b completed the 46.1 (core+OTLP) leg**: it consumed the 46.1a `tracing.Decision` to BUILD a per-request `ingress`/SERVER span (16 built-in STRING attrs; start at dispatch, end at the `accesslog_emit.go` stream-complete seam) and EXPORT sampled spans over the UNARY `TraceService.Export` via the reused 44.2/45.1 bounded-channel batching `OTLPExporter`. **The next action is the 46.2 SPEC** — the SECOND tracing exporter behind the `tracing.Exporter` seam: the **Zipkin provider + B3 propagation** (ANCHORS the next-free **ADR-0261**). **ROW 46 (`tracing`) STAYS `in-progress`** and the Observability FAMILY STAYS OPEN — the 46.2 IMPL is the FINAL chartered tracing leg, so its six-gate FLIPS row 46 → `done` (per ADR-0106 + `reference_roadmap_split_phase_row_done`) and MAY CLOSE the Observability family (if 46.2 is the final Observability row — confirm at the 46.2 SPEC against ROADMAP).

HEAD is the **46.1b IMPL squash `eef54c41`** atop the 46.1b PLAN landing `8f2e26a1` atop the 46.1a IMPL squash `b09dd404`. **Re-read the shas with `git log --oneline -8`, do NOT assume.** Confirm `git status` clean (the untracked `.claude/` is session cruft, leave it) + master == origin/master.

**As-built at 46.1b (the substrate 46.2 builds on — re-confirm line numbers before editing, files evolve):**
- `internal/tracing` is the home package — `decision.go` (46.1a `Decide`), `config.go` (`TracingConfig`), `propagation.go`/`requestid.go`/`rand.go` (46.1a W3C `traceparent`), `span.go` (`Span`/`KV{Key,Str,Int,IsInt}`/`SpanInputs`/`BuildServerSpan(d,in,start,end)`/`(*Span).toProto`), `exporter.go` (the `Exporter` interface `{Export(*Span); Close() error}` + `OTLPExporter` + the EXPORTED `TracesClient` structural seam + `tracesClientDialer` + `ExporterProvider` [per-cluster memoized, lazy `sync.Once` counter register, the unknown/non-H2 `log.Fatalf` boot-reject]), `stats.go` (`HCMCounters` [5 HCM-scoped] + `TracerCounters` [2 tracer-scoped, LAZY]).
- **The `Exporter` SEAM is the 46.2 extension point:** 46.2's Zipkin exporter is a SECOND `tracing.Exporter` impl (HTTP-JSON/proto to a Zipkin collector, NOT gRPC OTLP). The `ExporterProvider` currently builds ONLY OTLP exporters keyed by cluster; 46.2 must dispatch on the HCM `tracing` provider TYPE (OpenTelemetry vs Zipkin) — design the provider/seam split at the 46.2 SPEC.
- **B3 propagation** is the Zipkin analog of 46.1a's W3C `traceparent` engine (`propagation.go`): the `b3` single-header AND the multi-header `X-B3-{TraceId,SpanId,ParentSpanId,Sampled,Flags}` extract/continue/inject. 46.1a's `Decide` is propagation-format-agnostic at the Decision level but the EXTRACT/INJECT are W3C-specific — 46.2 adds the B3 codec.
- The span build is OTLP-proto-shaped (`toProto` → `tracepb.Span`); Zipkin's wire model is DIFFERENT (Zipkin v2 JSON span list) — 46.2 needs a Zipkin span encoder (NOT `toProto`). The 16-attr roster maps to Zipkin `tags`.
- **Counts at 46.1b IMPL-done (the 46.2 SPEC baseline — re-verify, do NOT assume):** stat surface **1198** (H2 cluster; non-H2 **1194**) / fixtures **89** (tail `0087-tracing-otlp`) / fuzzers **48** / BackendKind **38** / DECISIONS **ADR-0260** (CLOSED; next-free **ADR-0261**). ZERO new Go packages + ZERO new go.mod modules at 46.1b.
- **As-built 46.1b deviations the 46.2 SPEC should be aware of (recorded in ADR-0260 §Consequences):** (1) AMEND-TRACE-NO-BOOT-REJECT — `log.Fatalf` on a missing/non-H2 collector cluster (the Zipkin collector cluster gets the same treatment); (2) the EMPTY-VALUE span attrs `upstream_cluster`/`upstream_cluster.name`/`node_id`/`zone`/`peer.address` (framework gaps — the upstream cluster name + boot-node identity + downstream peer addr are not plumbed to the emit seam; `peer.address` IS available via `downstream.RemoteAddr()` as the cheapest future plumb); (3) all 16 attrs emitted as STRING (wire-parity); (4) the 404 no-route path has no span (Decide/inject run after route-match).

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** The next stage is the **46.2 SPEC** (charter the Zipkin provider + B3 propagation; ANCHOR ADR-0261 §Context; pin the provider-type dispatch + the Zipkin wire encoder + the B3 codec + the differential shape). Lifecycle-state 1 → 2.
- **Subagent-driven + worktrees** (`feedback_execution_style` · `feedback_git_worktrees`): the SPEC is a docs-stage (may be direct-on-master per the 46.1a/46.1b SPEC precedent, or a worktree if it grows code-adjacent probes); the IMPL that follows is subagent-driven in a fresh worktree.
- **Execution-chaos lesson** (`feedback_subagent_autocommit_claudemd`): dispatched subagents AUTO-COMMIT (global CLAUDE.md). Do NOT fight it — the CONTROLLER verifies EACH commit (correct fileset, real non-vacuous tests via `-v` + read assertions, gates green), cleans stray leak files, re-runs the FULL suite on the FINAL FROZEN HEAD, does the deliberate-break verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_subagents_no_push` · `feedback_push_to_origin`).
- **Per-task gates** (`feedback_pertask_gofmt_lint`): `gofmt -l` + `golangci-lint run` on touched pkgs + `go vet` + `go build ./...`.
- **Differential discipline:** `-run 'TestDifferential/<NNNN>'` NEVER bare `'<NNNN>'` (`reference_differential_run_selector`); `-count=1` on every break + `-race` run (`reference_differential_break_protocol_count1`); the exporter writer goroutine is a background mutator → FULL-package `-race` (`reference_full_suite_race_after_background_mutator`); poll-to-converge release barrier (`reference_concurrency_differential_release_barrier`); assert the aggregated PAYLOAD not framing (`reference_streaming_sink_differential_framing`); a driver-owned receiver is NOT a BackendKind (`reference_differential_grpc_receiver_driver_owned`); the Docker bridge + decode-ran proof (`reference_docker_probe_bridge_network`); the live-reference probe is the wire truth (`reference_wire_format_both_sides_see_same_bytes`).
- **Process anchors:** ADR-0106 (per-leg rows; row 46 `in-progress` until 46.2 IMPL) · ADR-0044 (ADR §Context-at-SPEC / §Decision+§Consequences-at-IMPL — ADR-0261 §Context drafts at the 46.2 SPEC) · ADR-0158 (the `grpcclient.Dialer` typed wrapper — N/A if Zipkin is HTTP not gRPC; confirm transport at the SPEC) · ADR-0080 (strict-reject) · ADR-0016 (proto blank-import) · next-free **ADR-0261**.

## Read first (in order)
1. THIS FILE — the cold-start router (the 46.1b IMPL is DONE + LANDED `eef54c41`; the next action is the 46.2 SPEC; row 46 `in-progress`).
2. `docs/envoy-go/phases/46-tracing/BRAINSTORM.md` — the parent phase-46 charter (the Zipkin/B3 leg scoping; the by-exporter split rationale).
3. `docs/envoy-go/phases/46-tracing/SPEC-46.1.md` + `PLAN-46.1a.md` + `PLAN-46.1b.md` — the 46.1 leg as-built (the engine + span + exporter the 46.2 Zipkin exporter parallels behind the `tracing.Exporter` seam).
4. `internal/tracing/` (the whole package) — the `Exporter` seam + `ExporterProvider` + `BuildServerSpan` + the propagation engine the 46.2 SPEC extends.
5. `docs/envoy-go/STATE.md` — the active-phase header (`phase 46.1b IMPL done`) + the recorded NEXT (the 46.2 SPEC) + the count figures (1198 / 89 / 48 / 38 / ADR-0260).
6. `docs/envoy-go/ROADMAP.md` — row 46 (`tracing`, `in-progress`, "46.1 COMPLETE; 46.2 [Zipkin+B3] next") + the `### Observability family` heading (confirm whether 46.2 is the FINAL Observability row).
7. `docs/envoy-go/DECISIONS.md` — **ADR-0260** (the 46.1 anchor, CLOSED) + next-free **ADR-0261** (the 46.2 Zipkin+B3 anchor — §Context drafts at the SPEC).
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `reference_phase_46_tracing_split`, `reference_tracing_upstream_cluster_framework_gap`, `reference_streaming_sink_differential_framing`, `reference_differential_grpc_receiver_driver_owned`, `reference_docker_probe_bridge_network`, `reference_concurrency_differential_release_barrier`, `reference_full_suite_race_after_background_mutator`, `reference_differential_fullsuite_startup_flake`, `reference_wire_format_both_sides_see_same_bytes`, `feedback_subagent_autocommit_claudemd`, `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `feedback_subagents_no_push`).
