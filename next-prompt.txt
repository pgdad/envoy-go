=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **PHASE 87 (`h2-double-slash-path-routing`) IS MID-LIFECYCLE: BRAINSTORM done (2026-08-12), SPEC done (2026-08-13), PLAN done (2026-08-13). ROW 87 IS `in-progress` AT `ROADMAP.md:149`, `want` IS 119.** ⇒ **THIS SESSION PERFORMS THE PHASE-87 IMPL — THE PHASE-CLOSING STAGE.** `STATE.md`'s `next-skill:` reads `IMPL for phase 87` — that IS your instruction. Do NOT re-pick a subject. **The PLAN decomposed D-87-SEQ into six ordered tasks T0-T5 (PLAN.md §2), ONE atomic commit, TDD held INSIDE it**, and re-proved BOTH RED anchors by execution at `0f015664` with failure lines read. ⚠️ **THIS IS THE STAGE THAT FLIPS ROW 87 `done`** (`numstat 1 1`, `want` STAYS 119, check (1) goes **SILENT** — the FOURTH silent reading in project history) **AND DISARMS THE STRICT `PROPOSED` GUARD 1 -> 0** by completing ADR-0309.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=119 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **`want` IS 119 AND STAYS 119 THROUGH THIS STAGE — the IMPL flips row 87's STATUS CELL, it does not add a row** (`numstat 1 1`). ⚠️ **RUN THE SENTINEL ON BOTH SIDES OF THE FLIP.** PRE-flip check (1) prints EXACTLY `NOT DONE: row 87`; POST-flip check (1) goes **SILENT** — and a silent check (1) is CORRECT AND EXPECTED AT THIS STAGE ONLY (it was a FINDING at the SPEC and the PLAN). Any OTHER row id appearing on either side is a FINDING. ⚠️ **THE SENTINEL STILL DOES NOT FIRE EVEN POST-FLIP:** check (2) prints SIX (at `:197 :203 :209 :219 :225 :233` — re-derive, the anchors drift with every edit) and the condition is a CONJUNCTION. Check (3) is SILENT.

⚠️ **THE DOCTORING NCs REMAIN MANDATORY EVERY SESSION — AND AT THIS STAGE THEY ARE THE ONLY THING PROVING CHECK (1) STILL LOOKS.** Both:
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md at want=119.
#   PRE-flip  expect: "NOT DONE: row 62" AND "NOT DONE: row 87"
#   POST-flip expect: "NOT DONE: row 62" ALONE -- the silent side still LOOKS. THIS IS THE
#   LOAD-BEARING NC OF THE WHOLE STAGE: without it a check (1) that had silently STOPPED
#   PARSING would be indistinguishable from a correctly-closed row (the phase-86 precedent).
```
```sh
# the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```

⚠️ **AND WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -oiE '-family row'` fails with `grep: amily row: No such file or directory` and the surrounding arithmetic prints `base=0 now=0 delta=0`, **which reads exactly like "no change".** Observed live at FOUR phase-84 stages. **Always pass `--` before the pattern**, and remember the counts are form-dependent: **95 occurrences vs 67 LINES** at this tip (`grep -c` counts LINES). **A gate that reads zero on BOTH sides is not evidence of invariance.** ⚠️ AND the carried **ARM-A {119, 131} figure binds ONLY with its escape-aware pipe-split command** (BRAINSTORM-86 §6) — ad-hoc field-count forms do NOT reproduce it (`NF != 8` flags 17 rows); do not "correct" the figure with a different form.

**SENTINEL RE-RUN MECHANICALLY AT THE PHASE-87 PLAN CLOSE (2026-08-13, base `0f015664`), ONE SIDE — a PLAN edits no ROADMAP (byte-untouched verified by EMPTY DIFF against master). IT DOES NOT FIRE; `stop` was NOT created** (verified absent at repo root AND stage worktree). ACTUAL output, recorded not predicted: input measured **237 lines / 119 data rows** · **(1) `NOT DONE: row 87`** alone at `want=119` (denominator silent) · **(2) SIX `:197 :203 :209 :219 :225 :233`** (⚠️ do NOT "fix" the six and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases; the candidates sentences are a WINDOW onto a larger deferred backlog, not an inventory of it) · **(3) SILENT**. The condition is a CONJUNCTION ⇒ the sentinel does NOT fire — and `STATE.md`'s `next-skill:` reads `IMPL for phase 87`, i.e. a next stage EXISTS and the loop continues.
- **ALL FOUR NCs FIRED AT THIS CLOSE:** row-62 doctoring => `NOT DONE: row 62` AND `NOT DONE: row 87` — with `NC LANDED? [ in-progress ]` inspected first; `want` off-by-one (`want=118` => `GATE FAIL: examined 119 data rows, expected 118`, plus the expected `NOT DONE: row 87`); check-(3) doctoring (residual confirmed **2 -> 0** on the doctored copy first) => `NEVER OPENED: gRPC` while `WASM` correctly stayed silent; check-(2) one-arm counts **5** (long arm alone) / **1** (short arm alone), union **6** (a one-arm strip is NOT an NC for the union).
- ⚠️ **Re-run all three checks and both doctoring NCs yourself and record what YOU see — BOTH SIDES of the row flip this time.**

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (ugrep under the hood). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Reproduced at TWENTY-ONE separate tips.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **THE METHOD BINDS; carry no standing discrepancy figure and no standing absolute.** ⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS; print FILE NAMES, never a count.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files; use `-v --no-index`, and `git status` is the discriminating check). ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **`grep -c` ON ZERO MATCHES IS TOOL-SPECIFIC** (only `git grep -c` prints nothing; all exit 1 — capture the value, never chain on the exit code). ⚠️ **`grep -c` COUNTS *LINES*, NOT *OCCURRENCES*.** ⚠️ **`\t` IN GNU ERE IS A LITERAL `t`** — use `-P`. ⚠️ **A `git grep` PATHSPEC ENDING IN `/` READS A FAIL-UNSAFE ZERO** (shape 33) — use the shell-glob form. ⚠️ **AN UNANCHORED PANIC-GREP FALSE-FIRES 14 TIMES ON A FULLY-GREEN DIFFERENTIAL LOG** — **the canonical gate is ANCHORED: `^panic:|DATA RACE|SIGSEGV`. A gate that fires on a green log is worse than no gate.**

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`** with FOUR blind spots (an em-dash, a DOT in `http-1.1`, two LETTER-suffixed ids). ⚠️ **EXECUTED on scratch copies at the phase-77 PLAN: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** ⚠️ **DO NOT restore the regex form.** ⚠️ **AT THIS STAGE CHECK (1) GOES SILENT BY DESIGN POST-FLIP — which is exactly the reading a BROKEN check produces. The row-62 doctoring NC is the ONLY thing that discriminates the two.** Run it on BOTH sides.

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

⚠️ **Run the union form, never one half of it.** ⚠️ **A ONE-ARM STRIP IS NOT AN NC** (6 -> 5 short / 6 -> 1 long; never 6 -> 0). ⚠️ **The documented trap's stated MECHANISM is refuted while the trap is real** — the inflation comes from ROADMAP table rows carrying BRAINSTORM prose. **Cite the command; do not re-inherit the explanation.** ⚠️ **The line numbers drift with edits — re-derive them; the current six are `:197 :203 :209 :219 :225 :233`.** ⚠️ **These six paragraphs remain the final sentinel blocker, and any edit near them is a sentinel-affecting edit.** All six are standalone family PROSE paragraphs (not rows), naming ~42 distinct candidates; every "retire the paragraphs" shape is doctrinally foreclosed (`reference_sentinel_matcher_string_self_clears`). ⚠️ Row 87's provenance is OUTSIDE the windows (the row-85/86 shape) — **nothing narrows in any window sentence at row-done; confirm that by re-running the union AFTER the flip.**


==============================================


## STATUS AS OF 2026-08-13: PHASE 87 (`h2-double-slash-path-routing`) PLAN DONE (row 87 `in-progress` at `ROADMAP.md:149`, `want` 119, base `0f015664`). **THIS SESSION PERFORMS THE PHASE-87 IMPL — THE PHASE-CLOSING STAGE** (`STATE.md`'s `next-skill:` reads `IMPL for phase 87`).

**What the 87 PLAN landed (2026-08-13, base `0f015664`, branch `phase-87-plan`, ONE squashed commit).** Docs-only: ZERO production `.go`, ZERO test `.go`; **`ROADMAP.md`, `DECISIONS.md` and `BEHAVIOR_CONTRACT.md` ALL BYTE-UNTOUCHED** (row 87 stays `in-progress`, `want` stays 119, strict `PROPOSED` guard stays **ARMED at 1**). ⚠️ NAMED DEPARTURE, THIRD CONSECUTIVE STAGE: no investigation agents — probes INLINE (a throwaway 13-row `buildRequest` table + a throwaway `test/helpers` drive probe, both written into the worktree, run, and **DELETED with `sha256sum -c` + empty-porcelain proof**; a 17-case `net/url` `go run`; the tip binary built with `-o` into scratch; ports 47430-47431 transient, released with `ss` proof; **NO docker**). **Subagent-driven returns at the IMPL, where the work is parallel.**

**BOTH RED ANCHORS RE-PROVEN BY EXECUTION, FAILURE LINES READ — and the run produced THREE facts no prior stage had:**

- **The unit layer: 8 of 13 rows RED at the tip.** `//foo` -> `URL.Path = ""`, `//` -> `""`, **`//foo/bar` -> `"/bar"`** (the silent mis-route), `//foo?x=1` -> `""`; and all four reject rows ACCEPTED silently (`foo` -> `Path="foo"`, `?a=b` -> `""`, `/foo#frag` -> `"/foo"` fragment silently stripped, `/foo?a=b#frag` -> `"/foo"`). ⚠️ **FIVE accept rows are GREEN AT THE TIP** (`/`, `/foo`, `/foo?a=b`, `/a//b`, `*`) **and are REGRESSION CONTROLS, NOT ANCHORS — the landed table MUST label them so** (`reference_liveness_break_needs_failing_baseline`). The `RequestURI`-stays-literal pin also passes at the tip: **also a control.**
- **The differential layer, driven through the repo's OWN `helpers.H2CRoundTrip`** (the same x/net path the `0004` driver uses) against the tip binary on the post-extension route table: `GET //edge` -> **404, EMPTY body**; `GET //edge/health` -> **200 `OK\n`** (the mis-route); controls `/health` -> `200 OK\n` and `/missing/0` -> `404 not found\n` green.
- **GREEN counter-proof** with the SPEC's edit re-applied as a throwaway patch: both arms -> **`200 edge-ok`**, unit table **13/13**, `go test ./internal/filter/hcm/... -count=1` **ok** (`hcm` 0.015s, `hcm/h2` 1.204s). Reverted, sha256-verified, tree proven clean.
- ⚠️ **NEW FACT 1 — THE CLAIM NO PRIOR STAGE MEASURED: `prefix: "//edge"` really does match `//edge/health` through envoy-go's own router post-fix.** The SPEC inferred D-87-DIFF's recipe from parse behavior alone; had the matcher normalized or rejected a `//`-prefixed CONFIG value, the recipe would have needed reshaping. It does not.
- ⚠️ **NEW FACT 2 — THE `//edge` PRE-FIX 404 IS A ROUTE-MISS WITH AN EMPTY BODY**, not the catch-all's `not found\n`: `url.Parse("//edge")` yields `Path=""`, which carries no `prefix: "/"`, so NO route matches. ⇒ **that arm's load-bearing assertion is STATUS; `//edge/health`'s is BODY. Neither arm alone catches both failure modes.** A body-only assertion on `//edge` would compare `""` to `""` and read green on a future regression.
- ⚠️ **NEW FACT 3 — THE CONTRACT ALREADY CARRIES A SENTENCE THIS ROW REFUTES:** `BEHAVIOR_CONTRACT.md:2034` claims *"the path is the bytes of the `:path` pseudo-header — there's no stdlib net/http parsing to inject normalisations"*. `url.Parse` IS stdlib parsing and it injected the corruption. **The IMPL's contract edit is a RIDER on an existing FALSE sentence** (the ADR-0307/`:2058` form: the original sentence is RETAINED, never rewritten), **not merely a new bullet.** Both texts are DRAFTED in PLAN §5.1 — lift them.
- **One SPEC-table refinement:** `url.ParseRequestURI("/foo#frag")` also populates `RawPath="/foo#frag"` (the §1.1 table named only Path). Immaterial — the guard fires before the parse — but the table is not complete as quoted.

### ⚠️ WHAT THE IMPL OWES (PLAN §2 — SIX ORDERED TASKS, ONE ATOMIC COMMIT, TDD HELD INSIDE)

**T0 — unit RED census.** Land the path-forms table in `internal/filter/hcm/h2/stream_test.go` beside `TestBuildRequest_ConnectionSpecificFields` (the Table-B idiom anchor at `:548`), nine accept + four reject rows, **with the five green-on-arrival rows LABELED REGRESSION CONTROL**. Run it; record the eight RED lines. **A RED set differing from PLAN §1.1's is a FINDING, not a rounding error.**

**T1 — differential RED census.** Land the `0004` extension per the PLAN §4 recipe; run `TestDifferential/0004-h2-routing` against the UNMODIFIED tree; expect the `//edge` **status** arm to fail. ⚠️ **Confirm the named subtest PRINTED** — a `-run` no-match prints `[no tests to run]` and **EXITS 0** (`reference_differential_run_selector`).

**T2 — the production edit** (`stream.go`, `buildRequest` — **cite BY SYMBOL**; the `url.Parse(path)` call sits at **`:465`** at the PLAN tip and already drifted from `:440`): the `strings.IndexByte(path, '#') >= 0` -> `&Error{Code: ErrProtocolError, Msg: "fragment in :path"}` guard immediately before the parse, then `url.Parse` -> `url.ParseRequestURI`. `strings` (`:11`) and `net/url` already imported. **Measured shape TWICE (SPEC prototype + PLAN throwaway patch): `11 insertions / 1 deletion` = net +10.**

**T3 — GREEN both layers** + `go test ./internal/filter/hcm/... -count=1`.

**T4 — docs.** ADR-0309 §Decision (the five-D ledger: D-87-PRIM / D-87-FRAG / D-87-REJECT-SHAPE / D-87-DIFF / D-87-SEQ) + §Consequences (i-vii, enumerated in PLAN §5.2) appended IN PLACE after the RETAINED italic footer, **strict guard 1 -> 0**; the two `BEHAVIOR_CONTRACT.md ## HTTP/2` edits drafted in PLAN §5.1 riding ADR-0309 per ADR-0052; `PROGRESS.md`; **ROADMAP row 87 -> `done`**.

**T5 — gates LAST** (PLAN §8), against the FINAL tree.

⚠️ **T0 AND T1 ARE BOTH PREREQUISITES OF T2.** Landing the ten-line T2 first — it is small and already measured — would convert BOTH RED censuses into unfalsifiable green-on-arrival claims. This is the single most likely way this stage goes wrong.

### ⚠️ THE FROZEN CONSTRAINT SETS — DECIDED AT THE PLAN, DO NOT RE-OPEN

**Error strings.** ZERO landed reject strings change. `bad :path` is **REUSED byte-identically** for the two `ParseRequestURI` rejects (`foo`, `?a=b`); **`fragment in :path` is the row's ONLY new string** (`/foo#frag`, `/foo?a=b#frag`). `Code` is `ErrProtocolError` on all four. **Assertion form (the PLAN decided what the SPEC left open):** `herr.Msg` by **EXACT EQUALITY** (the package's own Table-B idiom, justified in-file at `stream_test.go:596-604`) plus `Underlying` asserted for **NIL-NESS ONLY** — non-nil on the two parse rejects, nil on the two fragment rejects. ⚠️ **DO NOT pin the wrapped stdlib text** (`parse "foo": invalid URI for request`): it is `net/url`'s wording, a Go-toolchain-version hostage, and `Msg` + `Code` + nil-ness already separate all four rows. The nil-ness assertion is what keeps the two reject FAMILIES distinguishable.

**The `0004` recipe.** ONE route inserted **immediately BEFORE the `prefix: "/"` catch-all** in BOTH YAMLs (cite by the catch-all's `match: { prefix: "/" }` line, not by number — `:62` in both at the PLAN tip): `match: { prefix: "//edge" }` -> `direct_response 200 "edge-ok"`. **Verified by READING, not assumed: `renderBootstrap` / `substitutePEM` / `readYAML` are UNAFFECTED** — a `direct_response` route adds no `port_value: 0` line and no `{{...}}` token, so neither the ordered port substitution nor the first-occurrence PEM rule shifts (⚠️ this paragraph stops holding the moment a route names a CLUSTER). `drive()` gains a FOURTH loop **APPENDED** after the existing three so the 27-request transcript prefix stays byte-identical. **`doc.go` needs NO edit** (PKI-generation prose only) and **`test/helpers/h2.go` needs NO edit** (the PLAN drove both arms through the unmodified helper). Counts: fixtures **121 +0** (extension, no new dir ⇒ the three-registration-gates hazard does not arise) · BackendKind +0 · ports +0 · `BackendCount()` stays 3 · `AssertDistribution` untouched (the new arms are `direct_response`; `[3,3,3]` is unchanged by construction). Prose riders: `expectations.yaml` + `README.md` gain the route-table row (renumbering the catch-all to 4) and the 27 -> 29 request schedule.

**ADR-0309 completion.** Append `### Decision (landed at the phase-87 IMPL)` + `### Consequences (landed at the phase-87 IMPL)` after the retained italic footer, ADR-0294→0308 shared-block form. **No renumber. No new `---`** (`^---$` STAYS **216**; headings STAY **308**; tail STAYS ADR-0309; next-free STAYS **ADR-0310**, TAIL-derived; STATUS census STAYS **22**). ⚠️ **THE GUARD FLIP IS THE ONE NON-APPEND EDIT `DECISIONS.md` TAKES THIS ROW** — the `> **STATUS: PROPOSED …**` line becomes `> **STATUS: COMPLETE …**`, so the append-only proof is **`numstat N 1`, NOT `N 0`, and the byte-exact-prefix `cmp` WILL NOT HOLD.** Verify instead that the ONLY deleted line is the STATUS line. ⚠️ The completed block then NARRATES the word "PROPOSED", joining the **loose** matcher's standing false-positive set — **carry no whole-file count of the loose form** (five firings now).

### ⚠️ BUDGET — A FLOOR, NOT A CENTRAL ESTIMATE

Per PLAN §6: T0 `stream_test.go` **~100-175** · T1 `driver.go` **~25-45** · T2 `stream.go` **+10 net measured** (band 10-16) · **total `.go` ~135-236 net**, on top of the SPEC's ~130-225. Non-`.go`: 2 YAML routes (~4 lines each), `expectations.yaml` + `README.md` ~20-30, ADR-0309 ~12-20, contract ~2, ROADMAP `1 1`. ⚠️ **TEN consecutive `reference_measured_prototype_is_a_lower_bound` firings, cause UNDER-ENUMERATION every time.** Named un-enumerated classes: per-arm exact-message pins if the full Table-B comment idiom is adopted; reviewer-mandated arms; and the two the PLAN itself added (the regression-control labeling, the `Underlying` nil-ness assertions). **An overrun past 236 is not a failure — it is an ADR-0309 §Consequences (iii) entry and the ELEVENTH firing.**

### ⚠️ CARRIED META-LESSONS (they apply to the IMPL's method)

1. **CITE BY STRING/SYMBOL, NEVER BY LINE** — the `buildRequest` parse site drifted `:440` -> `:465` across this lineage; the 86 lineage minted TWO stale-locator defects inside its OWN branch. Every `:NNNN` in this file is a LIVE FIGURE at the PLAN tip — re-derive at yours.
2. **A reproduced defect on one path is not proof of the others** — H1 (`net/http`) and H3 (`internal/filter/hcm/h3dispatch.go`) are correct by separate code paths (H1 measured, H3 by code read). **DO NOT TOUCH THEM.**
3. **Read the launch OUTPUT, never just the readiness probe.** The PLAN lost two launches to this: the binary's flags are **`-c`** and **`-allow-h2c`** (NOT `--config-path`), and a bootstrap with zero clusters is rejected (`cluster manager: cluster: zero clusters in bootstrap`). An empty `curl /ready` looks identical to a slow boot (`reference_timeout_exit_124_shared_by_healthy_and_hung`).

### ⚠️ COUNTS AT THIS TIP — RE-DERIVE MECHANICALLY, NEVER COPY

- **differential fixtures 121** — numeric tail `0119-grpc-unary-trailers`, next-free `0120` (**stays FREE** — D-87-DIFF extends `0004`). H2 routing fixtures: `0004-h2-routing`, `0079-h2-multiplex-pool`, `0080-h2-goaway-rotation`.
- **`ROADMAP.md` 237 lines / 119 data rows** (row 87 `in-progress` at `:149`); check-(2) anchors `:197 :203 :209 :219 :225 :233`; `-family row` **95 occurrences / 67 LINES**; `gRPC-family row` **2**; `Operational-tooling-family row` **3**; ARM-A flags **{119, 131}** (escape-aware command ONLY).
- **`DECISIONS.md` 18114** lines · **308** headings · tail **ADR-0309 PROPOSED** (§Context only) · next-free **ADR-0310** (TAIL-derived) · **strict `PROPOSED` guard 1 — THIS STAGE TAKES IT TO 0** · STATUS census **22** · `^---$` **216**.
- **`BEHAVIOR_CONTRACT.md` 5957** (byte-untouched until this stage) — `## HTTP/2` at **`:2019`**, the FALSE sentence at **`:2034`**, the route-match bullet at **`:2035`**, the ADR-0307 rider FORM to copy at **`:2058`** · **`STATE.md` 64** · **`STATE_HISTORY.md` 488** · **`BOOTSTRAP_PROMPT.md` 522** (repo ROOT, not `docs/envoy-go/`) · phase dirs **128**, `REVIEW.md` **37** (standing departure, named not claimed).
- **h2spec: cite `95 tests, 94 passed, 1 skipped, 0 failed` ONLY from your own tip's run** (skip 6.9.2/2 invariant).
- **fuzzers 55 / 48 files** · **BackendKind tail 38** (`H2GoawayResponder BackendKind = 38`, `fixture.go:614`) over 39 declarations (do NOT "fix") · **stat call sites: assert DELTAS only and STATE WHICH FORM YOU RAN.** ⚠️ The PLAN measured `command grep -rno 'NewCounter('` = **327** and `NewGauge(` = **79** (occurrences over ALL `.go` incl. tests) — a DIFFERENT form from the 86/87 lineage's carried **145/21**. **No absolute is corrected and none is carried**; run the SAME command both sides (`reference_a_drift_correction_is_itself_a_claim`).
- **phase-87 surface (re-derive at your tip):** `internal/filter/hcm/h2/stream.go` `buildRequest` (**ONE** non-test `url.Parse` under `internal/filter/hcm/`, re-grepped at the PLAN) · `internal/filter/hcm/h2/stream_test.go` (Table-B anchor `:548`) · `test/fixtures/0004-h2-routing/` (`envoy.yaml`, `envoy-go.yaml`, `driver/driver.go` `drive()`, `expectations.yaml`, `README.md`) · `test/helpers/h2.go` **NOT edited**. H1 and H3 correct — **DO NOT TOUCH**.
- ⚠️ **STILL CONTESTED, NO NUMBER CARRIED:** the `STATE_HISTORY.md` archive-gap total · the loose `PROPOSED` matcher's whole-file count · the stat-surface absolute (1205 vs 1207 — WIDENED; only DELTAs asserted) · the zero-test `generic/*` suite count (13 vs 14).

### ⚠️ DOCUMENTARY DEFECTS — carried forward, still deliberately NOT fixed

**`ADR-0051` §2's false gate-scope sentence** corrected on the record by ADR-0307. **`ADR-0058`** cites `routerActionH2.doH2` at a dead location. **`ROADMAP.md` rows at lines 119/131 malformed** (ARM-A GUARD, not a fix). **`STATE.md` §Project counts FROZEN at phase 76.** **`harness_test.go:208` port inventory STALE.** **`body.go` nolint INERT** · **xDS cycle guard NOT AUTOMATED** · **`wasm/doc.go:219` two errors** · **ROADMAP cites `esalaine` FIVE times** · **`rbac.go:50` token `F2`** · **root `PROGRESS.md`** stray phase-32.1 doc · **SPEC-86/PLAN-86 prose cites the grpcclient Close pins at a nonexistent `internal/xds/xdsgrpc/...` path** (ADR-0308 §Decision (b) is the correction on the record) · **NEW, and this row's own vehicle handles it: `BEHAVIOR_CONTRACT.md:2034`'s "no stdlib net/http parsing" sentence is FALSE for the 05.1 -> 87 window** — RETAINED and ridden, never rewritten.

### Candidates ledger (FUTURE picks; re-derive EVERY cost at your tip)

- **The CONTINUATION two-sided repair** — strongest KNOWN product defect (server discard at `internal/filter/hcm/h2/conn.go`'s `dispatchFrame` `*http2.ContinuationFrame` arm — cite by symbol + client blindness + the client-side SETTINGS gap in `h2/client.go`, all named in ADR-0307). h2spec MEASURED not to gate either side. Est. 2-4x row 85 (+1046 realized) — **the natural next LARGE row and the strongest candidate for the post-87 self-pick; charter as a SPLIT phase.**
- **The stat-surface recount** (1205-vs-1207 WIDENED; rides a future +0 row) · **`ssl.connection_error`** (+444 whole-`.go` floor) · **`test/conformance/grpc/`** (9/26 reachable) · **REVIEW.md restoration** (process-not-product) · **hygiene fold-ins** (`harness_test.go:208`, xDS cycle-guard) · **the D-86-CONN `client.Close` gate** (UNGATED invariant, ~10 lines, fold-in) · the **~42 candidates in the six family windows**.

### ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + BOTH DOCTORING NCs YOURSELF — BOTH SIDES OF THE ROW FLIP** and record ACTUAL output. Post-flip check (1) SILENT is CORRECT here and nowhere else; the row-62 NC is what proves the check still LOOKS.
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** The PLAN re-proved the SPEC's REDs and found three things the SPEC did not have. Do the same to the PLAN: its §1.1 RED census and §4 recipe are claims to re-run, not quotes to trust.
3. **LINE ANCHORS DRIFT; SYMBOL ANCHORS DO NOT — RE-VERIFY EVERY CITE AT YOUR TIP.**
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Still live: SDS dial-budget (TWO packages — NAMED member `TestSDSEndToEnd_FetchFailure_BootFailsClosed/silent_SDS_server…`, `internal/boot/boot_sds_e2e_test.go:551`, gRPC `DeadlineExceeded` under full-suite load) · `internal/cluster` `-race` outlier · `internal/httpclient` zero-value · driver-receiver port race (roster 42; `0081` binds `0.0.0.0:42039`) · the two 84.2-era flakes (cleared, unclassed) · `TestServerConn_TinyWindowDelivery` (85-IMPL, cleared, unclassed) · the REFERENCE h2spec section-8 flip (reference-side only). **Capture `-v` output BEFORE re-running anything.**
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only).
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`.
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL.** **A harness's exit code is not the command's.** **`gofmt -l` never exits non-zero — gate on OUTPUT.**
8. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-87-*` or stage worktree should outlive its stage close** — expect only `master` plus your own (verify with `git worktree list`).
9. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** (a)/(b) differential 121 fixtures, full suite with `INNER_EXIT`; (c) h2spec cite only from your own run; grpc-conformance deferred in writing; proxy-wasm 10/16; (d) fuzzers 55/48; (e) `INNER_EXIT` on every differential launch AND `go test ./...`; (f) no REVIEW.md — standing departure.

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 87 (h2-double-slash-path-routing)' --format='%h %s'  # 3 at this tip: BRAINSTORM + SPEC + PLAN (the OPEN phase); becomes 4 after your IMPL
git log --grep '^phase 86 (validate-sds-nil-provider)' --format='%h %s'    # 4: a CLOSED phase, use it as a FIRING CONTROL
```
⚠️ **The count comments are LIVE FIGURES — VERIFY before relying.** Take the NEWEST line (`| head -1`).

Execution style: **subagent-driven per `feedback_execution_style` is the STANDING PREFERENCE and RETURNS AT THIS STAGE** — the 87 BRAINSTORM, SPEC and PLAN all took the NAMED inline departure because their probe sets were cheap and serial; an IMPL with six ordered tasks, two test layers, a docs wave and a gate suite is the shape subagents were preferred for (the phase-86 IMPL precedent: 6 implementers + 6 task reviewers + scoped re-reviews + a gates agent + a most-capable-model final whole-branch review caught FIVE defect classes pre-squash). **NAME the choice either way.** Subagents commit LOCALLY on the ONE stage branch; the controller squashes (`feedback_subagents_no_push`, `feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** and tear down docker **BY NAME**. ⚠️ **Band private ports outside BOTH `20000-31007` AND `11000-14999`, clear of the static fixture ports (10000-19172); `0081` binds `0.0.0.0:42039`.** The 87 SPEC used 47410-47421 and the PLAN 47430-47431; the IMPL's probes band from **47440+**. ⚠️ **ONE WORKTREE PER AGENT preferred; explicit-path staging if shared; never an unscoped `git restore`.** Per-task `gofmt` + `golangci-lint` on touched packages (⚠️ misspell locale US). ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch.** ⚠️ **Docker BY NAME (`p87-*` if this stage needs any — the reference record is BANKED in SPEC §4 and the IMPL owes no new reference claim), `--rm`, verify gone.**

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root. Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run. ⚠️ **AND `pkill -f <pattern>` MATCHES YOUR OWN SHELL'S COMMAND LINE AND KILLS THE TOOL CALL (exit 144)** — observed twice at the PLAN. Kill background probes by **PID from `pgrep -laf`**, and verify the ports released with `ss -ltn`.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — sha256 against the base blob, never by eye. The PLAN's form: `sha256sum` the file BEFORE patching, `git checkout -- <path>` after, `sha256sum -c` the capture, then `git status --porcelain` AND `git diff --stat master` both EMPTY.

⚠️ **THE ARCHIVE-ABSENCE GUARD — USE THE WHOLE-FILE FIXED-STRING FORM** (`grep -cF`), NC'd with REAL targets re-derived FROM THE FILE first; a control's own count is a LIVE FIGURE: at the phase-87 PLAN close `phase 82 (wasm-http-call-response-cache) IMPL done` read **12** and `phase 83 (wasm-pause-arm-leak) PLAN done` read **10** (both UNCHANGED from the SPEC close — the SECOND consecutive no-drift reading; still re-derive). ⚠️ **The §Recent-style bullet anchor `- **prior active-phase:**` reads 0 on ARCHIVED entries** (the archive wraps them `- **prior active-phase (evicted…):**`) — use the fixed-string form. **Verify STRICTLY APPEND-ONLY**: `git diff --numstat` = `N 0` AND base a byte-exact PREFIX (`cmp`). At the IMPL close §Current rolls PLAN-done -> **IMPL done / PHASE 87 CLOSED (lifecycle-state 3 -> DONE)** and §Recent evicts the oldest per a DIRECT DATE READ at that tip (⚠️ expect the oldest to be **`phase 86 (validate-sds-nil-provider) SPEC done`** at 2026-08-10, now UNIQUE after this close consumed the tie's second member — CONFIRM by direct read before removal).

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads `IMPL for phase 87` — that IS your instruction.** §Project counts SELF-CONTRADICT §Current — do not anchor on them (frozen at phase 76).
2. `docs/envoy-go/phases/87-h2-double-slash-path-routing/PLAN.md` — **the operative document**: §1 the re-proven RED anchors + the three new facts, §2 the six ordered tasks, §3 the frozen error-string set, §4 the pinned `0004` recipe, §5 the DRAFTED contract text + ADR-0309 completion plan, §6 the budget, §7 counts, §8 gates.
3. `docs/envoy-go/phases/87-h2-double-slash-path-routing/SPEC.md` — the seven disposed questions and the D-87 decisions the PLAN decomposes.
4. `internal/filter/hcm/h2/stream.go` (`buildRequest` — BY SYMBOL) + `internal/filter/hcm/h2/stream_test.go` (Table-B idiom, `:548`) + `test/fixtures/0004-h2-routing/` (`driver/driver.go` `drive()`, both YAMLs, `expectations.yaml`, `README.md`).
5. `docs/envoy-go/ROADMAP.md` — row 87 at `:149` (**FLIPPED `done` at this stage, `numstat 1 1`**); the six family-window paragraphs (check-(2) anchors); confirm nothing narrows.

## What the IMPL owes
T0-T5 per PLAN §2 in order, **ONE atomic commit** (T0/T1 RED censuses BEFORE T2) · the eight unit REDs and the `0004` RED observed with failure lines read · the +10-line `stream.go` edit with the symbol asserted, not just a green build · GREEN both layers · ADR-0309 completed with the strict guard **1 -> 0** (⚠️ `numstat N 1`, the STATUS line the ONLY deletion) · the two `BEHAVIOR_CONTRACT.md ## HTTP/2` edits from PLAN §5.1 · **ROADMAP row 87 -> `done`** (`numstat 1 1`, `want` stays 119) · realized cost recorded against PLAN §6's floor · gates LAST with `INNER_EXIT` and the ANCHORED panic gate · the sentinel + all four NCs **BOTH SIDES of the flip**, recorded · STATE.md rolled IN PLACE (PLAN-done -> IMPL done, **lifecycle-state 3 -> DONE**; §Recent eviction per DIRECT DATE READ — expect `phase 86 SPEC done`, now unique) · `next-prompt.txt` rolled to **the post-87 SELF-PICKED BRAINSTORM** (`git add -f`; the CONTINUATION two-sided repair is the strongest named candidate — re-derive its cost at that tip) · ONE squashed commit, merged to master, pushed.
