=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **PHASE 92 (`h2-response-header-validation`) IS OPEN AND MID-LIFECYCLE. ROW 92 READS `in-progress` AT `ROADMAP.md:154` AND `want` IS 124.** ⇒ **THERE IS BANKED MID-LIFECYCLE WORK. DO NOT SELF-PICK. THIS SESSION RUNS THE PHASE-92 IMPL (lifecycle-state 3 -> DONE).** ⚠️ **THE SENTINEL DOES NOT FIRE — check (1) prints `NOT DONE: row 92`, check (2) prints SIX, check (3) is SILENT. TWO checks block it. DO NOT create `stop`.** ⚠️ **MEASURE ALL THREE YOURSELF BEFORE DOING ANYTHING ELSE; do not take this file's word for it.**

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE. This form parses FIELDS instead of matching a pattern, and
#     asserts the row DENOMINATOR so a deleted row is itself an error.
awk -F'|' -v want=124 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **CHECK (1) IS NOT SILENT — THE OPEN-ROW BOARD SHAPE.** It prints `NOT DONE: row 92`. **Do NOT read that as breakage and do NOT "fix" it by flipping row 92** — the row flips `done` at the phase-92 IMPL, which is THIS session, as its LAST doc task. Run BOTH doctoring NCs EVERY SESSION anyway:
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md.
```
⚠️ **NC-A READS **TWO** LINES** (`NOT DONE: row 62` AND `NOT DONE: row 92`), because row 92 is legitimately open. **The one-line expectation was the ALL-`done` board's shape and DOES NOT CARRY.** ⚠️ **This is the FIFTH distinct NC-A shape in six stages.** ⚠️ **AND IT WILL CHANGE AGAIN AT THIS STAGE:** once THIS session flips row 92 to `done`, check (1) goes **SILENT** and NC-A collapses to the **ONE-line** `NOT DONE: row 62`. **Re-measure AFTER the flip; the pre-flip shape does not carry past your own commit.**
```sh
# the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```
Also NC the DENOMINATOR (`want=123` on the real file must print `GATE FAIL: examined 124 data rows, expected 123` **alongside** the `NOT DONE: row 92` line — ⚠️ **pre-flip NC-B is NOT a single-line result either; POST-flip it becomes one**).

⚠️ **AND WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -oiE '-family row'` fails (`ugrep: error: option -f: cannot read amily row`) and the surrounding arithmetic prints `0`, **which reads exactly like "no change."** **Always pass `--` before the pattern**, and the counts are form-dependent: with `--`, occurrences read **95** and LINES read **67**. **`grep -c` counts LINES, NOT OCCURRENCES.**

⚠️ **THE ARM-A MALFORMED-ROW FIGURE RECONCILES ONLY UNDER AN ESCAPE-AWARE FIELD COUNT — RE-VERIFIED AT THE PHASE-92 PLAN, WHERE IT STILL READS *TWO*.** The malformed rows are ids **57** (`NF=9`) and **69** (`NF=10`). ⚠️ **A NAIVE `awk -F'|'` `NF != 8` FORM READS *SEVENTEEN***. **Strip `\|` BEFORE splitting.** A gate asserting `== 0` FAILS on pre-existing content. ⚠️ The two forms **DISAGREE on row 57** (naive **13** vs escape-aware **9**) but **AGREE on row 69** (both **10**), so **any gate must state WHICH FORM it uses.** Row 92 field-counts **NF=8 under BOTH** forms — ⚠️ **keep it that way when you flip it: COUNT THE FIELDS BEFORE INSTALLING THE EDIT**, because an unescaped `|` silently passes the gate.

⚠️ **AND DO NOT SUBSTITUTE A "REASONABLE EQUIVALENT" FOR CHECK (1)'s AWK.** A measurement agent once tried `NF>=8 && $2 ~ /^ *[0-9]+ *$/`: it printed **92**, not the true denominator. **Use the form above verbatim.**

**SENTINEL RUN MECHANICALLY AT THE PHASE-92 PLAN (2026-08-26), FIRST at the pre-commit base `8c18100c` AND AGAIN AT THE PUBLISHING TIP AFTER THE COMMIT LANDED.** ACTUAL output, recorded not predicted, **IDENTICAL AT BOTH TIPS**:
- **(1)** `want=124` -> **`NOT DONE: row 92`**, ONE line · **(2) SIX** at `:202 :208 :214 :224 :230 :238` · **(3) SILENT.**
- **All four NCs fired:** NC-A **TWO** lines with `NC LANDED? [ in-progress ]` **inspected FIRST** · NC-B `NOT DONE: row 92` **AND** `GATE FAIL: examined 124 data rows, expected 123` · NC-C residual **2 -> 0** => `NEVER OPENED: gRPC`, WASM correctly silent · NC-D long **5** / short **1** / union **6**.
⇒ **THE SENTINEL DOES NOT FIRE — TWO checks block it. `stop` WAS EVALUATED AND DELIBERATELY NOT CREATED** (verified absent at the git root AND in the stage worktree).
- ⚠️ **EVERY LIVE FIGURE IN THIS FILE WAS RE-DERIVED AT THE PUBLISHING TIP.** `DECISIONS.md` **18467** · `^---$` **216** · `^## ADR-` **313** · bare `^## ` **321** · tail **ADR-0314** · `^## ADR-0315` **0** · strict `PROPOSED` guard **1** at `:18443` under `## ADR-0314` (`:18441`) · ADR-0231 decoy **1** at `:14866` · `STATE.md` **63** · `STATE_HISTORY.md` **528** with strict **163** / loose **214** · phase dirs **133** · fixtures **121** (tail `0119`, **`0120` FREE**) · fuzzers **55 / 48** · `BEHAVIOR_CONTRACT.md` **5962** · `go.mod` **67** (18 direct + 49 indirect) · BackendKind tail **38** (**39 declared, 39 FREE**) · blank imports **121 / 122 / 123 / 145** · `-family row` **95 occurrences / 67 lines**.
- ⚠️ **ONE INHERITED FIGURE WENT STALE AND IS CORRECTED HERE: the UNANCHORED `PROPOSED` count is 3, NOT 2.** `:18342` and `:18371` are **PROSE ABOUT** the guard; `:18443` is the real one. **It inflates every time an ADR documents the form. NEVER gate on it.**
- ⚠️ do NOT "fix" the six and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases. ⚠️ **Cite ~40 only as "sentence-visible", never as the inventory** — and **no gate may rest on a ` + ` split, in any form.**

==============================================

## STATUS AS OF 2026-08-26: PHASE 92 (`h2-response-header-validation`) **OPEN AT lifecycle-state 3**. **THIS SESSION RUNS THE PHASE-92 IMPL.**

**What the 92 PLAN landed (2026-08-26, base master `8c18100c`, ONE squashed commit `e69f1c58` PLUS a labelled correction `963d472d` — see the locate warning under Mechanics).** **DOCS ONLY — ZERO production `.go`, ZERO test `.go`** (`git diff --name-only master -- '*.go'` => **0 files**). `PLAN.md` created (**1482 lines**); phase dirs STAY **133**. `ROADMAP.md`, `DECISIONS.md`, `BEHAVIOR_CONTRACT.md`, `go.mod`, `go.sum` **BYTE-UNTOUCHED** (EMPTY DIFF); `want` STAYS **124**; row 92 STAYS `in-progress`. **NO ADR ADDED** — tail STAYS **ADR-0314**, next-free **ADR-0315**, strict guard STAYS **ARMED at 1**, `^---$` STAYS **216**. `STATE.md` rolled IN PLACE (**`9 9`**, 63 lines); `STATE_HISTORY.md` **526 -> 528** (`2 0`), **strict guard STAYS 163, DELTA 0**, loose **213 -> 214**.

### ⚠️ THE CHARTER (unchanged in substance)

**On the HTTP/2 downstream leg, REJECT a response whose UPSTREAM LEADING header block carries a connection-specific or otherwise malformed field, instead of laundering it onto the downstream stream.** The ENCODE-direction mirror of `h2.IsIllegalH2RequestHeader` (row 89, DECODE direction).

### ⚠️ THE DIVERGENCE SET IS **NINE** SHAPES, NOT SEVEN — the PLAN measured two more

`connection` · `transfer-encoding` · `keep-alive` · `upgrade` · `proxy-connection` · an **uppercase** name · a **duplicate `content-length`** · **`te` with a wrong value** · **`te` with an EMPTY value**. All NINE: subject **200, forwarded**; reference **502 `reset reason: protocol error`**. ⚠️ **`te: trailers` is PARITY — the reference FORWARDS IT VERBATIM and it must NOT be rejected.**

### ⚠️ WHAT THE PLAN REFUTED BY EXECUTION — TEN claims, THREE of them its own SPEC's

1. ⚠️ **THE OPEN ARM IS CLOSED, AND THE BROAD RULE SURVIVED.** The reference REJECTS an **IDENTICAL** duplicate `content-length` (`cl: 5`, `cl: 5`) with **502**, byte-identical to the differing-value arm. ⇒ **reject ANY second `content-length`. Do NOT narrow to "differing values".**
2. ⚠️ **THE `te` LEG WAS SHIPPED UNMEASURED BY THE SPEC AND IS NOW MEASURED.** `te: gzip` **502**, `te: ""` **502**, `te: trailers` **FORWARDED 200**. ⚠️ **`te: ""` is the arm an implementer gets wrong** — writing `value != "" && value != teTrailersValue` looks like hygiene and is **measurably wrong**.
3. ⚠️ **AN EIGHTH-CLASS SHAPE, SCOPED OUT IN WRITING.** A **SINGLE** `content-length` with a wrong value: reference **200 headers then RST_STREAM(INTERNAL_ERROR), no DATA**; subject silently rewrites to a clean 200. ⇒ **duplicate-NESS, not value-CORRECTNESS, trips the 502.** It belongs to the banked `content-length`-rewrite candidate, now **FOUR-legged**. **DO NOT scope it into row 92.**
4. ⚠️ **A THIRD FILE THE SPEC DID NOT KNOW ABOUT, AND IT IS A RED GUARD.** `router_h2_trailers_test.go:528` is an **AST-audit golden** pinning the MULTISET of `ActionResponse` Status literals in `doH2ClusterAction` (`want := []int{0,0,502,502,503,503,503}`). The new arm reddens it: `[0 0 502 502 502 503 503 503] (n=8)`. Minimum edit **`+2 / -1`**, and it is **MANDATORY**. Its sibling's `t.Logf` census moves **17 -> 18** but does NOT redden.
5. ⚠️ **THE VALIDATOR WOULD SHIP COMPLETELY UNGATED.** Adding it moves **ZERO** of **655** tests, RUN SETS identical by `diff`. **Deleting its whole body leaves the suite green. The unit table is the ONLY thing that can fail.**
6. ⚠️ **COST SUPERSEDED: `+74`/`+77` -> `+174 / -1`**, THREE files, TWO packages, one compiling prototype: `client.go` **137 0** (code 44 / comment 90 / blank 3) · `router_h2.go` **35 0** · `router_h2_trailers_test.go` **2 1**.
7. ⚠️ **THE COUNTER-NAMING PREMISE IS WRONG. `http2_tx_reset` EXISTS IN NO `.go` FILE** — it is the Prometheus-FLATTENED projection. envoy-go registers **DOTTED** names (`manager.go:200-201`: `http2.rx_reset`, `http2.tx_reset`). ⇒ the leaf is **`http2.rx_messaging_error`**, convention-native.
8. ⚠️ **`406` IS NOT THE STAT SURFACE AND IS NOT REPRODUCIBLE.** ZERO hits in `BEHAVIOR_CONTRACT.md` (ledger tail **1207**); provenance is an **unnamed grep** in ADR-0313. Seven candidate commands read **143/324/401/402/403/404/699**. ⇒ **assert the `+1` DELTA structurally; cite NO absolute; do NOT write `406 -> 407` into any artifact.**
9. ⚠️ **AN UPPERCASE STAT NAME *PASSES* `IsValidName`** (`^[a-zA-Z_]([a-zA-Z0-9_.]*[a-zA-Z0-9_])?$` — `a-zA-Z` in BOTH classes). **A proposed NC was vacuous.** Falsifying NCs: hyphen, space, trailing dot, leading digit, empty, trailing newline.
10. ⚠️ **THE UNANCHORED `PROPOSED` COUNT IS 3, NOT 2** — stale inside the ADR that documents it.

**AND: the SPEC's §3 symbol table survived 9/9 EXACT, ZERO DRIFT.** Recorded because a clean re-derivation is a RESULT, not a reason to stop re-deriving.

### ⚠️ THE FROZEN DESIGN — `PLAN.md` §4 is authoritative; do NOT re-open it

- **§4.1** `ErrMalformedResponseHeaders` + `malformedResponseHeadersError` in `client.go` after `:716`.
- **§4.2** `validateResponseHeaders(streamID uint32, fields []hpack.HeaderField) *Error` — **FOUR legs**, `hasUppercaseHeaderChar` **FIRST** (every leg below it is a CASE-SENSITIVE comparison). The duplicate-`content-length` leg is a **CROSS-FIELD COUNT**, not a predicate. ⚠️ **Do NOT collapse the connection-specific and `te` legs into `IsIllegalH2RequestHeader`** — it reddens `TestValidateResponseTrailers_Table/te_gzip`. ⚠️ **`:status` is NOT validated — deliberately.**
- **§4.3** the call site inside `if !cs.respHeadersSeen {`, **BEFORE `cs.respHeaders = decoded`**, mirroring the trailer path: `markReset` **FIRST**, locked `WriteRSTStream`, `onTxReset()` **OUTSIDE** `cc.mu`, `cs.finish(verr)`, `return nil`.
- **§4.4** the THIRD router arm **BETWEEN `router_h2.go:197` AND `:202`** — AFTER `EvictH2ConnOnError` (the evict IS parity), **BEFORE** the ctx-cancel check (which branches on `ctx.Err()` ALONE and would launder the reject into a `Status: 0` CANCEL). ⚠️ **Consequence stated in writing: a genuine client cancel coinciding with a malformed block now reports 502 rather than CANCEL.** ⚠️ **`localOrigin` STAYS UNSET while `LocalOriginErr` STAYS `true` — DIFFERENT CONSUMERS (outlier detection vs retry classification). DO NOT TIDY.**
- **§5** the counter **`http2.rx_messaging_error`** at `manager.go:112` inside the `if c.useH2 {` gate at `:194`.

### ⚠️ THREE HAZARDS CONFIRMED BY EXECUTION — design around them

1. ⚠️ **A NIL `*stats.Counter` `.Inc()` IS A PROCESS CRASH** (`counter.go:22`, no nil guard, no recover). The counter is `useH2`-gated ⇒ **NIL on every non-H2 cluster**. **ROUTE THE INCREMENT THROUGH `incCounter` (`connpool.go:211-215`)**, and **ASSERT THE POINTER non-nil on the H2 path** — a nil-guarded `Inc` that silently does nothing is a vacuous pin.
2. ⚠️ **REGISTERING A STAT INSIDE `Registry.Walk` DEADLOCKS** (RLock held across iteration; `sync.RWMutex` is not reentrant). **Confirmed by execution — the probe never returned in 2 s.** **Register at BOOT, never from a scrape/Walk callback.**
3. ⚠️ **A COUNTER CANNOT GATE A VALUE.** Pin the **DELTA**, **subject-side only**, with a **legal-path control** (delta 0).

### ⚠️ HARNESS CONSTRAINTS THE IMPL INHERITS — each cost a probe

- ⚠️ **The leading-block reject writes RST_STREAM FROM THE readLoop GOROUTINE** ⇒ a wire test MUST use **`dialClientConnTCP`** (`trailers_validate_test.go:291`), **NOT `dialClientConn`**. A `net.Pipe` probe **DEADLOCKED and was killed at 120 s**.
- ⚠️ **`startH2TrailerBackend`/`runH2TrailerBackend` CANNOT serve this shape** — their leading block is hard-coded to `:status`/`content-type`/`content-length` and `h2TrailerBehavior` has no leading-block arm. **PLAN T9 budgets either a fifth enum value + a caller-supplied leading-header slice threaded through both (5 call sites), or a third backend (~57 lines). THIS IS UNPRICED BY ANY EARLIER STAGE.**
- ⚠️ **Four of the nine shapes are STRUCTURALLY unreachable from a `net/http` backend** (`connection` deleted at `x/net/http2/server.go:2759`; `transfer-encoding`; an uppercase wire name; a duplicate `content-length`). Three DO leak (`keep-alive`, `upgrade`, `proxy-connection`) because `server.go:2757` carries a live `TODO: remove more Connection-specific header fields here`. ⚠️ **WHETHER `te: gzip` / `te: ""` LEAK IS UNMEASURED — PLAN T2 MUST MEASURE IT, NOT INFER IT FROM `server.go`.** Arm 7 is the standing proof that a code-reading of that file gets it wrong.
- ⚠️ **THE REFERENCE'S UPSTREAM POOLS ARE PER-WORKER / THREAD-LOCAL** — any connection-lifetime arm needs BOTH streams on **ONE** downstream conn, and **one goroutine per stream** (a synchronous backend blocks the read loop).
- ⚠️ **DOCKER: the host-gateway is `192.168.65.2`, resolvable ONLY from inside a container** (`getent ahostsv4` on the host returns nothing and `docker0` does not exist). ⚠️ **A PROBE BACKEND MUST BIND `0.0.0.0`, NOT `127.0.0.1`** — a loopback bind is unreachable from the container and the failure **looks exactly like an envoy-side rejection**.

### ⚠️ WHAT THE IMPL OWES — `PLAN.md` §14, TWENTY-THREE ordered tasks

**T1** baselines (RUN=655/FAIL=0 re-derived; capture the sorted `=== RUN` list for T15) · **T2-T3** the RED ANCHOR (differential arm `+193/-0`, RED on the unmodified subject, transcript diff **PER SHAPE**) · **T4-T8** production · **T9** the test-backend extension · **T10-T15** the unit tables and pins · **T16** the D-92-1XX comment (**10/3**) · **T17-T19** the NC campaign · **T20** fuzz · **T21** differential GREEN · **T22** the six-gate posture · **T23** docs.

⚠️ **THE RED ANCHOR IS T2+T3, AND IT IS A GENUINE ONE.** The unit tables name symbols the row creates, so writing them first is a **BUILD BREAK, not an assertion failure** — and a build break proves nothing.

⚠️ **THE SPLIT GATE WAS EVALUATED, NOT ASSUMED: 23 tasks vs ~25, ~760-975 net LoC vs ~1500 ⇒ NO SPLIT.** But the task margin is **TWO**, and BOOTSTRAP §6.1 also triggers a split **MID-EXECUTION**. `PLAN.md` §10.4 names **three** triggers (T9's blast radius, the NC campaign forcing pin redesigns, the counter needing a new registration path). **Splitting is not a way to defer work (BOOTSTRAP §6.3).**

## ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + ALL FOUR NCs YOURSELF** and record ACTUAL output. ⚠️ **Pre-flip, check (1) prints `NOT DONE: row 92` and NC-A/NC-B are BOTH two-line. POST-flip both collapse. MEASURE BOTH SIDES OF YOUR OWN EDIT.**
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** 91 SPEC seven · 91 PLAN ten · 91 IMPL four · 92 BRAINSTORM five · 92 SPEC nine · **92 PLAN ten, including THREE of its own SPEC's figures.** Do the same.
3. **LINE ANCHORS DRIFT; SYMBOL ANCHORS DO NOT — RE-VERIFY EVERY CITE AT YOUR TIP.** ⚠️ **PATHSPEC-SCOPE every symbol assertion.** ⚠️ **A RECEIVER WRITTEN `(cc *ClientConn)` MUST USE `grep -F`, NOT `-E`** — ERE reads the parens as a group and returns a **FAIL-UNSAFE ZERO** (`-F` 1, `-E` 0, re-reproduced at the PLAN).
3b. ⚠️ **CHARACTER-CLASS AND ESCAPE SELECTORS ARE THE FAIL-UNSAFE FAMILY.** (i) `grep -cE '^\s+[a-z0-9./-]+ v[0-9]'` on `go.mod` reads **62**, not **67** — it drops the five modules with an UPPERCASE letter or underscore. (ii) `grep -oE '[A-Za-z]+ +BackendKind = 38'` prints `GoawayResponder`, **truncating `H2`**. (iii) an over-anchored `^[0-9]{4}-` reads **119**, dropping `0007a-cors` / `0007b-iteration-probe`: use `ls -d test/fixtures/*/ | wc -l`. (iv) ⚠️ **`\t` in `-E` is TOOL-DEPENDENT: harness `grep` is ugrep 7.8.4 (reads 123), `/usr/bin/grep` is GNU 3.11 (reads 0 and exits 1). NAME THE TOOL.** Safe under both: `-P`, or `$(printf '\t')`.
3c. ⚠️ **`feedback_brief_citations_not_evidence` APPLIES TO INCIDENTAL FIGURES.** This stage: `+74/+77`, `406`, `http2_tx_reset` and the unanchored `PROPOSED` count were ALL wrong or unsourced. **Re-derive EVERY number you restate.**
3d. ⚠️ **NO GATE MAY REST ON A ` + ` SPLIT.** Both the trap and its recorded remedy are fail-unsafe.
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. ⚠️ **TWO SDS dial-budget flakes are live** (`internal/boot`, `internal/xds`); **DO NOT reuse the `go list -deps` exclusion blanket — TRUE for `xds` (0), FALSE for `boot` (1)**. Also live: the driver-owned receiver port race · `internal/httpclient` zero-value · the two 84.2-era flakes · the REFERENCE h2spec section-8 flip. ⚠️ **NEW SIGHTING AT THIS PLAN: `TestOutlierDetector_ConcurrentEjectExactlyOnce` (`internal/cluster`) fired ONCE (`ejections_enforced_total = 2, want exactly 1`) then went 0/24 across BOTH patched and clean arms** — **NOT attributable to row 92**, but an OVER-FIRING ejection counter under contention is worth a banked candidate per `reference_recurring_flake_may_be_production_bug`. ⚠️ **`TestServerConn_TinyWindowDelivery` IS NOT A FLAKE — phase 91 FIXED a live production deadlock. A recurrence is a REGRESSION of row 91.**
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only). Foreign containers left untouched throughout phases 90, 91 and 92: `infallible_booth`, `crazy_kare`, `golink-ai`, `quizzical_goldstine`.
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`.
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL.** **`gofmt -l` never exits non-zero — gate on OUTPUT.** ⚠️ **`golangci-lint` too.** ⚠️ **`go test` WITHOUT `-v` PRINTS ZERO `=== RUN`** — `RUN=0` beside `RC=0` is a VACUOUS GREEN. ⚠️ **A `-run` selector matching nothing prints `[no tests to run]` and EXITS 0.** ⚠️ **On `-v` output, unanchored `grep -c 'FAIL'` reads 11 ON A FULLY GREEN TREE** — use `grep -cE '^(FAIL|--- FAIL)|^ *--- FAIL'`.
7b. ⚠️ **`grep -c` ON ZERO MATCHES EXITS 1 — capture the value (`v=$(… || true)`), NEVER chain on it with `&&`.**
7c. ⚠️ **A GREEN TEST RUN IS NOT EVIDENCE A SITE IS UNPINNED — IT MAY BE DEAD CODE.** Prove it with a **`panic()` REACHABILITY CONTROL**. ⚠️ **The PLAN demonstrated one END TO END with a discriminating NC** (a legal response must NOT panic) — reproduce that pattern, do not skip the NC half.
7d. ⚠️ **NC EVERY NEW TEST, NOT JUST THE RISKY-LOOKING ONES.** At the phase-91 IMPL **three of the row's own new pins passed while being non-discriminating.** **A test that passes is not thereby a guard** — and this row's §10.2 measurement makes that acute: the validator moves ZERO existing tests.
8. ⚠️ **`pgrep -f` / `pkill -f` MATCHES YOUR OWN SHELL AND KILLS THE TOOL CALL (exit 144)**. **Kill ONLY PIDs you captured yourself** (from `$!`), and verify with `kill -0`.
9. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-*`, `wt-*` or stage worktree should outlive its stage close** — expect only `master` (verify with `git worktree list`).
10. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** (a)/(b) differential **121 fixtures** + full `go test ./...`, gated on **`PIPESTATUS[0]`** and a **SET RECONCILIATION** (⚠️ **NOT `INNER_EXIT`, which DOES NOT EXIST**); (c) h2spec cite ONLY from your own run — ⚠️ **h2spec is MEASURED BLIND to burst-drain ordering**; grpc-conformance deferred in writing; proxy-wasm 10/16; (d) fuzzers **55/48** today, **56/49** at row-done — ⚠️ **49 ONLY if the target lands in a NEW file; in the existing `fuzz_test.go` the FILE count stays 48. STATE WHICH AND MEASURE IT**; (e) the ANCHORED panic gate `^panic:|DATA RACE|SIGSEGV` on every differential launch; (f) no REVIEW.md — standing departure.
11. ⚠️ **THE FIXTURE INVOCATION: `go test ./test/differential/ -run 'TestDifferential/0004-h2-routing' -count=1 -v 2>&1`.** ⚠️ **Gate 2's failure mode is a SILENT PASS** — the runner `t.Skipf`s an unregistered fixture at `runner_test.go:200` and **NO fixture-count gate exists anywhere in the tree** (an absence established WITH a positive control that DOES find real count gates at `0070/driver_test.go:58`, `0004/driver.go:1061`, `internal/runtime/snapshot_test.go:85`). **ASSERT THE FIXTURE SET; confirm `=== RUN   TestDifferential/0004-h2-routing` APPEARS.**
12. ⚠️ **`-p` PUBLISHING WORKS; `--network host` IS WHAT DOES NOT.** ⚠️ A **STRICT_DNS** cluster whose name does not resolve **blocks listener init entirely**. ⚠️ **`clusters: []` is BOOT-REJECTED** even when every route is a `direct_response`. ⚠️ **envoy-go REJECTS `match.headers` outright**, ⚠️ **refuses `codec_type: HTTP2` without TLS unless `--allow-h2c`**, ⚠️ **boots with `-c`, NOT `--config-path`**, and ⚠️ **`access_log[].log_format` is BOOT-REJECTED** — `%RESPONSE_CODE_DETAILS%` cannot be requested on the subject at all. ⚠️ **`ENVOY_TARGET.md` is at `docs/envoy-go/ENVOY_TARGET.md`, NOT the repo root.**
13. ⚠️ **A RAW-FRAMER PEER MUST HOLD ONE `hpack.Decoder` PER CONNECTION.** ⚠️ **Set `ReadMetaHeaders` to NIL when probing malformed shapes**, or `x/net` validates away the very thing you are measuring. ⚠️ **`0004` is TLS+ALPN-h2, NOT h2c.**
14. ⚠️ **`bash /dev/tcp` + `timeout cat <&3` INSIDE COMMAND SUBSTITUTION RETURNS SILENTLY EMPTY.**
15. ⚠️ **GO DOES NOT FLAG UNUSED CONSTANTS.** Unused-symbol cleanup after a removal must be grep-driven.
16. ⚠️ **`golangci-lint`'s misspell runs in locale US.** **Sweep British spellings in `.go` comments BEFORE the gate.** ⚠️ **Markdown prose may use them freely; a `.go` comment may not.**
17. ⚠️ **A GREEN RUN OF A CONTENTION-SENSITIVE TEST PROVES NOTHING.** Phase 91's subject cleared 5/5 uncontended while failing 9/32 contended on the SAME binary.
18. ⚠️ **AN ABSENCE CLAIM NEEDS A POSITIVE CONTROL AND A DISCRIMINATING NEGATIVE CONTROL.** The PLAN's *"exactly ONE AST-audit golden exists"* is credible ONLY because the same search form **FOUND** it and three siblings were enumerated with their WALK TARGETS checked, not just their imports.
19. ⚠️ **DERIVE next-free ADR FROM THE TAIL, NEVER FROM THE HEADING COUNT.** The id space is SPARSE (one gap, `0209`, ZERO duplicates, re-audited at the PLAN) and **headings+1 reads 314 — the TAIL ITSELF, a TAKEN id.** ⚠️ **And the strict guard's UNANCHORED form OVER-counts — it reads 3 now, not the recorded 2.**
20. ⚠️ **`reference_measured_prototype_is_a_lower_bound` HAS NOW FIRED TWICE WITH THE SAME IDENTIFIED MECHANISM: UNDER-ENUMERATION OF *FILES*, NOT OF LINES.** The AST golden and the test-backend extension were both invisible to a prototype that did not RUN THE SUITE. **Run the suite before you publish a cost.**

## ⚠️ PORT BANDING

`net.ipv4.ip_local_port_range = 32768 60999`. ⇒ **BAND YOUR PROBE PORTS BELOW 32768.** `21000-24999` is clear. **Check availability with `ss -tan` (ALL states), never `ss -ltn`** — the listener-only form reads a fail-unsafe "free". **A `bind: address already in use` in a 4xxxx band is NOT evidence a sibling session is squatting.** ⚠️ **FIXTURE ports are FAMILY-BANDED as `10<index>` — but `10120` is TAKEN (fixture `0028`, `inputs/driver.go:65`); free in-band is `10126-10129`.** ⚠️ **`0120` as a DIRECTORY is free — but this row EXTENDS `0004` and mints nothing.**

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (**ugrep 7.8.4**). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Reproduced at **FORTY-TWO** separate tips — most recently at the phase-92 PLAN, where `command grep -rlF` returned four paths including `./next-prompt.txt` and the harness function returned the same three minus that file.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **THE METHOD BINDS; carry no standing discrepancy figure and no standing absolute.** ⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS; print FILE NAMES, never a count.** ⚠️ **THE HARNESS `grep` STRIPS A LEADING `./`.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files; use `-v --no-index`, and `git status` is the discriminating check). ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **`grep -c` COUNTS *LINES*, NOT *OCCURRENCES***.

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 92 (h2-response-header-validation)' --format='%h %s'   # 4 at this tip — BRAINSTORM + BRAINSTORM CORRECTION + SPEC + PLAN
git log --grep '^phase 92' --format='%h %s'                                  # the LOOSE form — RUN IT to CROSS-CHECK the slug form (see the warning below)
git log --grep '^phase 91 (h2-framer-partial-frame-desync)' --format='%h %s'  # 4 — a CLOSED phase, use as a FIRING CONTROL
```
⚠️ **The count comments are LIVE FIGURES — VERIFY before relying.** Take the NEWEST line (`| head -1`). A fabricated slug must read **0**.

⚠️ **THE SLUG-ANCHORED FORM HAS A BLIND SPOT AND IT BIT THIS PHASE. THE TWO CORRECTION COMMITS USE INCONSISTENT SUBJECT FORMS.** `97d7c519 phase 92 (h2-response-header-validation) BRAINSTORM CORRECTION` **carries the slug and IS matched**; `963d472d phase 92 PLAN CORRECTION` **does NOT carry the slug and is INVISIBLE to the slug-anchored grep** — which is why the slug grep read **4** while `^phase 92` read **7** at `963d472d`. ⚠️ **THE LOOSE COUNT IS A ROTTING FIGURE — IT INCREMENTS ON EVERY `next-prompt` ROLL** (those subjects begin `phase 92 next-prompt:`), so **do not compare it to any number quoted here; RUN BOTH AND RECONCILE THE SETS.** The SLUG count (**4**) is the stable one and does NOT move when a router file is rolled. ⚠️ **THE PHASE-92 TIP IS `963d472d`, NOT the `e69f1c58` the slug form returns as its newest.** ⇒ **ALWAYS RUN BOTH FORMS AND RECONCILE THE SETS**; a `| head -1` on the slug form alone silently returns a NON-TIP commit. ⚠️ **AND WHEN YOU LAND A CORRECTION, PUT THE FULL SLUG IN ITS SUBJECT** so the next reader's locate form can see it.

Execution style: **subagent-driven per `feedback_execution_style`**. Subagents commit LOCALLY on the ONE stage branch with EXPLICIT PATHSPECS; the controller squashes (`feedback_subagents_no_push`, `feedback_push_to_origin`). **Pure MEASUREMENT agents should commit NOTHING and prove their tree clean.** ⚠️ **PARALLEL AGENTS IN ONE WORKTREE WORK IF AND ONLY IF THEIR PATHSPECS ARE DISJOINT** — ⚠️ **the phase-90/91/92 pattern was PER-AGENT DETACHED WORKTREES pinned at a known commit**, which let three independent probes run at the PLAN without any of them seeing another's edits. ⚠️ **GIVE EACH PARALLEL AGENT ITS OWN PRIVATE SCRATCH DIR.** ⚠️ **ENUMERATE LIVE AGENTS BEFORE TEARING DOWN ANY WORKTREE THEY WERE GIVEN.** ⚠️ **AND VERIFY WHAT A SUBAGENT REPORTS** — at the SPEC two agents contradicted each other and the controller had to identify the TOOL to reconcile them; at the PLAN an agent correctly REFUSED to guess between two cost figures and the controller resolved it by commissioning a single combined prototype instead of picking one. Per-task `gofmt` + `golangci-lint` on touched packages, **gated on OUTPUT**. ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root** — build with `-o` into scratch.

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root — **observed repeatedly at every phase-88 through -92 stage, including twice during this PLAN**. Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` before any commit or gate run. ⚠️ **Shell VARIABLES do not survive between Bash calls** — re-export in every command. ⚠️ **AND AN UNQUOTED HEREDOC (`<<EOF`) COMMAND-SUBSTITUTES BACKTICKED TEXT** — a backticked `406` in a patch script silently vanished at this PLAN. **Use `<<'EOF'` and pass paths via the environment.**

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — `git status --porcelain` AND `git diff --stat master` must show ONLY your intended edits. For a patched TRACKED file: `sha256sum` BEFORE patching, `git checkout -- <path>` after, `sha256sum -c` the capture — ⚠️ **and COMMIT FIRST, because `git checkout --` restores from HEAD and will wipe uncommitted work.** ⚠️ **All three phase-92 PLAN probe agents did this and every restore verified `OK`.** ⚠️ **Also check the MAIN repo root.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads the phase-92 IMPL.** ⚠️ **§Project counts is STALE (phase-76 era: fixtures 119, ADR tail 0298, stat surface 1205) and SELF-CONTRADICTS §Current with NO label saying so — do not anchor on it.**
2. `docs/envoy-go/phases/92-h2-response-header-validation/PLAN.md` (**1482 lines**) — **§4 (the FROZEN DESIGN), §6 (the test roster), §7 (the NC campaign), §10 (cost + the split gate), §14 (the 23 ordered tasks)**.
3. `docs/envoy-go/phases/92-h2-response-header-validation/SPEC.md` — §4, §7, §10, §13. ⚠️ **THREE of its figures are SUPERSEDED by the PLAN (`+74`/`+77`, `406`, `http2_tx_reset`) and one of its legs (`te`) shipped unmeasured. The PLAN is authoritative where they disagree.**
4. `docs/envoy-go/DECISIONS.md` **ADR-0314** (PROPOSED, §Context only) — ¶4-¶7 carry the load-bearing findings. Also **ADR-0312 §Consequences (xix)** and **ADR-0313 §Consequences (ix-bis)**.
5. `BOOTSTRAP_PROMPT.md` §4-§7 (repo ROOT). **Phase-92 dir has `BRAINSTORM.md` + `SPEC.md` + `PLAN.md` and no `PROGRESS.md` ⇒ state 3 ⇒ the IMPL.**

## What the next session owes
**RUN THE PHASE-92 IMPL** (state 3 -> DONE), working `PLAN.md` T1 -> T23 and appending to a NEW `PROGRESS.md` per task ·
**RE-RUN the sentinel + all four NCs yourself** and record ACTUAL output — ⚠️ **BOTH BEFORE AND AFTER you flip row 92, because the flip changes every shape** ·
**DO NOT create `stop`** — two checks block it pre-flip; **re-evaluate AFTER the flip** (check (2) will still print SIX, so it still does not fire) ·
**ROW 92 FLIPS `in-progress` -> `done`** at `ROADMAP.md:154` — a FLIP, not an ADD; `want` STAYS **124**; **count the row's fields BEFORE installing the edit** (NF=8 under both forms) ·
**ADR-0314 gains §Decision + §Consequences APPENDED IN PLACE after the RETAINED italic footer** — no renumber, **NO `---`** (`^---$` STAYS **216**), strict guard **DISARMED 1 -> 0 verified BY LINE AND ADR**, the **ADR-0231 decoy LEFT ARMED** ·
**`BEHAVIOR_CONTRACT.md` gains the counter rider** — state the line delta (it is **5962** today); ⚠️ **do NOT write `406 -> 407`; assert the +1 DELTA and, if an absolute is wanted, use the contract's own ledger form against 1207** ·
**REFUTE YOUR PREDECESSOR BY EXECUTION** — the 92 PLAN refuted ten, three of them its own SPEC's ·
`STATE.md` rolled IN PLACE with the oldest §Recent entry evicted by a **DIRECT DATE READ** (⚠️ **read the dates yourself every time — the PLAN found a UNIQUE oldest with NO tie, the FIFTH distinct shape in five stages and NOT the SPEC's two-way tie at the same date; the shape does NOT carry**) and archived to `STATE_HISTORY.md` as **ONE INLINE LINE** whose label does NOT match `^- \*\*prior active-phase:\*\*`, so the strict guard stays at **163, DELTA 0** ·
`next-prompt.txt` rolled (`git add -f`) · ONE squashed commit, merged and pushed — ⚠️ **and if a late measurement invalidates something you already pushed, land a CLEARLY-LABELLED correction commit rather than force-pushing or leaving it standing.**
