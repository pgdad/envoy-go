=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

**THERE IS BANKED MID-LIFECYCLE WORK AT THIS TIP AND ADVANCING IT IS THE STAGE.** Phase 79 (`stats-prometheus-projection`) is **OPEN**: row 79 is `in-progress`, and `BRAINSTORM.md`, `SPEC.md`, `PLAN.md` and `PROGRESS.md` are ALL landed. **This session performs the phase-79 IMPL.** Do NOT open a new subject.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=111 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **`want=111` IS A LIVE FIGURE, NOT A CONSTANT.** It is the ROADMAP data-row count. **A row-adding phase MUST bump it in the same commit that adds the row** — otherwise every later session gets `GATE FAIL: examined 112 data rows, expected 111`. That noise is deliberate: a wrong denominator must be loud, because a silent one is how check (1) failed in the first place. **The phase-79 BRAINSTORM bumped 110 -> 111 when it chartered row 79. The SPEC and the PLAN each added NO row and did NOT touch `want`. The phase-79 IMPL adds NO row either and MUST NOT touch it — it FLIPS row 79 to `done`.**

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. When you open a new family, add its row-summary slug here.

**SENTINEL RE-RUN MECHANICALLY 2026-07-28 AT THE PHASE-79 PLAN. It does NOT fire. `stop` was NOT created and MUST NOT be.** ⚠️ **Re-run it yourself anyway and record what YOU see** — a prior run's result is not evidence for the current tip. ACTUAL output, recorded rather than predicted:
- **(1) `NOT DONE: row 79`.** **No `GATE FAIL` at `want=111`.** Negative controls: `want=109`, `want=110` AND `want=112` each ⇒ `GATE FAIL: examined 111 data rows, expected <want>`.
- **(2) FIVE, at `:189`, `:199`, `:209`, `:215` (long form) and `:223` (the Operational-tooling short form) — UNCHANGED.** **Row 79 narrows NOTHING and that is STATED rather than forecast.**
- **(3) `NEVER OPENED: gRPC` and `NEVER OPENED: WASM`** — unchanged. NC: an invented slug ⇒ `NEVER OPENED: ZZZ-nonexistent`.
- Input measured at **227 lines / 1 003 291 bytes / 13** bare `candidates:` hits (against the sentinel's own narrower 5), so an empty result could not read as a zero result.

⚠️ **`ROADMAP.md` WAS BYTE-UNTOUCHED AT BOTH THE SPEC AND THE PLAN**, so the leak-check hazard has been dormant for two stages. **IT RE-ARMS AT THIS IMPL**, which flips row 79 to `done`: the new cell text must contain ZERO occurrences of the two deferred-candidate phrases and no unregistered `<Family>-family row` slug, each grep negative-controlled against a deliberately doctored copy. **`grep` cannot tell a mention from a use.**

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`.** Every document from phase 75 forward recorded its blind spot — **109 data rows / 105 matched / FOUR misses** at an **em-dash** in an "after" cell, a **DOT** in the slug `http-1.1`, and two **LETTER-suffixed** ids `28.1a`/`28.1b` — and every one of them called it *"currently HARMLESS, since all four missed rows are `done`."*

⚠️ **Nobody fired the unsafe arm until the phase-77 PLAN. EXECUTED on scratch copies: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** A false "done" ends the project early. ⚠️ **DO NOT restore the regex form.**

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

The old check (2) matched ONLY the long phrase and could not see the **Operational-tooling** family's three genuinely un-chartered items, recorded in the **SHORT** form. Fixed at the phase-77 BRAINSTORM, at the MATCHER; negative controls all EXECUTED. ⚠️ **Run the union form above, never one half of it** — an agent running only the long-phrase regex sees FOUR lines, not five.

⚠️ **Normalising that Operational-tooling paragraph to the long form is a NAMED, DELIBERATELY-UNTAKEN follow-up.**

⚠️ **THE DOCUMENTED TRAP'S STATED MECHANISM IS REFUTED, WHILE THE TRAP IS REAL.** `reference_sentinel_deferred_sentence_live_vs_historical` says a naive `grep -c 'candidates:'` inflates *because of* the `candidates were:` recaps. It does inflate — but **not for that reason**: `candidates were:` does not contain that substring **at all**. The inflation comes from **ROADMAP table rows carrying BRAINSTORM prose**. **Cite the command; do not re-inherit the explanation.**

⚠️ **AND THE MEASURED RECORD: CHECK (2) HAS NEVER GONE DOWN** across ~22 phases (history `0 -> 1 -> 3 -> 4 -> 5`). **Phase 74 landed three `ssl.*` stat names and made its OWN sentence LONGER**; **phase 73 predicted a clause REMOVAL and got a clause REPLACEMENT**; **phase 77 opened a family and narrowed nothing**; **phase 78 narrowed nothing at ANY of its four stages**; **and phase 79 has now narrowed nothing at its BRAINSTORM, its SPEC and its PLAN** — the candidate sentence keeps its text until the row CLOSES. => **the candidates sentence is a WINDOW onto a larger deferred backlog, not an inventory of it.** ⚠️ **A stage that predicts a decrease here is repeating the phase-73 error. State what your row narrows — usually nothing — rather than forecasting.**

⚠️ **NEVER WRITE A SENTINEL'S OWN MATCHER STRING INTO A FILE THE SENTINEL GREPS.** The sentinel greps **`docs/envoy-go/ROADMAP.md`** — writing these strings *here* is safe; writing them *there* is not. **THIS IS LIVE THIS SESSION.**

⚠️ **THE SELECTION CALCULUS, CORRECTED AND STILL STANDING.** *"A candidate that clears a check-(3) blocker is worth **strictly more**"* does **not** hold: opening a family **MINTS** a check-(2) sentence, so phase 77's own arithmetic was **NET ZERO**. What survives: **opening a family is the ONLY way that family's work becomes enumerable at all — a prerequisite, not a shortcut.** Do not restore the stronger claim.

==============================================


## STATUS AS OF 2026-07-28: PHASE 79 IS **OPEN**. ROW 79 IS `in-progress`. THIS SESSION PERFORMS THE **PHASE-79 IMPL**.

**What the phase-79 PLAN landed** (`git log --grep 'phase 79'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): **FIVE files — ONE MORE than the phase-76/77/78 PLAN precedent, for a stated reason** — `docs/envoy-go/phases/79-stats-prometheus-projection/PLAN.md` (NEW), `PROGRESS.md` (NEW), `STATE.md` (§Current rolled IN PLACE per ADR-0288), **`STATE_HISTORY.md` (414 -> 416)**, and this file. ⚠️ **Those three prior PLAN commits were each exactly FOUR files and none touched `STATE_HISTORY.md`** (verified by `git show --stat` on `9b0077b9`, `acedfd2b`, `62588ade`). This one is five because **§Recent lineage was AT its five-entry cap AND the bullet it evicts had never been archived** — see item 4 below. **`ROADMAP.md`, `BEHAVIOR_CONTRACT.md` and `DECISIONS.md` BYTE-UNTOUCHED. ZERO production `.go`, ZERO test `.go` committed.** Four investigation agents on disjoint remits, each in its own detached worktree with private scratch and a private port band, plus controller re-derivation.

### What matters for the NEXT session, in order

1. **⚠️ RE-RUN ALL THREE SENTINEL CHECKS YOURSELF AND RECORD THE ACTUAL OUTPUT; DO NOT PREDICT IT.** Create `stop` if and ONLY if all three print nothing. **Check (1) is LIVE** (`NOT DONE: row 79`) until THIS session flips the row. `want` STAYS **111** — the IMPL adds no row. ⚠️ **RUN THE SENTINEL AGAIN AFTER THE `ROADMAP.md` EDIT**, and run the leak-check then too.

2. **⚠️ THE SUBJECT IS FIXED AND THE STAGE IS THE IMPL. DO NOT RE-OPEN THE SEARCH, AND DO NOT RE-LITIGATE THE D-SPP DOCKET — IT IS DISPOSED.** Read `PLAN.md` in full; it is the input, and its §1 ledger **supersedes the SPEC wherever they disagree**. ⚠️ **But a PLAN's citations are not evidence either** (`feedback_brief_citations_not_evidence`) — **re-derive every load-bearing figure at YOUR tip.** The record across phases 78 and 79 is that **every stage has found a load-bearing error in its predecessor, and each was found by EXECUTION, never by review.** The SPEC refuted nine BRAINSTORM claims; **this PLAN refuted or corrected ELEVEN SPEC claims and added SIX findings no phase-79 document carried.** **Budget for finding one more.**

3. **⚠️ THE PLAN'S TWO HEADLINES — an IMPL that loses either will ship a wrong or a vacuous row.**
   - **HEADLINE ONE — THE ROW'S OWN FIX IS INCOMPLETE IN EXACTLY THE WAY THE DEFECT IT REPAIRS IS.** SPEC §10 says *"Fixture (**1** file)"*. The stale top-level enumeration lives in **FOUR**: `0118/driver/driver.go:154-156`, `expectations.yaml:43-46`, `envoy.yaml:16`, `README.md:32-35`. ⚠️ **And `driver.go:155-156` is wrong TWICE** — it omits `wasm.` **and** `kafka.`, and it lists **`rbac.` as a top-level prefix** when `.rbac.` is an **INFIX** detector (proven live: `ANYTHING_AT_ALL.rbac.allowed` parses clean, and that token appears nowhere in the source; counting `rbac.`/`zookeeper.` as top-level gives **eleven**). ⇒ **THREE mutually inconsistent wrong counts are live in-tree** — `name.go:350` says FOUR, `BEHAVIOR_CONTRACT.md:5020` says FIVE, the 0118 files say FOUR-plus-a-wrong-list — **and they must move to NINE together.** The PLAN promotes this to **its own task, T3**. **Fix by PATTERN, not by line** (`reference_stale_cite_recurs_fix_by_pattern`).
   - **HEADLINE TWO — A GATE CELL THAT CANNOT FAIL; broken-gate shape FIFTEEN.** The mandatory §3.7 hoisting negative control was RUN at the PLAN on real wire bytes and **FIRES on all four sinks for BOTH injection sites** (`tracing.` and `access_logs.`; identical per-consumer diff-line counts ⇒ **not** site-dependent; verbatim `|#envoy.tracer_name:opentelemetry`, `;envoy.tracer_name=opentelemetry`, `label:{name:"envoy.tracer_name"}`). **But inside OTLP it is CELL-dependent:** `(useTagExtractedName=F, emitTagsAsAttributes=F)` is **byte-identical under the NC** (1144 -> 1144) ⇒ **a golden on the default knobs is VACUOUS**; and `(T,F)` **SHRINKS** (1128 -> 1086) ⇒ **do NOT assert monotonic byte growth.** ⚠️ **AND WITH THE HOISTING ARM APPLIED, TODAY'S WHOLE SUITE IS GREEN** (`go test ./internal/stats/... ./internal/statssink/... -count=1` INNER_EXIT **0**) — nothing in the tree distinguishes byte-mirror from tag-hoisting, which is what makes T7 load-bearing rather than decorative.

4. **⚠️ A FINDING OUTSIDE THE ROW'S SUBJECT THAT NEVERTHELESS BINDS THIS SESSION'S FILE SET: 40 LINEAGE BULLETS WERE DELETED, NOT ARCHIVED.** ADR-0288 §Decision 3 mandates that the sixth `§Recent lineage` entry **move to `STATE_HISTORY.md`**. Audited mechanically — all **210** distinct `- **prior active-phase:**` keys ever REMOVED from `STATE.md`, checked against both files today — **40 are in NEITHER.** `STATE_HISTORY.md`'s archived range is `38 … 66, 76, 77, 78`, so **phases 67 through 75 are wholly absent.** Negative-controlled both ways (every lost slug ⇒ `HIST=0` on a *loose* slug grep; `boot-panic-visibility` ⇒ 2, `runtime-static-layer` ⇒ 1). ADR-0288 §Context establishes the content is triply redundant, so nothing is unrecoverable — **what is false is the archive's claim to be the complete record.** ⚠️ **THE PHASE-79 PLAN HONORED THE RULE FOR ITS OWN EVICTION** (`phase 77 … SPEC done` moved VERBATIM, 10 989 bytes; `STATE_HISTORY.md` 414 -> 416), **which is why its file set is FIVE.** ⚠️ **THIS IMPL EVICTS ANOTHER BULLET AND MUST DO THE SAME.** The backfill of the 40 is a **NAMED DEFERRAL** (`PLAN.md` §7.3) — do NOT do it under a phase-79 commit message.

5. **⚠️ ANCHOR DRIFT THE PLAN FIXED — SEVEN, and three had propagated into three documents each. Use these, not the SPEC's.**
   - **`scrapeProm` is `:407-458`, NOT `:407-447`.** The short form truncates 11 lines, cutting the timestamp strip, the `ParseFloat`/NaN guard **and `return out, nil`**. Wrong in `SPEC.md:157`, `SPEC.md:335` and the prior `next-prompt.txt:107`.
   - **The both-forms parser is `driver.go:429-443`, NOT `:399-403`** — `:399-403` is **doc-comment text with ZERO executable lines**, so SPEC §3.5's parity-flip justification currently cites a comment.
   - **`normalizeAddr` is DEFINED at `internal/listener/manager.go:352`** (doc `:331`); **`:381` is its sole CALL SITE**, the first line of `registerListenerMetrics` (declared `:380`). T6's roster derivation depends on this.
   - **SPEC §10's "Fixture (1 file)" is FOUR** (item 3).
   - **`BEHAVIOR_CONTRACT.md:5020` does NOT close with the bolded "goes RED on purpose" clause** — two clauses follow (`…naming the follow-up. A prose-only deferral would have rotted; this one cannot.`). **A byte-anchored edit keyed on "closes with" will miss.**
   - **`STATE.md` §Project counts SELF-CONTRADICTS §Current inside the same file** — `:31` fixtures 119 (real **120**), `:33` surface 1205 (lineage **1207**), `:35` tail ADR-0298 / next-free ADR-0299 while `:21` says tail **ADR-0301** / next-free **ADR-0302**. **Deliberately NOT repaired. Anchor on §Current, which IS live.**
   - `STATE.md:44`'s stale *"§Current carries the phase-79 BRAINSTORM"* — **repaired at the PLAN.**

   **NOT drifted, re-derived and confirmed:** all 12 `name.go` anchors (`ExtractTags` `:47`, `switch {` **`:50`** — ⚠️ **NOT `~:100-110`, which is inside the `wasm.` comment block `:98-125`** — arms `:51 :61 :83 :93 :97`, `default:` `:132`, terminal error `:350`, `flattenToProm` `:370` / call `:371`, `helpText` **`:458-476`** = **15** entries, prose `:448`, SN-rule block `:24-46`) · the four `CutPrefix` cites `:286 :306 :323 :340` · `prom.go` comment `:18-22`, skip block `:39-41` with the bare `return` at `:40` (⚠️ **a per-metric `continue` inside the `Walk` closure opened `:37` / closed `:53`, NOT an early exit from `WriteProm`**), degradation site `:59-61` · `registry.go` `Walk` `:138-142` / `getOrRegister` `:179` · `name_test.go` 959 lines, 55 `^func Test`, 56 top-level funcs, 0 `^func Fuzz`, golden `:10`, `TestHelpText_Coverage` `:195`, slice `:196-207`, membership `:209`, "no reverse direction" `:231-236`, non-colliding negatives `:141` and `:956` · the four `statssink` call sites · **EXACTLY FIVE non-test `ExtractTags` call sites repo-wide** · the five `TestNoNewStat*` guards at `internal/statssink/registration_test.go:26 :53 :81 :109 :137` as the exhaustive set.

6. **⚠️ FURTHER SPEC CORRECTIONS THE IMPL MUST CARRY.**
   - **§3.3's `1151 bytes` is REFUTED AS STATED -> `1180`.** Under the SPEC's own message shape the 30-name roster emits **1180 B**; 1151 is reachable only with a bare-comma join, and `", "` gives 1180 — **delta exactly 29, the separator count.** ⚠️ **The SPEC never pins the format string or the separator, so the figure is NOT reproducible from the document and a gate asserting it is UNFALSIFIABLE. T4 PINS BOTH.** The **1 line** half is confirmed.
   - **§3.6's matrix must be stated as fired-leg SETS:** NC-1a -> **leg 3 ONLY** · NC-1b -> **legs 1 AND 3** · NC-2 -> **legs 1 AND 2**. ⇒ *"the two over-fire arms trip DIFFERENT legs"* is **partially refuted** (leg 3 fires in both) — **but leg 3's justification gets SHARPER: NC-1a fires leg 3 and NOTHING ELSE, so without leg 3 it passes entirely.** The SPEC's conclusion (a positive-only test passes all three NCs) **stands**.
   - **§3.7's NC total is 110 diff lines, NOT 152.** **§3.7's PROM bytes `304 -> 2171` are NOT reproduced** — measured **`380 -> 2246`** with a registration-derived roster; **line counts `2 -> 12` ARE confirmed.** ⚠️ **Byte counts depend on the unspecified control roster — do NOT pin them.** **OTLP `1154 -> 1292` absolutes not reproducible; the `+138` delta IS confirmed and pins to ONE cell.**
   - **§3.2's secret->fixture attribution is SWAPPED** — `edf_validation_ca` is **`0110`**, `rccf_validation_ca` is **`0111`**. The *set* is right, so the 30-name total and the 79.1 deferral are unaffected. **Do not let 79.1 inherit it.**

7. **⚠️ THE NINE-SEGMENT CLAIM SURVIVES — BUT T2 OWES A SPECIES ADJUDICATION BEFORE IT DRAFTS ITS GUARD.** NINE is CONFIRMED by two methods with **different blind axes**: a static audit of all 46 lines consuming the input across `ExtractTags`' 314-line body (**9** root detectors, **4** infix, nothing else; the pipeline's own NC exits 1) and a **48-token** live acceptance probe against a byte-identical copy (md5 verified both sides) whose accepted set is exactly `cluster http kafka listener mongo redis server thrift wasm`. **No tenth. None spurious.** ⚠️ **BUT a name reaching `:350` has failed THIRTEEN detectors — nine top-level PLUS four MID-NAME** (`.http_local_rate_limit.` `:147`, `.http_bandwidth_limit.` `:185`, `.rbac.` `:236`, `.zookeeper.` `:265`). *"Nine recognized top-level segments"* is true about top-level segments and **incomplete about why the name was rejected.** **Decide explicitly what the string claims, record the decision, THEN write the byte-stable guard** — otherwise the guard pins a **SIXTH** generation of an under-specified list, which is the exact failure mode the fix exists to prevent, one generation on.

8. **⚠️ THE BAND: 10-13, BUDGET 12 — and the LoC estimate is the load-bearing half.** Floor and budget agree with SPEC §9; **the ceiling does not** — §9 priced the probability that any task splits at ZERO, in a lineage where the immediately preceding row did exactly that. Three named, mutually independent ceiling mechanisms: **(a)** T7 splits OTLP off (three of four sinks are no-op BY CONSTRUCTION — `label.go:39`'s `err != nil || len(labels)==0` guard and `dogstatsd.go:86`/`graphite.go:70`'s literal `residual, labels = fam.GetName(), nil` fallback are byte-identical to a byte-mirror return; **`otlp.go:189-197` is the ONLY different-route consumer, and it is the one with the inert cell**); **(b)** T8 splits (label-aware `scrapeProm` is a **52-line** parser rewrite five other assertions consume); **(c)** T11 becomes two if the full suite or `-race` reds. ⚠️ **`~500 LoC` IS REFUTED TO `~875`** by two convergent methods — lineage ratios **2.04x** (phase 77: estimated ~700, shipped **+1428 `.go`**) and **1.50x** (phase 78: estimated ~330, shipped **+495**) — so *"<< ~1500"* becomes **"inside, ~1.7x margin"**. Corroborating: §9 budgets ~300-400 test lines **in total** while T7 alone is ~350, against existing `internal/statssink` goldens of 419/430/471/542/773 lines. ⚠️ **THE NORMATIVE SPLIT GATE IS `BOOTSTRAP_PROMPT.md` §6.1 at `:285-290`**, which the canonical `25 tasks`/`1500 LoC` grep **MISSES** (different numeral spelling); `:225` and `:472` are flow reminders. ⚠️ **§6.1 carries a THIRD trigger no prior stage discharges — mid-execution split when a task's sub-steps exceed ~10 items — LIVE on T7 and T8.** **Calibration, re-derived first-hand from primary documents: 76 SPEC 7-9 -> PLAN 9 (AT) · 77 SPEC 11-13 -> PLAN 12 (INSIDE) · 78 SPEC 7-9 -> PLAN 10 (ABOVE) — TWO-for-three.** ⚠️ **The banked `ROADMAP.md:209` figure is `11-14`**, read with **CHARACTER** offsets (@45763 of a 45 959-char / 46 423-**byte** line carrying SIX bands; `cut -c` is byte-based and truncates mid-word), and it covers **FOUR** arms including the deferred `sds.` one — **so 9-11 and 10-12 are both re-scopes DOWNWARD and 10-13 sits between them.** ⚠️ **RE-DERIVE IT AGAIN ANYWAY** (`reference_deferred_candidate_cost_restale`).

9. **⚠️ TWO BLAST-RADIUS FINDINGS NO PRIOR PHASE-79 DOCUMENT CARRIED.**
   - **`WriteProm` is called inside a FUZZ TARGET** — `internal/stats/fuzz_test.go:73`, inside `f.Fuzz(...)`. A skip-site `log.Printf` would fire **per fuzz iteration** under the short-budget CI run. §3.3's *"only non-test **production** caller"* is true and the qualifier is load-bearing. The target synthesizes `listener.<addr>.downstream_cx_total`, which projects via SN3, **so the line should not fire — but that is a PREDICTION and T5 EXECUTES it**, both directions (a `runtime.`-shaped seed must fire). **Three further test callers exist** (`prom_test.go`, `name_test.go:913`, `bandwidthlimit_test.go:1978`).
   - **The prometheus blast radius is ~30 fixture drivers, not one.** `setRuntimeStats` is called on **both** boot success paths including the `lr == nil` branch (`bootstrap.go:686` as well as `:734`), so registration is **unconditional** and **every subject's `/stats/prometheus` gains 6 lines** once the `runtime.` arm lands. `grep -rln 'stats/prometheus' test/` ⇒ **62 files across ~30 fixtures**; the parse shapes were audited and no exhaustive-set or count assertion was found, so it is *probably* benign — ⚠️ **but it is UNMEASURED and only the full 120-fixture run discharges it.** **This is why the full-differential mandate is right, for a reason SPEC §9 does not state.**

10. **⚠️ THE FULL 120-FIXTURE DIFFERENTIAL IS MANDATORY** — `internal/stats` links into `cmd/envoy-go`, built at **THREE** sites: `test/differential/harness.go:240`, `:594`, and `test/conformance/h2spec/h2spec_test.go:210` (`TestH2Spec` entry `:30`). ⚠️ **h2spec is a FOURTH consumer of the same binary and is NOT covered by `./test/differential/`** — run it explicitly rather than excluding it silently. Budget **~400-420 s** per green attempt; `-race` is a **SECOND** run, not a substitute. **Verified at this tip: fixture dirs 120, blank imports 120, `comm -3` EMPTY.** The mandated recipe:
    ```sh
    ( go test ./test/differential/ -count=1 -v > "$SCRATCH/full.log" 2>&1; echo "INNER_EXIT=$?" )
    grep -cE '^    --- PASS: TestDifferential/' "$SCRATCH/full.log"          # want 120
    grep -E  '^    --- (FAIL|SKIP): TestDifferential/' "$SCRATCH/full.log"   # want EMPTY
    grep -c  'no driver registered for fixture' "$SCRATCH/full.log"          # want 0
    grep -o  'TestDifferential/[^ ]*' "$SCRATCH/full.log" | sed 's|TestDifferential/||' | sort -u \
      | comm -3 - <(ls -1 test/fixtures/ | grep -E '^[0-9]{4}[a-z]?-' | sort)  # want EMPTY
    ```
    **The `comm -3` cross-check is the load-bearing gate, not the raw count** — 120 with one fixture renamed and another skipped still reads 120. ⚠️ **The faithful dir predicate is `^[0-9]{4}[a-z]?-`** — a bare `^[0-9]{4}-` gives **118**, missing `0007a-cors` and `0007b-iteration-probe`.

11. **⚠️ THE `+0` ARGUMENT MUST BE STRUCTURAL, AND THE `TestNoNewStat*` BLINDNESS IS PROVEN TWICE OVER.** All five guards live in `internal/statssink/registration_test.go` (`package statssink`, the exhaustive repo-wide set) and none reaches `internal/stats`. Re-executed at the PLAN with a **failing baseline**: **Arm A** — a genuine new registration injected inside `internal/stats` ⇒ all five `--- PASS`, `GUARD_EXIT=0`, ⚠️ **and the SAME BUILD gave `DEADLOCK_PROBE_EXIT=1`** with a goroutine trace, so they are blind not only to a stat-surface regression but to a **process hang in the code they nominally cover**; **Arm B** — the same registration moved to `NewRegistry()` ⇒ all five `--- FAIL`, `GUARD_EXIT=1`. ⇒ Arm A's green is **genuine blindness, not a no-run**, and it **cuts both ways**. **Enumerate the diff's registration call sites and show the set is empty.** Adding a `switch` arm is a **PROJECTION** change, not a **REGISTRATION** change. **Stat surface 1207 is DOCUMENTARY and CONFIG-CONDITIONAL — assert the DELTA.**

12. **⚠️ THE DEADLOCK, AND ITS ESCAPE HATCH, ARE BOTH CONFIRMED.** `Registry.Walk` (`registry.go:138-142`) holds `r.mu.RLock()` across every `fn` call; `getOrRegister` (`:179`) takes `r.mu.Lock()`; Go's `RWMutex` is not reentrant. A lazily-registered counter at `prom.go:40` ⇒ `panic: test timed out after 20s` with a trace naming **both** lock sites. ⚠️ **The DISCRIMINATING control was also run**: the same counter **pre-registered eagerly OUTSIDE `Walk`** and only `.Inc()`'d inside ⇒ `--- PASS`, `INNER_EXIT=0`. So the mechanism is exactly as stated **and the SPEC's escape hatch is sound** — it is simply a different leg shape than the one this row ships. **D-SPP-3 stays resolved to the aggregated LOG; `sync.Once` stays REJECTED on executed grounds; `+0` is UNCONDITIONAL.**

13. **⚠️ ADR + DOCUMENT CONVENTIONS.** **ADR-0301 §Context is LANDED and BYTE-UNTOUCHED at the PLAN**; **§Decision + §Consequences append IN PLACE at THIS IMPL** after the retained footer (**ADR-0044-as-used**; ⚠️ ADR-0044 does **not** itself contain that discipline), **no renumber, no `---` separator**. Next-free is **ADR-0302** (`grep -c '^## ADR-0302'` ⇒ **0**; control `^## ADR-0301` ⇒ 1; tail at `:17598`, STATUS `:17600`). ⚠️ **ADR-0301 carries NO whole-file grep count** — that species self-falsified in ADR-0296 ¶3 and twice in ADR-0297. ⚠️ **THE ADR RANGE-EXTRACTION HAZARD IS REFUTED AT THIS TIP** — `^## ADR-0107` matches **1** line (`:4304`), `:4858` begins `##` at byte 0, **zero** lines begin space-then-`#`. **`^## ADR-` is a safe anchor.** *(ADR-0209 has no heading by design; `## ADR-0127 v2:` at `:5973` is the sole heading matching neither form.)* ⚠️ **CITE THE SIX-GATE CORRECTLY: `BOOTSTRAP_PROMPT.md` §7.5, heading `:357`, gates (a)-(f) at `:360-365`, closing sentence `:367`**, at the **REPO ROOT** with the `.md` extension. **ADR-0106 does NOT define the six-gate** (it governs the SOLE-leg property). ⚠️ **ADR-0045 (`DECISIONS.md:1466`) does not STATE the split gate — it QUOTES it at `:1475`; and the NORMATIVE statement is `BOOTSTRAP_PROMPT.md` §6.1 at `:285-290`** (item 8). **Citing the figures to ADR-0045 alone is a laundered cite.**

14. **⚠️ THE IMPL'S FILE SET.** Production **2** (`internal/stats/{name.go,prom.go}`) · test **3** (`internal/stats/name_test.go` + a NEW observability test in `internal/stats` + a NEW sink-identity golden in `internal/statssink`) · fixture **4** (all of `test/fixtures/0118-runtime-static-layer/{driver/driver.go,expectations.yaml,envoy.yaml,README.md}`) · plus `internal/admin/stats.go:20` (the stale `(the redis. Prometheus tag-extractor arm is 32.2)` parenthetical — **that arm LANDED at `name.go:323`**) · docs `DECISIONS.md` (ADR-0301 §Decision + §Consequences) · `BEHAVIOR_CONTRACT.md` (§7's four edits, `:5020` **MANDATORY**) · `ROADMAP.md` (row 79 -> `done`) · `STATE.md` · **`STATE_HISTORY.md`** · this file. **Row 79's shape is canonical and re-verified: 7 pipes, `NF=8`, field 8 EMPTY, pipe-terminated.**

15. **⚠️ THE STANDING GATE HAZARDS — COUNT FIFTEEN.** A full-suite recipe **WITHOUT `-v` is VACUOUS** · a sha256 roster built from one tree is **desynced BY CONSTRUCTION against a DELETED file** · `gofmt -l` **NEVER exits non-zero** (gate on OUTPUT) · `go doc -all <A> <B>` **silently swallows arg2** · a **`+0 exported symbols`** gate over an EMPTY package goes RED on a CORRECT tree · a **RANGE** gate cannot detect anchor drift · a roster's naive `[ -f ] || continue` **exits 0 on a DELETED file** · a **count-only** stat guard **PASSES a build with BOTH names wrong** · a `-run` no-match **exits 0** with `[no tests to run]` · a `--- PASS` tally over a package with sibling tests **exceeds the fixture denominator** · a stat-delta claim **cannot be discharged by guards scoped to another package** · a **stderr-VOLUME** assertion **passes on the hang** · `golangci-lint` runs **`misspell` with `locale: US`** — ⚠️ **LIVE this row (it edits Go comments AND an error string); it FIRED at the SPEC on *"signalled"*. Do not paste SPEC or PLAN prose into code** · **a harness's exit code is not the command's** — capture `INNER_EXIT` · ⚠️ **AND THE FIFTEENTH, FOUND AT THE PLAN: A GATE CELL THAT CANNOT FAIL.** T7's OTLP `(F,F)` cell is byte-identical under the hoisting NC. **This is distinct from every prior entry — the command is right, the assertion is right, and the CELL is inert.** It was found only because the NC was run across the **full cross-product** rather than on the default configuration (`reference_probe_must_discriminate`).

16. **⚠️ A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX — the FOURTEENTH consecutive stage at which this held.** The index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Live hazards: the full-suite startup flake (`subject ready: EOF` **and** `bind: address already in use`, both failing **BEFORE any assertion**, the latter a **PANIC that can abort the whole binary**, firing **MORE readily under `-race`** and as the fixture count grows — now **120**; hardened at `0e9cc680` **for the SUBJECT PROXY ONLY**) · `reference_sds_init_fetch_timeout_dial_budget_flake` — **TWO packages** · the PRE-EXISTING `internal/cluster` `-race` outlier flake (`TestOutlierDetector_ConcurrentEjectExactlyOnce`) · `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery` (**FIRED at the phase-79 SPEC**; `go list -deps` shows that package has **ZERO** dependency on `internal/stats`; isolated re-run green) · `internal/httpclient TestOptions_ZeroValue_NoOpDefaults`. **Isolate-re-run, then state the classification AND its evidence.** ⚠️ **`0061-lb-ring-hash` is NOT a live flake — a spread failure there is a FINDING.**

17. **Counts at this tip (re-run MECHANICALLY in your worktree; never copy them):** fixtures **120** (next-free **0119**) / fuzzers **55** (⚠️ the `internal/`-vs-repo-wide pair is **NOT discriminating** — both return 55; use the append-a-`func Fuzz` control **on a scratch COPY**) / **stat surface 1207**, ⚠️ **DOCUMENTARY and CONFIG-CONDITIONAL — assert the DELTA** / **BackendKind tail 38** (⚠️ a TAIL VALUE — 39 constants, 0-38; **do NOT "fix" 38 to 39**) / go.mod modules **2** (⚠️ the phase-61.2 lineage figure — **NOT a repo total**; the single `go.mod` requires **67**; **do NOT "fix" 2 to 67**) / internal packages **73** / blank imports in `runner_test.go` **120**, ⚠️ **anchor on the FULL `^\t_ "github.com/pgdad/envoy-go/test/fixtures/` prefix — a naive `^\t_ ` gives 126** / `DECISIONS.md` **17628**, tail **ADR-0301 PROPOSED** (next-free **ADR-0302**) / `ROADMAP.md` **227 lines / 111 data rows** / `BEHAVIOR_CONTRACT.md` **5762** / `STATE_HISTORY.md` **416** / next-free reference port **10119** (⚠️ the convention IS `10<fixture index>`; `grep -rn '10119' test/` ⇒ 0, NC `10118` ⇒ 5) / non-test `ExtractTags` call sites **5** / `BEHAVIOR_CONTRACT.md` `### Does not yet apply to` headings **14** (not 15).

18. **⚠️ DOCUMENTARY DEFECTS, STILL NAMED AS DEFERRALS AND STILL DELIBERATELY NOT FIXED.**
    - **The `STATE_HISTORY.md` archive gap — 40 bullets** (item 4). A future maintenance row should backfill from `git log` and **state the denominator**.
    - **`ROADMAP.md`'s row 78 is the ONLY MALFORMED ROW of 111** — `NF=8` with **2 877 characters** of IMPL summary in field 8 and **no trailing `|`**, so GFM drops the entire phase-78 IMPL summary from the rendered table. **NOT FIXED**: the §Schema invariant at `:18` forbids editing a closed row's `summary` cell.
    - **The documented PUBLIC IMPORT PATH DOES NOT EXIST** — **36 live occurrences across SEVEN files**. **ZERO in compiled Go and `go build ./...` exits 0** under the real path `github.com/pgdad/envoy-go`. ⚠️ **`DECISIONS.md:142` is an ADR that DECIDES the wrong path, never superseded.** ⚠️ **All 8 root `PROGRESS.md` hits are PASTED `go test` OUTPUT LINES** — rewriting recorded output is a different doctrinal act from correcting a statement. ⚠️ **Fix by PATTERN `esalaine/envoy-go` (bare), NOT the `/validate` form.** **8-10 tasks, expect 10-11.**
    - **A mechanical stat-surface count** to replace the documentary 1207. **210** registration sites; **27**-row ledger at `BEHAVIOR_CONTRACT.md:4962` rows `:4966`-`:5018`, plus a 28th detached at `:805`; **5** `statroster.New` fan-out sites (zookeeper alone **201** names); **2** method-value registrations invisible to any regex. ⚠️ **The non-H2 PARALLEL TOTAL was ABANDONED three rows ago** — newest `non-H2` figure is `:5010` (phase 47.1), tail rows `:5014`/`:5016`/`:5018` carry none, **so a future `+1` row appends a single-absolute row and need not touch a non-H2 total.** **8-11 tasks, pure meta-work.**
    - **`STATE.md` §Project counts self-contradicts §Current** (item 5). **Anchor on §Current.**
    - **Symmetric bind hardening** — `mustAllocatePort()` in **10** drivers; the racy helper copy-pasted under FOUR names, ~15 definitions. *"Fixture BACKENDS have no retry"* is OVERBROAD: only **`fixture.HTTPSH2` (`runner_test.go:288-293`)** can close-then-rebind race. **Still rejected: it CANNOT be verified by a green suite run** (`0e9cc680` needed `-count=6`).
    - **Opening the gRPC family — HARD-BLOCKED.** `\.RunEncodeTrailers(` ⇒ **0** non-test, 1 test; ⚠️ **the DECODE side is equally dead.** **The entire trailers pair is unreachable from production code.** Both charter-satisfying candidates sit behind it: **16-22+ tasks.**
    - **The WASM row-summary rider stays DECLINED on the merits** — `ROADMAP.md:76` declares phase 25 the FINAL §9 HTTP-filters-family row and rows 25.x use "family" 23 times to declare it CLOSED, so registering WASM is a **doctrine adjudication against a landed closure statement**, and writing the marker would silence check (3) **BY MENTION**. **Do not re-adopt it as cheap.**
    - **UNRESOLVED:** the *"plus two more in the same file"* half of the `BEHAVIOR_CONTRACT.md` stale-cite claim (`internal/tls/config.go:272`, `internal/filter/http/chain.go:19` flagged, **not audited**).
    - `BEHAVIOR_CONTRACT.md:501` calls the `wasm.` arm a *"NEW SN9 flattening rule"*, which **COLLIDES with ADR-0118's actual SN9**.
    - **`ADR-0299`'s STATUS line still reads `PROPOSED`** although its §Decision and §Consequences landed at the phase-77 IMPL. **Recorded, not fixed.**
    - **Normalizing the Operational-tooling short-form deferred-candidate paragraph** to the long form.

19. **⚠️ ROW 79.1 IS BANKED (~7-9):** the `sds.` label-hoisting arm (**20** of the 30 names) + registration-time validation. ⚠️ **The `sds.` arm is the one that BREAKS the no-op sink result, and that is now EXECUTED rather than argued** — the PLAN's hoisting NC moved all four sinks. ⚠️ Per ADR-0300 §Consequences (ii), any injected-defer probing of the validation leg **MUST VARY THE INSERTION POINT** across the pre- and post-anchor windows. ⚠️ **§3.2's secret attribution is SWAPPED** (item 6) — do not inherit the SPEC's order.

## What THIS session does — the phase-79 IMPL on `stats-prometheus-projection`

One stage per session. Execute `docs/envoy-go/phases/79-stats-prometheus-projection/PLAN.md`'s **TWELVE-task** roster (band **10-13, budget 12**), appending to `PROGRESS.md` under a `# IMPL record` heading. Flip **row 79 -> `done`** in `ROADMAP.md`, append **ADR-0301 §Decision + §Consequences IN PLACE**, land the **four** `BEHAVIOR_CONTRACT.md` edits, roll `STATE.md` §Current **IN PLACE** (ADR-0288 — ⚠️ **§Recent lineage is AT its five-entry cap; this close adds a bullet and MUST evict the oldest to `STATE_HISTORY.md`, per item 4**) and this file. Run the **six-gate**.

⚠️ **THE IMPL'S JOB IS TO EXECUTE AND TO RE-DERIVE, NOT TO RATIFY.** Phase 78's IMPL **REFUTED its own PLAN's arm-θ demonstration**; this PLAN refuted eleven SPEC claims. **An IMPL that only reasons will be wrong.** ⚠️ **PLAN break INSTRUCTIONS don't compile** (`reference_plan_break_instructions_dont_compile`) — where a break arm is specified, substitute an equivalent that does, and **confirm WHICH assertion fired** (`reference_deliberate_break_wrong_assertion`). ⚠️ **A break arm's INJECTION SITE is itself a claim** (`reference_break_arm_injection_site_is_a_claim`) — vary it; the PLAN did, and found the sink gate **not** site-dependent. ⚠️ **BREAKS RUN AFTER COMMITTING** (`reference_break_protocol_commit_first`) and with **`-count=1`**. ⚠️ **Arm κ in the PLAN's break roster is a NEGATIVE result and the most valuable line in it** — it demonstrates a plausible, natural gate configuration that **cannot fail**. Record it whether or not it ships.

Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** (`reference_parallel_subagents_private_scratch`), **BANDED PORTS** — ⚠️ **the band must sit OUTSIDE the differential harness's 20000-31007 allocation range** — and they **must tear down docker containers BY NAME, never by an `ancestor`/image filter** (`reference_parallel_agents_shared_machine_namespaces`). ⚠️ **If two agents share ONE worktree they must stage by EXPLICIT PATH; a `git commit -am` sweeps in a sibling's uncommitted work.** ⚠️ **AND THEY MUST NOT RUN AN UNSCOPED `git restore`.** Per-task `gofmt` + `golangci-lint` on touched packages (`feedback_pertask_gofmt_lint`) — **four** packages this row: `internal/stats`, `internal/statssink`, `internal/admin`, the `0118` driver. ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch.**

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. ⚠️ **ASSUME IT FIRES EVERY SESSION — it has been treated as live for EIGHTEEN consecutive sessions and it FIRED REPEATEDLY at the phase-79 PLAN, observed live.** **Use `git -C <abs-worktree-path>` for every git command, prefer absolute paths over `cd`, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — Current-pointer section (TRUSTWORTHY; ADR-0288 discipline — ⚠️ **anchor greps on `^- **<field>`**, not bare; all seven fields verified singleton at the phase-79 PLAN close, bullets at `:15-21`). ⚠️ **§Project counts SELF-CONTRADICTS §Current** (item 5) — do not anchor on it.
2. `docs/envoy-go/phases/79-stats-prometheus-projection/PLAN.md` — **the input to this stage.** §1 is the re-derivation ledger and **supersedes the SPEC wherever they disagree**; §1.1/§1.2 are the two headlines; §1.5 is the anchor-drift table; §3 is the edit surface; the twelve **Task** sections carry the gates; §4 is the band; §8 is the fifteen broken-gate shapes; §9 is the self-review against the SPEC.
3. `docs/envoy-go/phases/79-stats-prometheus-projection/PROGRESS.md` — the `# PLAN record` half, which is where the executed evidence lives. **Append the `# IMPL record` half; do not rewrite the PLAN half.**
4. `docs/envoy-go/phases/79-stats-prometheus-projection/SPEC.md` — ⚠️ **READ IT ONLY THROUGH THE PLAN'S §1 LEDGER.** Eleven of its claims are refuted or corrected and three of its anchors are wrong; a stage that cites it directly will inherit a wrong figure.
5. `docs/envoy-go/phases/78-boot-panic-visibility/PROGRESS.md` — the shape an `# IMPL record` takes in this lineage, and how it records a refutation of its own PLAN.
6. `docs/envoy-go/DECISIONS.md` — **ADR-0301** (§Context, this row; §Decision + §Consequences land at THIS stage) · **ADR-0300** (the NEW-form template; §Consequences (ii) carries the injected-defer insertion-point rule) · **ADR-0045** (⚠️ it QUOTES the split gate, it does not state it) · **ADR-0288** (`:17010`, the roll-IN-PLACE rule **and the archive rule item 4 shows has been violated**) · ADR-0044 (⚠️ it does **NOT** contain the Context-draft discipline — use `ADR-0044-as-used`) · **ADR-0106** (⚠️ the SOLE-leg property, **not** the six-gate) · **ADR-0264** (metrics_service label mapping, which `internal/statssink/label.go` implements).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — especially **`reference_registry_walk_lock_inversion`**, **`reference_probe_must_discriminate`**, **`reference_probe_input_is_a_claim`**, **`reference_positive_arm_cannot_catch_overfiring`**, **`reference_liveness_break_needs_failing_baseline`**, **`reference_deliberate_break_wrong_assertion`**, **`reference_break_arm_injection_site_is_a_claim`**, **`reference_break_protocol_commit_first`**, **`reference_stat_count_guard_blind_to_rename`**, **`reference_gate_command_negative_control`**, **`reference_empty_output_is_not_a_zero_result`**, **`reference_stale_cite_recurs_fix_by_pattern`**, **`reference_sentinel_matcher_string_self_clears`**, **`reference_document_hygiene_claim_not_evidence`**, **`reference_fatalf_makes_assertions_unreachable`**, **`reference_golangci_misspell_locale_us`**, **`reference_differential_run_selector`**, `reference_sample_is_not_an_audit`, `reference_a_drift_correction_is_itself_a_claim`, `reference_deferred_candidate_cost_restale`, `feedback_brief_citations_not_evidence`, `reference_next_prompt_tracked_despite_gitignore`, `feedback_git_worktrees`, `feedback_execution_style`, and **`reference_bash_cwd_reset_commits_to_main`**.
