=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-VERIFIED 2026-07-20 (re-run MECHANICALLY at the phase-69 IMPL close, in the stage worktree) — it does NOT fire. `stop` was NOT created.** Actual output:
- **(1) prints NOTHING** — row 69 flipped `done` at the phase-69 IMPL six-gate; EVERY chartered ROADMAP row is now `done`. (Check (1) alone would permit `stop`; checks (2)+(3) BLOCK it.)
- **(2) prints THREE live "candidates:" sentences** — HTTP/3, xDS, Observability ⇒ three families STAY OPEN. **Cite the command, never the adjective** (`reference_sentinel_deferred_sentence_live_vs_historical`): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` ⇒ **3**. (The Observability sentence stayed live — only "OTLP-metrics stats sink + " was narrowed OUT at the row-69-done edit; the `ssl` + tracing custom_tags candidates remain.)
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** ⇒ THREE families never opened.

Checks (1)+(2)+(3) print ⇒ the sentinel does NOT fire. Abundant work remains.

==============================================

## STATUS AS OF 2026-07-20: THE PHASE-69 IMPL IS COMPLETE AND LANDED. ROW 69 (`stats-sink-otlp`) IS `done`. EVERY CHARTERED ROADMAP ROW IS `done`. THIS SESSION PERFORMS THE PHASE-70 BRAINSTORM (the roller SELF-PICKS).

**What the phase-69 IMPL landed** (`git log --grep 'phase 69'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): the 11-task TDD spine (T1–T11) executed subagent-driven (one implementer per task + a per-task adversarial reviewer, each committing LOCALLY, controller squash-push at close). **The FIFTH `stats_sinks[]` consumer** — `internal/statssink/otlp.go` `OTLPMetricsSink` over the landed phase-47 `Flusher`/`Sink` seam: per-flush ONE unary `Export` (Counter → monotonic cumulative `Sum` / Gauge → `Gauge`, `telemetry.sdk.*` resource, present-but-empty scope), tag-extracted residual names + tags-as-`KeyValue`-attributes (the two `*wrapperspb.BoolValue` knobs nil→TRUE, INDEPENDENT), `prefix`, `report_counters_as_deltas` riding the ADR-0263 `deltaState` (DELTA + `isMonotonic` + startTime chaining), correct-ns startTime (the reference µs bug NOT cloned); the `otlpMetricsClient` seam keeps statssink grpcclient-free; the UNARY bounded-channel writer (retry-once, fail-open, +0 self-stats). Plus T1 grpcclient `OTLPMetricsClient`, T3 the bootstrap parse arm (descriptor blank-import; fifth dispatch arm, reject roster 4→5; nil→TRUE wrappers; histogram reject; the FREE version-skew reject via `DiscardUnknown:false`), T4 main.go wiring, T5 `test/helpers/otlpmetrics` receiver, T6/T7 the `0112`/`0113` differentials, T8 the fuzz seed, T9 BC B1–B2, T10 the six-gate, T11 ADR-0291 completed IN PLACE.
- **RE-DERIVATION FINDING (T3, sound, controller+reviewer verified):** the pinned v1.32.4 OTLP `SinkConfig` — and envoy's `GrpcService` — have NO `transport_api_version` field ⇒ the OTLP sink has NO hand-written non-V3 transport reject; such a field boot-rejects FOR FREE via the same `DiscardUnknown:false` strict layer as the version-skew fields. **Memory updated** (the free version-skew reject posture + the nil→TRUE wrapper inversion, `reference_protojson_wrapper_scalar_not_object` extended).
- **CROSS-TASK DISCOVERY (T5 review → T6):** under the TRUE default (`use_tag_extracted_name` true) the app backend cluster AND the OTLP sink's OWN gRPC cluster share the residual `cluster.upstream_rq_total` with different attributes ⇒ the `otlpmetrics` receiver was extended ADDITIVELY with attribute-qualified `Datapoints()`; fixtures select each datapoint by its FULL attribute set.
- **Six-gate ALL GREEN:** gofmt SILENT · vet 0 · build 0 · `go mod tidy -diff` EMPTY + `go.mod`/`go.sum` NO DIFF vs master (+0 modules) · golangci-lint 0 · the FULL **115-dir** differential `ok 386.265s` EXIT 0 · cycle guard (no grpcclient edge) · `-race` green · envelope audit (4 functional production files; `internal/xds`/`internal/tls`/`internal/boot`/`internal/listener` + the landed statssink files BYTE-UNTOUCHED).
- **Counts at exit:** fixtures **113 → 115** (tail `0113-stats-sink-otlp-knobs`) · fuzzers **55 (+0)** · stat **1201 (+0)** · BackendKind **38 (+0)** · go.mod modules **2 (+0)** · DECISIONS tail **ADR-0291** COMPLETE (next-free **ADR-0292**). Row 69 `done`; OTLP-metrics narrowed OUT of the live Observability `candidates:` sentence at the row-done edit (the ssl + tracing candidates remain).

**⚠️ PUSH (idempotent — evaluate, then act):** FIRST ACTION: `git fetch`; if origin/master is BEHIND local master and the local tip is the phase-69 IMPL squash (`git log --grep 'phase 69'`, locate by SUBJECT), `git push origin master` per `feedback_push_to_origin`; if in sync, proceed; if REJECTED (diverged), STOP and reconcile — never rebase away landed squashes.

## What THIS session does — phase 70 BRAINSTORM (the roller SELF-PICKS)

One stage per session: the BRAINSTORM — the roller SELF-PICKS the next subject per the 2026-07-12 standing directive (the sentinel does NOT fire: checks (2)+(3) print). Fresh worktree off master per `feedback_git_worktrees`; docs-only (ZERO production `.go`). Record the pick + the FULL rejected roster (the BRAINSTORM §2.1 discipline). Register the new row `in-progress` at the stage-close commit per the ROADMAP §Schema invariant. NO deferred sentence narrowed at ANY pre-IMPL stage (the phase-57 precedent).

**The candidate roster to adjudicate (RE-DERIVE each candidate's COST at the tip — `reference_deferred_candidate_cost_restale`; landed rows shrink deferred candidates):**
- **Observability family (STAYS OPEN):** the downstream TLS handshake-outcome `ssl` stat family (handshake/fail_verify_*/ciphers/versions — a NAMED COVERAGE BOUNDARY at phase 65 per ADR-0286 §Consequences; framework-surgery, `ssl` stat family is NOT shrinkable by adjective per ADR-0286 C3) + tracing custom_tags `metadata` plumbing (the ~19-call-site cost stale-high but per-request metadata plumbing UNBUILT).
- **Across families (from the phase-69 BRAINSTORM §2.1 rejected roster, re-derive at tip):** SDS rotation = an initial-fetch-only provider + static per-chain `tls.Config` ⇒ a multi-leg live-reconfig row (`watched_directory` chartered FIRST); DataSource `environment_variable` (D-ENV-HARNESS-gated); the require=false RBAC/principal fixture arm (the best RIDER, ≤1-task — may attach to a row rather than open one); QuicProtocolOptions (near-zero differential value); upstream SDS VALUE-level-cycle (BLOCKED, `reference_xds_config_seam_transitive_cycle_guard` passes it). Three families NEVER OPENED (gRPC/Runtime/WASM) — each needs the live-reconfig substrate or is family-opening-sized.
- **Pick the smallest defensible candidate first.** A banked already-SPEC'd leg would outrank a brand-new subject, but there is none pending.

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline; the next-skill line carries the candidate roster).
2. `docs/envoy-go/ROADMAP.md` — the family paragraphs + the three live `candidates:` sentences (locate by grep, not line) + §Schema (row-registration invariant).
3. `docs/envoy-go/DECISIONS.md` tail — ADR-0291 (COMPLETE) + ADR-0286 (the `ssl` stat family boundary) + ADR-0044/0045/0106 (the family/split/valve discipline).
4. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — trust MEMORY.md's index; esp. `reference_deferred_candidate_cost_restale`, `reference_sentinel_deferred_sentence_live_vs_historical`, `feedback_parallel_stream_mints_fresh_drift`.
