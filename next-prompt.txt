=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, the active phase is fully landed, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

**STATUS AS OF 2026-06-29: ALL CHARTERED WORK COMPLETE — TERMINATION SENTINEL ACTIVE.**
- Every chartered ROADMAP row is `done` (rows 1–47; row 47 `stats-sink-metrics-service` — the highest — flipped `done` at the 47.2b IMPL, the FINAL sub-leg, ADR-0106 + `reference_roadmap_split_phase_row_done`).
- The active phase is fully landed: **phase 47.2b (`emit_tags_as_labels`) IMPL** squashed (`6bb287c2`), fast-forwarded onto master + pushed (master == origin/master); six-gate GREEN (full 93-dir differential `ok 283s`; gofmt/vet/golangci-lint/build clean; full unit suite green; `-race` clean on `internal/statssink` AND `internal/stats`; `0091` 4 deliberate breaks all LIVE + 20/20 flake-soak; `go mod tidy -diff` EMPTY). ANCHORS ADR-0264. Counts: stat 1200 / fixtures 93 / fuzzers 50 / BackendKind 38 / DECISIONS ADR-0264 (next-free ADR-0265).
- STATE.md's recorded NEXT is **`NEXT → (none chartered)`**. The metrics_service stats sink is complete across 47.1 core (ADR-0262) + 47.2a deltas (ADR-0263) + 47.2b tags-as-labels (ADR-0264); BOTH `MetricsServiceConfig` knobs now CONSUMED.

=> The `stop` sentinel applies. There is NO next stage to perform autonomously.

The Observability FAMILY STAYS OPEN, but NO further row is CHARTERED: statsd / dog_statsd / graphite / OTLP-metrics sinks + the tap filter + tracing `custom_tags`/`spawn_upstream_span`/`http_service`/force-trace remain DEFERRED candidates (mentioned in the ROADMAP family-progress note, NOT `pending` rows with their own `Next →` brainstorm pointer). The autonomous router ADVANCES chartered stages; it does NOT self-charter new rows/families — that is a human decision.

## To resume work (a human chartering the next phase)
To re-open the loop, charter the next stage, then re-roll this file to point at it:
1. Pick the next row to open (a deferred Observability candidate above, or a fresh feature family).
2. Add/flip its ROADMAP row to `pending` with a `Next → <row> BRAINSTORM` pointer, and set STATE.md's NEXT accordingly (lifecycle-state 0).
3. Replace this file's body with a cold-start router pointing at that BRAINSTORM (mirror the structure of any prior phase's `next-prompt.txt` in git history — e.g. the 47.x SPEC/PLAN/IMPL routers).
4. Delete the `stop` file at the git root so the loop can advance again.
==============================================

## Read first (in order)
1. THIS FILE — the router (TERMINAL: all chartered work complete; `stop` sentinel active).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 47.2b IMPL done`) + the recorded NEXT (`(none chartered)`) + the counts (1200 / 93 / 50 / 38 / ADR-0264).
3. `docs/envoy-go/ROADMAP.md` — all rows `done` (max row 47); the Observability family-progress note (DONE at phase 47; family STAYS OPEN; deferred-row list).
4. `docs/envoy-go/DECISIONS.md` — ADR-0264 (the 47.2b tags-as-labels anchor; next-free ADR-0265).
5. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_subagents_no_push`, `feedback_subagent_autocommit_claudemd`, `reference_roadmap_split_phase_row_done` — the split-phase row-completion rule that flipped row 47 `done`).
