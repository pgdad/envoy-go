START a fresh cold-start session for envoy-go — **the phase-40.1 (`outlier-detection-consecutive-5xx`, passive ejection on the `consecutive_5xx` detector) IMPL is DONE, LANDED on master (squash commit `bb4da7b`), and PUSHED to `origin/master`; the next lifecycle step is the phase-40.2 SPEC** (`superpowers:brainstorming`→ no, go straight to SPEC — the phase-40 BRAINSTORM already chartered all three legs: the OTHER consecutive detectors `consecutive_gateway_failure` + `consecutive_local_origin_failure` + `split_external_local_origin_errors`, threading `UpstreamResult.LocalOriginErr` through the pre-pick failure sites) OR a new subject. Confirm the tip via `git log --oneline -5` (the substantive master tip is the phase-40.1 IMPL squash **`bb4da7b`**; the prior substantive tips are the phase-40.1 PLAN `2100435` and the phase-40.1 SPEC `a350f0e`). Counts at the 40.1 IMPL: stat surface **1137** / fixtures **71** / fuzzers **42** / BackendKind tail **35** (`HTTP503Responder`) / DECISIONS tail **ADR-0245**, next-free **ADR-0246**.

## What just landed (phase 40.1 IMPL — the durable record is `docs/envoy-go/phases/40.1-outlier-detection-consecutive-5xx/{SPEC,PLAN,PROGRESS,README}.md`; read those, not this summary)

The 14-task TDD PLAN executed end-to-end subagent-driven (fresh subagent per task + spec/quality review), the six-gate GREEN, the full **71-dir differential GREEN**, the `0069` 2 deliberate breaks BOTH fail-as-required, 20/20 flake-free.

**As built — ADR-0245** (the project's FIRST passive upstream-health construct; REUSES the ADR-0242 `clusterHealth`/`hostHealth` registry + the ADR-0243 build-time-injected health-aware LB pick; **byte-identical when no `outlier_detection` is configured**):
- The per-request **passive seam** `cluster.RecordUpstreamResult(ep, UpstreamResult{StatusCode, LocalOriginErr})` (in `internal/cluster/cluster.go`; `LocalOriginErr` UNREAD at 40.1 — reserved for 40.2) — fired from the two LIVE router success sites `doH1ClusterAction` (`router.go:656`) + `doH2ClusterAction` (`router_h2.go:101`), NOT the test-only legacy `do`/`doH2`. No-op when `c.outlier == nil`.
- A SEPARATE per-host **ejection sub-state** on `hostHealth` (`ejected`/`unejectAtNanos`/`ejectCount`/`consec5xx`), DISTINCT from the active-HC `healthy` bit; time-based LAZY recovery — the lazy un-eject lives in `clusterHealth.isEjected` (the load-bearing un-eject path, since an ejected host gets no further traffic).
- The **`available = isHealthy && !isEjected`** LB-pick predicate at the five leaf consult sites (`loadbalancer.go`/`random.go`/`ringhash.go`/`maglev.go`/`leastrequest.go`; subset inherits); `inPanic` denominator → `availableCount`; `isHealthy`/`membership_healthy` STAY active-HC-only.
- The **`outlierDetector`** (`internal/cluster/outlier.go`): `consecutive_5xx` detect/eject (eject on N consecutive 5xx per host, reset only on a 2xx FROM that host); the `max_ejection_percent` cap **cross-multiplied** `(ejected+1)*100 <= cap*total` to match the live-pinned reference boundary (1-of-3 ejects iff cap≥34; cap 33 ⇒ `ejections_overflow`); the `enforcing_consecutive_5xx` roll (PCG-seeded `enforceRoll`, short-circuited at 0/≥100); **CAS-guarded once-only eject**. AMEND-OD1 (lazy un-eject vs the reference interval-sweep) + AMEND-OD2 (flat `base_ejection_time` vs the backoff×decay) are recorded departures for 40.2/40.3.
- The **+5 scoped stats** `cluster.<name>.outlier_detection.{ejections_active [gauge], ejections_enforced_total, ejections_overflow, ejections_detected_consecutive_5xx, ejections_enforced_consecutive_5xx}` (the double-count `enforced_total`+`enforced_consecutive_5xx` replicated; emitted ONLY on outlier clusters). The reference's 503-trips-gateway-detected NOT replicated (no gateway detector at 40.1).
- **+1 BackendKind** `HTTP503Responder` (35, an in-process always-503 responder) + the NEW OPTIONAL `fixture.PerHostBackendKind{ BackendKindAt(i int) BackendKind }` per-index runner override (byte-stable for the 70 prior fixtures). Differential `0069-outlier-detection-consecutive-5xx` (cross-side traffic-driven ejection; poll `ejections_active`→1 + warmup-until-K-200s + delta-counter measured phase 100% 2xx; cross-side `ejections_*` parity; recovery arm DEFERRED).

## The phase-40.2 SPEC obligations (the next leg — the OTHER consecutive detectors)

Phase 40.2 = the pre-authorized SECOND leg of the phase-40 3-leg by-detector-class split (the 39.1/39.2 + 36.1/36.2 precedent). Charters `consecutive_gateway_failure` + `consecutive_local_origin_failure` + `split_external_local_origin_errors`. The 40.1 seam was built FORWARD-COMPATIBLE precisely so 40.2 does NOT re-touch the router: 40.2 threads `UpstreamResult.LocalOriginErr` (already a field; unread at 40.1) by populating it at the **pre-pick dial/acquire-failure sites** (H1 503 `router.go:~610`, H2 502 `router_h2.go:~76` — where `picked == Endpoint{}`; 40.2 must decide how to attribute a local-origin failure to a host when the pick failed — likely the dialed endpoint must be surfaced) and adds the `consecutive_gateway_failure` detector (5xx ∈ {502,503,504} ⇒ gateway) + the `consecutive_local_origin_failure` detector (connect/reset ⇒ local-origin) + `split_external_local_origin_errors` (the external-vs-local accounting split). Execute the §11 D-OD2-* empirical pins live against `contrib-v1.37.2` per `reference_docker_probe_bridge_network`. Anchor the ADR-0246 §Context (per ADR-0044). Anticipated: the gateway/local-origin `detected_`/`enforced_` stat pairs (+N stats); possibly +1 BackendKind (a connect-refusing/resetting host — or reuse the 0066 dead-host); 1 ADR (ADR-0246).

## Honor throughout (the lifecycle discipline)

- **Lifecycle = one stage per session:** the SPEC (`superpowers:brainstorming` is DONE for the family; go to the SPEC directly — execute the §11 empirical pins live, anchor the ADR-0246 §Context, spec-document-reviewer loop, land + push). `feedback_git_worktrees` · `feedback_execution_style` (subagent-driven IMPL later) · `feedback_push_to_origin` · `feedback_subagents_no_push`.
- **IMPL-time discipline (the session after the 40.2 PLAN):** `reference_differential_run_selector` (`-run 'TestDifferential/<NNNN>'`) · `reference_differential_break_protocol_count1` (`-count=1`; the dead-driver trap — the seam is in `doH1/doH2ClusterAction`, and 40.2's local-origin attribution rides the SAME live drivers) · `reference_docker_probe_bridge_network` · `reference_health_check_propagation_warmup` (the traffic-driven ejection gates on `ejections_active` convergence + warmup + delta counters) · `reference_fixture_workload_constant_desync` · `feedback_pertask_gofmt_lint`.
- **ADR/process:** ADR-0044 (§Context at SPEC; body at IMPL) · ADR-0045 (split-gate re-check at the PLAN) · ADR-0052 (six-gate) · ADR-0080 (byte-stable rejects) · ADR-0106 + `reference_roadmap_split_phase_row_done` (row 40 flips `done` only when ALL THREE legs land — 40.2 is NOT the last; 40.3 [statistical detectors] remains) · ADR-0227 (`contrib-v1.37.2`).

## Read first (in order)

1. THIS FILE — the post-phase-40.1-IMPL cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 40.1 (outlier-detection-consecutive-5xx) IMPL done`; the count footer (1137 / 71 / 42 / 35 / ADR-0245, next-free ADR-0246); the lifecycle routing (next-skill = the 40.2 SPEC).
3. `docs/envoy-go/phases/40-outlier-detection/BRAINSTORM.md` (the parent charter — the 3-leg split + the 40.2 scope) + `docs/envoy-go/phases/40.1-outlier-detection-consecutive-5xx/{SPEC,PLAN}.md` (the 40.1 architecture the 40.2 detectors extend; the §2 non-purposes list the 40.2 deferrals).
4. `internal/cluster/{outlier.go,health.go,cluster.go,manager.go}` (the `outlierDetector`/`outlierConfig`/`parseOutlierDetection`; the ejection dimension; the `RecordUpstreamResult` seam + `UpstreamResult{StatusCode, LocalOriginErr}`; the detector construction) + `internal/filter/http/router/{router.go,router_h2.go}` (the live success sites :656/:101 AND the pre-pick failure sites the 40.2 local-origin detector must populate) + `test/fixtures/0069-outlier-detection-consecutive-5xx/` (the differential template 40.2 adapts) + `test/differential/runner_test.go` (the `HTTP503Responder` spawn + the `PerHostBackendKind` override).
5. `docs/envoy-go/DECISIONS.md` (ADR-0245 + the process ADRs) + `docs/envoy-go/BEHAVIOR_CONTRACT.md` (the `### Cluster — passive health (outlier detection)` subsection 40.2 extends).
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`.
