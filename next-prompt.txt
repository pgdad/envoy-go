=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

**THERE IS BANKED WORK AT THIS TIP.** Phase 77 (`runtime-static-layer`) is BRAINSTORMED **and SPEC'd**, and **row 77 is registered `in-progress`**. **This session performs the phase-77 PLAN.** Do NOT self-pick a new subject.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form. See the §(2) note below.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-RUN MECHANICALLY 2026-07-26 at the phase-77 SPEC. It does NOT fire. `stop` was NOT created and MUST NOT be.** ⚠️ **Re-run it yourself anyway and record what YOU see** — a prior run's result is not evidence for the current tip. ACTUAL output, recorded rather than predicted:
- **(1) `NOT DONE: row 77`** — live since the BRAINSTORM; it stays live through the PLAN and is silenced only at the phase-77 IMPL.
- **(2) FOUR under the old matcher, FIVE under the broadened one above.** ⚠️ **BOTH ARE CORRECT — do NOT "fix" either count down.** ⚠️ **The Operational-tooling short-form site has DRIFTED `:218` → `:221`** under the BRAINSTORM's own ROADMAP edit — a line cite goes stale under the very row that writes near it.
- **(3) `NEVER OPENED: gRPC` and `NEVER OPENED: WASM`** — `Runtime` is legitimately GONE, cleared by row 77. (The `WASM` line remains a ROADMAP **bookkeeping artifact** — `internal/wasm/` is a substantially-built wazero host and rows 25/25.1/25.2/25.3 are all `done` — deliberately LEFT AS-IS.)

## ⚠️ THE §(2) NOTE — CHECK (2) WAS FAILING UNSAFE, AND WHAT ITS NUMBERS NOW MEAN

**The old check (2) matched ONLY the long phrase.** The **Operational-tooling** family records three genuinely un-chartered items — xDS-sourced dry-validation · an admin-API-exposed live-reload-and-validate endpoint · an RTDS/SDS validate companion — in the **SHORT** form, in the *same paragraph* that reads `**The family STAYS OPEN**`. The old matcher **could not see them**.

⚠️ **Check (3) fails SAFE by construction, and check (1)'s four-miss blind spot is currently HARMLESS (all four missed rows are `done`) — but check (2)'s blind spot was LIVE.** Had the visible sentences ever been emptied and the three families opened, `stop` would have fired with real un-chartered work on the books. **A false "done" ends the project early.**

**The fix was applied at the MATCHER, not at the prose** — it fixes the CLASS and immediately surfaces the INSTANCE with no ROADMAP edit. Negative controls, all EXECUTED at the phase-77 BRAINSTORM: a `candidates were:` recap line ⇒ **silent** · a short-form-only line ⇒ **1** · an empty file ⇒ **silent**.

⚠️ **Normalising that Operational-tooling paragraph to the long form is a NAMED, DELIBERATELY-UNTAKEN follow-up.**

⚠️ **THE DOCUMENTED TRAP'S STATED MECHANISM IS REFUTED, WHILE THE TRAP IS REAL.** `reference_sentinel_deferred_sentence_live_vs_historical` says a naive `grep -c 'candidates:'` inflates *because of* the `candidates were:` recaps. It does inflate — but **not for that reason**: `candidates were:` does not contain the substring `candidates:` **at all**. The inflation comes from **ROADMAP table rows carrying BRAINSTORM prose**. **Cite the command; do not re-inherit the explanation.**

⚠️ **AND THE MEASURED RECORD: CHECK (2) HAS NEVER GONE DOWN** across ~17 phases (history `0 → 1 → 3`, then `→ 4` when row 77 opened the Runtime family). **Phase 74 landed three `ssl.*` stat names and made its OWN sentence LONGER**; **phase 73 predicted a clause REMOVAL and got a clause REPLACEMENT.** ⇒ **the candidates sentence is a WINDOW onto a larger deferred backlog, not an inventory of it.**

⚠️ **THE PRIOR ROUTER'S SELECTION CALCULUS IS REFUTED AS ARITHMETIC.** *"A candidate that clears a check-(3) blocker is worth **strictly more**"* does **not** hold: opening a family **MINTS** a check-(2) sentence, so the item count went **3 never-opened + 3 sentences → 2 + 4 = 6 → 6, NET ZERO.** What survives, and what actually justified the phase-77 pick: **opening a family is the ONLY way that family's work becomes enumerable at all — a prerequisite, not a shortcut.** Do not restore the stronger claim.

⚠️ **NEVER WRITE A SENTINEL'S OWN MATCHER STRING INTO A FILE THE SENTINEL GREPS.** The sentinel greps **`docs/envoy-go/ROADMAP.md`** — writing these strings *here* is safe; writing them *there* is not. This fired LIVE at the phase-76 BRAINSTORM, twice in one commit. **`grep` cannot tell a mention from a use.** ⚠️ **A PLAN normally leaves `ROADMAP.md` BYTE-UNTOUCHED; if this one edits it, re-run every sentinel check AFTER your edits land.**

⚠️ **CHECK (1)'s BLIND SPOT — RE-DERIVE IT; NEVER COPY IT.** At this tip it is **109 data rows, 105 matched, FOUR misses** — `:31` (`| 00 | bootstrap | — |`, an **em-dash** in the "after" cell), `:35` (`| 04 | http-1.1 |`, a **DOT** in the slug), and `:83`/`:84` (`| 28.1a |` / `| 28.1b |`, a **LETTER suffix** on the id). All four are `done`, and **row 77 is among the MATCHED** — so there is no current impact, **but a NEW row whose id, slug or "after" cell takes any of those shapes would be INVISIBLE to check (1) and would read as `done` forever.** ⚠️ **This figure was recorded WRONG in two consecutive lineages** before being re-derived correctly at phases 75, 76 and 77. **Derive it yourself.**

==============================================


## STATUS AS OF 2026-07-26: PHASE 77 IS **SPEC'd**. ROW 77 IS `in-progress`. THIS SESSION PERFORMS THE **PHASE-77 PLAN**.

**What the phase-77 SPEC landed** (`git log --grep 'phase 77'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): the **D-RSL-\* docket SETTLED BY EXECUTION on every item**, `docs/envoy-go/phases/77-runtime-static-layer/SPEC.md` (16 sections), and **ADR-0299 §Context** (12 paragraphs, STATUS **PROPOSED**). File set: `DECISIONS.md` + `STATE.md` + `SPEC.md` + `next-prompt.txt`. **`ROADMAP.md` and `BEHAVIOR_CONTRACT.md` BYTE-UNTOUCHED.** Four agents on disjoint remits with PRIVATE scratch — two LIVE reference probe fleets, one Go-execution agent in a throwaway worktree, one read-only survey — plus controller re-derivation of every load-bearing claim. **TWENTY-THREE claims REFUTED (SPEC §1.2, R1-R23).**

### What matters for the NEXT session, in order

1. **⚠️ RE-RUN ALL THREE SENTINEL CHECKS YOURSELF AND RECORD THE ACTUAL OUTPUT; DO NOT PREDICT IT.** Create `stop` if and ONLY if all three print nothing. A PLAN normally leaves `ROADMAP.md` byte-untouched, so one run at session open suffices — **unless you edit it**, in which case re-run after.

2. **⚠️ THE ROW'S FRAMING INVERTED AT THE SPEC, AND THE PLAN MUST NOT RESTORE THE OLD ONE. THE REFERENCE *ACCEPTS* ALL THREE SIBLING ONEOF ARMS.** `disk_layer`, `admin_layer` and `rtds_layer` each return `--mode validate` **rc=0** and boot to `/ready`=200; `disk_layer` with a real directory actually **loads keys**. ⚠️ **The briefed *"rtds fails validate"* result was an ARTIFACT** — a **node** error, then a **missing-cluster** error, never a runtime-layer rejection. ⇒ **envoy-go's rejects are DELIBERATE DEPARTURES, not parity**, and every document the PLAN writes must say so. The row fixes a reject-what-the-reference-serves defect for ONE arm and **deliberately preserves it for THREE** — correct, because silently ignoring `rtds_layer` means **a config asking for DYNAMIC runtime quietly gets STATIC values**.

3. **⚠️ THE LIFT AND THE ROSTER ARE ONE CHANGE, NOT TWO** (`reference_lifted_reject_hidden_enforcement`). A wholesale pre-check is a **SINGLE guard in front of FOUR oneof arms**; lifting it for one makes the other three **reachable and silently accepted**. **Do NOT schedule them as separate tasks with a green gate between.**

4. **⚠️ THE FLATTENING RULE THE LINEAGE RECORDED IS WRONG — CODE THE MEASURED ONE.** *"A Struct carrying `{numerator, denominator}` terminates flattening"* is true of that instance and wrong three ways (15 arms, 3× each): **either name ALONE terminates** · the test is **purely LEXICAL and CASE-SENSITIVE** (`{Numerator, Denominator}` **recurses**) · **the VALUES are never inspected** (`{numerator: "notanumber", denominator: NOTANENUM}` boots cleanly). **Field count is irrelevant** — one-field `{foo: 1}` **recurses** while one-field `{numerator: 25}` **terminates**. ⚠️ **An implementation that parses a FractionalPercent here REJECTS configs the reference ACCEPTS.** ⚠️ **AND THERE IS A SECOND TERMINATION BRANCH: THE EMPTY STRUCT** — `e: {f: {}}` ⇒ **1** key `e.f`; `e: {}` ⇒ **1** key `e`. The implementation needs **TWO** termination branches (SPEC §3.3.3 carries the pseudocode).

5. **⚠️ THE INHERITED TEST DESIGN WAS BLIND IN THE SHAPE OF ITS OWN ASSUMPTION.** The three-arm pin set (overlap · nested · fraction-named) contains **no empty leaf**, so it would have **PASSED against an implementation that emits ZERO keys** for an empty Struct. A **FOURTH arm** was added. ⚠️ **The four arms live in ONE bootstrap and all feed ONE gauge**, so the PLAN must choose per-arm key counts such that **no two arms' breakage produces the same `num_keys` total** — otherwise a break cannot identify which leg it broke. ⚠️ **Arm C must use a SINGLE lowercase `numerator` alongside an unrelated sibling** (`{numerator: 25, foo: 2}` ⇒ **1**), because the full pair still passes against a pair-matching implementation and fails to discriminate.

6. **⚠️ TWO FINDINGS EXIST ONLY BECAUSE ARMS WERE REPEATED.** The within-layer key-collision winner reports clean *last-declared-wins* on ONE run and is **~40/60 NON-DETERMINISTIC over 18 fresh processes** with identical config bytes; the `goo.gle/debug*` marker randomization is invisible until a process restarts. **Both would have been recorded as settled facts by a competent single-pass probe.** ⇒ `reference_probe_must_discriminate` is **necessary but NOT sufficient** — a probe can discriminate perfectly and still measure a coin flip once. **Where a wrong answer would be embarrassing, repeat the arm.**

7. **⚠️ A SECOND TEST SITE IN A SECOND PACKAGE, NAMED IN NO PHASE-77 DOCUMENT BEFORE THE SPEC: `validate/validate_test.go:65-79`** (`TestBootstrap_ReusesLoad_RejectsLayeredRuntime`), the **public-package** sibling of `internal/bootstrap/bootstrap_test.go:82-96`. **BOTH** use the fixture `name: static_layer` + `static_layer: {}` — **exactly the arm this row legalizes** — so **BOTH flip to `err == nil` and die at `t.Fatal`.** ⚠️ **Of the FOUR tests guarding these two rejects, exactly ONE asserts the `"bootstrap: "` prefix**, and ⚠️ **`FuzzBootstrapLoad` does NOT assert the invariant its own comment states** (`_, _ = Load(...)` — a **panic-only** guard). Both gaps close in one two-line change.

8. **⚠️ `internal/bootstrap` has ZERO named reject constants and ZERO `ByteStable` tests** — all **47** arms are inline `fmt.Errorf`. The *"per the landed discipline"* instruction points at a discipline landed in **filter** packages (`internal/filter/http/{admission_control,ratelimit,wasm}`, `internal/filter/network/mongoproxy`). This row **introduces** it here, **scoped to its own nine arms only**. ⚠️ **Copy the `wasm` variant, not `admission_control`** — `wasm/compiled_config_test.go:160` guards `len(cases)` so silently deleting a row FAILS. ⚠️ **The tenth roster arm (>1 admin layer) is DROPPED AS UNREACHABLE** and must not be restored while `admin_layer` is rejected outright.

9. **⚠️ NO CROSS-SIDE WORDING ASSERTION IS POSSIBLE.** Every reference PGV message carries a proto DebugString whose marker **rotates** across process starts; the unknown-field message varies in whitespace **and** in its `near L:C (offset N)` numbers across runs of the **same file**; the randomization **leaks into boot-error text** too. Only the three hand-written loader messages are byte-stable 3/3. ⚠️ **And envoy-go's own `protojson` error carries a `line L:C` derived from the MARSHALED JSON**, whose keys `json.Marshal` **sorts** — so that column shifts whenever any *other* key in the document changes. **Match on the field name with `strings.Contains`; never `==` on the whole message.**

10. **⚠️ THE FIXTURE IS `0118-runtime-static-layer` ON PORT `10118`, NOT `10450`.** The router's 10450 was a correct *max+1* figure (max in use **10449**) but ports are **family-banded**, and the dominant, most-recent convention is `10<fixture index>`: **0114→10114, 0115→10115, 0116→10116, 0117→10117**. 10450 is the TLS/SDS band; this is not a TLS fixture. ⚠️ **A fixture needs THREE registration gates** — the `NNNN-…` directory · `RegisterFixture` with the name **equal to the directory name** · **a BLANK IMPORT in `runner_test.go`** (currently **119**). Without the third, `init()` never runs and the runner **`t.Skipf`s SILENTLY GREEN**. ⚠️ **`expectations.yaml` is 100% comment prose read by ZERO Go code** (ADR-0019) — the live pair is `envoy.yaml` + `envoy-go.yaml`. ⚠️ **The `StatsAsserter` dispatch is a SILENT type assertion with no `else`** (`runner_test.go:1347-1349`, **first addr = REFERENCE**) — a signature typo makes the leg **vanish green**, so prove it RUNS, not merely that it passes. ⚠️ **`BackendCount()` must be ≥1** (`runner_test.go:240-243`); `return 1` + default `TCPEcho` is the minimum viable shape. ⚠️ **`fixture.TB` has NO `Logf`** — use `log.Printf`.

11. **⚠️ THE BROKEN-GATE COUNT IS EIGHT, AND A `+2`-PRODUCTION STAGE IS EXACTLY WHERE A BROKEN GATE HIDES.** `gofmt -l` **NEVER exits non-zero** (gate on OUTPUT: `[ "$(gofmt -l . | wc -l)" -eq 0 ]`) · raw `go doc -all` diffed is **not symbol-scoped** and **FALSE-POSITIVES on a correct tree** · `go doc -all <A> <B>` fails open with a `./` prefix (**audit ONE package per invocation**) · a **RANGE** gate **cannot detect anchor drift** (a +20 drift stays in range — **content-anchor instead**) · the `impblock` import gate has **three** defects · **a wrong-baseline caveat can name the WRONG sha** — verify a baseline is an ancestor before trusting a warning about it · a sha256 byte-untouched roster needs a **MISSING** leg as well as a MISMATCH leg (a DELETION reads as "no mismatch") and must be **set-differenced** against the EDIT roster. ⚠️ **`internal/bootstrap/**`, `internal/runtime/**` and `validate/**` must NOT be byte-gated for this row.**

12. **⚠️ A HARNESS'S EXIT CODE IS NOT THE COMMAND'S EXIT CODE.** At the phase-76 IMPL a background-task notification for a `-race` full suite reported *"exit code 0"*; that was the **WRAPPER shell's** status. The logged inner status was **1**, and the run had **PANICKED** at 84 of 119 subtests. **Capture the inner status explicitly** (`cmd; echo "EXIT=$?" >> log`) and **derive the PASS/FAIL tally from the log.**

13. **⚠️ ANCHORS DRIFT UNDER A STAGE'S OWN EDITS, AND THIS STAGE SAW IT LIVE.** The Operational-tooling short-form sentinel site moved **`:218` → `:221`** under the BRAINSTORM's own ROADMAP edit. Phase 76 saw a break target move `:295 → :307` and a *"ONE test of 403"* finding become *"TWO of 404"* because that row's own new test was the second. **Re-derive every count and cite at the tip that reads it.** ⚠️ **Self-falsifying counts remain the lineage's most persistent defect species** — never write a token into the file whose occurrences of that token you are counting. ⚠️ **The ADR-0288 singleton greps over `STATE.md` do NOT all return the same number**: the rule statement at `STATE.md:7` is the FIRST hit and the live value the second, so **anchor on `^- \*\*<field>`** — verified at this SPEC (anchored **1**, bare **2**).

14. **⚠️ A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX — the FIFTH consecutive stage at which this held.** Live hazards: the full-suite startup flake (`subject ready: EOF` **and** `bind: address already in use`, both failing **BEFORE any assertion**, the latter as a **PANIC that aborts the whole binary**, firing **MORE readily under `-race`**) · `reference_sds_init_fetch_timeout_dial_budget_flake` · the PRE-EXISTING `internal/cluster` `-race` outlier flake · two still-**UNINDEXED** load flakes (`internal/httpclient TestOptions_ZeroValue_NoOpDefaults`, `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery`). **Isolate-re-run, then state the classification AND its evidence.** ⚠️ **`0061-lb-ring-hash` is NO LONGER a live flake** — a spread failure there is now a **FINDING**.

15. **Counts at exit (re-run MECHANICALLY in the worktree; never copy them):** fixtures **119** (`ls -d test/fixtures/[0-9]*/ | wc -l`; next-free **0118**; ⚠️ that glob is **NOT** equivalent to `discoverFixtures` — the faithful predicate is `^[0-9]{4}[a-z]?-`) / fuzzers **55** (`grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`) / **stat surface 1205** (BEHAVIOR_CONTRACT doc count; ⚠️ **NO mechanical command**, the chain is discontinuous in TWO recorded places, and ⚠️ **there is NO `Phase 76` ledger entry** — the chain runs `46.1b → 47.1 → 51 → 74 → 75`, so the Phase 77 line follows **Phase 75 at `:5004`** directly; `1205` occurs at **three** lines `:831`/`:847`/`:5004`, and ⚠️ **`:847` also carries a stale *"115-dir"* against an actual 119** — assert the DELTA, never the total) / **BackendKind tail 38** (⚠️ a TAIL VALUE — the file declares **39** constants, 0-38; **do NOT "fix" 38 to 39**) / go.mod modules **2** (⚠️ the phase-61.2 lineage figure — **NOT a repo total**; the single `go.mod` requires **67** = 18 direct + 49 indirect in **TWO** `require` blocks, so a single-block grep undercounts; **do NOT "fix" 2 to 67**) / internal packages **73** / DECISIONS tail **ADR-0299 PROPOSED** (next-free **ADR-0300**) / **reference port for `0118`: `10118`**.

16. **⚠️ TWO DOCUMENTARY DEFECTS, NAMED AS DEFERRALS AND DELIBERATELY NOT FIXED.** Do not silently absorb either.
    - **The documented PUBLIC IMPORT PATH DOES NOT EXIST — and it is worse than a typo.** `head -1 go.mod` is `github.com/pgdad/envoy-go`; the docs say `github.com/esalaine/envoy-go/validate` on **20 lines / 24 occurrences** across four docs (DECISIONS **11**, ROADMAP **4**, STATE_HISTORY **3**, BEHAVIOR_CONTRACT **2**; STATE.md **0**). ⚠️ **`DECISIONS.md:142` is `## ADR-0006: module path github.com/esalaine/envoy-go` — an ADR that DECIDES the wrong path, never superseded.** ⚠️ **Phase 77's own BRAINSTORM propagates it.** A session copying it writes code that does not compile.
    - **`BEHAVIOR_CONTRACT.md:1857`'s two stale `+20` cites**, plus two more in the same file. Fix by replacing line numbers with **symbol anchors** so they cannot drift again.

17. **⚠️ A FOURTH DOCUMENTARY SITE THE BRAINSTORM DID NOT NAME: `DECISIONS.md:16430`.** ADR-0268 records that `validate/validate_test.go` carries *"4 reused `internal/bootstrap` reject-arm cases (dynamic_resources, **layered_runtime**, YAML syntax error, empty document)"* — a **live test roster inside an ADR** that item 7's rewrite falsifies. Amend it at the IMPL or record why not. ⚠️ **ADR-0278's heading is `DECISIONS.md:16670`, NOT the `:16672`** carried by the BRAINSTORM and by `STATE.md`.

18. **The BANKED next subject, for whenever phase 77 closes: `fault.abort.grpc_status`, ~7-9 tasks / +0 on EVERY envelope axis.** Its current state is **worse than "ignored"** — `abortEnabled` is set only inside the `HttpStatus` type switch, so a `grpc_status` variant fires **no abort at all** and the request reaches the backend. A 4-arm codec×content-type probe found the reference branches on the request **`content-type`** not the codec, returns HTTP **200** in every arm, and emits `grpc-status`/`grpc-message` as response **HEADERS not trailers** — which **RETIRES the framework blocker**. ⚠️ **It is an HTTP-filters-family row and does NOT open the gRPC family**; the provenance of the contrary claim is a **ROUTING LABEL** in row 09's summary, and **reading a filing label as a charter is the error.**

## What THIS session does — the phase-77 PLAN

One stage per session. Write `docs/envoy-go/phases/77-runtime-static-layer/PLAN.md` — a TDD spine over the SPEC's **11-13**-task surface (SPEC §10 carries the draft T1-T12) with a **break roster proving each of the FOUR fixture arms fires its OWN assertion** — plus `PROGRESS.md`, and roll `STATE.md` (**lifecycle-state 2 → 3**) and this file to the phase-77 IMPL. **`ROADMAP.md` stays BYTE-UNTOUCHED at a PLAN** and **ROW 77 STAYS `in-progress`**. **`DECISIONS.md` stays BYTE-UNTOUCHED** — ADR-0299 is **PROPOSED** and completes at the IMPL.

⚠️ **ADR-0045 split readiness: a SINGLE FLAT ROW is anticipated**, escape valve armable at the *(snapshot + reject roster)* / *(stats + fixture)* seam. **11-13 is at the upper edge of the modern cadence (9 or 11), not beyond it.**

Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** (`reference_parallel_subagents_private_scratch`) — and ⚠️ **if two agents share ONE worktree, they must stage by EXPLICIT PATH; a `git commit -am` sweeps in a sibling's uncommitted work.** Per-task `gofmt` + `golangci-lint` on touched packages (`feedback_pertask_gofmt_lint`).

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. ⚠️ **THIS HAS NOW FIRED LIVE IN NINE CONSECUTIVE SESSIONS** — observed at this SPEC as `Shell cwd was reset to /home/esa/git/envoy-go` immediately after a `cd` into scratch. **Use `git -C <abs-worktree-path>` for every git command, prefer absolute paths over `cd`, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline — ⚠️ **anchor greps on `^- **<field>`**, not bare) + §Project counts + the §Recent lineage (five bullets: phase-77 SPEC + phase-77 BRAINSTORM + phase-76 IMPL + phase-76 PLAN + phase-76 SPEC).
2. `docs/envoy-go/phases/77-runtime-static-layer/SPEC.md` — **the primary input.** §1.1 the refuted founding premise · §1.2 the **R1-R23** drift ledger · §1.3 what was and was NOT executed · §3.3 the measured semantics (**§3.3.2** the lexical rule, **§3.3.3** the empty-struct branch with pseudocode, **§3.3.4** the inverted precision hazard) · §6 the nine-arm roster · §8 the fixture with its **four** arms · §10 the draft task spine and the gate hygiene · §14 the ADR-0299 shape.
3. `docs/envoy-go/phases/77-runtime-static-layer/BRAINSTORM.md` — ⚠️ **read it AFTER the SPEC and treat its §2.3 semantics table and §6 reject roster as SUPERSEDED** (R1, R19, R20). Still live: §0 the sentinel findings, §2.6 the rivals, §1.7 the eight discharged forward-pointers.
4. `docs/envoy-go/phases/76-lb-ring-hash-spread-margin/PLAN.md` + `PROGRESS.md` — **the richest single source on how this lineage's stages fail**, and the shape of a nine-task TDD spine with a sixteen-break roster.
5. `docs/envoy-go/DECISIONS.md` — **ADR-0299** (this row's, PROPOSED, at the tail) · **ADR-0016** `:387` (unknown-field rejection — load-bearing, and EXECUTED at the SPEC) · **ADR-0089** `:3514` (the two admin deferrals at `:3543`/`:3550`; stays BYTE-UNTOUCHED) · **ADR-0187** `:11280` + **ADR-0195** `:12413` (the six `runtime_key` arms) · **ADR-0278** `:16670` (⚠️ **not `:16672`**) · **ADR-0268** `:16430` (the stale test roster) · ADR-0044 (⚠️ it does **NOT** contain the §Context-draft discipline — use `ADR-0044-as-used`) · ADR-0045 · ADR-0106 · ADR-0288.
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — especially `reference_lifted_reject_hidden_enforcement`, `reference_probe_must_discriminate`, `reference_gate_command_negative_control`, `reference_ordered_assertion_legs_vacuous_on_constant_change`, `reference_fatalf_makes_assertions_unreachable`, `reference_fixture_tb_has_no_logf`, `reference_differential_run_selector`, `reference_spec_drafted_identifier_collision_check`, `reference_quoting_is_not_executing`, `feedback_brief_citations_not_evidence`, `reference_next_prompt_tracked_despite_gitignore`, `feedback_git_worktrees`, `feedback_execution_style`, and **`reference_bash_cwd_reset_commits_to_main`**.
