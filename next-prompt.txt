=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, the active phase is fully landed, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action. (The sentinel does NOT apply right now — phase 50 is chartered and its BRAINSTORM has just landed; the SPEC is next.)

**STATUS AS OF 2026-07-05: phase 50 (`stats-sink-dogstatsd-batching`) BRAINSTORM LANDED; the NEXT stage is its SPEC.**
- HEAD carries the phase-50 BRAINSTORM commit (`docs/envoy-go/phases/50-stats-sink-dogstatsd-batching/BRAINSTORM.md` + ROADMAP row 50 `in-progress` + STATE active-phase). **Re-verify with `git log --oneline -6` + `git status`; do NOT assume.**
- The BRAINSTORM settled the SCOPE via a 4-question user dialogue: Q1 differential proof-shape → a NEW fixture `0094-stats-sink-dogstatsd-batching` (not an in-place `0093` extension); Q2 oversized-single-line posture → mirror the reference — send it alone, uncapped for that one line (confirmed via the `go-control-plane/envoy@v1.32.4` `config/metrics/v3/stats.pb.go:707-713` doc-comment: "By default Envoy will emit one metric per datagram... Note that this value may not be respected if smaller than a single metric" — no drop, no truncation); Q3 degenerate cap value (e.g. an explicit `0`) → accept it, no new strict-reject (an explicit `0` self-degrades to "every line sent alone" via the Q2 rule, behaviorally IDENTICAL to phase 49's unconditional one-per-datagram — so `DogStatsdSinkConfig.MaxBytesPerDatagram` is a PLAIN `uint64`, no pointer needed to distinguish absent-from-zero); Q4 envelope → SINGLE FLAT ROW (a buffering/flush-on-overflow rewrite of `DogStatsdSink.Submit`'s per-line `Write` loop + one config field + one differential; no new `Sink`, no new package; ADR-0045 escape-valve UNCONSUMED).
- **Self-answered from the proto doc-comment (NOT a live probe — the SPEC still live-pins it, D-DSDB-DEFAULT, per `reference_wire_format_both_sides_see_same_bytes`):** an absent `max_bytes_per_datagram` emits one metric per datagram — phase 49's existing unconditional behavior is ALREADY reference-parity when unset.
- **Design (self-answered, SPEC confirms the exact boundary):** the packing algorithm is a SINGLE unconditional accumulate-then-flush-on-overflow loop over the already-formatted lines (delta/tag computation UNCHANGED — batching is purely transport-layer, applied after line formatting); `MaxBytesPerDatagram == 0` is NOT a special-cased branch — it falls out of the general algorithm naturally (every line is "too large for a 0-byte cap" and gets the oversized-alone treatment, exactly reproducing phase 49's behavior with no conditional fork).
- Counts at BRAINSTORM (re-verify, do NOT assume): stat surface **1200** (non-H2 **1196**) / fixtures **95** / fuzzers **52** / BackendKind **38** / DECISIONS **ADR-0266** (next-free ADR-0267). Anticipated at the phase-50 IMPL: fixtures **96** (`0094`); fuzzers anticipated **UNCHANGED at 52** (D-DSDB-FUZZER pins whether the packing algorithm itself needs a dedicated fuzzer — `max_bytes_per_datagram` is an EXISTING field on the EXISTING `parseDogStatsdSinkConfig` arm, already exercised by `FuzzDogStatsdSinkConfigParse`, unlike phase 48/49's NEW-dispatch-arm fuzzers); ADR **ADR-0267**.

=> The NEXT action is the **phase 50 (`stats-sink-dogstatsd-batching`) SPEC** (lifecycle-state 1 → 2). NOT the termination sentinel.

==============================================

## The NEXT action — the phase 50 (`stats-sink-dogstatsd-batching`) SPEC
Write `docs/envoy-go/phases/50-stats-sink-dogstatsd-batching/SPEC-50.md` (docs-only, direct-on-master per the phase-44 through 49 family-row SPEC precedent; the 13/14-section `SPEC-49.md`/`SPEC-48.md` shape). This is where the BRAINSTORM's §10 D-DSDB-* open questions get EXECUTED as LIVE probes against the reference — not merely restated.

**Live probes to execute (per `reference_docker_probe_bridge_network` — a shared Docker bridge network + a decode-ran proof; the reference is `envoyproxy/envoy:contrib-v1.37.2` per `reference_envoy_contrib_image_tagging`):**
- **D-DSDB-DEFAULT:** boot the reference with a bootstrap carrying a `DogStatsdSink` and NO `max_bytes_per_datagram`; confirm every emitted UDP datagram carries exactly one line (live-confirming the proto doc-comment, not just trusting it — `reference_wire_format_both_sides_see_same_bytes`).
- **D-DSDB-BOUNDARY (LOAD-BEARING):** boot the reference with a deliberately small `max_bytes_per_datagram` sized to land EXACTLY on a line-length boundary (construct two lines whose combined length with the `\n` separator equals the cap exactly, then one byte over); observe whether the reference co-locates them (cap is inclusive, `<=`) or splits them into separate datagrams (cap is exclusive, `<`). This is THE load-bearing pin for the packing algorithm's comparison operator.
- **D-DSDB-OVERSIZED:** craft a single metric (via a long tag suffix) whose formatted line alone exceeds the configured cap; confirm the reference sends it as its own oversized datagram with no error, no drop, no truncation, no splitting of the single line's bytes across two datagrams.
- **D-DSDB-JOIN-ORDER:** with a cap large enough to hold several lines, confirm the reference does not reorder, sort, or dedupe lines when packing multiple into one datagram — pure sequential accumulation in emission order.
- **D-DSDB-FUZZER:** confirm (a design/code-reading question, not a live probe) whether the existing `FuzzDogStatsdSinkConfigParse` already exercises the `max_bytes_per_datagram` field once the strict-reject is removed, or whether the packing algorithm (pure Go, not protobuf-parse) warrants its own dedicated fuzzer for panic-safety over adversarial cap/line-length combinations.

**Design work to pin at the SPEC (mirroring the phase-49 SPEC's §Context draft + reject-arm precision):**
- The `bootstrap.go:591` strict-reject removal — confirm the EXACT resulting code shape (the `if dsd.GetMaxBytesPerDatagram() != nil { return ... }` block is deleted entirely, not narrowed) and confirm no OTHER validation is needed per Q3's "accept it" answer.
- `DogStatsdSinkConfig.MaxBytesPerDatagram uint64` — confirm the parse expression (`dsd.GetMaxBytesPerDatagram().GetValue()`, which already returns `0` for a nil wrapper — no extra nil-check needed).
- The exact packing algorithm shape inside `DogStatsdSink.Submit` (`internal/statssink/dogstatsd.go`) — a buffer-accumulate loop over the ALREADY-formatted lines (the existing delta/tag/line-formatting code stays untouched; only the final `s.write(line)`-per-line call is replaced by a buffering stage). Cite exact line numbers against the CURRENT `dogstatsd.go` (re-read it fresh — do not trust line numbers from the BRAINSTORM doc, which cites the file only in prose, not by line).
- The `test/helpers/statsdrecv` extension: a NEW datagram-level `\n`-pre-split BEFORE the existing first-`|`-then-colon per-line `ingest` parsing. Per `reference_line_parser_extension_delimiter_reuse`, trace ONE concrete multi-line datagram byte-for-byte (e.g. two DogStatsd lines joined by `\n`) through the CURRENT `ingest` function as read from the actual file, to confirm the pre-split doesn't collide with anything.
- The `0094-stats-sink-dogstatsd-batching` differential fixture design: clone the ACTUAL landed `0093` driver code (re-read it fresh, not from memory) — same two-per-side-UDP-receiver + hard-`Close()` shape, same `hostGatewayIP` local-helper-duplication pattern per `reference_host_gateway_ip_docker_desktop` if a literal-IP reference→host receiver address is needed. Assertions per BRAINSTORM §2.7: (a) emitted line SET unchanged from an unbatched baseline; (b) at least one received datagram contains >1 line; (c) no datagram exceeds the cap except a deliberately-oversized single line.
- Anchor the ADR-0267 §Context draft (per ADR-0044 — §Context at SPEC, §Decision/§Consequences in-place at the IMPL).

After the SPEC lands (ideally reviewed by a `spec-document-reviewer`-style pass per the phase-48/49 precedent — all code:line + proto citations VERIFIED EXACT against HEAD, the five high-risk areas covered), ROLL THIS FILE FORWARD to point at the phase-50 PLAN (mirror the structure of any prior phase's post-SPEC PLAN router in git history — e.g. the 48.x/49.x PLAN routers).

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** The next stage is the phase-50 SPEC (docs-only, direct-on-master). Lifecycle-state 1 → 2.
- **Subagent-driven execution** (`feedback_execution_style`) in a **fresh worktree** (`feedback_git_worktrees`) for any IMPL; subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit, re-runs the full suite on the frozen HEAD, does the deliberate-break verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). (BRAINSTORM/SPEC/PLAN are docs-only direct-on-master per the family-row precedent — no worktree needed.) Per-task gates: `gofmt -l` + `golangci-lint` on touched pkgs + `go vet` + `go build ./...` (`feedback_pertask_gofmt_lint`).
- **Process anchors:** ADR-0106 (per-leg rows) · ADR-0044 (ADR §Context-at-SPEC / §Decision+§Consequences-at-IMPL) · ADR-0045 (sub-split soft gate) · ADR-0080 (strict-reject) · ADR-0266 (the phase-49 dog_statsd sink this row extends) · next-free **ADR-0267**.
- **Differential discipline** (carries to the new `0094` fixture): `-run 'TestDifferential/<NNNN>'` NEVER bare (`reference_differential_run_selector`); `-count=1` on every break + `-race` (`reference_differential_break_protocol_count1`); the full-suite `subject ready: EOF` startup flake is isolatable (`reference_differential_fullsuite_startup_flake`); the Docker bridge + decode-ran proof (`reference_docker_probe_bridge_network`); the host-gateway literal-IP helper for reference→host UDP reachability (`reference_host_gateway_ip_docker_desktop`); driver-owned receivers are NOT BackendKinds (`reference_differential_grpc_receiver_driver_owned`); the live reference is the wire truth (`reference_wire_format_both_sides_see_same_bytes`); periodic-sink two-receivers + hard-Close (`reference_periodic_sink_differential_two_receivers`); a burst-all-requests delta sink needs a post-convergence stability barrier (`reference_delta_sink_differential_stability_barrier`); DogStatsd tag order is the extractor's natural/unsorted order (`reference_dogstatsd_tag_order_unsorted`); extending a delimited line parser can break the CURRENT delimiter split — trace an example line first (`reference_line_parser_extension_delimiter_reuse`); the admin-interface wire-name collision on shared HCM stat names (`reference_admin_interface_wire_name_collision`).

## Read first (in order)
1. THIS FILE — the cold-start router (phase 50's BRAINSTORM landed; the next action is its SPEC).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 50 BRAINSTORM done`) + the counts (1200 / 95 / 52 / 38 / ADR-0266, next-free ADR-0267).
3. `docs/envoy-go/ROADMAP.md` — row 50 `in-progress` (the SEVENTH Observability-family row) + the Observability family-progress note.
4. `docs/envoy-go/DECISIONS.md` — **ADR-0266** (the phase-49 dog_statsd sink this row extends) + next-free **ADR-0267** (the phase-50 batching anchor-to-be).
5. `docs/envoy-go/phases/50-stats-sink-dogstatsd-batching/BRAINSTORM.md` — the settled scope + the §10 D-DSDB-* open questions this SPEC must execute as live probes.
6. `docs/envoy-go/phases/49-stats-sink-dogstatsd/` (`SPEC-49.md`/`PLAN-49.md` + `internal/statssink/dogstatsd.go` + `internal/bootstrap/bootstrap.go:575-607` `parseDogStatsdSinkConfig` + `test/helpers/statsdrecv` + the `0093-stats-sink-dogstatsd` fixture/driver) — the LANDED phase-49 code this SPEC extends; re-read it FRESH (line numbers drift), do not trust citations from the phase-50 BRAINSTORM doc, which cites this code only in prose.
7. `go-control-plane/envoy@v1.32.4/config/metrics/v3/stats.pb.go:695-787` (`DogStatsdSink`) — the `max_bytes_per_datagram` field + its doc-comment (already read this session; re-verify if resolving a newer go-control-plane version).
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_subagent_autocommit_claudemd`, `feedback_pertask_gofmt_lint`, `reference_roadmap_split_phase_row_done`, `reference_line_parser_extension_delimiter_reuse`, `reference_periodic_sink_differential_two_receivers`, `reference_host_gateway_ip_docker_desktop`, `reference_envoy_contrib_image_tagging`, plus the differential-discipline references above).
