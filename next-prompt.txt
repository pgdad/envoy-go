=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-VERIFIED 2026-07-18 (re-run MECHANICALLY at the phase-68 BRAINSTORM close, in the stage worktree) — it does NOT fire. `stop` was NOT created.** Actual output:
- **(1) prints `NOT DONE: row 68`** — registering row 68 `in-progress` at the phase-68 BRAINSTORM stage-close re-opened check (1). It stays open until the phase-68 IMPL six-gate flips row 68 `done`.
- **(2) prints THREE live "candidates:" sentences** — HTTP/3, xDS, Observability ⇒ three families STAY OPEN. **Cite the command, never the adjective** (`reference_sentinel_deferred_sentence_live_vs_historical`): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` ⇒ **3**.
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** ⇒ THREE families never opened. Operational-tooling also STAYS OPEN.

Checks (2)+(3) print ⇒ the sentinel does NOT fire. Abundant work remains.

==============================================

## STATUS AS OF 2026-07-18: THE PHASE-68 BRAINSTORM IS COMPLETE AND LANDED. ROW 68 IS `in-progress`. THIS SESSION PERFORMS THE PHASE-68 SPEC.

**What the phase-68 BRAINSTORM landed** (`git log --grep 'phase 68'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): the SELF-PICKED subject `tls-cvc-empty-dynamic-fallback` (the SIXTH xDS-family row) — complete the `combined_validation_context` MERGE semantics on an ACKed-but-EMPTY dynamic half: the CVC arm of `NewDownstreamConfig`, on an ACK-succeeded-but-empty dynamic fetch, falls back to `default_validation_context.trusted_ca` (via `loadTrustedCAPool` + the phase-67 `installPool`) and SERVES, where envoy-go today boot-FAILS. On the honored surface (only `trusted_ca` — P1) the reference `MergeFrom` reduces EXACTLY to "dynamic `trusted_ca` if present, else default's" — NO `proto.Merge` (ADR-0287's "message-returning seam" pre-characterization refuted by re-derivation). LIVE-PROBED (Dossier A, `envoyproxy/envoy:v1.37.2`, fresh container per arm): empty-served → fall back + SERVE (arm 1); timeout → fail-closed, NO fallback (arm 3, the discriminator: ACK/`update_success` vs `init_fetch_timeout`); empty-both → serve UNAUTHENTICATED (arms 4a/5); missing default → PGV boot-reject (arm 4b, PGV-only). Envelope: ONE production function (`internal/tls/config.go`) + ONE new `internal/xds` exported symbol (the streak break, stated loudly); CVC-ONLY; ~8–9 tasks. Counts UNCHANGED (docs-only): fixtures **112** · fuzzers **55** · stat **1201** · BackendKind **38** · DECISIONS tail **ADR-0289** (next-free **ADR-0290**) · go.mod modules **2**.

**⚠️ PUSH (idempotent — evaluate, then act):** FIRST ACTION: `git fetch`; if origin/master is BEHIND local master and the local tip is the phase-68 BRAINSTORM squash, `git push origin master` per `feedback_push_to_origin`; if in sync, proceed; if REJECTED (diverged), STOP and reconcile — never rebase away landed squashes.

## What THIS session does — phase 68 SPEC

One stage per session: SPEC ONLY (docs-only, ZERO production `.go`; any probe harness lives in agent-PRIVATE scratch OUTSIDE the repo — `reference_parallel_subagents_private_scratch`). Fresh worktree off master, branch `phase-68-tls-cvc-empty-dynamic-fallback-spec`, per `feedback_git_worktrees`. **RE-DERIVE the BRAINSTORM rather than execute it** — a router pointer is not evidence (`feedback_brief_citations_not_evidence`); where it cites, go look; where it claims control flow, walk the call graph; default to REFUTED (`reference_quoting_is_not_executing`). **Row 68 STAYS `in-progress`** (the IMPL flips it `done` at the six-gate, ADR-0106, the SOLE leg). **ADR-0290 §Context DRAFTS at this SPEC per ADR-0044** — the DECISIONS tail flips ADR-0289 → ADR-0290 AT THIS SPEC COMMIT (the §Context append IS the tail flip; the IMPL completes §Decision/§Consequences IN PLACE, no further flip; next-free becomes ADR-0291).

**The SPEC owes (the D-EDF-* docket — RE-DERIVE each; the BRAINSTORM §10 carries the full list):**
- **D-EDF-CLASSIFY (the row's ONE growth risk):** coarse `errValidation` export vs a NARROW `errEmptyValidationContext` sentinel + a wrap-site in `parseValidationSecret`. **RUN a discriminating probe with THREE served shapes as separate arms** (NOT run at the BRAINSTORM — Dossier A limit): (i) no `trusted_ca` specifier (the truly-empty case, confirmed fallback), (ii) `trusted_ca:{inline_bytes:""}` (specifier set-but-empty — hits `AppendCertsFromPEM`=false at secret.go:134-135, NOT the `dataSourceBytes`-none arm), (iii) a corrupt/malformed PEM. If the reference NACKs/rejects (ii)/(iii) (does NOT fall back), the NARROW sentinel scoped to (i)-only is MANDATORY. Lean NARROW (a set-but-broken anchor must never silently degrade to the default). `reference_probe_must_discriminate`.
- **D-EDF-STREAMPOSTURE (the RESIDUAL divergence — the adversarial pass caught this):** envoy-go TODAY classifies the empty served resource as `errValidation` → a NACK on the wire + `sds.update_rejected`, where the reference cleanly ACKs (`update_success`). After the fallback the LISTENER matches the reference but the SDS-STREAM posture still diverges. Decide: (a) RECLASSIFY the ACKed-empty `validation_context` as a stream-level SUCCESS in envoy-go (ACK like the reference — larger, touches `applyValidationResponse`) vs (b) accept + NAME the NACK + `update_rejected` posture as a documented RESIDUAL departure (smaller — the traffic surface, the only differentially-asserted one, already matches). Lean (b) for a small row, but PICK EXPLICITLY — do NOT leave it silent.
- **D-EDF-FETCHSHAPE:** classified-error-then-fallback vs a `(nil, empty=true, nil)` fetch variant — prefer the former (the phase-67 nil-pool guard stays byte-intact; `reference_go_client_cert_withholding`).
- **D-EDF-PGV:** add an explicit reject for a CVC missing `default_validation_context` (matching the reference's PGV-only boot-reject, `reference_pgv_forecloses_go_hazard`) or leave a silent gap — lean ADD.
- **D-EDF-EMPTYBOTH-REQUIRE:** empty-both + require=true → the RETAINED envoy-go-STRICT reject (the phase-67 require-needs-an-anchor invariant); confirm the exact routing.
- **D-EDF-FIXTURE `0111-tls-cvc-empty-dynamic-fallback`** (fixtures 112 → 113; the 0109 chassis; three-arm FORCED-SEND verdict) + **D-EDF-FUZZSEED** (dispatch-verify to avoid the phase-66/67 `"downstream"`-side SDS-seed vacuity trap).
- **Envelope to PIN:** ONE functionally-edited production file (`internal/tls/config.go`) + **ONE new `internal/xds` exported symbol** (the FIRST break of the zero-new-symbol streak, DECISIONS.md:16973 — call it EXPLICITLY, never a silent relaxation; cycle guard STANDS TYPE-level); `internal/boot`/`validate/`/`internal/listener`/`test/helpers/sdsserver` UNTOUCHED; CVC-ONLY; +0 packages/modules/stats/fuzzers/BackendKinds. Re-check `git diff go.mod` after tidy (`reference_new_subpackage_pulls_transitive_module`); re-verify `go list -deps ./internal/xds` (no `...`).

**Sentinel maintenance:** empty-dynamic/Design B is named in NO live `candidates:` sentence — do NOT narrow one (the phase-64/66/67 precedent; `reference_sentinel_deferred_sentence_live_vs_historical`). Check (1) keeps printing `NOT DONE: row 68` until the IMPL; (2)+(3) unchanged ⇒ sentinel still does NOT fire.

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline). The `next-skill` line carries the D-EDF-* docket + the owed reads.
2. `docs/envoy-go/phases/68-tls-cvc-empty-dynamic-fallback/BRAINSTORM.md` — the FULL BRAINSTORM to RE-DERIVE (the D-EDF-* decisions §2, the edit-site enumeration §6, the open questions §10). RE-DERIVE, do not execute.
3. `docs/envoy-go/ROADMAP.md` — row 68 (`in-progress`), the §Schema, the THREE live deferred sentences, the family headings.
4. `docs/envoy-go/DECISIONS.md` — ADR-0287 (CVC pool substitution + the P1–P5 theorem the pick EXTENDS) + ADR-0289/0286/0280 (the SDS/verify-if-presented lineage). Tail ADR-0289; next-free ADR-0290 — re-derive (`grep '^## ADR-0290' → 0`).
5. `internal/tls/config.go` (the CVC arm ~:136-198, the P1–P5 block ~:137-164, `loadTrustedCAPool` :298, the sub-field rejects ~:442-461) + `internal/xds/{secret,provider,stream}.go` (the seam classifier `errValidation` stream.go:33) — the SPEC re-derives the constructibility itself.
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_*` + `reference_*`; trust MEMORY.md's index.
