=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**Check (1) is NOT permanently silent.** It is silent only while no phase is open. Row 56 is `in-progress`, so **check (1) prints `NOT DONE: row 56`** and will keep doing so until the 56.2 IMPL flips it. **The sentinel is an AND of all three checks**; (2) and (3) carry the remaining work.

**SENTINEL RE-CHECKED 2026-07-10 (post-phase-56.2-PLAN) — it does NOT fire. Do NOT create `stop`.** Evidence:
- (1) prints `NOT DONE: row 56` ⇒ phase 56 is OPEN (56.1 headers landed; the 56.2 bodies leg is SPEC'd AND PLAN'd but its IMPL remains).
- (2) prints the Observability family's deferred candidates ⇒ the **Observability family STAYS OPEN**.
- (3) prints `NEVER OPENED: HTTP/3`, `gRPC`, `xDS`, `Runtime`, `WASM` ⇒ FIVE families never opened; the **Operational-tooling family** also STAYS OPEN.

**STATUS AS OF 2026-07-10: phase 56.2 (`http-tap-filter`, BODIES leg) PLAN is DONE and LANDED.** Row 56 is `in-progress`. Counts UNCHANGED at the PLAN (docs-only): stat surface **1201** · fixtures **101** (tail `0099-http-tap-headers`) · fuzzers **53** · BackendKind tail **38** · DECISIONS tail **ADR-0273** (next-free **ADR-0274**). Anticipated at the 56.2 IMPL: fixtures **102** (`0100-http-tap-bodies`), DECISIONS tail **ADR-0274**, ROADMAP row 56 → **done**, all other counts **+0**.

**Locating the stage squash (a router cannot name its own sha — writing the sha changes it).** Identify it by SUBJECT, never by sha and never by position:
```sh
git log --oneline --grep='^phase 56.2 (http-tap-filter, bodies leg) PLAN' -1
```
This stage folded its own `next-prompt.txt` roll INTO the squash (per `reference_next_prompt_tracked_despite_gitignore`), so the squash **IS** the master tip — *unless* a router-only fixup commit sits on top. **Do not assume either shape.** The `--grep` above is correct in every shape.

**THE NEXT ACTION: the phase-56.2 (`http-tap-filter`, BODIES leg) IMPL.**

==============================================

## What THIS session does (an IMPL, lifecycle-state 3 → done-for-the-phase)

This session EXECUTES `PLAN-56.2.md`'s **7-task TDD spine** (subagent-driven per `feedback_execution_style`), landing production code in `internal/filter/http/tap` + the `0100-http-tap-bodies` differential + the ADR-0052 atomic docs bundle. Fresh worktree `.worktrees/phase-56.2-impl`, branch `phase-56.2-http-tap-filter-impl`. At the six-gate close **ROADMAP row 56 flips `done`** (the FINAL leg of phase 56 — `reference_roadmap_split_phase_row_done` + ADR-0106; no parent rollup). DECISIONS tail advances **ADR-0273 → ADR-0274** (§Decision/§Consequences land at THIS IMPL per ADR-0044; §Context was drafted at the SPEC).

1. Re-verify the cold-start baselines against the CURRENT master tip (`git log --oneline -1` — do not assume): fixtures **101**, fuzzers **53**, DECISIONS tail **ADR-0273**, BackendKind **38**, stat surface **1201**. Paste the literal output into `PROGRESS-56.2.md`'s Baseline Counts (PLAN T1).
2. **Read the landed 56.2 PLAN as the spine** — `docs/envoy-go/phases/56-http-tap-filter/PLAN-56.2.md`: the 7 tasks (T1 baselines · T2 config caps+format · T3 tap.go accumulation · T4 trace.go bodyProto+buildTrace · T5 fixture 0100 · T6 AssertStats+4 breaks · T7 docs bundle), the four §12 D-question PINS (D-TAP-BODY-UTF8 → `strings.ToValidUTF8` sanitize; ASBYTES-DIFF/CHUNKTEST → unit tests; EARLYOUT → defer), and the "Key source seams" block (which CAUGHT two SPEC citation drifts — RE-DERIVE AGAIN at the IMPL per `feedback_brief_citations_not_evidence`). Also read `PROGRESS-56.2.md`'s 12-row break ledger.
3. **Execute the spine, subagent-driven.** Each task: RED (a failing test that can actually FIRE) → GREEN → REFACTOR, per-task `gofmt -l` + `golangci-lint` + `go vet` + `go build` (`feedback_pertask_gofmt_lint`). The controller verifies each commit, cleans leak files, RE-PERFORMS every deliberate break ITSELF with `-count=1` (`reference_differential_break_protocol_count1`) confirming WHICH assertion fired (`reference_deliberate_break_wrong_assertion`), records the LITERAL failing text in the ledger, and squashes at stage-close. Watch for MORE vacuous-proof classes than the PLAN's review found (the 56.1 IMPL caught TWO — PROGRESS-56.1 §Findings IMPL-1/IMPL-2).
4. **Honor the load-bearing pins** (PLAN Global Constraints): default cap **1024** when the `UInt32Value` wrapper is nil, present-0 → **0** (NOT 1024); strict-`>` truncation (a body EXACTLY at the cap is NOT truncated — the `0100` boundary arm at length==C JUSTIFIES break (c)); `sawReqBody`/`sawRespBody` distinguishes cap-0-empty-but-truncated (body PRESENT) from a never-fired hook (body OMITTED); tap returns `DataContinue` ALWAYS (read-only observer — never `DataStopIteration*`/`OverwriteBody`); the AS_STRING `ToValidUTF8` sanitize (Go protojson ERRORS on non-UTF8 in a `string`, the sink swallows it → whole-trace drop); `EmitDefaultValues` renders `truncated` unconditionally; the `0100` fixture drives UTF-8-safe ASCII bodies only and REUSES `HTTPEchoBody` (BackendKind 38, +0).
5. **The ADR-0045 split gate is CONSUMED** (7 tasks, margin 8 under `~15`). If a task grows a second deliverable, split it + re-open the gate — but 56.2 is the FINAL leg; there is NO 56.3 to absorb spillover.
6. **Land the IMPL** (commit + push per `feedback_push_to_origin`; `go mod tidy -diff` EMPTY; full suite green on the frozen HEAD), flip **row 56 `done`**, then **ROLL THIS FILE FORWARD** to the NEXT stage — the next Observability-family row's BRAINSTORM (or a new subject per the ROADMAP), NOT a 56.3.

## Honor throughout (the discipline, unchanged)
- **Lifecycle = one stage per session.** This session: the phase-56.2 IMPL (production code + differential + docs; the phase's final stage).
- **`next-prompt.txt` IS TRACKED** (it appears in `.gitignore` line 2, but a `.gitignore` entry does not apply to an already-tracked file — `git ls-files --error-unmatch next-prompt.txt` succeeds). Edit it inside the stage worktree and fold it into the squash (`reference_next_prompt_tracked_despite_gitignore`). A router can't name its own sha — locate the squash by SUBJECT (`git log --grep`), never by position.
- **A BRIEF'S CITATIONS ARE NOT EVIDENCE** (`feedback_brief_citations_not_evidence`). Re-derive every `file:line` from source; never verify a citation against the document that made it. This row has paid out FIVE times now (a false double-`OnDestroy` at the 56.1 SPEC; two vacuous-proof defects at the 56.1 PLAN; two MORE vacuous-proof classes caught AT the 56.1 IMPL; and at the 56.2 PLAN, TWO stale SPEC citations — `router.go:625` does not exist (encode-one-call is `connection.go:743`/`h2dispatch.go:582`); the proto accessor line numbers are for a different module rev (accessor NAMES verified: `OutputConfig.GetMaxBufferedRxBytes/TxBytes`, `Body_AsString{AsString}`/`Body_AsBytes{AsBytes}`/`Truncated`, `OutputSink.GetFormat`)). Re-derive them AGAIN at the IMPL.
- **Deliberate-break discipline** (design breaks that FIRE): the strict-`>` break needs the body-length-==-cap boundary arm or it is vacuous (only the `truncated` flag flips, NOT the payload — confirm WHICH fired); the cap-0 test must assert `sawReqBody` (not just `reqTrunc`); the omitted-body test must grep RAW BYTES for `"body"` (a decoded `GetBody()==nil` cannot tell OMITTED from `null` — the 56.1 IMPL-1 defect); the read-only-observer break needs a distinct status `t.Errorf` (not folded into a Fatalf precondition). The `-run` selector must be the FULL `TestDifferential/0100-http-tap-bodies` (`reference_differential_run_selector`); breaks use `-count=1` (`reference_differential_break_protocol_count1`); `t.Errorf`-per-property, never `t.Fatalf` mid-list (`reference_fatalf_makes_assertions_unreachable`).
- **protojson detrand** (ADR-0273 §Consequences): the body render is ALSO subject to `internal/detrand` whitespace randomization. NEVER compare protojson output raw-byte-for-byte across build configs — use `encoding/json.Compact` canonicalization + detrand-robust substring pins. The differential decodes payloads; it never byte-compares.
- **Process anchors:** ADR-0106 (no parent rollup — row 56 flips `done` ONLY at THIS 56.2 IMPL) · ADR-0044 (§Context at SPEC; §Decision/§Consequences at IMPL — ADR-0274 body lands HERE) · ADR-0045 (the split gate is CONSUMED; 56.2 must NOT absorb spillover — it is the final leg) · ADR-0052 (atomic six-gate bundle) · ADR-0071 (two-step filter factory) · ADR-0080 (byte-stable rejects) · ADR-0196 (the encode-side `ResponseStatus()` accessor) · ADR-0227 (reference `envoyproxy/envoy:contrib-v1.37.2`) · ADR-0273 (the landed headers leg) · next-free **ADR-0274** (the bodies leg — its body lands at THIS IMPL).

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-56.2 IMPL is next).
2. `docs/envoy-go/phases/56-http-tap-filter/PLAN-56.2.md` — the 7-task TDD spine; esp. Global Constraints, the D-question resolutions, the "Key source seams" (with the two caught citation drifts), and each task's deliberate-break + Vacuous-proof guard.
3. `docs/envoy-go/phases/56-http-tap-filter/PROGRESS-56.2.md` — the baseline block to re-record + the 12-row break ledger to fill with LITERAL failing text.
4. `docs/envoy-go/phases/56-http-tap-filter/SPEC-56.2.md` — the bodies-leg charter (§1.1 AMEND-TAP-BODY2-*, §3 design, §8 the `0100` differential + breaks, §11 empirical pins, §13 the ADR-0274 §Context draft).
5. `docs/envoy-go/phases/56-http-tap-filter/PROGRESS-56.1.md` — the landed headers spine's §Findings (IMPL-1/2/3) + 14-row break ledger (the vacuous-proof traps to honor).
6. `docs/envoy-go/DECISIONS.md` — **ADR-0273** (the landed headers decision) + ADR-0044/0045/0052/0071/0080/0106/0196 for process.
7. The landed 56.1 code the IMPL extends: `internal/filter/http/tap/{config.go,tap.go,trace.go,sink.go}`, and the seams the PLAN cites (`internal/filter/http/types.go` DecodeData/EncodeData, `internal/filter/hcm/{connection.go,h2dispatch.go}` chunk-vs-one-call, `internal/filter/http/buffer/buffer.go` accumulation shape, `test/fixtures/0099-http-tap-headers/driver/driver.go` the fixture model, `test/differential/fixture/fixture.go` HTTPEchoBody).
8. `docs/envoy-go/STATE.md` — the active-phase header + full history.
9. `docs/envoy-go/ROADMAP.md` — §Schema, row 56, and the Observability family paragraph.
10. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline and the `reference_*` traps.
