RESUME a fresh session for envoy-go (router last updated 2026-06-28; the **phase 46.2 (tracing Zipkin+B3) SPEC is DONE and LANDED** — `docs/envoy-go/phases/46-tracing/SPEC-46.2.md`, docs-only direct-on-master; the §11 D-TRACE-ZIPKIN-* empirical pins were EXECUTED IN-SESSION live against `envoyproxy/envoy:contrib-v1.37.2` per `reference_docker_probe_bridge_network` [a shared bridge `zipkinprobe-net`; a `python:3-slim` probe container = HTTP backend (echoing inbound headers) + an HTTP Zipkin collector (de-chunking + JSON-dumping each POST); decode-ran proof: `tracing.zipkin.spans_sent: 8` + 6 `reports_sent` POSTs captured — all probe artifacts in the scratchpad, the repo tree is clean]; the spec-document-reviewer APPROVED after one fold-iteration [NO Critical; 3 Important folded: (1) the §5 module pin is `go-control-plane/envoy v1.32.4` (the import-resolved submodule) NOT the old-root `v0.12.0` the on-disk `find` hit; (2) the 14th Zipkin tag `peer.address` restored to the enumerations (the probe captured 14 tags incl. `peer.address`, the draft had enumerated only 13); (3) `upstream_cluster`/`upstream_cluster.name` (envoy-go emits EMPTY — `reference_tracing_upstream_cluster_framework_gap`, the 46.1b as-built) + `peer.address` (env-specific) moved to the KEY-PRESENT-only / value-UNasserted §8.1 set; + 2 nits]). **The 46.2 leg is the SECOND-and-FINAL chartered leg of the by-exporter split** (46.1 core+OTLP [DONE, ADR-0260 CLOSED] / 46.2 Zipkin+B3). **The next action is the 46.2 PLAN** — the TDD task spine for the Zipkin exporter + B3 propagation (resolves the §12 D-TRACE-ZIPKIN-* PLAN questions; ANCHORS ADR-0261 §Decision/§Consequences at the IMPL). **ROW 46 (`tracing`) STAYS `in-progress`** and the Observability FAMILY STAYS OPEN — the 46.2 IMPL is the FINAL chartered tracing leg, so its six-gate FLIPS row 46 → `done` (per ADR-0106 + `reference_roadmap_split_phase_row_done`); the Observability family STAYS OPEN regardless (stats sinks + the tap filter remain future rows — there is NO family-close at 46.2, per the phase-46 BRAINSTORM §1.3 + confirmed against ROADMAP at the 46.2 SPEC).

HEAD is the **46.2 SPEC docs-only commit** atop the 46.1b IMPL squash `eef54c41`. **Re-read the shas with `git log --oneline -8`, do NOT assume.** Confirm `git status` clean (the untracked `.claude/` is session cruft, leave it) + master == origin/master.

**As-built at the 46.2 SPEC (the design the 46.2 PLAN turns into a task spine — re-confirm line numbers before editing, files evolve):**
- The 46.1a/46.1b `internal/tracing` package is the home: `config.go` (`NewConfig` — TODAY accepts ONLY the OpenTelemetry provider; the `otelTypeName` gate at `:70`), `decision.go` (`Decide` — extracts `traceparent` INTERNALLY at `:64`; the provider-AGNOSTIC sampling/request-id precedence — REUSED verbatim by Zipkin), `propagation.go` (the W3C `traceparent` extract/inject + the `TraceContext{TraceID,ParentID,Sampled,TraceState}` shape), `span.go` (`Span`/`BuildServerSpan` — hardcodes `Name="ingress"` + the 16-attr roster; `toProto`), `exporter.go` (the `Exporter` interface `{Export(*Span); Close() error}` at `:56` — the 46.2 extension point; `OTLPExporter` at `:73`; `ExporterProvider` at `:243` — TODAY OTLP-only, keyed by cluster), `stats.go` (the 5 HCM-scoped + 2 OTLP tracer-scoped counters).
- **The Zipkin exporter is a SECOND `tracing.Exporter` impl** (`ZipkinExporter`) behind the EXISTING seam, exporting the SAME `Span` over a DIFFERENT transport — an HTTP/1 `POST <collector_endpoint>` of a Zipkin **v2 JSON** span ARRAY to the `collector_cluster`, via the phase-22 `httpclient.Client.ClusterDispatch(ctx, clusterName, *http.Request, *cluster.Manager)` (`internal/httpclient/httpclient.go:269`, ADR-0177 — the HTTP-over-cluster primitive already consumed by `internal/wasm/http_call.go`; does `clusterMgr.Get` [the cluster-exists gate] + `PickEndpoint` + a stdlib HTTP/1 POST). **NOT `grpcclient.Dialer`/`OTLPTracesClient`** (those are H2/gRPC); the collector cluster is plain HTTP/1 (no `http2_protocol_options{}`/`UseH2`). The FIRST non-gRPC tracing exporter + the FIRST `ClusterDispatch` consumer outside Lua/wasm.
- **Provider dispatch:** `NewConfig` (config.go) generalizes the provider arm from "OTel only" to "OTel OR Zipkin" (dispatch on the `typed_config` proto type — `zipkinTypeName` sibling of `otelTypeName`); the `ExporterProvider` dispatches on provider kind → builds an `OTLPExporter` (gRPC) or a `ZipkinExporter` (HTTP). The HCM wiring (`builtins.Deps.TracingExporters` → the per-HCM `Exporter` handle) is UNCHANGED — it already holds an interface.
- **B3 propagation** is the Zipkin analog of the 46.1a W3C engine: a `b3.go` codec — `ExtractB3` (single `b3` header `<traceid>-<spanid>-<sampled>[-<parentid>]` OR multi `X-B3-{TraceId,SpanId,ParentSpanId,Sampled,Flags}`) + `InjectB3` (writes the MULTI-HEADER `X-B3-*` set toward the upstream — TraceId/SpanId/Sampled/+ParentSpanId-when-continued). `Decide` is propagation-format-agnostic at the Decision level but EXTRACTS `traceparent` internally today — a small refactor lets `Decide` accept an already-extracted `(TraceContext, bool)` codec-dispatched by the caller (D-TRACE-ZIPKIN-DECIDE-SEAM, §12).
- **The Zipkin v2 JSON span encoder** (a `zipkin.go`, stdlib `encoding/json` — NOT `toProto`): span `name`=the request AUTHORITY (NOT the OTLP `"ingress"` constant), `kind:"SERVER"`, `timestamp`/`duration` in MICROseconds, `id`==low64(`traceId`) for a fresh root, `parentId`/`shared` per `shared_span_context`, 14 STRING `tags` (the 16-attr roster MINUS `node_id`/`zone`; `upstream_cluster(.name)` emit EMPTY [framework gap] + `peer.address` env-specific ⇒ KEY-PRESENT-only cross-side). `trace_id_128bit` toggles the 16/32-hex trace-id width; `collector_hostname` overrides the POST `Host` (default = the cluster name).
- **Counts at 46.2 SPEC (the 46.2 PLAN baseline — re-verify, do NOT assume):** stat surface **1198** (H2 cluster; non-H2 **1194**) / fixtures **89** (tail `0087-tracing-otlp`) / fuzzers **48** / BackendKind **38** / DECISIONS **ADR-0260** (CLOSED; next-free **ADR-0261**). UNCHANGED at SPEC (docs-only). Anticipated at the 46.2 IMPL: stat **1198+N** (N≈2–4 `tracing.zipkin.*`; IMPL-pinned) / fixtures **90** (`0088-tracing-zipkin`) / fuzzers **49** (`FuzzExtractB3`) / BackendKind **38** (driver-owned HTTP receiver) / DECISIONS **ADR-0261**. ZERO new Go packages + ZERO new go.mod modules anticipated.
- **The §12 D-questions the 46.2 PLAN resolves:** D-TRACE-ZIPKIN-CONFIG-SHAPE (the provider-dispatch config packaging — a `ProviderKind` enum + a tagged `TracingConfig`, vs a per-provider sub-struct); D-TRACE-ZIPKIN-DECIDE-SEAM (the `Decide`-takes-extracted-context refactor); D-TRACE-ZIPKIN-SPAN-NAME (carry the request authority provider-neutrally without disturbing the OTLP `"ingress"` constant — prefer a `Span.Authority` field); D-TRACE-ZIPKIN-ID-DERIVE; D-TRACE-ZIPKIN-TRANSPORT-WIRING (thread `*httpclient.Client` + `*cluster.Manager` into the `ExporterProvider` Zipkin arm; the `*http.Request` build; the non-2xx → drop classification); D-TRACE-ZIPKIN-RECEIVER-WIRING (the `0088` reachability + the `test/helpers/zipkincollector` shape); D-TRACE-ZIPKIN-STATS-FINAL (the exact +N); D-TRACE-ZIPKIN-ANNOTATIONS (emit the `"ss"` annotation or omit); D-TRACE-ZIPKIN-FUZZER (land `FuzzExtractB3`).

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** The next stage is the **46.2 PLAN** (`superpowers:writing-plans` — the ~10–12-task TDD spine in §10 of SPEC-46.2.md; resolve the §12 D-TRACE-ZIPKIN-* PLAN questions; the plan-document-reviewer loop). Lifecycle-state 2 → 3.
- **Subagent-driven + worktrees** (`feedback_execution_style` · `feedback_git_worktrees`): the PLAN is a docs-stage (docs-only direct-on-master per the 46.1a/46.1b PLAN precedent); the IMPL that follows is subagent-driven in a fresh worktree.
- **Execution-chaos lesson** (`feedback_subagent_autocommit_claudemd`): dispatched subagents AUTO-COMMIT (global CLAUDE.md). Do NOT fight it — the CONTROLLER verifies EACH commit (correct fileset, real non-vacuous tests via `-v` + read assertions, gates green), cleans stray leak files, re-runs the FULL suite on the FINAL FROZEN HEAD, does the deliberate-break verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_subagents_no_push` · `feedback_push_to_origin`).
- **Per-task gates** (`feedback_pertask_gofmt_lint`): `gofmt -l` + `golangci-lint run` on touched pkgs + `go vet` + `go build ./...`.
- **Differential discipline (for the 46.2 IMPL):** `-run 'TestDifferential/0088'` NEVER bare `'0088'` (`reference_differential_run_selector`); `-count=1` on every break + `-race` run (`reference_differential_break_protocol_count1`); the exporter writer goroutine is a background mutator → FULL-package `-race` (`reference_full_suite_race_after_background_mutator`); poll-to-converge release barrier (`reference_concurrency_differential_release_barrier`); assert the aggregated PAYLOAD not framing (`reference_streaming_sink_differential_framing`); the driver-owned HTTP Zipkin receiver is NOT a BackendKind (`reference_differential_grpc_receiver_driver_owned`); the Docker bridge + decode-ran proof (`reference_docker_probe_bridge_network`); the live-reference probe is the wire truth (`reference_wire_format_both_sides_see_same_bytes`); the EMPTY-value tags stay UNasserted cross-side (`reference_tracing_upstream_cluster_framework_gap`).
- **Process anchors:** ADR-0106 (per-leg rows; row 46 `in-progress` until the 46.2 IMPL) · ADR-0044 (ADR §Context-at-SPEC / §Decision+§Consequences-at-IMPL — ADR-0261 §Context drafts at SPEC-46.2 §13, body lands at the 46.2 IMPL) · ADR-0177 (the `httpclient.ClusterDispatch` HTTP-over-cluster primitive — the Zipkin transport) · ADR-0158 (the `grpcclient.Dialer` — N/A for Zipkin, HTTP not gRPC) · ADR-0080 (strict-reject) · ADR-0016 (proto blank-import) · next-free **ADR-0261**.

## Read first (in order)
1. THIS FILE — the cold-start router (the 46.2 SPEC is DONE + LANDED; the next action is the 46.2 PLAN; row 46 `in-progress`).
2. `docs/envoy-go/phases/46-tracing/SPEC-46.2.md` — the 46.2 charter (the §10 task structure the PLAN spine follows; the §11 D-TRACE-ZIPKIN-* pins; the §12 PLAN D-questions; the §13 ADR-0261 §Context draft).
3. `docs/envoy-go/phases/46-tracing/PLAN-46.1b.md` + `PLAN-46.1a.md` — the sibling 46.1 TDD-spine PLANs (the house shape the 46.2 PLAN mirrors).
4. `internal/tracing/` (the whole package) — the `Exporter` seam + `ExporterProvider` + `NewConfig` + `Decide` + `propagation.go` + `span.go` the 46.2 PLAN extends.
5. `internal/httpclient/httpclient.go:269` (`ClusterDispatch`) + `internal/wasm/http_call.go` — the HTTP-over-cluster transport the Zipkin exporter reuses.
6. `docs/envoy-go/STATE.md` — the active-phase header (`phase 46.2 SPEC done`) + the recorded NEXT (the 46.2 PLAN) + the counts (1198 / 89 / 48 / 38 / ADR-0260).
7. `docs/envoy-go/ROADMAP.md` — row 46 (`tracing`, `in-progress`, "46.1 COMPLETE; 46.2 SPEC done; 46.2 PLAN next").
8. `docs/envoy-go/DECISIONS.md` — **ADR-0260** (the 46.1 anchor, CLOSED) + next-free **ADR-0261** (the 46.2 Zipkin+B3 anchor — §Context drafts in SPEC-46.2 §13; the DECISIONS.md entry lands at the 46.2 IMPL).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `reference_phase_46_tracing_split`, `reference_tracing_upstream_cluster_framework_gap`, `reference_streaming_sink_differential_framing`, `reference_differential_grpc_receiver_driver_owned`, `reference_docker_probe_bridge_network`, `reference_concurrency_differential_release_barrier`, `reference_full_suite_race_after_background_mutator`, `reference_differential_fullsuite_startup_flake`, `reference_wire_format_both_sides_see_same_bytes`, `reference_fuzzer_count_docs_drift`, `feedback_subagent_autocommit_claudemd`, `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `feedback_subagents_no_push`).
