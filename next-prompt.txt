=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

**THERE IS BANKED MID-LIFECYCLE WORK AT THIS TIP.** Phase 80 (`stats-sds-projection`) is **OPEN** — row 80 is `in-progress` and `BRAINSTORM.md` + `SPEC.md` are landed. **This session performs the phase-80 PLAN.** Do NOT self-pick a new subject.

⚠️ **THE PREVIOUS SESSION PERFORMED NO LIFECYCLE STAGE.** It was a human-directed CI repair (*"check the most recent github action, fix anything that is not working"*), so the lifecycle pointer is UNCHANGED and still reads PLAN. Master moved `7d014546 -> f2dd994a` across two commits that are **docs-BYTE-UNTOUCHED** (`git diff --name-only 7d014546..f2dd994a -- docs/` => EMPTY, verified). **Branch off `f2dd994a`, not the SPEC's `53855de0`.** What changed is summarized at item 15 and item 17 — two of this file's own flake claims were falsified by that work and are corrected below.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=112 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **`want=112` IS A LIVE FIGURE, NOT A CONSTANT.** It is the ROADMAP data-row count. **A row-adding phase MUST bump it in the same commit that adds the row** — otherwise every later session gets `GATE FAIL: examined 113 data rows, expected 112`. That noise is deliberate: a wrong denominator must be loud, because a silent one is how check (1) failed in the first place. **The phase-80 BRAINSTORM bumped 111 -> 112 when it chartered row 80. The phase-80 SPEC left it at 112; the PLAN and IMPL each add NO row and MUST leave `want` at 112.**

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. When you open a new family, add its row-summary slug here.

**SENTINEL RE-RUN MECHANICALLY ON MASTER AT `f2dd994a` (2026-07-30), with FIRING negative controls. IT DOES NOT FIRE. `stop` was NOT created; `ls stop` => `No such file or directory`, and it MUST NOT be created.** ⚠️ **Re-run it yourself anyway and record what YOU see** — a prior run's result is not evidence for the current tip. ACTUAL output, recorded rather than predicted:
- **(1) prints `NOT DONE: row 80`** at `want=112` — correct, row 80 is `in-progress`. NCs, both fired: `want=111` => `GATE FAIL: examined 112 data rows, expected 111`; doctoring row **76** to `in-progress` on a scratch copy => `NC NOT DONE: row 76` **alongside** row 80 (the script self-reported `rows doctored: 1`).
- **(2) FIVE — `:190 :200 :210 :216 :224`** (long form at the first four, the Operational-tooling short form at the last) — **UNCHANGED**. **The twenty-sixth consecutive phase at which check (2) did not go down. STATED, not forecast.**
- **(3) `NEVER OPENED: gRPC` and `NEVER OPENED: WASM`** — unchanged. NCs: an invented slug => `NC NEVER OPENED: ZZZ-nonexistent`, while the REGISTERED slug `Observability` correctly printed nothing.
- Input measured at **228 lines / 112 data rows / 13** bare `candidates:` hits (against the sentinel's own narrower 5), so an empty result could not read as a zero result.

⚠️ **THE LEAK CHECK IS NOT LIVE THIS SESSION** — the PLAN leaves `ROADMAP.md` BYTE-UNTOUCHED. **It re-arms at the IMPL**, which authors row 80's closing cell. The rule: **NEVER WRITE A SENTINEL'S OWN MATCHER STRING INTO A FILE THE SENTINEL GREPS.** The sentinel greps `docs/envoy-go/ROADMAP.md` — writing these strings *here* is safe; writing them *there* is not. And the leak is proven to **SILENCE CHECK (3) BY MENTION**, not merely to be greppable: doctoring a cell to say `gRPC-family row` made `gRPC` stop printing while `WASM` kept printing. **`grep` cannot tell a mention from a use.**
```sh
# the row's cell only, not the whole file
awk -F'|' '/^\| *80 /' docs/envoy-go/ROADMAP.md > "$SCRATCH/row80.txt"
grep -coE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' "$SCRATCH/row80.txt"  # want 0
grep -oiE '(HTTP-filters|Network-filters|Load-balancing|Upstream-robustness|Observability|Operational-tooling|HTTP/3|gRPC|xDS|Runtime|WASM)-family row' "$SCRATCH/row80.txt" | sort -u
# ^ every slug printed MUST already be registered elsewhere in ROADMAP.md. NC both greps on a doctored copy,
#   AND re-run check (3) on the doctored file to prove the slug would actually silence it.
```

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt` — AND IT FIRED AGAIN AT THE SPEC

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files --hidden -I --exclude-dir=.git …` in ugrep-compatible mode. **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Reproduce with this stable needle — **the wrapped form lists 9 files, the GNU form lists 10, and the difference is EXACTLY `next-prompt.txt`:**
```sh
grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' .          # wrapped ugrep: next-prompt.txt ABSENT
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' .  # GNU grep:      next-prompt.txt PRESENT
grep -c  'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt   # direct access WORKS
```
⚠️ **Print the FILE NAMES, never a count.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** — it reports `next-prompt.txt` as *"not ignored"* (exit 1) because **git exempts TRACKED files from `.gitignore`** while ugrep does not. **Reliable forms: `command grep -r`, `git grep`, an explicit file list, or a recursive grep handed the FILE path directly.**
⚠️ **IT FIRED LIVE AT THE PHASE-80 SPEC, INSIDE THAT ROW'S OWN EDIT SET.** This file carries the `**TWELVE** non-test top-level segment detectors` figure that **row 80 must move to THIRTEEN**, and it was found only by `git grep`. **The IMPL's stale-count sweep MUST use `git grep`.**

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`.** Every document from phase 75 forward recorded its blind spot — **109 data rows / 105 matched / FOUR misses** at an **em-dash** in an "after" cell, a **DOT** in the slug `http-1.1`, and two **LETTER-suffixed** ids `28.1a`/`28.1b` — and every one of them called it *"currently HARMLESS, since all four missed rows are `done`."* ⚠️ **Nobody fired the unsafe arm until the phase-77 PLAN. EXECUTED on scratch copies: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** ⚠️ **DO NOT restore the regex form.**

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

The old check (2) matched ONLY the long phrase and could not see the **Operational-tooling** family's three genuinely un-chartered items, recorded in the **SHORT** form. Fixed at the phase-77 BRAINSTORM, at the MATCHER. ⚠️ **Run the union form above, never one half of it.** ⚠️ **A ONE-ARM STRIP IS NOT AN NC FOR CHECK (2)**: `sed 's/deferred candidates://'` moves the union count **5 -> 4, not 5 -> 0**, because the long form does not contain the short substring at all. **Re-confirmed at this tip: the stripped copy still counts 4.**

⚠️ **THE DOCUMENTED TRAP'S STATED MECHANISM IS REFUTED, WHILE THE TRAP IS REAL.** `reference_sentinel_deferred_sentence_live_vs_historical` says a naive `grep -c 'candidates:'` inflates *because of* the `candidates were:` recaps. It does inflate — but **not for that reason**: `candidates were:` does not contain that substring **at all**. The inflation comes from **ROADMAP table rows carrying BRAINSTORM prose**. **Cite the command; do not re-inherit the explanation.**

⚠️ **AND THE MEASURED RECORD: CHECK (2) HAS NEVER GONE DOWN** across ~26 phases (history `0 -> 1 -> 3 -> 4 -> 5`). => **the candidates sentence is a WINDOW onto a larger deferred backlog, not an inventory of it.** ⚠️ **A stage that predicts a decrease here is repeating the phase-73 error. State what your row narrows — usually nothing — rather than forecasting.**

⚠️ **THE MENU IS NOT COSTED — MEASURED.** `grep -obE '[0-9]+(-[0-9]+)? tasks'` returns **no match** on FOUR of the five paragraphs: HTTP/3 (7 uncosted candidates), xDS (9), Runtime (4) and Operational-tooling (3) carry **zero** task bands. **Only the Observability line carries costs.**

⚠️ **THE SELECTION CALCULUS, CORRECTED AND STILL STANDING.** *"A candidate that clears a check-(3) blocker is worth **strictly more**"* does **not** hold: opening a family **MINTS** a check-(2) sentence, so phase 77's own arithmetic was **NET ZERO**. What survives: **opening a family is the ONLY way that family's work becomes enumerable at all — a prerequisite, not a shortcut.**

==============================================


## STATUS AS OF 2026-07-30: PHASE 80 IS **OPEN**. ROW 80 IS `in-progress`. THIS SESSION PERFORMS THE **PHASE-80 PLAN**.

**What the phase-80 SPEC landed** (`7d014546`). Docs-only, **FIVE files**: `phases/80-stats-sds-projection/SPEC.md` (NEW) · `DECISIONS.md` (**ADR-0302 §Context**, 17658 -> 17692, **301** headings) · `STATE.md` (§Current rolled IN PLACE) · **`STATE_HISTORY.md` 422 -> 426** (the mandatory eviction) · this file. **ZERO production `.go`, ZERO test `.go`. `ROADMAP.md` and `BEHAVIOR_CONTRACT.md` BYTE-UNTOUCHED.**

**What landed SINCE, outside the lifecycle** (`f46ba419`, `f2dd994a` — a human-directed CI repair, **ZERO docs touched**, so every doc count below is still exact):
- **CI had been RED on master for a month** (last green 2026-06-29) from **two independent stacked causes, the second entirely masked by the first.** `f46ba419`: the differential job's `-timeout 5m` dated from phase 00 when the corpus was ONE fixture — at 120 it died `panic: test timed out after 5m0s` around fixture ~72 with **zero FAILs recorded**; now `-timeout 20m` + job `timeout-minutes: 30`. Same commit fixed `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery`, whose single absolute 5s read deadline spanned a 1024-round-trip loop and was therefore a **throughput assertion**; now refreshed per frame.
- **`f2dd994a`**: with the suite finally reaching the end, `0084-otlp-access-log` failed `listen tcp 0.0.0.0:36243: bind: address already in use`. `freeTCPPort` had **TWO** defects, not just the missing retry this file previously named — it probed **`127.0.0.1:0`** while every subprocess backend binds **`0.0.0.0:<port>`** (a strictly larger conflict set; demonstrated deterministically with a squatter: `127.0.0.1:46081` binds FREE while `0.0.0.0:46081` fails EADDRINUSE), and it drew from the **kernel ephemeral range**. Now strides a dedicated band **11000..14999** and probes on the **wildcard** address. `TestFreeTCPPort_BandedAndWildcardBindable` pins both properties plus a static band-disjointness invariant; **negative-controlled** — the old body fails it with 32 ephemeral ports.
- **CI is GREEN at `f2dd994a`** (run 30506628525, all 12 jobs). Verified NON-VACUOUSLY: **120 PASS / 0 FAIL** fixtures, `0084` specifically **PASS (2.99s)**, zero `no tests to run`, differential package **427.8s** in CI / **403.2s** locally.

### What matters for the NEXT session, in order

1. **⚠️ RE-RUN ALL THREE SENTINEL CHECKS YOURSELF AND RECORD THE ACTUAL OUTPUT; DO NOT PREDICT IT.** Create `stop` if and ONLY if all three print nothing. Check (1) prints `NOT DONE: row 80`, so the sentinel cannot fire. **`want` STAYS 112** — the PLAN adds no row.

2. **⚠️ THE DESIGN IS SETTLED. DO NOT RE-OPEN IT.** Read `SPEC.md` §3 (D-SDS-1, the projection arm) and §4 (D-SDS-2, the validation leg) and PLAN against them:
   - **D-SDS-1: SN1-shaped, FIRST-DOT, single-segment, non-greedy**, label `envoy_xds_resource_name`. ⚠️ **The BRAINSTORM's "fidelity vs correctness" framing is REFUTED** — first-dot is BOTH what the live reference does (three discriminating arms + the binary's own tag-extractor literal) AND the file's own convention (all four dynamic-token arms use first-dot; last-dot needs a `strings.LastIndex` found NOWHERE in `ExtractTags`). **There is no tension to adjudicate.**
   - **D-SDS-2: a boot-boundary REJECT in `boot.NewSDSProvider`**, not a log, not a sanitizer. ⚠️ **NOT in `ParseSDSConfig` — the SPEC's own first draft had that wrong**: FOUR non-test call sites, three in `internal/tls/config.go` wrapping into a DIFFERENT error chain, none of which registers a stat.

3. **⚠️ THE THINGS THE SPEC PROVED THAT THE PLAN MUST NOT RE-ARGUE.** All executed: the reference **ACCEPTS** a hyphenated secret and serves `envoy_sds_update_success{envoy_xds_resource_name="server-cert"} 1` with the hyphen **verbatim in the label value** (`od`-verified) => the silent skip is a **genuine conformance divergence** and the reject is **itself a documented departure** · a two-secret config is a **hard boot reject** (exit 1, 0 B stdout), `seen` counted across ALL listeners => **5 per process, and a two-secret fixture is NOT constructible** · `incNil` is complete (**95**-byte audit, `partial == 0`, **31** rejected bytes) · the skip log **fires on the healthy arm and is silent on the broken one** · a valid and a silently-skipped registration produce **byte-identical `/stats/prometheus` (same sha256)** today.

4. **⚠️ THE EXISTING GATES ARE NEARLY BLIND — THIS IS THE PLAN'S MAIN JOB.** Under an experimental arm the whole of `./internal/stats/... ./internal/statssink/...` fails **EXACTLY ONE** test. **ALL FOUR** phase-79 OTLP byte goldens are **unmoved**, because `goldenRoster`'s 13 entries carry **no `sds.*` name** — so the known-inert `(F,F)` cell **UNDERSTATES** it: every cell is vacuous until the roster is extended. The `# HELP` lines ship **SELF-EQUAL** and `TestHelpText_NoSelfEqualHelp` does not catch it (it drives only the roster). **No existing golden moves — sds coverage must be AUTHORED, not amended.** ⚠️ An agent told *"four goldens will move"* will look for four edits, find none, and mis-conclude the claim is wrong. (`reference_golden_roster_omits_family_under_test` — broken-gate shape SEVENTEEN.)

5. **⚠️ THE STALE-COUNT SWEEP NEEDLE IS ITSELF A FINDING — TWO AGENTS DISAGREED AND IT WAS ADJUDICATED BY EXECUTION.** The **numeral** `one of the 12` returns 4 hits of which **2 are FALSE POSITIVES** (phase-09 docs about *"the 12 existing fuzzers"*); a bare spelled-out `\btwelve\b` returns **45 files** of noise; **scoping the spelled needle to a detector context** yields **26 lines / 20 files**, of which the **live-editable set is ~16 lines across 8 files** — including **SIX in `0118-runtime-static-layer`** (an agent reported that fixture "de-fanged" on the strength of two lines that refuse to quote the constant). **Sweep BOTH forms; scope the spelled one; use `git grep`.** ⚠️ **And the BRAINSTORM names the WRONG FILE for the count: `segmentcount_test.go` hard-codes NOTHING** — it AST-derives one side and parses the other out of the live constant. The `12` lives in **`name.go`** (production) and the hand-written twin in **`name_test.go`**.

6. **⚠️ COST: THE SPEC BAND IS 12-15, BUDGET 13 — and calibration says read that as *expect 14*.** Three of four recent rows landed AT or ABOVE the SPEC ceiling and none below the floor (76: ~7-9 -> 9 · 77: 11-13 -> 12 · 78: 7-9 -> 10 · 79: 10-12/12 -> 12; all four re-derived from the SPECs' own quoted headings, PLAN counts mechanical via `^## Task [0-9]`). ⚠️ **THE §6.1 LoC TRIGGER IS LIVE: ~855 estimated LoC x 1.75 ~= 1496, x 2.04 ~= 1744.** Realized ratios reproduce to the byte (77: **2.040x** · 78: **1.500x** · 79: **1.751x**); phase 76 has no LoC estimate so no ratio exists. **DO NOT write "much less than ~1500"** — the phase-79 SPEC did and its own PLAN refuted it. **Split at the T4/T10 seam if the bottom-up lands >= ~800 LoC.** ⚠️ **The normative cite is `BOOTSTRAP_PROMPT.md` §6.1 `:287-292`, NOT `:287-291`** — `:291` is BLANK and the third (mid-execution, >~10 sub-steps) trigger lives at **`:292`**, outside the range both the BRAINSTORM and the phase-79 PLAN cite while relying on it. Prefer the heading anchor `### 6.1 When to split`. Six-gate cites verified EXACT: **§7.5 heading `:357`, gates (a)-(f) `:360-365`, close `:367`**. `ADR-0045` QUOTES rather than states; `ADR-0106` defines nothing about phase-done gates.

7. **⚠️ THE BRAINSTORM OMITS A BREAK-ROSTER TASK THAT ALL FOUR CALIBRATION PHASES SHIPPED (4/4).** 76 Task 8 · 77 Task 8 · 78 Task 6 (eight arms) · 79 Task 10. **The PLAN must carry one.** ⚠️ And this row's breaks are unusually hazardous *because* of item 4 — the tree is nearly blind to the arm, so a break arm that "fires" may be firing an unrelated assertion. **Each arm must name WHICH assertion fired** (`reference_deliberate_break_wrong_assertion`, `reference_break_arm_injection_site_is_a_claim`, `reference_vacuous_break_modes`).

8. **⚠️ THE FIXTURE HOME IS `0110`, NOT `0103` — the SPEC's own first draft had that wrong too.** `0103`/`0108`/`0109` have **neither** a `StatsAsserter` **nor** a `/stats/prometheus` scraper; `0110` and `0111` have **both**, already cross-side green. **Extending `0110` costs ZERO registration gates.** ⚠️ **But it is NOT free reuse: `0110`'s `scrapeProm` keys by metric name and DISCARDS the label set**, so it cannot assert `envoy_xds_resource_name=…` without a label-aware rework. ⚠️ **The assertion is a NAME+LABEL-SHAPE SUBSET — never set-equality (the reference's roster is 10 flat / 9 prom vs envoy-go's 5), never value-parity (`update_attempt` is 1 subject vs 2 reference — envoy-go is INITIAL-FETCH-ONLY).** Only `init_fetch_timeout` is safely `0 == 0` cross-side, and a `0 == 0` pin is vacuous. ⚠️ **AND THE FIXTURE CANNOT DISCRIMINATE THE FIRST-DOT FORK — all four corpus secrets are DOT-FREE.** A dedicated unit test on a dotted name is required or the row's central decision ships unguarded.

9. **⚠️ A NAME-FORMATION EDGE THE PLAN MUST DECIDE.** The reference maps `://`/`:/`/`:` -> `_` and strips **ALL** trailing dots **before** tag extraction (NC: dropping the trailing-dot strip fails **16/16** arms). envoy-go builds the name naively, so `trailing_dot.` yields `sds.trailing_dot..update_success` — which **`NamePattern` ACCEPTS**, because it is a **charset guard, not a well-formedness guard** (an interior empty segment passes). => **guard on the SEGMENTS, not only the assembled name** (`reference_dynamic_stat_name_charset_guard`). Mirroring the reference's sanitizer is **foreclosed by ADR-0065 §Context**.

10. **⚠️ TWO DESIGNS STAY EXCLUDED, both DEADLOCKING** (`reference_registry_walk_lock_inversion`): a counter registered from **inside** the projection path, and **lazy registration at first increment**. **The chosen reject sidesteps both by construction** and **moots the ADR-0300 §Consequences (iii) panic-visibility question entirely** (it returns an error into the existing `log.Fatalf` and never panics). ⚠️ A skip **counter** is rejected on a second, independent ground: it perturbs every SDS fixture roster and sink golden if unconditional, is an unstable roster entry if conditional, and being `sds.`-rooted would need this row's own arm to project — a **circularity**.

11. **⚠️ ADR + DOCUMENT CONVENTIONS.** Next-free is **ADR-0303**; tail is **ADR-0302**, STATUS **PROPOSED** (§Context only). **`grep -c '^## ADR-0303'` => 0** (NC: `^## ADR-0302` => 1) — **both re-verified at `f2dd994a`.** **301** `^## ADR-` headings, ids 0001-0302, **exactly one gap at ADR-0209**, zero duplicates — *"301 ADRs"* and *"tail ADR-0302"* are both true and must not be conflated. **The IMPL appends §Decision + §Consequences IN PLACE (ADR-0044-as-used), no renumber, no `---` separator, AND MUST FLIP ADR-0302's STATUS `PROPOSED` -> `COMPLETE`.** ⚠️ **The retained italic footer IS present** as the last line of §Context — `*(§Decision + §Consequences land at the phase-80 IMPL.)*` — byte-verified against ADR-0300; **RETAIN it after the append.** ⚠️ **ADR-0301's STATUS says *"seven blocks"* then names the SIX-block range *"ADR-0295 through ADR-0300"*; the seven are ADR-0294 … ADR-0300. Copy the FORM, not the miscount.** ⚠️ **CARRY NO WHOLE-FILE GREP COUNT** in an ADR — that species self-falsified in ADR-0296 ¶3 and twice in ADR-0297.

12. **⚠️ THE `79.1` ROSTER IS 50 OCCURRENCES / 9 FILES AT THE SPEC TIP — the BRAINSTORM's "36 / 8" was EXACT at its branch point and was falsified by its own commit** (`reference_branchpoint_roster_stale_midrow`). **Re-derive at YOUR tip; do not inherit either figure.** Classification: **live-normative, MANDATORY IMPL edits — 4 occurrences on 3 lines, all in `BEHAVIOR_CONTRACT.md`** (anchor on the PHRASES *"Until 79.1 lands, treat `sds.*` as flat-`/stats`-only"*, *"DEFERS the `sds.` twenty to row 79.1"*, *"79.1 takes it to 0 lines"* — **never on the line numbers**) · **`ROADMAP.md` row 79 x3 INVARIANT-BLOCKED** by §Schema (`status` and `sub-phases` are the ONLY editable columns) and NAMED as a permanent discrepancy · **phase-79 phase documents x25 are HISTORICAL and must NOT be rewritten** · **~11 in this row's OWN live documents, a class the BRAINSTORM's trichotomy omits entirely.** ⚠️ **`BEHAVIOR_CONTRACT.md` pre-79 cites are stale by `+54` only up to old line ~5023; a second hunk adds six more, so cites at or beyond old `:5024` are stale by `+60`.** A flat `+54` lands six lines short in the tail. **Prefer symbol anchors to any offset.**

13. **⚠️ THE FULL 120-FIXTURE DIFFERENTIAL IS MANDATORY for any row touching `internal/stats`** — it links into `cmd/envoy-go` at `test/differential/harness.go:240`, `:594` and `test/conformance/h2spec/h2spec_test.go:210`; **h2spec is a FOURTH consumer NOT covered by `./test/differential/`**. Budget **~400-430 s** per green attempt (measured `f2dd994a`: **403.2 s** locally, **427.8 s** on a CI runner); `-race` is a SECOND run. **The PLAN specifies it; the IMPL runs it.** ⚠️ **CI now runs this job at `-timeout 20m` with job `timeout-minutes: 30`** — if your local run approaches either, that is a finding, not a budget to raise reflexively. The recipe, with `comm -3` as the load-bearing gate (120 with one fixture renamed and another skipped still reads 120):
    ```sh
    ( go test ./test/differential/ -count=1 -v > "$SCRATCH/full.log" 2>&1; echo "INNER_EXIT=$?" )
    grep -cE '^    --- PASS: TestDifferential/' "$SCRATCH/full.log"          # want 120
    grep -E  '^    --- (FAIL|SKIP): TestDifferential/' "$SCRATCH/full.log"   # want EMPTY
    grep -c  'no driver registered for fixture' "$SCRATCH/full.log"          # want 0
    grep -o  'TestDifferential/[^ ]*' "$SCRATCH/full.log" | sed 's|TestDifferential/||' | sort -u \
      | comm -3 - <(ls -1 test/fixtures/ | grep -E '^[0-9]{4}[a-z]?-' | sort)  # want EMPTY
    ```
    ⚠️ The faithful dir predicate is `^[0-9]{4}[a-z]?-` — a bare `^[0-9]{4}-` gives **118**.
    ⚠️ **`go test ./test/differential/...` (with `...`) matches TWO packages, so `go test` BUFFERS each package's `-v` output until that package finishes.** A run that looks hung for 7 minutes with an empty log is normal, not a stall — do not kill it and do not conclude "no output" means "no progress".

14. **BROKEN-GATE COUNT IS SEVENTEEN.** Shape **seventeen**: **a GOLDEN ROSTER that omits the family under test, which makes EVERY cell of an otherwise-correct cross-product vacuous** — distinct from shape fifteen (one inert cell), because here the command, the assertion AND the cross-product are all right and the *input roster* is the defect. The other sixteen are unchanged: two defects that CANCEL in the gate metric (`NF==8` passes the malformed row 78) · an inert gate cell · a full-suite recipe without `-v` is VACUOUS · a sha256 roster desynced against a DELETED file · `gofmt -l` NEVER exits non-zero (gate on OUTPUT) · `go doc -all <A> <B>` swallows arg2 · a `+0 exported symbols` gate over an EMPTY package reds on a CORRECT tree · a RANGE gate cannot detect anchor drift · a roster's naive `[ -f ] || continue` exits 0 on a DELETED file · a count-only stat guard PASSES a build with BOTH names wrong · a `-run` no-match exits 0 with `[no tests to run]` · a `--- PASS` tally over a package with sibling tests exceeds the fixture denominator · a stat-delta claim cannot be discharged by guards scoped to another package · a stderr-VOLUME assertion passes on the hang · `golangci-lint` runs `misspell` with `locale: US` · **a harness's exit code is not the command's** — ⚠️ **that last one FIRED AGAIN in the CI-repair session**: `golangci-lint … | tail` reported `LINT_EXIT=0`, which was **`tail`'s** status. Capture the tool's OWN exit code and its output BYTE COUNT.

15. **⚠️ A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX — the EIGHTEENTH consecutive stage at which this held.** The index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. ⚠️ **TWO ENTRIES ON THIS LIST WERE FIXED AT `f46ba419`/`f2dd994a` AND ARE NO LONGER FLAKES — a recurrence of either is a FINDING:**
    - **`internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery` — FIXED**, was a whole-loop read deadline acting as a throughput assertion.
    - **The full-suite startup flake's BACKEND half (`bind: address already in use`) — FIXED** by the `11000..14999` band + wildcard probe. ⚠️ **A backend bind failure IN-BAND is now a real finding, not a known flake to re-run away.** The **SUBJECT** half (`subject ready: EOF`) was already hardened at `0e9cc680` and likewise warrants suspicion, not acceptance.
    Still live: **`reference_sds_init_fetch_timeout_dial_budget_flake` (TWO packages — LIVE for this row's subject)** · the pre-existing `internal/cluster` `-race` outlier · `internal/httpclient TestOptions_ZeroValue_NoOpDefaults`. **Isolate-re-run, then state the classification AND its evidence.** ⚠️ **`0061-lb-ring-hash` is NOT a live flake — a spread failure there is a FINDING.** **No flake fired at the SPEC**, and **none fired in the 120/120 run at `f2dd994a`.**

16. **Counts at this tip (re-run MECHANICALLY in your worktree; never copy them).** ⚠️ Use GNU `command grep` or `git grep` for anything repo-wide. **All doc counts below were re-verified at `f2dd994a` and are unchanged from the SPEC close, because the CI commits touched ZERO docs:**
    fixtures **120** (next-free **0119**; faithful predicate `^[0-9]{4}[a-z]?-`, a bare `^[0-9]{4}-` gives **118**) / fuzzers **55** (⚠️ the `internal/`-vs-repo-wide pair is NOT discriminating — both 55) / internal packages **73** / blank imports in `runner_test.go` **120**, ⚠️ **anchor on the FULL `^\t_ "github.com/pgdad/envoy-go/test/fixtures/` prefix — a naive `^\t_ ` gives 126** / **stat surface 1207**, ⚠️ **DOCUMENTARY and CONFIG-CONDITIONAL — assert the DELTA** / **BackendKind tail 38** (⚠️ a TAIL VALUE — **39** declarations, values 0-38, **no `iota`**; do NOT "fix" 38 to 39) / go.mod modules **2** (the phase-61.2 lineage figure — the single `go.mod` requires **67**; do NOT "fix" 2 to 67) / `DECISIONS.md` **17692**, **301** headings, tail **ADR-0302 PROPOSED**, next-free **ADR-0303** / `ROADMAP.md` **228 lines / 112 data rows** / `BEHAVIOR_CONTRACT.md` **5822** / `STATE_HISTORY.md` **426** / next-free reference port **10119** / **TWELVE** non-test top-level segment detectors in `ExtractTags` plus **FOUR** mid-name detectors, **code and error string AGREE** — ⚠️ **row 80 moves this to THIRTEEN and the error string must move IN THE SAME TASK** / `helpText` **25** keys and `helpTextRoster` **25** entries / `goldenRoster` **13** entries with **ZERO** `sds.*` names, `goldenTaggedIdx = {0,1}` / `goldenOTLPCells` pinned at **1134 / 1200 / 1118 / 1184**.

17. **⚠️ DOCUMENTARY DEFECTS — carried forward, still deliberately NOT fixed.**
    - **`ADR-0299`'s STATUS still reads `PROPOSED`** although its §Decision and §Consequences landed at the phase-77 IMPL. **The single word is the entire defect. 1 task, DEFENSIBLE AS A RIDER on this row's IMPL**, which already edits `DECISIONS.md` — with a guard of shape *"a `PROPOSED` STATUS must have no §Decision."* ⚠️ **ADR-0302's own `PROPOSED` is CORRECT at this stage and must not be confused with it** — but the same IMPL must flip ADR-0302 too, so the guard has to tolerate the in-flight case or run after both flips.
    - **The `STATE_HISTORY.md` archive gap: 36 bullets for phases 67-75 (9 phases x 4 stages) and 58 overall**, against a stated denominator of 164 present / 205 in git. ⚠️ **`phase 77 PLAN done` is in NEITHER file** — CONFIRMED with a firing NC (the IMPL/SPEC/BRAINSTORM siblings all match 1 under the identical matcher, PLAN matches 0) — so the ADR-0288 cap already leaks at the most recent phase but one. **4-6 tasks, defensible ONLY if it lands the `comm -13` set-difference as a recurrence guard.** The SPEC honored the rule for its own eviction (**422 -> 426**).
    - **`ROADMAP.md` malformation: rows 57, 69 and 78 all lose summary content in GFM render** (EXECUTED with two renderers against well-formed controls; 689 / 4032 / 2877 source chars lost, controls 74 and 76 INTACT). Row 78 additionally drops its trailing `|`. **All INVARIANT-BLOCKED by §Schema `:18`** => **defensible as a GUARD, not a fix**, and the right gate is a **DISJUNCTION** (escape-aware `NF!=8` finds 57/69; escape-aware trailing-piece finds 78; `NF==8` PASSES row 78).
    - **The PUBLIC IMPORT PATH defect — NOT DEFENSIBLE AS A PHASE: there is no green.** `DECISIONS.md:142` is an immutable ADR that must *name* the wrong path; 7 hits sit inside CLOSED rows' summary cells that §Schema forbids editing; the root `PROGRESS.md` hits are pasted `go test` output; any `count==0` guard is unsatisfiable by construction. `go build ./...` exits 0 under the real path; **0** occurrences in compiled Go.
    - **A mechanical stat-surface recount: 8-11 tasks, defensible only if reframed as an EXECUTABLE enumerator** (dump `Registry` keys after a boot); a grep-based recount covers ~0.8 % of the surface and stays rejected.
    - **`STATE.md` §Project counts self-contradicts §Current** inside the same file (`:31` 119 vs **120**, `:33` 1205 vs **1207**, `:35` ADR-0298 vs **ADR-0302**). **Anchor on §Current, which IS live.**
    - ⚠️ **SYMMETRIC BIND HARDENING — PARTIALLY LANDED AT `f2dd994a`; RE-COST BEFORE QUOTING THE OLD BAND.** The **allocator** half is DONE (band + wildcard probe + a pinning test with a firing NC). ⚠️ **The old *"8-12 tasks"* figure and the *"close-then-rebind"* characterization are now STALE — do not inherit either.** What REMAINS is the **retry** gap: a window persists between the probe's `Close` and the child's bind, and the child is `go run`, so the gap spans a BUILD. **`26` of the `29` `freeTCPPort(t)` call sites in `runner_test.go` are backend-startup arms with NO retry loop** (re-derived at `f2dd994a`; `harness_test.go` holds `4` more). The fix is a `startSubjectWithRetry` equivalent for those arms. ⚠️ **Still NOT verifiable by a single green suite run** — a prior instance needed `-count=6`.
    - **Opening the gRPC family — HARD-BLOCKED, evidence-backed:** `\.RunEncodeTrailers(` => **0** non-test / 1 test; `\.RunDecodeTrailers(` => **0** non-test / 1 test; both declared, neither production-reachable. NC: `\.RunEncodeHeaders(` non-test => **4**. The **16-22+** band is **NOT re-derivable** — flagged, not inherited.
    - **The WASM row-summary rider stays DECLINED ON THE MERITS** — `ROADMAP.md:76` (row 25.3) says verbatim *"phase 25 is the FINAL §9 HTTP-filters-family row"*, **and writing the marker would silence check (3) BY MENTION** (proven by NC).
    - **Normalizing the Operational-tooling short-form deferred-candidate paragraph** to the long form.
    - `BEHAVIOR_CONTRACT.md:501` calls the `wasm.` arm a *"NEW SN9 flattening rule"*, which **COLLIDES with ADR-0118's actual SN9**.
    - ⚠️ **`ROADMAP.md` §Schema `:18` says *"Sub-phases get their own rows"* while BRAINSTORM §1.1 claim 3 argues the legs live in the parent's `sub-phases` PROSE.** That is **practice diverging from written doctrine.** The row-id adjudication (80, not 79.1) survives on claims 1 and 2, which are independent and both hold. **Record the tension; do NOT relitigate the id.**
    - **`golangci-lint-action@v6.5.2` emits a Node-20 deprecation warning in CI.** Deliberately NOT bumped: v7+ requires golangci-lint **v2** while the repo pins **v1.64.8**, so a bump forces a config migration. **It is a warning, not a failure** — do not "fix" it as part of an unrelated row.

18. **⚠️ THINGS THE SPEC NAMED BUT DID NOT FIX — do not silently absorb them into this row.** Full hyphen fidelity (needs `NamePattern` relaxation or a raw-bytes seam — repo-wide) · the **general** dynamic-token charset exposure across six families · the reference emitting **ZERO `# HELP` lines** where envoy-go emits them for every family (a pre-existing format departure, newly measured) · **`--mode validate` cannot validate ANY SDS bootstrap** (`validate.Bootstrap` passes a nil provider, so `boot.NewSDSProvider` is never reached — this **forecloses `--mode validate` as a home for the new check**) · the `listener.`/`stat_prefix` sanitization inconsistency (ADR-0065 §Context rejected sanitizing a hoisted label value, yet `normalizeAddr` sanitizes the `listener.` address, which SN3 hoists).

## What THIS session does — the phase-80 PLAN

One stage per session. Read `SPEC.md` in full, then write `PLAN.md` **and** `PROGRESS.md` (the PLAN half) in `docs/envoy-go/phases/80-stats-sds-projection/`. Roll `STATE.md` §Current **IN PLACE** (ADR-0288 — ⚠️ **§Recent lineage is AT its five-entry cap; a close that adds a bullet MUST evict the oldest to `STATE_HISTORY.md`, and the oldest is now `phase 79 (stats-prometheus-projection) BRAINSTORM done` — verify it is ABSENT from the archive BEFORE moving it, and NC the probe against a sibling that IS present**) and this file. **`ROADMAP.md`, `BEHAVIOR_CONTRACT.md` and `DECISIONS.md` stay BYTE-UNTOUCHED and `want` stays 112.** Expected file set: **`PLAN.md` (NEW) + `PROGRESS.md` (NEW) + `STATE.md` + `STATE_HISTORY.md` + this file** — five files, docs-only, ZERO `.go`.

⚠️ **A PLAN's job is to REFUTE the SPEC by EXECUTION, not to formalize it.** The lineage record is that **every stage finds a load-bearing error in its predecessor, and every one is found by execution rather than review** — phase 79's PLAN refuted eleven SPEC claims; this SPEC refuted the BRAINSTORM's design-fork FRAMING, its rule COMPLETENESS, its reject SITE, its fixture HOME, its cost band, its golden-blast-radius claim and its own `79.1` roster, **and caught two of its own probes plus two of its agents'**. **Budget for finding one more.** Start with the three most likely to be wrong: the **12-15/13 band and the ~855 LoC estimate** (both are the SPEC's own arithmetic, unvalidated against a task-by-task build-up), the **`0110` scrapeProm rework cost** (asserted from source reading, never executed against the reference container), and the claim that **the boot-boundary reject is a no-op on the differential corpus** (derived from `IsValidName` over four secret names, NOT from a suite run).

Fresh worktree off master **`f2dd994a`** per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** (`reference_parallel_subagents_private_scratch`) and they **must tear down docker containers BY NAME, never by an `ancestor`/image filter** (`reference_parallel_agents_shared_machine_namespaces`). ⚠️ **THERE ARE NOW **TWO** RESERVED PORT BANDS, NOT ONE — band private ports outside BOTH `20000-31007` (subject blocks) AND `11000-14999` (backends, new at `f2dd994a`), and also clear of the static fixture ports `10000-10447` / `15000-15011` / `18001-18007`.** The SPEC's agents used `42000-42299`, which remains safe. ⚠️ **If two agents share ONE worktree they must stage by EXPLICIT PATH.** ⚠️ **AND THEY MUST NOT RUN AN UNSCOPED `git restore`.** Per-task `gofmt` + `golangci-lint` on touched packages (`feedback_pertask_gofmt_lint`) — **not owed by a docs-only PLAN.** ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch.**

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. ⚠️ **ASSUME IT FIRES EVERY SESSION — it has been treated as live for TWENTY-TWO consecutive sessions and it FIRED REPEATEDLY at both the phase-80 SPEC and the CI-repair session.** **Use `git -C <abs-worktree-path>` for every git command, prefer absolute paths over `cd`, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — Current-pointer section (TRUSTWORTHY; ADR-0288 discipline — ⚠️ **anchor greps on `^- **<field>`**; all seven fields verified singleton at the phase-80 SPEC close). ⚠️ **§Project counts SELF-CONTRADICTS §Current** — do not anchor on it.
2. `docs/envoy-go/phases/80-stats-sds-projection/SPEC.md` — **in full.** §2 carries the nine refutations, §3/§4 the two design decisions, §5 the blast radius, §6 the guards and the sweep-needle adjudication, §7 the fixture posture, §8 the contract/ADR edits, §9 the cost and the LoC trigger, §10 what is chartered but not taken, §12 the probes that were themselves unreliable.
3. `docs/envoy-go/phases/80-stats-sds-projection/BRAINSTORM.md` — §1.1 the row-id adjudication, §5 the silent skip, §8 the costed alternatives. ⚠️ **Its §6 design fork and §9 scope wording are SUPERSEDED by the SPEC** — read them as history.
4. `docs/envoy-go/phases/79-stats-prometheus-projection/PROGRESS.md` — **both halves.** The `# IMPL record` carries the method finding this row inherits (a hand-written golden shares the author's mistake; a branch-point roster goes stale mid-row).
5. `docs/envoy-go/DECISIONS.md` — **ADR-0302** (this row; §Context only, footer retained) · **ADR-0065** (the boundary-validation MANDATE and the sanitization rejection — the load-bearing precedent for D-SDS-2) · **ADR-0301** (the projection row; the model for the footer FORM, not its miscount) · **ADR-0300** (§Consequences (ii) the injected-defer insertion-point rule, (iii) the OVER-ATTRIBUTED claim) · **ADR-0288** (roll-IN-PLACE and the archive rule) · **ADR-0059** (the panic discipline ADR-0065 carves out of).
6. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the three live `79.1` statements (by PHRASE, not line) · the five-counter named-subset paragraph (which the IMPL must rewrite) · the phase-77 departure narrative.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — especially **`reference_probe_must_discriminate`**, **`reference_probe_input_is_a_claim`**, **`reference_a_drift_correction_is_itself_a_claim`**, **`reference_handwritten_golden_shares_author_mistake`**, **`reference_golden_roster_omits_family_under_test`**, **`reference_branchpoint_roster_stale_midrow`**, **`reference_registry_walk_lock_inversion`**, **`reference_nil_stats_counter_inc_crashes_goroutine`**, **`reference_dynamic_stat_name_charset_guard`**, **`reference_sds_init_fetch_timeout_dial_budget_flake`**, **`reference_differential_fullsuite_startup_flake`** (⚠️ **UPDATED at `f2dd994a` — two reserved bands, and an in-band recurrence is a FINDING**), **`reference_stale_cite_recurs_fix_by_pattern`**, **`reference_gate_command_negative_control`**, **`reference_harness_exit_code_is_not_command_exit_code`**, **`reference_empty_output_is_not_a_zero_result`**, **`reference_output_volume_is_not_output_content`**, **`reference_recursive_grep_blind_to_gitignored_tracked_file`**, **`reference_deliberate_break_wrong_assertion`**, **`reference_vacuous_break_modes`**, `feedback_brief_citations_not_evidence`, `feedback_git_worktrees`, `feedback_execution_style`, and **`reference_bash_cwd_reset_commits_to_main`**.
