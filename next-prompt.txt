=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: does NOT apply. Phase 53 (`load-balancer-priority`) is actively `in-progress` (lifecycle-state 0 → 1, BRAINSTORM done) — there is a clear next action (its SPEC). Do not check the cross-family audit again until phase 53 itself reaches phase-done with no further chartered work.

**STATUS AS OF 2026-07-07: a human picked the Load-balancing family (from the three open families) and, within it, priority load balancing (over the family's other remaining candidate, panic thresholds). Phase 53 (`load-balancer-priority`) is now CHARTERED and its BRAINSTORM is LANDED (row 53 registers `in-progress`).**

- The phase-53 BRAINSTORM landed (docs-only, worktree `.worktrees/phase-53-brainstorm`, branch `phase-53-load-balancer-priority-brainstorm`): `docs/envoy-go/phases/53-load-balancer-priority/BRAINSTORM.md`, the phase-52 12-section shape. Phase 53 lands `LocalityLbEndpoints.priority`-driven priority-tier overflow/failover — a new `priorityLB` wrapper (`internal/cluster/priority.go`) that groups a cluster's endpoints by a THIRD `Endpoint` dimension (`Priority uint32`, after phase-38's `Metadata` and phase-52's `Locality`/`LocalityWeight`), builds a per-tier LEAF child via the EXISTING `buildLeafLB` factory (ALL five policies, unanimous precedent), and at `Pick` weighted-random-draws a tier by an effective-load WATERFALL cascade reusing phase 52's `min(1, overprovisioning × healthy_fraction)` availability primitive per tier — **ZERO new `Pick` parameters** (the family's SECOND cheapest-wrapper phase). **User dialogue this session:** composition with `locality_weighted_lb_config` (and anticipated `lb_subset_config`) kept INDEPENDENT — an explicit reject when both are configured on one cluster, mirroring phase 52's own subset-composition-deferral posture, rather than assuming the reference's natural priority-wraps-locality nesting (a future row if ever warranted); differential proof scope is a hard **static + full-failover 2-arm** assertion (P0-healthy → 100%/0%; ALL of P0 down → 100% to P1), NOT a third banded partial-spillover arm — trading formula-completeness-as-fixture-proof for a flake-risk-free boundary assertion (the fractional waterfall is still verified analytically at SPEC via a live-probe empirical pin, D-P4). **The phase's single load-bearing empirical unknown (SPEC-BLOCKING, D-P1):** whether the panic/per-tier-health interaction mirrors phase 52's "reuse the cluster-wide `inPanic` gate one layer higher" shape, or is computed PER-TIER instead — deliberately NOT assumed at this BRAINSTORM. 1 ADR anticipated (ADR-0270, single-ADR reuse shape). Fixture `0096-lb-priority` anticipated (fixtures 97 → 98); NO new BackendKind (tail STAYS 38); NO new fuzzer (fuzzers STAY 52); stat surface anticipated 1200 → ~1200-1202 (D-P5 pins the exact set). ZERO new packages, ZERO new go.mod deps. Counts re-verified fresh this session (not trusted from any prior file): stat surface 1200 / fixtures 97 / fuzzers 52 (tail `FuzzDogStatsdSinkConfigParse`) / BackendKind tail 38 (39 total) / DECISIONS tail ADR-0269 (next-free ADR-0270).
- STATE.md/ROADMAP.md updated (row 53 `in-progress`; the new active-phase entry). This is a docs-only commit (no production code touched).

=> The NEXT action is **the phase-53 SPEC** (`superpowers:writing-plans` scoped to SPEC authoring — the ring_hash-36/maglev-37/subset-38/locality-weighted-52 precedent). Execute the BRAINSTORM's §10 D-P1..D-P6 empirical pins IN-SESSION against the contrib reference Envoy (`envoyproxy/envoy:contrib-v1.37.2`, ADR-0227) via the live-probe precedent (`reference_docker_probe_bridge_network`), anchor the ADR-0270 §Context draft, and confirm the single-flat-row envelope (§1.4 of the BRAINSTORM). **D-P1 (the panic/per-tier-health interaction) is the single most load-bearing pin** — do not assume it mirrors phase 52's shape; live-probe it per §2.5/§10 of the BRAINSTORM.

==============================================

## Read first (in order)
1. THIS FILE — the cold-start router (phase 53 chartered, BRAINSTORM done, SPEC is next).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 53 BRAINSTORM done`) + the full history.
3. `docs/envoy-go/ROADMAP.md` — confirm row 53 `in-progress`; the Load-balancing family section for full context (phases 34 through 53).
4. `docs/envoy-go/phases/53-load-balancer-priority/BRAINSTORM.md` — the phase to advance; ALL §10 D-P1..D-P6 empirical pins are SPEC-blocking or SPEC-pinned.
5. `docs/envoy-go/phases/52-load-balancer-locality-weighted/{BRAINSTORM,SPEC}.md` — the DIRECT structural precedent (the `localityWeightedLB` shape phase 53's `priorityLB` reapplies one axis over); the SPEC shows the expected live-probe execution shape (parallel-subagent fan-out, D-LW1..D-LW8 resolved with AMEND-LW1..LW5 corrections).
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — search for `reference_*`/`feedback_*` entries tagged for load-balancer/health/differential work (e.g. `reference_health_check_propagation_warmup`, `reference_percent_cap_cross_multiply`, `reference_differential_band_sigma_margin`, `reference_docker_probe_bridge_network`).

## Honor throughout (the discipline, unchanged for whichever stage comes next)
- **Lifecycle = one stage per session** (BRAINSTORM → SPEC → PLAN → IMPL). This session's job is ONLY the SPEC — do not proceed to PLAN/IMPL in the same session.
- **Subagent-driven execution** (`feedback_execution_style`) in a **fresh worktree** (`feedback_git_worktrees`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit, re-runs the full suite on the frozen HEAD, does any deliberate-break verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). Per-task gates: `gofmt -l` + `golangci-lint` + `go vet` + `go build ./...` (`feedback_pertask_gofmt_lint`).
- **Process anchors:** ADR-0106 (per-leg rows) · ADR-0044 (ADR §Decision+§Consequences land at IMPL) · ADR-0045 (sub-split soft gate) · next-free **ADR-0270**.
- Re-verify baselines fresh at cold-start — do NOT trust any count in this file without re-running the baseline commands (`ls -d test/fixtures/[0-9][0-9][0-9][0-9]* | wc -l`, `grep -rh '^func Fuzz' --include='*.go' . | wc -l`, etc.) against current HEAD.
- After the phase-53 SPEC lands, roll this file forward again to point at the phase-53 PLAN. The Load-balancing family's remaining deferred candidate (panic thresholds as an independent construct) and the other two open families' candidate lists (Observability — 5 candidates; Operational tooling — 3 candidates) stay parked, unaffected by this phase, until phase 53 itself reaches phase-done and a human is asked again.
