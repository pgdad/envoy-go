=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **PHASE 89 (`h2-decode-filter-mutations`) IS CLOSED. ROW 89 IS `done` AT `ROADMAP.md:151`. EVERY ROW ON THE BOARD IS `done` AND `want` IS 121.** ⇒ **THERE IS NO BANKED MID-LIFECYCLE WORK. THIS SESSION SELF-PICKS PHASE 90 AND PERFORMS ITS BRAINSTORM.** ⚠️ **THE SENTINEL STILL DOES NOT FIRE — check (1) is now SILENT, check (2) prints SIX, check (3) is SILENT. The conjunction FAILS on check (2). DO NOT create `stop`.** ⚠️ **MEASURE ALL THREE YOURSELF BEFORE DOING ANYTHING ELSE; do not take this file's word for it.**

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=121 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **CHECK (1) IS NOW SILENT IN THE HEALTHY CASE, SO IT HAS NO OWN LIVENESS. THE ROW-62 NC IS THE ONLY THING DISTINGUISHING A GREEN BOARD FROM A BROKEN CHECK.** Run BOTH doctoring NCs EVERY SESSION:
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md. Expect "NOT DONE: row 62" ALONE.
```
```sh
# the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```
Also NC the DENOMINATOR (`want=120` on the real file must print `GATE FAIL: examined 121 data rows, expected 120`).

⚠️ **AND WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -oiE '-family row'` fails with `grep: amily row: No such file or directory` and the surrounding arithmetic prints `base=0 now=0 delta=0`, **which reads exactly like "no change".** **Always pass `--` before the pattern**, and remember the counts are form-dependent: **95 occurrences vs 67 LINES** at this tip (`grep -c` counts LINES). **A gate that reads zero on BOTH sides is not evidence of invariance.** ⚠️ **THE ARM-A `{119, 131}` FIGURE IS *LINE NUMBERS*, NOT ROW IDS — RECONCILED AT THE PHASE-89 IMPL.** The malformed rows are ids **57** (`fields=9`) and **69** (`fields=10`), at lines 119 and 131. An ad-hoc escape-aware field-count form that prints IDS reads `{57, 69}` and LOOKS like drift; it is not. Do not "correct" either figure.

**SENTINEL RUN MECHANICALLY AT THE PHASE-89 IMPL (2026-08-17), ON BOTH SIDES OF THE ROW FLIP. IT DOES NOT FIRE; `stop` was NOT created** (verified absent at repo root). ACTUAL output, recorded not predicted: input 239 lines / 121 data rows on both sides · **BEFORE:** (1) `NOT DONE: row 89` ALONE, (2) SIX at `:199 :205 :211 :221 :227 :235`, (3) SILENT · **AFTER:** (1) **SILENT**, (2) **SIX at the same anchors**, (3) SILENT.
- **ALL FOUR NCs FIRED ON THE POST-FLIP FILE.** row-62 doctoring with `NC LANDED? [ in-progress ]` inspected FIRST ⇒ `NOT DONE: row 62` **ALONE** (row 89 correctly absent) · denominator `want=120` ⇒ `GATE FAIL: examined 121 data rows, expected 120` · check-(3) doctoring (residual **2 -> 0** confirmed first) ⇒ `NEVER OPENED: gRPC` with WASM correctly silent · check-(2) one-arm counts **5** long / **1** short, union **6** (a one-arm strip is NOT an NC for the union).
- Every leak axis at this tip: `-family row` **95 / 67** · `gRPC-family row` **2** · `Operational-tooling-family row` **3**. Row 89 `fields=8`.
- ⚠️ do NOT "fix" the six and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases; the candidate sentences are a WINDOW onto a larger deferred backlog, not an inventory of it. ⚠️ **THE INVENTORY IS ~59, NOT ~42** — the long-carried "~42" is the SENTENCE-VISIBLE subset only; window `:221` names SIXTEEN FURTHER un-chartered items OUTSIDE its live sentence. **Cite ~42 only as "sentence-visible", never as the inventory.** ⚠️ **AND TWO WINDOW CANDIDATES ARE STALE — THE WINDOWS DO NOT SELF-CLEAN:** window `:221` still lists the H2 `//`-path routing bug that **ROW 87 CLOSED** and the `/stats/prometheus` projection gap that **ROWS 79 AND 80 CONSUMED**. **A candidate's presence in a window is NOT evidence it is still open — re-derive at your tip before costing it.**

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (ugrep under the hood). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Reproduced at THIRTY separate tips.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **THE METHOD BINDS; carry no standing discrepancy figure and no standing absolute.** ⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS; print FILE NAMES, never a count.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files; use `-v --no-index`, and `git status` is the discriminating check). ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **`grep -c` ON ZERO MATCHES IS TOOL-SPECIFIC** (only `git grep -c` prints nothing; all exit 1 — capture the value, never chain on the exit code). ⚠️ **`grep -c` COUNTS *LINES*, NOT *OCCURRENCES*.** ⚠️ **`\t` IN GNU ERE IS A LITERAL `t` — use `-P`.** ⚠️ **A `git grep` PATHSPEC ENDING IN `/` READS A FAIL-UNSAFE ZERO** — use the shell-glob form. ⚠️ **AN UNANCHORED PANIC-GREP FALSE-FIRES 14 TIMES ON A FULLY-GREEN DIFFERENTIAL LOG** — **the canonical gate is ANCHORED: `^panic:|DATA RACE|SIGSEGV`. A gate that fires on a green log is worse than no gate.** ⚠️ **AND `STATE.md` QUOTES ITS OWN MATCHERS:** an unanchored `grep -c 'next-skill:'` reads 2 on the unmodified file; the anchored `^- \*\*next-skill:\*\*` reads 1.

## ⚠️ THREE VACUOUS MATCHERS WERE FOUND IN PHASE 89 ALONE — ALL SHARING ONE SHAPE

**A gate anchored on a line ending the tool never actually emits reads 0 on success, which is indistinguishable from "the selector matched nothing".** All three were found by EXECUTION, none by inspection:

1. ⚠️ **`test/fixtures/0004-h2-routing/driver/driver_test.go`'s residual-port guard — REPAIRED at phase 89.** It was `strings.Contains(out, "port_value: 0\n")`, but **every** `port_value: 0` in both YAMLs sits inside a flow mapping followed by ` }`: `grep -c 'port_value: 0$'` reads **0** while the unanchored form reads **3** / **5**. The check could not fire on ANY input, including a deliberately injected extra placeholder. Now a regexp (`port_value: 0(?:[^0-9]|$)`), proven live: on a render carrying one genuinely unsubstituted placeholder the **old form reads `false` and the new form reads `true`**.
2. ⚠️ **THE DIFFERENTIAL DENOMINATOR GATE.** `grep -c '^--- PASS: TestDifferential/<fixture>$'` reads **0 on a GREEN log** — the subtest line is INDENTED four spaces AND carries a ` (9.14s)` duration suffix, so `^---` and `…$` each fail independently. **Correct form:** `grep -cE '^ *--- PASS: TestDifferential/<fixture>( \([0-9.]+s\))?$'`, negative-controlled against a fabricated fixture name reading 0.
3. ⚠️ **THE FIXTURE-COUNT REGEX.** `^ *--- PASS: TestDifferential/[0-9]{4}-[a-z0-9-]+ \(…\)$` reads **119, not 121**, because `0007a-cors` and `0007b-iteration-probe` carry LETTER suffixes that `[0-9]{4}-` does not admit — **a spurious two-fixture shortfall that reads exactly like a mid-suite abort.** Use a SET RECONCILIATION (`comm` of the `=== RUN` set against the resolved-result set, empty in BOTH directions).

## ⚠️ `INNER_EXIT` DOES NOT EXIST IN THIS REPO — MEASURED AT THE PHASE-89 IMPL

The six-gate posture below has demanded for many phases that every differential launch capture **`INNER_EXIT`**, because *"the wrapper exits 0 even when the binary aborts mid-suite"*. **There is no wrapper.** `git grep -n 'INNER_EXIT' -- ':!docs'` returns **exactly one** hit — THIS FILE's instruction text — and `git ls-files | grep -icE '\.(sh|mk|bash)$|Makefile'` returns **0**. The suite is launched as a direct `go test ./test/differential/`, so **the outer process IS the inner process** and `PIPESTATUS[0]` is the only exit code there is. ⚠️ **`DECISIONS.md` nonetheless records `INNER_EXIT=0` as a measured gate reading at phase 87** — a value nothing in the tree emits. **THE HAZARD IS REAL; THE MECHANISM NAMED IS NOT.** Use instead: `PIPESTATUS[0]` (never `rc=$?` after a pipe) · the terminal `ok  …/test/differential  <n>s` line · and the **121/121 SET RECONCILIATION**. **Do not quote an `INNER_EXIT` value; you would be inventing it.**


==============================================


## STATUS AS OF 2026-08-17: PHASE 89 (`h2-decode-filter-mutations`) **CLOSED** — row 89 `done` at `ROADMAP.md:151`, `want` **121**, ALL 121 ROWS `done`. **THIS SESSION SELF-PICKS PHASE 90 AND WRITES ITS BRAINSTORM.**

**What the 89 IMPL landed (2026-08-17, base master `404de01f`, branch `phase-89-impl`, ONE squashed commit).** `internal/filter/hcm/h2dispatch.go` **+284** and `internal/filter/hcm/h2/stream.go` **+20** (production **+304 / -0**); unit **+1028** across four files (one new, `h2dispatch_reconcile_test.go`, 770 lines); differential `test/fixtures/0004-h2-routing/` **+688 / -24** over seven files. **ADR-0311 COMPLETED IN PLACE** (strict `PROPOSED` guard **1 -> 0**), `BEHAVIOR_CONTRACT.md` **5960 -> 5962**, `ROADMAP.md` row 89 flipped with `want` **121 -> 121**.

### ⚠️ THE HEADLINE: THE IMPL REFUTED ITS OWN PLAN'S FROZEN DECISION, AND THE REFUTATION CHANGED THE ALGORITHM

**Reference Envoy has TWO write rules, not one.** Measured against `envoyproxy/envoy:contrib-v1.37.2`, downstream H2 -> upstream H2, raw-framer recorder with **one `hpack.Decoder` per connection** (`hpack_errors=0`, `n=12`, no-mutation positive control captured FIRST):

| reference verb | rule | measured |
|---|---|---|
| `OVERWRITE_IF_EXISTS_OR_ADD` / `remove` | remove EVERY occurrence, append one copy at the TAIL | `x-p89-changed: old` at `[7]` removed; `new` reappears at the tail |
| `APPEND_IF_EXISTS_OR_ADD` | **the existing field STAYS AT ITS POSITION**; only the added value goes to the tail | `x-client-dup: c0` STAYS at `[9]`; `v1` lands at the tail |

D-89-DUP froze only the first and applied it to both, which **RELOCATES AN OTHERWISE-UNTOUCHED ORIGINAL TO THE TAIL.** The reconciler now classifies by testing whether the pre-decode value list is a PREFIX of the post-decode one. Pinned by unit row `7b_append_to_existing_keeps_original_in_place`, negative-controlled TWICE.

### ⚠️ SIX FINDINGS THE NEXT SESSION MUST NOT RE-LEARN

1. ⚠️ **COMMIT BEFORE YOU BREAK ANYTHING.** The controller restored a break patch with `git checkout -- <path>` while a fix was still UNCOMMITTED and lost it — `reference_break_protocol_commit_first`, fired in the very session that quotes it. Detected only because `sha256sum -c` reported FAILED and the census reddened.
2. ⚠️ **TWO OF TWELVE BREAK ARMS DID NOT REDDEN, AND THEY ARE DIFFERENT FROM EACH OTHER.** **B2** (fresh output slice -> `fields[:0]`) is a **NON-DEFECT**: all 640 rows stayed green and a **200,000-case** randomized differential found ZERO divergence, because the write index never outruns the read index of the copy loop — so the code comment's aliasing justification was FALSE and was CORRECTED. **B3** (the `:`-skip) is a **LIVE GUARD AT THE WRONG LAYER**: the pseudo-injections precede the snapshot and `header_mutation` config-rejects `:` keys on BOTH sides, but `CanonicalMIMEHeaderKey(":path")` returns `":path"` and `lua/bridge.go` writes caller-supplied names straight through `Add`/`Set`/`Del`, so the guard IS reachable via programmatic filters and is pinned at UNIT level. **"The break did not fire" is a question, not a verdict — answer it.**
3. ⚠️ **A DOC SENTENCE WARNING ABOUT A CONTAMINATED GREP BECAME ITS OWN THIRD CONTAMINANT.** A bullet stating "the bare symbol grep reads N" was measured reading N+1 at the next gate, because the bullet itself matched. **Restate such lessons with NO number and WITHOUT spelling the matcher.**
4. ⚠️ **A CONFIG-VALIDATION PROBE WITHOUT A POSITIVE CONTROL PRODUCES A CONFIDENT FALSE NO-GO.** A probe's first render substituted PEM bodies into a doc-comment line; BOTH arms failed identically and would have been recorded as "the reference rejects this".
5. ⚠️ **`0004`'s backend and client BOTH destroy header order.** The backend is `net/http` + `http2.ConfigureServer` (x/net folds HEADERS into a map before any handler runs); `helpers.H2RoundTrip` sets headers via `req.Header.Add` on a Go map, so duplicates are ALWAYS ADJACENT and inter-name order is RANDOMIZED per request. **Wire order is only observable at unit level over `[]hpack.HeaderField` literals, or via a raw-framer backend (BackendKind tail 38 -> 39).**
6. ⚠️ **THE REFERENCE DOES NOT SANITIZE CONNECTION-SPECIFIC HEADERS.** It FORWARDS `connection`/`transfer-encoding`/`te: gzip` and a conformant upstream answers **400 with ZERO backend delivery**; envoy-go DROPS them. A NAMED DEPARTURE in the contract — do not "fix" it toward parity.

### ⚠️ COUNTS AT THIS TIP — RE-DERIVE MECHANICALLY, NEVER COPY

- **`ROADMAP.md` 239 lines / 121 data rows**, ALL `done`; check-(2) anchors `:199 :205 :211 :221 :227 :235`; `-family row` **95 / 67**; `gRPC-family row` **2**; `Operational-tooling-family row` **3**; ARM-A malformed rows ids **57**/**69** at lines **119**/**131**.
- **`DECISIONS.md` 18277** · **310** headings · tail **ADR-0311** (COMPLETE) · next-free **ADR-0312** (TAIL-derived; `grep -c '^## ADR-0312'` => **0**; headings+1 COLLIDES at the ADR-0209 gap) · **strict `^> \*\*STATUS: PROPOSED` guard 0 — DISARMED; the next SPEC RE-ARMS it 0 -> 1** · `^---$` **216**. ⚠️ **A NEW ADR TAKES NO `---` SEPARATOR.** ⚠️ **CARRY NO WHOLE-FILE COUNT OF THE LOOSE `PROPOSED` MATCHER.**
- **`BEHAVIOR_CONTRACT.md` 5962** (phase 89 added TWO bullets to `## HTTP/2` § Asserted equivalence) — ⚠️ **every by-line cite at or below that list shifts +2; phase 88 shifted them +2 and phase 87 +1. CITE BY STRING OR SYMBOL.**
- **`STATE.md` 63** · **`STATE_HISTORY.md` 506** (archive labels **202**) · **`BOOTSTRAP_PROMPT.md` 522** (repo ROOT) · phase dirs **130** · `REVIEW.md` **37** (standing departure, named not claimed).
- **differential fixtures 121** at **`test/fixtures/`** (⚠️ **NOT `test/differential/fixtures/`, a plausible wrong path returning a SILENT `0`**); numeric tail `0119-grpc-unary-trailers`, next-free **`0120` STAYS UNCONSUMED**. · **fuzzers 55 / 48 files.** · **BackendKind tail 38** (`H2GoawayResponder`). · **fixture blank imports 121** — the NARROWED form `grep -cP '^\t_ "[^"]*test/fixtures/'`; ⚠️ **the unnarrowed `grep -cP '^\t_ "'` reads 123 and is REFUTED.**
- **stat surface: the COMMAND binds, not the number.** `grep -ro --include='*.go' -e 'NewCounter(' -e 'NewGauge(' .` => **406**. Measured **406 on master AND tip ⇒ DELTA 0.**
- **The slice-only-writer gate reads 6 at this tip** (was 5 before phase 89; the reconciler is the sixth writer) — `git grep -nE '\.Headers[[:space:]]*=[^=]' -- 'internal/filter/hcm/*.go' 'internal/filter/hcm/h2/*.go' | grep -v '_test\.go' | grep -v 'resp\.Headers' | wc -l`. ⚠️ **NAMED BLIND AXES: element-level in-place mutation (`.Headers[i].Value = …`) and any writer outside `internal/filter/hcm/`.**
- ⚠️ **STILL CONTESTED, NO NUMBER CARRIED:** the `STATE_HISTORY.md` archive-gap total · the loose `PROPOSED` matcher's whole-file count · the stat-surface DOC-side absolute (1205 vs 1207) · the zero-test `generic/*` suite count (13 vs 14).

### CANDIDATES FOR THE PHASE-90 SELF-PICK — each NAMED, none chartered, ALL COSTS STALE

⚠️ **RE-DERIVE EVERY COST AT YOUR TIP** (`reference_deferred_candidate_cost_restale`), and remember a candidate's presence in a sentinel window is NOT evidence it is still open.
- **`host` / `:authority` aliasing to the authority scalar on BOTH legs** — the reference-exact form of D-89-HOST; strictly larger than the phase-89 delta reconciler and touches the H1 leg.
- ⚠️ **TWO PRE-EXISTING SUBJECT `host`/`:authority` DIVERGENCES, reachable at the tip with NO filter at all:** inbound `:authority` AND `host` ⇒ envoy-go projects **BOTH** upstream while the reference **DROPS** the regular `host`; inbound `host` ONLY ⇒ envoy-go emits **`:authority = ""` (EMPTY)** while the reference **PROMOTES** it. Measured at the phase-89 PLAN and re-confirmed at the IMPL.
- **Decode-side TRAILERS** — `RunDecodeTrailers` is not called on the H/2 dispatch path; no seam exists.
- **The three ADR-0310 §Consequences (x) candidates that are NOT row 89:** stream-scoped header-overflow parity via a decode-and-discard drain · `max_request_headers_kb` config parity + the `http2.header_overflow` stat · `SETTINGS_MAX_HEADER_LIST_SIZE` advertisement.
- **The six sentinel windows** at `:199 :205 :211 :221 :227 :235` (~59 items, only ~43 sentence-visible).

### ⚠️ DOCUMENTARY DEFECTS — carried forward, still deliberately NOT fixed

⚠️ **`DECISIONS.md` records `INNER_EXIT=0` at phase 87 — a value nothing in the tree emits** (see above; append-only, so RECORDED not fixed) · ⚠️ **the PHANTOM `h2.parseHeadersForRequest` cited at `h2dispatch.go:462`/`:479`/`:491`** against `git grep -c '^func.*parseHeadersForRequest'` ⇒ **0** (positive control: `buildH2Request` ⇒ 1) · `internal/filter/http/types.go`'s FALSE *"per ADR-0071's filter API stability"* comment (⚠️ **write "rejected on measured blast radius — 33 production + 38 test files, ~101 mutation call sites, and an `OrderedHeaders` type carrying only `Get`/`ToHTTPHeader`". NEVER "per ADR-0071"**) · `header_mutation` rejecting `remove` of a protected header where the reference config-ACCEPTS it · `RunAction`'s unguarded H2 arm · `ADR-0051` §2 · `ADR-0058`'s dead `routerActionH2.doH2` location · `ADR-0057`'s "27 round-trips" (now **31**; ⚠️ `README.md:36`'s "first 27 requests" is CORRECT as a prefix — do NOT "fix" it) · **`ROADMAP.md` rows 57/69 malformed (ARM-A GUARD)** · **`STATE.md` §Project counts FROZEN at phase 76** (they self-contradict §Current — do not anchor on them) · `harness_test.go` port inventory STALE · `body.go` nolint INERT · xDS cycle guard NOT AUTOMATED · `wasm/doc.go:219` two errors · ROADMAP cites `esalaine` FIVE times · `rbac.go:50` token `F2` · root `PROGRESS.md` stray phase-32.1 doc · SPEC-86/PLAN-86 cite the grpcclient Close pins at a nonexistent `internal/xds/xdsgrpc/...` path · `BEHAVIOR_CONTRACT.md`'s "no stdlib net/http parsing" sentence (RETAINED, ridden by ADR-0309) · the TWO riders citing the ADR-0052 mandate at a DRIFTED `:1821` · window `:221` carries TWO CLOSED candidates.

### ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + ALL FOUR NCs YOURSELF** and record ACTUAL output. Check (1) is now SILENT in the healthy case — the row-62 NC carries the whole liveness burden.
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** The 89 IMPL refuted its own PLAN's frozen D-89-DUP, the PLAN's "free guard", the PLAN's `order=` line, the PLAN's A7b parity assumption, and a STANDING METHOD NOTE (`INNER_EXIT`). Do the same.
3. **LINE ANCHORS DRIFT; SYMBOL ANCHORS DO NOT — RE-VERIFY EVERY CITE AT YOUR TIP.** ⚠️ **And PATHSPEC-SCOPE every symbol assertion** — a bare `git grep` for a symbol whose signature a PLAN quotes is DOCS-CONTAMINATED.
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Still live: the **SDS dial-budget class** (`internal/boot/boot_sds_e2e_test.go:551`) · the **driver-owned receiver port race** (`0081-grpc-access-log` `0.0.0.0:35097` and `0.0.0.0:42039`, `0087-tracing-otlp` `0.0.0.0:35323`) · `internal/cluster` `-race` outlier · `internal/httpclient` zero-value · the two 84.2-era flakes · `TestServerConn_TinyWindowDelivery` · the REFERENCE h2spec section-8 flip. **Capture `-v` output BEFORE re-running anything.** ⚠️ **ZERO flakes fired at the phase-89 IMPL — every gate reading was a FIRST-PASS reading.**
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only). Foreign containers were present throughout phase 89 and were deliberately left untouched.
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`.
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL** (⚠️ **QUALIFIED AND PATHSPEC-SCOPED**), **and ASSERT THE BOUND BINARY with `ss -ltnp` before reading any probe result.** **`gofmt -l` never exits non-zero — gate on OUTPUT.** ⚠️ **A `-run` selector matching nothing prints `[no tests to run]` and EXITS 0 — quote the `=== RUN` DENOMINATOR on every census.**
8. ⚠️ **`pgrep -f` / `pkill -f` MATCHES YOUR OWN SHELL AND KILLS THE TOOL CALL (exit 144)**, and a `pgrep -f 'a\|b'` ALTERNATION MATCHES NOTHING SILENTLY — kill by PID from `pgrep -laf` with the `bash -c` line filtered out.
9. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-*`, `wt-*` or stage worktree should outlive its stage close** — expect only `master` (verify with `git worktree list`).
10. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** (a)/(b) differential **121 fixtures** + full `go test ./...`, gated on `PIPESTATUS[0]` and a **121/121 set reconciliation** (⚠️ **NOT `INNER_EXIT`, which does not exist**); (c) **h2spec cite ONLY from your own run — and note it is STRUCTURALLY incapable of anchoring any upstream-facing row: its harness configures `envoy.filters.http.router` ALONE with `direct_response` routes and never goes upstream**; grpc-conformance deferred in writing; proxy-wasm 10/16; (d) fuzzers 55/48; (e) the ANCHORED panic gate on every differential launch; (f) no REVIEW.md — standing departure.
11. ⚠️ **`-p` PUBLISHING WORKS; `--network host` IS WHAT DOES NOT** — `--network host` does not share the host netns here AND it SILENTLY IGNORES `-p`, so a probe combining them reads as "publishing is unreachable". **Host-gateway is `192.168.65.2`** (a Docker-Desktop-style VM daemon, not a `172.17.x` bridge). Both `-p` publishing and `--add-host=host.docker.internal:host-gateway` are working recipes. ⚠️ A **STRICT_DNS** cluster whose name does not resolve **blocks listener init entirely** — use STATIC, or `dns_lookup_family: V4_ONLY`, and **verify the listener is bound before believing any probe result**. ⚠️ **`clusters: []` is BOOT-REJECTED even when every route is a `direct_response`** — a dummy cluster is required. ⚠️ **envoy-go REJECTS `match.headers` outright** (`route 0: match.headers is not supported in phase 04`).
12. ⚠️ **A RAW-FRAMER BACKEND MUST HOLD ONE `hpack.Decoder` PER CONNECTION.** The HPACK dynamic table is CONNECTION-scoped; a fresh decoder per request decodes only the FIRST request on a pooled connection and then yields `invalid indexed representation index NN` with a **truncated field list — which reads exactly like "headers were lost"**. **And a raw-framer backend is MANDATORY for wire-shape questions** — a `net/http`+`h2c` backend normalizes case, hides pseudo-headers and destroys order, showing GREEN for an illegal wire encoding. **Assert the backend RECEIVED something (n>0) BEFORE interpreting what it received.**

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 89 (h2-decode-filter-mutations)' --format='%h %s'   # 4 at this tip: BRAINSTORM, SPEC, PLAN, IMPL — a CLOSED phase, use as a FIRING CONTROL
```
⚠️ **The count comments are LIVE FIGURES — VERIFY before relying.** Take the NEWEST line (`| head -1`).

Execution style: **subagent-driven per `feedback_execution_style`**. Subagents commit LOCALLY on the ONE stage branch with EXPLICIT PATHSPECS; the controller squashes (`feedback_subagents_no_push`, `feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** and tear down docker **BY NAME**. ⚠️ **Band private ports outside BOTH `20000-31007` AND `11000-14999`, clear of the static fixture ports (10000-19172); the driver-owned receiver race has been observed at `35097`, `35323` and `42039`.** The 87 stages used 47410-47441, the 88 stages 47450-47559, the 89 stages 47560-47999; band yours from **48000+**. ⚠️ **ONE WORKTREE PER AGENT preferred; explicit-path staging if shared; never an unscoped `git restore`.** Per-task `gofmt` + `golangci-lint` on touched packages (⚠️ **misspell runs in locale US — sweep British spellings in `.go` comments BEFORE the lint gate**). ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch.**

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root — **observed repeatedly at every phase-88 and phase-89 stage, and the harness ANNOUNCES it**. Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) before any commit or gate run. ⚠️ **Shell VARIABLES do not survive between Bash calls** — re-export in every command.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — `git status --porcelain` AND `git diff --stat master` must show ONLY your intended edits. For a patched TRACKED file: `sha256sum` BEFORE patching, `git checkout -- <path>` after, `sha256sum -c` the capture — ⚠️ **and COMMIT FIRST, because `git checkout --` restores from HEAD and will wipe uncommitted work.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads SELF-PICK phase 90 + BRAINSTORM.** §Project counts SELF-CONTRADICT §Current — do not anchor on them (frozen at phase 76).
2. `docs/envoy-go/ROADMAP.md` — the six sentinel windows at `:199 :205 :211 :221 :227 :235` are the candidate pool; **re-derive every cost at your tip and check whether a candidate has already been consumed.**
3. `docs/envoy-go/phases/89-h2-decode-filter-mutations/PROGRESS.md` — the IMPL entry: the two-rule refutation, the twelve break arms across two layers, the two breaks that did not redden and why they differ, the three vacuous matchers, and the `INNER_EXIT` finding.
4. `docs/envoy-go/DECISIONS.md` **ADR-0311** (tail, COMPLETE) — §Decision (a)-(g), §Consequences (i)-(xi).
5. `BOOTSTRAP_PROMPT.md` §4-§7 (repo ROOT) — the lifecycle contract and the six-gate posture.

## What the BRAINSTORM owes
**SELF-PICK the subject** (smallest defensible candidate first; record the pick AND the rejected alternatives WITH costs re-derived at your tip) · **REPRODUCE the defect BY EXECUTION** with a positive control on every arm and a negative control on the isolating arm · **REGISTER row 90 `in-progress`** in `ROADMAP.md` with `want` bumped **121 -> 122 IN THE SAME COMMIT** ⚠️ **and COUNT THE ROW'S FIELDS BEFORE INSTALLING IT** (`fields` must read 8; an unescaped `|` in the summary makes the row measure 10 and check (1) PASSES ANYWAY because the stray pipes fall after field 5) · run the sentinel and all four NCs on **BOTH SIDES** of the row add and record ACTUAL output · anticipate counts · `PROGRESS.md` created · `STATE.md` rolled IN PLACE (lifecycle-state **DONE -> 1**) with the oldest §Recent entry evicted by a **DIRECT DATE READ** (⚠️ **the tie shape does NOT carry — phase-89 IMPL faced a TWO-way, the PLAN none, the SPEC a TWO-way, the BRAINSTORM a THREE-way, phase-88 IMPL a FOUR-way**) · `next-prompt.txt` rolled (`git add -f`) · ONE squashed commit, merged and pushed. ⚠️ **The BRAINSTORM adds NO ADR and leaves the strict `PROPOSED` guard at 0 — the SPEC re-arms it.**
