START a fresh cold-start session for envoy-go — **phase 33 (`network-filter-thrift-proxy`) BRAINSTORM is DONE; the next lifecycle step is the phase-33 SPEC** (`superpowers:writing-plans` scoped to SPEC authoring — a SINGLE FLAT ROW, the kafka-31 precedent). The phase-33 BRAINSTORM squash LANDED at `23b5121` on origin/master (`docs/envoy-go/phases/33-network-filter-thrift-proxy/BRAINSTORM.md`; the 12-§ redis-32/kafka-31 template; ROADMAP row 33 registered `in-progress`; the §9 candidate-roster paragraph marks thrift IN-PROGRESS + records the family-closes-after-33 + the `/contrib`→core correction; squash-merged from `phase-33-network-filter-thrift-proxy-brainstorm` + pushed by the controller per `feedback_push_to_origin`; the brainstorm worktree cleaned up + the branch deleted at stage-close). `thrift_proxy` is the §9 Network-filters family's LAST candidate + the project's SECOND terminal routing proxy (after redis-32), on the ADR-0230 upstream-pool seam REUSED unchanged. **After phase 33 phase-done the §9 Network-filters family CLOSES.** This is a fresh cold-start for the phase-33 SPEC. This docs-only next-prompt update trails the phase-33 BRAINSTORM squash (`23b5121`) as the live master tip; confirm via `git log --oneline -1`.

The phase-33 SPEC executes the BRAINSTORM §10 empirical pins (D-T1..D-T9) IN-SESSION (parallel-subagent fan-out) live against the contrib reference Envoy + anchors the ADR-0231 §Context. Per `feedback_execution_style` the downstream PLAN→IMPL run subagent-driven; per `feedback_git_worktrees` work in a worktree.

## What this session does — the phase-33 {thrift} SPEC

Author `docs/envoy-go/phases/33-network-filter-thrift-proxy/SPEC.md` (+ README.md) for the **phase-33 thrift_proxy network filter** (`thrift_proxy`, `envoy.filters.network.thrift_proxy`, proto `envoy.extensions.filters.network.thrift_proxy.v3.ThriftProxy`). The BRAINSTORM settled the envelope; the SPEC pins the empirical surface against the reference. **The BRAINSTORM decisions the SPEC inherits (do NOT re-litigate):**

  - **Q0 subject + the dep CORRECTION** — thrift_proxy is a **CORE `/envoy v1.32.4` extension** (verified in the go module cache: `thrift_proxy/v3` lives under `github.com/envoyproxy/go-control-plane/envoy@v1.32.4`, where `redis_proxy/v3` lives — NOT under `/contrib` where `kafka_broker/v3` lives). The prior cold-start "`/contrib` like kafka_broker" framing was WRONG and is corrected in the BRAINSTORM. Anticipated ZERO new go.mod dep because the existing CORE `/envoy` dep already carries it (the redis D32-1 posture, NOT the kafka `/contrib` posture). Confirm the TypeURL via `proto.MessageName` (per `reference_network_filter_typeurl_extensions`; the `extensions.` segment intrinsic) at D-T1.
  - **Q1 envelope** — a SINGLE-PAIR SINGLE-ROUTE-TERMINAL MVP: ONE transport×protocol pair under `payload_passthrough` (route on the message-begin envelope — method name + message type + `seq_id`; skip the struct body) + `route_config` consumed as a match-all/single-method route to ONE upstream cluster + a router-only nested thrift-filter sub-chain + the global `thrift.<stat_prefix>.request*/response*` stat roster. DEFERRED (parse-accept-vs-reject per field, pinned at SPEC D-T4): the rest of the 3×3 transport×protocol matrix (`unframed`/`header` × `compact`/`twitter` + `auto`), full route_config richness, the non-router thrift_filters (`rate_limit`/`header_to_metadata`/`payload_to_metadata`), full struct parsing, seq_id-keyed multiplexing, `max_requests_per_connection`, `access_log`; the close-direction `cx_destroy_local/remote_with_active_rq` counters deferred as a coverage boundary (the §9 zero-touch AMEND-R9 posture); histograms deferred (ADR-0060).
  - **Q-seam** — the ADR-0230 upstream connection-pool / cluster-routing seam (`internal/filter/network/upstreampool.go`) is REUSED UNCHANGED (one-conn-per-downstream, synchronous single-flight, positional/FIFO correlation; thrift's `seq_id` parse-validated but NOT demux-load-bearing for the single-flight MVP). thrift is ADR-0230's anticipated SECOND consumer — the FIRST reuse of a prior framework seam → framework-ZERO-touch. NO new seam, NO new seam ADR.
  - **Q-split** — a SINGLE FLAT ROW 33 (no parent-SPEC/sub-phase split; the seam-first driver of redis's 2-way split is gone since the seam is reused) + a PRE-AUTHORIZED 33.1-codec / 33.2-stats escape-valve (unconsumed unless the SPEC/PLAN trips the ADR-0045 gate; ~800–1300 prod LoC / ~15–22 tasks anticipated, UNDER the gate).

## The empirical pins to execute at the SPEC (BRAINSTORM §10 D-T1..D-T9 — live against the contrib reference Envoy per `reference_docker_probe_bridge_network`)

  - **D-T1** (SPEC-BLOCKING) — TypeURL via `proto.MessageName` + confirm the CORE `/envoy` module (anticipated `github.com/envoyproxy/go-control-plane/envoy/extensions/filters/network/thrift_proxy/v3`, NOT `/contrib`) + a clean `go mod tidy` (ZERO new dep).
  - **D-T2** (SPEC-BLOCKING) — the transport×protocol PAIR the MVP decodes (the reference's default; anticipated `framed`×`binary`) + the exact wire format (transport frame + message-begin [method name + message type + `seq_id`] + the struct-skip for `payload_passthrough`); enough to hand-craft the `TCPThriftResponder` canned replies + the driver + the downstream-response byte-equivalence assertion (`reference_wire_format_both_sides_see_same_bytes`).
  - **D-T3** (SPEC-BLOCKING) — the global `thrift.<stat_prefix>.request*/response*` stat roster (names + eager-vs-dynamic + gauge/`request_active` members) + the downstream connection-counter set + whether any per-route/per-method stat is wire-derived (→ D-T7) + the `thrift.` Prometheus arm shape (INLINE [kafka] / MULTI-label [mongo] / SINGLE-label-hoist [redis]).
  - **D-T4** — the config PGV/parse arms (stat_prefix/route_config required? undefined-cluster rejected? an un-implemented transport/protocol value rejected at parse or accepted-and-fails-at-decode? thrift_filters/payload_passthrough accepted?) + the `0058-thrift-boot-reject` arm + include-or-fold.
  - **D-T5** (SPEC-BLOCKING) — router/local-reply semantics: does the reference answer anything LOCALLY (a routing miss → a local EXCEPTION; a decoding error → a local error reply) without dialing upstream? Governs the `TCPThriftResponder` traffic + the round-trip stat counts + the `0057` fixture (the redis D32-6 PING-local analogue).
  - **D-T6** — the `seq_id` echo + the positional-correlation contract (the reply echoes the request's seq_id; the single-flight one-conn-per-downstream model faithful for sequential traffic; decide whether the differential validates the seq_id echo).
  - **D-T7** — the `stats.IsValidName` charset-guard placement IF a per-route/method stat is wire-derived (else satisfied-by-construction over the fixed roster — the redis D-P32-7 posture); the `FuzzThriftDecode` no-panic fuzzer needs the guard regardless.
  - **D-T8** — the close-direction `cx_destroy_local/remote_with_active_rq` deferral as a coverage boundary (the `reference_close_direction_framework_gap` boundary, the §9 zero-touch AMEND-R9 posture); record the deferral + any subject-side coverage-boundary value.
  - **D-T9** — (a) the ADR-0045 task/LoC envelope confirming the single flat row holds (re-checked at PLAN); whether the 33.1/33.2 escape-valve is needed; (b) the `upstream_cx_*` per-side pooling-stat asymmetry pinned per-side (not equality) where the reference's pool diverges (the redis D-P32-9 precedent).

## The phase-32 as-built to carry forward (the redis_proxy terminal-proxy precedent the thrift SPEC mines)

- **redis_proxy DONE** — the 10th network-filter built-in + the FIRST terminal routing proxy. `internal/filter/network/redisproxy/` (config + RESP codec + the static command table + `classify` dispatch + the stat roster + the `Handle` pump). The package SHAPE thrift's terminal-proxy package (`thriftproxy`, the 11th built-in) mirrors for a different protocol.
- **The upstream-pool seam (ADR-0230)** — `internal/filter/network/upstreampool.go` (`UpstreamDialFunc`/`UpstreamConn`/`NewUpstreamConn`/`Send`/`Reader`/`Close`; lazy dial; one-conn-per-downstream; FIFO/positional correlation; the dial-closure decoupling — `internal/filter/network` has NO `internal/cluster` import). **thrift REUSES it UNCHANGED** (the FIRST reuse of a prior framework seam; validating its redis-scoped YAGNI sizing).
- **The prom-arm shapes** (`internal/stats/name.go`) — kafka INLINE (empty labels) / mongo MULTI-label / redis SINGLE-label-hoist. thrift's `thrift.` arm picks one at SPEC (D-T3).
- **The differential surface** — `0055-redis-roundtrip` (cross-side; downstream-RESP byte-equivalence + `StatsAsserter`) + `0056-redis-boot-reject`; the `TCPRedisResponder` BackendKind 32; `FuzzRESPDecode` (41st). The terminal-proxy differential template thrift extends (a new `TCPThriftResponder` BackendKind 33 + `0057-thrift-roundtrip` + `0058-thrift-boot-reject` + `FuzzThriftDecode` [42nd]).
- **The §9 close-direction-zero-touch posture (AMEND-R9)** — redis_proxy added no `cx_destroy_*` counters; thrift's two close-direction counters are likewise DEFERRED as a coverage boundary (D-T8).

## Counts at master tip (phase 33 BRAINSTORM done — the phase-33 SPEC session opens from here)

- stat surface **1091** (BEHAVIOR_CONTRACT doc count; advances at the phase-33 IMPL by the thrift roster, TBD at SPEC).
- differential fixtures **58** (canonical `ls -d test/fixtures/[0-9]* | wc -l`; tail `0056-redis-boot-reject`; advances to 59–60 at the phase-33 IMPL).
- fuzzers **41** (canonical `grep -rh "^func Fuzz" $(find ./internal -name fuzz_test.go) | wc -l`; tail `FuzzRESPDecode`; advances to 42 `FuzzThriftDecode` at the IMPL).
- BackendKind tail **32** (`TCPRedisResponder`; advances to 33 `TCPThriftResponder` at the IMPL; the values are a GLOBAL monotonic counter, decoupled from phase numbers).
- DECISIONS.md tail **ADR-0230** (next-free **ADR-0231** — the phase-33 SPEC anchors the ADR-0231 §Context [the thrift_proxy filter; NO parent-umbrella ADR since no split, NO seam ADR since the ADR-0230 seam is reused]; the §Decision/§Consequences body lands at the IMPL per ADR-0044).
- reference pin: `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227 — thrift_proxy is a CORE extension present in both the standard and contrib images; the project boots the contrib image).
- Toolchain: Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); go-control-plane `/envoy` v1.32.4 (ADR-0008) + `/contrib` v1.32.4 (ADR-0228) — thrift_proxy is a `/envoy` (CORE) subpackage → ZERO new go.mod dep anticipated.
- conformance h2spec 53/53 + proxy-wasm 10/10 (image-independent; UNAFFECTED by phase 33 — touches no HTTP/h2/proxy-wasm path).
- §9 Network-filters family: rows 26/27/28/29/30/31/32 DONE; **row 33 `in-progress` ({thrift}, the LAST candidate — the family CLOSES after phase 33 phase-done)**.

## Read first (in order)

1. THIS FILE — the phase-33 {thrift} SPEC cold-start.
2. `docs/envoy-go/phases/33-network-filter-thrift-proxy/BRAINSTORM.md` — the 12-§ design doc + the D-T1..D-T9 empirical pins the SPEC executes.
3. `docs/envoy-go/STATE.md` — active-phase `phase 33 BRAINSTORM done`; lifecycle-state → the phase-33 SPEC; next-skill `superpowers:writing-plans`; the counts (1091 stats / 58 fixtures / 41 fuzzers / BackendKind tail 32 / DECISIONS tail ADR-0230, next-free ADR-0231).
4. `docs/envoy-go/ROADMAP.md` — row 33 `in-progress` + the §9 candidate-roster paragraph (rows 26-32 DONE; thrift the sole open candidate; the family closes after 33; the `/contrib`→core correction).
5. `docs/envoy-go/DECISIONS.md` — **ADR-0230** (the upstream-pool seam — REUSED unchanged) + **ADR-0229** (the redis_proxy filter — the terminal-routing-proxy precedent) + **ADR-0228** (kafka_broker — the single-flat-row + the contrib precedent) + ADR-0227 (the contrib pin) + ADR-0106 (the §9 family flat-row discipline) + ADR-0045 (the split-gate) + ADR-0044/0052/0060.
6. The phase-32 as-built: `docs/envoy-go/phases/32-network-filter-redis-proxy/` (BRAINSTORM + parent SPEC + README) + `32.1-…/` (the seam + codec) + `32.2-…/` (the command set + roster + the `redis.` prom arm).
7. `internal/filter/network/upstreampool.go` (the seam thrift REUSES unchanged) + `internal/filter/network/redisproxy/` (the terminal-proxy package shape) + `internal/filter/network/builtins/builtins.go` (the built-in registration) + `internal/bootstrap/bootstrap.go` (the blank-import TypeURL anchor).
8. `internal/stats/name.go` (the kafka INLINE / mongo MULTI-label / redis SINGLE-label-hoist prom-arm contrast — thrift picks one) + `test/differential/fixture/fixture.go` (the `StatsAsserter` + the BackendKind enum, tail 32).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — especially `reference_wire_format_both_sides_see_same_bytes` (adopt upstream framing verbatim; recurs for thrift), `reference_differential_reference_parses_full_message` (cross-side fixtures need fully-valid frames + per-side `*.failure` pins), `reference_close_direction_framework_gap` (the §9 close-direction boundary — thrift's two close counters deferred), `reference_dynamic_stat_name_charset_guard` (wire-derived stat segments → IsValidName guard), `reference_network_filter_typeurl_extensions`, `reference_docker_probe_bridge_network`, `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_subagents_no_push`, `feedback_pertask_gofmt_lint`.

## Honor throughout

- `feedback_execution_style` (the downstream PLAN→IMPL run subagent-driven) · `feedback_git_worktrees` (work in a worktree) · `feedback_subagents_no_push` (subagents commit LOCAL-ONLY; the controller squash-merges + pushes at stage-close) · `feedback_push_to_origin` (after a clean landing on master with tests green, push without asking) · `feedback_pertask_gofmt_lint` (each task runs `gofmt -l` + `golangci-lint run` on touched packages).
- ADR-0106 (the §9 family flat-row discipline — phase 33 is a flat row) · ADR-0045 (the split-gate — re-check at SPEC + PLAN; the 33.1/33.2 escape-valve unconsumed unless tripped) · ADR-0230 (the upstream-pool seam — REUSED unchanged at the second consumer) · ADR-0044 (§Context-at-SPEC / §Decision+§Consequences-at-IMPL) · ADR-0052 (the atomic-landing six-gate) · ADR-0060 (histograms deferred) · ADR-0227 (the contrib pin) · ADR-0008/0228 (the go-control-plane deps — thrift_proxy is a CORE `/envoy` subpackage, anticipated ZERO new dep).
- The SPEC is a SINGLE FLAT ROW (the kafka-31 precedent — NO parent-SPEC/sub-phase split). It executes the §10 D-T1..D-T9 empirical pins IN-SESSION against the contrib reference Envoy per ADR-0004/ADR-0227, anchors the ADR-0231 §Context, and routes to the phase-33 PLAN (`superpowers:writing-plans`). spec-document-reviewer gate applies at the SPEC. At the SPEC-DONE commit ROADMAP row 33 STAYS `in-progress` (it flips `→ done` at the IMPL six-gate — a flat §9 row, NO parent rollup).
