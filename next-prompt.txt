=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-CHECKED 2026-07-12 (post-phase-57-IMPL) — it does NOT fire. Do NOT create `stop`.** Evidence:
- (1) prints NOTHING — every chartered row is `done` (row 57 flipped at the phase-57 IMPL). **An all-done row table ALONE does NOT fire the sentinel** — checks (2) and (3) both still print output and each independently blocks `stop`.
- (2) prints the Observability family's LIVE deferred-candidates sentence: OTLP-metrics stats sink + tracing `custom_tags`/`spawn_upstream_span`/`http_service`/force-trace + **the NEW dog_statsd explicit-`max_bytes_per_datagram: 0` parity fix** (recorded at phase 57 per SPEC-57 §2). ⇒ the **Observability family STAYS OPEN**. (NOTE: a second, HISTORICAL sentence in the family line reads "candidates were:" — deliberately non-matching; only the "candidates:" sentence is live.)
- (3) prints `NEVER OPENED: HTTP/3`, `gRPC`, `xDS`, `Runtime`, `WASM` ⇒ FIVE families never opened; the **Operational-tooling family** also STAYS OPEN (its deferred candidates live in its family section prose).

**STATUS AS OF 2026-07-12: phase 57 (`stats-sink-graphite`) is COMPLETE — row 57 `done` (sole leg, ADR-0106). NO phase is currently open.** The phase-57 IMPL landed: `internal/statssink/graphite.go` `GraphiteStatsdSink` (tags folded into the metric NAME as `;k=v` via the ~10-LoC `graphiteTagSuffix`; FOURTH sink-private `deltaState`); the phase-50 batching pair HOISTED to shared `appendBatchLine`/`flushBatch` in `udp.go` (D-GR-BATCHSHARE, dog_statsd tests byte-untouched); `parseGraphiteStatsdSinkConfig` + the FIRST `envoy.extensions.…` stat-sink type URL + the FOUR-sink sibling-reject (THREE test rows — a third found at IMPL beyond the PLAN's two) + the NEW explicit-zero reject (PGV parity); the fourth `main.go` loop; the additive `statsdrecv` `;k=v` extension; `FuzzGraphiteStatsdSinkConfigParse`; the `0101-stats-sink-graphite` merged tags+batching differential — five controller-executed breaks each firing its OWN assertion + non-break (d) order-reversal-green, 20/20 flake, full-package `-race`. ADR-0275 ANCHORED (§Decision/§Consequences per ADR-0044).

**Current counts (verified at the phase-57 IMPL exit):** fixtures **103** (tail `0101-stats-sink-graphite`) · fuzzers **54** · DECISIONS tail **ADR-0275** (next-free **ADR-0276**) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201 (+0)** · new Go packages **0** · new go.mod modules **0**.

**Locating the stage squash (a router cannot name its own sha — writing the sha changes it).** Identify it by SUBJECT, never by sha and never by position:
```sh
git log --oneline --grep='^phase 57 (stats-sink-graphite) IMPL' -1
```
This stage folded its own `next-prompt.txt` roll INTO the squash (per `reference_next_prompt_tracked_despite_gitignore`). The squash is NOT necessarily the master tip: parallel-workstream commits can land on top of it. ALWAYS `git fetch` + re-verify counts against the ACTUAL current master tip at the start of the next session.

**THE NEXT ACTION: RE-OPEN the loop — the next phase's BRAINSTORM (lifecycle-state 0 → 1).**

==============================================

## What THIS session does (the next-phase BRAINSTORM)

No phase is open. This session picks the next subject and performs its BRAINSTORM (docs-only; fresh worktree per `feedback_git_worktrees`, e.g. `.worktrees/phase-58-brainstorm`; register the new row `in-progress` in ROADMAP at the BRAINSTORM commit per the §Schema invariant; charter ADR-0276 as the row's anchor).

**Subject selection (the phase-57 precedent: after phase 56 landed, the loop was re-opened by a HUMAN PICK).** If the human has named a subject in this session's prompt, take it. Otherwise PAUSE and ask, presenting the candidate menu:
1. **Observability deferred candidates** (the family stays open): the dog_statsd explicit-`max_bytes_per_datagram: 0` parity fix (SMALLEST — a one-arm parse addition + tests; reuses the phase-57 reject shape verbatim); the OTLP-metrics stats sink (a gRPC sink over the phase-47 Flusher seam); tracing `custom_tags`/`spawn_upstream_span`/`http_service`/force-trace (phase-46 follow-ons).
2. **Operational-tooling deferred candidates** (family open — see its ROADMAP family section).
3. **Open a NEW family** (five never opened): HTTP/3, gRPC, xDS, Runtime, WASM — each a large charter; xDS was declined at the phase-57 pick but remains the most consequential.
(When a new family opens, ADD its row-summary slug to the sentinel check-(3) list above.)

## Honor throughout (the discipline, unchanged)
- **Lifecycle = one stage per session.** This session: the next phase's BRAINSTORM ONLY (docs-only; NO production `.go` changes at a BRAINSTORM).
- **Process anchors:** ADR-0044 (ADR §Context drafts at SPEC, §Decision/§Consequences at IMPL) · ADR-0045 (split gate ~15 tasks) · ADR-0080 (strict-reject anti-silent-divergence) · ADR-0106 (row flips `done` at the final IMPL six-gate). Next-free ADR is **ADR-0276**.
- **Live probes at SPEC time** use fresh containers per arm (`reference_probe_fresh_container_per_arm`) against the `envoyproxy/envoy:contrib-v1.37.2` pin (`reference_envoy_contrib_image_tagging`).
- **`next-prompt.txt` IS TRACKED** — edit it inside the stage worktree, fold into the stage commit; locate commits by SUBJECT (`git log --grep`), never by position.
- Baselines: RE-VERIFY the counts above against the actual master tip at session start (`git fetch` first; parallel workstreams land test-only commits).

## Read first (in order)
1. THIS FILE — the cold-start router (the next-phase BRAINSTORM is next; subject may need a human pick).
2. `docs/envoy-go/ROADMAP.md` — the family sections' deferred-candidate lists (Observability + Operational-tooling) + the never-opened families.
3. `docs/envoy-go/STATE.md` — the active-phase header (phase 57 IMPL done; no phase open).
4. `docs/envoy-go/DECISIONS.md` tail (ADR-0275) — the graphite row's §Decision/§Consequences (the reject-shape precedent the dog_statsd parity candidate would reuse).
5. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the `reference_*` traps.
6. A prior BRAINSTORM for the 12-section shape — `docs/envoy-go/phases/57-stats-sink-graphite/BRAINSTORM.md`.
