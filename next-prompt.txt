=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **PHASE 89 (`h2-decode-filter-mutations`) IS OPEN AT LIFECYCLE-STATE 2 — ROW 89 IS `in-progress` AT `ROADMAP.md:151`.** `want` IS **121**. ⇒ **THIS SESSION PERFORMS THE PHASE-89 PLAN.** This is BANKED mid-lifecycle work: per the standing directive, advancing it OUTRANKS opening a new subject. **DO NOT self-pick.** ⚠️ **THE SENTINEL DOES NOT FIRE AT YOUR START — check (1) PRINTS `NOT DONE: row 89`, check (2) prints SIX, check (3) is SILENT. DO NOT create `stop`.** ⚠️ **MEASURE IT YOURSELF BEFORE DOING ANYTHING ELSE; do not take the previous stage's word for it.**

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=121 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **`want` IS 121 AT THIS TIP AND A PLAN MOVES IT ZERO.** A PLAN adds no ROADMAP row; row 89 STAYS `in-progress` and `ROADMAP.md` should end the stage **BYTE-UNTOUCHED** (prove it with an EMPTY DIFF against master, not by inspection). Only a BRAINSTORM that opens a row moves `want`.

⚠️ **THE DOCTORING NCs REMAIN MANDATORY EVERY SESSION.** Both:
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md. Expect "NOT DONE: row 62" AND "NOT DONE: row 89"
# at this tip (row 89 is in-progress). ⚠️ CHECK (1) HAS ITS OWN LIVENESS BACK THIS ROW --
# it PRINTS in the healthy case -- but run the NC anyway: it is the only thing that
# distinguishes "row 62 is done" from "the doctoring silently failed to land".
```
```sh
# the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```

⚠️ **AND WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -oiE '-family row'` fails with `grep: amily row: No such file or directory` and the surrounding arithmetic prints `base=0 now=0 delta=0`, **which reads exactly like "no change".** Observed live at FOUR phase-84 stages and again at the phase-88 PLAN. **Always pass `--` before the pattern**, and remember the counts are form-dependent: **95 occurrences vs 67 LINES** at this tip (`grep -c` counts LINES). **A gate that reads zero on BOTH sides is not evidence of invariance.** ⚠️ AND the carried **ARM-A {119, 131} figure binds ONLY with its escape-aware pipe-split command** (BRAINSTORM-86 §6) — ad-hoc field-count forms do NOT reproduce it (an `NF!=8` form reads **17**); do not "correct" the figure with a different form.

**SENTINEL RUN MECHANICALLY AT THE PHASE-89 SPEC (2026-08-16, base `c1284a03`). IT DOES NOT FIRE; `stop` was NOT created** (verified absent at repo root). ACTUAL output, recorded not predicted: input 239 lines / 121 data rows · (1) **`NOT DONE: row 89` ALONE**, denominator silent · (2) **SIX at `:199 :205 :211 :221 :227 :235`** · (3) **SILENT**. **`ROADMAP.md` diff against master EMPTY (0 bytes)** — a SPEC edits no row, so there is only ONE side to measure and the empty diff is what makes that legitimate.
- **ALL FOUR NCs FIRED.** row-62 doctoring with `NC LANDED? [ in-progress ]` inspected FIRST ⇒ `NOT DONE: row 62` **AND** `NOT DONE: row 89` · denominator ⇒ `want=120` gave `NOT DONE: row 89` **plus** `GATE FAIL: examined 121 data rows, expected 120` · check-(3) doctoring (residual **2 -> 0** confirmed on the doctored copy first) ⇒ `NEVER OPENED: gRPC` with WASM correctly silent · check-(2) one-arm counts **5** (long) / **1** (short), union **6** (a one-arm strip is NOT an NC for the union).
- Every leak axis at this tip: `-family row` **95 / 67** · `gRPC-family row` **2** · `Operational-tooling-family row` **3**.
- **ROW 89 IS WELL-FORMED: `fields=8`, with row 88 as the control also reading `fields=8`.**
- ⚠️ do NOT "fix" the six and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases; the candidate sentences are a WINDOW onto a larger deferred backlog, not an inventory of it (`reference_sentinel_matcher_string_self_clears`).
- ⚠️ **Re-run all three checks and both doctoring NCs yourself and record what YOU see.**

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (ugrep under the hood). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Reproduced at TWENTY-EIGHT separate tips.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **THE METHOD BINDS; carry no standing discrepancy figure and no standing absolute.** ⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS; print FILE NAMES, never a count.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files; use `-v --no-index`, and `git status` is the discriminating check). ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **`grep -c` ON ZERO MATCHES IS TOOL-SPECIFIC** (only `git grep -c` prints nothing; all exit 1 — capture the value, never chain on the exit code). ⚠️ **`grep -c` COUNTS *LINES*, NOT *OCCURRENCES*.** ⚠️ **`\t` IN GNU ERE IS A LITERAL `t` — use `-P`.** ⚠️ **A `git grep` PATHSPEC ENDING IN `/` READS A FAIL-UNSAFE ZERO** (shape 33) — use the shell-glob form. ⚠️ **AN UNANCHORED PANIC-GREP FALSE-FIRES 14 TIMES ON A FULLY-GREEN DIFFERENTIAL LOG** — **the canonical gate is ANCHORED: `^panic:|DATA RACE|SIGSEGV`. A gate that fires on a green log is worse than no gate.** ⚠️ **NEW AT THE 89 SPEC — A `\b`-ANCHORED FORM AND A BARE-SUBSTRING FORM DISAGREED AND BOTH READINGS WERE WRONG IN TURN:** `\bSetRequest\b` read **0** test files, the substring form read **4**, and all four are **`SetRequestCtx`** — a different symbol on a different type. **Print the MATCHED FORMS (`grep -o 'Sym[A-Za-z]*' | sort -u`) and carry a LIVE positive control** (`SetAction` = 2) — `reference_symbol_assertion_needs_qualified_name`. ⚠️ **AND `STATE.md` QUOTES ITS OWN MATCHERS:** an unanchored `grep -c 'next-skill:'` reads **2** on the UNMODIFIED file (line 7 is the ADR-0288 rule quoting the token); the anchored `^- \*\*next-skill:\*\*` reads **1**. Same trap as the lineage bullets.

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`** with FOUR blind spots (an em-dash, a DOT in `http-1.1`, two LETTER-suffixed ids). ⚠️ **EXECUTED on scratch copies at the phase-77 PLAN: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** ⚠️ **DO NOT restore the regex form.** ⚠️ **NOTE THE POSTURE: with row 89 `in-progress`, check (1) PRINTS in the healthy case, so it has its own liveness — but that is TEMPORARY. The moment row 89 flips `done` the board is all-done and check (1) goes SILENT again, at which point the row-62 NC is once more the ONLY thing distinguishing a green board from a broken check.**

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

⚠️ **Run the union form, never one half of it.** ⚠️ **A ONE-ARM STRIP IS NOT AN NC** (6 -> 5 short / 6 -> 1 long; never 6 -> 0). ⚠️ **The documented trap's stated MECHANISM is refuted while the trap is real** — the inflation comes from ROADMAP table rows carrying BRAINSTORM prose. **Cite the command; do not re-inherit the explanation.** ⚠️ **The line numbers drift with edits — re-derive them; the current six are `:199 :205 :211 :221 :227 :235`.** All six are standalone family PROSE paragraphs (not rows).

⚠️ **THE INVENTORY IS ~59, NOT ~42.** The long-carried "~42" is the **SENTENCE-VISIBLE** subset only: 43 items by name-level count inside the six live `candidates:` sentences (42 if you collapse `/runtime` + `POST /runtime_modify` into the single clause the sentence writes). **Window `:221` (Observability) names SIXTEEN FURTHER un-chartered items OUTSIDE its live sentence.** **Cite ~42 only as "sentence-visible", never as the inventory.**

⚠️ **AND TWO WINDOW CANDIDATES ARE STALE — THE WINDOWS DO NOT SELF-CLEAN.** Window `:221` still lists (a) *"a pre-existing H2 `//`-path routing bug — `h2/stream.go:440` uses `url.Parse`"*, which **ROW 87 CLOSED** — **re-verified at the 89 SPEC: `internal/filter/hcm/h2/stream.go:478` now calls `url.ParseRequestURI`** — and (b) the **`/stats/prometheus` projection-completeness gap**, which **ROWS 79 AND 80 CONSUMED**. **A candidate's presence in a window is NOT evidence it is still open — re-derive at your tip before costing it.** Neither was edited (a window edit is sentinel-affecting).

⚠️ **THE FOUR PHASE-88 DEFERRED CANDIDATES live in ADR-0310 §Consequences (x)**, NOT in a ROADMAP window — stream-scoped header-overflow parity via a decode-and-discard drain · `max_request_headers_kb` config parity + the `http2.header_overflow` stat · `SETTINGS_MAX_HEADER_LIST_SIZE` advertisement · **decode-side filter mutations never reaching the H2 upstream request — THIS IS ROW 89, NO LONGER DEFERRED.** ⚠️ **ADR-0310's FIFTH carried class — "a split header block on a stream being concurrently RESET" — IS REFUTED** (phase-89 BRAINSTORM §10.3): `checkFrameOrder` in the pinned `x/net@v0.34.0/http2/frame.go:543` makes any non-CONTINUATION frame a connection PROTOCOL_ERROR while a block is open and `AllowIllegalReads` is never set, so the interleaving is UNREACHABLE. **Do not carry it forward as "unprobed".**


==============================================


## STATUS AS OF 2026-08-16: PHASE 89 (`h2-decode-filter-mutations`) **OPEN AT LIFECYCLE-STATE 2** — row 89 `in-progress` at `ROADMAP.md:151`, `want` **121**, base `c1284a03`. **THIS SESSION WRITES THE PLAN.**

**What the 89 SPEC landed (2026-08-16, base `c1284a03`, branch `phase-89-spec`, ONE squashed commit).** Docs-only: `phases/89-h2-decode-filter-mutations/SPEC.md` (new), `PROGRESS.md` (`numstat 65 0`), `DECISIONS.md` (**ADR-0311 §Context**, `numstat 20 0`, 18208 -> 18228), `STATE.md` (rolled IN PLACE, `numstat 10 10`, still 64 lines), `STATE_HISTORY.md` (`numstat 2 0`, 500 -> 502), `next-prompt.txt`. **ZERO production `.go`, ZERO test `.go`. `ROADMAP.md` and `BEHAVIOR_CONTRACT.md` BYTE-UNTOUCHED** (EMPTY diff against master) — the contract edit lands at the IMPL. **Strict `^> **STATUS: PROPOSED` guard RE-ARMED 0 -> 1; the IMPL disarms it.**

### ⚠️ THE HEADLINE: THE BRAINSTORM'S TWO-CONTAINER MODEL IS INCOMPLETE, AND THE MISSING PIECE SELECTED THE FIX

There is a **THIRD WRITER**. The phase-46.1a tracing seam in `h2dispatch.go` writes `x-request-id` / `traceparent` / `tracestate` / `X-B3-*` onto the ordered **carrier only**, never onto the decode map (`awk 'NR>=422 && NR<=456' internal/filter/hcm/h2dispatch.go | grep -c 'c\.req'` = **0**; the block's own `view` is a third, freshly-allocated map). **Any whole-map projection DELETES them from the upstream request.** Measured three independent ways: an OTLP wire probe across three binaries; a **4-of-4** `TestWriteH2_Tracing*` flip with a green baseline and the `=== RUN` denominator asserted at **4**; and the grep. **This eliminated two of the three candidate shapes, and it falls on an axis the BRAINSTORM had not flagged at all.**

### DECISIONS FROZEN AT THE SPEC — the PLAN implements these, it does not re-open them

| id | decision |
|---|---|
| **D-89-SHAPE** | **DELTA against a pre-decode snapshot.** Verbatim `ReconcileOrderedHeaders` reuse REFUTED (it COMPILES with zero diagnostics and then emits an illegal HEADERS block -> **502 with ZERO backend requests** at a conformant peer). Full rebuild REFUTED (4/4 tracing tests RED). Slice-native REJECTED on **MEASURED blast radius** — **NOT** on ADR-0071. |
| **D-89-SITE** | Keep the existing `SetH2Request`; **RE-ISSUE after `RunDecodeData`**, over a **FRESH** slice. Do NOT move it. |
| **D-89-PSEUDO** | **Skip every `:`-prefixed key.** Do NOT route to the scalar fields. `Host`/`host` left **OPEN for the PLAN**, deliberately. |
| **D-89-PROOF** | **Extend `0004-h2-routing` IN PLACE.** Every count axis +0; `0120` stays UNCONSUMED; `0012` is the H1 control at zero cost. |
| **D-89-DOC** | Close the `, H2 differential coverage.` carve-out **IN PLACE, zero line delta**. The mirror sentence **STAYS — EXACTLY TRUE**. |
| **D-89-BODY** | Decode-side body mutation **OUT of charter — no reachable defect**. |
| **D-89-STAT** | No new stat. DELTA **0**. |

### ⚠️ ELEVEN FINDINGS THE PLAN MUST NOT RE-LEARN

1. ⚠️ **A LANDED CODE COMMENT ACQUIRED ADR AUTHORITY ACROSS ELEVEN CITATIONS.** `internal/filter/http/types.go` says *"per ADR-0071's filter API stability: EncodeHeaders takes http.Header, not OrderedHeaders"*. **ADR-0071's body contains ZERO occurrences of `http.Header`, `OrderedHeaders`, `signature` or `stability`** — measured with `DecodeHeaders` = **1** as the live positive control (the ADR spans `DECISIONS.md:2586..2634`). **Write "rejected on measured blast radius (33 production + 38 test files, ~101 mutation call sites, and an `OrderedHeaders` type carrying only `Get`/`ToHTTPHeader` so the mutation API must be written from scratch)". NEVER write "per ADR-0071".** Recorded as a documentary defect, deliberately NOT fixed.
2. ⚠️ **THE VALUE COPY ALIASES ITS BACKING ARRAY, SO THE NAIVE REMOVE CORRUPTS RATHER THAN NO-OPS.** Measured against the REAL types through the router's own action closure: a post-Set **append** is invisible upstream; a post-Set **in-place value edit PROPAGATES**; a post-Set **remove via `fields[:0]`** leaves the snapshot reading **`[a, c, c]`** — removal not applied AND a field DUPLICATED on the wire. ⚠️ **That idiom is the one `upsertH2Header` uses.** Build a FRESH slice; re-issue the Set.
3. ⚠️ **PSEUDO-HEADER PROJECTION IS TOTAL, NOT AN EDGE CASE.** `h2dispatch` injects `:method`/`:authority`/`:path` into the decode map **unconditionally on EVERY request**, and the upstream block is built as four scalars followed by the carrier — so a projecting reconciler duplicates pseudo-headers after the regular headers on **every** H2 request. Measured: `RST_STREAM PROTOCOL_ERROR` at a conformant peer, with a clean 200 control, and again with a single stray `:path`. **The fix does not function without the skip.**
4. ⚠️ **THE EARLY-EXIT LIST WAS 2 OF 3** — the `RunDecodeData` error return was missed. None of the three issues an upstream request, so none needs a reconcile, but **the re-issue must sit AFTER `RunDecodeData`**. `recover()` appears NOWHERE in `internal/filter/hcm/`. The `RunDecodeHeaders`-error exit is **CODE-READ, NOT EXECUTED** — the PLAN owes a unit arm.
5. ⚠️ **`RunAction`'s H2 ARM HAS NO GUARD** while the H1 arm panics on a missing `SetRequest` — it comments *"h2Req is a value type so the zero value is allowed."* **This is why the Set is re-issued, not moved.**
6. ⚠️ **A RAW-FRAMER BACKEND IS MANDATORY.** A `net/http`+`h2c` backend normalizes case, hides pseudo-headers and destroys order — it shows GREEN for an illegal wire encoding. And **assert the backend RECEIVED something (n>0) BEFORE interpreting what it received.**
7. ⚠️ **`codec_type: AUTO` + `--allow-h2c` DOES NOT GIVE PLAINTEXT H2** — the BRAINSTORM's own probe recipe is WRONG, and two agents hit it independently. `AUTO` on a plaintext listener serves H1 (`curl: Remote peer returned unexpected data while we expected SETTINGS frame`); only the `HTTP2` arm consults `AllowH2C` (`internal/filter/hcm/config.go:239`). **Use `codec_type: HTTP2` + `--allow-h2c`.** ⚠️ **AND the differential harness passes NO `--allow-h2c`** (`test/differential/harness.go:252` and `:606`), so a downstream-H2 FIXTURE must use TLS+ALPN, not h2c.
8. ⚠️ **`hcm: h2: EOF` APPEARS ON THE TIP BINARY TOO.** Benign pooled-conn teardown; **not evidence of anything.** One agent nearly mis-attributed it.
9. ⚠️ **DOWNSTREAM H2 FORCES UPSTREAM H2, AND A FIXTURE HAS EXACTLY ONE BackendKind** — together these made "reuse `0012`'s header-echoing backend" **structurally impossible**. ⚠️ **Upstream H2 does NOT need TLS** (`H2HoldResponder`/`H2GoawayResponder` are in-process **h2c prior-knowledge** responders); only the DOWNSTREAM leg does.
10. ⚠️ **A YAML-ONLY FIXTURE SCAN IS BLIND TO DRIVER-INLINE FIXTURES.** `0079-h2-multiplex-pool`, `0080-h2-goaway-rotation` and `0119-grpc-unary-trailers` configure themselves from Go and have **no `envoy-go.yaml`**. The controller's own first Q4 probe was blind and was corrected by its second. **Search the drivers too.**
11. ⚠️ **`0004` CONFIGURES NO TRACING**, so **no fixture arm can catch finding-1-of-the-headline's regression** — it must be pinned by a **UNIT** test in `internal/filter/hcm/tracing_zipkin_dispatch_test.go`, the only test file referencing `upsertH2Header`.

### ⚠️ COST — MEASURED, AND STILL A FLOOR

**+162 / -0 net production `.go`, ONE file (`internal/filter/hcm/h2dispatch.go`), ZERO test files** — controller-re-measured independently of the agent that produced it. Composition counted: **69 comment lines (42.6%)**, 5 blank, 24 brace/paren-only, **64 substantive executable lines**. ⚠️ **1.8-3.2x over the BRAINSTORM's ~+50-90.** ⚠️ **STILL A FLOOR** (`reference_measured_prototype_is_a_lower_bound`, which fired on BOTH axes at the phase-88 IMPL) — the un-implemented remainder, ENUMERATED: outbound RFC 9113 §8.2.2 re-validation (⚠️ **MEASURED leaking `connection` and `transfer-encoding` on the sibling shape, and REACHABLE through `header_mutation` because neither name is in the protected set**) · the `Host`/`host` decision · duplicate-name collapse · four unit files · the differential arms · the unexercised `RunDecodeHeaders`-error exit. ⚠️ **THE VALIDATION ITEM IS THE PHASE-88 SHAPE REPEATING: the hazard does NOT exist at the tip because nothing reaches upstream today — THIS ROW'S FIX INTRODUCES IT.**

**IMPL bands:** production **~+165-230** · unit **~+150-350** · differential **~+145-200** (anchored on phase 88's **MEASURED** `+172/-7` into the SAME fixture — `git show --numstat 4eab3f72 -- test/fixtures/0004-h2-routing/`).

### ⚠️ COUNTS AT THIS TIP — RE-DERIVE MECHANICALLY, NEVER COPY

- **`ROADMAP.md` 239 lines / 121 data rows**; row 89 `in-progress` at `:151` (**`fields=8`, well-formed**), all others `done`; check-(2) anchors `:199 :205 :211 :221 :227 :235`; `-family row` **95 occurrences / 67 LINES**; `gRPC-family row` **2**; `Operational-tooling-family row` **3**; ARM-A flags **{119, 131}** (escape-aware command ONLY).
- **`DECISIONS.md` 18228** lines · **310** headings · tail **ADR-0311 (§Context only, PROPOSED)** · next-free **ADR-0312** (TAIL-derived; `grep -c '^## ADR-0312'` => **0**; headings+1 COLLIDES at the ADR-0209 gap) · **strict `^> \*\*STATUS: PROPOSED` guard 1 — ARMED; the PLAN KEEPS IT ARMED and the IMPL disarms it** · STATUS census **24** · `^---$` **216**. ⚠️ **A NEW ADR TAKES NO `---` SEPARATOR.** ⚠️ **CARRY NO WHOLE-FILE COUNT OF THE LOOSE `PROPOSED` MATCHER** — only the strict form is the guard; every completed ADR's own STATUS line narrates the word.
- **`BEHAVIOR_CONTRACT.md` 5960** — ⚠️ **BYTE-UNTOUCHED by this stage.** The IMPL's edit is an **IN-PLACE** deletion of the substring `, H2 differential coverage.` from ONE markdown table row ⇒ **ZERO line delta**, so no by-line citation shifts. ⚠️ **If the IMPL instead ADDS a bullet, that IS line-adding** — phase 88 shifted these anchors +2 and phase 87 +1. **CITE BY STRING OR SYMBOL.**
- **`STATE.md` 64** · **`STATE_HISTORY.md` 502** (archive labels **200**) · **`BOOTSTRAP_PROMPT.md` 522** (repo ROOT) · phase dirs **130** · `REVIEW.md` **37** (standing departure, named not claimed).
- **differential fixtures 121** at **`test/fixtures/`** (⚠️ **NOT `test/differential/fixtures/`, a plausible wrong path that returns a SILENT `0`**); numeric tail `0119-grpc-unary-trailers`, next-free **`0120` STAYS UNCONSUMED** under D-89-PROOF. · **fuzzers 55 / 48 files.** · **BackendKind tail 38** (`H2GoawayResponder`, `fixture.go:614`). · **fixture blank imports 121** — the correct gate is the NARROWED form `grep -cP '^\t_ "[^"]*test/fixtures/'` => **121**; ⚠️ **the unnarrowed `grep -cP '^\t_ "'` READS 123 AND IS REFUTED** (two non-fixture imports at `runner_test.go:157`/`:168`). · **fixture ports are family-banded `10<index>`, NEVER max+1** (measured on `0119->10119`, `0118->10118`, `0117->10117`, `0012->10012`; outliers `0004->15004`, `0111->10447`). · **fixture `codec_type` census: HTTP1 270 · AUTO 6 · HTTP3 3 · HTTP2 ZERO** — ⚠️ **but `codec_type: HTTP2` DOES exist repo-wide** (`test/conformance/h2spec/h2spec_test.go:66`, `cmd/envoy-go/main_test.go:714`); the zero is scoped to `test/fixtures/`. ⚠️ **THREE of the six AUTO hits are inside DRIVER Go files.**
- **stat surface: the COMMAND binds, not the number.** `grep -ro --include='*.go' -e 'NewCounter(' -e 'NewGauge(' .` => **406** occurrences (`NewCounter(` 327 + `NewGauge(` 79); `-rn` => **404** lines. **Measured 406 on the DELTA prototype too ⇒ DELTA 0.** Assert the DELTA, and if you cite an absolute, cite the command with it.
- ⚠️ **STILL CONTESTED, NO NUMBER CARRIED:** the `STATE_HISTORY.md` archive-gap total · the loose `PROPOSED` matcher's whole-file count · the stat-surface DOC-side absolute (1205 vs 1207) · the zero-test `generic/*` suite count (13 vs 14).

### ⚠️ NOT MEASURED AT THE SPEC — do not treat any of these as settled

- **Whether reference Envoy preserves inbound request header ORDER on the upstream leg.** The controller's probe failed on container networking (see method note 12). ⚠️ **Not load-bearing** — D-89-SHAPE preserves order by construction (measured: every survivor's relative position identical between tip and prototype). It matters only against the REJECTED full-rebuild shape, which loses **7 of 10** positions and is **86% nondeterministic without a sort** (172 of 200 runs differ).
- Whether the reference applies decode-side mutations over downstream **H2 with a Lua filter** (the `header_mutation` and H1/H2 reference arms WERE measured).
- `Host`/`host` projection semantics · duplicate-name collapse against the reference · the `wasm`/`lua`/`extproc` guest-boundary cost of the slice-native shape.

### ⚠️ DOCUMENTARY DEFECTS — carried forward, still deliberately NOT fixed

**NEW AT THIS STAGE:** ⚠️ **`internal/filter/http/types.go`'s "per ADR-0071's filter API stability" comment is FALSE** (echoed across six `PROGRESS.md`, three `REVIEW.md`, one fixture README and one `h2dispatch.go` site) · ⚠️ **envoy-go's `header_mutation` rejects `remove` of a protected header where the reference config-ACCEPTS it** and applies it (503 `missing required header: :path`) — a real, measured, pre-existing divergence, NOT chartered · ⚠️ **`RunAction`'s H2 arm has no `SetH2Request`-was-called guard** while the H1 arm panics.
**CARRIED:** `ADR-0051` §2's false gate-scope sentence · `ADR-0058` cites `routerActionH2.doH2` at a dead location · **`ROADMAP.md` rows 119/131 malformed** (ARM-A GUARD) · **`STATE.md` §Project counts FROZEN at phase 76** (they self-contradict §Current — do not anchor on them) · `harness_test.go` port inventory STALE · `body.go` nolint INERT · xDS cycle guard NOT AUTOMATED · `wasm/doc.go:219` two errors · ROADMAP cites `esalaine` FIVE times · `rbac.go:50` token `F2` · root `PROGRESS.md` stray phase-32.1 doc · SPEC-86/PLAN-86 cite the grpcclient Close pins at a nonexistent `internal/xds/xdsgrpc/...` path · `BEHAVIOR_CONTRACT.md`'s "no stdlib net/http parsing" sentence is FALSE for the 05.1 -> 87 window (RETAINED, ridden by ADR-0309) · `ADR-0057` says fixture `0004`'s driver issues "27 sequential H/2 round-trips per side" — it now issues 31 (⚠️ **`README.md:34`'s "27" is CORRECT for the pre-phase-87 prefix — do NOT "fix" it**) · `BEHAVIOR_CONTRACT.md`'s TWO riders cite the ADR-0052 mandate at a DRIFTED `:1821` · window `:221` carries TWO CLOSED candidates.

### ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + BOTH DOCTORING NCs YOURSELF** and record ACTUAL output. **A PLAN edits no ROADMAP — the binding proof is an EMPTY `ROADMAP.md` diff against master.**
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** The 89 SPEC refuted an ADR-authority claim, a test-coverage claim, its own first Q4 probe, the BRAINSTORM's two-container model, the BRAINSTORM's early-exit enumeration, the BRAINSTORM's probe recipe, and the BRAINSTORM's cost estimate. Do the same.
3. **LINE ANCHORS DRIFT; SYMBOL ANCHORS DO NOT — RE-VERIFY EVERY CITE AT YOUR TIP.**
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Still live: the **SDS dial-budget class** (`internal/boot/boot_sds_e2e_test.go:551`) · the **driver-owned receiver port race** (`0081-grpc-access-log` `0.0.0.0:35097`, `0087-tracing-otlp` `0.0.0.0:35323`; **it ABORTS the binary mid-suite and the harness wrapper still exits 0**) · `internal/cluster` `-race` outlier · `internal/httpclient` zero-value · the two 84.2-era flakes · `TestServerConn_TinyWindowDelivery` · the REFERENCE h2spec section-8 flip. **Capture `-v` output BEFORE re-running anything.**
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only). ⚠️ **A sibling container appeared mid-run at the 89 SPEC and was RECORDED, NOT TORN DOWN.** ZERO containers were running at that stage's start.
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`.
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL** (⚠️ **QUALIFIED**), **and ASSERT THE BOUND BINARY with `ss -ltnp` before reading any probe result.** **A harness's exit code is not the command's**; **`gofmt -l` never exits non-zero — gate on OUTPUT.**
8. ⚠️ **`pgrep -f` / `pkill -f` MATCHES YOUR OWN SHELL AND KILLS THE TOOL CALL (exit 144)**, and ⚠️ **a `pgrep -f 'a\|b'` ALTERNATION MATCHES NOTHING SILENTLY** — kill by PID from `pgrep -laf` with the `bash -c` line filtered out, ONE pattern at a time, and verify ports released with `ss -ltn`.
9. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-89-*`, `wt-89-*` or stage worktree should outlive its stage close** — expect only `master` plus your own (verify with `git worktree list`).
10. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** (a)/(b) differential 121 fixtures, full suite with `INNER_EXIT`; (c) h2spec cite only from your own run — ⚠️ **and note the phase-89 finding: the h2spec harness configures `envoy.filters.http.router` ALONE with `direct_response` routes and never goes upstream, so it is STRUCTURALLY incapable of anchoring this row**; grpc-conformance deferred in writing; proxy-wasm 10/16; (d) fuzzers 55/48; (e) `INNER_EXIT` on every differential launch AND `go test ./...`; (f) no REVIEW.md — standing departure.
11. ⚠️ **THE ARCHIVE-ABSENCE GUARD: USE THE OWN-LABEL FORM, AND NC IT.** The whole-file `grep -cF` form is FAIL-UNSAFE (an evicted label can ALREADY appear in a previous bullet's PROSE). The working form: `grep -oP '`[^`]+`(?= \(\d{4}-\d{2}-\d{2}\))'` — it read **199** labels before this row's append and **200** after. **NC it with a label that IS present** (done here: the positive control fired on `phase 87 … SPEC done`). ⚠️ **And census `STATE.md`'s lineage bullets LINE-ANCHORED** — the file quotes its own matchers.
12. ⚠️ **NEW: `--network host` DID NOT SHARE THE HOST NETNS IN THIS ENVIRONMENT.** The reference bound its ports inside its own namespace (visible in the container's `/proc/net/tcp`, ABSENT from the host's `ss -ltn`), and `-p` publishing was unreachable too. **Use `--add-host=host.docker.internal:host-gateway` and dial the HOSTNAME** — sibling agents reached the reference fine that way. ⚠️ Also: a **STRICT_DNS** reference cluster whose name does not resolve **blocks listener init entirely** — Envoy logs "starting main dispatch loop" while NO listener is bound. Use STATIC, or put **`dns_lookup_family: V4_ONLY`** on any cluster dialing `host.docker.internal`.

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 89 (h2-decode-filter-mutations)' --format='%h %s'   # 2 at this tip: BRAINSTORM, SPEC
git log --grep '^phase 88 (h2-continuation-frames)' --format='%h %s'       # 6: a CLOSED phase, use as a FIRING CONTROL
```
⚠️ **The count comments are LIVE FIGURES — VERIFY before relying.** Take the NEWEST line (`| head -1`).

Execution style: **subagent-driven per `feedback_execution_style`**. Subagents commit LOCALLY on the ONE stage branch with EXPLICIT PATHSPECS; the controller squashes (`feedback_subagents_no_push`, `feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** and tear down docker **BY NAME**. ⚠️ **Band private ports outside BOTH `20000-31007` AND `11000-14999`, clear of the static fixture ports (10000-19172); the driver-owned receiver race has been observed at `35097` and `35323`, and `0081` also binds `0.0.0.0:42039`.** The 87 stages used 47410-47441, the 88 stages 47450-47559, the 89 BRAINSTORM 47560-47599, the 89 SPEC 47600-47719; band yours from **47800+**. ⚠️ **ONE WORKTREE PER AGENT preferred; explicit-path staging if shared; never an unscoped `git restore`.** Per-task `gofmt` + `golangci-lint` on touched packages (⚠️ **misspell runs in locale US and has fired UNFORCED — sweep British spellings in `.go` comments BEFORE the lint gate**). ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch.** ⚠️ **Docker BY NAME, `--rm`, verify gone.**

⚠️ **CONTAINER→HOST REACHABILITY — READ METHOD NOTE 12 FIRST.** `172.17.0.1` is the BRIDGE IPAM GATEWAY and is documented NOT to work here. ⚠️ **AND envoy-go REJECTS `match.headers` outright** — `route 0: match.headers is not supported in phase 04` — so a header-match route works on the REFERENCE side only. ⚠️ **AND `clusters: []` is BOOT-REJECTED even when every route is a `direct_response`** (`cluster manager: zero clusters in bootstrap`) — a dummy cluster is required.

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root — **observed repeatedly at every phase-88 AND phase-89 stage, and the harness now ANNOUNCES it** (`Shell cwd was reset to /home/esa/git/envoy-go`). Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run. ⚠️ **Shell VARIABLES do not survive between Bash calls** — re-export `$W`/`$SC` in every command.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — `git status --porcelain` AND `git diff --stat master` must show ONLY your intended edits. For a patched TRACKED file: `sha256sum` BEFORE patching, `git checkout -- <path>` after, `sha256sum -c` the capture. ⚠️ **At the 89 SPEC every probe agent ended with EMPTY `status --porcelain` and `diff --stat`, and the controller's own probe worktree was deleted with proof.**

⚠️ **THE ARCHIVE-ABSENCE GUARD — SEE STANDING METHOD NOTE 11.** **Verify STRICTLY APPEND-ONLY**: `git diff --numstat` = `N 0` AND base a byte-exact PREFIX (`cmp`). At your close §Recent evicts the oldest per a DIRECT DATE READ at that tip. ✅ **CONFIRMED AT THE 89 SPEC CLOSE:** §Recent is capped at **5** bullets; this stage faced a **TWO-WAY TIE** at 2026-08-13 — ⚠️ **NOT the THREE-way tie the 89 BRAINSTORM faced and NOT the FOUR-way the 88 IMPL faced: THE TIE SHAPE DOES NOT CARRY** — resolved by **LIST POSITION (the LAST entry)**, evicting `phase 87 (h2-double-slash-path-routing) IMPL done`. ⚠️ **Read all five dates DIRECTLY at YOUR tip before removing anything.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads the phase-89 PLAN.** §Project counts SELF-CONTRADICT §Current — do not anchor on them (frozen at phase 76).
2. `docs/envoy-go/phases/89-h2-decode-filter-mutations/SPEC.md` — **§3 is D-89-SHAPE with all three prototypes; §4 is D-89-SITE and the aliasing table; §5 is D-89-PSEUDO; §6 is D-89-PROOF with the four priced options; §13.2 is the MEASURED cost and its enumerated remainder; §14 is the eleven findings; §15 is what is explicitly NOT MEASURED; §17 is the nine deliverables this PLAN owes.**
3. `docs/envoy-go/phases/89-h2-decode-filter-mutations/PROGRESS.md` — the SPEC entry: the frozen decisions, the sentinel output, the four controller-side traps.
4. `docs/envoy-go/DECISIONS.md` **ADR-0311** (tail, §Context only, STATUS **PROPOSED**) — six paragraphs; ⚠️ **the PLAN keeps the guard ARMED and adds NO ADR.**
5. `BOOTSTRAP_PROMPT.md` §4-§7 (repo ROOT) — the lifecycle contract and the six-gate posture.

## What the PLAN owes
`docs/envoy-go/phases/89-h2-decode-filter-mutations/PLAN.md` delivering **SPEC §17's nine items** · **TDD task decomposition** with the RED census observed BEFORE the production edit and **the tracing pin IN the RED set** · the **slice-only-writer inventory** as a NAMED deliverable · the **outbound RFC 9113 §8.2.2 validation decision** (⚠️ **a hazard the fix INTRODUCES**) · the **`Host`/`host` decision** · the **`0004` arm roster** with a break protocol per arm and the **injection site NAMED** (`reference_break_arm_injection_site_is_a_claim`) · the **unit roster** including the currently-ZERO `SetH2Request` coverage · a **duplicate-name-collapse reference measurement or an explicit written deferral** · the contract edit's **exact substring and its zero line delta** · **cost re-measured at the PUBLISHING commit** · `PROGRESS.md` appended · `STATE.md` rolled IN PLACE (lifecycle-state **2 -> 3**) with the oldest §Recent entry evicted by a DIRECT DATE READ · `next-prompt.txt` rolled (`git add -f`) · ONE squashed commit, merged and pushed. ⚠️ **A PLAN MOVES `want` ZERO, EDITS NO ROADMAP ROW, ADDS NO ADR AND KEEPS THE `PROPOSED` GUARD ARMED AT 1 — prove the first with an EMPTY `ROADMAP.md` diff, and still run the sentinel + both NCs and record ACTUAL output.**
