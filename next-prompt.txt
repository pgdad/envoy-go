START a fresh session for envoy-go (router last updated 2026-06-25; the 43.2b IMPL-done squash commit is `676e17cf` — the master tip, pushed to origin; the Upstream-robustness family (phases 39–43) CLOSED). **The next action is a fresh BRAINSTORM for the next SUBJECT** — invoke `superpowers:brainstorming` to select the next phase/family from the 5-family candidate roster below, then proceed SPEC → PLAN → IMPL (each stage SUBAGENT-DRIVEN, `feedback_execution_style`). **Git hygiene:** all phase work runs in a FRESH git worktree off master (`feedback_git_worktrees`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the controller squashes + pushes at stage-close (`feedback_push_to_origin`); each code task runs `gofmt -l` + `golangci-lint` on touched pkgs (`feedback_pertask_gofmt_lint`). There is NO pre-chartered next row — the roadmap lists FAMILIES, not pre-populated rows; the BRAINSTORM's Q0 picks the subject (the 38.2→39 / 40→41 family-boundary precedent: family-close ⇒ fresh BRAINSTORM).

First confirm the state via `git log --oneline -3` (expect the HEAD to be the 43.2b squash `phase 43.2b (h2-connection-pool) IMPL: graceful GOAWAY-driven H2 upstream connection rotation …` at `676e17cf`, directly atop the prior `next-prompt.txt …` roll; re-read the shas, do NOT assume). Counts at the 43.2b close (master tip): stat surface **1187** (H2 cluster; non-H2 **1183**) / fixtures **82** (tail `0080-h2-goaway-rotation`) / fuzzers **43** (reconciled from documented-42, `reference_fuzzer_count_docs_drift`) / BackendKind tail **38** (`H2GoawayResponder`) / DECISIONS tail **ADR-0254**, next-free **ADR-0255**.

## NEXT STEP — a fresh BRAINSTORM to select the next subject (`superpowers:brainstorming`)

The Upstream-robustness family (39 active-health-check · 40 outlier-detection · 41 retries · 42 retries+hedging · 43 connection-pooling) is COMPLETE — zero candidates remain in it. Per ADR-0106 the next subject opens a NEW family. The roadmap's post-Upstream-robustness candidate roster (5 families, sectional headings, NOT pre-populated rows — the BRAINSTORM Q0 picks one):

1. **HTTP/3 + QUIC** — a quic-go transport, a downstream H3 listener, an upstream H3 cluster, an `h3spec` conformance gate. (Composes with the 43.2 H2-pool work — the per-protocol connection-pool seam already exists; H3 pooling is the natural next pool consumer.)
2. **gRPC** — gRPC bridge, gRPC-Web, gRPC-JSON transcoding, interop/conformance.
3. **xDS / dynamic config** — ADS, delta-xDS, LDS/CDS/RDS/EDS/SDS/RTDS, reconnection, initial-fetch timeout. (The largest structural lift — moves the proxy from static bootstrap to dynamic control-plane-driven config.)
4. **Observability** — gRPC ALS, OTLP access log, OTel/Zipkin/Jaeger/Datadog/XRay tracing, stats sinks, the tap filter.
5. **Runtime + hot restart** — the runtime layer (an RTDS consumer), hot-restart / graceful-drain semantics beyond phase-08's minimum.

The BRAINSTORM should: (Q0) pick the family/subject from this roster (weigh composition with existing primitives, dependency unblocking, and reference-Envoy diff-feasibility — the H2-pool / connection-pool seam makes HTTP/3 the most directly-composing follow-on, but xDS unblocks the most downstream work); (Q1) scope the MVP envelope (parse-accept-vs-reject per field); (Q-split) the ADR-0045 split posture. It ANCHORS the new family's parent BRAINSTORM doc + the §Context of the next ADR (**ADR-0255**).

## Honor throughout (the discipline)

- **Lifecycle = one stage per session.** BRAINSTORM → SPEC → PLAN → IMPL, each subagent-driven, each squashed + pushed at stage-close. `feedback_git_worktrees` (fresh worktree off master) · `feedback_subagents_no_push` · `feedback_push_to_origin` · `feedback_execution_style` · `feedback_pertask_gofmt_lint` · `feedback_subagent_worktree_detach` / `feedback_subagent_worktree_path_targeting` (the worktree hygiene + path-pinning guards — a 43.2b subagent leaked an orphan edit into the MAIN checkout; the controller must verify `git -C <main> status` clean after each task) · `reference_full_suite_race_after_background_mutator` (after any task adds a background goroutine touching shared state, re-run the FULL package `-race`, NOT a `-run` subset).
- **Process anchors:** ADR-0106 (a new family opens; NO parent rollup for a flat row; family-close flips the row done) · ADR-0080 (strict-reject) · ADR-0044 (the ADR §Decision/§Consequences land at IMPL; §Context at SPEC) · ADR-0045 (the split gate, re-checked at PLAN).
- **Differential discipline (when the new subject reaches a fixture):** `reference_docker_probe_bridge_network` (shared bridge + a backend hostname reachable from BOTH containers; verify decode ran) · `reference_differential_run_selector` (`-run 'TestDifferential/<NNNN>'`, NOT a bare `'<NNNN>'`) · `reference_differential_break_protocol_count1` (`-count=1` on every deliberate-break AND every `-race` run) · `reference_differential_fullsuite_startup_flake` (a `subject ready: EOF` is a transient startup race — isolate-re-run, NOT a regression) · `reference_wire_format_both_sides_see_same_bytes` (adopt the reference framing verbatim) · `reference_fuzzer_count_docs_drift` (the documented running-total drifts — reconcile when a phase adds a fuzzer).

## Read first (in order)

1. THIS FILE — the cold-start router (43.2b IMPL is DONE + LANDED at `676e17cf`, pushed; the Upstream-robustness family CLOSED; the next action is a fresh BRAINSTORM to select the next subject).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 43.2b (h2-connection-pool) IMPL done`, the family-close note) + the count figures (1187 / 82 / 43 / 38 / ADR-0254, next-free ADR-0255).
3. `docs/envoy-go/ROADMAP.md` — the post-Upstream-robustness candidate-family roster (the 5 families above) + the family-selection / no-pre-populated-rows invariant.
4. `docs/envoy-go/BOOTSTRAP_PROMPT.md` — the lifecycle invariants (§4.1: STATE.md is the source of truth for "what next"; a session updates it before exiting).
5. `docs/envoy-go/DECISIONS.md` — the tail **ADR-0254** (the 43.2b as-built — the graceful GOAWAY rotation drain lifecycle + the watcher coverage-boundary note + the two 0080 fixture departures) + next-free **ADR-0255**.
6. `docs/envoy-go/phases/43.2-h2-connection-pool/` — the just-completed leg (BRAINSTORM/SPEC/PLAN/PROGRESS-43.2b) if context on the closed family is needed.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. the worktree-hygiene, differential, and the new `reference_full_suite_race_after_background_mutator` refs).
