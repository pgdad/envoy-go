=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-CHECKED 2026-07-14 (after the phase-63 BRAINSTORM landed + row 63 registered `in-progress`, run MECHANICALLY) — it does NOT fire. Do NOT create `stop`.** State:
- (1) prints `NOT DONE: row 63` — row 63 `tracing-custom-tags-environment` registered `in-progress` at the phase-63 BRAINSTORM (per the ROADMAP §Schema invariant; it flips `done` only at the phase-63 IMPL six-gate, the SOLE leg, ADR-0106). ⇒ check (1) prints ⇒ the sentinel does NOT fire.
- (2) prints THREE live "candidates:" sentences — HTTP/3, xDS, and Observability ⇒ three families STAY OPEN. The Observability sentence STILL reads `custom_tags (environment/metadata)` at this BRAINSTORM (it NARROWS to `(metadata)` only at the phase-63 IMPL — `reference_sentinel_deferred_sentence_live_vs_historical`).
- (3) prints `NEVER OPENED: gRPC`, `Runtime`, `WASM` ⇒ THREE families never opened. Operational-tooling also STAYS OPEN.

Any one of the three printing ⇒ the sentinel does NOT fire.

**STATUS AS OF 2026-07-14: the phase-63 BRAINSTORM LANDED and ROADMAP row 63 is `in-progress`.** The roller SELF-PICKED (AUTONOMOUS, no human pick, per the standing directive) the tracing `custom_tags` **`environment`** source arm — the FOURTEENTH Observability-family row, the THIRD `custom_tag` source type after phase-59 `literal` (ADR-0277) + phase-62 `request_header` (ADR-0283), and the immediate follow-on the phase-62 BRAINSTORM named. Rejected alternatives (formalized BRAINSTORM §2.1): `metadata` (needs a dynamic-metadata lookup path envoy-go lacks — the LAST/biggest `custom_tag` source) / `spawn_upstream_span`/`http_service`/force-trace / OTLP-metrics sink / opening a never-opened family (gRPC/Runtime/WASM) — all larger. **The settled design (BRAINSTORM):** lift the one `environment` reject (`internal/tracing/config.go:195-196`); read a named PROCESS ENV VAR (`CustomTag_Environment{name, default_value}`) into a `{tag, value}` STRING span attribute via the LANDED `Span.Attrs []KV`/`upsertAttr` seam (ONE path covers OTLP + Zipkin); proto-authoritative missing semantics IDENTICAL to `request_header` (present → env value; absent + `default_value` → default; absent + empty default → OMIT — the omit logic ALREADY landed at phase 62). **The CENTRAL (and only) design decision (D-ENV-RESOLVE-TIME):** env is process-static → anticipated PARSE-TIME-STATIC resolution (`os.Getenv` at `parseCustomTags`, collapsing to a `kindLiteral` spec / omit; ZERO new kind / ZERO `ResolveCustomTags` change / **ZERO call-site change** — the THREE `accesslog_emit.go` `BuildServerSpan` call sites `:55`/`:116`/`:177` ALREADY thread the phase-62 resolution seam) vs a `kindEnvironment` case; the one nuance is the omit-at-parse first-wins-dedup interaction (D-ENV-DEDUP). `metadata` STAYS parse-rejected loudly (the SOLE remaining `custom_tags` departure, ADR-0080). Per the lifecycle discipline, the NEXT stage is the phase-63 SPEC.

==============================================

## What THIS session does (the phase-63 SPEC)

Perform the SPEC stage (lifecycle-state 1 → 2) for phase 63 (`tracing-custom-tags-environment`), using the BRAINSTORM as the design input. This is a DOCS-ONLY stage in a FRESH worktree off master (`feedback_git_worktrees`); NO production `.go` changes. DISPOSE every D-ENV-* question from BRAINSTORM §10 — the empirical arms via LIVE probes against `envoyproxy/envoy:contrib-v1.37.2` (FRESH container per arm, `reference_probe_fresh_container_per_arm`; a throwaway probe dir reusing the `test/helpers/otlptrace` receiver, deleted after — docs-only). Author `docs/envoy-go/phases/63-tracing-custom-tags-environment/SPEC.md` recording: the disposed D-ENV-* findings (WIRE / MISSING ×3 / RESOLVE-TIME / DEDUP / PRECEDENCE / REJECT / FIXTURE / HARNESS / FUZZSEED / SPLIT), the RE-DERIVED edit-site roster (§6, against the current master tip — `git fetch` first), the design decision (parse-time-static vs `kindEnvironment` — D-ENV-RESOLVE-TIME), the fixture strategy (value-equality vs key-presence — D-ENV-FIXTURE/-HARNESS), and the anticipated counts. Draft **ADR-0284 §Context** (the provenance: the phase-59 `literal` + phase-62 `request_header` slices + ADR-0277/ADR-0283 §Consequences + the ROADMAP deferred sentence). Row 63 STAYS `in-progress` at this SPEC (a row flips `done` only at its IMPL six-gate). Land it (commit + push per `feedback_push_to_origin`) and ROLL THIS FILE to the PLAN stage.

**Honor throughout (the discipline, unchanged):**
- **Lifecycle = one stage per session.** This session: the SPEC ONLY. After it lands: roll to the PLAN.
- **Process anchors:** ADR-0044 (ADR §Decision/§Consequences land at the IMPL — the SPEC drafts only §Context) · ADR-0045 (the split escape-valve, only if the slice exceeds ~15 tasks) · ADR-0106 (a row flips `done` at its IMPL; the SOLE leg here) · `reference_roadmap_split_phase_row_done`.
- **Live probes:** `reference_probe_fresh_container_per_arm` (fresh container per arm) · `reference_envoy_contrib_image_tagging` (the `contrib-`-prefixed tag in the standard `envoyproxy/envoy` repo) · `reference_docker_probe_bridge_network` + `reference_host_gateway_ip_docker_desktop` (OTLP span-receiver reachability; verify the decode ACTUALLY ran, not a vacuous empty capture).
- **`feedback_brief_citations_not_evidence`** — RE-DERIVE every `file:line` in the SPEC's edit-site roster from source against the CURRENT master tip (the BRAINSTORM's citations were derived against `d14caa96`; re-verify). Pin the PGV line for `CustomTag_Environment.name` `min_len:1` against the PROJECT-pinned go-control-plane version (the BRAINSTORM confirmed the shape at v1.37.0 — the project resolves `/envoy v1.32.4`, re-derive there).
- **`reference_spec_drafted_identifier_collision_check`** — IF the SPEC proposes a new symbol (`kindEnvironment`, an `EnvName` field), GREP `internal/tracing` for a collision (the phase-62 precedent caught a `config_test.go` helper collision) BEFORE the PLAN adopts it.
- **`next-prompt.txt` IS TRACKED** (`reference_next_prompt_tracked_despite_gitignore`) — edit it inside the stage worktree and fold into the stage commit; locate commits by SUBJECT (`git log --grep`), never by position.
- **Worktree discipline** (`feedback_git_worktrees`, `feedback_subagent_worktree_detach`, `feedback_subagent_worktree_path_targeting`); if subagent-driven, `feedback_execution_style` + `feedback_subagents_no_push`.
- Read `docs/envoy-go/DECISIONS.md` ADR-0277 (phase-59 literal) + **ADR-0283** (phase-62 request_header) first — the landed `CustomTagSpec`/`ResolveCustomTags`/`upsertAttr`/first-wins-dedup seam this row extends.

**ADR reservation ledger (provisional; each finalized at its IMPL in landing order):** ADR-0277 = phase-59 tracing-custom-tags-literal (LANDED) · ADR-0278 = xDS 60.1 (LANDED) · ADR-0279 = HTTP/3 61.1 (LANDED) · ADR-0280 = xDS 60.2 (LANDED) · ADR-0281 = HTTP/3 61.2 (LANDED) · ADR-0282 = HTTP/3 61.3 (LANDED) · ADR-0283 = phase-62 tracing-custom-tags-request-header (LANDED) · **ADR-0284 = phase-63 tracing-custom-tags-environment** (RESERVED — §Context drafts at this SPEC; §Decision/§Consequences at the phase-63 IMPL) · next-free unreserved **ADR-0285**.

**Current counts (VERIFIED at the phase-63 BRAINSTORM against master tip `d14caa96` — RE-VERIFY at session start, `git fetch` first):** fixtures **107** (`ls -d test/fixtures/[0-9]*/ | wc -l`; tail `0105-tracing-custom-tags-request-header`) · fuzzers **55** · DECISIONS tail **ADR-0283** (next-free **ADR-0284**) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** · go.mod modules **2** (`quic-go` direct + `qpack` indirect). A SPEC is DOCS-ONLY ⇒ ALL counts UNCHANGED at that stage. **Anticipated at the phase-63 IMPL:** fixtures **107 → 108** (`0106-tracing-custom-tags-environment`) · fuzzers **55 (+0)** · stat **1201 (+0)** · DECISIONS tail **ADR-0284** · new packages **0** · new modules **0**; row 63 → `done`.

**Locating the phase-63 BRAINSTORM stage squash** (a router cannot name its own sha — identify by SUBJECT, never by position):
```sh
git log --oneline --grep='^phase 63 (tracing-custom-tags-environment) BRAINSTORM' -1
```

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-63 SPEC is next; row 63 is `in-progress`).
2. `docs/envoy-go/STATE.md` — the active-phase header (phase 63 BRAINSTORM done; NEXT = the phase-63 SPEC).
3. `docs/envoy-go/phases/63-tracing-custom-tags-environment/BRAINSTORM.md` — the settled design + the D-ENV-* question list this SPEC disposes.
4. `docs/envoy-go/phases/62-tracing-custom-tags-request-header/{BRAINSTORM,SPEC,PLAN}.md` + `docs/envoy-go/phases/59-tracing-custom-tags-literal/` + `docs/envoy-go/DECISIONS.md` ADR-0277/ADR-0283 — the LANDED custom_tag engine (`CustomTagSpec`/`ResolveCustomTags`/`upsertAttr`/first-wins-dedup), the seam this `environment` row extends; the phase-62 SPEC's live-probe methodology is the template.
5. `docs/envoy-go/ROADMAP.md` — the Observability family (row 63 + its narrowed-at-IMPL deferred sentence), the xDS + HTTP/3 live deferred sentences, and the never-opened gRPC/Runtime/WASM + Operational-tooling families.
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the tracing `reference_*` traps (esp. `reference_tracing_custom_tag_override_builtin`, `reference_b3_parent_is_incoming_spanid`, `reference_tracing_upstream_cluster_framework_gap`, `reference_spec_drafted_identifier_collision_check`) + the differential-harness + live-probe traps.
