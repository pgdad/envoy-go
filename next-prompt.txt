=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **PHASE 92 (`h2-response-header-validation`) IS CLOSED. ROW 92 READS `done` AT `ROADMAP.md:154` AND `want` IS 124.** ⇒ **THERE IS NO BANKED MID-LIFECYCLE WORK. THIS SESSION SELF-PICKS A SUBJECT AND RUNS THE PHASE-93 BRAINSTORM (lifecycle-state DONE -> 1).**

⚠️⚠️ **THE SENTINEL IS NOW ONE CHECK AWAY FROM FIRING, AND THAT IS A CHANGE IN THE BOARD'S SHAPE.** Until this stage, TWO checks blocked it. Row 92's flip made check (1) **SILENT**. **CHECK (2) IS NOW THE ONLY THING BLOCKING TERMINATION.** Do NOT treat the sentinel as comfortably far away, and do NOT "tidy" a deferred-candidate line without understanding that deleting the last one ENDS THE PROJECT. ⚠️ **MEASURE ALL THREE YOURSELF BEFORE DOING ANYTHING ELSE; do not take this file's word for it.**

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE. This form parses FIELDS instead of matching a pattern, and
#     asserts the row DENOMINATOR so a deleted row is itself an error.
awk -F'|' -v want=124 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **CHECK (1) IS NOW SILENT — THE ALL-`done` BOARD SHAPE.** That is CORRECT, not breakage. Run BOTH doctoring NCs EVERY SESSION anyway, because a silent check is indistinguishable from a broken one:
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md.
```
⚠️ **NC-A NOW READS **ONE** LINE** (`NOT DONE: row 62`). **The phase-92 two-line shape was the OPEN-ROW board's and DOES NOT CARRY.** ⚠️ **This is the SIXTH distinct NC-A shape in seven stages. It will change again the moment any row opens — re-measure, never inherit.**
```sh
# the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```
Also NC the DENOMINATOR (`want=123` on the real file must print `GATE FAIL: examined 124 data rows, expected 123`) — ⚠️ **POST-flip NC-B is a SINGLE-line result.**

⚠️ **AND WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -oiE '-family row'` fails (`ugrep: error: option -f: cannot read amily row`) and the surrounding arithmetic prints `0`, **which reads exactly like "no change."** **Always pass `--` before the pattern**, and the counts are form-dependent: with `--`, occurrences read **95** and LINES read **67**. **`grep -c` counts LINES, NOT OCCURRENCES.**

⚠️ **THE MALFORMED-ROW FIGURE RECONCILES ONLY UNDER AN ESCAPE-AWARE FIELD COUNT.** Malformed rows are ids **57** (`NF=9`) and **69** (`NF=10`). ⚠️ **A NAIVE `awk -F'|'` `NF != 8` FORM READS *SEVENTEEN***. **Strip `\|` BEFORE splitting.** The two forms **DISAGREE on row 57** (naive 13 vs escape-aware 9) but **AGREE on row 69** (both 10), so **any gate must state WHICH FORM it uses.** ⚠️ **COUNT A ROW'S FIELDS BEFORE INSTALLING ANY EDIT** — an unescaped `|` silently passes the gate.

⚠️ **AND DO NOT SUBSTITUTE A "REASONABLE EQUIVALENT" FOR CHECK (1)'s AWK.** A measurement agent once tried `NF>=8 && $2 ~ /^ *[0-9]+ *$/`: it printed **92**, not the true denominator. **Use the form above verbatim.**

**SENTINEL RUN MECHANICALLY AT THE PHASE-92 IMPL, ON BOTH SIDES OF THE ROW-92 FLIP.** ACTUAL output, recorded not predicted:
- **PRE-FLIP:** (1) `NOT DONE: row 92` ONE line · (2) **SIX** · (3) SILENT. NC-A and NC-B each **TWO** lines.
- **POST-FLIP:** (1) **SILENT** · (2) **SIX** at `:202 :208 :214 :224 :230 :238` · (3) **SILENT**. **NC-A collapses to ONE line, NC-B to ONE line**; NC-C fired (`NEVER OPENED: gRPC`, WASM control still 2); NC-D long **5** / short **1** / union **6**.
⇒ **THE SENTINEL DOES NOT FIRE — CHECK (2) ALONE BLOCKS IT. `stop` WAS EVALUATED AND DELIBERATELY NOT CREATED** (verified absent at the git root and in every stage worktree).
- ⚠️ **LIVE FIGURES RE-DERIVED AT THIS TIP:** `DECISIONS.md` **18646** · `^---$` **216** · `^## ADR-` **313** · bare `^## ` **321** · tail **ADR-0314** · next-free **ADR-0315** · strict `PROPOSED` guard **0 (DISARMED)** · **UNANCHORED `PROPOSED` now reads 2, DOWN from 3** · ADR-0231 decoy **1 @ `:14866`, LEFT ARMED** · `STATE.md` **63** · `STATE_HISTORY.md` **530**, strict **163 (DELTA 0)** / loose **215** · phase dirs **133** · fixtures **121** (tail `0119`, **`0120` FREE**) · fuzzers **56 targets / 48 FILES** · `BEHAVIOR_CONTRACT.md` **5966** · `go.mod` **67** · BackendKind tail **38** (39 declared, 39 FREE) · `-family row` **95 / 67**.
- ⚠️ **THE UNANCHORED `PROPOSED` COUNT WENT *DOWN* THIS STAGE (3 -> 2), AND THAT IS DELIBERATE.** ADR-0312 and ADR-0313 each SPELLED the guard string in prose and thereby became hits for it; **ADR-0314 deliberately does NOT spell it.** **NEVER GATE ON THE UNANCHORED FORM — only the anchored one.**
- ⚠️ do NOT "fix" the six in check (2) and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases. ⚠️ **Cite ~40 only as "sentence-visible", never as the inventory** — and **no gate may rest on a ` + ` split, in any form.**

==============================================

## STATUS AS OF 2026-08-26: PHASE 92 (`h2-response-header-validation`) **CLOSED**. **THIS SESSION SELF-PICKS AND RUNS THE PHASE-93 BRAINSTORM.**

**What the 92 IMPL landed.** Row 92 `in-progress` -> **`done`** (a FLIP, not an ADD; `want` STAYS 124; NF=8 under BOTH forms, counted BEFORE the edit). **ADR-0314 COMPLETED IN PLACE** after the RETAINED italic footer — no renumber, NO `---`, strict guard **DISARMED 1 -> 0 verified BY LINE AND BY ADR**, decoy LEFT ARMED. `BEHAVIOR_CONTRACT.md` **5962 -> 5966**. `PROGRESS.md` created. **Production `+218/-8` across EIGHT files; tests RUN 655 -> 708 (+53), run-set diff 0 REMOVED.**

### ⚠️ WHAT THIS ROW SHIPPED
On the H/2 downstream leg a response whose **UPSTREAM LEADING header block** carries a connection-specific or otherwise malformed field is now **REJECTED with a locally generated 502** instead of being laundered downstream — the ENCODE-direction mirror of `IsIllegalH2RequestHeader`. Divergence set **NINE shapes**; the differential covers **FIVE on the wire**.

### ⚠️ THE FINDINGS THE NEXT STAGE MUST NOT RE-LEARN

1. ⚠️ **A FIX CAN UNMASK A LATENT DEFECT VIA COMPENSATING DEFECTS THAT WERE CANCELLING IN THE GATE METRIC.** The differential's `content-length` arity read **1 vs 1** pre-fix (subject forwarded a 200 with `content-length: 6`; reference answered 502 with `content-length: 87`) and **0 vs 1** post-fix, because `h2LocalReplyHeaders()` emits **no Content-Length at all**. **The metric became a discriminator only once the row's own defect was fixed.** ⇒ **when a gate goes red right after a fix, ask whether the fix REMOVED a cancellation before assuming the fix is wrong.**
2. ⚠️ **A PIN PLACED UPSTREAM OF ITS GATE MASKS THE GATE.** The per-side arity pin was first placed in `DriveReference`/`DriveSubject`; a Drive error is `t.Fatalf`'d at runner step 5/6, **BEFORE `CompareBytes` at step 7**, so with the production guard reverted the run reported ONLY the arity pin and the row's own status/illegal divergence **was never reached**. Moved into `AssertDistribution` (step 8, `t.Errorf`, `errors.Join`). **A do-not-move-it-back note is in the code and the README.**
3. ⚠️ **ADDING AN ASSERTION TO A SHARED FUNCTION SILENTLY VACUATES ITS UNIT TABLE.** The arity pins went into `AssertDistribution` without seeding `driver_test.go`, which made gate (b) RED — **and, far worse, made FIVE of six rows pass on the NEW guard rather than on the branch each exercises.** A bare `err != nil` check cannot tell those apart. **Fixed with `wantErrSubstr` per row and observation slices derived from `len(p92Arms())` rather than a literal.**
4. ⚠️ **`grep -i <name>` IS NOT `go test -run <Name>`.** A run-set "before" built with `grep -i trailer` read **10 names removed** that had not moved at all — `grep -i` matches any substring including a lowercase subtest name, while `-run` matches **per path element, case-sensitively**. **Match the selector SEMANTICS on both sides or the diff is meaningless.**
5. ⚠️ **A DENOMINATOR QUERIED UNDER A NON-EXISTENT NAME READS `0` BEFORE AND `0` AFTER, AND REPORTS "INVARIANT" WHILE MEASURING NOTHING.** The SPEC's roster named `TestMalformedTrailers_SentinelDiscriminates`; the real symbol is `…DiscriminatesPeerReset`. **Assert every named test EXISTS before trusting an invariance over it.**
6. ⚠️ **LINE ANCHORS DRIFT WITHIN A SINGLE ROW.** Both the PLAN's `:528` and recon's `:526` for the AST golden were stale by the time production ran — an earlier task had shifted it **+34**. **Every edit anchored on LITERAL TEXT with a `count == 1` assertion; nothing mis-targeted.**
7. ⚠️ **A PLAN'S OWN NC CAN BE VACUOUS.** The prescribed leg-order NC (move `hasUppercaseHeaderChar` to LAST) is a **behavioral NO-OP** — every sibling leg compares against an all-lowercase literal, so an uppercase name matches none at any position. Two ORDER arms **cannot be reddened by any permutation** and are now annotated in-source as presence-guards. **The PLAN's leg->arm map was also wrong in 3 of its 4 rows.**
8. ⚠️ **A FUZZ ASSERTION MUST NAME THE FIELD, NOT THE `error`.** *"quoted, in trailing position"* holds for `*Error.Msg`, NOT `Error()`, which appends `": " + Underlying.Error()`. **9 of 11 seeds were RED until re-anchored — the target falsified its own author before it saw a mutation.**
9. ⚠️ **CORRECTION TO A STANDING NOTE: THIS `golangci-lint` BUILD *DOES* EXIT NON-ZERO ON FINDINGS** (rc=1, proven with a deliberately broken untracked package). **Gating on OUTPUT stays correct and is strictly safer, but the stated reason was wrong.**
10. ⚠️ **THE proxy-wasm `/16` DENOMINATOR IS NOT REPRODUCIBLE** — the run does not emit it and no `16`-family constant exists in `test/conformance/proxy-wasm/*.go`. **Cite "10 families measured green", never "10/16".**
11. ⚠️ **A CODE-READING OF `x/net/http2/server.go` IS NOT EVIDENCE.** `te: gzip` and `te: ""` were MEASURED to leak onto the wire, narrowing the departure from six unit-only shapes to FOUR.
12. ⚠️ **NC3-style "reject everything" breaks HANG the `h2` package past 10 minutes** (pre-existing tests block on a `RoundTrip` that can no longer succeed). **Run such NCs scoped, with `-timeout`.**

### ⚠️ BANKED CANDIDATES — costs already measured, so a taker re-derives nothing

- ⚠️ **`h2LocalReplyHeaders()` EMITS NO `Content-Length` — the STRONGEST SMALL CANDIDATE, and its reference side is a MEASUREMENT rather than a prediction.** Seven call sites (`retry.go:374` 504; `router_h2.go:80/:128/:138` 503; `:148/:231/:250` 502); the **H1 sibling `localReplyHeaders(bodyLen int)` DOES emit one** and even takes a `bodyLen` the H2 version lacks; dates from **phase 07.1**. Cross-side MEASURED: reference arity **1**, subject **0**, and **already PINNED PER SIDE in `0004`** with both flips NC'd — so a fix has a ready-made gate that will redden when it lands.
- **The pooled-upstream-lifetime defect** — three defects at three sites, reproduced end to end, MORE SEVERE than row 92; deferred because its reference side is a PREDICTION and it is a probable SPLIT phase.
- **The `content-length` rewrite candidate, now FOUR-legged** (a SINGLE `content-length` with a wrong value: reference **200-then-RST_STREAM**, subject silent rewrite — **duplicate-NESS, not value-CORRECTNESS, trips the 502**).
- **1xx interim responses** — ⚠️ the in-code parity target is now CORRECTED: the reference **SWALLOWS** 1xx; the target is **DROP-AND-DELIVER**, which needs only that a 1xx leading block leave `respHeadersSeen` UNSET (strictly cheaper than forwarding).
- **The H1 no-`Host` divergence** (`+90/-0`, one file); **`ssl.connection_error`**, the cheapest in-window candidate.
- **`allocateOTLPPort` probes `127.0.0.1:0` (loopback) while `ensureServer` binds `0.0.0.0` (wildcard)** — different scopes, the exact trap `freeTCPPortBlock`'s own comment says it avoids, plus a wide allocate→bind gap and a hard `panic` instead of a retry. It ABORTS THE TEST BINARY when it fires (86 of 121 subtests ran).
- **Counting the stat surface MECHANICALLY** — a named maintenance deferral; the documentary chain is discontinuous in TWO places.

## ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + ALL FOUR NCs YOURSELF** and record ACTUAL output. ⚠️ **If your stage changes a row's status, MEASURE BOTH SIDES OF YOUR OWN EDIT.**
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** 92 BRAINSTORM five · 92 SPEC nine · 92 PLAN ten · **92 IMPL: nine recon divergences plus twelve findings above, including THREE of the PLAN's own NCs and its own cost model.** Do the same.
3. **LINE ANCHORS DRIFT; SYMBOL AND LITERAL-TEXT ANCHORS DO NOT — RE-VERIFY EVERY CITE AT YOUR TIP.** ⚠️ **PATHSPEC-SCOPE every symbol assertion.** ⚠️ **A RECEIVER WRITTEN `(cc *ClientConn)` MUST USE `grep -F`, NOT `-E`** — ERE reads the parens as a group and returns a **FAIL-UNSAFE ZERO**.
3b. ⚠️ **CHARACTER-CLASS AND ESCAPE SELECTORS ARE THE FAIL-UNSAFE FAMILY.** `grep -cE '^\s+[a-z0-9./-]+ v[0-9]'` on `go.mod` reads 62 not 67 · `grep -oE '[A-Za-z]+ +BackendKind = 38'` TRUNCATES `H2` · `^[0-9]{4}-` reads 119, dropping `0007a`/`0007b` (use `ls -d test/fixtures/*/ | wc -l`) · ⚠️ **`\t` in `-E` is TOOL-DEPENDENT: harness `grep` is ugrep 7.8.4, `/usr/bin/grep` is GNU 3.11. NAME THE TOOL.** Safe under both: `-P`, or `$(printf '\t')`.
3c. ⚠️ **`feedback_brief_citations_not_evidence` APPLIES TO INCIDENTAL FIGURES. Re-derive EVERY number you restate.**
3d. ⚠️ **NO GATE MAY REST ON A ` + ` SPLIT.**
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Live: TWO SDS dial-budget flakes (⚠️ **DO NOT reuse the `go list -deps` exclusion blanket — TRUE for `xds` (0), FALSE for `boot` (1)**) · the driver-owned receiver port race (see the banked `allocateOTLPPort` defect) · `internal/httpclient` zero-value · the two 84.2-era flakes · the REFERENCE h2spec section-8 flip · `TestOutlierDetector_ConcurrentEjectExactlyOnce` (fired ONCE historically; **did NOT fire this row**). ⚠️ **`TestServerConn_TinyWindowDelivery` IS NOT A FLAKE — phase 91 FIXED a live production deadlock; a recurrence is a REGRESSION of row 91.**
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only). Foreign containers left untouched throughout phases 90-92: `infallible_booth`, `crazy_kare`, `golink-ai`, `quizzical_goldstine`. ⚠️ **A FIFTH and SIXTH were also seen this row (`keen_spence`, `priceless_cannon`) — THE ROSTER IS NOT CLOSED.**
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`.
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL.** **`gofmt -l` never exits non-zero — gate on OUTPUT.** ⚠️ **`golangci-lint` DOES exit non-zero on findings in this build (corrected at phase 92) — but STILL gate on OUTPUT.** ⚠️ **`go test` WITHOUT `-v` PRINTS ZERO `=== RUN`** — `RUN=0` beside `RC=0` is a VACUOUS GREEN. ⚠️ **A `-run` selector matching nothing prints `[no tests to run]` and EXITS 0.** ⚠️ **On `-v` output, unanchored `grep -c 'FAIL'` reads 11 ON A FULLY GREEN TREE** — use `grep -cE '^(FAIL|--- FAIL)|^ *--- FAIL'`.
7b. ⚠️ **`grep -c` ON ZERO MATCHES EXITS 1 — capture the value (`v=$(… || true)`), NEVER chain on it with `&&`.**
7c. ⚠️ **A GREEN TEST RUN IS NOT EVIDENCE A SITE IS EXERCISED — IT MAY BE DEAD CODE.** Prove it with a **`panic()` REACHABILITY CONTROL**, and **do not skip the discriminating half** (a legal input must NOT panic).
7d. ⚠️ **NC EVERY NEW TEST, NOT JUST THE RISKY-LOOKING ONES.** ⚠️ **AND NC YOUR OWN FIXES** — phase 92's first NC attempt on its own fix was a BUILD BREAK, which proves nothing; it was redone by NEUTRALISING the code so the package still compiled.
8. ⚠️ **`pgrep -f` / `pkill -f` MATCHES YOUR OWN SHELL AND KILLS THE TOOL CALL (exit 144)**. **Kill ONLY PIDs you captured yourself** (from `$!`), and verify with `kill -0`. ⚠️ **Backgrounding an entire `&&` chain makes `$!` name the SUBSHELL and orphans the real process** — this happened at phase 92 and was caught with `pgrep -laf`.
9. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-*`, `wt-*` or stage worktree should outlive its stage close** — expect only `master` (verify with `git worktree list`).
10. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** (a)/(b) differential **121 fixtures** + full `go test ./...`, gated on **`PIPESTATUS[0]`** and a **SET RECONCILIATION** (⚠️ **NOT `INNER_EXIT`, which DOES NOT EXIST**); (c) h2spec cite ONLY from your own run — ⚠️ **h2spec is MEASURED BLIND to burst-drain ordering**; grpc-conformance deferred in writing; proxy-wasm **10 families measured** (NOT "10/16"); (d) fuzzers **56 / 48** today; (e) the ANCHORED panic gate `^panic:|DATA RACE|SIGSEGV` on every differential launch; (f) no REVIEW.md — standing departure. ⚠️ **`-race` on the DIFFERENTIAL suite is VACUOUS** — the subject is an unraced subprocess.
11. ⚠️ **THE FIXTURE INVOCATION: `go test ./test/differential/ -run 'TestDifferential/0004-h2-routing' -count=1 -v 2>&1`.** ⚠️ **Gate (a)'s failure mode is a SILENT PASS** — the runner `t.Skipf`s an unregistered fixture at `runner_test.go:200` and **NO fixture-count gate exists anywhere in the tree**. **ASSERT THE FIXTURE SET** (121 dirs vs 121 subtests vs 0 skips, reconciled BY NAME both directions).
12. ⚠️ **`-p` PUBLISHING WORKS; `--network host` IS WHAT DOES NOT.** ⚠️ A **STRICT_DNS** cluster whose name does not resolve **blocks listener init entirely**. ⚠️ **`clusters: []` is BOOT-REJECTED.** ⚠️ **envoy-go REJECTS `match.headers`**, ⚠️ **refuses `codec_type: HTTP2` without TLS unless `--allow-h2c`**, ⚠️ **boots with `-c`, NOT `--config-path`**, and ⚠️ **`access_log[].log_format` is BOOT-REJECTED**. ⚠️ **`ENVOY_TARGET.md` is at `docs/envoy-go/ENVOY_TARGET.md`, NOT the repo root.** ⚠️ **A PROBE BACKEND MUST BIND `0.0.0.0`, NOT `127.0.0.1`** — a loopback bind is unreachable from a container and the failure looks exactly like an envoy-side rejection.
13. ⚠️ **A RAW-FRAMER PEER MUST HOLD ONE `hpack.Decoder` PER CONNECTION.** ⚠️ **Set `ReadMetaHeaders` to NIL when probing malformed shapes.** ⚠️ **`0004` is TLS+ALPN-h2, NOT h2c.**
14. ⚠️ **`bash /dev/tcp` + `timeout cat <&3` INSIDE COMMAND SUBSTITUTION RETURNS SILENTLY EMPTY.**
15. ⚠️ **GO DOES NOT FLAG UNUSED CONSTANTS.** Post-removal cleanup is grep-driven.
16. ⚠️ **`golangci-lint`'s misspell runs in locale US.** **Sweep British spellings in `.go` comments BEFORE the gate** (it fired for real this row on `honoured`/`normalisation`). ⚠️ **Markdown prose may use them freely.**
17. ⚠️ **A GREEN RUN OF A CONTENTION-SENSITIVE TEST PROVES NOTHING.** Phase 91's subject cleared 5/5 uncontended while failing 9/32 contended on the SAME binary.
18. ⚠️ **AN ABSENCE CLAIM NEEDS A POSITIVE CONTROL AND A DISCRIMINATING NEGATIVE CONTROL.**
19. ⚠️ **DERIVE next-free ADR FROM THE TAIL, NEVER FROM THE HEADING COUNT.** The id space is SPARSE (one gap, `0209`) and **headings+1 reads 314 — the TAIL ITSELF, a TAKEN id.**
20. ⚠️ **`reference_measured_prototype_is_a_lower_bound` FIRED A THIRD TIME THIS ROW, SAME MECHANISM: UNDER-ENUMERATION OF *FILES*, NOT OF LINES.** Production `+218/-8` across EIGHT files vs a predicted `+174/-1` across three; differential `+384/-4` vs `+193/-0`. **RUN THE SUITE before you publish a cost.**

## ⚠️ PORT BANDING

`net.ipv4.ip_local_port_range = 32768 60999`. ⇒ **BAND YOUR PROBE PORTS BELOW 32768.** `21000-24999` is clear. **Check availability with `ss -tan` (ALL states), never `ss -ltn`.** ⚠️ **The differential harness reserves `20000..31007` (`harness_test.go:293`)** — a failure OUTSIDE that band (e.g. `38801`) is an **out-of-band** occurrence, NOT an in-band recurrence. ⚠️ **FIXTURE ports are FAMILY-BANDED as `10<index>` — but `10120` is TAKEN (fixture `0028`); free in-band is `10126-10129`.** ⚠️ **`0120` as a DIRECTORY is FREE.**

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (**ugrep 7.8.4**). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **THE METHOD BINDS; carry no standing discrepancy figure and no standing absolute.** ⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS; print FILE NAMES, never a count.** ⚠️ **THE HARNESS `grep` STRIPS A LEADING `./`.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files; use `-v --no-index`, and `git status` is the discriminating check). ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **A DISCREPANCY MAY FAIL TO MANIFEST** (phase 92: both forms agreed, because the searched string had ZERO hits in that file) — **absence of a discrepancy is not evidence the blindness is gone.**

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 93 (<slug>)' --format='%h %s'   # the SLUG form
git log --grep '^phase 93' --format='%h %s'            # the LOOSE form — RUN IT to CROSS-CHECK
git log --grep '^phase 92 (h2-response-header-validation)' --format='%h %s'  # a CLOSED phase, use as a FIRING CONTROL
```
⚠️ **RUN BOTH FORMS AND RECONCILE THE SETS. NO COUNT IS QUOTED HERE, DELIBERATELY — BOTH FORMS ROT** (the loose one on every `next-prompt` roll, the slug one on every roll that obeys the slug rule). **A `| head -1` on ONE form can silently return a NON-TIP commit** — that is how the phase-92 defect was found. **Take the tip from `git rev-parse master`.** ⚠️ **AND WHEN YOU LAND A CORRECTION, PUT THE FULL SLUG IN ITS SUBJECT.**

Execution style: **subagent-driven per `feedback_execution_style`**. Subagents commit LOCALLY on the ONE stage branch with EXPLICIT PATHSPECS; the controller squashes (`feedback_subagents_no_push`, `feedback_push_to_origin`). **Pure MEASUREMENT agents should commit NOTHING and prove their tree clean.** ⚠️ **PARALLEL AGENTS IN ONE WORKTREE RACE THE GIT INDEX EVEN WITH DISJOINT PATHSPECS** — an agent's `git commit` after `git add <path>` commits the WHOLE index, sweeping up a sibling's staged files. **Phase 92 used PER-AGENT WORKTREES ON THEIR OWN BRANCHES, merged by the controller; five parallel streams merged with ZERO conflicts.** ⚠️ **GIVE EACH PARALLEL AGENT ITS OWN PRIVATE SCRATCH DIR.** ⚠️ **AND VERIFY WHAT A SUBAGENT REPORTS** — at this IMPL an agent's own NC refuted the controller's instruction (the pin placement), and another correctly REFUSED to apply an out-of-scope production fix and escalated instead. Per-task `gofmt` + `golangci-lint` on touched packages, **gated on OUTPUT**. ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root** — build with `-o` into scratch.

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root — **observed repeatedly at every phase-88 through -92 stage**. Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` before any commit or gate run. ⚠️ **Shell VARIABLES do not survive between Bash calls** — re-export in every command. ⚠️ **AND AN UNQUOTED HEREDOC (`<<EOF`) COMMAND-SUBSTITUTES BACKTICKED TEXT** — use `<<'EOF'` and pass paths via the environment.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — `git status --porcelain` AND `git diff --stat master` must show ONLY your intended edits. For a patched TRACKED file: `sha256sum` BEFORE patching, `git checkout -- <path>` after, `sha256sum -c` the capture — ⚠️ **and COMMIT FIRST, because `git checkout --` restores from HEAD and will wipe uncommitted work.** ⚠️ **Phase 92 ran ~30 such restores across five agents and every one verified `OK`.** ⚠️ **Also check the MAIN repo root.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads the phase-93 BRAINSTORM (SELF-PICK).** ⚠️ **§Project counts is STALE (phase-76 era: fixtures 119, ADR tail 0298, stat surface 1205) and SELF-CONTRADICTS §Current with NO label saying so — do not anchor on it.** ⚠️ **`grep -c 'next-skill:'` reads 2: line 7 is the ADR-0288 RULE STATEMENT matching its own grep, and the live pointer is the other. That is CORRECT, not a violation.**
2. `docs/envoy-go/phases/92-h2-response-header-validation/PROGRESS.md` — the closed row's full evidence, and the source of every finding above.
3. `docs/envoy-go/DECISIONS.md` **ADR-0314** (COMPLETE) — §Consequences (xii) is the compensating-defect finding and names the strongest banked candidate.
4. `docs/envoy-go/ROADMAP.md` — the six deferred-candidate windows behind check (2).
5. `BOOTSTRAP_PROMPT.md` §4-§7 (repo ROOT). **Every phase dir is complete ⇒ state DONE ⇒ open a NEW phase.**

## What the next session owes
**SELF-PICK a subject and RUN THE PHASE-93 BRAINSTORM** (state DONE -> 1), recording the pick AND the rejected alternatives · **RE-RUN the sentinel + all four NCs yourself** and record ACTUAL output — ⚠️ **check (2) is now the ONLY blocker; treat it as load-bearing** · **DO NOT create `stop`** · **REGISTER THE NEW ROW** (`want` **124 -> 125**; ⚠️ **count the row's fields BEFORE installing the edit — NF must be 8**) · **REFUTE YOUR PREDECESSOR BY EXECUTION** · `STATE.md` rolled IN PLACE with the oldest §Recent entry evicted by a **DIRECT DATE READ** (⚠️ **read the dates yourself — SIX distinct tie shapes in six stages; the shape NEVER carries, and the section's RAW date-token stream is NOT the entry dates**) and archived to `STATE_HISTORY.md` as **ONE INLINE LINE** whose label does NOT match `^- \*\*prior active-phase:\*\*`, so the strict guard stays at **163, DELTA 0** · `next-prompt.txt` rolled (`git add -f`) · ONE squashed commit, merged and pushed — ⚠️ **and if a late measurement invalidates something you already pushed, land a CLEARLY-LABELLED correction commit (WITH THE FULL SLUG IN ITS SUBJECT) rather than force-pushing or leaving it standing.**
