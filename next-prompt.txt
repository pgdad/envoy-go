RESUME a fresh session for envoy-go (router last updated 2026-06-26; the **phase 45.1 (`otlp-access-log` core) IMPL is DONE and LANDED** — built end-to-end subagent-driven in a fresh worktree off master per the 12-task TDD spine in `PLAN-45.1.md`, two-stage-reviewed per task + a final holistic review, squashed into ONE atomic commit `07439d1f` (subject `phase 45.1 (otlp-access-log) IMPL: the core OTLP access-log sink — …`), fast-forwarded onto master + **pushed to origin** (`1e3b0bc0..07439d1f`), the worktree removed + the branch deleted. The six-gate is GREEN: gofmt 0 / golangci-lint clean / go vet clean / go build ok / `go test ./... -count=1` all green incl. the full **86-dir differential** (259s; no non-OTLP fixture moved — the byte-stability anchor held) / `go test ./internal/accesslog/ -race -count=1` clean / `go mod tidy -diff` EMPTY. **The next action is the phase 45.2 (operator engine) SPEC** — the FINAL leg of the by-concern 2-leg OTLP split.). HEAD is at the **next-prompt.txt roll commit** atop the **phase-45.1 IMPL squash `07439d1f`**, which sits atop the prior next-prompt roll `1e3b0bc0` + the **phase-45.1 PLAN `48d6b189`** + the prior roll `9989a7a4` + the **phase-45.1 SPEC `35070bc3`** + the tip-stamp `95dc14e2` + the **phase-45 BRAINSTORM `97d57ae7`** (the stable anchor). Re-read the shas with `git log --oneline -12`, do NOT assume — any number of docs-only `next-prompt.txt:` commits may stack ATOP the IMPL squash. Counts at IMPL-done (the exit invariant — re-verify, do NOT assume): stat surface **1191** (H2 cluster; non-H2 **1187**) / fixtures **86** (`ls -d test/fixtures/[0-9][0-9][0-9][0-9]* | wc -l` == 86; tail `0084-otlp-access-log`) / fuzzers **45** (`grep -rh '^func Fuzz' --include='*.go' . | wc -l` == 45) / BackendKind tail **38** (UNCHANGED — the OTLP receiver is driver-owned) / DECISIONS tail **ADR-0258**, next-free **ADR-0259**. +1 go.mod module (`go.opentelemetry.io/proto/otlp` v1.0.0 transitive→direct; `go mod tidy -diff` EMPTY). ZERO new `internal/` packages (`test/helpers/otlplogs` is test-only).

**ROW 44 (`grpc-access-log`) is `done`**; **ROW 45 (`otlp-access-log`) is `in-progress`** (the 45.1 core leg LANDED ⇒ leg 45.1 `done`, but **row 45 STAYS `in-progress`** — it flips `done` at the 45.2 IMPL, the FINAL leg, per per-leg ADR-0106 + `reference_roadmap_split_phase_row_done`). The **Observability FAMILY STAYS OPEN** (OTLP access log [active — 45.2 operator engine remains] / OTel-Zipkin tracing / stats sinks / tap filter remain rows). **The active action is the phase 45.2 (operator engine) SPEC** — the SECOND/FINAL leg of the by-concern OTLP split: the operator-templated `body` + `attributes` + `resource_attributes` (the `Body *AnyValue` / `Attributes *KeyValueList` / `ResourceAttributes *KeyValueList` fields STRICT-REJECTED at 45.1 — see `parseOpenTelemetryAccessLog` in `internal/bootstrap/bootstrap.go`), lifting them from reject to LIVE operator-substituted OTLP `LogRecord.attributes` / `body` / extra `Resource.attributes`. This is a SPEC stage (the phase-45 family BRAINSTORM `97d57ae7` already chartered the split — 45.2 starts at SPEC, NOT a fresh brainstorm). ANCHORS **ADR-0259**.

First confirm state via `git log --oneline -12` (expect HEAD at the next-prompt.txt roll over the phase-45.1 IMPL squash `07439d1f` over the prior roll `1e3b0bc0` over the PLAN `48d6b189`; counts as above; re-read the shas, do NOT assume).

## NEXT STEP — the phase-45.2 (operator engine) SPEC
This is a SPEC-authoring stage (docs-only; direct-on-master is fine for a docs-only SPEC, per the 45.1 SPEC/PLAN precedent — no worktree needed for pure docs). Invoke `superpowers:writing-plans` discipline lightly for the SPEC shape, but the deliverable is `docs/envoy-go/phases/45-otlp-access-log/SPEC-45.2.md` modeled on `SPEC-45.1.md`. The SPEC must:
1. Charter the operator engine: how Envoy's OTLP access logger templates `body` (an `AnyValue`, command-operator-substituted to a single OTLP value) + `attributes` (`KeyValueList` → per-record `LogRecord.attributes`) + `resource_attributes` (`KeyValueList` → extra `Resource.attributes` beyond the 4 built-ins). Resolve the operator-substitution model against the existing envoy-go access-log command-operator substrate (the phase-06.2 `AsyncFileSink` formatter / the 44.x `Record` fields — `%REQ()%`/`%RESP()%`/`%START_TIME%`/etc.) — REUSE it, do not re-invent.
2. Run the §11 D-OTLP-2-* live-probe questions against `contrib-v1.37.2` (the `reference_docker_probe_bridge_network` shape; verify decode ran) — what operator set does the OTLP body/attributes actually support, what's the AnyValue typing (string vs structured), how do `formatters` interact (45.1 STRICT-REJECTS them — confirm whether 45.2 lifts that or keeps it out of scope).
3. Lift the 45.1 STRICT-REJECT arms (`body`/`attributes`/`resource_attributes`) to PARSE-ACCEPT + the new operator fields on `OTLPConfig`; keep `formatters` rejected unless the probe says otherwise.
4. Anticipate the stat/fixture deltas (likely +1 fixture `0085-otlp-access-log-attributes` or similar; stat surface likely UNCHANGED unless `stat_prefix` honoring is pulled in — D-OTLP-STATS-PREFIX deferred at 45.1) + draft the ADR-0259 §Context.
THEN roll this router to the 45.2 PLAN.

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** This stage is the phase-45.2 SPEC (docs-only). When 45.2 reaches IMPL it is subagent-driven in a fresh worktree per `feedback_execution_style` · `feedback_git_worktrees` · `feedback_subagents_no_push` (subagents commit locally; controller squashes + pushes at stage-close) · `feedback_subagent_worktree_detach` + `feedback_subagent_worktree_path_targeting` · `feedback_pertask_gofmt_lint` · `feedback_push_to_origin`.
- **Process anchors:** ADR-0106 (per-leg roadmap rows; row 45 flips `done` only at the 45.2 IMPL) · ADR-0045 (split-gate) · ADR-0041/0080 (silent-ignore vs strict-reject for access-log type-URLs) · ADR-0016 (proto blank-import registry) · ADR-0255/0256/0257 (44.x as-built) · **ADR-0258** (45.1 OTLP core, LANDED) · next-free **ADR-0259** (45.2 operator engine).
- **45.1 as-built seams the 45.2 SPEC builds on:** `internal/bootstrap/bootstrap.go` (`parseOpenTelemetryAccessLog` + `OTLPConfig` + the shared `parseCommonGrpcAccessLogConfig` helper) · `internal/accesslog/otlpmapping.go` (`buildLogRecord`/`buildResource`/`buildExportRequest` — the operator engine extends these) · `internal/accesslog/otlpsink.go` (the sink — unchanged shape) · `internal/grpcclient/grpcclient.go` (`OTLPLogsClient` — unchanged) · `test/helpers/otlplogs/` (the receiver — extend to assert attributes/body) · `test/fixtures/0084-otlp-access-log/` (the differential precedent to copy).

## Read first (in order)
1. THIS FILE — the cold-start router (the 45.1 core IMPL is DONE + pushed; the next action is the 45.2 operator-engine SPEC — docs-only).
2. `docs/envoy-go/phases/45-otlp-access-log/SPEC-45.1.md` — the chartered/landed core leg (the §11 D-OTLP-* live-probe PINS + the as-built reference for what 45.2 extends).
3. `docs/envoy-go/DECISIONS.md` — **ADR-0258** (the 45.1 as-built, §Decision/§Consequences LANDED) + next-free **ADR-0259**.
4. `docs/envoy-go/STATE.md` — the active-phase header (`phase 45.1 (otlp-access-log) IMPL done`) + the recorded NEXT (the 45.2 SPEC).
5. `docs/envoy-go/ROADMAP.md` — row 45 (`otlp-access-log`, `in-progress`, 45.1-IMPL-annotated) + the `### Observability family` heading.
6. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the `### Access log — OpenTelemetry (OTLP) access-log sink` subsection (the 45.1 contract 45.2 extends) + the stat-surface block (1191).
7. `docs/envoy-go/phases/45-otlp-access-log/PROGRESS-45.1.md` — the as-built task ledger (the seams + the six-gate evidence).
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `reference_streaming_sink_differential_framing`, `reference_differential_grpc_receiver_driver_owned`, `reference_docker_probe_bridge_network`, `reference_strict_reject_sibling_typeurl_gap`, `reference_dynamic_stat_name_charset_guard` [relevant if 45.2 pulls in `stat_prefix` honoring], `feedback_execution_style`, `feedback_git_worktrees`).
