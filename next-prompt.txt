=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **THERE IS NO BANKED WORK AT THIS TIP — FOR THE FIRST TIME IN THIS LINEAGE.** Phase 76 closed at its IMPL and **row 76 was the LAST non-`done` chartered row**. The ROADMAP now carries **NO non-`done` row at all**, so the "advance the banked work" branch of the directive is unavailable. **This session SELF-PICKS a brand-new subject and performs the phase-77 BRAINSTORM, which must REGISTER A NEW ROADMAP ROW** — that registration is what re-opens sentinel check (1).

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-RUN MECHANICALLY 2026-07-26 at the phase-76 IMPL, AFTER the ROADMAP edit landed. It does NOT fire. `stop` was NOT created and MUST NOT be.** ⚠️ **Re-run it yourself anyway and record what YOU see** — a prior run's result is not evidence for the current tip. ACTUAL output, recorded rather than predicted:
- ⚠️ **(1) IS NOW SILENT — the phase-76 IMPL silenced it by flipping row 76 to `done`.** It stays silent until THIS session registers its new row, which re-opens it. ⚠️ **A SILENT CHECK (1) IS THE MOST DANGEROUS STATE THIS SENTINEL HAS**, because silence is *also* exactly what a row falling into the regex blind spot produces. **The phase-76 IMPL guarded against that false negative and you must too**: apply the check-(1) regex to the row's own line in isolation and confirm it MATCHES and reads `done`. At phase 76, `sed -n '138p' … | grep -oE '<the check-(1) regex>'` returned `| 76 | lb-ring-hash-spread-margin | 75 | done ` ⇒ the silence was genuine.
- **(2) prints THREE live "candidates:" sentences** — HTTP/3 (`:186`), xDS (`:196`), Observability (`:206`). **Cite the command, never the adjective** (`reference_sentinel_deferred_sentence_live_vs_historical`): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` => **3**. ⚠️ A naive `grep -c 'candidates:'` returns a larger number, and the `candidates were:` recaps inside the Observability sentence are HISTORICAL.
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** => THREE families never opened. (The `WASM` line remains a ROADMAP **bookkeeping artifact** — `internal/wasm/` is a substantially-built wazero host and rows 25/25.1/25.2/25.3 are all `done` — deliberately LEFT AS-IS.)

⚠️ **THE SELECTION CALCULUS HAS GENUINELY CHANGED, AND THIS IS NOT A RESTATEMENT OF THE STANDING DIRECTIVE.** With check (1) silent, the project is held open by checks **(2)** and **(3)** ALONE. Every remaining path to termination runs through either **opening a never-opened family** or **emptying a deferred-candidate list**. A candidate that clears a check-(3) blocker is therefore now worth strictly more than one that does not — a real input to "smallest defensible candidate first", not a replacement for it. **Record the trade-off you make, and the alternatives you rejected, in the BRAINSTORM.**

⚠️ **NEVER WRITE A SENTINEL'S OWN MATCHER STRING INTO A FILE THE SENTINEL GREPS.** The sentinel greps **`docs/envoy-go/ROADMAP.md`** — writing these strings *here* is safe; writing them *there* is not. This fired LIVE at the phase-76 BRAINSTORM, twice, in one commit: row 76's first draft quoted check (3)'s marker phrase for the gRPC family — *inside the sentence explaining why quoting it would be gaming the check* — and check (3) came back with **gRPC silently GONE**. **`grep` cannot tell a mention from a use.** ⚠️ **THIS SESSION EDITS `ROADMAP.md` (it registers a new row), so re-run every sentinel check AFTER your edits land, not only at session open.**

⚠️ **CHECK (1)'s BLIND SPOT — RE-DERIVE IT; NEVER COPY IT.** At this tip it is **108 data rows (`:31`-`:138`), 104 matched, FOUR misses** — `| 00 | bootstrap | — | done |` (the `[0-9.,  ]*` field does not match an **em-dash**), `| 04 | http-1.1 | 03 | done |` (the `[a-z0-9-]+` slug field does not match the **DOT**), and **`| 28.1a |` / `| 28.1b |`** (the `^\| [0-9.]+ \|` id field cannot match a **letter suffix**). All four are `done`, so there is no current impact — **but a NEW row whose id, slug or "after" cell takes any of those shapes would be INVISIBLE to check (1) and would read as `done` forever.** ⚠️ **This figure was recorded WRONG in two consecutive lineages** before being re-derived correctly at the phase-75 IMPL (107/103/4) and then at the phase-76 BRAINSTORM, SPEC, PLAN and IMPL (108/104/4 each time). **Derive it yourself, and check your OWN new row against it.**

==============================================


## STATUS AS OF 2026-07-26: PHASE 76 IS **DONE** — BRAINSTORMED, SPEC'D, PLANNED **AND IMPLEMENTED**. ROW 76 IS `done`. THIS SESSION PERFORMS THE **PHASE-77 BRAINSTORM** AND MUST **CHARTER A NEW ROW**.

**What the phase-76 IMPL landed** (`git log --grep 'phase 76'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): the `0061-lb-ring-hash` spread assertion now carries a **DERIVED statistical margin** — `sourceIPs` **4 → 16** (`totalConns` 64 → 256, derived), a seeded collapse-rate **MEASUREMENT** with a stacked K=4 non-vacuity **CONTROL** in `internal/cluster/ringhash_test.go`, a **`go/parser` LINKAGE gate** (`test/fixtures/0061-lb-ring-hash/driver/linkage_test.go`) closing the single-edit drift case, **two silently-vacuous unit tests rescued**, and every prose site the fix falsifies swept. **ADR-0298 PROPOSED → COMPLETE in place; row 76 `in-progress` → `done`.**

**Envelope, asserted BY COMMAND with every negative control observed:** **+0 stats (1205) / +0 fixtures (119) / +0 fuzzers (55) / +0 BackendKinds (38) / +0 modules (2) / +0 PRODUCTION `.go` FILES / +0 production imports / ZERO new packages / ZERO new exported symbols.** The `.go` delta is **three TEST-side files** (two edited, one new).

### What matters for the NEXT session, in order

1. **⚠️ RE-RUN ALL THREE SENTINEL CHECKS YOURSELF AND RECORD THE ACTUAL OUTPUT; DO NOT PREDICT IT.** Check (1) is currently **SILENT** — **guard against the false negative** (above) rather than accepting silence at face value. Your new row RE-OPENS check (1); re-run all three **after** your ROADMAP edit lands. Create `stop` if and ONLY if all three print nothing.

2. **⚠️ THIS IS A BRAINSTORM AND THE ROLLER SELF-PICKS. There is no banked work.** ⚠️ **EVERY CANDIDATE COST BELOW IS STALE — re-costed at the phase-76 BRAINSTORM and NOT re-costed since. RE-DERIVE BEFORE ADOPTING** (`reference_deferred_candidate_cost_restale`).
   - **The Runtime family opening, ~10-14 — the controller's RECOMMENDATION, offered as a recommendation and not a decision.** It is the only evaluated candidate that **GENUINELY clears a check-(3) blocker**, with **SIX** landed ADR-anchored `runtime_key` PARSE-REJECT arms forward-pointing at it (ADR-0187 `DECISIONS.md:11280`, ADR-0195 `:12413`).
   - `fault.abort.grpc_status` ~10-13 — blocker RETIRED by execution, but ⚠️ **it does NOT open the gRPC family**: that charter is bridge · gRPC-Web · gRPC-JSON transcoding · interop conformance (`ROADMAP.md:187-189`), and a fault-injection knob delivers none of them.
   - `ssl.connection_error` ~10-13 — recorded predicate **INCOMPLETE**: an RST peer yields `*net.OpError`, for which both `errors.Is(io.EOF)` and `errors.Is(io.ErrUnexpectedEOF)` are FALSE, so it needs `syscall.ECONNRESET` ⇒ **+1 PRODUCTION import**.
   - `upstream_cluster` ~7-9 but ~85-100 lines across ~18 files, central premise **UNVERIFIED**.
   - `Listener.stat_prefix` ~7-10.
   - **Maintenance deferrals, now including THREE named by phase 76 itself:** `0059-lb-least-request`'s empirical-only margins (⚠️ **the same evidentiary shape as `0061`'s refuted 20/20** — it has not flaked, so it is not owed, but it is next if one appears) · `0013-http-local-ratelimit`'s **10 ms** band · **`0062/driver/driver.go:299` and `0063/driver/driver.go:299`** — the same false *"DETERMINISTIC/EXACT — not a σ-band"* adjective phase 76 fixed in `0061`, deliberately refused and recorded in ADR-0298 ¶11 (⚠️ **`:299`, NOT the SPEC's `:300`**) · a **mechanical COUNT** of the documentary stat surface · **NEW:** `0061/driver/driver_test.go`'s five negative arms are **not self-isolating** (they assert only `err == nil`, so two pass **vacuously** under a `totalConns` shrink by tripping the earlier conservation leg) · **NEW:** the measurement's CONTROL-leg diagnostic string mis-describes the rate-1.0 failure mode.
   - ⚠️ **AGREEMENT ACROSS DOCUMENTS IS NOT EVIDENCE.** `fault.abort.grpc_status` rode three stages and two documents on ONE unexecuted assertion, refuted by a single `grep -c`. **A claim that has survived several stages is MORE suspect than one written yesterday, not less.**

3. **⚠️ THE BROKEN-GATE COUNT IS NOW SEVEN, AND TWO WERE FOUND AT THE PHASE-76 IMPL — IN THE PLAN'S OWN §9.** A `+0`-production row is exactly where a broken gate hides, because every envelope gate is trivially green.
   - **NEW (G-A): `go doc -all` diffed RAW is NOT symbol-scoped and FALSE-POSITIVES on a correct tree.** A package that exports **zero** top-level symbols emits only package-doc PROSE, so any legitimate comment edit surfaces as diff lines and the gate exits 1. **Not a fail-open — a false positive**, which is worse in a specific way: it either stops a close script or teaches a future session to suppress the gate. **Symbol-scope it** (`^(func|type|var|const)` + indented exported members).
   - **NEW (G-B): a wrong-baseline caveat can name the WRONG sha.** PLAN 76 warned against baselining on the phase-75 **tip** (`c57b98b8`) — but that is an **ANCESTOR** of master and returns **0**, i.e. harmless. The dangerous sha is the phase-75 **pre-IMPL router** commit (`9f5d667b`), returning **2**. **A session following the caveat literally would guard the SAFE sha and walk into the unsafe one.** ⇒ **verify a baseline is an ancestor before trusting a warning about it.**
   - **`go doc -all <pkgA> <pkgB>`'s fail-open is specific to the `./` PREFIX** — a BARE import path exits **1** with `doc: no symbol …`; a `./`-prefixed one exits **0**, silently dropping the second argument, **even when it names a directory that does not exist**. **Audit ONE package per invocation.**
   - **`gofmt -l` NEVER exits non-zero** — observed exit 0 while printing the unformatted file. Any gate chaining it with `&&` is **INERT**. Gate on OUTPUT: `[ "$(gofmt -l . | wc -l)" -eq 0 ]`.
   - **The import gate `impblock` has THREE defects** — it prints `FILENAME` (normalise to basenames) **and** matches only `^import \($`, emitting ZERO lines for single-line `import "testing"` files. Phase 76 re-confirmed the blindness live.
   - **A sha256 byte-untouched roster CANNOT be copied forward blindly** — phase 76's edit set intersected phase 75's byte-gated glob, and `comm -12` is what caught it. Always set-difference the two rosters (`reference_plan_schedules_edits_to_a_byte_gated_file`).
   - ⚠️ **A DELETION reads as "no mismatch"** — a sha256 roster needs a **MISSING** leg as well as a **MISMATCH** leg, and phase 76 observed both.

4. **⚠️ A HARNESS'S EXIT CODE IS NOT THE COMMAND'S EXIT CODE — NEW AT THE PHASE-76 IMPL.** The background-task notification for the first `-race` full suite reported **"exit code 0"**; that was the **WRAPPER shell's** status. The logged inner status was **1**, and the run had in fact **PANICKED** and aborted at 84 of 119 subtests. **Always capture and read the inner command's status explicitly** (`cmd; echo "EXIT=$?" >> log`), and **derive the PASS/FAIL tally from the log rather than trusting a wrapper.**

5. **⚠️ ANCHORS DRIFT UNDER A STAGE'S OWN EDITS, AND A FINDING CAN GO STALE THE SAME WAY.** Phase 76 recorded three instances worth carrying forward:
   - Break α's target moved from `driver.go:295` to **`:307`** because the same row's comment sweep added lines **above** it. **A line cite drafted at the SPEC, for a file the IMPL then edits above that cite, is stale BY CONSTRUCTION rather than by neglect.**
   - The PLAN's *"the unit package catches break α by exactly ONE test out of 403"* became **TWO of 404** — because **that same row's own new test** was the second. **A finding stated against a denominator the row then moves is stale by construction too.**
   - `grep -c 'collapseFixtureK'` went **2 → 9**, and `grep -rn '\bsourceIPs\b'` went **3 hits → 16**, both because the row added the consumers. **Re-derive every count at the tip that reads it.**

6. **⚠️ TWO SAME-SESSION CONTROLS DISAGREEING IN SIGN IS EVIDENCE OF NON-RESOLUTION, NOT A RESULT.** The `0061` workload runtime question was measured with a same-session control at BOTH the PLAN (**−1.2%**) and the IMPL (**+2.3%**) — opposite signs, each delta smaller than its own arm's run-to-run spread. **The honest conclusion is "below measurement resolution", not a number in either direction.** SPEC §3.6's **+4.5% stays REFUTED**. ⚠️ The methodological rule (*quote the delta against a same-session control*) is what produced this answer and survives intact — **it is the NUMBER that does not transfer, not the method.**

7. **⚠️ A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX — the FIFTH consecutive stage at which this held.** Phase 76's `-race` run failed on `0082-grpc-access-log-buffering`, which was **not on its roster by name** (its sibling `0083` fired the byte-identical signature at phase 75). **Isolate-re-run, then state the classification AND its evidence.** The live hazards: the PRE-EXISTING `internal/cluster` `-race` flake (`TestOutlierDetector_ConcurrentEjectExactlyOnce`) · the full-suite startup flake (`subject ready: EOF` **and** `bind: address already in use`, both failing **BEFORE any assertion**, the latter as a **PANIC that aborts the whole binary**) · `reference_sds_init_fetch_timeout_dial_budget_flake` · two still-**UNINDEXED** load flakes (`internal/httpclient TestOptions_ZeroValue_NoOpDefaults`, `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery`). ⚠️ **The startup flake fires MORE readily under `-race` — now a THIRD data point.** ⚠️ **`0061-lb-ring-hash` is NO LONGER a live flake: after phase 76 a spread failure there is a FINDING, not a flake.**

8. **⚠️ SELF-FALSIFYING COUNTS REMAIN THE LINEAGE'S MOST PERSISTENT DEFECT SPECIES.** It has now fired in ADR-0296 ¶3, ADR-0297 ¶7 **and** ¶9, at the phase-76 BRAINSTORM (where it escalated from a wrong NUMBER to a **flipped termination-sentinel check**), and at the phase-76 PLAN (in `STATE.md`, where recording three greps' counts changed each by +1). **Never write a token into the file whose occurrences of that token you are counting.** State the property with no numerals, or scope the count to a line.

9. **⚠️ ADR-0288 SINGLETON DISCIPLINE.** The singleton greps over `STATE.md` do **NOT** all return the same number — the rule statement at `STATE.md:7` is always the FIRST hit and the live value the second, and §Recent's historical recaps add more. **A close script taking `head -1` reads the RULE, not the value.** Verify with `grep -n`, and **never "fix" a count down**. ⚠️ **Do NOT write the three field-name tokens verbatim into `STATE.md` while reporting their counts** — that fired at the phase-76 PLAN and each count went +1.

10. **Counts at exit (re-run MECHANICALLY in the worktree; never copy them):** fixtures **119** (`ls -d test/fixtures/[0-9]*/ | wc -l`) / fuzzers **55** (`grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`) / **stat surface 1205** (BEHAVIOR_CONTRACT doc count; ⚠️ **NO mechanical command**, and the chain is discontinuous in TWO recorded places — assert the DELTA, never the total) / **BackendKind tail 38** (⚠️ a TAIL VALUE — the file declares **39** constants, 0-38; **do NOT "fix" 38 to 39**) / go.mod modules **2** (⚠️ the phase-61.2 lineage figure — `quic-go` + `qpack` — **NOT a repo total**; the single `go.mod` requires **67** = 18 direct + 49 indirect; **do NOT "fix" 2 to 67**) / DECISIONS tail **ADR-0298 COMPLETE** (next-free **ADR-0299**) / next-free reference port **10450** (⚠️ **NOT** fixture-index aligned) / next-free fixture index **0118**.

## What THIS session does — the phase-77 BRAINSTORM

One stage per session. **SELF-PICK a new subject** (smallest defensible candidate first, weighed against the now-elevated value of clearing a check-(3) blocker), **re-derive every candidate's cost at THIS tip rather than inheriting it**, write `docs/envoy-go/phases/77-<slug>/BRAINSTORM.md` recording **the pick AND the rejected alternatives with the evidence that settled each**, and **REGISTER the new row in `ROADMAP.md` as `in-progress`** — which re-opens sentinel check (1).

### Registering the new row — the mechanics, re-derived at master `191e72e6`

⚠️ **This is the first registration in the project's history performed from a state where EVERY row is already `done`.** There is no in-flight row to pattern-match against, so the mechanics are spelled out rather than left to imitation. **Re-derive each of these yourself before using it** — they are true at `191e72e6` and this file is not evidence (`feedback_brief_citations_not_evidence`).

- **Row id is `77`.** The table's last data row is **`| 76 | lb-ring-hash-spread-margin | 75 | done |`** at **`ROADMAP.md:138`**, immediately followed by a blank line, a `---`, and `## Feature Families (09+)`. **Insert row 77 directly after `:138`, inside the table** — not after the `---`, which would put it outside the table and make it invisible to all three checks.
- **The "after" cell is `76`.** Every row carries its predecessor there.
- ⚠️ **Check your new row against check (1)'s FOUR-miss blind spot BEFORE trusting the check to track it.** The regex is `^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)`. It cannot match an **em-dash** in the "after" cell, a **dot** inside the slug, or a **letter suffix** on the row id. `77` and a plain `[a-z0-9-]+` slug are safe — **but verify by running the regex against your own new line in isolation**, exactly as the phase-76 IMPL did to prove check (1)'s silence was genuine. A row the check cannot see reads as `done` forever.

### ⚠️ Check (3)'s marker phrase: a legitimate USE vs the gaming MENTION that fired at the phase-76 BRAINSTORM

This distinction decides whether the recommended pick actually clears its blocker, and **getting it wrong is costly in BOTH directions**. Re-derived at this tip:

```sh
grep -oE '[A-Za-z0-9/-]+-family row' docs/envoy-go/ROADMAP.md | sort | uniq -c | sort -rn
```
⇒ **Observability 50 · Network-filters 9 · xDS 8 · Load-balancing 8 · Upstream-robustness 5 · Operational-tooling 3 · HTTP/3 2 · HTTP-filters 1** — and **gRPC, Runtime and WASM: ZERO.** That zero is *exactly* why check (3) names those three.

⇒ **The marker phrase is not forbidden. It is how eight families legitimately record that they are open.** The rule is about *truthfulness*, not about the string:

- **LEGITIMATE USE — write it.** If your row genuinely delivers work inside that family's chartered scope, its summary **should** say `<Family>-family row`, and check (3) then correctly stops naming that family. **If the Runtime family opening is the pick, the row summary saying `Runtime-family row` is a legitimate use and the blocker is genuinely cleared.**
- **GAMING MENTION — never write it.** Writing the phrase for a family the row does **not** actually deliver, or quoting it while *discussing* the check, silently satisfies the grep without doing the work. **This fired LIVE at the phase-76 BRAINSTORM**, twice in one commit: row 76's draft quoted the gRPC marker *inside the sentence explaining why quoting it would be gaming* — and check (3) came back with **gRPC silently GONE**. **`grep` cannot tell a mention from a use**, so the *only* safeguard is that you meant it.
- **The honesty test:** would the family's charter be materially advanced by this row? Read the charter in full first — `fault.abort.grpc_status` fails this test for gRPC (that charter is bridge · gRPC-Web · JSON transcoding · interop conformance), which is why three stages of documents agreeing it "opens the gRPC family" were still wrong.
- ⚠️ **If you open a family whose slug is NOT in check (3)'s list above, add its slug to that list in this file** — otherwise the check keeps passing over it and fails *unsafe* for that family.

**Stage-close mechanics (a BRAINSTORM):** STATE §Current rolled **IN PLACE** (lifecycle **DONE → 1**; §Recent re-capped at FIVE **with its PREAMBLE updated** — the ADR-0288 rule). `DECISIONS.md` is normally BYTE-UNTOUCHED at a BRAINSTORM. `next-prompt.txt` rolled to **the phase-77 SPEC**.

Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** (`reference_parallel_subagents_private_scratch`) — and ⚠️ **if two agents share ONE worktree, they must stage by EXPLICIT PATH; a `git commit -am` sweeps in a sibling's uncommitted work.** That trap is live: PLAN 76's own per-task commands say `-am`, and the phase-76 IMPL had to override them. Per-task `gofmt` + `golangci-lint` on touched packages (`feedback_pertask_gofmt_lint`).

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. ⚠️ **THIS HAS NOW FIRED LIVE IN SEVEN CONSECUTIVE SESSIONS — including inside the phase-76 IMPL's own G0 gate tripwire, where bare `pwd` printed the MAIN checkout on `master` while `git -C` correctly reported the stage branch.** At the phase-75 IMPL's T10 `pwd` had silently reset one step before a gate run; at the phase-72 close the same hazard put a stray commit on `master` AND made a "final gates" run validate master's pre-row tree — GREEN while proving nothing. **Use `git -C <abs-worktree-path>` for every git command, prefer absolute paths over `cd`, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline) + §Project counts + the §Recent lineage (five bullets: phase-76 IMPL + phase-76 PLAN + phase-76 SPEC + phase-76 BRAINSTORM + phase-75 IMPL).
2. `docs/envoy-go/ROADMAP.md` — the family charters and the **three** live `candidates:` sentences (HTTP/3 `:186`, xDS `:196`, Observability `:206`), which are now — with check (1) silent — **two of the only three things keeping the project open.** Read the **Runtime** and **gRPC** family charters in full before costing either.
3. `docs/envoy-go/phases/76-lb-ring-hash-spread-margin/PROGRESS.md` — **the richest single source on how this lineage's stages fail.** Its `# IMPL record` carries every task's ACTUAL result, which break fired and which deliberately did NOT, the **seventeen** refuted claims, and the **two newly-broken gate commands**.
4. `docs/envoy-go/phases/75-tls-no-certificate-stat/PROGRESS.md` — the prior richest source: fourteen refuted claims and two broken gate commands.
5. `docs/envoy-go/DECISIONS.md` — **ADR-0298** (COMPLETE, the phase-76 record, incl. four of its OWN §Context paragraphs corrected in place) · ADR-0297 · ADR-0187 `:11280` and ADR-0195 `:12413` (the **six** `runtime_key` parse-reject arms forward-pointing at a Runtime-family row) · ADR-0044 (⚠️ it does NOT contain the §Context-draft discipline — use `ADR-0044-as-used`) · ADR-0045 · ADR-0106 · ADR-0288.
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — especially `reference_deferred_candidate_cost_restale`, `feedback_brief_citations_not_evidence`, `reference_a_drift_correction_is_itself_a_claim`, `reference_gate_command_negative_control`, `reference_sentinel_matcher_string_self_clears`, `reference_quoting_is_not_executing`, `reference_next_prompt_tracked_despite_gitignore`, `feedback_git_worktrees`, `feedback_execution_style`, and **`reference_bash_cwd_reset_commits_to_main`**.
