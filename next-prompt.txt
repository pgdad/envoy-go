=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **PHASE 86 (`validate-sds-nil-provider`) IS OPEN AT THE SPEC (2026-08-10): ROW 86 IS `in-progress` AT `ROADMAP.md:148` AND `want` IS 118.** ⇒ **THIS SESSION PERFORMS THE PHASE-86 PLAN** (lifecycle-state 2 -> 3). `STATE.md`'s `next-skill:` reads `PLAN for phase 86` — that IS your instruction. The PLAN's obligations are enumerated in §What-the-PLAN-owes below and in SPEC.md §13.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=118 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **`want` IS 118 AT THIS TIP** (row 86 `in-progress`, all 117 others `done`). ⚠️ **AT THIS TIP CHECK (1) PRINTS EXACTLY `NOT DONE: row 86`** — the expected line while the phase is open; ANY OTHER row id appearing is a FINDING. ⚠️ **THE SENTINEL DOES NOT FIRE AT THIS TIP:** check (1) prints, check (2) prints SIX (at `:196 :202 :208 :218 :224 :232` — re-derive, the anchors drift with every edit), and the condition is a CONJUNCTION. Check (3) is SILENT. A PLAN is docs-only and leaves `ROADMAP.md` BYTE-UNTOUCHED (row 86 stays `in-progress`; `want` stays 118) — verify by empty diff at your close.

⚠️ **THE DOCTORING NCs REMAIN MANDATORY EVERY SESSION.** Both:
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md at want=118: expect "NOT DONE: row 62" AND "NOT DONE: row 86"
```
```sh
# the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```

⚠️ **AND WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -oiE '-family row'` fails with `grep: amily row: No such file or directory` and the surrounding arithmetic prints `base=0 now=0 delta=0`, **which reads exactly like "no change".** Observed live at FOUR phase-84 stages. **Always pass `--` before the pattern**, and remember the counts are form-dependent: **95 occurrences vs 67 LINES** at this tip (`grep -c` counts LINES). **A gate that reads zero on BOTH sides is not evidence of invariance.**

**SENTINEL RE-RUN MECHANICALLY AT THE PHASE-86 SPEC CLOSE (2026-08-10, base `11f5a92c`). IT DOES NOT FIRE; `stop` was NOT created** (`ls stop` => `No such file or directory`, repo root AND stage worktree). ACTUAL output, recorded not predicted: input measured **236 lines / 118 data rows** first; **(1) `NOT DONE: row 86`** at `want=118` — the single expected line, denominator assertion silent; **(2) SIX — `:196 :202 :208 :218 :224 :232`** (⚠️ do NOT "fix" it and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases; the candidates sentences are a WINDOW onto a larger deferred backlog, not an inventory of it); **(3) SILENT**. The condition is a CONJUNCTION ⇒ the sentinel does NOT fire — and `STATE.md`'s `next-skill:` reads `PLAN for phase 86`, i.e. a next stage EXISTS and the loop continues.
- **ALL FOUR NCs FIRED AT THIS CLOSE:** row-62 doctoring => `NOT DONE: row 62` AND `NOT DONE: row 86` — with `NC LANDED? [ in-progress ]` inspected first; `want=117` => `NOT DONE: row 86` + `GATE FAIL: examined 118 data rows, expected 117`; check-(3) doctoring (residual `gRPC-family row` confirmed **2 -> 0** on the doctored copy first) => `NEVER OPENED: gRPC` while `WASM` correctly stayed silent; check-(2) one-arm counts **5** (long arm alone) / **1** (short arm alone), union **6** (a one-arm strip is NOT an NC for the union).
- ⚠️ **Re-run all three checks and both doctoring NCs yourself and record what YOU see.**

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (ugrep under the hood). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Reproduced at NINETEEN separate tips.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **THE METHOD BINDS; carry no standing discrepancy figure and no standing absolute.** ⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS; print FILE NAMES, never a count.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files; use `-v --no-index`, and `git status` is the discriminating check). ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **`grep -c` ON ZERO MATCHES IS TOOL-SPECIFIC** (only `git grep -c` prints nothing; all exit 1 — capture the value, never chain on the exit code). ⚠️ **`grep -c` COUNTS *LINES*, NOT *OCCURRENCES*.** ⚠️ **`\t` IN GNU ERE IS A LITERAL `t`** — use `-P`. ⚠️ **A `git grep` PATHSPEC ENDING IN `/` READS A FAIL-UNSAFE ZERO** (shape 33) — use the shell-glob form. ⚠️ **AN UNANCHORED PANIC-GREP FALSE-FIRES 14 TIMES ON A FULLY-GREEN DIFFERENTIAL LOG** — **the canonical gate is ANCHORED: `^panic:|DATA RACE|SIGSEGV`. A gate that fires on a green log is worse than no gate.**

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`** with FOUR blind spots (an em-dash, a DOT in `http-1.1`, two LETTER-suffixed ids). ⚠️ **EXECUTED on scratch copies at the phase-77 PLAN: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** ⚠️ **DO NOT restore the regex form.** At this tip check (1) prints exactly `NOT DONE: row 86` — **any OTHER row id appearing there is a finding** — and the row-62 doctoring NC is what proves it still LOOKS.

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

⚠️ **Run the union form, never one half of it.** ⚠️ **A ONE-ARM STRIP IS NOT AN NC** (6 -> 5 short / 6 -> 1 long; never 6 -> 0). ⚠️ **The documented trap's stated MECHANISM is refuted while the trap is real** — the inflation comes from ROADMAP table rows carrying BRAINSTORM prose. **Cite the command; do not re-inherit the explanation.** ⚠️ **The line numbers drift with edits — re-derive them; the current six are `:196 :202 :208 :218 :224 :232`.** ⚠️ **These six paragraphs remain the final sentinel blocker, and any edit near them is a sentinel-affecting edit.** All six are standalone family PROSE paragraphs (not rows), naming ~42 distinct candidates; every "retire the paragraphs" shape is doctrinally foreclosed (`reference_sentinel_matcher_string_self_clears`). ⚠️ **Row 86's pick came from OUTSIDE the windows** (BRAINSTORM-83 §5.6 provenance, the phase-85 shape) — **nothing narrows in any window sentence at row-86-done**, and the Op-tooling window's "an RTDS/SDS validate companion" is ADJACENT but DISTINCT (feature vs repair; BRAINSTORM-86 §2, SPEC §4) — its sentence stays untouched.


==============================================


## STATUS AS OF 2026-08-10: PHASE 86 OPEN AT THE SPEC (row 86 `in-progress` at `ROADMAP.md:148`, base `11f5a92c`). **THIS SESSION PERFORMS THE PHASE-86 PLAN.**

**What the 86 SPEC landed (2026-08-10, base `11f5a92c`, branch `phase-86-spec`, ONE squashed commit).** Docs-only: `SPEC.md` + `PROGRESS.md` update in `docs/envoy-go/phases/86-validate-sds-nil-provider/`, **ADR-0308 §Context APPENDED STATUS `PROPOSED`** (`DECISIONS.md` 18050 -> 18066, numstat `16 0`, base a byte-exact prefix; headings **307**; tail **ADR-0308**; next-free **ADR-0309**; ⚠️ **strict `^> \*\*STATUS: PROPOSED` guard ARMED 0 -> 1 — a LIVE POINTER disarmed only by the phase-86 IMPL**), `STATE.md` rolled in place, IMPL-84.2 evicted to `STATE_HISTORY.md` (476 -> 478, strictly append-only, absence guard 0/0 with FROM-THE-FILE controls 9/9, 7/7, 0/0 — both real controls DRIFTED UP again). `ROADMAP.md` and `BEHAVIOR_CONTRACT.md` BYTE-UNTOUCHED. ⚠️ **NAMED DEPARTURE CONTINUES: no investigation agents — probes INLINE by the controller**, PLUS **a COMPILING, TEST-GREEN PROTOTYPE in a DETACHED worktree** (`wt-86-proto`, DELETED at close; diff in that session's scratch — re-derive, do not hunt for it).

**THE HEADLINE: Q1 IS DECIDED BY MEASUREMENT, NOT ARGUMENT — D-86-MECH, the no-fetch sentinel provider, BUILT and proven at `11f5a92c`:**

1. **`internal/xds/provider_nofetch.go` (NEW, ~37 lines):** unexported `noFetchProvider` + `NoFetchProvider()` + `IsNoFetch()`. **The discriminator is the SENTINEL, never `provider == nil`** — the nil-rejects' OTHER consumers (QUIC, test-only ctors, main.go seen==0) keep rejecting BYTE-IDENTICAL.
2. **`internal/boot/boot.go` (+30/-7):** `NewSDSProvider` body -> unexported `newSDSProviderAndClient` (returns provider + client); NEW **`NewValidateSDSProvider`** reuses the SAME function — **parity by CODE-PATH, future pre-scan arms inherited by construction** — then **CLOSES the never-dialed lazy client** (D-86-CONN; validate is a library) and returns the sentinel. `(nil, nil)` at seen==0.
3. **`internal/tls/config.go` (+25/-5):** three fetch-site skips (arm A `:390`; VC-SDS + CVC fetch blocks in `NewDownstreamConfig`, AFTER their `ParseSDSConfig` calls) **+ the `:518` `sdsCertPromised` interplay (D-86-NOFETCH-CERT)** — an SDS-only-cert listener must not hit `no tls_certificates configured` when the fetch is skipped; UNENUMERATED at the BRAINSTORM, found ONLY by executing the prototype.
4. **`validate/validate.go` (+10/-2):** literal nil -> `boot.NewValidateSDSProvider(dialer, bs, baseDir, bs.Stats)`.

**Prototype evidence:** net **+88 production `.go`** across 4 files; ALL ELEVEN probe shapes correct (3 SDS arms + static control **ACCEPT**; six build-time negatives emit **BYTE-IDENTICAL boot strings**; dead-endpoint shape ACCEPTS); **six packages' full test suites green** (`internal/tls`, `internal/boot`, `internal/xds/...`, `internal/listener`, `validate`, `cmd/envoy-go`) **including the three nil-reject pins UNTOUCHED** (`config_test.go:921/:1198/:1310`). Option (b) mode-flag threading measured **DEAD at ~108 call sites** (2+36+2+52+16 over five signatures); option (c) placeholder material REJECTED (fabricates state).

### What else the SPEC disposed (SPEC.md §2-§7 — cite it, re-derive at your tip)

- **Q2 boot-parity BY EXECUTION: SIX build-time arms + ONE fetch-time class.** Build-time (5-9 ms, I/O-free; `grpc.NewClient` is lazy): node requirement · one-secret cap · `ParseSDSConfig` arms · secret-name charset · **cluster-existence** · **cluster `http2_protocol_options` (n7 — NEW, absent from every prior enumeration)**. Fetch-time (validate exempts, D-86-N5): the dead-endpoint shape — **validate accepts iff boot accepts, modulo exactly the rejects that require I/O to discover.**
- **Q3 reference EXECUTED (`contrib-v1.37.2`, `s86-*` containers, all torn down):** all four arm/control shapes `configuration OK`; **n1 node-absent is a BOTH-SIDES reject** (`TlsCertificateSdsApi: node 'id' and 'cluster' are required` — the reference RUNS SDS checks in validate mode without dialing); n2-n7 reference-accept = the boot path's PRE-EXISTING documented departures inherited by parity. **No new departure minted.**
- **Q4 contract sweep:** ⚠️ **`BEHAVIOR_CONTRACT.md:1062` records the OPPOSITE decision as permanent** ("The `validate` path diverges, and stays that way") — its rationale REFUTED BY EXECUTION; **ADR-0308 is the mandated vehicle for the REVERSAL** + the `:1034`/`:1050` consumer-roster edits + the `:948-958` validate-section extension. The phase-60.2 no-DIAL decision SURVIVES. Production comment rewrites enumerated: `config.go:344-346/:407-435/:447-452`, `manager.go:569-573` (their consumer rosters become FALSE).
- **Q5 placement:** tls per-arm accepts + interplay arm + guard-preservation NCs · boot negatives on `NewValidateSDSProvider` · validate end-to-end · `main_test.go` CLI subprocess arms. RED anchors = the three arm rejects (re-reproduced at `11f5a92c`). NO differential fixture.
- **Q6:** ZERO landed reject strings change; boot-parity strings byte-identical BY CODE-PATH REUSE; only the sentinel's two never-called defense-in-depth strings are new.
- **Q7 (each verified POST-change on the prototype tree):** `go list -deps ./validate` ALREADY carries `internal/xds`+`xdsgrpc` · `go mod tidy -diff` clean · fuzzers **55/48** · fixtures/BackendKind **+0** · **stat-surface DELTA 0** by same-command census both sides (145/21 — a DIFFERENT counting form than the carried 208/36 ARM-1 census; NO absolute corrected, only the delta asserted).
- ⚠️ **COST — the NINTH consecutive `reference_measured_prototype_is_a_lower_bound` firing:** IMPL budget **~110-160 net production + ~400-680 test `.go`** (BRAINSTORM floors ~50-90/~150-300 REFUTED as central; unenumerated: n7, the `:518` interplay, consumer-comment rewrites, client-close plumbing). **Carry the budget as a FLOOR; overrun is recordable, not silently absorbed.**
- **D-86-SEQ: ONE IMPL leg, one atomic commit** — lift + guards + guard-preservation tests together (`reference_lifted_reject_hidden_enforcement` engaged prospectively).

### What the PLAN owes (SPEC §13)

1. **Task decomposition** of the four production edit sites (§1.1) + the §5 test placement, under D-86-SEQ (ONE leg — a lift whose guards land later is the exact shape this row exists to avoid).
2. **TDD order:** the three RED anchors re-proven at the IMPL tip WITH FAILURE LINES READ (the phase-85 census discipline); the five masked negatives (n1/n2/n4/n6/n7) double as message-transition arms (masking arm-A string -> boot string).
3. **The guard-preservation NC roster:** the three pins STAY UNTOUCHED **and** new NCs prove nil-still-rejects on every arm (QUIC shape included) post-lift.
4. **The contract-edit text** riding ADR-0308 (`:1062` REVERSAL, `:1034`/`:1050` rosters, `:948-958` extension) + the ADR-0308 §Decision/§Consequences completion plan (strict guard 1 -> 0 at IMPL).
5. **Budget carried as a FLOOR** (~110-160 prod / ~400-680 test) with per-task enumeration; every count re-derived at the PLAN tip.
6. **Probe-shape reuse:** the eleven config shapes are DESCRIBED in SPEC §1.3/§2/§3 — regenerate them at the PLAN/IMPL tip (they lived in session scratch; ports were 47100-47103 TEMPLATE values, validate binds nothing).

### ⚠️ COUNTS AT THIS TIP — RE-DERIVE MECHANICALLY, NEVER COPY

**HOUSE RULE: every number below is a LIVE FIGURE. Re-run it in your own worktree with its negative control observed.**

- **differential fixtures 121** — numeric tail `0119-grpc-unary-trailers`, next-free `0120`.
- **`ROADMAP.md` 236 lines / 118 data rows** (row 86 `in-progress` at `:148`, all others `done`); check-(2) anchors `:196 :202 :208 :218 :224 :232`; `-family row` **95/67**; `gRPC-family row` **2**; `Operational-tooling-family row` **3**; ARM-A well-formedness flags **{119, 131} ONLY**. ⚠️ A PLAN leaves ALL of these BYTE-UNTOUCHED.
- **`DECISIONS.md` 18066** lines · **307** `^## ADR-` headings · tail **`ADR-0308` PROPOSED** · next-free **ADR-0309** (TAIL-derived; headings+1 COLLIDES at the ADR-0209 gap) · ⚠️ **strict `^> \*\*STATUS: PROPOSED` guard = 1 — ARMED, the live pointer the phase-86 IMPL disarms. A PLAN leaves `DECISIONS.md` BYTE-UNTOUCHED.**
- **`BEHAVIOR_CONTRACT.md` 5955** · **`STATE.md` 64** · **`STATE_HISTORY.md` 478** · **`BOOTSTRAP_PROMPT.md` 522** at the repo ROOT · phase dirs **127**, `REVIEW.md` **37** (standing departure, named not claimed).
- **h2spec: cite `95 tests, 94 passed, 1 skipped, 0 failed` ONLY from your own tip's run** (skip 6.9.2/2 invariant). Reference (`contrib-v1.37.2`): 95/82/1/12, RECORD-only (`CONFORMANCE_PINS.md`).
- **fuzzers 55 / 48 files** · **BackendKind tail 38** over 39 declarations (do NOT "fix") · stat call sites: assert DELTAS only (two counting forms in circulation — 208/36 ARM-1 vs 145/21 `NewCounter|NewGauge` — NEITHER corrected; state which form you ran).
- **validate surface at this tip:** `validate/validate.go` **64** · `validate/validate_test.go` **432** (zero SDS) · `cmd/envoy-go/main_test.go` **1502** (zero SDS) · `internal/tls/config.go` **529** · `internal/boot/boot.go` **373** · reject sites `config.go:387-389 :436-438 :453-455`, fetch sites `:390` + the two `NewDownstreamConfig` blocks.
- ⚠️ **STILL CONTESTED, NO NUMBER CARRIED:** the `STATE_HISTORY.md` archive-gap total · the loose `PROPOSED` matcher's whole-file count · the stat-surface absolute (1205 vs 1207 — WIDENED; only DELTAs are asserted) · the zero-test `generic/*` suite count (13 vs 14).

### ⚠️ DOCUMENTARY DEFECTS — carried forward, still deliberately NOT fixed

**`ADR-0051` §2's false gate-scope sentence** corrected ON THE RECORD by ADR-0307 (the sentence stays as history). **`ADR-0058`** cites `routerActionH2.doH2` at a dead location (`router_h2.go:254` / `h2dispatch.go:720` are real). **`ROADMAP.md` rows at lines 119/131 malformed** (ARM-A GUARD, not a fix). **`STATE.md` §Project counts FROZEN at phase 76** (do not source; do not fix). **`harness_test.go:208` port inventory STALE.** **`body.go` nolint directives INERT** · **xDS cycle guard NOT AUTOMATED** · **`wasm/doc.go:219` two errors** · **ROADMAP cites `esalaine` FIVE times** (module is `pgdad`) · **`rbac.go:50` token `F2`** (don't grep-anchor) · **root `PROGRESS.md`** stray phase-32.1 doc · ⚠️ NEW at this SPEC: `BEHAVIOR_CONTRACT.md:1062` + the `config.go`/`manager.go` consumer-roster comments are now KNOWN-STALE-PENDING — they describe the pre-86 world and are IMPL edit sites, NOT fix-now items (a PLAN leaves both files byte-untouched).

### Candidates ledger (for the FUTURE post-86 pick; re-derive EVERY cost at that tip)

- **The CONTINUATION two-sided repair** — strongest KNOWN product defect (server discard `conn.go:255-259` + client blindness + the client-side SETTINGS gap `h2/client.go:376-407`, all named in ADR-0307). h2spec MEASURED not to gate either side. Est. 2-4x row 85 (+1046 realized) — the natural next LARGE row; charter as a SPLIT phase.
- **The stat-surface recount** (1205-vs-1207 WIDENED; rides a future +0 row) · **`ssl.connection_error`** (+444 whole-`.go` floor) · **`test/conformance/grpc/`** (9/26 reachable) · **REVIEW.md restoration** (process-not-product) · **hygiene fold-ins** (`harness_test.go:208`, xDS cycle-guard) · the **~42 candidates in the six family windows**.

### ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + BOTH DOCTORING NCs YOURSELF** — record ACTUAL output.
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** Lineage 86: B (three arms reproduced; one-arm framing refuted) -> S (mechanism measured; parity surface executed; n7 found; cost floors refuted; BC:1062 rationale refuted). The PLAN's job: re-prove the RED anchors at ITS tip, stress the SPEC's task decomposition (the phase-85 PLAN found FOUR moved claims by probing), and enumerate what the SPEC still under-enumerated.
3. **LINE ANCHORS DRIFT; SYMBOL ANCHORS DID NOT — RE-VERIFY EVERY CITE AT YOUR TIP.** Key sites: `validate/validate.go:48-49` · `internal/tls/config.go:387-389 :436-438 :453-455 :390 :518` · `internal/boot/boot.go` (`NewSDSProvider`, the pre-scan) · `internal/grpcclient/grpcclient.go:108-131` (`DialContext` — lazy, cluster+H2 checks synchronous) · `manager.go:567 :573 :655` · `main.go:156`.
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Still live: SDS dial-budget (TWO packages) · `internal/cluster` `-race` outlier · `internal/httpclient` zero-value · driver-receiver port race (roster 42; `0081` binds `0.0.0.0:42039`) · the two 84.2-era flakes (cleared, unclassed) · `TestServerConn_TinyWindowDelivery` (85-IMPL, cleared, unclassed) · the REFERENCE h2spec section-8 flip (reference-side only).
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only).
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`.
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL.** **A harness's exit code is not the command's.** **`gofmt -l` never exits non-zero — gate on OUTPUT.**
8. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-86-*` or stage worktree should outlive its stage close** — expect only `master` plus your own (the SPEC's `wt-86-proto` was deleted; verify with `git worktree list`).
9. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** (a)/(b) differential 121 fixtures, full suite with `INNER_EXIT`; (c) h2spec cite only from your own run; grpc-conformance deferred in writing; proxy-wasm 10/16; (d) fuzzers 55/48; (e) `INNER_EXIT` on every differential launch AND `go test ./...`; (f) no REVIEW.md — standing departure.

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 86 (validate-sds-nil-provider)' --format='%h %s'   # 2 at this tip: BRAINSTORM + SPEC. An invented slug returns 0.
git log --grep '^phase 85 (h2spec-selector-repair)' --format='%h %s'      # 4 — a CLOSED phase, use it as a FIRING CONTROL
```
⚠️ **The count comments are LIVE FIGURES — VERIFY before relying.** Take the NEWEST line (`| head -1`).

Subagent-driven per `feedback_execution_style` where the stage's work splits cleanly; a PLAN whose probes are cheap may CONTINUE the 86-lineage inline departure — NAME it either way. Subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** and tear down docker **BY NAME**. ⚠️ **Band private ports outside BOTH `20000-31007` AND `11000-14999`, clear of the static fixture ports (10000-19172); `0081` binds `0.0.0.0:42039`.** Phase-85 used `46500-46999`; the 86 BRAINSTORM/SPEC used `47000-47014`/`47100-47103` as TEMPLATE VALUES ONLY (validate binds nothing — never bound) — **the next session that binds real ports should band from `47200-47299`.** ⚠️ **ONE WORKTREE PER AGENT preferred; explicit-path staging if shared; never an unscoped `git restore`.** Per-task `gofmt` + `golangci-lint` on touched packages (⚠️ misspell locale US). ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch.** ⚠️ **If the PLAN re-runs reference probes: docker containers BY NAME (`p86-*` suggested), `--rm`, verify gone.**

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root — observed live AGAIN at the 86 SPEC (every compound command's trailing state printed the repo root). Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — sha256 against the base blob, never by eye; work DETACHED so a failed revert cannot dirty a branch (the SPEC's prototype worktree pattern: DETACHED, measured, DELETED WHOLE — copy it).

⚠️ **THE ARCHIVE-ABSENCE GUARD — USE THE ROBUST FORM** (bullet-anchored + whole-file fixed-string, NC'd with a REAL target re-derived FROM THE FILE first; a control's own count is a LIVE FIGURE: `phase 82 … IMPL done` read **9** at this close, up from 8; `phase 83 … PLAN done` read **7**, up from 6). **Verify STRICTLY APPEND-ONLY**: `git diff --numstat` = `N 0` AND base a byte-exact PREFIX (`cmp`). (A PLAN that evicts nothing from `STATE.md` §Recent needs none of this — the five-entry cap binds only when §Current rolls a NEW stage in, which the PLAN close does: the BRAINSTORM-86 entry will be evicted or retained per the DIRECT DATE READ at that close.)

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads `PLAN for phase 86` — that IS your instruction.** §Project counts SELF-CONTRADICT §Current — do not anchor on them (frozen at phase 76).
2. `docs/envoy-go/phases/86-validate-sds-nil-provider/SPEC.md` — the decided mechanism (§1), the executed parity/reference matrices (§2-§3), the sweep (§4), placement (§5), the budget-as-floor (§8), what the PLAN owes (§13).
3. `docs/envoy-go/phases/86-validate-sds-nil-provider/BRAINSTORM.md` — the charter and the §5 refutation ledger (extended by SPEC §10).
4. `docs/envoy-go/DECISIONS.md` — the ADR-0308 §Context tail block (PROPOSED — read what the IMPL must complete); ADR-0268/ADR-0280 for the validate/SDS history.
5. `validate/validate.go` + `internal/tls/config.go:340-529` + `internal/boot/boot.go` (`NewSDSProvider`) + `internal/grpcclient/grpcclient.go:108-131` — the code the PLAN decomposes.

## After the PLAN lands — the roll-forward
Row 86 STAYS `in-progress`; `want` stays 118; `ROADMAP.md` BYTE-UNTOUCHED (verify by empty diff); `DECISIONS.md` and `BEHAVIOR_CONTRACT.md` BYTE-UNTOUCHED (a PLAN adds no ADR; strict guard STAYS 1 — the IMPL disarms it). Re-run the sentinel + NCs and record ACTUAL output (expect (1) `NOT DONE: row 86` · (2) SIX · (3) SILENT ⇒ no fire). **Roll this file to the phase-86 IMPL**, carrying: the PLAN's task list and any claims it moved (extend the refutation ledger), the sentinel state you observed, the re-derived counts at your tip, the budget-as-floor, and the flake register verbatim.
