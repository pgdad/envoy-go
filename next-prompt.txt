=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage. To use it to advance the project: read `docs/envoy-go/STATE.md` for the recorded NEXT, perform that one stage (the concrete action is spelled out below), land it, and re-roll this file to the following stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, the active phase is fully landed, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action. While ANY stage remains — as NOW: the **47.2a PLAN + IMPL are pending, then 47.2b SPEC→PLAN→IMPL** (the FINAL sub-leg that flips row 47 `done`; NO separate 47.2b BRAINSTORM — BRAINSTORM-47.2 already chartered both sub-legs), so tasks are NOT complete — do NOT create `stop`; perform the stage described below.
==============================================

RESUME a fresh session for envoy-go (router last updated 2026-06-29; the **phase 47.2a (`report_counters_as_deltas`) SPEC is DONE and LANDED** — docs-only direct-on-master; `docs/envoy-go/phases/47-stats-sink-metrics-service/SPEC-47.2a.md` authored [14-section SPEC-47.1 shape], the §11 D-MS-DELTA{,-GAUGE,-SUBJECT} + D-MS-STATS-FINAL-2 + D-MS-REJECT-2 empirical pins EXECUTED in-session live vs `envoyproxy/envoy:contrib-v1.37.2` per `reference_docker_probe_bridge_network` [decode-ran proof: 90 `StreamMetricsMessage`s, the deterministic COUNTER subset's running delta-SUM reached 7==K; the spec-document-reviewer (general-purpose) APPROVED — all 11 `bootstrap.go` citations EXACT, zero drift]). The SPEC ANCHORS the **ADR-0263 §Context DRAFT** (§13; the body lands at the 47.2a IMPL per ADR-0044). **ROW 47 STAYS `in-progress`** (flips `done` at the FINAL sub-leg 47.2b IMPL per ADR-0106 + `reference_roadmap_split_phase_row_done`). The next action is the **47.2a (`report_counters_as_deltas`) PLAN** — the TDD spine for the delta sub-leg.

HEAD will be the phase-47.2a SPEC commit atop the post-47.2-BRAINSTORM routing tip `01c25698`. **Re-read the shas with `git log --oneline -6`, do NOT assume.** Confirm `git status` clean (the untracked `.claude/` is session cruft, leave it) + master == origin/master.

## What the 47.2a PLAN IS (the next stage — author the TDD spine)
Author `docs/envoy-go/phases/47-stats-sink-metrics-service/PLAN-47.2a.md` (the PLAN-47.1 / phase-44/45/46 ~7–9-task TDD-spine shape; `superpowers:writing-plans`). The SPEC (`SPEC-47.2a.md`) is the charter — read it first (esp. §3 the delta transform design, §8 the `0090` differential, §10 the per-task structure sketch, §11 the executed pins, §12 the PLAN/IMPL D-questions, §13 the ADR-0263 §Context draft). The PLAN RESOLVES the §12 D-questions with real LoC + concrete code seams:
- **D-MS-SPLIT-2** — the FINAL ADR-0045 re-check with real LoC (a 47.2a sub-split is NOT anticipated — one bool + one reject lift + a per-sink delta transform on the COUNTER arm + the main thread + one fixture).
- **D-MSD-PLACEMENT** — WHERE the per-sink `map[string]uint64` last-value delta state + transform lives: a sink-owned `Submit`/writer transform vs a per-sink `snapshotter` the `Flusher` invokes vs a small `delta.go` helper in `internal/statssink`. **HARD CONSTRAINT the SPEC pins, the PLAN realizes:** NO in-place mutation of the shared absolute `snapshot()` batch slice (`flusher.go:47` fans the SAME slice to every sink at `:48`–`:49`) — the delta transform builds the sink's OWN COUNTER families (GAUGE families may be shared as-is, un-transformed). Confirm the acyclic-import check holds (transform stays inside `internal/statssink`).
- **D-MSD-RECEIVER-SUM** — the `test/helpers/metricsservice` delta-SUM surface shape (a parallel `sums map[string]float64` + `FamilySum(name) (float64, bool)`, vs a richer accumulator); ADDITIVE (the existing last-seen `Family()` STAYS for `0089`'s non-regression); the `0090` converge-poll predicate `FamilySum(subset)==K`.
- **D-MSD-FIRST-FLUSH-IMPL** — confirm the empty-map zero-default needs NO explicit "first flush" branch (a map read of an absent key yields 0 = `current−0` = absolute; the frozen Registry's fixed counter set, AMEND-MSD-FIRST-FLUSH-ABSOLUTE).
- **D-MSD-BREAK-SET** — the final subject-side deliberate-break roster for `0090` (the load-bearing breaks: emit-absolute-instead-of-delta + skip-the-`last[name]=cur`-store, both EXPLODE the running SUM; a gauge-delta break is UNIT-test-only since gauges are unasserted in the differential).

The PLAN spine (the SPEC §10 sketch): PROGRESS + baselines + the FINAL ADR-0045 re-check → the `StatsSinkConfig.ReportCountersAsDeltas` field + the `bootstrap.go:446` reject LIFT + the parse-accept test [TDD; replace the 47.1 deltas-reject test; the `emit_tags_as_labels` reject test STAYS] → the per-sink delta transform on the COUNTER mapping arm [TDD table-driven; Counter-only; first=absolute; idle=0; non-negative; the no-shared-slice-mutation constraint] → the `cmd/envoy-go/main.go` thread (the bool → the delta sink/snapshotter) + the value-stability guard [false ⇒ 47.1-identical] → the `test/helpers/metricsservice` `FamilySum` surface [additive, 0089 non-regress] → the `0090-stats-sink-metrics-service-deltas` cross-side EXACT fixture [clone the AUTHORITATIVE `0089/driver/driver.go`, NOT the stale `0089/README.md`; two per-side receivers + hard `Close()`; poll-to-converge the delta-SUM == K; reconcile the 0089 README] → `0090` deliberate breaks + 20/20 flake + the FULL-package `internal/statssink` `-race` → full 92-dir differential + the six-gate + ADR-0263 body + BEHAVIOR_CONTRACT + STATE/ROADMAP. **DOCS-ONLY direct-on-master** (the family-row PLAN precedent — phases 44/45/46/47.1 PLANs committed direct on master; a fresh worktree off master per `feedback_git_worktrees` is the IMPL's mode, not the PLAN's). Run the **plan-document-reviewer** loop (the 46.2/47.1 PLAN precedent — fold any Critical/Important, re-confirm buildability of each per-task `go build ./...` gate). **ROW 47 STAYS `in-progress`**. After it lands, ROLL THIS FILE to the **47.2a IMPL**.

## What the 47.2a IMPL IS (the stage AFTER the PLAN — subagent-driven in a worktree)
Execute the PLAN-47.2a TDD spine subagent-driven (`feedback_execution_style` · `superpowers:subagent-driven-development`) in a FRESH worktree off master (`feedback_git_worktrees`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit + cleans leak files + re-runs the six-gate + the FULL-package `-race` (the per-sink delta map + the Flusher ticker + the sink writer goroutine are background mutators — `reference_full_suite_race_after_background_mutator`) + does the `0090` deliberate breaks ITSELF (`-count=1` per `reference_differential_break_protocol_count1`; `-run 'TestDifferential/0090'` NEVER bare per `reference_differential_run_selector`) + squashes + pushes at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). ANCHORS **ADR-0263** (§Decision/§Consequences body lands here per ADR-0044). Anticipated counts: stat **1200** (+0) / fixtures **91 → 92** (`0090-stats-sink-metrics-service-deltas`) / fuzzers **50** / BackendKind **38** / DECISIONS **ADR-0262 → ADR-0263**; NO new package, NO new go.mod module. **ROW 47 STAYS `in-progress`** (47.2a is NOT the final sub-leg). After it lands, ROLL THIS FILE to the **47.2b SPEC**.

## What 47.2b IS (the SECOND-and-FINAL sub-leg — opens at its own SPEC AFTER 47.2a lands)
**47.2b = `emit_tags_as_labels=true`** (anticipated **ADR-0264**): SN-rule tag→`LabelPair` extraction (the dotted-name tag SEGMENTS become `MetricFamily.metric[].label[]` pairs instead of inlined into the name), reusing the `internal/stats/name.go` SN1–SN9 extractors (`flattenToProm` at `:39`); lift the 47.1 reject at `bootstrap.go:449`; add `StatsSinkConfig.EmitTagsAsLabels`; the `0091-stats-sink-metrics-service-labels` LabelPair differential (fixtures **92 → 93**). The exact emitted name/label-key form is a 47.2b SPEC live-probe pin (D-MS-LABEL); the refactor-shared-`name.go`-core-vs-parallel-extractor decision is D-MS-LABEL-REUSE. **47.2b IMPL FLIPS ROW 47 `done`.** NO separate 47.2b brainstorm — BRAINSTORM-47.2 already chartered both sub-legs.

## Counts at the 47.2a SPEC (docs-only — UNCHANGED; re-verify, do NOT assume)
stat surface **1200** (non-H2 **1196**) / fixtures **91** (tail `0089-stats-sink-metrics-service`; `ls -d test/fixtures/*/ | wc -l`) / fuzzers **50** (`grep -rh '^func Fuzz' --include='*.go' . | wc -l`) / BackendKind **38** (`H2GoawayResponder = 38`) / DECISIONS **ADR-0262** (next-free **ADR-0263**); `github.com/prometheus/client_model v0.6.1` direct (landed at 47.1 — `LabelPair` already available for 47.2b); `go-control-plane/envoy v1.32.4`; `internal/statssink` is the package both sub-legs modify. Anticipated: 47.2a IMPL → fixtures **92** (`0090`), DECISIONS **ADR-0263**; 47.2b IMPL → fixtures **93** (`0091`), DECISIONS **ADR-0264**, row 47 `done`. Both legs: stat **1200 UNCHANGED** (+0 self-stats preserved) / fuzzers **50** / BackendKind **38** / NO new package / NO new go.mod module.

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** The next stage is the 47.2a PLAN (lifecycle-state 2 → 3 for the 47.2a sub-leg). After it lands, ROLL THIS FILE to the 47.2a IMPL.
- **SPEC/PLAN = docs-only direct-on-master** (the family-row precedent); the IMPL is subagent-driven (`feedback_execution_style` · `superpowers:subagent-driven-development`) in a worktree, subagents commit LOCALLY only (`feedback_subagents_no_push`), the CONTROLLER squashes + pushes at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`).
- **Process anchors:** ADR-0106 (per-leg rows — row 47 flips `done` at the 47.2b IMPL) · ADR-0045 (sub-split — 47.2 SUB-SPLIT into 47.2a/47.2b at the BRAINSTORM; per-sub-leg FINAL gate re-checked at each SPEC/PLAN [D-MS-SPLIT-2]) · ADR-0044 (ADR §Decision+§Consequences-at-IMPL — ADR-0263 §Context DRAFTED at the 47.2a SPEC §13, body at the 47.2a IMPL) · ADR-0080 (strict-reject — 47.2a LIFTS the deltas reject `:446`, KEEPS the rest; 47.2b LIFTS the tags-as-labels reject `:449`) · ADR-0059 (the in-tree `Registry`/`Walk`/`Freeze`) · ADR-0060 (histograms deferred — still moot) · ADR-0061 (the SN1–SN8 Prometheus name/label rules — the 47.2b tag-extraction substrate) · ADR-0064 (the `stats_sinks[]` silent-ignore — 47.1 LIFTED the metrics_service sink) · ADR-0158 (the `grpcclient.Dialer` typed-wrapper) · ADR-0262 (the 47.1 anchor) · ADR-0263 (ANCHORED at the 47.2a SPEC §13 §Context draft, §Decision/§Consequences body at the 47.2a IMPL).
- **Differential discipline** (for the 47.2a `0090` fixture): the TWO-per-side-receiver pattern + the hard `Close()` teardown + a NEW per-name delta-SUM surface (`reference_periodic_sink_differential_two_receivers` — clone the authoritative `0089/driver/driver.go`, NOT the stale README); `-run 'TestDifferential/0090'` NEVER bare (`reference_differential_run_selector`); `-count=1` on every break + `-race` (`reference_differential_break_protocol_count1`); the Docker bridge + decode-ran proof (`reference_docker_probe_bridge_network`); the gRPC metrics receiver is driver-owned NOT a BackendKind (`reference_differential_grpc_receiver_driver_owned`); assert the aggregated delta-SUM payload NOT framing (`reference_streaming_sink_differential_framing`); the live reference is the wire truth (`reference_wire_format_both_sides_see_same_bytes`); one fixture dir = ONE bootstrap config = ONE runner branch (`reference_differential_fixture_dispatch_constraint` — 0090 and 0089 CANNOT share a dir); the Flusher + sink writer goroutine + the NEW per-sink delta map are background mutators ⇒ FULL-package `-race` (`reference_full_suite_race_after_background_mutator`).

## Read first (in order)
1. THIS FILE — the cold-start router (the 47.2a SPEC is DONE + LANDED; row 47 `in-progress`; the next action is the 47.2a PLAN — `report_counters_as_deltas` TDD spine).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 47.2a SPEC done`) + the recorded NEXT (the 47.2a PLAN) + the counts (1200 / 91 / 50 / 38 / ADR-0262).
3. `docs/envoy-go/phases/47-stats-sink-metrics-service/SPEC-47.2a.md` — the 47.2a charter: §3 the delta transform design (per-sink `map[string]uint64`, Counter-only, the no-shared-slice-mutation HARD CONSTRAINT), §8 the `0090` delta-SUM differential + the `FamilySum` receiver surface, §10 the per-task structure, §11 the EXECUTED D-MS-DELTA{,-GAUGE,-SUBJECT}+STATS-FINAL-2+REJECT-2 pins, §12 the PLAN/IMPL D-questions (D-MSD-PLACEMENT, D-MSD-RECEIVER-SUM, D-MSD-FIRST-FLUSH-IMPL, D-MSD-BREAK-SET, D-MS-SPLIT-2), §13 the ADR-0263 §Context draft.
4. `docs/envoy-go/phases/47-stats-sink-metrics-service/BRAINSTORM-47.2.md` — the 47.2 charter (the §Q-split, the §2 design decisions, the §6 fixture envelope, the §10 empirical-pin obligations).
5. `docs/envoy-go/DECISIONS.md` — **ADR-0262** (the 47.1 anchor — its §Decision/§Consequences incl. the deferred set 47.2 picks up: deltas [47.2a] / tags-as-labels [47.2b] + the still-deferred histogram_emit_mode / stats_flush_on_admin / sibling sinks that STAY strict).
6. The 47.1 as-built code 47.2a modifies: `internal/statssink/mapping.go` (`snapshot` `:22` — the Counter arm `:26`/`:31` the delta transform changes; the Gauge arm `:35` UNCHANGED), `internal/statssink/flusher.go` (`flushOnce` `:46` — the per-sink fan-out `:47`–`:49`, the no-in-place-mutation constraint), `internal/statssink/sink.go` (`MetricsServiceSink` `:60` — mapping-agnostic; where the per-sink delta state may live), `internal/bootstrap/bootstrap.go` (`StatsSinkConfig` `:272`, `parseMetricsServiceConfig` `:431`, the deltas reject `:446` to LIFT, the append `:455`), `internal/stats/counter.go` (`Load` `:30`; monotone — `Inc`/`Add` only, no decrement), `cmd/envoy-go/main.go` (the sink construction wiring to thread the bool).
7. `test/fixtures/0089-stats-sink-metrics-service/{driver/driver.go,envoy.yaml,envoy-go.yaml,expectations.yaml}` (the AUTHORITATIVE differential template 0090 clones — two per-side receivers + hard `Close()` + the subset `{cluster.c_backend.upstream_rq_total, http.hcm_local.downstream_rq_total, http.hcm_local.downstream_rq_2xx}`, `numReq=7`; the README is STALE) + `test/helpers/metricsservice/metricsservice.go` (the receiver to extend with `FamilySum` — currently last-seen-wins `:160`).
8. `docs/envoy-go/ROADMAP.md` — row 47 (`stats-sink-metrics-service`, `in-progress`) + the Observability family-progress note.
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_subagents_no_push`, `feedback_subagent_autocommit_claudemd`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_detach`/`_path_targeting`, `reference_roadmap_split_phase_row_done`, `reference_stats_sink_metrics_service_probe`, `reference_periodic_sink_differential_two_receivers`, `reference_differential_grpc_receiver_driver_owned`, `reference_streaming_sink_differential_framing`, `reference_docker_probe_bridge_network`, `reference_full_suite_race_after_background_mutator`, plus the differential-discipline references above).
