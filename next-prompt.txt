START a fresh cold-start session for envoy-go — **the phase-40.3 (`outlier-detection-statistical`: the `success_rate` + `failure_percentage` statistical detectors + the per-interval cross-host aggregation runtime — the FIRST outlier goroutine) SPEC is DONE, LANDED on master (squash commit `154165f`), and PUSHED to `origin/master`; the next lifecycle step is the phase-40.3 PLAN** (`superpowers:writing-plans` — decompose SPEC §10 into bite-sized TDD tasks [~12–14], resolve the §12 D-S40.3-1..8 PLAN/IMPL questions, run the FINAL ADR-0045 split-gate re-check [anticipated NO FURTHER SPLIT — single flat 40.3 leg], plan-document-reviewer) OR a new subject. Confirm the tip via `git log --oneline -5` (HEAD is the docs-only next-prompt roll-forward sitting on the substantive phase-40.3 SPEC squash `154165f`, whose parent is the phase-40.2 IMPL squash `4aed3e1`; the prior substantive tips are the phase-40.2 PLAN `104daf2` and SPEC `2919a3a`). Counts at the 40.3 SPEC (UNCHANGED — SPEC is docs-only): stat surface **1141** / fixtures **73** / fuzzers **42** / BackendKind tail **35** (`HTTP503Responder`) / DECISIONS tail **ADR-0246**, next-free **ADR-0247**.

## What just landed (phase 40.3 SPEC — the durable record is `docs/envoy-go/phases/40.3-outlier-detection-statistical/{SPEC,README}.md`; read those, not this summary)

The SPEC (authored directly — the BRAINSTORM is DONE for the family) charters the THIRD (FINAL) leg of the phase-40 by-detector-class split: the STATISTICAL detectors over the 40.1/40.2 substrate (ADR-0245/0246, REUSED UNCHANGED). The §11 D-OD3-* empirical pins were EXECUTED IN-SESSION (2026-06-18) live against `envoyproxy/envoy:contrib-v1.37.2` per `reference_docker_probe_bridge_network` (a 6-host {5×200, 1×503} bridge-net cluster; the full 20-name reference `outlier_detection.*` roster scraped; `--mode validate` reject probes; a 3-host retro probe; `upstream_rq_total>0` verified). spec reviewer APPROVE-WITH-MINORS (zero Blocking/Major — every proto default/field-number/reject-string/stat-name/threshold-arithmetic claim verified against ground truth; the population-stddev pin + the `0072` failure_percentage-request-volume vacuousness caveat + the explicit next-free port 19161 applied).

**The detectors (SPEC §1/§3):** `success_rate` (each `interval`, eject hosts below `mean − (success_rate_stdev_factor/1000)×stddev` — POPULATION stddev — across the eligible hosts; gated by `success_rate_minimum_hosts`/`success_rate_request_volume`; **ejects-by-default**, `enforcing_success_rate` 100) + `failure_percentage` (eject hosts with `failure% ≥ failure_percentage_threshold` (85); gated similarly; **detect-only-by-default**, `enforcing_failure_percentage` 0). Both **SWEEP-driven** on the periodic `interval` (NOT per-request) via a per-cluster aggregation goroutine (the 39.1 `StartHealthChecks`/`Drain` lifecycle precedent — `Manager.StartOutlierDetection`/`Drain`), over windowed per-host `(intervalTotal, intervalSuccess)` counters fed by the EXISTING `RecordUpstreamResult` seam (which already fires on EVERY request — NO router touch). The 40.1/40.2 `tryEject` + ejection dimension + `available` predicate + cap + lazy un-eject + the consecutive detectors are REUSED UNCHANGED.

**Load-bearing SPEC facts (the §1.1 AMEND-OD3-1..7 + the §11 pins):**
- **+8 stat names → 1149** (SPEC §7): `ejections_{detected,enforced}_{success_rate,failure_percentage,local_origin_success_rate,local_origin_failure_percentage}`, registered UNCONDITIONALLY (the 40.2 precedent). The EXTERNAL `success_rate` + `failure_percentage` LOGIC is implemented (4 driven); the LOCAL-ORIGIN statistical variants' LOGIC is DEFERRED (4 names emit 0 for surface parity — a recorded departure). The 3 legacy aliases (`ejections_total`/`ejections_consecutive_5xx`/`ejections_success_rate`) stay departures (envoy-go emits 17 of the reference's 20 outlier names). D-S40.3-8: the PLAN confirms the register-all-8 choice doesn't break any fixture asserter.
- **The per-detector double-count is NOT replicated** (AMEND-OD3-3): when BOTH detectors flag one host in a sweep the reference bumps `ejections_active`/`enforced_total` by 2; envoy-go's `tryEject` CAS-once-only ejects a host once → the fixtures ISOLATE a single enforcing detector (`0072` default enforcing ⇒ success_rate enforces + failure_percentage detect-only; `0073` `enforcing_success_rate: 0` ⇒ failure_percentage the sole enforcer).
- **Retro-impact NONE** (AMEND-OD3-7): `0069`/`0070`/`0071` are 3-host < `minimum_hosts` 5 → the statistical detectors don't run; the +8 names emit 0 on both sides (not asserted) → NO fixture change.
- **Six new reject arms** (SPEC §6): five `enforcing_*`/`failure_percentage_threshold > 100` + `max_ejection_time <= 0s`. `success_rate_stdev_factor: 0` is ACCEPTED (no reject).
- **Threshold positivity** (AMEND-OD3-5): the success-rate threshold can go negative with few hosts/high variance → `0072` needs enough good hosts (K=5 → threshold ≈12.5% > 0, probe-proven) OR a tuned `stdev_factor`. The `0072` `ejections_detected_failure_percentage >= 1` cross-assertion is LIVE only if the bad host clears `failure_percentage_request_volume` (default 50) before the success_rate detector ejects it — single-source BOTH detectors' volume floors (D-S40.3-5) or drop that assertion from `0072`.
- **ZERO new BackendKind / fuzzers / packages / go.mod modules** (reuse `HTTP503Responder` 35 + `HTTPEcho` + `PerHostBackendKind`). Differentials `0072-outlier-detection-success-rate` (port 19161) + optionally `0073-outlier-detection-failure-percentage` (port 19162; D-S40.3-6 two-dirs-vs-fold).

**Anticipated at the 40.3 IMPL:** fixtures 73 → 74 or 75 (`0072` [+ `0073`]), stat surface 1141 → 1149 (+8), DECISIONS tail ADR-0246 → ADR-0247 (next-free ADR-0248), BackendKind tail 35 (UNCHANGED), fuzzers 42 (UNCHANGED), ZERO new packages/modules. **Landing 40.3 consumes the 3-leg by-detector-class split → ROADMAP row 40 flips `in-progress → done`** at the 40.3 IMPL six-gate (`reference_roadmap_split_phase_row_done`; NO parent rollup per ADR-0106; the Upstream-robustness family STAYS OPEN — 3 candidates remain: circuit breakers / retries + hedging / per-protocol connection pooling).

## Honor throughout (the PLAN discipline)

- **Lifecycle = one stage per session:** the PLAN (`superpowers:writing-plans` — decompose SPEC §10 into bite-sized TDD tasks; resolve §12 D-S40.3-1..8; the FINAL ADR-0045 split-gate re-check; plan-document-reviewer). `feedback_git_worktrees` · `feedback_push_to_origin` (after a clean local merge on master with the docs landing, push without asking) · `feedback_subagents_no_push`.
- **ADR/process:** ADR-0044 (ADR-0247 §Context already drafted in SPEC §13; §Decision/§Consequences land IN-PLACE at the 40.3 IMPL) · ADR-0052 (six-gate at the IMPL) · ADR-0080 (byte-stable rejects) · ADR-0045 (the split-gate, re-checked at the PLAN — anticipated NO FURTHER SPLIT) · ADR-0106 + `reference_roadmap_split_phase_row_done` (row 40 flips `done` at the 40.3 IMPL — the FINAL leg) · ADR-0227 (`contrib-v1.37.2`).
- **At the IMPL (forward):** `reference_differential_run_selector` (`-run 'TestDifferential/0072'`) · `reference_differential_break_protocol_count1` (`-count=1` on every break run) · `reference_health_check_propagation_warmup` (poll-`ejections_active`-to-converge + warmup; widen deadlines not assertions) · `reference_docker_probe_bridge_network` · `reference_fixture_workload_constant_desync` (single-source the workload constants incl. both volume floors) · `reference_differential_asserter_dispatch` (cross-side ⇒ `StatsAsserter`) · `feedback_pertask_gofmt_lint` · `reference_percent_cap_cross_multiply` (the reused cap).

## Read first (in order)

1. THIS FILE — the post-phase-40.3-SPEC cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 40.3 (outlier-detection-statistical) SPEC done`; the count footer (1141 / 73 / 42 / 35 / ADR-0246, next-free ADR-0247); the lifecycle routing (next-skill = the 40.3 PLAN).
3. `docs/envoy-go/phases/40.3-outlier-detection-statistical/SPEC.md` (the §1.1 AMEND-OD3-1..7; the §3 architecture — the parse extension + the windowed counters + the per-interval aggregation sweep + the goroutine lifecycle + the concurrency model; the §5 proto roster fields 6–9/16–20/`interval`; the §6 six reject arms; the §7 stat surface +8; the §8 `0072`/`0073` fixtures; the §11 D-OD3-* live pins; the §12 PLAN/IMPL D-questions; the §13 ADR-0247 §Context draft) + the README.
4. `internal/cluster/{outlier.go,health.go,manager.go,cluster.go}` (the `outlierConfig`/`parseOutlierDetection`/`outlierDetector`/`registerStats`/`tryEject`/`record` the PLAN extends; the `hostHealth` the windowed counters land on; the `StartHealthChecks`/`Drain`/`hcWG`/`hcCancel` lifecycle the sweep goroutine mirrors) + `test/fixtures/0069-outlier-detection-consecutive-5xx/driver/` (the StatsAsserter + poll-to-converge template the `0072`/`0073` drivers reuse) + `test/differential/{fixture/fixture.go,runner_test.go}` (`HTTP503Responder` 35 + `PerHostBackendKind`).
5. `docs/envoy-go/DECISIONS.md` (ADR-0245 + ADR-0246 [the substrate] + the process ADRs) + `docs/envoy-go/BEHAVIOR_CONTRACT.md` (the `### Cluster — passive health (outlier detection)` subsection the 40.3 IMPL extends + the stat-surface block 1141 → 1149).
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`.
