START a fresh session for envoy-go. **The phase-42.2a (`per_try_timeout`) SPEC is COMPLETE, spec-document-reviewer APPROVED, and LANDED on master — `docs/envoy-go/phases/42.2-hedging/SPEC.md`.** The §11 **D-H-{PROTO,TIMEOUT,RETRY,STATS,DIFFERENTIAL}** per-try-timeout pins were EXECUTED IN-SESSION (2026-06-21) live against `envoyproxy/envoy:contrib-v1.37.2` per `reference_docker_probe_bridge_network`. The next step is the **42.2a PLAN** (the lifecycle valve is SPEC → PLAN). Confirm the latest master is the 42.2a SPEC commit via `git log --oneline -3` (a NEW sha — do NOT assume a literal value). Counts at tip (UNCHANGED at the SPEC): stat surface **1173** / fixtures **77** / fuzzers **42** / BackendKind tail **36** (`BlockingHoldResponder`) / DECISIONS tail **ADR-0249**, next-free **ADR-0250**.

## THE load-bearing finding (drives the leg)

A per-attempt-timeout (`RetryPolicy.per_try_timeout`, field 3) produces a synthesized **504** and is RETRIED iff `retry_on` ∈ {`5xx`, `gateway-error`, `retriable-status-codes`∋504, **`reset`**} — NOT `connect-failure`-alone, NOT empty (live single-slow-host probe: 5xx/gateway-error/reset → 3 attempts; connect-failure/none → 1). A genuine dial-refusal retries under BOTH `reset` AND `connect-failure` (dead-backend probe, delta=2 each), so envoy-go's 42.1 `connect-failure`==`reset`==`retryConnectFail` fusion is faithful for dial-failures but cannot distinguish the per-try-timeout case ⇒ **42.2a SPLITS `reset` into its own `retryReset` bit** (the `localOrigin` dial-failure path retries under `retryConnectFail|retryReset`, preserving 42.1; the per-try-timeout predicate = `matches(504,false) || retryReset`). envoy-go does NOT implement the global `RouteAction.timeout` (code-confirmed) ⇒ `per_try_timeout` is the project's FIRST request-scoped deadline; no `min(per_try, global)` clamp. See the SPEC §1.1 (AMEND-PT1..7), §3, §11.

## NEXT STEP — write the phase-42.2a PLAN

Author `docs/envoy-go/phases/42.2-hedging/PLAN.md` (42.2a shares the `42.2-hedging/` dir; 42.2b gets its own `42.2b-…/` dir) as a bite-sized TDD spine in the phase-41/42.1 PLAN shape (`superpowers:writing-plans`). Resolve the §12 **D-S422A-1..7** PLAN questions: (1) the executor loop-placement of the per-try-timeout test relative to the 42.1 `matches`/`num_retries`/`tryAcquireRetry` ledger (a timed-out attempt counts toward `num_retries` like any retriable outcome — the reference confirms); (2) the negative-`per_try_timeout` disposition (a thin byte-stable reject `route: %q: retry_policy: per_try_timeout must not be negative` vs clamp-`≤0`-to-no-bound); (3) the 504-synthesis header set (REUSE `localReplyHeaders(0)` H1 / `h2LocalReplyHeaders()` H2; empty body); (4) `per_try_timeout` threaded onto `RetryPolicy` via a widened `NewRetryPolicy` vs a setter; (5) `0076` constants (N / T [200–500ms candidate] / topology / `refContainerListenerPort` next-free after `0075`'s 19164; H1-only cross-side + the H2 discriminator unit-tested); (6) the exact surface integer (1173 → ~1181 = +6 per NEW retry cluster + +1 per EXISTING [0075's two]); (7) the FINAL ADR-0045 split-gate re-check (anticipated NO further split — ~120–200 LoC / ~8–11 tasks). Mirror the §10 anticipated spine. Work in a WORKTREE (`feedback_git_worktrees`); commit docs-only; the plan-document-reviewer loop then the user-review gate.

**The 42.2a envelope (SPEC §3):** `RetryPolicy.perTryTimeout time.Duration` (parsed from `per_try_timeout`; `≤0` ⇒ no bound) carried on `RetryPolicy` (`retry.go`), parsed in `buildRouterActionWithVH` (`hcm/config.go`); in BOTH `retryExecutorH1`/`retryExecutorH2` each loop iteration wraps the driver call: `attemptCtx, cancel := context.WithTimeout(ctx, rp.perTryTimeout)` (when `>0`), `cancel()` per attempt; AFTER the driver returns, the discriminator `ctx.Err()==nil && errors.Is(attemptCtx.Err(), context.DeadlineExceeded)` ⇒ a per-try-timeout (override the H1 502 / H2 `Status:0` to a synthesized **504**, `IncUpstreamRqPerTryTimeout()`, retriable per `perTryTimeoutRetriable()`). The driver code is UNCHANGED (the H1 driver already `SetDeadline`s the socket `router.go:659-661`; the H2 driver already returns `Status:0` on a ctx deadline `router_h2.go:111`). The `reset`-split (`parseRetryOn` + `matches`) is the only 42.1-classifier surgery (`retryReset` = the 5th `retryOnBits` value; `uint8` has room, NO widening). The `upstream_rq_per_try_timeout` counter = the 6th `retryStats` member via `EnsureRetryStats` (`cluster.go:254`). Byte-identical when `per_try_timeout ≤ 0`.

## Exit posture (anticipated at the 42.2a IMPL — verify at the PLAN)

stat surface 1173 → **~1181** (+1 `upstream_rq_per_try_timeout` per retry cluster, scoped — PLAN-firms the integer) · fixtures 77 → **78** (`0076-per-try-timeout`) · fuzzers **42** · BackendKind tail **36** (UNCHANGED — REUSE `BlockingHoldResponder`) · DECISIONS tail ADR-0249 → **ADR-0250** (next-free ADR-0251) · ZERO new packages/modules. **ROADMAP row 42 STAYS `in-progress`** (flips `done` only when ALL THREE legs 42.1+42.2a+42.2b land — ADR-0106 + `reference_roadmap_split_phase_row_done`). After 42.2a lands (PLAN → IMPL), the next subject is the **42.2b (hedging) BRAINSTORM** (the concurrent `hedgeExecutor` + `hedge_on_per_try_timeout` + the fan-out, over the 42.2a deadline — ADR-0251).

## Honor throughout (the PLAN discipline)

- **Lifecycle = one stage per session.** `feedback_git_worktrees` · docs-only commit · `feedback_push_to_origin` (push after a clean docs commit on master) · the plan-document-reviewer loop (precisely-crafted review context, never session history) · the user-review gate before the IMPL.
- **Differential discipline (for the `0076` PLAN task):** `reference_concurrency_differential_release_barrier` (the held `/__release` backend + sequential-per-side; the per-try-timeout fires deterministically while held — the T delay is feature-inherent, NOT a flake-sleep) · `reference_round_robin_offset_randomized` (single held host ⇒ offset-irrelevant cross-side-exact — no exposure) · `reference_differential_run_selector` (the `0076` subtest is `TestDifferential/0076-…`) · `reference_differential_break_protocol_count1` (`-count=1` deliberate breaks) · `reference_docker_probe_bridge_network` (any PLAN re-probe needs Docker + `contrib-v1.37.2` over a shared bridge) · `reference_fixture_workload_constant_desync` (single-source N/T).
- **ADR/process:** ADR-0044 (§Context at the SPEC, §Decision/§Consequences IN-PLACE at the IMPL) · ADR-0080 (byte-stable rejects — the negative-`per_try_timeout` arm) · ADR-0227 (`contrib-v1.37.2`) · ADR-0249 (the 42.1 retry substrate 42.2a extends — the `retryExecutor`, `matches(status, localOrigin)`, `EnsureRetryStats`, the H2 `Status:0` ctx-cancel-not-retried path) · ADR-0250 (the 42.2a per-attempt-timeout architecture — §Context drafted at SPEC §13) · ADR-0106 + `reference_roadmap_split_phase_row_done` (row 42 flips `done` only when ALL THREE legs land).

## Read first (in order)

1. THIS FILE — the cold-start router (the 42.2a SPEC is DONE + reviewer-APPROVED + LANDED; the next step is the 42.2a PLAN).
2. `docs/envoy-go/phases/42.2-hedging/SPEC.md` — the §1.1 AMEND-PT1..7, the §3 architecture (the child-ctx + the discriminator + the synthesized-504 + the `reset`-split), the §10 anticipated spine, the §11 D-H-* pin table, the §12 D-S422A-* PLAN questions, the §13 ADR-0250 §Context draft.
3. `docs/envoy-go/phases/42-retries-and-hedging/PLAN.md` — the 13-task TDD spine to mirror (the bite-sized step granularity; the deliberate-break + 20-run-flake task shape; the six-gate task).
4. `internal/filter/http/router/retry.go` (the 42.1 `retryExecutorH1`/`retryExecutorH2` + `parseRetryOn` [the `connect-failure`/`reset` fusion to split] + `matches` + `NewRetryPolicy`) + `router.go:659-661` (the H1 `SetDeadline`) + `router_h2.go:111` (the H2 `Status:0` ctx-cancel sentinel) + `cluster.go:254` (`EnsureRetryStats`, the 5-counter `retryStats`).
5. `docs/envoy-go/STATE.md` — active-phase `phase 42.2a (per_try_timeout) SPEC done`; the count figures (1173 / 77 / 42 / 36 / ADR-0249, next-free ADR-0250).
6. `docs/envoy-go/DECISIONS.md` (the ADR-0249 entry — the 42.1 retry substrate; next-free ADR-0250) + `docs/envoy-go/BEHAVIOR_CONTRACT.md` (the `### Route — request recovery (retries)` subsection 42.2a extends).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `reference_concurrency_differential_release_barrier`, `reference_round_robin_offset_randomized`, `reference_docker_probe_bridge_network`).
