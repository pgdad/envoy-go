=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage. To use it to advance the project: read `docs/envoy-go/STATE.md` for the recorded NEXT, perform that one stage (the concrete action is spelled out below), land it, and re-roll this file to the following stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, the active phase is fully landed, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action. While ANY stage remains — as NOW: the **47.2a SPEC + PLAN + IMPL are pending, then the full 47.2b BRAINSTORM→SPEC→PLAN→IMPL** (the FINAL sub-leg that flips row 47 `done`), so tasks are NOT complete — do NOT create `stop`; perform the stage described below.
==============================================

RESUME a fresh session for envoy-go (router last updated 2026-06-29; the **phase 47.2 (deltas + tags-as-labels) BRAINSTORM is DONE and LANDED** — docs-only direct-on-master; `docs/envoy-go/phases/47-stats-sink-metrics-service/BRAINSTORM-47.2.md` authored [12-section family-row shape], the BRAINSTORM-reviewer [general-purpose] APPROVED with every load-bearing citation disk-verified). The brainstorm SUB-SPLIT 47.2 by-concern under the ADR-0045 gate (re-decided live with the human) into **47.2a** (`report_counters_as_deltas=true` — anticipated **ADR-0263**) then **47.2b** (`emit_tags_as_labels=true` — anticipated **ADR-0264**, the leg that flips row 47 `done`). Phase 47 is the **FOURTH Observability-family row** (`stats-sink-metrics-service`); the family STAYS OPEN. **ROW 47 STAYS `in-progress`** (flips `done` at the FINAL sub-leg 47.2b IMPL per ADR-0106 + `reference_roadmap_split_phase_row_done`). The next action is the **47.2a (`report_counters_as_deltas`) SPEC** — the FIRST sub-leg.

HEAD will be the phase-47.2 BRAINSTORM commit atop the post-47.1-IMPL routing tip `924bfc8e`. **Re-read the shas with `git log --oneline -6`, do NOT assume.** Confirm `git status` clean (the untracked `.claude/` is session cruft, leave it) + master == origin/master.

## What 47.2a IS (the FIRST sub-leg — the SPEC charters it at depth)
**47.2a = `report_counters_as_deltas=true`** (anticipated **ADR-0263**), per the §Q-split (ADR-0045) recorded at the BRAINSTORM-47.2:
- **The delta state:** a PER-SINK `map[string]uint64` of last-flushed Counter values keyed by full dotted name. Each flush emits `current − last` per Counter (then stores `current`), instead of the absolute `Load()` value the 47.1 `mapping.go:31` emits. **Counter-only** — Gauges stay absolute (`mapping.go:35` arm UNCHANGED; the proto field is named `report_counters_as_deltas`). The delta-aware snapshot is a sink-OWNED (or sink-parameterized) transform, NOT a mutation of the shared `Flusher.snapshot` fan-out (two sinks flush/reconnect independently → per-sink state).
- **The lift:** lift the 47.1 STRICT-REJECT at `internal/bootstrap/bootstrap.go:446` (`report_counters_as_deltas:true`); add a `ReportCountersAsDeltas bool` to `StatsSinkConfig` (`:272`, today a one-field `{ClusterName string}`); thread it through `cmd/envoy-go/main.go` sink construction. KEEP every OTHER 47.1 reject strict.
- **Live-probe (D-MS-DELTA, executed at THIS SPEC):** PIN the reference's delta semantics (contrib-v1.37.2, `reference_docker_probe_bridge_network`) — first-flush behavior (absolute? zero-delta? `current−0`?); reset-on-flush vs monotonic; whether a never-incremented (zero) counter is emitted under deltas; delta-vs-previous-FLUSH or previous-SEND; type stays `COUNTER`. + D-MS-DELTA-GAUGE (Gauges stay absolute) + D-MS-DELTA-SUBJECT (the counter subset whose per-flush deltas SUM to K cross-side) + D-MS-STATS-FINAL-2 (+0 self-stats re-confirm) + D-MS-REJECT-2 (the reference ACCEPTS the lifted knob ⇒ reference-parity-accept).
- **The differential `0090-stats-sink-metrics-service-deltas`** (fixtures **91 → 92**): the `0089` topology + workload with `report_counters_as_deltas:true`; the delta sink breaks 0089's `value==K`, so assert the **SUM of each deterministic counter's deltas across all received flushes == K** (poll-to-converge the running per-name sum — a release barrier, NEVER a sleep). Two per-side receivers + hard `Close()` cloned from the AUTHORITATIVE `0089/driver/driver.go` (NOT the stale `0089/README.md` — reconcile that README here). The receiver (`test/helpers/metricsservice`) gains a delta-SUM accumulator surface.

## The NEXT action — the 47.2a SPEC (lifecycle-state 1 → 2 for the 47.2a sub-leg)
Author `docs/envoy-go/phases/47-stats-sink-metrics-service/SPEC-47.2a.md` (the phase-44/45/46/47.1 SPEC shape). Execute the §10 D-MS-DELTA{,-GAUGE,-SUBJECT} + D-MS-STATS-FINAL-2 + D-MS-REJECT-2 empirical pins IN-SESSION against `envoyproxy/envoy:contrib-v1.37.2` (ADR-0227) via the live-probe precedent (`reference_docker_probe_bridge_network` — a shared bridge; a metrics-receiver hostname reachable from BOTH containers; verify the receiver actually decoded ≥1 `MetricFamily` from the reference). Anchor the anticipated **ADR-0263** §Context as a DRAFT (§13; the body lands at the 47.2a IMPL per ADR-0044). Verify all load-bearing claims (the `mapping.go`/`flusher.go`/`sink.go` 47.1 as-built seams, the `bootstrap.go:446` reject, the `stats.Counter`/`Gauge` `Load`, the metrics_service proto field numbers) against disk before writing — the spec-document-reviewer discipline. **DOCS-ONLY direct-on-master** (the SPEC is docs-only per the family-row precedent; a fresh worktree off master per `feedback_git_worktrees` ONLY if code-adjacent probe artifacts land). **ROW 47 STAYS `in-progress`**. After it lands, ROLL THIS FILE to the **47.2a PLAN**.

## What 47.2b IS (the SECOND-and-FINAL sub-leg — chartered by ITS OWN later BRAINSTORM? NO — chartered by BRAINSTORM-47.2; opens at the 47.2b SPEC)
**47.2b = `emit_tags_as_labels=true`** (anticipated **ADR-0264**): SN-rule tag→`LabelPair` extraction (the dotted-name tag SEGMENTS become `MetricFamily.metric[].label[]` pairs instead of inlined into the name), reusing the `internal/stats/name.go` SN1–SN9 extractors (`flattenToProm` at `:39`); lift the 47.1 reject at `bootstrap.go:449`; add `StatsSinkConfig.EmitTagsAsLabels`; the `0091-stats-sink-metrics-service-labels` LabelPair differential (fixtures **92 → 93**). The exact emitted name/label-key form is a 47.2b SPEC live-probe pin (D-MS-LABEL); the refactor-shared-`name.go`-core-vs-parallel-extractor decision is D-MS-LABEL-REUSE. **47.2b IMPL FLIPS ROW 47 `done`.** 47.2b opens at its own SPEC AFTER 47.2a lands (NO separate 47.2b brainstorm — BRAINSTORM-47.2 already chartered both sub-legs).

## Counts at the 47.2 BRAINSTORM (docs-only — UNCHANGED; re-verify, do NOT assume)
stat surface **1200** (non-H2 **1196**) / fixtures **91** (tail `0089-stats-sink-metrics-service`; `ls -d test/fixtures/*/ | wc -l`) / fuzzers **50** (`grep -rh '^func Fuzz' --include='*.go' . | wc -l`) / BackendKind **38** (`H2GoawayResponder = 38`) / DECISIONS **ADR-0262** (next-free **ADR-0263**); `github.com/prometheus/client_model v0.6.1` direct (landed at 47.1 — `LabelPair` already available for 47.2b); `go-control-plane/envoy v1.32.4`; `internal/statssink` is the package both sub-legs modify. Anticipated: 47.2a IMPL → fixtures **92** (`0090`), DECISIONS **ADR-0263**; 47.2b IMPL → fixtures **93** (`0091`), DECISIONS **ADR-0264**, row 47 `done`. Both legs: stat **1200 UNCHANGED** (+0 self-stats preserved) / fuzzers **50** / BackendKind **38** / NO new package / NO new go.mod module.

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** The next stage is the 47.2a SPEC (lifecycle-state 1 → 2 for the 47.2a sub-leg). After it lands, ROLL THIS FILE to the 47.2a PLAN.
- **SPEC/PLAN = docs-only direct-on-master** (the family-row precedent); a fresh worktree (`feedback_git_worktrees` · `superpowers:using-git-worktrees`) ONLY if code-adjacent probe artifacts land. The IMPL is subagent-driven (`feedback_execution_style` · `superpowers:subagent-driven-development`) in a worktree, subagents commit LOCALLY only (`feedback_subagents_no_push`), the CONTROLLER squashes + pushes at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`).
- **Process anchors:** ADR-0106 (per-leg rows — row 47 flips `done` at the 47.2b IMPL) · ADR-0045 (sub-split — 47.2 SUB-SPLIT into 47.2a/47.2b at the BRAINSTORM; per-sub-leg FINAL gate re-checked at each SPEC/PLAN) · ADR-0044 (ADR §Decision+§Consequences-at-IMPL — ADR-0263 §Context DRAFT at the 47.2a SPEC) · ADR-0080 (strict-reject — 47.2a LIFTS the deltas reject `:446`, KEEPS the rest; 47.2b LIFTS the tags-as-labels reject `:449`) · ADR-0059 (the in-tree `Registry`/`Walk`/`Freeze`) · ADR-0060 (histograms deferred — still moot) · ADR-0061 (the SN1–SN8 Prometheus name/label rules — the 47.2b tag-extraction substrate) · ADR-0064 (the `stats_sinks[]` silent-ignore — 47.1 LIFTED the metrics_service sink) · ADR-0158 (the `grpcclient.Dialer` typed-wrapper) · ADR-0262 (the 47.1 anchor + the deferral set 47.2 picks up) · next-free **ADR-0263** (ANCHORED at the 47.2a SPEC §Context draft, body at the 47.2a IMPL).
- **Differential discipline** (for the eventual 47.2a/47.2b fixtures): the TWO-per-side-receiver pattern + the hard `Close()` teardown (`reference_periodic_sink_differential_two_receivers` — clone the authoritative `0089/driver/driver.go`, NOT the stale README); `-run 'TestDifferential/00NN'` NEVER bare (`reference_differential_run_selector`); `-count=1` on every break + `-race` (`reference_differential_break_protocol_count1`); the Docker bridge + decode-ran proof (`reference_docker_probe_bridge_network`); the gRPC metrics receiver is driver-owned NOT a BackendKind (`reference_differential_grpc_receiver_driver_owned`); assert the aggregated payload (delta-SUM / `{name,label-set}` split) NOT framing (`reference_streaming_sink_differential_framing`); the live reference is the wire truth (`reference_wire_format_both_sides_see_same_bytes`); one fixture dir = ONE bootstrap config = ONE runner branch (`reference_differential_fixture_dispatch_constraint` — 0090 and 0091 CANNOT share a dir); the Flusher + sink writer goroutine + the NEW per-sink delta map are background mutators ⇒ FULL-package `-race` (`reference_full_suite_race_after_background_mutator`).

## Read first (in order)
1. THIS FILE — the cold-start router (the 47.2 BRAINSTORM is DONE + LANDED; row 47 `in-progress`; the next action is the 47.2a SPEC — `report_counters_as_deltas`).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 47.2 BRAINSTORM done`) + the recorded NEXT (the 47.2a SPEC) + the counts (1200 / 91 / 50 / 38 / ADR-0262).
3. `docs/envoy-go/phases/47-stats-sink-metrics-service/BRAINSTORM-47.2.md` — the 47.2 charter: the §Q-split (47.2a deltas / 47.2b tags-as-labels), the §2 design decisions (per-sink delta state, Counter-only; SN-rule label reuse), the §6 fixture envelope (0090/0091), the §10 empirical pins (D-MS-DELTA{,-GAUGE,-SUBJECT}, D-MS-LABEL{,-REUSE}), the §7 ADRs (0263/0264).
4. `docs/envoy-go/DECISIONS.md` — **ADR-0262** (the 47.1 anchor — its §Decision/§Consequences incl. the deferred set 47.2 picks up: deltas / tags-as-labels [+ the still-deferred histogram_emit_mode / stats_flush_on_admin / sibling sinks that STAY strict]).
5. The 47.1 as-built code 47.2a modifies: `internal/statssink/mapping.go` (`snapshot` `:22` — the Counter arm `:26`/`:31` the delta transform changes; the Gauge arm `:35` UNCHANGED), `internal/statssink/flusher.go` (`flushOnce` — the per-sink fan-out), `internal/statssink/sink.go` (`MetricsServiceSink` `:60` — mapping-agnostic; where the per-sink delta state may live), `internal/bootstrap/bootstrap.go` (`StatsSinkConfig` `:272`, `parseMetricsServiceConfig` `:431`, the deltas reject `:446` to LIFT), `internal/stats/{counter,gauge}.go` (`Load`), `cmd/envoy-go/main.go` (the sink construction wiring). For 47.2b: `internal/stats/name.go` (`flattenToProm` `:39`, SN1–SN9).
6. `docs/envoy-go/phases/47-stats-sink-metrics-service/SPEC-47.1.md` + `PLAN-47.1.md` + `PROGRESS-47.1.md` (the 47.1 charter + as-built — the SPEC shape to mirror + the §11 D-MS-* live pins).
7. `test/fixtures/0089-stats-sink-metrics-service/{driver/driver.go,envoy.yaml,envoy-go.yaml,expectations.yaml}` (the AUTHORITATIVE differential template 0090 clones — two per-side receivers + hard `Close()`; the README is STALE) + `test/helpers/metricsservice/metricsservice.go` (the receiver to extend with a delta-SUM surface).
8. `docs/envoy-go/ROADMAP.md` — row 47 (`stats-sink-metrics-service`, `in-progress`) + the Observability family-progress note.
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_subagents_no_push`, `feedback_subagent_autocommit_claudemd`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_detach`/`_path_targeting`, `reference_roadmap_split_phase_row_done`, `reference_stats_sink_metrics_service_probe`, `reference_periodic_sink_differential_two_receivers`, `reference_differential_grpc_receiver_driver_owned`, `reference_streaming_sink_differential_framing`, `reference_docker_probe_bridge_network`, `reference_full_suite_race_after_background_mutator`, plus the differential-discipline references above).
