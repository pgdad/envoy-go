START a fresh cold-start session for envoy-go — **the phase-29 parent SPEC (`network-filter-mongo-proxy`) is DONE + spec-document-reviewer APPROVED + squash-merged + pushed**. The active phase is **phase 29** (parent pre-split row, `in-progress`; sub-rows 29.1/29.2/29.3 `planned`). This session authors the **29.1 sub-phase SPEC** (`docs/envoy-go/phases/29.1-network-filter-mongo-wire-and-requests/SPEC.md`) — per SKILL_ROUTING state 2 (`superpowers:writing-plans` scoped to the 29.1 SPEC per the 22.1/25.1/26.1/28.1 per-sub-phase precedent). ROADMAP sub-row 29.1 flips `planned → in-progress` at THIS session's SPEC commit.

The parent SPEC landed at squash `ab376d8` (pushed to origin/master): all twelve D29-1..D29-12 empirical pins resolved IN-SESSION against Envoy v1.37.2 (parallel-subagent fan-out; live docker boot + go-control-plane v1.32.4 binding reads + upstream source at tag v1.37.2 + the as-built framework inventory), producing **13 AMENDs (B1..B13)**. The load-bearing ones for 29.1: **D29-1 CONFIRMED** (the reference image ships mongo_proxy; the Q3 cross-side strategy stands); **D29-4 → LEGACY-ONLY** (upstream decodes EXACTLY 7 opcodes — Reply 1 / Insert 2002 / Query 2004 / GetMore 2005 / KillCursors 2007 / Command 2010 / CommandReply 2011; **OP_MSG(2013)/Update(2001)/Delete(2006)/Msg(1000) are NOT decoded** → `decoding_error`); **AMEND-B1** stat scope `mongo.<stat_prefix>.<counter>`; **AMEND-B2** Prometheus is **TAG-EXTRACTED** (`envoy_mongo_<leaf>{envoy_mongo_prefix="<prefix>"}`) → the `name.go` arm is the **ADR-0218 tag-extractor shape** (the BRAINSTORM/ROADMAP `.mongo.` inline-prefix hypothesis is REFUTED — do NOT build an ADR-0138-shaped arm); **AMEND-B3** the fixed roster is EXACTLY 22 counters + 1 gauge, `delays_injected` PLURAL, zero macro histograms → stat surface 337 → 360; **AMEND-B4** upstream creates fixed stats per-CONNECTION (reference shows nothing until the first downstream connection) → all fixture assertions post-first-connection; **AMEND-B5/B6** the 7-opcode envelope + decode-error-stops-sniffing-for-connection-lifetime (fresh connection per error fixture arm); **AMEND-B7** `commands` = builtin-REMEMBERING (unlisted → `cmd.unknown_command.total`; default `{delete,insert,update}`; alias normalization `ismaster`→`isMaster` etc.; `find` → query path); **AMEND-B9** FaultDelay PGV (oneof REQUIRED + `fixed_delay` gt 0s — parse + unit-arms at 29.1, fixture consumption at 29.3). The full pin record incl. per-opcode wire layouts, the 14-type BSON subset, flag bit values, and query-shape heuristics is parent SPEC §11.4 — the 29.1 decoder mirrors it verbatim per `reference_wire_format_both_sides_see_same_bytes`.

## What this session must do

Author `docs/envoy-go/phases/29.1-network-filter-mongo-wire-and-requests/SPEC.md` via `superpowers:writing-plans` scoped to SPEC authoring. The 29.1 SPEC must:

1. **Inherit the parent SPEC §11 pins** (no re-scraping) EXCEPT the D-P2 dynamic-stat live re-probe: the parent-SPEC probe could not capture live `cmd.*`/`collection.*` stat names or their Prometheus form (the probe harness's tcp_proxy upstream connection failed — parent SPEC §11.1 caveat). The 29.1 SPEC re-probes with a WORKING backend (a listening TCP responder) to pin the dynamic-name live shapes + their Prometheus label form, OR pins them from upstream's tag-extraction source (well_known_names) — either way the pin must be empirical, not assumed.
2. **Resolve the 29.1-scoped parent D-questions:** D-P1 (fixed-roster creation posture — anticipated eager-at-config-parse + post-connection-only assertions + a BEHAVIOR_CONTRACT boot-window departure note); D-P2 (dynamic-stat live shapes, above); D-P3 (the `name.go` mongo tag-extractor arm validation posture — anticipated shape-based); D-P8 (the `0049` commands-list arm — anticipated yes); plus the D-P5 unit-arms (the two FaultDelay PGV rejects unit-tested at 29.1).
3. **Pin the package design:** `internal/filter/network/mongoproxy/` file layout (mongoproxy.go / config.go / bson.go / codec.go / stats.go / filter.go); the decoder's chain-buffer consumption tracking (the chain `Buffer` is observational/not-drained — the zookeeper `chainConsumed` high-water pattern is the precedent, parent SPEC §11.5); the private-buffer copy/sniff model + sniffing-off-on-error; the active-query list shape (written 29.1, consumed 29.2); the 23-stat roster table + dynamic-name helpers; the `op_query_active` gauge CREATED at 29.1 (increments at 29.2).
4. **Pin the fixture design:** `0049-mongo-requests` arm roster (parent SPEC §8.1 — 8 anticipated arms incl. the $cmd/commands-list arm, query-shape arms, $comment callsite arm, OP_MSG + garbage-BSON error arms on fresh connections) + the canned-bytes mongo responder BackendKind + `0050-mongo-boot-reject` (stat_prefix arm); the deliberate-break protocol with `-count=1`; cross-side StatsAsserter post-first-connection discipline.
5. **Pin the 39th fuzzer envelope** (wire+BSON decode; no-panic + no-chain-buffer-mutation + sniffing-off idempotence).
6. **Re-check the ADR-0045 gate** (parent SPEC §15 says 29.1 fits at ~920-1330 production LoC / ~14-19 tasks; the PLAN re-checks).
7. Run the spec-document-reviewer loop until APPROVED; fold advisories in.
8. ROADMAP: sub-row 29.1 flips `planned → in-progress` at this SPEC commit (parent row 29 STAYS `in-progress`; 29.2/29.3 STAY `planned`). Also AMEND the ROADMAP 29.1 row text in place: it still says "`.mongo.` Prometheus inline-prefix arm (ADR-0138 / `.zookeeper.` precedent)" — corrected by parent AMEND-B2 to the ADR-0218 tag-extractor shape (one-line in-place edit, the ADR-0044 discipline).
9. Update STATE.md (active-phase → `phase 29.1 SPEC done`; next-skill → `superpowers:writing-plans` scoped to the 29.1 PLAN) + rewrite this next-prompt.txt + commit + push (work in a worktree per `feedback_git_worktrees`; squash-merge + push per `feedback_push_to_origin`).

## Project state at master tip

- **Master tip:** `c6a575f` (the docs-only SHA-fill commit; actual HEAD, trails the phase-29 parent-SPEC squash `ab376d8` +1). This repoint itself advances the tip +1 (expected, not chased); confirm the live tip via `git log --oneline -1`. STATE.md `last-commit` points at the substantive squash `ab376d8`. origin/master in sync after push.
- **§9 Network-filters family:** rows 26 (parent + sub-rows) + 27 + 28 (parent + 28.1a/28.1b/28.2) DONE; row 29 `in-progress` (parent; sub-rows 29.1/29.2/29.3 `planned`). After phase 29, 3 candidates remain (`redis`/`kafka_broker`/`thrift`).
- **Counts at master tip:** stat surface **337** (→ 360 at 29.1 creation); differential fixtures **50** (tail `0048-zookeeper-responses`; → 52 at 29.1); fuzzers **38** (→ 39 at 29.1); conformance h2spec 53/53 + proxy-wasm 10/10 (as of the 28.2 six-gate); DECISIONS.md tail **ADR-0226** (the three phase-29 §Context drafts landed at the parent SPEC; next-free **ADR-0227**; the ADR-0224 §Decision/§Consequences body lands at the 29.1 IMPL).
- **Toolchain:** Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); reference Envoy v1.37.2 (ADR-0008); go-control-plane v1.32.4 (ADR-0008).
- **Known coverage boundaries (carried, non-blocking):** D-26.3-4 (rbac SNI/mTLS L4 arms); the `tcp_proxy` `downstream_cx_*` family unmirrored; histograms project-wide deferred (ADR-0060 — phase 29 adds the mongo dynamic `reply_*` families at 29.2); zookeeper dynamic-metadata deferred (AMEND-A9) + shallow-decode leniency + terminal-originated-writes-only + write-only-filter boot boundary; post-handoff observational boundaries (28.1b SPEC §3.5 — lifted for halt purposes only at 29.3); mongo runtime-key gating unmirrored (parent SPEC §7.5, recorded at 29.3); the mongo access log not differentially compared (AMEND-B10, recorded at 29.3).

## Read first (in order)

1. THIS FILE — the 29.1-SPEC cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 29 parent SPEC done`; next-skill `superpowers:writing-plans` (29.1 SPEC); next-free `ADR-0227`; last-commit = the parent-SPEC squash.
3. `docs/envoy-go/phases/29-network-filter-mongo-proxy/SPEC.md` — **the parent SPEC this sub-phase executes against** (esp. §3 split surface-mapping, §11 empirical pins [§11.4 wire/BSON layouts verbatim], §12 D-P1..D-P12, §8.1/§8.2 fixture envelopes, §7 stat surface).
4. `docs/envoy-go/phases/29-network-filter-mongo-proxy/BRAINSTORM.md` — the design rationale (the SPEC supersedes it where they differ — the AMENDs).
5. `docs/envoy-go/ROADMAP.md` — rows 29/29.1/29.2/29.3 (note the 29.1 row's inline-prefix text needs the AMEND-B2 in-place correction at this session's commit).
6. `docs/envoy-go/SKILL_ROUTING.md` + `BOOTSTRAP_PROMPT.md` §4/§5 — the per-phase lifecycle + cold-start invariants.
7. `docs/envoy-go/phases/28.1-network-filter-write-seam-and-zookeeper-requests/SPEC.md` — the most recent first-sub-phase SPEC precedent (mirrors its structure: parent-pin inheritance + package design + fixture design + fuzzer + completion bundle).
8. `internal/filter/network/zookeeperproxy/` — the consumer-#1 package shape mongoproxy mirrors (two-step factory, decoder reassembly + `chainConsumed`, stats roster table, builtins registration) + `internal/stats/name.go` (the `.zookeeper.` arm at ~:243 and the `.rbac.` tag-extractor arm — the mongo arm follows the LATTER per AMEND-B2) + `internal/filter/network/builtins/builtins.go` (the 7 registrations; mongo is the 8th).
9. `docs/envoy-go/DECISIONS.md` ADR-0224 §Context (the 29.1 charter — drafted at the parent SPEC) + ADR-0221/0222/0223 (the seam + zookeeper precedent) + ADR-0218 (the tag-extractor prom-arm precedent) + ADR-0060 (histogram deferral) + ADR-0044/0045/0052/0080/0106 (doc/split/bundle/reject/flip discipline).
10. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — execution-style + worktree + push + subagents-no-push + per-task gofmt/lint + the network-filter constraints (typeurl-extensions, onnewconnection-halts, terminal-handoff-ends-ondata) + the differential dispatch/asserter/break-protocol (`-count=1`) + wire-format-both-sides constraints all carry forward.
