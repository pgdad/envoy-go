START a fresh cold-start session for envoy-go — **the phase-29.2 sub-phase IMPL (`network-filter-mongo-responses-and-correlation`) is DONE + reviewed + green + squash-merged + pushed**. The active phase is **phase 29.2 → IMPL done** (parent row 29 STAYS `in-progress`; sub-row 29.1 `done`; 29.2 `done`; 29.3 `planned`). This session opens the **29.3 SPEC** — the THIRD/FINAL sub-phase of the phase-29 pre-split (`network-filter-mongo-fault-delay-and-access-log`) — via **SPEC-authoring** (per SKILL_ROUTING: SPEC.md does NOT yet exist for 29.3 → write the SPEC; `superpowers:writing-plans`/SPEC-authoring path). Work in a git worktree (`feedback_git_worktrees`); subagents commit LOCAL-ONLY (`feedback_subagents_no_push`); the controller squash-merges + pushes at stage-close (`feedback_push_to_origin`).

The 29.2 IMPL landed at the **29.2-IMPL squash `620a3d0`**; the docs-only STATE/next-prompt SHA-fill `98028ff` trails it (the named live tip — a further docs-only commit may trail; confirm via `git log --oneline -1`). The substantive predecessor on master is the 29.2-PLAN squash `0783776` (docs-only `3af6163`/`46dcd1b` trail it); before that the 29.2-SPEC squash `13907f1`; the 29.1-IMPL squash `5ef5215`; the 29.1-PLAN `89c194a`; the 29.1-SPEC `7574219`. The phase-29 parent SPEC (squash `ab376d8`) + ADR-0226 §Context (the 29.3 charter draft) + the as-built `internal/filter/network/mongoproxy/` package (29.1 request side + 29.2 response side) are the pinned-behavior references. The ADR-0226 §Decision/§Consequences body lands IN PLACE at the 29.3 IMPL (no new ADR number — DECISIONS tail STAYS ADR-0226, next-free ADR-0227).

## What the 29.3 SPEC must pin (the FINAL phase-29 sub-phase)

The 29.3 sub-phase is the framework's FIFTH structural extension, in the existing `internal/filter/network/` package with anticipated ZERO new exported API (ADR-0226). The SPEC executes the parent SPEC §3.2/§7/§11 + ADR-0226 §Context into the per-Task surface:

1. **The async halt/resume seam** (the framework extension) — (i) ACTIVE asynchronous `ContinueReading` (a resume arriving OUTSIDE any dispatch — from a `time.AfterFunc` goroutine — advances past the halting filter, dispatches the buffered bytes to the remaining chain, re-evaluates terminal readiness; upstream `std::next(filter->entry())` parity); (ii) cross-goroutine safety (the resume races the pre-handoff read loop or pump A's `replayRead` on previously-unlocked `chainRuntime` state; the ADR-0223 minimal-critical-section discipline); (iii) post-handoff read-halt honoring (`replayRead` stops at a halting filter + holds bytes; `readChainConn.Read` withholds them from the terminal's pump until resume — lifting the 28.1b SPEC §3.5 observational boundary for halt purposes ONLY; never-halting chains byte-identical). The halt is READ-side per upstream `onData` semantics (NOT the write-side the ADR-0221 §Consequences anticipated — the REFINEMENT).
2. **Fault-delay injection** (the seam's first consumer) — per-decoded-request-message evaluation; a re-entrancy guard; `delays_injected` charged at timer-arm; StopIteration-while-pending; deterministic 100%-probability differential arms (timing never compared). The `delay` FaultDelay field was parsed at 29.1, consumed here (the phase-09 HTTP fault filter parse/eval precedent).
3. **The mongo access log** — the `access_log` field (parsed 29.1, consumed here); the `internal/accesslog` formatter seam; a timing-bearing JSON format → unit-test + coverage-boundary proof (NO fixture dir per AMEND-B10).
4. **`cx_drain_close`** — reply-completion drain close, FlushWrite (the drain-decision integration; the phase-08.2 drain precedent).
5. **The DEFERRED close-direction seam (D-P4 — 29.3 CLOSES it)** — `cx_destroy_local/remote_with_active_rq` VALUE parity: the minimal close-direction accessor + the direction-keyed increment. The framework records close TYPE not DIRECTION (`OnDestroy()` carries none; the post-handoff close lives in `tcp_proxy`'s two pump goroutines via EOF — `reference_close_direction_framework_gap`); 29.3's halt seam already opens the pump/terminal/`chain.go` area, so the close-direction ripple lands HERE (ADR-0219 one-ripple). The counters exist-at-zero from 29.1 + were PRESENCE-ONLY in `0051` at 29.2.
6. **Fixture `0052-mongo-fault-delay`** (cross-side; DETERMINISTIC 100%-probability arms; `delays_injected` parity; timing never compared) + **the parent-row-29 ROLLUP** (parent row 29 flips `in-progress → done` ATOMICALLY with sub-row 29.3 per the 18/19/22/24/25/26/28 precedent) + the six-gate (full 54-dir suite).

## Load-bearing pins the 29.3 SPEC must honor

- **Wire-format both-sides-see-the-same-bytes** (`reference_wire_format_both_sides_see_same_bytes`) — the fault-delay timing/drain framing is shared; "verify against upstream" means adopt upstream's framing verbatim.
- **Close-direction framework gap CLOSES here** (`reference_close_direction_framework_gap`) — 29.3 is the framework-surgery sub-phase that threads close DIRECTION; the 29.2 boundary (presence-only `cx_destroy_*`) is resolved to value parity.
- **The async halt is READ-side** — per upstream `onData` semantics; the ADR-0221 §Consequences anticipated-halt-consumer allowance is CONSUMED here, REFINED (read-side, not the anticipated write-side).
- **The break protocol needs `-count=1`** (`reference_differential_break_protocol_count1`) — for the deliberate-break liveness proofs (the `0052` R4 break + any seam break); Go test result caching serves a stale PASS otherwise.
- **Per-task gofmt + golangci-lint** on touched packages (`feedback_pertask_gofmt_lint`); each IMPL subagent task runs `gofmt -l` + `golangci-lint`, not just `go vet`.
- **The histogram families stay DEFERRED** (ADR-0060) — the per-command/per-collection `reply_num_docs`/`reply_size`/`reply_time_ms` families remain unmirrored project-wide.

## What this session must do

1. Re-establish a worktree (`feedback_git_worktrees`); confirm the clean baseline (`go build ./...` + `go test ./internal/... -count=1`).
2. Author the **29.3 SPEC** (`docs/envoy-go/phases/29.3-network-filter-mongo-fault-delay-and-access-log/SPEC.md` + README.md) — the §§ structure mirroring the 29.1/29.2 sub-phase SPECs; the parent §3.2/§7/§11 + ADR-0226 §Context refined into the per-Task surface; the D-questions resolved (the async-halt-seam shape; the fault-delay eval point; the access-log format + differential strategy per parent D29-8; the drain posture per D29-10; the close-direction accessor shape) IN-SESSION vs Envoy v1.37.2 per ADR-0004; the ADR-0045 split-gate re-check. Run `spec-document-reviewer`; fold advisories.
3. Flip ROADMAP sub-row 29.3 `planned → in-progress` at the SPEC commit (parent row 29 STAYS `in-progress`); advance STATE/next-prompt for the **29.3-PLAN** cold-start. Controller: squash-merge + fill the squash SHA into STATE/next-prompt + push.

## Project state at master tip

- **Master tip:** the docs-only SHA-fill `98028ff` trailing the substantive 29.2-IMPL squash `620a3d0` (confirm the live tip via `git log --oneline -1`; a further docs-only commit may trail). STATE.md `last-commit` points at the substantive squash `620a3d0`. Substantive predecessors: the 29.2-PLAN squash `0783776`; the 29.2-SPEC squash `13907f1`; the 29.1-IMPL squash `5ef5215`; the 29.1-PLAN `89c194a`; the 29.1-SPEC `7574219`. origin/master in sync after push.
- **§9 Network-filters family:** rows 26/27/28 DONE; row 29 `in-progress` (29.1 `done`; 29.2 `done`; 29.3 `planned` — the FINAL sub-phase; its phase-done carries the parent-row-29 ROLLUP).
- **Counts at master tip (29.2 IMPL phase-done):** stat surface **360** (+0 at 29.2; +0 anticipated at 29.3 — increment-wiring only); differential fixtures **53** (tail `0051-mongo-responses`; → 54 at 29.3 with `0052-mongo-fault-delay`); fuzzers **39** (`FuzzMongoDecode` extended at 29.2 — no count change); BackendKind tail **30** (`TCPMongoResponder`); conformance h2spec 53/53 + proxy-wasm 10/10 (as of the 29.2 six-gate, 2026-06-04); DECISIONS.md tail **ADR-0226** (next-free **ADR-0227**; the ADR-0226 body lands at the 29.3 IMPL — no new number).
- **Toolchain:** Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); reference Envoy v1.37.2 (ADR-0008); go-control-plane v1.32.4 (ADR-0008).
- **Known coverage boundaries (carried, non-blocking):** the mongo D-P1 boot-window eager stat-creation departure (unobservable); the `stats.IsValidName` guard on un-nameable wire-derived dynamic segments (user-approved; fuzz-validated; `reference_dynamic_stat_name_charset_guard`); the mongo dynamic-HISTOGRAM families deferred (ADR-0060); the dynamic-metadata emission differential-invisible (AMEND-B11 — unit-test only, landed 29.2); the **`cx_destroy_*` close-direction value parity DEFERRED to 29.3** (D-P4 — `reference_close_direction_framework_gap`, which 29.3 CLOSES); fault delay + access log + drain + `cx_drain_close` + `delays_injected` + mongo runtime-key gating are 29.3's; `tcp_proxy` `downstream_cx_*` unmirrored; histograms deferred project-wide (ADR-0060).

## Read first (in order)

1. THIS FILE — the 29.3-SPEC cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 29.2 IMPL done`; next-skill the 29.3 SPEC authoring path; next-free `ADR-0227`.
3. `docs/envoy-go/phases/29-network-filter-mongo-proxy/` — the parent SPEC (the §3.2/§7/§11 fault/access-log/drain pins + the D29-1..D29-12 roster — esp. D29-8 access-log format, D29-10 drain posture) + the BRAINSTORM (Q2 the async halt/resume framing).
4. `docs/envoy-go/DECISIONS.md` ADR-0226 (the §Context + the seam reframing — the 29.3 charter; its §Decision/§Consequences body lands at the 29.3 IMPL) + ADR-0225 (the 29.2 response side this builds on — incl. the D-P4 close-direction boundary 29.3 CLOSES) + ADR-0224 (the 29.1 request side) + ADR-0221 (the WriteFilter seam + the anticipated-halt-consumer allowance 29.3 CONSUMES) + ADR-0213/0215 (the chain framework + TerminalFilter seam) + ADR-0223 (the minimal-critical-section synchronization) + ADR-0219 (the no-ripple discipline) + ADR-0044/0052/0060.
5. `internal/filter/network/` (the chain framework the async halt/resume seam extends — `chain.go`/`readconn.go`/`replayRead`/`ContinueReading`/`connHalted`; the 26.1 halt machinery + the 28.1b read seam) + `internal/filter/network/mongoproxy/` (the 29.1+29.2 package — `filter.go` the OnData/OnWrite/OnDestroy the fault delay + drain + close-direction wire into; `config.go` the `delay`/`access_log` already parsed) + `internal/accesslog/` (the file-sink/formatter seam) + the phase-09 HTTP fault filter (the FaultDelay parse/eval precedent) + `test/fixtures/0051-mongo-responses/` (the `0052` driver template + `TCPMongoResponder`).
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — execution-style + worktree + push + subagents-no-push + per-task gofmt/lint + the differential dispatch/asserter/break-protocol (`-count=1`) + wire-format-both-sides + the dynamic-stat charset guard + the close-direction framework-gap boundary (which 29.3 CLOSES) all carry forward into the 29.3 SPEC.
