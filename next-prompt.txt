=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **PHASE 94 (`tls-connection-error-stat`) IS OPEN AT LIFECYCLE-STATE 2. ROW 94 READS `in-progress` AT `ROADMAP.md:156`.** The phase directory holds `BRAINSTORM.md` (412) and `SPEC.md` (500). ⇒ **THIS SESSION RUNS THE PLAN (state 2 -> 3). DO NOT SELF-PICK; DO NOT OPEN A NEW SUBJECT.**

⚠️⚠️ **CHECK (1) IS NON-SILENT — IT PRINTS `NOT DONE: row 94`, WHICH IS CORRECT AND EXPECTED** while row 94 is open. Check (3) is SILENT. Check (2) prints SIX.

⚠️⚠️ **THE MARGIN IS TWO, NOT ONE — AND THAT IS UNCHANGED FROM THE PREVIOUS STAGE.** The sentinel is blocked **TWO INDEPENDENT WAYS**: check (1) by an OPEN ROW (work literally mid-flight) and check (2) by six lines. **Closing row 94 removes only the FIRST.** ⚠️ **The margin returns to ONE the moment row 94 flips `done`, so the phase-94 IMPL must re-read this paragraph rather than inherit either state.** ⚠️ **DO NOT "TIDY" A DEFERRED-CANDIDATE LINE — DELETING THE LAST ONE ENDS THE PROJECT.** ⚠️ **MEASURE ALL THREE YOURSELF BEFORE DOING ANYTHING ELSE; do not take this file's word for it.**

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE. This form parses FIELDS instead of matching a pattern, and
#     asserts the row DENOMINATOR so a deleted row is itself an error.
#     ⚠️ want IS 126 and ROADMAP.md IS 244 lines — BYTE-UNTOUCHED at the phase-94 SPEC.
awk -F'|' -v want=126 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **CHECK (1) PRINTS ONE LINE (`NOT DONE: row 94`) BY DESIGN.** Its NCs still matter, because a check that prints the RIGHT line for the WRONG reason is still broken. Run BOTH doctoring NCs EVERY SESSION:
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md.
```
⚠️ **NC-A READS EXACTLY *TWO* LINES** (`NOT DONE: row 62` and `NOT DONE: row 94`). ⚠️ **NC-B ALSO READS TWO** (`NOT DONE: row 94` and `GATE FAIL: examined 126 data rows, expected 125`). **BOTH SHAPES ARE UNCHANGED ACROSS THIS STAGE — because `ROADMAP.md` IS BYTE-UNTOUCHED AT A SPEC. THEY LAST CHANGED AT THE PHASE-94 BRAINSTORM'S ROW ADD (ONE -> TWO) AND WILL COLLAPSE BACK TO ONE WHEN ROW 94 FLIPS `done`. NEVER INHERIT A NC SHAPE ACROSS A ROW ADD OR FLIP; RE-MEASURE.**
```sh
# the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```
Also NC the DENOMINATOR (`want=125` on the real file).

⚠️ **AND WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -c '-family row'` fails (GNU: `grep: amily row: No such file or directory`) and the surrounding arithmetic prints `0`, **which reads exactly like "no change."** **Always pass `--` before the pattern**; with `--` occurrences read **96** and LINES read **68** (UNCHANGED across this stage — ROADMAP is byte-untouched). **`grep -c` counts LINES, NOT OCCURRENCES.** ⚠️ **AND `grep -c` PRINTS `0` *AND* EXITS 1, so `$(cmd || echo 0)` EMITS TWO ZEROS** — capture with `v=$(cmd || true)`.

⚠️ **THE MALFORMED-ROW FIGURE RECONCILES ONLY UNDER AN ESCAPE-AWARE FIELD COUNT.** Malformed rows are ids **57** (`NF=9`) and **69** (`NF=10`). ⚠️ **A NAIVE `awk -F'|'` `NF != 8` FORM READS SEVENTEEN.** **Strip `\|` BEFORE splitting.** The two forms **DISAGREE on row 57** (naive reads `NF=13`) but **AGREE on row 69** — RE-VERIFIED at this tip — so **any gate must state WHICH FORM it uses.** Row 94 is **NF=8 under BOTH**, with **7** pipe characters, the delimiters only.

⚠️ **AND DO NOT SUBSTITUTE A "REASONABLE EQUIVALENT" FOR CHECK (1)'s AWK.** A measurement agent once tried `NF>=8 && $2 ~ /^ *[0-9]+ *$/`: it printed **92**, not the true denominator. **Use the form above verbatim.**

**SENTINEL RUN MECHANICALLY AT THE PHASE-94 SPEC.** ACTUAL output, recorded not predicted:
**(1) `NOT DONE: row 94`, ONE line** · **(2) SIX** at **`:204 :210 :216 :226 :232 :240`**, and **all six per-line md5 BYTE-IDENTICAL** to the BRAINSTORM's record (`10d7807bf02d 4a92f7e62fc6 2a7eb298b9fd 4ad940205410 b2680e6f4fbf 6caa1c3ce0e7`) — **that digest match is the proof no deferred-candidate line was tidied** · **(3) SILENT**; **NC-A TWO**, **NC-B TWO**, NC-C fired (`NEVER OPENED: gRPC`, NC landed with **0** residual), NC-D **96 / 68**.
⇒ **THE SENTINEL DOES NOT FIRE. `stop` WAS EVALUATED AND DELIBERATELY NOT CREATED** (verified absent at the git root and in the stage worktree).
⚠️ **THE CHECK-(2) POSITIVE CONTROL WAS RE-RUN: doctoring both candidate PHRASES out of a scratch copy drives check (2) to 0**, so it is not a stuck gate. ⚠️ **BUT THE CONTROL HAD TO REMOVE THE *PHRASE*.** Removing a *clause* from a window sentence leaves the phrase, the line and the count untouched. ⇒ **NARROWING A WINDOW CANNOT MOVE CHECK (2). ONLY DELETING THE LINE CAN, AND DELETING IT IS FORBIDDEN.** **Charter narrowing rows AS narrowing; never as sentinel progress.**

⚠️ **THIS RUN DOES NOT CARRY FORWARD — run all three checks, all four NCs *and* the check-(2) positive control YOURSELF, at YOUR OWN tip.**

- ⚠️ **LIVE FIGURES RE-DERIVED AT THIS TIP:** `ROADMAP.md` **244** lines / **126** data rows / tail row **94** (`in-progress`) — **BYTE-UNTOUCHED at this SPEC** · `DECISIONS.md` **18834** (**18804 -> 18834**, +30) · `^---$` **216 (STAYS)** · `^## ADR-` **315** (was 314) · bare `^## ` **323** (was 322) · tail **ADR-0316** · next-free **ADR-0317** (`^## ADR-0317` reads **0**) · `BEHAVIOR_CONTRACT.md` **5987** — **BYTE-UNTOUCHED** · `STATE.md` **65** (was 64) · `STATE_HISTORY.md` **542** (was 540), strict **163 (DELTA 0)** / parenthetical **58** / loose **221** (**163 + 58 = 221 exactly**) · phase dirs **135 (UNCHANGED — the directory already existed)** · fixtures **121** (tail `0119`; **`0120` FREE and CLAIMED BY THIS ROW at its IMPL**) · fuzzers **56 targets / 48 FILES** · `go.mod` **67 REQUIRE ENTRIES** (**18 direct + 49 indirect**) — ⚠️ **67 IS THE REQUIRE COUNT, NOT A LINE COUNT, and the plausible `grep -cE '^\s+[a-z0-9./-]+ v[0-9]'` form reads 62** · BackendKind tail **38** (39 declared, values 0-38) · `-family row` **96 / 68**.
- ⚠️ **THE STAT SURFACE IS QUOTED AS A DELTA AND NEVER AS AN ABSOLUTE.** THREE different absolutes are live in this tree at one tip. Per `reference_a_drift_correction_is_itself_a_claim`, on a contested count: **NO NUMBER.** ⚠️ **AND THE REFUTED FIGURE MUST NOT BE SPELLED, IN EITHER ITS BARE OR ITS ARROW FORM.**
- ⚠️ do NOT "fix" the six in check (2) and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases, and **each new FAMILY opens a new window**, so the count has only ever grown. ⚠️ **Cite ~40 only as "sentence-visible", never as the inventory** — and **no gate may rest on a ` + ` split, in any form. THAT RULE IS EMPIRICALLY CONFIRMED: a ` + ` split is WRONG ON 3 OF THE 6 WINDOWS and reports `:240` as having ONE item when it has THREE.**

==============================================

## STATUS AS OF 2026-09-02: PHASE 94 (`tls-connection-error-stat`) **OPEN, lifecycle-state 2**. **THIS SESSION RUNS THE PLAN.**

**The subject.** Give `outcomeOther` — the fourth downstream-TLS handshake-outcome bucket, which today increments nothing — a **predicate-gated `Inc()`** landing `listener.<normalized-addr>.ssl.connection_error`, the FIFTH listener-scope TLS counter. Closes the NAMED DEPARTURE at `BEHAVIOR_CONTRACT.md:1971`. **The TWENTY-FIFTH Observability-family row**; the family STAYS OPEN.

**⚠️ READ `SPEC.md` §16 FIRST — IT IS THE PLAN'S OWED LIST. Everything below is orientation, not a substitute.**

### ⚠️ WHAT THE SPEC REFUTED BY EXECUTION — TWELVE CLAIMS, THREE OF THEM AGAINST ITS OWN AGENTS

1. ⚠️ **THE BRAINSTORM'S §7.2 VERDICT TABLE IS A CATEGORY ERROR.** `TestClassifyHandshakeErr` calls `classifyHandshakeErr(tc.err)` **DIRECTLY** and asserts the **OUTCOME only** (`manager_test.go:4404-4408`) — it **NEVER reaches an Inc site**. ⇒ **the five `outcomeOther` rows at `:4398-4402` stay GREEN and BYTE-UNCHANGED by this row**, and §7.2's `must EXCLUDE / must INCREMENT` column describes **a COUNTER table that does not yet exist.**
2. ⚠️ **AND §7.2 DERIVED ITS ONE WRONG VERDICT BY MESSAGE-TEXT MATCHING — THE EXACT PRACTICE THE DESIGN FORBIDS.** EXECUTED: `errors.Is(errors.New("connection reset by peer"), syscall.ECONNRESET)` is **FALSE**, while `syscall.ECONNRESET.Error()` is **byte-identical** to that string. That row's verdict is REVISED to **NO REFERENCE COUNTERPART** (the disposition already given `context.DeadlineExceeded`). **The predicate is 6/6 CORRECT ON REAL server-side errors** and 4/5 only on the synthetic table.
3. ⚠️ **"UNIQUE REPO-WIDE" IS FALSE FOR BOTH EDIT ANCHORS — they are unique only under `-- '*.go'`.** Unscoped: `return outcomeOther` **4**, `case outcomeVerifyError:` **3**, two of them inside `74/PLAN.md` and `94/BRAINSTORM.md`. **An unscoped `sed -i` would corrupt landed phase documents. PATHSPEC-SCOPE EVERY EDIT.**
4. ⚠️ **THE `manager.go` PROSE ROSTER IS SIX, NOT FOUR** — `:175-177 :363-364 :392-393 :398-400 :408-412 :1291-1293`. ⚠️ **AND `:414` IS A LOOK-ALIKE THAT STAYS TRUE** (*"crypto/tls exports four error TYPES"* counts error types, not counters). **A naive `four` sweep edits it and is wrong.**
5. ⚠️ **THE INHERITED TEST ROSTER OMITS `helpTextRoster` ENTIRELY** (`internal/stats/helptext_test.go:41-83`). It is a **MATCHED PAIR** with `helpText`; **either alone reddens `internal/stats`.** `reference_measured_prototype_is_a_lower_bound` **FIRED A NINTH CONSECUTIVE ROW**, again by under-enumerating FILES.
6. ⚠️ **`name_test.go:232-237`'s TRAP COMMENT IS HALF STALE — ITS GATE MOVED.** Its cited evidence (*"the whole package stayed GREEN"* at the phase-75 PLAN) is **FALSE at this tip**: `helptext_test.go` post-dates phase 75 and reddens a bare `helpText` addition with `extra:`. The silent gap is now keyed on the roster.
7. ⚠️ **`internal/stats/name.go` IS NOT A ONE-LINE ADD — `gofmt` REWRITES THE WHOLE MAP BLOCK** (the new key exceeds the alignment column). Budget a realignment diff.
8. ⚠️ **THE PORT PICK WAS NOT SETTLED, AND THE RUNNER-UP'S PREMISE IS FALSIFIED.** `0118/driver/driver.go:31` reserves `10450` as *"the TLS/SDS band (0108-0113)"* — but **`0112` and `0113` carry ZERO `DownstreamTlsContext`.** The range is a sequential run, not a TLS band. **CHOSEN: `10126`** (index-preserving repair; `10120` is held by `0028`'s `10120-10125` run; `10126-10129` all read ZERO in code scope).
9. ⚠️ **NO `tls_params` YAML EXISTS ANYWHERE IN THIS TREE** — all ten hits are under `docs/`. `0120` would be the FIRST. **"The reference container accepts it" is ASSERTED, NOT MEASURED — THE PLAN MUST BOOT BOTH SIDES ON IT BEFORE WRITING ANY ARM.**
10. ⚠️ **`TLSv1_0`/`TLSv1_1` IN THE YAML BOOT-REJECTS envoy-go** (`internal/tls/params.go:62-83`). **The bad-version arm is CLIENT-side ONLY** — pin the Go client to `MaxVersion: VersionTLS11`.
11. ⚠️ **THE "VACUOUS `0 == 0`" ARGUMENT IS TRUE ONLY POST-IMPL.** *Today* such a pin goes **RED** (the counter is unregistered). The precise statement: **a zero pin gates REGISTRATION ONLY and can NEVER gate the INCREMENT.** Conclusion (a new fixture is FORCED) **UPHELD**; reasoning **CORRECTED**.
12. ⚠️ **THE INHERITED `clusters: []` BOOT-REJECT DID NOT REPRODUCE AS STATED.** No literal reject located; nearest are `bootstrap.go:970` and `hcm/config.go:880`, different checks. Moot — `BackendCount()==1` forces a cluster.

### ⚠️ THE SPEC'S RESOLVED DOCKET — DO NOT RE-LITIGATE, DO RE-VERIFY

- **D-TLSCE-SEAM** — the predicate lives **AT THE Inc SITE** (`case outcomeOther:` on the existing switch, `manager.go:1294-1299`), **NOT inside `classifyHandshakeErr`**. The rejected fifth-`handshakeOutcome`-variant arm would **flip THREE of the five landed rows**, reddening a table that correctly pins a DIFFERENT question. ⚠️ **The taxonomy stays FOUR while the counters go to FIVE, and `outcomeOther` becomes the ONLY outcome mapping to a counter CONDITIONALLY.**
- **D-TLSCE-PREDICATE** — `errors.Is`-only exclusion of `{io.EOF, io.ErrUnexpectedEOF, syscall.ECONNRESET, net.ErrClosed, context.DeadlineExceeded}`, **no message-text matching**. ⚠️ **`net.ErrClosed` was produced by NO measured arm — it is DEFENSIVE and UNEXERCISED and MUST BE LABELLED AS SUCH.**
- **D-TLSCE-NILGATE** — the fifth nil guard lands in the **SAME commit** as the Inc. EXECUTED: deleting the registration while keeping the Inc **SIGSEGVs the process while `TestListenerMetrics_GateMatchesInc` PASSES.**
- **D-TLSCE-HELPTEXT** — insert **into the `ssl.*` group, FIRST within it**, NOT at EOF (an EOF append breaks the tail-anchored *"the last five are phase 80's `sds.` root"* clause). ⚠️ **The map is NOT stored sorted — this is a DECLARED CHOICE, not an inherited rule.** ⚠️ **The new name PREPENDS** (`LC_ALL=C`, confirmed by execution): every exact-set `want` slice takes it at **position 0**.

### ⚠️ THE RED SET, MEASURED — AND THE ROW INHERITS NO RED GATE

A production-only prototype (field + `NewCounter` + `case outcomeOther: Inc()` + the `helpText` entry, **no test edits**) reddens **exactly three** tests: `TestListenerMetrics_TLSListenerRegistersExactlyFourSSLNames`, `TestQUICListener_RegistersSSLNamesAtZero`, `TestHelpText_KeySetExact`. ⚠️ **ALL THREE ARE REDDENED BY THE REGISTRATION ALONE — ADDING THE Inc CHANGED THE RED SET BY ZERO TESTS**, and `outcomeOther` is produced by **ZERO fixture arms anywhere**. ⇒ **THE ROW OWES A NEW POSITIVE ARM OR THE Inc SHIPS UNEXECUTED.**

⚠️ **`sslLeafRoster` (`manager_test.go:4580`, calls `:4663 :4694 :4712 :4805`) IS WHAT MAKES THE PREDICATE FALSIFIABLE AT ALL — full 2x2 EXECUTED:** with the roster at FOUR, **removing the predicate is INVISIBLE**; with it at FIVE, removing the predicate goes **+2 RED**. **RUN NC 1 BOTH WAYS.**

⚠️ **THREE MANDATORY EDITS ARE SILENTLY GREEN IF OMITTED**: `name_test.go`'s `wantNames` slice (`:239-244`), `sslLeafRoster`, and the `GateMatchesInc` nil guards (`:2281-2298`, `:2340-2354` — omission's real failure is a **process crash**).

### ⚠️ FIXTURE `0120` — NO PRECEDENT EXISTS FOR ITS DRIVE LAYER

**No fixture in this tree has ever driven a deliberately failing TLS handshake of this kind.** Every existing TLS client pins `MinVersion: VersionTLS12` and is configured to SUCCEED at the protocol layer; `0110`/`0111` drive only CERTIFICATE-verification failures. **`0120` would be the FIRST.** Build the arms from `0110`/`0111`'s CLIENT-HARNESS shape and `0118`'s stats-only driver shape.
- **THREE registration gates**: the directory (`runner_test.go:1462`); `fixture.RegisterFixture` from the driver's `init()` (`fixture/fixture.go:92`); **a blank import in `runner_test.go` — the ONLY file outside the fixture dir that must be touched.** ⚠️ **A missing blank import is `t.Skipf`'d SILENTLY GREEN and NO fixture-count gate exists. ASSERT THE FIXTURE SET BY NAME, BOTH DIRECTIONS.**
- ⚠️ **ROSTER-GATE TRAP: there are TWO directory layouts — 97 fixtures use `<dir>/driver/`, 24 use `<dir>/inputs/`.** A naive `driver`-only extraction reports **24 phantom failures**. `0120` uses `driver/`.
- ⚠️ **`AssertStats` dispatch is an UNGUARDED type assertion with no `else` (`runner_test.go:1349`) — a signature typo makes the whole stats leg VANISH GREEN.** Carry the compile-time `fixture.StatsAsserter` assertion (`0118/driver/driver.go:588-594`, marked MANDATORY).
- **One dir = ONE runner branch** ⇒ exactly one `DriveReference`/`DriveSubject` pair and at most one `AssertStats`. **All arms sequence INSIDE that single pair**, and they must include **a clean-FIN arm that must NOT move the counter** — without it the fixture proves only that the counter can move, never that the predicate discriminates.
- **`BackendCount()` returns 1** (`runner_test.go:242-245` `t.Fatalf`s on 0; `0118` precedent). **+0 BackendKinds** (tail STAYS 38).
- **Config: the `0018-http-rbac/envoy.yaml:216-226` INLINE `validation_context` form**, NOT `combined_validation_context` — `0110`/`0111` drag in SDS and its known value-instability.
- ⚠️ **EVERY ARM MUST FORCE-SEND AND PRINT WHAT IT SENT.** `reference_go_client_cert_withholding` extends from untrusted to UNPARSEABLE certs; `0111:165-167` records a sibling arm silently DEGRADING into another while `CompareBytes` stayed green.
- **Asserter**: `/stats/prometheus`, key on the metric NAME, **ignore the `envoy_listener_address` LABEL value** (`0111/driver/driver.go:737-745`; parser `scrapeProm` `:828-878` — `ParseFloat` NOT `ParseUint`, histogram lines carry `nan`/`inf`).

### ⚠️ WHAT LANDS AT THE IMPL, NOT NOW — PINNED AT `SPEC.md` §11

**`BEHAVIOR_CONTRACT.md` and `ROADMAP.md` are BYTE-UNTOUCHED at the SPEC AND at the PLAN.** ⚠️ **THIS IS A MEASURED SCOPE FINDING, NOT AN ASSUMPTION** — the phase-93 (`975e527e`) and phase-75 (`2b5eff0a`) SPEC commits touch only `DECISIONS.md` / `STATE.md` / `STATE_HISTORY.md` / their own `SPEC.md` / `next-prompt.txt`. Contract edits land at the IMPL with the ADR as their **ADR-0052 `:1821` MANDATED vehicle**. The pinned map: delete ONLY the *"still blocked on enumerating its membership"* clause at `:1971` and **KEEP** the *"membership is UNENUMERATED"* sentence beside it; retitle the departure CLOSED; update `:1967`, `:1973` (QUIC parity **inherited BY CONSTRUCTION, not by measurement — claim no probe that was not run**) and `:1975`; and mark `ROADMAP.md:155`'s *"~4-6 production lines"* / *"unprobed doc claim"* **SUPERSEDED**. ⚠️ **`:1971` IS ONE ENORMOUS PARAGRAPH LINE — every edit is WITHIN-LINE; `sed`-style line replacement destroys neighbouring propositions.** ⚠️ **COUNT ROW 155's FIELDS UNDER BOTH FORMS BEFORE AND AFTER.**

## ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + ALL FOUR NCs + THE CHECK-(2) POSITIVE CONTROL YOURSELF** and record ACTUAL output.
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** 92 IMPL twelve · 93 BRAINSTORM eleven · 93 SPEC eleven · 93 PLAN twelve · 93 IMPL nine · 94 BRAINSTORM eleven · **94 SPEC TWELVE.** Do the same, and record it when your agents refute you. ⚠️ **AT THIS STAGE THREE OF THE TWELVE REFUTED THE STAGE'S OWN AGENTS** — one agent's port recommendation rested on a premise the controller then measured to be false, and two agents disagreed on a roster size, resolved by READING THE DISPUTED SITE rather than picking a winner (`reference_contradicting_agents_find_the_variable`: **both were right about what they were asked**).
3. **LINE ANCHORS DRIFT; SYMBOL AND LITERAL-TEXT ANCHORS DO NOT — RE-VERIFY EVERY CITE AT YOUR TIP.** ⚠️ **PATHSPEC-SCOPE every symbol assertion.** ⚠️ **A "DRIFT-PROOF" ANCHOR IS ITSELF A CLAIM — CHECK IT FOR UNIQUENESS, AND CHECK WHETHER ITS UNIQUENESS IS SCOPE-DEPENDENT** (see refutation 3 — both of this row's anchors are unique only under `-- '*.go'`). ⚠️ **`internal/filter/hcm/chain.go` DOES NOT EXIST — it is `internal/filter/http/chain.go`** — but ⚠️ **`actions.go` IS in `hcm`.**
3b. ⚠️ **CHARACTER-CLASS AND ESCAPE SELECTORS ARE THE FAIL-UNSAFE FAMILY.** `grep -cE '^\s+[a-z0-9./-]+ v[0-9]'` on `go.mod` reads 62 not 67 · `^[0-9]{4}-` on fixtures reads 119, dropping `0007a`/`0007b` (use `ls -d test/fixtures/*/ | wc -l`) · ⚠️ **`\t` in a regex is TOOL-DEPENDENT: harness `grep` is ugrep 7.8.4, `/usr/bin/grep` is GNU 3.11. NAME THE TOOL.** ⚠️ **AND A CASE-SENSITIVE GREP IS BLIND TO UPPERCASE PROSE — use `-i` when auditing comments.**
3c. ⚠️ **`feedback_brief_citations_not_evidence` APPLIES TO INCIDENTAL FIGURES. Re-derive EVERY number you restate — including every number in THIS file.**
3d. ⚠️ **NO GATE MAY REST ON A ` + ` SPLIT.** Empirically confirmed: wrong on 3 of 6 windows.
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Live: TWO SDS dial-budget flakes ⚠️ **plus `TestSDSEndToEnd_FetchFailure_BootFailsClosed`** · the driver-owned receiver port race · `internal/httpclient` zero-value · the two 84.2-era flakes · the REFERENCE h2spec section-8 flip · `TestOutlierDetector_ConcurrentEjectExactlyOnce` · `0061-lb-ring-hash`'s σ-margin (**a SECOND occurrence was recorded at phase 77; investigating it is a real follow-on the project owes**) · ⚠️ **NEW AT THIS STAGE: `TestP83_StopPauseTimer_IsAuthoritativeAgainstAnEnteredClosure`** (wasm), a 1/400-trial 2 ms-watchdog concurrency flake at `pause_gen_test.go:475`; it surfaced in this stage's prototype run, that package carries **no `ssl` code**, and it **passes at the clean tip**. ⚠️ **`TestServerConn_TinyWindowDelivery` IS NOT A FLAKE — phase 91 FIXED a live production deadlock; a recurrence is a REGRESSION of row 91.**
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only). ⚠️ **AT THIS STAGE A SIBLING `curl-world` SESSION WAS BUSY CONCURRENTLY and an `envoy-rust` session was idle.** ⚠️ **A `reaper_*` testcontainers Ryuk container is created BY THE DIFFERENTIAL ITSELF and is REUSED; leave it alone.** **Treat NO roster as stable.**
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`.
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL.** **`gofmt -l` never exits non-zero — gate on OUTPUT.** ⚠️ **`golangci-lint` DOES exit non-zero on findings — but STILL gate on OUTPUT.** ⚠️ **go test WITHOUT `-v` PRINTS ZERO `=== RUN`** — `RUN=0` beside `RC=0` is a VACUOUS GREEN. ⚠️ **A `-run` selector matching nothing prints `[no tests to run]` and EXITS 0 — REPRODUCED LIVE AT THIS STAGE against the post-rename selector.** ⚠️ **On `-v` output, unanchored `grep -c 'FAIL'` reads nonzero ON A FULLY GREEN TREE** — use `grep -cE '^(FAIL|--- FAIL)|^ *--- FAIL'`.
7b. ⚠️ **`grep -c` ON ZERO MATCHES PRINTS `0` AND EXITS 1** — capture (`v=$(… || true)`), NEVER chain with `&&`, and NEVER write `$(… || echo 0)`.
7c. ⚠️ **A GREEN TEST RUN IS NOT EVIDENCE A SITE IS EXERCISED** — prove it with a **`panic()` REACHABILITY CONTROL**. **This row needs it acutely: adding the Inc changed the red set by ZERO tests.**
7d. ⚠️ **NC EVERY NEW TEST, AND NC YOUR OWN FIXES** — a NC that is a BUILD BREAK proves nothing; **NEUTRALISE instead so the package still compiles.**
7e. ⚠️ **A UNIT TABLE OVER A SHARED ASSERTION IS NOT A GATE ON PRODUCTION BEHAVIOUR** — and ⚠️ **`t.Helper()` IS WHAT SAVES IT**: it gives each failure a DISTINCT call-site line.
7f. ⚠️ **A NC THAT LEAVES YOUR NEGATIVE CONTROL GREEN IS NOT EVIDENCE THAT CONTROL DOES ANY WORK** — you need a SECOND, ISOLATING NC. **The `sslLeafRoster` 2x2 is this row's worked example.**
7g. ⚠️ **ASSERT *WHICH* ERROR FIRED, AND ALSO WHICH DID NOT.** A pin proving `connection_error` moved says nothing about whether `fail_verify_error` also moved. ⚠️ **AND KEEP TABLE ROWS SINGLE-CAUSE.**
7h. ⚠️ **A PROBE CLIENT CAN WITHHOLD THE VERY THING THE ARM EXISTS TO TEST**, and ⚠️ **A TEST TABLE CAN CARRY AN INVENTED INPUT NO PRODUCTION PATH PRODUCES.** Both fired on this row: Go silently sent an EMPTY cert chain for an UNPARSEABLE leaf under TLS 1.3, and the landed classifier table carries a hand-written string error whose TEXT matches a syscall sentinel its VALUE does not. **Build counter tables on PRODUCTION-REPRESENTATIVE values and NC the identity-vs-text distinction.**
8. ⚠️ **`pgrep -f` / `pkill -f` MATCHES YOUR OWN SHELL AND KILLS THE TOOL CALL (exit 144)**. **Kill ONLY PIDs you captured yourself.**
9. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-*`, `wt-*` or stage worktree should outlive its stage close** — expect only `master` (verify with `git worktree list`).
10. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** This stage is **docs-only, ZERO production `.go` COMMITTED**, so it ran NO code gates and claims NONE — ⚠️ **though its measurement agents DID compile and run prototypes in throwaway worktrees, and every one proved its tree clean and committed NOTHING.** The last full measurement is the phase-93 IMPL's: (a) differential **121/121 PASS**; (b) non-Docker sweep **RC=0, FAIL=0, 234 packages**, gated on `PIPESTATUS[0]` and a SET RECONCILIATION (⚠️ **NOT `INNER_EXIT`, which DOES NOT EXIST**); (c) h2spec **`95 tests, 94 passed, 1 skipped, 0 failed`**; (d) fuzzers **56 / 48**; (e) the ANCHORED panic gate `^panic:|DATA RACE|SIGSEGV`, **0**; (f) no REVIEW.md — standing departure. ⚠️ **`-race` on the DIFFERENTIAL suite is VACUOUS** — the subject is an unraced subprocess.
11. ⚠️ **THE FIXTURE INVOCATION: `go test ./test/differential/ -run 'TestDifferential/0004-h2-routing' -count=1 -v 2>&1`.** ⚠️ **`-count=1` IS NOT OPTIONAL** — the harness builds envoy-go as a **SUBPROCESS**, so a production edit is **not a compile-time input** to that test binary and the cache serves a stale PASS. ⚠️ **Gate (a)'s failure mode is a SILENT PASS.** **ASSERT THE FIXTURE SET BY NAME, BOTH DIRECTIONS.** ⚠️ **The full suite takes ~400s.**
11b. ⚠️ **`go test ./...` DRIVES DOCKER — AND THERE ARE TWO DOCKER DRIVERS.** Besides `test/differential`, **`test/conformance/h2spec`** also drives Docker. **Exclude BOTH:** `go list ./... | grep -vE '/test/differential$|/test/conformance/h2spec$'`.
12. ⚠️ **`-p` PUBLISHING WORKS; `--network host` IS WHAT DOES NOT.** ⚠️ A **STRICT_DNS** cluster whose name does not resolve **blocks listener init entirely** — prefer `STATIC` by IP. ⚠️ **envoy-go REJECTS `match.headers`**, ⚠️ **refuses `codec_type: HTTP2` without TLS unless `--allow-h2c`**, ⚠️ **boots with `-c`, NOT `--config-path`**, and ⚠️ **`access_log[].log_format` is BOOT-REJECTED** (`bootstrap.go:1162`). ⚠️ **`clusters: []` — THE INHERITED BOOT-REJECT CLAIM DID NOT REPRODUCE; do not repeat it as fact.** ⚠️ **`TLSv1_0`/`TLSv1_1` IN A `tls_params` BLOCK DOES BOOT-REJECT** (`internal/tls/params.go:62-83`). ⚠️ **`ENVOY_TARGET.md` is at `docs/envoy-go/ENVOY_TARGET.md`, NOT the repo root.** ⚠️ **A PROBE BACKEND MUST BIND `0.0.0.0`, NOT `127.0.0.1`.** ⚠️ **The pinned reference is `envoyproxy/envoy:contrib-v1.37.2`, digest `7edd5b0fd763…`.**
13. ⚠️ **A RAW-FRAMER PEER MUST HOLD ONE `hpack.Decoder` PER CONNECTION.** ⚠️ **Set `ReadMetaHeaders` to NIL when probing malformed shapes.** ⚠️ **`0004` is TLS+ALPN-h2, NOT h2c.**
14. ⚠️ **`bash /dev/tcp` + `timeout cat <&3` INSIDE COMMAND SUBSTITUTION RETURNS SILENTLY EMPTY.**
15. ⚠️ **GO DOES NOT FLAG UNUSED CONSTANTS.** Post-removal cleanup is grep-driven.
16. ⚠️ **`golangci-lint`'s misspell runs in locale US and HAS FIRED ON THREE CONSECUTIVE STAGES' PROTOTYPES.** **Sweep British spellings in `.go` comments BEFORE the gate.** ⚠️ **Markdown prose may use them freely.**
17. ⚠️ **A GREEN RUN OF A CONTENTION-SENSITIVE TEST PROVES NOTHING.** Phase 91's subject cleared 5/5 uncontended while failing 9/32 contended on the SAME binary.
18. ⚠️ **AN ABSENCE CLAIM NEEDS A POSITIVE CONTROL AND A DISCRIMINATING NEGATIVE CONTROL.**
19. ⚠️ **DERIVE next-free ADR FROM THE TAIL, NEVER FROM THE HEADING COUNT.** The id space is SPARSE (exactly one gap, `0209`) and **headings+1 now reads 316 — a TAKEN id.**
20. ⚠️ **`reference_measured_prototype_is_a_lower_bound` HAS NOW FIRED NINE CONSECUTIVE ROWS, ALWAYS BY UNDER-ENUMERATING *FILES*.** At this stage the inherited roster named six sites; the true surface is **eight mandatory test-edit sites across five files, plus SIX prose blocks in one production file and FOUR fixture prose files**. **ENUMERATE BY `git grep -l` ON THE LITERAL CLAIM TEXT, AND BY COMPILING PROTOTYPE.**
21. ⚠️ **A MEASUREMENT THAT GOVERNS A DEFERRAL DECISION MUST LAND IN A GOVERNING DOCUMENT.** Phase 77's reference measurement stayed in its BRAINSTORM and was lost for seventeen rows. **DISCHARGED AT THIS STAGE: the rule now lives in `ADR-0316 §Context ¶2`.**
22. ⚠️ **A STAGE'S FILE SCOPE IS ITSELF MEASURABLE — MEASURE IT RATHER THAN INFERRING IT FROM AN INSTRUCTION'S WORDING.** `BRAINSTORM.md` §10 item 3 read as an instruction to edit the contract at the SPEC; the two nearest SPEC commits proved otherwise. **`git show --numstat` on the precedent stage settles this in one command.**

## ⚠️ THE `PROPOSED` GUARD — RE-ARMED AT THIS STAGE

| form | now | where |
|---|---|---|
| `^> \*\*STATUS: PROPOSED` — **the house form** | **ARMED** | the single hit is at `DECISIONS.md:18808`; a BACKWARD heading search resolves its enclosing block to `## ADR-0316` |
| `^\*\*Status:\*\* PROPOSED` — **the ADR-0231 decoy** | unchanged | `:14866` under `## ADR-0231`, byte-untouched |

⚠️ **VERIFY BY LINE AND BY ADR, NEVER BY THE COUNT ALONE.** ⚠️ **NEVER gate on the UNANCHORED form nor the middle-ground `^\*\*Status:\*\*.*PROPOSED` form.** ⚠️ **AND DO NOT QUOTE A COUNT FOR EITHER FORM IN PROSE THE GREP MATCHES — the ADR's own STATUS line deliberately names none, because that line is a hit of both forms and any figure would be falsified by its own landing.** **The phase-94 IMPL DISARMS the house form when it completes the block.**

## ⚠️ THE ARCHIVE GUARD

⚠️ **THE STRICT `163` IS NOT THE ENTRY COUNT.** Measured at this close: loose **221** = strict **163** + parenthetical **58**, exactly. **The guard is SOUND as a DELTA-0 SHAPE check** (a correctly-shaped parenthetical append moves it by 0 — and it did) **but the number is a SUBSET.** The forms disagree and **any gate must name WHICH it uses.**

⚠️ **THE ARCHIVE'S POSITIVE CONTROLS ARE SELF-INCREMENTING.** A control figure recorded in the file it measures is invalidated by the act of recording it. ⚠️ **THIS STAGE'S ARCHIVE LINE THEREFORE NAMES NO CONTROL FIGURE AT ALL.** ⚠️ **The eviction check has the same trap:** if the §Recent preamble NAMES the evictee, a naive grep on `STATE.md` reads **1**. **The DISCRIMINATING form is the strict `^- \*\*prior active-phase:\*\* \`<label>\`` form.** ⚠️ **AT THIS CLOSE BOTH forms read 0 on `STATE.md`**, because the preamble describes the evictee without spelling its label — **do that.**

⚠️ **NO DATE TIE EXISTED AT THIS CLOSE.** A DIRECT DATE READ gave `2026-09-01`, `2026-08-31`, `2026-08-29` x2, `2026-08-26` — a UNIQUE oldest, so no tie-break was applied. ⚠️ **THAT IS THE THIRD DIFFERENT SHAPE IN THREE CONSECUTIVE ROWS: the phase-93 IMPL found uniqueness where its PLAN recorded a tie, the phase-94 BRAINSTORM found a genuine tie, and this close finds uniqueness again. INHERIT NEITHER — READ THE DATES YOURSELF.**

## ⚠️ PORT BANDING

`net.ipv4.ip_local_port_range = 32768 60999`. ⇒ **BAND YOUR PROBE PORTS BELOW 32768.** ⚠️ **The differential harness reserves `20000..31007` (`harness_test.go:293`)**, so `12000-19000` is the safe band for ad-hoc probes — a failure OUTSIDE the harness band is an **out-of-band** occurrence, NOT an in-band recurrence. **Check availability with `ss -tan` (ALL states), never `ss -ltn`.** ⚠️ **Parts of `12000-19000` ARE OCCUPIED by the sibling `curl-world` containers (18080-19000 observed at this stage) — CHECK, do not assume.** ⚠️ **FIXTURE ports: the `10<index>` convention is DOMINANT but COLLIDES for `0120` — `10120` is held by `0028`'s `10120-10125` run. `10126` IS THE SPEC'S PICK** (index-preserving repair; `10126-10129` all ZERO in code scope). ⚠️ **DO NOT cite `0118:31`'s `10450` "TLS/SDS band" carve-out — its premise is FALSIFIED** (`0112`/`0113` carry ZERO `DownstreamTlsContext`). ⚠️ **`0120` as a DIRECTORY is FREE and is CLAIMED BY ROW 94 at its IMPL.**

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (**ugrep 7.8.4**). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **THE METHOD BINDS; carry no standing discrepancy figure and no standing absolute.** ⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS; print FILE NAMES, never a count.** ⚠️ **THE HARNESS `grep` STRIPS A LEADING `./`.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files; use `-v --no-index`, and `git status` is the discriminating check). ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **A DISCREPANCY MAY FAIL TO MANIFEST** — absence of a discrepancy is not evidence the blindness is gone.

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 94 (tls-connection-error-stat)' --format='%h %s'   # the SLUG form
git log --grep '^phase 94' --format='%h %s'                               # the LOOSE form — RUN IT to CROSS-CHECK
```
⚠️ **RUN BOTH FORMS AND RECONCILE THE SETS. NO COUNT IS QUOTED HERE, DELIBERATELY — BOTH FORMS ROT.** **A `| head -1` on ONE form can silently return a NON-TIP commit. Take the tip from `git rev-parse master`.** ⚠️ **AND WHEN YOU LAND A CORRECTION, PUT THE FULL SLUG IN ITS SUBJECT** — a slugless correction is invisible to the slug form.

Execution style: **subagent-driven per `feedback_execution_style`**. Subagents commit LOCALLY on their own stage branch with EXPLICIT PATHSPECS; the controller merges and squashes (`feedback_subagents_no_push`, `feedback_push_to_origin`). **Pure MEASUREMENT agents should commit NOTHING and prove their tree clean** — three did so at this stage, each with a `sha256sum` restore verification where it had patched a tracked file. ⚠️ **PARALLEL AGENTS IN ONE WORKTREE RACE THE GIT INDEX EVEN WITH DISJOINT PATHSPECS** — use **PER-AGENT WORKTREES ON THEIR OWN BRANCHES**. ⚠️ **GIVE EACH PARALLEL AGENT ITS OWN PRIVATE SCRATCH DIR**, and **SERIALIZE DOCKER OWNERSHIP TO EXACTLY ONE AGENT AT A TIME** — at this stage ALL THREE were Docker-free by instruction. ⚠️ **VERIFY WHAT A SUBAGENT REPORTS — BUT ALSO BELIEVE IT WHEN IT REFUTES YOU, AND CHECK IT WHEN IT REFUTES ANOTHER AGENT.** Per-task `gofmt` + `golangci-lint` on touched packages, **gated on OUTPUT**. ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root** — build with `-o` into scratch.

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root. Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` before any commit or gate run. ⚠️ **Shell VARIABLES do not survive between Bash calls** — re-export in every command. ⚠️ **AND AN UNQUOTED HEREDOC (`<<EOF`) COMMAND-SUBSTITUTES BACKTICKED TEXT** — use `<<'EOF'` and pass paths via the environment.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — `git status --porcelain` AND `git diff --stat master` must show ONLY your intended edits. For a patched TRACKED file: `sha256sum` BEFORE patching, `git checkout -- <path>` after, `sha256sum -c` the capture — ⚠️ **and COMMIT FIRST, because `git checkout --` restores from HEAD and will wipe uncommitted work.** ⚠️ **Also check the MAIN repo root, and `--untracked-files=all`.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads RUN THE PHASE-94 PLAN.** ⚠️ **`grep -c 'next-skill:'` reads 2: one is the ADR-0288 RULE STATEMENT matching its own grep, and the live pointer is the other. That is CORRECT, not a violation.** ⚠️ **§Project counts is a PHASE-76-ERA SNAPSHOT and SAYS SO.**
2. `docs/envoy-go/phases/94-tls-connection-error-stat/SPEC.md` — **500 lines. §16 is the PLAN's owed list; §0 is the twelve refutations; §3 the resolved D-TLSCE docket; §6 the EDIT ROSTER with executed classifications; §7 fixture `0120`; §8 the NC roster; §11 the pinned contract edit map; §13 the sentinel run.**
3. `docs/envoy-go/phases/94-tls-connection-error-stat/BRAINSTORM.md` — **412 lines. §2 is the reference measurement.** ⚠️ **§7.2's verdict column is REFUTED — see SPEC §3.3.**
4. `docs/envoy-go/DECISIONS.md` **ADR-0316** (§Context, eleven paragraphs, drafted at the SPEC) · **ADR-0296** / **ADR-0297** — the phase-74 and phase-75 decisions this row extends.
5. `docs/envoy-go/BEHAVIOR_CONTRACT.md:1965-1975` — the departure this row closes. ⚠️ **BYTE-UNTOUCHED until the IMPL.**

## What the next session owes
**RUN THE PHASE-94 PLAN** (state 2 -> 3) — ⚠️ **ONE STAGE PER SESSION.** Discharge the eight items at `SPEC.md` §16 · ⚠️ **BOOT BOTH SIDES ON THE `tls_params` CONFIG BEFORE WRITING ANY ARM** — it is unprecedented in this tree and is the row's largest unmeasured risk · ⚠️ **RUN NC 1 BOTH WAYS**, since removing the predicate is INVISIBLE until `sslLeafRoster` is extended · ⚠️ **DERIVE YOUR OWN TASK COUNT — none is inherited, deliberately** · **ROW 94 STAYS `in-progress`** (`want` STAYS **126**; `ROADMAP.md` and `BEHAVIOR_CONTRACT.md` stay **BYTE-UNTOUCHED at the PLAN**) · **`ADR-0316` STAYS in the house `PROPOSED` form — the IMPL completes and disarms it; the PLAN consumes NO new ADR number, so the tail STAYS `ADR-0316` and next-free STAYS `ADR-0317`** · **RE-RUN the sentinel + all four NCs + the check-(2) positive control yourself** — ⚠️ **check (1) PRINTS `NOT DONE: row 94`, and NC-A/NC-B are TWO-line shapes** · **REFUTE YOUR PREDECESSOR BY EXECUTION** · `STATE.md` rolled IN PLACE with the oldest §Recent entry evicted by a **DIRECT DATE READ** (⚠️ **read the dates yourself — three consecutive closes produced three different shapes**) and archived to `STATE_HISTORY.md` as **ONE INLINE LINE** in the PARENTHETICAL form, so the strict guard stays at **163, DELTA 0** · ⚠️ **AND ROLL THE §Recent PREAMBLE SENTENCE TOO — the guard is structurally blind to it** · ⚠️ **WORD THE PREAMBLE SO IT DOES NOT SPELL THE EVICTEE'S LABEL** · ⚠️ **NAME NO POSITIVE-CONTROL FIGURE IN THE ARCHIVE LINE** · `next-prompt.txt` rolled (`git add -f`) · ONE squashed commit, merged and pushed — ⚠️ **and if a late measurement invalidates something you already pushed, land a CLEARLY-LABELLED correction commit (WITH THE FULL SLUG IN ITS SUBJECT) rather than force-pushing or leaving it standing.**
