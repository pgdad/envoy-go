START a fresh cold-start session for envoy-go — **the phase-38.1 (`load-balancer-subset`) IMPL is DONE, LANDED on master, and PUSHED to `origin/master`; the next lifecycle step is the phase-38.2 SPEC** (`superpowers:writing-plans` scoped to SPEC authoring — the pre-authorized 38.2 weighted-cluster-routing leg; OR `superpowers:brainstorming` first if the SPEC session finds the weighted-cluster primitive needs a fresh charter). The substantive master tip is the phase-38.1 IMPL squash **`4eedf0e`** (`phase 38.1 IMPL: load-balancer-subset (Cluster.lb_subset_config) — the project's FIFTH LB construct + the family's FIRST wrapper`; the 33-file feature squash + the doc landing; pushed to `origin/master`; confirm via `git log --oneline -5`). The prior substantive tip is the phase-38.1 PLAN-done bundle **`4e96220`**, above the docs-only SHA-fill chain. This paragraph is the docs-only `next-prompt.txt` roll-forward that NAMES `4eedf0e` the substantive IMPL-done live tip; it sits directly above `4eedf0e`. Counts MOVED at the 38.1 IMPL: **stat surface 1121 → 1125** (+4 `lb_subsets_*`), **fixtures 65 → 66** (`0064-lb-subset`), **DECISIONS tail ADR-0238 → ADR-0240** (ADR-0239 seam+producer / ADR-0240 policy+`Endpoint.Metadata`, next-free ADR-0241); fuzzers 42 / BackendKind tail 33 UNCHANGED.

## What just landed (phase 38.1 IMPL — the durable record is PROGRESS.md + ADR-0239/0240, read those not this summary)

The subset LB wrapper `Cluster.lb_subset_config` (`envoy.config.cluster.v3`, proto field 22) — the project's FIFTH LB construct and the family's FIRST WRAPPER (a wrap-after-switch over the `lb_policy` child, NOT a sixth `case`). Built end-to-end across 14 TDD tasks, subagent-driven (two-stage review per task + a final holistic review); the ADR-0052 six-gate GREEN. As built:
- **ADR-0239 (the seam-BUILD half):** `loadBalancer.Pick(hashKey, hasHash)` widened to `Pick(hashKey, hasHash, match SubsetMatch, hasMatch bool)` — the five leaf policies absorb `(match, hasMatch)` behaviour-neutrally, only `subsetLB` consumes it; the byte-stable OPTION-C ctx-carried `cluster.WithSubsetMatch`/`subsetMatchFrom` (the `WithHashKey` template) extracted in the `Dial`/`AcquireH1`/`PickEndpoint` funnels. + the ROUTE-STATIC `RouteAction.metadata_match["envoy.lb"]` producer (`parseRouteSubsetMatch` in `hcm/config.go` → a proto-free `cluster.SubsetMatch` on `clusterRouteAction.subsetMatch`, threaded VERBATIM per request — NO per-request fold, the contrast with `applyHashKey`).
- **ADR-0240 (the policy half):** `subsetLB` (`internal/cluster/subset.go`) enumerates subsets per selector × distinct `envoy.lb` value-tuple, a `buildLeafLB` child per subset (all five children exercisable over a sub-slice); EXACT key-set resolution + the 3-way cluster-level fallback {NO_FALLBACK→errNoEndpoints/503/`UH`; ANY_ENDPOINT→all; DEFAULT_SUBSET→default-match, zero-match→NO_FALLBACK}. The NEW `Endpoint.Metadata` dimension (the FIRST per-endpoint dimension beyond `Host:Port`; `Addr()` unchanged). 4 `lb_subsets_*` stats (`active` gauge + `created`/`selected`/`fallback` counters; `active`/`created` ×1 distinct-subset count — NOT the reference's 33× artifact; `selected`/`fallback` the FIRST stats a `loadBalancer` Inc's from its own `Pick` path, request-counted + cross-side-equal). ADR-0024 NOTED (per-cluster scope holds), not amended.
- **`0064-lb-subset`:** STATIC-config NAT-transparent TRUE cross-side subset-affinity SET-membership arm + an ANY_ENDPOINT fallback arm + cross-side byte-equiv + a cross-side `StatsAsserter`; 3 `-count=1` deliberate breaks; 20/20 flake-free. ZERO new cluster reject arm (CLUSTER_PROVIDED pre-covered; the only new reject is the producer-side `router-metadata-match-nonscalar`). ZERO new packages + ZERO new go.mod deps.

## What 38.2 is (the pre-authorized second leg of the phase-38 by-plane split)

**38.2 = `RouteAction.weighted_clusters` routing + `WeightedCluster.ClusterWeight.metadata_match`.** A NEW per-request weighted-cluster-SELECTION primitive: the router resolves a SINGLE cluster per route TODAY (`hcm/actions.go` `clusterRouteAction`); 38.2 adds the `RouteAction_WeightedClusters` `ClusterSpecifier` oneof (VERIFIED to EXIST in the `/envoy v1.32.4` module cache at the phase-38 SPEC §11.1) — a route picks among N clusters by integer weight per request, and EACH `WeightedCluster.ClusterWeight` may carry its OWN `metadata_match` (field 3, `route_components.pb.go:4035`) that COMPOSES with the 38.1 subset LB (the per-weighted-cluster match feeds the cluster's subset selector). This is the hidden prerequisite the phase-38 BRAINSTORM surfaced; it was pre-authorized to its own SPEC/PLAN/IMPL (the 36.1/36.2 by-plane-split precedent — the parent BRAINSTORM chartered the split, the second leg goes straight to its own SPEC unless the SPEC session finds the design underspecified and drops back to `superpowers:brainstorming`).

The SPEC session must EXECUTE the empirical-pin discipline (ADR-0004) live against `envoyproxy/envoy:contrib-v1.37.2` (`reference_docker_probe_bridge_network`): the exact `RouteAction.weighted_clusters` proto surface + PGV (the `total_weight` default/semantics; the per-`ClusterWeight` `weight`/`metadata_match`/`request_headers_to_add`-etc. scope for the MVP); the per-request weight→cluster selection algorithm (how the weight RNG is seeded — confirm vs upstream `WeightedClusterEntry`); how a `weighted_clusters` route interacts with the EXISTING single-cluster route resolution + the 38.1 `WithSubsetMatch` thread (does each weighted cluster's `metadata_match` ride the SAME ctx seam?); the reject/accept matrix; the stat surface delta (likely none new — weighted clusters reuse `upstream_rq_*`); the differential design (cross-side weight-distribution `AssertDistribution` with σ-margins per `reference_differential_band_sigma_margin`); the ADR roster (≥1 anticipated — the weighted-cluster routing primitive). Anchor ADR §Context drafts at the SPEC (bodies at the IMPL per ADR-0044); next-free ADR is **ADR-0241**.

## Read first (in order)

1. THIS FILE — the post-phase-38.1-IMPL cold-start (next: the phase-38.2 SPEC).
2. `docs/envoy-go/STATE.md` — active-phase `phase 38.1 (load-balancer-subset) IMPL done`; the counts (fixtures 66 / fuzzers 42 / stat surface 1125 / BackendKind 33 / DECISIONS tail ADR-0240, next-free ADR-0241); the lifecycle routing (→ the 38.2 SPEC).
3. `docs/envoy-go/phases/38-load-balancer-subset/` — BRAINSTORM.md (§1.4/§2.3 the 38.1/38.2 by-plane split; the weighted-cluster prerequisite) + SPEC.md (§11.1 the `RouteAction_WeightedClusters` oneof + `ClusterWeight.MetadataMatch` field-3 verification) + PLAN.md + PROGRESS.md (the 38.1 as-built record the 38.2 SPEC composes against).
4. `docs/envoy-go/ROADMAP.md` — row 38 (38.1 leg `done`; the flat family row; 38.2 the pre-authorized next leg; the Load-balancing family stays OPEN — 3 health-gated candidates remain after 38.2).
5. `docs/envoy-go/DECISIONS.md` — ADR-0239 (the subset-match seam-input + the `RouteAction.metadata_match` producer — the seam + producer 38.2 EXTENDS/COMPOSES with) + ADR-0240 (the subset LB policy + `Endpoint.Metadata`) + ADR-0237 (the HTTP route `hash_policy` producer — the route-action producer STYLE TEMPLATE) + ADR-0235 (the pick-input seam) + the process ADRs (0044/0045/0052/0080/0106/0004/0227).
6. The 38.1 as-built code 38.2 builds ON: `internal/filter/hcm/{actions.go [clusterRouteAction + the ClusterAction call sites], config.go [buildRouterAction + parseRouteSubsetMatch]}` + `internal/filter/http/router/{router.go, router_h2.go [the H1/H2ClusterAction + the WithSubsetMatch dispatch thread]}` + `internal/cluster/{subset.go [subsetLB + SubsetMatch], cluster.go [WithSubsetMatch/subsetMatchFrom + the Pick funnels]}` + `test/fixtures/0064-lb-subset/` (the cross-side HTTP-route + StatsAsserter differential precedent) + `test/differential/fixture/fixture.go [DistributionAsserter, StatsAsserter, BackendKind tail]`.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_execution_style` (subagent-driven IMPL), `feedback_git_worktrees`, `feedback_subagents_no_push` + `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_path_targeting`/`_detach`, and the differential references (`_break_protocol_count1`, `_run_selector`, `_band_sigma_margin` [weight-distribution σ-margins], `_hash_key_cross_side_infeasible`, `_fixture_workload_constant_desync`, `_docker_probe_bridge_network`, `_asserter_dispatch`, `_fixture_dispatch_constraint`).

## Honor throughout

- The lifecycle is BRAINSTORM → SPEC → PLAN → IMPL, each its own session. The next session is the phase-38.2 **SPEC** (`superpowers:writing-plans` scoped to SPEC authoring — the empirical-pin discipline live; the ADR §Context drafts; the differential design). If the SPEC session finds the weighted-cluster primitive underspecified, drop back to `superpowers:brainstorming` for a fresh 38.2 charter FIRST.
- `feedback_git_worktrees` · `feedback_execution_style` (subagent-driven at the IMPL; one fresh subagent per task + two-stage review) · `feedback_subagents_no_push` (subagents commit local-only) · `feedback_push_to_origin` (the controller squash-merges + pushes at stage-close) · `feedback_pertask_gofmt_lint`.
- ADR-0004 (the empirical-pin discipline — EXECUTE at the 38.2 SPEC) · ADR-0044 (ADR §Context at SPEC, bodies at IMPL in-place) · ADR-0045 (the split-gate — re-check at the 38.2 PLAN) · ADR-0052 (the atomic-landing six-gate) · ADR-0080 (byte-stable parse-reject wording) · ADR-0106 (flat family row — NO parent rollup) · ADR-0227 (the contrib reference image).

## After phase 38.2 — the longer horizon (see `ROADMAP.md` §Feature Families for the authoritative detail)

Forward-looking ONLY — the IMMEDIATE next step is the phase-38.2 SPEC above. After 38.2 the Load-balancing family has **3 candidates left** {locality-weighted LB, priority load balancing, panic thresholds} — **ALL gated by the absent health-checking boundary**, so they CANNOT land until the Upstream-robustness family exists. Per ADR-0106 sibling rows are NOT pre-populated; the next subject (a remaining LB candidate, or a NEW family) is chosen at the next BRAINSTORM.

**Six families are essentially unstarted** — each opens with its own BRAINSTORM (`superpowers:brainstorming`), itself MVP-scoped ("parity" = behavioral equivalence vs the pinned `contrib-v1.37.2` under the differential contract, MISSION §2.1). Roughly by size/impact:
1. **xDS / dynamic config** — the single largest gap; the whole project is STATIC bootstrap today (ADS, delta xDS, LDS/CDS/RDS/EDS/SDS/RTDS, reconnection, initial-fetch timeout).
2. **Upstream robustness** — active health checks (HTTP/TCP/gRPC/custom), outlier detection, circuit breakers, retries + hedging, per-protocol connection pooling; this UNBLOCKS the 3 remaining health-gated LB candidates.
3. **HTTP/3 + QUIC** — quic-go transport, downstream H3 listener, upstream H3 cluster, the `h3spec` gate (only H1/H2 exist today).
4. **gRPC** — gRPC bridge, gRPC-Web, gRPC-JSON transcoding, interop conformance.
5. **Observability** (beyond the baseline Prometheus + file access log already done) — gRPC ALS, OTLP access log, OTel/Zipkin/Jaeger/Datadog/XRay tracing, stats sinks, tap filter.
6. **Runtime + hot restart** — the RTDS-driven runtime layer; hot-restart / graceful-drain beyond phase 08's minimum.

The §9 **Network-filters family is CLOSED** (phases 26–33). The **HTTP-filters family** (phases 09–25) shipped its curated set but is NOT formally closed. The **WASM host family** is partially satisfied (proxy-wasm HTTP-filter conformance passes via phase 25; a broader standalone host sub-project remains on the books).
