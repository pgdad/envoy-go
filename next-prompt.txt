=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **PHASE 93 (`h2-local-reply-content-length`) IS OPEN AT LIFECYCLE-STATE 1.** Row 93 reads `in-progress` at `ROADMAP.md:155`, the phase directory exists and holds `BRAINSTORM.md` (**503 lines**) and NOTHING ELSE. ⇒ **THIS SESSION RUNS THE PHASE-93 SPEC (lifecycle-state 1 -> 2). DO NOT SELF-PICK — there IS banked mid-lifecycle work and advancing it is the defensible move.**

⚠️⚠️ **THE SENTINEL'S MARGIN IS TWO ONLY BECAUSE THIS ROW IS OPEN.** Check (1) went SILENT at the row-92 flip and was re-armed solely by row 93's insertion. **The moment row 93 flips `done`, check (2) is again the ONLY thing blocking termination.** Do NOT read a two-deep margin as comfort, and do NOT "tidy" a deferred-candidate line — deleting the last one ENDS THE PROJECT. ⚠️ **MEASURE ALL THREE YOURSELF BEFORE DOING ANYTHING ELSE; do not take this file's word for it.**

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE. This form parses FIELDS instead of matching a pattern, and
#     asserts the row DENOMINATOR so a deleted row is itself an error.
#     ⚠️ want IS NOW 125 — row 93 was registered at the phase-93 BRAINSTORM.
awk -F'|' -v want=125 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **CHECK (1) IS ARMED AGAIN AND PRINTS `NOT DONE: row 93` — ONE line.** That is CORRECT while row 93 is open. Run BOTH doctoring NCs EVERY SESSION anyway, because a check whose output you predicted is indistinguishable from a broken one:
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md.
```
⚠️ **NC-A NOW READS **TWO** LINES** (`NOT DONE: row 62` THEN `NOT DONE: row 93`). **The phase-92 ONE-line shape was the all-`done` board's and DOES NOT CARRY.** ⚠️ **This is the SEVENTH distinct NC-A shape in eight stages. It changes again the moment row 93 flips — re-measure, never inherit.**
```sh
# the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```
Also NC the DENOMINATOR (`want=124` on the real file must print BOTH `NOT DONE: row 93` AND `GATE FAIL: examined 125 data rows, expected 124`) — ⚠️ **NC-B IS NOW A TWO-line result; it was ONE line before this insertion.**

⚠️ **AND WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -c '-family row'` fails (`grep: amily row: No such file or directory`) and the surrounding arithmetic prints `0`, **which reads exactly like "no change."** **Always pass `--` before the pattern**, and the counts are form-dependent: with `--`, occurrences read **95** and LINES read **67**. **`grep -c` counts LINES, NOT OCCURRENCES.** ⚠️ **AND `grep -c` PRINTS `0` *AND* EXITS 1, so `$(cmd || echo 0)` EMITS TWO ZEROS** — capture with `v=$(cmd || true)`.

⚠️ **THE MALFORMED-ROW FIGURE RECONCILES ONLY UNDER AN ESCAPE-AWARE FIELD COUNT.** Malformed rows are ids **57** (`NF=9`) and **69** (`NF=10`) — **UNCHANGED by row 93, measured on both sides.** ⚠️ **A NAIVE `awk -F'|'` `NF != 8` FORM READS SEVENTEEN.** **Strip `\|` BEFORE splitting.** The two forms **DISAGREE on row 57** but **AGREE on row 69**, so **any gate must state WHICH FORM it uses.** ⚠️ **COUNT A ROW'S FIELDS BEFORE INSTALLING ANY EDIT** — row 93 was counted off-line first and reads **NF=8 under BOTH forms**.

⚠️ **AND DO NOT SUBSTITUTE A "REASONABLE EQUIVALENT" FOR CHECK (1)'s AWK.** A measurement agent once tried `NF>=8 && $2 ~ /^ *[0-9]+ *$/`: it printed **92**, not the true denominator. **Use the form above verbatim.**

**SENTINEL RUN MECHANICALLY AT THE PHASE-93 BRAINSTORM, ON BOTH SIDES OF THE ROW-93 INSERTION.** ACTUAL output, recorded not predicted:
- **PRE-INSERTION:** (1) SILENT at `want=124` · (2) **SIX** at `:202 :208 :214 :224 :230 :238` · (3) SILENT. NC-A **ONE** line, NC-B **ONE** line, NC-C fired (WASM control 2), NC-D long **5** / short **1** / union **6**.
- **POST-INSERTION:** (1) `NOT DONE: row 93` — **ONE** line at `want=125`, **TWO** lines at the stale `want=124` · (2) **SIX** at `:203 :209 :215 :225 :231 :239` — **anchors shifted +1, COUNT AND CONTENT UNCHANGED, and row 93 adds NO new window (measured 0)** · (3) SILENT. **NC-A TWO lines, NC-B TWO lines**, NC-C fired (`NEVER OPENED: gRPC`, WASM control 2), NC-D **5 / 1 / 6**.
⇒ **THE SENTINEL DOES NOT FIRE. `stop` WAS EVALUATED AND DELIBERATELY NOT CREATED** (verified absent at the git root and in every stage worktree).
- ⚠️ **LIVE FIGURES RE-DERIVED AT THIS TIP:** `ROADMAP.md` **243** / **125** data rows / tail row **93** · `DECISIONS.md` **18646** (**BYTE-UNTOUCHED this stage**) · `^---$` **216** · `^## ADR-` **313** · bare `^## ` **321** (313 + 8 `## Amendment`) · tail **ADR-0314** · next-free **ADR-0315** · strict `PROPOSED` guard **1** (the ADR-0231 decoy at `:14866`, LEFT ARMED) · **UNANCHORED `PROPOSED` reads 90 LINES / 101 OCCURRENCES — NEVER GATE ON IT** · `STATE.md` **63** · `STATE_HISTORY.md` **532**, strict **163 (DELTA 0)** / colon **165** / loose **216** · phase dirs **134** · fixtures **121** (tail `0119`, **`0120` FREE**) · fuzzers **56 targets / 48 FILES** · `BEHAVIOR_CONTRACT.md` **5966** (**BYTE-UNTOUCHED this stage**) · `go.mod` **67** (18 direct + 49 indirect) · BackendKind tail **38** (39 declared) · `-family row` **95 / 67**.
- ⚠️ **THE STAT SURFACE IS QUOTED AS A DELTA AND NEVER AS AN ABSOLUTE.** THREE different absolutes are live in this tree at one tip. Per `reference_a_drift_correction_is_itself_a_claim`, on a contested count: **NO NUMBER.** ⚠️ **AND `406` / `406 -> 407` MUST NOT BE RESTATED ANYWHERE.**
- ⚠️ do NOT "fix" the six in check (2) and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases. ⚠️ **Cite ~40 only as "sentence-visible", never as the inventory** — and **no gate may rest on a ` + ` split, in any form.**

==============================================

## STATUS AS OF 2026-08-29: PHASE 93 (`h2-local-reply-content-length`) **OPEN AT STATE 1**. **THIS SESSION RUNS THE PHASE-93 SPEC.**

**What the 93 BRAINSTORM landed.** Row 93 REGISTERED `in-progress` (`want` **124 -> 125**; NF=8 under BOTH forms, counted BEFORE the edit). `BRAINSTORM.md` created (**503 lines**). `STATE.md` rolled IN PLACE **and TWO frozen regions inside it de-rotted**. `STATE_HISTORY.md` **530 -> 532** with the strict guard **163 -> 163, DELTA 0**. **`DECISIONS.md` and `BEHAVIOR_CONTRACT.md` are BYTE-UNTOUCHED** — no ADR is written at a BRAINSTORM.

### ⚠️ THE CHARTER
**On the HTTP/2 leg a locally generated reply emits a `Content-Length`, ALWAYS, valued at the body length.** The fix lands in **`h2LocalReplyHeaders()`** (`internal/filter/http/router/router_h2.go:294`) and **deliberately NOT in `writeH2Reply`**. **And because a content-length ARITY pin is structurally incapable of seeing the empty-body defect underneath it, the row ALSO upgrades the fixture's instrument per side** — landing the fix without that would leave a green gate over a live divergence.

### ⚠️ THE FINDINGS THE SPEC MUST NOT RE-LEARN

1. ⚠️ **THE BANKED "SEVEN CALL SITES" COST IS THE BLAST RADIUS OF THE SYMPTOM, NOT THE FIX.** `writeH2Reply` (`h2dispatch.go:1014`) already RECOMPUTES `content-length` from the real body for a field ALREADY PRESENT and merely never INJECTS one — measured by an overlay probe in which a deliberate `999` came out on the wire as `12`. **One helper edit reaches all seven sites. Cost floor `+3 / -0` in ONE production file.**
2. ⚠️ **THE H/1 SIBLING'S `bodyLen` IS INERT TWICE OVER** — all six callers pass literal `0`, and `writeH1Reply` (`codec.go:87-89`) overwrites the value anyway. **Mirroring H/1's signature buys nothing and costs seven call-site edits.**
3. ⚠️ **`h2LocalReplyHeaders()` IS THE SOLE OMITTER AMONG FIVE LOCAL-REPLY COMPOSERS** (`beginLocalReply` `chain.go:1313`, `directResponseAction.body()` `actions.go:95`, `writeH3Reply` `h3dispatch.go:56`, the H/1 sibling `router.go:675`). All 36 filter files calling `SendLocalReply` route through ONE definition that sets the header unconditionally — **a filter's 403 already carries one where the router's 502 does not. This is an internal inconsistency before it is a cross-side one.**
4. ⚠️ **THE RULE IS ALWAYS-EMIT, NOT EMIT-WHEN-NON-EMPTY — AND THE CONTROLLER'S OPPOSITE DESIGN WAS REFUTED BY ITS OWN AGENT.** `BEHAVIOR_CONTRACT.md:2164` holds a MEASURED reference transcript of an **empty-body** local reply carrying **`content-length: 0`**, ratified as ADR-0085 doctrine at `DECISIONS.md:8260`/`:8326`. A `len(body) > 0` guard would SUPPRESS a header the reference demonstrably sends.
5. ⚠️ **THE TREE HOLDS TWO CONTRADICTORY RATIFIED RULES about `content-length: 0`**, and `writeH3Reply` (`h3dispatch.go:47-49`, pinned at `h3dispatch_test.go:155`) is the one that disagrees with the measurement. ⚠️ **The H/3 arm is UNMEASURED — RECORD it, do NOT tidy either rule into the other.**
6. ⚠️ **A WRITER-SIDE PLACEMENT IS A MEASURED HAZARD.** `writeH2Reply` serves the PROXIED path (`h2dispatch.go:677`), and **the compressor filter DELIBERATELY deletes `Content-Length` on the compress path** (`compressor.go:784`, *"mirrors Envoy's compressor_filter.cc behavior"*). A writer-side injection re-injects what that filter stripped and diverges H/2 from its own H/1 sibling.
7. ⚠️ **AN ARITY PIN CANNOT SEE A MISSING BODY, AND THAT BLINDNESS IS ALREADY LIVE ON THE H/1 LEG TODAY.** Measured end-to-end: H/1 local replies emit `Content-Length: 0` with `Body: nil` ⇒ arity 1 against the reference's arity 1, while the reference's body is 87 / 167 / 19 / 24 bytes. **Pinned by NOTHING** — the reference's own strings appear in ZERO files under `test/`.
8. ⚠️ **THE ROW IS GATED BY EXACTLY ONE INHERITED ASSERTION AND INHERITS NO UNIT COVERAGE.** The phase-92 per-side pin reddens naming ALL FIVE arms through `AssertDistribution` (below `CompareBytes`), **vindicating phase 92's placement decision by a live red run** — while **ZERO Go unit tests redden** across 126 unit-test packages. **The row must bring its own unit coverage.**
9. ⚠️ **A STALE TEST CACHE SERVED A VACUOUS GREEN THROUGH THIS EXACT FIX, BY A MECHANISM NOT PREVIOUSLY RECORDED.** `test/differential` reported `ok (cached)` with a modified `router_h2.go`, because **the harness builds envoy-go as a SUBPROCESS and the router is not a compile-time input to that test binary.** ⇒ **`-count=1` ON EVERY `test/differential` INVOCATION, UNCONDITIONALLY.**
10. ⚠️ **`go test ./...` INCLUDES `test/differential` AND THEREFORE DRIVES DOCKER.** Scope cost runs with `go list ./... | grep -v '/test/differential$'` unless the differential IS the measurement, and serialize Docker ownership across parallel agents.
11. **Response header ORDER is NOT a contract** (`BEHAVIOR_CONTRACT.md:15`, *"Set-equal modulo documented allow-list"*). The reference emits `content-length` FIRST on a local reply. **Do NOT chase that ordering and do NOT "fix" the helper's documented insertion order.**
12. ⚠️ **A `ROADMAP` ROW'S ANTICIPATED COUNTS ARE NEVER RECONCILED AT ROW-DONE.** Row 92 still reads `fuzzers 55 / 48` — and the commit that made it **56** is row 92's OWN IMPL. **Take counts from a command, never from a row.**

### ⚠️ BANKED CANDIDATES — costs re-derived at the phase-93 BRAINSTORM

- ⚠️ **THE EMPTY-BODY DEFECT, SPANNING BOTH LEGS — NEWLY BANKED, and its reference side is a MEASUREMENT.** Ten sites (four body-less H/2 at `router_h2.go:80/:128/:138` + `retry.go:374`; all six H/1). **Reference bodies MEASURED:** 87 B (502 protocol error), 167 B (503 connect-refused), 19 B (`no healthy upstream`), 24 B (504 `upstream request timeout`). Prototype `+20 / -4` over FOUR files, suite RC=0. ⚠️ **Three gotchas: the 167-B string is the CONNECT-FAILURE body and the three 503 sites do NOT share one string; adding a body emits a DATA frame where END_STREAM used to ride HEADERS; access-log `bytesSent` moves 0 -> 19.**
- **The pooled-upstream-lifetime defect** — three sites, all re-verified UNFIXED. ⚠️ **Its D2 ordering window GREW when the phase-92 IMPL inserted its new 502 arm between the evict and the ctx check.** Still deferred: reference side is a PREDICTION, three packages, probable SPLIT.
- **`ssl.connection_error`** — now **MEASURED at ~4-6 production lines across 2 files** (`manager.go:187`/`:388`/`:1294` + `internal/stats/name.go:560`), with **five `outcomeOther` rows already waiting** at `manager_test.go:4394-4402`. The best next IN-WINDOW pick. ⚠️ **Its banked anchor `:223` is STALE — the window is now `:225`.**
- **1xx interim responses** — DROP-AND-DELIVER; `respHeadersSeen` set unconditionally at `client.go:695`, the `:status` parse already at `:697-700` but AFTER the flag. ~5-15 lines, one symbol.
- **The H/1 no-`Host` divergence** — ⚠️ **its `+90/-0` is a STALE PREDICTION, NOT re-derivable.**
- **`allocateOTLPPort`** — ⚠️ **26 driver files carry both a `127.0.0.1:0` probe and a `0.0.0.0:%d` bind**, not one site.
- **Counting the stat surface MECHANICALLY** — BLOCKED, not merely deferred; both ledger discontinuities re-confirmed.

## ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + ALL FOUR NCs YOURSELF** and record ACTUAL output. ⚠️ **The SPEC does NOT change a row's status, so measure your NO-OP too — a `ROADMAP.md` `--numstat` of nothing is itself the evidence.**
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** 92 IMPL twelve · **93 BRAINSTORM ELEVEN, THREE of which refuted the CONTROLLER'S OWN instructions mid-stage.** Do the same, and record it when your agents refute you.
3. **LINE ANCHORS DRIFT; SYMBOL AND LITERAL-TEXT ANCHORS DO NOT — RE-VERIFY EVERY CITE AT YOUR TIP.** ⚠️ **PATHSPEC-SCOPE every symbol assertion.** ⚠️ **A RECEIVER WRITTEN `(cc *ClientConn)` MUST USE `grep -F`, NOT `-E`.** ⚠️ **A banked ROADMAP LINE anchor rots on EVERY row insertion — anchor on window CONTENT.**
3b. ⚠️ **CHARACTER-CLASS AND ESCAPE SELECTORS ARE THE FAIL-UNSAFE FAMILY.** `grep -cE '^\s+[a-z0-9./-]+ v[0-9]'` on `go.mod` reads 62 not 67 · `grep -oE '[A-Za-z]+ +BackendKind = 38'` TRUNCATES `H2` · `^[0-9]{4}-` reads 119, dropping `0007a`/`0007b` (use `ls -d test/fixtures/*/ | wc -l`) · ⚠️ **`\t` in a regex is TOOL-DEPENDENT: harness `grep` is ugrep 7.8.4, `/usr/bin/grep` is GNU 3.11. NAME THE TOOL.** Safe under both: `-P`, or `$(printf '\t')`.
3c. ⚠️ **`feedback_brief_citations_not_evidence` APPLIES TO INCIDENTAL FIGURES. Re-derive EVERY number you restate — including every number in THIS file.**
3d. ⚠️ **NO GATE MAY REST ON A ` + ` SPLIT.**
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Live: TWO SDS dial-budget flakes (⚠️ **DO NOT reuse the `go list -deps` exclusion blanket — TRUE for `xds` (0), FALSE for `boot` (1)**) · the driver-owned receiver port race · `internal/httpclient` zero-value · the two 84.2-era flakes · the REFERENCE h2spec section-8 flip · `TestOutlierDetector_ConcurrentEjectExactlyOnce`. ⚠️ **`TestServerConn_TinyWindowDelivery` IS NOT A FLAKE — phase 91 FIXED a live production deadlock; a recurrence is a REGRESSION of row 91.**
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only). ⚠️ **THE FOREIGN-CONTAINER ROSTER IS NOT CLOSED.** Seen and LEFT UNTOUCHED at phase 93: `infallible_booth`, `crazy_kare`, `golink-ai`, `quizzical_goldstine`, `jolly_nobel`, `curl-world-tls-1`, `curl-world-httpbin-1`, and a `reaper_*`. Name your own with a distinctive per-stage prefix.
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`.
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL.** **`gofmt -l` never exits non-zero — gate on OUTPUT.** ⚠️ **`golangci-lint` DOES exit non-zero on findings in this build — but STILL gate on OUTPUT.** ⚠️ **`go test` WITHOUT `-v` PRINTS ZERO `=== RUN`** — `RUN=0` beside `RC=0` is a VACUOUS GREEN. ⚠️ **A `-run` selector matching nothing prints `[no tests to run]` and EXITS 0.** ⚠️ **On `-v` output, unanchored `grep -c 'FAIL'` reads 11 ON A FULLY GREEN TREE** — use `grep -cE '^(FAIL|--- FAIL)|^ *--- FAIL'`.
7b. ⚠️ **`grep -c` ON ZERO MATCHES PRINTS `0` AND EXITS 1** — capture (`v=$(… || true)`), NEVER chain with `&&`, and NEVER write `$(… || echo 0)`, which emits TWO zeros.
7c. ⚠️ **A GREEN TEST RUN IS NOT EVIDENCE A SITE IS EXERCISED — IT MAY BE DEAD CODE.** Prove it with a **`panic()` REACHABILITY CONTROL**, and **do not skip the discriminating half**.
7d. ⚠️ **NC EVERY NEW TEST, NOT JUST THE RISKY-LOOKING ONES. AND NC YOUR OWN FIXES** — a NC that is a BUILD BREAK proves nothing; neutralise instead so the package still compiles.
7e. ⚠️ **A UNIT TABLE OVER A SHARED ASSERTION IS NOT A GATE ON PRODUCTION BEHAVIOUR.** `0004`'s `driver_test.go:82-85` uses SYNTHETIC observations and stayed `ok` under every prototype arm.
8. ⚠️ **`pgrep -f` / `pkill -f` MATCHES YOUR OWN SHELL AND KILLS THE TOOL CALL (exit 144)**. **Kill ONLY PIDs you captured yourself** (from `$!`), and verify with `kill -0`. ⚠️ **Backgrounding an entire `&&` chain makes `$!` name the SUBSHELL and orphans the real process.**
9. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-*`, `wt-*` or stage worktree should outlive its stage close** — expect only `master` (verify with `git worktree list`).
10. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** (a)/(b) differential **121 fixtures** + full `go test ./...`, gated on **`PIPESTATUS[0]`** and a **SET RECONCILIATION** (⚠️ **NOT `INNER_EXIT`, which DOES NOT EXIST**); (c) h2spec cite ONLY from your own run — ⚠️ **h2spec is MEASURED BLIND to burst-drain ordering**; grpc-conformance deferred in writing; proxy-wasm **10 families measured** (NOT "10/16"); (d) fuzzers **56 / 48** today; (e) the ANCHORED panic gate `^panic:|DATA RACE|SIGSEGV` on every differential launch; (f) no REVIEW.md — standing departure. ⚠️ **`-race` on the DIFFERENTIAL suite is VACUOUS** — the subject is an unraced subprocess.
11. ⚠️ **THE FIXTURE INVOCATION: `go test ./test/differential/ -run 'TestDifferential/0004-h2-routing' -count=1 -v 2>&1`.** ⚠️ **`-count=1` IS NOT OPTIONAL — see finding 9.** ⚠️ **Gate (a)'s failure mode is a SILENT PASS** — the runner `t.Skipf`s an unregistered fixture at `runner_test.go:200` and **NO fixture-count gate exists anywhere in the tree**. **ASSERT THE FIXTURE SET** (121 dirs vs 121 subtests vs 0 skips, reconciled BY NAME both directions).
12. ⚠️ **`-p` PUBLISHING WORKS; `--network host` IS WHAT DOES NOT.** ⚠️ A **STRICT_DNS** cluster whose name does not resolve **blocks listener init entirely** — prefer `STATIC` by IP. ⚠️ **`clusters: []` is BOOT-REJECTED.** ⚠️ **envoy-go REJECTS `match.headers`**, ⚠️ **refuses `codec_type: HTTP2` without TLS unless `--allow-h2c`**, ⚠️ **boots with `-c`, NOT `--config-path`**, and ⚠️ **`access_log[].log_format` is BOOT-REJECTED**. ⚠️ **`ENVOY_TARGET.md` is at `docs/envoy-go/ENVOY_TARGET.md`, NOT the repo root.** ⚠️ **A PROBE BACKEND MUST BIND `0.0.0.0`, NOT `127.0.0.1`.**
13. ⚠️ **A RAW-FRAMER PEER MUST HOLD ONE `hpack.Decoder` PER CONNECTION.** ⚠️ **Set `ReadMetaHeaders` to NIL when probing malformed shapes.** ⚠️ **`0004` is TLS+ALPN-h2, NOT h2c.**
14. ⚠️ **`bash /dev/tcp` + `timeout cat <&3` INSIDE COMMAND SUBSTITUTION RETURNS SILENTLY EMPTY.**
15. ⚠️ **GO DOES NOT FLAG UNUSED CONSTANTS.** Post-removal cleanup is grep-driven.
16. ⚠️ **`golangci-lint`'s misspell runs in locale US.** **Sweep British spellings in `.go` comments BEFORE the gate.** ⚠️ **Markdown prose may use them freely.**
17. ⚠️ **A GREEN RUN OF A CONTENTION-SENSITIVE TEST PROVES NOTHING.** Phase 91's subject cleared 5/5 uncontended while failing 9/32 contended on the SAME binary.
18. ⚠️ **AN ABSENCE CLAIM NEEDS A POSITIVE CONTROL AND A DISCRIMINATING NEGATIVE CONTROL.**
19. ⚠️ **DERIVE next-free ADR FROM THE TAIL, NEVER FROM THE HEADING COUNT.** The id space is SPARSE (exactly one gap, `0209`, verified by a full sweep) and **headings+1 reads 314 — the TAIL ITSELF, a TAKEN id.**
20. ⚠️ **`reference_measured_prototype_is_a_lower_bound` HAS FIRED FIVE CONSECUTIVE ROWS, ALWAYS BY UNDER-ENUMERATING *FILES*.** The `+3/-0` floor is PRODUCTION-ONLY. **ENUMERATE BY COMPILING PROTOTYPE across the six named gaps in `BRAINSTORM.md` §6.1.**
21. ⚠️ **BEFORE GENERALIZING A MEASURED TABLE INTO A RULE, GREP THE TREE FOR A COUNTER-EXAMPLE TO THE RULE — NOT JUST TO THE TABLE.** The phase-93 controller over-generalized a docker measurement and was refuted by a transcript that had been sitting in the repo the whole time.

## ⚠️ PORT BANDING

`net.ipv4.ip_local_port_range = 32768 60999`. ⇒ **BAND YOUR PROBE PORTS BELOW 32768.** ⚠️ **The differential harness reserves `20000..31007` (`harness_test.go:293`)**, so `12000-19000` is the safe band for ad-hoc probes — a failure OUTSIDE the harness band is an **out-of-band** occurrence, NOT an in-band recurrence. **Check availability with `ss -tan` (ALL states), never `ss -ltn`.** ⚠️ **FIXTURE ports are FAMILY-BANDED as `10<index>` — but `10120` is TAKEN (fixture `0028`); free in-band is `10126-10129`.** ⚠️ **`0120` as a DIRECTORY is FREE.**

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (**ugrep 7.8.4**). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **THE METHOD BINDS; carry no standing discrepancy figure and no standing absolute.** ⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS; print FILE NAMES, never a count.** ⚠️ **THE HARNESS `grep` STRIPS A LEADING `./`.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files; use `-v --no-index`, and `git status` is the discriminating check). ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **A DISCREPANCY MAY FAIL TO MANIFEST** — absence of a discrepancy is not evidence the blindness is gone.

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 93 (h2-local-reply-content-length)' --format='%h %s'   # the SLUG form
git log --grep '^phase 93' --format='%h %s'                                   # the LOOSE form — RUN IT to CROSS-CHECK
git log --grep '^phase 92 (h2-response-header-validation)' --format='%h %s'   # a CLOSED phase, use as a FIRING CONTROL
```
⚠️ **RUN BOTH FORMS AND RECONCILE THE SETS. NO COUNT IS QUOTED HERE, DELIBERATELY — BOTH FORMS ROT.** **A `| head -1` on ONE form can silently return a NON-TIP commit.** **Take the tip from `git rev-parse master`.** ⚠️ **AND WHEN YOU LAND A CORRECTION, PUT THE FULL SLUG IN ITS SUBJECT.**

Execution style: **subagent-driven per `feedback_execution_style`**. Subagents commit LOCALLY on the ONE stage branch with EXPLICIT PATHSPECS; the controller squashes (`feedback_subagents_no_push`, `feedback_push_to_origin`). **Pure MEASUREMENT agents should commit NOTHING and prove their tree clean.** ⚠️ **PARALLEL AGENTS IN ONE WORKTREE RACE THE GIT INDEX EVEN WITH DISJOINT PATHSPECS** — use **PER-AGENT WORKTREES ON THEIR OWN BRANCHES**, merged by the controller. ⚠️ **GIVE EACH PARALLEL AGENT ITS OWN PRIVATE SCRATCH DIR.** ⚠️ **AND VERIFY WHAT A SUBAGENT REPORTS — BUT ALSO BELIEVE IT WHEN IT REFUTES YOU:** at the phase-93 BRAINSTORM an agent refuted the controller's central design on both the rule and the placement, and it was right on both. Per-task `gofmt` + `golangci-lint` on touched packages, **gated on OUTPUT**. ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root** — build with `-o` into scratch.

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root — **observed AGAIN at the phase-93 BRAINSTORM**. Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` before any commit or gate run. ⚠️ **Shell VARIABLES do not survive between Bash calls** — re-export in every command. ⚠️ **AND AN UNQUOTED HEREDOC (`<<EOF`) COMMAND-SUBSTITUTES BACKTICKED TEXT** — use `<<'EOF'` and pass paths via the environment.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — `git status --porcelain` AND `git diff --stat master` must show ONLY your intended edits. For a patched TRACKED file: `sha256sum` BEFORE patching, `git checkout -- <path>` after, `sha256sum -c` the capture — ⚠️ **and COMMIT FIRST, because `git checkout --` restores from HEAD and will wipe uncommitted work.** ⚠️ **Phase 93 ran four prototype arms across three restore rounds and every `sha256sum -c` reported `OK`.** ⚠️ **Also check the MAIN repo root, and `--untracked-files=all` to catch throwaway probe files.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads the phase-93 SPEC.** ⚠️ **`grep -c 'next-skill:'` reads 2: line 7 is the ADR-0288 RULE STATEMENT matching its own grep, and the live pointer is the other. That is CORRECT, not a violation.** ⚠️ **§Project counts is a PHASE-76-ERA SNAPSHOT and now SAYS SO — its header was de-rotted at phase 93 and carries a correction table; the bullets below it are retained verbatim for their NCs and their headline figures are superseded.**
2. `docs/envoy-go/phases/93-h2-local-reply-content-length/BRAINSTORM.md` — **the charter, the measurements, and §10 "What the SPEC owes" (eight items).**
3. `docs/envoy-go/DECISIONS.md` **ADR-0314** §Consequences (xii) — the compensating-defect finding that banked this row; and **ADR-0085** (`:8260`/`:8326`) — the ratified `content-length: 0` doctrine this row follows.
4. `test/fixtures/0004-h2-routing/README.md` `:137-153` — the per-side pins and the documented departure. ⚠️ **Its `:147` sentence is REFUTED by finding 1 and must be corrected when the fix lands.**
5. `docs/envoy-go/ROADMAP.md` — the six deferred-candidate windows behind check (2), now at `:203 :209 :215 :225 :231 :239`.

## What the next session owes
**RUN THE PHASE-93 SPEC** (state 1 -> 2), producing `SPEC.md` and discharging the eight items in `BRAINSTORM.md` §10 · **RE-RUN the sentinel + all four NCs yourself** and record ACTUAL output — ⚠️ **NC-A and NC-B are BOTH two-line shapes now** · **DO NOT create `stop`** · **ROW 93 STAYS `in-progress`; `want` STAYS 125** — the SPEC adds no row, so a `ROADMAP.md` `--numstat` of nothing IS the evidence · **DRAFT ADR-0315 §Context** and ARM the strict `PROPOSED` guard **1 -> 2**, verified BY LINE AND BY ADR because the ADR-0231 decoy already reads 1 at `:14866` · **REFUTE YOUR PREDECESSOR BY EXECUTION** · `STATE.md` rolled IN PLACE with the oldest §Recent entry evicted by a **DIRECT DATE READ** (⚠️ **read the dates yourself — the phase-93 close faced a UNIQUE OLDEST with NO TIE, and that shape does NOT carry**) and archived to `STATE_HISTORY.md` as **ONE INLINE LINE** whose label does NOT match `^- \*\*prior active-phase:\*\*`, so the strict guard stays at **163, DELTA 0** · ⚠️ **AND ROLL THE §Recent PREAMBLE SENTENCE TOO — it fossilised for EIGHT stage closes because the guard is structurally blind to it** · `next-prompt.txt` rolled (`git add -f`) · ONE squashed commit, merged and pushed — ⚠️ **and if a late measurement invalidates something you already pushed, land a CLEARLY-LABELLED correction commit (WITH THE FULL SLUG IN ITS SUBJECT) rather than force-pushing or leaving it standing.**
