=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-VERIFIED 2026-07-19 (re-run MECHANICALLY at the phase-69 SPEC close, in the stage worktree) — it does NOT fire. `stop` was NOT created.** Actual output:
- **(1) prints `NOT DONE: row 69`** — row 69 is `in-progress` (SPEC done; it flips `done` only at the phase-69 IMPL six-gate; every OTHER chartered row is `done`).
- **(2) prints THREE live "candidates:" sentences** — HTTP/3, xDS, Observability ⇒ three families STAY OPEN. **Cite the command, never the adjective** (`reference_sentinel_deferred_sentence_live_vs_historical`): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` ⇒ **3**.
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** ⇒ THREE families never opened.

Checks (1)+(2)+(3) print ⇒ the sentinel does NOT fire. Abundant work remains.

==============================================

## STATUS AS OF 2026-07-19: THE PHASE-69 SPEC IS COMPLETE AND LANDED. ROW 69 (`stats-sink-otlp`) IS `in-progress`. THIS SESSION PERFORMS THE PHASE-69 PLAN.

**What the phase-69 SPEC landed** (`git log --grep 'phase 69'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): the SPEC RE-DERIVED the BRAINSTORM against the tree at `7a293a06` (an independent re-derivation agent re-opened EVERY §6/§11 anchor + resolved the code-gated dockets) and DISPOSED the D-OTLPM docket one-for-one. **Headline resolutions:**
- **D-OTLPM-SKEW RESOLVED, INVERTING the BRAINSTORM's silent-ignore lean:** envoy-go parses the whole bootstrap with `protojson.UnmarshalOptions{DiscardUnknown:false}` (`bootstrap.go:510`), so the v1.37.2-only `resource_detectors`/`custom_metric_conversions` (absent from the pinned v1.32.4 `SinkConfig`) are boot-REJECTED FOR FREE once the `SinkConfig` descriptor is blank-imported — a named envoy-go-strict version-skew departure, **ZERO new detect-and-reject code** (NOT "silently ignored by `UnmarshalTo`" as BRAINSTORM §2.2/§8 supposed).
- **D-OTLPM-HISTO → STRICT-REJECT** `report_histograms_as_deltas: true` (the metrics_service `histogram_emit_mode` precedent at `bootstrap.go:595-597`, ADR-0080; accept-inert NAMED as the argued alternative).
- **D-OTLPM-SPLIT → SINGLE FLAT ROW** (~11 tasks; the phase-48/49/57 sink precedent — the substrate is landed, so the knobs are parameter-threading, not new machinery; the ADR-0045 valve is armable but UNCONSUMED).
- **D-OTLPM-FIXTURE → TWO** (`0112-stats-sink-otlp`@10448 default cumulative + `0113-stats-sink-otlp-knobs`@10449 deltas+prefix+both-false with the delta-SUM stability barrier; fixtures 113 → 115).
- D-OTLPM-STARTTIME → per-side structural (subject ns-magnitude + cumulative-constant; reference unasserted; NEVER cross-side equality). D-OTLPM-DUPNAME → receiver keys `(name, sorted-attrs)`, order-insensitive. D-OTLPM-NODE → envoy-go's own `telemetry.sdk.{name=envoy-go,language=go,version}` triple (keys cross-side, values per-side). D-OTLPM-FUZZSEED → +0, a seed on the `open_telemetry` `@type` dispatch-verified to reach the new arm (the descriptor-blank-import vacuity trap NAMED). **RIDER → the require=false RBAC/principal fixture arm STAYS DEFERRED** (off-subject, not ≤1-task).
- Also pinned: the two `*wrapperspb.BoolValue` knobs (`emit_tags_as_attributes` + `use_tag_extracted_name`) default TRUE (read `w==nil || w.GetValue()`, INVERTING the metrics_service scalar `.GetValue()` template — `reference_protojson_wrapper_scalar_not_object`); the UNARY-Export bounded-channel writer (the OTLP-logs/traces sink precedent, NOT the streaming metrics_service `StreamMetrics` lifecycle); the grpcclient wrapper named `OTLPMetricsClient` to disambiguate from `colmetricspb`'s own `MetricsServiceClient` type.
- **Envelope:** ZERO new production packages (`internal/statssink/otlp.go` new FILE + one `internal/bootstrap` parse arm + a `~40-line internal/grpcclient` stanza + one `cmd/envoy-go` build-loop arm; new TEST-ONLY `test/helpers/otlpmetrics`); new exported symbols ONLY in the existing `internal/statssink` (`OTLPMetricsSink`/`NewOTLPMetricsSink`) + `internal/grpcclient` (`OTLPMetricsClient`/`NewOTLPMetricsClient`) rosters — the `internal/xds` zero-new-symbol discipline UNTOUCHED. +0 stats / +0 fuzzers / +0 BackendKinds / +0 go.mod modules anticipated.
- **NO fresh reference probe this SPEC** (the BRAINSTORM's Dossier P discriminated every behavioral gate this session; no docket hinges on unverified reference behavior — stated honestly, not to skip rigor). **Adversarially verified before landing.** **ADR-0291 §Context DRAFTED — the DECISIONS tail flipped ADR-0290 → ADR-0291 at this SPEC commit** (next-free ADR-0292); §Decision/§Consequences land at the IMPL IN PLACE. Row 69 STAYS `in-progress`; NO deferred sentence narrowed (rolls OUT at the IMPL, phase-57 precedent). Counts at exit (all +0 apart from the DECISIONS tail): fixtures **113** · fuzzers **55** · stat **1201** · BackendKind **38** · go.mod modules **2** · DECISIONS tail **ADR-0291** (next-free **ADR-0292**).

**⚠️ PUSH (idempotent — evaluate, then act):** FIRST ACTION: `git fetch`; if origin/master is BEHIND local master and the local tip is the phase-69 SPEC squash (`git log --grep 'phase 69'`, locate by SUBJECT), `git push origin master` per `feedback_push_to_origin`; if in sync, proceed; if REJECTED (diverged), STOP and reconcile — never rebase away landed squashes.

## What THIS session does — phase 69 PLAN

One stage per session: PLAN ONLY (docs-only — `PLAN.md` + `PROGRESS.md`; ZERO production `.go`). Fresh worktree off master per `feedback_git_worktrees`; subagent-driven (`feedback_execution_style`); adversarially verified before landing; row 69 STAYS `in-progress`; sentinel re-run mechanically; roll this router to the phase-69 IMPL.

**The PLAN owes (the phase-66/67/68 precedent):**
- **RE-DERIVE the SPEC against the tree at the new tip** — do not execute it (`feedback_brief_citations_not_evidence`; every `file:line` in SPEC §11 re-opened; a parallel stream can mint fresh drift + shift anchors, `feedback_parallel_stream_mints_fresh_drift` — the phase-69 SPEC is docs-only, so the production tree is `7a293a06` == the BRAINSTORM tree, but re-derive anyway).
- **Decompose SPEC §10 into a TDD task spine** (~11 tasks, a SINGLE FLAT ROW — the SPLIT is DECIDED, no re-litigation; the ADR-0045 valve stays armable-but-unconsumed): T1 grpcclient `OTLPMetricsClient` · T2 `internal/statssink/otlp.go` `OTLPMetricsSink` + the `otlpMetricsClient` seam + the mapping + the UNARY writer · T3 the `bootstrap.go` parse arm (descriptor blank-import; the fifth dispatch arm; sibling-reject four→five; nil→TRUE wrappers; the `report_histograms_as_deltas` reject; the free version-skew reject) · T4 `main.go` gate+sub-loop · T5 `test/helpers/otlpmetrics` receiver · T6/T7 fixtures `0112`+`0113` + breaks · T8 fuzz seed + dispatch-verify · T9 BC edit map (incl. the BC:836 sibling-reject-example swap OFF "OTLP-metrics") · T10 six-gate/full-115-dir/-race/cycle-guard/envelope-audit · T11 ADR-0291 complete IN PLACE + ROADMAP row-done (roll "OTLP-metrics stats sink +" OUT of ROADMAP:198) + STATE/router.
- **Pin each task's RED-first breaks** (deliberate breaks that FIRE the exact assertion — `-count=1`, full selectors, confirm WHICH fired, `reference_deliberate_break_wrong_assertion`): the `0112` type-swap / tag-disable / resource-key-drop; the `0113` absolute-not-delta / barrier-masks / prefix-drop; the order-insensitive NON-break control (reversing the receiver's attr iteration must NOT fail). Dispatch-verify the fuzz seed REACHES the new arm (the descriptor-blank-import trap — else it hits the protojson type-registry error before dispatch).
- **RE-DERIVE the SPEC §11 anchors at the PLAN tip** and record any drift (the phase-68 PLAN found RD-DUPSITE etc.); the SPEC already folded C5 (the `StatsSinkConfig` struct span `:357-372`, the go-control-plane path form). Confirm the `otlpMetricsClient` seam keeps `internal/statssink` grpcclient-free (the cycle guard, TYPE-level).
- Adversarial verification before landing (independent verifiers in PRIVATE scratch, `reference_parallel_subagents_private_scratch`); row 69 STAYS `in-progress`; sentinel re-run MECHANICALLY (expect (1) `NOT DONE: row 69`, (2) three sentences, (3) gRPC/Runtime/WASM); DECISIONS UNTOUCHED at the PLAN (ADR-0291 is drafted; the PLAN does not touch it); roll this router to the phase-69 IMPL.

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline; the next-skill line carries the owed roster).
2. `docs/envoy-go/phases/69-stats-sink-otlp/SPEC.md` — the FULL D-OTLPM disposition + the §9 BC edit map + the §10 task spine + the §11 edit-site roster + the §14 ADR-0291 §Context draft.
3. `docs/envoy-go/ROADMAP.md` — row 69 + the Observability family paragraph (locate the live `candidates:` sentence by grep, not line) + §Schema.
4. `docs/envoy-go/DECISIONS.md` — ADR-0291 (§Context, just drafted) + ADR-0262/0263/0264/0265/0275 (the sink lineage) + ADR-0286 §Consequences C3 (`ssl` family posture) + ADR-0045 (split valve) + ADR-0044 (§Context-at-SPEC) + ADR-0106 (family expansion).
5. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — trust MEMORY.md's index; esp. `reference_stats_sink_emits_used_only`, `reference_periodic_sink_differential_two_receivers`, `reference_delta_sink_differential_stability_barrier`, `reference_cluster_sink_dial_unaccounted`, `reference_protojson_wrapper_scalar_not_object`, `reference_strict_reject_sibling_typeurl_gap`, `reference_differential_grpc_receiver_driver_owned`, `reference_host_gateway_ip_docker_desktop`.
