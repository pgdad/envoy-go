START a fresh cold-start session for envoy-go — **the phase-37 (`load-balancer-maglev`) SPEC is DONE and LANDED on master; the next lifecycle step is the phase-37 PLAN** (`superpowers:writing-plans` — decompose SPEC §10 into bite-sized TDD tasks; run the FINAL ADR-0045 split-gate re-check [anticipated NO SPLIT — a single flat row]; the PLAN ends with the plan-document-reviewer gate). The substantive master tip is the phase-37 SPEC squash (subject line `phase 37 SPEC: load-balancer-maglev …`; pushed to `origin/master`; confirm via `git log --oneline -5`). The phase-37 SPEC (`superpowers:writing-plans` scoped to SPEC authoring; the spec-document-reviewer gate PASS first pass — no blocking findings) EXECUTED the §10 D-M1..D-M6 empirical pins IN-SESSION (parallel-subagent fan-out) against the live contrib reference Envoy (`envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8`, ADR-0227) + the go-control-plane `/envoy v1.32.4` module cache + upstream v1.37.2 source, and anchored the ADR-0238 §Context DRAFT (SPEC §13).

## The phase-37 SPEC findings the PLAN consumes (SPEC §11; AMEND-M1..M6)

For the project's FIFTH LB policy + SECOND consistent-hashing policy `Cluster.LbPolicy MAGLEV` (`envoy.config.cluster.v3`, enum value 5):

- **D-M1 (CONFIRMED):** enum 5; `Cluster.MaglevLbConfig.table_size` `UInt64Value` proto field 1; **NO `hash_function` field** (maglev always uses xxHash64); PGV `value must be less than or equal to 5000011` (only the lte); the default **65537 is doc-comment-only** (the getter returns nil → the manager SELF-SUPPLIES it, the `parseRingHashLbConfig` precedent); the `maglev_lb_config` oneof wrapper is field 52; `go mod tidy -diff` empty → **ZERO new go.mod dep**.
- **D-M2 (PINNED — the table-build algorithm, SPEC §3.1/§11.2):** per host the key is `Endpoint.Addr()` = `"IP:PORT"` (= upstream `address()->asString()`); `offset = xxHash64(addr) % M`; `skip = (xxHash64Seed(addr, 1) % (M-1)) + 1` (∈ `[1, M-1]`) — BOTH already in `hash.go` (`xxHash64`/`xxHash64Seed`) → **ZERO new hash code**. **Sort endpoint indices by `Addr()` ascending BEFORE the populate loop** (the Envoy pre-build `std::sort`; the ONE structural detail ring_hash did not need — omit it and the table differs cross-side). Populate `table[(offset + skip·next) % M]` host-by-host (one slot per host per iteration under equal weight) until all `M` slots fill — TERMINATES because `M` prime → each `skip` coprime to `M` → a full `M`-cycle. Lookup `table[hashKey % M]`; no-hash → a random table index (`rng() % M`, the ring_hash fallback precedent). Equal-weight MVP omits the weight machinery (weighted maglev DEFERRED).
- **D-M3 (CONFIRMED):** the LANDED ADR-0237 producers (HTTP route `hash_policy` + tcp `source_ip`) feed `maglevLB.Pick(hashKey, hasHash)` IDENTICALLY to `ringHashLB` — a confirmation, NOT a build (ZERO producer churn).
- **D-M4 (CONFIRMED LIVE):** **+2 `maglev_lb.*` gauges** — `cluster.<name>.maglev_lb.min_entries_per_host` + `max_entries_per_host` (NO `size` gauge — the table size is config-known); STATIC + cross-side-exact (`floor(M/N)`/`ceil(M/N)`; for a 3-host default-65537 cluster: 21845/21846); stat surface **1119 → 1121** at the IMPL.
- **⚠️ D-M5 (REFUTATION — load-bearing):** the BRAINSTORM Q1 assumed the reference ACCEPTS a non-prime `table_size` (→ a "DEPARTURE"). **REFUTED:** the reference Envoy itself REJECTS a non-prime `table_size` (`The table size of maglev must be prime number`, app-layer at validate/boot), so envoy-go's primality reject is **PARITY, not a departure** (the decision to reject STANDS, now BETTER justified). The cap reject (`> 5000011`) is PGV parity. Both land UNIT-LEVEL in `manager_test.go` (the ring_hash/random precedent — no fixture pins them; a cross-side boot-reject dir is now FEASIBLE but NOT taken). The reject blast radius is THREE sites: `manager.go:275` supported-list (`…, RING_HASH` → `…, RING_HASH, MAGLEV`) + `manager_test.go:320 TestManager_Error_UnsupportedLBPolicy` (which ALREADY triggers on `Cluster_MAGLEV` from the phase-36 retarget → the doubly-hit retarget MAGLEV → **`Cluster_CLUSTER_PROVIDED`** [enum 6] + the new substring) + `BEHAVIOR_CONTRACT.md`.
- **D-M6 (RESOLVED):** the `0063-lb-maglev` differential mirrors `0062` (N=16 × K=16 = 256 routed `GET /get` + 8 `/health`; the per-side modular invariant `≡ 0 mod K` = affinity, BOTH sides; spread `≥ 2`; conservation 256; cross-side `/health` byte-equivalence + a cross-side `StatsAsserter` [the 2 gauges cross-equal + `upstream_rq_total`/`upstream_cx_total` = 256]; cross-side host IDENTITY NOT asserted — the tables are over different address strings). NO new BackendKind (33; the `0062` `HTTPEcho` reused), NO new fuzzer (42; a table property test is unit-level). Envelope **~140–200 prod LoC / ~8–10 tasks** → SINGLE flat row, NO split (the ADR-0045 gate holds by an order of magnitude); ADR-0024 UNAMENDED; no `FuzzMaglevLookup`.

## What this session does — author the phase-37 PLAN

Run `superpowers:writing-plans` (the kafka-31/thrift-33/least_request-34/random-35/ring_hash-36 PLAN precedent — a single flat row), producing `docs/envoy-go/phases/37-load-balancer-maglev/PLAN.md`: decompose SPEC §10's ~8–10-task indicative spine into bite-sized TDD tasks (each: write the failing test → run it red → minimal impl → run green → commit; per-task `gofmt -l` + `golangci-lint` on touched pkgs per `feedback_pertask_gofmt_lint`; subagents commit LOCAL-ONLY per `feedback_subagents_no_push`). Run the FINAL ADR-0045 split-gate re-check (anticipated NO SPLIT). End with the plan-document-reviewer gate. The IMPL follows in its own session (subagent-driven per `feedback_execution_style`).

## Counts at master tip (phase 37 SPEC done — confirm at the PLAN)

- stat surface **1119** (`ls`/recipe per BEHAVIOR_CONTRACT). Anticipated 1119 → 1121 at the IMPL (+2 `maglev_lb.*` gauges — D-M4). UNCHANGED at the SPEC.
- differential fixtures **64** (tail `0062-lb-ring-hash-http`; `ls -d test/fixtures/[0-9]* | wc -l`). Anticipated 64 → 65 at the IMPL (`0063-lb-maglev`). UNCHANGED at the SPEC.
- fuzzers **42** (tail `FuzzZookeeperResponseDecode`; `grep -rh "^func Fuzz" $(find ./internal -name fuzz_test.go) | wc -l`). Anticipated UNCHANGED. BackendKind tail **33** (`TCPThriftResponder`; anticipated UNCHANGED — `0063` reuses the `0062` HTTP backends).
- DECISIONS tail **ADR-0237** (next-free **ADR-0238**). STAYS ADR-0237 at the SPEC and the PLAN; ADR-0238 (the maglev policy) §Context is a DRAFT in SPEC §13; the full body lands at the IMPL per ADR-0044. ADR-0235 (the hash-key seam) + ADR-0236 (the ring_hash policy — the STYLE TEMPLATE for ADR-0238) + ADR-0237 (the HTTP producer) are the durable LB assets maglev REUSES unchanged.
- reference pin: `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227). Toolchain: Go 1.26.x; golangci-lint 1.64.8 (ADR-0009); go-control-plane `/envoy v1.32.4` (ADR-0008).

## Read first (in order)

1. THIS FILE — the post-phase-37-SPEC cold-start (next: the phase-37 PLAN — `superpowers:writing-plans`).
2. `docs/envoy-go/phases/37-load-balancer-maglev/SPEC.md` — **the phase-37 SPEC the PLAN decomposes** (§3 the `maglevLB` algorithm + the manager arm; §6 the reject roster [the primality PARITY + the cap + the supported-list change]; §7 the +2 gauges; §8 the `0063` differential; §10 the ~8–10-task indicative spine; §11 the D-M1..D-M6 empirical-pin block; §12 the PLAN/IMPL D-questions; §13 the ADR-0238 §Context DRAFT).
3. `docs/envoy-go/phases/37-load-balancer-maglev/BRAINSTORM.md` — the durable charter (the Q0/Q1 decisions; NOTE the §2.3 "primality DEPARTURE" framing is SUPERSEDED by SPEC AMEND-M5 → PARITY) + README.md (status SPEC DONE).
4. `docs/envoy-go/STATE.md` — active-phase `phase 37 (load-balancer-maglev) spec done`; the lifecycle-state; the counts.
5. `docs/envoy-go/ROADMAP.md` — row 37 (`in-progress`; flat family row, NO split).
6. `docs/envoy-go/DECISIONS.md` — **ADR-0235** (the hash-key seam — REUSED) + **ADR-0236** (the ring_hash policy — the STYLE TEMPLATE for ADR-0238) + **ADR-0237** (the HTTP producer — REUSED); + ADR-0044 (ADR §Context at SPEC, body at IMPL) / ADR-0052 (atomic six-gate landing) / ADR-0080 (byte-stable parse-reject wording) / ADR-0106 (flat family row) / ADR-0045 (the PLAN split-gate re-check) / ADR-0004 (the empirical-pin discipline).
7. The phase-35/36 PLANs (`docs/envoy-go/phases/35-load-balancer-random/PLAN.md` + `phases/36-load-balancer-ring-hash/PLAN.md` + `phases/36.2-…/PLAN.md`) — the SHAPE TEMPLATES for the phase-37 PLAN (the TDD-task decomposition; the deliberate-break tasks; the six-gate completion task).
8. The landed LB code maglev plugs into: `internal/cluster/{cluster.go (the Pick seam + Addr()),ringhash.go (the STRUCTURAL TEMPLATE),hash.go (xxHash64/xxHash64Seed),manager.go (the switch:243 + parseRingHashLbConfig:347 + the reject:275 + the gauges:110-117),loadbalancer.go,manager_test.go:320}` + the LANDED producers `internal/filter/{tcpproxy/filter.go,http/router/router.go}` (UNCHANGED) + `test/fixtures/0062-lb-ring-hash-http/` (the `0063` precedent).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_execution_style` (subagent-driven at the IMPL), `feedback_git_worktrees`, `feedback_subagents_no_push` + `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_path_targeting`/`_detach`, `reference_differential_break_protocol_count1`, `reference_differential_run_selector`, `reference_differential_band_sigma_margin`, `reference_differential_hash_key_cross_side_infeasible`, `reference_fixture_workload_constant_desync`, `reference_docker_probe_bridge_network`.

## Honor throughout

- The lifecycle is BRAINSTORM → SPEC → PLAN → IMPL, each its own session. The next session is the phase-37 **PLAN** (`superpowers:writing-plans` — decompose SPEC §10 into bite-sized TDD tasks; FINAL ADR-0045 gate re-check; end with the plan-document-reviewer gate; the IMPL follows in its own session).
- `feedback_execution_style` · `feedback_git_worktrees` · `feedback_subagents_no_push` + `feedback_push_to_origin` · `feedback_pertask_gofmt_lint`.
- ADR-0106 (flat family row) · ADR-0044 (ADR §Context at SPEC, body at IMPL, in-place) · ADR-0052 (the atomic-landing six-gate) · ADR-0080 (byte-stable parse-reject wording) · ADR-0227 (the contrib reference image; live probes per `reference_docker_probe_bridge_network`) · ADR-0045 (the split-gate re-check) · ADR-0004 (the empirical-pin discipline).
