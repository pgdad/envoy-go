START a fresh session for envoy-go (router last updated 2026-06-25; the **phase 44.2 (`grpc-access-log` buffering) IMPL is DONE + pushed to origin/master**). HEAD is the LATEST `next-prompt.txt:` docs-only tip-stamp commit; the **phase-44.2 IMPL squash `c4055b20`** (subject `phase 44.2 (grpc-access-log buffering) IMPL: the buffer_size_bytes/buffer_flush_interval flush triggers replacing the 44.1 fixed flush …`) is the stable anchor sitting directly BENEATH the run of `next-prompt.txt:` commits at the tip; re-read the shas with `git log --oneline -20`, do NOT assume — the master tip is the LATEST `next-prompt.txt:` commit (a roll or a tip-stamp fix), NOT the IMPL squash itself, and any number of docs-only `next-prompt.txt:` commits may stack ATOP the IMPL squash (which stays the stable anchor). BENEATH the 44.2 IMPL squash is the **phase-44.2 PLAN squash `b8cc53ca`** (`phase 44.2 (grpc-access-log buffering) PLAN: …`), then the **phase-44.2 SPEC squash `8d5cdb4a`** (`phase 44.2 (grpc-access-log buffering) SPEC: …`), the **phase-44.1 IMPL squash `ec05a421`** (`phase 44.1 (grpc-access-log core) IMPL: …`), the 44.1 PLAN squash `5c7cf481`, the 44.1 SPEC squash `a7a24d0c`, the phase-44 BRAINSTORM `81afdd59` (near the bottom of the -20 window). **The Observability family is OPEN** (phase 44 is its FIRST row; the family STAYS OPEN — 44.3 header-capture + OTLP / tracing / stats sinks / tap remain future rows). **The next action is the 44.3 (header-capture) SPEC** — author `docs/envoy-go/phases/44-grpc-access-log/SPEC-44.3.md` (the parent phase-44 BRAINSTORM already chartered the 3-leg split, so 44.3 goes STRAIGHT to SPEC per STATE.md, NO fresh BRAINSTORM); CONFIRM against `docs/envoy-go/STATE.md` (the source of truth for "what next") before committing to the stage. **Git hygiene:** SPEC authoring runs in a FRESH git worktree off master (`feedback_git_worktrees`) if any code-adjacent probe artifacts land — the doc itself commits DIRECT on master; the controller pushes at stage-close (`feedback_push_to_origin`).

First confirm the state via `git log --oneline -20` (expect HEAD to be the LATEST `next-prompt.txt:` docs-only commit, with the phase-44.2 IMPL squash `c4055b20` sitting directly BENEATH the run of `next-prompt.txt:` commits, then the phase-44.2 PLAN squash `b8cc53ca`, the phase-44.2 SPEC squash `8d5cdb4a`, the phase-44.1 IMPL squash `ec05a421`, the 44.1 PLAN `5c7cf481`, the 44.1 SPEC `a7a24d0c`, the phase-44 BRAINSTORM `81afdd59` near the bottom of the window; the tip-stamp commits interleave with each squash, so `81afdd59` sits at ~depth 18 (one slot of margin under -20); re-read the shas, do NOT assume). Counts at the 44.2 IMPL-done: stat surface **1189** (H2 cluster; non-H2 **1185**) — UNCHANGED (NO new buffering stat, AMEND-BUF-4) / fixtures **84** (tail `0082-grpc-access-log-buffering`; NOTE: count fixtures with `ls -d test/fixtures/[0-9][0-9][0-9][0-9]* | wc -l` — `grep -cE '^[0-9]{4}-'` UNDERCOUNTS by 2, skipping `0007a`/`0007b`) / fuzzers **44** (`^func Fuzz` == 44) / BackendKind tail **38** (`H2GoawayResponder`; the ALS receiver is the driver-owned test-only `test/helpers/accessloggrpc`, NOT a BackendKind) / DECISIONS tail **ADR-0256**, next-free **ADR-0257** (the anticipated 44.3 header-capture ADR).

## NEXT STEP — the 44.3 (header-capture) SPEC (`superpowers:writing-plans` scoped to SPEC authoring)

Author `docs/envoy-go/phases/44-grpc-access-log/SPEC-44.3.md` in the house 14-section SPEC shape (the SPEC-44.2 template). The 44.3 leg makes `additional_request_headers_to_log` / `additional_response_headers_to_log` (PARSE-ACCEPT-but-INERT through 44.1/44.2) LIVE: capture the named request/response headers into the `HTTPAccessLogEntry`'s `request.request_headers` / `response.response_headers` maps (a `Record` extension + emit-hook capture). ANCHORS **ADR-0257**.

The 44.3 SPEC must EXECUTE its live-probe pins IN-SESSION (the D-ALS-HEADERS-* pins) against `envoyproxy/envoy:contrib-v1.37.2` per `reference_docker_probe_bridge_network` (the shared-bridge containerized ALS receiver; the Docker-Desktop host-gateway gotcha — use the shared bridge, NOT `host.docker.internal:host-gateway`). Pin: which headers the reference captures (the exact `additional_*_headers_to_log` → `request.request_headers`/`response.response_headers` map shape); whether a missing header is OMITTED vs empty-string; case-normalization; the differential drive (a fixture `0083-grpc-access-log-headers` cross-side EXACT on the captured-header subset aggregated across entries — `reference_streaming_sink_differential_framing` / `reference_differential_grpc_receiver_driver_owned` / `reference_concurrency_differential_release_barrier`). Anchor the ADR-0257 §Context draft (§Decision/§Consequences land at the 44.3 IMPL per ADR-0044). Likely needs a `Record` field extension (the captured headers) + emit-hook capture at the H1/H2 emit sites + the `buildHTTPAccessLogEntry` mapping extension. Anticipate: stat **1189** (UNCHANGED unless the probe finds a header-capture stat — SPEC-pinned) / fixtures **85** (`0083-grpc-access-log-headers`) / fuzzers **44** (UNCHANGED) / BackendKind **38** (UNCHANGED — driver-owned receiver) / DECISIONS **ADR-0257**. ZERO new packages + ZERO new go.mod modules anticipated.

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** This session is the 44.3 SPEC (lifecycle-state 0 → 1 for the 44.3 leg; **row 44 STAYS `in-progress`** — the Observability family STAYS OPEN). `feedback_git_worktrees` (a fresh worktree off master ONLY if code-adjacent probe artifacts land; the SPEC doc commits DIRECT on master) · `feedback_push_to_origin`.
- **Process anchors:** ADR-0106 (row 44 flips `done` PER-LEG; NO parent rollup — 44.3 is the FINAL chartered leg, so its IMPL six-gate flips row 44 → `done` ⇒ but the Observability FAMILY STAYS OPEN — OTLP/tracing/stats-sinks/tap remain future rows) · ADR-0044 (the ADR-0257 §Context drafts at THIS SPEC; §Decision/§Consequences at the 44.3 IMPL) · ADR-0045 (the split gate — re-check at PLAN) · ADR-0080 (strict-reject — UNCHANGED unless the probe finds a header that must reject) · ADR-0255 (the 44.1 as-built sink) · ADR-0256 (the 44.2 as-built buffering) · ADR-0257 (the anticipated 44.3 header-capture ADR).
- **Streaming-sink differential discipline:** `reference_streaming_sink_differential_framing` (assert the PAYLOAD aggregated across messages, NOT framing) · `reference_differential_grpc_receiver_driver_owned` (the ALS receiver is the driver-owned `test/helpers/accessloggrpc`, NOT a BackendKind) · `reference_docker_probe_bridge_network` (shared-bridge + container-hostname; the host-gateway gotcha) · `reference_concurrency_differential_release_barrier` (poll-to-converge, NEVER `time.Sleep`) · `reference_differential_run_selector` (`-run 'TestDifferential/0083'`, NOT bare) · `reference_fuzzer_count_docs_drift` (re-verify `^func Fuzz` at completion); the AMEND-ALS-2 query-less-path constraint carries (and a header-capture analog may apply — probe it).

## Read first (in order)
1. THIS FILE — the cold-start router (phase 44.2 IMPL is DONE + pushed; the next action is the 44.3 header-capture SPEC).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 44.2 (grpc-access-log) IMPL done`) + the recorded NEXT action (the 44.3 header-capture SPEC) + the count figures (source of truth for "what next").
3. `docs/envoy-go/phases/44-grpc-access-log/SPEC-44.2.md` + `PLAN-44.2.md` — the 44.2 SPEC/PLAN templates to mirror for the 44.3 SPEC shape (the §11 live-probe block, the §12 D-questions, the §13 ADR §Context draft).
4. `docs/envoy-go/phases/44-grpc-access-log/BRAINSTORM.md` — the §-level 44.3 header-capture charter (the Q1 envelope + the by-concern split; `additional_{request,response}_headers_to_log` → `request.request_headers`/`response.response_headers`; ADR-0257).
5. `docs/envoy-go/DECISIONS.md` — ADR-0256 (the 44.2 as-built) + next-free **ADR-0257** (draft §Context at this SPEC).
6. `docs/envoy-go/ROADMAP.md` — row 44 (`grpc-access-log`, in-progress; legs 44.1 + 44.2 done, 44.3 remains) + the `### Observability family` heading.
7. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the `### Access log — gRPC Access Log Service (ALS) streaming sink` block (the §9 SPEC-44.3 contract-delta target — the header-capture addition) + the stat-surface 1189 entry.
8. The 44.1/44.2 as-built seams the 44.3 SPEC will extend: `internal/accesslog/grpcsink.go` (the buffer machinery + `run()`), `internal/accesslog/mapping.go` (`buildHTTPAccessLogEntry` — the 10-field mapping the header-capture extends), `internal/accesslog/accesslog.go` (the `Record` struct — the header-capture field extension target) + the H1/H2 emit hooks (`accesslog_emit.go`), `internal/bootstrap/bootstrap.go` (`ALSConfig` + `parseGrpcAccessLog` — the `additional_*_headers` parse-accept-inert arm to make LIVE), `test/helpers/accessloggrpc/accessloggrpc.go` (the receiver), `test/fixtures/0081-grpc-access-log/` + `0082-grpc-access-log-buffering/` (the differential precedents the `0083` copies).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `reference_streaming_sink_differential_framing`, `reference_differential_grpc_receiver_driver_owned`, `reference_docker_probe_bridge_network`, `reference_fuzzer_count_docs_drift`, `feedback_execution_style`, `feedback_git_worktrees`).
