RESUME a fresh session for envoy-go (router last updated 2026-06-27; the **phase 45.2 (otlp-access-log operator engine) IMPL is DONE and LANDED** — squashed onto master as **`59990afe`** [one atomic commit, fast-forwarded onto master + **pushed to origin**; six-gate GREEN incl. the full 87-dir differential modulo two documented UNRELATED startup-race flakes that isolate-re-ran clean; the worktree was removed + the branch deleted]. **ROW 45 (`otlp-access-log`) FLIPPED `done`** — the 45.2 operator engine was the FINAL leg of the by-concern 2-leg OTLP split [per-leg ADR-0106 + `reference_roadmap_split_phase_row_done`]. As-built: a NEW small two-phase command-operator engine `internal/accesslog/otlpformat.go` [compile-once at boot — `CompileOTLPTemplate` `%…%` scanner + `CompileOTLPValue` `AnyValue`-tree walker + `ValidateOTLPValue` type-only walk, STRICT-REJECT unknown operators + non-string/kvlist/array value-types — + substitute-per-record `Eval` with an explicit `otlpValueKind` tag preserving empty kvlist/array structure; curated to the `format.go` `Default()` `Record`-mapped roster] + the bootstrap LIFT [`parseOpenTelemetryAccessLog` compiles body/attributes + validates resource_attributes at parse over the NEW acyclic `bootstrap → accesslog` edge; exported `OTLPTemplate`/`OTLPValueTemplate`/`OTLPAttrTemplate`; `OTLPConfig.{Body,Attributes,ResourceAttributes}`; `formatters` STAYS rejected] + the `buildLogRecord`/`buildResource` extensions [per-record body/attributes substitution + the literal resource_attributes append SURVIVING `disable_builtin_labels` (AMEND-OPS-5); resource operators pass through VERBATIM (AMEND-OPS-1); all-string output (AMEND-OPS-2)] + the sink threading + main wiring + `FuzzCompileOTLPValue` + the `0085-otlp-access-log-operators` differential. ANCHORS **ADR-0259** (ACCEPTED).). HEAD is this **`next-prompt.txt:` roll** atop the **45.2 IMPL squash `59990afe`** atop the prior next-prompt roll `eb8f8664` atop the 45.2 PLAN `549a03c9` atop the phase-45.2 SPEC `b4767c43`. Re-read the shas with `git log --oneline -14`, do NOT assume — further docs-only `next-prompt.txt:` roll commits may stack ATOP this one. Counts at IMPL-done (the exit invariant — re-verify, do NOT assume): stat surface **1191** (H2 cluster; non-H2 **1187**) UNCHANGED [the operator engine adds NO stat] / fixtures **87** (`ls -d test/fixtures/[0-9][0-9][0-9][0-9]* | wc -l` == 87; tail `0085-otlp-access-log-operators`) / fuzzers **46** (`grep -rh '^func Fuzz' --include='*.go' . | wc -l` == 46) / BackendKind tail **38** (UNCHANGED — the OTLP receiver is driver-owned) / DECISIONS tail **ADR-0259**, next-free **ADR-0260**.

**ROW 44 (`grpc-access-log`) is `done`**; **ROW 45 (`otlp-access-log`) is `done`** (both legs landed — 45.1 core + 45.2 operator engine). The **Observability FAMILY STAYS OPEN** — the remaining rows are **OTel/Zipkin tracing**, **stats sinks**, and the **tap filter** (none yet chartered). **The next action is the NEXT Observability-family BRAINSTORM** — pick the next family row (tracing / stats sinks / tap) and charter it with `superpowers:brainstorming` (a docs-only BRAINSTORM commit direct-on-master, the house lifecycle opener; NO code). ANCHORS the next-free ADR when its SPEC lands.

First confirm state via `git log --oneline -14` (expect HEAD at a `next-prompt.txt:` roll atop the 45.2 IMPL squash `59990afe`; counts as above; re-read the shas, do NOT assume). Confirm `git status` clean + master == origin/master.

## NEXT STEP — the next Observability-family BRAINSTORM
This is a BRAINSTORM stage — docs-only, direct-on-master (the house lifecycle opener; `superpowers:brainstorming`). NO worktree, NO code. The deliverable is a `docs/envoy-go/phases/<NN>-<name>/BRAINSTORM-<NN>.md` chartering the next Observability-family row. Candidate rows (the family stays open):
- **OTel / Zipkin tracing** — a tracing provider over the HCM (spans, trace context propagation, the OTLP/Zipkin exporters). Likely the natural next row (it pairs with the now-complete OTLP access-log sink + shares the OTLP proto module already in the build graph).
- **stats sinks** — a metrics-sink subsystem (e.g. the OTLP/StatsD/Graphite stats sinks) over the existing stats registry.
- **tap filter** — the HTTP/network tap filter (request/response capture to a sink).
Use `superpowers:brainstorming` to explore intent + scope + the reference-Envoy surface BEFORE drafting; do live reference-Envoy probes where the behavior is uncertain (`reference_docker_probe_bridge_network`); end with the chartered BRAINSTORM doc + the ROADMAP family-row note. Then the usual lifecycle: SPEC → PLAN → IMPL (subagent-driven in a fresh worktree per `feedback_execution_style` · `feedback_git_worktrees`).

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** The next stage is a BRAINSTORM (docs-only, direct-on-master). If instead the user wants to jump to a specific family row, charter THAT row's BRAINSTORM.
- **Process anchors:** ADR-0106 (per-leg roadmap rows; row 45 is now `done`) · ADR-0045 (split-gate) · ADR-0041/0080 (silent-ignore vs strict-reject) · ADR-0016 (proto blank-import registry) · ADR-0255/0256/0257 (44.x as-built) · **ADR-0258** (45.1 OTLP core) · **ADR-0259** (45.2 operator engine — ACCEPTED, LANDED) · next-free **ADR-0260**.
- **45.x as-built (the Observability landings the next family row builds on):** `internal/accesslog/otlpformat.go` (the operator engine — registry + `CompileOTLPTemplate`/`CompileOTLPValue`/`ValidateOTLPValue` + `Eval`) · `internal/accesslog/otlpsink.go` (`OTLPAccessLogSink` + the buffer/export/retry loop) · `internal/accesslog/otlpmapping.go` (`buildLogRecord`/`buildResource`/`buildExportRequest`) · `internal/bootstrap/bootstrap.go` (`parseOpenTelemetryAccessLog` + `OTLPConfig`) · the `go.opentelemetry.io/proto/otlp v1.0.0` module (already direct-imported) · `test/helpers/otlplogs/` (the driver-owned OTLP receiver) · `test/fixtures/0084-otlp-access-log/` + `0085-otlp-access-log-operators/` (the OTLP differential precedents).

## Read first (in order)
1. THIS FILE — the cold-start router (the 45.2 IMPL is DONE + LANDED + pushed; row 45 `done`; the next action is the next Observability-family BRAINSTORM).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 45.2 (otlp-access-log operator engine) IMPL done`) + the recorded NEXT (the next Observability-family BRAINSTORM) + the count figures.
3. `docs/envoy-go/ROADMAP.md` — row 45 (`otlp-access-log`, now `done`) + the `### Observability family` heading + the remaining family rows (tracing / stats sinks / tap).
4. `docs/envoy-go/DECISIONS.md` — **ADR-0259** (the 45.2 as-built, ACCEPTED) + **ADR-0258** (45.1) + next-free **ADR-0260**.
5. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the `### Access log — OpenTelemetry (OTLP) access-log sink` subsection (the full 45.1+45.2 OTLP contract) + the stat-surface block (1191).
6. `docs/envoy-go/phases/45-otlp-access-log/` — `SPEC-45.2.md` / `PLAN-45.2.md` / `PROGRESS-45.2.md` (the operator-engine leg as-built) + the 45.1 docs (the core leg).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `reference_streaming_sink_differential_framing`, `reference_differential_grpc_receiver_driver_owned`, `reference_docker_probe_bridge_network`, `feedback_execution_style`, `feedback_git_worktrees`, `feedback_subagents_no_push`, `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`).
