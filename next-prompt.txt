=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **THERE IS BANKED MID-LIFECYCLE WORK AT THIS TIP: PHASE 85 (`h2spec-selector-repair`) IS OPEN AT LIFECYCLE-STATE 2.** The phase-85 SPEC landed 2026-08-09 (all seven charter questions DISPOSED; ADR-0307 §Context drafted STATUS `PROPOSED`; row 85 stays `in-progress` at `ROADMAP.md:147`; `want` stays 117). ⇒ **THIS SESSION PERFORMS THE PHASE-85 PLAN.** `STATE.md`'s `next-skill:` reads `PLAN — phase 85 (h2spec-selector-repair)` — that IS your instruction. The PLAN's obligations are enumerated in `SPEC.md` §14 and `STATE.md` next-skill: the single-leg task decomposition in TDD order, the §10 cost enumeration REFUTED BY EXECUTION, the 6.5.2/2 accidental-pass probe, the IWS=0 seeding-quirk probe, the guard NCs, and the ci.yml enrollment mechanics.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=117 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **`want` IS 117** (set at the phase-85 BRAINSTORM, which added row 85; precedent: 84's BRAINSTORM 115 -> 116). ⚠️ **AT THIS TIP CHECK (1) PRINTS `NOT DONE: row 85` — THAT IS THE CORRECT, EXPECTED OUTPUT WHILE PHASE 85 IS OPEN.** It goes silent again only at this row's IMPL flip. Check (2) prints **SIX** at **`:195 :201 :207 :217 :223 :231`** (unchanged across the SPEC because `ROADMAP.md` was BYTE-UNTOUCHED — re-derive them anyway, they drift with every edit). Check (3) is SILENT. ⇒ the sentinel does NOT fire. ⚠️ **A PLAN CHANGES NONE OF THIS: it leaves `ROADMAP.md` BYTE-UNTOUCHED and `want` STAYS 117** (0 of the last 9 PLANs touched ROADMAP; the phase-85 SPEC was verified byte-untouched on every leak axis). If your session's diff touches `ROADMAP.md` at all, that is a FINDING to justify, not a routine edit.

⚠️ **THE DOCTORING NCs REMAIN MANDATORY EVERY SESSION** — with check (3) silent and check (1) printing a single expected line, a broken check is nearly indistinguishable from a healthy one. Both:
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md at want=117: expect BOTH "NOT DONE: row 62" AND "NOT DONE: row 85"
```
```sh
# the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```

⚠️ **AND WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -oiE '-family row'` fails with `grep: amily row: No such file or directory` and the surrounding arithmetic prints `base=0 now=0 delta=0`, **which reads exactly like "no change".** Observed live at FOUR phase-84 stages. **Always pass `--` before the pattern**, and remember the counts are form-dependent: **95 occurrences vs 67 LINES** at this tip (`grep -c` counts LINES). ⚠️ **Row 85's summary deliberately reads `Operational-tooling-family MAINTENANCE row`, which does NOT match either the check-(3) pattern or `-family row`** (the rows-76/78 maintenance precedent). **A gate that reads zero on BOTH sides is not evidence of invariance.**

**SENTINEL RE-RUN MECHANICALLY AT THE PHASE-85 SPEC CLOSE (2026-08-09, base `cbaf5010`). IT DOES NOT FIRE; `stop` was NOT created** (`ls stop` => `No such file or directory`, repo root AND stage worktree). ACTUAL output, recorded rather than predicted: input measured **235 lines / 117 data rows** first; **(1) `NOT DONE: row 85`** at `want=117` — the single EXPECTED line, denominator assertion silent; **(2) SIX — `:195 :201 :207 :217 :223 :231`** (⚠️ do NOT "fix" it and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases; the candidates sentences are a WINDOW onto a larger deferred backlog, not an inventory of it); **(3) SILENT**. The condition is a CONJUNCTION and checks (1) and (2) print ⇒ the sentinel does NOT fire.
- **ALL FOUR NCs FIRED:** row-62 doctoring => `NOT DONE: row 62` AND `NOT DONE: row 85`, with `NC LANDED? [ in-progress ]` inspected first; `want=116` => `GATE FAIL: examined 117 data rows, expected 116`; check-(3) doctoring (residual `gRPC-family row` confirmed **2 -> 0** on the doctored copy first) => `NEVER OPENED: gRPC` while `WASM` correctly stayed silent; check-(2) one-arm counts **5** (long arm alone) / **1** (short arm alone), union **6** (a one-arm strip is NOT an NC for the union).
- **LEAK CHECK — the SPEC left `ROADMAP.md` BYTE-UNTOUCHED and every axis INVARIANT by whole-file count:** lines **235** · data rows **117** · check-(2) union **6** · `-family row` **95/67** · `gRPC-family row` **2** · `Operational-tooling-family row` **3** · escape-aware ARM-A flags **119 and 131 ONLY** (row 85 at `:147` NOT flagged).
- ⚠️ **Re-run all three checks and both doctoring NCs yourself and record what YOU see — before AND after any edit (a PLAN should make NO `ROADMAP.md` edit).**

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (ugrep under the hood). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Reproduced at EIGHTEEN separate tips.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **THE METHOD BINDS; carry no standing discrepancy figure and no standing absolute.** ⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS; print FILE NAMES, never a count.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files; use `-v --no-index`, and `git status` is the discriminating check). ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **`grep -c` ON ZERO MATCHES IS TOOL-SPECIFIC** (only `git grep -c` prints nothing; all exit 1 — capture the value, never chain on the exit code). ⚠️ **`grep -c` COUNTS *LINES*, NOT *OCCURRENCES*.** ⚠️ **`\t` IN GNU ERE IS A LITERAL `t`** — use `-P`. ⚠️ **A `git grep` PATHSPEC ENDING IN `/` READS A FAIL-UNSAFE ZERO** (shape 33) — use the shell-glob form. ⚠️ **AN UNANCHORED PANIC-GREP FALSE-FIRES 14 TIMES ON A FULLY-GREEN DIFFERENTIAL LOG** — **the canonical gate is ANCHORED: `^panic:|DATA RACE|SIGSEGV`. A gate that fires on a green log is worse than no gate.**

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`** with FOUR blind spots (an em-dash, a DOT in `http-1.1`, two LETTER-suffixed ids). ⚠️ **EXECUTED on scratch copies at the phase-77 PLAN: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** ⚠️ **DO NOT restore the regex form.** With row 85 open, check (1)'s single expected line is `NOT DONE: row 85` — **any OTHER row id appearing there is a finding.**

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

⚠️ **Run the union form, never one half of it.** ⚠️ **A ONE-ARM STRIP IS NOT AN NC** (6 -> 5 short / 6 -> 1 long; never 6 -> 0). ⚠️ **The documented trap's stated MECHANISM is refuted while the trap is real** — the inflation comes from ROADMAP table rows carrying BRAINSTORM prose. **Cite the command; do not re-inherit the explanation.** ⚠️ **The line numbers drift with edits — re-derive them; the current six are `:195 :201 :207 :217 :223 :231`.** ⚠️ **These six paragraphs remain the final sentinel blocker, and any edit near them is a sentinel-affecting edit.** The phase-85 BRAINSTORM classified them mechanically: all six are standalone family PROSE paragraphs (not rows), naming ~42 distinct candidates; every "retire the paragraphs" shape is doctrinally foreclosed (`reference_sentinel_matcher_string_self_clears`); the ONLY legitimate move they admit is chartering ONE real item — which is what row 85 does NOT do (its candidate came from SPEC-84 §13, OUTSIDE the windows; no window sentence is edited by this phase, at any stage).


==============================================


## STATUS AS OF 2026-08-09: PHASE 85 (`h2spec-selector-repair`) IS **OPEN AT LIFECYCLE-STATE 2** (SPEC done). ROW 85 IS **`in-progress`** at `ROADMAP.md:147`. **THIS SESSION PERFORMS THE PHASE-85 PLAN.**

**What the 85 SPEC landed (2026-08-09, base `cbaf5010`, branch `phase-85-spec`).** Docs-only, ZERO `.go`: `docs/envoy-go/phases/85-h2spec-selector-repair/SPEC.md` (209 lines — all seven BRAINSTORM-§4 questions DISPOSED plus one NEW decision) + `PROGRESS.md` rolled; **ADR-0307 §Context drafted STATUS `PROPOSED`** (`DECISIONS.md` **17990 -> 18010**, STRICTLY APPEND-ONLY `20 0` with base a byte-exact PREFIX; headings **305 -> 306**; tail now **ADR-0307**; next-free **ADR-0308 FROM THE NEW TAIL**; STATUS census **19 -> 20**; ⚠️ strict `PROPOSED` guard **ARMED 0 -> 1 — a LIVE POINTER a later stage must NOT "fix"**; `^---$` stays **216**); `STATE.md` rolled IN PLACE (64 lines, seven singleton fields verified); `STATE_HISTORY.md` **468 -> 470** (SPEC-84 evicted at the five-entry cap; strictly append-only, numstat `2 0`, base a byte-exact PREFIX via `cmp`; absence guard 0/0 pre-append with THREE controls re-derived FROM THE FILE: phase-82-IMPL-done **5/5** — drifted UP from 4 again, a control's own count is a LIVE FIGURE — phase-83-PLAN-done **3/3** up from 2, invented label **0/0** non-discriminating). `ROADMAP.md` and `BEHAVIOR_CONTRACT.md` **BYTE-UNTOUCHED** (the contract's `:2054`/`:2056` reconciliation is an IMPL edit riding ADR-0307 per ADR-0052 `:1821`). **THREE investigation agents** (A1 EXECUTED in a detached worktree at `cbaf5010`, band 46600-46699, containers `a1p85-*` removed BY NAME, worktree removed and verified; A2/A3 read-only); three agent/predecessor claims did NOT survive (SPEC §13).

### ⚠️ WHAT THE 85 SPEC MEASURED AND DECIDED — CARRY THESE; THE MEASUREMENTS ARE FACTS, THE DECISIONS BIND THE PLAN

- ⚠️ **D-85-SEQ (Q3): ONE IMPL LEG, ONE ATOMIC COMMIT** — fixes + selector flip + guards + JUnit parse fix + docs + CI enrollment together; TDD order INSIDE the leg (unit arms RED first, production fixes green them, THEN selectors + guards, then one full `TestH2Spec` = the commit's 95-green evidence). A fixes-first two-leg split ships production changes whose only gate arrives later — REJECTED (`reference_lifted_reject_hidden_enforcement`). No red tip at any point.
- ⚠️ **D-85-CI (Q1): ENROLL.** The corrected gate is MEASURED deterministic — 3/3 runs identical 95/90/1/4 with the SAME four failing IDs, inner-time spread **5 ms**, **6.58 s inner / ~9 s warm / 17 s cold-build** (⚠️ the BRAINSTORM's "3.77 s" was the BROKEN 53-case set — use 6.58 s for budgeting). The h2spec step joins the existing `differential` job (docker + testcontainers + cold pulls already proven there; ~6.5 min used of a 30-min cap; `-timeout 5m`), in the SAME commit as the fixes.
- ⚠️ **D-85-REF (Q2): MEASURED, and SPEC-84's "disjoint set of three" is REFUTED ON THE COUNT — the reference fails FOUR section-6 cases {6.3/1, 6.7/2, 6.9.1/2, 6.9.1/3}, CONFIRMED fully DISJOINT** from the subject's {6.5.2/1, 6.5.2/3, 6.5.2/4, 6.9.2/1} (empty overlap; each side passes the other's failures). Reference totals **95/82/1/12 on BOTH runs** (57.09 s each, many Timeout-shaped) with the TWELFTH slot FLIPPING inside section 8 (run 1: 8.1.2.1/3; run 2: 8.1/1) — **the reference is NOT flake-free under this harness (n=2) while the subject is zero-variance (n=3)**. FIX-TO-SPEC RULE: RFC 9113 MUSTs bind the subject even where upstream Envoy fails them; reference observations are RECORDED in the pins doc, never copied into expectations. Method caveat recorded: reference measured over the docker bridge, not host-gateway.
- ⚠️ **D-85-WALK (Q4): unit + h2spec suffice; NO differential fixture (+0 held).** The coverage hole is MEASURED AT EXACTLY ZERO — no test anywhere sends a mid-connection INITIAL_WINDOW_SIZE change (28 hits, all production/framer/handshake-initial). Walk surface enumerated with cites in SPEC §5: `ServerConn.streams` (`conn.go:50`) under `s.mu`, per-stream `sendW` (`stream.go:73`, `flow.go:12-16`), negative deltas legal (`flow.go:50` blocks on driven-negative), past-2^31-1 adjustment = CONNECTION error FLOW_CONTROL_ERROR (§6.9.2 — unlike WINDOW_UPDATE's stream error), `safeAddInt32` (`conn.go:25-31`) reusable; PLAN should add a single-critical-section `window.adjust` instead of copying the existing check-then-act TOCTOU. ⚠️ **The HANDSHAKE path `readClientSettings` (`settings.go:79-108`) has the SAME zero validation — ONE shared validator called from both paths.** NO existing unit test sends invalid SETTINGS (all send HeaderTableSize=64, IWS∈{1,16,65535}, MFS=16384, or empty) — nothing breaks.
- ⚠️ **D-85-GUARD (Q5): THREE layers, and the key is the `<testsuite>` `package` ATTRIBUTE** — layer 1 per-declared-selector >= 1 case (deletes the `:310` zero-case skip); layer 2 named-suite roster; layer 3 per-suite minimum counts. ⚠️ **Keying on `id` or a name-prefix parse DOUBLE-MATCHES: the captured XML carries `hpack/*` and `generic/*` suites and `id="6.1"` appears on BOTH `http2/6.1` AND `hpack/6.1`** (controller-verified). Filter to `package` prefix `http2/` before counting.
- ⚠️ **THE JUNIT ROOT CAUSE IS MEASURED FROM CAPTURED XML** (19898 bytes, controller-verified): h2spec emits failures as **`<error>` children, NEVER `<failure>`** — whole-file `<failure` **0** vs `<error>` **4**, suite attrs `errors="3" failures="0"` (the SUITE-level gate `s.Failures + s.Errors` counted correctly all along; only per-case reporting was blind). ⚠️ **SECOND latent bug: `<testcase>` has NO `name` attr** (only `package`/`classname`/`time`) — `junitTestCase.Name` parses `""`. Fix: add `Error *junitFailure` + `ClassName string`, coalesce `Failure != nil || Error != nil`, print ClassName (~10-20 lines).
- ⚠️ **D-85-66 (Q6):** the `h2spec.go:30` exclusion comment is REWRITTEN to state the measured vacuity (the pinned image ships NO 6.6 suite; the 8.2 probe covers client-sent PUSH_PROMISE), NOT deleted — deleting invites re-adding `http2/6.6`, which selects zero cases and trips layer 1.
- ⚠️ **D-85-SWEEP (Q7): the reconcile set is FIVE files** — `h2spec.go` (the fix), `CONFORMANCE_PINS.md` (APPEND-style per its own audit discipline: the 2026-04-25 53/53 record STAYS as true history; a corrected 95-scope table + new run record + reference-observation ¶ are APPENDED), `BEHAVIOR_CONTRACT.md` `:2052-2054` + `:2056` ONLY (riding ADR-0307 per `:1821`; the five historical 53/53 evidence ¶s at `:1464 :1669 :4263 :5002 :5009` are RECORDED, not rewritten), `STATE.md:38` (the living `conformance: h2spec 53/53` line), `.github/workflows/ci.yml` (the enrollment step). **RECORD-only:** DECISIONS 26x + ADR-0051 `:1747-1750` (false scope, QUOTED in ADR-0307) + ADR-0052 `:1803` · ROADMAP 33x on 31 historical rows (⚠️ retro-editing them is the leak-audit hazard; the ONLY sanctioned IMPL edit is row 85's done-flip) · phases 527x/226 files · STATE_HISTORY 6x · `docs/TEST_GAP_ANALYSIS.md:28` ("53/53 sections", doubly false — snapshot doc, RECORD) · the stray phase-32.1 `PROGRESS.md` at the repo ROOT.
- ⚠️ **D-85-CLIENT (NEW): the client side carries the EXACTLY-SYMMETRIC gap and it is OUT** — `h2/client.go:376-407` applies peer SETTINGS with zero validation and NO streams walk (`cc.streams` sync.Map; `.Range` precedent at `:628-638`), same `<= 0 -> 65535` seeding quirk at `:192-195`. h2spec exercises ONLY the server side — an ungated production change is the one-sided-gate shape (`reference_one_sided_gate_for_a_two_sided_fix`). NAMED in ADR-0307 beside the CONTINUATION-blind response loop; a future client-robustness row prices both WITH a gate.

### ⚠️ WHAT THE PLAN OWES (SPEC §14)

1. **Task decomposition of the SINGLE leg in TDD order** (subagent-driven per `feedback_execution_style`), with the gate-evidence run LAST.
2. **The §10 cost enumeration REFUTED BY EXECUTION** — floor bands: selectors ~10-15 · guards ~70-120 · JUnit parse ~10-20 · SETTINGS validation ~40-70 (shared validator, BOTH paths) · §6.9.2 walk ~35-55 (incl. `window.adjust`) · unit tests ~250-450 · docs ~55-90 · ADR-0307 completion ~60-100. Central band **~420-750 net `.go`**. ⚠️ **Every figure is a LOWER BOUND** — `reference_measured_prototype_is_a_lower_bound` fired on SIX consecutive rows; the cause is under-ENUMERATION. Likeliest unpriced lines, named in SPEC §10: a 6.9.2-adjacent case newly RED once the walk lands; `readClientSettings` reject-path error plumbing; the IWS=0 quirk fix if its probe reds.
3. **The 6.5.2/2 accidental-pass probe:** the unvalidated IWS > 2^31-1 case PASSES today — identify the MECHANISM before adding the check (`reference_compensating_defects_cancel_in_the_gate_metric`); the check (-> FLOW_CONTROL_ERROR, not PROTOCOL_ERROR) goes in IFF the pass is accidental.
4. **The IWS=0 seeding-quirk probe:** `conn.go:373-375` cannot distinguish "peer never announced" from "peer announced 0" (both uint32 zero -> 65535 default). Probe whether any corrected-set case discriminates; fix if RED, record in ADR-0307 as a named latent quirk if unreachable.
5. **Guard NCs:** a doctored selector MUST redden the repaired harness (`reference_gate_command_negative_control` — the NC ITSELF can be broken); also NC layer 2 (drop a suite from the roster copy) and layer 3 (raise a min count).
6. **ci.yml mechanics** for the enrollment step (~10-15 lines, `differential` job).
7. **A PLAN is docs-only**: ZERO production `.go`, ZERO test `.go`, `ROADMAP.md` BYTE-UNTOUCHED, `want` stays 117, ⚠️ **strict `PROPOSED` guard STAYS 1** — a PLAN that "fixes" it to 0 destroys a live pointer. Prototype/probe code lives in DETACHED worktrees and is reverted with sha256 proof.

### ⚠️ THE SIX-GATE STANDING POSTURE — NAME THE DEPARTURES, DO NOT CLAIM COMPLIANCE

**(a)/(b)** — a docs-only PLAN owes these only as POSTURE statements; the suite is **121** fixtures and this row anticipates **+0** (the h2spec gate is `test/conformance/`, not `test/differential/`). **(c)** — `test/conformance/grpc/` stays DEFERRED IN WRITING (SPEC-84 §4); proxy-wasm 10 of 16 families; ⚠️ **h2spec MUST be stated WITH the scope caveat until this row's IMPL lands: "NO ROW MAY CITE h2spec 53/53 AS FRAME-LEVEL EVIDENCE" — this row retires that caveat AT ITS IMPL, and citing its own future repair as present-tense evidence is the exact defect it fixes.** **(d)** — say "vacuous" not "green" when nothing introduces a parser/codec/filter; fuzzers **55** in **48** files (`FuzzFrameStream` already traverses `onSettings` and will explore the new reject paths free — no new fuzzer owed, no new parse surface). **(e)** — `INNER_EXIT` mandatory on every differential launch AND `go test ./...` (a PLAN typically runs neither; if a probe runs them, cite honestly). **(f)** — STANDING DEPARTURE, named not claimed: no `REVIEW.md`; **37 of 126** phase dirs carry one, none since 25.3. ⚠️ **`/BOOTSTRAP_PROMPT.md` WITH A LEADING SLASH RESOLVES NOWHERE** — the file is `<repo>/BOOTSTRAP_PROMPT.md` (522 lines); the `docs/superpowers/plans/` copy is the divergent GENERATOR PLAN with a NON-constant offset.

**STAT SURFACE — the 85 SPEC landed +0** (docs-only). The canonical measure remains **call-site enumeration (208/36 at the 84.2 close — cite 208/36, NEVER 208/84)**, re-derived at base AND tip when a stage touches `.go`; **NEVER via `TestNoNewStat*`** (proven blind by execution at the 84.1 PLAN). The absolute is **1207, DOC-SOURCED from `BEHAVIOR_CONTRACT.md`'s ledger tail** — ⚠️ **the 1205-vs-1207 contradiction (`STATE.md:33` vs ADR-0300/0301 headers) stays RECORDED as WIDENED; only the DELTA is ever asserted.** This row anticipates **+0** (no `NewCounter`/`NewGauge` site in any enumerated edit).

### ⚠️ ADR + DOCUMENT CONVENTIONS

**The DECISIONS tail is now `ADR-0307`, STATUS `PROPOSED` — next-free is `ADR-0308`, DERIVED FROM THE TAIL** (headings+1 COLLIDES at the ADR-0209 gap). ⚠️ **The strict guard `^> \*\*STATUS: PROPOSED` reads 1 at this tip and that is CORRECT — a LIVE POINTER armed at the SPEC, disarmed at the phase-85 IMPL** (§Decision + §Consequences appended IN PLACE after the RETAINED italic footer, no renumber, no `---`; the ADR-0294-0306 shared block form). ⚠️ **Do NOT use the loose form `^> \*\*STATUS: .*PROPOSED`** — every hit is a false positive; carry no whole-file count of it. `DECISIONS.md` is APPEND-ONLY: ADR-0051/0052 stay recorded-not-fixed; ADR-0307 is the designated correction vehicle.

### ⚠️ COUNTS AT THIS TIP — RE-DERIVE MECHANICALLY, NEVER COPY

**HOUSE RULE: every number below is a LIVE FIGURE. Re-run it in your own worktree with its negative control observed.**

- **differential fixtures 121** — numeric tail `0119-grpc-unary-trailers`, next-free `0120`. TWO reference-port conventions COEXIST (`10<index>` for 0103+; sequential 19xxx for 0040-0102). **This row anticipates +0 fixtures.**
- **`ROADMAP.md` 235 lines / 117 data rows**; row 85 at `:147`; check-(2) anchors `:195 :201 :207 :217 :223 :231`; `-family row` **95 occurrences / 67 LINES**; `gRPC-family row` **2**; `Operational-tooling-family row` **3**.
- **`DECISIONS.md` 18010** lines · **306** `^## ADR-` headings · tail **`ADR-0307` PROPOSED** · `^## ADR-0308` = **0** · `^---$` **216** · STATUS census **20** (matcher `^> \*\*STATUS: `, a BLOCKQUOTE) · strict `PROPOSED` guard **1** (LIVE, correct).
- **`BEHAVIOR_CONTRACT.md` 5955** (byte-untouched this stage) · **`STATE.md` 64** (seven singleton fields verified at this close) · **`STATE_HISTORY.md` 470** · **`BOOTSTRAP_PROMPT.md` 522** at the repo root · phase dirs **126**, `REVIEW.md` **37**.
- **h2spec at this tip (BY EXECUTION, n=3 subject / n=2 reference):** broken selectors **53** cases · corrected **95** (§6: 42/37/1/4; failing IDs {6.5.2/1, 6.5.2/3, 6.5.2/4, 6.9.2/1} — identical all runs) · reference corrected **95/82/1/12** (section-6 failures {6.3/1, 6.7/2, 6.9.1/2, 6.9.1/3}, twelfth slot FLAKY in section 8) · declared section-6 selector strings **9** + 1 comment · image pinned by sha256 in `h2spec_test.go`.
- **stat call sites 208 / 36 production files** (at the 84.2 close) · **fuzzers 55** in **48** files · **BackendKind tail 38** over **39** declarations (do NOT "fix").
- ⚠️ **STILL CONTESTED, NO NUMBER CARRIED:** the `STATE_HISTORY.md` archive-gap total · production `stats.IsValidName` guard sites · `allCallbacksNoOp` occurrences · the loose `PROPOSED` matcher's whole-file count · whether 84.1's realized net counts its committed probe (3414 vs 3124) · **the stat-surface absolute (1205 vs 1207 — WIDENED)** · ⚠️ **NEW: the count of zero-test `generic/*` suites in the h2spec XML (A1 said 14, controller line-count read 13 — the guard filters on `package` prefix `http2/` either way; the load-bearing fact is that non-http2 suites EXIST).**

### ⚠️ DOCUMENTARY DEFECTS — carried forward, still deliberately NOT fixed

- ⚠️ **`ADR-0051` §2 is FACTUALLY WRONG** about the h2spec sections the gate runs (`DECISIONS.md:1747-1750`; append-only ⇒ RECORDED, NOT FIXED — **ADR-0307 is the designated correction vehicle and now QUOTES the false sentence verbatim**); its PUSH_PROMISE rationale is moot twice over, and the 6.6 exclusion it defends is VACUOUS. `ADR-0052 :1803` repeats the wrong scope claim.
- ⚠️ **`ADR-0058` cites `internal/filter/hcm/actions.go:routerActionH2.doH2` TWICE and the symbol is not there** — `routerActionH2` is at `router_h2.go:254`; the emit is `writeH2Reply` at `h2dispatch.go:720`. Do not propagate either.
- ⚠️ **`ROADMAP.md` malformation: rows at lines 119, 131 lose summary content in GFM render** — ARM-A (escape-aware `pieces != 8`) flags **119 and 131 ONLY**; row 85 at `:147` is NOT flagged. Naive `NF != 8` is wrong in both directions; no ARM-B count is carried. INVARIANT-BLOCKED ⇒ a GUARD, not a fix.
- **`STATE.md` §Project counts SELF-CONTRADICT §Current and are FROZEN at phase 76** (fixtures 119; stat 1205; "next-free port 10450"). **Do not source from §Project; do not fix it.** ⚠️ **`STATE.md:38` (`conformance: h2spec 53/53`) is the LIVING false line — it flips at THIS ROW's IMPL, not before.**
- ⚠️ **`test/differential/harness_test.go:208`'s port inventory is STALE** (true set reaches 10449+; claims 10000..10447).
- ⚠️ **The two `//nolint:gosec` directives in `body.go` are INERT** (`.golangci.yml` `disable-all: true` + 9 linters; misspell locale US) · ⚠️ **The xDS config-seam cycle guard is NOT AUTOMATED ANYWHERE** · ⚠️ **`internal/wasm/doc.go:219` carries TWO errors** · **`ROADMAP.md` cites `github.com/esalaine/envoy-go/…` FIVE times** (module is `github.com/pgdad/envoy-go`) · **`rbac.go:50` contains the token `F2`** as a phase-26 fork label — do not grep-anchor on `F2` in that package · **`docs/TEST_GAP_ANALYSIS.md:28`** says "53/53 sections" (doubly false; snapshot doc, RECORD) · **a stray phase-32.1 `PROGRESS.md` sits at the repo ROOT** (historical stage content; location flagged, not this row's job).

### ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS YOURSELF AND RECORD ACTUAL OUTPUT** — both doctoring NCs every time; check (1)'s expected line is exactly `NOT DONE: row 85` and nothing else.
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** Lineage: phase-85 **B: three agents, the gate run both ways; S: three agents, the reference MEASURED (SPEC-84's "three" refuted on the count), the JUnit root cause captured, the guard key corrected against the XML (`id` collides), three claims rejected (SPEC §13)**. ⚠️ **A refutation is a claim too, and a controller/task BRIEF is a claim too.**
3. **BEFORE ANY LINE-ADDING EDIT TO A HEAVILY-CITED DOC, ENUMERATE THE CITES YOUR INSERT WOULD SHIFT** — the ADR-0307 append was TAIL-ONLY (zero by-line cites shift); the IMPL's `BEHAVIOR_CONTRACT.md` edit must re-run this check.
4. **LINE ANCHORS DRIFT; SYMBOL ANCHORS DID NOT.** Re-verified at the 85 SPEC: `thresholdSections` **`h2spec.go:21-37`** (nine slash-form strings :25-34, comment :30), the zero-case skip **`h2spec_test.go:310-312`**, `assertThreshold` **`:295-351`**, the JUnit structs **`:271-280`**, `onSettings` **`conn.go:507-538`** (IWS case :519-520), the frame switch **`conn.go:250-280`**, the CONTINUATION discard **`conn.go:255-259`**, `readClientSettings` **`settings.go:79-108`**, the client SETTINGS case **`client.go:376-407`**, streams map **`conn.go:50`**, `safeAddInt32` **`conn.go:25-31`**. **RE-VERIFY EVERY CITE AT YOUR TIP.**
5. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Still live: the SDS dial-budget flake (TWO packages) · the `internal/cluster` `-race` outlier · `internal/httpclient TestOptions_ZeroValue_NoOpDefaults` · the driver-receiver port race (roster 42; `0081` binds `0.0.0.0:42039`) · the TWO 84.2-era flakes (`0079` held-stream 502; `internal/wasm` A4), cleared on re-run, unclassed. ⚠️ **NEW at this SPEC: the REFERENCE flips one section-8 h2spec case between runs (8.1.2.1/3 vs 8.1/1) — reference-side ONLY; the subject is zero-variance at n=3.**
6. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only). The A1 pattern from this SPEC: reference container `a1p85-ref` on band ports + h2spec by pinned sha256 against the bridge IP; subject probes in a DETACHED worktree with the nine chars flipped — **revert with sha256 proof, remove the worktree, verify `git worktree list`**.
7. **MACHINE-STATE DELIBERATELY IN PLACE (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-85-*` worktree should outlive its stage close — check `git worktree list`; expect only `master` plus your own.**
8. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`.
9. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL.**
10. **A DEFERRED CANDIDATE'S COST GOES STALE** — re-derive at the current tip; **a MEASURED PROTOTYPE IS A LOWER BOUND** (six consecutive firings; under-ENUMERATION, not under-scaling).

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — take it from `git rev-parse master`, **NOT from a SHA quoted in this file**. ⚠️ **THE BARE SUBJECT-LOCATE GREP IS CONTAMINATED BY THIS FILE'S OWN PROSE** — router commits discuss the phase in their BODIES. **USE THE SLUG-ANCHORED FORM**, stage-exact:
```sh
git log --grep '^phase 85 (h2spec-selector-repair)' --format='%h %s'   # 2 at this tip: BRAINSTORM + SPEC. An invented slug returns 0.
git log --grep '^phase 84 (grpc-unary-response-trailers)' --format='%h %s'  # 6 — a CLOSED phase, use it as a FIRING CONTROL
```
⚠️ **The count comments above are LIVE FIGURES that have gone stale in this file before — VERIFY before relying on them.** Take the NEWEST line (`| head -1`).

Subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** and tear down docker **BY NAME**. ⚠️ **Band private ports outside BOTH `20000-31007` (subject blocks) AND `11000-14999` (backends + subject admin), and clear of the static fixture ports (19 bands, 10000-19172).** The `42000-46099` band has been used safely (phases 80-84) — ⚠️ **but `0081`'s ALS receiver binds `0.0.0.0:42039` inside it**; the 85 BRAINSTORM used `46500-46599` and the 85 SPEC used `46600-46699`, both cleanly. ⚠️ **If two agents share ONE worktree they must stage by EXPLICIT PATH and never run an unscoped `git restore`; PREFER ONE WORKTREE PER AGENT.** Per-task `gofmt` + `golangci-lint` on touched packages. ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch.**

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root. **Observed live AGAIN at this stage (every compound command's trailing `pwd` printed the repo root).** Use `git -C <abs-worktree-path>` for every git command; put any needed `cd` FIRST in a compound command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — `git add` ALONE WILL NOT STAGE IT. USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — restoration verified by sha256 against the base blob, never by eye; injection-site occurrence asserted `== 1` before writing; work DETACHED so a failed revert cannot dirty a branch.

⚠️ **THE ARCHIVE-ABSENCE GUARD — USE THE ROBUST FORM.** Anchor on the bullet, allow ANY run of characters before the backtick-quoted target, corroborate with a whole-file fixed-string search. ⚠️ **NC IT WITH A *REAL* TARGET re-derived FROM THE FILE first** — an invented label reads 0 on every arm and looks exactly like agreement. ⚠️ **A CONTROL'S OWN COUNT IS A LIVE FIGURE:** `phase 82 … IMPL done` read 4 at the 85-BRAINSTORM close and **5** at this SPEC close — each eviction entry citing it adds a line. **Re-derive it; never copy it.** ⚠️ **DESCRIBE the guard's regex in the archive entry label; never spell it.** ⚠️ **Verify the archive edit is STRICTLY APPEND-ONLY**: `git diff --numstat` = `N 0` AND the base file a byte-exact PREFIX (`cmp` the base blob against `head -<baselines>` of the new file).

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY** (seven singleton fields, each verified singleton at the 85 SPEC close; §Recent at its five-entry cap). ⚠️ **`next-skill:` reads `PLAN — phase 85 (h2spec-selector-repair)` — that IS your instruction.** ⚠️ **§Project counts SELF-CONTRADICT §Current — do not anchor on them, do not fix them.**
2. `docs/envoy-go/phases/85-h2spec-selector-repair/SPEC.md` — the binding dispositions: §2 D-85-SEQ (single leg, TDD order), §3 D-85-CI, §4 D-85-REF, §5 D-85-WALK, §6 D-85-GUARD/D-85-66, §7 the JUnit fix, §8 D-85-SWEEP, §9 D-85-CLIENT, §10 the cost enumeration the PLAN must refute, §14 what the PLAN owes. Then `BRAINSTORM.md` §3/§5 for the charter and what is already established BY EXECUTION.
3. `docs/envoy-go/ROADMAP.md` — **§Schema first** (`:18`), then row 85 at `:147`, then the check-(2) lines at `:195 :201 :207 :217 :223 :231` (sentinel-sensitive; a PLAN touches NONE of this file).
4. `test/conformance/h2spec/h2spec.go` + `h2spec_test.go` — the selector list `:21-37`, the zero-case skip `:310-312`, `assertThreshold` `:295-351`, the JUnit structs `:271-280`; `internal/filter/hcm/h2/conn.go` — `onSettings` `:507-538`, streams map `:50`, `safeAddInt32` `:25-31`; `internal/filter/hcm/h2/settings.go` — `readClientSettings` `:79-108`; `internal/filter/hcm/h2/flow.go` — the `window` type `:12-16`.
5. `BOOTSTRAP_PROMPT.md` — repo ROOT, no leading slash (522 lines). §6.1 `:285`, §6.2 `:294`, §7.3 `:346-351`, §7.5 `:357-367`, gates `:360-365`.
6. `docs/envoy-go/DECISIONS.md` — append-only; tail **`ADR-0307` PROPOSED** (the phase-85 SPEC's §Context draft — read it, the IMPL completes it in place), next-free **ADR-0308** derived from the tail; ADR-0051 `:1747-1750` and ADR-0052 `:1803`/`:1821` for the false-claim context.
7. `docs/envoy-go/CONFORMANCE_PINS.md` — the 18-suite audit table `:37-57` and the 2026-04-25 first-run record (both STAY; the IMPL appends the corrected record per D-85-SWEEP).

## After the PLAN lands — the roll-forward
A PLAN is docs-only: `ROADMAP.md` BYTE-UNTOUCHED, `want` STAYS **117**, row 85 STAYS `in-progress`, the strict `PROPOSED` guard STAYS **1**. Re-run the sentinel and record what you see: expect **(1) `NOT DONE: row 85`** (and NOTHING else), **(2) SIX** (re-derive the anchors), **(3) SILENT** ⇒ the sentinel does NOT fire and `stop` is NOT created. **Roll this file to point at the IMPL-85**, carrying: the sentinel state you actually observed with both doctoring NCs, the PLAN's task list and break roster, the measured answers to the 6.5.2/2 and IWS=0 probes, the re-derived counts, and the final cost figures the IMPL must land against.
