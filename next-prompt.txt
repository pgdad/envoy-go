=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-CHECKED 2026-07-12 (after the phase-59 IMPL landed, run MECHANICALLY) — it does NOT fire. Do NOT create `stop`.** State:
- (1) prints `NOT DONE: row 60`, `NOT DONE: row 61` — TWO `in-progress` rows (rows 60 `xds-sds-server-cert` + 61 `http3-downstream-listener`, each a CONFIRMED SPLIT banked at SPEC). Row 59 `tracing-custom-tags-literal` FLIPPED `done` at its IMPL six-gate this session (ADR-0106). This ALONE blocks `stop`.
- (2) prints THREE live "candidates:" sentences — Observability (`OTLP-metrics stats sink + tracing custom_tags (request_header/environment/metadata)/spawn_upstream_span/http_service/force-trace` — `custom_tags` NARROWED at the phase-59 IMPL: the literal type is now CONSUMED, the three non-literal types remain), xDS (`SDS rotation + validation_context/upstream SDS + CDS/EDS + LDS/RDS + ADS + Delta + RTDS/Runtime + reconnection/initial_fetch_timeout + google_grpc`), and HTTP/3 (`upstream H3 + alt-svc + 0-RTT + h3spec + QuicProtocolOptions + full transport-socket options + QUIC robustness`). ⇒ three families STAY OPEN. (`reference_sentinel_deferred_sentence_live_vs_historical`.)
- (3) prints `NEVER OPENED: gRPC`, `Runtime`, `WASM` ⇒ THREE families never opened. Operational-tooling also STAYS OPEN.

All three checks print ⇒ the sentinel does NOT fire.

**STATUS AS OF 2026-07-12: the phase-59 IMPL LANDED (row 59 `done`).** The NEXT IMPLEMENTABLE STAGE is the **phase-60.1 PLAN** (`xds-sds-stream-substrate`, lifecycle-state 2 → 3). Rows 60 and 61 are BANKED at SPEC by the parallel design fan-out — each a CONFIRMED SPLIT (60.1/60.2 and 61.1/61.2/61.3), so each leg needs its OWN PLAN then IMPL. The roller ADVANCES the banked xDS opener (the lighter of the two banked legs — NO new go.mod module, unit-proven, NO differential) before opening any brand-new subject, per the standing directive. The next stage is therefore the phase-60.1 PLAN — decomposing SPEC-60 §3.1/§3.3/§7/§10 (the 60.1 substrate scope) into a TDD spine.

**Why 60.1 over 61.1 (recorded — the roller's pick):** both banked openers are already BRAINSTORMED + SPEC'd. 60.1 `xds-sds-stream-substrate` is the smaller/more-defensible next PLAN — it stands up the `internal/xds` discovery-stream client at UNIT level (in-process fake SDS server; NO TLS integration, NO differential — the phase-39.1/46.1a substrate-leg precedent), reuses `grpcclient.Dialer`, and adds NO new go.mod module (go-control-plane v1.32.4 already carries `service/secret/v3` + `service/discovery/v3`). HTTP/3 61.1 requires the FIRST-ever new go.mod module (`github.com/quic-go/quic-go`) + a novel UDP/QUIC listen path — heavier; it rolls next after the xDS legs, or per a future roller pick.

**ADR reservation ledger (provisional; each finalized at its IMPL in landing order):** ADR-0277 = phase-59 (LANDED this session) · **ADR-0278 = xDS 60.1 (the NEXT to land)** · ADR-0279 = HTTP/3 (61) · ADR-0280 = xDS 60.2 · next-free unreserved **ADR-0281**. **60.1 lands NO fixture** (a substrate-only leg — the differential is 60.2's); no `0103-*` dir is created at 60.1. When 60.2 or HTTP/3-61.3 reach IMPL, RE-DERIVE the actual fixture-dir number against the master tip at IMPL time (the tail is now `0102-tracing-custom-tags-literal`; landing order assigns 0103/0104…).

**Current counts (verified after the phase-59 IMPL, on the frozen stage HEAD):** fixtures **104** (tail `0102-tracing-custom-tags-literal`) · fuzzers **54** · DECISIONS tail **ADR-0277** (next-free reserved ADR-0278) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** · new Go packages **0** · new go.mod modules **0**. RE-VERIFY against the actual master tip at session start (`git fetch` first).

**Locating the phase-60 SPEC stage squash** (a router cannot name its own sha — identify by SUBJECT, never by position):
```sh
git log --oneline --grep='^phase 60 (xds-sds-server-cert) SPEC' -1
```

**THE NEXT ACTION: the phase-60.1 PLAN (lifecycle-state 2 → 3; docs-only — produce `docs/envoy-go/phases/60-xds-sds-server-cert/PLAN-60.1.md` + a `PROGRESS-60.1.md` scaffold; NO production `.go` changes this stage; row 60 STAYS `in-progress`).** After the 60.1 PLAN lands, the roller's next stage is the phase-60.1 IMPL; the sentinel governs termination, not this list.

==============================================

## What THIS session does (the phase-60.1 PLAN)

Perform the PLAN for phase 60.1 (`xds-sds-stream-substrate`) — the docs-only decomposition of the SPEC-60 60.1 substrate scope into a MECHANICAL TDD spine. Use `superpowers:writing-plans` (recommended) in a FRESH worktree off master (`.worktrees/phase-60.1-plan`, branch `phase-60-xds-sds-stream-substrate-plan`, per `feedback_git_worktrees`). This is a SINGLE STAGE (the PLAN); do NOT start the IMPL.

**The 60.1 substrate scope (from SPEC-60 §3.0/§3.1/§3.3/§7 — RE-READ them; all fourteen D-XDS-* are disposed at the SPEC):**
- A NEW `internal/xds` (and/or `internal/xds/sds`) discovery-stream client: dial via the existing `grpcclient.Dialer`; open a dedicated `StreamSecrets` stream; run the SotW `DiscoveryRequest`/`DiscoveryResponse` ACK/NACK version+nonce loop; parse a `Secret` (`envoy.extensions.transport_sockets.tls.v3.Secret` → `TlsCertificate`) into a `*crypto/tls.Certificate`.
- The `SecretProvider` interface — a blocking `FetchInitialCertificate(ctx, secretName) (*stdtls.Certificate, error)` bounded by `initial_fetch_timeout` (reference default 15s) — the seam that 60.2 plumbs into `internal/tls`.
- The `sds.*` stats (a 5-counter SUBSET of the reference's 14; §7), registered under an `sds.<secret_name>.` scope with the secret-name segment guarded by `stats.IsValidName` before `NewCounterIfAbsent` (`reference_dynamic_stat_name_charset_guard`).
- Proven at UNIT level against an in-process FAKE SDS server (a `test/helpers`-style server the client DIALS — NOT a runner BackendKind, `reference_differential_grpc_receiver_driver_owned`) + a NEW `FuzzDiscoveryResponseParse` (the `DiscoveryResponse`→`Secret` parse is a new untrusted wire boundary; fuzzers **54 → 55**).
- **60.1 has NO TLS integration and NO differential/fixture** (the phase-39.1/46.1a substrate-leg precedent) — those are 60.2. Do NOT scope them into 60.1.

**Anticipated at the phase-60.1 IMPL (record in the PLAN's global constraints; RE-VERIFY at IMPL):** a NEW `internal/xds`[+`/sds`] package (packages **+1..+3**); fuzzers **54 → 55** (`FuzzDiscoveryResponseParse`); stat surface **1201 → ~1206-1208** (the `sds.*` 5-counter subset — DYNAMIC, registered only under SDS config, so +0 to the byte-stable no-SDS path); fixtures **104 (+0 at 60.1)**; BackendKind **38 (+0)**; go.mod modules **+0** (go-control-plane v1.32.4 already carries `service/secret/v3` + `service/discovery/v3`); DECISIONS tail **ADR-0277 → ADR-0278** (at the 60.1 IMPL, per ADR-0044). ADR-0045 escape-valve is CONSUMED (the 60.1/60.2 split); row 60 flips `done` only when BOTH legs land (`reference_roadmap_split_phase_row_done`).

**The PLAN this session produces:** a task-by-task TDD spine (`superpowers:writing-plans` shape) with per-task files/interfaces/steps + RE-DERIVED `file:line` citations (`feedback_brief_citations_not_evidence` — RE-DERIVE every citation against the master tip at PLAN time, do NOT copy the SPEC's), a self-review, and a `PROGRESS-60.1.md` scaffold. Anchor the ADR-0278 §Decision/§Consequences to land at the 60.1 IMPL (the SPEC drafted §Context in §13). Then ROLL THIS FILE to the phase-60.1 IMPL.

## Honor throughout (the discipline, unchanged)
- **Lifecycle = one stage per session.** This session: the phase-60.1 PLAN ONLY (docs-only). After it lands: the phase-60.1 IMPL.
- **Process anchors:** ADR-0044 (ADR §Decision/§Consequences land at the IMPL) · ADR-0045 (split gate ~15 tasks; CONSUMED by the 60.1/60.2 split) · ADR-0080 (distinct per-arm reject substrings) · ADR-0106 (a split-phase row flips `done` only when ALL legs land). Next-to-land ADR is **ADR-0278** (reserved for 60.1).
- **Subagent-driven execution** (`feedback_execution_style`, `feedback_subagent_autocommit_claudemd`, `feedback_subagents_no_push`): even a docs-only PLAN stage runs subagent-driven where it helps; subagents commit LOCALLY only; the controller squashes at stage-close and pushes. Watch for HEAD-detach on deliberate breaks (`feedback_subagent_worktree_detach`) and stale-path targeting (`feedback_subagent_worktree_path_targeting`).
- **`next-prompt.txt` IS TRACKED** (`reference_next_prompt_tracked_despite_gitignore`) — edit it inside the stage worktree and fold into the stage commit; locate commits by SUBJECT (`git log --grep`), never by position.
- **Deferred maintenance (fold into the next `udp.go`/statssink-driver-touching row — carried from the phase-57 final review; phases 58/59 touched NEITHER, and 60.1 is xDS/TLS so it does NOT touch `udp.go` either — these STAY OPEN):** F-1 the 0101 driver's counter tag snapshot records the EXPECTED map; F-2 `udp.go` `udpWriter` doc comment names two sinks (three embed it now); F-3 BEHAVIOR_CONTRACT 0101 note says "`differential.HostGatewayIP` helper" where the driver uses its LOCAL duplicate. (Ride until the OTLP-metrics or another statsd/graphite/dog_statsd sink row reopens `udp.go`.)

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-60.1 PLAN is next; the subject is BANKED — xDS SDS stream substrate; all D-XDS-* disposed at the SPEC).
2. `docs/envoy-go/phases/60-xds-sds-server-cert/SPEC.md` — the disposed D-XDS-* (§1 table), the CONFIRMED SPLIT (§3.0), the 60.1 `internal/xds` client scope (§3.1), the config-seam verdict "MANAGEABLE, no async warmup refactor" (§3.3), the initial-fetch BLOCK-then-BOOT-FAIL DEPARTURE (§3.4 — 60.2's, but read for the seam), the `sds.*` 5-counter subset (§7), the test plan (§10), the SPEC-time probe evidence (§11), the RE-DERIVED edit-site roster (§12), and the ADR-0278 §Context draft (§13).
3. `docs/envoy-go/phases/60-xds-sds-server-cert/BRAINSTORM.md` — the opener rationale + the rejected openers (RTDS/CDS/LDS/SDS-with-rotation/ADS).
4. `docs/envoy-go/ROADMAP.md` — row 60 `in-progress`; the xDS-family row + prose; the LIVE xDS deferred sentence (still ONE live match).
5. `docs/envoy-go/STATE.md` — the active-phase header (phase 59 IMPL done; NEXT = the phase-60.1 PLAN).
6. `docs/envoy-go/DECISIONS.md` — the ADR-0277 tail (next-to-land **ADR-0278**, reserved for 60.1); ADR-0158 (the `grpcclient.Dialer` seam 60.1 reuses).
7. `internal/grpcclient/` (the `Dialer` 60.1 dials through) · `internal/tls/config.go` (the `:153` `tls_certificate_sds_secret_configs` reject 60.2 lifts — read for the seam shape) · `internal/stats/` (`IsValidName` + `NewCounterIfAbsent` for the dynamic `sds.*` scope) · `internal/boot/` (the boot ordering `boot.go:42-52` the provider threads through).
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the `reference_*` traps (esp. `reference_dynamic_stat_name_charset_guard`, `reference_differential_grpc_receiver_driver_owned`, `reference_fuzzer_count_docs_drift`, `reference_brief_citations_not_evidence`, and the split-phase/sentinel references).
