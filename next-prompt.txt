START a fresh cold-start session for envoy-go — the §9 HTTP-filters family is CLOSED; route to the NEXT family.

This is a FAMILY-TRANSITION cold-start. **Phase 25 (`http-filter-wasm`) IMPL is DONE + squash-merged to master + pushed to origin** (25.3 IMPL squash `57c7c4d`; STATE/PROGRESS SHA-fill `24c8fe1`; origin/master at `24c8fe1`). With phase 25.3 IMPL phase-done, the **§9 HTTP-filters family is CLOSED** — phase 25 was the EIGHTEENTH-and-FINAL §9 production HTTP filter row (parent row 25 + sub-row 25.3 both flipped `in-progress → done` ATOMICALLY at the 25.3 IMPL squash per ADR-0106 + the 18/19/22/24 ROLLUP precedent). **0 remaining §9 HTTP-filters rows.** STATE.md points at `phase 25.3 IMPL done; §9 HTTP-filters family CLOSED`, next-skill `superpowers:brainstorming`, next-free `ADR-0213`.

## What this session must do: BRAINSTORM the next family/phase per BOOTSTRAP §5 routing

The §9 HTTP-filters family is exhausted. The NEXT feature family on the ROADMAP (`docs/envoy-go/ROADMAP.md` → `## Feature Families (09+)`) after `### HTTP filters family` is **`### Network filters family`** (candidates: `redis, mongo, kafka_broker, thrift, zookeeper [scope TBD], echo, direct_response, sni_cluster, rbac network`). Per the ROADMAP invariant "Families (§9 headings) are not rows — each heading is expanded into per-phase rows when a phase under that family enters in-progress", there is NO pre-populated row for the next phase yet. The next-skill per STATE.md + SKILL_ROUTING + BOOTSTRAP §5 is **`superpowers:brainstorming`** to scope the Network-filters family + pick + brainstorm its FIRST phase (the brainstorm chooses the filter + the surface, per the parent-family-BRAINSTORM precedent used for the wasm family at phase 25).

This session:
1. Reads STATE.md + ROADMAP.md + BOOTSTRAP_PROMPT.md §5 (the state machine + family-transition routing) + SKILL_ROUTING.md to confirm the post-§9-family-close transition routes to `superpowers:brainstorming`.
2. Confirms baseline GREEN at master tip (`go build ./... && go vet ./... && golangci-lint run && go test -race -short ./...` + differential 41/41 + h2spec 53/53 + proxy-wasm conformance 10/10).
3. Invokes `superpowers:brainstorming` to scope the Network-filters family + select + brainstorm the first phase. (Per ADR-0045, the family/phase may pre-split planner-time.) NO worktree/code changes at the brainstorm stage — brainstorm authors the family BRAINSTORM doc per the phase-25 parent-BRAINSTORM precedent.

## Project state at master tip (`24c8fe1`)

- **Master tip:** `24c8fe1` (`phase 25.3 IMPL stage-close: STATE.md + PROGRESS.md SHA-fill (TBD-25.3-IMPL-SQUASH -> 57c7c4d)`); predecessor `57c7c4d` (the 25.3 IMPL squash-merge — all 15 tasks). origin/master at `24c8fe1`. Run `git log --oneline -1` to confirm the actual tip (a subsequent docs-only repoint may have advanced it).
- **§9 HTTP-filters family CLOSED:** 20 production HTTP filters wired (phases 7.1 / 9-24 / 25.1 / 25.2 / 25.3). `envoy.filters.http.wasm` (phase 25, 3 sub-phases) was the final row.
- **Counts at master tip:** stat surface **132**; differential fixtures **41**; fuzzers **35**; capability keys 58; conformance families **10/10** (NEW `test/conformance/proxy-wasm/` in-process harness); ADR tail **ADR-0212**, next-free **ADR-0213** (ADR-0209 + ADR-0213 escape-valve reserves UNCONSUMED).
- **Toolchain:** Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); reference Envoy v1.37.2 (ADR-0008); h2spec 53/53 at the ADR-0051 v1.32.4 pin; Rust + `wasm32-wasip1` installed (for any future vendored-bytecode fixtures).
- **Six gates GREEN at master tip** (the 25.3 IMPL phase-done evidence; 25.3 landing is the squash + a docs-only SHA-fill).

## Phase 25.3 IMPL hand-off notes (settled; DO NOT reopen)

The 25.3 IMPL (per-route + multi-plugin vm_id-shared registry + FAIL_RELOAD reload + env_vars + phase-21 clock migration + conformance-harness seed) is DONE + CANONICAL. Notable outcomes recorded in `docs/envoy-go/phases/25.3-http-filter-wasm-perroute-and-conformance/REVIEW.md`:
- **The differential surfaced + we fixed 4 production bugs unit tests had masked** (frozen-registry per-route stats → `NewCounterIfAbsent`; guest-trap `Close` cascade → `StreamContext.trapped` guard; reload triplet; `ReloadDispatch`-before-context-create ordering). A reminder that the differential + deliberate-break discipline catches integration bugs unit tests miss.
- Empirical: reference Envoy v1.37.2 has NO per-route wasm support → envoy-go's per-route wholesale-`Wasm` override is surfaced subject-only in the differential (fixture 0038).
- Carried debts (NOT §9; do not block the next family): Q5 debt #2 (phase-22.2 lua filterstate storage migration) + debt #3 still deferred; the 6 DEFERRED proxy-wasm conformance families (shared_queue, signature_util, wasm[TLS-cache], vm_id_handle, null_vm, fuzz — forward-pointers in BOOTSTRAP §7.3 + ENVOY_TARGET.md); the `env_vars_cap_exceeded` counter is allocate-only at boot-PARSE-REJECT (documented departure); a minor test-isolation note on `TestFilter_RootVM_SharedAcrossStreams_NoCrossStreamLeak`.

## Read first (in order)

1. THIS FILE — the §9-family-close family-transition cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 25.3 IMPL done; §9 HTTP-filters family CLOSED`; next-skill `superpowers:brainstorming`; next-free `ADR-0213`; last-commit `57c7c4d` (SHA-fill `24c8fe1`).
3. `docs/envoy-go/ROADMAP.md` — `## Feature Families (09+)`: `### HTTP filters family` (CLOSED) → `### Network filters family` (the next family; candidate filters listed). Row 25 + sub-rows 25.1/25.2/25.3 all `done`.
4. `BOOTSTRAP_PROMPT.md` §5 (the state machine + how a closed family transitions to the next) + §4.1 STATE-update invariant.
5. `docs/envoy-go/SKILL_ROUTING.md` — confirm the post-family-close → brainstorming routing.
6. `docs/envoy-go/phases/25.3-http-filter-wasm-perroute-and-conformance/REVIEW.md` + `PROGRESS.md` — the closing 25.3 retrospective (the 4 differential-surfaced bugs + carried debts).
7. `docs/envoy-go/DECISIONS.md` — ADR tail ADR-0212; the §9-family ADR lineage (for any cross-family reuse precedent the next family inherits, e.g. ADR-0204 capability sandbox / ADR-0186 clock seam / the filter-chain framework ADRs).
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — all the execution-style + worktree + push + differential-asserter-dispatch + fixture-dispatch-constraint memories apply to the next phase too.

After the Network-filters-family BRAINSTORM is authored + reviewed + committed + squash-merged + pushed, advance STATE.md per BOOTSTRAP §5 and rewrite next-prompt.txt for the next-stage (SPEC) cold-start.
