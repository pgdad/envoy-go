START a fresh 24.2 IMPL cold-start session for envoy-go.

This is a fresh-session prompt for the **24.2 IMPL** of phase 24.2 (`global-ratelimit-perroute-and-headers`, the REMAINING-surface slice of `envoy.filters.http.ratelimit` — the GLOBAL rate-limit filter). The **24.2 PLAN is COMPLETE + squash-merged to master + pushed to origin** (plan-document-reviewer-APPROVED on the first iteration; 9 bite-sized TDD tasks: Pre-Task 0 + Tasks 1-8). STATE points at **24.2**, lifecycle-state 3 (PLAN.md exists; implementation incomplete). The natural next-skill per STATE.md + SKILL_ROUTING state 3 is **`superpowers:subagent-driven-development`** (per project memory `feedback_execution_style.md` — the user always prefers subagent-driven over inline execution) to execute `docs/envoy-go/phases/24.2-global-ratelimit-perroute-and-headers/PLAN.md` over fresh `general-purpose` subagents with two-stage review between Tasks.

## What this session must do: execute the 24.2 PLAN

Per SKILL_ROUTING state 3, run `superpowers:subagent-driven-development`: the PLAN.md (already authored at the 24.2 PLAN-stage-close commit + plan-document-reviewer-APPROVED) is the authoritative input. Each of the 9 Tasks (Pre-Task 0 + Tasks 1-8) maps 1:1 to a fresh `general-purpose` subagent dispatch envelope (the PLAN's "Subagent dispatch outline" sections); each Task runs a `superpowers:test-driven-development` red→green→commit cycle; PROGRESS.md gets one entry per Task per the D-P3 discipline. The IMPL session runs to completion (Pre-Task 0 → Tasks 1 → 2 → 3 → 4 → 5 → 6 → 7 → 8 in order — Tasks 1 + 3 are file-disjoint and CAN parallelize but D-P2 runs sequential for two-stage review).

1. **Create a fresh IMPL worktree** per `feedback_git_worktrees.md` (branch `phase-24.2-global-ratelimit-perroute-and-headers-impl`, off master tip — run `git log --oneline -1` to confirm the actual tip SHA, which is THIS PLAN-stage-close SHA-fill follow-up).
2. **Run Pre-Task 0** verbatim per the PLAN's `## Pre-Task 0` section — author PROGRESS.md preamble + verify all 12 preconditions (verbatim output capture per D-P3); record the 2-ADR landing table + the conditional ADR-0202 escape-valve mapping + D-RL8..D-RL18 + D-P1..D-P3 verbatim.
3. **Dispatch Tasks 1 → 8 sequentially** per the PLAN's `## Task graph` + per-Task dispatch outlines. Each Task subagent dispatch:
   - inherits a self-contained context (the PLAN entry alone is sufficient; the subagent does NOT need the parent SPEC or this prompt);
   - follows the PLAN's TDD step sequence (write failing test → verify FAIL → implement → verify PASS → verify gates + append PROGRESS + commit);
   - appends one PROGRESS.md entry per Task per the D-P3 discipline (quote the six-gate-relevant command outputs verbatim);
   - the two-stage review between Tasks (per `superpowers:subagent-driven-development`) catches deviations early.
4. **Carry forward the ADR-0202 escape valve** wired to TWO D-hypothesis surfaces:
   - **Task 1 — `metadata`-action dynamic-metadata accessor (parent §12 item 2 + D-RL8).** PLAN hypothesis: the existing `streamInfo().DynamicMetadata()` accessor satisfies DYNAMIC=0; ROUTE_ENTRY=1 may need a NEW `RouteMetadata()` `DecoderFilterCallbacks` accessor (ADR-0165 set-once-by-dispatch extension). If neither path fits cleanly, fire ADR-0202 (§Context + §Decision + §Consequences at the Task-1 commit per ADR-0044). Otherwise: ADR-0202 stays UNCONSUMED.
   - **Task 5 — X-RateLimit DRAFT_VERSION_03 byte format (parent §12 item 5 + D-RL9).** PLAN hypothesis: reproduce the upstream `ratelimit_headers.cc:13-65` byte format verbatim (MIN selection by `limit_remaining`; `;w=`/`;name=` quota-policy suffix per descriptor with non-zero window; unit→seconds map; tie-breaker = first equal-minimum status wins per insertion order). Byte-pinned at `headers_test.go` against captured upstream headers (side-by-side capture from reference Envoy v1.37.2 against the same shared fake `RateLimitService` — the 24.1-landed `test/helpers/ratelimitgrpc/`). If the byte format diverges (e.g., `;name=` quoting / fractional rounding on `duration_until_reset.seconds` / tie-breakers on equal `limit_remaining`), fire ADR-0202. Otherwise: ADR-0202 stays UNCONSUMED.

   PLAN hypothesis (per the parent §10-C D-style hypothesis, re-mapped post-24.1): ADR-0202 stays **UNCONSUMED at phase-24 phase-done**. The 24.1 DELTA-2 chain-seed surface (the predicted firing site at parent §12 item 1) ALREADY resolved as RAW-PROTO SEED CONFIRMED without firing the escape valve. The remaining surfaces are smaller — D-RL8 reuses an existing accessor pattern; D-RL9 reproduces a stable upstream byte format. Hypothesis: both hold.
5. **Anchor the 2 ADR landings at their Tasks** per ADR-0044 (§Context already drafted at the parent SPEC commit; §Decision + §Consequences land at IMPL):
   - **ADR-0199 (FULL §Decision + §Consequences)** + the **ADR-0125 §(xv) AMENDMENT 9 → 10 paragraph anchored in ADR-0199 §Consequences** — lands at **Task 3** (`compiled_perroute.go` per-route compile). RE-AMENDS the canonical-per-route roster after phase-23's REUSE-by-absence skip + phase-24.1's roster-defer; ends the 2-row deferral streak.
   - **ADR-0197 IN-PLACE §Decision amendment** (X-RateLimit DRAFT_VERSION_03 + remaining-actions slice; per ADR-0052 in-place edit discipline) — lands at **Task 5** (`encode.go` + `headers.go`). The 24.1-landed ADR-0197[core] §Decision body stays unchanged; this Task EXTENDS it with the X-RateLimit + remaining-actions slice paragraph.
6. **Atomic landing at Task 8** (the phase-09..23 + 24.1 precedent atomic-landing pattern):
   - Run the 6 phase-done gates A/B/C/D/E/F capturing verbatim outputs.
   - Land the BEHAVIOR_CONTRACT.md completion bundle (D-RL17) — extends the existing 24.1 `### envoy.filters.http.ratelimit` subsection in-place (engine completion to all 10 actions + `metadata` accessor disposition + §4.4 stage multi-bucket discipline + §4.3 Axis-B `vh_rate_limits` decision table + X-RateLimit DRAFT_VERSION_03 emission discipline + per-route `RateLimitPerRoute.domain` override + `RateLimitPerRoute.override_option` accepted-but-INERT departure note); per-route canonical-patterns cross-reference caption "through phase 24.1" → "through phase 24" + the §(xv) AMENDMENT paragraph; response-header allow-list adds `x-ratelimit-limit`/`-remaining`/`-reset` (set-equal byte-exact per scenario g); stat-name mapping adds a per-route `domain`-qualifier paragraph (stays 114). NO new departure records (the 3 from 24.1 cover the surface).
   - Flip ROADMAP **row 24.2 `planned → done` AND parent row 24 `in-progress → done` in the SAME commit** per the 18/19/22 ROLLUP precedent (D-RL18) — the commit-message body names BOTH transitions for grep-verifiability (`"phase 24.2: ... [ADR-0199, ADR-0197-amend, ADR-0125 §(xv)] — also closes parent row 24 [ROLLUP per 18/19/22 precedent]"`).
   - Re-advance STATE.md to lifecycle-state 6 → next-phase-or-awaiting-next-planning. The §9 HTTP-filters family closes to **1 remaining row: `wasm`** at this commit; STATE points at the next §9 family member if next-due, else `"awaiting next planning"`.
   - Author REVIEW.md per `superpowers:requesting-code-review` verifying the FULL parent §15 acceptance UNION (closing 24.1 partial items 7/9/15/16 + new items 12/14/17/18); quote the six-gate verbatim outputs.
7. **Squash-merge the 24.2 IMPL** to master per the phase-09..23 IMPL-stage-close pattern + push to origin per `feedback_push_to_origin.md`. Re-advance STATE.md `last-commit` from the `TBD-24.2-IMPL-SQUASH` placeholder to the actual squash SHA via a follow-up commit (the 24.1 analogue was `dbd2d3b`).

## Pre-existing settled context (DO NOT re-litigate)

- **The parent SPEC empirical AMEND-1..11 catalog is SETTLED** (parent SPEC §1.1) — the implementation contract. Do NOT re-derive. Load-bearing AMENDs the IMPL must carry forward: AMEND-4 inert `override_option` (`RateLimitPerRoute`); AMEND-5 Axis-B `vh_rate_limits` decision table; AMEND-7 `apply_on_stream_done` honor-as-absent; AMEND-8 X-RateLimit + reply byte-pin; AMEND-11 key defaults (`query_param` SINGULAR for `query_parameters`).
- **24.1 IMPL is DONE** (squash-merged at `a4fdc75` + SHA-fill `dbd2d3b`) — the FOUNDATIONS for 24.2 are in place: the `internal/filter/http/ratelimit/` package (8 production files + 6 test files) + DELTA-1 `RateLimitClient` + DELTA-2 HCM route-table exposure (`RouteRateLimits()`/`VirtualHostRateLimits()` `DecoderFilterCallbacks` accessor pair via the ADR-0165 set-once-by-dispatch primitive) + the 5 CORE actions in `descriptors.go` (the `actionUnsupportedAt241()` arm is replaced at Task 1) + OK/OVER_LIMIT/error dispositions (X-RateLimit STUB at 24.1 dispositions.go is replaced at Task 5) + cluster-scoped stats + 19 HTTP filters wired + the §5 PARSE-REJECT roster (incl. the 3 envoy-go-strict departures `disable_key`/`extension`/`dynamic_metadata`) + `0032`/`0033` fixture directories + `FuzzRateLimitConfigParse` corpus + 3 ADR landings (0197[core]/0198/0200). The 24.2 IMPL EXTENDS this surface; it does NOT re-land.
- **D-RL1 byte-confirmation RESOLVED** at 24.1 Task 5: RAW-PROTO SEED CONFIRMED; ADR-0202 UNCONSUMED at 24.1 phase-done. The 24.2 IMPL carries the escape valve forward to the remaining-risk surfaces (D-RL8 `metadata`-accessor at Task 1 + D-RL9 X-RateLimit byte-edge at Task 5).
- **24.2 scope (do NOT pull 24.1 surface in, do NOT push 24.3 forward):** the remaining 5 actions + `stage` multi-stage path + Axis-B `vh_rate_limits` table (full INCLUDE/IGNORE/OVERRIDE + legacy force-include) + X-RateLimit DRAFT_VERSION_03 emission + `RateLimitPerRoute` 10th-canonical + ADR-0125 §(xv) 9→10 amendment + scenarios (f)/(g) added to existing `0032` + (d) extended to all 10 actions. **NO new fixture dir** (stays 35) + **NO new fuzzer** (stays 33). NO new top-level §9 row (the family closes to 1 remaining row `wasm` AT the 24.2 phase-done commit; phase 24 is the last §9 row before `wasm`).

## Base + invariants at master tip

- **Master tip:** `350543b` (`next-prompt.txt: repoint master-tip references to 4fd4c14 (actual HEAD)`; docs-only). Predecessors: `4fd4c14` (`phase 24.2 PLAN stage-close: STATE.md SHA-fill (TBD-24.2-PLAN-SQUASH -> 81bc2be)`; docs-only); `81bc2be` (`Squash merge phase-24.2-global-ratelimit-perroute-and-headers-plan [ADR-0199 anchored @ Task 3, ADR-0197 amend @ Task 5, ADR-0125 §(xv) @ Task 3]`; the 24.2 PLAN squash collapsing PLAN.md authoring + STATE.md re-advance + ROADMAP.md row 24.2 per-cell PLAN-done annotation + next-prompt.txt rewrite, 4 files / +514 / -53 LoC); `1d3293b` / `349b7a1` / `070eb26` (`next-prompt.txt: repoint master-tip references` chain; docs-only); `dbd2d3b` (`phase 24.1 IMPL stage-close: STATE.md SHA-fill (TBD-24.1-IMPL-SQUASH -> a4fdc75)`; docs-only); `a4fdc75` (24.1 IMPL squash; 61 files / +11041 / -25 LoC). Run `git log --oneline -1` to confirm the actual tip SHA for the IMPL worktree base (this `next-prompt.txt`-repoint commit may itself have advanced the tip past `350543b`). Pushed to origin per `feedback_push_to_origin.md`.
- **Baseline at master tip:** **35 differential fixture directories** GREEN (0000-0033; lua 0026-0029 + multi-listener combined runs may hit the documented `freeTCPPort` flake per 22.2 REVIEW §7.4 — re-run in isolation); **33 fuzzers** green (`FuzzRateLimitConfigParse` clean at 30s); h2spec 53/53 at the ADR-0051 v1.32.4 pin; build/vet/lint/race clean; **19 HTTP filters wired** (oauth2 ↔ ratelimit ↔ rbac alphabetical); **114 stat names** (incl. the 4 cluster-scoped `cluster.<rls>.ratelimit.{ok,error,over_limit,failure_mode_allowed}` from 24.1); **18 envoy-go-strict departure records** (incl. the 3 ratelimit-strict 24.1 additions). DECISIONS.md tail at **ADR-0201**; ADR-0197[core]/0198/0200 full bodies landed; ADR-0199 §Context-anchored only (§Decision + §Consequences land at 24.2 IMPL Task 3); **next-free ADR-0202** (UNCONSUMED, escape-valve reserve for Task 1 D-RL8 OR Task 5 D-RL9).
- **Canonical-per-route roster STAYS 9 at this IMPL cold-start** (the 9→10 amendment is ANCHORED in ADR-0199 §Context but LANDS at 24.2 IMPL Task 3 — the §(xv) AMENDMENT paragraph edits ADR-0125 in-place per ADR-0044 + the 22.1→22.3 anticipation→landing precedent).
- **24.2 PLAN at master:** `docs/envoy-go/phases/24.2-global-ratelimit-perroute-and-headers/PLAN.md` (455 LoC; 9 tasks; the authoritative input to this IMPL session). Read it first.
- Project memories ALL apply: `feedback_git_worktrees.md`, `feedback_push_to_origin.md`, `feedback_execution_style.md`, `reference_differential_fixture_dispatch_constraint.md`, `reference_differential_asserter_dispatch.md` (scenarios (f)/(g)/(d-ext) at Task 6 use the EXISTING `0032` cross-side `CompareBytes` runner branch per the fixture-dispatch constraint; the existing 24.1 scenario (h) `stat_surface` REMAINS on `StatsAsserter` — Task 6 does NOT change its dispatch).

## Read first (in order)

1. THIS FILE — the 24.2 IMPL cold-start + the subagent-driven-development obligations.
2. `docs/envoy-go/phases/24.2-global-ratelimit-perroute-and-headers/PLAN.md` — the authoritative 9-task PLAN. THE primary input.
3. `docs/envoy-go/phases/24.2-global-ratelimit-perroute-and-headers/SPEC.md` — the 24.2 scope (referenced by the PLAN's Task entries).
4. `docs/envoy-go/phases/24-http-filter-global-ratelimit/SPEC.md` — the RETAINED parent master SPEC (§1.1 AMEND catalog; §4.1 all-10-actions table; §4.3 Axis-B; §4.4 stage; §4.7 X-RateLimit byte-pin; §5.3 `RateLimitPerRoute`; §6 code shapes; §10 ADR map; §11 D1–D7; §12 byte-confirmations; §13 BEHAVIOR_CONTRACT; §14 testing; §15 acceptance).
5. `docs/envoy-go/phases/24.1-global-ratelimit-core-and-route-table/REVIEW.md` — the 24.1 IMPL review (SHIP recommendation; the post-24.1 baseline summary; cross-references to the landed ADR bodies; the Task-7 + Task-10 follow-up fix notes; the items 7/9/15/16 partial-annotations that 24.2 closes).
6. `docs/envoy-go/phases/24.1-global-ratelimit-core-and-route-table/PROGRESS.md` — the 24.1 IMPL Task-by-Task trail (the precedent for the 24.2 PROGRESS.md structure; the actual landed file sizes + commit SHAs for the 14 IMPL commits).
7. `docs/envoy-go/STATE.md` — active-phase 24.2; lifecycle-state 3; next-skill `superpowers:subagent-driven-development`; next-free ADR-0202.
8. `docs/envoy-go/ROADMAP.md` — row 24 parent (`in-progress`; closes at 24.2 phase-done [ROLLUP]); row 24.1 (`done` per the 24.1 IMPL squash); row 24.2 (`planned` with the PLAN-done annotation; opens this session).
9. `docs/envoy-go/SKILL_ROUTING.md` — confirm state 3 (PLAN.md exists, implementation incomplete → subagent-driven-development).
10. `docs/envoy-go/DECISIONS.md` tail — ADR-0197..0201 (0197[core]+0198+0200+0201 full bodies; 0199 §Context-only — Task 3 lands FULL).
11. `BOOTSTRAP_PROMPT.md` (repo root) — §4.1 STATE-update invariant + §5 state machine (state 3 → subagent-driven-development OR executing-plans → IMPL Tasks → PROGRESS.md per Task → state 4).
12. The nearest sibling sub-phase IMPL precedent: `docs/envoy-go/phases/24.1-global-ratelimit-core-and-route-table/PROGRESS.md` (the immediate sibling — same parent SPEC, same project memory, the exact PROGRESS structure the 24.2 IMPL should mirror); `docs/envoy-go/phases/22.1-http-filter-lua-vm-and-headers-bridge/PROGRESS.md` (a sibling sub-phase IMPL precedent for shape).
13. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`.
