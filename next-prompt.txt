=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage. To use it to advance the project: read `docs/envoy-go/STATE.md` for the recorded NEXT, perform that one stage (the concrete action is spelled out below), land it, and re-roll this file to the following stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, the active phase is fully landed, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action. While ANY stage remains — as NOW: the **47.2b is PLAN → IMPL** (the remaining two stages of the SECOND-and-FINAL sub-leg of row 47 — its IMPL flips row 47 `done`; the SPEC LANDED this session; NO separate 47.2b BRAINSTORM — BRAINSTORM-47.2 chartered both sub-legs), so tasks are NOT complete — do NOT create `stop`; perform the stage described below.
==============================================

RESUME a fresh session for envoy-go (router last updated 2026-06-29; the **phase 47.2b (`emit_tags_as_labels`) SPEC is DONE and LANDED** — docs-only direct-on-master; spec-document-reviewer APPROVED [all code line citations EXACT against HEAD — `bootstrap.go` reject `:454`/`StatsSinkConfig` `:273`/kept-rejects `:413`/`:421`/`:445`/`:449`/`:451`/`:458`; `name.go` `flattenToProm` `:39`/SN-keys `:50`/`:60`/`:82`/SN4 `:346`; `mapping.go` `:22`/`:26`/`:35`; `sink.go` `:64`/`:68`/`:82`/`:110`; `main.go` `:194`/`:199`; all 7 live-probe ground-truth facts correctly encoded; only 2 MINOR polish items, both folded]). The **§11 D-MS-LABEL{,-KEY,-MULTI,-GAUGE,-VALUE} + D-MS-STATS-FINAL-2 + D-MS-REJECT-2 pins were EXECUTED IN-SESSION** live against `envoyproxy/envoy:contrib-v1.37.2` per `reference_docker_probe_bridge_network` (a shared bridge `dmsnet`; an `http-echo` backend + a driver-owned Go label-dumping h2c `MetricsService` receiver `host.docker.internal:4319`; `emit_tags_as_labels:true`; K=7; **decode-ran proof: 90+ `StreamMetricsMessage`s, 5852 label-bearing family lines**). **The DECISIVE D-MS-LABEL finding** (the genuine brainstorm unknown, now pinned): under `emit_tags_as_labels:true` the reference applies its FULL tag-extractor set — the family `name` is the INTERNAL dotted name with the tag VALUE removed + dots collapsed, **KEEPING DOTS** (`cluster.<n>.upstream_rq_total` → `name="cluster.upstream_rq_total"`, NOT the Prometheus `envoy_`-flattened base); the `LabelPair.name` is the Envoy **DOTTED** tag-name `envoy.<tag>` (`envoy.cluster_name`, `envoy.http_conn_manager_prefix`, `envoy.listener_address`, `envoy.response_code_class` — only the leading `envoy_` → `envoy.`, the rest underscored); a single name yields MULTIPLE labels with `_Nxx → _xx` collapse (`downstream_rq_2xx` → `name="http.downstream_rq_xx"` + `{prefix, response_code_class="2"}`); labels apply to BOTH Counter AND Gauge (CONTRAST the 47.2a Counter-only deltas); and the VALUE stays CUMULATIVE-ABSOLUTE (NOT deltas — so `0091` uses 0089's `value==K` last-seen model, NO delta-SUM barrier). The reference BOOTS the knob clean (reference-parity-accept); ZERO self-stats. **ROW 47 STAYS `in-progress`** (flips `done` at the 47.2b IMPL — the FINAL sub-leg — per ADR-0106 + `reference_roadmap_split_phase_row_done`). The next action is the **47.2b (`emit_tags_as_labels`) PLAN** — the TDD spine for the FINAL sub-leg.

HEAD will be the phase-47.2b SPEC docs-only commit atop the phase-47.2a IMPL squash `ae62d964`. **Re-read the shas with `git log --oneline -6`, do NOT assume.** Confirm `git status` clean (the untracked `.claude/` is session cruft, leave it) + master == origin/master.

## What the 47.2b PLAN IS (the next stage — docs-only direct-on-master)
Author `docs/envoy-go/phases/47-stats-sink-metrics-service/PLAN-47.2b.md` — the TDD spine (the 9-task PLAN-47.2a shape) realizing SPEC-47.2b. **Re-read `SPEC-47.2b.md` first** (it carries the full §11 live-probe pin table + the §12 D-questions the PLAN must resolve + the §10 per-task sketch). The spine:
- **PROGRESS + baselines + the FINAL ADR-0045 re-check (D-MS-SPLIT-2 — no further split anticipated; §3.0).**
- **The bootstrap reject-lift + field** [TDD]: lift `bootstrap.go:454` `emit_tags_as_labels:true` reject → a parse-accept recording a NEW `StatsSinkConfig.EmitTagsAsLabels bool` (`:273`; **scalar `bool` via `.GetEmitTagsAsLabels()` — NOT a `*wrapperspb.BoolValue`**, CONTRAST the 47.2a `report_counters_as_deltas` `.GetValue()`); the append at `:460`+ gains `EmitTagsAsLabels: msc.GetEmitTagsAsLabels()`. Replace the formerly-rejecting unit test with a parse-accepts test; KEEP every OTHER reject strict (`:413`/`:421`/`:445`/`:449`/`:451`/`:458`).
- **The sink-owned `labelMapper` transform** [TDD table-driven against the §11 pinned forms, full-package `-race`]: resolve **D-MS-LABEL-REUSE** (refactor `name.go` to a shared `(residualDottedName, []tagPair{dottedKey,value})` extraction core consumed by BOTH `flattenToProm` [underscore/`envoy_`-prefix projection] AND the statssink label mapping [dotted projection] — DRY but touches the hot prom path, guarded by the full prom differential — VS a parallel extractor in `internal/statssink`). The transform: from each family's internal dotted `Name()`, extract via the SN-rules → `(residual, []LabelPair)`; rewrite `Name` to the residual + set `Metric.Label`; the key projection is `"envoy." + strings.TrimPrefix(snKey, "envoy_")`; BOTH `mapping.go:26`/`:35` arms (Counter AND Gauge); the SN4 `_Nxx→_xx` multi-tag layered on the residual. Resolve **D-MSL-PLACEMENT**: a `labels *labelMapper` field on `MetricsServiceSink` (non-nil ⇒ knob), applied in `Submit` PARALLEL to the 47.2a `deltaState.apply` (`sink.go:110`–`:111`) — **HARD CONSTRAINT: NO in-place mutation of the shared `flusher.go:47` snapshot slice; build the sink's OWN families**; the deltas+labels compose order is delta-then-labels (orthogonal — delta on the Counter VALUE, labels on the NAME+LABEL).
- **The main thread + value/byte-stability guard**: thread `cfg.EmitTagsAsLabels` into `cmd/envoy-go/main.go:199` (`NewMetricsServiceSink` gains a 4th `bool` arg); false ⇒ 47.1/47.2a-identical; a deltas+labels both-true compose smoke.
- **The receiver label-aware surface** [TDD]: resolve **D-MSL-RECEIVER-KEY** — extend `test/helpers/metricsservice` with a `{name, sorted-labels}`-keyed accumulator + a `FamilyWithLabels(name, labels)` poll surface (multiple families share a residual name — `cluster.upstream_rq_total` for both `c_backend` AND `c_metrics`); ADDITIVE (the existing `Family`/`FamilySum`/`Messages` STAY for 0089/0090 non-regress — those families have empty labels).
- **The `0091-stats-sink-metrics-service-labels` fixture**: clone the AUTHORITATIVE `0090/driver/driver.go` (two per-side receivers + hard `Close()`), DROP the `0090` delta-SUM `Messages()` barrier (cumulative value model), SWAP to the keyed `{residual-name, sorted LabelPair[]}` split + `value==K`; the deterministic subset = `{cluster.<c_backend>.upstream_rq_total → name="cluster.upstream_rq_total" + {envoy.cluster_name}, http.<hcm_local>.downstream_rq_total → name="http.downstream_rq_total" + {envoy.http_conn_manager_prefix}, http.<hcm_local>.downstream_rq_2xx → name="http.downstream_rq_xx" + {envoy.http_conn_manager_prefix, envoy.response_code_class="2"}}` (D-MSL-SUBSET-2XX RECOMMENDS including the 2xx two-label split — envoy-go's SN4 `name.go:346` reproduces it; fall back to the two single-label splits if a residual mismatch surfaces at IMPL). **REGISTER `0091` in `test/differential/runner_test.go`** (add the import sibling at `:118` after `:117` — the C1 vacuous-green guard from the 47.2a PLAN). The deliberate breaks (subject-side; `-count=1`; `-run 'TestDifferential/0091'` NEVER bare): (a) emit the full inlined 47.1 name with NO label split → keyed lookup misses; (b) emit the Prometheus UNDERSCORE key `envoy_cluster_name` → LabelPair name mismatches; (c) emit the Prometheus FLATTENED name → family name mismatches; (d) drop the SN4 response-code-class label on `downstream_rq_2xx`. (a)/(b) are load-bearing (they prove the differential SEES the label split — the 47.1 no-labels inlining does NOT satisfy it).
- **The full 93-dir differential + six-gate + ADR-0264 body + BEHAVIOR_CONTRACT + STATE/ROADMAP**. ROADMAP row 47 **FLIPS `done`** at the 47.2b IMPL (NOT at the PLAN).

plan-document-reviewer pass before landing (the 47.2a PLAN precedent — it caught the C1 missing-registration vacuous-green guard). Docs-only direct-on-master. After it lands, ROLL THIS FILE to the **47.2b IMPL**.

## What the 47.2b IMPL is (the stage AFTER the PLAN — FLIPS ROW 47 `done`)
- **47.2b IMPL** = subagent-driven (`feedback_execution_style` · `superpowers:subagent-driven-development`) in a FRESH worktree off master (`feedback_git_worktrees`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit + cleans leak files + re-runs the six-gate + the FULL-package `-race` + does the `0091` deliberate breaks ITSELF (`-count=1`; `-run 'TestDifferential/0091'` NEVER bare) + squashes + pushes at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). **47.2b IMPL FLIPS ROW 47 `done`** (per ADR-0106 + `reference_roadmap_split_phase_row_done`) — the Observability family STAYS OPEN. After the 47.2b IMPL lands, **CHECK THE ROADMAP for whether any chartered row remains** (statsd / dog_statsd / OTLP metrics / the tap filter are noted as future Observability rows but confirm whether any is chartered/`pending` vs merely mentioned) — if a next row/phase is chartered, roll the router to its BRAINSTORM; if NONE remains and STATE.md's NEXT is empty, the TERMINATION SENTINEL applies (create `stop`).

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** The next stage is the 47.2b PLAN (lifecycle-state 2 → 3 for the 47.2b sub-leg). After it lands, ROLL THIS FILE to the 47.2b IMPL.
- **SPEC/PLAN = docs-only direct-on-master** (the family-row precedent); the IMPL is subagent-driven in a worktree.
- **Process anchors:** ADR-0106 (per-leg rows — row 47 flips `done` at the 47.2b IMPL) · ADR-0045 (sub-split — 47.2b is the final no-split sub-leg; D-MS-SPLIT-2 re-check at PLAN) · ADR-0044 (ADR §Decision+§Consequences-at-IMPL — ADR-0264 §Context DRAFTED at the 47.2b SPEC §13, body at the 47.2b IMPL) · ADR-0080 (strict-reject — 47.2b LIFTED the `emit_tags_as_labels` reject [`bootstrap.go:454`], KEEPS the rest) · ADR-0061 (the SN1–SN9 Prometheus name/label rules — the tag-extraction substrate the label mapping REUSES) · ADR-0064 (the `stats_sinks[]` silent-ignore — 47.1 LIFTED the metrics_service sink) · ADR-0059 (the in-tree `Registry`/`Walk`/`Freeze`) · ADR-0060 (histograms deferred — moot) · ADR-0262 (the 47.1 anchor) · ADR-0263 (the 47.2a deltas anchor) · ADR-0264 (ANCHORED at the 47.2b SPEC §13, body at the 47.2b IMPL).
- **Differential discipline** (for the 47.2b `0091` fixture): clone the AUTHORITATIVE `0090`/`0089` driver (two per-side receivers + hard `Close()` — `reference_periodic_sink_differential_two_receivers`); **a label sink differential needs a break the burst-snapshot can SEE** — assert the emitted `LabelPair` SET (a property the 47.1 absolute-name-inlining does NOT satisfy); `0091` is CUMULATIVE-value (NO delta-SUM stability barrier — `reference_delta_sink_differential_stability_barrier` is a 47.2a/`0090`-only concern); **REGISTER the driver in `test/differential/runner_test.go`** (else vacuous green — `reference_differential_run_selector`); `-run 'TestDifferential/0091'` NEVER bare; `-count=1` on every break + `-race` (`reference_differential_break_protocol_count1`); the Docker bridge + decode-ran proof (`reference_docker_probe_bridge_network`); the gRPC metrics receiver is driver-owned NOT a BackendKind (`reference_differential_grpc_receiver_driver_owned`); assert the aggregated `{name,label-set,value}` payload NOT framing, label set ordering NORMALIZED (`reference_streaming_sink_differential_framing`); the live reference is the wire truth (`reference_wire_format_both_sides_see_same_bytes`); one fixture dir = ONE runner branch (`reference_differential_fixture_dispatch_constraint`); the label mapper + delta map + writer goroutine + ticker are background mutators ⇒ FULL-package `-race` (`reference_full_suite_race_after_background_mutator`); the full-suite `subject ready: EOF` startup-race flake is isolate-re-run + full-re-run, NOT a regression (`reference_differential_fullsuite_startup_flake`).

## Read first (in order)
1. THIS FILE — the cold-start router (the 47.2b SPEC is DONE + LANDED; row 47 `in-progress`; the next action is the 47.2b PLAN).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 47.2b SPEC done`) + the recorded NEXT (the 47.2b PLAN) + the counts (1200 / 92 / 50 / 38 / ADR-0263).
3. `docs/envoy-go/phases/47-stats-sink-metrics-service/SPEC-47.2b.md` — the 47.2b charter (the §11 live-probe pin table + the §12 D-questions [D-MS-LABEL-REUSE / D-MSL-PLACEMENT / D-MSL-RECEIVER-KEY / D-MSL-SUBSET-2XX / D-MSL-BOTH-KNOBS / D-MS-SPLIT-2] the PLAN resolves + the §10 per-task sketch + the ADR-0264 §Context draft §13).
4. `docs/envoy-go/phases/47-stats-sink-metrics-service/PLAN-47.2a.md` — the 9-task TDD spine SHAPE to mirror (the reject-lift + `StatsSinkConfig` bool + the sink transform + the main thread + the `009N` differential + the receiver surface + breaks/flake/full-package-race + the ADR body).
5. `docs/envoy-go/DECISIONS.md` — **ADR-0263** (the 47.2a anchor; ADR-0264 body lands at the 47.2b IMPL).
6. The as-built code 47.2b modifies: `internal/bootstrap/bootstrap.go` (the `emit_tags_as_labels` reject `:454`–`:455` to LIFT; `StatsSinkConfig` `:273` [`ReportCountersAsDeltas` `:279`]; the append `:460`+), `internal/statssink/mapping.go` (`snapshot` `:22`; the Counter `:26`/Gauge `:35` arms — BOTH get labels), `internal/statssink/delta.go` + `internal/statssink/sink.go` (`MetricsServiceSink` `:64`; `delta *deltaState` `:68`; `delta.apply` in `Submit` `:110`–`:111` — the PARALLEL precedent for the `labelMapper`), `internal/stats/name.go` (`flattenToProm` `:39`; the SN-keys `:50`/`:60`/`:82`; the SN4 status-class collapse `:346`–`:348` — the extraction substrate to REUSE/refactor), `cmd/envoy-go/main.go:199` (the sink construction — gains a 4th bool).
7. `test/fixtures/0090-stats-sink-metrics-service-deltas/{driver/driver.go,envoy*.yaml,expectations.yaml}` (the FRESHEST differential template `0091` clones — two per-side receivers + hard `Close()`) + `test/helpers/metricsservice/metricsservice.go` (the receiver to extend with a `{name,sorted-labels}`-keyed `FamilyWithLabels` surface — `s.fams` `:170`/`s.sums` `:171`) + `test/differential/runner_test.go` (`:117` the 0090 import — add the 0091 sibling at `:118`).
8. `docs/envoy-go/ROADMAP.md` — row 47 (`stats-sink-metrics-service`, `in-progress`) + the Observability family-progress note (47.2a deltas DONE; 47.2b tags-as-labels SPEC DONE; the IMPL flips row 47 done).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_subagents_no_push`, `feedback_subagent_autocommit_claudemd`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_detach`/`_path_targeting`, `reference_roadmap_split_phase_row_done`, `reference_stats_sink_metrics_service_probe`, `reference_periodic_sink_differential_two_receivers`, `reference_delta_sink_differential_stability_barrier` [a 47.2a/`0090`-only concern — `0091` is cumulative], `reference_differential_grpc_receiver_driver_owned`, `reference_streaming_sink_differential_framing`, `reference_docker_probe_bridge_network`, `reference_full_suite_race_after_background_mutator`, `reference_differential_break_protocol_count1`, `reference_differential_run_selector`, `reference_differential_fullsuite_startup_flake`, plus the differential-discipline references above).
