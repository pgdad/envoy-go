=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **PHASE 93 (`h2-local-reply-content-length`) IS OPEN AT LIFECYCLE-STATE 3.** Row 93 reads `in-progress` at `ROADMAP.md:155`, and the phase directory holds `BRAINSTORM.md` (**503 lines**), `SPEC.md` (**409 lines**) and `PLAN.md` (**1031 lines**) — no `PROGRESS.md`. ⇒ **THIS SESSION RUNS THE PHASE-93 IMPL (lifecycle-state 3 -> DONE). DO NOT SELF-PICK.**

⚠️⚠️ **THE SENTINEL'S MARGIN IS TWO ONLY BECAUSE THIS ROW IS OPEN — AND THIS IS THE STAGE THAT CLOSES IT.** Check (1) went SILENT at the row-92 flip and was re-armed solely by row 93's insertion. **The moment YOU flip row 93 `done`, check (2) is again the ONLY thing blocking termination.** Do NOT read a two-deep margin as comfort, and do NOT "tidy" a deferred-candidate line — deleting the last one ENDS THE PROJECT. ⚠️ **MEASURE ALL THREE YOURSELF BEFORE DOING ANYTHING ELSE, AND AGAIN AFTER THE FLIP; do not take this file's word for it.**

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE. This form parses FIELDS instead of matching a pattern, and
#     asserts the row DENOMINATOR so a deleted row is itself an error.
#     ⚠️ want IS 125 — row 93 was registered at the phase-93 BRAINSTORM; neither
#     the SPEC nor the PLAN changed ANY row, both verified by sha256 + --numstat.
awk -F'|' -v want=125 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **CHECK (1) PRINTS `NOT DONE: row 93` — ONE line — UNTIL YOU FLIP IT, THEN SILENT.** Run BOTH doctoring NCs EVERY SESSION anyway, because a check whose output you predicted is indistinguishable from a broken one:
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md.
```
⚠️ **NC-A READS **TWO** LINES WHILE ROW 93 IS OPEN** (`NOT DONE: row 62` THEN `NOT DONE: row 93`) **AND COLLAPSES TO ONE THE MOMENT YOU FLIP IT.** ⚠️ **NC-B DOES THE SAME — TWO lines before the flip, ONE (`GATE FAIL: examined 125 … expected 124`) after.** **BOTH SHAPES CHANGE INSIDE YOUR OWN STAGE. Re-measure on BOTH sides of the flip; never inherit.**
```sh
# the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```
Also NC the DENOMINATOR (`want=124` on the real file).

⚠️ **AND WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -c '-family row'` fails (GNU: `grep: amily row: No such file or directory`; ugrep: `option -f: cannot read amily row`) and the surrounding arithmetic prints `0`, **which reads exactly like "no change."** **Always pass `--` before the pattern**, and the counts are form-dependent: with `--`, occurrences read **95** and LINES read **67**. **`grep -c` counts LINES, NOT OCCURRENCES.** ⚠️ **AND `grep -c` PRINTS `0` *AND* EXITS 1, so `$(cmd || echo 0)` EMITS TWO ZEROS** — capture with `v=$(cmd || true)`.

⚠️ **THE MALFORMED-ROW FIGURE RECONCILES ONLY UNDER AN ESCAPE-AWARE FIELD COUNT.** Malformed rows are ids **57** (`NF=9`) and **69** (`NF=10`). ⚠️ **A NAIVE `awk -F'|'` `NF != 8` FORM READS SEVENTEEN.** **Strip `\|` BEFORE splitting.** The two forms **DISAGREE on row 57** but **AGREE on row 69**, so **any gate must state WHICH FORM it uses.** ⚠️ **COUNT ROW 93's FIELDS BEFORE *AND AFTER* YOUR FLIP** — it reads **NF=8 under BOTH forms** today and must still read 8 after.

⚠️ **AND DO NOT SUBSTITUTE A "REASONABLE EQUIVALENT" FOR CHECK (1)'s AWK.** A measurement agent once tried `NF>=8 && $2 ~ /^ *[0-9]+ *$/`: it printed **92**, not the true denominator. **Use the form above verbatim.**

**SENTINEL RUN MECHANICALLY AT THE PHASE-93 PLAN (`ed4716fb`), NOT INHERITED FROM THE SPEC.** ACTUAL output, recorded not predicted: (1) `NOT DONE: row 93` — ONE line at `want=125` · (2) **SIX** at `:203 :209 :215 :225 :231 :239`, per-line md5 BASELINED (`10d7807bf02d 4a92f7e62fc6 2a7eb298b9fd 4ad940205410 b2680e6f4fbf 6caa1c3ce0e7`) **with a doctoring NC on `:225` moving ONLY that digest, proving the comparator discriminates** · (3) SILENT. NC-A **TWO** lines, NC-B **TWO** lines, NC-C fired (`NEVER OPENED: gRPC`, WASM control **2**), NC-D long **5** / short **1** / union **6**.
⇒ **THE SENTINEL DOES NOT FIRE. `stop` WAS EVALUATED AND DELIBERATELY NOT CREATED** (verified absent at the git root and in all four stage worktrees).
⚠️ **RE-CONFIRMED AT THE PUBLISHING COMMIT `90010c4c`, AFTER THE MERGE AND PUSH** — per `reference_cost_figure_measured_at_publishing_commit`, a figure measured only at the pre-commit tip is NOT evidence about the tree a later session actually reads. ALL THREE checks and ALL FOUR NCs were re-run on the pushed tree and every result was **BYTE-IDENTICAL** to the pre-commit run: (1) `NOT DONE: row 93` · (2) SIX at `:203 :209 :215 :225 :231 :239` with the six md5s unchanged (`10d7807bf02d 4a92f7e62fc6 2a7eb298b9fd 4ad940205410 b2680e6f4fbf 6caa1c3ce0e7`) · (3) SILENT; NC-A TWO lines, NC-B TWO lines, NC-C fired with WASM control **2**, NC-D **5 / 1**. **That the PLAN stage moved NO row is therefore MEASURED on both sides of its own commit, not assumed.** ⚠️ **EVERY LIVE FIGURE IN THIS FILE WAS ALSO RE-AUDITED AGAINST THE PUSHED TREE AND ALL TWENTY-ONE REPRODUCED EXACTLY** — ROADMAP 243/125, DECISIONS 18674, `^---$` 216, `^## ADR-` 314, bare `^## ` 322, tail ADR-0315, STATE.md 64, STATE_HISTORY 536, strict 163 / parenthetical 55 / loose 218, phase dirs 134, fixtures 121, fuzzers 56/48, contract 5966, `go.mod` 67 require across 76 lines, `-family row` 95/67, both `PROPOSED` forms 1 and 1, BackendKind tail 38. **The IMPL-facing anchors were re-verified too:** `ROADMAP.md:155` still carries the WRONG `ADR-0085` attribution (2 hits file-wide), `## ADR-0085` is at `:3282` and `## ADR-0155` at `:8187`, `p92WantSubjCLFields = 0` is at `driver.go:1383`, the inverting NC row is at `driver_test.go:85`, `ARITY, NEVER VALUE` is at `driver.go:1377`, README `:147` still reads *"changes every one of them"*, and the seven call sites are at `retry.go:374` + `router_h2.go:{80,128,138,148,231,250}` with the definition at `:294`.
⚠️ **THIS RE-CONFIRMATION DOES NOT CARRY FORWARD — run all three checks and all four NCs ITSELF, at ITS OWN tip, and AGAIN after flipping row 93.**

- ⚠️ **LIVE FIGURES RE-DERIVED AT THIS TIP:** `ROADMAP.md` **243** / **125** data rows / tail row **93** (**BYTE-UNTOUCHED by the PLAN — sha256 `82baac19fa2c1c13…` identical**) · `DECISIONS.md` **18674** · `^---$` **216** · `^## ADR-` **314** · bare `^## ` **322** (314 + 8 `## Amendment`) · tail **ADR-0315** · next-free **ADR-0316** · `STATE.md` **64** · `STATE_HISTORY.md` **536**, strict **163 (DELTA 0)** / parenthetical **55** / loose **218** (**163 + 55 = 218 exactly**) · phase dirs **134** · fixtures **121** (tail `0119`, **`0120` FREE**) · fuzzers **56 targets / 48 FILES** · `BEHAVIOR_CONTRACT.md` **5966** (**BYTE-UNTOUCHED**) · `go.mod` **67 REQUIRE ENTRIES** (18 direct + 49 indirect) across **76 LINES** — ⚠️ **67 IS THE REQUIRE COUNT, NOT THE LINE COUNT; the two were conflated in an earlier router** · BackendKind tail **38** (39 declared) · `-family row` **95 / 67**.
- ⚠️ **THE STAT SURFACE IS QUOTED AS A DELTA AND NEVER AS AN ABSOLUTE.** THREE different absolutes are live in this tree at one tip. Per `reference_a_drift_correction_is_itself_a_claim`, on a contested count: **NO NUMBER.** ⚠️ **AND THE REFUTED STAT-SURFACE FIGURE MUST NOT BE SPELLED, IN EITHER ITS BARE OR ITS ARROW FORM — the phase-93 SPEC wrote a prohibition that QUOTED the token and thereby FALSIFIED ITSELF on its own grep. The PLAN gated its own file and confirmed 0 occurrences.**
- ⚠️ do NOT "fix" the six in check (2) and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases. ⚠️ **Cite ~40 only as "sentence-visible", never as the inventory** — and **no gate may rest on a ` + ` split, in any form.**

==============================================

## STATUS AS OF 2026-08-31: PHASE 93 (`h2-local-reply-content-length`) **OPEN AT STATE 3**. **THIS SESSION RUNS THE PHASE-93 IMPL.**

**What the 93 PLAN landed.** `PLAN.md` created (**1031 lines**), discharging all EIGHT items of `SPEC.md` §14. **ZERO production `.go` bytes.** `ROADMAP.md`, `DECISIONS.md`, `BEHAVIOR_CONTRACT.md`, `go.mod`, `go.sum` **BYTE-UNTOUCHED**. `STATE.md` rolled IN PLACE; `STATE_HISTORY.md` **534 -> 536**, strict guard **DELTA 0**.

### ⚠️ THE CHARTER, UNCHANGED IN SUBSTANCE
**On the HTTP/2 leg a locally generated reply emits a `Content-Length`, ALWAYS.** The fix lands in **`h2LocalReplyHeaders(bodyLen int)`** and **deliberately NOT in `writeH2Reply`**. Four sites pass `0`, three pass `len(bad502Body)`. ⚠️ **The SPEC REVERSED the BRAINSTORM's parameterless-placeholder arm; the PLAN CONFIRMED that reversal and did not re-litigate it.**

### ⚠️ WHAT THE PLAN REFUTED — TWELVE CLAIMS, FIVE OF THEM THE SPEC'S OWN MEASURED FIGURES

1. ⚠️ **SPEC §6.1's `~+60` RECOMPUTE-PIN ESTIMATE IS REFUTED — MEASURED `147` LINES, ~2.45×.** From a compiling, running file whose NC-1 (neutralise the recompute) and **isolating NC-2b** (synthesize-when-absent) both fire, the latter reddening **exactly ONE row — the negative control** — which is what proves that control does independent work. **The file is embedded VERBATIM in `PLAN.md` Task 2. Do not re-derive it.**
2. ⚠️ **SPEC §4's hcm baseline `323 === RUN` IS REFUTED — IT IS `322`.** Measured three times: twice with the fix, once by the controller on the **clean tip**. Both 322 — **the fix touches no hcm test**, which is itself the discriminating observation. Router **129** CONFIRMED exactly. Post-fix denominators: **router 133**, **hcm 327**.
3. ⚠️ **SPEC §6's *"18-row table"* IS NOT REPRODUCIBLE — the `AssertDistribution` table is TEN rows** (`driver_test.go:73-78` + `:82-85`). §8's `10 -> 18` is the correct reading. ⚠️ **The SPEC's underlying CLAIM survives intact** (the table is synthetic; with the fix reverted it stays `ok` while exactly ONE package reddens). **Only the number is wrong — quote NO other number for this table.**
4. ⚠️ **SPEC §8's `~+12/-10` COMBINED PATCH IS REFUTED ON THE ADD SIDE.** The fix ALONE is `+10/-8` — **the SPEC's measured figure CONFIRMED BYTE-EXACT**. But the cheapest honest de-rot is **`+13/-10`** (the corrected clause does not fit the two original comment lines; the reflow costs `+3/-2`), and the shipped variant is **`+15/-10`**.
5. ⚠️ **TWO SPEC "DRIFT-PROOF" ANCHORS ARE NOT UNIQUE.** `val = strconv.Itoa(len(body))` matches **TWO** production lines (`h2dispatch.go:1015` H/2 lowercase-guard, `codec.go:88` H/1 canonical-MIME-guard); `chain.RunEncodeHeaders` matches **THREE** (`connection.go:738` H/1, `h2dispatch.go:650` H/2, `h3dispatch.go:366` H/3 — the H/2 one is uniquely identified by its `&& !hasRespTrailers` tail). ⇒ **a "drift-proof anchor" is itself a CLAIM and must be checked for UNIQUENESS, not just existence.**
6. ⚠️ **`directResponseAction.body()` IS CITED WITHOUT ITS PACKAGE, AND THE PACKAGE IS THE SURPRISING ONE** — `internal/filter/hcm/actions.go`, NOT the router package. The **mirror image** of the SPEC's own `internal/filter/hcm/chain.go` warning (that file does not exist; it is `internal/filter/http/chain.go`). **Do NOT "fix" it toward the router.**
7. ⚠️ **TWO GATES MUST BE RE-BASELINED AND THE SPEC NAMES NEITHER AS SUCH** — `driver.go` **`+1/-1`** (`p92WantSubjCLFields = 0` -> `1`) and `driver_test.go` **`+8/-8`**. SPEC §5.1 says only that the arity pin "stops discriminating"; **MEASURED, it goes RED**, and the differential fails with exactly one error, which is itself proof the byte compare passed.
8. ⚠️ **ONE OF THOSE ROWS IS A NEGATIVE CONTROL THAT MUST *INVERT*, NOT SHIFT.** `driver_test.go:85` `"p92 subj value: 1 where 0 expected"` passes `p92CLObs(1), p92CLObs(1)` with `wantErr: true`. **Once the want is 1 the observation MATCHES, no error is raised, and the row FAILS.** It must become `"0 where 1 expected"` with `p92CLObs(0)` — **rename it too**, or it becomes a lie the next reader trusts.
9. ⚠️ **THE EMPTY-VS-ZERO TRAP IS LIVE ON THIS FIXTURE AND IT IS THE ROW'S OWN SUBJECT.** MEASURED pre-fix on all five arms: the subject reads `clArity=0 declared=<absent>` — **ABSENT, NOT `0`.** ⇒ **`declared` must be a THREE-STATE observation (absent / duplicated / parsed integer), never an `int` with `0` overloaded.** ⚠️ **A DUPLICATE must yield `declaredOK = false`, NEVER "the first value."**
10. ⚠️ **THE FALSIFIED CLAIM LIVES IN MORE FILES THAN ANYONE COUNTED, AND THE COUNT ROSE TWICE INSIDE ONE STAGE.** SPEC §14 item 5 named **one** file; the PLAN's agent found **two**; the controller found **three**. ⇒ **`reference_measured_prototype_is_a_lower_bound` fires a SEVENTH consecutive row, and fired AGAIN on its own agent's enumeration.** **Enumerate by `git grep -l` on the LITERAL CLAIM TEXT.**
11. ⚠️ **SPEC §9.1's ADR-0315 §Context OUTLINE IS STALE** — it lists **NINE** paragraphs; the landed draft carries **TEN**, with a different mapping (landed ¶2 = the citation correction, ¶8 = unit coverage which §9.1 omits, ¶9 = the H/3 composition). **EXTEND FROM THE LANDED TEXT, or you renumber paragraphs that are already correct.**
12. ⚠️ **THE ARCHIVE EVICTION HAD NO UNIQUE OLDEST AT THE PLAN CLOSE.** `STATE.md:52` and `:54` BOTH read `2026-08-25`. The date read alone did NOT resolve it; the tie was broken by **LIFECYCLE PRECEDENCE**. **That entry also sat last for the THIRD close running — the coincidence the router warned about, which is exactly why position must NOT be adopted.**

### ⚠️ WHAT THE PLAN CONFIRMED BY EXECUTION — DO NOT RE-LITIGATE

- ⚠️ **`p93WantRefBodyLen = 87` IS CONFIRMED** — live Docker, all five arms, uniformly `status=502 clArity=1 declared=87 bodyLen=87`. **It was a CLAIM until this stage (§14 item 3, DISCHARGED).** `p93WantSubjBodyLen = 12` is likewise **MEASURED**, not merely derived. **Bonus the SPEC did not have: the reference's `declared` also reads 87, so `declared == delivered` ALREADY HOLDS on the reference side** — the invariant is a new SUBJECT-side gate, not a reference-side risk.
- ⚠️ **THE CROSS-SIDE BYTE TRANSCRIPT HAS ZERO CHURN (§14 item 2, DISCHARGED)** — proven by sha256: ref and subj streams dumped pre- and post-fix are all **420 bytes**, digest `c9739f62…`, `diff` empty both directions. ⚠️ **AND THE STRUCTURAL REASON IS STRONGER THAN THE MEASUREMENT: there is NO stored baseline anywhere in `test/`** (`golden|baseline|snapshot` reads **0**) and `expectations.yaml` is **PROSE, NOT MACHINE-EVALUATED** (ADR-0019). `content-length` appears there once, inside the *header allow-list — values not compared*.
- **All six §10.1 anchors are STILL CORRECT** at the PLAN's tip; `:1408` is still `bad = append(bad, …)` inside a loop. Nothing drifted between the SPEC and the PLAN.
- **The seven call sites and the 4/3 split are CONFIRMED**, and README `:147`'s parenthetical breakdown is **exactly right**. ⚠️ **Only its CONSEQUENCE clause is at issue — do NOT "fix" the seven.**
- **§5.2's blind spot REPRODUCES EXACTLY** — `\b(503|504)\b` over `driver.go` returns **exactly one line, `:1369`, and it is a COMMENT**. **Do not claim that gap closed.**
- ⚠️ **THE FOUR BODY-LESS SITES ARE NOT HOMOGENEOUS** — `retry.go:374` and `router_h2.go:80` write `Body: nil`; `:128` and `:138` OMIT the field. **Key the edit on `h2LocalReplyHeaders()`, never on `Body: nil`.** `:128`/`:138` are **BYTE-IDENTICAL** — disambiguate by enclosing guard (`attempt >= h2GrantRaceMaxRetries` vs `cluster.IsConnPoolOverflow(err)`), never by line.

### ⚠️ THE ONE ITEM NOBODY HAS PRICED
**`driver.go:1355-1380` — a 26-line comment block the fix falsifies in FIVE places**, including `:1369`'s *"BANKED, not fixed here"* (**this row IS that row**) and `:1372-1375`'s prediction that *"THESE PINS REDDEN and a human must consciously re-derive them."* ⚠️ **THAT PREDICTION CAME TRUE AND THE DE-ROT MUST SAY SO** — record it as the pin working as designed, do not quietly overwrite it. **SPEC §8's `+300/-44` for `driver.go` may or may not cover this; treat the instrument's real cost as a LOWER BOUND.**

### ⚠️ BANKED CANDIDATES
- **The `Content-Encoding: gzip` over an UNCOMPRESSED body corruption** — H/2-only, live on the tip. ⚠️ **EITHER phase-93 design incidentally fixes it. DO NOT CLAIM CREDIT** for a fix the row did not design; the reference-side comparison is unmeasured.
- **The `h3dispatch.go` Rule B comment de-rot** — ⚠️ **DECIDED AT THE PLAN: BANKED** (§14 item 6). It changes no behaviour and is wrong INDEPENDENTLY of this row, so the "don't ship a new falsehood in the fixing commit" rule that binds README `:147` does NOT bind it. **But ADR-0315 ¶9 must NAME it.** ⚠️ **`TestWriteH3Reply_EmptyBody` passes a NIL carrier — it is NOT a guard on the live path. Do not cite it as coverage.**
- **New arms driving the 503/504 classes** — the only way to make the instrument see the body-less sites.
- **The empty-body defect, BOTH legs** — ten sites. Reference bodies MEASURED: 87 / 167 / 19 / 24 B. ⚠️ **The contract ALREADY RELAXES local-reply body bytes** (`BEHAVIOR_CONTRACT.md:1993`) — a ratified relaxation, not an unnamed divergence. Adding a body emits a DATA frame where END_STREAM used to ride HEADERS.
- **The pooled-upstream-lifetime defect** — three sites, re-verified UNFIXED; reference side is a PREDICTION, three packages, probable SPLIT.
- **`ssl.connection_error`** — MEASURED ~4-6 production lines across 2 files, five `outcomeOther` rows already waiting. The best next IN-WINDOW pick.
- **1xx interim responses** — DROP-AND-DELIVER; ~5-15 lines, one symbol.
- **The H/1 no-`Host` divergence** — ⚠️ its `+90/-0` is a STALE PREDICTION.
- **`allocateOTLPPort`** — ⚠️ 26 driver files, not one site.
- **Counting the stat surface MECHANICALLY** — BLOCKED, not merely deferred.

## ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + ALL FOUR NCs YOURSELF** and record ACTUAL output — ⚠️ **AND AGAIN AFTER FLIPPING ROW 93, because NC-A and NC-B BOTH COLLAPSE from two-line to ONE-line shapes at the flip.**
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** 92 IMPL twelve · 93 BRAINSTORM eleven · 93 SPEC eleven · **93 PLAN TWELVE, FIVE of which were the SPEC'S OWN MEASURED FIGURES.** Do the same, and record it when your agents refute you.
3. **LINE ANCHORS DRIFT; SYMBOL AND LITERAL-TEXT ANCHORS DO NOT — RE-VERIFY EVERY CITE AT YOUR TIP.** ⚠️ **PATHSPEC-SCOPE every symbol assertion.** ⚠️ **A RECEIVER WRITTEN `(c *FilterChain)` MUST USE `grep -F`, NOT `-E`.** ⚠️ **AND A "DRIFT-PROOF" ANCHOR IS ITSELF A CLAIM — CHECK IT FOR UNIQUENESS** (the PLAN found two of the SPEC's matching 2 and 3 production lines). ⚠️ **`internal/filter/hcm/chain.go` DOES NOT EXIST — it is `internal/filter/http/chain.go`** — but ⚠️ **`actions.go` IS in `hcm`.** ⚠️ **`codec.go:87-89` is duplicated as a line-anchor INSIDE production code at `compressor.go:780`, and it is currently ACCURATE — inserting a line above `codec.go:87` rots a shipped comment in a file this row's blast radius already touches.**
3b. ⚠️ **CHARACTER-CLASS AND ESCAPE SELECTORS ARE THE FAIL-UNSAFE FAMILY.** `grep -cE '^\s+[a-z0-9./-]+ v[0-9]'` on `go.mod` reads 62 not 67 (misses uppercase/underscore paths) · `grep -oE '[A-Za-z]+ +BackendKind = 38'` TRUNCATES `H2` · `^[0-9]{4}-` reads 119, dropping `0007a`/`0007b` (use `ls -d test/fixtures/*/ | wc -l`) · ⚠️ **`\t` in a regex is TOOL-DEPENDENT: harness `grep` is ugrep 7.8.4, `/usr/bin/grep` is GNU 3.11. NAME THE TOOL.** Safe under both: `-P`, or `$(printf '\t')`.
3c. ⚠️ **`feedback_brief_citations_not_evidence` APPLIES TO INCIDENTAL FIGURES. Re-derive EVERY number you restate — including every number in THIS file.** ⚠️ **The 93 PLAN refuted FIVE of the SPEC's own measured figures. A predecessor's MEASURED number is not thereby true at your tip.**
3d. ⚠️ **NO GATE MAY REST ON A ` + ` SPLIT.**
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Live: TWO SDS dial-budget flakes (⚠️ **DO NOT reuse the `go list -deps` exclusion blanket — TRUE for `xds` (0), FALSE for `boot` (1)**) · the driver-owned receiver port race · `internal/httpclient` zero-value · the two 84.2-era flakes · the REFERENCE h2spec section-8 flip · `TestOutlierDetector_ConcurrentEjectExactlyOnce`. ⚠️ **`TestServerConn_TinyWindowDelivery` IS NOT A FLAKE — phase 91 FIXED a live production deadlock; a recurrence is a REGRESSION of row 91.**
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only). ⚠️ **THE FOREIGN-CONTAINER ROSTER IS NOT CLOSED AND IT CHANGED *TWICE DURING THE PLAN'S OWN SESSION*.** Seen and LEFT UNTOUCHED: `curl-world-tls-1`, `curl-world-httpd-1`, `curl-world-httpbin-1`, `infallible_booth`, `crazy_kare`, `golink-ai`, `quizzical_goldstine`, and mid-session `nice_satoshi`, `socks4a`, `socksspike`, `squidspike`, `infallible_darwin` (the last GONE by session end). **Treat NO roster as stable. Name your own with a distinctive per-stage prefix.**
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`.
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL.** **`gofmt -l` never exits non-zero — gate on OUTPUT.** ⚠️ **`golangci-lint` DOES exit non-zero on findings in this build — but STILL gate on OUTPUT.** ⚠️ **go test WITHOUT `-v` PRINTS ZERO `=== RUN`** — `RUN=0` beside `RC=0` is a VACUOUS GREEN. ⚠️ **A `-run` selector matching nothing prints `[no tests to run]` and EXITS 0.** ⚠️ **On `-v` output, unanchored `grep -c 'FAIL'` reads nonzero ON A FULLY GREEN TREE** — use `grep -cE '^(FAIL|--- FAIL)|^ *--- FAIL'`.
7b. ⚠️ **`grep -c` ON ZERO MATCHES PRINTS `0` AND EXITS 1** — capture (`v=$(… || true)`), NEVER chain with `&&`, and NEVER write `$(… || echo 0)`, which emits TWO zeros.
7c. ⚠️ **A GREEN TEST RUN IS NOT EVIDENCE A SITE IS EXERCISED — IT MAY BE DEAD CODE.** Prove it with a **`panic()` REACHABILITY CONTROL** (the 93 SPEC used one to prove all seven local-reply sites reach the encode chain).
7d. ⚠️ **NC EVERY NEW TEST, NOT JUST THE RISKY-LOOKING ONES. AND NC YOUR OWN FIXES** — a NC that is a BUILD BREAK proves nothing; **NEUTRALISE instead so the package still compiles.** ⚠️ **The 93 PLAN hit this concretely: a literal revert of the fix BUILD-BREAKS the new unit test, so it dropped only the emitted field and kept the parameter.**
7e. ⚠️ **A UNIT TABLE OVER A SHARED ASSERTION IS NOT A GATE ON PRODUCTION BEHAVIOUR** — and ⚠️ **`t.Helper()` IS WHAT SAVES IT**: it gives each failure a DISTINCT call-site line, so the table is not vacuated by its own shared assertion.
7f. ⚠️ **A NC THAT LEAVES YOUR NEGATIVE CONTROL GREEN IS NOT EVIDENCE THAT CONTROL DOES ANY WORK.** The 93 PLAN needed a SECOND, ISOLATING NC (synthesize-when-absent) to redden the control ALONE. **A coarse NC that reddens everything cannot show independence.**
8. ⚠️ **`pgrep -f` / `pkill -f` MATCHES YOUR OWN SHELL AND KILLS THE TOOL CALL (exit 144)**. **Kill ONLY PIDs you captured yourself** (from `$!`), and verify with `kill -0`. ⚠️ **Backgrounding an entire `&&` chain makes `$!` name the SUBSHELL and orphans the real process.**
9. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-*`, `wt-*` or stage worktree should outlive its stage close** — expect only `master` (verify with `git worktree list`).
10. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** (a)/(b) differential **121 fixtures** + full `go test ./...`, gated on **`PIPESTATUS[0]`** and a **SET RECONCILIATION** (⚠️ **NOT `INNER_EXIT`, which DOES NOT EXIST**); (c) h2spec cite ONLY from your own run — ⚠️ **h2spec is MEASURED BLIND to burst-drain ordering**; grpc-conformance deferred in writing; proxy-wasm **10 families measured** (NOT "10/16"); (d) fuzzers **56 / 48** today; (e) the ANCHORED panic gate `^panic:|DATA RACE|SIGSEGV` on every differential launch; (f) no REVIEW.md — standing departure. ⚠️ **`-race` on the DIFFERENTIAL suite is VACUOUS** — the subject is an unraced subprocess.
11. ⚠️ **THE FIXTURE INVOCATION: `go test ./test/differential/ -run 'TestDifferential/0004-h2-routing' -count=1 -v 2>&1`.** ⚠️ **`-count=1` IS NOT OPTIONAL** — the harness builds envoy-go as a **SUBPROCESS**, so a router edit is **not a compile-time input** to that test binary and the cache serves a stale PASS. **A fix whose own gate cannot see it is the most dangerous form of this trap.** ⚠️ **Gate (a)'s failure mode is a SILENT PASS** — the runner `t.Skipf`s an unregistered fixture and **NO fixture-count gate exists anywhere in the tree**. **ASSERT THE FIXTURE SET** and assert `--- PASS: TestDifferential/0004-h2-routing` **explicitly**.
11b. ⚠️ **`go test ./...` DRIVES DOCKER — AND THERE ARE **TWO** DOCKER DRIVERS, NOT ONE.** Besides `test/differential`, **`test/conformance/h2spec/h2spec_test.go`** also drives Docker. **Exclude BOTH:** `go list ./... | grep -vE '/test/differential$|/test/conformance/h2spec$'`. **The BRAINSTORM's recipe named only the first.** Serialize Docker ownership across parallel agents. ⚠️ **MEASURED at the PLAN: the clean non-Docker sweep is `RC=0, anchored FAIL=0, 125 ok + 109 no-test-files`; BEFORE the two re-baselines it reads FAIL=6, and that delta IS the `driver_test.go` finding.**
12. ⚠️ **`-p` PUBLISHING WORKS; `--network host` IS WHAT DOES NOT.** ⚠️ A **STRICT_DNS** cluster whose name does not resolve **blocks listener init entirely** — prefer `STATIC` by IP. ⚠️ **`clusters: []` is BOOT-REJECTED.** ⚠️ **envoy-go REJECTS `match.headers`**, ⚠️ **refuses `codec_type: HTTP2` without TLS unless `--allow-h2c`**, ⚠️ **boots with `-c`, NOT `--config-path`**, and ⚠️ **`access_log[].log_format` is BOOT-REJECTED.** ⚠️ **`ENVOY_TARGET.md` is at `docs/envoy-go/ENVOY_TARGET.md`, NOT the repo root.** ⚠️ **A PROBE BACKEND MUST BIND `0.0.0.0`, NOT `127.0.0.1`.**
13. ⚠️ **A RAW-FRAMER PEER MUST HOLD ONE `hpack.Decoder` PER CONNECTION.** ⚠️ **Set `ReadMetaHeaders` to NIL when probing malformed shapes.** ⚠️ **`0004` is TLS+ALPN-h2, NOT h2c.**
14. ⚠️ **`bash /dev/tcp` + `timeout cat <&3` INSIDE COMMAND SUBSTITUTION RETURNS SILENTLY EMPTY.**
15. ⚠️ **GO DOES NOT FLAG UNUSED CONSTANTS.** Post-removal cleanup is grep-driven.
16. ⚠️ **`golangci-lint`'s misspell runs in locale US and HAS NOW FIRED ON TWO CONSECUTIVE STAGES' PROTOTYPES** (the SPEC's `favour`, the PLAN's `behaviour` x2). **Sweep British spellings in `.go` comments BEFORE the gate.** ⚠️ **Markdown prose may use them freely.**
17. ⚠️ **A GREEN RUN OF A CONTENTION-SENSITIVE TEST PROVES NOTHING.** Phase 91's subject cleared 5/5 uncontended while failing 9/32 contended on the SAME binary.
18. ⚠️ **AN ABSENCE CLAIM NEEDS A POSITIVE CONTROL AND A DISCRIMINATING NEGATIVE CONTROL.**
19. ⚠️ **DERIVE next-free ADR FROM THE TAIL, NEVER FROM THE HEADING COUNT.** The id space is SPARSE (exactly one gap, `0209`) and **headings+1 reads 315 — the TAIL ITSELF, a TAKEN id.**
20. ⚠️ **`reference_measured_prototype_is_a_lower_bound` HAS FIRED SEVEN CONSECUTIVE ROWS, ALWAYS BY UNDER-ENUMERATING *FILES* — AND AT THE PLAN IT FIRED TWICE INSIDE ONE STAGE, THE SECOND TIME ON ITS OWN AGENT'S ENUMERATION.** **ENUMERATE BY `git grep -l` ON THE LITERAL CLAIM TEXT, AND BY COMPILING PROTOTYPE.**

## ⚠️ THE `PROPOSED` GUARD — TWO DIFFERENT REGEXES, AND THE IMPL DISARMS IT

| form | now | after the IMPL | where |
|---|---|---|---|
| `^> \*\*STATUS: PROPOSED` — **the house form** | **1** | **0** | `:18650` under `## ADR-0315` (`:18648`) |
| `^\*\*Status:\*\* PROPOSED` — **the ADR-0231 decoy** | **1** | **1** | `:14866` under `## ADR-0231` (`:14864`), byte-untouched |
| union of both | 2 | 1 | — |

⚠️ **VERIFY BY LINE AND BY ADR, NEVER BY THE COUNT ALONE** — a session gating on the decoy reads `1` after a correct DISARM and concludes nothing happened. ⚠️ **NEVER gate on the UNANCHORED form (90 lines / 101 occurrences) nor on the middle-ground `^\*\*Status:\*\*.*PROPOSED` form (23).** This is `ADR-0312 §Consequences (x)`: the SPEC hit it in the ARM direction, **the IMPL hits it in the DISARM direction.**

## ⚠️ THE ARCHIVE GUARD

⚠️ **THE STRICT `163` IS NOT THE ENTRY COUNT.** Measured at the PLAN close: loose **218** = strict **163** + parenthetical **55**, exactly. **The guard is SOUND as a DELTA-0 SHAPE check** (a correctly-shaped parenthetical append moves it by 0) **but the number is a SUBSET.** The three forms disagree and **any gate must name WHICH it uses**; the colon form carries **two false positives**.

⚠️ **THE ARCHIVE'S POSITIVE CONTROLS ARE SELF-INCREMENTING.** A control figure recorded in the file it measures is invalidated by the act of recording it. ⚠️ **THE PLAN'S ARCHIVE LINE THEREFORE NAMES NO CONTROL AT ALL** — the simplest correct move. If you name one, you MUST disclose the +1-per-use. ⚠️ **The same mechanism bites the eviction check:** the §Recent preamble NAMES the evictee, so a naive grep on `STATE.md` reads **1** — **MEASURED AGAIN AT THE PLAN CLOSE.** **The DISCRIMINATING form is the strict `^- \*\*prior active-phase:\*\* \`<label>\`` form, which read 0.**

## ⚠️ PORT BANDING

`net.ipv4.ip_local_port_range = 32768 60999`. ⇒ **BAND YOUR PROBE PORTS BELOW 32768.** ⚠️ **The differential harness reserves `20000..31007` (`harness_test.go:293`)**, so `12000-19000` is the safe band for ad-hoc probes — a failure OUTSIDE the harness band is an **out-of-band** occurrence, NOT an in-band recurrence. **Check availability with `ss -tan` (ALL states), never `ss -ltn`.** ⚠️ **FIXTURE ports are FAMILY-BANDED as `10<index>` — but `10120` is TAKEN (fixture `0028`); free in-band is `10126-10129`.** ⚠️ **`0120` as a DIRECTORY is FREE.**

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (**ugrep 7.8.4**). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **THE METHOD BINDS; carry no standing discrepancy figure and no standing absolute.** ⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS; print FILE NAMES, never a count.** ⚠️ **THE HARNESS `grep` STRIPS A LEADING `./`.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files; use `-v --no-index`, and `git status` is the discriminating check). ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **A DISCREPANCY MAY FAIL TO MANIFEST** — absence of a discrepancy is not evidence the blindness is gone.

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 93 (h2-local-reply-content-length)' --format='%h %s'   # the SLUG form
git log --grep '^phase 93' --format='%h %s'                                   # the LOOSE form — RUN IT to CROSS-CHECK
git log --grep '^phase 92 (h2-response-header-validation)' --format='%h %s'   # a CLOSED phase, use as a FIRING CONTROL
```
⚠️ **RUN BOTH FORMS AND RECONCILE THE SETS. NO COUNT IS QUOTED HERE, DELIBERATELY — BOTH FORMS ROT.** At the PLAN close both forms returned IDENTICAL SETS and the firing control fired. **A `| head -1` on ONE form can silently return a NON-TIP commit.** **Take the tip from `git rev-parse master`.** ⚠️ **AND WHEN YOU LAND A CORRECTION, PUT THE FULL SLUG IN ITS SUBJECT.**

Execution style: **subagent-driven per `feedback_execution_style`**. Subagents commit LOCALLY on the ONE stage branch with EXPLICIT PATHSPECS; the controller squashes (`feedback_subagents_no_push`, `feedback_push_to_origin`). **Pure MEASUREMENT agents should commit NOTHING and prove their tree clean.** ⚠️ **PARALLEL AGENTS IN ONE WORKTREE RACE THE GIT INDEX EVEN WITH DISJOINT PATHSPECS** — use **PER-AGENT WORKTREES ON THEIR OWN BRANCHES**, merged by the controller. ⚠️ **GIVE EACH PARALLEL AGENT ITS OWN PRIVATE SCRATCH DIR**, and **SERIALIZE DOCKER OWNERSHIP TO EXACTLY ONE AGENT.** ⚠️ **AND VERIFY WHAT A SUBAGENT REPORTS — BUT ALSO BELIEVE IT WHEN IT REFUTES YOU:** at the 93 PLAN, agents refuted five of the SPEC's own measured figures and were right every time; the controller then caught its OWN agent under-enumerating by one, so **verify in BOTH directions.** Per-task `gofmt` + `golangci-lint` on touched packages, **gated on OUTPUT**. ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root** — build with `-o` into scratch.

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root — **observed AGAIN at the phase-93 PLAN, TWICE, with the harness printing `Shell cwd was reset to /home/esa/git/envoy-go`**. Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` before any commit or gate run. ⚠️ **Shell VARIABLES do not survive between Bash calls** — re-export in every command. ⚠️ **AND AN UNQUOTED HEREDOC (`<<EOF`) COMMAND-SUBSTITUTES BACKTICKED TEXT** — use `<<'EOF'` and pass paths via the environment.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — `git status --porcelain` AND `git diff --stat master` must show ONLY your intended edits. For a patched TRACKED file: `sha256sum` BEFORE patching, `git checkout -- <path>` after, `sha256sum -c` the capture — ⚠️ **and COMMIT FIRST, because `git checkout --` restores from HEAD and will wipe uncommitted work.** ⚠️ **Also check the MAIN repo root, and `--untracked-files=all` to catch throwaway probe files.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads the phase-93 IMPL.** ⚠️ **`grep -c 'next-skill:'` reads 2: one is the ADR-0288 RULE STATEMENT matching its own grep, and the live pointer is the other. That is CORRECT, not a violation.** ⚠️ **§Project counts is a PHASE-76-ERA SNAPSHOT and SAYS SO.**
2. `docs/envoy-go/phases/93-h2-local-reply-content-length/PLAN.md` — **the NINE tasks, the Global Constraints (G1-G15), and §0's twelve refutations.** ⚠️ **Tasks 2 and 3 embed COMPLETE, MEASURED, both-directions-NC'd test files VERBATIM — use them, do not re-derive them.**
3. `docs/envoy-go/phases/93-h2-local-reply-content-length/SPEC.md` — the charter and the §4 reversal. ⚠️ **READ IT KNOWING FIVE OF ITS MEASURED FIGURES ARE REFUTED BY THE PLAN** (§6.1's ~+60, §4's 323, §6's "18-row", §8's ~+12/-10, and two §2 anchors).
4. `docs/envoy-go/phases/93-h2-local-reply-content-length/BRAINSTORM.md` — ⚠️ **READ IT KNOWING §3.2, §3.3 and §6.1's ARM CHOICE ARE REFUTED**, and that its ADR-0085 attribution is wrong. ⚠️ **IT IS A HISTORICAL PHASE DOCUMENT AND IS NOT RETRO-EDITED.**
5. `docs/envoy-go/DECISIONS.md` **ADR-0315** §Context (**TEN paragraphs**, not the nine SPEC §9.1 lists) — and **ADR-0155** (`:8187-:8381`) for the ratified `content-length: 0` doctrine the BRAINSTORM mis-attributed to ADR-0085 (`:3282-:3327`, ZERO doctrine matches).

## What the next session owes
**RUN THE PHASE-93 IMPL** (state 3 -> DONE), executing `PLAN.md`'s NINE tasks · **RE-RUN the sentinel + all four NCs yourself, BEFORE AND AFTER flipping row 93** — ⚠️ **NC-A and NC-B BOTH collapse from two-line to ONE-line shapes at the flip** · **FLIP ROW 93 `in-progress` -> `done` and CORRECT ITS `ADR-0085` ATTRIBUTION** (`ROADMAP.md:155` still carries the wrong form; the doctrine is ratified in **ADR-0155**) — ⚠️ **`want` STAYS 125 and row 93 must still read `NF=8` under BOTH field-count forms after your edit** · **COMPLETE ADR-0315** §Decision + §Consequences IN PLACE after the RETAINED italic footer, **no renumber, no `---` separator** ⇒ `^---$` STAYS **216**, tail STAYS **ADR-0315**, next-free STAYS **ADR-0316**, and the house `PROPOSED` guard **DISARMS 1 -> 0** (verify BY LINE AND BY ADR; the decoy stays 1) · **REFUTE YOUR PREDECESSOR BY EXECUTION** · `STATE.md` rolled IN PLACE with the oldest §Recent entry evicted by a **DIRECT DATE READ** (⚠️ **read the dates yourself — at the PLAN close there was NO unique oldest and the tie needed a LIFECYCLE-PRECEDENCE break; do NOT adopt position-by-convention**) and archived to `STATE_HISTORY.md` as **ONE INLINE LINE** whose label does NOT match `^- \*\*prior active-phase:\*\*`, so the strict guard stays at **163, DELTA 0** · ⚠️ **AND ROLL THE §Recent PREAMBLE SENTENCE TOO — the guard is structurally blind to it** · ⚠️ **NAME NO POSITIVE CONTROL IN THE ARCHIVE LINE, or disclose that you have just incremented it** · `next-prompt.txt` rolled (`git add -f`) · ONE squashed commit, merged and pushed — ⚠️ **and if a late measurement invalidates something you already pushed, land a CLEARLY-LABELLED correction commit (WITH THE FULL SLUG IN ITS SUBJECT) rather than force-pushing or leaving it standing.**
