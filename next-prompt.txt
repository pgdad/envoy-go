=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**Check (1) is NON-SILENT** (row 57 `stats-sink-graphite` is `in-progress`; check (1) prints `NOT DONE: row 57`). **The sentinel does NOT fire** — an open row alone blocks it, and (2)+(3) still carry work besides. Do NOT create `stop`.

**SENTINEL RE-CHECKED 2026-07-11 (post-phase-57-SPEC) — it does NOT fire. Do NOT create `stop`.** Evidence:
- (1) prints `NOT DONE: row 57` ⇒ phase 57 is OPEN (a PLAN is the next stage). Blocks `stop` outright.
- (2) prints the Observability family's deferred candidates ⇒ the **Observability family STAYS OPEN** (OTLP-metrics sink + tracing `custom_tags`/`spawn_upstream_span`/`http_service`/force-trace).
- (3) prints `NEVER OPENED: HTTP/3`, `gRPC`, `xDS`, `Runtime`, `WASM` ⇒ FIVE families never opened; the **Operational-tooling family** also STAYS OPEN.

**STATUS AS OF 2026-07-11: phase 57 (`stats-sink-graphite`) is OPEN — row 57 is `in-progress`; the BRAINSTORM and the SPEC have LANDED.** The SPEC session live-probed the reference across EIGHT fresh-container arms and DISPOSED all eleven D-GR-* questions. **Load-bearing pins (details in `docs/envoy-go/phases/57-stats-sink-graphite/SPEC.md` §11):** the graphite line is `<prefix>.<residual>[;key=value;…]:<value>|<c|g>` (tags IN THE NAME — they fold into `emitStatsdLines`'s name argument; the ONE novel piece is a ~10-LoC `graphiteTagSuffix`); `|c` is a per-flush DELTA (zero-delta lines re-emitted), `|g` absolute; default prefix `envoy`; tag order NATURAL-unsorted and cross-side REVERSED on two-tag names ⇒ tag assertions SET-based only; batching == phase-50 dog_statsd semantics verbatim (strict-`>`, `\n`-separated never terminated, oversized-sent-alone, absent ⇒ one-line-per-datagram); zero self-stats; three reference boot-rejects pinned verbatim (missing `statsd_specifier` / explicit `max_bytes_per_datagram: 0` / hostname address) — envoy-go mirrors the first two, keeps the hostname-accepting departure, extends the sibling-reject to FOUR sinks; the fixture is ONE dir `0101-stats-sink-graphite` merging the 0093+0094 shapes over an ADDITIVE `statsdrecv` name-embedded-tag extension; `FuzzGraphiteStatsdSinkConfigParse` lands 53 → 54. ADR-0275 §Context is DRAFTED in SPEC §13 (§Decision/§Consequences at the IMPL per ADR-0044).

**Current counts (verified at the phase-57 SPEC; UNCHANGED — docs-only):** fixtures **102** (tail `0100-http-tap-bodies`) · fuzzers **53** · DECISIONS tail **ADR-0274** (next-free **ADR-0275**) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** · new Go packages **0** · new go.mod modules **0**.

**Locating the stage squash (a router cannot name its own sha — writing the sha changes it).** Identify it by SUBJECT, never by sha and never by position:
```sh
git log --oneline --grep='^phase 57 (stats-sink-graphite) SPEC' -1
```
This stage folded its own `next-prompt.txt` roll INTO the squash (per `reference_next_prompt_tracked_despite_gitignore`). The squash is NOT necessarily the master tip: parallel-workstream commits can land on top of it. ALWAYS `git fetch` + re-verify counts against the ACTUAL current master tip at the start of the PLAN session.

**THE NEXT ACTION: the phase-57 PLAN (lifecycle-state 2 → 3) — decompose the SPEC into a TDD task spine.**

==============================================

## What THIS session does (the phase-57 PLAN, lifecycle-state 2 → 3)

Phase 57 is OPEN (row 57 `in-progress`, BRAINSTORM + SPEC landed). This session performs the **PLAN** (docs-only; NO production `.go` code). Fresh worktree per `feedback_git_worktrees` (e.g. `.worktrees/phase-57-plan`, branch `phase-57-stats-sink-graphite-plan`). **Skill gate: `superpowers:writing-plans`.**

1. **Read the SPEC** (`docs/envoy-go/phases/57-stats-sink-graphite/SPEC.md`) — the §11 pins, the §3 design, the §8 differential envelope, the §10 anticipated ~9–12-task spine, and the §12 PLAN-time D-questions.

2. **Resolve the §12 D-questions:** **D-GR-BATCHSHARE** (generalize `appendLine`/`flush` into a helper shared by DogStatsdSink + GraphiteStatsdSink vs duplicate ~20 LoC in `graphite.go` — bias generalize IF the dog_statsd tests stay untouched); **D-GR-SPLIT** (re-check the ADR-0045 `~15` ceiling with real task LoC); **break-(b) firing precision** (engineer WHICH assertion the tag-format break fires; add an isolating break if masked — `reference_deliberate_break_wrong_assertion`).

3. **Write `docs/envoy-go/phases/57-stats-sink-graphite/PLAN.md` + `PROGRESS.md`** (the phase-55 bare-name convention): a TDD task spine (each task = test-first, with a DELIBERATE BREAK proving each new assertion live per `reference_differential_break_protocol_count1` + `-count=1`), per-task gofmt + golangci-lint on touched pkgs (`feedback_pertask_gofmt_lint`), subagent execution notes (worktree path-pinning per `feedback_subagent_worktree_path_targeting`, GIT-HYGIENE block per `feedback_subagent_worktree_detach`, commit-locally-only per `feedback_subagents_no_push`). **RE-DERIVE every SPEC citation from source** (`feedback_brief_citations_not_evidence`) — the SPEC's `file:line` cites (e.g. `bootstrap.go:505-520` dispatch, `dogstatsd.go:122-144` batching, `udp.go:58-79` emit skeleton) must be re-verified, never trusted.

4. **Counts are UNCHANGED at a PLAN** (docs-only) — re-verify against the master tip and record. Update STATE.md's active-phase header (SPEC done → PLAN done, demote prior).

5. **Land the PLAN** (commit + push per `feedback_push_to_origin`; docs-only), then **ROLL THIS FILE FORWARD** to the phase-57 IMPL.

## Honor throughout (the discipline, unchanged)
- **Lifecycle = one stage per session.** This session: the phase-57 PLAN (docs-only).
- **Skills FIRST.** `superpowers:writing-plans` gates the PLAN write; honor `superpowers:systematic-debugging` if a re-derivation surprises.
- **`next-prompt.txt` IS TRACKED** (`.gitignore` line 2 does not apply to an already-tracked file). Edit it inside the stage worktree and fold it into the commit (`reference_next_prompt_tracked_despite_gitignore`). Locate commits by SUBJECT (`git log --grep`), never by position.
- **A BRIEF'S CITATIONS ARE NOT EVIDENCE** (`feedback_brief_citations_not_evidence`) — this discipline caught real defects at EVERY phase-56 stage and two more at the phase-57 SPEC (a stale `bootstrap.go:417-419` doc comment; the reference's exact-code `_NNN` collapse envoy-go lacks).
- **Sink/differential traps (apply at the PLAN):** `reference_delta_sink_differential_stability_barrier` · `reference_periodic_sink_differential_two_receivers` · `reference_streaming_sink_differential_framing` · `reference_stats_sink_emits_used_only` · `reference_admin_interface_wire_name_collision` · `reference_membership_total_vs_healthy_gauge` · `reference_framing_break_needs_unparsed_counter` · `reference_differential_break_protocol_count1` · `reference_deliberate_break_wrong_assertion` · `reference_fatalf_makes_assertions_unreachable` · `reference_differential_run_selector` · `reference_differential_backendcount_min_one` · `reference_fixture_workload_constant_desync` (K=7 + the ~172-char cluster name + cap 200 are COUPLED constants — name them once).
- **Process anchors:** ADR-0045 (re-check the `~15` ceiling at PLAN) · ADR-0106 (row 57 flips `done` at the IMPL) · ADR-0044 (ADR-0275 §Decision/§Consequences land at the IMPL) · the ROADMAP §Schema. Next-free ADR is **ADR-0275**.

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-57 PLAN is next).
2. `docs/envoy-go/phases/57-stats-sink-graphite/SPEC.md` — the pinned design (§3), the empirical pins (§11), the differential envelope (§8), the PLAN D-questions (§12).
3. `docs/envoy-go/phases/57-stats-sink-graphite/BRAINSTORM.md` — the settled scope (Q0–Q2).
4. The prior sink PLANs as templates — `docs/envoy-go/phases/50-stats-sink-dogstatsd-batching/PLAN-50.md` (batching tasks + breaks) + `docs/envoy-go/phases/49-stats-sink-dogstatsd/PLAN-49.md` (tags + receiver-extension tasks).
5. The reuse substrate to re-derive against — `internal/statssink/{dogstatsd.go,delta.go,udp.go,statsd.go,flusher.go,sink.go}` + `internal/bootstrap/bootstrap.go` (`parseStatsSinks` + `parseUDPSinkAddressAndPrefix`) + `cmd/envoy-go/main.go` (the three sink build loops) + `test/helpers/statsdrecv/statsdrecv.go` (`ingestLine`) + `test/fixtures/0093-*/`+`0094-*/` drivers.
6. `docs/envoy-go/STATE.md` — the active-phase header (phase 57 SPEC done).
7. `docs/envoy-go/DECISIONS.md` — ADR-0265/0266/0267 (the sink-ADR shape) + ADR-0044/0045/0080/0106.
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the `reference_*` traps.
