=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-CHECKED 2026-07-14 (after the phase-62 IMPL landed + row 62 flipped `done`, run MECHANICALLY) — it does NOT fire. Do NOT create `stop`.** State:
- (1) prints NOTHING — row 62 `tracing-custom-tags-request-header` flipped `in-progress` → `done` at its IMPL six-gate (the SOLE leg, ADR-0106). Row 62 was the LAST non-`done` ROADMAP row; every row is now `done`.
- (2) prints THREE live "candidates:" sentences — HTTP/3, xDS, and Observability ⇒ three families STAY OPEN. The Observability sentence is now NARROWED to `custom_tags (`environment`/`metadata`)` — `request_header` rolled out of the live list at this row's done-flip (`reference_sentinel_deferred_sentence_live_vs_historical`).
- (3) prints `NEVER OPENED: gRPC`, `Runtime`, `WASM` ⇒ THREE families never opened. Operational-tooling also STAYS OPEN.

Checks (2) and (3) print ⇒ the sentinel does NOT fire.

**STATUS AS OF 2026-07-14: the phase-62 IMPL LANDED and ROADMAP row 62 is `done`.** The request_header tracing custom_tag source arm is CLOSED — `internal/tracing/config.go:155-156`'s `request_header type unsupported` reject is LIFTED; `parseCustomTags` reshapes to `([]CustomTagSpec, error)` with a request_header accept arm (empty-`name` PGV-parity reject) + first-wins dedup by tag key (correcting a latent phase-59 last-wins divergence); a new per-request `ResolveCustomTags(specs, headerLookup) []KV` (literal→static; request_header→first header value / default / omit-on-missing) is threaded at the THREE `accesslog_emit.go` `BuildServerSpan` call sites (H1/H2/H3) via the EXISTING `reqHeaderLookupH1`/`reqHeaderLookupH2`; `BuildServerSpan`/`upsertAttr` stayed UNCHANGED; `environment`/`metadata` STAY loud strict-reject departures. Proven via the NEW `0105-tracing-custom-tags-request-header` OTLP differential (cloned from `0102`; present-case cross-side by key, break-proven live) plus deterministic unit tests for the default/omit/multi-value/present-empty edges + the precedence-dedup + the Zipkin path, and a `FuzzHCMConfigParse` seed (fuzzers stay 55). ANCHORED ADR-0283 (§Context drafted at the SPEC; §Decision/§Consequences authored at this IMPL). Six-gate ALL GREEN + the full 107-dir differential (`355.138s`, exit 0, byte-stable except the new `0105`). No banked mid-lifecycle split legs remain (no `in-progress` ROADMAP rows). Per the STANDING DIRECTIVE, the roller SELF-PICKS the next subject at the upcoming BRAINSTORM.

**Candidates for the roller's next self-pick (to be weighed and FORMALIZED — pick + rejected alternatives recorded — in that BRAINSTORM; THIS router does NOT commit to one):**
- The remaining non-literal Observability tracing `custom_tags` source types: **`environment`** (an env-var-valued tag — static-resolvable like `literal`, but its differential drags a harness env-var-injection wrinkle into BOTH the reference container AND the subject subprocess) and **`metadata`** (needs a dynamic-metadata lookup path envoy-go does not yet have).
- The **OTLP-metrics stats sink** (a whole new `stats_sinks[]` consumer; larger than the recent single-arm tracing rows).
- The remaining tracing follow-ons named in ADR-0277/ADR-0283 §Consequences: **`spawn_upstream_span`**, **`http_service`**, **force-trace** (each needs additional plumbing envoy-go currently lacks: a second span, a new transport, or internal-request/edge-header detection).
- **Opening a never-chartered family**: gRPC, Runtime (RTDS), or WASM (greenfield — larger than an increment on a landed engine) — or the **Operational-tooling** family (admin-API reload/validate, also never opened per the STATE/ROADMAP prose).
- The still-open **xDS** family's remaining candidates (SDS rotation, CDS/EDS, LDS/RDS, ADS, Delta xDS, RTDS, google_grpc transport) and the **HTTP/3** family's remaining candidates (upstream H3 cluster, alt-svc, 0-RTT, h3spec, QuicProtocolOptions tuning, full QUIC transport-socket options, QUIC robustness) — both listed in ROADMAP's live deferred sentences and available to the roller's smallest-defensible-candidate weighing alongside the Observability options above.

The BRAINSTORM session performs the full source read + weighing and records the final self-pick + the formally rejected alternatives (per the 2026-07-12 standing directive) — this list is a candidate roster, not a decision.

==============================================

## What THIS session does (the roller's next self-pick BRAINSTORM)

Perform the BRAINSTORM stage (lifecycle-state 0 → 1) for the roller's next self-picked subject, using `superpowers:brainstorming` (REQUIRED before any creative/design work). This is a DOCS-ONLY stage in a FRESH worktree off master (`feedback_git_worktrees`); NO production `.go` changes. Per the 2026-07-12 standing directive, weigh the candidate roster above (a full source read against the current master tip, not just this list) and SELF-PICK the smallest defensible subject — smallest CLEANLY-DIFFERENTIAL-PROVABLE candidate first, following the same reasoning the phase-58/59/62 BRAINSTORMs used. Author `docs/envoy-go/phases/<NN>-<slug>/BRAINSTORM.md` (pick the next-free phase number — RE-DERIVE the next-free phase dir; phase 62 is the current tail) recording: the subject + why it is the smallest-defensible self-pick, the FORMALIZED rejected alternatives (§2.1), the design questions (a D-* list scoped to the picked subject), the anticipated counts, and whether a SPLIT is needed (ADR-0045). It registers the new ROADMAP row (`in-progress` per the §Schema invariant) under the correct family (opening a new family heading + sentinel slug if the pick is a family opener). Land it (commit + push per `feedback_push_to_origin`) and ROLL THIS FILE to the SPEC stage.

**Honor throughout (the discipline, unchanged):**
- **Lifecycle = one stage per session.** This session: the BRAINSTORM ONLY. After it lands: roll to the SPEC.
- **Process anchors:** ADR-0044 (ADR §Decision/§Consequences land at the IMPL) · ADR-0045 (the split escape-valve, only if the slice exceeds ~15 tasks) · ADR-0106 (a row flips `done` at its IMPL; a split-phase row only when ALL legs land) · `reference_roadmap_split_phase_row_done`.
- **`next-prompt.txt` IS TRACKED** (`reference_next_prompt_tracked_despite_gitignore`) — edit it inside the stage worktree and fold into the stage commit; locate commits by SUBJECT (`git log --grep`), never by position.
- **Worktree discipline** (`feedback_git_worktrees`, `feedback_subagent_worktree_detach`, `feedback_subagent_worktree_path_targeting`) — a BRAINSTORM is light, but keep the worktree hygiene; if subagent-driven, `feedback_execution_style` + `feedback_subagents_no_push`.
- If the pick turns out to be a tracing `custom_tags` follow-on (`environment`/`metadata`), read `docs/envoy-go/DECISIONS.md` ADR-0277 + **ADR-0283** first — the landed `CustomTagSpec`/`ResolveCustomTags`/`upsertAttr` seam this row would extend.

**ADR reservation ledger (provisional; each finalized at its IMPL in landing order):** ADR-0277 = phase-59 tracing-custom-tags-literal (LANDED) · ADR-0278 = xDS 60.1 (LANDED) · ADR-0279 = HTTP/3 61.1 (LANDED) · ADR-0280 = xDS 60.2 (LANDED) · ADR-0281 = HTTP/3 61.2 (LANDED) · ADR-0282 = HTTP/3 61.3 harness/differential (LANDED) · ADR-0283 = phase-62 tracing-custom-tags-request-header (LANDED) · next-free unreserved **ADR-0284** (the next-subject phase anticipates it at its IMPL).

**Current counts (VERIFIED at the phase-62 IMPL against HEAD `514d78ae` / this docs commit — RE-VERIFY at session start, `git fetch` first):** fixtures **107** (`ls -d test/fixtures/[0-9]*/ | wc -l`; tail `0105-tracing-custom-tags-request-header`) · fuzzers **55** · DECISIONS tail **ADR-0283** (next-free **ADR-0284**) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** · go.mod modules **2** (`quic-go` direct + `qpack` indirect). A BRAINSTORM is DOCS-ONLY ⇒ ALL counts UNCHANGED at that stage.

**Locating the phase-62 IMPL stage squash** (a router cannot name its own sha — identify by SUBJECT, never by position):
```sh
git log --oneline --grep='^phase 62 (tracing-custom-tags-request-header) IMPL' -1
```

## Read first (in order)
1. THIS FILE — the cold-start router (the next-subject BRAINSTORM is next; row 62 is CLOSED, `done`).
2. `docs/envoy-go/STATE.md` — the active-phase header (phase 62 IMPL done; NEXT = the roller's self-pick BRAINSTORM).
3. `docs/envoy-go/ROADMAP.md` — the Observability family (its narrowed live deferred sentence: `environment`/`metadata` + OTLP-metrics + `spawn_upstream_span`/`http_service`/force-trace), the xDS + HTTP/3 families' live deferred sentences, and the never-opened gRPC/Runtime/WASM + Operational-tooling families — the full candidate surface for this BRAINSTORM's self-pick weighing.
4. `docs/envoy-go/phases/59-tracing-custom-tags-literal/` and `docs/envoy-go/phases/62-tracing-custom-tags-request-header/` (BRAINSTORM/SPEC/PLAN/PROGRESS) + `docs/envoy-go/DECISIONS.md` ADR-0277/ADR-0283 — the LANDED custom_tag engine (`CustomTagSpec`/`ResolveCustomTags`/`upsertAttr`), the seam any `environment`/`metadata` follow-on would extend.
5. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the tracing `reference_*` traps (esp. `reference_tracing_custom_tag_override_builtin`, `reference_phase_46_tracing_split`, `reference_b3_parent_is_incoming_spanid`, `reference_tracing_upstream_cluster_framework_gap`) + the differential-harness traps relevant to whichever subject is picked.
