START a fresh cold-start session for envoy-go — **the phase-39 (`upstream-health-check`, active health checks) SPEC is DONE, LANDED on master, and PUSHED to `origin/master`; the next lifecycle step is the phase-39 PLAN** (`superpowers:writing-plans` — decompose SPEC §10 [the ~14–20-task spine] into bite-sized TDD tasks; resolve the §12 D-S39-1..D-S39-6 PLAN/IMPL questions; the FINAL ADR-0045 split-gate re-check [single 39.1 leg vs a state+runtime / health-aware-LB sub-split]; the plan-document-reviewer gate). The substantive master tip is the phase-39 SPEC-DONE bundle (`docs/envoy-go/phases/39-upstream-health-check/SPEC.md` + README + STATE + ROADMAP row 39 SPEC-DONE note; confirm via `git log --oneline -5`). The prior substantive tips are the phase-39 BRAINSTORM bundle and the phase-38.2 IMPL squash `14053d1`. ALL counts UNCHANGED at this SPEC: **stat surface 1125 / fixtures 67 / fuzzers 42 / BackendKind 33 / DECISIONS tail ADR-0241 (next-free ADR-0242).**

## What just landed (phase 39 SPEC — the durable record is `docs/envoy-go/phases/39-upstream-health-check/SPEC.md` + BRAINSTORM.md; read those, not this summary)

The §11 D-HC1..D-HC8 empirical pins EXECUTED IN-SESSION live against `contrib-v1.37.2` (a Docker bridge-network probe per `reference_docker_probe_bridge_network`: a STRICT_DNS cluster over a live nginx + a dead unbound host with `health_checks: [http{path:/}]` + `--mode validate` reject probes + a single-dead-host panic probe). **SPEC-firmed (AMEND-HC1..HC8):**

- **HC1** first-check-immediate — a host becomes healthy on its FIRST success / unhealthy on its FIRST failure; `unhealthy_threshold`/`healthy_threshold` gate only SUBSEQUENT transitions. `/clusters` health_flags `healthy`/`/failed_active_hc`; the dead host excluded from `membership_healthy` + LB rotation. MVP host state = a single healthy/unhealthy bit (DEGRADED/DRAINING/TIMEOUT deferred).
- **HC2** the `HealthCheck` PGV rejects pinned byte-stable: `field: "health_checker", reason: is required` / `HttpHealthCheckValidationError.Path: value length must be at least 1 characters` / `HealthCheckValidationError.{Interval,Timeout,UnhealthyThreshold}: value is required`. The TCP checker PARSE-ACCEPTS at the reference → envoy-go 39.1 DEPARTURE-rejects tcp/grpc checkers (lifted at 39.2); gRPC needs cluster H2; `expected_statuses` default 200. envoy-go does its OWN parse-reject (no PGV).
- **HC3** interval/timeout/unhealthy_threshold/healthy_threshold PGV-required (NO defaults); `no_traffic_interval` default 60s; first check at startup.
- **HC4** the full reference per-cluster stat set pinned [8 `health_check.*` + `membership_{change,healthy,degraded,excluded,total}` + `lb_healthy_panic` + `update_no_rebuild`]; **envoy-go MVP +7 → stat surface 1132** [`health_check.{attempt,success,failure,network_failure}` counters + `health_check.healthy` gauge + `membership_healthy` gauge + `lb_healthy_panic` counter; emitted ONLY on clusters with `health_checks` → existing fixtures unaffected]; the deferred-feature stats {passive_failure/verify_cluster/degraded/membership_change/degraded/excluded/update_no_rebuild} are recorded departures.
- **HC5** `Cluster.common_lb_config.healthy_panic_threshold` default 50%, **strict `<`** (exactly 50% does NOT panic — verified `lb_healthy_panic: 0` at 1-of-2 healthy; a single-dead-host cluster 503'd + `lb_healthy_panic` 0→1); panic routes to ALL hosts → `0066` uses 2-live/1-dead (66%) to assert FILTERING not panic.
- **HC6** the unhealthy host is filtered from rotation (6/6 served by the live host, no 503); behavior-neutral when all healthy.
- **HC7** `0066-health-check-http` = 2 live + 1 dead, poll `/stats` to convergence then 100%-live assertion; single 39.1 leg (~14–20 tasks).
- **HC8** `go mod tidy -diff` EMPTY → ZERO new module; oneof fields http=8/tcp=9/grpc=11; `google.golang.org/grpc v1.70.0` ALREADY a direct dep → 39.2 gRPC uses `grpc_health_v1` directly (no hand-roll).

**Architecture (ADR-0242 + ADR-0243; §Context drafts anchored in SPEC §13, bodies at the 39.1 IMPL per ADR-0044):** a per-cluster host-health registry `map[string]*hostHealth` keyed by `Endpoint.Addr()` (atomic healthy + consecutive-result counters; `Endpoint` unchanged) + the cluster-level checker runtime (per cluster × per `health_check` goroutine; `Manager.StartHealthChecks(ctx)` post-`Freeze` boot start + `Manager.Drain()` cancel/wait — the project's FIRST background runtime) + the HTTP probe (reusing the H1/H2 client) → ADR-0242; the health-aware LB pick (Approach A — a build-time-injected health view consulted by all six constructs; skip + walk-to-next-healthy preserving ring_hash/maglev key stability; `healthy_panic_threshold` fallback; NOT a `Pick`-signature widening — health is cluster state; byte-stable surface; behavior-neutral when all healthy) → ADR-0243. ZERO new packages/go.mod modules at 39.1; the work lands in `internal/cluster` (`health.go` + the six-LB view-consult + the `Manager` lifecycle) + `cmd/envoy-go/main.go`.

## What's next — the phase-39 PLAN (`superpowers:writing-plans`)

Author `docs/envoy-go/phases/39-upstream-health-check/PLAN.md`. Decompose SPEC §10 (the ~14–20-task spine: baselines → `hostHealth` registry+transitions → `extractEndpoints` seed → HTTP probe codec → checker runtime → `StartHealthChecks`/`Drain` lifecycle + boot call → health-view injection → health-aware pick across six constructs + panic → stat registrations → config parse+rejects [incl. the tcp/grpc 39.1 departure] → `0066` fixture → break/flake → re-verify → completion bundle) into bite-sized TDD tasks. Resolve the §12 **D-S39-1..D-S39-6** (file placement + the health-view interface shape; the pre-first-check routability edge; the exact byte-stable reject strings; the `0066` constants + poll predicate + flake protocol; the checker-runtime test hygiene [context cancel in Drain, no goroutine leak under -race, an injectable clock]; the FINAL ADR-0045 re-check). Run the plan-document-reviewer gate. Row 39 STAYS `in-progress` at the PLAN commit.

## Read first (in order)

1. THIS FILE — the post-phase-39-SPEC cold-start (next: the phase-39 PLAN).
2. `docs/envoy-go/STATE.md` — active-phase `phase 39 (upstream-health-check) SPEC done`; the counts; the lifecycle routing (→ the phase-39 PLAN).
3. `docs/envoy-go/phases/39-upstream-health-check/SPEC.md` — **the SPEC** (the §1.1 AMEND-HC1..HC8 block; the §3 architecture; the §6 PARSE-REJECT roster; the §7 stat surface +7; the §8 `0066` differential; the §10 task spine; the §11 live-pin block; the §12 PLAN D-questions; the §13 ADR §Context drafts) + BRAINSTORM.md + README.md.
4. `docs/envoy-go/ROADMAP.md` — row 39 (`in-progress`, SPEC-DONE note) + the §Upstream robustness family heading note.
5. `docs/envoy-go/DECISIONS.md` — the process ADRs (ADR-0106/0045/0044/0052/0004/0080/0227) + ADR-0232 (byte-stable OPTION-C) + ADR-0235/0239 (the per-request pick-inputs the health view CONTRASTS) + ADR-0236/0238 (ring_hash/maglev — walk-to-next-healthy) + ADR-0230 (the prior framework-runtime seam) + ADR-0024.
6. `internal/cluster/{cluster.go,manager.go,loadbalancer.go,ringhash.go,maglev.go,leastrequest.go,random.go,subset.go,dial_h2.go}` + `internal/admin/admin.go` (`/stats`) + `cmd/envoy-go/main.go` (the boot sequence) + `test/fixtures/0065-weighted-clusters/` (the cross-side HTTP differential precedent `0066` follows).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_execution_style`, `feedback_git_worktrees`, `feedback_subagents_no_push` + `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_path_targeting`/`_detach`, `reference_differential_band_sigma_margin`, `reference_differential_break_protocol_count1`, `reference_differential_run_selector`, `reference_fixture_workload_constant_desync`, `reference_docker_probe_bridge_network` (surface them at the IMPL).

## Honor throughout

- The lifecycle is BRAINSTORM → SPEC → PLAN → IMPL, each its own session. The next session is the phase-39 **PLAN** (`superpowers:writing-plans` — decompose SPEC §10; resolve §12 D-S39-1..6; the FINAL ADR-0045 re-check; plan-document-reviewer).
- `feedback_git_worktrees` · `feedback_execution_style` (subagent-driven at the IMPL) · `feedback_subagents_no_push` · `feedback_push_to_origin` (the controller squash-merges + pushes at stage-close) · `feedback_pertask_gofmt_lint`.
- ADR-0004 (the empirical-pin discipline — EXECUTED at the SPEC, §11) · ADR-0044 (ADR §Context at SPEC, bodies at IMPL in-place) · ADR-0045 (the split-gate — the FINAL 39.1/39.2 decision at the PLAN) · ADR-0052 (the atomic-landing six-gate) · ADR-0080 (byte-stable parse-reject wording) · ADR-0106 (flat family row — NO parent rollup) · ADR-0227 (the contrib reference image `envoyproxy/envoy:contrib-v1.37.2`).
