=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage. To use it to implement the next phase: read `docs/envoy-go/STATE.md` for the recorded NEXT, perform that one stage (the concrete action is spelled out below), land it, and re-roll this file to the following stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, the active phase is fully landed, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action. While ANY stage remains — as NOW: the **47.1 SPEC is pending**, so tasks are NOT complete — do NOT create `stop`; perform the stage described below.
==============================================

RESUME a fresh session for envoy-go (router last updated 2026-06-29; the **phase 47 (stats-sink metrics_service) BRAINSTORM is DONE and LANDED** — docs-only direct-on-master, committed + pushed to origin). Phase 47 is the **FOURTH Observability-family row** (`stats-sink-metrics-service`); the family STAYS OPEN. **ROW 47 registers `in-progress`** (flips `done` at the FINAL leg 47.2 IMPL per ADR-0106 + `reference_roadmap_split_phase_row_done`). The next action is the **47.1 (core) SPEC**.

HEAD is the phase-47 BRAINSTORM docs commit atop the 46.2 IMPL squash `2e176d0b`. **Re-read the shas with `git log --oneline -6`, do NOT assume.** Confirm `git status` clean (the untracked `.claude/` is session cruft, leave it) + master == origin/master.

## What the phase-47 BRAINSTORM chartered (docs-only — `docs/envoy-go/phases/47-stats-sink-metrics-service/BRAINSTORM.md`, the 12-section phase-44/45/46 shape; BRAINSTORM-reviewer APPROVED, all 8 load-bearing claim groups verified TRUE against disk, ZERO corrections)
- **Subject:** a periodic gRPC **`metrics_service`** stats sink — `envoy.config.metrics.v3.MetricsServiceConfig` → `envoy.service.metrics.v3.MetricsService.StreamMetrics` (client-streaming) — the project's FIRST consumer of the bootstrap `stats_sinks[]` field (silently ignored to date per ADR-0064) and its FIRST **periodic registry-snapshot** sink (all prior Observability sinks — ALS/OTLP-log/tracing — are per-request/event-driven).
- **The NEW framework piece** is a bootstrap-level **`Flusher`** (a new `internal/statssink` package): a `time.NewTicker(stats_flush_interval)` loop started AFTER `bs.Stats.Freeze()` (`cmd/envoy-go/main.go:325`, alongside the health-check/outlier background goroutines) that `Walk`s the frozen `stats.Registry` (`internal/stats/registry.go:134`), builds `[]*client_model.MetricFamily`, and hands the batch to the `MetricsServiceSink` (the 46.1b `OTLPExporter` lifecycle shape — bounded channel + writer goroutine + retry-once + idempotent `Close`). NO per-event ingest; the Registry IS the source. **Only `Counter`+`Gauge` exist (histograms deferred, ADR-0060)** ⇒ the metric-type map is closed to `COUNTER`/`GAUGE`; `histogram_emit_mode` is moot.
- **Split (ADR-0045, BY-CONCERN 2 legs):** **47.1 (core)** = the cumulative/no-labels envelope (counters→`COUNTER` absolute, gauges→`GAUGE`, full dotted name, no labels — the `report_counters_as_deltas=false`+`emit_tags_as_labels=false` defaults) + the `Flusher` + the `MetricsServiceSink` + a typed `MetricsServiceClient` over `grpcclient.Dialer` (ADR-0158; `internal/grpcclient/grpcclient.go:399` `NewOTLPTracesClient` precedent) + the `stats_sinks[]` parse arm LIFTING the metrics_service sink from the ADR-0064 silent-ignore set + EXPLICIT sibling-TypeURL boot-rejects (`reference_strict_reject_sibling_typeurl_gap` — statsd/dog_statsd/etc.) + the strict-reject arms (deltas/tags-as-labels/non-V3 transport/`histogram_emit_mode`/`stats_flush_on_admin`) + the `HasCluster` boot-reject + the driver-owned `test/helpers` metrics receiver + the `0089-stats-sink-metrics-service` cross-side EXACT differential (ANCHORS **ADR-0262**). **47.2 (deltas + tags-as-labels)** = `report_counters_as_deltas=true` (per-counter last-flush delta state) + `emit_tags_as_labels=true` (SN-rule tag→label extraction) — chartered by its OWN later brainstorm (anticipated **ADR-0263**).
- **Notable:** ONE new Go package (`internal/statssink`) + **ONE new go.mod module — `github.com/prometheus/client_model`** (the `MetricFamily` entry proto referenced by `StreamMetricsMessage.EnvoyMetrics`; first-party, proto-only, cached `v0.6.2`, already in go-control-plane's graph, NOT in `go.sum` today) — the FIRST new module in the Observability family (phases 44/45/46 each held ZERO-new-module); an ADR-0262 §Consequence, re-verify at the SPEC with `go mod tidy -diff` (must show ONLY `client_model`).

## The NEXT action — the 47.1 (core) SPEC (lifecycle-state 1 → 2)
Author `docs/envoy-go/phases/47-stats-sink-metrics-service/SPEC-47.1.md` (the 14-section SPEC-46.x shape). **Execute the §10 D-MS-* empirical pins IN-SESSION** against the contrib reference Envoy (`envoyproxy/envoy:contrib-v1.37.2`, ADR-0227) via the live-probe precedent (`reference_docker_probe_bridge_network` — a shared bridge + a metrics-receiver hostname reachable from BOTH containers; verify decode ran = the receiver got ≥1 `MetricFamily` from the reference):
- **D-MS-FIELDS** — the exact `MetricFamily` shape the reference emits per Counter/Gauge under the unset defaults: the `name` form (full dotted name, tags inlined — confirm cross-side name-equality for the chosen subset), `type` (`COUNTER`/`GAUGE`), `metric[].counter.value`/`gauge.value` (cumulative absolute), whether `help`/`timestamp_ms` are set (leave `timestamp_ms` UNasserted), the `identifier{node}` content; confirm NO `LabelPair` splitting when `emit_tags_as_labels` is unset.
- **D-MS-STATS** — the exact sink self-stat names + scope + `+N` (likely 0–2; note the self-referential subtlety — a self-counter appears in its own flush).
- **D-MS-LIFECYCLE** — the `StreamMetrics` framing (identifier-once; metrics in `envoy_metrics`); reconnect on receiver close; final-flush-on-drain; the default `stats_flush_interval` (5s) + how to drive a deterministic short interval.
- **D-MS-RECEIVER** — the driver-owned in-process `MetricsServiceServer` shape (accumulate `MetricFamily`s + poll/assert); plain h2c vs TLS.
- **D-MS-FLUSH** — the `stats_flush_interval` parse + default; `stats_flush_on_admin` boot-reject vs no-op; flush-loop start/stop vs `Freeze()` + the drain.
- **D-MS-MODULE** — confirm `github.com/prometheus/client_model` resolves cleanly as a direct dep (no other transitive additions; `go mod tidy -diff` = ONLY `client_model`); pin the exact version in the ADR-0262 §Consequence.
- **D-MS-SUBJECT** — the deterministic counter subset to assert cross-side EXACT (e.g. `cluster.<n>.upstream_rq_total`, `http.<hcm>.downstream_rq_2xx`/`downstream_rq_total` after K requests).
- **D-MS-SPLIT** (ADR-0045 re-check with real LoC) + **D-MS-FUZZER** (config-parse or mapping fuzzer? reconcile the 49-actual `^func Fuzz` count).
Anchor the **ADR-0262 §Context DRAFT** (§Decision/§Consequences at the 47.1 IMPL per ADR-0044). Docs-only direct-on-master; a fresh worktree off master per `feedback_git_worktrees` ONLY if code-adjacent probe artifacts land (the SPEC doc itself commits direct on master). The reviewer's note: cite the fixture count as "90 dirs under `test/fixtures/`" (the differential `os.ReadDir`s ALL subdirs incl. the non-numbered `0007a`/`0007b`), NOT a 4-digit glob.

## Counts at the phase-47 BRAINSTORM (UNCHANGED — docs-only; re-verify, do NOT assume)
stat surface **1200** (non-H2 **1196**) / fixtures **90** (tail `0088-tracing-zipkin`) / fuzzers **49** / BackendKind **38** / DECISIONS **ADR-0261** (next-free **ADR-0262**). Anticipated at the 47.1 IMPL: stat **1200+N** (N≈0–2) / fixtures **91** (`0089-stats-sink-metrics-service`) / fuzzers **49** (or +1) / BackendKind **38** (driver-owned receiver) / DECISIONS **ADR-0262**.

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** The next stage is the 47.1 SPEC. Lifecycle-state 1 → 2.
- **Subagent-driven execution** (`feedback_execution_style`) in a **fresh worktree** (`feedback_git_worktrees`) for the eventual IMPL; subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit, re-runs the full suite on the frozen HEAD, does the deliberate-break verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). Per-task gates: `gofmt -l` + `golangci-lint` on touched pkgs + `go vet` + `go build ./...` (`feedback_pertask_gofmt_lint`).
- **Process anchors:** ADR-0106 (per-leg rows) · ADR-0044 (ADR §Context-at-SPEC / §Decision+§Consequences-at-IMPL) · ADR-0045 (sub-split soft gate) · ADR-0080 (strict-reject) · ADR-0059 (the in-tree `Registry`/`Walk`) · ADR-0060 (histograms deferred) · ADR-0064 (the `stats_sinks[]` silent-ignore — phase 47 LIFTS the metrics_service sink) · ADR-0158 (the `grpcclient.Dialer` typed-wrapper layer) · ADR-0016 (proto blank-import registry) · next-free **ADR-0262**.
- **Differential discipline** (carries to the `0089` fixture): `-run 'TestDifferential/0089'` NEVER bare; `-count=1` on every break + `-race` run; the full-suite `subject ready: EOF` startup flake is isolatable (`reference_differential_fullsuite_startup_flake`); the Docker bridge + decode-ran proof (`reference_docker_probe_bridge_network`); the gRPC metrics receiver is driver-owned, NOT a BackendKind (`reference_differential_grpc_receiver_driver_owned`); assert the aggregated payload entries NOT stream framing (`reference_streaming_sink_differential_framing`); the live reference is the wire truth (`reference_wire_format_both_sides_see_same_bytes`); the `Flusher`+sink writer goroutine are background mutators ⇒ FULL-package `-race` (`reference_full_suite_race_after_background_mutator`); guard config-derived stat segments through `stats.IsValidName` (`reference_dynamic_stat_name_charset_guard`).

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-47 BRAINSTORM is DONE + LANDED; row 47 `in-progress`; the next action is the 47.1 SPEC).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 47 BRAINSTORM done`) + the recorded NEXT (the 47.1 SPEC) + the counts (1200 / 90 / 49 / 38 / ADR-0261).
3. `docs/envoy-go/phases/47-stats-sink-metrics-service/BRAINSTORM.md` — the full charter (the load-bearing facts + the §10 D-MS-* pins to execute at the SPEC).
4. `docs/envoy-go/ROADMAP.md` — row 47 (`stats-sink-metrics-service`, `in-progress`) + the Observability family-progress note.
5. `docs/envoy-go/DECISIONS.md` — ADR-0261 (the 46.2 anchor) + next-free **ADR-0262** (the 47.1 anchor to draft).
6. The phase-44/45/46 SPECs (`SPEC-44.1.md` / `SPEC-45.1.md` / `SPEC-46.1.md`) — the 14-section SPEC shape + the streaming-sink + live-probe precedents.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_subagent_autocommit_claudemd`, `feedback_pertask_gofmt_lint`, `reference_roadmap_split_phase_row_done`, `reference_differential_grpc_receiver_driver_owned`, `reference_streaming_sink_differential_framing`, `reference_strict_reject_sibling_typeurl_gap`, `reference_docker_probe_bridge_network`, plus the differential-discipline references above).
