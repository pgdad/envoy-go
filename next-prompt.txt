RESUME a fresh session for envoy-go (router last updated 2026-06-28; the **phase 46.1a (tracing header engine) PLAN is DONE and LANDED** — a docs-only commit direct-on-master authoring the 12-task TDD spine for the FIRST sub-leg of the 46.1 (core+OTLP) by-exporter leg). **D-TRACE-SPLIT was RESOLVED → SUB-SPLIT** (the ADR-0045 re-check with REAL LoC: the full 46.1 leg ≈840 prod LoC [the as-built `otlpsink.go` 216 + `OTLPLogsClient` 57 + the engine pieces] ≈2.7× the ~305-LoC one-leg 45.1 precedent — past the soft gate; **user-CONFIRMED 2026-06-28** per `reference_phase_46_tracing_split`). **ROW 46 (`tracing`) STAYS `in-progress`** (46.1a is NOT the final leg — row 46 flips `done` only at the 46.2 IMPL per ADR-0106 + `reference_roadmap_split_phase_row_done`); the Observability FAMILY STAYS OPEN (Zipkin/B3 [46.2] / stats sinks / tap remain rows). **The next action is the 46.1a IMPL** (subagent-driven in a fresh worktree per `feedback_execution_style` · `feedback_git_worktrees`).

The PLAN (`docs/envoy-go/phases/46-tracing/PLAN-46.1a.md`) was authored in the house TDD-task-spine shape (12 tasks, mirrors `PLAN-45.1.md`/`PLAN-45.2.md`) + **plan-document-reviewer APPROVED first-iteration** (ALL load-bearing claims verified TRUE against the real tree: the HCM `Tracing` field tags 3–10 + `OpenTelemetryConfig` 1–5 against the resolved `go-control-plane/envoy v1.32.4`; the as-built seams `internal/filter/hcm/config.go` [`parseFilterWithCtx :202`, HCM unmarshal `:206`, `IsValidName` guard `:235`, the 5 `downstream_rq_*` counters + registration `:285–295`, `Filter` struct `:91`], `connection.go:448` dispatch + the H2 `h2dispatch.go` `WriteH2 :282` analogue, `accesslog_emit.go:19`/`:45`, `router.go:763`, `grpcclient.go:319–375`, `otlpsink.go` 216 LoC, `HTTPHeaderMutation = 9`, the `0084` driver shape; `hcm.GetTracing()` NOT called in production today; the UUID string-index-14 = version-nibble arithmetic; the `Decide` precedence consistent with the SPEC §11 pins; NO task references a 46.1b-only artifact). **3 advisory recommendations folded into the plan** (the `Filter`-struct `:81`→`:91` drift; the Task-1 baseline-grep reworded to flag the UNRELATED `extauthz` `x-request-id` reader so it isn't read as a failed precondition; the Task-9 H2-inject value-copy `WriteH2`/re-`SetH2Request` caution).

HEAD is the **PLAN-landing commit `3d8305ad`** (the `PLAN-46.1a.md` + the STATE active-phase stage-advance, ONE docs-only commit direct-on-master, pushed to origin) atop the prior `next-prompt.txt` roll `48a6a3ac` atop the SPEC-landing `3d5fdb22` atop the phase-46 BRAINSTORM `02deeb26`. **Re-read the shas with `git log --oneline -10`, do NOT assume** — a further docs-only `next-prompt.txt:` roll commit may stack ATOP this one. Confirm `git status` clean (the untracked `.claude/` is session cruft, leave it) + master == origin/master.

Counts at the PLAN (the exit invariant — re-verify, do NOT assume; UNCHANGED, docs-only): stat surface **1191** (H2 cluster; non-H2 **1187**) / fixtures **87** (`ls -d test/fixtures/[0-9][0-9][0-9][0-9]* | wc -l` == 87; tail `0085-otlp-access-log-operators`) / fuzzers **46** (`grep -rh '^func Fuzz' --include='*.go' . | wc -l` == 46) / BackendKind tail **38** / DECISIONS tail **ADR-0259**, next-free **ADR-0260**.

## NEXT STEP — the 46.1a (tracing header engine) IMPL
Build the 12-task TDD spine in `PLAN-46.1a.md` end-to-end, subagent-driven (`superpowers:subagent-driven-development`) in a FRESH git worktree off master (`feedback_git_worktrees`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the controller squashes + pushes at stage-close (`feedback_push_to_origin`). Per-task gates on every code task (`gofmt -l` + `golangci-lint run` + `go vet` + `go build ./...`, `feedback_pertask_gofmt_lint`); the differential `-run 'TestDifferential/0086'` selector (NEVER bare `0086`, `reference_differential_run_selector`); `-count=1` on every deliberate-break + `-race` run (`reference_differential_break_protocol_count1`); the full-package `-race` (`reference_full_suite_race_after_background_mutator`).

**The 12-task spine (see `PLAN-46.1a.md` for the full TDD steps):** T1 PROGRESS scaffold + baselines + the D-TRACE-SPLIT re-check record · T2 the `internal/tracing` skeleton (`TracingConfig` + the `RandSource` seam, prod `CryptoRand`) · T3 the `x-request-id` engine (generate/preserve/byte-14 stamp, NoTrace=`4`/Sampled=`9`/Client=`b`) + `FuzzStampRequestID` · T4 W3C `traceparent`/`tracestate` extract+inject + `FuzzExtractTraceparent` · T5 `tracing.NewConfig` proto→config + the 8 STRICT-REJECT arms (non-OTel provider / `google_grpc` / empty-cluster / `custom_tags` / `verbose` / `max_path_tag_length` / `spawn_upstream_span`-if-present / OTel `http_service`+`resource_detectors`+`sampler`) · T6 `tracing.Decide` (the full §11 precedence: continued-bypasses-caps → client-force-gated-by-`client_sampling` → `random_sampling` → `overall_sampling`-caps-local) + the `config/trace/v3` blank-import (ADR-0016) · T7 the 5 `RegisterHCMCounters` (`client_enabled`/`not_traceable`/`random_sampling` LIVE; `health_check`/`service_forced` register-but-0) + `Record` dispatch · T8 the HCM parse wiring (LIFT `hcm.GetTracing()`; no-tracing ⇒ no counters byte-stable) · T9 the HCM dispatch wiring (decision + `x-request-id` stamp + `traceparent` inject + counter, H1+H2 — **the H2 `WriteH2` takes `h2req` BY VALUE; re-`SetH2Request` after mutating**) + full-package `-race` · T10 the `0086-tracing-request-id` differential (the `HTTPHeaderMutation` Kind-9 echo backend; cross-side `x-request-id` nibble + `traceparent` continuation + subject `tracing.*` counters; NO receiver) · T11 deliberate breaks (`-count=1`) + 20/20 flake + full-package `-race` · T12 full 88-dir + six-gate + BEHAVIOR_CONTRACT + STATE/ROADMAP (row 46 STAYS `in-progress`; ADR-0260 body DEFERRED to 46.1b) + fuzzer reconcile.

**Exit invariant (the 46.1a IMPL — re-verify empirically, do NOT assume):** stat surface **1196** (+5 — `http.<stat_prefix>.tracing.{client_enabled,health_check,not_traceable,random_sampling,service_forced}`; DYNAMIC — register only under a tracing HCM; non-H2 **1192**) / fixtures **88** (`0086-tracing-request-id`) / fuzzers **48** (`FuzzStampRequestID` + `FuzzExtractTraceparent`; reconcile `^func Fuzz` per `reference_fuzzer_count_docs_drift`) / BackendKind **38** (UNCHANGED — header-echo reuse) / DECISIONS **ADR-0259** (ADR-0260 §Decision/§Consequences body lands at the 46.1b IMPL, the completing sub-leg). ONE new Go package (`internal/tracing`); ZERO new go.mod modules (`go mod tidy -diff` EMPTY). After the squash+push: roll `next-prompt.txt` to re-anchor on the 46.1a IMPL squash + route to the **46.1b PLAN** (span + `OTLPTracesClient` + `OTLPExporter` + `test/helpers/otlptrace` + `0087-tracing-otlp` + the ADR-0260 body that CLOSES the leg).

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** The next stage is the 46.1a IMPL (subagent-driven in a fresh worktree; the controller squashes + pushes at stage-close, then rolls the router).
- **Process anchors:** ADR-0106 (per-leg roadmap rows; row 46 STAYS `in-progress`) · ADR-0045 (the D-TRACE-SPLIT SUB-SPLIT, now RESOLVED) · ADR-0041/0080 (silent-ignore vs strict-reject — 46.1a LIFTS the HCM `tracing` field from silent-ignore) · ADR-0016 (proto blank-import registry — the `config/trace/v3` import) · ADR-0158 (the `grpcclient.Dialer` + typed-wrapper layer — the 46.1b `OTLPTracesClient` precedent) · ADR-0258/0259 (the phase-45 OTLP sink + operator engine — the OTLP module + the unary-Export batching-sink the 46.1b exporter reuses) · ADR-0044 (ADR §Context-at-SPEC [done, SPEC §13], §Decision/§Consequences body-at-IMPL — lands at 46.1b, NOT 46.1a) · ADR-0227 (the contrib reference) · next-free **ADR-0260** (CLOSED at the 46.1b IMPL), then **ADR-0261** (46.2 Zipkin+B3).
- **The substrates the 46.1a IMPL builds on (verified at the PLAN review):** the HCM filter `internal/filter/hcm/config.go` (`Filter` struct `:91`, `parseFilterWithCtx :202`, HCM unmarshal `:206`, the `IsValidName` stat_prefix guard `:235`, the 5 `downstream_rq_*` counters + registration `:285–295` — the home for the `tracing` parse arm + the 5 `tracing.*` counters) · the dispatch seams `connection.go:448` (H1) + `h2dispatch.go` `WriteH2 :282` (H2, value-copy) · the no-`x-request-id` invariant `router.go:763` (REVISED only under a configured provider) · the `0084-otlp-access-log/driver` driver-owned-assertion shape (the `0086` copies it, swapping the receiver for the `HTTPHeaderMutation` echo parse) · the `HTTPHeaderMutation = 9` backend (`test/fixtures/0012-http-header-mutation/backends/backend.go` echoes sorted `"name: value\n"`) · `runner_test.go:111–112` (the fixture-driver blank-import block). NEW: the `internal/tracing` package; NO new BackendKind, NO receiver (those are 46.1b).

## Read first (in order)
1. THIS FILE — the cold-start router (the 46.1a PLAN is DONE + LANDED + pushed; row 46 `in-progress`; the next action is the 46.1a IMPL, subagent-driven in a fresh worktree).
2. `docs/envoy-go/phases/46-tracing/PLAN-46.1a.md` — the 12-task TDD spine (the deliverable to execute), incl. the Orientation brief + the Key-source-seams block + the D-question resolutions.
3. `docs/envoy-go/phases/46-tracing/SPEC-46.1.md` — the charter (the §11 EXECUTED D-TRACE-* empirical pins, the §13 ADR-0260 §Context draft).
4. `docs/envoy-go/STATE.md` — the active-phase header (`phase 46.1a PLAN done`) + the recorded NEXT (the 46.1a IMPL) + the count figures.
5. `docs/envoy-go/ROADMAP.md` — row 46 (`tracing`, `in-progress`) + the `### Observability family` heading.
6. `docs/envoy-go/DECISIONS.md` — **ADR-0259** (45.2 as-built) + **ADR-0258** (45.1) + next-free **ADR-0260** (the 46.1 anchor — body lands at the 46.1b IMPL).
7. `docs/envoy-go/phases/45-otlp-access-log/PLAN-45.1.md` / `PLAN-45.2.md` — the PLAN/IMPL house shape (the TDD-task-spine the 46.1a IMPL executes).
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `reference_phase_46_tracing_split`, `reference_differential_grpc_receiver_driver_owned`, `reference_dynamic_stat_name_charset_guard`, `reference_wire_format_both_sides_see_same_bytes`, `reference_differential_run_selector`, `reference_differential_break_protocol_count1`, `reference_full_suite_race_after_background_mutator`, `reference_fuzzer_count_docs_drift`, `feedback_execution_style`, `feedback_git_worktrees`, `feedback_subagents_no_push`, `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`).
