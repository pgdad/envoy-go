=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

**THERE IS BANKED MID-LIFECYCLE WORK AT THIS TIP.** Phase 80 (`stats-sds-projection`) is **OPEN** — row 80 is `in-progress` and `BRAINSTORM.md` + `SPEC.md` + `PLAN.md` + `PROGRESS.md` (PLAN half) are landed. **This session performs the phase-80 IMPL — the row's CODE, and the stage that CLOSES the phase.** Do NOT self-pick a new subject.

⚠️ **THE IMPL IS THE FIRST PHASE-80 STAGE THAT TOUCHES `.go`, `ROADMAP.md`, `BEHAVIOR_CONTRACT.md` AND `DECISIONS.md`.** Every prior stage was docs-only. The leak check, the six-gate, per-task `gofmt`/`golangci-lint` and the full 120-fixture differential are **ALL live this session**.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=112 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **`want=112` IS A LIVE FIGURE, NOT A CONSTANT.** It is the ROADMAP data-row count. **A row-adding phase MUST bump it in the same commit that adds the row** — otherwise every later session gets `GATE FAIL: examined 113 data rows, expected 112`. That noise is deliberate: a wrong denominator must be loud, because a silent one is how check (1) failed in the first place. **The phase-80 BRAINSTORM bumped 111 -> 112 when it chartered row 80. The SPEC and the PLAN each left it at 112.** ⚠️ **THE IMPL ADDS NO ROW EITHER — it FLIPS row 80's `status` cell `in-progress` -> `done`, so `want` STAYS 112.**

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. When you open a new family, add its row-summary slug here.

**SENTINEL RE-RUN MECHANICALLY ON MASTER AT `47b9b378` (2026-07-30), with FIRING negative controls. IT DOES NOT FIRE. `stop` was NOT created; `ls stop` => `No such file or directory`, and it MUST NOT be created.** ⚠️ **Re-run it yourself anyway and record what YOU see** — a prior run's result is not evidence for the current tip. ACTUAL output, recorded rather than predicted:
- **(1) prints `NOT DONE: row 80`** at `want=112` — correct, row 80 is `in-progress`. NCs, both fired: `want=111` => `GATE FAIL: examined 112 data rows, expected 111`; doctoring row **76** to `in-progress` on a scratch copy => `NC NOT DONE: row 76` **alongside** row 80 (the script self-reported `rows doctored: 1`).
- **(2) FIVE — `:190 :200 :210 :216 :224`** (long form at the first four, the Operational-tooling short form at the last) — **UNCHANGED**. **The twenty-sixth consecutive phase at which check (2) did not go down. STATED, not forecast.**
- **(3) `NEVER OPENED: gRPC` and `NEVER OPENED: WASM`** — unchanged. NCs: an invented slug => `NC NEVER OPENED: ZZZ-nonexistent`, while the REGISTERED slug `Observability` correctly printed nothing.
- Input measured at **228 lines / 112 data rows / 13** bare `candidates:` hits (against the sentinel's own narrower 5), so an empty result could not read as a zero result.

⚠️ **THE LEAK CHECK IS LIVE THIS SESSION — IT RE-ARMS AT THE IMPL, WHICH AUTHORS ROW 80'S CLOSING CELL.** The rule: **NEVER WRITE A SENTINEL'S OWN MATCHER STRING INTO A FILE THE SENTINEL GREPS.** The sentinel greps `docs/envoy-go/ROADMAP.md` — writing these strings *here* is safe; writing them *there* is not. And the leak is proven to **SILENCE CHECK (3) BY MENTION**, not merely to be greppable: doctoring a cell to say `gRPC-family row` made `gRPC` stop printing while `WASM` kept printing. **`grep` cannot tell a mention from a use.**
```sh
# the row's cell only, not the whole file
awk -F'|' '/^\| *80 /' docs/envoy-go/ROADMAP.md > "$SCRATCH/row80.txt"
grep -coE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' "$SCRATCH/row80.txt"  # want 0
grep -oiE '(HTTP-filters|Network-filters|Load-balancing|Upstream-robustness|Observability|Operational-tooling|HTTP/3|gRPC|xDS|Runtime|WASM)-family row' "$SCRATCH/row80.txt" | sort -u
# ^ every slug printed MUST already be registered elsewhere in ROADMAP.md. NC both greps on a doctored copy,
#   AND re-run check (3) on the doctored file to prove the slug would actually silence it.
```
⚠️ **ROW 80'S CELL MUST ALSO BE WELL-FORMED.** Rows 57, 69 and 78 all lose summary content in GFM render, and **row 78's `NF==8` PASSES because an unescaped inner `|` and a missing trailing `|` CANCEL** (broken-gate shape sixteen). **Escape inner pipes; terminate the row with `|`.**

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt` — RE-CONFIRMED LIVE AT THE PLAN

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files --hidden -I --exclude-dir=.git …` in ugrep-compatible mode. **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Reproduced at the PLAN tip — **wrapped 9 files, GNU 10, delta EXACTLY `next-prompt.txt`, direct access 4 hits:**
```sh
grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort         # wrapped: ABSENT
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                    # direct access WORKS => 4
```
⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING THE TWO ARMS.** The wrapped arm prints bare paths while GNU prints `./`-prefixed ones, so a naive `comm` reports **all 10 as "only in GNU"** and the real delta is invisible. **That happened at the PLAN** and was caught by re-running with `sed 's|^\./||'`. ⚠️ **Print the FILE NAMES, never a count.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** — it reports `next-prompt.txt` as *"not ignored"* (exit 1) because **git exempts TRACKED files from `.gitignore`** while ugrep does not. **Reliable forms: `command grep -r`, `git grep`, an explicit file list, or a recursive grep handed the FILE path directly.**
⚠️ **THE IMPL'S STALE-COUNT SWEEP MUST USE `git grep` AND MUST USE `-i`** — see item 5.

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`.** Every document from phase 75 forward recorded its blind spot — **109 data rows / 105 matched / FOUR misses** at an **em-dash** in an "after" cell, a **DOT** in the slug `http-1.1`, and two **LETTER-suffixed** ids `28.1a`/`28.1b` — and every one of them called it *"currently HARMLESS, since all four missed rows are `done`."* ⚠️ **Nobody fired the unsafe arm until the phase-77 PLAN. EXECUTED on scratch copies: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** ⚠️ **DO NOT restore the regex form.**

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

The old check (2) matched ONLY the long phrase and could not see the **Operational-tooling** family's three genuinely un-chartered items, recorded in the **SHORT** form. Fixed at the phase-77 BRAINSTORM, at the MATCHER. ⚠️ **Run the union form above, never one half of it.** ⚠️ **A ONE-ARM STRIP IS NOT AN NC FOR CHECK (2)**: `sed 's/deferred candidates://'` moves the union count **5 -> 4, not 5 -> 0**, because the long form does not contain the short substring at all. **Re-confirmed at the PLAN tip by direct test: `printf 'remaining deferred (not-yet-chartered) candidates:\n' | grep -c 'deferred candidates:'` => 0.**

⚠️ **THE DOCUMENTED TRAP'S STATED MECHANISM IS REFUTED, WHILE THE TRAP IS REAL.** `reference_sentinel_deferred_sentence_live_vs_historical` says a naive `grep -c 'candidates:'` inflates *because of* the `candidates were:` recaps. It does inflate — but **not for that reason**: `candidates were:` does not contain that substring **at all**. The inflation comes from **ROADMAP table rows carrying BRAINSTORM prose**. **Cite the command; do not re-inherit the explanation.**

⚠️ **AND THE MEASURED RECORD: CHECK (2) HAS NEVER GONE DOWN** across ~26 phases (history `0 -> 1 -> 3 -> 4 -> 5`). => **the candidates sentence is a WINDOW onto a larger deferred backlog, not an inventory of it.** ⚠️ **A stage that predicts a decrease here is repeating the phase-73 error. State what your row narrows — usually nothing — rather than forecasting.**

⚠️ **THE MENU IS NOT COSTED — MEASURED.** `grep -obE '[0-9]+(-[0-9]+)? tasks'` returns **no match** on FOUR of the five paragraphs: HTTP/3 (7 uncosted candidates), xDS (9), Runtime (4) and Operational-tooling (3) carry **zero** task bands. **Only the Observability line carries costs.**

⚠️ **THE SELECTION CALCULUS, CORRECTED AND STILL STANDING.** *"A candidate that clears a check-(3) blocker is worth **strictly more**"* does **not** hold: opening a family **MINTS** a check-(2) sentence, so phase 77's own arithmetic was **NET ZERO**. What survives: **opening a family is the ONLY way that family's work becomes enumerable at all — a prerequisite, not a shortcut.**

==============================================


## STATUS AS OF 2026-07-30: PHASE 80 IS **OPEN**. ROW 80 IS `in-progress`. THIS SESSION PERFORMS THE **PHASE-80 IMPL**, WHICH CLOSES THE PHASE.

**What the phase-80 PLAN landed.** Docs-only, **FIVE files**: `phases/80-stats-sds-projection/PLAN.md` (NEW, **13 tasks**) · `PROGRESS.md` (NEW, the PLAN half) · `STATE.md` (§Current rolled IN PLACE) · **`STATE_HISTORY.md` 426 -> 428** (the mandatory eviction; body `cmp`-verified byte-identical) · this file. **ZERO production `.go`, ZERO test `.go`. `ROADMAP.md`, `BEHAVIOR_CONTRACT.md` and `DECISIONS.md` BYTE-UNTOUCHED.** Base master **`47b9b378`** — ⚠️ the PLAN had to correct the previous router, which named `f2dd994a`; **`47b9b378` is the router-roll commit sitting on top of it**, and branching off `f2dd994a` would have silently discarded it (`git diff --name-only f2dd994a..47b9b378` => `next-prompt.txt` ONLY, so every doc count survived).

### What matters for the NEXT session, in order

1. **⚠️ RE-RUN ALL THREE SENTINEL CHECKS YOURSELF AND RECORD THE ACTUAL OUTPUT; DO NOT PREDICT IT.** Create `stop` if and ONLY if all three print nothing. Check (1) prints `NOT DONE: row 80` until you flip it, so the sentinel cannot fire before your own edit. **`want` STAYS 112.** **Re-run the sentinel AFTER the ROADMAP flip, together with the leak check.**

2. **⚠️ THE DESIGN AND THE PLAN ARE SETTLED. DO NOT RE-OPEN THEM.** Read `PLAN.md` in full and execute its **13 tasks**.
   - **D-SDS-1: SN1-shaped, FIRST-DOT, single-segment, non-greedy**, label `envoy_xds_resource_name`, **no dot->underscore pre-transform in the arm** (`flattenToProm` does it at projection time), inserted after the `tracing.` arm (`name.go:138`) and before `wasm.` (`:139`). Reference-pinned against the binary's own extractor literal `^sds\.((<TAG_VALUE>)\.).+`.
   - **D-SDS-2: a boot-boundary REJECT — ⚠️ PINNED AT `boot.go:196`, BEFORE `grpcclient.NewSDSClient` (`:196-199`), not merely "before `RegisterSDSStats`" (`:200`).** The SPEC's wording admits a placement where **the dial error MASKS the name reject** (measured: **179 B** name error before the dial vs **128 B** dial error after it; and an SDS-less boot dies at `connection refused`, 332 B). That placement would make G4's positive arm require a live SDS server to observe. ⚠️ **Guard the segments on the ASSEMBLED name** — the SPEC's predicate as literally worded **passes the EMPTY string** (`sds..init_fetch_timeout`, which `IsValidName` ACCEPTS).

3. **⚠️ THE THREE PLAN FINDINGS THAT CHANGE WHAT YOU BUILD.**
   - **The fixture assertion needs a SPLIT ROSTER.** SPEC §7.2's *"values per-side only (`>= 1`)"* is **RED ON ARRIVAL**: measured 4/4 runs, `update_failure`, `update_rejected` and `init_fetch_timeout` are all **0 on the SUBJECT**. Ship `sdsProjectedNames` (**5** — name + hoisted label) and `sdsMovedNames` (**2** — `update_attempt`, `update_success` — value floor). ⚠️ Reference `update_attempt` is **3, not 2**; the reference `sds.*` roster is **14 flat / 12 prom, not 10/9**; `update_failure` is a genuine **1 vs 0** cross-side divergence the SPEC never names. **Assert NAME + LABEL cross-side; NEVER values cross-side.**
   - **T4 is a FORCED ATOMIC MERGE and must NOT split.** All four sink goldens fail off ONE roster entry — the tag literal is hard-coded at **four** sites (`golden_bytemirror_test.go:167 :208 :253 :354`) and `goldenName` is a **POSITIONAL** struct literal, so adding a per-entry tag field rewrites all 14 roster lines. **The SPEC's "T5" does not exist as a separate task**, which is why the enumeration is 13 and not 14.
   - **`0110`'s OWN documents FORBID reaching into the `sds.*` scope** as *"inherently unstable"* (`expectations.yaml:210-215`, `driver/driver.go:643-646`). Measured: the mechanism is real (`DriveSubject` hard-stops both SDS receivers, which is why the reference reads `attempt=3 / failure=1`) but **the instability lands on VALUES, not on names or labels** — stable 4/4. ⇒ name+label-only is what makes the assertion legal, and you owe a **prose reversal** in `0110/expectations.yaml:210`, `0110/README.md:178` (+ `0111`'s pair if mirrored). ⚠️ **Nothing parses `0110`'s `expectations.yaml`, so omitting the flip is SILENT.**

4. **⚠️ THE EXISTING GATES ARE NEARLY BLIND — BUILDING THE GATE IS THE ROW'S MAIN JOB.** Under an experimental arm the whole of `./internal/stats/... ./internal/statssink/...` fails **EXACTLY ONE** test (`TestTerminalError_TopLevelCountMatchesCode`); `internal/statssink` stays fully green. **ALL FOUR** phase-79 OTLP byte goldens are **unmoved**, because `goldenRoster`'s 13 entries carry **no `sds.*` name** — broken-gate shape **SEVENTEEN**: the command, the assertion and the cross-product are all correct and the **input roster** is the defect. **Extending the roster UN-BLINDS all four** (measured at the PLAN: 1134->1212 · 1200->1320 · 1118->1184 · 1184->1292). ⚠️ **RE-MEASURE the four `wantBytes`; do NOT compute them, and do NOT inherit the PLAN's numbers** — OTLP absolutes carry an unnamed 15-char version string, so absolutes are tree-local while deltas reproduce. ⚠️ **The reject's OWN blast radius is ZERO** (`go test` over **124** packages => exit 0) — a DIFFERENT measurement from the arm's "one failing test". **Do not conflate them.**

5. **⚠️ THE STALE-`TWELVE` SWEEP: 16 OCCURRENCES / 10 FILES, AND TWO TRAPS WOULD SILENTLY DEFEAT IT.**
   - ⚠️ **USE `-i`. FOUR of the six live `0118` hits are UPPERCASE `TWELVE`** — a case-sensitive sweep finds **2 of 6**. The `0118` six are confirmed exactly (`README.md` ×2, `driver/driver.go` ×2, `expectations.yaml` ×2; `envoy.yaml` => **0**). **The fixture is NOT de-fanged** — every one enumerates the roster.
   - ⚠️ **`BEHAVIOR_CONTRACT.md:167` carries TWO `twelve` with OPPOSITE verdicts** — one historical (*"enumerated the pre-79 nine while twelve were live"*, **must NOT change**) and one live (*"The message now enumerates twelve top-level segments"*, **must change**). **A line-level substitution corrupts one.**
   - ⚠️ **TWO CELLS THE SPEC MARKS LIVE-EDITABLE ARE HISTORICAL AND MUST NOT BE TOUCHED:** `internal/stats/segmentcount_test.go:21` (SPEC §6.1 item 9 — past tense, and the file hard-codes **no numeral at all**) and `docs/envoy-go/STATE.md:48` (item 13 — a §Recent entry that migrates VERBATIM at eviction).
   - ⚠️ **`internal/stats/name.go:490` reads *"Of the 25 entries"*** — `helpText` goes 25 -> 30, so **this is a live stale count no phase-80 document flagged.** It also carries a pre-existing unrelated **`13`** that a post-move numeral grep collides with. **Anchor on the phrase, not the numeral.**
   - Needle figures re-derived: numeral `one of the 12` => **6 hits / 6 files** at the PLAN tip (the SPEC's 4/4 was exact at ITS base, stale by its own commit); bare `\btwelve\b` => **43 files** (⚠️ **NOT 45 — see item 11**); scoped spelled needle => **33 lines / 19 files** (the SPEC's "26/20" had the FILE count wrong even at its own base: 26/18).
   - ⚠️ **SPEC §9 and §11 both cite "§6.1 item 15". §6.1 enumerates items 1-14.** A phantom item, cited twice.

6. **⚠️ COST: BAND 11-14, BUDGET 13 — the SPEC's 12-15/13 does NOT survive**, and calibration says read 13 as *expect 14* (three of four recent rows landed AT or ABOVE the SPEC ceiling, none below the floor: 76 ~7-9 -> 9 · 77 11-13 -> 12 · 78 7-9 -> 10 · 79 10-12 -> 12; PLAN counts mechanical via `^## Task [0-9]`). **NO SPLIT: 13 << ~25 and ~640 net `.go` << ~1500.** ⚠️ **SPEC §9.3's *"855 x 1.75 ~= 1496 ⇒ ON the ~1500 line"* IS ARITHMETIC ON MISMATCHED BASES** — phases 77/78 carry **NO SPEC LoC estimate at all** (case-sensitive `LoC` => 0 in both SPECs), so 2.040x/1.500x are *realized-over-**PLAN*** and cannot be applied to a SPEC estimate; the only SPEC->realized datapoint is phase 79 at **×3.06**, n=1, and both nearest comparables realized **net 1406 and 1446 — UNDER the line, without splitting.** ⚠️ **§6.1 gates the PLAN's own estimate of NET CHANGE**, not estimate × multiplier. ⚠️ **The SPEC's T4/T10 seam is INCOHERENT** (no shared file, package or dependency — two unrelated leaves, not §6.2's *"coherent slice"*); the natural seam is projection leg (T1-T4, T9) / validation leg (T5-T8). ⚠️ **The THIRD §6.1 trigger (mid-execution, >~10 sub-steps) IS live on T4** — if it blows past ~10 sub-steps, **RECORD it**. ⚠️ **Normative cite: the heading anchor `### 6.1 When to split` at `:285` of `/BOOTSTRAP_PROMPT.md` — AT THE REPO ROOT, not under `docs/envoy-go/`** (`:289` the ~25-task trigger, `:290` the ~1500-LoC trigger, **`:291` BLANK**, `:292` the third trigger). Six-gate cites verified EXACT: **§7.5 heading `:357`, gates (a)-(f) `:360-365`, close `:367`**. `ADR-0045` QUOTES rather than states; `ADR-0106` defines nothing about phase-done gates.

7. **⚠️ THE BREAK ROSTER IS PLAN TASK 11, AND ALL FOUR CALIBRATION PLANS SHIPPED ONE (4/4).** Twelve arms are specified, each with the assertion it must fire. ⚠️ **This row's breaks are unusually hazardous BECAUSE of item 4** — the tree is nearly blind to the arm, so an arm that "fires" may be firing something unrelated. **Each arm must name WHICH assertion fired.** The two most valuable are **NEGATIVE results**: arm **κ** (the OTLP `(F,F)` cell with the roster UN-extended — **nothing fires**, the shape-seventeen vacuity demonstration) and arm **λ** (a blanket `>= 1` on all five names — **reds on THREE**, SPEC §7.2 as an executable arm). ⚠️ Arm **β** (last-dot instead of first-dot) must fire **G6 ONLY** and leave `0110` **GREEN**, which is §2.8's "the fixture cannot discriminate the fork" demonstrated rather than argued. ⚠️ **Breaks run AFTER committing**, with **`-count=1`**; **PLAN break instructions don't compile** — substitute an equivalent and record the substitution.

8. **⚠️ ADR + DOCUMENT CONVENTIONS — AND THE SPEC'S OWN ADR BLOCK IS STALE.** Next-free is **ADR-0303**; tail is **ADR-0302**, STATUS **PROPOSED** (§Context only). **`grep -c '^## ADR-0303'` => 0** (NC: `^## ADR-0302` => 1). **301** `^## ADR-` headings, ids **0001-0302**, **exactly one gap at ADR-0209**, zero duplicates — *"301 ADRs"* and *"tail ADR-0302"* are both true and must not be conflated. ⚠️ **SPEC §8 item 7 says 300 headings / ids 0001-0301 / tail ADR-0301 / `^## ADR-0302` => 0 — STALE BY THE SPEC'S OWN COMMIT** (`7d014546` added ADR-0302, 34 lines). **Do not inherit it.** The IMPL **appends §Decision + §Consequences IN PLACE (ADR-0044-as-used), no renumber, no `---` separator, AND MUST FLIP ADR-0302's STATUS `PROPOSED` -> `COMPLETE`.** ⚠️ **The retained italic footer IS present** as the last line of §Context at `DECISIONS.md:17692` — `*(§Decision + §Consequences land at the phase-80 IMPL.)*`, `od`-verified (U+00A7 = `302 247`, no leading/trailing whitespace, no `**`, newline-terminated; **all 8 footers in the file normalize to ONE string**) — **RETAIN it after the append.** ⚠️ **The footer-carrying block is `ADR-0294 … ADR-0300` — SEVEN** (per-block scan: 0293=0, 0294-0300=**1 each**, **0301=0** the recorded departure, 0302=1). **ADR-0301's STATUS says *"seven blocks"* then names the SIX-block range *"ADR-0295 through ADR-0300"*. Copy the FORM, not the miscount.** ⚠️ **CARRY NO WHOLE-FILE GREP COUNT** in an ADR — that species self-falsified in ADR-0296 ¶3 and twice in ADR-0297.

9. **⚠️ THE `79.1` REPAIR IS FOUR OCCURRENCES ON THREE LINES, AND THE SPEC NAMES ONLY THREE PHRASES.** All four are in `BEHAVIOR_CONTRACT.md`, at **`:158`, `:5078`(×2), `:5080`**. The SPEC's anchors cover `:5078`×2 and `:5080` only. ⚠️ **THE FOURTH IS UNNAMED: `:158` — *"That asymmetry, not scope, is why the `sds.` family is deferred to row 79.1"*.** A phrase-anchored sweep trusting the SPEC's list leaves it stale. **Anchor on the PHRASES, never the line numbers.** Full roster at this tip: **61 occurrences / 10 files** (the SPEC's 50/9 was exact at ITS tip) — `ROADMAP.md` row 79 ×3 **INVARIANT-BLOCKED** by §Schema and named as a permanent discrepancy · phase-79 phase documents ×25 **HISTORICAL, must NOT be rewritten** · **29 in this row's own documents** (the SPEC said ~11). ⚠️ **`BEHAVIOR_CONTRACT.md` pre-79 cites are stale by `+54` only up to old line ~5023; a second hunk adds six more, so cites at or beyond old `:5024` are stale by `+60`.** A flat `+54` lands six lines short in the tail. **Prefer symbol anchors to any offset.**

10. **⚠️ THE FULL 120-FIXTURE DIFFERENTIAL IS MANDATORY for any row touching `internal/stats`** — it links into `cmd/envoy-go` at `test/differential/harness.go:240`, `:594` and `test/conformance/h2spec/h2spec_test.go:210`; **h2spec is a FOURTH consumer NOT covered by `./test/differential/`** — run it **explicitly**. Budget **~400-430 s** per green attempt (measured `f2dd994a`: **403.2 s** locally, **427.8 s** in CI); `-race` is a SECOND run. ⚠️ **CI runs this job at `-timeout 20m` with job `timeout-minutes: 30`** — if your local run approaches either, that is a finding, not a budget to raise reflexively. The recipe, with `comm -3` as the load-bearing gate (120 with one fixture renamed and another skipped still reads 120):
    ```sh
    ( go test ./test/differential/ -count=1 -v > "$SCRATCH/full.log" 2>&1; echo "INNER_EXIT=$?" )
    grep -cE '^    --- PASS: TestDifferential/' "$SCRATCH/full.log"          # want 120
    grep -E  '^    --- (FAIL|SKIP): TestDifferential/' "$SCRATCH/full.log"   # want EMPTY
    grep -c  'no driver registered for fixture' "$SCRATCH/full.log"          # want 0
    grep -o  'TestDifferential/[^ ]*' "$SCRATCH/full.log" | sed 's|TestDifferential/||' | sort -u \
      | comm -3 - <(ls -1 test/fixtures/ | grep -E '^[0-9]{4}[a-z]?-' | sort)  # want EMPTY
    ```
    ⚠️ The faithful dir predicate is `^[0-9]{4}[a-z]?-` — a bare `^[0-9]{4}-` gives **118**.
    ⚠️ **`go test ./test/differential/...` (with `...`) matches TWO packages, so `go test` BUFFERS each package's `-v` output until that package finishes.** A run that looks hung for 7 minutes with an empty log is normal, not a stall — do not kill it and do not conclude "no output" means "no progress".
    ⚠️ **Stat surface: assert the DELTA, expected +0.** The row **PROJECTS** existing names and registers none. **1207 is DOCUMENTARY and CONFIG-CONDITIONAL.** ⚠️ **The `TestNoNewStat*` guards are PROVEN BLIND to `internal/stats`** (all five live in `internal/statssink/registration_test.go` and none reaches it): **enumerate the diff's registration call sites and show the set is EMPTY.** Adding a `switch` arm is a **PROJECTION** change, not a **REGISTRATION** change.

11. **BROKEN-GATE COUNT IS EIGHTEEN.** Shape **eighteen**, found at the PLAN: **A NEGATIVE CONTROL POINTED AT A TARGET THAT DOES NOT EXIST.** `go list -deps ./internal/conv` **exits 1** with `directory not found` and prints nothing — and the resulting **`0` hits reads exactly like "the NC discriminates."** `internal/conv` has **0 tracked files**. **Distinct from all seventeen priors: the command is right, the assertion is right, and the NC's SUBJECT is a phantom.** ⚠️ **It fired TWICE inside SPEC §4.1** — the original NC was vacuous and the sentence announcing the re-run introduced a second one — and it also means **§4.1 CHECKS THE WRONG PACKAGE'S EDGE** (the reject lands in `internal/boot`, not `internal/xds`; the boot edge holds, and `internal/stats` **and** `strings` are already imported => **zero import changes**). **Remedy: an NC must assert its own target EXISTS and its command EXITED ZERO before its empty output is read as a result.** A real NC (`./internal/clock`: exit 0, 47 deps, 0 hits) discriminates. The seventeen carried forward are unchanged: two defects that CANCEL in the gate metric (`NF==8` passes row 78) · an inert gate cell · a full-suite recipe without `-v` is VACUOUS · a sha256 roster desynced against a DELETED file · `gofmt -l` NEVER exits non-zero (gate on OUTPUT) · `go doc -all <A> <B>` swallows arg2 · a `+0 exported symbols` gate over an EMPTY package reds on a CORRECT tree · a RANGE gate cannot detect anchor drift · a roster's naive `[ -f ] || continue` exits 0 on a DELETED file · a count-only stat guard PASSES a build with BOTH names wrong · a `-run` no-match exits 0 with `[no tests to run]` · a `--- PASS` tally over a package with sibling tests exceeds the fixture denominator · a stat-delta claim cannot be discharged by guards scoped to another package · a stderr-VOLUME assertion passes on the hang · `golangci-lint` runs `misspell` with `locale: US` · **a harness's exit code is not the command's** (⚠️ it fired in the CI-repair session via `| tail`, and **AGAIN at the PLAN** via `go list … | grep -c; echo $?`, which reported *grep's* status) · **a GOLDEN ROSTER that omits the family under test** (shape seventeen, live on T4). ⚠️ **AND ADR-0302 §Context ¶11 CARRIES A WRONG FIGURE — *"forty-five files"* where the truth is 43 (42 under the wrapped grep, and 42 even at the SPEC's own base). It is LANDABLE at this IMPL precisely because ADR-0302's STATUS is still `PROPOSED`.**

12. **⚠️ A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX — the NINETEENTH consecutive stage at which this held.** The index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. **Still live:** `reference_sds_init_fetch_timeout_dial_budget_flake` (**TWO packages — LIVE for this row's subject**; it did NOT fire at the SPEC or the PLAN) · the pre-existing `internal/cluster` `-race` outlier (`TestOutlierDetector_ConcurrentEjectExactlyOnce`) · `internal/httpclient TestOptions_ZeroValue_NoOpDefaults`. ⚠️ **TWO former flakes were FIXED at `f46ba419`/`f2dd994a` and a recurrence of either is now a FINDING, not a re-run:** `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery` (a whole-loop read deadline acting as a throughput assertion) and **the full-suite `bind: address already in use` BACKEND half** (now banded `11000..14999` + wildcard probe) — **an in-band backend bind failure is a real finding.** The `subject ready: EOF` half was hardened at `0e9cc680` and likewise warrants suspicion, not acceptance. ⚠️ **`0061-lb-ring-hash` is NOT a live flake — a spread failure there is a FINDING.** **Isolate-re-run, then state the classification AND its evidence.** **No flake fired at the PLAN.**

13. **Counts at this tip (re-run MECHANICALLY in your worktree; never copy them).** ⚠️ Use GNU `command grep` or `git grep` for anything repo-wide. **All re-verified at the PLAN close:**
    fixtures **120** (next-free **0119**; faithful predicate `^[0-9]{4}[a-z]?-`, a bare `^[0-9]{4}-` gives **118**) / fuzzers **55** (⚠️ the `internal/`-vs-repo-wide pair is NOT discriminating — both 55; zero `^func Fuzz` outside `internal/`) / internal packages **73** / blank imports in `runner_test.go` **120**, ⚠️ **anchor on the FULL `^\t_ "github.com/pgdad/envoy-go/test/fixtures/` prefix — a naive `^\t_ ` gives 126** — and ⚠️ **`\t` in GNU ERE is a literal `t`: use `-P`, or a bare `-cE '^\t_ '` silently returns 0** / **stat surface 1207**, ⚠️ **DOCUMENTARY and CONFIG-CONDITIONAL — assert the DELTA** / **BackendKind tail 38** (⚠️ a TAIL VALUE — **39** declarations, values 0-38 contiguous, **no `iota`**; do NOT "fix" 38 to 39) / go.mod modules **2** (the phase-61.2 lineage figure — the single `go.mod` requires **67**; do NOT "fix" 2 to 67) / `DECISIONS.md` **17692**, **301** headings, tail **ADR-0302 PROPOSED**, next-free **ADR-0303** / `ROADMAP.md` **228 lines / 112 data rows** / `BEHAVIOR_CONTRACT.md` **5822** / `STATE_HISTORY.md` **428** / next-free reference port **10119** / **TWELVE** non-test top-level segment detectors in `ExtractTags` plus **FOUR** mid-name detectors, **code and error string AGREE** — ⚠️ **row 80 moves this to THIRTEEN and the error string must move IN THE SAME TASK; the mid-name FOUR is UNCHANGED because `sds.` is a ROOT** / `helpText` **25** keys and `helpTextRoster` **25** entries, both -> **30** / `goldenRoster` **13** entries with **ZERO** `sds.*` names, `goldenTaggedIdx = {0,1}`, and `goldenRegistry`'s doc comment says *"13-entry"* / `goldenOTLPCells` pinned at **1134 / 1200 / 1118 / 1184** / the SDS corpus is **FIVE fixtures / FOUR distinct secret names / 25 registrations** (⚠️ `0108` and `0109` **SHARE** `validation_ca`; the SPEC's "four corpus secrets / 20" hides the fixture count. All four names are **DOT-FREE**, which is why **G6 is mandatory** — the fixture cannot discriminate the first-dot fork).

14. **⚠️ DOCUMENTARY DEFECTS — carried forward, still deliberately NOT fixed** (except where noted as a rider on this IMPL).
    - **`ADR-0299`'s STATUS still reads `PROPOSED`** although its §Decision and §Consequences landed at the phase-77 IMPL (`DECISIONS.md:17466`). **The single word is the entire defect. 1 task, DEFENSIBLE AS A RIDER on this IMPL**, which already edits `DECISIONS.md` — with a guard of shape *"a `PROPOSED` STATUS must have no §Decision."* ⚠️ **The guard must tolerate the in-flight case or run AFTER both flips** — ADR-0302 is legitimately `PROPOSED` until this IMPL completes its own append.
    - ⚠️ **`DECISIONS.md:17634` (ADR-0301 §Decision (b)) says *"has failed twelve top-level detectors"* — PRESENT TENSE inside immutable ADR text, and it goes factually WRONG the moment row 80 lands.** A named permanent discrepancy, alongside `ROADMAP.md`'s five occurrences (⚠️ **four of which are pure FALSE POSITIVES** — tap-filter *"charter on TWELVE counts"*, *"Twelve controller-reperformed deliberate breaks"*, *"TWELVE MINOR"*, *"TWELVE tasks executed"* — with the fifth historically true, so **nothing there needs a `12->13` move on the merits**, quite apart from the §Schema invariant).
    - **The `STATE_HISTORY.md` archive gap: 36 bullets for phases 67-75 and 58 overall.** ⚠️ **`phase 77 PLAN done` is in NEITHER file** — re-confirmed at the PLAN with a firing NC (IMPL/SPEC/BRAINSTORM siblings all match 1 under the identical matcher, PLAN matches 0) and non-vacuously (the archive carries PLAN entries for 78, 63, 62, 60.1, 56.1, 46.2, 42.2b, 42.2a, 42.1). **4-6 tasks, defensible ONLY if it lands the `comm -13` set-difference as a recurrence guard.** The PLAN honored the rule for its own eviction (**426 -> 428**).
    - **`ROADMAP.md` malformation: rows 57, 69 and 78 all lose summary content in GFM render** (EXECUTED with two renderers against well-formed controls; 689 / 4032 / 2877 source chars lost, controls 74 and 76 INTACT). All INVARIANT-BLOCKED by §Schema `:18` => **defensible as a GUARD, not a fix**, and the right gate is a **DISJUNCTION** (escape-aware `NF!=8` finds 57/69; escape-aware trailing-piece finds 78; **`NF==8` PASSES row 78**).
    - **The PUBLIC IMPORT PATH defect — NOT DEFENSIBLE AS A PHASE: there is no green.** `DECISIONS.md:142` is an immutable ADR that must *name* the wrong path; 7 hits sit inside CLOSED rows' summary cells that §Schema forbids editing; the root `PROGRESS.md` hits are pasted `go test` output; any `count==0` guard is unsatisfiable by construction. `go build ./...` exits 0 under the real path; **0** occurrences in compiled Go.
    - **A mechanical stat-surface recount: 8-11 tasks, defensible only if reframed as an EXECUTABLE enumerator** (dump `Registry` keys after a boot); a grep-based recount covers ~0.8 % of the surface and stays rejected.
    - **`STATE.md` §Project counts self-contradicts §Current** inside the same file (`:31` 119 vs **120**, `:33` 1205 vs **1207**, `:35` ADR-0298 vs **ADR-0302**) and is **FROZEN at the phase-76 IMPL close.** **Anchor on §Current, which IS live.** ⚠️ **The §Recent note at `:44` was ALSO stale** — it named the phase-80 BRAINSTORM as the newest stage right through the SPEC; **corrected at the PLAN.** A rolling document's own descriptive prose is a drift surface too.
    - ⚠️ **SYMMETRIC BIND HARDENING — the ALLOCATOR half LANDED at `f2dd994a`** (band + wildcard probe + a pinning test with a firing NC). ⚠️ **The old *"8-12 tasks"* figure and the *"close-then-rebind"* characterization are STALE — do not inherit either.** What REMAINS is the **retry** gap: a window persists between the probe's `Close` and the child's bind, and the child is `go run`, so the gap spans a **BUILD**. **26 of the 29 `freeTCPPort(t)` call sites in `runner_test.go` are backend-startup arms with NO retry loop**; `harness_test.go` holds 4 more. The fix is a `startSubjectWithRetry` equivalent for those arms. ⚠️ **Still NOT verifiable by a single green suite run** — a prior instance needed `-count=6`.
    - **Opening the gRPC family — HARD-BLOCKED, evidence-backed:** `\.RunEncodeTrailers(` => **0** non-test / 1 test; `\.RunDecodeTrailers(` => **0** non-test / 1 test; both declared, neither production-reachable. NC: `\.RunEncodeHeaders(` non-test => **4**. The **16-22+** band is **NOT re-derivable** — flagged, not inherited.
    - **The WASM row-summary rider stays DECLINED ON THE MERITS** — `ROADMAP.md:76` (row 25.3) says verbatim *"phase 25 is the FINAL §9 HTTP-filters-family row"*, **and writing the marker would silence check (3) BY MENTION** (proven by NC).
    - **Normalizing the Operational-tooling short-form deferred-candidate paragraph** to the long form.
    - `BEHAVIOR_CONTRACT.md:501` calls the `wasm.` arm a *"NEW SN9 flattening rule"*, which **COLLIDES with ADR-0118's actual SN9.**
    - ⚠️ **`ROADMAP.md` §Schema `:18` says *"Sub-phases get their own rows"* while BRAINSTORM §1.1 claim 3 argues the legs live in the parent's `sub-phases` PROSE.** That is **practice diverging from written doctrine.** The row-id adjudication (80, not `79.1`) survives on claims 1 and 2, which are independent and both hold. **Record the tension; do NOT relitigate the id.**
    - **`golangci-lint-action@v6.5.2` emits a Node-20 deprecation warning in CI.** Deliberately NOT bumped: v7+ requires golangci-lint **v2** while the repo pins **v1.64.8**, so a bump forces a config migration. **It is a warning, not a failure** — do not "fix" it as part of an unrelated row.

15. **⚠️ THINGS THE ROW NAMES BUT DOES NOT FIX — do not silently absorb them.** Full hyphen fidelity (needs `NamePattern` relaxation or a raw-bytes seam through the `Registry`/`ExtractTags` seam — repo-wide; ⚠️ **the reference serves `envoy_sds_update_success{envoy_xds_resource_name="server-cert"}` with the hyphen `od`-verified verbatim in the label value while envoy-go will boot-FAIL, and that is a DOCUMENTED envoy-go-strict DEPARTURE, not a fix — state it as such in the contract, with the reference behavior quoted**) · the **general** dynamic-token charset exposure across six families (this row brings `sds.` into line; it does **not** audit whether the five existing rejects are complete, nor whether wire-derived segments — mongo collection names, wasm plugin names — are reachable with an invalid name) · the reference emitting **ZERO `# HELP` lines** where envoy-go emits them for every family (a pre-existing FORMAT departure — ⚠️ **extending `helpText` WIDENS it; do NOT price it as parity work**) · **`--mode validate` cannot validate ANY SDS bootstrap** (`validate.Bootstrap` passes a nil provider, so `boot.NewSDSProvider` is never reached — this **forecloses `--mode validate` as a home for the new check**) · the `listener.`/`stat_prefix` sanitization inconsistency (ADR-0065 §Context rejected sanitizing a hoisted label value as *"a silent data-loss bug"*, yet `normalizeAddr` sanitizes the `listener.` address, which SN3 hoists — **the precedent set is internally inconsistent**).

## What THIS session does — the phase-80 IMPL

One stage per session. Read `PLAN.md` in full and **execute its 13 tasks**, then close the phase: `DECISIONS.md` (ADR-0302 §Decision + §Consequences + the STATUS flip, the ADR-0299 rider, the ¶11 `45`->43 correction) · `BEHAVIOR_CONTRACT.md` (close the narrowed departure, record the NEW one, repair the **FOUR** `79.1` occurrences, rewrite the five-counter paragraph, `TWELVE`->`THIRTEEN`) · **`ROADMAP.md` row 80 `in-progress` -> `done`** · `STATE.md` §Current rolled **IN PLACE** (ADR-0288 — ⚠️ **§Recent is AT its five-entry cap; a close that adds a bullet MUST evict the oldest to `STATE_HISTORY.md` VERBATIM, and the oldest is now `phase 79 (stats-prometheus-projection) SPEC done` — verify it is ABSENT from the archive BEFORE moving it, and NC the probe against a sibling that IS present**) · `PROGRESS.md` (append the `# IMPL record` half) · this file. **`want` STAYS 112.**

⚠️ **AN IMPL's job is to REFUTE the PLAN by EXECUTION, not to formalize it.** The lineage record is that **every stage finds a load-bearing error in its predecessor, and every one is found by execution rather than review** — the phase-79 PLAN refuted eleven SPEC claims; the phase-79 IMPL then found **the row's own fix was STALE ON ARRIVAL and every gate stayed green**; this SPEC refuted nine BRAINSTORM claims and four of its own probes; this PLAN refuted **nineteen** SPEC claims and caught **five** of its own probes' defects. **Budget for finding more.**

⚠️ **THE PHASE-79 IMPL'S LESSON IS THIS ROW'S PRIMARY HAZARD.** It authored *"NINE"* while **TWELVE** were live, swept the wrong number into four fixture files, and **PINNED IT BYTE-EXACT** — because **a guard whose expectation is authored by hand shares the author's mistake.** The protection is `internal/stats/segmentcount_test.go`, which **hard-codes nothing**: it AST-derives one side and parses the count **and the set** out of the live `noRecognizedSegmentErrFmt` constant via `claimedRe`, comparing sets **order-independently** and reporting missing/extra separately. **Do not hand-verify `13`; let the guard derive it, and run all three of `TestTerminalError_TopLevelCountMatchesCode`, `TestExtractTagsTerminalError_ByteStable` and `TestTerminalError_NamedRootsAreAccepted`.**

**Start with the three PLAN claims most likely to be wrong:** the **four re-measured OTLP `wantBytes`** (tree-local by construction — the PLAN says re-measure, so **re-measure**), the **~700 LoC / 13-task bottom-up** (unvalidated against a realized squash — compare against your own `git show --numstat` at close), and the **T9 prose-reversal roster** (three doc sites, **none machine-gated**, so an omission is silent).

Fresh worktree off master **at the PLAN squash tip** per `feedback_git_worktrees` (⚠️ **locate it by SUBJECT — `git log --grep 'phase 80'` — never by position**, `reference_next_prompt_tracked_despite_gitignore`); subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** (`reference_parallel_subagents_private_scratch`) and they **must tear down docker containers BY NAME, never by an `ancestor`/image filter** (`reference_parallel_agents_shared_machine_namespaces`). ⚠️ **THERE ARE **TWO** RESERVED PORT BANDS — band private ports outside BOTH `20000-31007` (subject blocks) AND `11000-14999` (backends), and also clear of the static fixture ports `10000-10447` / `15000-15011` / `18001-18007`.** The PLAN's probes used `42000-42299`, which remains safe. ⚠️ **If two agents share ONE worktree they must stage by EXPLICIT PATH.** ⚠️ **AND THEY MUST NOT RUN AN UNSCOPED `git restore`.** Per-task `gofmt` + `golangci-lint` on touched packages (`feedback_pertask_gofmt_lint`) — **OWED THIS SESSION**, on **six** packages (`internal/stats`, `internal/statssink`, `internal/boot`, `internal/xds`, `internal/admin`, the `0110` driver). ⚠️ **`gofmt -l` NEVER exits non-zero — gate on OUTPUT.** ⚠️ **`golangci-lint` runs `misspell` with `locale: US`, and this row edits Go comments AND an error string — do NOT paste PLAN or SPEC prose into `.go` files.** ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch.**

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. ⚠️ **ASSUME IT FIRES EVERY SESSION — it has been treated as live for TWENTY-THREE consecutive sessions and it FIRED TWICE, OBSERVED, at the phase-80 PLAN** (`Shell cwd was reset to /home/esa/git/envoy-go`). **Use `git -C <abs-worktree-path>` for every git command, prefer absolute paths over `cd`, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — Current-pointer section (TRUSTWORTHY; ADR-0288 discipline — ⚠️ **anchor greps on `^- **<field>`**; all seven fields verified singleton at the phase-80 PLAN close, NC both ways). ⚠️ **§Project counts SELF-CONTRADICTS §Current and is frozen at the phase-76 IMPL close** — do not anchor on it.
2. `docs/envoy-go/phases/80-stats-sds-projection/PLAN.md` — **in full.** §1 the nineteen refutations (**§1.1** the red-on-arrival fixture verdict, **§1.2** `0110`'s own prohibition, **§1.3** the LoC basis mismatch, **§1.4** the forced golden merge, **§1.5** the pinned reject line, **§1.6** the inverted ADR-0065(b) cell, **§1.7** the incomplete predicate, **§1.9** the unnamed fourth `79.1` phrase, **§1.10-§1.11** the sweep traps), §2 the global constraints, §3 the re-derived edit surface, **Tasks 1-13**, §4 the band, §5 the sentinel, §6 the counts, §7 the deferrals, §8 broken-gate **EIGHTEEN**, §9 the self-review, §10 the operative memories.
3. `docs/envoy-go/phases/80-stats-sds-projection/PROGRESS.md` — the `# PLAN record` half, which carries the probe-defect ledger and the "not run, flagged rather than dropped" list.
4. `docs/envoy-go/phases/80-stats-sds-projection/SPEC.md` — §3/§4 the two design decisions, §5 the blast radius, §6 the guards and the sweep-needle adjudication, §7 the fixture posture, §8 the contract/ADR edits, §10 what is chartered but not taken, §12 the probes that were themselves unreliable. ⚠️ **Read §7.2's VERDICT, §8 item 2's phrase list, §8 item 7's ADR block, §9's band and §9.3's LoC arithmetic as SUPERSEDED by `PLAN.md` §1.**
5. `docs/envoy-go/phases/79-stats-prometheus-projection/PROGRESS.md` — **the `# IMPL record`.** The single most relevant document in the repo for this session: *"THE ROW'S OWN FIX WAS STALE ON ARRIVAL, AND EVERY GATE STAYED GREEN."*
6. `docs/envoy-go/DECISIONS.md` — **ADR-0302** (this row; §Context only, footer retained, STATUS `PROPOSED`) · **ADR-0065** (the boundary-validation MANDATE **and** the sanitization rejection — the load-bearing precedent for D-SDS-2; ⚠️ **its §Consequences (b) "validate the longest" is CORRECT, but the SPEC §4.2 table cell asserting it is INVERTED**) · **ADR-0301** (the model for the footer FORM, not its miscount) · **ADR-0300** (§Consequences (ii) the insertion-point rule, (iii) the OVER-ATTRIBUTED claim — **mooted by the chosen design, which never panics**) · **ADR-0288** (roll-IN-PLACE and the archive rule) · **ADR-0059** (the panic discipline ADR-0065 carves out of).
7. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the **FOUR** live `79.1` occurrences on **THREE** lines (by PHRASE, not by line) · the five-counter named-subset paragraph, which this IMPL must rewrite because it says the segment *"is guarded by `stats.IsValidName` before registration"* — **true today, false after this row** · the phase-77 departure narrative.
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — especially **`reference_handwritten_golden_shares_author_mistake`**, **`reference_golden_roster_omits_family_under_test`**, **`reference_branchpoint_roster_stale_midrow`**, **`reference_gate_command_negative_control`**, **`reference_verification_table_launders_wrong_cites`**, **`reference_probe_must_discriminate`**, **`reference_probe_input_is_a_claim`**, **`reference_empty_output_is_not_a_zero_result`**, **`reference_output_volume_is_not_output_content`**, **`reference_sample_is_not_an_audit`**, **`reference_harness_exit_code_is_not_command_exit_code`**, **`reference_sentinel_matcher_string_self_clears`**, **`reference_stale_cite_recurs_fix_by_pattern`**, **`reference_recursive_grep_blind_to_gitignored_tracked_file`**, **`reference_registry_walk_lock_inversion`**, **`reference_nil_stats_counter_inc_crashes_goroutine`**, **`reference_dynamic_stat_name_charset_guard`**, **`reference_deliberate_break_wrong_assertion`**, **`reference_vacuous_break_modes`**, **`reference_break_arm_injection_site_is_a_claim`**, **`reference_plan_break_instructions_dont_compile`**, **`reference_fatalf_makes_assertions_unreachable`**, **`reference_differential_fullsuite_startup_flake`**, **`reference_sds_init_fetch_timeout_dial_budget_flake`**, **`reference_differential_run_selector`**, **`reference_golangci_misspell_locale_us`**, `feedback_brief_citations_not_evidence`, `feedback_git_worktrees`, `feedback_execution_style`, and **`reference_bash_cwd_reset_commits_to_main`**.
