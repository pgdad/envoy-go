START a fresh cold-start session for envoy-go — **the phase-34 (`load-balancer-least-request`) PLAN is DONE; the next lifecycle step is the phase-34 IMPL** (`superpowers:subagent-driven-development` — execute the 9-task PLAN task-by-task, per the established BRAINSTORM → SPEC → PLAN → IMPL cadence; the kafka-31/thrift-33 single-flat-row precedent). Phase 34 is the Load-balancing family opener: subject `least_request` (`Cluster.LbPolicy LEAST_REQUEST` + `Cluster.LeastRequestLbConfig`, `envoy.config.cluster.v3`) — the project's first LB policy beyond ROUND_ROBIN and its first framework seam OUTSIDE `internal/filter/network`.

The substantive live master tip is the phase-34 PLAN squash **`e97f7ea`** (`phase 34 PLAN: load-balancer-least-request — decompose SPEC §10 into 9 bite-sized TDD tasks …`; it carries `docs/envoy-go/phases/34-load-balancer-least-request/PLAN.md` + the STATE/ROADMAP advance + this file). The literal live tip is this docs-only `next-prompt.txt` SHA-fill commit above it; confirm via `git log --oneline -3`. Below it sits the phase-34 SPEC squash `3671167` (`phase 34 SPEC: load-balancer-least-request — D-L1..D-L7 …`); below that the phase-34 BRAINSTORM squash `87572c0`; below that the phase-33 IMPL squash `97aefd6` (the §9 family CLOSED).

## What this session does — execute the phase-34 IMPL

Execute `docs/envoy-go/phases/34-load-balancer-least-request/PLAN.md` task-by-task: a fresh subagent per task; TDD per task (write-failing-test → run-fail → minimal-impl → run-pass); per-task `gofmt -l` + `golangci-lint` on touched pkgs (`feedback_pertask_gofmt_lint`); subagents commit LOCAL-ONLY (`feedback_subagents_no_push`); the controller squash-merges + pushes at stage-close (`feedback_push_to_origin`); work in a worktree (`feedback_git_worktrees`); pin canonical paths + verify the main checkout stays clean (`feedback_subagent_worktree_path_targeting`).

The 9 tasks (PLAN §):

- **T1** — first-task baselines/anchors gate (re-confirm fixtures **60** [tail `0058`] / fuzzers **42** / stat surface **1116** / BackendKind tail **33** / DECISIONS tail **ADR-0233** via the canonical recipes; re-pin the as-built line anchors against the IMPL-session tip) + create PROGRESS.md.
- **T2** — the `loadBalancer` seam reshape: `Pick() (Endpoint, error)` → `Pick() (Endpoint, func(), error)` (ADR-0232 OPTION C) + `roundRobin` no-op release + the `cluster.go` release threading (`PickEndpoint` immediate-release; `Dial`/`AcquireH1` release-on-dial/TLS-failure + the `connWithGauge` `dec` composition; pool-hit immediate-release). BEHAVIOR-NEUTRAL — all 60 fixtures stay byte-exact (tested via a stub-LB counter-balance suite). NOTE: SPEC §10 Tasks 2+3 are MERGED here (compiler-coupled).
- **T3** — the `leastRequest` P2C type (`leastrequest.go` NEW): with-replacement sampling, strict `<` on raw active count, first-drawn ties, no clamp at choiceCount ≥ n; per-endpoint `atomic.Int64`; a mutex-guarded crypto-seeded `math/rand/v2` PCG (injectable via `newLeastRequestWithRNG` for deterministic tests); a `sync.Once`-guarded release. Mirrors v1.37.2 source EXACTLY (AMEND-L6).
- **T4** — manager acceptance + `parseLeastRequestLbConfig`: the two-policy construction switch + the NEW reject text (`cluster: %q: unsupported lb_policy %s (supported: ROUND_ROBIN, LEAST_REQUEST)`) + the hand-rolled `choice_count >= 2` gate + the `active_request_bias`/`slow_start_config` DEPARTURE rejects + the mismatched-oneof silent-ignore + retarget `TestManager_Error_NonRoundRobinLB` → RANDOM (the §6 matrix, byte-stable table test).
- **T5** — the in-process P2C skew integration test (held picks elevate counters; the heaviest endpoint stops being picked; `-count=1` inverted-comparison liveness) + a LEAST_REQUEST boot smoke.
- **T6** — the `0059-lb-least-request` differential fixture: the hold-4 + burst-60 + drain workload over a 3-endpoint LEAST_REQUEST cluster (`choice_count: 10`); the FIRST band-based `AssertDistribution` (conservation `sum == 64` + starvation `c1 <= 12` + concentration `c2 >= 16`, PER SIDE) + the cross/per-side `StatsAsserter` prong; reuses `TCPEcho` (NO new BackendKind); NO boot-reject dir.
- **T7** — band-constant tuning (≥20 runs flake-free PER SIDE) + the 3 `-count=1` deliberate-break liveness proofs (inverted-comparison [c2 leg] / no-op-release [c1 leg, the canonical deterministic break] / stats-prong).
- **T8** — the full 61-dir differential re-verify (the 60 prior dirs byte-exact through the seam reshape + `0059` green — the six-gate REAL guard) + `-race -short` + h2spec 53/53 + proxy-wasm 10/10 unaffected + `go mod tidy -diff` empty.
- **T9** — the completion bundle (ADR-0052 atomic landing): BEHAVIOR_CONTRACT least_request delta + the ADR-0232/0233 §Decision/§Consequences bodies in-place (ADR-0044; tail STAYS ADR-0233, NO new number) + STATE/ROADMAP row 34 `in-progress → done` (flat family row — NO parent rollup per ADR-0106) + the six-gate evidence.

## The PLAN's load-bearing resolutions (SPEC §12 D-questions; PLAN §)

- **D-S34-1** file split: the reshaped interface + `roundRobin` stay in `loadbalancer.go`; `leastRequest` in NEW `leastrequest.go`; `parseLeastRequestLbConfig` + `defaultChoiceCount` in `manager.go`.
- **D-S34-2** RNG: `newLeastRequest` crypto-seeds a `math/rand/v2` PCG once per construction, mutex-guarded (`rng func() uint64`) — Pick is concurrent + `math/rand/v2.Rand` is not per-source safe; `crypto/rand` error threads out → boot-fail; tests inject via `newLeastRequestWithRNG`.
- **D-S34-3** band: K=4 / S=60 / `c1 <= 12` / `c2 >= 16` / `sum == 64`; tune ≥20 runs flake-free per side + the 3 `-count=1` breaks (a band that cannot fail is a dead assertion — `reference_differential_break_protocol_count1` generalized).
- **D-S34-4** establishment witness: write+read-echo per held conn before holding (`acceptEchoCounting` is a streaming echo).
- **D-S34-5** release composition: compose into the existing `connWithGauge` `dec` closure (`dec: func() { c.upstreamCxActive.Dec(); release() }`) — the struct UNCHANGED, its `sync.Once` guards both.
- **ADR-0045 FINAL re-check → NO SPLIT** (9 tasks / ~155–255 prod LoC; the 34.1/34.2 escape valve UNCONSUMED).

## The SPEC's load-bearing pins (AMEND-L1..L7 — SPEC §1.1, AUTHORITATIVE in §11)

- **L1**: the v1.32.4 three-knob surface (`choice_count` PGV `>= 2` ONLY-when-set; `active_request_bias` + `slow_start_config`); the manager calls NO PGV → the choice_count gate is HAND-ROLLED; `least_request_lb_config` is an `lb_config` ONEOF member (field 37). ZERO new go.mod dep (`go mod tidy -diff` empty).
- **L2 (SPEC-BLOCKING)**: cx-as-rq CONFIRMED LIVE — an open TCP-proxied conn IS one active request; the per-endpoint counter counts CONNECTIONS (acquire at pick, release at final conn close).
- **L3**: FULL_SCAN DEFERRED; sampling WITH replacement → cc==N NOT deterministic; cc>N LEGAL + quasi-deterministic (live 0/30 at cc=10) → the `0059` fixture uses `choice_count: 10`.
- **L4**: ZERO stat-name delta (surface STAYS 1116); the `0059` StatsAsserter: `upstream_cx_total`/`membership_total`/quiesced-active CROSS-EQUAL; `upstream_rq_total` PER-SIDE (ref=conn-count; subj=0 — tcpproxy never calls `IncUpstreamRqTotal`).
- **L5**: the reject-text blast radius is THREE sites (`manager.go:216` + `TestManager_Error_NonRoundRobinLB` [retargets to RANDOM] + `BEHAVIOR_CONTRACT.md:897`); NO fixture pins the text → NO boot-reject dir (fixtures 60 → 61 only); bias/slow_start set under LEAST_REQUEST → DEPARTURE rejects; mismatched lb_config oneof → silent-ignore PARITY.
- **L6**: P2C from v1.37.2 source — k draws `rng() % n` WITH replacement; strict `<` on RAW `rq_active`; FIRST-DRAWN wins ties; no clamp; the weighted bias formula is EDF-path-only (never engages at equal weights — pure P2C for the MVP).
- **L7**: the seam is OPTION C — reshape ONLY the unexported `loadBalancer` interface; the EXPORTED `Cluster` surface BYTE-STABLE; ZERO consumer churn; ADR-0024 unamended.

## Counts at master tip (phase 34 in-progress, PLAN done — they advance only at the phase-34 IMPL)

- stat surface **1116** (ZERO delta anticipated at the IMPL — confirmed at D-L4; the first zero-delta phase, deliberate).
- differential fixtures **60** (tail `0058-thrift-boot-reject`; `ls -d test/fixtures/[0-9]* | wc -l`). IMPL: 60 → 61 (`0059-lb-least-request`); NO boot-reject dir.
- fuzzers **42** (tail `FuzzThriftDecode`). IMPL adds NONE (the first no-fuzzer phase, deliberate).
- BackendKind tail **33** (`TCPThriftResponder`). IMPL adds NONE (the `TCPEcho` kind-0 backends reused; deliberate).
- DECISIONS tail **ADR-0233** (ADR-0232 [the LB acquire/release seam] + ADR-0233 [the least_request policy], both PROPOSED — §Context anchored at the SPEC; §Decision/§Consequences bodies land at the IMPL per ADR-0044; tail STAYS ADR-0233, next-free **ADR-0234**).
- reference pin: `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227).
- Toolchain: Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); go-control-plane `/envoy` v1.32.4 (ADR-0008).
- conformance h2spec 53/53 + proxy-wasm 10/10.

## Read first (in order)

1. THIS FILE — the post-phase-34-PLAN cold-start (next: the phase-34 IMPL).
2. `docs/envoy-go/STATE.md` — active-phase `phase 34 (load-balancer-least-request) PLAN done`; lifecycle-state `phase 34 PLAN done — SKILL_ROUTING → the phase-34 IMPL`; the counts.
3. `docs/envoy-go/phases/34-load-balancer-least-request/PLAN.md` — **THE EXECUTION SCRIPT**: the 9-task spine (each task with exact files + complete code + exact commands + the LOCAL-ONLY commit); the D-S34-1..5 resolutions; the ADR-0045 NO-SPLIT re-check; the File Structure table; the conventions-honored list.
4. `docs/envoy-go/phases/34-load-balancer-least-request/SPEC.md` — AUTHORITATIVE for the WHY: §1.1 AMEND-L1..L7; §3 the OPTION-C seam + the `leastRequest` design; §5/§6 the proto + reject matrices; §7 the zero-delta stat surface + the StatsAsserter set; §8 the `0059` band-arm design; §11 the D-L pin record; §12 the D-S34 questions; Appendix A the live probe basis.
5. `docs/envoy-go/DECISIONS.md` — ADR-0232/0233 (§Context, this phase's SPEC) + ADR-0024 (per-cluster RR counter scope — cross-referenced, unamended) + ADR-0063 (`connWithGauge` — the release attach-point) + ADR-0106/0045/0044/0052/0060/0080.
6. `internal/cluster/{loadbalancer,manager,cluster}.go` (the seam sites: the interface, the `manager.go:215` guard + the `cl :=` literal, the `Dial`/`AcquireH1`/`connWithGauge` funnel) + `internal/httpclient/httpclient.go:280` + `internal/filter/network/thriftproxy/filter.go:90` (the direct-pick consumers — must stay source-compatible) + `test/fixtures/0001-tcp-proxy-rr/driver/driver.go` (the harness shape `0059` transposes) + `test/fixtures/0057-thrift-roundtrip/driver/driver.go` (`AssertStats`/`scrapeStats`) + `test/differential/runner_test.go` (`acceptEchoCounting` ~line 1329 + the asserter hooks ~lines 1102/1158).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_execution_style`, `feedback_git_worktrees`, `feedback_subagents_no_push`, `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_path_targeting`, `reference_differential_break_protocol_count1`, `reference_differential_asserter_dispatch`, `reference_differential_fixture_dispatch_constraint`, `reference_docker_probe_bridge_network`.

## Honor throughout

- `feedback_execution_style` (subagent-driven) · `feedback_git_worktrees` (work in a worktree) · `feedback_subagents_no_push` (subagents commit LOCAL-ONLY; the controller squash-merges + pushes at stage-close) · `feedback_push_to_origin` (after a clean landing on master with tests green, push without asking) · `feedback_pertask_gofmt_lint` · `feedback_subagent_worktree_path_targeting` (pin canonical paths; verify the main checkout stays clean).
- ADR-0106 (row 34 is a flat family row — NO sub-rows unless the 34.1/34.2 escape valve is consumed; NOT anticipated) · ADR-0045 (the split-gate; re-checked NO-SPLIT at the PLAN) · ADR-0044 (ADR bodies at the IMPL, in-place) · ADR-0052 (the atomic-landing six-gate) · ADR-0060 (histograms deferred) · ADR-0080 (byte-stable reject text, substring-pinned by table tests).
- The IMPL's six-gate REAL guard for the seam reshape is the full differential re-verify: all 60 existing dirs must stay byte-exact through the `roundRobin` no-op-release adoption (T8). The Docker-bearing gates (`0059` + the full differential) run where Docker is present, per `reference_docker_probe_bridge_network`.
- The lifecycle is BRAINSTORM (done) → SPEC (done) → PLAN (done) → **IMPL (this session)**, each its own session per the established cadence.
