=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. **There IS banked work at this tip** — phase 75 is mid-lifecycle at lifecycle-state 2 — so this session ADVANCES it and does NOT open a new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-RUN MECHANICALLY 2026-07-25 at the phase-75 SPEC close, TWICE (stage worktree AND landed master after the squash-push). It does NOT fire. `stop` was NOT created.** ACTUAL output, recorded rather than predicted:
- **(1) prints `NOT DONE: row 75`** — unchanged from the BRAINSTORM close, because **the ROADMAP is BYTE-UNTOUCHED at a SPEC** and row 75 STAYS `in-progress`. Check (1) stays LIVE and goes silent only at the phase-75 IMPL.
- **(2) prints THREE live "candidates:" sentences** — HTTP/3 (`:185`), xDS (`:195`), Observability (`:205`). **Cite the command, never the adjective** (`reference_sentinel_deferred_sentence_live_vs_historical`): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` => **3**. ⚠️ A naive `grep -c 'candidates:'` returns a larger number, and the **four** `candidates were:` recaps inside `:205` are HISTORICAL, not live. Occurrence-counting (`grep -oE … | wc -l`) also returns **3**, so no huge line hides a second live sentence. **NO deferred sentence was narrowed at this SPEC** (phase-57 precedent) — the ROADMAP diff is EMPTY.
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** => THREE families never opened. (The `WASM` line remains a ROADMAP **bookkeeping artifact** — `internal/wasm/` is a substantially-built wazero host and rows 25/25.1/25.2/25.3 are all `done` — deliberately LEFT AS-IS.)

All three print => the sentinel does NOT fire. Abundant work remains.

⚠️ **CHECK (1) HAS A KNOWN BLIND SPOT.** At the phase-75 BRAINSTORM tip it was RE-DERIVED as **106 data rows, 102 matched, FOUR misses**: `| 00 | bootstrap | — | done |` (the `[0-9.,  ]*` field does not match an **em-dash**), `| 04 | http-1.1 | 03 | done |` (the `[a-z0-9-]+` slug field does not match the **DOT**), and **`| 28.1a |` / `| 28.1b |`** (the `^\| [0-9.]+ \|` id field cannot match a **letter suffix**). All four are `done`, so there is no current impact. ⚠️ **RE-DERIVE this figure; never copy it** — the phase-74 PLAN recorded a STALE *"104 rows, 102 matched, TWO misses"*.

==============================================


## STATUS AS OF 2026-07-25: THE PHASE-75 SPEC IS COMPLETE AND LANDED. ROW 75 REMAINS `in-progress`. THIS SESSION PERFORMS THE PHASE-75 PLAN.

**What the phase-75 SPEC landed** (`git log --grep 'phase 75'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): docs-only, **ZERO production `.go`**, **EXACTLY TWO files in the delta** — the new `SPEC.md` and the `DECISIONS.md` **ADR-0297 §Context** append (a PURE APPEND: **30 insertions / 0 deletions**, `git diff --numstat`-verified; `ADR-0286` and `ADR-0296` BYTE-UNTOUCHED). ROADMAP BYTE-UNTOUCHED. **FIVE agents at tip `e822f1ad` on disjoint remits — TWO live-reference probe fleets** (disjoint port ranges, container prefixes `p75s-`/`p75f-`, PRIVATE scratch) **and THREE read-only re-derivation agents.**

**⚠️ THE DOCKET IS CLOSED. The PLAN owes a TDD spine and BREAK DESIGN, not further probing.** All five D-TLSNC-* items were RESOLVED BY EXECUTION, including the one the BRAINSTORM called *"genuinely open"*.

### What matters for the NEXT session, in order

1. **⚠️ THE SENTINEL DID NOT FIRE AND `stop` WAS NOT CREATED. Re-run all three checks yourself and record the ACTUAL output; do not predict it.** Check (1) still prints `NOT DONE: row 75` and stays live until the phase-75 IMPL. **Create `stop` if and ONLY if ALL THREE print nothing.**

2. **THE PREDICATE IS PINNED — do not re-open it.** D-TLSNC-SEMANTICS resolved to **(a) UNCONDITIONAL**. The Inc gate is **`len(tlsConn.ConnectionState().PeerCertificates) == 0` ALONE, with NO client-auth term.** Settled at the WIRE: a `DownstreamTlsContext` carrying ONLY `tls_certificates` (no `validation_context`, no `require_client_certificate`) driven by a no-cert client yields `ssl.handshake: 1` AND `ssl.no_certificate: 1`, reproduced in TWO fresh containers, with an `openssl s_client -msg` trace showing the server's ENTIRE flight and **ZERO** `CertificateRequest` — **the client was never asked and the counter fired anyway**. Confirmed on an HCM `direct_response` stack with `clusters: []`, at TLS 1.2 AND 1.3, and independently by the second fleet. **A mode-gated predicate would UNDER-count.** SPEC §3.1.

3. **⚠️ THE ROW'S BIGGEST TRAP, and the PLAN must task it explicitly.** **Every landed increment test asserts this counter does NOT move; NONE would assert that it DOES.** `assertSSLCrossProduct` (`manager_test.go:4474-4488`) checks one named suffix is 1 and all others are 0, so extending its `:4480` roster only ever adds a NEGATIVE — a `sslNoCertificate` field that is **registered but never Inc'd passes every unit guard.** The POSITIVE arm is **CONSTRUCTIBLE FROM EXISTING PARTS**: `mkDownstreamTSInline` (`manager_test.go:627`) already builds a ONE-WAY TLS transport socket — cert only, no validation context, the exact shape the probe proved fires the counter — so the PLAN adds a ~20-line `startOneWayTLSListener` sibling of `startMutualTLSListener` (`:4437`, which is MANDATORY mTLS and therefore CANNOT supply the arm), dials with no client cert, and asserts `ssl.handshake == 1` **AND** `ssl.no_certificate == 1`. **No new PKI, no new transport-socket builder.** SPEC §10.1.

4. **⚠️ `assertSSLCrossProduct` needs a SHAPE change, not just a roster entry.** Extending the hard-coded 3-leaf negative roster is **SAFE for the three existing arms** — controller-verified per arm: the `handshake` arm presents a trusted cert (`manager_test.go:4498-4503`) so `PeerCertificates` is non-empty; the two failure arms never reach the success path. **But a success-with-no-cert arm has `handshake` AND `no_certificate` legitimately non-zero TOGETHER**, which the helper's one-hot shape cannot express.

5. **THE BREAK DESIGN — three distinct hazards.** (a) The positive arm from item 3 **IS the row's discriminating break**: deleting the `len(...)==0` guard (making the Inc unconditional) leaves every other test green and fails only that one. (b) ⚠️ **A break that merely DELETES the registration fires the ABSENT check, not the value check** — demonstrate the two SEPARATELY (`reference_deliberate_break_wrong_assertion`; failing ≠ your assertion is live — confirm WHICH fired). (c) Breaks run **AFTER committing** (`reference_break_protocol_commit_first` — `git restore` wipes uncommitted work), always with **`-count=1`** (`reference_differential_break_protocol_count1`), and a break INSTRUCTION that does not compile proves nothing (`reference_plan_break_instructions_dont_compile`) — substitute an equivalent and say so.

6. **THREE GUARDS THE BRAINSTORM'S ROSTER MISSED** (SPEC §11): **`assertSSLCrossProduct`** (`manager_test.go:4474-4488`; callers `:4493`/`:4519`/`:4544`) · **`TestServeConnection_PlaintextListenerIncrementsNoSSL`** (`:4568`, count-free, needs nothing but was unenumerated) · **`0111`'s cross-side `AssertStats`** (`0111/driver.go:655`, **THREE** hard-coded rosters at `:682-684`, `:687-691`, `:692-696`, plus prose in its README and `expectations.yaml`). Under the unconditional semantics `0111` (`require=true`, success arm presents a cert) reads `no_certificate == 0`, so **no value changes** — but the PLAN must **decide explicitly** whether to add it as a fourth asserted ZERO. **Not deciding is how a roster goes stale.**

7. **The BRAINSTORM's guard roster, all RE-DERIVED EXACT at the SPEC tip** (re-derive again at the PLAN tip): `…RegistersExactlyThreeSSLNames` (`manager_test.go:2023`, `want` `:2049-2053`) needs a **sorted-4th** entry plus a **RENAME** and its doc comment · `TestListenerMetrics_GateMatchesInc` (`:2128-2251`) needs **+2** of the existing **6** pointer assertions (3 non-nil `:2190-2199`, 3 nil `:2240-2249`) — **the pointer half is the LOAD-BEARING half** · the QUIC name-set guard (`quic_test.go:274-278`) needs a `want` entry, after which its zero-loop at `:296-300` covers the new name FREE · `…PlaintextListenerRegistersNoSSLNames` (`:2076`) and `…AllocatesBaseListenerMetrics` (`:1951`) need nothing. ⚠️ **A nil `*stats.Counter`.Inc is a PROCESS CRASH** — no nil guard, no `recover()` on the `serveConnection` goroutine. **The pointer assertions are mandatory, not stylistic.**

8. **TWO production files, and the second one's guard will NOT catch you.** `internal/listener/manager.go` (field `:180-182` · registration `:378-382`, calls `:379-381` · **Inc at `:1277`**, where `tlsConn` is `*crypto/tls.Conn` declared at `:1259`) **and `internal/stats/name.go`** (the `helpText` map literal `:456-472`, entries `:469-471`, **14 entries**). The Prometheus key is **`envoy_listener_ssl_no_certificate`** — EXECUTED via `ExtractTags`+`flattenToProm`, not guessed. `TestHelpText_ListenerSSLHandshakeOutcomes` (`name_test.go:230-235`) iterates a **HAND-LISTED three-name slice** with no reverse direction ⇒ **a fourth name added without extending `wantNames` is SILENTLY UNGUARDED.** ⚠️ `IsValidName` PASSES for every address form incl. IPv6 (EXECUTED) — no charset hazard — but registration uses plain `NewCounter`, which **panics** on an invalid name (`registry.go:117`) and on a duplicate (`:107`).

9. **The `0110` fixture extension** (SPEC §8). `want`: `handshake=2`, **`no_certificate=1`**, `fail_verify_error=1`, `fail_verify_no_cert=0`; `downstream_cx_total=3` deterministic. **CONFIRMED reference-side by replaying `0110`'s exact three-arm structure.** ⚠️ **Arms 1 and 3 DISCRIMINATE — `no_certificate` is 1, NOT 2.** Transplant `0111`'s `AssertStats` (`driver.go:655`) + `scrapeProm` (`:739`). ⚠️ **A wanted value of ZERO makes the ABSENT check LOAD-BEARING, not defensive** — keep the two-value comma-ok lookup plus `continue`; an unregistered counter reads `0 == 0` and passes VACUOUSLY. ⚠️ `var _ fixture.StatsAsserter = (*rccfDriver)(nil)` is a **TRIPWIRE, not the dispatch mechanism** — dispatch is a silent type assertion at `runner_test.go:1347-1349` with no `else`. ⚠️ **Add a SECOND precondition beyond `downstream_cx_total`** (item 10). ⚠️ Corrected cites: the three arms are at `driver/driver.go:400-435` (**`:362-374` is a DOC COMMENT**), `ProbeAdmin` is `:554`, `wantObservable` `:89`, `BackendCount` `:289`.

10. **⚠️ THE DEAD-UPSTREAM HAZARD IS CHARACTERISED — AND IT IS NOT WHAT IT LOOKED LIKE.** It is **NOT deadness, it is FAST FAILURE.** `tcp_proxy` dials the upstream at **accept time**, before any TLS byte (a bare zero-byte connection still moves `upstream_cx_total`), so the connect and the handshake run **CONCURRENTLY**. An instantly-refused connect (`127.0.0.1:1`) tears the downstream connection down **mid-handshake** and the whole `ssl.*` family stays silent; a **BLACKHOLE** upstream (`192.0.2.1:81`, `connect_timeout: 10s`) — equally dead but SLOW — loses the race and fires everything normally. **That blackhole arm is the discriminator.** It is a RACE and therefore in principle load-dependent. ⇒ **any fixture asserting `ssl.*` needs a LIVE upstream or NO upstream at all.** `0110` is **NOT BLOCKING** (live in-process `TCPEcho` both sides, structurally mandatory for green via `wantObservable`). ⚠️ **COROLLARY: `downstream_cx_total > 0` is NOT a sufficient decode-ran guard for an `ssl.*` assertion** — it increments even when the family is wholly suppressed. This applies to `0111`'s LANDED asserter too.

11. **THREE stale `ssl.*` sites in `0110`, not one** (SPEC §8.4): **`README.md:160-163`** — ⚠️ **BUNDLED**, the `ssl.*` half is FALSE since the phase-74 IMPL but the *"never assert `/listeners` or `total_listeners_active`; never treat a docker-proxy accept as listener liveness"* half is **LIVE and correct** ⇒ **SPLIT the bullet, do not delete it** · **`envoy.yaml:24`** (single-clause) · **`expectations.yaml:166-171`**, which carries a **THIRD** clause (*"strictly STRONGER than a subject-only stat"*) that **also inverts** once the fixture asserts cross-side. `driver/driver.go` carries ZERO stale claims. **Do NOT touch** `README.md:164-165` / `expectations.yaml:173-175` (the `sds.<secret>.*` counters, correctly still unasserted). The `0111` fixed text is the template for both forms.

12. **The docs edit map** (SPEC §9). `grep -n 'ssl\.handshake' docs/envoy-go/BEHAVIOR_CONTRACT.md` => **916, 928, 1851, 1857, 1859, 5002** — the full surface. **`:1851` is PRIMARY** (the canonical roster under the `:1849` heading); then `:1853` (semantics), `:1857` (QUIC parity), `:1859` (scope divergences), `:928` (the C3 retired/surviving split), `:916`. ⚠️ **`:831`/`:847` are NOT the ledger** — they are the two narrative bare-total restatements (*"Stat surface UNCHANGED at **1204**"*) and must become **1205**; the `### Stat surface` ledger heading is **`:4950`**, entries **`:4954-5002`**, and the new line appends **after `:5002`** using the **`→` U+2192** arrow with the bold running THROUGH the parenthetical to the colon (⚠️ the form is NOT uniform — match the TAIL entry, not `:5000`). ⚠️ **There is NO `B5` step in BEHAVIOR_CONTRACT**; the departure is at `:1855`.

13. **ADR-0297 at the IMPL.** §Context is LANDED (tail is now **ADR-0297**, next-free **ADR-0298**); §Decision + §Consequences append **IN PLACE** after the RETAINED `*(§Decision + §Consequences land at the phase-75 IMPL.)*` footer — the ADR-0295/0296 shape, **no renumber, no new ADR**. ⚠️ **The ADR-0296 §Decision (g) correction lands at the IMPL, NOT before** — an indented `  > [CORRECTED at phase 75/ADR-0297: …]` blockquote after `DECISIONS.md:17308`, leading bytes **`0x20 0x20 0x3E 0x20`** (`od -c`-verified byte-identical across `:16901` and `:16910`, the ONLY two instances in the file), with a **blank line after** because a `###` heading follows at `:17310`. **Phrase it as a documentary finding, NOT criticism of phase 74** — and lead with what SURVIVES: no counter was owed *in phase 74*, whose sole stat-asserting fixture `0111` is `require=true`. ⚠️ **The FORM's discriminator is SELF-vs-OTHER-ADR, NOT the ADR family** — the record's family rule is REFUTED (`:16901` is a same-family case using the indented form). ⚠️ **Claim NO ORDINAL** for ADR-0296's defects.

14. **⚠️ NO BUILD OF ROW 75 EXISTS, in-tree or in scratch.** The field, registration, predicate, guards, the new helper and the `0110` asserter are ALL specified and **NONE is COMPILED**. Every number in SPEC §3 and §8 is a **reference-side** measurement; **the subject side has never emitted this counter.** Nothing in the SPEC may be cited as execution evidence for buildability — §1.2 says so explicitly. ⚠️ **The absolute total 1204 was NOT re-derived mechanically** (no such command exists; ADR-0296's own ledger asks a future phase to COUNT). The **DELTA (+1)** is solid; the TOTAL is documentary.

15. **The live hazards — never reflex-classify any of these as a regression.** `reference_sds_init_fetch_timeout_dial_budget_flake` (covers `internal/boot TestSDSEndToEnd_FetchFailure_BootFailsClosed` AND the original `internal/xds` test) · the PRE-EXISTING `internal/cluster` `-race` flake (`TestOutlierDetector_ConcurrentEjectExactlyOnce`, `outlier_test.go:766` — **isolate-re-run, do NOT re-classify**) · the full-suite startup flake (`subject ready: EOF` on an UNRELATED fixture) · and TWO still-**UNINDEXED** load flakes, isolate-green and NOT root-caused: `internal/httpclient TestOptions_ZeroValue_NoOpDefaults` and `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery`. ⚠️ **`-run '0110'` matches ZERO subtests** — use `TestDifferential/0110-tls-require-client-cert-false` (`reference_differential_run_selector`).

16. **Counts at exit (re-run MECHANICALLY in the worktree; never copy them):** fixtures **119** (`ls -d test/fixtures/[0-9]*/ | wc -l`) / fuzzers **55** (`grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`) / **stat surface 1204** (BEHAVIOR_CONTRACT doc count; **NO mechanical command**, and NOT re-derived) / BackendKind tail **38** (a TAIL VALUE — the file declares 39 constants, 0-38) / go.mod modules **2** (the phase-61.2 lineage figure; the single `go.mod` requires **67**) / DECISIONS tail **ADR-0297** (§Context only; next-free **ADR-0298**) / ZERO new packages. `go build ./...` and `go vet` clean; `0110` and `0111` both PASS at `-count=1`. **All UNCHANGED — the SPEC was docs-only.**

## What THIS session does — the phase-75 PLAN

One stage per session. A PLAN decomposes the SPEC into a **TDD task spine (~9-11 tasks)** with per-task RED→GREEN steps and an explicit **deliberate-break** per load-bearing assertion. Row 75 **STAYS `in-progress`**; the ROADMAP stays BYTE-UNTOUCHED; **DECISIONS.md is BYTE-UNTOUCHED at a PLAN**.

1. **Write `PLAN.md` + `PROGRESS.md`.** Open with an **RD-* drift ledger** re-deriving every SPEC anchor at the PLAN's own tip (`feedback_brief_citations_not_evidence` — **a SPEC's citations are not evidence either**; `reference_quoting_is_not_executing`). The phase-74 PLAN REFUTED twelve of its SPEC's claims, three load-bearing — **expect to find some, and record them rather than silently correcting**.
2. **Run an ADVERSARIAL pass over the PLAN's OWN instructions** and record what it changed. ⚠️ The phase-74 PLAN's adversarial pass changed SIX of its own instructions — including catching that the PLAN had **self-certified that very pass over a file which did not yet exist**. **Do not self-certify a pass over unwritten content.**
3. **Pin the break roster.** Every load-bearing assertion needs a break that fires **it** — see item 5 above. State per break: the exact edit, which assertion must fire, and how you will CONFIRM it was that one.
4. **Re-derive the guard roster at the PLAN tip** (items 6 and 7), and **decide the `0111` question explicitly** (item 6).
5. **Close by re-running all three sentinel checks mechanically, TWICE** — in the stage worktree AND on LANDED master after the squash-push (the phase-72/73/74/75 precedent: a stage worktree can disagree with what actually landed). Check (1) should still print `NOT DONE: row 75`; **record the ACTUAL output either way.**

**Stage-close mechanics (a PLAN):** the phase-directory delta is the new `PLAN.md` + `PROGRESS.md`; **DECISIONS.md BYTE-UNTOUCHED**; **ROADMAP BYTE-UNTOUCHED**, row 75 STAYS `in-progress`. STATE §Current rolled **IN PLACE** (lifecycle 2 → 3; §Recent re-capped at FIVE **with its PREAMBLE updated** — the ADR-0288 rule). ⚠️ **The ADR-0288 singleton greps return 2, not 1**, for each of the three field-name tokens — the second hit is `STATE.md:7`, the RULE STATEMENT itself. Verify with `grep -n`, and **never "fix" the count to 1**; that would delete the rule. `next-prompt.txt` rolled to the phase-75 IMPL.

Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** (`reference_parallel_subagents_private_scratch` — a shared scratch yields a FALSE NEGATIVE).

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. ⚠️ **THIS FIRED LIVE DURING THE PHASE-75 BRAINSTORM.** At the phase-72 close the same hazard put a stray commit on `master` in the MAIN checkout AND made a "final gates" run validate master's pre-row tree — GREEN while proving nothing. **Use `git -C <abs-worktree-path>` for every git command, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline) + §Project counts + the §Recent lineage (five bullets: phase-75 SPEC + phase-75 BRAINSTORM + phase-74 IMPL + phase-74 PLAN + phase-74 SPEC).
2. `docs/envoy-go/phases/75-tls-no-certificate-stat/SPEC.md` — **the whole document.** Especially **§3.1** (the SEMANTICS resolution and the wire trace — the predicate is PINNED there), **§3.6** (the fast-failure race + the cross-fleet reconciliation), **§8** (the fixture and its three stale sites), **§10/§10.1** (the task surface and the POSITIVE-arm gap), **§11** (the edit-site roster + the three MISSED guards) and **§16** (the adversarial-pass record, incl. the correction this SPEC made to its OWN reasoning).
3. `docs/envoy-go/phases/75-tls-no-certificate-stat/BRAINSTORM.md` — §2.1 (the pick + the ADR-0296 §Decision (g) headline), §2.4 (the rivals), §6 (anticipated edit sites), §7 (the docket, now CLOSED), §11.3 (the contested claims). ⚠️ **Read it THROUGH the SPEC's §1.1 drift ledger** — six of its claims are REFUTED there.
4. `docs/envoy-go/DECISIONS.md` — **ADR-0297 §Context** in full (the row's own ADR, drafted at the SPEC; ¶2 the semantics, ¶3 the predicate, ¶6 the race, ¶7 the ADR-0296 correction, ¶9 the FORM rule, ¶10 the missed guards, ¶11 what was NOT executed) · **ADR-0296** §Decision (e)/(g) and §Consequences (the direct parent; **(g) is the paragraph ADR-0297 must correct AT THE IMPL**) · `:16901` and `:16910` (the two indented-blockquote precedents) · ADR-0289 (phase 67) · ADR-0044 (⚠️ read it — it does NOT contain the §Context-draft discipline) · ADR-0045 · ADR-0106 · ADR-0117 · ADR-0288.
5. `internal/listener/manager.go` — `:172-177` (⚠️ stale *"three"* prose) · `:180-182` (fields) · `:347-349` (`normalizeAddr`) · `:358-373` (⚠️ stale doc comment) · `:374-383` (`registerListenerMetrics`) · `:385+` (`classifyHandshakeErr`) · **`:1256-1279`** (the handshake block; **Inc at `:1277`**) · `:392`/`:1265` (⚠️ the phantom `B5` comments).
6. `internal/listener/manager_test.go` — `:627` (`mkDownstreamTSInline`, the ONE-WAY builder the positive arm needs) · `:1951` · `:2023` · `:2076` · `:2128-2251` · `:4361` (`mkDownstreamTSMutualTLS`) · `:4437` (`startMutualTLSListener`) · **`:4474-4488`** (`assertSSLCrossProduct`) · `:4493`/`:4519`/`:4544` · `:4568`. Then `internal/listener/quic_test.go:249-300` and `internal/stats/name.go:448-472` + `name_test.go:195-241`.
7. `test/fixtures/0110-tls-require-client-cert-false/` — `driver/driver.go` (arms `:400-435`, `wantObservable` `:89`, `SubjectConfig` `:313`, `ProbeAdmin` `:554`, `BackendCount` `:289`, `var _` `:613`), `README.md:157-165`, `expectations.yaml:166-175`, `envoy.yaml:24`. Then `test/fixtures/0111-tls-cvc-empty-dynamic-fallback/driver/driver.go:629-800` — the `AssertStats`/`scrapeProm` template — and `test/differential/runner_test.go:1330-1349` (the dispatch site).
8. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — `:916` · `:928` · **`:962`** (⚠️ the `ssl.no_certificate` token sits at 0-based **character** index 627 / **byte** offset 630 of that line and is the ONLY occurrence in the file) · **`:1849-1859`** (the five-block subsection) · **`:4950`/`:4954-5002`** (the real ledger) · `:831`/`:847` (the two bare totals).
9. `docs/envoy-go/ROADMAP.md` — the §Schema invariant (`:21`), the row-75 cell (`:137`), row 67 (`:129`), the `### Observability family` heading (`:201`)/descriptor (`:203`)/history (`:205`), and the THREE live `candidates:` sentences (`:185`, `:195`, `:205`). ⚠️ **RE-DERIVE these with `grep -n`.**
10. `docs/envoy-go/phases/74-tls-handshake-outcome-stats/{SPEC,PLAN}.md` + `PROGRESS.md` §IMPL record — the immediately-prior lineage and the PLAN FORM to mirror (§1.1's RD-* ledger, the task spine, the break roster). ⚠️ `SPEC.md:1`, `:315`, `:320` carry that SPEC's OWN REFUTED first QUIC reading against its authoritative §3.4/§16 — deliberately left as a historical record of a reversal.
11. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Load-bearing for this PLAN: `reference_break_protocol_commit_first`, `reference_deliberate_break_wrong_assertion`, `reference_liveness_break_needs_failing_baseline`, `reference_plan_break_instructions_dont_compile`, `reference_vacuous_break_receiver_normalizes`, `reference_vacuous_break_convenience_accessor_default_path`, `reference_differential_break_protocol_count1`, `reference_differential_run_selector`, `reference_differential_asserter_dispatch`, `reference_fatalf_makes_assertions_unreachable`, `reference_nil_stats_counter_inc_crashes_goroutine`, `reference_dynamic_stat_name_charset_guard`, `reference_go_client_cert_withholding`, `reference_netpipe_deadlocks_client_cert_handshake`, `reference_iocopy_self_splice_echo_backend`, `reference_plan_schedules_edits_to_a_byte_gated_file`, `feedback_pertask_gofmt_lint`, `feedback_brief_citations_not_evidence`, `reference_quoting_is_not_executing`, `reference_a_drift_correction_is_itself_a_claim`, `reference_probe_must_discriminate`, and **`reference_bash_cwd_reset_commits_to_main`**.
