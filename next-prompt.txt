=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. **There IS banked work at this tip** — phase 76 is BRAINSTORMED **and SPEC'd**, and row 76 is `in-progress` — so this session **ADVANCES IT** and performs the **phase-76 PLAN**. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-RUN MECHANICALLY 2026-07-25 at the phase-76 SPEC. It does NOT fire. `stop` was NOT created and MUST NOT be.** ⚠️ **Re-run it yourself anyway and record what YOU see** — a prior run's result is not evidence for the current tip. ACTUAL output, recorded rather than predicted:
- **(1) prints `NOT DONE: row 76`.** Live since the BRAINSTORM registered the row; it stays live until row 76 flips `done` at the phase-76 IMPL.
- **(2) prints THREE live "candidates:" sentences** — HTTP/3 (`:186`), xDS (`:196`), Observability (`:206`). **Cite the command, never the adjective** (`reference_sentinel_deferred_sentence_live_vs_historical`): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` => **3**. ⚠️ A naive `grep -c 'candidates:'` returns a larger number, and the `candidates were:` recaps inside the Observability sentence are HISTORICAL.
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** => THREE families never opened. (The `WASM` line remains a ROADMAP **bookkeeping artifact** — `internal/wasm/` is a substantially-built wazero host and rows 25/25.1/25.2/25.3 are all `done` — deliberately LEFT AS-IS.)

⚠️ **PHASE 76 NARROWS NOTHING, EVER — verified mechanically, and this DIFFERS from phase 75.** None of the three `candidates:` sentences names a load-balancing candidate (the only `ring`/`Load-balanc` substrings on those lines are `buffering`, `Load-bearing`, and a historical *"the Load-balancing family closed at phase 54"*). ⇒ **check (2) STAYS 3 at the PLAN and at the IMPL.** Do not go looking for a narrow to land.

⚠️ **NEVER WRITE A SENTINEL'S OWN MATCHER STRING INTO A FILE THE SENTINEL GREPS.** This fired LIVE at the phase-76 BRAINSTORM, twice, in one commit: row 76's first draft quoted check (3)'s marker phrase for the gRPC family — *inside the sentence explaining why quoting it would be gaming the check* — and check (3) came back with **gRPC silently GONE**. The second occurrence quoted **the command and its result**, a claim the document's own landing falsifies. **`grep` cannot tell a mention from a use.** ⇒ **Re-run every sentinel check AFTER your stage's edits land, not only at session open.** The phase-76 SPEC did not touch `ROADMAP.md` at all, which removed the exposure but not the obligation.

⚠️ **CHECK (1)'s BLIND SPOT — RE-DERIVE IT; NEVER COPY IT.** At this tip it is **108 data rows (`:31`-`:138`), 104 matched, FOUR misses** — `| 00 | bootstrap | — | done |` (the `[0-9.,  ]*` field does not match an **em-dash**), `| 04 | http-1.1 | 03 | done |` (the `[a-z0-9-]+` slug field does not match the **DOT**), and **`| 28.1a |` / `| 28.1b |`** (the `^\| [0-9.]+ \|` id field cannot match a **letter suffix**). All four are `done`, so there is no current impact. ⚠️ **This figure was recorded WRONG in two consecutive lineages** before being re-derived at the phase-75 IMPL (107/103/4), the phase-76 BRAINSTORM (108/104/4) and again, independently, at the phase-76 SPEC (108/104/4). **Derive it yourself.**

==============================================


## STATUS AS OF 2026-07-25: PHASE 76 IS BRAINSTORMED **AND SPEC'D**. ROW 76 IS `in-progress`. THIS SESSION PERFORMS THE **PHASE-76 PLAN**.

**What the phase-76 SPEC landed** (`git log --grep 'phase 76'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): `docs/envoy-go/phases/76-lb-ring-hash-spread-margin/SPEC.md` (13 sections) settling the **D-RHSM-*** docket **by EXECUTION on every item**, plus the **ADR-0298 §Context** append (11 paragraphs) in `DECISIONS.md`, plus the STATE roll. **ROADMAP BYTE-UNTOUCHED; row 76 STAYS `in-progress`.** File set: `DECISIONS.md` + `STATE.md` + the new `SPEC.md` + `next-prompt.txt` — **ZERO production `.go`, ZERO test `.go` in the SPEC COMMIT.**

⚠️ **BUT THE ROW IS LARGELY BUILT ALREADY, IN PROBE WORKTREES.** `sourceIPs = 16`, the rescaled `driver_test.go`, and the full measurement test are **COMPILED, RUN and LOCALLY COMMITTED** at `phase-76-spec-v1` (`35bc70f5`) and `phase-76-spec-v2` (`ecbcfb82`). **Never pushed.** The PLAN should **re-derive rather than re-invent** — and must re-derive every anchor at its own tip (`reference_a_drift_correction_is_itself_a_claim`).

### What matters for the NEXT session, in order

1. **⚠️ THE SENTINEL DID NOT FIRE AND `stop` WAS NOT CREATED. Re-run all three checks yourself and record the ACTUAL output; do not predict it.** **Create `stop` if and ONLY if ALL THREE print nothing.** All three will print, so it will NOT fire.

2. **⚠️ THIS IS A PLAN — write the TDD spine, do not re-litigate the SPEC.** SPEC §9 already proposes **~7-9 tasks** (up from the BRAINSTORM's ~5-7). ⚠️ **The phase-75 PLAN "built the row it was only meant to plan" and this lineage's stages routinely exceed their nominal remit. That is acceptable when it is EXECUTED and RECORDED; it is not acceptable when it is asserted.**

3. **⚠️ THE TWO THINGS THE SPEC REFUTED, WHICH THE PLAN MUST NOT SILENTLY RE-INHERIT.**
   - **"The executable delta is ONE INTEGER" is FALSE.** `test/fixtures/0061-lb-ring-hash/driver/driver_test.go` (6 tests) hard-codes 64-summing tuples and is named in **NO** phase-76 document before the SPEC. At the raised constant `TestAssertDistribution_Affinity` **FAILS**, and **`_CollapseBitesSpread` and `_ReferenceConservation` go SILENTLY VACUOUS** — tripping the subject-conservation leg before reaching their own while still printing `--- PASS`. **`_CollapseBitesSpread` is the unit test for THIS ROW'S OWN ASSERTION.** ⚠️ **A rescale is not complete until each of the six tests has been RE-PROVED BY MEASUREMENT to fire its OWN leg** (SPEC §3.4 carries the before/after error-string table). ⚠️ **Three independent checks were blind in the same direction** — a file-scoped `grep` over `driver.go`, a `git diff --stat` reading `1 file changed`, and a green `go test ./test/differential/` (which never compiles the driver package's `_test.go`). ⇒ **a change-set measure is not a build measure; run the package, not the diff.**
   - **`sourceIPs = 10` was BELOW the project's own bar; the SPEC revised it to 16.** K=10 is **3.89σ**; `reference_differential_band_sigma_margin` records **4-5σ**; `0062-lb-ring-hash-http` and `0063-lb-maglev` fixed the **byte-identical** defect at **16** (5.27σ), `0062/README.md:57-63` calling it *"past the 5σ-equivalent flake-free margin"*. **`0061` was simply left behind — this row finishes a phase-36.2 migration.**

4. **⚠️ THE BREAK TRIO, WITH β CORRECTED — THIS IS THE ROW'S DESIGN CORE.**
   - **α (liveness)** — `internal/cluster/ringhash.go:140`, `m := sort.Search(...)` → `m := 0` (⚠️ **NOT `:129`**, the doc comment; **NOT `:142`**, the legitimate `m = 0 // wrap`). Expect `subject spread: only 1 backend(s) nonzero, want >= 2 (ring collapsed?)`. ⚠️ **Confirm WHICH assertion fired** — under total collapse **affinity SURVIVES** (256 % 16 == 0) and **conservation SURVIVES** (the sum is unchanged). `-count=1` (`reference_differential_break_protocol_count1`); run it AFTER committing (`reference_break_protocol_commit_first`). **α has NOT been run** — it needs a production edit the SPEC did not land.
   - **β — ⚠️ NOT EXECUTABLE AS THE BRAINSTORM AND THE OLD ROUTER SPECIFY IT. REFUTED BY EXECUTION.** Both said *"revert `sourceIPs` 10 → 4 and the unit MEASUREMENT FAILS while the fixture stays GREEN"*. **It does not fail.** The measurement's `collapseFixtureK` and the fixture's `sourceIPs` live in **different packages with no mechanical link** (`internal/cluster` cannot import a fixture driver; `sourceIPs` is unexported), so reverting the fixture ALONE leaves the measurement **GREEN**. ⇒ **β is a TWO-EDIT break** (revert both), and it then proves only that the **unit test's** constant is load-bearing — **not the fixture's**. **Do not describe β as proving more than it does.**
   - **The LINKAGE GATE closes what β cannot.** A grep-equality check over the two constants, **negative-controlled (RED on a desynced tree) AND positive-controlled (GREEN on a synced one)** — both already executed at the SPEC. It belongs in the six-gate, not in prose.
   - **γ (anti-vacuity), and the SHARPER γ.** Restore both to 16 and confirm the measurement PASSES rather than never having run. ⚠️ **The sharper one is the load-bearing one:** freeze the ephemeral ports so the ring is static and confirm the **CONTROL** leg goes RED while the **measured** leg stays GREEN at `0/2000`. **Executed.** At K=16, M=2000 the expected collapse count is **0.00014** ⇒ **the measured leg is a NULL RESULT standing alone.** ⚠️ **Any PLAN wording of the form *"the unit test proves the ring randomizes"* is true only of the CONTROL leg.**

5. **⚠️ A PASS-COUNT IS NOT A MARGIN — the project already fell into this once, and the evidence is in the fixture.** `0061/README.md:143-148` certifies the assertion *"overwhelmingly stable"* on a **`-count=20` → 20/20 PASS** and calls the ring *"fixed"*. **Both are false.** The ring is keyed on the endpoint address **including its OS-ephemeral port**, so it is a fresh random 3-way partition every run; and `(1−0.0355)^20 ≈ 0.48`, so the check was **more likely than not to pass even if the assertion were exactly as broken as it in fact is.** 95% power would need **`-count=81`**. ⇒ **do not verify this row by re-running it.**

6. **WHAT IS STILL OWED — the SPEC says so plainly so the PLAN inherits no false confidence.**
   - **`127.0.0.12`-`.17` have NOT been bind-tested.** Only `.2`-`.11` were, at the superseded K=10. Expected trivially fine (all of `127.0.0.0/8` is loopback on Linux) but it is **one `go test` away and must not be assumed.**
   - **The FULL differential suite has NOT been run at K=16** — only `-run 'TestDifferential/0061'`.
   - **No `-race` over `./test/differential/`** at the new constant.
   - **Break α has not been run.** **`BEHAVIOR_CONTRACT.md:1326` has not been edited**, only specified.

7. **⚠️ `BEHAVIOR_CONTRACT.md:1326` IS OWED AN EDIT — the BRAINSTORM's default posture was WRONG.** It said *"UNKNOWN … default: no edit, since no behavior changes."* That line states the fixture's workload verbatim: **four `64`s and one `127.0.0.2..5`**, and it is the **only** such line. The faulty inference was *"no behavior changes ⇒ no contract edit"* — **the contract documents the fixture's WORKLOAD as well as the proxies' behavior.** ⚠️ **`% 16 == 0` on that same line must STAY** (`burstPerIP` is unmoved). Edit **IN PLACE, no line-count change**, so by-line citations stay valid.

8. **⚠️ SWEEP FOR THE SHAPE OF THE OLD CLAIM, NOT ONLY ITS NUMERALS.** Beyond `4`/`64`/`20/20`/"fixed ring"/"overwhelmingly stable", grep **`DETERMINISTIC/EXACT`**: `0061/driver.go:270-271` and `0061/expectations.yaml:21-22` assert the spread leg is *"DETERMINISTIC/EXACT — not a σ-band"*, which is **true of affinity and FALSE of spread**. These sites contain **none** of the stale numerals and are structurally invisible to a numeral-keyed sweep — the phase-75 `0110/expectations.yaml` lesson. ⚠️ **`0062/driver.go:300` and `0063/driver.go:300` carry the same false adjective and are DELIBERATELY OUT OF SCOPE** (SPEC §3.1 records the widening it considered and refused; ADR-0298 ¶11 records the contradiction instead).

9. **⚠️ NEGATIVE-CONTROL EVERY GATE. Phase 76 is a +0-PRODUCTION-FILE row, which is exactly when a broken gate goes unnoticed.** Two phase-75 PLAN gate commands were broken in opposite directions: the **import gate FAILS CLOSED** (its `impblock` helper prints `FILENAME`, so a `/tmp` baseline vs an `internal/` HEAD differs on every line — exit 1 on a **+0-import** tree; normalise to basenames), and **`go doc -all <pkgA> <pkgB>` FAILS OPEN** (`go doc` takes `<pkg> [symbol]`, so the second package is read as a SYMBOL and silently dropped — exit 0, empty stderr). **Audit each package SEPARATELY.** **A green gate is evidence only if you have seen it go red.**

10. **The measurement's pinned parameters (SPEC §3.3), all EXECUTED.** M=**2000** · seed **20260725** · bar **1e-3** · `collapseFixtureK`=**16** · `collapseControlK`=**4** (permanent) · control band **[0.015, 0.070]** = counts [30,140] vs a mean of 71 ⇒ **4.95σ low / 8.3σ high**. ⚠️ A first draft used [0.02,0.06] = **3.7σ**, which does **not** clear the recorded 4-5σ bar. Runtime **0.62 s**. The seed was checked against **12 alternatives** (control 52..90, all inside band) ⇒ not cherry-picked. The law itself was measured over **1 000 000** trials, and p̂ runs slightly **BELOW** `3^(1−K)` ⇒ **the analytic form is a CONSERVATIVE UPPER BOUND.**

11. **⚠️ RUNTIME — quote the DELTA against a SAME-SESSION control, never an absolute against a recorded one.** K=4 → 3.512 s, K=10 → 3.638 s, K=16 → 3.670 s, all same-session ⇒ **+0.158 s (+4.5%)**. Against the *cross-session* recorded K=4 baseline (3.672 s) the same run reads **−0.002 s**, i.e. "free" — a **machine-speed artifact**. The absolute figures do not transfer across sessions; the deltas do.

12. **⚠️ THE LOAD-BALANCING FAMILY WAS DECLARED CLOSED AT ROW 54** (`ROADMAP.md:116`, *"EIGHTH and FINAL Load-balancing-family row — the family CLOSES at phase-done"*). The eight ordinal-claiming rows are 34/35/36/37/38/52/53/54. **Row 76's cell claims no ordinal, and that is CORRECT; ADR-0298 must not claim one either.** The BRAINSTORM's *"the family is already open"* is a mis-description — the right reason the heading paragraph stays unamended is *"maintenance rows do not extend a charter"*. Check (3) is unaffected (the marker occurs 8× regardless).

13. **⚠️ THE STAT-SURFACE ABSOLUTE TOTAL IS DOCUMENTARY, NOT MEASURED — and the chain is discontinuous in TWO places.** `1205` has **NO mechanical command**; it rides the recorded `1200 → 1201` gap **and** a second (`Phase 46.1b` closes at `1198` while `Phase 47.1` opens at `1200`, an unattributed **+2**). Phase 76 asserts a **delta of ZERO**, which IS mechanically checkable. **Do not present `1205` as re-derived.**

14. **The live hazards — never reflex-classify any of these as a regression.** the PRE-EXISTING `internal/cluster` `-race` flake (`TestOutlierDetector_ConcurrentEjectExactlyOnce` — ⚠️ it did **NOT** surface in two green `-race` runs at this SPEC) · the full-suite startup flake (`subject ready: EOF`, and at phase 75 also `bind: address already in use` on an UNRELATED fixture, **failing BEFORE any assertion**) · `reference_sds_init_fetch_timeout_dial_budget_flake` · **`0061-lb-ring-hash` — ⚠️ THIS ROW'S OWN SUBJECT; a spread firing BEFORE the fix is EXPECTED at ~3.6%/run, and AFTER the fix it is a FINDING** · and TWO still-**UNINDEXED** load flakes: `internal/httpclient TestOptions_ZeroValue_NoOpDefaults` and `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery`. ⚠️ **A stage brief's flake list is not the index — NEITHER flake that fired at phase 75 was on the PLAN's roster of six.** Isolate-re-run, then state the classification and its evidence.

15. **The rivals — re-costed at the phase-76 BRAINSTORM, NOT re-costed at the SPEC. Re-derive before adopting** (`reference_deferred_candidate_cost_restale`): **the Runtime family opening ~10-14 — the only evaluated candidate that GENUINELY clears a check-(3) blocker**, with SIX landed ADR-anchored `runtime_key` PARSE-REJECT arms forward-pointing at it (ADR-0187 `DECISIONS.md:11280`, ADR-0195 `:12413`) · `fault.abort.grpc_status` ~10-13 (blocker RETIRED by execution, but ⚠️ **it does NOT open the gRPC family** — that family charters bridge · gRPC-Web · gRPC-JSON transcoding · interop conformance — and it drags three live divergences) · `ssl.connection_error` ~10-13 (its recorded predicate is INCOMPLETE — an RST peer yields `*net.OpError`, for which BOTH `errors.Is(io.EOF)` and `errors.Is(io.ErrUnexpectedEOF)` are FALSE ⇒ needs `syscall.ECONNRESET`, **+1 PRODUCTION import**) · `upstream_cluster` ~7-9 but ~85-100 lines across ~18 files, central premise **UNVERIFIED** · `Listener.stat_prefix` ~7-10. ⚠️ **AGREEMENT ACROSS DOCUMENTS IS NOT EVIDENCE** — `fault.abort.grpc_status` rode three stages on one unexecuted assertion, refuted by a single `grep -c`. **A claim that has survived several stages is MORE suspect than one written yesterday, not less.**

16. **Counts at exit (re-run MECHANICALLY in the worktree; never copy them):** fixtures **119** (`ls -d test/fixtures/[0-9]*/ | wc -l`) / fuzzers **55** (`grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`) / **stat surface 1205** (BEHAVIOR_CONTRACT doc count; **NO mechanical command** — item 13) / BackendKind tail **38** (a TAIL VALUE — the file declares 39 constants, 0-38) / go.mod modules **2** (the lineage figure; the single `go.mod` requires **67**) / DECISIONS tail **ADR-0298 PROPOSED** (next-free **ADR-0299**) / next-free reference port **10450** (⚠️ **NOT** `10118` — ports are NOT fixture-index aligned; phase 76 needs no new port).

## What THIS session does — the phase-76 PLAN

One stage per session. Write `docs/envoy-go/phases/76-lb-ring-hash-spread-margin/PLAN.md` — a task-by-task TDD spine (**~7-9 tasks**; SPEC §9 has a proposed table), each task red-first → green → break-verified, each re-deriving its own anchors, with the **break roster** (α, the TWO-EDIT β, the linkage gate, and BOTH γs) and the **negative-controlled gate set** written out as runnable commands. Also create `PROGRESS.md`. **ROW 76 STAYS `in-progress`** and the ROADMAP is normally **BYTE-UNTOUCHED at a PLAN** (the phase-75 precedent). **DECISIONS.md is normally BYTE-UNTOUCHED at a PLAN** — ADR-0298 completes at the IMPL. **NO deferred sentence is narrowed, and this row never narrows one** (item 3 of the sentinel block); check (2) must **STAY 3**.

**Stage-close mechanics (a PLAN):** STATE §Current rolled **IN PLACE** (lifecycle **2 → 3**; §Recent re-capped at FIVE **with its PREAMBLE updated** — the ADR-0288 rule). ⚠️ **The ADR-0288 singleton greps return 2, not 1**, for each of the three field-name tokens — and **the RULE STATEMENT is `STATE.md:7`, the FIRST hit**, with the live value second (a close script taking `head -1` reads the rule, not the value). Verify with `grep -n`, and **never "fix" the count to 1**; that would delete the rule. `next-prompt.txt` rolled to the **phase-76 IMPL**.

Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** (`reference_parallel_subagents_private_scratch`).

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. ⚠️ **THIS HAS NOW FIRED LIVE IN FIVE CONSECUTIVE SESSIONS — including throughout the phase-76 SPEC, where the tool reported `Shell cwd was reset to /home/esa/git/envoy-go` inline on a majority of calls.** At the phase-75 IMPL's T10 `pwd` had silently reset to the MAIN checkout on `master` one step before a gate run; at the phase-72 close the same hazard put a stray commit on `master` AND made a "final gates" run validate master's pre-row tree — GREEN while proving nothing. **Use `git -C <abs-worktree-path>` for every git command, prefer absolute paths over `cd`, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline) + §Project counts + the §Recent lineage (five bullets: phase-76 SPEC + phase-76 BRAINSTORM + phase-75 IMPL + phase-75 PLAN + phase-75 SPEC).
2. `docs/envoy-go/phases/76-lb-ring-hash-spread-margin/SPEC.md` — **the whole document**, and especially **§1.1** (the drift ledger, whose HEADLINE is the refuted "one integer" claim), **§3.2** (the K=16 derivation and the rejected invented policy), **§3.3 + §3.3.1** (the measurement, its MEASURED vacuity, and why break β does not work as specified), **§3.4** (the `driver_test.go` before/after leg table), §3.6 (CXTOTAL/RUNTIME as executed), **§9** (the proposed task spine and break roster), **§10** (the edit-site roster), and **§13** (what was NOT executed).
3. `docs/envoy-go/phases/76-lb-ring-hash-spread-margin/BRAINSTORM.md` — for the diagnosis and the rivals, ⚠️ **but read it AGAINST SPEC §1.1**: its two headline scoping claims are refuted, and its `0062` anchor (`:90`) and grep count (7) are each off by one.
4. `test/fixtures/0061-lb-ring-hash/` in full — `driver/driver.go` (constants `:78-80`, `drive` `:193-216` incl. the `net.IPv4(127,0,0,byte(2+s))` bind at `:196`, `AssertDistribution` `:276-305` with the spread assertion at **`:295`**, `AssertStats` `:314+` with `:335`/`:367`), **`driver/driver_test.go` — the file the lineage missed**, `expectations.yaml`, and `README.md` (⚠️ **`:143-148` is the refuted flake-check paragraph**). Plus `internal/cluster/ringhash.go` (`:88-92`, `:133`, `:140`, `:142`), `internal/cluster/hash.go:133`, `internal/cluster/ringhash_test.go` (incl. `TestRingHash_DistinctKeysSpread` at `:50-64` and the `eps(n)` helper at `leastrequest_test.go:19`), and `test/differential/runner_test.go:272`. **And `0062-lb-ring-hash-http/README.md:57-63` + `0062`/`0063` `driver/driver.go:91`** — the house standard this row adopts.
5. `docs/envoy-go/phases/75-tls-no-certificate-stat/PROGRESS.md` — **the richest single source on how this lineage's stages fail**: what every task ACTUALLY did, which break fired and how, the fourteen refuted PLAN/SPEC claims, and the two broken gate commands.
6. `docs/envoy-go/DECISIONS.md` — **ADR-0298** (this row's own; §Context PROPOSED — the PLAN does not edit it) and **ADR-0297** in full as the immediately-prior completed form; ADR-0044 (⚠️ it does NOT contain the §Context-draft discipline — use `ADR-0044-as-used`) · ADR-0045 · ADR-0106 · ADR-0288.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Load-bearing for this PLAN: **`reference_0061_ring_hash_spread_flake`** and **`reference_differential_band_sigma_margin`** (this row's subject), `reference_fixture_workload_constant_desync`, `reference_differential_break_protocol_count1`, `reference_deliberate_break_wrong_assertion`, `reference_liveness_break_needs_failing_baseline`, `reference_positive_arm_cannot_catch_overfiring`, `reference_fatalf_makes_assertions_unreachable`, `reference_gate_command_negative_control`, `reference_code_comment_not_evidence`, `reference_quoting_is_not_executing`, `feedback_brief_citations_not_evidence`, `reference_document_hygiene_claim_not_evidence`, `reference_next_prompt_tracked_despite_gitignore`, `feedback_git_worktrees`, `feedback_execution_style`, and **`reference_bash_cwd_reset_commits_to_main`**.
