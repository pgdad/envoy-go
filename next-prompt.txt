=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, the active phase is fully landed, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action. (The sentinel does NOT apply right now — phase 49 is chartered, its BRAINSTORM + SPEC are LANDED, and the next stage is its PLAN.)

**STATUS AS OF 2026-07-02: phase 49 (`stats-sink-dogstatsd`) SPEC is LANDED; the NEXT stage is its PLAN.**
- The phase-49 SPEC landed: `docs/envoy-go/phases/49-stats-sink-dogstatsd/SPEC-49.md` (the 14-section shape mirroring `SPEC-48.md`; lifecycle-state 1 → 2; docs-only direct-on-master). The §11 D-DSD-* empirical pins were EXECUTED IN-SESSION live against `envoyproxy/envoy:contrib-v1.37.2` (5 config variants: base, no-`address`, explicit `max_bytes_per_datagram:512`, no-`prefix`, `protocol:TCP`) per `reference_docker_probe_bridge_network`. A spec-document-reviewer (general-purpose) pass APPROVED-WITH-FOLDS — all `bootstrap.go`/`main.go`/`internal/statssink`/`internal/stats` line citations + the go-control-plane `DogStatsdSink` proto field tags VERIFIED EXACT against HEAD — after two NON-BLOCKING folds (a "blank-imported"→"already imported" wording fix in 4 spots; a corrected `test/helpers/statsdrecv` §8.2 extension description). STATE.md active-phase header rolled to `phase 49 (stats-sink-dogstatsd) SPEC done`. **Re-verify with `git log --oneline -6` + `git status`; do NOT assume.**
- **Two LOAD-BEARING findings RESHAPE the design vs the BRAINSTORM's assumptions (do NOT re-litigate; the PLAN/IMPL EXECUTE these):**
  - **AMEND-DSD-DELTA-CONFIRMED** — dog_statsd counter `|c` is a per-flush DELTA-since-last-flush, INDEPENDENTLY confirmed matching phase 48's plain-statsd answer (`cluster.upstream_rq_total` emitted `6,1,0,0`/`7,0,0` across flushes, SUM==7==K). ⇒ `DogStatsdSink` owns its OWN sink-private `deltaState` — a SECOND, INDEPENDENT instance from `StatsdSink`'s (never shared).
  - **AMEND-DSD-TAGS-ORDER** — the reference does NOT alphabetically sort a multi-tag `|#` suffix; it emits `stats.ExtractTags`'s own NATURAL (unsorted) return order (the SN4 `response_code_class` label is PREPENDED ahead of the SN1/SN2/SN3 base tag in `name.go:354-357`, and this exactly matches the reference's observed order) — REVERSING the BRAINSTORM §2.3 "tags SORTED" default. The production `DogStatsdSink` tag formatter must NOT `sort.Slice` (CONTRAST the 47.2b `labelMapper`, which DOES sort because `LabelPair` order is immaterial there — a DogStatsd tag suffix is a literal wire string where order is part of the format, but envoy-go's own natural order happens to already match the reference).
  - Also PINNED (no amendment beyond confirming): D-DSD-NAME (residual byte-identical to `stats.ExtractTags`; tag-key format `envoy.<key>` IDENTICAL to `labelMapper`'s convention; default prefix `envoy`); D-DSD-LINE (one line per datagram by default; `|c`/`|g` only); D-DSD-LIFECYCLE (fire-and-forget UDP unchanged, `protocol:TCP` confirmed ignored); D-DSD-STATS (+0 self-stats); D-DSD-REJECT (a missing `dog_statsd_specifier` → REFERENCE-PARITY reject; an EXPLICIT `max_bytes_per_datagram` → the reference DEMONSTRABLY HONORS it with real multi-metric newline-batched datagrams up to the byte cap, so envoy-go-STRICT-REJECT is REQUIRED — not merely cautious).
- Counts at SPEC (re-verify, do NOT assume): stat surface **1200** (non-H2 **1196**) / fixtures **94** / fuzzers **51** / BackendKind **38** / DECISIONS **ADR-0265** (next-free **ADR-0266**).

=> The NEXT action is the **phase 49 (`stats-sink-dogstatsd`) PLAN** (lifecycle-state 2 → 3). NOT the termination sentinel. NOT a new SPEC.

==============================================

## The NEXT action — the phase 49 (`stats-sink-dogstatsd`) PLAN
Author `docs/envoy-go/phases/49-stats-sink-dogstatsd/PLAN-49.md` (a TDD task spine mirroring `PLAN-48.md`'s 10-task shape, anticipated ~9–13 tasks per SPEC §10). Docs-only direct-on-master (the family-row PLAN precedent — no worktree needed for the PLAN document itself; the IMPL that follows uses a fresh worktree). Lifecycle-state 2 → 3.

**The PLAN's job — RESOLVE every SPEC §12 D-DSD-* PLAN question:**
- **D-DSD-SPLIT** — the FINAL ADR-0045 re-check with real anticipated LoC (a single flat row is expected; SPEC §3.0).
- **D-DSD-ADDR** — the `UDPAddress` carrier shape (mirrors `StatsdSinkConfig`'s resolved `D-SD-ADDR`: a `host:port` string resolved at `NewDogStatsdSink` via `net.ResolveUDPAddr`).
- **D-DSD-CONFIG-HOME** — the parallel `DogStatsdSinkConfigs []DogStatsdSinkConfig` slice on `internal/bootstrap` (mirrors `StatsdSinkConfigs`); `main.go`'s THIRD build loop into the ONE `statsSinks []Sink`.
- **D-DSD-TAG-HELPER** — whether the `"envoy."+strings.TrimPrefix(key,"envoy_")` tag-key transform is factored into a shared helper reused by BOTH `labelMapper` and the new `DogStatsdSink` formatter, or duplicated inline (a 1-line transform; pick based on how the tag-suffix builder is structured).
- **D-DSD-DELTA-REUSE** — reuse `delta.go`'s `newDeltaState()`/`apply()` VERBATIM as a SECOND, `DogStatsdSink`-private instance (confirmed correct at SPEC §11 D-DSD-DELTA).
- **D-DSD-RECEIVER-WIRING** — the `0093` UDP receiver reachability (shared bridge + `differential.HostGatewayIP`, the `0092` precedent, reused verbatim); the `test/helpers/statsdrecv` extension shape — per the spec-document-reviewer's fold, this requires REVISING the CURRENT `ingest`'s `strings.LastIndexByte(line, ':')` name/value split (which breaks on a tagged line, since the LAST colon falls inside the `|#` tag suffix) to a first-`|`-then-colon split, THEN a second-`|` split isolating the `c`/`g` type from an optional `#tag1:val1,...` suffix; expose a NEW `Tags(name string) (map[string]string, bool)` accessor; confirm backward-compatibility with `0092`'s existing (tagless) usage.
- **D-DSD-FUZZER** — `FuzzDogStatsdSinkConfigParse` lands at 49 (fuzzers 51 → 52; confirmed at SPEC §6); reconcile the documented-vs-actual `^func Fuzz` count.
- **D-DSD-STATS-FINAL** — confirm +0 via the existing `registration_test.go` (mirroring `TestNoNewStat_StatsdRegistrationGuard`).
- **D-DSD-GAUGE-SUBSET** — whether `0093` adds a cross-side absolute-gauge-plus-tag subset assertion (e.g. `cluster.membership_total==1|g` tagged `envoy.cluster_name`) alongside the counter delta-SUM + tag-set assertions (SPEC §8.1).

**The task spine (per SPEC §10 — the PLAN decomposes into concrete TDD tasks):** PROGRESS + baselines + the ADR-0045 re-check (D-DSD-SPLIT) → the dog_statsd parse arm (`dogStatsdSinkTypeURL` descriptor-derived + `DogStatsdSinkConfig` + THREE-arm `stats_sinks[]` dispatch naming all three sinks + `parseDogStatsdSinkConfig` + strict-reject [`max_bytes_per_datagram`-set / missing-specifier / sibling-TypeURL], TDD) → `FuzzDogStatsdSinkConfigParse` → `internal/statssink/dogstatsd.go` `DogStatsdSink` (`*net.UDPConn` dial + `deltaState`-then-`ExtractTags` mapping + `|c`-delta/`|g`-absolute/prefix-join/tag-suffix-in-natural-order formatting + synchronous `Submit` + idempotent `Close`; TDD over a fake UDP listener + full-package `-race`) → `main.go` third build loop + flusher-gate generalization (three-way OR) → the `test/helpers/statsdrecv` tag-parsing extension (backward-compatible with `0092`) → the `0093-stats-sink-dogstatsd` cross-side fixture (poll-to-converge, K=7 requests, delta-SUM + stability barrier + tag-set assertions; clone the `0092` driver) → the deliberate breaks ((a) absolute-not-delta must fail the barrier, (b) dropped/wrong tags, (c) barrier-masks-(a)) + 20/20 flake + FULL-package `-race` (`internal/statssink` + `internal/stats`) → the registration test (1200 → 1200, +0) → the full 95-dir differential + six-gate → ADR-0266 body + BEHAVIOR_CONTRACT + STATE/ROADMAP (row 49 `done`) + the fuzzer-count reconcile (51 → 52).

**After the PLAN lands:** dispatch a `plan-document-reviewer` (general-purpose) pass — verify EVERY `bootstrap.go`/`main.go`/`internal/statssink`/`internal/stats`/`test/helpers/statsdrecv` line citation EXACT against HEAD (the 47.x/48 precedent), the import-cycle reasoning sound, baselines confirmed, the five high-risk areas covered (esp. the `statsdrecv` colon-split revision, the two independent `deltaState`/`*net.UDPConn` instances, the natural-vs-sorted tag order). Fold any NON-BLOCKING findings; re-litigate only BLOCKING ones. Then ROLL THIS FILE FORWARD to point at the phase-49 IMPL (mirror the phase-48 PLAN→IMPL router in git history — subagent-driven in a fresh worktree; FLIPS row 49 `done`; ANCHORS ADR-0266 §Decision/§Consequences).

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** The next stage is the phase-49 PLAN (author `PLAN-49.md`; docs-only direct-on-master). Lifecycle-state 2 → 3. After it, a `plan-document-reviewer` pass (the 47.x/48 precedent).
- **Subagent-driven execution** (`feedback_execution_style`) in a **fresh worktree** (`feedback_git_worktrees`) for the IMPL that follows the PLAN; subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit, re-runs the full suite on the frozen HEAD, does the deliberate-break verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). (BRAINSTORM/SPEC/PLAN are docs-only direct-on-master per the family-row precedent — no worktree needed.) Per-task gates: `gofmt -l` + `golangci-lint` on touched pkgs + `go vet` + `go build ./...` (`feedback_pertask_gofmt_lint`).
- **Process anchors:** ADR-0106 (per-leg rows) · ADR-0044 (ADR §Context-at-SPEC / §Decision+§Consequences-at-IMPL) · ADR-0045 (sub-split soft gate — escape-valve unconsumed, re-confirmed at PLAN) · ADR-0080 (strict-reject) · ADR-0064 (`stats_sinks[]` silent-ignore, now a THIRD consumer) · ADR-0060 (no histograms → no `|ms` timers) · next-free **ADR-0266**.
- **Differential discipline** (carries to `0093`): `-run 'TestDifferential/0093'` NEVER bare (`reference_differential_run_selector`); `-count=1` on every break + `-race` run (`reference_differential_break_protocol_count1`); the full-suite `subject ready: EOF` startup flake is isolatable (`reference_differential_fullsuite_startup_flake`); the Docker bridge + decode-ran proof (`reference_docker_probe_bridge_network`); the driver-owned UDP receiver EXTENSION is NOT a new BackendKind (`reference_differential_grpc_receiver_driver_owned`); the live reference is the wire truth (`reference_wire_format_both_sides_see_same_bytes`); periodic-sink two-receivers + hard-Close (`reference_periodic_sink_differential_two_receivers`); the delta-sink stability barrier (`reference_delta_sink_differential_stability_barrier`); reuse `differential.HostGatewayIP` verbatim (`reference_host_gateway_ip_docker_desktop`); `reference_full_suite_race_after_background_mutator` (the `Flusher` ticker + now a SECOND UDP sink's flush path).

## Read first (in order)
1. THIS FILE — the cold-start router (phase 49 `stats-sink-dogstatsd` SPEC LANDED; the next action is its PLAN).
2. `docs/envoy-go/phases/49-stats-sink-dogstatsd/SPEC-49.md` — the frozen scope + the §11 D-DSD-* live-probe pins (D-DSD-DELTA, D-DSD-TAGS-ORDER LOAD-BEARING) + the §12 PLAN/IMPL D-questions the PLAN must resolve + the ADR-0266 §Context draft (§13).
3. `docs/envoy-go/phases/49-stats-sink-dogstatsd/BRAINSTORM.md` — the frozen scope (tags in-scope / batching deferred / single flat row).
4. `docs/envoy-go/STATE.md` — the active-phase header (`phase 49 (stats-sink-dogstatsd) SPEC done`) + the recorded NEXT (`phase 49 PLAN`) + the counts (1200 / 94 / 51 / 38 / ADR-0265).
5. `docs/envoy-go/ROADMAP.md` — row 49 `in-progress` (the table row + the Observability family note); the family STAYS OPEN.
6. `docs/envoy-go/phases/48-stats-sink-statsd/PLAN-48.md` — the 10-task TDD spine shape to mirror + the phase-48 statsd sink substrate the dog_statsd sink REUSES.
7. `internal/statssink/{flusher,sink,mapping,delta,label,statsd}.go` + `internal/stats/name.go` (`ExtractTags`, esp. the SN4 label-prepend at `:354-357`) + `internal/bootstrap/bootstrap.go` (`parseStatsSinks` + the three-arm-to-be dispatch + `parseStatsdSinkConfig` as the shape to mirror) + `cmd/envoy-go/main.go` (the sink-slice + flusher build) + `test/helpers/statsdrecv/statsdrecv.go` (the `ingest` parser the PLAN must revise) + `test/differential/harness.go` (`HostGatewayIP`) — the EXACT seams the PLAN cites.
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `reference_stats_sink_metrics_service_probe`, `reference_periodic_sink_differential_two_receivers`, `reference_delta_sink_differential_stability_barrier`, `reference_differential_grpc_receiver_driver_owned`, `reference_strict_reject_sibling_typeurl_gap`, `reference_docker_probe_bridge_network`, `reference_host_gateway_ip_docker_desktop`, `reference_membership_total_vs_healthy_gauge`, plus the differential-discipline references above).
