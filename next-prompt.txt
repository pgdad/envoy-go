START a fresh session for envoy-go. **The phase-42.1 (`retries` — the retry loop) IMPL is COMPLETE, the six-gate GREEN, and LANDED on master (the controller squashed the 13 task commits + pushed) — `RouteAction.retry_policy` as a `retryExecutor` WRAPPING the single-attempt driver; ADR-0249 IN-PLACE; the full 77-dir differential GREEN; the 2 phase-41 emit-0 retry stats flipped LIVE.** The next step is the **42.2 hedging BRAINSTORM** (`superpowers:brainstorming` — the lifecycle valve is IMPL → next-subject BRAINSTORM; 42.2 is the PRE-AUTHORIZED second leg of row 42). Confirm the latest master is the phase-42.1 IMPL squash via `git log --oneline -3` (a NEW squash sha — do NOT assume a literal value). Counts at tip (the 42.2 IMPL will MOVE these): stat surface **1173** / fixtures **77** / fuzzers **42** / BackendKind tail **36** (`BlockingHoldResponder`) / DECISIONS tail **ADR-0249**, next-free **ADR-0250**.

## NEXT STEP — write the phase-42.2 (hedging) BRAINSTORM

Open a FRESH BRAINSTORM (`superpowers:brainstorming`) for **42.2 = hedging** — the SECOND and FINAL leg of the FOURTH Upstream-robustness-family row (row 42). Subject: **`HedgePolicy`** (`RouteAction.hedge_policy`, field 27 — REQUEST HEDGING: fire a PARALLEL attempt after a delay, race them, take the first response) + the project's **FIRST `per_try_timeout`** (field 3 — a per-attempt deadline, which hedging needs to bound each in-flight try). Reuses the 42.1 `retryExecutor` attempt substrate (the buffered-`ActionResponse` driver + body-replay + the per-attempt CB admission / `RecordUpstreamResult` seams) — but CONCURRENTLY rather than sequentially. `request_mirror_policies` (RouteAction field 30, shadow traffic) STAYS DEFERRED to a future row. The BRAINSTORM settles the subject + forks (the scope envelope; the concurrent-attempt architecture over the 42.1 substrate; the `per_try_timeout` plumbing; the hedging stat surface — `upstream_rq_retry`-adjacent `*_hedge_*` names + the 2 phase-41 retry stats already LIVE; the differential design) + anchors any ADR §Context drafts. **Honor `reference_round_robin_offset_randomized` + `reference_concurrency_differential_release_barrier` for ANY hedging-timing differential** (the reference randomizes the RR offset — assert offset-invariant counts, not host identity; a concurrent-attempt budget differential fills via a `/__release`-gate backend + sequential-per-side + poll-the-gauge, NEVER a sleep). Verify the `HedgePolicy` + `per_try_timeout` proto in the EXISTING go-control-plane dep live against `envoyproxy/envoy:contrib-v1.37.2` per `reference_docker_probe_bridge_network`.

**Row 42 STAYS `in-progress`** until 42.2 lands — the split row flips `done` only when BOTH 42.1 retries + 42.2 hedging land (ADR-0106 + `reference_roadmap_split_phase_row_done`; the rows-36/39 precedent; NO parent rollup). Landing 42.2 CLOSES the row; after phase-42 phase-done the Upstream-robustness family STAYS OPEN with **1** candidate remaining {per-protocol connection pooling — where the phase-41-deferred `max_connections` + `max_pending_requests` enforcement lands}.

## Honor throughout (the lifecycle discipline)

- **Lifecycle = one stage per session.** This session writes the 42.2 BRAINSTORM ONLY (the SPEC, PLAN, IMPL each follow in their own later sessions). `feedback_git_worktrees` (work in a worktree) · `feedback_subagents_no_push` (subagents commit locally; the controller squashes + pushes at stage-close) · `feedback_push_to_origin` · `feedback_execution_style` (subagent-driven; fresh subagent per task at the IMPL).
- **Differential discipline (for the §design + the later IMPL):** `reference_round_robin_offset_randomized` (assert offset-invariant counts, not host identity — the reference randomizes the RR initial offset) · `reference_concurrency_differential_release_barrier` (concurrent-attempt / budget differentials fill via a `/__release`-gate backend + sequential-per-side + poll-the-gauge, never a sleep) · `reference_differential_run_selector` (`TestDifferential/<NNNN>`, bare `<NNNN>` matches ZERO) · `reference_differential_break_protocol_count1` (deliberate breaks use `-count=1`) · `reference_differential_asserter_dispatch` (cross-side via `StatsAsserter`) · `reference_differential_band_sigma_margin` (timing assertions need ~4-5σ — but prefer count-based / poll-the-gauge over any timing band) · `reference_differential_fullsuite_startup_flake` (the full suite can transiently hit `subject ready: EOF` — isolate-re-run to distinguish from a regression) · `reference_docker_probe_bridge_network` (live probes need Docker + `contrib-v1.37.2` over a shared bridge).
- **ADR/process:** ADR-0044 (§Decision/§Consequences IN-PLACE at the IMPL) · ADR-0052 (the six-gate) · ADR-0080 (byte-stable rejects) · ADR-0106 + `reference_roadmap_split_phase_row_done` (row 42 flips `done` only when BOTH legs land) · ADR-0076 (the 1 MiB body cap — the replay source) · ADR-0245 (`RecordUpstreamResult`) · ADR-0248 (the phase-41 `circuitBreaker` + the now-LIVE `retry_budget`) · ADR-0249 (the 42.1 `retryExecutor` + the retry loop — the substrate 42.2 hedging reuses concurrently).

## Read first (in order)

1. THIS FILE — the cold-start router (42.1 retries LANDED on master; the next step is the 42.2 hedging BRAINSTORM, the FINAL leg of row 42).
2. `docs/envoy-go/phases/42-retries-and-hedging/{README,PROGRESS}.md` — the 42.1 IMPL record + the phase README (status **42.1 IMPL DONE / 42.2 PENDING**); the as-built retry architecture + the §"42.2 — hedging (PENDING)" pointer.
3. `docs/envoy-go/phases/42-retries-and-hedging/{BRAINSTORM,SPEC,PLAN}.md` — the phase-42 charter + the 42.1 SPEC/PLAN (the `retryExecutor` substrate 42.2 reuses; the pre-authorized 42.1/42.2 split; the §design conventions to mirror).
4. `docs/envoy-go/STATE.md` — active-phase `phase 42.1 (retries-and-hedging) IMPL done`; the count figures (1173 / 77 / 42 / 36 / ADR-0249, next-free ADR-0250).
5. `docs/envoy-go/phases/41-circuit-breakers/{BRAINSTORM,README}.md` — the precedent shape of a BRAINSTORM charter + a phase-completion README.
6. `docs/envoy-go/DECISIONS.md` (the ADR-0248 `circuitBreaker` + the ADR-0249 `retryExecutor` entries — the substrate 42.2 reuses; the process ADRs; next-free ADR-0250) + `docs/envoy-go/BEHAVIOR_CONTRACT.md`.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `reference_round_robin_offset_randomized`, `reference_concurrency_differential_release_barrier`, the subagent-worktree feedback notes).
