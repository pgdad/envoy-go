START a fresh cold-start session for envoy-go — **the phase 28.1b PLAN is DONE + squash-merged + pushed**. The active phase is **phase 28** (parent `in-progress`; sub-rows `28.1a` `done`, `28.1b` `in-progress`, `28.2` `planned`). This session executes the **28.1b IMPL** — per SKILL_ROUTING state 3 (PLAN.md exists, implementation incomplete → `superpowers:subagent-driven-development` per `feedback_execution_style`), running the PLAN's 10 TDD tasks.

Phase 28.1b (`network-filter-read-seam-and-zookeeper-requests-proof`) is the second/closing half of the user-approved ADR-0045 28.1a/28.1b invoked split (DECISIONS.md ADR-0045 §AMEND). 28.1a (squash `8703aeb`) landed the WriteFilter seam + the complete zookeeperproxy request package + the DISABLED `0046` driver. The 28.1b SPEC (squash `baf02ed`) designed the read-side seam; the 28.1b PLAN (squash `eff30e8`) decomposed it into 10 tasks. **This session implements them.**

## What this session must do

Execute `docs/envoy-go/phases/28.1b-network-filter-read-seam-and-zookeeper-requests-proof/PLAN.md` via `superpowers:subagent-driven-development`: a fresh subagent per task, two-stage review between tasks, in a git worktree (`feedback_git_worktrees`). The 10 tasks:

1. First-action baselines/anchors gate (48 dirs on disk / 47 active / 0046 disabled; fuzzers 37; stats 136; DECISIONS tail ADR-0223; the as-built line anchors) → creates PROGRESS.md.
2. `Buffer.TotalAppended()` (**int64**) + monotonicity tests + the zookeeperproxy decoder feed re-base (`decodeOnData(chainBytes []byte, totalAppended int64)`; `chainConsumed` widens to int64 in lockstep) + mechanical decoder_test/fuzz_test updates (assertions UNCHANGED — the §3.3 equivalence) + NEW drain-regime tests. ONE task (the spec-reviewer advisory: never split the int64 widening across commits).
3. `chainRuntime.replayRead` (chain.go) — all read filters, chain order, Status IGNORED, drain-after-pass + unit tests. (Re-cut BEFORE readconn.go: readChainConn.Read calls replayRead.)
4. `internal/filter/network/readconn.go` — the `readChainConn` (replay-before-return; EOF endStream replay; error passthrough) + readconn_test.go (defines the `multiReadConn` test double — the existing `scriptConn` is single-read).
5. The `handleTerminal` read-wrap insertion: `readChainConn` INNERMOST under the SHARED `len(rt.writeFilters) > 0` predicate; **load-bearing nesting change: `newPrefixConn(conn, prefix)` wraps the running `conn` variable, NOT `rt.conn`** → composition `writeChainConn(prefixConn(readChainConn(conn)))`; + R1 NEITHER-wrap test + composition tests + prefix-not-re-fed test + the §3.3 soundness-invariant test (every appended byte seen exactly once).
6. The §3.6 concurrency race test: synthetic both-directions filter + pumping terminal over `net.Pipe`, concurrent bidirectional traffic, `-race -count=5` (D-S28.1b-2 PLAN-resolved: unit shape, docker-independent).
7. `0046-zookeeper-requests` RE-ENABLE (uncomment `runner_test.go:72-77`; replace the driver DISABLED banner) + cross-side GREEN on all 7 arms (expected: `connect_rq=2 ping_rq=1 getdata_rq=2 create_rq=1 close_rq=1 create2_rq=1 getchildren2_rq=1 setwatches2_rq=1 decoder_error=1 request_bytes=307`) + R4 deliberate-break (wrong-value break + name.go-arm break, both reverted) + the fixture README. If RED → production bug in Tasks 2–5, NOT the driver: `superpowers:systematic-debugging`; never adjust the driver to pass.
8. `0047-zookeeper-boot-reject` per the 28.1 SPEC §8.2 verbatim (the 220-LoC `0044` template; port 15049; substring `stat_prefix`; symmetric `BootRejectFixture`).
9. Completion bundle: ADR-0221 §Decision/§Consequences body covering BOTH conn-wrap seam halves + the 28.2 synchronization forward-pointer; ADR-0222 body; BEHAVIOR_CONTRACT 28.1 bundle (conn-wrap-seam block + zookeeper_proxy subsection + stat table **136 → 337** + the §3.5 observational boundaries documented). DECISIONS tail STAYS ADR-0223.
10. Six-gate (build/vet/lint/race-test/**49-dir differential incl. the 47-dir R1 back-compat gate**/h2spec 53/53 + proxy-wasm 10/10) + STATE.md + ROADMAP sub-row 28.1b `in-progress → done` (parent 28 STAYS `in-progress` — D-28.1b-4) + next-prompt.txt for the 28.2-SPEC cold-start.

Per `feedback_subagents_no_push` subagents commit LOCAL-ONLY; the controller squash-merges + pushes at stage-close (`feedback_push_to_origin`), then fills the squash SHA into STATE.md.

## Load-bearing pins (carry into every task — from the 28.1b SPEC + PLAN)

1. **The wrap predicate is `len(rt.writeFilters) > 0`** — the SAME predicate as writeChainConn; both seams wrap together; readChainConn is INNERMOST so prefixConn's replayed prefix bytes are NOT re-fed.
2. **The decoder-feed re-base equivalence**: on never-drained executions `TotalAppended() == Len()` → the re-based feed is byte-identical → existing decoder unit-test ASSERTIONS must NOT change (only call signatures). An assertion change = a re-base bug.
3. **The soundness invariant**: the runtime never drains bytes a filter has not yet been shown; the Task-5 tracking-filter test proves every appended byte is seen exactly once.
4. **The replay is observational**: Status ignored, post-handoff Close not acted on, ContinueReading meaningless — documented BEHAVIOR_CONTRACT boundaries (Task 9), NOT silent.
5. **The 28.1b race surface is empty** (OnWrite is a no-op) — but Task 9's ADR-0221 body MUST carry the **28.2 synchronization forward-pointer** (the OnWrite response decoder will race the replay-path request decoder on the correlation structures; ADR-0223/the 28.2 SPEC must add a per-connection mutex or equivalent).
6. **Arm-by-arm seam dependency for 0046**: arms 1/5/6 were already green at 28.1a; arms 2/3/4 require the seam (multi-socket-read connections) — they are the R8 re-iteration proof.
7. **Counts at 28.1b close**: 49 active fixture dirs (tail `0047`), 37 fuzzers (unchanged), stat table 337, DECISIONS tail ADR-0223 / next-free ADR-0224 (both unchanged).
8. **`internal/listener/manager.go` / `tcp_proxy` / HCM are UNTOUCHED** — the 28.1b production diff to `internal/listener/` is ZERO files.

## Project state at master tip

- **Master tip:** the docs-only SHA-fill/repoint commits trail the substantive 28.1b-PLAN squash (STATE.md `last-commit`) by +1/+2. Confirm the live tip via `git log --oneline -1`; any further next-prompt repoint advances the tip +1 (expected, not chased). Substantive predecessors: the 28.1b-SPEC squash `baf02ed`, the 28.1a-IMPL squash `8703aeb`.
- **§9 Network-filters family:** rows 26 (parent + 26.1/26.2/26.3) + 27 DONE; row 28 `in-progress` (parent; 28.1a `done`, 28.1b `in-progress`, 28.2 `planned`). After phase 28, 4 candidates remain (`redis`/`mongo`/`kafka_broker`/`thrift`).
- **Counts at master tip:** stat surface **136** (rolls to **337** at IMPL Task 9); differential fixtures **47 active + 1 disabled** (→ 49 active at IMPL); fuzzers **37** (canonical recipe `grep -rh "^func Fuzz" $(find ./internal -name fuzz_test.go) | wc -l`; stays 37); conformance 10/10; h2spec 53/53; DECISIONS.md tail **ADR-0223** (next-free **ADR-0224**). ADR-0209 escape-valve reserve STANDS-UNCONSUMED.
- **Toolchain:** Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); reference Envoy v1.37.2 (ADR-0008); go-control-plane v1.32.4 (ADR-0008).
- **Known coverage boundaries (carried, non-blocking):** D-26.3-4; the `tcp_proxy` `downstream_cx_*` family unmirrored; histograms deferred (ADR-0060); zookeeper dynamic-metadata deferred (AMEND-A9) + shallow-decode leniency + terminal-originated-writes-only + the write-only-filter boot boundary (28.1 SPEC §7.5) + the NEW post-handoff observational boundaries (28.1b SPEC §3.5 — documented at IMPL Task 9).

## Read first (in order)

1. THIS FILE — the 28.1b-IMPL cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 28.1b PLAN done`; next-skill `superpowers:subagent-driven-development` (28.1b IMPL).
3. `docs/envoy-go/phases/28.1b-network-filter-read-seam-and-zookeeper-requests-proof/PLAN.md` — THE document this session executes (10 tasks, complete TDD steps, code snippets verified against the as-built tree).
4. `docs/envoy-go/phases/28.1b-network-filter-read-seam-and-zookeeper-requests-proof/SPEC.md` — the design the PLAN translates (§3 the seam, §5 the fixtures, §6 the completion bundle, §11 the test surface).
5. `docs/envoy-go/phases/28.1-network-filter-write-seam-and-zookeeper-requests/SPEC.md` + `PROGRESS.md` — the 28.1a master record (Task-16 BLOCKED analysis + divergence table + PoC evidence + 28.1a closure).
6. `docs/envoy-go/DECISIONS.md` ADR-0045 §AMEND (`:1526`) + ADR-0221 (`:14228`, incl. the 28.1b §AMEND at `:14235`) + ADR-0222 (`:14249`) + ADR-0223 (`:14268`) §Context drafts.
7. The as-built code the tasks touch: `internal/filter/network/chain.go` + `buffer.go` + `writeconn.go` + `prefixconn.go` + `chain_test.go`, `internal/filter/network/zookeeperproxy/decoder.go` + `decoder_test.go` + `zookeeperproxy.go` + `fuzz_test.go`, `test/differential/runner_test.go` (`:72-77`), `test/fixtures/0046-zookeeper-requests/driver/driver.go`, `test/fixtures/0044-network-rbac-boot-reject/driver/driver.go`.
8. `docs/envoy-go/SKILL_ROUTING.md` + the BOOTSTRAP §4/§5 cold-start invariants.
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — execution-style + worktree + push + subagents-no-push + per-task gofmt/lint + the network-filter constraints + the differential dispatch/asserter constraints all carry forward.
