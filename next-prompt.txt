START a fresh cold-start session for envoy-go — **the phase-29.3 sub-phase SPEC (`network-filter-mongo-fault-delay-and-access-log`) is DONE + reviewed (spec-document-reviewer APPROVED first iteration) + ROADMAP-flipped + squash-merged + pushed**. The active phase is **phase 29.3 → SPEC done** (parent row 29 STAYS `in-progress`; sub-row 29.1 `done`; 29.2 `done`; 29.3 `in-progress`). This session opens the **29.3 PLAN** — the FINAL phase-29 sub-phase — via **PLAN-authoring** (per SKILL_ROUTING: SPEC.md exists for 29.3 + PLAN.md does NOT → write the PLAN; `superpowers:writing-plans`). Work in a git worktree (`feedback_git_worktrees`); subagents commit LOCAL-ONLY (`feedback_subagents_no_push`); the controller squash-merges + pushes at stage-close (`feedback_push_to_origin`). Per `feedback_execution_style` the 29.3 IMPL (the session after this PLAN) runs `superpowers:subagent-driven-development`.

The 29.3 SPEC landed at the **29.3-SPEC squash `0415153`**; the docs-only STATE/next-prompt SHA-fill `d3d79f3` is the named live tip (a further docs-only commit may trail; confirm via `git log --oneline -1`). The substantive predecessor on master is the 29.2-IMPL squash `620a3d0` (docs-only `98028ff`/`735798e` trail it); before that the 29.2-PLAN squash `0783776`; the 29.2-SPEC squash `13907f1`; the 29.1-IMPL squash `5ef5215`; the 29.1-PLAN `89c194a`; the 29.1-SPEC `7574219`. The 29.3 SPEC (`docs/envoy-go/phases/29.3-network-filter-mongo-fault-delay-and-access-log/SPEC.md` + README) + the phase-29 parent SPEC + ADR-0226 §Context (the 29.3 charter) + the as-built `internal/filter/network/mongoproxy/` package (29.1+29.2) + the as-built `internal/filter/network/` chain framework are the pinned-behavior references. The ADR-0226 §Decision/§Consequences body lands IN PLACE at the 29.3 IMPL (no new ADR number — DECISIONS tail STAYS ADR-0226, next-free ADR-0227).

## What the 29.3 PLAN must decompose (the SPEC §12 task spine → bite-sized TDD tasks)

The 29.3 SPEC §12 anticipates ~12–16 tasks, **SEAM-FIRST** (the async halt/resume seam lands GREEN with its `-race` test BEFORE any mongo consumer wires into it — the framework is independently testable with a synthetic halting test filter). The PLAN authors the exact write-failing-test → run → implement → run → commit steps with complete Go code blocks, exact file paths, run commands + expected outputs, per-task `gofmt -l` + `golangci-lint`, and the `-count=1` deliberate-break protocol. The anticipated spine (the SPEC §12 table — the PLAN may merge/split):

1. **First-action baselines/anchors gate** — re-pin fixtures **53** (tail `0051`) + fuzzers **39** + stat surface **360** + BackendKind tail **30** + DECISIONS tail **ADR-0226** (next-free **ADR-0227**) + the §9.1 as-built anchors, against the live IMPL-session tip.
2–4. **The async halt/resume seam (FIRST, GREEN before any consumer)** — (2) the halt-state synchronization on `chainRuntime` (the halt mutex + the atomic FAST-PATH + the post-handoff halt flag + a synthetic halting test filter; the never-halting byte-identity unit proof); (3) `ContinueReading` async-active third context (advance-past-halting-filter + dispatch + re-eval terminal readiness; the pre-handoff async-resume unit test); (4) `replayRead` + `readChainConn.Read` post-handoff withhold-until-resume + the `-race -count=N` cross-goroutine seam test (R-HALT; the deliberate-break with `-count=1`).
5–6. **Fault-delay injection** — (5) the decoder roll+duration decision (per-request-message; re-entrancy guard; `rollPercent` deterministic at 100%) + `takePendingDelay`; (6) the filter timer (`time.AfterFunc` + `onDelayTimer` + `ContinueReading`; `delays_injected` at ARM; StopIteration-while-pending; timer cancel on `OnDestroy`) + the timer↔destroy `-race` test.
7–8. **The mongo access log** — (7) the `internal/accesslog` pluggable formatter seam (default `Default` → HCM BYTE-IDENTICAL) + the mongo record carrier + the JSON formatter + the per-opcode `message.toString` goldens; (8) the sink construction (gated on `cfg.accessLog`) + per-message emission both directions + the gated-off no-emit test.
9. **`cx_drain_close`** — the drain-state accessor (`Draining()` through `FactoryCtx`/callbacks; the phase-08.2 `*drain.Manager` is at the listener `rt.dm` but NOT yet at network filters) + `cx_drain_close` on the reply-completion list-empty path + `Connection().Close(FlushWrite)`.
10. **The close-direction seam D-P4 CLOSED** — the `tcp_proxy` pump-EOF-first → `chainRuntime.closeDirection` recording + the `CloseDirection()` accessor + the `OnDestroy` direction-keyed `cx_destroy_local/remote_with_active_rq` VALUE-parity increment (local/remote/all-answered unit tests).
11. **Fixture `0052-mongo-fault-delay`** (cross-side; deterministic 100% arms; the pre/post-handoff delay arms; the no-delay non-perturbation R1 arm; `cx_drain_close`; the `cx_destroy_*` VALUE-parity arms; the R4 break) + README.
12. **The completion bundle** — ADR-0226 §Decision/§Consequences body in place + the BEHAVIOR_CONTRACT 29.3 bundle (incl. the framework async-halt/resume subsection + the access-log + runtime-key boundaries) + STATE/ROADMAP sub-row 29.3 `in-progress → done` **AND the parent-row-29 ROLLUP** (parent `in-progress → done` ATOMICALLY) + next-prompt.txt + the six-gate (full 54-dir suite + h2spec 53/53 + proxy-wasm 10/10).

## The 8 additive D-questions the PLAN resolves (where PLAN-time)

- **D-S29.3-1 (halt-seam sync primitive)** — Mutex+`sync.Cond` (block the pump's `Read`) vs a release channel vs withhold-and-retry; the exact lock scope (the ADR-0223 minimal critical section). Anticipated: halt mutex + `sync.Cond`, gated behind an atomic fast-path. (The `-race` + back-compat byte-identity gates settle it — likely IMPL, but the PLAN pins the design.)
- **D-S29.3-2 (async-resume terminal handoff)** — how an async resume that advances `resumeIdx` past all filters triggers the deferred handoff from the timer goroutine. Anticipated: re-enter the read loop's handoff path under the halt mutex; the common `0052` first-message delay fires pre-handoff.
- **D-S29.3-3 (the callbacks accessor surface)** — `Draining()` + `CloseDirection()` on `ReadFilterCallbacks` vs `Connection`; a narrow bool vs the `*drain.Manager` handle through `network.FactoryCtx`. Anticipated: minimal accessors (NOT a manager leak), one folded callbacks-extension ripple.
- **D-S29.3-4 (access-log mongo carrier)** — extend `Record` with an opaque mongo field vs a typed-payload formatter vs a mongo-owned sibling sink. Anticipated: a minimal mongo record type + a mongo formatter; the HTTP `Default` bytes UNCHANGED (the HCM byte-identity gate constrains it).
- **D-S29.3-5 (fault-eval callback set — transcription check)** — the parent §11.6 six-callback set (decodeQuery/Insert/GetMore/KillCursors/Command/CommandReply — NOT decodeReply) inherited verbatim; transcribe against `proxy.cc` v1.37.2 (`reference_wire_format_both_sides_see_same_bytes`). `0052` exercises OP_QUERY.
- **D-S29.3-6 (fault filter↔decoder split + the guard)** — the decoder decides+signals `takePendingDelay`; the filter owns the `time.AfterFunc`+`cb`; an `atomic.Bool` re-entrancy guard + a CAS-guarded at-most-once timer-cancel (the phase-09 `markedActive` precedent).
- **D-S29.3-7 (the omitted zero-ms drain timer)** — confirm envoy-go's deferred `Connection.Close` subsumes upstream's zero-ms drain timer with no observable `0052`-arm difference.
- **D-S29.3-8 (the `0052` drain-trigger mechanism)** — how the fixture drives the drain decision cross-side (the phase-08.2 admin `/drain` vs a listener-drain trigger) so `cx_drain_close` is differentially asserted; else PRESENCE + a unit-test value proof. **Treat early as a real risk** (the spec-reviewer flagged it; do not rubber-stamp).

## Load-bearing pins the 29.3 PLAN must honor

- **29.3 is the framework-SURGERY sub-phase** (contrast 29.1/29.2 framework-ZERO-touch) — the ONE consolidated ripple (ADR-0219): the halt seam (`chain.go`/`readconn.go`/`callbacks.go`) + the close-direction recording (`tcp_proxy`/`chain.go`) + the drain-state threading (`types.go`/`manager.go`/`callbacks.go`) + the accesslog formatter seam (`internal/accesslog`). The **full 53-dir suite + h2spec 53/53 + proxy-wasm 10/10 MUST stay byte-exact green** at the 29.3 six-gate — the seam's R1 back-compat / non-perturbation gate.
- **Never-halting chains BYTE-IDENTICAL (R1)** — the synchronization gates behind an atomic FAST-PATH; a chain that never halts (zookeeper, every existing filter, mongo with no `delay`) takes the byte-identical pre-29.3 path.
- **The post-handoff halt path is LOAD-BEARING** — steady-state mongo decode runs post-handoff via `replayRead`; a delay on the second-or-later message MUST be honored there. `0052` drives ≥2 messages so a delay fires BOTH pre- and post-handoff.
- **`delays_injected` at timer-ARM; deterministic 100%-probability arms; timing NEVER compared** (BOOTSTRAP §7.2). The access log is differential-INVISIBLE (timing-bearing JSON — AMEND-B10): unit goldens + a coverage boundary, NO fixture dir.
- **The break protocol needs `-count=1`** (`reference_differential_break_protocol_count1`) — for the `0052` R4 break + the seam `-race` deliberate-break (Go test result caching serves a stale PASS otherwise).
- **Per-task gofmt + golangci-lint** on touched packages (`feedback_pertask_gofmt_lint`); each IMPL subagent task runs `gofmt -l` + `golangci-lint`, not just `go vet`.
- **The histogram families stay DEFERRED** (ADR-0060) — the per-command/per-collection `reply_*` families remain unmirrored project-wide.

## What this session must do

1. Re-establish a worktree (`feedback_git_worktrees`); confirm the clean baseline (`go build ./...` + `go test ./internal/... -count=1`).
2. Author the **29.3 PLAN** (`docs/envoy-go/phases/29.3-network-filter-mongo-fault-delay-and-access-log/PLAN.md`) — bite-sized TDD tasks decomposing the SPEC §12 spine (seam-first), with complete Go code blocks, exact file paths, run commands + expected outputs, per-task `gofmt -l` + `golangci-lint`, the `-count=1` deliberate-break protocol, and the D-S29.3-1..8 PLAN-time resolutions. Run the ADR-0045 FINAL gate-check (~420–760 production LoC / ~12–16 tasks → anticipated NO split). Run `plan-document-reviewer`; fold advisories.
3. Flip nothing in ROADMAP (sub-row 29.3 STAYS `in-progress` through PLAN+IMPL; parent STAYS `in-progress`); advance STATE/next-prompt for the **29.3-IMPL** cold-start. Controller: squash-merge + fill the squash SHA into STATE/next-prompt + push.

## Project state at master tip

- **Master tip:** the docs-only SHA-fill `d3d79f3` trailing the substantive 29.3-SPEC squash `0415153` (confirm the live tip via `git log --oneline -1`; a further docs-only commit may trail). STATE.md `last-commit` points at the substantive squash `0415153`. Substantive predecessors: the 29.2-IMPL squash `620a3d0`; the 29.2-PLAN `0783776`; the 29.2-SPEC `13907f1`; the 29.1-IMPL `5ef5215`; the 29.1-PLAN `89c194a`; the 29.1-SPEC `7574219`. origin/master in sync after push.
- **§9 Network-filters family:** rows 26/27/28 DONE; row 29 `in-progress` (29.1 `done`; 29.2 `done`; 29.3 `in-progress` — SPEC done; its IMPL carries the parent-row-29 ROLLUP).
- **Counts at master tip (29.3 SPEC done — docs-only):** stat surface **360** (+0 anticipated at 29.3 IMPL — increment-wiring only); differential fixtures **53** (tail `0051-mongo-responses`; → 54 at 29.3 IMPL with `0052-mongo-fault-delay`); fuzzers **39** (no new fuzzer at 29.3 — a seam `-race` test instead); BackendKind tail **30** (`TCPMongoResponder` reused); conformance h2spec 53/53 + proxy-wasm 10/10 (as of the 29.2 six-gate, 2026-06-04); DECISIONS.md tail **ADR-0226** (next-free **ADR-0227**; the ADR-0226 §Decision/§Consequences body lands at the 29.3 IMPL — no new number).
- **Toolchain:** Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); reference Envoy v1.37.2 (ADR-0008); go-control-plane v1.32.4 (ADR-0008).
- **Known coverage boundaries (carried, non-blocking):** the mongo D-P1 boot-window eager stat-creation departure (unobservable); the `stats.IsValidName` guard (`reference_dynamic_stat_name_charset_guard`); the mongo dynamic-HISTOGRAM families deferred (ADR-0060); the dynamic-metadata emission differential-invisible (AMEND-B11); the access-log timing-bearing differential boundary (AMEND-B10 — 29.3 unit goldens + boundary, NO fixture); the runtime-key-gating boundary (fault/drain/logging keys at defaults — 29.3); the close-direction `cx_destroy_*` value parity is 29.3's (D-P4 CLOSED at the SPEC; the increment + value parity land at the 29.3 IMPL); `tcp_proxy` `downstream_cx_*` unmirrored; histograms deferred project-wide (ADR-0060).

## Read first (in order)

1. THIS FILE — the 29.3-PLAN cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 29.3 SPEC done`; next-skill the 29.3 PLAN authoring path; next-free `ADR-0227`.
3. `docs/envoy-go/phases/29.3-network-filter-mongo-fault-delay-and-access-log/SPEC.md` + README — the authoritative 29.3 surface: §3 (the five deliverables + the seam design), §6 (the `0052` arms + the access-log goldens), §10 (the resolved D-P5/D-P7/D-P12 + the additive D-S29.3-1..8), §12 (the task spine + the ADR-0045 gate), §13 (the test surface + acceptance checklist).
4. `docs/envoy-go/phases/29-network-filter-mongo-proxy/SPEC.md` — the parent master: §4.1 (the async halt/resume seam), §11.6 (fault-delay), §11.7 (continueReading), §11.8 (access-log), §11.10 (close direction + drain), §8.4 (`0052`), §15 (the 29.3 envelope).
5. `docs/envoy-go/DECISIONS.md` ADR-0226 §Context (the 29.3 charter; its body lands at the 29.3 IMPL) + ADR-0225/0224 (the response/request side) + ADR-0221 (the WriteFilter seam + the anticipated-halt-consumer allowance 29.3 CONSUMES, read-side-refined) + ADR-0213/0215 (the chain framework + TerminalFilter seam) + ADR-0223 (the minimal-critical-section synchronization) + ADR-0219 (the one-ripple discipline) + ADR-0044/0052/0060.
6. The as-built surfaces the PLAN's code blocks target: `internal/filter/network/chain.go` (`runData`/`ContinueReading`/`replayRead`/`terminalReady`/`handleTerminal`/`connection.Close`) + `readconn.go` (`readChainConn.Read`) + `callbacks.go`/`types.go` (the `Connection` interface + `FactoryCtx`) + `internal/filter/tcpproxy/filter.go` (the two pumps) + `internal/listener/manager.go` (`rt.dm`) + `internal/drain/manager.go` (`IsDraining()`) + `internal/accesslog/writer.go`/`format.go` (the formatter seam) + `internal/filter/http/fault/fault.go` (the `rollPercent`/`time.AfterFunc`/`markedActive` precedent) + `internal/filter/network/mongoproxy/` (`config.go` the parsed `delay`/`access_log`; `codec.go` the decode loop + `takeQuery` + `onDestroy`; `filter.go` the `OnData`/`OnWrite`/`OnDestroy` + `cb`; `stats.go` the eager roster) + `test/fixtures/0051-mongo-responses/` (the `0052` driver template + `TCPMongoResponder`).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — execution-style + worktree + push + subagents-no-push + per-task gofmt/lint + the differential dispatch/asserter/break-protocol (`-count=1`) + wire-format-both-sides + the dynamic-stat charset guard + the close-direction framework-gap boundary (which 29.3 CLOSES) all carry forward into the 29.3 PLAN.
