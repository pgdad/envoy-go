START a fresh session for envoy-go (router last updated 2026-06-25; the **phase 44.2 (`grpc-access-log` buffering) SPEC is DONE + pushed to origin/master**). HEAD is the **phase-44.2 SPEC squash** (subject `phase 44.2 (grpc-access-log buffering) SPEC: …`) — a docs-only commit — OR a later `next-prompt.txt:` tip-stamp stacked ATOP it; re-read the shas with `git log --oneline -14`, do NOT assume — the master tip is EITHER the 44.2 SPEC squash itself OR the LATEST `next-prompt.txt:` commit, with the 44.2 SPEC squash the stable anchor BENEATH any run of docs-only tip-stamps. BENEATH the 44.2 SPEC squash is the **phase-44.1 IMPL squash `ec05a421`** (`phase 44.1 (grpc-access-log core) IMPL: …`), then the phase-44.1 PLAN squash `5c7cf481`, the phase-44.1 SPEC squash `a7a24d0c`, the phase-44 BRAINSTORM `81afdd59`. **The Observability family is OPEN** (phase 44 is its FIRST row; the family STAYS OPEN — 44.3 header-capture / OTLP / tracing / stats sinks / tap remain future rows). **The next action is the 44.2 (buffering) PLAN** — the §12 D-BUF-* PLAN questions + the ~7–9-task TDD spine; CONFIRM against `docs/envoy-go/STATE.md` (the source of truth for "what next") before committing to the stage. **Git hygiene:** all phase work runs in a FRESH git worktree off master (`feedback_git_worktrees`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the controller squashes + pushes at stage-close (`feedback_push_to_origin`); each code task runs `gofmt -l` + `golangci-lint` on touched pkgs (`feedback_pertask_gofmt_lint`); the controller verifies `git -C <main> status` clean + the worktree branch unchanged after each task (`feedback_subagent_worktree_detach` / `feedback_subagent_worktree_path_targeting`).

First confirm the state via `git log --oneline -14` (expect HEAD to be the 44.2 SPEC squash OR a `next-prompt.txt:` tip-stamp atop it, with `ec05a421` the 44.1 IMPL squash beneath, then `5c7cf481`/`a7a24d0c`/`81afdd59`; re-read the shas, do NOT assume). Counts at the 44.2 SPEC-done (docs-only — UNCHANGED from the 44.1 IMPL): stat surface **1189** (H2 cluster; non-H2 **1185**) / fixtures **83** (tail `0081-grpc-access-log`; NOTE: count fixtures with `ls -d test/fixtures/[0-9][0-9][0-9][0-9]* | wc -l` — `grep -cE '^[0-9]{4}-'` UNDERCOUNTS by 2, skipping `0007a`/`0007b`) / fuzzers **44** (`^func Fuzz` == 44) / BackendKind tail **38** (`H2GoawayResponder`; the ALS receiver is the driver-owned test-only `test/helpers/accessloggrpc`, NOT a BackendKind) / DECISIONS tail **ADR-0255**, next-free **ADR-0256** (the anticipated 44.2 buffering ADR).

## NEXT STEP — the 44.2 (buffering) PLAN (`superpowers:writing-plans`)

The 44.2 leg makes `buffer_size_bytes` / `buffer_flush_interval` (PARSE-ACCEPT-but-INERT at 44.1, D-ALS-CONFIG-DEFER) LIVE: the `GrpcAccessLogSink` writer goroutine REPLACES the 44.1 fixed one-entry-per-message flush with a BUFFER that accumulates `HTTPAccessLogEntry`s and flushes a BATCH on EITHER the accumulated serialized-entry-byte sum reaching `buffer_size_bytes` OR a `buffer_flush_interval` timer elapsing, whichever first. Anticipated ADR-0256. The SPEC is authored + reviewer-APPROVED + the §11 D-ALS-BUFFER live probe is DONE; the PLAN authors the ~7–9-task TDD spine + resolves the §12 D-BUF-* PLAN questions. A fresh worktree off master (`feedback_git_worktrees`).

Carry-forward design facts from the 44.2 SPEC (`docs/envoy-go/phases/44-grpc-access-log/SPEC-44.2.md`; ADR-0256 §Context drafted in §13; the §11 D-ALS-BUFFER pins + AMEND-BUF-1..5):
- **AMEND-BUF-1 (EITHER-OR flush + byte accounting):** flush the accumulated batch on serialized-entry-byte SUM ≥ `buffer_size_bytes` OR `buffer_flush_interval` elapsed; the byte sum = each buffered entry's `proto.Size`/`ByteSizeLong`, the crossing entry INCLUDED in the flushed batch.
- **AMEND-BUF-2 (defaults):** `buffer_size_bytes` default **16384**, `buffer_flush_interval` default **1s** — wrapper/duration ABSENT ⇒ default; explicit `buffer_size_bytes: 0` ⇒ flush-every-entry. The interval default is a HARD PANIC-GUARD (`time.NewTicker(d)` panics for `d <= 0`) — parse-layer coerce nil/zero → 1s; positive explicit values (incl. sub-second) honored verbatim. Use the GETTER nil-check (`if w := common.GetBufferSizeBytes(); w != nil { … }`), NOT the direct field.
- **AMEND-BUF-3 (reference buffers PER-WORKER-THREAD):** cross-side batch/message counts INFEASIBLE; the `0082` differential asserts the per-entry payload AGGREGATED across messages (AMEND-ALS-3 REINFORCED) + a SUBJECT-side-only `maxBatchSize >= 2` proof (which BITES against a revert to the 44.1 fixed flush). envoy-go's single process-global buffer is a legitimate alternative framing. The differential drive MUST guarantee coalescence (concurrent / single-keep-alive rapid-fire so ≥2 entries land in one flush interval) + confirm non-flaky over 20/20 (the §8.1 coalescence caveat).
- **AMEND-BUF-4 (NO new stat):** surface stays **1189**; the 2 existing `access_logs.grpc_access_log.{logs_written,logs_dropped}` counters carry; `logs_written` ++ per ENTRY (batch-invariant — `stats.Counter.Add(uint64(len(buf)))`).
- **AMEND-BUF-5 (drain):** flush the pending buffer on Close BEFORE `CloseAndRecv` (a faithful improvement over the reference's lossy per-worker `Canceled` drain).
- The buffer machinery lives in `internal/accesslog/grpcsink.go` `run()` (a `buf []*HTTPAccessLogEntry` + `bufBytes` running sum + a `time.NewTicker` flush-interval timer + a `flush()` closure firing on size-OR-timer; mind the `-race` background-mutator gate `reference_full_suite_race_after_background_mutator` — the buffer is touched ONLY by the single writer goroutine, no new shared state). The `NewGrpcAccessLogSink` signature extends with `bufferSizeBytes`/`bufferFlushInterval`; `main.go` passes `cfg.BufferSizeBytes`/`cfg.BufferFlushInterval`. The parse arm (`internal/bootstrap/bootstrap.go` `parseGrpcAccessLog`) lifts the two fields into `ALSConfig` (+ defaults). The driver-owned `test/helpers/accessloggrpc` receiver gains per-message batch exposure (`BatchSizes`/`MaxBatchSize`/`MessageCount`). The `0082-grpc-access-log-buffering` differential is the new fixture (83 → 84).

## §12 D-BUF-* PLAN questions to resolve
- **D-BUF-TIMER-MECHANISM** — `time.NewTicker` (fixed period; empty-buffer no-op tick) vs a reset-on-flush `time.Timer` (the "interval since last flush" semantics, closer to the reference). Ticker is the SPEC default.
- **D-BUF-FUZZER-CORPUS** — buffer-bearing seed corpus for the EXISTING `FuzzParseHttpGrpcAccessLogConfig` (tiny/0/huge `buffer_size_bytes`, sub-ms/very-long `buffer_flush_interval`); NO new `^func Fuzz`.
- **D-BUF-RECEIVER-BATCH-API** — the exact `accessloggrpc.Server` batch surface + `Reset()` clears it + the mutex discipline.
- **D-BUF-DIFFERENTIAL-DRIVE** — the exact `0082` buffer settings (large `buffer_size_bytes` + a `buffer_flush_interval` wide enough vs the burst that ≥2 entries coalesce) + N + the burst pattern + the `maxBatchSize >= 2` assertion form, non-flaky over 20/20.
- **D-BUF-SPLIT-FINAL** — the final ADR-0045 re-check at PLAN with real LoC (≈110–160 prod LoC, one leg).

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** This session is the 44.2 PLAN (lifecycle-state 1 → 2 for the 44.2 leg; row 44 STAYS `in-progress` — the Observability family STAYS OPEN). `feedback_git_worktrees` (fresh worktree off master) · `feedback_subagents_no_push` · `feedback_push_to_origin` · `feedback_execution_style` (the IMPL stage, later, runs subagent-driven).
- **Process anchors:** ADR-0106 (row 44 flips `done` PER-LEG; NO parent rollup) · ADR-0044 (the ADR-0256 §Decision/§Consequences land at the 44.2 IMPL; §Context drafted in SPEC-44.2 §13) · ADR-0045 (the split gate — re-check 44.2 sizing at PLAN, D-BUF-SPLIT-FINAL) · ADR-0080 (strict-reject — UNCHANGED at 44.2, no new reject) · ADR-0255 (the 44.1 as-built the buffer machinery extends) · ADR-0256 (the anticipated 44.2 buffering ADR).
- **Streaming-sink differential discipline:** `reference_streaming_sink_differential_framing` (assert the PAYLOAD aggregated across messages, NOT framing — AMEND-ALS-3, REINFORCED by the per-worker finding) · `reference_differential_grpc_receiver_driver_owned` (the ALS receiver is a driver-owned `test/helpers/accessloggrpc` server the proxy DIALS, NOT a BackendKind) · `reference_docker_probe_bridge_network` (shared-bridge + container-hostname; the Docker-Desktop host-gateway gotcha — the contrib Envoy c-ares resolver did NOT reach the host via `host.docker.internal:host-gateway`) · `reference_concurrency_differential_release_barrier` (poll-to-converge, NEVER `time.Sleep`) · `reference_differential_run_selector` (`-run 'TestDifferential/0082'`, NOT bare) · `reference_differential_break_protocol_count1` (`-count=1` on every break + `-race`) · `reference_differential_fullsuite_startup_flake` (a `subject ready: EOF` is a transient startup race — isolate-re-run) · `reference_full_suite_race_after_background_mutator` (the sink's writer goroutine is a background mutator — re-run the FULL package `-race`) · `reference_fuzzer_count_docs_drift` (re-verify `^func Fuzz` == 44 at the completion task) · the AMEND-ALS-2 query-less-path constraint carries.

## Read first (in order)
1. THIS FILE — the cold-start router (phase 44.2 SPEC is DONE + pushed; the next action is the 44.2 buffering PLAN).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 44.2 (grpc-access-log) SPEC done`) + the recorded NEXT action + the AMEND-BUF-1..5 findings + the count figures (source of truth for "what next").
3. `docs/envoy-go/phases/44-grpc-access-log/SPEC-44.2.md` — the 44.2 design (the §11 D-ALS-BUFFER pins + AMEND-BUF-1..5; the §3 buffer machinery; the §8 `0082` differential; the §12 D-BUF-* PLAN questions; the §13 ADR-0256 §Context draft).
4. `docs/envoy-go/phases/44-grpc-access-log/SPEC-44.1.md` + `PLAN-44.1.md` — the 44.1 design + the ~12-task TDD-spine SHAPE the 44.2 PLAN mirrors (the house plan-document shape).
5. `docs/envoy-go/DECISIONS.md` — ADR-0255 (the 44.1 as-built; the `GrpcAccessLogSink`/parse arm/stats) + next-free **ADR-0256** (the 44.2 buffering ADR).
6. `docs/envoy-go/ROADMAP.md` — row 44 (`grpc-access-log`, in-progress; leg 44.1 done, 44.2 SPEC done) + the `### Observability family` heading.
7. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the `### Access log — gRPC Access Log Service (ALS) streaming sink` block (the §9 SPEC-44.2 contract-delta target) + the stat-surface 1189 entry.
8. The 44.1 as-built seams the 44.2 PLAN/IMPL builds on: `internal/accesslog/grpcsink.go` (the `run()` fixed flush 44.2 replaces; the `closeDrainGrace` bounded-Close; the `NewGrpcAccessLogSink` signature), `internal/accesslog/mapping.go` (`buildHTTPAccessLogEntry`), `internal/accesslog/stats.go` (`RegisterGrpcSinkCounters`; `stats.Counter.Add`), `internal/bootstrap/bootstrap.go` (`parseGrpcAccessLog` + `ALSConfig`; the `CommonGrpcAccessLogConfig` buffer accessors), `test/helpers/accessloggrpc/` (the receiver accumulator to extend with batch exposure), `test/fixtures/0081-grpc-access-log/` (the differential precedent — driver dir `driver/`).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `reference_streaming_sink_differential_framing`, `reference_differential_grpc_receiver_driver_owned`, `reference_docker_probe_bridge_network`, `reference_full_suite_race_after_background_mutator`, `reference_fuzzer_count_docs_drift`).
