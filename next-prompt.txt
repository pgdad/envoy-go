START a fresh 24.1 IMPL cold-start session for envoy-go.

This is a fresh-session prompt for the **24.1 IMPL execution** of phase 24.1 (`global-ratelimit-core-and-route-table`, the CORE decision path of `envoy.filters.http.ratelimit` — the GLOBAL rate-limit filter). The **24.1 PLAN is authored + plan-document-reviewer-APPROVED** (iteration 1 of ≤3) and landed at master via squash-merge of `phase-24.1-global-ratelimit-core-and-route-table-plan` (+ SHA-fill follow-up; docs-only; pushed to origin). STATE points at **24.1**, lifecycle-state 3. The natural next-skill per STATE.md + SKILL_ROUTING state 3 is **`superpowers:subagent-driven-development`** to EXECUTE `docs/envoy-go/phases/24.1-global-ratelimit-core-and-route-table/PLAN.md` task-by-task (project memory `feedback_execution_style.md`: subagent-driven over inline). The PLAN has **12 bite-sized TDD tasks** (Pre-Task 0 + Tasks 1-12).

## What this session must do: execute the 24.1 PLAN

Per SKILL_ROUTING state 3, run `superpowers:subagent-driven-development`: a fresh `general-purpose` subagent per Task, TDD on each Task (`superpowers:test-driven-development`: write failing test → verify fail → minimal impl → verify pass → commit), a two-stage review between Tasks, append to PROGRESS.md on each Task completion. The PLAN's `## Task graph` gives the dependency order + the parallelizable batches; the `## Execution preconditions` block (12 preconditions) is verified at Pre-Task 0.

1. **Create a fresh IMPL worktree** per `feedback_git_worktrees.md` (branch `phase-24.1-global-ratelimit-core-and-route-table-impl`, off master tip — run `git log --oneline -1` to confirm the actual tip SHA).
2. **Pre-Task 0 → Task 12** per the PLAN. The dependency graph: Pre-Task 0 → Task 1 → {2,3,4 PARALLEL} → Task 3 gates {5,6} → {2,3,4,5,6} → Task 7 → {8,9 PARALLEL} → Task 9 gates {10,11 PARALLEL} → Task 12 (atomic landing).
3. **Land the 3 ADR §Decision+§Consequences bodies at their anchored Tasks**: ADR-0200 (FULL) @ Task 3 (compiled_config + §5 PARSE-REJECT roster); ADR-0198 (FULL) @ Task 5 (DELTA-2 route-table exposure); ADR-0197 (CORE slice) @ Task 7 (engine+client+dispositions). Their §Context drafts already exist at the parent SPEC commit.
4. **The DELTA-2 highest-risk byte-confirmation (parent SPEC §12 item 1) settles at Task 5** — the exact chain-seed type + accessor return-type. PLAN RECOMMENDED (D-RL1): seed RAW `[]*routev3.RateLimit`; expose `RouteRateLimits()`/`VirtualHostRateLimits()`. If the seed shape MUST diverge from the ADR-0165 set-once primitive, FIRE the escape-valve **ADR-0202** at Task 5 (§Context+§Decision+§Consequences) + escalate. Hypothesis: ADR-0202 stays UNCONSUMED.
5. **Six-gate verification at Task 12** (A build / B vet+lint / C race / D differential 35/35 [0000-0033] / E fuzz 33 fuzzers / F h2spec 53/53 at the ADR-0051 v1.32.4 pin); BEHAVIOR_CONTRACT partial bundle (departures 15→18; stat 110→114; NEW subsection core); ROADMAP row 24.1 `in-progress → done` (parent row 24 STAYS in-progress); STATE re-advance to 24.2; REVIEW.md.
6. After 24.1 phase-done (six gates GREEN + REVIEW approved), squash-merge to master + push to origin per `feedback_push_to_origin.md`; re-advance STATE for the **24.2 PLAN** (next-skill `superpowers:writing-plans`).

## Pre-existing settled context (DO NOT re-litigate)

- **The parent SPEC empirical AMEND-1..11 catalog is SETTLED** (parent SPEC §1.1) — the implementation contract. Do NOT re-derive. Load-bearing: AMEND-1 cluster-scope stats `cluster.<rls>.ratelimit[.<stat_prefix>].{ok,error,over_limit,failure_mode_allowed}` 110→114; AMEND-2 no runtime proto fields; AMEND-3 13-field roster + defaults/clamps; AMEND-6 descriptor `hits_addend` `UInt64Value` + proto-number-faithful fake encoding; AMEND-8 OVER_LIMIT/error byte-shape; AMEND-9 DELTA-2 chain-seeded accessor pair (HIGH-risk); AMEND-10 cross-namespace `NewCounterIfAbsent` write; AMEND-11 action roster + key defaults.
- **24.1 scope (do NOT pull 24.2 surface in):** package skeleton + boot-registration (19 HTTP filters) + `compiledConfig` + DELTA-1 `RateLimitClient` + the **5 CORE actions** (`generic_key`/`request_headers`/`remote_address`/`destination_cluster`/`header_value_match`) + dispatch + OK/OVER_LIMIT/error dispositions + cluster-scoped 4-counter stats + DELTA-2 route-table exposure + the FULL §5 PARSE-REJECT roster (departures 15→18) + the 33rd fuzzer + `0032`(a/b/c/d-core/e/h) + `0033` (fixtures 33→35).
- **24.2 surface is OUT OF SCOPE at 24.1** (guarded by PLAN precondition 6): the remaining 5 actions; X-RateLimit DRAFT_VERSION_03 emission (`enable_x_ratelimit_headers` is PARSED into compiledConfig but NOT emitted — `dispositions.go` leaves the injection point STUBBED, D-RL7); `RateLimitPerRoute` 10th canonical + ADR-0125 9→10; the `stage` multi-stage path; the Axis-B `vh_rate_limits` table; `0032`(f/g). Canonical-per-route roster STAYS 9.

## Base + invariants at master tip

- **Master tip:** the 24.1 PLAN squash-merge + SHA-fill follow-up (run `git log --oneline -1` to confirm the actual tip SHA for the IMPL worktree base). Predecessors include `5a83fad`/`9e5de25`/`5d0c601`/`bf868a6` (the split squash). Pushed to origin.
- **Baseline at master tip (docs-only since the split):** **33 differential fixture directories** GREEN (0000-0031; lua 0026-0029 + multi-listener combined runs may hit the documented `freeTCPPort` flake per 22.2 REVIEW §7.4 — re-run in isolation); **32 fuzzers** green; h2spec 53/53 at the ADR-0051 v1.32.4 pin; build/vet/lint/race clean; **18 HTTP filters wired** (→ 19 after Task 7 boot-reg); **110 stat names** (→ 114); **15 envoy-go-strict departure records** (→ 18). DECISIONS.md tail at **ADR-0201**; ADR-0197..0200 §Context-anchored; **next-free ADR-0202**.
- Project memories ALL apply: `feedback_git_worktrees.md`, `feedback_push_to_origin.md`, `feedback_execution_style.md`, `reference_differential_fixture_dispatch_constraint.md`, `reference_differential_asserter_dispatch.md` (scenario h `0032` uses `StatsAsserter`, proven live via deliberate-break).

## Read first (in order)

1. THIS FILE — the 24.1 IMPL cold-start + the subagent-driven execution obligations.
2. `docs/envoy-go/phases/24.1-global-ratelimit-core-and-route-table/PLAN.md` — the 12-task TDD plan (task graph + preconditions + per-task steps + commit templates). THE authoritative execution input.
3. `docs/envoy-go/phases/24.1-global-ratelimit-core-and-route-table/SPEC.md` — the 24.1 scope + file roster (§4) + fixtures (§3) + gates (§6) + ADR landing (§5).
4. `docs/envoy-go/phases/24-http-filter-global-ratelimit/SPEC.md` — the RETAINED parent master SPEC (§1.1 AMEND catalog; §4 engine; §5 PARSE-REJECT roster; §6 code shapes + §6.10 roster; §7 differential; §10 ADR map; §11 D1–D7; §12 byte-confirmations; §14 testing taxonomy; §15 acceptance).
5. `docs/envoy-go/STATE.md` — active-phase 24.1; lifecycle-state 3; next-skill `superpowers:subagent-driven-development`; next-free ADR-0202.
6. `docs/envoy-go/ROADMAP.md` — row 24 parent (`in-progress`); row 24.1 `in-progress` (PLAN authored); row 24.2 `planned`.
7. `docs/envoy-go/SKILL_ROUTING.md` — confirm state 3 (PLAN.md exists, IMPL incomplete → subagent-driven-development).
8. `docs/envoy-go/DECISIONS.md` tail — ADR-0197..0200 §Context drafts (the §Decision+§Consequences bodies land at Tasks 3/5/7) + ADR-0201 (split ADR, full body).
9. `BOOTSTRAP_PROMPT.md` (repo root) — §4.1 STATE-update invariant + §5 state machine (state 3 → IMPL).
10. The nearest sub-phase IMPL precedents: `docs/envoy-go/phases/22.1-http-filter-lua-vm-and-headers-bridge/PROGRESS.md` + `docs/envoy-go/phases/19.1-http-filter-ext-proc-headers/PROGRESS.md` (the subagent-driven IMPL execution shape + PROGRESS discipline).
11. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`.
