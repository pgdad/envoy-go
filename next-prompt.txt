START a fresh cold-start session for envoy-go — **phase 33 (`network-filter-thrift-proxy`) is DONE and the §9 Network-filters family is CLOSED (0 candidates remain); the next lifecycle step is a NEW feature-family BRAINSTORM** (`superpowers:brainstorming` — the next subject is the user's choice from the remaining unopened families; see "What this session does" below). There is NO pending phase work: phase 33 landed end-to-end (BRAINSTORM → SPEC → PLAN → IMPL) and is squash-merged + pushed to origin/master.

The literal live master tip is **`4252c1b`** (`Merge pull request #1 from pgdad/fix/ci-github-actions` — the GitHub-Actions CI build-fix PR, merged 2026-06-10, fast-forwarded into local master). The substantive phase-33 IMPL squash sits one commit below at **`97aefd6`** (`phase 33 IMPL: network-filter-thrift-proxy — land envoy.filters.network.thrift_proxy (the §9 Network-filters family CLOSES)`). Confirm via `git log --oneline -3`. The CI-fix PR is a test/harness/workflow-only change (Node-20 action de-deprecation in `.github/workflows/ci.yml`; subject-start-retry-with-fresh-ports in the differential harness that DEFLAKES the `0027-http-lua-full-bridge` "subject ready: EOF" intermittent — re-ran 0027 5/5 green post-merge; wasm/sdsfile `-short` deflaking; fixture-0006 access-log /tmp-mount fix) — it touches NO thriftproxy/production path and does not affect the phase-33 surface.

## What this session does — pick + BRAINSTORM the next feature family

The §9 Network-filters family that opened at phase 26 (chain framework + echo/direct_response/rbac_network) CLOSED at phase 33 (echo/direct_response/sni_cluster/rbac_network/zookeeper via 26/27/28; mongo via 29; kafka_broker via 30→31; redis via 32; thrift via 33). **0 §9 candidates remain.** The next phase opens a DIFFERENT feature family — the subject is a genuine design choice and is the FIRST thing to settle in the brainstorm (do NOT presume it). Per `feedback_execution_style` run subagent-driven once there's a plan; per `feedback_git_worktrees` do the work in a worktree; per `feedback_subagents_no_push` subagents commit LOCAL-ONLY and the controller squash-merges + pushes at stage-close.

**Remaining unopened feature families (ROADMAP "Feature Families (09+)"):**
- **HTTP filters family** — header manipulation, cors, compression, fault, local/global rate limit, jwt_authn, rbac, ext_authz, ext_proc, oauth2, csrf, buffer, lua, wasm, adaptive concurrency, admission control, bandwidth limit. (Has many prior landed phases; the heading is NOT marked closed — candidates may remain.)
- **Load balancing family** — least_request, random, ring_hash, maglev, subset LB, locality-weighted LB, priority load balancing, panic thresholds.
- **Upstream robustness family** — active health checks (HTTP/TCP/gRPC/custom), outlier detection, circuit breakers, retries + hedging, per-protocol connection pooling.
- **HTTP/3 + QUIC family** — quic-go transport, downstream H3 listener, upstream H3 cluster, `h3spec` gate.
- **gRPC family** — gRPC bridge, gRPC-Web, gRPC-JSON transcoding, interop conformance.
- **xDS / dynamic config family** — ADS, delta xDS, LDS/CDS/RDS/EDS/SDS/RTDS, reconnection, initial-fetch timeout.
- **Observability family** — gRPC ALS, OTLP access log, OTel/Zipkin/Jaeger/Datadog/XRay tracing, stats sinks, tap filter.
- **Runtime + hot restart family** — runtime layer (RTDS consumer), hot-restart/graceful-drain beyond phase-08's minimum.
- **WASM host family** — its own multi-phase sub-project (ABI, engine binding, proxy-wasm conformance).

The brainstorm settles: which family + which first subject; the MVP envelope (what lands vs defers); whether a new framework seam is needed (and the ADR-0045 split-gate); the differential proof shape (a NEW family may not be a network-filter — e.g. a load-balancer or health-checker is exercised differently than the §9 `StatsAsserter`/byte-equivalence prongs); the empirical-probe plan if a live reference comparison applies (`reference_docker_probe_bridge_network`).

## Counts at master tip (phase 33 done — they advance only when the NEXT phase lands)

- stat surface **1116** (BEHAVIOR_CONTRACT doc count; +25 at phase 33 = 24 thrift counters + 1 gauge `request_active`; the `request_time_ms` histogram DEFERRED per ADR-0060, not counted).
- differential fixtures **60** (tail `0058-thrift-boot-reject`; `ls -d test/fixtures/[0-9]* | wc -l`).
- fuzzers **42** (tail `FuzzThriftDecode`; `grep -rh "^func Fuzz" $(find ./internal -name fuzz_test.go) | wc -l`).
- BackendKind tail **33** (`TCPThriftResponder`; `test/differential/fixture/fixture.go`).
- DECISIONS tail **ADR-0231** (the thrift_proxy filter — ACCEPTED, §Decision/§Consequences body landed in-place per ADR-0044; next-free **ADR-0232**).
- reference pin: `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227).
- Toolchain: Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); go-control-plane `/envoy` v1.32.4 (ADR-0008) + `/contrib` v1.32.4 (kafka, phase 31).
- conformance h2spec 53/53 + proxy-wasm 10/10.

## Read first (in order)

1. THIS FILE — the post-phase-33 cold-start (the §9 family is closed; next is a new-family brainstorm).
2. `docs/envoy-go/STATE.md` — active-phase `phase 33 (network-filter-thrift-proxy) done`; lifecycle-state `phase 33 IMPL done — phase 33 CLOSED; the §9 Network-filters family CLOSES (0 candidates remain) — SKILL_ROUTING → the next phase is a NEW feature family/row (TBD; superpowers:brainstorming)`; the counts.
3. `docs/envoy-go/ROADMAP.md` — the "Feature Families (09+)" section (§9 Network filters marked **FAMILY CLOSED (2026-06-10)**; the remaining unopened family headings listed above); the Schema/Invariants for row discipline.
4. `docs/envoy-go/DECISIONS.md` — the tail ADR-0231 (thrift_proxy, ACCEPTED) + ADR-0230 (the upstream-pool seam) + ADR-0106 (family flat-row discipline) + ADR-0045 (the split-gate) + ADR-0044 (ADR-body-in-place policy) + ADR-0052 (the atomic-landing six-gate) — re-read whichever the chosen family touches.
5. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the as-built surface (the stat-name mapping count 1116; the §9 thrift_proxy subsection as the most-recent example).
6. The phase-33 record (the most recent worked example to mirror): `docs/envoy-go/phases/33-network-filter-thrift-proxy/{BRAINSTORM,SPEC,PLAN,PROGRESS}.md`.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_execution_style`, `feedback_git_worktrees`, `feedback_subagents_no_push`, `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_detach`, `feedback_subagent_worktree_path_targeting` (subagents in a worktree mis-target paths — pin the canonical PROGRESS.md path + worktree-relative paths + controller-verify the main checkout stays clean), `reference_docker_probe_bridge_network`, `reference_differential_break_protocol_count1`, `reference_differential_asserter_dispatch` — surface family-specific references when the subject is chosen.

## Honor throughout

- `feedback_execution_style` (subagent-driven) · `feedback_git_worktrees` (work in a worktree) · `feedback_subagents_no_push` (subagents commit LOCAL-ONLY; the controller squash-merges + pushes at stage-close) · `feedback_push_to_origin` (after a clean landing on master with tests green, push without asking) · `feedback_pertask_gofmt_lint` (each task runs `gofmt -l` + `golangci-lint run` on touched packages) · `feedback_subagent_worktree_path_targeting` (pin canonical paths; verify the main checkout stays clean).
- ADR-0106 (family flat-row discipline; a brand-new family adds a NEW heading/rows at brainstorm time — do NOT pre-populate) · ADR-0045 (the split-gate, re-checked at each BRAINSTORM/SPEC/PLAN) · ADR-0044 (ADR §Decision/§Consequences bodies land at the IMPL) · ADR-0052 (the atomic-landing six-gate) · ADR-0060 (histograms deferred).
- The §9 Network-filters family is CLOSED — do NOT reopen it; the next phase is a NEW family. The lifecycle is BRAINSTORM (`superpowers:brainstorming`, design-only) → SPEC → PLAN → IMPL, each its own session per the established cadence.
