START a fresh cold-start session for envoy-go — **the phase-34 (`load-balancer-least-request`) IMPL is DONE and LANDED on master; the next lifecycle step is the next-phase routing** (the Load-balancing family stays OPEN — 7 policy candidates remain). The substantive master tip is the phase-34 IMPL squash **`b086137`** (`phase 34 IMPL: load-balancer-least-request — land Cluster.LbPolicy LEAST_REQUEST (un-weighted P2C) over a NEW LB acquire/release seam …`; pushed to `origin/master`; confirm via `git log --oneline -1`). It carries the whole phase: the `internal/cluster` seam reshape + `leastrequest.go` + manager acceptance + the `0059-lb-least-request` fixture + the BEHAVIOR_CONTRACT/ADR-0232/0233/STATE/ROADMAP doc bundle.

The literal live master tip is the docs-only SHA-fill commit **`5815eae`** (which named the prior routing fill `a450950`; both docs-only commits trail the substantive phase-34 IMPL squash `b086137`; confirm via `git log --oneline -4`). The phase-34 IMPL was executed subagent-driven (`superpowers:subagent-driven-development`) — the 9-task TDD PLAN, two-stage spec+quality review per task, per-task gofmt+golangci-lint, `-count=1` deliberate-break liveness, the controller squash-merge + push at stage-close. Below the substantive `b086137` tip sit the phase-34 PLAN squash `e97f7ea` / SPEC `3671167` / BRAINSTORM `87572c0` / the phase-33 IMPL `97aefd6` (the §9 Network-filters family CLOSED).

## What this session does — open the NEXT Load-balancing-family phase (a BRAINSTORM)

The Load-balancing family OPENED at phase 34 and stays OPEN. **7 candidates remain** (the ROADMAP `### Load balancing family` authoritative roster): `random`, `ring_hash`, `maglev`, `subset LB`, `locality-weighted LB`, `priority load balancing`, `panic thresholds`. The next session is a **phase-35 BRAINSTORM** (`superpowers:brainstorming` — DESIGN-ONLY; pick the next subject from the 7 + settle Q0/Q1/Q-seam/Q-split per the established family template), per the BRAINSTORM → SPEC → PLAN → IMPL cadence (each its own session).

**Seam-reuse caveat for the next LB phase (ADR-0232 §Consequences, sharpened at the phase-34 final review):** the RELEASE half of the ADR-0232 seam generalizes to every future policy (each returns its own `release func()`, or the shared `noopRelease` when it holds no per-pick state) — so the non-keyed **RANDOM drops in at zero churn** (same `loadBalancer` interface, ignores the counters). BUT the **PICK-INPUT half is NOT yet general:** `Pick()` takes no arguments, so the hash-keyed **RING_HASH/MAGLEV will additionally require widening `Pick` to receive a request-derived hash key** — a signature extension the seam anticipates but does not provide at the MVP. A next phase that lands a hash policy MUST plan for that `Pick(hashInput)` widening (it is NOT the zero-churn drop-in the active-load-aware policies get). This makes RANDOM the cheapest next opener and RING_HASH/MAGLEV the seam-extending ones — a Q0/Q-split input.

## Counts at master tip (phase 34 done — they advance only at the next IMPL)

- stat surface **1116** (phase 34 was the FIRST zero-delta phase; least_request added NO stat names — the active counters are LB-internal `atomic.Int64`, not registry stats).
- differential fixtures **61** (tail `0059-lb-least-request`; `ls -d test/fixtures/[0-9]* | wc -l`). NO boot-reject dir was added at 34.
- fuzzers **42** (tail `FuzzThriftDecode`; phase 34 was the FIRST no-fuzzer phase — no wire decode).
- BackendKind tail **33** (`TCPThriftResponder`; phase 34 reused `TCPEcho` kind-0, added none).
- DECISIONS tail **ADR-0233** (ADR-0232 [the LB acquire/release seam] + ADR-0233 [the least_request policy], both ACCEPTED — §Decision/§Consequences bodies landed in-place at the IMPL per ADR-0044; next-free **ADR-0234**).
- reference pin: `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227).
- Toolchain: Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); go-control-plane `/envoy` v1.32.4 (ADR-0008; `Cluster.LeastRequestLbConfig` is in the pinned module — ZERO new go.mod dep at 34).
- conformance h2spec 53/53 + proxy-wasm 10/10.

## Read first (in order)

1. THIS FILE — the post-phase-34-IMPL cold-start (next: the next Load-balancing-family BRAINSTORM).
2. `docs/envoy-go/STATE.md` — active-phase `phase 34 (load-balancer-least-request) done`; lifecycle-state `phase 34 IMPL done — SKILL_ROUTING → the next-phase routing (7 LB candidates remain)`; the counts.
3. `docs/envoy-go/ROADMAP.md` — row 34 `done`; the `### Load balancing family` heading (least_request DONE, family OPEN, the 7-candidate roster).
4. `docs/envoy-go/DECISIONS.md` — ADR-0232 (the LB acquire/release seam — READ the §Consequences seam-reuse note: the release half generalizes; the pick-input half needs widening for hash policies) + ADR-0233 (the least_request policy) + ADR-0024/0063/0230/0106/0045/0044/0052.
5. `internal/cluster/{loadbalancer,leastrequest,manager,cluster}.go` — the as-built seam: the reshaped `loadBalancer` interface, `roundRobin`/`leastRequest` impls, the `connWithGauge` release composition, the two-policy manager switch + `parseLeastRequestLbConfig`.
6. `docs/envoy-go/phases/34-load-balancer-least-request/{BRAINSTORM,SPEC,PLAN,PROGRESS}.md` — the phase-34 record (the family-template the next BRAINSTORM mirrors; the D-L empirical-probe methodology against the live contrib reference).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_execution_style`, `feedback_git_worktrees`, `feedback_subagents_no_push`, `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_path_targeting`, `reference_differential_break_protocol_count1`, `reference_docker_probe_bridge_network`.

## Honor throughout

- `feedback_execution_style` (subagent-driven) · `feedback_git_worktrees` (work in a worktree) · `feedback_subagents_no_push` (subagents commit LOCAL-ONLY; the controller squash-merges + pushes at stage-close) · `feedback_push_to_origin` (after a clean landing on master with tests green, push without asking) · `feedback_pertask_gofmt_lint` · `feedback_subagent_worktree_path_targeting`.
- ADR-0106 (a flat family row per LB policy — NO parent rollup unless a row consumes its split escape valve) · ADR-0045 (the split-gate, re-checked at each PLAN) · ADR-0044 (ADR bodies at the IMPL, in-place) · ADR-0052 (the atomic-landing six-gate) · ADR-0227 (the contrib reference image; live probes per `reference_docker_probe_bridge_network`).
- The lifecycle is BRAINSTORM → SPEC → PLAN → IMPL, each its own session per the established cadence. The next session is the next Load-balancing-family **BRAINSTORM**.
