START a fresh cold-start session for envoy-go — **the phase-35 (`load-balancer-random`) SPEC is DONE and LANDED on master; the next lifecycle step is the phase-35 PLAN** (single flat row; NO escape valve; the Load-balancing family stays OPEN — 6 candidates remain after 35). The substantive master tip is the phase-35 SPEC squash **`bdef11a`** (`phase 35 SPEC: load-balancer-random — execute D-R1..D-R6 live (all CONFIRMED); the stateless randomLB reuses the ADR-0232 seam at zero churn`; pushed to `origin/master`; confirm via `git log --oneline -1` — the literal live tip is the docs-only SHA-fill commit just above it that named this SHA). It carries the SPEC.md + README.md + the ROADMAP row-35 STAYS `in-progress` + the STATE advance to `phase 35 SPEC done`. Below it sits the phase-35 BRAINSTORM squash `c5c6c55` (the 12-§ family-template doc), and below that the phase-34 IMPL squash `b086137` (the `leastRequest` policy + the ADR-0232 LB acquire/release seam that phase 35 REUSES).

## What this session does — author the phase-35 PLAN (`superpowers:writing-plans`)

The next session is the **phase-35 PLAN** (`superpowers:writing-plans` — decompose the SPEC §10 ~4–7-task spine into bite-sized TDD tasks; the kafka-31/thrift-33/least_request-34 precedent): write-failing-test → run-fail → minimal-impl → run-pass → per-task `gofmt -l` + `golangci-lint` (`feedback_pertask_gofmt_lint`) → LOCAL-ONLY commit (`feedback_subagents_no_push`). Resolve the SPEC §12 **D-S35-1..4** PLAN/IMPL questions; run the **FINAL ADR-0045 split-gate re-check** (anticipated NO SPLIT with an order-of-magnitude margin — ~45–60 prod LoC / ~4–7 tasks; NO escape valve, there is no seam build to split off). The plan-document-reviewer gate applies. Counts stay UNCHANGED at the PLAN (the DECISIONS tail STAYS ADR-0233 — the ADR-0234 §Context is a DRAFT in SPEC §13; the full entry + body land at the IMPL per ADR-0044). Per the BRAINSTORM → SPEC → PLAN → IMPL cadence (each its own session).

## The phase-35 design (settled at the SPEC — D-R1..D-R6 all CONFIRMED live)

- **Subject:** `random` (`Cluster.LbPolicy RANDOM`, enum value **3**, `envoy.config.cluster.v3`). NO `Cluster.RandomLbConfig` message → NO config-parse arm, NO PGV gate (the simplest family surface). ZERO new go.mod dep (`go mod tidy -diff` empty). The `lb_config` oneof has NO `random` member → a stray member under RANDOM is silently ignored (reference parity, D-R2).
- **The `randomLB` policy (ADR-0234):** a STATELESS uniform-pick LB (`rng() % len(endpoints)`, ONE draw, INSENSITIVE to load — the upstream v1.37.2 `RandomLoadBalancer::peekOrChoose` mirror, D-R5) in a NEW `internal/cluster/random.go`. Holds only `endpoints []Endpoint` + an injectable `rng func() uint64` (NO `active` slice, NO `sync.Once` release). REUSES the EXISTING package-private `newPCGRNG()` helper (`leastrequest.go:63`) DIRECTLY — no extraction (D-R5 confirmed shared-ready); `newRandomWithRNG(endpoints, rng)` mirrors `newLeastRequestWithRNG` minus `choiceCount`. Empty-set → `Endpoint{}, noopRelease, errNoEndpoints` (the `roundRobin`/`leastRequest` parity).
- **Seam REUSE (NO seam ADR):** `randomLB` implements the existing unexported `loadBalancer` interface (`Pick() (Endpoint, func(), error)`) returning the shared `noopRelease` — the ADR-0232 seam's FIRST reuse, validating its "the non-keyed RANDOM drops in at zero cost" §Consequences claim. ZERO seam churn, ZERO `cluster.go`/consumer touch, ADR-0024 UNAMENDED.
- **Manager acceptance:** ONE new `case clusterv3.Cluster_RANDOM` (bare construction, NO `parseRandomLbConfig`); the reject TEXT at `manager.go:252` extends `(supported: ROUND_ROBIN, LEAST_REQUEST)` → `(…, RANDOM)` — the ONE deliberate byte-stable-reject change; blast radius THREE sites (`manager.go:252` + `manager_test.go:320` `TestManager_Error_UnsupportedLBPolicy` [the DOUBLY-hit retarget: its trigger `Cluster_RANDOM` becomes accepted → retarget to RING_HASH/MAGLEV + the substring `"ROUND_ROBIN, LEAST_REQUEST"` → `"…, RANDOM"` at line 327] + `BEHAVIOR_CONTRACT.md:899`). NO fixture pins the text → NO boot-reject dir.
- **Differential `0060-lb-random` (anti-skew):** cross-side `[tcp_proxy]` both sides over a 3-endpoint RANDOM cluster; the 3 `TCPEcho` backends (kind 0) REUSED. The REUSED `0059` hold-4 + burst-60 stimulus with the assertion INVERTED — the SECOND band-based `AssertDistribution` asserting RANDOM STAYS uniform PER SIDE (conservation `sum == 64` + per-host floor `>= 12` + per-host ceiling `<= 32`) DESPITE the held-conn load (the contrapositive of least_request) + a cross-side `StatsAsserter` prong (cx_total/membership/quiesced-active cross-equal; `upstream_rq_total` per-side). Three deliberate-break liveness proofs with `-count=1` (consult-the-counters [floor leg — the canonical anti-skew break] / pin-to-endpoints[0] [floor+ceiling] / dropped-Inc [stats prong]). NO new fuzzer (fuzzers STAY 42), NO new BackendKind (tail STAYS 33), ZERO stat delta (1116).

## The §12 D-S35-1..4 PLAN/IMPL questions (PLAN resolves)

- **D-S35-1** — file placement (anticipated: the `randomLB` type in a NEW `random.go`; the manager case + reject text in `manager.go`; the retarget in `manager_test.go` — the `leastrequest.go` precedent).
- **D-S35-2** — `newRandom` reuses `newPCGRNG` verbatim (seed-error message reads `"cluster: least_request: seed rng"`) OR wraps it for a random-flavored message (anticipated: call directly + accept the shared message, OR a thin one-line wrapper; `newPCGRNG` itself needs NO change).
- **D-S35-3** — the final band constants (floor 12 / ceiling 32 / sum 64 anticipated) + the tuning protocol (N≥20 flake-free runs per side + the three `-count=1` deliberate-break proofs).
- **D-S35-4** — the anti-skew integration test shape (anticipated: a small deterministic-RNG unit test proving RANDOM does NOT avoid a loaded endpoint, distinct from the fixture).

## Counts at master tip (phase 35 SPEC done — they advance only at the phase-35 IMPL)

- stat surface **1116** (anticipated UNCHANGED at phase 35 — the SECOND zero-delta phase; D-R4 confirmed empty name-set diff).
- differential fixtures **61** (tail `0059-lb-least-request`; `ls -d test/fixtures/[0-9]* | wc -l`). Anticipated 61 → 62 at the IMPL (`0060-lb-random`). NO boot-reject dir.
- fuzzers **42** (tail `FuzzThriftDecode`; anticipated UNCHANGED — no wire decode).
- BackendKind tail **33** (`TCPThriftResponder`; anticipated UNCHANGED — `0060` reuses `TCPEcho` kind-0).
- DECISIONS tail **ADR-0233** (ADR-0232 [the LB seam] + ADR-0233 [least_request], both ACCEPTED; next-free **ADR-0234** — the ADR-0234 §Context is a DRAFT in SPEC §13; the full entry + §Decision/§Consequences body land at the phase-35 IMPL per ADR-0044).
- reference pin: `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227).
- Toolchain: Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); go-control-plane `/envoy` v1.32.4 (ADR-0008; `Cluster.LbPolicy RANDOM` is in the pinned module — ZERO new go.mod dep at 35).
- conformance h2spec 53/53 + proxy-wasm 10/10.

## Read first (in order)

1. THIS FILE — the post-phase-35-SPEC cold-start (next: the phase-35 PLAN).
2. `docs/envoy-go/STATE.md` — active-phase `phase 35 (load-balancer-random) SPEC done`; lifecycle-state `phase 35 SPEC done — SKILL_ROUTING → the phase-35 PLAN`; the counts.
3. `docs/envoy-go/phases/35-load-balancer-random/SPEC.md` — the authoritative phase SPEC (AMEND-R1..R6; the policy + seam-reuse design §3; the reject matrix §6; the `0060` anti-skew band-arm §8; the ~4–7-task spine §10; the D-R1..D-R6 empirical-pin block §11; the §12 D-S35-1..4 questions; the ADR-0234 §Context DRAFT §13). AUTHORITATIVE for the PLAN.
4. `docs/envoy-go/phases/35-load-balancer-random/BRAINSTORM.md` — the charter (Q0/Q1/Q-seam/Q-split; the enum-3 / no-config-message verification; the seam-reuse rationale).
5. `docs/envoy-go/DECISIONS.md` — **ADR-0232** (the LB seam — REUSED unchanged; read the §Consequences "the non-keyed RANDOM drops in at zero cost") + ADR-0233 (the least_request policy — the RNG posture random reuses) + ADR-0024 (the per-cluster LB-state scope — UNAMENDED) + ADR-0045/0044/0052/0080/0106/0060.
6. `internal/cluster/{loadbalancer,leastrequest,manager,cluster}.go` — the reused seam (`Pick() (Endpoint, func(), error)` + `noopRelease`), the `leastRequest` RNG helper (`newPCGRNG`, `newLeastRequestWithRNG`) random reuses, the lb_policy switch (`manager.go:234`) + the reject text (`manager.go:252`); `internal/cluster/manager_test.go:320` (`TestManager_Error_UnsupportedLBPolicy` — the doubly-hit retarget).
7. `test/fixtures/0059-lb-least-request/` — the held-conn skew driver `0060` transposes (the assertion INVERTED) + the band-`AssertDistribution` + `StatsAsserter` pattern.
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_execution_style`, `feedback_git_worktrees`, `feedback_subagents_no_push`, `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_path_targeting`, `reference_differential_break_protocol_count1`, `reference_differential_asserter_dispatch`, `reference_differential_run_selector`, `reference_docker_probe_bridge_network`.

## Honor throughout

- `feedback_execution_style` (subagent-driven) · `feedback_git_worktrees` (work in a worktree) · `feedback_subagents_no_push` (subagents commit LOCAL-ONLY; the controller squash-merges + pushes at stage-close) · `feedback_push_to_origin` (after a clean landing on master with tests green, push without asking) · `feedback_pertask_gofmt_lint` · `feedback_subagent_worktree_path_targeting`.
- ADR-0106 (a flat family row per LB policy — NO parent rollup) · ADR-0045 (the split-gate, re-checked at each PLAN) · ADR-0044 (ADR bodies at the IMPL, in-place) · ADR-0052 (the atomic-landing six-gate) · ADR-0080 (byte-stable parse-reject wording) · ADR-0227 (the contrib reference image; live probes per `reference_docker_probe_bridge_network`).
- The lifecycle is BRAINSTORM → SPEC → PLAN → IMPL, each its own session per the established cadence. The next session is the phase-35 **PLAN**.
