START a fresh cold-start session for envoy-go — **phase 28 is DONE (all three sub-phases + the parent row rolled) + squash-merged + pushed** (the squash SHA is recorded in `docs/envoy-go/STATE.md` last-commit). No phase is in flight. Per SKILL_ROUTING **state 0** (no active phase → `superpowers:brainstorming` for the next phase).

Phase 28 (`network-filter-zookeeper-proxy`) DELIVERED: the `network.WriteFilter` seam (ADR-0221) + complete both-direction zookeeper_proxy counter parity (ADR-0222 request side + ADR-0223 response side + latency-threshold counters) across 3 sub-phases — 28.1a (write seam + request package; squash `8703aeb`), 28.1b (read seam + cross-side proof; squash `fdf40ea`), 28.2 (response decoder + mutex + latency + rollup; squash `fde21c9`). All 3 ADR bodies landed. Six gates GREEN LIVE at 28.2 close: 50/50 differential, h2spec 53/53, proxy-wasm 10/10.

## What this session must do

Per SKILL_ROUTING **state 0**: invoke `superpowers:brainstorming` to select and scope the next phase. The 4 remaining §9 Network-filters-family candidates are:

- **mongo_proxy** — natural next (consumer #2 of the `network.WriteFilter` conn-wrap seam, per ADR-0221 §Consequences anticipation; both-direction decode + counter parity analogous to zookeeper_proxy)
- **redis_proxy** — the Redis protocol filter
- **kafka_broker** — the Kafka protocol filter
- **thrift_proxy** — the Thrift protocol filter

mongo_proxy is the natural next but the choice is the brainstorm's. Other ROADMAP families (Load balancing, Upstream robustness, HTTP/3+QUIC, gRPC, xDS, Observability) are also valid brainstorm targets.

## Project state at master tip

- **Master tip:** `3adc6a7` (the STATE.md SHA-fill docs commit; actual HEAD at repoint time, trails the substantive 28.2-IMPL squash `fde21c9` +1). This repoint itself advances the tip +1 (expected, not chased); confirm the live tip via `git log --oneline -1`.
- **Substantive predecessors:** the 28.2-IMPL squash `fde21c9`, the 28.1b-IMPL squash `fdf40ea`, the 28.1a-IMPL squash `8703aeb`.
- **§9 Network-filters family:** rows 26 (parent + sub-rows) + 27 + 28 (parent + 28.1a + 28.1b + 28.2) ALL DONE. 4 candidates remain: redis/mongo/kafka_broker/thrift.
- **Toolchain:** Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); reference Envoy v1.37.2 (ADR-0008); go-control-plane v1.32.4 (ADR-0008).

## Counts at session open

- **Fixtures:** 50 (tail `0048-zookeeper-responses`)
- **Fuzzers:** 38
- **Stat surface:** 337
- **DECISIONS.md tail:** ADR-0223 (next-free ADR-0224)
- **Conformance:** h2spec 53/53; proxy-wasm 10/10 (as of the 28.2 six-gate)

## Known coverage boundaries (carried, non-blocking)

- D-26.3-4 (network rbac coverage boundary)
- `tcp_proxy` `downstream_cx_*` family unmirrored (pre-existing gap)
- Histograms deferred (ADR-0060) — incl. the `*_response_latency` family from zookeeper_proxy
- Zookeeper dynamic-metadata deferred (AMEND-A9) + shallow-decode leniency (extends to the response side per AMEND-A8) + terminal-originated-writes-only + write-only-filter boot boundary
- Post-handoff observational boundaries (28.1b SPEC §3.5)

## Read first (in order)

1. THIS FILE — the next-phase cold-start prompt.
2. `docs/envoy-go/STATE.md` — active-phase `phase 28 DONE`; next-skill `superpowers:brainstorming`; project counts.
3. `docs/envoy-go/ROADMAP.md` §9 Network-filters family — the 4 remaining candidates + rows 26/27/28 as done-family context.
4. The phase-28 family of SPECs/PROGRESS for as-built zookeeper/seam surface: `docs/envoy-go/phases/28-network-filter-zookeeper-proxy/BRAINSTORM.md`, `docs/envoy-go/phases/28.2-network-filter-zookeeper-responses-and-latency/SPEC.md` + `PROGRESS.md` (esp. the D-S28.2-1 corrected watch-event minimum + the conn-wrap seam surface for mongo_proxy).
5. `docs/envoy-go/DECISIONS.md` ADR-0221 (WriteFilter seam + ADR-0213 allowance consumed; mongo_proxy = anticipated consumer #2) + ADR-0222 (zookeeper_proxy filter + request side) + ADR-0223 (response side + latency-threshold counters + ADR-0221 forward-pointer discharged).
6. `docs/envoy-go/SKILL_ROUTING.md` (state 0) + BOOTSTRAP §4/§5 cold-start invariants.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — execution-style + worktree + push + subagents-no-push + per-task gofmt/lint + the network-filter constraints + the differential dispatch/asserter/break-protocol (`-count=1`) constraints all carry forward.
