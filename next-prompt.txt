=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **PHASE 88 (`h2-continuation-frames`) IS OPEN — ROW 88 IS `in-progress` AT `ROADMAP.md:150`.** `want` IS **120**. ⇒ **THIS SESSION PERFORMS THE PHASE-88 IMPL — THE PHASE-CLOSING STAGE.** `STATE.md`'s `next-skill:` reads *the phase-88 IMPL* — that IS your instruction. ⚠️ **THE SENTINEL DOES NOT FIRE AT YOUR START** — check (1) prints exactly `NOT DONE: row 88` and check (2) prints SIX. **DO NOT create `stop` at the start.** ⚠️ **AT YOUR CLOSE, ROW 88 FLIPS `done` AND CHECK (1) GOES SILENT — RE-RUN THE WHOLE SENTINEL AFTER THE FLIP AND OBEY WHAT IT ACTUALLY PRINTS.** If all three checks are silent, the sentinel FIRES and you create `stop`. **Measure it; do not assume either outcome.**

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=120 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **`want` IS 120 AT THIS TIP AND THE IMPL DOES NOT MOVE IT.** The PLAN minted **NO** sub-phase rows (D-88-SEQ is ONE ATOMIC COMMIT), so `88.1`/`88.2`/`88.3` are PROSE only. **Your `ROADMAP.md` diff is exactly TWO edits: the row-88 status cell, and the `:204` gRPC-family sentence.** Both are sentinel-affecting — **MEASURE BOTH SIDES, NEVER FORECAST.**

⚠️ **THE DOCTORING NCs REMAIN MANDATORY EVERY SESSION.** Both:
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md. BEFORE your flip expect BOTH
#   "NOT DONE: row 62" AND "NOT DONE: row 88"; AFTER the flip expect "NOT DONE: row 62" ALONE.
```
```sh
# the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```

⚠️ **AND WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -oiE '-family row'` fails with `grep: amily row: No such file or directory` and the surrounding arithmetic prints `base=0 now=0 delta=0`, **which reads exactly like "no change".** Observed live at FOUR phase-84 stages **and AGAIN at the phase-88 PLAN** (`ugrep: invalid option - **prior active-phase...`). **Always pass `--` before the pattern**, and remember the counts are form-dependent: **95 occurrences vs 67 LINES** at this tip (`grep -c` counts LINES). **A gate that reads zero on BOTH sides is not evidence of invariance.** ⚠️ AND the carried **ARM-A {119, 131} figure binds ONLY with its escape-aware pipe-split command** (BRAINSTORM-86 §6) — ad-hoc field-count forms do NOT reproduce it; do not "correct" the figure with a different form.

**SENTINEL RE-RUN MECHANICALLY AT THE PHASE-88 PLAN CLOSE (2026-08-14, base `fe023fb5`), ONE SIDE — a PLAN edits no ROADMAP, and the BINDING proof of that is the EMPTY `ROADMAP.md` DIFF. IT DOES NOT FIRE; `stop` was NOT created** (verified absent at repo root AND stage worktree). ACTUAL output, recorded not predicted: input **238 lines / 120 data rows** · (1) **`NOT DONE: row 88`** ALONE, denominator silent · (2) **SIX at `:198 :204 :210 :220 :226 :234`** · (3) **SILENT**.
- ⚠️ do NOT "fix" the six and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases; the candidates sentences are a WINDOW onto a larger deferred backlog, not an inventory of it.
- **ALL FOUR NCs FIRED:** row-62 doctoring ⇒ `NOT DONE: row 62` AND `NOT DONE: row 88` (with `NC LANDED? [ in-progress ]` inspected first); `want=119` ⇒ `GATE FAIL: examined 120 data rows, expected 119`; check-(3) doctoring (residual confirmed **2 -> 0** on the doctored copy first) ⇒ `NEVER OPENED: gRPC` while `WASM` correctly stayed silent; check-(2) one-arm counts **5** (long arm alone) / **1** (short arm alone), union **6** (a one-arm strip is NOT an NC for the union).
- Every leak axis INVARIANT: `-family row` **95 / 67** · `gRPC-family row` **2** · `Operational-tooling-family row` **3**.
- ⚠️ **Re-run all three checks and both doctoring NCs yourself, BOTH SIDES of your ROADMAP edits, and record what YOU see.**

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (ugrep under the hood). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Reproduced at TWENTY-FIVE separate tips.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **THE METHOD BINDS; carry no standing discrepancy figure and no standing absolute.** ⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS; print FILE NAMES, never a count.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files; use `-v --no-index`, and `git status` is the discriminating check). ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **`grep -c` ON ZERO MATCHES IS TOOL-SPECIFIC** (only `git grep -c` prints nothing; all exit 1 — capture the value, never chain on the exit code). ⚠️ **`grep -c` COUNTS *LINES*, NOT *OCCURRENCES*.** ⚠️ **`\t` IN GNU ERE IS A LITERAL `t`** — use `-P`. ⚠️ **A `git grep` PATHSPEC ENDING IN `/` READS A FAIL-UNSAFE ZERO** (shape 33) — use the shell-glob form. ⚠️ **AN UNANCHORED PANIC-GREP FALSE-FIRES 14 TIMES ON A FULLY-GREEN DIFFERENTIAL LOG** — **the canonical gate is ANCHORED: `^panic:|DATA RACE|SIGSEGV`. A gate that fires on a green log is worse than no gate.**

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`** with FOUR blind spots (an em-dash, a DOT in `http-1.1`, two LETTER-suffixed ids). ⚠️ **EXECUTED on scratch copies at the phase-77 PLAN: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** ⚠️ **DO NOT restore the regex form.**

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN, AND WHY YOUR ROW TOUCHES IT

⚠️ **Run the union form, never one half of it.** ⚠️ **A ONE-ARM STRIP IS NOT AN NC** (6 -> 5 short / 6 -> 1 long; never 6 -> 0). ⚠️ **The documented trap's stated MECHANISM is refuted while the trap is real** — the inflation comes from ROADMAP table rows carrying BRAINSTORM prose. **Cite the command; do not re-inherit the explanation.** ⚠️ **The line numbers drift with edits — re-derive them; the current six are `:198 :204 :210 :220 :226 :234`.** ⚠️ **These six paragraphs are the FINAL and now the ONLY sentinel blocker, and any edit near them is a sentinel-affecting edit.** All six are standalone family PROSE paragraphs (not rows), naming ~42 distinct candidates; every "retire the paragraphs" shape is doctrinally foreclosed (`reference_sentinel_matcher_string_self_clears`).

⚠️ **LOAD-BEARING FOR YOUR STAGE: THE `:204` SENTENCE IS ONE OF YOUR TWO ROADMAP EDITS.** The gRPC-family paragraph at `:204` names **ELEVEN** candidates, one of which is *"the CONTINUATION-discard defect"* (MEASURED at the PLAN: gRPC streaming, `grpc_http1_bridge`, `grpc_web`, gRPC-JSON transcoding, gRPC-JSON reverse transcoding, `grpc_field_extraction`, `grpc_stats`, `connect_grpc_bridge`, the `test/conformance/grpc/` interop client, the CONTINUATION-discard defect, the dead `RunEncodeTrailers` hook). Removing it leaves **TEN**, so the phrase `remaining deferred (not-yet-chartered) candidates:` SURVIVES and check (2) **should** stay SIX. ⚠️ **THAT IS A PREDICTION AND MUST BE MEASURED ON BOTH SIDES — never forecast.** Row 88's own line carries the matcher **0** times (measured), so the status flip alone cannot move check (2).

⚠️ **AND THE PLAN ADDS FOUR NEW DEFERRED CANDIDATES that the IMPL must record somewhere rather than drop** (PLAN §10): stream-scoped header-overflow parity via a decode-and-discard drain · `max_request_headers_kb` config parity + the `http2.header_overflow` stat · `SETTINGS_MAX_HEADER_LIST_SIZE` advertisement · decode-side filter mutations never reaching the H2 upstream request (`h2dispatch.go:457` — a **pre-existing framework property, NOT a phase-88 defect**). ⚠️ **Adding candidate text near a window is a sentinel-affecting edit — measure it.**


==============================================


## STATUS AS OF 2026-08-14: PHASE 88 (`h2-continuation-frames`) **OPEN** — row 88 `in-progress` at `ROADMAP.md:150`, `want` **120**, base `fe023fb5`. **THIS SESSION PERFORMS THE PHASE-88 IMPL AND CLOSES THE PHASE.**

**What the 88 PLAN landed (2026-08-14, base `fe023fb5`, branch `phase-88-plan`, ONE squashed commit).** Docs-only: ZERO production `.go`, ZERO test `.go`. `PLAN.md` + the `PROGRESS.md` PLAN entry + the STATE roll. `ROADMAP.md`, `DECISIONS.md` and `BEHAVIOR_CONTRACT.md` **ALL BYTE-UNTOUCHED** (strict `PROPOSED` guard **STAYS ARMED at 1** — YOUR stage disarms it by completing ADR-0310).

⚠️ **METHOD NOTE: SUBAGENT-DRIVEN RETURNED at the PLAN** (four probe agents, disjoint worktrees, ports 47490-47529, none committing) after a FIVE-STAGE inline departure. **The controller re-derived every load-bearing agent claim by execution — and that is how a wrong reference-image cite was caught inside the branch.** Do the same with anything below.

### ⚠️ THE HEADLINE: THE PLAN REFUTED ITS OWN SPEC ON Q3 — THE CLIENT LEG IS THE WORST LEG, NOT THE SAFE ONE

SPEC §6 concluded the client (upstream) leg was *"silent-loss-only, masked by CONNECTION LIFECYCLE"*, resting on three distinct upstream source ports. **MEASURED at the PLAN tip the upstream connection is POOLED AND REUSED — `127.0.0.1:34754` for all three requests — so the masking does not exist and the damage is CUMULATIVE:**
```
[K2-many1]    status=200 nXMany=216            <- 84 of 300 headers LOST, silently
[K2-many2]    ROUNDTRIP-ERR after=8.004s context deadline exceeded   <- HANGS
[K2-control3] status=502 ; backend read GOAWAY LastStreamID=6 ErrCode=COMPRESSION_ERROR
```
**On the upstream leg the defect is a HANG, then a 502, then envoy-go tearing down a POOLED upstream connection that unrelated concurrent traffic is riding.**

### ⚠️ FOUR SPEC ARM PARAMETERS ARE VACUOUS — THE CONTROL VARIABLE IS THE HPACK-**ENCODED** BLOCK SIZE

`G@20000B` wrote `HEADERS len=16042` (ONE frame — all headers arrive) · `I@?big=20000` did not split at high entropy · `K@?many=200` wrote `len=15175` and delivered 200/200 twice. Re-run at parameters MEASURED to split (`?many=300`, 40000 B). ⚠️ **AND ENTROPY SILENTLY MOVES THE SPLIT POINT** — a compressible `'B'x20000` pad DID split where a high-entropy one did not. **PIN THE SPLIT BY ASSERTING WIRE FRAMES, NEVER A BYTE COUNT.**

⚠️ **THE LOSS IS PARTIAL, NOT TOTAL — THE SPEC'S INTENDED FIXTURE ASSERTION WAS A DECOY.** Two independent seams agree (h2c `NHDR 4 -> 1`, 216/300; `0004` TLS-h2 at 32000 B shows the small sentinel **ARRIVING** with `padlen=0` in BOTH directions). Fields encoded BEFORE the split survive. ⇒ **ASSERT THE LENGTH OF THE LARGE FIELD.** Never sentinel presence; never *which* headers survive (x/net encoder ordering, not a contract).

### ⚠️ THE THIRD LEG IS TWO-SIDED; ONLY THE SERVER SIDE IS INDEPENDENTLY ANCHORABLE

**88.3s (server/response write) IS red-anchored at the unmodified tip:** `direct_response` + 40 `header_mutation` response mutations ⇒ `RAW-FRAME: HEADERS len=29408 flags=0x04 END_HEADERS`, exceeding the peer's advertised 16384 by 13024 bytes; a conforming x/net peer gets `http2: frame too large`; a 5915-byte control is clean. **88.3c (client/request write) is NOT:** `h2dispatch.go:457` calls `SetH2Request` BEFORE the decode chain, so filter mutations never reach the H2 upstream request, and a large downstream block is capped on read. Corroborated on the upstream wire (`http2debug=2`: backend read `HEADERS len=35023` / `len=32022` against a peer advertising 16384). ⇒ **D-88-SEQ FROZEN: ONE ATOMIC IMPL COMMIT, NO sub-phase rows.**

### ⚠️ THE "DEFENCE IN DEPTH" INSTRUCTION IS REFUTED — THE 16 MiB BOUND IS REAL COVERAGE

nil-accumulator and wrong-stream **ARE** unreachable (isolation PROVEN: with `AllowIllegalReads` the byte-identical inputs both deliver cleanly, so `checkFrameOrder` is the sole gate) — label those two defence-in-depth. **But the over-bound arm is REACHABLE:** `frames=1025 accumulated=16793600 bound=16777216 firstErr=<nil>`, and end-to-end **the tip absorbed a 19.6 MB flood with no error and still proxied the request**. ⚠️ **Nothing upstream binds: envoy-go does NOT inherit x/net's CVE-2023-45288 mitigation** (it lives in `readMetaFrame`, never enabled) and `SETTINGS_MAX_HEADER_LIST_SIZE` is never advertised. ⚠️ **The exposure runs the OTHER WAY: the TIP is safe because discarding retains nothing — YOUR accumulator INTRODUCES the retention, and the bound is the mitigation for a hazard the fix itself creates.** It gets a REAL break arm.

### THE FROZEN DECISIONS — DO NOT RE-LITIGATE, DO RE-VERIFY

- **D-88-PRIM:** the per-connection accumulator (decided at the SPEC by two compiling prototypes; `Framer.ReadMetaHeaders` refuted on four grounds — ⚠️ it **compiles cleanly and then HANGS**).
- **D-88-SEQ:** ONE ATOMIC COMMIT, TDD held INSIDE. NO sub-phase rows ⇒ `want` stays 120.
- **D-88-CODE:** ⚠️ **BOTH SPEC CANDIDATES REFUTED BY REFERENCE MEASUREMENT (5x, `contrib-v1.37.2`):** the reference emits **`RST_STREAM INTERNAL_ERROR(2)`, STREAM-scoped, with NO GOAWAY at all** (`http2.too_many_headers`, stat `http2.header_overflow`, 60 KiB `max_request_headers_kb`). Neither `FRAME_SIZE_ERROR` nor `ENHANCE_YOUR_CALM`. **The real divergence is SCOPE.** RULED: **connection-scoped `ErrEnhanceYourCalm` at 16 MiB**, because under the byte-accumulator a stream-scoped reject leaves the HPACK table permanently desynced — ⚠️ **recorded as a COST TRADEOFF, NOT an impossibility** (a decode-and-discard drain would achieve parity; filed as a deferred candidate). **NAME BOTH divergences (scope AND value) in the contract.**
- **D-88-STAT:** **no new stat**, stat-surface delta **0**.
- **D-88-LASTID:** **PINNED.** `LastStreamID` **1 -> 0** CONFIRMED by execution; origin confirmed as x/net (`framer: connection-error code=1` on BOTH sides — `translateFramerErr`'s `ConnectionError` branch, `framer.go:56`). ⚠️ **Pin it together with the behavioural improvement the SPEC did not claim: the TIP still proxies the truncated request and returns 200 AFTER emitting the GOAWAY; the fix does not.** Use `t.Errorf` per property.
- **D-88-DIFF:** **EXTEND `0004-h2-routing`**, MEASURED at **`37 0` backend + `78 0` driver = 115 insertions, 0 deletions, 2 files, 0 new files**, +0 on every count axis. ⚠️ **Option 2 REFUTED BY EXECUTION** — only `0012`/`0015`/`0005` read request headers and `grep -ln http2` over all three returns **nothing**. Plus ~9 MODIFIED doc-count lines (29 -> 31); ⚠️ **the "27" at `README.md:34` is CORRECT for the pre-phase-87 prefix — do NOT "fix" it.**

### ⚠️ WHAT THIS SESSION OWES — THE PHASE-88 IMPL (see `PLAN.md` §3 for the ordered tasks)

1. **T0 unit RED census** — the **11-row** `writeHeaderBlock` roster (**130 lines MEASURED**, NOT the SPEC's 4 rows) + the accumulator arms, run against the UNFIXED tree with failure lines READ. ⚠️ **A hardcode-16384 break arm reddens ONLY `peer_max_larger`/`peer_max_max_legal` — EVERY SPEC-proposed row stays GREEN.**
2. **T1 differential RED census** on the extended `0004`. ⚠️ **ONE RUN PER ARM** — the driver is FAIL-FAST, so the request arm masks the response arm and every later arm is an unproven claim that READS as proven because the run did fail (`reference_failfast_driver_masks_later_red_arms`).
3. **T2/T3 production** — read legs (`conn.go` + `client.go` accumulator), then the write leg (`framer.go` `writeHeaderBlock` + BOTH call sites). ⚠️ **T0 and T1 are BOTH prerequisites — landing production first converts both censuses into unfalsifiable green-on-arrival claims.**
4. **T4 GREEN both layers · T5 docs · T6 gates LAST.**
5. **Docs:** a **NEW** `BEHAVIOR_CONTRACT.md ## HTTP/2` bullet (⚠️ **there is NO sentence to ride**; every whole-file `CONTINUATION` hit is the unrelated TRACING sense — do not conflate, do not edit; **CITE BY STRING OR SYMBOL**) NAMING BOTH divergences · **ADR-0310 §Decision + §Consequences appended IN PLACE after the RETAINED italic footer, no renumber, NO `---` separator** (`^---$` stays 216) ⇒ ⚠️ **this is the ONE non-append edit `DECISIONS.md` takes, so the proof is `numstat N 1` and the `cmp` PREFIX WILL NOT HOLD — verify instead that the ONLY deleted line is the STATUS line** · kill the landed FALSE `dispatchFrame` comment at `conn.go:264-268`.
6. **ROW 88 -> `done`** at `ROADMAP.md:150` (status cell ALONE; summary prose sha256-verified BYTE-IDENTICAL over the status-blanked line — the row-86/87 precedent) + the `:204` narrowing. **`want` stays 120.** Strict `PROPOSED` guard **1 -> 0**.
7. The sentinel + all four NCs **ON BOTH SIDES of your ROADMAP edits** · `STATE.md` rolled IN PLACE (lifecycle-state **3 -> DONE**) with the oldest §Recent entry evicted by DIRECT DATE READ · `next-prompt.txt` rolled (`git add -f`) · ONE squashed commit, merged and pushed.

### ⚠️ COST — MEASURED AT THE PLAN, AND STILL A FLOOR

**Production net ~+189** (read legs `client.go` +140/−88 and `conn.go` +82/−12 ⇒ **+122**; write leg `client.go` +5/−3, `conn.go` +6/−3, `framer.go` +62/−0 ⇒ **+67**), IMPL band **~+190-240**. ⚠️ **Of the read leg's 222 insertions, 94 (42%) are PURE CODE MOTION** (86 lines byte-identical after de-indent, 8 mechanical substitutions) **and ~95 are comments — genuinely new executable code is ~70-80 lines.** Test band **~+400-580** (differential **115** MEASURED, roster **130** MEASURED, accumulator/arm-E/flood units 150-250 NOT measured). **Quoted as a FLOOR** (`reference_measured_prototype_is_a_lower_bound`) — three decode sites, two write sites, per-connection state on two structs.

⚠️ **THREE UN-ENUMERATED CLASSES THE PLAN DISCOVERED AND COULD NOT PRICE:** the **PRIORITY flag on a split HEADERS** (carried on the accumulator, **ENTIRELY UNPROBED**) · **trailers arriving split** (`HEADERS`+`CONTINUATION` after DATA) · **a split block on a concurrently-reset stream.** ⚠️ **`hasPriority` is the one most likely to bite — a HEADERS with both the PRIORITY flag and a CONTINUATION was never sent by any probe in this phase.**

### ⚠️ THREE FINDINGS THE IMPL MUST NOT RE-LEARN

1. ⚠️ **h2spec is NOT a red anchor and NOT a regression gate here.** `95 tests, 94 passed, 1 skipped, 0 failed` **identically at tip and prototype**; §6.10 CONTINUATION passes **6/6 at the tip that discards every CONTINUATION frame**, because those tests assert the connection's *reaction*, never that the fields arrived. **Cite only from YOUR own run.**
2. ⚠️ **THE BINARY-ASSERTION TRAP HAS A FALSE-POSITIVE FORM.** `strings <tip-binary> | grep -c 'writeHeaderBlock'` reads **9 on the UNMODIFIED tip** (Go's bundled `net/http.(*http2writeResHeaders).writeHeaderBlock`) — the naive assertion reads as *already present*. **Use the QUALIFIED name: `hcm/h2\.(\*framer)\.writeHeaderBlock` ⇒ 0 tip / 2 fixed.** And **ASSERT THE BOUND BINARY WITH `ss -ltnp` BEFORE EVERY READING** — a stale server made a prototype read identical to the tip at the SPEC.
3. ⚠️ **THE ZERO-`MaxFrameSize` POLICY IS ALREADY LANDED TWICE — REUSE IT, DO NOT MINT ONE.** `conn.go:778-783` and `client.go:854-858` both carry `if maxFrame <= 0 { maxFrame = 16384 }` with the RFC 9113 §6.5.2 cite. The case is genuinely reachable (`driveHandshake`, `settings_validate_test.go:38`, sends `fr.WriteSettings()` with NO arguments, so that whole suite already runs at 0), and with the guard removed the codec emits **unbounded zero-length frames**.

### Candidates ledger (re-derive EVERY cost at your tip — a carried cost is stale by construction)

**`ssl.connection_error`** (+444 whole-`.go` floor) · **`test/conformance/grpc/`** (9/26 reachable) · **the stat-surface recount** (contested **1205 vs 1207**; occurrence form reads `NewCounter(` **327** / `NewGauge(` **79** = **406**; **no absolute corrected, none carried**) · **REVIEW.md restoration** (**37** of **129** phase dirs; process-not-product) · **the D-86-CONN `client.Close` gate** (~10 lines) · **hygiene fold-ins** · the **~42 candidates in the six family windows** · **the PLAN's four new candidates** (see the section-(2) note).

### ⚠️ COUNTS AT THIS TIP — RE-DERIVE MECHANICALLY, NEVER COPY

- **`ROADMAP.md` 238 lines / 120 data rows**; row 88 `in-progress` at `:150`, all others `done`; check-(2) anchors `:198 :204 :210 :220 :226 :234`; `-family row` **95 occurrences / 67 LINES**; `gRPC-family row` **2**; `Operational-tooling-family row` **3**; ARM-A flags **{119, 131}** (escape-aware command ONLY).
- **`DECISIONS.md` 18166** lines · **309** headings · tail **ADR-0310 (§Context ONLY, STATUS `PROPOSED`)** · next-free **ADR-0311** (TAIL-derived; `grep -c '^## ADR-0311'` => **0**; headings+1 COLLIDES at the ADR-0209 gap) · **strict `^> \*\*STATUS: PROPOSED` guard 1 — ARMED; YOUR stage disarms it** · STATUS census **23** · `^---$` **216**. ⚠️ **A NEW ADR TAKES NO `---` SEPARATOR.** ⚠️ **CARRY NO WHOLE-FILE COUNT OF THE LOOSE `PROPOSED` MATCHER.**
- **`BEHAVIOR_CONTRACT.md` 5958** — `## HTTP/2` at `:2019`, section ends at `## HTTP filter chain` `:2081`. ⚠️ Row 87's `:2036` insertion shifted EVERY by-line citation at or below it by +1 — **cite by STRING or SYMBOL.**
- **`STATE.md` 64** · **`STATE_HISTORY.md` 496** · **`BOOTSTRAP_PROMPT.md` 522** (repo ROOT) · phase dirs **129** · `REVIEW.md` **37** (standing departure, named not claimed).
- **differential fixtures 121** at **`test/fixtures/`** (⚠️ **NOT `test/differential/fixtures/`, a plausible wrong path that returns a SILENT `0`**); numeric tail `0119-grpc-unary-trailers`, next-free `0120` **STAYS UNCONSUMED** (D-88-DIFF extends `0004`). · **fuzzers 55 / 48 files** — anticipated **+0**. · **BackendKind tail 38** (`H2GoawayResponder`, `fixture.go:614`) over 39 declarations (do NOT "fix"). · **fixture ports are family-banded `10<index>`, NEVER max+1.**
- ⚠️ **STILL CONTESTED, NO NUMBER CARRIED:** the `STATE_HISTORY.md` archive-gap total · the loose `PROPOSED` matcher's whole-file count · the stat-surface absolute (1205 vs 1207) · the zero-test `generic/*` suite count (13 vs 14).

### ⚠️ DOCUMENTARY DEFECTS — carried forward, still deliberately NOT fixed

**`ADR-0051` §2's false gate-scope sentence** corrected on the record by ADR-0307 · **`ADR-0058`** cites `routerActionH2.doH2` at a dead location · **`ROADMAP.md` rows at lines 119/131 malformed** (ARM-A GUARD) · **`STATE.md` §Project counts FROZEN at phase 76** · **`harness_test.go:208` port inventory STALE** · **`body.go` nolint INERT** · **xDS cycle guard NOT AUTOMATED** · **`wasm/doc.go:219` two errors** · **ROADMAP cites `esalaine` FIVE times** · **`rbac.go:50` token `F2`** · **root `PROGRESS.md`** stray phase-32.1 doc · **SPEC-86/PLAN-86 prose cites the grpcclient Close pins at a nonexistent `internal/xds/xdsgrpc/...` path** · **`BEHAVIOR_CONTRACT.md:2034`'s "no stdlib net/http parsing" sentence is FALSE for the 05.1 -> 87 window** (RETAINED, ridden by ADR-0309) · **`ADR-0057` says fixture `0004`'s driver issues "27 sequential H/2 round-trips per side" — it now issues 29, and YOUR row takes it to 31** · **`BEHAVIOR_CONTRACT.md`'s TWO riders cite the ADR-0052 mandate at a DRIFTED `:1821`** · **the gRPC-family window cites the CONTINUATION arm at `conn.go:255-259` — it is `:264-268` at this tip** · ⚠️ **the `dispatchFrame` comment is a LANDED FALSE COMMENT — YOUR STAGE IS WHERE IT DIES.**

### ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + BOTH DOCTORING NCs YOURSELF, BOTH SIDES** of your ROADMAP edits, and record ACTUAL output.
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** The 88 PLAN refuted its SPEC's Q3 answer, four of its arm parameters, its fixture assertion, its guard-arm labelling, and both of its error-code candidates. Do the same.
3. **LINE ANCHORS DRIFT; SYMBOL ANCHORS DO NOT — RE-VERIFY EVERY CITE AT YOUR TIP.**
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Still live: the **SDS dial-budget class** (`TestSDSEndToEnd_FetchFailure_BootFailsClosed/silent_SDS_server…`, `internal/boot/boot_sds_e2e_test.go:551`, gRPC `DeadlineExceeded`, THREE firings) · the **driver-owned receiver port race** with TWO named members, `0081-grpc-access-log` (`0.0.0.0:35097`) and `0087-tracing-otlp` (`0.0.0.0:35323`) — **it ABORTS the binary mid-suite and the harness wrapper still exits 0** · `internal/cluster` `-race` outlier · `internal/httpclient` zero-value · the two 84.2-era flakes · `TestServerConn_TinyWindowDelivery` · the REFERENCE h2spec section-8 flip. **Capture `-v` output BEFORE re-running anything.**
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only). ⚠️ **The PLAN counted FOUR STABLE pre-existing containers (`infallible_booth`, `crazy_kare`, `golink-ai`, `quizzical_goldstine`) PLUS transient `envoy:v1.33.0` containers churning under another owner's activity.** Leave all of them alone.
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`.
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL** (⚠️ **QUALIFIED**, see finding 2 above), **and ASSERT THE BOUND BINARY with `ss -ltnp` before reading any probe result.** **A harness's exit code is not the command's**; **`gofmt -l` never exits non-zero — gate on OUTPUT.**
8. ⚠️ **`pgrep -f` / `pkill -f` MATCHES YOUR OWN SHELL AND KILLS THE TOOL CALL (exit 144)**, and ⚠️ **a `pgrep -f 'a\|b'` ALTERNATION MATCHES NOTHING SILENTLY** — kill by PID from `pgrep -laf` with the `bash -c` line filtered out, ONE pattern at a time, and verify ports released with `ss -ltn`.
9. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-88-*` or stage worktree should outlive its stage close** — expect only `master` plus your own (verify with `git worktree list`).
10. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** (a)/(b) differential 121 fixtures, full suite with `INNER_EXIT`; (c) h2spec cite only from your own run; grpc-conformance deferred in writing; proxy-wasm 10/16; (d) fuzzers 55/48; (e) `INNER_EXIT` on every differential launch AND `go test ./...`; (f) no REVIEW.md — standing departure.
11. ⚠️ **THE ARCHIVE-ABSENCE GUARD'S WHOLE-FILE FORM IS FAIL-UNSAFE AND WAS REPLACED AT THE PLAN.** The evicted label can ALREADY appear in a previous bullet's PROSE (the phase-88 SPEC's bullet names its tie partner), so `grep -cF` read **1** pre-append where the guard expects 0 — **which reads as "already archived, skip the append" and would LOSE the entry.** Use the **DISCRIMINATING own-label form**: extract each eviction bullet's OWN backticked label and match that. NC it with a label that IS present. ⚠️ **Whole-file controls moved 15 -> 16 and 13 -> 14 at this close — the +1 is SELF-INCLUSION, not drift.** ⚠️ **And census `STATE.md`'s lineage bullets LINE-ANCHORED: an unanchored form read 6 where 5 exist, because the file's own prose quotes the matcher.**

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 88 (h2-continuation-frames)' --format='%h %s'      # 5 at this tip: BRAINSTORM, follow-up, SPEC, ROUTER RE-VERIFY, PLAN
git log --grep '^phase 87 (h2-double-slash-path-routing)' --format='%h %s' # 4: a CLOSED phase, use as a FIRING CONTROL
```
⚠️ **The count comments are LIVE FIGURES — VERIFY before relying.** Take the NEWEST line (`| head -1`).

Execution style: **subagent-driven per `feedback_execution_style`**, and **the PLAN returned to it after five inline stages** (four probe agents, disjoint worktrees, ports 47490-47529, none committing, every load-bearing claim re-derived by the controller). **The IMPL is where the parallel work genuinely is** — the 87 IMPL's precedent is four task agents plus an adversarial whole-branch reviewer **which found three real coverage gaps the controller had not**. Subagents commit LOCALLY on the ONE stage branch with EXPLICIT PATHSPECS; the controller squashes (`feedback_subagents_no_push`, `feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** and tear down docker **BY NAME**. ⚠️ **Band private ports outside BOTH `20000-31007` AND `11000-14999`, clear of the static fixture ports (10000-19172); the driver-owned receiver race has been observed at `35097` and `35323`, and `0081` also binds `0.0.0.0:42039`.** The 87 stages used 47410-47441, the 88 BRAINSTORM 47450-47470, the SPEC 47480-47485 and the PLAN **47490-47529**; band yours from **47530+**. ⚠️ **ONE WORKTREE PER AGENT preferred; explicit-path staging if shared; never an unscoped `git restore`.** Per-task `gofmt` + `golangci-lint` on touched packages (⚠️ misspell locale US). ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch.** ⚠️ **Docker BY NAME, `--rm`, verify gone.**

⚠️ **CONTAINER→HOST REACHABILITY — READ THE CORRECTION, NOT THE HEADLINE.** `172.17.0.1` is the BRIDGE IPAM GATEWAY and is documented NOT to work here. Use `--add-host=host.docker.internal:host-gateway` and dial the HOSTNAME, or resolve the literal via a throwaway `getent hosts host.docker.internal` container (the `differential.HostGatewayIP` / `0092`-driver-local pattern); and put **`dns_lookup_family: V4_ONLY`** on any reference cluster dialing `host.docker.internal` (the embedded DNS also returns a non-forwarding AAAA that `AUTO` prefers — a SILENT connect-fail that reads as "the backend emits nothing"). **A host-bound backend IS reachable; verify delivery ran (receiver count > 0) before concluding anything.** ⚠️ **AND envoy-go REJECTS `match.headers` outright** — `route 0: match.headers is not supported in phase 04` — so a header-match route works on the REFERENCE side only. ⚠️ **AND `clusters: []` is BOOT-REJECTED even when every route is a `direct_response`** (`cluster manager: zero clusters in bootstrap`) — a dummy cluster is required.

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root — **observed repeatedly at the 88 BRAINSTORM, SPEC and PLAN**. Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run. ⚠️ **Shell VARIABLES do not survive between Bash calls** — re-export `$W`/`$SC` in every command.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — `git status --porcelain` AND `git diff --stat master` must show ONLY your intended edits. For a patched TRACKED file: `sha256sum` BEFORE patching, `git checkout -- <path>` after, `sha256sum -c` the capture.

⚠️ **THE ARCHIVE-ABSENCE GUARD — SEE STANDING METHOD NOTE 11; THE WHOLE-FILE FORM IS FAIL-UNSAFE.** **Verify STRICTLY APPEND-ONLY**: `git diff --numstat` = `N 0` AND base a byte-exact PREFIX (`cmp`). At your close §Recent evicts the oldest per a DIRECT DATE READ at that tip. ✅ **CONFIRMED BY DIRECT READ AT `fe023fb5` + this stage's roll:** §Recent is capped at **5** bullets (`STATE.md:46 48 50 52 54`); after this roll the oldest is **`phase 87 (h2-double-slash-path-routing) SPEC done` (2026-08-13) at `:54`** — but ⚠️ **the other four are ALSO 2026-08-13 or later, so YOUR close faces a FOUR-WAY TIE at the oldest date; resolve it by LIST POSITION (the LAST entry), the phase-88-SPEC precedent, and confirm by a DIRECT READ of all five dates BEFORE removing anything.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads the phase-88 IMPL — that IS your instruction.** §Project counts SELF-CONTRADICT §Current — do not anchor on them (frozen at phase 76).
2. `docs/envoy-go/phases/88-h2-continuation-frames/PLAN.md` — **the whole file**: §0 the Q3 refutation, §1 the re-proven anchors and the four vacuous arm parameters, §2 the two-sided third leg, §3 **the ordered IMPL tasks**, §4 the frozen decisions, §5 the 11-row roster, §6 the pinned `0004` recipe and its assertion constraints, §7 the docs text, §8 the sentinel, §10 the budget.
3. `docs/envoy-go/phases/88-h2-continuation-frames/SPEC.md` §1/§3 — the two prototypes and the third leg (⚠️ **§5's arm parameters and §10 item 5's guard labelling are REFUTED by the PLAN — read the PLAN's version**).
4. `internal/filter/hcm/h2/conn.go` (`dispatchFrame`, `onHeaders`, `encodeAndWriteHeaders`, and **`writeData` at `:778-783` — the sibling constant to REUSE**), `client.go` (the `HeadersFrame` arm's TWO decode sites, the request-write, `writeData` at `:854-858`), `hpack.go` (`decodeBlock`), `framer.go` (`translateFramerErr`; **`ReadMetaHeaders` is never assigned in `internal/`**).
5. `docs/envoy-go/DECISIONS.md` **ADR-0310** (tail, §Context ¶1-¶4, `PROPOSED`) — the four ¶s your IMPL completes; and ADR-0309 §Consequences (iii)/(vii) on the cost-floor argument and the fail-fast one-run-per-arm rule.

## What the IMPL owes
The seven items in **"WHAT THIS SESSION OWES"** above · **row 88 -> `done`** and the `:204` narrowing, `want` staying **120** · the strict `PROPOSED` guard **1 -> 0** by completing ADR-0310 · the sentinel + all four NCs **BOTH SIDES**, recorded · `STATE.md` rolled IN PLACE (lifecycle-state **3 -> DONE**) with the oldest §Recent entry evicted by DIRECT DATE READ (⚠️ **four-way tie — resolve by list position**) · `next-prompt.txt` rolled (`git add -f`) · ONE squashed commit, merged to master, pushed. ⚠️ **THEN RE-RUN THE SENTINEL: with row 88 `done`, check (1) goes SILENT and only check (2)'s SIX blocks. OBEY WHAT IT PRINTS — if all three are silent, CREATE `stop`.**
