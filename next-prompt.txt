START a fresh cold-start session for envoy-go — **phase 32.1 (`network-filter-redis-upstream-pool-seam-and-codec`) PLAN is DONE; the next lifecycle step is the 32.1 IMPL** (execute the 16-task PLAN, subagent-driven per `feedback_execution_style`). The 32.1 PLAN (`docs/envoy-go/phases/32.1-network-filter-redis-upstream-pool-seam-and-codec/PLAN.md`) decomposed the SPEC §14 spine into 16 bite-sized TDD tasks (write-failing-test → run-fail → minimal-impl → run-pass → gofmt/lint → commit) and re-checked the ADR-0045 split-gate at PLAN time (16 ≤ ~25 tasks / ~940-1420 ≤ ~1500 prod LoC → **NO split; 32.1 proceeds as ONE sub-phase**; the 32.1a/32.1b escape-valve stays UNCONSUMED). The 32.1 IMPL runs `superpowers:subagent-driven-development` (a fresh subagent per task + two-stage review; work in a git worktree per `feedback_git_worktrees`); subagents commit LOCAL-ONLY (`feedback_subagents_no_push`); the controller squash-merges + pushes at stage-close (`feedback_push_to_origin`); per-task `gofmt -l` + `golangci-lint run` on touched packages (`feedback_pertask_gofmt_lint`); deliberate-break liveness checks use `go test -count=1` (`reference_differential_break_protocol_count1`). The 32.1 IMPL fills the **ADR-0230 §Decision/§Consequences body** (the upstream-pool seam) + the **ADR-0229 §Decision/§Consequences (32.1 half)** in place per ADR-0044.

The phase-32.1 PLAN landed at the phase-32.1 PLAN squash `<PLAN_SQUASH_SHA>` on master (squash-merged from `phase-32.1-network-filter-redis-upstream-pool-seam-and-codec-plan` + pushed to origin/master by the controller; the NEW `docs/envoy-go/phases/32.1-network-filter-redis-upstream-pool-seam-and-codec/PLAN.md` + the ROADMAP sub-row-32.1 PLAN-DONE note + STATE/next-prompt advance; plan-document-reviewer APPROVED). It is the substantive master tip; this docs-only SHA-fill follow-up trails it — confirm via `git log --oneline -1`. Substantive predecessor on master: the phase-32.1 SPEC squash `b7f01a4`; earlier the phase-32 PARENT SPEC squash `0f7888d`; earlier the phase-32 BRAINSTORM squash `f473c4e`; earlier the phase-31 IMPL squash `928287a` (phase 31 CLOSED). origin/master in sync after push.

## What this session does — the phase-32.1 IMPL

Execute the 16-task PLAN task-by-task (subagent-driven). The task spine:

- **Task 1 = the first-action baselines/anchors gate** (re-pin against the live IMPL-session tip): fixtures **56** (tail `0054`) + fuzzers **40** + stat surface **536** + BackendKind tail **31** + DECISIONS tail **ADR-0230** (next-free **ADR-0231**) + the `proto.MessageName(&redis_proxyv3.RedisProxy{})` TypeURL pin + a clean `go mod tidy` (ZERO new go.mod module — redis_proxy is CORE `/envoy v1.32.4`, AMEND-R1/R8/R9) + the §11.1 as-built anchors; create `PROGRESS.md`.
- Then the spine: the `redisproxy` package skeleton + TypeURL (Task 2); `config.go` parse (the PGV-required `stat_prefix`/`settings`/`settings.op_timeout`/`catch_all_route.cluster` arms + the runtime no-upstream reject + the unknown-catch_all-cluster TOLERANCE + the `IsValidName` guard + the byte-stable `redis_proxy:` reject constants) (Task 3); `TestParseRejectConstants_ByteStable` (Task 4); the **RESP codec** `resp.go` — request decode (Task 5) + reply decode + encode (Task 6); the **upstream-pool seam** `internal/filter/network/upstreampool.go` — `UpstreamDialFunc` + `UpstreamConn` + lazy dial + single-flight `Send`/`Reader`/`Close` (NO `internal/cluster` import in `internal/filter/network`; D-P32-3 + the ADR-0230 §Context) (Task 7); `commands.go` PING/AUTH local-reply + dispatch (Task 8); `stats.go` the 10-name eager roster + `TestStatRoster32_1_MatchesUpstream` (Task 9); `filter.go` the `NewFactory` + `TerminalFilter.Handle` pump (Task 10); the 10th built-in + the `redis_proxy/v3` blank-import + boot smoke (Task 11); the `TCPRedisResponder` BackendKind (value 32) (Task 12); `0055-redis-roundtrip` driver — bootstraps + RESP builders + the PING arm (Task 13) + the proxied SET/GET round-trip + the flat-`/stats` StatsAsserter + the R6 break-liveness proof (Task 14); `0056-redis-boot-reject` (the `stat_prefix` arm) (Task 15); the completion bundle — ADR-0230 §Decision/§Consequences body + the ADR-0229 32.1 half + the BEHAVIOR_CONTRACT 32.1 bundle (stats 536→546) + STATE/ROADMAP + next-prompt + the six-gate (Task 16).
- The 32.1 IMPL advances counts: fixtures **56→58**, fuzzers **40** (unchanged — `FuzzRESPDecode` is 32.2), stat surface **536→546** (+10 fixed: 6 downstream counters + 4 gauges), BackendKind tail **31→32** (`TCPRedisResponder`), DECISIONS tail STAYS **ADR-0230** (the body fills in place; no new number). ROADMAP sub-row 32.1 `in-progress → done`; parent row 32 STAYS `in-progress` (the ROLLUP is 32.2's).

The 32.1 IMPL runs NO new docker probes (the parent §11 D32-1..D32-8 block + the 32.1 SPEC are the authoritative empirical records). It MAY re-read the as-built source corpus.

## Honor throughout

- `feedback_execution_style` (subagent-driven IMPL) · `feedback_git_worktrees` · `feedback_subagents_no_push` (subagents commit local-only) · `feedback_push_to_origin` (the controller squash-merges + pushes at stage-close) · `feedback_pertask_gofmt_lint` · `feedback_subagent_worktree_detach` (GIT HYGIENE re-verify-branch).
- ADR-0230 (the upstream-pool seam — §Decision/§Consequences body lands THIS IMPL) · ADR-0229 (the redis_proxy filter umbrella — the 32.1 half lands THIS IMPL) · ADR-0044 (§Context-at-SPEC, §Decision+§Consequences-at-IMPL) · ADR-0079 (two-step factory) · ADR-0080 (byte-stable rejects) · ADR-0052 (atomic landing + six-gate) · ADR-0045 (split-gate) · ADR-0106(d) · ADR-0219 (the routing precedent) · ADR-0223 (the per-conn mutex — DEFERRED at 32.1) · ADR-0060 (histograms deferred) · ADR-0227 (the contrib pin).
- Project memories: `reference_network_filter_typeurl_extensions`, `reference_wire_format_both_sides_see_same_bytes` (EXTENDS to the downstream RESPONSE bytes redis_proxy generates), `reference_close_direction_framework_gap` (does NOT bite — AMEND-R9), `reference_dynamic_stat_name_charset_guard` (the `IsValidName(stat_prefix)` guard is the only 32.1 use), `reference_differential_asserter_dispatch`, `reference_differential_fixture_dispatch_constraint`, `reference_differential_break_protocol_count1`, `reference_differential_reference_parses_full_message`.

## Read first (in order)

1. THIS FILE — the phase-32.1-IMPL cold-start.
2. `docs/envoy-go/phases/32.1-network-filter-redis-upstream-pool-seam-and-codec/PLAN.md` — **THE 16-TASK PLAN** (execute it task-by-task; the File Structure section, the PLAN-time D-question dispositions, the ADR-0045 FINAL re-check, the per-task TDD steps with complete code + expected command output).
3. `docs/envoy-go/phases/32.1-network-filter-redis-upstream-pool-seam-and-codec/SPEC.md` — **THIS SUB-PHASE SPEC** (the package §3, the codec §3.5 [D-P32-4], the seam §4 [D-P32-3 + the ADR-0230 §Context blueprint], the stats §7, the fixtures §8, the ADR anchor map §10, the as-built anchors §11.1, the D-questions §12, the task spine §14, the test surface §16).
4. `docs/envoy-go/phases/32-network-filter-redis-proxy/SPEC.md` — **THE PARENT SPEC** (the inherited §5 proto / §6 reject / §7 stat / §8 fixture rosters + the §11 D32-1..D32-8 empirical-pin block [AMEND-R1..R9]).
5. `docs/envoy-go/STATE.md` — active-phase `phase 32.1 PLAN done`; lifecycle-state → the 32.1 IMPL; next-skill `superpowers:subagent-driven-development`; counts (536 stats / 56 fixtures / 40 fuzzers / BackendKind tail 31 / DECISIONS tail ADR-0230, next-free ADR-0231).
6. `docs/envoy-go/ROADMAP.md` — parent row 32 + sub-row 32.1 (`in-progress`, SPEC-DONE + PLAN-DONE notes) + sub-row 32.2 (`planned`).
7. `docs/envoy-go/DECISIONS.md` — **ADR-0230** (the §Context anchored at the 32.1 SPEC — the body lands THIS IMPL) + ADR-0229 (the filter umbrella — the 32.1 half lands THIS IMPL) + ADR-0044 / ADR-0079 / ADR-0080 / ADR-0052 / ADR-0045 / ADR-0219 / ADR-0223 / ADR-0060 / ADR-0227.
8. `docs/envoy-go/phases/29.1-network-filter-mongo-wire-and-requests/PLAN.md` + `PROGRESS.md` — the per-sub-phase IMPL lifecycle precedent (the Task-1 baselines gate; the completion-bundle task; the PROGRESS log discipline).
9. `internal/filter/network/terminal.go` (the `TerminalFilter.Handle` contract) + `upstreamcluster.go` (the ADR-0219 decoupling discipline the seam preserves) + `internal/filter/tcpproxy/filter.go` (the terminal-dial + cluster-resolve-and-REJECT contrast for the unknown-cluster tolerance) + `internal/cluster/manager.go` + `cluster.go` (`Manager.Get` + `Cluster.Dial` + `IncUpstreamRqTotal`) + `internal/filter/network/kafkabroker/` + `mongoproxy/` (the §9 package-shape precedents) + `internal/filter/network/builtins/builtins.go` + `internal/bootstrap/bootstrap.go` (the 10th-built-in + blank-import sites) + `test/differential/fixture/fixture.go` (the `StatsAsserter` + the BackendKind roster, next-free 32) + `internal/stats/registry.go` (`NewCounterIfAbsent`/`NewGaugeIfAbsent`/`IsValidName`).
10. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`.

## Counts at master tip (phase 32.1 PLAN — UNCHANGED until the 32.1 IMPL)

- stat surface **536** (→ 546 at the 32.1 IMPL: +10 fixed); differential fixtures **56** (tail `0054-kafka-boot-reject`; → 58 at the 32.1 IMPL); fuzzers **40** (`FuzzKafkaDecode`; `FuzzRESPDecode` is 32.2); BackendKind tail **31** (`TCPKafkaResponder`; → 32 `TCPRedisResponder` at the 32.1 IMPL); DECISIONS.md tail **ADR-0230** (§Context anchored at the 32.1 SPEC; the §Decision/§Consequences body fills in place at the 32.1 IMPL; next-free **ADR-0231**).
- conformance h2spec 53/53 + proxy-wasm 10/10 (image-independent).
- reference pin: `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227).
- Toolchain: Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); go-control-plane `/envoy` v1.32.4 (ADR-0008) + `/contrib` v1.32.4 (the phase-31 dep). redis_proxy is CORE `/envoy` (D32-1 CONFIRMED) → NO new dep.
- §9 Network-filters family: rows 26/27/28/29/30/31 DONE + **row 32 `redis_proxy` IN-PROGRESS (parent SPEC + 32.1 SPEC + 32.1 PLAN done; 32.1 sub-row `in-progress`, 32.2 `planned`)**. After phase 32 the candidate {thrift} remains (the last §9 candidate — reuses the 32.1 upstream-pool seam).
