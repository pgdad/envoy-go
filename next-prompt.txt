=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

**THERE IS BANKED MID-LIFECYCLE WORK AT THIS TIP.** Phase 78 (`boot-panic-visibility`) is **OPEN** — row 78 is `in-progress`, `BRAINSTORM.md` is landed, `DECISIONS.md` and `BEHAVIOR_CONTRACT.md` are BYTE-UNTOUCHED. **This session performs the phase-78 SPEC.** Do NOT open a new subject.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=110 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **`want=110` IS A LIVE FIGURE, NOT A CONSTANT.** It is the ROADMAP data-row count, **bumped 109 -> 110 at the phase-78 BRAINSTORM in the same commit that added row 78.** **A row-adding phase MUST bump it in the same commit that adds the row** — otherwise every later session gets `GATE FAIL: examined 111 data rows, expected 110`. That noise is deliberate: a wrong denominator must be loud, because a silent one is how check (1) failed in the first place. **The phase-78 SPEC adds NO row, so `want` STAYS 110.**

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. When you open a new family, add its row-summary slug here.

**SENTINEL RE-RUN MECHANICALLY 2026-07-27 AT THE PHASE-78 BRAINSTORM, BOTH AT SESSION OPEN AND AFTER THE `ROADMAP.md` EDIT LANDED. It does NOT fire. `stop` was NOT created and MUST NOT be.** ⚠️ **Re-run it yourself anyway and record what YOU see** — a prior run's result is not evidence for the current tip. ACTUAL output, recorded rather than predicted:
- **(1) `NOT DONE: row 78`** — **RE-OPENED, which is correct and intended.** ⚠️ **The `want` bump was negative-controlled BEFORE the edit and again AFTER it**: `want=109` over the 110-row file prints `GATE FAIL: examined 110 data rows, expected 109`, and a scratch copy with row 78 flipped to `done` is SILENT. So the denominator is load-bearing and the matcher is not always-firing.
- **(2) FIVE, UNCHANGED.** ⚠️ **The SITES SHIFTED +1 — now `:188`, `:198`, `:208`, `:214` (long form) and `:222` (the Operational-tooling short form)** — because row 78 was inserted above them. **The COUNT held because the phase-78 deferral EXTENDED an existing sentence rather than minting a new one.** Do NOT "fix" the count.
- **(3) `NEVER OPENED: gRPC` and `NEVER OPENED: WASM`** — unchanged. (The `WASM` line remains a ROADMAP **bookkeeping artifact** — `internal/wasm/` is a substantially-built wazero host and rows 25/25.1/25.2/25.3 are all `done` — deliberately LEFT AS-IS. A zero-code relabel to clear it was re-examined at this BRAINSTORM and REJECTED as the MENTION-not-USE failure the sentinel cannot detect.)

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`.** Every document from phase 75 forward recorded its blind spot — **109 data rows / 105 matched / FOUR misses** at an **em-dash** in an "after" cell, a **DOT** in the slug `http-1.1`, and two **LETTER-suffixed** ids `28.1a`/`28.1b` — and every one of them called it *"currently HARMLESS, since all four missed rows are `done`."*

⚠️ **Nobody fired the unsafe arm until the phase-77 PLAN. EXECUTED on scratch copies: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** A false "done" ends the project early. ⚠️ **DO NOT restore the regex form.**

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

The old check (2) matched ONLY the long phrase and could not see the **Operational-tooling** family's three genuinely un-chartered items, recorded in the **SHORT** form. Fixed at the phase-77 BRAINSTORM, at the MATCHER; negative controls all EXECUTED.

⚠️ **Normalising that Operational-tooling paragraph to the long form is a NAMED, DELIBERATELY-UNTAKEN follow-up.**

⚠️ **THE DOCUMENTED TRAP'S STATED MECHANISM IS REFUTED, WHILE THE TRAP IS REAL.** `reference_sentinel_deferred_sentence_live_vs_historical` says a naive `grep -c 'candidates:'` inflates *because of* the `candidates were:` recaps. It does inflate — but **not for that reason**: `candidates were:` does not contain that substring **at all**. The inflation comes from **ROADMAP table rows carrying BRAINSTORM prose**. **Cite the command; do not re-inherit the explanation.**

⚠️ **AND THE MEASURED RECORD: CHECK (2) HAS NEVER GONE DOWN** across ~19 phases (history `0 -> 1 -> 3 -> 4 -> 5`). **Phase 74 landed three `ssl.*` stat names and made its OWN sentence LONGER**; **phase 73 predicted a clause REMOVAL and got a clause REPLACEMENT**; **phase 77 opened a family and narrowed nothing**; **phase 78 ADDED an item to an existing sentence.** => **the candidates sentence is a WINDOW onto a larger deferred backlog, not an inventory of it.** ⚠️ **A stage that predicts a decrease here is repeating the phase-73 error. State what your row narrows — usually nothing — rather than forecasting.**

⚠️ **NEVER WRITE A SENTINEL'S OWN MATCHER STRING INTO A FILE THE SENTINEL GREPS.** The sentinel greps **`docs/envoy-go/ROADMAP.md`** — writing these strings *here* is safe; writing them *there* is not. This fired LIVE at the phase-76 BRAINSTORM, twice in one commit. **`grep` cannot tell a mention from a use.** ⚠️ **The phase-78 SPEC should leave `ROADMAP.md` BYTE-UNTOUCHED**; if it does edit it, re-run ALL THREE checks afterwards and verify mechanically that no matcher string leaked — at this BRAINSTORM that verification was done by confirming check (2) still returned exactly its five sites and check (3) still reported gRPC/WASM, either of which a leaked `-family row` token would have silenced.

⚠️ **THE SELECTION CALCULUS, CORRECTED AND STILL STANDING.** *"A candidate that clears a check-(3) blocker is worth **strictly more**"* does **not** hold: opening a family **MINTS** a check-(2) sentence, so phase 77's own arithmetic was **NET ZERO**. What survives: **opening a family is the ONLY way that family's work becomes enumerable at all — a prerequisite, not a shortcut.** Do not restore the stronger claim.

==============================================


## STATUS AS OF 2026-07-27: PHASE 78 IS **OPEN**. ROW 78 IS `in-progress`. THIS SESSION PERFORMS THE **PHASE-78 SPEC**.

**What the phase-78 BRAINSTORM landed** (`git log --grep 'phase 78'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): row 78 chartered `in-progress`, `docs/envoy-go/phases/78-boot-panic-visibility/BRAINSTORM.md`, the sentinel `want` bumped 109 -> 110, and a measured 30-name audit banked as a NAMED ROADMAP deferral. **Docs-only: ZERO production `.go`, ZERO test `.go`.** Four investigation agents on disjoint remits + controller re-derivation of every load-bearing claim.

### What matters for the NEXT session, in order

1. **⚠️ RE-RUN ALL THREE SENTINEL CHECKS YOURSELF AND RECORD THE ACTUAL OUTPUT; DO NOT PREDICT IT.** Create `stop` if and ONLY if all three print nothing. **Check (1) will print `NOT DONE: row 78` — that is CORRECT and the row stays `in-progress` until the IMPL.** `want` STAYS 110 (the SPEC adds no row).

2. **THE ROW — BOOT-PANIC VISIBILITY.** `cmd/envoy-go/main.go:298-303` registers a shutdown `defer` that blocks on `<-flusherDone`, a channel closed ONLY at `:368` (the flusher goroutine) or `:370` (the no-flusher branch). **Any panic in that ~70-line window unwinds into a defer that waits forever => the process HANGS with ZERO BYTES OF OUTPUT.** Reproduced by the controller in three arms:
   - duplicate `stat_prefix`, **normal boot** => `EXIT=124` (timed out), **0 bytes**;
   - **the SAME config** under **`--mode validate`** => `EXIT=2`, panic **PRINTED**: `panic: stats: duplicate metric registration: "http.SAME.downstream_rq_total"`, trace through `internal/stats/registry.go:107`;
   - **negative control** (second prefix renamed) => boots cleanly, `envoy-go ready`.
   **Arm 1 vs arm 3 is the decisive pair** — same config, same panic, printed on one entry path and swallowed on the other — which isolates the cause to the defer.
   ⚠️ **The window is NOT stat-specific**: it spans `boot.Construct`/`admin.New`/`lm.Start`/`bs.Stats.Freeze`, and `registry.go` carries **FIVE** `panic(` sites (`:107` duplicate, `:117` invalid name, `:129` frozen, `:165`/`:212` the `IfAbsent` type-mismatch pair). The fix is a **defer RELOCATION** (~8 lines moved) plus a black-box guard in `cmd/envoy-go/main_test.go`.

3. **⚠️ THE SPEC'S JOB — SETTLE THE D-BPV-* DOCKET BY EXECUTION.** The BRAINSTORM section 7 carries it. **`D-BPV-ORDER` is the one way this fix could do HARM and must be settled by a RUN, not by reading the comment that describes the OLD order:** relocating the defer changes its LIFO position relative to `lm.Stop()` and `tracingProvider.CloseAll()` (`main.go:305`), so the SPEC owes the post-move shutdown order and a proof that the sink contract *"no Submit after Close"* still holds. Also owed: **D-BPV-WINDOW** (derive the window's true span; ⚠️ the *"3 certainly + 1 conditionally boot-reachable"* classification of the five panic sites is **INHERITED and UNVERIFIED**), **D-BPV-OTHER-DEFERS** (is this the only defer in `main()` that can block indefinitely? a partial fix wearing a full fix's commit message is the risk), **D-BPV-LOGFATAL** (`log.Fatalf` calls `os.Exit`, which **SKIPS defers** — confirm, because it bounds what the guard can catch), **D-BPV-GUARD-TRIGGER** (the duplicate-`stat_prefix` trigger goes **VACUOUS** if a later row makes duplicate registration non-fatal).

4. **⚠️ THE GUARD'S FAILING BASELINE IS ALREADY EXECUTED — do not re-argue it, but DO re-run it at your tip.** Today the pre-fix state is `EXIT=124` with 0 bytes, so a green-by-default guard is impossible here. ⚠️ **The assertion must be on BOTH the exit status AND the output**: exit-status alone is satisfied by any failure, output alone by a process that prints and then hangs. **The defect is the conjunction.** And the failure message must distinguish "hung" from "exited wrong" — a bare timeout failure reads identically to a slow machine.

5. **⚠️ WHAT THE ROW DOES NOT DELIVER, DELIBERATELY: whether duplicate `stat_prefix` should be REJECTED.** The reference **boots green** on that config (Envoy's stat scope is get-or-create, not register-once), so a reject is a **deliberate divergence** owed its own ADR — and `BootRejectFixture`'s contract (**BOTH** sides exit non-zero) is the **WRONG HARNESS SHAPE** for it. **Do not fold it in**; that would smuggle a parity decision into a visibility fix.

6. **THE BANKED SUCCESSOR — `/stats/prometheus` PROJECTION COMPLETENESS, 11-14 tasks, and it is MEASURED, not estimated.** THIRTY live registered names across SIX families are silently dropped by `internal/stats.ExtractTags` (an allow-list `switch`) + `internal/stats.WriteProm` (a bare `return` under a *"should not occur"* comment that **neither logs nor errors** — ⚠️ **the doc comment claiming it logs is FALSE, and no logging facility is even in scope in that package**). ⚠️ **TWENTY-EIGHT of the thirty PRE-DATE phase 77 by up to eleven months** — `access_logs.` x2 (44.1/45.1), `tracing.` x2 (46.1b/46.2), `sds.<secret>.` x20 (60.1), `runtime.` x2 (77) — **so the router's inherited *"runtime prometheus fix"* framing is REFUTED; phase 77 was the SIXTH family to hit the gap and the FIRST to notice.** Audited by two agreeing methods (293 registration sites; 4145 distinct names over 107 bootable fixtures, 271 end-to-end confirmations, ZERO mispredictions in either direction). Measured facts it inherits: **`sds.` needs a LABEL-HOISTING arm (`envoy_xds_resource_name`) — measured ONCE, the SPEC of that row MUST re-pin it**; the other three are byte-mirrors of the SN5 `server.` arm - a trial edit applied cleanly (71 packages green) and made fixture `0118`'s red-on-fix pin **FIRE exactly as designed** - **statssink blast radius is ZERO and provably so** (all four sink consumers already fall back to the full dotted name) - stat-surface delta **+0** — a **PROJECTION delta, NOT a registration delta** - the reference emits **NO `# HELP` lines at all** (800 lines / 255 `# TYPE` / **0** `# HELP` / 0 blank) so ~30 helpText entries carry **zero parity content** - reference Envoy's formatter is **TOTAL** (it sanitizes — a cluster named `c.echo-weird:name#1` emitted a present-but-mis-split metric, sample count 680 == 680) while envoy-go's is **PARTIAL**. ⚠️ **Its "impossible by construction" endgame (registration-time validation) is BLOCKED BEHIND ROW 78** — that fix panics at boot, which today is a silent hang. **That ordering is forced by evidence, not preference.**

7. **THE OTHER BANKED ALTERNATIVE: `fault.abort.grpc_status`, cost RE-DERIVED to 10-13 (the inherited ~7-9 is REFUTED).** `abortEnabled` is set on **exactly one line** (`internal/filter/http/fault/fault.go:144`) inside the `HttpStatus` arm, so a `grpc_status` variant is *accepted-and-inert* — `abortPercentage` **is** populated and attached to a permanently-false flag. ⚠️ **The ~7-9 figure silently dropped a ~10-13 finding recorded one row earlier, and the omitted task is a SECOND live divergence on already-shipped code**: the reference **transcodes an `http_status` abort into the gRPC shape** when the request carries `content-type: application/grpc` (503 => HTTP **200** + `grpc-status: 14`), measured over 20 arms; envoy-go emits `text/plain` unconditionally, and fixture `0011` is green only because its probes send no `content-type`. It is an **HTTP-filters-family** row and does **NOT** open the gRPC family — ⚠️ but the contrary claim's provenance is **TWO** sites, not the one previously recorded (`ROADMAP.md:48` routing label **and** `BEHAVIOR_CONTRACT.md:2142`).

8. **⚠️ AGENT-REPORTED LEADS THE CONTROLLER DID NOT VERIFY — recorded as leads, NOT as findings.** An H2 `//`-path `ParseRequestURI` defect (~4-6, reportedly the sole outlier across H1/H2/H3) - `host_rewrite_literal` (~5-6, re-sliced away from `prefix_rewrite`, which needs matched-prefix plumbing; both carry a retry double-strip idempotence trap) - the `upstream_cluster` span tag (~6/~10) - `ssl.connection_error` (~11-12) - an undocumented `server_name` silent-ignore with an `APPEND_IF_ABSENT`-vs-`OVERWRITE` divergence masked by an allow-list in `test/helpers/http_diff.go`. **Each is a single-agent claim. Re-derive before adopting any of them.**

9. **⚠️ `reference_deferred_candidate_cost_restale`: RE-DERIVE THE 5-7 TASK COST AT YOUR OWN TIP.** ⚠️ **AGREEMENT ACROSS DOCUMENTS IS NOT EVIDENCE — a claim that has survived several stages is MORE suspect than one written yesterday, not less.** Phase 77 refuted four of its own PLAN's claims; phase 78 refuted the router's framing of its own named candidate.

10. **⚠️ TWO CONTROLLER SELF-CORRECTIONS FROM THIS STAGE, BOTH TRANSFERABLE.**
    - **A hand-invented probe INPUT manufactured a phantom defect.** Feeding `listener.0.0.0.0_10000.downstream_cx_total` through `ExtractTags` showed the address truncated to `0` — which reads exactly like a live SN3 bug. **It is not**: `normalizeAddr` (`internal/listener/manager.go:352`) strips every `.` and `:` before the name is built, and says at `:337-342` that it does so precisely to prevent that truncation. **The probed shape never occurs.** ⚠️ **A probe's INPUT is a claim too** — inventing a plausible-looking name is not observing a registered one.
    - **An EMPTY probe output read as a CONFIRMING result.** The first reference probe returned **0 `# HELP` lines** — the exact claim under test, apparently confirmed. The container had never started (`node: { id: n }`; **YAML 1.1 parses a bare `n` as boolean `false`** and Envoy rejects `id: false`). Caught only because the probe printed the total line count beside the match count. ⚠️ **A zero-match grep and an empty input are indistinguishable unless you MEASURE THE INPUT.** Same species as a sibling agent's finding that a **SANDBOXED** `docker run` + `curl` returned a full, plausible stats body for a container stuck in `Created` with nothing bound — **run docker probes with the sandbox disabled and verify `docker inspect .State.Status` is `running` before believing any output.**

11. **⚠️ A NEW SHARED-RESOURCE HAZARD — PARALLEL AGENTS AND DOCKER.** One agent's cleanup ran `docker ps | grep contrib-v1.37.2 | xargs docker kill` and killed **THREE containers belonging to concurrently-running SIBLING agents** (its own `--rm` ones had already self-terminated). `reference_parallel_subagents_private_scratch` covers scratch **directories**; it does **not** cover docker containers, ports, or any other **machine-global namespace**. **Tear down by your own container NAME, never by an `ancestor`/image filter.**

12. **⚠️ BROKEN-GATE COUNT: THIRTEEN, unchanged at this docs-only stage.** The standing ones: a full-suite recipe **WITHOUT `-v` is VACUOUS** (`go test > full.log` yields a 2-line log, so `grep -c -- '--- PASS'` **and** `grep -c -- '--- FAIL'` both return **0** on a fully green run — a "0 FAIL" reading proves nothing) - a sha256 byte-gate roster built from the tip's `git ls-files` alone is **desynced BY CONSTRUCTION against a DELETED file**; the roster must span BOTH trees (`ls-files` union `ls-tree master`) - `gofmt -l` **NEVER exits non-zero** (gate on OUTPUT; the `&&` form is inert) - `go doc -all <A> <B>` **silently swallows arg2** when arg1 is valid — ONE package per invocation - a **`+0 exported symbols` gate over an EMPTY package goes RED on a CORRECT tree** unless the lister's stdout goes straight to a file - a **RANGE** gate cannot detect anchor drift (use a content/symbol anchor) - a roster's naive `[ -f ] || continue` **exits 0 on a DELETED file** - a **count-only** stat guard **PASSES a build with BOTH names wrong**.

13. **⚠️ A MEMORY CORRECTION, VERIFIED AT THIS TIP.** `reference_differential_fixture_three_registration_gates` records a **runner branch** as one of three fixture registration gates. **There is no per-fixture branch** — dispatch is `discoverFixtures` (`test/differential/runner_test.go:187`) plus `fixture.DriverRegistry[fx]` (`:193`). A `switch` branch is needed only for a new **BackendKind** (`:272`). The memory has been corrected.

14. **⚠️ ANCHORS DRIFT.** `ROADMAP.md` is **225 -> 226 (+1)** — row 78 was inserted after row 77, so **every ROADMAP line cite at or below the old :139 shifts by 1**, and the check-(2) sites are now `:188 :198 :208 :214 :222`. `BEHAVIOR_CONTRACT.md` **5750 (+0)** and `DECISIONS.md` **17531 (+0)** — both BYTE-UNTOUCHED. ⚠️ **Re-derive every count and cite at the tip that reads it**, and **never write a token into the file whose occurrences of that token you are counting**.

15. **⚠️ A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX — the EIGHTH consecutive stage at which this held.** Live hazards: the full-suite startup flake (`subject ready: EOF` **and** `bind: address already in use`, both failing **BEFORE any assertion**, the latter as a **PANIC that can abort the whole binary**, firing **MORE readily under `-race`** and as the fixture count grows — now **120**) - `reference_sds_init_fetch_timeout_dial_budget_flake` - the PRE-EXISTING `internal/cluster` `-race` outlier flake - two still-**UNINDEXED** load flakes (`internal/httpclient TestOptions_ZeroValue_NoOpDefaults`, `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery`). **Isolate-re-run, then state the classification AND its evidence.** ⚠️ **`0061-lb-ring-hash` is NOT a live flake — a spread failure there is a FINDING.**

16. **Counts at this tip (re-run MECHANICALLY in your worktree; never copy them):** fixtures **120** (`ls -d test/fixtures/[0-9]*/ | wc -l`; ⚠️ the faithful `discoverFixtures` predicate is `^[0-9]{4}[a-z]?-`; next-free **0119**) / fuzzers **55** (a seed is not a `func Fuzz`) / **stat surface 1207**, ⚠️ **DOCUMENTARY — assert the DELTA; the absolute rides TWO unaudited ledger gaps** / **BackendKind tail 38** (⚠️ a TAIL VALUE — the file declares **39** constants, 0-38; **do NOT "fix" 38 to 39**) / go.mod modules **2** (⚠️ the phase-61.2 lineage figure — **NOT a repo total**; the single `go.mod` requires **67**; **do NOT "fix" 2 to 67**) / internal packages **73** / blank imports in `runner_test.go` **120**, ⚠️ **anchor on the FULL `^\t_ "github.com/pgdad/envoy-go/test/fixtures/` prefix — a naive `^\t_ ` gives 126** / DECISIONS tail **ADR-0299 COMPLETE** (next-free **ADR-0300**) / next-free reference port **10119** (family-banded `10<index>`). ⚠️ **`STATE.md` Project-counts section still says fixtures 119** — NOT drift; phase 77 landed `0118` after that block was written. Both numbers coexist in the tree.

17. **⚠️ DOCUMENTARY DEFECTS, STILL NAMED AS DEFERRALS AND STILL DELIBERATELY NOT FIXED.**
    - **The documented PUBLIC IMPORT PATH DOES NOT EXIST.** `head -1 go.mod` is `github.com/pgdad/envoy-go`; the docs say `github.com/esalaine/envoy-go/validate` on ~20 lines across four docs. ⚠️ **`DECISIONS.md:142` is an ADR that DECIDES the wrong path, never superseded.** A session copying it writes code that does not compile.
    - **A mechanical stat-surface count** to replace the documentary 1207 — **8-11 tasks** (non-literal names, `statroster` table fan-out, registration functions passed as method values).
    - **UNRESOLVED:** the *"plus two more in the same file"* half of the `BEHAVIOR_CONTRACT.md` stale-cite claim. A spot-check flagged `internal/tls/config.go:272` and `internal/filter/http/chain.go:19` as suspicious — **not audited, not edited.**
    - ⚠️ **NEW at this stage:** `BEHAVIOR_CONTRACT.md:501` calls the `wasm.` arm a *"NEW SN9 flattening rule"*, which **COLLIDES with ADR-0118's actual SN9**. And `internal/stats/name.go:350`'s error string enumerates `(want cluster.|http.|listener.|server.)` — **already wrong**, omitting `wasm.` and all eight second-pass shapes.

## What THIS session does — the phase-78 SPEC

One stage per session. Settle the D-BPV-* docket **BY EXECUTION**. Create `docs/envoy-go/phases/78-boot-panic-visibility/SPEC.md`. Draft **ADR-0300 Context section** in `DECISIONS.md` (per `ADR-0044-as-used` — ⚠️ ADR-0044 itself does **NOT** contain the Context-draft discipline). **ROW 78 STAYS `in-progress`**; leave `ROADMAP.md` BYTE-UNTOUCHED if you can. Roll `STATE.md` Current-pointer section **IN PLACE** (ADR-0288) and this file.

⚠️ **`ADR-0089` stays BYTE-UNTOUCHED unless your row lands `/runtime` or `POST /runtime_modify`** — row 78 lands neither.

⚠️ **ADR-0300 carries NO whole-file grep count** — that species self-falsified in ADR-0296 paragraph 3 and ADR-0297 paragraphs 7 **and** 9, and at the phase-76 BRAINSTORM it escalated from a wrong number to a flipped termination-sentinel check. Every count line-scoped or stated with no numeral.

Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** (`reference_parallel_subagents_private_scratch`) **and must tear down docker containers BY NAME** (item 11) — and ⚠️ **if two agents share ONE worktree they must stage by EXPLICIT PATH; a `git commit -am` sweeps in a sibling's uncommitted work.** ⚠️ **AND THEY MUST NOT RUN AN UNSCOPED `git restore`.** Per-task `gofmt` + `golangci-lint` on touched packages (`feedback_pertask_gofmt_lint`).

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. ⚠️ **THIS HAS NOW FIRED LIVE IN TWELVE CONSECUTIVE SESSIONS** — at this BRAINSTORM it fired repeatedly, including on plain read-only commands. **Assume it fires every session.** **Use `git -C <abs-worktree-path>` for every git command, prefer absolute paths over `cd`, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — Current-pointer section (TRUSTWORTHY; ADR-0288 discipline — ⚠️ **anchor greps on `^- **<field>`**, not bare; all seven fields verified singleton at the phase-78 close) + Project counts + the Recent lineage. ⚠️ **Two PRE-EXISTING bookkeeping gaps are recorded there rather than silently fixed:** there has never been a `phase-77 PLAN` lineage entry, and the heading says FIVE while Current plus five prior entries is SIX.
2. `docs/envoy-go/phases/78-boot-panic-visibility/BRAINSTORM.md` — the whole document, especially **2.2** (the relocation and its LIFO risk), **2.3** (the guard and its already-executed failing baseline), **7** (the D-BPV-* docket you must settle), **8** (what is deliberately forwarded and why), and **11** (the rejected alternatives and the two controller self-corrections).
3. `cmd/envoy-go/main.go` — read `:290-310` and `:355-375` FIRST-HAND before writing anything.
4. `docs/envoy-go/DECISIONS.md` — **ADR-0106** (the six-gate) - **ADR-0045** (the split gate) - **ADR-0288** - ADR-0044 (⚠️ it does **NOT** contain the Context-draft discipline — use `ADR-0044-as-used`).
5. `docs/envoy-go/ROADMAP.md` — row 78 and the Schema section before touching the table.
6. `docs/envoy-go/phases/77-runtime-static-layer/SPEC.md` — the shape of a SPEC in this lineage.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — especially **`reference_probe_must_discriminate`**, **`reference_gate_command_negative_control`**, **`reference_liveness_break_needs_failing_baseline`**, `reference_harness_exit_code_is_not_command_exit_code`, `reference_quoting_is_not_executing`, `feedback_brief_citations_not_evidence`, `reference_a_drift_correction_is_itself_a_claim`, `reference_deferred_candidate_cost_restale`, `reference_next_prompt_tracked_despite_gitignore`, `feedback_git_worktrees`, `feedback_execution_style`, and **`reference_bash_cwd_reset_commits_to_main`**.
