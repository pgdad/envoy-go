START a fresh cold-start session for envoy-go — phase 26.2 (`network-filter-registry-migration`) IMPL is DONE + squash-merged + pushed; OPEN the **phase 26.3 SPEC** (`network-filter-rbac`), the THIRD and FINAL sub-phase of the phase-26 §9 Network-filters family.

Phase 26.2 IMPL landed at squash `24f9b13` (pushed to origin/master): tcp_proxy + HCM migrated onto the `internal/filter/network/` framework + `*network.Registry`, the hardcoded `manager.go` terminal-filter registry RETIRED, the dual-dispatch UNIFIED into one `serveNetworkChain` path — at byte-exact parity (R3). Six gates GREEN LIVE; counts neutral (fuzzers 35, fixtures 44, stats 132). ADR-0215 §Decision/§Consequences landed in place (DECISIONS.md tail STAYS ADR-0215; next-free ADR-0216). The `network-filter-mixed-chain-unsupported` reject was LIFTED — mixed `[read-filter*, terminal-filter?]` chains are now expressible (shaped + unit-tested via the buffered-prefix `prefixConn` handover); 26.3's `rbac_network` is the FIRST production consumer.

## What this session must do: AUTHOR the phase-26.3 SPEC

Per SKILL_ROUTING (sub-row 26.3 at state 0/1 — `planned`, no SPEC yet; the phase-26 parent BRAINSTORM already covers 26.1/26.2/26.3), author the per-sub-phase 26.3 SPEC per the 22.1/25.1/26.1/26.2 per-sub-phase-SPEC precedent: it INHERITS the phase-26 parent SPEC §5/§6/§7/§8/§11/§12/§13 rosters + refines per-Task surface, executes any §10 D-question empirical pins IN-SESSION against reference Envoy v1.37.2 (ADR-0004/0005), drafts the §Context of the new ADRs, and runs the spec-document-reviewer loop to APPROVED. Work in a git worktree (`feedback_git_worktrees`; mirror the convention `.worktrees/phase-26.3-network-filter-rbac-spec` off master). The phase-26.3 directory `docs/envoy-go/phases/26.3-network-filter-rbac/` is created at this SPEC entry (git does not track empty dirs).

## Phase 26.3 scope (from the phase-26 BRAINSTORM + ROADMAP row 26.3)

**`network-filter-rbac`** — land `rbac_network` (`envoy.extensions.filters.network.rbac.v3.RBAC`) at FULL upstream parity:
- **Enforced rules + shadow rules** + the connection-level stat roster (`allowed`/`denied`/`shadow_allowed`/`shadow_denied`).
- **Connection-level dynamic-metadata emission** — a NEW connection-scoped sink (the existing `internal/dynamicmetadata/` + `internal/filterstate/` are HTTP-stream-scoped per ADR-0207; 26.3 adds the connection-scoped reuse). This is the FIRST real production write through `ReadFilterCallbacks.DynamicMetadata()` (shaped at 26.1).
- **Extract the shared `internal/rbac/` engine** — the phase-16 RBAC policy engine (currently HTTP-filter-local in `internal/filter/http/rbac/`) extracted into a NEW shared primitive evaluating against an abstract matchable-context interface: HTTP rbac migrated as consumer #1 (re-verified by its phase-16 differential fixtures), network rbac as consumer #2 (extract-at-second-consumer per 22.1/25.1; carries an API-revision-allowance clause).
- **L4 principal/permission inputs** (source/dest IP+port, SNI/`requested_server_name`, downstream-cert principals); HTTP-only matchers (header/path/url) PARSE-REJECT.
- **The FIRST production mixed read→terminal chain:** `rbac_network` allow → `Continue` → `tcp_proxy`/HCM via the `prefixConn` handover. CRITICAL (memory `reference_network_read_filter_onnewconnection_halts`): a network read filter that inspects buffered bytes before deciding MUST `Continue` from `OnNewConnection` and do its stop/release in `OnData` — an `OnNewConnection` `StopIteration` sets the sticky `connHalted` flag which blocks all `OnData`. The deferred "read filter preceding terminal" cross-side differential fixture lands at 26.3 (echo halts + direct_response closes, so 26.2 could only unit-test the handoff).
- Anticipated **ADR-0216** (`internal/rbac/` extraction) + **ADR-0217** (connection-metadata sink) + **ADR-0218** (`rbac_network` filter). NO new third-party `go.mod` deps (the whole §9 family is in-house). New differential fixture(s) + likely new stats + possibly a new fuzzer — the SPEC pins the exact deltas.

## Project state at master tip

- **Master tip:** `b5b8d8e` (the 26.2 IMPL stage-close commit — STATE `last-commit` repoint + this next-prompt rewrite — on top of the substantive 26.2 IMPL squash `24f9b13`). A further next-prompt master-tip repoint (if any) advances the tip +1; confirm the live tip via `git log --oneline -1`. origin/master in sync after push.
- **§9 Network-filters family:** parent row 26 `in-progress`; 26.1 `done`; 26.2 `done`; 26.3 `planned` (flips `planned → in-progress` at the 26.3 SPEC commit per ADR-0106). Parent row 26 flips `in-progress → done` ATOMICALLY with 26.3 at 26.3 phase-done (ROLLUP precedent 18/19/22/24/25).
- **Counts at master tip:** stat surface **132**; differential fixtures **44** (tail **0042**); fuzzers **35**; conformance 10/10; h2spec 53/53; DECISIONS.md tail **ADR-0215**; next-free **ADR-0216**.
- **Toolchain:** Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); reference Envoy v1.37.2 (ADR-0008); go-control-plane v1.32.4 (ADR-0008).

## Read first (in order)

1. THIS FILE — the phase-26.3 SPEC cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 26.2 phase-done; awaiting 26.3 SPEC`; next-skill the 26.3 SPEC authoring; next-free `ADR-0216`; last-commit `24f9b13`.
3. `docs/envoy-go/phases/26-network-filter-chain-and-rbac/BRAINSTORM.md` + the parent `SPEC.md` — the family BRAINSTORM (Q5 rbac-engine extraction; Q6 full-parity rbac scope) + the parent SPEC §5/§6/§7/§8/§11/§12/§13 rosters the 26.3 SPEC inherits + §3.x rbac D-questions.
4. `docs/envoy-go/phases/26.2-network-filter-registry-migration/SPEC.md` + `PLAN.md` + `PROGRESS.md` — the 26.2 precedent (the as-built `network.TerminalFilter`/`prefixConn`/`buildNetworkChainFactory`/`RegisterBuiltins` the rbac_network filter consumes); and `26.1/SPEC.md` (the read-filter chain API `rbac_network` implements).
5. The CONSUMED surface: `internal/filter/network/{types,terminal,chain,prefixconn,registry}.go` + `internal/filter/network/builtins/builtins.go` (register the 5th built-in); `internal/filter/http/rbac/` (the engine to EXTRACT into `internal/rbac/`); `internal/dynamicmetadata/` + `internal/filterstate/` (HTTP-stream-scoped — the connection-scoped sink mirrors/reuses); `internal/listener/manager.go` `serveNetworkChain` (the mixed read→terminal dispatch path).
6. `docs/envoy-go/DECISIONS.md` ADR-0213/0214/0215 (the framework + registry + migration the rbac filter builds on) + ADR-0207 (HTTP-stream-scoped dynamic-metadata — the connection-scoped contrast); `docs/envoy-go/ROADMAP.md` rows 26/26.3; `BOOTSTRAP_PROMPT.md` §5 (state machine) + ADR-0004/0005 (autonomous adaptation; in-session empirical pins) + ADR-0044 (in-place ADR-body discipline) + ADR-0106 (ROADMAP flip-on-SPEC); `docs/envoy-go/SKILL_ROUTING.md` (confirm sub-row 26.3 routing).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — execution-style + worktree + push + subagents-no-push + network-filter-typeurl-extensions (verify the rbac proto type URL via `proto.MessageName`; differential bootstraps need a cluster) + network-read-filter-onnewconnection-halts (the `rbac_network` Continue-from-OnNewConnection constraint) + differential-asserter-dispatch + fixture-dispatch-constraint all apply.

After the 26.3 SPEC is APPROVED + squash-merged + pushed, advance STATE.md per BOOTSTRAP §5 (ROADMAP sub-row 26.3 `planned → in-progress` per ADR-0106; parent row 26 STAYS `in-progress`) and rewrite next-prompt.txt for the 26.3 PLAN cold-start (the `superpowers:writing-plans` decomposition of the 26.3 SPEC §10 task spine).
