=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-CHECKED 2026-07-14 (after the phase-63 SPEC landed + row 63 STAYS `in-progress`, run MECHANICALLY) — it does NOT fire. Do NOT create `stop`.** State:
- (1) prints `NOT DONE: row 63` — row 63 `tracing-custom-tags-environment` STAYS `in-progress` (a row flips `done` only at its IMPL six-gate, ADR-0106 — the SOLE leg here). ⇒ check (1) prints ⇒ the sentinel does NOT fire.
- (2) prints THREE live "candidates:" sentences — HTTP/3, xDS, and Observability ⇒ three families STAY OPEN. The Observability sentence STILL reads `custom_tags (environment/metadata)` at this SPEC (it NARROWS to `(metadata)` only at the phase-63 IMPL — `reference_sentinel_deferred_sentence_live_vs_historical`).
- (3) prints `NEVER OPENED: gRPC`, `Runtime`, `WASM` ⇒ THREE families never opened. Operational-tooling also STAYS OPEN.

Any one of the three printing ⇒ the sentinel does NOT fire.

**STATUS AS OF 2026-07-14: the phase-63 SPEC LANDED and ROADMAP row 63 STAYS `in-progress`.** The SPEC (`docs/envoy-go/phases/63-tracing-custom-tags-environment/SPEC.md`, 14 sections) DISPOSED every D-ENV-* question via LIVE probes against `envoyproxy/envoy:contrib-v1.37.2` (FRESH container per arm; OTLP capture via `test/helpers/otlptrace`; throwaway probe DELETED — docs-only). **Load-bearing findings:** (a) **D-ENV-EMPTYVAL** (NEW, unanticipated) — a PRESENT-but-EMPTY env var → the tag is OMITTED (not `""`, not the default) ⇒ unified rule **omit iff the RESOLVED value is empty**, requiring `os.LookupEnv` (present-ness); (b) **D-ENV-DEDUP** — an `environment` tag that resolves to NOTHING but is FIRST in config order STILL reserves its config-order key slot (the reference dedups by key at config-LOAD, before value resolution); (c) together they **FLIP the CENTRAL decision D-ENV-RESOLVE-TIME from the BRAINSTORM's anticipated Option A → Option B** (ADR-0044 empirical refinement) — a `kindEnvironment` `CustomTagSpec` stored at parse (dedup-slot reserved naturally) + a `kindEnvironment` case in `ResolveCustomTags` (`os.LookupEnv`, omit-iff-empty, ignores `headerLookup`), mirroring the reference's two-phase model + the landed `kindRequestHeader` structure; Option A would need a special-case slot-reservation control-flow wart. **ZERO call-site change** (the phase-62 seam is already threaded). New symbols `kindEnvironment`/`EnvName` GREP-confirmed collision-free. **D-ENV-FIXTURE** = ONE new OTLP fixture `0106` (KEY-PRESENCE via `PATH`, ZERO harness surgery ⇒ SINGLE FLAT ROW; value-equality + the D-ENV-HARNESS injection WEIGHED + DEFERRED). `metadata` STAYS parse-rejected loudly (the SOLE remaining `custom_tags` departure, ADR-0080). **ADR-0284 §Context DRAFTED** (SPEC §13; §Decision/§Consequences at the IMPL — DECISIONS tail STAYS ADR-0283 at this SPEC). Per the lifecycle discipline, the NEXT stage is the phase-63 PLAN.

==============================================

## What THIS session does (the phase-63 PLAN)

Perform the PLAN stage (lifecycle-state 2 → 3) for phase 63 (`tracing-custom-tags-environment`), using the SPEC as the input. This is a DOCS-ONLY stage in a FRESH worktree off master (`feedback_git_worktrees`); NO production `.go` changes. Author `docs/envoy-go/phases/63-tracing-custom-tags-environment/PLAN.md` — a mechanical TDD decomposition of SPEC §10 into ~8–10 ordered red→green tasks (`superpowers:test-driven-development` + `superpowers:writing-plans`), each with its liveness break (`reference_differential_break_protocol_count1` where a differential assertion is load-bearing; `-count=1`), honoring the SPEC's PINNED decisions:
- **D-ENV-RESOLVE-TIME = Option B** (a `kindEnvironment` spec + a `kindEnvironment` resolver case; NOT parse-time-static). Do NOT re-open this — the SPEC §11 arm F + arm G probes decided it.
- The **parse arm** (lift `config.go:195-196`; append a `kindEnvironment` spec; empty-`environment.name` PGV-parity reject `.pb.validate.go:468`), the **`customTagKind`/`CustomTagSpec` extension** (`kindEnvironment` + `EnvName`, `config.go:42-63`), the **resolver case** (`resolve.go:18` + the `os` import; `os.LookupEnv`; omit-iff-resolved-empty; ignores `headerLookup`), **ZERO `accesslog_emit.go` call-site change** (already threaded — SPEC §3.4), the **`config_test.go` change** (the `environment` row `:463-465` reject → ACCEPT; a NEW empty-name reject; the `metadata` row `:468-470` STAYS; dedup-with-omit arm F), the **`resolve_test.go` matrix** (present/default/omit/**present-empty→omit via `t.Setenv`**/dedup), the **Zipkin unit test**, the **`0106` fixture** (key-presence via `PATH`, value-non-empty both sides), the **`FuzzHCMConfigParse` seed** (NO new fuzzer — count STAYS 55), the **six-gate verify** + full 108-dir differential, and the **ADR-0284 body / STATE / ROADMAP row-63-`done` + deferred-sentence-NARROW / router roll** as the closing task.
- **RE-DERIVE every SPEC §12 edit-site `file:line` against the CURRENT master tip** (`git fetch` first) before the PLAN pins them (`feedback_brief_citations_not_evidence`) — the SPEC's citations were derived against `796f97e3`; re-verify. Scaffold `PROGRESS.md`. Row 63 STAYS `in-progress` at this PLAN. Land it (commit + push per `feedback_push_to_origin`) and ROLL THIS FILE to the IMPL stage.

**Honor throughout (the discipline, unchanged):**
- **Lifecycle = one stage per session.** This session: the PLAN ONLY. After it lands: roll to the IMPL.
- **Process anchors:** ADR-0044 (ADR §Decision/§Consequences land at the IMPL — the SPEC drafted only §Context) · ADR-0045 (the split escape-valve — SPEC pinned a SINGLE FLAT ROW; re-arm only if the PLAN's task count exceeds ~15) · ADR-0106 (a row flips `done` at its IMPL; the SOLE leg here) · `reference_roadmap_split_phase_row_done`.
- **`feedback_brief_citations_not_evidence`** — RE-DERIVE every `file:line` the PLAN pins from source against the CURRENT master tip. A brief's citation is not evidence — a subagent will copy it verbatim.
- **`reference_spec_drafted_identifier_collision_check`** — the SPEC's proposed `kindEnvironment`/`EnvName` were GREP-confirmed collision-free this session, but RE-CONFIRM against the master tip before the PLAN adopts them.
- **`reference_fuzzer_count_docs_drift`** — the seed must NOT move the fuzzer count off 55; reconcile actual `^func Fuzz` before AND after.
- **`next-prompt.txt` IS TRACKED** (`reference_next_prompt_tracked_despite_gitignore`) — edit it inside the stage worktree and fold into the stage commit; locate commits by SUBJECT (`git log --grep`), never by position.
- **Worktree discipline** (`feedback_git_worktrees`, `feedback_subagent_worktree_detach`, `feedback_subagent_worktree_path_targeting`); if subagent-driven, `feedback_execution_style` + `feedback_subagents_no_push`.
- Read the phase-62 PLAN (`docs/envoy-go/phases/62-tracing-custom-tags-request-header/PLAN.md`) as the template — the SAME custom-tag engine, the SAME TDD spine shape (resolver+types first, then the parse arm, then fixture/fuzz/verify/ADR).

**ADR reservation ledger (provisional; each finalized at its IMPL in landing order):** ADR-0277 = phase-59 tracing-custom-tags-literal (LANDED) · ADR-0278 = xDS 60.1 (LANDED) · ADR-0279 = HTTP/3 61.1 (LANDED) · ADR-0280 = xDS 60.2 (LANDED) · ADR-0281 = HTTP/3 61.2 (LANDED) · ADR-0282 = HTTP/3 61.3 (LANDED) · ADR-0283 = phase-62 tracing-custom-tags-request-header (LANDED) · **ADR-0284 = phase-63 tracing-custom-tags-environment** (RESERVED — §Context DRAFTED at the SPEC [SPEC §13]; §Decision/§Consequences at the phase-63 IMPL) · next-free unreserved **ADR-0285**.

**Current counts (VERIFIED at the phase-63 SPEC against master tip `796f97e3` — RE-VERIFY at session start, `git fetch` first):** fixtures **107** (`ls -d test/fixtures/[0-9]*/ | wc -l`; tail `0105-tracing-custom-tags-request-header`) · fuzzers **55** · DECISIONS tail **ADR-0283** (next-free **ADR-0284**) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** · go.mod modules **2** (`quic-go` direct + `qpack` indirect). A PLAN is DOCS-ONLY ⇒ ALL counts UNCHANGED at that stage. **Anticipated at the phase-63 IMPL:** fixtures **107 → 108** (`0106-tracing-custom-tags-environment`) · fuzzers **55 (+0)** · stat **1201 (+0)** · DECISIONS tail **ADR-0284** · new packages **0** · new modules **0**; row 63 → `done`.

**Locating the phase-63 SPEC stage squash** (a router cannot name its own sha — identify by SUBJECT, never by position):
```sh
git log --oneline --grep='^phase 63 (tracing-custom-tags-environment) SPEC' -1
```

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-63 PLAN is next; row 63 is `in-progress`).
2. `docs/envoy-go/STATE.md` — the active-phase header (phase 63 SPEC done; NEXT = the phase-63 PLAN).
3. `docs/envoy-go/phases/63-tracing-custom-tags-environment/SPEC.md` — the PINNED design (D-ENV-RESOLVE-TIME=Option B, the §11 live-probe block incl. arm F/G, the §12 edit-site roster, the ADR-0284 §Context) this PLAN decomposes.
4. `docs/envoy-go/phases/62-tracing-custom-tags-request-header/PLAN.md` + `docs/envoy-go/DECISIONS.md` ADR-0277/ADR-0283 — the LANDED custom_tag engine (`CustomTagSpec`/`ResolveCustomTags`/`upsertAttr`/first-wins-dedup), the seam this `environment` row extends; the phase-62 PLAN's TDD spine is the template.
5. `docs/envoy-go/ROADMAP.md` — the Observability family (row 63 + its narrowed-at-IMPL deferred sentence), the xDS + HTTP/3 live deferred sentences, and the never-opened gRPC/Runtime/WASM + Operational-tooling families.
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the tracing `reference_*` traps (esp. `reference_tracing_custom_tag_override_builtin`, `reference_spec_drafted_identifier_collision_check`, `reference_fuzzer_count_docs_drift`) + the differential-harness traps.
