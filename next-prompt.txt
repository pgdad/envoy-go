START a fresh post-phase-24-SPEC cold-start session for envoy-go.

This is a fresh-session prompt for the post-phase-24-SPEC cold-start. **Phase 24 (`http-filter-global-ratelimit`, `envoy.filters.http.ratelimit` — the GLOBAL rate-limit filter) SPEC is DONE.** It landed at master via squash-merge `7a80c8a` + the SHA-fill follow-up `d4076ff` (pushed to origin). ROADMAP row 24 stays `in-progress` (SPEC-done annotation added; flips to `done` only at IMPL phase-done with all 6 gates GREEN). The natural next-skill per STATE.md + SKILL_ROUTING state 2 → 3 is **`superpowers:writing-plans` to author the phase-24 PLAN** — decompose `docs/envoy-go/phases/24-http-filter-global-ratelimit/SPEC.md` into bite-sized TDD tasks at `docs/envoy-go/phases/24-http-filter-global-ratelimit/PLAN.md`, **APPLY the ADR-0045 split-gate**, and run the plan-document-reviewer loop. **PLAN authoring is the interactive `superpowers:writing-plans` work (like the phase-09..23 PLAN sessions); subagent-driven execution (project memory `feedback_execution_style.md`) applies at the IMPL EXECUTION session (state 3 → IMPL uses `superpowers:subagent-driven-development`), NOT at the PLAN-authoring stage.**

## What this session must do: author the phase-24 PLAN (and almost-certainly SPLIT into 24.1/24.2)

Per SKILL_ROUTING state 2 → 3, the post-SPEC session authors PLAN.md from the SPEC. The PLAN must:

1. **APPLY the ADR-0045 split-gate FIRST.** The SPEC's post-empirical LoC envelope is **~1900–2700 LoC** (SPEC §1.2 + §3.4), **ABOVE the ~1500 split-gate** — so a 24.1/24.2 split is the EXPECTED outcome (Q1 deferred the split decision to PLAN time). The SPEC §16 records the candidate axis: **24.1** = the `internal/grpcclient` `RateLimitClient` (DELTA 1) + filter config + the descriptor engine for a CORE action subset (`generic_key`, `request_headers`, `remote_address`, `destination_cluster`, `header_value_match`) + `ShouldRateLimit` dispatch + OK/OVER_LIMIT/error dispositions + failure modes + the cluster-scoped stat surface + **DELTA 2 (the route-table `rate_limits` exposure — a precondition for ANY descriptor build)** + the cross-side differential `0032` (b/c/d/e) + the boot-reject `0033`; **24.2** = the remaining actions (`source_cluster`, `masked_remote_address`, `metadata`, `query_parameters`, `query_parameter_value_match`) + the X-RateLimit DRAFT_VERSION_03 headers + `RateLimitPerRoute` (the 10th canonical + ADR-0125 9→10) + the `stage` multi-stage path + the `vh_rate_limits` Axis-B composition + `0032` (f/g). ADR mapping: 24.1 = ADR-0197 core + ADR-0198 (DELTA 2) + ADR-0200; 24.2 = ADR-0199 + the X-RateLimit/remaining-actions slice of ADR-0197. **If you split:** create sibling dirs `24.1-*`/`24.2-*` each with its own SPEC slice, make ROADMAP row 24 the parent (`in-progress`, `sub-phases = 24.1, 24.2`), add per-sub-phase rows (`planned`), point STATE at 24.1, append an ADR explaining the split (next-free `ADR-0201`), and **STOP** (BOOTSTRAP §6.2) — the next session starts 24.1 at lifecycle state 1. **If you judge the surface carve-able within one row** (unlikely at this LoC), author a single PLAN.md and proceed to the reviewer loop.
2. **Decompose into bite-sized TDD tasks** (each: write failing test → verify fail → minimal impl → verify pass → commit) with exact file paths, per `superpowers:writing-plans`. The SPEC §6.10 source-file roster + §4 descriptor engine + §5 PARSE-REJECT roster + §6 code shapes + §7 fixtures + §14 testing taxonomy are the authoritative inputs. Anchor each ADR's §Decision/§Consequences body-landing to a specific Task per the SPEC §10 Lands-in-Task map.
3. **Honor the SPEC's empirical AMENDs as the implementation contract** (SETTLED — do NOT re-derive or re-litigate; see the AMEND catalog below).
4. **Create a fresh worktree** per `feedback_git_worktrees.md` (branch `phase-24-http-filter-global-ratelimit-plan`, off master tip — run `git log --oneline -1` to confirm the actual tip SHA, which is this follow-up commit).
5. **Run the plan-document-reviewer subagent loop** (≤3 iterations per ADR-0005 / the writing-plans review loop). If not approved after 3, set STATE blocked + exit.
6. **Update STATE.md** at session end per BOOTSTRAP §4.1 invariant (lifecycle-state 3 `next-skill = superpowers:subagent-driven-development`, or — if you split — STATE points at 24.1 state 1) + push to origin per `feedback_push_to_origin.md`.

## Pre-existing settled context (DO NOT re-litigate)

The phase-24 SPEC is DONE and CANONICAL at master tip `7a80c8a` (+ this SHA-fill follow-up). Do NOT reopen the SPEC decisions, the D1–D7 empirical pins, or the BRAINSTORM Q-decisions.

- **The 11-entry AMEND catalog (SPEC §1.1) — 6 substantive REFUTATIONS of the BRAINSTORM, empirically verified against v1.37.2 source + go-control-plane v1.32.4:**
  - **AMEND-1 (stat anchoring — REFUTES):** 4 counters `ok`/`error`/`over_limit`/`failure_mode_allowed` (common lib `stat_names.h:15-18`; NO `cluster_not_found`; no gauges) anchored at the **CLUSTER scope** `cluster.<rls_cluster_name>.ratelimit[.<stat_prefix>].<stat>` (NOT `http.<HCM_prefix>.ratelimit.<stat>`); modulated by the filter-config `stat_prefix`. Stat count 110 → 114.
  - **AMEND-2 (no runtime proto fields — REFUTES):** the `RateLimit` filter config has NO `filter_enabled`/`filter_enforced` `RuntimeFractionalPercent` fields; enable/enforce are hardcoded runtime keys `ratelimit.http_filter_enabled`/`ratelimit.http_filter_enforcing` (honored-as-static, always-on, since envoy-go has no runtime layer). The route-level `RateLimit.disable_key` (string) is the PARSE-REJECT surface (ADR-0200).
  - **AMEND-3 (filter-config roster — REFINES):** 13 fields incl. NEW `status_on_error` (default 500) + `stat_prefix`; `request_type` is a STRING (empty⇒"both"); `rate_limited_status` clamps <400⇒429; `response_headers_to_add` IS on the filter config (max 10); `timeout` 20ms; `failure_mode_deny` false⇒fail-open.
  - **AMEND-4 (inert `override_option` — REFUTES):** `RateLimitPerRoute.override_option` is `[#not-implemented-hide:]` and NEVER read; embedded `rate_limits[]` is an unconditional early-return (Axis A) — NOT driven by `override_option`. NEW per-route `domain` field. envoy-go PARSE-ACCEPTS-but-IGNORES `override_option`.
  - **AMEND-5 (vh composition — RATIFIES):** `vh_rate_limits{OVERRIDE=0,INCLUDE=1,IGNORE=2}`; route policy ALWAYS walked; VH per IGNORE⇒never / INCLUDE⇒always / OVERRIDE⇒only-if-route-empty; legacy `RouteAction.include_vh_rate_limits=true` forces INCLUDE.
  - **AMEND-6 (descriptor `hits_addend` UInt64Value — REFUTES):** descriptor-level `hits_addend` is `google.protobuf.UInt64Value` (not bare uint32); request-level `RateLimitRequest.hits_addend` is bare uint32 default 0; Unit enum is non-monotonic (WEEK=7) ⇒ the shared fake `RateLimitService` MUST encode by proto field NUMBER + omit unset optionals; descriptor `entries` in action-list order.
  - **AMEND-7 (disable_key + runtime enforcement — REFINES):** route `disable_key` non-empty ⇒ PARSE-REJECT; `RateLimit.stage` is a UInt32Value (0-10); the `RateLimit.Override` (`limit`) field's only v1.32.4 arm is `dynamic_metadata` ⇒ honor-as-absent (no override).
  - **AMEND-8 (reply byte-pin — REFINES):** OVER_LIMIT status `rate_limited_status` (429), body=RLS `raw_body` (empty default), rc_details `"request_rate_limited"`, `x-envoy-ratelimited: true` (unless disabled), RLS+filter-config `response_headers_to_add` (descriptor-first), gRPC status 8 (resource_exhausted) else 14; error/fail-closed status `status_on_error` (500), rc_details `"rate_limiter_error"`, NO header attach (nullptr-mutate); X-RateLimit (`x-ratelimit-limit/-remaining/-reset`) emitted on ALL dispositions when DRAFT_VERSION_03, driven by the MIN `limit_remaining` descriptor-status, `;w=`/`;name=` quota suffix.
  - **AMEND-9 (DELTA-2 architecture — REFUTES; HIGHEST-RISK):** the route/vhost `rate_limits` are first-class Route/VirtualHost proto fields NOT in `typed_per_filter_config`, and the hcm `routeTable` is package-internal — so `RequestRouteConfig()` (TPFC-by-filter-name) CANNOT carry them. DELTA 2 is a genuinely NEW chain-seeded `DecoderFilterCallbacks` accessor pair `RouteRateLimits()`/`VirtualHostRateLimits()`, plumbed like the ADR-0165 `DownstreamRemoteAddr` set-once-by-dispatch primitives (the exact chain-seed type + accessor return-type is the SPEC §12 item-1 highest-risk byte-confirmation — could force an escape-valve ADR at IMPL like phase-23's PD-5 → ADR-0196).
  - **AMEND-10 (cross-namespace stat write — REFUTES/NEW):** the cluster-scoped surface (AMEND-1) is self-registered by the filter via `ctx.Stats.NewCounterIfAbsent("cluster."+clusterName+".ratelimit."+...)` — the FIRST landed cross-namespace cluster-stat-charge (the SAME pattern ext_authz's `charge_cluster_response_stats` DEFERRED, BEHAVIOR_CONTRACT §6 amendment 8). The cluster package offers NO registry/scope helper; ADR-0197 §Decision sanctions the write.
  - **AMEND-11 (action roster + key defaults — RATIFIES):** 12 action arms (10 canonical + `dynamic_metadata` DEPRECATED-in-proto + `extension`); descriptor-key defaults `generic_key`→"generic_key", `header_value_match`→"header_match", `query_parameters`→**"query_param"** (singular), `query_parameter_value_match`→"query_match"; `request_headers`+`metadata` require a config key; `expect_match` default true; empty-action-drop has TWO behaviors (action false ⇒ drop WHOLE descriptor + break; empty-key ⇒ skip entry, descriptor survives).
- **ADR-0197 + ADR-0198 + ADR-0199 + ADR-0200 §Context drafts are LANDED at DECISIONS.md** (anchored at the SPEC commit per ADR-0044). Their **§Decision + §Consequences bodies land at phase-24 IMPL** (per the SPEC §10 Lands-in-Task map: ADR-0197 → filter-package + engine + RateLimitClient Task; ADR-0198 → route-table parse/seed + accessor-pair Task; ADR-0199 → per-route compile + ADR-0125 9→10 amendment Task; ADR-0200 → compiled_config + PARSE-REJECT roster Task). Next-free ADR is `ADR-0201` (reserved for the split ADR if you split, OR for an IMPL-time escape-valve). **ADR-0201 hypothesized UNCONSUMED at phase-24 phase-done** (D-style HOLD-with-known-risk; LARGER surprise surface than phase-23 — DELTA-2 + the cross-namespace stat write; a 24.1/24.2 split re-maps ADR consumption across sub-phases).
- **TWO framework deltas STAND:** DELTA 1 = `internal/grpcclient/ratelimit_client.go` (clone the unary `AuthClient`, NOT the bidi `ProcessorClient`; `ShouldRateLimit` unary; no `Dialer` API change; the THIRD ADR-0158 typed wrapper). DELTA 2 = the AMEND-9 chain-seeded route-table exposure. NO new top-level `internal/` package. Boot-registration alphabetical between `oauth2` and `rbac`; 19 HTTP filters post-phase-24.
- **Per-route `RateLimitPerRoute` = NEW 10th canonical** ("data-only-with-vh-inclusion-enum") + ADR-0125 amendment 9 → 10 (anchored in ADR-0199 §Context; LANDS at IMPL). RE-AMENDS after phase-23's REUSE-by-absence skip; canonical-per-route roster STAYS 9 until the IMPL amendment.
- **Two-directory differential STANDS** per `reference_differential_fixture_dispatch_constraint`: `0032-http-ratelimit` (cross-side via a SHARED fake gRPC `RateLimitService` in a NEW `test/helpers/ratelimitgrpc/` + a `fixture.HTTPGlobalRateLimitGRPC` BackendKind + a fixture-local `driver.go` following the ext_authz `0021` fixed-pre-allocated-port pattern; scenarios a–h, subject-side stat assertions in `StatsAsserter` per `reference_differential_asserter_dispatch`) + `0033-http-ratelimit-boot-reject` (`domain`-empty shared reject). Fixtures 33 → 35; fuzzer `FuzzRateLimitConfigParse` 32 → 33. The `disable_key`/`extension`/`dynamic_metadata` rejects are NOT boot-reject fixtures (upstream accepts them; unit-tested + BEHAVIOR_CONTRACT-recorded, count 15 → 18).

## Base + invariants at master tip `d4076ff` (SHA-fill follow-up; squash-merge predecessor `7a80c8a`)

- **Master tip:** `d4076ff` (`phase 24 SPEC follow-up: STATE.md SHA-fill (TBD -> 7a80c8a) + next-prompt advance to post-phase-24-SPEC PLAN cold-start`; docs-only). Predecessor: `7a80c8a` (squash-merge phase-24-http-filter-global-ratelimit-spec). Run `git log --oneline -1` to confirm the actual tip SHA for the worktree base. Pushed to origin.
- **Baseline at master tip (UNCHANGED by the SPEC — docs-only):** **33 differential fixture directories** GREEN (0000-0031; the lua 0026-0029 + any multi-listener combined run may hit the documented `freeTCPPort` flake per 22.2 REVIEW §7.4 — re-run in isolation; not a defect); **32 fuzzers** green; h2spec 53/53 PASS at ADR-0051 v1.32.4 pin; build/vet/lint clean; race-tests clean; **18 HTTP filters wired**; **110 stat names**; **15 envoy-go-strict departure records** at BEHAVIOR_CONTRACT. DECISIONS.md tail at **ADR-0200** (§Context-draft state; §Decision/§Consequences stubbed "Lands at IMPL"); ADR-0197..ADR-0200 §Context-anchored; **next-free ADR-0201** (unconsumed). Canonical-per-route roster STAYS **9** (the 9 → 10 amendment LANDS at phase-24 IMPL via ADR-0199). §9 HTTP-filters family: **1 remaining row post-phase-24** (`wasm`); phase 24 opened `global rate limit`.
- Project memories ALL apply: `feedback_git_worktrees.md`, `feedback_push_to_origin.md`, `feedback_execution_style.md`, `reference_differential_fixture_dispatch_constraint.md`, `reference_differential_asserter_dispatch.md`.

## Read first (in order)

1. THIS FILE — post-phase-24-SPEC cold-start + the PLAN-authoring obligations (split-gate FIRST).
2. `docs/envoy-go/phases/24-http-filter-global-ratelimit/SPEC.md` — the authoritative phase-24 design (the 11-entry AMEND catalog §1.1; the §4 descriptor engine; the §5 PARSE-REJECT roster; the §6 code shapes + §6.10 file roster; the §7 differential; the §10 ADR anchor map; the §11 D1–D7 pin matrix; the §15 acceptance checklist; the §16 split-axis planning anchor).
3. `docs/envoy-go/phases/24-http-filter-global-ratelimit/BRAINSTORM.md` — the SETTLED Q-decisions + framework deltas (do NOT reopen).
4. `docs/envoy-go/STATE.md` — phase 24 SPEC done; next-skill `superpowers:writing-plans` (PLAN); next-free ADR-0201.
5. `docs/envoy-go/ROADMAP.md` — row 24 `in-progress` (SPEC-done annotation).
6. `docs/envoy-go/SKILL_ROUTING.md` — confirm state 2 → 3 (PLAN.md exists? no → author it; apply the split-gate).
7. `docs/envoy-go/DECISIONS.md` tail — ADR-0197..ADR-0200 §Context drafts (the IMPL §Decision/§Consequences land at their Lands-in-Tasks).
8. `BOOTSTRAP_PROMPT.md` (repo root) — §4.1 STATE-update invariant + §5 state machine (state 2 → PLAN + the split-gate) + §6 split policy.
9. The nearest split precedents: `docs/envoy-go/phases/18-http-filter-ext-authz/` (18.1+18.2 — the gRPC + fake-service split) + `docs/envoy-go/phases/22-*` (22.1+22.2+22.3) for the PLAN-time split mechanics; `docs/envoy-go/phases/23-http-filter-admission-control/PLAN.md` for the single-filter PLAN shape + the TDD task granularity.
10. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`.
