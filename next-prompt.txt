=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **PHASE 92 (`h2-response-header-validation`) IS OPEN AND MID-LIFECYCLE. ROW 92 READS `in-progress` AT `ROADMAP.md:154` AND `want` IS 124.** ⇒ **THERE IS BANKED MID-LIFECYCLE WORK. DO NOT SELF-PICK. THIS SESSION RUNS THE PHASE-92 PLAN (lifecycle-state 2 -> 3).** ⚠️ **THE SENTINEL DOES NOT FIRE — check (1) prints `NOT DONE: row 92`, check (2) prints SIX, check (3) is SILENT. TWO checks block it. DO NOT create `stop`.** ⚠️ **MEASURE ALL THREE YOURSELF BEFORE DOING ANYTHING ELSE; do not take this file's word for it.**

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE. This form parses FIELDS instead of matching a pattern, and
#     asserts the row DENOMINATOR so a deleted row is itself an error.
awk -F'|' -v want=124 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **CHECK (1) IS NOT SILENT — THE OPEN-ROW BOARD SHAPE.** It prints `NOT DONE: row 92`. **Do NOT read that as breakage and do NOT "fix" it by flipping row 92** — the row flips `done` at the phase-92 IMPL, not before. Run BOTH doctoring NCs EVERY SESSION anyway:
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md.
```
⚠️ **NC-A READS **TWO** LINES** (`NOT DONE: row 62` AND `NOT DONE: row 92`), because row 92 is legitimately open. **The one-line expectation was the ALL-`done` board's shape and DOES NOT CARRY.** ⚠️ **This is the FOURTH distinct NC-A shape in five stages** (two-line at the phase-91 PLAN, one-line at the 91 IMPL, two-line at the 92 BRAINSTORM, two-line again now). **Read it yourself; never inherit the expected shape.**
```sh
# the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```
Also NC the DENOMINATOR (`want=123` on the real file must print `GATE FAIL: examined 124 data rows, expected 123` **alongside** the `NOT DONE: row 92` line — ⚠️ **on this board NC-B is NOT a single-line result either**).

⚠️ **AND WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -oiE '-family row'` fails with `grep: amily row: No such file or directory` and the surrounding arithmetic prints `base=0 now=0 delta=0`, **which reads exactly like "no change."** **Always pass `--` before the pattern**, and the counts are form-dependent: with `--`, occurrences read **95** and LINES read **67**. **`grep -c` counts LINES, NOT OCCURRENCES. A gate that reads zero on BOTH sides is not evidence of invariance.**

⚠️ **THE ARM-A MALFORMED-ROW FIGURE RECONCILES ONLY UNDER AN ESCAPE-AWARE FIELD COUNT — RE-VERIFIED AT THE PHASE-92 SPEC, WHERE IT STILL READS *TWO*.** The malformed rows are ids **57** (`NF=9`) and **69** (`NF=10`). ⚠️ **A NAIVE `awk -F'|'` `NF != 8` FORM READS *SEVENTEEN*** — legitimately ESCAPED pipes (`\|`) split as fields. **Strip `\|` BEFORE splitting.** A gate asserting `== 0` FAILS on pre-existing content; assert against the known-2 baseline. ⚠️ **NEW THIS STAGE: the two forms DISAGREE ON ROW 57 ITSELF** (naive reads `NF=13`, escape-aware reads `NF=9`), so **any gate must state WHICH FORM it uses.** Row 92 field-counts to NF=8 under BOTH forms.

⚠️ **AND DO NOT SUBSTITUTE A "REASONABLE EQUIVALENT" FOR CHECK (1)'s AWK.** A measurement agent once tried `NF>=8 && $2 ~ /^ *[0-9]+ *$/`: it printed **92**, not the true denominator, because the naive split of escaped-pipe rows moves `NF` and the numeric `$2` anchor then rejects rows the canonical form accepts. **Use the form above verbatim.**

**SENTINEL RUN MECHANICALLY AT THE PHASE-92 SPEC (2026-08-25) at `221eedf4`.** ACTUAL output, recorded not predicted:
- **(1)** `want=124` -> **`NOT DONE: row 92`**, ONE line · **(2) SIX** at `:202 :208 :214 :224 :230 :238` · **(3) SILENT.**
- **All four NCs fired:** NC-A **TWO** lines (`NOT DONE: row 62` + `NOT DONE: row 92`) with `NC LANDED? [ in-progress ]` **inspected FIRST** · NC-B `NOT DONE: row 92` **AND** `GATE FAIL: examined 124 data rows, expected 123` · NC-C residual **2 -> 0** => `NEVER OPENED: gRPC`, WASM correctly silent · NC-D long **5** / short **1** / union **6**.
⇒ **THE SENTINEL DOES NOT FIRE — TWO checks block it. `stop` WAS EVALUATED AND DELIBERATELY NOT CREATED** (verified absent at the git root AND in the stage worktree).
- ⚠️ do NOT "fix" the six and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases; the candidate sentences are a WINDOW onto a larger deferred backlog, not an inventory of it. ⚠️ **Cite ~40 only as "sentence-visible", never as the inventory** — and see METHOD NOTE 3d: **no gate may rest on a ` + ` split.**

==============================================

## STATUS AS OF 2026-08-25: PHASE 92 (`h2-response-header-validation`) **OPEN AT lifecycle-state 2**. **THIS SESSION RUNS THE PHASE-92 PLAN.**

**What the 92 SPEC landed (2026-08-25, base master `221eedf4`, ONE squashed commit).** **DOCS ONLY — ZERO production `.go`, ZERO test `.go`** (`git diff --name-only master -- '*.go'` => **0 files**). `SPEC.md` created (**586 lines**) in the existing phase dir; phase dirs STAY **133**. `ROADMAP.md`, `BEHAVIOR_CONTRACT.md` (**5962**), `go.mod` and `go.sum` are **BYTE-UNTOUCHED** (verified by EMPTY DIFF against master); `want` STAYS **124**; row 92 STAYS `in-progress`. **ADR-0314 §Context DRAFTED** — `DECISIONS.md` **18439 -> 18467** (`--numstat` **`28 0`**), `^---$` STAYS **216**, `^## ADR-` **312 -> 313**, bare `^## ` **320 -> 321**, tail **ADR-0314**, next-free **ADR-0315**. Strict `^> **STATUS: PROPOSED` guard **RE-ARMED 0 -> 1**, verified **BY LINE AND ADR** (`:18443`, nearest preceding heading `## ADR-0314` at `:18441`; the ADR-0231 decoy at `:14866` still reads 1 and is byte-untouched). `STATE.md` rolled IN PLACE (**`9 9`**, 63 lines); `STATE_HISTORY.md` **524 -> 526** (`2 0`), **strict guard `^- \*\*prior active-phase:\*\*` STAYS 163, DELTA 0**, loose **212 -> 213**.

### ⚠️ THE CHARTER (unchanged in substance)

**On the HTTP/2 downstream leg, REJECT a response whose upstream leading header block carries a connection-specific or otherwise malformed field, instead of laundering it onto the downstream stream.** The ENCODE-direction mirror of `h2.IsIllegalH2RequestHeader` (row 89, DECODE direction). Seven shapes forwarded with **200** by the subject where the reference `contrib-v1.37.2` answers **502 `reset reason: protocol error`**.

### ⚠️ THE FOUR DECISIONS THE SPEC MADE — a taker implements these, it does not re-open them

1. **D-92-POSTURE — 502, EVICTING, NOT RETRIABLE.** The new validator returns an `*h2.Error` carrying a NEW sentinel `ErrMalformedResponseHeaders` (explicitly **NOT** `ErrMalformedTrailers`, whose arm gives a non-parity `Status: 0`), and `doH2ClusterAction` gains a **THIRD arm placed AFTER `EvictH2ConnOnError`** — so eviction still happens, which IS the parity behaviour — returning **502 WITHOUT `localOrigin`**. ⚠️ **THAT ARM IS A SECOND FILE IN A SECOND PACKAGE AND NEITHER COST PROTOTYPE INCLUDES IT.** ⚠️ **`RecordUpstreamResult`'s `LocalOriginErr` stays `true` while `ActionResponse.localOrigin` stays unset — they are DIFFERENT CONSUMERS (outlier detection vs retry classification) and must NOT be "tidied" into agreement without measuring outlier detection separately.**
2. **D-92-VALIDATOR — TWO INDEPENDENT FUNCTIONS sharing the three carrying predicates.** NOT a mode flag, NOT thin wrappers. `validateResponseTrailers` has SIX legs; only THREE carry (`hasUppercaseHeaderChar`, `isConnectionSpecificField`, the `te` rule) and THREE **invert** (END_STREAM framing; the pseudo-header ban, since `:status` is REQUIRED; the `content-length` ban, since ONE is legal). ⚠️ **The duplicate-`content-length` leg is a CROSS-FIELD COUNT, not a predicate** — it cannot live in a `switch` over one `(name, value)`, which alone forecloses "one body, one flag". ⚠️ **DO NOT collapse the connection-specific + `te` legs into `IsIllegalH2RequestHeader`** — it REDDENS `TestValidateResponseTrailers_Table/te_gzip`, which asserts `wantMsgSubstr: "not 'trailers'"` while each connection-specific member asserts its own quoted name.
3. **D-92-DIFF — EXTEND fixture `0004`, do NOT mint `0120`.** Priced by a COMPILING prototype at **`+193 / -0` across two files** (`backends/main.go` `31 0`, `driver/driver.go` `162 0`), `gofmt` clean, `go vet` rc=0 both packages, `go test -c` rc=0 — plus **0 YAML lines, 0 new BackendKind, 0 registration gates, 0 port allocations**, `BackendCount()` unchanged at 3. Minting would cost ≈ **1440 lines across ~8 files**.
4. **D-92-FUZZ — YES**, `FuzzValidateResponseHeaderBlock`, ~60 LoC, fuzzers **55/48 -> 56/49**. ⚠️ The precedent argument is the OPPOSITE of the one hypothesised: **`IsIllegalH2RequestHeader` has NO fuzz target**, nor does `isConnectionSpecificField`, `hasUppercaseHeaderChar` or `validateResponseTrailers`. ⚠️ **Reachability is not coverage** — `FuzzFrameStream` transitively reaches `isConnectionSpecificField` but only asserts "no panic + errors begin with `h2:`", so it can never observe a wrong classification.
5. **D-92-1XX — CORRECT IT, in the 10/3 accurate form.** The wrong claim *"the reference FORWARDS 1xx"* is the tail of **`client.go:673`**, inside the NAMED NON-GOAL comment in the very symbol this row edits. Minimal fix is `1 1`; the chosen accurate restatement is **`10 3`** (gofmt clean, lint rc=0). **Scope settled: exactly ONE wrong instance tree-wide.** ⚠️ `ROADMAP.md:154` and `next-prompt.txt` carry the string only to REFUTE it — **already correct, do NOT "fix" them.**
6. **OUT OF CHARTER, stated in writing:** the posture FLAG (envoy-go parses NEITHER spelling; zero `.go` hits repo-wide; silently ignored — the documented 05.2 disposition) and `%RESPONSE_CODE_DETAILS%` (no analogue anywhere in the HTTP stack; `log_format` is BOOT-REJECTED). **IN CHARTER: ONE new counter**, the `http2.rx_messaging_error` analogue, stat surface **406 -> 407**.

### ⚠️ WHAT THIS SPEC REFUTED BY EXECUTION — do NOT re-inherit the old wording

1. ⚠️ **THE SPEC REFUTED ITS OWN CONTROLLER-SIDE HYPOTHESIS.** Reading showed the 502 arm hard-`Close()`s a SHARED MULTIPLEXED pooled conn while the trailer arm deliberately sits above the evict. The hypothesis drawn from it — *"matching the status while diverging on connection lifetime is a NEW divergence"* — **is FALSE at the default posture.** Measured with both sides on ONE downstream conn and backend-accepted-TCP-conns = 1: the subject kills an in-flight legal sibling (502) and **so does the reference** (`upstream_cx_destroy_local` 0->1, `upstream_cx_protocol_error` 0->1, `upstream_rq_tx_reset` 0->**2**). ⇒ the banked *"FALSE for every sibling stream"* claim is **right about the harm and wrong as a divergence claim**. The real gap is narrower: **the reference offers an OPT-OUT and envoy-go has none.**
2. ✅ **BUT THE RETRY CONSEQUENCE IS A REAL DIVERGENCE THE FIX WOULD INTRODUCE.** Under `retry_on: connect-failure, num_retries: 2` the patched subject makes **3 backend-observed attempts on 3 SEPARATE TCP CONNS** (`upstream_rq_retry` **2**, `upstream_cx_total` 1->3, `retry_limit_exceeded` 0->1) where the **reference makes 1 and never retries its own protocol-error 502**. Cause: `localOrigin: true` makes `RetryPolicy.matches` (`retry.go:128`) classify a perfectly REACHABLE but malformed upstream as a CONNECT FAILURE. **D-92-POSTURE exists to kill this.**
3. ⚠️ **THE POSTURE FLAG IS THE WRONG KNOB AS THE BRAINSTORM FRAMED IT.** The **HCM (downstream)** flag has **NO effect** on the response direction; the controlling field is the **CLUSTER's** `http2_protocol_options`, and both spellings behave identically. **The downstream STATUS is posture-INVARIANT at 502 — only CONNECTION LIFETIME moves.**
4. ⚠️ **`0120`'s PORT `10120` IS NOT FREE**, refuting `phases/88-.../PLAN.md:303` and `phases/89-.../SPEC.md:222`. It is held by `test/fixtures/0028-http-lua-multi-script-and-per-route/inputs/driver.go:65` (`refLATestPort`), with `:66-70` holding 10121-10125. Census of `101xx` in `test/`: **10100-10125 and 10130-10140 taken; 10126-10129 FREE.**
5. ⚠️ **THE INHERITED `311/319` ADR HEADING COUNTS WERE STALE BY ONE** — they read **312/320** at the pre-edit tip (the phase-91 SPEC's own ADR-0313 moved them). The `+8 ## Amendment` relationship is INTACT. **They are 313/321 now.**
6. ⚠️ **THE `\t`-IN-ERE TRAP IS TOOL-DEPENDENT, AND TWO AGENTS CONTRADICTED EACH OTHER ON IT.** One measured `grep -cE '^\t_ "'` => **0**, another **123**, same file same tip. **Both correct**: the harness `grep` is a shell function wrapping **ugrep 7.8.4** (whose ERE DOES honour `\t`); `/usr/bin/grep` is **GNU grep 3.11** (which matches a literal `t`). ⇒ **the standing record's blanket "ERE reads 0" is true only of GNU grep. ANY gate restating it MUST NAME THE TOOL.** Safe under both: `-P`, or `$(printf '\t')`.
7. ⚠️ **THE RECORDED REMEDY FOR THE ` + ` SPLIT TRAP IS ITSELF INSUFFICIENT.** The trap reproduces (`awk split(s,a," + ")` reads **1 on all six** windows). But `sed 's/ + /\n/g' | wc -l` run as written reads `:202`7 `:208`10 `:214`12 `:224`92 `:230`8 `:238`19 = **148**. List-scoped and semantically read the total is **40**, but **NO SINGLE MECHANICAL COMMAND PRODUCES IT**: `:214` carries ` + ` INSIDE one candidate, `:224`'s list ends mid-cell, and **`:238` is COMMA-delimited** so a ` + ` splitter reads 1. ⇒ **no gate may rest on a ` + ` split in any form.**
8. **`IsIllegalH2RequestHeader` HAS NO FUZZ TARGET** — the opposite of the hypothesis put to the probe.
9. **`codec_type: HTTP2` appears ZERO times across the fixture corpus** (`HTTP1` × 267, `AUTO` × 5, `HTTP3` × 3). All four H2-capable fixtures are `AUTO` + ALPN-driven — confirming the phase-91 finding that the real discriminator is the downstream **ALPN `h2` offer**.

### ⚠️ CLOSED BY MEASUREMENT — do NOT re-probe these

- **THE CRUX IS CLOSED IN THE FIX SITE'S FAVOUR.** Instrumenting the `if !cs.respHeadersSeen` branch shows `name="X-Upper-Case"` arriving **INTACT** — `writeH2Reply`'s lowercasing is DOWNSTREAM of the guard, and nothing normalizes upstream of it. ⇒ **the fix site catches all seven arms.**
- **Duplicate `content-length` is FULLY VISIBLE at the fix site** — a slice preserving both fields in wire order, collapsed by nothing at or below it. **Arm 10 needs no second site.**
- **Trailer behaviour can be held invariant** — RUN sets **byte-identical by `diff`** across 655 RUN / 0 FAIL, across `-run 'Trailer'` (63 RUN / 19 top-level), and across the five named h2 trailer tests (34 RUN).
- **Cost:** two independent prototypes read **`+74 / -0`** and **`+77 / -0`**, ONE file, both `gofmt` clean and `golangci-lint` rc=0 zero output. The `+74` splits **code=44, comment=27, blank=3** — the banked `+44` matches the CODE-ONLY count EXACTLY, so **`+44` was a COMMENT-FREE lower bound, not an under-enumeration**. ⚠️ **This is the first row in 87-92 where `reference_measured_prototype_is_a_lower_bound` fires with an IDENTIFIED MECHANISM.**

### ⚠️ WHAT THE PLAN OWES — six items, §13 of the SPEC

1. ⚠️ **CLOSE THE OPEN ARM (SPEC §5.1): measure whether the reference rejects IDENTICAL duplicate `content-length` values.** The measured arm used DIFFERING values. The prototype rejects any second CL, including identical ones. **Do NOT ship the broad rule on an assumption of parity** — if the reference accepts identical duplicates the rule narrows.
2. **Price the THIRD `doH2ClusterAction` arm by a compiling prototype**, in `internal/filter/http/router/`. A SECOND package; neither cost figure includes it.
3. **Name the new counter and prove it passes `stats.IsValidName`**; pin its **DELTA**, never its absolute. ⚠️ Assert it **subject-side only** — cross-side stat scope diverges and the reference's spelling differs (`upstream_rq_tx_reset` vs `http2_tx_reset`). ⚠️ **A COUNTER CANNOT GATE A VALUE** — it pins that a rejection happened, never which field caused it.
4. **Write the SPEC §10 NC table as real tests and RECORD each NC's result.** ⚠️ The no-retry pin **needs `retry_on` CONFIGURED or it cannot fire under any input**; the eviction pin needs a **stacked control** because a passing arm cannot catch an OVER-firing evict; trailer invariance is a NON-regression whose "NC" is the **RUN-SET diff**, not a red run.
5. **Re-derive every count and cite at the PLAN's own tip.** Two inherited numbers were stale this stage and one trap was tool-dependent.
6. **Do not gate on a ` + ` split; name the grep tool in any `\t` gate.**

### ⚠️ HARNESS CONSTRAINTS THE IMPL INHERITS — each cost a probe to find

- ⚠️ **The leading-block reject writes RST_STREAM FROM THE readLoop GOROUTINE**, so a wire test MUST use **`dialClientConnTCP`, NOT `dialClientConn`**. A first probe over `net.Pipe` **DEADLOCKED and was killed at 120 s**. `trailers_validate_test.go:281-290` documents this for the trailer path; it applies identically here.
- ⚠️ **The reference's upstream pools are PER-WORKER / THREAD-LOCAL.** Two requests on two DOWNSTREAM conns landed on **2** upstream conns and invalidated a first reference run. **Both streams must ride ONE downstream connection.**
- ⚠️ **A backend handling streams SYNCHRONOUSLY blocks the read loop**, so the two streams are never concurrent — that produced a DEAD PROBE which was caught and discarded. **One goroutine per stream.**
- ⚠️ **ASSERT THE FIXTURE SET.** The runner `t.Skipf`s an unregistered fixture at **`test/differential/runner_test.go:200`** (exact), `DriverRegistry` is read at exactly ONE site (`:194`), and **no fixture-count gate exists anywhere in the tree** — an absence established WITH A POSITIVE CONTROL that DOES find real count gates elsewhere (`0070/driver_test.go:58`, `0004/driver.go:1061`, `internal/runtime/snapshot_test.go:85`). Invocation: `go test ./test/differential/ -run 'TestDifferential/0004-h2-routing' -count=1 -v`, confirming `=== RUN   TestDifferential/0004-h2-routing` appears.
- ⚠️ **Four of seven shapes are STRUCTURALLY unreachable from a `net/http` backend** and are pinned at the UNIT layer: `connection` (deleted at `x/net/http2/server.go:2759`), `transfer-encoding`, an uppercase wire name (canonicalization + `lowerHeader`), a duplicate `content-length` (collapsed). The three that DO leak (`keep-alive`, `upgrade`, `proxy-connection`) leak because `server.go:2757` carries a live `TODO: remove more Connection-specific header fields here`.

### ⚠️ THE ADR APPEND POINT — the SPEC wrote the footer the phase-90 IMPL had to add

`ADR-0314` ends with the **RETAINED italic footer** `*§Decision and §Consequences follow at the phase-92 IMPL.*`, written AT THE SPEC. The phase-90 IMPL discovered its ADR's own STATUS line named a footer that did not exist and had to add one before appending; **that defect is not repeated here.** The IMPL appends §Decision + §Consequences **IN PLACE, AFTER that footer**, with no renumber and no `---` (the ADR-0294-0314 shared block form), and **disarms the strict guard 1 -> 0 verified BY LINE AND ADR** — the ADR-0231 decoy at `:14866` reads 1 and must be **LEFT ARMED**.

### ⚠️ FIVE CANDIDATES STILL BANKED — costs measured, a taker re-derives nothing

1. ⚠️ **THE POOLED-UPSTREAM-LIFETIME DEFECT — THREE defects at THREE sites, REPRODUCED END TO END, MORE SEVERE THAN ROW 92. TAKE THIS NEXT.** D1 ctx ownership in `h2.NewClientConn` · D2 evict-on-own-cancel in `router_h2.go` hard-`Close()`ing a SHARED MULTIPLEXED conn · D3 `h2pool.go` `watchDrain`'s EMPTY `case <-cc.Done():` arm. On ONE provably shared pooled conn a sibling gets **502** where the control gets 200, in BOTH the `per_try_timeout` and `hedge_on_per_try_timeout` arms (the latter with REQ1 **SUCCEEDING at 200**). ⚠️ **D3 needs NO retry policy, NO hedging and NO unusual config: any upstream dropping conns without a GOAWAY WEDGES THE CLUSTER PERMANENTLY against its HARD `max_connections` cap with requests HANGING, not 503-ing** (`upstream_cx_active` pinned, `pending_overflow: 0`). Cost **`24 added / 12 removed`** over three files, lint-clean; **UNPINNED IN BOTH DIRECTIONS at all three sites** across 791 named tests, all 73 `./internal/...` packages and `-race`, with `panic()` reachability controls reddening at every one. ⚠️ **ITS REFERENCE SIDE IS A PREDICTION, NOT A MEASUREMENT** — a taker MUST measure it first; it spans three packages and is a probable SPLIT-phase; its H1 sibling is entirely unmeasured. ⚠️ **PHASE 92 MEASURED PART OF ITS TERRITORY AND THE RESULT CUTS BOTH WAYS:** the reference DOES tear down the upstream conn and kill in-flight siblings on a malformed response at default posture, so *"hard-closing a shared conn is FALSE for every sibling stream"* is **right about the harm and wrong as a divergence claim**. A taker must re-frame D2 around the **missing opt-out**, not around the teardown itself.
2. **HTTP/1.1 with no `Host`** — subject **200 forwarding a literal empty `Host: `**, reference **400 `missing_host_header`**; divergence set is exactly ONE arm. Fix priced **`+90 / -0`, ONE file, ZERO new imports**, package green at RUN=322; pin half is **`+7`** and is a PROVEN GUARD. The smuggling hazard was DISCHARGED by measurement (v2 peeking to the blank line, CRLF **and** bare-LF, held across six adversarial arms). ⚠️ `missing_host_header` is **invisible at `--log-level info`** — it needs `%RESPONSE_CODE_DETAILS%`, which **phase 92 measured as NON-EXISTENT in envoy-go** (`log_format` is BOOT-REJECTED), so **this candidate's observability half is now known to be blocked**.
3. **The `content-length` rewrite — THREE-LEGGED.** `writeH2Reply` rewrites `content-length` to `len(body)` **method-blind and status-blind**: a `304` with CL 42 ships 0; a bodyless 200 with CL 5 (the HEAD shape) ships 0; a mismatched 999 over a 5-byte body is laundered where the reference forwards 999 then RST_STREAMs. Deletion prototype is `0 3` and does NOT reach parity. ⚠️ **The identical rewrite exists on ALL THREE codecs** (`codec.go` H1, `h2dispatch.go` H2, `h3dispatch.go` H3). ⚠️ **Phase 92 confirmed the H2 site at `h2dispatch.go:1014-1016` and that it rewrites EVERY CL**, which is why arm 10 leaves two IDENTICAL CLs on the subject wire.
4. **1xx interim responses** — subject **RST_STREAM(INTERNAL_ERROR)**, reference **delivers the 200**. ⚠️ The in-code parity target is **REFUTED — the reference SWALLOWS 1xx**, it does not forward. Design against *drop-and-deliver*. ⚠️ **Phase 92 corrects the wrong comment in passing (D-92-1XX), so a taker inherits accurate prose.**
5. **`ssl.connection_error` — the CHEAPEST IN-WINDOW candidate.** The only strong candidate **inside a live sentinel window** (`:224`, item 2 of 3) with the reference side already measured. `connection_error` occurs in `*.go` **ONLY inside comments**; `switch classifyHandshakeErr(err)` in `serveConnection` has exactly **TWO** cases and **no `case outcomeOther:`**, so five classified failure shapes increment nothing. Under 20 production lines, and **it would NARROW a sentinel window at row-done** — which no maintenance row can do.

**NEW candidates banked by this SPEC:** the missing **cluster-level `stream_error_on_invalid_http_messaging` posture** (envoy-go parses neither spelling; the reference's opt-out is unreachable) · **`%RESPONSE_CODE_DETAILS%`** as a multi-row surface (access-log format parsing + a plumbed field + operator tables) · **BackendKind 39**, a raw-framer illegal-response responder (≈ +303 runner lines) that would put the four unreachable shapes on the wire.

## ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + ALL FOUR NCs YOURSELF** and record ACTUAL output. ⚠️ **Check (1) PRINTS `NOT DONE: row 92` and NC-A reads TWO lines. Neither shape carries to an all-`done` board.**
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** The 91 SPEC refuted seven claims; the 91 PLAN ten; the 91 IMPL four; the 92 BRAINSTORM five; **this SPEC refuted nine — INCLUDING ITS OWN controller-side hypothesis, which it states FIRST rather than burying.** Do the same.
3. **LINE ANCHORS DRIFT; SYMBOL ANCHORS DO NOT — RE-VERIFY EVERY CITE AT YOUR TIP.** ⚠️ **PATHSPEC-SCOPE every symbol assertion.** ⚠️ **THE RECEIVER NAME IS PART OF THE ANCHOR.** ⚠️ **A SYMBOL ASSERTION WHOSE RECEIVER IS WRITTEN `(cc *ClientConn)` MUST USE `grep -F`, NOT `-E`** — ERE reads the parentheses as a group and returns a **FAIL-UNSAFE ZERO** (re-reproduced this stage: `-F` reads 1, `-E` reads 0).
3b. ⚠️ **CHARACTER-CLASS AND ESCAPE SELECTORS ARE THE FAIL-UNSAFE FAMILY.** (i) `grep -cE '^\s+[a-z0-9./-]+ v[0-9]'` on `go.mod` reads **62**, not **67** — it drops the five modules whose path carries an UPPERCASE letter or an underscore (`AdaLogics/go-fuzz-headers`, `Azure/go-ansiterm`, `Microsoft/go-winio`, `Microsoft/hcsshim`, `prometheus/client_model`). (ii) `grep -oE '[A-Za-z]+ +BackendKind = 38'` prints `GoawayResponder`, not **`H2GoawayResponder`** — a **digit-blind** class truncating an identifier. (iii) **an over-anchored `^[0-9]{4}-` drops the two letter-suffixed fixture dirs** (`0007a-cors`, `0007b-iteration-probe`): use `ls -d test/fixtures/*/ | wc -l`. (iv) ⚠️ **`\t` in `-E` is TOOL-DEPENDENT — see §6 above. NAME THE TOOL.**
3c. ⚠️ **`feedback_brief_citations_not_evidence` APPLIES TO INCIDENTAL FIGURES.** This stage: an inherited `311/319` was stale, an inherited "10120 is free" was wrong, and **two agents returned contradictory results for the same command** — reconciled only by identifying the TOOL. **Re-derive EVERY number you restate, and when two agents disagree, find the variable rather than picking a winner.**
3d. ⚠️ **NO GATE MAY REST ON A ` + ` SPLIT.** Both the trap AND its recorded remedy are fail-unsafe — see §7 above.
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. ⚠️ **TWO SDS dial-budget flakes are live** — `TestSDSEndToEnd_FetchFailure_BootFailsClosed` (`internal/boot`) and `TestProvider_FetchInitialCertificate_Timeout` (`internal/xds`). ⚠️ **DO NOT reuse the `go list -deps` exclusion blanket — TRUE for `xds` (0), FALSE for `boot` (1).** Also live: the driver-owned receiver port race · `internal/cluster` outlier · `internal/httpclient` zero-value · the two 84.2-era flakes · the REFERENCE h2spec section-8 flip. ⚠️ **`TestServerConn_TinyWindowDelivery` IS NOT A FLAKE — phase 91 FIXED a live production deadlock. A recurrence is a REGRESSION of row 91.**
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only). Foreign containers left untouched throughout phases 90, 91 and 92: `infallible_booth`, `crazy_kare`, `golink-ai`, `quizzical_goldstine`.
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`.
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL.** ⚠️ At the phase-91 IMPL even that was not enough: an ablation was confirmed in the COMPILED BINARY BY DISASSEMBLY before its non-reddening gate result was believed. **`gofmt -l` never exits non-zero — gate on OUTPUT.** ⚠️ **`golangci-lint` must be gated on OUTPUT too.** ⚠️ **`go test` WITHOUT `-v` PRINTS ZERO `=== RUN`** — `RUN=0` beside `RC=0` is a VACUOUS GREEN. ⚠️ **A `-run` selector matching nothing prints `[no tests to run]` and EXITS 0.** ⚠️ **On `-v` output, unanchored `grep -c 'FAIL'` reads 11 ON A FULLY GREEN TREE** — use `grep -cE '^(FAIL|--- FAIL)|^ *--- FAIL'`.
7b. ⚠️ **`grep -c` ON ZERO MATCHES EXITS 1 — capture the value (`v=$(… || true)`), NEVER chain on it with `&&`.**
7c. ⚠️ **A GREEN TEST RUN IS NOT EVIDENCE A SITE IS UNPINNED — IT MAY BE DEAD CODE.** Prove it with a **`panic()` REACHABILITY CONTROL** before concluding "nothing pins this".
7d. ⚠️ **NC EVERY NEW TEST, NOT JUST THE RISKY-LOOKING ONES.** At the phase-91 IMPL **three of the row's own new pins passed while being non-discriminating** — one structurally unable to fire, one vacuous, one a coin flip. **A test that passes is not thereby a guard.**
8. ⚠️ **`pgrep -f` / `pkill -f` MATCHES YOUR OWN SHELL AND KILLS THE TOOL CALL (exit 144)**. **Kill ONLY PIDs you captured yourself** (from `$!`), and verify residue with `kill -0`.
9. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-*`, `wt-*` or stage worktree should outlive its stage close** — expect only `master` (verify with `git worktree list`).
10. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** (a)/(b) differential **121 fixtures** + full `go test ./...`, gated on **`PIPESTATUS[0]`** and a **SET RECONCILIATION** (⚠️ **NOT `INNER_EXIT`, which DOES NOT EXIST**); (c) h2spec cite ONLY from your own run — ⚠️ **h2spec is MEASURED BLIND to burst-drain ordering**; grpc-conformance deferred in writing; proxy-wasm 10/16; (d) fuzzers **55/48** today, **56/49** at row-done; (e) the ANCHORED panic gate `^panic:|DATA RACE|SIGSEGV` on every differential launch; (f) no REVIEW.md — standing departure.
11. ⚠️ **THE FIXTURE INVOCATION: `go test ./test/differential/ -run 'TestDifferential/0004-h2-routing' -count=1 -v`.** ⚠️ **Gate 2's failure mode is a SILENT PASS.** **ASSERT THE FIXTURE SET.**
12. ⚠️ **`-p` PUBLISHING WORKS; `--network host` IS WHAT DOES NOT.** **Host-gateway is `192.168.65.2`** (⚠️ `getent hosts` resolves it IPv6-ONLY — use `getent ahostsv4`). ⚠️ A **STRICT_DNS** cluster whose name does not resolve **blocks listener init entirely**. ⚠️ **`clusters: []` is BOOT-REJECTED even when every route is a `direct_response`.** ⚠️ **envoy-go REJECTS `match.headers` outright**, ⚠️ **refuses `codec_type: HTTP2` without TLS unless `--allow-h2c`**, and ⚠️ **boots with `-c`, NOT `--config-path`.** ⚠️ **`ENVOY_TARGET.md` is at `docs/envoy-go/ENVOY_TARGET.md`, NOT the repo root.** ⚠️ **NEW: `access_log[].log_format` is BOOT-REJECTED** — you cannot request `%RESPONSE_CODE_DETAILS%` on the subject at all.
13. ⚠️ **A RAW-FRAMER PEER MUST HOLD ONE `hpack.Decoder` PER CONNECTION.** ⚠️ **Set `ReadMetaHeaders` to NIL when probing malformed shapes**, or `x/net` validates away the very thing you are measuring. ⚠️ **`0004` is TLS+ALPN-h2, NOT h2c.**
14. ⚠️ **`bash /dev/tcp` + `timeout cat <&3` INSIDE COMMAND SUBSTITUTION RETURNS SILENTLY EMPTY.** **Do not build an H1 probe on `/dev/tcp`.**
15. ⚠️ **GO DOES NOT FLAG UNUSED CONSTANTS.** Unused-symbol cleanup after a removal must be grep-driven, and the removed-literal NC must target the USE SITE, never the declaration.
16. ⚠️ **`golangci-lint`'s misspell runs in locale US.** **Sweep British spellings in `.go` comments BEFORE the gate.** ⚠️ **Markdown prose may use them freely; a `.go` comment may not.**
17. ⚠️ **A GREEN RUN OF A CONTENTION-SENSITIVE TEST PROVES NOTHING.** Phase 91's subject cleared 5/5 uncontended while failing 9/32 contended on the SAME binary.
18. ⚠️ **AN ABSENCE CLAIM NEEDS A POSITIVE CONTROL AND A DISCRIMINATING NEGATIVE CONTROL.** This stage's "no fixture-count gate exists" is credible ONLY because the same search form found real count gates elsewhere. **Build the arm that would have shown the opposite.**
19. ⚠️ **DERIVE next-free ADR FROM THE TAIL, NEVER FROM THE HEADING COUNT.** The id space is SPARSE (one gap, `0209`) and **headings+1 now reads 314 — the TAIL ITSELF, a TAKEN id** — so that form would try to write ADR-0314 a SECOND time. ⚠️ **And the strict `PROPOSED` guard's UNANCHORED form OVER-counts** (`grep -F` read **2** at the pre-arm tip, both matches PROSE ABOUT the guard); the anchor is load-bearing and inflates each time an ADR documents the form.

## ⚠️ PORT BANDING

`net.ipv4.ip_local_port_range = 32768 60999`. ⇒ **BAND YOUR PROBE PORTS BELOW 32768.** `21000-24999` is clear of both the ephemeral range and every reserved band. **Check availability with `ss -tan` (ALL states), never `ss -ltn`** — the listener-only form reads a fail-unsafe "free". **A `bind: address already in use` in a 4xxxx band is NOT evidence a sibling session is squatting.** The static fixture band (10000-19172) and the reserved bands (20000-31007, 11000-14999) are all BELOW 32768. ⚠️ **FIXTURE ports are FAMILY-BANDED as `10<index>` — but `10120` is TAKEN (fixture `0028`); free in-band is `10126-10129`.**

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (**ugrep 7.8.4** under the hood). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Reproduced at FORTY separate tips.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **THE METHOD BINDS; carry no standing discrepancy figure and no standing absolute.** ⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS; print FILE NAMES, never a count.** ⚠️ **THE HARNESS `grep` STRIPS A LEADING `./` FROM PATHS.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files; use `-v --no-index`, and `git status` is the discriminating check). ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **`grep -c` COUNTS *LINES*, NOT *OCCURRENCES***. ⚠️ **AND THE TWO GREPS DIFFER ON `\t` IN ERE — see METHOD NOTE 3b(iv).**

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 92 (h2-response-header-validation)' --format='%h %s'   # 3 at this tip — BRAINSTORM + its CORRECTION + the SPEC
git log --grep '^phase 91 (h2-framer-partial-frame-desync)' --format='%h %s'  # 4 — a CLOSED phase, use as a FIRING CONTROL
```
⚠️ **The count comments are LIVE FIGURES — VERIFY before relying.** Take the NEWEST line (`| head -1`). A fabricated slug must read **0**.

Execution style: **subagent-driven per `feedback_execution_style`**. Subagents commit LOCALLY on the ONE stage branch with EXPLICIT PATHSPECS; the controller squashes (`feedback_subagents_no_push`, `feedback_push_to_origin`). **Pure MEASUREMENT agents should commit NOTHING and prove their tree clean.** ⚠️ **PARALLEL AGENTS IN ONE WORKTREE WORK IF AND ONLY IF THEIR PATHSPECS ARE DISJOINT** — ⚠️ **the phase-90/91/92 pattern was PER-AGENT DETACHED WORKTREES pinned at a known commit**, which let four independent probes run without any of them seeing another's edits. ⚠️ **A CONTROLLER WRITING INTO A SHARED WORKTREE WHILE AGENTS RUN WILL SHOW UP IN THEIR `git status` GATE.** ⚠️ **GIVE EACH PARALLEL AGENT ITS OWN PRIVATE SCRATCH DIR.** ⚠️ **ENUMERATE LIVE AGENTS BEFORE TEARING DOWN ANY WORKTREE THEY WERE GIVEN** — the phase-92 BRAINSTORM removed a worktree while its probe was still running and got away with it by luck. Per-task `gofmt` + `golangci-lint` on touched packages, **gated on OUTPUT**. ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root** — build with `-o` into scratch. ⚠️ **AND VERIFY WHAT A SUBAGENT REPORTS** — this stage had two agents contradict each other and the controller had to identify the TOOL to reconcile them.

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root — **observed repeatedly at every phase-88 through -92 stage, including twice in the first ten commands of this one**. Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` before any commit or gate run. ⚠️ **Shell VARIABLES do not survive between Bash calls** — re-export in every command.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — `git status --porcelain` AND `git diff --stat master` must show ONLY your intended edits. For a patched TRACKED file: `sha256sum` BEFORE patching, `git checkout -- <path>` after, `sha256sum -c` the capture — ⚠️ **and COMMIT FIRST, because `git checkout --` restores from HEAD and will wipe uncommitted work.** ⚠️ **All four phase-92 SPEC probe agents did this and every restore verified `OK`.** ⚠️ **Also check the MAIN repo root.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads the phase-92 PLAN.** ⚠️ **§Project counts is STALE (phase-76 era: fixtures 119, ADR tail 0298) and SELF-CONTRADICTS §Current with NO label saying so — do not anchor on it.**
2. `docs/envoy-go/phases/92-h2-response-header-validation/SPEC.md` — **especially §4 (D-92-POSTURE and why the disposition is THREE-way), §5.1 (the OPEN ARM you must close), §7 (the differential decision and its departure), §10 (the NC table) and §13 (what you owe)**.
3. `docs/envoy-go/phases/92-h2-response-header-validation/BRAINSTORM.md` — §2 (the seven-shape reproduction) and §4 (the banked candidates with costs).
4. `docs/envoy-go/DECISIONS.md` **ADR-0314** (PROPOSED, §Context only) — ¶4-¶7 carry the load-bearing findings. Also **ADR-0312 §Consequences (xix)**, this row's provenance, and **ADR-0313 §Consequences (ix-bis)** on non-discriminating pins.
5. `BOOTSTRAP_PROMPT.md` §4-§7 (repo ROOT). **Phase-92 dir has `BRAINSTORM.md` + `SPEC.md` and no `PLAN.md` ⇒ state 2 ⇒ the PLAN.** ⚠️ **GATE AT STATE 2: split into `92.1, 92.2, …` if the PLAN exceeds ~25 tasks or ~1500 LoC.**

## What the next session owes
**RUN THE PHASE-92 PLAN** (state 2 -> 3), producing `PLAN.md` in the existing phase dir ·
**RE-RUN the sentinel + all four NCs yourself** and record ACTUAL output (⚠️ **check (1) prints `NOT DONE: row 92`; NC-A and NC-B are BOTH two-line results**) ·
**DO NOT create `stop`** — two checks block it · **DO NOT self-pick** — row 92 is banked mid-lifecycle work ·
**A PLAN ADDS NO ADR** — tail STAYS **ADR-0314**, next-free STAYS **ADR-0315**, the strict `PROPOSED` guard **STAYS ARMED at 1** (verify BY LINE AND ADR; the ADR-0231 decoy also reads 1), `^---$` STAYS **216** ·
**ROW 92 STAYS `in-progress`** — `ROADMAP.md` should be BYTE-UNTOUCHED at the PLAN; `want` STAYS **124** ·
**CLOSE THE OPEN ARM FIRST** (SPEC §5.1 — identical duplicate `content-length` against the reference) and **PRICE THE THIRD ROUTER ARM BY COMPILING PROTOTYPE** ·
**REFUTE YOUR PREDECESSOR BY EXECUTION** — this SPEC refuted nine claims including its own hypothesis; the 91 PLAN refuted ten ·
`STATE.md` rolled IN PLACE with the oldest §Recent entry evicted by a **DIRECT DATE READ** (⚠️ **read the dates yourself every time — this stage found a TWO-WAY tie at 2026-08-22 broken by LAST LIST POSITION, the FOURTH distinct tie shape in four stages; the shape does NOT carry**) and archived to `STATE_HISTORY.md` as **ONE INLINE LINE** whose label does NOT match `^- \*\*prior active-phase:\*\*`, so the strict guard stays at **163, DELTA 0** ·
`next-prompt.txt` rolled (`git add -f`) · ONE squashed commit, merged and pushed — ⚠️ **and if a late measurement invalidates something you already pushed, land a CLEARLY-LABELLED correction commit rather than force-pushing or leaving it standing.**
