START a fresh session for envoy-go. **The phase-42 (`retries + hedging`) BRAINSTORM charter is WRITTEN, reviewer-APPROVED, and LANDED on master (squash `1aba3956`) — the subject is CHOSEN, ALL forks are SETTLED, and the durable `docs/envoy-go/phases/42-retries-and-hedging/BRAINSTORM.md` exists.** The next step is to WRITE the **phase-42.1 SPEC** (`superpowers:brainstorming` is COMPLETE for this row; the lifecycle valve is BRAINSTORM → SPEC). ROADMAP row 42 (`retries-and-hedging | 41 | in-progress`) is registered with the pre-authorized 42.1/42.2 by-feature split; STATE active-phase is `phase 42 (retries-and-hedging) BRAINSTORM done`. Confirm the tip is UNCHANGED at `1aba3956` via `git log --oneline -3`. Counts at tip (a pre-SPEC state — ALL stay UNCHANGED at the docs-only SPEC commit): stat surface **1163** / fixtures **76** / fuzzers **42** / BackendKind tail **36** (`BlockingHoldResponder`) / DECISIONS tail **ADR-0248**, next-free **ADR-0249**.

## What the BRAINSTORM already SETTLED for 42.1 (do NOT re-litigate — read the charter, then SPEC it)

**Subject + split:** phase 42 = `retries + hedging`, the FOURTH Upstream-robustness-family row, with a **pre-authorized 42.1/42.2 by-feature split**. **42.1 = the retry loop** (this SPEC): `RouteAction.retry_policy` (`route.v3.RetryPolicy`, field 9; `VirtualHost.retry_policy` field-16 fallback) — `retry_on` + `num_retries` + `retry_back_off` + the phase-41 `retry_budget` ACTIVATION. **42.2 = hedging** (a LATER leg): `HedgePolicy` (RouteAction field 27) + the project's FIRST `per_try_timeout` (RetryPolicy field 3). `request_mirror_policies` (field 30) DEFERRED. Row 42 flips `done` only when BOTH legs land (ADR-0106 + `reference_roadmap_split_phase_row_done`).

**42.1 architecture (ADR-0249; possibly +ADR-0250 for the retry_budget model — anticipate 1–2):** a `retryExecutor` WRAPPING the EXISTING single-attempt `doH1ClusterAction` (`router.go:588`) / `doH2ClusterAction` (`router_h2.go:57`) at their call sites (the H1/H2 main dispatch `router.go:566` / `router_h2.go:42` + the weighted-cluster dispatch `router_weighted.go:110/:123`). The wrapped call ALREADY buffers the full response into `ActionResponse{Status,Headers,Body,Close}` (`router.go:119`) → the loop classifies `Status` against a parsed `retry_on` bitset; on a retriable outcome with attempts + budget remaining it sleeps the exponential backoff, re-picks via the LB, and replays the ALREADY-buffered request body (fresh `bytes.Reader`; body buffered at `connection.go:548-600` under the 1 MiB `FilterBufferLimitBytes` cap, ADR-0076; over-cap ⇒ non-retriable, a SPEC pin). Each attempt re-runs circuit-breaker admission (`TryAcquireRequest`) + `RecordUpstreamResult` (so BOTH circuit breaking and outlier detection see EVERY attempt — these seams already exist on the driver). A `retryPolicy` struct carried on `clusterRouteAction` (`hcm/actions.go:201`), parsed in `buildRouterAction` (`hcm/config.go:536`). **Byte-identical when no `retry_policy`** (nil-guard). ZERO new package (retry executor in the router pkg; parse in hcm; budget activation in `internal/cluster`).

**retry_budget activation:** the phase-41 `circuitBreaker` gains a cluster-level atomic active-retry counter; a retry try-acquires a slot (`max(min_retry_concurrency, budget_percent% × active requests)`) before each attempt; on exhaustion ⇒ no retry + `upstream_rq_retry_overflow`++ + `rq_retry_open`=1 — the phase-41 EMIT-0 stats (`circuitbreaker.go:78,83`) flip LIVE. Static `num_retries` caps independently ⇒ `upstream_rq_retry_limit_exceeded` on exhaustion.

**Backoff:** exponential (`retry_back_off` base/max interval, reference default ~25ms/250ms, full jitter). Delay-only ⇒ never perturbs the count-based differential.

**retry_on subset:** MVP-enforce the connection/5xx-class tokens (`5xx`, `gateway-error`, `connect-failure`, `reset`, `retriable-status-codes` + `retriable_status_codes[]`); parse-accept the rest (`retriable-headers`, `envoy-ratelimited`, the gRPC tokens). `retry_on` is freeform (NO PGV) ⇒ thin reject surface.

**Response flags:** the `RX`/`URX` retry access-log flags are a RECORDED DEPARTURE (envoy-go has NO response-flags plumbing — the phase-41 CB4 precedent). The differential asserts stats + final status, NEVER the access-log line.

**Differential:** `0075-retry-…` REUSES the per-host-503 topology (`HTTP503Responder`, BackendKind 35, `PerHostBackendKind`) under deterministic round-robin so the first pick hits the 503 host ⇒ retry re-picks a healthy host ⇒ final 200 (cross-side final 200 + `upstream_rq_retry` delta + `upstream_rq_total`); a second fully-deterministic EXHAUSTION arm (single 503 host, `num_retries=N` ⇒ final 503 + `upstream_rq_retry==N` + `upstream_rq_retry_limit_exceeded==1`); one-vs-two dirs a SPEC pin. Sleepless/count-based; RR-determinism cross-side a SPEC live-pin. NO new BackendKind.

**Count envelope at the 42.1 IMPL** (from tip 1163 / 76 / 42 / 36 / ADR-0248): stat surface **1163 → ~1168** (the phase-41 `rq_retry_open` + `upstream_rq_retry_overflow` flip LIVE [no count delta] + ~+4–6 new retry counters: `upstream_rq_retry` / `_retry_success` / `_retry_limit_exceeded` / `_retry_backoff_exponential` / `_retry_backoff_ratelimited` — exact roster a SPEC pin D-RT-STATS) · fixtures **76 → 77 (or 78)** · fuzzers **42 (or 43** — candidate `retry_policy` config-parse fuzzer**)** · BackendKind **36 UNCHANGED** · DECISIONS tail **ADR-0248 → ADR-0249 (or 0250)** · **ZERO** new packages / go.mod modules.

## NEXT STEP — write the phase-42.1 SPEC

Author `docs/envoy-go/phases/42-retries-and-hedging/SPEC.md` (the 42.1 leg) in the phase-41 SPEC shape (`docs/envoy-go/phases/41-circuit-breakers/SPEC.md` is the section-for-section template). EXECUTE the BRAINSTORM §10 empirical pins **D-RT-PROTO / D-RT-RETRYON / D-RT-STATS / D-RT-BUDGET / D-RT-DIFFERENTIAL** IN-SESSION (parallel-subagent fan-out OK) live against `envoyproxy/envoy:contrib-v1.37.2` (ADR-0227) per `reference_docker_probe_bridge_network` (shared Docker bridge + STRICT_DNS backend hostname; verify the retry path ran — `upstream_rq_retry > 0`): pin the exact `route.v3.RetryPolicy`/`RetryPolicy_RetryBackOff` field roster + defaults + PGV + `go mod tidy -diff` EMPTY (D-RT-PROTO); the enforced `retry_on` token→`ActionResponse.Status` classification mapping + the reject arms + fuzzer-warranted? (D-RT-RETRYON); the `upstream_rq_retry*` counter names + counter→event mapping from a live `/stats` scrape of a retrying cluster (D-RT-STATS); the `retry_budget` formula + defaults `20%/3` + the overflow-vs-limit-exceeded discriminator (D-RT-BUDGET); the fixture split + RR-determinism cross-side guarantee + the cross-side assertions, sleepless (D-RT-DIFFERENTIAL). Anchor the **ADR-0249 §Context** draft from the pins. Apply any human-confirmed scope narrowing as an AMEND-RT note (the phase-41 AMEND-CB1 precedent — e.g. if a live probe narrows the enforced `retry_on` set). Run the FINAL ADR-0045 split-gate re-check (the 42.1/42.2 split is pre-authorized; re-confirm 42.1 stands alone). Then dispatch the spec-document-reviewer, fix to approval, and commit docs-only. ALL counts stay UNCHANGED at the SPEC commit. The PLAN (42.1) follows in a later session.

## Honor throughout

- **Lifecycle = one stage per session.** `feedback_git_worktrees` (isolated work in a worktree) · `feedback_subagents_no_push` (task subagents commit locally only; the controller squashes + pushes at stage-close) · `feedback_push_to_origin` (after a clean local commit on master with tests green, push without asking) · `feedback_execution_style` (subagent-driven over inline).
- **ADR/process:** ADR-0044 (§Context at the SPEC, §Decision/§Consequences IN-PLACE at the IMPL) · ADR-0045 (the split-gate) · ADR-0052 (the six-gate at the IMPL) · ADR-0080 (envoy-go's own byte-stable rejects) · ADR-0106 + `reference_roadmap_split_phase_row_done` (a split-phase row flips `done` only when ALL legs land; NO parent rollup) · ADR-0227 (`contrib-v1.37.2`) · ADR-0076 (the 1 MiB body-buffer cap — the retry-replay source) · ADR-0245 (the `RecordUpstreamResult` seam — re-run per attempt) · ADR-0248 (the phase-41 `circuitBreaker` + the dormant `retry_budget` slot 42.1 activates).
- **At the SPEC:** `reference_docker_probe_bridge_network` (any live reference-Envoy probe over a shared Docker bridge + STRICT_DNS backend hostname; verify the request/retry path ran) · `reference_proto_roster_extraction_digits` (digit-inclusive regexes when scraping the `upstream_rq_retry*` stat roster + the proto field roster) · `reference_differential_run_selector` + `reference_differential_break_protocol_count1` (the `0075` subtest is `TestDifferential/<NNNN>`; deliberate breaks use `-count=1`) · `reference_differential_band_sigma_margin` (count-based, never a timing-margin assertion).

## Read first (in order)

1. THIS FILE — the cold-start router (phase-42 BRAINSTORM DONE + reviewer-APPROVED + LANDED; the next step is to WRITE the 42.1 SPEC).
2. `docs/envoy-go/phases/42-retries-and-hedging/BRAINSTORM.md` — the durable charter; the §10 D-RT-* pins this SPEC executes; the settled forks.
3. `docs/envoy-go/STATE.md` — active-phase `phase 42 (retries-and-hedging) BRAINSTORM done`; the count figures (1163 / 76 / 42 / 36 / ADR-0248, next-free ADR-0249).
4. `docs/envoy-go/ROADMAP.md` — row 42 `in-progress` (the pre-authorized split) + the §Upstream-robustness family heading (active-HC / outlier / circuit-breakers DONE; retries+hedging in-flight; 1 candidate remains after done).
5. `docs/envoy-go/phases/41-circuit-breakers/{SPEC,PLAN,PROGRESS,README}.md` (the durable phase-41 record) — for the precedent shape of the phase-42.1 SPEC/PLAN/IMPL.
6. `docs/envoy-go/DECISIONS.md` (the ADR-0248 circuit-breaker entry — the `retry_budget` parse + emit-0 stats 42.1 activates; the process ADRs; next-free ADR-0249) + `docs/envoy-go/BEHAVIOR_CONTRACT.md`.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`.
