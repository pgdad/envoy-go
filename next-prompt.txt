=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, the active phase is fully landed, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action. (The sentinel does NOT apply right now — phase 51 is chartered, its BRAINSTORM is LANDED, and the next stage is its SPEC.)

**STATUS AS OF 2026-07-05: phase 51 (`bootstrap-validate-mode`) BRAINSTORM is LANDED; the NEXT stage is its SPEC.**

- The phase-51 BRAINSTORM landed: `docs/envoy-go/phases/51-bootstrap-validate-mode/BRAINSTORM.md` (the 12-section shape, adapted for a non-differential, non-wire-protocol feature — sections 5/6/9/10 note explicitly where they don't apply the same way a wire-protocol phase's would). Row 51 registered `in-progress` at this BRAINSTORM commit (the ROADMAP §Schema invariant), opening a NEW "Operational tooling family" heading in `docs/envoy-go/ROADMAP.md` (the FIRST new family since the Observability family opened at phase 44). STATE.md active-phase header rolled to `phase 51 (bootstrap-validate-mode) BRAINSTORM done`. **Re-verify with `git log --oneline -6` + `git status`; do NOT assume.**
- **The feature, in one paragraph:** bootstrap config validation reachable from OUTSIDE the binary's normal boot path — a Go equivalent of upstream Envoy's `--mode validate`. A NEW public (non-`internal/`) package `github.com/esalaine/envoy-go/validate` exposing `Bootstrap(io.Reader, baseDir string, allowH2C bool) error` + `BootstrapFile(path string) error`, plus a `--mode validate` CLI flag on `cmd/envoy-go/main.go`. Motivated by Kubernetes Gateway API implementations (e.g. Envoy Gateway) needing to validate envoy-go-generated bootstrap config before applying it to a live proxy, without needing a full running instance.
- **The four resolved user-dialogue questions (do NOT re-litigate; the SPEC/PLAN/IMPL EXECUTE these):**
  - **Q1 — validation depth → FULL CONSTRUCTION.** Run `bootstrap.Load` + `cluster.NewManagerWithBaseDir` + full listener-manager construction (filter chains, TLS certs, Lua compilation, HCM route tables) — stop BEFORE `admin.New`/`admSrv.Start`/`lm.Start`/any `net.Listen`. This exploits an ALREADY-LANDED seam: `listener.NewManagerWithBaseDirAndAllowH2C` fully constructs every filter chain without binding a socket; the actual binds happen later in a SEPARATE `lm.Start(ctx)` call. Rejected a cheaper parse-only alternative (wrapping `bootstrap.Load` alone) since it would miss the error class Gateway API controllers actually care about (bad TLS cert refs, malformed filter wiring, broken Lua scripts, bad routes).
  - **Q2 — sink side effects → SKIP BOTH.** Access-log file opens (`accesslog.NewAsyncFileSink`) and stats-sink UDP dials (`statssink.NewStatsdSink`/`NewDogStatsdSink`) are excluded entirely from the validate path — pass an EMPTY `[]accesslog.Sink` (confirmed nil-tolerant per `internal/filter/network/builtins.Deps`'s own doc comment) and skip stats-sink construction altogether. Neither has dry-run diagnostic value (UDP dial never proves reachability; file open is an unwanted side effect of a "just checking" operation).
  - **Q3 — public API shape → single fail-fast error.** `validate.Bootstrap`/`BootstrapFile` return a single `error` (first-bad-thing-wins), matching EVERY existing internal validation path in this project (`internal/bootstrap`, `internal/cluster`, `internal/listener` are all fail-fast today). Rejected a multi-diagnostic/structured-errors shape — would require restructuring long-landed internal validation this phase has no reason to touch.
  - **Q4 — CLI integration → `--mode validate`.** Mirrors upstream Envoy's own flag verbatim. `envoy-go -c bootstrap.yaml --mode validate` exits 0 ("configuration OK") or 1 (error to stderr) instead of booting. Absent/empty `--mode` is BYTE-IDENTICAL to today's behavior — purely additive, not a breaking CLI change.
- Counts at BRAINSTORM (re-verify, do NOT assume): stat surface **1200** (non-H2 **1196**) / fixtures **96** / fuzzers **52** / BackendKind **38** / DECISIONS **ADR-0267** (next-free **ADR-0268**). This phase is anticipated to leave ALL of these UNCHANGED at IMPL (+0 everywhere except THREE new Go packages) — it has NO differential surface (no wire behavior to compare against a live reference Envoy) and registers no new stat.

=> The NEXT action is the **phase 51 (`bootstrap-validate-mode`) SPEC** (lifecycle-state 1 → 2). NOT the termination sentinel. NOT a new PLAN or IMPL.

==============================================

## The NEXT action — the phase 51 (`bootstrap-validate-mode`) SPEC

Invoke `superpowers:brainstorming`'s natural successor for this stage: write `docs/envoy-go/phases/51-bootstrap-validate-mode/SPEC-51.md`, resolving the BRAINSTORM's §10 D-VALIDATE-* open questions by READING THE CURRENT CODE FRESH (not trusting the BRAINSTORM's line-number citations without re-verifying against HEAD — they drift). This is a **docs-only, direct-on-master** stage (the phase-44 through 50 SPEC-stage precedent — no worktree needed until IMPL).

**The §10 D-VALIDATE-* questions this SPEC must resolve (re-read `docs/envoy-go/phases/51-bootstrap-validate-mode/BRAINSTORM.md` §10 for full context before starting):**
- **D-VALIDATE-CONSTRUCT-BOUNDARY (LOAD-BEARING):** trace `cmd/envoy-go/main.go`'s EXACT current construction sequence line-by-line, fresh against HEAD (line numbers WILL have drifted since the BRAINSTORM — re-verify, do not trust the BRAINSTORM's citations). Confirm the "everything up to but not including `admin.New`/`lm.Start`" boundary is genuinely clean — i.e. that NOTHING between `bootstrap.Load` and `listener.NewManagerWithBaseDirAndAllowH2C`'s return binds a socket, dials a real (non-UDP-connectionless) connection, or starts a goroutine, beyond the two already-identified sink-construction side effects (access-log file opens, stats-sink UDP dials). If a THIRD hidden side effect is found, decide whether to exclude it too (matching the sink precedent) or whether it's cheap/safe enough to include — pin the decision here, do not defer it to PLAN/IMPL.
- **D-VALIDATE-BOOT-REUSE:** design the EXACT signature of `internal/boot.Construct` such that BOTH `cmd/envoy-go/main.go`'s normal boot path AND the new `validate` package can call the SAME function without duplicating construction logic. The real boot path needs the constructed `*bootstrap.Bootstrap`/`*cluster.Manager`/`*listener.Manager` back (to continue booting: open real sinks, build admin, call `lm.Start`); `validate` only needs the error. Decide: does `Construct` take real-or-throwaway registry/drain-manager/http-client/tracing-provider as PARAMETERS (letting each caller supply its own), or does it always build throwaway ones internally and the real boot path adapts around that? Pin this precisely — it is the SPEC's central design task, not a mechanical detail.
- **D-VALIDATE-MODE-VALUES:** the exact accepted `--mode` string set. Anticipated: bare `validate` only — no `--mode serve`/default value (introducing a REQUIRED `--mode serve` would be a breaking CLI change to every existing `-c`-only caller). Confirm and pin the exact flag semantics (case sensitivity, unknown-value handling — likely a usage error via the EXISTING `os.Exit(2)` convention).
- **D-VALIDATE-FUZZER:** does `internal/boot.Construct`/`validate.Bootstrap` warrant a NEW dedicated fuzzer, or does reuse of the EXISTING `internal/bootstrap` fuzzer family suffice (since `Construct` calls the SAME `bootstrap.Load` those fuzzers already target, and everything AFTER that call operates on an ALREADY-validated proto, not raw untrusted bytes)? Anticipated: no new fuzzer (fuzzers stay 52) — pin this with reasoning, not just an assumption.
- **D-VALIDATE-EXIT-CODES:** confirm the exact exit-code contract (`0` = valid, `1` = invalid, matching upstream Envoy's own `--mode validate` convention) and whether a THIRD code is warranted for a flag-usage error (e.g. `--mode validate` without `-c`) — likely reusing the EXISTING `os.Exit(2)` usage-error convention already in `main.go`. Pin the exact stdout/stderr wording for both the OK and error cases (the BRAINSTORM's §2.5 sketch — `"configuration OK"` on success, the raw underlying error on stderr on failure — is a starting point, not a locked contract).

**Also nail down at SPEC time (not previously resolved as open D-questions, but load-bearing for the PLAN):**
- The EXACT shape of the new `internal/filter/http/builtins` package (mirroring `internal/filter/network/builtins`'s `Deps` struct + `RegisterBuiltins(reg, deps)` convention) — read the CURRENT `main.go`'s `httpReg` registration block fresh (it will have grown/changed since the BRAINSTORM's ~25-call estimate) and the CURRENT `internal/filter/network/builtins/builtins.go` to mirror its exact style (doc comments, `Deps` field naming, no-Freeze-inside convention).
- Whether `internal/boot.Construct` should live in `internal/boot` (the BRAINSTORM's proposed name) or a different package name/location — check for any naming collision or better-fitting existing package before locking it in.
- Draft the **ADR-0268 §Context** (per ADR-0044 — §Context lands at SPEC, §Decision/§Consequences land at IMPL): the motivation (Kubernetes Gateway API / Envoy Gateway needing offline bootstrap validation), the "construct vs. bind" seam this phase exploits, and why a NEW public package (not exposing `internal/` packages directly) is required (Go's `internal/` import restriction blocks any external module from importing `internal/bootstrap`/`internal/cluster`/`internal/listener` directly — this is the WHOLE reason a new top-level public wrapper package is needed, not a stylistic choice).

**Testing envelope for this SPEC to pin (no differential fixture — this phase has none):**
- Unit tests for `internal/boot`/`validate`: a battery of valid + deliberately-broken bootstrap configs. Identify which EXISTING strict-reject test fixtures (scattered through `internal/bootstrap/bootstrap_test.go`, `internal/cluster/*_test.go`, `internal/listener/*_test.go`) can be REUSED (calling `validate.Bootstrap` against the SAME broken-config YAML strings those tests already use) versus which need NEW coverage specific to the construction-path boundary (e.g. a config that's bootstrap-valid but fails at cluster/listener construction — TLS cert path doesn't exist, a Lua script fails to compile).
- ONE new CLI-subprocess test in the EXISTING `cmd/envoy-go/main_test.go` (which already has the build-the-real-binary-and-exec convention this project uses elsewhere in that file) exercising `--mode validate` against a good config (exit 0) and a bad config (exit 1, stderr contains the expected substring).

**Global constraints to carry into the SPEC (from the BRAINSTORM, re-confirm don't re-derive):** anticipated counts at IMPL — stat **1200** (+0) / fixtures **96** (+0, no differential surface) / fuzzers **52** (+0, D-VALIDATE-FUZZER pins) / BackendKind **38** (+0) / DECISIONS **ADR-0268** (next-free ADR-0269); THREE new packages (`internal/boot`, `internal/filter/http/builtins`, `validate`), ZERO new go.mod modules (`go mod tidy -diff` must stay EMPTY — this is pure refactor + stdlib CLI plumbing, no new dependency).

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** The next stage is the phase-51 SPEC (docs-only, direct-on-master). Lifecycle-state 1 → 2.
- **Re-verify, do not assume.** Every file/line citation in the BRAINSTORM must be re-checked against the CURRENT `main.go`/`internal/listener`/`internal/cluster`/`internal/filter/network/builtins` before the SPEC relies on it — this project's standing discipline (`D-3.4` context isolation + the repeated "line numbers drift" lesson observed at every prior phase's SPEC/PLAN stage).
- **Process anchors:** ADR-0044 (ADR §Context at SPEC, §Decision+§Consequences at IMPL) · ADR-0045 (sub-split soft gate — re-confirm the BRAINSTORM's "single flat row, escape-valve unconsumed" call still holds once the SPEC has read the current code in detail) · next-free **ADR-0268** (§Context drafted at THIS SPEC).
- **This phase has no differential discipline to carry** (`reference_differential_*` memories do not apply — no fixture, no live reference Envoy comparison). The relevant carried lessons are instead about CODE-READING discipline against a monolithic `main.go` that has grown across 50 prior phases: do not trust any previously-cited line number without re-grepping first.

## Read first (in order)
1. THIS FILE — the cold-start router (phase 51 `bootstrap-validate-mode` BRAINSTORM LANDED; the next action is its SPEC).
2. `docs/envoy-go/phases/51-bootstrap-validate-mode/BRAINSTORM.md` — the full design context, the 4 resolved user-dialogue questions, the §10 D-VALIDATE-* open questions this SPEC resolves, and the §12 section closeout summary.
3. `docs/envoy-go/STATE.md` — the active-phase header (`phase 51 (bootstrap-validate-mode) BRAINSTORM done`) + the recorded NEXT (`phase 51 SPEC`) + the counts (1200 / 96 / 52 / 38 / ADR-0267).
4. `docs/envoy-go/ROADMAP.md` — row 51 `in-progress` under the NEW "Operational tooling family" heading (flips `done` at the phase-51 IMPL).
5. `cmd/envoy-go/main.go` (the FULL boot sequence: `bootstrap.Load` → `cluster.NewManagerWithBaseDir` → access-log/stats-sink construction → `httpReg`/`lfReg`/`netReg` registry building → `listener.NewManagerWithBaseDirAndAllowH2C` → `admin.New`/`admSrv.Start` → `lm.Start` → block-on-signal) + `internal/listener/manager.go` (confirm `NewManagerWithBaseDirAndAllowH2C` truly binds NO socket, and find the EXACT `net.Listen` call site inside `Start`) + `internal/cluster/manager.go` (`NewManagerWithBaseDir`'s signature + what it resolves from `baseDir`, e.g. TLS certs) + `internal/filter/network/builtins/builtins.go` (the EXISTING pattern `internal/filter/http/builtins` must mirror) + `internal/bootstrap/bootstrap.go` (`Load`'s signature + existing strict-reject arms, to identify reusable test fixtures) — re-read fresh, do not trust any BRAINSTORM line-number citation without re-verifying against HEAD.
6. `cmd/envoy-go/main_test.go` — the EXISTING build-the-binary-and-exec subprocess-test convention this phase's ONE new CLI test will extend.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — this phase's SPEC/PLAN/IMPL carry NO differential-specific memories (no fixture, no live reference comparison); the relevant standing disciplines are `feedback_execution_style`/`feedback_git_worktrees`/`feedback_subagents_no_push`/`feedback_subagent_autocommit_claudemd`/`feedback_pertask_gofmt_lint`/`feedback_push_to_origin` (all apply at IMPL, not this SPEC stage).
