START a fresh cold-start session for envoy-go — phase 26.2 (`network-filter-registry-migration`) PLAN is DONE; EXECUTE the **phase 26.2 IMPL** (the 12-task TDD PLAN) via `superpowers:subagent-driven-development`.

This is a per-sub-phase IMPL cold-start. **Phase 26.2 PLAN is DONE + squash-merged to master + pushed to origin** (squash `9a61788` — the 26.2 PLAN.md, a 12-task TDD decomposition of the 26.2 SPEC §10 task spine; docs only, no code change). The PLAN was authored via `superpowers:writing-plans` (per the 26.1 PLAN precedent), plan-document-reviewer APPROVED: 1 MAJOR (a Task-7 self-contradiction on the unknown-type-url reject) RESOLVED by keeping `buildNetChainFactory`'s 3-value `(factory, isNetChain, error)` signature + the index-0 netReg-miss fall-through THROUGH Task 7 (the old `buildTerminalFilter:628` path keeps owning the `unknown filter type_url` wording); the unified unknown-type reject + the 2-value collapse + `buildTerminalFilter` deletion all land together at Task 10 so every intermediate commit stays green. 3 MINOR/ADVISORY folded + re-confirmed ✅. STATE.md points at `phase 26.2 PLAN-done; awaiting 26.2 IMPL`, next-skill `superpowers:subagent-driven-development` (executing the 12-task PLAN), next-free `ADR-0216` (the 26.2 SPEC drafted ADR-0215 §Context; its §Decision/§Consequences bodies land IN PLACE at 26.2 IMPL Task 12 per ADR-0044 — tail STAYS ADR-0215).

## What this session must do: execute the 12-task phase-26.2 IMPL

Per SKILL_ROUTING (sub-row 26.2 at state 3 — SPEC.md + PLAN.md exist, no IMPL yet), the next-skill is **`superpowers:subagent-driven-development`** (per the `feedback_execution_style` project memory: ALWAYS subagent-driven over inline; the 26.1 IMPL precedent ran exactly this way). Execute `docs/envoy-go/phases/26.2-network-filter-registry-migration/PLAN.md` task-by-task: a FRESH subagent per task, two-stage review between tasks, commits LOCAL-ONLY (subagents do NOT push — `feedback_subagents_no_push`; a stray subagent push polluted origin/master in 25.3 IMPL), controller pushes at stage-close (squash). Work in a git worktree (`feedback_git_worktrees` — categorical preference; mirror the SPEC/PLAN worktree convention `.worktrees/phase-26.2-network-filter-registry-migration-impl` off master).

## Phase 26.2 scope (settled at the SPEC + decomposed by the PLAN)

**`network-filter-registry-migration`** — migrate `tcp_proxy` (`internal/filter/tcpproxy/`) + HCM (`internal/filter/hcm/`) onto the 26.1 `internal/filter/network/` framework + `*network.Registry`, RETIRE the hardcoded `manager.go` terminal-filter registry, and UNIFY the dual-dispatch — at byte-exact parity (R3 back-compat paramount). The 12-task PLAN (the IMPL executes it verbatim; PLAN-time D-questions already resolved):

- **T1** baselines + `manager.go` anchor re-pin (HARD GATE: fuzzers 35, fixtures 44/tail 0042, stats 132, ADR tail ADR-0215; tcp_proxy/HCM TypeURLs; the retirement-target line anchors against the IMPL-session tip).
- **T2** NEW `network.TerminalFilter` interface + SEALED `NetworkFilter` marker via the exported embeddable `network.Marker` (D-26.2-1; blast radius = `echoFilter`/`directresponse.filter` + the 6 `chain_test.go` fakes + `noopFilter` embed `Marker`; `FilterInstanceFactory` STAYS `func() ReadFilter` here — generalized in T4).
- **T3** extend `network.FactoryCtx` (+HasTLS/AllowH2C/ListenerPrincipal/NodeServiceCluster; D-26.2-2; heavy singletons closure-captured).
- **T4** generalize `FilterInstanceFactory func() NetworkFilter` + `ChainRuntime` classify `[]NetworkFilter` → read-prefix + optional terminal + `TerminalReady()`/`HandleTerminal()` + the buffered-prefix `prefixConn` (D-26.2-3; R-M unit test: synthetic always-`Continue` read filter → recording terminal asserts prefix THEN live bytes). Mechanical `[]ReadFilter`→`[]NetworkFilter` ripple to echo/directresponse + manager.go (`buildNetChainFactory` closure / `chainInfo.netChainFactory` / `serveReadFilterChain`).
- **T5** `tcpproxy.NewNetworkFactory(cm, dm)` adapter (capture cm/dm; wrap `NewFilter`; SHARED-instance; `var _ network.TerminalFilter`; embed `Marker`; parse-reject pass-through byte-stable).
- **T6** `hcm.NewNetworkFactory(cm, registry, sinks, httpReg, dm, httpClient)` adapter (capture singletons; bridge `FactoryCtx`→`hcm.ListenerCtx` — the bridge MOVED from `manager.go:112-132`; SHARED-instance; assertion; embed `Marker`; stat-registration parity R-A).
- **T7** unified `buildNetworkChainFactory` (KEEP 3-value sig; classify + validate `[read*, terminal?]` shape → NEW `network-filter-terminal-not-last` + `network-filter-multiple-terminals` rejects; DELETE the 26.1 `network-filter-mixed-chain-unsupported` arm; index-0 netReg-miss STILL falls through to the old path; thread the per-chain `FactoryCtx`).
- **T8** `serveConnection` step-7 → ONE `serveNetworkChain` (pure-read read loop unchanged; pure-terminal immediate `Handle` byte-identical to today; mixed handoff on `TerminalReady`; old `selected.filter.Handle` branch RETAINED until T10).
- **T9** NEW `internal/filter/network/builtins` package `RegisterBuiltins(reg, Deps)` (D-26.2-5; registers all four; import-cycle-safe).
- **T10** `netReg` intrinsic (drop nil-tolerance) + rewire all ctor callers (`internal/admin/admin_helpers_test.go`, `internal/admin/listeners_test.go`, `internal/listener/manager_test.go`, `cmd/envoy-go/main_test.go`, + the thinner `NewManager`/`NewManagerWithBaseDir`) via `RegisterBuiltins` + DELETE the old terminal path (`filterHandler`/`filterConstructor`/`filterRegistry`/`buildTerminalFilter`/`listenerCtx`/`chainInfo.filter`; collapse to 2-value; unified unknown-type reject owns the byte-stable wording — D-26.2-4/-6; R-U zero post-retirement refs).
- **T11** `cmd/envoy-go/main.go` boot-wiring via `RegisterBuiltins` (MOVE the `httpClient :=` declaration above the netReg block — it currently lands AFTER it) + live boot smoke.
- **T12** BEHAVIOR_CONTRACT 26.2 bundle + ADR-0215 §Decision/§Consequences bodies (tail STAYS ADR-0215; NO new number) + STATE/ROADMAP advance + the SIX-GATE: build/vet/lint/`-race -short` + the FULL differential suite (44, R3 back-compat — run LIVE, NOT asserted-unaffected: the dispatch genuinely changed for tcp_proxy/HCM) + conformance 10/10 + h2spec 53/53 (run LIVE). Counts: fuzzers 35, fixtures 44, stat surface 132 (all +0 — migration is stat/fixture/fuzzer-NEUTRAL).

This is a MIGRATION/refactor at byte-exact parity (R3 paramount). NO new operator-visible feature; the existing tcp_proxy/HCM differential fixtures staying byte-exact green IS the migration proof. If ANY tcp_proxy/HCM fixture breaks byte-exactness, STOP and debug (`superpowers:systematic-debugging`) — do NOT adjust the fixture.

## Project state at master tip

- **Master tip:** `5404cdc` (a next-prompt master-tip repoint; trails the 26.2-PLAN stage-close `9fc4735` and the substantive 26.2-PLAN squash `9a61788`). Confirm via `git log --oneline -1` — the commit landing any further next-prompt repoint will itself advance the tip +1 (expected, not chased). origin/master in sync after push. STATE.md `last-commit` points at the substantive squash `9a61788` (not the stage-close/repoint commits).
- **§9 Network-filters family OPEN:** parent row 26 `in-progress`; sub-row 26.1 `done`; 26.2 `in-progress` (flips `in-progress → done` at 26.2 IMPL phase-done); 26.3 `planned`. Parent row 26 flips `in-progress → done` at 26.3 phase-done (ROLLUP precedent 18/19/22/24/25).
- **Counts at master tip:** stat surface **132**; differential numbered-fixtures **44** (tail **0042**); fuzzers **35**; conformance 10/10; h2spec 53/53; DECISIONS.md tail **ADR-0215** (the §Decision/§Consequences bodies land at 26.2 IMPL Task 12 in place per ADR-0044 — tail STAYS ADR-0215), next-free **ADR-0216**. 26.2 is a migration: anticipated NO new fixtures/fuzzers/stats deltas (the IMPL Task-1 re-verifies the baselines).
- **Toolchain:** Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); reference Envoy v1.37.2 (ADR-0008); go-control-plane v1.32.4 (ADR-0008).

## Read first (in order)

1. THIS FILE — the phase-26.2 IMPL cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 26.2 PLAN-done; awaiting 26.2 IMPL`; next-skill `superpowers:subagent-driven-development`; next-free `ADR-0216`.
3. `docs/envoy-go/phases/26.2-network-filter-registry-migration/PLAN.md` — **THE PLAN TO EXECUTE** (12 TDD tasks; the ADR-0045 split-gate check; the PLAN-time D-question resolutions; the sealed-marker + terminal-handoff + RegisterBuiltins design pins; the File Structure; per-task exact-file/exact-command/expected-output steps; the Acceptance checklist; the Execution handoff). Create `PROGRESS.md` at Task 1 (record every gate output per `superpowers:verification-before-completion`).
4. `docs/envoy-go/phases/26.2-network-filter-registry-migration/SPEC.md` — the SPEC the PLAN decomposes (§3 framework + dispatch; §4 adapters; §6 PARSE-REJECT; §12 D-26.2-1..7; §13 R3/R-T/R-M/R-U/R-S/R-A; §15 acceptance).
5. `docs/envoy-go/phases/26.1-network-filter-chain-framework-and-echo/PLAN.md` + `PROGRESS.md` — the 26.1 IMPL precedent (the REAL `fixture.Driver`/`harness` API; the `git ls-files` fuzzer-count gate; the ctor-caller blast radius; the two-stage-review subagent-driven-development model) + the as-built 26.1 framework the migration extends.
6. The MIGRATION TARGETS: `internal/filter/network/{types,callbacks,chain,registry,terminal?,prefixconn?}.go` (the framework to extend — `terminal.go`/`prefixconn.go`/`builtins/` are CREATED by the IMPL); `internal/listener/manager.go` (the dual-dispatch + the hardcoded terminal path to retire/unify — re-pin the line anchors at PLAN Task 1 against the IMPL-session tip); `internal/filter/tcpproxy/filter.go` (`Handle`@69 + `NewFilter`@40); `internal/filter/hcm/{filter.go,config.go}` (`Handle`@66 + `NewFilterWithCtxAndSinksAndRegistry`@45 + `ListenerCtx`@55 + `Filter`@config.go:87); `cmd/envoy-go/main.go` (the netReg block + the `httpClient` ordering).
7. `docs/envoy-go/DECISIONS.md` ADR-0215 (the §Context drafted at the SPEC — the IMPL Task 12 fills §Decision/§Consequences in place) + ADR-0213/0214 (the framework + registry the migration builds on); `docs/envoy-go/ROADMAP.md` rows 26/26.2; `BOOTSTRAP_PROMPT.md` §5 (state machine) + §4.1 STATE invariant + ADR-0004/0005 (autonomous adaptation) + ADR-0044 (in-place ADR-body discipline) + ADR-0052 (BEHAVIOR_CONTRACT one-bundle) + ADR-0106 (ROADMAP flip-on-SPEC, already applied); `docs/envoy-go/SKILL_ROUTING.md` (confirm sub-row 26.2 state 3 → subagent-driven-development for the IMPL).
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — execution-style (subagent-driven) + worktree + push (controller pushes at stage-close) + subagents-no-push + network-filter-typeurl-extensions (verify type URLs via `proto.MessageName`; differential bootstraps need a cluster) + differential-asserter-dispatch + fixture-dispatch-constraint all apply.

After the 26.2 IMPL is complete (all 12 tasks done + six gates green + requesting-code-review/finishing-a-development-branch) + squash-merged + pushed, advance STATE.md per BOOTSTRAP §5 (SKILL_ROUTING state 3 → phase-done for sub-row 26.2; ROADMAP sub-row 26.2 `in-progress → done`; parent row 26 STAYS `in-progress` — flips at 26.3 phase-done) and rewrite next-prompt.txt for the 26.3 SPEC cold-start (`network-filter-rbac` — `internal/rbac/` extraction + connection-metadata sink + `rbac_network` filter, ADR-0216/0217/0218).
