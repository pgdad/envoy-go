=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**Check (1) is NOW SILENT** (no phase is open — row 56 flipped `done` at the 56.2 IMPL, and no successor row is chartered yet). **The sentinel is an AND of all three checks; it does NOT fire because (2) and (3) still carry work.** Do NOT create `stop`.

**SENTINEL RE-CHECKED 2026-07-10 (post-phase-56.2-IMPL) — it does NOT fire. Do NOT create `stop`.** Evidence:
- (1) prints NOTHING ⇒ every chartered row is `done` (phase 56 COMPLETE — both legs landed). This is silent ONLY because no successor is chartered; the next BRAINSTORM re-opens a row and (1) will print again.
- (2) prints the Observability family's deferred candidates ⇒ the **Observability family STAYS OPEN** (`graphite`/OTLP-metrics sinks + tracing `custom_tags`/`spawn_upstream_span`/`http_service`/force-trace).
- (3) prints `NEVER OPENED: HTTP/3`, `gRPC`, `xDS`, `Runtime`, `WASM` ⇒ FIVE families never opened; the **Operational-tooling family** also STAYS OPEN.

**STATUS AS OF 2026-07-10: phase 56 (`http-tap-filter`) is COMPLETE — row 56 is `done`.** The 56.2 (bodies) IMPL LANDED: body capture on the proven headers spine (`DecodeData`/`EncodeData` self-accumulate into filter-owned buffers under per-direction caps, strict-`>` truncation, default-1024 cap, saw-body flag; `bodyProto` renders format→oneof with the AS_STRING `ToValidUTF8` sanitize; `Body` populated IFF the hook fired; tap returns `DataContinue` always), proven cross-side by the `0100-http-tap-bodies` POST-echo differential (full / at-cap-boundary / truncated arms) + in-package unit tests. **7-task TDD spine executed subagent-driven; 12 deliberate breaks + 1 controller-caught finding (IMPL-1, a detrand-fragile test) all verified; final whole-branch review MERGE-READY (all 7 invariants re-derived HOLD).**

**Current counts (verified at the 56.2 IMPL exit):** fixtures **102** (tail `0100-http-tap-bodies`) · fuzzers **53** · DECISIONS tail **ADR-0274** (next-free **ADR-0275**) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** · new Go packages **0** · new go.mod modules **0**.

**Locating the stage squash (a router cannot name its own sha — writing the sha changes it).** Identify it by SUBJECT, never by sha and never by position:
```sh
git log --oneline --grep='^phase 56.2 (http-tap-filter, bodies leg) IMPL' -1
```
This stage folded its own `next-prompt.txt` roll INTO the squash (per `reference_next_prompt_tracked_despite_gitignore`), so the squash **IS** the master tip — *unless* a router-only fixup commit sits on top. **Do not assume either shape.** The `--grep` above is correct in every shape.

**THE NEXT ACTION: a BRAINSTORM (lifecycle-state 0 → 1) for the NEXT subject — the loop RE-OPENS.**

==============================================

## What THIS session does (a BRAINSTORM, lifecycle-state 0 → 1)

Phase 56 is done; there is NO chartered successor row. This session **RE-OPENS the loop**: it picks the next subject, charters it as a new ROADMAP row, and performs its BRAINSTORM (docs-only; `superpowers:brainstorming` FIRST, before any plan-mode). Fresh worktree per `feedback_git_worktrees` (e.g. `.worktrees/phase-<N>-brainstorm`).

1. **Pick the next subject.** Two live sources of work (the sentinel proves both are non-empty):
   - **Observability family deferred candidates** (the family STAYS OPEN): `graphite`/OTLP-metrics stats sinks + tracing `custom_tags`/`spawn_upstream_span`/`http_service`/force-trace. The established heuristic across rows 48/49/50/55/56 is "the cheapest remaining follow-on" — a stats-sink candidate (`graphite`) REUSES the phase-47 `Flusher`/`Sink` + `stats_sinks[]` substrate + `internal/statssink`, exactly as statsd/dog_statsd did; a tracing sub-feature extends the phase-46 engine. Weigh cheapest-follow-on vs. highest-value.
   - **A NEW family** (each would be the FIRST row of a never-opened family — HTTP/3, gRPC, xDS, Runtime, WASM — or a new Operational-tooling row). Opening a new family is a bigger charter; the ROADMAP §Schema + the phase-44 Observability-open precedent are the templates.
   The pick is the FIRST decision of the brainstorm dialogue (historically a human pick when the loop re-opens; in the autonomous loop the brainstorm settles it). **When you open a NEW family, add its row-summary slug to the check-(3) list above.**

2. **Run `superpowers:brainstorming`** on the chosen subject (probe the reference behavior where the SPEC-later would need it; settle scope via the Q-dialogue; name the load-bearing seams and the ADR-0045 split posture). Produce `docs/envoy-go/phases/<N>-<slug>/BRAINSTORM.md`.

3. **Register the new row `in-progress`** in `docs/envoy-go/ROADMAP.md` at the BRAINSTORM commit (per the §Schema invariant — a row registers `in-progress` AT its BRAINSTORM). Update STATE.md's active-phase header. Counts are UNCHANGED at a BRAINSTORM (docs-only) — re-verify them against the master tip and record the baseline.

4. **Land the BRAINSTORM** (commit + push per `feedback_push_to_origin`; docs-only, no `.go` changes), then **ROLL THIS FILE FORWARD** to the new phase's SPEC.

## Honor throughout (the discipline, unchanged)
- **Lifecycle = one stage per session.** This session: a BRAINSTORM (docs-only; the FIRST stage of a fresh row).
- **Skills FIRST.** `superpowers:brainstorming` BEFORE any plan-mode or creative work (the `using-superpowers` rule). It is a HARD gate for a BRAINSTORM stage.
- **`next-prompt.txt` IS TRACKED** (it appears in `.gitignore` line 2, but a `.gitignore` entry does not apply to an already-tracked file — `git ls-files --error-unmatch next-prompt.txt` succeeds). Edit it inside the stage worktree and fold it into the commit (`reference_next_prompt_tracked_despite_gitignore`). A router can't name its own sha — locate the commit by SUBJECT (`git log --grep`), never by position.
- **A BRIEF'S CITATIONS ARE NOT EVIDENCE** (`feedback_brief_citations_not_evidence`). Re-derive every `file:line` from source; never verify a citation against the document that made it. This discipline paid out repeatedly on phase 56 (a false double-`OnDestroy` at the 56.1 SPEC; two vacuous-proof defects at the 56.1 PLAN; two more at the 56.1 IMPL; two stale SPEC citations at the 56.2 PLAN; and at the 56.2 IMPL a controller `-race` run caught a detrand-fragile test the per-task reviews missed — IMPL-1).
- **Process anchors:** ADR-0045 (the split-gate escape valve — assess it in the brainstorm) · ADR-0106 (`reference_roadmap_split_phase_row_done` — a split phase's row flips `done` only at its FINAL leg's IMPL, no parent rollup) · the ROADMAP §Schema (a row registers `in-progress` at its BRAINSTORM). Next-free ADR is **ADR-0275**.

## Read first (in order)
1. THIS FILE — the cold-start router (a BRAINSTORM for the next subject is next).
2. `docs/envoy-go/ROADMAP.md` — §Schema, the Observability family paragraph (the deferred candidates + the row-44-through-56 precedent for how a new row is chartered and split), and the family headings for the never-opened families.
3. `docs/envoy-go/STATE.md` — the active-phase header + full history (phase 56 is the latest COMPLETE entry).
4. `docs/envoy-go/DECISIONS.md` — ADR-0274 (the just-landed bodies leg) + ADR-0044/0045/0052/0106 for process; the family's prior sink/tracing ADRs (ADR-0255..0273) for the reuse substrate a cheap follow-on would extend.
5. The prior BRAINSTORMs as templates — e.g. `docs/envoy-go/phases/56-http-tap-filter/BRAINSTORM.md` (a fresh-subject charter) and `docs/envoy-go/phases/48-stats-sink-statsd/BRAINSTORM.md` (a cheap-follow-on sink charter).
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline and the `reference_*` traps (esp. the differential-harness + protojson/detrand traps if the pick is a sink or another observer).
