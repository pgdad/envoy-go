=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-CHECKED 2026-07-13 (after the phase-60.2 IMPL landed, run MECHANICALLY) — it does NOT fire. Do NOT create `stop`.** State:
- (1) prints `NOT DONE: row 61` — ONE `in-progress` row. Row 60 `xds-sds-server-cert` is now `done` (BOTH legs landed: 60.1 substrate IMPL + 60.2 TLS-apply IMPL, ADR-0106 / `reference_roadmap_split_phase_row_done`). Row 61 `http3-downstream-listener` is banked at SPEC (a 61.1/61.2/61.3 split awaiting its first PLAN). This ALONE blocks `stop`.
- (2) prints THREE live "candidates:" sentences — HTTP/3, xDS, and Observability ⇒ three families STAY OPEN. (`reference_sentinel_deferred_sentence_live_vs_historical`.)
- (3) prints `NEVER OPENED: gRPC`, `Runtime`, `WASM` ⇒ THREE families never opened. Operational-tooling also STAYS OPEN.

All three checks print ⇒ the sentinel does NOT fire.

**STATUS AS OF 2026-07-13: the phase-60.2 IMPL LANDED (the SDS TLS-apply leg; ROADMAP row 60 → `done`, both legs landed; DECISIONS tail ADR-0278 → ADR-0280).** The xDS-family opener is COMPLETE. The NEXT IMPLEMENTABLE STAGE is the **phase-61.1 PLAN** (`http3-downstream-listener` leg 61.1 — QUIC/UDP listen substrate + the `quic-go` module; lifecycle-state 2 for the first HTTP/3 leg — decompose SPEC-61 into a TDD spine). Advancing the banked HTTP/3 SPEC (row 61 `in-progress`, BRAINSTORM + SPEC landed, split decided, NO PLAN yet) is the roller's self-pick per the standing directive (a banked SPEC'd family awaiting its first PLAN is more defensible than opening a brand-new subject). Row 61 flips `done` only when ALL three legs (61.1/61.2/61.3) land (ADR-0106).

**What the 60.2 IMPL delivered (the xDS-family opener is now COMPLETE — for CONTEXT only; row 60 is CLOSED):**
- `internal/xds/config.go` `ParseSDSConfig` (arms 1–4/8/9 + V3 + one-config MVP cap); the `internal/tls/config.go` one-arm downstream-gated SDS LIFT + `provider xds.SecretProvider` param (arm 6 upstream byte-identical reject, arm 5 unchanged, all errors `tls: downstream:`-prefixed); `sdsProvider` threaded through the listener manager; `boot.NewSDSProvider` (pre-scan + node arm 7) + `Construct`/main/validate wiring; `sdsserver.NewAtAddr` + `helpers.TLSServedLeaf` + `sdsserver.Server.Close()`; the `0103-xds-sds-server-cert` differential (served-leaf cross-side via `CompareBytes`, liveness proven by a two-different-certs break); BEHAVIOR_CONTRACT xDS/SDS section; ADR-0280. The ADR-0278 cycle guard held throughout (`internal/tls → internal/xds` interface-only; the grpcclient edge isolated in `internal/xds/xdsgrpc`; `go list -deps` re-verified at stage close).
- The xDS FAMILY STAYS OPEN. Remaining deferred candidates (ROADMAP xDS live sentence): SDS rotation + SDS validation_context/upstream SDS + CDS/EDS + LDS/RDS + ADS (muxed) + Delta xDS + RTDS/Runtime-layer + reconnection-backoff/initial_fetch_timeout edges + google_grpc transport.

**The phase-61.1 PLAN scope (RE-READ SPEC-61 + BRAINSTORM-61 at session start; `git fetch` first):**
- **This is a DOCS-ONLY stage** — decompose SPEC-61's 61.1 leg into a TDD task spine (`docs/envoy-go/phases/61-http3-downstream-listener/PLAN-61.1.md`) + a `PROGRESS-61.1.md` scaffold. NO production `.go` changes; row 61 STAYS `in-progress`.
- **Leg 61.1 (per SPEC-61 §3.0 D-H3-SLICE):** the QUIC/UDP listen substrate + the FIRST new go.mod module `github.com/quic-go/quic-go` **v0.54.1** (the LAST release keeping the project's `go 1.23` directive; a LIVE `quic-go ↔ reference` H3-GET interop probe PINNED it — `HTTP/3.0`/200/ALPN `h3`/TLS 1.3). Introduce a UDP/packet listen path parallel to `manager.go`'s `net.Listen("tcp",…)` (bind `net.ListenUDP` + a quic-go listener over it); parse the bootstrap wire shape (`udp_listener_config` + `quic_options` + the `envoy.transport_sockets.quic` downstream transport socket wrapping a `DownstreamTlsContext`); LIFT the `transport_protocol "quic"` reject; build the `*stdtls.Config` (ALPN `h3`) and complete the QUIC/`h3`-ALPN handshake; register the reused per-listener `downstream_cx_total` stat; the strict-reject arms (SPEC §6). Verified subject-side by a unit/integration test of the handshake (a local quic-go client) — NO HTTP serving yet (that is 61.2). ANTICIPATES **ADR-0279** (the family opener + the listen-path seam + the module decision — its §Context DRAFT is in SPEC-61 §13; §Decision/§Consequences land at the 61.1 IMPL per ADR-0044).
- **Legs 61.2/61.3 (for CONTEXT — NOT this PLAN):** 61.2 = the H3 codec + HCM adapter (a THIRD dispatch arm `internal/filter/hcm/h3` modeled on `WriteH2`, which is already codec-agnostic — H3 is ADDITIVE, not framework surgery); 61.3 = the differential fixture + harness UDP/H3-client surgery (a shared `test/helpers/h3.go` `H3RoundTrip` + the harness's first non-TCP transport). The 61.1 PLAN decomposes ONLY 61.1.

**ADR reservation ledger (provisional; each finalized at its IMPL in landing order):** ADR-0277 = phase-59 (LANDED) · ADR-0278 = xDS 60.1 (LANDED) · ADR-0280 = xDS 60.2 (LANDED) · **ADR-0279 = HTTP/3 (61) — the NEXT ADR to land, at the first 61.x IMPL (a multi-leg split MAY anchor per-leg ADRs — the PLAN/IMPL decide)** · next-free unreserved **ADR-0281**.

**Current counts (VERIFIED at the phase-60.2 IMPL six-gate — RE-VERIFY against the actual master tip at session start, `git fetch` first):** fixtures **105** (tail `0103-xds-sds-server-cert`) · fuzzers **55** · DECISIONS tail **ADR-0280** (next-to-land **ADR-0279** = HTTP/3; next-free **ADR-0281**) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** · new go.mod modules **0** (phase 61.1 adds the FIRST — `quic-go` — at its IMPL, NOT this PLAN). Phase-61.1 is a DOCS-ONLY PLAN ⇒ all counts UNCHANGED at this stage.

**Locating the phase-60.2 IMPL stage squash** (a router cannot name its own sha — identify by SUBJECT, never by position):
```sh
git log --oneline --grep='^phase 60.2 (xds-sds-tls-cert-apply) IMPL' -1
```

**THE NEXT ACTION: the phase-61.1 PLAN (lifecycle-state 2 for the 61.1 leg — decompose SPEC-61's QUIC/UDP-substrate leg into a TDD spine + PROGRESS scaffold; docs-only; row 61 STAYS `in-progress`).** After the 61.1 PLAN lands, the roller's next stage is the phase-61.1 IMPL. The sentinel governs termination, not this list.

==============================================

## What THIS session does (the phase-61.1 PLAN)

Decompose SPEC-61's leg 61.1 (QUIC/UDP listen substrate + the `quic-go` module) into a TDD task spine, subagent-driven where useful (`feedback_execution_style`), in a FRESH worktree off master (`.worktrees/phase-61.1-plan`, branch `phase-61-http3-quic-substrate-plan`, per `feedback_git_worktrees`). This is a SINGLE STAGE (the PLAN — docs-only). NO production `.go` changes; row 61 STAYS `in-progress` (flips `done` only when ALL three legs land, ADR-0106).

**Deliverables:**
- `docs/envoy-go/phases/61-http3-downstream-listener/PLAN-61.1.md` — a TDD task spine for leg 61.1 (the `quic-go` module add + `go mod tidy` — the FIRST non-empty `go mod tidy -diff` in the project; the UDP/QUIC listen path parallel to the TCP one; the bootstrap wire parse; the `transport_protocol "quic"` reject lift; the `*stdtls.Config` ALPN-`h3` build + handshake; the strict-reject arms; the subject-side handshake test with a local quic-go client). RE-DERIVE every `file:line` against the master tip (`feedback_brief_citations_not_evidence`) — SPEC-61 §12 has the source roster (`manager.go` TCP listen path + the `transport_protocol "quic"` reject).
- `docs/envoy-go/phases/61-http3-downstream-listener/PROGRESS-61.1.md` — the baselines + the task checklist + the design pins + the anticipated exit counts (new go.mod module `quic-go`; +1 non-empty `go mod tidy -diff`; ADR-0279 anchored at the 61.1 IMPL).

## Honor throughout (the discipline, unchanged)
- **Lifecycle = one stage per session.** This session: the phase-61.1 PLAN ONLY. After it lands: the phase-61.1 IMPL (or the roller's self-pick; the sentinel governs).
- **Process anchors:** ADR-0044 (ADR §Decision/§Consequences land at the IMPL — ADR-0279 body at the 61.1 IMPL, NOT this PLAN) · ADR-0045 (the split escape-valve — CONSUMED for row 61) · ADR-0080 (distinct per-arm reject substrings) · ADR-0106 (row 61 flips `done` only when ALL legs land).
- **`next-prompt.txt` IS TRACKED** (`reference_next_prompt_tracked_despite_gitignore`) — edit it inside the stage worktree and fold into the stage commit; locate commits by SUBJECT (`git log --grep`), never by position.
- **Deferred maintenance (carried; ride until the relevant row reopens the file):** the phase-57 statssink notes F-1/F-2/F-3 (fold into the next `udp.go`/statssink-driver-touching row — phases 58/59/60.x touched NONE); the phase-60.2 final-review MINOR roll-up (all cosmetic/coverage, DEFERRED): T2 two structurally-unreachable `ParseSDSConfig` grpc_service arms; T5 `seen>1` + `NewSDSClient` dial-wrap untested single-branch pass-throughs + a cosmetic doubled cluster-name in one error string; T6 redundant `raw.Close()` on the handshake-failure path; T7 `sdsserver/doc.go` API list omits `Close()`/`NewAtAddr()`. NONE must-fix; fold opportunistically when the relevant file next reopens.

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-61.1 PLAN is next; SPEC-61 + BRAINSTORM-61 are banked; row 60 is CLOSED).
2. `docs/envoy-go/phases/61-http3-downstream-listener/SPEC.md` — §3.0 (the 61.1/61.2/61.3 split), §2.4 (the `quic-go` v0.54.1 module decision), §3 (the change), §5 (the bootstrap wire shape), §6 (the strict-reject roster), §11 (the LIVE-probe pins), §12 (the source `file:line` roster — RE-DERIVE against the tip), §13 (the ADR-0279 §Context DRAFT).
3. `docs/envoy-go/phases/61-http3-downstream-listener/BRAINSTORM.md` — the D-H3-* question dispositions (esp. D-H3-SLICE/D-H3-SPLIT, D-H3-QUICLIB, D-H3-HCM-SEAM, D-H3-DIFF-HARNESS).
4. `docs/envoy-go/DECISIONS.md` — the tail ADR-0280 (xDS 60.2, just landed) + ADR-0044/0045/0106 (the process anchors); ADR-0279 is RESERVED (its body lands at the 61.1 IMPL).
5. `docs/envoy-go/ROADMAP.md` — row 61 `in-progress` (the HTTP/3-family opener); the HTTP/3-family row + the LIVE HTTP/3 deferred sentence.
6. `docs/envoy-go/STATE.md` — the active-phase header (phase 60.2 IMPL done; NEXT = the roller's self-pick / the phase-61.1 PLAN).
7. The seam source for the PLAN's `file:line` re-derivation: `internal/listener/manager.go` (the TCP `net.Listen` listen path + the `transport_protocol "quic"` reject) · `internal/filter/hcm/h2dispatch.go` (`WriteH2` — the codec-agnostic dispatch precedent, for 61.2 context) · `go.mod`/`go.sum` (the FIRST module add lands at the 61.1 IMPL).
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the `reference_*` traps (esp. `reference_differential_grpc_receiver_driver_owned`, `reference_docker_probe_bridge_network`, `reference_next_prompt_tracked_despite_gitignore`, `reference_roadmap_split_phase_row_done`, `reference_sentinel_deferred_sentence_live_vs_historical`, `feedback_brief_citations_not_evidence`).
