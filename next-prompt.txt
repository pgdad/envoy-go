START a fresh cold-start session for envoy-go — **the phase-37 (`load-balancer-maglev`) IMPL is DONE, LANDED on master, and PUSHED to origin; the next lifecycle step is a NEW phase BRAINSTORM** (`superpowers:brainstorming` — the Load-balancing FAMILY stays OPEN with **4** candidates remaining: {subset LB, locality-weighted LB, priority load balancing, panic thresholds}; pick the next candidate or the next ROADMAP family at the brainstorm). The substantive master tip is the phase-37 IMPL squash **`5238c5a`** (`phase 37 IMPL: load-balancer-maglev (Cluster.LbPolicy MAGLEV, enum 5) — the project's FIFTH LB policy + SECOND consistent-hash policy; the ADR-0235 seam's FIRST hash-policy reuse`; pushed to `origin/master`; confirm via `git log --oneline -5`), sitting directly above the phase-37 docs-only SHA-fill chain (`61b576e`/`9f48d5b`/`07e6c8f`) and the phase-37 PLAN squash `ce3a3eb`. The literal live master tip is the docs-only `next-prompt.txt` SHA-fill **`0443dbe`** (which named `f19791b` — the post-phase-37-IMPL route to a new BRAINSTORM — as the docs-only live tip above the IMPL squash `5238c5a`) sitting directly above `f19791b`/`5238c5a`; this paragraph is the next docs-only SHA-fill above `0443dbe`.

The phase-37 IMPL executed `docs/envoy-go/phases/37-load-balancer-maglev/PLAN.md` Tasks 1–10 subagent-driven (`superpowers:subagent-driven-development` per `feedback_execution_style`) in worktree `.worktrees/phase-37-impl` (now removed; branch deleted), fresh subagent per task + two-stage review, subagents LOCAL-ONLY commits, the controller squash-merged + pushed at stage-close (`feedback_subagents_no_push` + `feedback_push_to_origin`). The final code-reviewer (`superpowers:code-reviewer`) APPROVED (no critical/important issues; two trivial doc-precision nits, no change required).

## What landed (phase 37 IMPL — as built)

For `Cluster.LbPolicy MAGLEV` (`envoy.config.cluster.v3`, enum value 5) — the project's FIFTH LB policy + SECOND consistent-hashing policy + the ADR-0235 hash-key seam's FIRST hash-policy REUSE:

- The fixed-size Maglev lookup-table `maglevLB` (`internal/cluster/maglev.go`, ~149 LoC) mirroring `ringHashLB` structurally — endpoints sorted by `Addr()` ascending (the load-bearing pre-build `std::sort`), per host `offset = xxHash64(addr)%M` + `skip = (xxHash64Seed(addr,1)%(M-1))+1`, populate `(offset+skip·next)%M` until all M slots fill (terminates: M prime → skip coprime → full M-cycle); `Pick` indexes `table[hashKey % M]` + a no-hash uniform-random `rng()%M` fallback + empty-set `errNoEndpoints` + `noopRelease`; built once, immutable, lock-free; the standalone `isPrime(uint64)` trial-division helper. ZERO new hash code (`xxHash64`/`xxHash64Seed` reused).
- `cluster.Manager` accepts MAGLEV via `case Cluster_MAGLEV` + `parseMaglevLbConfig` (default 65537 self-supplied; PGV cap `≤ 5000011` THEN the primality gate — both reference PARITY, the reference itself rejects `The table size of maglev must be prime number`; mismatched-oneof silent-ignored). Reject text extended `…, RING_HASH` → `…, RING_HASH, MAGLEV`; `TestManager_Error_UnsupportedLBPolicy` doubly-hit-retargeted MAGLEV → `Cluster_CLUSTER_PROVIDED`.
- 2 MAGLEV-only gauges `cluster.<name>.maglev_lb.{min,max}_entries_per_host` (Set once at build via a `*maglevLB` type-assert; 21845/21846 for default 65537 over 3 hosts; NO `size` gauge).
- Differential `0063-lb-maglev` (route header `hash_policy` → MAGLEV; the `0062` convention reused verbatim): N=16 × K=16 = 256 `GET /get` + 8 `/health`; per-side modular invariant (`≡ 0 mod K` affinity, BOTH sides) + spread (`≥ 2`) + conservation 256 + cross-side `/health` byte-equiv + a cross-side `StatsAsserter` (the 2 gauges + `upstream_rq_total` cross-equal). 3 `-count=1` deliberate breaks proven live (scatter-key → affinity; BUILD-level collapse → spread + both gauges; stats-drop → stats); 20/20 flake-free.
- REUSES the ADR-0235 seam + BOTH ADR-0237 producer planes UNCHANGED; ZERO new packages / go.mod deps / hash code / exported symbols. ADR-0024 UNAMENDED.

## Counts at master tip (phase 37 IMPL done — confirm at the next session)

- stat surface **1121** (was 1119; +2 `maglev_lb.*` gauges; a DOC count in BEHAVIOR_CONTRACT.md, no programmatic golden).
- differential fixtures **65** (tail `0063-lb-maglev`; `ls -d test/fixtures/[0-9]* | wc -l`).
- fuzzers **42** (UNCHANGED — the table property test is unit-level, FOLDED into `maglev_test.go`; tail `FuzzZookeeperResponseDecode`). BackendKind tail **33** (`TCPThriftResponder`; `0063` reuses `HTTPEcho`).
- DECISIONS tail **ADR-0238** (the maglev policy; full entry ACCEPTED in-place per ADR-0044; next-free **ADR-0239**). The durable LB assets a future hash policy reuses: ADR-0235 (the hash-key seam) + ADR-0236 (ring_hash) + ADR-0237 (the HTTP producer) + ADR-0238 (maglev).
- reference pin: `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227). Toolchain: Go 1.26.x; golangci-lint 1.64.8 (ADR-0009); go-control-plane `/envoy v1.32.4` (ADR-0008).
- six gates GREEN at the IMPL stage-close on master: build/vet/gofmt/golangci-lint clean; `-race -short` clean; 65-dir differential byte-exact (210s, Docker present); `go mod tidy -diff` empty; h2spec 53/53 + proxy-wasm 10/10 asserted-unaffected by change-scope construction.

## Read first (in order)

1. THIS FILE — the post-phase-37-IMPL cold-start (next: a NEW phase BRAINSTORM — `superpowers:brainstorming`).
2. `docs/envoy-go/STATE.md` — active-phase `phase 37 (load-balancer-maglev) IMPL done`; the counts; the lifecycle routing (→ the next Load-balancing candidate or the next ROADMAP family).
3. `docs/envoy-go/ROADMAP.md` — row 37 `done` (2026-06-13; flat family row); the Load-balancing family heading (4 candidates remain) + the other §9 family headings for the next-phase choice.
4. `docs/envoy-go/DECISIONS.md` — ADR-0238 (maglev — the just-landed policy) + ADR-0235/0236/0237 (the reused LB assets) + the process ADRs honored throughout (ADR-0044 / ADR-0052 / ADR-0080 / ADR-0106 / ADR-0045 / ADR-0004 / ADR-0227).
5. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the maglev (MAGLEV) subsection + the stat-name mapping (the 2 new gauges; surface 1121) + the reject-text line.
6. `docs/envoy-go/phases/37-load-balancer-maglev/PROGRESS.md` — the IMPL ledger (the 10-task TDD record; the isPrime oracle correction; the 3 deliberate-break records + the build-vs-Pick contrast; the six-gate evidence) — the SHAPE TEMPLATE for the next IMPL.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_execution_style`, `feedback_git_worktrees`, `feedback_subagents_no_push` + `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_path_targeting`/`_detach`, and the differential references (`_break_protocol_count1`, `_run_selector`, `_band_sigma_margin`, `_hash_key_cross_side_infeasible`, `_fixture_workload_constant_desync`, `_docker_probe_bridge_network`).

## Honor throughout

- The lifecycle is BRAINSTORM → SPEC → PLAN → IMPL, each its own session. The next session is a NEW phase **BRAINSTORM** (`superpowers:brainstorming` — choose the next Load-balancing candidate {subset LB, locality-weighted LB, priority load balancing, panic thresholds} or open the next ROADMAP family; charter Q0/Q1 + the empirical-pin plan against `envoyproxy/envoy:contrib-v1.37.2`).
- `feedback_execution_style` (subagent-driven at the IMPL) · `feedback_git_worktrees` · `feedback_subagents_no_push` + `feedback_push_to_origin` · `feedback_pertask_gofmt_lint` · `feedback_subagent_worktree_path_targeting`/`_detach`.
- ADR-0106 (flat family row) · ADR-0044 (ADR §Context at SPEC, body at IMPL, in-place) · ADR-0052 (the atomic-landing six-gate) · ADR-0080 (byte-stable parse-reject wording) · ADR-0227 (the contrib reference image; live probes per `reference_docker_probe_bridge_network`) · ADR-0045 (the split-gate) · ADR-0004 (the empirical-pin discipline).
