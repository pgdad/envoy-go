START a fresh session for envoy-go (router last updated 2026-06-24, at the 43.2b SPEC-done tip `f823aa7e`). **The next action is the phase-43.2b (graceful GOAWAY rotation) PLAN** (`superpowers:writing-plans` — author the TDD spine from `SPEC-43.2b.md`, resolve the §12 D-H2B-* PLAN questions, then plan-document-reviewer). **Git hygiene:** the docs-only PLAN commit lands DIRECT on master (the SPEC/PLAN lifecycle convention); no live probe this stage. The later 43.2b IMPL runs subagent-driven in a fresh worktree off master per `feedback_git_worktrees` + `feedback_subagents_no_push` + `feedback_execution_style`. Phase 43 (`connection-pooling`) is the FIFTH-and-FINAL Upstream-robustness-family row and is `in-progress`: **leg 43.1 DONE** (max_connections + max_pending_requests, ADR-0252) + **leg 43.2a DONE** (the H2 multiplex pool substrate, ADR-0253, SUPERSEDES ADR-0056) + **leg 43.2b PENDING** (this one — the LAST; BRAINSTORM + SPEC done). When 43.2b's IMPL lands, **row 43 flips `done`** and the Upstream-robustness family (phases 39–43) CLOSES (`reference_roadmap_split_phase_row_done`).

First confirm the landed tip is the 43.2b SPEC commit via `git log --oneline -3` (expect `phase 43.2b (h2-connection-pool) SPEC …`; re-read the sha, do NOT assume). Counts at tip (UNCHANGED — docs-only SPEC): stat surface **1185** (H2 cluster; non-H2 stays 1183) / fixtures **81** (tail `0079-h2-multiplex-pool`) / fuzzers **42** (documented; actual `^func Fuzz`=43 per `reference_fuzzer_count_docs_drift`) / BackendKind tail **37** (`H2HoldResponder`) / DECISIONS tail **ADR-0253**, next-free **ADR-0254**.

## NEXT STEP — the 43.2b PLAN

Author `docs/envoy-go/phases/43.2-h2-connection-pool/PLAN-43.2b.md` in the 43.2a PLAN shape (the 12-task spine in `PLAN.md` is the model; 43.2b anticipates a SMALLER ~8–10-task flat spine). The SPEC (`SPEC-43.2b.md`) is the direct input; it SETTLED the design via the IN-SESSION live probe (do NOT re-litigate — the §11 D-H2B-* pins are EXECUTED). **The headline the PLAN builds on (AMEND-H2B-1..4 — the probe REVERSED the brainstorm DOWN):** a peer GOAWAY is observed ONLY via the `upstream_cx_*` lifecycle counters (`cx_close_notify` then `cx_destroy_local`; conn held until the last in-flight stream completes — graceful, `_with_active_rq`==0; replacement LAZY on the next request via `cx_http2_total`++) with **NO `http2.*` counter moving during rotation** and **NO `goaway_received` counter existing**; of the brainstorm's 4 candidate stats only **`http2.rx_reset` + `http2.tx_reset`** are LIVE + cross-side assertable ⇒ register exactly those two (**1185 → 1187, +2**, NOT the brainstorm's ~1190); `goaway_sent` (reference-0-on-drain — would diverge) + `stream_refused_errors` (reference-dormant) DEFERRED.

**The as-specified design (ADR-0254 — verify each claim against the source before planning a task on it):**
1. **Codec accessors** (`internal/filter/hcm/h2/client.go`): `GoneAway() bool` + `GoneAwayCh() <-chan struct{}` — read-only over the existing `goawayCh` (closed in `dispatchFrame`'s `GoAwayFrame` case, line ~444; NO `cc.cancel()` so `Closed()` [= `ctx.Err()!=nil`, line ~614] stays false on a GOAWAY'd-but-alive conn). No new codec state.
2. **Admission skip** (`internal/cluster/h2pool.go`): the TWO ESTABLISHED-conn sites — `findStreamHitLocked` (line ~444) + `h2PromoteLocked`'s stream-slot branch (line ~131) — add `&& !pc.cc.GoneAway()`. **NOT `findConnectingHitLocked`** (a `connectingH2Conn` there has `cc == nil` — set only at conversion in `dialAndPool` line ~603 AFTER `removeConnectingLocked` line ~599 — so the guard would nil-deref + is vacuous; the SPEC §3.2 corrected this at review).
3. **Eviction generalization:** `makeRelease`'s eager-close (line ~623) `pc.cc.Closed() && pc.inFlight==0` → `(pc.cc.Closed() || pc.cc.GoneAway()) && pc.inFlight==0` (the in-flight drain-close-on-last-stream).
4. **The per-conn drain-watcher goroutine:** spawned in `dialAndPool` AFTER `c.h2Pool[addr] = append(...)` (line ~601); `select { <-cc.GoneAwayCh() → re-lock `h2PoolMu`, re-check `findPooledLocked != nil && inFlight==0`, then `evictH2ConnLocked` + `h2PromoteLocked` (idle prompt-close) ; <-cc.ctx.Done() → exit, evicted-by-another-path }`. The re-check dodges the double-evict race with `EvictH2ConnOnError`/`makeRelease` (D-H2B-WATCHER-LIFECYCLE — the LOAD-BEARING `-race -count=1` task).
5. **Codec→cluster stat wiring (the FIRST):** inject the cluster's `http2.rx_reset`/`http2.tx_reset` counter handles into the `ClientConn` at the `dialPooledH2To`→`NewClientConn` seam; the codec increments at its EXISTING sites — `rx_reset` in `dispatchFrame`'s `RSTStreamFrame` case (line ~389), `tx_reset` in `RoundTrip`'s `ctx.Done` `WriteRSTStream(id, CANCEL)` (line ~537). Nil-guarded (D-H2B-WIRING: constructor-param vs setter; `DialH2` passes nil — it has no production callers).
6. **The `0080-h2-goaway-rotation` cross-side EXACT differential:** an H2 (TLS+ALPN-h2) DOWNSTREAM listener (the 0004 PKI shape — MANDATORY, `reference_h2_pool_downstream_codec_gate`) → an h2c upstream cluster with a GOAWAY/RST-capable backend; sequenced poll-to-converge barriers (establish → in-flight drain → idle drain → rx_reset prong → tx_reset prong → quiesce); assert cross-side EXACT on `upstream_cx_http2_total` + `http2.streams_active` + `rx_reset==1` + `tx_reset==1` (NOT `cx_close_notify`/`cx_destroy_local` — envoy-go does not emit those, D-H2B-CXSTATS). Backend: extend `H2HoldResponder` (kind 37) with `/__goaway`+`/__rst` gates vs a new kind 38 (D-H2B-BACKEND — a PLAN call).

**Resolve at PLAN the §12 D-questions:** D-H2B-{BACKEND, WATCHER-LIFECYCLE, WIRING, SPLIT-FINAL (final ADR-0045 re-check), FUZZER-RECONCILE (reconcile documented-42→actual-43 at this family-closing leg, or carry forward), BYTESTAB, CXSTATS}. Anchor the ADR-0254 §Decision/§Consequences land at the 43.2b IMPL (ADR-0044; §Context is drafted at SPEC §13).

## What 43.2a delivered (the substrate 43.2b extends — ADR-0253, SUPERSEDES ADR-0056)

A per-endpoint H2 multiplex connection pool (`internal/cluster/h2pool.go`) over the 43.1 permit substrate (ADR-0252). The four-tier `AcquireH2Stream(ctx)` ladder: stream-HIT → connecting-HIT/attach (Task-9.5 connect-time dial COALESCING — K concurrent acquires open exactly `ceil(K/C)` conns) → MISS dial-as-initiator (`tryAcquireConnSlot` reserves a 43.1 permit) → PEND in the SEPARATE stream-aware queue (woken on stream-free via `h2PromoteLocked` OR conn-close; bounded by `max_pending_requests`; queue-full → `errConnPoolOverflow` → 503). A SINGLE ctx-aware `c.lb.Pick(…)` (Task-7.5) threaded through the ladder. Per-conn stream budget = the cluster's OWN `http2_protocol_options.max_concurrent_streams` (the LOCAL cap C; AMEND-H2-1, `reference_h2_pool_local_cap_driven`). Two conserved resources (the 43.1 permit + the LB-pick release) each fire EXACTLY ONCE per acquire; `-race -count=20` clean. The router rewire (`router_h2.go`) moved BOTH H2 sites onto `AcquireH2Stream`+`release()`+evict-on-error; legacy `doH2` proven dead + REMOVED. **The H2 pool engages ONLY for an H2 (TLS+ALPN-h2) DOWNSTREAM listener** (no H1-down→H2-up bridge). Stats: `upstream_cx_http2_total` + `http2.streams_active` (useH2-gated; 1183→1185). 43.2a shipped MINIMAL liveness only (`Closed()`-skip + evict-on-RoundTrip-error) and DEFERRED the drain lifecycle + reset stats to 43.2b — the seam 43.2b extends is `makeRelease`→`evictH2ConnLocked` (today `Closed() && inFlight==0`; 43.2b adds the draining case) + the codec's `goawayCh` signal (today closed by `dispatchFrame` but NOT exposed to the pool + NOT cancelling ctx).

## Honor throughout (the discipline)

- **Lifecycle = one stage per session.** A PLAN is a docs commit DIRECT on master · `feedback_git_worktrees` (the IMPL stage) · `feedback_subagents_no_push` (subagents commit LOCALLY; the controller squashes + pushes at stage-close) · `feedback_push_to_origin` (push after a clean commit on master with tests green) · `feedback_execution_style` (subagent-driven IMPL) · `feedback_pertask_gofmt_lint`.
- **Process anchors:** ADR-0106 + `reference_roadmap_split_phase_row_done` (row 43 flips `done` only when ALL legs land — 43.1 + 43.2a done, 43.2b is the LAST); ADR-0080 (strict-reject); ADR-0044 (the ADR §Decision/§Consequences land at IMPL); ADR-0045 (the split gate — the SPLIT-FINAL re-check); `reference_docker_probe_bridge_network` (the live-probe pattern — already EXECUTED at SPEC; the IMPL re-probes via the `0080` differential) + `reference_concurrency_differential_release_barrier` + `reference_concurrent_attempt_downstream_class_assertion` (the differential discipline) + `reference_differential_run_selector` + `reference_differential_break_protocol_count1` + `reference_differential_fullsuite_startup_flake` (a `subject ready: EOF` is a transient — isolate-re-run, NOT a regression) + `reference_fuzzer_count_docs_drift` (D-H2B-FUZZER-RECONCILE).
- **The H2 pool memory:** `reference_h2_pool_local_cap_driven` (the `ceil(K/C)` architecture); `reference_h2_pool_connect_coalescing` (the connecting-tier coalescing 43.2b must PRESERVE — the `findConnectingHitLocked` site stays GoneAway-guard-free); `reference_h2_pool_downstream_codec_gate` (the GOAWAY differential MUST use an H2 downstream); `reference_max_connections_soft_breaker` (the 43.1 contrast).

## Read first (in order)

1. THIS FILE — the cold-start router (43.2b SPEC is DONE + LANDED; the next action is the 43.2b PLAN).
2. `docs/envoy-go/STATE.md` — active-phase `phase 43.2b (h2-connection-pool) SPEC done`; the AMEND-H2B-1..4 record + the count figures (1185 / 81 / 42 / 37 / ADR-0253, next-free ADR-0254).
3. `docs/envoy-go/phases/43.2-h2-connection-pool/SPEC-43.2b.md` — the SPEC (the §11 D-H2B-* EXECUTED pins, the §12 PLAN D-questions, the §13 ADR-0254 §Context, the §10 task sketch) — the PLAN's direct input.
4. `docs/envoy-go/phases/43.2-h2-connection-pool/PLAN.md` — the 43.2a 12-task PLAN (the spine SHAPE the 43.2b PLAN mirrors).
5. `docs/envoy-go/DECISIONS.md` — **ADR-0253** (the 43.2a as-built; its §Consequences "DEFERRED to 43.2b" is the charter) + next-free **ADR-0254**.
6. `internal/cluster/h2pool.go` (`dialAndPool` [watcher-spawn site ~601], `findStreamHitLocked` ~444, `findConnectingHitLocked` ~456 [stays UNGUARDED], `h2PromoteLocked` ~124, `makeRelease` ~617, `evictH2ConnLocked` ~638, `EvictH2ConnOnError` ~665, `findPooledLocked` ~679) + `internal/filter/hcm/h2/client.go` (`goawayCh`, `dispatchFrame`'s `GoAwayFrame` ~444 + `RSTStreamFrame` ~389 cases, `Closed()` ~614, `Close()` ~619, `RoundTrip` ctx.Done CANCEL ~534, `NewClientConn` ~153) + `internal/cluster/dial_h2.go` (the `dialPooledH2To`→`NewClientConn` wiring seam).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `reference_h2_pool_local_cap_driven`, `reference_h2_pool_connect_coalescing`, `reference_h2_pool_downstream_codec_gate`, `reference_roadmap_split_phase_row_done`, `reference_docker_probe_bridge_network`, `reference_differential_fullsuite_startup_flake`, `reference_concurrency_differential_release_barrier`, `reference_concurrent_attempt_downstream_class_assertion`, `reference_differential_run_selector`, `reference_differential_break_protocol_count1`, `reference_fuzzer_count_docs_drift`, `feedback_git_worktrees`, `feedback_subagents_no_push`, `feedback_push_to_origin`, `feedback_execution_style`, `feedback_pertask_gofmt_lint`).
