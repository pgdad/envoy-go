START a fresh session for envoy-go (router last updated 2026-06-25; the **phase 44.2 (`grpc-access-log` buffering) PLAN is DONE + pushed to origin/master**). HEAD is the LATEST `next-prompt.txt:` docs-only tip-stamp commit; the **phase-44.2 PLAN squash `b8cc53ca`** (subject `phase 44.2 (grpc-access-log buffering) PLAN: the ~9-task TDD spine …`) is the stable anchor sitting directly BENEATH the run of `next-prompt.txt:` commits at the tip; re-read the shas with `git log --oneline -18`, do NOT assume — the master tip is the LATEST `next-prompt.txt:` commit (a roll or a tip-stamp fix), NOT the PLAN squash itself, and any number of docs-only `next-prompt.txt:` commits may stack ATOP the PLAN squash (which stays the stable anchor). BENEATH the 44.2 PLAN squash is the **phase-44.2 SPEC squash `8d5cdb4a`** (`phase 44.2 (grpc-access-log buffering) SPEC: …`), then the **phase-44.1 IMPL squash `ec05a421`** (`phase 44.1 (grpc-access-log core) IMPL: …`), the 44.1 PLAN squash `5c7cf481`, the 44.1 SPEC squash `a7a24d0c`, the phase-44 BRAINSTORM `81afdd59` (near the bottom of the -16 window). **The Observability family is OPEN** (phase 44 is its FIRST row; the family STAYS OPEN — 44.3 header-capture / OTLP / tracing / stats sinks / tap remain future rows). **The next action is the 44.2 (buffering) IMPL** — execute the ~9-task TDD spine in `docs/envoy-go/phases/44-grpc-access-log/PLAN-44.2.md` subagent-driven (`superpowers:subagent-driven-development` + `feedback_execution_style` — a fresh implementer per task + two-stage review); CONFIRM against `docs/envoy-go/STATE.md` (the source of truth for "what next") before committing to the stage. **Git hygiene:** all phase work runs in a FRESH git worktree off master (`feedback_git_worktrees`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the controller squashes + pushes at stage-close (`feedback_push_to_origin`); each code task runs `gofmt -l` + `golangci-lint` on touched pkgs (`feedback_pertask_gofmt_lint`); the controller verifies `git -C <main> status` clean + the worktree branch unchanged after each task (`feedback_subagent_worktree_detach` / `feedback_subagent_worktree_path_targeting`).

First confirm the state via `git log --oneline -18` (expect HEAD to be the LATEST `next-prompt.txt:` docs-only commit, with the phase-44.2 PLAN squash `b8cc53ca` sitting directly BENEATH the run of `next-prompt.txt:` commits, then the phase-44.2 SPEC squash `8d5cdb4a`, the phase-44.1 IMPL squash `ec05a421`, the 44.1 PLAN `5c7cf481`, the 44.1 SPEC `a7a24d0c`, the phase-44 BRAINSTORM `81afdd59` near the bottom of the window; the tip-stamp commits interleave with each squash, so `81afdd59` sits at ~depth 15; re-read the shas, do NOT assume). Counts at the 44.2 PLAN-done (docs-only — UNCHANGED from the 44.1 IMPL): stat surface **1189** (H2 cluster; non-H2 **1185**) / fixtures **83** (tail `0081-grpc-access-log`; NOTE: count fixtures with `ls -d test/fixtures/[0-9][0-9][0-9][0-9]* | wc -l` — `grep -cE '^[0-9]{4}-'` UNDERCOUNTS by 2, skipping `0007a`/`0007b`) / fuzzers **44** (`^func Fuzz` == 44) / BackendKind tail **38** (`H2GoawayResponder`; the ALS receiver is the driver-owned test-only `test/helpers/accessloggrpc`, NOT a BackendKind) / DECISIONS tail **ADR-0255**, next-free **ADR-0256** (the anticipated 44.2 buffering ADR).

## NEXT STEP — the 44.2 (buffering) IMPL (`superpowers:subagent-driven-development`)

Execute the ~9-task TDD spine in `docs/envoy-go/phases/44-grpc-access-log/PLAN-44.2.md` (plan-document-reviewer APPROVED first-iteration). A fresh worktree off master (`feedback_git_worktrees`); a fresh implementer per task (`feedback_execution_style`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the controller squashes + pushes at stage-close.

The 44.2 leg makes `buffer_size_bytes` / `buffer_flush_interval` (PARSE-ACCEPT-but-INERT at 44.1) LIVE: the `GrpcAccessLogSink.run()` writer goroutine REPLACES the 44.1 fixed one-entry-per-message flush with a BUFFER that accumulates `HTTPAccessLogEntry`s and flushes a BATCH on EITHER the accumulated serialized-entry-byte sum reaching `buffer_size_bytes` OR a `buffer_flush_interval` timer elapsing, whichever first. ANCHORS ADR-0256.

The ~9-task spine (see PLAN-44.2.md for the full per-task steps + code sketches):
- **T1** PROGRESS-44.2 scaffold + baselines + the final ADR-0045 re-check (D-BUF-SPLIT-FINAL, ≈132 prod LoC, one leg).
- **T2** lift `buffer_size_bytes`/`buffer_flush_interval` into `ALSConfig` with **16384 / 1s** defaults (+ the `"time"` import in `bootstrap.go`; the 1s interval is a `time.NewTicker(<=0)` PANIC-GUARD coercion; explicit `buffer_size_bytes: 0` ⇒ flush-every-entry) [TDD].
- **T3** buffer-bearing seed corpus for the EXISTING `FuzzParseHttpGrpcAccessLogConfig` — NO new `^func Fuzz` (fuzzers STAY 44) [fuzz].
- **T4** the buffer machinery in `GrpcAccessLogSink.run()` (`buf`/`bufBytes`/`time.Ticker`/`flush()` size-OR-timer trigger + flush-on-close per AMEND-BUF-5; `s.logsWritten.Add(uint64(len(buf)))` batch-invariant; the `NewGrpcAccessLogSink`/`newGrpcSinkWithCapacity` signature extension; add the `google.golang.org/protobuf/proto` import for `proto.Size`) [TDD, full-package `-race`]. MIND the `buf[:0]` reuse hazard — prefer the test fake taking a defensive copy of `GetLogEntry()` on record (production `Send` serializes synchronously so the reuse is safe there).
- **T5** the `main.go` pass-through (`int(cfg.BufferSizeBytes)`, `cfg.BufferFlushInterval`).
- **T6** the `accessloggrpc` receiver per-message batch exposure (`BatchSizes`/`MaxBatchSize`/`MessageCount` + `Reset` clears `batchSizes`) [TDD].
- **T7** the `0082-grpc-access-log-buffering` differential — copy `0081`, set `buffer_size_bytes: 1048576` (dormant) + `buffer_flush_interval: 1s`, N=16 fired CONCURRENTLY (the coalescence guarantee), cross-side aggregated 7-field payload + subject-side `MaxBatchSize() >= 2` proof (fixtures 83 → 84).
- **T8** `0082` deliberate breaks (`-count=1`, incl the revert-to-fixed-flush break that collapses `maxBatchSize` to 1) + 20/20 flake + full-package `-race`.
- **T9** full 84-dir differential + six-gate + ADR-0256 §Decision/§Consequences + BEHAVIOR_CONTRACT + STATE/ROADMAP + fuzzer reconcile (row 44 leg 44.2 → done; the Observability family STAYS OPEN; next → the 44.3 header-capture SPEC).

Anticipated at the 44.2 IMPL-done: stat **1189** (UNCHANGED — NO new buffering stat, AMEND-BUF-4) / fixtures **84** (`0082-grpc-access-log-buffering`) / fuzzers **44** (UNCHANGED) / BackendKind **38** (UNCHANGED — driver-owned receiver) / DECISIONS **ADR-0256**. ZERO new packages + ZERO new go.mod modules.

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** This session is the 44.2 IMPL (lifecycle-state 2 → 3 → 4 for the 44.2 leg; **row 44 leg 44.2 → `done`** at the six-gate, row 44 STAYS `in-progress` — the Observability family STAYS OPEN). `feedback_git_worktrees` (fresh worktree off master) · `feedback_subagents_no_push` · `feedback_push_to_origin` · `feedback_execution_style` (subagent-driven, fresh implementer per task) · `feedback_pertask_gofmt_lint`.
- **Process anchors:** ADR-0106 (row 44 flips `done` PER-LEG; NO parent rollup) · ADR-0044 (the ADR-0256 §Decision/§Consequences land at THIS IMPL; §Context already drafted in SPEC-44.2 §13) · ADR-0045 (the split gate — re-checked at PLAN, D-BUF-SPLIT-FINAL ≈132 LoC, one leg) · ADR-0080 (strict-reject — UNCHANGED at 44.2, no new reject) · ADR-0255 (the 44.1 as-built the buffer machinery extends) · ADR-0256 (the anticipated 44.2 buffering ADR).
- **Streaming-sink differential discipline:** `reference_streaming_sink_differential_framing` (assert the PAYLOAD aggregated across messages, NOT framing — AMEND-ALS-3, REINFORCED by the per-worker finding) · `reference_differential_grpc_receiver_driver_owned` (the ALS receiver is the driver-owned `test/helpers/accessloggrpc` server the proxy DIALS, NOT a BackendKind) · `reference_docker_probe_bridge_network` (shared-bridge + container-hostname; the host-gateway gotcha) · `reference_concurrency_differential_release_barrier` (poll-to-converge, NEVER `time.Sleep` — the coalescence drive fires CONCURRENTLY so ≥2 entries land in one flush interval; verify 20/20) · `reference_differential_run_selector` (`-run 'TestDifferential/0082'`, NOT bare) · `reference_differential_break_protocol_count1` (`-count=1` on every break + `-race`) · `reference_differential_fullsuite_startup_flake` (a `subject ready: EOF` is a transient startup race — isolate-re-run) · `reference_full_suite_race_after_background_mutator` (the sink's writer goroutine + the NEW `time.Ticker` goroutine are background mutators — re-run the FULL `internal/accesslog` package `-race`) · `reference_fuzzer_count_docs_drift` (re-verify `^func Fuzz` == 44 at the completion task — UNCHANGED, no new fuzzer) · the AMEND-ALS-2 query-less-path constraint carries.

## Read first (in order)
1. THIS FILE — the cold-start router (phase 44.2 PLAN is DONE + pushed; the next action is the 44.2 buffering IMPL).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 44.2 (grpc-access-log) PLAN done`) + the recorded NEXT action + the §12 D-BUF-* resolutions + the count figures (source of truth for "what next").
3. `docs/envoy-go/phases/44-grpc-access-log/PLAN-44.2.md` — the ~9-task TDD spine to EXECUTE (the per-task steps, code sketches, the D-BUF-* resolutions, the verified source seams + line numbers, the discipline block).
4. `docs/envoy-go/phases/44-grpc-access-log/SPEC-44.2.md` — the 44.2 design (the §11 D-ALS-BUFFER pins + AMEND-BUF-1..5; the §3 buffer machinery; the §8 `0082` differential; the §13 ADR-0256 §Context draft).
5. `docs/envoy-go/DECISIONS.md` — ADR-0255 (the 44.1 as-built) + next-free **ADR-0256** (land §Decision/§Consequences at this IMPL beneath the SPEC §13 §Context draft).
6. `docs/envoy-go/ROADMAP.md` — row 44 (`grpc-access-log`, in-progress; leg 44.1 done, 44.2 PLAN done) + the `### Observability family` heading.
7. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the `### Access log — gRPC Access Log Service (ALS) streaming sink` block (the §9 SPEC-44.2 contract-delta target) + the stat-surface 1189 entry.
8. The 44.1 as-built seams the 44.2 IMPL modifies: `internal/accesslog/grpcsink.go` (the `run()` fixed flush 44.2 replaces [`:127`]; the `NewGrpcAccessLogSink` signature [`:60`]; the `logsWritten.Inc()` site [`:169`]), `internal/accesslog/mapping.go` (`buildHTTPAccessLogEntry`), `internal/bootstrap/bootstrap.go` (`ALSConfig` [`:183`] + `parseGrpcAccessLog` [`:349`]), `cmd/envoy-go/main.go` (the `NewGrpcAccessLogSink` call site), `test/helpers/accessloggrpc/accessloggrpc.go` (the receiver to extend with batch exposure), `test/fixtures/0081-grpc-access-log/` (the differential precedent the `0082` copies — driver dir `driver/`), `internal/bootstrap/alsconfig_fuzz_test.go` (the existing fuzzer to extend).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `reference_streaming_sink_differential_framing`, `reference_differential_grpc_receiver_driver_owned`, `reference_docker_probe_bridge_network`, `reference_full_suite_race_after_background_mutator`, `reference_fuzzer_count_docs_drift`, `feedback_execution_style`, `feedback_git_worktrees`).
