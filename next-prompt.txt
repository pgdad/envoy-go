=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, the active phase is fully landed, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action. (The sentinel does NOT apply right now — phase 49 is chartered, its BRAINSTORM is LANDED, and the next stage is its SPEC.)

**STATUS AS OF 2026-07-02: phase 49 (`stats-sink-dogstatsd`) BRAINSTORM is LANDED; the NEXT stage is its SPEC.**
- The phase-49 BRAINSTORM landed: `docs/envoy-go/phases/49-stats-sink-dogstatsd/BRAINSTORM.md` (the 12-section shape; lifecycle-state 0 → 1; docs-only direct-on-master). Row 49 registered `in-progress` in `ROADMAP.md`; STATE.md active-phase header rolled. **Re-verify with `git log --oneline -6` + `git status`; do NOT assume.**
- The BRAINSTORM settled the SCOPE via a 3-question user dialogue: **Q1 tags → IN-SCOPE, reusing `stats.ExtractTags`** (DogStatsd's defining feature over plain statsd — the SAME SN1–SN9+SN4 extraction core the phase-47.2b `labelMapper` calls, reformatted as an inline `|#tag1:val1,...` suffix instead of `metric[].label[]` `LabelPair`s; an MVP with zero tags was rejected as pointless); **Q2 `max_bytes_per_datagram` batching → DEFERRED, envoy-go-STRICT-REJECTED if set** (one-metric-per-datagram only this row, mirroring the phase-48 `tcp_cluster_name`-set reject posture); **Q3 phasing → SINGLE FLAT ROW** (the flush substrate, delta-state pattern, AND tag-extraction core are ALL already landed and proven; ADR-0045 escape-valve UNCONSUMED). Self-answered: transport is UDP `address` only — NOT a real scope choice, since `DogStatsdSink`'s oneof has no `tcp_cluster_name`-equivalent member (CONTRAST `StatsdSink`).
- Counts at BRAINSTORM (re-verify, do NOT assume): stat surface **1200** (non-H2 **1196**) / fixtures **94** / fuzzers **51** / BackendKind **38** / DECISIONS **ADR-0265** (next-free **ADR-0266**).

=> The NEXT action is the **phase 49 (`stats-sink-dogstatsd`) SPEC** (lifecycle-state 1 → 2). NOT the termination sentinel. NOT a new BRAINSTORM.

==============================================

## The NEXT action — the phase 49 (`stats-sink-dogstatsd`) SPEC
Author `docs/envoy-go/phases/49-stats-sink-dogstatsd/SPEC-49.md` (the 14-section SPEC shape — mirror `docs/envoy-go/phases/48-stats-sink-statsd/SPEC-48.md` in git history). Docs-only direct-on-master (the family-row SPEC precedent). Lifecycle-state 1 → 2. ANCHOR the **ADR-0266 §Context DRAFT** (§13; §Decision/§Consequences land in-place at the IMPL per ADR-0044).

**Subject (frozen at the BRAINSTORM):** the bootstrap `stats_sinks[]` `envoy.config.metrics.v3.DogStatsdSink` (UDP `address` variant — the ONLY variant) — a periodic UDP DogStatsd-line-protocol stats sink WITH TAGS over the LANDED phase-47 `Flusher`/`Sink` flush subsystem. A NEW `DogStatsdSink` `Sink` impl in the EXISTING `internal/statssink` package (the phase-48-shaped UDP datagram seam + a NEW line/tag formatter over `stats.ExtractTags`) consuming the SAME `[]*dto.MetricFamily` snapshot the metrics_service/statsd sinks do; lift the DogStatsdSink TypeURL from the `bootstrap.go:430` sibling-reject; main.go's third build loop fans the Flusher out to ALL THREE sinks.

**The SPEC's load-bearing job — EXECUTE the §10 D-DSD-* live pins against `envoyproxy/envoy:contrib-v1.37.2`** (`reference_docker_probe_bridge_network` — a shared Docker bridge + a decode-ran proof; `reference_stats_sink_metrics_service_probe` — name-subset not whole-set, no self-stats). The probe runs in the scratchpad, torn down clean. The pins (from BRAINSTORM §10):
- **D-DSD-DELTA (LOAD-BEARING):** is the reference dog_statsd `|c` a delta-since-last-flush or absolute? INDEPENDENT of phase 48's plain-statsd answer — do NOT assume identical. DECIDES whether `DogStatsdSink` owns its OWN sink-local `deltaState` (a second, independent instance — NOT shared with `StatsdSink`) AND whether `0093` asserts the delta-SUM with a post-convergence stability barrier (`reference_delta_sink_differential_stability_barrier`) or 0089's absolute `value==K` last-seen.
- **D-DSD-TAGS (LOAD-BEARING):** the exact `|#tag1:val1,tag2:val2` grammar — delimiter (`,` between pairs, `:` within a pair — confirm), tag-key format (raw internal segment vs `envoy.`-prefixed vs other — CONTRAST the 47.2b `LabelPair` convention, which was envoy-go's OWN choice, not necessarily the wire truth here), presence/absence of the `|#` suffix on untagged names, tag ordering.
- **D-DSD-NAME:** confirm the residual-name projection (tag-value segments removed, dots kept) matches the 47.2b `ExtractTags` residual exactly, and the exact `prefix` join (`prefix + "." + residual-name`; default `envoy`).
- **D-DSD-LINE:** the exact line format — integer value, the `|c`/`|g` suffixes, one-datagram-per-line (the reference's own `max_bytes_per_datagram`-unset default).
- **D-DSD-LIFECYCLE:** confirm the phase-48 synchronous-write shape carries unchanged (no reason to expect otherwise — SPEC confirms per-phase per project discipline).
- **D-DSD-STATS:** confirm +0 dog_statsd-sink self-stats (the metrics_service/statsd precedent).
- **D-DSD-REJECT:** does the reference accept/reject a missing address, a non-UDP socket protocol, an explicit `max_bytes_per_datagram`? (envoy-go-strict rejects vs reference-parity.)

**Anticipated (the SPEC PINS these; the BRAINSTORM did NOT freeze them):** a NEW `parseDogStatsdSinkConfig` dispatch arm + a `DogStatsdSinkConfig{UDPAddress, Prefix}` struct in `internal/bootstrap` (the reject text now names ALL THREE supported sinks — `reference_strict_reject_sibling_typeurl_gap`) + a NEW `internal/statssink/dogstatsd.go` `DogStatsdSink` emitter (the phase-48-shaped UDP seam + a NEW line/tag-suffix formatter over `stats.ExtractTags`; sink-local `deltaState` IFF D-DSD-DELTA confirms deltas) + the `cmd/envoy-go/main.go` third build loop + `FuzzDogStatsdSinkConfigParse` + the `0093-stats-sink-dogstatsd` cross-side differential (a DRIVER-OWNED UDP DogStatsd-line receiver, extending `test/helpers/statsdrecv` — NOT a BackendKind per `reference_differential_grpc_receiver_driver_owned`; TWO per-side receivers + hard `Close()` per `reference_periodic_sink_differential_two_receivers`). Anticipated counts at the IMPL: stat **1200** (+0) / fixtures **95** (`0093`) / fuzzers **52** (`FuzzDogStatsdSinkConfigParse`) / BackendKind **38** / DECISIONS **ADR-0266** (next-free ADR-0267); ZERO new packages, ZERO new go.mod modules (HAND-ROLLED line protocol). Row 49 STAYS `in-progress` (it flips `done` at the phase-49 IMPL six-gate — NO parent rollup per ADR-0106).

After the SPEC lands, ROLL THIS FILE FORWARD to point at the phase-49 PLAN (mirror a prior phase's post-SPEC PLAN router in git history — e.g. the phase-48 PLAN router).

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** The next stage is the phase-49 SPEC (author `SPEC-49.md`; docs-only direct-on-master). Lifecycle-state 1 → 2. After it, a `spec-document-reviewer` pass (the 47.x/48 precedent — verify every `bootstrap.go`/`internal/statssink` line citation EXACT against HEAD).
- **Subagent-driven execution** (`feedback_execution_style`) in a **fresh worktree** (`feedback_git_worktrees`) for any IMPL; subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit, re-runs the full suite on the frozen HEAD, does the deliberate-break verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). (BRAINSTORM/SPEC/PLAN are docs-only direct-on-master per the family-row precedent — no worktree needed.) Per-task gates: `gofmt -l` + `golangci-lint` on touched pkgs + `go vet` + `go build ./...` (`feedback_pertask_gofmt_lint`).
- **Process anchors:** ADR-0106 (per-leg rows) · ADR-0044 (ADR §Context-at-SPEC / §Decision+§Consequences-at-IMPL) · ADR-0045 (sub-split soft gate — escape-valve unconsumed) · ADR-0080 (strict-reject) · ADR-0064 (`stats_sinks[]` silent-ignore, now a THIRD consumer) · ADR-0060 (no histograms → no timers/`|ms`) · next-free **ADR-0266**.
- **Differential discipline** (carries to `0093`): `-run 'TestDifferential/0093'` NEVER bare (`reference_differential_run_selector`); `-count=1` on every break + `-race` run (`reference_differential_break_protocol_count1`); the full-suite `subject ready: EOF` startup flake is isolatable (`reference_differential_fullsuite_startup_flake`); the Docker bridge + decode-ran proof (`reference_docker_probe_bridge_network`); the driver-owned UDP receiver is NOT a BackendKind (`reference_differential_grpc_receiver_driver_owned`); the live reference is the wire truth (`reference_wire_format_both_sides_see_same_bytes`); periodic-sink two-receivers + hard-Close (`reference_periodic_sink_differential_two_receivers`); delta-sink stability barrier IFF D-DSD-DELTA confirms deltas (`reference_delta_sink_differential_stability_barrier`); the metrics_service probe boundary (`reference_stats_sink_metrics_service_probe`); reuse `differential.HostGatewayIP` verbatim if `0093` needs a literal-IP reference→host receiver address (`reference_host_gateway_ip_docker_desktop`).

## Read first (in order)
1. THIS FILE — the cold-start router (phase 49 `stats-sink-dogstatsd` BRAINSTORM LANDED; the next action is its SPEC).
2. `docs/envoy-go/phases/49-stats-sink-dogstatsd/BRAINSTORM.md` — the frozen scope (UDP-only / tags in-scope / batching deferred / single flat row) + the §10 D-DSD-* probe questions the SPEC must EXECUTE + the §12 closeout.
3. `docs/envoy-go/STATE.md` — the active-phase header (`phase 49 (stats-sink-dogstatsd) BRAINSTORM done`) + the recorded NEXT (`phase 49 SPEC`) + the counts (1200 / 94 / 51 / 38 / ADR-0265).
4. `docs/envoy-go/ROADMAP.md` — row 49 `in-progress` (the table row + the Observability family note); the family STAYS OPEN.
5. `docs/envoy-go/phases/48-stats-sink-statsd/SPEC-48.md` — the 14-section SPEC shape to mirror + the phase-48 statsd sink substrate the dog_statsd sink REUSES (the `Flusher`/`Sink` seam, the UDP-writer shape, the `stats_sinks[]` parse, the main.go wiring, the `delta.go` pattern).
6. `internal/statssink/{flusher,sink,mapping,delta,label,statsd}.go` + `internal/bootstrap/bootstrap.go` (`parseStatsSinks` `:415` + the `:430` sibling-reject) + `cmd/envoy-go/main.go` (the sink-slice + flusher build) — the EXACT seams the SPEC cites.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `reference_stats_sink_metrics_service_probe`, `reference_periodic_sink_differential_two_receivers`, `reference_delta_sink_differential_stability_barrier`, `reference_differential_grpc_receiver_driver_owned`, `reference_strict_reject_sibling_typeurl_gap`, `reference_docker_probe_bridge_network`, `reference_host_gateway_ip_docker_desktop`, `reference_membership_total_vs_healthy_gauge`, plus the differential-discipline references above).
