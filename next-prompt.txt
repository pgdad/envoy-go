START a fresh session for envoy-go. **The phase-42.2b (hedging) SPEC is COMPLETE and LANDED on master.** It executed the §11 D-H-* hedging pins live against `contrib-v1.37.2` (several SURPRISES reshaped the architecture) + anchored the ADR-0251 §Context. The next step is the **42.2b (hedging) PLAN** (the lifecycle valve is SPEC → PLAN for the same leg). Confirm the latest master is the 42.2b SPEC docs-only commit via `git log --oneline -3` (a NEW sha — do NOT assume a literal value). Counts at tip (UNCHANGED by the SPEC): stat surface **1181** / fixtures **78** (tail `0076-per-try-timeout`) / fuzzers **42** / BackendKind tail **36** (`BlockingHoldResponder`) / DECISIONS tail **ADR-0250**, next-free **ADR-0251**.

## What the SPEC firmed (the live-probe SURPRISES — read SPEC §1.1 AMEND-H1..7 + §11 FIRST)

The §11 D-H-{PROTO,HEDGE-BEHAVIOR,RACE,STATS,DIFFERENTIAL} pins (live against `envoyproxy/envoy:contrib-v1.37.2` per `reference_docker_probe_bridge_network`) REFRAMED the BRAINSTORM design. Author `docs/envoy-go/phases/42.2b-hedging/PLAN.md` against the AS-BUILT findings, NOT the BRAINSTORM's pre-probe assumptions:

- **AMEND-H1 (load-bearing — REFUTES the BRAINSTORM):** a hedged per-try-timeout increments `upstream_rq_retry`, NOT `upstream_rq_per_try_timeout` (which STAYS **0** under hedge mode). The `hedgeExecutor` must NOT call `IncUpstreamRqPerTryTimeout`. Live: `/delay/2`+ptt0.5+nr3+hedge ⇒ ptt=0/retry=3/total=4/200; `/delay/10` ⇒ ptt=0/retry=3/limit=1/200@10s.
- **AMEND-H2 (headline):** hedging RECOVERS a slow request (the in-flight original WINS 200) where 42.2a sequential ABANDONS it (504). `retry_limit_exceeded` under hedging = "no more hedges can launch" NOT "final failure" — the request awaits the FIRST in-flight success (first-acceptable-wins).
- **AMEND-H3 (DISSOLVES the BRAINSTORM's headline D-H-RACE hazard):** the reference CANCELS in-flight losers (uncounted — `total=4`/`200=1`). Combined with H1, the `hedgeExecutor` uses a **HEDGE-TRIGGER timer** (on `per_try_timeout`: launch a hedge, leave the original goroutine running to natural completion/cancel) — NOT the 42.2a cancelling-`context.WithTimeout`-deadline-+-race-robust-discriminator. So it NEVER touches `upstream_rq_per_try_timeout`, and a cancelled loser cannot corrupt it. D-H-RACE reduces to goroutine hygiene (cancel + drain + no-leak + `-race`) + first-acceptable-collector correctness.
- **AMEND-H4:** the `initial_requests`/`additional_request_chance` fan-out fields are reference NO-OPS (`initial_requests:3` fires 1 upstream req) ⇒ the fan-out is SUBJECT-SIDE (BUDGET-COUNTED — the BRAINSTORM fork). `initial_requests:0` ⇒ a `gte:1` reject; `:3` ⇒ accepted.
- **AMEND-H5:** NO new stat — surface STAYS **1181** (no `*hedge*` counter; a hedge reuses `upstream_rq_retry`).
- **AMEND-H6 (the differential):** `0077-hedge-on-per-try-timeout` — a SINGLE held `BlockingHoldResponder` host ⇒ original + N hedges all launch (per-try-timeouts at T,…,NT), poll until `upstream_rq_retry==N` + `_retry_limit_exceeded==1`, `/__release` ⇒ first-acceptable 200. Cross-side EXACT (single host, offset-irrelevant): `upstream_rq_retry==N`, **`upstream_rq_per_try_timeout==0`** (the load-bearing assertion + deliberate-break B), `_retry_limit_exceeded==1`, `upstream_rq_total==N+1`, `upstream_rq_200==1`, final 200. Disable the reference route timeout (`timeout:0s` — AMEND-H7; envoy-go has no `RouteAction.timeout`).

## NEXT STEP — the 42.2b (hedging) PLAN

Author `docs/envoy-go/phases/42.2b-hedging/PLAN.md` (`superpowers:writing-plans`) — the bite-sized TDD spine (SPEC §10 anticipates ~13–18 tasks; the FINAL ADR-0045 split-gate re-check, anticipated NO split). Resolve the **D-S422B-1..9** PLAN/IMPL D-questions (SPEC §12): the `hedgeExecutor` placement (`hedge.go` sibling + an extracted shared `runAttempt` helper — the per-attempt unit at risk of 4 copies); the first-acceptable collector mechanics (channel sizing, loser cancel/drain ordering, `-race`); acquire-gates-spawn for the fan-out; whether a hedge sleeps the backoff or fires immediately (D-S422B-6); the subject-side fan-out arm UNIT-tested (default, fixtures 79) vs a `0078` subject-side dir (80); the `upstream_rq_retry_success` accounting under hedging (D-S422B-9 — unprobed, re-probe at IMPL); the `0077` constants (`refContainerListenerPort=19166`).

**Anticipated at the 42.2b IMPL:** fixtures 78 → **79** (`0077` cross-side; fan-out unit-tested) or **80** (optional `0078` subject-side dir); stat surface **1181 UNCHANGED** (+0 — AMEND-H5); BackendKind tail **36** (REUSE `BlockingHoldResponder`); DECISIONS tail ADR-0250 → **ADR-0251**; fuzzers **42** (or 43); ZERO new packages/modules. **Row 42 flips `done` at the 42.2b IMPL** (ALL THREE legs landed — ADR-0106 + `reference_roadmap_split_phase_row_done`); the Upstream-robustness family then has **1** candidate left {per-protocol connection pooling}.

## Honor throughout (the PLAN discipline)

- **Lifecycle = one stage per session.** `feedback_git_worktrees` (a fresh worktree off master) · docs-only commit · `feedback_push_to_origin` (push after a clean docs commit on master) · the plan-document-reviewer loop (precisely-crafted review context, never session history) · the user gate. The PLAN re-runs the FINAL ADR-0045 split-gate re-check.
- **Differential discipline (for the 42.2b IMPL):** `reference_concurrency_differential_release_barrier` (the held `/__release` backend — the hedges fire while the host is held; poll-the-gauge until retry==N + limit==1, never a sleep) · `reference_round_robin_offset_randomized` (single host ⇒ offset-irrelevant for `0077`; the subject-side fan-out asserts the offset-invariant downstream outcome) · `reference_differential_break_protocol_count1` (`-race` + `-count=1` for the concurrent collector) · `reference_differential_fixture_dispatch_constraint` (the subject-side fan-out arm can NOT share the cross-side `0077` dir — unit-test it, or a separate `0078`) · `reference_per_try_timeout_504_reset_classification` (the 42.2a substrate; note AMEND-H1 — the hedge path does NOT reuse the 42.2a abandon-and-count discriminator) · `reference_differential_run_selector` (`TestDifferential/0077-…`) · `reference_fixture_workload_constant_desync` (single-source N/T/port) · `reference_docker_probe_bridge_network`.
- **Subagent-driven IMPL (later):** `feedback_subagents_no_push` (subagents commit locally; the controller squashes + pushes) · `feedback_pertask_gofmt_lint` · `feedback_subagent_worktree_detach` (deliberate-break GIT HYGIENE) · `feedback_subagent_worktree_path_targeting`.
- **ADR/process:** ADR-0044 (§Context at the SPEC [done], §Decision/§Consequences at the IMPL) · ADR-0249 (the 42.1 retry substrate) · ADR-0250 (the 42.2a per-attempt deadline — but AMEND-H1: the hedge path does NOT reuse its abandon-and-count discriminator) · ADR-0106 + `reference_roadmap_split_phase_row_done` (row 42 flips `done` only when ALL THREE legs land).

## Read first (in order)

1. THIS FILE — the cold-start router (the 42.2b SPEC is DONE + LANDED; the next step is the 42.2b PLAN).
2. `docs/envoy-go/phases/42.2b-hedging/SPEC.md` — the 14-section charter: §1.1 AMEND-H1..7 (the live SURPRISES), §3 the hedge architecture (the HEDGE-TRIGGER timer, §3.2), §7 the no-new-stat surface, §8 the `0077` differential, §11 the D-H-* pin table, §12 the D-S422B-1..9 PLAN/IMPL questions, §13 the ADR-0251 §Context draft.
3. `docs/envoy-go/phases/42.2b-hedging/BRAINSTORM.md` — the settled forks (note: the SPEC's AMEND-H1/H3 REFRAME the BRAINSTORM's per_try_timeout-counter + D-H-RACE assumptions — follow the SPEC).
4. `internal/filter/http/router/retry.go` (the 42.1 `retryExecutorH1`/`retryExecutorH2` + the 42.2a per-attempt unit — REUSE the body replay + `do{H1,H2}ClusterAction` + `matches` + the CB-admission/outcome/`retry_budget` seams concurrently; the hedge path does NOT reuse the abandon-and-count discriminator per AMEND-H1) + `router.go` (the `H1ClusterAction` closure switch at `:578` — the hedge branch slots before the retry branch) + `router_h2.go` (the H2 path) + `hcm/config.go` (`parseRetryPolicy` — mirror for `parseHedgePolicy`) + `hcm/actions.go` (`clusterRouteAction.retryPolicy` — add `hedgePolicy`).
5. `docs/envoy-go/STATE.md` — active-phase `phase 42.2b (hedging) SPEC done`; the count figures (1181 / 78 / 42 / 36 / ADR-0250, next-free ADR-0251).
6. `docs/envoy-go/DECISIONS.md` (ADR-0249 + ADR-0250; next-free ADR-0251) + `docs/envoy-go/BEHAVIOR_CONTRACT.md` (the `### Route — request recovery (retries)` subsection — the 42.2b hedging sub-block extends it; the stat-surface block STAYS 1181).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `reference_per_try_timeout_504_reset_classification`, `reference_concurrency_differential_release_barrier`, `reference_round_robin_offset_randomized`, `reference_differential_fixture_dispatch_constraint`, `feedback_subagents_no_push`, `feedback_pertask_gofmt_lint`).
