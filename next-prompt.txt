=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**⚠ CHECK (1) IS NOW PERMANENTLY SILENT — and that is EXPECTED, not a signal to stop.** As of phase 55 every chartered ROADMAP table row is `done`; unchartered work lives in the **prose family paragraphs**, never in rows (ROADMAP §Schema: "Families are not rows"). Check (1) will therefore print nothing until a future BRAINSTORM registers a new `in-progress` row. **The sentinel is an AND of all three checks.** Do NOT read a silent check (1) as "the project is finished" — checks (2) and (3) are what carry the remaining work, and both currently print output.

**SENTINEL RE-CHECKED 2026-07-09 (post-phase-55-IMPL, verified mechanically) — it does NOT fire. Do NOT create `stop`.** Evidence, from the actual command output:
- (1) prints NOTHING (all rows `done` — see the warning above).
- (2) prints: `remaining deferred (not-yet-chartered) candidates: graphite/OTLP-metrics sinks + tracing custom_tags/spawn_upstream_span/http_service/force-trace + the tap filter.` ⇒ the **Observability family STAYS OPEN**. (`the statsd tcp_cluster_name transport` was consumed by phase 55 and removed; `dog_statsd` was ALSO removed — rows 49+50 consumed every field the `DogStatsdSink` proto has, so listing it was stale and would have sent a future session to re-charter done work.)
- (3) prints: `NEVER OPENED: HTTP/3`, `NEVER OPENED: gRPC`, `NEVER OPENED: xDS`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM` ⇒ **FIVE families have never been opened.**
- The **Operational-tooling family** also STAYS OPEN (xDS-sourced dry-validation, an admin-API live-reload-and-validate endpoint, an RTDS/SDS validate companion).

**STATUS AS OF 2026-07-09: phase 55 (`stats-sink-statsd-tcp`) IMPL is DONE and LANDED. Row 55 `done`. The phase-55 lifecycle is COMPLETE.** Squash `d38aa66e`, FF-merged to `master` and pushed. Counts at exit: stat surface **1200 (+0)** · fixtures **100** · fuzzers **52** · BackendKind tail **38** · DECISIONS tail **ADR-0272** (next-free **ADR-0273**).

**THE NEXT ACTION: charter the NEXT PHASE (row 56) — its BRAINSTORM.**

==============================================

## What THIS session does (a BRAINSTORM, lifecycle-state 0 → 1)

1. Re-verify the cold-start baselines (below) against the CURRENT master tip (`git log --oneline -1` — do not assume; expect `d38aa66e`).
2. **Invoke `superpowers:brainstorming`.** This is a creative/charter stage — the skill is mandatory before any plan-mode or design work.
3. **Pick the next deferred candidate WITH THE HUMAN.** The loop RE-OPENS here; the human picks. Do not pick unilaterally. Present the real, currently-open options:
   - **Observability family (OPEN)** — `graphite` stats sink · OTLP-metrics stats sink · tracing `custom_tags` / `spawn_upstream_span` / `http_service` / force-trace · the **tap filter**.
   - **Operational-tooling family (OPEN)** — xDS-sourced dry-validation · an admin-API live-reload-and-validate endpoint · an RTDS/SDS validate companion.
   - **Open a NEW family** — HTTP/3 + QUIC · gRPC · **xDS / dynamic config** · Runtime + hot restart · WASM host. (Note: xDS/CDS is now an *explicit* forward-dependency — ADR-0272 records that phase 55's static-cluster `tcp_cluster_name` reject must MOVE to the cluster manager when CDS lands.)
4. **Register the new row `in-progress` AT the BRAINSTORM commit** — the ROADMAP §Schema invariant. If you open a new family, **add its row-summary slug to the sentinel's check-(3) list in this file.**
5. Run the 4–6-question user dialogue, settle the envelope against the **ADR-0045 split gate**, anticipate the counts, and write `docs/envoy-go/phases/56-<slug>/BRAINSTORM.md`.
6. Land the ADR-0052 bundle: advance `STATE.md`, flip the ROADMAP row, then **ROLL THIS FILE FORWARD to the phase-56 SPEC.**
7. Docs-only stage. **No production code.** Fresh worktree `.worktrees/phase-56-brainstorm`, branch `phase-56-<slug>-brainstorm`.

## Honor throughout (the discipline, unchanged)
- **Lifecycle = one stage per session.** This session: the phase-56 BRAINSTORM ONLY.
- **Subagent-driven execution** (`feedback_execution_style`) in a **fresh worktree** (`feedback_git_worktrees`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit, re-runs gates on the frozen HEAD, does deliberate-break/liveness verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). Per-task gates: `gofmt -l` + `golangci-lint` + `go vet` + `go build ./...` (`feedback_pertask_gofmt_lint`).
- **COMMIT BEFORE YOU BREAK.** `git restore <file>` reverts to **HEAD**, not to "before the break". Three phase-55 subagents destroyed their own uncommitted work this way. Finish → gate → **commit** → only then break, restore, and re-run with `-count=1`.
- **Path targeting** (`feedback_subagent_worktree_path_targeting`): pin the canonical worktree root in every dispatch, give worktree-relative paths, and after each task verify `git -C /home/esa/git/envoy-go status --porcelain` is EMPTY (the main checkout must never be written; `next-prompt.txt` is the ONE exception — it is gitignored).
- **GIT HYGIENE** (`feedback_subagent_worktree_detach`): revert a deliberate break with `git restore <file>` ONLY. Never `git checkout <sha>`, never `git commit --amend`. Re-verify `git branch --show-current` after every break. Deliberate-break runs ALWAYS use `-count=1` (`reference_differential_break_protocol_count1`).
- **Differential `-run` selector** (`reference_differential_run_selector`): `go test -run '<NNNN>'` matches ZERO subtests and reports a vacuous PASS. Always `-run 'TestDifferential/<NNNN>-<slug>'`.
- **Full-suite runs must be UNLOADED.** Phase 55's Gate 3 flaked on an untouched package (`internal/filter/hcm/h2` `TestServerConn_TinyWindowDelivery`, i/o timeout) purely because it ran concurrently with two `-race` suites. Isolated on the frozen HEAD it passed 5/5 in 0.073 s. Do not run the unit suite and a `-race` suite at the same time, and do not accept a timing failure in an untouched package without isolate-re-running it.
- **Process anchors:** ADR-0106 · ADR-0044 (ADR §Decision+§Consequences at IMPL; §Context drafted at SPEC §13) · ADR-0045 (split soft gate) · ADR-0080 (byte-stable rejects) · ADR-0052 (atomic six-gate bundle) · ADR-0227 (reference `envoyproxy/envoy:contrib-v1.37.2`) · next-free **ADR-0273**.
- Re-verify baselines fresh at cold-start against current HEAD:
  - `ls -d test/fixtures/[0-9][0-9][0-9][0-9]* | wc -l` → expect **100** (tail `0098-stats-sink-statsd-tcp`; next fixture dir `0099-…`)
  - `grep -rh '^func Fuzz' --include='*.go' --exclude-dir=.worktrees . | wc -l` → expect **52** (reconcile per `reference_fuzzer_count_docs_drift`)
  - `grep -nE '^## ADR-0[0-9]+' docs/envoy-go/DECISIONS.md | tail -1` → expect **ADR-0272** (next-free **ADR-0273**)
  - `grep -n "BackendKind = " test/differential/fixture/fixture.go | tail -1` → expect **38**
  - stat surface → **1200**

## Carried forward from phase 55 — read before designing anything adjacent

- **ADR-0272 leaves ONE named forward-dependency:** envoy-go has **no CDS**, so `parseStatsdTCPArm` validates `tcp_cluster_name` against `static_resources.clusters` at PARSE time (which *is* boot time today). **When the xDS family opens and CDS lands, that reject MUST move to the cluster manager.** `cmd/envoy-go/main.go` already carries a defensive `cm.Get` fatal + a `// when CDS lands, this static check must move` marker.
- **`Cluster.DialSink` is STATS-SINKS-ONLY.** It takes no `max_connections` permit and increments no `upstream_cx_*`. Any data-plane caller wanting a connection MUST use `Dial`. Its doc comment says so; enforce it in review.
- **`internal/statssink` must never import `internal/cluster`.** The seam is a `DialFunc`. Guard: `go list -deps ./internal/statssink/ | grep 'envoy-go/internal/cluster'` prints nothing.
- **`TCPStatsdSink` is the statsd sinks' FIRST background mutator** ⇒ a `-run`-subset `-race` is not evidence for `internal/statssink`. Full-package only.
- **Coverage boundaries phase 55 accepted (boundaries, not gaps):** the line-aligned resume is UNIT-proven, not differentially proven (`0098`'s receiver never dies); cross-side `ConnCount` equality is infeasible **by the histogram boundary**; the emitted line SET differs structurally (the reference emits only USED stats) so only NAMED SUBSETS are asserted; `membership_total == 1` is asserted but targeted by no deliberate break.

## Read first (in order)
1. THIS FILE — the cold-start router (a phase-56 BRAINSTORM is next; the human picks the candidate).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 55 IMPL done`) + full history. The phase-55 entry records the deliberate-break protocol that **refuted the PLAN's own prediction**; read it before trusting any future PLAN's vacuity analysis.
3. `docs/envoy-go/ROADMAP.md` — §Schema, the family paragraphs, and the deferred-candidate sentences that define what is still open.
4. `docs/envoy-go/DECISIONS.md` — **ADR-0272** (the tail) for the statsd TCP transport; ADR-0045/0052/0080/0106 for process.
5. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the phase-55 block.
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline, and the `reference_*` traps. **Two NEW lessons from phase 55 are worth writing down if they are not already there:**
   - *A deliberate break can fail at the WRONG assertion.* Phase 55's break (b) failed earlier than the PLAN predicted, masking the very assertion the PLAN added to make it reliable. Always confirm **which** assertion fired — and when a break masks another, add an ISOLATING break for the masked one.
   - *`t.Fatalf` in a multi-property test makes later assertions unreachable dead code.* Phase 55's Task 7 had three properties that no break had ever exercised. Use `t.Errorf` for independent properties; reserve `t.Fatalf` for a broken harness precondition.
