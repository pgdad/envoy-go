=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-CHECKED 2026-07-13 (after the phase-61.1 IMPL landed, run MECHANICALLY) — it does NOT fire. Do NOT create `stop`.** State:
- (1) prints `NOT DONE: row 61` — ONE `in-progress` row. Row 61 `http3-downstream-listener` is the HTTP/3-family opener; leg 61.1 (QUIC/UDP listen substrate) now LANDED (BRAINSTORM + SPEC + PLAN + IMPL), but legs 61.2 (H3 codec + HCM) and 61.3 (fixture + harness) are NOT yet done. Row 61 flips `done` only when ALL THREE legs land (ADR-0106). This ALONE blocks `stop`.
- (2) prints THREE live "candidates:" sentences — HTTP/3, xDS, and Observability ⇒ three families STAY OPEN. (`reference_sentinel_deferred_sentence_live_vs_historical`.)
- (3) prints `NEVER OPENED: gRPC`, `Runtime`, `WASM` ⇒ THREE families never opened. Operational-tooling also STAYS OPEN.

All three checks print ⇒ the sentinel does NOT fire.

**STATUS AS OF 2026-07-13: the phase-61.1 IMPL LANDED (envoy-go's FIRST UDP/QUIC downstream listen substrate + the FIRST external go.mod module `quic-go v0.54.1`; handshake-only, ALPN `h3` + TLS 1.3 proven subject-side; ROADMAP row 61 STAYS `in-progress`; DECISIONS tail ADR-0280 → ADR-0279).** The NEXT IMPLEMENTABLE STAGE is the **phase-61.2 PLAN** (`http3-h3-codec-hcm` leg 61.2 — the H3 codec + HCM adapter; decompose SPEC-61 leg 61.2 into a TDD spine; lifecycle-state 2 for the 61.2 leg — DOCS-ONLY). Advancing the banked HTTP/3 split's next leg is the roller's self-pick per the standing directive. Row 61 flips `done` only when ALL three legs (61.1/61.2/61.3) land (ADR-0106).

**What the 61.1 IMPL delivered (for CONTEXT only; leg 61.1 is CLOSED):**
- `internal/listener/quic.go` (NEW — the sole quic-go import) `startQUIC`/`quicAcceptLoop`/`serveQUICConnection`/`quicTLSConfig`; `internal/listener/manager.go` the `listenerKind` discriminant (`udp_listener_config.quic_options` presence) + the QUIC transport-socket branch + the mandatory-TLS config-parity reject + `validateQUICOptions` (the ADR-0080 strict-reject roster) + the `transport_protocol "quic"` lift + `udpConn`/`quicCloser` fields + `closeBind()` + kind-aware `Start`/`Stop`/`Listeners`; `internal/tls.NewQUICDownstreamConfig` (unwrap `QuicDownstreamTransport` → inner `DownstreamTlsContext` → shared `commonTLSContextToConfig`, ALPN h3; `internal/tls` stays quic-go-free); `go.mod`/`go.sum` gain `github.com/quic-go/quic-go v0.54.1` (the FIRST external module; `go 1.23.0` intact); a subject-side handshake integration test (local quic-go client, ALPN `h3` + TLS 1.3, `-race`-clean); BEHAVIOR_CONTRACT HTTP/3 section; ADR-0279.
- The HTTP/3 FAMILY STAYS OPEN. Remaining deferred candidates (ROADMAP HTTP/3 live sentence): upstream H3, alt-svc, 0-RTT, h3spec, QUIC robustness, SNI-dispatched multi-chain QUIC — plus legs 61.2 (H3 codec + HCM) and 61.3 (differential fixture + harness surgery) still to land for row 61.

**The phase-61.2 PLAN scope (RE-READ SPEC-61 §3.0/§3.3/§10 + the landed 61.1 seams at session start; `git fetch` first):**
- **This is a DOCS-ONLY stage** — decompose SPEC-61's 61.2 leg (the H3 codec + HCM adapter) into a TDD task spine (`docs/envoy-go/phases/61-http3-downstream-listener/PLAN-61.2.md`) + a `PROGRESS-61.2.md` scaffold. NO production `.go` changes; row 61 STAYS `in-progress`.
- **Leg 61.2 (per SPEC-61 §3.3):** a THIRD dispatch arm `internal/filter/hcm/h3` (sibling of `internal/filter/hcm/h2`) adapting quic-go's `http3.Server` `(*http.Request, http.ResponseWriter)` into the EXISTING shared filter-chain → router → `ActionResponse` path — modeled on the H2 arm `WriteH2` (which is already `net.Conn`-free / codec-agnostic; RE-DERIVE its current site — SPEC cited `h2dispatch.go:269` against an older tip). LIFT the `codec_type HTTP3` reject (`internal/filter/hcm/config.go:240`, the `is not supported in phase 05.1` string — RE-DERIVE; UPDATE the reject-assertion test) + add the HTTP3-on-non-QUIC-listener config-parity reject (SPEC §11 arm reject-B). Add `emitAccessLogH3` (`accesslog_emit.go`, `Protocol="HTTP/3"`). WIRE the 61.1 `serveQUICConnection` (currently handshake-only) into an `http3.Server` serving the H3 request into the new arm. Serve a GET → routed backend, verified SUBJECT-side by a local quic-go `http3.Transport` client (FREE now the module landed at 61.1). A per-leg ADR (the codec-arm seam) OR fold into ADR-0279 — the PLAN/IMPL decide (next-free **ADR-0281**).
- **Leg 61.3 (for CONTEXT — NOT this PLAN):** the differential fixture + harness UDP/H3-client surgery (a shared `test/helpers/h3.go` `H3RoundTrip` + the harness's first non-TCP transport; the `0102-http3-downstream-get` cross-side fixture; three container-starters gain `/udp` exposure). The 61.2 PLAN decomposes ONLY 61.2.

**ADR reservation ledger (provisional; each finalized at its IMPL in landing order):** ADR-0277 = phase-59 (LANDED) · ADR-0278 = xDS 60.1 (LANDED) · ADR-0280 = xDS 60.2 (LANDED) · ADR-0279 = HTTP/3 61.1 (LANDED) · **next-free unreserved ADR-0281** (the 61.2 codec-arm decision MAY consume it, or fold into ADR-0279 — the 61.2 PLAN/IMPL decide).

**Current counts (VERIFIED at the phase-61.1 IMPL six-gate against the frozen squash HEAD — RE-VERIFY at session start, `git fetch` first):** fixtures **105** (tail `0103-xds-sds-server-cert`) · fuzzers **55** · DECISIONS tail **ADR-0279** (next-free **ADR-0281**) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** · new go.mod modules **1** (`quic-go v0.54.1`, landed at 61.1). Phase-61.2 is a DOCS-ONLY PLAN ⇒ all counts UNCHANGED at that stage; at the 61.2 IMPL expect fixtures/fuzzers/module UNCHANGED (the cross-side fixture is 61.3), stat surface **1201 → 1201 + N** (the H3 HCM request stats — IMPL-pinned), +1 or +0 packages (`internal/filter/hcm/h3`).

**Locating the phase-61.1 IMPL stage squash** (a router cannot name its own sha — identify by SUBJECT, never by position):
```sh
git log --oneline --grep='^phase 61.1 (http3-quic-substrate) IMPL' -1
```

**THE NEXT ACTION: the phase-61.2 PLAN (lifecycle-state 2 for the 61.2 leg — decompose SPEC-61's H3-codec/HCM-adapter leg into a TDD spine + PROGRESS scaffold; docs-only; row 61 STAYS `in-progress`).** After the 61.2 PLAN lands, the roller's next stage is the phase-61.2 IMPL, then the 61.3 PLAN/IMPL (which flips row 61 `done`). The sentinel governs termination, not this list.

==============================================

## What THIS session does (the phase-61.2 PLAN)

Decompose SPEC-61's leg 61.2 (the H3 codec + HCM adapter — a third dispatch arm) into a TDD task spine, subagent-driven where useful (`feedback_execution_style`), in a FRESH worktree off master (`.worktrees/phase-61.2-plan`, branch `phase-61-http3-h3-codec-hcm-plan`, per `feedback_git_worktrees`). This is a SINGLE STAGE (the PLAN — docs-only). NO production `.go` changes; row 61 STAYS `in-progress` (flips `done` only when ALL three legs land, ADR-0106).

**Deliverables:**
- `docs/envoy-go/phases/61-http3-downstream-listener/PLAN-61.2.md` — a TDD task spine for leg 61.2 (the `internal/filter/hcm/h3` codec arm modeled on `WriteH2`; the `http.ResponseWriter → writeH3Reply` adapter; lifting the `codec_type HTTP3` reject + the HTTP3-on-non-QUIC config-parity reject + UPDATE the reject test; `emitAccessLogH3`; wiring the 61.1 `serveQUICConnection` into an `http3.Server`; a subject-side H3 GET→200 test with a local quic-go `http3.Transport` client). RE-DERIVE every `file:line` against the master tip (`feedback_brief_citations_not_evidence`) — SPEC-61 §12 has the source roster (the H2 dispatch seam `WriteH2`, the `codec_type HTTP3` reject, `accesslog_emit.go`), but the phase-61.1 IMPL shifted numbers (esp. in `internal/listener` and around the QUIC serve path) — VERIFY.
- `docs/envoy-go/phases/61-http3-downstream-listener/PROGRESS-61.2.md` — baselines + task checklist + design pins + anticipated exit counts (stat surface +N for the H3 request stats; +1/+0 packages; the codec-arm ADR decision).

## Honor throughout (the discipline, unchanged)
- **Lifecycle = one stage per session.** This session: the phase-61.2 PLAN ONLY. After it lands: the phase-61.2 IMPL (or the roller's self-pick; the sentinel governs).
- **Process anchors:** ADR-0044 (ADR §Decision/§Consequences land at the IMPL) · ADR-0045 (the split escape-valve — CONSUMED for row 61) · ADR-0080 (distinct per-arm reject substrings) · ADR-0106 (row 61 flips `done` only when ALL legs land).
- **`next-prompt.txt` IS TRACKED** (`reference_next_prompt_tracked_despite_gitignore`) — edit it inside the stage worktree and fold into the stage commit; locate commits by SUBJECT (`git log --grep`), never by position.
- **Deferred maintenance (carried; ride until the relevant row reopens the file):** the phase-57 statssink notes F-1/F-2/F-3 (fold into the next `udp.go`/statssink-driver-touching row — none through 61.1); the phase-60.2 final-review MINOR roll-up (T2/T5/T6/T7 — cosmetic/coverage, fold when the SDS files next reopen); the phase-61.1 review MINORS (all within the explicitly-deferred multi-chain/`default_filter_chain` QUIC scope — none is a security/resource/crash risk; the final whole-branch review confirmed MERGE-READY): M6-1 `quicAcceptLoop` returns on any non-shutdown Accept error with no TCP-style backoff (revisit when 61.2 adds real serving); M6-2 `serveQUICConnection` discards ctx (61.2 wires the ctx-honoring serve path); M-FB1 the QUIC transport-socket decode + mandatory-TLS reject are wired only into the `filter_chains[]` loop, not `default_filter_chain` (a QUIC listener declared SOLELY via `default_filter_chain` mis-decodes to a generic type_url reject rather than the clean mandatory-TLS message — no plaintext QUIC possible, `startQUIC` still fails loudly; a 61.2/robustness pickup); M-FB2 `quicTLSConfig()`'s "first non-nil over chainByName" is map-iteration nondeterministic (harmless for the single-chain slice; the wording overstates determinism — a 61.3 SNI-dispatch pickup). M6-1/M6-2 are natural 61.2 pickups (61.2 replaces the handshake-only `serveQUICConnection` body); M-FB1/M-FB2 fold into the 61.2/61.3 multi-chain work.

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-61.2 PLAN is next; SPEC-61 + the 61.1 IMPL are banked; leg 61.1 is CLOSED).
2. `docs/envoy-go/phases/61-http3-downstream-listener/SPEC.md` — §3.3 (the H3 codec + HCM adapter, the central DE-RISKING), §3.0 (the split), §10 (leg-61.2 task list), §11 (arm reject-B HTTP3-on-non-QUIC), §12 (the source roster — RE-DERIVE against the tip), §13 (ADR-0279 §Context, LANDED).
3. `docs/envoy-go/phases/61-http3-downstream-listener/PLAN-61.1.md` + `PROGRESS-61.1.md` — the landed 61.1 seams (the `kind` discriminant, `serveQUICConnection` handshake-only body to be extended, the quic-go confinement in `internal/listener/quic.go`).
4. `docs/envoy-go/DECISIONS.md` — the tail ADR-0279 (61.1, just landed) + ADR-0044/0045/0080/0106 (the process anchors); ADR-0281 is next-free.
5. `docs/envoy-go/ROADMAP.md` — row 61 `in-progress`; the HTTP/3-family row + the LIVE HTTP/3 deferred sentence.
6. `docs/envoy-go/STATE.md` — the active-phase header (phase 61.1 IMPL done; NEXT = the phase-61.2 PLAN).
7. The seam source for the PLAN's `file:line` re-derivation: `internal/filter/hcm/h2dispatch.go` (`WriteH2` — the codec-agnostic dispatch precedent) · `internal/filter/hcm/filter.go` (the `Handle` codec fork) · `internal/filter/hcm/config.go` (the `codec_type HTTP3` reject ~`:240`) · `internal/filter/hcm/accesslog_emit.go` (the `emitAccessLog`/`emitAccessLogH2` siblings) · `internal/listener/quic.go` (the 61.1 `serveQUICConnection` to wire an `http3.Server` into) · `internal/filter/hcm/h2/` (the H2 codec package, the sibling to model `h3` on).
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the `reference_*` traps (esp. `reference_ondestroy_fires_once_encoder_unreachable`, `feedback_brief_citations_not_evidence`, `reference_next_prompt_tracked_despite_gitignore`, `reference_roadmap_split_phase_row_done`, `reference_sentinel_deferred_sentence_live_vs_historical`, `reference_fatalf_makes_assertions_unreachable`).
