RESUME a fresh session for envoy-go (router last updated 2026-06-26; the **phase 45.1 (`otlp-access-log` core) PLAN is DONE and LANDED** — `docs/envoy-go/phases/45-otlp-access-log/PLAN-45.1.md` written in the house TDD-task-spine shape (12 tasks, mirrors `PLAN-44.1.md`), the §12 D-OTLP-* PLAN questions ALL resolved, the plan-document-reviewer loop run (1 BLOCKER found + FIXED — the go.mod transitive→direct sequencing — then re-APPROVED), ROADMAP row 45 PLAN-annotated (STAYS `in-progress` — 45.1 is NOT the final leg), STATE.md rolled to PLAN-done, committed direct-on-master (`48d6b189`, docs-only) + pushed. **The next action is the phase 45.1 (core) IMPL — subagent-driven, in a fresh worktree off master.**). HEAD is at the **next-prompt.txt roll commit** atop the **phase-45.1 PLAN commit `48d6b189`** (subject `phase 45.1 (otlp-access-log core) PLAN: the 12-task TDD spine …`), which sits atop the prior next-prompt roll `9989a7a4` + the **phase-45.1 SPEC `35070bc3`** + the docs-only tip-stamp `95dc14e2` + the **phase-45 BRAINSTORM `97d57ae7`** (the stable anchor), then the **phase-44.3 IMPL squash `4abf2c49`**. Re-read the shas with `git log --oneline -12`, do NOT assume — any number of docs-only `next-prompt.txt:` commits may stack ATOP the PLAN commit. Counts are UNCHANGED from the SPEC (the PLAN is docs-only): stat surface **1189** (non-H2 **1185**) / fixtures **85** (`ls -d test/fixtures/[0-9][0-9][0-9][0-9]* | wc -l` == 85; tail `0083-grpc-access-log-headers`) / fuzzers **44** (`grep -rh '^func Fuzz' --include='*.go' . | wc -l` == 44) / BackendKind tail **38** / DECISIONS tail **ADR-0257**, next-free **ADR-0258**. NOTE: the working-tree `go.sum` carries a pre-existing unstaged modification (the otlp/proto hash lines from the SPEC probe session) — the IMPL reconciles it via `go mod tidy` at Task 2 (NOT a stray edit to revert).

**ROW 44 (`grpc-access-log`) is `done`**; **ROW 45 (`otlp-access-log`) is `in-progress`** (the PLAN-45.1 ANNOTATED it but it STAYS `in-progress`; it flips `done` at the 45.2 IMPL — the FINAL leg, per-leg ADR-0106 + `reference_roadmap_split_phase_row_done`). The **Observability FAMILY STAYS OPEN** (OTLP access log [active] / OTel-Zipkin tracing / stats sinks / tap filter remain rows). **The active action is the phase 45.1 (core) IMPL** — the FIRST leg of the by-concern 2-leg OTLP split, executed SUBAGENT-DRIVEN (`feedback_execution_style`) in a FRESH worktree off master (`feedback_git_worktrees`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the controller squashes + pushes at stage-close (`feedback_push_to_origin`). THEN roll this router to the 45.2 (operator engine) SPEC.

First confirm state via `git log --oneline -12` (expect HEAD at the next-prompt.txt roll over the phase-45.1 PLAN commit `48d6b189` over the SPEC `35070bc3` + the BRAINSTORM `97d57ae7`; counts as above; re-read the shas, do NOT assume).

## NEXT STEP — the phase-45.1 (core) IMPL (subagent-driven per `feedback_execution_style`)
Invoke `superpowers:subagent-driven-development`: (1) a fresh worktree off master per `feedback_git_worktrees` (the PLAN doc already landed on master); (2) execute the **12-task TDD spine in `PLAN-45.1.md`** — a fresh implementer subagent per task + two-stage review between tasks; subagents commit LOCALLY only (`feedback_subagents_no_push`), each code task ends with per-task gofmt + golangci-lint on the touched pkgs (`feedback_pertask_gofmt_lint`); (3) at stage-close the controller squashes the worktree branch into ONE atomic commit (subject `phase 45.1 (otlp-access-log) IMPL: the core OTLP access-log sink — …`), verifies the main checkout is clean, pushes to origin (`feedback_push_to_origin`), removes the worktree. THEN roll this router to the 45.2 (operator engine) SPEC.

## THE CHARTERED PHASE-45.1 BUILD (from the LANDED PLAN-45.1.md — do NOT re-derive; the §12 D-OTLP-* are RESOLVED in the PLAN; the as-built seams + embedded code sketches are PLAN-time-verified against master `9989a7a4`/`48d6b189`)

The 12-task spine (each TDD — failing test → run-fail → minimal impl → run-pass → per-task gates → commit):
- **T1** PROGRESS-45.1 scaffold + baselines (1189/85/44/38; go.mod otlp == 0) + the final ADR-0045 split re-check (D-OTLP-SPLIT-FINAL, ~305 prod LoC, one leg).
- **T2** the `OTLPConfig` parse arm (`internal/bootstrap/bootstrap.go`): the `open_telemetry/v3` blank-import (**the FIRST go.mod touch — `go mod tidy` lands `go.opentelemetry.io/proto/otlp` `// indirect`; commit `go.mod`/`go.sum` or the build fails `-mod=readonly`**) + the `otlpAccessLogTypeURL` const + the `OTLPConfig` struct + `Bootstrap.OTLPConfigs` + the FACTORED shared `parseCommonGrpcAccessLogConfig(common, idx, sinkLabel)` helper (`sinkLabel="grpc ALS"` preserves the 44.x reject wording BYTE-FOR-BYTE — existing tests are the green guard) + the OTLP per-sibling STRICT-REJECTs (`body`/`attributes`/`resource_attributes` [45.2] + `formatters`) + `disable_builtin_labels` CONSUMED + `stat_prefix` PARSE-ACCEPT-INERT [TDD].
- **T3** `FuzzParseOpenTelemetryAccessLogConfig` (fuzzers 44 → 45; reconcile per `reference_fuzzer_count_docs_drift`).
- **T4** the `OTLPLogsClient` UNARY typed wrapper (`internal/grpcclient/grpcclient.go`, the `ALSClient` shape but `Export` unary) + the go.mod `// indirect`→direct PROMOTION (the first DIRECT `collogspb` import; `go mod tidy -diff` EMPTY after) [TDD].
- **T5** the built-in mapping (`internal/accesslog/otlpmapping.go`): `buildLogRecord` (`time_unix_nano` ONLY) + `buildResource` (the 4 labels `{log_name,zone_name,cluster_name,node_name}`; `disableBuiltinLabels` ⇒ empty Resource) + `buildExportRequest` (one ResourceLogs → one empty-scope ScopeLogs → the batch) [TDD table-driven].
- **T6** the `OTLPAccessLogSink` (`internal/accesslog/otlpsink.go`): the `otlpClient` seam + the bounded channel + writer goroutine + the 44.2 size/interval/close BUFFER → per-`Export` batch + retry-once + idempotent Close [TDD, FULL-package `-race` — the writer goroutine is a background mutator per `reference_full_suite_race_after_background_mutator`].
- **T7** `RegisterOTLPSinkCounters` (`access_logs.open_telemetry_access_log.{logs_written,logs_dropped}`; +2 → 1191; STATIC names) + a registration test [TDD].
- **T8** the main wiring (`cmd/envoy-go/main.go`): **HOIST `dialer := grpcclient.New(cm)` out of the `if len(bs.ALSConfigs) > 0` block** to fire when EITHER family is configured (an OTLP-only boot must build it) + per-`OTLPConfig` `NewOTLPLogsClient`→`NewOTLPAccessLogSink` + counter registration + the FULL bootstrap node `bs.Proto.GetNode()` (D-OTLP-NODE — for `zone_name`; the ALS minimal node UNCHANGED).
- **T9** the driver-owned `test/helpers/otlplogs` receiver (the `accessloggrpc` precedent; an `Export`-unary `LogsServiceServer` accumulating `LogRecord`s + per-`ResourceLogs` `Resource.attributes`) [TDD, `-race`].
- **T10** the `0084-otlp-access-log` differential (copy `test/fixtures/0081-grpc-access-log/`; H1 down → `HTTPFixedBody` backend; HCM `access_log[]` OTLP → h2c `c_otlp` cluster → the driver-owned receiver; poll-to-converge NEVER sleep per `reference_concurrency_differential_release_barrier`; assert record count + `time_unix_nano` presence + the 4 Resource label keys + `log_name` value + subject `logs_written == N`; framing UNasserted per `reference_streaming_sink_differential_framing`; fixtures 85 → 86; BackendKind STAYS 38 — driver-owned receiver per `reference_differential_grpc_receiver_driver_owned`) + the `disable_builtin_labels` empty-Resource SUBJECT unit test (one fixture dir = one runner branch per `reference_differential_fixture_dispatch_constraint`).
- **T11** `0084` deliberate breaks (`-count=1` per `reference_differential_break_protocol_count1`; selector `-run 'TestDifferential/0084'` per `reference_differential_run_selector`) + 20/20 flake + FULL-package `-race`.
- **T12** full 86-dir differential + six-gate + ADR-0258 §Decision/§Consequences + BEHAVIOR_CONTRACT (1189→1191) + STATE/ROADMAP (row 45 STAYS `in-progress`; family STAYS OPEN) + fuzzer reconcile (44→45) + the completion bundle.

Anticipated at the 45.1 IMPL: stat **1191** (+2; non-H2 **1187**) / fixtures **86** (`0084-otlp-access-log`) / fuzzers **45** (`FuzzParseOpenTelemetryAccessLogConfig`) / BackendKind **38** (UNCHANGED — driver-owned receiver) / DECISIONS **ADR-0258** / **+1 go.mod module** (`go.opentelemetry.io/proto/otlp` transitive→direct; `go mod tidy -diff` EMPTY afterward). ZERO new packages.

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** This stage is the phase-45.1 IMPL (lifecycle-state 3 → done for the 45.1 leg). `feedback_execution_style` (subagent-driven) · `feedback_git_worktrees` (fresh worktree) · `feedback_subagents_no_push` (subagents commit locally; controller pushes at stage-close) · `feedback_subagent_worktree_detach` + `feedback_subagent_worktree_path_targeting` (GIT HYGIENE blocks + controller re-verifies the clean main checkout + worktree-relative paths after each task) · `feedback_pertask_gofmt_lint` · `feedback_push_to_origin`.
- **Process anchors:** ADR-0106 (per-leg roadmap rows; row 45 flips `done` only at the 45.2 IMPL) · ADR-0045 (split-gate, re-checked at the PLAN — one leg) · ADR-0041/0080 (silent-ignore vs strict-reject for access-log type-URLs) · ADR-0016 (proto blank-import registry) · ADR-0255/0256/0257 (44.x as-built) · next-free **ADR-0258** (45.1 OTLP core, §Context anchored in SPEC-45.1 §13; §Decision/§Consequences land at this IMPL) then **ADR-0259** (45.2 operator engine).

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-45.1 PLAN is DONE; the next action is the 45.1 core IMPL — subagent-driven via `PLAN-45.1.md` → stage-close squash + push → roll to the 45.2 SPEC).
2. `docs/envoy-go/phases/45-otlp-access-log/PLAN-45.1.md` — the 12-task TDD spine (the Orientation zero-context brief + the PLAN-time-verified seams + the §12 D-OTLP-* resolutions + the go.mod sequencing fix).
3. `docs/envoy-go/phases/45-otlp-access-log/SPEC-45.1.md` — the chartered core leg (the §1.1 AMEND-OTLP-* empirical corrections + the §3 build + the §11 D-OTLP-* live-probe PINS + the ADR-0258 §Context draft §13).
4. `docs/envoy-go/STATE.md` — the active-phase header (`phase 45.1 (otlp-access-log) PLAN done`) + the recorded NEXT (the 45.1 IMPL).
5. `docs/envoy-go/ROADMAP.md` — row 45 (`otlp-access-log`, `in-progress`, PLAN-annotated) + the `### Observability family` heading.
6. `docs/envoy-go/phases/44-grpc-access-log/PLAN-44.1.md` — the house PLAN/IMPL shape + the as-built ALS subsystem the OTLP row reuses (`internal/accesslog/grpcsink.go`, `internal/grpcclient/grpcclient.go`, `internal/bootstrap/bootstrap.go`, `test/helpers/accessloggrpc/`, `test/fixtures/0081-grpc-access-log/`).
7. `docs/envoy-go/DECISIONS.md` — ADR-0255/0256/0257 (the 44.x as-built) + next-free **ADR-0258**.
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `feedback_execution_style`, `feedback_git_worktrees`, `feedback_subagents_no_push`, `feedback_subagent_worktree_detach`, `feedback_subagent_worktree_path_targeting`, `feedback_pertask_gofmt_lint`, `reference_streaming_sink_differential_framing`, `reference_differential_grpc_receiver_driver_owned`, `reference_docker_probe_bridge_network`, `reference_strict_reject_sibling_typeurl_gap`, `reference_dynamic_stat_name_charset_guard`, `reference_fuzzer_count_docs_drift`, `reference_differential_fixture_dispatch_constraint`, `reference_full_suite_race_after_background_mutator`, `reference_concurrency_differential_release_barrier`, `reference_differential_break_protocol_count1`, `reference_differential_run_selector`).
