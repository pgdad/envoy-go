START a fresh session for envoy-go. **The phase-42.1 (`retries` — the retry loop) SPEC is WRITTEN, reviewer-APPROVED, and LANDED on master (squash `bf1ee2ff`) — the §11 D-RT-* empirical pins were EXECUTED live against `contrib-v1.37.2` and the design is FIRMED (AMEND-RT1..7).** The next step is to WRITE the **phase-42.1 PLAN** (`superpowers:writing-plans` — the TDD task spine; the lifecycle valve is SPEC → PLAN). ROADMAP row 42 (`retries-and-hedging | 41 | in-progress`) carries the SPEC note; STATE active-phase is `phase 42.1 (retries-and-hedging) SPEC done`. Confirm the tip is UNCHANGED at `bf1ee2ff` via `git log --oneline -3`. Counts at tip (a pre-PLAN state — ALL stay UNCHANGED at the docs-only PLAN commit): stat surface **1163** / fixtures **76** / fuzzers **42** / BackendKind tail **36** (`BlockingHoldResponder`) / DECISIONS tail **ADR-0248**, next-free **ADR-0249**.

## What the SPEC already FIRMED for 42.1 (read `SPEC.md` first — these are LIVE-PINNED, do NOT re-probe)

**Architecture (ADR-0249; the anticipated ADR-0250 FOLDED IN — a single ADR):** a `retryExecutor` (new file in `internal/filter/http/router`, e.g. `retry.go`) WRAPPING the existing single-attempt `doH1ClusterAction` (`router.go:588`) / `doH2ClusterAction` (`router_h2.go:57`) at their call sites (H1 dispatch `router.go:566` + H2 `router_h2.go:42` + weighted `router_weighted.go:110/:123`). Each loop iteration is ONE driver call (host pick via LB + CB admission `TryAcquireRequest` + dial + the buffered `ActionResponse{Status,...}` at `router.go:119` + `RecordUpstreamResult`) — so EVERY attempt already re-runs circuit-breaker admission + the outcome seam (no extra wiring). The loop classifies `Status` against a parsed `retry_on` bitset; on a retriable outcome with `num_retries` + `retry_budget` slots left → exponential full-jitter backoff (delay-only), replay the buffered body (`bytes.NewReader` over `bodyBuf`, the 1 MiB `FilterBufferLimitBytes` cap at `connection.go:548-600`, ADR-0076; over-cap ⇒ non-retriable), re-invoke the driver. Nil `retryPolicy` ⇒ direct pass-through (BYTE-IDENTICAL). A `retryPolicy` struct on `clusterRouteAction` (`hcm/actions.go:201`), parsed in `buildRouterAction` (`hcm/config.go:536`), `VirtualHost.retry_policy` fallback threaded through `buildRouteTable` (`config.go:406`, the `includeVhRateLimits` precedent). ZERO new packages/modules (`go mod tidy -diff` EMPTY; route.v3+cluster.v3 already imported).

**retry_on (AMEND-RT1):** ENFORCE {`5xx` ⇒ Status∈[500,599], `gateway-error` ⇒ {502,503,504}, `connect-failure`/`reset` ⇒ the router local-origin failure shape (NOT a 5xx response — live `c_gw502b.upstream_rq_retry=0`), `retriable-status-codes` ⇒ Status∈`retriable_status_codes[]`}. PARSE-ACCEPT-defer {`retriable-headers`, `envoy-ratelimited`, `grpc-*`}; `retry_on` is freeform (NO PGV) ⇒ unknown tokens IGNORED.

**Stats (AMEND-RT2):** +5 NEW counters `upstream_rq_retry` / `_retry_success` / `_retry_limit_exceeded` / `_retry_backoff_exponential` / `_retry_backoff_ratelimited` (the last emit-0 — `rate_limited_retry_back_off` deferred), SCOPED to retry-policy clusters (a departure — keeps existing fixtures byte-stable; the route→cluster scan predicate is D-S42-5). The 2 phase-41 stats (`circuit_breakers.<prio>.rq_retry_open` gauge + `upstream_rq_retry_overflow` counter, already registered emit-0 at `circuitbreaker.go:78,83`) FLIP LIVE. `upstream_rq_total` already counts every attempt. Surface **1163 → 1173** (+5 × the `0075` two retry clusters).

**retry_budget (AMEND-RT3):** the phase-41 `circuitBreaker` gains `activeRetries atomic.Int64` + `tryAcquireRetry()`/`releaseRetry()`; before each retry, acquire iff `activeRetries < max(min_retry_concurrency, ⌈budget_percent% × activeRequests⌉)`; on fail ⇒ no retry + `upstream_rq_retry_overflow`++ + `rq_retry_open`=1 (DISTINCT from static-cap `upstream_rq_retry_limit_exceeded`). Defaults 20%/3. UNIT-tested (concurrency-shaped — NOT a differential arm; the deterministic core is the `0075` retry loop).

**Reject (AMEND-RT5):** THIN — `retry_back_off.base_interval` absent/≤0 (when `retry_back_off` set) + `max_interval < base_interval` (boot-reject, live-confirmed `--mode validate`). House wording `route: %q: retry_policy: …` (D-S42-1). `num_retries` unbounded. NO new fuzzer (config-parse unit-tested; a candidate tokenize fuzzer is D-S42-2 — default fuzzers STAY 42).

**Differential (AMEND-RT4):** ONE fixture `0075-retry-loop`, NO new BackendKind (REUSE `HTTP503Responder` 35 + `HTTPEcho`). The reference RANDOMIZES the round-robin initial offset ⇒ "first pick hits 503" is FALSE. TWO arms: (A) EXHAUSTION — cluster `c_exhaust` {single 503}, `retry_on:5xx` `num_retries:N`, drive 1 ⇒ cross-side EXACT final 503 + `upstream_rq_retry==N` + `upstream_rq_retry_limit_exceeded==1` + `upstream_rq_total==N+1`; (B) RECOVER — cluster `c_recover` {503,echo} RR, `num_retries:1`, drive K ⇒ cross-side `http.<prefix>.downstream_rq_2xx` delta == K (offset-invariant) + `retry_limit_exceeded==0`, subject-side `upstream_rq_retry_success==upstream_rq_retry>0` (exact retry count NOT cross-side-asserted). Sleepless/count-based; `-count=1` + `TestDifferential/0075`; 2 deliberate breaks. Template = `test/fixtures/0069-outlier-detection-consecutive-5xx/driver/driver.go` (the per-host-503 + RR + cross-side `StatsAsserter` precedent). The route's `retry_policy` attaches under the `route:` block in both bootstraps.

**Backoff/defaults (AMEND-RT6):** `num_retries` default 1; `retry_back_off` default ~25ms base / 250ms max (10×base); full jitter; delay-only. **Response flags (AMEND-RT7):** `RX`/`URX` a recorded departure.

## NEXT STEP — write the phase-42.1 PLAN

Use `superpowers:writing-plans`. Author `docs/envoy-go/phases/42-retries-and-hedging/PLAN.md` in the phase-41 PLAN shape (`docs/envoy-go/phases/41-circuit-breakers/PLAN.md` is the template) — a ~12–16-task TDD spine (the SPEC §10 anticipated spine is the starting decomposition). Run the **FINAL ADR-0045 split-gate re-check** (the 42.1 leg is pre-authorized to stand alone; re-confirm NO FURTHER split of 42.1 — anticipated ~300–450 prod LoC / ~12–16 tasks). RESOLVE the SPEC §12 **D-S42-1..8** PLAN questions: (1) the house reject wording; (2) fuzzer yes/no (default NO); (3) the over-cap-body non-retriable guard point (D-S42-3); (4) the `VirtualHost.retry_policy` fallback threading (D-S42-4); (5) the retry-counter scoping predicate + the route→cluster scan (D-S42-5) — this fixes the surface integer (+5 per retry cluster ⇒ 1173 for the two-cluster `0075`); (6) the `0075` constants single-sourced + the downstream-vs-upstream stat choice (D-S42-6); (7) the `retry_budget` activation shape on `circuitBreaker` + the limit_exceeded firing condition (only when the final attempt's outcome was itself retriable — the reviewer callout); (8) whether a deterministic budget-overflow differential arm via `BlockingHoldResponder` is added or deferred (default UNIT-tested). Then dispatch the plan-document-reviewer (the writing-plans loop), fix to approval, and commit docs-only. ALL counts stay UNCHANGED at the PLAN commit. The IMPL (42.1) follows in a later session; the 42.2 hedging leg follows after 42.1 lands.

## Honor throughout

- **Lifecycle = one stage per session.** `feedback_git_worktrees` (isolated work in a worktree) · `feedback_subagents_no_push` (task subagents commit locally only; the controller squashes + pushes at stage-close) · `feedback_push_to_origin` (after a clean local commit on master with tests green, push without asking) · `feedback_execution_style` (subagent-driven over inline).
- **ADR/process:** ADR-0044 (§Context at the SPEC [done], §Decision/§Consequences IN-PLACE at the IMPL) · ADR-0045 (the split-gate — the PLAN's final re-check) · ADR-0052 (the six-gate at the IMPL) · ADR-0080 (envoy-go's own byte-stable rejects) · ADR-0106 + `reference_roadmap_split_phase_row_done` (row 42 flips `done` only when BOTH 42.1+42.2 land; NO parent rollup) · ADR-0227 (`contrib-v1.37.2`) · ADR-0076 (the 1 MiB body cap) · ADR-0245 (`RecordUpstreamResult`) · ADR-0248 (the phase-41 `circuitBreaker` + the dormant `retry_budget` slot 42.1 activates).
- **At the PLAN:** `reference_fixture_workload_constant_desync` (single-source the `0075` N/K/topology constants; a hand-rolled count slice desyncs silently under go-test caching) · `reference_differential_run_selector` + `reference_differential_break_protocol_count1` (the `0075` subtest is `TestDifferential/0075`; deliberate breaks use `-count=1`) · `reference_differential_asserter_dispatch` (cross-side assertions use `StatsAsserter`, not `SubjectAsserter`) · `feedback_subagent_worktree_path_targeting` + `feedback_subagent_worktree_detach` (if the PLAN dispatch uses subagents).

## Read first (in order)

1. THIS FILE — the cold-start router (42.1 SPEC DONE + reviewer-APPROVED + LANDED; the next step is the 42.1 PLAN, NOT re-SPEC).
2. `docs/envoy-go/phases/42-retries-and-hedging/SPEC.md` — the FIRMED design; the §1.1 AMEND-RT1..7 block; the §3 architecture; the §5 proto roster; the §6 reject roster; the §7 stat surface (+5, scoped); the §8 `0075` differential (exhaustion-exact + recover-invariant); the §10 anticipated task spine; the §11 D-RT-* live-pin disposition; the §12 D-S42-1..8 PLAN questions; the §13 ADR-0249 §Context draft.
3. `docs/envoy-go/phases/42-retries-and-hedging/BRAINSTORM.md` — the durable charter (the settled forks; the §10 pins the SPEC executed).
4. `docs/envoy-go/STATE.md` — active-phase `phase 42.1 (retries-and-hedging) SPEC done`; the count figures (1163 / 76 / 42 / 36 / ADR-0248, next-free ADR-0249).
5. `docs/envoy-go/phases/41-circuit-breakers/{PLAN,PROGRESS,README}.md` — the precedent shape of the phase-42.1 PLAN/IMPL (the 12-task TDD spine; the D-S41-* resolutions; the six-gate).
6. `docs/envoy-go/DECISIONS.md` (the ADR-0248 entry — the `retry_budget` parse + emit-0 stats 42.1 activates; the process ADRs; next-free ADR-0249) + `docs/envoy-go/BEHAVIOR_CONTRACT.md`.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`.
