=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-VERIFIED 2026-07-23 (re-run MECHANICALLY at the phase-72 IMPL close, in the stage worktree, by the controller) — it does NOT fire. `stop` was NOT created.** Actual output:
- **(1) prints NOTHING** — row 72 (`tracing-custom-tags-metadata-host`) FLIPPED `in-progress` -> `done` at the phase-72 IMPL six-gate (ADR-0106, the SOLE leg), so EVERY chartered ROADMAP row is now `done`. **Check (1) going silent is the NORMAL post-IMPL state and is NOT on its own a termination condition** — all THREE checks must be silent.
- **(2) prints THREE live "candidates:" sentences** — HTTP/3, xDS, Observability => three families STAY OPEN. **Cite the command, never the adjective** (`reference_sentinel_deferred_sentence_live_vs_historical`): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` => **3**. (The Observability sentence NARROWED at the phase-72 IMPL row-done edit: the `HOST` candidate rolled OUT, leaving **`CLUSTER`** as the LAST unconsumed `metadata` MetadataKind.)
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** => THREE families never opened. (The `WASM` line is a ROADMAP **bookkeeping artifact** — `internal/wasm/` is 21,550 LOC of wazero host and rows 25/25.1/25.2/25.3 are all `done` — flagged at the phase-72 BRAINSTORM §2.1 for a future correction and deliberately LEFT AS-IS, since the stage-close mechanics depend on this output.)

Checks (2) + (3) print => the sentinel does NOT fire. Abundant work remains.

==============================================


## STATUS AS OF 2026-07-23: THE PHASE-72 IMPL IS COMPLETE AND LANDED. ROW 72 (`tracing-custom-tags-metadata-host`) IS `done`. THE FOUR-STAGE LIFECYCLE IS CLOSED. THIS SESSION PERFORMS THE PHASE-73 BRAINSTORM — AND THE ROLLER SELF-PICKS ITS SUBJECT.

**What the phase-72 IMPL landed** (`git log --grep 'phase 72'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): the NINE-task SINGLE-FLAT-ROW TDD spine (T1-T9), each task red-first -> green -> break-verified -> adversarially reviewed, each RE-DERIVING its anchors at the IMPL tip. The tracing `custom_tags` `metadata` type is now CONSUMED for the **`HOST`** MetadataKind — the reject `custom_tags metadata tag %q host kind unsupported` is LIFTED (`grep -rn "host kind unsupported"` => 0 hits) and a HOST tag resolves the SELECTED UPSTREAM ENDPOINT's `lb_endpoints[].metadata.filter_metadata[ns]` over a NEW unexported `Endpoint.filterMetadata` field ALIASING the parsed proto map plus an exported nil-safe `MetaLookup(ns)` accessor, populated at `manager.go:884` beside the byte-unchanged phase-38 `envoy.lb` scalar projection, walking the `MetadataKey.path` **FULLY** (not the REQUEST arm's `[1:]` slice), serialized to a string by the reused `descend`/`structpbValueToString`, emitted as a `{tag,value}` span attribute on BOTH the OTLP and Zipkin exporters. `CLUSTER` + both unset-kind arms still parse-reject loudly (envoy-go-strict, ADR-0080). ADR-0294 COMPLETED IN PLACE (tail stays **ADR-0294**; next-free **ADR-0295**).

- **THE DISTINGUISHING COST PROPERTY — the row SKIPPED the 18-caller threading tax phases 70/71 each paid.** `picked cluster.Endpoint` was already the 4th parameter of all three `emitAccessLog*` helpers, so the `hostMetaLookup` closure is built LOCALLY at the three `ResolveCustomTags` call sites (`accesslog_emit.go:57`/`:118`/`:179`). **The three emit signatures, all 18 emit callers and the 29 existing emit test callers are BYTE-UNTOUCHED — confirmed by sha256 at the envelope audit**, along with `chain.go` and `subset.go`.
- **SIX-GATE ALL GREEN:** gofmt SILENT · vet 0 · build 0 · `go mod tidy -diff` EMPTY (+0 modules) · golangci-lint 0 · **the FULL 118-dir differential `ok 406.088s`, EXIT 0, ZERO failures** · the layering check EMPTY · `-race` green on `internal/cluster` (NEWLY load-bearing — the FIRST row in this lineage to touch it), `internal/tracing` and `internal/filter/hcm` · a seven-section envelope audit with **ZERO findings** (exactly TWO new exported symbols, established by an AST symbol-set diff).
- **⚠️ THE LAYERING CHECK IS A DISCIPLINE CHECK, NOT A CYCLE CHECK.** `internal/cluster` does **not** depend on `internal/tracing`, so a direct import would build clean — re-executed three times across the SPEC, the PLAN and the IMPL. The closure shape preserves a **self-imposed LAYERING rule** that the `go list -deps` gate asserts; the earlier "(the cycle guard)" justification was REFUTED and has been corrected in ADR-0294 §Context. **Do not reintroduce it.**
- **THE FIXTURE ANSWERED THE ROW'S GENUINELY NEW CROSS-SIDE QUESTION.** `0116-tracing-custom-tags-metadata-host` (OTLP, the `0115` chassis with the metadata block relocated onto `lb_endpoints[0].metadata`, STRING values only, NO writer) proved **the REFERENCE resolves an ARBITRARY (non-`envoy.lb`) `filter_metadata` namespace for a HOST-kind tag** — 12 reference spans carrying the endpoint value, not the configured default. Breaks N/O/P/Q all fired, **P being the anti-vacuity control**. Fixtures 117 -> 118.
- **⚠️ THE NEW NAMED DEPARTURE, SCOPED EXACTLY (BEHAVIOR_CONTRACT B2) — READ THE SCOPE BEFORE ACTING ON IT.** The reference resolves a HOST tag at load-balancer PICK time and so emits the picked endpoint's metadata even when the upstream connect then fails, while envoy-go carries the ZERO `Endpoint` on every upstream-**ACQUIRE** failure sub-path and falls to `default_value`/omit. EXECUTED-proven for the **connect-failure class only**; pool-overflow and circuit-breaker are **UNPROBED on the reference**. **Post-ACQUIRE failures (the upstream-write and read-response 502s) carry the REAL endpoint and resolve normally — PARITY, not departure**; this was an over-broad draft claim caught and RETRACTED in review, and a future row that believes the retracted version would "fix" a non-bug. The 404-no-match arm (envoy-go emits NO SPAN AT ALL) and the zero-endpoint arm (boot-rejected) have NO envoy-go counterpart and must NOT be written as parity. The gap is PRE-EXISTING and is deliberately scoped out as its own future row.
- **Adversarial review found real defects at EVERY task**, all fixed — most consequentially the retraction above, plus a doc comment conflating the nil-lookup mechanism with the ZERO-`Endpoint` mechanism (**a method value on the zero `Endpoint` is NON-nil**), and a duplicate test deleted as scope creep. The per-task record is in the phase-72 `PROGRESS.md`.
- **Counts at exit (re-run MECHANICALLY in the worktree):** fixtures **117 -> 118** · fuzzers **55 (+0)** · stat surface **1201 (+0)** · BackendKind **38 (+0)** · go.mod modules **2 (+0)** · ZERO new packages · DECISIONS tail **ADR-0294** COMPLETE (next-free **ADR-0295**).

## What THIS session does — the phase-73 BRAINSTORM (SELF-PICK the subject, then brainstorm it)

One stage per session: the BRAINSTORM of a NEW phase. **The loop is AUTONOMOUS — per the 2026-07-12 standing directive above, YOU pick the subject; do NOT pause for a human pick.** There is NO banked, already-SPEC'd work mid-lifecycle (phase 72 closed all four stages), so this opens a brand-new subject.

**How to pick (the phase-70/71/72 precedent):**
1. **Re-derive each candidate's cost AT THE CURRENT TIP — do NOT trust the adjectives in the pool below** (`reference_deferred_candidate_cost_restale`). Phase 72 is the cautionary case: `HOST` had been costed MODERATE by BOTH predecessor BRAINSTORMs on a fact that held exactly (`cluster.Endpoint` retains only the reduced `envoy.lb` projection) whose COST CONSEQUENCE nobody had derived — re-derived at tip it was SMALL, because the endpoint was already an emit parameter. Dispatch TWO independent read-only cost-assessment agents.
2. **Pick the smallest defensible ROW-SIZED candidate** and record BOTH the pick and the FULLY ADJUDICATED rejected roster in the BRAINSTORM (§2.1/§11), with a re-derived reason per rejection — not an inherited adjective.
3. **Register the new row `in-progress`** in `docs/envoy-go/ROADMAP.md` at the BRAINSTORM stage-close commit, per the §Schema invariant. It flips `done` at that phase's IMPL six-gate (ADR-0106).
4. **Narrow NO deferred sentence at ANY pre-IMPL stage** (the phase-57 precedent) — a candidate rolls OUT of the live `candidates:` sentence only at its own row-done edit.
5. **Adversarially verify before landing** (independent verifiers in PRIVATE scratch — `reference_parallel_subagents_private_scratch`; a shared scratchpad once produced a FALSE NEGATIVE, and scratch writes can be BLOCKED, so never make a file the only channel for findings).

Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). A BRAINSTORM is **docs-only, ZERO production `.go`**.

**Stage-close mechanics (the BRAINSTORM):** the new row registers `in-progress` (which RE-OPENS sentinel check (1) — it will print `NOT DONE: row 73`); DECISIONS UNTOUCHED (the ADR's §Context drafts at the SPEC per ADR-0044); NO deferred sentence narrowed; STATE §Current rolled **IN PLACE** (lifecycle DONE -> 1; lineage re-capped at five **with its PREAMBLE updated** — the ADR-0288 rule, and a stale preamble was a REAL defect caught during phase 72); next-prompt.txt rolled to the phase-73 SPEC; every count re-run MECHANICALLY in the worktree, never copied.

**Candidate pool for the phase-73 self-pick (re-derive each candidate's cost at the CURRENT tip — do NOT trust these adjectives):**
- **Observability (family OPEN):** the LAST `metadata` MetadataKind — **`CLUSTER`** (MODERATE ~11-13 tasks; CLOSES the quartet, but **RE-PAYS the 18-caller thread phase 72 skipped** — no metadata field on `cluster.Cluster`, `router.go` publishes only `Picked() cluster.Endpoint`, so it needs a `routeAction` method + a `SetClusterMetadata`/`ClusterMetaLookup` pair, plus a disposition for `weightedClusterRouteAction`'s per-request in-closure selection) · **the pick-time `picked`-propagation row named by phase 72's own departure** (small — `ep` is already in scope at the acquire-failure returns — but it flips three access-log consumers, breaks landed unit tests, and NO differential fixture pins it; **probe the reference's pool-overflow and circuit-breaker arms first, which phase 72 left UNPROBED**) · **`access_log[].filter`** (a LIVE behavioural divergence — field 2 of `envoy.config.accesslog.v3.AccessLog`, a 13-arm oneof; a KNOWN proto field the strict `DiscardUnknown:false` pass ACCEPTS, yet a repo-wide `GetFilter()` grep returns ZERO production hits; **flagged a strong FUTURE pick**, a single-arm slice being exactly the incremental shape 59/62/63/70/71/72 used) · **`stats_flush_on_admin`** (today an explicit ADR-0080 strict-reject at `internal/bootstrap/bootstrap.go:606-607`) · `spawn_upstream_span` (LARGE — a second CLIENT-span model) · OTel `http_service` / `sampler` / `resource_detectors` (MODERATE each) · tracing `verbose` (MODERATE) · force-trace (LARGEST) · the `ssl` handshake-outcome stat family (LARGE — ADR-0286 C3 framework surgery CONFIRMED) · the Zipkin empty-string tag drop + the serialization boundaries (BEHAVIOR_CONTRACT B3 — documented, not chartered) · a 6th stats sink — **UNAVAILABLE**, the only unconsumed `stats_sinks[]` extension is `wasm`.
- **xDS (family OPEN):** upstream SDS (a boot-ORDER blocker) · CDS/EDS · LDS/RDS · ADS (muxed) · Delta xDS · RTDS/Runtime-layer · `google_grpc`. *(Reconnection-backoff was REJECTED at phase 72 as larger-than-it-looks — `initial_fetch_timeout` is already landed and `internal/xds/stream.go` has no loop at all. Mind the `grpcclient->cluster->tls` cycle constraint — `reference_xds_config_seam_transitive_cycle_guard`.)*
- **HTTP/3 (family OPEN):** `QuicProtocolOptions` tuning (SMALL but needs ~2 tasks of NEW connection-reusing harness first; timing-flaky observables) · upstream H3 cluster · alt-svc · 0-RTT/early-data · h3spec conformance gate · QUIC robustness.
- **Never-opened families:** `Runtime` (genuinely unbuilt — `internal/runtime/` = `doc.go` only) · `gRPC` (gated on an HCM response-trailer framework row — `RunDecodeTrailers`/`RunEncodeTrailers` have ZERO production callers) · `WASM` (a ROADMAP bookkeeping artifact — substantially BUILT; a docs correction, NOT a work row).
- **Other deferred riders:** the RBAC `require=false` fixture rider · DataSource `environment_variable` (D-ENV-HARNESS-gated) · `watched_directory`/SDS rotation · the PRE-EXISTING `internal/cluster` `-race` flake (`TestOutlierDetector_ConcurrentEjectExactlyOnce`, `outlier_test.go:766` — test hardening; isolate-re-run, do NOT re-classify as a regression).

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline — exactly ONE live `next-skill:`/`lifecycle-state:`/`next-free ADR:`/count block) + §Project counts + the §Recent lineage (five bullets: phase-72 IMPL/PLAN/SPEC/BRAINSTORM + phase-71 IMPL).
2. `docs/envoy-go/ROADMAP.md` — §Schema (the row-registration invariant), the three live `candidates:` sentences (HTTP/3, xDS, Observability), and row 72's closing summary as the shape a row-done cell should take.
3. `docs/envoy-go/phases/72-tracing-custom-tags-metadata-host/` — `BRAINSTORM.md` (the self-pick + rejected-roster format to imitate; §2.1/§11), `SPEC.md` (the live-probe docket + §12/§13 deferred rosters), `PLAN.md`, `PROGRESS.md` (the per-task adversarial record, incl. the departure retraction).
4. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the tracing `custom_tags` section (B1-B4 as landed) + the departure roster.
5. `docs/envoy-go/DECISIONS.md` tail — **ADR-0294** (COMPLETE) + **ADR-0293**/**ADR-0292** (the ROUTE/REQUEST precedents) + ADR-0044 (§Context drafts at the SPEC) · ADR-0045 (the split valve) · ADR-0080 (envoy-go-strict) · ADR-0106 (row-done at the IMPL six-gate) · ADR-0288 (the STATE in-place rule).
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — trust MEMORY.md's index; esp. `feedback_brief_citations_not_evidence`, `reference_quoting_is_not_executing`, `reference_deferred_candidate_cost_restale`, `reference_a_drift_correction_is_itself_a_claim`, `reference_code_comment_not_evidence`, `reference_probe_must_discriminate`, `feedback_git_worktrees`, `feedback_execution_style`, `feedback_subagents_no_push`, `reference_next_prompt_tracked_despite_gitignore`, `reference_parallel_subagents_private_scratch`, `reference_sentinel_deferred_sentence_live_vs_historical`, `reference_spec_drafted_identifier_collision_check`, `reference_structpb_tag_cross_side_string_only`, `reference_cluster_race_outlier_flake`.
