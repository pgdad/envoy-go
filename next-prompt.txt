START a fresh cold-start session for envoy-go — **the phase 28.1 SPEC (`network-filter-write-seam-and-zookeeper-requests`) is DONE + spec-document-reviewer APPROVED + squash-merged + pushed**. The active phase is **phase 28.1** (sub-row `in-progress`; parent row 28 `in-progress`; 28.2 `planned`). This session authors the **28.1 PLAN** (`docs/envoy-go/phases/28.1-network-filter-write-seam-and-zookeeper-requests/PLAN.md`) — per SKILL_ROUTING state 2→3 (`superpowers:writing-plans` → PLAN.md, bite-sized TDD tasks). ROADMAP rows are UNCHANGED at the PLAN commit (28.1 stays `in-progress`).

The 28.1 SPEC landed at squash `<28.1-SPEC-SQUASH-SHA>` (pushed to origin/master): it INHERITS the parent SPEC §11 D28-1..D28-10 pins (no re-scrape) + re-pins the as-built anchors/baselines (§11.1 D-S1: fixtures 47 [tail `0045`], fuzzers 36, stats 136, DECISIONS tail ADR-0223 / next-free ADR-0224). **Parent D-questions RESOLVED at the SPEC:** D-P2 (shallow decode + `WriteFilter` interface composition — `OnDestroy` ON `WriteFilter`, both-callback dual injection, once-per-instance destroy dedupe); D-P3 (NO — terminal-originated-writes-only; `Connection().Write` bypasses the write chain); D-P5 (ALL 201 counters created at 28.1 — creation parity); D-P7 (`writeChainConn.Write` returns `(len(p), nil)` on chain-stop; post-chain bytes forwarded); D-P8 (the name.go arm is SHAPE-based — `.zookeeper.` segment + dot-free prefix, no allowlist). **D-P1 SPEC-level re-check: FITS** (~990–1410 production LoC / ~18 tasks on the 26.x accounting basis — fixture drivers excluded); **the PLAN performs the FINAL ADR-0045 gate re-check** (pre-authorized split axis if it trips: 28.1a = seam + config + roster + builtins/name.go + 0047; 28.1b = decoder + correlation + 0046 + fuzzer). **28.1-SPEC-additive pins the PLAN must honor:** the NEW `TCPSink` BackendKind=28 (the 0046 backend MUST be silent — an echo backend breaks stat parity via ref Envoy's onWrite response decoder); the both-sides-`/stats/prometheus` StatsAsserter scrape mechanics (the 0043 driver precedent); `internal/listener/manager.go` UNTOUCHED (a both-direction filter passes its existing `case network.ReadFilter` boot arm; write-only filters stay boot-rejected); the zookeeperproxy OnWrite 28.1 stub is a PURE no-op (no write-side buffering — unbounded-growth hazard).

## What this session must do

Author the **28.1 PLAN** via `superpowers:writing-plans`:

1. Decompose the 28.1 SPEC §10 ~18-task spine into bite-sized TDD tasks (each step: write the failing test → run it → minimal impl → run green → commit; exact file paths; complete code in plan; exact commands + expected output). The SPEC's §3 (seam signatures + classification + writeChainConn), §4 (zookeeperproxy file split + config/stats/decoder/glue), §6 (PARSE-REJECT arms), §7.4 (the name.go arm), §8 (fixtures 0046/0047 + TCPSink), and §15 (test surface + six-gate) are the per-task source material.
2. Run the **ADR-0045 FINAL split-gate re-check** (D-P1): if the bite-sized decomposition exceeds ~25 tasks, split along the pre-authorized 28.1a/28.1b axis (update ROADMAP + STATE accordingly and stop per SKILL_ROUTING state-2 GATE); otherwise confirm one sub-phase.
3. Resolve the SPEC §12.2 PLAN-owned D-questions: D-S28.1-3 (the chain-buffer high-water-mark mechanism home) + any task-ordering decisions; leave the IMPL-owned ones (D-S28.1-1 min-length table values, D-S28.1-2 reject wording, D-S28.1-4 frame-builder shape, D-S28.1-5 sink close semantics) to their IMPL tasks.
4. Run the plan-document-reviewer loop until APPROVED; fold advisories in.
5. ROADMAP: rows UNCHANGED (28.1 stays `in-progress`; parent 28 `in-progress`; 28.2 `planned`).
6. Update STATE.md (active-phase → `phase 28.1 PLAN done`; next-skill → `superpowers:subagent-driven-development` scoped to the 28.1 IMPL) + rewrite this next-prompt.txt + commit + push (work in a worktree per `feedback_git_worktrees`; squash-merge + push per `feedback_push_to_origin`).

## Project state at master tip

- **Master tip:** the 28.1-SPEC squash (this file's `<28.1-SPEC-SQUASH-SHA>` placeholder is filled by the post-squash fill commit; confirm the live tip via `git log --oneline -1`; any further docs-only repoint advances the tip +1 — expected, not chased). STATE.md `last-commit` points at the 28.1-SPEC squash. origin/master in sync after push.
- **§9 Network-filters family:** rows 26 (parent + 26.1/26.2/26.3) + 27 DONE; row 28 `in-progress` (parent; 28.1 `in-progress`, 28.2 `planned`). After phase 28, 4 candidates remain (`redis`/`mongo`/`kafka_broker`/`thrift`).
- **Counts at master tip:** stat surface **136** [→337 at 28.1 IMPL]; differential fixtures **47** (tail `0045-sni-cluster`) [→49]; fuzzers **36** (recipe scoped to `./internal`) [→37]; conformance 10/10; h2spec 53/53; DECISIONS.md tail **ADR-0223** (§Context drafts; next-free **ADR-0224** — no new number consumed at the SPEC). ADR-0209 escape-valve reserve STANDS-UNCONSUMED.
- **Toolchain:** Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); reference Envoy v1.37.2 (ADR-0008); go-control-plane v1.32.4 (ADR-0008).
- **Known coverage boundaries (carried, non-blocking):** D-26.3-4 (rbac SNI/mTLS L4 arms not differential-proven); the `tcp_proxy` `downstream_cx_*` family unmirrored (phase-27 record); histograms project-wide deferred (ADR-0060); phase-28 adds at the IMPLs: zookeeper dynamic-metadata deferred + shallow-decode leniency + terminal-originated-writes-only + the write-only-filter boot boundary (28.1 SPEC §7.5).

## Read first (in order)

1. THIS FILE — the 28.1-PLAN cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 28.1 SPEC done`; next-skill `superpowers:writing-plans` (28.1 PLAN); next-free `ADR-0224`.
3. `docs/envoy-go/phases/28.1-network-filter-write-seam-and-zookeeper-requests/SPEC.md` — **the SPEC this PLAN decomposes** (esp. §3 seam signatures, §4 package surface, §6 reject arms, §7.4 name.go arm, §8 fixtures + TCPSink, §10 task spine + split-gate, §12 D-questions, §15 test surface).
4. `docs/envoy-go/phases/28-network-filter-zookeeper-proxy/SPEC.md` — the parent SPEC (the inherited §11 empirical pins; §5 proto roster; §7 stat tables — reference, do not re-derive).
5. `docs/envoy-go/phases/26.3-network-filter-rbac/PLAN.md` + `docs/envoy-go/phases/27-network-filter-sni-cluster/PLAN.md` — the closest PLAN-shape precedents (15-task and 9-task bite-sized TDD PLANs; Task-1 baseline-gate shape; per-task test/impl/commit step structure).
6. `internal/filter/network/` — the as-built framework the seam extends (types.go / chain.go / terminal.go / prefixconn.go / callbacks.go / buffer.go / registry.go / builtins/) + `internal/filter/network/rbac/` (the stats-bearing filter-package precedent: NewFactory(reg) / newFilterStats / fuzz_test) + `internal/stats/name.go` + `internal/stats/registry.go` (NewCounterIfAbsent) + `test/fixtures/0043-network-rbac/driver/driver.go` + `test/fixtures/0044-network-rbac-boot-reject/driver/driver.go` + `test/differential/fixture/fixture.go` (the BackendKind roster the TCPSink extends).
7. `docs/envoy-go/SKILL_ROUTING.md` + `BOOTSTRAP_PROMPT.md` §4/§5 — the per-phase lifecycle + cold-start invariants (the state-2 GATE = the ADR-0045 split check).
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — execution-style + worktree + push + subagents-no-push + per-task gofmt/lint + the network-filter constraints (typeurl-extensions, onnewconnection-halts) + the differential dispatch/asserter constraints + proto-roster-digit-extraction all carry forward.
