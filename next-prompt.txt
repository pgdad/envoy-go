RESUME a fresh session for envoy-go (router last updated 2026-06-28; the **phase 46.1a (tracing header engine) IMPL is DONE and LANDED** — squash-merged onto master + pushed, the worktree removed). 46.1a built the FIRST sub-leg of the 46.1 (core+OTLP) by-exporter leg: the header-level request-tracing engine — a NEW `internal/tracing` package (the sampling/request-id `Decide` engine + W3C `traceparent` extract/continue/inject + `tracestate` pass-through + the `x-request-id` byte-14 trace-reason stamp [NoTrace=`4`/Sampled=`9`/Client=`b`] + the `RandSource` seam + the proto→`TracingConfig` parse with 8 STRICT-REJECT arms + 5 HCM-scoped `tracing.*` counters) wired into the HCM filter (H1+H2 dispatch), LIFTING the `HttpConnectionManager.tracing` field from the ADR-0041 silent-ignore set. **ROW 46 (`tracing`) STAYS `in-progress`** (46.1a is the FIRST sub-leg — row 46 flips `done` at the 46.2 IMPL per ADR-0106 + `reference_roadmap_split_phase_row_done`); the Observability FAMILY STAYS OPEN. **The next action is the 46.1b PLAN** (span emission + OTLP export — the COMPLETING sub-leg of 46.1, which CLOSES ADR-0260).

HEAD is the **46.1a IMPL squash `b09dd404`** (30 files, +3031: the `internal/tracing` package + the HCM parse/dispatch wiring + the `0086-tracing-request-id` fixture + BEHAVIOR_CONTRACT/STATE/ROADMAP/PROGRESS + a SPEC §11 annotation; fast-forwarded onto master at `1dd4fbbf` + pushed) — possibly with a `next-prompt.txt:` tip-stamp roll stacked atop. **Re-read the shas with `git log --oneline -8`, do NOT assume.** Confirm `git status` clean (the untracked `.claude/` is session cruft, leave it) + master == origin/master.

**As-built at 46.1a (each VERIFIED; the exit invariant — re-verify, do NOT assume):** stat surface **1196** (H2 cluster; non-H2 **1192**; +5 the DYNAMIC `http.<stat_prefix>.tracing.{client_enabled,health_check,not_traceable,random_sampling,service_forced}` counters, register ONLY under a configured tracing HCM) / fixtures **88** (tail `0086-tracing-request-id`) / fuzzers **48** (`^func Fuzz` == 48 — `FuzzStampRequestID` + `FuzzExtractTraceparent`) / BackendKind tail **38** / DECISIONS tail **ADR-0259**, next-free **ADR-0260**. ONE new Go package (`internal/tracing`); ZERO new go.mod modules. The six-gate was GREEN (the full 88-dir differential green modulo the documented `reference_differential_fullsuite_startup_flake` class — a full run transiently failed the UNRELATED `0036`/`0048`, both isolate-re-ran green; `0086` passed). TWO code-quality reviews APPROVED (no Critical/Important). **Note the IMPL-time CORRECTION (Task 10b, recorded in SPEC-46.1.md §11):** a continued+SAMPLED trace (inbound `traceparent` flags `01`) stamps `x-request-id` nibble `9` (the reason reflects the SAMPLE STATUS, matching the reference — cross-side EXACT), while the COUNTER class stays `not_traceable` (the SPEC §11 "continued keeps `4`" pin was a probe error). **Known 46.1b boundaries (from the final review, recorded in BEHAVIOR_CONTRACT):** the no-route-match (404) path does NOT inject (the decision runs AFTER the route match; reference injects at the HCM layer regardless) — revisit at 46.1b; `Decision.SpanID`/`ParentSpanID`/`TraceState` are 46.1b span-model scaffolding (set at 46.1a, the span reads them at 46.1b).

## NEXT STEP — the 46.1b (span emission + OTLP export) PLAN
This is a PLAN stage — docs-only, direct-on-master (the house TDD-task-spine shape mirroring `PLAN-46.1a.md`). The deliverable is `docs/envoy-go/phases/46-tracing/PLAN-46.1b.md` (the ~9–12-task TDD spine). **46.1b scope** (≈480 prod LoC per the SPEC §3.0 / `reference_phase_46_tracing_split` boundary): `internal/tracing/span.go` (the per-request SERVER span model + the CONCRETE 16-attr roster pinned at SPEC-46.1 §11 D-TRACE-SPAN: `http.method`/`http.url`/`http.protocol`/`http.status_code`/`component=proxy`/`upstream_cluster(.name)`/`downstream_cluster=-`/`response_flags=-`/`request_size`/`response_size`/`user_agent`/`guid:x-request-id`/`node_id`/`zone`/`peer.address`/`guid:x-client-trace-id`; `name:"ingress"`/kind SERVER; `ScopeSpans.scope={name:envoy,version}` POPULATED; `Resource`=`service.name`(←`service_name`)) + the `OTLPTracesClient` UNARY typed wrapper (mirror `grpcclient.go:319–375` `OTLPLogsClient`, over `collector/trace/v1.TraceService.Export`) + the `OTLPExporter` (mirror `internal/accesslog/otlpsink.go` 216 LoC — bounded channel + writer-goroutine + size/interval/close buffer) + the span-lifecycle wiring (D-TRACE-SPANEND-PLUMBING — carry the `tracing.Decision` from the `connection.go` dispatch seam to the `accesslog_emit.go:19`(H1)/`:45`(H2) END-seam = stream-complete; prefer the shared per-request context the access-log `Record` already uses) + the tracer-scoped `tracing.opentelemetry.{spans_sent,spans_dropped}` counters (+2 → 1198) + the `log.Fatalf` missing/non-H2 collector-cluster gate (the documented departure — envoy-go-strict; the reference is permissive) + the driver-owned `test/helpers/otlptrace` receiver (BackendKind STAYS 38 per `reference_differential_grpc_receiver_driver_owned`) + the `0087-tracing-otlp` span differential (cross-side per-span PAYLOAD aggregated, NOT framing, per `reference_streaming_sink_differential_framing`; the `service.name` + the stable attr subset cross-side assertable; a trace-id continuation prong). **CLOSES ADR-0260** (the §Decision/§Consequences body lands at the 46.1b IMPL per ADR-0044; §Context already drafted at SPEC-46.1 §13). Resolve the open 46.1b D-questions (the span-end context threading; the exporter cluster-resolution timing; the receiver shape; the `timer_flushed` keep/drop; whether a span/exporter fuzzer lands). Run the plan-document-reviewer loop. Then the 46.1b IMPL (subagent-driven in a fresh worktree per `feedback_execution_style` · `feedback_git_worktrees`), which CLOSES ADR-0260; THEN the 46.2 (Zipkin+B3) leg, whose IMPL flips row 46 → `done`.

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** The next stage is the 46.1b PLAN (docs-only, direct-on-master; no worktree — the doc commits direct on master).
- **Execution-chaos lesson from 46.1a (HEED THIS):** the repo's global CLAUDE.md ("after successfully testing changes, add the files to git and commit them") makes dispatched subagents AUTO-COMMIT their work (and a stray async subagent even made an uncontrolled follow-up "Task 10b" correction commit). Do NOT fight it with "don't commit" instructions (subagents obey CLAUDE.md over the dispatch). Instead: let subagents commit, the CONTROLLER verifies EACH resulting commit (correct fileset, real non-vacuous tests, gates green), cleans stray next-task leak files between tasks, and re-runs the FULL suite on the final frozen HEAD (a transient inconsistent mid-execution state caused a spurious full-suite failure at 46.1a). Per-task commit messages/trailers don't matter — the controller SQUASHES at stage-close.
- **Process anchors:** ADR-0106 (per-leg rows; row 46 `in-progress`) · ADR-0044 (ADR §Context-at-SPEC [done], §Decision/§Consequences body-at-IMPL — ADR-0260 body lands at the 46.1b IMPL) · ADR-0158 (the `grpcclient.Dialer` + typed-wrapper — the `OTLPTracesClient`) · ADR-0258/0259 (the phase-45 OTLP sink + `otlpsink.go` batching pattern the exporter reuses) · ADR-0016 (proto blank-import — a `collector/trace/v1` import) · ADR-0080 (strict-reject — the `log.Fatalf` cluster gate) · ADR-0227 (the contrib reference) · next-free **ADR-0260** (CLOSED at the 46.1b IMPL), then **ADR-0261** (46.2 Zipkin+B3).
- **The substrates the 46.1b PLAN builds on (verified at the 46.1a IMPL):** the `internal/tracing` package (`Decision{Sample,Reason,Class,Continued,TraceID[16],SpanID[8],ParentSpanID[8],TraceState,RequestID}` — the span is built from these; `SpanID`/`ParentSpanID`/`TraceState` already populated at dispatch) · the dispatch seam `connection.go` (H1) / `h2dispatch.go` `WriteH2` (H2, value-copy) where the `Decision` is computed · the END seam `accesslog_emit.go:19`(H1)/`:45`(H2) = stream-complete (the span-end; carry the `Decision` here) · `grpcclient.go:319–375` (`OTLPLogsClient` template) · `internal/accesslog/otlpsink.go` 216 LoC (`OTLPExporter` template) · `go.opentelemetry.io/proto/otlp` (`trace/v1.Span` + `collector/trace/v1.TraceService.Export` UNARY) · the `service_name`/`ClusterName` ALREADY parsed onto `TracingConfig` (UNUSED at 46.1a — 46.1b dials/labels).

## Read first (in order)
1. THIS FILE — the cold-start router (the 46.1a IMPL is DONE + LANDED + pushed; row 46 `in-progress`; the next action is the 46.1b PLAN).
2. `docs/envoy-go/phases/46-tracing/SPEC-46.1.md` — the charter (the §11 EXECUTED D-TRACE-* pins esp. D-TRACE-SPAN [the 16-attr roster] + D-TRACE-OTLP-WIRE + D-TRACE-REJECT, the §11 D-TRACE-REQUESTID `[CORRECTED 46.1a IMPL]` annotation, the §13 ADR-0260 §Context draft).
3. `docs/envoy-go/phases/46-tracing/PLAN-46.1a.md` + `PROGRESS-46.1a.md` — the as-built 46.1a engine the span model builds on (the `Decision` shape, the dispatch seams).
4. `docs/envoy-go/STATE.md` — the active-phase header (`phase 46.1a IMPL done`) + the recorded NEXT (the 46.1b PLAN) + the count figures.
5. `docs/envoy-go/ROADMAP.md` — row 46 (`tracing`, `in-progress`, the 46.1a stage-note) + the `### Observability family` heading.
6. `docs/envoy-go/DECISIONS.md` — **ADR-0259** + next-free **ADR-0260** (the 46.1 anchor — body lands at the 46.1b IMPL).
7. `docs/envoy-go/phases/45-otlp-access-log/PLAN-45.1.md` — the PLAN house shape + the `otlpsink.go` exporter / `OTLPLogsClient` patterns the 46.1b PLAN mirrors.
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `reference_phase_46_tracing_split`, `reference_streaming_sink_differential_framing`, `reference_differential_grpc_receiver_driver_owned`, `reference_docker_probe_bridge_network`, `reference_dynamic_stat_name_charset_guard`, `reference_differential_fullsuite_startup_flake`, `reference_differential_run_selector`, `reference_differential_break_protocol_count1`, `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`).
