=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, the active phase is fully landed, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action. (The sentinel does NOT apply right now — phase 51 is chartered, its SPEC is LANDED, and the next stage is its PLAN.)

**STATUS AS OF 2026-07-05: phase 51 (`bootstrap-validate-mode`) SPEC is LANDED; the NEXT stage is its PLAN.**

- The phase-51 SPEC landed: `docs/envoy-go/phases/51-bootstrap-validate-mode/SPEC-51.md` (a 13-section shape mirroring the phase-50 SPEC template, adapted for this phase's non-differential, non-wire-protocol nature per BRAINSTORM guidance). Row 51 stays `in-progress` (lifecycle-state 1 → 2). STATE.md active-phase header rolled to `phase 51 (bootstrap-validate-mode) SPEC done`. ROADMAP.md row 51 + the "Operational tooling family" section prose both rolled to reflect SPEC-done. **Re-verify with `git log --oneline -6` + `git status`; do NOT assume.**
- **All five BRAINSTORM §10 D-VALIDATE-* questions are PINNED at this SPEC (do NOT re-litigate; the PLAN/IMPL EXECUTE these):**
  - **D-VALIDATE-CONSTRUCT-BOUNDARY** — the construction boundary between `bootstrap.Load` and `listener.NewManagerWithBaseDirAndAllowH2C`'s return is CONFIRMED CLEAN (SPEC §3.1): no third hidden side effect beyond the two already-known ones (access-log file opens, stats-sink UDP dials).
  - **D-VALIDATE-BOOT-REUSE** (the SPEC's central finding, **AMEND-VALIDATE-DEPGRAPH**, SPEC §3.2) — `internal/boot.Construct` does **NOT** build the cluster manager internally: `sinks`/`tracingProvider` already depend on the `cm`-derived `grpcclient.Dialer`, so `cluster.NewManagerWithBaseDir` stays a one-line call made identically by BOTH callers OUTSIDE `Construct`. `Construct`'s pinned signature: `Construct(bs *bootstrap.Bootstrap, cm *cluster.Manager, baseDir string, allowH2C bool, sinks []accesslog.Sink, dm *drain.Manager, httpClient *httpclient.Client, tracingProvider *tracing.ExporterProvider) (*listener.Manager, error)` — it owns only the three registry builds + `listener.NewManagerWithBaseDirAndAllowH2C`. A `boot.NewTracingProvider` helper (folding in the relocated `tracesDialerAdapter`/`zipkinTransportAdapter`) and the relocated `maybeWrapLuaScriptLoadError` (+ its two consts) round out `internal/boot`.
  - **D-VALIDATE-MODE-VALUES** — bare `"validate"` only, case-sensitive exact match; any other non-empty `--mode` value is a usage error via the EXISTING `os.Exit(2)` convention.
  - **D-VALIDATE-FUZZER** — NO new fuzzer (fuzzers stay **52**): `Construct`/`validate.Bootstrap` operate on an already-`bootstrap.Load`-validated proto, not a new untrusted-input parse boundary.
  - **D-VALIDATE-EXIT-CODES** — `0` = valid ("configuration OK" to stdout) / `1` = invalid (the underlying error to stderr) / `2` = usage error (unrecognized `--mode` or missing `-c`, both via the pre-existing convention).
  - **AMEND-VALIDATE-HTTPBUILTINS-NO-DEPS** (a correction to the BRAINSTORM's assumption) — `internal/filter/http/builtins.RegisterBuiltins(reg *filter_http.HTTPRegistry)` takes **NO `Deps` struct** (unlike its `internal/filter/network/builtins` sibling): re-verified against all 20 current `httpReg.Register` call sites in `main.go`, no HTTP builtin factory captures a boot singleton at registration time (HTTP filters defer ALL dependency injection to per-chain build time via `hcm.ListenerCtx`/`FactoryCtx` — a genuine architectural asymmetry vs. network filters, not a mirroring choice).
  - The CLI's `--mode validate` handler calls `validate.Bootstrap` **directly** (NOT the `BootstrapFile` convenience wrapper), so `-allow-h2c` continues to compose correctly with `--mode validate` (SPEC §3.5).
- A spec-document-reviewer (general-purpose) pass ran against this SPEC before landing: one trivial off-by-one line-citation was fixed (`main_test.go:32-120` → `32-121`); every other file:line/signature/doc-comment citation plus the fuzzer(52)/fixture(96)/ADR-tail(ADR-0267, next-free ADR-0268) counts were VERIFIED EXACT against HEAD `78dcd772`.
- Counts at SPEC (re-verify, do NOT assume): stat surface **1200** (non-H2 **1196**) / fixtures **96** / fuzzers **52** / BackendKind **38** / DECISIONS **ADR-0267** (next-free **ADR-0268**). This phase is anticipated to leave ALL of these UNCHANGED at IMPL (+0 everywhere) except **THREE new Go packages** (`internal/boot`, `internal/filter/http/builtins`, `github.com/esalaine/envoy-go/validate` — the first public one) and **ADR-0268** landing (next-free ADR-0269) — it has NO differential surface and registers no new stat.

=> The NEXT action is the **phase 51 (`bootstrap-validate-mode`) PLAN** (lifecycle-state 2 → 3). NOT the termination sentinel. NOT a new BRAINSTORM or SPEC. NOT the IMPL directly.

==============================================

## The NEXT action — the phase 51 (`bootstrap-validate-mode`) PLAN

Invoke `superpowers:writing-plans`: write `docs/envoy-go/phases/51-bootstrap-validate-mode/PLAN-51.md`, the task-by-task TDD spine for the extraction + new packages + CLI flag pinned at the SPEC. This is a **docs-only, direct-on-master** stage (the phase-44 through 50 PLAN-stage precedent — a fresh worktree is created at IMPL, not at PLAN, per `feedback_git_worktrees`).

**The §11 D-VALIDATE-* PLAN/IMPL-ergonomics questions this PLAN must resolve (re-read `docs/envoy-go/phases/51-bootstrap-validate-mode/SPEC-51.md` §11 for full context before starting):**
- **D-VALIDATE-CONTRACT-FILE** — confirm the exact filename/section this project's behavior-contract doc uses (SPEC §9 assumes `BEHAVIOR_CONTRACT.md` — verify against the actual repo-root file fresh, do not trust the assumption).
- **D-VALIDATE-TEST-FIXTURES** — pin the exact minimal set of NEW bootstrap-valid-but-construction-invalid YAML fixtures for `internal/boot`/`validate` unit tests (SPEC §8.1 names three categories: a bad TLS cert path [fails inside `cluster.NewManagerWithBaseDir`], a Lua script that fails to compile [fails inside `listener.NewManagerWithBaseDirAndAllowH2C`, exercising the relocated `maybeWrapLuaScriptLoadError` wrap], and a malformed/unregistered HTTP-filter `typed_config` [fails via the frozen `httpReg`'s resolution]) plus which EXISTING `internal/bootstrap/bootstrap_test.go` reject-arm fixtures get reused verbatim (fed through `validate.Bootstrap` instead of `bootstrap.Load` directly, per SPEC §8.1) — likely 3-5 new small test functions plus reuse of the existing ones. At least ONE fully-valid bootstrap fixture is required too (the positive-path `nil`-return proof).
- **D-VALIDATE-ALIAS-CONVENTION** — pin the exact import-alias name for `internal/filter/network/builtins` vs `internal/filter/http/builtins` in `main.go`/`internal/boot/boot.go` (SPEC §3.3 proposes keeping network `builtins` unaliased and aliasing the new one `httpbuiltins` — confirm no better existing convention elsewhere in the codebase before locking it in).
- **D-VALIDATE-CLI-TEST-CONFIG-SHAPE** — pin the exact minimal "bad config" YAML the new `cmd/envoy-go/main_test.go` CLI-subprocess test (SPEC §8.2) uses to trigger a construction-time (not parse-time) failure cheaply and deterministically — SPEC proposes a nonexistent TLS cert path as the cheapest/simplest; confirm or pick an alternative.

**Also nail down at PLAN time (mechanical TDD-task breakdown, not previously resolved as open D-questions):**
- The exact task order: SPEC §3 lays out the extraction in a natural sequence (1: `internal/filter/http/builtins` extraction + its own unit test proving registration parity with `main.go`'s pre-extraction behavior; 2: `internal/boot.Construct` + `NewTracingProvider` + the relocated Lua-wrap/tracing-adapter helpers, with `main.go` rewired to call them, proving the EXISTING full differential suite + `main_test.go` subprocess tests still pass byte-identically — this is the regression anchor for the refactor, since NOTHING should observably change yet; 3: the new `validate` package + its unit tests per D-VALIDATE-TEST-FIXTURES; 4: the `--mode validate` CLI flag + its new `main_test.go` subprocess test per D-VALIDATE-CLI-TEST-CONFIG-SHAPE; 5: ADR-0268 body + BEHAVIOR_CONTRACT + STATE/ROADMAP + PROGRESS close + the fuzzer-count reconcile) — PLAN pins the exact task count/boundaries, likely 6-8 tasks matching the phase-50 PLAN's granularity precedent.
- Confirm whether Tasks 1-2 (the pure refactor, before ANY new package/behavior exists) should be verified via the FULL existing differential suite (`test/differential/`, all 96 fixtures) + the FULL `cmd/envoy-go` test suite BEFORE proceeding to Task 3 — this is the byte-stability regression anchor the SPEC's Non-purposes section (§2) implicitly requires ("every existing strict-reject/parse-arm... stays byte-for-byte unchanged"), since a refactor this size (moving ~120 lines out of a 517-line `main.go`) is exactly the kind of change that could silently break something without a full-suite gate mid-way.

**Global constraints to carry into the PLAN (from the SPEC, re-confirm don't re-derive):** anticipated counts at IMPL — stat **1200** (+0) / fixtures **96** (+0, no differential surface — the FULL 96-fixture differential suite is a REGRESSION gate for this phase, not a growth target) / fuzzers **52** (+0, D-VALIDATE-FUZZER resolved) / BackendKind **38** (+0) / DECISIONS **ADR-0268** (next-free ADR-0269); THREE new packages (`internal/boot`, `internal/filter/http/builtins`, `validate`), ZERO new go.mod modules (`go mod tidy -diff` must stay EMPTY).

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** The next stage is the phase-51 PLAN (docs-only, direct-on-master). Lifecycle-state 2 → 3.
- **Re-verify, do not assume.** Every file/line citation in the SPEC must be re-checked against the CURRENT `main.go`/`internal/boot` (once it exists)/`internal/filter/http/builtins` (once it exists) before the PLAN relies on it — this project's standing discipline. The SPEC's own citations were spec-document-reviewer-verified against commit `78dcd772`; re-confirm HEAD hasn't moved since.
- **Process anchors:** ADR-0044 (ADR §Context at SPEC [DONE, SPEC-51.md §12], §Decision+§Consequences at IMPL) · ADR-0045 (sub-split soft gate — re-confirm the SPEC's "single flat row" call still holds once the PLAN has real per-task LoC estimates, §3.0) · next-free **ADR-0269** (ADR-0268 lands at THIS phase's IMPL).
- **This phase has no differential discipline to carry for NEW fixtures** (`reference_differential_*` memories do not apply to new-fixture design — no fixture, no live reference Envoy comparison), but the EXISTING 96-fixture differential suite IS the regression anchor for the refactor portion (Tasks 1-2) — treat it with the same rigor as any phase that touches shared boot-sequence code.
- At IMPL (not this PLAN stage): `feedback_execution_style` (subagent-driven) / `feedback_git_worktrees` (fresh worktree) / `feedback_subagents_no_push` / `feedback_subagent_autocommit_claudemd` / `feedback_pertask_gofmt_lint` / `feedback_push_to_origin` all apply — carried forward from BRAINSTORM/SPEC memory, not re-derived here.

## Read first (in order)
1. THIS FILE — the cold-start router (phase 51 `bootstrap-validate-mode` SPEC LANDED; the next action is its PLAN).
2. `docs/envoy-go/phases/51-bootstrap-validate-mode/SPEC-51.md` — the full design (§3 the construction-boundary extraction + exact `Construct`/`validate.Bootstrap`/CLI signatures, §8 the testing envelope, §10 the resolved D-VALIDATE-* pins, §11 the PLAN/IMPL D-questions this PLAN resolves, §12 the ADR-0268 §Context draft).
3. `docs/envoy-go/phases/51-bootstrap-validate-mode/BRAINSTORM.md` — the original design context + the 4 resolved user-dialogue questions (do not re-litigate).
4. `docs/envoy-go/STATE.md` — the active-phase header (`phase 51 (bootstrap-validate-mode) SPEC done`) + the recorded NEXT (`phase 51 PLAN`) + the counts (1200 / 96 / 52 / 38 / ADR-0267, next-free ADR-0268).
5. `docs/envoy-go/ROADMAP.md` — row 51 `in-progress` + the "Operational tooling family" section, both reflecting SPEC-done.
6. `cmd/envoy-go/main.go` fresh against HEAD (the SPEC's §3.1/§3.2/§3.3 citations were spec-document-reviewer-verified at commit `78dcd772` — re-confirm HEAD hasn't drifted) + `internal/filter/network/builtins/builtins.go` (the pattern the new HTTP sibling mirrors where applicable) + `internal/bootstrap/bootstrap_test.go` (the existing reject-arm tests D-VALIDATE-TEST-FIXTURES reuses) + `cmd/envoy-go/main_test.go` (the subprocess-test convention the new CLI test extends).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — this phase's PLAN carries NO differential-specific memories (no fixture, no live reference comparison); the relevant standing disciplines are `feedback_execution_style`/`feedback_git_worktrees`/`feedback_subagents_no_push`/`feedback_subagent_autocommit_claudemd`/`feedback_pertask_gofmt_lint`/`feedback_push_to_origin` (all apply at IMPL, not this PLAN stage).
