=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **PHASE 91 (`h2-framer-partial-frame-desync`) IS CLOSED. ROW 91 READS `done` AT `ROADMAP.md:153` AND `want` IS 123.** ⇒ **THERE IS NO BANKED MID-LIFECYCLE WORK. THIS SESSION SELF-PICKS A NEW SUBJECT AND RUNS ITS BRAINSTORM (phase 92).** ⚠️ **THE SENTINEL DOES NOT FIRE — check (1) is SILENT, check (2) prints SIX, check (3) is SILENT. ONE check blocks it. DO NOT create `stop`.** ⚠️ **MEASURE ALL THREE YOURSELF BEFORE DOING ANYTHING ELSE; do not take this file's word for it.**

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE. This form parses FIELDS instead of matching a pattern, and
#     asserts the row DENOMINATOR so a deleted row is itself an error.
awk -F'|' -v want=123 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **CHECK (1) IS NOW SILENT — THE ALL-DONE BOARD SHAPE.** With row 91 flipped, ONLY check (2) blocks the sentinel. Run BOTH doctoring NCs EVERY SESSION anyway:
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md.
```
⚠️ **NC-A IS BACK IN ITS ONE-LINE FORM** (`NOT DONE: row 62`) because the board is all-`done` again. **Do NOT carry the phase-91 TWO-line expectation onto this board**, and do not read the one-line result as breakage.
```sh
# the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```
Also NC the DENOMINATOR (`want=122` on the real file must print `GATE FAIL: examined 123 data rows, expected 122` — and, now that the board is all-`done`, **NO `NOT DONE:` line**).

⚠️ **AND WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -oiE '-family row'` fails with `grep: amily row: No such file or directory` and the surrounding arithmetic prints `base=0 now=0 delta=0`, **which reads exactly like "no change."** **Always pass `--` before the pattern**, and the counts are form-dependent: **95 occurrences vs 67 LINES** (`grep -c` counts LINES). **A gate that reads zero on BOTH sides is not evidence of invariance.**

⚠️ **THE ARM-A MALFORMED-ROW FIGURE RECONCILES ONLY UNDER AN ESCAPE-AWARE FIELD COUNT — RE-VERIFIED AT THE PHASE-91 IMPL, WHERE IT STILL READS *TWO*.** The malformed rows are ids **57** (`NF=9`) and **69** (`NF=10`), at lines **119** and **131**. ⚠️ **A NAIVE `awk -F'|'` `NF != 8` FORM READS *SEVENTEEN*** — legitimately ESCAPED pipes (`\|`) split as fields. **Strip `\|` BEFORE splitting.** A gate asserting `== 0` FAILS on pre-existing content; assert against the known-2 baseline. ⚠️ **Row 91 field-counts to NF=8 under BOTH forms AFTER the flip — verified before committing.**

⚠️ **AND DO NOT SUBSTITUTE A "REASONABLE EQUIVALENT" FOR CHECK (1)'s AWK.** A measurement agent once tried `NF>=8 && $2 ~ /^ *[0-9]+ *$/`: it printed **92**, not 123, because the naive split of escaped-pipe rows moves `NF` and the numeric `$2` anchor then rejects rows the canonical form accepts. **Use the form above verbatim.**

**SENTINEL RUN MECHANICALLY AT THE PHASE-91 IMPL (2026-08-24), BEFORE AND AFTER the row-91 flip.** ACTUAL output, recorded not predicted:
- **BEFORE** — (1) `NOT DONE: row 91` · (2) SIX · (3) SILENT. **AFTER** — **(1) SILENT** · (2) **SIX** at `:201 :207 :213 :223 :229 :237` · (3) **SILENT**.
- **All four NCs fired:** NC-A **ONE-line** `NOT DONE: row 62` after the flip (two-line before), with `NC LANDED? [ in-progress ]` **inspected FIRST** · NC-B `want=122` => `GATE FAIL: examined 123 data rows, expected 122` **only** · NC-C residual **2 -> 0** => `NEVER OPENED: gRPC`, WASM correctly silent · NC-D long **5** / short **1** / union **6**.
⇒ **THE SENTINEL DOES NOT FIRE — ONE check blocks it. `stop` WAS EVALUATED AND DELIBERATELY NOT CREATED** (verified absent at the git root AND in the stage worktree).
- ⚠️ do NOT "fix" the six and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases; the candidate sentences are a WINDOW onto a larger deferred backlog, not an inventory of it. ⚠️ **THE INVENTORY IS ~59, NOT ~42** — the long-carried "~42" is the SENTENCE-VISIBLE subset only. **Cite ~42 only as "sentence-visible", never as the inventory.**

==============================================

## STATUS AS OF 2026-08-24: PHASE 91 (`h2-framer-partial-frame-desync`) **CLOSED** — row 91 `done` at `ROADMAP.md:153`, `want` **123**. **THIS SESSION SELF-PICKS PHASE 92 AND RUNS ITS BRAINSTORM.**

**What the 91 IMPL landed (2026-08-24, base master `7dd10924`, branch `phase-91-impl`, ONE squashed commit).** Production **+300 / −55 across 3 files in ONE package** (`internal/filter/hcm/h2/{framer.go,conn.go,client.go}`); tests **+660 / −0** in **TWO NEW files** (`framer_reader_test.go` 460, `framer_leak_test.go` 200). `PROGRESS.md` created (phase dirs STAY **132**). **ADR-0313 COMPLETED IN PLACE** — §Decision + §Consequences appended after the RETAINED italic footer, **no renumber, NO `---` separator** (`^---$` STAYS **216**), tail STAYS **ADR-0313**, next-free STAYS **ADR-0314**. `go.mod`/`go.sum` **BYTE-UNTOUCHED**; `BEHAVIOR_CONTRACT.md` BYTE-UNTOUCHED. `ROADMAP.md` **1 insertion / 1 deletion** — a FLIP, not an ADD; stays **241 lines / 123 data rows**.

### ⚠️ THE DEFECT IS FIXED AND THE FIX IS PROVEN — this is the row's actual result

`framer.go` no longer wraps `http2.Framer.ReadFrame()` in a read deadline. The read side runs on a dedicated goroutine (`startReader`/`readerLoop`/`release`/`exitErr`/`closeReader`) that reads with **NO deadline, ever**; ctx-awareness comes from selecting on an **UNBUFFERED** frame channel instead. A frame read is therefore never abandoned part-way, so `io.ReadFull` can never discard bytes it already drained.

**Measured, interleaved (U,P,U,P,…), 8 burners pinned to core 0 + the test ALSO pinned + `GOMAXPROCS=1`:**
| arm | n | FAIL | fast `<1s` | **third mode 1-9s** | hang `>9s` |
|---|---|---|---|---|---|
| UNPATCHED (T2) | 36 | **9** (25.0%) | 27 | **0** | 9 (10.00-10.03s) |
| UNPATCHED (T11) | 32 | **9** (28.1%) | 23 | **0** | 9 (10.01-10.03s) |
| **PATCHED (T11)** | 32 | **0** | 32 | **0** | **0** |

⚠️ **ARMS INTERLEAVED, NEVER BLOCKED** — the base rate is non-stationary (3/12, 6/12, 9/36, 9/32 across this row). Interleaving shares machine drift AND proves the machine was still producing the hang during the window the patched arm was sampled. ⚠️ **The uncontended control is 0.01 s PASS on BOTH arms and proves NOTHING** — that reading absorbed this deadlock as a flake three times. Stall points scattered **438-810 of 1024**. p(32 clean by chance) ≈ **2.6e-5**.

### ⚠️ FOUR INHERITED CLAIMS THE IMPL REFUTED BY EXECUTION — do NOT re-inherit the old wording

1. ⚠️ **h2spec 5.1.2/1 IS NOT A GATE for the burst-drain ordering guarantee, and *"the ONLY existing gate"* is REFUTED.** With `tryReadFrame` ablated to `return nil, nil` — which removes the burst deferral outright — h2spec stayed at **`95 tests, 94 passed, 1 skipped, 0 failed`, 4 runs out of 4 on the unpatched tree AND again on the patched tree**, per-case lines **byte-identical**, `5.1.2 Stream Concurrency: 1/1 passed` every time. **The ablation was CONFIRMED PRESENT IN THE COMPILED SUBJECT BY DISASSEMBLY** (`tryReadFrame` 5 instructions vs 63; the caller `processFrameAndMaybeDrain` 32 instructions with ZERO refs to `framer.go`, zero `NewTimer`, zero `selectgo`). **A build was never trusted as evidence the ablation landed.** Mechanism: the ablation drops no frames — the outer `readFrameCtx` loop still reads and dispatches every one; the only change is `flushPendingDispatch()` running per-frame instead of per-burst. ⇒ combined with the SPEC's 204/204 result, the guarantee was pinned by **ZERO tests at ANY layer**, so **this row BUILT the missing gate** (below) rather than ship an unguarded rewrite. ⚠️ **The old "residual risk" framing UNDERSTATED it — the gate covers NONE of it.**
2. ⚠️ ***"`framer.go` MUST END WITH ZERO `SetReadDeadline` SITES"* IS FALSE AS LITERALLY STATED**, and the frozen design contradicted it in the same document: the `aLongTimeAgo` stamp lives in `closeReader`, which lives in `framer.go`. **A gate asserting `grep -c SetReadDeadline framer.go == 0` REDS ON A CORRECT FIX.** The landed and asserted posture is **ZERO of the SIX polling/clearing sites** (NCs: `time.Now().Add(`, `time.Time{}`, `50 * time.Millisecond`, `os.ErrDeadlineExceeded`, `nerr.Timeout()` all **0**) and **EXACTLY ONE remaining site — the stamp**, asserted to be inside the `closeReader` body with argument `aLongTimeAgo`.
3. ⚠️ **`TestFramer_ReadFrameCtxCancel` PASSES UNMODIFIED — the predicted "+2/−0 repair" was UNNECESSARY and NO EDIT WAS MADE.** `newFramer` allocates all four channels, so `frameCh != nil`, the not-started guard cannot fire there, and the test returns via the `<-ctx.Done()` arm exactly as before. **Editing a passing test to match a wrong prediction is the silently-adjusted assertion the project has a standing rule against.**
4. ⚠️ **The leak window is `0` versus `40`, NOT the inherited *"delta 5 over 40 connections"*.** With the fix delta is **0** immediate and 0 post-poll; with `closeReader` gutted it is **40**, still 40 after a 2 s poll. The guard's slack is therefore **2, set from the NOISE FLOOR** — ⚠️ **the precedent's `+8` would have MASKED the very leak the inherited figure described.**

### ⚠️ THE GATE THIS ROW BUILT, AND THE THREE VACUOUS PINS ITS NCs CAUGHT

`TestServerConn_BurstDrainDefersDispatchUntilBurstDrained` — 24 streams at `MaxConcurrentStreams = 4` in a **single `Write`**, **fast** dispatcher, **two arms with `t.Errorf` so neither masks the other**: refusal COUNT == `streams - max` == 20, and EVERY refusal precedes EVERY DATA. **NC 8/8 RED, both arms firing.** Under the ablation the full package shows only the two new guards red while **all 204 pre-existing tests stay green**.

⚠️ **THE NEGATIVE CONTROLS CAUGHT THREE OF THIS ROW'S OWN NEW TESTS BEING NON-DISCRIMINATING. ALL THREE PASSED WHEN WRITTEN — PASSING WAS NEVER THE QUESTION.**
- ordering pin, first draft: **0/5 RED** — at `max=2` the first RST is at index 0 *regardless* (the frame loop out-runs dispatch), so first-RST-before-first-DATA was **structurally unable to fire**. Three candidates then measured over 20 ablated runs: count **20/20**, all-before-all **19/20**, first-before-first **11/20 — DISCARDED**.
- `ReadErrIsSticky`: **0/10 RED, VACUOUS** — a bare peer close stores `io.EOF`, which is exactly what `exitErr` substitutes when `readErr` is nil, so dropping the stored error still returned `io.EOF`. Fixed by provoking a real `FRAME_SIZE_ERROR` and asserting the error CLASS -> **10/10 RED**.
- `CtxErrTakesPrecedence`: **7/20 RED, COIN FLIP** — with the early return deleted BOTH select arms are simultaneously ready and Go picks uniformly at random. Fixed by **32 independent draws** -> **20/20 RED**.
⚠️ **A TEST THAT PASSES IS NOT THEREBY A GUARD.** Every one of these would have shipped as coverage it did not provide. **Budget for NC-ing every new pin, not just the ones that look risky.**

⚠️ **AND THE LEAK GUARD WOULD HAVE BEEN VACUOUS BUILT THE OBVIOUS WAY — MEASURED, NOT REASONED.** With a plain request/response iteration the reader parks in `read(2)`, where `Run`'s own `defer s.conn.Close()` unblocks it **without** `closeReader` — the ablated build showed delta **0** every run, i.e. **green with the fix deleted**. The helper floods PINGs so the server blocks writing ACKs to a non-reading client, parking the reader in the release-wait state that **only** `close(stopCh)` reaches. **NC 5/5 RED.**

### ⚠️ THE INLINE-JOIN DISCLOSURE — a MEASURED no-op, stated as one

Deleting drain join A (`numstat 0 3`) and separately join B (`0 6`) each leaves the package at **rc=0, 204/204/116/0/0** — the baseline exactly. ⇒ **both `s.fr.closeReader()` drain calls are no-ops at this tip.** They are **retained deliberately** (they make socket-exclusivity local to the drain, and become live guards if a future change lets a running reader reach one). **Recorded as measured, never implied.** ⚠️ **Do NOT read this as a reason to delete them.**

### D-91-RACE — three parts, all executed

(a) full differential suite **without** `-race` (vacuous on this row's axis — the subject is a separate process built without it and there is NO build seam; ⚠️ the differential *test binary* DOES link the h2 package and `go list -deps -test` says so, but **`-test` is load-bearing — without it the selector reads 0** — and **linking is not executing**). (b) `-race` MANDATORY at `./internal/filter/hcm/h2/`, `./internal/cluster/` (the only in-process gate driving the CLIENT read path) and `./internal/filter/hcm/`. (c) a bounded race-instrumented SUBJECT probe over the four ALPN-`h2` fixtures — `-race` added at **BOTH** build sites (one alone silently instruments one spawn path), all four ran `--- PASS`, SKIP 0, anchored `^panic:|DATA RACE|SIGSEGV` = **0** over combined output with `2>&1`. ⚠️ **The zero was NEGATIVE-CONTROLLED**: the `-race` build links **51** `racefuncenter`/`__tsan_*` symbols vs **0** plain. ⚠️ **NAMED LIMIT: a SMOKE PROBE, not fleet coverage; it cannot reproduce the contended trigger.**

### ⚠️ AN UNATTRIBUTED ONE-OFF — recorded, not buried, not attributed

One top-level failure appeared in a single early `-race` full-package run during test development. It was **NOT captured**, and did not reproduce in 35 subsequent full-package `-race` runs, 810 `-race` executions of the new tests, or 4 further controller runs. **Not attributed to this row; not dismissed.** Shape is consistent with the known full-suite startup flake — **a conjecture, labelled as one.** If it recurs, capture the failing test name FIRST.

### ⚠️ THE SELF-PICK INVENTORY — ✅ **APPLICABLE THIS SESSION** (row 91 is `done`; no banked work)

⚠️ **"The FIVE terse windows split cleanly on ` + `" is FALSE.** `:212` naively splits to **10**, but items 2-3 are ONE candidate — `upstream SDS (server-cert + validation_context)` — split by a `+` **inside parentheses**. True count **9**. ⚠️ **"`:222` is NOT MECHANICALLY COUNTABLE" is FALSE** — terminate the live sentence at the first `**Phase` marker and it counts cleanly. ⚠️ **The "TWO closed `:222` candidates" warning is MIS-SCOPED** — those appear on line 222 only inside historical narrative, never in the live deferred sentence.

Extraction form (anchors are the POST-INSERTION ones):
```sh
sed -n "${L}p" docs/envoy-go/ROADMAP.md | command grep -oE '(remaining deferred \(not-yet-chartered\)|deferred) candidates:.*' | sed 's/\*\*Phase .*//; s/\*\*`.*//'
```
- **`:201` HTTP/3 — 7:** upstream H3 cluster · alt-svc advertisement · 0-RTT/early-data · h3spec conformance gate · QuicProtocolOptions tuning · full QUIC transport-socket options · QUIC robustness.
- **`:207` gRPC — 10:** gRPC streaming (**blocked** on the buffering seam) · `grpc_http1_bridge` and `grpc_web` (**both blocked** on the H1-to-H2 502) · gRPC-JSON transcoding · reverse transcoding · `grpc_field_extraction` · `grpc_stats` · `connect_grpc_bridge` · the `test/conformance/grpc/` interop client declared at `BOOTSTRAP_PROMPT.md:350` but never created · wiring the dead `RunEncodeTrailers` hook.
- **`:213` xDS — 9** (NOT 10 — see the paren artifact above): SDS rotation · upstream SDS (server-cert + validation_context) · CDS/EDS · LDS/RDS · ADS (muxed) · Delta xDS · RTDS/Runtime-layer · reconnection-backoff/`initial_fetch_timeout` edges · `google_grpc` transport.
- **`:223` Observability — 3:** the DYNAMIC half of the downstream TLS `ssl` stat family (**blocked on NAMING**: the stats name pattern bans the hyphen, so an OpenSSL-form cipher name fails validation and panics at registration) · the uncounted non-certificate handshake-failure bucket (`connection_error`) · tracing `spawn_upstream_span`/`http_service`/force-trace.
- **`:229` Runtime — 8:** RTDS layer · the `/runtime` and `POST /runtime_modify` admin endpoints · disk layer + override dir · admin layer · lifting the six `runtime_key` filter parse-reject arms · honoring the silent-IGNORE runtime knobs · hot restart proper · graceful-drain beyond phase 08.
- **`:237` Operational-tooling — 1 by ` + ` split, but THREE comma-separated items:** xDS-sourced dry-validation, an admin-API live-reload-and-validate endpoint, ⚠️ **and an RTDS/SDS validate companion the prior inventory OMITTED.**
⇒ **Corrected live total 38, NOT the inherited 35.** ⚠️ **DO NOT QUOTE 88** — that is the naive whole-line split of the Observability window. ⚠️ **BLOCKED IS NOT THE SAME AS DEFERRED.** Three `:207` candidates carry MEASURED ceiling blockers. **Picking one means picking its blocker first.**

⚠️ **A MAINTENANCE-ROW CANDIDATE THE 91 IMPL SURFACED AND DID NOT FIX** (a legitimate small self-pick): **the pre-existing `net.Conn` leak at `client.go`'s three `NewClientConn` error paths** — all three `cancel()` without closing `upstream`, and `git grep -n 'upstream' client.go` shows **no `upstream.Close()` anywhere in the file**. It is unrelated to frame reading, was out of row 91's charter, and was deliberately NOT fixed quietly.

## ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + ALL FOUR NCs YOURSELF** and record ACTUAL output. **Check (1) is SILENT on this all-`done` board and NC-A reads ONE line.**
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** The 91 BRAINSTORM refuted five of seven ADR-0312 follow-ons; the SPEC refuted seven inherited claims; the PLAN refuted **ten** and discharged one of the SPEC's own design arguments; the IMPL refuted **four more**, two of which would have produced a gate that REDS ON A CORRECT FIX or PASSES ON A BROKEN ONE. Do the same.
3. **LINE ANCHORS DRIFT; SYMBOL ANCHORS DO NOT — RE-VERIFY EVERY CITE AT YOUR TIP.** ⚠️ **PATHSPEC-SCOPE every symbol assertion.** ⚠️ **THE RECEIVER NAME IS PART OF THE ANCHOR.** ⚠️ **A SYMBOL ASSERTION WHOSE RECEIVER IS WRITTEN `(s *T)` MUST USE `grep -F`, NOT `-E`** — ERE reads the parentheses as a group and returns a **FAIL-UNSAFE ZERO**.
3b. ⚠️ **CHARACTER-CLASS SELECTORS ARE THE OTHER FAIL-UNSAFE FAMILY.** (i) `grep -cE '^\s+[a-z0-9./-]+ v[0-9]'` on `go.mod` reads **62**, not **67** — it silently drops the five modules whose path carries an UPPERCASE letter or an underscore. (ii) `grep -oE '[A-Za-z]+ +BackendKind = 38'` prints `GoawayResponder`, not **`H2GoawayResponder`** — a **digit-blind** class silently truncating an identifier. **Prefer the structural form and always run a control that shows what was MISSED, not just what matched.**
3c. ⚠️ **`feedback_brief_citations_not_evidence` APPLIES TO A SUBAGENT'S INCIDENTAL FIGURES, NOT JUST ITS HEADLINE CLAIMS.** **Re-derive EVERY number you restate.** At the phase-91 IMPL the controller independently re-derived the package figures, the production-byte diff, the `goleak` absence and the landed test assertions before relying on any of them.
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. ⚠️ **TWO SDS dial-budget flakes are live** — `TestSDSEndToEnd_FetchFailure_BootFailsClosed` (`internal/boot`) and `TestProvider_FetchInitialCertificate_Timeout` (`internal/xds`). ⚠️ **DO NOT reuse the `go list -deps` exclusion blanket — TRUE for `xds` (0), FALSE for `boot` (1).** Also live: the driver-owned receiver port race · `internal/cluster` outlier · `internal/httpclient` zero-value · the two 84.2-era flakes · the REFERENCE h2spec section-8 flip. ⚠️ **`TestServerConn_TinyWindowDelivery` IS NO LONGER A FLAKE OR A FINDING — IT WAS A LIVE PRODUCTION DEADLOCK AND PHASE 91 FIXED IT.** If it ever fails again, that is a REGRESSION of row 91, not a flake.
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only). Foreign containers left untouched throughout phases 90 and 91: `infallible_booth`, `crazy_kare`, `golink-ai`, `quizzical_goldstine`.
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`.
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL.** ⚠️ **AND AT THE PHASE-91 IMPL EVEN THAT WAS NOT ENOUGH: an ablation was confirmed present in the COMPILED BINARY BY DISASSEMBLY** before its non-reddening gate result was believed. **When a gate fails to redden, disassemble before concluding the gate is blind.** **`gofmt -l` never exits non-zero — gate on OUTPUT.** ⚠️ **`golangci-lint` must be gated on OUTPUT too.** ⚠️ **`go test` WITHOUT `-v` PRINTS ZERO `=== RUN`** — `RUN=0` beside `RC=0` is a VACUOUS GREEN. ⚠️ **A `-run` selector matching nothing prints `[no tests to run]` and EXITS 0.** ⚠️ **On `-v` output, unanchored `grep -c 'FAIL'` reads 11 ON A FULLY GREEN TREE** — use `grep -cE '^(FAIL|--- FAIL)|^ *--- FAIL'`. ⚠️ **That hazard is TREE-scope; at PACKAGE scope both forms agree, and that agreement is NOT evidence the anchored form is unnecessary.**
7b. ⚠️ **`grep -c` ON ZERO MATCHES EXITS 1 — capture the value (`v=$(… || true)`), NEVER chain on it with `&&`.** This bit the phase-91 IMPL controller mid-stage and silently truncated a verification chain.
8. ⚠️ **`pgrep -f` / `pkill -f` MATCHES YOUR OWN SHELL AND KILLS THE TOOL CALL (exit 144)**. **Kill ONLY PIDs you captured yourself** (from `$!`), and verify residue with `kill -0`. ⚠️ **Start burners, run all iterations, and kill burners INSIDE ONE Bash call** — shell variables do not survive between calls, and a split loses the PIDs.
9. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-*`, `wt-*` or stage worktree should outlive its stage close** — expect only `master` (verify with `git worktree list`).
10. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** (a)/(b) differential **121 fixtures** + full `go test ./...`, gated on **`PIPESTATUS[0]`** and a **SET RECONCILIATION** (⚠️ **NOT `INNER_EXIT`, which DOES NOT EXIST**); (c) h2spec cite ONLY from your own run — ⚠️ **and h2spec is now MEASURED BLIND to burst-drain ordering, so do not cite it as an ordering gate**; grpc-conformance deferred in writing; proxy-wasm 10/16; (d) fuzzers 55/48; (e) the ANCHORED panic gate `^panic:|DATA RACE|SIGSEGV` on every differential launch; (f) no REVIEW.md — standing departure.
11. ⚠️ **THE FIXTURE INVOCATION: `go test ./test/differential/ -run 'TestDifferential/0004-h2-routing' -count=1 -v`.** ⚠️ **Gate 2's failure mode is a SILENT PASS** — the runner `t.Skipf`s an unregistered fixture and **no fixture-count gate exists anywhere**. **ASSERT THE FIXTURE SET, not merely that the suite was green.**
12. ⚠️ **`-p` PUBLISHING WORKS; `--network host` IS WHAT DOES NOT.** **Host-gateway is `192.168.65.2`** (⚠️ `getent hosts` resolves it IPv6-ONLY — use `getent ahostsv4`). ⚠️ A **STRICT_DNS** cluster whose name does not resolve **blocks listener init entirely**. ⚠️ **`clusters: []` is BOOT-REJECTED even when every route is a `direct_response`.** ⚠️ **envoy-go REJECTS `match.headers` outright**, and ⚠️ **refuses `codec_type: HTTP2` without TLS unless `--allow-h2c` is passed.** ⚠️ **`ENVOY_TARGET.md` is at `docs/envoy-go/ENVOY_TARGET.md`, NOT the repo root.**
13. ⚠️ **A RAW-FRAMER PEER MUST HOLD ONE `hpack.Decoder` PER CONNECTION.** ⚠️ **`0004` is TLS+ALPN-h2, NOT h2c.**
14. ⚠️ **`bash /dev/tcp` + `timeout cat <&3` INSIDE COMMAND SUBSTITUTION RETURNS SILENTLY EMPTY.** **Do not build an H1 probe on `/dev/tcp`.**
15. ⚠️ **GO DOES NOT FLAG UNUSED CONSTANTS.** Unused-symbol cleanup after a removal must be grep-driven. ⚠️ **This bit the phase-91 ablations: `const tryReadFrameWait` survives when its only use is deleted, so the package still builds** — which is why the removed-literal NC must target the USE SITE (`time.NewTimer(tryReadFrameWait)`), never the declaration.
16. ⚠️ **`golangci-lint`'s misspell runs in locale US.** **Sweep British spellings in `.go` comments BEFORE the gate.** ⚠️ **Markdown prose may use them freely; a `.go` comment may not** — check any code block you paste from a design doc.
17. ⚠️ **A GREEN RUN OF A CONTENTION-SENSITIVE TEST PROVES NOTHING.** Phase 91's subject cleared 5/5 uncontended while failing 9/32 contended on the SAME binary. **This is the reading that absorbed a real production deadlock as a flake three times.**
18. ⚠️ **NC EVERY NEW TEST, NOT JUST THE RISKY-LOOKING ONES.** At the phase-91 IMPL, **three of the row's own new pins passed while being non-discriminating** — one structurally unable to fire, one vacuous, one a coin flip. **A test that passes is not thereby a guard.** ⚠️ **And a poll-based leak guard's SLACK must be set from the measured NOISE FLOOR, never copied from a precedent** — the precedent's `+8` would have masked this row's leak.

## ⚠️ PORT BANDING

`net.ipv4.ip_local_port_range = 32768 60999`. ⇒ **BAND YOUR PROBE PORTS BELOW 32768.** `21000-24999` is clear of both the ephemeral range and every reserved band. **Check availability with `ss -tan` (ALL states), never `ss -ltn`** — the listener-only form reads a fail-unsafe "free". **A `bind: address already in use` in a 4xxxx band is NOT evidence a sibling session is squatting.** The static fixture band (10000-19172) and the reserved bands (20000-31007, 11000-14999) are all BELOW 32768.

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (ugrep under the hood). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Reproduced at THIRTY-SEVEN separate tips.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **THE METHOD BINDS; carry no standing discrepancy figure and no standing absolute.** ⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS; print FILE NAMES, never a count.** ⚠️ **THE HARNESS `grep` STRIPS A LEADING `./` FROM PATHS.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files; use `-v --no-index`, and `git status` is the discriminating check). ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **`grep -c` COUNTS *LINES*, NOT *OCCURRENCES***.

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 91 (h2-framer-partial-frame-desync)' --format='%h %s'   # 4 at this tip — a CLOSED phase, use as a FIRING CONTROL
```
⚠️ **The count comments are LIVE FIGURES — VERIFY before relying.** Take the NEWEST line (`| head -1`). A fabricated slug must read **0**.

Execution style: **subagent-driven per `feedback_execution_style`**. Subagents commit LOCALLY on the ONE stage branch with EXPLICIT PATHSPECS; the controller squashes (`feedback_subagents_no_push`, `feedback_push_to_origin`). **Pure MEASUREMENT agents should commit NOTHING and prove their tree clean.** ⚠️ **PARALLEL AGENTS IN ONE WORKTREE WORK IF AND ONLY IF THEIR PATHSPECS ARE DISJOINT** — ⚠️ **and at the phase-91 IMPL the safer pattern was PER-AGENT DETACHED WORKTREES pinned at a known commit**, which let a contention measurement, an h2spec run and a test-authoring agent proceed without any of them seeing another's edits. ⚠️ **A CONTROLLER WRITING INTO A SHARED WORKTREE WHILE AGENTS RUN WILL SHOW UP IN THEIR `git status` GATE — hold doc edits until the agents finish, or give them their own tree.** ⚠️ **GIVE EACH PARALLEL AGENT ITS OWN PRIVATE SCRATCH DIR.** Per-task `gofmt` + `golangci-lint` on touched packages, **gated on OUTPUT**. ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root** — build with `-o` into scratch. ⚠️ **AND VERIFY WHAT A SUBAGENT REPORTS.**

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root — **observed repeatedly at every phase-88 through -91 stage**. Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` before any commit or gate run. ⚠️ **Shell VARIABLES do not survive between Bash calls** — re-export in every command.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — `git status --porcelain` AND `git diff --stat master` must show ONLY your intended edits. For a patched TRACKED file: `sha256sum` BEFORE patching, `git checkout -- <path>` after, `sha256sum -c` the capture — ⚠️ **and COMMIT FIRST, because `git checkout --` restores from HEAD and will wipe uncommitted work.** ⚠️ **The phase-91 IMPL did this FIVE times** (h2spec NC ×2, the two ordering/leak NCs, the join deletions, `harness.go` `-race`) and every restore was verified by `sha256sum -c` => `OK`.

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads the phase-92 BRAINSTORM.** ⚠️ **§Project counts SELF-CONTRADICT §Current and carry NO label saying so — do not anchor on them.**
2. `docs/envoy-go/ROADMAP.md` — the six sentinel windows at `:201 :207 :213 :223 :229 :237` (the self-pick inventory), and row 91 at `:153` as the CLOSED precedent.
3. `docs/envoy-go/phases/91-h2-framer-partial-frame-desync/PROGRESS.md` — **especially §3 (h2spec is NOT a gate), §6 (three vacuous pins) and §7 (the leak guard's slack)**, which generalize to any row.
4. `docs/envoy-go/DECISIONS.md` **ADR-0313** — COMPLETE; §Consequences (ix)/(ix-bis)/(xi) carry the transferable findings.
5. `BOOTSTRAP_PROMPT.md` §4-§7 (repo ROOT). **No phase-92 dir ⇒ state DONE ⇒ BRAINSTORM.**

## What the next session owes
**SELF-PICK the phase-92 subject and RUN ITS BRAINSTORM** (state DONE -> 1), recording the pick AND the rejected alternatives ·
**RE-RUN the sentinel + all four NCs yourself** and record ACTUAL output (⚠️ **check (1) is SILENT and NC-A is ONE line on this all-`done` board**) ·
**DO NOT create `stop`** — check (2) still prints SIX ·
**smallest defensible candidate first**; ⚠️ **BLOCKED IS NOT DEFERRED** — three `:207` candidates carry measured ceiling blockers ·
a strong small candidate is **the `client.go` `NewClientConn` `upstream` leak** surfaced and deliberately not fixed by row 91 ·
**REFUTE YOUR PREDECESSOR BY EXECUTION** — every stage of row 91 did, and the IMPL's four refutations included two that would have produced a gate reading the wrong answer ·
open `ROADMAP.md` row **92** (`want` **123 -> 124**) at the SPEC, not the BRAINSTORM ·
`STATE.md` rolled IN PLACE with the oldest §Recent entry evicted by a **DIRECT DATE READ** (⚠️ **read the dates yourself every time — the phase-91 IMPL had a UNIQUE oldest and NO tie, while the PLAN had a two-way tie broken by last list position; the shape does NOT carry**) and archived to `STATE_HISTORY.md` as **ONE INLINE LINE** so the strict guard stays at **163, DELTA 0** ·
`next-prompt.txt` rolled (`git add -f`) · ONE squashed commit, merged and pushed.
