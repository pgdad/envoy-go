START a fresh 24.2 PLAN cold-start session for envoy-go.

This is a fresh-session prompt for the **24.2 PLAN authoring** of phase 24.2 (`global-ratelimit-perroute-and-headers`, the REMAINING-surface slice of `envoy.filters.http.ratelimit` — the GLOBAL rate-limit filter). The **24.1 IMPL is COMPLETE + squash-merged to master + pushed to origin** (the FIRST sub-phase of the phase-24 PLAN-time ADR-0045 split per ADR-0201); STATE points at **24.2**, lifecycle-state 2. The natural next-skill per STATE.md + SKILL_ROUTING state 2 is **`superpowers:writing-plans`** to author `docs/envoy-go/phases/24.2-global-ratelimit-perroute-and-headers/PLAN.md` from the existing 24.2 SPEC.md.

## What this session must do: author the 24.2 PLAN

Per SKILL_ROUTING state 2, run `superpowers:writing-plans`: the SPEC.md (already authored at the phase-24 split commit) is the authoritative input; the PLAN decomposes the 24.2 surface into bite-sized TDD tasks (each Task maps 1:1 to a `superpowers:test-driven-development` red→green→commit cycle). Per project memory `feedback_execution_style.md`, the NEXT (24.2 IMPL) session will run `superpowers:subagent-driven-development` over the PLAN's tasks — the PLAN must be subagent-friendly (each Task self-contained dispatch outline + commit template + TDD steps + acceptance gates).

1. **Create a fresh PLAN worktree** per `feedback_git_worktrees.md` (branch `phase-24.2-global-ratelimit-perroute-and-headers-plan`, off master tip — run `git log --oneline -1` to confirm the actual tip SHA).
2. **Apply the ADR-0045 split-gate verification** in-session: re-evaluate whether the 24.2 surface (the §4 file roster + the parent SPEC §1.2/§3.4 LoC envelope re-estimated against the post-24.1 baseline) stays ABOVE or BELOW the ~1500 split-gate. **Hypothesis:** 24.2 is BELOW the gate (the 24.1 PLAN noted the post-empirical phase-24 envelope at ~1900-2700 split across two sub-phases; 24.1 IMPL landed ~11k LoC of which ~2.7k was production code in `internal/filter/http/ratelimit/`; 24.2 adds ~5 files + ~150-250 LoC to `descriptors.go` + ~80-140 LoC each to `encode.go`/`headers.go`/`compiled_perroute.go` + ~300-500 LoC tests + ~scenarios). Verify the estimate in-session and either split-again (unlikely) or land 24.2 as a single sub-phase (expected).
3. **Decompose the 24.2 surface** into Tasks per the SPEC §1 5-bullet roster + §4 file roster + §3 differential envelope. Anticipated coarse-grained Tasks (the actual decomposition is the PLAN author's call):
   - Pre-Task 0 (PROGRESS preamble + 24.2 preconditions on the IMPL worktree)
   - Engine extension for the 5 remaining actions (`source_cluster`, `masked_remote_address`, `metadata`, `query_parameters`, `query_parameter_value_match`) + AMEND-11 key defaults (`query_param` SINGULAR + `query_match`) + `metadata` value-extraction accessor (parent §12 item 2 — `streamInfo().dynamicMetadata()` DYNAMIC vs route-metadata ROUTE_ENTRY)
   - `stage` multi-stage path (parse-time bucketing 0-10 + per-request selection; parent §4.4)
   - Axis-B `vh_rate_limits` cross-tier composition table (INCLUDE/IGNORE/OVERRIDE + legacy `include_vh_rate_limits` force-include; parent §4.3 + AMEND-5)
   - `compiled_perroute.go` `RateLimitPerRoute` 10th-canonical (parent §5.3; ADR-0199; data-only-with-vh-inclusion-enum + `override_option` INERT + `rate_limits[]` Axis-A + per-route `domain` override) + the **ADR-0125 roster amendment 9 → 10** in-place ADR edit
   - `encode.go` + `headers.go` X-RateLimit DRAFT_VERSION_03 emission (parent §6.6/§4.7; MIN `limit_remaining` + `;w=`/`;name=` quota suffix + unit→seconds; BEHAVIOR_CONTRACT §7.2 response-header allow-list addition) + the **byte-pin against captured upstream headers** (parent §12 item 5 — settled at IMPL `headers_test.go`)
   - Differential fixture EXTENSIONS to the existing `0032-http-ratelimit/` (no NEW dir): scenario (f) `vh_inclusion` cross-side + scenario (g) `x_ratelimit_headers` cross-side + scenario (d) `descriptor_actions` EXTENDED to all 10 actions
   - Fuzzer corpus extension (no NEW fuzzer; the existing `FuzzRateLimitConfigParse` gets seeds for the new actions + `RateLimitPerRoute` shapes + `stage` arms)
   - Atomic landing (BEHAVIOR_CONTRACT bundle completion + ROADMAP row 24.2 done + **parent row 24 in-progress → done [ROLLUP]** + STATE re-advance to whatever follows 24 + REVIEW)
4. **Anchor the 2 ADR landings at their Tasks** per ADR-0044 (§Context already drafted at the parent SPEC commit; §Decision + §Consequences land at IMPL):
   - **ADR-0199 (FULL)** — `RateLimitPerRoute` + ADR-0125 9→10 — lands at the perroute-compile Task
   - **ADR-0197 (REMAINING slice)** — X-RateLimit DRAFT_VERSION_03 headers + remaining-actions descriptor coverage — lands at the encode/headers Task (the CORE slice landed at 24.1 Task 7)
5. **Carry forward the ADR-0202 escape valve** (still UNCONSUMED post-24.1 per the D-hypothesis HELD). Anchor it at the X-RateLimit MIN-status quota-policy byte-edge Task + the `metadata`-action accessor Task. PLAN hypothesis: ADR-0202 stays UNCONSUMED at phase-24 phase-done; FIRES only if the X-RateLimit byte-edge or the `metadata`-accessor surface forces a divergence.
6. **Plan-document-reviewer review** (≤3 iterations per `superpowers:writing-plans`) — invoke `plan-document-reviewer` against the authored PLAN.md; address any rejections; re-review until APPROVED.
7. **Squash-merge the PLAN.md authoring** to master per the phase-09..23 stage-close pattern + push to origin per `feedback_push_to_origin.md`. Re-advance STATE.md to lifecycle-state 3 (`superpowers:subagent-driven-development` next) + update `next-prompt.txt` for the 24.2 IMPL cold-start session.

## Pre-existing settled context (DO NOT re-litigate)

- **The parent SPEC empirical AMEND-1..11 catalog is SETTLED** (parent SPEC §1.1) — the implementation contract. Do NOT re-derive. Load-bearing AMENDs the PLAN must carry forward: AMEND-4 inert `override_option` (`RateLimitPerRoute`); AMEND-5 Axis-B `vh_rate_limits` decision table; AMEND-7 `apply_on_stream_done` honor-as-absent; AMEND-11 key defaults (`query_param` SINGULAR for `query_parameters`).
- **24.1 IMPL is DONE** (squash-merged at `a4fdc75` + SHA-fill `dbd2d3b`) — the FOUNDATIONS for 24.2 are in place: the `internal/filter/http/ratelimit/` package (8 production files) + DELTA-1 `RateLimitClient` + DELTA-2 HCM route-table exposure + the 5 CORE actions in `descriptors.go` + OK/OVER_LIMIT/error dispositions + cluster-scoped stats + 19 HTTP filters + the §5 PARSE-REJECT roster (incl. the 3 envoy-go-strict departures) + `0032`/`0033` fixture directories + `FuzzRateLimitConfigParse` + 3 ADR landings (0197[core]/0198/0200). The 24.2 PLAN EXTENDS this surface; it does NOT re-land.
- **D-RL1 byte-confirmation RESOLVED** at 24.1 Task 5: RAW-PROTO SEED CONFIRMED; ADR-0202 UNCONSUMED. The 24.2 PLAN carries the escape valve forward to the remaining-risk surfaces (X-RateLimit MIN-status byte-edge + `metadata`-action accessor).
- **24.2 scope (do NOT pull 24.1 surface in):** the remaining 5 actions + `stage` multi-stage path + Axis-B `vh_rate_limits` table (full INCLUDE/IGNORE/OVERRIDE + legacy force-include) + X-RateLimit DRAFT_VERSION_03 emission + `RateLimitPerRoute` 10th-canonical + ADR-0125 9→10 amendment + scenarios (f)/(g) added to existing `0032` + (d) extended to all 10 actions. **NO new fixture dir** (stays 35) + **NO new fuzzer** (stays 33).

## Base + invariants at master tip

- **Master tip:** `070eb26` (`next-prompt.txt: repoint master-tip references to 93cab20 (actual HEAD)`; docs-only). Predecessors: `93cab20` (`next-prompt.txt: rewrite for 24.2 PLAN cold-start (post-24.1 IMPL squash)`; docs-only); `dbd2d3b` (`phase 24.1 IMPL stage-close: STATE.md SHA-fill (TBD-24.1-IMPL-SQUASH -> a4fdc75)`; docs-only); `a4fdc75` (squash-merge of `phase-24.1-global-ratelimit-core-and-route-table-impl`; 61 files / +11041 / -25 LoC); `e8a8881`/`55f7620`/`64078a3`/`1350e69` (the prior 24.1 PLAN-flavored docs commits). Run `git log --oneline -1` to confirm the actual tip SHA for the PLAN worktree base. Pushed to origin.
- **Baseline at master tip:** **35 differential fixture directories** GREEN (0000-0033; lua 0026-0029 + multi-listener combined runs may hit the documented `freeTCPPort` flake per 22.2 REVIEW §7.4 — re-run in isolation); **33 fuzzers** green (`FuzzRateLimitConfigParse` clean at 30s); h2spec 53/53 at the ADR-0051 v1.32.4 pin; build/vet/lint/race clean; **19 HTTP filters wired** (oauth2 ↔ ratelimit ↔ rbac alphabetical); **114 stat names** (incl. the new `cluster.<rls>.ratelimit.{ok,error,over_limit,failure_mode_allowed}`); **18 envoy-go-strict departure records** (incl. the 3 ratelimit-strict 24.1 additions). DECISIONS.md tail at **ADR-0201**; ADR-0197[core]/0198/0200 full bodies landed; ADR-0199 §Context-anchored only (§Decision+§Consequences land at 24.2 IMPL); **next-free ADR-0202** (UNCONSUMED).
- **Canonical-per-route roster STAYS 9 at this PLAN session** (the 9→10 amendment is ANCHORED in ADR-0199 §Context but LANDS at 24.2 IMPL — the PLAN must specify WHERE the amendment paragraph edits ADR-0125; ADR-0044 ADR-on-IMPL discipline).
- Project memories ALL apply: `feedback_git_worktrees.md`, `feedback_push_to_origin.md`, `feedback_execution_style.md`, `reference_differential_fixture_dispatch_constraint.md`, `reference_differential_asserter_dispatch.md` (scenario h on `0032` is StatsAsserter-dispatched; (g) on `0032` is cross-side `CompareBytes`-dispatched per the existing fixture's discipline).

## Read first (in order)

1. THIS FILE — the 24.2 PLAN cold-start + the PLAN authoring obligations.
2. `docs/envoy-go/phases/24.2-global-ratelimit-perroute-and-headers/SPEC.md` — the 24.2 scope + file roster (§4) + differential additions (§3) + gates (§6) + ADR landing (§5) + acceptance (§7). THE authoritative SPEC input.
3. `docs/envoy-go/phases/24-http-filter-global-ratelimit/SPEC.md` — the RETAINED parent master SPEC (§1.1 AMEND catalog; §4.1 all-10-actions table; §4.3 Axis-B; §4.4 stage; §4.7 X-RateLimit byte-pin; §5.3 `RateLimitPerRoute`; §6.6/§6.7/§6.10 code shapes; §10 ADR map; §11 D1–D7; §12 byte-confirmations; §13 BEHAVIOR_CONTRACT; §14 testing; §15 acceptance).
4. `docs/envoy-go/phases/24.1-global-ratelimit-core-and-route-table/REVIEW.md` — the 24.1 IMPL review (SHIP recommendation; the post-24.1 baseline summary; cross-references to the landed ADR bodies; the Task-7 + Task-10 follow-up fix notes).
5. `docs/envoy-go/phases/24.1-global-ratelimit-core-and-route-table/PROGRESS.md` — the 24.1 IMPL Task-by-Task trail (the precedent for the 24.2 PROGRESS.md structure; the actual landed file sizes + commit SHAs for the 14 IMPL commits).
6. `docs/envoy-go/STATE.md` — active-phase 24.2; lifecycle-state 2; next-skill `superpowers:writing-plans`; next-free ADR-0202.
7. `docs/envoy-go/ROADMAP.md` — row 24 parent (`in-progress`; closes at 24.2 phase-done); row 24.1 (`done` per the 24.1 IMPL squash); row 24.2 (`planned`).
8. `docs/envoy-go/SKILL_ROUTING.md` — confirm state 2 (SPEC.md exists, PLAN.md does not → writing-plans).
9. `docs/envoy-go/DECISIONS.md` tail — ADR-0197..0200 (0197[core]+0198+0200 full bodies landed at 24.1; 0199 §Context-only) + ADR-0201 (split ADR full body) + ADR-0125 (the canonical-per-route roster — the 9→10 amendment lands at 24.2 IMPL).
10. `BOOTSTRAP_PROMPT.md` (repo root) — §4.1 STATE-update invariant + §5 state machine (state 2 → writing-plans → output PLAN.md → if >25 tasks OR >~1500 LoC, split per §6).
11. The nearest sibling sub-phase PLAN precedents: `docs/envoy-go/phases/24.1-global-ratelimit-core-and-route-table/PLAN.md` (the immediate sibling — same parent SPEC, same project memory, the exact Task-decomposition + commit-template shape the 24.2 PLAN should mirror); `docs/envoy-go/phases/22.1-http-filter-lua-vm-and-headers-bridge/PLAN.md` (a sibling sub-phase PLAN precedent for shape).
12. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`.
