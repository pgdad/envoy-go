START a fresh cold-start session for envoy-go — **phase 29 BRAINSTORM (`network-filter-mongo-proxy`) is DONE + document-review APPROVED + squash-merged + pushed**. The active phase is **phase 29** (parent pre-split row, `in-progress`; sub-rows 29.1/29.2/29.3 `planned`). This session authors the **phase-29 parent SPEC** — per SKILL_ROUTING state 1→2 (`superpowers:writing-plans` scoped to parent-SPEC authoring per the 22/24/25/26/28 parent-row precedent).

Phase 29 BRAINSTORM landed at squash `TBD-29-BRAINSTORM-SQUASH` (pushed to origin/master): the FOURTH §9 Network-filters-family row, brainstormed via a 4-question dialogue. **Q0** subject = `mongo_proxy` (`envoy.filters.network.mongo_proxy`, type `…filters.network.mongo_proxy.v3.MongoProxy`; bindings verified in go-control-plane v1.32.4: `stat_prefix`/`access_log`/`delay`/`emit_dynamic_metadata`/`commands`) — a passive observability sniffer decoding the MongoDB legacy wire protocol (LITTLE-endian MsgHeader + BSON) in BOTH directions; consumer #2 of the ADR-0221 `network.WriteFilter` seam, exactly as anticipated. **Q1** scope = FULL counter+gauge parity minus histograms — both-direction op-code counters + per-command (`cmd.<cmd>.total`) + per-collection (`collection.<collection>.query.*`) counters via in-house BSON decode + the `op_query_active` GAUGE (the project's first differentially-mirrored gauge) + fault-delay injection + the mongo access log + dynamic metadata; the `reply_num_docs`/`reply_size`/`reply_time_ms` HISTOGRAM families stay DEFERRED per ADR-0060 (coverage-boundary record); 3-way FEATURE-PROGRESSIVE pre-split. **Q2** the upstream-faithful **async halt/resume seam** — an EXTENSION of the as-built machinery, NOT a new API: `ReadFilterCallbacks.ContinueReading()` already exists (26.1) but supports only OnNewConnection-halt resume + re-entrant in-OnData resume; the 29.3 seam adds OnData-halt persistence + cross-goroutine-safe `ContinueReading` + post-handoff read-halt honoring in `readChainConn` (lifting the 28.1b SPEC §3.5 observational boundary for halt purposes only; anticipated zero new exported API; `TerminalFilter.Handle` UNCHANGED). This is the ADR-0221 §Consequences "mongo fault-delay" anticipated-consumer closure pickup, REFINED: the halt is READ-side per upstream `onData` semantics, not write-side. **Q3** hermetic synthesized-byte differential fixtures (NO real mongod) + cross-side `StatsAsserter` counter+gauge parity as the LOAD-BEARING proof + deliberate-break liveness per fixture (`-count=1`) + DETERMINISTIC fault-delay arms (100%-probability; timing never compared). Sub-phases: **29.1** `network-filter-mongo-wire-and-requests` (in-house BSON parser + wire decoder request side + op/cmd/collection counters + active-query list + 8th built-in + `.mongo.` prom arm + fixtures `0049-mongo-requests`/`0050-mongo-boot-reject` + 39th fuzzer [ADR-0224]); **29.2** `network-filter-mongo-responses-and-correlation` (OP_REPLY decoder in `OnWrite` + requestID↔responseTo correlation + `op_query_active` gauge + `cx_destroy_*` + dynamic metadata + fixture `0051-mongo-responses` [ADR-0225]); **29.3** `network-filter-mongo-fault-delay-and-access-log` (the async halt/resume seam + fault-delay injection + mongo access log + `cx_drain_close` + fixture `0052-mongo-fault-delay` + parent ROLLUP [ADR-0226]).

## What this session must do

Author `docs/envoy-go/phases/29-network-filter-mongo-proxy/SPEC.md` (the parent SPEC) via `superpowers:writing-plans` scoped to SPEC authoring. The parent SPEC must:

1. **Execute the BRAINSTORM §10 D29-1..D29-12 empirical pins IN-SESSION** against Envoy v1.37.2 per ADR-0004 (parallel-subagent fan-out per the 25/26/27/28 SPEC precedent — fetch `source/extensions/filters/network/mongo_proxy/*` at tag v1.37.2; run `proto.MessageName` in-session; boot the reference image with a mongo_proxy listener for D29-1). **D29-1 (the standard `envoyproxy/envoy:v1.37.2` image ships `mongo_proxy`) is SPEC-BLOCKING for the Q3 cross-side strategy** — if it fails, the differential strategy needs rework (escalate, do not improvise). **D29-4 (does upstream v1.37.2 decode OP_MSG, or legacy opcodes only?) is the largest decode-envelope unknown** — the mirrored decode envelope is whatever upstream actually decodes, no more (per `reference_wire_format_both_sides_see_same_bytes`: adopt upstream framing verbatim).
2. Pin the exact stat scope/naming (`mongo.<stat_prefix>.*` hypothesis) + the full fixed-counter roster + the dynamic `cmd.*`/`collection.*` naming rules + eager-vs-lazy + the `$comment` callsite family include-or-defer (D29-3) + the wire/BSON framing facts (D29-4) + decoding_error/passthrough semantics (D29-5) + fault-delay evaluation semantics (D29-6) + upstream continueReading re-dispatch semantics (D29-7) + the mongo access-log record format + differential strategy (D29-8) + the dynamic-metadata namespace/keys + observability strategy (D29-9) + the cx_destroy close-direction + drain integration posture (D29-10) + the fuzzer envelope (D29-12).
3. Formalize the 3-way split surface-mapping (which deliverables/tests/fixtures land at 29.1 vs 29.2 vs 29.3) + re-check the ADR-0045 gate per sub-phase (D29-11).
4. Draft the ADR-0224 (mongo_proxy filter + BSON/wire decode + request side) + ADR-0225 (response side + correlation + gauge) + ADR-0226 (async halt/resume seam + fault delay + access log) §Context blocks into DECISIONS.md (tail ADR-0223 → ADR-0226; next-free → ADR-0227; §Decision/§Consequences bodies land at the respective IMPLs per ADR-0044).
5. Run the spec-document-reviewer loop until APPROVED; fold advisories in.
6. ROADMAP: parent row 29 STAYS `in-progress`; sub-rows 29.1/29.2/29.3 STAY `planned` (29.1 flips `planned → in-progress` at the **29.1 SPEC** commit, NOT at the parent SPEC — per the 26.x/28.x precedent).
7. Update STATE.md (active-phase → `phase 29 parent SPEC done`; next-skill → `superpowers:writing-plans` scoped to the 29.1 SPEC) + rewrite this next-prompt.txt + commit + push (work in a worktree per `feedback_git_worktrees`; squash-merge + push per `feedback_push_to_origin`).

## Project state at master tip

- **Master tip:** `TBD-29-BRAINSTORM-SQUASH` (the phase-29 BRAINSTORM squash) + the immediately-following docs-only SHA-fill/next-prompt commit (+1, expected). Confirm the live tip via `git log --oneline -1`. STATE.md `last-commit` points at the squash. origin/master in sync after push.
- **§9 Network-filters family:** rows 26 (parent + sub-rows) + 27 + 28 (parent + 28.1a/28.1b/28.2) DONE; row 29 `in-progress` (parent; sub-rows 29.1/29.2/29.3 `planned`). After phase 29, 3 candidates remain (`redis`/`kafka_broker`/`thrift`).
- **Counts at master tip:** stat surface **337**; differential fixtures **50** (tail `0048-zookeeper-responses`); fuzzers **38**; conformance h2spec 53/53 + proxy-wasm 10/10 (as of the 28.2 six-gate); DECISIONS.md tail **ADR-0223**; next-free **ADR-0224** (anticipated consumed by the phase-29 roster ADR-0224/0225/0226; ADR-0209 escape-valve reserve STANDS-UNCONSUMED).
- **Toolchain:** Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); reference Envoy v1.37.2 (ADR-0008); go-control-plane v1.32.4 (ADR-0008).
- **Known coverage boundaries (carried, non-blocking):** D-26.3-4 (rbac SNI/mTLS L4 arms not differential-proven); the `tcp_proxy` `downstream_cx_*` family unmirrored (phase-27 record); histograms project-wide deferred (ADR-0060 — phase 29 adds the mongo `reply_*` families to this list at 29.2); zookeeper dynamic-metadata deferred (AMEND-A9) + shallow-decode leniency + terminal-originated-writes-only + write-only-filter boot boundary; post-handoff observational boundaries (28.1b SPEC §3.5 — phase 29.3 lifts these for halt purposes only).

## Read first (in order)

1. THIS FILE — the parent-SPEC cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 29 BRAINSTORM done`; next-skill `superpowers:writing-plans` (parent SPEC); next-free `ADR-0224`; last-commit = the BRAINSTORM squash.
3. `docs/envoy-go/phases/29-network-filter-mongo-proxy/BRAINSTORM.md` — the 12-section design this SPEC formalizes (esp. §2 Q-decisions incl. the §2.3 as-built correction, §10 D29-1..D29-12 pins, §6 fixture envelope, §7 ADR roster).
4. `docs/envoy-go/ROADMAP.md` — rows 29/29.1/29.2/29.3 + the §9 family note.
5. `docs/envoy-go/SKILL_ROUTING.md` + `BOOTSTRAP_PROMPT.md` §4/§5 — the per-phase lifecycle + cold-start invariants.
6. `docs/envoy-go/phases/28-network-filter-zookeeper-proxy/SPEC.md` — the most recent parent-SPEC precedent (mirrors its section structure + empirical-pin execution shape) + `docs/envoy-go/phases/28.2-network-filter-zookeeper-responses-and-latency/SPEC.md` §3.6 — the per-connection-mutex concurrency design the 29.2 correlation structures will need.
7. `internal/filter/network/` — the as-built framework the seam extension targets: `chain.go` (connHalted/resumeIdx/ContinueReading), `readconn.go` (the 28.1b replayRead path whose Status-ignore the 29.3 seam lifts for halts), `writeconn.go`, `callbacks.go`, `types.go`, `builtins/`, and `zookeeperproxy/` (the consumer-#1 package shape mongoproxy mirrors).
8. `docs/envoy-go/DECISIONS.md` ADR-0221 (the WriteFilter seam + §Consequences anticipation this phase consumes) + ADR-0222/0223 (the zookeeper precedent incl. the per-connection mutex) + ADR-0213/0214/0215 (§9 framework) + ADR-0217 (dynamic-metadata Bucket) + ADR-0060 (histogram deferral) + ADR-0044/0045/0052/0106 (doc/split/bundle/flip discipline).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — execution-style + worktree + push + subagents-no-push + per-task gofmt/lint + the network-filter constraints (typeurl-extensions, onnewconnection-halts, terminal-handoff-ends-ondata) + the differential dispatch/asserter/break-protocol (`-count=1`) + wire-format-both-sides constraints all carry forward.
