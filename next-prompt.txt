START a fresh session for envoy-go. **The phase-42.1 (`retries` — the retry loop) PLAN is WRITTEN, reviewer-APPROVED, and LANDED on master (squash `9e5c6a7d`) — a 13-task TDD spine with the D-S42-1..9 resolutions settled.** The next step is to EXECUTE the **phase-42.1 IMPL** (`superpowers:subagent-driven-development` — the lifecycle valve is PLAN → IMPL; fresh subagent per task + two-stage review). ROADMAP row 42 (`retries-and-hedging | 41 | in-progress`) carries the SPEC + PLAN notes; STATE active-phase is `phase 42.1 (retries-and-hedging) PLAN done`. Confirm the tip is UNCHANGED at `9e5c6a7d` via `git log --oneline -3`. Counts at tip (the IMPL will MOVE these): stat surface **1163** / fixtures **76** / fuzzers **42** / BackendKind tail **36** (`BlockingHoldResponder`) / DECISIONS tail **ADR-0248**, next-free **ADR-0249**.

## NEXT STEP — execute the phase-42.1 IMPL (the 13-task PLAN)

Read `docs/envoy-go/phases/42-retries-and-hedging/PLAN.md` (the authoritative task-by-task spine) and execute it with `superpowers:subagent-driven-development` — a FRESH task subagent per task, two-stage review (code-reviewer + the controller), per `feedback_execution_style`. Work in a WORKTREE (`feedback_git_worktrees`). Task subagents commit LOCALLY ONLY (`feedback_subagents_no_push`); the controller squashes the 13 task commits at stage-close + pushes to origin/master (`feedback_push_to_origin`). The PLAN bakes in the SPEC's firmed design + the D-S42-1..9 resolutions — do NOT re-derive; follow the PLAN's code blocks + TDD steps verbatim, adapting only to live anchors.

**The 13-task spine (PLAN-detailed):** T1 baselines/PROGRESS · T2 `retry_on` tokenizer + `matches(status, localOrigin)` classifier (router pkg, new `retry.go`) · T3 the `RetryPolicy` ctor + exponential full-jitter backoff + the `max<base` reject · T4 the hcm `retry_policy` parse in `buildRouterAction` + the `VirtualHost` fallback (`buildRouteTable`→`buildAction`→`buildRouterAction`) + the `H{1,2}ClusterAction` signature threading (+BYTE-STABILITY gate) · T5 the +5 retry counters on `Cluster` via the lazy idempotent `EnsureRetryStats()` (scoped — non-retry clusters byte-stable) · T6 the `retry_budget` activation on the phase-41 `circuitBreaker` (`activeRetries` + `tryAcquireRetry`/`releaseRetry`; `rq_retry_open` + `upstream_rq_retry_overflow` flip LIVE) · T7 the `retryExecutorH1` loop + body capture/replay + the increments + `ActionResponse.localOrigin` (set on the dial/connection-failure sites, NOT the CB-overflow 503) · T8 the `retryExecutorH2` + the weighted path (+BYTE-STABILITY gate) · T9 the `0075-retry-loop` cross-side fixture (2 backends {`HTTP503Responder` 35, `HTTPEcho` 1}, 2 clusters {`c_exhaust`=[503], `c_recover`=[503,echo]}, port 19164; EXHAUSTION arm cross-side-EXACT, RECOVER arm offset-invariant `downstream_rq_2xx==K`) · T10 deliberate breaks + 20-run flake · T11 full 77-dir six-gate (ADR-0052) · T12 ADR-0249 body + BEHAVIOR_CONTRACT · T13 completion bundle.

**Exit deltas (verify at the IMPL six-gate):** stat surface 1163 → **1173** (+5 per retry cluster × the `0075` two retry clusters; the 2 phase-41 stats flip LIVE with NO surface delta) · fixtures 76 → **77** (`0075`) · fuzzers **42** (unchanged) · BackendKind tail **36** (REUSE `HTTP503Responder` — NO new kind) · DECISIONS tail ADR-0248 → **ADR-0249** (next-free ADR-0250) · ZERO new packages/go.mod modules. **ROADMAP row 42 STAYS `in-progress`** (the split row flips `done` only when BOTH 42.1 + 42.2 land — ADR-0106 + `reference_roadmap_split_phase_row_done`); the 42.1 IMPL records the leg landed. ADR-0249 lands §Decision/§Consequences IN-PLACE per ADR-0044. After 42.1 lands, the next subject is the **42.2 hedging** BRAINSTORM (`HedgePolicy` + the FIRST `per_try_timeout`, reusing the 42.1 attempt substrate).

## Honor throughout (the IMPL discipline)

- **Lifecycle = one stage per session.** `feedback_git_worktrees` · `feedback_subagents_no_push` (subagents commit locally; controller squashes + pushes at stage-close) · `feedback_push_to_origin` · `feedback_execution_style` (subagent-driven; fresh subagent per task) · `feedback_subagent_worktree_path_targeting` (pin the canonical PROGRESS path + worktree-relative paths; controller verifies clean main repo) · `feedback_subagent_worktree_detach` (deliberate-break checks can detach HEAD — dispatch a GIT HYGIENE block [git restore, NO checkout-sha/amend] + re-verify the branch each task) · `feedback_pertask_gofmt_lint` (each task runs gofmt -l + golangci-lint on touched pkgs, not just go vet).
- **Differential discipline:** `reference_differential_run_selector` (the `0075` subtest is `TestDifferential/0075`; bare `0075` matches ZERO) · `reference_differential_break_protocol_count1` (deliberate breaks use `-count=1` — go-test caching serves a stale PASS) · `reference_differential_asserter_dispatch` (cross-side via `StatsAsserter`, NOT `SubjectAsserter`) · `reference_round_robin_offset_randomized` (the RECOVER arm asserts the offset-invariant `downstream_rq_2xx==K`, NOT an exact per-host/retry count — the reference randomizes the RR initial offset) · `reference_differential_band_sigma_margin` (count-based, never a timing/sleep assertion — backoff is delay-only) · `reference_fixture_workload_constant_desync` (single-source the `0075` N/K/topology constants) · `reference_differential_fullsuite_startup_flake` (the full suite can transiently hit `subject ready: EOF` — isolate-re-run to distinguish from a regression) · `reference_docker_probe_bridge_network` (the `0075` cross-side run needs Docker + `contrib-v1.37.2` over a shared bridge).
- **ADR/process:** ADR-0044 (§Decision/§Consequences IN-PLACE at the IMPL) · ADR-0052 (the six-gate) · ADR-0080 (byte-stable rejects) · ADR-0106 + `reference_roadmap_split_phase_row_done` (row 42 flips `done` only when BOTH legs land) · ADR-0076 (the 1 MiB body cap — the replay source; over-cap is a prior 413) · ADR-0245 (`RecordUpstreamResult`) · ADR-0248 (the phase-41 `circuitBreaker` + the dormant `retry_budget` slot 42.1 activates).

## Read first (in order)

1. THIS FILE — the cold-start router (42.1 PLAN DONE + reviewer-APPROVED + LANDED; the next step is the 42.1 IMPL, NOT re-PLAN).
2. `docs/envoy-go/phases/42-retries-and-hedging/PLAN.md` — the 13-task TDD spine (the authoritative execution doc); the D-question resolutions; the File structure; the per-task code blocks + TDD steps; the Exit deltas.
3. `docs/envoy-go/phases/42-retries-and-hedging/SPEC.md` — the firmed design (the §1.1 AMEND-RT1..7 live pins; the §11 D-RT-* disposition; the §13 ADR-0249 §Context draft to promote at T12).
4. `docs/envoy-go/STATE.md` — active-phase `phase 42.1 (retries-and-hedging) PLAN done`; the count figures (1163 / 76 / 42 / 36 / ADR-0248, next-free ADR-0249).
5. `docs/envoy-go/phases/41-circuit-breakers/{PROGRESS,README}.md` — the precedent shape of an IMPL execution record + completion bundle (the 12-task subagent-driven run; the six-gate evidence; the break/flake record).
6. `docs/envoy-go/DECISIONS.md` (the ADR-0248 entry — the `circuitBreaker` + the emit-0 retry stats 42.1 flips LIVE; the process ADRs; next-free ADR-0249) + `docs/envoy-go/BEHAVIOR_CONTRACT.md`.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `reference_round_robin_offset_randomized`, `reference_concurrency_differential_release_barrier`, the subagent-worktree feedback notes).
