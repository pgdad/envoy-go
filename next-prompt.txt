START a fresh cold-start session for envoy-go — **the phase-34 (`load-balancer-least-request`) SPEC is DONE; the next lifecycle step is the phase-34 PLAN** (`superpowers:writing-plans` — decompose the SPEC §10 ~10-task spine into bite-sized TDD tasks, per the established BRAINSTORM → SPEC → PLAN → IMPL cadence; the kafka-31/thrift-33 single-flat-row precedent). Phase 34 is the Load-balancing family opener: subject `least_request` (`Cluster.LbPolicy LEAST_REQUEST` + `Cluster.LeastRequestLbConfig`, `envoy.config.cluster.v3`).

The literal live master tip is the phase-34 SPEC squash (confirm via `git log --oneline -2`; this file's SHA-fill names it after the push). Below it sit the docs-only routing tips `a463fe8`/`8c0bd36` and the substantive phase-34 BRAINSTORM squash **`87572c0`**; below those the phase-33 IMPL squash `97aefd6` (the §9 family CLOSED).

## What this session does — author the phase-34 PLAN

Decompose `docs/envoy-go/phases/34-load-balancer-least-request/SPEC.md` §10 (the ~10-task spine) into bite-sized TDD tasks (write-failing-test → run-fail → minimal-impl → run-pass → per-task `gofmt -l` + `golangci-lint` per `feedback_pertask_gofmt_lint` → LOCAL-ONLY commit per `feedback_subagents_no_push`), resolving the SPEC §12 D-questions:

- **D-S34-1** — the file split inside `internal/cluster` (anticipated: the reshaped interface + `roundRobin` stay in `loadbalancer.go`; the `leastRequest` type in a NEW sibling `leastrequest.go`; `parseLeastRequestLbConfig` in `manager.go`).
- **D-S34-2** — the production RNG source + seeding (anticipated `math/rand/v2` PCG crypto-seeded once per `leastRequest` construction; injectable `rng func() uint64` for deterministic unit tests — the upstream mock posture, AMEND-L6).
- **D-S34-3** — the FINAL `0059` band constants (anticipated K=4 held / S=60 burst / c1≤12 starvation / c2≥16 concentration / sum==64) + the tuning protocol (≥20 local repeat runs flake-free per side + the three `-count=1` deliberate-break proofs: inverted-comparison / no-op-release / dropped-Inc — `reference_differential_break_protocol_count1` generalized to BAND assertions).
- **D-S34-4** — the held-conn establishment witness (write+read-echo per held conn before holding; `acceptEchoCounting` is a STREAMING echo — verified at the SPEC).
- **D-S34-5** — the `connWithGauge` release-composition shape (anticipated: compose into the existing `dec func()` closure — `dec: func() { gauge.Dec(); release() }`).
- The **FINAL ADR-0045 split-gate re-check** (~155–255 prod LoC / ~10 tasks per SPEC §3.0/AMEND-L7 → NO split anticipated; the pre-authorized 34.1-seam/34.2-policy escape valve stays available, expected unconsumed).

The plan-document-reviewer gate applies. Counts stay UNCHANGED at the PLAN.

## The SPEC's load-bearing pins (AMEND-L1..L7 — SPEC §1.1, AUTHORITATIVE detail in §11)

- **L1**: the v1.32.4 three-knob surface (`choice_count` [PGV `>= 2` ONLY-when-set, string `value must be greater than or equal to 2`] + `active_request_bias` + `slow_start_config`); the manager calls NO PGV today → the choice_count gate is HAND-ROLLED; `least_request_lb_config` is an `lb_config` ONEOF member (field 37). ZERO new go.mod dep (`go mod tidy -diff` empty).
- **L2 (SPEC-BLOCKING)**: cx-as-rq CONFIRMED LIVE — an open TCP-proxied conn IS one active request (per-host `rq_active == cx_active` 1:1); the per-endpoint counter counts CONNECTIONS (acquire at pick, release at final conn close).
- **L3**: FULL_SCAN DEFERRED (the reference's own legacy surface unknown-field-rejects `enable_full_scan`); sampling WITH replacement → `choice_count == N` NOT deterministic; `choice_count > N` LEGAL + quasi-deterministic (live 0/30 at cc=10) → the `0059` fixture uses `choice_count: 10`.
- **L4**: ZERO stat-name delta (surface STAYS 1116); the `0059` StatsAsserter: `upstream_cx_total`/`membership_total`/quiesced-active CROSS-EQUAL; `upstream_rq_total` PER-SIDE (ref=conn-count [rq-per-cx]; subj=0 — tcpproxy never calls `IncUpstreamRqTotal`).
- **L5**: the reject-text blast radius is THREE sites (`manager.go:216` production string + `TestManager_Error_NonRoundRobinLB` [DOUBLY hit — retargets to RANDOM] + `BEHAVIOR_CONTRACT.md:897`); NO fixture pins the text → NO boot-reject dir (fixtures 60 → 61 only at the IMPL); bias/slow_start set under LEAST_REQUEST → DEPARTURE rejects; mismatched lb_config oneof → silent-ignore PARITY.
- **L6**: P2C from v1.37.2 source — k draws `rng() % n` WITH replacement; strict `<` on RAW `rq_active`; FIRST-DRAWN wins ties; no clamp; the weighted bias formula is EDF-path-only (never engages at equal weights — pure P2C for the MVP).
- **L7**: the seam is **OPTION C** — reshape ONLY the unexported `loadBalancer` interface to `Pick() (Endpoint, release func(), error)`; the EXPORTED `Cluster` surface BYTE-STABLE; ZERO consumer churn; ADR-0024 unamended.

## Counts at master tip (phase 34 in-progress, SPEC done — they advance only at the phase-34 IMPL)

- stat surface **1116** (ZERO delta anticipated at the IMPL — confirmed at D-L4; deliberate first).
- differential fixtures **60** (tail `0058-thrift-boot-reject`; `ls -d test/fixtures/[0-9]* | wc -l`). IMPL: 60 → 61 (`0059-lb-least-request`); NO boot-reject dir.
- fuzzers **42** (tail `FuzzThriftDecode`). IMPL adds NONE (deliberate).
- BackendKind tail **33** (`TCPThriftResponder`). IMPL adds NONE (the `TCPEcho` kind-0 backends reused; deliberate).
- DECISIONS tail **ADR-0233** (ADR-0232 [the LB acquire/release seam] + ADR-0233 [the least_request policy], both PROPOSED — §Context anchored at the SPEC; bodies at the IMPL per ADR-0044; next-free **ADR-0234**).
- reference pin: `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227).
- Toolchain: Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); go-control-plane `/envoy` v1.32.4 (ADR-0008) + `/contrib` v1.32.4.
- conformance h2spec 53/53 + proxy-wasm 10/10.

## Read first (in order)

1. THIS FILE — the post-phase-34-SPEC cold-start (next: the phase-34 PLAN).
2. `docs/envoy-go/STATE.md` — active-phase `phase 34 (load-balancer-least-request) SPEC done`; lifecycle-state `phase 34 SPEC done — SKILL_ROUTING → the phase-34 PLAN`; the counts.
3. `docs/envoy-go/phases/34-load-balancer-least-request/SPEC.md` — **AUTHORITATIVE**: §1.1 AMEND-L1..L7; §3 the OPTION-C seam + the `leastRequest` design (+§3.0 the no-split table); §5/§6 the proto + reject matrices; §7 the zero-delta stat surface + the StatsAsserter set; §8 the `0059` band-arm design; §10 the ~10-task spine; §11 the D-L pin record; §12 the D-S34 questions; Appendix A the live probe basis.
4. `docs/envoy-go/phases/34-load-balancer-least-request/BRAINSTORM.md` + README.md — the charter + the phase summary.
5. `docs/envoy-go/ROADMAP.md` — row 34 (SPEC-done note) + the Load-balancing family § (OPEN; 7 candidates remain after 34).
6. `docs/envoy-go/DECISIONS.md` — the tail ADR-0232/0233 (§Context, this SPEC) + ADR-0024 (cross-referenced, unamended) + ADR-0063 (`connWithGauge` — the release attach-point) + ADR-0106/0045/0044/0052/0060.
7. `internal/cluster/{loadbalancer,manager,cluster}.go` (the seam sites: the interface, the `manager.go:215` guard, the `Dial`/`AcquireH1`/`connWithGauge` funnel) + `test/fixtures/0001-tcp-proxy-rr/driver/driver.go` (the harness shape `0059` transposes) + `test/differential/runner_test.go` (`acceptEchoCounting` ~line 1329 + the DistributionAsserter hook ~line 1102).
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_execution_style`, `feedback_git_worktrees`, `feedback_subagents_no_push`, `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_path_targeting`, `reference_differential_break_protocol_count1`, `reference_differential_asserter_dispatch`, `reference_differential_fixture_dispatch_constraint`.

## Honor throughout

- `feedback_execution_style` (subagent-driven) · `feedback_git_worktrees` (work in a worktree) · `feedback_subagents_no_push` (subagents commit LOCAL-ONLY; the controller squash-merges + pushes at stage-close) · `feedback_push_to_origin` (after a clean landing on master with tests green, push without asking) · `feedback_pertask_gofmt_lint` · `feedback_subagent_worktree_path_targeting` (pin canonical paths; verify the main checkout stays clean).
- ADR-0106 (row 34 is a flat family row — NO sub-rows unless the 34.1/34.2 escape valve is consumed) · ADR-0045 (the split-gate, FINAL re-check at this PLAN) · ADR-0044 (ADR bodies at the IMPL) · ADR-0052 (the atomic-landing six-gate) · ADR-0060 (histograms deferred).
- The lifecycle is BRAINSTORM (done) → SPEC (done) → **PLAN (this session)** → IMPL, each its own session per the established cadence. The IMPL's six-gate REAL guard for the seam reshape is the full differential re-verify: all 60 existing dirs must stay byte-exact through the `roundRobin` adoption.
