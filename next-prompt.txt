START a fresh session for envoy-go (router last updated 2026-06-25; the **phase 44.3 (`grpc-access-log` header-capture) SPEC is DONE + pushed to origin/master**). HEAD is the LATEST `next-prompt.txt:` docs-only tip-stamp commit; the **phase-44.3 SPEC commit** (subject `phase 44.3 (grpc-access-log header-capture) SPEC: …`, a docs-only direct-on-master commit — NOT a squash) is the stable anchor sitting directly BENEATH the run of `next-prompt.txt:` commits at the tip; re-read the shas with `git log --oneline -24`, do NOT assume — the master tip is the LATEST `next-prompt.txt:` commit (a roll or a tip-stamp fix), NOT the SPEC commit itself, and any number of docs-only `next-prompt.txt:` commits may stack ATOP the SPEC commit (which stays the stable anchor). BENEATH the 44.3 SPEC commit is the **phase-44.2 IMPL squash `c4055b20`** (`phase 44.2 (grpc-access-log buffering) IMPL: …`), then the 44.2 PLAN squash `b8cc53ca`, the 44.2 SPEC squash `8d5cdb4a`, the 44.1 IMPL squash `ec05a421`, the 44.1 PLAN squash `5c7cf481`, the 44.1 SPEC squash `a7a24d0c`, the phase-44 BRAINSTORM `81afdd59` (at ~depth 22, near the bottom of the -24 window — the tip-stamp/roll commits interleave with each squash, so re-read; the deepest named anchors 44.1 IMPL `ec05a421` / 44.1 PLAN `5c7cf481` / 44.1 SPEC `a7a24d0c` sit at ~depths 15/18/20). **The Observability family is OPEN** (phase 44 is its FIRST row; the family STAYS OPEN — OTLP / tracing / stats sinks / tap remain future rows; 44.3 is the FINAL chartered LEG of row 44, so the 44.3 IMPL six-gate flips ROW 44 → `done` but the FAMILY stays open). **The next action is the 44.3 (header-capture) PLAN** — author `docs/envoy-go/phases/44-grpc-access-log/PLAN-44.3.md` (the PLAN-44.2.md TDD-spine template); resolve the SPEC §12 D-HDR-{RESPONSE-THREADING, RECORD-CAPTURE-SCOPE, SINK-FILTER, FUZZER-CORPUS, DIFFERENTIAL-DRIVE, SPLIT-FINAL} questions; the ~8–10-task TDD spine; CONFIRM against `docs/envoy-go/STATE.md` (the source of truth for "what next") before committing to the stage. **Git hygiene:** the PLAN authoring runs in a FRESH git worktree off master (`feedback_git_worktrees`); the doc commits DIRECT on master; the controller pushes at stage-close (`feedback_push_to_origin`). Then the 44.3 IMPL is subagent-driven (`feedback_execution_style`/`feedback_subagents_no_push`/`feedback_pertask_gofmt_lint`).

First confirm the state via `git log --oneline -24` (expect HEAD to be the LATEST `next-prompt.txt:` docs-only commit, with the phase-44.3 SPEC commit sitting directly BENEATH the run of `next-prompt.txt:` commits, then the phase-44.2 IMPL squash `c4055b20`; re-read the shas, do NOT assume). Counts at the 44.3 SPEC-done (docs-only — UNCHANGED): stat surface **1189** (H2 cluster; non-H2 **1185**) / fixtures **84** (tail `0082-grpc-access-log-buffering`; NOTE: count with `ls -d test/fixtures/[0-9][0-9][0-9][0-9]* | wc -l` — `grep -cE '^[0-9]{4}-'` UNDERCOUNTS by 2, skipping `0007a`/`0007b`) / fuzzers **44** (`^func Fuzz` == 44) / BackendKind tail **38** (`H2GoawayResponder`; the ALS receiver is the driver-owned test-only `test/helpers/accessloggrpc`, NOT a BackendKind) / DECISIONS tail **ADR-0256**, next-free **ADR-0257** (the anticipated 44.3 header-capture ADR — §Context already drafted in SPEC-44.3 §13; §Decision/§Consequences land at the 44.3 IMPL per ADR-0044).

## NEXT STEP — the 44.3 (header-capture) PLAN (`superpowers:writing-plans`)

Author `docs/envoy-go/phases/44-grpc-access-log/PLAN-44.3.md` in the house TDD-spine shape (the PLAN-44.2.md template). The 44.3 leg makes `additional_request_headers_to_log` / `additional_response_headers_to_log` (PARSE-ACCEPT-but-INERT through 44.1/44.2) LIVE: capture the named request/response header VALUES at the HCM emit hooks into two new `Record` maps (`RequestHeaders`/`ResponseHeaders`) and map each sink's configured subset into the entry's `request.request_headers` / `response.response_headers` `map<string,string>`. ANCHORS **ADR-0257**. The SPEC (`SPEC-44.3.md`) already EXECUTED the §11 D-ALS-HEADERS live probe — the PLAN does NOT re-probe; it decomposes the SPEC §3 design into the TDD spine + resolves the §12 D-HDR-* PLAN questions.

The §12 D-HDR-* to RESOLVE at PLAN: **D-HDR-RESPONSE-THREADING** (the exact emit-hook response-header parameter type per codec — the H1 `resp.Headers` `OrderedHeaders` collection vs the H2 representation — + the per-call-site source/`nil`-disposition across the ~12 `connection.go`/`h2dispatch.go` emit sites; SPEC default: upstream/action-response sites pass the in-scope `resp.Headers`, error/local-reply sites pass `nil`); **D-HDR-RECORD-CAPTURE-SCOPE** (the union-on-the-Filter capture [SPEC default — byte-stable nil-maps when the union is empty] vs per-sink capture); **D-HDR-SINK-FILTER** (`buildHTTPAccessLogEntry` signature extension vs a post-mapping step; the per-sink filter form); **D-HDR-FUZZER-CORPUS** (header-bearing seed corpus for the EXISTING `FuzzParseHttpGrpcAccessLogConfig` — mixed-case/duplicate/empty/large name lists; NO new `^func Fuzz` — fuzzers STAY 44; whether a `captureHeaders` mapping fuzzer is warranted [likely NOT — the helper is total + exhaustively table-tested]); **D-HDR-DIFFERENTIAL-DRIVE** (the exact `0083` capture set — the request-header names incl. the absent-name OMIT proof + whether the multi-value comma-join stays cross-side-robust in the H1 differential or is unit-only; the response side `content-type` only [backend-origin, AMEND-HDR-4]; N + the poll-to-converge deadline + the cross-side EXACT map-equality assertion form); **D-HDR-SPLIT-FINAL** (the final ADR-0045 re-check with real LoC — 44.3 anticipated ≈120–170 prod LoC, one leg).

The ~8–10-task TDD spine (SPEC §10): T1 PROGRESS scaffold + baselines + the final ADR-0045 re-check (D-HDR-SPLIT-FINAL) · T2 the `ALSConfig` header-name parse arm (lowercased; `cfg.GetAdditional{Request,Response}HeadersToLog()`) [TDD] + header-bearing fuzzer seed corpus (D-HDR-FUZZER-CORPUS) · T3 the `Record` two-map extension + the `captureHeaders` helper (lowercase-key / comma-join / omit-absent / empty-for-present-empty — exhaustive table tests) [TDD] · T4 the emit-hook capture (H1 + H2) + the response-header threading through the ~12 emit call sites (D-HDR-RESPONSE-THREADING) [TDD; the no-capture path BYTE-STABLE] · T5 the `buildHTTPAccessLogEntry` mapping extension + the `NewGrpcAccessLogSink` signature extension + the per-sink filter (D-HDR-SINK-FILTER) [TDD, full-package `-race`] · T6 the `main.go` wiring pass-through (`cfg.AdditionalRequestHeaders`/`cfg.AdditionalResponseHeaders` + the HCM-filter union threading) · T7 the `0083-grpc-access-log-headers` cross-side fixture (captured request maps + the `content-type` response map + the absent-name OMIT proof, poll-to-converge, query-less, REUSES `HTTPFixedBody` — D-HDR-DIFFERENTIAL-DRIVE) · T8 `0083` deliberate breaks (`-count=1`, incl. the disable-capture break that empties the maps) + 20/20 flake + the FULL-package `-race` · T9 full 85-dir differential + six-gate + ADR-0257 body + BEHAVIOR_CONTRACT + STATE/ROADMAP + the fuzzer-count re-verify (ROADMAP row 44 → `done` [FINAL leg]; the Observability family STAYS OPEN).

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** This session is the 44.3 PLAN (lifecycle-state 1 → 2 for the 44.3 leg; **row 44 STAYS `in-progress`** — flips `done` at the 44.3 IMPL). `feedback_git_worktrees` (a fresh worktree off master; the PLAN doc commits DIRECT on master) · `feedback_push_to_origin`.
- **Process anchors:** ADR-0106 (row 44 flips `done` PER-LEG; NO parent rollup — 44.3 is the FINAL chartered leg, so its IMPL six-gate flips row 44 → `done` ⇒ but the Observability FAMILY STAYS OPEN) · ADR-0044 (the ADR-0257 §Decision/§Consequences land at the 44.3 IMPL; §Context already drafted in SPEC-44.3 §13) · ADR-0045 (the split gate — re-check at PLAN, D-HDR-SPLIT-FINAL) · ADR-0080 (strict-reject — UNCHANGED; header capture adds no reject) · ADR-0255 (the 44.1 as-built sink) · ADR-0256 (the 44.2 as-built buffering) · ADR-0257 (the anticipated 44.3 header-capture ADR).
- **Streaming-sink + header-capture differential discipline:** `reference_streaming_sink_differential_framing` (assert the PAYLOAD aggregated across messages, NOT framing) · `reference_differential_grpc_receiver_driver_owned` (the ALS receiver is the driver-owned `test/helpers/accessloggrpc`, NOT a BackendKind — NO helper change at 44.3; the driver reads `GetRequestHeaders()`/`GetResponseHeaders()` off `Entries()`) · `reference_docker_probe_bridge_network` (shared-bridge + container-hostname; the host-gateway gotcha) · `reference_concurrency_differential_release_barrier` (poll-to-converge, NEVER `time.Sleep`) · `reference_differential_run_selector` (`-run 'TestDifferential/0083'`, NOT bare) · `reference_differential_break_protocol_count1` (`-count=1` on every deliberate break + `-race` run) · `reference_full_suite_race_after_background_mutator` (the sink writer goroutine is a background mutator — run the FULL package `-race`) · `reference_fuzzer_count_docs_drift` (re-verify `^func Fuzz` == 44 at completion). The AMEND-HDR-4 backend-origin-response-headers-only constraint carries into the `0083` drive (capture `content-type`, AVOID `server`/`date`/`x-envoy-*`).

## Read first (in order)
1. THIS FILE — the cold-start router (phase 44.3 SPEC is DONE + pushed; the next action is the 44.3 header-capture PLAN).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 44.3 (grpc-access-log) SPEC done`) + the recorded NEXT action (the 44.3 header-capture PLAN) + the count figures (source of truth for "what next").
3. `docs/envoy-go/phases/44-grpc-access-log/SPEC-44.3.md` — the 44.3 SPEC the PLAN decomposes (esp. §3 the design, §8 the `0083` differential, §10 the per-task structure, §11 the D-ALS-HEADERS pins [AMEND-HDR-1..5], §12 the D-HDR-* PLAN questions, §13 the ADR-0257 §Context draft).
4. `docs/envoy-go/phases/44-grpc-access-log/PLAN-44.2.md` — the TDD-spine template to mirror for the 44.3 PLAN shape.
5. `docs/envoy-go/DECISIONS.md` — ADR-0256 (the 44.2 as-built) + next-free **ADR-0257** (§Context drafted in SPEC-44.3 §13; body lands at the 44.3 IMPL).
6. `docs/envoy-go/ROADMAP.md` — row 44 (`grpc-access-log`, in-progress; legs 44.1 + 44.2 done, 44.3 SPEC done, 44.3 PLAN/IMPL remain) + the `### Observability family` heading.
7. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the `### Access log — gRPC Access Log Service (ALS) streaming sink` block (the §9 SPEC-44.3 contract-delta target — the header-capture addition) + the stat-surface 1189 entry.
8. The 44.1/44.2 as-built seams the 44.3 PLAN decomposes: `internal/bootstrap/bootstrap.go` (`ALSConfig` [`:190`] + `parseGrpcAccessLog` [`:361`] — the header-name parse-accept-inert arm to make LIVE), `internal/accesslog/accesslog.go` (`Record` [`:29`] — the two-map extension target), `internal/accesslog/mapping.go` (`buildHTTPAccessLogEntry` [`:47`] — the mapping extension), `internal/accesslog/grpcsink.go` (`NewGrpcAccessLogSink` [`:71`] — the signature extension), `internal/filter/hcm/accesslog_emit.go` (`emitAccessLog` [`:18`]/`emitAccessLogH2` [`:43`]/`h2UserAgent` [`:67`] — the capture + the response-header threading) + the ~12 emit call sites (`connection.go:322/442/536/637/715` + `h2dispatch.go:304/386/470/516/522/550`; `resp.Headers` `OrderedHeaders` in scope at `connection.go:704`/`h2dispatch.go:304`), `internal/filter/hcm/config.go` (`accessLog []accesslog.Sink` [`:110`]/`parseFilterWithCtx` [`:191`]/`:285` — the union capture-set threading), `test/helpers/accessloggrpc/accessloggrpc.go` (`Entries()` [`:160`] — the driver reads the captured maps; NO helper change), `test/fixtures/0006-access-log/backends/main.go` (`Content-Type: text/plain` [`:47`] — the backend-origin response header `0083` captures via `HTTPFixedBody=4`), `test/fixtures/0081-grpc-access-log/` + `0082-grpc-access-log-buffering/` (the differential precedents the `0083` copies).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `reference_streaming_sink_differential_framing`, `reference_differential_grpc_receiver_driver_owned`, `reference_docker_probe_bridge_network`, `reference_fuzzer_count_docs_drift`, `feedback_execution_style`, `feedback_git_worktrees`, `feedback_pertask_gofmt_lint`).
