START a fresh cold-start session for envoy-go — **the phase-40 (`outlier-detection`, PASSIVE upstream health) BRAINSTORM is DONE, LANDED on master (commit `332bdce`), and PUSHED to `origin/master`; the next lifecycle step is the phase-40.1 SPEC** (the `consecutive_5xx` keystone leg of the pre-authorized 40.1/40.2/40.3 by-detector-class split). Author the SPEC, executing the §10 D-OD-PROTO/LIFECYCLE/STATS/REJECT/BACKEND/SEAM empirical pins IN-SESSION live against `envoyproxy/envoy:contrib-v1.37.2` (per `reference_docker_probe_bridge_network`), anchoring the ADR-0245 §Context draft; run the spec-document-reviewer loop; land + push. OR a new subject. Confirm the tip via `git log --oneline -5` (the substantive master tip is the phase-40 BRAINSTORM commit **`332bdce`**; the prior substantive tip is the phase-39.2 IMPL squash `5ddc588`). Counts UNCHANGED at the 40 BRAINSTORM: stat surface **1132** / fixtures **70** / fuzzers **42** / BackendKind tail **34** / DECISIONS tail **ADR-0244**, next-free **ADR-0245**.

## What just landed (phase 40 BRAINSTORM — the durable record is `docs/envoy-go/phases/40-outlier-detection/{BRAINSTORM,README}.md`; read those, not this summary)

Phase 40 charters `Cluster.outlier_detection` (`cluster.v3.OutlierDetection`, `Cluster` field 19 — in the EXISTING go-control-plane dep, ZERO new go.mod module) — **passive** upstream health: eject hosts that fail on REAL traffic, the counterpart to phase-39 active health checks. The SECOND Upstream-robustness-family row. REUSES the ADR-0242 `clusterHealth`/`hostHealth` registry + the ADR-0243 build-time-injected health-aware LB pick; ADDS one genuinely-new seam — a per-request `cluster.RecordUpstreamResult(ep, UpstreamResult{StatusCode, LocalOriginErr})` callback from the H1 (`router.go`) + H2 (`router_h2.go`) router paths back to the cluster (the response status is observed in the router today but never threaded back).

**Q-decisions:** (Q-subject) outlier detection; (Q-scope) **full** (all detector variants); (Q-split) **3 legs by detector class** — **40.1** = the passive seam + the ejection lifecycle + the single `consecutive_5xx` detector + the LB-filter extension (`isHealthy → available`) + `0069` [the minimal keystone]; **40.2** = the other consecutive detectors (`consecutive_gateway_failure` + `consecutive_local_origin_failure` + `split_external_local_origin_errors`); **40.3** = the statistical detectors (`success_rate` + `failure_percentage`) + a NEW per-interval cross-host mean/stddev aggregation runtime.

**Architecture (Approach A → ADR-0245 anticipated):** ejection is a SEPARATE per-host sub-state (new fields `ejected`/`unejectAtNanos`/`ejectCount`/`consec5xx` on the existing `hostHealth`), DISTINCT from the active-HC `healthy` bit — outlier recovery is TIME-based (`base_ejection_time × num_ejections` linear backoff), NOT consecutive-success. `isHealthy`/`membership_healthy` KEEP their 39.1 active-HC-only meaning; the LB pick filters on `available = isHealthy && !isEjected` (the six constructs move their predicate `isHealthy → available`; **byte-identical when no `outlier_detection`**). The `UpstreamResult` seam struct is FORWARD-COMPATIBLE + fires on EVERY request so 40.2/40.3 do NOT re-touch the router (the 39.2 re-signature-churn lesson). Rejected: folding outlier outcomes into the same `healthy` bit (wrong recovery semantics); a stats-poll observer (no per-host outcome stat to poll). spec/brainstorm-document-reviewer APPROVED (3 advisory Minors, all deferred to the SPEC's D-OD* pins).

## The 40.1 SPEC obligations (the §10 D-OD* pins — EXECUTE live, do not assume)

- **D-OD-PROTO** — the exact `cluster.v3.OutlierDetection` field roster + types + PGV + defaults (`consecutive_5xx` ~5, `interval` ~10s, `base_ejection_time` ~30s, `max_ejection_percent` ~10%, `enforcing_consecutive_5xx` ~100); `go mod tidy -diff` EMPTY.
- **D-OD-LIFECYCLE** — eject/un-eject semantics: when `consecutive_5xx` ejects; the `base_ejection_time × num_ejections` backoff; the `max_ejection_percent` cap arithmetic; the `interval`'s role (does 40.1's lazy un-eject make `interval` a subject-side no-op — a cross-side stat-parity wrinkle to pin).
- **D-OD-STATS** — the exact `outlier_detection.*` names + types from a live `/stats` scrape (whether `ejections_enforced_total` double-counts with the per-variant `enforced` counter — changes the ~+4–6).
- **D-OD-REJECT** — the envoy-go-strict `outlier_detection` reject arms (byte-stable wording, ADR-0080).
- **D-OD-BACKEND** — whether an existing BackendKind can return 5xx, or a +1 always-5xx responder is needed for `0069` (BackendKind 34 → 35 conditional).
- **D-OD-SEAM** — the exact `RecordUpstreamResult` call-site placement in `router.go` (H1) + `router_h2.go` (H2): the point where `(picked endpoint, resp.StatusCode, connect-error)` is jointly available on EVERY completion path (success, upstream-5xx, write-error-502, dial-error-503), and that the picked `Endpoint` is in scope there.

Anticipated at the 40.1 IMPL: fixtures 70 → 71 (`0069`), BackendKind tail 34 → 35 (TBD), DECISIONS tail ADR-0244 → ADR-0245 (next-free ADR-0246), stat surface 1132 → ~1137, fuzzers 42 (UNCHANGED), ZERO new packages + ZERO new go.mod modules.

## Honor throughout (the lifecycle discipline)

- **Lifecycle = one stage per session:** BRAINSTORM→SPEC→PLAN→IMPL, each landed+pushed at stage-close. The SPEC is authored in-session (execute the §10 pins, anchor the ADR-0245 §Context per ADR-0044, spec-document-reviewer loop). `feedback_git_worktrees` (work in a worktree) · `feedback_push_to_origin` (push ready work without asking) · `feedback_subagents_no_push`.
- **Empirical pins:** `reference_docker_probe_bridge_network` (the reference is a container; backends via `host.docker.internal`; verify `upstream_rq_total>0`) · `reference_health_check_propagation_warmup` (the `0069` traffic-driven ejection gates on `ejections_active` convergence + warmup + delta counters — NOW also applies to outlier detection) · `reference_differential_run_selector` / `_break_protocol_count1` / `_fixture_workload_constant_desync` (at the IMPL).
- **ADR/process:** ADR-0044 (§Context at SPEC, body at IMPL) · ADR-0045 (the split-gate re-check at each leg's SPEC/PLAN) · ADR-0052 (the six-gate) · ADR-0080 (byte-stable rejects) · ADR-0106 + `reference_roadmap_split_phase_row_done` (flat family row; row 40 flips `done` only when all THREE legs land — NO parent rollup) · ADR-0227 (`contrib-v1.37.2`).

## Read first (in order)

1. THIS FILE — the post-phase-40-BRAINSTORM cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 40 (outlier-detection) BRAINSTORM done`; the count footer (1132 / 70 / 42 / 34 / ADR-0244, next-free ADR-0245); the lifecycle routing (next-skill = the phase-40.1 SPEC).
3. `docs/envoy-go/phases/40-outlier-detection/BRAINSTORM.md` (+ README.md) — the phase-40 charter (the Q-decisions; the Approach-A architecture §1.7/§2.2; the 3-leg split §1.4; the `0069` differential §6; the §10 D-OD* pins the SPEC executes; the §11 lessons).
4. `docs/envoy-go/phases/39-upstream-health-check/` + `39.2-health-check-tcp-grpc/` — the phase-39 active-HC substrate 40 REUSES (ADR-0242/0243/0244); the prior SPEC house format to mirror.
5. `internal/cluster/{health.go,cluster.go,manager.go}` (the `hostHealth`/`clusterHealth` registry the ejection dimension extends; the LB build; `IncStatusClass`) + `internal/cluster/{loadbalancer,ringhash,maglev,leastrequest,random,subset}.go` (the six constructs whose pick predicate moves `isHealthy → available`) + `internal/filter/http/router/{router.go,router_h2.go}` (the `RecordUpstreamResult` call sites) + `docs/envoy-go/DECISIONS.md` (ADR-0242/0243/0244 + the process ADRs) + `docs/envoy-go/BEHAVIOR_CONTRACT.md`.
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the lifecycle-discipline set above.
