=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

✅ **PHASE 100 (`listener-filters-timeout-enforce`) IS CLOSED. ROW 100 READS `done` AT `ROADMAP.md:162`. LIFECYCLE-STATE IS `DONE`, AND EVERY CHARTERED ROADMAP ROW IS NOW `done`.** ⚠️⚠️ **THAT IS NOT THE SENTINEL: check (2) still prints SIX deferred-candidate windows (`:210 :216 :222 :232 :238 :246`, bytes unchanged) — `stop` must NOT be created.** ⇒ **THIS SESSION OPENS PHASE 101 BY SELF-PICK AND WRITES ITS `BRAINSTORM.md`** (BOOTSTRAP §5 state 0 -> 1, `superpowers:brainstorming`), under the 2026-07-12 standing directive: **no human is consulted**, the smallest defensible candidate goes first, and **the pick AND the rejected alternatives are recorded in the BRAINSTORM.** ✅ **THE `listener_filters_timeout` `[1s, 60s]` ENVELOPE LIFT IS NOW UNBLOCKED** (row 100's enforcement landed) **and was ADJUDICATED NEXT IN LINE** (`100/BRAINSTORM.md` §4.1, `100/SPEC.md` §14.3, ADR-0322 §Consequences (g)) — ⚠️ **an adjudication by closed stages, not a charter: MEASURE BOTH SIDES AT YOUR TIP BEFORE ADOPTING IT (method note 69); you may overturn it IN WRITING.** ⚠️ **EVERY CLOSED PHASE'S GOVERNING DOCUMENT IS EVIDENCE, NOT INSTRUCTIONS — YOUR JOB IS TO REFUTE YOUR PREDECESSOR BY EXECUTION (method note 2).**

🔴 **YOUR STAGE IS THE BRAINSTORM. IT PICKS THE SUBJECT, ADJUDICATES THE REJECTED ROSTER IN WRITING, *ADDS* ROW 101 TO `ROADMAP.md` AS `in-progress`, AND WRITES `SPEC.md`'s PREDECESSOR — IT WRITES NO PRODUCTION CODE, NO ADR, NO LEDGER ENTRY, AND RUNS NONE OF THE SIX GATES.** ⚠️ **AN ADD MOVES THE DENOMINATOR AND THE NC SHAPES:** `want` is EXPECTED to move **132 -> 133** and `ROADMAP.md` **250 -> 251**, check (1) SILENT -> **ONE** (`NOT DONE: row 101`), NC-A / NC-B ONE -> **TWO**, and — if row 101 lands directly below row 100 at `:163`, ABOVE the six windows — the windows shift down by ONE line to `:211 :217 :223 :233 :239 :247` with the SAME six md5s. ⚠️ **Those are expectations, not measurements: MEASURE them on BOTH sides of the ADD.** ⚠️ **COUNT ROW 101's FIELDS (want 8) UNDER BOTH FORMS BEFORE INSTALLING IT, and assert its cell spells NEITHER sentinel match phrase (nor the bare word `deferred`, case-insensitive).** ⚠️ **AND CHECK THIS FILE FOR A SPENT STAGE'S IMPERATIVE BEFORE OBEYING ANY LINE IN IT (method note 68).**

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. Parses FIELDS instead of
#     matching a pattern, and asserts the row DENOMINATOR so a deleted row is
#     itself an error.
#     ⚠️ want IS 132 and ROADMAP.md IS 250 lines at the phase-100 IMPL close
#     (its FLIP of row 100 moved neither; the phase-100 BRAINSTORM's ADD moved both).
#     ⚠️ CHECK (1) IS SILENT at this tip — row 100 was FLIPPED `done` at the
#     phase-100 IMPL. A SILENT check (1) is correct NOW, and it is exactly why
#     the NCs below stay MANDATORY. A BRAINSTORM that ADDS row 101 must move
#     want 132 -> 133 in the SAME edit; check (1) is then EXPECTED to read ONE
#     (`NOT DONE: row 101`). Measure it on BOTH sides of the ADD.
awk -F'|' -v want=132 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️⚠️ **CHECK (2)'s SIX IS THE ONLY STRUCTURAL BARRIER.** At this tip check (1) is SILENT (row 100 was flipped `done` at the phase-100 IMPL; a BRAINSTORM's ADD makes it ONE again) and check (3) is SILENT — **a silent check is indistinguishable from a broken one, and a one-line check is indistinguishable from one that has stopped seeing every OTHER row.** That is not a reason to relax: it is precisely why the NCs are mandatory. **RUN ALL FOUR NCs EVERY SESSION:**
```sh
# NC-A — doctor a row to `in-progress`. ⚠️ READS *ONE* LINE at this tip
#        (`NOT DONE: row 62`) — MEASURED on BOTH sides of the phase-100 IMPL's
#        FLIP: TWO before, ONE after (and again after its final-review row edit).
#        A BRAINSTORM's ADD of row 101 is EXPECTED to take it back to TWO
#        (`NOT DONE: row 62`, `NOT DONE: row 101`). MEASURE IT on BOTH sides.
#        NEVER INHERIT AN NC SHAPE ACROSS A ROW CHANGE.
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md with want=132 (want=133 after an ADD).

# NC-B — the DENOMINATOR, want=131 on the real file (ONE LESS than the live
#        want). ⚠️ READS *ONE* LINE at this tip — `GATE FAIL: examined 132 data
#        rows, expected 131` — MEASURED after the phase-100 IMPL's FLIP (it read
#        TWO before it). ⚠️ Re-derive `want` from YOUR tip: after an ADD run it at
#        want=132 and EXPECT TWO (`NOT DONE: row 101`, `GATE FAIL: examined 133
#        data rows, expected 132`).

# NC-C — the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```
⚠️ **AND RUN THE CHECK-(2) POSITIVE CONTROL — AND READ THE COMMAND BEFORE TRUSTING ITS RESULT.** The longer phrase does not CONTAIN the shorter one as a substring, so a control that substitutes only one phrase reports a residual of 5 and reads like a finding. Substitute BOTH and assert the substitution count:
```sh
sed -e 's/deferred candidates:/deferred candidatesXX/g' \
    -e 's/remaining deferred (not-yet-chartered) candidates:/remaining deferred (not-yet-chartered) candidatesXX/g' \
    docs/envoy-go/ROADMAP.md > "$SCRATCH/c2pc.md"
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' "$SCRATCH/c2pc.md" | wc -l  # want 0
grep -o 'candidatesXX' "$SCRATCH/c2pc.md" | wc -l                                                                # want 6 — PROVE it landed
```

⚠️ **WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -c '-family row'` fails (GNU: `grep: amily row: No such file or directory`; ugrep: `option -f: cannot read amily row` — **both rc=2**) and the surrounding arithmetic prints `0`, **which reads exactly like "no change."** **Always pass `--` before the pattern**; with `--` occurrences read **96** and LINES read **68** — ⚠️ **UNMOVED by the phase-98, phase-99 AND phase-100 row ADDs, measured on both sides of each, and by the phase-99 AND phase-100 IMPL FLIPs.**

⚠️ **THE MALFORMED-ROW FIGURE RECONCILES ONLY UNDER AN ESCAPE-AWARE FIELD COUNT — AND THE ESCAPE-AWARE COMMAND ITSELF CAN BE BROKEN.** Malformed rows are **ROW IDs 57** and **69**, at file lines **119** and **131** (**UNMOVED by the row-98, row-99 and row-100 ADDs, which land BELOW them**). **Strip `\|` BEFORE splitting.** Row 57 reads escape-aware NF=9; row 69 reads 10; rows 74, 94, 95, 96, 97, 98, 99 and **100** read **8** (row 100 measured **8** under BOTH forms in a scratch file BEFORE installing, again after, again at its IMPL flip, and again at that IMPL's final-review numstat fix — each time in a scratch copy first). ⚠️ **The correct form is `sed 's/\\|//g' F | awk -F'|' '/^\| *[0-9]/ && NF!=8'` with NO file argument to awk** — passing the file makes awk IGNORE STDIN and print the NAIVE count under the escape-aware label.

⚠️ **AND DO NOT SUBSTITUTE A "REASONABLE EQUIVALENT" FOR CHECK (1)'s AWK.** `NF>=8 && $2 ~ /^ *[0-9]+ *$/` once printed **92**, not the true denominator. **Use the form above verbatim.**

**SENTINEL RUN MECHANICALLY AT THE PHASE-100 IMPL CLOSE, IN THE PUBLISHING TREE, WITH `/usr/bin/grep`.** ACTUAL output, recorded not predicted — **AFTER the IMPL's flip of row 100 (Task 16) and after its final-review edit of the row-100 cell, identical at both:**
(1) **SILENT** · (2) **SIX** at `:210 :216 :222 :232 :238 :246` · (3) **SILENT** · NC-A **ONE** (`NOT DONE: row 62`; substitution inspected first: `NC LANDED? [ in-progress ]`) · NC-B **ONE** at `want=131` (`GATE FAIL: examined 132 data rows, expected 131`) · NC-C **FIRED** (residual 0) · NC-D **96 / 68** under `--` · check-(2) positive control **6 substitutions ASSERTED, residual 0** · escape-aware malformed set **exactly {57, 69}** at file lines **119** (NF 9) and **131** (NF 10) · row 100 NF **8** under BOTH forms · fixture set **127 = 127** (**103 `driver/` + 24 `inputs/`**; both `comm` directions EMPTY under the `(imports, dirs)` argument order). ⚠️ **AT THE FLIP check (1) went ONE -> SILENT and NC-A / NC-B each went TWO -> ONE — MEASURED ON BOTH SIDES, NOT INHERITED; `want` 132 and `ROADMAP.md` 250 on both sides.**
Per-line md5 of the six windows, **trailing newline INCLUDED** (`sed -n 'Np' f | md5sum`, first 12 hex) — **BYTE-IDENTICAL ACROSS TWELVE CONSECUTIVE STAGES (phase-98 BRAINSTORM through phase-100 IMPL), INCLUDING ACROSS THE phase-99 AND phase-100 ADDs, EACH OF WHICH MOVED THE LINES BUT NOT THE BYTES:**
`210 10d7807bf02d` · `216 4a92f7e62fc6` · `222 2a7eb298b9fd` · `232 242e53c6f7a3` · `238 b2680e6f4fbf` · `246 6caa1c3ce0e7`
⇒ **THE SENTINEL DOES NOT FIRE. `stop` WAS EVALUATED AND DELIBERATELY NOT CREATED** (verified absent at the git root and in the stage worktree).
⇒ ⚠️⚠️ **THIS RUN DOES NOT CARRY FORWARD. RUN ALL THREE CHECKS, ALL FOUR NCs AND THE CHECK-(2) POSITIVE CONTROL YOURSELF, AT YOUR OWN TIP.** ⚠️ **THE MARGIN REMAINS ONE.**
⚠️⚠️ **YOUR STAGE IS A BRAINSTORM THAT ADDS ROW 101, so the shapes CHANGE at the ADD — measure BEFORE it (check (1) SILENT, NC-A ONE, NC-B ONE at want=131, `want` 132, `ROADMAP.md` 250) and AFTER it (EXPECTED: check (1) ONE `NOT DONE: row 101`, NC-A TWO, NC-B TWO at want=132, `want` 133, `ROADMAP.md` 251, the windows at `:211 … :247` with the same six md5s IF row 101 lands above them). An expectation is not a measurement. NEVER INHERIT AN NC SHAPE ACROSS A ROW CHANGE.**
⚠️ **IF YOU EVER ADD A ROW, COUNT ITS FIELDS UNDER BOTH FORMS BEFORE INSTALLING IT** — an unescaped `|` PASSES check (1) and silently breaks the field count.
⚠️ **THE MARGIN IS STILL ONE.** ⚠️⚠️ **DELETING THE LAST DEFERRED-CANDIDATE LINE ENDS THE PROJECT. DO NOT "TIDY" ONE, AND DO NOT "FIX" THE SIX.** History `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases; **no gate may rest on a ` + ` split** (wrong on 3 of the 6 windows).
⚠️⚠️ **CHECK (2) MATCHES THE WHOLE FILE, NOT ONLY THOSE SIX LINES.** A new or edited row whose summary cell spelled either match phrase would mint a SEVENTH hit and read as a finding. **The phase-98, phase-99 and phase-100 row cells were written with this in mind and spell NEITHER phrase — each gated before installing (row 100's also for the bare word `deferred`, case-insensitive), and rows 99's and 100's again at their flips (row 100's a third time at its IMPL's final-review numstat fix).** ⚠️ **NEVER RE-SPELL A SENTINEL MATCH PHRASE INSIDE A SENTINEL WINDOW, AND NEVER SPELL ONE IN A NEW ROW CELL EITHER.**
⚠️ **THE md5 DIGEST IS METHOD-SENSITIVE:** the six match ONLY with the **trailing newline included**. **State the method whenever you quote the digest.**
⚠️ **AUDIT THIS FILE'S OWN MEMORY CITATIONS BEFORE TRUSTING THEM.** Extract every `` `reference_*` ``/`` `feedback_*` ``/`` `topic_*` `` slug and test each against `~/.claude/projects/-home-esa-git-envoy-go/memory/<slug>.md`. A router that names a file which does not exist is the same defect class as a stale line anchor. **All cited slugs resolved at the phase-100 IMPL close (every distinct slug across this file and `STATE.md`, each tested with `[ -f … ]`; backticked and `[[…]]` forms both extracted by one `(reference|feedback|topic)_[a-z0-9_]+` pattern).** ⚠️ **A BRAINSTORM that cites a new slug must test it the same way.**

⚠️⚠️ **A STALE PRESENT-TENSE CLAIM SITS INSIDE SENTINEL WINDOW `:232` (was `:231` before the row-100 ADD, `:230` and `:229` before that — every row ADD shifts it, while a SPEC, PLAN or IMPL adds none; expect `:233` after a row-101 ADD) AND IS DELIBERATELY UNFIXED.** It says the duplicate-`stat_prefix` panic is a *"swallowed-panic BOOT HANG"* that *"neither crashes nor boots"*. **Row 78 fixed that on 2026-07-27, and the phase-97 BRAINSTORM re-measured it as rc=2 in 0 seconds on BOTH the boot and validate paths.** ⚠️ **A MEASUREMENT AGENT REPORTED THAT CLAIM ABSENT FROM THAT LINE AND WAS WRONG — it searched for the panic MESSAGE while the line spells the CONFIG FIELD.** **When an agent reports an absence, ask what it SPELLED.** Repairing it means editing a line the sentinel matches on a margin of one: **record it, do not tidy it.**

⚠️ **NEVER RE-SPELL A SENTINEL MATCH PHRASE INSIDE A SENTINEL WINDOW** (the phase-94 rule: the sentence asserting the sentinel could not move MOVED IT, six -> seven).

- ⚠️ **LIVE FIGURES RE-DERIVED AT THIS TIP (the phase-100 IMPL close), EACH BY RUNNING ITS COMMAND IN THE COMMIT THAT SHIPS THESE EDITS:** `ROADMAP.md` **250** lines / **132** data rows / tail row **100** (**`done`**) at file line **162** (UNMOVED by the flip) · `DECISIONS.md` **19644** (was 19509: ADR-0322 completed, plus the review and final-review fixes, `136 1` in all), `^---$` **216**, `^## ADR-` **321**, bare `^## ` **329**, tail **ADR-0322** (**`ACCEPTED` — the house guard is DISARMED**), next-free **ADR-0323** · `BEHAVIOR_CONTRACT.md` **6000** (was 5998, `3 1`: the `Per-pipeline timeout` bullet and the delta-only `+1` ledger entry) · `STATE.md` **66** (rolled IN PLACE) · `STATE_HISTORY.md` **592 -> 594** (**+2**), strict **163 (DELTA 0)** / parenthetical **84** / loose **247** (**163 + 84 = 247** exactly) · phase dirs **141** (UNMOVED — an IMPL adds none) · `100/BRAINSTORM.md` **514** lines, `100/SPEC.md` **853**, `100/PLAN.md` **2938**, `100/PROGRESS.md` **1461** · fixtures **127 = 127** (**103 `driver/` + 24 `inputs/`**, re-run at this close) · `-family row` **96 / 68** under `--` · ✅ **MEASURED AT THE phase-100 IMPL, THE LAST STAGE THAT RAN THEM (Task 1 at the un-fixed tip, Tasks 10 and 17 after the fix):** `go test -count=1 -v ./internal/listener/... ./internal/stats/...` **470** `=== RUN` (266 + 196 at the un-fixed tip, plus 8 new), the five-selector listener roster **421** `=== RUN` (Task 1, un-fixed tip), gate (b) **242** packages (241 + `0125/driver`, MEASURED), fuzzers **56 targets / 48 FILES**, h2spec **`95 tests, 94 passed, 1 skipped, 0 failed`**. ⚠️ **A BRAINSTORM re-runs none of the gate figures — carry them LABELLED as the phase-100 IMPL's.**
- ⚠️ **A BRAINSTORM, A SPEC AND A PLAN RUN NONE OF THE SIX GATES, AND AN IMPL RUNS ALL SIX — THAT IS SCOPE, NOT OMISSION. ⚠️⚠️ YOUR STAGE IS A BRAINSTORM AND RUNS NONE — NAME THAT AS SCOPE, DO NOT CLAIM COMPLIANCE.** ✅ **THE phase-100 IMPL RAN ALL SIX (Task 17):** gate (b) GREEN in ONE sweep under the AMBIENT go1.27.1, and `golangci-lint` RUNS as `GOTOOLCHAIN=go1.26.2 golangci-lint run ./...` (rc 0; a COMPILING planted control fired `errcheck`, `revive` and `ineffassign`). **Neither is a departure — do not restate either as one.** The one STANDING DEPARTURE left is no `REVIEW.md` (NONE of 93-100). ⚠️ **A v2 linter move was NEVER part of the toolchain fold-in** — 97 new findings, ADR-gated, unchartered.
- ⚠️ **THE STAT SURFACE IS QUOTED AS A DELTA AND NEVER AS AN ABSOLUTE.** Three different absolutes are live at one tip. ✅ **ROW 100 LANDED ITS `+1` NAME (`listener.<addr>.downstream_pre_cx_timeout`), CONFIRMED BY VALUE on both sides** (`0125`'s S arm: `l_false` 51, `l_true` 1, `l_true_tls` 1, `l_zero` 0, `l_nofilt` 0, each side looked up by its OWN address label), registered unconditionally per listener in `registerListenerMetrics`, with a **delta-only** ledger entry (`BEHAVIOR_CONTRACT.md:5150`, NO ABSOLUTE — a deliberate departure from phase 94's `1208 → 1209 (+1)`). ⚠️ **QUIC listeners register the name too** (`quic.go` calls `registerListenerMetrics`), value 0 forever; the reference's QUIC behaviour is UNMEASURED — pin nothing (banked). ⚠️ **The reference registers the name at BOOT with value 0 — a name-presence pin is VACUOUS (method note 81); pin the VALUE beside a mirror arm.**

==============================================

## STATUS AS OF 2026-09-22: PHASE 100 (`listener-filters-timeout-enforce`) **IMPL DONE — LIFECYCLE-STATE `DONE`**. Row 100 reads `done` at `ROADMAP.md:162`. **THE SENTINEL WAS RE-EVALUATED MECHANICALLY ACROSS THE FLIP AND DOES NOT FIRE.** `stop` was NOT created. **THIS SESSION OPENS PHASE 101 BY SELF-PICK AND WRITES ITS `BRAINSTORM.md`.**

**What phase 100 landed, in one paragraph.** On a TCP listener with a listener filter, envoy-go never enforced `listener_filters_timeout`: a silent client was held until the CLIENT acted, while the reference times out at the deadline, books `downstream_pre_cx_timeout`, and closes under `continue_on_listener_filters_timeout: false` or falls through to chain selection under `true`. **The repair is ONE CLOCK INSIDE `Pipeline.Run` (PB1)**: a `context.AfterFunc` on the pipeline's own context sets the socket read deadline into the past, so a blocked peek is cut only after the context is done and every cut is reported as the context's error; the deadline is cleared before `Run` returns, and `pipeline.go:43` is UNMOVED under a five-sub-gate layout gate. `serveConnection` books `listener.<addr>.downstream_pre_cx_timeout` (**+1 NAME**) under both `continue…` values; `parseListenerFiltersTimeout` splits an ABSENT field (15 s) from an explicit `0s` (DISABLED). Production by `git diff --numstat` against the pre-row tip: `pipeline.go` `17 2`, `manager.go` `28 19`, COMMENT-ONLY `tls_inspector.go` `3 3`, and the help-text pair `name.go` `2 0` + `helptext_test.go` `1 0`. Fixture **`0125-listener-filters-timeout`** (**+836**, five listeners on `15125` + `15228`-`15231`, no PKI, no `inputs/`) was RED at the un-fixed tip on F1, F2, S and CompareBytes and GREEN 3/3 after. **ADR-0322 `ACCEPTED`**; `BEHAVIOR_CONTRACT.md`'s `Per-pipeline timeout` bullet rewritten plus the delta-only `+1`; `REVIEW_FINDINGS.md`'s timeout clause annotated fixed. **NOT bought (banked below):** the envelope lift, the close KIND, the HTTP/1 invalid-method-byte wait, the `downstream_listener_filter_{remote_close,error}` names, post-filter `downstream_cx_total`, the reference's QUIC registration of the new name.

### ⚠️ WHAT THE PHASE-100 IMPL REFUTED — FOURTEEN CLAIMS (full detail `100/PROGRESS.md` §Close-out)

1. ⚠️ **PLAN Task 14's `BEHAVIOR_CONTRACT.md` 5998 -> 5999 is 5998 -> 6000** — a ledger entry is a blank-separated paragraph; PLAN §1.2's own `~+3 / -1` agreed with the measured `3 1`.
2. ⚠️ **PLAN §1.2's `manager.go 25 16` measures `28 19`** — Appendices A + B are exactly `25 16`; the review-wave comment fix moved it.
3. ⚠️ **PLAN §1.2's `DECISIONS.md ~+100 / 0` measures `129 1`** at Task 17 (`136 1` after the final-review fix) — ADR-0322's status line is rewritten, not only appended to.
4. ⚠️ **One reference close spread reached 1030 ms (σ 13.72, n=51)** against the PLAN's 1000-1004 ms (n=459); the other two runs read max 1002 / 1003; the `[700, 1800]` window held.
5. ⚠️ **NC1 unit: U1 fell through 9, 5, 6 times of 20** against the declared 5-8.
6. ⚠️ **NC1 fixture: S `l_true` / `l_true_tls` red in 2 of 2 runs** against the declared 1 of 2.
7. ⚠️ **NC1 fixture: S `l_false` 14 / 13 and F2 open 36 / 37** against 17 / 24 and 33 / 26 — same direction, never pinned.
8. 🔴 **Appendix B's metric-block comment was FALSE** ("Inc/Dec'd from the per-connection path" — both `Inc()`s are in `acceptLoop`); found by the Task 8-11 review, fixed comment-only, accurate prose ruled over byte-fidelity to an appendix.
9. ⚠️ **The Task 19 brief's extraction swallowed every appendix (2017 lines)** — benign; the appendices were extracted from `PLAN.md` itself.
10. ⚠️ **The IMPL's own Task 4 scratch copy of the OLD U2 had no `import` block** and broke the package build when Task 12 used it (fixed in the throwaway only).
11. 🔴 **The row-100 cell's `manager.go 25 / 16` was TRUE when Task 16 wrote it and FALSE at the branch tip** — a later commit of the same stage moved the file (final review; method note 97).
12. ⚠️ **ADR-0322 §Decision omitted the comment-only `tls_inspector.go` edit** (final review).
13. ⚠️ **`0125/README.md` carried only PLAN-time window and NC figures** (final review; IMPL observations ADDED beside them).
14. 🔴 **Task 17's "17 files, narrower than all three precedents" compared unlike things** — the precedents are squashed commits that carry the three close-out files; at the close the branch reads **20**, equal to phases 99 and 96 (method note 98).
✅ **CONFIRMED, not refuted:** every figure the PLAN BUILT (byte-identical appendices, 470 RUN, the six named tip REDs, `0125` +836), gate (b) **242** (the router's `+1` prediction), the flip's sentinel shapes, and the projected FOUR-WIDE §Recent tie. ⚠️ **Items 11-14 came from the IMPL's OWN final review and close — method note 2's "expect to be refuted by your own review seam" held for a FOURTH row.**

### ⚠️ WHAT PHASE 100 ESTABLISHED — **EVIDENCE YOU MAY CITE, NOT WORK YOU OWE**

- **`100/SPEC.md` §2 — the reference, 15 arms + validate mode, in-network, by digest** (including `0.5s`, `61s` and `120s` validating AND enforcing — the envelope-lift evidence; `0.5s` re-measured at validate rc=0 with a drop at 502 ms); §3 the subject's shapes; §4 the decided edit and its layout gate; §14.3 what is banked.
- **`100/PLAN.md` §0 — sixteen findings against its SPEC** (a layout gate that could not pass as written; two NC rows with no path to the arm they named; a fixture floor overstated by ~60 %); §4 the per-arm matrices; §6 `layout-gate.sh`; §7 the corrected NC roster; Appendices A-H the built artifacts.
- **`100/PROGRESS.md` — a 19-task IMPL record**: the un-fixed tip captured before Task 8, the NC roster scored per arm with each mutant's `NC<n>` marker asserted first, a single-sweep six-gate record, and the close-out's fourteen refutations.
- **`100/BRAINSTORM.md` §4 — the adjudicated rejected roster** (§4.1 the envelope lift NEXT IN LINE, §4.2 post-filter `downstream_cx_total`, §4.3 the order-dependent fold's sharpened axes) and §0 its thirteen refutations of this file. **Where it disagrees with `100/SPEC.md` §0, the SPEC's measurement governs.**
- **`ADR-0322` (`ACCEPTED`)** — §Consequences (f) lists what the row did NOT buy, (g) states the envelope lift is unblocked, and (a) records the `0s` shutdown hold (derived from the mechanism, NOT measured).

### ⚠️ WHAT PHASE 99 ESTABLISHED — **EVIDENCE YOU MAY CITE, NOT WORK YOU OWE**

- **`99/SPEC.md` §2 — the reference, both transports, by digest**; `99/PLAN.md` §4 — the MEASURED per-arm matrix (tip / P2 / inverted / rank-mutant / P1 / drop-length); **`99/PLAN.md` §6 — a comment-only AND line-count-neutral gate with five controls A-E shown to FIRE (reusable)**; §7 plus `PROGRESS.md` Task 13 — five NC roster rows, each COMPILED before it was scored, scored PER ARM.
- **`99/BRAINSTORM.md` §0.1-0.4 — the toolchain re-diagnosis** (the model for method note 83); §0.12-0.14 and §4.3-4.4 — nested descent, duplicate-matcher reject parity, the no-match close the reference does not count, and the `listener_filters_timeout` NEXT-IN-LINE adjudication.
- **`99/PROGRESS.md` — a 19-task IMPL record**: the un-fixed tip captured before Task 10, and a six-gate sweep with BOTH gate-(b) sweeps kept side by side (a green second sweep does not erase a red first one).
- **`ADR-0321` (`ACCEPTED`) — SNI matched-pattern precedence**; its (f) lists what the row did NOT buy, the order-dependent fold among them.
- ⚠️ **The phase-99 PLAN ran in the SAME session as its SPEC, at the human's explicit `continue`** — a recorded deviation from BOOTSTRAP §5.1, not a precedent to chain stages by default.

### Banked, NOT chartered

- ⚠️ **`TestEnvoyGoBinary_TwoListenerCutover` PORT FLAKE — NEW at the phase-98 SPEC.** Fired twice in one session (`bind 127.0.0.1:36601` at the un-fixed tip, `:33215` under a prototype), both inside the ephemeral range; three isolated reruns green. **A green rerun clears nothing.** Same class as the driver-owned receiver port race; not yet in any flake register.
- ✅ **SNI LONGEST-SUFFIX PRECEDENCE — DONE AS ROW 99.** Not banked; see the STATUS section.
- ✅ **`listener_filters_timeout` NON-ENFORCEMENT — DONE AS ROW 100** (the phase-100 IMPL, ADR-0322 `ACCEPTED`). Not banked; see the STATUS section.
- ⚠️ **QUIC LISTENERS REGISTER `downstream_pre_cx_timeout` — NEW AT THE phase-100 PLAN (`100/PLAN.md` §0.9).** `quic.go:66` calls `registerListenerMetrics`, so now that row 100 has landed every QUIC listener carries the name at value 0 forever (QUIC runs no listener-filter pipeline). **Whether the reference registers it on a QUIC listener is UNMEASURED** — measure before any pin; a name-presence pin would be vacuous anyway (method note 81).
- ⚠️ **`manager.go:<line>` CITATIONS ARE PRE-DRIFTED — NEW AT THE phase-100 PLAN (`100/PLAN.md` §0.14).** Ten sampled across `internal/`, `test/` and `BEHAVIOR_CONTRACT.md`; NONE points at its named symbol at `b6aa3ab3` (e.g. `0121`'s `manager.go:1393` names `rt.sslHandshake.Inc()`, which sits at `:1410`). A citation-hygiene item, not a row; **anchor on SYMBOLS** (method note 3).
- ⚠️ **THE CLOSE KIND UNDER `continue…: false` — NEW AT THE phase-100 SPEC (`SPEC.md` §0.1, §14.3).** When a peeked byte is unread the reference's close is an **RST** (it peeks with `MSG_PEEK`); envoy-go's `bufio` peeker has already consumed the byte, so every shape closes with **FIN**. ⚠️ **INVISIBLE TO THE DIFFERENTIAL HARNESS** — a host `-p` mapping (docker-proxy) turns the reference's RST into FIN too; only an in-network client sees it. The repair lives in the peeker (linger-0 when the buffer holds unread bytes, or a real `MSG_PEEK`); ⚠️ `serveConnection`'s comment says the wrapper deliberately does NOT implement `SetLinger` so `0043`'s NoFlush close stays a FIN — **census that pin first**.
- ⚠️ **envoy-go's HTTP/1 CODEC DOES NOT REJECT AN INVALID METHOD BYTE UNTIL A LINE TERMINATOR — NEW AT THE phase-100 SPEC (`SPEC.md` §0.2).** A lone `0x16` (or a 5-byte TLS header) reaching the HCM: the reference answers `400` at once (`NO_REQUEST_LINE_IN_REQUEST`, then a 1 s delayed close); envoy-go holds the connection, answers nothing, and books `rq_4xx` only when the client closes. Exists with NO listener filter; an HCM row, reference measured only behind a timeout fall-through.
- ⚠️ **`downstream_listener_filter_remote_close` AND `downstream_listener_filter_error` — NEW AT THE phase-100 SPEC (`SPEC.md` §0.9).** Registered at boot on every reference listener (admin too); absent on envoy-go. `remote_close` moves when a client closes or half-closes DURING inspection (half-close at 301 ms → FIN at 301-302 ms, `remote_close` 1); the subject agrees on the timing. One reference run read 2 for one connection (three reruns 1) — unexplained. **A name-presence pin is vacuous (boot registration); pin the VALUE.**
- 🔴 **THE `listener_filters_timeout` ENVELOPE `[1s, 60s]` IS envoy-go's OWN — NEW AT THE phase-100 BRAINSTORM (§0.2), REFERENCE MEASURED, ADJUDICATED THE NEXT ROW AFTER 100 (§4.1).** The reference validates AND enforces `0.5s` (drop at 501 ms), `61s` and `120s` (rc=0, no warning); envoy-go boot-REJECTS all three (`parseListenerFiltersTimeout`). Fail-CLOSED on a bootstrap the reference runs. Same ADR (ADR-0082) and function as row 100; ⚠️ **it must FOLLOW row 100, never precede it** — lifting the reject first would admit configs whose timeout still does nothing. ⚠️ `500ms` is a `protojson` reject, not an envelope reject — spell `0.5s`. Upper bound past `120s` UNMEASURED. ✅ **UNBLOCKED — ROW 100's IMPL LANDED ON 2026-09-22** (the phase-100 SPEC re-measured `0.5s` at validate rc=0 and a drop at 502 ms; ADR-0322 KEEPS the envelope and names its lift as the next row, §Consequences (g)). ⚠️ **`manager.go:951-962` HAS DRIFTED — row 100 edited `parseListenerFiltersTimeout` (the `0s` split); anchor on the SYMBOL (method note 3).** ⚠️ **A LOWER bound below 1 s now meets row 100's ENFORCEMENT: a `0.5s` arm must DRIVE the drop, not only boot.**
- ⚠️ **`downstream_cx_total` IS POST-FILTER ON THE REFERENCE, AT-ACCEPT ON envoy-go — NEW AT THE phase-100 BRAINSTORM (§0.10, §4.2).** A reference drop arm reads `cx_total: 0`; envoy-go increments `cx_total` and `cx_active` in the accept loop before `serveConnection`. Every listener with filters, not only timeouts. Moving the `Inc` moves every fixture pin on `cx_total` — census them first.
- ⚠️ **`REVIEW_FINDINGS.md` (repo root, commit `9f26f380`, 2026-07-07) IS AN UN-CHARTERED FINDINGS SOURCE NO ROUTER CITED** — it held the timeout defect seventy-five days before the phase-98 PLAN re-discovered it, plus SNI case-sensitivity and an H2 `closedStreams` growth item. **GREP IT WHEN BANKING OR PICKING.**
- ⚠️ **THE ORDER-DEPENDENT PAIRWISE FOLD IN `SelectChain` — NEW AT THE phase-99 SPEC (`SPEC.md` §0.4), SHARPENED AT THE phase-99 IMPL's FINAL REVIEW, SUBJECT-SIDE ONLY.** `SelectChain` returns `ErrAmbiguousChainMatch` on the FIRST nil `breakTie`, before a later chain that outranks both is compared. **It bites on TWO axes, not one:** (1) the ALPN axis, F1 A{`application_protocols:[h2]`}, B{`[http/1.1]`}, C{`["*.foo.test"]`} CLOSES while F2 (C declared first) serves C — at the tip AND under the row-99 repair; (2) 🔴 **the SNI slot ITSELF, after the row-99 repair**: with the final review's three-chain set, `[OA, OB, LONG]` under SNI `a.b.foo.test` returns AMBIGUOUS while `[LONG, OA, OB]` serves LONG (probe-confirmed at the final review; OA and OB tie each other, which is the only way the fold can stop there). **So row 99's "independent of declaration order" is PAIRWISE ONLY** — `BEHAVIOR_CONTRACT.md:4367` now carries that caveat, pointing at ADR-0321 (f). ⚠️ **AND THE `ErrAmbiguousChainMatch` DOC COMMENT (`chainmatch.go:54-58`, "two eligible chains have identical specificity vectors and breakTie cannot separate them") IS ACCURATE ONLY ONCE THE FOLD IS REPAIRED** — today a third chain that WOULD separate them is never consulted. Repair it WITH the fold. **The reference was NOT driven on F1 or on the SNI triple** (its nested descent would pick the most specific chain — INFERRED). A different function body and arm set from row 99; ⚠️ **measure the reference BEFORE chartering.** ⚠️ **SHARPENED AT THE phase-100 BRAINSTORM (§4.3): the SNI-triple axis needs two chains whose best MATCHED patterns TIE — a class the reference REFUSES at validate (`multiple filter chains with overlapping matching rules`) — so that arm may have NO reference behaviour to match; the ALPN axis is entangled with nested descent. Measure F1 on the reference first.**
- ⚠️ **THE `rank > 2` GUARD IN `sniMatchedRank` HAS NO COVERING ROW — NEW AT THE phase-99 IMPL (Task 10 review).** No unit row, QUIC arm or fixture listener reaches it; a `{"*.b.foo.test","*"}` vs `*.foo.test` pair would pin it. A coverage gap, not a known defect — a fold-in candidate, not a row.
- ⚠️ **THE SDS DIAL-BUDGET FLAKE RECURRED AT THE phase-99 IMPL** — `TestSDSEndToEnd_FetchFailure_BootFailsClosed` (`internal/boot`) failed in 1 of 2 full gate-(b) sweeps (`DeadlineExceeded` where the test wants the initial-fetch-timeout message), green 3/3 in isolation. **A green rerun clears nothing** — both sweeps are recorded side by side in `99/PROGRESS.md`. It is on method note 4's list; this is a RECURRENCE, not a new flake.
- ⚠️ **THE `0s` SHUTDOWN HOLD — RECORDED IN ADR-0322 §Consequences (a), NOT BANKED AS A DEFECT (phase-100 IMPL review wave).** PB1's `context.AfterFunc` is armed only when `timeoutMs > 0`, so on an explicit-`0s` listener a manager shutdown does NOT cut a silent client's peek: the `serveConnection` goroutine waits for the client or process exit (`acceptLoop` does not track it). DERIVED from the mechanism; the reference's shutdown behaviour was never measured. **Measure the reference before calling it a divergence.**
- ⚠️ **PARKED phase-100 REVIEW MINORS (cosmetic, fold-ins, not rows):** the `listenerRuntime` metric-block comment in `manager.go` describes the TCP path only (QUIC listeners register `downstream_pre_cx_timeout` too); the U5 / U5f helper duplication in `listener_filters_timeout_test.go` (a PLAN-built, byte-identical appendix); one ~110-char comment line at the listener-filter registration site.
- ⚠️ **`0125`'s REFERENCE CLOSE SPREAD WIDENED ONCE AT THE phase-100 IMPL** — max **1030 ms**, σ **13.72** (n=51, Task 10 run 2) against 1000-1004 ms, σ 0.92 at the PLAN (n=459). Still ~50σ inside `[700, 1800]`. **A wider recurrence is a finding; a green run clears nothing** (`reference_differential_band_sigma_margin`).
- ⚠️ **`server_names` PARTIAL-WILDCARD ACCEPTANCE.** Reference rejects `*`, `bar.*.test`, `*foo.test`, `foo.test.*`; envoy-go accepts all of them plus `**.test`, `*.` and `""`. The repair deletes envoy-go's `"*"` tier, documented at **eight** sites (ADR-0033 clause 9, ADR-0078 clause 9, `BEHAVIOR_CONTRACT.md`, `chainmatch.go` comments — **the union of TWO matchers, neither complete alone**) and pinned by two `chainmatch_test.go` assertions plus three `quic_test.go` comments.
- ⚠️ **SNI CASE-INSENSITIVITY — MEASURED ON BOTH SIDES AT THE phase-99 BRAINSTORM, AND DELIBERATELY *NOT* FOLDED INTO ROW 99** (against this file's old "land WITH, not alone"; `99/BRAINSTORM.md` §1.3). The reference folds PATTERN and INPUT, wildcard and exact, and its duplicate check too; envoy-go folds neither. A pattern-only fold (`4 1` in `manager.go`) fixes the pattern arm and **NOT** an uppercase SNI on the wire — the input side runs through `tls_inspector` AND the QUIC SNI path. 0 tests pin case at all.
- ⚠️ **NESTED-DESCENT PRECEDENCE — NEW AT THE phase-99 BRAINSTORM, the largest chain-match divergence known.** envoy-go scores a flat presence bitmask and consults finer grain only on exact bitmask ties; the reference descends dimension by dimension and **does not backtrack** (reference arm G3: `*.b.foo.test` vs `*.foo.test`+`application_protocols:[h2]`, SNI `x.foo.test` offering `http/1.1` only ⇒ reference CLOSES). Subject side and every mixed-dimension shape UNMEASURED. `99/BRAINSTORM.md` §0.12, §4.3.
- ⚠️ **DUPLICATE-MATCHER REJECT PARITY — NEW AT THE phase-99 BRAINSTORM.** The reference rejects OVERLAPPING server_names sets (`multiple filter chains with overlapping matching rules are defined`) and case-variant ones; envoy-go rejects only structurally IDENTICAL specs and boots the overlapping one. `99/BRAINSTORM.md` §0.13.
- ⚠️ **`catchAllCount` IS DEAD CODE.** `0 / 7`, 0 tests RED, NOT CONSTRUCTIBLE, and its only guard is vacuous. **Cheapest by every figure and REJECTED as a row because it has NO cross-side surface** — a fold-in, not a phase.
- ⚠️ **THE HCM `stat_prefix` DUPLICATE-REGISTRATION PANIC — measured at phase 97 (`BRAINSTORM.md` §4.1) and NOT re-derived at phase 98, deliberately.** Ten files register through the panicking constructor; the repair mints a boot panic `TestMain_BootPanicIsVisible` says must be RE-POINTED; it needs **TWO** fixture directories at a measured floor of ~1359-1393 added lines each. ⚠️⚠️ **FIXTURE `0122` SITS ONE IDENTIFIER AWAY FROM IT**: `chain_indexed` and `chain_default` MUST differ, and that is load-bearing.
- ⚠️⚠️ **THE DRIVER-OWNED RECEIVER PORT RACE — EVIDENCED, NOT HYPOTHETICAL.** It fired **TWICE at TWO fixtures in ONE session** at the phase-97 IMPL (`0085` port 41177, `0082` port 41561, both inside `net.ipv4.ip_local_port_range` 32768-60999) and its failure mode is **MASKING** — the abort stops the run and **everything after it is unmeasured** (the second abort masked **40** fixtures including the row's own). It is a HOST-PORT bind by the Go driver, **NOT a container publish** — do not blame `curl-world-*`. **36** files under the banked narrow predicate, **37** under a wider one; ⚠️ **the 31 / 5 / 0 split is PER FILE, not per bind site** — the same 36 files carry **43** bind sites and split **36 / 7 / 0** that way. **Name the unit or the split is meaningless.**
- ⚠️ **PER-CONNECTION TLS *IDENTITY* DISPATCH — the residual phase 97 left, STATED not hidden.** Selection is per connection; the certificate is chosen once at Start. **NOT MEASURED** — reasoned from the mechanism. Closing it means a per-client config callback that **ADR-0317 `D-ALPNFB-TCPONLY` forbids on this path** and that an existing `manager_test.go` pin asserts stays nil.
- ⚠️ **THE STAMPED QUIC ALPN CONSTANT IS A NEW ASSUMPTION, NOT A REUSED INVARIANT** — the value lives in no production Go file. Correct for an `h3` listener because negotiation can only yield it or fail, but **a listener with a different ALPN would have its `application_protocols` dimension evaluated against a hardcoded one.** **DELIBERATELY NOT REPAIRED: changing the shape without re-measuring would encode an UNMEASURED PARITY ANSWER.**
- ⚠️ **A MEASURED PHASE-97 COVERAGE GAP: zeroing the destination and source address fields on the per-connection path reddens NO arm**, because the only `destination_port` arms are nil-conn arms. Closing it means pinning `source_type`, **which the reference was never measured on** — the deferral is about MEASUREMENT, not mechanism.
- ⚠️ **THE DIFFERENTIAL HARNESS CANNOT CARRY TWO UDP LISTENERS, OR A MIXED TCP+UDP PAIR.** Three named edits close it (`97/PLAN.md` §3); the reason is `runFixture`'s transport-blind single-valued address dispatch, **not** port plumbing. Also `freeTCPPortBlock` probes only TCP, so a derived port used for a QUIC listener is never UDP-probed.
- ⚠️ **THE `0066`/`0067`/`0068`/`0071` DEAD-PORT FALSE-GREEN — a WORSE failure mode than the port race.** They allocate a port, close it, and rely on nothing listening. The port is inside the ephemeral range. If anything grabs it first the fixture asserts the OPPOSITE of its proposition — a silent wrong **PASS**.
- ⚠️ **THE BYTE-UNTOUCHED / EDIT ROSTER PAIR IS NOT A PARTITION** — at phase 97 `BEHAVIOR_CONTRACT.md` was touched and sat on neither. A roster-DOCUMENTATION gap, recorded not repaired.
- **D2-QUICTS** — reference-confirmed with its exact message, `no transport socket specified for connection oriented UDP listener`, rc=1 at listener-add. envoy-go accepts, boots and serves. Fails CLOSED. **Consumed as a phase-97 test fixture without being repaired.**
- **envoy-go emits no `listener.<addr>.http.<prefix>.*` scope at all** — the reference emits **12** such lines for a two-chain QUIC listener and the subject **0**.
- **Duplicate listener ADDRESSES — NARROWED at the phase-100 BRAINSTORM (§0.13): the divergence is VALIDATE-ONLY.** The reference rejects them in `--mode validate` (rc=1, `error adding listener: 'l_dup' has duplicate address '0.0.0.0:10000' as existing listener …`); envoy-go's `-mode validate` reads `configuration OK` rc=0, but its BOOT fails rc=1 (`bind: address already in use`). Both fail CLOSED at boot.
- **An SDS by `alpn_protocols` fixture** — intersection CONFIRMED empty; ~1200-1400 added lines; coverage, not a defect.
- **ADR-0025's stale `Accepted` status** and **the ADR-0044 misattribution** — recorded at `97/SPEC.md` §0.4 and §0.10, neither repaired. ⚠️ **Quote NO count of the misattributing lines.**
- **The `registry.go` citation set** — `ROADMAP.md:136` col 6095 (wrong), `DECISIONS.md:2356` (wrong), and the ADR-0296 §Context one deliberately left in place.
- **`ROADMAP.md:136` AND `:139` CARRY THE SAME STALE SWALLOWED-PANIC CLAIM AS `:232` (`:231` before the row-100 ADD, `:230` before the row-99 ADD; `:233` after a row-101 ADD)** — `:136` was REPAIRED at the phase-97 IMPL; `:139` and `:231` were not, and `:231` sits inside a sentinel window on a margin of one. **RECORD IT, DO NOT TIDY IT.**
- `0120/expectations.yaml`'s stale SCOPE · the drifted `manager_test.go` citation now at `:5832` · `0108`'s three false `ssl.*` confessions · `0118/driver/driver.go:31`'s falsified band · the `GetConfigForClient` history-class comment sites (**a CONTESTED count — quote NO number**) · `0061-lb-ring-hash`'s sigma-margin second occurrence · 1xx interim responses · the H/1 no-`Host` divergence · the pooled-upstream-lifetime defect · the TEN fixed `ssl.*` names and FOUR dynamic families (blocked on NAMING) · the DYNAMIC half of the downstream TLS `ssl` stat family (blocked on the stats-name charset banning the hyphen).

## ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + ALL FOUR NCs + THE CHECK-(2) POSITIVE CONTROL YOURSELF** and record ACTUAL output. ⚠️ **NC SHAPES CHANGE ACROSS A ROW CHANGE — NEVER INHERIT ONE.** They changed at the phase-98 BRAINSTORM's ADD (check (1) SILENT -> ONE, NC-A/NC-B ONE -> TWO), were MEASURED UNCHANGED at the phase-98 SPEC and PLAN (neither adds a row nor flips one), and **changed back at the phase-98 IMPL's FLIP (check (1) ONE -> SILENT, NC-A and NC-B TWO -> ONE), measured on both sides.** **They changed AGAIN at the phase-99 BRAINSTORM's ADD (check (1) SILENT -> ONE, NC-A and NC-B ONE -> TWO, `want` 130 -> 131, `ROADMAP.md` 248 -> 249), measured on both sides**, were measured UNCHANGED at the phase-99 SPEC and PLAN, **and changed back at the phase-99 IMPL's FLIP (check (1) ONE -> SILENT, NC-A and NC-B TWO -> ONE; `want` 131 and `ROADMAP.md` 249 on both sides), measured on both sides.** **They changed AGAIN at the phase-100 BRAINSTORM's ADD (check (1) SILENT -> ONE, NC-A and NC-B ONE -> TWO, `want` 131 -> 132, `ROADMAP.md` 249 -> 250), measured on both sides**, and were **measured UNCHANGED at the phase-100 SPEC** (it neither adds nor flips a row). They were **measured UNCHANGED at the phase-100 PLAN** too, **and changed back at the phase-100 IMPL's FLIP (check (1) ONE -> SILENT, NC-A and NC-B TWO -> ONE; `want` 132 and `ROADMAP.md` 250 on both sides), measured on both sides.** ⚠⚠ **YOUR STAGE IS A BRAINSTORM: its ADD of row 101 is EXPECTED to take check (1) SILENT -> ONE and NC-A / NC-B ONE -> TWO, with `want` 132 -> 133 and `ROADMAP.md` 250 -> 251. MEASURE THEM ON BOTH SIDES OF THE ADD.**
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** 95 BRAINSTORM nine · 95 SPEC eleven · 95 PLAN seventeen · 95 IMPL eighteen · 96 BRAINSTORM thirteen · 96 SPEC ten · 96 PLAN fifteen · 96 IMPL six · 97 BRAINSTORM twelve · 97 SPEC eleven · 97 PLAN sixteen · 97 IMPL fourteen · 98 BRAINSTORM thirteen · 98 SPEC ten · 98 PLAN fourteen · **100 IMPL fourteen (nine against the PLAN — among them `BEHAVIOR_CONTRACT.md` 5998 -> 6000 not 5999, `manager.go` `28 19` not `25 16`, a reference close at 1030 ms against 1000-1004, three NC1 probabilistic cells, and an appendix comment that was FALSE — and five against the IMPL's OWN work, four of them from its final review or its close: a row cell falsified by a LATER commit of the same stage, and a file-scope comparison of 17 against squashed commits that carry 20; every figure the PLAN had BUILT reproduced exactly)** · **100 PLAN sixteen (eleven outright against SPEC rows — among them a layout gate that could not pass as written (THREE `WithTimeout` hits, two of them comments), two NC rows with no path to the arm they named (NC4 → U4(ii), NC8 → no unit arm), and a fixture FLOOR overstated by ~60 % (`≥ ~1360` vs a MEASURED +836) — while the one load-bearing unmeasured item, the reference's F2 through `-p`, SURVIVED: 1000-1004 ms, not smeared)** · **100 SPEC nine (among them its predecessor's own prescription to re-point a parse pin that DOES NOT EXIST, and a close-kind cell the harness CANNOT OBSERVE on the reference — while both load-bearing claims the router flagged for re-measurement SURVIVED)** · **100 BRAINSTORM thirteen (among them this file's own banked split (b), its `downstream_cx_total → 3` control figure and its duplicate-address verdict — while the banked record's CORE survived re-measurement)** · **99 BRAINSTORM fourteen · 99 SPEC eleven (its §0.1 refuted the BRAINSTORM's own prototype against the reference) · 99 PLAN nine (five outright, including two of the SPEC's own NC-roster rows) · 99 IMPL eight (five against the PLAN — including the gate-(b) denominator this file also carried — one against the SPEC, and two against the IMPL's own prose, caught by its own final review)** · **98 IMPL EIGHTEEN, every one of them against a GOVERNING DOCUMENT or the router — including a fixture `expectations.yaml` that contradicted the PLAN and was itself the wrong document, and a PLAN code block NOT INSTALLABLE AS PRINTED** — and the earlier chain: **98 PLAN FOURTEEN, NINE by its own agents — including `SPEC.md` §14 ITEM 1's OWN INSTRUCTION and the fixture cost floors THIS FILE quotes** — and the SOURCE matters more than the count: **THREE of the sixteen came from that stage's OWN agents**, one of which corrected its own summary line against its own table mid-report, and one of which refuted a STANDING METHOD NOTE. ⚠️ **EXPECT TO BE REFUTED BY YOUR OWN REVIEW SEAM, NOT ONLY BY YOUR SUCCESSOR** — FOURTH consecutive row (the phase-100 IMPL's final review and close produced four of its fourteen). ⚠️ **AND WHEN TWO PARTIES CONTRADICT EACH OTHER, FIND THE VARIABLE RATHER THAN PICKING A WINNER.**
3. **LINE ANCHORS DRIFT; SYMBOL AND LITERAL-TEXT ANCHORS DO NOT — RE-VERIFY EVERY CITE AT YOUR TIP.** ⚠️ **PATHSPEC-SCOPE every symbol assertion and every `sed`.** ⚠️ **`internal/filter/hcm/chain.go` DOES NOT EXIST — it is `internal/filter/http/chain.go`** — but **`actions.go` IS in `hcm`.** ⚠️ **PROVEN AGAIN AT PHASE 96: `manager.go:1393` -> `:1406` and the D4 block `:746` -> `:759`, both moved by the SAME stage's own edits.**
3a. ⚠️ **AN ANCHOR CAN BE NON-UNIQUE.** **Anchor on the ENCLOSING SYMBOL.**
3b. ⚠️ **CHARACTER-CLASS AND ESCAPE SELECTORS ARE THE FAIL-UNSAFE FAMILY.** `^[0-9]{4}-` on fixtures reads 121, dropping `0007a`/`0007b` (use `ls -d test/fixtures/*/ | wc -l`) · **a CASE-SENSITIVE GREP IS BLIND TO UPPERCASE PROSE — use `-i` when auditing comments** (`0008`'s `EXACTLY one chain` was invisible to a case-sensitive sweep) · ⚠️ **AND `[ \t]` INSIDE A GNU ERE BRACKET EXPRESSION IS A LITERAL BACKSLASH AND `t`, NOT A TAB.** See also notes 33 and 56.
3c. ⚠️ **`feedback_brief_citations_not_evidence` APPLIES TO INCIDENTAL FIGURES — including every number in THIS file and in every AGENT REPORT.**
3d. ⚠️ **NO GATE MAY REST ON A ` + ` SPLIT.**
3e. ⚠️ **A GATE COMMAND CAN MATCH PROSE INSTEAD OF CODE.** Anchor fixture-set extraction on the blank-import LINE:
```sh
extract () { grep -oE '^[[:space:]]*_ "github\.com/pgdad/envoy-go/test/fixtures/[^/]+/(driver|inputs)"$' "$1" \
  | sed -E 's#.*/test/fixtures/##; s#/(driver|inputs)"$##' | sort; }
```
🔴 **CORRECTED TWICE, AND THE SECOND CORRECTION IS THIS ONE.** The note read `123 / 123` and `99 + 24` for two rows while this file's own live-figures bullet already said `124`, so the router DISAGREED WITH ITSELF inside one document; that was fixed to `124 / 124` and `100 + 24` at the phase-98 PLAN — **and phase 98's own fixture `0123` moved it again, and phase 99's `0124` once more.** Re-run VERBATIM at the phase-99 IMPL close: **dirs 126 = imports 126**, both `comm` directions EMPTY, split **102 `driver/` + 24 `inputs/`** (125 / 125 and 101 + 24 before `0124`, measured on both sides at that IMPL). **Re-run VERBATIM at the phase-100 IMPL (Task 6, and again at its close): dirs 127 = imports 127, both `comm` directions EMPTY, split 103 `driver/` + 24 `inputs/` (`0125` is a `driver/` fixture); the rename NC fired BOTH directions and the deletion NC fired only `comm -13` under the `(imports, dirs)` order, exactly as the phase-99 IMPL measured.** **The GATE was sound throughout; only the NUMBERS rot, and they rot once per fixture-adding row.** **NC the extractor** by RENAME (in a SCRATCH COPY) — **a RENAME fires BOTH `comm` directions** — and by DELETION. ⚠️ **A COUNT-ONLY CHECK IS VACUOUS: the import count stays constant under a rename.** 🔴 **A DELETION FIRES ONLY ONE DIRECTION, AND WHICH ONE IS AN ARTIFACT OF ARGUMENT ORDER** — this note said `comm -23`; the phase-99 IMPL measured `comm -13` under the `(imports, dirs)` argument order that IMPL ran (refutation 3 of that stage). **Name the argument order with the direction.**
3f. ⚠️ **"AT THE RETURN" IS NOT A PLACEMENT.** Anchor installs on the ENTRY of the path that must be covered and PROVE the install with an arm that reddens without it.
3g. ⚠️ **A "CHEAP ASSERTION GAP" CAN BE A BEHAVIOUR DIVERGENCE WEARING A DISGUISE.** Measure the reference for THAT ARM before asserting any cross-side stat.
3i. ⚠️ **A LOOSE `--grep 'phase NN.*STAGE'` MATCHES THE WRONG STAGE.** **Anchor stage-commit locates on `^phase NN (slug) STAGE:`** — ⚠️ **and see method note 57: the anchored form is BLIND where the author omitted the stage word.**
3j. 🔴 **REFUTED AT THE PHASE-97 PLAN — `grep` IS THE *SAME* WRAPPER IN BOTH SHELLS.** This note used to say the subagent shell carries `/usr/bin/grep` (GNU 3.11) while the controller carries a **ugrep** function. **An agent ran `type grep` in its own shell and found the same function** (`exec -a ugrep "$_cc_bin" -G --ignore-files --hidden -I --exclude-dir=.git …`). ugrep honours `.gitignore`, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway** — so **a recursive bare `grep` is BLIND to it in EVERY shell in this environment.** Use `/usr/bin/grep`, `command grep -rl`, `git grep`, or a direct path, **in the controller AND in every brief you issue**. ⚠️ **`command grep` DOES NOT SURVIVE `xargs`.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files). ⚠️ **ugrep ALSO REJECTS SOME COMPLEX `grep -oE` PATTERNS with `exceeds complexity limits` where GNU accepts them.** **NAME THE BINARY, NOT THE SHELL.**
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Live: TWO SDS dial-budget flakes plus `TestSDSEndToEnd_FetchFailure_BootFailsClosed` (⚠️ **RECURRED at the phase-99 IMPL: 1 of 2 full gate-(b) sweeps**) · the driver-owned receiver port race · `internal/httpclient` zero-value · the two 84.2-era flakes · the REFERENCE h2spec section-8 flip · `TestOutlierDetector_ConcurrentEjectExactlyOnce` · `0061-lb-ring-hash`'s σ-margin · `TestP83_StopPauseTimer_IsAuthoritativeAgainstAnEnteredClosure` (wasm, 1/400) · `internal/filter/hcm/h2 TestFramer_ReaderGoroutineDoesNotLeak`. ⚠️ **`TestServerConn_TinyWindowDelivery` IS NOT A FLAKE** — a recurrence is a REGRESSION of row 91. ⚠️ **A GREEN RERUN CLEARS NOTHING.**
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only). ⚠️ **AT THE PHASE-97 SPEC A SIBLING `curl-world` SESSION STILL OWNED SEVENTEEN CONTAINERS**, and a foreign `envoyproxy/envoy:v1.33.0` container appeared and exited inside the measurement window without being touched. ⚠️ **A `reaper_*` testcontainers Ryuk container is created BY THE DIFFERENTIAL ITSELF and is REUSED; leave it alone.**
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`. ⚠️ **`INNER_EXIT` DOES NOT EXIST in this repo.**
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL, THEN DRIVE THE ARM.** **`gofmt -l` never exits non-zero — gate on OUTPUT.** ⚠️ **go test WITHOUT `-v` PRINTS ZERO `=== RUN`** — `RUN=0` beside `RC=0` is a VACUOUS GREEN. ⚠️ **A `-run` selector matching nothing prints `[no tests to run]` and EXITS 0.** ⚠️ **On `-v` output, unanchored `grep -c 'FAIL'` reads nonzero ON A FULLY GREEN TREE** — use `grep -cE '^(FAIL|--- FAIL)|^ *--- FAIL'`.
7b. ⚠️ **`grep -c` ON ZERO MATCHES PRINTS `0` AND EXITS 1** — capture, NEVER chain with `&&`, NEVER `$(… || echo 0)`.
7c. ⚠️ **A GREEN TEST RUN IS NOT EVIDENCE A SITE IS EXERCISED** — prove it with a **`panic()` REACHABILITY CONTROL**. **There is NO `recover()` in non-test `internal/tls` or `internal/listener`, so a panic aborts the binary — gate on `grep -cE '^panic:|DATA RACE|SIGSEGV'`.** ⚠️⚠️ **AND A FAIL-FAST CONTROL MEASURES ONE SITE PER RUN — SEE NOTE 55.**
7d. ⚠️ **NC EVERY NEW TEST, AND NC YOUR OWN FIXES** — NEUTRALISE so the package still compiles; check the NC is EXECUTABLE. ⚠️⚠️ **AND CHECK THAT YOUR NC *CAN* FIRE AT ALL.** **Before running an NC, ask what mechanism would carry its mutation to a failure — if you cannot name one, the control is vacuous.**
7e. ⚠️ **A UNIT TABLE OVER A SHARED ASSERTION IS NOT A GATE ON PRODUCTION BEHAVIOUR** — a shared `t.Helper()` body collapses every property onto ONE line, so **make each message name its property**.
7f. ⚠️ **A NC THAT LEAVES YOUR NEGATIVE CONTROL GREEN IS NOT EVIDENCE THAT CONTROL DOES ANY WORK.**
7g. ⚠️ **ASSERT *WHICH* ERROR FIRED, AND ALSO WHICH DID NOT.** ⚠️ **KEEP TABLE ROWS SINGLE-CAUSE.** ⚠️ **PREFER AN EXACT EQUALITY TO A FLOOR.**
7h. ⚠️ **A PROBE CLIENT CAN WITHHOLD THE VERY THING THE ARM EXISTS TO TEST.** ⚠️ **`openssl s_client` answers "did the handshake complete", NOT "was the connection served"** — drive a real request AFTER the handshake. ⚠️⚠️ **AND A PROBE CLIENT CAN OMIT AN INPUT AND MAKE A WHOLE ARM VACUOUS**: the `0104` driver does not set `ServerName`, so every `server_names` arm at the phase-97 SPEC would have been vacuous had the client not set SNI explicitly. **Name the inputs your probe sends, not only the ones it asserts.**
7i. ⚠️ **A GATE THAT READS 0 HAS NOT BEEN SHOWN TO WORK.** Run every silent gate over an input KNOWN to trip it.
7j. ⚠️ **A PIN CAN FAIL AGAINST *CORRECT* CODE.** **Before writing an expectation map, MEASURE the leaf set a completing handshake actually books.**
7k. ⚠️ **ASSERT THE POINTERS, NOT ONLY THE EFFECT** ([[reference_nil_stats_counter_inc_crashes_goroutine]]).
8. ⚠️ **`pgrep -f` / `pkill -f` MATCHES YOUR OWN SHELL AND KILLS THE TOOL CALL (exit 144)**. **Kill ONLY PIDs you captured yourself at launch.** ⚠️ **`pgrep -la a b` ERRORS** — one pattern per call.
9. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-*`, `wt-*` or stage worktree should outlive its stage close** — expect only `master` (verify with `git worktree list`) — 🔴 **AND IT WAS FALSE AT THE phase-99 BRAINSTORM's START: `wt-phase-98-impl` had outlived the phase-98 close** (clean, nothing unmerged; removed, branch kept). **A stage close must `git worktree remove` its own worktree.** ✅ **It was TRUE at the phase-100 BRAINSTORM's start** (`git worktree list` → master only). ⚠️⚠️ **THAT CLAIM IS ABOUT WORKTREES AND ONLY WORKTREES — DO NOT AUDIT IT WITH `git branch`.** At the phase-97 SPEC's pushed tip `git worktree list` reads exactly one entry, the canonical root on `master`, while `git branch` reads **173** local branches of which **83** are NOT merged into `master`, including spent re-evaluation branches sitting **0 commits ahead**. **The branch graveyard is deliberate history and is NOT a stage-close defect; scoring note 9 on branches manufactures a false alarm.**
10. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** Re-derived at the phase-100 IMPL close (Task 17), the LAST stage that RAN all six (a BRAINSTORM runs none, by scope — these are carried, not re-measured): (a) differential **127 PASS / 0 FAIL / 0 SKIP** in `477.179s`, the fixture set asserted BY NAME in BOTH `comm` directions, never by the exit code; (b) the non-Docker sweep gated on `PIPESTATUS[0]` + a SET RECONCILIATION — **242 packages** (241 + `0125/driver`, MEASURED) — ONE sweep, **RC=0** (125 `ok` + 0 FAIL + 117 `[no test files]`), no listed flake fired, ⚠️ **which clears no flake** (the phase-99 IMPL's sweep 1 was RED on the SDS dial-budget flake and its sweep 2 green — both kept on its record, the model for any future RED sweep); (c) h2spec `95 tests, 94 passed, 1 skipped, 0 failed`, the skip being 6.9.2/2 — ⚠️ **a NON-verbose run prints NO summary line at all, only `ok … Ns`**; (d) fuzzers **56 targets / 48 FILES** — ⚠️ **48 is FILES and 56 is TARGETS, do not conflate them**; (e) the ANCHORED panic gate `^panic:|DATA RACE|SIGSEGV`, **0** *and PROVEN LIVE* (Task 1), and `golangci-lint` RUNS as `GOTOOLCHAIN=go1.26.2 golangci-lint run ./...` (rc 0; a COMPILING planted control fired `errcheck`, `revive` and `ineffassign` — note 88); (f) no `REVIEW.md` — the ONE **STANDING DEPARTURE** left, **NONE of 93-100** carry one. ⚠️ **`-race` on the DIFFERENTIAL suite is VACUOUS** — the subject there is an unraced subprocess; run `-race` on the FULL `internal/listener` package instead (the phase-100 IMPL did, clean). **AN IMPL RUNS ALL SIX; A BRAINSTORM, SPEC AND PLAN RUN NONE, AND THAT IS SCOPE, NOT OMISSION. ⚠️⚠️ YOUR STAGE IS A BRAINSTORM AND RUNS NONE.**
11. ⚠️ **THE DIFFERENTIAL'S FAILURE MODE IS A SILENT PASS.** `-count=1` IS NOT OPTIONAL. **ASSERT THE FIXTURE SET BY NAME, BOTH DIRECTIONS.** The full suite takes ~400s.
11b. ⚠️ **`go test ./...` DRIVES DOCKER — AND THERE ARE TWO DOCKER DRIVERS.** Exclude BOTH: `go list ./... | grep -vE '/test/differential$|/test/conformance/h2spec$'`.
12. ⚠️ **`-p` PUBLISHING WORKS; `--network host` IS WHAT DOES NOT.** For a UDP/QUIC listener publish UDP (`-p <port>:<port>/udp`) AND the admin TCP port. ⚠️ **`STRICT_DNS` + `host.docker.internal` + `--add-host=host.docker.internal:host-gateway` IS REQUIRED for a container-to-host cluster HERE.** ⚠️ **THE REFERENCE CONTAINER CANNOT READ `filename:` CERT PATHS** — use `inline_string:` — ⚠️ **AND INDENT THE PEM ONE LEVEL DEEPER THAN `inline_string:`**. ⚠️ **A PROBE BACKEND MUST BIND `0.0.0.0`, NOT `127.0.0.1`.** ⚠️ **Keep PEM substitutions OUT of YAML comments.** ⚠️ **envoy-go REJECTS `match.headers`**, **refuses `codec_type: HTTP2` without TLS unless `--allow-h2c`**, **boots with `-c`, NOT `--config-path`**, **`access_log[].log_format` is BOOT-REJECTED**, **`TLSv1_0`/`TLSv1_1` in `tls_params` BOOT-REJECT**. ⚠️ **AN OMITTED `clusters` KEY BOOT-REJECTS envoy-go** — give a probe subject a placeholder STATIC cluster, and `BackendCount()` must be **≥ 1**. ⚠️ **`ENVOY_TARGET.md` is at `docs/envoy-go/ENVOY_TARGET.md`, NOT the repo root.** ⚠️ **The pinned reference is `envoyproxy/envoy:contrib-v1.37.2`, digest `7edd5b0fd763…` — PIN BY DIGEST, verify against `ENVOY_TARGET.md:3-4`** (RE-VERIFIED IN USE at the phase-97 SPEC). ⚠️ **`timeout` rc=124 is shared by a HEALTHY server and a HUNG boot — read the OUTPUT, never the code.** ⚠️ **AND envoy-go's validate mode is `-mode validate`, the reference's is `--mode validate`.** ⚠️ **A `direct_response` status of `111` came back as `200` on the H3 path while `222` came back as `222` — CHOOSE NON-1xx SENTINEL STATUSES.**
13. ⚠️ **A RAW-FRAMER PEER MUST HOLD ONE `hpack.Decoder` PER CONNECTION.**
14. ⚠️ **`bash /dev/tcp` + `timeout cat <&3` INSIDE COMMAND SUBSTITUTION RETURNS SILENTLY EMPTY** — use Go for socket probes.
15. ⚠️ **GO DOES NOT FLAG UNUSED CONSTANTS.** Post-removal cleanup is grep-driven.
16. ⚠️ **`golangci-lint`'s misspell runs in locale US.** **Sweep British spellings in `.go` comments BEFORE the gate.** Markdown prose may use them freely.
17. ⚠️ **A GREEN RUN OF A CONTENTION-SENSITIVE TEST PROVES NOTHING.**
18. ⚠️ **AN ABSENCE CLAIM NEEDS A POSITIVE CONTROL AND A DISCRIMINATING NEGATIVE CONTROL.**
19. ⚠️ **DERIVE next-free ADR FROM THE TAIL, NEVER FROM THE HEADING COUNT.** The id space is SPARSE (exactly one gap, `0209`). The correct derivation is `grep -oE '^## ADR-[0-9]+' … | tail -1` ⇒ `## ADR-0319`, next-free **ADR-0320**, confirmed by `grep -c '^## ADR-0320'` ⇒ **0**. ⚠️ **AND THE HEADING REGEX ITSELF IS HOLED**: `^## ADR-[0-9]+[:—]` misses `## ADR-0127 v2`, which carries a version suffix between the id and the colon. ⚠️ **Re-derive at your tip rather than quoting these.**
20. ⚠️ **`reference_measured_prototype_is_a_lower_bound` HAS NOW FIRED TWENTY-ONE CONSECUTIVE ROWS — ✅ **AND AT THE phase-100 IMPL IT DID NOT FIRE ON ANYTHING THE PLAN HAD BUILT: the appendices applied byte-identically, 470 RUN / six named tip REDs / `0125` +836 / PB1 `15 0` + `14 6` all reproduced EXACTLY (note 89 held). It fired only on what the PLAN did NOT build — its DOC estimates (`DECISIONS.md` `~+100 / 0` measured `129 1`, then `136 1`; `BEHAVIOR_CONTRACT.md` 5999 measured 6000) — while `manager.go`'s `25 16` -> `28 19` was a review-driven comment fix, not an estimate error. AND AT THE phase-100 PLAN IT FIRED IN THE OVERSTATEMENT DIRECTION A SECOND TIME: the SPEC's fixture floor `≥ ~1360` (borrowed from `0123`'s +1361, a DOCUMENTATION-DENSITY figure) met a complete `0125` at **+836**, while the production edit it did price (`15 0` + `14 6`) reproduced EXACTLY. A floor borrowed from a sibling's SIZE is not a floor for a different fixture's SHAPE. (row 100's `23 6` floor OMITTED the `0s` fold, the help text, the real-inspector unit arms and the abort-test repair — and at the phase-100 SPEC it proved to be the WRONG FILE SET too: the chosen PB1 is `pipeline.go 15 0` + `manager.go 14 6`, `SPEC.md` §0.8) (at the phase-99 SPEC the `22 0` floor was not merely low but the WRONG SHAPE — P2 is `26 23`), AND AT THE PHASE-98 PLAN IT FIRED IN THE OTHER DIRECTION TOO: an inherited FLOOR can be an OVERSTATEMENT.** The `1359`/`1393` fixture floors this file quotes are `0120`/`0121` **dir-only** figures, both **PKI-inflated**; the NEWEST fixture, `0122`, is **946**. **Re-derive a floor by SHAPE, not by recency — and check what the quoted number MEASURES.** Row 97's floor moved **12x** BRAINSTORM -> SPEC on a WRONG shape; **row 98's `+1/-6` became `+4/-9` on a WIDER shape** because the BRAINSTORM's positive control never drove the subject. **ENUMERATE BY GREPPING THE LITERAL SYMBOL OUTWARD, CASE-INSENSITIVELY, PATHSPEC-SCOPED, AND BY COMPILING — and treat every cost estimate as a FLOOR.**
21. ⚠️ **A MEASUREMENT THAT GOVERNS A DEFERRAL DECISION MUST LAND IN A GOVERNING DOCUMENT.** `SPEC.md` §2 is carried into ADR-0319 §Context ¶2 for exactly this reason.
22. ⚠️ **A STAGE'S FILE SCOPE IS ITSELF MEASURABLE, AND THE GOVERNING DOCUMENT CAN BE WRONG ABOUT IT — TWICE IN A ROW.** `git show --numstat` on the precedent stages settles it, and **MEASURE MORE THAN ONE**. A SPEC's scope is **five** files, measured across FOUR (`307f2e3d` `9ed6a620` `da6ea191` `331c3524`) — ⚠️ **the four agree on PATHS, not on the `DECISIONS.md` line count (all `+30`; the phase-98 SPEC `+22`)**; a PLAN's is **four**, measured across **FIVE** precedents (`e69f1c58` `90010c4c` `db539e7d` `f647dd72` `0eab42e3`), each reading `STATE_HISTORY.md` `2 0`. ⚠️ **AN IMPL'S IS THE BIG ONE — the IMPL that owns it MEASURES it against `c7bd2880` (phase 96) and `0a985a35` (phase 94)**, which touch **20** and **28** paths respectively (`git show --numstat --format= <sha> | wc -l`, re-measured at the phase-99 IMPL close — this note said **29** for phase 94, which that command does not reproduce), and the phase-99 IMPL `d5bc9153` reads **20**; the phase-100 IMPL reads **20** at its close (`git diff --numstat master | wc -l`). 🔴 **COMPARE LIKE WITH LIKE: those are SQUASHED stage commits that already carry `STATE.md`, `STATE_HISTORY.md` and `next-prompt.txt`; the phase-100 IMPL's Task 17 set its pre-close 17 against them and called it narrower (note 98).** A BRAINSTORM's scope is **five** paths (`674786bd`, `c9b9ecfc`). **Wording is not scope.**
23. ⚠️ **`git diff --stat` IS A SUM, NOT ADDITIONS.** Cost figures come from `--numstat`.
24. ⚠️⚠️ **AN UNESCAPED `|` IN A ROADMAP ROW PASSES CHECK (1) AND SILENTLY BREAKS THE FIELD COUNT.** ⚠️⚠️ **IT IS LIVE AT YOUR STAGE: a BRAINSTORM ADDS a row.** Count fields (want **8**) under BOTH the naive and the escape-aware form BEFORE installing, and **assert the new cell spells NEITHER sentinel match phrase** — check (2) matches the WHOLE FILE, not only the six windows. Rows 98, 99 and 100 were gated that way before insertion and read **8 under both**; 98, 99 and 100 still read 8 after their IMPLs rewrote the cell (100's also after its final-review numstat fix). **Reword a pipe AWAY rather than escaping it.** ⚠️ **THIS FIRED AGAINST THE PHASE-96 IMPL'S OWN AUTHOR**, who wrote a Go `||` into row 96's narrative. **It is dormant at the SPEC and the PLAN, which edit no row, and live at any BRAINSTORM (which adds one) or IMPL (which flips one).**
25. ⚠️ **THE EVICTION CHECK IS A *PAIR*, AND THE BARE FORMS DO NOT ANSWER IT.** Both bare forms read the same value on `STATE.md` and both are invariant under which entry is evicted. Only the LABEL-BOUND form discriminates, on BOTH files: the evictee's own label goes `STATE.md` **1 -> 0** and `STATE_HISTORY.md` **0 -> 1**. **VERIFIED IN BOTH DIRECTIONS at this close**, with a fabricated-label NC reading 0 in BOTH files and a positive control on a sibling label that IS present in the archive. ⚠️ **The sibling positive control must name an ARCHIVED label, not a §Recent one.**
26. ⚠️⚠️ **A DIRECT DATE READ SOMETIMES PICKS THE EVICTEE AND SOMETIMES CANNOT — READ THE SHAPE EVERY TIME.** 🔴 **AND THIS NOTE WAS ITSELF WRONG AT THE PHASE-98 IMPL CLOSE, IN A WAY WORTH NAMING:** it predicted a **FOUR-WIDE** tie for that close because it restated the PLAN close's **PRE**-roll list as though it were that close's **POST**-roll list, silently dropping the newest entry; `98/PLAN.md` Task 20 Step 2 inherited the same error, while this file's own closing bullet and `STATE.md`'s preamble said **three**. **Measured at the IMPL close: one `09-16`, one `09-12`, THREE `09-09` — a THREE-WIDE tie at the tail**, so the date narrowed the field to three and **LIST POSITION picked the tail**. ⚠️ **A PROJECTED HISTOGRAM IS NOT A MEASURED ONE, AND A PROJECTION THAT FORGETS THE ENTRY YOUR OWN CLOSE IS ABOUT TO PROMOTE IS WRONG BY EXACTLY ONE, EVERY TIME.** ⚠️ **The phase-99 BRAINSTORM measured that TWO-WIDE tie as predicted and broke it by list position; the phase-99 SPEC, PLAN and IMPL closes each measured a UNIQUE tail and picked by date alone; the phase-100 BRAINSTORM measured the projected TWO-WIDE `09-20` tie (FOUR `09-21`, TWO `09-20`, INCLUDING the promoted entry) and broke it by list position. The phase-100 SPEC measured the projected UNIQUE `09-20` (FIVE `09-21`, ONE `09-20`, INCLUDING the promoted entry) and picked by date alone. The phase-100 PLAN measured the projected FIVE-WIDE `09-21` tie (ONE `09-22`, FIVE `09-21`, INCLUDING the promoted entry) and broke it by LIST POSITION (evicting the phase-99 BRAINSTORM entry). The phase-100 IMPL measured the projected FOUR-WIDE `09-21` tie (TWO `09-22`, FOUR `09-21`, INCLUDING the promoted entry) and broke it by LIST POSITION (evicting the phase-99 SPEC entry). The post-roll §Recent reads TWO `09-22` and THREE `09-21`, and YOUR close promotes the phase-100 IMPL entry (`09-22`) on top of it — so YOUR pre-roll tail is PROJECTED to be a THREE-WIDE `09-21` tie that LIST POSITION breaks (the evictee would be the phase-99 PLAN entry). MEASURE IT; INHERIT NO SHAPE, INCLUDING THIS ONE.**
27. ⚠️ **A SYMBOL-SCOPED CLAIM IS NOT A PATH-SCOPED ONE.** **Enumerate the call sites and check each caller's own branch.** ⚠️ **AND ONE SYMBOL CAN RUN AT TWO MOMENTS**: `quicTLSConfig` is called at Start AND per connection, which is the whole reason phase 97's repair splits the two accessors instead of collapsing them.
28. ⚠️ **A `+0/+0` NEGATIVE-CONTROL ARM CAN BE SILENTLY DELETED WITH EVERY GATE STAYING GREEN.** **Diff the ARM ROSTER, not the counters.**
29. ⚠️ **A MULTI-INSERT LINE SHIFT IS BANDED, NOT A CONSTANT.** **Never hand a scalar shift for a multi-insert file — re-locate every cite by LITERAL TEXT (`grep -nF -- "<exact line>" <path>`).**
30. ⚠️ **A `go test` SELECTOR NAMING A PACKAGE THAT DOES NOT EXIST PRINTS `FAIL … [setup failed]` AND EXITS 1.** **Confirm a selector resolves (`go list ./path/...`) before believing a FAIL.** This is the mirror of the `-run` footgun, which fails the OTHER way (no match ⇒ EXIT 0).
31. ⚠️ **RECONCILE A CORRECTED CLAIM ACROSS ITS WHOLE OCCURRENCE SET, CASE-INSENSITIVELY** — and state which hits you are deliberately leaving and why.
32. ⚠️ **A ROW CAN SHIP A FALSE FIGURE IN THE COMMIT THAT WRITES IT, AND A "CORRECTION" IS NOT A MEASUREMENT.** **NAME THE MEASURE (bytes or chars) OR THE FIGURE IS MEANINGLESS**, and **re-derive any count you quote by running the command at YOUR publishing tip, in the SAME commit as the edit that moves it.** ⚠️ **The phase-97 SPEC's own `SPEC.md` line count was patched in AFTER the last section was appended and re-verified before committing.**
33. ⚠️ **A CHARACTER CLASS SILENTLY DROPS WHAT IT DOES NOT SPELL.** `grep -cE '^\s+[a-z0-9./-]+ v[0-9]'` on `go.mod` reads **62**; the count is **67**. **RE-CONFIRMED AT THE PHASE-97 SPEC — and see note 56 for how an agent "refuted" it.** **Prefer a structural extractor (`awk` over the `require` blocks) to a character class, and diff the two when they disagree.**
34. ⚠️ **TWO INDEPENDENT AGREEING MEASUREMENTS DO NOT MAKE A CLAIM GENERAL.** **Enumerate the class, THEN measure.**
35. ⚠️ **A GUARD'S NAME IS NOT ITS COVERAGE — AND A DEAD ASSERTION CAN BE INVERTED AS WELL AS ABSENT.** **Read the arms, then read what each one would assert if it ran.**
36. ⚠️ **A VARIABLE REUSED FOR A SECOND PURPOSE OUTLIVES THE INVARIANT THAT MADE IT SAFE.** **When you gate a new feature on an existing boolean, prove that boolean still means what the new use needs, and NAME THE ADR THAT COULD REPEAL IT.**
37. ⚠️ **A FIX SHAPE CAN ENCODE A PARITY ANSWER NOBODY HAS MEASURED.** ⚠️⚠️ **THIS HAS NOW FIRED THREE ROWS RUNNING, AND AT PHASE 97 IT KILLED THE PREDECESSOR'S PROTOTYPE OUTRIGHT.** **Measure the reference, then choose — and diff size is never the tiebreaker.**
38. ⚠️ **AN OCCURRENCE SET CAN BE NORMATIVE, NOT MERELY COMMENTARY.** **Sweep `docs/` with the same case-insensitive breadth as the code, resolve every markdown hit to its enclosing ADR by BACKWARD heading search, and say which hits you are deliberately leaving and why.**
39. ⚠️ **A BARE-TOKEN GREP'S FALSE POSITIVES ARE CONFIG-DEPENDENT, SO A KNOWN WITNESS IS NOT A GATE.** **Anchor and NC the anchored form against the arm that must read zero.**
40. ⚠️ **A COUNT WITHOUT ITS MATCHER IS MEANINGLESS, AND TWO CORRECT COUNTS CAN DISAGREE.** **Name the matcher AND the moment beside every figure.**
41. ⚠️ **AN ILLUSTRATION IN §CONSEQUENCES IS NOT THE LIMIT OF A §DECISION.** **Read the decision, not the example.**
42. ⚠️ **A SECTION HEADED *"RE-DERIVED AT THIS STAGE'S OWN TIP"* IS FALSE IF IT NAMES A COMMIT THAT IS NOT THE ONE IT SHIPS ON** ([[reference_row_can_ship_false_figure_at_birth]]).
43. ⚠️ **AN ENUMERATION'S EXCLUSION RULE CAN HAVE A HOLE ITS AUTHOR DID NOT SEE.** **State the exclusion as a PREDICATE and then test every live file against it.**
44. 🔴 **CORRECTED AT THE PHASE-97 PLAN — THE `[]struct{` ZERO IS A *SPACE*, NOT A FOLLOWING-LINE BRACE.** This note used to say `manager_test.go`'s six tables read zero under `[]struct{` *"because their brace sits on the FOLLOWING line"*, and the phase-97 SPEC's §14 inherited that and prescribed a following-line matcher. **BOTH ARE WRONG: `\[\]struct[ ]*$` reads 0 as well.** gofmt writes **`[]struct {` with a SPACE**. The matchers that are correct by construction are **`\[\]struct[ ]*\{`** and the bare **`\[\]struct`**, each reading **6** at `:1092 :1484 :4777 :5444 :5691 :6102` with loops at `:1109 :1559 :4792 :5456 :5752 :6157`, all binding `tc` (the `tt` idiom appears nowhere in the package). ⚠️ **A MATCHER'S VOCABULARY CAN BE WRONG IN A DIRECTION ITS AUTHOR DID NOT PREDICT — and a PRESCRIPTION inherits the error.** Also measured: **both QUIC test files are table-FREE under every form.**
45. ⚠️ **A REGISTRATION-TIME SYMBOL IS NOT A CONSTRUCTION-TIME SYMBOL.** **Enumerate the call sites and ask WHEN each runs, not only WHERE it is.**
46. ⚠️ **A CROSS-SIDE PIN ON A NAME ONE SIDE NEVER EMITS IS SILENTLY VACUOUS, NOT RED.** **Before pinning a stat cross-side, prove the NAME exists on both sides.** ⚠️ **RE-CONFIRMED AT PHASE 97: for a two-chain QUIC listener the reference emits ~156 lines across the two HCM scopes and a 12-line listener-qualified scope; envoy-go emits TEN lines and NO listener-qualified HCM scope at all. PIN A NAMED SUBSET.** ⚠️ **And the listener ADDRESS TOKEN differs cross-side even where both emit a listener scope.**
47. ⚠️⚠️ **A LIFECYCLE STATE DEFINED BY WORK REMAINING IS RE-ENTRANT, AND STATE 3 IS THE ONE WHERE IT BITES.** **A 20-TO-24-TASK SPINE WILL NOT FINISH IN ONE SESSION.** The discriminator is `PROGRESS.md` and the ROADMAP row, **not the session boundary** — a session that runs out of budget mid-spine leaves the state at **3**, says which task it reached, and does NOT flip the row. ⚠️ **The phase-99 AND phase-100 IMPLs each finished a 19-task spine in one session — that is not a precedent that the next IMPL will.** Dormant at a BRAINSTORM, whose discriminator is the EXISTENCE of `BRAINSTORM.md` (note 47a). ⚠️ **Evaluate the context budget BEFORE starting a task, not after** (`checking-context-budget`).
47a. ⚠️ **A BRAINSTORM, A SPEC AND A PLAN ARE NOT RE-ENTRANT THE SAME WAY** — their discriminator is the EXISTENCE of `BRAINSTORM.md` / `SPEC.md` / `PLAN.md`, not work remaining. Do not leave a partial one.
48. ⚠️ **PARALLELISE ACROSS DISJOINT FILE SETS, NOT INSIDE ONE**, with **Docker serialized to exactly ONE agent**. ⚠️ **AND KEEP A LOAD-BEARING ORDER STRICTLY SERIAL inside its own worktree.**
49. ⚠️⚠️ **AN AGENT'S ABSENCE CLAIM IS ONLY AS GOOD AS ITS MATCHER'S VOCABULARY.** **When an agent reports an absence, ask what it SPELLED before believing what it did not find** — and when two parties disagree, FIND THE VARIABLE rather than picking a winner.
50. ⚠️ **A GREEN PACKAGE UNDER A BEHAVIOUR-INVERTING PATCH IS A COVERAGE FINDING, NOT A PASS.** `go test ./internal/listener/...` is rc=0 both before and after a patch that reverses which filter chain serves every QUIC request — **CONFIRMED FIRST-HAND at the phase-97 SPEC with a `+5 / -0` inverting patch.** **Run your fix AND your un-fix through the same suite and compare; identical greens mean the suite is BLIND.**
51. ⚠️ **AN UNRELATED BOOT REJECT MASKS THE SITE UNDER TEST.** **Prove your reproduction REACHED the site — by the message, never by the exit code.**
52. ⚠️ **SPLIT A BANKED VERDICT BEFORE PRICING IT.**
53. ⚠️ **A COST ADJECTIVE ROTS IN THE BLAST RADIUS, NOT ONLY IN THE COUNT.** **Before pricing a repair, grep for the guard whose trigger it removes.**
54. ⚠️ **CHOOSE NON-1xx SENTINEL STATUSES IN PROBES.** See note 12.
55. ⚠️⚠️ **A SINGLE POSITIVE ARM CANNOT DISTINGUISH "THE INPUT WAS STAMPED AND MATCHED" FROM "THE DIMENSION IS UNENFORCED" — PAIR EVERY DIMENSION WITH A MATCHED NEGATIVE ON A BYTE-IDENTICAL SUBJECT.** At the phase-97 SPEC an arm showing the reference serving a `transport_protocol: "quic"` chain was ambiguous until the `"tls"` arm fell to the default chain; the same pairing was needed on the ALPN and SNI axes. ⚠️ **AND THE SAME LOGIC APPLIES TO A FAIL-FAST REACHABILITY CONTROL: with a `panic()` in TWO sites the run aborts on the first and says NOTHING about the second. ISOLATE — one site per run.**
56. ⚠️⚠️ **AN AGENT CAN "REFUTE" AN INHERITED FIGURE BY RUNNING A DIFFERENT COMMAND THAN THE ONE THE CLAIM NAMES.** At the phase-97 SPEC an agent reported method note 33's `62` non-reproducible; it had run `^\t[a-z]` where the note names `^\s+[a-z0-9./-]+ v[0-9]`. Both counts were correct and the note was right. **ASK WHAT IT RAN, NOT ONLY WHAT IT FOUND** — the sibling of note 49, and now the second consecutive row on which a stage's own agent produced the refutation.
57. ⚠️ **THE ANCHORED STAGE-COMMIT FORM IS BLIND WHERE AN AUTHOR OMITTED THE STAGE WORD.** Phase 95's IMPL landed as `0f3b98b3` with a subject carrying no stage token at all, so `git log --grep '^phase 95 (.*) IMPL'` reads **ZERO**. **Run the anchored AND the loose form and RECONCILE THE SETS**, and **when you land a stage, put both the FULL SLUG and the STAGE WORD in its subject.**
58. ⚠️ **A ROW THAT AGREES FOR A REASON THE MECHANISM CANNOT SUPPLY IS A FALSE AGREEMENT.** envoy-go answers *"default chain"* on every QUIC arm, so it coincidentally matches the reference wherever the reference also picks the default — while evaluating nothing. **Before reading agreement as evidence, ask whether the subject could have answered anything else.**
59. ⚠️ **ADR-0044 DOES NOT CONTAIN THE ADR-DRAFTING DISCIPLINE THIS PROJECT CITES IT FOR** — CONFIRMED by reading it in full at the phase-97 SPEC; it is about the BEHAVIOR_CONTRACT HTTP/1.1 subsection. The real discipline is the `> **STATUS: PROPOSED` blockquote plus a **RETAINED** italic footer (the ADR-0294-through-0321 shared block form), which the IMPL appends **after**, never replaces, with **no `**Status:**` line, no renumber and no `---`**. **No ADR documents it.** Recorded, not repaired; ⚠️ **quote NO count of the misattributing lines — any figure would be falsified by its own landing.**
60. ⚠️ **THERE ARE FOUR FIXTURE REGISTRATION GATES, NOT THREE, AND EVERY ONE OF THEM SKIPS RATHER THAN FAILS.** (1) `fixture.RegisterFixture` in the driver `init()`; (2) the blank import in `test/differential/runner_test.go`; (3) **byte-identity between the directory name and the registered string**; (4) the `NNNN-` / `NNNNa-` directory-name shape `discoverFixtures` enumerates. Gates 1-3 converge on a `t.Skipf`; **gate 4 produces no subtest and therefore not even a skip line.** **Score them on the fixture-set set-difference, never on the exit code.**
61. ⚠️⚠️ **AN ARM THAT IS GREEN BEFORE THE FIX IS NOT NECESSARILY A TDD FAILURE — IT MAY BE THE FALSE-AGREEMENT CLASS SHOWING UP INSIDE YOUR OWN SUITE.** At phase 97 the subject answers *"default chain"* on EVERY QUIC arm, so **four of the ten new arms (b, e, g, i) are GREEN at the un-fixed tip, structurally**, because *default* happens to be the right answer there. **Their only falsifiability is an NC roster row, which must therefore be scored PER ARM, not per run.** ⚠️ **Before reading a pre-fix green as a bug in the arm, ask whether the subject could have answered anything else.**
62. ⚠️ **A GOVERNING DOCUMENT'S ROSTER CAN CONTRADICT ITSELF ACROSS TWO SECTIONS, AND THE INTERSECTION IS THE FINDING.** Phase 96 §0.13 found the byte-untouched roster and the edit roster meeting at `manager.go`; phase 97 found the byte-untouched roster meeting the **PROSE-RECONCILIATION** set at the same file. **Enumerate the occurrence set of every claim your repair falsifies, resolve each hit to its file, and SET-DIFFERENCE that against the byte-untouched roster BEFORE writing the task list.** ⚠️ **A COMMENT-ONLY EDIT IS A DEFENSIBLE RESOLUTION IF, AND ONLY IF, THE COMMENT-ONLY CONSTRAINT IS MECHANICALLY GATED** — and that gate must itself be run over an input known to trip it.
63. ⚠️ **A SENTENCE CAN BE FALSIFIED BY A REPAIR IN TWO INDEPENDENT HALVES, AND A HALF-REPAIR MINTS A NEW STALE CLAIM.** *"`quicTLSConfig()` returns `rt.defaultChain.tlsCfg` FIRST, before consulting `chainByName`"* is falsified BOTH by the new precedence AND by `chainByName` no longer being consulted at all. **Say which halves die and which survive; LEAD WITH WHAT SURVIVES.** ⚠️ **AND ADJUDICATE PAST-TENSE NARRATION SEPARATELY** — two of the five hits narrate a *previously fixed* hole and are untouched by the repair.
64. ⚠️ **NAME THE MEASURE OR THE FIGURE IS MEANINGLESS — AND "LINE LENGTH" HAS FOUR ANSWERS.** `ROADMAP.md:229` reads **47234** bytes INCLUDING its trailing newline, **47233** excluding it, **46753** characters under `LC_ALL=C.UTF-8 awk length()`, and **47233** under `LC_ALL=C awk`. **The router's inherited figure reconciles only as bytes-including-newline.** ⚠️ **AND A STALE CLAIM'S OCCURRENCE SET IS RARELY THE ONE LINE THAT WAS NAMED** — the swallowed-panic claim lives on **FOUR** ROADMAP lines (136, 139, 159, 229) across TWO different phrases, and only `:229` sits inside a sentinel window.
65. ⚠️ **A HARNESS LIMITATION CAN LIVE IN THE DISPATCH LAYER WHILE THE PLUMBING IS ALREADY GENERAL.** The differential's Docker publishing and address maps are N-capable for UDP; what collapses a two-UDP fixture is `runFixture`'s **transport-blind, single-valued address dispatch** at two sites plus a fixture-wide nullary `ReferenceListenerIsUDP` marker. ⚠️ **RULE OUT THE OBVIOUS NON-CAUSES EXPLICITLY** — publishing and readiness were both fine, and there is **no per-listener readiness probe of any transport** in this harness at all.
66. ⚠️ **COUNT YOUR OWN DOCUMENT'S STRUCTURE WITH A COMMAND, NOT WITH YOUR MEMORY OF WRITING IT.** The phase-97 PLAN's self-review first claimed *"the largest tasks carry nine sub-steps"*; `awk` over `^- \[ \] ` per `^### Task ` heading read **eleven** for two of them, past `BOOTSTRAP_PROMPT.md` §6.1's ~10 line. **The false figure was caught before publishing only because the command was run.** ⚠️ **Every threshold your own stage sits near must be MEASURED and STATED, including the ones you would rather round down.**
67. ⚠️⚠️ **AN INVENTED GUARD FORM DOES NOT REPORT A WRONG NUMBER — IT REPORTS A DISARMED GATE AND A FALSE ZERO.** At the phase-97 router roll BOTH house guards were re-spelled from memory instead of copied from this file. The archive triple read **264 / 0 / 268** where the house forms read **163 / 71 / 234** ⚠️ **(that parenthetical 71 was the reading AT THAT TIP and is HISTORICAL — for the live triple see THE ARCHIVE GUARD, and never copy one from here)** — a **parenthetical ZERO**, the same false-zero class `PLAN.md` §0.1 refuted in `manager_test.go`, one stage later and in a different file. Worse, `^\*\*Status:\*\* PROPOSED` over `DECISIONS.md` returned **exactly ONE hit, at `:14866`, resolving to `## ADR-0231`** — which reads as *“the house guard is DISARMED and the tail ADR is unguarded.”* **It is ARMED**: the house form is `^> \*\*STATUS: PROPOSED`, it hits `:19054`, and that resolves by backward heading search to `## ADR-0319`. **COPY THE GUARD COMMAND, NEVER PARAPHRASE IT — a guard you re-spell can report the ABSENCE of the thing it guards, and an absence reads as permission.**
68. ⚠️⚠️ **A ROLLER THAT LEAVES A SPENT STAGE'S IMPERATIVE STANDING HAS MINTED A STALE INSTRUCTION — AND IT IS WORSE THAN A STALE FIGURE, BECAUSE A READER OBEYS IT.** The phase-97 IMPL rolled this file's header, live figures and owed-work list, and **still left FOUR blocks addressed to a stage that no longer exists**: a `DO NOT RE-DERIVE` directive for a closed phase's SPEC/PLAN, a `WHAT THE IMPL MUST NOT RE-DERIVE` section, a six-gate note reading **123/123** and **237** after the row had moved both, and a `PROPOSED` guard section headed **ARMED, AND YOUR STAGE DISARMS IT** when the row had already disarmed it. **Caught only by a separate audit pass, three days later, reading the file as the NEXT session would.** ⚠️ **WHEN YOU ROLL THIS FILE, GREP IT FOR `YOUR STAGE`, FOR YOUR OWN STAGE WORD, AND FOR EVERY FIGURE YOUR ROW MOVED — then read the result as the next reader, who has no idea which sentences you meant to leave.** ⚠️ **A `+0` claim about this file is exactly as falsifiable as one about the tree.**
69. ⚠️⚠️ **A BANKED "UNMEASURED" CLAIM CAN BE FALSE IN THE DIRECTION THAT KILLS THE CANDIDATE — AND THE REPAIR IT IMPLIES CAN BE AFFIRMATIVELY WRONG.** This file banked, three times over two phases, that the reference's `*.` SNI suffix is single-label. Measured at the phase-98 BRAINSTORM: **it is multi-label, exactly like envoy-go's**, and implementing the banked repair would have INTRODUCED a divergence into code that already agreed. **MEASURE A BANKED CLAIM BEFORE ADOPTING IT, NOT AFTER CHARTERING IT** — and note that the same measurement surfaced a *different*, real divergence on the same axis (longest-suffix precedence) that no document had named. **A wrong banked framing can hide a real defect behind a fake one.**
70. ⚠️⚠️ **ONLY THE REVERSAL ARM SEPARATES "LONGEST WINS" FROM "FIRST WINS."** Two chains that both match one input tell you nothing about precedence until you reorder the array and change **nothing else**. At phase 98 the reference returned the same chain under both orders on byte-length-identical configs — that, plus a third arm proving the losing chain still serves other traffic, is what makes "longest-suffix, order-independent" a measurement rather than a guess. **A single ordering is one sample of a two-sample question.**
71. ⚠️⚠️ **A TEST CAN BE SATISFIED BY ITS OWN FIXTURE'S NAME.** `TestNewManager_MultiChain_TooManyCatchAlls_Errors` guards on `strings.Contains(err.Error(), "catch")` and names its listener `l_2catch`, so the error message it claims to pin is irrelevant: measured with the production check deleted, `contains catch: true` **from the listener name alone**, and the guard does not fire. **When a guard uses a substring predicate, enumerate everything else in scope that contains the substring** — the sibling of the vacuous-golden-roster class, one level lower.
72. ⚠️⚠️ **A CENSUS INSTRUMENT CAN COUNT ITSELF, AND THIS FILE HAS DONE IT.** The port-band census sentence claimed **two** `156xx` hits; re-run, it read **three**, because the sentence spells both numbers and is a hit in its own census. **The figure was false at its own publishing commit.** Same self-incrementing class as the archive's positive controls. **Whenever you record a count of a token in a file that will contain your recording, name what each hit IS and expect your own sentence to be one of them** — or record no number.
73. ⚠️ **`NOT CONSTRUCTIBLE` IS A REAL VERDICT AND IT NEEDS A NAMED MECHANISM, NOT A SWEEP.** Phase 98 proved `catchAllCount > 1` unreachable without the identical-spec check with a 91-pair sweep — but the sweep alone would have been an ABSENCE claim. What makes it a proof is naming the foreclosing mechanism: `chainSpecKey` short-circuits `if s.Empty { return "EMPTY" }`, so any two Empty specs collapse to one constant key unconditionally. **State the mechanism, then let the sweep corroborate it.**
74. ⚠️⚠️ **A PROBE THAT STOPS ONE STEP SHORT OF THE PROPERTY IS NOT A CONTROL.** The phase-98 BRAINSTORM's positive-control row drove a request on the reference and only BUILT a manager on the subject (`build OK`). The second divergence of the row sat in exactly that cell, and the fixture chartered on top of it would have been RED against its own repair. **Drive BOTH sides to the observable, every arm.**
75. ⚠️⚠️ **A CHARTERED GATE THAT CANNOT BE BUILT HONESTLY IS A SCOPE SIGNAL.** When the matched-positive arm a row needs fails against the row's own repair, the options are (a) widen the repair, or (b) bend the gate around the divergence (here: add `tls_inspector`). **(b) produces a GREEN fixture over a live divergence in the exact dimension it claims to cover — the worst outcome available.** Choose on the gate's honesty, not on size.
76. ⚠️ **AN NC ROW CAN BE WRONG ABOUT WHAT *DOESN'T* REDDEN AS EASILY AS ABOUT WHAT DOES — AND A VALUE-PRESERVING MUTATION IS INVISIBLE TO EVERY ARM WHOSE INPUT ALREADY HAS THAT VALUE.** The phase-98 SPEC's first draft had both errors in one row (§0.10). **For each row, trace the mutation through EVERY arm, including the ones listed under "must NOT redden".**

77. ⚠⚠ **A GOVERNING DOCUMENT'S OWN INSTRUCTION CAN BE THE DEFECT, AND OBEYING IT LITERALLY CAN DELETE WORK.** `98/SPEC.md` §14 item 1 told the PLAN to *"re-run §6's matchers — the union may grow."* Run as written, **all six are BLIND to `types.go:44-46`**, one of the code sites §6 itself lists as a mandated EDIT. The tables had been built partly by READING, so they are a **SUPERSET** of what the matchers produce. **Inherit the TABLES; use the matchers only to look for GROWTH. A re-derivation that LOSES a row is not a re-derivation, it is a DELETION** — and it arrives wearing the authority of the document that asked for it.
78. ⚠️ **A LINE-BASED MATCHER IS BLIND TO A CLAIM THAT SPANS A LINE BREAK.** `git grep` and `grep` match within one line. The `types.go` claim reads `… or "" if no listener` / `// filter inspected the connection.` across two lines, so `no listener.?filter` cannot span it — while a **positive control** (`git grep -nF -- 'no listener'` on the same file) proves the file is perfectly reachable. **When a matcher reports an absence, ask whether the thing it sought could fit on ONE LINE.**
79. ⚠️ **A MATCHER'S *ESCAPING* CAN BE WRONG WHERE ITS VOCABULARY IS RIGHT.** `§6`'s M1 is the literal `must be "tls"`; the Go source writes `must be \"tls\"`, so M1 reads **ZERO** against the very production file it is named for, and `manager.go:998` is reachable only through the escaped spelling. **Run BOTH spellings of any literal that crosses a language boundary.** Sibling of notes 49 and 56, one level lower: not what it SPELLED, but how it QUOTED.
80. ⚠⚠ **A COMMENT-ONLY EDIT CAN STILL BREAK FIFTEEN CITATIONS — "comment-only" AND "line-count-neutral" ARE TWO INDEPENDENT CONSTRAINTS, AND ONE GATE CANNOT CATCH BOTH.** `quic_test.go` carries **15** `chainmatch.go:<line>` cites, **all below line 32**, while the mandated comment edit sits at `:30-32`. A four-line comment replacing a three-line one **passes** a comment-only gate and staleys all fifteen in the commit that "reconciles" the prose. **Gate the SHAPE (`3	3`) as well as the KIND.**
81. ⚠⚠ **A NAME-PRESENCE PIN CAN BE VACUOUS ON *BOTH* SIDES.** Method note 46 warns that pinning a name one side never emits is silently vacuous. The phase-98 PLAN measured the **opposite** failure: both sides publish the zero-serving chain's `http.<prefix>.downstream_rq_total` **before any traffic at all** (a pre-traffic reference scrape read **168** matching lines with zero connections), because the per-chain HCM scope is created at **CONFIG** time, not at first request. **So "the name is present" is satisfied from BOOT and proves nothing. The only non-vacuous pin is the VALUE, and a value needs its MIRROR ARM.** ⚠️ **And `stats sinks emit only USED stats` does NOT transfer to the admin `/stats` or `/stats/prometheus` endpoints on either side.**
82. ⚠⚠ **A CONFIG KNOB CAN BE PARSED, RANGE-VALIDATED, DOCUMENTED AS HONORED — AND NEVER ENFORCED.** envoy-go parses `listener_filters_timeout`, rejects it outside `[1s, 60s]`, stores it, passes it to `Pipeline.Run`, and `Pipeline.Run` builds a real `context.WithTimeout` from it — **and it still never fires**, because the deadline is only consulted AFTER the blocking call returns and the blocking call has no deadline of its own. `BEHAVIOR_CONTRACT.md:4359` says it is *"honored"*. **A knob's plumbing is not its enforcement: follow the value to the thing it is supposed to INTERRUPT, and drive an arm that makes it interrupt.**
83. ⚠⚠ **A "TOOL CANNOT RUN" VERDICT CAN BE AN UNNAMED *VARIABLE*, AND THE OBVIOUS REPAIR FOR IT CAN BE WRONG.** For two stages this file recorded `golangci-lint` as unrunnable and gate (b) as red for "a toolchain reason", as TWO departures. At the phase-99 BRAINSTORM one variable — `GOTOOLCHAIN=go1.26.2` — turned BOTH green, and the lint zero was proven live by a planted-violation control. **Rebuilding the linter under the new toolchain (the obvious fix) still failed**, because the broken reader was vendored. **Before banking "cannot run", find the variable that separates the last green run from this one, and test the fix you would reach for FIRST.**
84. ⚠️ **A PROBE CLIENT CAN REWRITE THE INPUT BEFORE IT LEAVES THE MACHINE.** curl LOWERCASES the SNI it sends (captured with `openssl s_server -tlsextdebug`), so every "uppercase SNI" arm driven with curl is silently a lowercase arm. **Capture what the client SENT, on the wire, before believing an arm about it** — sibling of note 7h.
85. ⚠️ **READ THE TEST BEFORE PRESCRIBING ITS RE-POINT.** The phase-99 BRAINSTORM's first draft said `TestSelectChainAmbiguousReturnsError` must be re-pointed because the repair removes ambiguity; reading its arms showed an identical-`transport_protocol` shape the repair cannot reach. **A test's NAME is its subject; its INPUT is its coverage** (note 35).
86. ⚠️⚠️ **A PROTOTYPE THAT GETS EVERY ARM ITS AUTHOR RAN RIGHT CAN STILL BE THE WRONG SHAPE — THE ARM THAT KILLS IT IS THE ONE NOBODY RAN.** The phase-99 BRAINSTORM's `22 0` agreed with the reference on all fourteen of its rows; the mixed exact+wildcard arm it had itself named as INFERRED (§0.11) was the one that refuted it. **An arm a document lists as "inferred, not measured" is the next measurement, not a footnote.**
87. ⚠️ **A REPAIR CAN MOVE A CONFIG FROM "SERVED" TO "CLOSED" AND STILL BE THE PARITY DIRECTION — OR NEITHER.** P2 closes `O1`, which the tip served by an accident of whole-set rank; the reference REFUSES `O1`'s class at validate, so neither side matches. **Before calling a served->closed change a regression, measure what the REFERENCE does with that CONFIG — including whether it boots at all.**
88. ⚠️ **A NON-COMPILING PLANTED-VIOLATION CONTROL IS A MASKED CONTROL.** A lint control with an unused variable fires only `typecheck`, which hides every other linter; the control is valid only when the target linters each fire on a file that COMPILES (`SPEC.md` §10). Sibling of method note 51, in tooling.
89. ⚠️⚠️ **A PLAN THAT BUILDS AND RUNS WHAT IT EMBEDS HAS MEASURED FIGURES, NOT ESTIMATES — AND THE MEASUREMENT MOVED THREE OF THE SPEC's NUMBERS.** At the phase-99 PLAN the whole fixture, every unit arm and every NC mutant were built and run in throwaway worktrees before being written into the plan: the fixture floor fell from ~1300-1500 to ~941, the production edit rose from `26 23` to `42 39`, and two NC rows turned out wrong (one did not even COMPILE). **A spine whose code blocks were never run is a sketch; say which yours are.**
90. ⚠️ **A `dir/**/*.go` PATHSPEC DOES NOT MATCH FILES DIRECTLY IN `dir`.** `git ls-files 'internal/listener/**/*.go'` omits `internal/listener/manager.go` and `quic.go` — `**/` needs at least one directory level — so the phase-98 PLAN's `SetReadDeadline` absence count never searched the file that owns the accept path. It was right only because the true count is also zero. **Pathspec-scope with the DIRECTORY (`'internal/listener/'`) and prove the scope with `git ls-files` on the file you most need covered.** Sibling of notes 49 and 78.
91. ⚠️⚠️ **TWO CLOCKS SET TO ONE INSTANT RACE, AND A FILTER THAT MAPS A DEADLINE ERROR TO SUCCESS HIDES THE LOSER.** A socket read deadline and a `context.WithTimeout` at the same deadline: when the socket fires first, `tls_inspector` classifies the zero-byte error as `raw_buffer` and returns `Continue, nil`, `ctx.Err()` is still nil, and the pipeline reports success — **65 of 100 concurrent drops lost** at the phase-100 BRAINSTORM. **A single-connection arm passes the racy shape most of the time; the discriminating gate is the CONCURRENT arm, run more than once.**
92. ⚠️ **A FINDINGS FILE NO ROUTER READS IS NOT A REGISTER.** `REVIEW_FINDINGS.md` (commit `9f26f380`, 2026-07-07) recorded the row-100 defect seventy-five days before the phase-98 PLAN "discovered" it. **When you bank or pick, `git grep` the repo-root findings files as well as this file and `ROADMAP.md`.**
93. ⚠️⚠️ **A PRESCRIBED RE-POINT CAN TARGET A TEST THAT DOES NOT EXIST.** The phase-100 BRAINSTORM, and this file, told the SPEC to *"re-point the `TestParseListenerFiltersTimeout*` row that pins zero -> 15000"*. Reading the inputs: the `Default` test builds with a NIL field, and no test constructs a zero duration at all — the fold reddens NOTHING. Obeyed literally, the instruction edits a nil test to assert what the fold does not change, and the fold ships with no RED arm. **A re-point is a claim about an INPUT; read the input before accepting the claim** (sibling of notes 77 and 85).
94. ⚠️ **A PROTOTYPE'S HELPER PLACEMENT CAN MOVE A CITED LINE.** The phase-100 SPEC's first build of the chosen shape put a helper and two imports above `Pipeline.Run`, moving `pipeline.go:43` — a line cited by `manager.go`, `BEHAVIOR_CONTRACT.md:1969` and ADR-0296 as "the one production `context.WithTimeout`". The CLAIM stayed true; only the LINE would have rotted. **Before choosing where new code goes, `git grep -n '<file>:[0-9]'` for citations INTO the file, and make the layout part of the decision, with a gate.**
95. ⚠️⚠️ **A GATE WRITTEN AS A LITERAL IN A GOVERNING DOCUMENT CAN FAIL AGAINST CORRECT CODE.** `100/SPEC.md` §4.1 (e) said `git grep -n 'context.WithTimeout' -- internal/listener/ ':!*_test.go'` "reads exactly the one `pipeline.go:43` hit before and after"; it reads **THREE** at the tip AND under the fix — two are COMMENTS (`pipeline.go:21`, `manager.go:476`) citing the one code line. Obeyed literally, the gate is RED on the correct repair and invites a "fix" that deletes accurate prose. **Run a prescribed gate on the TIP before trusting it, and make the matcher's vocabulary match the claim (code vs prose)** — the built gate drops `//` lines (`100/PLAN.md` §0.1, §6). Sibling of notes 7j and 77.
96. ⚠️⚠️ **AN NC RESULT READ WITHOUT ASSERTING THE MUTANT IS IN PLACE CAN BE A FALSE READ OF EITHER POLARITY.** At the phase-100 PLAN the controller's first NC6 re-run read the OLD U2 as passing at **1.00 s** instead of the measured 3.00 s; a reverse-then-forward `patch` sequence had left `manager.go` UN-mutated. Every NC patch in `100/PLAN.md` Appendix H carries an `NC<n>` marker line: **`grep -c` the marker ≥ 1 BEFORE reading the result, and 0 after reversing it** (`100/PLAN.md` Task 12 Step 3; the phase-100 IMPL obeyed it for all eight NCs).
97. ⚠️⚠️ **A FIGURE AN EARLIER TASK WROTE TRUE CAN BE FALSIFIED BY A LATER COMMIT OF THE SAME STAGE.** At the phase-100 IMPL, Task 16 wrote row 100's cell with `manager.go` `25 / 16` — true at that commit; the review wave's comment-only fix (`a1de8a67`) then moved the file to `28 19`, and the cell stood false until the final review caught it. **Re-derive every figure a governing document quotes at the stage's LAST commit, not at the task that wrote it** — the sibling of `reference_row_can_ship_false_figure_at_birth`, one commit later.
98. ⚠️ **A SCOPE COMPARISON MUST COMPARE LIKE WITH LIKE.** The phase-100 IMPL's Task 17 measured its branch at **17** paths and called it narrower than `d5bc9153` / `c7bd2880` / `0a985a35` (**20 / 20 / 28**) — but those are SQUASHED stage commits that already carry the close-out's `STATE.md`, `STATE_HISTORY.md` and `next-prompt.txt`, and at the close the branch read **20**. **Name what each side of a comparison INCLUDES before calling one larger** (note 22).

## ⚠️ THE `PROPOSED` GUARD — **DISARMED AT THE phase-100 IMPL (ADR-0322 `ACCEPTED`, its Task 13); THE NEXT SPEC RE-ARMS IT WITH ADR-0323**

| form | now | where |
|---|---|---|
| `^> \*\*STATUS: PROPOSED` — **the house form** | ✅ **DISARMED — reads ZERO hits at this tip** | the tail ADR-0322's status line (`:19491`) reads `> **STATUS: ACCEPTED` — measured at the phase-100 IMPL close by READING IT DIRECTLY, **because an EMPTY result also means the matcher BROKE.** ⚠️ **The next SPEC drafts ADR-0323 as `PROPOSED`: after that the house form reads ONE hit, which must resolve by backward heading search to `## ADR-0323`.** |
| `^\*\*Status:\*\* PROPOSED` — **the ADR-0231 decoy** | unchanged, still hits | `:14866`, resolved BY BACKWARD HEADING SEARCH to `## ADR-0231` (`:14864`); **BYTE-UNTOUCHED across all four phase-98 stages, all four phase-99 stages and all four phase-100 stages** (md5 of `:14866` `929719b67c87…`, identical to `515856da`, re-measured at the phase-100 IMPL close) — ⚠️ **it is now the ONLY hit across both forms: reading it alone reads as an ARMED guard when the house form is DISARMED** |
| `^\*\*Status:\*\*.*PROPOSED` — **the middle ground** | a large number | ⚠️ **NEVER A GATE** |

⚠️⚠️ **THE POLARITY IS DISARMED, AND THE NEXT SPEC INVERTS IT TO ARMED.** Today only the decoy (ADR-0231) hits — so **a count of ONE across both forms reads the same whether it is the decoy or a stray house hit**; after the next SPEC a count of TWO reads the same whether the house hit is ADR-0323 or a stray. **Verify BY LINE AND BY ADR, never by the count, in EITHER direction.** ⚠️ **COPY THE GUARD COMMAND, NEVER PARAPHRASE IT** — at the phase-97 router roll both house guards were re-spelled from memory and reported a **parenthetical ZERO** and a DISARMED gate that was in fact ARMED.

⚠️ **VERIFY BY LINE AND BY ADR, NEVER BY THE COUNT ALONE** (`awk 'NR<=<HITLINE> && /^## ADR-/ {h=$0; n=NR} END {print n, h}'`). ⚠️ **NEVER gate on the UNANCHORED form.** ⚠️ **AND DO NOT QUOTE A COUNT FOR EITHER GATE FORM IN PROSE THE GREP MATCHES.** `DECISIONS.md` at the phase-100 IMPL close: **19644** (was 19509: ADR-0322's §Decision + §Consequences, the review-wave §Consequences line and the final-review §Decision fix, `136 1` in all), `^---$` **216**, `^## ADR-` **321**, bare `^## ` **329**, tail **ADR-0322** (`ACCEPTED`), next-free **ADR-0323** — all five MEASURED after the last edit. ⚠️ **A SPEC that drafts ADR-0323 ADDS a heading — the phase-100 SPEC's ADR-0322 draft is the precedent: `^## ADR-` 320 -> 321, bare `^## ` 328 -> 329, `^---$` UNMOVED, `22 0`. MEASURE all five after the draft.** ⚠️ **NEVER derive next-free from the heading count** — the id space is sparse at the `0209` gap — ⚠️ **and the heading regex `^## ADR-[0-9]+[:—]` is itself holed by `## ADR-0127 v2`.**
⚠️ **THE `BEHAVIOR_CONTRACT.md` LEDGER QUESTION IS SETTLED AND DISCHARGED — DO NOT RE-LITIGATE IT.** A `+0` row DOES earn a ledger chain entry; the phase-99 IMPL wrote one at `:5148`, so the tail chain of `+0, UNCHANGED` entries reads 44.2, 44.3, 45.2, 47.1, 51, 96, 97, 98, 99, **and the phase-100 IMPL appended a delta-only `+1` entry after it (`:5150`) — the chain now ENDS with a `+1`, not a `+0`** — ⚠️ **and a Phase-55 `+0, UNCHANGED` paragraph ALSO exists at `:891`, OUTSIDE the tail chain, which the "EIGHT" this file used to quote did not count. Name the matcher and the region with any count, or quote none.** **Follow the phase-96/97/98/99/100 FORM (delta only), not the sibling `A → B` form, and quote NO ABSOLUTE** — three mutually inconsistent stat-surface absolutes are live in this tree at one tip, and on a contested count the answer is **no number**.

## ⚠️ THE ARCHIVE GUARD

⚠️ **THE STRICT `163` IS NOT THE ENTRY COUNT.** Re-measured at THIS tip (phase-100 **IMPL** close, `STATE_HISTORY.md` **594**): loose **247** = strict **163** + parenthetical **84**, exactly — **the triple reconciles only under these ANCHORED-OCCURRENCE forms:**
```sh
H=docs/envoy-go/STATE_HISTORY.md
grep -oE '^- \*\*prior active-phase:\*\* ' $H | wc -l   # strict         163  <- DELTA 0 across a correct close
grep -oE '^- \*\*prior active-phase \('    $H | wc -l   # parenthetical   84  <- +1 per close
grep -oE '^- \*\*prior active-phase'       $H | wc -l   # loose          247  = 163 + 84
```
⚠️ The unanchored `grep -c 'prior active-phase'` counts LINES and coincides by accident; the colon form is a FOURTH regex. **Name the form or the figure is meaningless.** ⚠️ **`grep -F` WITH A PATTERN STARTING `- ` HITS THE FLAG TRAP** — pass `--` first. **The guard is SOUND as a DELTA-0 SHAPE check** but the number is a SUBSET. ⚠️⚠️ **AND A SELF-WRITTEN SUBSTITUTE IS WORSE THAN A WRONG ONE:** at `01e9d72b` the invented forms `^- \*\*` / `^- \(` / `^- ` read **264 / 0 / 268** against the house forms — **a parenthetical ZERO, which reads like the archive lost every entry of that shape.** Method note 67.

⚠️ **THE ARCHIVE'S POSITIVE CONTROLS ARE SELF-INCREMENTING.** The house rule is **name the LABEL, never the COUNT**: any FIGURE recorded in the file the control measures is invalidated by the act of recording it. ⚠️ **THE RAW LINE DELTA IS `+2`, NOT `+1`** — a blank line PLUS the entry line. **The phase-100 IMPL close: `STATE_HISTORY.md` 592 -> 594**, evicting the phase-99 SPEC entry, strict **163, DELTA 0**, parenthetical **83 -> 84**, loose **246 -> 247**.
⚠️ **THAT CLOSE'S TIE WAS BROKEN BY LIST POSITION — AS PROJECTED.** The MEASURED pre-roll list, INCLUDING the entry that close promoted from §Current (the phase-100 PLAN, `09-22`), was **TWO `09-22`, FOUR `09-21`**: a FOUR-WIDE tie at the tail, broken by list position (the phase-99 SPEC entry). ⚠️⚠️ **The post-roll §Recent reads TWO `09-22`, THREE `09-21` — and YOUR close promotes the phase-100 IMPL entry (`09-22`) on top of it, so YOUR pre-roll tail is PROJECTED to be a THREE-WIDE `09-21` tie that LIST POSITION breaks (evictee: the phase-99 PLAN entry). That is a PROJECTION: MEASURE IT (method note 26).**
⚠️ **THE BARE FORMS ANSWER NOTHING.** **Only the LABEL-BOUND PAIR discriminates**, run on BOTH files, with a fabricated-label NC reading zero in both and a positive control on a label that IS present in the archive and ABSENT from §Recent. **Both directions were verified at the phase-100 IMPL close**: the evictee's own label went `STATE.md` **1 -> 0** and `STATE_HISTORY.md` **0 -> 1**, the fabricated-label NC read **0 / 0**, and the positive control read **0 in `STATE.md` / present in the archive**.

## ⚠️ PORT BANDING

`net.ipv4.ip_local_port_range = 32768 60999`. ⇒ **BAND YOUR PROBE PORTS BELOW 32768.** ⚠️ **The differential harness reserves `20000..31007` (`harness_test.go:292-296`) and `11000..14999` (`:209-212`)**, so `15000-19000` is the ad-hoc band — ⚠️ **minus `18080-19000`, held by sibling `curl-world` containers, and minus the FIXTURE `15xxx` literals.**
🔴 **THE `15000-19000` CONVENTION IS EXHAUSTED BELOW 16000.** Bands SPENT: phase-96 IMPL `15500-15599`, phase-97 BRAINSTORM `15700-15799`, phase-97 SPEC `15800-15899`, phase-97 IMPL `15900-15999`, phase-98 BRAINSTORM `15600-15699`, phase-98 SPEC `16000-16099`, phase-98 PLAN `16100-16199`, phase-99 BRAINSTORM `16200-16299`, **phase-99 SPEC `16300-16399`** (reference agent `16301`/`16302`/`16311`/`16312`, subject agent `16350-16399`, all cleanly; left only client TIME-WAIT). The phase-99 PLAN and IMPL used NO ad-hoc band (their fixture work ran through the harness's own allocation, and the IMPL's NC mutants were unit-level). **phase-100 BRAINSTORM `16400-16499`** (reference agent host ports `16400-16427`, subject agent `16450-16467` plus a placeholder `16499`), **phase-100 SPEC `16500-16599`** (reference agent `16500`/`16501`, subject agent `16550-16599`; census read two hits, both hex constants in `internal/cluster/hash.go`, not ports), **phase-100 PLAN `16600-16699` RESERVED AND UNUSED** (censused ZERO hits; its fixture runs went through the harness's own allocation and its unit arms used port 0), and **the phase-100 IMPL used NO ad-hoc band** (fixture runs through the harness, unit arms on port 0, NC mutants in a throwaway worktree). ⇒ **YOUR STAGE SHOULD TAKE `16700-16799` IF IT PROBES OUTSIDE THE HARNESS** — a BRAINSTORM that measures both sides usually does (the phase-100 BRAINSTORM used `16400-16499`). ⚠️ **This sentence spells both ends of that band, so it is a hit in YOUR census. CENSUS IT YOURSELF (`git grep -In '167[0-9][0-9]' -- test/ internal/ cmd/`), SCOPE IT, USE `-I`, AND SAY WHAT EACH HIT IS** — a bare 5-digit census also matches hex constants and byte sizes: at the phase-100 IMPL close that pattern matched only SUBSTRINGS of `16777215` / `16777216` (H2 frame-size bounds and 16 MiB caps), no port. ⚠️ **Fixture `0124`'s reference ports `15124`, `15225`, `15226`, `15227` are now TAKEN** (landed at the phase-99 IMPL). ⚠️ **Fixture `0125`'s reference ports `15125`, `15228`, `15229`, `15230`, `15231` are now TAKEN** (landed at the phase-100 IMPL, re-censused at its Task 5 before landing).
⚠️⚠️ **AND THE CENSUS INSTRUMENT CAN COUNT ITSELF.** This file's previous census sentence claimed **TWO** `156xx` hits; re-run at the phase-98 BRAINSTORM it read **THREE**, because that sentence spells both numbers and is therefore a hit in its own census. **The figure was false at its own publishing commit.** Same self-incrementing class as the archive's positive controls. **Name what each hit IS, and expect your own sentence to be one of them.**
⚠️⚠️ **THE FIXTURE PORT BANDS ARE FAMILY CONVENTIONS WITH LOCAL EXCEPTIONS, NOT INVARIANTS — CENSUS THE BAND YOU ARE JOINING.** **42 distinct `15xxx` literals** live under `test/fixtures/*/driver` and `*/inputs` at the phase-100 IMPL close (`git grep -ohE '\b15[0-9]{3}\b' -- 'test/fixtures/*/driver/*' 'test/fixtures/*/inputs/*'`, `sort -u`; **38** at the phase-99 IMPL close, `0125` adding `15228`-`15231`; ⚠️ **the pathspec needs the trailing `/*` — `'test/fixtures/*/driver'` alone reads ZERO**; ⚠️ **the `28` this sentence once carried was STALE — the same command read 35 at `515856da`**), spanning `15000`-`15125`, `15223`-`15231` and `15360`, with collisions at `15000` and `15050` (not re-audited). What IS true: the highest CONVENTION-FOLLOWING reference port is **`15125`** (fixture `0125`, phase 100), and the convention that produced it is `15000 + <fixture index>` — **a derived observation NO document states** — while `0123`, `0124` and `0125` also hold OFF-convention `15223`-`15231`. ⇒ **a new fixture `0126` would take `15126` on convention, which reads ZERO hits across `test/ internal/ cmd/` at the phase-100 IMPL close — RE-CENSUS before use** (`git grep -c '\b<port>\b' -- test/ internal/ cmd/`; a literal is not a bind, and a reserving comment is a literal). The TLS/SDS fixtures `0108`-`0113` hold `10444`-`10449` and `0103` holds `10443`; **`10450` IS HELD BY NOTHING**, its only occurrence under `test/` being `0118/driver/driver.go:31`'s own warning comment.
**Check availability with `ss -tan` AND `ss -uan` (ALL states), never `ss -ltn`.** ⚠️ **FOR A UNIT TABLE, USE PORT 0.** ⚠️ **`net.Pipe` DEADLOCKS a client-cert handshake — use a loopback TCP pair.**

## ⚠️ A RECURSIVE `grep` IS BLIND TO `next-prompt.txt` IN **EVERY** SHELL HERE

🔴 **CORRECTED AT THE PHASE-97 PLAN — see method note 3j.** This section used to say the blindness was CONTROLLER-ONLY and that a subagent shell carried GNU `grep`. **It does not.** `type grep` in a subagent shell returns the same function wrapping **ugrep 7.8.4** (`-G --ignore-files --hidden -I --exclude-dir=.git`), which honours `.gitignore`; `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. `/usr/bin/grep` (GNU 3.11), `command grep -rl`, `git grep` and direct-path access all see it. ⚠️ **`command grep` DOES NOT SURVIVE `xargs`.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files). ⚠️ **ugrep ALSO REJECTS SOME COMPLEX `grep -oE` PATTERNS with `exceeds complexity limits` where GNU grep accepts them.** **NAME THE BINARY IN EVERY BRIEF YOU ISSUE, AND CROSS-CHECK A FIGURE UNDER BOTH BEFORE TRUSTING EITHER.**

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 100 (listener-filters-timeout-enforce)' --format='%h %s'   # the SLUG form — the last CLOSED row (a new phase brings a new slug)
git log --grep '^phase 100' --format='%h %s'                                       # the LOOSE form — RUN IT to CROSS-CHECK
```
⚠️ **RUN BOTH FORMS AND RECONCILE THE SETS. NO COUNT IS QUOTED HERE, DELIBERATELY — BOTH FORMS ROT, A SLUGLESS CORRECTION COMMIT IS INVISIBLE TO THE ANCHORED FORM, AND A COMMIT MISSING ITS *STAGE WORD* IS INVISIBLE TO A STAGE-ANCHORED ONE** (method note 57; the live witness is phase 95's IMPL, `0f3b98b3`). **Take the tip from `git rev-parse master`.**
⚠⚠ **SHARPENED AT THE PHASE-98 PLAN — THE BLINDNESS IS IN THE *STAGE-WORD FILTER*, NOT IN THE LOOSE FORM.** `git log --grep '^phase 95 (.*) IMPL'` reads **ZERO** (phase 95's IMPL is `0f3b98b3`, whose subject carries no stage token), but `git log --grep '^phase 95'` reads **six and DOES contain it**. So the loose form is not the cure by itself: **post-filtering it on the same stage word (`| grep -i IMPL`) reproduces the blindness exactly.** **Reconcile the SETS, not the greps** — list the loose form's commits and read their subjects.

Execution style: **subagent-driven per `feedback_execution_style`**. Subagents commit LOCALLY on their own stage branch with EXPLICIT PATHSPECS; the controller merges and squashes (`feedback_subagents_no_push`, `feedback_push_to_origin`). **Pure MEASUREMENT agents should commit NOTHING and prove their tree clean.** ⚠️ **PARALLEL AGENTS IN ONE WORKTREE RACE THE GIT INDEX** — per-agent worktrees on their own branches, private scratch dirs, **Docker serialized to exactly ONE agent at a time.** ⚠️ **THE PHASE-98 BRAINSTORM RAN TWO AGENTS IN PARALLEL SAFELY BY SPLITTING ON THE DOCKER AXIS** — one Docker-only reference agent and one Docker-free subject agent, disjoint port bands, disjoint worktrees. **That split is the reusable pattern.**
⚠️ **VERIFY WHAT A SUBAGENT REPORTS — BUT ALSO BELIEVE IT WHEN IT REFUTES YOU.** At the phase-98 BRAINSTORM the subject agent refuted **both** hypotheses its own brief offered for the load-bearing arm (neither "order-dependent" nor "longest-suffix" — the answer was an ambiguity error) and was RIGHT. At the phase-96 IMPL an agent refuted the controller's brief and was RIGHT; at the phase-97 BRAINSTORM one was WRONG because its matcher was blind to the wording; at the phase-97 SPEC one was WRONG because it ran a different command than the claim named (notes 49 and 56). **Verify BOTH directions against the source.** ⚠️ **An agent's own SUMMARY LINE can disagree with its own TABLE — count the table.** ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root** — build with `-o` into scratch.

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root — **it fired on roughly every other call at the phase-98 BRAINSTORM, with an explicit `Shell cwd was reset` line.** Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` before any commit. ⚠️ **Shell VARIABLES do not survive between Bash calls** — re-export in every command. ⚠️ **AN UNQUOTED HEREDOC (`<<EOF`) COMMAND-SUBSTITUTES BACKTICKED TEXT** — use `<<'EOF'`.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE OR PROTOTYPE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — or better, **build it in a THROWAWAY worktree and remove that worktree**, which is what both phase-98 agents did. ⚠️ **Also check the MAIN repo root, and `--untracked-files=all`** — note the pre-existing untracked `.claude/` directory is NOT yours and is present in every snapshot; filter it out rather than "cleaning" it.

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads OPEN PHASE 101 BY SELF-PICK AND WRITE ITS `BRAINSTORM.md`.** ⚠️ Its §Recent is FIVE entries under the ADR-0288 cap, and its `lifecycle-state:` reads **DONE**.
2. `docs/envoy-go/ROADMAP.md` — **the whole of it, because your stage ADDS row 101 to it.** ⚠️⚠️ **READ THE SIX DEFERRED-CANDIDATE WINDOWS (`:210 :216 :222 :232 :238 :246`); DO NOT EDIT, TIDY OR REFLOW THEM. THE MARGIN IS ONE, AND DELETING THE LAST ONE ENDS THE PROJECT.** Those windows are also the best single source of unchartered subjects. ⚠️ **Row 100 at `:162` is the newest tail row and reads `done`.**
3. `docs/envoy-go/phases/100-listener-filters-timeout-enforce/PROGRESS.md` — **the newest IMPL record and the closest precedent for what a row costs end to end**: a 19-task spine, the un-fixed tip recorded before Task 8, the NC roster scored per arm with each mutant's marker asserted first, a six-gate sweep that NAMES its departure, and a close-out listing fourteen refutations. ⚠️ **A closed phase's governing documents are EVIDENCE, NOT INSTRUCTIONS.**
4. `docs/envoy-go/phases/100-listener-filters-timeout-enforce/BRAINSTORM.md` — **514 lines, the FORM your stage must produce**: §0 its refutations of this file, §1 the pick, §4 the fully-adjudicated rejected roster (**§4.1 is the envelope-lift NEXT-IN-LINE adjudication, §0.2 its reference measurement**), §8 the sentinel on both sides of its own ADD, §10 the owed items the SPEC discharged. ⚠️ **Where it disagrees with `100/SPEC.md` §0, the SPEC's measurement governs** — and `100/SPEC.md` §14.3 re-measured the envelope's `0.5s` arm.
5. `BOOTSTRAP_PROMPT.md` **at the repo root** — **§5 state 0 -> 1** (phase not yet in `ROADMAP.md` ⇒ `superpowers:brainstorming`, which adds/refines the row) and **§6.1's split gate** (it binds the PLAN, but a row you scope carelessly here is what forces a split two stages later). ⚠️ **§5 appears VERBATIM TWICE — find the second copy by HEADING, never by arithmetic; its offset is NON-constant.**
6. `docs/envoy-go/DECISIONS.md` — **`ADR-0322`** (`ACCEPTED`, the tail: enforcement, the `0s` split, and §Consequences (g) naming the envelope lift as next) and **`## ADR-0082:`** (OLD form: the `[1s, 60s]` envelope lives in its ¶1 and §Consequences (a) and (c), superseded clause by clause, NEVER edited). ⚠️ **ADR-0044 does NOT contain the ADR-drafting discipline this project cites it for** — the discipline is the ADR-0294-through-0322 shared block form, and **no ADR documents it.** ⚠️ **A BRAINSTORM DRAFTS NO ADR; THE SPEC DOES.**
7. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — **6000 lines**, the parity record your candidate must be checked against BEFORE it is chartered. ⚠️ **The `Per-pipeline timeout` bullet (rewritten at row 100 — anchor on that LITERAL, not a line) states the `[1s, 60s]` envelope as envoy-go's OWN and the lift as the next row.** The ledger chain ends with the phase-100 delta-only `+1` entry (`:5150`).
8. `REVIEW_FINDINGS.md` **at the repo root** — an un-chartered findings source (method note 92); its row-100 timeout clause is now annotated fixed, its other two listener clauses are still open. **`git grep` it when picking.**

## What the next session owes
**OPEN PHASE 101 BY SELF-PICK AND WRITE ITS `BRAINSTORM.md` (lifecycle-state DONE -> 1)** — ⚠️ **ONE STAGE PER SESSION.** Concretely:
1. **PICK THE SUBJECT AND WRITE DOWN WHAT YOU REJECTED.** Smallest defensible candidate first; the pick AND the fully-adjudicated rejected roster go in `BRAINSTORM.md` §1 and §4, per the phase-95-through-100 form. ⚠️ **The `[1s, 60s]` envelope lift is UNBLOCKED and was adjudicated NEXT IN LINE (`100/BRAINSTORM.md` §4.1, ADR-0322 §Consequences (g))** — an adjudication by closed stages, which you may overturn IN WRITING. ⚠️ **MEASURE THE CANDIDATE BEFORE CHARTERING IT (method note 69)**, and **SPLIT A BANKED VERDICT BEFORE PRICING IT (note 52)** — the envelope bullet names a LOWER bound (`0.5s`; ⚠️ `500ms` is a `protojson` reject, spell `0.5s`) and an UPPER bound (`61s`, `120s`; past `120s` UNMEASURED), and ⚠️ **a sub-second lower bound now meets row 100's ENFORCEMENT — drive the DROP, not only the boot.**
2. **DRIVE BOTH SIDES TO THE OBSERVABLE, ON EVERY ARM (method note 74).** ⚠️ **PAIR EVERY DIMENSION WITH A MATCHED NEGATIVE ON A BYTE-IDENTICAL INPUT (method note 55)**, or you cannot tell *"matched"* from *"unenforced"*. ⚠️ **A timing arm needs a TIMED observable and a stated window** (the `0125` window rule: ~4-5σ, `reference_differential_band_sigma_margin`).
3. **ADD ROW 101 `in-progress`** — an **ADD, not a flip**, so the denominator MOVES: `want` **132 -> 133**, `ROADMAP.md` **250 -> 251**. ⚠️ **COUNT THE NEW ROW'S FIELDS (want 8) UNDER BOTH THE NAIVE AND THE ESCAPE-AWARE FORM, BEFORE AND AFTER INSTALLING**, and ⚠️ **ASSERT THE CELL SPELLS NEITHER SENTINEL MATCH PHRASE (nor the bare word `deferred`).** **Reword a pipe AWAY rather than escaping it.**
4. ⚠️ **RE-RUN the sentinel + all four NCs + the check-(2) positive control YOURSELF, at YOUR OWN tip, and record ACTUAL output — on BOTH SIDES of your ADD.** The shapes move at an ADD and they moved back at phase 100's FLIP; **both were measured, neither was inherited.** ⚠️ **Re-derive the six windows' LINE NUMBERS after the ADD (expected `+1` each: `:211 … :247`) and compare their md5s, never their numbers.**
5. ⚠️ **PRICE THE ROW AS A FLOOR, NEVER AS AN ESTIMATE** (`reference_measured_prototype_is_a_lower_bound`) — and an inherited FLOOR can be an OVERSTATEMENT too. **Re-derive a fixture floor by SHAPE, not by recency** — the newest fixture, `0125`, is **+836** (non-TLS, no PKI, no `inputs/`; `git diff --numstat` at the phase-100 IMPL), `0124` is **940** (TLS, with a `pki/gen` package), while the `1359`/`1393` figures elsewhere are PKI-inflated.
6. **A BRAINSTORM RUNS NONE OF THE SIX GATES AND WRITES NO ADR, NO LEDGER ENTRY AND NO PRODUCTION CODE.** ⚠️ **NAME THAT AS SCOPE, NOT AS COMPLIANCE.** ⚠️ **AND DO NOT RESTATE THE DISCHARGED DEPARTURES** — lint RUNS under `GOTOOLCHAIN=go1.26.2` and gate (b) is GREEN under the ambient toolchain; the one standing departure is no `REVIEW.md` (NONE of 93-100).
- `STATE.md` rolled **IN PLACE** with the oldest §Recent entry evicted — ⚠️ **use the LABEL-BOUND PAIR across BOTH files, with a fabricated-label NC and a positive control naming an ARCHIVED label.** ⚠️ **YOUR pre-roll tail is PROJECTED to be a THREE-WIDE `09-21` tie broken by LIST POSITION (see THE ARCHIVE GUARD) — MEASURE THE HISTOGRAM, INCLUDING THE ENTRY YOUR CLOSE PROMOTES; INHERIT NO SHAPE, INCLUDING THIS ONE.** Archive as **ONE INLINE LINE** in the PARENTHETICAL form, **strict guard DELTA 0**, raw delta **+2**, ⚠️ **naming the LABEL and NO FIGURE of the archive's own counts.**
- ⚠️ **A STAGE'S FILE SCOPE IS MEASURABLE — MEASURE YOURS** with `git show --numstat` against the phase-100 BRAINSTORM (`674786bd`: **5** paths — `ROADMAP.md` `1 0`, `STATE.md`, `STATE_HISTORY.md` `2 0`, the new `BRAINSTORM.md`, `next-prompt.txt`) and the phase-99 one (`c9b9ecfc`: **5**), rather than trusting any wording. For reference, the phase-100 **IMPL** touched **20** paths — LIKE FOR LIKE with the squashed `d5bc9153` (20), `c7bd2880` (20) and `0a985a35` (28) (method note 98).
- **ROLL THE §Recent PREAMBLE SENTENCE TOO, without spelling the evictee's label** · ⚠️ **RE-DERIVE ANY LINE COUNT YOU QUOTE IN THE SAME COMMIT AS THE EDIT THAT MOVES IT — AND AT YOUR STAGE's LAST COMMIT (method note 97)** · `next-prompt.txt` rolled (`git add -f`) · ONE squashed commit, merged and pushed, **with the FULL SLUG *AND* THE STAGE WORD in its subject** · ⚠️ **`git worktree remove` YOUR OWN STAGE WORKTREE AT CLOSE.**
- ⚠⚠ **WHEN YOU ROLL THIS FILE, GREP IT FOR `YOUR STAGE`, FOR YOUR OWN STAGE WORD, AND FOR EVERY FIGURE YOUR ROW MOVED** (`want` 133, `ROADMAP.md` 251, check (1) ONE, NC-A/NC-B TWO, the six windows' new line numbers, the stale-claim window `:233`) — then read the result as the next reader, who has no idea which sentences you meant to leave (method note 68). **The phase-97 roll left FOUR blocks addressed to a stage that no longer existed; the phase-100 IMPL's final review found a row cell its own stage had falsified after writing it.**
