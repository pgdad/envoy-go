START a fresh cold-start session for envoy-go — **the phase-29.2 sub-phase SPEC (`network-filter-mongo-responses-and-correlation`) is DONE + reviewer-APPROVED + squash-merged + pushed**. The active phase is **phase 29.2 → SPEC done** (parent row 29 STAYS `in-progress`; sub-row 29.1 `done`; 29.2 `in-progress`; 29.3 `planned`). This session opens the **29.2 PLAN** — authoring `docs/envoy-go/phases/29.2-network-filter-mongo-responses-and-correlation/PLAN.md` via **`superpowers:writing-plans`** (per SKILL_ROUTING state 2: SPEC.md exists, PLAN.md does not → writing-plans → PLAN.md, with the ADR-0045 split-gate FINAL check). Work in a git worktree (`feedback_git_worktrees`); commit LOCAL-ONLY; the controller squash-merges + pushes at stage-close (`feedback_push_to_origin`).

The 29.2 SPEC landed at the SPEC squash (filled by the trailing docs-only SHA-fill commit; confirm the live tip via `git log --oneline -1`). The substantive predecessor on master is the 29.1-IMPL squash `5ef5215` (before that: 29.1-PLAN `89c194a`; 29.1-SPEC `7574219`). The 29.2 SPEC (`docs/envoy-go/phases/29.2-network-filter-mongo-responses-and-correlation/SPEC.md`, 16 §§ + 2 appendices) + README are the PLAN's blueprint; the parent SPEC + the 29.1 SPEC/PLAN + the as-built `internal/filter/network/mongoproxy/` package are the pinned-behavior references. ADR-0225 §Context (incl. the new 2026-06-04 D-P4 AMEND) is the charter; its §Decision/§Consequences body lands at the 29.2 IMPL (no new ADR number — DECISIONS tail STAYS ADR-0226).

## What the 29.2 PLAN decomposes (the SPEC §12 ~11-task spine → bite-sized TDD tasks)

The PLAN turns the SPEC §12 spine into write-failing-test → run → implement → run → commit steps with complete Go code blocks, exact file paths, run commands + expected outputs, and per-task `gofmt -l` + `golangci-lint` (`feedback_pertask_gofmt_lint`). The SPEC-anchored spine (green-compiling dependency order — the 28.2 ordering):

1. **T1** — first-action baselines/anchors gate: re-pin fixtures **52** (tail `0050`) + fuzzers **39** + stat surface **360** + DECISIONS tail **ADR-0226** (next-free **ADR-0227**) + the SPEC §9.1 as-built anchors, against the live IMPL-session tip (R6).
2. **T2** — `decoder` struct: add `writeBuf []byte` + `mu sync.Mutex`; MOVE the 29.1 request-path list-append + gauge-Inc sites under `mu`; mechanical existing-test updates (all 29.1 tests stay green). [SPEC §3.5/§3.4]
3. **T3** — response framing + dispatch: `decodeOnWrite` + `nextWriteMessage` + `decodeResponseMessage` + the `opReply`/`opCommandReply` dispatch arm + the write-side `decoding_error`/sniffing-off path (direction-shared) + unit tests. [SPEC §3.1/§3.2]
4. **T4** — OP_REPLY + OP_COMMANDREPLY body decode + the 5 response counters (`op_reply` + `op_reply_cursor_not_found` [0x01] + `op_reply_query_failure` [0x02] + `op_reply_valid_cursor` [cursorID≠0] + `op_command_reply`) + unit tests (each flag bit; valid_cursor; numberReturned docs; malformed → decoding_error). [SPEC §3.2]
5. **T5** — correlation: `takeQuery` (first-match-erase by `query.requestID == reply.responseTo` under `mu`) + the gauge `Dec` on a hit + uncorrelated-miss handling + unit tests (first-match; only-OP_QUERY-entries; miss charges fixed only). [SPEC §3.3/§3.4]
6. **T6** — `OnDestroy` gauge teardown (drain residual list under `mu`, Dec per entry) + the inc/dec lifecycle unit tests (the list-size↔gauge invariant). [SPEC §3.4]
7. **T7** — `OnWrite` glue (replace the no-op stub) + the concurrent request/response race test (`-race -count=5`; mutex necessary + sufficient — R9). [SPEC §3.1/§3.5]
8. **T8** — `emit_dynamic_metadata` emission (the single-Set Bucket StructValue model; collection→ops; per-pass overwrite-clear; gated) + unit tests (namespace/keys/clear; gated-off no-emit) — D-P11. [SPEC §3.7]
9. **T9** — the 39th fuzzer `FuzzMongoDecode` EXTENDED to the response opcodes + the mutex (D-P6 — NO 40th). [SPEC §7]
10. **T10** — `TCPMongoResponder` BackendKind **30** + the runner backend arm (`acceptMongoResponder`) + the OP_REPLY/OP_COMMANDREPLY builders (beside the 29.1 `opQuery`/`opInsert` builders). [SPEC §6.1]
11. **T11** — `0051-mongo-responses` driver + cross-side GREEN all arms (the gauge quiesced-point arms answered→0/unanswered→1; `cx_destroy_*` PRESENCE-ONLY; the R4 deliberate-break with `-count=1`) + README; then the completion bundle: ADR-0225 §Decision/§Consequences body in-place (ADR-0044) + the BEHAVIOR_CONTRACT 29.2 bundle (§8 — the response semantics + the gauge + the dynamic-HISTOGRAM coverage boundary [ADR-0060] + the cx_destroy close-direction coverage boundary [D-P4] + the dynamic-metadata note) + STATE/ROADMAP sub-row 29.2 `in-progress → done` (parent row 29 STAYS `in-progress`) + next-prompt.txt + the six-gate (full 53-dir suite). [SPEC §6.2/§8/§13]

## Load-bearing SPEC pins the PLAN must honor

- **The D-P4 COVERAGE BOUNDARY (user-confirmed 2026-06-04).** `cx_destroy_local/remote_with_active_rq` get NO increment at 29.2 — exist-at-zero, PRESENCE-ONLY in `0051`. The close-direction accessor + the direction-keyed increment + VALUE parity DEFER to 29.3 (framework-zero-touch at 29.2; the 28.2 precedent; ADR-0219). The ADR-0225 §Context AMEND (already landed at the SPEC commit) supersedes the parent §7.2/§3.1 "Incremented 29.2" roster cells for the two counters.
- **The per-connection `sync.Mutex` (ADR-0223, narrowed to ONE list).** Guards EXACTLY `dec.queries` (+ `sniffing` per D-S29.2-4); copy-out-under-lock; gauge/counters OUTSIDE the lock; the pre-handoff request path locks too. The `-race -count=5` test is load-bearing (must report a race without `mu`).
- **No write-side high-water mark** — `writeChainConn.Write` allocates a fresh per-Write Buffer (the 28.2 asymmetry); `decodeOnWrite` appends `buf.Bytes()` directly to `writeBuf`.
- **The gauge is the project's FIRST differentially-mirrored gauge** — atomic ops outside the lock; the D-P9 quiesced-point assertion (driver waits for the reply round-trip; answered→0, unanswered→1 while open, 0 post-close).
- **+0 stat creation; no new name.go arm; no new built-in** — all 23 stats created eagerly at 29.1; the four-rule arm already handles the reply counters + the gauge TYPE line; the `0049` `scrapeMongoStats`/`scrapeTypeLine` helpers handle the gauge as-is (D-P10).
- **The 29.2-additive D-questions for IMPL** (SPEC §10.2): D-S29.2-1 (reply-frame byte minimums vs upstream — `reference_wire_format_both_sides_see_same_bytes`), D-S29.2-2 (OP_GET_MORE reply disposition — uncorrelated), D-S29.2-3 (Bucket single-Set model — PREFERRED, zero primitive change), D-S29.2-4 (sniffing-flag synchronization — race-clean), D-S29.2-5 (residual-list drain at OnDestroy).

## What this session must do

1. Re-establish a worktree (`feedback_git_worktrees`); confirm the clean baseline (`go build ./...` + `go test ./internal/... -count=1`).
2. Run `superpowers:writing-plans` to author the 29.2 PLAN from the SPEC §12 spine: complete Go code blocks per task, exact file paths (`internal/filter/network/mongoproxy/{codec,filter,stats,doc}.go` + `*_test.go` + `fuzz_test.go`; `test/differential/fixture/fixture.go` [BackendKind 30] + the runner arm; `test/fixtures/0051-mongo-responses/`), run commands + expected outputs, per-task `gofmt -l` + `golangci-lint`; the ADR-0045 FINAL gate re-check (~330–540 production LoC; no split anticipated — the 28.2 single-sub-phase precedent); run the plan-document-reviewer; fold advisories.
3. STATE.md advance (active-phase → `phase 29.2 PLAN done`; next-skill → the 29.2 IMPL via `superpowers:subagent-driven-development` per SKILL_ROUTING state 3 + `feedback_execution_style`); ROADMAP sub-row 29.2 STAYS `in-progress`; rewrite next-prompt.txt for the **29.2-IMPL** cold-start; commit + squash-merge + push.

## Project state at master tip

- **Master tip:** the 29.2-SPEC squash + its trailing docs-only SHA-fill commit (confirm the live tip via `git log --oneline -1`). STATE.md `last-commit` points at the 29.2-SPEC squash. Substantive predecessors: the 29.1-IMPL squash `5ef5215`; the 29.1-PLAN squash `89c194a`; the 29.1-SPEC squash `7574219`. origin/master in sync after push.
- **§9 Network-filters family:** rows 26/27/28 DONE; row 29 `in-progress` (29.1 `done`; 29.2 `in-progress` — SPEC done; 29.3 `planned`).
- **Counts at master tip (UNCHANGED at the 29.2 SPEC — no code):** stat surface **360** (+0 at 29.2/29.3); differential fixtures **52** (tail `0050-mongo-boot-reject`; → 53 at the 29.2 IMPL, 54 at 29.3); fuzzers **39** (the 29.2 IMPL EXTENDS `FuzzMongoDecode`, no count change); conformance h2spec 53/53 + proxy-wasm 10/10 (as of the 29.1 six-gate, 2026-06-04); DECISIONS.md tail **ADR-0226** (next-free **ADR-0227**; the ADR-0225 body lands at the 29.2 IMPL — no new number).
- **Toolchain:** Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); reference Envoy v1.37.2 (ADR-0008); go-control-plane v1.32.4 (ADR-0008).
- **Known coverage boundaries (carried, non-blocking):** the mongo D-P1 boot-window stat-creation departure (eager vs upstream per-connection — unobservable); the `stats.IsValidName` guard on un-nameable wire-derived dynamic segments (user-approved; fuzz-validated); the mongo dynamic-HISTOGRAM families deferred (ADR-0060 — RECORDED at the 29.2 IMPL); the **`cx_destroy_*` close-direction value parity DEFERRED to 29.3** (D-P4 — recorded at the 29.2 IMPL); the dynamic-metadata emission differential-invisible (AMEND-B11 — unit-test only); fault delay + access log + drain + `cx_drain_close` + `delays_injected` are 29.3's; mongo runtime-key gating unmirrored (29.3 record); the mongo access log not differentially compared (AMEND-B10; 29.3 record); `tcp_proxy` `downstream_cx_*` unmirrored; histograms deferred project-wide (ADR-0060).

## Read first (in order)

1. THIS FILE — the 29.2-PLAN cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 29.2 SPEC done`; next-skill `superpowers:writing-plans` (the 29.2 PLAN); next-free `ADR-0227`.
3. `docs/envoy-go/phases/29.2-network-filter-mongo-responses-and-correlation/SPEC.md` — the PLAN's blueprint (esp. §3 the design; §6 the fixture + `TCPMongoResponder`; §7 the fuzzer; §10 the D-questions; §12 the task spine + the split-gate check; §13 the test surface + the six-gate) + the sibling README.md.
4. `docs/envoy-go/DECISIONS.md` ADR-0225 (the §Context + the 2026-06-04 D-P4 AMEND — the charter) + ADR-0224 §Decision/§Consequences (the 29.1 request side this builds on) + ADR-0223 §Decision (the per-connection-mutex + no-write-side-high-water-mark + copy-out-under-lock discipline the correlation mirrors) + ADR-0217 (the dynamic-metadata Bucket) + ADR-0221 (the write chain) + ADR-0044/0052/0060/0219.
5. `docs/envoy-go/phases/29-network-filter-mongo-proxy/SPEC.md` — the parent SPEC (§3.2 the 29.2 scope; §7 the roster; §11.4/§11.5 the OP_REPLY/OP_COMMANDREPLY wire layout + correlation; §11.9 dynamic metadata; §11.10 close direction).
6. `docs/envoy-go/phases/29.1-network-filter-mongo-wire-and-requests/SPEC.md` + `PLAN.md` — the request-side design 29.2 extends (the active-query list; the eager roster; the four-rule arm; the OnWrite no-op stub the PLAN replaces) + the 3033-line 29.1 PLAN as the PLAN-format template.
7. `docs/envoy-go/phases/28.2-network-filter-zookeeper-responses-and-latency/PLAN.md` — the closest PLAN-format precedent (the response-side decoder + the per-connection mutex + the responder BackendKind decomposed into TDD tasks).
8. `internal/filter/network/mongoproxy/` (the 29.1 package the PLAN extends in place — `codec.go` the response decoder joins; `filter.go` the OnWrite stub + the OnDestroy; `stats.go` the reply counters + the gauge; `bson.go` reused for reply-doc walks) + `internal/stats/gauge.go` + `internal/dynamicmetadata/dynamicmetadata.go` + `test/differential/fixture/fixture.go` (the BackendKind roster — next-free 30) + `test/fixtures/0049-mongo-requests/driver/driver.go` (the `0051` driver template + the LE wire/BSON builders + the scrape helpers) + `test/fixtures/0048-zookeeper-responses/driver/driver.go` (the `acceptZKResponder` → `acceptMongoResponder` plumbing template).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — execution-style + worktree + push + subagents-no-push + per-task gofmt/lint + the network-filter constraints + the differential dispatch/asserter/break-protocol (`-count=1`) + wire-format-both-sides + the dynamic-stat charset guard all carry forward into the 29.2 PLAN.
