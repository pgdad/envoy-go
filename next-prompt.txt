START a fresh cold-start session for envoy-go — phase 26 (`network-filter-chain-and-rbac`) BRAINSTORM is DONE; author the parent SPEC.

This is a parent-SPEC cold-start. **Phase 26 BRAINSTORM is DONE + squash-merged to master + pushed to origin** (BRAINSTORM squash `77d6a01`; origin/master at the same). Phase 26 OPENS the §9 **Network filters family** — the first new §9 feature family since the HTTP-filters family closed at 25.3. The BRAINSTORM (a 6-question user dialogue) settled the family-bootstrap scope + a BRAINSTORM-time 3-way pre-split. STATE.md points at `phase 26 BRAINSTORM done; Network-filters family OPEN`, next-skill `superpowers:writing-plans` (scoped to parent SPEC), next-free `ADR-0213`.

## What this session must do: author the phase-26 parent SPEC per BOOTSTRAP §5 routing

Per SKILL_ROUTING state 1→2 (SPEC.md does not yet exist), the next-skill is **`superpowers:writing-plans` scoped to PARENT-SPEC authoring** (`docs/envoy-go/phases/26-network-filter-chain-and-rbac/SPEC.md`), per the phase-22/24/25 parent-row precedent. The parent SPEC:
1. Executes the BRAINSTORM §10 D1–D8 empirical-pin obligations IN-SESSION against reference Envoy v1.37.2 + go-control-plane v1.32.4 per ADR-0004 (parallel-subagent fan-out per the phase-25 SPEC precedent). **D4 (connection-metadata-sink primitive shape: extend `internal/dynamicmetadata/` vs new package) is SPEC-BLOCKING** — it must be settled here because the 26.1 `ReadFilterCallbacks` interface is shaped to accommodate it and ADR-0217 anchors it.
2. Anchors the phase-26 ADR §Context drafts beginning at ADR-0213 (clean span ADR-0213..~ADR-0218 per the BRAINSTORM §7 locked numbering: 26.1 = 0213 chain-framework + 0214 registry; 26.2 = 0215 migration; 26.3 = 0216 internal/rbac + 0217 conn-metadata-sink + 0218 rbac_network).
3. Formalizes the 3-way split surface-mapping + per-sub-phase scope boundaries (26.1 framework+echo+direct_response / 26.2 migrate tcp_proxy+HCM+retire-hardcoded-registry / 26.3 rbac_network+internal/rbac-extraction+conn-metadata-sink).

This session:
1. Reads STATE.md + the phase-26 BRAINSTORM.md + ROADMAP.md (rows 26 / 26.1 / 26.2 / 26.3) + BOOTSTRAP_PROMPT.md §5 + SKILL_ROUTING.md to confirm the BRAINSTORM→SPEC routing.
2. Confirms baseline GREEN at master tip (`go build ./... && go vet ./... && golangci-lint run && go test -race -short ./...` + differential 41/41 + h2spec 53/53 + proxy-wasm conformance 10/10).
3. Invokes `superpowers:writing-plans` scoped to parent SPEC authoring (in a worktree per the standing worktree preference; the SPEC stage is doc-authoring + empirical pins, no production code).

## Phase 26 design (settled at BRAINSTORM; DO NOT re-litigate the 6 Q-decisions)

Phase 26 bootstraps the L4 network read-filter chain framework (the network-layer analogue of phase 07.1's HTTP filter framework) + lands the family's first 3 filters across a 3-way pre-split:
- **26.1 `network-filter-chain-framework-and-echo`** — NEW `internal/filter/network/` read-filter chain: iteration-status protocol (`OnNewConnection`/`OnData` → `Continue`/`StopIteration`), `ReadFilterCallbacks` (connection accessor + `ContinueReading()`), per-connection read buffering on `StopIteration`, extensible freeze-after-boot threaded-constructor registry (mirrors ADR-0072/0079) boot-wired in `cmd/envoy-go/main.go`; + `echo` (`envoy.filters.network.echo`) + `direct_response` (`envoy.filters.network.direct_response`) as first consumers. Read-filter-ONLY (Q4; write-filter deferred + API-revision allowance). tcp_proxy/HCM untouched at 26.1.
- **26.2 `network-filter-registry-migration`** — migrate `tcp_proxy` (`internal/filter/tcpproxy/`) + HCM (`internal/filter/hcm/`) onto the new read-filter interface + registry; RETIRE the hardcoded `filterRegistry` map + `filterHandler`/`filterConstructor` in `internal/listener/manager.go`. Back-compat proven by EXISTING fixtures (0000-tcp-echo + TLS-TCP + HCM) staying byte-exact green. NO new operator-visible filter.
- **26.3 `network-filter-rbac`** — `rbac_network` (`envoy.extensions.filters.network.rbac.v3.RBAC`) FULL upstream parity (enforced + shadow rules + connection-level dynamic-metadata + allowed/denied/shadow_* stats); extract the phase-16 RBAC policy engine into shared `internal/rbac/` (abstract matchable-context interface; HTTP rbac = consumer #1 migrated + re-verified by its phase-16 fixtures; network = #2; API-revision-allowance clause); NEW connection-level dynamic-metadata sink. L4 inputs (source/dest IP+port, SNI/`requested_server_name`, downstream-cert principals); HTTP-only matchers (header/path/url) PARSE-REJECT.

Self-answered per §9 precedent (SPEC confirms): full cross-side byte-exact differential fixtures; upstream-parity + envoy-go-strict stat surface; ZERO new third-party go.mod deps. Network filters have NO per-route `typed_per_filter_config` (BRAINSTORM §4; ADR-0125 roster untouched — SPEC confirms by absence).

## Project state at master tip (`6319671`)

- **Master tip:** `6319671` (the phase-26 BRAINSTORM stage-close SHA-fill `TBD-26-BRAINSTORM-SQUASH -> 77d6a01` — docs-only follow-up). Substantive predecessor `77d6a01` (the phase-26 BRAINSTORM squash — BRAINSTORM.md + 4 ROADMAP rows + STATE advance + the next-prompt rewrite). Earlier `b0007a4` (the §9-family-close docs-only repoint). origin/master at `6319671`. Run `git log --oneline -1` to confirm the actual tip (a further docs-only repoint may advance the tip one more; the caveat is expected).
- **§9 HTTP-filters family CLOSED at 25.3** (commit `57c7c4d` IMPL squash; SHA-fill `24c8fe1`); 20 production HTTP filters wired. DO NOT reopen.
- **Network filters family OPEN:** parent row 26 `in-progress`; sub-rows 26.1/26.2/26.3 `planned`. 6 family candidates remain after phase 26 (redis, mongo, kafka_broker, thrift, zookeeper, sni_cluster).
- **Counts at master tip (carried from 25.3):** stat surface **132**; differential fixtures **41**; fuzzers **35**; conformance **10/10**; ADR tail **ADR-0212**, next-free **ADR-0213**. Anticipated phase-26 family deltas (SPEC pins): fixtures 41 → ~46–47; stats 132 → ~136–138; fuzzers 35 → ~37–38; ADRs 0213 → ~0218.
- **Toolchain:** Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); reference Envoy v1.37.2 (ADR-0008); h2spec 53/53 at the ADR-0051 v1.32.4 pin.
- **Six gates GREEN at master tip** (carried from the 25.3 IMPL phase-done evidence; the phase-26 BRAINSTORM is docs-only).

## Reuse precedent the SPEC inherits (the network framework deliberately mirrors the HTTP framework)

- ADR-0038 — HTTP-filter iteration-status protocol (the model the 26.1 read-filter iteration mirrors).
- ADR-0072 (HTTPRegistry) + ADR-0079 (listener-filter registry) + ADR-0059 (stats-registry LBP-1) — the freeze-after-boot threaded-constructor registry discipline the 26.1 network registry mirrors; ADR-0079 also gives the two-step factory pattern.
- ADR-0071 — single-goroutine-per-stream concurrency + chain discipline (the 26.1 single-goroutine-per-connection spirit).
- phase-22.1 `internal/lua` (ADR-0188) + phase-25.1 `internal/wasm` (ADR-0202) — the extract-at-Nth-consumer-with-API-revision-allowance pattern the 26.3 `internal/rbac/` extraction follows (with the added discipline of re-verifying the LIVE phase-16 first consumer).
- phase-16 `internal/matcher/` + the HTTP-local RBAC engine at `internal/filter/http/rbac/` — the direct reuse/extraction target of 26.3.
- ADR-0207 — the per-stream `internal/filterstate/` (HTTP-scoped); 26.3's connection-metadata sink is its connection-scoped sibling.

## Read first (in order)

1. THIS FILE — the phase-26 parent-SPEC cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 26 BRAINSTORM done; Network-filters family OPEN`; next-skill `superpowers:writing-plans` (parent SPEC); next-free `ADR-0213`; last-commit `77d6a01`.
3. `docs/envoy-go/phases/26-network-filter-chain-and-rbac/BRAINSTORM.md` — the full settled design (§1–§12; the 6 Q-decisions; §7 locked ADR numbering; §10 D1–D8 empirical-pin obligations with D4 SPEC-BLOCKING).
4. `docs/envoy-go/ROADMAP.md` — rows 26 / 26.1 / 26.2 / 26.3.
5. `BOOTSTRAP_PROMPT.md` §5 (state machine; BRAINSTORM→SPEC routing) + §4.1 STATE-update invariant + ADR-0004 (SPEC-time empirical-pin hard-gate).
6. `docs/envoy-go/SKILL_ROUTING.md` — confirm state-1→2 → `superpowers:writing-plans` (SPEC).
7. The existing network/L4 code the SPEC must pin against: `internal/listener/manager.go` (the hardcoded `filterRegistry` + `filterHandler` to be migrated at 26.2), `internal/filter/tcpproxy/`, `internal/filter/hcm/`, `internal/filter/http/` (the HTTP framework being mirrored), `internal/filter/http/rbac/` (the engine being extracted), `internal/matcher/`, `internal/listener/listenerfilter/` (the listener-filter pipeline + registry precedent).
8. `docs/envoy-go/DECISIONS.md` — ADR tail ADR-0212; the framework-chain + registry + extract-at-consumer ADR lineage cited above.
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — execution-style + worktree + push + differential-asserter-dispatch + fixture-dispatch-constraint memories all apply to phase 26.

After the parent SPEC is authored + reviewed + committed + squash-merged + pushed, advance STATE.md per BOOTSTRAP §5 and rewrite next-prompt.txt for the next-stage cold-start (26.1 SPEC, or 26.1 PLAN, per the per-sub-phase precedent).
