START a fresh post-phase-23-IMPL session for envoy-go.

This is a fresh-session prompt for the post-phase-23-IMPL cold-start. **Phase 23 (`http-filter-admission-control`, `envoy.filters.http.admission_control`) IMPL is DONE.** It landed at master via squash-merge `577be97` + SHA-fill follow-up `ee2c656` (pushed to origin). ROADMAP row 23 is now `done`. All six phase-done gates were GREEN (build / vet+lint / race / differential 33/33 / fuzz / h2spec 53/53); REVIEW.md authored + Approved. The natural next step per STATE.md + SKILL_ROUTING is to **identify and BRAINSTORM the next phase** — the §9 HTTP-filters family has **2 remaining rows: `wasm` and `global rate limit`** (the user picks which at this cold-start; the next phase is NOT yet pre-determined).

## What this session must do: identify + brainstorm the next phase

Per SKILL_ROUTING state 0/1, this session does NOT execute code — it opens the next phase:

1. **The user names the next phase** (e.g. the §9 row `wasm` or `global rate limit`, or another roadmap item). Do NOT presume; ask/confirm which row to open.
2. **Invoke `superpowers:brainstorming`** scoped to that phase (per project memory + ADR-0005). Create `docs/envoy-go/phases/NN-slug/` and produce `SPEC.md` (or refine the ROADMAP row first if the row needs adding/splitting). Use a fresh worktree per `feedback_git_worktrees.md`.
3. Follow the BOOTSTRAP §5 state machine from state 0/1 onward (brainstorm → SPEC → PLAN → IMPL across separate sessions, per the phase-09..23 precedent).

## Pre-existing settled context (DO NOT re-litigate)

Phase 23 is DONE and CANONICAL at master tip `ee2c656`. Do NOT reopen the admission_control filter, its SPEC/PLAN, the 11 AMENDs, or the ADRs. **Notable IMPL outcome to carry forward:**

- **ADR-0196 was CONSUMED at IMPL (the SPEC §10 D-hypothesis BROKE).** Differential testing found planner-decision PD-5 invalid: envoy-go's encode filter chain did NOT expose the HTTP response status to filters (`:status` is not in the encode header map). The user-approved fix added a NEW encode-side framework primitive — **`ResponseStatus() int` on `EncoderFilterCallbacks`** (set-once by HCM dispatch via `SetEncodeResponseStatus`, read via accessor; mirrors the ADR-0165 set-once-by-dispatch pattern; cross-phase reusable). So phase-23 introduced ONE new framework primitive (NOT zero, contrary to the original plan claim). **next-free is now ADR-0197.** Future encode-side filters that classify on response status should read `f.ecb.ResponseStatus()`, NOT a `:status` header — see ADR-0196 + memory.
- **Differential-harness gotcha (now a project memory):** the cross-side runner branch invokes `StatsAsserter.AssertStats`, NOT `SubjectAsserter.AssertSubject` (the latter runs only on the reference-less branch). Cross-side fixtures must put subject-side stat/structural assertions in `StatsAsserter` and prove them live (deliberate-break → expect FAIL → restore). See `reference_differential_asserter_dispatch.md`.

## Base + invariants at master tip `ee2c656`

- **Master tip:** `ee2c656` (phase 23 IMPL follow-up: STATE.md SHA-fill → 577be97 post-squash; docs-only). Pushed to origin.
- **Master tail:**
  - `ee2c656 phase 23 IMPL follow-up: STATE.md SHA-fill (TBD -> 577be97 post-squash)`
  - `577be97 Squash merge phase-23-http-filter-admission-control-impl`
  - `99c8fef next-prompt.txt: repoint master-tip references to 4cd46a8 (actual HEAD)`
  - `4cd46a8 next-prompt.txt: advance to post-phase-23-PLAN IMPL cold-start`
- Baseline at master tip: **33 differential fixture directories** GREEN (0000-0031; the lua 0026-0029 + any multi-listener combined run may hit the documented `freeTCPPort` flake per 22.2 REVIEW §7.4 — re-run in isolation; not a defect); **32 fuzzers** green; h2spec 53/53 PASS at ADR-0051 v1.32.4 pin; build/vet/lint clean; race-tests clean; **18 HTTP filters wired**; **110 stat names**; **15 envoy-go-strict departure records** at BEHAVIOR_CONTRACT. DECISIONS.md tail at **ADR-0196** (full §Decision+§Consequences body); ADR-0194 + ADR-0195 + ADR-0196 all CONSUMED; **next-free ADR-0197** (unconsumed). §9 HTTP-filters family: **2 remaining rows (`wasm`, `global rate limit`)**.
- Project memories ALL apply: `feedback_git_worktrees.md`, `feedback_push_to_origin.md`, `feedback_execution_style.md`, `reference_differential_fixture_dispatch_constraint.md`, `reference_differential_asserter_dispatch.md`.

## Read first (in order)

1. THIS FILE — post-phase-23-IMPL cold-start + the next-phase obligations.
2. `docs/envoy-go/STATE.md` — the authoritative "what next" (phase 23 IMPL done; next-skill `superpowers:brainstorming`; next-free ADR-0197; §9 family 2 remaining).
3. `docs/envoy-go/ROADMAP.md` — row 23 `done`; the remaining §9 rows (`wasm`, `global rate limit`) + their dependency/sizing notes; pick + open the next row here.
4. `docs/envoy-go/SKILL_ROUTING.md` — confirm state 0/1 (phase not yet in directory → `superpowers:brainstorming`).
5. `BOOTSTRAP_PROMPT.md` (repo root) — §4.1 STATE-update invariant + §5 state machine (state 0/1 → brainstorm → SPEC).
6. `docs/envoy-go/phases/23-http-filter-admission-control/` (BRAINSTORM/SPEC/PLAN/PROGRESS/REVIEW) — the nearest just-completed single-filter precedent for the brainstorm/SPEC shape; and `docs/envoy-go/DECISIONS.md` ADR-0196 for the new encode-side `ResponseStatus()` primitive.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`: `feedback_git_worktrees.md`, `feedback_push_to_origin.md`, `feedback_execution_style.md`, `reference_differential_fixture_dispatch_constraint.md`, `reference_differential_asserter_dispatch.md`.
