=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-CHECKED 2026-07-14 (after the phase-62 PLAN landed; row 62 STAYS `in-progress`, run MECHANICALLY) — it does NOT fire. Do NOT create `stop`.** State:
- (1) prints `NOT DONE: row 62` — the phase-62 row STAYS `in-progress` at its PLAN (a row flips `done` only at its IMPL six-gate, ADR-0106). At least one non-`done` row ⇒ the sentinel is blocked.
- (2) prints THREE live "candidates:" sentences — HTTP/3, xDS, and Observability ⇒ three families STAY OPEN. (`reference_sentinel_deferred_sentence_live_vs_historical`.)
- (3) prints `NEVER OPENED: gRPC`, `Runtime`, `WASM` ⇒ THREE families never opened. Operational-tooling also STAYS OPEN.

Any of (1)/(2)/(3) printing ⇒ the sentinel does NOT fire.

**STATUS AS OF 2026-07-14: the phase-62 PLAN LANDED; ROADMAP row 62 (`tracing-custom-tags-request-header`) STAYS `in-progress`.** The PLAN is a MECHANICAL TDD decomposition of SPEC §10 into a **9-task** spine (SINGLE FLAT ROW, ADR-0045 escape-valve UNCONSUMED). No new empirical question remains — the SPEC's live probes pinned everything. **The task spine (PLAN.md):** T1 `resolve.go` — `CustomTagSpec`/`customTagKind` types + `ResolveCustomTags(specs, headerLookup) []KV` + `resolve_test.go` matrix (purely additive, build green); T2 the ATOMIC field-type change — reshape `parseCustomTags` → `([]CustomTagSpec, error)` (request_header accept + empty-name reject + **first-wins dedup by tag key**) + `TracingConfig.CustomTags []CustomTagSpec` + thread the THREE `accesslog_emit.go` call sites (H1 `:55`/H2 `:116`/H3 `:177`) via `tracing.ResolveCustomTags(..., reqHeaderLookupH1/H2(...))` + `config_test.go` (accept→spec shape + request_header accept + first-wins dedup + empty-name reject, DROP the stale `request_header`-reject row `:456-460`); T3 `span_test.go` arm-B upsert-over-built-in (`BuildServerSpan`/`upsertAttr` UNCHANGED — confirm); T4 Zipkin encoder unit test; T5 new OTLP fixture `0105-tracing-custom-tags-request-header` (clone `0102`, driver SENDS `x-trace-user`, present-case cross-side by key, register in `runner_test.go`); T6 `FuzzHCMConfigParse` request_header seed (fuzzers stay 55); T7 BEHAVIOR_CONTRACT; T8 verify (six-gate + full 107-dir differential); T9 ADR-0283 §Decision/§Consequences + STATE + ROADMAP (row 62 `done` + deferred-sentence narrow) + router roll. **The load-bearing build-ordering insight (PLAN):** the field-type change breaks the three call sites + two config accept-tests at once, so the resolver (T1) lands FIRST additively, then T2 is atomic; `span.go` stays `[]KV`. Per the STANDING DIRECTIVE, the lifecycle advances THIS phase to its IMPL.

==============================================

## What THIS session does (the phase-62 IMPL)

Perform the IMPL stage (lifecycle-state 3 → done) for the phase-62 subject — the **tracing `request_header` custom_tag source arm** (Observability family). This EXECUTES `docs/envoy-go/phases/62-tracing-custom-tags-request-header/PLAN.md` task-by-task in a FRESH worktree off master (`feedback_git_worktrees`), subagent-driven (`feedback_execution_style` / `feedback_subagent_autocommit_claudemd` / `feedback_subagents_no_push` — each task a fresh subagent committing LOCALLY only; the controller verifies each commit, cleans leak files, squashes at stage-close, re-runs the suite on the frozen HEAD, and PUSHES). It lands the production change (lift the `request_header` reject; `[]CustomTagSpec` config model deduped first-wins; per-request `ResolveCustomTags`; three call-site threadings), the tests (resolve matrix, config accept/reject/dedup, span arm-B, Zipkin, the `0105` OTLP fixture, the fuzz seed), the BEHAVIOR_CONTRACT edit, ADR-0283 §Decision/§Consequences, and flips ROADMAP row 62 → `done`.

**The task spine (from PLAN.md — 9 tasks; execute IN ORDER, T1 before T2 for build-green):**
1. `resolve.go` — `CustomTagSpec`/`customTagKind` types (added to `config.go`) + `ResolveCustomTags` resolver + `resolve_test.go` matrix. Purely additive (build stays green). `-count=1` breaks on first-value / omit-on-missing / present-empty-not-default.
2. `config.go` ATOMIC — reshape `parseCustomTags` → `([]CustomTagSpec, error)` (request_header accept + empty-name reject + first-wins dedup) + `TracingConfig.CustomTags []CustomTagSpec` + thread the THREE call sites via `ResolveCustomTags` + update `config_test.go` (accept spec shape + request_header accept + dedup + empty-name reject; DROP the stale `request_header`-reject row). `-count=1` breaks on accept / empty-name substring / dedup.
3. `span_test.go` — CONFIRM `BuildServerSpan`/`upsertAttr` UNCHANGED + arm-B resolved-request_header-upserts-over-built-in test. `-count=1` break (always-append → count 2).
4. Zipkin encoder unit test — a resolved request_header tag in the `tags` map. `-count=1` break.
5. New OTLP fixture `0105-tracing-custom-tags-request-header` (clone `0102`; driver sends `x-trace-user`; present-case cross-side; register). Break the EXPECTATION only (`reference_vacuous_break_receiver_normalizes`); FULL `-run` selector (`reference_differential_run_selector`).
6. `FuzzHCMConfigParse` request_header seed (fuzzers stay 55; reconcile `^func Fuzz` before + after).
7. BEHAVIOR_CONTRACT edits (RE-DERIVE the drifted lines; `request_header` → CONSUMED; add empty-name reject; narrow the deferred bullet).
8. Verify — six-gate (gofmt / golangci-lint / vet / build / `go mod tidy -diff` + `git diff go.mod` / `-race` on `internal/tracing` + `internal/filter/hcm`) + the full 107-dir differential (byte-stable except `0105`).
9. ADR-0283 §Decision/§Consequences + STATE + ROADMAP (row 62 → `done`, ADR-0106; the LIVE Observability deferred sentence NARROWS `custom_tags (request_header/environment/metadata)` → `(environment/metadata)` — re-run check-(2), still THREE live matches total, the Observability one narrowed) + PROGRESS close + router roll.

Land it (controller squash + push per `feedback_push_to_origin` / `feedback_subagents_no_push`) and ROLL THIS FILE to the next router decision — the roller's next self-pick BRAINSTORM (the sentinel governs; it does NOT fire — re-check MECHANICALLY).

**Honor throughout (the discipline, unchanged):**
- **Lifecycle = one stage per session.** This session: the IMPL ONLY. After it lands: roll to the roller's next self-pick BRAINSTORM.
- **Process anchors:** ADR-0044 (ADR §Decision/§Consequences land at the IMPL — ADR-0283 here) · ADR-0045 (single flat row — 9 tasks, escape-valve UNCONSUMED) · ADR-0106 (row 62 flips `done` at THIS IMPL, SOLE leg, no parent rollup) · `reference_roadmap_split_phase_row_done`.
- **`next-prompt.txt` IS TRACKED** (`reference_next_prompt_tracked_despite_gitignore`) — edit it inside the stage worktree and fold into the stage squash; locate commits by SUBJECT (`git log --grep`), never by position.
- **Worktree discipline** (`feedback_git_worktrees`, `feedback_subagent_worktree_detach`, `feedback_subagent_worktree_path_targeting`) — pin the canonical worktree root; subagents write worktree-relative paths; on a deliberate break restore with `git restore` only (no checkout-sha/amend) + re-verify the branch each task; VERIFY the main checkout stays clean.
- **Per-task gates** (`feedback_pertask_gofmt_lint`) — each code task runs `gofmt -l` + `golangci-lint` on touched packages + `go build ./...` + touched-package `go test` before its commit.
- **The tracing traps** (`reference_tracing_custom_tag_override_builtin` — the upsert-by-key seam; custom-vs-custom is FIRST-wins dedup at parse, NOT last-wins upsert; `reference_tracing_upstream_cluster_framework_gap` — the 4 EMPTY built-ins are a SEPARATE gap, do NOT conflate; `reference_differential_fixture_dispatch_constraint`; `reference_fuzzer_count_docs_drift`; `reference_fatalf_makes_assertions_unreachable`; `reference_deliberate_break_wrong_assertion`; `reference_differential_break_protocol_count1`; `reference_sentinel_deferred_sentence_live_vs_historical`; `reference_new_subpackage_pulls_transitive_module` — re-check `git diff go.mod` after tidy).

**ADR reservation ledger (provisional; each finalized at its IMPL in landing order):** ADR-0277 = phase-59 tracing-custom-tags-literal (LANDED) · ADR-0278 = xDS 60.1 (LANDED) · ADR-0279 = HTTP/3 61.1 (LANDED) · ADR-0280 = xDS 60.2 (LANDED) · ADR-0281 = HTTP/3 61.2 (LANDED) · ADR-0282 = HTTP/3 61.3 (LANDED) · **ADR-0283 = phase-62 tracing-custom-tags-request-header** (§Context DRAFTED in SPEC §13; §Decision/§Consequences land at THIS phase-62 IMPL) · next-free unreserved **ADR-0284**.

**Current counts (VERIFIED at the phase-62 PLAN against master tip `460f761e` — RE-VERIFY at session start, `git fetch` first):** fixtures **106** (`ls -d test/fixtures/[0-9]*/ | wc -l`; tail `0104-http3-downstream-get`) · fuzzers **55** · DECISIONS tail **ADR-0282** (next-free **ADR-0283**) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** · go.mod modules **2** (`quic-go` direct + `qpack` indirect). **Anticipated at the phase-62 IMPL:** fixtures **106 → 107** (`0105`) · all other counts +0 (a seed, not a new fuzzer; +0 stats/packages/modules) · DECISIONS tail **ADR-0283**.

**Locating the phase-62 PLAN stage squash** (a router cannot name its own sha — identify by SUBJECT, never by position):
```sh
git log --oneline --grep='^phase 62 (tracing-custom-tags-request-header) PLAN' -1
```

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-62 IMPL is next; the PLAN landed, row 62 `in-progress`).
2. `docs/envoy-go/phases/62-tracing-custom-tags-request-header/PLAN.md` — the 9-task TDD spine (the File Structure table + the RE-DERIVED edit-site roster + the build-ordering box; each task's red→green + `-count=1` breaks).
3. `docs/envoy-go/phases/62-tracing-custom-tags-request-header/PROGRESS.md` — the scaffold to fill (baseline block at T1, break log + verify evidence, close at T9).
4. `docs/envoy-go/phases/62-tracing-custom-tags-request-header/SPEC.md` — the pinned design (§1.1 the first-wins-dedup amendment; §3 the change; §11 the live-probe results; §12 the edit-site roster; §13 the ADR-0283 §Context).
5. `docs/envoy-go/phases/59-tracing-custom-tags-literal/{PLAN,PROGRESS}.md` — the LANDED literal-row IMPL shape (the TDD spine template + the PROGRESS close this row mirrors).
6. `docs/envoy-go/STATE.md` — the active-phase header (phase 62 PLAN done; NEXT = the phase-62 IMPL).
7. `docs/envoy-go/ROADMAP.md` — row 62 (with the PLAN-done annotation) + the Observability family prose.
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the tracing `reference_*` traps + the differential-harness traps for the `0105` fixture.
