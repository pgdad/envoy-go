=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

✅ **PHASE 96 (`listener-default-chain-tlsmode`) IS CLOSED. ROW 96 READS `done`. LIFECYCLE-STATE IS `DONE`, AND THERE IS NO OPEN PHASE.** ⇒ **THIS SESSION OPENS A NEW PHASE WITH A BRAINSTORM** (state DONE -> 1) under `superpowers:brainstorming`. **SELF-PICK the subject** per the standing directive — smallest defensible candidate first — and record the pick AND the adjudicated rejected roster in `BRAINSTORM.md`.

⚠️ **THE STRONGEST BANKED CANDIDATE IS NAMED AND IT IS A CRASH, NOT A COVERAGE GAP: the `stat_prefix` DUPLICATE-REGISTRATION PANIC.** Two listeners sharing an HCM `stat_prefix` — a common, valid Envoy config the reference **ACCEPTS** — make envoy-go **panic rc=2** (`stats: duplicate metric registration`, `internal/stats/registry.go:107`, raised from `internal/filter/hcm/config.go:358`), **including under `-mode validate`**. It was found INCIDENTALLY while measuring the reference for phase 96 and has been banked, unchartered, for two rows. ⚠️ **RE-DERIVE ITS COST RATHER THAN RE-READING THE ADJECTIVE** (`reference_deferred_candidate_cost_restale`) — phase 74's pick was changed by doing exactly that after a nine-row-old adjective went untested. ⚠️ **AND NOTE THE PHASE-96 PRECEDENT: a banked "small" crash row measured ONE production line at the prototype and landed at +1285 `.go` additions.** The runner-up is an SDS + `alpn_protocols` fixture, whose intersection is genuinely EMPTY — it adds coverage rather than fixing a defect.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. Parses FIELDS instead of
#     matching a pattern, and asserts the row DENOMINATOR so a deleted row is
#     itself an error.
#     ⚠️ want IS 128 and ROADMAP.md IS 246 lines at the phase-96 IMPL close.
#     ⚠️ CHECK (1) IS NOW **SILENT** — row 96 flipped `done` at the phase-96 IMPL.
#     A SILENT CHECK IS INDISTINGUISHABLE FROM A BROKEN ONE: run NC-A and NC-B.
awk -F'|' -v want=128 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️⚠️ **ALL THREE CHECKS ARE NOW SILENT, AND THE ONLY THING STANDING BETWEEN THIS PROJECT AND `stop` IS CHECK (2)'s SIX.** That makes the NCs load-bearing in a way they have never been. **RUN ALL FOUR EVERY SESSION:**
```sh
# NC-A — doctor a row to `in-progress`. ⚠️ READS *ONE* LINE (`NOT DONE: row 62`) at this tip.
#        It read TWO before the phase-96 IMPL flipped row 96. MEASURED at the phase-96 IMPL close.
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md with want=128.

# NC-B — the DENOMINATOR, want=127 on the real file. ⚠️ READS *ONE* LINE —
#        `GATE FAIL: examined 128 data rows, expected 127`. It read TWO before the flip.

# NC-C — the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```
⚠️ **AND RUN THE CHECK-(2) POSITIVE CONTROL — AND READ THE COMMAND BEFORE TRUSTING ITS RESULT.** The longer phrase does not CONTAIN the shorter one as a substring, so a control that substitutes only one phrase reports a residual of 5 and reads like a finding. Substitute BOTH and assert the substitution count:
```sh
sed -e 's/deferred candidates:/deferred candidatesXX/g' \
    -e 's/remaining deferred (not-yet-chartered) candidates:/remaining deferred (not-yet-chartered) candidatesXX/g' \
    docs/envoy-go/ROADMAP.md > "$SCRATCH/c2pc.md"
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' "$SCRATCH/c2pc.md" | wc -l  # want 0
grep -o 'candidatesXX' "$SCRATCH/c2pc.md" | wc -l                                                                # want 6 — PROVE it landed
```

⚠️ **WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -c '-family row'` fails (GNU: `grep: amily row: No such file or directory`; ugrep: `option -f: cannot read amily row` — **both rc=2**) and the surrounding arithmetic prints `0`, **which reads exactly like "no change."** **Always pass `--` before the pattern**; with `--` occurrences read **96** and LINES read **68**. **`grep -c` counts LINES, NOT OCCURRENCES,** and **`grep -c` on zero matches PRINTS `0` *AND* EXITS 1, so `$(cmd || echo 0)` EMITS TWO ZEROS** — capture with `v=$(cmd || true)`.

⚠️ **THE MALFORMED-ROW FIGURE RECONCILES ONLY UNDER AN ESCAPE-AWARE FIELD COUNT — AND THE ESCAPE-AWARE COMMAND ITSELF CAN BE BROKEN.** Malformed rows are **ROW IDs 57** and **69**. **Strip `\|` BEFORE splitting.** Row 57 reads escape-aware NF=9; row 69 reads 10; rows 74, 94, 95 and 96 read **8**. ⚠️ **The correct form is `sed 's/\\|//g' F | awk -F'|' '/^\| *[0-9]/ && NF!=8'` with NO file argument to awk** — passing the file makes awk IGNORE STDIN and print the NAIVE count under the escape-aware label.

⚠️ **AND DO NOT SUBSTITUTE A "REASONABLE EQUIVALENT" FOR CHECK (1)'s AWK.** `NF>=8 && $2 ~ /^ *[0-9]+ *$/` once printed **92**, not the true denominator. **Use the form above verbatim.**

**SENTINEL RUN MECHANICALLY AT THE PHASE-96 IMPL CLOSE, BOTH BEFORE AND AFTER THIS STAGE'S `ROADMAP.md` EDIT** — because this was the first stage in three where the shapes MOVED INSIDE the session. ACTUAL output, recorded not predicted:
BEFORE: (1) **`NOT DONE: row 96`**, one line · (2) SIX · (3) SILENT · NC-A **TWO** · NC-B **TWO**
AFTER:  (1) **SILENT** · (2) **SIX** at `:206 :212 :218 :228 :234 :242` · (3) SILENT · NC-A **ONE** (`NOT DONE: row 62`) · NC-B **ONE** (`GATE FAIL: examined 128 data rows, expected 127`) · NC-C fired (0 residual) · NC-D **96 / 68** · check-(2) positive control **6 -> 0 with 6 substitutions asserted**.
Per-line md5 of the six windows, **trailing newline INCLUDED** (`sed -n 'Np' f | md5sum`) — **ALL SIX BYTE-IDENTICAL before and after this stage's edits, and back to the phase-95 SPEC close:**
`206 10d7807bf02d` · `212 4a92f7e62fc6` · `218 2a7eb298b9fd` · `228 242e53c6f7a3` · `234 b2680e6f4fbf` · `242 6caa1c3ce0e7`
⇒ **THE SENTINEL DOES NOT FIRE — NOW FOR EXACTLY ONE REASON, WHERE IT USED TO HAVE TWO.** **`stop` WAS EVALUATED AND DELIBERATELY NOT CREATED** (verified absent at the git root and in every stage worktree).
⚠️ **THE MARGIN IS NOW ONE.** ⚠️⚠️ **DELETING THE LAST DEFERRED-CANDIDATE LINE ENDS THE PROJECT. DO NOT "TIDY" ONE, AND DO NOT "FIX" THE SIX.** History `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases; **no gate may rest on a ` + ` split** (wrong on 3 of the 6 windows).
⚠️ **THE md5 DIGEST IS METHOD-SENSITIVE:** the six match ONLY with the **trailing newline included**. **State the method whenever you quote the digest.**
⚠️ **THIS RUN DOES NOT CARRY FORWARD — run all three checks, all four NCs *and* the check-(2) positive control YOURSELF, at YOUR OWN tip.**

⚠️⚠️ **NEVER RE-SPELL A SENTINEL MATCH PHRASE INSIDE A SENTINEL WINDOW** (the phase-94 rule: the sentence asserting the sentinel could not move MOVED IT, six -> seven, inside `:226`). **A BRAINSTORM registers its row `in-progress`, so it DOES edit `ROADMAP.md` — THIS RULE IS LIVE AT YOUR STAGE, and so is the unescaped-pipe trap (method note 24), which fired against the phase-96 IMPL's own author.**

- ⚠️ **LIVE FIGURES RE-DERIVED AT THIS TIP (the phase-96 IMPL close):** `ROADMAP.md` **246** lines / **128** data rows / tail row **96** (**`done`**) · `DECISIONS.md` **19050** · `^---$` **216** · `^## ADR-` **317** · bare `^## ` **325** · tail **ADR-0318** (**ACCEPTED** — the house guard is DISARMED) · next-free **ADR-0319** (`^## ADR-0319` reads **0**) · ADR-0231 decoy **1** at `:14866`, resolved to `14864 ## ADR-0231`, byte-untouched · middle-ground `^\*\*Status:\*\*.*PROPOSED` (NEVER a gate) · `BEHAVIOR_CONTRACT.md` **5991** · `STATE.md` **65** (rolled IN PLACE, net 0) · `STATE_HISTORY.md` **562** (560 -> 562, **+2**), strict **163 (DELTA 0, as promised)** / parenthetical **68** / loose **231** (**163 + 68 = 231 exactly**) · `PLAN.md` **1666** · `SPEC.md` **668** · `BRAINSTORM.md` **761** · `PROGRESS.md` **362** · phase dirs **137** · fixtures **123** (tail `0121-listener-default-chain-tls`) · fixture extractor **123 = 123**, both `comm` directions EMPTY, split **99 `driver/` + 24 `inputs/`** · fuzzers **56 targets / 48 FILES** · `go.mod` **67 REQUIRE ENTRIES** under a structural `awk` extractor (⚠️ the character-class form reads 62 — see method note 33) · `go list ./...` **239** (**237** after excluding the two Docker drivers — ⚠️ **NOT the 237/235 the phase-96 PLAN quoted; the delta is that row's two new fixture packages**) · BackendKind tail **38** · `-family row` **96 / 68** · `internal/tls/config.go` **666** · `internal/listener/manager.go` **1649** · `internal/listener/quic.go` whole-file `071cb7a536872da1605f3ef213187141b7aa19029ffd001569e4bda7845b0edb` (BYTE-UNTOUCHED across phase 96) · **D4-block literal-anchored digest `9154e453c99515d96a5d4bd9aea17fcd9b70c39682bf4418f40e0136a8ec0817` with uniqueness 1** — ⚠️ **the block now sits at `manager.go:759`, NOT `:746`; the LITERAL is the identity and the line number is derived data.**
- ⚠️ **THE SIX-GATE POSTURE WAS RUN IN FULL AT THIS CLOSE AND ALL SIX ARE RECORDED IN `PROGRESS.md` §7**: differential **123/123** (`ok … 407.357s`), the non-Docker sweep at **237** packages under `PIPESTATUS[0]` + a SET RECONCILIATION (both `comm` directions EMPTY), h2spec `95 tests, 94 passed, 1 skipped, 0 failed`, fuzzers **56 / 48**, the ANCHORED panic gate `^panic:|DATA RACE|SIGSEGV` at **0** *and PROVEN LIVE post-fix*, and no `REVIEW.md` as the standing departure. **A BRAINSTORM RUNS NONE OF THEM, AND THAT IS SCOPE, NOT OMISSION.**
- ⚠️ **THE STAT SURFACE IS QUOTED AS A DELTA AND NEVER AS AN ABSOLUTE.** Three different absolutes are live at one tip. **Phase 96 landed +0 stat NAMES** and still earned a `+0, UNCHANGED` ledger chain entry at `### Stat surface`, `BEHAVIOR_CONTRACT.md` — ⚠️ **the claim that a `+0` row earns NO chain entry is REFUTED and was wrong twice** (it cited a BLANK LINE, and phases 44.2, 44.3, 45.2, 47.1 and 51 each carry one).

==============================================

## STATUS AS OF 2026-09-08: PHASE 96 (`listener-default-chain-tlsmode`) **IMPL DONE — PHASE CLOSED**. Lifecycle-state **3 -> DONE**; ROADMAP row 96 flipped `in-progress` -> `done`. **THE SENTINEL WAS RE-EVALUATED MECHANICALLY BEFORE AND AFTER THE `ROADMAP.md` EDIT AND DOES NOT FIRE.** `stop` was NOT created. **THIS SESSION OPENS A NEW PHASE WITH A BRAINSTORM.**

**What the IMPL landed, in one paragraph.** All **19** `PLAN.md` §5 tasks, IN ORDER, in one session. **The row is ONE production line**: `internal/listener/manager.go`'s sole `tlsMode:` write, `+1 / -1`, now covering the `default_filter_chain` slot as well as the `filter_chains[]` accumulator — closing a remotely-triggerable process SIGSEGV and a QUIC stat-registration divergence with the same line. **Tasks 1-5 landed and RAN at the UN-FIXED TIP and that ordering was load-bearing**: both live-handshake crash pins aborted the binary (anchored gate **2**, trace naming the SUCCESS-path `rt.sslHandshake.Inc()`) with the POINTER assertions observed to fire BEFORE the abort, and arm (f) showed the distinguishing signature — five nil pointers, **NO crash**. Task 6 then flipped all four green in one run (RUN 157 -> **162**, FAIL 0, gate 0), and the three reverting NCs re-fired with exact discrimination. Fixture **`0121-listener-default-chain-tls`** (port **10127**, fixtures 122 -> **123**) pins the cross-side surface at N=3: `ssl.handshake` and `ssl.no_certificate` at **3**, the other three at **0**, identical on both sides. **ADR-0318 COMPLETED IN PLACE** and the house `PROPOSED` guard **DISARMED**. The false invariant's **TEN** live carriers are all reconciled.

### ⚠️ WHAT THE PHASE-96 IMPL REFUTED — SIX CLAIMS, FOUR OF THEM IN THE PLAN'S OWN TASK STEPS

**Full detail in `PROGRESS.md` §4, §5 and §9. The load-bearing four:**

1. 🔴 **`Manager.Start` DOES launch an accept loop for `kindQUIC`.** The SPEC and the PLAN (Task 4 Step 6, Task 14 Step 1) both say it does not, and that phrasing reached THREE governing documents. Measured: `Manager.Start`'s FIRST range loop (`:1148`) dispatches `kindQUIC` to `startQUIC`, which runs `go rt.quicAcceptLoop(...)` (`quic.go:88`) -> `go rt.serveQUICConnection(...)` (`:104`). What is skipped is the **TCP** accept loop — the SECOND range loop (`:1181`) `continue`s on `rt.kind == kindQUIC` — and `serveConnection`, the sole Inc site for this family, has **exactly one** call site in the package. **The conclusion survives; only the mechanism was wrong.** ⚠️ `BEHAVIOR_CONTRACT.md:1973`'s surrounding paragraph ALREADY stated the symbol-anchored version correctly and warned to find these anchors by SHAPE — **the loose paraphrase was introduced beside a correct statement and contradicted it.**
2. 🔴 **TWO NC ROSTER ROWS CANNOT FIRE AS SPECIFIED, AND THE PLAN CONTRADICTS ITSELF ON ONE.** Row 6 (*"delete `AssertStats` -> RED"*) stays **GREEN**, necessarily: the runner dispatches step 10 via `if sa, ok := d.(fixture.StatsAsserter); ok` with **no `else`** — which the PLAN's own Task 10 Step 1 states. Row 7 (*"drop one of the five names -> RED"*) also stays green, for a simpler reason: **dropping an assertion that PASSES can never redden a run.** Both were REPAIRED and both repaired forms FIRED. ⚠️ **This is `reference_deleted_zero_delta_control_is_invisible` appearing INSIDE the NC roster itself.** Task 11 Step 4's *"both `comm` directions"* is likewise impossible for a pure deletion.
3. 🔴 **THE SPLIT-GATE VERDICT'S FIRST AND HEAVIEST GROUND IS REFUTED BY MEASUREMENT.** `PLAN.md` §1.2 decided DO NOT SPLIT partly because the row was *"SMALLER than one that was not split"* — ≈+1350 against `0a985a35`'s +1635. **Measured at the close: `.go` additions +1285 (vs 1143) and code+YAML+PEM+expectations +1725 (vs 1635) — LARGER on BOTH readings.** The verdict stands on grounds 2, 3 and 4, not on ground 1. [[reference_measured_prototype_is_a_lower_bound]] fires for the **FIFTEENTH** consecutive row and this time **inverted a comparison** rather than merely widening a range.
4. ⚠️ **`go list ./...` is 239 / 237, not the PLAN's 237 / 235** — the delta is exactly this row's two new fixture packages. A stage's own edits move its own package census; §10-style figures are stale by construction.

Also refuted: **`PLAN.md` §3.5's `manager.go:1393` crash-line pin is stale after Task 6's own Step 6** (now `:1406`, same literal — a line-number pin would have read as a finding), and **the D4 block moved `:746` -> `:759`**, confirming §0.13's prediction that BOTH obvious byte-untouched guards fail against correct code.

### ⚠️ WHAT THE NEXT PHASE MUST NOT RE-DERIVE

- **The phase-96 reference measurement and `0121`'s expectation map** — they live in **ADR-0318 §Context ¶2** and `PLAN.md` §3.1, both governing records (method note 21).
- **The ten-site occurrence set is CLOSED.** Every surviving hit under `internal/` is a QUOTE-TO-REPEAL. Do not "re-fix" one.
- **D4-MIXEDTLS is NOT a bug** — ADR-0080 §Decision 3 authorises the exemption VERBATIM, and §Decision 3 is SYMMETRIC where §Consequences (c)'s illustration is one-sided.

### Banked, NOT chartered

- ⚠️ **THE `stat_prefix` DUPLICATE-REGISTRATION PANIC — the strongest banked candidate.** See the head of this file. **It deserves its own row.**
- **An SDS + `alpn_protocols` fixture — the runner-up.** Intersection genuinely EMPTY; adds coverage rather than fixing a defect.
- **D10-QUICSEL — the `quicChain()`/`quicTLSConfig()` accessor split.** Needs its own ADR (multi-chain QUIC dispatch vs ADR-0080's fallback semantics). ⚠️ **Phase 96 made the disagreement OBSERVABLE without resolving it**, by giving the shape a registered counter set.
- **D2-QUICTS — the QUIC no-`transport_socket` deferred reject.** Reference-confirmed PARITY; fails CLOSED; already banked in ADR-0317 and ADR-0318 §Consequences (e).
- **The driver-owned receiver port race — 37 files** (31 panic / 5 `fmt.Errorf` / 1 other), under `test/fixtures/*/driver/` and `*/inputs/`, NOT `test/differential/`. ⚠️ **Its trigger has still NOT fired.** Do not open it on the note alone.
- **D8-FCM's PGV-strictness nit** — a structurally invalid `filter_chain_match` inside `default_filter_chain` is PGV-rejected by the reference and silently ignored here. **UNMEASURED against a live reference.**
- ⚠️ **`ROADMAP.md:136` cites `registry.go:107` for the stat-charset panic where `BEHAVIOR_CONTRACT.md:1042` says the correct site is `:117`** — a pre-existing defect in the same cell phase 96 edited, RECORDED and deliberately NOT fixed there. A one-line candidate for a maintenance row.
- `0120/expectations.yaml`'s *"Phase 94 fixture"* first line · `manager_test.go:5571` and the other stale citations · `0108`'s three false `ssl.*` confessions · `0118/driver/driver.go:31`'s falsified band (0108-0113 is **10444-10449**) · the nine-or-twelve history-class `GetConfigForClient` comment sites (**a CONTESTED count — quote NO number**) · `0061-lb-ring-hash`'s σ-margin second occurrence · 1xx interim responses · the H/1 no-`Host` divergence · the pooled-upstream-lifetime defect · the other TEN fixed `ssl.*` names and FOUR dynamic families (blocked on NAMING; the three the reference actually emits are `ssl.ciphers.*`, `ssl.curves.*`, `ssl.versions.*` — **no `sigalgs.*` family appeared at all**).

## ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + ALL FOUR NCs + THE CHECK-(2) POSITIVE CONTROL YOURSELF** and record ACTUAL output. ⚠️ **NC SHAPES CHANGE ACROSS A ROW CHANGE — NEVER INHERIT ONE.** They changed at phase 95 (the flip), again at the phase-96 ADD in the opposite direction, and again at the phase-96 IMPL flip. **A BRAINSTORM ADDS A ROW, so they will change under you: `want` goes 128 -> 129 and NC-A/NC-B each go from ONE line to TWO.**
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** 94 IMPL twelve · 95 BRAINSTORM nine · 95 SPEC eleven · 95 PLAN seventeen · 95 IMPL eighteen · 96 BRAINSTORM thirteen · 96 SPEC ten · 96 PLAN fifteen · **96 IMPL SIX** — and the SOURCE matters more than the count: **four of the six were in the PLAN's own task steps, and one overturned its split-gate verdict.** ⚠️ **EXPECT TO BE REFUTED BY YOUR OWN REVIEW SEAM, NOT ONLY BY YOUR SUCCESSOR.** ⚠️ **AND WHEN TWO AGENTS CONTRADICT EACH OTHER, FIND THE VARIABLE RATHER THAN PICKING A WINNER.**
3. **LINE ANCHORS DRIFT; SYMBOL AND LITERAL-TEXT ANCHORS DO NOT — RE-VERIFY EVERY CITE AT YOUR TIP.** ⚠️ **PATHSPEC-SCOPE every symbol assertion and every `sed`.** ⚠️ **`internal/filter/hcm/chain.go` DOES NOT EXIST — it is `internal/filter/http/chain.go`** — but **`actions.go` IS in `hcm`.** ⚠️ **PROVEN AGAIN AT PHASE 96: `manager.go:1393` -> `:1406` and the D4 block `:746` -> `:759`, both moved by the SAME stage's own edits.**
3a. ⚠️ **AN ANCHOR CAN BE NON-UNIQUE.** **Anchor on the ENCLOSING SYMBOL.**
3b. ⚠️ **CHARACTER-CLASS AND ESCAPE SELECTORS ARE THE FAIL-UNSAFE FAMILY.** `^[0-9]{4}-` on fixtures reads 121, dropping `0007a`/`0007b` (use `ls -d test/fixtures/*/ | wc -l`) · **a CASE-SENSITIVE GREP IS BLIND TO UPPERCASE PROSE — use `-i` when auditing comments** · ⚠️ **AND `[ \t]` INSIDE A GNU ERE BRACKET EXPRESSION IS A LITERAL BACKSLASH AND `t`, NOT A TAB** — that silently made a phase-96 set-reconciliation read **0 packages** on a run that had actually reported 237. **A silent 0 from a reconciliation is indistinguishable from a broken matcher: check the matcher.** See also note 33.
3c. ⚠️ **`feedback_brief_citations_not_evidence` APPLIES TO INCIDENTAL FIGURES — including every number in THIS file and in every AGENT REPORT.**
3d. ⚠️ **NO GATE MAY REST ON A ` + ` SPLIT.**
3e. ⚠️ **A GATE COMMAND CAN MATCH PROSE INSTEAD OF CODE.** Anchor fixture-set extraction on the blank-import LINE:
```sh
extract () { grep -oE '^[[:space:]]*_ "github\.com/pgdad/envoy-go/test/fixtures/[^/]+/(driver|inputs)"$' "$1" \
  | sed -E 's#.*/test/fixtures/##; s#/(driver|inputs)"$##' | sort; }
```
**At this tip: dirs 123 = imports 123 (`test/differential/runner_test.go`), both `comm` directions EMPTY**, split **99 `driver/` + 24 `inputs/`**. **NC the extractor** (rename one import in a SCRATCH COPY — it must fire in BOTH `comm` directions). ⚠️ **A COUNT-ONLY CHECK IS VACUOUS: the import count stays constant under a rename.** ⚠️ **BUT A DELETION FIRES ONLY `comm -23`** — a pure deletion cannot manufacture an import-with-no-directory, so "both directions" belongs to the RENAME control alone (refuted at the phase-96 IMPL).
3f. ⚠️ **"AT THE RETURN" IS NOT A PLACEMENT.** Anchor installs on the ENTRY of the path that must be covered and PROVE the install with an arm that reddens without it.
3g. ⚠️ **A "CHEAP ASSERTION GAP" CAN BE A BEHAVIOUR DIVERGENCE WEARING A DISGUISE.** Measure the reference for THAT ARM before asserting any cross-side stat.
3i. ⚠️ **A LOOSE `--grep 'phase NN.*STAGE'` MATCHES THE WRONG STAGE.** **Anchor stage-commit locates on `^phase NN (slug) STAGE:`.**
3j. ⚠️ **`grep` IS A DIFFERENT PROGRAM IN A SUBAGENT SHELL THAN IN THE CONTROLLER SHELL.** In the CONTROLLER shell `grep` is a **function** resolving to **ugrep 7.8.4** which honours `.gitignore` and is therefore **BLIND to `next-prompt.txt`**; in a SUBAGENT shell `grep` is **`/usr/bin/grep` (GNU 3.11)** and sees it fine. **Both readings are correct; the variable is the SHELL, not the tree.** In the controller shell use `command grep -rl`, `/usr/bin/grep`, `git grep`, or a direct path; ⚠️ **`command grep` DOES NOT SURVIVE `xargs`.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files). **NAME THE SHELL AS WELL AS THE TOOL.**
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Live: TWO SDS dial-budget flakes plus `TestSDSEndToEnd_FetchFailure_BootFailsClosed` · the driver-owned receiver port race · `internal/httpclient` zero-value · the two 84.2-era flakes · the REFERENCE h2spec section-8 flip · `TestOutlierDetector_ConcurrentEjectExactlyOnce` · `0061-lb-ring-hash`'s σ-margin · `TestP83_StopPauseTimer_IsAuthoritativeAgainstAnEnteredClosure` (wasm, 1/400) · `internal/filter/hcm/h2 TestFramer_ReaderGoroutineDoesNotLeak`. **All were searched for in the phase-96 IMPL's own gate logs and read ZERO FAIL occurrences each.** ⚠️ **`TestServerConn_TinyWindowDelivery` IS NOT A FLAKE** — a recurrence is a REGRESSION of row 91. ⚠️ **A GREEN RERUN CLEARS NOTHING; no registration was retired.**
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only). ⚠️ **AT THE PHASE-96 IMPL A SIBLING `curl-world` SESSION STILL OWNED SEVENTEEN CONTAINERS holding `18080-19000`.** ⚠️ **A `reaper_*` testcontainers Ryuk container is created BY THE DIFFERENTIAL ITSELF and is REUSED; leave it alone.**
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`. ⚠️ **`INNER_EXIT` DOES NOT EXIST in this repo.**
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL, THEN DRIVE THE ARM.** **`gofmt -l` never exits non-zero — gate on OUTPUT.** ⚠️ **go test WITHOUT `-v` PRINTS ZERO `=== RUN`** — `RUN=0` beside `RC=0` is a VACUOUS GREEN. ⚠️ **A `-run` selector matching nothing prints `[no tests to run]` and EXITS 0.** ⚠️ **On `-v` output, unanchored `grep -c 'FAIL'` reads nonzero ON A FULLY GREEN TREE** — use `grep -cE '^(FAIL|--- FAIL)|^ *--- FAIL'`.
7b. ⚠️ **`grep -c` ON ZERO MATCHES PRINTS `0` AND EXITS 1** — capture, NEVER chain with `&&`, NEVER `$(… || echo 0)`.
7c. ⚠️ **A GREEN TEST RUN IS NOT EVIDENCE A SITE IS EXERCISED** — prove it with a **`panic()` REACHABILITY CONTROL**. **There is NO `recover()` in non-test `internal/tls` or `internal/listener`, so a panic aborts the binary — gate on `grep -cE '^panic:|DATA RACE|SIGSEGV'`.** **PROVEN LIVE TWICE at the phase-96 IMPL, once before the fix and once after.**
7d. ⚠️ **NC EVERY NEW TEST, AND NC YOUR OWN FIXES** — NEUTRALISE so the package still compiles; check the NC is EXECUTABLE. ⚠️⚠️ **AND CHECK THAT YOUR NC *CAN* FIRE AT ALL.** Phase 96 found TWO roster rows whose specified mutation was structurally incapable of reddening: deleting a SILENTLY-DISPATCHED optional interface method, and dropping a PASSING assertion. **Before running an NC, ask what mechanism would carry its mutation to a failure — if you cannot name one, the control is vacuous.**
7e. ⚠️ **A UNIT TABLE OVER A SHARED ASSERTION IS NOT A GATE ON PRODUCTION BEHAVIOUR** — `t.Helper()` gives each failure a DISTINCT call-site line, but ⚠️ **a shared `t.Helper()` body collapses every property onto ONE line**: discrimination then comes from the MESSAGE, so make each message name its property.
7f. ⚠️ **A NC THAT LEAVES YOUR NEGATIVE CONTROL GREEN IS NOT EVIDENCE THAT CONTROL DOES ANY WORK.**
7g. ⚠️ **ASSERT *WHICH* ERROR FIRED, AND ALSO WHICH DID NOT.** ⚠️ **KEEP TABLE ROWS SINGLE-CAUSE.** ⚠️ **PREFER AN EXACT EQUALITY TO A FLOOR.**
7h. ⚠️ **A PROBE CLIENT CAN WITHHOLD THE VERY THING THE ARM EXISTS TO TEST.** ⚠️ **`openssl s_client` answers "did the handshake complete", NOT "was the connection served"** — drive a real request AFTER the handshake. ⚠️ **A TEST TABLE CAN CARRY AN INVENTED INPUT NO PRODUCTION PATH PRODUCES** — build contexts with the package's OWN helpers.
7i. ⚠️ **A GATE THAT READS 0 HAS NOT BEEN SHOWN TO WORK.** Run every silent gate over an input KNOWN to trip it.
7j. ⚠️ **A PIN CAN FAIL AGAINST *CORRECT* CODE.** **Before writing an expectation map, MEASURE the leaf set a completing handshake actually books.** Phase 96 ran the `{handshake: N, rest: 0}` map deliberately as a control: it FAILED on BOTH sides, turning the warning into evidence.
7k. ⚠️ **ASSERT THE POINTERS, NOT ONLY THE EFFECT.** At phase 96 `sslHandshake == nil` was the CAUSE and the SIGSEGV the EFFECT; the pins asserted both, in that order, and the pointer assertions were observed to fire BEFORE the abort ([[reference_nil_stats_counter_inc_crashes_goroutine]]).
8. ⚠️ **`pgrep -f` / `pkill -f` MATCHES YOUR OWN SHELL AND KILLS THE TOOL CALL (exit 144)**. **Kill ONLY PIDs you captured yourself at launch.** ⚠️ **`pgrep -la a b` ERRORS** — one pattern per call.
9. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-*`, `wt-*` or stage worktree should outlive its stage close** — expect only `master` (verify with `git worktree list`).
10. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** (a) differential — **123/123**; (b) non-Docker sweep gated on `PIPESTATUS[0]` + a SET RECONCILIATION — **237 packages**; (c) h2spec `95 tests, 94 passed, 1 skipped, 0 failed`; (d) fuzzers **56 / 48**; (e) the ANCHORED panic gate, **0** *and PROVEN LIVE*; (f) no REVIEW.md — standing departure. ⚠️ **`-race` on the DIFFERENTIAL suite is VACUOUS** — the subject is an unraced subprocess; run `-race` on the FULL `internal/listener` package instead. **AN IMPL RUNS ALL SIX; A BRAINSTORM, SPEC AND PLAN RUN NONE, AND THAT IS SCOPE, NOT OMISSION.**
11. ⚠️ **THE DIFFERENTIAL'S FAILURE MODE IS A SILENT PASS.** `-count=1` IS NOT OPTIONAL. **ASSERT THE FIXTURE SET BY NAME, BOTH DIRECTIONS.** The full suite takes ~400s (measured **407.357s** at this close).
11b. ⚠️ **`go test ./...` DRIVES DOCKER — AND THERE ARE TWO DOCKER DRIVERS.** Exclude BOTH: `go list ./... | grep -vE '/test/differential$|/test/conformance/h2spec$'`.
12. ⚠️ **`-p` PUBLISHING WORKS; `--network host` IS WHAT DOES NOT.** ⚠️ **`STRICT_DNS` + `host.docker.internal` + `--add-host=host.docker.internal:host-gateway` IS REQUIRED for a container-to-host cluster HERE.** ⚠️ **THE REFERENCE CONTAINER CANNOT READ `filename:` CERT PATHS** — use `inline_string:` — ⚠️ **AND INDENT THE PEM ONE LEVEL DEEPER THAN `inline_string:`**. ⚠️ **A PROBE BACKEND MUST BIND `0.0.0.0`, NOT `127.0.0.1`.** ⚠️ **Keep PEM substitutions OUT of YAML comments.** ⚠️ **envoy-go REJECTS `match.headers`**, **refuses `codec_type: HTTP2` without TLS unless `--allow-h2c`**, **boots with `-c`, NOT `--config-path`**, **`access_log[].log_format` is BOOT-REJECTED**, **`TLSv1_0`/`TLSv1_1` in `tls_params` BOOT-REJECT**. ⚠️ **AN OMITTED `clusters` KEY BOOT-REJECTS envoy-go** — give a probe subject a placeholder STATIC cluster, and `BackendCount()` must be **≥ 1**. ⚠️ **`ENVOY_TARGET.md` is at `docs/envoy-go/ENVOY_TARGET.md`, NOT the repo root.** ⚠️ **The pinned reference is `envoyproxy/envoy:contrib-v1.37.2`, digest `7edd5b0fd763…` — PIN BY DIGEST, verify against `ENVOY_TARGET.md:3-4`** (RE-VERIFIED IN USE at the phase-96 IMPL). ⚠️ **`timeout` rc=124 is shared by a HEALTHY server and a HUNG boot — read the OUTPUT, never the code.** ⚠️ **AND envoy-go's validate mode is `-mode validate`, the reference's is `--mode validate`.**
13. ⚠️ **A RAW-FRAMER PEER MUST HOLD ONE `hpack.Decoder` PER CONNECTION.**
14. ⚠️ **`bash /dev/tcp` + `timeout cat <&3` INSIDE COMMAND SUBSTITUTION RETURNS SILENTLY EMPTY** — use Go for socket probes.
15. ⚠️ **GO DOES NOT FLAG UNUSED CONSTANTS.** Post-removal cleanup is grep-driven.
16. ⚠️ **`golangci-lint`'s misspell runs in locale US.** **Sweep British spellings in `.go` comments BEFORE the gate.** Markdown prose may use them freely.
17. ⚠️ **A GREEN RUN OF A CONTENTION-SENSITIVE TEST PROVES NOTHING.**
18. ⚠️ **AN ABSENCE CLAIM NEEDS A POSITIVE CONTROL AND A DISCRIMINATING NEGATIVE CONTROL.**
19. ⚠️ **DERIVE next-free ADR FROM THE TAIL, NEVER FROM THE HEADING COUNT.** The id space is SPARSE (exactly one gap, `0209`). The correct derivation is `grep -oE '^## ADR-[0-9]+' … | tail -1` ⇒ `## ADR-0318`, next-free **ADR-0319**, confirmed by `grep -c '^## ADR-0319'` ⇒ **0**. ⚠️ **Re-derive at your tip rather than quoting these.**
20. ⚠️ **`reference_measured_prototype_is_a_lower_bound` HAS NOW FIRED FIFTEEN CONSECUTIVE ROWS — AND AT PHASE 96 IT INVERTED A COMPARISON, NOT MERELY A RANGE.** A one-line measured production edit landed **+1285 `.go`** and **+1725** all-in, overturning the split gate's headline ground. **ENUMERATE BY GREPPING THE LITERAL SYMBOL OUTWARD, CASE-INSENSITIVELY, PATHSPEC-SCOPED, AND BY COMPILING — and treat every cost estimate as a FLOOR.**
21. ⚠️ **A MEASUREMENT THAT GOVERNS A DEFERRAL DECISION MUST LAND IN A GOVERNING DOCUMENT.**
22. ⚠️ **A STAGE'S FILE SCOPE IS ITSELF MEASURABLE, AND THE ROUTER CAN BE WRONG ABOUT IT.** `git show --numstat` on the precedent stages settles it, and **MEASURE MORE THAN ONE**. **For a BRAINSTORM, measure against the phase-94, -95 and -96 BRAINSTORM commits.**
23. ⚠️ **`git diff --stat` IS A SUM, NOT ADDITIONS.** Cost figures come from `--numstat`.
24. ⚠️ **AN UNESCAPED `|` IN A ROADMAP ROW PASSES CHECK (1) AND SILENTLY BREAKS THE FIELD COUNT.** Count fields (want **8**) under BOTH forms, BEFORE and AFTER installing. **Reword a pipe AWAY rather than escaping it.** ⚠️⚠️ **THIS FIRED AGAINST THE PHASE-96 IMPL'S OWN AUTHOR**, who wrote a Go `||` into row 96's narrative: NF went 8 -> 10 and the escape-aware malformed set grew `{57,69}` -> `{57,69,96}` **while check (1) stayed SILENT**. **LIVE at a BRAINSTORM, which adds a row.**
25. ⚠️ **THE EVICTION CHECK IS A *PAIR*, AND THE BARE FORMS DO NOT ANSWER IT.** Both bare forms read the same value on `STATE.md` and both are invariant under which entry is evicted. Only the LABEL-BOUND form discriminates, on BOTH files: the evictee's own label goes `STATE.md` **1 -> 0** and `STATE_HISTORY.md` **0 -> 1**. **VERIFIED IN BOTH DIRECTIONS at this close, with a fabricated-label NC (0 in both files) and a positive control on a sibling label that IS present (reads 1).**
26. ⚠️⚠️ **A DIRECT DATE READ CANNOT PICK THE EVICTEE, AND AT THIS CLOSE IT COULD NOT PICK ONE *AT ALL*.** All five pre-roll §Recent entries read `2026-09-07` — a TOTAL tie. The fallback is LIST POSITION, **the tail**, never a guess. Across seven consecutive closes the tie has sat in the MIDDLE, AT THE OLDEST POSITION, ABSENT, AT THE HEAD three-wide, AT THE HEAD four-wide, and now TOTAL. **Inherit no shape.**
27. ⚠️ **A SYMBOL-SCOPED CLAIM IS NOT A PATH-SCOPED ONE.** **Enumerate the call sites and check each caller's own branch.** **PROVEN AGAIN AT PHASE 96**: `Manager.Start` has FOUR `range m.runtimes` loops and only the SECOND one skips QUIC.
28. ⚠️ **A `+0/+0` NEGATIVE-CONTROL ARM CAN BE SILENTLY DELETED WITH EVERY GATE STAYING GREEN.** **Diff the ARM ROSTER, not the counters** (phase 96: `(a)(b)(c)` -> `(a)(b)(c)(d)(e)(f)`, six, none removed).
29. ⚠️ **A MULTI-INSERT LINE SHIFT IS BANDED, NOT A CONSTANT.** **Never hand a scalar shift for a multi-insert file — re-locate every cite by LITERAL TEXT (`grep -nF -- "<exact line>" <path>`).** Line numbers are derived data; the literal is the identity.
30. ⚠️ **A `go test` SELECTOR NAMING A PACKAGE THAT DOES NOT EXIST PRINTS `FAIL … [setup failed]` AND EXITS 1.** **Confirm a selector resolves (`go list ./path/...`) before believing a FAIL.** This is the mirror of the `-run` footgun, which fails the OTHER way (no match ⇒ EXIT 0).
31. ⚠️ **RECONCILE A CORRECTED CLAIM ACROSS ITS WHOLE OCCURRENCE SET.** **After correcting any claim, grep its distinctive phrase tree-wide and fix every hit, or state which hits you are deliberately leaving and why.** ⚠️ **AND SWEEP THE CLAIM, NOT ONE SPELLING OF IT, CASE-INSENSITIVELY** — phase 96's invariant had three wordings and one uppercase spelling across ten live carriers.
32. ⚠️ **A ROW CAN SHIP A FALSE FIGURE IN THE COMMIT THAT WRITES IT, AND A "CORRECTION" IS NOT A MEASUREMENT.** **NAME THE MEASURE (bytes or chars) OR THE FIGURE IS MEANINGLESS**, and **re-derive any count you quote by running the command at YOUR publishing tip, in the SAME commit as the edit that moves it.**
33. ⚠️ **A CHARACTER CLASS SILENTLY DROPS WHAT IT DOES NOT SPELL.** `grep -cE '^\s+[a-z0-9./-]+ v[0-9]'` on `go.mod` reads **62**; the count is **67**. **Prefer a structural extractor (`awk` over the `require` blocks) to a character class, and diff the two when they disagree.**
34. ⚠️ **TWO INDEPENDENT AGREEING MEASUREMENTS DO NOT MAKE A CLAIM GENERAL.** **Enumerate the class, THEN measure.**
35. ⚠️ **A GUARD'S NAME IS NOT ITS COVERAGE — AND A DEAD ASSERTION CAN BE INVERTED AS WELL AS ABSENT, OR SIMPLY WRONG.** **Read the arms, then read what each one would assert if it ran.** Phase 96 INVERTED one and DELETED its mirror, because ADR-0080 §Decision 3 authorised exactly what the mirror forbade.
36. ⚠️ **A VARIABLE REUSED FOR A SECOND PURPOSE OUTLIVES THE INVARIANT THAT MADE IT SAFE.** **When you gate a new feature on an existing boolean, prove that boolean still means what the new use needs, and NAME THE ADR THAT COULD REPEAL IT.** This was phase 96's root cause: `anyTLS` was correct as a mixed-TLS accumulator and phase 74 reused it as the stat-registration gate, inheriting an invariant ADR-0080 had already repealed at phase 07.2 — **twenty-two rows earlier.**
37. ⚠️ **A FIX SHAPE CAN ENCODE A PARITY ANSWER NOBODY HAS MEASURED.** Two repairs that both stop a crash but disagree about the resulting stat surface are NOT interchangeable, **and diff size is not the tiebreaker. Measure the reference, then choose.**
38. ⚠️ **AN OCCURRENCE SET CAN BE NORMATIVE, NOT MERELY COMMENTARY.** **Sweep `docs/` with the same case-insensitive breadth as the code, resolve every markdown hit to its enclosing ADR by BACKWARD heading search, and say which hits you are deliberately leaving and why.**
39. ⚠️ **A BARE-TOKEN GREP'S FALSE POSITIVES ARE CONFIG-DEPENDENT, SO A KNOWN WITNESS IS NOT A GATE.** **Anchor (`^listener\.[^:]*\.ssl\.`) and NC the anchored form against the arm that must read zero.** Measured at phase 96: unanchored `grep -c ssl` reads **4 on the reference** and **1 on the subject** for the SAME config pair — both witnesses appear simultaneously, one per side.
40. ⚠️ **A COUNT WITHOUT ITS MATCHER IS MEANINGLESS, AND TWO CORRECT COUNTS CAN DISAGREE.** The reference's listener-scope `ssl.*` family reads **17** post-drive under the anchored form and **14 AT BOOT** — the three latecomers are the dynamic families. **Name the matcher AND the moment beside every figure.**
41. ⚠️ **AN ILLUSTRATION IN §CONSEQUENCES IS NOT THE LIMIT OF A §DECISION.** ADR-0080 §Decision 3 authorises an independent default-chain TLS posture in BOTH directions; its §Consequences (c) illustrates only ONE, and the shape that crashed was the other. **Read the decision, not the example.**
42. ⚠️ **A SECTION HEADED *"RE-DERIVED AT THIS STAGE'S OWN TIP"* IS FALSE IF IT NAMES A COMMIT THAT IS NOT THE ONE IT SHIPS ON.** **A stage's own edits are part of its tip. Re-measure AFTER writing, in the SAME commit** ([[reference_row_can_ship_false_figure_at_birth]]).
43. ⚠️ **AN ENUMERATION'S EXCLUSION RULE CAN HAVE A HOLE ITS AUTHOR DID NOT SEE.** **State the exclusion as a PREDICATE and then test every live file against it**, rather than listing the directories you happen to be thinking of.
44. ⚠️ **A TEST-STRUCTURE ASSUMPTION IS A CLAIM AND MUST BE READ, NOT PRESUMED.** **Before writing a task against a test, grep it for `[]struct{`, `for _, tc := range` and `t.Helper()`, and say what you found.**
45. ⚠️ **A REGISTRATION-TIME SYMBOL IS NOT A CONSTRUCTION-TIME SYMBOL.** `registerListenerMetrics` runs at `Manager.Start`, from TWO call sites, never at `NewManager`. **Enumerate the call sites and ask WHEN each runs, not only WHERE it is.**
46. ⚠️ **A CROSS-SIDE PIN ON A NAME ONE SIDE NEVER EMITS IS SILENTLY VACUOUS, NOT RED.** **Before pinning a stat cross-side, prove the NAME exists on both sides.** `no_filter_chain_match` has NO production site in this repo and is deliberately UNasserted in `0121`.
47. ⚠️ **A LIFECYCLE STATE DEFINED BY WORK REMAINING IS RE-ENTRANT, AND THE ROUTER MUST SAY SO OR A SHORT SESSION WILL FALSELY CLOSE.** The discriminator is `PROGRESS.md` and the ROADMAP row, **not the session boundary.** ⚠️ **Evaluate the context budget BEFORE starting a task, not after** (`checking-context-budget`). The phase-96 IMPL ran all 19 tasks in one session and did not need the re-entrant path, but it existed and was correct to state.
48. ⚠️ **PARALLELISE ACROSS DISJOINT FILE SETS, NOT INSIDE ONE.** The phase-96 IMPL ran THREE worktrees on THREE branches concurrently — unit tests (`internal/listener/`), the fixture (`test/`), and the documents (`docs/`) — with Docker serialized to exactly ONE agent. All three merged with ZERO conflicts. ⚠️ **The load-bearing ORDER (Tasks 1-5 before Task 6) was kept STRICTLY SERIAL inside its own worktree; only genuinely independent work was parallelised.**

## ⚠️ THE `PROPOSED` GUARD — **DISARMED AT THE PHASE-96 IMPL**

| form | now | where |
|---|---|---|
| `^> \*\*STATUS: PROPOSED` — **the house form** | **DISARMED** | ADR-0318 was flipped IN PLACE to `ACCEPTED` at the phase-96 IMPL (Task 15). ⚠️ **A ZERO HERE IS THE RESTING STATE, NOT EVIDENCE THE GUARD IS BROKEN** — PROVEN LIVE at that close on a scratch copy (append a house-form line ⇒ it moves by exactly one). **A SPEC that drafts a new ADR §Context RE-ARMS it.** |
| `^\*\*Status:\*\* PROPOSED` — **the ADR-0231 decoy** | unchanged, reads 1 | `:14866`, resolved BY BACKWARD HEADING SEARCH to `14864 ## ADR-0231`, byte-untouched (line sha256 verified identical across phase 96) |
| `^\*\*Status:\*\*.*PROPOSED` — **the middle ground** | a large number | ⚠️ **NEVER A GATE** |

⚠️ **VERIFY BY LINE AND BY ADR, NEVER BY THE COUNT ALONE** — resolve any hit to its enclosing ADR by a BACKWARD heading search (`awk 'NR<=<HITLINE> && /^## ADR-/ {h=$0; n=NR} END {print n, h}'`). ⚠️ **NEVER gate on the UNANCHORED form.** ⚠️ **AND DO NOT QUOTE A COUNT FOR EITHER GATE FORM IN PROSE THE GREP MATCHES** — the phase-93 SPEC falsified itself doing exactly that. `DECISIONS.md` at this close: **19050**, `^---$` **216**, `^## ADR-` **317**, bare `^## ` **325**, tail **ADR-0318**, next-free **ADR-0319**. ⚠️ **A BRAINSTORM ADDS NO ADR — every one of those figures must STAY across your stage.**

## ⚠️ THE ARCHIVE GUARD

⚠️ **THE STRICT `163` IS NOT THE ENTRY COUNT.** Re-measured at THIS tip (phase-96 IMPL close, `STATE_HISTORY.md` **562**): loose **231** = strict **163** + parenthetical **68**, exactly — **the triple reconciles only under these ANCHORED-OCCURRENCE forms:**
```sh
H=docs/envoy-go/STATE_HISTORY.md
grep -oE '^- \*\*prior active-phase:\*\* ' $H | wc -l   # strict         163  <- DELTA 0 across a correct close
grep -oE '^- \*\*prior active-phase \('    $H | wc -l   # parenthetical   68  <- +1 per close
grep -oE '^- \*\*prior active-phase'       $H | wc -l   # loose          231  = 163 + 68
```
⚠️ The unanchored `grep -c 'prior active-phase'` counts LINES and coincides by accident; the colon form is a FOURTH regex. **Name the form or the figure is meaningless.** ⚠️ **`grep -F` WITH A PATTERN STARTING `- ` HITS THE FLAG TRAP** — pass `--` first. **The guard is SOUND as a DELTA-0 SHAPE check** (a correctly-shaped parenthetical append moves the STRICT form by 0 — verified again at this close) **but the number is a SUBSET.**

⚠️ **THE ARCHIVE'S POSITIVE CONTROLS ARE SELF-INCREMENTING.** This stage's archive line names NO control figure. ⚠️ **AND THE RAW LINE DELTA IS `+2`, NOT `+1`** — the entry is a blank line PLUS the entry line. **The phase-96 IMPL close: `STATE_HISTORY.md` 560 -> 562**, evicting the phase-95 SPEC entry by LIST POSITION (the tail), with the strict guard DELTA 0 exactly as promised. ⚠️⚠️ **THE DATE TIE WAS TOTAL — ALL FIVE pre-roll §Recent entries read `2026-09-07`, so a direct date read could not pick an evictee AT ALL.** See method note 26. ⚠️ **THE BARE FORMS ANSWER NOTHING** — both read FIVE on `STATE.md` and both are invariant under which entry is evicted. **Only the LABEL-BOUND PAIR discriminates**, run on BOTH files, with a fabricated-label NC and a positive control on a sibling label that IS present.

## ⚠️ PORT BANDING

`net.ipv4.ip_local_port_range = 32768 60999`. ⇒ **BAND YOUR PROBE PORTS BELOW 32768.** ⚠️ **The differential harness reserves `20000..31007` (`harness_test.go:292-296`) and `11000..14999` (`:209-212`)**, so `15000-19000` is the ad-hoc band — ⚠️ **minus `18080-19000`, held by sibling `curl-world` containers.** **The phase-96 IMPL used `15500-15599` cleanly.** **Check availability with `ss -tan` AND `ss -uan` (ALL states), never `ss -ltn`.** ⚠️ **FOR A UNIT TABLE, USE PORT 0.** ⚠️ **`net.Pipe` DEADLOCKS a client-cert handshake — use a loopback TCP pair.** ⚠️ **FIXTURE ports follow the `10<index>` family band, BUT THE BAND IS NOT INDEX-ALIGNED WHERE IT COLLIDES**: `0028` holds `10120`-`10125`, `0120` holds `10126`, and **`0121` holds `10127`**. ⚠️ **The TLS/SDS fixtures `0108`-`0113` hold `10444`-`10449`, so `10450` is one PAST that band.**

## ⚠️ A RECURSIVE `grep` IN THE *CONTROLLER* SHELL IS BLIND TO `next-prompt.txt`

See method note **3j** — this is SHELL-DEPENDENT. In the controller shell `grep` is a **function** (ugrep 7.8.4, honours `.gitignore`); `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. `command grep -rl …` / `/usr/bin/grep` (GNU) sees it; direct-path access works. **In a SUBAGENT shell the bare `grep` is `/usr/bin/grep` and already sees it.** ⚠️ **`command grep` DOES NOT SURVIVE `xargs`.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files). ⚠️ **ugrep ALSO REJECTS SOME COMPLEX `grep -oE` PATTERNS with `exceeds complexity limits` where GNU grep accepts them.**

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 96 (listener-default-chain-tlsmode)' --format='%h %s'   # the SLUG form
git log --grep '^phase 96' --format='%h %s'                                    # the LOOSE form — RUN IT to CROSS-CHECK
```
⚠️ **RUN BOTH FORMS AND RECONCILE THE SETS. NO COUNT IS QUOTED HERE, DELIBERATELY — BOTH FORMS ROT, AND A SLUGLESS CORRECTION COMMIT IS INVISIBLE TO THE ANCHORED FORM** (the live witness is `963d472d`). ⚠️ **A `.*SPEC` / `.*PLAN` STYLE LOOSE GREP MATCHES THE WRONG STAGE** (method note 3i). **Take the tip from `git rev-parse master`.** ⚠️ **AND WHEN YOU LAND A CORRECTION, PUT THE FULL SLUG IN ITS SUBJECT.**

Execution style: **subagent-driven per `feedback_execution_style`**. Subagents commit LOCALLY on their own stage branch with EXPLICIT PATHSPECS; the controller merges and squashes (`feedback_subagents_no_push`, `feedback_push_to_origin`). **Pure MEASUREMENT agents should commit NOTHING and prove their tree clean.** ⚠️ **PARALLEL AGENTS IN ONE WORKTREE RACE THE GIT INDEX** — per-agent worktrees on their own branches, private scratch dirs, **Docker serialized to exactly ONE agent at a time** (see method note 48 for how the phase-96 IMPL ran three concurrently with zero conflicts). ⚠️ **VERIFY WHAT A SUBAGENT REPORTS — BUT ALSO BELIEVE IT WHEN IT REFUTES YOU.** At the phase-96 IMPL an agent refuted the controller's own brief on `Manager.Start`'s QUIC accept loop; the controller verified it against the source and corrected three governing documents. ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root** — build with `-o` into scratch.

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root — **it fired repeatedly again at the phase-96 IMPL.** Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` before any commit. ⚠️ **Shell VARIABLES do not survive between Bash calls** — re-export in every command. ⚠️ **AN UNQUOTED HEREDOC (`<<EOF`) COMMAND-SUBSTITUTES BACKTICKED TEXT** — use `<<'EOF'`.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE OR PROTOTYPE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — `sha256sum` BEFORE patching, `git checkout -- <path>` after, `sha256sum -c` the capture — ⚠️ **and COMMIT FIRST if anything else is pending, because `git checkout --` restores from HEAD.** ⚠️ **Also check the MAIN repo root, and `--untracked-files=all`.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads OPEN THE NEXT PHASE WITH A BRAINSTORM.** ⚠️ **Its §Recent is FIVE entries under the ADR-0288 cap.**
2. `BOOTSTRAP_PROMPT.md` **at the repo root** — §5 the lifecycle state machine (**state DONE ⇒ open a new phase with `superpowers:brainstorming`**) and §6.1's split trigger. ⚠️ **§5 appears VERBATIM TWICE**, §5 and the §11 appendix; **the second copy's offset is NON-constant; find it by heading, never by arithmetic.**
3. `docs/envoy-go/ROADMAP.md` — the deferred-candidate lists behind check (2)'s SIX, and the family sections. ⚠️ **Your BRAINSTORM ADDS row 97 and registers it `in-progress`**, which moves `want` 128 -> 129 and `ROADMAP.md` 246 -> 247, and changes NC-A and NC-B from ONE line to TWO. **MEASURE, DO NOT PREDICT.**
4. `docs/envoy-go/phases/96-listener-default-chain-tlsmode/PROGRESS.md` — **362 lines. THE CLOSE OF THE LAST ROW**, and the model for what an IMPL owes: §4 (two NC roster rows that could not fire), §5 (the six refutations), §7 (the six gates, with departures named), §9 (the split-gate ground that measurement overturned).
5. `docs/envoy-go/DECISIONS.md` **ADR-0318** (COMPLETE) and **ADR-0080** §Decision 3 — ⚠️ **ADR-0080 AUTHORISES EIGHT OF THE TEN listener divergences. DO NOT "FIX" D4.**

## What the next session owes
**OPEN A NEW PHASE WITH A BRAINSTORM (lifecycle DONE -> 1)** — ⚠️ **ONE STAGE PER SESSION** · **SELF-PICK the subject** per the 2026-07-12 standing directive, smallest defensible candidate first, and record the pick AND the fully-adjudicated rejected roster in `BRAINSTORM.md` · ⚠️ **RE-DERIVE every banked candidate's COST rather than re-reading its adjective** (`reference_deferred_candidate_cost_restale`) — and treat every estimate as a **FLOOR**, because method note 20 has now fired fifteen consecutive rows and at phase 96 it INVERTED a comparison · ⚠️ **THE STRONGEST BANKED CANDIDATE IS THE `stat_prefix` DUPLICATE-REGISTRATION PANIC** — a config the reference ACCEPTS that panics envoy-go rc=2, including under `-mode validate` · **ADD row 97 to `ROADMAP.md` and register it `in-progress`** — ⚠️ **count row fields (want 8) under BOTH forms BEFORE and AFTER installing, and REWORD A PIPE AWAY rather than escaping it** (method note 24 fired against the last stage's own author) · ⚠️ **NEVER RE-SPELL A SENTINEL MATCH PHRASE INSIDE A SENTINEL WINDOW — LIVE AT YOUR STAGE** · ⚠️ **RE-RUN the sentinel + all four NCs + the check-(2) positive control BEFORE *AND AFTER* YOUR `ROADMAP.md` EDIT** — the shapes MOVE INSIDE YOUR SESSION, in the opposite direction from last time · ⚠️⚠️ **THE MARGIN IS NOW ONE: checks (1) and (3) are BOTH SILENT and only check (2)'s SIX stands between this project and `stop`. DO NOT "TIDY" A DEFERRED-CANDIDATE LINE.** · **A BRAINSTORM adds NO ADR and touches NO `.go`** — `DECISIONS.md`'s five structural figures must STAY · `STATE.md` rolled IN PLACE with the oldest §Recent entry evicted — ⚠️ **use LIST POSITION and the LABEL-BOUND PAIR across BOTH files; the dates cannot pick an evictee** — and archived to `STATE_HISTORY.md` as **ONE INLINE LINE** in the PARENTHETICAL form, **strict guard 163 DELTA 0**, raw delta **+2**, ⚠️ **name NO positive-control figure in the archive line** · **ROLL THE §Recent PREAMBLE SENTENCE TOO, without spelling the evictee's label** · ⚠️ **RE-DERIVE ANY LINE COUNT YOU QUOTE IN `STATE.md` IN THE SAME COMMIT AS THE EDIT THAT MOVES IT** · `next-prompt.txt` rolled (`git add -f`) · ONE squashed commit, merged and pushed · **REFUTE YOUR PREDECESSOR BY EXECUTION** and record it — ⚠️ **the IMPL refuted SIX, four of them inside the PLAN's own task steps, so re-derive every figure you quote from any document in this repo, including this one.**
