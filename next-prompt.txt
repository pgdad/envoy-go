START a fresh cold-start session for envoy-go — **the phase-34 (`load-balancer-least-request`) BRAINSTORM is DONE; the next lifecycle step is the phase-34 SPEC** (`superpowers:writing-plans` scoped to SPEC authoring, per the established BRAINSTORM → SPEC → PLAN → IMPL cadence — the kafka-31/thrift-33 single-flat-row precedent). Phase 34 OPENS the **Load-balancing family** (the first new feature family since the §9 Network-filters family closed at phase 33): subject `least_request` (`Cluster.LbPolicy LEAST_REQUEST` + config message `Cluster.LeastRequestLbConfig`, `envoy.config.cluster.v3`).

The literal live master tip is **this brainstorm commit** (`phase 34 BRAINSTORM: load-balancer-least-request — the Load-balancing family OPENS …` — confirm via `git log --oneline -1`; it carries `docs/envoy-go/phases/34-load-balancer-least-request/BRAINSTORM.md` + the ROADMAP row-34/family-OPEN registration + the STATE advance + this file). Below it sit the prior tips: the docs-only routing commits `e1da5b7`/`6721618`, the CI-fix merge **`4252c1b`** (PR #1 — test/harness/workflow-only), and the substantive phase-33 IMPL squash **`97aefd6`** (thrift_proxy; the §9 family CLOSED).

## What this session does — author the phase-34 SPEC

Execute the BRAINSTORM §10 **D-L1..D-L7** empirical pins IN-SESSION (parallel-subagent fan-out per the 25–33 SPEC precedent), live against the contrib reference Envoy `envoyproxy/envoy:contrib-v1.37.2` (ADR-0227) per `reference_docker_probe_bridge_network` (a shared docker bridge network + STRICT_DNS backend hostnames + verify traffic actually ran via `downstream_cx_rx_bytes_total > 0`):

- **D-L1** — re-pin the v1.32.4 proto surface of `Cluster.LeastRequestLbConfig` + PGV. The BRAINSTORM already VERIFIED it in the go module cache: EXACTLY three knobs — `choice_count` (`UInt32Value`, PGV ≥ 2 when set, default 2) + `active_request_bias` (`RuntimeDouble`) + `slow_start_config` — and **NO `enable_full_scan` / NO `selection_method`** on this surface (those live only in the `load_balancing_policies.least_request.v3` extension proto, NOT in scope). ZERO new go.mod dep; confirm `go mod tidy` clean.
- **D-L2** — what "active request" means for TCP-proxied connections in the reference (cx-as-rq?); probe with held-open conns; pins the per-endpoint counter semantics + the `0059` skew-arm design.
- **D-L3** — FULL_SCAN availability + tie-break semantics: the settled proto surface cannot express it (D-L1) → the strong prior is FULL_SCAN DEFERRED + the deterministic exact-count fixture arm DROPPED; the probe finalizes the `0059` arm roster.
- **D-L4** — the stat-surface delta (anticipated ZERO new names — surface stays 1116); scrape the reference `/stats` under LEAST_REQUEST; pin the `StatsAsserter` counter set + cross-equal-vs-per-side disposition.
- **D-L5** — the accept/reject matrix + the manager reject-text blast radius (`internal/cluster/manager.go` ~line 216 today rejects all but ROUND_ROBIN with `"only ROUND_ROBIN lb_policy supported"` — accepting LEAST_REQUEST forces the byte-stable-reject text change; enumerate every pinning test/fixture, `internal/cluster/manager_test.go` is a known pinner; the deferred-knob parse-accept-vs-reject disposition matched against the reference).
- **D-L6** — P2C sampling semantics (with/without replacement; tie-break; `choice_count >= len(endpoints)`; injectable RNG for unit tests) — per-side/unit-level ONLY (RNG never cross-side-comparable).
- **D-L7** — the ADR-0045 envelope re-check (~300–600 prod LoC / ~10–14 tasks anticipated; the pre-authorized 34.1-seam/34.2-policy escape valve consumed only if tripped) + the exact exported seam surface (release-handle vs PickAcquire/Release; the direct-`PickEndpoint` consumer sites `internal/httpclient/httpclient.go:280` + the thriftproxy probe; release-on-dial-failure; confirm ADR-0024 needs no amendment).

Then anchor the **ADR-0232** (the LB acquire/release seam — the project's first framework seam outside `internal/filter/network`; the `loadBalancer` interface in `internal/cluster/loadbalancer.go` extends from stateless `Pick() (Endpoint, error)` to acquire/release; per-endpoint LB-owned atomic active counters; release on conn close via the dial path; `roundRobin` behavior-neutral; every future family row reuses it) + **ADR-0233** (the least_request policy — un-weighted P2C over `choice_count` sampled hosts) **§Context drafts** (DECISIONS tail STAYS ADR-0231; bodies land at the IMPL per ADR-0044). The **spec-document-reviewer gate applies**. Counts stay UNCHANGED at the SPEC (they move only at the IMPL: fixtures 60 → 61 anticipated [`0059-lb-least-request`]; DECISIONS tail → ADR-0233; stat surface / fuzzers / BackendKind UNCHANGED — NO new fuzzer [no wire decode — the FIRST no-fuzzer phase since the fuzzing regime began, DELIBERATE] and NO new BackendKind [the `0001-tcp-proxy-rr` echo backends reused], both recorded at the BRAINSTORM as family-level proof-shape novelties; the differential proof is the band-based per-side `DistributionAsserter` skew arm + a cross-side `StatsAsserter` prong).

## Counts at master tip (phase 34 in-progress, BRAINSTORM done — they advance only at the phase-34 IMPL)

- stat surface **1116** (BEHAVIOR_CONTRACT doc count; +25 at phase 33 = 24 thrift counters + 1 gauge `request_active`; the `request_time_ms` histogram DEFERRED per ADR-0060, not counted). Phase 34 anticipates ZERO delta (D-L4 pins).
- differential fixtures **60** (tail `0058-thrift-boot-reject`; `ls -d test/fixtures/[0-9]* | wc -l`). Phase 34 anticipates 60 → 61 (`0059-lb-least-request`) at the IMPL.
- fuzzers **42** (tail `FuzzThriftDecode`; `grep -rh "^func Fuzz" $(find ./internal -name fuzz_test.go) | wc -l`). Phase 34 adds NONE (deliberate).
- BackendKind tail **33** (`TCPThriftResponder`; `test/differential/fixture/fixture.go`). Phase 34 adds NONE (deliberate).
- DECISIONS tail **ADR-0231** (the thrift_proxy filter — ACCEPTED; next-free **ADR-0232**). Phase 34 anticipates ADR-0232 (seam) + ADR-0233 (policy); §Context at this SPEC.
- reference pin: `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227).
- Toolchain: Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); go-control-plane `/envoy` v1.32.4 (ADR-0008) + `/contrib` v1.32.4 (kafka, phase 31).
- conformance h2spec 53/53 + proxy-wasm 10/10.

## Read first (in order)

1. THIS FILE — the post-phase-34-BRAINSTORM cold-start (next: the phase-34 SPEC).
2. `docs/envoy-go/STATE.md` — active-phase `phase 34 (load-balancer-least-request) BRAINSTORM done`; lifecycle-state `phase 34 BRAINSTORM done — SKILL_ROUTING → the phase-34 SPEC (single flat row; the Load-balancing family OPENS at phase 34)`; the counts.
3. `docs/envoy-go/ROADMAP.md` — the Load-balancing family § (FAMILY OPEN at phase 34; least_request IN-PROGRESS; 7 candidates remain) + row 34 (the envelope/seam/ADR/proof-shape summary); the Schema/Invariants for row discipline.
4. `docs/envoy-go/DECISIONS.md` — the tail ADR-0231 + ADR-0106 (family flat-row discipline) + ADR-0045 (the split-gate) + ADR-0044 (ADR-body-in-place policy) + ADR-0052 (the atomic-landing six-gate) + ADR-0060 (histograms deferred) + **ADR-0024** (the per-cluster RR counter-scope ADR — the existing LB-state-scope decision whose territory the acquire/release seam touches; no amendment anticipated, the SPEC confirms) + ADR-0063 (the Inc-then-wrap `connWithGauge` Close-once discipline — the anticipated release attach-point) + ADR-0230 (the build-at-first-consumer seam-scoping precedent the ADR-0232 seam transplants).
5. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the as-built surface (the stat-name mapping count 1116; the TCP-proxy/RR subsection — the existing LB behavioral boundary the new policy sits beside).
6. `docs/envoy-go/phases/34-load-balancer-least-request/BRAINSTORM.md` — **the worked record** (the 12 §§: the settled Q0/Q1/Q-seam/Q-split, the v1.32.4 three-knob verification + the FULL_SCAN-knob-ABSENT finding, the D-L1..D-L7 pin definitions, the §9 cross-reference pickups [the phase-02 SPEC LB deferral retired], the lessons).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `reference_docker_probe_bridge_network` (the probe discipline), `reference_differential_break_protocol_count1` (deliberate-break liveness with `-count=1` — doubly load-bearing for BAND assertions), `reference_differential_asserter_dispatch` + `reference_differential_fixture_dispatch_constraint` (asserter/fixture dispatch), `feedback_execution_style`, `feedback_git_worktrees`, `feedback_subagents_no_push`, `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_detach`, `feedback_subagent_worktree_path_targeting`.

## Honor throughout

- `feedback_execution_style` (subagent-driven) · `feedback_git_worktrees` (work in a worktree) · `feedback_subagents_no_push` (subagents commit LOCAL-ONLY; the controller squash-merges + pushes at stage-close) · `feedback_push_to_origin` (after a clean landing on master with tests green, push without asking) · `feedback_pertask_gofmt_lint` (each task runs `gofmt -l` + `golangci-lint run` on touched packages) · `feedback_subagent_worktree_path_targeting` (pin canonical paths; verify the main checkout stays clean).
- ADR-0106 (family flat-row discipline; row 34 is a flat row — NO sub-rows unless the 34.1/34.2 escape valve is consumed; sibling family rows are NOT pre-populated) · ADR-0045 (the split-gate, re-checked at SPEC [D-L7] and PLAN) · ADR-0044 (ADR §Decision/§Consequences bodies land at the IMPL) · ADR-0052 (the atomic-landing six-gate) · ADR-0060 (histograms deferred).
- The §9 Network-filters family is CLOSED (phase 33) — do NOT reopen it; **phase 34 OPENS the Load-balancing family** (least_request first; {random, ring_hash, maglev, subset LB, locality-weighted LB, priority load balancing, panic thresholds} remain). The lifecycle is BRAINSTORM (done) → **SPEC (this session)** → PLAN → IMPL, each its own session per the established cadence.
