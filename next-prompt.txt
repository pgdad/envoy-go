=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, the active phase is fully landed, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action. (The sentinel does NOT apply right now — phase 48 is chartered, its BRAINSTORM is LANDED, and the next stage is its SPEC.)

**STATUS AS OF 2026-06-30: phase 48 (`stats-sink-statsd`) BRAINSTORM is LANDED; the NEXT stage is its SPEC.**
- The phase-48 BRAINSTORM landed: `docs/envoy-go/phases/48-stats-sink-statsd/BRAINSTORM.md` (the 12-section shape; lifecycle-state 0 → 1; docs-only direct-on-master). Row 48 registered `in-progress` in `ROADMAP.md`; STATE.md active-phase header rolled. **Re-verify with `git log --oneline -6` + `git status`; do NOT assume.**
- The BRAINSTORM settled the SCOPE via a 2-question user dialogue: **Q1 transport → UDP `address` ONLY** (the canonical statsd default; envoy-go's FIRST UDP datagram seam; `tcp_cluster_name` DEFERRED + strict-rejected this row); **Q2 phasing → SINGLE FLAT ROW** (the phase-47 flush substrate already exists — no new framework piece, no second subsystem to couple; ADR-0045 escape-valve UNCONSUMED).
- Counts at BRAINSTORM (re-verify, do NOT assume): stat surface **1200** (non-H2 **1196**) / fixtures **93** / fuzzers **50** / BackendKind **38** / DECISIONS **ADR-0264** (next-free **ADR-0265**).

=> The NEXT action is the **phase 48 (`stats-sink-statsd`) SPEC** (lifecycle-state 1 → 2). NOT the termination sentinel. NOT a new BRAINSTORM.

==============================================

## The NEXT action — the phase 48 (`stats-sink-statsd`) SPEC
Author `docs/envoy-go/phases/48-stats-sink-statsd/SPEC-48.md` (the 14-section SPEC shape — mirror `docs/envoy-go/phases/47-stats-sink-metrics-service/SPEC-47.1.md` in git history). Docs-only direct-on-master (the family-row SPEC precedent). Lifecycle-state 1 → 2. ANCHOR the **ADR-0265 §Context DRAFT** (§13; §Decision/§Consequences land in-place at the IMPL per ADR-0044).

**Subject (frozen at the BRAINSTORM):** the bootstrap `stats_sinks[]` `envoy.config.metrics.v3.StatsdSink` (UDP `address` variant) — a periodic UDP statsd-line-protocol stats sink over the LANDED phase-47 `Flusher`/`Sink` flush subsystem. A NEW `StatsdSink` `Sink` impl in the EXISTING `internal/statssink` package (UDP datagram seam + line mapping) consuming the SAME `[]*dto.MetricFamily` snapshot the metrics_service sink does; lift the StatsdSink TypeURL from the `bootstrap.go:430` sibling-reject; main.go fans the Flusher out to BOTH sinks.

**The SPEC's load-bearing job — EXECUTE the §10 D-SD-* live pins against `envoyproxy/envoy:contrib-v1.37.2`** (`reference_docker_probe_bridge_network` — a shared Docker bridge + a decode-ran proof; `reference_stats_sink_metrics_service_probe` — name-subset not whole-set, no self-stats). The probe runs in the scratchpad, torn down clean. The pins (from BRAINSTORM §10):
- **D-SD-DELTA (LOAD-BEARING):** is the reference statsd `|c` a delta-since-last-flush (the canonical statsd increment) or absolute? DECIDES whether the `StatsdSink` owns a sink-local last-value delta state (the 47.2a `internal/statssink/delta.go` `deltaState` PATTERN, sink-private) AND whether `0092` asserts the delta-SUM with a post-convergence stability barrier (`reference_delta_sink_differential_stability_barrier`) or 0089's absolute `value==K` last-seen. PROBE DECIDES — do NOT pre-assume.
- **D-SD-NAME:** confirm the metric-name projection is the full dotted internal name with tags INLINED (NOT tag-expanded — the proto "does not support tagged metrics") + the EXACT `prefix` join (`prefix + "." + name`; default `envoy`). The load-bearing differential surface.
- **D-SD-LINE:** the exact line format — integer value, the `|c`/`|g` suffixes, one-datagram-per-line vs newline-batched packing, gauge `+`/`-` delta-gauge conventions.
- **D-SD-LIFECYCLE:** a synchronous UDP write vs a bounded-channel writer-goroutine (the `MetricsServiceSink` shape); the write-error drop-log posture.
- **D-SD-STATS:** confirm +0 statsd-sink self-stats (the metrics_service precedent).
- **D-SD-REJECT:** does the reference accept/reject a standalone `tcp_cluster_name`, a missing address, a non-UDP socket protocol? (envoy-go-strict rejects vs reference-parity.)

**Anticipated (the SPEC PINS these; the BRAINSTORM did NOT freeze them):** a NEW `parseStatsdSinkConfig` dispatch arm + a `StatsdSinkConfig{UDPAddress, Prefix}` struct in `internal/bootstrap` (the reject text now names BOTH supported sinks — `reference_strict_reject_sibling_typeurl_gap`) + a NEW `internal/statssink/statsd.go` `StatsdSink` emitter (UDP seam + line mapping; sink-local `deltaState` IFF D-SD-DELTA confirms deltas) + the `cmd/envoy-go/main.go` sink-slice wiring + `FuzzStatsdSinkConfigParse` + the `0092-stats-sink-statsd` cross-side differential (a DRIVER-OWNED UDP statsd-line receiver — NOT a BackendKind per `reference_differential_grpc_receiver_driver_owned`; TWO per-side receivers + hard `Close()` per `reference_periodic_sink_differential_two_receivers`). Anticipated counts at the IMPL: stat **1200** (+0) / fixtures **94** (`0092`) / fuzzers **51** (`FuzzStatsdSinkConfigParse`) / BackendKind **38** / DECISIONS **ADR-0265** (next-free ADR-0266); ZERO new packages, ZERO new go.mod modules (HAND-ROLLED line protocol). Row 48 STAYS `in-progress` (it flips `done` at the phase-48 IMPL six-gate — NO parent rollup per ADR-0106).

After the SPEC lands, ROLL THIS FILE FORWARD to point at the phase-48 PLAN (mirror a prior phase's post-SPEC PLAN router in git history — e.g. the 47.x PLAN routers).

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** The next stage is the phase-48 SPEC (author `SPEC-48.md`; docs-only direct-on-master). Lifecycle-state 1 → 2. After it, a `spec-document-reviewer` pass (the 47.x precedent — verify every `bootstrap.go`/`internal/statssink` line citation EXACT against HEAD).
- **Subagent-driven execution** (`feedback_execution_style`) in a **fresh worktree** (`feedback_git_worktrees`) for any IMPL; subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit, re-runs the full suite on the frozen HEAD, does the deliberate-break verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). (BRAINSTORM/SPEC/PLAN are docs-only direct-on-master per the family-row precedent — no worktree needed.) Per-task gates: `gofmt -l` + `golangci-lint` on touched pkgs + `go vet` + `go build ./...` (`feedback_pertask_gofmt_lint`).
- **Process anchors:** ADR-0106 (per-leg rows) · ADR-0044 (ADR §Context-at-SPEC / §Decision+§Consequences-at-IMPL) · ADR-0045 (sub-split soft gate — escape-valve unconsumed) · ADR-0080 (strict-reject) · ADR-0064 (`stats_sinks[]` silent-ignore, now a SECOND consumer) · ADR-0060 (no histograms → no timers/`|ms`) · next-free **ADR-0265**.
- **Differential discipline** (carries to `0092`): `-run 'TestDifferential/0092'` NEVER bare (`reference_differential_run_selector`); `-count=1` on every break + `-race` run (`reference_differential_break_protocol_count1`); the full-suite `subject ready: EOF` startup flake is isolatable (`reference_differential_fullsuite_startup_flake`); the Docker bridge + decode-ran proof (`reference_docker_probe_bridge_network`); the driver-owned UDP receiver is NOT a BackendKind (`reference_differential_grpc_receiver_driver_owned`); the live reference is the wire truth (`reference_wire_format_both_sides_see_same_bytes`); periodic-sink two-receivers + hard-Close (`reference_periodic_sink_differential_two_receivers`); delta-sink stability barrier IFF D-SD-DELTA confirms deltas (`reference_delta_sink_differential_stability_barrier`); the metrics_service probe boundary (`reference_stats_sink_metrics_service_probe`).

## Read first (in order)
1. THIS FILE — the cold-start router (phase 48 `stats-sink-statsd` BRAINSTORM LANDED; the next action is its SPEC).
2. `docs/envoy-go/phases/48-stats-sink-statsd/BRAINSTORM.md` — the frozen scope (UDP-only / single flat row) + the §10 D-SD-* probe questions the SPEC must EXECUTE + the §12 closeout.
3. `docs/envoy-go/STATE.md` — the active-phase header (`phase 48 (stats-sink-statsd) BRAINSTORM done`) + the recorded NEXT (`phase 48 SPEC`) + the counts (1200 / 93 / 50 / 38 / ADR-0264).
4. `docs/envoy-go/ROADMAP.md` — row 48 `in-progress` (the table row + the Observability family note); the family STAYS OPEN.
5. `docs/envoy-go/phases/47-stats-sink-metrics-service/SPEC-47.1.md` — the 14-section SPEC shape to mirror + the phase-47 stats-sink substrate the statsd sink REUSES (the `Flusher`/`Sink` seam, the `snapshot()` mapping, the `stats_sinks[]` parse, the main.go wiring, the 47.2a `deltaState` pattern).
6. `internal/statssink/{flusher,sink,mapping,delta}.go` + `internal/bootstrap/bootstrap.go` (`parseStatsSinks` `:415` + the `:430` sibling-reject) + `cmd/envoy-go/main.go` (`:189-201` the sink-slice + flusher build) — the EXACT seams the SPEC cites.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `reference_stats_sink_metrics_service_probe`, `reference_periodic_sink_differential_two_receivers`, `reference_delta_sink_differential_stability_barrier`, `reference_differential_grpc_receiver_driver_owned`, `reference_strict_reject_sibling_typeurl_gap`, `reference_docker_probe_bridge_network`, plus the differential-discipline references above).
