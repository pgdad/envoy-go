=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action. (The sentinel does NOT apply right now — phase 54 is chartered and mid-lifecycle, and Observability + Operational tooling both carry non-empty deferred-candidate lists.)

**STATUS AS OF 2026-07-08: phase 54 (`load-balancer-panic-threshold`) BRAINSTORM is DONE (row 54 registered `in-progress`, lifecycle-state 0 → 1). The NEXT action is the phase-54 SPEC.**

- **The phase:** `healthy_panic_threshold` as an independent construct — the EIGHTH and FINAL Load-balancing-family row (the family CLOSES at phase-done). The field is consumed-but-never-proven (`parsePanicThreshold`, `internal/cluster/health.go` — default 50%, strict `<`; NO test of any kind exercises a non-default value). Scope per the human's Q1 (prove + harden): the first-ever differential proof (`0097-lb-panic-threshold`, the Q2 two-cluster discriminator — TWO health-checked clusters differing ONLY in threshold, 80% vs absent/50%, SAME 2-of-5 degradation, hard 0-vs-nonzero assertions) + the D-PT semantics pins + out-of-range validation parity (ADR-0080 probe-then-mirror) + TWO evidence-gated corrections: the `lb_healthy_panic` DOUBLE-INCREMENT fix under locality-weighted panic (the `REVIEW_FINDINGS.md` deferred finding — the shared-health flat child double-counts; fix shape pinned by D-PT1's increment-semantics probe) and the `locality.go` child-local-panic RETROFIT (phase 52's accepted boundary / phase 53's D-P-RETROFIT deferral; `tierHealth` as the template; gated on D-PT4's locality-scoped probe). Multi-tier-priority threshold inertness is confirmed-correct per phase-53 AMEND-P1 — a coverage boundary, NOT a gap.
- **The SPEC session executes the BRAINSTORM §10 empirical pins IN-SESSION** (D-PT1..D-PT6) against the contrib reference (`envoyproxy/envoy:contrib-v1.37.2`, ADR-0227) via live Docker-bridge probes (`reference_docker_probe_bridge_network`), anchors the ADR-0271 §Context draft, and re-confirms the single-flat-row envelope (anticipated ~120–250 LoC / ~8–10 tasks; D-PT6). SPEC-BLOCKING: D-PT1 (boundary strictness / per-policy in-panic distribution / increment-layer semantics), D-PT2 (disable-at-0 vs absent presence semantics), D-PT4 (the retrofit gate: does a single degraded locality internally flatten in the reference? + the information-only subset-child sub-probe).
- **Also landed this session (2026-07-08), context for the SPEC author:** the parallel repo-wide maintenance pass `9f26f380` was pulled, fully re-verified, and its differential-flake fallout root-caused to the PRE-EXISTING harness bind-collision weakness — fixed by `0e9cc680` (`freeTCPPortBlock` low-range probed block + `startSubjectWithRetry` with backoff across all three subject-start sites). The full suite is green at HEAD. `REVIEW_FINDINGS.md` documents the pass's deferred behavior-changing findings — phase 54 picks up ONLY the `lb_healthy_panic` double-increment (the rest stay deferred, incl. `membership_healthy`-vs-ejections, declined at Q1).

==============================================

## Honor throughout (the discipline, unchanged)
- **Lifecycle = one stage per session** (BRAINSTORM → SPEC → PLAN → IMPL). This session: the phase-54 SPEC ONLY; land it, then roll this file to the phase-54 PLAN.
- **Subagent-driven execution** (`feedback_execution_style`) in a **fresh worktree** (`feedback_git_worktrees`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit, re-runs gates on the frozen HEAD, does deliberate-break verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). Per-task gates: `gofmt -l` + `golangci-lint` + `go vet` + `go build ./...` (`feedback_pertask_gofmt_lint`).
- **Process anchors:** ADR-0106 (per-leg rows) · ADR-0044 (ADR §Decision+§Consequences land at IMPL) · ADR-0045 (split soft gate) · ADR-0080 (byte-stable rejects) · next-free **ADR-0271**.
- Re-verify baselines fresh at cold-start — do NOT trust any count in this file without re-running the baseline commands (`ls -d test/fixtures/[0-9][0-9][0-9][0-9]* | wc -l` → expect 98, `grep -rh '^func Fuzz' --include='*.go' --exclude-dir=.worktrees . | wc -l` → expect 52, DECISIONS tail → ADR-0270, stat surface → 1200) against current HEAD.

## Read first (in order)
1. THIS FILE — the cold-start router (phase-54 SPEC is next).
2. `docs/envoy-go/phases/54-load-balancer-panic-threshold/BRAINSTORM.md` — the charter: §10 D-PT1..D-PT6 are the SPEC's empirical-pin obligations; §2 the design decisions; §6 the `0097` fixture envelope.
3. `docs/envoy-go/STATE.md` — the active-phase header (`phase 54 BRAINSTORM done`) + full history.
4. `docs/envoy-go/ROADMAP.md` — row 54 `in-progress`; the Load-balancing family section (the family CLOSES at phase-done).
5. `docs/envoy-go/phases/53-load-balancer-priority/SPEC.md` — the shape template (the 14-section SPEC form; its AMEND-P1 finding + §11 probe methodology are directly load-bearing for D-PT1/D-PT4) + `REVIEW_FINDINGS.md` (the double-increment finding's origin).
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — especially `reference_docker_probe_bridge_network`, `reference_health_check_propagation_warmup`, `reference_percent_cap_cross_multiply`, `reference_round_robin_offset_randomized`, `reference_differential_*`, `feedback_*`.
