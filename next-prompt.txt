START a fresh cold-start session for envoy-go — **phase 33 (`network-filter-thrift-proxy`) SPEC is DONE; the next lifecycle step is the phase-33 PLAN** (`superpowers:writing-plans` — PLAN authoring proper: decompose the SPEC §10 ~16–20-task spine into bite-sized TDD tasks). The phase-33 SPEC squash LANDED at `7158b21` on origin/master (`docs/envoy-go/phases/33-network-filter-thrift-proxy/SPEC.md` + README.md; the ADR-0231 §Context in DECISIONS.md; the ROADMAP row-33 SPEC-DONE note; the STATE advance to `phase 33 SPEC done`; squash-merged from `phase-33-network-filter-thrift-proxy-spec` + pushed by the controller per `feedback_push_to_origin`; the SPEC worktree cleaned up + the branch deleted at stage-close — `git worktree list` is now just `[master]`). The literal live master tip is the docs-only next-prompt routing commit (`5c572ed`), trailing the substantive phase-33 SPEC squash (`7158b21`); confirm via `git log --oneline -1`.

The phase-33 SPEC executed the BRAINSTORM §10 D-T1..D-T9 empirical pins IN-SESSION (3 parallel probe subagents) live against the contrib reference Envoy `envoyproxy/envoy:contrib-v1.37.2` + go-control-plane `/envoy` v1.32.4 + upstream v1.37.2 CORE source; AMEND-T1..T7 are recorded in SPEC §1.1 + §11. Per `feedback_execution_style` the downstream PLAN→IMPL run subagent-driven; per `feedback_git_worktrees` work in a worktree (anticipated `.worktrees/phase-33-network-filter-thrift-proxy-plan`, branch `phase-33-network-filter-thrift-proxy-plan`).

## What this session does — the phase-33 {thrift} PLAN

Author `docs/envoy-go/phases/33-network-filter-thrift-proxy/PLAN.md` (`superpowers:writing-plans`) decomposing the SPEC §10 spine (~16–20 tasks) into bite-sized TDD tasks (each: write the failing test → run-fail → minimal impl → run-pass → commit; per-task `gofmt -l` + `golangci-lint run` on touched pkgs per `feedback_pertask_gofmt_lint`; subagents commit LOCAL-ONLY per `feedback_subagents_no_push`). The PLAN re-checks the ADR-0045 split-gate (the 33.1/33.2 escape-valve UNCONSUMED unless it trips) and resolves the SPEC §12 PLAN/IMPL D-questions. plan-document-reviewer gate applies at the PLAN (the writing-plans review loop). **The PLAN does NOT re-run the empirical probes — the SPEC §11 D-T1..D-T9 block + Appendix A (the framed×binary wire format) are authoritative.**

## The SPEC the PLAN decomposes (the as-pinned MVP — do NOT re-litigate)

  - **The envelope** — a SINGLE-PAIR SINGLE-ROUTE-TERMINAL MVP: the **framed × binary** message-begin codec (4-byte BE frame-len + binary strict message-begin `0x8001 00 <msgtype>` + i32 name-len + name + i32 `seq_id`; Appendix A) under `payload_passthrough` (route on the message-begin, forward the struct body opaquely) + a `route_config` single exact-match/match-all method route to ONE upstream cluster + the REUSED ADR-0230 upstream round-trip (one-conn-per-downstream, single-flight, positional; `seq_id` passed-through) + a router-only nested sub-chain (the implicit `envoy.filters.thrift.router`, auto-appended when `thrift_filters` is omitted) + a LOCAL `UnknownMethod` exception on a routing miss (zero upstream).
  - **The package** — NEW `internal/filter/network/thriftproxy/` (`thriftproxy.go` TypeURL+factory, `config.go` parse + PGV/departure rejects, `thrift.go` codec + reply classifier + the `AppException` encoder, `route.go` the method-routing table, `stats.go` the EAGER 25-name roster, `filter.go` the `TerminalFilter.Handle` pump) — the redis_proxy package shape. Implements `network.TerminalFilter` (the 11th built-in). ZERO new go.mod dep (CORE `/envoy`; the `thrift_proxy/v3` blank-import in `bootstrap.go`).
  - **The seam** — the ADR-0230 upstream-pool seam (`internal/filter/network/upstreampool.go`) is REUSED UNCHANGED (the FIRST reuse; framework-ZERO-touch).
  - **The stats** — EAGER 25-name roster (24 counters + 1 gauge `request_active`) under `thrift.<sp>.`; the `request_time_ms` histogram DEFERRED (ADR-0060); the 2 close-direction counters `cx_destroy_local/remote_with_active_rq` + `downstream_response_drain_close` created-but-NEVER-incremented (the framework-gap coverage boundary, asserted PER-SIDE on the `0057` miss arm). Stat surface 1091 → 1116 (+25). A new `thrift.` SINGLE-label-HOIST prom arm (`envoy_thrift_<leaf>{envoy_thrift_prefix="<sp>"}` — the redis-32 shape; mirror `internal/stats/name.go:315`).
  - **The differential** — `0057-thrift-roundtrip` (cross-side; route-HIT round-trip + route-MISS local-exception arms; downstream-Thrift-response byte-equivalence + cross-side `StatsAsserter`; the per-side cx_destroy boundary on the miss arm) + `0058-thrift-boot-reject` (missing `stat_prefix`); a new `TCPThriftResponder` BackendKind (seq_id-agnostic canned framed-binary REPLY, value 33); the 42nd fuzzer `FuzzThriftDecode`.

## The SPEC §12 D-questions the PLAN resolves

  - **D-S33-1** — the exact file split of `thriftproxy/` (anticipated per SPEC §3.2: `thrift.go` vs a separate `route.go`/`exception.go`).
  - **D-S33-2** — whether the `0057` optional reply-EXCEPTION / reply-ERROR arms land in the IMPL or stay unit-tested (anticipated: at least the reply-EXCEPTION arm earns a fixture arm to exercise `response_exception` from a backend reply, distinct from the local-miss exception).
  - **D-S33-3** — the `request_active` gauge differential treatment (quiesced-to-0 post-workload, or a held-open arm — the redis `downstream_rq_active` precedent).
  - **D-S33-4** — the reply-frame success/error classification under `payload_passthrough` (peek the first result-struct field header — field-id 0/STOP → success, ≥1 → error).
  - **D-S33-5** — the `FuzzThriftDecode` corpus seeds (a valid framed-binary CALL + a truncated frame + a bad-magic frame + an oversized length prefix).
  - **D-S33-6** — whether the malformed-frame path emits a local `ProtocolError` exception (the reference does, post-metadata) or silently closes counting `request_decoding_error` (a byte-equivalence-on-malformed-frame arm is a PLAN call; the reviewer flagged this as the one item to settle before relying on a ProtocolError fixture arm).
  - ADR-0045 split-gate FINAL re-check at PLAN.

## Counts at master tip (phase 33 SPEC done — the phase-33 PLAN session opens from here)

- stat surface **1091** (advances to **1116** at the phase-33 IMPL: +25 = 24 counters + 1 gauge; the `request_time_ms` histogram deferred ADR-0060, not counted).
- differential fixtures **58** (canonical `ls -d test/fixtures/[0-9]* | wc -l`; tail `0056-redis-boot-reject`; advances to **60** at the IMPL: `0057`/`0058`).
- fuzzers **41** (canonical `grep -rh "^func Fuzz" $(find ./internal -name fuzz_test.go) | wc -l`; tail `FuzzRESPDecode`; advances to **42** `FuzzThriftDecode` at the IMPL).
- BackendKind tail **32** (`TCPRedisResponder`; advances to **33** `TCPThriftResponder` at the IMPL; the values are a GLOBAL monotonic counter, decoupled from phase numbers).
- DECISIONS.md tail **ADR-0231** (the `thrift_proxy` filter; **§Context anchored at the SPEC**, status PROPOSED; the §Decision/§Consequences body lands IN-PLACE at the IMPL per ADR-0044 — NO new number; next-free **ADR-0232**).
- reference pin: `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227 — thrift_proxy is a CORE extension present in both the standard and contrib images; the project boots the contrib image).
- Toolchain: Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); go-control-plane `/envoy` v1.32.4 (ADR-0008) — thrift_proxy/v3 is a `/envoy` (CORE) subpackage → ZERO new go.mod dep (confirmed at SPEC D-T1).
- conformance h2spec 53/53 + proxy-wasm 10/10 (image-independent; UNAFFECTED by phase 33 — touches no HTTP/h2/proxy-wasm path).
- §9 Network-filters family: rows 26/27/28/29/30/31/32 DONE; **row 33 `in-progress` ({thrift}, the LAST candidate — the family CLOSES after phase 33 phase-done)**.

## Read first (in order)

1. THIS FILE — the phase-33 {thrift} PLAN cold-start.
2. `docs/envoy-go/phases/33-network-filter-thrift-proxy/SPEC.md` — **the authoritative phase SPEC** (§1.1 AMEND-T1..T7; §3 the REUSED seam + the package; §5/§6 the proto/reject rosters; §7 the 25-name roster + the `thrift.` SINGLE-label-hoist arm; §8 the `0057`/`0058` fixtures + the `TCPThriftResponder`; §10 the ~16–20-task spine the PLAN decomposes; §11 the D-T1..D-T9 empirical-pin block; §12 the PLAN D-questions; Appendix A the framed×binary wire format).
3. `docs/envoy-go/phases/33-network-filter-thrift-proxy/BRAINSTORM.md` — the charter (Q0/Q1/Q-seam/Q-split).
4. `docs/envoy-go/STATE.md` — active-phase `phase 33 SPEC done`; lifecycle-state → the phase-33 PLAN; next-skill `superpowers:writing-plans`; the counts (1091 stats / 58 fixtures / 41 fuzzers / BackendKind tail 32 / DECISIONS tail ADR-0231, next-free ADR-0232).
5. `docs/envoy-go/ROADMAP.md` — row 33 `in-progress` + the SPEC-DONE note + the §9 candidate-roster paragraph (the family closes after 33).
6. `docs/envoy-go/DECISIONS.md` — **ADR-0231** (the thrift_proxy filter — §Context anchored) + **ADR-0230** (the upstream-pool seam — REUSED unchanged) + ADR-0229 (the redis_proxy filter — the terminal-routing-proxy precedent) + ADR-0228 (kafka_broker — the single-flat-row precedent) + ADR-0106/0045/0044/0052/0060/0080.
7. `internal/filter/network/upstreampool.go` (the seam thrift REUSES unchanged) + `internal/filter/network/redisproxy/{filter,stats,config,resp,commands}.go` (the terminal-proxy package shape thrift mirrors) + `internal/filter/network/builtins/builtins.go` (the registration site) + `internal/bootstrap/bootstrap.go` (the blank-import block).
8. `internal/stats/name.go` (the `redis.` SINGLE-label-hoist arm at line ~315 the new `thrift.` arm mirrors) + `test/differential/fixture/fixture.go` (the `StatsAsserter` + the BackendKind enum, tail 32).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — especially `reference_close_direction_framework_gap` (the §9 close-direction boundary — thrift's 2 close counters + drain-close created-but-never-incremented, asserted per-side), `reference_wire_format_both_sides_see_same_bytes` (the framed-binary framing adopted verbatim), `reference_differential_reference_parses_full_message` (cross-side fixtures need fully-valid frames + per-side pins), `reference_differential_break_protocol_count1` (the `-count=1` deliberate-break protocol), `reference_dynamic_stat_name_charset_guard` (IsValidName — satisfied by construction for the fixed thrift roster), `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_subagents_no_push`, `feedback_pertask_gofmt_lint`.

## Honor throughout

- `feedback_execution_style` (the downstream IMPL runs subagent-driven) · `feedback_git_worktrees` (work in a worktree) · `feedback_subagents_no_push` (subagents commit LOCAL-ONLY; the controller squash-merges + pushes at stage-close) · `feedback_push_to_origin` (after a clean landing on master, push without asking) · `feedback_pertask_gofmt_lint` (each task runs `gofmt -l` + `golangci-lint run` on touched packages).
- ADR-0106 (the §9 family flat-row discipline — phase 33 is a flat row) · ADR-0045 (the split-gate — FINAL re-check at PLAN; the 33.1/33.2 escape-valve UNCONSUMED unless tripped) · ADR-0230 (the upstream-pool seam — REUSED unchanged) · ADR-0044 (§Context-at-SPEC / §Decision+§Consequences-at-IMPL — the ADR-0231 body lands at the IMPL) · ADR-0052 (the atomic-landing six-gate) · ADR-0060 (histograms deferred) · ADR-0080 (byte-stable PARSE-REJECT wording) · ADR-0227 (the contrib pin) · ADR-0008 (the go-control-plane dep — thrift_proxy is a CORE `/envoy` subpackage, ZERO new dep).
- The PLAN is a SINGLE FLAT ROW (the kafka-31 precedent — NO parent-SPEC/sub-phase split). plan-document-reviewer gate applies at the PLAN. At the PLAN-DONE commit ROADMAP row 33 STAYS `in-progress` (it flips `→ done` at the IMPL six-gate — a flat §9 row, NO parent rollup). The phase-33 IMPL six-gate CLOSES the §9 Network-filters family.
