START a fresh cold-start session for envoy-go — **the phase-37 (`load-balancer-maglev`) PLAN is DONE and LANDED on master; the next lifecycle step is the phase-37 IMPL** (`superpowers:subagent-driven-development` per `feedback_execution_style` — execute `docs/envoy-go/phases/37-load-balancer-maglev/PLAN.md` Tasks 1–10 in a fresh worktree, fresh subagent per task + two-stage review between tasks; subagents commit LOCAL-ONLY per `feedback_subagents_no_push`; the controller squash-merges + pushes at stage-close per `feedback_push_to_origin`). The substantive master tip is the phase-37 PLAN squash **`ce3a3eb`** (`phase 37 PLAN: load-balancer-maglev — SPEC §10 → 10 bite-sized TDD tasks; FINAL ADR-0045 re-check NO SPLIT; plan-document-reviewer APPROVED`; pushed to `origin/master`; confirm via `git log --oneline -5`), sitting directly above the phase-37 SPEC SHA-fill chain (`893de2a`/`67b23c4`) and the SPEC squash `ace5fd6`. The literal live master tip is the docs-only `next-prompt.txt` SHA-fill **`07e6c8f`** (which named `ce3a3eb` the substantive phase-37 PLAN squash and routed this cold-start to the phase-37 IMPL) sitting directly above the PLAN squash `ce3a3eb`; this paragraph is the next docs-only SHA-fill above `07e6c8f`. The phase-37 PLAN (`superpowers:writing-plans`; the plan-document-reviewer gate APPROVED first pass — no blocking findings, two advisory nits) decomposed SPEC §10 into **10 TDD tasks**, ran the FINAL ADR-0045 split-gate re-check (**NO SPLIT** — ~140–200 prod LoC / 10 tasks, both axes an order of magnitude under the gate; a single flat row), and resolved the D-S37-1..5 questions.

## The phase-37 PLAN the IMPL executes (PLAN.md Tasks 1–10)

For the project's FIFTH LB policy + SECOND consistent-hashing policy `Cluster.LbPolicy MAGLEV` (`envoy.config.cluster.v3`, enum value 5) — the seam-REUSING Maglev lookup-table policy. The 10-task spine (each TDD: failing test → red → minimal impl → green → commit; per-task `gofmt -l` + `golangci-lint` on touched pkgs per `feedback_pertask_gofmt_lint`; subagents LOCAL-ONLY commits):

- **Task 1** — baselines/anchors gate (confirm fixtures **64** / fuzzers **42** / stat surface **1119** / BackendKind tail **33** / DECISIONS tail **ADR-0237** via the canonical recipes; re-pin the as-built anchors against the IMPL-session tip; the reject-text 3-site blast radius; the seam+producers REUSE-only) + PROGRESS.md.
- **Task 2** — `isPrime(uint64)` trial-division helper (`maglev.go`); reference PARITY gate (the reference rejects a non-prime `table_size`, AMEND-M5).
- **Task 3** — the `maglevLB` table build (sort endpoints by `Addr()` ascending + `offset = xxHash64(addr)%M` / `skip = (xxHash64Seed(addr,1)%(M-1))+1` + the populate loop `(offset+skip·next)%M` until full; M prime → terminates; ZERO new hash code — `xxHash64`/`xxHash64Seed` reused; min/max = floor/ceil(M/N); default 3-host build = **21845/21846**).
- **Task 4** — `maglevLB.Pick` (`table[hashKey % M]` + the no-hash `rng()%M` fallback + empty-set `errNoEndpoints` + `noopRelease`) + the FOLDED random-keys property test (D-S37-5 — in lieu of a fuzzer; fuzzers STAY 42).
- **Task 5** — manager accept: `case Cluster_MAGLEV` + `parseMaglevLbConfig` (default 65537 self-supplied; PGV cap `≤ 5000011` THEN the primality gate) + the NEW reject text `…, RING_HASH, MAGLEV` + the doubly-hit retarget (`TestManager_Error_UnsupportedLBPolicy` trigger MAGLEV → **`Cluster_CLUSTER_PROVIDED`** [enum 6] + the new substring) + mismatched-oneof silent-ignore.
- **Task 6** — the 2 `maglev_lb.{min,max}_entries_per_host` gauges (MAGLEV-only via a `*maglevLB` type-assert mirroring the `*ringHashLB` block; Set once at build; NO `size` gauge; surface **1119 → 1121**).
- **Task 7** — the `0063-lb-maglev` differential fixture (mirror `0062` verbatim, retargeted RING_HASH → MAGLEV + `maglev_lb_config: {}` + the 2 maglev gauges; route header `hash_policy`; N=16 × K=16 = 256 `GET /get` + 8 `/health`; BOTH-SIDE affinity `≡ 0 mod K` + spread `≥ 2` + conservation 256; cross-side `StatsAsserter`; reuses `HTTPEcho` — BackendKind tail STAYS 33; `refContainerListenerPort` next-free **19152**).
- **Task 8** — deliberate-break liveness (`-count=1`, `reference_differential_break_protocol_count1`): scatter-the-key [affinity fails], **collapse-the-table-AT-BUILD** [spread + gauge prongs fail — name the break precisely; a `Pick`-level collapse leaves the gauges intact → vacuous, `reference_differential_asserter_dispatch`], stats-corrupt [stats fails]; ≥20-run flake check (`reference_fixture_workload_constant_desync` — N/K/totalReqs DERIVED).
- **Task 9** — full differential re-verify (the 64 prior dirs byte-exact through the new manager case + `0063` green → **65** dirs) + `-race -short` + `go mod tidy -diff` empty + h2spec 53/53 + proxy-wasm 10/10 asserted-unaffected.
- **Task 10** — the ADR-0052 completion bundle (BEHAVIOR_CONTRACT 37 delta [surface 1119 → 1121] + the ADR-0238 §Context+§Decision+§Consequences [ADR-0044 in-place; tail → ADR-0238] + STATE/ROADMAP row 37 `in-progress → done` [flat family row, NO parent rollup per ADR-0106] + the six-gate evidence) + the controller stage-close (squash-merge to master, final six-gate, push).

## Counts at master tip (phase 37 PLAN done — confirm at the IMPL)

- stat surface **1119** (`ls`/recipe per BEHAVIOR_CONTRACT). 1119 → **1121** at the IMPL (+2 `maglev_lb.*` gauges — D-M4; Task 6 registers, Task 10 advances the doc count). UNCHANGED at the PLAN.
- differential fixtures **64** (tail `0062-lb-ring-hash-http`; `ls -d test/fixtures/[0-9]* | wc -l`). 64 → **65** at the IMPL (`0063-lb-maglev`, Task 7). UNCHANGED at the PLAN.
- fuzzers **42** (tail `FuzzZookeeperResponseDecode`). STAYS 42 (NO new fuzzer — the property test is unit-level, FOLDED into Task 4, D-S37-5). BackendKind tail **33** (`TCPThriftResponder`; STAYS 33 — `0063` reuses the `0062` HTTP backends).
- DECISIONS tail **ADR-0237** (next-free **ADR-0238**). STAYS ADR-0237 at the PLAN; ADR-0238 (the maglev policy) §Context is a DRAFT in SPEC §13; the full body lands at the phase-37 IMPL Task 10 per ADR-0044. ADR-0235 (the hash-key seam) + ADR-0236 (the ring_hash policy — the STRUCTURAL + ADR STYLE TEMPLATE) + ADR-0237 (the HTTP producer) are the durable LB assets maglev REUSES unchanged.
- reference pin: `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227). Toolchain: Go 1.26.x; golangci-lint 1.64.8 (ADR-0009); go-control-plane `/envoy v1.32.4` (ADR-0008).

## Read first (in order)

1. THIS FILE — the post-phase-37-PLAN cold-start (next: the phase-37 IMPL — `superpowers:subagent-driven-development`).
2. `docs/envoy-go/phases/37-load-balancer-maglev/PLAN.md` — **the phase-37 PLAN the IMPL executes** (the 10-task TDD spine; the SPEC §3.1/§3.3 code reproduced VERBATIM in Tasks 3/4/5; the as-built anchors; the D-S37-1..5 resolutions; the file structure; the exit criteria).
3. `docs/envoy-go/phases/37-load-balancer-maglev/SPEC.md` — the authoritative SPEC the PLAN decomposes (§3 the `maglevLB` algorithm + the manager arm; §6 the reject roster [the primality PARITY + the cap + the supported-list change]; §7 the +2 gauges; §8 the `0063` differential; §11 the D-M1..D-M6 empirical-pin block; §13 the ADR-0238 §Context DRAFT).
4. `docs/envoy-go/STATE.md` — active-phase `phase 37 (load-balancer-maglev) plan done`; the lifecycle-state (2 → 3); the counts.
5. `docs/envoy-go/ROADMAP.md` — row 37 (`in-progress`; flat family row, NO split; flips `→ done` at the IMPL six-gate).
6. `docs/envoy-go/DECISIONS.md` — **ADR-0235** (the hash-key seam — REUSED) + **ADR-0236** (the ring_hash policy — the STYLE TEMPLATE for ADR-0238) + **ADR-0237** (the HTTP producer — REUSED); + ADR-0044 (ADR §Context at SPEC, body at IMPL) / ADR-0052 (atomic six-gate landing) / ADR-0080 (byte-stable parse-reject wording) / ADR-0106 (flat family row) / ADR-0045 (the split-gate, re-checked NO SPLIT) / ADR-0024 (UNAMENDED — maglev holds no per-cluster counter state).
7. The phase-35/36 IMPL records (`docs/envoy-go/phases/35-load-balancer-random/PROGRESS.md` + `phases/36-load-balancer-ring-hash/PROGRESS.md` + `phases/36.2-…/PROGRESS.md`) — the SHAPE TEMPLATES for the phase-37 IMPL (the per-task TDD execution; the deliberate-break records; the six-gate completion).
8. The landed LB code maglev plugs into: `internal/cluster/{cluster.go (the Pick seam + Addr():39),ringhash.go (the STRUCTURAL TEMPLATE),hash.go (xxHash64:40/xxHash64Seed:46),manager.go (the switch:243 + parseRingHashLbConfig:347 + the reject:275 + the gauges:110-114),loadbalancer.go,manager_test.go:320}` + the LANDED producers `internal/filter/{tcpproxy/filter.go,http/router/router.go}` (UNCHANGED) + `test/fixtures/0062-lb-ring-hash-http/` (the `0063` template).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_execution_style` (subagent-driven at the IMPL — REQUIRED), `feedback_git_worktrees`, `feedback_subagents_no_push` + `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_path_targeting`/`_detach`, `reference_differential_break_protocol_count1`, `reference_differential_run_selector`, `reference_differential_band_sigma_margin`, `reference_differential_hash_key_cross_side_infeasible`, `reference_fixture_workload_constant_desync`, `reference_docker_probe_bridge_network`.

## Honor throughout

- The lifecycle is BRAINSTORM → SPEC → PLAN → IMPL, each its own session. The next session is the phase-37 **IMPL** (`superpowers:subagent-driven-development` — execute PLAN.md Tasks 1–10; fresh subagent per task + two-stage review; subagents commit LOCAL-ONLY; the controller squash-merges the IMPL branch + runs the final six-gate on master + pushes at stage-close).
- `feedback_execution_style` (subagent-driven — REQUIRED) · `feedback_git_worktrees` · `feedback_subagents_no_push` + `feedback_push_to_origin` · `feedback_pertask_gofmt_lint` · `feedback_subagent_worktree_path_targeting`/`_detach`.
- ADR-0106 (flat family row) · ADR-0044 (ADR §Context at SPEC, body at IMPL, in-place) · ADR-0052 (the atomic-landing six-gate) · ADR-0080 (byte-stable parse-reject wording) · ADR-0227 (the contrib reference image; live probes per `reference_docker_probe_bridge_network`) · ADR-0024 (UNAMENDED) · ADR-0004 (the empirical-pin discipline — the pins are LANDED in the SPEC; the IMPL executes against them).
