START a fresh post-phase-23-SPEC cold-start session for envoy-go.

This is a fresh-session prompt for the post-phase-23-SPEC cold-start. **Phase 23 (`http-filter-admission-control`, `envoy.filters.http.admission_control`) SPEC is DONE.** It landed at master via squash-merge `a64ee71` + SHA-fill follow-up `ec68627` (pushed to origin). ROADMAP row 23 stays `in-progress` (SPEC-done annotation added; flips to `done` only at IMPL phase-done with all 6 gates GREEN); the §9 HTTP-filters family still has 3 remaining rows (`wasm`, `admission_control`, `global rate limit`) and closes to 2 (`wasm`, `global rate limit`) only once phase-23 IMPL lands. The natural next-skill per STATE.md + SKILL_ROUTING state 2 → 3 is **`superpowers:writing-plans` scoped to PLAN authoring** for phase 23 — decompose `docs/envoy-go/phases/23-http-filter-admission-control/SPEC.md` into bite-sized TDD tasks at `docs/envoy-go/phases/23-http-filter-admission-control/PLAN.md`, run the ADR-0045 split-gate check, and run the plan-document-reviewer loop. **PLAN authoring is the interactive `superpowers:writing-plans` work (like the phase-09..22 PLAN sessions); subagent-driven execution (project memory `feedback_execution_style.md`) applies at the IMPL EXECUTION session (state 3 → IMPL uses `superpowers:subagent-driven-development`), NOT at the PLAN-authoring stage.**

## What this session must do: author the phase-23 PLAN

Per SKILL_ROUTING state 2 → 3, the post-SPEC session authors PLAN.md from the SPEC. The PLAN must:

1. **Decompose the SPEC into bite-sized TDD tasks** (each task: write failing test → verify it fails → minimal impl → verify pass → commit), with exact file paths, per the `superpowers:writing-plans` discipline. The SPEC §6.8 source-file roster (~9 production + ~6 test Go files) + §4 algorithm + §5 PARSE-REJECT roster + §6 code shapes + §7 fixtures + §14 testing taxonomy are the authoritative inputs. Anchor each ADR's §Decision/§Consequences body-landing to a specific Task (ADR-0194 → the controller+filter materialization Task; ADR-0195 → the compiled_config+PARSE-REJECT roster Task) per the SPEC §10 Lands-in-Task map.
2. **Run the ADR-0045 split-gate check at PLAN time.** The SPEC settled SINGLE-ROW landing (~1000-1400 LoC prod, well under the gate). The PLAN author re-evaluates ONLY if the task-decomposition LoC drifts past ~1500 LoC OR ~25 tasks → if so, split into 23.1/23.2 + update ROADMAP + STATE and stop; otherwise single-row stands.
3. **Honor the SPEC's empirical AMENDs as the implementation contract** (these are SETTLED — do NOT re-derive or re-litigate): the integer-modulo reject decision `(1e4·P) > (r%1e4)` with the inline `Rand` seam shape `Uint64()` (NOT `Float64()`); the formula `P_reject = max(0, min(max_rejection_probability, ((n − s/sr_threshold)/(n+1))^(1/aggression)))` (aggression EXPONENT floored to 1.0; sr_threshold DIVIDES successes); the 3-counter roster `rq_rejected`/`rq_success`/`rq_failure`; `enabled`-absent ⇒ ENABLED; the EMPTY 503 reject body + `denied_by_admission_control` rc-details; the boot-reject `sr_threshold < 1.0%`; the deque-of-per-second-buckets window; the gRPC-trailers classification path; the record discipline.
4. **Create a fresh worktree** per `feedback_git_worktrees.md` (branch `phase-23-http-filter-admission-control-plan`, off master tip `ec68627`).
5. **Run the plan-document-reviewer subagent loop** (≤3 iterations per ADR-0005 / the writing-plans review loop). If not approved after 3, set STATE blocked + exit.
6. **Update STATE.md** at session end per BOOTSTRAP §4.1 invariant (lifecycle-state 3 `next-skill = superpowers:subagent-driven-development`) + push to origin per `feedback_push_to_origin.md`.

## Pre-existing settled context (DO NOT re-litigate)

The phase-23 SPEC is DONE and CANONICAL at master tip `ec68627`. Do NOT reopen the SPEC decisions, the empirical pins, or the BRAINSTORM Q-decisions:

- **The 11-entry AMEND catalog (SPEC §1.1) — 5 substantive REFUTATIONS of the BRAINSTORM, empirically verified against v1.37.2 source:**
  - **AMEND-1 formula:** `P_reject = max(0, min(max_rejection_probability, ((n − s/sr_threshold)/(n+1))^(1/aggression)))`; `aggression` is an EXPONENT `^(1/aggression)` floored to 1.0 (`std::max(1.0,·)`), NOT a multiplier; `sr_threshold` DIVIDES the success count (a fraction `min(pct,100)/100`), NOT a separate gate (`admission_control.cc:161-179`).
  - **AMEND-2 reject decision + Rand seam:** INTEGER-MODULO `(1e4·max(P,0)) > (r % 1e4)` with `r = rand.Uint64()` (`admission_control.cc:175-178`) — so the inline `Rand` seam is `Uint64()` NOT `Float64()`. P=0 ⇒ `0 > (r%1e4)` false for every `r` ⇒ all-admit leg is RNG-INDEPENDENT (cross-side byte-exact). Forced-reject byte-exact needs `P>0.9999` i.e. ≥10000 primed failures in one window/worker (impractical) ⇒ STAYS subject-only structural.
  - **AMEND-3 stats:** third counter `rq_failure` NOT `rq_error`; exactly 3 counters, NO gauges (`ALL_ADMISSION_CONTROL_STATS` is COUNTER-only); prefix `http.<HCM_stat_prefix>.admission_control.<stat>`; 107 → 110 holds.
  - **AMEND-4 enabled:** absent `enabled` ⇒ filter ENABLED (OPPOSITE of phase-21 adaptive_concurrency's absent⇒OFF; via `PROTOBUF_GET_WRAPPED_OR_DEFAULT(...,true)` at `runtime_protos.h:46`). default_value=false ⇒ disabled; default_value=true ⇒ enabled; runtime_key ⇒ PARSE-REJECT.
  - **AMEND-7 reject body:** 503 + EMPTY body + `response_code_details = "denied_by_admission_control"` + no added headers + no grpc_status (`admission_control.cc:101-102`).
  - **AMEND-8 boot-reject:** the differential boot-reject fixture (`0031`) uses `sr_threshold.default_value < 1.0%` → `"Success rate threshold cannot be less than 1.0%."` (`config.cc:25-27`) — NOT the BRAINSTORM's `sr_threshold > 100%` (which upstream CLAMPS at runtime, not rejects at boot).
  - Plus AMEND-5 (success defaults: HTTP all codes `<500`, NOT 2xx; gRPC the 11-code well-known set, NOT just OK), AMEND-6 (deque-of-per-second-buckets window; window rounded via integer `ms/1000`; default 30s), AMEND-9 (dual-factory; downstream-HCM-only MVP), AMEND-10 (gRPC-status-in-trailers classification), AMEND-11 (rejected/health-check/disabled requests NOT recorded into the window).
- **ADR-0194 + ADR-0195 §Context drafts are LANDED at DECISIONS.md** (anchored at the SPEC commit per ADR-0044). Their **§Decision + §Consequences bodies land at phase-23 IMPL** (ADR-0194 at the controller+filter materialization Task; ADR-0195 at the compiled_config+PARSE-REJECT roster Task). Next-free ADR is `ADR-0196`. **ADR-0196 hypothesized UNCONSUMED at phase-23 phase-done** (D-style HOLD-with-known-risk; one-slot escape-valve buffer).
- **ZERO new framework primitives + ZERO in-place §Decision AMENDMENTs + ZERO ADR-0125 amendments** (REUSE-by-absence per-route; FIRST ADR-0125-skip since phase-22's roster amendment; canonical-per-route roster STAYS 9). Both seams (`Clock`=`Now()` + `Rand`=`Uint64()`) inline in-package. Returns to phase-21's LEAN posture.
- **Two-directory differential STANDS** per `reference_differential_fixture_dispatch_constraint`: `0030-http-admission-control` (cross-side — scenario (b) all-admit P=0 byte-exact + (a)/(c)/(d) subject-only structural) + `0031-http-admission-control-boot-reject` (`sr_threshold < 1.0%` shared reject). Fixtures 31 → 33; fuzzer `FuzzAdmissionControlConfigParse` 31 → 32. The RTDS `runtime_key` reject is NOT a boot-reject fixture (upstream accepts it; that departure is unit-tested + BEHAVIOR_CONTRACT-recorded, count 14 → 15).
- **Single-phase landing STANDS** (~1000-1400 LoC prod; well under the ADR-0045 split-gate). **Downstream HCM filter only** at MVP (AMEND-9 — dual-factory upstream registration deferred). Boot-registration alphabetical between `adaptive_concurrency` and `bandwidthlimit` (18 HTTP filters post-phase-23-IMPL).

## Base + invariants at master tip `ec68627`

- **Master tip:** `ec68627` (phase 23 SPEC follow-up: STATE.md SHA-fill → `a64ee71`). Pushed to origin.
- **Master tail:**
  - `ec68627 phase 23 SPEC follow-up: STATE.md SHA-fill (TBD → a64ee71 post-squash)`
  - `a64ee71 Squash merge phase-23-http-filter-admission-control-spec`
  - `0bc26f5 phase 23 BRAINSTORM follow-up: STATE.md SHA-fill (TBD → 3040a6b post-squash)`
  - `3040a6b Squash merge phase-23-http-filter-admission-control-brainstorm`
- Baseline at master tip (UNCHANGED by the docs-only SPEC commit): **31 differential fixture directories** GREEN (0000-0029; lua fixtures 0026/0027/0028/0029 GREEN in isolation; combined multi-listener runs may hit the documented pre-existing `freeTCPPort` port-allocation flake per 22.2 REVIEW §7.4 — not a defect); **31 fuzzers** green; h2spec 53/53 PASS at ADR-0051 v1.32.4 pin; build/vet/lint clean; race-tests clean; **17 HTTP filters wired** (18 anticipated post-phase-23 IMPL); **107 stat names** (110 anticipated post-IMPL); **14 envoy-go-strict departure records** at BEHAVIOR_CONTRACT (15 anticipated post-IMPL — a NEW `### envoy.filters.http.admission_control` subsection with the RTDS `runtime_key` PARSE-REJECT departure). DECISIONS.md tail at **ADR-0195 §Context stub** (ADR-0194 + ADR-0195 §Context drafts landed; bodies pending IMPL); ADR-0193 full body + ADR-0125 §(xiv) amended remain; **next-free ADR-0196**.
- Project memories ALL apply: `feedback_git_worktrees.md` (always-worktrees), `feedback_push_to_origin.md` (push after a clean merge, no asking), `feedback_execution_style.md` (subagent-driven at the IMPL EXECUTION session — NOT at PLAN authoring), `reference_differential_fixture_dispatch_constraint.md` (one fixture dir = one runner branch — already baked into the two-directory differential plan).

## Read first (in order)

1. THIS FILE — post-phase-23-SPEC cold-start + the PLAN-authoring obligations.
2. `docs/envoy-go/STATE.md` — current state (phase 23 SPEC done; next-skill `superpowers:writing-plans` scoped to PLAN; next-free ADR-0196).
3. `docs/envoy-go/phases/23-http-filter-admission-control/SPEC.md` — the authoritative PLAN input (§1 purpose + §1.1 11-AMEND catalog; §3 framework survey; §4 algorithm line-cited lemmata; §5 PARSE-REJECT roster + §5.3 enabled-matrix + §5.4 per-route; §6 compiledConfig + code shapes + §6.8 file roster; §7 two-directory differential; §10 ADR anchor map + Lands-in-Tasks; §11 empirical-pin block; §14 testing taxonomy; §15 acceptance checklist).
4. `docs/envoy-go/phases/23-http-filter-admission-control/BRAINSTORM.md` — the settled design envelope (§2 Q-decisions + defaults; §8 deferred-items register).
5. `docs/envoy-go/phases/21-http-filter-adaptive-concurrency/PLAN.md` — the nearest single-filter algorithmic PLAN precedent (task decomposition shape; TDD task structure; ADR §Decision/§Consequences body-landing per Task; six-gate verification Task).
6. `docs/envoy-go/DECISIONS.md` tail — ADR-0194 + ADR-0195 §Context stubs (bodies land at the PLAN's IMPL Lands-in-Tasks); ADR-0044 (§Context-draft + ADR-on-impl convention); ADR-0005 (autonomous-planning adaptation — no Execution Handoff question; plan-document-reviewer loop retained); ADR-0045 (split-gate); ADR-0080 (byte-stable PARSE-REJECT wording).
7. `BOOTSTRAP_PROMPT.md` (repo root) — §4.1 STATE-update invariant + §5 state machine (step 2 → 3) + the ADR-0045 split-gate at PLAN time.
8. `docs/envoy-go/SKILL_ROUTING.md` — confirm state 2 → 3 (`SPEC.md` exists, `PLAN.md` does not → `superpowers:writing-plans`; GATE: split if PLAN > ~25 tasks OR > ~1500 LoC).
9. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the per-filter departure-record convention (phase 23 IMPL adds a NEW `### envoy.filters.http.admission_control` subsection with the RTDS-departure record).
10. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`: `feedback_git_worktrees.md`, `feedback_push_to_origin.md`, `feedback_execution_style.md`, `reference_differential_fixture_dispatch_constraint.md`.
