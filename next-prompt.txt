START a fresh cold-start session for envoy-go — **the phase-38.2 (`weighted-clusters`) SPEC is DONE; the next lifecycle step is the phase-38.2 PLAN** (`superpowers:writing-plans` — decompose the SPEC §10 indicative spine into bite-sized TDD tasks; the FINAL ADR-0045 split-gate re-check, anticipated NO further split [~200–320 prod LoC / ~10–13 tasks — both axes under the gate]; the plan-document-reviewer gate). The phase-38.2 SPEC is the pre-authorized SECOND leg of the phase-38 by-plane split (the 36.1/36.2 precedent — the parent phase-38 BRAINSTORM/SPEC chartered the split; the second leg went straight to its own SPEC). The substantive master tip is the phase-38.2 SPEC-done bundle (`docs/envoy-go/phases/38.2-weighted-clusters/{SPEC.md,README.md}` + STATE + ROADMAP row 38; confirm via `git log --oneline -5`). The prior substantive tip is the phase-38.1 IMPL squash `4eedf0e` (subset LB; LANDED + PUSHED). Counts UNCHANGED at the 38.2 SPEC: **stat surface 1125 / fixtures 66 / fuzzers 42 / BackendKind tail 33 / DECISIONS tail ADR-0240, next-free ADR-0241**.

## What just landed (phase 38.2 SPEC — the durable record is the SPEC.md, read it not this summary)

The SPEC for `RouteAction.weighted_clusters` routing — the project's **FIRST per-request cluster-SELECTION primitive** — + `WeightedCluster.ClusterWeight.metadata_match` (the per-weighted-cluster subset match that COMPOSES with the 38.1 subset LB). The §11 D-WC1..D-WC6 empirical pins were EXECUTED live this session (parallel-subagent fan-out against `envoyproxy/envoy:contrib-v1.37.2` per `reference_docker_probe_bridge_network`). As SPEC'd:
- **The architecture (SPEC §3):** the router resolves a SINGLE `*cluster.Cluster` per route today (`buildRouterAction` accepts ONLY `RouteAction_Cluster`; `clusterRouteAction`); 38.2 adds the SECOND `ClusterSpecifier` arm. A new `weightedClusterRouteAction` bridge (the `clusterRouteAction` sibling) holds N entries `{cluster, weight, subsetMatch}` + the shared route-level `hashPolicies` + a per-action `weightedSelector` (cumulative weights + a per-action mutex-guarded `newPCGRNG()` draw `rng() % Σweight` + a cumulative walk — the `randomLB` model lifted to the router layer); it satisfies the UNCHANGED `routeAction` interface and, per request, picks an entry then runs the EXISTING `doH1ClusterAction`/`doH2ClusterAction` with the chosen entry's cluster + subsetMatch — a SELECTION primitive layered ON the 38.1 dispatch, NO `loadBalancer.Pick` change. `buildWeightedRouterAction` is the producer (the §6 reject roster + the per-entry metadata_match merge).
- **The SPEC-firmed pins:** (D-WC1) `total_weight` is DEPRECATED upstream — the client uses the weight SUM (ignored, no cross-check); `weight` is a `UInt32Value` PRESENCE-required (explicit `0` legal); `name` has NO PGV `min_len`; `metadata_match` merges entry-precedence; envoy-go does its OWN parse-reject (NO PGV); ZERO new go.mod dep. (D-WC2) per-request weighted-RANDOM (live 70/30 → 1035/465 = 69.0% over 1500, binomial ±~11/500 — NOT deterministic RR) → a PER-SIDE distribution band (the `0060-lb-random` lineage; cross-side per-request identity infeasible for a randomized policy). (D-WC3) SIX code-level rejects {empty-clusters/name-required/cluster_header-unsupported/weight-required/sum-0/dangling}; `total_weight` mismatch ACCEPTS. (D-WC4) ZERO new stat surface (no `*weighted*` stat; observable via the pre-existing per-cluster `upstream_rq_*`; surface STAYS 1125 — the FIRST LB-family leg to add NO stats). (D-WC5) the metadata_match merge is route-static + reuses the ADR-0239 `WithSubsetMatch` seam UNCHANGED. (D-WC6) envelope ~200–320 prod LoC / ~10–13 tasks → single flat 38.2 leg; no FuzzWeightedSelect; ONE ADR-0241.
- **The differential (SPEC §8):** `0065-weighted-clusters` — a cross-side PER-SIDE weight-distribution band (`AssertDistribution` over the per-cluster `upstream_rq_total` at ~4–5σ per `reference_differential_band_sigma_margin`) + a metadata_match-composition arm (subset affinity within a chosen subset cluster) + cross-side conservation (the sum of per-cluster `upstream_rq_total` = N) + a `/health` byte-equiv stream. NO new BackendKind (the `0063`/`0064` `HTTPEcho` reused) + NO new fuzzer.
- **ADR-0241** §Context anchored as a DRAFT in the SPEC §13 (the weighted-cluster routing primitive + the `ClusterWeight.metadata_match` merge composition); the full entry (§Context + §Decision + §Consequences) lands at the phase-38.2 IMPL per ADR-0044.

## What the phase-38.2 PLAN does (the next step)

Decompose SPEC §10's indicative spine (~10–13 TDD tasks) into bite-sized TDD tasks: Task 1 baselines/anchors gate + PROGRESS.md; the `weightedSelector` + the `pick()` walk; `buildWeightedRouterAction` + the §6 reject roster; the per-entry metadata_match merge; the `weightedClusterRouteAction` bridge + the `H1/H2WeightedClusterAction` constructors; the `buildRouterAction` `RouteAction_WeightedClusters` arm wiring; the `0065-weighted-clusters` fixture; deliberate-break liveness + flake; the optional selection property test; the full differential re-verify + the completion bundle. The FINAL ADR-0045 split-gate re-check (anticipated NO further split). Resolve the §12 D-WC-IMPL-1..D-WC-IMPL-5 PLAN/IMPL questions (file placement; the exact reject wordings + the deferred-field disposition [`cluster_header` reject / header-mutation silent-ignore]; the `0065` final constants + the per-cluster band formula σ = √(N·p·(1−p)) at ~4–5σ; the composition arm shape; the property-test placement). plan-document-reviewer gate. Then the phase-38.2 IMPL (`superpowers:subagent-driven-development` per `feedback_execution_style` — one fresh subagent per task + two-stage review; subagents commit LOCAL-ONLY per `feedback_subagents_no_push`; the controller squash-merges + pushes per `feedback_push_to_origin`).

## Read first (in order)

1. THIS FILE — the post-phase-38.2-SPEC cold-start (next: the phase-38.2 PLAN).
2. `docs/envoy-go/STATE.md` — active-phase `phase 38.2 (weighted-clusters) SPEC done`; the counts (fixtures 66 / fuzzers 42 / stat surface 1125 / BackendKind 33 / DECISIONS tail ADR-0240, next-free ADR-0241); the lifecycle routing (→ the 38.2 PLAN).
3. `docs/envoy-go/phases/38.2-weighted-clusters/` — SPEC.md (the §3 architecture + §5 proto roster + §6 reject roster + §7 zero-stat + §8 `0065` differential + §10 per-task spine + §11 D-WC1..D-WC6 pins + §12 D-WC-IMPL questions + §13 ADR-0241 draft) + README.md.
4. `docs/envoy-go/phases/38-load-balancer-subset/` — BRAINSTORM.md (§1.4/§2.3 the by-plane split charter) + SPEC.md (§3.0 the split disposition) + PROGRESS.md (the 38.1 as-built subset LB + the ADR-0239 seam 38.2 reuses UNCHANGED).
5. `docs/envoy-go/DECISIONS.md` — ADR-0239 (the subset-match seam + the `RouteAction.metadata_match` producer — the ctx seam 38.2 REUSES UNCHANGED + the producer STYLE TEMPLATE) + ADR-0240 (the subset LB policy — the 38.2 composition target) + ADR-0237 (the HTTP route producer template) + ADR-0235 (the pick-input seam) + the process ADRs (0044/0045/0052/0080/0106/0004/0227).
6. The 38.1 as-built code 38.2 builds ON: `internal/filter/hcm/{actions.go [clusterRouteAction], config.go [buildRouterAction + the ClusterSpecifier switch + parseRouteSubsetMatch + parseRouteHashPolicies], route.go [the routeAction interface]}` + `internal/filter/http/router/{router.go [H1ClusterAction + doH1ClusterAction + applyHashKey + routerAction], router_h2.go [H2ClusterAction + doH2ClusterAction + routerActionH2]}` + `internal/cluster/{subset.go [SubsetMatch/NewSubsetMatch/ScalarsFromStruct], cluster.go [WithSubsetMatch], random.go + leastrequest.go [newPCGRNG — the selection RNG model]}` + `test/fixtures/{0060-lb-random [the per-side band precedent], 0064-lb-subset}/` + `test/differential/fixture/fixture.go [DistributionAsserter, StatsAsserter]`.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_execution_style` (subagent-driven IMPL), `feedback_git_worktrees`, `feedback_subagents_no_push` + `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_path_targeting`/`_detach`, and the differential references (`_break_protocol_count1`, `_run_selector`, `_band_sigma_margin` [the per-side weight-distribution σ-margins — load-bearing for `0065`], `_hash_key_cross_side_infeasible` [why the band is per-side], `_fixture_workload_constant_desync`, `_docker_probe_bridge_network`, `_asserter_dispatch`, `_fixture_dispatch_constraint`).

## Honor throughout

- The lifecycle is BRAINSTORM → SPEC → PLAN → IMPL, each its own session. The next session is the phase-38.2 **PLAN** (`superpowers:writing-plans` — decompose SPEC §10; the FINAL ADR-0045 gate re-check; the plan-document-reviewer gate).
- `feedback_git_worktrees` · `feedback_execution_style` (subagent-driven at the IMPL; one fresh subagent per task + two-stage review) · `feedback_subagents_no_push` (subagents commit local-only) · `feedback_push_to_origin` (the controller squash-merges + pushes at stage-close) · `feedback_pertask_gofmt_lint`.
- ADR-0004 (the empirical-pin discipline — already EXECUTED at the SPEC; the PLAN re-uses the pins) · ADR-0044 (ADR §Context at SPEC, bodies at IMPL in-place) · ADR-0045 (the split-gate — re-check at the 38.2 PLAN) · ADR-0052 (the atomic-landing six-gate) · ADR-0080 (byte-stable parse-reject wording) · ADR-0106 (flat family row — NO parent rollup) · ADR-0227 (the contrib reference image).

## After phase 38.2 — the longer horizon (see `ROADMAP.md` §Feature Families for the authoritative detail)

Forward-looking ONLY — the IMMEDIATE next step is the phase-38.2 PLAN above. After 38.2 the Load-balancing family has **3 candidates left** {locality-weighted LB, priority load balancing, panic thresholds} — **ALL gated by the absent health-checking boundary**, so they CANNOT land until the Upstream-robustness family exists. Per ADR-0106 sibling rows are NOT pre-populated; the next subject (a remaining LB candidate, or a NEW family) is chosen at the next BRAINSTORM. **Six families are essentially unstarted** (each opens with its own `superpowers:brainstorming`, MVP-scoped): xDS / dynamic config (the single largest gap — the whole project is STATIC bootstrap today); Upstream robustness (active health checks / outlier detection / circuit breakers / retries + hedging / connection pooling — UNBLOCKS the 3 health-gated LB candidates); HTTP/3 + QUIC; gRPC (bridge / gRPC-Web / transcoding); Observability (gRPC ALS / OTLP / tracing / stats sinks / tap); Runtime + hot restart (the RTDS-driven runtime layer — note `WeightedCluster.runtime_key_prefix` was DEFERRED at 38.2 to this family). The §9 Network-filters family is CLOSED (phases 26–33); the HTTP-filters family (09–25) shipped its curated set but is NOT formally closed; the WASM host family is partially satisfied (proxy-wasm HTTP-filter conformance via phase 25).
