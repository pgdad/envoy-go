START a fresh cold-start session for envoy-go — **the phase-29.1 sub-phase PLAN (`network-filter-mongo-wire-and-requests`) is DONE + plan-document-reviewer APPROVED + squash-merged + pushed**. The active phase is **phase 29.1** (`in-progress`; parent row 29 `in-progress`; 29.2/29.3 `planned`). This session runs the **29.1 IMPL** — executing `docs/envoy-go/phases/29.1-network-filter-mongo-wire-and-requests/PLAN.md` (3022 lines, 17 bite-sized TDD tasks) via **`superpowers:subagent-driven-development`** (per SKILL_ROUTING state 3 + `feedback_execution_style`: a fresh subagent per task, two-stage review between tasks). Work in a git worktree (`feedback_git_worktrees`); subagents commit LOCAL-ONLY (`feedback_subagents_no_push`); the controller squash-merges + pushes at stage-close (`feedback_push_to_origin`).

The 29.1 PLAN landed at squash `<FILL-AT-SQUASH>` (pushed to origin/master). It is the executable blueprint; the 29.1 SPEC (`SPEC.md`, squash `7574219`) + the parent SPEC (`29-network-filter-mongo-proxy/SPEC.md`, squash `ab376d8`) are the pinned-behavior references the PLAN's code blocks already transcribe. **The ADR-0045 gate PASSED at PLAN time: 17 tasks / ~1070–1470 production LoC → NO split** (the pre-authorized 29.1a/29.1b axis STANDS-UNCONSUMED).

## The 17-task spine (PLAN.md is authoritative — exact code/commands/expected-outputs per task)

1. First-action baselines gate (no code): re-pin fixtures **50** (tail `0048`) + fuzzers **38** + stats **337** + DECISIONS tail **ADR-0226** (next-free **ADR-0227**) + `proto.MessageName` mongo TypeURL + the 5-field/FaultDelay accessor roster, against the live IMPL tip; create PROGRESS.md.
2. `mongoproxy` skeleton: `doc.go` + `mongoproxy.go` (TypeURL) + `config.go` (5-field parse + commands remembered-set + alias normalization). `NewFactory` is SPLIT to Task 10.
3. FaultDelay PGV validation (oneof-required + fixed_delay gt-0s + denominator-defined) + byte-stable PARSE-REJECT constants (prefix `mongo_proxy: `).
4. `bson.go` part 1: reader primitives + document framing + the scalar types (Double/Boolean/Datetime/Null/Int32/Timestamp/Int64) + throw-on-unknown + underflow.
5. `bson.go` part 2: String/Symbol/Document/Array/Binary/ObjectId/Regex + lookups (`first`/`find`/`asInt64`).
6. `stats.go` (REORDERED before codec — green-compiling order): the 23-stat eager roster (22 counters + `op_query_active` gauge; `TestStatRoster_MatchesUpstreamMacro` golden — `delays_injected` PLURAL) + dynamic cmd/collection/callsite helpers.
7. `codec.go` part 1: MsgHeader framing + `chainConsumed` reassembly (D-S29.1-4) + the 7-opcode dispatch (Reply/CommandReply recognized-not-decoded; OP_MSG/Update/Delete/Msg→`decoding_error`) + sniffing-off at-most-once (D-S29.1-6); body decoders STUBBED.
8. `codec.go` part 2: OP_QUERY decode (flags + collection-after-first-dot + `$cmd` command + query-shape scatter/multi/primary + `$maxTimeMS` + `$comment` callsite double-count + active-query append).
9. `codec.go` part 3: OP_INSERT/OP_GET_MORE/OP_KILL_CURSORS/OP_COMMAND body decode + validate.
10. `filter.go`: `NewFactory` (eager roster) + the both-directions filter glue (OnData feeds decoder; OnWrite pure no-op stub; R3 never-drains; OnDestroy).
11. The 8th built-in registration + `bootstrap.go` blank-import + boot smoke (23 stats eager at 0; both-directions classification). **§4 zero-touch property is a regression gate — registration alone must perturb nothing.**
12. The `mongo.` four-rule `name.go` tag-extractor arm (prefix/cmd/collection/callsite; labels SORTED LOCALLY → `prom.go` UNTOUCHED per D-S29.1-5) + a byte-exact `WriteProm` callsite-line test.
13. The 39th fuzzer `FuzzMongoDecode` (no-panic + chain-bytes-unmutated + sniffing-off idempotence + bounded readBuf).
14. `0049-mongo-requests` driver part 1: bootstraps + LE wire/BSON builders (D-S29.1-3) + MultiListener (`l_default` mongo_a / `l_commands` mongo_b) + `TCPSink` (BackendKind 28; NO `fixture.go` change).
15. `0049` part 2: the LABEL-AWARE StatsAsserter (canonical name+sorted-labels scrape; R7) + arms 1–5. **Fill the cumulative-accounting table — do NOT leave the `0` placeholders.**
16. `0049` part 3: arms 6–9 (error arms on fresh conns; exists-at-zero + gauge-TYPE parity; `cx_destroy_*` presence-only per AMEND-C2; R4 deliberate-break with `-count=1`) + `0050-mongo-boot-reject` — fixtures 50→**52**.
17. Completion bundle: BEHAVIOR_CONTRACT 29.1 (337→**360**) + ADR-0224 §Decision/§Consequences body in-place (no new ADR number) + STATE/ROADMAP (sub-row 29.1 `in-progress → done`; parent row 29 STAYS `in-progress` — the ROLLUP is 29.3's) + next-prompt.txt for the **29.2-SPEC** cold-start + the SIX-GATE (incl. the FULL 52-dir differential + the 50-dir back-compat gate + h2spec 53/53 + proxy-wasm 10/10).

## Load-bearing SPEC pins the IMPL must honor (all already encoded in PLAN.md)

- **AMEND-C1 (D-P2/D-P3):** the dynamic Prometheus form is FULLY label-hoisted (`envoy_mongo_cmd_total{envoy_mongo_cmd=…}`, `…collection_query_*{envoy_mongo_collection=…}`, callsite carries 3 labels). The `name.go` arm is a FOUR-RULE tag extractor mirroring upstream `well_known_names.cc` `addTokenized`; labels emit in SORTED key order (sorted in the arm — `prom.go` stays byte-identical, D-S29.1-5).
- **D-P1:** EAGER roster creation at config parse (all 23 via `NewCounterIfAbsent`/`NewGaugeIfAbsent`); the boot-window departure vs upstream per-connection creation is unobservable to the differential (post-first-connection assertions).
- **AMEND-C2:** `cx_destroy_*_with_active_rq` increments on the REFERENCE at every query-bearing connection close → the `0049` assertions are PRESENCE-ONLY for that pair (value parity is 29.2's `0051`).
- **AMEND-C3:** `$comment` callsite queries DOUBLE-COUNT (both `collection.<c>.query.*` AND `collection.<c>.callsite.<cs>.query.*` — the SAME leaves).
- **AMEND-C4:** the `0049` backend is the EXISTING `TCPSink` (silent; BackendKind 28); NO new BackendKind at 29.1 (the mongo responder, anticipated 30, is 29.2's).
- **The 7-opcode envelope + sniffing-off-on-error (AMEND-B5/B6):** OP_MSG(2013)/Update/Delete/Msg → `decoding_error` (at most once per connection) + sniffing off for the connection lifetime; OP_REPLY/OP_COMMANDREPLY recognized-not-decoded until 29.2.
- **PLAN dispositions:** stats.go before codec.go (dependency order); the active-query list lives ON THE DECODER (`dec.queries`), not the filter; D-S29.1-2 (`bsonDoc` ordered elements + `asInt64`), D-S29.1-4 (`chainConsumed` vs `TotalAppended`), D-S29.1-6 (decoding_error single-increment in the decoder).

## What this session must do

1. Re-establish a worktree (`feedback_git_worktrees`); confirm the clean baseline (`go build ./...` + `go test ./internal/... -count=1`).
2. Run `superpowers:subagent-driven-development` over PLAN.md Tasks 1–17: a fresh subagent per task, two-stage review (code-reviewer + your check) between tasks; each task is write-failing-test → run → implement → run → `gofmt -l` + `golangci-lint` → commit (LOCAL-ONLY by subagents). Honor `reference_differential_break_protocol_count1` (use `-count=1` after any deliberate break) + `reference_differential_asserter_dispatch` (prove every new fixture assertion is live).
3. Land the Task-17 completion bundle + the SIX-GATE green (quote outputs honestly into PROGRESS.md).
4. STATE.md advance (active-phase → `phase 29.1 IMPL done`; next-skill → the 29.2 SPEC per SKILL_ROUTING); rewrite next-prompt.txt for the **29.2-SPEC** cold-start; commit + squash-merge + push.

## Project state at master tip

- **Master tip:** the 29.1-PLAN squash `<FILL-AT-SQUASH>` (confirm via `git log --oneline -1`; docs-only — PLAN.md + STATE.md + next-prompt.txt). Substantive predecessor: the 29.1-SPEC squash `7574219`. origin/master in sync.
- **§9 Network-filters family:** rows 26/27/28 DONE; row 29 `in-progress` (29.1 `in-progress`; 29.2/29.3 `planned`).
- **Counts at master tip:** stat surface **337** (→ 360 at 29.1 IMPL); differential fixtures **50** (tail `0048-zookeeper-responses`; → 52 at 29.1 IMPL); fuzzers **38** (→ 39); conformance h2spec 53/53 + proxy-wasm 10/10 (as of the 28.2 six-gate); DECISIONS.md tail **ADR-0226** (next-free **ADR-0227**; the ADR-0224 body lands at the 29.1 IMPL — no new number).
- **Toolchain:** Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); reference Envoy v1.37.2 (ADR-0008); go-control-plane v1.32.4 (ADR-0008).
- **Known coverage boundaries (carried, non-blocking):** D-26.3-4 (rbac SNI/mTLS L4 arms); `tcp_proxy` `downstream_cx_*` unmirrored; histograms deferred project-wide (ADR-0060); the mongo OP_REPLY/OP_COMMANDREPLY decode + the gauge increments + `cx_destroy_*` value parity are 29.2's; fault delay + access log + drain are 29.3's; mongo runtime-key gating unmirrored (29.3 record); the mongo access log not differentially compared (AMEND-B10; 29.3 record).

## Read first (in order)

1. THIS FILE — the 29.1-IMPL cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 29.1 PLAN done`; next-skill `superpowers:subagent-driven-development` (29.1 IMPL); next-free `ADR-0227`.
3. `docs/envoy-go/phases/29.1-network-filter-mongo-wire-and-requests/PLAN.md` — **THE document this session executes** (17 tasks; complete code blocks; the PLAN-time D-question dispositions at the top are binding).
4. `docs/envoy-go/phases/29.1-network-filter-mongo-wire-and-requests/SPEC.md` — the pinned-behavior reference (esp. §3 package design, §6 reject arms, §7.4 the four-rule name.go arm, §8 fixture arms, §11.2 the D-P2 probe data, §12.2 the D-S29.1-* questions the PLAN resolved).
5. `docs/envoy-go/phases/29-network-filter-mongo-proxy/SPEC.md` — the parent SPEC (§11.4 wire/BSON layouts the decoder mirrors verbatim; §5 proto roster; §7 stat surface).
6. `docs/envoy-go/SKILL_ROUTING.md` + the lifecycle docs — state 3 (IMPL) + the split policy.
7. `internal/filter/network/zookeeperproxy/` (the consumer-#1 package the PLAN's code mirrors: config.go/stats.go/decoder.go/zookeeperproxy.go) + `internal/stats/name.go` (the `.zookeeper.`/`.rbac.` tag-extractor arms the mongo arm generalizes) + `internal/stats/prom.go` (`writeMetricLine` — confirms the D-S29.1-5 local-sort disposition).
8. `test/fixtures/0046-zookeeper-requests/` + `0047-zookeeper-boot-reject/` (the cross-side + boot-reject driver precedents) + `test/differential/fixture/fixture.go` (TCPSink=28; StatsAsserter; MultiListenerDriver) + `test/differential/runner_test.go` (the driver blank-import roster).
9. `docs/envoy-go/DECISIONS.md` ADR-0224 §Context (the 29.1 charter; the §Decision/§Consequences body lands at IMPL Task 17) + ADR-0044/0045/0052/0080 (doc/split/bundle/reject discipline).
10. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — execution-style + worktree + push + subagents-no-push + per-task gofmt/lint + the network-filter constraints (typeurl-extensions, onnewconnection-halts, terminal-handoff-ends-ondata) + the differential dispatch/asserter/break-protocol (`-count=1`) + wire-format-both-sides constraints all carry forward into the IMPL.
