=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**Check (1) is NOW NON-SILENT** (phase 57 is OPEN — row 57 `stats-sink-graphite` registered `in-progress` at its BRAINSTORM; check (1) prints `NOT DONE: row 57`). **The sentinel does NOT fire** — an open row alone blocks it, and (2)+(3) still carry work besides. Do NOT create `stop`.

**SENTINEL RE-CHECKED 2026-07-11 (post-phase-57-BRAINSTORM) — it does NOT fire. Do NOT create `stop`.** Evidence:
- (1) prints `NOT DONE: row 57` ⇒ phase 57 is OPEN (a SPEC is the next stage). Blocks `stop` outright.
- (2) prints the Observability family's deferred candidates ⇒ the **Observability family STAYS OPEN** (OTLP-metrics sink + tracing `custom_tags`/`spawn_upstream_span`/`http_service`/force-trace — `graphite` is now CONSUMED by row 57).
- (3) prints `NEVER OPENED: HTTP/3`, `gRPC`, `xDS`, `Runtime`, `WASM` ⇒ FIVE families never opened; the **Operational-tooling family** also STAYS OPEN.

**STATUS AS OF 2026-07-11: phase 57 (`stats-sink-graphite`) is OPEN — row 57 is `in-progress`; the BRAINSTORM LANDED.** The loop was RE-OPENED after phase 56 landed COMPLETE; a human pick chose the `graphite` stats sink (the cheapest remaining Observability follow-on). **Settled scope (Q0–Q2):** `envoy.extensions.stat_sinks.graphite_statsd.v3.GraphiteStatsdSink`, a periodic UDP graphite-flavored statsd-line sink over the LANDED phase-47 `Flusher`/`Sink` seam (the FOURTH `stats_sinks[]` consumer); UDP `address` only (the oneof has no `tcp_cluster_name`); tags IN-SCOPE (reusing `stats.ExtractTags`, formatted `;key=value` appended to the metric NAME — CONTRAST dog_statsd's `|#key:value` suffix); `max_bytes_per_datagram` batching HONORED DAY 1 (reusing phase-50 `appendLine`/`flush`); a SINGLE FLAT ROW (ADR-0275, sink only). A TYPED-EXTENSION dispatch — the type URL DERIVED from the descriptor + blank-imported (`reference_network_filter_typeurl_extensions`); the CORE `/envoy v1.32.4` module carries it (ZERO new go.mod dep); the sibling-reject message EXTENDED (`reference_strict_reject_sibling_typeurl_gap`). **The ONE novel piece is the graphite tag-format function (~10 LoC); ALL else reuses the phase-49 dog_statsd substrate + phase-50 batching machinery verbatim.**

**Current counts (verified at the phase-57 BRAINSTORM; UNCHANGED — docs-only):** fixtures **102** (tail `0100-http-tap-bodies`) · fuzzers **53** · DECISIONS tail **ADR-0274** (next-free **ADR-0275**) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** · new Go packages **0** · new go.mod modules **0**.

**Locating the stage squash (a router cannot name its own sha — writing the sha changes it).** Identify it by SUBJECT, never by sha and never by position:
```sh
git log --oneline --grep='^phase 57 (stats-sink-graphite) BRAINSTORM' -1
```
This stage folded its own `next-prompt.txt` roll INTO the squash (per `reference_next_prompt_tracked_despite_gitignore`). The squash is **NOT** necessarily the master tip: PARALLEL-WORKSTREAM commits can (and did) land on top of it — after this BRAINSTORM squash `d2fe54df` was pushed, two test-only commits from a parallel workstream (`a2abddf7` HTTP/1.1 request-validation suite + CI hardening, `39e2140e` TLS-handshake negative test) were pulled and fast-forwarded, so the master tip is `39e2140e`, TWO commits above the BRAINSTORM squash. They touched NO production/differential-harness code and the full suite re-verified green (the lone `0083` full-suite failure was a confirmed startup-timing flake, green on isolated re-run — `reference_differential_fullsuite_startup_flake`). Counts are therefore UNCHANGED (fixtures 102 / fuzzers 53 / stat 1201 / BackendKind 38 / DECISIONS tail ADR-0274). **Do not assume the squash is the tip; do not assume the tip is the squash.** The `--grep` above locates the BRAINSTORM squash correctly in EVERY shape (it is sha- and position-agnostic) — but ALWAYS `git fetch` + re-verify counts against the ACTUAL current master tip at the start of the SPEC session, since more parallel commits may have landed.

**THE NEXT ACTION: the phase-57 SPEC (lifecycle-state 1 → 2) — live-probe the graphite behavior and pin the D-GR-* questions.**

==============================================

## What THIS session does (the phase-57 SPEC, lifecycle-state 1 → 2)

Phase 57 is OPEN (row 57 `in-progress`, BRAINSTORM landed). This session performs the **SPEC** (docs-only; NO production `.go` code — SPEC pins behavior, IMPL builds it). Fresh worktree per `feedback_git_worktrees` (e.g. `.worktrees/phase-57-spec`, branch `phase-57-stats-sink-graphite-spec`).

1. **Read the BRAINSTORM** (`docs/envoy-go/phases/57-stats-sink-graphite/BRAINSTORM.md`) — the settled scope + the eleven D-GR-* open questions (§10).

2. **LIVE-PROBE the reference** (`envoyproxy/envoy:contrib-v1.37.2` on a Docker bridge network, FRESH container per arm per `reference_docker_probe_bridge_network` + `reference_probe_fresh_container_per_arm`) to PIN every D-GR-* question — decisively **D-GR-TAGFORMAT** (the exact graphite `;k=v` grammar: the separator, the `=` delimiter, first-tag separation, the empty-tags line) and **D-GR-DELTA** (`|c` delta-vs-absolute, probed INDEPENDENTLY of phases 48/49 — do NOT assume it mirrors them). Also D-GR-PREFIX, D-GR-TAGORDER (`reference_dogstatsd_tag_order_unsorted` — do NOT sort without probing), D-GR-BATCH (cross-check the overflow operator + join separator against phase-50's D-DSDB-* answers), D-GR-STATS (name-subset probe per `reference_stats_sink_metrics_service_probe` / `reference_stats_sink_emits_used_only`), D-GR-REJECT, D-GR-IMAGE (does the contrib image boot a graphite sink), D-GR-FIXTURE, D-GR-FUZZER (`reference_fuzzer_count_docs_drift` — reconcile the running total), D-GR-LIFECYCLE. **RE-DERIVE every proto field + `bootstrap.go` dispatch citation from source** (`feedback_brief_citations_not_evidence`) — never verify a citation against the BRAINSTORM that made it. Reach the reference receiver via `differential.HostGatewayIP` (`reference_host_gateway_ip_docker_desktop`).

3. **Write `docs/envoy-go/phases/57-stats-sink-graphite/SPEC.md`** (+ a `README.md` if the phase-49/50 convention warrants): pin the line mapping (`<prefix>.<residual>;k=v:val|type`), the delta/tag/batching semantics, the strict-reject roster (extend the sibling-reject message; `reference_strict_reject_sibling_typeurl_gap`), the `0101-stats-sink-graphite` differential envelope (the delta-SUM-with-stability-barrier + TWO per-side receivers shape from `reference_delta_sink_differential_stability_barrier` + `reference_periodic_sink_differential_two_receivers`; assert the PAYLOAD multiset not framing per `reference_streaming_sink_differential_framing`), and ANCHOR **ADR-0275 §Context DRAFT** (§Decision/§Consequences land at the IMPL per ADR-0044; DECISIONS tail STAYS ADR-0274 at this SPEC). Re-check the ADR-0045 split gate (a SINGLE FLAT ROW is anticipated; re-confirm the task count stays well under `~15`).

4. **Counts are UNCHANGED at a SPEC** (docs-only) — re-verify against the master tip and record. Update STATE.md's active-phase header.

5. **Land the SPEC** (commit + push per `feedback_push_to_origin`; docs-only, no `.go` changes), then **ROLL THIS FILE FORWARD** to the phase-57 PLAN.

## Honor throughout (the discipline, unchanged)
- **Lifecycle = one stage per session.** This session: the phase-57 SPEC (docs-only; live-probe the reference).
- **Skills FIRST.** The SPEC has no single mandated skill gate, but honor `superpowers:systematic-debugging` if a probe surprises, and re-derive from source. (The `superpowers:brainstorming` gate was for the BRAINSTORM, already landed.)
- **`next-prompt.txt` IS TRACKED** (it appears in `.gitignore` line 2, but a `.gitignore` entry does not apply to an already-tracked file — `git ls-files --error-unmatch next-prompt.txt` succeeds). Edit it inside the stage worktree and fold it into the commit (`reference_next_prompt_tracked_despite_gitignore`). A router can't name its own sha — locate the commit by SUBJECT (`git log --grep`), never by position.
- **A BRIEF'S CITATIONS ARE NOT EVIDENCE** (`feedback_brief_citations_not_evidence`). Re-derive every `file:line` + proto field from source; never verify a citation against the document that made it. This discipline paid out repeatedly on phase 56 (a false double-`OnDestroy` at the 56.1 SPEC; two vacuous-proof defects at the 56.1 PLAN; two more at the 56.1 IMPL; two stale SPEC citations at the 56.2 PLAN; and at the 56.2 IMPL a controller `-race` run caught a detrand-fragile test — IMPL-1).
- **Sink/observer traps (apply at the SPEC):** `reference_dogstatsd_tag_order_unsorted` · `reference_delta_sink_differential_stability_barrier` · `reference_periodic_sink_differential_two_receivers` · `reference_streaming_sink_differential_framing` · `reference_stats_sink_emits_used_only` · `reference_stats_sink_metrics_service_probe` · `reference_host_gateway_ip_docker_desktop` · `reference_network_filter_typeurl_extensions` · `reference_strict_reject_sibling_typeurl_gap` · `reference_fuzzer_count_docs_drift`.
- **Process anchors:** ADR-0045 (the split-gate escape valve — a SINGLE FLAT ROW anticipated; re-confirm) · ADR-0106 (`reference_roadmap_split_phase_row_done` — a single-leg row flips `done` at its IMPL) · ADR-0044 (ADR §Decision/§Consequences land at the IMPL, §Context drafts at the SPEC) · the ROADMAP §Schema. Next-free ADR is **ADR-0275**.

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-57 SPEC is next).
2. `docs/envoy-go/phases/57-stats-sink-graphite/BRAINSTORM.md` — the settled scope + the D-GR-* open questions.
3. `docs/envoy-go/ROADMAP.md` — the row-57 entry (the charter) + the Observability family paragraph (the phase-47..55 sink precedent for how a sink SPEC pins its line mapping + differential).
4. `docs/envoy-go/STATE.md` — the active-phase header (phase 57 BRAINSTORM done) + the phase-48/49/50/55 sink history entries.
5. The prior sink SPECs as templates — `docs/envoy-go/phases/49-stats-sink-dogstatsd/SPEC.md` (tags) + `docs/envoy-go/phases/50-stats-sink-dogstatsd-batching/SPEC.md` (`max_bytes_per_datagram`) — graphite combines BOTH.
6. The existing sink code to re-derive against — `internal/statssink/{dogstatsd.go,delta.go,udp.go,flusher.go}` (the reuse substrate) + `internal/bootstrap/bootstrap.go` (`parseStatsSinks` dispatch + the statsd/dog_statsd parse arms) + `test/helpers/statsdrecv/` (the UDP receiver the `0101` differential reuses).
7. `docs/envoy-go/DECISIONS.md` — ADR-0265 (statsd) / ADR-0266 (dog_statsd) / ADR-0267 (batching) for the sink-ADR shape ADR-0275 mirrors; ADR-0044/0045/0080/0106 for process.
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the `reference_*` sink/differential/protojson traps.
