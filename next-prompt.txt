=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **PHASE 86 (`validate-sds-nil-provider`) IS OPEN AT THE PLAN (2026-08-11): ROW 86 IS `in-progress` AT `ROADMAP.md:148` AND `want` IS 118.** ⇒ **THIS SESSION PERFORMS THE PHASE-86 IMPL** (lifecycle-state 3 -> DONE) — the SINGLE leg of PLAN §2, Tasks 0-7 in the §3 TDD order, ONE atomic commit (D-86-SEQ). `STATE.md`'s `next-skill:` reads `IMPL for phase 86` — that IS your instruction. The IMPL's obligations are enumerated in §What-the-IMPL-owes below and in PLAN.md §11.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=118 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **`want` IS 118 AT THIS TIP AND STAYS 118 THROUGH THE IMPL** (the row FLIPS `done`; no row is added or removed). ⚠️ **BEFORE the flip check (1) prints EXACTLY `NOT DONE: row 86`; AFTER the flip it goes SILENT — the THIRD silent reading in project history. State both as measurements, run BOTH sides.** ANY OTHER row id appearing is a FINDING. ⚠️ **THE SENTINEL DOES NOT FIRE AT EITHER SIDE OF THE FLIP:** check (2) prints SIX (at `:196 :202 :208 :218 :224 :232` — re-derive, the anchors drift with every edit) and the condition is a CONJUNCTION. Check (3) is SILENT. ⚠️ **The IMPL's ONLY `ROADMAP.md` edit is row 86's status cell at `:148`** (`git diff --numstat` = `1 1`); every leak axis must be INVARIANT by whole-file count (lines 236 · rows 118 · union 6 · `-family row` 95/67 · `gRPC-family row` 2 · `Operational-tooling-family row` 3) and NOTHING narrows in any family-window sentence at row-done (the row's provenance is OUTSIDE the windows; the Op-tooling "RTDS/SDS validate companion" sentence stays BYTE-UNTOUCHED).

⚠️ **THE DOCTORING NCs REMAIN MANDATORY EVERY SESSION.** Both:
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md at want=118: pre-flip expect "NOT DONE: row 62" AND "NOT DONE: row 86"; post-flip "NOT DONE: row 62" ALONE
```
```sh
# the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```

⚠️ **AND WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -oiE '-family row'` fails with `grep: amily row: No such file or directory` and the surrounding arithmetic prints `base=0 now=0 delta=0`, **which reads exactly like "no change".** Observed live at FOUR phase-84 stages. **Always pass `--` before the pattern**, and remember the counts are form-dependent: **95 occurrences vs 67 LINES** at this tip (`grep -c` counts LINES). **A gate that reads zero on BOTH sides is not evidence of invariance.** ⚠️ AND the carried **ARM-A {119, 131} figure binds ONLY with its escape-aware pipe-split command** (BRAINSTORM-86 §6) — two ad-hoc forms tried at the PLAN do NOT reproduce it (`NF != 8` flags 17 rows); do not "correct" the figure with a different form.

**SENTINEL RE-RUN MECHANICALLY AT THE PHASE-86 PLAN CLOSE (2026-08-11, base `23ad1232`). IT DOES NOT FIRE; `stop` was NOT created** (verified absent at repo root AND stage worktree). ACTUAL output, recorded not predicted: input measured **236 lines / 118 data rows** first; **(1) `NOT DONE: row 86`** at `want=118` — the single expected line, denominator assertion silent; **(2) SIX — `:196 :202 :208 :218 :224 :232`** (⚠️ do NOT "fix" it and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases; the candidates sentences are a WINDOW onto a larger deferred backlog, not an inventory of it); **(3) SILENT**. The condition is a CONJUNCTION ⇒ the sentinel does NOT fire — and `STATE.md`'s `next-skill:` reads `IMPL for phase 86`, i.e. a next stage EXISTS and the loop continues.
- **ALL FOUR NCs FIRED AT THIS CLOSE:** row-62 doctoring => `NOT DONE: row 62` AND `NOT DONE: row 86` — with `NC LANDED? [ in-progress ]` inspected first; `want=117` => `NOT DONE: row 86` + `GATE FAIL: examined 118 data rows, expected 117`; check-(3) doctoring (residual `gRPC-family row` confirmed **2 -> 0** on the doctored copy first) => `NEVER OPENED: gRPC` while `WASM` correctly stayed silent; check-(2) one-arm counts **5** (long arm alone) / **1** (short arm alone), union **6** (a one-arm strip is NOT an NC for the union).
- ⚠️ **Re-run all three checks and both doctoring NCs yourself, BOTH SIDES of the row flip, and record what YOU see.**

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (ugrep under the hood). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Reproduced at TWENTY separate tips.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **THE METHOD BINDS; carry no standing discrepancy figure and no standing absolute.** ⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS; print FILE NAMES, never a count.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files; use `-v --no-index`, and `git status` is the discriminating check). ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **`grep -c` ON ZERO MATCHES IS TOOL-SPECIFIC** (only `git grep -c` prints nothing; all exit 1 — capture the value, never chain on the exit code). ⚠️ **`grep -c` COUNTS *LINES*, NOT *OCCURRENCES*.** ⚠️ **`\t` IN GNU ERE IS A LITERAL `t`** — use `-P`. ⚠️ **A `git grep` PATHSPEC ENDING IN `/` READS A FAIL-UNSAFE ZERO** (shape 33) — use the shell-glob form. ⚠️ **AN UNANCHORED PANIC-GREP FALSE-FIRES 14 TIMES ON A FULLY-GREEN DIFFERENTIAL LOG** — **the canonical gate is ANCHORED: `^panic:|DATA RACE|SIGSEGV`. A gate that fires on a green log is worse than no gate.**

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`** with FOUR blind spots (an em-dash, a DOT in `http-1.1`, two LETTER-suffixed ids). ⚠️ **EXECUTED on scratch copies at the phase-77 PLAN: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** ⚠️ **DO NOT restore the regex form.** Pre-flip check (1) prints exactly `NOT DONE: row 86`; post-flip it is SILENT — **any OTHER row id appearing on either side is a finding** — and the row-62 doctoring NC is what proves the silent side still LOOKS.

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

⚠️ **Run the union form, never one half of it.** ⚠️ **A ONE-ARM STRIP IS NOT AN NC** (6 -> 5 short / 6 -> 1 long; never 6 -> 0). ⚠️ **The documented trap's stated MECHANISM is refuted while the trap is real** — the inflation comes from ROADMAP table rows carrying BRAINSTORM prose. **Cite the command; do not re-inherit the explanation.** ⚠️ **The line numbers drift with edits — re-derive them; the current six are `:196 :202 :208 :218 :224 :232`.** ⚠️ **These six paragraphs remain the final sentinel blocker, and any edit near them is a sentinel-affecting edit.** All six are standalone family PROSE paragraphs (not rows), naming ~42 distinct candidates; every "retire the paragraphs" shape is doctrinally foreclosed (`reference_sentinel_matcher_string_self_clears`). ⚠️ **Row 86's pick came from OUTSIDE the windows** (BRAINSTORM-83 §5.6 provenance, the phase-85 shape) — **nothing narrows in any window sentence at row-86-done**, and the Op-tooling window's "an RTDS/SDS validate companion" is ADJACENT but DISTINCT (feature vs repair; BRAINSTORM-86 §2, SPEC §4, PLAN §2 Task 6f) — its sentence stays untouched.


==============================================


## STATUS AS OF 2026-08-11: PHASE 86 OPEN AT THE PLAN (row 86 `in-progress` at `ROADMAP.md:148`, base `23ad1232`). **THIS SESSION PERFORMS THE PHASE-86 IMPL — THE SINGLE LEG, ONE ATOMIC COMMIT (D-86-SEQ).**

**What the 86 PLAN landed (2026-08-11, base `23ad1232`, branch `phase-86-plan`, ONE squashed commit).** Docs-only: `PLAN.md` + `PROGRESS.md` in `docs/envoy-go/phases/86-validate-sds-nil-provider/`, `STATE.md` rolled, BRAINSTORM-85 evicted to `STATE_HISTORY.md` (478 -> 480, strictly append-only, controls 10/10, 8/8, 0/0 — both real controls DRIFTED UP again). `ROADMAP.md`, `DECISIONS.md`, `BEHAVIOR_CONTRACT.md` BYTE-UNTOUCHED (strict `PROPOSED` guard STAYS 1 — ARMED, disarmed only by THIS session's IMPL). ⚠️ **NAMED DEPARTURE CONTINUES: no investigation agents — the PLAN's cheap probes ran INLINE** (tip binary + SEVEN `--mode validate` executions; ports 47200-47203 template values only, nothing bound; no docker). **The RED anchors were RE-PROVEN at `23ad1232` with failure lines read:** arms A/B/C exit 1 (each wrapped `listener: %q: filter_chains[%d]:` — the IMPL's assertions MUST be substring, never full-string), control `configuration OK` 0; n1/n7 MASKED (arm-A string — the message-transition RED side), n3 already-parity. **NO SPEC CLAIM MOVED.**

### What the IMPL owes (PLAN.md §2-§3 — the EIGHT tasks, TDD order held INSIDE one commit)

1. **Task 0 — the RED census:** rebuild the tip binary (`-o` into scratch), regenerate ALL ELEVEN probe shapes (PLAN §6 recipe: fixture-0103/0108/0109-derived; template ports 47200-47299, validate binds nothing), run all under `--mode validate`, read every failure line. Expected pre-fix: control 0; A/B/C exit 1 arm strings; n1/n2/n4/n6/n7 exit 1 MASKED; n3 the `DELTA_GRPC` boot string; n5 exit 1 masked. ANY deviation stops the leg.
2. **T1 `internal/xds/provider_nofetch.go` (NEW ~37-45) + `provider_nofetch_test.go` (NEW):** `noFetchProvider` value type, `NoFetchProvider()`, `IsNoFetch()` (nil NEVER classifies — NC-8), two defense-in-depth erroring fetch methods (`xds: sds: internal: no-fetch (validate-mode) provider asked to fetch …` — the row's ONLY new strings). Tests RED first (compile fail, read the line).
3. **T2 `internal/boot`:** the `newSDSProviderAndClient` split (the ENTIRE `NewSDSProvider` body moves — parity by CODE-PATH, future arms inherited); `NewSDSProvider` wraps it; NEW `NewValidateSDSProvider` = run pre-scan, `(nil, nil)` at seen==0, else `client.Close()` (D-86-CONN; Close is pinned idempotent+nil-safe at `grpcclient_test.go:1911/:1982`) and return the sentinel. NEW `internal/boot/validate_sds_provider_test.go`: the six negatives table-driven (n1/n2/n3/n4/n6/n7 — n4's want-substring keeps the misleading `dial cluster` prefix VERBATIM), seen==0, arm-positive⇒IsNoFetch. `main.go:156` is the ONLY production `NewSDSProvider` call site — the split is signature-safe.
4. **T3 `internal/tls/config.go` (+25/-5):** arm-A `IsNoFetch` skip + `sdsCertPromised := true`; the `:518` check gains `&& !sdsCertPromised` (string bytes UNTOUCHED); VC-SDS and CVC blocks in `NewDownstreamConfig` return early on `IsNoFetch` AFTER their `ParseSDSConfig` calls (structural validation preserved, NO fabricated pool). Tests RED first: the accept arms fail with the sentinel's defense-in-depth fetch string (a DISCRIMINATING red). ⚠️ The three pins `config_test.go:921/:1198/:1310` BYTE-UNTOUCHED — verify green AND no diff-hunk overlap.
5. **T4 `validate/validate.go` (+10/-2):** literal nil -> `boot.NewValidateSDSProvider(dialer, bs, baseDir, bs.Stats)`, error propagated (these ARE the boot-parity rejects); `validate_test.go` end-to-end (3 accepts + n5-class accept + negative substrings) and `main_test.go` CLI subprocess arms — all RED first with the masking arm-A string. **Then the parity-string diff: re-run the probe battery vs the REBUILT binary AND diff each n-shape's validate string against a normal-mode boot run — byte-identical, DEMONSTRATED not asserted.**
6. **T5 comment rewrites** (LEAD with what survives): `config.go:344-346` (nil = upstream; validate now passes the sentinel), `:407-435` + `:447-452` (consumer rosters: validate LEAVES; THREE -> TWO production consumers), `manager.go:569-573`, the `validate/validate.go` package doc + `:48` comment (no-DIAL survives; the reject was its over-broad implementation).
7. **T6 docs riding ADR-0308:** the four `BEHAVIOR_CONTRACT.md` edits with text DRAFTED in PLAN §2 Task 6a-6d (finalize against landed code): `:1062` REVERSAL ("The `validate` path REJOINS…"), `:1050` THREE -> TWO consumers, `:1034` roster drop (the defense-in-depth parenthetical STAYS TRUE), `:948-958` SDS-acceptance extension (the phase-51 "No change to WHAT is validated" sentence STAYS). ADR-0308 §Decision + §Consequences APPENDED IN PLACE after the RETAINED footer (no renumber, NO `---` — `^---$` stays 216; strict guard 1 -> 0; headings stay 307; tail ADR-0308; next-free ADR-0309; §Consequences records: the parity contract, the reversal, realized cost vs floor, the reference per-side record, the two new never-called strings, the row flip). `ROADMAP.md:148` row 86 -> `done` (numstat `1 1`, `want` STAYS 118).
8. **T7 gates LAST as the commit's evidence:** gofmt (OUTPUT-gated) + golangci-lint (misspell US) on the five touched packages; `go test ./... -count=1` rc captured (`out=$(…); rc=$?`); `-race` on the four production-touched packages; the FULL differential suite **121/121 `INNER_EXIT=0`**, anchored panic gate silent; counts each with NC observed: fixtures 121 +0 · fuzzers 55/48 +0 · BackendKind 38 +0 · `go mod tidy -diff` empty · `go list -deps ./validate` edge set unchanged · **stat-surface DELTA 0 by same-command census both sides** (state the form; 145/21 `NewCounter(|NewGauge(` at the SPEC tip). Sentinel + all four NCs BOTH SIDES of the flip. STATE.md roll; §Recent eviction per DIRECT DATE READ; `next-prompt.txt` rolled to the post-86 pick (`git add -f`).

### Guard-preservation NC roster (PLAN §4 — lands IN THE SAME COMMIT as the lift)

The three pins stay; NINE new NCs: nil-still-rejects on each arm (NC-1/2/3) · **QUIC-wrapped SDS cert and CVC shapes still reject (NC-4/5 — QUIC's nil is hardcoded, mode-independent)** · test-only ctors reject (NC-6) · seen==0 + pure-inline CVC keeps the retained string (NC-7) · `IsNoFetch(nil)==false` (NC-8) · the sentinel's fetch methods error (NC-9).

### Budget — a FLOOR, not an estimate

PLAN §7 per-task: **~117-165 net production / ~440-760 test `.go`** (T1 37-45/40-80 · T2 30-40/120-200 · T3 25-35/100-160 · T4 10-15/180-320 · T5 +15-30 comment lines). The SPEC's ~110-160/~400-680 is the inner floor; NINE consecutive `reference_measured_prototype_is_a_lower_bound` firings say the top end is reachable. **Overrun beyond ~165/~760 is a RECORDABLE FINDING for ADR-0308 §Consequences, not a silent absorption.**

### The frozen error-string constraint set (PLAN §5)

**ZERO landed reject strings change.** The three arm strings, the six boot-parity strings (node · one-secret cap · ParseSDSConfig/DELTA_GRPC · `invalid secret name` · `unknown cluster` · `http2_protocol_options`), and `no tls_certificates configured` (gate gains `&& !sdsCertPromised`; bytes untouched). Only the sentinel's two never-called strings are new. Substring assertions ONLY (the listener wrap).

### ⚠️ COUNTS AT THIS TIP — RE-DERIVE MECHANICALLY, NEVER COPY

- **differential fixtures 121** — numeric tail `0119-grpc-unary-trailers`, next-free `0120`.
- **`ROADMAP.md` 236 lines / 118 data rows** (row 86 `in-progress` at `:148`); check-(2) anchors `:196 :202 :208 :218 :224 :232`; `-family row` **95/67**; `gRPC-family row` **2**; `Operational-tooling-family row` **3**; ARM-A flags **{119, 131}** (escape-aware command ONLY). ⚠️ The IMPL's ONLY edit is row 86's status cell (`1 1`).
- **`DECISIONS.md` 18066** lines · **307** headings · tail **ADR-0308 PROPOSED** · next-free **ADR-0309** (TAIL-derived) · strict guard **1 — the IMPL disarms it** · STATUS census **21** · `^---$` **216** (the completion adds NO `---`).
- **`BEHAVIOR_CONTRACT.md` 5955** · **`STATE.md` 64** · **`STATE_HISTORY.md` 480** · **`BOOTSTRAP_PROMPT.md` 522** · phase dirs **127**, `REVIEW.md` **37** (standing departure, named not claimed).
- **h2spec: cite `95 tests, 94 passed, 1 skipped, 0 failed` ONLY from your own tip's run** (skip 6.9.2/2 invariant).
- **fuzzers 55 / 48 files** · **BackendKind tail 38** over 39 declarations (do NOT "fix") · stat call sites: assert DELTAS only (two forms in circulation — 208/36 ARM-1 vs 145/21 `NewCounter|NewGauge` — NEITHER corrected; state which form you ran).
- **validate surface:** `validate/validate.go` **64** · `validate/validate_test.go` **432** (zero SDS) · `cmd/envoy-go/main_test.go` **1502** (zero SDS) · `internal/tls/config.go` **529** · `internal/tls/config_test.go` **2398** · `internal/boot/boot.go` **373** · reject sites `config.go:387-389 :436-438 :453-455`, fetch sites `:390` + the VC-SDS (`:100-136`) and CVC (`:137-228`) blocks, the `:518` check, QUIC nil `manager.go:567`, `main.go:156`.
- ⚠️ **STILL CONTESTED, NO NUMBER CARRIED:** the `STATE_HISTORY.md` archive-gap total · the loose `PROPOSED` matcher's whole-file count · the stat-surface absolute (1205 vs 1207 — WIDENED; only DELTAs asserted) · the zero-test `generic/*` suite count (13 vs 14).

### ⚠️ DOCUMENTARY DEFECTS — carried forward, still deliberately NOT fixed

**`ADR-0051` §2's false gate-scope sentence** corrected on the record by ADR-0307. **`ADR-0058`** cites `routerActionH2.doH2` at a dead location. **`ROADMAP.md` rows at lines 119/131 malformed** (ARM-A GUARD, not a fix). **`STATE.md` §Project counts FROZEN at phase 76.** **`harness_test.go:208` port inventory STALE.** **`body.go` nolint INERT** · **xDS cycle guard NOT AUTOMATED** · **`wasm/doc.go:219` two errors** · **ROADMAP cites `esalaine` FIVE times** · **`rbac.go:50` token `F2`** · **root `PROGRESS.md`** stray phase-32.1 doc · **`BEHAVIOR_CONTRACT.md:1062` + the `config.go`/`manager.go` consumer comments are KNOWN-STALE-PENDING — they are THIS IMPL's edit sites** (T5/T6), no longer carried once it lands.

### Candidates ledger (for the post-86 pick; re-derive EVERY cost at that tip)

- **The CONTINUATION two-sided repair** — strongest KNOWN product defect (server discard `conn.go:255-259` + client blindness + the client-side SETTINGS gap `h2/client.go:376-407`, all named in ADR-0307). h2spec MEASURED not to gate either side. Est. 2-4x row 85 (+1046 realized) — the natural next LARGE row; charter as a SPLIT phase.
- **The stat-surface recount** (1205-vs-1207 WIDENED; rides a future +0 row) · **`ssl.connection_error`** (+444 whole-`.go` floor) · **`test/conformance/grpc/`** (9/26 reachable) · **REVIEW.md restoration** (process-not-product) · **hygiene fold-ins** (`harness_test.go:208`, xDS cycle-guard) · the **~42 candidates in the six family windows**.

### ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + BOTH DOCTORING NCs YOURSELF, BOTH SIDES OF THE FLIP** — record ACTUAL output.
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** Lineage 86: B (three arms; one-arm framing refuted) -> S (mechanism measured; n7 found; BC:1062 rationale refuted) -> P (RED anchors re-proven; NO claim moved — stated as a measurement). The IMPL's job: the eleven-shape census at ITS tip, the parity-string diff, and recording every PLAN claim that does NOT survive execution (the phase-85 IMPL found TWO).
3. **LINE ANCHORS DRIFT; SYMBOL ANCHORS DID NOT — RE-VERIFY EVERY CITE AT YOUR TIP.** Key sites: `validate/validate.go:48-49` · `internal/tls/config.go:387-389 :436-438 :453-455 :390 :518` · `internal/boot/boot.go` (`NewSDSProvider` `:120-210`) · `internal/grpcclient/grpcclient.go:111-148` (`DialContext`) + `:369-372` (`NewSDSClient`) + `:395` (`Close`) · `manager.go:567 :573` · `main.go:156`.
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Still live: SDS dial-budget (TWO packages) · `internal/cluster` `-race` outlier · `internal/httpclient` zero-value · driver-receiver port race (roster 42; `0081` binds `0.0.0.0:42039`) · the two 84.2-era flakes (cleared, unclassed) · `TestServerConn_TinyWindowDelivery` (85-IMPL, cleared, unclassed) · the REFERENCE h2spec section-8 flip (reference-side only).
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only).
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`.
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL.** **A harness's exit code is not the command's.** **`gofmt -l` never exits non-zero — gate on OUTPUT.**
8. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-86-*` or stage worktree should outlive its stage close** — expect only `master` plus your own (verify with `git worktree list`).
9. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** (a)/(b) differential 121 fixtures, full suite with `INNER_EXIT`; (c) h2spec cite only from your own run; grpc-conformance deferred in writing; proxy-wasm 10/16; (d) fuzzers 55/48; (e) `INNER_EXIT` on every differential launch AND `go test ./...`; (f) no REVIEW.md — standing departure.

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 86 (validate-sds-nil-provider)' --format='%h %s'   # 3 at this tip: BRAINSTORM + SPEC + PLAN. An invented slug returns 0.
git log --grep '^phase 85 (h2spec-selector-repair)' --format='%h %s'      # 4 — a CLOSED phase, use it as a FIRING CONTROL
```
⚠️ **The count comments are LIVE FIGURES — VERIFY before relying.** Take the NEWEST line (`| head -1`).

Subagent-driven per `feedback_execution_style` where the leg's work splits cleanly — but note D-86-SEQ makes this ONE atomic commit: if subagents are used they commit LOCALLY on the ONE stage branch in TDD order and the controller squashes (`feedback_subagents_no_push`, `feedback_push_to_origin`); the 86-lineage inline departure is ALSO available — NAME the choice either way. Parallel subagents need **PRIVATE scratch** and tear down docker **BY NAME**. ⚠️ **Band private ports outside BOTH `20000-31007` AND `11000-14999`, clear of the static fixture ports (10000-19172); `0081` binds `0.0.0.0:42039`.** The 86 lineage used 47000-47014/47100-47103/47200-47203 as TEMPLATE VALUES ONLY (validate binds nothing) — **the IMPL's template values band from `47210-47299`; if anything binds REAL ports (it should not — validate binds nothing, and the n-shape normal-mode boots fail before bind), band from `47300-47399`.** ⚠️ **ONE WORKTREE PER AGENT preferred; explicit-path staging if shared; never an unscoped `git restore`.** Per-task `gofmt` + `golangci-lint` on touched packages (⚠️ misspell locale US). ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch.** ⚠️ **If reference probes are re-run (the IMPL does NOT need any — Q3 is closed): docker BY NAME (`i86-*` suggested), `--rm`, verify gone.**

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root — observed live AGAIN at the 86 PLAN (every compound command's trailing state printed the repo root). Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — sha256 against the base blob, never by eye; work DETACHED so a failed revert cannot dirty a branch.

⚠️ **THE ARCHIVE-ABSENCE GUARD — USE THE ROBUST FORM** (bullet-anchored + whole-file fixed-string, NC'd with a REAL target re-derived FROM THE FILE first; a control's own count is a LIVE FIGURE: `phase 82 … IMPL done` read **10** at this close, up from 9; `phase 83 … PLAN done` read **8**, up from 7). **Verify STRICTLY APPEND-ONLY**: `git diff --numstat` = `N 0` AND base a byte-exact PREFIX (`cmp`). The IMPL close rolls §Current to IMPL-done and §Recent evicts per the DIRECT DATE READ at that close.

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads `IMPL for phase 86` — that IS your instruction.** §Project counts SELF-CONTRADICT §Current — do not anchor on them (frozen at phase 76).
2. `docs/envoy-go/phases/86-validate-sds-nil-provider/PLAN.md` — the EIGHT tasks (§2), the TDD order (§3), the NC roster (§4), the string constraint set (§5), the probe recipe (§6), the budget floor (§7), what the IMPL owes (§11).
3. `docs/envoy-go/phases/86-validate-sds-nil-provider/SPEC.md` — the decided mechanism (§1), the parity/reference matrices (§2-§3), the contract sweep (§4).
4. `docs/envoy-go/DECISIONS.md` — the ADR-0308 tail block (PROPOSED — §Decision/§Consequences are THIS session's to append).
5. `validate/validate.go` + `internal/tls/config.go` + `internal/boot/boot.go` + `internal/xds/provider.go` — the code the leg edits.

## After the IMPL lands — the roll-forward
Row 86 flips `done`; `want` stays 118; `ROADMAP.md` numstat `1 1`; ADR-0308 COMPLETE (strict guard 1 -> 0); the four contract edits landed. Re-run the sentinel + NCs BOTH SIDES and record ACTUAL output (pre-flip (1) `NOT DONE: row 86` · post-flip (1) SILENT — the third in history · (2) SIX · (3) SILENT ⇒ no fire EITHER side — the conjunction needs ALL THREE silent AND no deferred candidates, and check (2) still prints SIX). **Roll this file to the post-86 SELF-PICKED BRAINSTORM** per the 2026-07-12 standing directive (no banked mid-lifecycle work will remain), carrying: the realized cost vs the floor, every PLAN claim that did not survive execution, the sentinel state you observed, the re-derived counts, the candidates ledger, and the flake register verbatim.
