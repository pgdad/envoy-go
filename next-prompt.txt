=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-CHECKED 2026-07-13 (after the phase-60.1 IMPL landed, run MECHANICALLY) — it does NOT fire. Do NOT create `stop`.** State:
- (1) prints `NOT DONE: row 60`, `NOT DONE: row 61` — TWO `in-progress` rows. Row 60 `xds-sds-server-cert` STAYS `in-progress`: its 60.1 substrate leg is now IMPL-LANDED, but the row flips `done` only when the 60.2 TLS-apply leg ALSO lands (ADR-0106, `reference_roadmap_split_phase_row_done`). Row 61 `http3-downstream-listener` is banked at SPEC (a 61.1/61.2/61.3 split). This ALONE blocks `stop`.
- (2) prints THREE live "candidates:" sentences — Observability, xDS, and HTTP/3 ⇒ three families STAY OPEN. (`reference_sentinel_deferred_sentence_live_vs_historical`.)
- (3) prints `NEVER OPENED: gRPC`, `Runtime`, `WASM` ⇒ THREE families never opened. Operational-tooling also STAYS OPEN.

All three checks print ⇒ the sentinel does NOT fire.

**STATUS AS OF 2026-07-13: the phase-60.1 IMPL LANDED (the xDS SDS substrate; row 60 STAYS `in-progress`).** The NEXT IMPLEMENTABLE STAGE is the **phase-60.2 PLAN** (`xds-sds-tls-cert-apply`, lifecycle-state 2 → 3 for the 60.2 leg — SPEC-60 is already banked; 60.2 has never been PLAN'd). 60.2 is the OBSERVABLE end-to-end leg that plumbs the 60.1 substrate into the downstream TLS context + adds the differential. Advancing the banked xDS 60.2 leg is more defensible than opening a brand-new subject (a split leg awaiting its PLAN, per the standing directive). Row 61 (HTTP/3) rolls after the xDS legs, or per a future roller pick.

**What the 60.1 IMPL delivered (the substrate 60.2 builds on — RE-VERIFY against the tree at session start):**
- `internal/xds` (production package, package proper — imports NEITHER `internal/grpcclient` NOR `internal/tls`, the CYCLE GUARD): `SecretProvider interface { FetchInitialCertificate(ctx, secretName) (*crypto/tls.Certificate, error) }`; `Provider` + `NewProvider(opener StreamOpener, node Node, baseDir string, timeout time.Duration, stats *SDSStats)`; the EXPORTED `Stream`/`StreamOpener` seams; `fetchSecret` (SotW ACK/NACK); `parseSecret` (Any→Secret→cert); `Node{ID,Cluster}`; `SDSStats` + `RegisterSDSStats(reg, secretName)` (the 5-counter `sds.<secret>.*` subset, `IsValidName`-guarded); `FuzzDiscoveryResponseParse`.
- `internal/xds/xdsgrpc` (a SECOND production package — the BOOT-SIDE adapter that isolates the `grpcclient → cluster → tls` edge): `NewOpener(*grpcclient.SDSClient) xds.StreamOpener`. **60.2's boot wiring builds the provider via `xdsgrpc.NewOpener(grpcclient.NewSDSClient(dialer, sdsCluster))` — NOT by importing grpcclient into `internal/tls`.** This is the load-bearing design that keeps `tls → xds` acyclic at 60.2.
- `internal/grpcclient.SDSClient` (BIDI `StreamSecrets` wrapper) + `test/helpers/sdsserver` (driver-owned fake SDS server).
- **The 60.1→60.2 apply-cycle rule:** `internal/tls` (60.2) references `xds.SecretProvider` as a `NewDownstreamConfig` parameter. This is acyclic ONLY because `internal/xds` proper is grpcclient/cluster/tls-free. Do NOT reintroduce a `grpcclient`/`cluster` import into `internal/xds` — thread the built `*xds.Provider` (constructed in `main.go` via `xdsgrpc.NewOpener`) DOWN through `boot.Construct` → the listener manager → `NewDownstreamConfig(ts, baseDir, provider)`.

**ADR reservation ledger (provisional; each finalized at its IMPL in landing order):** ADR-0277 = phase-59 (LANDED) · ADR-0278 = xDS 60.1 (LANDED this stage) · ADR-0279 = HTTP/3 (61) · **ADR-0280 = xDS 60.2 (the NEXT xDS ADR to land, at the 60.2 IMPL)** · next-free unreserved **ADR-0281**. **60.2 LANDS THE DIFFERENTIAL FIXTURE** — RE-DERIVE the actual dir number against the master tip at the 60.2 IMPL (the tail is now `0102-tracing-custom-tags-literal`; landing order assigns `0103-xds-sds-server-cert` unless another fixture lands first).

**Current counts (VERIFIED after the phase-60.1 IMPL, on the frozen stage HEAD):** fixtures **104** (tail `0102-tracing-custom-tags-literal`) · fuzzers **55** (`FuzzDiscoveryResponseParse` added) · DECISIONS tail **ADR-0278** (next-free reserved ADR-0279/0280) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** (+0 static; the 5 `sds.*` counters are DYNAMIC per-secret) · new Go packages this stage **+2 prod** (`internal/xds`, `internal/xds/xdsgrpc`) **+1 test** (`test/helpers/sdsserver`) · new go.mod modules **0**. RE-VERIFY against the actual master tip at session start (`git fetch` first).

**Locating the phase-60.1 IMPL stage squash** (a router cannot name its own sha — identify by SUBJECT, never by position):
```sh
git log --oneline --grep='^phase 60.1 (xds-sds-stream-substrate) IMPL' -1
```

**THE NEXT ACTION: the phase-60.2 PLAN (lifecycle-state 2 → 3 for the 60.2 leg; docs-only — produce `docs/envoy-go/phases/60-xds-sds-server-cert/PLAN-60.2.md` + a `PROGRESS-60.2.md` scaffold; NO production `.go` changes this stage; row 60 STAYS `in-progress`).** After the 60.2 PLAN lands, the roller's next stage is the phase-60.2 IMPL (which flips row 60 `done`). The sentinel governs termination, not this list.

==============================================

## What THIS session does (the phase-60.2 PLAN)

Perform the PLAN for phase 60.2 (`xds-sds-tls-cert-apply`) — the docs-only decomposition of the SPEC-60 60.2 TLS-apply scope into a MECHANICAL TDD spine. Use `superpowers:writing-plans` in a FRESH worktree off master (`.worktrees/phase-60.2-plan`, branch `phase-60-xds-sds-tls-cert-apply-plan`, per `feedback_git_worktrees`). This is a SINGLE STAGE (the PLAN); do NOT start the IMPL.

**The 60.2 TLS-apply scope (from SPEC-60 §3.2/§3.3/§3.4/§6/§8/§9 — RE-READ them):**
- **The one-arm reject lift** (`internal/tls/config.go:153`, downstream-gated): REPLACE the wholesale `tls_certificate_sds_secret_configs` reject with a downstream-only lift that parses the `SdsSecretConfig{name, sds_config}`, dispatches to the threaded `provider`, BLOCKS on the first Secret. Add a `provider xds.SecretProvider` (or a narrow interface) parameter to `NewDownstreamConfig`/`commonTLSContextToConfig` (nil on the upstream/validate paths — the upstream arm keeps the byte-identical reject).
- **The config seam** (§3.2, DISPOSED MANAGEABLE — no async warmup refactor): thread the built provider from `main.go` (constructed via `xdsgrpc.NewOpener(grpcclient.NewSDSClient(dialer, sdsCluster))`, next to the sinks/`tracingProvider`) → `boot.Construct` → `listener.NewManagerWithBaseDirAndAllowH2C` → `buildListenerRuntimeWithCtx` → `NewDownstreamConfig(ts, baseDir, provider)`. RE-DERIVE every `file:line` at PLAN time (`feedback_brief_citations_not_evidence`; the 60.1 IMPL may have shifted line numbers).
- **The init-fetch BLOCK-then-BOOT-FAIL DEPARTURE** (§3.4): `FetchInitialCertificate` already blocks bounded by `initial_fetch_timeout` + returns a classified error (60.1); 60.2 makes that error BOOT-FAIL the listener build (envoy-go's documented DEPARTURE — the reference serves cert-less; §11 Arm B). A subject-side boot-reject test asserts the timeout error substring.
- **The strict-reject roster** (§6, all ADR-0080-distinct): the six departures (ads/self/DELTA_GRPC/google_grpc/validation_context-SDS/upstream-SDS) + the NODE boot-requirement arm (arm 7 — SDS configured while `node.id`/`node.cluster` empty ⇒ boot-reject). RE-DERIVE the bootstrap node access.
- **The driver-owned SDS differential fixture** (§8): a new dir (RE-DERIVE the number — anticipated `0103-xds-sds-server-cert`; fixtures 104 → 105) with the `test/helpers/sdsserver` (built at 60.1) reachable by BOTH proxies on a shared bridge (`reference_docker_probe_bridge_network`); assert the CLIENT-OBSERVED SERVED LEAF (serial/SAN) cross-side (`Errorf`-per-property, prove the assertion LIVE with a `-count=1` break, `reference_differential_break_protocol_count1`/`reference_deliberate_break_wrong_assertion`). The timeout/mgmt-down/secret-not-found cases are SUBJECT-side boot-reject unit tests, NOT extra differential dirs (`reference_differential_fixture_dispatch_constraint`).
- **BEHAVIOR_CONTRACT** (§9) xDS/SDS section + **ADR-0280** (§Context re-uses SPEC §13's frame; §Decision/§Consequences at the 60.2 IMPL per ADR-0044) + **ROADMAP row 60 → `done`** (once 60.2 lands, ADR-0106; UPDATE the xDS deferred sentence — re-run the sentinel check-(2) grep, `reference_sentinel_deferred_sentence_live_vs_historical`, EXACTLY ONE live "candidates:" match).

**Anticipated at the phase-60.2 IMPL (record in the PLAN's global constraints; RE-VERIFY at IMPL):** the `tls/config.go` lift + the `provider` threading (config seam) + the boot-fail gate + the 6+1 reject arms + `test/helpers/sdsserver`-driven differential (`0103-*`, fixtures 104 → 105) + BEHAVIOR_CONTRACT + ADR-0280 + ROADMAP row 60 `done`. Counts: stat surface **1201 → +5** (the `sds.*` counters now register at boot under the differential's SDS config — the DYNAMIC surface materializes; RE-VERIFY the exact count convention) · fixtures **104 → 105** · fuzzers **55 (+0)** · BackendKind **38 (+0)** · DECISIONS tail **ADR-0278 → ADR-0280** (0279 = HTTP/3) · new Go packages **0** (60.1 built them) · new go.mod modules **0**. Row 60 flips `done` at the 60.2 IMPL (both legs landed, ADR-0106).

## Honor throughout (the discipline, unchanged)
- **Lifecycle = one stage per session.** This session: the phase-60.2 PLAN ONLY (docs-only). After it lands: the phase-60.2 IMPL.
- **Process anchors:** ADR-0044 (ADR §Decision/§Consequences land at the IMPL — ADR-0280 body at the 60.2 IMPL) · ADR-0080 (distinct per-arm reject substrings — the 6+1 arms) · ADR-0106 (row 60 flips `done` only when BOTH legs land — the 60.2 IMPL is that moment). Next-to-land xDS ADR is **ADR-0280**.
- **The cycle guard (load-bearing, from the 60.1 IMPL / ADR-0278):** `internal/xds` proper is `grpcclient`/`cluster`/`tls`-free; the boot-side `internal/xds/xdsgrpc` carries the `grpcclient` edge. At 60.2 `internal/tls` references `xds.SecretProvider` (acyclic BECAUSE xds is tls-free); the provider is CONSTRUCTED in `main.go` via `xdsgrpc.NewOpener` and THREADED down as a value — do NOT import `grpcclient`/`cluster` into `internal/xds` or `internal/tls`. The 60.2 PLAN must verify the resulting import graph stays acyclic (`go list -deps` at each edit site).
- **Subagent-driven execution** (`feedback_execution_style`, `feedback_subagent_autocommit_claudemd`, `feedback_subagents_no_push`): even a docs-only PLAN stage runs subagent-driven where it helps; subagents commit LOCALLY only; the controller squashes at stage-close and pushes. Watch for HEAD-detach on deliberate breaks (`feedback_subagent_worktree_detach`) and stale-path targeting (`feedback_subagent_worktree_path_targeting`).
- **`next-prompt.txt` IS TRACKED** (`reference_next_prompt_tracked_despite_gitignore`) — edit it inside the stage worktree and fold into the stage commit; locate commits by SUBJECT (`git log --grep`), never by position.
- **Deferred maintenance (fold into the next `udp.go`/statssink-driver-touching row — carried from the phase-57 final review; phases 58/59/60.1 touched NEITHER `udp.go` NOR a statsd/graphite/dog_statsd sink, and 60.2 is TLS/differential so it does NOT either — these STAY OPEN):** F-1 the 0101 driver's counter tag snapshot records the EXPECTED map; F-2 `udp.go` `udpWriter` doc comment names two sinks (three embed it now); F-3 BEHAVIOR_CONTRACT 0101 note says "`differential.HostGatewayIP` helper" where the driver uses its LOCAL duplicate. (Ride until the OTLP-metrics or another statsd/graphite/dog_statsd sink row reopens `udp.go`.)

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-60.2 PLAN is next; SPEC-60 is banked; the 60.1 substrate is landed).
2. `docs/envoy-go/phases/60-xds-sds-server-cert/SPEC.md` — the 60.2 scope: §3.2 (config seam, MANAGEABLE), §3.3 (the one-arm reject lift), §3.4 (init-fetch BLOCK-then-BOOT-FAIL DEPARTURE), §6 (the reject roster incl. arm 7 node), §8 (the differential fixture), §9 (BEHAVIOR_CONTRACT), §11 (probe evidence — esp. Arm A served-leaf, Arm B timeout, Arm A-pre node), §12 (edit-site roster — RE-DERIVE against the tip), §13 (ADR §Context frame).
3. `docs/envoy-go/phases/60-xds-sds-server-cert/PLAN-60.1.md` + `PROGRESS-60.1.md` — what the substrate delivered (the `xds.SecretProvider`/`Provider`/`NewProvider` API, the `Stream`/`StreamOpener` seams, `xdsgrpc.NewOpener`, the `sds.*` stats, the cycle guard the 60.2 wiring must honor).
4. `docs/envoy-go/DECISIONS.md` — **ADR-0278** (the 60.1 substrate + the cycle-avoidance design 60.2 depends on); the ADR-0278 §Consequences naming 60.2/ADR-0280.
5. `docs/envoy-go/ROADMAP.md` — row 60 `in-progress`; the xDS-family row + the LIVE xDS deferred sentence (still ONE live match).
6. `docs/envoy-go/STATE.md` — the active-phase header (phase 60.1 IMPL done; NEXT = the phase-60.2 PLAN).
7. The seam source (RE-DERIVE every `file:line` against the tip): `internal/tls/config.go` (`:153` reject, `NewDownstreamConfig`/`commonTLSContextToConfig`) + `internal/tls/datasource.go` · `internal/listener/manager.go` (`NewManagerWithBaseDirAndAllowH2C`/`buildListenerRuntimeWithCtx` + the `NewDownstreamConfig` call sites) · `internal/boot/boot.go` (`Construct`) + `cmd/envoy-go/main.go` (the dialer/sink/tracingProvider construction — where the SDS provider is built via `xdsgrpc.NewOpener`) · `internal/xds/` + `internal/xds/xdsgrpc/` (the API 60.2 consumes) · `internal/bootstrap/bootstrap.go` (the node access for arm 7).
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the `reference_*` traps (esp. `reference_differential_grpc_receiver_driver_owned`, `reference_docker_probe_bridge_network`, `reference_differential_fixture_dispatch_constraint`, `reference_differential_break_protocol_count1`, `reference_deliberate_break_wrong_assertion`, `reference_fatalf_makes_assertions_unreachable`, `reference_dynamic_stat_name_charset_guard`, `reference_sentinel_deferred_sentence_live_vs_historical`, and the split-phase references), plus any NEW memory on the xDS transitive-import cycle guard.
