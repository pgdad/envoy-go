START a fresh cold-start session for envoy-go — **phase 32 (`network-filter-redis-proxy`) BRAINSTORM is DONE; the SIXTH §9 Network-filters-family row is OPEN as a parent pre-split row**. `redis_proxy` (`envoy.filters.network.redis_proxy`) is the project's FIRST terminal routing proxy: unlike every prior §9 row (echo/sni_cluster/zookeeper_proxy/mongo_proxy/kafka_broker — observational sniffers on a `[filter, tcp_proxy]` chain), redis_proxy IS the connection terminator (parses RESP, routes commands to an upstream cluster, pools/pipelines upstream connections itself — there is NO passive-observe MVP). The BRAINSTORM settled a SINGLE-ROUTE-TERMINAL MVP behind a 2-way seam-first pre-split (parent row 32 + 32.1/32.2) and registered the ROADMAP rows. The next lifecycle step is the **phase-32 PARENT SPEC** (`superpowers:writing-plans` scoped to SPEC authoring — per `feedback_execution_style`; work in a git worktree per `feedback_git_worktrees`).

The phase-32 BRAINSTORM landed on the `phase-32-network-filter-redis-proxy-brainstorm` branch (BRAINSTORM.md + README.md + ROADMAP rows 32/32.1/32.2 + the §9 roster refined + STATE/next-prompt advance; to be squash-merged to master + pushed by the controller). Substantive predecessor on master: the phase-31 IMPL squash `928287a` (the `kafka_broker` §9 row; phase 31 CLOSED; docs-only SHA-fill follow-ups trail it as the live tip — confirm via `git log --oneline -1`). Earlier on master: the phase-31 PLAN squash `12f7cac`; the phase-31 SPEC squash `6104036`; the phase-31 BRAINSTORM squash `78d5f36`; the phase-30 IMPL squash `2d24f34` (phase 30 CLOSED).

## What this session does — the phase-32 PARENT SPEC

Author `docs/envoy-go/phases/32-network-filter-redis-proxy/SPEC.md` (the PARENT SPEC, per the phase-22/25/26/28/29 parent-row precedent) from the BRAINSTORM. The parent SPEC:

- **Formalizes the 2-way split surface-mapping** — 32.1 (`network-filter-redis-upstream-pool-seam-and-codec` — the upstream connection-pool / cluster-routing FRAMEWORK SEAM + the RESP codec + the `redisproxy` package foundation + the 10th built-in + the `TCPRedisResponder` BackendKind + a minimal catch_all single-command round-trip via `0055-redis-roundtrip`) and 32.2 (`network-filter-redis-commands-and-stats` — the full single-route command set + the per-command/downstream/upstream stat roster + the `redis.` prom arm + the differential command matrix + the 41st fuzzer `FuzzRESPDecode` + the parent-row-32 ROLLUP). Each sub-phase re-checks the ADR-0045 gate at its own PLAN.
- **Executes the BRAINSTORM §10 empirical pins D32-1..D32-8 IN-SESSION** (parallel-subagent fan-out per the 25/26/27/28/29/31 SPEC precedent) live against the contrib reference Envoy (`envoyproxy/envoy:contrib-v1.37.2`, ADR-0227) + go-control-plane `/envoy` v1.32.4 bindings, per `reference_docker_probe_bridge_network` (bridge network + STRICT_DNS backend hostname; verify the round-trip ran via `downstream_cx_rx_bytes_total > 0` + a non-empty downstream RESP response). The BLOCKING pins: **D32-1** (TypeURL via `proto.MessageName` on `RedisProxy`; confirm it is CORE `/envoy`, NOT `/contrib` → anticipated ZERO new go.mod dep — UNLIKE kafka_broker; confirm `go mod tidy` adds nothing); **D32-2** (the EXACT stat roster — `command.<cmd>.*` eager-vs-DYNAMIC creation, the downstream/upstream counter set, which `upstream_cx_*` are mirrorable vs per-side-pinned, the `redis.` Prometheus arm inline-vs-label-hoist); **D32-3** (the upstream connection-pool / cluster-routing seam API — host resolution + dial reuse of `cluster.Manager`, the FIFO/positional reply-correlation contract, the multiplexing model). Plus D32-4 (per-field parse-accept-vs-reject + the `0056` boot-reject arm), D32-5 (RESP wire framing adopted verbatim + the downstream-response byte-equivalence proof + per-side pooling-stat asymmetry), D32-6 (PING/AUTH local-reply semantics), D32-7 (the `IsValidName` charset guard at the codec boundary), D32-8 (the ADR-0045 envelope per sub-phase + any close-direction counters).
- **Anchors the ADR-0229 §Context draft** (the redis_proxy filter + parent umbrella) per ADR-0044 — DECISIONS.md tail advances ADR-0228 → ADR-0229 at THIS parent SPEC. (ADR-0230, the upstream-pool framework seam, anchors its §Context at the 32.1 SPEC; §Decision/§Consequences bodies at the respective IMPL.)

Counts stay UNCHANGED at the SPEC except the DECISIONS tail (→ ADR-0229). The surface/fixture/fuzzer/BackendKind counts advance at the IMPL sub-phases.

## Honor throughout

- `feedback_execution_style` (subagent-driven IMPL when it gets there; the SPEC empirical-pin fan-out is parallel-subagent) · `feedback_git_worktrees` · `feedback_subagents_no_push` (subagents commit local-only) · `feedback_push_to_origin` (the controller squash-merges + pushes at stage-close) · `feedback_pertask_gofmt_lint`.
- ADR-0106 (§9 family flat-row discipline; (d) the in-row split-gate) · ADR-0045 (split-gate) · ADR-0219 (the upstream-cluster-override seam — the nearest routing precedent) · ADR-0223 (per-connection mutex) · ADR-0060 (histograms deferred) · ADR-0044 (§Context-at-SPEC, §Decision+§Consequences-at-IMPL) · ADR-0227 (the contrib pin) · ADR-0080 (byte-stable rejects).
- Project memories: `reference_network_filter_typeurl_extensions`, `reference_wire_format_both_sides_see_same_bytes` (the principle EXTENDS to the downstream RESPONSE bytes — redis_proxy GENERATES them), `reference_close_direction_framework_gap`, `reference_docker_probe_bridge_network`, `reference_dynamic_stat_name_charset_guard` (redis commands are likely DYNAMIC, unlike kafka's table-bounded eager roster — the guard is load-bearing), `reference_network_chain_terminal_handoff_ends_ondata`, `reference_differential_asserter_dispatch`, `reference_differential_fixture_dispatch_constraint`, `reference_differential_break_protocol_count1`, `reference_differential_reference_parses_full_message`.

## Read first (in order)

1. THIS FILE — the phase-32 parent-SPEC cold-start.
2. `docs/envoy-go/phases/32-network-filter-redis-proxy/BRAINSTORM.md` — the phase-32 charter (Q0/Q1/Q-split/Q-pool-seam; the framework-survey §3 — the new upstream-pool seam; the stat hypothesis §5; the fixture envelope §6; the anticipated ADR-0229/0230 §7; the deferred items §8; the §10 D32-1..D32-8 empirical pins this SPEC executes) + `./README.md`.
3. `docs/envoy-go/STATE.md` — active-phase `phase 32 BRAINSTORM done`; lifecycle-state → the phase-32 parent SPEC; next-skill `superpowers:writing-plans`; the current counts (536 stats / 56 fixtures / 40 fuzzers / BackendKind tail 31 / DECISIONS tail ADR-0228, next-free ADR-0229).
4. `docs/envoy-go/ROADMAP.md` — parent row 32 + sub-rows 32.1/32.2 + the §9 Network-filters family roster ({thrift} remains after redis) + the row-26..31 history.
5. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the `### envoy.filters.network.*` subsections (echo / sni_cluster / zookeeper_proxy / mongo_proxy / kafka_broker — the §9-row precedents) + the `## Network filters` framework subsections (the chain framework + the 28.1 conn-wrap seam + the 29.3 async halt/resume seam — the as-built seams the new upstream-pool seam extends).
6. `docs/envoy-go/DECISIONS.md` — ADR-0106 / ADR-0045 / ADR-0219 / ADR-0223 / ADR-0060 / ADR-0044 / ADR-0227 / ADR-0080 + ADR-0228 (the kafka_broker filter — the most-recent §9-row decision record).
7. The phase-31 + phase-29 phase directories (the §9-row lifecycle precedents: BRAINSTORM → SPEC → PLAN → IMPL; phase-29 is the parent-pre-split precedent the phase-32 parent SPEC mirrors).
8. `internal/filter/network/` framework (`chain.go`/`terminal.go`/`prefixconn.go`/`writeconn.go`/`readconn.go` — the terminal + read/write seams the upstream-pool seam builds on) + `internal/filter/tcpproxy/` + `internal/cluster/` (the terminal-filter + cluster-host-resolution + TCP dial path the seam reuses) + `internal/filter/network/kafkabroker/` + `internal/filter/network/mongoproxy/` (the §9 package-shape precedents) + `internal/stats/name.go` (the prom-arm patterns).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`.

## Counts at master tip (phase 32 BRAINSTORM — UNCHANGED until the SPEC/IMPL)

- stat surface **536**; differential fixtures **56** (tail `0054-kafka-boot-reject`); fuzzers **40** (`FuzzKafkaDecode`); BackendKind tail **31** (`TCPKafkaResponder`); DECISIONS.md tail **ADR-0228** (next-free **ADR-0229**; phase 32 anticipates ADR-0229 [filter] + ADR-0230 [seam]).
- conformance h2spec 53/53 + proxy-wasm 10/10 (image-independent).
- reference pin: `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227).
- Toolchain: Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); go-control-plane `/envoy` v1.32.4 (ADR-0008) + `/contrib` v1.32.4 (the phase-31 dep). redis_proxy is anticipated to be CORE `/envoy` (D32-1) → NO new dep.
- §9 Network-filters family: rows 26/27/28/29 DONE + row 30 (pin-refresh) DONE + row 31 `kafka_broker` DONE + **row 32 `redis_proxy` IN-PROGRESS (BRAINSTORM done)**. After phase 32 the candidate {thrift} remains (the last §9 candidate — a terminal-proxy surface reusing the 32.1 upstream-pool seam).
