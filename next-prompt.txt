=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

✅ **PHASE 97 (`quic-chain-selection-order`) IS OPEN. ROW 97 READS `in-progress` AT `ROADMAP.md:159`. LIFECYCLE-STATE IS `1`.** ⇒ **THIS SESSION RUNS THE PHASE-97 SPEC** (state 1 -> 2), discharging every owed item of `BRAINSTORM.md` §10 and producing `SPEC.md` in the phase directory. **DO NOT open a new subject and DO NOT re-pick** — there is banked mid-lifecycle work and it is this row.

⚠️ **THE SPEC'S BLOCKING UNKNOWN IS NAMED AND IT IS A REFERENCE MEASUREMENT: does the pinned reference ACCEPT a QUIC listener carrying BOTH a `default_filter_chain` and an eligible `filter_chains[]` entry, and WHICH chain does it serve?** The BRAINSTORM measured the SUBJECT on two shapes and could not measure the reference — Docker was serialized to one agent and spent on the front-runner's parity question. ⚠️ **THE FIX SHAPE ENCODES THAT ANSWER** (`reference_fix_shape_encodes_unmeasured_parity`, phase 96 §9.6, firing again unchanged): a repair that always prefers `filter_chains[]` is correct for §Decision 2's empty-match arm and WRONG for §Decision 1's no-match arm, so **arms are owed on BOTH sides of the eligibility conditional**. ⚠️ **AND `startQUIC` NEEDS A TLS CONFIG BEFORE ANY CONNECTION EXISTS**, so "call `SelectChain`" is not a specification until the SPEC says with what inputs, at what moment, for all three call sites.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. Parses FIELDS instead of
#     matching a pattern, and asserts the row DENOMINATOR so a deleted row is
#     itself an error.
#     ⚠️ want IS 129 and ROADMAP.md IS 247 lines at the phase-97 BRAINSTORM close.
#     ⚠️ CHECK (1) IS NO LONGER SILENT — it reads ONE line, `NOT DONE: row 97`,
#     because THIS phase's row is registered `in-progress`. It goes silent again
#     at the phase-97 IMPL. A NON-SILENT check (1) is the NORMAL mid-phase state.
awk -F'|' -v want=129 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️⚠️ **CHECK (2)'s SIX IS STILL THE ONLY STRUCTURAL BARRIER, and check (1) is merely borrowing a voice from this open row.** **RUN ALL FOUR NCs EVERY SESSION:**
```sh
# NC-A — doctor a row to `in-progress`. ⚠️ READS *TWO* LINES at this tip
#        (`NOT DONE: row 62` AND `NOT DONE: row 97`). It read ONE before the
#        phase-97 BRAINSTORM added row 97. MEASURED at that close.
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md with want=129.

# NC-B — the DENOMINATOR, want=128 on the real file. ⚠️ READS *TWO* LINES —
#        `NOT DONE: row 97` AND `GATE FAIL: examined 129 data rows, expected 128`.
#        It read ONE before the row ADD.

# NC-C — the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```
⚠️ **AND RUN THE CHECK-(2) POSITIVE CONTROL — AND READ THE COMMAND BEFORE TRUSTING ITS RESULT.** The longer phrase does not CONTAIN the shorter one as a substring, so a control that substitutes only one phrase reports a residual of 5 and reads like a finding. Substitute BOTH and assert the substitution count:
```sh
sed -e 's/deferred candidates:/deferred candidatesXX/g' \
    -e 's/remaining deferred (not-yet-chartered) candidates:/remaining deferred (not-yet-chartered) candidatesXX/g' \
    docs/envoy-go/ROADMAP.md > "$SCRATCH/c2pc.md"
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' "$SCRATCH/c2pc.md" | wc -l  # want 0
grep -o 'candidatesXX' "$SCRATCH/c2pc.md" | wc -l                                                                # want 6 — PROVE it landed
```

⚠️ **WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -c '-family row'` fails (GNU: `grep: amily row: No such file or directory`; ugrep: `option -f: cannot read amily row` — **both rc=2**) and the surrounding arithmetic prints `0`, **which reads exactly like "no change."** **Always pass `--` before the pattern**; with `--` occurrences read **96** and LINES read **68**. **`grep -c` counts LINES, NOT OCCURRENCES,** and **`grep -c` on zero matches PRINTS `0` *AND* EXITS 1, so `$(cmd || echo 0)` EMITS TWO ZEROS** — capture with `v=$(cmd || true)`.

⚠️ **THE MALFORMED-ROW FIGURE RECONCILES ONLY UNDER AN ESCAPE-AWARE FIELD COUNT — AND THE ESCAPE-AWARE COMMAND ITSELF CAN BE BROKEN.** Malformed rows are **ROW IDs 57** and **69**, at file lines **119** and **131**. **Strip `\|` BEFORE splitting.** Row 57 reads escape-aware NF=9; row 69 reads 10; rows 74, 94, 95, 96 and **97** read **8**. ⚠️ **The correct form is `sed 's/\\|//g' F | awk -F'|' '/^\| *[0-9]/ && NF!=8'` with NO file argument to awk** — passing the file makes awk IGNORE STDIN and print the NAIVE count (17 lines) under the escape-aware label.

⚠️ **AND DO NOT SUBSTITUTE A "REASONABLE EQUIVALENT" FOR CHECK (1)'s AWK.** `NF>=8 && $2 ~ /^ *[0-9]+ *$/` once printed **92**, not the true denominator. **Use the form above verbatim.**

**SENTINEL RUN MECHANICALLY AT THE PHASE-97 BRAINSTORM CLOSE, BOTH BEFORE AND AFTER THIS STAGE'S `ROADMAP.md` ROW ADD.** ACTUAL output, recorded not predicted:
BEFORE (`5fe3c460`, want=128): (1) **SILENT** · (2) **SIX** at `:206 :212 :218 :228 :234 :242` · (3) SILENT · NC-A **ONE** · NC-B **ONE** · NC-C fired (residual 0) · NC-D **96 / 68** · check-(2) positive control **6 substitutions, residual 0**.
AFTER (want=129): (1) **ONE** line, `NOT DONE: row 97` · (2) **SIX** at `:207 :213 :219 :229 :235 :243` · (3) **SILENT** · NC-A **TWO** · NC-B **TWO** · NC-C fired (residual 0) · NC-D **96 / 68** · check-(2) positive control **6 -> 0 with 6 substitutions asserted** · escape-aware malformed set **exactly {57, 69}**, unchanged, **row 97 did NOT join it**.
Per-line md5 of the six windows, **trailing newline INCLUDED** (`sed -n 'Np' f | md5sum`, first 12 hex) — **ALL SIX BYTE-IDENTICAL before and after, at their OLD and NEW line numbers alike, and back to the phase-95 SPEC close:**
`207 10d7807bf02d` · `213 4a92f7e62fc6` · `219 2a7eb298b9fd` · `229 242e53c6f7a3` · `235 b2680e6f4fbf` · `243 6caa1c3ce0e7`
⇒ **THE SENTINEL DOES NOT FIRE. `stop` WAS EVALUATED AND DELIBERATELY NOT CREATED** (verified absent at the git root and in the stage worktree).
⚠️ **THE MARGIN IS STILL ONE.** ⚠️⚠️ **DELETING THE LAST DEFERRED-CANDIDATE LINE ENDS THE PROJECT. DO NOT "TIDY" ONE, AND DO NOT "FIX" THE SIX.** History `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases; **no gate may rest on a ` + ` split** (wrong on 3 of the 6 windows).
⚠️ **CHECK (2) MATCHES THE WHOLE FILE, NOT ONLY THOSE SIX LINES.** A new row whose summary cell spelled either match phrase would mint a SEVENTH hit and read as a finding. Row 97's cell says *"BANKED, NOT CHARTERED"* instead, on the row-96 precedent. **A SPEC touches no ROADMAP row, so this is dormant at your stage — and live again at the IMPL.**
⚠️ **THE md5 DIGEST IS METHOD-SENSITIVE:** the six match ONLY with the **trailing newline included**. **State the method whenever you quote the digest.**
⚠️ **THIS RUN DOES NOT CARRY FORWARD — run all three checks, all four NCs *and* the check-(2) positive control YOURSELF, at YOUR OWN tip.**
⚠️ **INDEPENDENT RE-RUN AT `a45f1914` ON 2026-09-09 — a SEPARATE session, no worktree, `master` clean and IN SYNC WITH `origin/master`, `ROADMAP.md` UNTOUCHED:** (1) **ONE** line, `NOT DONE: row 97` · (2) **SIX** at `:207 :213 :219 :229 :235 :243` · (3) **SILENT** · NC-A **TWO** (`NOT DONE: row 62` and `NOT DONE: row 97`, substitution inspected first) · NC-B **TWO** (`NOT DONE: row 97` and `GATE FAIL: examined 129 data rows, expected 128`) · NC-C **FIRED** (residual 0) · NC-D **96 / 68** · check-(2) positive control **6 substitutions asserted, residual 0** · escape-aware malformed set **exactly {57, 69}** at file lines **119** and **131** (NF **9** and **10**), row 97 NOT among them and reading **NF=8** under BOTH forms · the six per-line md5s **BYTE-IDENTICAL** to the phase-97 BRAINSTORM close, trailing newline INCLUDED. **⇒ THE SENTINEL DOES NOT FIRE, AND `stop` WAS EVALUATED AND DELIBERATELY NOT CREATED** (verified absent at the git root; `git worktree list` shows the canonical root ALONE). **Every LIVE FIGURE in the block below was ALSO re-derived at this tip and ALL RECONCILE — zero drift on all twenty-plus axes.** ⚠️ **That is a fact about `a45f1914` and expires the moment YOUR stage edits anything. It is not a licence to skip your own run.**
⚠️ **A DANGLING MEMORY CITATION IN THIS FILE WAS FOUND AND CLOSED AT THAT RE-RUN.** This router cited `reference_fix_shape_encodes_unmeasured_parity`, which did not exist; the memory has now been WRITTEN (the lesson had fired on two consecutive rows). **AUDIT THIS FILE'S OWN CITATIONS BEFORE TRUSTING THEM** — extract every `` `reference_*` ``/`` `feedback_*` ``/`` `topic_*` `` slug and test each against `~/.claude/projects/-home-esa-git-envoy-go/memory/<slug>.md`. A router that names a file which does not exist is the same defect class as a stale line anchor.

⚠️⚠️ **A STALE PRESENT-TENSE CLAIM SITS INSIDE SENTINEL WINDOW 228 (now `:229`) AND IS DELIBERATELY UNFIXED.** It says the duplicate-`stat_prefix` panic is a *"swallowed-panic BOOT HANG"* that *"neither crashes nor boots"*. **Row 78 fixed that on 2026-07-27, and the phase-97 BRAINSTORM re-measured it as rc=2 in 0 seconds on BOTH the boot and validate paths.** ⚠️ **A MEASUREMENT AGENT REPORTED THAT CLAIM ABSENT FROM THAT LINE AND WAS WRONG — it searched for the panic MESSAGE while the line spells the CONFIG FIELD.** The text sits at byte offset **42927** of a **47234**-byte line. **When an agent reports an absence, ask what it SPELLED.** Repairing it means editing a line the sentinel matches on a margin of one: **record it, do not tidy it**, and if a row does repair it, preserve that window's candidate clause byte-for-byte and re-run all three checks and all four NCs on BOTH sides of the edit.

⚠️ **NEVER RE-SPELL A SENTINEL MATCH PHRASE INSIDE A SENTINEL WINDOW** (the phase-94 rule: the sentence asserting the sentinel could not move MOVED IT, six -> seven, inside `:226`).

- ⚠️ **LIVE FIGURES RE-DERIVED AT THIS TIP (the phase-97 BRAINSTORM close):** `ROADMAP.md` **247** lines / **129** data rows / tail row **97** (**`in-progress`**) at file line **159** · `DECISIONS.md` **19050** · `^---$` **216** · `^## ADR-` **317** · bare `^## ` **325** · tail **ADR-0318** (**ACCEPTED** — the house guard is DISARMED and **0 is its RESTING STATE**) · next-free **ADR-0319** (`^## ADR-0319` reads **0**) · ADR-0231 decoy **1** at `:14866` · middle-ground `^\*\*Status:\*\*.*PROPOSED` (NEVER a gate) · `BEHAVIOR_CONTRACT.md` **5991** · `STATE.md` **65** (rolled IN PLACE, net 0, numstat **10/10** exactly as all three precedents) · `STATE_HISTORY.md` **564** (562 -> 564, **+2**), strict **163 (DELTA 0, as promised)** / parenthetical **69** / loose **232** (**163 + 69 = 232** exactly) · `BRAINSTORM.md` **396** · phase dirs **138** · fixtures **123** (tail `0121-listener-default-chain-tls`) · fuzzers **56 targets / 48 FILES** · `go.mod` **67 REQUIRE ENTRIES** under a structural `awk` extractor (⚠️ the character-class form reads 62 — method note 33) · `go list ./...` **239** (**237** after excluding the two Docker drivers) · BackendKind tail **38** · `-family row` **96 / 68** · `internal/listener/quic.go` **BYTE-UNTOUCHED**, sha256 verified after the prototype revert.
- ⚠️ **A BRAINSTORM RUNS NONE OF THE SIX GATES, AND A SPEC RUNS NONE EITHER — THAT IS SCOPE, NOT OMISSION.** The gates belong to the IMPL.
- ⚠️ **THE STAT SURFACE IS QUOTED AS A DELTA AND NEVER AS AN ABSOLUTE.** Three different absolutes are live at one tip. **This row anticipates +0 stat NAMES.**

==============================================

## STATUS AS OF 2026-09-09: PHASE 97 (`quic-chain-selection-order`) **BRAINSTORM DONE**. Lifecycle-state **DONE -> 1**; ROADMAP row 97 ADDED `in-progress`. **THE SENTINEL WAS RE-EVALUATED MECHANICALLY BEFORE AND AFTER THE ROW ADD AND DOES NOT FIRE.** `stop` was NOT created. **THIS SESSION RUNS THE PHASE-97 SPEC.**

**What the BRAINSTORM landed, in one paragraph.** The subject is a **chain-selection inversion on the QUIC/HTTP-3 path**. On a listener carrying both a `default_filter_chain` and a universally-eligible empty-match `filter_chains[0]`, envoy-go serves from the **default** chain and leaves the eligible indexed chain at **zero** requests — which is exactly the alternative **ADR-0080 §Alternatives (A)** enumerates and **REJECTS**, and which **§Decision 1** and **§Decision 2** forbid, both empirically pinned to Envoy v1.37.2. **REPRODUCED BY EXECUTION on TWO shapes** (a plaintext default chain, and one carrying its OWN QUIC transport socket — the second being the parity-relevant arm, because phase 96 already measured the reference REJECTING a no-`transport_socket` default chain, so the first alone would merely re-find the banked D2-QUICTS). Both read `http.DEFAULT_CHAIN.downstream_rq_total` **1** and `http.INDEXED_CHAIN.downstream_rq_total` **0**; the plaintext arm **also cross-wires provenance within ONE connection**, TLS coming from `filter_chains[0]` and filters from the default slot. **ROOT CAUSE:** `internal/listener/quic.go`'s two accessors use DIFFERENT predicates and **neither calls the mandated algorithm** — the TCP path selects with `listenerfilter.SelectChain(inputs, rt.chainSpecs, rt.defaultSpec)` over an ORDERED slice, while QUIC reaches around it into `chainByName`, a MAP, behind a comment asserting a one-chain precondition **nothing enforces**. **COST FLOOR: ONE production file, `+7 / -10`, built, run and REVERTED** (revert proven by `sha256sum -c` and an empty `--untracked-files=all` status), flipping both arms to `INDEXED_CHAIN` 1 / `DEFAULT_CHAIN` 0 — explicitly a LOWER BOUND.

### ⚠️ WHAT THE PHASE-97 BRAINSTORM REFUTED — TWELVE CLAIMS, TWO OF THEM BY ITS OWN AGENTS

**Full detail in `BRAINSTORM.md` §0. The load-bearing four:**

1. 🔴 **THE ROUTER'S FRONT-RUNNER IS MATERIALLY LARGER THAN ITS ADJECTIVE.** The HCM `stat_prefix` duplicate-registration panic was named *"the strongest banked candidate"*. Re-derived: **TEN** files register HCM-`stat_prefix`-derived counters through the panic-on-duplicate constructor rather than the `*IfAbsent` seam; `jwtauthn.go` predicted exactly this in its own comment; the repair **must mint a NEW config-reachable boot panic** because `cmd/envoy-go/main_test.go`'s `TestMain_BootPanicIsVisible` names that trigger as **the only one in the tree, with no fallback**, and goes RED after ~31s against a five-line prototype; and the row implies **TWO** fixture directories at a measured **1359** and **1393** added lines each. **It is rejected as NOT-SMALLEST, never as unimportant, and its measurements are banked in `BRAINSTORM.md` §4.1 so the next pick need not re-derive them.**
2. 🔴 **AN AGENT'S ABSENCE CLAIM USED A MATCHER BLIND TO THE WORDING.** It searched `ROADMAP.md` for the panic MESSAGE and reported the stale *"boot hang"* prose absent; the claim is present under the CONFIG-FIELD spelling. **Both greps were correctly executed and the variable was the VOCABULARY, not the tree.** ⚠️ **WHEN AN AGENT REPORTS AN ABSENCE, ASK WHAT IT SPELLED.**
3. 🔴 **D10-QUICSEL's *"needs its own ADR"* IS REFUTED IN HALF.** The **ordering** half needs no new decision — ADR-0080 §Decision 1 and §Decision 2 already answer it, and §Decision 3, which the banked note cites, governs only independent TLS **posture**. The **SNI-dispatch** half is genuinely open and STAYS DEFERRED. ⚠️ **SPLIT A BANKED VERDICT BEFORE PRICING IT** — pricing the pair together priced the cheap half at the expensive half's rate for two rows.
4. ⚠️ **THE WHOLE `internal/listener` PACKAGE IS GREEN BOTH BEFORE AND AFTER A PATCH THAT REVERSES WHICH CHAIN SERVES EVERY QUIC REQUEST.** No existing test asserts the current ordering and none would have caught the repair. **That green is a COVERAGE finding, not a clean bill** — run your fix and your un-fix through the same suite and compare.

Also refuted: the banked port-race figure is **PREDICATE-DEPENDENT** (36 = 31/5/**0** under the banked note's own wildcard command; 37 only under a wider predicate admitting a loopback re-bind, the 37th being `0024-http-oauth2`'s bare `return err`); the banked **D8-FCM** framing is refuted by the **pinned proto's own doc comment**, *"The filter chain match is ignored in this field"*; `ROADMAP.md:136` carries **TWO** `registry.go:107` tokens of which the charset one is WRONG and the duplicate one is **RIGHT**, so a line-scoped substitution would corrupt a correct citation, and a **THIRD** wrong live citation nobody banked sits at `DECISIONS.md:2356`; the banked `manager_test.go:5571` anchor has **already drifted to `:5832`** and phase 96's own correction of a sibling anchor rotted `:4597` -> `:4848`; `0118/driver/driver.go:31`'s falsified band is falsified TWICE over (the range is **10443-10449** once `0103` is counted, and **10450 is held by NOTHING**); the `0120/expectations.yaml` complaint is about stale **SCOPE**, not false ORIGIN; `internal/stats.IsValidName` is a **CHARSET guard only** and no site checks for collision; and an unrelated **`missing admin` boot reject MASKED** the front-runner's panic at rc=1 on a config written to produce rc=2.

### ⚠️ WHAT THE SPEC MUST NOT RE-DERIVE

- **The front-runner's reference measurement** — two listeners sharing an HCM `stat_prefix` are **ACCEPTED** by the pinned reference at rc=0 and **MERGED** into one flat `http.<prefix>.*` scope reading **8** for a 3-plus-5 drive, while a **second** listener-qualified `listener.<addr>.http.<prefix>.*` scope keeps them separate at **3** and **5** — a scope **envoy-go does not emit at all**. ⇒ the parity-correct repair there is **get-or-create**, and a config reject would DIVERGE. It cost a Docker-serialized agent; it lives in `BRAINSTORM.md` §4.1.
- **The subject-side QUIC measurement and the prototype result** — `BRAINSTORM.md` §2.2 and §6.
- **The SDS-by-ALPN intersection is CONFIRMED EMPTY** (6 SDS fixtures against 7 ALPN mentions, `comm -12` empty). ⚠️ A case-insensitive `sds` match also hits `StatsdSink` and inflates the set to 8.

### Banked, NOT chartered

- ⚠️ **THE HCM `stat_prefix` DUPLICATE-REGISTRATION PANIC — still the strongest banked candidate, and now MEASURED ON BOTH SIDES.** See above and `BRAINSTORM.md` §4.1. **It deserves its own row and probably a SPLIT.**
- **SNI-dispatched multi-chain QUIC** — the half of D10-QUICSEL this row does NOT repair. Genuinely needs its own ADR.
- **D2-QUICTS** — the QUIC no-`transport_socket` deferred reject. Reference-confirmed PARITY; fails CLOSED.
- **An SDS by `alpn_protocols` fixture** — intersection CONFIRMED empty; ~1200-1400 added lines; coverage, not a defect.
- ⚠️ **`parseChainSpec`'s `transport_protocol` OVER-strictness — UN-BANKED UNTIL NOW.** It boot-rejects any value outside `{"", "tls", "raw_buffer", "quic"}`, while the proto field is a free string with no constraint. If the reference accepts an unknown value and simply never matches it, envoy-go rejects a config the reference accepts. **UNMEASURED against a live reference.**
- ⚠️ **THE `0066`/`0067`/`0068`/`0071` DEAD-PORT FALSE-GREEN — UN-BANKED UNTIL NOW, and a WORSE failure mode than the port race.** They allocate a port, close it, and rely on nothing listening so a health-check probe FAILS. The port is inside the ephemeral range. If anything grabs it first the probe SUCCEEDS and the fixture asserts the opposite of its proposition — a silent wrong **PASS**, where the port-race roster produces a loud abort.
- **envoy-go emits no `listener.<addr>.http.<prefix>.*` scope at all** — a second, larger, independent gap found while measuring the front-runner.
- **Duplicate listener ADDRESSES** — the reference rejects them in `--mode validate`; envoy-go accepts (rc=0) because validate binds nothing and has no duplicate-address check, failing only at real boot.
- **The driver-owned receiver port race** — **36** files under the banked narrow predicate (wildcard re-bind), **37** only under a wider one that admits a loopback re-bind. ⚠️ **THE 31 / 5 / 0 SPLIT IS PER FILE, NOT PER BIND SITE** — the same 36 files carry **43** bind sites and split **36 / 7 / 0** that way, so **name the unit or the split is meaningless**. The five `fmt.Errorf` files are exactly `0020/0021/0022/0023/0032`. Re-derived first-hand at the phase-97 router refresh; the long-carried *"30 panic / 4 errorf"* is wrong on BOTH figures, missing `0019` and `0020`. ⚠️ **Its trigger has still NOT fired.**
- **The `registry.go` citation set** — `ROADMAP.md:136` col 6095 (wrong), `DECISIONS.md:2356` (wrong, `:100` for `checkName`), and the ADR-0296 §Context one deliberately left uncorrected in place.
- `0120/expectations.yaml`'s stale SCOPE · the drifted `manager_test.go` citation now at `:5832` · `0108`'s three false `ssl.*` confessions · `0118/driver/driver.go:31`'s falsified band · the `GetConfigForClient` history-class comment sites (**a CONTESTED count — quote NO number**) · `0061-lb-ring-hash`'s sigma-margin second occurrence · 1xx interim responses · the H/1 no-`Host` divergence · the pooled-upstream-lifetime defect · the TEN fixed `ssl.*` names and FOUR dynamic families (blocked on NAMING).

## ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + ALL FOUR NCs + THE CHECK-(2) POSITIVE CONTROL YOURSELF** and record ACTUAL output. ⚠️ **NC SHAPES CHANGE ACROSS A ROW CHANGE — NEVER INHERIT ONE.** They changed at phase 95 (the flip), at the phase-96 ADD in the opposite direction, at the phase-96 IMPL flip, and again at the phase-97 BRAINSTORM ADD. **A SPEC ADDS NO ROW AND FLIPS NONE, so they should STAY: `want` **129**, NC-A and NC-B each **TWO** lines. MEASURE IT — a shape that *should* stay is still a measurement.**
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** 95 BRAINSTORM nine · 95 SPEC eleven · 95 PLAN seventeen · 95 IMPL eighteen · 96 BRAINSTORM thirteen · 96 SPEC ten · 96 PLAN fifteen · 96 IMPL six · **97 BRAINSTORM TWELVE** — and the SOURCE matters more than the count: **TWO of the twelve were made by that stage's OWN measurement agents, and one of those was an ABSENCE claim from a matcher blind to the wording.** ⚠️ **EXPECT TO BE REFUTED BY YOUR OWN REVIEW SEAM, NOT ONLY BY YOUR SUCCESSOR.** ⚠️ **AND WHEN TWO AGENTS CONTRADICT EACH OTHER, FIND THE VARIABLE RATHER THAN PICKING A WINNER.**
3. **LINE ANCHORS DRIFT; SYMBOL AND LITERAL-TEXT ANCHORS DO NOT — RE-VERIFY EVERY CITE AT YOUR TIP.** ⚠️ **PATHSPEC-SCOPE every symbol assertion and every `sed`.** ⚠️ **`internal/filter/hcm/chain.go` DOES NOT EXIST — it is `internal/filter/http/chain.go`** — but **`actions.go` IS in `hcm`.** ⚠️ **PROVEN AGAIN AT PHASE 96: `manager.go:1393` -> `:1406` and the D4 block `:746` -> `:759`, both moved by the SAME stage's own edits.**
3a. ⚠️ **AN ANCHOR CAN BE NON-UNIQUE.** **Anchor on the ENCLOSING SYMBOL.**
3b. ⚠️ **CHARACTER-CLASS AND ESCAPE SELECTORS ARE THE FAIL-UNSAFE FAMILY.** `^[0-9]{4}-` on fixtures reads 121, dropping `0007a`/`0007b` (use `ls -d test/fixtures/*/ | wc -l`) · **a CASE-SENSITIVE GREP IS BLIND TO UPPERCASE PROSE — use `-i` when auditing comments** · ⚠️ **AND `[ \t]` INSIDE A GNU ERE BRACKET EXPRESSION IS A LITERAL BACKSLASH AND `t`, NOT A TAB** — that silently made a phase-96 set-reconciliation read **0 packages** on a run that had actually reported 237. **A silent 0 from a reconciliation is indistinguishable from a broken matcher: check the matcher.** See also note 33.
3c. ⚠️ **`feedback_brief_citations_not_evidence` APPLIES TO INCIDENTAL FIGURES — including every number in THIS file and in every AGENT REPORT.**
3d. ⚠️ **NO GATE MAY REST ON A ` + ` SPLIT.**
3e. ⚠️ **A GATE COMMAND CAN MATCH PROSE INSTEAD OF CODE.** Anchor fixture-set extraction on the blank-import LINE:
```sh
extract () { grep -oE '^[[:space:]]*_ "github\.com/pgdad/envoy-go/test/fixtures/[^/]+/(driver|inputs)"$' "$1" \
  | sed -E 's#.*/test/fixtures/##; s#/(driver|inputs)"$##' | sort; }
```
**At this tip: dirs 123 = imports 123 (`test/differential/runner_test.go`), both `comm` directions EMPTY**, split **99 `driver/` + 24 `inputs/`**. **NC the extractor** (rename one import in a SCRATCH COPY — it must fire in BOTH `comm` directions). ⚠️ **A COUNT-ONLY CHECK IS VACUOUS: the import count stays constant under a rename.** ⚠️ **BUT A DELETION FIRES ONLY `comm -23`** — a pure deletion cannot manufacture an import-with-no-directory, so "both directions" belongs to the RENAME control alone (refuted at the phase-96 IMPL).
3f. ⚠️ **"AT THE RETURN" IS NOT A PLACEMENT.** Anchor installs on the ENTRY of the path that must be covered and PROVE the install with an arm that reddens without it.
3g. ⚠️ **A "CHEAP ASSERTION GAP" CAN BE A BEHAVIOUR DIVERGENCE WEARING A DISGUISE.** Measure the reference for THAT ARM before asserting any cross-side stat.
3i. ⚠️ **A LOOSE `--grep 'phase NN.*STAGE'` MATCHES THE WRONG STAGE.** **Anchor stage-commit locates on `^phase NN (slug) STAGE:`.**
3j. ⚠️ **`grep` IS A DIFFERENT PROGRAM IN A SUBAGENT SHELL THAN IN THE CONTROLLER SHELL.** In the CONTROLLER shell `grep` is a **function** resolving to **ugrep 7.8.4** which honours `.gitignore` and is therefore **BLIND to `next-prompt.txt`**; in a SUBAGENT shell `grep` is **`/usr/bin/grep` (GNU 3.11)** and sees it fine. **Both readings are correct; the variable is the SHELL, not the tree.** In the controller shell use `command grep -rl`, `/usr/bin/grep`, `git grep`, or a direct path; ⚠️ **`command grep` DOES NOT SURVIVE `xargs`.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files). **NAME THE SHELL AS WELL AS THE TOOL.**
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Live: TWO SDS dial-budget flakes plus `TestSDSEndToEnd_FetchFailure_BootFailsClosed` · the driver-owned receiver port race · `internal/httpclient` zero-value · the two 84.2-era flakes · the REFERENCE h2spec section-8 flip · `TestOutlierDetector_ConcurrentEjectExactlyOnce` · `0061-lb-ring-hash`'s σ-margin · `TestP83_StopPauseTimer_IsAuthoritativeAgainstAnEnteredClosure` (wasm, 1/400) · `internal/filter/hcm/h2 TestFramer_ReaderGoroutineDoesNotLeak`. **All were searched for in the phase-96 IMPL's own gate logs and read ZERO FAIL occurrences each.** ⚠️ **`TestServerConn_TinyWindowDelivery` IS NOT A FLAKE** — a recurrence is a REGRESSION of row 91. ⚠️ **A GREEN RERUN CLEARS NOTHING; no registration was retired.**
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only). ⚠️ **AT THE PHASE-96 IMPL A SIBLING `curl-world` SESSION STILL OWNED SEVENTEEN CONTAINERS holding `18080-19000`.** ⚠️ **A `reaper_*` testcontainers Ryuk container is created BY THE DIFFERENTIAL ITSELF and is REUSED; leave it alone.**
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`. ⚠️ **`INNER_EXIT` DOES NOT EXIST in this repo.**
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL, THEN DRIVE THE ARM.** **`gofmt -l` never exits non-zero — gate on OUTPUT.** ⚠️ **go test WITHOUT `-v` PRINTS ZERO `=== RUN`** — `RUN=0` beside `RC=0` is a VACUOUS GREEN. ⚠️ **A `-run` selector matching nothing prints `[no tests to run]` and EXITS 0.** ⚠️ **On `-v` output, unanchored `grep -c 'FAIL'` reads nonzero ON A FULLY GREEN TREE** — use `grep -cE '^(FAIL|--- FAIL)|^ *--- FAIL'`.
7b. ⚠️ **`grep -c` ON ZERO MATCHES PRINTS `0` AND EXITS 1** — capture, NEVER chain with `&&`, NEVER `$(… || echo 0)`.
7c. ⚠️ **A GREEN TEST RUN IS NOT EVIDENCE A SITE IS EXERCISED** — prove it with a **`panic()` REACHABILITY CONTROL**. **There is NO `recover()` in non-test `internal/tls` or `internal/listener`, so a panic aborts the binary — gate on `grep -cE '^panic:|DATA RACE|SIGSEGV'`.** **PROVEN LIVE TWICE at the phase-96 IMPL, once before the fix and once after.**
7d. ⚠️ **NC EVERY NEW TEST, AND NC YOUR OWN FIXES** — NEUTRALISE so the package still compiles; check the NC is EXECUTABLE. ⚠️⚠️ **AND CHECK THAT YOUR NC *CAN* FIRE AT ALL.** Phase 96 found TWO roster rows whose specified mutation was structurally incapable of reddening: deleting a SILENTLY-DISPATCHED optional interface method, and dropping a PASSING assertion. **Before running an NC, ask what mechanism would carry its mutation to a failure — if you cannot name one, the control is vacuous.**
7e. ⚠️ **A UNIT TABLE OVER A SHARED ASSERTION IS NOT A GATE ON PRODUCTION BEHAVIOUR** — `t.Helper()` gives each failure a DISTINCT call-site line, but ⚠️ **a shared `t.Helper()` body collapses every property onto ONE line**: discrimination then comes from the MESSAGE, so make each message name its property.
7f. ⚠️ **A NC THAT LEAVES YOUR NEGATIVE CONTROL GREEN IS NOT EVIDENCE THAT CONTROL DOES ANY WORK.**
7g. ⚠️ **ASSERT *WHICH* ERROR FIRED, AND ALSO WHICH DID NOT.** ⚠️ **KEEP TABLE ROWS SINGLE-CAUSE.** ⚠️ **PREFER AN EXACT EQUALITY TO A FLOOR.**
7h. ⚠️ **A PROBE CLIENT CAN WITHHOLD THE VERY THING THE ARM EXISTS TO TEST.** ⚠️ **`openssl s_client` answers "did the handshake complete", NOT "was the connection served"** — drive a real request AFTER the handshake. ⚠️ **A TEST TABLE CAN CARRY AN INVENTED INPUT NO PRODUCTION PATH PRODUCES** — build contexts with the package's OWN helpers.
7i. ⚠️ **A GATE THAT READS 0 HAS NOT BEEN SHOWN TO WORK.** Run every silent gate over an input KNOWN to trip it.
7j. ⚠️ **A PIN CAN FAIL AGAINST *CORRECT* CODE.** **Before writing an expectation map, MEASURE the leaf set a completing handshake actually books.** Phase 96 ran the `{handshake: N, rest: 0}` map deliberately as a control: it FAILED on BOTH sides, turning the warning into evidence.
7k. ⚠️ **ASSERT THE POINTERS, NOT ONLY THE EFFECT.** At phase 96 `sslHandshake == nil` was the CAUSE and the SIGSEGV the EFFECT; the pins asserted both, in that order, and the pointer assertions were observed to fire BEFORE the abort ([[reference_nil_stats_counter_inc_crashes_goroutine]]).
8. ⚠️ **`pgrep -f` / `pkill -f` MATCHES YOUR OWN SHELL AND KILLS THE TOOL CALL (exit 144)**. **Kill ONLY PIDs you captured yourself at launch.** ⚠️ **`pgrep -la a b` ERRORS** — one pattern per call.
9. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-*`, `wt-*` or stage worktree should outlive its stage close** — expect only `master` (verify with `git worktree list`).
10. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** (a) differential — **123/123**; (b) non-Docker sweep gated on `PIPESTATUS[0]` + a SET RECONCILIATION — **237 packages**; (c) h2spec `95 tests, 94 passed, 1 skipped, 0 failed`; (d) fuzzers **56 / 48**; (e) the ANCHORED panic gate, **0** *and PROVEN LIVE*; (f) no REVIEW.md — standing departure. ⚠️ **`-race` on the DIFFERENTIAL suite is VACUOUS** — the subject is an unraced subprocess; run `-race` on the FULL `internal/listener` package instead. **AN IMPL RUNS ALL SIX; A BRAINSTORM, SPEC AND PLAN RUN NONE, AND THAT IS SCOPE, NOT OMISSION.**
11. ⚠️ **THE DIFFERENTIAL'S FAILURE MODE IS A SILENT PASS.** `-count=1` IS NOT OPTIONAL. **ASSERT THE FIXTURE SET BY NAME, BOTH DIRECTIONS.** The full suite takes ~400s (measured **407.357s** at this close).
11b. ⚠️ **`go test ./...` DRIVES DOCKER — AND THERE ARE TWO DOCKER DRIVERS.** Exclude BOTH: `go list ./... | grep -vE '/test/differential$|/test/conformance/h2spec$'`.
12. ⚠️ **`-p` PUBLISHING WORKS; `--network host` IS WHAT DOES NOT.** ⚠️ **`STRICT_DNS` + `host.docker.internal` + `--add-host=host.docker.internal:host-gateway` IS REQUIRED for a container-to-host cluster HERE.** ⚠️ **THE REFERENCE CONTAINER CANNOT READ `filename:` CERT PATHS** — use `inline_string:` — ⚠️ **AND INDENT THE PEM ONE LEVEL DEEPER THAN `inline_string:`**. ⚠️ **A PROBE BACKEND MUST BIND `0.0.0.0`, NOT `127.0.0.1`.** ⚠️ **Keep PEM substitutions OUT of YAML comments.** ⚠️ **envoy-go REJECTS `match.headers`**, **refuses `codec_type: HTTP2` without TLS unless `--allow-h2c`**, **boots with `-c`, NOT `--config-path`**, **`access_log[].log_format` is BOOT-REJECTED**, **`TLSv1_0`/`TLSv1_1` in `tls_params` BOOT-REJECT**. ⚠️ **AN OMITTED `clusters` KEY BOOT-REJECTS envoy-go** — give a probe subject a placeholder STATIC cluster, and `BackendCount()` must be **≥ 1**. ⚠️ **`ENVOY_TARGET.md` is at `docs/envoy-go/ENVOY_TARGET.md`, NOT the repo root.** ⚠️ **The pinned reference is `envoyproxy/envoy:contrib-v1.37.2`, digest `7edd5b0fd763…` — PIN BY DIGEST, verify against `ENVOY_TARGET.md:3-4`** (RE-VERIFIED IN USE at the phase-96 IMPL). ⚠️ **`timeout` rc=124 is shared by a HEALTHY server and a HUNG boot — read the OUTPUT, never the code.** ⚠️ **AND envoy-go's validate mode is `-mode validate`, the reference's is `--mode validate`.**
13. ⚠️ **A RAW-FRAMER PEER MUST HOLD ONE `hpack.Decoder` PER CONNECTION.**
14. ⚠️ **`bash /dev/tcp` + `timeout cat <&3` INSIDE COMMAND SUBSTITUTION RETURNS SILENTLY EMPTY** — use Go for socket probes.
15. ⚠️ **GO DOES NOT FLAG UNUSED CONSTANTS.** Post-removal cleanup is grep-driven.
16. ⚠️ **`golangci-lint`'s misspell runs in locale US.** **Sweep British spellings in `.go` comments BEFORE the gate.** Markdown prose may use them freely.
17. ⚠️ **A GREEN RUN OF A CONTENTION-SENSITIVE TEST PROVES NOTHING.**
18. ⚠️ **AN ABSENCE CLAIM NEEDS A POSITIVE CONTROL AND A DISCRIMINATING NEGATIVE CONTROL.**
19. ⚠️ **DERIVE next-free ADR FROM THE TAIL, NEVER FROM THE HEADING COUNT.** The id space is SPARSE (exactly one gap, `0209`). The correct derivation is `grep -oE '^## ADR-[0-9]+' … | tail -1` ⇒ `## ADR-0318`, next-free **ADR-0319**, confirmed by `grep -c '^## ADR-0319'` ⇒ **0**. ⚠️ **Re-derive at your tip rather than quoting these.**
20. ⚠️ **`reference_measured_prototype_is_a_lower_bound` HAS NOW FIRED FIFTEEN CONSECUTIVE ROWS — AND AT PHASE 96 IT INVERTED A COMPARISON, NOT MERELY A RANGE. ROW 97's `+7 / -10` PROTOTYPE FLOOR IS THE SIXTEENTH CANDIDATE AND IS UNTESTED.** A one-line measured production edit landed **+1285 `.go`** and **+1725** all-in, overturning the split gate's headline ground. **ENUMERATE BY GREPPING THE LITERAL SYMBOL OUTWARD, CASE-INSENSITIVELY, PATHSPEC-SCOPED, AND BY COMPILING — and treat every cost estimate as a FLOOR.**
21. ⚠️ **A MEASUREMENT THAT GOVERNS A DEFERRAL DECISION MUST LAND IN A GOVERNING DOCUMENT.**
22. ⚠️ **A STAGE'S FILE SCOPE IS ITSELF MEASURABLE, AND THE ROUTER CAN BE WRONG ABOUT IT.** `git show --numstat` on the precedent stages settles it, and **MEASURE MORE THAN ONE**. **For a SPEC, measure against the phase-94, -95 and -96 SPEC commits** — the phase-97 BRAINSTORM measured its own five-file scope across `b75b0e2d` `7f568db2` `e99e9cb6` and landed EXACTLY it: `ROADMAP.md` +1/-0, `STATE.md` +10/-10, `STATE_HISTORY.md` +2/-0, the new `BRAINSTORM.md`, and `next-prompt.txt`. **A SPEC's measured scope leaves `ROADMAP.md` and `BEHAVIOR_CONTRACT.md` BYTE-UNTOUCHED.**
23. ⚠️ **`git diff --stat` IS A SUM, NOT ADDITIONS.** Cost figures come from `--numstat`.
24. ⚠️ **AN UNESCAPED `|` IN A ROADMAP ROW PASSES CHECK (1) AND SILENTLY BREAKS THE FIELD COUNT.** Count fields (want **8**) under BOTH forms, BEFORE and AFTER installing. **Reword a pipe AWAY rather than escaping it.** ⚠️⚠️ **THIS FIRED AGAINST THE PHASE-96 IMPL'S OWN AUTHOR**, who wrote a Go `||` into row 96's narrative: NF went 8 -> 10 and the escape-aware malformed set grew `{57,69}` -> `{57,69,96}` **while check (1) stayed SILENT**. **DORMANT at a SPEC, which adds no row — and LIVE again at the IMPL, which flips one.** Row 97's cell was field-counted under BOTH forms before installing, read **NF=8** on both, and did NOT join the malformed set.
25. ⚠️ **THE EVICTION CHECK IS A *PAIR*, AND THE BARE FORMS DO NOT ANSWER IT.** Both bare forms read the same value on `STATE.md` and both are invariant under which entry is evicted. Only the LABEL-BOUND form discriminates, on BOTH files: the evictee's own label goes `STATE.md` **1 -> 0** and `STATE_HISTORY.md` **0 -> 1**. **VERIFIED IN BOTH DIRECTIONS at this close: the evictee reads STATE.md 1 -> 0 and STATE_HISTORY.md 0 -> 1, with a fabricated-label NC reading 0 in BOTH files and a positive control on a sibling label that IS present in the archive (STATE.md 0, STATE_HISTORY.md 1).** ⚠️ **The sibling positive control must name an ARCHIVED label, not a §Recent one** — an archive line's own parenthetical names the EVICTION EVENT, and the evicted entry's label sits INSIDE the line.
26. ⚠️⚠️ **A DIRECT DATE READ CANNOT PICK THE EVICTEE, AND AT THIS CLOSE IT COULD NOT PICK ONE *AT ALL*.** All five pre-roll §Recent entries read `2026-09-07` — a TOTAL tie. The fallback is LIST POSITION, **the tail**, never a guess. Across eight consecutive closes the tie has sat in the MIDDLE, AT THE OLDEST POSITION, ABSENT, AT THE HEAD three-wide, AT THE HEAD four-wide, TOTAL, and now FOUR-WIDE AT THE TAIL. **Inherit no shape.**
27. ⚠️ **A SYMBOL-SCOPED CLAIM IS NOT A PATH-SCOPED ONE.** **Enumerate the call sites and check each caller's own branch.** **PROVEN AGAIN AT PHASE 96**: `Manager.Start` has FOUR `range m.runtimes` loops and only the SECOND one skips QUIC.
28. ⚠️ **A `+0/+0` NEGATIVE-CONTROL ARM CAN BE SILENTLY DELETED WITH EVERY GATE STAYING GREEN.** **Diff the ARM ROSTER, not the counters** (phase 96: `(a)(b)(c)` -> `(a)(b)(c)(d)(e)(f)`, six, none removed).
29. ⚠️ **A MULTI-INSERT LINE SHIFT IS BANDED, NOT A CONSTANT.** **Never hand a scalar shift for a multi-insert file — re-locate every cite by LITERAL TEXT (`grep -nF -- "<exact line>" <path>`).** Line numbers are derived data; the literal is the identity.
30. ⚠️ **A `go test` SELECTOR NAMING A PACKAGE THAT DOES NOT EXIST PRINTS `FAIL … [setup failed]` AND EXITS 1.** **Confirm a selector resolves (`go list ./path/...`) before believing a FAIL.** This is the mirror of the `-run` footgun, which fails the OTHER way (no match ⇒ EXIT 0).
31. ⚠️ **RECONCILE A CORRECTED CLAIM ACROSS ITS WHOLE OCCURRENCE SET.** **After correcting any claim, grep its distinctive phrase tree-wide and fix every hit, or state which hits you are deliberately leaving and why.** ⚠️ **AND SWEEP THE CLAIM, NOT ONE SPELLING OF IT, CASE-INSENSITIVELY** — phase 96's invariant had three wordings and one uppercase spelling across ten live carriers.
32. ⚠️ **A ROW CAN SHIP A FALSE FIGURE IN THE COMMIT THAT WRITES IT, AND A "CORRECTION" IS NOT A MEASUREMENT.** **NAME THE MEASURE (bytes or chars) OR THE FIGURE IS MEANINGLESS**, and **re-derive any count you quote by running the command at YOUR publishing tip, in the SAME commit as the edit that moves it.**
33. ⚠️ **A CHARACTER CLASS SILENTLY DROPS WHAT IT DOES NOT SPELL.** `grep -cE '^\s+[a-z0-9./-]+ v[0-9]'` on `go.mod` reads **62**; the count is **67**. **Prefer a structural extractor (`awk` over the `require` blocks) to a character class, and diff the two when they disagree.**
34. ⚠️ **TWO INDEPENDENT AGREEING MEASUREMENTS DO NOT MAKE A CLAIM GENERAL.** **Enumerate the class, THEN measure.**
35. ⚠️ **A GUARD'S NAME IS NOT ITS COVERAGE — AND A DEAD ASSERTION CAN BE INVERTED AS WELL AS ABSENT, OR SIMPLY WRONG.** **Read the arms, then read what each one would assert if it ran.** Phase 96 INVERTED one and DELETED its mirror, because ADR-0080 §Decision 3 authorised exactly what the mirror forbade.
36. ⚠️ **A VARIABLE REUSED FOR A SECOND PURPOSE OUTLIVES THE INVARIANT THAT MADE IT SAFE.** **When you gate a new feature on an existing boolean, prove that boolean still means what the new use needs, and NAME THE ADR THAT COULD REPEAL IT.** This was phase 96's root cause: `anyTLS` was correct as a mixed-TLS accumulator and phase 74 reused it as the stat-registration gate, inheriting an invariant ADR-0080 had already repealed at phase 07.2 — **twenty-two rows earlier.**
37. ⚠️ **A FIX SHAPE CAN ENCODE A PARITY ANSWER NOBODY HAS MEASURED.** Two repairs that both stop a crash but disagree about the resulting stat surface are NOT interchangeable, **and diff size is not the tiebreaker. Measure the reference, then choose.**
38. ⚠️ **AN OCCURRENCE SET CAN BE NORMATIVE, NOT MERELY COMMENTARY.** **Sweep `docs/` with the same case-insensitive breadth as the code, resolve every markdown hit to its enclosing ADR by BACKWARD heading search, and say which hits you are deliberately leaving and why.**
39. ⚠️ **A BARE-TOKEN GREP'S FALSE POSITIVES ARE CONFIG-DEPENDENT, SO A KNOWN WITNESS IS NOT A GATE.** **Anchor (`^listener\.[^:]*\.ssl\.`) and NC the anchored form against the arm that must read zero.** Measured at phase 96: unanchored `grep -c ssl` reads **4 on the reference** and **1 on the subject** for the SAME config pair — both witnesses appear simultaneously, one per side.
40. ⚠️ **A COUNT WITHOUT ITS MATCHER IS MEANINGLESS, AND TWO CORRECT COUNTS CAN DISAGREE.** The reference's listener-scope `ssl.*` family reads **17** post-drive under the anchored form and **14 AT BOOT** — the three latecomers are the dynamic families. **Name the matcher AND the moment beside every figure.**
41. ⚠️ **AN ILLUSTRATION IN §CONSEQUENCES IS NOT THE LIMIT OF A §DECISION.** ADR-0080 §Decision 3 authorises an independent default-chain TLS posture in BOTH directions; its §Consequences (c) illustrates only ONE, and the shape that crashed was the other. **Read the decision, not the example.**
42. ⚠️ **A SECTION HEADED *"RE-DERIVED AT THIS STAGE'S OWN TIP"* IS FALSE IF IT NAMES A COMMIT THAT IS NOT THE ONE IT SHIPS ON.** **A stage's own edits are part of its tip. Re-measure AFTER writing, in the SAME commit** ([[reference_row_can_ship_false_figure_at_birth]]).
43. ⚠️ **AN ENUMERATION'S EXCLUSION RULE CAN HAVE A HOLE ITS AUTHOR DID NOT SEE.** **State the exclusion as a PREDICATE and then test every live file against it**, rather than listing the directories you happen to be thinking of.
44. ⚠️ **A TEST-STRUCTURE ASSUMPTION IS A CLAIM AND MUST BE READ, NOT PRESUMED.** **Before writing a task against a test, grep it for `[]struct{`, `for _, tc := range` and `t.Helper()`, and say what you found.**
45. ⚠️ **A REGISTRATION-TIME SYMBOL IS NOT A CONSTRUCTION-TIME SYMBOL.** `registerListenerMetrics` runs at `Manager.Start`, from TWO call sites, never at `NewManager`. **Enumerate the call sites and ask WHEN each runs, not only WHERE it is.**
46. ⚠️ **A CROSS-SIDE PIN ON A NAME ONE SIDE NEVER EMITS IS SILENTLY VACUOUS, NOT RED.** **Before pinning a stat cross-side, prove the NAME exists on both sides.** `no_filter_chain_match` has NO production site in this repo and is deliberately UNasserted in `0121`.
47. ⚠️ **A LIFECYCLE STATE DEFINED BY WORK REMAINING IS RE-ENTRANT, AND THE ROUTER MUST SAY SO OR A SHORT SESSION WILL FALSELY CLOSE.** The discriminator is `PROGRESS.md` and the ROADMAP row, **not the session boundary.** ⚠️ **Evaluate the context budget BEFORE starting a task, not after** (`checking-context-budget`). The phase-96 IMPL ran all 19 tasks in one session and did not need the re-entrant path, but it existed and was correct to state.
47a. ⚠️ **A SPEC IS NOT RE-ENTRANT IN THE SAME WAY** — its discriminator is the existence of `SPEC.md`, not work remaining. Do not leave a partial one.
48. ⚠️ **PARALLELISE ACROSS DISJOINT FILE SETS, NOT INSIDE ONE.** The phase-96 IMPL ran THREE worktrees on THREE branches concurrently — unit tests (`internal/listener/`), the fixture (`test/`), and the documents (`docs/`) — with Docker serialized to exactly ONE agent. All three merged with ZERO conflicts. ⚠️ **The load-bearing ORDER (Tasks 1-5 before Task 6) was kept STRICTLY SERIAL inside its own worktree; only genuinely independent work was parallelised.**
49. ⚠️⚠️ **AN AGENT'S ABSENCE CLAIM IS ONLY AS GOOD AS ITS MATCHER'S VOCABULARY.** At the phase-97 BRAINSTORM an agent searched a document for a panic MESSAGE, found nothing, and reported the claim absent; the claim was present under the CONFIG-FIELD spelling, 42927 bytes into a 47234-byte line. **Both greps were correctly executed. The variable was the VOCABULARY, not the tree.** **When an agent reports an absence, ask what it SPELLED before believing what it did not find** — and when two parties disagree, FIND THE VARIABLE rather than picking a winner.
50. ⚠️ **A GREEN PACKAGE UNDER A BEHAVIOUR-INVERTING PATCH IS A COVERAGE FINDING, NOT A PASS.** `go test ./internal/listener/...` is rc=0 both before and after a patch that reverses which filter chain serves every QUIC request. **Run your fix AND your un-fix through the same suite and compare; identical greens mean the suite is BLIND.** This is the constructive twin of the `panic()` reachability control.
51. ⚠️ **AN UNRELATED BOOT REJECT MASKS THE SITE UNDER TEST.** A config written to produce rc=2 from a panic returned rc=1 with `extract admin: bootstrap: missing admin`, because envoy-go rejects a missing `admin` block BEFORE filter construction. **Prove your reproduction REACHED the site — by the message, never by the exit code.**
52. ⚠️ **SPLIT A BANKED VERDICT BEFORE PRICING IT.** D10-QUICSEL was banked as one design question needing one ADR. It is two: an ORDERING half ADR-0080 already settles, and a DISPATCH half that is genuinely open. Pricing the pair together priced the cheap half at the expensive half's rate for two rows.
53. ⚠️ **A COST ADJECTIVE ROTS IN THE BLAST RADIUS, NOT ONLY IN THE COUNT.** The front-runner's five-line prototype is real AND turns `TestMain_BootPanicIsVisible` RED by design, because that guard's sole trigger IS the defect. **Before pricing a repair, grep for the guard whose trigger it removes.**
54. ⚠️ **CHOOSE NON-1xx SENTINEL STATUSES IN PROBES.** A `direct_response` status of **111** came back as **200** with the body intact on the H3 path while **222** came back as **222**. Unexplained and NOT investigated — recorded as an observation, not a claim.

## ⚠️ THE `PROPOSED` GUARD — **DISARMED, AND YOUR STAGE RE-ARMS IT**

| form | now | where |
|---|---|---|
| `^> \*\*STATUS: PROPOSED` — **the house form** | **DISARMED** | ADR-0318 was flipped IN PLACE to `ACCEPTED` at the phase-96 IMPL (Task 15). ⚠️ **A ZERO HERE IS THE RESTING STATE, NOT EVIDENCE THE GUARD IS BROKEN** — PROVEN LIVE at that close on a scratch copy (append a house-form line ⇒ it moves by exactly one). ⚠️⚠️ **A SPEC THAT DRAFTS A NEW ADR §Context RE-ARMS IT — AND THAT IS YOUR STAGE.** Expect this count to move 0 -> 1 under you, and say so in `STATE.md`. |
| `^\*\*Status:\*\* PROPOSED` — **the ADR-0231 decoy** | unchanged, reads 1 | `:14866`, resolved BY BACKWARD HEADING SEARCH to `14864 ## ADR-0231`, byte-untouched (line sha256 verified identical across phase 96) |
| `^\*\*Status:\*\*.*PROPOSED` — **the middle ground** | a large number | ⚠️ **NEVER A GATE** |

⚠️ **VERIFY BY LINE AND BY ADR, NEVER BY THE COUNT ALONE** — resolve any hit to its enclosing ADR by a BACKWARD heading search (`awk 'NR<=<HITLINE> && /^## ADR-/ {h=$0; n=NR} END {print n, h}'`). ⚠️ **NEVER gate on the UNANCHORED form.** ⚠️ **AND DO NOT QUOTE A COUNT FOR EITHER GATE FORM IN PROSE THE GREP MATCHES** — the phase-93 SPEC falsified itself doing exactly that. `DECISIONS.md` at this close: **19050**, `^---$` **216**, `^## ADR-` **317**, bare `^## ` **325**, tail **ADR-0318**, next-free **ADR-0319**. ⚠️ **A SPEC ADDS ONE ADR HEADING AND ONE `^---$` SEPARATOR PER THE ADR-0044 IN-PLACE DISCIPLINE — RE-DERIVE ALL FIVE FIGURES AT YOUR OWN PUBLISHING COMMIT AND SAY WHICH MOVED AND BY HOW MUCH.** The phase-97 BRAINSTORM left all five UNCHANGED because a BRAINSTORM drafts no ADR.

## ⚠️ THE ARCHIVE GUARD

⚠️ **THE STRICT `163` IS NOT THE ENTRY COUNT.** Re-measured at THIS tip (phase-97 BRAINSTORM close, `STATE_HISTORY.md` **564**): loose **232** = strict **163** + parenthetical **69**, exactly — **the triple reconciles only under these ANCHORED-OCCURRENCE forms:**
```sh
H=docs/envoy-go/STATE_HISTORY.md
grep -oE '^- \*\*prior active-phase:\*\* ' $H | wc -l   # strict         163  <- DELTA 0 across a correct close
grep -oE '^- \*\*prior active-phase \('    $H | wc -l   # parenthetical   69  <- +1 per close
grep -oE '^- \*\*prior active-phase'       $H | wc -l   # loose          232  = 163 + 69
```
⚠️ The unanchored `grep -c 'prior active-phase'` counts LINES and coincides by accident; the colon form is a FOURTH regex. **Name the form or the figure is meaningless.** ⚠️ **`grep -F` WITH A PATTERN STARTING `- ` HITS THE FLAG TRAP** — pass `--` first. **The guard is SOUND as a DELTA-0 SHAPE check** (a correctly-shaped parenthetical append moves the STRICT form by 0 — verified again at this close) **but the number is a SUBSET.**

⚠️ **THE ARCHIVE'S POSITIVE CONTROLS ARE SELF-INCREMENTING.** This stage's archive line names NO control figure. ⚠️ **AND THE RAW LINE DELTA IS `+2`, NOT `+1`** — the entry is a blank line PLUS the entry line. **The phase-97 BRAINSTORM close: `STATE_HISTORY.md` 562 -> 564**, evicting the phase-95 PLAN entry by LIST POSITION (the tail), with the strict guard DELTA 0 exactly as promised. ⚠️⚠️ **THE DATE READ NARROWED THE FIELD TO FOUR AND COULD NOT PICK AMONG THEM** — four of the five pre-roll §Recent entries read `2026-09-07` and one read `2026-09-08`, so LIST POSITION picked the tail; position and date AGREED here, which is a coincidence of that tip and not a rule. See method note 26. ⚠️ **THE BARE FORMS ANSWER NOTHING** — both read FIVE on `STATE.md` and both are invariant under which entry is evicted. **Only the LABEL-BOUND PAIR discriminates**, run on BOTH files, with a fabricated-label NC and a positive control on a sibling label that IS present.

## ⚠️ PORT BANDING

`net.ipv4.ip_local_port_range = 32768 60999`. ⇒ **BAND YOUR PROBE PORTS BELOW 32768.** ⚠️ **The differential harness reserves `20000..31007` (`harness_test.go:292-296`) and `11000..14999` (`:209-212`)**, so `15000-19000` is the ad-hoc band — ⚠️ **minus `18080-19000`, held by sibling `curl-world` containers.** **The phase-96 IMPL used `15500-15599` cleanly and the phase-97 BRAINSTORM used `15700-15799` and `15500-15599` cleanly.** **Check availability with `ss -tan` AND `ss -uan` (ALL states), never `ss -ltn`.** ⚠️ **FOR A UNIT TABLE, USE PORT 0.** ⚠️ **`net.Pipe` DEADLOCKS a client-cert handshake — use a loopback TCP pair.** ⚠️ **FIXTURE ports follow the `10<index>` family band, BUT THE BAND IS NOT INDEX-ALIGNED WHERE IT COLLIDES**: `0028` holds `10120`-`10125`, `0120` holds `10126`, and **`0121` holds `10127`**. ⚠️ **THE `10<index>` FAMILY BAND DOES NOT COVER THE QUIC/H3 FAMILY AT ALL — `0104`, the tree's ONLY QUIC fixture, sits at reference port `15104`.** Census the band you are actually joining. ⚠️ **The TLS/SDS fixtures `0108`-`0113` hold `10444`-`10449` and `0103` holds `10443`, so the range is `10443-10449` and `10450` is one PAST it — AND `10450` IS HELD BY NOTHING**, its only occurrence under `test/` being `0118/driver/driver.go:31`'s own warning comment.

## ⚠️ A RECURSIVE `grep` IN THE *CONTROLLER* SHELL IS BLIND TO `next-prompt.txt`

See method note **3j** — this is SHELL-DEPENDENT. In the controller shell `grep` is a **function** (ugrep 7.8.4, honours `.gitignore`); `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. `command grep -rl …` / `/usr/bin/grep` (GNU) sees it; direct-path access works. **In a SUBAGENT shell the bare `grep` is `/usr/bin/grep` and already sees it.** ⚠️ **`command grep` DOES NOT SURVIVE `xargs`.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files). ⚠️ **ugrep ALSO REJECTS SOME COMPLEX `grep -oE` PATTERNS with `exceeds complexity limits` where GNU grep accepts them.**

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 97 (quic-chain-selection-order)' --format='%h %s'   # the SLUG form
git log --grep '^phase 97' --format='%h %s'                                # the LOOSE form — RUN IT to CROSS-CHECK
```
⚠️ **RUN BOTH FORMS AND RECONCILE THE SETS. NO COUNT IS QUOTED HERE, DELIBERATELY — BOTH FORMS ROT, AND A SLUGLESS CORRECTION COMMIT IS INVISIBLE TO THE ANCHORED FORM** (the live witness is `963d472d`). ⚠️ **A `.*SPEC` / `.*PLAN` STYLE LOOSE GREP MATCHES THE WRONG STAGE** (method note 3i). **Take the tip from `git rev-parse master`.** ⚠️ **AND WHEN YOU LAND A CORRECTION, PUT THE FULL SLUG IN ITS SUBJECT.**

Execution style: **subagent-driven per `feedback_execution_style`**. Subagents commit LOCALLY on their own stage branch with EXPLICIT PATHSPECS; the controller merges and squashes (`feedback_subagents_no_push`, `feedback_push_to_origin`). **Pure MEASUREMENT agents should commit NOTHING and prove their tree clean.** ⚠️ **PARALLEL AGENTS IN ONE WORKTREE RACE THE GIT INDEX** — per-agent worktrees on their own branches, private scratch dirs, **Docker serialized to exactly ONE agent at a time** (see method note 48 for how the phase-96 IMPL ran three concurrently with zero conflicts). ⚠️ **VERIFY WHAT A SUBAGENT REPORTS — BUT ALSO BELIEVE IT WHEN IT REFUTES YOU.** At the phase-96 IMPL an agent refuted the controller's own brief and was RIGHT; at the phase-97 BRAINSTORM an agent refuted the controller's own brief and was WRONG, because its matcher was blind to the wording (method note 49). **Verify BOTH directions against the source — an agent's refutation is a claim like any other.** ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root** — build with `-o` into scratch.

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root — **it fired on nearly every call at the phase-97 BRAINSTORM, with an explicit `Shell cwd was reset` line.** Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` before any commit. ⚠️ **Shell VARIABLES do not survive between Bash calls** — re-export in every command. ⚠️ **AN UNQUOTED HEREDOC (`<<EOF`) COMMAND-SUBSTITUTES BACKTICKED TEXT** — use `<<'EOF'`.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE OR PROTOTYPE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — `sha256sum` BEFORE patching, `git checkout -- <path>` after, `sha256sum -c` the capture — ⚠️ **and COMMIT FIRST if anything else is pending, because `git checkout --` restores from HEAD.** ⚠️ **Also check the MAIN repo root, and `--untracked-files=all`.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads RUN THE PHASE-97 SPEC.** ⚠️ **Its §Recent is FIVE entries under the ADR-0288 cap.**
2. `docs/envoy-go/phases/97-quic-chain-selection-order/BRAINSTORM.md` — **396 lines. THE STAGE YOU ARE DISCHARGING.** §10 is your owed list (nine items); §2 is the measurement; §3 is the hazard set; §4 is the adjudicated rejected roster; §6 is the cost floor; §8 is the sentinel run.
3. `BOOTSTRAP_PROMPT.md` **at the repo root** — §5 the lifecycle state machine (**state 1 ⇒ create the SPEC**) and §6.1's split trigger. ⚠️ **§5 appears VERBATIM TWICE**, §5 and the §11 appendix; **the second copy's offset is NON-constant; find it by heading, never by arithmetic.**
4. `docs/envoy-go/DECISIONS.md` **ADR-0080** — read §Decision 1, §Decision 2, §Alternatives (A) and §Consequences (a) **in full**, by BACKWARD heading search from the hit, not by line number. ⚠️ **§Decision 3 is the WRONG section for this row's question** — it governs independent TLS POSTURE only, and the banked note cites it wrongly. ⚠️ **DO NOT "FIX" D4-MIXEDTLS**; §Decision 3 authorises it verbatim.
5. `docs/envoy-go/ROADMAP.md` — the six candidate lines behind check (2), and the family sections. ⚠️ **A SPEC leaves this file BYTE-UNTOUCHED**, a scope measured across four consecutive SPEC commits. **Read it; do not edit it.**
6. `internal/listener/quic.go` and `internal/listener/manager.go` — the two accessors, their three call sites, `rt.chainSpecs`/`rt.defaultSpec`/`rt.chainByName`, and the TCP path's `listenerfilter.SelectChain` call. **Anchor by SYMBOL. Every line number in this file and in the BRAINSTORM is derived data.**

## What the next session owes
**RUN THE PHASE-97 SPEC (lifecycle 1 -> 2)** — ⚠️ **ONE STAGE PER SESSION** · **DISCHARGE ALL NINE OWED ITEMS of `BRAINSTORM.md` §10 and say which you discharged and how** · ⚠️⚠️ **ITEM 1 IS THE BLOCKING UNKNOWN AND IT IS A DOCKER MEASUREMENT: does the pinned reference `envoyproxy/envoy:contrib-v1.37.2`, digest `7edd5b0fd763…` verified against `ENVOY_TARGET.md:3-4`, ACCEPT a QUIC listener carrying BOTH a `default_filter_chain` and an eligible `filter_chains[]` entry, and WHICH chain serves?** Run BOTH probe shapes with a plaintext negative control, and **measure before choosing a repair shape** — `reference_fix_shape_encodes_unmeasured_parity` · ⚠️ **ARMS ARE OWED ON BOTH SIDES OF THE ELIGIBILITY CONDITIONAL**: an eligible indexed chain must WIN and an INELIGIBLE one must leave the default chain winning; a guard that tests one side of a branch is the vacuous-guard family · ⚠️ **SPECIFY THE SELECTION MOMENT** — `startQUIC` needs a TLS config BEFORE any connection exists, so name what is selected, from what, and when, for ALL THREE call sites · **DECIDE the fate of the two accessors' *"exactly one chain"* comments** — enforce the precondition at boot or delete the claim; leaving it hands the next author a false invariant, which is exactly phase 96's root cause · **STATE whether a fixture is chartered**, and if so census its port band rather than inheriting one (the QUIC family sits at `15<index>`, NOT `10<index>`) and name all THREE registration gates including the blank import that is SILENTLY GREEN if missed · **DRAFT ADR-0319 §Context ONLY** per ADR-0044 (§Decision and §Consequences land at the IMPL) — ⚠️ **this RE-ARMS the house `PROPOSED` guard, which currently reads 0 as its resting state; expect 0 -> 1 and say so** · ⚠️ **`ROADMAP.md` and `BEHAVIOR_CONTRACT.md` stay BYTE-UNTOUCHED — MEASURE that scope across the phase-94, -95 and -96 SPEC commits rather than inferring it from wording** · ⚠️ **RE-RUN the sentinel + all four NCs + the check-(2) positive control YOURSELF** — a SPEC adds no row, so the shapes SHOULD stay (`want` **129**, NC-A **TWO**, NC-B **TWO**, check (2) **SIX**), and *should stay* is still a measurement · ⚠️⚠️ **THE MARGIN IS ONE: only check (2)'s SIX stands between this project and `stop`. DO NOT "TIDY" A CANDIDATE LINE**, and do not repair the stale claim inside window `:229` · `STATE.md` rolled IN PLACE with the oldest §Recent entry evicted — ⚠️ **use LIST POSITION and the LABEL-BOUND PAIR across BOTH files, with a fabricated-label NC and a sibling positive control naming an ARCHIVED label** — and archived to `STATE_HISTORY.md` as **ONE INLINE LINE** in the PARENTHETICAL form, **strict guard 163 DELTA 0**, raw delta **+2**, ⚠️ **name NO positive-control figure in the archive line** · **ROLL THE §Recent PREAMBLE SENTENCE TOO, without spelling the evictee's label** · ⚠️ **RE-DERIVE ANY LINE COUNT YOU QUOTE IN THE SAME COMMIT AS THE EDIT THAT MOVES IT** — the phase-97 BRAINSTORM's own `ROADMAP.md` citation read 377 until §8.6 was appended and had to be re-synced to **396** before committing · `next-prompt.txt` rolled (`git add -f`) · ONE squashed commit, merged and pushed · **REFUTE YOUR PREDECESSOR BY EXECUTION** and record it — ⚠️ **the BRAINSTORM refuted TWELVE, TWO of them by its own agents, so re-derive every figure you quote from any document in this repo, including this one.**
