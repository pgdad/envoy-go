START a fresh session for envoy-go. **The phase-43.1 (connection-pool budgets + pending wait-queue) IMPL is COMPLETE, six-gate GREEN, all 80 fixtures GREEN, ADR-0252 landed, and ROW 43 (`connection-pooling`) is `in-progress` with LEG 43.1 DONE.** 43.1 is the FIRST by-concern leg of the FIFTH-and-FINAL Upstream-robustness-family row (43.1 = the budget substrate + pending wait-queue — DONE; 43.2 = the H2 multiplex pool, which SUPERSEDES ADR-0056 — the remaining leg). The next step is the **43.2 (H2 multiplex pool) BRAINSTORM** (`superpowers:brainstorming`). Confirm the latest master is the phase-43.1 IMPL completion commit via `git log --oneline -3` (a NEW sha — do NOT assume a literal value). Counts at tip (CONFIRMED at the 43.1 IMPL): stat surface **1183** / fixtures **80** (tail `0078-connection-pool-max-connections`) / fuzzers **42** / BackendKind tail **36** (`BlockingHoldResponder`) / DECISIONS tail **ADR-0252**, next-free **ADR-0253**.

## NEXT STEP — the 43.2 (H2 multiplex pool) BRAINSTORM

Execute `superpowers:brainstorming` for the **43.2 H2 multiplex pool** leg. This leg SUPERSEDES ADR-0056 (the deferred H2 connection-pool ADR from the project's H2 phase). The brainstorm must charter:

1. **The per-endpoint H2 `ClientConn` pool** — lifts the `router_h2.go` `defer cc.Close()` per-request-fresh discipline; the `h2.ClientConn` is ALREADY multiplex-capable (monotonic `nextStreamID` + concurrent `RoundTrip` + reads `serverS.MaxConcurrentStreams` + `goawayCh`) so 43.2 adds POOL-MANAGEMENT + stream-enforcement/GOAWAY-rotation, NOT a codec rewrite.
2. **`max_concurrent_streams` enforcement** — `min(local, peer)` where local comes from `circuit_breakers.max_requests` (already parsed, already emit-0 at 43.1 baseline) and peer from the server's H2 SETTINGS frame (the `h2.ClientConn` already reads it).
3. **GOAWAY-driven conn rotation** — when a GOAWAY arrives on an open `ClientConn`, drain it + open a replacement.
4. **ADR-0053/ADR-0056 supersession** — the 43.2 ADR (ADR-0253) explicitly SUPERSEDES ADR-0056 (the deferred H2 pool charter).

The brainstorm settles: (Q-subject) confirm 43.2 IS the correct next subject from the `reference_roadmap_split_phase_row_done` constraint (leg 43.2 is the remaining leg; row 43 flips `done` only when BOTH 43.1+43.2 land); (Q-ambition) scope of the H2 pool — per-endpoint vs per-cluster vs per-host-per-port; (Q-stream-budget) where `max_concurrent_streams` comes from; (Q-goaway) the GOAWAY-rotation discipline; (Q-split) a SPEC-split check (the ADR-0045 gate — `~300-450 prod LoC / ~12 tasks` target; 43.2 may be a single flat sub-phase or may need a further split); (Q-stats) new stat shapes vs existing (the 43.1 surface is already 1183; 43.2 may add H2-pool-specific stats like `upstream_cx_http2_total`/`http2_active` or reuse existing `upstream_cx_total`/`upstream_cx_active`).

## Architecture context (as landed at 43.1)

**The 43.1 substrate** (available to 43.2): `connPool` (`internal/cluster/connpool.go`) with `max_connections` hard-cap + `max_pending_requests` bounded wait-queue; the `acquireConnOrPend`/`releaseConn`/`removeWaiterLocked` permit mechanism (the `sync.Mutex` + FIFO buffered-channel wake). The 43.1 permit applies at the H2 `Dial` site already (the `DialH2` overflow seam) — 43.2 builds the H2 `ClientConn` pool ON TOP of this permit layer. **Current H2 flow** (per-request-fresh, pre-43.2): `router_h2.go` calls `cluster.DialH2(ctx)` which dials a fresh `h2.ClientConn` (under the 43.1 permit), does the request via `cc.RoundTrip(req)`, then `defer cc.Close()` closes it. 43.2 replaces the per-request-fresh discipline with a pool.

## Honor throughout (the discipline)

- **Lifecycle = one stage per session.** `feedback_git_worktrees` (a fresh worktree off master for the SPEC/PLAN/IMPL stages) · `feedback_subagents_no_push` (subagents commit LOCALLY; the controller squashes + pushes at stage-close) · `feedback_push_to_origin` (push after a clean squash on master with tests green) · `feedback_execution_style` (subagent-driven execution for IMPL) · `feedback_pertask_gofmt_lint` (each task runs gofmt -l + golangci-lint on touched pkgs).
- **Process anchors:** ADR-0106 + `reference_roadmap_split_phase_row_done` (row 43 flips `done` only when BOTH 43.1+43.2 land — 43.1 is done, 43.2 is the remaining leg; the rows-36/39/42 split-phase precedent); ADR-0080 (strict-reject); `reference_docker_probe_bridge_network` (live `contrib-v1.37.2` probes for the SPEC D-CP-* pins); `reference_concurrency_differential_release_barrier` + `reference_concurrent_attempt_downstream_class_assertion` (the concurrency-differential discipline); `reference_max_connections_soft_breaker` (the 43.1 load-bearing finding — 43.2 inherits the hard-cap `max_connections` discipline; `max_concurrent_streams` is a DIFFERENT budget that 43.2 owns).
- **The 43.1 departure (AMEND-CP1 / ADR-0252):** the reference's `max_connections` is a SOFT breaker; envoy-go's hard-cap is a documented departure. 43.2's `max_concurrent_streams` is NOT similarly soft — it is enforced cleanly by the `h2.ClientConn`'s `RoundTrip` flow-control mechanism, so the 43.2 differential may be cross-side-EXACT on stream counts.

## Read first (in order)

1. THIS FILE — the cold-start router (43.1 IMPL is DONE + LANDED; row 43 is `in-progress` with leg 43.1 done; the next step is the 43.2 BRAINSTORM).
2. `docs/envoy-go/STATE.md` — active-phase `phase 43.1 (connection-pooling) IMPL done`; the count figures (1183 / 80 / 42 / 36 / ADR-0252, next-free ADR-0253).
3. `docs/envoy-go/phases/43-connection-pooling/README.md` — the 43.1 IMPL summary (what was built, the AMEND-CP1 departure, the `connPool` architecture, the stat surface 1181→1183).
4. `docs/envoy-go/phases/43-connection-pooling/SPEC.md` — the 43.1 SPEC (the §1.1 AMEND-CP1..7; the §13 ADR-0252 §Context draft; the §8 `0078` differential).
5. `docs/envoy-go/DECISIONS.md` — ADR-0056 (the H2 pool ADR that 43.2 SUPERSEDES; read its §Context + §Decision), ADR-0248 (the phase-41 circuit-breaker struct that 43.1 extends and 43.2 builds on), ADR-0252 (the 43.1 architecture — the hard-cap + bounded-queue departure); next-free ADR-0253.
6. `internal/cluster/dial_h2.go` + `internal/filter/http/router/router_h2.go` — the current H2 dial + per-request-fresh flow that 43.2 will replace with a pool.
7. `internal/cluster/connpool.go` — the 43.1 permit substrate (the `acquireConnOrPend`/`releaseConn` mechanism; 43.2 builds ON TOP of this at the pool level).
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `reference_max_connections_soft_breaker` [the 43.1 departure — understand it before designing 43.2's stream-budget model], `reference_roadmap_split_phase_row_done` [row 43 flips done only when BOTH legs land], `reference_concurrency_differential_release_barrier`, `reference_concurrent_attempt_downstream_class_assertion`, `reference_docker_probe_bridge_network`, `feedback_git_worktrees`, `feedback_subagents_no_push`, `feedback_push_to_origin`, `feedback_execution_style`, `feedback_pertask_gofmt_lint`).
