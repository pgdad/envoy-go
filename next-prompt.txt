=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, the active phase is fully landed, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action. (The sentinel does NOT apply right now — phase 48 is chartered and its BRAINSTORM has not yet been performed.)

**STATUS AS OF 2026-06-30: THE LOOP IS RE-OPENED — phase 48 (`stats-sink-statsd`) CHARTERED; the NEXT stage is its BRAINSTORM.**
- All PRIOR chartered ROADMAP rows (1–47) are `done` and LANDED: HEAD is `c67ba03f` (master == origin/master; the untracked `.claude/` is session cruft, leave it). **Re-verify with `git log --oneline -6` + `git status`; do NOT assume.**
- The Observability family STAYS OPEN. A human (re)opened the loop and chartered the next deferred Observability candidate — **`statsd` stats sink** — as **phase 48 (`stats-sink-statsd`)**, the FIFTH Observability-family row. It is the cheapest follow-on to phase-47 `metrics_service`: it REUSES the phase-47 `stats.Registry`-walking flush subsystem + the bootstrap `stats_sinks[]` consumer slot + the `internal/statssink` package, swapping the gRPC `MetricsService.StreamMetrics` submit for a statsd line-protocol emitter.
- Counts at charter (re-verify, do NOT assume): stat surface **1200** (non-H2 **1196**) / fixtures **93** / fuzzers **50** / BackendKind **38** / DECISIONS **ADR-0264** (next-free **ADR-0265**).

=> The NEXT action is the **phase 48 (`stats-sink-statsd`) BRAINSTORM** (lifecycle-state 0 → 1). NOT the termination sentinel.

==============================================

## The NEXT action — the phase 48 (`stats-sink-statsd`) BRAINSTORM
Open phase 48 with `superpowers:brainstorming` (a BRAINSTORM is the next lifecycle stage; lifecycle-state 0 → 1; docs-only direct-on-master per the phase-44/45/46/47 family-row BRAINSTORM precedent). The pick is ALREADY made (statsd — a human decision via the re-open); the BRAINSTORM settles the SCOPE, not the "which row" question.

**Subject:** the bootstrap `stats_sinks[]` `envoy.config.metrics.v3.StatsdSink` (the SECOND `stats_sinks[]` consumer after phase-47 `metrics_service`; `stats_sinks[]` was silently ignored before ADR-0064, then first consumed at phase 47) — a periodic statsd line-protocol stats sink driven by the SAME phase-47 `stats.Registry`-walking flush subsystem (the periodic flusher + the registry snapshot + the `internal/statssink` package), emitting statsd lines (`<prefix>.<name>:<value>|c` counters / `|g` gauges; timers `|ms` likely out of MVP scope — envoy-go lacks histograms, the phase-47 `reference_stats_sink_metrics_service_probe` boundary) over a UDP `address` and/or a `tcp_cluster_name` transport.

**Open questions for the BRAINSTORM to settle (Q0-style, confirm WITH the user):**
- Q1 — transport scope: UDP `address` only, `tcp_cluster_name` only, or both? (UDP is the canonical statsd default; TCP routes over a named cluster — reuses the cluster/dispatch seam.)
- Q2 — name shaping: which name projection does the reference statsd sink emit? PROBE it live (`reference_docker_probe_bridge_network` + `reference_stats_sink_metrics_service_probe`) — is it the dotted internal name, the tag-expanded name, or a flatten? Does `prefix` prepend? This is the load-bearing differential surface.
- Q3 — config knobs: `prefix` (default `envoy`), and the strict-reject posture for the rest (ADR-0080) — which `StatsdSink` fields are accept-and-consume vs strict-reject? (Mirror the phase-47 `MetricsServiceConfig` reject discipline.)
- Q4 — `dog_statsd` / `graphite`: explicitly OUT of phase 48 (each its own future deferred row); statsd only.
- Q5 — split? Likely a SINGLE FLAT ROW (the flush substrate already exists — no new seam; the phase-47.1-core precedent) with a pre-authorized escape-valve per ADR-0045; confirm at the SPEC.
- Q6 — proof-shape: a cross-side differential — a driver-owned UDP (and/or TCP) statsd-line RECEIVER (NOT a BackendKind, per `reference_differential_grpc_receiver_driver_owned`; the phase-47 two-per-side-receivers + hard-Close pattern, `reference_periodic_sink_differential_two_receivers`) asserting the emitted statsd-line subset; the periodic-flush + delta-vs-absolute concerns from phase-47 (`reference_delta_sink_differential_stability_barrier`) likely DON'T apply (statsd counters are typically absolute-per-flush — PROBE to confirm).

**Anticipated (the BRAINSTORM does NOT freeze these — the SPEC/IMPL do):** a NEW `StatsdSink` parse arm in `internal/bootstrap` + a NEW statsd emitter in `internal/statssink` (likely a sibling to the phase-47 `MetricsServiceSink`); +N statsd stats (probe); +1 fixture; possibly +1 fuzzer (a statsd-config parse fuzzer); DECISIONS tail → ADR-0265; possibly +1 go.mod module IF a statsd client lib is pulled (PREFER hand-rolled — the line protocol is trivial). Row 48 registers `in-progress` AT the BRAINSTORM commit (per the ROADMAP invariant — rows are created when a phase enters `in-progress`, NOT pre-populated) with the Observability-family `Next →` pointer; it flips `done` at the phase-48 IMPL six-gate (NO parent rollup per ADR-0106; if split, per-leg).

After the BRAINSTORM lands, ROLL THIS FILE FORWARD to point at the phase-48 SPEC (mirror the structure of any prior phase's post-BRAINSTORM SPEC router in git history — e.g. the 47.x SPEC routers).

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** The next stage is the phase-48 BRAINSTORM (`superpowers:brainstorming`). Lifecycle-state 0 → 1.
- **Subagent-driven execution** (`feedback_execution_style`) in a **fresh worktree** (`feedback_git_worktrees`) for any IMPL; subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit, re-runs the full suite on the frozen HEAD, does the deliberate-break verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). (BRAINSTORM/SPEC/PLAN are docs-only direct-on-master per the family-row precedent — no worktree needed.) Per-task gates: `gofmt -l` + `golangci-lint` on touched pkgs + `go vet` + `go build ./...` (`feedback_pertask_gofmt_lint`).
- **Process anchors:** ADR-0106 (per-leg rows) · ADR-0044 (ADR §Context-at-SPEC / §Decision+§Consequences-at-IMPL) · ADR-0045 (sub-split soft gate) · ADR-0080 (strict-reject) · ADR-0064 (`stats_sinks[]` silent-ignore, now being consumed) · next-free **ADR-0265**.
- **Differential discipline** (carries to any new fixture): `-run 'TestDifferential/<NNNN>'` NEVER bare (`reference_differential_run_selector`); `-count=1` on every break + `-race` run (`reference_differential_break_protocol_count1`); the full-suite `subject ready: EOF` startup flake is isolatable (`reference_differential_fullsuite_startup_flake`); the Docker bridge + decode-ran proof (`reference_docker_probe_bridge_network`); driver-owned receivers are NOT BackendKinds (`reference_differential_grpc_receiver_driver_owned`); the live reference is the wire truth (`reference_wire_format_both_sides_see_same_bytes`); periodic-sink two-receivers + hard-Close (`reference_periodic_sink_differential_two_receivers`); the metrics_service probe boundary (`reference_stats_sink_metrics_service_probe`).

## Read first (in order)
1. THIS FILE — the cold-start router (the loop is RE-OPENED; phase 48 `stats-sink-statsd` CHARTERED; the next action is its BRAINSTORM).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 47.2b IMPL done`) + the recorded NEXT (`phase 48 (stats-sink-statsd) BRAINSTORM`) + the counts (1200 / 93 / 50 / 38 / ADR-0264).
3. `docs/envoy-go/ROADMAP.md` — rows 1–47 all `done` (max row 47); the Observability family-progress note (the family STAYS OPEN; statsd now chartered as phase 48 with the `Next →` pointer; the remaining deferred candidates).
4. `docs/envoy-go/DECISIONS.md` — **ADR-0264** (the 47.2b tags-as-labels anchor) + next-free **ADR-0265** (the phase-48 statsd anchor-to-be).
5. `docs/envoy-go/phases/47-stats-sink-metrics-service/` (PROGRESS-47.2b.md + the SPEC/PLAN docs) — the phase-47 stats-sink substrate the statsd sink REUSES (the periodic flusher + the `stats.Registry` walk + the `internal/statssink` package + the bootstrap `stats_sinks[]` consumer); the cheapest-follow-on framing.
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_subagent_autocommit_claudemd`, `feedback_pertask_gofmt_lint`, `reference_roadmap_split_phase_row_done`, `reference_stats_sink_metrics_service_probe`, `reference_periodic_sink_differential_two_receivers`, `reference_delta_sink_differential_stability_barrier`, plus the differential-discipline references above).
