=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-VERIFIED 2026-07-16 (re-run MECHANICALLY against master `da766140` — UNCHANGED from the 2026-07-15 phase-65-SPEC-close check; all three checks still print) — it does NOT fire. Do NOT create `stop`.** State:
- (1) prints `NOT DONE: row 65` — row 65 `xds-sds-validation-context` STAYS `in-progress` (it flips `done` only at its IMPL six-gate, ADR-0106). Check (1) BLOCKS `stop`.
- (2) prints THREE live "candidates:" sentences — HTTP/3, xDS, and Observability ⇒ three families STAY OPEN. The xDS sentence is UNCHANGED by this SPEC (`SDS validation_context/upstream SDS` still listed; it narrows to `upstream SDS` only at the phase-65 IMPL per the observed convention).
- (3) prints `NEVER OPENED: gRPC`, `Runtime`, `WASM` ⇒ THREE families never opened. Operational-tooling also STAYS OPEN.

Checks (1), (2) and (3) print ⇒ the sentinel does NOT fire. Row 65 is mid-lifecycle (BRAINSTORM + SPEC done, PLAN/IMPL to go) AND there is banked deferred work AND never-opened families — abundant work remains.

**STATUS AS OF 2026-07-15: the phase-65 SPEC LANDED and ROADMAP row 65 is `in-progress`.** The SPEC (`docs/envoy-go/phases/65-xds-sds-validation-context/SPEC.md`, the 14-section shape) DISPOSED every D-SDSVC-* question via LIVE probes against `envoyproxy/envoy:contrib-v1.37.2` — **EVERY anticipation HELD (no ADR-0044 flip).** Key probe outcomes: D-SDSVC-REFSERVE HELD (a CA-signed client cert → 200; a wrong/un-served-CA cert → `ssl.fail_verify_error`; no cert → `ssl.fail_verify_no_cert` — three DISTINCT outcomes prove verification is LIVE); D-SDSVC-RESOURCE pinned via the reference's own `config_dump` (`dynamic_active_secrets` → `Secret{validation_context: CertificateValidationContext{trusted_ca: {inline_bytes}}}`); D-SDSVC-REQUIRE-SCOPE = mandatory mTLS on `require_client_certificate: true` (`false` defers, INERT). **Design decisions PINNED:** D-SDSVC-PROVIDER = **Option A (parallel chain, LEAN)** — `parseValidationSecret`/`applyValidationResponse`/`fetchValidationSecret`/`FetchInitialValidationContext(...) (*x509.CertPool, error)`, leaving the landed `*stdtls.Certificate` chain UNTOUCHED (all four names GREP-collision-free); D-SDSVC-APPLY-POINT = **fetch + install in `NewDownstreamConfig`'s `require_client_certificate` block** (gated on `require==true`), `commonTLSContextToConfig` no-ops the reject for the downstream+provider path only (upstream/QUIC keep the byte-identical reject) — a REFINEMENT over BRAINSTORM §2.6; D-SDSVC-BOOT-SCAN (the load-bearing re-read finding) = **extend `boot.NewSDSProvider`'s pre-scan** (`boot.go:138`, today `tls_certificate_sds` only) to also detect `validation_context_sds_secret_config` (compose-two-secrets DEFERRED via the `seen>1` guard). Fixture: ONE new `0108-xds-sds-validation-context` (clone `0103`×`0018`; the driver presents a client cert via a stdlib `tls.Config{Certificates}` — NO runner API; `helpers.TLSServedLeaf` can't). +0 stats (reuse `sds.*`) / +0 fuzzers (SEEDS, count stays 55) / +0 packages / +0 modules; SINGLE FLAT ROW (~11 tasks; ADR-0045 escape-valve armable-unconsumed); the 60.2 cycle guard STANDS. **ADR-0286 §Context DRAFTED** (SPEC §13; §Decision/§Consequences at the IMPL). STATE flipped; router rolled to the phase-65 PLAN.

**Counts at phase-65 SPEC-DONE (VERIFIED on the landed tree; RE-VERIFY at session start, `git fetch` first):** fixtures **109** (`ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0107-tracing-max-path-tag-length`) · fuzzers **55** · DECISIONS tail **ADR-0285** (next-free **ADR-0286**) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** · go.mod modules **2** (`quic-go` direct + `qpack` indirect). Counts are UNCHANGED at a SPEC (docs-only).

==============================================

## What THIS session does (the phase-65 PLAN)

Perform the PLAN stage (lifecycle-state 2 → 3) for phase 65 (`xds-sds-validation-context`). This is a DOCS-ONLY stage — NO production `.go` changes (row 65 STAYS `in-progress`; the IMPL flips it `done`). Run in a FRESH worktree off master, branch `phase-65-xds-sds-validation-context-plan`, per `feedback_git_worktrees`.

**The PLAN deliverables:** `docs/envoy-go/phases/65-xds-sds-validation-context/{PLAN,PROGRESS}.md` — a TDD task spine decomposing SPEC §10's ~11-task sequence into ordered, independently-buildable, test-first tasks (the SPEC-64 PLAN shape). RE-DERIVE every SPEC §12 edit-site `file:line` against the master tip (`feedback_brief_citations_not_evidence` — a citation is not evidence; RE-READ `internal/xds/{secret,stream,provider,stats,config}.go`, `internal/tls/config.go`, `internal/boot/boot.go`, the `sdsserver` helper, and the `0103`/`0018` fixtures in full). Row 65 STAYS `in-progress`.

**The SPEC §10 task spine to decompose (RE-DERIVE boundaries; the SPEC pins the design):**
- T1 `internal/xds` applier — `parseValidationSecret` (`secret.go`) + the CVC-reject roster (SPEC §6) + unit tests (`Errorf` per property, `reference_fatalf_makes_assertions_unreachable`).
- T2 `internal/xds` stream arm — `fetchValidationSecret` + `applyValidationResponse` (`stream.go`, parallel to `:38-95`) + unit tests.
- T3 `internal/xds` provider — `FetchInitialValidationContext` (`provider.go:47-75` parallel) + the `SecretProvider` interface method (`:14-16`) + unit tests; ALL test fakes (in `internal/tls`) gain the method.
- T4 `internal/tls` reject-lift — the `commonTLSContextToConfig` no-op for downstream+provider (SPEC §3.3a); config test: downstream+provider no longer rejects, upstream+QUIC STILL reject (byte-identical substring).
- T5 `internal/tls` apply-point — `NewDownstreamConfig` fetch+install (SPEC §3.3b) + flip `config_test.go:936-957` reject→ACCEPT (fake provider → `ClientCAs != nil` + `RequireAndVerifyClientCert`); keep the inline `require_client_certificate` tests (`:286-349`) + a `require==false`-inert test.
- T6 `internal/boot` pre-scan — the `validation_context_sds_secret_config` detection (SPEC §3.4) + a boot test (validation-via-SDS builds a provider; both-via-SDS hits `seen>1`; `0103` unchanged).
- T7 `sdsserver` — `WithValidationContext` + the `buildResponse` branch (`:118-136`) + a helper unit test.
- T8 fixture `0108` — driver + `envoy.yaml`/`envoy-go.yaml` + `pki/` (CA + server leaf + a CA-signed client cert + an un-served client cert) + expectations/README; positive cross-side arm + a wrong-CA negative sub-assertion (`ssl.fail_verify_error` `StatsAsserter`) + the client-cert dial; break-prove live (`-count=1`, `-run 'TestDifferential/0108-xds-sds-validation-context'`, confirm WHICH assertion fires).
- T9 fuzz SEEDS — `FuzzDiscoveryResponseParse` (`xds/fuzz_test.go:71`) + `FuzzTLSContextParse` (`tls/fuzz_test.go:24`); reconcile the count STAYS 55.
- T10 BEHAVIOR_CONTRACT — the TLS/SDS REJECT→CONSUMED edit (SPEC §9).
- T11 verify (six-gate + full 110-dir differential) + ADR-0286 §Decision/§Consequences + STATE + ROADMAP row 65 `done` + the sentinel-sentence narrow (SPEC §10) + PROGRESS close + router roll.

**Load-bearing ordering insight to surface in the PLAN:** T1/T2/T3 (the `internal/xds` parallel chain) are additive — the build stays green after each because the new symbols have no caller yet; T4 (reject-lift no-op) + T5 (apply-point) wire them into `internal/tls`; T6 (boot pre-scan) is what makes a validation-only-SDS listener actually build a provider (without T6, T5's fetch path is unreachable in a real boot — the fixture would fail with `requires a live SDS provider`). So the fixture (T8) depends on T5+T6 both landing.

**Honor throughout:**
- **Lifecycle = one stage per session.** This session: the PLAN ONLY (docs-only; NO `.go`). After it lands: roll to the phase-65 IMPL.
- **The 60.2 cycle guard STANDS** (`reference_xds_config_seam_transitive_cycle_guard`) — `internal/xds` must NOT import `internal/tls`; the CA-pool build is DUPLICATED in `internal/xds` (mirroring `dataSourceBytes`); the PLAN notes the IMPL verifies with `go list -deps ./internal/xds` (no `...`).
- **Collision-check STANDS** — the four new symbol names (`parseValidationSecret`/`applyValidationResponse`/`fetchValidationSecret`/`FetchInitialValidationContext`) were GREP-confirmed free at the SPEC; RE-GREP at the PLAN (`reference_spec_drafted_identifier_collision_check`). `WithValidationContext` also free.
- **Process anchors:** ADR-0044 (ADR §Context at the SPEC — DONE; §Decision/§Consequences at the IMPL) · ADR-0045 (single-flat-row; escape-valve armable-unconsumed) · ADR-0106 (row flips `done` at the IMPL six-gate) · ADR-0080 (envoy-go-strict distinct-substring rejects) · `reference_roadmap_split_phase_row_done` · `feedback_brief_citations_not_evidence`.
- **`next-prompt.txt` IS TRACKED** (`reference_next_prompt_tracked_despite_gitignore`) — edit it inside the stage worktree and fold into the stage squash; locate commits by SUBJECT (`git log --grep`), never by position.
- **Worktree discipline** (`feedback_git_worktrees`, `feedback_subagent_worktree_detach`, `feedback_subagent_worktree_path_targeting`) — pin the canonical worktree root; the controller verifies the MAIN checkout stays clean.

**ADR reservation ledger (provisional; each finalized at its IMPL in landing order):** ADR-0283 = phase-62 tracing-custom-tags-request-header (LANDED) · ADR-0284 = phase-63 tracing-custom-tags-environment (LANDED) · ADR-0285 = phase-64 tracing-max-path-tag-length (LANDED) · **ADR-0286 = phase-65 xds-sds-validation-context** (§Context DRAFTED at the phase-65 SPEC, SPEC §13; §Decision/§Consequences at the IMPL per ADR-0044; DECISIONS tail STAYS ADR-0285 until the IMPL) · **next-free unreserved: ADR-0287**.

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-65 PLAN is next).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 65 SPEC done`; NEXT = the phase-65 PLAN) + the prior-phase lineage.
3. `docs/envoy-go/phases/65-xds-sds-validation-context/SPEC.md` — the PRIMARY input: the 14-section SPEC pinning the design (§3 the reject-lift + parallel applier + apply-point + boot pre-scan), the §6 reject roster, the §8 fixture, the §10 ~11-task spine (decompose THIS), the §11 live-probe dispositions, the §12 edit-site roster (RE-DERIVE), the §13 ADR-0286 §Context draft.
4. `docs/envoy-go/phases/65-xds-sds-validation-context/BRAINSTORM.md` — the subject + self-pick rationale + the anticipated envelope (context for the SPEC).
5. `docs/envoy-go/phases/64-tracing-max-path-tag-length/PLAN.md` + `docs/envoy-go/phases/60-xds-sds-server-cert/PLAN.md` — the FORMAT precedents for a PLAN (the TDD task spine; the per-task test-first structure; the load-bearing-ordering call-outs). SPEC-60/PLAN-60 is the closest subject precedent (the SDS provider/stream/applier + fixture).
6. `docs/envoy-go/ROADMAP.md` — the xDS family section + the THREE live deferred sentences + the §Schema invariant (row 65 is `in-progress`) + the phase-60 SDS precedents (rows 60/60.1/60.2).
7. `docs/envoy-go/DECISIONS.md` — ADR-0278 (SDS substrate) + ADR-0280 (SDS server-cert wiring) + ADR-0147 (static-mTLS `ClientCAs`) + the ADR-0044/0045/0106/0080 process anchors.
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the differential-harness `reference_*` traps + the xDS `reference_xds_config_seam_transitive_cycle_guard` + the SDS/differential-gRPC-receiver traps.
