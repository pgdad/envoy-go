START a fresh post-phase-24-SPLIT cold-start session for envoy-go.

This is a fresh-session prompt for the post-split 24.1-PLAN cold-start. **Phase 24 (`http-filter-global-ratelimit`, `envoy.filters.http.ratelimit` — the GLOBAL rate-limit filter) was SPLIT at PLAN time into 24.1 + 24.2 (ADR-0045 split-gate; ADR-0201).** The split landed at master via squash-merge `bf868a6` + the SHA-fill follow-up `5d0c601` (current master tip; pushed to origin). ROADMAP row 24 is now the PARENT (`in-progress`, `sub-phases = 24.1, 24.2`); row 24.1 is `in-progress` (ships first); row 24.2 is `planned` (depends-on 24.1). STATE points at **24.1**. The natural next-skill per STATE.md + SKILL_ROUTING state 2 → 3 is **`superpowers:writing-plans` to author the 24.1 PLAN** — decompose `docs/envoy-go/phases/24.1-global-ratelimit-core-and-route-table/SPEC.md` into bite-sized TDD tasks at `docs/envoy-go/phases/24.1-global-ratelimit-core-and-route-table/PLAN.md`, anchor each landing ADR's §Decision/§Consequences body to a specific Task, and run the plan-document-reviewer loop. **PLAN authoring is the interactive `superpowers:writing-plans` work (like the phase-09..23 + 18.1/19.1/22.1 PLAN sessions); subagent-driven execution (project memory `feedback_execution_style.md`) applies at the 24.1 IMPL EXECUTION session (state 3 → IMPL uses `superpowers:subagent-driven-development`), NOT at the PLAN-authoring stage.**

## What this session must do: author the 24.1 PLAN

Per SKILL_ROUTING state 2 → 3, the post-split session authors `24.1-.../PLAN.md` from the 24.1 SPEC slice + the parent master SPEC. The PLAN must:

1. **Decompose into bite-sized TDD tasks** (each: write failing test → verify fail → minimal impl → verify pass → commit) with exact file paths, per `superpowers:writing-plans`. Inputs: the **24.1 SPEC slice** (`docs/envoy-go/phases/24.1-global-ratelimit-core-and-route-table/SPEC.md`) for the 24.1 scope + roster + gates, and the **parent master SPEC** (`docs/envoy-go/phases/24-http-filter-global-ratelimit/SPEC.md`) for the authoritative §4 descriptor engine, §5 PARSE-REJECT roster, §6 code shapes + §6.10 file roster, §7 fixtures, §10 ADR anchor map, §11 D1–D7 pins, §14 testing taxonomy. The 24.1 file roster is §4 of the 24.1 slice.
2. **Anchor the 24.1 landing ADRs to specific Tasks** per the parent SPEC §10 Lands-in-Task map: **ADR-0197 (CORE slice)** → the filter-package + engine (5 core actions) + `RateLimitClient` + dispositions + cluster-stats Task(s); **ADR-0198 (FULL)** → the DELTA-2 route-table parse/seed + accessor-pair Task; **ADR-0200 (FULL)** → the compiled_config + §5 PARSE-REJECT roster Task. The §Context drafts are already anchored at the parent SPEC commit in DECISIONS.md; the §Decision + §Consequences bodies land at their 24.1 IMPL Tasks. **Escape-valve reserve: ADR-0202** (the highest-risk DELTA-2 chain-seed type per parent SPEC §12 item 1 could fire it at 24.1 IMPL — hypothesized UNCONSUMED).
3. **Honor the parent SPEC's empirical AMENDs as the implementation contract** (SETTLED — do NOT re-derive or re-litigate; see the AMEND catalog below).
4. **Create a fresh worktree** per `feedback_git_worktrees.md` (branch `phase-24.1-global-ratelimit-core-and-route-table-plan`, off master tip — run `git log --oneline -1` to confirm the actual tip SHA).
5. **Run the plan-document-reviewer subagent loop** (≤3 iterations per ADR-0005 / the writing-plans review loop). If not approved after 3, set STATE blocked + exit.
6. **Update STATE.md** at session end per BOOTSTRAP §4.1 invariant (lifecycle-state 3 `next-skill = superpowers:subagent-driven-development` for the 24.1 IMPL) + push to origin per `feedback_push_to_origin.md`.

**Do NOT re-apply the split-gate** — the split already FIRED (ADR-0201). 24.1 is a focused single-sub-phase PLAN (the core decision path; well within one PLAN). 24.2 gets its OWN PLAN session after 24.1 phase-done.

## Pre-existing settled context (DO NOT re-litigate)

The phase-24 SPEC is DONE and CANONICAL; the PLAN-time split into 24.1/24.2 is DONE (ADR-0201). Do NOT reopen the SPEC decisions, the D1–D7 empirical pins, the BRAINSTORM Q-decisions, or the split axis.

- **The 11-entry AMEND catalog (parent SPEC §1.1) — 6 substantive REFUTATIONS, empirically verified against v1.37.2 + go-control-plane v1.32.4:**
  - **AMEND-1 (stat anchoring — REFUTES):** 4 counters `ok`/`error`/`over_limit`/`failure_mode_allowed` (no `cluster_not_found`; no gauges) at the **CLUSTER** scope `cluster.<rls_cluster_name>.ratelimit[.<stat_prefix>].<stat>` (NOT `http.<HCM_prefix>.`); modulated by the filter-config `stat_prefix`. Stat count 110 → 114. **[24.1]**
  - **AMEND-2 (no runtime proto fields — REFUTES):** the `RateLimit` config has NO `filter_enabled`/`filter_enforced` fields; enable/enforce are hardcoded runtime keys honored-as-static (always-on). The route-level `disable_key` is the PARSE-REJECT surface (ADR-0200). **[24.1]**
  - **AMEND-3 (filter-config roster — REFINES):** 13 fields incl. NEW `status_on_error` (default 500) + `stat_prefix`; `request_type` STRING (empty⇒both); `rate_limited_status` clamps <400⇒429; `response_headers_to_add` IS on the filter config (max 10); `timeout` 20ms; `failure_mode_deny` false⇒fail-open. **[24.1]**
  - **AMEND-4 (inert `override_option` — REFUTES):** `RateLimitPerRoute.override_option` is `[#not-implemented-hide:]`, NEVER read; embedded `rate_limits[]` is an unconditional early-return (Axis A). NEW per-route `domain`. **[24.2]**
  - **AMEND-5 (vh composition — RATIFIES):** `vh_rate_limits{OVERRIDE=0,INCLUDE=1,IGNORE=2}`; route policy ALWAYS walked; VH per IGNORE⇒never / INCLUDE⇒always / OVERRIDE⇒only-if-route-empty; legacy `include_vh_rate_limits=true` forces INCLUDE. **[24.2 Axis-B table; 24.1 covers OVERRIDE default only]**
  - **AMEND-6 (descriptor `hits_addend` UInt64Value — REFUTES):** descriptor `hits_addend` is `UInt64Value`; Unit enum non-monotonic (WEEK=7) ⇒ the shared fake `RateLimitService` MUST encode by proto field NUMBER + omit unset optionals; descriptor `entries` in action-list order. **[24.1 fake-service contract]**
  - **AMEND-7 (disable_key + runtime enforcement — REFINES):** route `disable_key` non-empty ⇒ PARSE-REJECT; `RateLimit.stage` UInt32Value (0-10); the `RateLimit.Override` (`limit`) only v1.32.4 arm is `dynamic_metadata` ⇒ honor-as-absent. **[24.1 PARSE-REJECT; 24.2 stage path]**
  - **AMEND-8 (reply byte-pin — REFINES):** OVER_LIMIT status `rate_limited_status` (429), body=RLS `raw_body` (empty), rc_details `"request_rate_limited"`, `x-envoy-ratelimited: true`, RLS+filter-config `response_headers_to_add` (descriptor-first), gRPC 8 else 14; error status `status_on_error` (500), rc_details `"rate_limiter_error"`, nullptr-mutate; X-RateLimit on ALL dispositions when DRAFT_VERSION_03, MIN `limit_remaining`, `;w=`/`;name=` suffix. **[24.1 OVER_LIMIT/error byte-shape; 24.2 X-RateLimit headers]**
  - **AMEND-9 (DELTA-2 architecture — REFUTES; HIGHEST-RISK):** the route/vhost `rate_limits` are first-class Route/VirtualHost fields NOT in `typed_per_filter_config`, and the hcm `routeTable` is package-internal — so `RequestRouteConfig()` CANNOT carry them. DELTA 2 is a genuinely NEW chain-seeded `DecoderFilterCallbacks` accessor pair `RouteRateLimits()`/`VirtualHostRateLimits()`, plumbed like the ADR-0165 set-once-by-dispatch primitives (the exact chain-seed type + accessor return-type is the parent SPEC §12 item-1 highest-risk byte-confirmation — could force ADR-0202 at IMPL). **[24.1]**
  - **AMEND-10 (cross-namespace stat write — REFUTES/NEW):** the cluster-scoped surface is self-registered via `ctx.Stats.NewCounterIfAbsent("cluster."+clusterName+".ratelimit."+...)` — the FIRST landed cross-namespace cluster-stat-charge (the pattern ext_authz's `charge_cluster_response_stats` DEFERRED); ADR-0197 §Decision sanctions the write. **[24.1]**
  - **AMEND-11 (action roster + key defaults — RATIFIES):** 12 action arms (10 canonical + `dynamic_metadata` DEPRECATED + `extension`); key defaults `generic_key`→"generic_key", `header_value_match`→"header_match", `query_parameters`→"query_param" (singular), `query_parameter_value_match`→"query_match"; `request_headers`+`metadata` require a config key; `expect_match` default true; empty-action-drop has TWO behaviors. **[24.1 core 5 actions; 24.2 remaining 5]**
- **ADR-0197 + ADR-0198 + ADR-0199 + ADR-0200 §Context drafts are LANDED at DECISIONS.md** (anchored at the SPEC commit per ADR-0044). Their **§Decision + §Consequences bodies land at the sub-phase IMPLs** per the parent SPEC §10 Lands-in-Task map (ADR-0197 core→24.1 / headers→24.2; ADR-0198→24.1; ADR-0199→24.2; ADR-0200→24.1). **ADR-0201 is CONSUMED** (the split-application ADR — full body). **Next-free ADR is `ADR-0202`** (the D-hypothesis escape-valve reserve; hypothesized UNCONSUMED at phase-24 phase-done; LARGER surprise surface than phase-23 — DELTA-2 + cross-namespace stat write).
- **TWO framework deltas STAND:** DELTA 1 = `internal/grpcclient/ratelimit_client.go` (clone the unary `AuthClient`; `ShouldRateLimit` unary; no `Dialer` API change; the THIRD ADR-0158 typed wrapper) **[24.1]**. DELTA 2 = the AMEND-9 chain-seeded route-table exposure **[24.1]**. NO new top-level `internal/` package. Boot-registration alphabetical between `oauth2` and `rbac`; 19 HTTP filters post-phase-24.
- **Per-route `RateLimitPerRoute` = NEW 10th canonical** + ADR-0125 amendment 9 → 10 — LANDS at **24.2** IMPL (anchored in ADR-0199 §Context). Canonical-per-route roster STAYS 9 until then.
- **Two-directory differential STANDS** per `reference_differential_fixture_dispatch_constraint`: `0032-http-ratelimit` (cross-side via a SHARED fake gRPC `RateLimitService` in a NEW `test/helpers/ratelimitgrpc/` + a `fixture.HTTPGlobalRateLimitGRPC` BackendKind + a fixture-local `driver.go` following the ext_authz `0021` pattern; **24.1 scenarios a/b/c/d-core/e/h** in `StatsAsserter` per `reference_differential_asserter_dispatch`; 24.2 ADDS f/g + extends d) + `0033-http-ratelimit-boot-reject` (`domain`-empty). **Both directories CREATED at 24.1** (fixtures 33 → 35); fuzzer `FuzzRateLimitConfigParse` 32 → 33 **[24.1]**. The `disable_key`/`extension`/`dynamic_metadata` rejects are NOT boot-reject fixtures (upstream accepts them; unit-tested + BEHAVIOR_CONTRACT-recorded, count 15 → 18) **[24.1]**.

## Base + invariants at master tip (post-split squash-merge)

- **Master tip:** `5d0c601` (`phase 24 split follow-up: STATE.md SHA-fill (TBD -> bf868a6)`; docs-only). Predecessor: `bf868a6` (squash-merge of `phase-24-http-filter-global-ratelimit-plan` — the PLAN-time ADR-0045 split into 24.1 + 24.2; docs-only). Run `git log --oneline -1` to confirm the actual tip SHA for the worktree base. Pushed to origin.
- **Baseline at master tip (UNCHANGED by the split — docs-only):** **33 differential fixture directories** GREEN (0000-0031; lua 0026-0029 + multi-listener combined runs may hit the documented `freeTCPPort` flake per 22.2 REVIEW §7.4 — re-run in isolation); **32 fuzzers** green; h2spec 53/53 PASS at ADR-0051 v1.32.4 pin; build/vet/lint clean; race-tests clean; **18 HTTP filters wired**; **110 stat names**; **15 envoy-go-strict departure records** at BEHAVIOR_CONTRACT. DECISIONS.md tail at **ADR-0201**; ADR-0197..ADR-0200 §Context-anchored; **next-free ADR-0202**. Canonical-per-route roster STAYS **9** (the 9 → 10 amendment LANDS at 24.2 IMPL via ADR-0199). §9 HTTP-filters family: **1 remaining row** after 24.2 phase-done (`wasm`).
- Project memories ALL apply: `feedback_git_worktrees.md`, `feedback_push_to_origin.md`, `feedback_execution_style.md`, `reference_differential_fixture_dispatch_constraint.md`, `reference_differential_asserter_dispatch.md`.

## Read first (in order)

1. THIS FILE — post-split 24.1-PLAN cold-start + the PLAN-authoring obligations.
2. `docs/envoy-go/phases/24.1-global-ratelimit-core-and-route-table/SPEC.md` — the 24.1 scope + file roster (§4) + fixtures (§3) + gates (§6) + ADR landing (§5).
3. `docs/envoy-go/phases/24-http-filter-global-ratelimit/SPEC.md` — the RETAINED parent master SPEC (the authoritative §1.1 AMEND catalog; §4 descriptor engine; §5 PARSE-REJECT roster; §6 code shapes + §6.10 file roster; §7 differential; §10 ADR anchor map; §11 D1–D7 pins; §12 IMPL byte-confirmations; §14 testing taxonomy; §15 acceptance checklist; §16 split axis — note the SPLIT-FIRED annotation at the top).
4. `docs/envoy-go/phases/24.2-global-ratelimit-perroute-and-headers/SPEC.md` — the 24.2 slice (NOT this session's work; read for scope-boundary clarity — do NOT pull 24.2 surface into the 24.1 PLAN).
5. `docs/envoy-go/STATE.md` — active-phase 24.1; next-skill `superpowers:writing-plans`; next-free ADR-0202.
6. `docs/envoy-go/ROADMAP.md` — row 24 parent (`in-progress`, sub-phases 24.1, 24.2); row 24.1 `in-progress`; row 24.2 `planned`.
7. `docs/envoy-go/SKILL_ROUTING.md` — confirm state 2 → 3 (24.1 PLAN.md exists? no → author it).
8. `docs/envoy-go/DECISIONS.md` tail — ADR-0201 (the split ADR, full body) + ADR-0197..0200 §Context drafts (the IMPL bodies land at their sub-phase Tasks).
9. `BOOTSTRAP_PROMPT.md` (repo root) — §4.1 STATE-update invariant + §5 state machine (state 2 → PLAN).
10. The nearest sub-phase PLAN precedents: `docs/envoy-go/phases/18.1-ext-authz-http/PLAN.md` + `docs/envoy-go/phases/19.1-http-filter-ext-proc-headers/PLAN.md` + `docs/envoy-go/phases/22.1-http-filter-lua-vm-and-headers-bridge/PLAN.md` (the foundational-sub-phase PLAN shape + TDD task granularity).
11. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`.
