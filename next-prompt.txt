=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **PHASE 90 (`h2-host-authority-normalization`) IS OPEN AT LIFECYCLE-STATE 3. ROW 90 IS `in-progress` AT `ROADMAP.md:152` AND `want` IS 122.** ⇒ **THERE IS BANKED, FULLY-PLAN'd WORK. THIS SESSION EXECUTES THE PHASE-90 IMPL. DO NOT SELF-PICK A NEW SUBJECT AND DO NOT RE-PLAN.** ⚠️ **THE SENTINEL DOES NOT FIRE — check (1) prints `NOT DONE: row 90`, check (2) prints SIX, check (3) is SILENT. DO NOT create `stop`.** ⚠️ **MEASURE ALL THREE YOURSELF BEFORE DOING ANYTHING ELSE; do not take this file's word for it.**

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE. This form parses FIELDS instead of matching a pattern, and
#     asserts the row DENOMINATOR so a deleted row is itself an error.
awk -F'|' -v want=122 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was BLIND to the Operational-tooling family's SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). Rows live in the main table, never under family
#     HEADINGS. `Deprecated / edge features` is intentionally out of scope.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **CHECK (1) HAS ITS OWN LIVENESS WHILE PHASE 90 IS OPEN — IT PRINTS `NOT DONE: row 90`.** That is the HEALTHY reading, not a defect. Run BOTH doctoring NCs EVERY SESSION anyway:
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md.
```
⚠️ **THE NC-A SIGNATURE CHANGES SHAPE WITH THE BOARD STATE.** On an ALL-`done` board it reads `NOT DONE: row 62` **ALONE**. **While row 90 is in-progress it reads `NOT DONE: row 62` AND `NOT DONE: row 90`, and BOTH lines are required** — row 62 proves the check is live, row 90 proves the row is still registered. **A reading of `row 62` alone would mean THE ROW WAS LOST.** ⚠️ **AND THE SHAPE FLIPS AT THIS ROW'S CLOSE:** once the IMPL flips row 90 `done`, NC-A returns to the ONE-line `row 62` form and check (1) goes SILENT — **at which point checks (2) and (3) are the only things still blocking the sentinel, and check (2) prints SIX. `stop` STILL MUST NOT BE CREATED.**
```sh
# the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```
Also NC the DENOMINATOR (`want=121` on the real file must print `GATE FAIL: examined 122 data rows, expected 121`).

⚠️ **AND WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -oiE '-family row'` fails with `grep: amily row: No such file or directory` and the surrounding arithmetic prints `base=0 now=0 delta=0`, **which reads exactly like "no change".** **Always pass `--` before the pattern**, and the counts are form-dependent: **95 occurrences vs 67 LINES** at this tip (`grep -c` counts LINES). **A gate that reads zero on BOTH sides is not evidence of invariance.**

⚠️ **THE ARM-A MALFORMED-ROW FIGURE RECONCILES ONLY UNDER AN ESCAPE-AWARE FIELD COUNT — RE-VERIFIED AT THE PHASE-90 PLAN.** The malformed rows are ids **57** (`NF=9`) and **69** (`NF=10`), at lines **119** and **131**. ⚠️ **A NAIVE `awk -F'|'` `NF != 8` FORM READS *SEVENTEEN* ROWS** — reproduced again at this tip — because legitimately ESCAPED pipes (`\|`) in row summaries split as fields. **Strip `\|` BEFORE splitting.** Row 90 is `NF=8`, well-formed. The naive count is NOT a drift signal. ⚠️ **AND THE DENOMINATOR CONVENTION: the recorded figures are awk `NF` (8 for a well-formed row), NOT column count (6).** ⚠️ **THE IMPL EDITS ROW 90's TEXT — RE-RUN THIS GUARD AFTERWARDS AND ASSERT {57, 69} AND NO THIRD ID.**

**SENTINEL RUN MECHANICALLY AT THE PHASE-90 PLAN (2026-08-22), at stage start AND at close. `ROADMAP.md` is BYTE-UNTOUCHED this stage, so there is ONE side, not two. IT DOES NOT FIRE; `stop` was NOT created** (verified absent at the git root AND in the stage worktree). ACTUAL output, recorded not predicted: (1) **`NOT DONE: row 90`** with the denominator asserted at 122 · (2) **SIX** at `:200 :206 :212 :222 :228 :236` · (3) **SILENT**.
- **ALL FOUR NCs FIRED.** NC-A `NC LANDED? [ in-progress ]` inspected FIRST => `NOT DONE: row 62` **AND** `NOT DONE: row 90` · NC-B `want=121` => `GATE FAIL: examined 122 data rows, expected 121` · NC-C residual **2 -> 0** confirmed first => `NEVER OPENED: gRPC`, WASM correctly silent · NC-D long **5** / short **1**, union **6**.
- Every leak axis at this tip: `-family row` **95 / 67** · `gRPC-family row` **2** · `Operational-tooling-family row` **3**.
- ⚠️ do NOT "fix" the six and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases; the candidate sentences are a WINDOW onto a larger deferred backlog, not an inventory of it. ⚠️ **THE INVENTORY IS ~59, NOT ~42** — the long-carried "~42" is the SENTENCE-VISIBLE subset only. **Cite ~42 only as "sentence-visible", never as the inventory.** ⚠️ **AND TWO WINDOW CANDIDATES ARE STALE — RE-VERIFIED BY EXECUTION AT THE PHASE-90 PLAN:** window `:222` still lists the H2 `//`-path routing bug that **ROW 87 CLOSED** (`internal/filter/hcm/h2/stream.go:498` is `url.ParseRequestURI`, confirmed) and the `/stats/prometheus` projection gap that **ROWS 79 AND 80 CONSUMED** (rows 79/80/87 all read `done`, confirmed). **A candidate's presence in a window is NOT evidence it is still open.**
- ⚠️ **THE PROVENANCE GREP: USE THE DISCRIMINATING FORM.** `:authority` + backticked `` `host` ``/`` `Host` `` + `h2-host` reads **0 across all six windows**, with the positive control reading **22** on row 90's own line (`:152`) and a fabricated token reading **0** six times. The LOOSE form reads 1 on `:222` (the English word inside *"acquired ADR authority by proximity"*).

==============================================

## STATUS AS OF 2026-08-22: PHASE 90 (`h2-host-authority-normalization`) **OPEN AT LIFECYCLE-STATE 3** — row 90 `in-progress` at `ROADMAP.md:152`, `want` **122**. **THIS SESSION EXECUTES THE PHASE-90 IMPL.**

**What the 90 PLAN landed (2026-08-22, base master `c7fef29b`, branch `phase-90-plan`, ONE squashed commit).** Docs-only: **ZERO production `.go`, ZERO test `.go`** (gated by `git diff --stat c7fef29b -- '*.go'` ⇒ EMPTY). `PLAN.md` created (**1039 lines**, **NINE tasks**). `PROGRESS.md` 187 -> 294. `STATE.md` rolled IN PLACE. `STATE_HISTORY.md` **510 -> 512** (`2 0`). **`ROADMAP.md`, `BEHAVIOR_CONTRACT.md` AND `DECISIONS.md` ALL BYTE-UNTOUCHED** (verified by EMPTY DIFF against master, each individually) — **a PLAN adds no ADR**. Next-free stays **`ADR-0313`**; the strict `PROPOSED` guard **STAYS ARMED at 1**.

### ⚠️ THE PHASE-90 PLAN REFUTED **FIVE** SPEC CLAIMS AND **ONE OF ITS OWN MEASUREMENT AGENTS

**Every one is settled by execution. Do not re-litigate them, and do not inherit the refuted wording.**

1. ⚠️ **THERE IS NO `host` BRANCH IN `stream.go` — AND NO `host` IDENTIFIER AT ALL.** ADR-0312 §Context ¶1 says *"`buildRequest` independently does `regular.Add` for `host`"*, which reads as though a `host`-specific site exists to modify. `grep -in 'host' internal/filter/hcm/h2/stream.go` returns **exactly two lines, `:503` and `:507`, and BOTH are the WRITE of `authority`** onto the `*http.Request`. A regular `host` reaches the **generic** `regular.Add(name, h.Value)` at **:472** with zero special-casing (`http.Header.Add` then canonicalizes the map key to `Host`). ⇒ **THE IMPL ADDS A BRANCH; IT DOES NOT MODIFY ONE.** Every break arm on `buildRequest` names an injection site in NEW code, and this is why the measured prototype is `−0`.
2. ⚠️ **THE GUARD ASYMMETRY IS BY PROPERTY, NOT BY SYMBOL — SPEC §9.3 IS REFUTED.** Executed: `if h.Name == "host" { continue }` **unconditionally inside `buildH2Request`** — the exact production behaviour arm A introduces — leaves **`rc=0`, BOTH packages `ok`. VACUOUS.** The only property pinned anywhere in the tree is **`H2Request.Authority`'s VALUE**, via `TestBuildH2Request_PseudoHeaderExclusion`'s `wantAuth`. **Carrier membership is unpinned in BOTH symbols.** ⇒ the roster is organised around **FOUR PROPERTIES** (PLAN §5.2), not two symbols, and *"cover `buildRequest` specifically"* would still have shipped a vacuous guard for arm A.
3. ⚠️ **COST +36/−0, NOT +34/−0** (post-`gofmt`; `gofmt -l` empty before AND after). `authoritySeen` **6** CONFIRMED · `hostField` **6**, not 7 — **REFUTED** · and a **THIRD required symbol the SPEC never names, `hostSeen` (8)**: a bare `hostField != ""` **cannot distinguish ABSENT from PRESENT-AND-EMPTY** and cannot express first-occurrence-wins, both explicit D-90-SCOPE requirements. ⇒ **the IMPL's symbol gate is a THREE-row table.** `reference_measured_prototype_is_a_lower_bound` fires a THIRD time; the cause is again UNDER-ENUMERATION.
4. ⚠️ **THE FIXTURE CONSTRAINT IS OVER-BROAD.** SPEC §8.2's *"raw-framer arms must not hit `/api`"* is FALSE. `counts[idx]++` occurs at **exactly ONE line — `test/fixtures/0004-h2-routing/driver/driver.go:308`** — inside the `/api/v1/<n>` loop; the eight phase-89 arms already hit `/api/v1/reflect-headers/` through a different loop and `[3,3,3]` holds. ⇒ **the real constraint is "do not add requests to the COUNTING LOOP"**, a `p90*` path takes the documented `a5` fall-through to `- match: { prefix: "/api" }`, and **BOTH `0004` YAMLs STAY BYTE-UNTOUCHED** — no route edit and no `TestRenderBootstrap_*` update.
5. ⚠️ **"THE 69 WAS A MEASUREMENT ARTIFACT" IS PARTIALLY REFUTED.** A 69 reading was reproduced AT THE PLAN and came with **`rc=1` and a real `--- FAIL: TestSDSEndToEnd_FetchFailure_BootFailsClosed`** (`internal/boot`). A **SECOND** flake, **`TestProvider_FetchInitialCertificate_Timeout`** (`internal/xds`), fired on **3 of 8** full-suite runs. Both pass **5/5 in isolation**; both are the standing SDS dial-budget class. ⚠️ **CAUSATION EXCLUDED MECHANICALLY, NOT BY RE-RUNNING:** `go list -deps -test ./internal/xds/ | grep -c 'filter/hcm/h2'` ⇒ **0** — that test binary does not link the patched package at all. ⇒ **69 IS A RED RUN, NOT A COUNT DISCREPANCY. THE IMPL'S GATE MUST NAME BOTH TESTS** or it will read a flake as a row-90 regression.
6. ⚠️ **A MEASUREMENT AGENT WAS REFUTED BY THE CONTROLLER.** An agent reported that `net/http` lifts the authority out of `r.Header` so `0004`'s reflected block *"can never contain it"*. At the pinned source, `golang.org/x/net@v0.34.0/http2/server.go` deletes **only `Trailer`** (`:2341`) and has **NO `Del("Host")`**; `Header: rp.header` (`:2367`) and `Host: rp.authority` (`:2373`). **A regular `host` survives into `req.Header` as `Host`. SPEC §8.4's table is CORRECT.** ⚠️ **`feedback_brief_citations_not_evidence` APPLIES TO SUBAGENT REPORTS EXACTLY AS TO STAGE BRIEFS.**

### THE DECISIONS THE PLAN FROZE — and one is NEW

- **D-90-DUP (NEW; the SPEC did not reach it).** `buildRequest` guards `:method`/`:path`/`:scheme` against duplication (`:426`/`:432`/`:441`) and **does NOT guard `:authority`** (`:446-447`, silent last-wins). An implementer following the local idiom would add the reject — **an unpriced behaviour change outside this charter, and parity with an UNMEASURED side** (no SPEC arm ever sent `:authority` twice). **`authoritySeen` tracks PRESENCE ONLY.** A **stability-pin test** (`TestAuthorityNormalization_DuplicateAuthorityUnchanged`) asserts the non-change — it PASSES pre- and post-fix and is **NOT part of the RED census**. The reject belongs to the deferred arm-C row.
- **D-90-TARGET — the differential axis is `host`-presence + `r.Host`.** A **route assertion is FORBIDDEN** (SPEC §4: `routeMatch` is literally `matches(path string) bool`; `git grep '\.Host' -- internal/filter/hcm/route.go` ⇒ **rc=1** against a positive control of **3** on `.Path`; both YAMLs are `domains: ["*"]`). The **access-log / Zipkin axis is REJECTED on a NEW measurement: NOT ONE of the four H2-capable fixtures (`0004`, `0079`, `0080`, `0119`) carries an access-log or tracing surface** — banked as follow-on **(vi)**, not left as an implied gap.
- **D-90-YAML — both `0004` YAMLs BYTE-UNTOUCHED** (refutation 4).
- **THE ARM BLOCK MUST NOT BE FAIL-FAST.** `0004`'s existing arms assert in-band with `return nil, fmt.Errorf(...)` (`driver.go:449/452/456`), so the first failing arm aborts the Drive and later arms are unreachable (`reference_failfast_driver_masks_later_red_arms`). Follow **`0119`'s** discipline (`driver.go:429-432`: *"It NEVER returns an error"*): record a normalized line per arm into `out`; **the runner's cross-side byte-compare becomes the assertion.** Every arm sends a **fixed literal** authority, never the dial address.

### WHAT THE IMPL EXECUTES — PLAN §6, NINE TASKS

**T1** RED census, tests only, ZERO production bytes — `internal/filter/hcm/h2/authority_norm_test.go` (NEW, `package h2`, `t.Errorf` per property NEVER `t.Fatalf`) + ⚠️ **the REACHABILITY control** (`panic("NC-REACH-buildRequest")` must redden `internal/filter/hcm/h2`; **without it the census's green columns prove nothing**) + denominators asserted · **T2** production `buildH2Request` (a NEW `case "host":`) · **T3** production `buildRequest` (a `continue` before `regular.Add`) — ⚠️ **SEPARATE TASKS deliberately: separate properties, independently unpinned** · **T4** GREEN census + the **THREE**-symbol gate + the slice-only-writer gate re-read at **6** · **T5** the ~6-line `r.Host` backend edit · **T6** the raw-framer arm block appended after `driver.go:459` · **T7** **FOUR** break arms on **FOUR DISTINCT** injection sites · **T8** docs · **T9** the six-gate posture LAST.

⚠️ **B3 (delete the `continue` in `buildRequest`) IS EXPECTED TO BE DIFFERENTIALLY SILENT** — `0004` runs an **empty** `header_mutation` + `router`, so nothing reads the decode map. **It must redden the UNIT roster instead, and the IMPL MUST RECORD THAT ASYMMETRY AS THE OBSERVED RESULT**, because a break arm that reddens nothing anywhere is indistinguishable from one that was never applied. **Assert B3 landed by grepping the patched file, not by observing a build.**

⚠️ **A FIFTH BREAK IS DELIBERATELY NOT WRITTEN** — no break isolates `req.URL.Host` from `req.Host`, because both are set from the same `authority` local. **P4 is pinned by two unit assertions, not by a break.**

### THE RED BASELINE — ALREADY CAPTURED. Reproduce it; do NOT invent a different one.

```
--- PASS: .../P_authority_only        <-- POSITIVE CONTROL: not vacuously failing
--- FAIL: .../A_both                  host on carrier = true, want false ; req.Header Host = "h.example", want absent
--- FAIL: .../B_host_only             Authority = "", want "h.example"   ; req.Host = "", want "h.example"
--- FAIL: .../C_empty_authority       host on carrier = true, want false
```
5/5 PASS with the prototype installed. ⚠️ **`E_dup_host_first_wins` and `D_dup_authority` are NEW at the PLAN and are NOT in that transcript** — record your own census over the FULL roster and assert the denominator.

### THE DOC EDITS THE IMPL OWES — text already fixed in PLAN §8

- **`ROADMAP.md` row 90's *"an empty authority is a ROUTE-MATCHING input, not merely a wire artifact"* is FALSE.** Replacement text is written in PLAN §8.1. **Gate: `grep -c 'ROUTE-MATCHING input'` ⇒ 1 before, 0 after**, positive control `grep -c 'routeMatch'` on the replacement. ⚠️ **Row 90 is ONE line — do not change its field count; re-run the ARM-A guard and assert {57, 69} and no third id.**
- **`BEHAVIOR_CONTRACT.md:2034`** — *"`:authority` forwarded verbatim from downstream to upstream"* becomes untrue. ⚠️ **Found BY STRING: `grep -n 'forwarded verbatim'` returns TWO hits and `:2033` is a DECOY about RESPONSE headers.** The bullet **already carries** a phase-87 ADR-0309 rider about `:path` — **that is the vehicle and the precedent. ADD A SECOND RIDER TO THE SAME BULLET, do not rewrite the sentence**, so every by-line cite stays valid. Rider text is written in PLAN §8.2.
- **`DECISIONS.md`** — ADR-0312 §Decision + §Consequences appended **IN PLACE** after the RETAINED italic footer, **no renumber, NO `---` separator** (`^---$` stays **216**), strict guard **1 -> 0**. §Consequences must carry the **SIX** deferred follow-ons as named rows AND record this PLAN's refutations — **including that ADR-0312 §Context ¶1's own `regular.Add` wording reads as if a `host` branch exists.**

### ⚠️ COUNTS AT THIS TIP — RE-DERIVE MECHANICALLY, NEVER COPY

- **`ROADMAP.md` 240 lines / 122 data rows**; row 90 `in-progress`, all others `done`; check-(2) anchors `:200 :206 :212 :222 :228 :236`; `-family row` **95 / 67**; `gRPC-family row` **2**; `Operational-tooling-family row` **3**; ARM-A malformed ids **57**/**69** at lines **119**/**131** (ESCAPE-AWARE, `NF` semantics; naive reads **17**).
- **`DECISIONS.md` 18297** · tail **ADR-0312** (⚠️ **PROPOSED — the IMPL completes it and disarms the guard 1 -> 0**) · next-free **ADR-0313** (TAIL-derived; `grep -c '^## ADR-0313'` ⇒ **0**) · strict `PROPOSED` guard **1** · `^---$` **216**. ⚠️ **THE HEADING FIGURE IS SCOPED: `^## ADR-` reads 311 while bare `^## ` reads 319** — the extra **8** are `## Amendment (per phase …)` headings. **Both are right; they measure different things. STATE THE SCOPE whenever the number is restated.** headings+1 COLLIDES at the ADR-0209 gap regardless (ADR-0209 ABSENT, verified 0). ⚠️ **A NEW ADR TAKES NO `---` SEPARATOR.**
- **`BEHAVIOR_CONTRACT.md` 5962** — BYTE-UNTOUCHED at the PLAN. ⚠️ **every by-line cite at or below the `## HTTP/2` asserted list has shifted +2 (phase 89), +2 (phase 88) and +1 (phase 87). CITE BY STRING OR SYMBOL.**
- **`STATE.md` 63** · **`STATE_HISTORY.md` 512** · **`BOOTSTRAP_PROMPT.md` 522** (repo ROOT) · phase dirs **131** · ⚠️ **`REVIEW.md` 37 is a FILE COUNT** — `git ls-files | grep -c 'REVIEW\.md$'` ⇒ **37**, newest at `phases/25.3-…`; **no phase since 25.3 has written one — the standing REVIEW departure, NAMED not claimed.**
- **differential fixtures 121** at **`test/fixtures/`** (⚠️ **NOT `test/differential/fixtures/`, a plausible wrong path returning a SILENT `0`**); numeric tail `0119-grpc-unary-trailers`, next-free **`0120` STAYS UNCONSUMED**. · **fuzzers 55 / 48 files.** · **BackendKind tail 38** (`H2GoawayResponder`, `test/differential/fixture/fixture.go:614`) — ⚠️ **a TAIL VALUE, not a count**; `0004` uses `HTTPSH2 = 2` and **needs no new kind**.
- ⚠️ **fixture blank imports 121 — THE NUMBER IS RIGHT ONLY WITH ITS SCOPE STATED.** `grep -cP '^\t_ "[^"]*test/fixtures/'` reads **121** scoped to `test/differential/runner_test.go` but **122 repo-wide**; the extra hit is `test/fixtures/0018-http-rbac/inputs/driver.go:105`, a `pki` sub-package import that is **NOT a fixture registration**. The unnarrowed `grep -cP '^\t_ "'` reads **123** and is REFUTED.
- **stat surface: the COMMAND binds, not the number.** `grep -ro --include='*.go' -e 'NewCounter(' -e 'NewGauge(' .` ⇒ **406**. ⚠️ **A "403" claim is a LINE count scoped to `internal/` — different unit, different scope. NOT drift.**
- **The slice-only-writer gate reads 6** by the canonical form (`git grep -nE '\.Headers[[:space:]]*=[^=]' -- 'internal/filter/hcm/*.go' 'internal/filter/hcm/h2/*.go' | grep -v '_test\.go' | grep -v 'resp\.Headers'`). ⚠️ **THIS ROW MODIFIES WRITER #1 (`h2/stream.go:345`) RATHER THAN ADDING A SEVENTH — the expected reading at row-done is STILL 6, and the IMPL must STATE that.** ⚠️ **THE COMPANION `*H2Request` COUNTER IS VACUOUS AS A CONTROL** — it reads **0**, exactly like a deliberately fabricated selector, so it cannot discriminate; use `git grep -c 'H2Request'` (**20** files) as the live positive control. NAMED BLIND AXES: element-level in-place mutation (`.Headers[i].Value = …`); any writer outside `internal/filter/hcm/`; and ⚠️ **the `resp\.Headers` exclusion silently drops `h3dispatch.go` even though the pathspec covers it.**
- **Module path is `github.com/pgdad/envoy-go`** — which CONFIRMS `ROADMAP.md`'s five `esalaine` cites as a live documentary defect.
- **Anticipated deltas: +0 ON EVERY AXIS** — fixtures 121 · blank imports 121 · BackendKind tail 38 · new ports none · `go.mod` +0 (`golang.org/x/net` is already a direct require at `go.mod:18`; `http2` is already imported by five files under `test/`) · config fields +0 · fuzzers 55/48 · stat surface +0 · `0120` UNCONSUMED · **both `0004` YAMLs BYTE-UNTOUCHED** · phase dirs 131.

### ⚠️ CONTESTED COUNTS — NO NUMBER IS CARRIED

- ⚠️ **`STATE_HISTORY.md`'s "archive labels 202" IS NOT REPRODUCIBLE.** The strict `^- \*\*prior active-phase:\*\*` form reads **163 before AND after an eviction — DELTA 0** — because an evicted entry carries `(evicted at …)` *before* the colon and therefore never matches. ⇒ **that form is the EVICTEE ABSENCE GUARD and is STRUCTURALLY INCAPABLE of counting archive labels.** ⚠️ **AND THE ARCHIVAL SHAPE IS ONE SINGLE LINE** — the `(evicted …):**` label with the moved bullet **INLINE**, which is exactly why the strict form does not move; a two-line append (label + verbatim bullet) breaks the guard and reads 164. **Re-verified at the phase-90 PLAN: 510 -> 512 (`2 0`), strict 163 DELTA 0, loose 205 -> 206.** Carry no archive-label absolute.
- the loose `PROPOSED` matcher's whole-file count · the stat-surface DOC-side absolute (1205 vs 1207) · the zero-test `generic/*` suite count (13 vs 14).

### ⚠️ DOCUMENTARY DEFECTS — carried forward, still deliberately NOT fixed

⚠️ **`ROADMAP.md` row 90's *"ROUTE-MATCHING input"* sentence is FALSE — the IMPL fixes it** · ⚠️ **the SPEC's *"`0004`'s NINE `H2RoundTrip` call sites"* is a `grep -c` LINE count; the real call count is SEVEN** (`:433` and `:739` are prose) · ⚠️ **SPEC §8.3's `discoverFixtures` cite `1462-1497` is off — the function ends at `:1499`** · ⚠️ **the phantom gate `git grep -c 'h2.parseHeadersForRequest'` reads 1, not 0** — it counts a COMMENT CITATION; the *definition* selector `^func.*parseHeadersForRequest` is the one reading **0** (positive control `buildH2Request` ⇒ 1) · **`0004-h2-routing/envoy-go.yaml:3` says *"This file is documentation only"* while `driver/driver.go:214` does `readYAML("envoy-go.yaml")`** · **the subject injects `User-Agent: Go-http-client/1.1` on every H1 upstream request where the reference injects none** · ADR-0310 §Consequences (x)'s C3 *"deferred parity"* framing and (xi)'s *"~64 KiB band"* · `DECISIONS.md` records `INNER_EXIT=0` at phase 87, a value nothing in the tree emits · `internal/filter/http/types.go`'s FALSE *"per ADR-0071's filter API stability"* comment (⚠️ **write "rejected on measured blast radius — 33 production + 38 test files, ~101 mutation call sites". NEVER "per ADR-0071"**) · the H1→H2 502 cite naming the wrong site and its UNRECORDED H3 leg · the stale H1-trailers prose at `chain.go:477-489` and `connection.go:562-568` · `header_mutation` rejecting `remove` of a protected header where the reference config-ACCEPTS it · `RunAction`'s unguarded H2 arm · `ADR-0051` §2 · `ADR-0058`'s dead `routerActionH2.doH2` location · `ADR-0057`'s "27 round-trips" (now **31**; ⚠️ `README.md:36`'s "first 27 requests" is CORRECT as a prefix) · **`ROADMAP.md` rows 57/69 malformed (ARM-A GUARD)** · **`STATE.md` §Project counts FROZEN at phase 76** (they self-contradict §Current — do not anchor on them) · `harness_test.go` port inventory STALE · `body.go` nolint INERT · xDS cycle guard NOT AUTOMATED · `wasm/doc.go:219` two errors · ROADMAP cites `esalaine` FIVE times · `rbac.go:50` token `F2` · root `PROGRESS.md` stray phase-32.1 doc · SPEC-86/PLAN-86 cite a nonexistent `internal/xds/xdsgrpc/...` path · `BEHAVIOR_CONTRACT.md`'s "no stdlib net/http parsing" sentence (RETAINED, ridden by ADR-0309) · the TWO riders citing the ADR-0052 mandate at a DRIFTED `:1821` · window `:222` carries TWO CLOSED candidates.

### ⚠️ EXPLICITLY NOT MEASURED AT THE PLAN — stated so it is never inferred

- **NO differential fixture was built or executed, and NO Docker container was started.** The §7 arm roster, break protocol and backend edit are **DESIGNED FROM READ CODE**. The IMPL runs them for the first time.
- **The reference side was NOT re-probed at the PLAN** — every reference behaviour cited (the nine-arm validity partition, the three filter-write fold rules, the H1 status matrix, arm C's zero-byte teardown) is **INHERITED FROM THE SPEC'S RUNS**.
- **`-race` was run on nothing.** · **The H3 leg — ZERO probes**, at the SPEC and at the PLAN.
- **The encode/response direction and upstream-side authority handling.**
- ⚠️ **Downstream TLS/ALPN H/2 for the arms themselves** — the SPEC's arms all used **h2c prior knowledge**, while `0004` is **TLS+ALPN-h2**. **The instrument is being moved to a transport it was not exercised on**; the arm MUST assert `conn.ConnectionState().NegotiatedProtocol == "h2"` before writing the preface (`0119:468`) and reuse `d.loadTLSConfig()` (`driver.go:280`).
- **The exact charset the reference accepts in an authority** · whether a conformant peer accepts the authority-less request the reference forwards after a filter `remove("host")` · whether the reference emits a GOAWAY before closing on arm C (a plain EOF was seen; no packet capture taken).

### ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + ALL FOUR NCs YOURSELF** and record ACTUAL output. **Check (1) prints `NOT DONE: row 90` while phase 90 is open — that is HEALTHY.** NC-A needs BOTH `row 62` AND `row 90` — **until the IMPL flips the row, after which the ONE-line form is correct.**
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** The 90 PLAN refuted FIVE SPEC claims and ONE of its own measurement agents. Do the same to the PLAN.
3. **LINE ANCHORS DRIFT; SYMBOL ANCHORS DO NOT — RE-VERIFY EVERY CITE AT YOUR TIP.** ⚠️ **PATHSPEC-SCOPE every symbol assertion** — `^func buildRequest(` reads **1** scoped to `internal/filter/hcm/h2/*.go` but **3 repo-wide**, and ⚠️ **only ONE of the two extras is docs: `internal/filter/network/kafkabroker/codec_test.go` defines its OWN package-local `buildRequest`** — a genuine IDENTIFIER COLLISION, not contamination. ⚠️ **AND THE RECEIVER NAME IS PART OF THE ANCHOR:** `^func (c *ClientConn) RoundTrip(` reads **0** because the receiver is `cc` (`h2/client.go:817`, `:authority` value at **`:832`**, carrier appended at **`:834`**).
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. ⚠️ **TWO FLAKES ARE LIVE AND BOTH ARE THE SDS DIAL-BUDGET CLASS:** `TestSDSEndToEnd_FetchFailure_BootFailsClosed` (`internal/boot`) and `TestProvider_FetchInitialCertificate_Timeout` (`internal/xds`, **3 of 8** full-suite runs at the PLAN). **Both pass 5/5 in isolation. NAME THEM IN THE GATE.** Also live: the driver-owned receiver port race (EXPLAINED — the ephemeral range) · `internal/cluster` outlier · `internal/httpclient` zero-value · the two 84.2-era flakes · `TestServerConn_TinyWindowDelivery` · the REFERENCE h2spec section-8 flip. **Capture `-v` output BEFORE re-running anything.**
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only). Foreign containers left untouched throughout phase 90: `infallible_booth`, `crazy_kare`, `golink-ai`, `quizzical_goldstine`.
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`.
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL** (⚠️ **QUALIFIED AND PATHSPEC-SCOPED**), **and ASSERT THE BOUND BINARY with `ss -ltnp` before reading any probe result.** **`gofmt -l` never exits non-zero — gate on OUTPUT.** ⚠️ **`go test` WITHOUT `-v` PRINTS ZERO `=== RUN`** — a census can read `RUN=0` beside `RC=0`, a VACUOUS GREEN. ⚠️ **A `-run` selector matching nothing prints `[no tests to run]` and EXITS 0.** ⚠️ **NEW GATE HAZARD, FOUND AT THE PLAN: on `-v` output, unanchored `grep -c 'FAIL'` reads 11 ON A FULLY GREEN TREE** — nine `INFO wasm: FAIL_CLOSED/FAIL_OPEN …` log lines plus two lines of a test NAME containing `boot-FAILS`. **Use `grep -cE '^(FAIL|--- FAIL)|^ *--- FAIL'`, which reads 0 green.** The GREEN BASELINE is **`rc=0`, 70 `ok`, 0 anchored FAIL, 8201 `=== RUN`, anchored panic gate 0.**
8. ⚠️ **`pgrep -f` / `pkill -f` MATCHES YOUR OWN SHELL AND KILLS THE TOOL CALL (exit 144)**, and a `pgrep -f 'a\|b'` ALTERNATION MATCHES NOTHING SILENTLY. **Kill ONLY PIDs whose cmdline contains your OWN scratch path.** ⚠️ **AND THE `\|` TRAP FIRED ON THE PLAN CONTROLLER'S OWN SELF-REVIEW** — `grep -ciE 'a\|b'` matches a LITERAL pipe in ERE, so three real hits read **0** and briefly looked like coverage gaps. **In ERE the alternation is a BARE `|`.**
9. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-*`, `wt-*` or stage worktree should outlive its stage close** — expect only `master` (verify with `git worktree list`).
10. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** (a)/(b) differential **121 fixtures** + full `go test ./...`, gated on `PIPESTATUS[0]` and a **SET RECONCILIATION** (⚠️ **NOT `INNER_EXIT`, which DOES NOT EXIST — the suite is a direct `go test ./test/differential/`, so the outer process IS the inner process**); (c) **h2spec cite ONLY from your own run — and it is STRUCTURALLY incapable of anchoring this row: its harness configures `envoy.filters.http.router` ALONE with `direct_response` routes and never goes upstream**; grpc-conformance deferred in writing; proxy-wasm 10/16; (d) fuzzers 55/48; (e) the ANCHORED panic gate `^panic:|DATA RACE|SIGSEGV` on every differential launch (⚠️ an UNANCHORED form FALSE-FIRES 14 times on a fully-green log); (f) no REVIEW.md — standing departure.
11. ⚠️ **THE FIXTURE INVOCATION: `go test ./test/differential/ -run 'TestDifferential/0004-h2-routing' -count=1 -v`.** ⚠️ **`0004`'s own README `:127-131` OMITS `-count=1` — DO NOT copy it verbatim**; the break protocol edits the driver AND backend packages, so a cache hit is live. `TestDifferential` is skipped under `-short` and calls `ensureDocker(t)`. ⚠️ **Gate 2's failure mode is a SILENT PASS** — `runner_test.go:200` `t.Skipf`s an unregistered fixture and **no fixture-count gate exists anywhere** (`git grep -c 'len(fixture.DriverRegistry)\|fixtureCount' -- 'test/**/*.go'` ⇒ **rc=1**). **ASSERT THE FIXTURE RAN, not merely that the suite was green.**
12. ⚠️ **`-p` PUBLISHING WORKS; `--network host` IS WHAT DOES NOT** — it does not share the host netns here AND it SILENTLY IGNORES `-p`. **Host-gateway is `192.168.65.2`** (⚠️ **`getent hosts` resolves it IPv6-ONLY — use `getent ahostsv4`**). ⚠️ A **STRICT_DNS** cluster whose name does not resolve **blocks listener init entirely**. ⚠️ **`clusters: []` is BOOT-REJECTED even when every route is a `direct_response`.** ⚠️ **envoy-go REJECTS `match.headers` outright**, and ⚠️ **refuses `codec_type: HTTP2` without TLS unless `--allow-h2c` is passed.**
13. ⚠️ **A RAW-FRAMER PEER MUST HOLD ONE `hpack.Decoder` PER CONNECTION.** The dynamic table is CONNECTION-scoped; a fresh decoder per request decodes only the FIRST request on a pooled connection and then yields `invalid indexed representation index NN` with a **truncated field list — which reads exactly like "headers were lost"**. **`0119`'s `driveArm` satisfies this by dialing a FRESH connection per arm** (`:455`, decoder `:479`, `streamID = 1`, `defer conn.Close()` `:466`) — **copy that shape.** ⚠️ The `hpack.NewDecoder(n, nil)` SIGSEGV is NARROWER than long recorded: real for a standalone `dec.Write()`, **SAFE as `Framer.ReadMetaHeaders`** (`frame.go:1541` calls `SetEmitFunc` first, `:1578` restores a no-op, not nil).
14. ⚠️ **`bash /dev/tcp` + `timeout cat <&3` INSIDE COMMAND SUBSTITUTION RETURNS SILENTLY EMPTY** — a false "no reply" a Go raw-TCP client immediately disproves. **Do not build an H1 probe on `/dev/tcp`.**

## ⚠️ EVERY PORT BAND THIS LOOP ASSIGNED BETWEEN PHASES 87 AND 90 WAS INSIDE THE KERNEL EPHEMERAL RANGE

`net.ipv4.ip_local_port_range = 32768 60999`. The 47410-47441, 47450-47559, 47560-47999 and 48000+ bands were **all inside it**. ⚠️ **THIS IS ALSO THE MECHANISM BEHIND THE LONG-CARRIED "driver-owned receiver port race":** all three recorded ports — **35097, 35323, 42039** — are inside the ephemeral range, while the static fixture band (10000-19172) and the reserved bands (20000-31007, 11000-14999) are all **BELOW 32768**.

⇒ **BAND YOUR PROBE PORTS BELOW 32768.** `21000-24999` is clear of both the ephemeral range and every reserved band. **Check availability with `ss -tan` (ALL states), never `ss -ltn`** — the listener-only form reads a fail-unsafe "free". **A `bind: address already in use` in a 4xxxx band is NOT evidence a sibling session is squatting.**

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (ugrep under the hood). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Reproduced at THIRTY-THREE separate tips.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **THE METHOD BINDS; carry no standing discrepancy figure and no standing absolute.** ⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS; print FILE NAMES, never a count.** ⚠️ **THE HARNESS `grep` STRIPS A LEADING `./` FROM PATHS.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files; use `-v --no-index`, and `git status` is the discriminating check). ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **`grep -c` ON ZERO MATCHES IS TOOL-SPECIFIC** (only `git grep -c` prints nothing; all exit 1 — capture the value, never chain on the exit code). ⚠️ **`grep -c` COUNTS *LINES*, NOT *OCCURRENCES*** — this bit the SPEC's `H2RoundTrip` figure (9 lines vs **7** calls) and the phantom-symbol gate (1 comment line vs **0** definitions). ⚠️ **`\t` IN GNU ERE IS A LITERAL `t` — use `-P`.** ⚠️ **A `git grep` PATHSPEC ENDING IN `/` READS A FAIL-UNSAFE ZERO** — use the shell-glob form. ⚠️ **AND `STATE.md` QUOTES ITS OWN MATCHERS:** an unanchored `grep -c 'next-skill:'` reads more than 1; the anchored `^- \*\*next-skill:\*\*` reads 1.

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 90 (h2-host-authority-normalization)' --format='%h %s'   # 3 at this tip: BRAINSTORM, SPEC, PLAN
git log --grep '^phase 89 (h2-decode-filter-mutations)' --format='%h %s'        # 4 — a CLOSED phase, use as a FIRING CONTROL
```
⚠️ **The count comments are LIVE FIGURES — VERIFY before relying.** Take the NEWEST line (`| head -1`). A fabricated slug must read **0**.

Execution style: **subagent-driven per `feedback_execution_style`**. Subagents commit LOCALLY on the ONE stage branch with EXPLICIT PATHSPECS; the controller squashes (`feedback_subagents_no_push`, `feedback_push_to_origin`). **Pure MEASUREMENT agents should commit NOTHING and prove their tree clean** — that worked cleanly at the phase-90 SPEC and PLAN across six agents. Parallel subagents need **PRIVATE scratch** and tear down docker **BY NAME**. ⚠️ **BAND PRIVATE PORTS BELOW 32768.** ⚠️ **ONE WORKTREE PER AGENT preferred; explicit-path staging if shared; never an unscoped `git restore`.** Per-task `gofmt` + `golangci-lint` on touched packages (⚠️ **misspell runs in locale US — sweep British spellings in `.go` comments BEFORE the lint gate**). ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch.** ⚠️ **AND VERIFY WHAT A SUBAGENT REPORTS** — the phase-90 PLAN had to refute one of its own agents at the pinned source (`feedback_brief_citations_not_evidence` applies to subagent reports too).

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root — **observed repeatedly at every phase-88, -89 and -90 stage, and the harness ANNOUNCES it**. Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) before any commit or gate run, **and after any multi-step edit VERIFY WHICH TREE RECEIVED IT** (the phase-90 PLAN did exactly this and confirmed the canonical root was untouched). ⚠️ **Shell VARIABLES do not survive between Bash calls** — re-export in every command.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — `git status --porcelain` AND `git diff --stat master` must show ONLY your intended edits. For a patched TRACKED file: `sha256sum` BEFORE patching, `git checkout -- <path>` after, `sha256sum -c` the capture — ⚠️ **and COMMIT FIRST, because `git checkout --` restores from HEAD and will wipe uncommitted work.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads the phase-90 IMPL.** §Project counts SELF-CONTRADICT §Current — do not anchor on them (frozen at phase 76).
2. `docs/envoy-go/phases/90-h2-host-authority-normalization/PLAN.md` — **§0 the five refutations · §1 the sentinel · §2 D-90-DUP and the missing `host` branch · §3 the fixture-constraint refutation · §5 the property-organised unit roster WITH REAL CODE · §6 the NINE tasks · §7 the differential recipe, arm roster and FOUR break arms · §8 the exact ROADMAP and BEHAVIOR_CONTRACT replacement text · §9 the cost and the THREE-symbol gate · §10 the gates, the TWO named flakes and the split-gate evaluation · §11 the SIX deferred follow-ons · §12 what was NOT measured.**
3. `docs/envoy-go/phases/90-h2-host-authority-normalization/SPEC.md` — ⚠️ **READ IT KNOWING FIVE OF ITS CLAIMS ARE REFUTED** (see above). §3 D-90-SCOPE, §5 the arm-C spade-work, §6 the instrument, §8.4 the backend table (CORRECT — an agent's refutation of it did not survive) still stand.
4. `docs/envoy-go/phases/90-h2-host-authority-normalization/PROGRESS.md` — the BRAINSTORM, SPEC and PLAN entries.
5. `docs/envoy-go/DECISIONS.md` **ADR-0312** (PROPOSED; **the IMPL completes it**) and **ADR-0311 §Consequences (v)/(x)** — this row's true provenance, stronger than `STATE.md:18`.
6. `docs/envoy-go/ROADMAP.md` row 90 at `:152` (⚠️ **its "ROUTE-MATCHING input" sentence is FALSE — the IMPL fixes it**), and the six sentinel windows at `:200 :206 :212 :222 :228 :236`.
7. `BOOTSTRAP_PROMPT.md` §4-§7 (repo ROOT) — the lifecycle contract and the six-gate posture. **State 3 -> implementation.**

## What the IMPL owes
**PLAN §6's NINE tasks, all of them**, TDD, **ONE atomic commit**, `PROGRESS.md` appended per task ·
**the RED census observed FIRST with its denominators asserted, plus the REACHABILITY control** ·
**the THREE-symbol gate** (`authoritySeen`, `hostField`, `hostSeen` — the SPEC's two-symbol table is incomplete) ·
**FOUR break arms on FOUR DISTINCT injection sites, with B3's expected DIFFERENTIAL SILENCE recorded as an observed result** ·
**the `ROADMAP.md` row-90 text correction and the `BEHAVIOR_CONTRACT.md:2034` RIDER** (text already fixed in PLAN §8) ·
**ADR-0312 completed IN PLACE** (guard **1 -> 0**, no `---`, `^---$` stays 216) **with the SIX deferred follow-ons as named rows** ·
**cost re-measured at the IMPL's OWN publishing commit** (the PLAN's `+36/−0` is valid only because that stage was docs-only) ·
**row 90 flipped `done`** (a row FLIP, not a row ADD — `want` STAYS **122** and `ROADMAP.md` stays 240 lines / 122 data rows) ·
run the sentinel and all four NCs and record ACTUAL output (⚠️ **NC-A returns to the ONE-line `row 62` form once the row flips, and check (1) goes SILENT — `stop` STILL MUST NOT BE CREATED, because check (2) prints SIX**) ·
`STATE.md` rolled IN PLACE (lifecycle-state **3 -> DONE**) with the oldest §Recent entry evicted by a **DIRECT DATE READ** (⚠️ **the tie shape does NOT carry — the phase-90 PLAN had a UNIQUE OLDEST and NO TIE; the SPEC had a TWO-way broken by LAST LIST POSITION; the phase-88 IMPL a FOUR-way**) and archived to `STATE_HISTORY.md` as **ONE INLINE LINE** so the strict guard stays at **163, DELTA 0** ·
`next-prompt.txt` rolled (`git add -f`) · ONE squashed commit, merged and pushed.
