RESUME a fresh session for envoy-go (router last updated 2026-06-26; the **phase 45.2 (operator engine) SPEC is DONE and LANDED** — `docs/envoy-go/phases/45-otlp-access-log/SPEC-45.2.md` authored direct-on-master [docs-only], modeled on `SPEC-45.1.md`, code-reviewer-APPROVED [3 minor accuracy fixes folded], committed `b4767c43`. The §11 D-OTLP-2-* operator live-probes were EXECUTED IN-SESSION against `envoyproxy/envoy:contrib-v1.37.2` per `reference_docker_probe_bridge_network` [a throwaway `cmd/otlpprobe` receiver — built, probed, removed]. **The next action is the phase 45.2 (operator engine) PLAN** — the FINAL leg of the by-concern 2-leg OTLP split; its IMPL FLIPS ROADMAP row 45 → `done`.). HEAD is at the **next-prompt.txt roll commit** atop the **phase-45.2 SPEC `b4767c43`**, which sits atop the prior next-prompt roll `ac21a13c` + the **phase-45.1 IMPL squash `07439d1f`** + the prior roll `1e3b0bc0` + the **phase-45.1 PLAN `48d6b189`** + the prior roll `9989a7a4` + the **phase-45.1 SPEC `35070bc3`**. Re-read the shas with `git log --oneline -12`, do NOT assume — any number of docs-only `next-prompt.txt:` commits may stack ATOP the SPEC. Counts at SPEC-done (the exit invariant — re-verify, do NOT assume): stat surface **1191** (H2 cluster; non-H2 **1187**) / fixtures **86** (`ls -d test/fixtures/[0-9][0-9][0-9][0-9]* | wc -l` == 86; tail `0084-otlp-access-log`) / fuzzers **45** (`grep -rh '^func Fuzz' --include='*.go' . | wc -l` == 45) / BackendKind tail **38** (UNCHANGED — the OTLP receiver is driver-owned) / DECISIONS tail **ADR-0258**, next-free **ADR-0259**. ZERO new `internal/` packages, ZERO new go.mod modules anticipated at the 45.2 IMPL.

**ROW 44 (`grpc-access-log`) is `done`**; **ROW 45 (`otlp-access-log`) is `in-progress`** (the 45.1 core leg LANDED ⇒ leg 45.1 `done`; the 45.2 operator-engine SPEC LANDED ⇒ 45.2 lifecycle-state 2; but **row 45 STAYS `in-progress`** — it flips `done` at the 45.2 IMPL, the FINAL leg, per per-leg ADR-0106 + `reference_roadmap_split_phase_row_done`). The **Observability FAMILY STAYS OPEN** (OTLP access log [active — 45.2 PLAN+IMPL remain] / OTel-Zipkin tracing / stats sinks / tap filter remain rows). **The active action is the phase 45.2 (operator engine) PLAN** — the §12 D-OTLP-2-* PLAN questions in `SPEC-45.2.md` (esp. **D-OTLP-2-COMPILE-SITE** [where the operator templates compile + strict-reject: bootstrap-imports-accesslog at parse RECOMMENDED — exporting the compiled template types — vs. raw-proto-threaded + sink-compiles] · **D-OTLP-2-SUPPORTED-SET-FINAL** [the curated operator roster + per-operator missing-value/`-` disposition] · **D-OTLP-2-FUZZER** [new operator-engine fuzzer vs. extend the existing one; 45 → 46] · **D-OTLP-2-RECEIVER-ASSERT** · **D-OTLP-2-FIXTURE-SHAPE** [the `0085` templated config] · **D-OTLP-2-SPLIT-FINAL** [the ADR-0045 re-check with real LoC]) + the ~9–11-task TDD spine authored into `docs/envoy-go/phases/45-otlp-access-log/PLAN-45.2.md` (modeled on `PLAN-45.1.md`). This is a PLAN-authoring stage (docs-only; direct-on-master is fine per the 45.1 PLAN precedent). THEN roll this router to the 45.2 IMPL (subagent-driven in a fresh worktree). ANCHORS **ADR-0259**.

First confirm state via `git log --oneline -12` (expect HEAD at the next-prompt.txt roll over the phase-45.2 SPEC `b4767c43` over the prior roll `ac21a13c` over the phase-45.1 IMPL squash `07439d1f`; counts as above; re-read the shas, do NOT assume).

## NEXT STEP — the phase-45.2 (operator engine) PLAN
This is a PLAN-authoring stage (docs-only; direct-on-master is fine for a docs-only PLAN, per the 45.1 SPEC/PLAN precedent — no worktree needed for pure docs). Invoke `superpowers:writing-plans`. The deliverable is `docs/envoy-go/phases/45-otlp-access-log/PLAN-45.2.md` modeled on `PLAN-45.1.md` (the house TDD-task-spine shape). The PLAN must:
1. RESOLVE the §12 D-OTLP-2-* PLAN questions (above). The pivotal one is **D-OTLP-2-COMPILE-SITE** — RECOMMENDED in the SPEC: bootstrap calls an `internal/accesslog` compile helper at parse (so the unknown-operator + bad-value-type STRICT-REJECT is a clean `bootstrap.Load` boot error; ONE operator implementation) which forces the compiled template types to be EXPORTED (`OTLPTemplate`/`OTLPValueTemplate`) so an `OTLPConfig` field can name them (and to dodge the golangci-lint exported-returns-unexported nit, `feedback_pertask_gofmt_lint`). Confirm no import cycle (`internal/accesslog` imports `stats` only — it reaches the gRPC client via the `otlpClient` interface seam at `otlpsink.go:19`, NOT a `grpcclient` import; `accesslog` does NOT import `bootstrap`).
2. Decompose the ~9–11-task TDD spine: T1 PROGRESS+baselines+ADR-0045 re-check (D-OTLP-2-SPLIT-FINAL) · the operator engine `otlpformat.go` (registry + `CompileOTLPTemplate` `%…%` scanner [`%%` literal] + `CompileOTLPValue` AnyValue-tree walker [string/kvlist/array only — int/bool/double/bytes type-reject] + per-record `Eval`) [TDD, table-driven incl. the supported-set + unknown-operator reject + missing-value `-`] · the operator-engine fuzzer (D-OTLP-2-FUZZER) · the config-parse lift (remove the body/attributes/resource_attributes reject arms at `bootstrap.go:480-488`; compile-at-boot + the value-type reject; `OTLPConfig` field extensions) [TDD] · the `buildLogRecord`/`buildResource` extensions (body/attributes substitution + the resource_attributes append SURVIVING `disable_builtin_labels` — AMEND-OPS-5) [TDD table-driven incl. nested kvlist/array] · the sink template threading + `NewOTLPAccessLogSink` signature [TDD, full-package `-race`] · the main wiring · the `otlplogs` receiver assert extension (if any — the raw `Records()`+`ResourceAttributes()` likely suffice) · the `0085-otlp-access-log-operators` differential (cross-side EXACT on the DETERMINISTIC operators only — query-less path; exclude `%START_TIME%`/`%DURATION%`/`%UPSTREAM_HOST%`) + the `disable_builtin_labels`/unsupported-operator/bad-value-type subject unit tests · deliberate breaks (`-count=1`, `reference_differential_break_protocol_count1`) + 20/20 flake + full-package `-race` (`reference_full_suite_race_after_background_mutator`) · full 87-dir + six-gate + ADR-0259 body + BEHAVIOR_CONTRACT + STATE/ROADMAP (row 45 → `done`) + the fuzzer-count reconcile (`reference_fuzzer_count_docs_drift`) · completion bundle (row 45 FLIPS `done` — 45.2 is the FINAL leg; the Observability family STAYS OPEN).
3. Run the plan-document-reviewer loop (writing-plans discipline). Anticipated 45.2-IMPL counts: stat **1191 UNCHANGED** / fixtures **87** (`0085`) / fuzzers **46** / BackendKind **38** / DECISIONS **ADR-0259**; ZERO new packages + ZERO new go.mod modules.
THEN roll this router to the 45.2 IMPL (subagent-driven per `feedback_execution_style` in a fresh worktree per `feedback_git_worktrees`).

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** This stage is the phase-45.2 PLAN (docs-only). When 45.2 reaches IMPL it is subagent-driven in a fresh worktree per `feedback_execution_style` · `feedback_git_worktrees` · `feedback_subagents_no_push` (subagents commit locally; controller squashes + pushes at stage-close) · `feedback_subagent_worktree_detach` + `feedback_subagent_worktree_path_targeting` · `feedback_pertask_gofmt_lint` · `feedback_push_to_origin`.
- **Process anchors:** ADR-0106 (per-leg roadmap rows; row 45 flips `done` only at the 45.2 IMPL) · ADR-0045 (split-gate) · ADR-0041/0080 (silent-ignore vs strict-reject for access-log type-URLs) · ADR-0016 (proto blank-import registry) · ADR-0255/0256/0257 (44.x as-built) · **ADR-0258** (45.1 OTLP core, LANDED) · next-free **ADR-0259** (45.2 operator engine — §Context drafted in `SPEC-45.2.md` §13; body lands at the 45.2 IMPL).
- **45.1 as-built seams the 45.2 PLAN builds on:** `internal/bootstrap/bootstrap.go` (`parseOpenTelemetryAccessLog` `:475` — the body/attributes/resource_attributes reject arms `:480-488` to LIFT; `OTLPConfig` `:217`; `formatters` reject `:489` STAYS) · `internal/accesslog/otlpmapping.go` (`buildLogRecord` `:20` / `buildResource` `:28` / `buildExportRequest` `:46` — the operator engine extends these) · `internal/accesslog/otlpsink.go` (`:42` the sink; `proto.Size(lr)` `:190` size-trigger; the `otlpClient` seam `:19`) · `internal/accesslog/format.go` (`Default()` — the operator SEMANTICS source; NOT a reusable engine [hard-coded]) · `internal/accesslog/accesslog.go` (`Record` `:29` — the 10 scalar fields the curated operator set maps to) · `internal/filter/hcm/accesslog_emit.go` (`:26` H1 `r.URL.Path` strips query; `:52` H2 `req.Path` keeps it — AMEND-OPS-6) · `test/helpers/otlplogs/otlplogs.go` (`Records()`+`ResourceAttributes()` — already expose body/attributes; extend if needed) · `test/fixtures/0084-otlp-access-log/` (the differential precedent to copy for `0085`).

## Read first (in order)
1. THIS FILE — the cold-start router (the 45.2 SPEC is DONE + committed; the next action is the 45.2 operator-engine PLAN — docs-only).
2. `docs/envoy-go/phases/45-otlp-access-log/SPEC-45.2.md` — the chartered operator-engine leg (the §11 D-OTLP-2-* live-probe PINS [AMEND-OPS-1..6] + the §3 design + the §12 D-OTLP-2-* PLAN questions to RESOLVE + the §13 ADR-0259 §Context draft).
3. `docs/envoy-go/phases/45-otlp-access-log/SPEC-45.1.md` + `PLAN-45.1.md` + `PROGRESS-45.1.md` — the core-leg as-built (the seams the 45.2 PLAN extends + the house spine shape to mirror).
4. `docs/envoy-go/DECISIONS.md` — **ADR-0258** (the 45.1 as-built, LANDED) + next-free **ADR-0259**.
5. `docs/envoy-go/STATE.md` — the active-phase header (`phase 45.2 (otlp-access-log operator engine) SPEC done`) + the recorded NEXT (the 45.2 PLAN).
6. `docs/envoy-go/ROADMAP.md` — row 45 (`otlp-access-log`, `in-progress`, 45.2-SPEC-annotated) + the `### Observability family` heading.
7. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the `### Access log — OpenTelemetry (OTLP) access-log sink` subsection (the 45.1 contract 45.2 extends) + the stat-surface block (1191).
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `reference_streaming_sink_differential_framing`, `reference_differential_grpc_receiver_driver_owned`, `reference_docker_probe_bridge_network`, `reference_strict_reject_sibling_typeurl_gap`, `reference_fuzzer_count_docs_drift`, `reference_full_suite_race_after_background_mutator`, `feedback_pertask_gofmt_lint`, `feedback_execution_style`, `feedback_git_worktrees`).
