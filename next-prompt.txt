=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-CHECKED 2026-07-14 (after the phase-64 SPEC landed + row 64 STAYS in-progress, run MECHANICALLY) — it does NOT fire. Do NOT create `stop`.** State:
- (1) prints `NOT DONE: row 64` — row 64 `tracing-max-path-tag-length` STAYS `in-progress` (a row flips `done` only at its IMPL six-gate per ADR-0106; a SPEC is docs-only). This ALONE blocks `stop` (a mid-lifecycle phase is open).
- (2) prints THREE live "candidates:" sentences — HTTP/3, xDS, and Observability ⇒ three families STAY OPEN. The Observability sentence is UNCHANGED by this row (`max_path_tag_length` is a §8-tier candidate, NOT in the live `custom_tags (metadata)` + OTLP-metrics + `spawn_upstream_span`/`http_service`/force-trace sentence).
- (3) prints `NEVER OPENED: gRPC`, `Runtime`, `WASM` ⇒ THREE families never opened. Operational-tooling also STAYS OPEN.

Checks (1), (2) and (3) print ⇒ the sentinel does NOT fire.

**STATUS AS OF 2026-07-14: the phase-64 SPEC LANDED and ROADMAP row 64 STAYS `in-progress`.** The SPEC (`docs/envoy-go/phases/64-tracing-max-path-tag-length/SPEC.md`, 14 sections) DISPOSED every D-MPTL-* open question via LIVE probes against `envoyproxy/envoy:contrib-v1.37.2` (fresh container per arm; OTLP span capture via `test/helpers/otlptrace`). **EVERY BRAINSTORM anticipation HELD — no ADR-0044 flip this row** (unlike phases 62/63 whose probes overturned a decision). The load-bearing pins: **D-MPTL-REFTRUNC** the reference truncates `http.url` at the knob (arm 0 — provability CONFIRMED, no re-scope); **D-MPTL-TARGET** only the `:path` (path+query) is cut, the `scheme://host` prefix survives (arm 0); **D-MPTL-DEFAULT** an ABSENT field caps at **256** (arm 1 — envoy-go emits UNTRUNCATED today ⇒ a latent > 256 divergence this row closes); **D-MPTL-ZERO** an explicit `0` → empty path `scheme://host` (arm 2, preserved not unlimited); **D-MPTL-QUERY** the query is included, a cut lands inside it (arm 3); **D-MPTL-TRUNCUNIT** BYTES (ASCII fixture sidesteps); **D-MPTL-REJECT** the field is a `UInt32Value` with NO PGV numeric constraint ⇒ NO new structural reject. **D-MPTL-TRUNC-LOCATION = Option A** (a shared `tracing.BuildHTTPURL(scheme, host, pathAndQuery, maxPathTagLen)` helper, GREP-collision-checked, called at the THREE `accesslog_emit.go` URL-build sites H1 `:40` / H2 `:93` / H3 `:162`). **D-MPTL-FIXTURE** ONE new OTLP fixture `0107` (a small explicit cap + a long ASCII path, cross-side `http.url` VALUE-equality — deterministic, no harness surgery); **D-MPTL-DEFAULT-PROOF** a UNIT test (LEAN — ADR-0045 escape-valve UNCONSUMED). ADR-0285 §Context drafted (SPEC §13; §Decision/§Consequences at the IMPL). Row 64 STAYS `in-progress`; STATE flipped; router rolled to the PLAN.

==============================================

## What THIS session does (the phase-64 PLAN)

Perform the PLAN stage (lifecycle-state 2 → 3) for phase 64 (`tracing-max-path-tag-length`). This is a DOCS-ONLY stage in a FRESH worktree off master (`feedback_git_worktrees`); NO production `.go` changes. Author `docs/envoy-go/phases/64-tracing-max-path-tag-length/PLAN.md` (+ scaffold `PROGRESS.md`) as a mechanical TDD decomposition (`superpowers:test-driven-development`) of SPEC §10 — a SINGLE FLAT ROW (ADR-0045 escape-valve UNCONSUMED; ~8–11 tasks). No empirical question remains (the SPEC disposed every D-MPTL-* arm); the PLAN is a task spine with each `file:line` RE-DERIVED against the current master tip (`feedback_brief_citations_not_evidence` — the SPEC's citations are NOT evidence). Land it (commit + push per `feedback_push_to_origin`) and ROLL THIS FILE to the IMPL stage.

**The PLAN must decompose (from SPEC §10 / §12 — RE-DERIVE every line against the master tip):**
- **T1 — config model + parse arm.** Add `TracingConfig.MaxPathTagLength uint32` (`config.go:25-40`); replace the `config.go:112-114` reject with the resolve arm (`maxPathTagLen := uint32(256); if m := t.GetMaxPathTagLength(); m != nil { maxPathTagLen = m.GetValue() }`) + set `cfg.MaxPathTagLength = maxPathTagLen` post-dispatch alongside `cfg.CustomTags` at `config.go:154` (provider-neutral — parseOTel/parseZipkin signatures UNCHANGED). `config_test.go`: MOVE the `max_path_tag_length` row (`:337-343`) OUT of the reject table into an ACCEPT (`cfg.MaxPathTagLength == 128`); ADD ABSENT→256 + explicit-0 tests. `-count=1` liveness break per new assertion.
- **T2 — the `BuildHTTPURL` helper + unit test.** Add `func BuildHTTPURL(scheme, host, pathAndQuery string, maxPathTagLen uint32) string` (byte-truncate `pathAndQuery` FIRST via `len(pathAndQuery) > int(maxPathTagLen)` then prepend `scheme://host`) in `span.go` or a new `url.go`. Unit matrix (`Errorf`-per-property, `reference_fatalf_makes_assertions_unreachable`): under-cap unchanged / over-cap truncated to N / explicit-0 → `scheme://host` (D-MPTL-ZERO) / query-cut (D-MPTL-QUERY) / > 256 path with cap 256 (D-MPTL-DEFAULT-PROOF) / byte boundary (ASCII, D-MPTL-TRUNCUNIT).
- **T3 — call-site rewiring.** The THREE `accesslog_emit.go` sites (H1 `:40` / H2 `:93` / H3 `:162`) call `tracing.BuildHTTPURL(scheme, host, pathAndQuery, f.tracingConfig.MaxPathTagLength)`; an HCM span test asserts a truncated `http.url` reaches the span through the three sites (`f.tracingConfig` is a non-nil invariant when the span block runs — already dereferenced for `.CustomTags`; `MaxPathTagLength` always set by `NewConfig`).
- **T4 — Zipkin encoder unit test** — a truncated `http.url` surfaces in the Zipkin `tags` map (provider-neutral truncation; phase-59/62/63 precedent).
- **T5 — new OTLP fixture `0107-tracing-max-path-tag-length`** — clone `0106` (drop custom_tags), `max_path_tag_length: {value: N}`, a driver driving a long ASCII-path GET, cross-side `http.url` VALUE-equality on the truncated form; register in the runner; break-proven live (`reference_differential_break_protocol_count1` `-count=1`; `reference_differential_run_selector` — `-run 'TestDifferential/0107-tracing-max-path-tag-length'`, NEVER bare). H1/H2-over-TCP so `reference_differential_http_expectations_tcp_only` does not bite (assert in the `otlptrace` receiver).
- **T6 — `FuzzHCMConfigParse` seed** — a `max_path_tag_length` seed (incl. explicit 0); reconcile fuzzers = **55** before AND after (`reference_fuzzer_count_docs_drift`).
- **T7 — BEHAVIOR_CONTRACT** — flip `max_path_tag_length` REJECT → CONSUMED (truncates the `http.url` `:path` to N bytes, default 256, explicit 0 = empty path, both exporters); the sibling rejects STAY.
- **T8 — verify** — the six-gate (gofmt / golangci-lint / go vet / build / `go mod tidy -diff` / full-package `-race` on `internal/tracing` + `internal/filter/hcm`) + the full **109-dir** differential (byte-stable except `0107`).
- **T9 — ADR-0285 §Decision/§Consequences + STATE + ROADMAP (row 64 `done` per ADR-0106; the LIVE Observability deferred sentence is UNCHANGED — re-run the check-(2) grep only to CONFIRM EXACTLY ONE live match, `reference_sentinel_deferred_sentence_live_vs_historical`) + PROGRESS close + router roll.**

(T1–T3 are the TDD core; the PLAN may split/merge. Total ~8–11, single flat row. The load-bearing ordering insight: T1 (parse) + T2 (helper) are independent additive pieces; T3 wires the helper into the call sites — the build never breaks because the helper is a no-op for short paths and `NewConfig` always sets the cap.)

**Honor throughout (the discipline, unchanged):**
- **Lifecycle = one stage per session.** This session: the PLAN ONLY. After it lands: roll to the IMPL.
- **Process anchors:** ADR-0044 (ADR §Decision/§Consequences land at the IMPL; the SPEC drafted only §Context) · ADR-0045 (the split escape-valve, only if the slice exceeds ~15 tasks — anticipated NOT consumed) · ADR-0106 (a row flips `done` at its IMPL) · `reference_roadmap_split_phase_row_done`.
- **`next-prompt.txt` IS TRACKED** (`reference_next_prompt_tracked_despite_gitignore`) — edit it inside the stage worktree and fold into the stage commit; locate commits by SUBJECT (`git log --grep`), never by position.
- **Worktree discipline** (`feedback_git_worktrees`, `feedback_subagent_worktree_detach`, `feedback_subagent_worktree_path_targeting`) — if subagent-driven, `feedback_execution_style` + `feedback_subagents_no_push`.
- **Tracing traps to re-read** (`reference_tracing_upstream_cluster_framework_gap` — `http.url` is a POPULATED built-in, NOT one of the 4 EMPTY framework-gap attributes, so the truncation acts on a real value · `reference_tracing_custom_tag_override_builtin` — a custom_tag keyed `http.url` still upsert-overrides the truncated built-in).

**ADR reservation ledger (provisional; each finalized at its IMPL in landing order):** ADR-0277 = phase-59 tracing-custom-tags-literal (LANDED) · ADR-0283 = phase-62 tracing-custom-tags-request-header (LANDED) · ADR-0284 = phase-63 tracing-custom-tags-environment (LANDED) · **ADR-0285** = phase-64 tracing-max-path-tag-length (RESERVED; §Context at the SPEC — DONE; §Decision/§Consequences at the IMPL) · next-free unreserved after: **ADR-0286**.

**Current counts (VERIFIED at the phase-64 SPEC against master tip `7d724423`; a SPEC is DOCS-ONLY ⇒ all UNCHANGED — RE-VERIFY at session start, `git fetch` first):** fixtures **108** (`ls -d test/fixtures/[0-9]*/ | wc -l`; tail `0106-tracing-custom-tags-environment`) · fuzzers **55** · DECISIONS tail **ADR-0284** (next-free **ADR-0285**) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** · go.mod modules **2** (`quic-go` direct + `qpack` indirect). A PLAN is DOCS-ONLY ⇒ ALL counts UNCHANGED at that stage.

**Locating the phase-64 SPEC stage squash** (a router cannot name its own sha — identify by SUBJECT, never by position):
```sh
git log --oneline --grep='^phase 64 (tracing-max-path-tag-length) SPEC' -1
```

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-64 PLAN is next; row 64 is `in-progress`).
2. `docs/envoy-go/phases/64-tracing-max-path-tag-length/SPEC.md` — the DISPOSED D-MPTL-* roster (§1, §11 live probes), the change shape (§3 — the parse arm, the `BuildHTTPURL` helper, the three call-site wirings), the test plan (§10), the edit-site roster (§12 — RE-DERIVE every line), ADR-0285 §Context (§13).
3. `docs/envoy-go/phases/64-tracing-max-path-tag-length/BRAINSTORM.md` — the subject + rejected alternatives (§2.1), the framework survey (§3).
4. `docs/envoy-go/STATE.md` — the active-phase header (phase 64 SPEC done; NEXT = the phase-64 PLAN).
5. `docs/envoy-go/ROADMAP.md` — row 64 (`in-progress`) + the Observability family prose; the live deferred sentences (UNCHANGED by this row).
6. `docs/envoy-go/phases/63-tracing-custom-tags-environment/PLAN.md` (+ PROGRESS.md) — the PLAN-stage FORMAT precedent (a TDD task spine, PROGRESS scaffold, single-flat-row).
7. `internal/tracing/config.go` (the `NewConfig` reject roster + `TracingConfig`), `internal/tracing/span.go` (`BuildServerSpan`/`http.url`/`SpanInputs`), `internal/filter/hcm/accesslog_emit.go` (the three URL-construction sites `:40`/`:93`/`:162`), `internal/tracing/config_test.go` (the reject table `:337-343`) — RE-DERIVE every §12 edit-site file:line against the current master tip.
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the tracing `reference_*` traps + the differential-harness traps (esp. `reference_differential_fixture_dispatch_constraint`, `reference_differential_break_protocol_count1`, `reference_differential_run_selector`, `reference_fatalf_makes_assertions_unreachable`).
