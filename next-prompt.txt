=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **PHASE 94 (`tls-connection-error-stat`) IS OPEN AT LIFECYCLE-STATE 1. ROW 94 READS `in-progress` AT `ROADMAP.md:156`.** The phase directory holds `BRAINSTORM.md` (412) and nothing else. ⇒ **THIS SESSION RUNS THE SPEC (state 1 -> 2). DO NOT SELF-PICK; DO NOT OPEN A NEW SUBJECT.**

⚠️⚠️ **CHECK (1) IS NON-SILENT AGAIN — IT PRINTS `NOT DONE: row 94`, WHICH IS CORRECT AND EXPECTED** while row 94 is open. Check (3) is SILENT. Check (2) prints SIX.

⚠️⚠️ **AND THE INHERITED "THE MARGIN IS ONE" FRAMING IS NOW FALSE — DO NOT CARRY IT FORWARD.** The phase-93 file said *"CHECK (2) IS NOW THE SOLE THING BLOCKING TERMINATION … THE MARGIN IS ONE."* That was true at that tip and is **NOT true here**: the sentinel is now blocked **TWO INDEPENDENT WAYS** — check (1) by an OPEN ROW (work literally mid-flight) and check (2) by six lines. **Closing row 94 removes only the FIRST.** ⚠️ **The margin returns to ONE the moment row 94 flips `done`, so the phase-94 IMPL must re-read this paragraph rather than inherit either state.** ⚠️ **DO NOT "TIDY" A DEFERRED-CANDIDATE LINE — DELETING THE LAST ONE ENDS THE PROJECT.** ⚠️ **MEASURE ALL THREE YOURSELF BEFORE DOING ANYTHING ELSE; do not take this file's word for it.**

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE. This form parses FIELDS instead of matching a pattern, and
#     asserts the row DENOMINATOR so a deleted row is itself an error.
#     ⚠️ want IS 126 — row 94 was ADDED (not flipped) at the phase-94 BRAINSTORM,
#     so the denominator MOVED 125 -> 126 and ROADMAP.md moved 243 -> 244.
awk -F'|' -v want=126 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **CHECK (1) NOW PRINTS ONE LINE (`NOT DONE: row 94`) BY DESIGN.** Its NCs still matter, because a check that prints the RIGHT line for the WRONG reason is still broken. Run BOTH doctoring NCs EVERY SESSION:
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md.
```
⚠️ **NC-A NOW READS EXACTLY *TWO* LINES** (`NOT DONE: row 62` and `NOT DONE: row 94`) — it read ONE at the phase-93 close and **GREW at the phase-94 BRAINSTORM's own row ADD.** ⚠️ **NC-B ALSO READS TWO** (`NOT DONE: row 94` and `GATE FAIL: examined 126 data rows, expected 125`). **BOTH SHAPES CHANGED AGAIN, IN THE OPPOSITE DIRECTION TO LAST TIME: TWO -> ONE at the phase-93 flip, ONE -> TWO at the phase-94 add. NEVER INHERIT A NC SHAPE ACROSS A ROW ADD OR FLIP; RE-MEASURE. THEY WILL COLLAPSE BACK TO ONE WHEN ROW 94 FLIPS `done`.**
```sh
# the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```
Also NC the DENOMINATOR (`want=125` on the real file).

⚠️ **AND WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -c '-family row'` fails (GNU: `grep: amily row: No such file or directory`) and the surrounding arithmetic prints `0`, **which reads exactly like "no change."** **Always pass `--` before the pattern**; with `--` occurrences read **96** and LINES read **68** (both **+1** at this stage, from row 94's own `Observability-family row`). **`grep -c` counts LINES, NOT OCCURRENCES.** ⚠️ **AND `grep -c` PRINTS `0` *AND* EXITS 1, so `$(cmd || echo 0)` EMITS TWO ZEROS** — capture with `v=$(cmd || true)`.

⚠️ **THE MALFORMED-ROW FIGURE RECONCILES ONLY UNDER AN ESCAPE-AWARE FIELD COUNT.** Malformed rows are ids **57** (`NF=9`) and **69** (`NF=10`). ⚠️ **A NAIVE `awk -F'|'` `NF != 8` FORM READS SEVENTEEN.** **Strip `\|` BEFORE splitting.** The two forms **DISAGREE on row 57** (naive reads `NF=13`) but **AGREE on row 69** — RE-VERIFIED at this tip — so **any gate must state WHICH FORM it uses.** Row 94 is **NF=8 under BOTH**.

⚠️ **AND DO NOT SUBSTITUTE A "REASONABLE EQUIVALENT" FOR CHECK (1)'s AWK.** A measurement agent once tried `NF>=8 && $2 ~ /^ *[0-9]+ *$/`: it printed **92**, not the true denominator. **Use the form above verbatim.**

**SENTINEL RUN MECHANICALLY AT THE PHASE-94 BRAINSTORM, BOTH BEFORE AND AFTER THE ROW-94 INSERTION.** ACTUAL output, recorded not predicted.
**BEFORE the insertion (`want=125`):** (1) **SILENT** · (2) **SIX** at `:203 :209 :215 :225 :231 :239` · (3) SILENT; NC-A **ONE**, NC-B **ONE**, NC-C fired (WASM control **2**), NC-D **95 / 67**.
**AFTER the insertion (`want=126`):** **(1) `NOT DONE: row 94`, ONE line** · (2) **STILL SIX**, now at **`:204 :210 :216 :226 :232 :240`**, and **all six per-line md5 BYTE-IDENTICAL** (`10d7807bf02d 4a92f7e62fc6 2a7eb298b9fd 4ad940205410 b2680e6f4fbf 6caa1c3ce0e7`) — **that digest match is the proof no deferred-candidate line was tidied** · (3) SILENT; **NC-A TWO**, **NC-B TWO**, NC-C fired (WASM control **2**), NC-D **96 / 68**.
⇒ **THE SENTINEL DOES NOT FIRE. `stop` WAS EVALUATED AND DELIBERATELY NOT CREATED** (verified absent at the git root and in the stage worktree).
⚠️ **RE-CONFIRMED A SECOND TIME AT THE PUSHED TIP `b75b0e2d`**, after the merge and push, per `reference_cost_figure_measured_at_publishing_commit`: all three checks and all four NCs re-run with every result BYTE-IDENTICAL, **and separately EVERY figure in this file was re-derived against the live tree at that same tip** — `ROADMAP.md` 244/126, row 94 at `:156` reading `in-progress`, `BRAINSTORM.md` 412, phase dirs 135, `STATE.md` 64, `STATE_HISTORY.md` 540 (163 / 57 / 220), `DECISIONS.md` 18804 tail ADR-0315, `BEHAVIOR_CONTRACT.md` 5987, fixtures 121, fuzzers 56/48, `-family row` 96/68, and `0120` + ports `10126-10129` all still FREE. ⚠️ **THAT AUDIT DOES NOT CARRY FORWARD EITHER — RE-DERIVE AT YOUR OWN TIP.**

⚠️ **AND THE CHECK-(2) POSITIVE CONTROL WAS RE-RUN AND PROVES SOMETHING THE PICK MENU DOES NOT SAY.** Doctoring both candidate PHRASES out of a scratch copy drives check (2) to **0**, so it is not a stuck gate. ⚠️ **BUT THE CONTROL HAD TO REMOVE THE *PHRASE*.** Removing a *clause* from a window sentence leaves the phrase, the line and the count untouched. ⇒ **NARROWING A WINDOW CANNOT MOVE CHECK (2). ONLY DELETING THE LINE CAN, AND DELETING IT IS FORBIDDEN.** This is the mechanical confirmation of the phase-77 "decisive asymmetry", and it means **`next-prompt.txt`'s former instruction to "prefer a candidate that EMPTIES a check-(2) window" WAS UNACHIEVABLE AS WRITTEN** — phase 77 measured the SHORTEST window at ~6-8 rows and ~60-70 tasks to empty. **The instruction is corrected to "narrows" below. Charter narrowing rows AS narrowing; never as sentinel progress.**

⚠️ **THIS RUN DOES NOT CARRY FORWARD — run all three checks, all four NCs *and* the check-(2) positive control YOURSELF, at YOUR OWN tip.**

- ⚠️ **LIVE FIGURES RE-DERIVED AT THIS TIP:** `ROADMAP.md` **244** lines / **126** data rows / tail row **94** (`in-progress`) · `DECISIONS.md` **18804** · `^---$` **216** · `^## ADR-` **314** · bare `^## ` **322** (314 + 8 `## Amendment`) · tail **ADR-0315** · next-free **ADR-0316** (`^## ADR-0316` reads **0**) · house `PROPOSED` **0**, ADR-0231 decoy **1** at `:14866` · `BEHAVIOR_CONTRACT.md` **5987** · `STATE.md` **64** · `STATE_HISTORY.md` **540**, strict **163 (DELTA 0 across this stage)** / parenthetical **57** / loose **220** (**163 + 57 = 220 exactly**) · phase dirs **135** · fixtures **121** (tail `0119`, **`0120` FREE and CLAIMED BY THIS ROW at its IMPL**) · fuzzers **56 targets / 48 FILES** · `go.mod` **67 REQUIRE ENTRIES** (**18 direct + 49 indirect**) — ⚠️ **67 IS THE REQUIRE COUNT, NOT A LINE COUNT, and the plausible `grep -cE '^\s+[a-z0-9./-]+ v[0-9]'` form reads 62** · BackendKind tail **38** (39 declared, values 0-38) · `-family row` **96 / 68**.
- ⚠️ **THE STAT SURFACE IS QUOTED AS A DELTA AND NEVER AS AN ABSOLUTE.** THREE different absolutes are live in this tree at one tip. Per `reference_a_drift_correction_is_itself_a_claim`, on a contested count: **NO NUMBER.** ⚠️ **AND THE REFUTED FIGURE MUST NOT BE SPELLED, IN EITHER ITS BARE OR ITS ARROW FORM.**
- ⚠️ do NOT "fix" the six in check (2) and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases, and **each new FAMILY opens a new window**, so the count has only ever grown. ⚠️ **Cite ~40 only as "sentence-visible", never as the inventory** — and **no gate may rest on a ` + ` split, in any form. THAT RULE IS NOW EMPIRICALLY CONFIRMED: a ` + ` split is WRONG ON 3 OF THE 6 WINDOWS and reports `:240` as having ONE item when it has THREE.**

==============================================

## STATUS AS OF 2026-09-01: PHASE 94 (`tls-connection-error-stat`) **OPEN, lifecycle-state 1**. **THIS SESSION RUNS THE SPEC.**

**The subject.** Give `outcomeOther` — the fourth downstream-TLS handshake-outcome bucket, which today increments nothing — a **predicate-gated `Inc()`** landing `listener.<normalized-addr>.ssl.connection_error`, the FIFTH listener-scope TLS counter. This closes the NAMED DEPARTURE at `BEHAVIOR_CONTRACT.md:1971` and completes the taxonomy phase 74 opened and phase 75 extended. **The TWENTY-FIFTH Observability-family row**; the family STAYS OPEN.

### ⚠️ THE MEASUREMENT THAT UNBLOCKED THIS ROW — DO NOT RE-DERIVE IT, BUT DO RE-CONFIRM THE PIN

Twelve arms on `envoyproxy/envoy:contrib-v1.37.2`, digest `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8`, **verified against `docs/envoy-go/ENVOY_TARGET.md` before any arm was trusted**. Two listeners in ONE process, scraped together, with a positive control, a discriminating negative control, and a **full two-listener set reconciliation** (both listeners reconcile exactly; no stray increments).

| arm | reference |
|---|---|
| TLS-1.0-only client, plaintext HTTP to the TLS port, garbage bytes | `ssl.connection_error +1` |
| client cert with mismatched private key, malformed-DER client cert | `ssl.connection_error +1` |
| partial ClientHello then FIN, partial ClientHello then RST, 0-byte close, 0-byte RST | **NOTHING under `ssl.*`** |
| no client cert on mTLS (control) | `ssl.fail_verify_no_cert +1` |
| valid cert (control) | `ssl.handshake +1` |

**THE RULE:** the reference increments `ssl.connection_error` **iff BoringSSL reports an actual SSL protocol error** (an `ssl_socket.cc` `TLS_error` line). A transport-level EOF or reset takes `ConnectionImpl`'s generic `remote close` path and books **nothing** under `ssl.*`.

### ⚠️ WHAT THE BRAINSTORM REFUTED — ELEVEN CLAIMS

1. ⚠️ **"THE REFERENCE SIDE IS AN UNPROBED DOC CLAIM" IS FALSE.** `ROADMAP.md:155` and the old `STATE.md` both said so. **Phase 77 probed it on 2026-07-26** and the result **never left its BRAINSTORM** — `SSL_ERROR_SSL` reads **0** in `DECISIONS.md` and **0** in `BEHAVIOR_CONTRACT.md`. **A measurement governing a deferral decision must land in a GOVERNING document; this one was lost for seventeen rows.**
2. ⚠️ **NOT A TWO-DOCUMENT CONTRADICTION — A FIVE-POSITION DIVERGENCE.** See `BRAINSTORM.md` §3.4.
3. ⚠️ **TWO OF THOSE POSITIONS WERE LANDED BY ONE AUTHOR IN ONE COMMIT** (`c57b98b8`, the phase-75 IMPL): `BEHAVIOR_CONTRACT.md:1971` says "still blocked" while `DECISIONS.md:17390` says "blocker RETIRED". **`git blame` before assuming a chronology.**
4. ⚠️ **PHASE 77'S "THE POSITIVE POPULATION CANNOT BE TYPED IN GO" FALLS BY INVERSION.** True but irrelevant: **you type the CLOSED transport complement** (`io.EOF`, `io.ErrUnexpectedEOF`, `syscall.ECONNRESET`, `net.ErrClosed`, `context.DeadlineExceeded` — all `errors.Is`-able) **and `Inc()` otherwise.** No message-text matching. **When a positive class is open and untypeable, check whether its COMPLEMENT is closed.**
5. ⚠️ **`~4-6 PRODUCTION LINES` IS REFUTED — MEASURED 35-55 ADDED / 12-22 REMOVED**, across the same 2 files. Four landed prose comments become FALSE (`manager.go:175-177`, `:392-393`, `:398-400`, `:1291-1293`).
6. ⚠️ **THE TEST-PIN SURFACE SPANS FOUR FILES THE ESTIMATE NEVER NAMED**, including **TWO exact-set `reflect.DeepEqual` pins** (`manager_test.go:2136-2145`, `quic_test.go:280-288`) and `stats/helptext_test.go`'s **bidirectional set equality**.
7. ⚠️ **`stats/name_test.go:232-237` IS A DOCUMENTED TRAP NAMING THIS EXACT CHANGE** — its own comment records that at the phase-75 PLAN a fifth `helpText` entry with the slice left short kept **the whole package GREEN**.
8. ⚠️ **THE NEW NAME PREPENDS, NOT APPENDS** — `LC_ALL=C` puts `ssl.connection_error` FIRST of five. Phase 75's append precedent does NOT transfer.
9. ⚠️ **`helptext_test.go` POST-DATES PHASE 75** (created `895f0be2`, 2026-07-29; phase-75 IMPL `c57b98b8`, 2026-07-25), so **any estimate anchored on the phase-75 diff structurally understates this row.**
10. ⚠️ **FOUR MUTUALLY INCONSISTENT COST FIGURES ARE LIVE, IN TWO DIFFERENT UNITS** (`~9-11` / `~10-13` / `~12-15` tasks / `~4-6` lines), and the `~9-11` **collides** with an unrelated `~9-11` at `phases/75-.../SPEC.md:349`.
11. ⚠️ **A ROSTER-ORDINAL GREP ANCHORING FORWARD RETURNED THE WRONG TAIL.** Rows 79-81 interpose a `§9` token between the ordinal and `Observability-family row`; a forward-anchored pattern returned row 75 (TWENTY-FIRST) as the tail when the true tail is row 81 (TWENTY-FOURTH). **The row would have collided with row 79.** Locate the phrase and read BACKWARDS.

### ⚠️ WHAT THE SPEC OWES — EIGHT ITEMS, ENUMERATED AT `BRAINSTORM.md` §10

1. **Draft `ADR-0316`** (TAIL-derived; headings+1 reads **315**, a TAKEN id, because the space is sparse at the `0209` gap). §Context only, `PROPOSED` in the **house form**. ⚠️ **Do NOT quote a count for either `PROPOSED` form in prose the grep matches — the phase-93 SPEC falsified itself doing exactly that.**
2. **Write out the predicate** — the closed transport-exclusion set, with the `tls.RecordHeaderError`-**BY-VALUE** footgun guarded (`errors.As(err,&ptr)` is **false**, wrapped or not; a pointer-drafted predicate **compiles and never matches**).
3. **Correct all five divergent positions SURGICALLY.** Delete ONLY the *"still blocked on enumerating its membership"* clause from `BEHAVIOR_CONTRACT.md:1971`; **KEEP** the *"membership is UNENUMERATED"* sentence beside it — they are different propositions and only the second survives.
4. ⚠️ **PROPAGATE THE REFERENCE RULE INTO A GOVERNING DOCUMENT.** The row's most durable deliverable; see refutation 1.
5. **Design fixture `0120`** (dir verified FREE), port from **`10126-10129`** (all four verified unused repo-wide), `BackendCount >= 1`, **+0 BackendKinds**. ⚠️ **A NEW FIXTURE IS FORCED** — `0110`/`0111` use NAMED SUBSETS so a fifth name does not redden them, but **no existing arm anywhere produces an `outcomeOther`**, and adding one breaks their "deterministically 3 accepts" reasoning. A `connection_error: 0` pin instead is a **vacuous `0 == 0`**.
6. **Enumerate the test-pin roster as an EDIT roster**, including the **4-site rename** `…RegistersExactlyFourSSLNames` -> `…Five…` (`manager_test.go:2021 :2102 :2111 :4576`). ⚠️ **A stale `-run` selector carrying the OLD name PRINTS `ok` AND EXITS 0 HAVING RUN NOTHING.**
7. **NC every new pin, and NEUTRALISE rather than revert.**
8. **Re-derive every count at your own tip.**

### ⚠️ BANKED / REJECTED, WITH COSTS MEASURED AT THIS TIP
- **GET `/runtime`** — ⚠️ **THE STRONGEST RUNNER-UP.** `~+160/-15` across 4 files; **no constructor widening** (`admin.Server.bs` already reaches `Bootstrap.Runtime`); **+0 fixtures** (extend `0118`), **+0 stats**, **+0 BackendKinds**. Its reference side is already measured (phase-77 SPEC §3.3.1). ⚠️ **Its `snapshot.go` doc comment is a LANDED FORWARD-POINTER inviting it** (*"this row serves no `/runtime` endpoint"*). ⚠️ **Within-layer collisions flip ~40/60 across process starts — a cross-side body comparison is sound ONLY on a collision-free config.**
- **`POST /runtime_modify`** — ~2-3x the above; **drags a reject-lift** (`bootstrap.go:575`) that must land ATOMICALLY per `reference_lifted_reject_hidden_enforcement`; makes two gauges LIVE and invalidates `0118`.
- **Lifting the six `runtime_key` rejects** — ⚠️ **A STRICT SUPERSET of GET `/runtime`, not a peer.** Needs the whole `Snapshot` widening PLUS a new `FactoryCtx` seam (verified: `internal/filter/http/types.go` has **no runtime field**, and `internal/bootstrap/bootstrap.go` is the ONLY consumer of `internal/runtime`). ⚠️ **`parseRejectEnabledRuntimeKey` is defined INDEPENDENTLY IN TWO PACKAGES — a bare-name grep conflates them.**
- **1xx interim responses** — cheapest CODE (~5-15 lines, `client.go:631`/`:655`/`:199`) but ⚠️ **IT SITS IN NO WINDOW AT ALL** (`git grep -i -- '1xx' docs/envoy-go/ROADMAP.md` returns ONLY lines 154/155, both phase-ROW cells) **and needs a NEW BackendKind + fixture** (~+900 all-in). Its banked "one symbol" framing omits both.
- **Window `:240` (Operational tooling)** — ⚠️ **FEWEST ITEMS, WORST PICK.** All three are blocked on absent subsystems, and one depends on a DIFFERENT window (`:232`). **"Smallest window" is NOT "closest to empty", and cross-window dependencies exist and are unrecorded.**
- **The four dynamic `ssl` families** — blocked on NAMING (the stats name pattern bans the hyphen, so an OpenSSL cipher name **panics at registration**); `sigalgs` may be **unimplementable** (`crypto/tls.ConnectionState` exposes no signature-scheme field).
- **`ssl.connection_error`'s own siblings** — the other TEN fixed `ssl.*` names remain deferred.

## ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + ALL FOUR NCs + THE CHECK-(2) POSITIVE CONTROL YOURSELF** and record ACTUAL output.
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** 92 IMPL twelve · 93 BRAINSTORM eleven · 93 SPEC eleven · 93 PLAN twelve · 93 IMPL nine · **94 BRAINSTORM ELEVEN.** Do the same, and record it when your agents refute you. ⚠️ **AT THIS STAGE THE CONTROLLER'S OWN OPENING FRAMING WAS REFUTED BY ITS OWN AGENT** (it posited a two-document contradiction; there are five positions) **AND A PROBE AGENT CAUGHT ITS OWN MID-RUN MISREADING.** Both are recorded, not smoothed.
3. **LINE ANCHORS DRIFT; SYMBOL AND LITERAL-TEXT ANCHORS DO NOT — RE-VERIFY EVERY CITE AT YOUR TIP.** ⚠️ **PATHSPEC-SCOPE every symbol assertion.** ⚠️ **A "DRIFT-PROOF" ANCHOR IS ITSELF A CLAIM — CHECK IT FOR UNIQUENESS.** This row's two edit targets were uniqueness-checked: `return outcomeOther` and `case outcomeVerifyError` each return **exactly one** match repo-wide. ⚠️ **`internal/filter/hcm/chain.go` DOES NOT EXIST — it is `internal/filter/http/chain.go`** — but ⚠️ **`actions.go` IS in `hcm`.**
3b. ⚠️ **CHARACTER-CLASS AND ESCAPE SELECTORS ARE THE FAIL-UNSAFE FAMILY.** `grep -cE '^\s+[a-z0-9./-]+ v[0-9]'` on `go.mod` reads 62 not 67 · `^[0-9]{4}-` on fixtures reads 119, dropping `0007a`/`0007b` (use `ls -d test/fixtures/*/ | wc -l`) · ⚠️ **`\t` in a regex is TOOL-DEPENDENT: harness `grep` is ugrep 7.8.4, `/usr/bin/grep` is GNU 3.11. NAME THE TOOL.** ⚠️ **AND A CASE-SENSITIVE GREP IS BLIND TO UPPERCASE PROSE — use `-i` when auditing comments.**
3c. ⚠️ **`feedback_brief_citations_not_evidence` APPLIES TO INCIDENTAL FIGURES. Re-derive EVERY number you restate — including every number in THIS file.**
3d. ⚠️ **NO GATE MAY REST ON A ` + ` SPLIT.** Empirically confirmed at this stage: wrong on 3 of 6 windows.
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Live: TWO SDS dial-budget flakes ⚠️ **plus `TestSDSEndToEnd_FetchFailure_BootFailsClosed`** · the driver-owned receiver port race · `internal/httpclient` zero-value · the two 84.2-era flakes · the REFERENCE h2spec section-8 flip · `TestOutlierDetector_ConcurrentEjectExactlyOnce` · `0061-lb-ring-hash`'s σ-margin (**a SECOND occurrence was recorded at phase 77; investigating it is a real follow-on the project owes**). ⚠️ **`TestServerConn_TinyWindowDelivery` IS NOT A FLAKE — phase 91 FIXED a live production deadlock; a recurrence is a REGRESSION of row 91.**
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only). ⚠️ **AT THIS STAGE A SIBLING `envoy-rust` SESSION WAS BUSY CONCURRENTLY and an idle `curl-world` session owned NINE containers.** Seen and LEFT UNTOUCHED: `curl-world-h3-1`, `curl-world-tls-1`, `curl-world-proxy-tls-1`, `curl-world-httpd-1`, `curl-world-httpbin-1`, `curl-world-quirks-1`, `curl-world-socks-1`, `curl-world-proxy-digest-1`, `curl-world-proxy-1`, plus previously-seen `infallible_booth`, `crazy_kare`, `golink-ai`, `quizzical_goldstine`. ⚠️ **A `reaper_*` testcontainers Ryuk container is created BY THE DIFFERENTIAL ITSELF and is REUSED; leave it alone.** **Treat NO roster as stable.**
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`.
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL.** **`gofmt -l` never exits non-zero — gate on OUTPUT.** ⚠️ **`golangci-lint` DOES exit non-zero on findings — but STILL gate on OUTPUT.** ⚠️ **go test WITHOUT `-v` PRINTS ZERO `=== RUN`** — `RUN=0` beside `RC=0` is a VACUOUS GREEN. ⚠️ **A `-run` selector matching nothing prints `[no tests to run]` and EXITS 0.** ⚠️ **On `-v` output, unanchored `grep -c 'FAIL'` reads nonzero ON A FULLY GREEN TREE** — use `grep -cE '^(FAIL|--- FAIL)|^ *--- FAIL'`.
7b. ⚠️ **`grep -c` ON ZERO MATCHES PRINTS `0` AND EXITS 1** — capture (`v=$(… || true)`), NEVER chain with `&&`, and NEVER write `$(… || echo 0)`.
7c. ⚠️ **A GREEN TEST RUN IS NOT EVIDENCE A SITE IS EXERCISED** — prove it with a **`panic()` REACHABILITY CONTROL**.
7d. ⚠️ **NC EVERY NEW TEST, AND NC YOUR OWN FIXES** — a NC that is a BUILD BREAK proves nothing; **NEUTRALISE instead so the package still compiles.**
7e. ⚠️ **A UNIT TABLE OVER A SHARED ASSERTION IS NOT A GATE ON PRODUCTION BEHAVIOUR** — and ⚠️ **`t.Helper()` IS WHAT SAVES IT**: it gives each failure a DISTINCT call-site line.
7f. ⚠️ **A NC THAT LEAVES YOUR NEGATIVE CONTROL GREEN IS NOT EVIDENCE THAT CONTROL DOES ANY WORK** — you need a SECOND, ISOLATING NC.
7g. ⚠️ **ASSERT *WHICH* ERROR FIRED, AND ALSO WHICH DID NOT.** This row needs it acutely: a pin proving `connection_error` moved says nothing about whether `fail_verify_error` also moved. ⚠️ **AND KEEP TABLE ROWS SINGLE-CAUSE.**
7h. ⚠️ **A PROBE CLIENT CAN WITHHOLD THE VERY THING THE ARM EXISTS TO TEST.** NEW AT THIS STAGE: Go silently sent an EMPTY cert chain for an UNPARSEABLE leaf under TLS 1.3, so arm (f2) initially measured the CLIENT, not the reference. Caught only by making the client **print what it sent**, and resolved by re-running under TLS 1.2. `reference_go_client_cert_withholding` **extends from untrusted certs to UNPARSEABLE ones — the SPEC's arms must FORCE-SEND in both cases.**
8. ⚠️ **`pgrep -f` / `pkill -f` MATCHES YOUR OWN SHELL AND KILLS THE TOOL CALL (exit 144)**. **Kill ONLY PIDs you captured yourself.**
9. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-*`, `wt-*` or stage worktree should outlive its stage close** — expect only `master` (verify with `git worktree list`).
10. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** This stage is **docs-only, ZERO production `.go`**, so it ran NO code gates and claims NONE. The last full measurement is the phase-93 IMPL's: (a) differential **121/121 PASS**; (b) non-Docker sweep **RC=0, FAIL=0, 234 packages**, gated on `PIPESTATUS[0]` and a SET RECONCILIATION (⚠️ **NOT `INNER_EXIT`, which DOES NOT EXIST**); (c) h2spec **`95 tests, 94 passed, 1 skipped, 0 failed`**; (d) fuzzers **56 / 48**; (e) the ANCHORED panic gate `^panic:|DATA RACE|SIGSEGV`, **0**; (f) no REVIEW.md — standing departure. ⚠️ **`-race` on the DIFFERENTIAL suite is VACUOUS** — the subject is an unraced subprocess.
11. ⚠️ **THE FIXTURE INVOCATION: `go test ./test/differential/ -run 'TestDifferential/0004-h2-routing' -count=1 -v 2>&1`.** ⚠️ **`-count=1` IS NOT OPTIONAL** — the harness builds envoy-go as a **SUBPROCESS**, so a production edit is **not a compile-time input** to that test binary and the cache serves a stale PASS. ⚠️ **Gate (a)'s failure mode is a SILENT PASS** — the runner `t.Skipf`s an unregistered fixture and **NO fixture-count gate exists anywhere in the tree.** **ASSERT THE FIXTURE SET BY NAME, BOTH DIRECTIONS.** ⚠️ **The full suite takes ~400s.**
11b. ⚠️ **`go test ./...` DRIVES DOCKER — AND THERE ARE TWO DOCKER DRIVERS.** Besides `test/differential`, **`test/conformance/h2spec`** also drives Docker. **Exclude BOTH:** `go list ./... | grep -vE '/test/differential$|/test/conformance/h2spec$'`.
12. ⚠️ **`-p` PUBLISHING WORKS; `--network host` IS WHAT DOES NOT.** ⚠️ A **STRICT_DNS** cluster whose name does not resolve **blocks listener init entirely** — prefer `STATIC` by IP. ⚠️ **`clusters: []` is BOOT-REJECTED.** ⚠️ **envoy-go REJECTS `match.headers`**, ⚠️ **refuses `codec_type: HTTP2` without TLS unless `--allow-h2c`**, ⚠️ **boots with `-c`, NOT `--config-path`**, and ⚠️ **`access_log[].log_format` is BOOT-REJECTED.** ⚠️ **`ENVOY_TARGET.md` is at `docs/envoy-go/ENVOY_TARGET.md`, NOT the repo root.** ⚠️ **A PROBE BACKEND MUST BIND `0.0.0.0`, NOT `127.0.0.1`.** ⚠️ **The pinned reference is `envoyproxy/envoy:contrib-v1.37.2`, digest `7edd5b0fd763…` — RE-VERIFIED AGAINST `ENVOY_TARGET.md` AT THIS STAGE.**
13. ⚠️ **A RAW-FRAMER PEER MUST HOLD ONE `hpack.Decoder` PER CONNECTION.** ⚠️ **Set `ReadMetaHeaders` to NIL when probing malformed shapes.** ⚠️ **`0004` is TLS+ALPN-h2, NOT h2c.**
14. ⚠️ **`bash /dev/tcp` + `timeout cat <&3` INSIDE COMMAND SUBSTITUTION RETURNS SILENTLY EMPTY.**
15. ⚠️ **GO DOES NOT FLAG UNUSED CONSTANTS.** Post-removal cleanup is grep-driven.
16. ⚠️ **`golangci-lint`'s misspell runs in locale US and HAS FIRED ON THREE CONSECUTIVE STAGES' PROTOTYPES.** **Sweep British spellings in `.go` comments BEFORE the gate.** ⚠️ **Markdown prose may use them freely.**
17. ⚠️ **A GREEN RUN OF A CONTENTION-SENSITIVE TEST PROVES NOTHING.** Phase 91's subject cleared 5/5 uncontended while failing 9/32 contended on the SAME binary.
18. ⚠️ **AN ABSENCE CLAIM NEEDS A POSITIVE CONTROL AND A DISCRIMINATING NEGATIVE CONTROL.** This stage's reference probe used both plus a two-listener SET RECONCILIATION; copy that shape.
19. ⚠️ **DERIVE next-free ADR FROM THE TAIL, NEVER FROM THE HEADING COUNT.** The id space is SPARSE (exactly one gap, `0209`) and **headings+1 reads 315 — a TAKEN id.**
20. ⚠️ **`reference_measured_prototype_is_a_lower_bound` HAS NOW FIRED EIGHT CONSECUTIVE ROWS, ALWAYS BY UNDER-ENUMERATING *FILES*.** ⚠️ **AT THIS STAGE IT WAS CAUGHT BEFORE FIRING A NINTH TIME**: the inherited roster named 2 files; the true pin surface is **six**, and `helptext_test.go` POST-DATES the phase-75 anchor everyone was estimating from. **ENUMERATE BY `git grep -l` ON THE LITERAL CLAIM TEXT, AND BY COMPILING PROTOTYPE.**
21. ⚠️ **A MEASUREMENT THAT GOVERNS A DEFERRAL DECISION MUST LAND IN A GOVERNING DOCUMENT.** NEW AT THIS STAGE. Phase 77's reference measurement stayed in its BRAINSTORM and was lost for seventeen rows, until a later row rejected the candidate for lacking evidence that already existed. `reference_brainstorm_adjective_acquires_adr_authority` covers BRAINSTORM prose gaining authority it should not have; **this is the MIRROR failure — prose that FAILED to gain authority it should have had.**

## ⚠️ THE `PROPOSED` GUARD — DISARMED, AND THE DECOY IS WHAT REMAINS

| form | now | where |
|---|---|---|
| `^> \*\*STATUS: PROPOSED` — **the house form** | **0** | DISARMED at the 93 IMPL |
| `^\*\*Status:\*\* PROPOSED` — **the ADR-0231 decoy** | **1** | `:14866` under `## ADR-0231`, byte-untouched for many phases |
| union of both | **1** | — |

⚠️ **VERIFY BY LINE AND BY ADR, NEVER BY THE COUNT ALONE** — a session gating on the union or the decoy reads `1` and wrongly concludes an ADR is still open. ⚠️ **NEVER gate on the UNANCHORED form nor the middle-ground `^\*\*Status:\*\*.*PROPOSED` form.** ⚠️ **AND DO NOT QUOTE A COUNT FOR EITHER OF THOSE TWO FORMS IN PROSE THE GREP MATCHES.** **The phase-94 SPEC will RE-ARM the house form 0 -> 1 when it drafts ADR-0316 §Context.**

## ⚠️ THE ARCHIVE GUARD

⚠️ **THE STRICT `163` IS NOT THE ENTRY COUNT.** Measured at this close: loose **220** = strict **163** + parenthetical **57**, exactly. **The guard is SOUND as a DELTA-0 SHAPE check** (a correctly-shaped parenthetical append moves it by 0) **but the number is a SUBSET.** The forms disagree and **any gate must name WHICH it uses.**

⚠️ **THE ARCHIVE'S POSITIVE CONTROLS ARE SELF-INCREMENTING.** A control figure recorded in the file it measures is invalidated by the act of recording it. ⚠️ **THIS STAGE'S ARCHIVE LINE THEREFORE NAMES NO CONTROL FIGURE AT ALL.** ⚠️ **The eviction check has the same trap:** if the §Recent preamble NAMES the evictee, a naive grep on `STATE.md` reads **1**. **The DISCRIMINATING form is the strict `^- \*\*prior active-phase:\*\* \`<label>\`` form.** ⚠️ **AT THIS CLOSE BOTH forms read 0**, because the preamble describes the evictee without spelling its label — **do that.**

⚠️ **A GENUINE DATE TIE EXISTED AT THIS CLOSE AND WAS BROKEN, NOT WISHED AWAY.** Direct date read gave `2026-08-31`, `2026-08-29` x2, `2026-08-26` x2 — the two oldest tie exactly. Broken by LIFECYCLE PRECEDENCE, which agrees with file order. ⚠️ **THE PHASE-93 IMPL FOUND A UNIQUE OLDEST WHERE ITS PLAN HAD RECORDED A TIE; THIS STAGE FOUND A TIE WHERE THE IMPL HAD FOUND UNIQUENESS. INHERIT NEITHER — READ THE DATES YOURSELF.**

## ⚠️ PORT BANDING

`net.ipv4.ip_local_port_range = 32768 60999`. ⇒ **BAND YOUR PROBE PORTS BELOW 32768.** ⚠️ **The differential harness reserves `20000..31007` (`harness_test.go:293`)**, so `12000-19000` is the safe band for ad-hoc probes — a failure OUTSIDE the harness band is an **out-of-band** occurrence, NOT an in-band recurrence. **Check availability with `ss -tan` (ALL states), never `ss -ltn`.** ⚠️ **Parts of `12000-19000` ARE OCCUPIED by the sibling `curl-world` containers (13000, 13001, 13901, 18080-18128 observed) — CHECK, do not assume.** ⚠️ **FIXTURE ports are FAMILY-BANDED as `10<index>` — `10120` is TAKEN (fixture `0028`); free in-band is `10126-10129`, all four VERIFIED UNUSED REPO-WIDE at this stage.** ⚠️ **`0120` as a DIRECTORY is FREE and is CLAIMED BY ROW 94 at its IMPL.**

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (**ugrep 7.8.4**). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **THE METHOD BINDS; carry no standing discrepancy figure and no standing absolute.** ⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS; print FILE NAMES, never a count.** ⚠️ **THE HARNESS `grep` STRIPS A LEADING `./`.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files; use `-v --no-index`, and `git status` is the discriminating check). ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **A DISCREPANCY MAY FAIL TO MANIFEST** — absence of a discrepancy is not evidence the blindness is gone.

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 94 (tls-connection-error-stat)' --format='%h %s'   # the SLUG form
git log --grep '^phase 94' --format='%h %s'                               # the LOOSE form — RUN IT to CROSS-CHECK
```
⚠️ **RUN BOTH FORMS AND RECONCILE THE SETS. NO COUNT IS QUOTED HERE, DELIBERATELY — BOTH FORMS ROT.** **A `| head -1` on ONE form can silently return a NON-TIP commit. Take the tip from `git rev-parse master`.** ⚠️ **AND WHEN YOU LAND A CORRECTION, PUT THE FULL SLUG IN ITS SUBJECT** — a slugless correction is invisible to the slug form.

Execution style: **subagent-driven per `feedback_execution_style`**. Subagents commit LOCALLY on their own stage branch with EXPLICIT PATHSPECS; the controller merges and squashes (`feedback_subagents_no_push`, `feedback_push_to_origin`). **Pure MEASUREMENT agents should commit NOTHING and prove their tree clean** — four did so at this stage. ⚠️ **PARALLEL AGENTS IN ONE WORKTREE RACE THE GIT INDEX EVEN WITH DISJOINT PATHSPECS** — use **PER-AGENT WORKTREES ON THEIR OWN BRANCHES**. ⚠️ **GIVE EACH PARALLEL AGENT ITS OWN PRIVATE SCRATCH DIR**, and **SERIALIZE DOCKER OWNERSHIP TO EXACTLY ONE AGENT AT A TIME** — done at this stage; three of four agents were Docker-free by instruction. ⚠️ **VERIFY WHAT A SUBAGENT REPORTS — BUT ALSO BELIEVE IT WHEN IT REFUTES YOU.** Per-task `gofmt` + `golangci-lint` on touched packages, **gated on OUTPUT**. ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root** — build with `-o` into scratch.

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root. Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` before any commit or gate run. ⚠️ **Shell VARIABLES do not survive between Bash calls** — re-export in every command. ⚠️ **AND AN UNQUOTED HEREDOC (`<<EOF`) COMMAND-SUBSTITUTES BACKTICKED TEXT** — use `<<'EOF'` and pass paths via the environment.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — `git status --porcelain` AND `git diff --stat master` must show ONLY your intended edits. For a patched TRACKED file: `sha256sum` BEFORE patching, `git checkout -- <path>` after, `sha256sum -c` the capture — ⚠️ **and COMMIT FIRST, because `git checkout --` restores from HEAD and will wipe uncommitted work.** ⚠️ **Also check the MAIN repo root, and `--untracked-files=all`.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads RUN THE PHASE-94 SPEC.** ⚠️ **`grep -c 'next-skill:'` reads 2: one is the ADR-0288 RULE STATEMENT matching its own grep, and the live pointer is the other. That is CORRECT, not a violation.** ⚠️ **§Project counts is a PHASE-76-ERA SNAPSHOT and SAYS SO.**
2. `docs/envoy-go/phases/94-tls-connection-error-stat/BRAINSTORM.md` — **412 lines. §10 is the SPEC's eight owed items; §2 is the reference measurement; §3.4 is the five-position divergence to reconcile.**
3. `docs/envoy-go/ROADMAP.md` — row 94 at `:156`; the deferred-candidate windows have SHIFTED to `:204 :210 :216 :226 :232 :240`.
4. `docs/envoy-go/BEHAVIOR_CONTRACT.md:1965-1975` — the departure this row closes, and the ONE CLAUSE that must be deleted (keep its neighbour).
5. `docs/envoy-go/DECISIONS.md` **ADR-0296** / **ADR-0297** — the phase-74 and phase-75 decisions this row extends.

## What the next session owes
**RUN THE PHASE-94 SPEC** (state 1 -> 2) — ⚠️ **ONE STAGE PER SESSION.** Discharge the eight items at `BRAINSTORM.md` §10 · **DRAFT `ADR-0316` §Context** and RE-ARM the house `PROPOSED` guard **0 -> 1** · **ROW 94 STAYS `in-progress`** (`want` STAYS **126**) — ⚠️ **COUNT THE ROW'S FIELDS UNDER *BOTH* FORMS IF YOU EDIT IT** · **RE-RUN the sentinel + all four NCs + the check-(2) positive control yourself** — ⚠️ **check (1) PRINTS `NOT DONE: row 94` now, and NC-A/NC-B are TWO-line shapes** · **REFUTE YOUR PREDECESSOR BY EXECUTION** · `STATE.md` rolled IN PLACE with the oldest §Recent entry evicted by a **DIRECT DATE READ** (⚠️ **read the dates yourself — a tie existed at THIS close and did NOT at the one before**) and archived to `STATE_HISTORY.md` as **ONE INLINE LINE** in the PARENTHETICAL form, so the strict guard stays at **163, DELTA 0** · ⚠️ **AND ROLL THE §Recent PREAMBLE SENTENCE TOO — the guard is structurally blind to it** · ⚠️ **WORD THE PREAMBLE SO IT DOES NOT SPELL THE EVICTEE'S LABEL** · ⚠️ **NAME NO POSITIVE-CONTROL FIGURE IN THE ARCHIVE LINE** · `next-prompt.txt` rolled (`git add -f`) · ONE squashed commit, merged and pushed — ⚠️ **and if a late measurement invalidates something you already pushed, land a CLEARLY-LABELLED correction commit (WITH THE FULL SLUG IN ITS SUBJECT) rather than force-pushing or leaving it standing.**
