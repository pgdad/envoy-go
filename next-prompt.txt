START a fresh cold-start session for envoy-go — **phase 28 BRAINSTORM (`network-filter-zookeeper-proxy`) is DONE + document-review APPROVED + squash-merged + pushed**. The active phase is **phase 28** (parent pre-split row, `in-progress`; sub-rows 28.1/28.2 `planned`). This session authors the **phase-28 parent SPEC** — per SKILL_ROUTING state 1→2 (`superpowers:writing-plans` scoped to parent-SPEC authoring per the 22/24/25/26 parent-row precedent).

Phase 28 BRAINSTORM landed at squash `a410659` (pushed to origin/master): the THIRD §9 Network-filters-family row, brainstormed via a 4-question dialogue. **Q0** subject = `zookeeper_proxy` (`envoy.filters.network.zookeeper_proxy`, type `…filters.network.zookeeper_proxy.v3.ZooKeeperProxy`; bindings verified in go-control-plane v1.32.4) — a passive observability sniffer decoding the ZooKeeper client protocol (jute) in BOTH directions, emitting per-opcode counters. **Q1** scope = both-direction COUNTER parity (requests at 28.1; responses + the deterministic `enable_latency_threshold_metrics` fast/slow counters at 28.2); the latency HISTOGRAM family (`*_response_latency`) stays DEFERRED per ADR-0060 (coverage-boundary record); 2-way DIRECTION-PROGRESSIVE pre-split. **Q2** the upstream-faithful **`network.WriteFilter` seam** — `OnWrite(buf *Buffer, endStream bool) Status` + write chain in REVERSE chain order + delivery via the chain runtime wrapping the downstream `net.Conn` handed to `handleTerminal` (`writeChainConn`; `prefixConn` precedent); `TerminalFilter.Handle` signature UNCHANGED, `tcp_proxy`/HCM untouched; CONSUMES the ADR-0213 API-revision allowance (26.1 Q4 defer-with-allowance → consume-at-consumer; zookeeper = consumer #1, mongo = anticipated #2). **Q3** hermetic synthesized-byte differential fixtures (NO real ZooKeeper server) + cross-side `StatsAsserter` per-opcode counter parity as the LOAD-BEARING proof + deliberate-break liveness per fixture. Sub-phases: **28.1** `network-filter-write-seam-and-zookeeper-requests` (WriteFilter seam [ADR-0221] + the `internal/filter/network/zookeeperproxy/` package request side + 7th built-in + prom tag-extractor arm + fixtures `0046-zookeeper-requests`/`0047-zookeeper-boot-reject` + 37th fuzzer [ADR-0222]); **28.2** `network-filter-zookeeper-responses-and-latency` (response decoder in `OnWrite` + xid correlation + latency-threshold counters + fixture `0048-zookeeper-responses` + parent ROLLUP [ADR-0223]).

## What this session must do

Author `docs/envoy-go/phases/28-network-filter-zookeeper-proxy/SPEC.md` (the parent SPEC) via `superpowers:writing-plans` scoped to SPEC authoring. The parent SPEC must:

1. **Execute the BRAINSTORM §10 D28-1..D28-10 empirical pins IN-SESSION** against Envoy v1.37.2 per ADR-0004 (parallel-subagent fan-out per the 25/26/27 SPEC precedent — fetch `source/extensions/filters/network/zookeeper_proxy/*` at tag v1.37.2; run `proto.MessageName` in-session; boot the reference image with a zookeeper_proxy listener for D28-1). **D28-1 (the standard `envoyproxy/envoy:v1.37.2` image ships `zookeeper_proxy`) is SPEC-BLOCKING for the Q3 cross-side strategy** — if it fails, the differential strategy needs rework (escalate, do not improvise).
2. Pin the exact stat scope/naming (`zookeeper.<stat_prefix>.*` hypothesis) + the full per-opcode counter roster (the proto's 27-value `LatencyThresholdOverride` opcode enum vs upstream's stat-macro list, which may add non-enum names incl. watch_event/connect_readonly) + the ZK wire framing facts (connect special framing; xid/opcode layout; special xids ping −2 / auth −4 / watch −1) + decoder-error semantics + the dynamic-metadata namespace/keys (D28-6 mirror-or-defer) + the WriteFilter StopIteration posture (D28-7).
3. Formalize the 2-way split surface-mapping (which deliverables/tests/fixtures land at 28.1 vs 28.2) + re-check the ADR-0045 gate per sub-phase.
4. Draft the ADR-0221 (WriteFilter seam) + ADR-0222 (zookeeper_proxy filter + request side) + ADR-0223 (response side + latency-threshold counters) §Context blocks into DECISIONS.md (tail ADR-0220 → ADR-0223; next-free → ADR-0224; §Decision/§Consequences bodies land at the respective IMPLs per ADR-0044).
5. Run the spec-document-reviewer loop until APPROVED; fold advisories in.
6. ROADMAP: parent row 28 STAYS `in-progress`; sub-rows 28.1/28.2 STAY `planned` (28.1 flips `planned → in-progress` at the **28.1 SPEC** commit, NOT at the parent SPEC — per the 26.x precedent).
7. Update STATE.md (active-phase → `phase 28 parent SPEC done`; next-skill → `superpowers:writing-plans` scoped to the 28.1 SPEC) + rewrite this next-prompt.txt + commit + push (work in a worktree per `feedback_git_worktrees`; squash-merge + push per `feedback_push_to_origin`).

## Project state at master tip

- **Master tip:** `3f3b43d` (the docs-only SHA-fill/next-prompt commit that trails the substantive phase-28 BRAINSTORM squash `a410659` by +1). Confirm the live tip via `git log --oneline -1`; any further next-prompt repoint advances the tip +1 (expected, not chased). STATE.md `last-commit` points at the substantive BRAINSTORM squash `a410659`. origin/master in sync after push.
- **§9 Network-filters family:** rows 26 (parent + 26.1/26.2/26.3) + 27 DONE; row 28 `in-progress` (parent; sub-rows 28.1/28.2 `planned`). After phase 28, 4 candidates remain (`redis`/`mongo`/`kafka_broker`/`thrift`).
- **Counts at master tip:** stat surface **136**; differential fixtures **47** (tail `0045-sni-cluster`); fuzzers **36**; conformance 10/10; h2spec 53/53; DECISIONS.md tail **ADR-0220**; next-free **ADR-0221** (anticipated consumed by the phase-28 roster ADR-0221/0222/0223; ADR-0209 escape-valve reserve STANDS-UNCONSUMED).
- **Toolchain:** Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); reference Envoy v1.37.2 (ADR-0008); go-control-plane v1.32.4 (ADR-0008).
- **Known coverage boundaries (carried, non-blocking):** D-26.3-4 (rbac SNI/mTLS L4 arms not differential-proven); the `tcp_proxy` `downstream_cx_*` family unmirrored (phase-27 record); histograms project-wide deferred (ADR-0060 — phase 28 adds the `*_response_latency` family to this list at 28.2).

## Read first (in order)

1. THIS FILE — the parent-SPEC cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 28 BRAINSTORM done`; next-skill `superpowers:writing-plans` (parent SPEC); next-free `ADR-0221`; last-commit `a410659`.
3. `docs/envoy-go/phases/28-network-filter-zookeeper-proxy/BRAINSTORM.md` — the 12-section design this SPEC formalizes (esp. §2 Q-decisions, §10 D28-1..D28-10 pins, §6 fixture envelope, §7 ADR roster).
4. `docs/envoy-go/ROADMAP.md` — rows 28/28.1/28.2 + the §9 family note.
5. `docs/envoy-go/SKILL_ROUTING.md` + `BOOTSTRAP_PROMPT.md` §4/§5 — the per-phase lifecycle + cold-start invariants.
6. `docs/envoy-go/phases/26-network-filter-chain-and-rbac/SPEC.md` — the parent-SPEC precedent (the only prior §9 parent SPEC; mirrors its section structure) + `docs/envoy-go/phases/27-network-filter-sni-cluster/SPEC.md` — the most recent SPEC (empirical-pin execution shape).
7. `internal/filter/network/` — the as-built framework the WriteFilter seam extends (types.go / chain.go / terminal.go / prefixconn.go / callbacks.go / builtins/).
8. `docs/envoy-go/DECISIONS.md` ADR-0213/0214/0215 (§9 framework; the ADR-0213 API-revision allowance the seam consumes) + ADR-0216/0217/0218 (rbac) + ADR-0219/0220 (override seam + sni_cluster) + ADR-0060 (histogram deferral) + ADR-0044/0045/0052/0106 (doc/split/bundle/flip discipline).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — execution-style + worktree + push + subagents-no-push + per-task gofmt/lint + the network-filter constraints (typeurl-extensions, onnewconnection-halts) + the differential dispatch/asserter constraints all carry forward.
