START a fresh cold-start session for envoy-go — **the phase-35 (`load-balancer-random`) BRAINSTORM is DONE and LANDED on master; the next lifecycle step is the phase-35 SPEC** (single flat row; the Load-balancing family stays OPEN — 6 candidates remain after 35). The substantive master tip is the phase-35 BRAINSTORM squash **`c5c6c55`** (`phase 35 BRAINSTORM: load-balancer-random — open the SECOND Load-balancing-family row as the zero-churn ADR-0232 seam drop-in …`; pushed to `origin/master`; confirm via `git log --oneline -1`). It carries the BRAINSTORM.md + the ROADMAP row-35 `in-progress` registration + the Load-balancing family-heading `random IN-PROGRESS` note + the STATE advance. Below it sits the substantive phase-34 IMPL squash `b086137` (the `leastRequest` policy + the ADR-0232 LB acquire/release seam; the family OPENED).

The literal live master tip is the docs-only SHA-fill commit **`debe504`** (`next-prompt.txt: SHA-fill the literal live tip — name c5c6c55 …`; it named the phase-35 BRAINSTORM squash `c5c6c55` as the substantive tip; it trails `c5c6c55`; confirm via `git log --oneline -3`). The phase-35 BRAINSTORM was authored inline by the controller (DESIGN-ONLY; `superpowers:brainstorming`) — the 12-§ family-template doc + the spec-document-reviewer gate (APPROVED) + the ROADMAP/STATE registration, squash-merged to master + pushed at stage-close.

## What this session does — author the phase-35 SPEC (`superpowers:writing-plans` scoped to SPEC authoring)

The next session is a **phase-35 SPEC** (`superpowers:writing-plans` scoped to SPEC authoring — single flat row, the kafka-31/thrift-33/least_request-34 precedent): execute the BRAINSTORM §10 **D-R1..D-R6** empirical pins IN-SESSION (parallel-subagent fan-out) live against the contrib reference Envoy (`envoyproxy/envoy:contrib-v1.37.2`, ADR-0227) per `reference_docker_probe_bridge_network` (a shared bridge network + STRICT_DNS backend hostnames + verify traffic ran via `downstream_cx_rx_bytes_total > 0`), and anchor the **ADR-0234** (the random policy) §Context draft (DECISIONS tail STAYS ADR-0233 → next-free ADR-0234; §Decision/§Consequences body at the IMPL per ADR-0044; NO seam ADR — the ADR-0232 seam is REUSED unchanged). The spec-document-reviewer gate applies; counts stay UNCHANGED at the SPEC. Per the BRAINSTORM → SPEC → PLAN → IMPL cadence (each its own session).

## The phase-35 design (settled at the BRAINSTORM — DESIGN-ONLY)

- **Q0 subject:** `random` (`Cluster.LbPolicy RANDOM`, enum value **3**, `envoy.config.cluster.v3` — VERIFIED in the EXISTING core `/envoy v1.32.4` module cache: **NO `Cluster.RandomLbConfig` message** — zero config knobs, simpler than least_request's `choice_count`; RANDOM's locality-weighting config lives only on the DEFERRED `load_balancing_policies.random.v3` extension-point proto; ZERO new go.mod dep). The SECOND Load-balancing-family row — the family STAYS OPEN; picked as the **zero-churn ADR-0232 seam drop-in** over the seam-EXTENDING hash policies (RING_HASH/MAGLEV additionally need the ADR-0232 `Pick(hashInput)` widening — DEFERRED). 6 candidates remain after 35 {ring_hash, maglev, subset LB, locality-weighted LB, priority load balancing, panic thresholds}.
- **Q1 envelope (LEAN):** `RANDOM` accepted by `cluster.Manager` (the lb_policy reject TEXT extends its supported-list `… ROUND_ROBIN, LEAST_REQUEST` → `…, RANDOM` — a byte-stable-reject touchpoint, THREE-site blast radius at D-R2) + a stateless `randomLB` (uniform `rng() % n`, INSENSITIVE to load; the injectable mutex-guarded crypto-seeded `math/rand/v2` PCG REUSED from `leastRequest`); NO config-parse arm, NO PGV gate.
- **Q-seam:** REUSE the ADR-0232 seam UNCHANGED — `randomLB` implements the existing `loadBalancer` interface (`Pick() (Endpoint, func(), error)`) returning the shared `noopRelease` (ignores the active counters at zero cost — the FIRST reuse of the ADR-0232 seam, validating its "RANDOM drops in at zero cost" §Consequences claim). ZERO seam code, ZERO seam ADR (the thrift-33 single-ADR-on-reuse precedent).
- **Q-split:** a SINGLE FLAT ROW 35 (~40–90 prod LoC / ~4–7 tasks, FAR under the ADR-0045 gate — smaller than least_request; NO escape valve — no seam build to split off).
- **Differential (anti-skew):** fixture `0060-lb-random` reuses the `0059` held-open-conn stimulus but asserts RANDOM STAYS uniform-band PER SIDE despite the load imbalance (the contrapositive of least_request; RNG-driven, never cross-side-exact; the EXISTING `DistributionAsserter`) + a cross-side `StatsAsserter` prong. **NO new fuzzer** (fuzzers STAY 42) + **NO new BackendKind** (tail STAYS 33; the `0001`/`0059` echo backends reused) + ZERO stat-delta (1116) — the phase-34 family-level firsts, now ESTABLISHED.

## The D-R1..D-R6 empirical pins (SPEC executes IN-SESSION)

- **D-R1** *(SPEC-BLOCKING)* — re-pin the v1.32.4 surface: `Cluster.LbPolicy RANDOM` = enum value 3, NO `Cluster.RandomLbConfig` message; clean `go mod tidy` (ZERO new dep); the mismatched-oneof silent-ignore disposition (a stray `least_request_lb_config`/`ring_hash_lb_config` under a RANDOM cluster — pin the reference's behavior).
- **D-R2** *(SPEC-BLOCKING)* — the accept/reject matrix + the manager reject-text blast radius: confirm the THREE sites (`manager.go:252` the reject string + `manager_test.go:322` `TestManager_Error_UnsupportedLBPolicy` [the doubly-hit retarget — its trigger `Cluster_RANDOM` becomes accepted → retarget to RING_HASH/MAGLEV + the substring assertion at ~line 327] + `BEHAVIOR_CONTRACT.md:899`); the NEW byte-stable text `… ROUND_ROBIN, LEAST_REQUEST, RANDOM`; confirm no fixture pins the text → no boot-reject dir; the still-rejected policies' continued-rejection departure (the reference validate-accepts RANDOM/RING_HASH/MAGLEV — record).
- **D-R3** *(SPEC-BLOCKING for the fixture stimulus)* — confirm the reference's RANDOM stays UNIFORM under the `0059` held-conn load (the anti-skew core claim; contrast the `0059` least_request skew); pin the `0060` band constants + the ≥20-run flake-free protocol + the deliberate-break design (skew toward/away from load). The D-L2 cx-as-rq finding means held conns DO register as active on the loaded backend — RANDOM ignores that, the property under test.
- **D-R4** — the stat-surface delta (anticipated ZERO new names): scrape the reference `/stats` under RANDOM with live traffic; pin any lb-specific cluster stat names (mirror-vs-departure if found; anticipated none); pin the `0060` `StatsAsserter` counter set + cross-equal-vs-per-side disposition (anticipated identical to `0059`).
- **D-R5** — the uniform-sampling/RNG semantics, per-side/unit-level ONLY: uniform over the full endpoint set (`rng() % n`); healthy-host filtering (none at the MVP — no health checking; record the boundary); REUSE the `leastRequest` injectable mutex-guarded crypto-seeded PCG (confirm `newPCGRNG` sharable as a package-private helper vs. duplicated; anticipated SHARED); the empty-endpoint-set disposition (`errNoEndpoints` + `noopRelease`, the `roundRobin`/`leastRequest` parity).
- **D-R6** — the ADR-0045 envelope re-check (~40–90 prod LoC / ~4–7 tasks; NO escape valve) + confirm the seam REUSES unchanged (random returns `noopRelease`; no `Pick`-signature change; no consumer touch; ADR-0024 unamended) + the single ADR (ADR-0234, policy only — confirm no seam ADR).

## Counts at master tip (phase 35 BRAINSTORM done — they advance only at the phase-35 IMPL)

- stat surface **1116** (anticipated UNCHANGED at phase 35 — random adds NO stat names; the zero-delta first, now a family expectation).
- differential fixtures **61** (tail `0059-lb-least-request`; `ls -d test/fixtures/[0-9]* | wc -l`). Anticipated 61 → 62 at the IMPL (`0060-lb-random`). NO boot-reject dir anticipated (D-R2).
- fuzzers **42** (tail `FuzzThriftDecode`; anticipated UNCHANGED at phase 35 — no wire decode).
- BackendKind tail **33** (`TCPThriftResponder`; anticipated UNCHANGED — `0060` reuses `TCPEcho` kind-0).
- DECISIONS tail **ADR-0233** (ADR-0232 [the LB seam] + ADR-0233 [least_request], both ACCEPTED; next-free **ADR-0234** — anticipated consumed at phase 35 [the random policy, §Context at SPEC, body at IMPL per ADR-0044]).
- reference pin: `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227).
- Toolchain: Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); go-control-plane `/envoy` v1.32.4 (ADR-0008; `Cluster.LbPolicy RANDOM` is in the pinned module — ZERO new go.mod dep at 35).
- conformance h2spec 53/53 + proxy-wasm 10/10.

## Read first (in order)

1. THIS FILE — the post-phase-35-BRAINSTORM cold-start (next: the phase-35 SPEC).
2. `docs/envoy-go/STATE.md` — active-phase `phase 35 (load-balancer-random) BRAINSTORM done`; lifecycle-state `phase 35 BRAINSTORM done — SKILL_ROUTING → the phase-35 SPEC`; the counts.
3. `docs/envoy-go/phases/35-load-balancer-random/BRAINSTORM.md` — the worked record (the 12 §§ + the D-R1..D-R6 pins + the enum-3 / no-config-message verification + the seam-reuse rationale). AUTHORITATIVE for the SPEC.
4. `docs/envoy-go/ROADMAP.md` — row 35 `in-progress`; the `### Load balancing family` heading (least_request DONE, random IN-PROGRESS, family OPEN, the 6-candidate-after-35 roster).
5. `docs/envoy-go/DECISIONS.md` — **ADR-0232** (the LB acquire/release seam — READ the §Consequences "the non-keyed RANDOM drops in at zero cost" + the `Pick(hashInput)` widening note for hash policies) + ADR-0233 (the least_request policy — the RNG posture random reuses) + ADR-0024/0106/0045/0044/0052/0080.
6. `internal/cluster/{loadbalancer,leastrequest,manager,cluster}.go` — the as-built seam (`Pick() (Endpoint, func(), error)` + `noopRelease`), the `leastRequest` RNG helpers (`newPCGRNG`, the mutex-guarded `rng func() uint64`, the `*WithRNG` test seam) random reuses, the lb_policy switch (~`manager.go:234`) + the reject text (~`manager.go:252`); `internal/cluster/manager_test.go:320` (`TestManager_Error_UnsupportedLBPolicy` — the doubly-hit retarget).
7. `test/fixtures/0059-lb-least-request/` — the held-conn skew driver `0060` transposes (the contrapositive assertion) + the band-`AssertDistribution` + `StatsAsserter` pattern.
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_execution_style`, `feedback_git_worktrees`, `feedback_subagents_no_push`, `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_path_targeting`, `reference_differential_break_protocol_count1`, `reference_differential_asserter_dispatch`, `reference_differential_run_selector`, `reference_docker_probe_bridge_network`.

## Honor throughout

- `feedback_execution_style` (subagent-driven) · `feedback_git_worktrees` (work in a worktree) · `feedback_subagents_no_push` (subagents commit LOCAL-ONLY; the controller squash-merges + pushes at stage-close) · `feedback_push_to_origin` (after a clean landing on master with tests green, push without asking) · `feedback_pertask_gofmt_lint` · `feedback_subagent_worktree_path_targeting`.
- ADR-0106 (a flat family row per LB policy — NO parent rollup) · ADR-0045 (the split-gate, re-checked at each PLAN) · ADR-0044 (ADR bodies at the IMPL, in-place) · ADR-0052 (the atomic-landing six-gate) · ADR-0080 (byte-stable parse-reject wording) · ADR-0227 (the contrib reference image; live probes per `reference_docker_probe_bridge_network`).
- The lifecycle is BRAINSTORM → SPEC → PLAN → IMPL, each its own session per the established cadence. The next session is the phase-35 **SPEC**.
