START a fresh cold-start session for envoy-go — **the phase-40 outlier-detection FAMILY is COMPLETE (40.1 `consecutive_5xx` + 40.2 other-consecutive + 40.3 statistical — all three legs LANDED on master, squash `148f288`, PUSHED to `origin/master`; ROADMAP row 40 `done`, the 3-leg by-detector-class split CONSUMED, NO parent rollup per ADR-0106), and the next lifecycle step — a FRESH BRAINSTORM — is ALREADY IN-FLIGHT: the human PICKED the subject = circuit breakers = phase 41 (the THIRD Upstream-robustness-family row, on the phase-39/40 cluster-runtime substrate), the FOUR load-bearing design forks are SETTLED, and the full design was PRESENTED to the human (see §Circuit-breaker BRAINSTORM — decisions settled, below). NOTHING has landed yet — the design is pending the human's go-ahead, after which the controller: writes `docs/envoy-go/phases/41-circuit-breakers/BRAINSTORM.md` (mirror the 12-section phase-39/40 precedent) in a worktree, runs a reviewer pass, updates STATE/ROADMAP/next-prompt, then merges to master + pushes. The SPEC follows in a LATER session (one stage per session).** Confirm the tip via `git log --oneline -5` (HEAD is the latest docs-only next-prompt roll — THIS in-flight-BRAINSTORM update — sitting on the prior roll `d0ecefa` and, the load-bearing anchor, the substantive phase-40.3 IMPL squash `148f288`; NO substantive work has landed since `148f288`). Counts UNCHANGED (the BRAINSTORM has not been written to disk): stat surface **1149** / fixtures **75** / fuzzers **42** / BackendKind tail **35** (`HTTP503Responder`) / DECISIONS tail **ADR-0247**, next-free **ADR-0248**.

## What just landed (phase 40.3 IMPL — the durable record is `docs/envoy-go/phases/40.3-outlier-detection-statistical/{SPEC,PLAN,PROGRESS,README}.md`; read those, not this summary)

The 13-task TDD PLAN executed end-to-end subagent-driven (a fresh subagent per task + two-stage review); six-gate GREEN; full **75-dir differential GREEN**; **ADR-0247** §Decision/§Consequences landed IN-PLACE per ADR-0044. The STATISTICAL passive-outlier detectors over the 40.1/40.2 substrate (ADR-0245/0246, REUSED UNCHANGED):

- **success_rate detector** (`evalSuccessRate`) — the eligible-host set (≥ `success_rate_minimum_hosts`, each ≥ `success_rate_request_volume`) + the cross-host mean + the **POPULATION stddev** + the `mean − (success_rate_stdev_factor/1000)×stddev` ejection threshold (a negative threshold is a no-op); ejects-by-default (`enforcing_success_rate` default 100).
- **failure_percentage detector** (`evalFailurePercentage`) — the integer cross-multiplied `(total−success)*100 >= threshold*total` `>=` boundary over the eligible set; detect-only-by-default (`enforcing_failure_percentage` default 0).
- Both **SWEEP-driven** (NOT per-request): the windowed per-host `(intervalTotal, intervalSuccess)` `atomic.Uint64` counters live on `hostHealth`, accumulated by a success/failure classification (success = NOT `localOriginErr` AND NOT 5xx) at the TOP of `record` on EVERY path; the per-interval `sweep()` snapshots-and-`Swap`-resets the windows, runs both evaluators in order, and `tryEject`s each flagged host CAS-once-only per sweep.
- **The per-interval aggregation runtime — the project's FIRST outlier background goroutine:** `Manager.StartOutlierDetection` (a sibling of `StartHealthChecks` with parallel `odCancel`/`odWG`, one `run(ctx)` goroutine per outlier cluster, no immediate t=0 sweep) + the `Drain` join + the `main.go:274` boot call (after `StartHealthChecks`); `-race`-clean, no leak.
- **+8 stat registrations** (1141 → 1149): the 4 EXTERNAL driven `ejections_{detected,enforced}_{success_rate,failure_percentage}` + the 4 LOCAL-ORIGIN `ejections_{detected,enforced}_local_origin_{success_rate,failure_percentage}` (logic DEFERRED — registered unconditionally for surface parity, emit 0; AMEND-OD3-4) via the extended `registerStats` + `outlierStatNames()` (9 → 17). envoy-go now emits 17 of the reference's 20 `outlier_detection.*` names (the 3 legacy aliases stay departures).
- **Six new parse-reject arms** (`enforcing_success_rate`/`enforcing_failure_percentage`/`failure_percentage_threshold`/`enforcing_local_origin_success_rate`/`enforcing_failure_percentage_local_origin > 100` + `max_ejection_time <= 0s`; NO `success_rate_stdev_factor` reject — 0 accepted); `interval` made load-bearing.
- The 40.1/40.2 seam + ejection dimension + `available` predicate + cap + lazy un-eject + `tryEject` + the consecutive detectors are REUSED UNCHANGED — the per-detector eject double-count is NOT replicated (AMEND-OD3-3 — CAS-once-only ejects a host once → the fixtures isolate a single enforcing detector).
- **Differentials** `0072-outlier-detection-success-rate` (a 503 host statistically ejected via `success_rate`; K=5 healthy + 1 bad via `PerHostBackendKind`+`HTTP503Responder`; consecutive detectors OFF; cross-side `ejections_enforced_success_rate` parity + `ejections_detected_failure_percentage >= 1` + `ejections_enforced_failure_percentage == 0`) + `0073-outlier-detection-failure-percentage` (the symmetric twin — `enforcing_success_rate: 0` ⇒ failure_percentage the SOLE enforcer; the LIVE `ejections_enforced_success_rate == 0` isolation); both poll-`ejections_active`-to-converge + warmup + delta-counter; the single-sourced `reqVolume` 10 across BOTH detectors' volume floors (the §8.1 vacuousness fix makes the detect-only cross-assertion LIVE); 2 `-count=1` deliberate breaks each; `0072` 20/20 + `0073` 20/20 flake-free. The AMEND-OD3-7 retro-invariant HELD — `0069`/`0070`/`0071` (3-host < `minimum_hosts` default 5) → the statistical detectors no-op, the +8 names emit 0 on both sides (not asserted), the three prior fixtures passed UNCHANGED in the green 75.

**As-built counts:** stat surface **1141 → 1149** (+8) / fixtures **73 → 75** (`0072`+`0073`) / fuzzers **42** (UNCHANGED) / BackendKind tail **35** (UNCHANGED — AMEND-OD3-6, reuse `HTTP503Responder` + `HTTPEcho` via `PerHostBackendKind`) / DECISIONS tail **ADR-0246 → ADR-0247** (next-free **ADR-0248**). ZERO new packages + ZERO new go.mod modules.

## Circuit-breaker BRAINSTORM — decisions settled (the in-flight phase-41 design; NOT yet written to disk)

The phase-40 outlier-detection family is fully consumed (row 40 `done`). At the start of THIS BRAINSTORM the human picked **circuit breakers** = **phase 41** (next free row; the THIRD Upstream-robustness-family row). The proto is VERIFIED in the EXISTING module cache (`go-control-plane envoy@v1.37.0`, `config/cluster/v3/circuit_breaker.pb.go`): `CircuitBreakers{Thresholds[] (field 1), PerHostThresholds[] (field 2)}`; each `Thresholds`: `priority` (`RoutingPriority` DEFAULT=0/HIGH=1) + `max_connections`(dflt 1024) + `max_pending_requests`(1024) + `max_requests`(1024) + `max_retries`(3) + `max_connection_pools`(∞) + `track_remaining`(false) + `retry_budget{budget_percent 20%, min_retry_concurrency 3}`.

The FOUR load-bearing forks (settled with the human; the BRAINSTORM dialogue stopped at design-presented, pending go-ahead):
- **Fork 1 — SCOPE (core overflow trio; single phase, NO ADR-0045 split):** ENFORCE `max_connections` + `max_pending_requests` + `max_requests` (the three budgets with live request/connection substrate). PARSE-and-register but DEFER enforcement on `max_retries` / `retry_budget` (no retry substrate — retries+hedging is a separate future family candidate), `max_connection_pools` (HTTP/2 multiplexed-pool semantics), `per_host_thresholds` — emit-0 surface parity, the outlier local-origin precedent (AMEND-OD3-4 shape).
- **Fork 2 — ENFORCEMENT ARCHITECTURE (→ ADR-0248):** a `circuitBreaker` struct on `Cluster`, per-priority atomic counters (`openConns`/`activeRequests`/`pendingRequests`) parsed from `Thresholds[]`; NON-BLOCKING try-acquire/release (fail-fast, never queue) at the lifecycle points — `max_requests` try-acquire at request admission (router, after cluster pick) → release at `RecordUpstreamResult`/stream-end; `max_connections` try-acquire at `Dial` → release on upstream-conn close; `max_pending_requests` binds while a request awaits a connection (saturated `max_connections`). On exhaustion → immediate **503 upstream-overflow (UO flag)** + increment the matching cluster-level overflow COUNTER; the `circuit_breakers.<priority>.*_open` GAUGE reads 1 while at/over limit.
- **Fork 3 — PER-PRIORITY + STAT SURFACE:** nothing routes by `RoutingPriority` yet → ENFORCE DEFAULT-only, but REGISTER BOTH `circuit_breakers.default.*` + `circuit_breakers.high.*` trees for surface parity (HIGH never binds, emits 0). Sketch: **+10** gauges (5 names × 2 priorities: `cx_open`/`cx_pool_open`/`rq_open`/`rq_pending_open`/`rq_retry_open`) + **~3–4** cluster-level overflow counters (`upstream_cx_overflow`/`upstream_rq_pending_overflow`/`upstream_rq_retry_overflow` + the exact `max_requests`-overflow counter) → **~1149 → ~1162**. `track_remaining`/`remaining_*` DEFERRED. The EXACT overflow-counter→budget mapping is a SPEC-time EMPIRICAL PIN vs the contrib reference (`contrib-v1.37.2`) over the docker bridge (`reference_docker_probe_bridge_network`) — NOT hard-committed in the BRAINSTORM.
- **Fork 4 — DIFFERENTIAL (deterministic concurrency):** a blocking backend (`BackendKind #36`, `BlockingHoldResponder`) + release barrier on a shared bridge-network backend both sides hit; fixture(s) `0074` (+ possibly `0075`): fill the budget with N held-open concurrent requests, fire the N+1th → assert cross-side **503 UO + `circuit_breakers.default.rq_open == 1`** + the overflow-counter delta, then release. Deterministic / flake-free over the 20-run gate (NO sleep/timing window — the `reference_differential_band_sigma_margin` lesson). Sketch: **+1–2 fixtures** (75 → 76/77), BackendKind 35→36, possibly **+1 fuzzer** (CB-config parse, 42→43). Exact fixture split (one multi-assert vs per-budget) pinned at SPEC.

**Anticipated ADRs:** ADR-0248 (enforcement architecture + per-priority surface), possibly ADR-0249 (the defer-enforcement posture for retry/pool/per-host budgets). **New parse-reject arms:** negative/zero budget guards as the proto admits, byte-stable per ADR-0080 — exact arms pinned at SPEC. **DEFERRED items (→ BRAINSTORM §8):** `max_retries`+`retry_budget` enforcement (blocked on the retries+hedging family), `max_connection_pools` (HTTP/2 multiplexed pools), `per_host_thresholds`, `track_remaining`/`remaining_*`, HIGH-priority binding (blocked on priority routing).

**The rest of the family landscape (for phase 42+, NOT phase 41):** the Upstream-robustness family STILL has 2 candidates after circuit breakers — **retries + hedging** (`RouteAction.retry_policy` / `request_mirror_policies` / hedging) + **per-protocol connection pooling** (`Cluster.{typed_extension_protocol_options, http_protocol_options}` pool tuning); phase 39 also UNBLOCKED 3 health-gated Load-balancing candidates {locality-weighted LB, priority load balancing, panic thresholds}. The human picks among these at the NEXT new-subject BRAINSTORM (after phase 41 lands) — do NOT pre-decide that one.

## Honor throughout (the BRAINSTORM discipline)

- **Lifecycle = one stage per session:** the BRAINSTORM (`superpowers:brainstorming` — explore the subject/scope/split + verify the proto in the EXISTING module cache + sketch the architecture/differential/ADR/count envelope; the SPEC follows in a later session). `feedback_git_worktrees` (any isolated work in a worktree) · `feedback_subagents_no_push` (task subagents commit locally only; the controller squashes + pushes at stage-close) · `feedback_push_to_origin` (after a clean local merge on master with tests green, push without asking).
- **ADR/process:** ADR-0044 (the §Context drafts at the SPEC, the §Decision/§Consequences bodies land IN-PLACE at the IMPL) · ADR-0045 (the split-gate) · ADR-0052 (the six-gate at the IMPL) · ADR-0080 (envoy-go's own byte-stable rejects) · ADR-0106 + `reference_roadmap_split_phase_row_done` (a split-phase row flips `done` only when ALL legs land; NO parent rollup) · ADR-0227 (`contrib-v1.37.2`).
- **At the BRAINSTORM:** `reference_docker_probe_bridge_network` (any live reference-Envoy probe runs over a shared Docker bridge network + a STRICT_DNS backend hostname; verify the decode ran) · `reference_proto_roster_extraction_digits` (digit-inclusive regexes when scraping a proto/stat roster).

## Read first (in order)

1. THIS FILE — the post-phase-40.3-IMPL cold-start (the phase-40 family COMPLETE; the next step is the IN-FLIGHT phase-41 circuit-breakers BRAINSTORM — subject picked, the four forks settled, design presented — pending the human's go-ahead, then write `docs/envoy-go/phases/41-circuit-breakers/BRAINSTORM.md`).
2. `docs/envoy-go/STATE.md` — active-phase `phase 40.3 (outlier-detection-statistical) IMPL done`; the count footer (1149 / 75 / 42 / 35 / ADR-0247, next-free ADR-0248); the lifecycle routing (next-skill = a FRESH BRAINSTORM for a new subject).
3. `docs/envoy-go/ROADMAP.md` — row 40 `done` (the 40.1/40.2/40.3 sub-phase completion column + the 40.3 IMPL-DONE as-built record); the §9 family headings + the open Upstream-robustness / Load-balancing candidate rosters (the BRAINSTORM-subject landscape).
4. `docs/envoy-go/phases/40.3-outlier-detection-statistical/{SPEC,PLAN,PROGRESS,README}.md` (the durable phase-40.3 record) + the phase-40 parent `docs/envoy-go/phases/40-outlier-detection/{BRAINSTORM,README}.md` (the family charter) — for the precedent shape of the NEXT BRAINSTORM.
5. `docs/envoy-go/DECISIONS.md` (the ADR-0245/0246/0247 outlier triad + the process ADRs) + `docs/envoy-go/BEHAVIOR_CONTRACT.md` (the `### Cluster — passive health (outlier detection)` subsection + the stat-surface block now at 1149).
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`.
