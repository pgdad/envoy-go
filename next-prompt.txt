=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **PHASE 92 (`h2-response-header-validation`) IS OPEN AND MID-LIFECYCLE. ROW 92 READS `in-progress` AT `ROADMAP.md:154` AND `want` IS 124.** ⇒ **THERE IS BANKED MID-LIFECYCLE WORK. DO NOT SELF-PICK. THIS SESSION RUNS THE PHASE-92 SPEC (lifecycle-state 1 -> 2).** ⚠️ **THE SENTINEL DOES NOT FIRE — check (1) prints `NOT DONE: row 92`, check (2) prints SIX, check (3) is SILENT. TWO checks block it. DO NOT create `stop`.** ⚠️ **MEASURE ALL THREE YOURSELF BEFORE DOING ANYTHING ELSE; do not take this file's word for it.**

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE. This form parses FIELDS instead of matching a pattern, and
#     asserts the row DENOMINATOR so a deleted row is itself an error.
awk -F'|' -v want=124 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **CHECK (1) IS NO LONGER SILENT — THE OPEN-ROW BOARD SHAPE.** It prints `NOT DONE: row 92`, the row phase 92 just opened. **Do NOT read that as breakage and do NOT "fix" it by flipping row 92** — the row flips `done` at the phase-92 IMPL, not before. Run BOTH doctoring NCs EVERY SESSION anyway:
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md.
```
⚠️ **NC-A NOW READS **TWO** LINES** (`NOT DONE: row 62` AND `NOT DONE: row 92`), because row 92 is legitimately open. **The one-line expectation was the ALL-`done` board's shape and DOES NOT CARRY.** ⚠️ **This is the THIRD distinct NC-A shape in four stages** (two-line at the phase-91 PLAN, one-line at the 91 IMPL, two-line again now). **Read it yourself; never inherit the expected shape.**
```sh
# the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```
Also NC the DENOMINATOR (`want=123` on the real file must print `GATE FAIL: examined 124 data rows, expected 123` **alongside** the `NOT DONE: row 92` line — ⚠️ **on this board NC-B is NO LONGER a single-line result either**).

⚠️ **AND WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -oiE '-family row'` fails with `grep: amily row: No such file or directory` and the surrounding arithmetic prints `base=0 now=0 delta=0`, **which reads exactly like "no change."** **Always pass `--` before the pattern**, and the counts are form-dependent: `grep -c` counts LINES, not occurrences. **A gate that reads zero on BOTH sides is not evidence of invariance.**

⚠️ **THE ARM-A MALFORMED-ROW FIGURE RECONCILES ONLY UNDER AN ESCAPE-AWARE FIELD COUNT — RE-VERIFIED AT THE PHASE-92 BRAINSTORM, WHERE IT STILL READS *TWO*.** The malformed rows are ids **57** (`NF=9`) and **69** (`NF=10`). ⚠️ **A NAIVE `awk -F'|'` `NF != 8` FORM READS *SEVENTEEN*** — legitimately ESCAPED pipes (`\|`) split as fields. **Strip `\|` BEFORE splitting.** A gate asserting `== 0` FAILS on pre-existing content; assert against the known-2 baseline. ⚠️ **Row 92 field-counts to NF=8 under BOTH forms — verified before committing, and re-verified after a mid-stage correction to the row's own text.**

⚠️ **AND DO NOT SUBSTITUTE A "REASONABLE EQUIVALENT" FOR CHECK (1)'s AWK.** A measurement agent once tried `NF>=8 && $2 ~ /^ *[0-9]+ *$/`: it printed **92**, not the true denominator, because the naive split of escaped-pipe rows moves `NF` and the numeric `$2` anchor then rejects rows the canonical form accepts. **Use the form above verbatim.**

**SENTINEL RUN MECHANICALLY AT THE PHASE-92 BRAINSTORM (2026-08-25), BEFORE AND AFTER the row-92 insertion.** ACTUAL output, recorded not predicted:
- **BEFORE** (`want=123`) — **(1) SILENT** · (2) SIX at `:201 :207 :213 :223 :229 :237` · (3) SILENT.
- **AFTER** (`want=124`) — (1) **`NOT DONE: row 92`** · (2) **SIX** at `:202 :208 :214 :224 :230 :238` · (3) **SILENT**.
- **All four NCs fired:** NC-A **ONE-line** `NOT DONE: row 62` on the pre-insertion board with `NC LANDED? [ in-progress ]` **inspected FIRST** · NC-B `want=122` => `GATE FAIL: examined 123 data rows, expected 122` **only** · NC-C residual **2 -> 0** => `NEVER OPENED: gRPC`, WASM correctly silent · NC-D long **5** / short **1** / union **6**.
- ⚠️ **THE SIX WINDOWS' CONTENT IS INVARIANT ACROSS THE +1 SHIFT — MEASURED, NOT ASSUMED.** Each shifted line's md5 is byte-identical to its pre-insertion self, and **the comparator was NEGATIVE-CONTROLLED** (`:201` vs a non-corresponding `:203` produce different digests), so the match is not vacuous.
⇒ **THE SENTINEL DOES NOT FIRE — TWO checks block it. `stop` WAS EVALUATED AND DELIBERATELY NOT CREATED** (verified absent at the git root AND in the stage worktree).
- ⚠️ do NOT "fix" the six and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases; the candidate sentences are a WINDOW onto a larger deferred backlog, not an inventory of it. ⚠️ **Cite ~42 only as "sentence-visible", never as the inventory.**

==============================================

## STATUS AS OF 2026-08-25: PHASE 92 (`h2-response-header-validation`) **OPEN AT lifecycle-state 1**. **THIS SESSION RUNS THE PHASE-92 SPEC.**

**What the 92 BRAINSTORM landed (2026-08-25, base master `47e44004`, branch `phase-92-brainstorm`, ONE squashed commit).** **DOCS ONLY — ZERO production `.go`, ZERO test `.go`** (gated by `git diff --stat master -- '*.go'` => EMPTY). `BRAINSTORM.md` created (**572 lines**); phase dirs **132 -> 133**. `ROADMAP.md` **241 -> 242 lines / 123 -> 124 data rows**, `git diff --numstat` = **`1 0`** (a PURE INSERTION, matching the phase-91 BRAINSTORM's own numstat), `want` **123 -> 124**. `STATE.md` rolled IN PLACE (`9 9`, 63 lines, unchanged); `STATE_HISTORY.md` **522 -> 524** (`2 0`), **strict guard `^- \*\*prior active-phase:\*\*` STAYS 163, DELTA 0**. **`DECISIONS.md` and `BEHAVIOR_CONTRACT.md` BYTE-UNTOUCHED** — a BRAINSTORM adds no ADR; tail STAYS **ADR-0313**, next-free STAYS **ADR-0314**, `^---$` STAYS **216**, the strict `PROPOSED` guard reads **0** and the ADR-0231 decoy still reads 1.

### ⚠️ THE CHARTER

**On the HTTP/2 downstream leg, REJECT a response whose upstream header block carries a connection-specific or otherwise malformed field, instead of laundering it onto the downstream stream.** This is the **ENCODE-direction mirror of `h2.IsIllegalH2RequestHeader`**, which row 89 landed on the DECODE direction and wired into `reconcileH2DecodeDelta`. The request side has the guard; the response side has none.

**Mechanism, three symbols (cite BY SYMBOL — line anchors drift):** `h2.(*ClientConn).onResponseHeaderBlock` validates the **TRAILING** block only (`validateResponseTrailers`) and stores the **LEADING** block verbatim; `router.doH2ClusterAction` strips only `:`-prefixed names; `hcm.writeH2Reply` applies **no RFC 9113 §8.2.2 filter**. ⇒ **no validation anywhere on the leading-block encode path.**

**REPRODUCED BY EXECUTION at `47e44004`** — raw-framer client with ONE `hpack.Decoder` per connection and `ReadMetaHeaders` deliberately UNSET, a purpose-built raw h2c backend with byte-level control of the response block, positive controls FIRST and LAST, **three byte-identical reproductions**, and a **DIRECT-TO-BACKEND discriminating negative control** (without which "the subject stripped it" and "the instrument cannot see it" are indistinguishable):

| upstream response carries | envoy-go | reference `contrib-v1.37.2` |
|---|---|---|
| `connection: keep-alive` | forwarded verbatim, **200** | **502** `reset reason: protocol error` |
| `transfer-encoding: chunked` | forwarded, 200 | **502** |
| `keep-alive: timeout=5` | forwarded, 200 | **502** |
| `upgrade: websocket` | forwarded, 200 | **502** |
| `proxy-connection: keep-alive` | forwarded, 200 | **502** |
| uppercase name `X-Upper-Case` | lowercased, 200 | **502** |
| duplicate `content-length` | both forwarded, 200 | **502** |

⚠️ **ARM 7 IS THE ONE A CODE-READING MISSES** — `writeH2Reply` lowercases every name, so the downstream wire is *syntactically legal* H/2; the divergence survives because the **reference rejects the UPSTREAM message**. A fix aimed only at downstream byte-legality leaves it divergent.

**PARITY arms, recorded so they are never re-probed:** header value with leading/trailing OWS · empty header value · legal trailers (`x-trailer`, `grpc-status`, wire order preserved).

### ⚠️ WHAT THE SPEC OWES — eight items, §7 of the BRAINSTORM

1. **Price the SIX NAMED GAPS the `+44` floor does not cover, BY COMPILING PROTOTYPE:** the stat surface (does a rejected response book `upstream_rq_5xx` or a protocol-error counter, and does the reference's 502 book anything the subject does not — **UNMEASURED**) · the error text / `%RESPONSE_CODE_DETAILS%` analogue · whether the guard belongs behind a `stream_error_on_invalid_http_messaging`-style **posture flag** (ADR-0312 §Context ¶4 measured that the reference's H/2 request-side reject IS posture-dependent) · **arm 7's asymmetry** · the **trailing-block relationship** (`validateResponseTrailers` already exists — share its body or stand beside it, and prove no trailer behaviour changes) · the **differential surface**.
2. **Decide the reject POSTURE** — 502 (measured parity) vs stream reset — and pin it to the mechanism in item 3, not to preference.
3. ⚠️ **A DESIGN CONSTRAINT THE PROTOTYPE ALREADY DISCOVERED, do NOT re-derive it: the new error MUST NOT carry the `h2.ErrMalformedTrailers` sentinel**, or `router.doH2ClusterAction` takes its **stream-reset** arm instead of its **502** arm — and it is the 502 arm that produces reference parity.
4. **Decide the differential question explicitly**: extend `0004` vs mint `0120` vs unit-only. ⚠️ Minting has **three registration gates and the SECOND fails as a SILENT PASS** — the runner `t.Skipf`s an unregistered fixture and **no fixture-count gate exists anywhere in the tree**. **ASSERT THE FIXTURE SET**, never merely that the suite was green.
5. **Say yes or no, explicitly, to a fuzz target** for the new validator (`55 / 48` today).
6. **Decide in writing** whether to correct the wrong in-code 1xx claim (below) in passing — it is in the file being edited — or leave it and record it. **Do not absorb it silently either way.**
7. **Name the guard's negative controls BEFORE writing them.** ⚠️ At the phase-91 IMPL **three of the row's own new pins were non-discriminating and ALL THREE PASSED WHEN WRITTEN**.
8. **Re-derive every count and every cite at the SPEC's own tip.**

⚠️ **A STRUCTURAL FACT THAT BOUNDS THE CHARTER, MEASURED:** an H/2 **downstream** always drives an H/2 **upstream** (`asRouterActionH2` -> `H2ClusterAction` -> `AcquireH2Stream`, unconditionally). **There is no H2-in/H1-out path**, so the charter is closed under one codec pair and the SPEC owes no H1-upstream arm.

### ⚠️ THE COST FLOOR, AND WHY IT IS SHORT

**`+44 / -0` in ONE file and ONE symbol** (`internal/filter/hcm/h2/client.go`) from a prototype **BUILT, RUN and REVERTED** (`sha256sum -c` => `OK`), **re-driven on the wire** so arms 2-7 and 10 all became 502 with both controls still green. ⚠️ **A LOWER BOUND. `reference_measured_prototype_is_a_lower_bound` has fired FIVE CONSECUTIVE ROWS (87-91) and the cause is always UNDER-ENUMERATION, not estimation error.** Row estimate `~+44-120` production / `~+150-400` test — **AN ESTIMATE, NOT A MEASUREMENT.**

### ⚠️ NO EXISTING TEST PINS THE DEFECT — AND THE GREEN IS PROVEN NON-VACUOUS

Baseline `./internal/filter/hcm/... ./internal/filter/http/router/...` => **655 `=== RUN` / 0 FAIL**; **with the fix applied, still 655 / 0**; the second prototype leaves `./internal/...` at **70/70 packages ok**. ⚠️ **That green is NOT vacuous, and it was proven so by a REACHABILITY CONTROL rather than by a grep:** a `panic()` injected at the site gives rc=1, one hit, `--- FAIL: TestChainIntegration_H2_DirectResponseHappy`. **The site IS executed; the suite simply never asserts what it writes** — assertion blindness, not dead code, the same distinction ADR-0312 §Consequences (ii) had to make for row 90. ⇒ **the row ships with ZERO inherited coverage and must bring its own guard.**

### ⚠️ FIVE CANDIDATES BANKED WITH MEASURED COSTS — a taker re-derives nothing

1. **The `*ClientConn` LIFETIME-CTX DEFECT — the strongest banked candidate.** `NewClientConn` derives the pooled connection's **lifetime** ctx from the **dialing caller's** ctx, and `(*ClientConn).Closed()` is literally `cc.ctx.Err() != nil`. A per-attempt cancel therefore **tears down a pooled connection other requests are multiplexed on**, and the pool never reaps it. Measured: `cc2.Closed()==true` while request 2 still holds a stream (control: `false`); orphan FD **+100 over N=50** vs **+2** uncancelled. Mechanism: `makeRelease` evicts+Closes a `Closed()` conn only on the **last** in-flight release, so a cancel landing after `inFlight` reached 0 is never followed by any release, and `findStreamHitLocked` merely **SKIPS** the dead conn. Production cancel sites verified in `hedge.go` (`hedgeCtx` + `defer cancelAll()`) and `retry.go` (`attemptCtx` from `per_try_timeout`). `context.WithoutCancel` is available (`go 1.23.0`, toolchain go1.26.7) and has **ZERO** uses in the tree. ⚠️ **ITS END-TO-END REACHABILITY THROUGH THE FILTER CHAIN WAS NOT EXECUTED — that is the first thing a taker owes**, and a measured *"the blast radius is fd/permit exhaustion only, not request failure"* would legitimately SHRINK the charter. The `max_connections` permit question is likewise UNMEASURED.
2. **HTTP/1.1 with no `Host`** — subject **200 forwarding a literal empty `Host: `**, reference **400 `missing_host_header`**, and **A2 (empty) / A3 (whitespace) are 200 on BOTH sides**, so the divergence set is exactly ONE arm. Fix priced **`+90 / -0`, ONE file, ZERO new imports** (`bytes` already imported), package green at RUN=322. ⚠️ **The smuggling hazard phase 91 used to reject it was DISCHARGED BY MEASUREMENT:** prototype v1 (peeking only what is buffered) is **EVASIBLE** — a 250 ms head split makes the guard silently not fire — while v2 (growing the peek to the blank line, CRLF **and** bare-LF) held across six adversarial arms incl. 1-byte fragmentation and a decoy `X-Not-Host:`. ⚠️ **AND THE FIX SITE PHASE 91 NAMED IS ALSO WRONG:** `serveOneRequest` receives the **PARSED** `*http.Request` and cannot reach the raw head; `runConnection` holds the `bufio.Reader`. The **pin** half is **`+7` lines** (not "roughly a dozen") and is a **PROVEN GUARD**. ⚠️ `missing_host_header` is **invisible at `--log-level info`** — it needs `%RESPONSE_CODE_DETAILS%` in an access log.
3. **The `content-length` rewrite — THREE-LEGGED.** `writeH2Reply` rewrites `content-length` to `len(body)` **method-blind and status-blind**: a `304` with `content-length: 42` ships `0` (reference preserves 42); a bodyless 200 with `content-length: 5` — the HEAD shape — ships `0` (reference preserves 5); a mismatched `999` over a 5-byte body is laundered into a well-formed 200 where the reference forwards 999 then RST_STREAMs. The deletion prototype is `0 3` and **does NOT reach parity** (the reference additionally rejects a `204` carrying CL and resets on under-run). ⚠️ **The identical rewrite exists on ALL THREE codecs** — `internal/filter/hcm/codec.go` (H1), `h2dispatch.go` (H2), `h3dispatch.go` (H3).
4. **1xx interim responses** — subject **RST_STREAM(INTERNAL_ERROR)**, reference **delivers the 200**. ⚠️ **The in-code parity target is REFUTED: `client.go` states as a named non-goal *"the reference FORWARDS 1xx"* — MEASURED on `contrib-v1.37.2` at default HCM config, it does NOT, it SWALLOWED both the `103` and the `100` block and delivered only the final 200.** A taker must design against *drop-and-deliver*, not *forward*.
5. **`ssl.connection_error` — the CHEAPEST IN-WINDOW candidate.** The only strong candidate **inside a live sentinel window** (`:224`, item 2 of 3) with the reference side already measured. `connection_error` occurs in `*.go` **ONLY inside comments**; the `switch classifyHandshakeErr(err)` in `serveConnection` has exactly **TWO** cases and **no `case outcomeOther:`**, so five classified failure shapes increment nothing. Under 20 production lines, in a file whose shape was landed twice (phases 74, 75), and **it would NARROW a sentinel window at row-done** — which no maintenance row can do.

### ⚠️ WHAT THIS BRAINSTORM REFUTED BY EXECUTION — do NOT re-inherit the old wording

1. ⚠️ **ADR-0313 §Consequences (viii) IS FALSE, AND IT WAS THIS FILE'S OWN "STRONG SMALL CANDIDATE".** The recorded *"pre-existing `net.Conn` leak at the three `NewClientConn` error paths"* does not exist. The grep is true; the conclusion is not. `h2ConnFromDialed` (`internal/cluster/dial_h2.go`, BY SYMBOL) does `_ = wrapped.Close()` on **any** `NewClientConn` error under a doc comment stating that ownership contract in terms. **Measured with a load-bearing leak arm:** production path FD delta **0** over N=200 with the peer observing close **205/205**, against a deliberate-leak arm reading **+200** and **0/205** on the SAME instrument. **And there are FOUR error paths, not three** — the fourth is the `<-ctx.Done()` arm of the SETTINGS_ACK wait, measured clean on both axes (goroutine delta 0 over 100, instrument shown to read +100 on a real leak). ⇒ **a row chartered on (viii) would have had nothing to fix.** Class: `reference_code_comment_not_evidence` — **walk the call graph one layer UP.**
2. ⚠️ **THIS FILE'S OWN ROUTING INSTRUCTION WAS WRONG.** The prior edition directed *"open row 92 at the SPEC, not the BRAINSTORM."* **MEASURED: the phase-91 BRAINSTORM commit carries `ROADMAP.md` at `--numstat` `1 0` — a pure insertion registering row 91 `in-progress` — and the phase-91 SPEC commit touches `ROADMAP.md` NOT AT ALL.** It also contradicts `BOOTSTRAP_PROMPT.md` §5 state 0. **The BRAINSTORM registers the row.** Following the inherited instruction would have desynced `STATE.md` from `ROADMAP.md` for a whole stage.
3. ⚠️ **THE SELF-PICK INVENTORY IS 40, NOT 38.** `:202` 7 · `:208` 10 · `:214` **9** (paren artifact) · `:224` 3 · `:230` 8 · `:238` **3** (comma-delimited; a ` + ` split reads 1). **The inherited 38 totals `:238` as 1 while its own enumeration names THREE — arithmetically inconsistent with itself.** The six windows are family-charter **paragraphs** BELOW the data table.
4. ⚠️ **THE PHASE-87 "STALE ANCHOR" DOCUMENTARY DEFECT DOES NOT EXIST.** `phases/87-.../PROGRESS.md` records that `BEHAVIOR_CONTRACT.md`'s riders cite ``ADR-0052 `:1821` `` and that *"line 1821 is retry-policy prose"*. **Measured: `DECISIONS.md:1821` reads the ADR-0052 mandate verbatim — exactly what the riders cite it for.** What IS retry prose is **`BEHAVIOR_CONTRACT.md:1821`**. The finding resolved an ADR line anchor **against the wrong file**, and is itself an instance of the class it claimed to find.
5. ⚠️ **PHASE 91's REJECTION OF THE H1 no-`Host` CANDIDATE IS PARTLY REFUTED** (see banked candidate 2). Also: its *"converts ONE divergence into THREE"* is **TWO** — and that inflation was minted in **this stage's own agent brief**, caught by the agent, and is recorded rather than buried.

### ⚠️ FOUR FAIL-UNSAFE GATE FORMS FOUND THIS STAGE — all by execution

1. **`awk split(s, a, " + ")` treats the separator as a REGEX.** ` + ` means *two-or-more spaces*, which occurs nowhere in these lines, so it reads **1 on ALL SIX windows** — a uniform-looking fail-unsafe zero. Use `sed 's/ + /\n/g' | wc -l`.
2. **`grep -cE '^\t_ "'` reads 0 where `grep -cP` reads 123.** GNU grep's **ERE has no `\t` escape** — it matches a literal `t`. The blank-import gate form as WRITTEN in the standing record is fail-unsafe under `-E`. Use `$(printf '\t')` or `-P`.
3. **`ls test/fixtures/ | grep -cE '^[0-9]{4}-'` reads 119, not 121** — `0007a-cors` and `0007b-iteration-probe` carry a LETTER between the digits and the hyphen. Use `ls -d test/fixtures/*/ | wc -l`.
4. ⚠️ **THIS STAGE'S OWN §5 PROVENANCE CLAIM WAS FALSE AS FIRST WRITTEN AND ITS OWN NC CAUGHT IT PRE-COMMIT.** It asserted that a grep of all six windows for `response header|8.2.2|connection-specific|encode` returns **0**. It returns **1**, at `:208` — the bare token `encode` matching **`RunEncodeTrailers`**, an unrelated filter-hook candidate. Three of four terms do return 0 and the conclusion survives, but **the claim had to be restated with its match disclosed.** ⇒ **a provenance-absence claim is only as good as the breadth of the selector that establishes it. Run the control that shows what was MATCHED, not only the count.**

### ⚠️ RECORD DEFECTS FOUND IN PASSING — RECORDED, deliberately NOT fixed

- ⚠️ **`STATE.md` §Project counts is STALE and SELF-CONTRADICTS §Current, with NO label saying so.** Measured at this tip: it says fixtures **119** (true **121**), ADR tail **ADR-0298** / next-free **ADR-0299** (true tail **ADR-0313**, next-free **ADR-0314**), and its whole block is labelled *"at this phase-76 IMPL close"*. **DO NOT ANCHOR ON IT.** A one-edit fix for a taker; deliberately out of a BRAINSTORM's scope.
- `internal/wasm/dynamic_stats.go` says *"the Task 17 wiring will ALSO increment two counters ... (counter wiring deferred)"* **fifteen lines above the code that already does it**.
- `Listener.stat_prefix` is parsed and silently discarded — **0** `GetStatPrefix()` consumers under `internal/listener/` + `internal/bootstrap/`, against a firing control of **26 lines across 17 files**. SMALL fix, MEDIUM blast radius (renames every listener-scope stat, moves fixture goldens).
- ⚠️ **A ONE-SHOT (non-keep-alive) backend makes envoy-go reuse a DEAD pooled upstream conn and return 502** instead of re-dialing, producing an alternating 200/502 pattern that **reads exactly like a Host divergence**. Any future H1 probe must use a keep-alive backend. Whether that stale-pool 502 is a real robustness gap is **UNPROBED** — and it rhymes with the H2 pool mechanism in banked candidate 1.

## ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + ALL FOUR NCs YOURSELF** and record ACTUAL output. ⚠️ **Check (1) now PRINTS `NOT DONE: row 92` and NC-A now reads TWO lines. Both changed at this stage; neither shape carries.**
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** The 91 BRAINSTORM refuted five of seven ADR-0312 follow-ons; the 91 SPEC refuted seven; the 91 PLAN refuted ten; the 91 IMPL refuted four more; **this BRAINSTORM refuted its own inherited pick, its own routing instruction, the inventory total, a recorded documentary defect that does not exist, and one of its own claims pre-commit.** Do the same.
3. **LINE ANCHORS DRIFT; SYMBOL ANCHORS DO NOT — RE-VERIFY EVERY CITE AT YOUR TIP.** ⚠️ **PATHSPEC-SCOPE every symbol assertion.** ⚠️ **THE RECEIVER NAME IS PART OF THE ANCHOR.** ⚠️ **A SYMBOL ASSERTION WHOSE RECEIVER IS WRITTEN `(cc *ClientConn)` MUST USE `grep -F`, NOT `-E`** — ERE reads the parentheses as a group and returns a **FAIL-UNSAFE ZERO**.
3b. ⚠️ **CHARACTER-CLASS AND ESCAPE SELECTORS ARE THE FAIL-UNSAFE FAMILY.** (i) `grep -cE '^\s+[a-z0-9./-]+ v[0-9]'` on `go.mod` reads **62**, not **67** — it drops modules whose path carries an UPPERCASE letter or an underscore. (ii) `grep -oE '[A-Za-z]+ +BackendKind = 38'` prints `GoawayResponder`, not **`H2GoawayResponder`** — a **digit-blind** class truncating an identifier. (iii) **`\t` in `-E` is a literal `t`** (this stage). (iv) **an over-anchored `^[0-9]{4}-` drops the two letter-suffixed fixture dirs** (this stage). **Prefer the structural form and always run a control that shows what was MISSED, not just what matched.**
3c. ⚠️ **`feedback_brief_citations_not_evidence` APPLIES TO INCIDENTAL FIGURES, NOT JUST HEADLINE CLAIMS — and it fired TWICE this stage, once in each direction.** An agent reported a firing control as **16** where it reads **26**; and the controller's own brief inflated a "TWO" into a "THREE" and **the agent caught the controller**. **Re-derive EVERY number you restate.**
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. ⚠️ **TWO SDS dial-budget flakes are live** — `TestSDSEndToEnd_FetchFailure_BootFailsClosed` (`internal/boot`) and `TestProvider_FetchInitialCertificate_Timeout` (`internal/xds`). ⚠️ **DO NOT reuse the `go list -deps` exclusion blanket — TRUE for `xds` (0), FALSE for `boot` (1).** Also live: the driver-owned receiver port race · `internal/cluster` outlier · `internal/httpclient` zero-value · the two 84.2-era flakes · the REFERENCE h2spec section-8 flip. ⚠️ **`TestServerConn_TinyWindowDelivery` IS NOT A FLAKE — it was a LIVE PRODUCTION DEADLOCK and phase 91 FIXED it. A recurrence is a REGRESSION of row 91.**
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only). Foreign containers left untouched throughout phases 90, 91 and 92: `infallible_booth`, `crazy_kare`, `golink-ai`, `quizzical_goldstine`.
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`.
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL.** ⚠️ At the phase-91 IMPL even that was not enough: an ablation was confirmed in the COMPILED BINARY BY DISASSEMBLY before its non-reddening gate result was believed. **When a gate fails to redden, disassemble before concluding the gate is blind.** **`gofmt -l` never exits non-zero — gate on OUTPUT.** ⚠️ **`golangci-lint` must be gated on OUTPUT too.** ⚠️ **`go test` WITHOUT `-v` PRINTS ZERO `=== RUN`** — `RUN=0` beside `RC=0` is a VACUOUS GREEN. ⚠️ **A `-run` selector matching nothing prints `[no tests to run]` and EXITS 0.** ⚠️ **On `-v` output, unanchored `grep -c 'FAIL'` reads 11 ON A FULLY GREEN TREE** — use `grep -cE '^(FAIL|--- FAIL)|^ *--- FAIL'`.
7b. ⚠️ **`grep -c` ON ZERO MATCHES EXITS 1 — capture the value (`v=$(… || true)`), NEVER chain on it with `&&`.**
7c. ⚠️ **A GREEN TEST RUN IS NOT EVIDENCE A SITE IS UNPINNED — IT MAY BE DEAD CODE.** This stage proved its green non-vacuous with a **`panic()` REACHABILITY CONTROL**. Run one before concluding "nothing pins this".
8. ⚠️ **`pgrep -f` / `pkill -f` MATCHES YOUR OWN SHELL AND KILLS THE TOOL CALL (exit 144)**. **Kill ONLY PIDs you captured yourself** (from `$!`), and verify residue with `kill -0`.
9. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-*`, `wt-*` or stage worktree should outlive its stage close** — expect only `master` (verify with `git worktree list`).
10. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** (a)/(b) differential **121 fixtures** + full `go test ./...`, gated on **`PIPESTATUS[0]`** and a **SET RECONCILIATION** (⚠️ **NOT `INNER_EXIT`, which DOES NOT EXIST**); (c) h2spec cite ONLY from your own run — ⚠️ **h2spec is MEASURED BLIND to burst-drain ordering, so never cite it as an ordering gate**; grpc-conformance deferred in writing; proxy-wasm 10/16; (d) fuzzers 55/48; (e) the ANCHORED panic gate `^panic:|DATA RACE|SIGSEGV` on every differential launch; (f) no REVIEW.md — standing departure.
11. ⚠️ **THE FIXTURE INVOCATION: `go test ./test/differential/ -run 'TestDifferential/0004-h2-routing' -count=1 -v`.** ⚠️ **Gate 2's failure mode is a SILENT PASS** — the runner `t.Skipf`s an unregistered fixture and **no fixture-count gate exists anywhere**. **ASSERT THE FIXTURE SET.**
12. ⚠️ **`-p` PUBLISHING WORKS; `--network host` IS WHAT DOES NOT.** **Host-gateway is `192.168.65.2`** (⚠️ `getent hosts` resolves it IPv6-ONLY — use `getent ahostsv4`); `--add-host=host.docker.internal:host-gateway` also works. ⚠️ A **STRICT_DNS** cluster whose name does not resolve **blocks listener init entirely**. ⚠️ **`clusters: []` is BOOT-REJECTED even when every route is a `direct_response`.** ⚠️ **envoy-go REJECTS `match.headers` outright**, ⚠️ **refuses `codec_type: HTTP2` without TLS unless `--allow-h2c`**, and ⚠️ **boots with `-c`, NOT `--config-path`** (a `--config-path` boot exits 2). ⚠️ **`ENVOY_TARGET.md` is at `docs/envoy-go/ENVOY_TARGET.md`, NOT the repo root.**
13. ⚠️ **A RAW-FRAMER PEER MUST HOLD ONE `hpack.Decoder` PER CONNECTION.** ⚠️ **Set `ReadMetaHeaders` to NIL when probing malformed shapes**, or `x/net` validates away the very thing you are measuring. ⚠️ **`0004` is TLS+ALPN-h2, NOT h2c.**
14. ⚠️ **`bash /dev/tcp` + `timeout cat <&3` INSIDE COMMAND SUBSTITUTION RETURNS SILENTLY EMPTY.** **Do not build an H1 probe on `/dev/tcp`.**
15. ⚠️ **GO DOES NOT FLAG UNUSED CONSTANTS.** Unused-symbol cleanup after a removal must be grep-driven, and the removed-literal NC must target the USE SITE, never the declaration.
16. ⚠️ **`golangci-lint`'s misspell runs in locale US.** **Sweep British spellings in `.go` comments BEFORE the gate.** ⚠️ **Markdown prose may use them freely; a `.go` comment may not** — check any code block you paste from a design doc.
17. ⚠️ **A GREEN RUN OF A CONTENTION-SENSITIVE TEST PROVES NOTHING.** Phase 91's subject cleared 5/5 uncontended while failing 9/32 contended on the SAME binary.
18. ⚠️ **NC EVERY NEW TEST, NOT JUST THE RISKY-LOOKING ONES.** At the phase-91 IMPL **three of the row's own new pins passed while being non-discriminating** — one structurally unable to fire, one vacuous, one a coin flip. **A test that passes is not thereby a guard.** ⚠️ **A poll-based leak guard's SLACK must be set from the measured NOISE FLOOR, never copied from a precedent.**
19. ⚠️ **AN ABSENCE CLAIM NEEDS A POSITIVE CONTROL AND A DISCRIMINATING NEGATIVE CONTROL.** This stage's headline finding is unfalsifiable without the direct-to-backend arm, and the `NewClientConn` refutation is unfalsifiable without the deliberate-leak arm. **Build the arm that would have shown the opposite.**

## ⚠️ PORT BANDING

`net.ipv4.ip_local_port_range = 32768 60999`. ⇒ **BAND YOUR PROBE PORTS BELOW 32768.** `21000-24999` is clear of both the ephemeral range and every reserved band. **Check availability with `ss -tan` (ALL states), never `ss -ltn`** — the listener-only form reads a fail-unsafe "free". **A `bind: address already in use` in a 4xxxx band is NOT evidence a sibling session is squatting.** The static fixture band (10000-19172) and the reserved bands (20000-31007, 11000-14999) are all BELOW 32768.

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (ugrep under the hood). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Reproduced at THIRTY-EIGHT separate tips.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **THE METHOD BINDS; carry no standing discrepancy figure and no standing absolute.** ⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS; print FILE NAMES, never a count.** ⚠️ **THE HARNESS `grep` STRIPS A LEADING `./` FROM PATHS.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files; use `-v --no-index`, and `git status` is the discriminating check). ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **`grep -c` COUNTS *LINES*, NOT *OCCURRENCES***.

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 92 (h2-response-header-validation)' --format='%h %s'   # 1 at this tip — the BRAINSTORM
git log --grep '^phase 91 (h2-framer-partial-frame-desync)' --format='%h %s'  # 4 — a CLOSED phase, use as a FIRING CONTROL
```
⚠️ **The count comments are LIVE FIGURES — VERIFY before relying.** Take the NEWEST line (`| head -1`). A fabricated slug must read **0**.

Execution style: **subagent-driven per `feedback_execution_style`**. Subagents commit LOCALLY on the ONE stage branch with EXPLICIT PATHSPECS; the controller squashes (`feedback_subagents_no_push`, `feedback_push_to_origin`). **Pure MEASUREMENT agents should commit NOTHING and prove their tree clean.** ⚠️ **PARALLEL AGENTS IN ONE WORKTREE WORK IF AND ONLY IF THEIR PATHSPECS ARE DISJOINT** — ⚠️ **and the phase-91 and phase-92 pattern was PER-AGENT DETACHED WORKTREES pinned at a known commit**, which let four independent probes run without any of them seeing another's edits. ⚠️ **A CONTROLLER WRITING INTO A SHARED WORKTREE WHILE AGENTS RUN WILL SHOW UP IN THEIR `git status` GATE — hold doc edits until the agents finish, or give them their own tree.** ⚠️ **GIVE EACH PARALLEL AGENT ITS OWN PRIVATE SCRATCH DIR.** Per-task `gofmt` + `golangci-lint` on touched packages, **gated on OUTPUT**. ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root** — build with `-o` into scratch. ⚠️ **AND VERIFY WHAT A SUBAGENT REPORTS.**

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root — **observed repeatedly at every phase-88 through -92 stage**. Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` before any commit or gate run. ⚠️ **Shell VARIABLES do not survive between Bash calls** — re-export in every command.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — `git status --porcelain` AND `git diff --stat master` must show ONLY your intended edits. For a patched TRACKED file: `sha256sum` BEFORE patching, `git checkout -- <path>` after, `sha256sum -c` the capture — ⚠️ **and COMMIT FIRST, because `git checkout --` restores from HEAD and will wipe uncommitted work.** ⚠️ **The phase-92 BRAINSTORM did this FOUR times across four probe worktrees and every restore was verified `OK`.** ⚠️ **Also check the MAIN repo root** — one probe's mis-parsed `cd … && … &` dropped two transient files there; they were removed and the removal verified.

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads the phase-92 SPEC.** ⚠️ **§Project counts is STALE (phase-76 era: fixtures 119, ADR tail 0298) and SELF-CONTRADICTS §Current with NO label saying so — do not anchor on it.**
2. `docs/envoy-go/phases/92-h2-response-header-validation/BRAINSTORM.md` — **especially §3 (the mechanism and the `ErrMalformedTrailers` constraint), §6.1 (the six gaps the floor does not cover) and §7 (what you owe)**.
3. `docs/envoy-go/ROADMAP.md` — row 92 at `:154` as the OPEN row; the six windows at `:202 :208 :214 :224 :230 :238`.
4. `docs/envoy-go/DECISIONS.md` **ADR-0313** (COMPLETE) — §Consequences (ix)/(ix-bis)/(xi) carry transferable findings; ⚠️ **(viii) is REFUTED by phase 92 and (xii) restates it — do not inherit either.** Also **ADR-0312 §Consequences (xix)**, this row's provenance.
5. `BOOTSTRAP_PROMPT.md` §4-§7 (repo ROOT). **Phase-92 dir exists with `BRAINSTORM.md` and no `SPEC.md` ⇒ state 1 ⇒ the SPEC.**

## What the next session owes
**RUN THE PHASE-92 SPEC** (state 1 -> 2), producing `SPEC.md` in the existing phase dir ·
**RE-RUN the sentinel + all four NCs yourself** and record ACTUAL output (⚠️ **check (1) now PRINTS `NOT DONE: row 92` and NC-A now reads TWO lines**) ·
**DO NOT create `stop`** — two checks block it · **DO NOT self-pick** — row 92 is banked mid-lifecycle work ·
**DRAFT ADR-0314 §Context** and **RE-ARM the strict `^> **STATUS: PROPOSED` guard 0 -> 1, verified BY LINE AND ADR** (the ADR-0231 decoy also reads 1 and must be LEFT ARMED) ·
**PRICE THE SIX NAMED GAPS BY COMPILING PROTOTYPE** — the `+44` floor is a LOWER BOUND and `reference_measured_prototype_is_a_lower_bound` has fired FIVE CONSECUTIVE ROWS ·
**ROW 92 STAYS `in-progress`** — `ROADMAP.md` should be BYTE-UNTOUCHED at the SPEC (the phase-91 SPEC's own numstat was empty); `want` STAYS **124** ·
**REFUTE YOUR PREDECESSOR BY EXECUTION** — this BRAINSTORM refuted its own inherited pick, its own routing instruction, the inventory total, a documentary defect that does not exist, and one of its own claims pre-commit ·
`STATE.md` rolled IN PLACE with the oldest §Recent entry evicted by a **DIRECT DATE READ** (⚠️ **read the dates yourself every time — this stage found a THREE-WAY tie at 2026-08-22 broken by LAST LIST POSITION, the THIRD distinct tie shape in three stages; the shape does NOT carry**) and archived to `STATE_HISTORY.md` as **ONE INLINE LINE** whose label does NOT match `^- \*\*prior active-phase:\*\*`, so the strict guard stays at **163, DELTA 0** ·
`next-prompt.txt` rolled (`git add -f`) · ONE squashed commit, merged and pushed.
