START a fresh cold-start session for envoy-go — **the phase-40.2 (`outlier-detection-other-consecutive`: the gateway + local-origin consecutive detectors + the split accounting switch) IMPL is DONE, LANDED on master (squash commit `<SQUASH_SHA>`), and PUSHED to `origin/master`; the next lifecycle step is the phase-40.3 SPEC** — the FINAL leg of the phase-40 3-leg by-detector-class split: the STATISTICAL detectors (`success_rate_*` + `failure_percentage_*`) + a NEW per-interval cross-host mean/stddev aggregation runtime (the FIRST new outlier goroutine). Author the SPEC directly (the 40.1/40.2 precedent — the BRAINSTORM is DONE for the family; `superpowers:brainstorming` only if the 40.3 envelope wants re-opening), executing the §11 D-OD3-* empirical pins live against `envoyproxy/envoy:contrib-v1.37.2` per `reference_docker_probe_bridge_network`; anchor the ADR-0247 §Context draft per ADR-0044; FINAL ADR-0045 split-gate re-check. **NOTE: `<SQUASH_SHA>` is a PLACEHOLDER — the controller fills it after squashing the phase-40.2 IMPL branch into master.** Confirm the tip via `git log --oneline -5` (the substantive tip should be the phase-40.2 IMPL squash `<SQUASH_SHA>`, sitting on the phase-40.2 PLAN `104daf2`; the prior substantive tips are the phase-40.2 SPEC `2919a3a` and the phase-40.1 IMPL squash). As-built counts at the 40.2 IMPL: stat surface **1141** / fixtures **73** / fuzzers **42** / BackendKind tail **35** (`HTTP503Responder`) / DECISIONS tail **ADR-0246**, next-free **ADR-0247**.

## What just landed (phase 40.2 IMPL — the durable record is `docs/envoy-go/phases/40.2-outlier-detection-other-consecutive/{SPEC,PLAN,PROGRESS,README}.md`; read those, not this summary)

The 12-task TDD PLAN executed end-to-end subagent-driven (fresh subagent per task + two-stage review), the six-gate GREEN, the full **73-dir differential GREEN**. **ADR-0246** landed IN-PLACE (§Context from the SPEC §13 draft; §Decision/§Consequences bodies per ADR-0044). The 40.1 substrate (ADR-0245 — the `RecordUpstreamResult` seam + the ejection dimension on `hostHealth` + the `available` LB-pick predicate + the `consecutive_5xx` detector + the cap + lazy un-eject) was REUSED UNCHANGED.

**As built (the gateway + local-origin consecutive detectors + the split accounting):**
- **The gateway detector** (`recordExternal5xx`) — gateway-class 5xx `{502,503,504}`; active-by-default but DETECT-ONLY by default (`enforcing_consecutive_gateway_failure` default 0). **Gateway-checked-FIRST:** a gateway-class 5xx bumps `consecGw`; if it EJECTS it returns BEFORE the 5xx detector (`detected_consecutive_5xx` stays 0 — live-pinned); if it does NOT eject (the detect-only `0069`/`0070` path) it FALLS THROUGH so `consec5xx` also advances. A non-gateway 5xx resets `consecGw`.
- **The local-origin detector** (`recordLocalOrigin`) — connect/reset failures; active ONLY when `split_external_local_origin_errors: true`. split=true ⇒ a `LocalOriginErr` result feeds ONLY the local-origin detector (a completed external response resets `consecLO`); split=false ⇒ a `LocalOriginErr` maps to `recordExternal5xx(statusCode)` (the local-reply 502/503, always gateway-class).
- **The `tryEject(enforcing, enforcedCounter) bool` refactor** — the 40.1 inline eject extracted into a reusable helper shared by all three detectors; the 40.1 `consecutive_5xx` behavior preserved EXACTLY (the cross-multiplied cap `(ejected+1)*100 > cap*total`, the CAS once-only eject, the double-count `ejections_enforced_total` + the per-detector enforced, the detected-Inc-before-already-ejected-guard ordering). `record(ep, statusCode, localOriginErr)` is the 3-arg dispatch signature; `consecGw`/`consecLO` are the consecutive-streak counters on `hostHealth`.
- **The live `LocalOriginErr` seam** — the 40.1-reserved `UpstreamResult.LocalOriginErr` field made LIVE, populated at the FIVE router connection-failure sites (H1 acquire-503/write-502/read-502, H2 dial-502/roundtrip-502; the LIVE drivers only). The picked endpoint surfaced from the shared `Cluster.Dial`/`AcquireH1`/`DialH2` connect-failure paths (`AcquireH1` widened to `(*PooledH1Conn, Endpoint, error)`); `(Endpoint).IsZero()` (added because `Endpoint` carries a `Metadata` map → NOT comparable) is the no-host-no-attribution guard; the LB-pick-itself-failed path attributes to no host (no seam call); the H2 ctx-cancel path is a downstream cancel (no seam call).
- **+4 stats** (1137 → **1141**, registered unconditionally on any outlier cluster): `ejections_{detected,enforced}_consecutive_gateway_failure` + `ejections_{detected,enforced}_consecutive_local_origin_failure`. The 40.1 double-count EXTENDED — a gateway/local-origin eject bumps `ejections_enforced_total` too. Two new parse-reject arms (`enforcing_consecutive_{gateway,local_origin}_failure > 100`).
- **Fixtures `0070`/`0071`** (cross-side traffic-driven ejection; both `StatsAsserter`, poll-`ejections_active`-to-converge + warmup + delta-counter): `0070-outlier-detection-consecutive-gateway-failure` (cluster `c_gw`, `consecutive_5xx` HIGH so the 5xx detector can't fire; `PerHostBackendKind` + `HTTP503Responder` 35; cross-asserts `detected_consecutive_5xx == 0`) + `0071-outlier-detection-local-origin` (cluster `c_lo`, split=true, the `allocDeadPort` dead host, `BackendCount()==2`; cross-asserts `detected_consecutive_5xx == 0`). 2 `-count=1` deliberate breaks each; both 20/20 flake-free. The `0069` `StatsAsserter` WIDENED to cross-assert `ejections_detected_consecutive_gateway_failure` (the 40.1 AMEND-OD4 exclusion LIFTED; `0069` routing/ejection UNCHANGED). The split=false local-origin-as-5xx arm is a `record`-level unit test.

As-built counts: fixtures **71 → 73** (`0070`+`0071`); stat surface **1137 → 1141** (+4); BackendKind tail **35** (UNCHANGED — `0070` reuses `HTTP503Responder`, `0071` reuses `allocDeadPort`); DECISIONS tail **ADR-0245 → ADR-0246** (next-free ADR-0247); fuzzers **42** (UNCHANGED); ZERO new packages + ZERO new go.mod modules. ROW 40 STAYS `in-progress`.

## The next lifecycle step — the phase-40.3 SPEC (the FINAL leg)

Chartered by the phase-40 BRAINSTORM §1.4 / §8 (`docs/envoy-go/phases/40-outlier-detection/BRAINSTORM.md`): **40.3 = the statistical detectors (`success_rate` + `failure_percentage`) + a NEW per-interval cross-host mean/stddev aggregation runtime — the LARGEST leg and the FIRST new outlier goroutine.** Compute each host's success rate over `interval`, the cross-host mean/stddev, and eject hosts below `mean − success_rate_stdev_factor × stddev` (the success-rate detector) / above `failure_percentage_threshold` (the failure-percentage detector); gated by `success_rate_minimum_hosts`/`success_rate_request_volume` (+ the `failure_percentage_*` analogues). The proto fields: `success_rate_minimum_hosts`/`success_rate_request_volume`/`success_rate_stdev_factor` + `failure_percentage_threshold`/`failure_percentage_minimum_hosts`/`failure_percentage_request_volume` + the `*_local_origin_*` split variants + the per-detector `enforcing_*` knobs (all on the EXISTING `cluster.v3.OutlierDetection` — ZERO new go.mod module). Contrast with 40.1/40.2 (synchronous per-request detectors): 40.3 adds a WINDOWED runtime (a per-cluster per-`interval` sweep goroutine — the first new outlier goroutine; the seam already fires on EVERY request per the 40.1 design, so NO further router surgery). Anticipated: the 8 statistical `ejections_{detected,enforced}_{success_rate,failure_percentage,local_origin_success_rate,local_origin_failure_percentage}` stats (+ the legacy `ejections_total`/`ejections_consecutive_5xx`/`ejections_success_rate` deferred-departures may land); ≥1 fixture (a multi-host cluster where one host is statistically anomalous); 1 ADR (ADR-0247). With 40.3 the phase-40 by-detector-class split is FULLY CONSUMED and **row 40 flips `done`** (per the row-39 precedent + `reference_roadmap_split_phase_row_done`). After phase-40 phase-done, **3** Upstream-robustness candidates remain: {circuit breakers, retries + hedging, per-protocol connection pooling}.

**SPEC obligations (the 40.1/40.2 SPEC shape):** execute the §11 D-OD3-* empirical pins IN-SESSION live against `contrib-v1.37.2` per `reference_docker_probe_bridge_network` (a multi-host cluster where N−1 hosts succeed and 1 is statistically anomalous; `/stats` scrapes for the exact statistical stat names + the eject/no-eject boundary against `success_rate_stdev_factor`/`failure_percentage_threshold`/the `*_minimum_hosts`/`*_request_volume` gates; `--mode validate` reject probes; verify the request path `upstream_rq_total>0`); firm the AMEND-OD3-* block; anchor the ADR-0247 §Context draft in SPEC §13 (DECISIONS tail STAYS ADR-0246 at the SPEC); FINAL ADR-0045 split-gate re-check (the windowed runtime is the largest leg — re-confirm NO FURTHER SPLIT or split it).

## Honor throughout (the discipline)

- **Lifecycle = one stage per session:** the SPEC (author directly — the 40.1/40.2 precedent). `feedback_git_worktrees` · `feedback_execution_style` · `feedback_push_to_origin` (after a clean merge on master with tests green, push without asking) · `feedback_subagents_no_push` (the CONTROLLER squashes/ff-merges/pushes at stage-close).
- **Statistical-detector specifics:** the windowed runtime is the FIRST new outlier goroutine (the 40.1/40.2 detectors are synchronous per-request) — model the per-`interval` sweep + the cross-host aggregation + the lazy-vs-sweep un-eject interaction; `reference_health_check_propagation_warmup` (poll the gauge to converge + warmup-until-stable + delta counters; widen deadlines not assertions on flake) · `reference_docker_probe_bridge_network` (Docker bridge; verify `upstream_rq_total>0` ref-side) · `reference_fixture_workload_constant_desync` (single-source the workload constants) · `reference_differential_run_selector` (`-run 'TestDifferential/<NNNN>'`) · `reference_differential_break_protocol_count1` (`-count=1` on EVERY break run) · `reference_percent_cap_cross_multiply` (the reused 40.1 cap; the failure-percentage threshold is also a percent comparison — cross-multiply, do NOT truncate).
- **ADR/process:** ADR-0044 (the ADR-0247 §Context at the 40.3 SPEC; §Decision/§Consequences at the 40.3 IMPL) · ADR-0052 (six-gate) · ADR-0080 (byte-stable rejects) · ADR-0106 + `reference_roadmap_split_phase_row_done` (row 40 flips `done` when the 40.3 final leg lands) · ADR-0227 (`contrib-v1.37.2`).

## Read first (in order)

1. THIS FILE — the post-phase-40.2-IMPL cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 40.2 (outlier-detection-other-consecutive) IMPL done`; the count footer (1141 / 73 / 42 / 35 / ADR-0246, next-free ADR-0247); the lifecycle routing (next-skill = the 40.3 SPEC).
3. `docs/envoy-go/phases/40-outlier-detection/BRAINSTORM.md` (the phase-40 charter — the 3-leg split §1.4; the 40.3 statistical-detector scope §1.4/§8/§Q-split; the `OutlierDetection` proto roster §2.1; the windowed-runtime note §1.7 [the first new outlier goroutine]) + `docs/envoy-go/phases/40.2-outlier-detection-other-consecutive/{SPEC,PLAN,PROGRESS,README}.md` (the as-built 40.2 leg the 40.3 detectors sit beside) + `docs/envoy-go/phases/40.1-outlier-detection-consecutive-5xx/{SPEC,PLAN,PROGRESS}.md` (the ADR-0245 substrate — the seam + ejection dimension + `available` predicate + cap + lazy un-eject that 40.3 EXTENDS with the windowed runtime).
4. `internal/cluster/{outlier.go,health.go,cluster.go,manager.go}` (the 40.1/40.2 `outlierConfig`/`parseOutlierDetection`/`outlierDetector.record`/`recordExternal5xx`/`recordLocalOrigin`/`tryEject`/`registerStats` the 40.3 statistical detectors + the windowed sweep extend; the `RecordUpstreamResult`/`UpstreamResult` seam — already fires on EVERY request) + `test/fixtures/{0069,0070,0071}-*/driver/` (the cross-side traffic-driven-ejection StatsAsserter templates) + `test/differential/{fixture/fixture.go,runner_test.go}` (the `HTTP503Responder` 35 + `PerHostBackendKind` + `allocDeadPort`).
5. `docs/envoy-go/DECISIONS.md` (ADR-0245 + ADR-0246 [the substrate the 40.3 leg extends] + the process ADRs) + `docs/envoy-go/BEHAVIOR_CONTRACT.md` (the `### Cluster — passive health (outlier detection)` subsection + the "Phase 40.2 — 1137 → 1141" stat-mapping block 40.3 extends).
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`.
