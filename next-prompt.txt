START a fresh cold-start session for envoy-go — **the phase-36.1 (`load-balancer-ring-hash`) PLAN is DONE and LANDED on master; the next lifecycle step is the phase-36.1 IMPL** (`superpowers:subagent-driven-development` — execute the 10-task PLAN one bite-sized TDD task at a time, fresh subagent per task, two-stage review between tasks; the kafka-31/thrift-33/least_request-34/random-35 IMPL precedent). The substantive master tip is the phase-36.1 PLAN squash **`<<FILL-AFTER-SQUASH>>`** (`phase 36.1 PLAN: load-balancer-ring-hash — decompose the SPEC §10 36.1 spine into 10 bite-sized TDD tasks; resolve D-S36-1..7; the affinity proof is the aggregate-count modular invariant [subject per-backend count ≡ 0 mod 16]; ADR-0045 re-check → NO FURTHER SPLIT for 36.1; plan-document-reviewer GREEN`; pushed to `origin/master`; confirm via `git log --oneline -3`). It carries `docs/envoy-go/phases/36-load-balancer-ring-hash/PLAN.md` (the 10-task TDD plan) + README.md (status 2 → 3) + the STATE.md advance (active-phase → `phase 36.1 (load-balancer-ring-hash) PLAN done`; next → the 36.1 IMPL). The literal live master tip is this docs-only `next-prompt.txt` SHA-fill roll-forward commit (the substantive PLAN squash is its parent; confirm via `git log --oneline -3`); the substantive predecessor below it is the phase-36 SPEC squash `273579d`, and below that the phase-36 BRAINSTORM squash `ffb3a61`.

## What this session does — execute the phase-36.1 IMPL

The phase-36.1 PLAN is COMPLETE (`PLAN.md` — 10 bite-sized TDD tasks decomposed from SPEC §10's 36.1 spine; plan-document-reviewer GREEN). The next session EXECUTES it via `superpowers:subagent-driven-development` (per `feedback_execution_style` — subagent-driven, NOT inline): work in a fresh worktree (`feedback_git_worktrees`); a fresh subagent per task; per-task `gofmt -l` + `golangci-lint` on touched pkgs (`feedback_pertask_gofmt_lint`); subagents commit LOCAL-ONLY (`feedback_subagents_no_push`); the controller squash-merges + pushes at stage-close (`feedback_push_to_origin`, tests green). The full differential suite needs Docker + the contrib reference image on a bridge network (`reference_docker_probe_bridge_network`) — the controller runs that gate where Docker is present.

## The PLAN at a glance (10 tasks; do NOT re-litigate the design — the SPEC + PLAN settled it)

1 baselines/anchors gate (fixtures 62, fuzzers 42, surface 1116, BackendKind 33, DECISIONS tail ADR-0234; reject-text blast radius 3 sites; seam-consumer survey) + PROGRESS.md · 2 the pure-Go `xxHash64` (seed 0; XXH64 port — anchor vector `xxHash64("")==0xEF46DB3751D8E999`, the rest oracle-pinned at IMPL via `cespare/xxhash` in a SCRATCH module NOT added to go.mod) + `murmurHash2` (seed `0xc70f6907`) in new `hash.go` · 3 the seam EXTENSION (ADR-0235 — widen `loadBalancer.Pick(hashKey,hasHash)`; 3 incumbent policies gain ignored params [behavior-neutral, 62 fixtures byte-identical]; `WithHashKey`/`hashKeyFrom` ctx-carry in `cluster.go`; thread `Dial`/`AcquireH1`/`PickEndpoint`; exported `Cluster` surface BYTE-STABLE) · 4 the `ringHashLB` (ADR-0236 — Ketama ring `ceil(min/N)` entries keyed `xxHash64("addr:port_i")` sorted; binary-search first `>=` + wrap; no-hash rng fallback; default 3-host build = size 1026/min,max 342 matching the live reference; murmur arm; random-keys property test in lieu of a fuzzer) in new `ringhash.go` · 5 manager acceptance (`case Cluster_RING_HASH` + `parseRingHashLbConfig` two-layer gate [PGV `<=8388608` + runtime `min>max`, defaults 1024/8388608/XX_HASH] + NEW reject text `…, RANDOM, RING_HASH` + the `TestManager_Error_UnsupportedLBPolicy` doubly-hit retarget RING_HASH→MAGLEV + register the 3 `ring_hash_lb.*` gauges RING_HASH-only in `registerClusterMetrics`) · 6 the tcp_proxy `source_ip` plane (parse `TcpProxy.hash_policy`; `cluster.HashSourceIP(bare IP, no port)` + `WithHashKey` before `Dial(ctx)`; filter_state DEPARTURE-reject) · 7 the `0061-lb-ring-hash` fixture (driver binds 16 conns to each of 127.0.0.2..5 = 64 total; SUBJECT affinity = every per-backend count ≡ 0 mod 16 + spread `>=2`; reference conservation-only [Docker NAT → single-key pin]; cross-side `StatsAsserter` + 3 gauges 1026/342/342) · 8 deliberate-break liveness (`-count=1`: scatter-key→affinity fails, collapse-ring→spread fails, stats-drop→stats fails, corrupt-gauge→gauge prong fails) + ≥20-run flake check · 9 full differential re-verify (62 prior byte-exact through the seam ext + new case; `0061` green; 63 dirs; `-race -short`; `go mod tidy -diff` empty; h2spec 53/53 + proxy-wasm 10/10 unaffected) · 10 completion bundle (BEHAVIOR_CONTRACT ring_hash delta + surface doc 1116→1119; ADR-0235 + ADR-0236 full entries [ADR-0044 in-place; tail → ADR-0236]; STATE/ROADMAP row 36.1 done [flat family row]; the six-gate).

## Counts at master tip (phase 36.1 PLAN done — confirm at the 36.1 IMPL baseline)

- stat surface **1116** (a DOC count in BEHAVIOR_CONTRACT.md, NOT a programmatic golden) → **1119** at the 36.1 IMPL (+3 `ring_hash_lb.*` gauges; AMEND-RH4; the FIRST non-zero LB-stat delta).
- differential fixtures **62** (tail `0060-lb-random`; `ls -d test/fixtures/[0-9]* | wc -l`) → **63** at the 36.1 IMPL (`0061-lb-ring-hash`); NO boot-reject dir.
- fuzzers **42** (tail `FuzzThriftDecode`) → **42** (no wire decode; the ring-lookup property test is unit-level).
- BackendKind tail **33** (`TCPThriftResponder`) → **33** (`TCPEcho` reused; the affinity proof needs NO new BackendKind — the aggregate-count modular invariant, D-S36-4).
- DECISIONS tail **ADR-0234** (next-free **ADR-0235**). The ADR-0235 (seam) + ADR-0236 (policy) §Context drafts are in SPEC §13; the full bodies land at the 36.1 IMPL per ADR-0044 (tail → ADR-0236; next-free ADR-0237).
- exported-surface delta for 36.1: **2 additive symbols** (`cluster.WithHashKey` + `cluster.HashSourceIP`); the existing exported `Cluster` method signatures stay BYTE-STABLE.
- reference pin: `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227).
- Toolchain: Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); go-control-plane `/envoy` v1.32.4 (ADR-0008) — ZERO new dep (D-RH1; xxHash64/murmurHash2 hand-rolled).

## Read first (in order)

1. THIS FILE — the post-phase-36.1-PLAN cold-start (next: the phase-36.1 IMPL).
2. `docs/envoy-go/phases/36-load-balancer-ring-hash/PLAN.md` — the authoritative 10-task TDD plan (the File Structure table; the D-S36-1..7 resolutions; the ADR-0045 NO-FURTHER-SPLIT re-check; Tasks 1–10 with exact code, test scaffolds, commands, expected output, and per-task commit messages).
3. `docs/envoy-go/phases/36-load-balancer-ring-hash/SPEC.md` — the authoritative phase SPEC (§1.1 AMEND-RH1..RH8; §3 the policy + seam-extension design; §3.0 the CONSUMED-split; §6 the two-layer gate; §7 the +3 gauges; §8.1 the `0061` affinity+spread fixture; §11 the D-RH1..D-RH8 empirical pins; §13 the ADR-0235 + ADR-0236 §Context drafts).
4. `docs/envoy-go/STATE.md` — active-phase `phase 36.1 (load-balancer-ring-hash) PLAN done`; the lifecycle → the 36.1 IMPL routing; the counts.
5. `docs/envoy-go/ROADMAP.md` — row 36 `in-progress` (split CONSUMED; 36.1 leg flips at the 36.1 IMPL) + the Load-balancing family-heading.
6. `docs/envoy-go/DECISIONS.md` — **ADR-0232** (the LB seam — its §Consequences PICK-INPUT-half anticipation the 36.1 IMPL CONSUMMATES via ADR-0235) + ADR-0233 (least_request gate precedent) + ADR-0234 (random — the RNG/seam-reuse posture) + ADR-0024/0045/0044/0052/0080/0106/0219/0063.
7. `internal/cluster/{loadbalancer,leastrequest,random,manager,cluster}.go` (the seam `Pick()` funnel the PLAN widens + `noopRelease` + `parseLeastRequestLbConfig` [the gate precedent] + `newPCGRNG` + `registerClusterMetrics`:99 + `manager.go:235` switch + `:257` reject text + `manager_test.go:320` retarget) + the consumer `internal/filter/tcpproxy/filter.go` (the `NewFilter` parse + the `eff.Dial(ctx)` site:127).
8. `test/differential/runner_test.go` (the per-backend `accepts` aggregate snapshot → `AssertDistribution(refCounts, subjCounts)`:1104; `acceptEchoCounting`:1331 = PURE byte-echo) + `test/differential/fixture/fixture.go` (the `Driver`/`DistributionAsserter`/`StatsAsserter` interfaces; `TCPEcho BackendKind=0`) + `test/fixtures/0060-lb-random/` (the closest fixture template; as-built band `{6,40}`).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_execution_style`, `feedback_git_worktrees`, `feedback_subagents_no_push`, `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_path_targeting`, `feedback_subagent_worktree_detach`, `reference_differential_break_protocol_count1`, `reference_differential_asserter_dispatch`, `reference_differential_run_selector`, `reference_differential_band_sigma_margin`, `reference_differential_hash_key_cross_side_infeasible`, `reference_differential_fixture_dispatch_constraint`, `reference_docker_probe_bridge_network`.

## Honor throughout

- `feedback_execution_style` (subagent-driven IMPL — fresh subagent per task, two-stage review) · `feedback_git_worktrees` (work in a worktree) · `feedback_subagents_no_push` (subagents commit LOCAL-ONLY; the controller squash-merges + pushes at stage-close) · `feedback_push_to_origin` (after a clean landing on master with tests green, push without asking) · `feedback_pertask_gofmt_lint` · `feedback_subagent_worktree_path_targeting` (the canonical PROGRESS.md path; the IMPL worktree is the canonical checkout; controller verifies the main checkout clean) · `feedback_subagent_worktree_detach` (deliberate-break checks can detach HEAD — dispatch a GIT HYGIENE block; controller re-verifies the branch each task).
- ADR-0106 (flat family row per LB policy — NO parent rollup; the 36.1 leg flips its own portion) · ADR-0045 (the split-gate — re-checked at the PLAN → NO FURTHER SPLIT for 36.1) · ADR-0044 (ADR bodies at the IMPL, in-place) · ADR-0052 (the atomic-landing six-gate) · ADR-0080 (byte-stable parse-reject wording) · ADR-0227 (the contrib reference image; live probes per `reference_docker_probe_bridge_network`).
- The lifecycle is BRAINSTORM → SPEC → PLAN → IMPL, each its own session. The next session is the phase-36.1 **IMPL** (`superpowers:subagent-driven-development` — execute the 10-task PLAN). 36.2 (the HTTP route `hash_policy` plane) follows AFTER the 36.1 IMPL with its own SPEC → PLAN → IMPL.
