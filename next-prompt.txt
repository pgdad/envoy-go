=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-CHECKED 2026-07-15 (after the phase-65 BRAINSTORM landed + ROADMAP row 65 registered `in-progress`, run MECHANICALLY) — it does NOT fire. Do NOT create `stop`.** State:
- (1) prints `NOT DONE: row 65` — row 65 `xds-sds-validation-context` registered `in-progress` at the phase-65 BRAINSTORM commit per the §Schema invariant (it flips `done` only at its IMPL six-gate, ADR-0106). Check (1) BLOCKS `stop`.
- (2) prints THREE live "candidates:" sentences — HTTP/3, xDS, and Observability ⇒ three families STAY OPEN. The xDS sentence is UNCHANGED by this BRAINSTORM (`SDS validation_context/upstream SDS` still listed; it narrows to `upstream SDS` only at the phase-65 IMPL per the observed convention).
- (3) prints `NEVER OPENED: gRPC`, `Runtime`, `WASM` ⇒ THREE families never opened. Operational-tooling also STAYS OPEN.

Checks (1), (2) and (3) print ⇒ the sentinel does NOT fire. Row 65 is mid-lifecycle (BRAINSTORM done, SPEC/PLAN/IMPL to go) AND there is banked deferred work AND never-opened families — abundant work remains.

**STATUS AS OF 2026-07-15: the phase-65 BRAINSTORM LANDED and ROADMAP row 65 is `in-progress`.** The roller AUTONOMOUSLY SELF-PICKED (no human pick, per the 2026-07-12 standing directive) the THIRD xDS-family row + the SECOND SDS resource type: **xDS SDS `validation_context`** — lift the `internal/tls/config.go:227-228` `validation_context_sds_secret_config is not supported` reject (DOWNSTREAM only) and install the SDS-delivered CA bundle into the downstream listener's `ClientCAs` for mandatory mTLS client-cert validation, over the landed phase-60.2 SotW SDS discovery stream + the phase-16 static-mTLS `ClientCAs` path. Picked as the smallest CLEANLY-DIFFERENTIAL-PROVABLE candidate whose ENTIRE substrate is confirmed-landed (the mTLS handshake outcome is directly observable cross-side; the SDS discovery client + static-mTLS `ClientCAs` + the driver-owned `sdsserver` + the `0103` fixture template + the `0018` client-cert harness are ALL landed ⇒ NO new discovery/harness infra), over the declined tracing `metadata` (provability CONDITIONAL — needs MetadataKey-traversal + metadata-plumbing machinery envoy-go lacks) / HTTP/3 QUIC knobs (not cleanly provable with the GET-only H3 harness) / SDS rotation / upstream SDS / CDS-EDS-LDS-RDS-ADS-Delta-RTDS / larger Observability follow-ons / opening a new family (recorded BRAINSTORM §2.1). The BRAINSTORM (`docs/envoy-go/phases/65-xds-sds-validation-context/BRAINSTORM.md`, the 12-section shape) names the D-SDSVC-* SPEC-probe questions, the anticipated envelope (ONE new fixture / +0 stats [reuse `sds.*`] / +0 fuzzers [SEEDS] / +0 packages / +0 modules), the anticipated SINGLE FLAT ROW disposition (ADR-0045 escape-valve armable as a 65.1/65.2 split), and reserves **ADR-0286**. Row 65 registered `in-progress`; STATE flipped; router rolled to the phase-65 SPEC.

**Counts at phase-65 BRAINSTORM-DONE (VERIFIED on the landed tree; RE-VERIFY at session start, `git fetch` first):** fixtures **109** (`ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0107-tracing-max-path-tag-length`) · fuzzers **55** · DECISIONS tail **ADR-0285** (next-free **ADR-0286**) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** · go.mod modules **2** (`quic-go` direct + `qpack` indirect). Counts are UNCHANGED at a BRAINSTORM (docs-only).

==============================================

## What THIS session does (the phase-65 SPEC)

Perform the SPEC stage (lifecycle-state 1 → 2) for phase 65 (`xds-sds-validation-context`). This is a DOCS-ONLY stage — NO production `.go` changes (SPEC probes may build a THROWAWAY probe dir/container; DELETE it before committing). Run in a FRESH worktree off master, branch `phase-65-xds-sds-validation-context-spec` (or reuse the BRAINSTORM branch line), per `feedback_git_worktrees`.

**The SPEC deliverable:** `docs/envoy-go/phases/65-xds-sds-validation-context/SPEC.md` (the 14-section SPEC-45.1/SPEC-64 shape) DISPOSING every D-SDSVC-* question via LIVE probes against `envoyproxy/envoy:contrib-v1.37.2` (FRESH container per arm — `reference_probe_fresh_container_per_arm`; shared bridge + reachable driver-owned SDS server + a client-cert-presenting client — `reference_docker_probe_bridge_network`/`reference_host_gateway_ip_docker_desktop`; VERIFY the handshake decision ACTUALLY exercised, not a vacuous no-cert capture — `reference_deliberate_break_wrong_assertion`). Draft ADR-0286 §Context (§Decision/§Consequences at the IMPL per ADR-0044). Row 65 STAYS `in-progress`.

**The D-SDSVC-* questions to DISPOSE at the SPEC (from BRAINSTORM §10):**
- **D-SDSVC-REFSERVE** (the CENTRAL provability probe) — CONFIRM the reference serves + applies a `validation_context` via SDS (a `tls.v3.Secret` with the `validation_context` oneof carrying `trusted_ca`) and validates client certs against it. Probe: an SDS-served CA + a client presenting a CA-signed cert (handshake completes → 200) vs a wrong-CA cert (rejected → `ssl.fail_verify_*`). If the reference behaves unexpectedly, RE-SCOPE.
- **D-SDSVC-RESOURCE** — the served `Secret` wire shape (`Secret_ValidationContext` oneof, `CertificateValidationContext{trusted_ca}`).
- **D-SDSVC-REQUIRE-SCOPE** — the `require_client_certificate` interaction; scope to `true` (mandatory mTLS, mirroring phase 16); `false` (verify-if-present) DEFERS. Probe.
- **D-SDSVC-FETCHTIMEOUT** — the `initial_fetch_timeout` + boot-FAIL DEPARTURE extends to the `validation_context` fetch unchanged. Probe for confirmation.
- **D-SDSVC-CVC-REJECT** — the applier mirrors the inline CVC sub-field reject roster (`internal/tls/config.go:234-244`: `match_typed_subject_alt_names`/`custom_validator_config`/`verify_certificate_hash`/`verify_certificate_spki`; also weigh `crl`). RE-DERIVE.
- **D-SDSVC-PROVIDER** — the provider/stream seam: a parallel `FetchInitialValidationContext(...) (*x509.CertPool, error)` chain (Option A, LEAN — leaves the landed `*stdtls.Certificate` chain untouched) vs generalizing `fetchSecret`/`applyResponse` over a parse callback (Option B, DRY). Pin the names (GREP-collision-checked in `internal/xds`+`internal/tls`, `reference_spec_drafted_identifier_collision_check`) + signatures + the `xdsgrpc` adapter wiring.
- **D-SDSVC-APPLY-POINT** — where the SDS-fetched CA pool installs into `cfg.ClientCAs` + `ClientAuth`; the `require_client_certificate` INLINE-`trusted_ca` precondition relax (`config.go:67-79`); the hold-and-thread structure between `commonTLSContextToConfig` and `NewDownstreamConfig` (mirroring how `sdsCert` flows, `config.go:206-223`/`:250-252`).
- **D-SDSVC-FIXTURE** — ONE new fixture (`0108` anticipated; RE-DERIVE the next-free number): an SDS-served CA + a mandatory-mTLS client-cert handshake, cross-side request-outcome equality. CONFIRM the differential runner presents a client cert (the `0018-http-rbac` precedent — RE-DERIVE the exact mechanism). New dir (`reference_differential_fixture_dispatch_constraint` — do NOT mutate `0103`/`0018`); break-prove live (`reference_differential_break_protocol_count1`/`reference_differential_run_selector`; the natural break = serve a WRONG CA, confirm WHICH assertion fires).
- **D-SDSVC-NEGATIVE** — the wrong-CA negative arm (proves the SDS-served CA is the actual trust anchor): a second dir vs a second scenario vs a driver sub-assertion.
- **D-SDSVC-STATS** — reuse the phase-60.2 `sds.*` lifecycle counters (+0, anticipated) vs a per-resource-type counter. RE-DERIVE the `SDSStats` set (`internal/xds/provider.go`).
- **D-SDSVC-FUZZSEED** — SEEDS to `FuzzDiscoveryResponseParse` (`internal/xds/fuzz_test.go:71`) + `FuzzTLSContextParse` (`internal/tls/fuzz_test.go:24`), NOT new fuzzers; fuzzer count STAYS 55 (`reference_fuzzer_count_docs_drift` — reconcile before AND after).
- **D-SDSVC-SPLIT** — the ADR-0045 disposition (SINGLE FLAT ROW anticipated, ~10–16 tasks; the escape valve is a 65.1 `internal/xds` applier + 65.2 `internal/tls` wiring split mirroring 60.1/60.2, re-armed only if the two-package surface would strand a leg).
- **D-SDSVC-DOCSHAPE** — RE-DERIVE the full edit-site roster (BRAINSTORM §6) against the master tip.

**Honor throughout:**
- **Lifecycle = one stage per session.** This session: the SPEC ONLY (docs-only; NO `.go`; a throwaway probe dir DELETED before commit). After it lands: roll to the phase-65 PLAN.
- **The 60.2 cycle guard STANDS** (`reference_xds_config_seam_transitive_cycle_guard`) — `internal/xds` must NOT import `internal/tls` (the reverse edge cycles); the SPEC pins the CA-pool build DUPLICATED in `internal/xds` (mirroring `dataSourceBytes`'s deliberate duplication of `loadDataSource`) and verifies with `go list -deps` (no `...`).
- **Process anchors:** ADR-0044 (ADR §Context drafted at the SPEC, §Decision/§Consequences at the IMPL) · ADR-0045 (single-flat-row unless a second subsystem would strand) · ADR-0106 (row flips `done` at the IMPL six-gate) · ADR-0080 (envoy-go-strict distinct-substring rejects) · `reference_roadmap_split_phase_row_done` · `feedback_brief_citations_not_evidence` (RE-DERIVE every `file:line` against the master tip; a citation is not evidence — RE-READ `internal/xds/{secret,provider,stream}.go` + `internal/tls/config.go` in full).
- **`next-prompt.txt` IS TRACKED** (`reference_next_prompt_tracked_despite_gitignore`) — edit it inside the stage worktree and fold into the stage squash; locate commits by SUBJECT (`git log --grep`), never by position.
- **Worktree discipline** (`feedback_git_worktrees`, `feedback_subagent_worktree_detach`, `feedback_subagent_worktree_path_targeting`) — pin the canonical worktree root; the controller verifies the MAIN checkout stays clean.

**ADR reservation ledger (provisional; each finalized at its IMPL in landing order):** ADR-0283 = phase-62 tracing-custom-tags-request-header (LANDED) · ADR-0284 = phase-63 tracing-custom-tags-environment (LANDED) · ADR-0285 = phase-64 tracing-max-path-tag-length (LANDED) · **ADR-0286 = phase-65 xds-sds-validation-context** (reserved at the phase-65 BRAINSTORM; §Context drafted at the SPEC, §Decision/§Consequences at the IMPL) · **next-free unreserved: ADR-0287**.

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-65 SPEC is next).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 65 BRAINSTORM done`; NEXT = the phase-65 SPEC) + the prior-phase lineage.
3. `docs/envoy-go/phases/65-xds-sds-validation-context/BRAINSTORM.md` — the subject, the self-pick rationale + rejected alternatives, the D-SDSVC-* questions, the §6 edit-site roster, the anticipated envelope + split disposition.
4. `docs/envoy-go/ROADMAP.md` — the xDS family section + the THREE live deferred sentences (HTTP/3, xDS, Observability) + the §Schema invariant (row 65 is `in-progress`) + the phase-60 SDS-family precedents (rows 60/60.1/60.2).
5. `docs/envoy-go/DECISIONS.md` — ADR-0278 (the SDS substrate) + ADR-0280 (the SDS server-cert wiring) + ADR-0147 (the static-mTLS `ClientCAs`) + the ADR-0044/0045/0106/0080 process anchors.
6. `docs/envoy-go/phases/64-tracing-max-path-tag-length/SPEC.md` + `docs/envoy-go/phases/60-xds-sds-server-cert/SPEC.md` — the FORMAT precedents for a SPEC (the 14-section shape; the live-probe §11 disposition; the ADR §Context draft). SPEC-60 is the closest subject precedent (the SDS live-probe arms).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the differential-harness `reference_*` traps + the xDS `reference_xds_config_seam_transitive_cycle_guard` + the SDS/differential-gRPC-receiver traps.
