START a fresh session for envoy-go (router last updated 2026-06-25; the **phase 44.1 (`grpc-access-log` core) SPEC is DONE**). HEAD is the **phase-44.1 SPEC squash commit** (subject `phase 44.1 (grpc-access-log) SPEC: …` — it landed `docs/envoy-go/phases/44-grpc-access-log/SPEC-44.1.md` + the STATE.md roll + this next-prompt.txt roll TOGETHER as one docs-only squash; re-read the shas with `git log --oneline -5`, do NOT assume — any number of docs-only `next-prompt.txt:` tip-stamp commits may later stack ATOP the SPEC squash). The stable anchor BENEATH is the phase-44 BRAINSTORM squash `81afdd59` (`phase 44 (grpc-access-log) BRAINSTORM: … OPENS the Observability family`), and BENEATH that the 43.2b IMPL squash `676e17cf`. **The Observability family is OPEN** (phase 44 is its FIRST row; the family STAYS OPEN — OTLP / tracing / stats sinks / tap remain future rows). **The next action is the 44.1 (core) PLAN** — invoke `superpowers:writing-plans` (the project's PLAN stage), resolve the SPEC §12 D-ALS-* PLAN questions, author the ~10–12-task TDD spine, get plan-document-reviewer APPROVAL, then proceed to the 44.1 IMPL (each stage SUBAGENT-DRIVEN, `feedback_execution_style`). **Git hygiene:** all phase work runs in a FRESH git worktree off master (`feedback_git_worktrees`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the controller squashes + pushes at stage-close (`feedback_push_to_origin`); each code task runs `gofmt -l` + `golangci-lint` on touched pkgs (`feedback_pertask_gofmt_lint`).

First confirm the state via `git log --oneline -5` (expect HEAD to be the phase-44.1 SPEC squash — or a later docs-only `next-prompt.txt:` tip-stamp commit atop it — with the phase-44 BRAINSTORM squash `81afdd59` and the 43.2b squash `676e17cf` beneath; re-read the shas, do NOT assume). Counts at the 44.1 SPEC (UNCHANGED, docs-only): stat surface **1187** (H2 cluster; non-H2 **1183**) / fixtures **82** (tail `0080-h2-goaway-rotation`) / fuzzers **43** / BackendKind tail **38** (`H2GoawayResponder`) / DECISIONS tail **ADR-0254**, next-free **ADR-0255**.

## NEXT STEP — the 44.1 (core) PLAN (`superpowers:writing-plans`, PLAN-scoped)

Phase 44.1 delivers the CORE streaming gRPC ALS sink (the SPEC §3 design, already empirically pinned by the §11 D-ALS-* in-session probe). The PLAN authors the TDD spine (the SPEC §10 ~10–12-task structure is the skeleton) and RESOLVES the SPEC §12 D-questions with real code:

- **D-ALS-CONFIG-DEFER** — confirm PARSE-ACCEPT-but-INERT for the 44.2 `buffer_*` + 44.3 `additional_*_headers` (vs strict-reject-until-the-leg); unify `AccessLogConfigs`+`ALSConfigs` into one ordered list vs keep parallel slices.
- **D-ALS-RECEIVER-WIRING** — the `0081` receiver: in-process `AccessLogServiceServer` accumulator + how the reference Docker container reaches the host-bound receiver (shared bridge + hostname per `reference_docker_probe_bridge_network`; the `0080` backend / `test/helpers/extauthzgrpc` precedent); fixture-local backend vs a `test/helpers` package.
- **D-ALS-NODE** — the `identifier.node` content envoy-go sends (minimal id/cluster from bootstrap.node vs empty; UNasserted regardless).
- **D-ALS-SPLIT-FINAL** — the final ADR-0045 re-check with real LoC (≈300–360 prod LoC anticipated; one leg).
- **D-ALS-FUZZER** — land a `HttpGrpcAccessLogConfig` parse fuzzer at 44.1 (fuzzers 43→44) vs defer; reconcile the documented-vs-actual `^func Fuzz` count regardless (`reference_fuzzer_count_docs_drift`).

The PLAN ships in the house TDD shape (the 43.2b `PLAN-43.2b.md` template): each task = a failing test → minimal impl → green → commit; `-race` on the `GrpcAccessLogSink` writer-goroutine task; the FULL-package `-race` after it lands (`reference_full_suite_race_after_background_mutator`); a `-count=1` deliberate-break pass on `0081` (`reference_differential_break_protocol_count1`); the `-run 'TestDifferential/0081'` selector (`reference_differential_run_selector`). Anticipated at the 44.1 IMPL: stat **1189** / fixtures **83** / fuzzers **44** (or 43) / BackendKind **39** / DECISIONS **ADR-0255**.

## Honor throughout (the discipline)

- **Lifecycle = one stage per session.** PLAN → IMPL, each subagent-driven, each squashed + pushed at stage-close. `feedback_git_worktrees` (fresh worktree off master) · `feedback_subagents_no_push` · `feedback_push_to_origin` · `feedback_execution_style` · `feedback_pertask_gofmt_lint` · `feedback_subagent_worktree_detach` / `feedback_subagent_worktree_path_targeting` (worktree hygiene + path-pinning; the controller verifies `git -C <main> status` clean after each task) · `reference_full_suite_race_after_background_mutator` (the sink's writer goroutine is a background mutator — after it lands, re-run the FULL package `-race`, NOT a `-run` subset).
- **Process anchors:** ADR-0106 (row 44 flips `done` PER-LEG at each IMPL; the Observability family STAYS OPEN) · ADR-0080 (strict-reject — `tcp_logs`/non-V3/`google_grpc`/empty-cluster) · ADR-0044 (the ADR §Decision/§Consequences land at IMPL; §Context drafted in SPEC §13) · ADR-0045 (the split gate, re-checked at PLAN) · ADR-0041 (gRPC ALS LIFTED from the silent-ignore set) · ADR-0016 (the proto blank-import registry) · ADR-0067 (the access-log 10-operator `Record` shape) · ADR-0158 (the `grpcclient.Dialer` typed-wrapper layer — the `ALSClient` precedent).
- **Differential discipline (the `0081` fixture):** `reference_docker_probe_bridge_network` (shared bridge + a receiver hostname reachable from BOTH the subject host AND the reference container; verify decode ran) · `reference_wire_format_both_sides_see_same_bytes` (adopt the `HTTPAccessLogEntry` PAYLOAD verbatim — assert per-entry aggregated, NOT stream/message framing, per AMEND-ALS-3) · `reference_concurrency_differential_release_barrier` (poll-to-converge until N entries arrive, NEVER a `time.Sleep`) · `reference_differential_run_selector` (`-run 'TestDifferential/0081'`) · `reference_differential_break_protocol_count1` (`-count=1` on every deliberate-break AND every `-race` run) · `reference_differential_fullsuite_startup_flake` (a `subject ready: EOF` is a transient startup race — isolate-re-run) · `reference_dynamic_stat_name_charset_guard` (NOT needed at 44.1 — the `access_logs.grpc_access_log.*` names are STATIC per AMEND-ALS-1; stay alert if 44.3 adds a header-keyed segment) · `reference_fuzzer_count_docs_drift` (reconcile the running `^func Fuzz` total — D-ALS-FUZZER).
- **The query-less-path constraint (AMEND-ALS-2):** the `0081` request MUST use a query-less path — envoy-go's `Record.Path` is path-only (`accesslog_emit.go:25`) while the reference's `request.path` carries the query string; a query-bearing request would diverge cross-side. A documented faithful constraint, not a bug.

## Read first (in order)

1. THIS FILE — the cold-start router (phase 44.1 SPEC is DONE + LANDED; the next action is the 44.1 core PLAN).
2. `docs/envoy-go/phases/44-grpc-access-log/SPEC-44.1.md` — the 44.1 SPEC (the §3 design + the §11 D-ALS-* empirical pins + the §1.1 AMEND-ALS-1..4 + the §12 PLAN D-questions + the §13 ADR-0255 §Context draft).
3. `docs/envoy-go/phases/44-grpc-access-log/BRAINSTORM.md` — the parent BRAINSTORM (the §1–§12 design + the 3-leg split + the load-bearing code/proto facts with file:line refs).
4. `docs/envoy-go/STATE.md` — the active-phase header (`phase 44.1 (grpc-access-log) SPEC done`) + the count figures.
5. `docs/envoy-go/ROADMAP.md` — row 44 (`grpc-access-log`, in-progress) + the `### Observability family` heading (FAMILY OPEN at phase 44).
6. `docs/envoy-go/BOOTSTRAP_PROMPT.md` — the lifecycle invariants (§4.1: STATE.md is the source of truth for "what next"; a session updates it before exiting).
7. `docs/envoy-go/DECISIONS.md` — the tail **ADR-0254** (the 43.2b as-built) + next-free **ADR-0255** (the 44.1 IMPL lands its §Decision/§Consequences; §Context is drafted in SPEC §13).
8. The existing seams the PLAN builds on: `internal/accesslog/accesslog.go` (the `Sink` `:18` + `Record` `:29`), `internal/accesslog/writer.go` (the `AsyncFileSink` pattern), `internal/accesslog/stats.go` (`RegisterDroppedCounter` `:14`), `internal/filter/hcm/accesslog_emit.go` (the H1/H2 emit hooks `:18`/`:43`, `Record.Path = r.URL.Path` `:25`), `internal/bootstrap/bootstrap.go` (`parseOneAccessLog` `:265`, the silent-ignore `:271`, the blank-imports `:39`/`:40`), `internal/grpcclient/grpcclient.go` (the `Dialer` `:85`/`:105`/`:112`–`118` + `AuthClient` `:157`/`:178`/`:231`), `cmd/envoy-go/main.go` (the sink-build loop `:106`–`119`/`:225`/`:232`), `test/differential/fixture/fixture.go` (the `BackendKind` enum tail `:606`).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. the worktree-hygiene, differential, and the `reference_wire_format_both_sides_see_same_bytes` / `reference_docker_probe_bridge_network` refs).
