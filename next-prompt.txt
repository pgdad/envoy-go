RESUME a fresh session for envoy-go (router last updated 2026-06-28; the **phase 46.2 (tracing Zipkin+B3) PLAN is DONE and LANDED** — `docs/envoy-go/phases/46-tracing/PLAN-46.2.md`, a **13-task TDD spine**, docs-only direct-on-master per the 46.1a/46.1b PLAN precedent; the plan-document-reviewer APPROVED after **one fold-iteration** [1 buildability Issue + 2 advisories, all folded then re-confirmed: (Issue) the Task-8 breaking `NewExporterProvider`/`ExporterFor` signature change now folds BOTH call-site migrations INTO the same Task-8 commit — `internal/filter/hcm/config.go`→`provider.ExporterFor(tcfg)` + `cmd/envoy-go/main.go`→a `nil` `ZipkinTransport` placeholder (swapped to the real `zipkinTransportAdapter` at Task 10) — so EVERY per-task `go build ./...` gate stays green (Task 9 no longer touches `config.go`); (advisory-1) Task-5 uses an inline `shared_span_context` nil-guard avoiding a `wrapperspb` import; (advisory-2) Task-2's `InjectB3` assertion reads canonical `http.Header.Get`]). **The 46.2 leg is the SECOND-and-FINAL chartered leg of the by-exporter split** (46.1 core+OTLP [DONE, ADR-0260 CLOSED] / 46.2 Zipkin+B3). **The next action is the 46.2 IMPL** — execute the 13-task TDD spine SUBAGENT-DRIVEN in a FRESH worktree off master (`feedback_execution_style` · `feedback_git_worktrees`), ANCHORING ADR-0261 §Decision/§Consequences at the completion task. **ROW 46 (`tracing`) STAYS `in-progress`** and the Observability FAMILY STAYS OPEN — **the 46.2 IMPL six-gate FLIPS row 46 → `done`** (the FINAL chartered tracing leg, per ADR-0106 + `reference_roadmap_split_phase_row_done`); the Observability family STAYS OPEN regardless (stats sinks + the tap filter remain future rows — NO family-close at 46.2, per the phase-46 BRAINSTORM §1.3).

HEAD is the **46.2 PLAN docs-only commit** atop the 46.2 SPEC `5ea88386`. **Re-read the shas with `git log --oneline -8`, do NOT assume.** Confirm `git status` clean (the untracked `.claude/` is session cruft, leave it) + master == origin/master.

## The 13-task TDD spine (PLAN-46.2.md §"Per-task structure" — re-read the PLAN for the per-task code, the verbatim test/run/commit steps, and the locked D-resolutions; re-confirm line numbers before editing, files evolve):
1. PROGRESS-46.2.md scaffold + baselines (1198 / 89 / 48 / 38 / ADR-0260; next-free ADR-0261).
2. `internal/tracing/b3.go` — `ExtractB3` (single `b3` `<traceid>-<spanid>-<sampled>[-<parentid>]` OR multi `X-B3-{TraceId,SpanId,ParentSpanId,Sampled,Flags}`) + `InjectB3` (the MULTI-HEADER `X-B3-*` set toward upstream) + `FuzzExtractB3` [fuzzers 48→49]. TDD table-driven.
3. `internal/tracing/decision.go` — the `DecideWithContext(h, ic, continued, cfg, rng)` refactor (lift the `traceparent` extract to the caller; `Decide` stays a thin wrapper; the rng-call ORDER preserved → OTel/`0086`/`0087` byte-stable). D-TRACE-ZIPKIN-DECIDE-SEAM.
4. `internal/tracing/zipkin.go` (v2 JSON encoder — `name`=`Span.Authority` NOT `"ingress"`, `kind:"SERVER"`, MICROsecond `timestamp`/`duration`, 14 STRING tags [the 16-attr roster MINUS `node_id`/`zone`], `shared`/`parentId` per `shared_span_context`, `zipkinIdentity` shared by the encoder AND `InjectB3`) + add the provider-neutral `Authority` field to `SpanInputs`+`Span` (`span.go`). D-TRACE-ZIPKIN-WIRE/IDS/SHARED/SPAN-NAME.
5. `internal/tracing/config.go` — `ProviderKind` enum + `ZipkinSettings` sub-struct + `zipkinTypeName`; `NewConfig` provider dispatch (OTel|Zipkin); the Zipkin parse arm + STRICT-REJECT arms (`collector_endpoint_version`≠`HTTP_JSON` / `split_spans_for_request` / empty `collector_cluster`); inline `shared_span_context` nil-guard (default true, NO `wrapperspb` import). D-TRACE-ZIPKIN-CONFIG-SHAPE.
6. `internal/tracing/stats.go` — `RegisterZipkinCounters` → `tracing.zipkin.{spans_sent,spans_dropped}` [+2 → 1200]; registration test. D-TRACE-ZIPKIN-STATS-FINAL.
7. `internal/tracing/zipkin.go` — the `ZipkinExporter` (`Exporter` impl: bounded channel + writer goroutine + size/interval/close buffer + v2-JSON POST over the `ZipkinTransport` seam [`HasCluster`+`Dispatch`, the `wasm.HTTPDispatcher` analog wrapping `httpclient.ClusterDispatch`] + retry-once + drop-newest + idempotent Close). TDD + **FULL-package `-race`** (the writer goroutine is a background mutator, `reference_full_suite_race_after_background_mutator`). D-TRACE-ZIPKIN-WIRE/TRANSPORT-WIRING.
8. `internal/tracing/exporter.go` — generalize `ExporterFor(*TracingConfig)` + the `ExporterProvider` Zipkin arm (provider dispatch + memoize per cluster + lazy `RegisterZipkinCounters` + the `HasCluster` boot-reject gate [reference-parity — AMEND-ZIPKIN-BOOT-REJECT]); `NewExporterProvider` grows the `ZipkinTransport` param; **MIGRATE both call sites IN THIS COMMIT** (`config.go:334`→`ExporterFor(tcfg)`, `main.go:131`→`nil` transport placeholder) so the repo builds.
9. `internal/filter/hcm/{connection.go:520,h2dispatch.go:421,accesslog_emit.go:25/84}` — provider-aware extract (`ExtractB3` vs `ExtractTraceparent`) → `DecideWithContext` → provider-aware inject (`InjectB3` vs `InjectTraceparent`) keyed by `tcfg.Provider`; set `in.Authority` = `r.Host` (H1) / `req.Authority` (H2); no-tracing byte-stable. (Does NOT touch `config.go` — migrated at Task 8.)
10. `cmd/envoy-go/main.go` — HOIST `httpClient := httpclient.New(...)` above the `tracingProvider :=` line (`:131`); add the `zipkinTransportAdapter{httpClient, cm}` (`HasCluster`=`cm.Get`, `Dispatch`=`httpClient.ClusterDispatch`); SWAP the Task-8 `nil` placeholder for it; `CloseAll`/the defer-LIFO UNCHANGED.
11. `test/helpers/zipkincollector/` — the driver-owned HTTP/JSON Zipkin collector (de-chunk + `json.Unmarshal` `[]span` + accumulate + `Spans()`/`Count()`/`Reset()`/`Addr()`/`Stop()`; reply `202`). BackendKind STAYS 38 (`reference_differential_grpc_receiver_driver_owned`).
12. `test/fixtures/0088-tracing-zipkin/` — COPY `0087-tracing-otlp` + adapt (plaintext HTTP/1 collector cluster — NO h2c; Zipkin provider, `random_sampling=100%`, `HTTP_JSON`, fixed `Host`, `/api/v2/spans`); cross-side EXACT poll-to-converge over the shared bridge; assert the stable v2-JSON span subset + the B3 continuation prong; the `HTTP_PROTO`/`split_spans`/cluster-miss SUBJECT-side unit tests (Tasks 5/8 cover them); blank-import the `0088` driver in `runner_test.go`.
13. `0088` deliberate breaks (`-count=1`, `reference_differential_break_protocol_count1`: name→ingress / drop continuation / break SERVER kind / skip export) + 20/20 flake + FULL-package `-race`; full **90-dir** differential + six-gate; ADR-0261 §Decision/§Consequences body + BEHAVIOR_CONTRACT + STATE/ROADMAP (**row 46 → `done`**) + the fuzzer-count reconcile (`reference_fuzzer_count_docs_drift`, 48→49).

## As-built at the 46.2 PLAN (the design the IMPL builds — re-confirm line numbers before editing):
- The `internal/tracing` package is the home: `config.go` (`NewConfig` — TODAY OTel-only, the `otelTypeName` gate `:70`), `decision.go` (`Decide` extracts `traceparent` INTERNALLY `:64` — Task 3 refactors), `propagation.go` (the W3C engine + `TraceContext{TraceID,ParentID,Sampled,TraceState}` — REUSED by B3), `span.go` (`BuildServerSpan` — `Name="ingress"` + the 16-attr `[]KV` roster; `toProto`), `exporter.go` (the `Exporter` interface `:56`; `OTLPExporter` `:73`; `tracesClientDialer` `:233`; `ExporterProvider` `:243`; `ExporterFor(clusterName,serviceName)` `:275` — Task 8 generalizes to `(*TracingConfig)`), `stats.go` (`RegisterTracerCounters` `:51` — the OTel pair).
- The Zipkin exporter is a SECOND `tracing.Exporter` exporting the SAME `Span` over a DIFFERENT transport — HTTP/1 `POST <collector_endpoint>` of a Zipkin **v2 JSON** span ARRAY to the `collector_cluster`, via `httpclient.Client.ClusterDispatch(ctx, clusterName, *http.Request, *cluster.Manager)` (`internal/httpclient/httpclient.go:269`, ADR-0177 — does `clusterMgr.Get` [the cluster-exists gate] + `PickEndpoint` + a stdlib HTTP/1 POST; the `internal/wasm/http_call.go` `HTTPDispatcher` decoupling-seam is the precedent the `ZipkinTransport` mirrors). NOT `grpcclient.Dialer`/`OTLPTracesClient` (H2/gRPC); the collector cluster is plain HTTP/1 (no `http2_protocol_options{}`).
- `ZipkinConfig` resolves at `go-control-plane/envoy v1.32.4` (the SAME `tracev3` import `config.go:10` carries — FullName `envoy.config.trace.v3.ZipkinConfig`; accessors `GetCollectorCluster`/`GetCollectorEndpoint`/`GetTraceId_128Bit`/`GetSharedSpanContext`[`*wrapperspb.BoolValue`, absent⇒true]/`GetCollectorEndpointVersion`[`HTTP_JSON`=1/`HTTP_PROTO`=2/`GRPC`=3/`0`]/`GetCollectorHostname`/`GetSplitSpansForRequest` — ALL verified at PLAN; `go mod tidy -diff` EMPTY).
- Counts at 46.2 PLAN (the IMPL baseline — re-verify, do NOT assume): stat surface **1198** (H2; non-H2 **1194**) / fixtures **89** (tail `0087-tracing-otlp`) / fuzzers **48** / BackendKind **38** / DECISIONS **ADR-0260** (CLOSED; next-free **ADR-0261**). UNCHANGED at PLAN (docs-only). Anticipated at the 46.2 IMPL: stat **1200** (+2 `tracing.zipkin.*`) / fixtures **90** (`0088-tracing-zipkin`) / fuzzers **49** (`FuzzExtractB3`) / BackendKind **38** / DECISIONS **ADR-0261**. ZERO new Go packages + ZERO new go.mod modules.

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** The next stage is the **46.2 IMPL** (`superpowers:subagent-driven-development` — the 13-task spine in PLAN-46.2.md; a fresh subagent per task + two-stage review). Lifecycle-state 3.
- **Execution-chaos lesson** (`feedback_subagent_autocommit_claudemd`): dispatched subagents AUTO-COMMIT (global CLAUDE.md). Do NOT fight it — the CONTROLLER verifies EACH commit (correct fileset, real non-vacuous tests via `-v` + read assertions, gates green), cleans stray leak files, re-runs the FULL suite on the FINAL FROZEN HEAD, does the deliberate-break verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_subagents_no_push` · `feedback_push_to_origin`).
- **Per-task gates** (`feedback_pertask_gofmt_lint`): `gofmt -l` + `golangci-lint run` on touched pkgs + `go vet` + `go build ./...`.
- **Differential discipline:** `-run 'TestDifferential/0088'` NEVER bare `'0088'` (`reference_differential_run_selector`); `-count=1` on every break + `-race` run (`reference_differential_break_protocol_count1`); the exporter writer goroutine is a background mutator → FULL-package `-race` (`reference_full_suite_race_after_background_mutator`); poll-to-converge release barrier (`reference_concurrency_differential_release_barrier`); assert the aggregated PAYLOAD not framing (`reference_streaming_sink_differential_framing`); the driver-owned HTTP Zipkin collector is NOT a BackendKind (`reference_differential_grpc_receiver_driver_owned`); the Docker bridge + decode-ran proof (`reference_docker_probe_bridge_network`); the live-reference probe is the wire truth (`reference_wire_format_both_sides_see_same_bytes`); the EMPTY-value tags (`upstream_cluster`/`upstream_cluster.name`/`peer.address`) stay UNasserted cross-side (`reference_tracing_upstream_cluster_framework_gap`); the full-suite startup flake is isolatable (`reference_differential_fullsuite_startup_flake`).
- **Process anchors:** ADR-0106 (per-leg rows; row 46 `in-progress` until the 46.2 IMPL) · ADR-0044 (ADR §Context-at-SPEC / §Decision+§Consequences-at-IMPL — ADR-0261 body lands at the 46.2 IMPL) · ADR-0177 (the `httpclient.ClusterDispatch` HTTP-over-cluster primitive — the Zipkin transport) · ADR-0080 (strict-reject) · ADR-0016 (proto blank-import) · next-free **ADR-0261**.

## Read first (in order)
1. THIS FILE — the cold-start router (the 46.2 PLAN is DONE + LANDED; the next action is the 46.2 IMPL; row 46 `in-progress`).
2. `docs/envoy-go/phases/46-tracing/PLAN-46.2.md` — the 13-task TDD spine (the per-task code + test/run/commit steps; the D-question resolutions; the file structure; the exit criteria).
3. `docs/envoy-go/phases/46-tracing/SPEC-46.2.md` — the 46.2 charter (the §11 D-TRACE-ZIPKIN-* live pins; the §13 ADR-0261 §Context draft the IMPL completes).
4. `docs/envoy-go/phases/46-tracing/PROGRESS-46.1b.md` + `PLAN-46.1b.md` — the sibling 46.1 IMPL record + TDD-spine PLAN (the house shape the 46.2 IMPL mirrors).
5. `internal/tracing/` (the whole package) — the `Exporter` seam + `ExporterProvider` + `NewConfig` + `Decide` + `propagation.go` + `span.go` + `stats.go` the 46.2 IMPL extends.
6. `internal/httpclient/httpclient.go:269` (`ClusterDispatch`) + `internal/wasm/http_call.go` (`HTTPDispatcher`) — the HTTP-over-cluster transport + the decoupling-seam precedent the Zipkin exporter reuses.
7. `docs/envoy-go/STATE.md` — the active-phase header (`phase 46.2 PLAN done`) + the recorded NEXT (the 46.2 IMPL) + the counts (1198 / 89 / 48 / 38 / ADR-0260).
8. `docs/envoy-go/ROADMAP.md` — row 46 (`tracing`, `in-progress`, "46.2 PLAN done; 46.2 IMPL next").
9. `docs/envoy-go/DECISIONS.md` — **ADR-0260** (the 46.1 anchor, CLOSED) + next-free **ADR-0261** (the 46.2 Zipkin+B3 anchor — §Context in SPEC-46.2 §13; the DECISIONS.md body lands at the 46.2 IMPL).
10. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `reference_phase_46_tracing_split`, `reference_tracing_upstream_cluster_framework_gap`, `reference_streaming_sink_differential_framing`, `reference_differential_grpc_receiver_driver_owned`, `reference_docker_probe_bridge_network`, `reference_concurrency_differential_release_barrier`, `reference_full_suite_race_after_background_mutator`, `reference_differential_fullsuite_startup_flake`, `reference_wire_format_both_sides_see_same_bytes`, `reference_fuzzer_count_docs_drift`, `feedback_subagent_autocommit_claudemd`, `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `feedback_subagents_no_push`).
