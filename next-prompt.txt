=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

**THERE IS BANKED MID-LIFECYCLE WORK AT THIS TIP.** Phase 78 (`boot-panic-visibility`) is **OPEN** — row 78 is `in-progress`, `BRAINSTORM.md` and `SPEC.md` are landed, `DECISIONS.md` carries **ADR-0300 §Context** (PROPOSED), and `ROADMAP.md` + `BEHAVIOR_CONTRACT.md` are BYTE-UNTOUCHED. **This session performs the phase-78 PLAN.** Do NOT open a new subject.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=110 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **`want=110` IS A LIVE FIGURE, NOT A CONSTANT.** It is the ROADMAP data-row count, bumped 109 -> 110 at the phase-78 BRAINSTORM in the same commit that added row 78. **A row-adding phase MUST bump it in the same commit that adds the row** — otherwise every later session gets `GATE FAIL: examined 111 data rows, expected 110`. That noise is deliberate: a wrong denominator must be loud, because a silent one is how check (1) failed in the first place. **The phase-78 PLAN adds NO row, so `want` STAYS 110.**

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. When you open a new family, add its row-summary slug here.

**SENTINEL RE-RUN MECHANICALLY 2026-07-27 AT THE PHASE-78 SPEC — at session open AND AGAIN after the `DECISIONS.md` edit landed. It does NOT fire. `stop` was NOT created and MUST NOT be.** ⚠️ **Re-run it yourself anyway and record what YOU see** — a prior run's result is not evidence for the current tip. ACTUAL output, recorded rather than predicted:
- **(1) `NOT DONE: row 78`** — correct and intended; the row stays `in-progress` until the IMPL. **No `GATE FAIL` printed**, so the denominator 110 holds.
- **(2) FIVE, UNCHANGED** — `:188`, `:198`, `:208`, `:214` (long form) and `:222` (the Operational-tooling short form). **This row narrows NOTHING and that is STATED rather than forecast.**
- **(3) `NEVER OPENED: gRPC` and `NEVER OPENED: WASM`** — unchanged.

⚠️ **`ROADMAP.md` was BYTE-UNTOUCHED at the phase-78 SPEC (sha256-verified against HEAD), so no matcher string can have leaked.** That verification carried its own NEGATIVE CONTROL: the same sha256 comparison over `DECISIONS.md` correctly reported MISMATCH, proving the gate is not inert.

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`.** Every document from phase 75 forward recorded its blind spot — **109 data rows / 105 matched / FOUR misses** at an **em-dash** in an "after" cell, a **DOT** in the slug `http-1.1`, and two **LETTER-suffixed** ids `28.1a`/`28.1b` — and every one of them called it *"currently HARMLESS, since all four missed rows are `done`."*

⚠️ **Nobody fired the unsafe arm until the phase-77 PLAN. EXECUTED on scratch copies: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** A false "done" ends the project early. ⚠️ **DO NOT restore the regex form.**

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

The old check (2) matched ONLY the long phrase and could not see the **Operational-tooling** family's three genuinely un-chartered items, recorded in the **SHORT** form. Fixed at the phase-77 BRAINSTORM, at the MATCHER; negative controls all EXECUTED.

⚠️ **Normalising that Operational-tooling paragraph to the long form is a NAMED, DELIBERATELY-UNTAKEN follow-up.**

⚠️ **THE DOCUMENTED TRAP'S STATED MECHANISM IS REFUTED, WHILE THE TRAP IS REAL.** `reference_sentinel_deferred_sentence_live_vs_historical` says a naive `grep -c 'candidates:'` inflates *because of* the `candidates were:` recaps. It does inflate — but **not for that reason**: `candidates were:` does not contain that substring **at all**. The inflation comes from **ROADMAP table rows carrying BRAINSTORM prose**. **Cite the command; do not re-inherit the explanation.**

⚠️ **AND THE MEASURED RECORD: CHECK (2) HAS NEVER GONE DOWN** across ~19 phases (history `0 -> 1 -> 3 -> 4 -> 5`). **Phase 74 landed three `ssl.*` stat names and made its OWN sentence LONGER**; **phase 73 predicted a clause REMOVAL and got a clause REPLACEMENT**; **phase 77 opened a family and narrowed nothing**; **phase 78's BRAINSTORM ADDED an item to an existing sentence.** => **the candidates sentence is a WINDOW onto a larger deferred backlog, not an inventory of it.** ⚠️ **A stage that predicts a decrease here is repeating the phase-73 error. State what your row narrows — usually nothing — rather than forecasting.**

⚠️ **NEVER WRITE A SENTINEL'S OWN MATCHER STRING INTO A FILE THE SENTINEL GREPS.** The sentinel greps **`docs/envoy-go/ROADMAP.md`** — writing these strings *here* is safe; writing them *there* is not. This fired LIVE at the phase-76 BRAINSTORM, twice in one commit. **`grep` cannot tell a mention from a use.** ⚠️ **The phase-78 PLAN should leave `ROADMAP.md` BYTE-UNTOUCHED**; if it does edit it, re-run ALL THREE checks afterwards and verify mechanically that no matcher string leaked.

⚠️ **THE SELECTION CALCULUS, CORRECTED AND STILL STANDING.** *"A candidate that clears a check-(3) blocker is worth **strictly more**"* does **not** hold: opening a family **MINTS** a check-(2) sentence, so phase 77's own arithmetic was **NET ZERO**. What survives: **opening a family is the ONLY way that family's work becomes enumerable at all — a prerequisite, not a shortcut.** Do not restore the stronger claim.

==============================================


## STATUS AS OF 2026-07-27: PHASE 78 IS **OPEN**. ROW 78 IS `in-progress`. THIS SESSION PERFORMS THE **PHASE-78 PLAN**.

**What the phase-78 SPEC landed** (`git log --grep 'phase 78'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): `docs/envoy-go/phases/78-boot-panic-visibility/SPEC.md`, **ADR-0300 §Context** in `DECISIONS.md` (tail ADR-0299 -> **ADR-0300 PROPOSED**, next-free **ADR-0301**), and the `STATE.md` roll. **`ROADMAP.md` and `BEHAVIOR_CONTRACT.md` BYTE-UNTOUCHED** (sha256-verified against HEAD, with the gate's negative control observed). **Docs-only: ZERO production `.go`, ZERO test `.go`.** Four investigation agents on disjoint remits + controller re-derivation of every load-bearing claim. **The D-BPV-* docket is settled BY EXECUTION on every item; eleven claims (R1-R11) were REFUTED.**

### What matters for the NEXT session, in order

1. **⚠️ RE-RUN ALL THREE SENTINEL CHECKS YOURSELF AND RECORD THE ACTUAL OUTPUT; DO NOT PREDICT IT.** Create `stop` if and ONLY if all three print nothing. **Check (1) will print `NOT DONE: row 78` — that is CORRECT and the row stays `in-progress` until the IMPL.** `want` STAYS 110 (the PLAN adds no row).

2. **⚠️ THE SINGLE MOST IMPORTANT THING TO CARRY FORWARD: THE FIX IS *NOT* "MOVE THE DEFER".** The BRAINSTORM specified the shipped change as a relocation of the `<-flusherDone` defer (`cmd/envoy-go/main.go:298-303`) past the `close(flusherDone)` branches. **SPEC §1.1 REFUTED that BY EXECUTION: the relocation does not remove the hang, it MOVES it.** The relocated defer becomes the last-registered and therefore the **FIRST** to run in LIFO — ahead of `defer cancel()` (`main.go:340`), whose firing is what releases its own wait. Measured cross-product `{panic position} x {stats-sink presence}`:
   - baseline, pre-anchor panic, sink ⇒ **EXIT=124, 0 bytes** (the defect);
   - naive relocation, pre-anchor panic, sink ⇒ **EXIT=2, 2277 bytes** (fixed);
   - naive relocation, **post-anchor** panic, **NO sink** ⇒ **EXIT=2** — the defect is structurally IMPOSSIBLE here, because the `else` branch closes the channel before the defer is registered;
   - naive relocation, **post-anchor** panic, **SINK** ⇒ **EXIT=124, THE MOVED HANG**;
   - relocation **+ `cancel()` as the FIRST statement of the deferred body** ⇒ **EXIT=2 in 0.0105 s**.
   ⚠️ **The moved hang is INVISIBLE to a SIGTERM-based harness** — `timeout -s TERM` gives 124 at 15.005 s *with the panic text present* (the signal cancels ctx, releases the wait, and lets it finally print — it reads as "printed, just slow"), while `timeout -s KILL` gives 137 with the panic text **ABSENT**. **Pin the `cancel()`-paired form (SPEC §3.1). The bounded-`select` alternative is recorded, not pinned.**

3. **⚠️ BOTH GUARDS THE SPEC'S AGENTS DESIGNED PASS ON THE BUGGY TREE — SPEC §1.2, VERIFIED.** The behavioural guard's trigger panics PRE-anchor (genuinely fixed by the naive relocation), and the structural guard only asserts the receive follows every close (`367 > 364`). **A tree that still hangs would have shipped with two purpose-built guards green and a passing six-gate.** SPEC §3.7 opens **D-BPV-GUARD-COVERAGE**, a third arm asserting the release is present in the deferred body, whose **delete-the-`cancel()` negative control must go RED while the other two stay GREEN.** The PLAN must carry that arm AND that control.

4. **THE T5 BREAK ROSTER NEEDS AT MINIMUM SEVEN ARMS**, and two exist because the SPEC proved each half of the guard's conjunction load-bearing **by counter-example**: a **print-then-hang** build (recover + print the exact panic + block) **passes an output-only assertion**, and a **healthy boot** under a bounded-timeout harness **exits 124 exactly like the hang**, so an exit-status-only assertion is satisfied by **SUCCESS**. ⚠️ **The hang must be detected from the DEADLINE STATE (`ctx.Err() == context.DeadlineExceeded`), not from an exit code** — a deadline-killed process reports only `signal: killed`. ⚠️ **The repo has NO precedent for this**: `cmd/envoy-go/main_test.go` has zero `DeadlineExceeded` hits, so the PLAN introduces it. ⚠️ Use `t.Errorf` per property (`reference_fatalf_makes_assertions_unreachable`); the hang leg is the sole `t.Fatalf`, because after a hang there is nothing left to assert.

5. **⚠️ THE FULL 120-FIXTURE DIFFERENTIAL IS A MANDATORY IMPL GATE, NOT OPTIONAL.** `test/differential/harness.go:240` and `:594` build and spawn `./cmd/envoy-go` for EVERY fixture, so a shutdown-ordering change to `main()` has a 120-fixture blast radius, and the stats-sink fixtures (`0089-stats-sink-metrics-service` + the statsd / dogstatsd / graphite / OTLP siblings) are exactly the ones whose assertions depend on flush-before-close. **A green `cmd/envoy-go` package is NOT evidence about those fixtures.** Run with `-count=1` AND `-v` (a full-suite log without `-v` is VACUOUS — both `grep -c -- '--- PASS'` and `--- FAIL` return 0 on a fully green run).

6. **⚠️ COST: 7-9, NOT 5-7 — and RE-DERIVE IT AGAIN AT YOUR OWN TIP** (`reference_deferred_candidate_cost_restale`). The SPEC refuted the inherited 5-7 at the bottom, calibrated against phase 76 — also a small maintenance row, also anticipated ~5-7, **shipped NINE**. ⚠️ **AGREEMENT ACROSS DOCUMENTS IS NOT EVIDENCE.**

7. **THE DOCKET, ALL SETTLED — do not re-litigate, but DO re-derive any cite you rely on.** **D-BPV-ORDER**: the order changes, ONE pair matters, the other three changed pairs are benign by callee-read, and the sink contract holds STRUCTURALLY (the ONLY non-test `Submit` caller is `internal/statssink/flusher.go:49`) — ⚠️ **but the SIGTERM harness that "proves" it is VACUOUS**; a deliberately contract-violating build exits 0 with no panic 3/3 under SIGTERM and fails loudly on the panic path. **D-BPV-WINDOW**: span `main.go:304-371`, **68 lines**; ten in-window calls, three of which only SPAWN goroutines and therefore crash VISIBLY; ⚠️ **the inherited "3 certainly + 1 conditionally" reachability of the five `registry.go` panic sites is REFUTED — ONE is config-reachable, four are not.** **D-BPV-OTHER-DEFERS**: **NO SIBLING**, and the null is load-bearing because an injected blocking defer reproduced the hang instantly. **D-BPV-LOGFATAL**: confirmed three ways; ⚠️ the strongest control **holds the CALL SITE FIXED** — `boot.Construct` at `main.go:316` either returns an error (exit 1, 149 bytes printed) or panics (124, 0 bytes) ⇒ **the hang is PANIC-UNWIND-specific.** **D-BPV-GUARD-TRIGGER**: the vacuity PREMISE is REFUTED — both legislated-away futures go **RED**, so keep the trigger and strengthen the assertion; ⚠️ **but there is exactly ONE config-reachable trigger and NO fallback.**

8. **⚠️ THE EXPOSURE IS PANIC-GENERIC.** An inserted nil-deref hangs identically to an explicit `panic(`. **Scoping any guard or contract to "the five registry sites" or "the 18 `internal/` `panic(` sites" scopes it to the WRONG SET** — runtime panics carry no `panic(` token and are the larger share. Whenever you quote a `panic(` denominator, label it structurally incomplete at the point of use.

9. **⚠️ TWO NEW HAZARDS THE SPEC FOUND, NAMED AND DELIBERATELY NOT FIXED.** (a) `AsyncFileSink.Close` (`internal/accesslog/writer.go:91-98`) is the ONLY sink `Close` with **no grace timeout** — shutdown-class, out of this row's scope. (b) ⚠️ **A SECOND SILENT ZERO-BYTE BOOT HANG THAT IS NOT A DEFER AT ALL**: `os.OpenFile` at `internal/accesslog/writer.go:56`, reached from `main.go:117`, blocks forever on a FIFO access-log path with no reader — **BEFORE any defer is registered**, so this row's fix cannot reach it (EXECUTED with a reader-attached control). ⇒ **the BEHAVIOR_CONTRACT statement must say what it actually covers and must NOT claim boot failures are never silent.**

10. **⚠️ A CITE THE LINEAGE HAS BEEN INHERITING WRONG. ADR-0106 does NOT define the six-gate** — its block (`DECISIONS.md:4788-4857`) contains **zero** occurrences of "six", and its subject is the §9 family-expansion shape (flat rows, no-sibling-stub, the SOLE-leg property). **The six gates (a)-(f) are at `BOOTSTRAP_PROMPT.md:357-366` — at the REPO ROOT, not under `docs/envoy-go/`.** Cite it as *"the six-gate (`BOOTSTRAP_PROMPT` §7.5) ... the SOLE leg (ADR-0106)"*.

11. **⚠️ THE METHOD FINDING, WORTH MORE THAN ANY RESULT: THREE INDEPENDENT INVESTIGATIONS AGREED AND WERE ALL TESTING THE SAME BLIND SPOT.** Two agents and the controller's own first probe concluded the relocation was sufficient; every one of them had exercised only pre-anchor panic positions or the SIGTERM path — the two conditions under which the moved hang cannot appear. **The agreement was a shared input defect, not corroboration.** It broke only when one investigation varied the panic POSITION while another varied the DEADLINE SIGNAL and the SINK PRESENCE. ⇒ `reference_probe_must_discriminate` is necessary but **NOT sufficient**: each probe discriminated perfectly along the axis it varied, and every one of them held the decisive axis fixed.

12. **⚠️ THREE CONTROLLER SELF-CORRECTIONS FROM THIS STAGE, ALL TRANSFERABLE.**
    - **A sink-less probe config made the defect structurally IMPOSSIBLE, and the arm read as a refutation of the agent who was right.** The `else` branch closes `flusherDone` before the defer is registered, so that shape can never hang. **A probe's INPUT is a claim** (`reference_probe_input_is_a_claim`) — the arm was not a negative result, it was a control that could not fail.
    - **A first reproduction used `clusters: []`**; every arm died at `cluster: zero clusters in bootstrap`, and the arm meant to hang printed a message and exited — which reads as a clean refutation of the whole row. Caught only because the probe reported input byte counts beside its outputs. **A zero result and a broken arm are indistinguishable unless you measure the input.**
    - **A mechanism statement claimed `--mode validate` registers no defers.** It registers one (`validate/validate.go:47`) that RUNS during the unwind without blocking. **Both entry paths run deferred calls while the panic unwinds; one blocks and one does not.** The hazard is a **BLOCKING** defer, not defers as such — which is also why the pinned fix releases the wait rather than removing the defer.

13. **⚠️ BROKEN-GATE COUNT: THIRTEEN, unchanged at this docs-only stage.** The standing ones: a full-suite recipe **WITHOUT `-v` is VACUOUS** - a sha256 byte-gate roster built from the tip's `git ls-files` alone is **desynced BY CONSTRUCTION against a DELETED file** (span BOTH trees) - `gofmt -l` **NEVER exits non-zero** (gate on OUTPUT; the `&&` form is inert) - `go doc -all <A> <B>` **silently swallows arg2** (ONE package per invocation) - a **`+0 exported symbols` gate over an EMPTY package goes RED on a CORRECT tree** - a **RANGE** gate cannot detect anchor drift (use a content/symbol anchor) - a roster's naive `[ -f ] || continue` **exits 0 on a DELETED file** - a **count-only** stat guard **PASSES a build with BOTH names wrong**. ⚠️ **A harness's exit code is not the command's.**

14. **⚠️ ANCHORS DRIFT.** `DECISIONS.md` is **17531 -> 17560 (+29)** — the growth is a pure APPEND at the tail, so every pre-existing DECISIONS line cite is unaffected; any NEW one must still be re-derived. `ROADMAP.md` **226 (+0)** and `BEHAVIOR_CONTRACT.md` **5750 (+0)** — both BYTE-UNTOUCHED. ⚠️ **Re-derive every count and cite at the tip that reads it**, and **never write a token into the file whose occurrences of that token you are counting**.

15. **⚠️ A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX — the NINTH consecutive stage at which this held.** Live hazards: the full-suite startup flake (`subject ready: EOF` **and** `bind: address already in use`, both failing **BEFORE any assertion**, the latter as a **PANIC that can abort the whole binary**, firing **MORE readily under `-race`** and as the fixture count grows — now **120**) - `reference_sds_init_fetch_timeout_dial_budget_flake` - the PRE-EXISTING `internal/cluster` `-race` outlier flake - two still-**UNINDEXED** load flakes (`internal/httpclient TestOptions_ZeroValue_NoOpDefaults`, `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery`). **Isolate-re-run, then state the classification AND its evidence.** ⚠️ **`0061-lb-ring-hash` is NOT a live flake — a spread failure there is a FINDING.**

16. **Counts at this tip (re-run MECHANICALLY in your worktree; never copy them):** fixtures **120** (`ls -d test/fixtures/[0-9]*/ | wc -l`; ⚠️ the faithful `discoverFixtures` predicate is `^[0-9]{4}[a-z]?-`; next-free **0119**) / fuzzers **55** (a seed is not a `func Fuzz`) / **stat surface 1207**, ⚠️ **DOCUMENTARY — assert the DELTA; the absolute rides TWO unaudited ledger gaps** / **BackendKind tail 38** (⚠️ a TAIL VALUE — the file declares **39** constants, 0-38; **do NOT "fix" 38 to 39**) / go.mod modules **2** (⚠️ the phase-61.2 lineage figure — **NOT a repo total**; the single `go.mod` requires **67**; **do NOT "fix" 2 to 67**) / internal packages **73** / blank imports in `runner_test.go` **120**, ⚠️ **anchor on the FULL `^\t_ "github.com/pgdad/envoy-go/test/fixtures/` prefix — a naive `^\t_ ` gives 126** / DECISIONS tail **ADR-0300 PROPOSED** (next-free **ADR-0301**) / next-free reference port **10119** (family-banded `10<index>`). ⚠️ **`STATE.md` Project-counts section still says fixtures 119** — NOT drift; phase 77 landed `0118` after that block was written.

17. **⚠️ DOCUMENTARY DEFECTS, STILL NAMED AS DEFERRALS AND STILL DELIBERATELY NOT FIXED.**
    - **The documented PUBLIC IMPORT PATH DOES NOT EXIST.** `head -1 go.mod` is `github.com/pgdad/envoy-go`; the docs say `github.com/esalaine/envoy-go/validate` on ~20 lines across four docs. ⚠️ **`DECISIONS.md:142` is an ADR that DECIDES the wrong path, never superseded**, and ⚠️ **`BEHAVIOR_CONTRACT.md:862` carries it inside the VERY SECTION the phase-78 IMPL extends** — the IMPL must not inherit it into the new subsection.
    - **A mechanical stat-surface count** to replace the documentary 1207 — **8-11 tasks**.
    - **UNRESOLVED:** the *"plus two more in the same file"* half of the `BEHAVIOR_CONTRACT.md` stale-cite claim (`internal/tls/config.go:272`, `internal/filter/http/chain.go:19` flagged, **not audited**).
    - `BEHAVIOR_CONTRACT.md:501` calls the `wasm.` arm a *"NEW SN9 flattening rule"*, which **COLLIDES with ADR-0118's actual SN9**; and `internal/stats/name.go:350`'s error string enumerates `(want cluster.|http.|listener.|server.)` — **already wrong**.
    - **The `/stats/prometheus` projection-completeness gap** (`ROADMAP.md:208`) — 30 names / 6 families / 28 pre-dating phase 77. **UNBLOCKED by row 78** once it lands.

## What THIS session does — the phase-78 PLAN

One stage per session. Create `docs/envoy-go/phases/78-boot-panic-visibility/PLAN.md` (a TDD spine over SPEC §9's **7-9** tasks, with the T5 break roster of **at minimum SEVEN arms**) and `PROGRESS.md`. **ROW 78 STAYS `in-progress`**; leave `ROADMAP.md` and `BEHAVIOR_CONTRACT.md` BYTE-UNTOUCHED if you can. `DECISIONS.md` should stay BYTE-UNTOUCHED too — ADR-0300 §Decision + §Consequences land at the **IMPL**, appended IN PLACE after the retained italic footer, **no renumber and NO `---` separator**. Roll `STATE.md` Current-pointer section **IN PLACE** (ADR-0288) and this file.

⚠️ **`ADR-0089` stays BYTE-UNTOUCHED unless your row lands `/runtime` or `POST /runtime_modify`** — row 78 lands neither.

Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** (`reference_parallel_subagents_private_scratch`), **BANDED PORTS**, and **must tear down docker containers BY NAME, never by an `ancestor`/image filter** (`reference_parallel_agents_shared_machine_namespaces`) — and ⚠️ **if two agents share ONE worktree they must stage by EXPLICIT PATH; a `git commit -am` sweeps in a sibling's uncommitted work.** ⚠️ **AND THEY MUST NOT RUN AN UNSCOPED `git restore`.** Per-task `gofmt` + `golangci-lint` on touched packages (`feedback_pertask_gofmt_lint`).

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. ⚠️ **THIS HAS NOW FIRED LIVE IN THIRTEEN CONSECUTIVE SESSIONS** — at this SPEC it fired after a `cd` into a throwaway worktree. **Assume it fires every session.** **Use `git -C <abs-worktree-path>` for every git command, prefer absolute paths over `cd`, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — Current-pointer section (TRUSTWORTHY; ADR-0288 discipline — ⚠️ **anchor greps on `^- **<field>`**, not bare; all seven fields verified singleton at the phase-78 SPEC close) + Project counts + the Recent lineage. ⚠️ **Two PRE-EXISTING bookkeeping gaps are recorded there rather than silently fixed:** there has never been a `phase-77 PLAN` lineage entry, and the heading says FIVE while Current plus the prior entries is more.
2. `docs/envoy-go/phases/78-boot-panic-visibility/SPEC.md` — the whole document, especially **§1.1** (the fix as specified is INSUFFICIENT), **§1.2** (both guards pass on the buggy tree), **§1.3** (the R1-R11 drift ledger), **§3.1** (the pinned fix), **§3.7** (the NEW coverage docket item), **§3.8** (the comment audit: 3 FALSE / 2 MISLEADING / 2 SURVIVE — ⚠️ **`:304-308` and `:112-113` must stay BYTE-UNTOUCHED**), **§9** (the 7-9 task surface + the mandatory full-differential gate), **§11** (the two new deferrals), and **§14** (the adversarial-pass record and the method finding).
3. `docs/envoy-go/phases/78-boot-panic-visibility/BRAINSTORM.md` — ⚠️ **read it AFTER the SPEC, and read §2.2 as SUPERSEDED**; its "move the defer" scope is exactly what SPEC §1.1 refuted.
4. `cmd/envoy-go/main.go` — read `:186-210`, `:289-350` and `:355-380` FIRST-HAND before writing anything.
5. `docs/envoy-go/DECISIONS.md` — **ADR-0300 §Context** (the eleven paragraphs this PLAN must not contradict) - **ADR-0045** (the split gate) - **ADR-0288** - ADR-0044 (⚠️ it does **NOT** contain the Context-draft discipline — use `ADR-0044-as-used`) - **ADR-0106** (⚠️ the SOLE-leg property, **not** the six-gate — that is `BOOTSTRAP_PROMPT.md` §7.5).
6. `docs/envoy-go/phases/77-runtime-static-layer/PLAN.md` and `docs/envoy-go/phases/76-lb-ring-hash-spread-margin/PLAN.md` — the shape of a PLAN in this lineage, and the NINE-task calibration behind the 7-9 re-derivation.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — especially **`reference_probe_must_discriminate`**, **`reference_probe_input_is_a_claim`**, **`reference_liveness_break_needs_failing_baseline`**, **`reference_gate_command_negative_control`**, `reference_empty_output_is_not_a_zero_result`, `reference_fatalf_makes_assertions_unreachable`, `reference_harness_exit_code_is_not_command_exit_code`, `reference_a_drift_correction_is_itself_a_claim`, `reference_deferred_candidate_cost_restale`, `feedback_brief_citations_not_evidence`, `reference_next_prompt_tracked_despite_gitignore`, `feedback_git_worktrees`, `feedback_execution_style`, and **`reference_bash_cwd_reset_commits_to_main`**.
