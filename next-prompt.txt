=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. **There IS banked work at this tip** — phase 75 is mid-lifecycle at lifecycle-state 1 — so this session ADVANCES it and does NOT open a new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-RUN MECHANICALLY 2026-07-24 at the phase-75 BRAINSTORM close, TWICE (stage worktree AND landed master after the squash-push). It does NOT fire. `stop` was NOT created.** ACTUAL output, recorded rather than predicted:
- **(1) prints `NOT DONE: row 75`** — the row-75 registration **RE-OPENED check (1)** after its first-ever silent close at the phase-74 IMPL, exactly as the phase-74 BRAINSTORM did for row 74. Check (1) is therefore LIVE again and can catch a mistake once more; it goes silent again only at the phase-75 IMPL.
- **(2) prints THREE live "candidates:" sentences** — HTTP/3, xDS, Observability. **Cite the command, never the adjective** (`reference_sentinel_deferred_sentence_live_vs_historical`): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` => **3**. ⚠️ A naive `grep -c 'candidates:'` returns a larger number and would mislead. ⚠️ **The line anchors ALL SHIFTED +1 at this close** (row 75 was inserted into the main table) — RE-DERIVE them with `grep -n`, do not copy. **NO deferred sentence was narrowed at this BRAINSTORM** (phase-57 precedent) — PROVEN MECHANICALLY: the ROADMAP diff is exactly **2 insertions / 1 deletion**, the family-paragraph edit is a **PURE APPEND** (the old line is a byte-prefix of the new, **+1255** chars), and the deferred sentence is **BYTE-IDENTICAL** across the diff, still terminating at `force-trace.` so the `[^.]*\.` regex still binds.
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** => THREE families never opened. (The `WASM` line remains a ROADMAP **bookkeeping artifact** — `internal/wasm/` is a substantially-built wazero host and rows 25/25.1/25.2/25.3 are all `done` — deliberately LEFT AS-IS.)

All three print => the sentinel does NOT fire. Abundant work remains.

⚠️ **CHECK (1) HAS A KNOWN BLIND SPOT — RE-DERIVED AT THIS BRAINSTORM, and the figure HELD.** At this tip: **106 data rows, 102 matched, FOUR misses**, identified by set-difference: `| 00 | bootstrap | — | done |` (the `[0-9.,  ]*` field does not match an **em-dash** "after" column), `| 04 | http-1.1 | 03 | done |` (the `[a-z0-9-]+` slug field does not match the **DOT**), and **`| 28.1a |` and `| 28.1b |`** (the `^\| [0-9.]+ \|` id field cannot match a **letter suffix**). All four are `done`, so there is no current impact. ⚠️ The phase-74 PLAN recorded a STALE *"104 rows, 102 matched, TWO misses"* — **RE-DERIVE this figure; never copy it.** (Row 75 matches normally and is included in the 106/102.)

==============================================


## STATUS AS OF 2026-07-24: THE PHASE-75 BRAINSTORM IS COMPLETE AND LANDED. ROW 75 IS REGISTERED `in-progress`. THIS SESSION PERFORMS THE PHASE-75 SPEC.

**What the phase-75 BRAINSTORM landed** (`git log --grep 'phase 75'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): the subject `tls-no-certificate-stat`, SELF-PICKED per the standing directive after **FIVE read-only re-costing agents** at tip `f8f6cd44` on disjoint remits (two running LIVE reference containers, three running Go-toolchain probes). Docs-only: **ZERO production `.go`**, exactly THREE files edited (`ROADMAP.md`, `STATE.md`, `next-prompt.txt`) plus the new `BRAINSTORM.md`; **DECISIONS.md BYTE-UNTOUCHED**.

**THE SUBJECT.** ONE fixed name — **`listener.<normalized-addr>.ssl.no_certificate`**, a **SUCCESS-PATH annotation** counting *"this COMPLETED handshake presented no client certificate"* (⚠️ **NOT** a synonym for `ssl.fail_verify_no_cert` — wrong in BOTH directions) — added to the block `registerListenerMetrics` ALREADY gates on `rt.tlsMode` (`internal/listener/manager.go:378-382`) and Inc'd on the success fall-through phase 74 left at `manager.go:1276-1278`, where `tlsConn` is in scope. **NO new gate, NO new classifier, NO new registration function.** Anticipated: **+1 stat (1204 → 1205)** · **+0 fixtures (119** — `0110-tls-require-client-cert-false` EXTENDED**)** · +0 fuzzers (**55**) · +0 BackendKinds (**38**) · +0 modules (**2**) · +0 production imports · ZERO new packages · ZERO new exported symbols · ONE ADR (**ADR-0297**).

### What matters for the NEXT session, in order

1. **⚠️ THE SENTINEL DID NOT FIRE AND `stop` WAS NOT CREATED. Re-run all three checks yourself and record the ACTUAL output; do not predict it.** Check (1) is **LIVE again** (it prints `NOT DONE: row 75`) and stays live until the phase-75 IMPL. **Create `stop` if and ONLY if ALL THREE print nothing.**

2. **⚠️ THE ONE DOCKET ITEM THAT DECIDES THE ROW — D-TLSNC-SEMANTICS, and it is GENUINELY OPEN.** Does the reference increment `ssl.no_certificate` on a plain **one-way-TLS** listener with **NO `validation_context` at all** (`ClientAuth == NoClientCert`, where a certificate is never even requested)? **TWO readings are live and both fit the record:** **(a) unconditional** — every completed handshake without a peer certificate, so the Inc gate is `len(tlsConn.ConnectionState().PeerCertificates) == 0` alone and the counter fires on the 9 fixtures carrying a downstream TLS context; **(b) request-gated** — only where a certificate was SOLICITED, so the gate needs the chain's client-auth mode too. `BEHAVIOR_CONTRACT.md:962` records the reference incrementing it in an **anchorless `validation_context: {}`** shape where *"the reference … never sends a CertificateRequest in ANY cell"*, which **LEANS (a)** — but it was probed for a different purpose and is **NOT decisive**. **This decides the predicate and must be settled by probe before the PLAN.**

3. **The rest of the D-TLSNC-* docket.** **CERTPRESENT** — confirm the reference books **0** on the cert-presented arm (predicted 0, unprobed). **FAILPATH** — confirm it does NOT move on a FAILED handshake; the whole success-path reading depends on it. **SCOPE** — re-confirm registration is TLS-chains-only **PER NAME**, not by family inheritance from phase 74. **FIXTURE** — confirm Shape A (scrape once, absolute counts) is sound on `0110`, i.e. that nothing pre-moves its `ssl.*` before the driver's arms.

4. **⚠️ A HAZARD FOR THE SPEC'S OWN PROBES, discovered by execution at the BRAINSTORM and NOT previously recorded anywhere.** With the reference's tcp_proxy upstream cluster pointed at a **DEAD** endpoint, a fully-successful TLS 1.3 handshake left `ssl.handshake` at **0** across two runs — along with `ssl.no_certificate` and the whole `logHandshake` family — and repointing the upstream at a **LIVE** endpoint, a single-variable change, made all of them fire. **Two observations vs one; the mechanism is NOT established.** `0111` has a live echo backend, which is why phase 74's cross-side match held. **Any probe or fixture asserting `ssl.*` on a failing-upstream arm must treat this as LIVE**, and `0110`'s backend liveness must be confirmed before its counts are trusted. A probe that ignores this will read a real counter as a framework gap.

5. **The fixture is ALREADY BUILT and it DISCRIMINATES — that is the row's non-vacuity argument.** `test/fixtures/0110-tls-require-client-cert-false/` drives **THREE arms** against one `require_client_certificate: false` listener (`driver/driver.go:362-373`; `wantObservable` at `:89`): trusted (forced-send, ACCEPTED **with** a cert) / untrusted (REJECTED) / **none (no cert, ACCEPTED — the flag's discriminator)**. Predicted per side: `handshake=2`, **`no_certificate=1`**, `fail_verify_error=1`, `fail_verify_no_cert=0`. ⚠️ **Arms 1 and 3 DISCRIMINATE — `no_certificate` is 1, NOT 2** — and `0111` (require=true) could never have supplied that. `0110` already carries `subjAdminPort` plumbing (`driver.go:313`) and `ProbeAdmin` (`:552`) but has **no** `AssertStats`; the `0111` template at `driver/driver.go:655` (+`scrapeProm` at `:739`) is directly transplantable. ⚠️ `var _ fixture.StatsAsserter` is a **TRIPWIRE, not the dispatch mechanism**, and the **ABSENT check must be SEPARATE from the value check** — an unregistered counter reads `0 == 0` and passes VACUOUSLY.

6. **⚠️ TWO PRODUCTION FILES, and the second one's guard will NOT catch you.** Edits land in `internal/listener/manager.go` **and `internal/stats/name.go`** (the `helpText` entry at `:469-471`). Without it `prom.go` degrades HELP to the metric name. `TestHelpText_ListenerSSLHandshakeOutcomes` (`internal/stats/name_test.go:230`) asserts exactly that degradation signature — **but over a HAND-LISTED three-name roster** (`:231-235`), so a fourth name added without extending `wantNames` is **SILENTLY UNGUARDED**. Same staleness class phase 74 found in `TestListenerManager_AllocatesTwoMetricsPerListener`.

7. **The guard roster a +1 name delta touches** (all RE-DERIVED at the BRAINSTORM tip; RE-DERIVE again at the SPEC tip): `TestListenerMetrics_TLSListenerRegistersExactlyThreeSSLNames` (`manager_test.go:2023`) needs a `want` entry **in SORTED position** AND a **RENAME** plus its doc comment · `TestListenerMetrics_GateMatchesInc` (`:2128`) needs **+2 pointer assertions** — the pointer half is the LOAD-BEARING half · the QUIC name-set guard (`quic_test.go:277`) needs a `want` entry, after which its zero-loop covers the new name free · `PlaintextListenerRegistersNoSSLNames` (`:2076`) and `AllocatesBaseListenerMetrics` (`:1951`) need nothing. ⚠️ **A nil `*stats.Counter`.Inc is a PROCESS CRASH** — no nil guard, no `recover()` on the `serveConnection` goroutine. The pointer assertions are mandatory.

8. **⚠️ ADR-0297 OWES A CROSS-ADR CORRECTION, and the BRAINSTORM established it mechanically.** ADR-0296 §Decision **(g)** (`DECISIONS.md:17308`) claims *"envoy-go does not implement `require_client_certificate: false` (verify-if-present) at all — §Context ¶7 / `VERIFYIFGIVEN` is explicitly OUT OF SCOPE"*. **Both halves fail.** Phase 67 (**row 67 `done`**) landed it across ALL THREE validation-source shapes — `internal/tls/config.go:79-84`, documented in place at `:60-68` — with fixture `0110` driving it since phase 67. And `grep -n 'VERIFYIFGIVEN' docs/envoy-go/DECISIONS.md` returns **exactly ONE hit: `:17308`, the citing sentence ITSELF**; §Context ¶7 (`:17274`) is about the four-family split and contains **ZERO** `require_client_certificate`. This is the **THIRD** internal mis-pointer in ADR-0296. The narrower claim (not owed *in phase 74*) SURVIVES; the reason given does not. **FORM: the INDENTED `> [CORRECTED at phase 75/ADR-0297: …]` blockquote** (two literal spaces, then `> [`) — the discriminator is the **ADR FAMILY, not the phase gap**. Phrase it as a **documentary finding, NOT as criticism of phase 74**, whose row was correctly scoped.

9. **⚠️ THE `0110` README EDIT SPLITS A BULLET; IT DOES NOT DELETE ONE.** `README.md:160-161` reads *"**No `ssl.*` stats** — envoy-go emits none, so a verdict `StatsAsserter` is infeasible … Never assert `/listeners` or `total_listeners_active`; never treat a docker-proxy accept as listener liveness."* The `ssl.*` half went **FALSE at the phase-74 IMPL**; the `/listeners` guard stays **LIVE and correct**. Phase 74's SPEC found this exact bundling at FOUR sites in `0111`.

10. **The deferred roster, re-costed at the BRAINSTORM tip — read these as CURRENT, and RE-DERIVE anyway (`reference_deferred_candidate_cost_restale`).** **`ssl.connection_error`** — its *"membership is UNENUMERATED"* blocker is **REFUTED BY EXECUTION** (18 Go arms + 16 LIVE reference arms, every round control-validated): the reference books it for every **decodable but rejected** handshake attempt and books **NOTHING** when the peer left without a complete ClientHello. But the finding **RAISED** the cost — a naive catch-all **OVER-COUNTS** the EOF class (`io.EOF`/`io.ErrUnexpectedEOF`), the highest-frequency class in production, so a PREDICATE is required, `manager_test.go:4294`'s landed `{"io.EOF", io.EOF, outcomeOther}` row **IS** that over-count, and it **cannot ride `0111`** (a 4th arm changes `wantObservable`, moves `downstream_cx_total` 3→4 and invalidates Shape A) ⇒ a NEW fixture is FORCED. ~9-11 tasks. · **`fault.abort.grpc_status` — THE RECOMMENDED NEXT OPENING** (7-9 tasks), the ONLY identified candidate that clears a sentinel check-(3) blocker; ROADMAP row 09 already assigns it to the gRPC family and it is a LIVE divergence (`internal/filter/http/fault/fault.go:132-140` sets `abortEnabled` ONLY for the `FaultAbort_HttpStatus` arm). It escapes the family's hard blocker (trailers observed-and-discarded per ADR-0058, `internal/filter/hcm/h2/client.go:440`; `RunDecodeTrailers`/`RunEncodeTrailers` have **ZERO** production callers — all five non-test hits are COMMENTS) because a gRPC error local reply is **Trailers-Only, headers-only**, already emitted at `internal/filter/http/extproc/check.go:518-530`. **ONE probe owed: if the reference emits REAL trailers the row collapses into the blocked set.** · **`upstream_cluster` span tag — NO LONGER BLOCKED**, its residual divergence is CLOSABLE via a **route-resolved** name in scope at 4 of the 5 zero-endpoint sites; ~25 lines, 7-9 tasks. ⚠️ **The DISCRIMINATING break phase 74 handed forward DOES NOT EXIST** — `extractEndpoints` (`internal/cluster/manager.go:875`) makes ZERO `GetClusterName()` calls; the break would have been **VACUOUS**. · **`Listener.stat_prefix`** (~8; ONE production prefix site at `manager.go:375`; live probe shows a pure RENAME with the prefix hoisted into the `envoy_listener_address` LABEL ⇒ `internal/stats/name.go` needs ZERO change; its payoff is making that label CROSS-SIDE IDENTICAL) · **Runtime family opening** (9-11, +1 package; ⚠️ `layered_runtime` is a hand-written **REJECT** at `internal/bootstrap/bootstrap.go:568-569`, not a silent ignore) · `stats_flush_on_admin` (8-10; the 53-site `admin.New` figure CONFIRMS but is **NOT load-bearing** — `MarkReady()` at `internal/admin/admin.go:110` is the setter precedent) · `hcm.access_log_options` (9-12) · HCM `server_name`/`via` + the `Server` OVERWRITE-vs-APPEND defect (10-14) · the `stdout`/`stderr` loggers (10-12; blocker CONFIRMED REAL — `test/differential/harness.go:253`'s `cmd.StdoutPipe()` is never drained after the ready sentinel) · `hcm.merge_slashes` (11-14) · the other TEN fixed `ssl.*` names · all tracing remainders · all xDS · all HTTP/3.

11. **⚠️ THREE CORRECTIONS TO THE LANDED RECORD THE SPEC SHOULD NOT RE-INHERIT.** **(a)** `registry.go:107` is the **DUPLICATE-registration** panic; the **INVALID-NAME** panic is **`:117`** (mis-cited as `:107` in ADR-0296 §Context ¶7, the phase-74 BRAINSTORM and the row-74 cell). **(b)** **There is NO `B5` step in `BEHAVIOR_CONTRACT.md`** — all eight `B5` hits are `AMEND-B5`/phase-25.2 Wasm, and the `connection_error` departure actually lives at `:1855` under the `:1849` heading. Yet *"BEHAVIOR_CONTRACT B5"* **leaked into TWO LANDED PRODUCTION CODE COMMENTS** (`internal/listener/manager.go:392` and `:1265`) plus `DECISIONS.md:17304`/`:17314` — PLAN-internal step numbering became a permanent cross-reference pointing at nothing (`reference_code_comment_not_evidence`, newly instantiated IN PRODUCTION). **(c)** The **FOUR-family dynamic deferral is WRONG IN BOTH DIRECTIONS**: `ssl.versions.*` has **NEITHER** recorded blocker (Envoy's own `TLSv1.2` form passes `NamePattern`; the charset trip is in **Go's** `VersionName`, which returns `"TLS 1.2"` and fails on a **SPACE**, not a hyphen), while `ssl.sigalgs.*` carries a **THIRD, HARDER blocker the record does not name** — `testingOnlyPeerSignatureAlgorithm` is **UNEXPORTED** on `tls.ConnectionState`, making it **STRUCTURALLY UNIMPLEMENTABLE**; and `ConnectionState.CurveID` **IS** exported at go1.26.5.

12. **⚠️ THE NEXT-FREE REFERENCE PORT IS `10450`, NOT `10118`.** Max allocated is **10449** (`test/fixtures/0113-stats-sink-otlp-knobs/driver/driver.go:115`); **ports are NOT fixture-index aligned** (`0117` uses `10117`, `0113` uses `10449`). The next fixture DIRECTORY is `0118`. Moot for row 75 (+0 fixtures) but load-bearing for the deferred roster. *(The BRAINSTORM controller initially repeated `10118` and corrected it on re-derivation — recorded, not quietly amended.)*

13. **⚠️ A CONTESTED CLAIM WAS RESOLVED AGAINST A SUBAGENT — do not re-inherit its refutation.** One re-costing agent reported ADR-0296's `BEHAVIOR_CONTRACT.md:962` anchor as FALSE, claiming `ssl.no_certificate` appears nowhere in that file. **That refutation is itself WRONG.** The token sits at **character offset 627** of line 962 (*"…accepts every client with `ssl.no_certificate` incrementing…"*), whole-file `grep -c 'ssl\.no_certificate'` returns **1**, and a second agent independently quoted the same text. **The `:962` anchor STANDS** — the misreading is explained by that line ALSO containing `require_client_certificate`. Recorded because **a drift CORRECTION is itself a claim** (`reference_a_drift_correction_is_itself_a_claim`), and this one failed. **A subagent's refutation is not evidence either.**

14. **⚠️ NO PROBE WAS RUN AGAINST THE REFERENCE FOR THIS ROW'S OWN SUBJECT, AND NO BUILD OF ROW 75 EXISTS, in-tree or in scratch.** The `ssl.no_certificate` success-path semantics are **DOC-SOURCED** from phase 74's probe fleets and `BEHAVIOR_CONTRACT.md:962`, **NOT re-executed**. Nothing in the BRAINSTORM may be cited as execution evidence for buildability — §11.1 says so explicitly. The Go-toolchain probes that DID run (four live loopback handshakes confirming `PeerCertificates` is 0 vs 1 at both TLS 1.2 and 1.3) ran **outside this repository's build**.

15. **The live hazards — never reflex-classify any of these as a regression.** `reference_sds_init_fetch_timeout_dial_budget_flake` (⚠️ **WIDENED at the phase-74 close** — it now covers `internal/boot TestSDSEndToEnd_FetchFailure_BootFailsClosed` as well as the original `internal/xds` test, same 200 ms-budget mechanism, REPRODUCED ON MASTER 3/20 with a byte-identical error string) · the PRE-EXISTING `internal/cluster` `-race` flake (`TestOutlierDetector_ConcurrentEjectExactlyOnce`, `internal/cluster/outlier_test.go:766` — **isolate-re-run, do NOT re-classify**) · the full-suite startup flake (`subject ready: EOF` on an UNRELATED fixture) · and TWO still-**UNINDEXED** load flakes, isolate-green and NOT root-caused: `internal/httpclient TestOptions_ZeroValue_NoOpDefaults` and `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery`.

16. **Counts at exit (re-run MECHANICALLY in the worktree; never copy them):** fixtures **119** (`ls -d test/fixtures/[0-9]*/ | wc -l`) / fuzzers **55** (`grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`) / **stat surface 1204** (BEHAVIOR_CONTRACT doc count; **NO mechanical command**) / BackendKind tail **38** (a TAIL VALUE, not a count — the file declares 39 constants, 0-38) / go.mod modules **2** (the phase-61.2 lineage figure; the single `go.mod` requires **67**) / DECISIONS tail **ADR-0296 COMPLETE** (next-free **ADR-0297**) / ZERO new packages. **All UNCHANGED — the BRAINSTORM was docs-only.**

## What THIS session does — the phase-75 SPEC

One stage per session. A SPEC resolves the BRAINSTORM's docket **BY EXECUTION where execution is possible**, drafts **ADR-0297 §Context** (per **ADR-0044-as-used** — ⚠️ ADR-0044 does NOT actually contain that discipline; use the hedge), and **narrows NO deferred sentence** (the phase-57 precedent: the narrow lands at the IMPL only). Row 75 **STAYS `in-progress`**; the ROADMAP stays BYTE-UNTOUCHED.

1. **Resolve the D-TLSNC-* docket by live probe against `envoyproxy/envoy:contrib-v1.37.2`** — **SEMANTICS is the one that matters** (item 2 above); CERTPRESENT, FAILPATH, SCOPE and FIXTURE follow. Fresh container per arm (`feedback_probe_fresh_container_per_arm`), bridge networks (`reference_docker_probe_bridge_network`), a positive control in EVERY bootstrap, and **each arm's discrimination criterion stated BEFORE the run** (`reference_probe_must_discriminate` — an input consistent with BOTH hypotheses proves nothing). ⚠️ **Heed item 4's dead-upstream hazard**: give every probe bootstrap a LIVE upstream, or a real counter will read as a framework gap.
2. **RE-DERIVE every anchor you cite.** A BRAINSTORM is not evidence, an ADR is not evidence, **a document's own hygiene claim is not evidence**, and — newly demonstrated at this BRAINSTORM — **a subagent's refutation is not evidence either** (item 13). Re-derive PROSE claims too.
3. **State plainly what was and was NOT executed.** The BRAINSTORM's §11.1 does this; keep the discipline.
4. **Close by re-running all three sentinel checks mechanically, TWICE** — in the stage worktree AND on LANDED master after the squash-push (the phase-72/73/74/75 precedent: a stage worktree can disagree with what actually landed). Check (1) should still print `NOT DONE: row 75`; **record the ACTUAL output either way.**

**Stage-close mechanics (a SPEC):** the phase-directory delta is the new `SPEC.md`; **DECISIONS.md gains the ADR-0297 §Context append ONLY** (a PURE APPEND — verify `### Context` 1 / `### Decision` 0 / `### Consequences` 0, and that the tail flip ADR-0296 → ADR-0297 IS that append); **ROADMAP BYTE-UNTOUCHED**, row 75 STAYS `in-progress`. STATE §Current rolled **IN PLACE** (lifecycle 1 → 2; §Recent re-capped at FIVE **with its PREAMBLE updated** — the ADR-0288 rule). ⚠️ **The ADR-0288 singleton greps return 2, not 1**, for each of the three field-name tokens — the second hit is `STATE.md:7`, the RULE STATEMENT itself. Verify with `grep -n`, and **never "fix" the count to 1**; that would delete the rule. `next-prompt.txt` rolled to the phase-75 PLAN.

Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel probe fleets need **DISJOINT port ranges, container prefixes and PRIVATE scratch** (`reference_parallel_subagents_private_scratch` — a shared scratch yields a FALSE NEGATIVE).

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. ⚠️ **THIS FIRED LIVE DURING THE PHASE-75 BRAINSTORM** — a `cd` into the scratch directory to run a Go probe reset the shell to `/home/esa/git/envoy-go`, and only the `git -C` discipline kept the stage edits in the worktree. At the phase-72 close the same hazard put a stray commit on `master` in the MAIN checkout AND made a "final gates" run validate master's pre-row tree — GREEN while proving nothing. **Use `git -C <abs-worktree-path>` for every git command, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline) + §Project counts + the §Recent lineage (five bullets: phase-75 BRAINSTORM + phase-74 IMPL + phase-74 PLAN + phase-74 SPEC + phase-74 BRAINSTORM).
2. `docs/envoy-go/phases/75-tls-no-certificate-stat/BRAINSTORM.md` — **the whole document**, and especially **§2.1** (the pick + the ADR-0296 §Decision (g) headline), **§2.4** (the rivals, two of them materially changed by re-derivation), **§6** (the anticipated edit sites + the guard roster), **§7** (the D-TLSNC-* docket + the dead-upstream probe hazard) and **§11.3** (the contested claims resolved by the controller).
3. `docs/envoy-go/ROADMAP.md` — the §Schema invariant, the row-75 cell, the THREE live `candidates:` sentences and the `### Observability family` heading. ⚠️ **RE-DERIVE all of these line numbers with `grep -n`** — they ALL shifted +1 when row 75 was inserted. The Observability family-history line is the ~44,000-char one; the short line just above it is the family DESCRIPTOR.
4. `docs/envoy-go/DECISIONS.md` — **ADR-0296** in full (§Context ¶7/¶8, §Decision (e)/(g), §Consequences — the row's direct parent, and **(g) is the paragraph ADR-0297 must correct**) · **ADR-0286** §Consequences **C3** and its phase-74 `[CORRECTED …]` blockquote (the FORM to copy: **two literal spaces, then `> [`**) · ADR-0289 (phase 67, verify-if-present) · ADR-0044 (⚠️ read it — it does NOT contain the §Context-draft discipline) · ADR-0045 · ADR-0106 · ADR-0117 · ADR-0288.
5. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — **`:962`** (the phase-67/ADR-0289 anchorless-`validation_context` departure; ⚠️ the `ssl.no_certificate` mention sits at **character offset 627** of that line and is the ONLY occurrence in the file) · the `:928` C3 coverage boundary · the `:1849` downstream-TLS-handshake-outcome subsection and its `:1855` `connection_error` departure (⚠️ **NOT** "B5") · the `### Stat surface` ledger at `:831`/`:847`.
6. `test/fixtures/0110-tls-require-client-cert-false/` — `README.md` (esp. `:150-163`, the bundled coverage-boundary bullet), `driver/driver.go` (the three arms at `:362-373`, `wantObservable` `:89`, `SubjectConfig` `:313`, `ProbeAdmin` `:552`), and `envoy.yaml`. Then `test/fixtures/0111-tls-cvc-empty-dynamic-fallback/driver/driver.go:655-760` — the `AssertStats`/`scrapeProm` template to transplant.
7. `docs/envoy-go/phases/74-tls-handshake-outcome-stats/{BRAINSTORM,SPEC,PLAN}.md` + `PROGRESS.md` §IMPL record — the immediately-prior lineage. ⚠️ **`SPEC.md:1`, `:315` and `:320` carry that SPEC's OWN REFUTED first QUIC reading** against its authoritative §3.4/§16; they were deliberately left as a historical record of a reversal. Read the SPEC THROUGH `PLAN.md` §1.1's RD-* ledger.
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Load-bearing for this SPEC: `reference_probe_must_discriminate`, `feedback_probe_fresh_container_per_arm`, `reference_docker_probe_bridge_network`, `reference_host_gateway_ip_docker_desktop`, `reference_docker_desktop_kvm_acl_after_reboot`, `reference_deferred_candidate_cost_restale`, `feedback_brief_citations_not_evidence`, `reference_quoting_is_not_executing`, `reference_a_drift_correction_is_itself_a_claim`, `reference_document_hygiene_claim_not_evidence`, `reference_go_client_cert_withholding`, `reference_netpipe_deadlocks_client_cert_handshake`, `reference_nil_stats_counter_inc_crashes_goroutine`, `reference_dynamic_stat_name_charset_guard`, `reference_listener_stat_scope_cross_side_divergence`, `reference_differential_asserter_dispatch`, `reference_parallel_subagents_private_scratch`, `reference_next_prompt_tracked_despite_gitignore`, and **`reference_bash_cwd_reset_commits_to_main`** (⚠️ it FIRED LIVE at the BRAINSTORM).
