=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

**THERE IS BANKED MID-LIFECYCLE WORK AT THIS TIP.** Phase 80 (`stats-sds-projection`) is **OPEN** — row 80 is `in-progress` and only `BRAINSTORM.md` is landed. **This session performs the phase-80 SPEC.** Do NOT self-pick a new subject; advancing banked mid-lifecycle work is the more defensible move and the standing directive says so explicitly.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=112 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **`want=112` IS A LIVE FIGURE, NOT A CONSTANT.** It is the ROADMAP data-row count. **A row-adding phase MUST bump it in the same commit that adds the row** — otherwise every later session gets `GATE FAIL: examined 113 data rows, expected 112`. That noise is deliberate: a wrong denominator must be loud, because a silent one is how check (1) failed in the first place. **The phase-80 BRAINSTORM bumped 111 -> 112 when it chartered row 80, rehearsed with controls BEFORE the edit. The phase-80 SPEC, PLAN and IMPL each add NO row and MUST leave `want` at 112.**

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. When you open a new family, add its row-summary slug here.

**SENTINEL RE-RUN MECHANICALLY AT THE PHASE-80 BRAINSTORM CLOSE, BOTH BEFORE AND AFTER the `ROADMAP.md` edit, on branch `phase-80-brainstorm` off master `80cafb7e`. IT DOES NOT FIRE. `stop` was NOT created; `ls stop` ⇒ `No such file or directory`, and it MUST NOT be created.** ⚠️ **Re-run it yourself anyway and record what YOU see** — a prior run's result is not evidence for the current tip. ACTUAL output, recorded rather than predicted:
- **BEFORE the edit: (1) printed NOTHING at `want=111`.** **AFTER: (1) prints `NOT DONE: row 80`** at `want=112` — correct, row 80 is `in-progress`. NCs, all fired: `want=110`/`111` ⇒ `GATE FAIL: examined 112 data rows, expected <want>`; doctoring row 79 back to `in-progress` on a scratch copy ⇒ `NOT DONE: row 79` (the script self-reported `rows doctored: 1`).
- **(2) FIVE — now at `:190 :200 :210 :216 :224`, shifted +1 by this row's insertion (long form at the first four, the Operational-tooling short form at the last) — UNCHANGED in COUNT.** **The twenty-fourth consecutive phase at which check (2) did not go down. STATED, not forecast.** Union **5** vs long-form-only **4** re-measured at this tip.
- **(3) `NEVER OPENED: gRPC` and `NEVER OPENED: WASM`** — unchanged. NCs: an invented slug ⇒ `NEVER OPENED: ZZZ-nonexistent`, while the REGISTERED slug `Observability` correctly printed nothing.
- Input measured at **228 lines / 13** bare `candidates:` hits (against the sentinel's own narrower 5), so an empty result could not read as a zero result.

⚠️ **THE LEAK CHECK FIRED AT THE PHASE-80 BRAINSTORM AND PASSED.** Row 80's new cell carries **ZERO** occurrences of either deferred-candidate phrase and the only `<Family>-family row` slug it prints is **`Observability-family row`**, which is **REGISTERED on 24 other lines**, so it silences nothing. ⚠️ **AND THE NC WENT FURTHER THAN GREPPING:** doctoring the cell to say `gRPC-family row` and then **re-running check (3)** made `gRPC` **stop printing** while `WASM` kept printing — so the leak is proven to **SILENCE CHECK (3) BY MENTION**, not merely to be greppable. **`grep` cannot tell a mention from a use.** **It re-arms at any stage that WRITES A `ROADMAP.md` CELL** — a row-OPENING stage is as dangerous as a row-closing one, because it authors a cell from scratch with no pre-existing text to constrain its phrasing. **The phase-80 SPEC leaves `ROADMAP.md` BYTE-UNTOUCHED, so it is NOT live this session; it re-arms at the IMPL.**
```sh
# the new row's cell only, not the whole file
awk -F'|' '/^\| *80 /' docs/envoy-go/ROADMAP.md > "$SCRATCH/row80.txt"
grep -coE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' "$SCRATCH/row80.txt"  # want 0
grep -oiE '(HTTP-filters|Network-filters|Load-balancing|Upstream-robustness|Observability|Operational-tooling|HTTP/3|gRPC|xDS|Runtime|WASM)-family row' "$SCRATCH/row80.txt" | sort -u
# ^ every slug printed MUST already be registered elsewhere in ROADMAP.md. NC both greps on a doctored copy,
#   AND re-run check (3) on the doctored file to prove the slug would actually silence it.
```

## ⚠️ A NEW EVIDENCE HAZARD, FOUND THIS SESSION — READ BEFORE ANY REPO-WIDE AUDIT

**A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`.** `grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files --hidden -I --exclude-dir=.git …` in ugrep-compatible mode. **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Measured:
Measured at the phase-80 BRAINSTORM against the then-live needle `want=111`: recursive wrapped `grep` returned **8 hits / 6 files with `next-prompt.txt` ABSENT**, while `command grep -rn` returned **11 / 7 with it PRESENT**; a direct `grep -c` on the file itself returned **3**. ⚠️ **That needle is now stale by this very commit** (the bump moved it to `want=112`), so reproduce the hazard with this stable needle instead — **EXECUTED at the phase-80 BRAINSTORM close: the wrapped form lists 9 files, the GNU form lists 10, and the difference is EXACTLY `next-prompt.txt`:**
```sh
# The needle lives in next-prompt.txt AND in 9 phase BRAINSTORMs -- that is deliberate:
# both greps return non-empty, so only the NAME LIST discriminates, never emptiness.
grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' .          # wrapped ugrep: next-prompt.txt ABSENT
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' .  # GNU grep:      next-prompt.txt PRESENT
grep -c  'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt   # direct access WORKS
```
⚠️ **Print the FILE NAMES, never a count.** A count-only form is how this hid in the first place: a probe reporting `grep -rl <needle> . | wc -l` ⇒ `1` was read as "the file is visible", when the single match was a DIFFERENT file entirely (`phases/61-http3-downstream-listener/PLAN-61.3.md`) and `next-prompt.txt` was being skipped throughout. **`reference_output_volume_is_not_output_content`, committed inside the probe built to test for exactly this blindness.**
⚠️ **THIS IS LIVE FOR EVERY "AUDIT WHERE THE NUMBER LIVES" SWEEP** — the sentinel's `want` figure and this whole router live in exactly the file directory-recursion drops. A stage that sweeps recursively and repairs what it finds will **silently leave this file stale.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** — it reports `next-prompt.txt` as *"not ignored"* (exit 1) because **git exempts TRACKED files from `.gitignore`** while ugrep does not. **Two tools disagree for a principled reason and the obvious one is the wrong one.** **Reliable forms: `command grep -r`, `git grep`, an explicit file list, or a recursive grep handed the FILE path directly.** Found INDEPENDENTLY by the controller and by an agent, from opposite directions. `reference_next_prompt_tracked_despite_gitignore` records that the file is tracked; it does NOT record this blindness — that is the new half.

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`.** Every document from phase 75 forward recorded its blind spot — **109 data rows / 105 matched / FOUR misses** at an **em-dash** in an "after" cell, a **DOT** in the slug `http-1.1`, and two **LETTER-suffixed** ids `28.1a`/`28.1b` — and every one of them called it *"currently HARMLESS, since all four missed rows are `done`."*

⚠️ **Nobody fired the unsafe arm until the phase-77 PLAN. EXECUTED on scratch copies: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** A false "done" ends the project early. ⚠️ **DO NOT restore the regex form.**

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

The old check (2) matched ONLY the long phrase and could not see the **Operational-tooling** family's three genuinely un-chartered items, recorded in the **SHORT** form. Fixed at the phase-77 BRAINSTORM, at the MATCHER; negative controls all EXECUTED. ⚠️ **Run the union form above, never one half of it** — an agent running only the long-phrase regex sees FOUR lines, not five. ⚠️ **A ONE-ARM STRIP IS NOT AN NC FOR CHECK (2)**: `sed 's/deferred candidates://'` moves the union count **5 -> 4, not 5 -> 0**, because the long form does not contain the short substring at all.

⚠️ **Normalising that Operational-tooling paragraph to the long form is a NAMED, DELIBERATELY-UNTAKEN follow-up.**

⚠️ **THE DOCUMENTED TRAP'S STATED MECHANISM IS REFUTED, WHILE THE TRAP IS REAL.** `reference_sentinel_deferred_sentence_live_vs_historical` says a naive `grep -c 'candidates:'` inflates *because of* the `candidates were:` recaps. It does inflate — but **not for that reason**: `candidates were:` does not contain that substring **at all**. The inflation comes from **ROADMAP table rows carrying BRAINSTORM prose**. **Cite the command; do not re-inherit the explanation.**

⚠️ **AND THE MEASURED RECORD: CHECK (2) HAS NEVER GONE DOWN** across ~24 phases (history `0 -> 1 -> 3 -> 4 -> 5`). **Phase 74 landed three `ssl.*` stat names and made its OWN sentence LONGER**; **phase 73 predicted a clause REMOVAL and got a clause REPLACEMENT**; **phase 77 opened a family and narrowed nothing**; **phases 78 and 79 narrowed nothing at ANY of their stages**; **and phase 80's BRAINSTORM narrowed nothing either — its subject is the residue of a CLOSED row's departure and is drawn from no candidate paragraph at all.** => **the candidates sentence is a WINDOW onto a larger deferred backlog, not an inventory of it.** ⚠️ **A stage that predicts a decrease here is repeating the phase-73 error. State what your row narrows — usually nothing — rather than forecasting.**

⚠️ **THE MENU IS NOT COSTED, and calling it "the self-pick menu" overstates it — MEASURED.** `grep -obE '[0-9]+(-[0-9]+)? tasks'` returns **no match** on FOUR of the five paragraphs: HTTP/3 (7 uncosted candidates), xDS (9), Runtime (4) and Operational-tooling (3) carry **zero** task bands. **Only the Observability line carries costs** — six bands tied to ADR-0276/0277/0283/0284/0285 plus the `11-14` projection parent.

⚠️ **NEVER WRITE A SENTINEL'S OWN MATCHER STRING INTO A FILE THE SENTINEL GREPS.** The sentinel greps **`docs/envoy-go/ROADMAP.md`** — writing these strings *here* is safe; writing them *there* is not. **NOT live this session (the SPEC leaves `ROADMAP.md` byte-untouched).**

⚠️ **THE SELECTION CALCULUS, CORRECTED AND STILL STANDING.** *"A candidate that clears a check-(3) blocker is worth **strictly more**"* does **not** hold: opening a family **MINTS** a check-(2) sentence, so phase 77's own arithmetic was **NET ZERO**. What survives: **opening a family is the ONLY way that family's work becomes enumerable at all — a prerequisite, not a shortcut.** Do not restore the stronger claim.

==============================================


## STATUS AS OF 2026-07-29: PHASE 80 IS **OPEN**. ROW 80 IS `in-progress`. THIS SESSION PERFORMS THE **PHASE-80 SPEC**.

⚠️ **THE HANDOFF PRECONDITIONS WERE VERIFIED MECHANICALLY, NOT ASSERTED, at the phase-80 BRAINSTORM close:** `docs/envoy-go/phases/80-stats-sds-projection/` exists and contains **`BRAINSTORM.md` ONLY** (no half-written SPEC) · the highest `ROADMAP.md` row id is **80** and its status field is **`in-progress`** · `ROADMAP.md` is **228 lines / 112 awk-parsed data rows** · `STATE.md` §Current's seven fields are each **singleton** and §Recent lineage is **5 — AT THE CAP** · `DECISIONS.md` and `BEHAVIOR_CONTRACT.md` are **BYTE-UNTOUCHED** by the BRAINSTORM.

**What the phase-80 BRAINSTORM landed.** Docs-only, **FIVE files**: `phases/80-stats-sds-projection/BRAINSTORM.md` (NEW) · `ROADMAP.md` (row 80 registered) · `STATE.md` (§Current rolled IN PLACE) · **`STATE_HISTORY.md` 418 -> 422** (the mandatory eviction) · this file. **ZERO production `.go`, ZERO test `.go`.**

### What matters for the NEXT session, in order

1. **⚠️ RE-RUN ALL THREE SENTINEL CHECKS YOURSELF AND RECORD THE ACTUAL OUTPUT; DO NOT PREDICT IT.** Create `stop` if and ONLY if all three print nothing. **Check (1) is now RE-OPENED** by row 80, so it prints `NOT DONE: row 80` and the sentinel cannot fire. **`want` STAYS 112** — the SPEC adds no row.

2. **⚠️ THE SUBJECT IS SETTLED; DO NOT RE-PICK IT.** Row 80 = the `sds.` Prometheus-projection arm **plus** the registration-time silent-skip leg. Read `BRAINSTORM.md` §1 for the pick and §8 for the costed rejected alternatives. ⚠️ **The row ID adjudication (80, not `79.1`) is recorded in §1.1 with three independent lines of evidence — do NOT relitigate it, and do NOT "fix" the id to 79.1.**

3. **⚠️ WHAT THE SPEC MUST ADJUDICATE — the design fork, where REFERENCE FIDELITY and apparent correctness DISAGREE.** Secret names may contain **dots** (`IsValidName` accepts them) and a dotted name is **reachable from config, executed end-to-end**. Three options, in `BRAINSTORM.md` §6: **(a) first-dot split — what the LIVE REFERENCE does**, measured: secret `alpha.beta.gamma` renders `envoy_sds_beta_gamma_update_success{envoy_xds_resource_name="alpha"}`; **(b) last-dot split** — well-defined because all five suffixes are fixed and dot-free, but **DIVERGENT**; **(c) a registration-time dot-free guard** — which silently drops names that boot fine today, i.e. the very defect the row repairs. ⚠️ **One agent reasoned (b) was correct WITHOUT probing the reference; the reference probe INVERTS that.** Under differential-conformance discipline **(a) is presumptive** — state it as a fidelity decision, not a correctness one.

4. **⚠️ THE THINGS THE BRAINSTORM PROVED THAT THE SPEC MUST NOT RE-ARGUE.** All executed, all with discriminating controls: the drop is **20/20 against an 8/8 control**; end-to-end it is **PRESENT on flat `/stats`, ABSENT on `/stats/prometheus`**; the label key is **`envoy_xds_resource_name`** (a plausible-but-wrong key greps **0** against **66**) confirmed against the pinned `envoyproxy/envoy:contrib-v1.37.2`, **discharging the contract's own "owed a re-pin"**; the silent-skip trigger is an **idiomatic HYPHEN** (31 rejected printable bytes; `server-cert`, `my-tls-cert`, SPIFFE URIs all skip all five counters with **ZERO bytes of stderr**); `incNil` is **complete but load-bearing** (deleting the guard SIGSEGVs on the production boot path); and `RegisterSDSStats` has **1** non-test call site.

5. **⚠️ THE DENOMINATOR TRAP — 20 PER CORPUS, 5 PER PROCESS.** `boot.NewSDSProvider`'s `seen > 1` guard admits ONE secret per boot, so **any fixture or end-to-end assertion must expect 5, not 20.** The four corpus secrets are `server_cert` (0103), `validation_ca` (0108, 0109), `rccf_validation_ca` (**0110**), `edf_validation_ca` (**0111**). ⚠️ **`0024`'s `client_secret`/`hmac` are `path_config_source` and are EXCLUDED — including them gives 6x5=30 and would coincidentally "confirm" the banked 30 for the wrong reason.** ⚠️ **AND `ROADMAP.md:210` ITSELF IS WRONG HERE** — it says *"THIRTY names across SIX families"* then enumerates **four** families summing to **26**. **The correct decomposition is the contract's at `BEHAVIOR_CONTRACT.md:5078`: 20 + 4 + 4 + 2 = 30, of which phase 79 closed 10. Do not inherit "20 of 30" from `:210`.**

6. **⚠️ THE FALSE DRIFT-CORRECTION IS FIXED IN THE ROLLING DOCUMENTS AND NAMED IN THE HISTORICAL ONES.** The claim that the phase-79 SPEC swapped the secret-to-fixture attribution is **ITSELF WRONG**: **`0110` is `rccf_validation_ca`, `0111` is `edf_validation_ca`**, evidenced by **executable Go** in both drivers with **zero** cross-occurrences, corroborated by the mnemonics and by `phases/68-.../PROGRESS.md:18`. **`79/SPEC.md:114` was RIGHT.** It had propagated into `PLAN.md:174`, `PLAN.md:585`, `PROGRESS.md:111`, `STATE.md:18` and the prior router. `PLAN.md`/`PROGRESS.md` are **historical record — named, not rewritten.**

7. **⚠️ GUARDS MUST DERIVE BOTH SIDES FROM THE CODE. This is the method finding phases 79 and 80 both paid for.** A hand-written golden **shares the author's mistake** — phase 79's terminal error string was stale ON ARRIVAL and a byte-stability guard pinned the wrong text exactly, with every gate green. The shipped model is `internal/stats/segmentcount_test.go`, which walks `ExtractTags`' **AST**. ⚠️ **THIS ROW ADDS A THIRTEENTH TOP-LEVEL DETECTOR**, so the claimed count moves **12 -> 13** and the terminal error string must move **IN THE SAME TASK** — otherwise the row reproduces phase 79's defect one generation on. Both AST guards are live and were NC-verified this session (`12->13` on the constant fires `terminal message claims 13 top-level segments; ExtractTags has 12`). ⚠️ **PREFER SYMBOL ANCHORS TO LINE CITES** — every by-line cite written during phase 79 went stale *inside* phase 79.

8. **⚠️ THE SINK GOLDENS WILL MOVE, AND ONE GATE CELL CANNOT FAIL.** This arm is **NOT sink-neutral** — proven by live UDP capture (`envoygo.sds.server_cert.update_success:7|c` today) and by phase 79's hoisting NC moving all four goldens at two injection sites. **`ExtractTags` has FIVE production consumers**: `internal/stats/prom.go` plus `internal/statssink/{label,graphite,dogstatsd,otlp}.go`; only `WriteProm` DROPS on error, the four sinks degrade gracefully. ⚠️ **Broken-gate shape FIFTEEN is live here**: OTLP's `(useTagExtractedName=F, emitTagsAsAttributes=F)` cell is **byte-identical** under a hoisting arm, so a golden on the default knobs **passes on the wrong tree**, while `(T,F)` **SHRINKS**. **Run the FULL cross-product** (`reference_probe_must_discriminate`).

9. **⚠️ TWO DESIGNS THE SPEC MUST EXCLUDE, both DEADLOCKING** (`reference_registry_walk_lock_inversion`, reproduced live at a 3 s bound): (a) instrumenting the skip with a **counter registered from inside the projection path** — `Walk` holds `RLock` across the iteration while `getOrRegister` takes `Lock`, which is why phase 79's D-SPP-3 resolved to a LOG; (b) **lazy registration at first increment**, since `NewCounterIfAbsent` is permitted post-`Freeze`. Neither is reachable today — all three non-test `Walk` sites have pure accumulator closures. **Keep it that way.**

10. **⚠️ A LANDED ADR CONSEQUENCE IS OVER-ATTRIBUTED — do not cite it as support.** `ADR-0300 §Consequences (iii)` credits phase 78 with making this candidate's boot panic visible. **Probed at FIVE varied insertion points per §Consequences (ii)** — whose verbatim requirement is *"A future row applying that method must vary the INSERTION POINT across the pre-anchor and post-anchor windows: a single pre-anchor injection reports 'caught' and conceals the case that is genuinely uncovered"* — **every `defer` in `main()` (lines 186, 295, 328, 331, 336, 384) is strictly AFTER the registration call at :156**, so a validation panic unwinds an **EMPTY defer chain** and prints for the plainest possible reason. Arms A-D exit **2** in ~10 ms with the panic printed; only arm E (phase-78's `cancel()` deleted) wedges 12 s silently. ⚠️ **Hang-vs-exit was discriminated by OUTPUT, never by status** — arm E printed 1269 B of statsd flush noise with no panic, while the healthy control exited at 12.013 s with 68 B of ready sentinels.

11. **BROKEN-GATE COUNT IS SIXTEEN.** Shape **sixteen**, new this session: **two defects that CANCEL in the metric the gate measures.** `ROADMAP.md` row 78 carries one unescaped inner `|` AND no trailing `|`; in an `awk -F'|'` field count these cancel to `NF=8`, byte-identical to a well-formed row — **so a gate asserting `NF==8` PASSES on the malformed row.** No single predicate finds every bad row: escape-blind `NF!=8` gives 17 rows (**16 false positives** from escaped `\|`) and misses 78; escape-aware `NF!=8` finds 57 and 69 and misses 78; escape-aware trailing-piece finds 78 alone. **Use the disjunction.** The other fifteen are unchanged: an inert gate cell · a full-suite recipe without `-v` is VACUOUS · a sha256 roster desynced against a DELETED file · `gofmt -l` NEVER exits non-zero (gate on OUTPUT) · `go doc -all <A> <B>` swallows arg2 · a `+0 exported symbols` gate over an EMPTY package reds on a CORRECT tree · a RANGE gate cannot detect anchor drift · a roster's naive `[ -f ] || continue` exits 0 on a DELETED file · a count-only stat guard PASSES a build with BOTH names wrong · a `-run` no-match exits 0 with `[no tests to run]` · a `--- PASS` tally over a package with sibling tests exceeds the fixture denominator · a stat-delta claim cannot be discharged by guards scoped to another package · a stderr-VOLUME assertion passes on the hang · `golangci-lint` runs `misspell` with `locale: US` · a harness's exit code is not the command's.

12. **⚠️ THE FULL 120-FIXTURE DIFFERENTIAL IS MANDATORY for any row touching `internal/stats`** — it links into `cmd/envoy-go` at `test/differential/harness.go:240`, `:594` and `test/conformance/h2spec/h2spec_test.go:210`; **h2spec is a FOURTH consumer NOT covered by `./test/differential/`**. Budget ~400-420 s per green attempt; `-race` is a SECOND run. **The SPEC is docs-only and owes none of this**; the PLAN specifies it and the IMPL runs it. The recipe, with `comm -3` as the load-bearing gate (120 with one fixture renamed and another skipped still reads 120):
    ```sh
    ( go test ./test/differential/ -count=1 -v > "$SCRATCH/full.log" 2>&1; echo "INNER_EXIT=$?" )
    grep -cE '^    --- PASS: TestDifferential/' "$SCRATCH/full.log"          # want 120
    grep -E  '^    --- (FAIL|SKIP): TestDifferential/' "$SCRATCH/full.log"   # want EMPTY
    grep -c  'no driver registered for fixture' "$SCRATCH/full.log"          # want 0
    grep -o  'TestDifferential/[^ ]*' "$SCRATCH/full.log" | sed 's|TestDifferential/||' | sort -u \
      | comm -3 - <(ls -1 test/fixtures/ | grep -E '^[0-9]{4}[a-z]?-' | sort)  # want EMPTY
    ```
    ⚠️ The faithful dir predicate is `^[0-9]{4}[a-z]?-` — a bare `^[0-9]{4}-` gives **118**.

13. **⚠️ A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX — the SIXTEENTH consecutive stage at which this held.** The index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Live: the full-suite startup flake (`subject ready: EOF` **and** `bind: address already in use`, both failing **BEFORE any assertion** — it FIRED at the phase-79 IMPL on `0084-otlp-access-log`) · **`reference_sds_init_fetch_timeout_dial_budget_flake` (TWO packages — LIVE for this row's subject)** · the pre-existing `internal/cluster` `-race` outlier · `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery` · `internal/httpclient TestOptions_ZeroValue_NoOpDefaults`. **Isolate-re-run, then state the classification AND its evidence.** ⚠️ **`0061-lb-ring-hash` is NOT a live flake — a spread failure there is a FINDING.** No flake fired at the BRAINSTORM: two independent SDS boots were clean first-run, evidenced by `update_success=1` with `init_fetch_timeout=0`.

14. **Counts at this tip (re-run MECHANICALLY in your worktree; never copy them).** ⚠️ **EVERY FIGURE BELOW WAS RE-EXECUTED WITH GNU `command grep` AT THE PHASE-80 BRAINSTORM AND EVERY ONE HELD** (see the ugrep hazard above — the wrapped `grep` is unreliable for repo-wide rosters):
    fixtures **120** (next-free **0119**; faithful predicate `^[0-9]{4}[a-z]?-`, a bare `^[0-9]{4}-` gives **118**) / fuzzers **55** (⚠️ the `internal/`-vs-repo-wide pair is NOT discriminating — both 55; NC by appending a `func Fuzz` to a scratch COPY) / internal packages **73** (two methods agree) / blank imports in `runner_test.go` **120**, ⚠️ **anchor on the FULL `^\t_ "github.com/pgdad/envoy-go/test/fixtures/` prefix — a naive `^\t_ ` gives 126**; the import roster is **set-complete** against the 120 dirs / **stat surface 1207**, ⚠️ **DOCUMENTARY and CONFIG-CONDITIONAL — assert the DELTA; there is NO mechanical counting command** / **BackendKind tail 38** (⚠️ a TAIL VALUE — **39** declarations, values 0-38, **explicit `X BackendKind = N` with NO `iota` anywhere**; an iota-anchored grep returns garbage; do NOT "fix" 38 to 39) / go.mod modules **2** (the phase-61.2 lineage figure — the single `go.mod` requires **67**; do NOT "fix" 2 to 67) / `DECISIONS.md` **17658**, tail **ADR-0301 COMPLETE**, next-free **ADR-0302** (⚠️ **300 ADR headings but `ADR-0209` DOES NOT EXIST** — a live sequence gap; *300 ADRs* and *tail ADR-0301* are both true and must not be conflated) / `ROADMAP.md` **228 lines / 112 data rows** / `BEHAVIOR_CONTRACT.md` **5822** / `STATE_HISTORY.md` **422** / next-free reference port **10119** (⚠️ **CONFIRMED UNOCCUPIED — `0` hits in `.go`/`.yaml` — but the "family-banded" convention holds for only 40 of 118 numeric fixtures, so assert "unoccupied", not "banded"**) / **TWELVE** non-test top-level segment detectors in `ExtractTags` plus **FOUR** mid-name detectors, **code and error string AGREE** and both AST guards NC-verified.

15. **⚠️ ADR + DOCUMENT CONVENTIONS.** Next-free is **ADR-0302** (`grep -c '^## ADR-0302'` ⇒ 0; NC: `^## ADR-0301` ⇒ 1). A new ADR's §Context is drafted at the SPEC and §Decision + §Consequences append IN PLACE at the IMPL (**ADR-0044-as-used**; ⚠️ ADR-0044 does **not** itself contain that discipline), **no renumber, no `---` separator**. ⚠️ **WRITE THE RETAINED ITALIC FOOTER** `*(§Decision + §Consequences land at the phase-80 IMPL.)*` — ADR-0301 omitted it and forced the phase-79 IMPL into a recorded departure; do not repeat that. ⚠️ **CARRY NO WHOLE-FILE GREP COUNT** — that species self-falsified in ADR-0296 ¶3 and twice in ADR-0297. ⚠️ **`^## ADR-` is a safe anchor** (refuted hazard). ⚠️ **CITE THE SIX-GATE CORRECTLY: `BOOTSTRAP_PROMPT.md` §7.5, heading `:357`, gates (a)-(f) `:360-365`, closing `:367`**, at the REPO ROOT with the `.md` extension — **all four cites verified EXACT this session. `ADR-0106` does NOT define the six-gate** (it is a family-shape ADR). ⚠️ **`ADR-0045` QUOTES the split gate rather than STATING it — citing the numerals to ADR-0045 alone is a LAUNDERED cite. The NORMATIVE statement is `BOOTSTRAP_PROMPT.md` §6.1, heading `:285`, body `:287-291`**, which the canonical `25 tasks`/`1500 LoC` grep **MISSES** (§6.1 spells them *"~25 numbered tasks"* / *"~1500 lines of code"*), **and it carries THREE triggers** — the third being **mid-execution** when any task's sub-steps exceed ~10 items, which **FIRED at phase 79's T7**.

16. **⚠️ COST CALIBRATION, re-derived from primary documents this session.** SPEC band -> PLAN count: **76: ~7-9 -> 9 (AT ceiling)** · **77: 11-13 -> 12 (INSIDE)** · **78: 7-9 -> 10 (ABOVE ceiling)** · **79: 10-12 budget 12 -> 12 (AT ceiling)**. **Three of four at or above the ceiling; none below the floor.** ⚠️ **`ROADMAP.md:142` misreports this twice** — it claims *"three-for-three at or above"* (it is **two-for-three**) and attributes row 79 a *"SPEC band 9-11, budget 11"* when the SPEC §9 heading **overrode that to 10-12/12**. **LoC multiplier: ×1.75 central, ×2.05 ceiling** from realized-vs-estimate ratios **77: 2.04× · 78: 1.50× · 79: 1.75×** (phase 76 states no LoC estimate, so no 76 ratio exists). ⚠️ **Both 77 and 79 landed NET just under the 1500 gate (1406, 1446)** ⇒ **a row estimated at ≥ ~750 LoC trips §6.1's LoC trigger once the multiplier is applied.** The BRAINSTORM's band is **9-12, budget 11**; the SPEC re-derives independently.

17. **⚠️ DOCUMENTARY DEFECTS — RE-MEASURED THIS SESSION, WITH FIVE BANKED FIGURES REFUTED. Still deliberately NOT fixed.**
    - **The `STATE_HISTORY.md` archive gap: the banked 40 is REFUTED — it is 36 bullets for phases 67-75 (9 phases × 4 stages exactly) and 58 overall**, against a stated denominator of **164 present / 205 in git**. Phases **67-75 wholly absent**. ⚠️ **NEW LEAK: `phase 77 PLAN done` is in NEITHER file**, so the ADR-0288 cap already leaks at the most recent phase but one. **4-6 tasks, and defensible ONLY if it lands the `comm -13` set-difference as a recurrence guard.** This session honored the rule for its own eviction (`STATE_HISTORY.md` 418 -> 422).
    - **`ROADMAP.md` malformation: the banked *"ONLY MALFORMED ROW"* is TRUE under a trailing-pipe predicate (1 of 112) and FALSE under the render predicate.** Rows **57, 69 and 78** all carry unescaped `|` and all three **lose summary content in GFM render — EXECUTED with two renderers against well-formed controls** (689 / 4032 / 2877 source chars lost; controls 74 and 76 render INTACT). The banked claim's own justification appeals to the render consequence, which **had never been rendered**. ⚠️ `ROADMAP.md:142` also says *"of 110"* where there are **112** rows. **All INVARIANT-BLOCKED by §Schema `:18`** (only `status` and `sub-phases` may be edited in place) ⇒ **defensible as a GUARD, not a fix.**
    - **The PUBLIC IMPORT PATH defect: banked 36/SEVEN files is STALE — it is 33/SEVEN at this tip** (36 reproduces exactly at the two older tips; the drift is this file being rewritten). ⚠️ **AND IT IS NOT DEFENSIBLE AS A PHASE — there is no green:** `DECISIONS.md:142` is an immutable ADR that must *name* the wrong path so the count can never reach 0; **7 of the 32 sit inside CLOSED rows' summary cells** which §Schema `:18` forbids editing; the 8 root `PROGRESS.md` hits are **pasted `go test` output**; and any `count==0` guard is unsatisfiable by construction. `go build ./...` exits 0 under the real path `github.com/pgdad/envoy-go`; **0** occurrences in compiled Go.
    - **A mechanical stat-surface recount: every banked figure REPRODUCES** (**210** registration sites, **27**+1 ledger rows, **5** `statroster.New` fan-outs, zookeeper **201** by execution, the **+3** unattributed ledger gap) ⚠️ **except "2 method-value registrations" — it is 4 lines / 6 arguments in 2 files**, which STRENGTHENS the case that grep cannot do this. **8-11 tasks, and defensible only if reframed as an EXECUTABLE enumerator** (dump `Registry` keys after a boot); a grep-based recount covers ~0.8 % of the surface and stays rejected.
    - **`ADR-0299`'s STATUS still reads `PROPOSED` — and it is now confirmed the ONLY one** (13 `COMPLETE`, 1 `PROPOSED`) although its §Decision and §Consequences demonstrably landed at the phase-77 IMPL squash. **The single word is the entire defect. 1 task, DEFENSIBLE AS A RIDER on this row's IMPL**, which already edits `DECISIONS.md` — with a guard of shape *"a `PROPOSED` STATUS must have no §Decision."* Not defensible standalone.
    - **`STATE.md` §Project counts self-contradicts §Current** inside the same file (`:31` 119 vs **120**, `:33` 1205 vs **1207**, `:35` ADR-0298 vs **ADR-0301**). **Anchor on §Current, which IS live.**
    - **Symmetric bind hardening: the banked narrowing is REFUTED IN THE UNSAFE DIRECTION.** *"Only `fixture.HTTPSH2` can close-then-rebind race"* is wrong — **26 of 29** backend-startup arms call the close-then-rebind `freeTCPPort`; only the in-process `TCPEcho`/`HTTPEcho` arm, which HOLDS its listener, is immune. The subject has a retry loop; the backends have none. `mustAllocatePort()` in **10** drivers under **4** helper names in fixtures (**6** repo-wide). **8-12 tasks, NOT the banked 4-6, and still rejected: it CANNOT be verified by a green suite run** (a prior instance needed `-count=6`).
    - **Opening the gRPC family — HARD-BLOCKED, now evidence-backed:** `\.RunEncodeTrailers(` ⇒ **0** non-test / 1 test; `\.RunDecodeTrailers(` ⇒ **0** non-test / 1 test; both declared (`chain.go:455`, `:622`), neither production-reachable. NC: `\.RunEncodeHeaders(` non-test ⇒ **4**. The **16-22+** band is **NOT re-derivable** (no in-tree figure) — flagged, not inherited.
    - **The WASM row-summary rider stays DECLINED ON THE MERITS** — `ROADMAP.md:76` (row 25.3) says **verbatim** *"phase 25 is the FINAL §9 HTTP-filters-family row"* (cite EXACT, 4 files carry the sentence), so registering WASM is a doctrine adjudication against a landed closure statement, **and writing the marker would silence check (3) BY MENTION** — proven by NC this session. **Do not re-adopt it as cheap.**
    - **Normalizing the Operational-tooling short-form deferred-candidate paragraph** to the long form.
    - **UNRESOLVED:** the *"plus two more in the same file"* half of the `BEHAVIOR_CONTRACT.md` stale-cite claim. ⚠️ **`BEHAVIOR_CONTRACT.md` line cites in every pre-79 document are stale by exactly +54 after line ~862** — banked `:862`/`:5012` are now `:916`/`:5066`. **Fix by PATTERN, not by line** (`reference_stale_cite_recurs_fix_by_pattern`).
    - `BEHAVIOR_CONTRACT.md:501` calls the `wasm.` arm a *"NEW SN9 flattening rule"*, which **COLLIDES with ADR-0118's actual SN9**.
    - ⚠️ **THREE `79.1` FORWARD-REFERENCES ARE NOW STALE BY DESIGN.** `BEHAVIOR_CONTRACT.md:158`, `:5078`, `:5080` are **live normative statements** (*"Until 79.1 lands, treat `sds.*` as flat-`/stats`-only"*) and are **MANDATORY edits at this row's IMPL**. `ROADMAP.md` row 79's three occurrences are **INVARIANT-BLOCKED** and are NAMED as a permanent discrepancy. The phase-79 phase documents' 25 occurrences are **historical record and must NOT be rewritten.**

## What THIS session does — the phase-80 SPEC

One stage per session. Read `BRAINSTORM.md` in full, then write `SPEC.md` in `docs/envoy-go/phases/80-stats-sds-projection/`. **Draft `ADR-0302` §Context (WITH the retained italic footer).** Roll `STATE.md` §Current **IN PLACE** (ADR-0288 — ⚠️ **§Recent lineage is AT its five-entry cap; a close that adds a bullet MUST evict the oldest to `STATE_HISTORY.md`, and the oldest is now `phase 78 (boot-panic-visibility) IMPL done` — verify it is absent from the archive BEFORE moving it**) and this file. **`ROADMAP.md` stays BYTE-UNTOUCHED and `want` stays 112** (the SPEC adds no row). Expected file set: **`SPEC.md` (NEW) + `DECISIONS.md` + `STATE.md` + `STATE_HISTORY.md` + this file** — five files, docs-only, ZERO `.go`.

⚠️ **A SPEC's job is to REFUTE the BRAINSTORM by EXECUTION, not to formalize it.** The lineage record is that **every stage finds a load-bearing error in its predecessor, and every one is found by execution rather than review** — phase 79's SPEC refuted nine BRAINSTORM claims, its PLAN refuted eleven SPEC claims, its IMPL refuted four PLAN claims and found the row's own fix stale on arrival; and **this BRAINSTORM refuted five banked figures plus a landed ADR consequence, and caught four of its own probes.** **Budget for finding one more.** Start with the three claims most likely to be wrong: the **9-12 band**, the **first-dot fidelity call** (probe a THIRD distinct secret name and a non-ASCII one — the reference probe explicitly did neither), and the **"5 per process"** figure (derived from a `seen > 1` guard READ, not from a two-secret boot attempt).

Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** (`reference_parallel_subagents_private_scratch`), **BANDED PORTS outside the differential harness's 20000-31007 range**, and they **must tear down docker containers BY NAME, never by an `ancestor`/image filter** (`reference_parallel_agents_shared_machine_namespaces`). ⚠️ **If two agents share ONE worktree they must stage by EXPLICIT PATH.** ⚠️ **AND THEY MUST NOT RUN AN UNSCOPED `git restore`.** Per-task `gofmt` + `golangci-lint` on touched packages (`feedback_pertask_gofmt_lint`) — **not owed by a docs-only SPEC.** ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch.**

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. ⚠️ **ASSUME IT FIRES EVERY SESSION — it has been treated as live for TWENTY consecutive sessions and it FIRED THREE TIMES at the phase-80 BRAINSTORM**, once corrupting an agent's log file and dropping a stray zero-byte file in the main repo root. **Use `git -C <abs-worktree-path>` for every git command, prefer absolute paths over `cd`, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — Current-pointer section (TRUSTWORTHY; ADR-0288 discipline — ⚠️ **anchor greps on `^- **<field>`**; all seven fields verified singleton at the phase-80 BRAINSTORM close). ⚠️ **§Project counts SELF-CONTRADICTS §Current** — do not anchor on it.
2. `docs/envoy-go/phases/80-stats-sds-projection/BRAINSTORM.md` — **in full.** §1.1 carries the row-id adjudication, §3-§5 the executed reproduction, §6 the design fork the SPEC must settle, §7 the findings outside the subject, §8 the costed alternatives, §10 the cost derivation.
3. `docs/envoy-go/phases/79-stats-prometheus-projection/PROGRESS.md` — **both halves.** The `# IMPL record` carries the method finding (a hand-written golden shares the author's mistake; a branch-point roster goes stale mid-row) that this row must inherit.
4. `docs/envoy-go/DECISIONS.md` — **ADR-0301** (the projection row; **the model for what NOT to omit — no retained footer**) · **ADR-0300** (§Consequences (ii) the injected-defer insertion-point rule, (iii) the OVER-ATTRIBUTED claim) · **ADR-0288** (roll-IN-PLACE **and the archive rule**) · **ADR-0264** (metrics_service label mapping).
5. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — **`:5078`** (the narrowed departure and the `sds.` deferral, whose "owed re-pin" this BRAINSTORM DISCHARGED) · `:158` (the byte-mirror-vs-hoisting asymmetry) · `:974` (the five-counter subset as a defensible named subset).
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — especially **`reference_probe_must_discriminate`**, **`reference_probe_input_is_a_claim`**, **`reference_a_drift_correction_is_itself_a_claim`**, **`reference_handwritten_golden_shares_author_mistake`**, **`reference_registry_walk_lock_inversion`**, **`reference_nil_stats_counter_inc_crashes_goroutine`**, **`reference_sds_init_fetch_timeout_dial_budget_flake`**, **`reference_dynamic_stat_name_charset_guard`**, **`reference_stale_cite_recurs_fix_by_pattern`**, **`reference_gate_command_negative_control`**, **`reference_empty_output_is_not_a_zero_result`**, **`reference_output_volume_is_not_output_content`**, **`reference_sentinel_matcher_string_self_clears`**, **`reference_next_prompt_tracked_despite_gitignore`**, **`reference_deferred_candidate_cost_restale`**, `feedback_brief_citations_not_evidence`, `feedback_git_worktrees`, `feedback_execution_style`, and **`reference_bash_cwd_reset_commits_to_main`**.
