START a fresh cold-start session for envoy-go — **the phase-29.3 sub-phase PLAN (`network-filter-mongo-fault-delay-and-access-log`) is DONE + reviewed (plan-document-reviewer APPROVED; 2 iterations) + squash-merged + pushed**. The active phase is **phase 29.3 → PLAN done** (parent row 29 STAYS `in-progress`; sub-row 29.1 `done`; 29.2 `done`; 29.3 `in-progress`). This session opens the **29.3 IMPL** — the FINAL phase-29 sub-phase IMPL — via **`superpowers:subagent-driven-development`** (per SKILL_ROUTING: SPEC.md + PLAN.md exist for 29.3 + no PROGRESS.md → EXECUTE the PLAN; per `feedback_execution_style` a fresh subagent per task + two-stage review). Work in a git worktree (`feedback_git_worktrees`); subagents commit LOCAL-ONLY (`feedback_subagents_no_push`); the controller squash-merges + pushes at stage-close (`feedback_push_to_origin`); each task runs `gofmt -l` + `golangci-lint` on touched packages (`feedback_pertask_gofmt_lint`); the Task-4 seam `-race` break + the Task-11 `0052` R4 break run with `-count=1` (`reference_differential_break_protocol_count1`).

The 29.3 PLAN landed at the **29.3-PLAN squash `e8805e4`**; the docs-only STATE/next-prompt SHA-fill `1ae8f69` is the named live tip (a further docs-only commit may trail; confirm via `git log --oneline -1`). The substantive predecessor on master is the 29.3-SPEC squash `0415153`; before that the 29.2-IMPL squash `620a3d0`; the 29.2-PLAN `0783776`; the 29.2-SPEC `13907f1`; the 29.1-IMPL `5ef5215`; the 29.1-PLAN `89c194a`; the 29.1-SPEC `7574219`. The 29.3 PLAN (`docs/envoy-go/phases/29.3-network-filter-mongo-fault-delay-and-access-log/PLAN.md`, 12 bite-sized TDD tasks) + the 29.3 SPEC + the phase-29 parent SPEC + ADR-0226 §Context (the 29.3 charter) + the as-built `internal/filter/network/` chain framework + `internal/filter/network/mongoproxy/` + `internal/accesslog/` + `internal/filter/tcpproxy/filter.go` + `internal/listener/manager.go` + `internal/drain/manager.go` are the pinned-behavior references. The ADR-0226 §Decision/§Consequences body lands IN-PLACE at IMPL Task 12 (no new ADR number — DECISIONS tail STAYS ADR-0226, next-free ADR-0227).

## What the 29.3 IMPL executes (the PLAN's 12-task spine — SEAM-FIRST)

Execute the PLAN task-by-task, a fresh subagent per task + two-stage review (`superpowers:subagent-driven-development`). The seam (T2–T4) lands GREEN with its `-race` test BEFORE any mongo consumer wires in (the framework is independently testable via a SYNTHETIC `haltingFilter`). The spine:

1. **First-action baselines/anchors gate** — re-pin fixtures **53** (tail `0051`) + fuzzers **39** + stat surface **360** + BackendKind tail **30** + DECISIONS tail **ADR-0226** (next-free **ADR-0227**) + the §9.1 as-built anchors, against the live IMPL-session tip; create PROGRESS.md.
2. **The halt-state synchronization on `chainRuntime`** — the `MayHalt` construction-time `haltable` fast-path gate + halt `sync.Mutex`/`sync.Cond` + `held`/`resumeReady`/`replayIdx`/`replayHeld` + the pre-handoff hold in `runData` + a SYNTHETIC halting test filter + the never-halting byte-identity proof.
3. **`ContinueReading` async-active third context** — the off-dispatch resume releases the holder; the pre-handoff async-resume reaches `TerminalReady`.
4. **`replayRead` + `readChainConn.Read` post-handoff withhold-until-resume** — the `replayHeld` park-state + the `ContinueReading` post-handoff advance + the `-race -count=N` R-HALT seam test + the deliberate-break (`-count=1`).
5. **Fault-delay decision in the decoder** — `maybeInjectDelay` at the AS-BUILT five request decoders (D-S29.3-5 reconciled from the parent six) + `takePendingDelay` + `rollPercent` deterministic at 100% + the `delayPending` re-entrancy guard + stop-after-decide.
6. **Fault-delay timer in the filter** — `MayHalt`; `time.AfterFunc`+`onDelayTimer`+`ContinueReading`; `delays_injected` at ARM; StopIteration-while-pending in `OnData` (pre + post handoff via the chain); `OnDestroy` cancel + the timer↔destroy `-race`.
7. **The `internal/accesslog` pluggable `Formatter func(any)` seam** — default `Default(*Record)` → HCM byte-identical + the `MongoRecord` + `MongoFormat` + the per-opcode `message.toString` goldens (transcribed vs `codec_impl.cc` v1.37.2; differential-invisible).
8. **The mongo access-log sink** — constructed gated on `cfg.accessLog` (factory; shared) + per-message emission both directions + the gated-off no-emit test.
9. **The drain-state accessor + `cx_drain_close`** — `DrainState` interface + `Draining()`/`CloseDirection()` on `ReadFilterCallbacks` + the GUARDED `SetDrainState(rt.dm)` (the typed-nil `*drain.Manager` trap) + `cx_drain_close` on the reply-completion list-empty path + `Connection().Close(FlushWrite)`.
10. **The close-direction seam D-P4 CLOSED** — `tcp_proxy` pump-EOF-first → `chainRuntime.closeDir` + the EXPLICIT `SetCloseDirection` FORWARDING methods on `prefixconn.go`+`writeconn.go` (the embedded `net.Conn` interface does NOT auto-promote — `TestCloseDirectionThroughWrapStack` is MANDATORY) + the `OnDestroy` direction-keyed `cx_destroy_local/remote_with_active_rq` VALUE-parity increment.
11. **Fixture `0052-mongo-fault-delay`** (cross-side; `MultiListenerDriver` delayed `mongo_d` + no-delay `mongo_nd`; the deterministic 100% pre/post-handoff delay arms; the R1 non-perturbation arm; `cx_drain_close` driven-or-PRESENCE per D-S29.3-8; the `cx_destroy_*` local/remote/all-answered VALUE-parity arms; the R4 break) + README.
12. **The completion bundle** — ADR-0226 §Decision/§Consequences body in place + the BEHAVIOR_CONTRACT 29.3 bundle (incl. the framework async-halt/resume subsection + the access-log + runtime-key boundaries) + STATE/ROADMAP sub-row 29.3 `in-progress → done` **AND the parent-row-29 ROLLUP** (parent `in-progress → done` ATOMICALLY) + next-prompt.txt + the six-gate (full 54-dir suite + h2spec 53/53 + proxy-wasm 10/10).

## The PLAN's risk register (READ FIRST — these bit the plan-reviewer)

- **The seam is genuine concurrency (T2–T4)** — TDD it with the synthetic `haltingFilter`, NOT mongo; it lands GREEN incl. `-race` before T5 wires mongo in. The exact halt primitive (mutex+cond vs a release channel) is IMPL-refinable (D-S29.3-1) — the tests are the arbiter; the SEMANTICS (block-the-dispatcher, release exactly once, never-halting byte-identical R1) are fixed.
- **The `SetCloseDirection` non-promoting trap (T10)** — `prefixConn`/`writeChainConn` embed `net.Conn` as an INTERFACE, so a custom method on `readChainConn` is INVISIBLE on the outer conn `tcp_proxy` gets. The forwarding methods on BOTH wraps are MANDATORY; `TestCloseDirectionThroughWrapStack` (through the real composition, not a fake `cb`) is the guard — without it `cx_destroy_*` is silently zero while the fake-`cb` unit test passes.
- **The typed-nil-in-interface trap (T9)** — guard `SetDrainState(rt.dm)` with `if rt.dm != nil` (a typed-nil `*drain.Manager` in the `DrainState` interface is non-nil → would panic in `Draining()`).
- **`cx_drain_close` cross-side reproducibility (D-S29.3-8)** — land the UNIT value proof first (T9, deterministic); the `0052` differential arm is best-effort with a PRESENCE downgrade. Do NOT block the phase on a flaky differential drain arm.
- **The `chan *Record → chan any` accesslog change (T7)** — the `Default` byte path is the regression gate (the existing accesslog + HCM tests stay byte-identical); `*Record` satisfies `any` so HCM call sites are unchanged.
- **`ReadFilterCallbacks` grows two methods (T9)** — every test fake implementing it must gain `Draining()`/`CloseDirection()`; update them in the same task.

## Load-bearing pins the 29.3 IMPL must honor

- **29.3 is the framework-SURGERY sub-phase** (ADR-0219 one ripple): the halt seam (`chain.go`/`readconn.go`/`callbacks.go`/`types.go`) + the close-direction recording (`tcp_proxy`/`prefixconn`/`writeconn`/`chain.go`) + the drain-state threading (`manager.go` one guarded line) + the accesslog formatter seam (`internal/accesslog`). The **full 53-dir suite + h2spec 53/53 + proxy-wasm 10/10 MUST stay byte-exact green** at the six-gate — the seam's R1 back-compat / non-perturbation gate.
- **Never-halting chains BYTE-IDENTICAL (R1)** — gated behind the construction-time `haltable` (false for zookeeper, every existing filter, mongo with no `delay`).
- **The post-handoff halt path is LOAD-BEARING** — steady-state mongo decode runs post-handoff via `replayRead`; `0052` drives ≥2 messages so a delay fires BOTH pre- and post-handoff.
- **`delays_injected` at timer-ARM; deterministic 100%-probability arms; timing NEVER compared** (BOOTSTRAP §7.2). The access log is differential-INVISIBLE (AMEND-B10): unit goldens + a coverage boundary, NO fixture dir.
- **The histogram families stay DEFERRED** (ADR-0060) — the per-command/per-collection `reply_*` families remain unmirrored project-wide.
- **No new BackendKind** (`TCPMongoResponder = 30` reused); **no new fuzzer** (39; a seam `-race` test instead); **+0 stat creation** (the four counters go increment-active).

## What this session must do

1. Re-establish a worktree (`feedback_git_worktrees`); confirm the clean baseline (`go build ./...` + `go test ./internal/... -count=1`).
2. Execute the **29.3 PLAN** task-by-task via `superpowers:subagent-driven-development` (a fresh subagent per task + two-stage review). Land the seam (T2–T4) GREEN first; then the mongo consumers (T5–T11); then the completion bundle (T12).
3. At T12: flip ROADMAP sub-row 29.3 `in-progress → done` **AND the parent-row-29 ROLLUP** (parent `in-progress → done` ATOMICALLY); land the ADR-0226 body in place; the BEHAVIOR_CONTRACT 29.3 bundle; advance STATE/next-prompt for the NEXT-phase cold-start (the §9 family's remaining `redis`/`kafka_broker`/`thrift`, or the next ROADMAP row). Controller: squash-merge + fill the squash SHA into STATE/next-prompt + push.

## Project state at master tip

- **Master tip:** the docs-only SHA-fill `1ae8f69` trailing the substantive 29.3-PLAN squash `e8805e4` (a further docs-only commit may trail; confirm via `git log --oneline -1`). STATE.md `last-commit` points at the substantive PLAN squash. Substantive predecessors: the 29.3-SPEC squash `0415153`; the 29.2-IMPL `620a3d0`; the 29.2-PLAN `0783776`; the 29.2-SPEC `13907f1`; the 29.1-IMPL `5ef5215`; the 29.1-PLAN `89c194a`; the 29.1-SPEC `7574219`. origin/master in sync after push.
- **§9 Network-filters family:** rows 26/27/28 DONE; row 29 `in-progress` (29.1 `done`; 29.2 `done`; 29.3 `in-progress` — SPEC + PLAN done; its IMPL carries the parent-row-29 ROLLUP).
- **Counts at master tip (29.3 PLAN done — docs-only):** stat surface **360** (+0 anticipated at 29.3 IMPL — increment-wiring only); differential fixtures **53** (tail `0051-mongo-responses`; → 54 at 29.3 IMPL with `0052-mongo-fault-delay`); fuzzers **39** (no new fuzzer); BackendKind tail **30** (`TCPMongoResponder` reused); conformance h2spec 53/53 + proxy-wasm 10/10 (as of the 29.2 six-gate); DECISIONS.md tail **ADR-0226** (next-free **ADR-0227**).
- **Toolchain:** Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); reference Envoy v1.37.2 (ADR-0008); go-control-plane v1.32.4 (ADR-0008).
- **Known coverage boundaries (carried, non-blocking):** the mongo D-P1 boot-window eager stat-creation departure; the `stats.IsValidName` guard (`reference_dynamic_stat_name_charset_guard`); the mongo dynamic-HISTOGRAM families deferred (ADR-0060); the dynamic-metadata emission differential-invisible (AMEND-B11); the access-log timing-bearing differential boundary (AMEND-B10 — 29.3 unit goldens + boundary, NO fixture); the runtime-key-gating boundary (fault/drain/logging keys at defaults — 29.3); the close-direction `cx_destroy_*` value parity is 29.3's (D-P4 CLOSED at the SPEC; the increment + value parity land at the 29.3 IMPL); `tcp_proxy` `downstream_cx_*` unmirrored; histograms deferred project-wide (ADR-0060).

## Read first (in order)

1. THIS FILE — the 29.3-IMPL cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 29.3 PLAN done`; next-skill the 29.3 IMPL execution path; next-free `ADR-0227`.
3. `docs/envoy-go/phases/29.3-network-filter-mongo-fault-delay-and-access-log/PLAN.md` — the 12-task spine + the seam-design picture + the D-S29.3-1..8 resolutions + the risk register (READ THE RISK REGISTER).
4. `docs/envoy-go/phases/29.3-network-filter-mongo-fault-delay-and-access-log/SPEC.md` + README — the authoritative 29.3 surface (§3 the seam design; §6 the `0052` arms + access-log goldens; §10 the D-resolutions; §12 the task spine; §13 the test surface + acceptance checklist).
5. `docs/envoy-go/phases/29-network-filter-mongo-proxy/SPEC.md` — the parent master (§4.1 the seam; §11.6 fault-delay; §11.7 continueReading; §11.8 access-log; §11.10 close+drain; §8.4 `0052`).
6. `docs/envoy-go/DECISIONS.md` ADR-0226 §Context (the 29.3 charter; its body lands at the IMPL) + ADR-0225/0224 + ADR-0221 (the WriteFilter seam + the anticipated-halt-consumer allowance 29.3 CONSUMES) + ADR-0213/0215 (the chain framework) + ADR-0223 (the minimal-critical-section synchronization) + ADR-0219 (the one-ripple discipline) + ADR-0044/0052/0060.
7. The as-built surfaces the PLAN's code blocks target: `internal/filter/network/chain.go`/`readconn.go`/`writeconn.go`/`prefixconn.go`/`callbacks.go`/`types.go`/`buffer.go`; `internal/filter/tcpproxy/filter.go`; `internal/listener/manager.go` (`serveNetworkChain`/`rt.dm`); `internal/drain/manager.go` (`IsDraining`); `internal/accesslog/writer.go`/`format.go`; `internal/filter/http/fault/fault.go` (the `rollPercent`/`time.AfterFunc`/`markedActive` precedent); `internal/filter/network/mongoproxy/` (config/codec/filter/stats); `test/fixtures/0049-mongo-requests/` (the `MultiListenerDriver`) + `test/fixtures/0051-mongo-responses/` (the `TCPMongoResponder` + the scrape helpers + the `0052` driver template).
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — execution-style + worktree + push + subagents-no-push + per-task gofmt/lint + the differential dispatch/asserter/break-protocol (`-count=1`) + wire-format-both-sides + the dynamic-stat charset guard + the close-direction framework-gap boundary (which 29.3 CLOSES) all carry forward into the 29.3 IMPL.
