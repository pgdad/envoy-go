=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-VERIFIED 2026-07-23 (re-run MECHANICALLY at the phase-72 SPEC close, in the stage worktree, by the controller) — it does NOT fire. `stop` was NOT created.** Actual output:
- **(1) prints `NOT DONE: row 72`** — row 72 (`tracing-custom-tags-metadata-host`) is `in-progress` and STAYS so through the SPEC and the PLAN; it flips `done` at the phase-72 IMPL six-gate (ADR-0106, the SOLE leg). Every other chartered ROADMAP row is `done`.
- **(2) prints THREE live "candidates:" sentences** — HTTP/3, xDS, Observability ⇒ three families STAY OPEN. **Cite the command, never the adjective** (`reference_sentinel_deferred_sentence_live_vs_historical`): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` ⇒ **3**. (The Observability sentence still names `CLUSTER`/`HOST`; the `HOST` candidate rolls OUT — narrowing it to `CLUSTER`-only — at the phase-72 IMPL row-done edit, NOT at the PLAN. The phase-57 precedent: NO deferred sentence is narrowed at ANY pre-IMPL stage.)
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** ⇒ THREE families never opened. (The `WASM` line is a ROADMAP **bookkeeping artifact** — `internal/wasm/` is 21,550 LOC of wazero host and rows 25/25.1/25.2/25.3 are all `done` — flagged at the phase-72 BRAINSTORM §2.1 for a future correction and deliberately LEFT AS-IS, since the stage-close mechanics depend on this output.)

Checks (2) + (3) print ⇒ the sentinel does NOT fire. Abundant work remains.

==============================================


## STATUS AS OF 2026-07-23: THE PHASE-72 SPEC IS COMPLETE AND LANDED. ROW 72 (`tracing-custom-tags-metadata-host`) STAYS `in-progress`. THIS SESSION PERFORMS THE PHASE-72 PLAN.

**What the phase-72 SPEC landed** (`git log --grep 'phase 72'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): the `D-CTMH-*` docket DISPOSED one-for-one via **SEVEN EXECUTED live-probe arms (P1–P7)** against `envoyproxy/envoy:contrib-v1.37.2` + a full read-only code re-derivation of every BRAINSTORM §6/§11 anchor at tip + **a by-execution scratch build of the row's entire claimed shape**. Docs-only: `SPEC.md` new + the ADR-0294 §Context append (the anticipated design is its closing paragraph, the ADR-0293 shape) — ZERO production `.go`, ROADMAP UNTOUCHED.

- **The subject (unchanged):** lift the `tracing: custom_tags metadata tag %q host kind unsupported` reject (`internal/tracing/config.go:267-268`) → an accept arm cloning the phase-71 ROUTE accept (`:248-264`), resolving the SELECTED UPSTREAM ENDPOINT's metadata (`lb_endpoints[].metadata.filter_metadata[ns]`, walked by `MetadataKey.path`) onto the ingress SERVER span as a `{tag,value}` attribute on BOTH exporters via the shared `ResolveCustomTags` → `Span.Attrs []KV` seam. `CLUSTER` + both unset-kind arms stay reject-loud (envoy-go-strict, ADR-0080 — the reference BOOTS them, P1).
- **THE LOAD-BEARING ANSWER (P2):** **`envoy.lb` is NOT privileged — ANY `filter_metadata` namespace is addressable** by `metadata_key.key` (three namespaces resolved in one config; a control with `envoy.lb` entirely absent still resolved a custom namespace). ⇒ the phase-38 `envoy.lb`-scalars-only projection is genuinely insufficient on BOTH counts (namespace AND nested paths). **S1 DECIDED:** add `filterMetadata map[string]*structpb.Struct` to `cluster.Endpoint`, ALIASING `lbe.GetMetadata().GetFilterMetadata()`, populated BESIDE the byte-unchanged projection, plus a nil-safe `MetaLookup(ns) (*structpb.Value, bool)` accessor. Chosen over retaining the whole `*corev3.Metadata` (ONE new import instead of two; a `MetadataKey` can only address `filter_metadata`). Zero new allocation (it aliases the parsed proto map); `unsafe.Sizeof(Endpoint)` 104 → 112 B, MEASURED.
- **THE ROW'S MOST CONSEQUENTIAL FINDING (P6 × the code) — a NAMED DEPARTURE, deliberately SCOPED OUT:** the reference resolves a HOST tag at **LOAD-BALANCER PICK TIME, not on upstream success** — with a dead endpoint (connect refused → 503 `UF`) it emitted that picked-but-unreachable endpoint's OWN metadata. envoy-go carries the ZERO `Endpoint` on every failure sub-path (`router.go`/`router_h2.go` assign `picked` only after the acquire succeeds), so a HOST tag falls to `default_value`/omit there. **PARITY on the post-Decode local-reply class.** The reference's other two no-host arms have NO envoy-go counterpart and must NOT be written up as parity — a 404 no-match emits **no span at all** in envoy-go (all three no-match sites are PRE-Decide, `traceDecision` nil; a PRE-EXISTING documented departure) and a zero-endpoint cluster is **BOOT-REJECTED** (`manager.go:890`). Established **PRE-EXISTING**: `picked` reaches no span attribute today, its only consumer is the access-log upstream-host field which is ALREADY empty on those paths, pinned by three landed UNIT tests (`router_test.go:333-334`/`:492`/`:597`) and by **NO differential fixture**. Recorded as BEHAVIOR_CONTRACT **B2** and forwarded as its own future row.
- **P7 (a question nobody had asked):** `LocalityLbEndpoints.metadata` — a SECOND, DISTINCT metadata source envoy-go retains none of — **contributes NOTHING** to HOST-kind resolution (locality-only falls to the default; the same key at both levels resolves to the ENDPOINT value; different keys do NOT union). ⇒ **NO departure owed**, and the S1 shape needs no locality merge.
- **P3/P5 — the serializer reuses VERBATIM, but three PRE-EXISTING boundaries are newly QUANTIFIED:** scalar-number precision (`9007199254740992` → `9.0072e+15` on the reference vs full precision in Go), a DIFFERENT renderer for numbers nested in a struct/list, and **an ARBITRARY reference key order for multi-key structs** (Go always sorts) ⇒ **the `0116` fixture asserts STRING values only.** Plus: the reference's **Zipkin** serializer DROPS empty-string tag values (and the empty `node_id`/`zone` built-ins) where OTLP emits `""` — pre-existing, equally affecting the landed REQUEST/ROUTE kinds. Both recorded as **B3**. Fixture-authoring trap: Envoy's YAML loader turns any non-integer scalar into a STRING, so an `envoy.yaml` CANNOT express a fractional metadata number.
- **TWO BRAINSTORM PREMISES FOUND FALSE (S2):** (i) "an `Endpoint`-side accessor localizes the `structpb` import" — **FALSE**, `internal/filter/hcm/accesslog_emit.go` ALREADY imports `structpb` at `:9`, so NO shape adds an import there; (ii) "two cheaper closure shapes" — **REFUTED BY MEASUREMENT**: the method value, the inline closure and the package adapter all cost exactly **1 alloc / 128 B / ~24 ns**, and the pointer-receiver variant is **strictly worse (2 allocs)**. DECISION: the `Endpoint`-side accessor as a method value, on readability + symmetry. Note the closure is MANDATORY, not stylistic — `internal/tracing` must not import `internal/cluster` (the cycle guard).
- **The central cost claim EXECUTION-CONFIRMED:** `go build ./...` EXIT 0 · `go mod tidy -diff` EMPTY · the cycle guard clean · **exactly the two predicted `config_test.go` `/host` failures across 70 packages** · and **sha256 proof that the 18 emit callers, the 29 emit test callers and `internal/filter/http/chain.go` are BYTE-UNTOUCHED**. Exactly SIX files differ. The 18-site roster was re-enumerated and partitioned by SPAN-CAPABILITY (the tracing-relevant axis — partitioning by `picked` alone produced two FALSE parity claims that adversarial review caught): **6 of 18 are PRE-Decide and can never emit a span at all**, leaving **12 span-capable = 7 real-`picked` + 5 zero-endpoint**; and those 7 carry the zero endpoint on every upstream-failure sub-path.
- **D-CTMH-SPLIT DECIDED: a SINGLE FLAT ROW of NINE tasks (T1-T9)** (ADR-0045 valve armable-but-unconsumed) — the load-bearing `internal/cluster` question turned up **ZERO** behavioral disturbance (`Endpoint` is never a map key, never `==`-compared, never serialized; identity is `Addr()`-keyed; ONE non-empty composite literal in production).
- **⚠️ ONE HAZARD CARRIED FORWARD:** `TestOutlierDetector_ConcurrentEjectExactlyOnce` (`internal/cluster/outlier_test.go:766`) is a **PRE-EXISTING intermittent `-race` failure, reproduced on the UNMODIFIED baseline** at `-count=400`. `internal/cluster` is a package this row newly touches, so its `-race` leg becomes load-bearing at the IMPL six-gate. **Isolate-re-run; do NOT re-classify it as a phase-72 regression** unless it reproduces deterministically or the failure text differs.
- **Envelope (at the phase-72 IMPL):** ZERO new packages / +0 stats (**1201**) / +0 fuzzers (**55** — a purely ADDITIVE `meta_host_ok` seed, **NO `meta_bad` repoint**) / +0 BackendKinds (**38**) / +0 go.mod modules (**2**) / ONE new OTLP fixture `0116-tracing-custom-tags-metadata-host` (port 10116 free) ⇒ fixtures **117 → 118** / DECISIONS tail **ADR-0294** (PROPOSED; next-free ADR-0295).

**⚠️ PUSH (idempotent — evaluate, then act):** FIRST ACTION: `git fetch`; if origin/master is BEHIND local master and the local tip is the phase-72 SPEC squash (`git log --grep 'phase 72'`, locate by SUBJECT), `git push origin master` per `feedback_push_to_origin`; if in sync, proceed; if REJECTED (diverged), STOP and reconcile — never rebase away landed squashes.

## What THIS session does — the phase-72 PLAN (RE-DERIVE the SPEC)

One stage per session: the PLAN. **RE-DERIVE the SPEC, do NOT execute it.** A SPEC cite is not evidence either (`feedback_brief_citations_not_evidence`, `reference_quoting_is_not_executing`) — re-open EVERY anchor at the PLAN tip. Note the SPEC itself corrected three BRAINSTORM cites and found TWO BRAINSTORM premises FALSE, and it records that **the phase-71 SPEC's own cites are now STALE** (its dedup `:257-262` is `:275-279`; its `descend`/`structpbValueToString` `:100`/`:123` are `:128`/`:151`) — so **copy anchors from the phase-72 SPEC, never from the 71 SPEC**. Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`), controller squash-push at close (`feedback_push_to_origin`); **docs-only — `PLAN.md` + `PROGRESS.md`, ZERO production `.go`, DECISIONS/ROADMAP UNTOUCHED.** Row 72 STAYS `in-progress`. Then roll THIS file to the phase-72 **IMPL**.

**⚠️ CRITICAL — the PLAN rolls the router to the phase-72 IMPL.** The lifecycle is FOUR stages (BRAINSTORM → SPEC → PLAN → IMPL), one per session.

**The NINE-task (T1-T9) SINGLE-FLAT-ROW spine to decompose (SPEC §10) — each task red-first → green → break-verified → adversarially reviewed:**
- **T1** `internal/cluster`: the `Endpoint.filterMetadata` field + the nil-safe `MetaLookup` accessor + the `manager.go` populate line, with `ScalarsFromStruct` / the `envoy.lb` projection / `defaultSubset` **byte-unchanged** (+ a guard test proving it). ONE new import (`structpb`) in `cluster.go`. **Mind `structpb.NewStructValue(nil)` returns a NON-nil wrapper — the nil guard is load-bearing (the phase-71 finding).**
- **T2** `internal/tracing/config.go`: `kindMetadataHost` **APPENDED after `kindMetadataRoute` (⇒ iota == 5) — never INSERTED before it** (a silent renumber of a landed value would fail nowhere) + the HOST accept arm REPLACING the `:267-268` reject, cloning `:248-264` verbatim except the kind. Branch on **`k.GetHost()` on the bound `k := md.GetKind()`** — `md.GetHost()` does not compile (the phase-70 V1 SEVERE). ZERO new imports. **The pre-known red set is EXACTLY two `config_test.go` `/host` table rows (`:639`/`:765`, asserting at `:648`/`:774`); the sibling `/cluster` + unset rows stay reject and are the live regression guard.**
- **T3** `internal/tracing/resolve.go`: a FIFTH nil-tolerant `hostMetaLookup func(ns string) (*structpb.Value, bool)` param + the `kindMetadataHost` arm descending the **FULL** `MetaPath` (NOT `[1:]` — that is REQUEST-only), reusing `descend`/`structpbValueToString` VERBATIM, NO new import; + a trailing `nil` at the **32** real `resolve_test.go` callers (**re-grep: a raw count returns 33 — the extra is the string literal at `:106`**).
- **T4** `internal/filter/hcm/accesslog_emit.go:57`/`:118`/`:179`: pass `picked.MetaLookup` as the 5th argument. **The three signatures and all 18 emit callers stay BYTE-UNTOUCHED** — verify mechanically, it is the row's central cost claim. Add a live HOST-metadata-span test AND a first-class **zero-`picked`** test (5 of the 12 SPAN-CAPABLE sites — see SPEC §11's span-capability table, and do NOT use the raw 11-of-18 figure, which counts 6 sites that can never emit a span).
- **T5** the fuzz seed: ADD `meta_host_ok` to `withMetaTags` (`fuzz_test.go:97-115`), **`meta_bad` STAYS on CLUSTER — no repoint**; dispatch-verify the accept arm is reached; **narrow the stale `:92-93` doc comment** ("CLUSTER/HOST stay envoy-go-strict departures") to CLUSTER-only. +0 fuzzers.
- **T6** the `0116-tracing-custom-tags-metadata-host` OTLP fixture on the `0115` chassis: move the `metadata` block off the route onto `lb_endpoints[0].metadata` in BOTH YAMLs, flip both `custom_tags` `kind:` to `host: {}`, keep the single-endpoint cluster. **STRING values only** (P3). Cross-side EXACT key+value, key-based (intra-tag order is internal). Breaks AFTER committing, `-count=1`, the FULL subtest selector, and confirm WHICH assertion fires; minimum three breaks incl. a wrong-namespace control.
- **T7** BEHAVIOR_CONTRACT **B1–B4** (`:688` metadata → CONSUMED for REQUEST+ROUTE+HOST, departures narrowed to `CLUSTER`/unset-A; **B2** the pick-time departure with its exact scoping; **B3** the Zipkin empty-drop + serialization boundaries; `:741` the deferred-field line).
- **T8** the six-gate (gofmt/vet/build/lint · `go mod tidy -diff` empty · the FULL 118-dir differential · `-race` on the touched packages · the cycle guard via `go list -deps`, no `...` · envelope audit). **The envelope audit CANNOT assert `internal/cluster` BYTE-UNTOUCHED** — pin the SHAPE instead (one field + one accessor + one populate line).
- **T9** ADR-0294 completed IN PLACE — **APPEND** `### Decision (landed at the phase-72 IMPL)` + `### Consequences` after the §Context hand-off line (no renumber, and no second §Decision heading — the SPEC deliberately left none, per the ADR-0293 precedent) + **ROADMAP row 72 → `done`** + **the deferred-sentence narrow (`CLUSTER`/`HOST` → `CLUSTER`-only)** + STATE/router.

**Stage-close mechanics (the PLAN):** **DECISIONS UNTOUCHED** (ADR-0294 stays PROPOSED; completed IN PLACE at the IMPL). **Row 72 STAYS `in-progress`.** **NO deferred sentence narrowed** (it rolls OUT at the IMPL — the phase-57 precedent). ROADMAP UNTOUCHED. STATE §Current rolled IN PLACE (lifecycle 2 → 3; lineage re-capped at five — never prepend above §Current, the ADR-0288 rule). next-prompt.txt rolled to the phase-72 **IMPL**. Re-run the sentinel MECHANICALLY at close: (1) `NOT DONE: row 72`; (2) ⇒ **3**; (3) `NEVER OPENED: gRPC/Runtime/WASM`. Does NOT fire; no `stop`. Re-run every mechanical count in the worktree (`ls -d test/fixtures/[0-9]*/ | wc -l` ⇒ 117; `grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l` ⇒ 55; `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1` ⇒ ADR-0294). **Adversarially verify before landing** (the phase-70/71/72 precedent — TWO independent verifiers in PRIVATE scratch, `reference_parallel_subagents_private_scratch`).

**Candidate pool for a FUTURE roll (re-derive each candidate's cost at the CURRENT tip — do NOT trust these adjectives):**
- **Observability (family OPEN):** the LAST `metadata` MetadataKind — **`CLUSTER`** (MODERATE ~11–13 tasks; CLOSES the quartet; needs a metadata field + accessor on `cluster.Cluster`, a `routeAction` method + a `SetClusterMetadata`/`ClusterMetaLookup` pair **RE-PAYING the 18-caller thread**, and a disposition for `weightedClusterRouteAction`'s per-request in-closure selection) · **pick-time `picked` propagation on upstream-failure paths** (NEWLY SURFACED at the phase-72 SPEC — a PRE-EXISTING divergence; ~4 production lines since `ep` is already in scope at the pool-overflow and connect-failure returns, but it flips three access-log consumers and breaks three landed unit tests, and NO differential fixture pins it; **a strong small FUTURE pick — probe the reference's pool-overflow and circuit-breaker arms first, which phase 72 left unprobed**) · `spawn_upstream_span` (LARGE) · OTel `http_service` / `sampler` / `resource_detectors` (MODERATE each) · tracing `verbose` (MODERATE) · force-trace (LARGEST) · the `ssl` handshake-outcome stat family (LARGE — ADR-0286 C3 framework surgery CONFIRMED) · **`access_log[].filter`** (a LIVE behavioral divergence — a KNOWN proto field the strict pass ACCEPTS but ZERO production `GetFilter()` consumers; **flagged a strong FUTURE pick**) · **`stats_flush_on_admin`** · the Zipkin empty-string tag drop + the serialization boundaries (B3 — documented, not chartered) · a 6th stats sink — **UNAVAILABLE**, the only unconsumed `stats_sinks[]` extension is `wasm`.
- **xDS (family OPEN):** upstream SDS (a boot-ORDER blocker) · CDS/EDS · LDS/RDS · ADS (muxed) · Delta xDS · RTDS/Runtime-layer · `google_grpc`. *(Reconnection-backoff was REJECTED at phase 72 as larger-than-it-looks. Mind the `grpcclient→cluster→tls` cycle constraint — `reference_xds_config_seam_transitive_cycle_guard`.)*
- **HTTP/3 (family OPEN):** `QuicProtocolOptions` tuning (SMALL but needs ~2 tasks of NEW connection-reusing harness first; timing-flaky observables) · upstream H3 cluster · alt-svc · 0-RTT/early-data · h3spec conformance gate · QUIC robustness.
- **Never-opened families:** `Runtime` (genuinely unbuilt — `internal/runtime/` = `doc.go` only) · `gRPC` (gated on an HCM response-trailer framework row — `RunDecodeTrailers`/`RunEncodeTrailers` have ZERO production callers) · `WASM` (a ROADMAP bookkeeping artifact — substantially BUILT; a docs correction, not a work row).
- **Other deferred riders:** the RBAC `require=false` fixture rider · DataSource `environment_variable` (D-ENV-HARNESS-gated) · `watched_directory`/SDS rotation · the `internal/cluster` `-race` flake (test hardening).

## Read first (in order)
1. `docs/envoy-go/phases/72-tracing-custom-tags-metadata-host/SPEC.md` — the disposed `D-CTMH-*` docket (§3), the S1–S8 sharpening ledger + what HELD (§1.1), the edit-site roster + the 18-caller classification (§11), the fixture design (§8), the BC edit map (§9), the task surface + the `-race` hazard (§10), the sentinel + deferred rosters (§12, §13).
2. `docs/envoy-go/phases/72-tracing-custom-tags-metadata-host/BRAINSTORM.md` — the origin docket (§7) and the cost derivations (§11), **read with the SPEC's corrections in hand** (two of its premises are FALSE — S2).
3. `docs/envoy-go/ROADMAP.md` — row 72 (`in-progress`) + the phase-72 charter sentence in the §Observability-family heading paragraph + the live Observability `candidates:` sentence + §Schema.
4. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline) + the §Recent lineage + the §Project counts.
5. `docs/envoy-go/DECISIONS.md` tail — **ADR-0294** (PROPOSED — §Context only, with the anticipated design as its closing paragraph and a `*(§Decision + §Consequences land at the phase-72 IMPL.)*` hand-off line, exactly the ADR-0293 shape; **APPEND** `### Decision (landed at the phase-72 IMPL)` + `### Consequences` at the IMPL — do NOT create a second §Decision heading) + **ADR-0293**/**ADR-0292** (the `ROUTE`/`REQUEST` precedents) + ADR-0044 · ADR-0045 · ADR-0080 · ADR-0106 · ADR-0165.
6. The phase-71 PLAN (`docs/envoy-go/phases/71-tracing-custom-tags-metadata-route/PLAN.md`) for the PLAN's SHAPE and its RD-* ledger idiom — **but take every `file:line` from the phase-72 SPEC, not from the 71 documents (their cites are stale).**
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — trust MEMORY.md's index; esp. `feedback_brief_citations_not_evidence`, `reference_quoting_is_not_executing`, `feedback_git_worktrees`, `feedback_execution_style`, `feedback_subagents_no_push`, `reference_route_metadata_resolve_full_metapath`, `reference_filterchain_dynamicmetadata_accessor`, `reference_spec_drafted_identifier_collision_check`, `reference_parallel_subagents_private_scratch`, `reference_next_prompt_tracked_despite_gitignore`, `reference_break_protocol_commit_first`, `reference_differential_break_protocol_count1`, `reference_deliberate_break_wrong_assertion`, `reference_differential_run_selector`, `reference_plan_break_instructions_dont_compile`, `reference_fuzzer_count_docs_drift`, `reference_code_comment_not_evidence`.
