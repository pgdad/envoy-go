=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-VERIFIED 2026-07-20 (re-run MECHANICALLY at the phase-69 PLAN close, in the stage worktree) — it does NOT fire. `stop` was NOT created.** Actual output:
- **(1) prints `NOT DONE: row 69`** — row 69 is `in-progress` (PLAN done; it flips `done` only at the phase-69 IMPL six-gate; every OTHER chartered row is `done`).
- **(2) prints THREE live "candidates:" sentences** — HTTP/3, xDS, Observability ⇒ three families STAY OPEN. **Cite the command, never the adjective** (`reference_sentinel_deferred_sentence_live_vs_historical`): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` ⇒ **3**.
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** ⇒ THREE families never opened.

Checks (1)+(2)+(3) print ⇒ the sentinel does NOT fire. Abundant work remains.

==============================================

## STATUS AS OF 2026-07-20: THE PHASE-69 PLAN IS COMPLETE AND LANDED. ROW 69 (`stats-sink-otlp`) IS `in-progress`. THIS SESSION PERFORMS THE PHASE-69 IMPL.

**What the phase-69 PLAN landed** (`git log --grep 'phase 69'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): the PLAN RE-DERIVED the SPEC against the tree at `0ea2ec20` (a read-only re-derivation agent re-opened EVERY SPEC §11 anchor at tip — **ZERO unacknowledged drift, every anchor EXACT**, unlike phase 68's RD-LINES boundary drift — and extracted the exact clone skeletons: the `NewOTLPLogsClient` stanza, the `parseMetricsServiceConfig` reject template, the streaming-vs-unary mapping site `sink.go:198-202`, the six-getter `SinkConfig` descriptor, the `otlptrace` receiver), then decomposed SPEC §10 into an 11-task TDD spine (`PLAN.md`) + a `PROGRESS.md` skeleton. **Key PLAN findings (§1 RD-*, all RE-DERIVED, adversarially verified):**
- **RD-EXACT:** every SPEC §11 cite is exact at `0ea2ec20` — no line-boundary correction to carry; the SPEC's anchors are adopted verbatim.
- **RD-STREAMING:** the `delta.apply`+`labels.apply` mapping site is `run()` NOT `Submit()` (`sink.go:198-202`) — the OTLP writer applies the delta in the writer goroutine so an enqueue-drop never latches `deltaState`; it clones the UNARY otlplogs/otlptrace shape (retry-once `Export`), NOT the streaming `MetricsServiceSink` lifecycle.
- **RD-SKEW (EXECUTED by the V1 verifier in a clone):** a `protojson.UnmarshalOptions{DiscardUnknown:false}` unmarshal of a bootstrap carrying `resource_detectors` on the `open_telemetry` `SinkConfig` Any ERRORS `unknown field "resource_detectors"` INSIDE the Any; flipped to `true` it silently drops it; WITHOUT the blank import it errors `unable to resolve "…SinkConfig": not found`. **The free version-skew reject HOLDS and DISCRIMINATES, and the T8 descriptor-blank-import dispatch trap is real.**
- **RD-WRAPPER:** the two `*wrapperspb.BoolValue` knobs are struct-doc "Default value is true." — read `w==nil || w.GetValue()` (nil→TRUE), INVERTING the metrics_service `.GetValue()` (nil→FALSE) at `bootstrap.go:600`. **RD-TAGS:** the two knobs are INDEPENDENT, so `otlp.go` calls `stats.ExtractTags` DIRECTLY (same-package) and decides name/attributes SEPARATELY. **RD-VERSION:** `telemetry.sdk.{name=envoy-go,language=go,version=admin.BuildVersionString()}`, threaded into `NewOTLPMetricsSink` from `main.go` (already imports `internal/admin`). **RD-COLLIDE:** the wrapper is `OTLPMetricsClient` to disambiguate `colmetricspb.MetricsServiceClient` from `metricsv3.MetricsServiceClient`. **RD-RECV:** `test/helpers/otlpmetrics` clones `test/helpers/otlptrace`, keyed `(name, sorted-attrs)` order-insensitive.
- **Adversarial verification (PLAN §1.2):** V1 (code-claims + the RD-SKEW by-execution probe) → ZERO SEVERE/MODERATE, four MINOR folded. V2 (process/coverage) → **ONE SEVERE folded** — the SPEC §7 `TestNoNewStat_OTLPRegistrationGuard` (the +0-stats one-per-sink house guard, `registration_test.go` precedent) was cited by T10 but written by NO task; CORRECTED by adding **T2 Step 5b** + the `registration_test.go` File-structure entry + the Self-review mapping. Design direction unchanged.
- **Counts at exit (docs-only; DECISIONS UNTOUCHED — ADR-0291 completes at the IMPL):** fixtures **113** · fuzzers **55** · stat **1201** · BackendKind **38** · go.mod modules **2** · DECISIONS tail **ADR-0291** (next-free **ADR-0292**). Row 69 STAYS `in-progress`; NO deferred sentence narrowed (rolls OUT at the IMPL, phase-57 precedent).

**⚠️ PUSH (idempotent — evaluate, then act):** FIRST ACTION: `git fetch`; if origin/master is BEHIND local master and the local tip is the phase-69 PLAN squash (`git log --grep 'phase 69'`, locate by SUBJECT), `git push origin master` per `feedback_push_to_origin`; if in sync, proceed; if REJECTED (diverged), STOP and reconcile — never rebase away landed squashes.

## What THIS session does — phase 69 IMPL

One stage per session: the IMPL — EXECUTE the 11-task TDD spine (T1–T11) in `PLAN.md`, landing production `.go` + tests + the two fixtures + the BC delta + ADR-0291 completion. Fresh worktree off master per `feedback_git_worktrees`; subagent-driven — one implementer per task + a per-task adversarial reviewer + a final whole-branch review (`feedback_execution_style`); commit LOCALLY only, controller squash-pushes at close (`feedback_subagents_no_push` / `feedback_push_to_origin`). **Row 69 FLIPS `done` at the T10 six-gate** (ADR-0106, the SOLE leg).

**The IMPL owes (the phase-68 precedent):**
- **RE-DERIVE each task's anchors at the IMPL tip — a PLAN is not evidence** (`feedback_brief_citations_not_evidence`; the phase-68 IMPL re-derived every PLAN anchor per task). Start where PLAN §1 is most confident: RD-EXACT (the zero-drift anchor set), RD-STREAMING (`sink.go:198-202`), RD-WRAPPER (the nil→TRUE inversion), RD-SKEW (`bootstrap.go:510` the ONLY `DiscardUnknown`, executed-verified).
- **Execute T1–T11** (PLAN §File structure + per-task steps): T1 grpcclient `OTLPMetricsClient` · T2 `internal/statssink/otlp.go` `OTLPMetricsSink` + seam + mapping + the UNARY writer + **`TestNoNewStat_OTLPRegistrationGuard` (T2 Step 5b — do NOT drop it)** · T3 the `bootstrap.go` parse arm (descriptor blank-import; fifth dispatch arm; reject 4→5; nil→TRUE wrappers; histogram reject; free version-skew reject) · T4 `main.go` gate+sub-loop · T5 `test/helpers/otlpmetrics` receiver · T6/T7 fixtures `0112`@10448 + `0113`@10449 + breaks · T8 fuzz seed + dispatch-verify · T9 BC B1–B2 (incl. the BC:836 sibling-reject-example swap OFF "OTLP-metrics") · T10 six-gate/full-115-dir/-race/cycle-guard/envelope-audit · T11 ADR-0291 complete IN PLACE + ROADMAP row 69 → `done` + roll "OTLP-metrics stats sink +" OUT of ROADMAP:198 + STATE/router/memory.
- **RED-first + deliberate breaks that FIRE the exact assertion** (`-count=1`, full selectors, confirm WHICH fired, `reference_deliberate_break_wrong_assertion`): the PLAN pins each (Breaks A–Q + the order-insensitive NON-break control). Dispatch-verify the fuzz seed REACHES `parseOpenTelemetrySinkConfig` (without the `SinkConfig` blank-import it dies at `unable to resolve … not found` before dispatch). A break that does NOT fire is a FINDING recorded in `PROGRESS.md`, not routed around.
- **Envelope discipline:** 4 functional production files (`internal/statssink/otlp.go` new + `internal/bootstrap/bootstrap.go` + `internal/grpcclient/grpcclient.go` + `cmd/envoy-go/main.go`); new exported symbols ONLY in the existing `internal/statssink`+`internal/grpcclient` rosters; `internal/xds`/`internal/tls`/`internal/boot`/`internal/listener` + the landed `internal/statssink/{sink,delta,label,mapping,flusher}.go` BYTE-UNTOUCHED. +0 packages/modules/stats/fuzzers/BackendKinds; fixtures 113 → 115. `dns_lookup_family: V4_ONLY` on sink clusters (P-16). Re-check `git diff go.mod` after tidy (`reference_new_subpackage_pulls_transitive_module`).
- **ADR-0291 completes IN PLACE at T11** (§Decision + §Consequences, STATUS → COMPLETE; NO new ADR, NO renumber — tail stays ADR-0291, next-free ADR-0292). Sentinel re-run MECHANICALLY at close: check (1) goes SILENT for row 69 (every row `done`); (2) still prints 3; (3) still prints gRPC/Runtime/WASM ⇒ does NOT fire; no `stop`. Then the roller SELF-PICKS the phase-70 subject (the 2026-07-12 standing directive). Memory updates owed (PLAN §T11 / SPEC §13): the free version-skew reject posture + the nil→TRUE wrapper inversion.

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline; the next-skill line carries the owed roster).
2. `docs/envoy-go/phases/69-stats-sink-otlp/PLAN.md` — the 11-task spine (T1–T11), §1 the RD-* re-derivation ledger, §1.2 the adversarial record, per-task steps + RED-first breaks, the File structure, the Self-review-against-SPEC table.
3. `docs/envoy-go/phases/69-stats-sink-otlp/PROGRESS.md` — the task ledger to FILL at the IMPL (red-first verbatims, which break fired, substitutions) + the RD-* findings carried.
4. `docs/envoy-go/phases/69-stats-sink-otlp/SPEC.md` — the FULL D-OTLPM disposition + the §9 BC edit map + the §11 edit-site roster + the §14 ADR-0291 §Context draft.
5. `docs/envoy-go/ROADMAP.md` — row 69 + the Observability family paragraph (locate the live `candidates:` sentence by grep, not line) + §Schema. `docs/envoy-go/DECISIONS.md` — ADR-0291 (§Context; complete IT in place) + ADR-0262/0263/0264/0265/0275 (the sink lineage) + ADR-0044/0045/0106/0080.
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — trust MEMORY.md's index; esp. `reference_stats_sink_emits_used_only`, `reference_periodic_sink_differential_two_receivers`, `reference_delta_sink_differential_stability_barrier`, `reference_cluster_sink_dial_unaccounted`, `reference_protojson_wrapper_scalar_not_object`, `reference_strict_reject_sibling_typeurl_gap`, `reference_differential_grpc_receiver_driver_owned`, `reference_host_gateway_ip_docker_desktop`, `reference_full_suite_race_after_background_mutator`, `reference_new_subpackage_pulls_transitive_module`.
