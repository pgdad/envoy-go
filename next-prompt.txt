=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-VERIFIED 2026-07-18 (re-run MECHANICALLY at the phase-67 IMPL close, in the stage worktree) — it does NOT fire. `stop` was NOT created.** Actual output:
- **(1) prints NOTHING** — row 67 flipped `done` at the phase-67 IMPL six-gate; every ROADMAP row is now `done`. Check (1) NO LONGER blocks `stop` (it re-opens when the phase-68 BRAINSTORM registers row 68 `in-progress`).
- **(2) prints THREE live "candidates:" sentences** — HTTP/3, xDS, Observability ⇒ three families STAY OPEN. **Cite the command, never the adjective** (`reference_sentinel_deferred_sentence_live_vs_historical`): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` ⇒ **3**.
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** ⇒ THREE families never opened. Operational-tooling also STAYS OPEN.

Checks (2)+(3) print ⇒ the sentinel does NOT fire. Abundant work remains.

==============================================

## STATUS AS OF 2026-07-18: THE PHASE-67 IMPL IS COMPLETE AND LANDED. ROW 67 IS `done`. THIS SESSION PERFORMS THE PHASE-68 BRAINSTORM (the roller SELF-PICKS the subject per the standing directive).

**What the phase-67 IMPL landed** (`git log --grep 'phase 67'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): the 9-task TDD spine (T1–T9) — `NewDownstreamConfig` now maps `ClientAuth` three-way across FOUR cells (true+anchor→`RequireAndVerifyClientCert` · false/absent+anchor→`VerifyClientCertIfGiven` · false/absent+no-anchor→`NoClientCert` · true+no-anchor→the RETAINED `requires validation_context.trusted_ca` reject) via a shared `installPool`/`clientAuthFor` closure (assignment-adjacency + the interface-pinned nil-pool unconstructibility); E3 RETIRED atomically; fixture `0110-tls-require-client-cert-false` (CVC-primary at require=false, three-arm FORCED-SEND driver); ADR-0289 COMPLETED IN PLACE + the B12 annotation; the five-site serve-anyway drift reconciled. Six-gate + full 112-dir differential (`ok`, 0 FAIL, 375s) + `-race` + cycle guard all GREEN. Counts: fixtures **112** · fuzzers **55** · stat **1201** · BackendKind **38** · DECISIONS tail **ADR-0289** (next-free **ADR-0290**) · go.mod modules **2**.

**⚠️ PUSH (idempotent — evaluate, then act):** FIRST ACTION: `git fetch`; if origin/master is BEHIND local master and the local tip is the phase-67 IMPL squash, `git push origin master` per `feedback_push_to_origin`; if in sync, proceed; if REJECTED (diverged), STOP and reconcile — never rebase away landed squashes.

## What THIS session does — phase 68 BRAINSTORM (the roller SELF-PICKS)

One stage per session: BRAINSTORM ONLY (docs-only, ZERO production `.go`). Fresh worktree off master, branch `phase-68-<slug>-brainstorm`, per `feedback_git_worktrees`. Invoke `superpowers:brainstorming` FIRST. Register row 68 `in-progress` at the BRAINSTORM stage-close per the ROADMAP §Schema invariant (it flips `done` at that phase's IMPL six-gate, the SOLE leg unless split). **RE-DERIVE every anchor** — a router pointer is not evidence (`feedback_brief_citations_not_evidence`).

**SELF-PICK (standing directive): record the pick + the FULL rejected roster in the BRAINSTORM.** STATE.md §Current pointer's `next-skill` line carries the provisional pick and the pre-rejected roster — READ IT. In brief:
- **PROVISIONAL subject: the empty-dynamic Design B fallback** (the phase-67 BRAINSTORM's explicit RUNNER-UP) — implement the reference's served-but-EMPTY-dynamic-context ACK-fallback-serve (fall back to the inline `default_validation_context.trusted_ca` and SERVE) where envoy-go currently boot-FAILS (the phase-66 empty-dynamic departure, precisely characterized at ADR-0289 / BC B18). Substrate-reused (`internal/tls` + the landed CVC arm). **PROVISIONAL — the BRAINSTORM may OVERTURN on evidence (the phase-66 precedent, where the router's provisional upstream-SDS pick was overturned).**
- **DO NOT naively re-pick — each ALREADY evaluated + REJECTED (phase-66 BRAINSTORM §2.1, archived in `STATE_HISTORY.md`; mechanisms recorded — do NOT re-derive):** `xds-sds-upstream-server-cert` (BLOCKED by a VALUE-level constructibility cycle — the SDS provider needs the `*cluster.Manager` being constructed; a boot-model reshape, NOT smallest; the TYPE-level cycle guard does NOT catch it) · DataSource `environment_variable` (declined D-ENV-HARNESS seam) · HTTP/3 `QuicProtocolOptions` (no reject to lift; no observable moves) · tracing `custom_tags` `metadata` (~19 call-site threading + a signature change — not small).
- **Other live candidates:** SDS rotation (`watched_directory` positive control FIRST) · the `ssl` stat family (framework surgery, ADR-0286 C3) · the `validation_context_type` switch's missing `default:` arm · `crl` · the compose-two edge · CDS/EDS/LDS/RDS/ADS/Delta xDS · OTLP-metrics stats sink. If a smaller/cleaner candidate than Design B is found, PICK IT and record the comparison.
- The BRAINSTORM PROBES the reference (fresh container per arm, `reference_probe_fresh_container_per_arm`; private scratch OUTSIDE the repo, `reference_parallel_subagents_private_scratch`) — a BRAINSTORM is not evidence; RUN, don't cite (`reference_quoting_is_not_executing`).

**Sentinel maintenance:** do NOT narrow a deferred sentence unless the picked subject is literally named in one — check first (the phase-64/66 precedent: verify-if-presented was never IN one). Registering row 68 `in-progress` re-opens check (1); (2)+(3) stay as above ⇒ sentinel still does NOT fire.

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline). The `next-skill` line carries the self-pick roster + the owed reads.
2. `docs/envoy-go/ROADMAP.md` — row 67 (now `done`), the §Schema, the THREE live deferred sentences, the family headings.
3. `docs/envoy-go/phases/67-tls-require-client-cert-false/SPEC.md` §13 — the carried deferred-items roster (the candidate source).
4. `docs/envoy-go/DECISIONS.md` — ADR-0289 (completed) + ADR-0287/0286/0280 (the SDS lineage the pick continues). Tail ADR-0289 — re-derive.
5. `docs/envoy-go/STATE_HISTORY.md` — the phase-66 BRAINSTORM §2.1 archived rejected roster (the pre-rejection mechanisms; do NOT re-derive). NEVER grep it for a pointer or count.
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_*` + `reference_*`; trust MEMORY.md's index.
