START a fresh session for envoy-go (router last updated 2026-06-24, at the 43.2b BRAINSTORM-done tip `b8ec7b4a`). **The next action is the phase-43.2b (graceful GOAWAY rotation) SPEC** (`superpowers:brainstorming` is DONE → now author the SPEC and EXECUTE the §-level live-probe pins, then spec-document-reviewer). **Git hygiene:** the docs-only SPEC commit itself lands DIRECT on master (the BRAINSTORM/SPEC lifecycle convention), but run the live-probe SCRATCH — the Go h2c GOAWAY-emitting probe programs + the Docker bridge/compose — in a throwaway git worktree per `feedback_git_worktrees` (categorical preference: any isolated work, regardless of size); the probe code is in-session throwaway (NOT committed — the 43.2a-SPEC in-session-probe precedent), so discard the worktree after pinning the D-H2B-* findings into the SPEC. Phase 43 (`connection-pooling`) is the FIFTH-and-FINAL Upstream-robustness-family row and is `in-progress`: **leg 43.1 DONE** (max_connections hard-cap + max_pending_requests queue, ADR-0252) + **leg 43.2a DONE** (the H2 multiplex pool substrate, ADR-0253, SUPERSEDES ADR-0056) + **leg 43.2b PENDING** (this one — the LAST; BRAINSTORM done). When 43.2b lands, **row 43 flips `done`** and the Upstream-robustness family (phases 39–43) CLOSES (`reference_roadmap_split_phase_row_done`).

First confirm the landed tip is the 43.2b BRAINSTORM commit via `git log --oneline -3` (expect `phase 43.2b (h2-connection-pool) BRAINSTORM …`; re-read the sha, do NOT assume). Counts at tip (UNCHANGED — docs-only brainstorm): stat surface **1185** (H2 cluster; non-H2 stays 1183) / fixtures **81** (tail `0079-h2-multiplex-pool`) / fuzzers **42** (documented; actual `^func Fuzz`=43 per `reference_fuzzer_count_docs_drift`) / BackendKind tail **37** (`H2HoldResponder`) / DECISIONS tail **ADR-0253**, next-free **ADR-0254**.

## NEXT STEP — the 43.2b SPEC

Author `docs/envoy-go/phases/43.2-h2-connection-pool/SPEC-43.2b.md` in the house SPEC shape from the brainstorm (`BRAINSTORM-43.2b.md`, committed this session), then EXECUTE the D-H2B-* live-probe pins against `envoyproxy/envoy:contrib-v1.37.2` per `reference_docker_probe_bridge_network` (a shared bridge; a Go h2c GOAWAY-emitting backend; sequenced held-stream + GOAWAY-trigger drive; `/stats`-delta scrapes), then spec-document-reviewer. The brainstorm SETTLED these forks (do NOT re-litigate — execute):

1. **Scope: CORE-ONLY** — GOAWAY rotation + the behavior-driven `http2.*` reset/goaway stats. `min(local,peer)` enforcement (AMEND-H2-5) + `nextStreamID` 2^31-exhaustion retirement (M-12/Task-11) are DEFERRED hardening items — do NOT pull them in.
2. **Drain detection: a per-conn drain-watcher goroutine** (spawned in `dialAndPool` after the `h2Pool[addr]` append; `select { <-cc.GoneAwayCh() ; <-cc.ctx.Done() }`; on GOAWAY re-lock `h2PoolMu`, re-check `findPooledLocked != nil`, evict-if-idle). New codec accessors `GoneAway() bool` + `GoneAwayCh() <-chan struct{}` on `ClientConn`. Admission scans (`findStreamHitLocked`/`findConnectingHitLocked` + `h2PromoteLocked`'s stream-slot branch) add `&& !pc.cc.GoneAway()`; `makeRelease` eager-close generalizes to `(Closed() || GoneAway()) && inFlight==0`.
3. **Stats: a behavior-driven `http2.*` subset** (candidates `goaway_sent`/`rx_reset`/`tx_reset`/`stream_refused_errors`; every one LIVE). The EXACT set + the `+N` (anticipated 1185 → ~1190) is the headline SPEC live-probe pin (D-H2B-STATS). useH2-gated; a registration test analog to `TestRegisterClusterMetrics_H2Stats`.
4. **Stat wiring: INJECT cluster `http2.*` stat handles into the `ClientConn` at dial** (`dialPooledH2To` → `NewClientConn`) — the FIRST codec→cluster stat wiring (43.2a drove all stats from the pool layer). The codec increments at each event site (`dispatchFrame` RST cases, `emitGoaway`, `Close`).
5. **Differential `0080-h2-goaway-rotation`: SEQUENCED-BARRIER EXACT, attempt first** (subject-EXACT + cross-ROBUST fallback per-count if the probe shows a residual race). MUST use an H2 (TLS+ALPN-h2) DOWNSTREAM listener (the 0004 PKI shape — `reference_h2_pool_downstream_codec_gate`). The GOAWAY-capable backend = extend `H2HoldResponder` (kind 37) with a `/__goaway` gate vs a new kind 38 (D-H2B-BACKEND — a PLAN call; the SPEC may anticipate either).

The SPEC live-probe block (execute IN-SESSION): **D-H2B-STATS** (exact `http2.*` names + scopes + `+N`); **D-H2B-ROTATION** (the reference's lazy-replacement `upstream_cx_*`/`cx_destroy`/`cx_active` sequence on a peer GOAWAY); **D-H2B-EXACT** (per-count cross-side determinism under the sequenced drive); **D-H2B-GOAWAYSENT** (whether `goaway_sent` is OUR-Close-driven; whether the reference counts a received-GOAWAY at cluster scope — Envoy historically has no `goaway_received` cluster counter). Anchor the **ADR-0254 §Context** at the SPEC (PROPOSED; §Decision/§Consequences land at the 43.2b IMPL per ADR-0044). Carry the PLAN D-questions (D-H2B-{BACKEND, WATCHER-LIFECYCLE, WIRING, SPLIT-FINAL, FUZZER-RECONCILE, BYTESTAB}).

## What 43.2a delivered (the substrate 43.2b builds on — ADR-0253, SUPERSEDES ADR-0056)

A per-endpoint H2 multiplex connection pool (`internal/cluster/h2pool.go`) over the 43.1 permit substrate (ADR-0252). The four-tier admission ladder in `AcquireH2Stream(ctx)`: stream-HIT → connecting-HIT/attach (the Task-9.5 connect-time dial COALESCING — K concurrent acquires open exactly `ceil(K/C)` conns) → MISS dial-as-initiator (`tryAcquireConnSlot` reserves a 43.1 permit) → PEND in the SEPARATE stream-aware queue (woken on stream-free via `h2PromoteLocked` OR conn-close; bounded by `max_pending_requests`; queue-full → `errConnPoolOverflow` → 503). A SINGLE ctx-aware `c.lb.Pick(…)` (the Task-7.5 fix) threaded through the whole ladder. The per-conn stream budget is the cluster's OWN `http2_protocol_options.max_concurrent_streams` (the LOCAL cap C; AMEND-H2-1). Two conserved resources (the 43.1 permit + the LB-pick release) each fire EXACTLY ONCE per acquire; `-race -count=20` clean. The router rewire (`router_h2.go`) moved BOTH H2 sites onto `AcquireH2Stream`+`release()`+evict-on-error; the legacy `doH2` was proven dead + REMOVED. **The H2 pool engages ONLY for an H2 (TLS+ALPN-h2) DOWNSTREAM listener** (no H1-down→H2-up bridge). Stats: `upstream_cx_http2_total` + `http2.streams_active` (useH2-gated; 1183→1185). 43.2a ships MINIMAL liveness only (`Closed()`-skip + evict-on-RoundTrip-error) and DEFERRED the drain lifecycle + the reset/goaway stats to 43.2b — the seam 43.2b extends is `releaseStream`→`evictH2ConnLocked` (today fires on `Closed() && inFlight==0`; 43.2b adds the draining case) + the codec's `goawayCh` signal (today closed by `dispatchFrame` but NOT exposed to the pool and NOT cancelling ctx).

## Honor throughout (the discipline)

- **Lifecycle = one stage per session.** A SPEC is a docs commit DIRECT on master (a worktree only if code-adjacent probe artifacts land) — but `feedback_git_worktrees` applies to the later 43.2b PLAN/IMPL stages · `feedback_subagents_no_push` (subagents commit LOCALLY; the controller squashes + pushes at stage-close) · `feedback_push_to_origin` (push after a clean commit on master with tests green) · `feedback_execution_style` (subagent-driven execution for IMPL) · `feedback_pertask_gofmt_lint`.
- **Process anchors:** ADR-0106 + `reference_roadmap_split_phase_row_done` (row 43 flips `done` only when ALL legs land — 43.1 + 43.2a done, 43.2b is the LAST); ADR-0080 (strict-reject); ADR-0044 (the ADR §Decision/§Consequences land at IMPL; §Context drafts at SPEC); `reference_docker_probe_bridge_network` (the live `contrib-v1.37.2` probe for the D-H2B-* pins) + `reference_concurrency_differential_release_barrier` + `reference_concurrent_attempt_downstream_class_assertion` (the differential discipline); `reference_differential_run_selector` + `reference_differential_break_protocol_count1` + `reference_differential_fullsuite_startup_flake` (a `subject ready: EOF` is a transient — isolate-re-run, NOT a regression); `reference_fuzzer_count_docs_drift` (the documented 42 vs actual 43 — reconcile at this family-closing leg, D-H2B-FUZZER-RECONCILE).
- **The H2 pool memory:** `reference_h2_pool_local_cap_driven` (the local-cap-driven `ceil(K/C)` finding — the load-bearing 43.2a/43.2b architecture); `reference_h2_pool_connect_coalescing` (the connecting-tier coalescing 43.2b must PRESERVE); `reference_h2_pool_downstream_codec_gate` (the GOAWAY differential MUST use an H2 downstream listener — the 0004 PKI shape); `reference_max_connections_soft_breaker` (the 43.1 contrast).

## Read first (in order)

1. THIS FILE — the cold-start router (43.2b BRAINSTORM is DONE + LANDED; the next action is the 43.2b SPEC).
2. `docs/envoy-go/STATE.md` — active-phase `phase 43.2b (h2-connection-pool) BRAINSTORM done`; the count figures (1185 / 81 / 42 / 37 / ADR-0253, next-free ADR-0254) + the full fork record.
3. `docs/envoy-go/phases/43.2-h2-connection-pool/BRAINSTORM-43.2b.md` — the brainstorm (the design forks, the D-H2B-* pin questions, the ADR-0254 §Context draft) — the SPEC's direct input.
4. `docs/envoy-go/DECISIONS.md` — **ADR-0253** (the 43.2a architecture as-built, the substrate 43.2b extends; its §Consequences "DEFERRED to 43.2b" is the charter) + **ADR-0056** (SUPERSEDED by ADR-0253); next-free **ADR-0254**.
5. `internal/cluster/h2pool.go` (the as-built pool — `dialAndPool` [the watcher-spawn site], `findStreamHitLocked`/`findConnectingHitLocked`, `makeRelease`, `evictH2ConnLocked` [the eviction seam 43.2b's draining-close extends], `EvictH2ConnOnError` [the double-evict race partner], `h2PromoteLocked`) + `internal/filter/hcm/h2/client.go` (the `ClientConn` — `goawayCh`/`dispatchFrame`'s `GoAwayFrame` case [closes goawayCh, does NOT cancel ctx], `Closed()`, `Close`→GOAWAY-emit, `emitGoaway`, the RST_STREAM receive/send sites, `nextStreamID`).
6. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the `### Cluster — HTTP/2 upstream multiplex connection pool` subsection (43.2a) the 43.2b leg extends.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `reference_h2_pool_local_cap_driven`, `reference_h2_pool_connect_coalescing`, `reference_h2_pool_downstream_codec_gate`, `reference_roadmap_split_phase_row_done`, `reference_docker_probe_bridge_network`, `reference_differential_fullsuite_startup_flake`, `reference_concurrency_differential_release_barrier`, `reference_concurrent_attempt_downstream_class_assertion`, `reference_differential_run_selector`, `reference_differential_break_protocol_count1`, `reference_fuzzer_count_docs_drift`, `feedback_git_worktrees`, `feedback_subagents_no_push`, `feedback_push_to_origin`, `feedback_execution_style`, `feedback_pertask_gofmt_lint`).
