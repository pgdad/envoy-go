=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-VERIFIED 2026-07-23 (re-run MECHANICALLY at the phase-72 BRAINSTORM close, in the stage worktree, by the controller — then RE-CONFIRMED a third time on LANDED master after the squash-push, byte-identical output) — it does NOT fire. `stop` was NOT created.** Actual output:
- **(1) prints `NOT DONE: row 72`** — row 72 (`tracing-custom-tags-metadata-host`) was registered `in-progress` at the phase-72 BRAINSTORM commit per the ROADMAP §Schema invariant; the registration RE-OPENS check (1). It flips `done` at the phase-72 IMPL six-gate (ADR-0106, the SOLE leg). Every other chartered ROADMAP row is `done`.
- **(2) prints THREE live "candidates:" sentences** — HTTP/3, xDS, Observability ⇒ three families STAY OPEN. **Cite the command, never the adjective** (`reference_sentinel_deferred_sentence_live_vs_historical`): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` ⇒ **3**. (The Observability sentence names the non-`REQUEST`/non-`ROUTE` MetadataKinds `CLUSTER`/`HOST`; the phase-72 pick `HOST` is NAMED in it and does NOT drop it — it narrows to `CLUSTER`-only at the phase-72 IMPL row-done edit, the phase-57 precedent. NO deferred sentence is narrowed at ANY pre-IMPL stage.)
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** ⇒ THREE families never opened. (The phase-72 BRAINSTORM §2.1 records that the `WASM` line is a ROADMAP **bookkeeping artifact** — `internal/wasm/` is 21,550 LOC of wazero host and rows 25/25.1/25.2/25.3 are all `done` — flagged for a future correction and deliberately LEFT AS-IS here, since the stage-close mechanics depend on this output.)

Checks (2) + (3) print ⇒ the sentinel does NOT fire. Abundant work remains.

==============================================


## STATUS AS OF 2026-07-23: THE PHASE-72 BRAINSTORM IS COMPLETE AND LANDED. ROW 72 (`tracing-custom-tags-metadata-host`) IS REGISTERED `in-progress`. THIS SESSION PERFORMS THE PHASE-72 SPEC.

**What the phase-72 BRAINSTORM landed** (`git log --grep 'phase 72'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): the roller's AUTONOMOUS self-pick opened the EIGHTEENTH Observability-family row — the tracing `custom_tags` `metadata` type for the **`HOST`** MetadataKind (the SIXTH tracing `custom_tags` capability, the THIRD of the four `metadata` MetadataKinds after `REQUEST`@70 and `ROUTE`@71). docs-only, ZERO production `.go`; row 72 registered `in-progress` (§Schema) + a phase-72 charter sentence appended to the §Observability-family heading paragraph. The pick was established by TWO independent read-only cost-assessment agents at tip `4238a4d3`.
- **The subject:** lift the `custom_tags metadata tag %q host kind unsupported` reject (`internal/tracing/config.go:267-268`) → an accept arm cloning the phase-71 ROUTE accept (`config.go:248-264`), resolving the SELECTED UPSTREAM ENDPOINT's metadata (`lb_endpoints[].metadata.filter_metadata[ns]`, walked by `MetadataKey.path`) onto the ingress SERVER span as a `{tag, value}` attribute on BOTH the OTLP and Zipkin exporters via the shared `ResolveCustomTags` → `Span.Attrs []KV` seam (phase 46). `CLUSTER` + the two unset-kind arms stay reject-loud with distinct substrings (envoy-go-strict, ADR-0080 — the reference BOOTS them).
- **The DECISIVE cost finding — `HOST` SKIPS THE 18-CALLER THREADING TAX:** `picked cluster.Endpoint` is ALREADY the 4th parameter of all three emit functions (`internal/filter/hcm/accesslog_emit.go:27`/`:87`/`:149`, already consumed at `:72`/`:133`/`:194` via `upstreamHostString`) and the three `ResolveCustomTags` call sites (`:57`/`:118`/`:179`) sit INSIDE them. So the `hostMetaLookup` closure is built LOCALLY from the in-scope `picked`, the three emit SIGNATURES are UNCHANGED, the 18 emit callers (5 `connection.go` + 6 `h2dispatch.go` + 7 `h3dispatch.go`) are BYTE-UNTOUCHED, and `internal/filter/http/chain.go` is not touched at all (unlike 70's `DynamicMetadata()` and 71's `RouteMetaLookup()` adds). **The SPEC must RE-CONFIRM this by execution, not by citation** (`reference_quoting_is_not_executing`) — it is the row's central cost claim.
- **The ONE genuine gap (D-CTMH-ENDPOINT-METADATA, the load-bearing question):** `cluster.Endpoint` retains only a LOSSY projection — the `envoy.lb` namespace ONLY, scalars ONLY (`internal/cluster/cluster.go:46-49`, populated at `internal/cluster/manager.go:883` via `ScalarsFromStruct`, `subset.go:263-285`, which routes `StructValue`/`ListValue`/`NullValue`/nil into a DISCARDED `nonScalar` return). A HOST tag must address a caller-chosen namespace AND walk a nested path, so the RAW per-namespace metadata must be RETAINED ALONGSIDE it (an added field + a one-line populate; the phase-38 projection BYTE-UNCHANGED; no identity ripple — `Endpoint` is already non-comparable per `cluster.go:88-92` and every identity use is `Addr()`-keyed). **`internal/cluster` is phase 72's ONE genuinely new blast-radius surface vs phases 70/71** — the IMPL envelope audit must pin the change's SHAPE, not assert BYTE-UNTOUCHED.
- **TWO STALE-COST CORRECTIONS recorded** (`reference_deferred_candidate_cost_restale`): (1) `HOST` had been costed MODERATE by both predecessor BRAINSTORMs on "only the reduced `envoy.lb` subset is in scope" — the FACT holds exactly, but its cost IMPLICATION was never derived (neither noticed the endpoint is already an emit parameter); `HOST` is SMALL. (2) The `CLUSTER` "LARGE because of the `upstream_cluster` framework gap" adjective is a **CATEGORY ERROR**, not merely stale: the gap is real (`accesslog_emit.go:51`/`:112`/`:173` hard-code `UpstreamCluster: ""`) but belongs to the `upstream_cluster` span TAG — a DIFFERENT feature. `CLUSTER` is MODERATE (~11–13 tasks), rejected on SIZE not impossibility, and is the natural phase-73 candidate that CLOSES the `metadata` quartet.
- **Also recorded:** `NEVER OPENED: WASM` is a ROADMAP **bookkeeping artifact** (21,550 LOC of wazero host; rows 25/25.1/25.2/25.3 all `done`) — flagged, deliberately NOT acted on. And TWO newly-named deferred candidates from this session's sweep: **`access_log[].filter`** (a 13-arm oneof that is a KNOWN proto field, so the strict `DiscardUnknown:false` pass ACCEPTS it, yet a repo-wide `GetFilter()` grep returns ZERO production hits ⇒ a LIVE BEHAVIORAL DIVERGENCE; **flagged a strong FUTURE pick**) and **`stats_flush_on_admin`** (`internal/bootstrap/bootstrap.go:606-607`).
- **Anticipated envelope (at the phase-72 IMPL):** ZERO new packages (edits in `internal/tracing` + `internal/cluster` + `internal/filter/hcm`, all existing) / +0 stats (**1201**) / +0 fuzzers (**55** — a PURELY ADDITIVE `meta_host_ok` `FuzzHCMConfigParse` seed; NO `meta_bad` repoint, CLUSTER stays the live reject-arm seed) / +0 BackendKinds (**38**) / +0 go.mod modules (**2**) / ONE new OTLP fixture `0116-tracing-custom-tags-metadata-host` (a static `lb_endpoints[].metadata` hit arm + a `default_value` arm, single-endpoint cluster, NO writer) ⇒ fixtures **117 → 118** / DECISIONS tail **ADR-0293 → ADR-0294** (at the SPEC; next-free ADR-0295). SINGLE FLAT ROW anticipated (~9–11 tasks; ADR-0045 armable-but-unconsumed).

**⚠️ PUSH (idempotent — evaluate, then act):** FIRST ACTION: `git fetch`; if origin/master is BEHIND local master and the local tip is the phase-72 BRAINSTORM squash (`git log --grep 'phase 72'`, locate by SUBJECT), `git push origin master` per `feedback_push_to_origin`; if in sync, proceed; if REJECTED (diverged), STOP and reconcile — never rebase away landed squashes.

## What THIS session does — the phase-72 SPEC (RE-DERIVE the BRAINSTORM)

One stage per session: the SPEC. **RE-DERIVE the BRAINSTORM, do NOT execute it.** Dispose the `D-CTMH-*` docket one-for-one via EXECUTED live-probe arms against `envoyproxy/envoy:contrib-v1.37.2` + a full read-only code re-derivation of every §6 edit site at the SPEC tip (a BRAINSTORM cite is NOT evidence — `feedback_brief_citations_not_evidence`; RE-DERIVE each anchor, and note the BRAINSTORM §11 already corrected seven of its own cites). Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`), controller squash-push at close (`feedback_push_to_origin`); docs-only APART from the ADR-0294 §Context append. Row 72 STAYS `in-progress`. Then roll THIS file to the phase-72 **PLAN**.

**⚠️ CRITICAL — the SPEC rolls the router to the phase-72 PLAN, NOT the IMPL.** The lifecycle is FOUR stages (BRAINSTORM → SPEC → PLAN → IMPL), one per session. Do not skip the PLAN.

**The `D-CTMH-*` docket to dispose (BRAINSTORM §7):**
- **D-CTMH-SCOPE** **[CODE]** — `HOST` MetadataKind only; `CLUSTER` + the unset kind stay reject-only.
- **D-CTMH-PROTO** **[CODE]** — the go-control-plane `CustomTag_Metadata`/`MetadataKind`/`MetadataKey` field shapes at the pin, and specifically that `GetHost()` lives on `*metadatav3.MetadataKind` (the phase-70 V1 SEVERE receiver fact — `k.GetHost()` on the bound `k := md.GetKind()`; `md.GetHost()` does NOT compile).
- **D-CTMH-WIRE / D-CTMH-ZIPKIN-WIRE** **[PROBE]** — the reference emits `{key = the literal tag name, value = the serialized string}` on the OTLP span (appended after the built-ins) and in the Zipkin `tags` map. The row ships NO Zipkin fixture, so the probe IS the Zipkin evidence.
- **D-CTMH-VALUE-SERIALIZE** **[PROBE]** — the `structpb.Value` → string table for a HOST-kind tag (anticipated the phase-70 P3 table exactly: string→raw; `NullValue`→omit; number/bool/struct/list→`protojson` + compact). `structpbValueToString` is being reused VERBATIM, so a divergence would invalidate the reuse.
- **D-CTMH-DEFAULT** **[PROBE]** — the `default_value`/omit rule when the namespace or path is absent, plus the present-empty-string edge (anticipated the `request_header` rule: present-empty EMITS `""`).
- **D-CTMH-RESOLVE-SEAM** **[CODE]** — the exact shape of the FIFTH nil-tolerant `hostMetaLookup func(ns string) (*structpb.Value, bool)` parameter on `ResolveCustomTags` (`resolve.go:32`), and where the closure is constructed: inline at the three call sites, or behind an `Endpoint`-side accessor (the `RouteMetaLookup` precedent, which also localizes the `structpb` import — the anticipated shape). Re-confirm the "18 callers untouched" claim BY EXECUTION.
- **D-CTMH-ENDPOINT-METADATA** **[CODE + PROBE] — THE LOAD-BEARING QUESTION.** *[PROBE half]* **Does the reference resolve HOST-kind metadata from a STATIC `lb_endpoints[].metadata` block, and from WHICH namespace(s) — is `envoy.lb` privileged, or is any `filter_metadata` namespace addressable?** (Anticipated: any namespace, the same `MetadataKey.key` semantics as ROUTE.) *[CODE half]* what exactly must `Endpoint` retain — the whole `*corev3.Metadata`, a `map[string]*structpb.Struct` of namespaces, or a lazily-built per-namespace `*structpb.Value`? And does widening it disturb (a) the phase-38 subset projection + `defaultSubset` (`manager.go:754`), (b) the ring_hash/maglev/health-state dial identity (all `Addr()`-keyed), (c) locality/priority grouping, or (d) per-endpoint memory at scale? Every clause must be re-derived, and the probe half EXECUTED. **ADDED OBLIGATION (BRAINSTORM §11.2, a verifier positive finding) — RE-WALK ENDPOINT PROVENANCE THROUGH BOTH CONNECTION POOLS.** This is the most plausible SILENT failure mode: if either pool reconstructed an `Endpoint` from its cached addr string, a new metadata field would arrive at the emit seam ZEROED on pool HITS and every fresh-dial test would still pass. Verified CLEAN at the BRAINSTORM tip and must be re-verified at the SPEC tip: (H1) `connection.go:717 picked := rf.Picked()` → `router.go:255 Picked()` → `router.go:592 picked = pooled.Endpoint()` → `cluster.go:106 PooledH1Conn.Endpoint()` returning `p.ep`, the whole dial-time struct copy stashed at `cluster.go:709` in `AcquireH1` (`:657`); (H2) `pooledH2Conn` (`h2pool.go:31-33`) stores NO endpoint and `AcquireH2Stream` (`h2pool.go:298`) returns the FRESH `c.lb.Pick(...)` result bound at `:313` (the `ep.Addr()` at `:317` is only the pool MAP KEY). Neither path reconstructs from the addr string.
- **D-CTMH-CONFIG-KIND** **[CODE]** — `kindMetadataHost` as a sixth `customTagKind` constant at `iota == 5` (vs a `MetaSource` sub-enum). Grep the package first — a SPEC-drafted identifier needs a collision check (`reference_spec_drafted_identifier_collision_check`); already run at the BRAINSTORM tip, `grep -rn 'kindMetadataHost' internal/` ⇒ **ZERO**, re-run anyway. **APPEND the constant AFTER `kindMetadataRoute` (⇒ `iota == 5`), never INSERT before it** — inserting silently renumbers the landed `kindMetadataRoute` value and nothing would fail loudly.
- **D-CTMH-NOHOST** **[PROBE + CODE]** — what the reference does when NO endpoint was selected (404/no-route, local reply, 500, connect failure — any path where `picked` is the zero `cluster.Endpoint{}`). Omit, `default_value`, or empty? Anticipated: treat as "namespace absent" ⇒ the default/omit rule. **UPGRADED by the BRAINSTORM verifier fold (§1.7, a MODERATE finding) — this is a MAJORITY arm, not an edge: 10 of the 18 emit call sites pass a zero-value `cluster.Endpoint{}` and only 8 carry a real `picked` (H1 is 1-of-5).** So a HOST tag silently falls to `default_value`/omit on EVERY local-reply / 500 / no-route path on all three protocols. TWO obligations, not one: **(a) [CODE]** enumerate all 18 sites with their `picked` argument at the SPEC's OWN tip and classify each real-endpoint vs no-endpoint-selected (the BRAINSTORM table is evidence, not a licence to skip re-derivation); **(b) [PROBE]** pin the reference's behavior PER CLASS with two DISCRIMINATING arms (one that selects an upstream endpoint, one that does not, same tag configured). **This is the one behavior with NO analogue in phases 70/71** and it is trivially reachable in production. NOTE the two dimensions CROSS: the zero-`picked` set is NOT the nil-lookup set (`h2dispatch.go:313` passes a real `picked` with `nil, nil` lookups; `connection.go:464`/`:597`/`:699` pass the zero endpoint with real lookups).
- **D-CTMH-REJECT** **[PROBE]** — the accept arm's structural-edge rejects (empty `metadata_key.key` / empty `path` / empty path segment — PGV parity, as phase-71 P1 established for ROUTE), PLUS one arm confirming the reference ACCEPTS a `HOST` metadata tag (`--mode validate` OK) and still BOOTS `CLUSTER`, so that reject stays a real documented DEPARTURE. Re-confirm the phase-71 S3 split wording (nil `*MetadataKind` = DEPARTURE; present-empty oneof = PARITY). `reference_pgv_forecloses_go_hazard`: probe the reference before recording any Go-derived divergence.
- **D-CTMH-FIXTURE / D-CTMH-FIXTURE-SOURCE** **[CODE]** — ONE new OTLP dir `0116-tracing-custom-tags-metadata-host` on the `0115` chassis; a static `lb_endpoints[].metadata` block as the source, NO writer; two HOST tags (static hit + `default_value`) asserted cross-side EXACT key+value; a single-endpoint cluster for a deterministic pick; a separate Zipkin dir anticipated **NO**.
- **D-CTMH-FUZZSEED** **[CODE]** — ADD a `meta_host_ok` seed to `withMetaTags` (`internal/filter/hcm/fuzz_test.go:97-115`); `meta_bad` stays on CLUSTER, **no repoint**; dispatch-verify the HCM fuzzer actually reaches the `kindMetadataHost` accept arm. +0 fuzzers (**55**).
- **D-CTMH-SPLIT** **[CODE]** — SINGLE FLAT ROW anticipated (~9–11 tasks; ADR-0045 valve armable-but-unconsumed). The only thing that could force a split is D-CTMH-ENDPOINT-METADATA turning up behavioral disturbance in `internal/cluster`.

**Probe note:** like phase 71's ROUTE arm and unlike phase 70's REQUEST arm, the HOST source is STATIC config — **NO runtime writer is needed**. The probe fixture puts `metadata: {filter_metadata: {ns: {key: "value"}}}` on a cluster's `load_assignment.endpoints[].lb_endpoints[]` entry and a `custom_tag{metadata{kind: {host: {}}, metadata_key: {key: ns, path: [{key: ...}]}}}` on the HCM `tracing` block. Probe hygiene: a FRESH container per arm (`feedback_probe_fresh_container_per_arm`), a bridge network (`reference_docker_probe_bridge_network`), the host-gateway LITERAL plus `dns_lookup_family: V4_ONLY` on `host.docker.internal` clusters (`reference_host_gateway_ip_docker_desktop`), decode-ran asserted per arm, and every arm must DISCRIMINATE between the competing hypotheses rather than merely run (`reference_probe_must_discriminate`).

**Stage-close mechanics (the SPEC):** draft **ADR-0294 §Context** per ADR-0044 — the DECISIONS tail flips **ADR-0293 → ADR-0294 AT the SPEC commit** (§Decision + §Consequences complete IN PLACE at the IMPL, no renumber; next-free after it is ADR-0295). **Row 72 STAYS `in-progress`** (it flips `done` only at the phase-72 IMPL six-gate). **NO deferred sentence is narrowed at the SPEC** (the phase-57 precedent — the `HOST` candidate rolls OUT at the IMPL row-done edit); ROADMAP otherwise UNTOUCHED. STATE §Current rolled IN PLACE (lifecycle 1 → 2; lineage re-capped at five — never prepend above §Current, the ADR-0288 rule). next-prompt.txt rolled to the phase-72 **PLAN**. Re-run the sentinel MECHANICALLY at close: (1) `NOT DONE: row 72`; (2) `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` ⇒ **3**; (3) `NEVER OPENED: gRPC/Runtime/WASM`. Does NOT fire; no `stop`. Re-run every mechanical count in the worktree (`ls -d test/fixtures/[0-9]*/ | wc -l` ⇒ 117; `grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l` ⇒ 55; `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1`). **Adversarially verify before landing** (the phase-70/71 SPEC precedent — TWO independent verifiers in PRIVATE scratch, `reference_parallel_subagents_private_scratch`).

**Candidate pool for a FUTURE roll (unchanged minus `HOST`, now chartered as row 72 — re-derive each candidate's cost at the CURRENT tip, do NOT trust these adjectives):**
- **Observability (family OPEN):** the LAST `metadata` MetadataKind — **`CLUSTER`** (MODERATE ~11–13 tasks; the natural phase-73 candidate that CLOSES the quartet; needs a metadata field + accessor on `cluster.Cluster`, a `routeAction` method + a `SetClusterMetadata`/`ClusterMetaLookup` pair RE-PAYING the 18-caller thread, and a disposition for `weightedClusterRouteAction`'s per-request in-closure selection) · `spawn_upstream_span` (LARGE) · OTel `http_service` / `sampler` / `resource_detectors` (MODERATE each) · tracing `verbose` (MODERATE) · force-trace (LARGEST) · the `ssl` handshake-outcome stat family (LARGE — ADR-0286 C3 framework surgery CONFIRMED) · **`access_log[].filter`** (NEWLY NAMED at phase 72; a LIVE behavioral divergence, flagged a strong FUTURE pick) · **`stats_flush_on_admin`** (NEWLY NAMED at phase 72) · a 6th stats sink — **UNAVAILABLE**, not merely deferred: the only unconsumed `stats_sinks[]` extension is `wasm`.
- **xDS (family OPEN):** upstream SDS (server-cert + validation_context; a boot-ORDER blocker) · CDS/EDS · LDS/RDS · ADS (muxed) · Delta xDS · RTDS/Runtime-layer · `google_grpc` transport. *(Reconnection-backoff was REJECTED at phase 72 as larger-than-it-looks — `initial_fetch_timeout` is ALREADY landed and `internal/xds/stream.go` has no loop to reconnect. Note the `grpcclient→cluster→tls` cycle constraint — `reference_xds_config_seam_transitive_cycle_guard`.)*
- **HTTP/3 (family OPEN):** `QuicProtocolOptions` tuning (SMALL but needs ~2 tasks of NEW connection-reusing harness first; timing-flaky observables) · upstream H3 cluster · alt-svc advertisement · 0-RTT/early-data · h3spec conformance gate · full QUIC transport-socket options · QUIC robustness.
- **Never-opened families:** `Runtime` (genuinely unbuilt — `internal/runtime/` = `doc.go` only) · `gRPC` (gated on an HCM response-trailer framework row — `RunDecodeTrailers`/`RunEncodeTrailers` have ZERO production callers) · `WASM` (a ROADMAP bookkeeping artifact — substantially BUILT; a docs correction, not a work row).
- **Other deferred riders:** the RBAC `require=false` fixture rider · DataSource `environment_variable` (D-ENV-HARNESS-gated) · `watched_directory`/SDS rotation.

## Read first (in order)
1. `docs/envoy-go/phases/72-tracing-custom-tags-metadata-host/BRAINSTORM.md` — the `D-CTMH-*` docket (§7), the anticipated edit sites (§6), the resolve-seam + endpoint-metadata design (§2.3, §2.5), the rejected roster (§2.1), the cost derivations + the seven self-corrected cites (§11), the stage-close mechanics (§12).
2. `docs/envoy-go/ROADMAP.md` — row 72 (registered `in-progress`) + the phase-72 charter sentence at the end of the §Observability-family heading paragraph + the live Observability `candidates:` sentence + §Schema.
3. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline) + the §Recent lineage (the phase-72 BRAINSTORM + the four phase-71 stages) + the §Project counts.
4. `docs/envoy-go/DECISIONS.md` tail — **ADR-0293** (phase 71, COMPLETE — the `metadata`/`ROUTE` precedent, the closest template for ADR-0294) + **ADR-0292** (phase 70 — the `metadata`/`REQUEST` precedent) + ADR-0044 (the §Context-at-the-SPEC discipline) + ADR-0045 (the split valve) + ADR-0080 (envoy-go-strict reject) + ADR-0106 (row-done at the IMPL six-gate) + ADR-0165 (the route-metadata accessor lineage).
5. The phase-71 SPEC (`docs/envoy-go/phases/71-tracing-custom-tags-metadata-route/SPEC.md`) for the SPEC shape + the P1–P6 probe-arm structure to mirror.
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — trust MEMORY.md's index; esp. `feedback_brief_citations_not_evidence`, `reference_quoting_is_not_executing`, `feedback_git_worktrees`, `feedback_execution_style`, `feedback_subagents_no_push`, `reference_route_metadata_resolve_full_metapath` (the FULL `MetaPath`, NOT `[1:]`), `reference_filterchain_dynamicmetadata_accessor` (verify the RECEIVER, not the method name), `reference_probe_must_discriminate`, `feedback_probe_fresh_container_per_arm`, `reference_docker_probe_bridge_network`, `reference_host_gateway_ip_docker_desktop`, `reference_deferred_candidate_cost_restale`, `reference_sentinel_deferred_sentence_live_vs_historical`, `reference_spec_drafted_identifier_collision_check`, `reference_parallel_subagents_private_scratch`, `reference_next_prompt_tracked_despite_gitignore`.
