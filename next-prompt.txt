=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, the active phase is fully landed, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action. (The sentinel does NOT apply right now — phase 50 is chartered, its BRAINSTORM + SPEC are LANDED, and the next stage is its PLAN.)

**STATUS AS OF 2026-07-05: phase 50 (`stats-sink-dogstatsd-batching`) SPEC is LANDED; the NEXT stage is its PLAN.**
- The phase-50 SPEC landed: `docs/envoy-go/phases/50-stats-sink-dogstatsd-batching/SPEC-50.md` (the 13-section shape mirroring `SPEC-49.md`; lifecycle-state 1 → 2; docs-only direct-on-master). The §10 D-DSDB-* empirical pins were EXECUTED IN-SESSION live against `envoyproxy/envoy:contrib-v1.37.2` (4 config variants: no-cap/default, `max_bytes_per_datagram: 75`, `max_bytes_per_datagram: 74`, `max_bytes_per_datagram: 120` with a deliberately long cluster name) on a dedicated Docker bridge, per `reference_docker_probe_bridge_network`; the reference was reached at the Docker-Desktop host-gateway literal IP `192.168.65.2`, re-confirming `reference_host_gateway_ip_docker_desktop`. A `general-purpose` spec-document-reviewer pass VERIFIED every line/proto citation + count EXACT against HEAD, after ONE non-blocking fold (a proto struct citation line-range off-by-4, `stats.pb.go:695-718` → `695-714`). STATE.md active-phase header rolled to `phase 50 (stats-sink-dogstatsd-batching) SPEC done`. **Re-verify with `git log --oneline -6` + `git status`; do NOT assume.**
- **The LOAD-BEARING finding RESHAPES the BRAINSTORM's phrasing into a precise, live-proven operator (do NOT re-litigate; the PLAN/IMPL EXECUTE this):**
  - **AMEND-DSDB-BOUNDARY-CONFIRMED** — the overflow comparison is INCLUSIVE: `prospective > cap` triggers a flush-before-append; a buffer that lands EXACTLY at the cap after appending still FITS. Proven both directions with a byte-exact two-line pair (`dns.cares.not_found` [32B] + `\n` + `server.hot_restart_generation` [42B] = 75B EXACTLY): co-located in ONE datagram at cap=75, split into TWO at cap=74, on every occurrence across a live capture.
  - Also PINNED (no amendment beyond confirming): **AMEND-DSDB-DEFAULT-CONFIRMED** (a no-cap run captured 1942/1942 datagrams at `nlines=1`, 100% — absent-field behavior is LIVE-confirmed reference-parity with phase 49); **AMEND-DSDB-OVERSIZED-CONFIRMED** (a ~130-char cluster name forced 715 datagrams over a 120-byte cap, up to 218 bytes, 100% `nlines=1` — never co-batched — ZERO truncation); **AMEND-DSDB-JOIN-ORDER-CONFIRMED** (zero repeated names in any datagram, zero adjacent-pair order inversions across every multi-line datagram captured).
- **A separate CODE-READING finding (re-reading the ACTUAL landed phase-49 `test/helpers/statsdrecv` code, not the phase-50 BRAINSTORM's prose description of it) corrects the BRAINSTORM's §2.8 anticipated risk — do NOT re-plan a parser rewrite:**
  - **AMEND-DSDB-RECEIVER-NO-CHANGE-NEEDED** — `test/helpers/statsdrecv.go:131`'s `ingest` ALREADY splits a received datagram's payload on `\n` before per-line parsing (`strings.Split(strings.TrimSpace(string(b)), "\n")`), traced byte-for-byte with a concrete two-line example — zero collision, zero parser change needed. The ONLY `statsdrecv` change phase 50 needs is two small additive last-seen-by-name accessors (`MaxLinesInAnyDatagram`/`LinesInDatagram`, SPEC §8.2) so the `0094` differential can PROVE batching occurred and PROVE the oversized line stayed alone.
  - **D-DSDB-FUZZER resolved (code-reading, not a probe):** NO new fuzzer — `FuzzDogStatsdSinkConfigParse`'s existing seed corpus already exercises `max_bytes_per_datagram` through the full `bootstrap.Load` parse path; the packing algorithm consumes trusted, already-formatted inputs, not a new parse boundary. Fuzzers stay **52**.
- Counts at SPEC (re-verify, do NOT assume): stat surface **1200** (non-H2 **1196**) / fixtures **95** / fuzzers **52** / BackendKind **38** / DECISIONS **ADR-0266** (next-free **ADR-0267**).

=> The NEXT action is the **phase 50 (`stats-sink-dogstatsd-batching`) PLAN** (lifecycle-state 2 → 3). NOT the termination sentinel. NOT a new SPEC.

==============================================

## The NEXT action — the phase 50 (`stats-sink-dogstatsd-batching`) PLAN
Author `docs/envoy-go/phases/50-stats-sink-dogstatsd-batching/PLAN-50.md` (a TDD task spine mirroring `PLAN-49.md`'s 9-task shape, anticipated ~7-10 tasks — FEWER than phase 49's per SPEC §3.0, since NO new `Sink`/tag/delta logic and NO parser rewrite are needed this row). Docs-only direct-on-master (the family-row PLAN precedent — no worktree needed for the PLAN document itself; the IMPL that follows uses a fresh worktree). Lifecycle-state 2 → 3.

**The PLAN's job — RESOLVE every SPEC §11 D-DSDB-* PLAN question:**
- **D-DSDB-SPLIT** — the FINAL ADR-0045 re-check with real anticipated LoC (a single flat row, SMALLER than phase 49, is expected; SPEC §3.0).
- **D-DSDB-CAP** — the EXACT `0094` fixture cap value + `backendName` length that (a) comfortably batches the short HCM-tagged subset lines together and (b) comfortably makes the cluster-tagged subset lines exceed the cap alone. SPEC §8.1 proposes cap=200 / backendName ~150-200 chars as a starting point — the PLAN (or IMPL, empirically) confirms the exact byte math against the ACTUAL envoy-go + reference line formats.
- **D-DSDB-STATS-FINAL** — the exact name/shape of the +0 registration guard test extension (mirroring `TestNoNewStat_DogStatsdRegistrationGuard`; SPEC §7).
- **D-DSDB-FUZZER-SEED** — whether the PLAN adds an extra fuzz seed (e.g. a boundary-adjacent or very-large `max_bytes_per_datagram` value) for additional mutation coverage beyond the already-sufficient existing seed (SPEC §6, a nicety not a requirement).
- **D-DSDB-BUFFER-TYPE** — whether `appendLine`/`flush` use `strings.Builder` (SPEC §3.3's sketch, matching `formatTagSuffix`'s existing use) or a `[]byte`/`bytes.Buffer` — a PLAN-level implementation-ergonomics choice with no behavioral difference.

**The task spine (per SPEC §3/§8 — the PLAN decomposes into concrete TDD tasks):** PROGRESS + baselines + the ADR-0045 re-check (D-DSDB-SPLIT) → delete the `bootstrap.go:591-593` strict-reject + add `DogStatsdSinkConfig.MaxBytesPerDatagram uint64` + thread `.GetMaxBytesPerDatagram().GetValue()` into the `DogStatsdSinkConfig{...}` literal (TDD) → the `internal/statssink/dogstatsd.go` `appendLine`/`flush` buffer-accumulate-then-flush-on-overflow rewrite of `Submit`'s `s.write(line)` call site (the `maxBytesPerDatagram` field + threading through `NewDogStatsdSink`; TDD table-driven over a fake UDP listener covering: cap=0/absent [one-per-datagram], an exact-boundary co-locate, an oversized-single-line-alone case, a multi-line-preserves-order case; full-package `-race`) → `main.go:222`'s third parameter thread (byte-stability guard when no cap is set) → the `test/helpers/statsdrecv` additive `MaxLinesInAnyDatagram`/`LinesInDatagram` accessors (D-DSDB-CAP verified against real line lengths) → the `0094-stats-sink-dogstatsd-batching` cross-side fixture (clone `0093`'s driver; distinct prefix; a long `backendName`; reuse `0093`'s delta-SUM/tag-set/gauge subset assertions verbatim + the two NEW batching proofs [`MaxLinesInAnyDatagram() > 1`, `LinesInDatagram(oversizedName) == 1`]) → the deliberate breaks ((a) force one-line-per-datagram regardless of cap — must fail the batching-occurred assertion; (b) silently drop an overflow line instead of flushing-then-appending — must undercount the delta-SUM) + 20/20 flake + FULL-package `-race` (`internal/statssink` + `internal/stats`) → the registration test (1200 → 1200, +0) → the full 96-dir differential + six-gate → ADR-0267 body + BEHAVIOR_CONTRACT + STATE/ROADMAP (row 50 `done`) → the fuzzer-count reconcile (confirm 52 UNCHANGED, per D-DSDB-FUZZER).

**After the PLAN lands:** dispatch a `plan-document-reviewer` (general-purpose) pass — verify EVERY `bootstrap.go`/`main.go`/`internal/statssink`/`test/helpers/statsdrecv` line citation EXACT against HEAD (the 47.x/48/49 precedent), baselines confirmed, the five high-risk areas covered (esp. the boundary-operator unit tests, the `0094` cap/backendName byte math, the deliberate-break design, the byte-stability guard for the no-cap case). Fold any NON-BLOCKING findings; re-litigate only BLOCKING ones. Then ROLL THIS FILE FORWARD to point at the phase-50 IMPL (mirror the phase-49 PLAN→IMPL router in git history — subagent-driven in a fresh worktree; FLIPS row 50 `done`; ANCHORS ADR-0267 §Decision/§Consequences).

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** The next stage is the phase-50 PLAN (author `PLAN-50.md`; docs-only direct-on-master). Lifecycle-state 2 → 3. After it, a `plan-document-reviewer` pass (the 47.x/48/49 precedent).
- **Subagent-driven execution** (`feedback_execution_style`) in a **fresh worktree** (`feedback_git_worktrees`) for the IMPL that follows the PLAN; subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit, re-runs the full suite on the frozen HEAD, does the deliberate-break verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). (BRAINSTORM/SPEC/PLAN are docs-only direct-on-master per the family-row precedent — no worktree needed.) Per-task gates: `gofmt -l` + `golangci-lint` on touched pkgs + `go vet` + `go build ./...` (`feedback_pertask_gofmt_lint`).
- **Process anchors:** ADR-0106 (per-leg rows) · ADR-0044 (ADR §Context-at-SPEC / §Decision+§Consequences-at-IMPL) · ADR-0045 (sub-split soft gate — escape-valve unconsumed, re-confirmed at PLAN) · ADR-0080 (strict-reject; NOTE this row REMOVES a prior strict-reject, the reverse direction) · ADR-0266 (the phase-49 dog_statsd sink this row extends) · next-free **ADR-0267**.
- **Differential discipline** (carries to `0094`, and to re-running `0093` unaffected in the full suite): `-run 'TestDifferential/0094'` NEVER bare (`reference_differential_run_selector`); `-count=1` on every break + `-race` (`reference_differential_break_protocol_count1`); the full-suite `subject ready: EOF` startup flake is isolatable (`reference_differential_fullsuite_startup_flake`); the Docker bridge + decode-ran proof (`reference_docker_probe_bridge_network`); the driver-owned UDP receiver EXTENSION is NOT a new BackendKind (`reference_differential_grpc_receiver_driver_owned`); the live reference is the wire truth (`reference_wire_format_both_sides_see_same_bytes`); periodic-sink two-receivers + hard-Close (`reference_periodic_sink_differential_two_receivers`); the delta-sink stability barrier (`reference_delta_sink_differential_stability_barrier`); reuse the `0093` driver's local `hostGatewayIP` helper verbatim (`reference_host_gateway_ip_docker_desktop`); DogStatsd tag order stays unsorted (`reference_dogstatsd_tag_order_unsorted`); the admin-interface wire-name collision (`reference_admin_interface_wire_name_collision`) — `0094` reuses `DeltaSumTagged`, unaffected by batching; `reference_full_suite_race_after_background_mutator`.

## Read first (in order)
1. THIS FILE — the cold-start router (phase 50 `stats-sink-dogstatsd-batching` SPEC LANDED; the next action is its PLAN).
2. `docs/envoy-go/phases/50-stats-sink-dogstatsd-batching/SPEC-50.md` — the frozen design + the §10 D-DSDB-* live-probe pins (D-DSDB-BOUNDARY LOAD-BEARING) + the §11 PLAN/IMPL D-questions the PLAN must resolve + the ADR-0267 §Context draft (§12).
3. `docs/envoy-go/phases/50-stats-sink-dogstatsd-batching/BRAINSTORM.md` — the frozen scope (single flat row / transport-layer-only / the 4-question user dialogue).
4. `docs/envoy-go/STATE.md` — the active-phase header (`phase 50 (stats-sink-dogstatsd-batching) SPEC done`) + the recorded NEXT (`phase 50 PLAN`) + the counts (1200 / 95 / 52 / 38 / ADR-0266).
5. `docs/envoy-go/ROADMAP.md` — row 50 `in-progress` (the table row + the Observability family note); the family STAYS OPEN.
6. `docs/envoy-go/phases/49-stats-sink-dogstatsd/PLAN-49.md` — the 9-task TDD spine shape to mirror + the phase-49 dog_statsd substrate this row extends.
7. `internal/statssink/dogstatsd.go` (`Submit` at `:72-103`, the `s.write(line)` call site at `:100`) + `internal/bootstrap/bootstrap.go` (`parseDogStatsdSinkConfig` at `:586-607`, the strict-reject at `:591-593`, `DogStatsdSinkConfig` at `:301-304`) + `cmd/envoy-go/main.go` (`:222`, the `NewDogStatsdSink` call site) + `test/helpers/statsdrecv/statsdrecv.go` (`ingest` at `:131`, the `Tags`/`tags` last-seen precedent at `:49`/`:189-194`) + `test/fixtures/0093-stats-sink-dogstatsd/driver/driver.go` (the differential template to clone) — the EXACT seams the PLAN cites; re-read fresh (line numbers drift), do not trust citations from the SPEC without re-verifying against HEAD.
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `reference_periodic_sink_differential_two_receivers`, `reference_delta_sink_differential_stability_barrier`, `reference_differential_grpc_receiver_driver_owned`, `reference_docker_probe_bridge_network`, `reference_host_gateway_ip_docker_desktop`, `reference_dogstatsd_tag_order_unsorted`, `reference_admin_interface_wire_name_collision`, `reference_membership_total_vs_healthy_gauge`, plus the differential-discipline references above).
