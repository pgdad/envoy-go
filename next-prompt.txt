=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **THERE IS *NO* BANKED MID-LIFECYCLE WORK AT THIS TIP — SO THIS SESSION SELF-PICKS.** Phase 84 (`grpc-unary-response-trailers`) is **CLOSED**: the SPEC split the row 84.1 / 84.2, the 84.1 IMPL landed the FIVE-file seam 2026-08-08, and ⚠️ **the 84.2 IMPL landed fixture `0119-grpc-unary-trailers` and FLIPPED ROADMAP row 84 `done` on 2026-08-08.** Every chartered row now reads `done`. ⇒ **This session performs a BRAINSTORM on a subject IT PICKS ITSELF** — smallest defensible candidate first, with **the pick AND the rejected alternatives recorded in the BRAINSTORM**. Two candidates are already in writing and neither is chartered:
- **the six family backlog paragraphs** — the check-(2) lines of `docs/envoy-go/ROADMAP.md` at `:194 :200 :206 :216 :222 :230`, which are the **SOLE remaining sentinel blocker**; and
- **the h2spec-selector repair row** (SPEC-84 §13 item 1, which calls it *"one coherent future row"*): **NINE** malformed slash-form section-6 selectors that match **ZERO** cases (h2spec addresses sections with DOTTED numbers; an unmatched positional argument is a silent no-op), **42** unrun RFC 9113 §6 cases, **4** pre-existing envoy-go-specific failures on the correct `http2/6` selector, and the missing `Tests == 0` guard at `h2spec_test.go:310` — plus the fact that the gate is not run in CI at all.

⚠️ **THIS IS THE CANDIDATE LIST, NOT THE PICK.** Re-derive the cost of anything you shortlist AT YOUR OWN TIP (`reference_deferred_candidate_cost_restale`), and record the alternatives you rejected and why.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=116 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **CHECKS (1) AND (3) ARE *BOTH* SILENT AT THIS TIP AND ONLY CHECK (2) PRINTS — the sentinel STILL does not fire (the condition is a CONJUNCTION and (2) prints SIX), and `stop` must NOT be created.** ⚠️ **WITH TWO SILENT CHECKS THE DOCTORING NCs ARE THE *ONLY* THING DISTINGUISHING SUCCESS FROM A BROKEN CHECK — BOTH ARE MANDATORY EVERY SESSION:** the row-doctoring NC below (a `sed` targeting `done      ` does NOT work — it misses the real `| done |` column spacing and prints nothing, which reads exactly like *"the check is blind"*), and the check-(3) doctoring NC:
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
```
```sh
# the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```

⚠️ **AND WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -oiE '-family row'` fails with `grep: amily row: No such file or directory` and the surrounding arithmetic prints `base=0 now=0 delta=0`, **which reads exactly like "no change".** Observed live at FOUR phase-84 stages. **Always pass `--` before the pattern**, and remember the counts are form-dependent: **95 occurrences vs 67 LINES** at this tip (`grep -c` counts LINES). **A gate that reads zero on BOTH sides is not evidence of invariance.**

⚠️ **`want=116` IS A LIVE FIGURE, NOT A CONSTANT — AND A CHARTERING BRAINSTORM *DOES* CHANGE IT.** The 84.2 IMPL's row flip was an UPDATE-IN-PLACE of row 84's status cell (`in-progress` -> `done`), **ONE line changed, `git diff --numstat` = `1 1`, no row added or removed, so `want` STAYED 116.** ⚠️ **YOUR STAGE IS DIFFERENT: a BRAINSTORM that charters a row ADDS a data row, so it MUST bump `want` 116 -> 117 IN THE SAME COMMIT AS THE ROW.** This holds **whether or not it opens a NEW family** — a row chartered inside an EXISTING family adds a row too. Precedent: the phase-84 BRAINSTORM bumped **115 -> 116** in one commit; the phase-83 BRAINSTORM **114 -> 115**; the phase-82 BRAINSTORM **113 -> 114**. ⚠️ **THE LEAK CHECK GOES LIVE THE MOMENT YOU TOUCH `ROADMAP.md` — run it as a WHOLE-FILE BEFORE/AFTER COUNT, never a grep of the diff's `+` lines.** Baselines re-derived at the 84.2 IMPL close: check-(2) union **6** · `-family row` **95 occurrences / 67 LINES** · `gRPC-family row` **2** · **234 lines / 116 data rows**. ⚠️ **NEVER WRITE A SENTINEL'S OWN MATCHER STRING INTO `ROADMAP.md`** — `done` prose naming the deferred-candidate phrases MANUFACTURES a check-(2) hit; a `-family row` phrase inflates check (3)'s pass census. **The two classes are OPPOSITE: one silences a real failure, the other manufactures one.** Writing those strings *here* or in `SPEC.md`/`PLAN-*.md`/`STATE.md` is safe; writing them *there* is not. ⚠️ **If your BRAINSTORM opens a NEW family, add its row-summary slug to check (3)'s list AND expect check (3) to go from silent to printing until the row lands.**

Check (3) fails SAFE by construction. When you open a new family, add its row-summary slug to the list above.

**SENTINEL RE-RUN MECHANICALLY AT THE 84.2 IMPL CLOSE (2026-08-08, tip `ff71318d` plus this docs-only stage-close commit, which leaves `ROADMAP.md` BYTE-UNTOUCHED). IT DOES NOT FIRE; `stop` was NOT created** (`ls stop` => `No such file or directory`, checked at the repo root AND the worktree root). Input measured **234 lines / 116 data rows** BEFORE anything was written. ACTUAL output, recorded rather than predicted:
- **(1) SILENT — FOR THE FIRST TIME IN THE PROJECT'S HISTORY.** All 116 data rows read `done`; the denominator assertion printed nothing, i.e. it examined exactly 116. ⚠️ **A silent check is indistinguishable from a broken check without its NC. Run the NC.**
- **(2) SIX — `:194 :200 :206 :216 :222 :230`.** ⚠️ **Do NOT "fix" it and do NOT forecast a decrease — it has NEVER gone down across ~40 phases** (history `0 -> 1 -> 3 -> 4 -> 5 -> 6`): the candidates sentences are a WINDOW onto a larger deferred backlog, not an inventory of it. **This is now the SOLE sentinel blocker.**
- **(3) SILENT.** **This is the state that makes the check-(3) doctoring NC mandatory.**
- **FOUR NCs, ALL FIRED:** row 62 doctored ⇒ `NOT DONE: row 62` (with `NC LANDED? [ in-progress ]` inspected first — and note that with row 84 now `done` this NC prints **row 62 ALONE**, where at every prior stage it printed 62 **and** 84); `want=115` ⇒ `GATE FAIL: examined 116 data rows, expected 115`; the check-(3) doctoring (residual occurrences confirmed **0** first) ⇒ `NEVER OPENED: gRPC` while `WASM` correctly stays silent; the check-(2) **one-arm** strip ⇒ **6 -> 5** when the SHORT arm is stripped and **6 -> 1** when the LONG arm is stripped, **never 6 -> 0** (the long form does not contain the short substring — a one-arm strip is NOT an NC for the union).
- ⚠️ **THE OUTPUT ABOVE WAS PRODUCED BY THE CODE BLOCKS IN *THIS* FILE, EXTRACTED PROGRAMMATICALLY AND EXECUTED** — not transcribed from a prior stage and not re-typed. A router that records a result its own blocks cannot reproduce is worse than one that records nothing.
- ⚠️ **Re-run all three checks and both NCs yourself and record what YOU see — both BEFORE and AFTER any `ROADMAP.md` edit.**

**RE-EVALUATED MECHANICALLY ON EXPLICIT HUMAN INSTRUCTION 2026-08-08 (post-84.2-IMPL, at tip `28749069` == `origin/master`). IT DOES NOT FIRE; `stop` was NOT created (`ls stop` => `No such file or directory`).** ACTUAL output, recorded not predicted: input measured **234 lines / 116 data rows** first; **(1) SILENT** at `want=116` — all 116 rows `done`, the denominator assertion printed nothing; **(2) SIX — `:194 :200 :206 :216 :222 :230`**; **(3) SILENT**. The condition is a CONJUNCTION and (2) prints, so the sentinel does NOT fire — the six family backlog paragraphs are the SOLE remaining blocker, and `STATE.md`'s `next-skill:` reads `BRAINSTORM — SELF-PICK`, i.e. a next stage EXISTS. All FOUR NCs fired: row-62 doctoring => `NOT DONE: row 62` ALONE (row 84 correctly absent now that it is `done`), with `NC LANDED? [ in-progress ]` inspected first; `want=115` => `GATE FAIL: examined 116 data rows, expected 115`; check-(3) doctoring (residual `gRPC-family row` confirmed **0** first) => `NEVER OPENED: gRPC` while `WASM` correctly stayed silent; check-(2) one-arm strips => **6 -> 5** (short arm) / **6 -> 1** (long arm), never 6 -> 0. This file was already rolled to the **SELF-PICK BRAINSTORM** at the 84.2 IMPL close and STAYS there — that is the next actionable stage. `want` STAYS **116** until a chartering BRAINSTORM adds its row; `ROADMAP.md` BYTE-UNTOUCHED by this re-evaluation.

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (ugrep under the hood). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Reproduced at SIXTEEN separate tips.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **THE METHOD BINDS; carry no standing discrepancy figure and no standing absolute** — the phase-84 SPEC's own commit falsified the standing example within one commit. ⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS; print FILE NAMES, never a count.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files; use `-v --no-index`, and `git status` is the discriminating check). ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **`grep -c` ON ZERO MATCHES IS TOOL-SPECIFIC** (only `git grep -c` prints nothing; all exit 1 — capture the value, never chain on the exit code). ⚠️ **`grep -c` COUNTS *LINES*, NOT *OCCURRENCES*.** ⚠️ **`\t` IN GNU ERE IS A LITERAL `t`** — use `-P`. ⚠️ **A `git grep` PATHSPEC ENDING IN `/` READS A FAIL-UNSAFE ZERO** (shape 33) — use the shell-glob form. ⚠️ **AN UNANCHORED PANIC-GREP FALSE-FIRES 14 TIMES ON A FULLY-GREEN DIFFERENTIAL LOG** (a lua "panic recovered" log line, wasm rust-panic symbol names, `0097-lb-panic-threshold` fixture names) — **the canonical gate is ANCHORED: `^panic:|DATA RACE|SIGSEGV`. A gate that fires on a green log is worse than no gate.**

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`** with FOUR blind spots (an em-dash, a DOT in `http-1.1`, two LETTER-suffixed ids). ⚠️ **EXECUTED on scratch copies at the phase-77 PLAN: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** ⚠️ **DO NOT restore the regex form.** ⚠️ **AND THE STAKES JUST ROSE: check (1) is now SILENT in the healthy state, so a silently-broken check (1) is invisible except through its NC.**

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

⚠️ **Run the union form, never one half of it.** ⚠️ **A ONE-ARM STRIP IS NOT AN NC** (6 -> 5 on the short arm, 6 -> 1 on the long arm; never 6 -> 0). ⚠️ **The documented trap's stated MECHANISM is refuted while the trap is real** — the inflation comes from ROADMAP table rows carrying BRAINSTORM prose. **Cite the command; do not re-inherit the explanation.** ⚠️ **`:213` IS NOT A SENTENCE ANCHOR** (a very long line carrying SEVERAL deferred phrases; only ONE is the colon-terminated form) **and the line numbers drift with edits — re-derive them, the current six are `:194 :200 :206 :216 :222 :230`.** ⚠️ **These six paragraphs are now the ONLY thing between this project and the termination sentinel, which makes them a legitimate BRAINSTORM subject — and also makes any edit near them a sentinel-affecting edit.**


==============================================


## STATUS AS OF 2026-08-08: PHASE 84 (`grpc-unary-response-trailers`) IS **CLOSED**. ROW 84 IS **`done`** (flipped at the IMPL-84.2). THERE IS **NO BANKED MID-LIFECYCLE WORK**; **THIS SESSION SELF-PICKS A SUBJECT AND RUNS ITS BRAINSTORM.**

**What the 84.2 IMPL landed (2026-08-08, base `35e3933e`, branch `phase-84-impl-84.2`).** Fixture `test/fixtures/0119-grpc-unary-trailers/` NEW — `driver/driver.go` **669** lines, `driver/driver_test.go` **196**, `README.md` **137**, `expectations.yaml` **106** (both documentary), 0079's three PEMs copied into `pki/` with **sha256 asserted pair-by-pair**; registration = **ONE blank-import line** at `test/differential/runner_test.go:146`; `ROADMAP.md` row 84 `in-progress` -> **`done`** (ONE line, numstat `1 1`, `want` STAYS 116). ⚠️ **`DECISIONS.md` and `BEHAVIOR_CONTRACT.md` BYTE-UNTOUCHED** (`git diff <base>..HEAD -- …` = 0 bytes, against the `ROADMAP.md` `1 1` control on the same command form) — the fixture surfaced ZERO new contract statements; **no ADR consumed, next-free stays `ADR-0307`**. **ZERO production `.go`.** TWELVE tasks, subagent-driven.

### ⚠️ WHAT THE 84.2 IMPL PROVED — CARRY THESE, THEY ARE MEASUREMENTS

- ⚠️ **THE FOURTH ARM IS THE ONLY REASON THE ROW'S HIGHEST-LEVERAGE DECISION IS MEASURED.** The arms are `success` / `notfound` / `unimplemented` / **`plain`** (a trailer-less non-gRPC H2 GET through the same listener — the D-84-ENDSTREAM gate). Break **F2** (variant B, unconditional END_STREAM, with the plain arm DISABLED) went **vacuously GREEN**; **F2′** with the arm restored went **RED at offset 940**. **The PLAN's blindness finding was REPRODUCED, not "fixed".**
- **Break/control roster re-derived at tip `b637f656`, all six arms:** **F1** drop-the-emit (`h2dispatch.go:751`) **RED @ offset 192** — only `CompareBytes` fired; subject transcript **0** `TRAILERS` lines vs reference **3**, measured · **F3** drop-the-carrier (`router_h2.go:250`) **RED @ 192, BYTE-IDENTICAL to F1** — the fixture localises the SYMPTOM, not the LAYER (84.1's unit arms own layer localisation) · **F4** SYMMETRIC (same bogus line in BOTH transcripts) **PASS** · **F5** VACUITY: `AssertStats` **GREEN** (`downstream_rq_2xx` = **4** measured under F1 via a nested NC) while `CompareBytes` **RED** — **broken-gate shape 31 LIVE on this fixture** · **F6** registration removed ⇒ `no driver registered` + **SKIP + exit 0 in 0.85 s**. All restores sha256-verified against base blobs; `-count=1` throughout; the FIRING assertion confirmed per arm.
- ⚠️ **PATH CORRECTION — RECORD IT SO A FUTURE RE-DERIVATION DOES NOT CONCLUDE THE SITE VANISHED: the F3 populate site is `internal/filter/http/router/router_h2.go:250`** (`Trailers: resp.Trailers,`). A task brief mis-inferred `internal/filter/hcm/`, and the PLAN's own cite was **path-less**, so nothing in writing contradicted the wrong inference. Re-verified at this tip.
- ⚠️ **THE `AssertStats` LEG WAS STRENGTHENED AGAINST THE PLAN — a RECORDED review-driven deviation** (task-4/5 review finding I-1): subject `http.ingress_http.downstream_rq_2xx >= len(arms())` = **4**, not the chartered `>= 1`. Reason: **READ-ERR is recorded INTO the transcript**, so a failure hitting BOTH sides identically yields byte-equal transcripts and `CompareBytes` passes — a vacuous green. The `>= 4` floor is the committed liveness guard.
- **Canonicalization stayed CLOSED at THREE — ZERO new rules forced.** The two mandated name-drops (`x-envoy-upstream-service-time`, `date`) plus a never-fired READ-ERR addr scrub absorb every divergence **including wire order**; **the sort stays vacuous and NONE was written.** The PLAN's zero-iteration claim REPRODUCED at the IMPL tip.
- **The driver drives raw `x/net/http2` Framer + hpack through a shared `drive()`** — **first-of-kind among all fixture drivers**, permitted by the harness comment at `runner_test.go:3311`; `helpers.H2RoundTrip` structurally cannot carry trailers or frame shape.

### ⚠️ THE GATES — RE-RUN AT THE 84.2 IMPL, BY EXECUTION

Full **121-fixture** differential, two launches: **launch 1 → 120/121** with a NEW flake (below); **launch 2 → 121/121 PASS, `INNER_EXIT=0`, 0 FAIL/SKIP, 0 `no driver registered`, anchored panic gate 0, wall 391.189 s** (`0119` PASS on BOTH). `go test ./... -count=1`: run 1 `INNER_EXIT=1` on a NEW flake, run 2 **`INNER_EXIT=0`**. `-race` on `hcm`, `hcm/h2`, `http/router` and the `0119` driver: **`RACE_EXIT=0`**. `go vet ./...` 0 · `gofmt -l` empty · `golangci-lint` 0.
```sh
( go test ./test/differential/ -count=1 -v > "$SCRATCH/full.log" 2>&1; echo "INNER_EXIT=$?" )
grep -cE '^    --- PASS: TestDifferential/' "$SCRATCH/full.log"          # want 121 after 0119 lands (120 before)
grep -E  '^    --- (FAIL|SKIP): TestDifferential/' "$SCRATCH/full.log"   # want EMPTY
grep -c  'no driver registered for fixture' "$SCRATCH/full.log"          # want 0
grep -cE '^panic:|DATA RACE|SIGSEGV' "$SCRATCH/full.log"                 # want 0 -- ANCHORED; unanchored false-fires 14x on a GREEN log
```
⚠️ **`-run` selectors, `...` buffering, and the `./...` concurrency caveat all still apply:** a `-run` selector matching NOTHING prints `[no tests to run]` and **EXITS 0** — assert the `=== RUN` line for any fixture you scope to. `go test ./test/differential/...` (with `...`) matches TWO packages and BUFFERS `-v`. `go test ./... -count=1` runs the differential CONCURRENTLY with the other packages — a WORSE environment for the port race; cite it honestly, both ways. Scoped `0119` runs cost **~2.1-2.2 s**; break-arm iteration is cheap, full launches are not (**budget ~3 launches**). The faithful fixture-dir predicate is `^[0-9]{4}[a-z]?-`, and in code it is a hand-rolled predicate at `runner_test.go:1461-1498`, not a regex.

⚠️ **THE FLAKE PICTURE CHANGED AT THIS ROW — TWO NEW ONES, NEITHER IN A DOCUMENTED CLASS, BOTH CLEARED ON RE-RUN. A RECURRENCE OF EITHER IS WORTH RECORDING:**
1. **`0079-h2-multiplex-pool`**, differential launch 1 — `runner_test.go:1350: subject: ceil[0]: status 502, want 200 (held stream not served after drain)` (same for `ceil[2]`). ⚠️ **NOT the driver-receiver port-race class** — no panic, no `bind: address already in use`, not `0081`/`0084`. Did not reproduce on launch 2.
2. **`internal/wasm TestA4_HttpCallResponseTrap_PoisonsStreamContext`**, full `go test ./...` run 1 — `sc.trapped = false after proxy_on_http_call_response TRAPPED; want true`. **Passes standalone** (0.003 s) and on the clean re-run.

⚠️ **KNOWN PRE-EXISTING FLAKE, still live:** the driver-receiver port race (behavioural roster **42**). ⚠️ **`0081`'s ALS receiver binds `0.0.0.0:42039`, INSIDE the `42000-46099` agent band — never park a private listener there while the differential runs.** **Check `git worktree list` + `ps` for siblings BEFORE blaming your own row for a port flake.**

**h2spec, standing:** `http2/6/10` is a silent no-op (exit 0, zero cases); the correct `http2/6` yields 42 tests / 4 pre-existing failures; **NO ROW MAY CITE "h2spec 53/53" AS FRAME-LEVEL EVIDENCE** — 42 strict cases (44% of declared scope) silently unrun since 2026-04-25; the gate is not in CI. **NINE** slash-form selectors (not ten). `assertThreshold`'s `Tests == 0` skip is at `h2spec_test.go:310` (re-verified at this tip). ⚠️ **This whole paragraph IS candidate (ii) above — if you pick it, every figure here is a starting hypothesis to re-measure, not a finding to inherit.**

### ⚠️ THE SIX-GATE STANDING POSTURE — NAME THE DEPARTURES, DO NOT CLAIM COMPLIANCE

**(a)** — the 84.2 leg WAS gate (a): `0119` raised the downstream-ALPN-h2 fixture count **3 -> 4** and is the only fixture asserting frame-level `end_stream` placement cross-side. **A docs-only BRAINSTORM owes (a)/(b) only as a POSTURE statement** — zero `.go` changed means not-exercised, not green. **(b)** — the suite is now **121** fixtures. **(c)** — `test/conformance/grpc/` **DEFERRED IN WRITING** (SPEC-84 §4's two grounds; `ROADMAP.md` carries it); proxy-wasm **10 of 16 families (62.5%)**; h2spec stated WITH the scope caveat. **(d)** — say **"vacuous"**, not "green", when nothing introduces a parser/codec/filter; fuzzers **55** in **48** files (`-- '*.go'`-scoped), ⚠️ all under `internal/`, ZERO under `test/` (the §7.4-location violation stays named). **(e)** — `INNER_EXIT` mandatory on every differential launch AND on `go test ./...`. **(f)** — ⚠️ **STANDING DEPARTURE, named not claimed**: no `REVIEW.md`; **37 of 125** phase dirs carry one, none since 25.3. Normative cites: §6.1 `:285`, §6.2 `:294`, §7.3 `:346-351`, §7.5 `:357`, gates `:360-365`. ⚠️ **`/BOOTSTRAP_PROMPT.md` WITH A LEADING SLASH RESOLVES NOWHERE** — the file is `<repo>/BOOTSTRAP_PROMPT.md` (522 lines); the 1024-line file under `docs/superpowers/plans/` is the GENERATOR PLAN, content-divergent with a NON-constant offset.

**STAT SURFACE — the 84.2 IMPL landed +0**, discharged by **call-site enumeration — 208 code sites / 36 production files (cite 208/36, NEVER 208/84)**, re-derived at BOTH base and tip with **delta 0/0**. ⚠️ **A test-only leg makes ARM-1 (added production registration sites) EMPTY-BY-CONSTRUCTION — the input measure is therefore MANDATORY**: at this row ARM-1's production-scoped diff measured **0** while the raw diff measured **675** lines, all 8 changed files under `test/`, and the NC (one registration line injected into a scratch clone of the pipeline) **fired 6**. ⚠️ **NEVER via `TestNoNewStat*`** — proven blind BY EXECUTION at the 84.1 PLAN (a compiling registration inside `writeH2Reply` left all five guards PASS); neither run nor cited at 84.2. The absolute is **1207, DOC-SOURCED from `BEHAVIOR_CONTRACT.md`'s ledger tail `:5118`** — never `STATE.md` §Project's stale 1205. **ONLY THE DELTA IS ASSERTED.**

### ⚠️ ADR + DOCUMENT CONVENTIONS

**The DECISIONS tail is `ADR-0306`, STATUS `COMPLETE`. Next-free is `ADR-0307` — DERIVE FROM THE TAIL** (headings+1 COLLIDES at the single gap, ADR-0209: it yields 0306). ⚠️ **The 84.2 IMPL consumed NO ADR and left `DECISIONS.md` BYTE-UNTOUCHED.** ⚠️ **Do NOT use the loose form `^> \*\*STATUS: .*PROPOSED`** — every hit is a false positive; the strict guard reads **0** at this tip and that is CORRECT (ADR-0306 completed in place at the 84.1 IMPL, disarming it 1 -> 0). **Carry no whole-file count of the loose matcher.**

### ⚠️ COUNTS AT THIS TIP — RE-DERIVE MECHANICALLY, NEVER COPY

**HOUSE RULE: every number below is a LIVE FIGURE. Re-run it in your own worktree with its negative control observed. Numbers in this file have gone stale WITHIN a single commit before.**

- **differential fixtures 121** — numeric tail **`0119-grpc-unary-trailers`**, **next-free `0120`**. ⚠️ **Its reference port under the `10<index>` convention would be `10120` — but TWO reference-port conventions COEXIST** (`10<index>` for 0103+, e.g. `0118`/`0119`; sequential 19xxx for the 0040-0102 era, e.g. 0068=19157, 0079=19168). **STATE THE CONVENTION YOU USE; DO NOT INVENT A RULE.** Port occupancy is **146 distinct ports across 19 bands, range 10000-19172**; collisions are documentation-only (`ReferenceListenerPort` is container-internal, the runner dials the mapped port, `t.Parallel` = 0).
- **full-suite wall `391.2 s`** at this tip — inside the previously observed **389-404 s** band, not a new regime. **Scoped `0119` ~2.1-2.2 s.**
- **`ROADMAP.md` 234 lines / 116 data rows**; check-(2) union **6**; **`-family row` 95 occurrences / 67 LINES**; **`gRPC-family row` 2**. Row 84 is at `:146`.
- **`AssertStats` carriers 85 of 121** and **`driver/driver_test.go` carriers 33 of 121** — ⚠️ **BOTH ARE MATCHER-DEPENDENT; STATE YOUR MATCHER.** (`AssertStats` counted as fixture dirs containing a `func …AssertStats`; the test-file census counted as `driver/driver_test.go` specifically — an any-`*_test.go` form gives **35**.) Layout censuses, same caveat: `driver/` **97**/121 · `README.md` **119**/121 · `expectations.yaml` **97**/121.
- **`DECISIONS.md` 17990** lines · **305** `^## ADR-` headings · tail **`ADR-0306` COMPLETE** · next-free **`ADR-0307` FROM THE TAIL** · `^---$` **216** · STATUS census **19** (matcher `^> \*\*STATUS: `, a BLOCKQUOTE).
- **`BEHAVIOR_CONTRACT.md` 5955** · **`STATE.md` 64** · **`STATE_HISTORY.md` 466** (`prior active-phase` bullets **183**) · phase-84 **`PROGRESS.md` 323**.
- **stat call sites 208 / 36 production files** · **fuzzers 55** in **48** files · **phase dirs 125**, `REVIEW.md` **37** · **BackendKind tail 38** over **39** declarations (do NOT "fix").
- ⚠️ **STILL CONTESTED, NO NUMBER CARRIED:** the `STATE_HISTORY.md` archive-gap total · production `stats.IsValidName` guard sites · the `ROADMAP.md:<line>` cite count · `allCallbacksNoOp` occurrences · the loose `PROPOSED` matcher's whole-file count · whether 84.1's realized net counts its committed probe (**3414 vs 3124 — both stated, neither adjudicated**).

### ⚠️ DOCUMENTARY DEFECTS — carried forward, still deliberately NOT fixed

- ⚠️ **`ADR-0051` §2 is FACTUALLY WRONG** about the h2spec sections the gate runs (append-only ⇒ RECORDED, NOT FIXED); its PUSH_PROMISE rationale is moot twice over.
- ⚠️ **`ADR-0058` cites `internal/filter/hcm/actions.go:routerActionH2.doH2` TWICE and the symbol is not there** — `routerActionH2` is at `router_h2.go:254`; the emit is `writeH2Reply` at `h2dispatch.go:720` (⚠️ **the pre-84.1 cite `:671` is STALE**). Do not propagate either.
- ⚠️ **`ROADMAP.md` malformation: rows at lines 119, 131, 140 lose summary content in GFM render** — the well-formedness gate MUST be the DISJUNCTION: ARM-A (escape-aware `pieces != 8`) catches **119 and 131 ONLY** (re-run at the 84.2 IMPL close: exactly those two, and row 84 at `:146` is NOT flagged); ARM-B (escape-aware trailing/empty piece) catches **140 ONLY**. ⚠️ **Naive `NF != 8` / `NF != 10` is wrong in BOTH directions** — an ad-hoc ARM-B reconstruction attempted at the 84.2 IMPL close **over-fired on ~74 rows**, so **write ARM-B from the documented definition or do not run it**; no ARM-B count is carried. INVARIANT-BLOCKED ⇒ a GUARD, not a fix.
- **`STATE.md` §Project counts SELF-CONTRADICT §Current and are FROZEN at phase 76** (fixtures 119; stat 1205; "next-free port 10450"). **Do not source from §Project; do not fix it.** The real figures are in §Current and in the count block above.
- ⚠️ **`test/differential/harness_test.go:208`'s port inventory is STALE** (true set reaches 10449+; a false inventory a future allocator would trust).
- ⚠️ **The two `//nolint:gosec` directives in `body.go` are INERT** (`.golangci.yml` `disable-all: true` + 9 linters at v1.64.8; `misspell` locale US — British spellings in PROSE fail; identifiers do not; a `typecheck` failure short-circuits `misspell`).
- ⚠️ **The xDS config-seam cycle guard is NOT AUTOMATED ANYWHERE.**
- ⚠️ **`internal/wasm/doc.go:219` carries TWO errors** (ABICallbacks count; wrong file) · **`ROADMAP.md` cites `github.com/esalaine/envoy-go/…` FIVE times** (module is `github.com/pgdad/envoy-go`) — documentary, INVARIANT-BLOCKED · **`rbac.go:50` contains the token `F2`** as a phase-26 fork label — do not grep-anchor on `F2` in that package.

### ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS YOURSELF AND RECORD ACTUAL OUTPUT — before AND after any `ROADMAP.md` edit.** Both doctoring NCs every time; **TWO of the three checks are now silent in the healthy state.**
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** Lineage: phase-83 B21/S23/P19/I37; phase-84 B22/S26/P31(84.1)/I-84.1 20 tasks/P-84.2 13 corrections/**I-84.2 four-arm fixture + roster reproduced + one path correction**. ⚠️ **A refutation is a claim too**, and ⚠️ **a controller/task BRIEF is a claim too** — the 84.2 IMPL's own brief mis-inferred the F3 populate site's directory, and the PLAN's cite was path-less, so only re-derivation caught it.
3. **BEFORE ANY LINE-ADDING EDIT TO A HEAVILY-CITED DOC, ENUMERATE THE CITES YOUR INSERT WOULD SHIFT — and carry no whole-file count of a pattern your own prose spells.**
4. **LINE ANCHORS DRIFT; SYMBOL ANCHORS DID NOT.** Re-verified at the 84.2 IMPL: `writeH2Reply` def **`h2dispatch.go:720`**, the D-84-ENDSTREAM site (`hasTrailers :=`) **`:751`**, the populate site **`router_h2.go:250`**, the fixture blank import **`runner_test.go:146`**, `CompareBytes` **`runner_test.go:1283`**, the h2spec zero-case skip **`h2spec_test.go:310`**. **RE-VERIFY EVERY CITE AT YOUR TIP BEFORE RELYING ON IT.**
5. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Still live: `reference_sds_init_fetch_timeout_dial_budget_flake` (TWO packages) · the `internal/cluster` `-race` outlier · `internal/httpclient TestOptions_ZeroValue_NoOpDefaults` · the driver-receiver port race (roster 42) · **plus the TWO NEW ONES above, which are NOT yet in any documented class.**
6. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only; never `prune`/`ancestor=`). A live reference probe needs a BRIDGE network and a FRESH container per arm. `kill` on a `go run` parent leaves the COMPILED CHILD holding the socket.
7. **MACHINE-STATE DELIBERATELY IN PLACE (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain** (`rustup default` unchanged). ⚠️ **No `phase-84-*` worktree should outlive the 84.2 IMPL close — check `git worktree list` and expect only `master` plus your own.**
8. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`.
9. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL.**
10. **A DEFERRED CANDIDATE'S COST GOES STALE** (`reference_deferred_candidate_cost_restale`) — re-derive at the current tip before shortlisting, and **a MEASURED PROTOTYPE IS A LOWER BOUND** (`reference_measured_prototype_is_a_lower_bound` fired on SIX consecutive rows; 84.1 realized **1.50x/1.37x** over its budget). The cause is under-**ENUMERATION**, not under-scaling.


## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — take it from `git rev-parse master`, **NOT from a SHA quoted in this file**. ⚠️ **THE BARE SUBJECT-LOCATE GREP IS CONTAMINATED BY THIS FILE'S OWN PROSE** — router commits discuss the phase in their BODIES. **USE THE SLUG-ANCHORED FORM**, stage-exact:
```sh
git log --grep '^phase 84 (grpc-unary-response-trailers)' --format='%h %s'  # 6 at this tip: BRAINSTORM + SPEC + PLAN-84.1 + IMPL-84.1 + PLAN-84.2 + IMPL-84.2
git log --grep '^phase 83 (wasm-pause-arm-leak)' --format='%h %s'           # 4 — a CLOSED phase, use it as a FIRING CONTROL
```
⚠️ **The count comment above is a LIVE FIGURE that has gone stale in this file before (it read "3" while the tree held 4) — VERIFY the count before relying on it; an invented slug returns 0.** Take the NEWEST line (`| head -1`). ⚠️ **YOUR OWN phase's slug does not exist yet — its firing control is a CLOSED phase's slug, not your own.**

Subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** (`reference_parallel_subagents_private_scratch`) and tear down docker **BY NAME** (`reference_parallel_agents_shared_machine_namespaces`). ⚠️ **Band private ports outside BOTH `20000-31007` (subject blocks) AND `11000-14999` (backends + subject admin), and clear of the static fixture ports (19 bands, 10000-19172).** The `42000-46099` band has been used safely (phases 80-84) — ⚠️ **but `0081`'s ALS receiver binds `0.0.0.0:42039` inside it: never hold a listener there while the differential runs.** ⚠️ **If two agents share ONE worktree they must stage by EXPLICIT PATH and never run an unscoped `git restore`; PREFER ONE WORKTREE PER AGENT.** Per-task `gofmt` + `golangci-lint` on touched packages (`feedback_pertask_gofmt_lint`). ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch (`-o` precedes the package path).**

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root. **Treated as live for THIRTY-EIGHT consecutive sessions.** Use `git -C <abs-worktree-path>` for every git command; put any needed `cd` FIRST in a compound command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — `git add` ALONE WILL NOT STAGE IT. USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — restoration verified by sha256 against the base blob, never by eye; injection-site occurrence asserted `== 1` before writing; work DETACHED so a failed revert cannot dirty a branch. (The 84.2 PLAN's prototype agent did exactly this: full fixture + six arms, then `git status --porcelain` = 0 with three production files sha256-matched; the 84.2 IMPL's break-roster task repeated it with 4/4 sha256 MATCH.)

⚠️ **THE ARCHIVE-ABSENCE GUARD — USE THE ROBUST FORM.** Anchor on the bullet, allow ANY run of characters before the backtick-quoted target, corroborate with `grep -cF`. ⚠️ **BOTH prescribed alternatives (colon-anchored; "tolerant" non-asterisk) are demonstrated FAIL-UNSAFE on real data** (they read 0 on PRESENT annotated entries, which AUTHORIZES A DUPLICATE APPEND). ⚠️ **NC IT WITH A *REAL* TARGET — and re-derive the real label FROM THE FILE first**: at the 84.2 PLAN the controller's first control input was an invented slug reading 0 on every arm — non-discriminating, and it looked exactly like agreement. ⚠️ **AND A CONTROL'S OWN COUNT IS A LIVE FIGURE:** the standing control `phase 82 (wasm-http-call-response-cache) IMPL done` read **2** at the 84.2 PLAN close and **3** at the 84.2 IMPL close, because each eviction entry that cites it as a control adds a line. **Re-derive it; never copy it.** ⚠️ **DESCRIBE the guard's regex in the archive entry label; never spell it** (a quoting entry self-clears). ⚠️ **Verify the archive edit is STRICTLY APPEND-ONLY**: `git diff --numstat` = `N 0` AND the base file a byte-exact PREFIX (`cmp` the base blob against `head -<baselines>` of the new file).

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY** (seven singleton fields, each verified singleton at the 84.2 IMPL close; §Recent at its five-entry cap). ⚠️ **`next-skill:` reads `BRAINSTORM — SELF-PICK` — that IS your instruction.** ⚠️ **§Project counts SELF-CONTRADICT §Current — do not anchor on them, do not fix them.**
2. `docs/envoy-go/ROADMAP.md` — **§Schema first** (`:18`: update-in-place + "Sub-phases get their own rows" — ⚠️ **practice DIVERGES: zero dotted rows above 32.2; rows 60/61/84 record legs in PROSE**), then the **check-(2) lines at `:194 :200 :206 :216 :222 :230`**, which are both a candidate subject and a sentinel-sensitive region. ⚠️ **A chartering BRAINSTORM adds ONE row and bumps `want` 116 -> 117 in the SAME commit.**
3. `docs/envoy-go/phases/84-grpc-unary-response-trailers/PROGRESS.md` — the FIVE stage sections; the IMPL-84.2 section's handoff is the long form of this file's STATUS block, including the break roster table and the two new flakes.
4. `BOOTSTRAP_PROMPT.md` — **repo ROOT, no leading slash** (522 lines; the `docs/superpowers/plans/` file is the divergent GENERATOR PLAN). §6.1 `:285`, §6.2 `:294`, §7.3 `:346-351`, §7.5 `:357-367`, gates `:360-365`.
5. `docs/envoy-go/DECISIONS.md` — append-only; tail `ADR-0306` COMPLETE, next-free `ADR-0307` **derived from the tail**. Bound any ADR block by the next `^## ADR-` heading of ANY id. A BRAINSTORM normally drafts no ADR; if yours does, §Context at STATUS `PROPOSED` is the convention.
6. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — context only; a BRAINSTORM owes no reconciliation. ⚠️ **ADR-0052 `:1821` makes an ADR the MANDATED vehicle for a new contract statement** — never a silent edit.
7. `test/conformance/h2spec/h2spec_test.go` and `docs/envoy-go/CONFORMANCE_PINS.md` — **only if you shortlist candidate (ii)**; the selector defect and the missing zero-case guard live there.

## After the BRAINSTORM lands — the roll-forward
A chartering BRAINSTORM registers ONE ROADMAP row `in-progress` and bumps `want` **116 -> 117 in the SAME commit**. Re-run the sentinel afterwards and record what you see: **check (1) will print `NOT DONE: row <your row>` at the new denominator — CORRECT while your phase is open** (it goes silent again only at your IMPL's row flip); **check (2) will still print** (SIX, or more if your row's family-open paragraph records a backlog — the phase-84 BRAINSTORM took it 5 -> 6 deliberately rather than word around the matcher); **check (3) stays silent unless you open a family whose slug is not yet in the list.** ⇒ **the sentinel does NOT fire and `stop` is NOT created.** **Roll this file to point at your SPEC**, carrying: the new `want`, the new row number and its line, the re-derived counts, the sentinel state you actually observed with both doctoring NCs, and the rejected alternatives so the next session does not re-litigate them.
