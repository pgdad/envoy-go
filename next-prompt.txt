=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-VERIFIED 2026-07-21 (re-run MECHANICALLY at the phase-70 SPEC close, in the stage worktree) — it does NOT fire. `stop` was NOT created.** Actual output:
- **(1) prints `NOT DONE: row 70`** — row 70 (`tracing-custom-tags-metadata`) is `in-progress` (registered at the phase-70 BRAINSTORM commit per the §Schema invariant); it flips `done` at the phase-70 IMPL six-gate. (The registration re-opens check (1) until the IMPL.)
- **(2) prints THREE live "candidates:" sentences** — HTTP/3, xDS, Observability ⇒ three families STAY OPEN. **Cite the command, never the adjective** (`reference_sentinel_deferred_sentence_live_vs_historical`): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` ⇒ **3**. (The Observability sentence stays live — the `custom_tags (metadata)` candidate is NARROWED OUT only at the phase-70 IMPL row-done edit; the `ssl` + `spawn_upstream_span`/`http_service`/force-trace candidates remain.)
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** ⇒ THREE families never opened.

All three checks print ⇒ the sentinel does NOT fire. Abundant work remains.

==============================================

## STATUS AS OF 2026-07-21: THE PHASE-70 SPEC IS COMPLETE AND LANDED. ROW 70 (`tracing-custom-tags-metadata`) IS `in-progress`. THIS SESSION PERFORMS THE PHASE-70 PLAN.

**What the phase-70 SPEC landed** (`git log --grep 'phase 70'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): docs-only (`docs/envoy-go/phases/70-tracing-custom-tags-metadata/SPEC.md` + the ADR-0292 §Context append to `DECISIONS.md` [the tail flips ADR-0291 → ADR-0292] + STATE + this router-roll; ZERO production `.go`). Fresh worktree off master `9338507a` (the phase-70 BRAINSTORM squash), branch `phase-70-tracing-custom-tags-metadata-spec`, per `feedback_git_worktrees`. Subagent-driven: an independent read-only re-derivation agent re-opened EVERY BRAINSTORM §6/§11 anchor at tip + SIX EXECUTED live-probe arms (P1–P6) against `envoyproxy/envoy:contrib-v1.37.2`.
- **The D-CTM-* docket DISPOSED one-for-one** (SPEC §3). **D-CTM-WIRE/ZIPKIN-WIRE HELD (P1/P2):** the reference emits `{key = the LITERAL `tag` name, value = the serialized dynamic-metadata value as a STRING}`, APPENDED AFTER the built-ins on the OTLP span + in the Zipkin v2 JSON `tags` map — the EXISTING `Span.Attrs` upsert seam already produces this. **D-CTM-VALUE-SERIALIZE (P3):** string→raw, number→canonical decimal, bool→`true`/`false`, struct/list→compact JSON — IMPLEMENTED to match (string→`GetStringValue()`; non-string→`protojson.Marshal`+`json.Compact` to strip the detrand whitespace), NOT deferred (deferring would silently diverge, ADR-0080). **D-CTM-DEFAULT (P4):** the `request_header` rule, NOT `environment` — a present-empty value EMITS `""` (does NOT fall to the default); `HasDefault = DefaultValue != ""`. **D-CTM-PATHWALK (P5):** nested `StructValue` walk works; an unresolvable segment → default/omit. **D-CTM-REJECT (P6):** the reference BOOTS `ROUTE`/`CLUSTER`/`HOST`/unset-kind (so envoy-go's reject is a real envoy-go-strict DEPARTURE) but PGV-rejects empty `metadata_key.key`/empty `path` (PARITY).
- **⚠️ THE ONE LOAD-BEARING CORRECTION (C1, SPEC §3.5):** `*FilterChain` has **NO** `DynamicMetadata()` accessor — `grep 'func (c *FilterChain) DynamicMetadata'` → ZERO; the `chain.go:837`/`:963` accessors are on the `decoderCB`/`encoderCB` callback types (the BRAINSTORM mis-cited them as on the chain — `feedback_brief_citations_not_evidence`). The phase-70 IMPL **ADDS** `func (c *FilterChain) DynamicMetadata() *dynamicmetadata.Bucket` (a NEW export on `internal/filter/http`). The seam threads cleanly: every span-capable emit site has `chain` in scope; every no-chain 404 site passes `traceDecision==nil`, so `ResolveCustomTags` is provably never reached there (pass `nil`).
- **Four sharpenings + one refutation:** C2 the `request_header` default rule; C3 the ratelimit `descendStructpbValue` (`descriptors.go:1116`) as the path-walk SHAPE to CLONE (not import — layering); C4 the non-string serialization implemented; C6 the PGV departure/parity split; **C5 REFUTED — no "next free port"** (tracing/OTLP fixtures TEMPLATE all ports; next fixture is `0114`).
- **Pinned:** D-CTM-CONFIG-SEAM (`CustomTagSpec += MetaNamespace/MetaPath`, reuse `DefaultValue`/`HasDefault`; new `kindMetadata` const); D-CTM-FIXTURE → **ONE** new OTLP `0114-tracing-custom-tags-metadata` (a Lua-set FIXED-string writer + TWO metadata custom_tags — one resolving to the Lua value, one falling to `default_value` — asserted cross-side EXACT key+value, STRONGER than 0106; fixtures 115 → 116; NO separate Zipkin dir — the shared `Span.Attrs` seam + unit tests prove both exporters, the 59/62/63 precedent); D-CTM-FUZZSEED → +0 (a `FuzzHCMConfigParse` seed dispatch-verified to reach the metadata arm); D-CTM-SPLIT → SINGLE FLAT ROW (~10 tasks, ADR-0045 valve armable-but-unconsumed).
- **Adversarially verified before landing** (two independent verifiers in PRIVATE scratch — the record is folded into SPEC §1.2). Row 70 STAYS `in-progress`. NO deferred sentence narrowed at this pre-IMPL stage (phase-57 precedent). ADR-0292 §Context DRAFTED (the tail flipped ADR-0291 → ADR-0292 at the SPEC commit per ADR-0044); §Decision/§Consequences land at the IMPL IN PLACE.
- **Counts UNCHANGED at this SPEC (docs-only apart from the DECISIONS append; re-run mechanically):** fixtures **115** / fuzzers **55** / stat **1201** / BackendKind **38** / DECISIONS tail **ADR-0292** (PROPOSED; next-free **ADR-0293**) / go.mod modules **2**.

**⚠️ PUSH (idempotent — evaluate, then act):** FIRST ACTION: `git fetch`; if origin/master is BEHIND local master and the local tip is the phase-70 SPEC squash (`git log --grep 'phase 70'`, locate by SUBJECT), `git push origin master` per `feedback_push_to_origin`; if in sync, proceed; if REJECTED (diverged), STOP and reconcile — never rebase away landed squashes.

## What THIS session does — the phase-70 PLAN (RE-DERIVE the SPEC, do NOT execute it)

One stage per session: the PLAN. Fresh worktree off master per `feedback_git_worktrees`; docs-only (`PLAN.md` + `PROGRESS.md`; DECISIONS UNTOUCHED at the PLAN). RE-DERIVE the SPEC at tip (an independent re-derivation agent re-opens EVERY §11 edit site + extracts the exact clone skeletons — `feedback_brief_citations_not_evidence`; a PLAN is not evidence), then decompose SPEC §10's SINGLE-FLAT-ROW ~10-task TDD spine. Re-derive at tip:
- **The parse arms** — `config.go:214-215` (the metadata reject to replace), `CustomTagSpec` `:63-71` + `customTagKind` `:52-56` (the extension shape); the metadata accept (REQUEST) + FOUR distinct rejects (ROUTE/CLUSTER/HOST/unset-kind = DEPARTURE; empty-namespace/empty-path/empty-segment = PARITY).
- **The resolve arm** — `resolve.go:14` `ResolveCustomTags` (the `metaLookup func(ns,key)(*structpb.Value,bool)` param + the `kindMetadata` arm); the `descend` clone of the ratelimit `descendStructpbValue` (`internal/filter/http/ratelimit/descriptors.go:1116` — CLONE the SHAPE, do NOT import; keep `internal/tracing` filter-free); `structpbValueToString` (string→raw / non-string→`protojson`+`json.Compact`); the `request_header` default rule (`HasDefault = DefaultValue != ""`, present-empty EMITS `""`).
- **The NEW accessor** — `func (c *FilterChain) DynamicMetadata() *dynamicmetadata.Bucket` beside `SetDynamicMetadata` (`chain.go:1014`) — the C1 correction.
- **The threading** — the `metaLookup` param on the three emit methods (`accesslog_emit.go:55`/`:116`/`:177`) + ALL 18 caller sites (H1 `connection.go:330 nil`/`:464`/`:597`/`:699`/`:777`; H2 `h2dispatch.go:313 nil`/`:396`/`:530`/`:577`/`:584`/`:613`; H3 `h3dispatch.go:130 nil`/`:210`/`:280`/`:341`/`:367`/`:373`/`:395`) — `chain.DynamicMetadata().Get` at span-capable sites, `nil` at the 3 no-chain 404 sites; RE-DERIVE the exact per-caller `chain` variable name at tip.
- **The fuzz seed** — a `FuzzHCMConfigParse` (`fuzz_test.go:28`) seed (accepted REQUEST + rejected ROUTE), dispatch-verified to reach the `parseCustomTags` metadata arm; count STAYS 55 (`reference_fuzzer_count_docs_drift`).
- **The fixture** — `0114-tracing-custom-tags-metadata` (OTLP; a Lua writer + two custom_tags; cross-side EXACT; breaks per `reference_differential_break_protocol_count1`).

Sketch (SPEC §10): **T1** config parse arms + `config_test.go` · **T2** resolve arm + `descend` + `structpbValueToString` + `resolve_test.go` (the P3 serialization table + the P4 default/omit matrix + path-walk) · **T3** the `*FilterChain.DynamicMetadata()` accessor + a test · **T4** the `metaLookup` thread at all 18 callers + updated `accesslog_emit_test.go`/`span_emit_test.go` · **T5** the fuzz seed + dispatch-verify · **T6** the `0114` fixture + breaks · **T7** BEHAVIOR_CONTRACT B1-B2 · **T8** six-gate + full-116-dir differential + `-race` + cycle guard (`go list -deps ./internal/tracing` — no filter-pkg edge) + `go mod tidy -diff` empty + envelope audit · **T9** ADR-0292 completed IN PLACE + ROADMAP row-70-done + the deferred-sentence narrow (`custom_tags (metadata)` OUT) + STATE/router.

**Stage-close mechanics:** docs-only (`PLAN.md` + `PROGRESS.md`); DECISIONS UNTOUCHED at the PLAN (ADR-0292 stays PROPOSED; completed IN PLACE at the IMPL). Row 70 STAYS `in-progress` (flips `done` at the phase-70 IMPL six-gate). NO deferred sentence narrowed at the PLAN (rolls OUT at the IMPL — the phase-57 precedent). Counts UNCHANGED (docs-only; DECISIONS tail STAYS ADR-0292). Re-run the sentinel MECHANICALLY at close (it does NOT fire: (1) `NOT DONE: row 70`; (2) THREE `candidates:` sentences; (3) `NEVER OPENED: gRPC/Runtime/WASM`). Roll THIS file to the phase-70 IMPL. **Adversarially verify before landing** (`reference_parallel_subagents_private_scratch`).

## Read first (in order)
1. `docs/envoy-go/phases/70-tracing-custom-tags-metadata/SPEC.md` — the D-CTM-* dispositions (§3) + the §10 task spine + the §11 edit-site roster + the §14 ADR-0292 §Context draft. RE-DERIVE, do NOT execute.
2. `docs/envoy-go/phases/70-tracing-custom-tags-metadata/BRAINSTORM.md` — the full pick + rejected roster (context).
3. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline; the next-skill line carries the PLAN's owed work).
4. `internal/tracing/{config.go,resolve.go}` + `internal/filter/http/chain.go` (the `dynamicMetadata` field `:274`, `SetDynamicMetadata` `:1014`, the callback accessors `:837`/`:963`) + `internal/filter/http/ratelimit/descriptors.go` (the `descendStructpbValue` clone target `:1116`) + `internal/filter/hcm/{accesslog_emit.go,connection.go,h2dispatch.go,h3dispatch.go}` — RE-DERIVE the seam + the 18 callers at tip.
5. `docs/envoy-go/ROADMAP.md` — the Observability family paragraph + the three live `candidates:` sentences (locate by grep, not line) + §Schema (row-registration invariant).
6. `docs/envoy-go/DECISIONS.md` tail — ADR-0292 (PROPOSED, §Context only — the IMPL completes it), ADR-0277/0283/0284 (the prior `custom_tags` literal/request_header/environment ADRs) + ADR-0044/0045/0106 (the ADR/split/valve discipline).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — trust MEMORY.md's index; esp. `feedback_brief_citations_not_evidence`, `reference_differential_fixture_dispatch_constraint`, `reference_differential_break_protocol_count1`, `reference_detrand_race_catches_protojson_value_substring`, `reference_sentinel_deferred_sentence_live_vs_historical`.
