=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-CHECKED 2026-07-13 (after the phase-60.2 PLAN landed, run MECHANICALLY) — it does NOT fire. Do NOT create `stop`.** State:
- (1) prints `NOT DONE: row 60`, `NOT DONE: row 61` — TWO `in-progress` rows. Row 60 `xds-sds-server-cert` STAYS `in-progress`: its 60.1 substrate leg is IMPL-LANDED and its 60.2 TLS-apply leg is now PLAN'd, but the row flips `done` only when the 60.2 IMPL lands (ADR-0106, `reference_roadmap_split_phase_row_done`). Row 61 `http3-downstream-listener` is banked at SPEC (a 61.1/61.2/61.3 split). This ALONE blocks `stop`.
- (2) prints THREE live "candidates:" sentences — Observability, xDS, and HTTP/3 ⇒ three families STAY OPEN. (`reference_sentinel_deferred_sentence_live_vs_historical`.)
- (3) prints `NEVER OPENED: gRPC`, `Runtime`, `WASM` ⇒ THREE families never opened. Operational-tooling also STAYS OPEN.

All three checks print ⇒ the sentinel does NOT fire.

**STATUS AS OF 2026-07-13: the phase-60.2 PLAN LANDED (the SDS TLS-apply decomposition; row 60 STAYS `in-progress`).** The NEXT IMPLEMENTABLE STAGE is the **phase-60.2 IMPL** (`xds-sds-tls-cert-apply`, lifecycle-state 3 for the 60.2 leg — execute `PLAN-60.2.md` task-by-task). 60.2 is the OBSERVABLE end-to-end leg that plumbs the 60.1 `internal/xds` substrate into the downstream TLS context, adds the six-plus-one strict-reject roster + the initial-fetch BLOCK-then-BOOT-FAIL departure + the driver-owned-SDS differential, and **flips ROADMAP row 60 `done`** (both legs landed, ADR-0106). Advancing the banked xDS 60.2 leg is more defensible than opening a brand-new subject (a split leg awaiting its IMPL, per the standing directive). Row 61 (HTTP/3) rolls after the xDS legs, or per a future roller pick.

**What the 60.2 PLAN delivered (RE-VERIFY against the tree at session start; `git fetch` first):**
- `docs/envoy-go/phases/60-xds-sds-server-cert/PLAN-60.2.md` — a 9-task TDD spine: (2) `xds.ParseSDSConfig` [arms 1–4/8/9]; (3) the `tls/config.go:153` downstream-gated LIFT + the `provider xds.SecretProvider` param [arms 1–6 + timeout-propagation]; (4) thread `sdsProvider` through the listener manager [`nil` at all existing callers, mechanical]; (5) `boot.NewSDSProvider` (pre-scan + node arm 7 + provider build via `xdsgrpc.NewOpener(grpcclient.NewSDSClient)`) + `Construct` param + main/validate wiring; (6) `sdsserver.NewAtAddr` (0.0.0.0 bind) + `helpers.TLSServedLeaf`; (7) the `0103-xds-sds-server-cert` differential (two per-side driver-owned SDS servers, served-leaf cross-side via `CompareBytes`, assertion proven live by a two-different-certs break); (8) BEHAVIOR_CONTRACT; (9) ADR-0280 + STATE + ROADMAP row 60 `done` + the xDS deferred-sentence update + the full 105-dir six-gate.
- `PROGRESS-60.2.md` — scaffolded (baselines, the cycle-guard re-check block, the 60.2 design pins, the 9-task checklist, the six-gate + break-evidence placeholders).

**The load-bearing 60.2 design (RE-READ PLAN-60.2 §"Orientation" + §"Design pins"):**
- **The cycle guard (from ADR-0278 — MUST stay intact):** `internal/xds` proper imports NEITHER `grpcclient` NOR `cluster` NOR `tls`. At 60.2 `internal/tls` imports `internal/xds` ONLY for the `xds.SecretProvider` interface type (acyclic BECAUSE `xds` is grpcclient/cluster/tls-free). The provider VALUE is CONSTRUCTED in `internal/boot`/`main.go` via `xdsgrpc.NewOpener(grpcclient.NewSDSClient(dialer, cluster))` and THREADED down as a value. Do NOT add a `grpcclient`/`cluster` import to `internal/xds` or a `grpcclient`/`xdsgrpc` import to `internal/tls`. VERIFY with `go list -deps` at Tasks 2/3/5/9 (PLAN-60.2 pins the exact greps).
- **The config seam (SPEC §3.2, MANAGEABLE):** a pre-built `xds.SecretProvider` threaded `main.go → boot.Construct → NewManagerWithBaseDirAndAllowH2C → buildListenerRuntimeWithCtx → NewDownstreamConfig(ts, baseDir, provider)`, mirroring `boot.NewTracingProvider`. `boot.NewSDSProvider` pre-scans the bootstrap listeners for the single MVP downstream SDS-bound TLS context (the accepted, bounded cost of the built-provider model).
- **The 60.1 API the 60.2 IMPL consumes** (VERIFY at session start): `xds.SecretProvider`/`xds.StreamOpener`/`xds.Stream`; `xds.Provider` + `xds.NewProvider(opener, node, baseDir, timeout, stats)`; `xds.Node{ID,Cluster}`; `xds.RegisterSDSStats(reg, secret)`; `xdsgrpc.NewOpener(*grpcclient.SDSClient) xds.StreamOpener`; `grpcclient.NewSDSClient(d, cluster)`; `test/helpers/sdsserver` (add `NewAtAddr` at Task 6).

**ADR reservation ledger (provisional; each finalized at its IMPL in landing order):** ADR-0277 = phase-59 (LANDED) · ADR-0278 = xDS 60.1 (LANDED) · **ADR-0280 = xDS 60.2 (the NEXT xDS ADR to land, at the 60.2 IMPL)** · ADR-0279 = HTTP/3 (61) · next-free unreserved **ADR-0281**. **60.2 LANDS THE DIFFERENTIAL FIXTURE `0103-xds-sds-server-cert`** — RE-DERIVE the actual dir number against the master tip at the 60.2 IMPL (the tail is `0102-tracing-custom-tags-literal`; landing order assigns `0103` unless another fixture lands first).

**Current counts (VERIFIED after the phase-60.2 PLAN, docs-only — UNCHANGED from the 60.1 IMPL):** fixtures **104** (tail `0102-tracing-custom-tags-literal`) · fuzzers **55** · DECISIONS tail **ADR-0278** (next-to-land **ADR-0280**; ADR-0279 = HTTP/3) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** · new Go packages **0** · new go.mod modules **0**. RE-VERIFY against the actual master tip at session start (`git fetch` first).

**Locating the phase-60.2 PLAN stage squash** (a router cannot name its own sha — identify by SUBJECT, never by position):
```sh
git log --oneline --grep='^phase 60.2 (xds-sds-tls-cert-apply) PLAN' -1
```

**THE NEXT ACTION: the phase-60.2 IMPL (lifecycle-state 3 for the 60.2 leg — execute `PLAN-60.2.md` task-by-task; production `.go` changes + the `0103` differential; row 60 flips `done` at the six-gate).** After the 60.2 IMPL lands, the roller's next stage is the roller's self-pick (HTTP/3 row 61's next leg or a fresh pick per the standing directive). The sentinel governs termination, not this list.

==============================================

## What THIS session does (the phase-60.2 IMPL)

Execute `docs/envoy-go/phases/60-xds-sds-server-cert/PLAN-60.2.md` task-by-task (Tasks 1–9), subagent-driven (`feedback_execution_style`, `feedback_subagent_autocommit_claudemd`, `feedback_subagents_no_push`), in a FRESH worktree off master (`.worktrees/phase-60.2-impl`, branch `phase-60-xds-sds-tls-cert-apply-impl`, per `feedback_git_worktrees`). This is a SINGLE STAGE (the IMPL). At its six-gate, ROADMAP **row 60 flips `in-progress` → `done`** (both legs landed, ADR-0106) and DECISIONS tail advances **ADR-0278 → ADR-0280**.

**The IMPL scope (from PLAN-60.2 — RE-DERIVE every `file:line` against the tip, `feedback_brief_citations_not_evidence`; the 60.1 IMPL and any interim landing may have shifted them):**
- **Task 2 — `xds.ParseSDSConfig`** (new `internal/xds/config.go`): validates a `SdsSecretConfig` and extracts `(secretName, clusterName, timeout)`; carries arms 1–4 (ads/self/DELTA_GRPC/google_grpc), 8 (empty name), 9 (missing sds_config), the V3 checks, and the MVP one-config cap — all `xds: sds:`-prefixed, ADR-0080-distinct. NO grpcclient/cluster/tls import.
- **Task 3 — the `tls/config.go:153` one-arm LIFT** + the `provider xds.SecretProvider` param on `NewDownstreamConfig` + `commonTLSContextToConfig` (nil on upstream/validate). Downstream + provider present: `ParseSDSConfig` → `provider.FetchInitialCertificate(ctx, secret)` → append the leaf to `cfg.Certificates`. Upstream keeps the byte-identical reject (arm 6, gated on `side != "downstream"`). `config_test.go`: accept (fake provider) + arms 1–6 + timeout-propagation + nil-provider reject; update all existing `NewDownstreamConfig(ts, "")` callers to `(ts, "", nil)`.
- **Task 4 — thread `sdsProvider`** through `NewManagerWithBaseDirAndAllowH2C` → `buildListenerRuntimeWithCtx` → both `NewDownstreamConfig` call sites; `nil` at all existing manager/admin test callers (compiler-enumerated).
- **Task 5 — `boot.NewSDSProvider`** (pre-scan the bootstrap listeners + the node boot-requirement arm 7 + build the provider via `xdsgrpc.NewOpener(grpcclient.NewSDSClient(dialer, cluster))`) + the `Construct` param + `main.go`/`validate.go` wiring (validate passes nil).
- **Task 6 — `sdsserver.NewAtAddr`** (0.0.0.0 bind for the differential's containerized reference) + `helpers.TLSServedLeaf` (the served-leaf accessor).
- **Task 7 — the `0103-xds-sds-server-cert` differential** (fixtures 104 → 105): two per-side driver-owned `sdsserver` instances on `0.0.0.0` (reference reaches `host.docker.internal:<port>` STRICT_DNS, subject reaches `127.0.0.1:<port>` — the `0089-stats-sink-metrics-service` precedent, `reference_docker_probe_bridge_network`; NOT `HostGatewayIP`); both serve the SAME committed leaf; assert the served leaf (serial + SAN) cross-side via `DriveReference`/`DriveSubject` + Step-7 `CompareBytes`; PROVE the assertion LIVE with a two-different-certs break (`-count=1 -run 'TestDifferential/0103-xds-sds-server-cert'`, `reference_differential_break_protocol_count1`/`reference_deliberate_break_wrong_assertion`/`reference_differential_run_selector`). The timeout/mgmt-down/secret-not-found cases are SUBJECT-side UNIT tests (Tasks 3/5), NOT extra differential dirs (`reference_differential_fixture_dispatch_constraint`).
- **Task 8 — BEHAVIOR_CONTRACT** xDS/SDS section.
- **Task 9 — ADR-0280** (§Decision/§Consequences at THIS IMPL per ADR-0044) + STATE + **ROADMAP row 60 → `done`** (ADR-0106) + the xDS deferred-sentence UPDATE (re-run the sentinel check-(2) grep — EXACTLY ONE live "candidates:" xDS match, `reference_sentinel_deferred_sentence_live_vs_historical`) + the six-gate (FULL 105-dir differential, byte-stable except `0103`).

**Anticipated exit counts (RE-VERIFY at IMPL):** stat surface **1201 (+0 static; +5 DYNAMIC `sds.*` materialized at boot under the `0103` SDS config)** · fixtures **104 → 105** · fuzzers **55 (+0)** · BackendKind **38 (+0)** · DECISIONS tail **ADR-0278 → ADR-0280** · new Go packages **0** · new go.mod modules **0** · **ROADMAP row 60 → `done`**.

## Honor throughout (the discipline, unchanged)
- **Lifecycle = one stage per session.** This session: the phase-60.2 IMPL ONLY. After it lands: the roller's self-pick (HTTP/3 row 61 or a fresh pick; the sentinel governs).
- **Process anchors:** ADR-0044 (ADR §Decision/§Consequences land at the IMPL — ADR-0280 body at THIS IMPL) · ADR-0080 (distinct per-arm reject substrings — the 6+1 arms) · ADR-0106 (row 60 flips `done` only when BOTH legs land — THIS IMPL is that moment).
- **The cycle guard (load-bearing, ADR-0278):** `internal/xds` stays grpcclient/cluster/tls-free; the grpcclient edge lives in `internal/xds/xdsgrpc`; `internal/tls → internal/xds` is acyclic ONLY because of this. Verify the import graph stays acyclic (`go list -deps`) at each edit site — PLAN-60.2 Tasks 2/3/5/9 pin the exact greps.
- **TDD (`superpowers:test-driven-development`) + per-task gates (`feedback_pertask_gofmt_lint`): gofmt -l + golangci-lint + go vet + go build + go mod tidy -diff on every code task.** Break protocol (`reference_differential_break_protocol_count1`): `-count=1` + the FULL subtest name for every liveness break; confirm WHICH assertion fires (`reference_deliberate_break_wrong_assertion`).
- **Subagent-driven execution** (`feedback_execution_style`, `feedback_subagent_autocommit_claudemd`, `feedback_subagents_no_push`): subagents commit LOCALLY only; the controller squashes at stage-close and pushes. Watch for HEAD-detach on deliberate breaks (`feedback_subagent_worktree_detach`) and stale-path targeting (`feedback_subagent_worktree_path_targeting`).
- **`next-prompt.txt` IS TRACKED** (`reference_next_prompt_tracked_despite_gitignore`) — edit it inside the stage worktree and fold into the stage commit; locate commits by SUBJECT (`git log --grep`), never by position.
- **Deferred maintenance (fold into the next `udp.go`/statssink-driver-touching row — carried from the phase-57 final review; phases 58/59/60.1/60.2 touched NEITHER `udp.go` NOR a statsd/graphite/dog_statsd sink — these STAY OPEN):** F-1 the 0101 driver's counter tag snapshot records the EXPECTED map; F-2 `udp.go` `udpWriter` doc comment names two sinks (three embed it now); F-3 BEHAVIOR_CONTRACT 0101 note says "`differential.HostGatewayIP` helper" where the driver uses its LOCAL duplicate. (Ride until the OTLP-metrics or another statsd/graphite/dog_statsd sink row reopens `udp.go`.)

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-60.2 IMPL is next; SPEC-60 is banked; the 60.1 substrate is landed; PLAN-60.2 + PROGRESS-60.2 are landed).
2. `docs/envoy-go/phases/60-xds-sds-server-cert/PLAN-60.2.md` — the 9-task TDD spine + the §"Orientation" zero-context brief + §"Design pins settled here" + §"Key source seams" (RE-DERIVE every `file:line` against the tip) + §"Self-Review".
3. `docs/envoy-go/phases/60-xds-sds-server-cert/PROGRESS-60.2.md` — the baselines + the cycle-guard re-check block + the design pins + the 9-task checklist.
4. `docs/envoy-go/phases/60-xds-sds-server-cert/SPEC.md` — §3.2 (config seam), §3.3 (the one-arm lift), §3.4 (init-fetch BLOCK-then-BOOT-FAIL DEPARTURE), §6 (the reject roster incl. arm 7 node), §8 (the differential fixture), §9 (BEHAVIOR_CONTRACT), §11 (probe evidence), §13 (ADR §Context frame).
5. `docs/envoy-go/phases/60-xds-sds-server-cert/PLAN-60.1.md` + `PROGRESS-60.1.md` — what the substrate delivered (the `xds` API the 60.2 IMPL consumes + the cycle guard it must honor).
6. `docs/envoy-go/DECISIONS.md` — **ADR-0278** (the substrate + the cycle-avoidance design 60.2 depends on; its §Consequences naming 60.2/ADR-0280).
7. `docs/envoy-go/ROADMAP.md` — row 60 `in-progress` (flips `done` at the 60.2 IMPL); the xDS-family row + the LIVE xDS deferred sentence (ONE live match; UPDATE at the IMPL).
8. `docs/envoy-go/STATE.md` — the active-phase header (phase 60.2 PLAN done; NEXT = the phase-60.2 IMPL).
9. The seam source (RE-DERIVE every `file:line` against the tip): `internal/tls/config.go` (`:34` `NewDownstreamConfig`, `:79` `NewUpstreamConfig`, `:149` `commonTLSContextToConfig`, `:153` reject) · `internal/listener/manager.go` (`:254` ctor, `:340` `buildListenerRuntimeWithCtx`, `:382`/`:457` `NewDownstreamConfig` calls) · `internal/boot/boot.go` (`:53` `Construct`, `:96` `NewTracingProvider` precedent) · `cmd/envoy-go/main.go` (`:134` dialer, `:150` tracingProvider, `:155` node access, `:295` `boot.Construct`) · `validate/validate.go` (`:46`/`:48`) · `internal/bootstrap/bootstrap.go` (`:657` the node-check precedent) · `internal/xds/` + `internal/xds/xdsgrpc/` + `internal/grpcclient` (`SDSClient`) + `test/helpers/sdsserver` (the API 60.2 consumes) · `test/differential/fixture/fixture.go` (the `Driver` interface) + `test/fixtures/0089-stats-sink-metrics-service/driver/` (the driver-owned-server precedent) + `test/fixtures/0002-tls-tcp/` (the downstream-TLS + pki precedent).
10. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the `reference_*` traps (esp. `reference_differential_grpc_receiver_driver_owned`, `reference_docker_probe_bridge_network`, `reference_host_gateway_ip_docker_desktop`, `reference_differential_fixture_dispatch_constraint`, `reference_differential_break_protocol_count1`, `reference_differential_run_selector`, `reference_deliberate_break_wrong_assertion`, `reference_fatalf_makes_assertions_unreachable`, `reference_dynamic_stat_name_charset_guard`, `reference_sentinel_deferred_sentence_live_vs_historical`, `reference_roadmap_split_phase_row_done`, and the `xds` transitive-import cycle-guard reference).
