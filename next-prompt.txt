START a fresh cold-start session for envoy-go — **phase 32 (`network-filter-redis-proxy`) PARENT SPEC is DONE; the next lifecycle step is the 32.1 SUB-PHASE SPEC** (`network-filter-redis-upstream-pool-seam-and-codec`). The phase-32 parent SPEC settled the SINGLE-ROUTE-TERMINAL MVP surface-mapping across the 2-way pre-split (32.1 seam+codec+round-trip / 32.2 commands+stats+matrix), executed the §10 D32-1..D32-8 empirical pins IN-SESSION live against the contrib reference Envoy (a real `redis:7` backend on a docker bridge network — round-trip CONFIRMED ran), recorded AMEND-R1..R9, and anchored the ADR-0229 §Context. The next session authors the **32.1 SPEC** (`superpowers:writing-plans` scoped to the sub-phase SPEC — per `feedback_execution_style`; work in a git worktree per `feedback_git_worktrees`), which pins the upstream connection-pool / cluster-routing SEAM's exact exported API + multiplexing model and **anchors the ADR-0230 §Context** (the seam ADR, deferred from the parent SPEC per the BRAINSTORM §7).

The phase-32 parent SPEC landed at the phase-32 PARENT SPEC squash `0f7888d` on master (squash-merged from `phase-32-network-filter-redis-proxy-spec` + pushed to origin/master by the controller; SPEC.md + the ADR-0229 §Context draft in DECISIONS.md + README/ROADMAP/STATE/next-prompt advance; spec-document-reviewer APPROVED first iteration). It is the substantive master tip; this docs-only SHA-fill follow-up trails it as the live tip — confirm via `git log --oneline -1`. Substantive predecessor on master: the phase-32 BRAINSTORM squash `f473c4e`; earlier the phase-31 IMPL squash `928287a` (the `kafka_broker` §9 row; phase 31 CLOSED). origin/master in sync after push.

## What this session does — the phase-32.1 SUB-PHASE SPEC

Author the 32.1 sub-phase SPEC in `docs/envoy-go/phases/32-network-filter-redis-proxy/` (mirror the 29.1/28.1 sub-phase-SPEC filename convention — check the phase-29 dir for the exact pattern). The 32.1 SPEC, per the parent SPEC §3.2 + §4.1 + §12:

- **Pins the upstream connection-pool / cluster-routing SEAM (ADR-0230) exact surface** (parent D-P32-3): the new exported type(s) the redisproxy terminal calls; the host-resolution + dial reuse of `cluster.Manager.Get` + `cluster.Cluster.Dial` (the tcp_proxy dial path, `internal/filter/tcpproxy/filter.go:127`); the FIFO/positional pop-front reply-correlation contract (RESP has no correlation id); the MULTIPLEXING MODEL — one-upstream-conn-per-downstream-conn (the simplest faithful MVP — trivially-correct positional correlation) vs a shared per-host pool. Redis-scoped (no thrift generalization; YAGNI). Built on `TerminalFilter.Handle` (`internal/filter/network/terminal.go`, UNCHANGED). **Anchors the ADR-0230 §Context** in DECISIONS.md (DECISIONS tail ADR-0229 → ADR-0230; §Decision/§Consequences body at the 32.1 IMPL per ADR-0044).
- **Pins the RESP codec partial-frame reassembly model** (parent D-P32-4) — the decoder-internal buffering across reads for a TERMINAL filter that owns the conn (no chain Buffer; contrast the mongo/kafka private-buffer-on-an-observer-chain model).
- **Pins the fixed-roster creation posture** (parent D-P32-1: eager-at-config-parse anticipated) + the 32.1 stat subset (the downstream cx/rq counters for the round-trip; the 4 gauges CREATED at 32.1, inc/dec at 32.2).
- **Pins the boot-reject fixture-arm selection** (parent D-P32-5: anticipated `0056-redis-boot-reject` carries the `stat_prefix`-required arm; the `settings`/`op_timeout`/`no-upstream`/`catch-all-cluster` arms unit-tested) + the local-reply set for 32.1 (PING + AUTH; parent D-P32-6).
- **Decomposes the 32.1 surface into a task spine** (the codec, the seam, the redisproxy package foundation, the 10th built-in + `redis_proxy/v3` blank-import [ZERO new go.mod dep], the `TCPRedisResponder` BackendKind, the `0055-redis-roundtrip` round-trip arm + `0056`) and re-checks the ADR-0045 split-gate at this sub-phase (parent §15: ~900-1200 LoC / ~14-20 tasks → fits; a 32.x escape-valve split stays available). The 32.1 PLAN follows the 32.1 SPEC.

The 32.1 SPEC does NOT execute new docker probes (the parent SPEC's §11 D32-1..D32-8 block is the authoritative empirical record — reference it). It MAY re-probe the upstream-pool seam internals or the RESP framing detail from the parent's source corpus if a design question needs it.

## Honor throughout

- `feedback_execution_style` (subagent-driven IMPL when it gets there) · `feedback_git_worktrees` · `feedback_subagents_no_push` (subagents commit local-only) · `feedback_push_to_origin` (the controller squash-merges + pushes at stage-close) · `feedback_pertask_gofmt_lint`.
- ADR-0106(d) (§9 family in-row split-gate) · ADR-0045 (split-gate) · ADR-0219 (the upstream-cluster-override seam — the nearest routing precedent) · ADR-0223 (per-connection mutex — for the seam's pending-queue cross-goroutine state if the multiplexing model needs it) · ADR-0060 (histograms deferred) · ADR-0044 (§Context-at-SPEC, §Decision+§Consequences-at-IMPL — ADR-0230 §Context anchors at THIS 32.1 SPEC) · ADR-0227 (the contrib pin) · ADR-0080 (byte-stable rejects).
- Project memories: `reference_network_filter_typeurl_extensions`, `reference_wire_format_both_sides_see_same_bytes` (EXTENDS to the downstream RESPONSE bytes redis_proxy generates), `reference_close_direction_framework_gap` (does NOT bite — redis has no close-direction counters, AMEND-R9), `reference_docker_probe_bridge_network`, `reference_dynamic_stat_name_charset_guard` (redis per-command roster is EAGER + table-bounded per AMEND-R3 → guard satisfied by construction if the eager-table posture is adopted — the 32.2-SPEC pin, NOT 32.1), `reference_network_chain_terminal_handoff_ends_ondata`, `reference_differential_asserter_dispatch`, `reference_differential_fixture_dispatch_constraint`, `reference_differential_break_protocol_count1`, `reference_differential_reference_parses_full_message`.

## Read first (in order)

1. THIS FILE — the phase-32.1-SPEC cold-start.
2. `docs/envoy-go/phases/32-network-filter-redis-proxy/SPEC.md` — **THE PARENT SPEC** (the surface-mapping §3, the seam design §4.1, the proto/parse/stat rosters §5-§7, the differential envelope §8, the empirical-pin block §11 [D32-1..D32-8; AMEND-R1..R9 — the authoritative empirical record], the sub-phase D-questions §12 [D-P32-1..9 — the 32.1 SPEC resolves D-P32-1/3/4/5/6]) + `./BRAINSTORM.md` (the charter) + `./README.md`.
3. `docs/envoy-go/STATE.md` — active-phase `phase 32 PARENT SPEC done`; lifecycle-state → the 32.1 SPEC; next-skill `superpowers:writing-plans`; counts (536 stats / 56 fixtures / 40 fuzzers / BackendKind tail 31 / DECISIONS tail ADR-0229, next-free ADR-0230).
4. `docs/envoy-go/ROADMAP.md` — parent row 32 (SPEC-DONE note) + sub-rows 32.1/32.2 + the §9 roster.
5. `docs/envoy-go/DECISIONS.md` — ADR-0229 (the §Context just anchored — the redis_proxy filter umbrella) + ADR-0106 / ADR-0045 / ADR-0219 / ADR-0223 / ADR-0060 / ADR-0044 / ADR-0227 / ADR-0080.
6. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the `### envoy.filters.network.*` subsections + the `## Network filters` framework subsections (the `TerminalFilter` seam + the conn-wrap/async-halt seams the new upstream-pool seam parallels).
7. The phase-29 + phase-28 sub-phase SPEC files (the per-sub-phase SPEC lifecycle precedents — 29.1/28.1 SPEC shape + the sub-phase-SPEC filename convention).
8. `internal/filter/network/terminal.go` + `types.go` + `upstreamcluster.go` (the `TerminalFilter` + override seams) + `internal/filter/tcpproxy/filter.go` (the terminal dial + pump precedent) + `internal/cluster/manager.go` + `cluster.go` (`Manager.Get` + `Cluster.Dial` — the dial path the seam reuses) + `internal/filter/network/kafkabroker/` + `mongoproxy/` (the §9 package-shape precedents) + `internal/stats/name.go` (the prom-arm patterns).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`.

## Counts at master tip (phase 32 PARENT SPEC — UNCHANGED until the IMPL sub-phases)

- stat surface **536**; differential fixtures **56** (tail `0054-kafka-boot-reject`); fuzzers **40** (`FuzzKafkaDecode`); BackendKind tail **31** (`TCPKafkaResponder`); DECISIONS.md tail **ADR-0229** (the redis_proxy filter §Context; next-free **ADR-0230** — its §Context anchors at the 32.1 SPEC).
- conformance h2spec 53/53 + proxy-wasm 10/10 (image-independent).
- reference pin: `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227).
- Toolchain: Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); go-control-plane `/envoy` v1.32.4 (ADR-0008) + `/contrib` v1.32.4 (the phase-31 dep). redis_proxy is CORE `/envoy` (D32-1 CONFIRMED) → NO new dep.
- §9 Network-filters family: rows 26/27/28/29/30/31 DONE + **row 32 `redis_proxy` IN-PROGRESS (parent SPEC done; sub-rows 32.1/32.2 planned)**. After phase 32 the candidate {thrift} remains (the last §9 candidate — reuses the 32.1 upstream-pool seam).
