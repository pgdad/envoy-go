=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-VERIFIED 2026-07-24 at the phase-73 PLAN close — TWICE: once in the stage worktree, and AGAIN on LANDED master AFTER the squash-push, with byte-identical output (the phase-72/73 precedent — a stage worktree can disagree with what actually landed, so the post-push re-run is the one that counts). It does NOT fire. `stop` was NOT created.** Actual output:
- **(1) prints `NOT DONE: row 73`** — row 73 (`tracing-custom-tags-metadata-cluster`) is `in-progress`, registered at the BRAINSTORM commit per the ROADMAP §Schema invariant. ⚠️ **Row 73 is the LAST non-`done` row: 100 `done` + 1 `in-progress` among the 101 rows check (1) can SEE (103 rows total).** So when row 73 flips at the IMPL six-gate, **check (1) GOES SILENT** — verified by flipping it in a scratch copy. That is the NORMAL post-IMPL state and is **NEVER on its own a termination condition**; all THREE checks must be silent. *(The phase-73 PLAN draft claimed check (1) would NOT go silent; its own adversarial pass REFUTED that.)*
- **(2) prints THREE live "candidates:" sentences** — HTTP/3, xDS, Observability. **Cite the command, never the adjective** (`reference_sentinel_deferred_sentence_live_vs_historical`): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` => **3**. NO sentence was narrowed at this PLAN (the phase-57 precedent), proven MECHANICALLY: `git diff master..HEAD -- docs/envoy-go/ROADMAP.md` is EMPTY.
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** => THREE families never opened. (The `WASM` line remains a ROADMAP **bookkeeping artifact** — `internal/wasm/` is a substantially-built wazero host and rows 25/25.1/25.2/25.3 are all `done` — deliberately LEFT AS-IS, since the stage-close mechanics depend on this output.)

⚠️ **Check (1) has a KNOWN BLIND SPOT, for TWO distinct reasons** (PRE-EXISTING, no action owed — re-derived at the phase-73 PLAN by set-differencing all `| <num> |` rows against the rows check (1) matches, NOT by inspecting the regex): `| 00 | bootstrap | — | done |` is missed because the field `[0-9.,  ]*` does not match an **em-dash** "after" column, and `| 04 | http-1.1 | 03 | done |` is missed because the slug field `[a-z0-9-]+` does not match the **dot** in `http-1.1`. **2 of 103 rows are silently skipped.** Both are `done`, so there is no current impact — but check (1) is **not exhaustive**, and ANY future dotted slug or em-dash cell goes invisible too.

Checks (1)+(2)+(3) print => the sentinel does NOT fire. Abundant work remains.

==============================================


## STATUS AS OF 2026-07-24: THE PHASE-73 PLAN IS COMPLETE AND LANDED. ROW 73 (`tracing-custom-tags-metadata-cluster`) IS `in-progress`. THIS SESSION PERFORMS THE PHASE-73 IMPL — THE LAST STAGE OF THE PHASE.

**What the phase-73 PLAN landed** (`git log --grep 'phase 73'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): `PLAN.md` + `PROGRESS.md`. Docs-only, ZERO production `.go`, **DECISIONS and ROADMAP UNTOUCHED**, row 73 STAYS `in-progress`, ADR-0295 stays PROPOSED. TWO read-only agents on disjoint remits RE-DERIVED every SPEC §3/§9/§10/§11/§12/§14 anchor at the PLAN tip — **ZERO DRIFT and ZERO REFUTED on every CODE anchor**, plus NINE new findings (F1-F9). Then TWO adversarial verifiers ran against the COMMITTED draft in private scratch: **ONE SEVERE, TWO MODERATE, ONE MAJOR-process, TEN MINOR — all folded** (PLAN §1.4). **V1 verified BY EXECUTION, building T1→T4 from the PLAN's own skeletons pasted VERBATIM.**

### The five things the IMPL must carry forward, in order of how badly getting them wrong would hurt

1. **⚠️ TWO OF THE PLAN'S OWN INSTRUCTIONS WERE REFUTED BY ITS ADVERSARIAL PASS — the corrected forms are what T1 executes.** (a) **T1's four manager-side tests go in a NEW `internal/cluster/cluster_meta_test.go`, NOT `manager_test.go`** — that file is sha256-gated BYTE-UNTOUCHED at T4/T8, so adding tests to it makes its own gate UNSATISFIABLE (the identical defect the PLAN had already diagnosed for the emit test files without applying it to itself). V1 executed the corrected routing: `sha256sum -c` reports `manager_test.go: OK`. (b) **Break D — THE ROW'S DISTINGUISHING BREAK — is VACUOUS through `Cluster.PickEndpoint()`, EXECUTED GREEN by V1.** `PickEndpoint` passes `SubsetMatch{}, false` (`cluster.go:531-532`), so `subsetLB.Pick` (`subset.go:213-219`) never consults `s.subsets` and falls straight to the `ANY_ENDPOINT` fallback `factory(endpoints)` over the **ORIGINAL slice** (`subset.go:183-185`) — which ALIASES, so a zeroed field is invisible. **Reach the subset endpoint via the package-internal `cl.lb.(*subsetLB).Pick(0, false, NewSubsetMatch(map[string]SubsetValue{…}), true)`** — only `hasMatch=true` traverses `s.subsets`, and this needs **ZERO new exported symbols** (`NewSubsetMatch` `subset.go:68`, `SubsetValue` `:28` already exported; `subsetLB` `:140` same-package). **If Break D does not fire, that is a FINDING, not a pass.**

2. **⚠️ THE POPULATE-LOOP PLACEMENT IS LOAD-BEARING: THREE constructors copy `Endpoint` BY VALUE across FOUR copy sites.** The loop sits **IMMEDIATELY after the `err != nil` guard at `manager.go:433-436`, BEFORE ANY LB CONSTRUCTION** — never as a third `extractEndpoints` parameter (that would drag 10 `manager_test.go` callers and a `structpb` import). `buildLeafLB` (`:472`) retains the **slice header** (`roundRobin{endpoints: endpoints}`, `manager.go:344`) and therefore ALIASES the backing array — **which is exactly why a misplaced loop stays GREEN on round-robin** — while `newSubsetLB` (`:481`) copies in BOTH its selector-grouping loop (`subset.go:168`) AND its `fallbackDefaultSubset` branch (`subset.go:193`), `newLocalityWeightedLB` (`:516`) and `newPriorityLB` (`:550`) each `append(…, ep)` by value. No landed test catches this and the pool-HIT provenance test would NOT catch it either. **T1 owes the explicit subset-LB assertion; Break D enforces it.**

3. **⚠️ T2 MUST RETAIN THE TWO `/cluster` REJECT ROWS THROUGH STEPS 2-3 AND CONVERT THEM ONLY AFTERWARDS.** Converting them in Step 1 makes the pre-known red UNFIREABLE — those same rows ARE the red. Retained, V1 executed it: **exactly TWO `/cluster` subtests fire and the output PRINTS `Kind:6`**, the by-EXECUTION proof that `kindMetadataCluster` APPENDED at iota == 6. Their TABLE rows are `config_test.go:639`/`:764`, **byte-identical to each other** (`grep -Fxc` => 2) so an `old_string` on either alone is NON-UNIQUE; the FAILURES print at `:648`/`:773`. The const block is **NOT a pure-append diff** — gofmt re-aligns `config.go:58-61`'s trailing comments (+2 spaces each, measured), so **run `gofmt -w` BEFORE any `gofmt -l`-must-be-silent gate** or it trips on expected re-alignment.

4. **⚠️ T4's ENTRY STATE, STATED HONESTLY: `internal/filter/hcm` does NOT compile** — **exactly THREE** `not enough arguments in call to tracing.ResolveCustomTags` errors, at `accesslog_emit.go:57`, `:118`, `:179` (V1 executed and confirmed the count and the sites). That is the **`ResolveCustomTags` arity** changing exactly as in 70/71/72 — **do NOT conflate it with the EMIT-signature arity, which is genuinely unchanged and IS this row's cost claim** (phase 72's V1 caught exactly that conflation). ⚠️ **`:57` and `:179` are BYTE-IDENTICAL to each other** (PLAN F2) — edit by line number or with context, then verify 3 occurrences. The envelope gate is **TWO-PART and the parts are NOT interchangeable**: sha256 byte-identity on **SEVEN** production files (`connection.go`, `h2dispatch.go`, `h3dispatch.go`, `chain.go`, `subset.go`, `actions.go`, `route.go` — the last two are what the REJECTED route-carried seam would have touched), and **CALLER-SET invariance (13 + 21 = 34), NOT a hash**, on `accesslog_emit_test.go`/`span_emit_test.go`, which T4 itself ADDS tests to. Use `grep -cE 'emitAccessLog(H2|H3)?\('` — the naive `grep -c` reads 23 for `span_emit_test.go`.

5. **⚠️ FOUR STALE CLAIMS TO CORRECT AT T9 — NOT THREE, AND THE FOURTH IS ONE THE SPEC BELIEVED IT HAD ALREADY FIXED.** The ROADMAP row-73 cell carries three: (a) its narrow-hazard sentence — **REFUTED**: the narrow SHORTENS the clause, leaves **FOUR** candidates, and **check (2) STAYS 3** (SPEC §12 pins the exact `old_string`/`new_string`, verified `count == 1` and verified by APPLYING it in a scratch copy — the PLAN re-applied it independently and it still holds); (b) its *"the ONLY remaining reject arms are the two unset-kind ones"* clause — refuted, it is **TWO reject ARMS across THREE config spellings**; (c) its single-cluster `0117` description — superseded by the TWO-CLUSTER decision. **The FOURTH: the ADR-0295 HEADING at `DECISIONS.md:17211`** still carries *"manufactured **three divergence classes**"* (it is **ONE** expressible class) and *"**THREE arms, not two**"* (the unit is **TWO ARMS / THREE SPELLINGS**). The ADR **body** at `:17225` was corrected; the heading was not, and it is the **SOLE surviving carrier repo-wide** (zero occurrences in `SPEC.md` or the SPEC commit message). Also owed: **ADR-0294 `:17209`** still ends on the refuted *"the one that RE-PAYS the 18-caller threading tax"* claim (bracketed correction per ADR-0294's own `:17185` precedent, or an explicit written decline).

**Everything else the IMPL executes is in `PLAN.md`** — the NINE-task single-flat-row TDD spine (T1-T9), each task red-first → green → break-verified with its own pre-known red set and exit gate, plus **EIGHTEEN pinned breaks (A-R)**. Other PLAN-pinned mechanics: **49** real `resolve_test.go` callers (raw grep **50**; `:107` is a `t.Errorf` literal, `:106` is its real call — gate by EXPLICIT LINE NUMBER) · the **FULL `MetaPath`**, never `[1:]` · `descend`/`structpbValueToString` reused VERBATIM, **proven by sha256** · the `meta_bad` fuzz repoint target is **`Kind: &metadatav3.MetadataKind{}`** (present kind, oneof unset — the PARITY arm; the BRAINSTORM's proposed nil-`kind` target is itself a DEPARTURE arm), and since `config.go:233` and `:288` now emit the IDENTICAL string `kind required`, the **dispatch-verify must TAG the arms separately or the error text alone launders the result** · fixture `0117` carries **TWO clusters behind two path-prefix routes onto the SAME single backend** (deterministic, zero LB spread, `BackendCount` **1**) so it DISCRIMINATES *selected* from *only* — the limitation `0116` had to confess — with **STRING values only** and an arbitrary non-`envoy.lb` namespace · BEHAVIOR_CONTRACT **B1 and B2 edit the SAME physical line (`:688`) and must be applied as ONE `old_string`**, while **B3's `:690` is a SEPARATE line and its OWN `old_string`**.

**⚠️ THE IMPL'S FIRST LIVE RISK — T6 Step 1, the `0117` per-path span partition.** `upstream_cluster`, the obvious discriminator, is **UNUSABLE**: envoy-go emits it EMPTY (`0116` asserts it present-only). The PLAN pins **`http.url` as PRIMARY but REQUIRES dumping it on both sides FIRST** to confirm the path is recoverable before any assertion depends on it, with a **NAMED FALLBACK** (two drive batches + a poll barrier). Record which was used and why. Keep `numTotal` at **12** (split 8→4+4, 4→2+2) so the span-count and `spans_sent` assertions stay consistent (`reference_fixture_workload_constant_desync`).

**⚠️ WHAT THE PLAN DID NOT VERIFY — do not inherit false confidence.** **Nothing reference-side**: no Docker, no Envoy probes ran at the PLAN, so every reference/PGV claim rests on the SPEC's own 21-arm probe record. **T5, T6, T7 and T9 were not executed**; the `0117` fixture does not exist yet; **Breaks N/O/P/Q/R are untested**; the full differential suite was not run; Break M2's *firing* at span level is unproven (only its compile-clean half, which is the load-bearing half — V1 confirmed that swapping the 5th/6th `ResolveCustomTags` args COMPILES CLEAN and passes vet and lint, so **the compiler cannot catch a crossing and the distinctness tests are the ONLY guard**).

**Live hazards to carry:** the PRE-EXISTING `internal/cluster` `-race` flake (`TestOutlierDetector_ConcurrentEjectExactlyOnce`, `outlier_test.go:766` — isolate-re-run, do NOT re-classify) · the full-suite startup flake · the SDS init-fetch dial-budget flake · and **TWO newly-observed, UNINDEXED load flakes** in byte-identical packages, isolate-green and NOT root-caused: `internal/httpclient TestOptions_ZeroValue_NoOpDefaults` and `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery`.

**Counts at exit (re-run MECHANICALLY in the worktree; the PLAN was docs-only so all are UNCHANGED):** fixtures **118** (tail `0116-tracing-custom-tags-metadata-host`; `0117` and reference port `10117` both FREE) / fuzzers **55** / stat surface **1201** / BackendKind **38** / go.mod modules **2** (lineage figure; the single `go.mod` requires **67**) / DECISIONS tail **ADR-0295** PROPOSED (next-free **ADR-0296**).

## What THIS session does — the phase-73 IMPL

One stage per session. The IMPL **EXECUTES `PLAN.md` rather than re-deriving it from scratch — but it RE-RUNS every anchor at the IMPL tip before editing**, because a PLAN cite is not evidence (`feedback_brief_citations_not_evidence`), anchors shift (`feedback_parallel_stream_mints_fresh_drift`), and quoting is not executing (`reference_quoting_is_not_executing`).

**The owed roster:**
1. **Execute T1-T9 in order, red-first.** Each task: pre-known red recorded VERBATIM from an ACTUAL run → minimal green → breaks run with `-count=1` **AFTER committing** (`reference_break_protocol_commit_first`, `reference_differential_break_protocol_count1`) → **confirm WHICH assertion fired** (`reference_deliberate_break_wrong_assertion`) → `git restore` → re-green. **A break that does not COMPILE proves nothing** (`reference_plan_break_instructions_dont_compile`); if a break must be substituted, **its stated rationale is itself a claim** — phase 72 shipped a false one. **A break that does NOT fire is a FINDING, not a pass.**
2. **Re-run Step 0 at the IMPL tip:** the FOUR collision greps (`kindMetadataCluster` / `clusterMetaLookup` / `ClusterMetaLookup` / `clusterFilterMetadata` — all **0** at the PLAN tip; re-run anyway, `reference_spec_drafted_identifier_collision_check`) **plus the parallel-stream check** (`git diff --stat <plan-base>..master -- '*.go' go.mod go.sum test/`). Re-derive the 18-emit-site roster (**5 + 6 + 7**; ⚠️ a raw `grep -c` reads **5+7+7=19** — `h2dispatch.go:188` is a DOC COMMENT, and h3's seven are all real) and the `/cluster` and `resolve_test.go` counts by EXPLICIT LINE GATE.
3. **T8 is the six-gate and ROW 73 FLIPS `done` THERE** (ADR-0106, the SOLE leg — a SINGLE FLAT ROW, `reference_roadmap_split_phase_row_done`): gofmt / vet / build / `go mod tidy -diff` EMPTY + `go.mod`/`go.sum` no-diff (**+0 modules — ZERO new imports anywhere**) / lint / the **FULL 119-dir** differential / the layering check (`go list -deps ./internal/tracing`, **no `...`**, filter- AND cluster-free — ⚠️ **a DISCIPLINE check, NOT a cycle check; do not re-introduce the refuted "cycle guard" framing**) / `-race` on the 3 touched packages / counts / the envelope audit **pinning the SHAPE for `internal/cluster`** (one field + one accessor + one loop + **no import hunk**). **Exactly ONE new exported symbol**, by AST/`go doc -all` symbol-set diff.
4. **T9 completes ADR-0295 IN PLACE** — **APPEND** §Decision + §Consequences after the `:17229` footer (**RETAIN it**); **NO second `### Decision` heading** (verify 0 before / 1 after); no renumber; tail stays ADR-0295, next-free **ADR-0296**. Plus the four stale-claim corrections above, the pinned ROADMAP `:203` narrow (**re-verify `count == 1` first**; check (2) STAYS **3**), and the THREE stale row-cell claims at `:135` (the row's field count must stay **8**).
5. **The ONE memory write owed is neither of SPEC §13's two names** — both are ALREADY DISCHARGED (verify, do not rewrite). What IS owed: narrow `reference_host_tag_resolves_at_pick_time.md:19`, which still says *"phase 72 left **both** UNPROBED"* while the circuit-breaker arm is now EXECUTED-PARITY (S8), leaving pool-overflow the sole unprobed arm. A candidate second memory: the populate-placement hazard (a value-copying LB constructor silently zeroing a newly-added `Endpoint` field while the aliasing round-robin path stays green).

**Stage-close mechanics (the IMPL):** **ROW 73 FLIPS `in-progress` → `done`** at the T8 six-gate; **the deferred sentence narrows HERE** (the phase-57 precedent — it is narrowed at the IMPL row-done edit, at no earlier stage), per the `old_string`/`new_string` pinned at SPEC §12; **DECISIONS gains ADR-0295's §Decision + §Consequences IN PLACE**; STATE §Current rolled **IN PLACE** (lifecycle 3 → DONE; lineage re-capped at five **with its PREAMBLE updated** — the ADR-0288 rule). ⚠️ **The ADR-0288 singleton check returns 2, not 1**, for each of `next-skill:` / `lifecycle-state:` / `next-free ADR:` — the second hit is `STATE.md:7`, the RULE STATEMENT itself. Verify with `grep -n`, and never "fix" the count to 1. **next-prompt.txt rolled to the NEXT PHASE'S BRAINSTORM — the roller SELF-PICKS the subject** (smallest defensible candidate first; record the pick AND the rejected alternatives in the BRAINSTORM) per the 2026-07-12 standing directive, unless the sentinel fires. Every count re-run MECHANICALLY in the worktree, never copied.

⚠️ **When row 73 flips, sentinel check (1) GOES SILENT** — it is the LAST non-`done` row. **That is expected and is NOT a termination condition.** Checks (2) and (3) will still print (three deferred-candidate sentences; three never-opened families), so the sentinel does NOT fire and **`stop` must NOT be created**. **Run all three and record the ACTUAL output rather than predicting it — TWICE: in the worktree AND on LANDED master after the squash-push.**

Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`).

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. At the phase-72 close that put a stray commit on `master` in the MAIN checkout AND made a "final gates" run validate master's pre-row tree — GREEN while proving nothing. **Use `git -C <abs-worktree-path>` for every git command, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline) + §Project counts + the §Recent lineage (five bullets: phase-73 PLAN + phase-73 SPEC + phase-73 BRAINSTORM + phase-72 IMPL/PLAN).
2. `docs/envoy-go/phases/73-tracing-custom-tags-metadata-cluster/PLAN.md` — **§1.4 (the adversarial ledger — read this BEFORE citing anything in the PLAN as settled; two of its own instructions were refuted) and the Task 1-9 spine FIRST**, then §1 (the re-derivation ledger), §1.2 (the pinned code skeletons (a)-(i)), §1.3 (findings F1-F9), Global Constraints (incl. the break protocol and the identifier roster), and the File structure block.
3. `docs/envoy-go/phases/73-tracing-custom-tags-metadata-cluster/PROGRESS.md` — the live task ledger you FILL IN as you go (Status/Commit columns are populated at THIS stage), the mirrored adversarial record, and the cross-cutting hazards.
4. `docs/envoy-go/phases/73-tracing-custom-tags-metadata-cluster/SPEC.md` — **§3.3 (D-CTMC-SEAM, RESOLVED — read its scope note, not just its headline)**, §3.4, §3.6, §7, §8, §9, §11, §12, §13, **§16 (the SPEC-stage adversarial ledger)**.
5. `docs/envoy-go/phases/72-tracing-custom-tags-metadata-host/PROGRESS.md` — the HOST precedent, executed: the per-task adversarial record and what its breaks actually did.
6. `docs/envoy-go/DECISIONS.md` tail — **ADR-0295** (PROPOSED, §Context only — this stage COMPLETES it) + **ADR-0294** (the HOST precedent, COMPLETE) + ADR-0044 · ADR-0045 · ADR-0080 · ADR-0106 · ADR-0288.
7. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — `:688` (the `custom_tags` paragraph, B1+B2's shared line), `:690` (the phase-72 B2 departure clause this row EXTENDS), `:692`, `:745`. **RE-DERIVE all four line numbers at the IMPL tip.**
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — trust MEMORY.md's index. Load-bearing for THIS row: `reference_endpoint_metalookup_host_metadata_seam` (the accessor template — **and note the SPEC refuted its "CLUSTER re-pays the thread" claim**), `reference_host_tag_resolves_at_pick_time` (the B2 scope this row extends — **and the ONE memory write owed**), `reference_envoy_lb_namespace_not_privileged`, `reference_route_metadata_resolve_full_metapath`, `reference_cluster_tag_gated_on_pick_not_route`, `reference_structpb_tag_cross_side_string_only`, `reference_probe_must_discriminate`, `reference_a_drift_correction_is_itself_a_claim`, `reference_lifted_reject_hidden_enforcement`, `reference_verification_table_launders_wrong_cites`, `reference_cluster_race_outlier_flake`, and **`reference_bash_cwd_reset_commits_to_main`**.