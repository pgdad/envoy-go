=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

**THERE IS BANKED MID-LIFECYCLE WORK AT THIS TIP.** Phase 83 (`wasm-pause-arm-leak`) was **BRAINSTORMED (2026-08-03)**; **row 83 is `in-progress` and the phase is OPEN.** ⇒ **This session performs the phase-83 SPEC. Do NOT self-pick a new subject.**

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=115 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **CHECK (1) IS LOUD AGAIN — IT PRINTS `NOT DONE: row 83`, BECAUSE THE PHASE-83 BRAINSTORM REGISTERED ROW 83 `in-progress`.** It went silent for exactly one tip (the phase-82 IMPL). **It goes silent again only when the phase-83 IMPL flips row 83 to `done`.** ⚠️ **RUN THE DOCTORED-COPY NC ANYWAY.** It is cheap, and the moment check (1) is silent again its failure mode and its success mode become indistinguishable. The form that works (a `sed` targeting `done      ` does NOT — it misses the real `| done |` column spacing and prints nothing, which reads exactly like *"the check is blind"*):
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
```
At the phase-83 BRAINSTORM the base-tip NCs were run at `want=114` and **all three fired** (row 62 doctored ⇒ `NOT DONE: row 62`; row 82 doctored back ⇒ `NOT DONE: row 82`; `want=113` ⇒ `GATE FAIL: examined 114 data rows, expected 113`), and after the insert the `want=114` NC fired against the new 115-row file. **Run at least the row-62 and the `want` NCs. Prefer all three.**

⚠️ **`want=115` IS A LIVE FIGURE, NOT A CONSTANT.** It is the ROADMAP data-row count. **A row-adding phase MUST bump it in the same commit that adds the row.** ⚠️ **YOUR SPEC ADDS NO ROW, SO `want` STAYS 115** — the phase-83 BRAINSTORM bumped it 114 -> 115; a SPEC/PLAN does not touch `status`, and the IMPL updates it IN PLACE per §Schema `:18`.

Check (3) fails SAFE by construction. When you open a new family, add its row-summary slug to the list above.

**SENTINEL RE-RUN MECHANICALLY AT THE PHASE-83 BRAINSTORM, WITH FIVE FIRING NEGATIVE CONTROLS. IT DOES NOT FIRE. `stop` was NOT created; `ls stop` => `No such file or directory`, and it MUST NOT be created while checks (2) and (3) are still live — which they are.** ⚠️ **Re-run it yourself anyway and record what YOU see.** ACTUAL output at the base tip, recorded rather than predicted:
- **(1) SILENT** at `want=114` with the denominator printed. **Now prints `NOT DONE: row 83` at `want=115`.**
- **(2) FIVE — UNCHANGED. The THIRTY-SIXTH consecutive phase at which check (2) did not go down. STATED, not forecast.** ⚠️ **THE ANCHORS MOVED WITH ROW 83's INSERT: `:192 :202 :212 :218 :226` -> `:193 :203 :213 :219 :227`.** Any document still citing the old five is stale.
- **(3) `NEVER OPENED: gRPC` — ALONE.** NC: an invented slug fires; `WASM` and `HTTP-filters` correctly do not.
- Input measured at **230 lines / 114 data rows** at base, **231 / 115** after, so an empty result could not read as a zero result.

⚠️ **THE LEAK CHECK GOES LIVE THE MOMENT YOU WRITE A ROADMAP ROW.** The rule: **NEVER WRITE A SENTINEL'S OWN MATCHER STRING INTO A FILE THE SENTINEL GREPS.** The sentinel greps `docs/envoy-go/ROADMAP.md` — writing these strings *here* (or in `DECISIONS.md`) is safe; writing them *there* is not.
⚠️ **AND THE TWO CLASSES ARE OPPOSITE.** `<slug>-family row` is check (3)'s **PASS** condition. `deferred candidates:` / `remaining deferred (not-yet-chartered) candidates:` are check (2)'s **FAIL** conditions.
⚠️ **UPDATED AT PHASE 83: the literal `WASM-family row` now occurs TWICE in `ROADMAP.md` — at line 144 (row 82, field 7) and line 145 (row 83, field 7).** The old warning that it occurred EXACTLY ONCE is superseded; row 83 legitimately names its own family, which is the convention (`Observability-family row` occurs **53** times). **Row 82's field 7 was byte-untouched by the phase-83 BRAINSTORM.** The heading is still `### WASM host family` and the naive convention would produce `WASM-host-family row`, **which does NOT match** — that trap is unchanged.

⚠️ **ROW WELL-FORMEDNESS: THE GATE MUST BE A DISJUNCTION.** Re-run over all **115** rows at the phase-83 BRAINSTORM: **ARM-A (escape-aware, `pieces != 8` on a `|`-split) catches lines 119 and 131 ONLY; ARM-B (escape-aware trailing piece) catches line 140 ONLY** — unchanged by row 83, whose own row is clean on both arms. ⚠️ **Naive `NF==8` flags SEVENTEEN — 15 FALSE POSITIVES + 2 true — and MISSES line 140** (compensating defects cancel) ⇒ wrong in **both** directions.

⚠️ **A GATE-AUTHORING TRAP RECORDED RATHER THAN HIDDEN:** the phase-82 PLAN's first ARM-A used the wrong pipe-split threshold (`!=9` instead of `!=8`) and flagged **113 of 114 rows**. **A gate that fires on almost everything reads as THOROUGH and is WORTHLESS. Only the printed denominator exposed it.** Print your denominators.

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …`. **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Reproduced at TWELVE separate tips.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS** (wrapped prints bare paths, GNU prints `./`-prefixed). ⚠️ **Print FILE NAMES, never a count.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** — git exempts TRACKED files, and it also exits 0 for a *negated* pattern, so it cannot discriminate an un-ignore exception either; `git status` is the discriminating check. ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** (`xargs` execs a binary; `command` is a shell keyword) — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **`grep -c` ON ZERO MATCHES IS TOOL-SPECIFIC**: only `git grep -c` prints nothing; GNU and the wrapper print `0`. Both exit 1, so an exit-code gate is wrong for either. ⚠️ **AND `grep -c` COUNTS *LINES*, NOT *OCCURRENCES***. ⚠️ **`\t` IN GNU ERE IS A LITERAL `t`** while the harness function normalizes it ⇒ the trap bites exactly where gates run and is invisible interactively. **Use `-P` unconditionally.**

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`** with FOUR blind spots (an em-dash, a DOT in `http-1.1`, two LETTER-suffixed ids). Every document from phase 75 forward called it *"currently HARMLESS."* ⚠️ **Nobody fired the unsafe arm until the phase-77 PLAN. EXECUTED on scratch copies: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** ⚠️ **DO NOT restore the regex form.**

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

⚠️ **Run the union form, never one half of it.** ⚠️ **A ONE-ARM STRIP IS NOT AN NC**: `sed 's/deferred candidates://'` moves the union **5 -> 4, not 5 -> 0**. ⚠️ **The documented trap's stated MECHANISM is refuted while the trap is real** — the inflation comes from ROADMAP table rows carrying BRAINSTORM prose, not from `candidates were:` recaps. **Cite the command; do not re-inherit the explanation.**

⚠️ **CHECK (2) HAS NEVER GONE DOWN** across ~36 phases (history `0 -> 1 -> 3 -> 4 -> 5`) => **the candidates sentence is a WINDOW onto a larger deferred backlog, not an inventory of it.** ⚠️ **A stage that predicts a decrease is repeating the phase-73 error. State what your row narrows — usually nothing.**

⚠️ **`:213` IS NOT A SENTENCE ANCHOR.** It is a **45,959-char** line carrying SEVERAL deferred phrases; only ONE is the colon-terminated form the sentinel matches, at **character offset 14459 / BYTE offset 14608** (the two differ because of non-ASCII characters — both were verified at the phase-83 BRAINSTORM). Citing *"the `:213` sentence"* without the offset is ambiguous. ⚠️ **And `:227` does not carry the long phrase at all** — SHORT form only, which is exactly the Operational-tooling blindness check (2) was broadened to fix.

==============================================


## STATUS AS OF 2026-08-03: PHASE 83 (`wasm-pause-arm-leak`) IS **OPEN**. ROW 83 IS `in-progress`. LIFECYCLE-STATE **1**. THIS SESSION PERFORMS THE **SPEC**.

**What the phase-83 BRAINSTORM landed.** Base master **`8a0126d2`** (from `git rev-parse master`), branch `phase-83-brainstorm`. Docs-only: ZERO production `.go`, ZERO test `.go`. **FIVE investigation agents** on disjoint remits, each in its own DETACHED worktree with private scratch and disjoint port bands inside `43000-43399`; all five reported `git status --porcelain` = 0 lines, removed their own worktrees, and tore down every docker container **BY NAME**. `ROADMAP.md` **230 -> 231** (+1 row) · `STATE.md` rolled IN PLACE (63 -> 63) · `STATE_HISTORY.md` **446 -> 448** · `DECISIONS.md` and `BEHAVIOR_CONTRACT.md` **BYTE-UNTOUCHED**.

### ⚠️ THE ROW: PHASE 82's OWN LEAK FIX COVERED TWO OF SIX ARMS, AND ITS SAFETY ARGUMENT WAS FALSIFIED BY THE COMMIT THAT WROTE IT

Phase 82 landed **S9** because honoring `Action::Pause` without a resume path leaks a connection and a goroutine per request. It armed the watchdog and set the resume flag in the **two headers arms**. The **four remaining `abi.ProxyActionPause` arms return statuses that PARK the goroutine** (`chain.go:430` / `:474` / `:569` / `:635`) **while arming nothing** — the S9 leak verbatim.

Census, denominator SIX, symbol-anchored: `decode_headers.go:269` **flagged** · `encode_headers.go:126` **flagged** · `body.go:226` **no** · `body.go:313` **no** · `trailers.go:142` **no** · `trailers.go:199` **no**.

Proven by execution: a synthetic Pause-from-body guest stayed parked **1 s past a 250 ms watchdog window**; `ContinueStream` returned `WasmResultOk` **while resuming nothing** (the guest cannot even detect the failure); the stream released **only after the flag was hand-set**.

⚠️ **THE RECORDED MITIGATION IS FALSE.** *"ZERO guest crates call any resume hostcall"* — `l_httpcall_success/src/lib.rs:43` calls `self.resume_http_request()`, and `git log` shows it was added by **`fb93845f`, the phase-82 IMPL commit that wrote the claim.** It is false a second and more important way: **reaching the leak needs only a Pause**, and `a_body_read_only`, `b_body_mutate_passthrough` and `c_body_mutate_replace` all Pause from `on_http_request_body` and never resume.

⚠️ **THE LIVE BLAST RADIUS IS TWO ARMS ON THREE PROTOCOL PATHS.** `Run*Trailers` has **ZERO** non-test call sites (all four invocations are `_test.go`); `Run*Data` has **EIGHT**, across `connection.go` (H1), `h2dispatch.go` (H2) and `h3dispatch.go` (H3). **The body arms leak today on every protocol path; the trailers arms are LATENT** and are fixed in this row anyway, because a measured in-tree prototype would activate them (see the gRPC section).

⚠️ **A SECOND PRODUCTION DEFECT THAT THE FIX ITSELF CREATES IF WRITTEN NAIVELY.** `beginDecodePause` assigns `f.decodePauseTimer = t` under `pauseMu` **without stopping the previous handle** (`pause.go:107-109`; mirror `:132-134`). Latent today because only one arm per side can set it; with four arms flagged, a headers-pause-then-body-pause stream **drops the first timer**, which later fires and force-resumes a stream it no longer owns. **Named by no prior document.**

### ⚠️ WHAT THIS SESSION DOES — THE SPEC

Draft `SPEC.md` for phase 83, draft **ADR-0305 §Context** (⚠️ **§Decision + §Consequences are appended at the IMPL**, ADR-0044-**as-used**; ADR-0044 does NOT actually contain that discipline), roll `STATE.md` IN PLACE, roll this file forward. **`ROADMAP.md` stays BYTE-UNTOUCHED and `want` stays 115.** ⚠️ **Your SPEC re-arms the recurrence guard at 1** (`^> \*\*STATUS: PROPOSED`, currently **0**); the IMPL disarms it again. ⚠️ **Do NOT use the loose form `^> \*\*STATUS: .*PROPOSED`** — it is a FALSE-POSITIVE GATE matching COMPLETE blocks that merely narrate the word.

**The SPEC must answer four open questions, and QUESTION 1 BY MEASUREMENT, NOT BY REASONING:**
1. ⚠️ **Does arming a 10 s watchdog on the `0036` (n) body-cap arm change that scenario's timing?** `n_body_cap_exceeded` returns `Action::Pause` unconditionally from `on_http_request_body`, and today the cap branch (`body.go:143`) fires *before* dispatch on an oversized chunk. **A scenario whose whole point is indefinite accumulation now gains a bounded release. THIS IS THE ROW'S PRINCIPAL RISK.**
2. Do the two trailers arms need the flag, or a documented no-op, given the hooks have no production caller?
3. Should `defaultPauseWatchdog` stay 10 s, and on what **surviving** evidence — half its stated derivation is void (below)?
4. Where does the trap-arm logging-surface convention question get answered? It is **NOT** in this row's scope but needs a home.

**Scope as brainstormed:** S1 flag+arm the two LIVE body arms · S2 flag+arm the two LATENT trailers arms · S3 stop the previous timer before reassigning · S4 rebuild the VACUOUS census guard · S5 rewrite the false comment surface (`pause.go:8-29`, `:65-70`, `decode_headers.go:256`) · S6 re-derive `defaultPauseWatchdog` · S7 de-hardcode the test port.

**Band 350-600 net `.go`, budget ~450, 6-8 tasks — DECLARED A LOWER BOUND.** At budget **neither §6.1 trigger fires**; at a phase-81-style 3.07x repeat the LoC trigger **would** cross. **The PLAN owes an explicit §6.1 re-evaluation against a MEASURED test side, and the precedent if it crosses is RECORD, DO NOT RETRO-SPLIT.**

### ⚠️ THE ONE THAT GOT AWAY — `grpc-unary-interop`, AND ITS HARD-BLOCKED VERDICT IS **DEAD**

**This is the most valuable thing this BRAINSTORM found and it was NOT chartered, on the smallest-first rule alone.** It would retire the sentinel's **LAST** check-(3) failure.

The inherited HARD-BLOCKED verdict rested on a grep — *"`RunEncodeTrailers` has zero non-test callers"* — that is **true but was never converted into a cost measurement**. Measured, by building it:
- **+92/−11 across FIVE production files** (`h2/client.go`, `h2/stream.go`, `h2dispatch.go`, `router/router.go`, `router/router_h2.go`), plus **2** test-fake lines — the entire blast radius of the interface widening.
- `go vet ./...` clean; `go test ./internal/filter/hcm/... ./internal/filter/http/router/...` → **3 packages ok, zero regressions**.
- A real `grpc-go` client → envoy-go → a real `grpc-go` health server goes from `Internal: server closed the stream without sending trailers` to **`RPC OK: status=SERVING`**.
- **Re-derived: 12-16 tasks**, not 16-22+. ⚠️ **92 is a LOWER BOUND — the prototype ships ZERO tests and had no `-race` run.** The diff was written to session-local scratch and **will not survive; re-derive it.**

⚠️ **WHAT GENUINELY STAYS BLOCKED IS NOT TRAILERS — and both blockers are larger:**
- **An H2-upstream cluster from an H1 downstream listener returns a MEASURED 502.** `hcm/config.go:686-688` admits the H2 chain-mediated dispatch path "lands at Task 16"; Task 16 never landed. This kills `grpc_http1_bridge` **and** browser-path `grpc_web` independently of trailers.
- **The proxy FULLY BUFFERS the response body** (`H2Response.Body []byte`, `ActionResponse.Body []byte`). Measured server-streaming first `Recv`: **subject 5 s / DeadlineExceeded** vs **control 0 s / nil** and **reference 0 s / nil**. This forecloses ALL gRPC streaming and full interop conformance, and it **survives the trailer fix**.

Also settled, and recorded nowhere before: **gRPC ERROR RPCs already pass through unpatched** (`grpc-status` rides the first HEADERS block; cross-side identical) — **the seam breaks SUCCESSFUL RPCs only**. **Request trailers are not on the gRPC path at all** (clients half-close with END_STREAM on DATA), so *"the entire trailers pair is unreachable"* over-scopes by half. **The gRPC filter type-URL denominator is EIGHT, not seven** — `connect_grpc_bridge` appears nowhere in the tree — and **none of the eight has a parse-reject arm**, because the generated protos are not linked in, so the failure is a **protojson type-resolution** failure: **no seam has been pre-paid.** The gRPC charter is three lines at `ROADMAP.md:195` (`gRPC bridge, gRPC-Web, gRPC-JSON transcoding, interop conformance`) and the term *"charter-satisfying"* is written **nowhere** — it was coined in phase 77's BRAINSTORM. ⚠️ **A gRPC fixture's downstream must be TLS+ALPN h2: the differential harness does NOT pass `--allow-h2c`** (one hit repo-wide, in `h2spec_test.go:104`); precedent is `0004`.

### ⚠️ THE AUTHORITATIVE CANDIDATE POOL IS THE SENTINEL'S OWN CHECK (2), NOT THIS FILE'S PROSE

**The project's actual un-chartered backlog is the five deferred-candidate sentences check (2) prints.** ⚠️ **Re-read them at YOUR tip.** Anchors, POST-INSERT: `:193` (HTTP/3, **7** items) · `:203` (xDS, **9** — ⚠️ a naive ` + ` split gives 10; `upstream SDS (server-cert + validation_context)` carries an internal `+`; paren-aware split CONFIRMED 9 by execution) · `:213` (Observability, **3 top-level**) · `:219` (Runtime, **8**) · `:227` (Operational tooling, **3**, SHORT form).

**Candidates measured at the phase-83 BRAINSTORM and NOT chartered — costs are RE-DERIVED, not inherited:**
- **`grpc-unary-interop`** — 92 prod MEASURED / budget 130-200, tests 450-800, **12-16 tasks**, +1 fixture. **The strongest candidate in the project.**
- **`ssl.connection_error`** — ⚠️ **the router's carried "~230 net LOWER BOUND" is REFUTED**: the strictly-*smaller* landed analogue (phase 75 — one ssl counter, no classifier work, same fixture) measured **net +444**, so **444 is the floor**. Re-derived **700-900 net `.go`, 11-13 tasks**. ⚠️ **The tcp_proxy risk is CLOSED IN ITS FAVOUR** — a record-layer failure on `0110`'s tcp_proxy listener DOES book `connection_error` in the reference (`0110` confirmed `tcp_proxy`, `stat_prefix: ingress_rccf`). ⚠️ **But it is larger in KIND than recorded: the obvious `case outcomeOther: Inc()` OVER-COUNTS and diverges.** Measured reference deltas — garbage first byte **+1**, TLS1.0-only offer **+1**, malformed-DER peer alert **+1**; partial ClientHello **0**, connect-then-FIN **0**, RST mid-handshake **0**, untrusted cert **0** (books `fail_verify_error`); clean handshake **0**. All six failure shapes collapse to `outcomeOther` today ⇒ **the row is a THREE-WAY SPLIT of `outcomeOther`** on `io.EOF` / `io.ErrUnexpectedEOF` / `ECONNRESET` (⚠️ the ECONNRESET clause is REQUIRED and was NOT proven end-to-end). ⚠️ **"ZERO new fixtures" is true but not free** — `connection_error` books on NO existing `0110` arm, so it needs a **new arm 4**, which mutates the byte observable. Charset confirmed legal bare AND assembled, with a firing NC.
- **xDS config-seam strict-reject sweep** — ~40-60 prod + 150-250 test, **no fixture, no flake exposure**. **14 silently-ignored fields** enumerated by proto reflection across `SdsSecretConfig`/`ConfigSource`/`ApiConfigSource`/`GrpcService`/`EnvoyGrpc`. ⚠️ **One is a LIVE DIVERGENCE, not a silent ignore:** `stream.go:80-88` sets `Node` on the **ACK** as well as the initial request, so `set_node_on_first_message_only: true` is ignored where the reference would omit it.
- **upstream SDS (server-cert)** — MODERATE, and ⚠️ **the 3-line reject relaxation is a DECOY**: `main.go:105` builds `cluster.NewManagerWithBaseDir` **before** `boot.NewSDSProvider` at `:156`, and upstream SDS inverts that dependency. **Un-prototyped; possibly the whole row.** ⚠️ Also constrained by `NewSDSProvider`'s `seen > 1` boot-reject — **an upstream-ONLY fixture works; combining downstream+upstream does not.** ⚠️ It is nonetheless **the ONLY xDS item whose sentence text is realistically consumable**, and only if BOTH halves land, because the item names `server-cert + validation_context`.
- **`validate` nil-`sdsProvider` bug** — **~3-5 tasks**, no fixture. ⚠️ **A LIVE USER-FACING DIVERGENCE:** `--mode validate` **rejects** a config the reference validates **OK, exit 0** and that envoy-go's own boot path **accepts**. `validate/validate.go:49` threads a literal `nil` provider into `boot.Construct`; `internal/tls/config.go:436` rejects on `provider == nil`. Driven on fixture `0108`'s OWN rendered configs, with all controls firing. **Consumes no named deferred item. The cheapest real work in the Operational-tooling family.**
- **Runtime disk layer** — ~10-13 tasks, +1 fixture (3 gates), needs `fixture.HostMount{Dir:true}` (already exists, two users). **Eight reference semantics measured in one arm** (dotfiles INCLUDED; nested dirs join with `.`; dots in filenames NOT escaped, so they collide with nesting; an empty file IS a counted key; trailing `\n` STRIPPED but interior kept; symlinks followed even OUT of the subtree; **an empty directory produces NO key — asymmetric with the static layer**). ⚠️ `append_service_cluster` is fed by the **`--service-cluster` CLI flag**, not `node.cluster`, and resolves to `<symlink_root>/<subdirectory>/<service_cluster>` — **contradicting the proto doc**. ⚠️ **Its deferred item says "disk layer AND ITS OVERRIDE DIR", and `override_subdirectory` DOES NOT EXIST** in the pinned dep's `RuntimeLayer_DiskLayer` (only `SymlinkRoot`/`Subdirectory`/`AppendServiceCluster`; setting it live-rejects with `no such field`). **The item is partly unconsumable by construction.**
- **`runtime.num_layers` counts LOADED, not DECLARED, layers** — two declared (static + disk with a nonexistent root) gives `num_layers: 1`, `load_error: 0`. **Phase 77's *"held in every arm"* is true AND NON-DISCRIMINATING** — all eleven arms were static-only and a static layer always loads. ⚠️ **`internal/runtime.Snapshot` retains NO VALUES** (`type Snapshot struct { keys []string; numLayers int }`), so every knob-honoring / `/runtime` / `/runtime_modify` candidate must first build a value API.
- **The wasm http-call trap arm** — ⚠️ **NOT an implementation row: a CONVENTION DECISION.** `internal/wasm` has **0** `log.Printf` and **0** `"log"` imports across **29** non-test files (NC fires on the sibling `internal/filter/http/wasm`). Three shapes priced (per-callback counter 60-90 prod; `var logf` 5; expose `sc.trapped` + a site string 20-30) and **every one either creates the surface or duplicates the ambiguity; NONE has a clean failing-first anchor.** ⚠️ The recorded blocker is WEAKER than stated — **31** non-test `.go` files repo-wide import `"log"`, `.golangci.yml` has no `depguard`/`forbidigo`, and the `var logf = log.Printf` idiom exists in TWO packages already.

⚠️ **THE DYNAMIC `ssl` FAMILY — THE RECORDED FOUR-WAY DISPOSITION IS WRONG ON TWO ARMS AND THE WHOLE FRAMING IS WRONG.** Measured against the pinned reference (baseline first: 446 stats lines, **14** `ssl.*` names, **ZERO** dynamic):
- `ciphers` TLS1.2 charset-blocked — **HOLDS**. `ciphers` TLS1.3 passes — **HOLDS**.
- ⚠️ **`curves` is charset-BLOCKED, NOT passing** — the reference emits `ssl.curves.P-256`; `IsValidName` **false**; `NewCounter` **PANICS**. Half the reachable curve set (`{X25519, P-256}`) panics.
- ⚠️ **`sigalgs` IS EMITTED** — `ssl.sigalgs.rsa_pss_rsae_sha256: 2` under mTLS. *"Never observed"* was **probe coverage** (no client cert), not a property. It is still blocked, for a **DIFFERENT** reason: the peer signature algorithm is **UNEXPORTED** in `crypto/tls` (`testingOnlyPeerSignatureAlgorithm`) — **a Go framework gap, not a naming gap.**
- ⚠️ **ALL FOUR ARMS DIVERGE CROSS-SIDE.** On `/stats/prometheus` the reference carries the value in a **LABEL** — `envoy_listener_ssl_curves{envoy_ssl_curve="P-256"}` — where hyphens and dots are unconstrained. **A name-carrier design ships the WRONG Prometheus name on EVERY arm and COLLIDES `TLSv1.2` with `TLSv1_2`.** An `ExtractTags` value-hoist rule fixes it and **one already exists in-tree** (`name.go:144`, the `sds.` hoist). So *"IMPOSSIBLE"* is **too strong** for `ciphers`/`curves` and **too weak** for the arms called passing.
- ⚠️ **UNDECLARED PREREQUISITE: `curves` needs Go >= 1.25** (`ConnectionState.CurveID`, `api/go1.25.txt`) while `.github/workflows/ci.yml` pins `go-version: '1.23'` at **three** sites.
- ⚠️ **`internal/tls/params.go:93/:116/:129` ALREADY hold Envoy-name↔Go-ID tables.** Inverting them yields reference spellings **without a hand-written golden** (`reference_handwritten_golden_shares_author_mistake`). Caveat: `tls12CipherByName` has **6** entries and omits CBC suites Go can still negotiate.
- ⚠️ **`initial_fetch_timeout` is ALREADY FULLY LANDED**, including the `0 = unbounded` edge (`internal/xds/config.go:74`, `provider.go:57`/`:100`). Treating it as open work is stale.

### What matters for the NEXT session, in order

1. **⚠️ RE-RUN ALL THREE SENTINEL CHECKS YOURSELF AND RECORD THE ACTUAL OUTPUT; DO NOT PREDICT IT.** Check (1) now prints `NOT DONE: row 83` at `want=115` — **that is CORRECT and expected** while the phase is open. **Your SPEC adds no row: `want` STAYS 115 and `ROADMAP.md` stays BYTE-UNTOUCHED.**

2. **⚠️ EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION, NOT BY REVIEW.** The lineage is unbroken: phase-81 BRAINSTORM 14, SPEC 22, PLAN 14, IMPL 17; phase-82 BRAINSTORM 26, SPEC 23, PLAN 14, IMPL 17; **phase-83 BRAINSTORM 21 (EIGHT load-bearing).** **Budget for finding more. A SPEC that refutes nothing has not looked.** ⚠️ **And an AGENT'S refutation is a claim too** — at the phase-83 BRAINSTORM the controller **did not accept two agent claims**: an agent's *"four arms leak"* over-stated the live blast radius (the controller's own call-site enumeration showed two of the four unreachable), and an agent self-caught that a blob-level `strings` census of `proxy_continue_stream` is **NON-DISCRIMINATING** — 33 of 36 `.wasm` blobs contain the string, including `e_log_only.wasm`, because the Rust SDK emits the whole extern block regardless of use. **Only the SOURCE-level grep discriminates.**

3. **⚠️ BROKEN-GATE COUNT IS TWENTY-FIVE — ONE NEW SHAPE LANDED AT THE PHASE-83 BRAINSTORM.**
   **The twenty-fifth: A GATE WHOSE OWN DOCSTRING CLAIMS IT IS BEHAVIORAL WHEN ITS BODY IS A TAUTOLOGY OVER PACKAGE CONSTANTS.** `TestFilter_Pause_CensusOfHonoredArms` (`pause_test.go:130`) says *"This is a behavioral assertion, not a grep: it drives the real dispatch and reads the returned status."* Its body compares three pairs of package constants (`DataStopIterationAndBuffer == DataContinue`, …). **It constructs no filter, no chain and no guest, and dispatches nothing.** It is the named guard *"that keeps a future edit from silently changing the frozen body/trailers dispositions"* — **and it would not catch any such edit.** ⚠️ **A DOCSTRING ASSERTING NON-VACUITY IS EVIDENCE OF NOTHING. READ THE BODY.**
   The twenty-four carried forward, unchanged: a counter that proves a callback RAN cannot prove it returned the RIGHT ANSWER · a replacement gate that INHERITS the defect it replaces · a gate that FIRES ON ALMOST EVERY ROW reads as thorough and is worthless · a guard is a claim at TRANSCRIPTION time · two defects that CANCEL in the gate metric · an inert gate cell · a full-suite recipe without `-v` is VACUOUS · a sha256 roster desynced against a DELETED file · `gofmt -l` NEVER exits non-zero · `go doc -all <A> <B>` swallows arg2 · a `+0 exported symbols` gate over an EMPTY package reds on a CORRECT tree · a RANGE gate cannot detect anchor drift · a roster's naive `[ -f ] || continue` exits 0 on a DELETED file · a count-only stat guard PASSES a build with BOTH names wrong · a `-run` no-match exits 0 with `[no tests to run]` · a `--- PASS` tally over a package with sibling tests exceeds the fixture denominator · a stat-delta claim cannot be discharged by guards scoped to another package · a stderr-VOLUME assertion passes on the hang · `golangci-lint` misspell locale US · a harness's exit code is not the command's · a GOLDEN ROSTER that omits the family under test · a gate metric INVARIANT under the change it guards · a hand-written ENUM golden pinning WRONG wire values · a stat COUNT guard blind to a RENAME.

4. **⚠️ BEFORE ANY LINE-ADDING EDIT TO A HEAVILY-CITED DOC, ENUMERATE THE CITES YOUR INSERT WOULD SHIFT.** The phase-83 BRAINSTORM did this and PROVED it: a row appended after line 144 shifts **exactly 38 of the 117** `ROADMAP.md:<line>` cites; **79 are safe**; the NC at insertion-point-1 gives **117**. ⚠️ **ALL 38 SIT AT >= 184, WHICH IS WHY THE FIVE CHECK-(2) ANCHORS MOVED +1.** **Those 38 cites in other documents are now stale and ~35 are structurally unrepairable** (append-only records). ⚠️ **`BEHAVIOR_CONTRACT.md:<line>` cites are 196, NOT 195** — bisected: 195 at `61f4f5a3`, **196 from `71fc86d7` onward**; the previous router carried a two-commit-stale figure. Max cited line anywhere is **5078** against a **5900**-line file, so a TAIL append shifts ZERO.

5. **⚠️ LINE ANCHORS DRIFT; PHRASE AND SYMBOL ANCHORS DID NOT.** ⚠️ **`*RootVM.dispatchHttpCallResponse` DOES NOT EXIST** — it is cited in **THREE landed code comments** (`abi_callbacks.go:1101`, `stream_context.go:249`, `:254`) plus two phase-25.2 documents. **The symbol is `(*RootVM).handleHttpCallResponse`.** ⚠️ `headerMapForType` is a METHOD with an `*abiCallbacks` receiver — grep the SYMBOL, not `func headerMapForType`. **RE-VERIFY EVERY `.go` AND `.md` LINE CITE BEFORE RELYING ON IT.**

6. **⚠️ THE GATES, WITH MEASURED BASELINES FROM THE PHASE-82 IMPL (a docs-only stage does NOT re-run them and must not claim them).** Full 120-fixture differential **120/120 in 388.961 s** · **`0036` alone 18.87 s** · h2spec **53 tests, 53 passed, 0 skipped, 0 failed** — ⚠️ **ONE report, NO reference arm; the phase-82 PLAN's "106 passed" DOES NOT REPRODUCE. State your own denominator.** · `go test ./cmd/envoy-go/` **8.974 s** · touched packages ~1 s. `-race` is a SECOND run. ⚠️ **CI runs the differential at `-timeout 20m`, job `timeout-minutes: 30`** (`.github/workflows/ci.yml:60,63`).
    ```sh
    ( go test ./test/differential/ -count=1 -v > "$SCRATCH/full.log" 2>&1; echo "INNER_EXIT=$?" )
    grep -cE '^    --- PASS: TestDifferential/' "$SCRATCH/full.log"          # want 120
    grep -E  '^    --- (FAIL|SKIP): TestDifferential/' "$SCRATCH/full.log"   # want EMPTY
    grep -c  'no driver registered for fixture' "$SCRATCH/full.log"          # want 0
    grep -cE '^panic:|DATA RACE|SIGSEGV' "$SCRATCH/full.log"                 # want 0
    grep -o  'TestDifferential/[^ ]*' "$SCRATCH/full.log" | sed 's|TestDifferential/||' | sort -u \
      | comm -3 - <(ls -1 test/fixtures/ | grep -E '^[0-9]{4}[a-z]?-' | sort)  # want EMPTY
    ```
    ⚠️ The faithful dir predicate is `^[0-9]{4}[a-z]?-` — a bare `^[0-9]{4}-` gives **118**.
    ⚠️ **`go test ./test/differential/...` (with `...`) matches TWO packages and BUFFERS `-v` output.** A run that looks hung with an empty log is normal — do not kill it.
    ⚠️ **Subtest `--- PASS` lines flush only when the PARENT test completes**, so a mid-run `grep -c '--- PASS'` legitimately reads **0**. Watch `=== RUN` instead.
    ⚠️ **THE RUN ABORTS, IT DOES NOT MERELY FAIL. CAPTURE `INNER_EXIT` AND THE PANIC ARM.**
    ⚠️ **`reference_differential_log_lacks_subject_stderr` IS REFUTED FOR THIS HARNESS** — `harness.go:258` sets `cmd.Stderr = os.Stderr` and there is a SECOND site at `:615` (`io.MultiWriter`), so BOTH launch paths tee stderr; under `go test … > log 2>&1` it lands in the log. **Still true:** fixture `StatsAsserter` counter values print only inside `t.Errorf`, so a **green** run shows none of them.

7. **⚠️ THE SIX-GATE HAS A STANDING LINEAGE DEPARTURE — NAME IT, DO NOT CLAIM COMPLIANCE.** Gate **(f)** expects a `REVIEW.md`; **none since 25.3 — 86 of 123 phase dirs carry none** (37 carry one). Gate **(d)** is **VACUOUS for a row that adds none** — say "vacuous", not "green" (**55** fuzzers; ⚠️ **scoped to `-- '*.go'`, unrestricted gives 161, of which 106 are Markdown code blocks**). Gate **(c)**: proxy-wasm is INHERITED unless you run it — ⚠️ **and when you do, STATE THE DENOMINATOR: 10 of the cpp-host's 16 files (62.5%), 6 deferred.** ⚠️ **A DOCS-ONLY SPEC OWES (a)-(f) ONLY AS A POSTURE STATEMENT, NOT AS A GREEN.** Normative cites re-verified at the phase-83 BRAINSTORM: **§6.1 `:285`**, `:289` ~25-task, `:290` ~1500-LoC, **`:291` BLANK**, `:292` third trigger; **§6.2 `:294`**; **§6.3 `:304` is the HEADING and `:306` is the PHRASE**; **§7.5 `:357`, gates `:360-365`, close `:367`**. ⚠️ **`/BOOTSTRAP_PROMPT.md` WITH A LEADING SLASH RESOLVES NOWHERE** — the file is `<repo>/BOOTSTRAP_PROMPT.md` (**522** lines). ⚠️ **`docs/superpowers/plans/` CONTAINS NO FILE OF THAT NAME** — the 1024-line copy is `2026-04-21-envoy-go-bootstrap-prompt.md`, and its offsets are **NOT a constant shift** (§6.1 Δ+197, §7.5 **Δ+228**). **Open the repo-root file.**

8. **⚠️ ADR + DOCUMENT CONVENTIONS.** **The DECISIONS tail is `ADR-0304`, STATUS `COMPLETE`. Next-free is `ADR-0305` — YOUR SPEC DRAFTS ITS §Context.** **303** `^## ADR-` headings, ids **0001-0304**, ⚠️ **exactly one gap at ADR-0209** — ⚠️ **the 303-vs-0304 arithmetic is CONSISTENT *because of* the gap, so "next-free = headings + 1" gives ADR-0304 and COLLIDES. DO NOT "FIX" IT.** `DECISIONS.md` **17858** lines. STATUS census **17** (matcher `^> \*\*STATUS: `, a BLOCKQUOTE; ⚠️ `^\*\*STATUS:\*\*` matches NOTHING and reads as a false zero). Retained italic footers **10**. ⚠️ **`^---$` is 216 and must stay 216.** ⚠️ **CARRY NO WHOLE-FILE GREP COUNT IN AN ADR** — that species self-falsified in ADR-0296 ¶3 and ADR-0302 ¶11. ⚠️ **ADR-0044 does NOT contain the §Context-at-SPEC discipline**; write **ADR-0044-as-used**.

9. **⚠️ A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX — the TWENTY-NINTH consecutive stage at which this held.** The index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. **Still live:** `reference_sds_init_fetch_timeout_dial_budget_flake` (**TWO packages** — ⚠️ **and the upstream-SDS candidate touches BOTH**) · the pre-existing `internal/cluster` `-race` outlier (`TestOutlierDetector_ConcurrentEjectExactlyOnce`) · `internal/httpclient TestOptions_ZeroValue_NoOpDefaults` · `internal/filter/http/wasm` hardcodes port **42552**. ⚠️ **UPDATED AT PHASE 83: the TIME-WAIT half of the 42552 claim is REFUTED** — three consecutive `-count=1` runs all PASSED while TIME-WAIT entries accumulated to 3, because Go's `net.Listen` sets `SO_REUSEADDR`. **Only a LIVE listener collides** (parallel session). ⚠️ **The driver-owned receiver port race did NOT fire at this tip — that is not evidence it is fixed.**

10. **⚠️ CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE — and NEVER tear down a container this session did not create**, by name or otherwise. The phase-83 BRAINSTORM's five agents created containers only under `p83b-a1-*` … `p83b-a5-*` plus per-agent networks and removed **exactly those, BY NAME**; no image or ancestor filter was used at any point. ⚠️ **A trap worth carrying: `kill` on a `go run` parent leaves the COMPILED CHILD holding the socket**, which produced one false failing measurement before it was caught.
    ⚠️ **MACHINE-STATE CHANGES DELIBERATELY LEFT IN PLACE (from phase 82): `rustup toolchain 1.94.0-x86_64-unknown-linux-gnu` + its `wasm32-wasip1` target** (the only toolchain that reproduces the vendored `0036` blobs — PINNED in that fixture's `scripts/README.md`, *"not 'or newer'"*; the link step fails on 1.96.0), **and a `1.96.0` toolchain** from a probe. `rustup default` unchanged (`stable`). ⚠️ **The pinned toolchain is NOT on phase 83's critical path** — the `0036` (n) re-run uses the already-vendored blob unchanged, and the row's new arms use the existing Go `fixBuildModule` vocabulary (`proxy_on_request_body` has the same `(i32,i32,i32)->i32` signature as `proxy_on_request_headers`), so **NO new Rust blob is needed.**

11. **Counts at this tip (re-run MECHANICALLY in your worktree; never copy them).** ⚠️ Use GNU `command grep` or `git grep` repo-wide — but ⚠️ **NOT `command grep` inside `xargs`.**
    fixtures **120** (next-free **0119**; naive `^[0-9]{4}-` gives **118**) / fuzzers **55** (⚠️ `-- '*.go'`-scoped; unrestricted 161) / internal packages **73** (⚠️ **`ls -d internal/*/` gives 29 — that is TOP-LEVEL DIRECTORIES, not packages; use `grep -rl --include='*.go' '' internal/ | xargs -n1 dirname | sort -u | wc -l`**) / phase dirs **123**, of which **37** carry `REVIEW.md` and **86** do not / **BackendKind tail 38** over **39** declarations (do NOT "fix" 38 to 39) / guest crates **35**, **36 blobs** (⚠️ **one vendored blob has no crate source** — 35 `Cargo.toml`, 35 `.rs`, 36 `.wasm`) / **`DECISIONS.md` 17858**, **303** headings, tail **ADR-0304 COMPLETE**, next-free **ADR-0305**, retained footers **10**, `^---$` **216**, STATUS census **17** / **`ROADMAP.md` 231 lines / 115 data rows** (⚠️ a numeric-id regex gives **113**, silently dropping `28.1a`/`28.1b`) / **`BEHAVIOR_CONTRACT.md` 5900** (⚠️ **max cited line anywhere is 5078**) / **`STATE.md` 63** / **`STATE_HISTORY.md` 448, bullet-anchored entries 174** / ⚠️ **`ROADMAP.md:<line>` cites 117** / ⚠️ **`BEHAVIOR_CONTRACT.md:<line>` cites 196**.
    ⚠️ **THE "NEXT-FREE REFERENCE PORT" CONTEST IS DISSOLVED, NOT RESOLVED.** Measured occupancy is **39 distinct ports in FAMILY BANDS** — `10000-10001`, `10012-10013`, `10131-10140`, `10443-10447`, then `12345 / 15000-15011 / 15104 / 18000-18002 / 18007 / 19000 / 19999`. The 104xx band tops out at **10447**. **NEITHER `10119` NOR `10450` is bound**, so both prior figures are "free" and the choice is BAND-DRIVEN (`reference_differential_fixture_port_convention`). **There is no single next-free port to contest.** ⚠️ Note the xds-sds family runs `10443/10444/10445`, and `0110`'s driver already cites `listener.0.0.0.0_10446` — check before claiming `10446`.
    ⚠️ **STILL CONTESTED, SO NO NUMBER IS CARRIED:** the `STATE_HISTORY.md` archive-gap total · production `stats.IsValidName` GUARD SITES **25** vs a naive line-grep's **56** · the stat-surface total **1205**, which is DOC-SOURCED with a known-discontinuous chain (**only the DELTA is ever asserted**; pick a matcher, state it, assert the delta, never the absolute).
    ⚠️ **DO NOT SOURCE ANY COUNT FROM `STATE.md` §Project.** It is **NINE PHASES STALE** (self-declared *"at this phase-76 IMPL close"* while presented as *"(Live…)"*) and wrong on at least three axes. **Anchor on §Current. Do NOT "fix" §Project.**

12. **⚠️ DOCUMENTARY DEFECTS — carried forward, still deliberately NOT fixed.**
    - ⚠️ **`*RootVM.dispatchHttpCallResponse` does not exist** — three landed code comments plus two phase-25.2 documents. **NEW at phase 83.**
    - ⚠️ **A FOURTH false non-test comment about the trailers seam: `internal/filter/http/router/router.go:285`** claims *"Called by HCM dispatch AFTER chain.RunDecodeHeaders + RunDecodeData + RunDecodeTrailers complete"* — `RunDecodeTrailers` is never called. (`connection.go:300` carries a softer fifth.) Joins the two already recorded at `connection.go:565` and `h2dispatch.go:503`. **NEW at phase 83.**
    - ⚠️ **`pause.go:65-70`'s watchdog UPPER-BOUND rationale is HALF VOID** — it says scenario (l) *"NEVER calls `resume_http_request`"*; lines 40-43 of that guest do exactly that. **This is one of the TWO measured constraints pinning `defaultPauseWatchdog = 10s`.** Phase 83 S6 owns it. **NEW at phase 83.**
    - ⚠️ **`ROADMAP.md`'s Operational-tooling cell cites `github.com/esalaine/envoy-go/validate`** — the module is `github.com/pgdad/envoy-go` (`go.mod:1`). **NEW at phase 83.**
    - ⚠️ **The header-map enum values are STILL WRONG in the append-only phase-25.1 SPEC record** (`docs/envoy-go/phases/25.1-.../SPEC.md:481-484`). Row 82 fixed the two `.go` carriers; the doc record is RECORDED, not fixed.
    - ⚠️ **`internal/wasm/doc.go:219` says "ABICallbacks 13->20 methods"** — the interface has **21**.
    - ⚠️ **`internal/filter/http/wasm/root_abi_callbacks.go:43` names a type `allCallbacksNoOp` that DOES NOT EXIST.**
    - ⚠️ **All three cpp-host cites in `http_call.go`'s header comment are WRONG for v1.37.2.**
    - ⚠️ **cancel-at-destruction is a DEPARTURE documented as PARITY**; **envoy-go passes the wrong `plugin_context_id`**; **there is NO cap on the http-call response body** (unbounded `io.ReadAll`).
    - ⚠️ **`internal/filter/http/wasm/trailers.go` is internally contradictory in three tenses** and its capture is a literal no-op. **The type-1/3 trailer map surface is DEAD** — re-confirmed verbatim at phase 83 (`abi_callbacks.go:223-225` sends types 1/3 to `default: return nil, false, false`).
    - ⚠️ **`ROADMAP.md` row 82's summary carries TWO wrong-on-arrival claims** — `abi_callbacks.go:186-192` (the method was `:178-191`) and *"handles only map types 0 and 1"* (it handled **0 and 2**). **INVARIANT-BLOCKED** by §Schema `:18` ⇒ **recorded, not fixed**. Row 82's field 7 is **1998 chars UNTRIMMED / 1996 TRIMMED** and byte-stable across all four of its stages — ⚠️ **the lineage figure 1998 is the UNTRIMMED convention; do not "correct" it.**
    - ⚠️ **`BEHAVIOR_CONTRACT.md:1967`** asserts a blocker its own successor phase KILLED (`ssl.connection_error`).
    - ⚠️ **`DECISIONS.md` ADR-0301 §Decision (b) says *"has failed twelve top-level detectors"*** — PRESENT TENSE inside immutable ADR text, factually wrong since row 80 landed.
    - **`ROADMAP.md` malformation: three rows lose summary content in GFM render** (lines 119, 131, 140), INVARIANT-BLOCKED => **defensible as a GUARD with the DISJUNCTION gate, not a fix**. ⚠️ **There is NO doc-invariant test anywhere in this repo.** The reusable precedent is `test/differential/harness_test.go:123-137`'s `runtime.Caller(0)` -> repoRoot idiom.
    - **`STATE.md` §Project counts self-contradicts §Current and is FROZEN at the phase-76 IMPL close.** **Anchor on §Current. Do NOT "fix" §Project.**
    - **`BEHAVIOR_CONTRACT.md`'s `wasm.` arm is called a *"NEW SN9 flattening rule"*, which COLLIDES with ADR-0118's actual SN9.**
    - ⚠️ **The two `//nolint:gosec` directives row 80 added are INERT** — `gosec` is not among the 9 linters `.golangci.yml` enables (`disable-all: true`; `govet errcheck staticcheck unused ineffassign gofmt goimports misspell revive`; golangci-lint **v1.64.8**). ⚠️ **`nolintlint` is NOT enabled either.** ⚠️ **And neither is `depguard` or `forbidigo`** — relevant to the trap-arm logging-convention question.
    - **`golangci-lint-action@v6.5.2` emits a Node-20 deprecation warning in CI.** Deliberately NOT bumped: v7+ requires golangci-lint **v2** while the repo pins **v1.64.8**. **A warning, not a failure.**
    - ⚠️ **`ROADMAP.md` §Schema `:18` says *"Sub-phases get their own rows"* while practice puts split legs in the parent's `sub-phases` PROSE.** **ZERO dotted rows exist above 32.2**. **Record the tension; do not relitigate.**
    - **Production-DEAD trailer code** in four filters (bar-dependent: bodies >2 lines give wasm/admission_control/envoygotest/lua = **4**; **6** if buffer + extproc's 3-line bodies count; denominator **38 trailer methods across 23 non-test files**) · **`DECISIONS.md`'s wasm module-function roster** enumerates 24 keys, **8 of which do not exist in `internal/`** · **one defective `freeTCPPort`** (`test/conformance/h2spec/h2spec_test.go:219`), which **cannot** abort the differential — ⚠️ **there are THREE live `freeTCPPort` definitions, not one** (`cmd/envoy-go/main_test.go:180`, the conformance one, and the BANDED rewrite at `test/differential/harness_test.go:246`+`:292`), and **none is the right idiom for an IN-PROCESS bind** — use `net.Listen("tcp","127.0.0.1:0")`, which has no race window at all.
    - ⚠️ **`internal/filter/network/rbac/rbac.go:50` contains the token `F2`** as an unrelated **phase-26 fork label**. **Do not use `F2` as a grep anchor in that package.**

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — take it from `git rev-parse master`, **NOT from a SHA quoted in this file**. ⚠️ **AND THE BARE SUBJECT-LOCATE GREP IS CONTAMINATED BY THIS FILE'S OWN PROSE ABOUT IT** — `git log` greps the whole commit message and router commits discuss the phase in their BODIES, so **the sentence warning about the grep satisfies the grep.** ⚠️ **USE THE SLUG-ANCHORED FORM**, which is stage-exact:
```sh
git log --grep '^phase 83 (wasm-pause-arm-leak)' --format='%h %s'   # 1 at this tip: BRAINSTORM
```
Measured with both controls: an invented slug returns **0**, and the completed phase-82 form returns **4**. **Take the NEWEST line (`| head -1`) and verify the count before relying on it.** Subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** (`reference_parallel_subagents_private_scratch`) and they **must tear down docker containers BY NAME, never by an `ancestor`/image filter** (`reference_parallel_agents_shared_machine_namespaces`). ⚠️ **THERE ARE TWO RESERVED PORT BANDS — band private ports outside BOTH `20000-31007` (subject blocks) AND `11000-14999` (backends), and also clear of the static fixture ports `10000-10447` / `15000-15011` / `15104` / `18000-18007` / `19000` / `19999`.** ⚠️ **AND NOW ALSO CLEAR OF `42552`, which `internal/filter/http/wasm` hardcodes.** The `42000-43399` band has been used safely (phases 80, 81, all four phase-82 stages, and the phase-83 BRAINSTORM's five agents at `43000-43399`). ⚠️ **If two agents share ONE worktree they must stage by EXPLICIT PATH** and **must NOT run an unscoped `git restore`**. ⚠️ **PREFER ONE WORKTREE PER AGENT OFF A COMMON BASE** — and note the proven pattern: **independent streams in DETACHED worktrees cherry-pick back CLEAN when their file sets are disjoint**, while an order-constrained core chain shares ONE worktree sequentially. Per-task `gofmt` + `golangci-lint` on touched packages (`feedback_pertask_gofmt_lint`). ⚠️ **`gofmt -l` NEVER exits non-zero — gate on OUTPUT.** ⚠️ **`golangci-lint` runs `misspell` with `locale: US`.** ⚠️ **AN EMPTY LINT RESULT IS NOT EVIDENCE THE LINTER LOOKED — inject a British spelling, confirm it fires, restore byte-identically.** ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch, and note `-o` precedes the package path.**

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. ⚠️ **ASSUME IT FIRES EVERY SESSION — it has been treated as live for THIRTY-THREE consecutive sessions and it FIRED, OBSERVED, THREE TIMES at the phase-83 BRAINSTORM** (`Shell cwd was reset to /home/esa/git/envoy-go`). **Use `git -C <abs-worktree-path>` for every git command, prefer absolute paths over `cd`, put any needed `cd` FIRST in a compound command, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN.** At the phase-83 BRAINSTORM five agents wrote throwaway `.go` probes, docker probes, a full 5-file trailers prototype and a squatter-socket harness, ran them, reverted them, and each confirmed `git status --porcelain` = 0 lines. **Every agent worked in a DETACHED worktree precisely so a failed revert could not dirty a branch — copy that.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline — ⚠️ **anchor greps on `^- **<field>`**; all seven fields verified singleton at the phase-83 BRAINSTORM close). ⚠️ **§Project counts SELF-CONTRADICTS §Current and is NINE PHASES STALE** — do not anchor on it and do not fix it.
2. `docs/envoy-go/phases/83-wasm-pause-arm-leak/BRAINSTORM.md` — **the scope (§4), the four open questions (§4.1), the declared LOWER-BOUND cost posture (§4.3), and the 21-item refutation ledger (§5).**
3. `docs/envoy-go/ROADMAP.md` — **§Schema first** (`:18` carries BOTH the update-in-place invariant AND *"Sub-phases get their own rows"* on ONE line), then **the five deferred-candidate paragraphs at `:193 :203 :213 :219 :227`**, then the family headings. **You add NO row; `want` STAYS 115.**
4. `BOOTSTRAP_PROMPT.md` — **at the REPO ROOT, no leading slash.** §6.1 `:285`, §6.2 `:294`, §6.3 `:304` heading / **`:306` phrase**, §7.5 `:357-367`. ⚠️ **Not the 1024-line copy under `docs/superpowers/plans/`.**
5. `docs/envoy-go/DECISIONS.md` — **ADR-0304** (the immediately-prior completion, for the append shape) · **ADR-0288** (roll-IN-PLACE and the archive rule) · **ADR-0044** as-used · **ADR-0106** (the sole-leg property).
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — especially **`reference_measured_prototype_is_a_lower_bound`** (⚠️ **fired on TWO CONSECUTIVE ROWS; this row's own band is declared a lower bound because of it**), **`reference_probe_must_discriminate`**, **`reference_gate_command_negative_control`**, **`reference_code_comment_not_evidence`**, **`reference_a_drift_correction_is_itself_a_claim`**, **`reference_empty_output_is_not_a_zero_result`**, **`reference_independent_probes_can_share_a_blind_axis`**, **`reference_probe_input_is_a_claim`**, **`reference_vacuous_break_modes`**, **`reference_sentinel_matcher_string_self_clears`**, **`reference_deferred_candidate_cost_restale`**, **`reference_differential_fixture_port_convention`**, **`feedback_brief_citations_not_evidence`**, `feedback_git_worktrees`, `feedback_execution_style`, and **`reference_bash_cwd_reset_commits_to_main`**.
