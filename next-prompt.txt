=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

**THERE IS BANKED MID-LIFECYCLE WORK AT THIS TIP.** Phase 81 (`stats-name-charset-guards`) was **CHARTERED and BRAINSTORMED**, **SPEC'd**, and **PLANNED**, all at 2026-07-31; **row 81 is `in-progress`**. ⇒ **This session performs the phase-81 IMPL.** Do NOT self-pick a new subject.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=113 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **`want=113` IS A LIVE FIGURE, NOT A CONSTANT.** It is the ROADMAP data-row count. **A row-adding phase MUST bump it in the same commit that adds the row** — otherwise every later session gets `GATE FAIL: examined 114 data rows, expected 113`. That noise is deliberate: a wrong denominator must be loud, because a silent one is how check (1) failed in the first place. **The phase-81 BRAINSTORM bumped 112 -> 113; the SPEC and the PLAN both left it at 113.** ⚠️ **YOUR IMPL ADDS NO ROW, SO IT MUST LEAVE `want` AT 113** — it flips row 81's STATUS, it does not add a row.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. When you open a new family, add its row-summary slug here.

**SENTINEL RE-RUN MECHANICALLY AT THE PHASE-81 PLAN, with FIRING negative controls. IT DOES NOT FIRE. `stop` was NOT created; `ls stop` => `No such file or directory`, and it MUST NOT be created.** ⚠️ **Re-run it yourself anyway and record what YOU see** — a prior run's result is not evidence for the current tip. ACTUAL output, recorded rather than predicted:
- **(1) prints `NOT DONE: row 81`** at `want=113` — correct; the row is `in-progress` and will stay so until YOUR stage flips it. NCs, ALL fired: `want=112` => `GATE FAIL: examined 113 data rows, expected 112`; row 81 doctored to `done` on a scratch copy => **SILENT**; row **62** doctored on that same copy => `NOT DONE: row 62`.
  ⚠️ **THE ROW-62 NC DID NOT LAND ON FIRST ATTEMPT AT THE PLAN.** A `sed` targeting `done      ` missed the real `| done |` column spacing and printed **nothing** — which reads exactly like *"the check is blind."* Caught only by inspecting the doctored field BEFORE trusting the result, then redone with `awk`. **`reference_gate_command_negative_control`, firing on the controller in the session's FIRST gate.** ⚠️ **YOUR IMPL FLIPS ROW 81, SO CHECK (1) GOES SILENT — which is the ONE stage where a doctored-copy NC is MANDATORY**, because silence is otherwise indistinguishable from a broken check.
- **(2) FIVE — `:191 :201 :211 :217 :225`** — **UNCHANGED. The THIRTIETH consecutive phase at which check (2) did not go down. STATED, not forecast.**
- **(3) `NEVER OPENED: gRPC` and `NEVER OPENED: WASM`** — unchanged. NCs: an invented slug => `NC NEVER OPENED: ZZZ-nonexistent`, while the REGISTERED slug `Observability` correctly printed nothing.
- Input measured at **229 lines / 113 data rows**, so an empty result could not read as a zero result.

⚠️ **THE LEAK CHECK IS LIVE AT ANY STAGE THAT WRITES A ROADMAP CELL — AND IT RE-ARMS AT YOUR STAGE.** The rule: **NEVER WRITE A SENTINEL'S OWN MATCHER STRING INTO A FILE THE SENTINEL GREPS.** The sentinel greps `docs/envoy-go/ROADMAP.md` — writing these strings *here* is safe; writing them *there* is not. The leak is proven to **SILENCE CHECK (3) BY MENTION** (reproduced live at the phase-81 BRAINSTORM: doctoring row 81's cell to say `gRPC-family row` made `gRPC` stop printing while `WASM` kept printing). **`grep` cannot tell a mention from a use.** **The SPEC and the PLAN both correctly said the check was INAPPLICABLE — they wrote no ROADMAP cell. YOURS DOES.**
```sh
# the row's cell only, not the whole file
awk -F'|' '/^\| *81 /' docs/envoy-go/ROADMAP.md > "$SCRATCH/row81.txt"
grep -coE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' "$SCRATCH/row81.txt"  # want 0
grep -oiE '(HTTP-filters|Network-filters|Load-balancing|Upstream-robustness|Observability|Operational-tooling|HTTP/3|gRPC|xDS|Runtime|WASM)-family row' "$SCRATCH/row81.txt" | sort -u
# ^ every slug printed MUST already be registered elsewhere in ROADMAP.md. Row 81 prints `Observability-family row`
#   ONLY, which is registered elsewhere -- a USE, not a mention.
```
⚠️ **ROW WELL-FORMEDNESS: THE GATE MUST BE A DISJUNCTION.** Re-executed over all **113** rows: **ARM-A (escape-aware `NF!=8`) catches 57 (NF=9) and 69 (NF=10) ONLY; ARM-B (escape-aware trailing-piece) catches 78 ONLY. Neither arm alone suffices**, and **row 81 is flagged by neither**. ⚠️ **Naive `NF==8` flags SEVENTEEN rows — `16 17 18 18.1 19 20 22.1 24.2 25.1 43 48 49 55 57 62 69 74` — of which FIFTEEN are FALSE POSITIVES and TWO (57, 69) are TRUE.** Row **78 is absent from it** (compensating defects cancel). ⇒ the naive form is wrong in **both** directions: **15 FP + 1 FN**.

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files --hidden -I --exclude-dir=.git …` in ugrep-compatible mode. **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. The delta is exactly this file, reproduced at six separate tips:
```sh
grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort         # wrapped: ABSENT
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                    # direct access WORKS
```
⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING THE TWO ARMS.** The wrapped arm prints bare paths while GNU prints `./`-prefixed ones, so a naive `comm` reports **every file as "only in GNU"** and the real delta is invisible. ⚠️ **Print the FILE NAMES, never a count.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** — it reports `next-prompt.txt` as *"not ignored"* (exit 1) because **git exempts TRACKED files from `.gitignore`** while ugrep does not. **Reliable forms: `command grep -r`, `git grep`, an explicit file list, or a recursive grep handed the FILE path directly.**

⚠️ **AND `grep -c` ON ZERO MATCHES IS TOOL-SPECIFIC.** Only **`git grep -c`** prints nothing. **GNU `grep -c` prints `0`** (and `path:0` per file on a list or `-r`), as does the ugrep wrapper. **Both still exit 1**, so an exit-code gate is wrong for either.

⚠️ **AND `\t` IN GNU ERE IS A LITERAL `t` — BUT THE TRAP FIRES IN THE OPPOSITE DIRECTION FROM THE DOCUMENTED WARNING.** Measured at the phase-81 PLAN: the harness `grep` **shell function** NORMALIZES `\t` and returns **126** for `-cE '^\t_ '`, while **`command grep -cE '^\t_ '` returns 0**. ⇒ **the trap bites exactly where gates run** (CI, scripts, `git grep`) and is **invisible interactively**. **Use `-P` unconditionally.**

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`.** Every document from phase 75 forward recorded its blind spot — **109 data rows / 105 matched / FOUR misses** at an **em-dash** in an "after" cell, a **DOT** in the slug `http-1.1`, and two **LETTER-suffixed** ids `28.1a`/`28.1b` — and every one of them called it *"currently HARMLESS, since all four missed rows are `done`."* ⚠️ **Nobody fired the unsafe arm until the phase-77 PLAN. EXECUTED on scratch copies: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** ⚠️ **DO NOT restore the regex form.**

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

The old check (2) matched ONLY the long phrase and could not see the **Operational-tooling** family's three genuinely un-chartered items, recorded in the **SHORT** form. Fixed at the phase-77 BRAINSTORM, at the MATCHER. ⚠️ **Run the union form above, never one half of it.** ⚠️ **A ONE-ARM STRIP IS NOT AN NC FOR CHECK (2)**: `sed 's/deferred candidates://'` moves the union count **5 -> 4, not 5 -> 0**, because the long form does not contain the short substring at all. Re-executed at the PLAN: **4**.

⚠️ **THE DOCUMENTED TRAP'S STATED MECHANISM IS REFUTED, WHILE THE TRAP IS REAL.** `reference_sentinel_deferred_sentence_live_vs_historical` says a naive `grep -c 'candidates:'` inflates *because of* the `candidates were:` recaps. It does inflate — but **not for that reason**: `candidates were:` does not contain that substring **at all**. The inflation comes from **ROADMAP table rows carrying BRAINSTORM prose**. **Cite the command; do not re-inherit the explanation.**

⚠️ **AND THE MEASURED RECORD: CHECK (2) HAS NEVER GONE DOWN** across ~30 phases (history `0 -> 1 -> 3 -> 4 -> 5`). => **the candidates sentence is a WINDOW onto a larger deferred backlog, not an inventory of it.** ⚠️ **A stage that predicts a decrease here is repeating the phase-73 error. State what your row narrows — usually nothing — rather than forecasting.** **Row 81 narrows NOTHING**, and the SPEC and PLAN together ADDED five named deferred candidates without touching the sentence.

==============================================


## STATUS AS OF 2026-07-31: PHASE 81 (`stats-name-charset-guards`) IS **BRAINSTORMED, SPEC'd AND PLANNED**. ROW 81 IS `in-progress`. THIS SESSION PERFORMS THE **IMPL** — THE STAGE THAT CLOSES THE ROW.

**What the phase-81 PLAN landed.** Docs-only, base master **`82e425ac`** (taken from `git rev-parse master`; at that tip the SPEC squash WAS the master tip, so the usual "quoted SHA sits below the tip" hazard did not bite — it was still re-derived rather than assumed), branch `phase-81-plan`. **ZERO production `.go`, ZERO test `.go`.** New: `docs/envoy-go/phases/81-stats-name-charset-guards/PLAN.md` (**628 lines**). Modified: `PROGRESS.md` (**+115**, the `# PLAN record`), `STATE.md` (§Current rolled IN PLACE, §Recent add + evict, **64 -> 64**), `STATE_HISTORY.md` (**434 -> 436**), this file. **`ROADMAP.md`, `BEHAVIOR_CONTRACT.md` and `DECISIONS.md` BYTE-UNTOUCHED — verified by `git diff --stat` EMPTY on all three AND by `sha256sum` against the base, against a firing NC on `STATE.md`.**

### ⚠️ THE FIVE THINGS THE PLAN PROVED THAT CHANGE THE IMPL

1. ⚠️ **THE BARE-VS-ASSEMBLED QUESTION IS DECIDED BY *SEGMENT POSITION*, AND THE SPEC MEASURED IT AT THE ONE POSITION WHERE THE ANSWER IS DEGENERATE.** SPEC §2.8 measures `network/rbac` — where the operator token **LEADS** the assembled name — concludes *"MIS-ACCEPTS = 0, OVER-REJECTS = 1"*, and §3 promotes that template row-wide. Controller-executed cross-product over 9 tokens: **LEADING disagrees on 1** (trailing dot only); **INTERIOR disagrees on 5** (`0policy`, `policy.`, `.policy`, `9`, `""`). ⚠️ **EIGHT OF THE NINE SOURCES ARE INTERIOR** — only B (zookeeper) is leading. **Three agents hit this independently on three different sources.** ⇒ **PROBE THE ASSEMBLED NAME AT ALL NINE**, per ADR-0065 §Consequences (b) (`DECISIONS.md:2379`). A bare probe at an interior position would **boot-reject four config shapes that today boot, serve and register a valid counter**, and would make **F1 and F2 disagree**. Two consequences are **deliberate, not discovered**: the guards inherit the interior-empty-segment hole (ACCEPT-pins are owed, naming SPEC §13.1), and mongo ends up more permissive than its three bare-guarded siblings.
2. ⚠️ **D-81-EMPTY'S PREMISE COLLAPSES.** Three-arm cross-product: unconditional **BARE** => rbac **FAIL (26 top-level)**, wasm/compressor `ok`; unconditional **ASSEMBLED** => all `ok`; skip-if-empty => all `ok`. Under the assembled template an empty token yields a **valid** name at C/G, and **D/E never see an empty segment** because `namespacePrefix("")` substitutes the literal `"rbac"` (`rbac.go:507`). **§8 reasoned about a BARE token and applied it to an ASSEMBLED template.** **Four of its seven cited tests never reach a production entry point** (`buildFromAny`, `newFilterStats`, `registerPluginConfigName`, `unregisterPluginConfigName`), and its H row is miscited (**ONE** bare `RateLimit{}`, `fuzz_test.go:380`). ⇒ **Keep skip-if-empty for legibility; do NOT claim it is load-bearing; OMIT it at A and B** (empty already rejected upstream).
3. ⚠️ **§7's SINGLE SHARED TABLE IS NOT CONSTRUCTIBLE.** Five of six guard targets are **package-private across five packages**, and **F1's and F2's tables sit in different packages**. A Go test shares a table only within a package. ⇒ **the audit is ~7 near-identical per-package drivers**, and the keystone pays for the charset sweep **twice**. The disposition still stands; the **cost model changes**.
4. ⚠️ **A SECOND CORPUS RED, AND THE DIFFERENTIAL ABORTS.** `wasm/abi_callbacks_test.go:1453` sets `&compiledConfig{pluginName: "my-plugin"}` — the source-C field itself, `IsValidName` **false**. **AMBER only because guard C sits at the `pc.GetName()` boundary; it becomes a HARD RED the moment the guard relocates**, which §7's shared-driver work makes tempting. Separately, the first full differential **ABORTED at fixture 84 of 119** (`0083`'s own ALS receiver, `bind: address already in use`), **35 fixtures silently never ran**, and the notification reported *"exit code 0"* against `INNER_EXIT=1`.
5. ⚠️ **THE COST IS MEASURED, NOT MODELLED — AND THE ROW IS SMALLER THAN ITS OWN SPEC BAND FLOOR.** Every figure built, run, and read off `git diff --numstat`: A **59** · B **59** · H **72** · C **99** · D **38** · E **38** · G **86** · F1 **178** · F2 **96** = **725 net** (guard 165 / test 560). SPEC's F1 174 **CONFIRMED at 178**; its F2 **~50 REFUTED at 96**; its **~75/source model wrong in BOTH directions**. **BAND 750-1000, BUDGET ~850, TWELVE tasks. NEITHER §6.1 trigger fires** (2.1× tasks, 1.5-2.0× LoC). **NO SPLIT.**

### ⚠️ WHAT THIS SESSION DOES — the phase-81 IMPL

**One stage per session.** Execute the PLAN's **twelve tasks**. This is the stage that **flips row 81 to `done` and CLOSES the phase**. It writes production `.go`, test `.go`, `DECISIONS.md` (ADR-0303 completion), `BEHAVIOR_CONTRACT.md` (the departure paragraph), `ROADMAP.md` (the status flip), plus `PROGRESS.md`, `STATE.md`, `STATE_HISTORY.md` and this file.

**THE FIVE ATOMICITY / PLACEMENT CONSTRAINTS — THESE ARE NOT STYLE:**

| constraint | why it must not be relaxed |
|---|---|
| **T7 (F1 boot reject) + T8 (F2 `Inc` backstop) in ONE commit** | shipping (a) without (b) leaves the process crashable by the very config class the row exists to close |
| **T3's `tenant-foo` -> `tenant_foo` repair in the SAME commit as guard H** | split across commits the tree is red in between, and **no differential run reports it** (source H has ZERO fixture coverage) |
| **Guard C PINNED at the `pc.GetName()` boundary** | relocating it turns `my-plugin` (`abi_callbacks_test.go:1453`) into a hard red |
| **ASSEMBLED probe at all nine sources** | a bare probe at an interior position over-rejects working configs and desyncs F1 from F2 |
| **ADR-0303's STATUS flip `PROPOSED` -> `COMPLETE` in the SAME commit as its §Decision** | **ADR-0299 shipped stale at `PROPOSED` for two full rows.** Recurrence guard: *"a block whose STATUS reads `PROPOSED` must carry no `### Decision` heading"* — **verified DISCRIMINATING at the PLAN**, both doctored NCs fired |

**THE TWELVE TASKS** (full detail in `PLAN.md`): T1 A mongo · T2 B zookeeper · T3 H ratelimit **+ the corpus repair** · T4 C wasm (**covers both tiers**) · T5 D+E rbac prefixes (**the only pair that genuinely shares a driver**) · T6 G compressor **+ the D5 non-regression arm driving the production `New` path** · T7 F1 · T8 F2 · T9 the **seven** per-package charset drivers **+ the segment-position table** · T10 the break roster (**four arms already executed and confirmed**) · T11 the gates · T12 ADR-0303 + contract + row flip + sentinel + close.

**COST THE PLAN WROTE DOWN: band 750-1000 net `.go`, budget ~850, TWELVE tasks.** ⚠️ `BOOTSTRAP_PROMPT.md` §6.1 gates on **the number the PLAN writes down**, not the realized figure. ⚠️ **The 725 measured figure is REALIZED-BASIS and must NOT be re-multiplied** — SPEC §12 says so explicitly, and phase 80's 2.56× does not transfer (its fixture bucket was **631 of 1636 at 4.10×**, and phase 81 adds **+0 fixtures**). ⚠️ **Read a budget as *"expect the ceiling"***: 76 ~7-9 -> 9 · 77 11-13 -> 12 · 78 7-9 -> **10 (above)** · 79 10-12 -> 12 · 80 11-14 -> 13. **Four of five landed at or above ceiling** ⇒ expect **13-14** tasks. ⚠️ **The third §6.1 trigger (mid-execution, >~10 sub-steps) FIRED at phase 80 (6 enumerated vs 17 executed) — RECORD such a firing, never absorb it.** ⚠️ **If the realized figure crosses ~1200, split on the SPEC's coherent axis — 81.1 `rbac-stat-name-guards` (F1+F2+D+E) / 81.2 `filter-stat-prefix-guards` (A+B+C+G+H) — NOT the router's refuted "sources / retrofit" axis.**

### What matters for the NEXT session, in order

1. **⚠️ RE-RUN ALL THREE SENTINEL CHECKS YOURSELF AND RECORD THE ACTUAL OUTPUT; DO NOT PREDICT IT.** Create `stop` if and ONLY if all three print nothing. **All three print today, so it cannot fire.** ⚠️ **YOUR STAGE FLIPS ROW 81, SO CHECK (1) GOES SILENT — run it AGAIN after the flip, and a doctored-copy NC is MANDATORY there**, because silence is otherwise indistinguishable from a broken check. ⚠️ **Leave `want` at 113.** ⚠️ **THE LEAK CHECK RE-ARMS AT YOUR STAGE.**

2. **⚠️ EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION, NOT BY REVIEW.** The lineage record is unbroken: the phase-80 SPEC refuted nine BRAINSTORM claims; its PLAN **nineteen**; its IMPL **sixteen**. The phase-81 BRAINSTORM refuted **fourteen** router claims; the SPEC **twenty-two**; **the PLAN FOURTEEN, four load-bearing**. **Budget for finding more. An IMPL that refutes nothing has not looked.**

3. **⚠️ THE PLAN'S LOAD-BEARING REFUTATIONS — carry these forward, they are now FACT and must not be re-inherited in their old form.**
   - **Segment position decides bare-vs-assembled** (item 1 above). §2.8's "OVER-REJECTS = 1" is an artifact of the leading position.
   - **D-81-EMPTY is retained for legibility, NOT load-bearing** (item 2).
   - **The single shared table does not exist** (item 3) — budget seven drivers.
   - ⚠️ **SPEC §2.1's PASTED PANIC NAME IS THE *REFERENCE'S*.** envoy-go emits **`http.myhcm.rbac.rbac.policy.allow-admins.allowed`** — a **DOUBLED** `rbac` segment, because `namespacePrefix("")` substitutes the literal after `baseStatPrefix` already emitted one. **Any test pinning the panic string must use the DOUBLE form.** This independently re-proves SPEC §13 item 3 **by execution**.
   - ⚠️ **`208-code-site / 84-file` IS A MIXED DENOMINATOR.** 208 code sites live in **36 production files**; **84** is the production+**test** count, whose hit total is **508**, not 210. **Cite 208 / 36.**
   - ⚠️ **`ADR-0045 MIS-LOCATES THE SPLIT GATE` IS REFUTED.** `BOOTSTRAP_PROMPT.md:209` §5 state 2 **genuinely carries the gate** at `:225-226`, and ADR-0045 cites **both** §6.1 and §5 state 2, **both accurately**. The *"quotes rather than states"* half survives as a style note; **the mis-location charge must not be repeated.**
   - ⚠️ **`84 of 121 phase dirs` IS STALE — it is 85 of 122** (37 carry `REVIEW.md`, unchanged; newest 25.3).
   - ⚠️ **SPEC §9's `1830 extracted values` IS NOT REPRODUCIBLE** (four extractions: 5336 / 987 / 1202 / 2241) and **`http.test` has ZERO occurrences in `test/`** — it lives only in `internal/filter/hcm`, `http/admission_control`, `http/adaptive_concurrency`, none of them among the nine — while the fixture corpus carries **170** distinct dotted values. **The PROPERTY holds on a stated denominator: 0 of 108 nine-source values carry an interior `..`, leading or trailing dot.** **State your own method and denominator.**
   - ⚠️ **`21 files register a per-route validator` IS A FILE-MENTION COUNT** — the real figure is **FIVE filters** (`builtins.go:67-71`, verified against the live registry; rbac `false`).
   - ⚠️ **h2spec IS NOT A CO-EQUAL GATE COST** — **4 s** against the differential's **406 s**, overstated ~100× by being priced alongside it. ⚠️ **AND THE `./cmd/envoy-go` CONSUMER SET IS THREE PACKAGES, NOT TWO**: `cmd/envoy-go/main_test.go` builds and boots the same binary at **four** sites (`:51 :198 :230 :943`) and **can red on a new boot reject**. It costs **9 s**. **RUN IT.**
   - **Fixture coverage for A and B counts INSTANTIATIONS, not token values** — `0050` and `0047` **deliberately omit** `stat_prefix`. Value-supplying coverage is **A = 3, B = 2**.
   - **Two anchors drifted**: `namespacePrefix` decl is **`:507`** (the SPEC's `:500-506` is its doc comment); `mongoproxy/codec_test.go` decls are **`:517`/`:538`**, not `:518`/`:539`. ⚠️ **The one SPEC §14 WARNED would drift — `DECISIONS.md:6291` — did NOT**: the +42 lines were a tail append that cannot shift line 6291. **Verify, do not assume, in either direction.**

4. **⚠️ FIVE DEFERRED CANDIDATES ARE NOW NAMED, and none sits on any family's deferred-candidate sentence, so check (2) is unmoved.** The interior empty-segment hole (⚠️ **BLOCKED — contradicts ADR-0132 §Decision (v)** at `DECISIONS.md:6291`; slug `stats-name-empty-segment-guards`) · the RBAC policy-name **PROJECTION** gap · the HTTP-rbac **empty-prefix fallback divergence** (⚠️ **UPGRADED from inference to MEASURED FACT at the PLAN** — the doubled `rbac` segment) · the matcher-tree `Action`-name boot walk · the three bare-prefix incumbents (now a **two-way** divergence after row 81). ⚠️ **PLUS TWO NEW AT THE PLAN:** the **ALS-family driver port race** (`allocateALSPort` bind->close->re-bind in `0081`/`0082`/`0083`, which **aborts the whole test binary**, observed 1-in-2, untracked anywhere) and **`my-plugin`** at `abi_callbacks_test.go:1453` (owed only if a later row relocates guard C).

5. **⚠️ BROKEN-GATE COUNT IS EIGHTEEN — no nineteenth shape at the PLAN, but FIVE priors fired LIVE.**
   - a **negative control that DID NOT LAND** (the row-62 `sed`, §sentinel above);
   - a **build failure read as a ZERO RESULT** — an orphaned import made `[build failed]` print **zero `--- FAIL:` lines**, so a count-based gate read **0** and looked like "the NC did not fire";
   - **a harness's exit code is not the command's** — the differential abort reported `INNER_EXIT=1` while the notification said *"exit code 0"*, at the **same 84-of-119 abort point** as the phase-76 instance;
   - **a vacuous gate arm on a clean tree** — the stat-surface ARM 1 is empty-in/empty-out; the real discriminator was a **comment-only production edit** giving a 6-line input diff;
   - **`\t` in GNU ERE**, firing in the **OPPOSITE** direction from the documented warning.
   The eighteen carried forward, unchanged: two defects that CANCEL in the gate metric · an inert gate cell · a full-suite recipe without `-v` is VACUOUS · a sha256 roster desynced against a DELETED file · `gofmt -l` NEVER exits non-zero (gate on OUTPUT) · `go doc -all <A> <B>` swallows arg2 · a `+0 exported symbols` gate over an EMPTY package reds on a CORRECT tree · a RANGE gate cannot detect anchor drift · a roster's naive `[ -f ] || continue` exits 0 on a DELETED file · a count-only stat guard PASSES a build with BOTH names wrong · a `-run` no-match exits 0 with `[no tests to run]` · a `--- PASS` tally over a package with sibling tests exceeds the fixture denominator · a stat-delta claim cannot be discharged by guards scoped to another package · a stderr-VOLUME assertion passes on the hang · `golangci-lint` runs `misspell` with `locale: US` · a harness's exit code is not the command's · a GOLDEN ROSTER that omits the family under test · a NEGATIVE CONTROL POINTED AT A TARGET THAT DOES NOT EXIST.

6. **⚠️ LINE ANCHORS DRIFT; PHRASE AND SYMBOL ANCHORS DID NOT.** Every anchor in `PLAN.md` was verified at **`82e425ac`** — and **your own edits will move them**, in the very files you are editing. **Prefer symbol anchors. RE-VERIFY EVERY `.go` LINE CITE BEFORE RELYING ON IT.**

7. **⚠️ THE GATES, WITH MEASURED BASELINES FROM THE PLAN.** Full 120-fixture differential **120/120 in 406 s** (inside SPEC §3's ~400-430 s band) · h2spec **53/53 in 4 s** · `go test ./cmd/envoy-go/` **9 s** · the nine packages **1 s**. `-race` is a SECOND run. ⚠️ **CI runs the differential at `-timeout 20m`, job `timeout-minutes: 30`.**
    ```sh
    ( go test ./test/differential/ -count=1 -v > "$SCRATCH/full.log" 2>&1; echo "INNER_EXIT=$?" )
    grep -cE '^    --- PASS: TestDifferential/' "$SCRATCH/full.log"          # want 120
    grep -E  '^    --- (FAIL|SKIP): TestDifferential/' "$SCRATCH/full.log"   # want EMPTY
    grep -c  'no driver registered for fixture' "$SCRATCH/full.log"          # want 0
    grep -cE '^panic:|DATA RACE|SIGSEGV' "$SCRATCH/full.log"                 # want 0
    grep -o  'TestDifferential/[^ ]*' "$SCRATCH/full.log" | sed 's|TestDifferential/||' | sort -u \
      | comm -3 - <(ls -1 test/fixtures/ | grep -E '^[0-9]{4}[a-z]?-' | sort)  # want EMPTY
    ```
    ⚠️ The faithful dir predicate is `^[0-9]{4}[a-z]?-` — a bare `^[0-9]{4}-` gives **118**.
    ⚠️ **`go test ./test/differential/...` (with `...`) matches TWO packages and BUFFERS `-v` output.** A run that looks hung for 7 minutes with an empty log is normal — do not kill it.
    ⚠️ **THE RUN ABORTS, IT DOES NOT MERELY FAIL. CAPTURE `INNER_EXIT` AND THE PANIC ARM.** A 1-in-2 ALS-driver port race kills the binary at fixture 84 and **35 fixtures silently never run**, while the notification reports success. **A naive `--- PASS` tally of 84 reads as "a few failures", not as an abort. Budget ~2 launches per green pass.**
    ⚠️ **But do NOT budget the differential as EVIDENCE for this row's correctness.** **Sources F1/F2 and H have ZERO fixture coverage** — `track_per_rule_stats` appears **once** in all of `test/`, as PROSE in `0018-http-rbac/README.md:293` (NC: `rules_stat_prefix` ⇒ 43 hits), and `0032`/`0033` never set the ratelimit filter's own `stat_prefix`. ⚠️ **And that README line does NOT say "deferred"** — it is a `D6 … per ADR-0146` decisions-honoured recital; the location and the zero-coverage conclusion are exact, the adjective was invented. **The differential is a REGRESSION ANCHOR ONLY; unit tests are the sole venue.**
    ⚠️ **Stat surface: assert the DELTA by CALL-SITE ENUMERATION.** The `TestNoNewStat*` guards are **PROVEN BLIND BY EXECUTION** (all five in `internal/statssink/registration_test.go`; `go list -deps ./internal/statssink` = 328 packages containing **zero** of the seven targets; injected registrations red them in `statssink` and **pass** in `http/wasm`). Denominator **208 code sites / 36 production files**. ⚠️ **ARM 1 on a clean tree is VACUOUS** — assert the input diff is `> 0`, or the arm proves nothing.

8. **⚠️ THE SIX-GATE HAS A STANDING LINEAGE DEPARTURE — NAME IT, DO NOT CLAIM COMPLIANCE.** Gate **(f)** expects a `REVIEW.md`; **none since 25.3 — 85 of 122 phase dirs carry none** (⚠️ **not 84 of 121**; 37 carry one, unchanged). Record it as a departure. Gate **(d)** is **VACUOUS for a row that adds none** — say "vacuous", not "green" (**55** fuzzers repo-wide). Gate **(c)**: proxy-wasm is **INHERITED, not re-run** — say so. ⚠️ **YOUR STAGE OWES (a)/(b)/(c)/(e) FOR REAL** — it is the phase-done gate and you are the closing stage. Normative cites re-verified EXACT at the PLAN: **§6.1 `:285`** (`:289` the ~25-task trigger, `:290` the ~1500-LoC trigger, **`:291` BLANK**, `:292` the third trigger); **§6.2 `:294`** (the coherent-slice obligation is item **3**); **§6.3 `:306`**; six-gate **§7.5 heading `:357`, gates (a)-(f) `:360-365`, close `:367`**. ⚠️ **`/BOOTSTRAP_PROMPT.md` WITH A LEADING SLASH RESOLVES NOWHERE** — the file exists only at `<repo>/BOOTSTRAP_PROMPT.md`. ⚠️ **AND THE SECOND COPY IS A LIVE STALE-CITE HAZARD**: `docs/superpowers/plans/2026-04-21-envoy-go-bootstrap-prompt.md` is **1024** lines vs **522**, and **all nine anchors differ** — offset **+197** for §6.x but **+228** for §7.5, so **no constant correction works**. **Open the repo-root file.**

9. **⚠️ ADR + DOCUMENT CONVENTIONS.** **The DECISIONS tail is `ADR-0303`, STATUS `PROPOSED` — the ONLY `PROPOSED` block in the file.** Next-free is **ADR-0304** (`grep -c '^## ADR-0304'` => 0; NC `^## ADR-0303` => 1). **302** `^## ADR-` headings, ids **0001-0303**, **exactly one gap at ADR-0209**, zero duplicates. STATUS census **16 = 15 COMPLETE + 1 PROPOSED**. ⚠️ **ADR-0303's §Decision and §Consequences land at YOUR stage, and the STATUS word must flip in that SAME commit.** ⚠️ **What ADR-0303 must record:** the **segment-position rule** and why assembled won · the **two-part F1/F2 remedy and why the asymmetry forces it** · the two-limb ADR-0065 discriminator · and that **D-81-EMPTY is retained for legibility, not for load-bearing effect**. ⚠️ **The retained italic footer count is NINE** — ADR-0294..0300 plus ADR-0302 plus ADR-0303; **ADR-0301 carries none**. ⚠️ **`^---$` is 216 and must stay 216.** ⚠️ **CARRY NO WHOLE-FILE GREP COUNT IN AN ADR** — that species self-falsified in ADR-0296 ¶3 and again in ADR-0302 ¶11; **enumerate by site instead.** ⚠️ **ADR-0044 does NOT contain the §Context-at-SPEC discipline** (mechanically re-scanned at the PLAN: zero hits for every relevant token, against a firing NC); always write **ADR-0044-as-used** with the caveat. ⚠️ **ADR-0045 QUOTES rather than states the split gate — but does NOT mis-locate it** (see item 3). Its §Context is the lineage's canonical worked cost example, and **ADR-0303 §Context already reproduces that form**.

10. **⚠️ A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX — the TWENTY-THIRD consecutive stage at which this held.** The index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. **Still live:** `reference_sds_init_fetch_timeout_dial_budget_flake` (**TWO packages**) · the pre-existing `internal/cluster` `-race` outlier (`TestOutlierDetector_ConcurrentEjectExactlyOnce`) · `internal/httpclient TestOptions_ZeroValue_NoOpDefaults`. ⚠️ **TWO former flakes were FIXED at `f46ba419`/`f2dd994a` and a recurrence of either is now a FINDING:** `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery` and the full-suite `bind: address already in use` **BACKEND** half. ⚠️ **`0061-lb-ring-hash` is NOT a live flake — a spread failure there is a FINDING.** ⚠️ **NEW AT THE PLAN — a THIRD species, and it is NOT one of the hardened halves:** the **ALS-family driver-owned receiver race** (`0081`/`0082`/`0083`), which binds an **out-of-band kernel-ephemeral** port via bind->close->re-bind and **aborts the whole binary**. Observed 1-in-2; isolate-re-run PASSED; full re-run 120/120. **Classify, isolate-re-run, and state the evidence.**

11. **Counts at this tip (re-run MECHANICALLY in your worktree; never copy them).** ⚠️ Use GNU `command grep` or `git grep` for anything repo-wide. **All re-derived at the phase-81 PLAN close:**
    fixtures **120** (next-free **0119**, no gaps; bare `^[0-9]{4}-` gives **118**) / fuzzers **55** / internal packages **73** (cross-checked two ways) / blank imports **120** on the FULL `^\t_ "github.com/pgdad/envoy-go/test/fixtures/` prefix (naive `^\t_ ` gives **126**; ⚠️ **use `-P`**) / **BackendKind tail 38** over **39** declarations, no gap, no dup, no `iota` (do NOT "fix" 38 to 39) / go.mod modules **2** (the phase-61.2 lineage figure — the single `go.mod` requires **67**; do NOT "fix" 2 to 67) / **`DECISIONS.md` 17766**, **302** headings, tail **ADR-0303 PROPOSED**, next-free **ADR-0304**, retained footers **9**, `^---$` **216** / **`ROADMAP.md` 229 lines / 113 data rows** / **`BEHAVIOR_CONTRACT.md` 5868** / **`STATE.md` 64** / **`STATE_HISTORY.md` 436** / **production `stats.IsValidName` guard sites 15** (chain: 40 -> minus 15 `_test.go` -> minus 2 `test/helpers/` -> minus 8 comments) / **stat registration: 508 tree-wide -> 210 production grep-hits -> 208 CODE sites in 36 PRODUCTION files** ⚠️ **not 84** / **distinct non-literal token sources 23** / **stat surface 1207** ⚠️ **DOCUMENTARY and CONFIG-CONDITIONAL — assert the DELTA**.
    ⚠️ **CONTESTED OR UNREPRODUCIBLE, SO NO NUMBER IS CARRIED:** the next-free REFERENCE port (routers say `10119`; `STATE.md` §Project says `10450`) · the `STATE_HISTORY.md` **archive-gap total of 57** (phases 67-75 = 36 bullets and the phase-77 PLAN reproduce exactly, but **phase 76 is additionally missing THREE bullets**, never called out, and no denominator closes) · SPEC §9's **1830**. **Row 81 needs none of them.**
    ⚠️ **A THIRD EXTRACTOR TRAP IN `STATE_HISTORY.md`:** naive `^- \*\*prior active-phase:\*\*` ⇒ **161**; parenthetical-tolerant `^- \*\*prior active-phase[^*]*:\*\*` ⇒ **167**. The **6** eviction-form bullets sit at `:420 :424 :428 :430 :432 :434`. ⚠️ **With the naive anchor, phase 79 reads as ENTIRELY ABSENT when all four of its bullets are present.** Any archive gate must use the tolerant anchor.

12. **⚠️ DOCUMENTARY DEFECTS — carried forward, still deliberately NOT fixed.**
    - ⚠️ **`DECISIONS.md` ADR-0301 §Decision (b) says *"has failed twelve top-level detectors"* — PRESENT TENSE inside immutable ADR text, factually wrong since row 80 landed.** A **named permanent discrepancy**, alongside `ROADMAP.md`'s five `twelve` occurrences (⚠️ **four are pure FALSE POSITIVES**).
    - ⚠️ **`0110`'s driver comment *"envoy_sds_* metric names (twelve families…)"* is ACCURATE** (14 distinct names / 12 families) **but is a FALSE POSITIVE for any `\btwelve\b` sweep.**
    - **`ROADMAP.md` malformation: rows 57, 69 and 78 lose summary content in GFM render.** All INVARIANT-BLOCKED by §Schema (*"Only `status` and `sub-phases` columns are updated in place"*) => **defensible as a GUARD, not a fix**, with the DISJUNCTION gate. ⚠️ **There is NO doc-invariant test anywhere in this repo.** The reusable precedent is `test/differential/harness_test.go:123-137`'s `runtime.Caller(0)` -> repoRoot idiom.
    - **`STATE.md` §Project counts self-contradicts §Current and is FROZEN at the phase-76 IMPL close.** **Anchor on §Current, which IS live.** ⚠️ **Do NOT "fix" §Project** — repairing a count by editing the sentence that states it is how the ADR-0296/0297 self-falsifying species starts.
    - **`BEHAVIOR_CONTRACT.md`'s `wasm.` arm is called a *"NEW SN9 flattening rule"*, which COLLIDES with ADR-0118's actual SN9.** Pre-existing.
    - ⚠️ **The two `//nolint:gosec` directives row 80 added are INERT** — `gosec` is not among the 9 linters `.golangci.yml` enables (`disable-all: true`; the nine are `govet errcheck staticcheck unused ineffassign gofmt goimports misspell revive`; golangci-lint **v1.64.8**).
    - **`golangci-lint-action@v6.5.2` emits a Node-20 deprecation warning in CI.** Deliberately NOT bumped: v7+ requires golangci-lint **v2** while the repo pins **v1.64.8**. **A warning, not a failure.**
    - ⚠️ **`ROADMAP.md` §Schema `:18` says *"Sub-phases get their own rows"* while phase 80 argued the legs live in the parent's `sub-phases` PROSE.** Practice diverging from written doctrine. **Record the tension; do not relitigate.**
    - **Production-DEAD trailer code** in four filters · **a VACUOUS differential arm**, `0036`'s `e_trailers_read` · **two stale non-test comments** at `internal/filter/hcm/connection.go:565` and `h2dispatch.go:503` claiming `RunDecodeTrailers` does not exist (it does, at `internal/filter/http/chain.go:455`) · **`DECISIONS.md`'s wasm module-function roster** enumerates 24 keys, **8 of which do not exist in `internal/`** · **two sibling `freeTCPPort` helpers** still carry BOTH pre-`f2dd994a` defects.
    - **`guest-side-config`** in `internal/filter/http/wasm/compiled_config_test.go:1147` looks like a charset violation and **IS one as a string** (`IsValidName` = **false**) — but guard C reads the **outer** `pc.GetName()`, so it is exempt **by call graph, not by the string being harmless**. **Recorded so a later sweep does not "fix" it.**
    - ⚠️ **NEW AT THE PLAN — `internal/filter/network/rbac/rbac.go:50` already contains the token `F2`** as an unrelated **phase-26 fork label**. **Do not use `F2` as a grep anchor in that package** (`reference_sentinel_matcher_string_self_clears`, in a new carrier).

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — take it from `git rev-parse master`, **NOT from a SHA quoted in this file**. The phase-81 PLAN squash is not named here on purpose: **router-only commits routinely land above it**, so a quoted SHA is stale the moment the router is touched again — **branch off the tip.** ⚠️ **AND THE BARE SUBJECT-LOCATE GREP IS CONTAMINATED BY THIS FILE'S OWN PROSE ABOUT IT.** `git log` greps the **whole commit message**, and router commits discuss the phase in their BODIES, so **the sentence warning about the grep satisfies the grep** (`reference_sentinel_matcher_string_self_clears`, in a commit-body carrier). ⚠️ **USE THE SLUG-ANCHORED FORM INSTEAD**, which is stage-exact:
```sh
git log --grep '^phase 81 (stats-name-charset-guards)' --format='%h %s'   # 3 at this tip: PLAN, SPEC, BRAINSTORM
```
Measured with both controls: the completed **phase 80** form returns **4** (IMPL/PLAN/SPEC/BRAINSTORM — a firing positive control), and an invented slug `^phase 81 (zzz-nonexistent)` returns **0**. It returns **4** once your IMPL lands. **Take the NEWEST line (`| head -1`) and verify the count before relying on it** (`reference_next_prompt_tracked_despite_gitignore`). Subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** (`reference_parallel_subagents_private_scratch`) and they **must tear down docker containers BY NAME, never by an `ancestor`/image filter** (`reference_parallel_agents_shared_machine_namespaces`). ⚠️ **THERE ARE TWO RESERVED PORT BANDS — band private ports outside BOTH `20000-31007` (subject blocks) AND `11000-14999` (backends), and also clear of the static fixture ports `10000-10447` / `15000-15011` / `18001-18007`.** The `42000-42999` band has been used safely (the phase-81 SPEC and PLAN each used `42000-42499` for five agents). ⚠️ **If two agents share ONE worktree they must stage by EXPLICIT PATH**, and **must NOT run an unscoped `git restore`**. ⚠️ **`internal/filter/http/rbac/rbac.go` IS EDITED BY TWO TASKS** (T5's D/E hunks and T7's F1 hunks) — they are **disjoint**, verified by two agents independently, but **stage by explicit path**. Per-task `gofmt` + `golangci-lint` on touched packages (`feedback_pertask_gofmt_lint`) — **OWED IN FULL at your stage.** ⚠️ **`gofmt -l` NEVER exits non-zero — gate on OUTPUT.** ⚠️ **`golangci-lint` runs `misspell` with `locale: US`, and it FIRED LIVE TWICE at the PLAN on first-draft guard comment prose** — all nine guards ship new comments. ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch, and note `-o` precedes the package path.**

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. ⚠️ **ASSUME IT FIRES EVERY SESSION — it has been treated as live for TWENTY-SEVEN consecutive sessions and it FIRED REPEATEDLY, OBSERVED, at the phase-81 PLAN** (`Shell cwd was reset to /home/esa/git/envoy-go`). **Use `git -C <abs-worktree-path>` for every git command, prefer absolute paths over `cd`, put any needed `cd` FIRST in a compound command, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN.** At the phase-81 PLAN, five agents wrote `_test.go` probes and prototype edits into their own detached worktrees, ran them, reverted **by explicit path** with `sha256sum` byte-identity, and **all five reported `git status --porcelain` = 0 lines**, controller-re-confirmed. The controller's own three probes were deleted the same way. A stage that ships a stray `.go` file has silently changed its own gate obligations.

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline — ⚠️ **anchor greps on `^- **<field>`**; all seven fields verified singleton at the phase-81 PLAN close, with a firing NC). ⚠️ **§Project counts SELF-CONTRADICTS §Current and is frozen at the phase-76 IMPL close** — do not anchor on it.
2. `docs/envoy-go/phases/81-stats-name-charset-guards/PLAN.md` — **the plan you are executing.** §1 the fourteen refutations (**§1.1 the segment-position rule, §1.2 D-81-EMPTY's collapse, §1.5 the non-constructible table, §1.6 the measured cost, §1.9 the second red, §1.12 the aborting differential** are the load-bearing ones) · §2 global constraints · §3 the edit surface · **Tasks 1-12** · §4 the band · §5 the sentinel · §6 counts · §7 deferred · §8 gate hygiene · §9 self-review · §10 memories.
3. `docs/envoy-go/phases/81-stats-name-charset-guards/SPEC.md` — the settled dispositions. ⚠️ **Read it THROUGH the PLAN's §1** — §2.8, §7, §8, §9 and §12 are all materially corrected there.
4. `docs/envoy-go/phases/81-stats-name-charset-guards/PROGRESS.md` — the `# BRAINSTORM record`, `# SPEC record` and `# PLAN record`.
5. `docs/envoy-go/ROADMAP.md` — **§Schema first** (the row invariants), then **row 81** (which YOU flip), then the five deferred-candidate paragraphs (`:191 :201 :211 :217 :225`).
6. `BOOTSTRAP_PROMPT.md` — **at the REPO ROOT, no leading slash.** §6.1 `:285`, §6.2 `:294`, §6.3 `:306`, §7.5 `:357-367`. ⚠️ **Not the 1024-line copy under `docs/superpowers/plans/`.**
7. `docs/envoy-go/DECISIONS.md` — **ADR-0303** (the block YOU complete) · **ADR-0288** (roll-IN-PLACE and the archive rule) · **ADR-0065 §Consequences (b) `:2379` and (e) `:2382`** (the longest-assembled-name rule *with its argument* — the anchor for the segment-position decision) · **ADR-0132 §Decision (v) `:6291`** (the compressor D5 pin) · **ADR-0044** as-used · **ADR-0045** (⚠️ it QUOTES the split gate but does NOT mis-locate it).
8. `docs/envoy-go/phases/80-stats-sds-projection/PROGRESS.md` — **the `# IMPL record`**, for calibration: sixteen PLAN refutations and the lineage's first realized ~1500-LoC crossing.
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — especially **`reference_probe_must_discriminate`**, **`reference_probe_input_is_a_claim`**, **`reference_gate_command_negative_control`** (⚠️ **fired LIVE on the controller at the PLAN**), **`reference_empty_output_is_not_a_zero_result`** (⚠️ **fired LIVE — a build failure read as zero**), **`reference_harness_exit_code_is_not_command_exit_code`** (⚠️ **fired LIVE on the differential abort**), **`reference_a_drift_correction_is_itself_a_claim`**, **`reference_code_comment_not_evidence`**, **`reference_sample_is_not_an_audit`**, **`reference_grep_c_zero_is_a_broken_gate`**, **`reference_differential_run_selector`**, **`reference_recursive_grep_blind_to_gitignored_tracked_file`**, **`reference_dynamic_stat_name_charset_guard`**, **`reference_nil_stats_counter_inc_crashes_goroutine`** (⚠️ **the WRONG mechanism for this row — `checkName` PANICS**), **`reference_golangci_misspell_locale_us`** (⚠️ **fired TWICE**), **`reference_stale_cite_recurs_fix_by_pattern`**, **`reference_break_protocol_commit_first`**, **`reference_deliberate_break_wrong_assertion`**, **`feedback_brief_citations_not_evidence`**, `feedback_git_worktrees`, `feedback_execution_style`, and **`reference_bash_cwd_reset_commits_to_main`**.
