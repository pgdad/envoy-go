START a fresh cold-start session for envoy-go — phase 26.2 (`network-filter-registry-migration`) SPEC is DONE; author the **phase 26.2 PLAN** (bite-sized TDD tasks decomposing the 26.2 SPEC §10 task spine).

This is a per-sub-phase PLAN cold-start. **Phase 26.2 SPEC is DONE + squash-merged to master + pushed to origin** (squash `7c19957` — the 26.2 SPEC.md + the ADR-0215 §Context draft + STATE/ROADMAP advance; docs only, no code change). The SPEC was authored via `superpowers:writing-plans` (per the 22.1/25.1/26.1 per-sub-phase-SPEC precedent), spec-document-reviewer APPROVED (no CRITICAL/MAJOR; every code claim verified true against the migration-target source; 4 MINOR/ADVISORY polish items folded in). STATE.md points at `phase 26.2 SPEC-done; awaiting 26.2 PLAN`, next-skill `superpowers:writing-plans` (scoped to the 26.2 per-sub-phase PLAN), next-free `ADR-0216` (the 26.2 SPEC drafted ADR-0215 §Context; tail advanced ADR-0214 → ADR-0215).

## What this session must do: author the phase-26.2 per-sub-phase PLAN

Per SKILL_ROUTING (sub-row 26.2 at state 2 — SPEC.md exists, no PLAN.md yet), the next-skill is **`superpowers:writing-plans`** scoped to authoring the **26.2 PLAN** (`docs/envoy-go/phases/26.2-network-filter-registry-migration/PLAN.md`) — bite-sized TDD tasks decomposing the 26.2 SPEC §10 task spine (~10-16 tasks), mirroring the 26.1 PLAN precedent (`docs/envoy-go/phases/26.1-network-filter-chain-framework-and-echo/PLAN.md`, ~1450 lines, 17-task TDD plan). The PLAN does NOT re-decide the SPEC's resolved design; it decomposes it into exact-file/exact-command/expected-output TDD steps and resolves the SPEC §12 D-questions (D-26.2-1..7) at PLAN/IMPL granularity.

## Phase 26.2 scope (settled at the 26.2 SPEC; ADR-0215 §Context drafted)

**`network-filter-registry-migration`** — migrate `tcp_proxy` (`internal/filter/tcpproxy/`) + HCM (`internal/filter/hcm/`) onto the 26.1 `internal/filter/network/` framework + `*network.Registry`, RETIRE the hardcoded `manager.go` terminal-filter registry, and UNIFY the dual-dispatch — at byte-exact parity (R3 back-compat paramount). The SPEC's resolved design (the PLAN decomposes, does not re-litigate):

- **Terminal-filter seam (RESOLVES parent §3.2):** NEW `network.TerminalFilter interface { Handle(ctx context.Context, downstream net.Conn) }` — byte-identical to the retired `manager.go` `filterHandler`, so `*tcpproxy.Filter` (`tcpproxy/filter.go:69`) + `*hcm.Filter` (`hcm/filter.go:66`) satisfy it with ZERO method changes. Their `Handle` connection-takeover loops are UNTOUCHED → byte-exact parity is intrinsic. NOT an `OnData` rewrite (rejected: HCM's H1 driver + H2 ServerConn fully own the conn).
- **Registry generalization:** `FilterInstanceFactory func() NetworkFilter` (sealed `NetworkFilter` marker = `ReadFilter` | `TerminalFilter`; D-26.2-1 anticipates the sealed `networkFilter()` method — echo/direct_response embed it). The `*network.Registry` becomes the SOLE network-filter registry (now also tcp_proxy + HCM).
- **Extended `network.FactoryCtx`:** add per-chain PRIMITIVE fields (HasTLS/AllowH2C/ListenerPrincipal/NodeServiceCluster) — the heavy boot singletons (cm/dm/stats/sinks/httpReg/httpClient) are CLOSURE-CAPTURED in the `main.go` registration adapters `tcpproxy.NewNetworkFactory(cm, dm)` / `hcm.NewNetworkFactory(cm, registry, sinks, httpReg, dm, httpClient)`, keeping `internal/filter/network` import-light (NO cluster/stats/hcm imports — D-26.2-7 import-cycle audit). The HCM-bridge closure MOVES from `manager.go:112-132` into the `hcm` adapter (mechanical).
- **`manager.go` retirement + unification:** DELETE `filterHandler`@46 / `filterConstructor`@97 / `filterRegistry`@104 / `buildTerminalFilter`@612; collapse `chainInfo`@184 (drop `filter`; generalize `netChainFactory` → `func() []network.NetworkFilter`); `buildNetChainFactory`@654 becomes the sole chain builder (resolve ALL filters against netReg; classify; validate `[read*, terminal?]` shape); unify `serveConnection`@1045 step-7 (@1104-1109) into ONE `serveNetworkChain` branch (pure-read read loop unchanged; terminal handoff; pure-terminal immediate `Handle`).
- **Mixed-chain LIFT + buffered-prefix handover:** the 26.1 `network-filter-mixed-chain-unsupported` reject is DELETED; mixed `[read-filter*, terminal-filter]` chains expressible. Shape the buffered-prefix `prefixConn` net.Conn wrapper (replays undrained buffered bytes to the terminal before the live socket) + unit-test via a synthetic always-`Continue` read filter + recording terminal (D-26.2-3 anticipates landing at 26.2). NEW chain-shape rejects: `network-filter-terminal-not-last` + `network-filter-multiple-terminals`. FIRST production consumer of a mixed read→terminal chain = `rbac_network` 26.3 (echo halts + direct_response closes — neither `Continue`s to a terminal at 26.2), so the multi-chain DIFFERENTIAL fixture is DEFERRED to 26.3.
- **Registration seam:** `netReg` becomes INTRINSIC (nil-tolerance dropped — every chain resolves through netReg). A shared `RegisterBuiltins` helper (placement avoiding import cycles — likely `cmd/envoy-go/` or a tiny wiring package, NOT `network`/`listener` — D-26.2-5) registers all four built-ins (echo/direct_response/tcp_proxy/hcm) for main.go + the thinner ctors + the test callers (`internal/admin/admin_helpers_test.go`, `internal/admin/listeners_test.go`, `internal/listener/manager_test.go`, `cmd/envoy-go/main_test.go`).

This is a MIGRATION/refactor (move existing behavior at byte-exact parity — R3 paramount). NO new operator-visible feature; stat surface stays 132; fixtures stay 44; fuzzers stay 35 (all +0 — verify at the IMPL six-gate). The full tcp_proxy/HCM differential suite + conformance 10/10 + h2spec 53/53 are re-run LIVE as the back-compat gate (NOT asserted-unaffected — the dispatch path genuinely changed for tcp_proxy/HCM).

## Project state at master tip

- **Master tip:** `fe5f8cc` (the 26.2-SPEC stage-close: STATE last-commit SHA-fill + this next-prompt rewrite; trails the substantive 26.2-SPEC squash `7c19957`). Confirm via `git log --oneline -1` — any further next-prompt repoint will itself advance the tip +1 (expected, not chased). origin/master in sync after push. STATE.md `last-commit` points at the substantive squash `7c19957` (not the repoint tip).
- **§9 Network-filters family OPEN:** parent row 26 `in-progress`; sub-row 26.1 `done`; 26.2 `in-progress` (flipped `planned → in-progress` at the 26.2 SPEC commit per ADR-0106; flips `in-progress → done` at 26.2 IMPL phase-done); 26.3 `planned`. Parent row 26 flips `in-progress → done` at 26.3 phase-done (ROLLUP precedent 18/19/22/24/25).
- **Counts at master tip:** stat surface **132**; differential numbered-fixtures **44** (tail **0042**); fuzzers **35**; conformance 10/10; h2spec 53/53; DECISIONS.md tail **ADR-0215** (the 26.2 SPEC drafted the §Context; §Decision/§Consequences are RATIFIED-PENDING-IMPL — land at 26.2 IMPL in place per ADR-0044, tail STAYS ADR-0215), next-free **ADR-0216**. 26.2 is a migration: anticipated NO new fixtures/fuzzers/stats deltas (verify at the IMPL Task-1 baselines).
- **Toolchain:** Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); reference Envoy v1.37.2 (ADR-0008); go-control-plane v1.32.4 (ADR-0008).

## Read first (in order)

1. THIS FILE — the phase-26.2 PLAN cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 26.2 SPEC-done; awaiting 26.2 PLAN`; next-skill `superpowers:writing-plans` (26.2 PLAN); next-free `ADR-0216`.
3. `docs/envoy-go/phases/26.2-network-filter-registry-migration/SPEC.md` — THE SPEC TO DECOMPOSE (§3 framework + dispatch changes; §4 tcp_proxy/HCM adapters; §6 PARSE-REJECT; §10 the ~12-task spine the PLAN expands; §11.1 the D-S1 baselines + manager.go line anchors; §12 the D-26.2-1..7 D-questions the PLAN resolves; §13 R3/R-T/R-M/R-U/R-S/R-A; §15 the acceptance checklist).
4. `docs/envoy-go/phases/26.1-network-filter-chain-framework-and-echo/PLAN.md` + `PROGRESS.md` — the per-sub-phase PLAN PRECEDENT to mirror (task granularity, the REAL `fixture.Driver`/`harness` API for fixtures, the fuzzer-count `git ls-files` gate, the ctor-caller blast-radius enumeration, the two-stage-review subagent-driven-development model) + the as-built 26.1 framework the migration extends.
5. `docs/envoy-go/phases/26-network-filter-chain-and-rbac/SPEC.md` — the PARENT SPEC (the §3.2 terminal-fit D-question the 26.2 SPEC resolved; the inherited §5/§6/§7/§8/§11/§12/§13 rosters; §11.8 the LoC/task envelope).
6. The MIGRATION TARGETS: `internal/filter/network/{types,callbacks,chain,registry,buffer}.go` (the framework to extend); `internal/listener/manager.go` (the dual-dispatch + the hardcoded terminal path to retire/unify — re-pin the §11.1 line anchors against the IMPL-session tip at PLAN Task 1); `internal/filter/tcpproxy/filter.go` (`Handle`@69 + `NewFilter`@40); `internal/filter/hcm/{filter.go,config.go}` (`Handle`@66 + `NewFilterWithCtxAndSinksAndRegistry`@45 + `ListenerCtx`@55).
7. `docs/envoy-go/DECISIONS.md` ADR-0215 (the §Context drafted at the 26.2 SPEC — the §Decision/§Consequences bodies land at 26.2 IMPL per ADR-0044) + ADR-0213/0214 (the framework + registry the migration builds on); `docs/envoy-go/ROADMAP.md` rows 26/26.2; `BOOTSTRAP_PROMPT.md` §5 (state machine) + §4.1 STATE invariant + ADR-0004/0005 (autonomous adaptation) + ADR-0044 (in-place ADR-body discipline) + ADR-0045 (split-gate — CHECK the ~12-task/~500-900-LoC PLAN fits ~25 tasks/~1500 LoC, net-new basis; it does); ADR-0106 (ROADMAP flip-on-SPEC, already applied); `docs/envoy-go/SKILL_ROUTING.md` (confirm sub-row 26.2 state 2 → writing-plans for the PLAN).
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — execution-style (subagent-driven) + worktree + push + subagents-no-push + differential-asserter-dispatch + fixture-dispatch-constraint + network-factoryctx-basedir all apply.

After the 26.2 PLAN is authored + plan-document-reviewer-APPROVED + squash-merged + pushed, advance STATE.md per BOOTSTRAP §5 (SKILL_ROUTING state 2 → 3 for sub-row 26.2; ROADMAP sub-row 26.2 STAYS `in-progress`; parent row 26 STAYS `in-progress`) and rewrite next-prompt.txt for the 26.2 IMPL cold-start (`superpowers:subagent-driven-development` executing the 26.2 PLAN task-by-task).
