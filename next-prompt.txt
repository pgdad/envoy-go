=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-VERIFIED 2026-07-16 (re-run MECHANICALLY against the PUSHED master tip `d8134985`, post-squash — checks (2) and (3) still print) — it does NOT fire. `stop` was NOT created.** Actual output:
- **(1) prints NOTHING** — row 65 flipped `done` at the phase-65 IMPL six-gate, and every other ROADMAP row already was. **⚠️ Check (1) NO LONGER blocks `stop`** — it is now checks (2)+(3) alone that hold the loop open. A future roller MUST NOT read "(1) is silent" as "the project is done".
- **(2) prints THREE live "candidates:" sentences** — HTTP/3 (`:175`), xDS (`:185` — **NARROWED at this IMPL**: `SDS validation_context/upstream SDS` → `upstream SDS (server-cert + validation_context)`), Observability (`:193` — **EXTENDED at this IMPL** with the downstream TLS handshake-outcome `ssl` stat family, the ADR-0286 C3 named coverage boundary) ⇒ three families STAY OPEN. **NOTE: line `:193` ALSO carries HISTORICAL `candidates were:` recaps — do not confuse them with the live sentence** (`reference_sentinel_deferred_sentence_live_vs_historical`; the live grep needs "candidates:" adjacent, and there must be exactly ONE live match per family).
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** ⇒ THREE families never opened. Operational-tooling also STAYS OPEN.

Checks (2) and (3) print ⇒ the sentinel does NOT fire. Banked deferred work + three never-opened families remain — abundant work.

**STATUS AS OF 2026-07-16: the phase-65 IMPL LANDED and ROADMAP row 65 is `done`.** ADR-0286 (§Decision/§Consequences) landed in `DECISIONS.md`; DECISIONS tail **ADR-0285 → ADR-0286** (next-free **ADR-0287**). The THIRD xDS-family row + the SECOND SDS resource type is CONSUMED: the downstream `validation_context_sds_secret_config` reject is LIFTED (downstream+provider ONLY; upstream + QUIC keep the BYTE-IDENTICAL substring, ADR-0080), the CA is fetched over the LANDED phase-60.2 SotW stream by a PARALLEL `internal/xds` `*x509.CertPool` chain, and installed as `cfg.ClientCAs` + `RequireAndVerifyClientCert` under `require_client_certificate: true`. The landed `*stdtls.Certificate` chain is BYTE-UNTOUCHED (`0103` structurally cannot regress); the 60.2 cycle guard HOLDS.

**⚠️ WHAT PHASE 65 LEARNED THAT THE NEXT PHASE SHOULD NOT RE-LEARN:**
- **A PLAN is not evidence either.** Phase 65's PLAN had itself corrected FIVE SPEC defects — and the IMPL then found **ELEVEN defects in the PLAN** by re-derivation (`counterValue` did not exist · THREE snippets did not compile · an upstream subtest was unreachable/vacuous · a boot-FAIL subtest passed pre-implementation · every T6 helper name was wrong · a break expectation was logically impossible · a differential break was vacuous · a seed rationale was false · **TWO break INSTRUCTIONS did not compile**). `feedback_brief_citations_not_evidence` applies to EVERY upstream document, not just briefs.
- **A PLAN's BREAK INSTRUCTIONS are as defect-prone as its snippets — and fail more QUIETLY** (new memory: `reference_plan_break_instructions_dont_compile`). A break that does not compile shows red that LOOKS like a fired assertion but proves NOTHING about which assertion is live. Phase 65 hit TWO non-compiling breaks (T1's `GetTlsCertificate()` swap lacks the accessors; T2's "drop the `%w: `" leaves a `go vet`-rejected two-arg/one-verb `fmt.Errorf`) plus TWO impossible/vacuous ones. **Substitute a compiling equivalent with identical intent, REPORT the substitution, and record the TRUE result — never let a build failure stand in for a fired assertion.**
- **The worst failure mode is uncited PROSE, not a wrong `file:line`.** The PLAN asserted the upstream arm reaches the SDS-validation guard. It does NOT — `validation_context_type` is a **oneof**, so `NewUpstreamConfig` refuses EARLIER with its own `trusted_ca is required` message. **QUIC is the guard's ONLY live consumer** (via the `provider == nil` half). That false claim **reached a shipped code comment** before being caught (T5-fix `af55ac9e`).
- **A break that does NOT fire is a finding, not a nuisance.** T3's `errValidation`-before-`ctx.Err()` break did not fire because no test creates the discriminating condition. It is recorded as an HONEST, UNCLAIMED coverage gap — not routed around.
- **`reference_vacuous_break_receiver_normalizes` was DEMONSTRATED, not cited.** With `0108`'s `structuralCheck` disabled, a served-CA break ships **PASS** — both sides emit `good=REJECTED`/`bad=ACCEPTED` and compare EQUAL. **A pure-`CompareBytes` fixture would have shipped green on a completely broken trust anchor.** Any future fixture whose breaks change BOTH sides identically needs an in-driver structural check, and the trap must be shown, not asserted.
- **A `-race` failure was observed ONCE and correctly classified as PRE-EXISTING** (`TestProvider_FetchInitialCertificate_Timeout`, `init_fetch_timeout = 0, want 1`) — now banked as memory `reference_sds_init_fetch_timeout_dial_budget_flake`. The test body is BYTE-IDENTICAL to master; master and branch each pass 3/3; 6 saturated runs passed; the mechanism is a 200ms budget covering BOTH the gRPC dial and the recv (a miss → `open stream` error → `incUpdateFailure` → the counter stays 0 while every other assertion stays green). **It was NOT "fixed" in row 65.** If it recurs, read the memory — do not re-derive it from scratch, and do not reflex-classify it as a phase-66 regression. A SECOND occurrence justifies widening the budget or splitting the dial out of it.

**Counts at phase-65 IMPL-DONE (VERIFIED on the landed tree; RE-VERIFY at session start, `git fetch` first):** fixtures **110** (`ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0108-xds-sds-validation-context`) · fuzzers **55** (`grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`) · DECISIONS tail **ADR-0286** (next-free **ADR-0287**) · BackendKind tail **38** (`H2GoawayResponder`; explicit numeric consts in `test/differential/fixture/fixture.go`) · stat surface **1201** (docs-verified against `BEHAVIOR_CONTRACT.md`; there is NO mechanical counting command — registration guards + a zero-new-call-site diff enforce +0) · go.mod modules **2** (`quic-go` direct + `qpack` indirect).

==============================================

## What THIS session does (the phase-66 BRAINSTORM)

Perform the **BRAINSTORM** stage (lifecycle-state 0 → 1) for **phase 66**. Docs-only (NO production `.go`). Fresh worktree off master, branch `phase-66-<slug>`, worktree `.worktrees/phase-66-<slug>`, per `feedback_git_worktrees`. The deliverable is `docs/envoy-go/phases/66-<slug>/BRAINSTORM.md`. Row 66 registers `in-progress` at the BRAINSTORM commit per the ROADMAP §Schema invariant.

**⚠️ THE SUBJECT IS THE BRAINSTORM'S TO PICK — THE PROVISIONAL PICK BELOW IS NOT SETTLED.** Per the standing directive the roller self-picks; this router records a PROVISIONAL pick + the rejected alternatives so the BRAINSTORM starts from a considered position, NOT so it rubber-stamps one. **RE-VALIDATE the pick against source before adopting it, and record YOUR pick + YOUR rejected alternatives in the BRAINSTORM itself.** If the feasibility check below does not hold up, PICK DIFFERENTLY and say why — that is the correct outcome, not a deviation.

**There is NO banked mid-lifecycle work.** Check (1) is silent: every chartered row is `done`, so no split leg awaits its PLAN/IMPL. The directive's "advance banked work first" clause does NOT apply — this is a brand-new subject.

### PROVISIONAL pick: `xds-sds-upstream-server-cert` (the FOURTH xDS-family row)

Lift the **upstream** `tls_certificate_sds_secret_configs` reject and thread the LANDED SDS provider into `NewUpstreamConfig`, so a CLUSTER's client cert is delivered dynamically over the same SotW stream.

> **⚠️ CITATION CORRECTED AT THIS ROLL — and the correction is itself the lesson.** The reject is the `side != "downstream"` gate inside `commonTLSContextToConfig`'s `if len(c.GetTlsCertificateSdsSecretConfigs()) > 0` block — **`internal/tls/config.go:241-243`** as of master `d8134985`, **NOT the `:208-209` that SPEC-65 and this router's first draft both claimed** (T10 had already flagged that SPEC number as drifted; the router propagated it anyway until a mechanical re-derive caught it). **Cite by SYMBOL, not by line** — line numbers drift every phase, and this is the third time in one row that a stale one nearly shipped.

**Why it is the smallest defensible candidate:**
- **ZERO new xDS machinery.** It reuses the ENTIRE landed `*stdtls.Certificate` chain verbatim — `parseSecret` / `fetchSecret` / `applyResponse` / `FetchInitialCertificate` all already exist and are already proven by the passing `0103`. Phase 65 had to BUILD a parallel applier chain; phase 66 builds NONE. No other candidate reuses this much.
- It is the family's **explicitly-next-named** deferred candidate (`upstream SDS (server-cert + validation_context)` — this row would charter the server-cert half, narrowing the sentence again).
- Phase 60.2 proved the substrate carries one applier; phase 65 proved it carries a SECOND RESOURCE TYPE; phase 66 would prove the SECOND APPLY-SIDE. A clean, cheap third increment on a substrate that keeps paying.

**⚠️ The load-bearing risk the BRAINSTORM MUST dispose (RE-DERIVED from source at this roll, not assumed):**
- `NewUpstreamConfig(ts *corev3.TransportSocket, baseDir string)` (`internal/tls/config.go:152`) **takes NO provider today**, and its SOLE production caller is `internal/cluster/manager.go:604`. So the row must thread a provider boot → cluster-manager → `NewUpstreamConfig` — a real, bounded seam change, NOT a pure reject-lift.
- **`reference_xds_config_seam_transitive_cycle_guard` names EXACTLY this edge:** a provider package that `internal/tls` imports must not import `grpcclient`/`cluster`, or the reverse edge cycles. The phase-60.2 mitigation is a sibling boot-side adapter package (`xdsgrpc.Opener`). **Verify with `go list -deps ./internal/xds` (NO `...`) — the dep set is currently EXACTLY `internal/stats` + `internal/xds` and MUST stay that way.** This is the single most likely place phase 66 discovers it is bigger than it looks.
- **Boot-ordering is a genuine open question:** clusters are built at boot for ALL clusters, so a BLOCKING per-cluster initial fetch is a new question the ADR-0280 boot-FAIL DEPARTURE does not automatically answer. Probe it; do not assume phase 65's answer transfers.
- Confirm against the reference (`envoyproxy/envoy:contrib-v1.37.2`, FRESH container per arm — `reference_probe_fresh_container_per_arm`) that upstream SDS server-cert behaves as anticipated BEFORE committing to the shape.

### Rejected alternatives (record these, and revisit if the pick collapses)
- **tracing `custom_tags` `metadata`** (Observability) — superficially attractive as the 4th row on the well-trodden `[]CustomTagSpec`/`ResolveCustomTags` seam (phases 59/62/63 did `literal`/`request_header`/`environment`). **Rejected: it is the LAST remaining custom_tag type precisely BECAUSE it is the hardest** — it needs dynamic-metadata plumbing that the prior three did not. "Same seam" is not "same size".
- **The downstream TLS handshake-outcome `ssl` stat family** (Observability) — freshly added to the deferred sentence by ADR-0286 C3. **Rejected: explicitly recorded as a FRAMEWORK-SURGERY row** (envoy-go emits ZERO `ssl.*` stats; the reference emits the whole family live). It would blow a +0-stat envelope and is the opposite of "smallest".
- **xDS compose-two** (SDS for BOTH the server cert and the validation_context on ONE context) — **Rejected: deliberately DEFERRED behind the landed `seen>1` guard**; it needs the single-slot provider model (ONE `secretName`, ONE `*SDSStats`) generalized to multi-secret, which would put the passing `0103` at risk. A real row, not a small one.
- **SDS rotation** (xDS) — the SotW stream currently does one initial fetch. Rotation needs a live-update path into an already-built `tls.Config` (`GetCertificate`/`GetConfigForClient` callbacks) — a new seam, not a reject-lift.
- **`google_grpc` transport / CDS/EDS / LDS/RDS / ADS / Delta xDS / RTDS** (xDS) — each is a substantially larger subsystem. Not smallest.
- **QuicProtocolOptions tuning** (HTTP/3) — plausibly small (the phase-64 numeric-knob shape on the landed H3 listener) and the **strongest fallback if the upstream-SDS cycle/boot-ordering risk proves fatal**. Rejected only on the reuse argument: upstream SDS reuses an entire proven applier chain, which is a bigger saving than a knob row's.
- **`spawn_upstream_span` / `http_service` / force-trace** (Observability tracing) — `spawn_upstream_span` needs an upstream-span concept that does not exist; the others are plausible but reuse less than the pick.
- **Opening gRPC / Runtime / WASM** (the three never-opened families) — each is a family OPENER, categorically the largest possible move. The directive says smallest-first; these are last.

**Honor throughout:**
- **Lifecycle = one stage per session.** This session: the BRAINSTORM ONLY (no SPEC, no PLAN, no code). After it lands: roll to the phase-66 SPEC.
- **The BRAINSTORM's job** is to frame the subject, enumerate the D-<TAG>-* open questions (the load-bearing ones flagged for LIVE reference probes at the SPEC), sketch the anticipated shape + counts, and record the self-pick + rejected alternatives. It DISPOSES nothing empirically — that is the SPEC's job.
- **Counts are UNCHANGED at a BRAINSTORM** (docs-only). Re-verify them at session start and state them; do not copy them from this router without checking (`git fetch` first).
- **RE-DERIVE every `file:line` from source** (`feedback_brief_citations_not_evidence`) — and **prefer citing by SYMBOL**. Status of this router's own citations, mechanically re-derived against master `d8134985` at this roll: `NewUpstreamConfig` at `config.go:152` taking NO provider — **HOLDS**; sole production caller `internal/cluster/manager.go:604` — **HOLDS**; the cycle-guard baseline (`internal/stats` + `internal/xds` ONLY) — **HOLDS**; the upstream reject at `:208-209` — **WRONG, corrected to `:241-243`**. That is 1 of 4 router citations wrong *after* a phase whose entire lesson was citation drift. **Re-derive them again anyway.**
- **Process anchors:** ADR-0044 (a BRAINSTORM anchors NO ADR; the §Context draft lands at the SPEC) · ADR-0045 (single-flat-row bias; the escape-valve is armable) · ADR-0106 (a row flips `done` only at its IMPL six-gate) · ADR-0080 (envoy-go-strict distinct-substring rejects) · ADR-0280 (the boot-FAIL-on-fetch-timeout DEPARTURE — do NOT assume it transfers to the upstream/cluster path) · `reference_roadmap_split_phase_row_done`.
- **The ROADMAP row-66 registration** happens AT the BRAINSTORM commit per the §Schema invariant. **The deferred-sentence NARROW does NOT happen at a BRAINSTORM** — it lands at the IMPL (the observed convention; phase 65 narrowed `SDS validation_context/upstream SDS` at its IMPL, not its BRAINSTORM).
- **`next-prompt.txt` IS TRACKED** (`reference_next_prompt_tracked_despite_gitignore`) — a `.gitignore` entry does not apply to an already-tracked file. Edit it inside the stage worktree and fold it into the stage squash; locate commits by SUBJECT (`git log --grep`), NEVER by position ("the tip's PARENT" is not a general rule).
- **Worktree discipline** (`feedback_git_worktrees`, `feedback_subagent_worktree_detach`, `feedback_subagent_worktree_path_targeting`) — pin the canonical worktree root; the controller verifies the MAIN checkout stays clean; on a deliberate break restore with `git restore` only (no checkout-sha, no `--amend`, no detached HEAD).

**ADR reservation ledger (provisional; each finalized at its IMPL in landing order):** ADR-0283 = phase-62 tracing-custom-tags-request-header (LANDED) · ADR-0284 = phase-63 tracing-custom-tags-environment (LANDED) · ADR-0285 = phase-64 tracing-max-path-tag-length (LANDED) · ADR-0286 = phase-65 xds-sds-validation-context (**LANDED at the phase-65 IMPL** — §Context drafted at the SPEC per ADR-0044, §Decision/§Consequences at the IMPL) · **next-free unreserved: ADR-0287** (phase 66 anticipates it; a BRAINSTORM reserves nothing).

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-66 BRAINSTORM is next; the subject is the BRAINSTORM's to pick).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 65 IMPL done`; NEXT = the phase-66 BRAINSTORM) + the prior-phase lineage.
3. `docs/envoy-go/ROADMAP.md` — the §Schema invariant (how a row registers + when it flips `done`) + the THREE live deferred sentences (`:175` HTTP/3, `:185` xDS, `:193` Observability) + the xDS family section (rows 60/60.1/60.2/65 — the SDS precedents).
4. `docs/envoy-go/phases/65-xds-sds-validation-context/PROGRESS.md` — **the most recent IMPL close**: the liveness-break log (incl. the breaks that did NOT fire), the `-race` finding, and the residual-risk resolution. The FORMAT precedent + the cautionary tale.
5. `docs/envoy-go/DECISIONS.md` — ADR-0286 (phase 65: the parallel chain, the C1-C3 corrections, the IMPL-time findings) + ADR-0280 (SDS server-cert wiring + the boot-FAIL departure) + ADR-0278 (the SDS substrate) + ADR-0147 (static-mTLS `ClientCAs`) + the ADR-0044/0045/0106/0080 process anchors.
6. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the xDS/SDS section (what is CONSUMED vs REJECTED today) + the recorded coverage boundaries.
7. A recent BRAINSTORM as the FORMAT precedent — `docs/envoy-go/phases/65-xds-sds-validation-context/BRAINSTORM.md` (the immediately-prior self-pick, incl. how it recorded its pick + rejected alternatives).
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the differential-harness `reference_*` traps + **`reference_xds_config_seam_transitive_cycle_guard`** (load-bearing if the provisional pick survives) + the TWO memories banked at the phase-65 IMPL: **`reference_plan_break_instructions_dont_compile`** (a non-compiling break proves nothing) and **`reference_sds_init_fetch_timeout_dial_budget_flake`** (the observed-once `-race` flake + its mechanism).
