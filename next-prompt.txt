START a fresh cold-start session for envoy-go — **the phase-41 circuit-breakers SPEC is COMPLETE and LANDED on master** (the THIRD Upstream-robustness-family row; `docs/envoy-go/phases/41-circuit-breakers/SPEC.md` — 14-section phase-40.1 keystone precedent, the §11 D-CB-* pins executed LIVE against `contrib-v1.37.2`, reviewer-APPROVED — + the STATE/ROADMAP/next-prompt roll, all docs-only; counts UNCHANGED). **The next lifecycle step is the phase-41 PLAN** — `superpowers:writing-plans`: decompose SPEC §10 (the ~12–16-task spine) into bite-sized TDD tasks, resolve the §12 D-S41-1..7 PLAN/IMPL D-questions, run the FINAL ADR-0045 split-gate re-check (pre-authorized NO split), and the plan-document-reviewer pass. The §11 D-CB-* empirical pins are RESOLVED — the PLAN does NOT re-probe. Confirm the tip via `git log --oneline -5` (HEAD is the phase-41 SPEC docs commit — the `41-circuit-breakers/SPEC.md` + the STATE/ROADMAP/next-prompt roll — sitting on the phase-41 BRAINSTORM commit `306e26a` + the substantive phase-40.3 IMPL squash `148f288`; NO substantive code has landed since `148f288` — both the BRAINSTORM and the SPEC are docs-only). Counts UNCHANGED at this SPEC: stat surface **1149** / fixtures **75** / fuzzers **42** / BackendKind tail **35** (`HTTP503Responder`) / DECISIONS tail **ADR-0247**, next-free **ADR-0248**.

## What just landed (the phase-41 SPEC — the durable record is `docs/envoy-go/phases/41-circuit-breakers/SPEC.md`; read that, not this summary)

The SPEC charters circuit breaking on the **`max_requests` keystone** with the full per-priority `circuit_breakers.*` stat surface registered for parity. The §11 D-CB-* pins were EXECUTED IN-SESSION live against `envoyproxy/envoy:contrib-v1.37.2` per `reference_docker_probe_bridge_network` (a slot-holding backend + concurrent load + `--mode validate` reject probes + a `/stats` name-roster scrape) + the go-control-plane v1.32.4 cache.

**THE HEADLINE — the live pins NARROWED the enforced scope (AMEND-CB1, human-confirmed 2026-06-19):** the BRAINSTORM Fork-1 "core overflow trio" (`max_connections` + `max_pending_requests` + `max_requests`) → the **`max_requests` keystone ONLY**. Why: the reference's fail-fast `503/UO` comes ONLY from a *request* budget — `max_connections` saturation is a SOFT THROTTLE that PENDS the request (sets `cx_open`=1, `upstream_cx_overflow`++, but does NOT 503), and `max_pending_requests` bounds the connection-wait QUEUE that envoy-go's synchronous, queue-less upstream path does not have. So cx/pending enforcement is DEFERRED to the future **per-protocol connection-pooling** family-row (where the pool/queue semantics belong); their stat names register emit-0 for parity. `max_requests` (the active-in-flight-request concurrency cap) maps EXACTLY onto envoy-go's model ⇒ clean cross-side parity.

**SPEC-firmed (AMEND-CB1..7):**
- **CB1** — `max_requests`-only enforcement (the human-confirmed narrowing); `max_connections`/`max_pending_requests` register-for-parity-but-defer.
- **CB2** — a `max_requests` overflow increments **`upstream_rq_pending_overflow`** (the SAME counter `max_pending_requests` would use); there is NO `upstream_rq_overflow`. The two request budgets differ ONLY by gauge (`rq_open` vs `rq_pending_open`).
- **CB3** — stat surface **+14 → 1163**: 10 per-priority `*_open` GAUGES (`cx_open`/`cx_pool_open`/`rq_open`/`rq_pending_open`/`rq_retry_open` × {default, high}) + 4 cluster-level overflow COUNTERS (`upstream_cx_overflow`/`upstream_cx_pool_overflow`/`upstream_rq_pending_overflow`/`upstream_rq_retry_overflow`), scoped to `circuit_breakers` clusters (existing fixtures unaffected — a recorded departure: the reference emits the overflow counters on ALL clusters). Only `circuit_breakers.default.rq_open` + `upstream_rq_pending_overflow` go LIVE; the other 12 emit 0 (the AMEND-OD3-4 emit-0-for-parity precedent). `remaining_*` gauges DEFERRED (`track_remaining`-gated).
- **CB4** — overflow ⇒ `503` + `UO` flag + `upstream_reset_before_response_started{overflow}`; envoy-go emits the 503 via the existing local-reply path + sets `UO` where the `%RESPONSE_FLAGS%` accesslog surface allows (a PLAN/IMPL pin — D-S41-3); the differential asserts the robust `503`-status + stats pair, NOT the access-log line.
- **CB5** — effective defaults 1024/1024/1024/3/∞ (via the `remaining_*` gauges); `max_requests` defaults 1024 ⇒ the breaker is OFF unless explicitly small.
- **CB6** — the PGV reject surface is THIN: only `Thresholds.priority` enum-range + `RetryBudget.budget_percent ∈ [0,100]` bite (budget `UInt32Value`s unbounded; duplicate-priority + `per_host_thresholds` ACCEPTED). envoy-go mirrors the 2 PGV rejects + adds a STRICT duplicate-priority house reject (ADR-0080) + SILENT-IGNORES `per_host_thresholds`. NO new fuzzer (config-parse, not wire-decode) — fuzzers STAY 42.
- **CB7** — +1 BackendKind `BlockingHoldResponder` (35 → 36 — holds a request until the driver releases) for the deterministic budget-fill.

**Architecture (ADR-0248 §Context anchored in SPEC §13):** a per-priority `circuitBreaker` counter struct on `Cluster` (`cluster.go:85`); a synchronous `tryAcquireRequest(DEFAULT)` at router admission (`do{H1,H2}ClusterAction` — `router.go:588`/`router_h2.go:57`, before the upstream call) + a `defer releaseRequest` on completion (endpoint-free, NOT piggybacked on `RecordUpstreamResult`); fail-fast `503`-UO + `upstream_rq_pending_overflow`++ + `rq_open`=1 on exhaustion; NO background goroutine; byte-identical when no `circuit_breakers`. The single ADR-0248 ABSORBS the BRAINSTORM's anticipated 2nd ADR (ADR-0249 no longer needed at the narrowed scope). Differential `0074-circuit-breaker-max-requests` (cross-side concurrency-driven overflow: fill `max_requests` with N held-open requests, poll `rq_open`→1 on both sides, fire the N+1th ⇒ cross-side `503` + `upstream_rq_pending_overflow` delta + `rq_open` parity, then release — SLEEPLESS via the release barrier; 2 `-count=1` deliberate breaks; ONE fixture — only `max_requests` enforced).

## The PLAN obligations (resolve the §12 D-S41-1..7 PLAN/IMPL D-questions; NO re-probe)

- **D-S41-1** the exact envoy-go house reject wording (`cluster: %q: circuit_breakers: …` — priority-range / retry_budget-percent / duplicate-priority).
- **D-S41-2** the `max_requests` absent-vs-0 disposition (absent ⇒ the 1024 default; explicit 0 ⇒ reject-all-requests — confirm 0 is a hard cap not "unset").
- **D-S41-3** the `UO` response-flag emission mechanism (whether the overflow local-reply can set `%RESPONSE_FLAGS%` `UO`, or it's a recorded departure — the differential does NOT depend on it).
- **D-S41-4** the `BlockingHoldResponder` release mechanism (a control endpoint vs a shared in-process channel) + how the driver confirms N-in-flight (poll `rq_open`→1) for BOTH the in-process subject AND the reference container over the bridge net. THE key IMPL risk (cross-Docker concurrency coordination).
- **D-S41-5** the `0074` constants (N / backendCount / convergeDeadline / held-count / refContainerListenerPort) single-sourced.
- **D-S41-6** the `circuitBreaker.prio[2]` priority-indexing (HIGH parses into a never-consulted slot + a registered emit-0 stat tree).
- **D-S41-7** the FINAL ADR-0045 split-gate re-check (anticipated NO SPLIT — ~250–400 prod LoC / ~12–16 tasks).

## Honor throughout (the PLAN discipline)

- **Lifecycle = one stage per session:** the PLAN next (`superpowers:writing-plans` — decompose SPEC §10 into bite-sized TDD tasks; resolve D-S41-1..7; the plan-document-reviewer pass). `feedback_git_worktrees` · `feedback_subagents_no_push` · `feedback_push_to_origin`.
- **ADR/process:** ADR-0044 (the ADR-0248 §Context drafted at the SPEC, the §Decision/§Consequences land IN-PLACE at the IMPL) · ADR-0045 (the split-gate re-check) · ADR-0052 (the six-gate at the IMPL) · ADR-0080 (envoy-go's own byte-stable rejects) · ADR-0106 + `reference_roadmap_split_phase_row_done` (row 41 is a flat un-split family row — it flips `done` at its single IMPL six-gate; NO parent rollup; the family stays open) · ADR-0227 (`contrib-v1.37.2`).
- **At the IMPL (later):** `reference_docker_probe_bridge_network` (the `0074` reference container reaches the shared `BlockingHoldResponder` over a bridge net; verify `upstream_rq_total > 0`) · `reference_differential_band_sigma_margin` (the `0074` is SLEEPLESS — the release barrier + poll-to-converge, NO `time.Sleep`) · `reference_fixture_workload_constant_desync` (single-source the `0074` constants).

## Read first (in order)

1. THIS FILE — the post-phase-41-SPEC cold-start (the SPEC is DONE + LANDED; the next step is the phase-41 PLAN — decompose SPEC §10, resolve D-S41-1..7, NO re-probe).
2. `docs/envoy-go/phases/41-circuit-breakers/SPEC.md` — the durable phase-41 SPEC (the §1.1 AMEND-CB1..7 block, the §3 architecture, the §5 proto roster, the §6 reject roster, the §7 +14 stat surface, the §8 `0074` differential, the §11 live-pin block, the §12 D-S41-* PLAN questions, the §13 ADR-0248 §Context). Read this, not the summary above.
3. `docs/envoy-go/phases/41-circuit-breakers/BRAINSTORM.md` — the phase-41 charter (superseded on the Fork-1 scope by AMEND-CB1; otherwise the design rationale).
4. `docs/envoy-go/STATE.md` — active-phase `phase 41 (circuit-breakers) SPEC done`; the count footer (1149 / 75 / 42 / 35 / ADR-0247, next-free ADR-0248); the lifecycle routing (next-skill = the 41 PLAN).
5. `docs/envoy-go/ROADMAP.md` — row 41 `in-progress` (the SPEC-DONE note + the scope-narrowing record) + the §Upstream-robustness family heading (the family-progress note).
6. The phase-40.1 PLAN `docs/envoy-go/phases/40.1-outlier-detection-consecutive-5xx/PLAN.md` (the keystone-PLAN precedent shape — the TDD-task spine, the D-question resolutions, the ADR-0045 re-check) + `docs/envoy-go/DECISIONS.md` (next-free ADR-0248) + `docs/envoy-go/BEHAVIOR_CONTRACT.md` (the stat-surface block at 1149 — the PLAN anticipates the `circuit_breakers.*` +14 block).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`.
