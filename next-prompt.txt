=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-VERIFIED 2026-07-21 (re-run MECHANICALLY at the phase-71 BRAINSTORM close, in the stage worktree) — it does NOT fire. `stop` was NOT created.** Actual output:
- **(1) prints `NOT DONE: row 71`** — row 71 (`tracing-custom-tags-metadata-route`) was registered `in-progress` at the phase-71 BRAINSTORM commit per the ROADMAP §Schema invariant; the registration re-opens check (1). It flips `done` at the phase-71 IMPL six-gate.
- **(2) prints THREE live "candidates:" sentences** — HTTP/3, xDS, Observability ⇒ three families STAY OPEN. **Cite the command, never the adjective** (`reference_sentinel_deferred_sentence_live_vs_historical`): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` ⇒ **3**. (The Observability sentence names the non-`REQUEST` MetadataKinds `ROUTE`/`CLUSTER`/`HOST`; the phase-71 pick `ROUTE` is NAMED in it and does NOT drop it — it narrows at the phase-71 IMPL row-done edit, the phase-57 precedent.)
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** ⇒ THREE families never opened.

Checks (2) + (3) print ⇒ the sentinel does NOT fire. Abundant work remains.

==============================================


## STATUS AS OF 2026-07-21: THE PHASE-71 BRAINSTORM IS COMPLETE AND LANDED. ROW 71 (`tracing-custom-tags-metadata-route`) IS REGISTERED `in-progress`. THIS SESSION PERFORMS THE PHASE-71 SPEC.

**What the phase-71 BRAINSTORM landed** (`git log --grep 'phase 71'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): the roller's AUTONOMOUS self-pick opened the SEVENTEENTH Observability-family row — the tracing `custom_tags` `metadata` type for the **`ROUTE`** MetadataKind (the FIFTH tracing `custom_tags` capability, the SECOND `metadata` MetadataKind after `REQUEST` @ 70). docs-only, ZERO production `.go`; row 71 registered `in-progress` (§Schema). The pick was established by TWO independent read-only cost-assessment agents at tip.
- **The subject:** lift the `custom_tags metadata route kind unsupported` reject (`internal/tracing/config.go:245-246`) → an accept arm cloning the phase-70 REQUEST accept, resolving the matched route's STATIC config metadata (`route.metadata.filter_metadata[ns]`) onto the ingress span over the LANDED `FilterChain.RouteMetadata() *corev3.Metadata` accessor (`internal/filter/http/chain.go:1156`, phase 24.2 / ADR-0165); emitted on BOTH OTLP + Zipkin via the phase-46 `Span.Attrs` seam. `CLUSTER`/`HOST` stay reject-loud (envoy-go-strict, ADR-0080).
- **Why smallest-defensible (BRAINSTORM §2.1 + §11):** it clones the phase-70 `descend`/`structpbValueToString` machinery VERBATIM (landed LAST SESSION, freshest substrate, lowest risk); the source is landed + reachable at the emit sites; the source is STATIC route config needing NO runtime writer (simpler than phase 70's Lua REQUEST arm). **STALE-COST CORRECTION:** the phase-70 BRAINSTORM lumped all three non-`REQUEST` MetadataKinds as "not exposed at the emit seam" — that is STALE for `ROUTE` (`chain.RouteMetadata()` IS landed; `reference_deferred_candidate_cost_restale`). Rivals ranked: `CLUSTER` (LARGE — the `upstream_cluster` gap) · `HOST` (MODERATE — reduced `envoy.lb` subset) · reconnection-backoff (SMALL but THIN) · upstream SDS (MODERATE) · `http_service` (MODERATE) · `spawn_upstream_span` (MOD-LARGE) · force-trace (LARGEST) · SDS rotation (LARGE) · the `ssl` stat family · never-opened families.
- **The one genuine design question (D-CTMR-RESOLVE-SEAM):** the source-shape asymmetry — `REQUEST` reads `metaLookup(ns,key)→*structpb.Value` (Bucket pre-keys the first segment, descend `MetaPath[1:]`); `ROUTE` reads `*corev3.Metadata.FilterMetadata[ns]→*structpb.Struct` (namespace→struct, descend the FULL `MetaPath`). Anticipated seam: a new nil-tolerant `routeMetaLookup func(ns) (*structpb.Value, bool)` param (namespace struct wrapped as a `StructValue`), symmetric with `metaLookup`, threaded at all 18 emit callers (closure at the 15 span sites, `nil` at the 3 no-chain-404 sites).
- **Anticipated envelope (at the phase-71 IMPL):** ZERO new packages (edits in `internal/tracing` + `internal/filter/hcm`) / +0 stats / +0 fuzzers (a `FuzzHCMConfigParse` seed extension) / +0 BackendKinds / +0 go.mod modules (`corev3`/`metadatav3`/`structpb`/`protojson` all already imported) / ONE new OTLP fixture `0115` (static-route-metadata positive arm + `default_value` arm; NO writer) ⇒ fixtures **116 → 117** / DECISIONS tail **ADR-0292 → ADR-0293** (at the SPEC).

**⚠️ PUSH (idempotent — evaluate, then act):** FIRST ACTION: `git fetch`; if origin/master is BEHIND local master and the local tip is the phase-71 BRAINSTORM squash (`git log --grep 'phase 71'`, locate by SUBJECT), `git push origin master` per `feedback_push_to_origin`; if in sync, proceed; if REJECTED (diverged), STOP and reconcile — never rebase away landed squashes.

## What THIS session does — the phase-71 SPEC (RE-DERIVE the BRAINSTORM)

One stage per session: the SPEC. **RE-DERIVE the BRAINSTORM, do NOT execute it.** Dispose the D-CTMR-* docket one-for-one via EXECUTED live-probe arms against `envoyproxy/envoy:contrib-v1.37.2` + a full read-only code re-derivation of every §6 edit site (a BRAINSTORM cite is NOT evidence — `feedback_brief_citations_not_evidence`; RE-DERIVE each anchor at tip). Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; docs-only APART from the ADR-0293 §Context append. Row 71 STAYS `in-progress`. Then roll THIS file to the phase-71 IMPL.

**The D-CTMR-* docket to dispose (BRAINSTORM §7):**
- **D-CTMR-SCOPE** — `ROUTE` MetadataKind only; `CLUSTER`/`HOST` stay reject-only.
- **D-CTMR-PROTO** — RE-DERIVE the go-control-plane `CustomTag_Metadata`/`MetadataKind`/`MetadataKey` field shapes at the pin (phase 70 confirmed the kind-getters live on `*metadatav3.MetadataKind`, `k.GetRoute()`).
- **D-CTMR-WIRE / D-CTMR-ZIPKIN-WIRE** — probe the reference emits `{key=tag name, value=serialized string}` on OTLP + the Zipkin tags map (anticipated identical to REQUEST).
- **D-CTMR-VALUE-SERIALIZE** — probe the `structpb.Value`→string table matches the phase-70 P3 (shared machinery).
- **D-CTMR-DEFAULT** — probe the default/omit rule matches phase-70's `request_header` rule (present-empty EMITS `""`).
- **D-CTMR-RESOLVE-SEAM** — the source-shape asymmetry (§2.3): `routeMetaLookup(ns)` returns the namespace struct as a `StructValue`; the arm descends the FULL `MetaPath` (NOT `[1:]`). Pin the exact param shape.
- **D-CTMR-CONFIG-KIND** — `kindMetadataRoute` constant vs a `MetaSource` sub-enum (anticipated `kindMetadataRoute`).
- **D-CTMR-BUCKET-SEAM** (route variant) — the `routeMetaLookup` threading; RE-DERIVE the exact caller count + the 3 nil sites at tip (phase-70 was 18 = 5+6+7).
- **D-CTMR-REJECT** — the accept-arm structural-edge rejects (empty ns/path/segment PGV-parity); probe the reference ACCEPTS ROUTE / BOOTS CLUSTER+HOST.
- **D-CTMR-FIXTURE / D-CTMR-FIXTURE-SOURCE** — ONE new OTLP `0115`, static-route-metadata (NO writer); Zipkin dir anticipated NO (the shared `Span.Attrs` seam + unit tests prove both).
- **D-CTMR-FUZZSEED** — extend the phase-70 `withMetaTags` seed with a ROUTE tag (+0 fuzzers); dispatch-verify.
- **D-CTMR-SPLIT** — SINGLE FLAT ROW anticipated (~8–10 tasks; ADR-0045 armable).

**Probe note:** unlike phase 70 (which needed a Lua-set dynamic-metadata WRITER for the REQUEST arm), the ROUTE source is STATIC route config — the probe fixture configures a RouteConfiguration route with `metadata: {filter_metadata: {ns: {key: "value"}}}` + a `custom_tag{metadata{kind: ROUTE, metadata_key:{key: ns, path:[{key}]}}}`; NO writer needed. Use fresh probe containers per arm (`feedback_probe_fresh_container_per_arm`), the host-gateway IP for reference→host reachability, and assert decode actually ran.

**Stage-close mechanics (the SPEC):** draft ADR-0293 §Context per ADR-0044 (the DECISIONS tail flips ADR-0292 → ADR-0293 AT the SPEC commit; §Decision + §Consequences at the IMPL IN PLACE, no renumber; next-free ADR-0294). Row 71 STAYS `in-progress`. NO deferred-sentence narrow at the SPEC (the phase-57 precedent). STATE §Current rolled IN PLACE (lifecycle 1 → 2; lineage re-capped at five). next-prompt.txt rolled to the phase-71 IMPL. Re-run the sentinel MECHANICALLY at close: (1) `NOT DONE: row 71`; (2) **3**; (3) `NEVER OPENED: gRPC/Runtime/WASM`. Does NOT fire; no `stop`. **Adversarially verify before landing** (the phase-70 SPEC precedent — TWO independent verifiers in PRIVATE scratch, `reference_parallel_subagents_private_scratch`).

## Read first (in order)
1. `docs/envoy-go/phases/71-tracing-custom-tags-metadata-route/BRAINSTORM.md` — the D-CTMR-* docket (§7), the anticipated edit sites (§6), the resolve-seam design (§2.3, §2.5), the rejected roster (§2.1), the cost derivations (§11).
2. `docs/envoy-go/ROADMAP.md` — row 71 (registered `in-progress`) + the live Observability `candidates:` sentence + §Schema.
3. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline) + the §Recent lineage (the phase-71 BRAINSTORM + the phase-70 stages).
4. `docs/envoy-go/DECISIONS.md` tail — ADR-0292 (the phase-70 IMPL, COMPLETE — the `metadata`/`REQUEST` precedent to clone for ADR-0293) + ADR-0044/0045/0106 (the ADR/split/valve discipline) + ADR-0165 (the `RouteMetadata` accessor) + ADR-0080 (envoy-go-strict reject).
5. The phase-70 SPEC (`docs/envoy-go/phases/70-tracing-custom-tags-metadata/SPEC.md`) for the SPEC shape + the D-CTM probe-arm structure to mirror.
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — trust MEMORY.md's index; esp. `feedback_brief_citations_not_evidence`, `feedback_git_worktrees`, `feedback_execution_style`, `reference_filterchain_dynamicmetadata_accessor` (verify the RECEIVER), `reference_detrand_race_catches_protojson_value_substring`, `reference_probe_must_discriminate`, `feedback_probe_fresh_container_per_arm`, `reference_host_gateway_ip_docker_desktop`.
