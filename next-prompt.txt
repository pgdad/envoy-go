=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage. To use it to advance the project: read `docs/envoy-go/STATE.md` for the recorded NEXT, perform that one stage (the concrete action is spelled out below), land it, and re-roll this file to the following stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, the active phase is fully landed, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action. While ANY stage remains — as NOW: the **47.2a IMPL is pending, then 47.2b SPEC→PLAN→IMPL** (the FINAL sub-leg that flips row 47 `done`; NO separate 47.2b BRAINSTORM — BRAINSTORM-47.2 already chartered both sub-legs), so tasks are NOT complete — do NOT create `stop`; perform the stage described below.
==============================================

RESUME a fresh session for envoy-go (router last updated 2026-06-29; the **phase 47.2a (`report_counters_as_deltas`) PLAN is DONE and LANDED** — docs-only direct-on-master; `docs/envoy-go/phases/47-stats-sink-metrics-service/PLAN-47.2a.md` authored [9-task TDD spine, the PLAN-47.1 shape]; it RESOLVES the §12 D-questions with concrete code seams [D-MSD-PLACEMENT → a NEW `internal/statssink/delta.go` `deltaState` applied in `Submit`, building a NEW batch that never mutates the shared `flusher.go:47` slice; D-MSD-RECEIVER-SUM → an additive `FamilySum`; D-MSD-FIRST-FLUSH-IMPL → no special branch; D-MSD-BREAK-SET → emit-absolute + skip-the-latch; D-MS-SPLIT-2 → no sub-split]; the **plan-document-reviewer (general-purpose) APPROVED after one fold-iteration** — 1 CRITICAL folded [C1: the spine had omitted the `0090` driver blank-import in `test/differential/runner_test.go` → without it every `0090` run is a vacuous green; added a dedicated registration step + a "must show a real RUN" guard] + 1 Important [I2: the `transport_api_version: V3` line IS present in both 0089 yamls] + 3 Minor [M3 ADR grep / M4 the real `fakeMetricsClient` / M5 the stale `StatsSinkConfig` type doc]). **ROW 47 STAYS `in-progress`** (flips `done` at the FINAL sub-leg 47.2b IMPL per ADR-0106 + `reference_roadmap_split_phase_row_done`). The next action is the **47.2a (`report_counters_as_deltas`) IMPL** — execute the PLAN-47.2a TDD spine.

HEAD will be the phase-47.2a PLAN commit atop the phase-47.2a SPEC commit `2adc76f7`. **Re-read the shas with `git log --oneline -6`, do NOT assume.** Confirm `git status` clean (the untracked `.claude/` is session cruft, leave it) + master == origin/master.

## What the 47.2a IMPL IS (the next stage — execute the TDD spine subagent-driven in a worktree)
Execute `docs/envoy-go/phases/47-stats-sink-metrics-service/PLAN-47.2a.md` (the 9-task TDD spine) subagent-driven (`feedback_execution_style` · `superpowers:subagent-driven-development`) in a FRESH worktree off master (`feedback_git_worktrees`). The PLAN is the authority — read it first (esp. the File map, the per-task Files/Steps, and the "Notes carried from the SPEC" tail). Read the SPEC (`SPEC-47.2a.md`) for the WHY behind each seam. The spine, in order:
1. Baselines + PROGRESS + the FINAL ADR-0045 re-check (D-MS-SPLIT-2 — no sub-split).
2. Lift the `internal/bootstrap/bootstrap.go:446` deltas reject + add `StatsSinkConfig.ReportCountersAsDeltas bool` (`:272`) + set it on the append (`:455`) + the THREE doc comments (`:265`–`:271`, `:322`–`:331`, `:427`–`:430`). TDD: replace the deltas-reject test with a parse-accept (true sets the field); KEEP the `emit_tags_as_labels:true` reject + all others strict.
3. The NEW `internal/statssink/delta.go` `deltaState` (`map[string]uint64`) + `apply(abs) []*dto.MetricFamily` (Counter-only delta; first=absolute via the empty-map zero-default; idle=0 still-emitted; gauges shared-by-pointer absolute; NO input mutation — build new COUNTER families). TDD table-driven.
4. Thread the bool into the sink (`sink.go`: a `delta *deltaState` field; a `reportCountersAsDeltas bool` param on `NewMetricsServiceSink` + `newSinkWithCapacity`; apply in `Submit` BEFORE the channel send) + the 5 call-site fixes (`registration_test.go:33`; `sink_test.go:156,198,236,252`) + a delta-mode Submit test using the real `fakeMetricsClient`/`fakeMetricsStream.messages()`. false ⇒ 47.1-identical (value-stability).
5. `cmd/envoy-go/main.go:199` thread `cfg.ReportCountersAsDeltas`.
6. The `test/helpers/metricsservice` delta-SUM surface (an additive `sums map[string]float64` + `FamilySum(name)`; update `StreamMetrics` + `Reset`). `Family()` STAYS for 0089 non-regress. TDD.
7. The `0090-stats-sink-metrics-service-deltas` cross-side EXACT fixture: clone the AUTHORITATIVE `0089/driver/driver.go` (NOT the stale README — `reference_periodic_sink_differential_two_receivers`); FamilySum poll-to-converge the SUM == K (replacing 0089's `value==K`); add `report_counters_as_deltas: true` to BOTH yamls (alongside the already-present `transport_api_version: V3`); **REGISTER the driver blank-import in `test/differential/runner_test.go` after `:116`** (LOAD-BEARING — without it every 0090 run matches zero subtests = vacuous green; confirm a real RUN, not "no tests to run"); reconcile the stale 0089 README + expectations.yaml two-receiver prose.
8. CONTROLLER: the `0090` deliberate breaks (a) emit-absolute + (b) skip-the-latch (`-count=1` per `reference_differential_break_protocol_count1`; `-run 'TestDifferential/0090'` NEVER bare per `reference_differential_run_selector`) — both must FAIL (the SUM explodes), restore via `git restore` (`feedback_subagent_worktree_detach`); 20/20 flake-soak; the FULL-package `internal/statssink` `-race` (the per-sink delta map + the Flusher ticker + the sink writer goroutine are background mutators — `reference_full_suite_race_after_background_mutator`).
9. Full **92-dir** differential + the six-gate + the ADR-0263 §Decision/§Consequences body (per ADR-0044) + BEHAVIOR_CONTRACT delta + STATE/ROADMAP (row 47 STAYS `in-progress`).

Subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit + cleans leak files + re-runs the six-gate + the FULL-package `-race` + does the `0090` deliberate breaks ITSELF + squashes + pushes at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). Per-task `gofmt -l` + `golangci-lint` on touched packages (`feedback_pertask_gofmt_lint`). ANCHORS **ADR-0263**. Anticipated counts: stat **1200** (+0) / fixtures **91 → 92** (`0090-stats-sink-metrics-service-deltas`) / fuzzers **50** / BackendKind **38** / DECISIONS **ADR-0262 → ADR-0263**; NO new package, NO new go.mod module. **ROW 47 STAYS `in-progress`** (47.2a is NOT the final sub-leg). After it lands, ROLL THIS FILE to the **47.2b SPEC**.

## What 47.2b IS (the SECOND-and-FINAL sub-leg — opens at its own SPEC AFTER 47.2a lands)
**47.2b = `emit_tags_as_labels=true`** (anticipated **ADR-0264**): SN-rule tag→`LabelPair` extraction (the dotted-name tag SEGMENTS become `MetricFamily.metric[].label[]` pairs instead of inlined into the name), reusing the `internal/stats/name.go` SN1–SN9 extractors (`flattenToProm` at `:39`); lift the 47.1 reject at `bootstrap.go:449`; add `StatsSinkConfig.EmitTagsAsLabels`; the `0091-stats-sink-metrics-service-labels` LabelPair differential (fixtures **92 → 93**). The exact emitted name/label-key form is a 47.2b SPEC live-probe pin (D-MS-LABEL); the refactor-shared-`name.go`-core-vs-parallel-extractor decision is D-MS-LABEL-REUSE. **47.2b IMPL FLIPS ROW 47 `done`.** NO separate 47.2b brainstorm — BRAINSTORM-47.2 already chartered both sub-legs.

## Counts at the 47.2a PLAN (docs-only — UNCHANGED; re-verify, do NOT assume)
stat surface **1200** (non-H2 **1196**) / fixtures **91** (tail `0089-stats-sink-metrics-service`; `ls -d test/fixtures/*/ | wc -l`) / fuzzers **50** (`grep -rh '^func Fuzz' --include='*.go' . | wc -l`) / BackendKind **38** (`H2GoawayResponder = 38`) / DECISIONS **ADR-0262** (next-free **ADR-0263**); `github.com/prometheus/client_model v0.6.1` direct (landed at 47.1 — `LabelPair` already available for 47.2b); `go-control-plane/envoy v1.32.4`; `internal/statssink` is the package both sub-legs modify. Anticipated: 47.2a IMPL → fixtures **92** (`0090`), DECISIONS **ADR-0263**; 47.2b IMPL → fixtures **93** (`0091`), DECISIONS **ADR-0264**, row 47 `done`. Both legs: stat **1200 UNCHANGED** (+0 self-stats preserved) / fuzzers **50** / BackendKind **38** / NO new package / NO new go.mod module.

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** The next stage is the 47.2a IMPL (lifecycle-state 3 → 4 for the 47.2a sub-leg). After it lands, ROLL THIS FILE to the 47.2b SPEC.
- **SPEC/PLAN = docs-only direct-on-master** (the family-row precedent); the IMPL is subagent-driven (`feedback_execution_style` · `superpowers:subagent-driven-development`) in a worktree, subagents commit LOCALLY only (`feedback_subagents_no_push`), the CONTROLLER squashes + pushes at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`).
- **Process anchors:** ADR-0106 (per-leg rows — row 47 flips `done` at the 47.2b IMPL) · ADR-0045 (sub-split — 47.2a final no-split confirmed at the PLAN [D-MS-SPLIT-2]) · ADR-0044 (ADR §Decision+§Consequences-at-IMPL — ADR-0263 §Context DRAFTED at the SPEC §13, body at the 47.2a IMPL) · ADR-0080 (strict-reject — 47.2a LIFTS the deltas reject `:446`, KEEPS the rest; 47.2b LIFTS the tags-as-labels reject `:449`) · ADR-0059 (the in-tree `Registry`/`Walk`/`Freeze`) · ADR-0060 (histograms deferred — still moot) · ADR-0061 (the SN1–SN8 Prometheus name/label rules — the 47.2b tag-extraction substrate) · ADR-0064 (the `stats_sinks[]` silent-ignore — 47.1 LIFTED the metrics_service sink) · ADR-0158 (the `grpcclient.Dialer` typed-wrapper) · ADR-0262 (the 47.1 anchor) · ADR-0263 (ANCHORED at the SPEC §13 §Context draft, §Decision/§Consequences body at the 47.2a IMPL).
- **Differential discipline** (for the 47.2a `0090` fixture): clone the authoritative `0089/driver/driver.go` (NOT the stale README — `reference_periodic_sink_differential_two_receivers`); the TWO-per-side-receiver pattern + the hard `Close()` teardown + the NEW per-name delta-SUM surface (`FamilySum`); **REGISTER the driver in `test/differential/runner_test.go`** (else vacuous green); `-run 'TestDifferential/0090'` NEVER bare (`reference_differential_run_selector`); `-count=1` on every break + `-race` (`reference_differential_break_protocol_count1`); the Docker bridge + decode-ran proof (`reference_docker_probe_bridge_network`); the gRPC metrics receiver is driver-owned NOT a BackendKind (`reference_differential_grpc_receiver_driver_owned`); assert the aggregated delta-SUM payload NOT framing (`reference_streaming_sink_differential_framing`); the live reference is the wire truth (`reference_wire_format_both_sides_see_same_bytes`); one fixture dir = ONE bootstrap config = ONE runner branch (`reference_differential_fixture_dispatch_constraint` — 0090 and 0089 CANNOT share a dir); the Flusher + sink writer goroutine + the NEW per-sink delta map are background mutators ⇒ FULL-package `-race` (`reference_full_suite_race_after_background_mutator`).

## Read first (in order)
1. THIS FILE — the cold-start router (the 47.2a PLAN is DONE + LANDED; row 47 `in-progress`; the next action is the 47.2a IMPL — execute the PLAN-47.2a TDD spine subagent-driven in a worktree).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 47.2a PLAN done`) + the recorded NEXT (the 47.2a IMPL) + the counts (1200 / 91 / 50 / 38 / ADR-0262).
3. `docs/envoy-go/phases/47-stats-sink-metrics-service/PLAN-47.2a.md` — the 9-task TDD spine (the File map; the per-task Files/Steps; the "Notes carried from the SPEC" tail). THE EXECUTION AUTHORITY.
4. `docs/envoy-go/phases/47-stats-sink-metrics-service/SPEC-47.2a.md` — the 47.2a charter (the WHY): §3 the delta transform design + the no-shared-slice-mutation HARD CONSTRAINT, §8 the `0090` differential + the `FamilySum` receiver surface, §11 the EXECUTED pins, §12 the D-questions, §13 the ADR-0263 §Context draft.
5. `docs/envoy-go/DECISIONS.md` — **ADR-0262** (the 47.1 anchor; ADR-0263 §Context drafted at SPEC-47.2a §13, body lands at the 47.2a IMPL per ADR-0044).
6. The 47.1 as-built code 47.2a modifies: `internal/statssink/mapping.go` (`snapshot` `:22` — the absolute Counter arm `:31` the delta transform consumes), `internal/statssink/flusher.go` (`flushOnce` `:46`–`:50` — the per-sink fan-out + the no-in-place-mutation constraint), `internal/statssink/sink.go` (`MetricsServiceSink` `:60`, `Submit` `:101`, the constructors `:77`/`:83` — where the bool + the `deltaState` thread in), `internal/bootstrap/bootstrap.go` (`StatsSinkConfig` `:272`, `parseMetricsServiceConfig` `:431`, the deltas reject `:446` to LIFT, the append `:455`), `internal/stats/counter.go` (`Load` `:30`; monotone), `cmd/envoy-go/main.go` (`:199` the sink construction).
7. `test/fixtures/0089-stats-sink-metrics-service/{driver/driver.go,envoy.yaml,envoy-go.yaml,expectations.yaml}` (the AUTHORITATIVE differential template 0090 clones — two per-side receivers + hard `Close()` + the subset, `numReq=7`; the README is STALE) + `test/helpers/metricsservice/metricsservice.go` (the receiver to extend with `FamilySum` — `Family()` last-seen `:169`, `StreamMetrics` `:136`, `Reset` `:202`) + `test/differential/runner_test.go` (`:116` the 0089 import — add the 0090 sibling).
8. `docs/envoy-go/ROADMAP.md` — row 47 (`stats-sink-metrics-service`, `in-progress`) + the Observability family-progress note.
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_subagents_no_push`, `feedback_subagent_autocommit_claudemd`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_detach`/`_path_targeting`, `reference_roadmap_split_phase_row_done`, `reference_stats_sink_metrics_service_probe`, `reference_periodic_sink_differential_two_receivers`, `reference_differential_grpc_receiver_driver_owned`, `reference_streaming_sink_differential_framing`, `reference_docker_probe_bridge_network`, `reference_full_suite_race_after_background_mutator`, `reference_differential_break_protocol_count1`, `reference_differential_run_selector`, plus the differential-discipline references above).
