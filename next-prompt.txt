=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. **There IS banked work at this tip** — phase 75 is mid-lifecycle at lifecycle-state 3, with a PLAN whose spine is already largely EXECUTED — so this session ADVANCES it and does NOT open a new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-RUN MECHANICALLY 2026-07-25 at the phase-75 PLAN close, TWICE (stage worktree AND landed master after the squash-push), with IDENTICAL output both times. It does NOT fire. `stop` was NOT created and MUST NOT be.** ⚠️ **Re-run it yourself anyway and record what YOU see** — a prior run's result is not evidence for the current tip. ACTUAL output, recorded rather than predicted:
- **(1) prints `NOT DONE: row 75`** — unchanged, because **the ROADMAP is BYTE-UNTOUCHED at a PLAN** and row 75 STAYS `in-progress`. Check (1) stays LIVE and goes silent only at the phase-75 IMPL, which is THIS session.
- **(2) prints THREE live "candidates:" sentences** — HTTP/3 (`:185`), xDS (`:195`), Observability (`:205`). **Cite the command, never the adjective** (`reference_sentinel_deferred_sentence_live_vs_historical`): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` => **3**. ⚠️ A naive `grep -c 'candidates:'` returns a larger number, and the **four** `candidates were:` recaps inside `:205` are HISTORICAL. Occurrence-counting (`grep -oE … | wc -l`) also returns **3**.
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** => THREE families never opened. (The `WASM` line remains a ROADMAP **bookkeeping artifact** — `internal/wasm/` is a substantially-built wazero host and rows 25/25.1/25.2/25.3 are all `done` — deliberately LEFT AS-IS.)

All three print => the sentinel does NOT fire. Abundant work remains.

⚠️ **CHECK (1)'s BLIND SPOT — THE RECORDED FIGURE WAS WRONG AND IS NOW CORRECTED. RE-DERIVE IT; NEVER COPY IT.** At this tip it is **107 data rows, 103 matched, FOUR misses** — `| 00 | bootstrap | — | done |` (the `[0-9.,  ]*` field does not match an **em-dash**), `| 04 | http-1.1 | 03 | done |` (the `[a-z0-9-]+` slug field does not match the **DOT**), and **`| 28.1a |` / `| 28.1b |`** (the `^\| [0-9.]+ \|` id field cannot match a **letter suffix**). All four are `done`, so there is no current impact. The main table runs `:31`–`:137` = **107** rows. ⚠️ **The prior router recorded "106 rows, 102 matched" and claimed it was re-derived at the phase-75 BRAINSTORM tip; both halves were false** — 106/102 is the *phase-74 IMPL* figure, and **row 75's own addition invalidated it in the very commit that claimed to re-derive it.** The ROADMAP is byte-identical from `e822f1ad` to now and measures 107/103 at every phase-75 tip. **This single figure has now been recorded wrong in two consecutive lineages** (phase-74 PLAN: *"104 rows, 102 matched, TWO misses"*).

==============================================


## STATUS AS OF 2026-07-25: THE PHASE-75 PLAN IS COMPLETE AND LANDED. ROW 75 REMAINS `in-progress`. THIS SESSION PERFORMS THE PHASE-75 IMPL — THE FINAL STAGE OF THE ROW.

**What the phase-75 PLAN landed** (`git log --grep 'phase 75'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): docs-only, **ZERO production `.go`**, **EXACTLY TWO files in the phase-directory delta** — the new `PLAN.md` (an **11-task TDD spine T1-T11** with a **TEN-break roster**) and `PROGRESS.md`. **ROADMAP and DECISIONS BYTE-UNTOUCHED.** **SEVEN agents at tip `cedd2f27` on disjoint remits — FOUR read-only re-derivation agents and THREE BUILD-BY-EXECUTION agents in SEPARATE git worktrees** (`phase-75-plan-v1/v2/v3`, private scratch, local commits only, never pushed).

**⚠️⚠️ THIS PLAN IS NOT LIKE ITS PREDECESSORS: IT EXECUTED MOST OF THE ROW.** SPEC §16 closed with *"What NO ONE checked, stated plainly: **NO BUILD OF ROW 75 EXISTS.**"* **That is no longer true.** The row was compiled, the unit suite and the **FULL differential** ran green, and **EIGHT of the TEN breaks were executed**. **Your job is verification-in-place and the docs half — not discovery.** Where `PLAN.md` says `[RUN]`, re-run and confirm rather than re-deriving from scratch.

### What matters for the NEXT session, in order

1. **⚠️ THE SENTINEL DID NOT FIRE AND `stop` WAS NOT CREATED. Re-run all three checks yourself and record the ACTUAL output; do not predict it.** Check (1) still prints `NOT DONE: row 75` and **should go SILENT at THIS session's close** (row 75 is the last non-`done` chartered row) — **but record what you actually see either way.** **Create `stop` if and ONLY if ALL THREE print nothing.** Checks (2) and (3) will still print, so it will NOT fire.

2. **⚠️⚠️ READ `PLAN.md` §1.3 BEFORE TOUCHING ANYTHING. IT REFUTES BOTH THE SPEC AND THE PRIOR ROUTER ON THIS ROW'S CENTRAL CLAIM.** SPEC §10.1 and the prior router item 5(a) both state that the new positive unit arm **IS** the discriminating break for the pinned predicate. **It is not, and the prediction is inverted.** **Break D** (deleting the `len(tlsConn.ConnectionState().PeerCertificates) == 0` guard so the Inc is unconditional) fires **`TestServeConnection_SSLHandshakeIncrements`** — a **PHASE-74** test — through `assertSSLCrossProduct`'s **NEGATIVE** half (`ssl.no_certificate = 1, want 0 — only [handshake] may move on this arm`), **while the new positive arm PASSES**: an unconditional Inc still leaves the counter at 1 on the arm that wants 1, so the positive half is structurally blind to it. **And Break D′ — the decisive stacked control** (same edit, with `sslLeafRoster` reverted to the three phase-74 leaves) leaves the **ENTIRE package GREEN, `ok … 3.233s`.** ⇒ **the one-line roster extension in `assertSSLCrossProduct` is the SOLE guard on the row's central pinned decision — NOT accommodation for the new arm.** Reproduced by the controller end-to-end. **The positive arm catches *never-Inc'd* (Break E) and *mode-gated* (Break F); the roster extension catches *unconditional* (Break D). Neither substitutes for the other. Land BOTH (T3).**

3. **THE PREDICATE IS PINNED — do not re-open it, and it is now PROVEN load-bearing.** `len(tlsConn.ConnectionState().PeerCertificates) == 0` **ALONE, NO client-auth term.** Settled at the WIRE at the SPEC (a `DownstreamTlsContext` with only `tls_certificates`, `openssl s_client -msg` showing ZERO `CertificateRequest`, counter fires anyway) and **proven load-bearing at the PLAN by Break F**, which adds the refuted mode term `selected.tlsCfg.ClientAuth != stdtls.NoClientCert &&` — ⚠️ **it COMPILES in one edit** (`selected.tlsCfg` is already in scope two lines above), so the SPEC's hedge about needing a substitution was unnecessary.

4. **THE HEADLINE RESULT — THE FIRST SUBJECT-SIDE MEASUREMENT EXISTS AND THE SIDES AGREE EXACTLY.** `0110-tls-require-client-cert-false` gained a cross-side `fixture.StatsAsserter` and was RUN against the live reference: `ssl.handshake=2 ssl.no_certificate=1 ssl.fail_verify_error=1 ssl.fail_verify_no_cert=0 (downstream_cx_total=3)` on **BOTH** sides. `envoy_listener_ssl_no_certificate{envoy_listener_address="0.0.0.0_10446"} 1` (reference) vs `…{envoy_listener_address="___20016"} 1` (subject) — **metric NAMES byte-identical, addresses differing ONLY in the label.** The discriminating asymmetry (`handshake=2` vs `no_certificate=1`) is observed per side. **FULL differential suite: `ok … 399.675s`, exit 0, no flake; fixture count stays 119.** ⚠️ **The subject binds the IPv6 WILDCARD** — the label is `___<port>`, **not** `0_0_0_0_<port>`; the agent wrote the keying comment wrongly and caught itself by execution.

5. **⚠️ T1 MUST ORDER THE GUARD FIRST.** Two `reflect.DeepEqual` **exact-set** pins (`manager_test.go` name-set, `quic_test.go` QUIC name-set) compare a 3-element `want` against what `listenerSSLNames` collects, so **the production registration alone turns both RED before any guard edit is written.** Extend both `want` slices FIRST (RED: `got` 3 / `want` 4), then land the field + registration (GREEN). `"ssl.no_certificate"` sorts **4th / LAST** — a pure APPEND, nothing shifts.

6. **⚠️ A STALE `-run` SELECTOR ON A UNIT TEST PRINTS `ok … [no tests to run]` AND EXITS 0.** A NEW SHAPE of `reference_differential_run_selector`, previously indexed only for the differential runner's subtests. T1 renames `…RegistersExactlyThreeSSLNames` → `…FourSSLNames`, so **every `-run` string must be re-derived after T1** or a break command self-certifies green while executing nothing. ⚠️ The rename touches **THREE** sites, one of them a cross-reference inside another test's doc comment.

7. **⚠️ THE "DELETE THE REGISTRATION" BREAK DESTROYS ITS OWN EVIDENCE FULL-PACKAGE.** It is a SIGSEGV in a background goroutine (`stats.(*Counter).Inc` ← `serveConnection` ← `acceptLoop`) that names **no test** and aborts the binary before the guard's output flushes. **Run it `-run` isolated.** The answer to *"assertion or panic?"* is **BOTH, decided by the command.** Related: **a pre-existing test already drives the phase-75 Inc site** — `TestNewManager_ChainSelectionPropagation` builds one-way TLS chains via `mkDownstreamTSInline` — so the nil-counter hazard is immediate, not hypothetical.

8. **⚠️ THREE SPEC CLAIMS ABOUT THE FIXTURE LEG ARE REFUTED.** (a) **The fast-failure `ssl.*` suppression is REFERENCE-ONLY** — envoy-go's `serveConnection` accounts `ssl.*` at step (6), strictly before the step-(7) upstream dial, and the subject's numbers were **byte-identical** under a refused upstream while the reference gave four honest zeros. `reference_ssl_stats_suppressed_by_fast_failing_upstream` is reference-scoped. (b) **The ABSENT check does NOT guard what SPEC §8.2 says** — deleting the registration of a counter that IS `Inc`'d SIGSEGVs the subject subprocess and the run dies at `structuralCheck` before `AssertStats` executes; its real remit is a registered-but-unscraped name plus stopping the `want: 0` row passing vacuously. (c) **The fast-failure arm is UNREACHABLE on `0110`** — `structuralCheck` outranks both preconditions, so a dead upstream kills the run at step 8. **Do not spend a cycle re-attempting it.**

9. **⚠️ ADR-0297's OWN ¶7 AND ¶9 ARE DEFECTIVE, AND T11 CORRECTS THEM IN PLACE.** **¶7's grep claim is SELF-FALSIFYING** — it asserts *"exactly ONE hit … the citing sentence itself"* while `grep -n 'VERIFYIFGIVEN'` now returns **TWO lines / THREE occurrences**, one of them ¶7. **This reproduces, one ADR later, the exact species the phase-74 IMPL had just fixed in ADR-0296 ¶3.** ⇒ restate with NO number, or scope the grep; **and the correction blockquote must NOT spell the token or restate any whole-file count**, or it mints another counter-example. **¶9's *"SELF vs OTHER-ADR (n=4)"* form rule is REFUTED** by an **n=7** population with a THIRD form: `DECISIONS.md:17211` sits inside ADR-0294 and reads *"CORRECTED at the phase-73 IMPL"* — a later phase correcting a DIFFERENT landed ADR, rendered **INLINE**. **The surviving discriminator is graft SCALE** (inline attaches to the clause it contradicts; indented stands alone where a whole bullet/paragraph is re-characterised). **The INDENTED form stays right for phase 75; replace the reason.** ⚠️ **The prescription has now outlived THREE wrong justifications — family, self-vs-other, scale.**

10. **⚠️ SPEC §9's *"No two-ADR heading precedent exists in this file"* IS REFUTED — there are 16.** `BEHAVIOR_CONTRACT.md:785` is the exact later-phase-extends shape (`… (per phase 49 ADR-0266; batching per phase 50 ADR-0267)`), semicolon-joined. **The `:1849` heading IS extended.** Obeying SPEC §9 would have left it falsely attributing a four-name roster to one ADR.

11. **⚠️ BEHAVIOR_CONTRACT EDITS APPEND IN PLACE — DO NOT INSERT PARAGRAPHS.** `:1849/:1851/:1853/:1855/:1857/:1859/:5002` are cited BY LINE from `ROADMAP.md:137`, `STATE.md:20/46/48`, phase-75 `SPEC.md` and `BRAINSTORM.md`. **The new ledger line after `:5002` must be the ONLY line-adding edit** (`5744 → 5746`). The ledger line matches the **TAIL** entry's byte form: em dash **U+2014**, arrow **`→` U+2192** (never ASCII), bold running **THROUGH** the parenthetical to the colon — `:5000` is the sole outlier, do not copy it. ⚠️ **Remove the *"three-fifths"* ratio rather than bumping it** — it was already wrong at the phase-74 tip (`:928`, same phase, enumerates FOUR surviving families ⇒ 3+4=7). ⚠️ **The ledger is discontinuous in TWO places** (`1198 → 1200` is unrecorded, additional to `1200 → 1201`) — **record both, back-fill neither.**

12. **⚠️ THE `0111` DECISION, AND THE CONTROLLER'S REVERSAL.** `0111`'s three value rosters **STAY AT THREE**; its **PROSE MUST change**. Of the NINE TLS-downstream fixtures only **`0110` is `require=false`**, so `0111`'s `require=true` shape holds this counter at 0 on every arm and a value pin would document a vacuous `0 == 0` while `0110` carries a discriminating non-zero. **But both `0111` passages are CLOSED ENUMERATIONS** (asserted list + named-remainder list), so a name in neither reads as ASSERTED — **the prose edit is not optional.** `0111/driver/driver.go` is **BYTE-UNTOUCHED**, yet **`0111` MUST still be RUN** (T1 adds a name to the subject's scrape). The reversal is recorded in `PLAN.md` §2.4 and `PROGRESS.md`.

13. **⚠️ THE ROW-75 ROADMAP CELL (`:137`) CARRIES FOUR STALE CLAIMS, and T11 opens that very cell.** (i) *"the discriminator for that form is the **ADR FAMILY, not the phase gap**"* — refuted, and its replacement is **graft SCALE**, not self-vs-other; (ii) *"the **THIRD** internal mis-pointer in ADR-0296"* — **not established, claim no ordinal**; (iii) *"`ProbeAdmin` at `:552`"* → **`:554`**; (iv) *"all eight `B5` hits are `AMEND-B5`/phase-25.2 Wasm"* — **over-broad**, `:4685` is phase-**29.1 mongo**. **Fix all four in the same edit as the row flip.** Also: inserting the ADR-0296 blockquote shifts `:17314 → :17316`, cited from **`ROADMAP.md:137` and `STATE.md:48`** — both edited at T11 anyway; **do NOT rewrite the frozen `SPEC.md`/`BRAINSTORM.md`.**

14. **The deferred-sentence narrow (`ROADMAP.md:205`).** It edits the **RETIRED clause**, not the candidates list — the sentence never named `no_certificate`, so **this is NOT a name deletion**. ⚠️ **HARD CONSTRAINT: the matched sentence contains ZERO interior periods — that is WHY `[^.]*\.` binds. The replacement must introduce NO `.`** (no `manager.go`, no `internal/…`). **Verified at the PLAN over an applied scratch copy:** check (2) ⇒ **3**, occurrence-count ⇒ **3**, terminator still `force-trace.`, match length 999 → 1033. **Re-verify in a scratch copy before landing.**

15. **⚠️ TWO gofmt traps, and a call that does not compile as the SPEC implies.** (a) The new `listenerRuntime` field's preceding doc comment **splits the alignment run**, so it takes a SINGLE space — and that is the good outcome, it keeps the phase-74 lines byte-identical. (b) The `helpText` entry needs a **BLANK SEPARATOR LINE** or gofmt re-pads all four `envoy_listener_ssl_*` keys and dirties three phase-74 lines. (c) **`mkDownstreamTSInline` takes `string` PEMs while `handshakeTestPKI` holds `[]byte`** — the new one-way helper needs `string(pki.serverCertPEM), string(pki.serverKeyPEM)` or it does not compile.

16. **Other refuted anchors — take them from `PLAN.md` §1.1, not from the SPEC.** `registerListenerMetrics`' doc comment starts at **`:351`**, not `:358` · the SPEC's three `assertSSLCrossProduct` "callers" (`:4493/:4519/:4544`) are **test-func DECLARATION lines**; the real call sites are **`:4508/:4539/:4557`** · the pointer assertions use **`t.Error`**, not `t.Errorf` · `pollCounter`/`counterValue` are declared in **`quic_test.go`** (`:34`/`:66`), not `manager_test.go`, and **`counterValue` `Errorf`s on an absent name** (that is what makes a zero-loop non-vacuous) · `registry.go` has **FIVE** panic sites, not three · `0110`'s reference port is **10446** (10447 is `0111`'s) · `0110`'s `driveSide` doc comment is **`:362-387`**, and the SPEC's own correction of that anchor was itself a prefix · **`classifyHandshakeErr` (`:424`) has NO doc comment** — the comment above it belongs to `const noClientCertErrText`, so *"amend the comment above `classifyHandshakeErr`"* has no target · ⚠️ **DO NOT touch `handshakeOutcome`** — `no_certificate` is a success-path annotation outside the classifier switch, and adding a variant is a design error for this row · **do NOT add a nil guard at the Inc site** (it would make the registration-delete break vacuous).

17. **⚠️ WHAT WAS NOT VERIFIED — you inherit no false confidence.** **`-race` over `./test/differential/` was NOT run** (the plain full suite is already ~400 s) — run it or **say plainly it was skipped**. **`ssl.no_certificate` on QUIC was never DRIVEN** (registration pinned and green; the parity argument is STRUCTURAL — no accept loop for `kindQUIC` — hence name-independent, but it is an argument not a measurement). **The predicate was never cross-checked at `require=true`** (`0109` not run). **Resumption/renegotiation never exercised** (`session_reused` 0 everywhere) — the one scenario in which the pinned predicate could be wrong in production. **Every §3 reference figure in T8's prose is transcribed from ADR-0297 §Context, not re-executed — cite the ADR paragraph, do not present them as re-derived.** The absolute total **1205** has no mechanical command. **Two of the PLAN's own agents had a correction refuted by the controller** — treat every RD-* row as a claim.

18. **The live hazards — never reflex-classify any of these as a regression.** `reference_sds_init_fetch_timeout_dial_budget_flake` (covers `internal/boot TestSDSEndToEnd_FetchFailure_BootFailsClosed` AND the original `internal/xds` test) · the PRE-EXISTING `internal/cluster` `-race` flake (`TestOutlierDetector_ConcurrentEjectExactlyOnce`, `outlier_test.go:766` — **isolate-re-run, do NOT re-classify**) · the full-suite startup flake (`subject ready: EOF` on an UNRELATED fixture) · and TWO still-**UNINDEXED** load flakes: `internal/httpclient TestOptions_ZeroValue_NoOpDefaults` and `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery`. ⚠️ **NONE fired in any of the PLAN's three build worktrees, including a full 400 s differential run — so a failure here is MORE LIKELY REAL than at most stages.** Isolate-re-run, then state the classification and its evidence. ⚠️ `-run '0110'` matches ZERO subtests — use `TestDifferential/0110-tls-require-client-cert-false`.

19. **Counts at exit (re-run MECHANICALLY in the worktree; never copy them):** fixtures **119** (`ls -d test/fixtures/[0-9]*/ | wc -l`) / fuzzers **55** (`grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`) / **stat surface 1204 → 1205** (BEHAVIOR_CONTRACT doc count; **NO mechanical command**; the +1 DELTA is solid, the TOTAL is documentary and rides TWO documented ledger gaps) / BackendKind tail **38** (a TAIL VALUE — the file declares 39 constants, 0-38) / go.mod modules **2** (the lineage figure; the single `go.mod` requires **67**) / DECISIONS tail **ADR-0297** (§Context only at this tip; next-free **ADR-0298**) / ZERO new packages / **ZERO new exported symbols** / **+0 PRODUCTION imports** (⚠️ **TEST-side grows by four — `log`, `math`, `net/http`, `strconv` — and that is PERMITTED; audit the two categories SEPARATELY, and use `PLAN.md`'s negative-controlled import gate, NOT the phase-74 command, which returns false positives and exits 0**).

## What THIS session does — the phase-75 IMPL

One stage per session. Execute `PLAN.md`'s **11-task spine (T1-T11)** with its **TEN breaks**. **ROW 75 FLIPS `in-progress` → `done`** at the T10/T11 six-gate (ADR-0106, the SOLE leg — a SINGLE FLAT ROW, `reference_roadmap_split_phase_row_done`). ADR-0297 **completes IN PLACE** (§Decision + §Consequences after the RETAINED footer at `:17350`; PROPOSED → COMPLETE; no renumber, tail stays ADR-0297) **and is CORRECTED** (¶7, ¶9). The ADR-0296 §Decision (g) blockquote lands. The ROADMAP row flips, its four stale claims are fixed, and the deferred sentence narrows.

**Stage-close mechanics (an IMPL):** STATE §Current rolled **IN PLACE** (lifecycle 3 → **DONE**; §Recent re-capped at FIVE **with its PREAMBLE updated** — the ADR-0288 rule). ⚠️ **The ADR-0288 singleton greps return 2, not 1**, for each of the three field-name tokens — the second hit is `STATE.md:7`, the RULE STATEMENT itself. Verify with `grep -n`, and **never "fix" the count to 1**; that would delete the rule. `next-prompt.txt` rolled to the **phase-76 BRAINSTORM** — the roller SELF-PICKS; SPEC §13 records **`fault.abort.grpc_status`** as the recommended next opening (7-9 tasks; the only identified candidate that clears a sentinel check-(3) blocker; **ONE probe owed**).

Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** (`reference_parallel_subagents_private_scratch`).

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. ⚠️ **THIS FIRED LIVE DURING BOTH THE PHASE-75 BRAINSTORM AND THE PHASE-75 PLAN.** At the phase-72 close the same hazard put a stray commit on `master` in the MAIN checkout AND made a "final gates" run validate master's pre-row tree — GREEN while proving nothing. **Use `git -C <abs-worktree-path>` for every git command, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline) + §Project counts + the §Recent lineage (five bullets: phase-75 PLAN + phase-75 SPEC + phase-75 BRAINSTORM + phase-74 IMPL + phase-74 PLAN).
2. `docs/envoy-go/phases/75-tls-no-certificate-stat/PLAN.md` — **the whole document, and §1.3 FIRST.** Then §1.1 (the RD-* ledger — take every anchor from HERE, not from the SPEC), §1.2 (F1-F12), §1.4 (what is NOT verified), §1.5 (the adversarial record), §2 (the seven explicit decisions incl. the `0111` reversal), the Global Constraints, the File structure, T1-T11, and the break map.
3. `docs/envoy-go/phases/75-tls-no-certificate-stat/PROGRESS.md` — the stage pointer, the adversarial-pass record, **what the build agents CONFIRMED by execution** (do not re-derive it) and the twelve cross-cutting hazards. Append the IMPL record as you go.
4. `docs/envoy-go/phases/75-tls-no-certificate-stat/SPEC.md` — ⚠️ **read it THROUGH `PLAN.md` §1.1/§1.2: FOURTEEN of its claims are REFUTED there**, including §10.1's break claim and §9's two-ADR-heading claim. §3.1 (the pinned predicate + the wire trace), §3.5 (the fixture numbers — now CONFIRMED on both sides), §3.6 (the race — now scoped to the REFERENCE), §8 (the fixture), §11 (the edit-site roster).
5. `docs/envoy-go/DECISIONS.md` — **ADR-0297** §Context ¶1-¶11 in full (⚠️ **¶7 and ¶9 are DEFECTIVE — see item 9**) · **ADR-0296** §Decision (g) at `:17308` (the paragraph the IMPL must correct) · `:16901` and `:16910` (the two indented-blockquote precedents; leading bytes `0x20 0x20 0x3E 0x20`) · **`:17211`** (the INLINE other-ADR counter-example that refutes ¶9) · ADR-0044 (⚠️ it does NOT contain the §Context-draft discipline — use `ADR-0044-as-used`) · ADR-0045 · ADR-0106 · ADR-0288.
6. `internal/listener/manager.go` — `:172-177` (the field comment, "three" at `:175`/`:177`) · `:180-182` (the three fields; the 4th inserts at `:183`) · `:347-349` (`normalizeAddr`) · **`:351-373`** (the doc comment — ⚠️ it starts at `:351`) · `:374-383` (`registerListenerMetrics`; the 4th registration at `:382`) · `:385-386` (`handshakeOutcome`'s doc — PROSE only) · `:424` (`classifyHandshakeErr`, **no doc comment**) · **`:1258-1279`** (the handshake block; `tlsConn` `:1259`, Inc `:1277`, `dispatchConn` `:1278`) · `:392`/`:1265` (the phantom `B5`).
7. `internal/listener/manager_test.go` — `:627` (`mkDownstreamTSInline`, **takes `string`**) · `:1624` (`TestNewManager_ChainSelectionPropagation` — **already drives the Inc site**) · `:1940` (the rename's third site) · `:1998-2008` (`listenerSSLNames`, `sort.Strings` at `:2006`) · `:2019-2023` · `:2049-2053` · `:2128-2251` (`GateMatchesInc`; pointers at `:2191/2194/2197` and `:2241/2244/2247`, all `t.Error`) · `:4032-4038` (`handshakeTestPKI`, `[]byte` fields) · `:4361` · `:4437-4462` (`startMutualTLSListener`) · **`:4464-4488`** (`assertSSLCrossProduct`; roster `:4480`) · `:4508`/`:4539`/`:4557` (the REAL call sites) · `:4568`. Then `internal/listener/quic_test.go:34`/`:66`/`:249-300` and `internal/stats/name.go:445-472` + `name_test.go:195-249`.
8. `test/fixtures/0110-tls-require-client-cert-false/` — `driver/driver.go` (arms `:402-435`, `wantObservable` `:89`, `BackendCount` `:289`, `SubjectConfig` `:313`, `DriveSubject` `:334-341`, `ProbeAdmin` **`:554`**, `var _` `:613`) · `README.md:160-165` · `expectations.yaml:124-142`/`:166-175` · `envoy.yaml:24`. Then `test/fixtures/0111-tls-cvc-empty-dynamic-fallback/driver/driver.go:629-800` (the template — `AssertStats` `:655`, `scrapeProm` `:739`, rosters `:682-686`/`:687-691`/`:692-696`, the ABSENT split `:698-713`) + its `README.md:167-174` and `expectations.yaml:196-207`, and `test/differential/runner_test.go:1330-1349` (the SILENT dispatch).
9. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — `:152-157` (⚠️ the *"Listener — 2 names"* table phase 74 deliberately did NOT extend — **do not extend it either**) · `:831`/`:847` (the two totals) · `:916`/`:918`/`:928` · **`:962`** (⚠️ `ssl.no_certificate` at 0-based CHARACTER index **627** / BYTE offset **630**; 1002 chars / 1007 bytes — **cite the unit or do not cite the number**; this anchor has survived THREE challenges) · **`:1849-1859`** · `:785` (the two-ADR heading precedent) · **`:4950`/`:4996`/`:4998`/`:5000`/`:5002`/`:5004`** (the ledger, its TAIL form, and both gaps).
10. `docs/envoy-go/ROADMAP.md` — the §Schema invariant (`:21`), the row-75 cell (`:137`, **four stale claims**), row 67 (`:129`), the Observability family heading (`:201`)/descriptor (`:203`)/history (`:205`), and the THREE live `candidates:` sentences (`:185`, `:195`, `:205`). ⚠️ **RE-DERIVE these with `grep -n`.**
11. `docs/envoy-go/phases/74-tls-handshake-outcome-stats/{PLAN,PROGRESS}.md` — the immediately-prior IMPL form to mirror (the task ledger, the break ledger, the "claims REFUTED BY EXECUTION" section, the two-category envelope audit, the counts-at-exit block).
12. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Load-bearing for this IMPL: `reference_break_protocol_commit_first`, `reference_deliberate_break_wrong_assertion`, `reference_liveness_break_needs_failing_baseline`, `reference_plan_break_instructions_dont_compile`, `reference_differential_break_protocol_count1`, **`reference_differential_run_selector`** (⚠️ NEW SHAPE — it applies to UNIT tests too), `reference_differential_asserter_dispatch`, `reference_fatalf_makes_assertions_unreachable`, `reference_nil_stats_counter_inc_crashes_goroutine`, `reference_probe_must_discriminate`, `reference_a_drift_correction_is_itself_a_claim`, `reference_plan_schedules_edits_to_a_byte_gated_file`, **`reference_ssl_stats_suppressed_by_fast_failing_upstream`** (⚠️ RE-SCOPE: reference-only), `reference_listener_stat_scope_cross_side_divergence`, `reference_fixture_tb_has_no_logf`, `reference_go_client_cert_withholding`, `reference_sentinel_deferred_sentence_live_vs_historical`, `reference_roadmap_split_phase_row_done`, `feedback_pertask_gofmt_lint`, `feedback_brief_citations_not_evidence`, `reference_quoting_is_not_executing`, and **`reference_bash_cwd_reset_commits_to_main`**.
