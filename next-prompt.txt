START a fresh cold-start session for envoy-go — **the phase-39.2 (`health-check-tcp-grpc`, the TCP + gRPC active-HC checker codecs) PLAN is DONE, LANDED on master (commit `853b873`), and PUSHED to `origin/master`; the next lifecycle step is the phase-39.2 IMPL** (`superpowers:subagent-driven-development` — execute `docs/envoy-go/phases/39.2-health-check-tcp-grpc/PLAN.md` Tasks 1–13 in a fresh worktree per `feedback_git_worktrees`/`feedback_execution_style`; the controller squash-merges + pushes at stage-close per `feedback_push_to_origin`/`feedback_subagents_no_push`) OR a new subject. The substantive master tip is the phase-39.2 PLAN commit **`853b873`** (the 13-task TDD spine; the ADR-0045 NO-FURTHER-SPLIT re-check; the D-S39.2-1..6 resolutions; confirm via `git log --oneline -5`). The prior substantive tips are the phase-39.2 SPEC `14cb8fc`, the phase-39.1 IMPL squash `6714575` (ADR-0242 + ADR-0243 — the substrate 39.2 reuses), and the phase-38.2 IMPL squash `14053d1`. Counts UNCHANGED at the 39.2 PLAN (stat surface **1132** / fixtures **68** / fuzzers **42** / BackendKind **33** / DECISIONS tail **ADR-0243**, next-free **ADR-0244**); the 39.2 IMPL will move them (fixtures → 70, BackendKind → 34, DECISIONS tail → ADR-0244).

## What just landed (phase 39.2 PLAN — the durable record is `docs/envoy-go/phases/39.2-health-check-tcp-grpc/PLAN.md` + SPEC.md + README.md; read those, not this summary)

The PLAN decomposes the 39.2 SPEC §10 into **13 bite-sized TDD tasks** (the prober dispatch + the two codecs + the +1 BackendKind + the two fixtures + the docs). Execute it SUBAGENT-DRIVEN (`superpowers:subagent-driven-development` — fresh implementer per task + a spec-compliance review then a code-quality review per task; a final holistic review before landing). The spine:
1. baselines/anchors + PROGRESS.md.
2. **The `prober` interface + the dispatch generalization** of `healthChecker`/`parseHealthChecks` (`httpProber` = the 39.1 `probeHTTP` body UNCHANGED; lift `interval`/`unhealthy`/`healthy` into a `checkerSpec`; `timeout` stays per-prober) — the load-bearing refactor; the **full 68-dir differential must stay GREEN after it** (byte-stable HTTP) BEFORE any new codec lands.
3. `tcpProber` (connect-only: `net.Dialer.Dial` + close) + the `tcp_health_check` parse arm + the `send`/`receive` DEPARTURE-reject + unit tests.
4. `0067-health-check-tcp` (copy the `0066` driver; swap `http_health_check` → `tcp_health_check{}`; reuse `HTTPEcho`, **NO new BackendKind**; KEEP cluster name `c_hc`).
5. `0067` deliberate-break (×2) + 20-run flake.
6. `grpcProber` (`grpc.health.v1.Health/Check` over H2 via `grpc.NewClient` insecure + `grpc_health_v1`; `err != nil` ⇒ network_failure, `Status != SERVING` ⇒ failure-only) + the `grpc_health_check` parse arm (`service_name` supported; authority/initial_metadata silent-ignore) + the NOT_SERVING discriminator unit test.
7. The gRPC cluster-must-be-H2 reject (reuse `extractH2Mode`; house wording `cluster: %q: grpc_health_check requires the cluster to support HTTP/2`) + unit test.
8. The **+1 `GRPCHealthResponder` BackendKind = 34** (an IN-PROCESS h2c responder in the runner switch — the `TCPThriftResponder` precedent: `application/grpc` ⇒ a `health.NewServer()`-backed `grpc.Server.ServeHTTP`; else ⇒ 200 + `backend-<idx>:` body).
9. `0068-health-check-grpc` (copy the `0067` driver; H2 UPSTREAM cluster via `http2_protocol_options`; the downstream listener STAYS HTTP1 — the driver reuses the H1 round-trip; `BackendKind() = GRPCHealthResponder`; KEEP cluster name `c_hc`).
10. `0068` deliberate-break (×2) + 20-run flake.
11. Full **70-dir** differential + the six-gate (gofmt/lint/build/`go mod tidy -diff` empty/test/`-race`).
12. ADR-0244 body (promote the SPEC §13 §Context + add §Decision/§Consequences per ADR-0044; DECISIONS tail ADR-0243 → ADR-0244) + the BEHAVIOR_CONTRACT 39.2 delta (stat surface STAYS 1132).
13. Completion bundle (STATE/ROADMAP/README/PROGRESS + the as-built counts).

The FINAL ADR-0045 split-gate re-check at the PLAN → **NO FURTHER SPLIT** (~150–260 prod LoC / 13 tasks — single flat 39.2 leg covering BOTH codecs). The §12 D-S39.2-1..6 are RESOLVED in the PLAN's Pre-flight. plan-document-reviewer APPROVED (7 advisory fixes applied — the two IMPORTANT: the Task-2 test-adaptation scope + the Task-1 baseline command).

## Honor throughout (the IMPL discipline)

- **Subagent-driven, worktree, push-at-close:** `feedback_execution_style` (subagent-driven IMPL) · `feedback_git_worktrees` · `feedback_subagents_no_push` (task subagents commit LOCAL-only) · `feedback_push_to_origin` (the controller squash-merges to master + pushes at stage-close) · `feedback_subagent_worktree_path_targeting`/`_detach` (pin the canonical worktree paths; `git restore` after deliberate-breaks, never checkout-sha/amend; the controller re-verifies the branch + a clean main repo after each task) · `feedback_pertask_gofmt_lint` (every task runs gofmt -l + golangci-lint on the touched pkgs).
- **Differential discipline:** `reference_differential_run_selector` (`-run 'TestDifferential/0067'` / `'TestDifferential/0068'`, never `-run '0067'`) · `reference_differential_break_protocol_count1` (`-count=1` for break/flake) · `reference_docker_probe_bridge_network` (`0068`'s SERVING backends reachable from the reference container via `host.docker.internal`; verify `upstream_rq_total > 0` reference-side) · `reference_health_check_propagation_warmup` (gate on `membership_healthy` convergence + the warmup-until-K-consecutive-200s pattern + delta counters — NEVER raw `attempt` deltas; widen deadlines never assertions) · `reference_fixture_workload_constant_desync` (single-source the constants).
- **ADR/process:** ADR-0052 (the atomic-landing six-gate) · ADR-0044 (ADR bodies at IMPL in-place) · ADR-0080 (byte-stable parse-reject wording) · ADR-0106 (flat family row — the 39.2 sub-leg flips `done` at the IMPL six-gate, NO parent rollup) · ADR-0227 (`envoyproxy/envoy:contrib-v1.37.2`).

## Read first (in order)

1. THIS FILE — the post-phase-39.2-PLAN cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 39.2 (health-check-tcp-grpc) PLAN done`; the counts footer (fixtures 68 / fuzzers 42 / stat surface 1132 / BackendKind 33 / DECISIONS tail ADR-0243, next-free ADR-0244); the lifecycle routing.
3. `docs/envoy-go/phases/39.2-health-check-tcp-grpc/PLAN.md` — **the 13-task IMPL plan** (the Pre-flight ADR-0045 re-check + D-S39.2 resolutions + the File structure; the per-task code/files/commands) + SPEC.md (the AMEND-TG1..TG7 pins + §3 architecture + §6 reject roster + §8 `0067`/`0068` design + §13 ADR-0244 §Context) + README.md.
4. `docs/envoy-go/phases/39-upstream-health-check/` — the 39.1 PROGRESS.md + ADR-0242/0243 (the substrate 39.2 reuses UNCHANGED).
5. `internal/cluster/{health.go,manager.go}` — the as-built 39.1 substrate Tasks 2/3/6/7 generalize (the `httpHealthCheckCfg`/`probeHTTP`/`healthChecker`/`parseHealthChecks`; `buildCluster` + `extractH2Mode`) + `internal/grpcclient/grpcclient.go` (the grpc insecure-dial precedent) + `test/differential/fixture/fixture.go` (the BackendKind enum, tail 33) + `test/differential/runner_test.go` (the in-process-backend spawn switch — the `TCPThriftResponder` precedent Task 8 mirrors; the blank-import list) + `test/fixtures/0066-health-check-http/driver/driver.go` (the driver template Tasks 4/9 copy).
6. `docs/envoy-go/DECISIONS.md` (ADR-0242/0243 + the process ADRs) + `docs/envoy-go/ROADMAP.md` (row 39 — the 39.1-leg DONE + 39.2-SPEC/PLAN-DONE notes; the 39.2 IMPL pending).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the IMPL-discipline set above.
