=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-CHECKED 2026-07-13 (after the phase-61.2 IMPL landed, run MECHANICALLY) — it does NOT fire. Do NOT create `stop`.** State:
- (1) prints `NOT DONE: row 61` — ONE `in-progress` row. Row 61 `http3-downstream-listener` is the HTTP/3-family opener; legs 61.1 (QUIC/UDP listen substrate) + 61.2 (H3 codec + HCM adapter — THIS IMPL just landed) are DONE, but leg 61.3 (cross-side `0102` differential + harness UDP/H3-client surgery) is NOT yet done. Row 61 flips `done` only when ALL THREE legs land (ADR-0106). This ALONE blocks `stop`.
- (2) prints THREE live "candidates:" sentences — HTTP/3, xDS, and Observability ⇒ three families STAY OPEN. (`reference_sentinel_deferred_sentence_live_vs_historical`.)
- (3) prints `NEVER OPENED: gRPC`, `Runtime`, `WASM` ⇒ THREE families never opened. Operational-tooling also STAYS OPEN.

All three checks print ⇒ the sentinel does NOT fire.

**STATUS AS OF 2026-07-13: the phase-61.2 IMPL LANDED (a CODE stage — executed `PLAN-61.2.md`'s 9-task TDD spine subagent-driven; envoy-go's QUIC listener now SERVES HTTP/3: a stdlib-typed THIRD dispatch arm `runH3`/`ServeH3` in a NEW `internal/filter/hcm/h3dispatch.go` [quic-go-free, 14-seeder FULL parity with the H1 `dispatchRequest`] reached via a NEW `network.H3Terminal` bridge, wiring `serveQUICConnection` into `http3.Server.ServeQUICConn`; the `codec_type HTTP3` reject LIFTED gated on a threaded `IsQUIC` flag + the HTTP3-on-non-QUIC config-parity reject; ADR-0281 anchored; proven SUBJECT-side by a local quic-go `http3.Transport` client GET→200. Row 61 STAYS `in-progress`; DECISIONS tail ADR-0279 → ADR-0281).** The NEXT IMPLEMENTABLE STAGE is the **phase-61.3 PLAN** (`http3-downstream-get-differential` leg 61.3 — decompose SPEC-61's cross-side differential + harness-surgery leg into a TDD spine + a `PROGRESS-61.3.md` scaffold; lifecycle-state 2 for the 61.3 leg — a DOCS-ONLY stage; NO production `.go` changes; row 61 STAYS `in-progress`). Row 61 flips `done` only at the 61.3 IMPL (ADR-0106).

**What the 61.2 IMPL delivered (for CONTEXT; the code is landed — RE-VERIFY the seams against the master tip at 61.3 PLAN start, `git fetch` first):**
- `internal/filter/hcm/h3dispatch.go` (NEW, ~376 lines) — `ServeH3`/`runH3` (the H3 dispatch arm, 14-seeder full parity with the H1 `dispatchRequest`: TLS seeds from `r.TLS`, source_ip ctx-string + all rate-limit/metadata seeds, `:method`/`:authority`/`:path` pseudo-header injection, both SendLocalReply branches, the decode-data buffering loop, the encode-chain tail) + `writeH3Reply` (`ActionResponse → http.ResponseWriter`, pseudo-header-skipping, MINIMAL — no server/date/content-length synthesis, DEFERRED to 61.3 for differential byte-parity). Stdlib `net/http` only; ZERO quic-go.
- `internal/filter/network/terminal.go` — the `network.H3Terminal` interface (`TerminalFilter` + `ServeH3(w,r)`, stdlib-only); `*hcm.Filter` satisfies it (compile-assert in `h3dispatch.go`). `internal/filter/network/types.go` + `internal/filter/hcm/config.go` — `IsQUIC bool` (threaded `FactoryCtx → ListenerCtx`).
- `internal/filter/hcm/config.go` — the `IsQUIC`-gated `codec_type HTTP3` accept / HTTP3-on-non-QUIC parity reject; `internal/filter/hcm/accesslog_emit.go` — `emitAccessLogH3` (`Protocol="HTTP/3"`); `internal/listener/manager.go` — `IsQUIC: kind==kindQUIC` at both `FactoryCtx` literals.
- `internal/listener/quic.go` — `quicChain()` + `serveQUICConnection → http3.Server.ServeQUICConn` (leak-free ctx-watcher; the `http3` import is the ONLY new import; quic-go stays confined here). go.mod gained `github.com/quic-go/qpack v0.5.1 // indirect` (the QPACK transitive dep of `http3`; properly tidied).
- Tests: `internal/filter/hcm/h3dispatch_test.go` (`writeH3Reply` + `runH3` via `httptest`) · `config_test.go` (the flipped HTTP3 codec test + accept-on-QUIC) · `internal/filter/network/terminal_test.go` (interface shape) · `internal/listener/quic_test.go` (`TestQUICListener_ServesH3GET` — the subject-side H3 GET→200 proof + -race).
- Docs: BEHAVIOR_CONTRACT HTTP/3 serve extension · ADR-0281 §Context/§Decision/§Consequences · PROGRESS-61.2 final.

**The phase-61.3 PLAN scope (a DOCS-ONLY stage — decompose SPEC-61 leg 61.3; NO production `.go` changes; row 61 STAYS `in-progress`):**
- **This is a PLAN stage** — author `docs/envoy-go/phases/61-http3-downstream-listener/PLAN-61.3.md` (a TDD task spine) + `PROGRESS-61.3.md` (scaffold), in a FRESH worktree off master (`.worktrees/phase-61.3-plan`, branch `phase-61-http3-downstream-get-differential-plan`, per `feedback_git_worktrees`). NO code. Row 61 STAYS `in-progress`.
- **The 61.3 leg scope (from SPEC-61 §3.0/§3.5 + the 61.2 §Consequences):** the FIRST cross-side HTTP/3 differential fixture `0102-http3-downstream-get` (a GET over H3 to BOTH the reference [envoy contrib image] and the subject, asserting a byte-stable observable) + the harness's FIRST non-TCP transport surgery: `test/differential/harness.go` is TCP-only (`harness.go:108` exposes only `/tcp`; NO H3 client anywhere in `test/`) — leg 61.3 adds a shared `test/helpers/h3.go` `H3RoundTrip` (a quic-go `http3.Transport` client, pin-the-addr, ignore Alt-Svc) + `/udp` exposure on the three container-starters + the reference contrib-image QUIC-listener bootstrap. NOTE the traps: `reference_docker_probe_bridge_network` (--network host is useless under Docker Desktop; shared bridge + STRICT_DNS), `reference_host_gateway_ip_docker_desktop` (`differential.HostGatewayIP` for reference→host reachability), `reference_envoy_contrib_image_tagging` (contrib-prefixed tag in the standard repo). The 61.2 §Consequences pin the DEFERRED differential-parity items the fixture must now VERIFY: the exact access-log/span `Protocol` string cross-side, the `downstream_*_http3_total` stat family (asserted as a NAMED SUBSET per `reference_stats_sink_emits_used_only`), and `writeH3Reply`'s server/date/content-length synthesis (needed for cross-side byte-parity — the 61.2 arm was minimal).
- **ADR-0282 (RESERVED for the 61.3 leg; the PLAN decides its own anchor or a fold into ADR-0281).** DECISIONS tail is **ADR-0281**; next-free **ADR-0282**.

**ADR reservation ledger (provisional; each finalized at its IMPL in landing order):** ADR-0277 = phase-59 (LANDED) · ADR-0278 = xDS 60.1 (LANDED) · ADR-0280 = xDS 60.2 (LANDED) · ADR-0279 = HTTP/3 61.1 (LANDED) · ADR-0281 = HTTP/3 61.2 codec-arm (LANDED at THIS IMPL) · **next-free unreserved ADR-0282** (provisional 61.3 anchor).

**Current counts (VERIFIED at the phase-61.2 IMPL session against master tip `b00d8d20` + the landed branch — RE-VERIFY at session start, `git fetch` first):** fixtures **105** (tail `0103-xds-sds-server-cert`; 61.3 adds `0102-http3-downstream-get` → 106) · fuzzers **55** · DECISIONS tail **ADR-0281** (next-free **ADR-0282**) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** · go.mod modules **2** (`quic-go v0.54.1` direct + `qpack v0.5.1` indirect, both confined to `internal/listener` deps). Phase-61.3 PLAN is DOCS-ONLY ⇒ anticipated exit: ALL counts UNCHANGED (a PLAN stage adds no code); row 61 STAYS `in-progress`. Anticipated at the 61.3 IMPL: fixtures **105 → 106** (`0102`); row 61 → `done`; possibly +2 stat surface (`downstream_{cx,rq}_http3_total`).

**Locating the phase-61.2 IMPL stage squash** (a router cannot name its own sha — identify by SUBJECT, never by position):
```sh
git log --oneline --grep='^phase 61.2 (http3-h3-codec-hcm) IMPL' -1
```

**THE NEXT ACTION: the phase-61.3 PLAN (lifecycle-state 2 for the 61.3 leg — decompose SPEC-61 leg 61.3 into a TDD spine + PROGRESS scaffold; a DOCS-ONLY stage; row 61 STAYS `in-progress`).** After the 61.3 PLAN lands, the roller's next stage is the phase-61.3 IMPL (which flips row 61 `done`). The sentinel governs termination, not this list.

==============================================

## What THIS session does (the phase-61.3 PLAN)

Author `docs/envoy-go/phases/61-http3-downstream-listener/PLAN-61.3.md` (a TDD task spine for the cross-side `0102-http3-downstream-get` differential + the harness UDP/H3-client surgery) + `PROGRESS-61.3.md` (scaffold), in a FRESH worktree off master (`.worktrees/phase-61.3-plan`, branch `phase-61-http3-downstream-get-differential-plan`, per `feedback_git_worktrees`). This is a SINGLE STAGE (the PLAN — a DOCS-ONLY stage; NO production `.go` changes). Row 61 STAYS `in-progress` (flips `done` only at the 61.3 IMPL when the LAST leg lands, ADR-0106).

**Deliverables:**
- `docs/envoy-go/phases/61-http3-downstream-listener/PLAN-61.3.md` — a TDD task spine decomposing SPEC-61 leg 61.3: (a) the harness UDP/H3-client surgery (`test/helpers/h3.go` `H3RoundTrip`, `harness.go` `/udp` exposure on the container-starters, the reference contrib-image QUIC-listener bootstrap); (b) the `0102-http3-downstream-get` fixture (config.yaml for BOTH sides + the asserter — a byte-stable H3 GET observable); (c) the DEFERRED-parity pickups from ADR-0281 §Consequences (the exact access-log/span `Protocol` string, the `downstream_*_http3_total` named-stat subset, `writeH3Reply` server/date/content-length synthesis for cross-side byte-parity); (d) row 61 → `done` + ADR-0282 (or a documented fold into ADR-0281) at the IMPL. RE-DERIVE every `file:line` against the master tip (`git fetch` first) — the harness/`test/` citations are from SPEC-61's survey and MUST be re-confirmed.
- `docs/envoy-go/phases/61-http3-downstream-listener/PROGRESS-61.3.md` — the baselines + task checklist + design pins + the carried 61.1/61.2 deferred-maintenance dispositions.
- `next-prompt.txt` — the router roll to the phase-61.3 IMPL (folded into the PLAN squash; `reference_next_prompt_tracked_despite_gitignore`).

## Honor throughout (the discipline, unchanged)
- **Lifecycle = one stage per session.** This session: the phase-61.3 PLAN ONLY. After it lands: the phase-61.3 IMPL (or the roller's self-pick; the sentinel governs).
- **Process anchors:** ADR-0044 (ADR §Decision/§Consequences land at the IMPL) · ADR-0045 (the split escape-valve — CONSUMED for row 61) · ADR-0080 (distinct per-arm reject substrings) · ADR-0106 (row 61 flips `done` only when ALL legs land) · `reference_roadmap_split_phase_row_done`.
- **The differential harness traps (LOAD-BEARING for the 61.3 PLAN/IMPL):** `reference_differential_fixture_dispatch_constraint` (one fixture dir = ONE runner branch), `reference_differential_asserter_dispatch`, `reference_differential_run_selector` (`-run 'TestDifferential/<NNNN>-<slug>'`), `reference_differential_break_protocol_count1`, `reference_deliberate_break_wrong_assertion`, `reference_docker_probe_bridge_network`, `reference_host_gateway_ip_docker_desktop`, `reference_envoy_contrib_image_tagging`, `reference_differential_backendcount_min_one`, `reference_stats_sink_emits_used_only` (the named-stat-subset assertion for `downstream_*_http3_total`).
- **`next-prompt.txt` IS TRACKED** (`reference_next_prompt_tracked_despite_gitignore`) — edit it inside the stage worktree and fold into the stage commit; locate commits by SUBJECT (`git log --grep`), never by position.
- **Deferred maintenance (carried; fold when the relevant file next reopens):** the phase-57 statssink notes F-1/F-2/F-3 (fold into the next `udp.go`/statssink-driver-touching row); the phase-60.2 final-review MINOR roll-up (T2/T5/T6/T7 — cosmetic/coverage, fold when the SDS files next reopen); the phase-61.1 review MINORS M6-1 (`quicAcceptLoop` no backoff), M-FB1 (`default_filter_chain` QUIC decode gap), M-FB2 (`quicTLSConfig`/`quicChain` map-nondeterminism) — RE-DEFERRED to a 61.3/QUIC-robustness row; the phase-61.2 review MINORS T5-M1 (runH3 encode-error path skips the `downstream_rq_<Nxx>` Inc — intentional WriteH2 parity), T5-M2 (POST-body test insensitive to body-buffering), T5-B1 (`SetDownstreamLocalAddr` nil — quic-go doesn't populate `LocalAddrContextKey`), T7-M1 (`quicChain`/`quicTLSConfig` divergence only under deferred multi-chain SNI). None is a security/resource/crash risk within the explicitly-deferred scope.

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-61.3 PLAN is next; legs 61.1 + 61.2 CLOSED).
2. `docs/envoy-go/phases/61-http3-downstream-listener/SPEC.md` — §3.0 (the 61.1/61.2/61.3 split), §3.5 / leg-61.3 (the differential + harness surgery), §7 (the stat surface — `downstream_*_http3_total` as a NAMED SUBSET), §8 (the access-log/span `Protocol` string), the -DIFF-HARNESS pin (the harness is TCP-only; the H3-client surgery).
3. `docs/envoy-go/phases/61-http3-downstream-listener/PLAN-61.2.md` + `PROGRESS-61.2.md` — the landed 61.2 codec-arm seams + ADR-0281 §Consequences (the DEFERRED-parity pickups the 61.3 fixture must verify: the exact `Protocol` string, the `downstream_*_http3_total` subset, `writeH3Reply` server/date/content-length synthesis).
4. `docs/envoy-go/phases/61-http3-downstream-listener/PLAN-61.1.md` + `PROGRESS-61.1.md` — the landed 61.1 substrate (the QUIC listener config shape the `0102` fixture reuses on the subject side).
5. `docs/envoy-go/DECISIONS.md` — the tail ADR-0281 (61.2) + ADR-0279 (61.1) + ADR-0044/0045/0080/0106 (the process anchors); ADR-0282 is next-free (provisional 61.3 anchor).
6. `docs/envoy-go/ROADMAP.md` — row 61 `in-progress`; the HTTP/3-family row + the LIVE HTTP/3 deferred sentence. `docs/envoy-go/STATE.md` — the active-phase header (phase 61.2 IMPL done; NEXT = the phase-61.3 PLAN).
7. `test/differential/harness.go` + `test/differential/runner.go` + an existing multi-transport fixture (e.g. a recent `01xx` dir) — the harness seams the 61.3 surgery extends (the TCP-only `/tcp` exposure at ~`harness.go:108`, the container-starters, the asserter dispatch). RE-DERIVE the current `file:line` (the phase-60/61 legs shifted numbers).
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the `reference_*` differential-harness traps (esp. the Docker/bridge/host-gateway/contrib-image trio + the fixture-dispatch/asserter/run-selector/break-protocol set + `reference_stats_sink_emits_used_only` + `reference_roadmap_split_phase_row_done` + `reference_sentinel_deferred_sentence_live_vs_historical`).
