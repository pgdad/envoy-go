=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **PHASE 94 (`tls-connection-error-stat`) IS CLOSED. ROW 94 READS `done` AT `ROADMAP.md:156`. EVERY CHARTERED ROW IS NOW `done` AND THERE IS NO OPEN PHASE.** ⇒ **THIS SESSION SELF-PICKS A NEW SUBJECT AND RUNS ITS BRAINSTORM.** There is NO banked mid-lifecycle work.

⚠️⚠️ **THE MARGIN IS ONE. THIS IS THE MOST DANGEROUS STATE THE PROJECT HAS EVER BEEN IN.** Check (1) is now **SILENT** and check (3) is **SILENT**. **THE SENTINEL IS BLOCKED BY CHECK (2) ALONE — six lines in `ROADMAP.md`.** ⚠️ **DELETING THE LAST DEFERRED-CANDIDATE LINE ENDS THE PROJECT. DO NOT "TIDY" ONE.** ⚠️ **AND NC-A/NC-B NOW READ *ONE* LINE EACH, NOT TWO — the phase-94 IMPL re-measured them on both sides of its own row flip. MEASURE ALL THREE CHECKS AND ALL FOUR NCs YOURSELF BEFORE DOING ANYTHING ELSE.**

⚠️ **OPENING A NEW ROW RE-BLOCKS CHECK (1) AND RESTORES THE MARGIN TO TWO.** Registering the new row `in-progress` is an **ADD, not a flip**: `want` goes **126 -> 127** and `ROADMAP.md` **244 -> 245**.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE. This form parses FIELDS instead of matching a pattern, and
#     asserts the row DENOMINATOR so a deleted row is itself an error.
#     ⚠️ want IS 126 and ROADMAP.md IS 244 lines at the phase-94 IMPL close.
#     ⚠️ THIS CHECK IS NOW SILENT. Opening a new row makes want 127 / 245 lines.
awk -F'|' -v want=126 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **ALL THREE CHECKS ARE NOW SILENT EXCEPT (2). A SILENT CHECK IS INDISTINGUISHABLE FROM A BROKEN ONE, SO THE NCs ARE NO LONGER OPTIONAL — THEY ARE THE ONLY EVIDENCE ANY OF THE THREE STILL WORKS.** Run all four EVERY SESSION:
```sh
# NC-A — doctor a row to `in-progress`. ⚠️ NOW READS *ONE* LINE (`NOT DONE: row 62`).
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md.

# NC-B — the DENOMINATOR, want=125 on the real file. ⚠️ NOW READS *ONE* LINE
#        (`GATE FAIL: examined 126 data rows, expected 125`).

# NC-C — the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```
⚠️ **AND RUN THE CHECK-(2) POSITIVE CONTROL — AND READ THE COMMAND BEFORE TRUSTING ITS RESULT.** At the phase-94 IMPL the first attempt was **BROKEN and read 5, not 0**: it neutralised only `deferred candidates:` and left the five `remaining deferred (not-yet-chartered) candidates:` lines untouched, **because the longer phrase does not CONTAIN the shorter one as a substring.** A positive control reporting a non-zero residual is indistinguishable from a real finding until you read it. The correct form substitutes BOTH phrases and asserts the substitution count:
```sh
sed -e 's/deferred candidates:/deferred candidatesXX/g' \
    -e 's/remaining deferred (not-yet-chartered) candidates:/remaining deferred (not-yet-chartered) candidatesXX/g' \
    docs/envoy-go/ROADMAP.md > "$SCRATCH/c2pc.md"
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' "$SCRATCH/c2pc.md" | wc -l  # want 0
grep -o 'candidatesXX' "$SCRATCH/c2pc.md" | wc -l                                                                # want 6 — PROVE it landed
```

⚠️ **WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -c '-family row'` fails (GNU: `grep: amily row: No such file or directory`) and the surrounding arithmetic prints `0`, **which reads exactly like "no change."** **Always pass `--` before the pattern**; with `--` occurrences read **96** and LINES read **68**. **`grep -c` counts LINES, NOT OCCURRENCES.** ⚠️ **AND `grep -c` PRINTS `0` *AND* EXITS 1, so `$(cmd || echo 0)` EMITS TWO ZEROS** — capture with `v=$(cmd || true)`.

⚠️ **THE MALFORMED-ROW FIGURE RECONCILES ONLY UNDER AN ESCAPE-AWARE FIELD COUNT.** Malformed rows are **ROW IDs 57** and **69** — ⚠️ **THOSE ARE ROW IDS, AT FILE LINES 119 AND 131; a naive list is printed in FILE-LINE terms, so the two are MIXED UNITS.** **Strip `\|` BEFORE splitting.** RE-MEASURED at the phase-94 IMPL: row 57 reads **naive NF=13 / escape-aware NF=9** (the forms DISAGREE); row 69 reads **10 / 10** (they AGREE). **Rows 93 and 94 both read 8 / 8.** **Any gate must state WHICH FORM it uses.**

⚠️ **AND DO NOT SUBSTITUTE A "REASONABLE EQUIVALENT" FOR CHECK (1)'s AWK.** A measurement agent once tried `NF>=8 && $2 ~ /^ *[0-9]+ *$/`: it printed **92**, not the true denominator. **Use the form above verbatim.**

**SENTINEL RUN MECHANICALLY AT THE PHASE-94 IMPL, ON BOTH SIDES OF THE ROW FLIP.** ACTUAL output, recorded not predicted:
**BEFORE** — (1) `NOT DONE: row 94` ONE line · (2) **SIX** · (3) SILENT · NC-A **TWO** · NC-B **TWO** · NC-C fired (0 residual) · NC-D **96 / 68**.
**AFTER** — (1) ⚠️ **SILENT** · (2) **SIX** at `:204 :210 :216 :226 :232 :240` · (3) SILENT · NC-A ⚠️ **ONE** · NC-B ⚠️ **ONE** · NC-C fired (0 residual) · NC-D **96 / 68** · check-(2) positive control drives it to **0** with **6** substitutions asserted.
Per-line md5 of the six windows, **trailing newline INCLUDED** (`sed -n 'Np' f | md5sum`) — **FIVE ARE BYTE-IDENTICAL to the inherited record and `:226` CHANGED, deliberately**, because phase 94 NARROWED that window:
`204 10d7807bf02d` · `210 4a92f7e62fc6` · `216 2a7eb298b9fd` · **`226 242e53c6f7a3` (was `4ad940205410`)** · `232 b2680e6f4fbf` · `240 6caa1c3ce0e7`
⇒ **THE SENTINEL DOES NOT FIRE. `stop` WAS EVALUATED AND DELIBERATELY NOT CREATED** (verified absent at the git root and in all four stage worktrees).
⚠️ **THE md5 DIGEST IS METHOD-SENSITIVE:** the six match ONLY with the **trailing newline included**. A `tr -d '\n'` form **MISMATCHES ALL SIX**. **State the method whenever you quote the digest.**
⚠️ **THIS RUN DOES NOT CARRY FORWARD — run all three checks, all four NCs *and* the check-(2) positive control YOURSELF, at YOUR OWN tip.**

⚠️⚠️ **THE NEW RULE THE PHASE-94 IMPL LEARNED THE HARD WAY, AND THE MOST TRANSFERABLE THING IT PRODUCED: NEVER RE-SPELL A SENTINEL MATCH PHRASE INSIDE A SENTINEL WINDOW.** That stage had to NARROW window `:226`, and the sentence it wrote explaining that *"narrowing cannot move check (2)"* **QUOTED THE MATCH PHRASE VERBATIM** — so `:226` matched TWICE and check (2) went **SIX -> SEVEN**. **The sentence asserting the sentinel could not move MOVED IT.** It was caught ONLY because the check was re-run after the edit; a session reasoning *"narrowing cannot move check (2), so there is nothing to re-measure"* would have shipped a broken termination sentinel while quoting the rule that says it is safe. This is `reference_self_incrementing_positive_control` in its sharpest form — the self-reference was **inside the gate's own scan region**.

- ⚠️ **LIVE FIGURES RE-DERIVED AT THIS TIP:** `ROADMAP.md` **244** lines / **126** data rows / tail row **94** (`done`) · `DECISIONS.md` **18872** · `^---$` **216** · `^## ADR-` **315** · bare `^## ` **323** · tail **ADR-0316** · next-free **ADR-0317** (`^## ADR-0317` reads **0**) · house `PROPOSED` **0 (DISARMED)** · ADR-0231 decoy **1** at `:14866` · `BEHAVIOR_CONTRACT.md` **5989** · `STATE.md` **65** (rolled IN PLACE, 9 insertions / 9 deletions) · `STATE_HISTORY.md` **546** (544 -> 546, +2), strict **163 (DELTA 0)** / parenthetical **60** / loose **223** (**163 + 60 = 223 exactly**) · phase dirs **135** · fixtures **122** (tail `0120-tls-connection-error`; **`0121` is the next free index**) · fuzzers **56 targets / 48 FILES** · `go.mod` **67 REQUIRE ENTRIES** — ⚠️ **67 IS THE REQUIRE COUNT, NOT A LINE COUNT; the `grep -cE '^\s+[a-z0-9./-]+ v[0-9]'` form reads 62** · `go list ./...` **237** (**235** after excluding the two Docker drivers) · BackendKind tail **38** (`H2GoawayResponder BackendKind = 38`) · `-family row` **96 / 68**.
- ⚠️ **THE STAT SURFACE IS QUOTED AS A DELTA AND NEVER AS AN ABSOLUTE.** THREE different absolutes are live in this tree at one tip. Per `reference_a_drift_correction_is_itself_a_claim`, on a contested count: **NO NUMBER.** ⚠️ **THE ONE EXCEPTION IS THE LEDGER'S OWN INTERNAL CHAIN** at `BEHAVIOR_CONTRACT.md`'s `### Stat surface`, whose tail is now the phase-94 entry; that chain is self-consistent and documentary, and it explicitly disclaims being authoritative.
- ⚠️ do NOT "fix" the six in check (2) and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases, and **each new FAMILY opens a new window**, so the count has only ever grown. ⚠️ **Cite ~40 only as "sentence-visible", never as the inventory** — and **no gate may rest on a ` + ` split. THAT RULE IS EMPIRICALLY CONFIRMED: a ` + ` split is WRONG ON 3 OF THE 6 WINDOWS and reports `:240` as having ONE item when it has THREE.**

==============================================

## STATUS AS OF 2026-09-05: PHASE 94 (`tls-connection-error-stat`) **CLOSED, lifecycle-state DONE**. **THIS SESSION SELF-PICKS AND RUNS A BRAINSTORM.**

**What phase 94 landed.** `outcomeOther` — the fourth downstream-TLS handshake-outcome bucket, which previously incremented nothing — now increments `listener.<normalized-addr>.ssl.connection_error`, the FIFTH listener-scope TLS counter, gated by `isTransportHandshakeErr`: a **CLOSED transport COMPLEMENT** (`io.EOF`, `io.ErrUnexpectedEOF`, `syscall.ECONNRESET`, `net.ErrClosed`, `context.DeadlineExceeded`) matched by `errors.Is` ALONE, with **no message-text matching**. The named departure at `BEHAVIOR_CONTRACT.md:1971` is **CLOSED**. ⚠️ **The `handshakeOutcome` taxonomy STAYS FOUR while this scope's counters go to FIVE** — `outcomeOther` is now the ONLY outcome mapping to a counter CONDITIONALLY. Fixture `0120-tls-connection-error` (port `10126`) landed with five drive arms. The TWENTY-FIFTH Observability-family row; **the family STAYS OPEN.**

**Cost, MEASURED by `--numstat`:** `+2474 / -72` total, of which `.go` is `+1143 / -52`. The PLAN estimated `≈+1608 / -25` with `≈+995` `.go` ⇒ **1.54× and 1.15×**. `reference_measured_prototype_is_a_lower_bound` held an **ELEVENTH** consecutive row, with the gap concentrated in prose and enumeration, not code.

### ⚠️ WHAT THE NEXT SESSION MUST SELF-PICK FROM — candidates with MEASURED hooks

Record the pick AND the rejected alternatives in the BRAINSTORM. Smallest defensible candidate first.

1. ⚠️ **THE SEVENTH `outcomeOther` ARM — ALPN NEGOTIATION FAILURE. THE CHEAPEST, AND IT HAS A LIVE UNASSERTED SIDE EFFECT.** It reaches the `Inc` site TODAY (proven at phase 94 by a `panic()` reachability control, which found it the *only* pre-existing reacher), and since phase 94 landed, **`TestNewManager_LiveHandshake_ALPNNegotiationFailure_Aborts` silently increments `ssl.connection_error` while asserting nothing about it.** It is absent from `SPEC.md` §2.2's six-row table. Phase 94 did NOT add it to the unit table because it needs an ALPN-configured listener — against `startOneWayTLSListener` the handshake SUCCEEDS.
2. ⚠️ **THE DRIVER-OWNED RECEIVER PORT RACE — NOW TWICE-OBSERVED, AND IT COST A FULL 400-SECOND DIFFERENTIAL RUN AT PHASE 94.** It ABORTS THE TEST BINARY: `panic: driver: start ALS receiver on 0.0.0.0:40065: bind: address already in use`, at `0081-grpc-access-log`, so **`0082`-`0120` never ran** and the run reported `RUN=96` with the newest fixture ABSENT. Root cause is structural and known: the driver's receiver band sits **inside `net.ipv4.ip_local_port_range` (32768-60999)**, so it collides with ephemeral allocation. **A real, chartered-scale fix.**
3. `0108`'s two *"envoy-go emits NO `ssl.*` stats whatsoever"* confessions — **false since phase 74**, pre-existing drift; `0110`/`0111` retired that wording and `0108` was never updated.
4. **No `len(helpText)` guard exists**, and `internal/stats/name.go` carries two ungated prose counts inside the file they count.
5. `0118/driver/driver.go:31`'s falsified *"TLS/SDS band"* characterization (`0112`/`0113` carry ZERO `DownstreamTlsContext`).
6. **`0061-lb-ring-hash`'s σ-margin second occurrence** — a real follow-on the project owes.
7. **Carried:** the pooled-upstream-lifetime defect (three defects at three sites, reference side a PREDICTION not a measurement) · 1xx interim responses · the H/1 no-`Host` divergence · the other TEN fixed `ssl.*` names and the FOUR dynamic families (blocked on NAMING: the stat-name charset bans the hyphen, so an OpenSSL-form cipher name fails validation and panics at registration; `sigalgs` may be unimplementable, `crypto/tls.ConnectionState` exposing no signature-scheme field).

## ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + ALL FOUR NCs + THE CHECK-(2) POSITIVE CONTROL YOURSELF** and record ACTUAL output. ⚠️ **NC SHAPES CHANGE ACROSS A ROW FLIP OR ADD — NEVER INHERIT ONE.** Opening a row moves NC-A and NC-B back to TWO lines.
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** 92 IMPL twelve · 93 BRAINSTORM eleven · 93 SPEC eleven · 93 PLAN twelve · 93 IMPL nine · 94 BRAINSTORM eleven · 94 SPEC twelve · 94 PLAN sixteen · **94 IMPL TWELVE, of which THREE invalidated text the stage had ALREADY COMMITTED and were corrected in place.** Do the same, and record it when your agents refute you. ⚠️ **AND WHEN TWO AGENTS CONTRADICT EACH OTHER, FIND THE VARIABLE RATHER THAN PICKING A WINNER.** At phase 94 two streams disagreed on whether the tree already held a committed `clientAuth` leaf; the controller resolved it by measuring, and **the variable was `committed` vs `present on disk`** — one probe scanned the working tree where a prior test run had left a `.gitignore`d generated file. **Both probes reported truthfully about different things.**
3. **LINE ANCHORS DRIFT; SYMBOL AND LITERAL-TEXT ANCHORS DO NOT — RE-VERIFY EVERY CITE AT YOUR TIP.** ⚠️ **PATHSPEC-SCOPE every symbol assertion and every `sed`.** ⚠️ **A "DRIFT-PROOF" ANCHOR IS ITSELF A CLAIM: the phase-94 PLAN introduced a twelve-row STABLE ANCHOR table precisely because line numbers drift, and TWO of its rows did not survive contact with the tree** (a `helpTextRoster` entry cited with an `<addr>` placeholder that does not exist, and a test symbol that does not exist under the name given). ⚠️ **`internal/filter/hcm/chain.go` DOES NOT EXIST — it is `internal/filter/http/chain.go`** — but ⚠️ **`actions.go` IS in `hcm`.**
3b. ⚠️ **CHARACTER-CLASS AND ESCAPE SELECTORS ARE THE FAIL-UNSAFE FAMILY.** `grep -cE '^\s+[a-z0-9./-]+ v[0-9]'` on `go.mod` reads 62 not 67 · `^[0-9]{4}-` on fixtures reads 120, dropping `0007a`/`0007b` (use `ls -d test/fixtures/*/ | wc -l`) · ⚠️ **`\t` in a regex is TOOL-DEPENDENT: harness `grep` is ugrep 7.8.4, `/usr/bin/grep` is GNU 3.11. NAME THE TOOL.** ⚠️ **AND A CASE-SENSITIVE GREP IS BLIND TO UPPERCASE PROSE — use `-i` when auditing comments.**
3c. ⚠️ **`feedback_brief_citations_not_evidence` APPLIES TO INCIDENTAL FIGURES. Re-derive EVERY number you restate — including every number in THIS file.**
3d. ⚠️ **NO GATE MAY REST ON A ` + ` SPLIT.** Empirically confirmed: wrong on 3 of 6 windows.
3e. ⚠️ **A GATE COMMAND CAN MATCH PROSE INSTEAD OF CODE, AND IT READS LIKE A REAL FINDING.** The phase-94 PLAN's fixture-set gate extracted blank imports with a bare path grep; **five fixtures are named in COMMENTS as well as imports**, so it read **126 entries for 121 imports** and `comm` reported five phantom "imports with no dir". Its `sed` also used `|` as the delimiter, which **errors out** the moment the driver-or-inputs alternation the gate itself needs is added — the extraction then reads **0** and ALL 122 directories look unimported. **Anchor on the blank-import LINE:**
```sh
extract () { grep -oE '^[[:space:]]*_ "github\.com/pgdad/envoy-go/test/fixtures/[^/]+/(driver|inputs)"$' "$1" \
  | sed -E 's#.*/test/fixtures/##; s#/(driver|inputs)"$##' | sort; }
```
**At this tip: dirs 122 = imports 122, both `comm` directions EMPTY**, layout split **98 `driver/` + 24 `inputs/`**. **NC the extractor** (rename one import) — it must report the rename in BOTH directions.
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Live: TWO SDS dial-budget flakes ⚠️ **plus `TestSDSEndToEnd_FetchFailure_BootFailsClosed`, WHICH RECURRED AT THE PHASE-94 IMPL** (untouched package, green in isolation and on the next full run) · ⚠️ **the driver-owned receiver port race, WHICH ALSO RECURRED AT THE PHASE-94 IMPL and aborted a full differential run** · `internal/httpclient` zero-value · the two 84.2-era flakes · the REFERENCE h2spec section-8 flip · `TestOutlierDetector_ConcurrentEjectExactlyOnce` · `0061-lb-ring-hash`'s σ-margin · `TestP83_StopPauseTimer_IsAuthoritativeAgainstAnEnteredClosure` (wasm, 1/400-trial; did NOT recur at the 94 PLAN or IMPL). ⚠️ **`TestServerConn_TinyWindowDelivery` IS NOT A FLAKE** — phase 91 FIXED a live production deadlock; a recurrence is a REGRESSION of row 91. ⚠️ **A GREEN RERUN CLEARS NOTHING.**
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only). ⚠️ **AT THE PHASE-94 IMPL A SIBLING `curl-world` SESSION OWNED SIXTEEN CONTAINERS** and was busy throughout. ⚠️ **A `reaper_*` testcontainers Ryuk container is created BY THE DIFFERENTIAL ITSELF and is REUSED; leave it alone.** ⚠️ **Randomly-named containers appear and vanish between listings — that is sibling churn, not yours.** **Treat NO roster as stable.**
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`. ⚠️ **`INNER_EXIT` DOES NOT EXIST in this repo.**
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL.** **`gofmt -l` never exits non-zero — gate on OUTPUT.** ⚠️ **`golangci-lint` DOES exit non-zero on findings — but STILL gate on OUTPUT.** ⚠️ **go test WITHOUT `-v` PRINTS ZERO `=== RUN`** — `RUN=0` beside `RC=0` is a VACUOUS GREEN. ⚠️ **A `-run` selector matching nothing prints `[no tests to run]` and EXITS 0.** ⚠️ **On `-v` output, unanchored `grep -c 'FAIL'` reads nonzero ON A FULLY GREEN TREE** — use `grep -cE '^(FAIL|--- FAIL)|^ *--- FAIL'`.
7b. ⚠️ **`grep -c` ON ZERO MATCHES PRINTS `0` AND EXITS 1** — capture (`v=$(… || true)`), NEVER chain with `&&`, and NEVER write `$(… || echo 0)`.
7c. ⚠️ **A GREEN TEST RUN IS NOT EVIDENCE A SITE IS EXERCISED** — prove it with a **`panic()` REACHABILITY CONTROL**. At phase 94 this found the *only* pre-existing reacher of the new `Inc` site and overturned a SPEC conclusion.
7d. ⚠️ **NC EVERY NEW TEST, AND NC YOUR OWN FIXES** — a NC that is a BUILD BREAK proves nothing; **NEUTRALISE instead so the package still compiles.** ⚠️ **AND CHECK THE NC IS EVEN EXECUTABLE**: phase 94's NC 9 as written was **self-contradictory** (with the compile-time interface assertion in place, renaming the method is a BUILD BREAK, so it cannot also "still pass"); it had to be SPLIT into a vanish-proof half and a guard-fires half.
7e. ⚠️ **A UNIT TABLE OVER A SHARED ASSERTION IS NOT A GATE ON PRODUCTION BEHAVIOUR** — and ⚠️ **`t.Helper()` IS WHAT SAVES IT**: it gives each failure a DISTINCT call-site line.
7f. ⚠️ **A NC THAT LEAVES YOUR NEGATIVE CONTROL GREEN IS NOT EVIDENCE THAT CONTROL DOES ANY WORK** — you need a SECOND, ISOLATING NC. ⚠️ **AND THE CONTROL CELL IS WORTH RUNNING EVEN WHEN THE PLAN DOES NOT ASK**: at phase 94, re-running the isolating NC with the roster reverted to its OLD size came back FULLY GREEN, which is what turned "the roster is the guard" from a reading into a measurement.
7g. ⚠️ **ASSERT *WHICH* ERROR FIRED, AND ALSO WHICH DID NOT.** ⚠️ **AND KEEP TABLE ROWS SINGLE-CAUSE.** ⚠️ **PREFER AN EXACT EQUALITY TO A FLOOR** where a floor cannot distinguish the hypotheses.
7h. ⚠️ **A PROBE CLIENT CAN WITHHOLD THE VERY THING THE ARM EXISTS TO TEST** — force-send and PRINT what was sent. ⚠️ **AND A TEST TABLE CAN CARRY AN INVENTED INPUT NO PRODUCTION PATH PRODUCES**: `errors.Is(errors.New("connection reset by peer"), syscall.ECONNRESET)` is **FALSE** while the texts are byte-identical. Build counter tables on PRODUCTION-REPRESENTATIVE values.
7i. ⚠️ **A GATE THAT READS 0 HAS NOT BEEN SHOWN TO WORK.** Phase 94 ran its anchored panic gate over a log KNOWN to contain a panic (its own aborted differential run) and got **1** — which is what distinguishes *"nothing panicked"* from *"the gate is broken."* **Do that for every silent gate.**
8. ⚠️ **`pgrep -f` / `pkill -f` MATCHES YOUR OWN SHELL AND KILLS THE TOOL CALL (exit 144)**. **Kill ONLY PIDs you captured yourself**, matched exactly.
9. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-*`, `wt-*` or stage worktree should outlive its stage close** — expect only `master` (verify with `git worktree list`).
10. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** (a) differential — **122/122 expected now**; (b) non-Docker sweep gated on `PIPESTATUS[0]` + a SET RECONCILIATION — **235 packages**; (c) h2spec `95 tests, 94 passed, 1 skipped, 0 failed`; (d) fuzzers **56 / 48**; (e) the ANCHORED panic gate `^panic:|DATA RACE|SIGSEGV`, **0** *and PROVEN LIVE*; (f) no REVIEW.md — standing departure. ⚠️ **`-race` on the DIFFERENTIAL suite is VACUOUS** — the subject is an unraced subprocess.
11. ⚠️ **THE DIFFERENTIAL'S FAILURE MODE IS A SILENT PASS, AND PHASE 94 SAW IT FOR REAL.** ⚠️ **`-count=1` IS NOT OPTIONAL** — the harness builds envoy-go as a **SUBPROCESS**, so a production edit is **not a compile-time input** and the cache serves a stale PASS. ⚠️ **ASSERT THE FIXTURE SET BY NAME, BOTH DIRECTIONS, AGAINST THE FIXTURES ON DISK** — at phase 94 the first full run ABORTED at `0081` and reported `RUN=96` **with the newest fixture never having run at all**, which a count-only or RC-only gate would have read as a pass. ⚠️ **The full suite takes ~400s.**
11b. ⚠️ **`go test ./...` DRIVES DOCKER — AND THERE ARE TWO DOCKER DRIVERS.** Besides `test/differential`, **`test/conformance/h2spec`** also drives Docker. **Exclude BOTH:** `go list ./... | grep -vE '/test/differential$|/test/conformance/h2spec$'`.
12. ⚠️ **`-p` PUBLISHING WORKS; `--network host` IS WHAT DOES NOT.** ⚠️ **`STRICT_DNS` + `host.docker.internal` + `--add-host=host.docker.internal:host-gateway` IS REQUIRED for a container-to-host cluster HERE** (VM-backed Docker, **`docker0` DOES NOT EXIST on the host**; `host.docker.internal` resolves to `192.168.65.2`), so a `STATIC`-by-bridge-IP reference cluster is **SILENTLY UNREACHABLE** (ADR-0010). ⚠️ **THE REFERENCE CONTAINER CANNOT READ `filename:` CERT PATHS** — a host path does not exist inside it; use `inline_string:` (the `0110` route, PEMs pre-indented into the template) unless the driver implements `fixture.ReferenceLogMounter`. ⚠️ **A PROBE BACKEND MUST BIND `0.0.0.0`, NOT `127.0.0.1`.** ⚠️ **Keep PEM substitutions OUT of YAML comments** — `text/template` expands actions inside comments too, and a multi-line PEM splatters outside the `#`. ⚠️ **envoy-go REJECTS `match.headers`**, ⚠️ **refuses `codec_type: HTTP2` without TLS unless `--allow-h2c`**, ⚠️ **boots with `-c`, NOT `--config-path`**, ⚠️ **`access_log[].log_format` is BOOT-REJECTED**, and ⚠️ **`TLSv1_0`/`TLSv1_1` in a `tls_params` block BOOT-REJECT envoy-go** (`internal/tls/params.go`) — a bad-version arm must be produced **CLIENT-side**. ⚠️ **`clusters: []` — THE INHERITED BOOT-REJECT CLAIM DID NOT REPRODUCE; do not repeat it as fact.** ⚠️ **`ENVOY_TARGET.md` is at `docs/envoy-go/ENVOY_TARGET.md`, NOT the repo root.** ⚠️ **The pinned reference is `envoyproxy/envoy:contrib-v1.37.2`, digest `7edd5b0fd763…` — PIN BY DIGEST, and verify against `ENVOY_TARGET.md:4` before trusting any arm.**
13. ⚠️ **A RAW-FRAMER PEER MUST HOLD ONE `hpack.Decoder` PER CONNECTION.** ⚠️ **Set `ReadMetaHeaders` to NIL when probing malformed shapes.** ⚠️ **`0004` is TLS+ALPN-h2, NOT h2c.**
14. ⚠️ **`bash /dev/tcp` + `timeout cat <&3` INSIDE COMMAND SUBSTITUTION RETURNS SILENTLY EMPTY** — use Go for socket probes.
15. ⚠️ **GO DOES NOT FLAG UNUSED CONSTANTS.** Post-removal cleanup is grep-driven.
16. ⚠️ **`golangci-lint`'s misspell runs in locale US.** **Sweep British spellings in `.go` comments BEFORE the gate.** ⚠️ **Markdown prose may use them freely.**
17. ⚠️ **A GREEN RUN OF A CONTENTION-SENSITIVE TEST PROVES NOTHING.** Phase 91's subject cleared 5/5 uncontended while failing 9/32 contended on the SAME binary.
18. ⚠️ **AN ABSENCE CLAIM NEEDS A POSITIVE CONTROL AND A DISCRIMINATING NEGATIVE CONTROL.**
19. ⚠️ **DERIVE next-free ADR FROM THE TAIL, NEVER FROM THE HEADING COUNT.** The id space is SPARSE (exactly one gap, `0209`) and **headings+1 now reads 316 — a TAKEN id.**
20. ⚠️ **`reference_measured_prototype_is_a_lower_bound` HAS NOW FIRED **ELEVEN** CONSECUTIVE ROWS, ALWAYS BY UNDER-ENUMERATING *FILES*, *SITES* OR *SYMBOLS*.** At phase 94 it fired again on both axes: the SPEC's pinned contract-edit map **missed two live sites** (the phase-65 C3 boundary and the stat-surface ledger) plus a third false proposition on a line it DID name, and the measured cost came in at **1.54×** the PLAN's estimate. **ENUMERATE BY GREPPING THE LITERAL CLAIM TEXT OUTWARD FROM ANY PINNED MAP — NEVER TRUST THE MAP'S OWN COMPLETENESS — AND BY COMPILING.**
21. ⚠️ **A MEASUREMENT THAT GOVERNS A DEFERRAL DECISION MUST LAND IN A GOVERNING DOCUMENT.** Phase 77's reference measurement stayed in its BRAINSTORM and was lost for seventeen rows. **DISCHARGED at phase 94: the rule lives in `ADR-0316 §Context ¶2`.**
22. ⚠️ **A STAGE'S FILE SCOPE IS ITSELF MEASURABLE — MEASURE IT RATHER THAN INFERRING IT FROM AN INSTRUCTION'S WORDING.** `git show --numstat` on the precedent stage settles it in one command. **For a BRAINSTORM, measure against the phase-94 BRAINSTORM commit `b75b0e2d` and the phase-93 one** — a BRAINSTORM **DOES** touch `ROADMAP.md` (it registers the new row `in-progress`, an ADD that moves `want`), unlike a PLAN, which touches none of the governing documents.
23. ⚠️ **`git diff --stat` IS A SUM, NOT ADDITIONS.** Cost figures come from `--numstat`.
24. ⚠️ **AN UNESCAPED `|` IN A ROADMAP ROW PASSES CHECK (1) AND SILENTLY BREAKS THE FIELD COUNT.** **COUNT THE FIELDS UNDER *BOTH* FORMS BEFORE INSTALLING.** At phase 94 the first draft of row 94's summary read **NF=10 under both forms** because the narrative contained two literal `|` characters; escaping them as `\|` fixed the escape-aware form but left NAIVE at 10, which would have added a THIRD naive-malformed row. **Both were REWORDED AWAY entirely instead** — that is the right fix, not escaping. Row 94 now carries **7** pipe characters and reads **8 / 8**.

## ⚠️ THE `PROPOSED` GUARD — CURRENTLY DISARMED; A NEW SPEC RE-ARMS IT

| form | now | where |
|---|---|---|
| `^> \*\*STATUS: PROPOSED` — **the house form** | ⚠️ **DISARMED (reads 0)** | disarmed at the phase-94 IMPL when `ADR-0316` was completed in place |
| `^\*\*Status:\*\* PROPOSED` — **the ADR-0231 decoy** | unchanged, reads 1 | `:14866` under `## ADR-0231`, byte-untouched |

⚠️ **VERIFY BY LINE AND BY ADR, NEVER BY THE COUNT ALONE** — resolve any hit to its enclosing ADR by a BACKWARD heading search (`awk 'NR<=<HITLINE> && /^## ADR-/ {h=$0; n=NR} END {print n, h}'`). ⚠️ **NEVER gate on the UNANCHORED form nor the middle-ground `^\*\*Status:\*\*.*PROPOSED` form — they are DIFFERENT REGEXES and must never be conflated.** ⚠️ **AND DO NOT QUOTE A COUNT FOR EITHER FORM IN PROSE THE GREP MATCHES.** **A SPEC stage RE-ARMS the house form 0 -> 1; the matching IMPL disarms it back to 0.**

## ⚠️ THE ARCHIVE GUARD

⚠️ **THE STRICT `163` IS NOT THE ENTRY COUNT.** Measured at this close: loose **223** = strict **163** + parenthetical **60**, exactly. **The guard is SOUND as a DELTA-0 SHAPE check** (a correctly-shaped parenthetical append moves the STRICT form by 0 — and it did, verified by NC before the append) **but the number is a SUBSET.** The forms disagree and **any gate must name WHICH it uses.**

⚠️ **THE ARCHIVE'S POSITIVE CONTROLS ARE SELF-INCREMENTING.** A control figure recorded in the file it measures is invalidated by the act of recording it. ⚠️ **THIS STAGE'S ARCHIVE LINE THEREFORE NAMES NO CONTROL FIGURE AT ALL.** ⚠️ **The eviction check has the same trap:** if the §Recent preamble NAMES the evictee, a naive grep on `STATE.md` reads **1**. **The DISCRIMINATING form is the strict `^- \*\*prior active-phase:\*\* \`<label>\`` form.** ⚠️ **AT THIS CLOSE BOTH forms read 0 on `STATE.md`** — the preamble describes the evictee as *"the SPEC stage of the phase preceding the current one"* without spelling its label. **Do that.**

⚠️ **THE DATE SHAPE CHANGED AGAIN — THAT IS FIVE DIFFERENT SHAPES IN FIVE CONSECUTIVE CLOSES.** A DIRECT DATE READ at this close gave `2026-09-02`, `2026-09-01` x2, `2026-08-31`, `2026-08-29`: **the oldest is UNIQUE and is ALSO the tail entry**, so no tie-break was needed. ⚠️ **A TIE DID EXIST — at `2026-09-01` — BUT IT WAS NOT AT THE OLDEST POSITION AND WAS THEREFORE IRRELEVANT.** *"There is a tie"* and *"the eviction needs a tie-break"* are DIFFERENT PROPOSITIONS; the preceding close's tie WAS at the oldest position and did need breaking, by LIFECYCLE ORDER within the shared date. **INHERIT NO SHAPE; READ THE DATES YOURSELF, AND CHECK WHETHER ANY TIE YOU FIND IS ACTUALLY AT THE OLDEST POSITION.**

## ⚠️ PORT BANDING

`net.ipv4.ip_local_port_range = 32768 60999`. ⇒ **BAND YOUR PROBE PORTS BELOW 32768.** ⚠️ **The differential harness reserves `20000..31007` (`harness_test.go:293`)**, so `12000-19000` is the safe band for ad-hoc probes — a failure OUTSIDE the harness band is an **out-of-band** occurrence, NOT an in-band recurrence. **Check availability with `ss -tan` (ALL states), never `ss -ltn`.** ⚠️ **Parts of `12000-19000` ARE OCCUPIED by the sibling `curl-world` containers (18080-19000 observed) — CHECK, do not assume.** ⚠️ **BUT FOR A UNIT TABLE, USE PORT 0:** `connPair` and `startOneWayTLSListener` bind `127.0.0.1:0`, so the banding rule is MOOT there and a fixed port would be a needless flake source. ⚠️ **FIXTURE ports follow the `10<index>` family band; `0120` holds `10126`** (`10120` is held by `0028`'s `10120-10125` run). ⚠️ **DO NOT cite `0118:31`'s `10450` "TLS/SDS band" carve-out — its premise is FALSIFIED** (`0112`/`0113` carry ZERO `DownstreamTlsContext`). ⚠️ **AND SEE CANDIDATE 2 ABOVE: the driver-owned receiver band sits INSIDE the ephemeral range and has now aborted two full runs.**

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (**ugrep 7.8.4**). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **THE METHOD BINDS; carry no standing discrepancy figure and no standing absolute.** ⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS; print FILE NAMES, never a count.** ⚠️ **THE HARNESS `grep` STRIPS A LEADING `./`.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files; use `-v --no-index`, and `git status` is the discriminating check). ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **A DISCREPANCY MAY FAIL TO MANIFEST** — absence of a discrepancy is not evidence the blindness is gone.

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 94 (tls-connection-error-stat)' --format='%h %s'   # the SLUG form
git log --grep '^phase 94' --format='%h %s'                               # the LOOSE form — RUN IT to CROSS-CHECK
```
⚠️ **RUN BOTH FORMS AND RECONCILE THE SETS. NO COUNT IS QUOTED HERE, DELIBERATELY — BOTH FORMS ROT.** **A `| head -1` on ONE form can silently return a NON-TIP commit. Take the tip from `git rev-parse master`.** ⚠️ **AND WHEN YOU LAND A CORRECTION, PUT THE FULL SLUG IN ITS SUBJECT** — a slugless correction is invisible to the slug form.

Execution style: **subagent-driven per `feedback_execution_style`**. Subagents commit LOCALLY on their own stage branch with EXPLICIT PATHSPECS; the controller merges and squashes (`feedback_subagents_no_push`, `feedback_push_to_origin`). **Pure MEASUREMENT agents should commit NOTHING and prove their tree clean.** ⚠️ **PARALLEL AGENTS IN ONE WORKTREE RACE THE GIT INDEX EVEN WITH DISJOINT PATHSPECS** — use **PER-AGENT WORKTREES ON THEIR OWN BRANCHES**. ⚠️ **GIVE EACH PARALLEL AGENT ITS OWN PRIVATE SCRATCH DIR**, and **SERIALIZE DOCKER OWNERSHIP TO EXACTLY ONE AGENT AT A TIME** — at the phase-94 IMPL, three streams ran and Docker was handed from one to the next explicitly. ⚠️ **VERIFY WHAT A SUBAGENT REPORTS — BUT ALSO BELIEVE IT WHEN IT REFUTES YOU, AND ARBITRATE BY MEASURING WHEN TWO OF THEM REFUTE EACH OTHER.** Per-task `gofmt` + `golangci-lint` on touched packages, **gated on OUTPUT**. ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root** — build with `-o` into scratch.

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root — **it fired repeatedly at the phase-94 IMPL and was caught only because every command used absolute paths.** Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` before any commit or gate run. ⚠️ **Shell VARIABLES do not survive between Bash calls** — re-export in every command. ⚠️ **AND AN UNQUOTED HEREDOC (`<<EOF`) COMMAND-SUBSTITUTES BACKTICKED TEXT** — use `<<'EOF'` and pass paths via the environment.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — `git status --porcelain` AND `git diff --stat master` must show ONLY your intended edits. For a patched TRACKED file: `sha256sum` BEFORE patching, `git checkout -- <path>` after, `sha256sum -c` the capture — ⚠️ **and COMMIT FIRST, because `git checkout --` restores from HEAD and will wipe uncommitted work.** ⚠️ **Also check the MAIN repo root, and `--untracked-files=all`.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads SELF-PICK A NEW SUBJECT AND RUN ITS BRAINSTORM.** ⚠️ **`grep -c 'next-skill:'` reads 2: one is the ADR-0288 RULE STATEMENT matching its own grep, and the live pointer is the other. That is CORRECT, not a violation.** ⚠️ **§Project counts is a PHASE-76-ERA SNAPSHOT and SAYS SO.**
2. `docs/envoy-go/ROADMAP.md` — the six deferred-candidate windows at `:204 :210 :216 :226 :232 :240` are where the SELF-PICK comes from. ⚠️ **Row 94's summary (file line 156) carries the phase-94 close narrative and the banked follow-ons.**
3. `docs/envoy-go/phases/94-tls-connection-error-stat/PROGRESS.md` — **560 lines.** §2 is the TWELVE refutations plus §2.13, the sentinel self-break; §4 the six-gate posture; §5 the before/after sentinel table; §7 the banked follow-ons. ⚠️ **Read §2.13 even if you skip everything else.**
4. `docs/envoy-go/DECISIONS.md` **ADR-0316** — the phase-94 decision, now COMPLETE (§Context + §Decision + §Consequences).
5. `docs/envoy-go/BEHAVIOR_CONTRACT.md:1965-1975` — the closed departure. ⚠️ **`:1971` IS ONE ENORMOUS PARAGRAPH LINE — every edit is WITHIN-LINE; `sed`-style line replacement destroys neighbouring propositions.** The same is true of `:1042`, `:1967`, `:1973` and `:1975`.
6. `docs/envoy-go/BOOTSTRAP_PROMPT.md` §4-§6 — the lifecycle invariants and the split-gate trigger.

## What the next session owes
**SELF-PICK A SUBJECT AND RUN ITS BRAINSTORM** (state DONE -> 1) — ⚠️ **ONE STAGE PER SESSION.** Record the pick AND the rejected alternatives · **REGISTER THE NEW ROW `in-progress` at the BRAINSTORM commit** per the §Schema invariant — an **ADD, not a flip**, so `want` **126 -> 127** and `ROADMAP.md` **244 -> 245** · ⚠️ **COUNT THE NEW ROW'S FIELDS UNDER *BOTH* FORMS BEFORE INSTALLING IT, AND PREFER REWORDING A PIPE AWAY OVER ESCAPING IT** · ⚠️ **PROVE THE PROVENANCE OF YOUR PICK AGAINST THE SIX WINDOWS AND NEGATIVE-CONTROL THE INSTRUMENT** — a bare token can share a substring with an unrelated candidate, so DISCLOSE any match rather than asserting a zero · **RE-RUN the sentinel + all four NCs + the check-(2) positive control yourself, BEFORE AND AFTER the row ADD** — ⚠️ **the ADD re-blocks check (1) and moves NC-A/NC-B back to TWO lines; RE-MEASURE, NEVER INHERIT** · ⚠️ **NEVER RE-SPELL A SENTINEL MATCH PHRASE INSIDE A SENTINEL WINDOW** · **REFUTE YOUR PREDECESSOR BY EXECUTION** and record it when your own agents refute you · `STATE.md` rolled IN PLACE with the oldest §Recent entry evicted by a **DIRECT DATE READ** (⚠️ **read the dates yourself — five consecutive closes produced five different shapes — and check whether any tie is actually AT the oldest position**) and archived to `STATE_HISTORY.md` as **ONE INLINE LINE** in the PARENTHETICAL form, so the strict guard stays at **163, DELTA 0** · ⚠️ **AND ROLL THE §Recent PREAMBLE SENTENCE TOO — the guard is structurally blind to it** · ⚠️ **WORD THE PREAMBLE SO IT DOES NOT SPELL THE EVICTEE'S LABEL** · ⚠️ **NAME NO POSITIVE-CONTROL FIGURE IN THE ARCHIVE LINE** · `next-prompt.txt` rolled (`git add -f`) · ONE squashed commit, merged and pushed — ⚠️ **and if a late measurement invalidates something you already pushed, land a CLEARLY-LABELLED correction commit (WITH THE FULL SLUG IN ITS SUBJECT) rather than force-pushing or leaving it standing.**
