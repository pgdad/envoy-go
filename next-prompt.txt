=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage. To use it to implement the next phase: read `docs/envoy-go/STATE.md` for the recorded NEXT, perform that one stage (the concrete action is spelled out below), land it, and re-roll this file to the following stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, the active phase is fully landed, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action. While ANY stage remains — as NOW: the **47.1 IMPL is pending** (and 47.2's full SPEC→PLAN→IMPL lifecycle after it), so tasks are NOT complete — do NOT create `stop`; perform the stage described below.
==============================================

RESUME a fresh session for envoy-go (router last updated 2026-06-29; the **phase 47.1 (stats-sink metrics_service) PLAN is DONE and LANDED** — docs-only direct-on-master, committed + pushed to origin; plan-document-reviewer APPROVED). Phase 47 is the **FOURTH Observability-family row** (`stats-sink-metrics-service`); the family STAYS OPEN. **ROW 47 STAYS `in-progress`** (flips `done` at the FINAL leg 47.2 IMPL per ADR-0106 + `reference_roadmap_split_phase_row_done`). The next action is the **47.1 (core) IMPL** — execute `PLAN-47.1.md` subagent-driven.

HEAD is the phase-47.1 PLAN docs commit atop the phase-47.1 SPEC commit `e054336a`. **Re-read the shas with `git log --oneline -6`, do NOT assume.** Confirm `git status` clean (the untracked `.claude/` is session cruft, leave it) + master == origin/master.

## What the 47.1 PLAN chartered (the IMPL contract — `docs/envoy-go/phases/47-stats-sink-metrics-service/PLAN-47.1.md`, the 10-task TDD spine)
The IMPL builds envoy-go's FIRST stats-export sink + FIRST `stats_sinks[]` consumer + FIRST new go.mod module in the Observability family (`github.com/prometheus/client_model v0.6.1`). The 10 tasks (each TDD: failing-test → run-fail → minimal-impl → run-pass → per-task gates → commit):
1. **PROGRESS-47.1.md scaffold + baselines** (fixtures 90 / fuzzers 49 / stat 1200 / BackendKind 38 / DECISIONS ADR-0261; client_model absent; go-control-plane v1.32.4) + the D-MS-SPLIT NO-sub-split note.
2. **The `Counter`/`Gauge`→`MetricFamily` mapping** (`internal/statssink/mapping.go` — NEW package; type-switch on concrete `*stats.Counter`/`*stats.Gauge` since the `Metric` interface has NO `Load()`; full dotted name, 0 labels, `timestamp_ms`, no help) **+ LAND `prometheus/client_model v0.6.1`** here behind the first `dto` import via `go get …@v0.6.1 && go mod tidy` (tidy-safe — the import keeps it).
3. **The `MetricsServiceClient` CLIENT-streaming typed wrapper** (`internal/grpcclient/grpcclient.go` — mirror `ALSClient :279`, NOT the unary OTLP wrappers; `metricsv3` service proto, already-direct go-control-plane).
4. **The `MetricsServiceSink`** (`internal/statssink/sink.go` — the `GrpcAccessLogSink` template SIMPLIFIED to a 1-deep handoff: bounded chan cap 8 + writer goroutine + identifier-once + reconnect-resend + drop-on-full + idempotent Close; a `metricsClient` seam for the fake) [full-package `-race`].
5. **The `Flusher`** (`internal/statssink/flusher.go` — the `stats_flush_interval` ticker walking the frozen Registry per tick; NO final-flush on ctx.Done) [full-package `-race`].
6. **The `stats_sinks[]`/`stats_flush_interval` parse arm + strict-rejects** (`internal/bootstrap/bootstrap.go` — a NEW top-level `parseStatsSinks` pass; `StatsSinkConfig`+`FlushInterval` live HERE; the TypeURL DERIVED from the proto descriptor + a literal-assertion guard [`reference_network_filter_typeurl_extensions`]; accept {AUTO,V3}; reject V2/knob-values/sibling-TypeURLs/google_grpc/empty-cluster/stats_flush_on_admin; note the enum is top-level `HistogramEmitMode_SUMMARY_AND_HISTOGRAM`) **+ `FuzzStatsSinkConfigParse`** (fuzzers 49→50).
7. **Boot wiring** (`cmd/envoy-go/main.go` — reuse the unconditional `dialer :131`; build `MetricsServiceSink`+`Flusher` when `len(bs.StatsSinkConfigs)>0`; **DO NOT append the metrics sink to the `[]accesslog.Sink` slice** [Submit-signature mismatch — use a SEPARATE `statssink.Sink` closer defer]; `Flusher.Start(ctx)` AFTER `bs.Stats.Freeze() :325`; `log.Fatalf` on a missing cluster [reference-parity]) + byte-stability (no sink ⇒ no goroutine) + the **+0 no-new-stat registration guard** (D-MS-STATS-FINAL).
8. **The driver-owned `test/helpers/metricsservice` receiver** (the `otlptrace` template, h2c gRPC `MetricsServiceServer` accumulating `MetricFamily` by name + the identifier node; NOT a BackendKind — stays 38; the §11 scratchpad seed is GONE, build fresh).
9. **The `0089-stats-sink-metrics-service` cross-side EXACT differential** (poll-to-converge, K=7, the deterministic COUNTER NAME-SUBSET `{cluster.<backend>.upstream_rq_total, http.<stat_prefix>.downstream_rq_total, downstream_rq_2xx}` all ==K + the node identifier; UNassert the whole set/histograms/timestamps/churn/framing; shared bridge + h2c receiver hostname; decode-ran proof) + the boot-reject SUBJECT unit confirmations (NO new fixture dirs).
10. **Deliberate breaks** (`-count=1`, `-run 'TestDifferential/0089'` NEVER bare) **+ 20/20 flake-soak + full-package `-race` + the full 91-dir differential + six-gate + ADR-0262 body + BEHAVIOR_CONTRACT + STATE/ROADMAP (row 47 STAYS in-progress) + count reconcile** (1200/91/50/38/ADR-0262; +1 module).

## The NEXT action — the 47.1 (core) IMPL (lifecycle-state 3 → 4)
**Execute `PLAN-47.1.md` SUBAGENT-DRIVEN** (`feedback_execution_style` + `superpowers:subagent-driven-development`) in a **FRESH git worktree off master** (`feedback_git_worktrees` — `superpowers:using-git-worktrees`). Dispatch ONE subagent per task; subagents commit LOCALLY only (`feedback_subagents_no_push`). The CONTROLLER (you):
- verifies each commit (correct fileset, real non-vacuous tests via `-v` + read assertions, per-task gates green: `gofmt -l` + `golangci-lint` on touched pkgs + `go vet` + `go build ./...` per `feedback_pertask_gofmt_lint`), cleans stray next-task leak files, re-verifies the branch is undetached + the main checkout clean (`feedback_subagent_worktree_detach`/`_path_targeting`);
- on the FINAL frozen HEAD: re-runs the FULL six-gate + the FULL-package `internal/statssink` `-race` (the Flusher ticker + sink writer are background mutators — `reference_full_suite_race_after_background_mutator`) + the full 91-dir differential, does the DELIBERATE-BREAK verification ITSELF (`-count=1` on every break + `-race`, `reference_differential_break_protocol_count1`; `-run 'TestDifferential/0089'` NEVER bare, `reference_differential_run_selector`; the full-suite `subject ready: EOF` startup flake is isolatable, `reference_differential_fullsuite_startup_flake`), then SQUASHES the worktree commits into one + fast-forwards onto master + PUSHES (`feedback_push_to_origin`) + removes the worktree.
The IMPL ANCHORS ADR-0262 (its §Decision/§Consequences body lands atomically at Task 10, ADR-0044 — the §Context draft is in SPEC-47.1.md §13; DECISIONS.md tail advances ADR-0261 → ADR-0262, next-free ADR-0263). Row 47 STAYS `in-progress` (47.1 is NOT the final leg).

## Counts at the 47.1 PLAN (UNCHANGED — docs-only; re-verify, do NOT assume)
stat surface **1200** (non-H2 **1196**) / fixtures **90** (tail `0088-tracing-zipkin`; count = `ls -d test/fixtures/*/ | wc -l`) / fuzzers **49** / BackendKind **38** / DECISIONS **ADR-0261** (next-free **ADR-0262**); `prometheus/client_model` NOT in go.mod/go.sum; go-control-plane/envoy **v1.32.4**. Anticipated at the 47.1 IMPL: stat **1200** (+0 — D-MS-STATS-FINAL) / fixtures **91** (`0089-stats-sink-metrics-service`) / fuzzers **50** (`FuzzStatsSinkConfigParse`) / BackendKind **38** / DECISIONS **ADR-0262** / **+1 go.mod module** (`prometheus/client_model v0.6.1`) + **1 new package** (`internal/statssink`).

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** The next stage is the 47.1 IMPL. Lifecycle-state 3 → 4. After it lands, ROLL THIS FILE to the **47.2 BRAINSTORM** (deltas + tags-as-labels; anticipated ADR-0263) — the FINAL leg that flips row 47 `done`.
- **Subagent-driven execution** (`feedback_execution_style`) in a **fresh worktree** (`feedback_git_worktrees`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER squashes + pushes at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). Per-task gates per `feedback_pertask_gofmt_lint`.
- **Process anchors:** ADR-0106 (per-leg rows) · ADR-0044 (ADR §Decision+§Consequences-at-IMPL) · ADR-0045 (NO sub-split — confirmed at PLAN) · ADR-0080 (strict-reject) · ADR-0059 (the in-tree `Registry`/`Walk`/`Freeze`) · ADR-0060 (histograms deferred — COUNTER/GAUGE-only) · ADR-0064 (the `stats_sinks[]` silent-ignore — 47.1 LIFTS the metrics_service sink) · ADR-0158 (the `grpcclient.Dialer` typed-wrapper — the `ALSClient` client-streaming precedent) · ADR-0016 (proto blank-import registry) · next-free **ADR-0262** (ANCHORED at the 47.1 IMPL).
- **Differential discipline** (the `0089` fixture): `-run 'TestDifferential/0089'` NEVER bare (`reference_differential_run_selector`); `-count=1` on every break + `-race` (`reference_differential_break_protocol_count1`); the full-suite `subject ready: EOF` startup flake is isolatable (`reference_differential_fullsuite_startup_flake`); the Docker bridge + decode-ran proof (`reference_docker_probe_bridge_network`); the gRPC metrics receiver is driver-owned, NOT a BackendKind (`reference_differential_grpc_receiver_driver_owned`); assert the aggregated COUNTER/GAUGE NAME-SUBSET by dotted name NOT the whole family set NOR stream framing (`reference_streaming_sink_differential_framing` + AMEND-MS-HISTOGRAM-PRESENT); the live reference is the wire truth (`reference_wire_format_both_sides_see_same_bytes`); the `Flusher`+sink writer goroutine are background mutators ⇒ FULL-package `-race` (`reference_full_suite_race_after_background_mutator`).

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-47.1 PLAN is DONE + LANDED; row 47 `in-progress`; the next action is the 47.1 IMPL — execute the PLAN subagent-driven).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 47.1 PLAN done`) + the recorded NEXT (the 47.1 IMPL) + the counts (1200 / 90 / 49 / 38 / ADR-0261).
3. `docs/envoy-go/phases/47-stats-sink-metrics-service/PLAN-47.1.md` — **THE IMPL CONTRACT** (the 10-task TDD spine + the Orientation zero-context brief [the source seams with line numbers + proto facts + discipline] + the D-MS-* resolutions + the locked file structure).
4. `docs/envoy-go/phases/47-stats-sink-metrics-service/SPEC-47.1.md` — the charter (the §11 D-MS-* live pins + the FOUR amendments + the §13 ADR-0262 §Context draft).
5. `docs/envoy-go/ROADMAP.md` — row 47 (`stats-sink-metrics-service`, `in-progress`) + the Observability family-progress note.
6. The phase-44/45/46 PLANs (`PLAN-45.1.md` the bootstrap-level grpc-sink twin; `PLAN-46.2.md` the driver-owned-receiver + differential precedent) + their PROGRESS files (the as-built six-gate + deliberate-break shape).
7. The as-built precedents the PLAN cites: `internal/accesslog/grpcsink.go` (the sink template), `internal/grpcclient/grpcclient.go` (`ALSClient` `:279`/`Dialer` `:108`), `internal/stats/registry.go` (`Walk` `:134`/`Freeze` `:209` + counter.go/gauge.go `Load`), `cmd/envoy-go/main.go` (`:131`/`:325`), `internal/bootstrap/bootstrap.go` (`Load` `:308` + `parseCommonGrpcAccessLogConfig` `:465`), `test/helpers/otlptrace`.
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_subagents_no_push`, `feedback_subagent_autocommit_claudemd`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_detach`/`_path_targeting`, `reference_roadmap_split_phase_row_done`, `reference_differential_grpc_receiver_driver_owned`, `reference_streaming_sink_differential_framing`, `reference_strict_reject_sibling_typeurl_gap`, `reference_docker_probe_bridge_network`, `reference_full_suite_race_after_background_mutator`, plus the differential-discipline references above).
