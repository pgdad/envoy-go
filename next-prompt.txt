START a fresh cold-start session for envoy-go — **the phase-41 circuit-breakers row is COMPLETE and LANDED on master** (the THIRD Upstream-robustness-family row; the 12-task TDD PLAN executed end-to-end subagent-driven, the full **76-dir differential GREEN**, the six-gate GREEN, **ADR-0248** landed IN-PLACE, ROADMAP row 41 flipped `in-progress → done`, PUSHED to `origin/master`). The next lifecycle step is a **FRESH BRAINSTORM for a NEW subject** (`superpowers:brainstorming` — the human picks the subject; the Upstream-robustness FAMILY STAYS OPEN with **2** candidates remaining: **retries + hedging** / **per-protocol connection pooling** [the latter is where the DEFERRED `max_connections` + `max_pending_requests` enforcement lands], or a new family row). Confirm the tip via `git log --oneline -5` (HEAD is the phase-41 IMPL squash — circuit breaking on the `max_requests` keystone + the STATE/ROADMAP/next-prompt roll — sitting on the phase-41 PLAN/SPEC/BRAINSTORM docs commits + the prior substantive phase-40.3 IMPL squash `148f288`). Counts: stat surface **1163** / fixtures **76** / fuzzers **42** / BackendKind tail **36** (`BlockingHoldResponder`) / DECISIONS tail **ADR-0248**, next-free **ADR-0249**.

## What just landed (phase 41 IMPL — the durable record is `docs/envoy-go/phases/41-circuit-breakers/{SPEC,PLAN,PROGRESS,README}.md`; read those, not this summary)

The 12-task TDD PLAN executed end-to-end subagent-driven (fresh subagent per task); six-gate GREEN; full **76-dir differential GREEN**; **ADR-0248** §Decision/§Consequences landed IN-PLACE per ADR-0044. Circuit breaking on the **`max_requests` keystone** (the SPEC narrowed the BRAINSTORM "core overflow trio" to `max_requests`-only per the live pins — AMEND-CB1; the reference's fail-fast 503 comes ONLY from a request budget, `max_connections` pends + `max_pending_requests` needs the connection-pool queue envoy-go's synchronous model lacks, so cx/pending enforcement DEFERS to the future per-protocol connection-pooling family row, registered emit-0 for parity):

- **The architecture (ADR-0248):** a per-priority `circuitBreaker` counter struct on `Cluster` (`internal/cluster/circuitbreaker.go` — `prio[2]cbPriority` indexed by `RoutingPriority` DEFAULT=0/HIGH=1 + `parseCircuitBreakers` [the 3 reject arms: `priority` enum-range / `retry_budget.budget_percent ∈ [0,100]` / strict duplicate-priority; the 1024 `max_requests` default; `per_host_thresholds` silent-ignored] + the non-blocking `tryAcquire`/`release` CAS + `registerStats`); the `circuitBreaker` field + `Cluster.TryAcquireRequest()`/`ReleaseRequest()` on `cluster.go` (no-op when nil); the `buildCluster` parse+attach + the scoped `registerClusterMetrics` block.
- **The admission seam:** a synchronous `TryAcquireRequest()` at `doH1ClusterAction` (`router.go:591`) + `doH2ClusterAction` (`router_h2.go:60`) — after `IncUpstreamRqTotal`, before the upstream call — with the overflow early-return (`ActionResponse{Status:503,...}`) PRECEDING the `defer ReleaseRequest()` (an over-budget request never releases a slot it didn't acquire → no double-release). On exhaustion: fail-fast `503` + `upstream_rq_pending_overflow`++ + `rq_open`=1; NO `IncStatusClass` on the overflow path. NO background goroutine; **byte-identical when no `circuit_breakers`** (the nil-guard — the 75-dir byte-stability gate held GREEN through every code task).
- **+14 stats (1149 → 1163):** 10 per-priority `*_open` gauges (`cx_open`/`cx_pool_open`/`rq_open`/`rq_pending_open`/`rq_retry_open` × {default,high}) + 4 cluster overflow counters (`upstream_{cx,cx_pool,rq_pending,rq_retry}_overflow`), scoped to `circuit_breakers` clusters; only `circuit_breakers.default.rq_open` + `upstream_rq_pending_overflow` go LIVE, the other 12 emit 0.
- **AMEND-CB held as-built:** (CB2) `max_requests` overflow uses `upstream_rq_pending_overflow` — the SAME counter as `max_pending_requests`; there is NO `upstream_rq_overflow`. (CB4) the `UO` access-log flag is a RECORDED DEPARTURE — envoy-go has NO response-flags plumbing (`ActionResponse`/the accesslog `Record` lack a flags field; `RESPONSE_FLAGS` hardcoded `-`); envoy-go emits the 503 STATUS + the overflow counter + `rq_open` (the observable contract). (CB6) NO new fuzzer (config-parse).
- **Differential `0074-circuit-breaker-max-requests`** (cross-side; +1 BackendKind `BlockingHoldResponder` 36 — a re-armable `/__release` gate): fill `max_requests` (N=4) with held-open requests, poll `rq_open == 1` on BOTH sides, fire the 5th ⇒ cross-side `503` + `upstream_rq_pending_overflow` delta + `rq_open` parity, then release via the backend `/__release` control port + drain to `rq_open == 0`. SLEEPLESS (the release barrier + poll-to-converge); sequential-per-side; 2 `-count=1` deliberate breaks + 20/20 flake-free.

**As-built counts:** stat surface **1149 → 1163** (+14) / fixtures **75 → 76** (`0074`) / fuzzers **42** (UNCHANGED) / BackendKind tail **35 → 36** (`BlockingHoldResponder`) / DECISIONS tail **ADR-0247 → ADR-0248** (next-free **ADR-0249**). ZERO new packages + ZERO new go.mod modules. The single ADR-0248 ABSORBED the BRAINSTORM's anticipated ADR-0249 at the narrowed scope.

## The next-subject landscape (the human picks at the BRAINSTORM — do NOT pre-decide)

The Upstream-robustness family (opened phase 39) STAYS OPEN. After phase 41, **2** candidates remain:
- **retries + hedging** (`RouteAction.retry_policy` / `request_mirror_policies` / hedging) — the retry substrate the phase-41 `max_retries`/`retry_budget` budgets (parsed-but-deferred) would feed.
- **per-protocol connection pooling** (`Cluster.{typed_extension_protocol_options, http_protocol_options}` pool tuning) — **this is where the phase-41-DEFERRED `max_connections` + `max_pending_requests` enforcement lands** (the connection-pool/queue semantics).

Phase 39 also UNBLOCKED 3 health-gated Load-balancing candidates {locality-weighted LB, priority load balancing, panic thresholds}. The human picks among these (or a new family row) at the BRAINSTORM.

## Honor throughout (the BRAINSTORM discipline)

- **Lifecycle = one stage per session:** the BRAINSTORM next (`superpowers:brainstorming` — explore the subject/scope/split + verify the proto in the EXISTING go-control-plane v1.32.4 module cache + sketch the architecture/differential/ADR/count envelope; the SPEC follows in a later session). `feedback_git_worktrees` (any isolated work in a worktree) · `feedback_subagents_no_push` (task subagents commit locally only; the controller squashes + pushes at stage-close) · `feedback_push_to_origin` (after a clean local merge on master with tests green, push without asking).
- **ADR/process:** ADR-0044 (the §Context drafts at the SPEC, the §Decision/§Consequences bodies land IN-PLACE at the IMPL) · ADR-0045 (the split-gate) · ADR-0052 (the six-gate at the IMPL) · ADR-0080 (envoy-go's own byte-stable rejects) · ADR-0106 + `reference_roadmap_split_phase_row_done` (a split-phase row flips `done` only when ALL legs land; NO parent rollup) · ADR-0227 (`contrib-v1.37.2`).
- **At the BRAINSTORM:** `reference_docker_probe_bridge_network` (any live reference-Envoy probe runs over a shared Docker bridge network + a STRICT_DNS backend hostname; verify the decode/request path ran) · `reference_proto_roster_extraction_digits` (digit-inclusive regexes when scraping a proto/stat roster).

## Read first (in order)

1. THIS FILE — the post-phase-41-IMPL cold-start (the phase-41 circuit-breakers row is COMPLETE + LANDED; the next step is a FRESH BRAINSTORM for a new subject; the human picks among the 2 remaining Upstream-robustness candidates or a new family row).
2. `docs/envoy-go/STATE.md` — active-phase `phase 41 (circuit-breakers) IMPL done`; the count footer (1163 / 76 / 42 / 36 / ADR-0248, next-free ADR-0249); the lifecycle routing (next-skill = a FRESH BRAINSTORM).
3. `docs/envoy-go/ROADMAP.md` — row 41 `done` + the §Upstream-robustness family heading (the family-progress note: active-HC DONE / outlier DONE / circuit breakers DONE; 2 candidates remain) + the open candidate rosters (the BRAINSTORM-subject landscape).
4. `docs/envoy-go/phases/41-circuit-breakers/{SPEC,PLAN,PROGRESS,README}.md` (the durable phase-41 record) — for the precedent shape of the NEXT BRAINSTORM/SPEC/PLAN/IMPL.
5. `docs/envoy-go/DECISIONS.md` (the ADR-0245/0246/0247 outlier triad + ADR-0248 circuit breakers + the process ADRs; next-free ADR-0249) + `docs/envoy-go/BEHAVIOR_CONTRACT.md` (the `### Cluster — load-shedding (circuit breakers)` subsection + the stat-surface block now at 1163).
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`.
