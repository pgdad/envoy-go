START a fresh cold-start session for envoy-go — **phase 27 (`network-filter-sni-cluster`) PLAN is DONE + committed + pushed** (master tip `42af3b1`, a docs-only next-prompt repoint that trails the substantive PLAN squash `a6f5ebb`), and the phase is OPEN/`in-progress`. There is an active phase. This session does the **27 IMPL** — per SKILL_ROUTING state 3 → 4 (`SPEC.md` + `PLAN.md` exist; PROGRESS.md does NOT) the next lifecycle step is `superpowers:subagent-driven-development` (per `feedback_execution_style`) → execute `docs/envoy-go/phases/27-network-filter-sni-cluster/PLAN.md` task-by-task → output PROGRESS.md + the code + the completion bundle, landing the phase atomically with the six-gate.

The phase-27 PLAN (9 bite-sized TDD tasks; plan-document-reviewer APPROVED first iteration, 3 advisories folded in; ADR-0045 split-gate RE-CHECKED → NO split [9 tasks / ~180–400 net-new LoC]) decomposes the SPEC §10 spine. `sni_cluster` (`envoy.filters.network.sni_cluster`, type `…sni_cluster.v3.SniCluster` — an EMPTY proto, `@type` via `proto.MessageName` carrying the `extensions.` segment) is a config-less read-filter that in `OnNewConnection` reads `Connection.RequestedServerName()` and, if non-empty, calls a NEW `SetUpstreamCluster(name)` on `ReadFilterCallbacks` with the SNI VERBATIM, then returns `Continue` (no sticky halt). The override is a NARROW typed `chainRuntime.upstreamClusterOverride` field (NOT a general filter-state primitive — Q2/YAGNI), threaded to the terminal `tcp_proxy` via the call `ctx` (`handleTerminal` wraps it iff set; `network.UpstreamClusterOverride(ctx)`; the `TerminalFilter.Handle` signature is UNCHANGED — no HCM ripple). `tcp_proxy` is made per-connection-resolving (retain `*cluster.Manager` + boot-resolved `defaultCluster`; override-then-fallback at `Handle`; unknown override cluster → downstream close / zero bytes [Envoy's `downstream_cx_no_route` family is NOT mirrored — surface +0]; back-compat byte-exact via the EXISTING 0000/0001/0002 + 26.x fixtures). ADR-0219 (override seam + tcp_proxy resolution) + ADR-0220 (sni_cluster filter) §Context are DRAFTED in DECISIONS.md (tail ADR-0220; next-free ADR-0221); their §Decision/§Consequences bodies land at THIS IMPL session per ADR-0044 (NO new ADR number consumed).

## What this session must do

There IS an active phase (27, `in-progress`, PLAN done). Per SKILL_ROUTING state 3 → 4 the next step is the **IMPL** via `superpowers:subagent-driven-development` (fresh subagent per task, two-stage review). Read the PLAN first (it is self-contained — exact file paths, code, commands, TDD steps per task); then execute its 9 tasks in order: (1) baselines/anchors gate (re-pin fixtures **46** tail `0044` / stats **136** / fuzzers **36** / DECISIONS tail **ADR-0220** [next-free **ADR-0221** — NOTE the SPEC §10 Task-1 row's "tail ADR-0218" was pre-SPEC-commit; the LIVE tail is ADR-0220, as the PLAN Task-1 already corrects] + `proto.MessageName` + the as-built line anchors); (2) the `chainRuntime.upstreamClusterOverride` field + `SetUpstreamCluster` ON `ReadFilterCallbacks` + concrete `*callbacks` impl; (3) the `UpstreamClusterOverride` ctx accessor (new `internal/filter/network/upstreamcluster.go`) + `handleTerminal` ctx-threading; (4) `tcp_proxy` per-connection resolution (`Filter.cluster` → `cm` + `defaultCluster`; override-then-fallback + unknown→close; the no-override back-compat sentinel stays in `tcpproxy/filter_test.go`); (5) the `internal/filter/network/snicluster/` config-less filter (`OnNewConnection` SNI→override+`Continue` LIVE-asserted; no-op `OnData`/`OnDestroy`); (6) registration as the 6th built-in (`builtins.RegisterBuiltins` SINGLE insertion point — D27-S2 RESOLVED: main.go delegates wholly to `RegisterBuiltins`, NO parallel edit) + the override-routes/unknown-close END-TO-END integration tests (homed in the `builtins` package test — the only no-import-cycle home; drive the FULL seam via `NewChainRuntime`→`OnNewConnection`→`OnData`→`HandleTerminal`, NOT `HandleTerminal` straight after `OnNewConnection` — for a mixed chain `onNewConnection` resets `resumeIdx` to 0 so `TerminalReady()` only fires after an `OnData` pass); (7) the `0045-sni-cluster` 3-arm cross-side TLS fixture (route / empty-SNI-fallback / unknown-close; mirror `0043-network-rbac` driver shape; deliberate-break proof); (8) back-compat differential re-verify + full suite 46→47; (9) the completion bundle (BEHAVIOR_CONTRACT 27 subsection + the `tcp_proxy` amendment + ADR-0219/0220 §Decision/§Consequences bodies in-place per ADR-0044 + STATE/ROADMAP row-27 `in-progress → done` + the six-gate GREEN LIVE quoted into PROGRESS.md). Subagents commit LOCAL-ONLY [`feedback_subagents_no_push`]; the controller squash-merges + pushes at stage-close [`feedback_push_to_origin`]; work in a git worktree [`feedback_git_worktrees`]; per-task `gofmt -l` + `golangci-lint` on touched pkgs [`feedback_pertask_gofmt_lint`].

## Project state at master tip

- **Master tip:** `42af3b1` (a docs-only next-prompt master-tip repoint), which trails the substantive phase-27 PLAN squash `a6f5ebb` (`phase 27 PLAN: sni_cluster — 9-task TDD decomposition; ADR-0045 NO split; D27-S1..S4 resolved; plan-document-reviewer APPROVED`; the PLAN.md + STATE/ROADMAP advance live there). Predecessor of the PLAN squash: the SPEC squash `e7c59e9` (trailed by the docs-only next-prompt repoints `85116a3`/`26e60f4`). Confirm the live tip via `git log --oneline -1`; any further next-prompt-only repoint advances the tip +1 (expected, not chased; per the 26.x precedent). STATE.md `last-commit` points at the PLAN squash `a6f5ebb`. origin/master in sync after push.
- **§9 Network-filters family:** parent row 26 DONE; row 27 (`network-filter-sni-cluster`) `in-progress` (PLAN done — flips `in-progress → done` at THIS IMPL phase-done). The family stays OPEN — after phase-27 phase-done, 5 candidates remain (`redis`/`mongo`/`kafka_broker`/`thrift`/`zookeeper`).
- **Counts at master tip (unchanged through PLAN):** stat surface **136**; differential fixtures **46** (tail `0044`); fuzzers **36**; conformance 10/10; h2spec 53/53; DECISIONS.md tail **ADR-0220** (§Context drafted; bodies land at THIS IMPL); next-free **ADR-0221**.
- **Anticipated phase-27 deltas at IMPL:** fixtures 46 → 47 (one `0045-sni-cluster` cross-side multi-cluster TLS dir; NO boot-reject — config-less); stat surface 136 (+0); fuzzers 36 (+0 — DEFERRED, echo-parity config-less); ADRs +2 bodies (ADR-0219 + ADR-0220; NO new number consumed at IMPL).
- **PLAN-time D-questions already resolved (carry into IMPL):** D27-S1 (the `0045` dir re-pinned at IMPL Task-1; fallback arm = a TLS connection with NO SNI on a `[sni_cluster, tcp_proxy]` chain); D27-S2 (main.go delegates wholly to `RegisterBuiltins` — single insertion point); D27-S3 (unknown-override close via the `Handle` early-`return` → deferred `downstream.Close()` FIN → zero-byte body parity; NO `SO_LINGER` on the terminal path); D27-S4 (fuzzer DEFER — fuzzers stay 36).
- **Toolchain:** Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); reference Envoy v1.37.2 (ADR-0008); go-control-plane v1.32.4 (ADR-0008).

## Read first (in order)

1. THIS FILE — the 27-IMPL cold-start.
2. `docs/envoy-go/phases/27-network-filter-sni-cluster/PLAN.md` — the SELF-CONTAINED 9-task TDD plan to execute (exact file paths, code, commands, expected output per step; the ADR-0045 re-check + D27-S1..S4 dispositions at the top; the Task-4 test-home-split + the Task-6 dispatch-fidelity notes).
3. `docs/envoy-go/phases/27-network-filter-sni-cluster/SPEC.md` — the settled SPEC (16 §§ + 1 appendix; §3 the override seam shaping [D27-3], §3.3 tcp_proxy per-connection resolution, §4 the filter, §8 the fixture taxonomy, §9/§14 the BEHAVIOR_CONTRACT bundle, §11 the empirical-pin evidence, §15 the test surface + six-gate + acceptance checklist).
4. `docs/envoy-go/phases/27-network-filter-sni-cluster/BRAINSTORM.md` — the 3 Q-decisions the SPEC/PLAN refine.
5. `docs/envoy-go/STATE.md` — active-phase `phase 27 PLAN done`; next-skill `superpowers:subagent-driven-development` (scoped to 27 IMPL); next-free `ADR-0221`; last-commit the PLAN squash.
6. `docs/envoy-go/ROADMAP.md` — row 27 `in-progress` (PLAN DONE note); the §9 family-open note + the 5 remaining post-27 candidates.
7. `docs/envoy-go/SKILL_ROUTING.md` + `BOOTSTRAP_PROMPT.md` §4/§5 — the per-phase lifecycle (state 3 → 4 IMPL) + the cold-start invariants.
8. The AS-BUILT framework the IMPL targets (the PLAN pins exact anchors — re-confirm at Task-1 since the tip may drift): `internal/filter/network/chain.go` (the `chainRuntime` struct @127, `handleTerminal` @209, the concrete `*callbacks` @321, `SetResponseCodeDetails` @355); `internal/filter/network/callbacks.go` (the `ReadFilterCallbacks` interface @16 + `DynamicMetadata()` @29 the on-interface precedent + `RequestedServerName()` @69); `internal/filter/network/terminal.go` (the `TerminalFilter.Handle` signature @42); `internal/filter/tcpproxy/filter.go` (the `Filter` struct @26 / `NewFilter` @47 / `NewNetworkFactory` @81 / `Handle` @94 — the per-connection-resolution target; only TWO `f.cluster` refs, both in `Handle`); `internal/filter/network/echo/echo.go` (the config-less template); `internal/filter/network/builtins/builtins.go` (the 5 built-ins @42, sni_cluster = 6th) + `builtins_test.go` (the all-built-ins assertion to extend 5→6); `internal/listener/manager.go` (`serveNetworkChain` @1025; the in-loop `TerminalReady` mixed handoff @1066); `test/fixtures/0043-network-rbac/driver/driver.go` (the closest cross-side fixture template).
9. `docs/envoy-go/DECISIONS.md` ADR-0219 + ADR-0220 (the §Context drafts — fill §Decision/§Consequences bodies at Task-9) + ADR-0213/0214/0215 (the §9 framework) + ADR-0044 (in-place ADR-body discipline) + ADR-0052 (atomic doc bundle) + ADR-0072 (registration-order-neutral) + ADR-0079 (two-step factory) + ADR-0080 (byte-stable wording).
10. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — execution-style + worktree + push + subagents-no-push + the network-filter constraints (typeurl-extensions, onnewconnection-halts) + the differential dispatch/asserter constraints + per-task gofmt/lint — all carry forward to the 27 IMPL.
