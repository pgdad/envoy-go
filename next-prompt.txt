=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-RUN MECHANICALLY 2026-07-24 at the phase-74 PLAN close, TWICE (stage worktree AND landed master after the squash-push). It does NOT fire. `stop` was NOT created.** ACTUAL output, recorded rather than predicted:
- **(1) prints `NOT DONE: row 74`.** Row 74 stays `in-progress` — a PLAN never flips a row. It flips `done` only at the phase-74 IMPL six-gate. ⚠️ **Check (1) will go SILENT at that close** — row 74 is the ONLY non-`done` chartered row, CONFIRMED BY EXECUTION at this PLAN (a verifier flipped it in a scratch copy and check (1) printed nothing). Checks (2) and (3) still print, so the sentinel still does NOT fire.
- **(2) prints THREE live "candidates:" sentences** — HTTP/3 (`ROADMAP.md:184`), xDS (`:194`), Observability (`:204`). **Cite the command, never the adjective** (`reference_sentinel_deferred_sentence_live_vs_historical`): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` => **3**. ⚠️ A naive `grep -c 'candidates:'` returns **11** and would mislead. **ROADMAP and DECISIONS are BYTE-UNTOUCHED at this PLAN.**
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** => THREE families never opened. (The `WASM` line remains a ROADMAP **bookkeeping artifact** — `internal/wasm/` is a substantially-built wazero host and rows 25/25.1/25.2/25.3 are all `done` — deliberately LEFT AS-IS.)

All three checks print => the sentinel does NOT fire. Abundant work remains.

⚠️ **CHECK (1) HAS A KNOWN BLIND SPOT — RE-DERIVED AT THIS PLAN, AND THE FIGURE MOVED.** At this tip: **104 table rows, 102 matched.** The two it cannot see are `| 00 | bootstrap | — | done |` (the `[0-9.,  ]*` field does not match an **em-dash** "after" column) and `| 04 | http-1.1 | 03 | done |` (the `[a-z0-9-]+` slug field does not match the **DOT**). Both are `done`, so there is no current impact — but check (1) is **not exhaustive**, and any future dotted slug, em-dash cell or letter-suffixed row number goes invisible too. ⚠️ **The phase-73 close recorded FOUR misses out of 105 (including `28.1a`/`28.1b`); the figure moves with the table and must be RE-DERIVED, never copied.** This matters more at the next stage than ever before, because check (1) goes silent there.

==============================================


## STATUS AS OF 2026-07-24: THE PHASE-74 PLAN IS COMPLETE AND LANDED. ROW 74 IS `in-progress`. THIS SESSION PERFORMS THE PHASE-74 IMPL — THE FINAL STAGE OF THE ROW.

**What the phase-74 PLAN landed** (`git log --grep 'phase 74'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): **exactly TWO files in the phase-directory delta** — the new `PLAN.md` (a 9-task TDD spine, T1-T9) and the new `PROGRESS.md` (the live task ledger). **ZERO production `.go`. ROADMAP and DECISIONS BYTE-UNTOUCHED. Row 74 STAYS `in-progress`.** The stage ran **SIX agents**: four read-only re-derivation agents on disjoint remits, plus **two adversarial verifiers in PRIVATE scratch — V1 building T1-T4 BY EXECUTION and running their breaks, V2 re-running ~120 anchors.**

### What matters for the NEXT session, in order

1. **⚠️ THE SENTINEL DID NOT FIRE AND `stop` WAS NOT CREATED.** Re-run all three checks yourself and record the ACTUAL output; do not predict it. **⚠️ At THIS session's close, check (1) goes SILENT** (row 74 is the last non-`done` row) — that is EXPECTED and does **not** mean the sentinel fires, because checks (2) and (3) still print. **Create `stop` if and ONLY if ALL THREE print nothing.**

2. **⚠️ THE GATE IS `rt.tlsMode` ALONE — NO KIND CHECK. This is the single instruction most likely to be got wrong.** `SPEC.md:1` (*"TLS-listener-gated and **TCP-only**"*), `:315` (*"§3.4 **gates QUIC out**"*) and `:320` (*"`rt.tlsMode && rt.kind != kindQUIC`"*) still carry the SPEC's **OWN REFUTED first reading**, contradicting its authoritative §3.4/§16 and **ADR-0296 §Context ¶8(ii)** (`DECISIONS.md:17274`: *"QUIC is SHARED PARITY, not a departure … The gate is `rt.tlsMode` alone"*). **A PLAN that had copied SPEC §10 item 2 verbatim would have implemented the wrong gate AND made Break D unfireable — there would be nothing left to ADD.** ⚠️ **A FOURTH stale site sits OUTSIDE `SPEC.md`, at `ROADMAP.md:136`** — the very cell T9 opens — still calling uncounted QUIC handshakes *"a DEPARTURE that must be written down"*. `STATE.md` is CLEAN. **Read `PLAN.md` §1.1 RD-QUICSTALE before touching `registerListenerMetrics`.**

3. **⚠️ THE IMPL INHERITS A PARTIAL BUILD — USE IT, AND KNOW ITS EDGE.** V1 built **T1-T4** in private scratch from the PLAN's skeletons pasted verbatim and ran every one of their breaks; that evidence is recorded in `PROGRESS.md` §What V1 CONFIRMED and does **not** need re-deriving (the live no-cert string at BOTH TLS versions, `errors.As` as a total discriminator, the exact three-name registration, plaintext-registers-zero, the charset guard, Break D firing assertion (1), Break E's QUIC half NOT firing, Break B firing exactly two of three, Break C's crash stack, "+0 production imports", `flattenToProm`'s output). **But T5-T9 were NEVER executed, the `0111` `AssertStats` has still never been written or run, Breaks G/G'/H are untested, nothing reference-side was executed at the PLAN, and the T2 rename was never exercised in `manager_test.go` in place.**

4. **⚠️ FIVE EXECUTION TRAPS THE PLAN PAID FOR — do not rediscover them.** **(a) `net.Pipe()` DEADLOCKS** in the no-cert arm and does so SILENTLY (a 45s panic timeout with no failing assertion) — use a **loopback TCP pair**. **(b) Break F must scope BOTH live no-cert arms** (`TestClassifyHandshakeErr` AND `..._TLS12`), or the break declared *"must not fire"* fires and raises a false stage-stop; and after the swap, `liveNoCertErr` is unused and the build fails `vet: declared and not used`. **(c) `TestListenerMetrics_GateMatchesInc` must assert the three COUNTER POINTERS** — its build-time predicates stayed GREEN under Break E. **(d) Break C is a PROCESS CRASH**, not an assertion failure: a nil `*stats.Counter`.Inc in the `serveConnection` goroutine with **no `recover()` anywhere in `internal/listener/`** — confirm the stack names `stats.(*Counter).Inc` under `serveConnection`. **(e) Break H must NOT simply delete `sendForced`** — `mtlsEcho` has no `side` param (so the cross-side leg could never fire) and deleting it strips the **trusted** arm ⇒ `structuralCheck` fails ⇒ the runner `t.Fatalf`s in the DRIVE and step 10 never runs. Use a new `sendPolite` mode on ONE side.

5. **⚠️ THREE SEVERE FINDINGS THE SPEC DID NOT CARRY.** **F1** — `internal/listener/`'s test corpus has **NO mTLS PKI**: a CA *certificate* (`testCAPEM`) but **no CA private key** and **no client certificate**, and **zero** `trusted_ca` hits in any `*_test.go` ⇒ a NEW runtime PKI generator is required (clone `internal/tls/config_test.go:42-80` / `0111/driver.go:193-260`). ⚠️ **Name it `handshakeTestPKI`, not `testPKI`** — `manager_test.go:559` already carries a doc comment for a DIFFERENT `testPKI`. **F2** — `mkDownstreamTSRequireClientCert` (`manager_test.go:644`) is **NOT reusable**: no `trusted_ca`, so it hits the phase-67 boot-reject; its own doc says *"should be rejected at build time"*. **F3** — the nil-Counter crash above, which is also why the registration gate and the Inc guard being equivalent (by the `:516-525` mixed-chain reject) earns its own named test. ⚠️ **Test-side imports DO grow** (`crypto/ecdsa`, `crypto/elliptic`, `crypto/rand`, `crypto/x509/pkix`, `encoding/pem`, `math/big`, **`sort`**, **`reflect`**, driver `net/http`/`strconv`) — **"+0 imports" is a PRODUCTION claim and it survives; audit the two categories SEPARATELY at T8.**

6. **⚠️ TWO GREPS THAT LIE, AND ONE GATE THAT CANNOT PASS.** **(a) RD-GREP0** — the SPEC's `grep -c 'VerifyPeerCertificate\|handshake-error callback' docs/envoy-go/DECISIONS.md` => **0** is now **1**; the sole hit is `DECISIONS.md:17264`, the ADR-0296 paragraph that *asserts the count is 0*. **The SPEC's own §Context append made its own grep self-refuting. Do NOT read 1 as drift** — scope it with `awk 'NR>=16888 && NR<16930'`, and fix ¶3's wording at T9. **(b)** A naive `counterValue` collision grep returns **120** repo-wide but **0 in `internal/listener/`** — scope it. **(c)** T7 mandates a ledger line reading `Phase 74 — **1201** → 1204`, so a `grep '1201' => 0` gate is unsatisfiable; the PLAN's corrected gates expect exactly **1**.

7. **⚠️ THE DOCS EDITS ARE BIGGER THAN THEY LOOK.** **B7** — `1201` → **1204** at exactly TWO lines (`:831`, `:847`); the **three NARRATIVE** stale `1200`s at `:1429`/`:1463`/`:1495`; **DO NOT touch the other TEN `\b1200\b` hits, which are historically correct** (including the two LEDGER lines `:4986`/`:4988`); ADD a ledger line after `:4988` and **RECORD the missing `1200 → 1201` step as an unattributed gap — do not invent it.** **B4** must be written as **SHARED PARITY, not a departure.** **B1** must cite `registry.go:117` (not `:107`) and name **FOUR** surviving families (`ciphers`/`versions`/**`curves`**/**`sigalgs`**). **The ROADMAP narrow is on `:204`** (NOT `:202` — that is the 92-char family descriptor), must **ADD `curves`/`sigalgs`**, and must **REPLACE THE PROSE**: after deleting three names the clause still says *"envoy-go emits ZERO such stats"* (FALSE) and *"a framework-surgery row, NOT an inline add"* (the very adjective this row refutes). **Row `:136` carries FOUR stale claims, not one.** **`ssl.no_certificate`** — which SPEC §13.1 asserts is recorded in ADR-0296 §Context — is in **NEITHER** the ADR nor any B-step; record it at T9.

8. **The live hazards — never reflex-classify any of these as a regression.** The PRE-EXISTING `internal/cluster` `-race` flake (`TestOutlierDetector_ConcurrentEjectExactlyOnce`, `internal/cluster/outlier_test.go:766` — **isolate-re-run, do NOT re-classify**) · the full-suite startup flake (`subject ready: EOF` on an UNRELATED fixture) · the SDS init-fetch dial-budget flake · and TWO still-**UNINDEXED** load flakes, isolate-green and NOT root-caused: `internal/httpclient TestOptions_ZeroValue_NoOpDefaults` and `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery`.

9. **The deferred roster.** Unchanged from the SPEC close, and **the IMPL narrows the Observability sentence by exactly the three landed names while ADDING `curves`/`sigalgs`** — check (2) must **STAY 3**. Newly surfaced and NOT chartered: **`ssl.connection_error`** (the row's one new DEPARTURE and the cheapest identified follow-on) · the other ELEVEN fixed reference `ssl.*` names incl. **`ssl.no_certificate`** · **`Listener.stat_prefix`** (accepted-and-silently-ignored) · the IPv6-bracket and dot-normalization scope divergences (PRE-EXISTING, affecting `downstream_cx_total` today) · the stat-surface ledger HOLE · the reference's whole QUIC accounting surface. **Carried unchanged:** the `upstream_cluster` span tag (the RUNNER-UP — deferred on a RESIDUAL DIVERGENCE, not on cost) · pick-time `picked`-propagation · `access_log[].filter` · `hcm.merge_slashes` · `hcm.access_log_options` · the `stdout`/`stderr` loggers · `stats_flush_on_admin` · the swallowed-panic BOOT HANG · route-level header manipulation · `prefix_rewrite`/`host_rewrite_*` · HCM `server_name`/`via` · the default-config header injection divergence · all tracing remainders · all xDS · all HTTP/3 · and the never-opened families (**WASM is a bookkeeping artifact**).

**Counts at exit (re-run MECHANICALLY in the worktree; never copy them):** fixtures **119** (`ls -d test/fixtures/[0-9]*/ | wc -l`) / fuzzers **55** (`grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`) / stat surface **1201** (BEHAVIOR_CONTRACT doc count; NO mechanical command) / BackendKind tail **38** (a TAIL VALUE, not a count — the file declares 39 constants, 0-38) / go.mod modules **2** (the phase-61.2 lineage figure; the single `go.mod` requires **67**) / DECISIONS tail **ADR-0296** PROPOSED (next-free **ADR-0297**) / ZERO new packages. Phase-74 ANTICIPATIONS, **NOT YET LANDED**: **+3 stats (1201 → 1204)** · +0 fixtures (**119**) · +0 fuzzers (**55**) · +0 BackendKinds (**38**) · +0 modules (**2**) · **+0 PRODUCTION imports** · ZERO new packages · **ZERO new exported symbols**.

## What THIS session does — the phase-74 IMPL

One stage per session, and this is the LAST for row 74. **ROW 74 FLIPS `in-progress` → `done`** at the T8/T9 six-gate (ADR-0106, the SOLE leg — a SINGLE FLAT ROW, `reference_roadmap_split_phase_row_done`).

**The owed roster — execute `PLAN.md`'s T1-T9 in order, red-first → green → break-verified per task:**
1. **T1** the classifier · **T2** the three fields + the `rt.tlsMode` gate + the NAME-SET guard · **T3** the QUIC registration test · **T4** the two Inc points + three increment tests · **T5** `helpText` ×3 · **T6** the `0111` `StatsAsserter` + the FOUR boundary retirements + the RD3 revision · **T7** BEHAVIOR_CONTRACT B1-B8 · **T8** the six-gate · **T9** ADR-0296 + the ADR-0286 C3 correction + ROADMAP + close.
2. **TWELVE breaks** — the SPEC's **A-G** plus the PLAN-added **C′/E′/F′/G′/H**. Run with `-count=1` **AFTER committing** (`reference_differential_break_protocol_count1`, `reference_break_protocol_commit_first`) and **confirm WHICH assertion fired** (`reference_deliberate_break_wrong_assertion`) — including for PANICS. **A break that does NOT fire is a FINDING; record it, do not route around it.** **Break F is the one that MUST NOT fire.**
3. **The registration guard asserts a NAME SET, not a cardinality** — the landed precedent (`internal/statssink/registration_test.go:26`, one of FIVE in that one file) counts via `reg.Walk` and **never inspects `m.Name()`**, so a count-only inversion would PASS with all three names misspelled.
4. **The `0111` asserter:** `/stats/prometheus`, **Shape A**, keyed on the metric NAME with `envoy_listener_address` IGNORED. `var _ fixture.StatsAsserter = (*edfDriver)(nil)` is **MANDATORY** (dispatch is a SILENT type assertion — compiler, `go vet` AND `golangci-lint` are all silent). **The ABSENT check must be SEPARATE from the value check and must `continue`** (the `0055` shape, NOT `0005`'s zero-defaulting snapshot) — a counter that fails to register reads `0 == 0` and passes **VACUOUSLY**. Full selector: `go test ./test/differential/ -count=1 -run 'TestDifferential/0111-tls-cvc-empty-dynamic-fallback' -v`.
5. **ADR-0296 COMPLETES IN PLACE** — append `### Decision` + `### Consequences` after the **RETAINED** `:17280` footer, mirroring **ADR-0295** (`:17229`→`:17231`→`:17243`), **NOT** ADR-0286; `### Decision` inside the block **0 before / 1 after**; flip `:17256` PROPOSED → COMPLETE; no renumber; tail stays ADR-0296, next-free ADR-0297. **Fix its TWO internal defects** (the ¶6→¶4(i) mis-pointer; ¶3's self-refuting grep).
6. **The ADR-0286 C3 correction** lands as an **INDENTED blockquote** in the `DECISIONS.md:16901` form (**exactly two literal spaces, then `> [`**) — ⚠️ **the discriminator is the ADR FAMILY, not the phase gap**: `:17209` is a later-phase-corrects-earlier-ADR case that uses the INLINE form. Phrase it as a documentary finding, **NOT** as criticism of phase 65, whose own text was accurate.
7. **RE-DERIVE every anchor at the IMPL tip.** A PLAN is not evidence and a SPEC is not evidence (`feedback_brief_citations_not_evidence`, `reference_quoting_is_not_executing`); re-derive PROSE claims too. `reference_parallel_stream_mints_fresh_drift`: re-run `git diff --stat <spec-tip> HEAD -- '*.go' go.mod go.sum test/` at the IMPL tip.
8. **Close by re-running all three sentinel checks mechanically, TWICE** — in the stage worktree AND on LANDED master after the squash-push (the phase-72/73/74 precedent: a stage worktree can disagree with what actually landed, and the post-push re-run is the one that counts). ⚠️ **Check (1) goes SILENT here.** **Create `stop` if and ONLY if all three print nothing** — they will not, because (2) prints 3 and (3) prints three never-opened families.
9. **Memory writes owed** (PLAN T9 Step 6), each only if it survives the "is this derivable from the repo?" test: the nil-`*stats.Counter` + goroutine + no-`recover()` crash mode · the `Pipeline.Run` single-return containment of the handshake-ctx hazard · and the "an ADR §Context append can make its own grep self-refuting" pattern.

**Stage-close mechanics (an IMPL):** the phase-directory delta is `PROGRESS.md`'s §IMPL record ONLY (no new phase file). STATE §Current rolled **IN PLACE** (lifecycle 3 → DONE; §Recent re-capped at FIVE **with its PREAMBLE updated** — the ADR-0288 rule). ⚠️ **The ADR-0288 singleton greps return 2, not 1**, for each of the three field-name tokens — the second hit is `STATE.md:7`, the RULE STATEMENT itself. Verify with `grep -n`, and **never "fix" the count to 1**; that would delete the rule. `next-prompt.txt` rolled to the phase-75 BRAINSTORM (the roller SELF-PICKS per the 2026-07-12 standing directive) — unless the sentinel fires, which it will not.

Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`).

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. At the phase-72 close that put a stray commit on `master` in the MAIN checkout AND made a "final gates" run validate master's pre-row tree — GREEN while proving nothing. **Use `git -C <abs-worktree-path>` for every git command, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline) + §Project counts + the §Recent lineage (five bullets: phase-74 PLAN + phase-74 SPEC + phase-74 BRAINSTORM + phase-73 IMPL + phase-73 PLAN).
2. `docs/envoy-go/phases/74-tls-handshake-outcome-stats/PLAN.md` — **THE SPINE. Read it BEFORE the SPEC.** §1.1 (the RD-* ledger — **TWELVE SPEC claims REFUTED**) · §1.2 (**F1-F10, three SEVERE**) · §1.3 (what was and was NOT verified) · **§1.4 (the adversarial-pass record — the SIX findings that changed instructions)** · §Global Constraints · §Break protocol · §Identifier roster · §File structure · **T1-T9** · §Self-review (the deviations from the SPEC's spine, each justified).
3. `docs/envoy-go/phases/74-tls-handshake-outcome-stats/PROGRESS.md` — the task ledger to FILL IN, plus **§What V1 CONFIRMED by execution** (evidence you do not need to re-derive) and **§NOT verified** (what you cannot inherit).
4. `docs/envoy-go/phases/74-tls-handshake-outcome-stats/SPEC.md` — the row's source of truth for REASONING, but ⚠️ **read it THROUGH the PLAN's §1.1 ledger: twelve of its claims are refuted, and `:1`/`:315`/`:320` contradict its own §3.4.** §3 (the D-TLSHS-* docket) · §7 · §8 · §9 · §10 · §11 · §12 · §14 · **§16 (the adversarial record — including the SPEC's reversal of its OWN QUIC decision)**.
5. `docs/envoy-go/ROADMAP.md` — the §Schema invariant (`:21`), the THREE live `candidates:` sentences (`:184`, `:194`, `:204`), and the row-74 cell (`:136` — **FOUR stale claims**). **Row 74 is `in-progress`; every other chartered row is `done`.**
6. `docs/envoy-go/DECISIONS.md` — **ADR-0296** (`:17254-:17280`, PROPOSED, §Context only — the shape the IMPL appends into) · **ADR-0295** (`:17211`+, the COMPLETED template to mirror) · **ADR-0286** §Context `:16890` / §Consequences **C3** `:16908` (the boundary this row retires) and the phase-67 `[CORRECTED …]` blockquote at `:16901` (the FORM to copy) · ADR-0045 · ADR-0106 · ADR-0117 · ADR-0288. ⚠️ **ADR-0044 does NOT contain the §Context-draft discipline the project cites it for** — use "ADR-0044-as-used".
7. `docs/envoy-go/BEHAVIOR_CONTRACT.md:916` / `:918` / `:928` / `:962` / `:831` / `:847` / `:4938` / `:4988` / `:4990` — the B1-B8 targets and the stat-surface ledger.
8. `test/fixtures/0111-tls-cvc-empty-dynamic-fallback/` — `README.md` (`:100-110`, `:162-165`), `driver/driver.go` (`:72-82` RD3, `:317`, `:340`, `:393-445`, `:494`, `:520`, `:615-616`), `envoy.yaml:24-25`, `envoy-go.yaml:70`, `expectations.yaml:109-119`/`:133-153`/`:177-184`/`:189`. Precedents: `0055/driver.go:655-669` (the ABSENT+`continue` shape) and `:795-824`, `0005/driver.go:427`/`:537-541`, `0097/driver.go:687-690`.
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the full operative list is in `PLAN.md` §Self-review. Load-bearing here: `reference_probe_must_discriminate`, `reference_go_client_cert_withholding`, `reference_dynamic_stat_name_charset_guard`, `reference_retention_field_populate_before_value_copy`, `reference_differential_asserter_dispatch`, `reference_panic_counter_differential_delta_assertion`, `reference_listener_stat_scope_cross_side_divergence`, `reference_fixture_tb_has_no_logf`, `reference_fatalf_makes_assertions_unreachable`, `reference_differential_run_selector`, `reference_differential_break_protocol_count1`, `reference_break_protocol_commit_first`, `reference_deliberate_break_wrong_assertion`, `reference_plan_break_instructions_dont_compile`, `reference_plan_schedules_edits_to_a_byte_gated_file`, `reference_a_drift_correction_is_itself_a_claim`, `feedback_brief_citations_not_evidence`, `reference_quoting_is_not_executing`, `reference_parallel_stream_mints_fresh_drift`, `reference_parallel_subagents_private_scratch`, `reference_cluster_race_outlier_flake`, and **`reference_bash_cwd_reset_commits_to_main`**.
