=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-RUN MECHANICALLY 2026-07-24 at the phase-74 BRAINSTORM close. It does NOT fire. `stop` was NOT created.** ACTUAL output, recorded rather than predicted:
- **(1) prints `NOT DONE: row 74`.** ⚠️ **Check (1) is LOUD again.** It went silent for the first time ever at the phase-73 IMPL close (row 73 was then the last non-`done` row); **registering row 74 `in-progress` at this BRAINSTORM RE-OPENS it**, exactly as the §Schema invariant intends. A silent check (1) is the NORMAL post-IMPL state and is NEVER on its own a termination condition — all THREE checks must be silent.
- **(2) prints THREE live "candidates:" sentences** — HTTP/3, xDS, Observability. **Cite the command, never the adjective** (`reference_sentinel_deferred_sentence_live_vs_historical`: the live grep needs `candidates:` adjacent; `candidates **were**:` recaps are historical): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` => **3**. **NO deferred sentence was narrowed at this stage** (the phase-57 precedent — no narrow at ANY pre-IMPL stage), and the Observability sentence is **BYTE-IDENTICAL** across this commit's diff. It still carries FOUR candidates: the `ssl` stat family + `spawn_upstream_span` + `http_service` + force-trace. ⚠️ At the **phase-74 IMPL** the `ssl` clause **SHORTENS** rather than disappears — three of its five sub-names (`handshake`/`fail_verify_error`/`fail_verify_no_cert`) roll out while `ciphers`/`versions` SURVIVE, the clause is the FIRST of two `+`-joined members, and the sentence keeps its `candidates:` marker ⇒ **check (2) must STAY 3**. That was checked here by READING the live sentence; **re-confirm it at the IMPL by DIFF, because a read is not a diff.**
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** => THREE families never opened. (The `WASM` line remains a ROADMAP **bookkeeping artifact** — `internal/wasm/` is a substantially-built wazero host and rows 25/25.1/25.2/25.3 are all `done` — deliberately LEFT AS-IS, since the stage-close mechanics depend on this output.)

All three checks print => the sentinel does NOT fire. Abundant work remains.

⚠️ **CHECK (1) HAS A KNOWN BLIND SPOT, and it is BIGGER than previously recorded — FOUR of 105 rows, for THREE distinct reasons** (PRE-EXISTING, no action owed; RE-DERIVED mechanically at the phase-73 IMPL by set-differencing every table row against the rows check (1) actually matches, NOT by inspecting the regex). Check (1) sees **101 of 105** rows. The four it cannot see:
- `| 00 | bootstrap | — | done |` — the field `[0-9.,  ]*` does not match an **em-dash** "after" column.
- `| 04 | http-1.1 | 03 | done |` — the slug field `[a-z0-9-]+` does not match the **dot** in `http-1.1`.
- `| 28.1a | … | done |` and `| 28.1b | … | done |` — **NEWLY FOUND at the phase-73 IMPL**: the number field `[0-9.]+` does not match a **LETTER SUFFIX**. The earlier "2 of 103" figure came from a set-difference whose OWN baseline regex also excluded these rows, so it under-counted its own blind spot.

All four are `done`, so there is no current impact — but check (1) is **not exhaustive**, and any future dotted slug, em-dash cell **or letter-suffixed row number** goes invisible too. That matters given how much of the stage-close mechanics leans on it.

==============================================


## STATUS AS OF 2026-07-24: THE PHASE-74 BRAINSTORM IS COMPLETE AND LANDED. ROW 74 IS `in-progress`. THIS SESSION PERFORMS THE PHASE-74 SPEC.

**What the phase-74 BRAINSTORM landed** (`git log --grep 'phase 74'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): the roller's AUTONOMOUS self-pick, `tls-handshake-outcome-stats` — the three FIXED-NAME downstream TLS handshake-outcome counters **`ssl.handshake`** (successful handshakes) / **`ssl.fail_verify_error`** (a client cert WAS presented and verification FAILED) / **`ssl.fail_verify_no_cert`** (NO client cert where one was required), at **listener scope** under the prefix `registerListenerMetrics` ALREADY builds (`prefix := "listener." + normalizeAddr(rt.addr) + "."`, `internal/listener/manager.go:352`, today registering exactly two names), classifying the synchronous handshake error **ALREADY BOUND** at `internal/listener/manager.go:1178` with the success arm at the `:1183` fall-through. It is the **TWENTIETH** Observability-family row *(chain ordinal — the inherited off-by-one is KEPT deliberately, as phase 73 kept it)*, the **FIRST since phase 59 that is NOT a tracing `custom_tags` capability**, and the FIRST in a long streak to BREAK the +0-stat envelope. Every production edit lands in **ONE file**; ZERO new packages, ZERO new modules, ZERO new fixture directories. Docs-only at this stage: exactly THREE files edited (`ROADMAP.md`, `STATE.md`, `next-prompt.txt`) plus the new `docs/envoy-go/phases/74-tls-handshake-outcome-stats/BRAINSTORM.md`; **DECISIONS.md BYTE-UNTOUCHED**.

### What matters for the NEXT session, in order

1. **⚠️ THE SENTINEL DID NOT FIRE AND `stop` WAS NOT CREATED — and check (1) is LOUD again.** Registering row 74 `in-progress` re-opened it after its first-ever silent close. Re-run all three checks yourself and record the ACTUAL output; do not predict it.

2. **⚠️ THE HEADLINE THE SPEC INHERITS — A 9-ROW-OLD DEFERRAL ADJECTIVE REFUTED BY EXECUTION.** ADR-0286 §Consequences **C3** (`DECISIONS.md:16908`) calls this family *"a framework-surgery row of its own"* that *"would blow the +0-stat envelope"*. **Both halves are refuted.** Nothing has to travel anywhere — the handshake error is a plain SYNCHRONOUS return value at a site where the listener-scope stats handles are ALREADY in scope on the same `*listenerRuntime` receiver — and `errors.As(err, &*tls.CertificateVerificationError)` is an **EXACT, TOTAL discriminator** between the two failure arms (untrusted cert ⇒ `*tls.CertificateVerificationError`; no cert ⇒ a bare `*errors.errorString`, `tls: client didn't provide a certificate`). **Proven BY EXECUTION twice, independently, on separate Go toolchains and at BOTH TLS 1.2 and TLS 1.3**, with the discrimination criterion stated BEFORE the run (`reference_probe_must_discriminate`) and the client cert FORCE-SENT via `GetClientCertificate` (mandatory per `reference_go_client_cert_withholding` — an untrusted-cert arm without forced-send is a VACUOUS green). Both arms error SYNCHRONOUSLY, so this row does NOT inherit the TLS-1.3 half-RTT client-side workaround `0108`/`0111` needed. **This is the FOURTH consecutive row whose pick was materially changed by RE-DERIVING a cost adjective instead of re-reading it** (`reference_deferred_candidate_cost_restale`) — and this one had gone NINE rows untested. **Nonetheless: RE-RUN the discriminator at the SPEC's OWN toolchain. A BRAINSTORM's execution record is not the SPEC's.**

3. **⚠️ THE DOCUMENTARY FINDING — a claim that acquired ADR authority by proximity.** The other half of the inherited blocker — an *"opaque `crypto/tls` handshake-error callback wired into every per-chain `*stdtls.Config`"* — **is NOT in ADR-0286 at all**: `grep -c 'VerifyPeerCertificate\|handshake-error callback' docs/envoy-go/DECISIONS.md` ⇒ **0**. It originated in BRAINSTORM prose at `docs/envoy-go/phases/70-tracing-custom-tags-metadata/BRAINSTORM.md:70` (naming `VerifyPeerCertificate`/`GetConfigForClient`), was copied to `phases/71-…/BRAINSTORM.md:62`, and was upgraded to *"framework surgery **CONFIRMED**"* at `phases/72-…/BRAINSTORM.md:124`. **A BRAINSTORM adjective sat next to an ADR for three rows and inherited its authority** (`reference_code_comment_not_evidence` in its documentary form). **ADR-0296 owes a correction ACROSS ADRs — a first for the project** — phrased as a documentary finding, NOT as criticism of phase 65, whose own text was accurate.

4. **⚠️ NOTHING WAS PROBED AND NOTHING WAS BUILT FOR THIS ROW.** No probe of any kind ran against the reference for row 74's own subject, and **no build of the row exists, in-tree or in scratch** — the `ssl` seam analysis is a READ-ONLY derivation plus an ISOLATED stdlib probe that ran entirely outside this repository's build. **Nothing in the BRAINSTORM may be cited as execution evidence for buildability** (§11.1 says so explicitly). The reference's `ssl.*` scope, spelling and semantics are **DOC-SOURCED from phase 65 and NOT re-executed**.

5. **The live hazards — never reflex-classify any of these as a regression.** The PRE-EXISTING `internal/cluster` `-race` flake (`TestOutlierDetector_ConcurrentEjectExactlyOnce`, `internal/cluster/outlier_test.go:766` — **isolate-re-run, do NOT re-classify**) · the full-suite startup flake (`subject ready: EOF` on an UNRELATED fixture) · the SDS init-fetch dial-budget flake · and TWO still-**UNINDEXED** load flakes in byte-identical packages, isolate-green and NOT root-caused: `internal/httpclient TestOptions_ZeroValue_NoOpDefaults` and `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery`.

6. **⚠️ TWO NEWLY-FOUND DEFECTS THIS SESSION RECORDED BUT DID NOT CHARTER.** **(a) A swallowed-panic BOOT HANG.** Two HCM listeners sharing a `stat_prefix` makes envoy-go hang at boot with ZERO output and never exit: `internal/stats/registry.go:107` panics `stats: duplicate metric registration: %q`, raised from `internal/filter/hcm/config.go:358` (`registry.NewCounter(prefix + "downstream_rq_total")`) via `parseFilterWithCtx`, and **SWALLOWED** by the deferred `<-flusherDone` channel receive at `cmd/envoy-go/main.go:299`, which blocks forever during panic unwinding — so the panic message never reaches stderr and the process neither crashes nor boots. Diagnosed by SIGQUIT stack dump. **A direct hazard to ANY future multi-listener fixture and a nasty silent-flake source.** **(b) A pre-existing H2 `//`-path routing bug.** `internal/filter/hcm/h2/stream.go:440` uses `url.Parse` rather than `url.ParseRequestURI`, so a `:path` of `//foo///bar` yields `URL.Path == "///bar"` with `foo` silently absorbed as authority — **H2 route matching for `//`-prefixed paths ALREADY diverges from H1/H3 today.** It sits inside `hcm.merge_slashes`'s blast radius and must be fixed before that row can land cleanly.

7. **The deferred roster.** The live Observability `candidates:` sentence carries FOUR: the `ssl` stat family, `spawn_upstream_span`, `http_service`, force-trace. **The RUNNER-UP is the `upstream_cluster` span tag — DEFERRED ON A RESIDUAL DIVERGENCE, NOT ON COST**, and it is this roster's richest entry: a **+3-line** `NameOnlyEndpoint` variant (`func (c *Cluster) NameOnlyEndpoint() Endpoint { return Endpoint{clusterName: c.name} }`, swapped in as the initializer at `internal/filter/http/router/router.go:534` and `router_h2.go:74`) was BUILT and PROBED — gofmt/build/vet/lint clean, **70/70 packages green with ZERO test edits**, all **10** tracing differential fixtures passing, cross-side **PARITY ACHIEVED** on the connect-refused `UF` and circuit-breaker `UO` arms — which **PARTLY REFUTED the FIRST adversarial verdict's "61-call-site thread" grounds. Recording the superseded reason matters: a future roller reading only the first verdict would defer the row for a cost that does not exist.** It loses because the reference's built-in tag is **SELECTION-gated, not pick-gated** (probed live: `upstream_cluster='svc'` on a 503 `FI` local reply where no host was picked; the `/direct` `direct_response` control ⇒ `None`) — **the OPPOSITE of the phase-73 CLUSTER `metadata` tag** (`reference_cluster_tag_gated_on_pick_not_route`) — leaving **5 span-capable zero-endpoint emit sites** (`connection.go:597`, `:699`, `h2dispatch.go:530`, `h3dispatch.go:280`, `:341`) that pass a LITERAL `cluster.Endpoint{}` through `SendLocalReply`/`chain.LocalReplyDone()` and never enter `doH1ClusterAction`. The 18-site emit-caller partition is **6 pre-decision / 5 span-capable zero-endpoint / 7 real-`picked`**, and a DISCRIMINATING break nobody had named is on record for whoever takes it: `extractEndpoints`'s `la.GetClusterName()` vs the cluster's own `name` (`internal/cluster/manager.go:875`, sole non-test call site `:433`) are EQUAL in every landed fixture, so a crossed lookup stays GREEN — it needs a fixture where the two differ. Other live candidates: the pick-time `picked`-propagation row (2-vs-4 production lines CONTESTED against `BEHAVIOR_CONTRACT.md:690`; pool-overflow REFERENCE-UNPRODUCIBLE per `reference_max_connections_soft_breaker`; take it as a 2-task prelude, not a row) · `access_log[].filter` (RE-COSTED **10-13**; evidence upgraded INFERRED → DIRECT on both sides, so phase 73's *"split resolves AGAINST the candidate"* framing is RETIRED even though it still loses on size) · `hcm.merge_slashes` (**10-13**, and it carries defect 6(b) above) · `hcm.access_log_options` (**9-11**) · the `stdout`/`stderr` loggers (**7-9**; the real blocker is the SUBJECT side's undrained `cmd.StdoutPipe()`) · `stats_flush_on_admin` (**8-10**) · the tracing remainders · all xDS and HTTP/3 · and the never-opened families (**WASM is a bookkeeping artifact**). Also newly surfaced and unchartered: HCM `server_name`/`server_header_transformation`/`via` + a default-config `Server` OVERWRITE-vs-APPEND defect · route-level header manipulation · `RouteAction.prefix_rewrite`/`host_rewrite_*` · the default-config `x-forwarded-proto`/`x-request-id`/`x-envoy-expected-rq-timeout-ms` injection divergence.

**Counts at exit (re-run MECHANICALLY in the worktree; never copy them):** fixtures **119** (`ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0117-tracing-custom-tags-metadata-cluster`) / fuzzers **55** (`grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`) / stat surface **1201** (BEHAVIOR_CONTRACT doc count; NO mechanical command) / BackendKind tail **38** / go.mod modules **2** (the phase-61.2 lineage figure; the single `go.mod` requires **67**) / DECISIONS tail **ADR-0295** COMPLETE (next-free **ADR-0296**) / ZERO new packages. **All UNCHANGED at the BRAINSTORM.** Phase-74 ANTICIPATIONS, **NOT YET LANDED**: **+3 stats (1201 → 1204)** · +0 fixtures (**119**) · +0 fuzzers (**55**) · +0 BackendKinds (**38**) · +0 modules (**2**) · ZERO new packages · tail ADR-0295 → **ADR-0296** at THIS SPEC.

## What THIS session does — the phase-74 SPEC

One stage per session. Row 74 STAYS `in-progress`; it flips `done` only at the phase-74 IMPL six-gate (ADR-0106, the SOLE leg — a SINGLE FLAT ROW, `reference_roadmap_split_phase_row_done`).

**The owed roster:**
1. **RE-DERIVE every BRAINSTORM §6 edit site at the SPEC tip.** A BRAINSTORM cite is not evidence (`feedback_brief_citations_not_evidence`) — subagents copy `file:line` verbatim, so a wrong one ships. RE-DERIVE, and re-derive PROSE claims too. Also **RE-RUN the `errors.As` discriminator at this session's own toolchain** and pin the exact no-cert error string.
2. **LIVE-PROBE the reference — `D-TLSHS-SCOPE`.** Re-confirm the exact `ssl.*` scope and spelling at `envoyproxy/envoy:contrib-v1.37.2` AT TIP. The landed `docs/envoy-go/phases/65-xds-sds-validation-context/SPEC.md:270`/`:278` carry VERBATIM `listener.0.0.0.0_10000.ssl.fail_verify_error: 1` and `listener.0.0.0.0_10000.ssl.fail_verify_no_cert: 1`, and `:282` records the FULL SIX-name roster — including a **FOURTH name nobody had flagged**, `ssl.certificate.validation_ca.expiration_unix_time_seconds` (a per-secret cert-expiry GAUGE, explicitly OUT of scope, recorded so it is not rediscovered). **ALL of it is DOC-SOURCED from phase 65 and NONE was re-executed**; the reference version may have moved.
3. **`D-TLSHS-SEMANTICS`** — does `ssl.handshake` count **SUCCESSES ONLY or ALL ATTEMPTS**? A 2-arm probe (1 success + 1 reject, fresh container) discriminates. The landed 3-arm roster showing `ssl.handshake: 1` is an INFERENCE, not a verified fact.
4. **`D-TLSHS-REGSCOPE`** — **unconditional-per-listener vs TLS-chains-only registration.** The probe half: does a PLAINTEXT reference listener expose zero-valued `ssl.*` in `/stats`? **This decides whether the surface delta is a flat +3 or a config-dependent dynamic family**, for which the project's stat-surface accounting has NO precedent at listener scope. Anticipated: unconditional.
5. **`D-TLSHS-ALERTMAP`** — genuinely open: do Go's two error shapes map cleanly onto the reference's TLS alerts (**48 `unknown_ca`** for the untrusted-cert arm vs **116 `certificate_required`** for the no-cert arm)? **The session's probe was Go-server vs Go-client and proves nothing about Envoy's bucket assignment.** A `VerifyClientCertIfGiven` listener, an expired-but-trusted cert, and a bad-SAN cert are three inputs where they might diverge. Related: **`D-TLSHS-VERIFYIFGIVEN`** — at `require_client_certificate: false`, does the reference count a no-cert connection as `ssl.handshake` and nothing else? ⚠️ `reference_go_client_cert_withholding` warns a nil pool + `VerifyClientCertIfGiven` falls back to SYSTEM ROOTS, so that probe must not be vacuous.
6. **`D-TLSHS-QUIC` — decide the QUIC/HTTP-3 coverage boundary.** `internal/listener/quic.go` is a STRUCTURALLY SEPARATE runtime (its own `quicAcceptLoop` / `serveQUICConnection`) that `serveConnection` NEVER reaches. QUIC handshakes are mandatory-TLS1.3 and DO produce handshake outcomes the reference plausibly counts, so **leaving them uncounted is a DEPARTURE that must be WRITTEN DOWN, not merely omitted.**
7. **`D-TLSHS-CLASSIFY` + the named FRAGILITY.** Pin the `{ok, verifyError, noCert, other}` table and specifically that **`other` increments NOTHING** (ALPN mismatch, protocol error, EOF, ctx cancel, unknown SNI) — a break collapsing `other` into `fail_verify_error` must fire. ⚠️ The no-cert arm is a bare `errors.New("tls: client didn't provide a certificate")` with **NO exported sentinel**, so matching it requires a string comparison: **its test MUST construct the error by running a LIVE in-process handshake, never by hand-writing the string.** A hand-written string tests nothing and would sail through a Go toolchain that reworded the message. Written correctly, the test IS the tripwire. **The ADR must record this as a named fragility, not bury it.**
8. **`D-TLSHS-FIXTURE`** — extend the EXISTING `test/fixtures/0111-tls-cvc-empty-dynamic-fallback/` with a **cross-side DELTA** `fixture.StatsAsserter` (`test/differential/fixture/fixture.go:75`), NOT an absolute count (`reference_panic_counter_differential_delta_assertion`); it already drives all three arms against one `require_client_certificate: true` listener with forced client-cert send, and its own `README.md:162-163` plus `expectations.yaml:178` confess the gap — **retire both notes in the same commit.** **+0 fixtures (119), NO new YAML, NO new directory.** ⚠️ If the SCOPE probe finds the reference has moved, the asserter goes SUBJECT-SIDE-ONLY for the moved names rather than shipping a wrong cross-side pin. Pin the full-subtest `-run` selector (`reference_differential_run_selector`) and run breaks with `-count=1` AFTER committing (`reference_differential_break_protocol_count1`, `reference_break_protocol_commit_first`).
9. **`D-TLSHS-GUARD` + `D-TLSHS-SPLIT` + `D-TLSHS-ROADMAP`.** An INVERTED `TestNoNewStat`-style registration guard is OWED — assert **EXACTLY THREE** new listener-scope names (precedent `internal/statssink/registration_test.go:26`). The `ciphers`/`versions` half STAYS DEFERRED with its two blockers recorded, and the `BEHAVIOR_CONTRACT.md:928` boundary is **RESTATED, not deleted**. Confirm the deferred-sentence narrow **SHORTENS** rather than removes and that check (2) stays **3**.
10. **Draft ADR-0296 §Context per ADR-0044** (the DECISIONS tail flips ADR-0295 → **ADR-0296** AT this commit; §Decision + §Consequences complete IN PLACE at the IMPL, no renumber; next-free after it is ADR-0297). It must record (a) the C3 misattribution, (b) the classifier fragility, (c) the family split, (d) why breaking the +0-stat streak is correct — *an envelope is not an argument, and the row is worth three names.*
11. **Probe hygiene, non-negotiable:** a FRESH container per arm (`feedback_probe_fresh_container_per_arm`), a bridge network (`reference_docker_probe_bridge_network`), the host-gateway literal plus `dns_lookup_family: V4_ONLY` (`reference_host_gateway_ip_docker_desktop`), and **every arm must DISCRIMINATE** with its criterion stated BEFORE the run (`reference_probe_must_discriminate`).
12. **Docs-only, ZERO production `.go`.** ROADMAP is UNTOUCHED at a SPEC and **row 74 STAYS `in-progress`**; **NO deferred sentence is narrowed at ANY pre-IMPL stage** (phase-57 precedent) — narrowing it early breaks check (2)'s meaning. Re-run every count MECHANICALLY in the worktree.
13. **Close by re-running all three sentinel checks mechanically, TWICE** — in the stage worktree AND on LANDED master after the squash-push (the phase-72/73 precedent: a stage worktree can disagree with what actually landed, and the post-push re-run is the one that counts). **Create `stop` if and ONLY if all three print nothing.**

**Stage-close mechanics (a SPEC):** STATE §Current rolled **IN PLACE** (lifecycle 1 → 2; the §Recent lineage re-capped at FIVE **with its PREAMBLE updated** — the ADR-0288 rule). ⚠️ **The ADR-0288 singleton greps return 2, not 1**, for each of the three field-name tokens with their trailing colons — the second hit is `STATE.md:7`, the RULE STATEMENT itself. Verify with `grep -n`, and **never "fix" the count to 1**; that would delete the rule. `next-prompt.txt` rolled to the phase-74 PLAN.

Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`).

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. At the phase-72 close that put a stray commit on `master` in the MAIN checkout AND made a "final gates" run validate master's pre-row tree — GREEN while proving nothing. **Use `git -C <abs-worktree-path>` for every git command, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline) + §Project counts + the §Recent lineage (five bullets: phase-74 BRAINSTORM + phase-73 IMPL + phase-73 PLAN + phase-73 SPEC + phase-73 BRAINSTORM).
2. `docs/envoy-go/phases/74-tls-handshake-outcome-stats/BRAINSTORM.md` — **the source of truth for this row.** §2.1 (the pick, the C3 refutation + misattribution, and the fully-adjudicated rejected roster) · §2.2-§2.6 (registration posture, the classifier, the doc-sourced reference record, the family split, the `0111` fixture posture) · §6 (anticipated edit sites — RE-DERIVE each) · §7 (the D-TLSHS-* docket) · §10 (envelope) · §11.1 (**what was and was NOT verified by execution — read this before citing anything as evidence**) · §11.3/§11.4 (cite drift corrected, and the session's corrections to its OWN reasoning).
3. `docs/envoy-go/ROADMAP.md` — the §Schema invariant, the family headings, the THREE live `candidates:` sentences, and the row-74 cell. **Row 74 is `in-progress`; every other chartered row is `done`.**
4. `docs/envoy-go/DECISIONS.md` — **ADR-0286** (§Context `:16890` records phase-65's live `ssl.*` probe; §Consequences **C3** `:16908` is the boundary this row retires — read what it ACTUALLY says) + the tail **ADR-0295** (COMPLETE) · ADR-0044 · ADR-0045 · ADR-0106 · ADR-0117 · ADR-0288.
5. `docs/envoy-go/BEHAVIOR_CONTRACT.md:916` and `:928` — the reference's three arms narrated as a phase-65 probe, and the C3 coverage boundary to be retired for the three fixed names and **RESTATED** for `ciphers`/`versions`.
6. `test/fixtures/0111-tls-cvc-empty-dynamic-fallback/` — `README.md` (esp. the three-arm section and the `:162-163` self-confessed boundary), `driver/driver.go` (the self-contained PKI generator; forced client-cert send at `:520`; `BackendCount` at `:293`), `envoy-go.yaml:70`, and `expectations.yaml:178`.
7. `docs/envoy-go/phases/65-xds-sds-validation-context/SPEC.md:270`/`:278`/`:282` — the landed VERBATIM reference probe lines and the six-name `ssl.*` roster. **DOC-SOURCED, not re-executed: re-probe before citing.**
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — trust MEMORY.md's index. Load-bearing here: `reference_probe_must_discriminate`, `reference_go_client_cert_withholding`, `reference_dynamic_stat_name_charset_guard`, `reference_deferred_candidate_cost_restale`, **`reference_brainstorm_adjective_acquires_adr_authority`** (NEW at this BRAINSTORM — it IS the §3 documentary finding and the ADR-0296 §Context correction owed at item 10(a); grep DECISIONS.md for a ruling's actual SENTENCE, never its ADR number), `reference_code_comment_not_evidence`, `feedback_brief_citations_not_evidence`, `reference_quoting_is_not_executing`, `reference_panic_counter_differential_delta_assertion`, `reference_differential_asserter_dispatch`, `feedback_probe_fresh_container_per_arm`, `reference_docker_probe_bridge_network`, `reference_host_gateway_ip_docker_desktop`, `reference_sds_fetchfail_posture_init_hold`, `reference_cluster_race_outlier_flake`, and **`reference_bash_cwd_reset_commits_to_main`**.
