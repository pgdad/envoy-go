START a fresh cold-start session for envoy-go — **the phase-35 (`load-balancer-random`) IMPL is DONE and LANDED on master; the next lifecycle step is the next-phase routing → the phase-36 BRAINSTORM** (`superpowers:brainstorming` — the Load-balancing family stays OPEN; 6 policy candidates remain after 35). The substantive master tip is the phase-35 IMPL squash **`4618841`** (`phase 35 IMPL: load-balancer-random — RANDOM the THIRD LB policy; stateless uniform-pick reusing the ADR-0232 seam at zero cost; 0060 anti-skew differential; ADR-0234`; pushed to `origin/master`; confirm via `git log --oneline -3`). It carries `internal/cluster/random.go` (the stateless `randomLB`) + the manager `case clusterv3.Cluster_RANDOM` + the `0060-lb-random` differential fixture + the full ADR-0234 entry + the STATE/ROADMAP advance (row 35 `in-progress → done`). The literal live master tip is the docs-only SHA-fill commit **`3948ed7`** (`next-prompt.txt: SHA-fill the literal live tip — name fa6d693 …`; the substantive IMPL squash `4618841` is trailed by the two docs-only roll-forward commits `fa6d693` then `3948ed7`; confirm via `git log --oneline -3`). Below the IMPL squash sits the phase-35 PLAN squash `1f22dbe`, the phase-35 SPEC squash `bdef11a`, the phase-35 BRAINSTORM squash `c5c6c55`, and the phase-34 IMPL squash `b086137` (the `leastRequest` policy + the ADR-0232 LB acquire/release seam + `newPCGRNG` that phase 35 REUSED).

## What this session does — route to the next phase (the phase-36 BRAINSTORM)

The phase-35 {random} IMPL is COMPLETE and pushed. The next session opens the NEXT Load-balancing-family row via `superpowers:brainstorming` (DESIGN-ONLY; the kafka-31/thrift-33/least_request-34/random-35 12-§ family-template precedent). The lifecycle is BRAINSTORM → SPEC → PLAN → IMPL, each its own session per the established cadence; the next session is the phase-36 **BRAINSTORM**.

The Load-balancing family stays OPEN — **6 policy candidates remain** (ADR-0106 flat family rows; sibling rows NOT pre-populated): **ring_hash, maglev, subset LB, locality-weighted LB, priority load balancing, panic thresholds**. The brainstorm Q0 picks the subject from the roster. The key structural consideration the dialogue must weigh:

- The **ADR-0232 LB acquire/release seam** is the reusable asset. `random` (35) just consummated its FIRST reuse (returning the shared `noopRelease` at zero cost), validating the seam's "non-keyed policies drop in at zero cost" §Consequences claim.
- The **hash policies (ring_hash / maglev)** additionally need the ADR-0232 `Pick()` widened to `Pick(hashInput)` (a hash key threaded from the request) — a seam-EXTENDING change, heavier than random's zero-churn drop-in, and they need a hash-policy config surface + the ring/table build. If the brainstorm picks a hash policy, anticipate a seam-extension ADR (the phase-34 ADR-0232 two-ADR precedent: a seam ADR + a policy ADR).
- **subset LB / locality-weighted LB / priority LB / panic thresholds** are selection-layer/health-layer policies with their own surfaces; weigh which is the next-smallest zero-or-low-churn drop-in vs. which needs framework surgery.

Let the brainstorm dialogue settle Q0 (subject) / Q1 (envelope) / Q-seam (reuse vs. extend ADR-0232) / Q-split (ADR-0045 envelope) — do NOT pre-decide here.

## Counts at master tip (phase 35 IMPL done — confirm at the BRAINSTORM Task-1-style baseline)

- stat surface **1116** (UNCHANGED at phase 35 — the SECOND zero-delta phase; confirm via the repo's stat-surface recipe).
- differential fixtures **62** (tail `0060-lb-random`; `ls -d test/fixtures/[0-9]* | wc -l`). NO boot-reject dir at 35.
- fuzzers **42** (tail `FuzzThriftDecode`; UNCHANGED at 35 — no wire decode).
- BackendKind tail **33** (`TCPThriftResponder`; UNCHANGED at 35 — `0060` reused `TCPEcho` kind-0).
- DECISIONS tail **ADR-0234** (the `random` policy; next-free **ADR-0235**).
- reference pin: `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227).
- Toolchain: Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); go-control-plane `/envoy` v1.32.4 (ADR-0008).
- conformance h2spec 53/53 + proxy-wasm 10/10.

## As-built phase-35 record (the seam-reuse asset the next phase builds on)

- `internal/cluster/random.go` — the stateless `randomLB`: `newRandom`/`newRandomWithRNG` (REUSE `newPCGRNG` verbatim, mirror `newLeastRequestWithRNG` minus `choiceCount`); `Pick()` = one draw `int(rng() % uint64(n))` → `endpoints[i]` + the shared `noopRelease`; empty-set → `errNoEndpoints` parity. D-S35-2: accepts the shared `"cluster: least_request: seed rng"` seed-error message (effectively-unreachable boot path; a recorded cosmetic note).
- `internal/cluster/manager.go` — the bare `case clusterv3.Cluster_RANDOM` (NO config parse — RANDOM has no config message); the reject text now `(supported: ROUND_ROBIN, LEAST_REQUEST, RANDOM)` (ADR-0080); mismatched-oneof under RANDOM silently ignored.
- `test/fixtures/0060-lb-random/` — the SECOND band-based `AssertDistribution` (the contrapositive of `0059`): RANDOM STAYS uniform PER SIDE despite a held-conn load imbalance. The band was re-tuned IMPL-time from the SPEC's `{floor 12, ceiling 32}` (~2.5σ, flaked 2/20) to **`{floor 6, ceiling 40}`** (~4–5σ, 60/60 flake-free) within the pinned PRINCIPLE {conservation + uniform-floor + uniform-ceiling}; the 3 deliberate `-count=1` breaks all still bite. (An auditable amendment to D-S35-3 — recorded in BEHAVIOR_CONTRACT/ADR-0234/PROGRESS.)
- DECISIONS.md ADR-0234 (the `random` policy; NO seam ADR — reuse, the thrift-33 single-ADR-on-reuse precedent).

## Read first (in order)

1. THIS FILE — the post-phase-35-IMPL cold-start (next: the phase-36 BRAINSTORM).
2. `docs/envoy-go/STATE.md` — active-phase `phase 35 (load-balancer-random) done`; lifecycle-state → the next-phase routing; the counts.
3. `docs/envoy-go/ROADMAP.md` — row 35 `done` (flat family row); the Load-balancing family-heading + the 6-candidate roster.
4. `docs/envoy-go/DECISIONS.md` — **ADR-0232** (the LB seam — the reusable asset; its §Consequences "the non-keyed RANDOM drops in at zero cost" now consummated) + ADR-0233 (least_request) + **ADR-0234** (random) + ADR-0024/0045/0044/0052/0080/0106/0060.
5. `internal/cluster/{loadbalancer,leastrequest,random,manager,cluster}.go` — the as-built LB layer (the seam `Pick() (Endpoint, func(), error)` + `noopRelease`; the three policies roundRobin/leastRequest/random; the lb_policy switch + reject text). For a hash policy the BRAINSTORM must scope the `Pick(hashInput)` widening.
6. The phase-34/35 phase directories (`docs/envoy-go/phases/34-load-balancer-least-request/`, `…/35-load-balancer-random/`) — the family-template + the as-built precedents.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_execution_style`, `feedback_git_worktrees`, `feedback_subagents_no_push`, `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_path_targeting`, `reference_differential_break_protocol_count1`, `reference_differential_asserter_dispatch`, `reference_differential_run_selector`, `reference_docker_probe_bridge_network`.

## Honor throughout

- `feedback_execution_style` (subagent-driven) · `feedback_git_worktrees` (work in a worktree) · `feedback_subagents_no_push` (subagents commit LOCAL-ONLY; the controller squash-merges + pushes at stage-close) · `feedback_push_to_origin` (after a clean landing on master with tests green, push without asking) · `feedback_pertask_gofmt_lint` · `feedback_subagent_worktree_path_targeting`.
- ADR-0106 (flat family row per LB policy — NO parent rollup) · ADR-0045 (the split-gate) · ADR-0044 (ADR bodies at the IMPL, in-place) · ADR-0052 (the atomic-landing six-gate) · ADR-0080 (byte-stable parse-reject wording) · ADR-0227 (the contrib reference image; live probes per `reference_docker_probe_bridge_network`).
- The lifecycle is BRAINSTORM → SPEC → PLAN → IMPL, each its own session. The next session is the phase-36 **BRAINSTORM** (DESIGN-ONLY).
