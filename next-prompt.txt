=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, the active phase is fully landed, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action. (The sentinel does NOT apply right now — see below.)

**STATUS AS OF 2026-07-05: phase 51 (`bootstrap-validate-mode`) IMPL is LANDED (row 51 flips `done`). NO phase is currently chartered. This is the "none chartered, awaiting a human pick" shape — NOT the termination sentinel.**

- The phase-51 IMPL landed (subagent-driven, in a worktree, six tasks): extracts the shared `internal/boot.Construct`/`NewTracingProvider` construction-tail (called identically by `cmd/envoy-go/main.go`'s normal boot path AND the new public `validate` package, per AMEND-VALIDATE-DEPGRAPH — `Construct` does NOT build the cluster manager itself), extracts a new `internal/filter/http/builtins.RegisterBuiltins` (no `Deps` struct, unlike its `internal/filter/network/builtins` sibling — AMEND-VALIDATE-HTTPBUILTINS-NO-DEPS), adds the new public `github.com/esalaine/envoy-go/validate` package (`Bootstrap`/`BootstrapFile` — the FIRST non-`internal/`/non-`cmd/`/non-`test/` package in this repo), and adds a `--mode validate` CLI flag on `main.go` (0 valid / 1 invalid / 2 usage error; calls `validate.Bootstrap` directly, not `BootstrapFile`, so `-allow-h2c` composes). **ANCHORS ADR-0268** (§Decision/§Consequences landed IN-PLACE per ADR-0044). No change to WHAT is validated anywhere — every existing strict-reject/parse-arm across `internal/bootstrap`/`internal/cluster`/`internal/listener` is reused byte-for-byte. Counts at IMPL exit: stat surface **1200** (non-H2 **1196**, +0) / fixtures **96** (+0 — no differential surface; the FULL EXISTING 96-fixture suite is the byte-stability regression anchor for the extraction, proven unchanged under `-race`) / fuzzers **52** (unchanged — D-VALIDATE-FUZZER resolved no new fuzzer) / BackendKind **38** (unchanged) / DECISIONS tail **ADR-0268** (next-free **ADR-0269**); **THREE new Go packages** (`internal/boot`, `internal/filter/http/builtins`, `github.com/esalaine/envoy-go/validate`), ZERO new go.mod modules. **ROW 51 (`bootstrap-validate-mode`) FLIPS `done`** (the sole leg, ADR-0106; NO parent rollup). The final six-gate was independently re-run on the frozen HEAD: `go build ./...` OK; `gofmt -l .` empty; `golangci-lint run ./...` clean; `go vet ./...` clean; `go test ./... -count=1` all pass; the full 96-dir differential `-run TestDifferential -race -count=1` all pass; `go mod tidy -diff` EMPTY; fuzzer count 52 confirmed; fixture count 96 confirmed.
- **EVERY ROADMAP row (00 through 51, all sub-phases) is now `done`** — re-verify via `grep -n '^| [0-9]' docs/envoy-go/ROADMAP.md`. **This does NOT mean all work is complete** — the router's long-standing invariant is that "none chartered" ≠ "all work complete." The Operational tooling family STAYS OPEN with several deferred-but-not-yet-chartered candidates (see below) that a human has not yet picked from; several OTHER families (Observability, §9 Network-filters, etc.) also carry their own deferred-but-not-yet-chartered candidates from prior phases. No human has signaled in this session that the project itself is finished. **Do NOT create the `stop` file.** This mirrors the phase-50-IMPL→row-50-done precedent exactly (`3d10670d`) — NOT a termination-sentinel shape (a CORRECTION note is recorded in STATE.md's phase-50 IMPL entry recounting exactly this distinction; the same discipline applies again here).
- STATE.md/ROADMAP.md/BEHAVIOR_CONTRACT.md/DECISIONS.md (ADR-0268) all updated (and pushed by the controller at stage-close). PROGRESS-51.md closed out with the final re-verified six-gate output, `Status: IMPL DONE (6/6)`.

=> The NEXT action is **NOT a new IMPL, NOT the termination sentinel.** A human must pick the next candidate — from the Operational tooling family's own deferred list below, or from another family's deferred list, or an entirely new family — to charter a BRAINSTORM. Until that pick is made, there is no active phase to advance — this router should be re-invoked once a human has chosen, at which point the chosen candidate's BRAINSTORM becomes "the NEXT action."

==============================================

## Deferred, not-yet-chartered Operational-tooling candidates (a human picks ONE to open the next BRAINSTORM, or picks from a different family)

These are the candidates noted since phase 51 opened the Operational tooling family, still not chartered as ROADMAP rows:

- **xDS-sourced dry-validation** — validating a Bootstrap/xDS-delivered config (not just a static YAML file) without booting, extending the `validate` package's reach beyond the CLI's static-file entry point.
- **An admin-API-exposed live-reload-and-validate endpoint** — an HTTP admin-interface route that validates a candidate config against the ALREADY-RUNNING process's own `internal/boot.Construct` seam, without a full process restart.
- **An RTDS/SDS validate companion** — extending the validation surface to runtime-delivered (RTDS) or secret-delivered (SDS) config fragments, not just the static bootstrap file.

None of these is pre-chartered. **Do NOT invent/charter one yourself** — this is a human decision. If a human has NOT picked one when this router is next read, ask which candidate (from this family or any other open family) to charter, then open its BRAINSTORM per the project's standard lifecycle (`superpowers:brainstorming` first).

## Honor throughout (the discipline, unchanged for whichever phase comes next)
- **Lifecycle = one stage per session** (BRAINSTORM → SPEC → PLAN → IMPL).
- **Subagent-driven execution** (`feedback_execution_style`) in a **fresh worktree** (`feedback_git_worktrees`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit, re-runs the full suite on the frozen HEAD, does any deliberate-break verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). Per-task gates: `gofmt -l` + `golangci-lint` + `go vet` + `go build ./...` (`feedback_pertask_gofmt_lint`).
- **Process anchors:** ADR-0106 (per-leg rows) · ADR-0044 (ADR §Decision+§Consequences land at IMPL) · ADR-0045 (sub-split soft gate) · next-free **ADR-0269**.
- Re-verify baselines fresh at cold-start — do NOT trust any count in this file without re-running the baseline commands (`ls -d test/fixtures/*/ | wc -l`, `grep -rh '^func Fuzz' --include='*.go' . | wc -l`, etc.) against current HEAD.

## Read first (in order), once a human has picked a candidate
1. THIS FILE — the cold-start router (none chartered; awaiting a human pick).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 51 IMPL done`) + the full history of prior "none chartered" reopenings (esp. the phase-49→50 and phase-50→51 precedents).
3. `docs/envoy-go/ROADMAP.md` — confirm all rows `done`, read the Operational tooling family note for the deferred-candidate list (and any other open family's own deferred list).
4. `docs/envoy-go/phases/51-bootstrap-validate-mode/` (BRAINSTORM/SPEC/PLAN/PROGRESS-51.md) — the most recent landed phase, useful as a shape template if the next pick is another Operational-tooling candidate.
5. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — re-check for any entries relevant to whichever candidate is picked next.
