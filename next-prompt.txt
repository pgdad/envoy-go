=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **THERE *IS* BANKED MID-LIFECYCLE WORK AT THIS TIP — DO NOT SELF-PICK.** Phase 84 (`grpc-unary-response-trailers`) was **BRAINSTORMED and SPEC'd on 2026-08-06**; **row 84 is `in-progress`** and the phase is OPEN at lifecycle-state **2**. ⚠️ **AND THE SPEC SPLIT THE ROW INTO 84.1 / 84.2.** ⇒ **This session performs `PLAN-84.1.md`** — the seam leg. `PLAN-84.2.md` (the differential fixture) is the session after, and **its IMPL is what flips row 84 `done`.**

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=116 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **CHECK (1) PRINTS — `NOT DONE: row 84` — AND THAT IS CORRECT.** Row 84 goes silent only when the **84.2 IMPL** flips it (the SPEC split the row; **the parent flips `done` when BOTH legs land**, per `reference_roadmap_split_phase_row_done`). ⚠️ **CHECK (3) IS SILENT, AND THAT IS THE STANDING HAZARD.** Its success mode and its "broken loop" mode are indistinguishable, so **the check-(3) doctoring NC is mandatory every session**: doctor the `gRPC-family row` mention on a scratch copy and confirm `NEVER OPENED: gRPC` comes back. **Run the row-doctoring NC below as well** — it is still the cheapest proof that check (1)'s parser works (a `sed` targeting `done      ` does NOT work: it misses the real `| done |` column spacing and prints nothing, which reads exactly like *"the check is blind"*):
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
```
```sh
# the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```

⚠️ **AND WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -oiE '-family row'` fails with `grep: amily row: No such file or directory` and the surrounding arithmetic prints `base=0 now=0 delta=0`, **which reads exactly like "no change".** Observed live at the phase-84 BRAINSTORM **and again at the phase-84 SPEC**. **Always pass `--` before the pattern**, and remember the counts are form-dependent: **95 occurrences vs 67 LINES** at this tip (`grep -c` counts LINES). **A gate that reads zero on BOTH sides is not evidence of invariance.**

⚠️ **`want=116` IS A LIVE FIGURE, NOT A CONSTANT.** ⚠️ **YOUR PLAN ADDS NO ROW, SO IT MUST *NOT* TOUCH `want`** — and it must leave `ROADMAP.md` **BYTE-UNTOUCHED** (row 84 stays `in-progress`; row summaries are BRAINSTORM-authored and are NEVER restated per stage). ⚠️ **THE SPLIT ADDS NO ROW EITHER — THIS IS MEASURED PRECEDENT, NOT AN ASSUMPTION.** Phase 61's splitting SPEC commit `6a26909b` touched **exactly one file** (`SPEC.md`, +274/−0) with `ROADMAP.md` **byte-untouched**, and **zero dotted rows exist above 32.2** (highest are 29.x, 32.1, 32.2). Split legs live in the parent row's **`sub-phases` PROSE** (row 60 does exactly this). ⚠️ **This DIVERGES from `BOOTSTRAP_PROMPT.md` §6.2 steps 3 and 4** — practice has diverged since 32.2; **follow practice and NAME the divergence.**

Check (3) fails SAFE by construction. When you open a new family, add its row-summary slug to the list above.

**SENTINEL RE-RUN MECHANICALLY AT THE PHASE-84 SPEC, WITH SIX FIRING NEGATIVE CONTROLS. IT DOES NOT FIRE. `stop` was NOT created; `ls stop` => `No such file or directory`.** ⚠️ **Re-run it yourself anyway and record what YOU see.** ACTUAL output, recorded rather than predicted:
- **(1) `NOT DONE: row 84`** at `want=116`, denominator printed (`examined 116 data rows`) — **CORRECT AND EXPECTED while the phase is open.**
- **(2) SIX — `:194 :200 :206 :216 :222 :230`.** ⚠️ **Do NOT "fix" it and do NOT forecast a decrease.**
- **(3) SILENT.** **This is the state that makes the check-(3) doctoring NC mandatory.**
- Input measured at **234 lines / 116 data rows** before anything was written, so an empty result could not read as a zero result.
- **ALL SIX NCs FIRED:** row 62 doctored ⇒ `NOT DONE: row 62` **and** `NOT DONE: row 84` (with `NC LANDED? [ in-progress ]` inspected first); `want=115` ⇒ `GATE FAIL: examined 116 data rows, expected 115`; the check-(3) doctoring (residual occurrences confirmed **0** first) ⇒ `NEVER OPENED: gRPC`; an invented slug fires while `gRPC`/`WASM`/`HTTP-filters` correctly do not; check-(2) **one-arm** strip ⇒ **6 -> 5, NOT 6 -> 0**; both arms ⇒ 0.
- ⚠️ **THE ONLY REMAINING SENTINEL BLOCKERS ARE CHECK (1) (row 84, closes at the 84.2 IMPL) AND CHECK (2) (six family backlogs).** Once row 84 lands, **check (2) is the sole thing standing between this project and the termination sentinel.**

⚠️ **THE LEAK CHECK GOES LIVE THE MOMENT YOU WRITE A ROADMAP ROW.** The rule: **NEVER WRITE A SENTINEL'S OWN MATCHER STRING INTO A FILE THE SENTINEL GREPS.** The sentinel greps `docs/envoy-go/ROADMAP.md` — writing these strings *here* (or in `SPEC.md` / `PLAN-*.md` / `DECISIONS.md` / `STATE.md`) is safe; writing them *there* is not. ⚠️ **AND THE TWO CLASSES ARE OPPOSITE.** `<slug>-family row` is check (3)'s **PASS** condition; the deferred-candidate phrases are check (2)'s **FAIL** conditions. **One leak silences a real failure; the other manufactures one.**
⚠️ **AND THE LEAK CHECK MUST BE A BEFORE/AFTER COUNT OVER THE WHOLE FILE, NOT A GREP OF THE DIFF'S `+` LINES.** Baselines measured at the phase-84 SPEC, for you to diff against: check-(2) union **6** · `-family row` **95 occurrences / 67 LINES** · `gRPC-family row` **2** · **234 lines / 116 data rows**.

⚠️ **ROW WELL-FORMEDNESS: THE GATE MUST BE A DISJUNCTION.** **ARM-A (escape-aware, `pieces != 8` on a `|`-split) catches lines 119 and 131 ONLY; ARM-B (escape-aware trailing piece) catches line 140 ONLY.** ⚠️ **Naive `NF!=8` flags SEVENTEEN — 15 FALSE POSITIVES + 2 true — and MISSES line 140** (compensating defects cancel) ⇒ wrong in **both** directions. ⚠️ **Inspect that your NC landed before trusting its result.** ⚠️ **A GATE-AUTHORING TRAP RECORDED RATHER THAN HIDDEN:** the phase-82 PLAN's first ARM-A used the wrong pipe-split threshold (`!=9` instead of `!=8`) and flagged **113 of 114 rows**. **A gate that fires on almost everything reads as THOROUGH and is WORTHLESS. Only the printed denominator exposed it. Print your denominators.**

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (ugrep under the hood). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Reproduced at FOURTEEN separate tips.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS.** ⚠️ **Print FILE NAMES, never a count.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** — git exempts TRACKED files, so it exits 1 and reads as *"not ignored"*; `git check-ignore -v --no-index` prints the truth, and `git status` is the discriminating check. ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **`grep -c` ON ZERO MATCHES IS TOOL-SPECIFIC**: only `git grep -c` prints nothing; GNU and the wrapper print `0`. Both **exit 1**, so an exit-code gate is wrong for either — and `$(cmd || echo ERR)` corrupts the reading (observed live at the phase-84 SPEC). **Capture the value; never chain on the exit code.** ⚠️ **AND `grep -c` COUNTS *LINES*, NOT *OCCURRENCES***. ⚠️ **`\t` IN GNU ERE IS A LITERAL `t`**; use `-P` unconditionally.
⚠️ **NEW AT THE PHASE-84 SPEC — BROKEN-GATE SHAPE 33: A `git grep` PATHSPEC ENDING IN `/` READS A FAIL-UNSAFE ZERO.** The quoted git-pathspec form returns **0 lines / 0 files** where the equivalent shell-glob form returns **70**. Same family as the leading-hyphen flag trap, and it reads exactly like *"the pattern is absent"*.

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`** with FOUR blind spots (an em-dash, a DOT in `http-1.1`, two LETTER-suffixed ids). Every document from phase 75 forward called it *"currently HARMLESS."* ⚠️ **Nobody fired the unsafe arm until the phase-77 PLAN. EXECUTED on scratch copies: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** ⚠️ **DO NOT restore the regex form.**

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

⚠️ **Run the union form, never one half of it.** ⚠️ **A ONE-ARM STRIP IS NOT AN NC**: stripping only `deferred candidates:` moves the union **6 -> 5, not 6 -> 0**, because the long form does not contain the short substring. ⚠️ **The documented trap's stated MECHANISM is refuted while the trap is real** — the inflation comes from ROADMAP table rows carrying BRAINSTORM prose. **Cite the command; do not re-inherit the explanation.**

⚠️ **CHECK (2) HAS NEVER GONE DOWN** across ~40 phases (history `0 -> 1 -> 3 -> 4 -> 5 -> 6`) => **the candidates sentence is a WINDOW onto a larger deferred backlog, not an inventory of it.** ⚠️ **A stage that predicts a decrease is repeating the phase-73 error.**

⚠️ **`:213` IS NOT A SENTENCE ANCHOR.** It is a very long line carrying SEVERAL deferred phrases; only ONE is the colon-terminated form the sentinel matches. Citing *"the `:213` sentence"* without the offset is ambiguous. ⚠️ **And `:227` does not carry the long phrase at all** — SHORT form only.


==============================================


## STATUS AS OF 2026-08-06: PHASE 84 (`grpc-unary-response-trailers`) IS **OPEN** AT LIFECYCLE-STATE **2**. ROW 84 IS `in-progress`. **THE SPEC SPLIT THE ROW.** THIS SESSION WRITES **`PLAN-84.1.md`**.

**What the phase-84 SPEC landed.** Base master **`e1c07208`**, branch `phase-84-spec`, ONE commit, docs-only (**ZERO production `.go`, ZERO test `.go`**). Five investigation agents in DETACHED worktrees, private scratch, port band `44800-45299`. `SPEC.md` NEW · `DECISIONS.md` **17926 -> 17956** (`ADR-0306` §Context, STATUS `PROPOSED`, verified `30 0` append-only with the base a byte-exact PREFIX) · `STATE.md` rolled **IN PLACE** (10/10, still **64** lines) · `STATE_HISTORY.md` **456 -> 458** strictly append-only · `PROGRESS.md` **51 -> 107**. ⚠️ **`ROADMAP.md` and `BEHAVIOR_CONTRACT.md` BYTE-UNTOUCHED.**

### ⚠️ THE HEADLINE: THE CONFORMANCE GATE THIS ROW WAS CHARTERED TO REASON ABOUT HAS **NEVER RUN** THE SECTION IN QUESTION — OR ANY OF RFC 9113 §6

The BRAINSTORM asked *why* `http2/6/10` reports green over a live CONTINUATION defect. **The premise is false: the selector matches ZERO cases.** h2spec addresses sections with **dotted** numbers (`http2/6.10`); `test/conformance/h2spec/h2spec.go:22-36` declares **TEN** slash-form strings; h2spec treats an unmatched positional argument as a **silent no-op**.

**Provable from inside the repo without running anything.** The total decomposes as `http2/3`=2 + `http2/4`=9 + `http2/5`=22 + `http2/7`=2 + `http2/8`=18 = **53** — and `docs/envoy-go/CONFORMANCE_PINS.md:37-57`, the gate's own audit trail, tabulates the **18 suites actually observed since 2026-04-25 with ZERO 6.x rows and `Total 53`**, while **`DECISIONS.md` ADR-0051 §2 asserts the gate runs `http2/6/1-6/5` and `http2/6/7-6/10` (53 test cases)**. ⚠️ **TWO AUTHORITATIVE DOCUMENTS HAVE CONTRADICTED EACH OTHER FOR ~80 PHASES AND NEITHER WAS NOTICED BECAUSE BOTH END IN "53".**

Measured: the correct selector `http2/6` yields **42 tests / 37 passed / 1 skipped / 4 FAILED** — four envoy-go-specific failures (three `6.5.2` SETTINGS-validation, one `6.9.2` flow-control) hidden the whole time, against a **disjoint** set of three on the reference. `assertThreshold` (`h2spec_test.go:309-311`) **cannot see a zero-case suite** (`if s.Tests == 0 { continue }`), and the JUnit XML literally carries `<testsuite name="6.10. CONTINUATION" tests="0" …/>`. ⚠️ **AND THE GATE IS NOT RUN IN CI AT ALL** — zero conformance references in `.github/` (NC: `go test|golangci` returns 8), and `TestH2Spec` skips under `-short` while CI runs `go test -short -race ./...`.

⚠️ **STANDING RULE: NO ROW MAY CITE "h2spec 53/53" AS EVIDENCE ABOUT FRAME-LEVEL BEHAVIOUR.** 42 strict cases — **44% of the declared scope**, covering DATA, HEADERS, PRIORITY, RST_STREAM, SETTINGS, PING, GOAWAY, WINDOW_UPDATE and CONTINUATION — have been silently unrun since 2026-04-25. **The number is honest; the scope claim attached to it is not.** **RECORDED, NOT FIXED** — the repair turns a green gate red over four pre-existing failures and is a row of its own.

### ⚠️ SECOND: Q4 INVERTED — ADR-0058 ALREADY PRESCRIBES THIS ROW'S FIX, SO THE ROW IS A **FULFILMENT**

`DECISIONS.md:1987-2033` §Consequences, verbatim: *"future trailer-forwarding work will widen `H2Response` with a `Trailers []hpack.HeaderField` field and the router will emit a trailing HEADERS frame conditionally when the upstream provided one. The widening is forward-compatible — no field renames; only an additive field."* **That is the fix, line for line.** ⇒ **NARROW, not supersede** — only the upstream-from-server clause; request trailers, H1, H3 and the two REVIEW carry-forwards survive.
⚠️ **AND A BINDING CONSTRAINT NO PRIOR DOCUMENT NAMED:** **ADR-0052 `:1821`** requires that future phases extending the H2 equivalence surface — naming *"gRPC in a gRPC-family phase"* explicitly — add contract sub-sections **"via a new ADR, not by editing 05.1's `### Not asserted` block silently."* **`ADR-0306` is the MANDATED vehicle, not merely the conventional one.**

### ⚠️ THIRD: §6.1 CROSSES AT THE FLOOR, SO THE SPEC **SPLIT THE ROW 84.1 / 84.2**

Enumerated floor **≈1738** net `.go`, central **≈2290**, against `:290`'s ~1500 — computed with the **smallest** unit-test bucket and the **smallest** fixture any comparable row produced. The BRAINSTORM's ~950-1200 is **below the floor**, for three measured **category** errors: the inherited ratio's denominator **counts fixture drivers as production** while the ratio is applied to production alone with the fixture added separately; all three rows in its median shipped **~no fixture** while the one fixture-bearing row (phase 80) was **excluded** at a ratio of **1.22** vs the median's 3.83; and the comment fraction is **39.2% and RISING monotonically** (34.8 / 38.0 / 40.4 / 46.6), not the ~30% budgeted.

**THE SPLIT:** **84.1** = the seam (4 production files + upstream-side RFC validation + unit tests + ADR + contract) · **84.2** = the differential fixture `0119`. ⚠️ **Unlike phase 83, the axis cuts NO correctness constraint** — no compile dependency, no ordering constraint, no shared file, and 84.1's RED anchor already exists **outside** the harness. **Precedent is the phase-61 HTTP/3 family opener, which split AT ITS SPEC** (commit subject: *"…ADR-0279 Context drafted, a 61.1/61.2/61.3 split"*), with 61.2 the codec leg (`writeH3Reply` — the direct sibling of the function this row edits) proven subject-side only and 61.3 the differential leg that flipped row 61 `done`.

### ⚠️ WHAT THIS SESSION DOES — `PLAN-84.1.md`

A TDD spine for the **seam leg**. ⚠️ **`ROADMAP.md` stays BYTE-UNTOUCHED, `want` stays 116, row 84 stays `in-progress`.** ⚠️ **`DECISIONS.md` and `BEHAVIOR_CONTRACT.md` stay BYTE-UNTOUCHED at the PLAN** (precedent: 0 of 7 recent PLANs touched either).

⚠️ **THE PLAN MUST OPEN WITH §6.1 ALREADY CROSSED.** §6.1 gates *"`PLAN.md` **estimates**"* — **phases 80 and 81 escaped the trigger purely by writing down a number that was too low.** A PLAN that re-derives a figure under 1500 is repeating that error, not refuting the SPEC. **Re-derive the floor; do not inherit it — but do not shrink it by re-scaling either.**

**THE SEAM, AS MEASURED TWICE INDEPENDENTLY.** Four production files: `internal/filter/hcm/h2/client.go` (capture the trailing HEADERS at `:440`; add `H2Response.Trailers`) · `internal/filter/http/router/router.go` (`ActionResponse.Trailers` carrier) · `internal/filter/http/router/router_h2.go` (populate in `doH2ClusterAction`) · `internal/filter/hcm/h2dispatch.go` (`writeH2Reply`). Two builds: **`+67/−18`** and **`+53/−11`** — **the spread is the comment fraction, not the logic.** The fifth inherited file is **unnecessary**: `StreamWriter.WriteHeaders` is already generic, so a trailing block is `sw.WriteHeaders(tf, true)` with **zero interface change**.

**DISPOSITIONS THE PLAN INHERITS (do not relitigate; DO re-derive):**
1. ⚠️ **D-84-ENDSTREAM = CONDITIONAL.** Hold END_STREAM off HEADERS/DATA **only when trailers are non-empty**. Unconditional changes the wire for **every** H2 response and **40 of 120** fixtures declare `http2_protocol_options`; conditional blast radius is **nil**. ⚠️ **Separately, the encode CHAIN is told a lie** — `h2dispatch.go:575` passes `len(resp.Body)==0` to `RunEncodeHeaders` and `:583` a hardcoded `true` to `RunEncodeData`; with trailers following, END_STREAM is on **neither**. **What the chain is told is not the same question as what the wire carries.**
2. ⚠️ **DO NOT WIRE `RunEncodeTrailers`** — three independent measurements: zero non-test callers (34 lines / 11 non-test / 1 call-shaped, and that one is the **definition**; NC `RunDecodeData` = 87/6, fires); `headerMapForType` (a **method** on `*abiCallbacks`) sends response trailers (map type **3**) to its `default:` arm so all **seven** consumers see `active=false`; and variant A leaves the seams dead (**0 lines** vs a firing control of **6** in the same binary). **ADR-0273 already boot-rejects the two trailer match arms on this basis.**
3. **D-84-VALIDATE = IN SCOPE for 84.1**, ~27 prod + ~110 test. The client side enforces **neither** RFC rule the server side does (`recvTrailingHeaders` requires END_STREAM and bars pseudo-headers), and nothing filters hop-by-hop fields on emit — **an upstream can smuggle `content-length` into the downstream trailer block.**
4. **H1 and H3 must PROVABLY not change** — `writeH1Reply`/`writeH3Reply` keep 4-arg signatures. ⚠️ **Each needs an explicit non-change sentence AND a regression test** (`reference_one_sided_gate_for_a_two_sided_fix`).

**THE ARM CENSUS IS BOUNDED AT 3, NOT 11 — run, not assumed.** `sw.WriteHeaders(|sw.WriteData(` over non-test `internal/*.go` gives **3 emit sites in 2 files**: `h2dispatch.go:699/:703` (`writeH2Reply`, the chartered arm) · `h2dispatch.go:723` (`write500H2` — dispose as "no upstream ⇒ no trailers") · ⚠️ **`actions.go:135/:138` (`directResponseAction.writeH2`) — A SECOND FILE, NOT IN THE 4-FILE ROSTER.** (NC: the same pattern in `_test.go` returns 9 files / 41 lines.)

**Churn surface:** **69 lines mentioning `endStream`/`END_STREAM` across 7 test files**; `writeH2Reply`'s own doc comment (`h2dispatch.go:669-670`) states the rule the row breaks **verbatim**. `ActionResponse` is referenced in **19 `.go` files**, constructed at **22 sites (18 non-test)** — additive, no compile break, but the PLAN must say which populate it.

### ⚠️ WHAT THE SPEC MEASURED SO YOU DO NOT RE-MEASURE IT

**gRPC error RPCs do NOT pass unpatched** — grpc-go's http-handler transport (which `serveGRPCHealth` uses) **never emits Trailers-Only**, so **all three fixture arms are RED** at this tip. ⚠️ **The BRAINSTORM's §5.1 item 9 is refuted; do not design around an already-green arm.** · **Three of the four "cosmetic header divergences" are REQUEST headers** going upstream and never appear downstream — the real set is `x-envoy-upstream-service-time`, `date`'s value, and the `server`/`date` order · **the TRAILER block is byte-exact cross-side** (Envoy adds nothing), so trailers pin **verbatim** and only headers need filtering · **the drop-filter NC FIRED; a `sort.Strings` NC did NOT — the sort is VACUOUS** and must not be claimed as what fixes the order · **`0079` is 1,039 lines, not 923** (the 923 omits exactly the README and the PKI) · **`0004`/`0079`/`0080` ship BYTE-IDENTICAL PEMs** (sha256-verified) so PKI is *copy 26 lines + ~31-60 lines of plumbing*, **not** a 173-line generator CI never runs · **`10119` is free** (0 hits; NC `10118` = 3) and the convention is **`10<fixture index>`**, stated in a landed comment at `0118/driver/driver.go:29-31` · **no new BackendKind, no new go.mod module** (`google.golang.org/grpc v1.70.0` already direct; `go mod tidy -diff` 0 bytes, NC fires at 404).

⚠️ **A STATS-ONLY FIXTURE IS VACUOUS ON *BOTH* SIDES — BROKEN-GATE SHAPE 31, STRENGTHENED.** The subject books `upstream_rq_2xx: 2` after two FAILED RPCs, **and** the pinned reference's moved-name set is **identical** with and without trailers (**44 names of 375, symmetric difference ∅**), with a firing positive control on *request* trailers. **Envoy books nothing for response trailers**, so a stats comparison cannot discriminate a correct tree from a broken one on either side. **82 of 120 fixtures define `AssertStats`.**

### ⚠️ THE GATES — BASELINES MEASURED AT THE PHASE-83 IMPL

Full 120-fixture differential **120/120 in 384.311 s**, `INNER_EXIT=0` (⚠️ **DOC-SOURCED — competing 388.961 s / 402 s figures exist and no artifact is committed**) · `0036` alone **19.33-20.36 s ENVELOPE, not a point** · h2spec **53/53** ⚠️ **NOW KNOWN VACUOUS FOR §6 — state the caveat** · `go test ./...` **126 ok / 0 FAIL** · `-race` as a SECOND run **125 ok / 1 FAIL, `DATA RACE` count 0**. ⚠️ **CI: `-timeout 20m`, `timeout-minutes: 30` (`ci.yml:60,63`), per-fixture budget 90 s (`runner_test.go:237`) — all verified NOT stale.**
```sh
( go test ./test/differential/ -count=1 -v > "$SCRATCH/full.log" 2>&1; echo "INNER_EXIT=$?" )
grep -cE '^    --- PASS: TestDifferential/' "$SCRATCH/full.log"          # want 120
grep -E  '^    --- (FAIL|SKIP): TestDifferential/' "$SCRATCH/full.log"   # want EMPTY
grep -c  'no driver registered for fixture' "$SCRATCH/full.log"          # want 0
grep -cE '^panic:|DATA RACE|SIGSEGV' "$SCRATCH/full.log"                 # want 0
```
⚠️ **`INNER_EXIT` IS NOT OPTIONAL.** The phase-83 IMPL's FIRST differential launch **ABORTED** (`INNER_EXIT=1`, panic on `0084-otlp-access-log`, **85 PASS + 1 FAIL of 120**) **while the surrounding tooling reported success.** **Budget ~3 launches.**
⚠️ The faithful dir predicate is `^[0-9]{4}[a-z]?-` — a bare `^[0-9]{4}-` gives **118**.
⚠️ **`go test ./test/differential/...` (with `...`) matches TWO packages and BUFFERS `-v`.** A run that looks hung with an empty log is normal — do not kill it.
⚠️ **Subtest `--- PASS` lines flush only when the PARENT completes**, so a mid-run `grep -c '--- PASS'` legitimately reads **0**. Watch `=== RUN`.
⚠️ **CI budgets are NOT this row's constraint** — three comparables measured `-count=1 -v`: `0004` **2.74 s**, `0068` **1.83 s**, `0079` **2.17 s**; headroom ≈ **254 more fixtures**. ⚠️ **The real finding: CI has NO `docker pull` step** — the 299 MB image is pulled lazily inside the **first fixture's 90 s ctx**. ⚠️ **And `-race` never covers the differential** (`-short` skips it at `runner_test.go:143`).
⚠️ **KNOWN PRE-EXISTING FLAKE:** the driver-receiver port race (behavioural roster **42**, not the `ensureServer`-symbol roster of 14). **Phase 84 is OUTSIDE the class** — `GRPCHealthResponder` is runner-spawned on a kernel-ephemeral port. **Check for sibling sessions first.**

### ⚠️ THE SIX-GATE STANDING POSTURE — NAME THE DEPARTURES, DO NOT CLAIM COMPLIANCE

Gate **(f)** expects a `REVIEW.md`; **none since 25.3 — 87 of 124 phase dirs carry none**. Gate **(d)** is **VACUOUS for this row** — say "vacuous", not "green": §7.4 binds a phase introducing a *parser, codec, or filter* and this row introduces none (the HPACK decode already runs at `client.go:425`; the frame goes through already-landed code). Fuzzers **55** (⚠️ `-- '*.go'`-scoped; unrestricted 161, whose delta is **100% Markdown code fences**). Gate **(c)**: ⚠️ **`test/conformance/grpc/` is DEFERRED IN WRITING at the SPEC on two independent grounds** — §7.5(c) binds *"the declared threshold"* and §7.3 declares one for only **two** of its four suites (the gRPC and h3spec lines are **bare**), and **`test/conformance/h3spec/` has never existed while the HTTP/3 family opened at phase 61**, which mentions gate (c) **zero times across all eight of its documents** and whose h3spec deferral `ROADMAP.md:194` still carries **23 phases later**. proxy-wasm: **STATE THE DENOMINATOR — 10 of the cpp-host's 16 families (62.5%), 6 deferred.** Normative cites: **§6.1 `:285`**, `:289` ~25-task, `:290` ~1500-LoC, **§6.2 `:294`**; **§7.3 `:346-351`**; **§7.5 `:357`, gates `:360-365`**. ⚠️ **`/BOOTSTRAP_PROMPT.md` WITH A LEADING SLASH RESOLVES NOWHERE** — the file is `<repo>/BOOTSTRAP_PROMPT.md` (**522** lines). ⚠️ **THE SECOND COPY IS NOT A COPY — IT IS THE GENERATOR PLAN** (`docs/superpowers/plans/2026-04-21-envoy-go-bootstrap-prompt.md`, **1024** lines): it carries an **unexpanded placeholder** `<family-list-from-spec-§7.2-verbatim>` where the root file has the family list, **`BOOTSTRAP_PROMPT.md:402` has NO counterpart in it at all**, and the offset is non-constant (**+228** at §7.3/§7.5/§7.4, **+165** at a §4 anchor). **CONTENT-DIVERGENT, not merely shifted. Open the repo-root file.**

### ⚠️ ADR + DOCUMENT CONVENTIONS

**The DECISIONS tail is now `ADR-0306`, STATUS `PROPOSED`. Next-free is `ADR-0307`.** ⚠️ **YOUR PLAN ADDS NO ADR AND MUST LEAVE `DECISIONS.md` BYTE-UNTOUCHED** — the phase-84 IMPL completes `ADR-0306` in place after the RETAINED italic footer, no renumber, NO `---` separator. ⚠️ **THE STRICT GUARD `^> \*\*STATUS: PROPOSED` IS NOW 1, AND THAT IS CORRECT — the SPEC armed it and the 84 IMPL disarms it. A PLAN that "fixes" it to 0 is destroying a live pointer.** ⚠️ **Do NOT use the loose form `^> \*\*STATUS: .*PROPOSED`** — it returns **5** and **ALL FIVE ARE FALSE POSITIVES**, and completing ADR-0306 will make it **6**, not restore the old figure. ⚠️ **CARRY NO LIVE WHOLE-FILE COUNT OF THAT PATTERN ANYWHERE.**

Invariants re-derived at the phase-84 SPEC close, each with a control: `DECISIONS.md` **17956** lines · **305** `^## ADR-` headings, ids **0001-0306**, ⚠️ **exactly one gap, at ADR-0209**, zero duplicates — ⚠️ **the arithmetic is CONSISTENT *because of* the gap, so "next-free = headings + 1" COLLIDES. DO NOT "FIX" IT.** · `^---$` **216** (last at `:17020`; ADR-0289..0306 carry none) · STATUS census **19** (matcher `^> \*\*STATUS: `, a BLOCKQUOTE; ⚠️ `^\*\*STATUS:\*\*` matches NOTHING and reads as a false zero) · retained italic footers **12** ⚠️ **the set is NOT contiguous — phase 79 has none**, so "one per phase since 72" is a wrong denominator · **`ROADMAP.md` 234 lines / 116 data rows** (⚠️ **state which regex** — the plainest `^\| *[0-9]+ *\|` undercounts badly) · **`STATE.md` 64** · **`STATE_HISTORY.md` 458** — ⚠️ **THE ROUTER CARRIED 454 FOR A THIRD CONSECUTIVE ROLL; that was the PRE-BRAINSTORM figure. Re-measure, never inherit.** · **`BEHAVIOR_CONTRACT.md` 5900** (⚠️ **max cited line is 5078 — VERIFIED, 197 cites in 71 files plus 76 `BC:` cites, so a TAIL append shifts 0 of 273; but an INSERT at `:2043`/`:2068` shifts 23 of 197.** ⚠️ **And `5078` is ITSELF STALE — phase 80's inserts moved that sentence to `:5124`**) — ⚠️ **THE ROW FALSIFIES FIVE LANDED CONTRACT STATEMENTS** (`:16`, `:682`, `:2043`, `:2068`, and the `#### Trailers and :scheme coverage boundaries` block at `:4291-4311`); **prefer an in-place net-zero rewrite plus a tail append.** · **stat surface 1207** (⚠️ **NOT the 1205 `STATE.md:33` carries — stale since the phase-76 close. Cite `BEHAVIOR_CONTRACT.md`, never `STATE.md` §Project**) — ⚠️ **DOC-SOURCED; ONLY THE DELTA is ever asserted.** ⚠️ **AND DO NOT DISCHARGE +0 VIA `TestNoNewStat*` — ALL FIVE ARE `internal/statssink`-SCOPED SINK-CONSTRUCTION GUARDS, STRUCTURALLY BLIND TO `internal/filter/hcm/`. There is NO global stat-surface freeze test in this repo.** Use **call-site enumeration** (**208 code sites across 36 production files** — ⚠️ cite 208/36, never 208/84). ⚠️ **`ADR-0044` does NOT contain the §Context-at-SPEC discipline** (the fullest written statement is a **reference note** at `DECISIONS.md:8534`, not an ADR clause) and ⚠️ **`ADR-0106` does NOT contain a sole-leg rule** — write **ADR-0044-as-used** and **ADR-0106-as-used**. ⚠️ **`DECISIONS.md` HEADINGS ARE NOT IN NUMERIC ORDER** — a range extraction `awk '/^## ADR-NNNN/,/^## ADR-NNNN+1/'` misbehaves; at ADR-0058 it **OVER-CAPTURES 96 lines** by swallowing ADR-0057 rather than running to EOF. **Bound the block by "the next `^## ADR-` heading of ANY id", and print the denominator.**

### ⚠️ DOCUMENTARY DEFECTS — carried forward, still deliberately NOT fixed

- ⚠️ **NEW: `ADR-0051` §2 is FACTUALLY WRONG** about which h2spec sections the gate runs, and `CONFORMANCE_PINS.md`'s own table contradicts it. **Append-only ⇒ RECORDED, NOT FIXED**; `ADR-0306` §Context ¶4 carries the correction. ⚠️ **`ADR-0051`'s PUSH_PROMISE exclusion rationale is ALSO moot** — `http2/6.6` does not exist in h2spec v2.6.0.
- ⚠️ **NEW: `ADR-0058` cites `internal/filter/hcm/actions.go:routerActionH2.doH2` TWICE and that symbol is not there** — `routerActionH2` is at `internal/filter/http/router/router_h2.go:254`; the emit is `writeH2Reply` at `internal/filter/hcm/h2dispatch.go:671`. Its other two cites are live. **Do not propagate.**
- ⚠️ **`internal/wasm/doc.go:219` carries TWO errors** — *"ABICallbacks 13->20 methods"* against an AST-counted **21**, **and** it names `abi_callbacks.go` when the interface is declared at **`internal/wasm/registration.go:130`**.
- ⚠️ **FOUR false non-test comments about the trailers seam:** `hcm/connection.go:565` and `hcm/h2dispatch.go:503` both say the chain *"does not yet expose a RunDecodeTrailers method"* — it has existed since Task 19; `router/router.go:284-285` claims a call that never happens. ⚠️ **Do NOT "fix" `router.go:284-285`** — it papers over the fact that `Run*Trailers` is DEAD production code.
- ⚠️ **`ROADMAP.md` cites `github.com/esalaine/envoy-go/…` FIVE times** (module is `github.com/pgdad/envoy-go`). **`esalaine` occurs in ZERO `.go` files** — purely documentary, INVARIANT-BLOCKED.
- ⚠️ **`ROADMAP.md` malformation: three rows lose summary content in GFM render** (lines 119, 131, 140), INVARIANT-BLOCKED => **defensible as a GUARD with the DISJUNCTION gate, not a fix.** ⚠️ **There is NO doc-invariant test anywhere in this repo.**
- **`STATE.md` §Project counts self-contradict §Current and are FROZEN at the phase-76 IMPL close** (fixtures **119** vs a real 120; *"next-free reference port is 10450"* vs the landed 10118). ⚠️ **DO NOT SOURCE ANY COUNT FROM §Project — anchor on §Current, and do NOT "fix" §Project.**
- ⚠️ **`test/differential/harness_test.go:208`'s port inventory is STALE** — it claims `10000..10447, 15000..15011, 18001..18007`; the true set reaches **10449** and includes `10120-10125`, `10200-10202`, `15042-15056`, `15104` and all of `19140-19172`. Harmless today, but **a false inventory a future allocator would trust.**
- ⚠️ **The two `//nolint:gosec` directives in `body.go` are INERT** — `.golangci.yml` is `disable-all: true` with **9** linters (`govet errcheck staticcheck unused ineffassign gofmt goimports misspell revive`), golangci-lint **v1.64.8**. ⚠️ **`gosec`, `nolintlint`, `depguard` and `forbidigo` are NONE of them.** `misspell` locale **US**.
- ⚠️ **The xDS config-seam cycle guard is NOT AUTOMATED ANYWHERE** — 13 `go list -deps` hits, all documentation, plus one **unexecuted comment** at `upstreampool_test.go:104-111`. CI never runs it.
- ⚠️ **ALL 55 FUZZERS LIVE UNDER `internal/`, NONE UNDER `test/`** — a 100% violation of §7.4's own location clause that no stage in the 77-83 window records. Newly named at the phase-84 SPEC.
- ⚠️ **`internal/filter/network/rbac/rbac.go:50` contains the token `F2`** as an unrelated **phase-26 fork label**. **Do not use `F2` as a grep anchor in that package.**

### ⚠️ STANDING METHOD NOTES

1. **⚠️ RE-RUN ALL THREE SENTINEL CHECKS YOURSELF AND RECORD THE ACTUAL OUTPUT; DO NOT PREDICT IT.** ⚠️ **CHECK (3) IS SILENT — RUN THE DOCTORED-COPY NC EVERY TIME.**
2. **⚠️ EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION, NOT BY REVIEW.** Lineage: phase-81 B14/S22/P14/I17; phase-82 B26/S23/P14/I17; phase-83 B21/S23/P19/**I37**; phase-84 **B22/S26**. ⚠️ **AND A REFUTATION IS A CLAIM TOO** — at the phase-84 SPEC one agent's "refutation" attacked a claim the BRAINSTORM **never made** (`reference_refutation_must_answer_the_claim_as_stated`), and two agents collided over a line cite whose real answer was **"neither — the cause is an ABSENCE"**. **When two measurements collide, find the axis they held differently before picking a winner.** ⚠️ **AND THE CONTROLLER BRIEF WAS WRONG AGAIN** (it asserted a stat-surface freeze test that does not exist). **A controller brief is a claim too.**
3. **⚠️ BEFORE ANY LINE-ADDING EDIT TO A HEAVILY-CITED DOC, ENUMERATE THE CITES YOUR INSERT WOULD SHIFT — AND DO NOT CARRY A WHOLE-FILE COUNT OF A PATTERN YOUR OWN PROSE SPELLS.** That species has fired **FIVE** times, most recently on the count that DOCUMENTED A FALSE-POSITIVE GATE — **the error hid inside a warning about errors.**
4. **⚠️ LINE ANCHORS DRIFT; PHRASE AND SYMBOL ANCHORS DID NOT.** ⚠️ **`*RootVM.dispatchHttpCallResponse` DOES NOT EXIST** — the symbol is `(*RootVM).handleHttpCallResponse` (`internal/wasm/http_call.go:365`). ⚠️ `headerMapForType` is a METHOD with an `*abiCallbacks` receiver at `internal/filter/http/wasm/abi_callbacks.go:204` — grep the SYMBOL, not `func headerMapForType`. **RE-VERIFY EVERY `.go` AND `.md` LINE CITE BEFORE RELYING ON IT.**
5. **⚠️ A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX.** The index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. **Still live:** `reference_sds_init_fetch_timeout_dial_budget_flake` (**TWO packages**) · the pre-existing `internal/cluster` `-race` outlier · `internal/httpclient TestOptions_ZeroValue_NoOpDefaults` · the driver-receiver port race (**42**-fixture behavioural roster).
6. **⚠️ CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE — and NEVER tear down a container this session did not create**, by name or otherwise. ⚠️ **A live reference probe needs a BRIDGE network — `--network host` is useless — and a FRESH CONTAINER PER ARM**, because `docker logs` accumulates. ⚠️ **Tear down BY NAME; never `prune`, never an `ancestor=`/image filter.** ⚠️ **`kill` on a `go run` parent leaves the COMPILED CHILD holding the socket.**
7. **⚠️ MACHINE-STATE CHANGES DELIBERATELY LEFT IN PLACE (from phase 82): `rustup toolchain 1.94.0-x86_64-unknown-linux-gnu` + its `wasm32-wasip1` target**, plus a `1.96.0` toolchain. `rustup default` unchanged (`stable`).
8. **⚠️ A `golangci-lint` NC CAN FAIL TO FIRE AND READ AS "THE GATE DISCRIMINATES."** A British-spelled **identifier** produces empty output / exit 0 (`misspell` does not flag CamelCase); only British spellings in **prose** fire. **And a `typecheck` failure short-circuits `misspell` entirely** — a *compiling* defect is required to exercise the style linters.
9. **⚠️ `rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** (`reference_harness_exit_code_is_not_command_exit_code`). Use `out=$(…); rc=$?` or `PIPESTATUS`. Observed live at two agents during the phase-84 SPEC.
10. **Counts at this tip (re-run MECHANICALLY in your worktree; never copy them).** ⚠️ Use GNU `command grep` or `git grep` repo-wide — but ⚠️ **NOT `command grep` inside `xargs`.**
    fixtures **120** (next-free **0119**, reference port **10119**; naive `^[0-9]{4}-` gives **118**) / fuzzers **55** (⚠️ `-- '*.go'`-scoped; unrestricted 161) / phase dirs **124**, of which **37** carry `REVIEW.md` and **87** do not / **BackendKind tail 38** over **39** declarations (do NOT "fix" 38 to 39) / **`DECISIONS.md` 17956**, **305** headings, tail **ADR-0306 PROPOSED**, next-free **ADR-0307** / **`ROADMAP.md` 234 lines / 116 data rows** / **`STATE.md` 64** / **`STATE_HISTORY.md` 458** / **`BEHAVIOR_CONTRACT.md` 5900**.
    ⚠️ **STILL CONTESTED, SO NO NUMBER IS CARRIED:** the `STATE_HISTORY.md` archive-gap total · production `stats.IsValidName` GUARD SITES **25** vs a naive line-grep's **56** · the stat-surface total **1207** (DOC-SOURCED; **only the DELTA is ever asserted**) · the `ROADMAP.md:<line>` and `BEHAVIOR_CONTRACT.md:<line>` cite counts · `allCallbacksNoOp` occurrences.
    ⚠️ **THE PORT OCCUPANCY IS 146 DISTINCT PORTS ACROSS 19 BANDS, NOT THE 39 THIS ROUTER USED TO CARRY** — re-derived from every driver's `ReferenceListenerPort()` **body**, not by grepping literals: 117 of 120 fixtures bind ≥1 port, **184 slots, range 10000-19172**. Five of the old entries (`12345`, `18000-18002`, `18007`, `19000`, `19999`) **are not reference listener ports at all**. **The convention is `10<fixture index>`**, stated in a landed comment at `0118/driver/driver.go:29-31`; **"10443 = TLS" is a leaked mnemonic, not a rule.** ⚠️ **Collisions are a DOCUMENTATION concern only** — `ReferenceListenerPort` is container-internal, the runner dials a randomly mapped host port, **31 ports are already shared**, and `t.Parallel` count is **0**.


## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — take it from `git rev-parse master`, **NOT from a SHA quoted in this file**. ⚠️ **AND THE BARE SUBJECT-LOCATE GREP IS CONTAMINATED BY THIS FILE'S OWN PROSE ABOUT IT** — `git log` greps the whole commit message and router commits discuss the phase in their BODIES, so **the sentence warning about the grep satisfies the grep.** ⚠️ **USE THE SLUG-ANCHORED FORM**, which is stage-exact:
```sh
git log --grep '^phase 84 (grpc-unary-response-trailers)' --format='%h %s'  # 2 at this tip: BRAINSTORM + SPEC. YOUR stage adds the 3rd.
git log --grep '^phase 83 (wasm-pause-arm-leak)' --format='%h %s'           # 4 — the CLOSED phase, use it as a FIRING CONTROL
```
Measured with both controls: an invented slug returns **0**, the CLOSED phase-83 form returns **4**. **Take the NEWEST line (`| head -1`) and verify the count before relying on it.** Subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** (`reference_parallel_subagents_private_scratch`) and must tear down docker containers **BY NAME** (`reference_parallel_agents_shared_machine_namespaces`). ⚠️ **THERE ARE TWO RESERVED PORT BANDS — band private ports outside BOTH `20000-31007` (subject blocks) AND `11000-14999` (backends), and also clear of the static fixture ports, whose REAL extent is 19 bands from 10000 to 19172.** The `42000-45299` band has been used safely (phases 80-84). ⚠️ **If two agents share ONE worktree they must stage by EXPLICIT PATH** and **must NOT run an unscoped `git restore`**. ⚠️ **PREFER ONE WORKTREE PER AGENT OFF A COMMON BASE.** Per-task `gofmt` + `golangci-lint` on touched packages (`feedback_pertask_gofmt_lint`). ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch, and note `-o` precedes the package path.**
**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. ⚠️ **ASSUME IT FIRES EVERY SESSION — it has been treated as live for THIRTY-FIVE consecutive sessions and it FIRED, OBSERVED, at the phase-84 SPEC** (`Shell cwd was reset to /home/esa/git/envoy-go`). **Use `git -C <abs-worktree-path>` for every git command, prefer absolute paths over `cd`, put any needed `cd` FIRST in a compound command, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — `git add` ALONE WILL NOT STAGE IT. USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN.** At the phase-84 SPEC five agents built full fixture prototypes, two independent production patches, instrumented binaries and docker reference probes, ran them, reverted them, and each confirmed `git status --porcelain` = 0 lines. **Every agent worked in a DETACHED worktree precisely so a failed revert could not dirty a branch — copy that.**

⚠️ **THE ARCHIVE-ABSENCE GUARD — USE THE ROBUST FORM. TWO INDEPENDENT DEFECTS, BOTH LIVE.**

1. **A guard anchored on the PHASE NAME self-clears.** `STATE.md`'s `last-updated` field NAMES the entry it just evicted, so a name-anchored grep returns 1 and reads as *"the eviction did not happen"*. **Anchor on the BULLET, not the name.**
2. ⚠️ **BUT THE COLON-ANCHORED BULLET FORM IS FAIL-UNSAFE.** Anchoring directly on the colon misses every entry whose label carries a parenthetical between the field name and the colon. It reads **0 on an entry that IS present**. Since the guard exists to prove **ABSENCE** before appending, that **AUTHORIZES A DUPLICATE APPEND**.
3. ⚠️ **AND THE "TOLERANT" NON-ASTERISK FORM IS *ALSO* FAIL-UNSAFE** — measured at the phase-84 BRAINSTORM, it reproduced that exact miss. Do not use it.

**USE THE ROBUST FORM: anchor on the bullet and allow ANY run of characters before the backtick-quoted target**, and ⚠️ **CORROBORATE IT WITH A WHOLE-FILE FIXED-STRING SEARCH (`grep -cF`)**, which depends on no regex at all. Measured cross-product on **REAL** targets at the phase-84 SPEC:

| target | raw `grep -cF` | OLD colon form | **ROBUST form** |
|---|---|---|---|
| an ANNOTATED-label entry that IS present | **1** | **0 — FALSE ABSENT** | **1** ✅ |
| the eviction target | 0 | 0 | **0** ✅ |
| another present entry | 1 | 1 ✅ | **1** ✅ |
| an invented target | 0 | 0 ✅ | **0** ✅ |

⚠️ **AND THE ENTRY THE PHASE-84 SPEC APPENDED IMMEDIATELY REPRODUCED THE DEFECT** — post-append it reads **1 raw / 1 robust / 0 colon**, because its own label carries a parenthetical. **The defect is self-perpetuating: every correctly-annotated archive entry is invisible to the colon form.**

⚠️ **NC IT WITH A *REAL* TARGET.** A probe using an **invented phase name** reads 0 on both arms — non-discriminating, and it looks like agreement (`reference_probe_input_is_a_claim`). ⚠️ **AND DO NOT SPELL THE GUARD'S REGEX INSIDE AN ARCHIVE ENTRY LABEL** — a quoting entry self-clears. **Describe the guard in prose in the label; keep the regex out of the archived file.**

⚠️ **AND VERIFY THE ARCHIVE EDIT IS STRICTLY APPEND-ONLY**, not merely "looks right": `git diff --numstat` must show `N 0` (zero deletions) **and** the base file must be a byte-exact **prefix** of the new one.

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY** (TRUSTWORTHY; ADR-0288 discipline — ⚠️ **anchor greps on `^- **<field>`**; all seven fields verified singleton and §Recent verified at its five-entry cap at the phase-84 SPEC close). ⚠️ **§Project counts SELF-CONTRADICT §Current and are TEN-PLUS PHASES STALE** — do not anchor on them and do not fix them.
2. `docs/envoy-go/phases/84-grpc-unary-response-trailers/SPEC.md` — **YOUR OWN PHASE'S SPEC, AND IT SPLIT THE ROW.** Read §1 (the three central findings), §3-§8 (the five disposed open questions plus **three new forks**), §9 (blast radius + the 3-site arm census), §12 (**the §6.1 verdict and the split**), §14 (hazards) and §15 (gates). ⚠️ **RE-DERIVE ANYTHING YOU TAKE** — this lineage has refuted its predecessor at every stage without exception. Its siblings `BRAINSTORM.md` and `PROGRESS.md` carry the charter and the stage record.
3. `docs/envoy-go/ROADMAP.md` — **§Schema first** (`:18` carries BOTH the update-in-place invariant AND *"Sub-phases get their own rows"* on ONE line — ⚠️ **practice DIVERGES; zero dotted rows exist above 32.2**), then the SIX deferred-candidate paragraphs, then the family headings. ⚠️ **YOUR PLAN ADDS NO ROW AND MUST LEAVE THIS FILE BYTE-UNTOUCHED.**
4. `BOOTSTRAP_PROMPT.md` — **at the REPO ROOT, no leading slash.** §6.1 `:285`, §6.2 `:294`, §7.3 `:346-351`, §7.5 `:357-367`. ⚠️ **Not the 1024-line file under `docs/superpowers/plans/`, which is the GENERATOR PLAN and is content-divergent.**
5. `docs/envoy-go/DECISIONS.md` — **ADR-0306** (this row's own, STATUS `PROPOSED` — read its eleven §Context paragraphs) · **ADR-0058** (`:1987-2033`, NARROWED not superseded) · **ADR-0052 `:1821`** (the BINDING contract-edit vehicle) · **ADR-0051** (the h2spec pin — ⚠️ **its §2 is factually wrong**) · **ADR-0288** (roll-IN-PLACE and the archive rule) · **ADR-0044-as-used** · **ADR-0106-as-used**. ⚠️ **Your PLAN adds NO ADR and leaves this file BYTE-UNTOUCHED.**
