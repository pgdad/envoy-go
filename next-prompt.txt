=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: does NOT apply. Phase 53 (`load-balancer-priority`) is actively `in-progress` (lifecycle-state 2 → 3, PLAN done) — there is a clear next action (its IMPL). Do not check the cross-family audit again until phase 53 itself reaches phase-done with no further chartered work.

**STATUS AS OF 2026-07-07: phase 53's PLAN landed — an 11-task TDD spine matching SPEC §10's own indicative numbering 1:1 (no fold/split needed), decomposing the `priorityLB` construct into `tierHealth`/`tierCapacity`/`cascadeLoads` pure functions (Task 3), `priorityGroup`/`priorityLB` construction+grouping (Task 4), the real `Pick` (Task 5), the `manager.go` third wrap-after-switch (Task 6), unit tests against the confirmed empirical data tables (Task 7), the `0096-lb-priority` differential driver/assertions/deliberate-breaks (Tasks 8-10), and the BEHAVIOR_CONTRACT + ADR-0270 body + six-gate + ROADMAP flip (Task 11).**

- The phase-53 PLAN landed (docs-only, worktree `.worktrees/phase-53-plan`, branch `phase-53-load-balancer-priority-plan`, squash-merged to master): `docs/envoy-go/phases/53-load-balancer-priority/PLAN.md` (2513 lines).
- **All 3 SPEC §12 PLAN-level D-questions RESOLVED with concrete code + dedicated unit tests:** D-P-DUP (duplicate `priority` values across `LocalityLbEndpoints` groups MERGE into the same tier — simpler than phase 52's D-LW-DUP, no weight conflict to resolve, just an endpoint-slice merge; proven by `TestNewPriorityLB_DuplicatePriority_EndpointsMerge`); D-P-FACTORY (a phase-53-LOCAL `priorityLeafFactory` type, accepting an explicit health-view parameter, kept deliberately separate from `subset.go`'s shared `leafFactory` — NOT a retrofit of the shared type); D-P-RETROFIT (confirmed explicitly OUT of this phase's scope — whether `locality.go`'s own accepted "child-local-panic" coverage boundary should retroactively adopt the same `tierHealth`-style fix stays a candidate future maintenance item, not bundled here).
- Every drafted Go code block (Tasks 2-9) was assembled into a scratch copy of the real repo and compile+test VERIFIED (including `-race`) against the actual current codebase during PLAN authoring — catching and fixing, before landing: a stale module-path artifact copied from the phase-52 template; two import-ordering issues (an import used only starting a later task, moved to that task); a nonexistent stats-registry API call (`Registry.Snapshot()` doesn't exist — fixed to hold the `*Counter` handle directly); a test/type name collision with the pre-existing `locality_test.go` (renamed to avoid colliding with `locality.go`'s own `argRecordingLB`).
- **FINAL ADR-0045 split-gate re-check: NO split** — 11 tasks / ~200-250 prod LoC (`priority.go` ~170-190 + `cluster.go` +10 + `manager.go` ~40 delta), at the LOW end of SPEC's own ~230-330 estimate (the three-way pure-function split made the confirmed formulas testable without `Pick`-level stochastic scaffolding), comfortably under the ADR-0045 `>~25 tasks OR >~1500 LoC` gate.
- An independent `general-purpose` plan-document-reviewer pass (mirroring the phase-52 precedent) found **ZERO real, load-bearing defects** after a genuinely adversarial effort: 20+ file:line citations spot-checked exact against the real worktree (every `manager.go`/`locality.go`/`health.go`/`subset.go`/`loadbalancer.go` anchor); the `cascadeLoads`/AMEND-P1 bypass math hand-recomputed against SPEC §11.4's data tables and confirmed exact (including both 3-tier scenarios); the flat-fallback-gets-nil-health guarantee traced end-to-end; the `Endpoint{}` construction-site sweep independently re-run and confirmed (only `manager.go`'s single production keyed literal needs the new field); the composition-reject wording confirmed byte-identical to SPEC §6.1; confirmed no boot-reject cross-side fixture dir was created (§8.2 honored) — explicitly BETTER-verified than the phase-52 precedent that established this review protocol (which had caught 3 real issues).
- STATE.md/ROADMAP.md updated (row 53 STAYS `in-progress`, lifecycle-state 2 → 3; the active-phase entry rewritten to lead with PLAN-done, nesting the prior SPEC-done detail). This was a docs-only commit (no production code touched).
- Anticipated at IMPL: fixtures 97 → 98 (`0096-lb-priority`); stat surface 1200 (+0, CONFIRMED); fuzzers 52 (+0); BackendKind tail 38 (+0); DECISIONS ADR-0269 → ADR-0270 (next-free ADR-0271). ZERO new packages, ZERO new go.mod deps.

=> The NEXT action is **the phase-53 IMPL** (subagent-driven execution, per `feedback_execution_style`, in a FRESH worktree off master per `feedback_git_worktrees` — e.g. `.worktrees/phase-53-impl`, branch `phase-53-load-balancer-priority-impl`). Execute `docs/envoy-go/phases/53-load-balancer-priority/PLAN.md`'s 11 tasks in order, one subagent per task, controller-reviewed between tasks (the two-stage review discipline). Subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit, re-runs the full suite on the frozen HEAD, does any deliberate-break verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). This IMPL ANCHORS ADR-0270 (§Decision/§Consequences land in-place per ADR-0044, §Context already drafted at SPEC §13) and FLIPS ROADMAP row 53 `in-progress → done` (NO parent rollup per ADR-0106 — a single flat row).

==============================================

## Read first (in order)
1. THIS FILE — the cold-start router (phase 53 chartered, PLAN done, IMPL is next).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 53 PLAN done`) + the full history, including the SPEC's AMEND-P1..P6 findings and this PLAN's D-question resolutions.
3. `docs/envoy-go/ROADMAP.md` — confirm row 53 `in-progress` (lifecycle-state 2 → 3); the Load-balancing family section for full context (phases 34 through 53).
4. `docs/envoy-go/phases/53-load-balancer-priority/PLAN.md` — the PRIMARY input for this session: the 11-task TDD spine, each with Files/Interfaces/Steps and complete, compile-verified Go code. Execute it task-by-task.
5. `docs/envoy-go/phases/53-load-balancer-priority/SPEC.md` — background/authority for the PLAN's decisions; read if a task's rationale needs the full empirical context (§11's AMEND-P1..P6 blocks).
6. `docs/envoy-go/phases/52-load-balancer-locality-weighted/PLAN.md` and its IMPL history in STATE.md — the DIRECT structural precedent for how a Load-balancing-family IMPL session runs (worktree setup, per-task subagent dispatch, the differential fixture build-out, the final six-gate + ADR body + BEHAVIOR_CONTRACT delta + ROADMAP flip).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — search for `reference_*`/`feedback_*` entries tagged for load-balancer/health/differential work (e.g. `reference_health_check_propagation_warmup`, `reference_differential_band_sigma_margin` [note: this fixture deliberately uses hard 100%/0% boundaries, NOT a banded arm — confirm no band-margin tuning is needed], `reference_differential_break_protocol_count1`, `reference_differential_asserter_dispatch`, `reference_differential_run_selector`, `reference_fixture_workload_constant_desync`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_path_targeting`, `feedback_subagent_worktree_detach`).

## Honor throughout (the discipline, unchanged for whichever stage comes next)
- **Lifecycle = one stage per session** (BRAINSTORM → SPEC → PLAN → IMPL). This session's job is the phase-53 IMPL — land all 11 tasks, run the full six-gate, and flip ROADMAP row 53 `done`.
- **Subagent-driven execution** (`feedback_execution_style`) in a **fresh worktree** (`feedback_git_worktrees`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit, re-runs the full suite on the frozen HEAD, does any deliberate-break verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). Per-task gates: `gofmt -l` + `golangci-lint` + `go vet` + `go build ./...` (`feedback_pertask_gofmt_lint`).
- **Process anchors:** ADR-0106 (per-leg rows) · ADR-0044 (ADR §Decision+§Consequences land at IMPL) · ADR-0052 (atomic six-gate completion bundle) · next-free **ADR-0270** (anchored at this IMPL).
- Re-verify baselines fresh at cold-start — do NOT trust any count in this file without re-running the baseline commands (`ls -d test/fixtures/[0-9][0-9][0-9][0-9]* | wc -l`, `grep -rh '^func Fuzz' --include='*.go' . | wc -l`, etc.) against current HEAD.
- After the phase-53 IMPL lands and ROADMAP row 53 flips `done`, roll this file forward to the "none chartered, awaiting a human pick" state — 1 Load-balancing-family candidate remains (panic thresholds as an independent construct), plus Observability (5 candidates) and Operational tooling (3 candidates) stay open, exactly as the phase-49→50/phase-50→51/phase-51→52 precedent handled it. Do NOT create a `stop` file; this is not the termination sentinel.
