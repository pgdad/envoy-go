START a fresh cold-start session for envoy-go — **phase 32.1 (`network-filter-redis-upstream-pool-seam-and-codec`) IMPL is DONE; the next lifecycle step is the 32.2 SPEC** (`network-filter-redis-commands-and-stats`, `superpowers:writing-plans`). The 32.1 IMPL (all 16 tasks) landed the upstream-pool seam (`UpstreamConn`/`UpstreamDialFunc`), the RESP codec (request + reply decode + encode), the `redisproxy` package foundation (10th built-in, config parse, PING/AUTH local-reply, 10-name EAGER stat roster, `Handle` pump), the `TCPRedisResponder` BackendKind (32), the `0055-redis-roundtrip` + `0056-redis-boot-reject` fixtures, the NEW `GET /stats` flat admin endpoint (eighth admin endpoint, required for `StatsAsserter`), and the ADR-0230 §Decision/§Consequences body + ADR-0229 §Decision/§Consequences 32.1 half in place (per ADR-0044). Six gates GREEN (build / vet / golangci-lint / `-race -short` / differential 58/58 / h2spec 53/53 + proxy-wasm 10/10). The 32.1 IMPL squash LANDED at `fa58bc9` on origin/master (squash-merged from `phase-32.1-network-filter-redis-upstream-pool-seam-and-codec-impl` + pushed by the controller per `feedback_push_to_origin`; the IMPL branch + worktree were cleaned up at stage-close). This docs-only SHA-fill trails it; confirm the live master tip via `git log --oneline -1`.

The 32.2 SPEC is the second sub-phase of the parent phase-32 (`redis_proxy`) row. It builds ON the 32.1 seam + codec foundation and delivers: the **full single-key command set** (core commands routed via `catch_all`); the **per-command + downstream + upstream stat roster** under `redis.<stat_prefix>.` (EAGER + table-bounded per D32-2 — ~180 command names × 3 stat slots ≈ ~540 counters, stat surface ~546 → ~1091; the `stats.IsValidName` guard is satisfied by construction for table-bounded names); the new **`redis.` LABEL-HOISTED Prometheus tag-extractor arm** at `internal/stats/name.go` (`envoy_redis_<leaf>{envoy_redis_prefix="<sp>"}` — the mongo `.rbac.` shape, NOT kafka inline; D32-2 pinned this at the parent SPEC); the 4 **gauge inc/dec** calls (the 4 gauges landed at 32.1 but their inc/dec paths are 32.2's scope); the **differential command matrix** (a command spread + error/edge arms, extending `0055`); the **41st fuzzer `FuzzRESPDecode`** (no-panic / no-mutation / bounded-buffer); the BEHAVIOR_CONTRACT 32.2 bundle + the **parent-row-32 ROLLUP** (parent row 32 flips `in-progress → done` ATOMICALLY with sub-row 32.2 per the 18/19/22/24/25/26/28/29 precedent); the ADR-0229 §Decision/§Consequences 32.2 half (completing the PARTIAL body).

## What this session does — the phase-32.2 SPEC

Run `superpowers:writing-plans` scoped to the **phase-32.2 SPEC** (`network-filter-redis-commands-and-stats`). The SPEC must pin:

- **Full command-set roster** — which commands are in-scope at 32.2 (core single-key: GET/SET/DEL/EXISTS/EXPIRE/TTL/TYPE/INCR/DECR/APPEND/GETSET/MGET/MSET/SADD/SCARD/SMEMBERS/LPUSH/LLEN/LRANGE + unknown-command forwarding; AUTH/PING/ECHO/TIME/QUIT/HELLO stay local-reply); which remain DEFERRED (multi-key fragmentation [MGET/MSET sharding], `enable_redirection`, faults, mirroring — parse-accept, behavior-deferred).
- **Per-command stat names** — the exact `redis.<sp>.command.<cmd>.*` slot names from `ALL_REDIS_PROXY_STATS` (total/success/error per command); the splitter / `REDIS_CLUSTER_STATS` additional names (if 32.2-scoped); the `stats.IsValidName` charset guarantee by construction.
- **Stat surface accounting** — 546 + fixed-per-command roster + per-command-seen dynamic counter creation posture (EAGER full table at init vs DYNAMIC per-seen; D32-2 resolved EAGER + table-bounded ~180 → exact count; spell out the ≈1091 accounting).
- **`redis.` LABEL-HOISTED Prometheus arm** (D32-2 / AMEND-R2) — exact label key/value, the `internal/stats/name.go` site, the `envoy_redis_<leaf>{envoy_redis_prefix="<sp>"}` form; contrast with the kafka INLINE arm and the mongo `.rbac.` hoist.
- **Gauge inc/dec** — which of the 4 gauges (`downstream_cx_active`, `upstream_cx_active`, and the two connection-pool gauges) are inc/dec at 32.2; the as-built 32.1 positions they were registered but not yet incremented.
- **Differential command matrix** — extending `0055-redis-roundtrip` with a command spread fixture; the `TCPRedisResponder` FIFO/positional choreography for each arm; the `StatsAsserter` stat names to assert per arm; whether the command matrix is ONE fixture with multiple sub-arms or multiple separate fixtures (one dir = one runner-branch constraint per `reference_differential_fixture_dispatch_constraint`).
- **`FuzzRESPDecode`** — the 41st fuzzer; which package it lives in (`redisproxy`); what it fuzzes (the `resp.go` decode path; no-panic; no-mutation; bounded-buffer).
- **Parent-row-32 ROLLUP** — ATOMICALLY at sub-row 32.2 close: parent row 32 `in-progress → done`, sub-row 32.2 `planned → done`, the ADR-0229 body completes (PARTIAL → ACCEPTED), the `redisproxy` BEHAVIOR_CONTRACT section fills its 32.2 coverage boundary.
- **ADR-0229 §Decision/§Consequences 32.2 half** — what new content lands (the per-command stat roster, the `redis.` arm, the differential command matrix, the fuzzer, the gauge inc/dec, the ROLLUP counts).
- **ADR-0045 split-gate re-check at PLAN** — anticipated LoC + task count for 32.2; confirm NO further split needed.
- **D-question dispositions** at 32.2: any new empirical pins needed (likely none — D32-2 resolved; but confirm `redis.` arm shape, exact `ALL_REDIS_PROXY_STATS` slot names, gauge inc/dec call sites in the upstream Envoy source).

## Key invariants to carry forward

- `reference_network_filter_typeurl_extensions` — TypeURL carries `extensions.`; verified at 32.1 Task 1 (no re-probe needed).
- `reference_dynamic_stat_name_charset_guard` — for any DYNAMIC stat name creation, `stats.IsValidName` before `NewCounterIfAbsent`; at 32.2 the roster is EAGER table-bounded so the guard is satisfied by construction at table-build time.
- `reference_differential_asserter_dispatch` — `0055` is a cross-side fixture → uses `StatsAsserter` (not `SubjectAsserter`); new command-matrix arms must also use `StatsAsserter`.
- `reference_differential_fixture_dispatch_constraint` — one fixture dir = one runner branch (cross-side XOR boot-reject); plan command-matrix as cross-side arms within one dir OR separate dirs.
- `reference_differential_break_protocol_count1` — deliberate-break checks use `go test -count=1` (not cached).
- `reference_differential_reference_parses_full_message` — fully-valid frames; pin per-side `*.failure` values where ref ≠ subj.
- `reference_wire_format_both_sides_see_same_bytes` — the downstream RESPONSE bytes are the proof; adopt upstream's framing verbatim (EXTENDS to the response side for redis_proxy).
- `reference_close_direction_framework_gap` — AMEND-R9 confirmed NO close-direction counters at 32.2 (zero-touch; `cx_destroy_*` deferred to a future framework-surgery sub-phase).
- `feedback_pertask_gofmt_lint` — each subagent task: `gofmt -l` + `golangci-lint run` on touched packages.
- `feedback_subagents_no_push` — subagents commit LOCAL-ONLY; controller squash-merges + pushes at stage-close.
- `feedback_push_to_origin` — after the 32.2 IMPL six-gate is green and the squash lands on master, push to origin/master.

## Honor throughout

- `feedback_execution_style` (subagent-driven IMPL at 32.2 IMPL) · `feedback_git_worktrees` · `feedback_subagents_no_push` · `feedback_push_to_origin` · `feedback_pertask_gofmt_lint` · `feedback_subagent_worktree_detach`.
- ADR-0229 (the redis_proxy filter umbrella — the 32.2 half completes the PARTIAL body → ACCEPTED) · ADR-0230 (the upstream-pool seam — ACCEPTED, bodies landed at 32.1) · ADR-0044 (§Context-at-SPEC, §Decision+§Consequences-at-IMPL) · ADR-0079 (two-step factory) · ADR-0080 (byte-stable rejects) · ADR-0052 (atomic landing + six-gate) · ADR-0045 (split-gate re-check at PLAN) · ADR-0106(d) (parent-row ROLLUP atomically with 32.2) · ADR-0060 (histograms deferred — command latency histograms NOT counted at 32.2) · ADR-0219 (the routing precedent) · ADR-0223 (per-conn mutex — if a pipelined model is introduced at 32.2; otherwise DEFERRED) · ADR-0227 (the contrib pin).

## Read first (in order)

1. THIS FILE — the phase-32.2-SPEC cold-start.
2. `docs/envoy-go/phases/32.1-network-filter-redis-upstream-pool-seam-and-codec/SPEC.md` — **THE 32.1 SPEC** (the codec §3.5, the seam §4, the stat roster §7, the fixture roster §8, the 32.1 D-questions §12.2 — all durable anchors that 32.2 inherits).
3. `docs/envoy-go/phases/32-network-filter-redis-proxy/SPEC.md` — **THE PARENT SPEC** (the §5 proto / §6 reject / §7 stat / §8 fixture rosters + the §11 D32-1..D32-8 empirical-pin block; D32-2 [EAGER table-bounded per-command roster; 15 fixed names; `redis.` LABEL-HOISTED] + D32-4 [parse-accept-vs-reject field map] are the key 32.2 anchors).
4. `docs/envoy-go/STATE.md` — active-phase `phase 32.1 IMPL done`; lifecycle-state → the 32.2 SPEC; next-skill `superpowers:writing-plans`; counts (546 stats / 58 fixtures / 40 fuzzers / BackendKind tail 32 / DECISIONS tail ADR-0230, next-free ADR-0231).
5. `docs/envoy-go/ROADMAP.md` — parent row 32 `in-progress`; sub-row 32.1 `done`; sub-row 32.2 `planned`.
6. `docs/envoy-go/DECISIONS.md` — **ADR-0229** (PARTIAL — the 32.1 half landed; the 32.2 half lands THIS SPEC/IMPL) + **ADR-0230** (ACCEPTED; the bodies are the authoritative seam record) + ADR-0044 / ADR-0079 / ADR-0080 / ADR-0052 / ADR-0045 / ADR-0106 / ADR-0060 / ADR-0219 / ADR-0223 / ADR-0227.
7. `docs/envoy-go/phases/32.1-network-filter-redis-upstream-pool-seam-and-codec/PLAN.md` + `PROGRESS.md` — the 32.1 IMPL task log (the as-built deviations, the six-gate record, the Task-14 unplanned `GET /stats` endpoint note, the Task-16 BEHAVIOR_CONTRACT "eighth endpoint" note).
8. `docs/envoy-go/phases/29.2-network-filter-mongo-response-side/SPEC.md` + `PLAN.md` + `PROGRESS.md` — the per-sub-phase SPEC/IMPL lifecycle precedent (the BRAINSTORM-to-ROLLUP shape; the SPEC D-pin execution; the completion-bundle task).
9. `internal/filter/network/redisproxy/` — the **as-built 32.1 source** (filter.go + config.go + resp.go + commands.go + stats.go); the seam `internal/filter/network/upstreampool.go`; the stat registry `internal/stats/registry.go` (`NewCounterIfAbsent`/`NewGaugeIfAbsent`/`IsValidName`); the Prometheus arm `internal/stats/name.go` (the kafka `kafka.` INLINE arm = the 32.2 contrast; the mongo `.rbac.` hoist = the 32.2 shape for `redis.`).
10. `test/differential/fixture/fixture.go` (the `StatsAsserter` + `TCPRedisResponder` BackendKind 32) + `test/fixtures/0055-redis-roundtrip/` (the as-built cross-side fixture — the 32.2 command matrix extends it).
11. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`.

## Counts at master tip (phase 32.1 IMPL — the 32.2 SPEC session opens from here)

- stat surface **546** (→ ~1091 at the 32.2 IMPL: +10 fixed [32.2 D32-2 EAGER per-command roster] + ~540 dynamic per-command ≈ 180 cmds × 3 slots; `command.<cmd>.latency` histograms + `*_fault` counters NOT counted per ADR-0060); differential fixtures **58** (tail `0056-redis-boot-reject`; advances at the 32.2 IMPL); fuzzers **40** (tail `FuzzKafkaDecode`; → **41** `FuzzRESPDecode` at the 32.2 IMPL); BackendKind tail **32** (`TCPRedisResponder`; unchanged at 32.2 IMPL); DECISIONS.md tail **ADR-0230** (body ACCEPTED; next-free **ADR-0231** — no new ADR consumed at 32.2 IMPL, ADR-0229 body completes in-place).
- conformance h2spec 53/53 + proxy-wasm 10/10 (image-independent; UNAFFECTED by phase 32 — touches no HTTP/h2/proxy-wasm path).
- reference pin: `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227).
- Toolchain: Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); go-control-plane `/envoy` v1.32.4 (ADR-0008) + `/contrib` v1.32.4 (phase-31 dep). redis_proxy is CORE `/envoy` (D32-1 CONFIRMED) → ZERO new go.mod dep at 32.2 IMPL.
- §9 Network-filters family: rows 26/27/28/29/30/31 DONE + **row 32 `redis_proxy` IN-PROGRESS (parent SPEC done; 32.1 IMPL done; sub-row 32.1 `done`; sub-row 32.2 `planned`)**. After phase 32 the candidate {thrift} remains (the last §9 candidate — reuses the 32.1 upstream-pool seam).
