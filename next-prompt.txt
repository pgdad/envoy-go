RESUME a fresh session for envoy-go (router last updated 2026-06-27; the **phase 46.1 (tracing core+OTLP) SPEC is DONE and LANDED** — a docs-only commit direct-on-master chartering the FIRST leg of the THIRD Observability-family row [`tracing`] at SPEC depth. **ROW 46 (`tracing`) STAYS `in-progress`** (46.1 is NOT the final leg — row 46 flips `done` at the 46.2 IMPL per ADR-0106 + `reference_roadmap_split_phase_row_done`); the Observability FAMILY STAYS OPEN (Zipkin/B3 [46.2] / stats sinks / tap remain rows). The SPEC (`docs/envoy-go/phases/46-tracing/SPEC-46.1.md`) was authored in the house 14-section shape (mirrors `SPEC-45.1.md`) + spec-document-reviewer-APPROVED first-iteration [ALL load-bearing proto + code:line + count claims verified TRUE against the resolved `go-control-plane/envoy v1.32.4` + `go.opentelemetry.io/proto/otlp v1.0.0` modules + the as-built seams; 1 NIT folded — the `spawn_upstream_span` reject-trigger present-vs-`true` semantics pinned as a PLAN call]. **The §11 D-TRACE-* empirical pins were EXECUTED IN-SESSION 2026-06-27** live against `envoyproxy/envoy:contrib-v1.37.2` per `reference_docker_probe_bridge_network` (a shared bridge `traceprobe-net`; a `probe` container = HTTP backend [echoing inbound trace headers] + an h2c OTLP `TraceService` receiver [protojson-dumping each `ResourceSpans`]; the probe binary was built INSIDE the repo module then DELETED + Docker torn down ⇒ the tree is CLEAN; **decode-ran proof: `tracing.opentelemetry.spans_sent: 6` + 6 captured `ingress`/SERVER spans**). **The PINNED findings (several CORRECT the brainstorm guesses):** (D-TRACE-SPAN) ONE SERVER span/request, the constant `name:"ingress"`, kind `SPAN_KIND_SERVER`, a CONCRETE 16-attr roster [`http.method`/`http.url`/`http.protocol`/`http.status_code`/`component=proxy`/`upstream_cluster(.name)`/`downstream_cluster=-`/`response_flags=-`/`request_size`/`response_size`/`user_agent`/`guid:x-request-id`/`node_id`/`zone`/`peer.address` (+`guid:x-client-trace-id`)], `ScopeSpans.scope={name:envoy,version}` (POPULATED, unlike the 45.1 logs empty scope), `Resource`=`service.name`(←`service_name`)+SDK-trio (only `service.name` cross-side assertable), span END = stream-complete = the `accesslog_emit.go` seam; (D-TRACE-REQUESTID) the trace reason at `x-request-id` STRING index 14 — NoTrace=`4`/Sampled=`9`/Client(`x-client-trace-id`)=`b`; GENERATE-when-absent / PRESERVE-verbatim-and-overwrite-only-index-14-when-present; a CONTINUED trace keeps `4`; the id is generated + injected upstream REGARDLESS of sampling; (D-TRACE-PROPAGATION) W3C `traceparent` `00-<32hex>-<16hex>-<2hex>`, continue the incoming trace-id + `parent_span_id`=incoming-parent + fresh span-id + `tracestate` pass-through; (D-TRACE-SAMPLING) precedence = incoming-`traceparent`-continues-authoritatively-BYPASSING-local-caps → `x-client-trace-id`-force-gated-by-`client_sampling` → `random_sampling` → `overall_sampling`-caps-the-LOCAL-sample (overall=0 suppressed BOTH random AND client-force; NOT a continued trace); stat classes `client_enabled`/`random_sampling`/`not_traceable` [NOT the brainstorm's `client_disabled`/`not_traced`]; (D-TRACE-STATS) 5 HCM-scoped `http.<stat_prefix>.tracing.{client_enabled,health_check,not_traceable,random_sampling,service_forced}` [`<stat_prefix>` DYNAMIC ⇒ `stats.IsValidName`-guard per `reference_dynamic_stat_name_charset_guard`] + 3 tracer-scoped `tracing.opentelemetry.{spans_sent,spans_dropped,timer_flushed}` ⇒ surface 1191 → 1191+N [N≈5–7, IMPL-pinned]; (D-TRACE-OTLP-WIRE) UNARY `Export`, one `ResourceSpans`/Export, framing varies ⇒ assert per-span payload not framing; (D-TRACE-REJECT) the reference is PERMISSIVE [a missing collector cluster + `custom_tags`/`verbose`/`max_path_tag_length`/`spawn_upstream_span` all BOOT; `spawn_upstream_span:true` adds a 2nd CLIENT span `"router <cluster> egress"`] ⇒ envoy-go-strict ACCEPTs only the 3 Percent knobs + OTel `grpc_service.envoy_grpc.cluster_name`+`service_name`, STRICT-REJECTs the rest + `log.Fatalf`s on a missing/non-H2 collector cluster (the documented departure). **Q-decisions (from the BRAINSTORM, unchanged):** (Q0) THIRD Observability row = tracing; (Q1) OTLP-first by-exporter 2-leg [46.1 core+OTLP / 46.2 Zipkin+B3] with a 46.1a/46.1b sub-split contingency; (Q2) FULL sampling fidelity. HEAD is this **SPEC-landing commit** (the `SPEC-46.1.md` + the ROADMAP row-46 SPEC-done stage-note + the STATE active-phase + this `next-prompt.txt` roll, ONE docs-only commit direct-on-master, pushed to origin) atop the phase-46 BRAINSTORM landing `02deeb26` atop the 45.2 IMPL squash `59990afe`. Re-read the shas with `git log --oneline -10`, do NOT assume — further docs-only `next-prompt.txt:` roll commits may stack ATOP this one. Counts at the SPEC (the exit invariant — re-verify, do NOT assume; UNCHANGED, docs-only): stat surface **1191** (H2 cluster; non-H2 **1187**) / fixtures **87** (`ls -d test/fixtures/[0-9][0-9][0-9][0-9]* | wc -l` == 87; tail `0085-otlp-access-log-operators`) / fuzzers **46** (`grep -rh '^func Fuzz' --include='*.go' . | wc -l` == 46) / BackendKind tail **38** / DECISIONS tail **ADR-0259**, next-free **ADR-0260**.

**ROW 44 (`grpc-access-log`) `done`**; **ROW 45 (`otlp-access-log`) `done`**; **ROW 46 (`tracing`) `in-progress`** (46.1 SPEC done; flips `done` once BOTH legs land). The **Observability FAMILY STAYS OPEN**. **The next action is the 46.1 (core+OTLP) PLAN.** ANCHORS **ADR-0260** (its §Context DRAFT is in SPEC-46.1.md §13; the §Decision/§Consequences body lands at the 46.1 IMPL per ADR-0044).

First confirm state via `git log --oneline -10` (expect HEAD at this SPEC-landing commit atop the phase-46 BRAINSTORM landing `02deeb26` atop the 45.2 IMPL squash `59990afe`; counts as above; re-read the shas, do NOT assume). Confirm `git status` clean + master == origin/master.

## NEXT STEP — the 46.1 (core+OTLP) tracing PLAN
This is a PLAN stage — docs-only, direct-on-master (the house TDD-task-spine shape mirroring `PLAN-45.1.md`/`PLAN-45.2.md`). The deliverable is `docs/envoy-go/phases/46-tracing/PLAN-46.1.md` — the ~11–13-task TDD spine (SPEC-46.1.md §10) — resolving the §12 PLAN/IMPL D-questions. **The FIRST-ORDER PLAN decision is D-TRACE-SPLIT** (the ADR-0045 re-check with REAL LoC): the full-fidelity engine [sampling precedence + the UUID byte-14 stamp/preserve + W3C `traceparent` extract/continue/inject + the span model + the 16-attr roster + the `OTLPTracesClient` + the batching exporter] is LARGE — decide whether 46.1 stays ONE leg or sub-splits BY-CONCERN into **46.1a** (the sampling/request-id decision engine + the HCM-scoped `tracing.*` stats — OBSERVABLE via `x-request-id` generation + the sampling counters before any span emits) and **46.1b** (the span model + propagation + the OTLP exporter + the `0086` differential). The other §12 D-questions to resolve: D-TRACE-CONFIG-HOME (the `TracingConfig` package placement + the acyclic `bootstrap → tracing` import check), D-TRACE-RNG-SEAM (the injectable `RandSource` for the deterministic differential + the cap-branch unit tests), D-TRACE-RECEIVER-WIRING (the `test/helpers/otlptrace` driver-owned receiver shape + the `0086` shared-bridge reachability + whether the continuation prong needs a backend-echo helper), D-TRACE-STATS-FINAL (the exact +N via an IMPL registration test — firm 5 HCM-scoped + which exporter counters; `timer_flushed` keep/drop), D-TRACE-FUZZER (land `FuzzExtractTraceparent` + `FuzzStampRequestID` ⇒ 46 → 47/48; reconcile the `^func Fuzz` total per `reference_fuzzer_count_docs_drift`), D-TRACE-SPANEND-PLUMBING (the in-flight span handle threads the existing per-request context to the `accesslog_emit.go` seam — prefer the shared context the access-log Record already uses). Run the plan-document-reviewer loop. Then the 46.1 IMPL (subagent-driven in a fresh worktree per `feedback_execution_style` · `feedback_git_worktrees`).

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** The next stage is the 46.1 PLAN (docs-only, direct-on-master; no worktree — the doc commits direct on master).
- **Process anchors:** ADR-0106 (per-leg roadmap rows; row 46 `in-progress`) · ADR-0045 (split-gate — the D-TRACE-SPLIT 46.1a/46.1b re-check, the live PLAN contingency) · ADR-0041/0080 (silent-ignore vs strict-reject — phase 46 LIFTS the HCM `tracing` field from silent-ignore) · ADR-0016 (proto blank-import registry) · ADR-0158 (the `grpcclient.Dialer` + typed-wrapper layer — the `OTLPTracesClient` precedent) · ADR-0258/0259 (the phase-45 OTLP sink + operator engine — the OTLP module + the unary-Export batching-sink the trace exporter reuses) · ADR-0044 (ADR §Context-at-SPEC [done, SPEC §13], body-at-IMPL) · ADR-0227 (the contrib reference) · next-free **ADR-0260** (46.1 core+OTLP), then **ADR-0261** (46.2 Zipkin+B3).
- **The substrates the 46.1 PLAN builds on (verified at the SPEC):** the HCM `tracing` field (`bootstrap.go:354` HCM unmarshal — currently silent-ignored) · the request-lifecycle seams (`connection.go:448` span-start `startTime`, `accesslog_emit.go:19`/`:45` span-end) · the no-`x-request-id` invariant phase 46 revises (`router.go:763`) · the `grpcclient.Dialer` + `NewOTLPLogsClient` typed wrapper (`grpcclient.go:339`) · the `otlpsink.go` batching pattern (`NewOTLPAccessLogSink :81`/`Submit :111`/`run :155`/`Close :135`) · the `go.opentelemetry.io/proto/otlp v1.0.0` module (`trace/v1` `Span` + `collector/trace/v1` `TraceService.Export`, UNARY) · the tracing config protos at `go-control-plane/envoy v1.32.4`. NEW: the `internal/tracing` package; the trace receiver is DRIVER-OWNED in `test/helpers/otlptrace` (BackendKind STAYS 38).

## Read first (in order)
1. THIS FILE — the cold-start router (the 46.1 SPEC is DONE + LANDED + pushed; row 46 `in-progress`; the next action is the 46.1 core+OTLP PLAN).
2. `docs/envoy-go/phases/46-tracing/SPEC-46.1.md` — the charter (the §10 task spine, the §11 EXECUTED D-TRACE-* pins, the §12 PLAN D-questions [esp. D-TRACE-SPLIT], the §13 ADR-0260 §Context draft).
3. `docs/envoy-go/phases/46-tracing/BRAINSTORM.md` — the parent (the 2-leg split, the full-fidelity intent, the §8 deferred items).
4. `docs/envoy-go/STATE.md` — the active-phase header (`phase 46.1 SPEC done`) + the recorded NEXT (the 46.1 PLAN) + the count figures.
5. `docs/envoy-go/ROADMAP.md` — row 46 (`tracing`, `in-progress`, the 46.1-SPEC stage-note) + the `### Observability family` heading.
6. `docs/envoy-go/DECISIONS.md` — **ADR-0259** (the 45.2 as-built) + **ADR-0258** (45.1) + next-free **ADR-0260** (the 46.1 anchor — body lands at the 46.1 IMPL).
7. `docs/envoy-go/phases/45-otlp-access-log/PLAN-45.1.md` / `PLAN-45.2.md` — the PLAN house shape (the TDD-task-spine the 46.1 PLAN mirrors).
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `reference_streaming_sink_differential_framing`, `reference_differential_grpc_receiver_driver_owned`, `reference_docker_probe_bridge_network`, `reference_dynamic_stat_name_charset_guard`, `reference_wire_format_both_sides_see_same_bytes`, `reference_full_suite_race_after_background_mutator`, `feedback_execution_style`, `feedback_git_worktrees`, `feedback_subagents_no_push`, `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`).
