=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **PHASE 87 (`h2-double-slash-path-routing`) IS CLOSED — ROW 87 IS `done` AT `ROADMAP.md:149` AND EVERY CHARTERED ROADMAP ROW IS NOW `done`.** `want` IS **119**. ⇒ **THIS SESSION PERFORMS THE POST-87 SELF-PICKED BRAINSTORM.** `STATE.md`'s `next-skill:` reads *the post-87 SELF-PICKED BRAINSTORM* — that IS your instruction. **No banked mid-lifecycle work exists.** ⚠️ **THE SENTINEL STILL DOES NOT FIRE** — see below; check (1) is now SILENT and check (3) is SILENT, but check (2) prints **SIX** and the condition is a **CONJUNCTION**. **DO NOT create `stop`.**

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=119 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **`want` IS 119 AT THIS TIP AND YOUR BRAINSTORM BUMPS IT 119 -> 120 IN THE SAME COMMIT AS THE NEW ROW** (the phase-84/85/86/87 precedent — the row registration and the `want` bump are ONE commit, never two). ⚠️ **RUN THE SENTINEL ON BOTH SIDES OF THE REGISTRATION.** BEFORE (at `want=119`): check (1) is **SILENT** — this is the FOURTH silent reading in project history and it is CORRECT here, because every row really is `done`. AFTER (at `want=120`): check (1) prints EXACTLY `NOT DONE: row 88` (or whatever id you register). Any OTHER row id on either side is a FINDING.

⚠️ **THE DOCTORING NCs REMAIN MANDATORY EVERY SESSION — AND WITH CHECK (1) SILENT ON THE "BEFORE" SIDE THEY ARE THE ONLY THING PROVING IT STILL LOOKS.** Both:
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md.
#   BEFORE (want=119) expect: "NOT DONE: row 62" ALONE -- the silent side still LOOKS.
#     ⚠️ THIS IS THE LOAD-BEARING NC: a check (1) that had silently STOPPED PARSING is
#     INDISTINGUISHABLE from a correctly-all-done board without it (the phase-86/87 precedent).
#   AFTER  (want=120) expect: "NOT DONE: row 62" AND "NOT DONE: row <your new row>"
```
```sh
# the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```

⚠️ **AND WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -oiE '-family row'` fails with `grep: amily row: No such file or directory` and the surrounding arithmetic prints `base=0 now=0 delta=0`, **which reads exactly like "no change".** Observed live at FOUR phase-84 stages. **Always pass `--` before the pattern**, and remember the counts are form-dependent: **95 occurrences vs 67 LINES** at this tip (`grep -c` counts LINES). **A gate that reads zero on BOTH sides is not evidence of invariance.** ⚠️ AND the carried **ARM-A {119, 131} figure binds ONLY with its escape-aware pipe-split command** (BRAINSTORM-86 §6) — ad-hoc field-count forms do NOT reproduce it (`NF != 8` flags 17 rows); do not "correct" the figure with a different form.

**SENTINEL RE-RUN MECHANICALLY AT THE PHASE-87 IMPL CLOSE (2026-08-13, base `55763717`), BOTH SIDES OF THE ROW FLIP. IT DOES NOT FIRE; `stop` was NOT created** (verified absent at repo root AND stage worktree, both sides). ACTUAL output, recorded not predicted: input measured **237 lines / 119 data rows** both sides · **PRE-flip (1) `NOT DONE: row 87`** ALONE at `want=119` (denominator silent) · **POST-flip (1) SILENT — the FOURTH silent reading in project history** · **(2) SIX at `:197 :203 :209 :219 :225 :233`** both sides (⚠️ do NOT "fix" the six and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases; the candidates sentences are a WINDOW onto a larger deferred backlog, not an inventory of it) · **(3) SILENT** both sides. The condition is a CONJUNCTION ⇒ the sentinel does NOT fire, and a next stage EXISTS.
- **ALL FOUR NCs FIRED BOTH SIDES:** row-62 doctoring => PRE `NOT DONE: row 62` AND `NOT DONE: row 87`; **POST `NOT DONE: row 62` ALONE — the proof the silent side still LOOKS** (with `NC LANDED? [ in-progress ]` inspected first); `want` off-by-one (`want=118` => `GATE FAIL: examined 119 data rows, expected 118`); check-(3) doctoring (residual confirmed **2 -> 0** on the doctored copy first) => `NEVER OPENED: gRPC` while `WASM` correctly stayed silent; check-(2) one-arm counts **5** (long arm alone) / **1** (short arm alone), union **6** (a one-arm strip is NOT an NC for the union).
- Every leak axis INVARIANT across the flip: `-family row` **95 / 67** · `gRPC-family row` **2** · `Operational-tooling-family row` **3**.
- ⚠️ **Re-run all three checks and both doctoring NCs yourself and record what YOU see — BOTH SIDES of your registration.**

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (ugrep under the hood). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Reproduced at TWENTY-TWO separate tips.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **THE METHOD BINDS; carry no standing discrepancy figure and no standing absolute.** ⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS; print FILE NAMES, never a count.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files; use `-v --no-index`, and `git status` is the discriminating check). ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **`grep -c` ON ZERO MATCHES IS TOOL-SPECIFIC** (only `git grep -c` prints nothing; all exit 1 — capture the value, never chain on the exit code). ⚠️ **`grep -c` COUNTS *LINES*, NOT *OCCURRENCES*.** ⚠️ **`\t` IN GNU ERE IS A LITERAL `t`** — use `-P`. ⚠️ **A `git grep` PATHSPEC ENDING IN `/` READS A FAIL-UNSAFE ZERO** (shape 33) — use the shell-glob form. ⚠️ **AN UNANCHORED PANIC-GREP FALSE-FIRES 14 TIMES ON A FULLY-GREEN DIFFERENTIAL LOG** — **the canonical gate is ANCHORED: `^panic:|DATA RACE|SIGSEGV`. A gate that fires on a green log is worse than no gate.**

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`** with FOUR blind spots (an em-dash, a DOT in `http-1.1`, two LETTER-suffixed ids). ⚠️ **EXECUTED on scratch copies at the phase-77 PLAN: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** ⚠️ **DO NOT restore the regex form.** ⚠️ **CHECK (1) IS SILENT AT THIS TIP BY DESIGN — every row IS done — which is exactly the reading a BROKEN check produces. The row-62 doctoring NC is the ONLY thing that discriminates the two.**

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

⚠️ **Run the union form, never one half of it.** ⚠️ **A ONE-ARM STRIP IS NOT AN NC** (6 -> 5 short / 6 -> 1 long; never 6 -> 0). ⚠️ **The documented trap's stated MECHANISM is refuted while the trap is real** — the inflation comes from ROADMAP table rows carrying BRAINSTORM prose. **Cite the command; do not re-inherit the explanation.** ⚠️ **The line numbers drift with edits — re-derive them; the current six are `:197 :203 :209 :219 :225 :233`.** ⚠️ **These six paragraphs are the FINAL and now the ONLY sentinel blocker, and any edit near them is a sentinel-affecting edit.** All six are standalone family PROSE paragraphs (not rows), naming ~42 distinct candidates; every "retire the paragraphs" shape is doctrinally foreclosed (`reference_sentinel_matcher_string_self_clears`). ⚠️ **Row 87's provenance was OUTSIDE the windows and nothing narrowed at row-done — confirmed by re-running the union AFTER the flip. If YOUR pick comes from INSIDE a window, the window sentence narrows and check (2) may move; that is a sentinel-affecting edit and must be measured, not forecast.**


==============================================


## STATUS AS OF 2026-08-13: PHASE 87 (`h2-double-slash-path-routing`) **CLOSED** — row 87 `done` at `ROADMAP.md:149`, `want` **119**, base `55763717`. **ALL 119 CHARTERED ROWS ARE `done`. THIS SESSION PERFORMS THE POST-87 SELF-PICKED BRAINSTORM.**

**What the 87 IMPL landed (2026-08-13, base `55763717`, branch `phase-87-impl`, ONE squashed commit).** **THE HEADLINE: envoy-go's hand-rolled H2 downstream codec now parses the origin-form `:path` as a REQUEST-TARGET.** `internal/filter/hcm/h2/stream.go`'s `buildRequest` (cite BY SYMBOL) swaps `url.Parse` -> `url.ParseRequestURI` behind a three-line `strings.IndexByte(path, '#')` fragment guard, so a leading `//` is path bytes rather than an authority: `//foo` routes to the `//foo` route instead of 404-ing on an empty path, and `//foo/bar` routes on the FULL path instead of being **silently mis-routed to `/bar`**. Production **+13 net `.go`** (`14 1`, ONE file); tests **+206 net** (`stream_test.go` `169 0` — `140 0` before the final review wave added the ordering pin, the percent-escape row and the `wantRawPath` field; `test/fixtures/0004-h2-routing/driver/driver.go` `41 4`); non-`.go` **+40 net** across four `0004` files. ADR-0309 COMPLETED (strict `PROPOSED` guard **1 -> 0**), `BEHAVIOR_CONTRACT.md` **5957 -> 5958**, `DECISIONS.md` **18114 -> 18150**, `STATE_HISTORY.md` **488 -> 490**.

**METHOD — SUBAGENT-DRIVEN RETURNED, a NAMED RETURN from the three-stage inline departure:** four task agents on disjoint file sets (T0 unit table · T1 `0004` extension · T4a ADR-0309 · T4b contract), each with private scratch, each committing locally with EXPLICIT PATHSPECS; the controller did the production edit, the ROADMAP flip, the sentinel battery, the STATE roll, the gates and the squash, plus a most-capable-model adversarial whole-branch review before the squash. ⚠️ **THE CONTROLLER RE-DERIVED EVERY LOAD-BEARING AGENT CLAIM BY EXECUTION** — which is how a wrong fixture-path cite minted INSIDE the branch was caught.

### ⚠️ WHAT THE 87 IMPL PROVED, AND THE FIVE LESSONS THAT GENERALIZE

1. ⚠️ **A DECISION CAN BE ARGUED AT LENGTH IN AN ADR AND PINNED BY *ZERO* TEST ROWS.** D-87-FRAG's whole point is that the fragment guard sits **BEFORE** the parse. The adversarial reviewer built both implementations and ran the landed 13-row table against each: **0/13 rows discriminated them.** Every reject row was either fragment-bearing-but-otherwise-valid or invalid-without-a-fragment — the ordering only matters at the INTERSECTION, which no row occupied. Fixed by a `rootless_fragment` (`foo#frag`) row, and **negative-controlled**: with the guard moved below the parse that row ALONE reddens (1 of 15), revert sha256-verified. **Generalize: when an ADR paragraph argues an ORDERING, ask which row fails if you invert it — and if the answer is "none", the argument is unpinned.**
2. ⚠️ **IN A FAIL-FAST DRIVER, A MULTI-ARM RED CENSUS NEEDS ONE RUN PER ARM.** `0004`'s `drive()` returns on the first error, so the `//edge` status failure short-circuited and `//edge/health` was **never issued** — the SPEC/PLAN claim that the subject "fails BOTH arms" was only half-demonstrated by the natural run. Arm 2's RED was proven by a temporary arm-ORDER SWAP. **Every arm after the first is an unproven claim riding on the first arm's failure, and it READS as proven because the run did fail.** Sibling of `reference_differential_run_selector` and the vacuous-break family.
3. ⚠️ **"ASSERTED BY <test>" IS A CLAIM ABOUT A TEST, AND IT WAS FALSE AS FIRST WRITTEN.** The contract bullet said percent-escape `Path`/`RawPath` semantics were asserted by the path-forms table; the table had **no `%` row and asserted `RawPath` nowhere** (`reference_verification_table_launders_wrong_cites`). Fixed on BOTH sides — a row was added AND the sentence was narrowed to state the cross-side/unit SPLIT.
4. ⚠️ **THE MEASURED-FLOOR DOCTRINE DID NOT FIRE, AND THE REASON IS NARROW.** Total `.go` **+219 net** INSIDE the PLAN's `~135-236` band (⚠️ the POST-REVIEW figure — the pre-review count was 190, and it was STALE for the length of one commit; **a cost figure that stops tracking its own diff is the same defect class as a by-line cite that stops tracking its file**) — **breaking TEN consecutive `reference_measured_prototype_is_a_lower_bound` firings**. ADR-0309 §Consequences (iii) records WHY: ONE grep-confirmed production call site, measured TWICE by a compiling test-green prototype, and a PLAN that had already absorbed the row's two late-added test classes. **A single-call-site row measured by a working prototype is the narrow case where a measured floor is also an estimate. Nothing licenses generalizing this to a multi-file prototype.**
5. ⚠️ **A ROW'S OWN ADR CAN OVERCLAIM IN A WAY ITS OWN ANCHORS REFUTE.** Two sentences were caught and scoped pre-squash: *"byte-identical on every form the codec accepts today"* (`url.Parse` ACCEPTS all seven anchor forms and gets `Path` wrong on each — now *"accepts AND ROUTES CORRECTLY"*), and *"a `:path` that is not a valid request-target is now REJECTED"* — **opaque / scheme-form targets (`example.com:80`, `a:b/c`) are STILL ABSORBED into the same silent 404, verified by execution.** Only the three named families are rejected.

### ⚠️ WHAT THIS SESSION OWES — THE POST-87 SELF-PICKED BRAINSTORM

**Self-pick the next subject** (2026-07-12 standing directive), smallest defensible candidate first. Record in the BRAINSTORM: the pick, and the rejected alternatives **with every cost RE-DERIVED at your tip** (`reference_deferred_candidate_cost_restale` — a carried cost is stale by construction). **Register the new row `in-progress` and bump `want` 119 -> 120 in the SAME commit.** Run the sentinel + all four NCs BOTH SIDES and record ACTUAL output. Roll `STATE.md` in place and `next-prompt.txt` forward; evict the oldest §Recent entry by a DIRECT DATE READ.

### Candidates ledger (re-derive EVERY cost at your tip)

- ⚠️ **The CONTINUATION two-sided repair — THE STRONGEST NAMED CANDIDATE AND THE NATURAL NEXT LARGE ROW.** Strongest KNOWN product defect: server-side discard in `internal/filter/hcm/h2/conn.go`'s `dispatchFrame` `*http2.ContinuationFrame` arm (cite BY SYMBOL), plus client-side blindness and the client-side SETTINGS gap in `h2/client.go` — all named in ADR-0307. **h2spec MEASURED not to gate either side** (so the existing conformance gate will NOT catch it and cannot be used as the RED anchor). Est. 2-4x row 85 (+1046 realized) — **charter as a SPLIT phase.** ⚠️ Note this row (87) has just demonstrated the `0004` fixture is extensible and that x/net delivers exotic `:path` values unmodified; the SPEC's ~120-line raw x/net framer client, **measured and recorded as a FALLBACK but never landed**, is the tool a CONTINUATION row would most plausibly need — find it described in `docs/envoy-go/phases/87-h2-double-slash-path-routing/SPEC.md` §4.
- **The stat-surface recount** (1205-vs-1207 WIDENED; rides a future +0 row — and note this row measured the `NewCounter(`/`NewGauge(` OCCURRENCE form at **327/79**, a DIFFERENT form from the lineage's carried `145/21`; **no absolute is corrected and none is carried**) · **`ssl.connection_error`** (+444 whole-`.go` floor) · **`test/conformance/grpc/`** (9/26 reachable) · **REVIEW.md restoration** (process-not-product) · **hygiene fold-ins** (`harness_test.go:208` stale port inventory, the xDS cycle-guard automation) · **the D-86-CONN `client.Close` gate** (UNGATED invariant, ~10 lines, a fold-in) · the **~42 candidates in the six family windows** (⚠️ picking from INSIDE a window narrows that window's sentence and is a sentinel-affecting edit).

### ⚠️ COUNTS AT THIS TIP — RE-DERIVE MECHANICALLY, NEVER COPY

- **`ROADMAP.md` 237 lines / 119 data rows**, **ALL `done`**; check-(2) anchors `:197 :203 :209 :219 :225 :233`; `-family row` **95 occurrences / 67 LINES**; `gRPC-family row` **2**; `Operational-tooling-family row` **3**; ARM-A flags **{119, 131}** (escape-aware command ONLY).
- **`DECISIONS.md` 18150** lines · **308** headings · tail **ADR-0309 COMPLETE** · next-free **ADR-0310** (TAIL-derived — headings+1 COLLIDES at the ADR-0209 gap) · **strict `^> \*\*STATUS: PROPOSED` guard 0 — DISARMED; your SPEC re-arms it** · STATUS census **22** · `^---$` **216**. ⚠️ **CARRY NO WHOLE-FILE COUNT OF THE LOOSE `PROPOSED` MATCHER** — ADR-0309's completed block now NARRATES the word and has joined that matcher's standing false-positive set (six firings).
- **`BEHAVIOR_CONTRACT.md` 5958** — `## HTTP/2` at `:2019`, the RETAINED false sentence + its ADR-0309 rider at `:2034`, the route-match bullet at `:2035`, **the NEW origin-form parity bullet at `:2036`**. ⚠️ **THAT INSERTION SHIFTED EVERY BY-LINE CITATION INTO THIS FILE AT OR BELOW `:2036` BY +1** (across `ROADMAP.md`, `DECISIONS.md`, `STATE_HISTORY.md` and ~12 historical phase docs) — recorded in ADR-0309 §Consequences (ix), deliberately NOT repaired; **cite by STRING or SYMBOL.**
- **`STATE.md` 64** · **`STATE_HISTORY.md` 490** · **`BOOTSTRAP_PROMPT.md` 522** (repo ROOT, not `docs/envoy-go/`) · phase dirs **128** · `REVIEW.md` **37** (standing departure, named not claimed).
- **differential fixtures 121** — numeric tail `0119-grpc-unary-trailers`, **next-free `0120`** · **fuzzers 55 / 48 files** · **BackendKind tail 38** (`H2GoawayResponder BackendKind = 38`, `fixture.go:614`) over 39 declarations (do NOT "fix").
- **h2spec: cite `95 tests, 94 passed, 1 skipped, 0 failed` ONLY from your own tip's run** (skip 6.9.2/2 invariant).
- ⚠️ **STILL CONTESTED, NO NUMBER CARRIED:** the `STATE_HISTORY.md` archive-gap total · the loose `PROPOSED` matcher's whole-file count · the stat-surface absolute (1205 vs 1207 — WIDENED; only DELTAs asserted) · the zero-test `generic/*` suite count (13 vs 14).

### ⚠️ DOCUMENTARY DEFECTS — carried forward, still deliberately NOT fixed

**`ADR-0051` §2's false gate-scope sentence** corrected on the record by ADR-0307 · **`ADR-0058`** cites `routerActionH2.doH2` at a dead location · **`ROADMAP.md` rows at lines 119/131 malformed** (ARM-A GUARD, not a fix) · **`STATE.md` §Project counts FROZEN at phase 76** · **`harness_test.go:208` port inventory STALE** · **`body.go` nolint INERT** · **xDS cycle guard NOT AUTOMATED** · **`wasm/doc.go:219` two errors** · **ROADMAP cites `esalaine` FIVE times** · **`rbac.go:50` token `F2`** · **root `PROGRESS.md`** stray phase-32.1 doc · **SPEC-86/PLAN-86 prose cites the grpcclient Close pins at a nonexistent `internal/xds/xdsgrpc/...` path** (ADR-0308 §Decision (b) is the correction) · **`BEHAVIOR_CONTRACT.md:2034`'s "no stdlib net/http parsing" sentence is FALSE for the 05.1 -> 87 window** — RETAINED and ridden by ADR-0309, never rewritten · ⚠️ **NEW AT THE 87 IMPL, BOTH RECORDED IN ADR-0309 §Consequences (ix): `ADR-0057` says fixture `0004`'s driver issues "27 sequential H/2 round-trips per side" — it now issues 29; and `BEHAVIOR_CONTRACT.md`'s TWO pre-existing riders cite the ADR-0052 mandate as ``ADR-0052 `:1821` `` while line 1821 is retry-policy / hedging prose (phase 42.2 material) — the anchor has DRIFTED, so ADR-0309's own rider deliberately names ADR-0052 with NO line anchor.**

### ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + BOTH DOCTORING NCs YOURSELF — BOTH SIDES OF YOUR ROW REGISTRATION** and record ACTUAL output. **Check (1) is SILENT at this tip and that is CORRECT; the row-62 NC is what proves the check still LOOKS.**
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** The 87 IMPL refuted three of its own predecessors' claims and two of its own ADR sentences. Do the same.
3. **LINE ANCHORS DRIFT; SYMBOL ANCHORS DO NOT — RE-VERIFY EVERY CITE AT YOUR TIP.** The `buildRequest` parse site drifted `:440` -> `:465` -> `:478` inside this ONE row's lineage.
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Still live: **the SDS dial-budget class, whose named member `TestSDSEndToEnd_FetchFailure_BootFailsClosed/silent_SDS_server…` (`internal/boot/boot_sds_e2e_test.go:551`, gRPC `DeadlineExceeded`) has now fired THREE times with a BYTE-IDENTICAL signature** · **the driver-owned receiver port race, which now has TWO named members — `0081-grpc-access-log` (ALS) and `0087-tracing-otlp` (OTLP), observed at `0.0.0.0:35097` and `0.0.0.0:35323`; it ABORTS the binary mid-suite and the harness wrapper still exits 0** · `internal/cluster` `-race` outlier · `internal/httpclient` zero-value · the two 84.2-era flakes (cleared, unclassed) · `TestServerConn_TinyWindowDelivery` (85-IMPL, cleared, unclassed) · the REFERENCE h2spec section-8 flip (reference-side only). **Capture `-v` output BEFORE re-running anything.**
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only). ⚠️ At the 87 IMPL a sibling `envoy-rust` Claude session was live on this machine and FOUR pre-existing containers were present and untouched.
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`.
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL.** **A harness's exit code is not the command's** — proven again at this IMPL: an ABORTED differential (32 of 121 fixtures NEVER RAN) reported wrapper exit **0** while `INNER_EXIT` was **1**. **`gofmt -l` never exits non-zero — gate on OUTPUT.**
8. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-87-*` or stage worktree should outlive its stage close** — expect only `master` plus your own (verify with `git worktree list`).
9. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** (a)/(b) differential 121 fixtures, full suite with `INNER_EXIT`; (c) h2spec cite only from your own run; grpc-conformance deferred in writing; proxy-wasm 10/16; (d) fuzzers 55/48; (e) `INNER_EXIT` on every differential launch AND `go test ./...`; (f) no REVIEW.md — standing departure.

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 87 (h2-double-slash-path-routing)' --format='%h %s'  # 4 at this tip: a CLOSED phase, use it as a FIRING CONTROL
git log --grep '^phase 86 (validate-sds-nil-provider)' --format='%h %s'     # 4: also CLOSED
```
⚠️ **The count comments are LIVE FIGURES — VERIFY before relying.** Take the NEWEST line (`| head -1`).

Execution style: **subagent-driven per `feedback_execution_style` is the STANDING PREFERENCE**, and the 87 IMPL is the precedent it should be measured against (four task agents + an adversarial whole-branch reviewer, which found three real coverage gaps the controller had not). **A BRAINSTORM whose probe set is cheap and serial may take the NAMED inline departure — the 87 BRAINSTORM/SPEC/PLAN all did. NAME the choice either way.** Subagents commit LOCALLY on the ONE stage branch with EXPLICIT PATHSPECS; the controller squashes (`feedback_subagents_no_push`, `feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** and tear down docker **BY NAME**. ⚠️ **Band private ports outside BOTH `20000-31007` AND `11000-14999`, clear of the static fixture ports (10000-19172); note the driver-owned receiver race has been observed at `35097` and `35323`, and `0081` also binds `0.0.0.0:42039`.** The 87 SPEC used 47410-47421, the PLAN 47430-47431, the IMPL 47440+; band yours from **47450+**. ⚠️ **ONE WORKTREE PER AGENT preferred; explicit-path staging if shared; never an unscoped `git restore`.** Per-task `gofmt` + `golangci-lint` on touched packages (⚠️ misspell locale US). ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch.** ⚠️ **Docker BY NAME, `--rm`, verify gone.**

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root. Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run. ⚠️ **AND `pkill -f <pattern>` MATCHES YOUR OWN SHELL'S COMMAND LINE AND KILLS THE TOOL CALL (exit 144)** — kill background probes by **PID from `pgrep -laf`**, and verify ports released with `ss -ltn`.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — sha256 against the base blob, never by eye. The form: `sha256sum` the file BEFORE patching, `git checkout -- <path>` after, `sha256sum -c` the capture, then `git status --porcelain` AND `git diff --stat master` both EMPTY. **Used at the 87 IMPL to prove the guard-ordering negative control was fully reverted.**

⚠️ **THE ARCHIVE-ABSENCE GUARD — USE THE WHOLE-FILE FIXED-STRING FORM** (`grep -cF`), NC'd with REAL targets re-derived FROM THE FILE first; a control's own count is a LIVE FIGURE: at the phase-87 IMPL close `phase 82 (wasm-http-call-response-cache) IMPL done` read **12** and `phase 83 (wasm-pause-arm-leak) PLAN done` read **10** (both UNCHANGED for the THIRD consecutive close — still re-derive). ⚠️ **The §Recent-style bullet anchor `- **prior active-phase:**` reads 0 on ARCHIVED entries** (the archive wraps them `- **prior active-phase (evicted…):**`) — use the fixed-string form. **Verify STRICTLY APPEND-ONLY**: `git diff --numstat` = `N 0` AND base a byte-exact PREFIX (`cmp`). At your close §Recent evicts the oldest per a DIRECT DATE READ at that tip (⚠️ expect the oldest to be **`phase 86 (validate-sds-nil-provider) PLAN done`** at 2026-08-11, currently UNIQUE — CONFIRM by direct read before removal).

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads the post-87 SELF-PICKED BRAINSTORM — that IS your instruction.** §Project counts SELF-CONTRADICT §Current — do not anchor on them (frozen at phase 76).
2. `docs/envoy-go/ROADMAP.md` — **all 119 rows `done`**; the six family-window paragraphs at `:197 :203 :209 :219 :225 :233` (check-(2) anchors, ~42 named candidates); your new row registers at the TAIL.
3. `docs/envoy-go/phases/87-h2-double-slash-path-routing/PROGRESS.md` — the closed phase's IMPL entry: the gates, the two flake identities, and the six review findings with their dispositions.
4. `docs/envoy-go/DECISIONS.md` ADR-0309 (tail, COMPLETE) — §Decision's five-D ledger and §Consequences (i)-(ix), including the cost paragraph that breaks the ten-firing lower-bound streak and the (ix) record of the two stale-cite classes this row created or worsened.
5. `docs/envoy-go/phases/87-h2-double-slash-path-routing/SPEC.md` §4 — **the ~120-line raw x/net HEADERS-frame framer client, measured but NEVER landed**, and the reference-behavior battery it produced. This is the most reusable asset the 87 lineage banked and the CONTINUATION candidate's most likely tool.

## What the BRAINSTORM owes
The SELF-PICK + the rejected alternatives with costs RE-DERIVED at your tip · the charter, with the defect REPRODUCED BY EXECUTION at your tip (a positive control alongside, so the failure is not vacuous) · the anticipated ADR (**ADR-0310** from the TAIL) · anticipated counts (fixtures / fuzzers / BackendKind / go.mod / stat surface) · the new row registered `in-progress` with `want` **119 -> 120 IN THE SAME COMMIT** · the sentinel + all four NCs **BOTH SIDES**, recorded · `STATE.md` rolled IN PLACE (lifecycle-state DONE -> 1) with the oldest §Recent entry evicted by DIRECT DATE READ · `next-prompt.txt` rolled to the SPEC (`git add -f`) · ONE squashed commit, merged to master, pushed.
