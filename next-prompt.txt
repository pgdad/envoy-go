=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**Check (1) is NOT permanently silent.** It is silent only while no phase is open. Row 56 is `in-progress`, so **check (1) prints `NOT DONE: row 56`** and will keep doing so until the 56.2 IMPL flips it. **The sentinel is an AND of all three checks**; (2) and (3) carry the remaining work.

**SENTINEL RE-CHECKED 2026-07-10 (post-phase-56.2-SPEC) — it does NOT fire. Do NOT create `stop`.** Evidence:
- (1) prints `NOT DONE: row 56` ⇒ phase 56 is OPEN (56.1 headers landed; the 56.2 bodies leg is SPEC'd but its PLAN + IMPL remain).
- (2) prints the Observability family's deferred candidates ⇒ the **Observability family STAYS OPEN**.
- (3) prints `NEVER OPENED: HTTP/3`, `gRPC`, `xDS`, `Runtime`, `WASM` ⇒ FIVE families never opened; the **Operational-tooling family** also STAYS OPEN.

**STATUS AS OF 2026-07-10: phase 56.2 (`http-tap-filter`, BODIES leg) SPEC is DONE and LANDED.** Row 56 is `in-progress`. Counts UNCHANGED at the SPEC (docs-only): stat surface **1201** · fixtures **101** (tail `0099-http-tap-headers`) · fuzzers **53** · BackendKind tail **38** · DECISIONS tail **ADR-0273** (next-free **ADR-0274**). Anticipated at the 56.2 IMPL: fixtures **102** (`0100-http-tap-bodies`), DECISIONS tail **ADR-0274**, all other counts **+0**.

**Locating the stage squash (a router cannot name its own sha — writing the sha changes it).** Identify it by SUBJECT, never by sha and never by position:
```sh
git log --oneline --grep='^phase 56.2 (http-tap-filter, bodies leg) SPEC' -1
```
This stage folded its own `next-prompt.txt` roll INTO the squash (per `reference_next_prompt_tracked_despite_gitignore`), so the squash **IS** the master tip — *unless* a router-only fixup commit sits on top. **Do not assume either shape.** The `--grep` above is correct in every shape.

**THE NEXT ACTION: the phase-56.2 (`http-tap-filter`, BODIES leg) PLAN.**

==============================================

## What THIS session does (a PLAN, lifecycle-state 2 → 3)

This session decomposes `SPEC-56.2.md` into a **TDD spine** (writes `PLAN-56.2.md` + `PROGRESS-56.2.md`, updates the phase `README.md`). **Docs-only, NO production code.** Fresh worktree `.worktrees/phase-56.2-plan`, branch `phase-56.2-http-tap-filter-plan`. Row 56 STAYS `in-progress`. DECISIONS tail STAYS **ADR-0273** at this PLAN (per ADR-0044; ADR-0274 §Decision/§Consequences land at the 56.2 IMPL).

1. Re-verify the cold-start baselines against the CURRENT master tip (`git log --oneline -1` — do not assume): fixtures **101**, fuzzers **53**, DECISIONS tail **ADR-0273**, BackendKind **38**, stat surface **1201**.
2. **Read the landed 56.2 SPEC as the charter** — `docs/envoy-go/phases/56-http-tap-filter/SPEC-56.2.md`, esp. §1.1 (the six `AMEND-TAP-BODY2-*` findings), §3 (the body-capture design on the proven spine), §8 (the `0100-http-tap-bodies` differential + the 4 deliberate breaks), §10 (the ~6-8 task sketch), §11 (the empirical pins), §12 (the FOUR PLAN D-questions). Also re-read the landed 56.1 artifacts it builds on (SPEC-56.1, PROGRESS-56.1 §Findings, ADR-0273 in DECISIONS.md, `internal/filter/http/tap/{config.go,tap.go,trace.go,sink.go}`).
3. **Write `PLAN-56.2.md`** — decompose SPEC-56.2 §10 into a concrete TDD task spine, RE-DERIVING every `file:line` and proto field from source (`feedback_brief_citations_not_evidence` — this row has paid out THREE times; the SPEC's citations are NOT evidence, re-derive them). Each task: RED (a failing test) → GREEN → REFACTOR, with the deliberate break that proves the assertion is LIVE (design breaks that can actually FIRE — the 56.1 IMPL caught TWO MORE vacuous-proof classes; see PROGRESS-56.1 §Findings IMPL-1/IMPL-2). **RESOLVE the four §12 D-questions** with a pinned decision each:
   - **D-TAP-BODY-UTF8** — the non-UTF8 AS_STRING disposition (Go's `protojson.Marshal` ERRORS on invalid UTF-8 in a `string` field — CONFIRMED empirically at the SPEC; the sink swallows the error → a silent whole-trace drop). SPEC RECOMMENDS `strings.ToValidUTF8` sanitize (a documented DEPARTURE, lossless-of-the-trace). Pin it.
   - **D-TAP-BODY-ASBYTES-DIFF** — AS_BYTES render cross-side (a 2nd listener in `0100`) vs a unit test. SPEC RECOMMENDS the unit test (AS_BYTES is Go-native-deterministic base64). Pin it.
   - **D-TAP-BODY-CHUNKTEST** — >32KiB multi-`DecodeData` accumulation proven differentially vs a unit test feeding two `DecodeData` calls. SPEC RECOMMENDS the unit test. Pin it.
   - **D-TAP-BODY-EARLYOUT** — whether to skip body buffering when the predicate is already resolvably-False at `DecodeHeaders`. SPEC RECOMMENDS deferring the optimization (buffer up to the cap, discard on no-match). Pin it, and if adopted it must NOT change any observable trace.
4. **The ADR-0045 split gate is CONSUMED (SPEC-56.1 §3.0 / SPEC-56.2 §10)** — 56.2 is additive on a proven spine (~6-8 tasks, WELL under the ~15 ceiling). The PLAN must still RE-CHECK the concrete decomposition against the gate and CONFIRM no re-split; the escape valve stays armed only if the count somehow exceeds ~15 (not anticipated).
5. **Honor the SPEC's load-bearing pins in the task design:** the strict-`>` truncation boundary (a body EXACTLY at the cap is NOT truncated — the `0100` boundary arm at body-length==C JUSTIFIES break (c)); the default cap **1024** when `max_buffered_*` is unset; `truncated` ALWAYS emitted (EmitDefaultValues); the format→oneof render (AS_STRING/AS_BYTES); the cap-0-nonempty → empty-but-truncated body vs the never-fired-hook → omitted body distinction (a `sawReqBody`/`sawRespBody` flag); tap returns `DataContinue` ALWAYS (read-only observer — never `DataStopIterationAndBuffer`/`OverwriteBody`); decode-side chunk accumulation (the `buffer` filter precedent). The `0100` fixture REUSES `HTTPEchoBody` (BackendKind 38, +0) and drives UTF-8-safe bodies only (AMEND-TAP-BODY2-UTF8).
6. Land the PLAN (commit + push per `feedback_push_to_origin`; docs-only), then **ROLL THIS FILE FORWARD to the phase-56.2 IMPL**.

## Honor throughout (the discipline, unchanged)
- **Lifecycle = one stage per session.** This session: the phase-56.2 PLAN ONLY (docs-only).
- **`next-prompt.txt` IS TRACKED** (it appears in `.gitignore` line 2, but a `.gitignore` entry does not apply to an already-tracked file — `git ls-files --error-unmatch next-prompt.txt` succeeds). Edit it inside the stage worktree and fold it into the squash (`reference_next_prompt_tracked_despite_gitignore`). A router can't name its own sha — locate the squash by SUBJECT (`git log --grep`), never by position.
- **A BRIEF'S CITATIONS ARE NOT EVIDENCE** (`feedback_brief_citations_not_evidence`). Re-derive every `file:line` from source; never verify a citation against the document that made it. This row has paid out FOUR times now (a false double-`OnDestroy` at the 56.1 SPEC; two vacuous-proof defects at the 56.1 PLAN; two MORE vacuous-proof classes caught AT the 56.1 IMPL by controller-run break verification — PROGRESS-56.1 §Findings IMPL-1/IMPL-2). The 56.2 SPEC's data-hook citations (`types.go:56/67`, `connection.go:629-656/561`, `h2dispatch.go:496-540/505`, `router.go:625`, `buffer/buffer.go:65-243`, `compressor.go:1029-1080`, `callbacks.go:350`, `fixture.go:165-175` HTTPEchoBody) were re-derived from source at the SPEC — but re-derive them AGAIN at the PLAN.
- **Deliberate-break discipline** (design breaks that FIRE): the strict-`>` truncation break needs a body-length-==-cap arm or it is vacuous; a body-present assertion needs a break that makes bodies ABSENT (inert hooks); the AS_STRING oneof break needs a decode that sees an empty `as_string`. The `-run` selector must be the FULL `TestDifferential/0100-http-tap-bodies` (`reference_differential_run_selector`); breaks use `-count=1` (`reference_differential_break_protocol_count1`); confirm WHICH assertion fired (`reference_deliberate_break_wrong_assertion`); `t.Errorf`-per-property, never `t.Fatalf` mid-list (`reference_fatalf_makes_assertions_unreachable`).
- **protojson detrand** (ADR-0273 §Consequences): the body render is ALSO subject to `internal/detrand` whitespace randomization (confirmed at the SPEC — `"as_string":  "abc"` double-space under one build seed). NEVER compare protojson output raw-byte-for-byte across build configs — use `encoding/json.Compact` canonicalization + detrand-robust substring pins. The differential decodes payloads; it never byte-compares.
- **Process anchors:** ADR-0106 (no parent rollup — row 56 flips `done` ONLY at the 56.2 IMPL) · ADR-0044 (§Context at SPEC; §Decision/§Consequences at IMPL — ADR-0274 body lands at the 56.2 IMPL) · ADR-0045 (the split gate is CONSUMED; 56.2 must NOT absorb 56.1 spillover) · ADR-0052 (atomic six-gate bundle) · ADR-0071 (two-step filter factory) · ADR-0080 (byte-stable rejects) · ADR-0196 (the encode-side `ResponseStatus()` accessor) · ADR-0227 (reference `envoyproxy/envoy:contrib-v1.37.2`) · ADR-0273 (the landed headers leg) · next-free **ADR-0274** (the bodies leg).

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-56.2 PLAN is next).
2. `docs/envoy-go/phases/56-http-tap-filter/SPEC-56.2.md` — the bodies-leg charter; esp. §1.1 (AMEND-TAP-BODY2-*), §3 (the design), §5 (the body proto roster), §8 (the `0100` differential + breaks), §10 (the task sketch), §11 (the empirical pins), §12 (the four PLAN D-questions), §13 (the ADR-0274 §Context draft).
3. `docs/envoy-go/phases/56-http-tap-filter/SPEC-56.1.md` + `PROGRESS-56.1.md` — the landed headers spine + §Findings log (IMPL-1/2/3) + the 14-row break ledger (the traps to honor).
4. `docs/envoy-go/DECISIONS.md` — **ADR-0273** (the landed headers decision — the marshal opts, the sink, the reject roster) + ADR-0044/0045/0052/0071/0080/0106/0196 for process.
5. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the phase-56.1 block (the detrand caveat + reject roster + coverage boundaries the 56.2 bundle extends).
6. The landed 56.1 code the PLAN builds on: `internal/filter/http/tap/{config.go,tap.go,trace.go,sink.go}`, and the seams SPEC-56.2 cites (`internal/filter/http/types.go`, `internal/filter/hcm/{connection.go,h2dispatch.go}`, `internal/filter/http/{buffer,compressor}/`, `test/differential/fixture/fixture.go` HTTPEchoBody).
7. `docs/envoy-go/STATE.md` — the active-phase header + full history.
8. `docs/envoy-go/ROADMAP.md` — §Schema, row 56, and the Observability family paragraph.
9. `docs/envoy-go/phases/56-http-tap-filter/README.md` — the phase orientation (56.1 done, 56.2 SPEC'd, PLAN next).
10. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline and the `reference_*` traps.
