START a fresh cold-start session for envoy-go — **the phase-36.2 (`load-balancer-ring-hash-http`) IMPL is DONE and LANDED on master; the next lifecycle step is the NEXT Load-balancing-family candidate BRAINSTORM** (`superpowers:brainstorming` — the family STAYS OPEN; **maglev** is the recommended cheap follow-on, reusing the ADR-0235 ctx-carried-hash-key seam + the ADR-0236/0237 hash producers UNCHANGED). The substantive master tip is the phase-36.2 IMPL squash **`7a38a20`** (`phase 36.2 IMPL: load-balancer-ring-hash-http — the HTTP route hash_policy ring_hash producer (8-task TDD spine; ADR-0237)`; pushed to `origin/master`; confirm via `git log --oneline -5`). The literal live master tip is the docs-only `next-prompt.txt` SHA-fill **`aa734ea`** (which named the prior roll-forward `a043417` — the one that named `7a38a20` substantive and routed this cold-start to the next Load-balancing candidate BRAINSTORM — as the docs-only live tip above the IMPL squash `7a38a20`) sitting directly above the IMPL squash `7a38a20`. The phase-36.2 IMPL executed the 8-task TDD PLAN subagent-driven, six-gate GREEN: the ONE additive exported `cluster.HashHeaderValues` (seed-chained XXH64 fold) + `parseRouteHashPolicies` at the hcm config-build boundary (proto-free `router.HashPolicy` descriptor) + the `applyHashKey` fold (`rotl64(prev,1)^new`, first-verbatim, nullopt-skip, `terminal` short-circuit) at the 4 router dial sites + the uniform ctx-carried `router.WithDownstreamRemoteAddr` + the `0062-lb-ring-hash-http` NAT-transparent TRUE cross-side affinity differential. The substantive predecessor below it is the phase-36.2 PLAN squash `85207c6` (with its docs-only roll-forwards `00cfbd8`/`5d2eeb2` between); below that the phase-36.2 SPEC squash `da295ab`, the phase-36.1 IMPL squash `e8e8c48`. The 36.2 IMPL squash carries the full producer (`internal/cluster/hash.go`, `internal/filter/hcm/{config.go,actions.go,connection.go,h2dispatch.go}`, `internal/filter/http/router/{router.go,router_h2.go}`) + the `0062` fixture + the completion bundle (BEHAVIOR_CONTRACT 36.2 HTTP-plane addendum + the full ADR-0237 + STATE/ROADMAP row-36 advance `36.1 done, 36.2 done` flat family row per ADR-0106 + PROGRESS.md six-gate evidence).

## What this session does — open the NEXT Load-balancing candidate BRAINSTORM

The phase-36 (`load-balancer-ring-hash`) family row is COMPLETE (36.1 tcp source_ip plane + 36.2 HTTP route hash_policy plane — the CONSUMED 36.1/36.2 by-plane split is now CLOSED). The Load-balancing family STAYS OPEN with **5 candidates** remaining: **maglev**, subset LB, locality-weighted LB, priority load balancing, panic thresholds. The next session runs `superpowers:brainstorming` to scope the next candidate — **maglev (phase 37)** is the recommended cheap follow-on per STATE: it reuses the ADR-0235 ctx-carried-hash-key seam + the SAME hash producers (the tcp_proxy `source_ip` plane at 36.1 + the HTTP route `hash_policy` plane at 36.2 — `cluster.HashSourceIP`/`cluster.HashHeaderValues`/`applyHashKey` all LANDED) UNCHANGED; maglev swaps the ADR-0236 Ketama RING for a fixed-size MAGLEV lookup table (a different `cluster.loadBalancer` impl behind the SAME `Pick(hashKey, hasHash)` seam — anticipate NO seam change, NO new producer, NO new stat-producer plane; the hash data planes are already built). The BRAINSTORM (not this routing note) settles: the MVP envelope (default table size, `MaglevLbConfig.table_size`), the enum-path placement (legacy `Cluster.LbPolicy MAGLEV` like RING_HASH, or the `load_balancing_policy` extension point — anticipate legacy enum, the RING_HASH precedent), the byte-stable-reject surface, the ADR count (anticipate 1 policy ADR mirroring ADR-0236), and the differential fixture shape (a `0063-lb-maglev` affinity+spread proof reusing the `0061`/`0062` modular-invariant convention).

## Counts at master tip (phase 36.2 IMPL done — confirm at the next BRAINSTORM)

- stat surface **1119** (`ls`/recipe per BEHAVIOR_CONTRACT). A maglev policy is anticipated to add its OWN ring/table-describing gauges (mirroring 36.1's 3 `ring_hash_lb.*` gauges — the BRAINSTORM settles the maglev stat surface); the hash producers add 0.
- differential fixtures **64** (tail `0062-lb-ring-hash-http`; `ls -d test/fixtures/[0-9]* | wc -l`).
- fuzzers **42** (tail `FuzzZookeeperResponseDecode` — sorted-unique; canonical recipe `grep -rh "^func Fuzz" $(find ./internal -name fuzz_test.go) | wc -l`). BackendKind tail **33** (`TCPThriftResponder`).
- DECISIONS tail **ADR-0237** (next-free **ADR-0238**). ADR-0235 (the ctx-carried-hash-key seam) + ADR-0236 (the ring_hash policy) + ADR-0237 (the HTTP route hash_policy producer) are the durable LB assets maglev builds on.
- exported LB surface (all LANDED, maglev CONSUMES): `cluster.WithHashKey`/`HashSourceIP`/`HashHeaderValues` + `router.WithDownstreamRemoteAddr`/`applyHashKey`/`HashPolicy`. The `Pick(hashKey, hasHash)` seam (ADR-0235) is the extension point maglev plugs a new table impl into.
- reference pin: `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227). Toolchain: Go 1.26.x; golangci-lint 1.64.8 (ADR-0009); go-control-plane `/envoy` v1.32.4 (ADR-0008 — `MaglevLbConfig` already in the pinned module).

## Read first (in order)

1. THIS FILE — the post-phase-36.2-IMPL cold-start (next: the next Load-balancing candidate BRAINSTORM — maglev recommended).
2. `docs/envoy-go/STATE.md` — active-phase `phase 36.2 (load-balancer-ring-hash-http) done`; the lifecycle-state (the family stays OPEN, 5 candidates, maglev the cheap follow-on); the counts.
3. `docs/envoy-go/ROADMAP.md` — row 36 (`36.1 done, 36.2 done`; flat family row per ADR-0106; the Load-balancing family OPEN, 5 candidates remain).
4. `docs/envoy-go/DECISIONS.md` — **ADR-0235** (the ctx-carried-hash-key seam — `Pick(hashKey,hasHash)`) + **ADR-0236** (the ring_hash policy — the STYLE TEMPLATE for the next policy ADR) + **ADR-0237** (the HTTP route hash_policy producer); + ADR-0044 (ADR bodies at the IMPL in-place) / ADR-0052 (the atomic six-gate landing) / ADR-0080 (byte-stable parse-reject wording) / ADR-0106 (flat family row) / ADR-0227 (the contrib reference image) / ADR-0045 (the SPEC split-gate).
5. `docs/envoy-go/phases/36-load-balancer-ring-hash/` + `phases/36.2-load-balancer-ring-hash-http/` — the AS-BUILT ring_hash family (the landed seam + `ringHashLB` + the BOTH hash producer planes maglev reuses); the PROGRESS.md ledgers are the precedent shape for the maglev phase docs.
6. The landed LB code maglev plugs into: `internal/cluster/{cluster.go (WithHashKey/hashKeyFrom/Pick seam), ringhash.go (ringHashLB — the impl maglev mirrors as a table), hash.go (HashSourceIP/HashHeaderValues/xxHash64/murmurHash2), manager.go (the LB-policy gate), loadbalancer.go}` + the producers `internal/filter/{tcpproxy/filter.go (source_ip), http/router/router.go (applyHashKey/HashPolicy)}`.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_execution_style` (subagent-driven at the IMPL), `feedback_git_worktrees`, `feedback_subagents_no_push` + `feedback_push_to_origin` (controller squash-merges + pushes at stage-close), `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_path_targeting`/`_detach`, `reference_differential_break_protocol_count1`, `reference_differential_run_selector`, `reference_differential_band_sigma_margin`, `reference_differential_hash_key_cross_side_infeasible` (a maglev source_ip key is cross-side-infeasible like 0061 → subject-side affinity; a header/request-key fixture is NAT-transparent like 0062 → TRUE cross-side affinity), `reference_proto_roster_extraction_digits` (digit-inclusive regexes for the maglev proto roster), `reference_docker_probe_bridge_network`.

## Honor throughout

- The lifecycle is BRAINSTORM → SPEC → PLAN → IMPL, each its own session. The next session is the next Load-balancing candidate **BRAINSTORM** (`superpowers:brainstorming` — scope the MVP envelope + the D-questions; anticipate maglev as the cheap follow-on reusing the landed seam + both hash producer planes; the SPEC/PLAN/IMPL follow in their own sessions).
- `feedback_execution_style` · `feedback_git_worktrees` · `feedback_subagents_no_push` + `feedback_push_to_origin` · `feedback_pertask_gofmt_lint`.
- ADR-0106 (flat family row — the maglev leg opens its own portion) · ADR-0044 (ADR bodies at the IMPL, in-place) · ADR-0052 (the atomic-landing six-gate) · ADR-0080 (byte-stable parse-reject wording) · ADR-0227 (the contrib reference image; live probes per `reference_docker_probe_bridge_network`) · ADR-0045 (the SPEC split-gate re-check).
