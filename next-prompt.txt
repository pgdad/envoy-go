=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-CHECKED 2026-07-14 (after the phase-63 PLAN landed + row 63 STAYS `in-progress`, run MECHANICALLY) — it does NOT fire. Do NOT create `stop`.** State:
- (1) prints `NOT DONE: row 63` — row 63 `tracing-custom-tags-environment` STAYS `in-progress` (a row flips `done` only at its IMPL six-gate, ADR-0106 — the SOLE leg here). ⇒ check (1) prints ⇒ the sentinel does NOT fire.
- (2) prints THREE live "candidates:" sentences — HTTP/3, xDS, and Observability ⇒ three families STAY OPEN. The Observability sentence STILL reads `custom_tags (environment/metadata)` at this PLAN (it NARROWS to `(metadata)` only at the phase-63 IMPL — `reference_sentinel_deferred_sentence_live_vs_historical`).
- (3) prints `NEVER OPENED: gRPC`, `Runtime`, `WASM` ⇒ THREE families never opened. Operational-tooling also STAYS OPEN.

Any one of the three printing ⇒ the sentinel does NOT fire.

**STATUS AS OF 2026-07-14: the phase-63 PLAN LANDED and ROADMAP row 63 STAYS `in-progress`.** The PLAN (`docs/envoy-go/phases/63-tracing-custom-tags-environment/PLAN.md`) is a **9-task** TDD spine (SINGLE FLAT ROW, ADR-0045 escape-valve UNCONSUMED) decomposing SPEC §10, with `PROGRESS.md` scaffolded. **The task spine:** T1 the ADDITIVE half (`kindEnvironment` const + `EnvName` field in `config.go`; `import "os"` + a `case kindEnvironment:` in `resolve.go` — `os.LookupEnv`, omit-iff-resolved-empty, IGNORES `headerLookup`; `resolve_test.go` matrix present/absent+default/absent+no-default→omit/**present-empty→omit** via `t.Setenv`; build green — parse still rejects environment so nothing constructs a `kindEnvironment` spec in production); T2 lift the `environment` reject in `parseCustomTags` (`config.go:195-196`) → ACCEPT arm + empty-name PGV-parity reject appended through the EXISTING first-wins-dedup path (reserves the slot NATURALLY — arm F; dedup block UNCHANGED) + `config_test.go` (REMOVE the `environment` reject row `:462-466` → an `environment-empty-name` reject; `metadata` row STAYS; ADD accept + env-dedup tests) — **ZERO field-type / call-site change**; T3 span arm-B (resolved-environment-upserts-over-built-in; `BuildServerSpan` UNCHANGED); T4 Zipkin encoder unit test; T5 the `0106-tracing-custom-tags-environment` OTLP fixture (clone `0105`; `environment{name: PATH}`; PLAIN GET; KEY-PRESENCE + value-non-empty cross-side, value differs per side; register `runner_test.go:132`); T6 `FuzzHCMConfigParse` environment seed (fuzzers stay 55); T7 BEHAVIOR_CONTRACT (`environment` → CONSUMED; narrow the departure to `metadata`); T8 verify (six-gate + full 108-dir differential); T9 ADR-0284 body + STATE + ROADMAP (row 63 `done` + deferred-sentence NARROW) + PROGRESS close + router roll. **D-ENV-RESOLVE-TIME = Option B is PINNED by the SPEC** (§1.1/§11 arm F/G) — the IMPL does NOT re-open it. **Every `file:line` RE-DERIVED against master tip `33d5ed4c`** this PLAN session (`feedback_brief_citations_not_evidence`); `kindEnvironment`/`EnvName`/`os` RE-GREP-confirmed collision-free. **ADR-0284 §Context DRAFTED at the SPEC** (§Decision/§Consequences at the IMPL, ADR-0044; DECISIONS tail STAYS ADR-0283 at this PLAN). Per the lifecycle discipline, the NEXT stage is the phase-63 IMPL.

==============================================

## What THIS session does (the phase-63 IMPL)

Perform the IMPL stage (lifecycle-state 3 → done) for phase 63 (`tracing-custom-tags-environment`), executing `docs/envoy-go/phases/63-tracing-custom-tags-environment/PLAN.md` task-by-task. This stage makes PRODUCTION `.go` changes in a FRESH worktree off master (`.worktrees/phase-63-impl`, branch `phase-63-tracing-environment-custom-tag-impl`; `feedback_git_worktrees`). SUBAGENT-DRIVEN per `feedback_execution_style` (`superpowers:subagent-driven-development`): a fresh subagent per task that commits LOCALLY only (`feedback_subagent_autocommit_claudemd`, `feedback_subagents_no_push`); the controller verifies each commit, cleans any leak files, squashes at stage-close, re-runs the six-gate + full 108-dir differential on the frozen HEAD, and PUSHES (`feedback_push_to_origin`).

Execute the 9-task PLAN spine faithfully, honoring the SPEC's PINNED decisions (do NOT re-open them):
- **D-ENV-RESOLVE-TIME = Option B** (a `kindEnvironment` spec stored at parse + a `kindEnvironment` resolver case; NOT parse-time-static). SPEC §11 arm F/G decided it.
- **D-ENV-EMPTYVAL** — the resolver MUST use `os.LookupEnv` (present-ness), NOT `os.Getenv`; the tag is OMITTED iff the RESOLVED value is empty (a present-empty var, an absent var with no/empty default). This DIVERGES from phase-62's `request_header` present-empty edge (which emits `""`) — do NOT copy that into the environment arm. The T1 Step-7 break #2 (Getenv-vs-LookupEnv) proves this is live.
- **ZERO call-site / field-type change** (SPEC §3.4) — the phase-62 seam already threads H1/H2/H3; `TracingConfig.CustomTags` is already `[]CustomTagSpec`. The T1/T2 ordering (additive-first, then the parse arm) keeps the build green throughout.
- The **fixture** is KEY-PRESENCE via `PATH` (value differs per side ⇒ value-non-empty, NOT value-equality); break the ASSERTION's expected KEY only (`reference_vacuous_break_receiver_normalizes`); use the FULL `-run 'TestDifferential/0106-tracing-custom-tags-environment'` selector (`reference_differential_run_selector`); every differential/liveness break runs `-count=1` (`reference_differential_break_protocol_count1`).
- **RE-DERIVE every PLAN §File-Structure `file:line` against the CURRENT master tip** (`git fetch` first) before each subagent edits — the PLAN's citations were RE-DERIVED against `33d5ed4c`; a subagent copies a brief's citation verbatim (`feedback_brief_citations_not_evidence`). The fixture NUMBER (`0106`) + the DECISIONS/fuzzer/fixture counts must be RE-VERIFIED at Task 1 (`ls -d test/fixtures/[0-9]*/ | tail -1`; `grep -rn '^func Fuzz' --include='*.go' . | wc -l`).

**Honor throughout (the discipline, unchanged):**
- **Lifecycle = one stage per session.** This session: the IMPL ONLY. After it lands: roll to the roller's next self-pick BRAINSTORM (the sentinel governs termination).
- **Process anchors:** ADR-0044 (ADR-0284 §Decision/§Consequences land at THIS IMPL — the SPEC drafted only §Context; DECISIONS tail ADR-0283 → **ADR-0284**) · ADR-0045 (SINGLE FLAT ROW — 9 tasks, escape-valve UNCONSUMED) · ADR-0106 (row 63 flips `done` at THIS six-gate — the SOLE leg) · `reference_roadmap_split_phase_row_done`.
- **`feedback_pertask_gofmt_lint`** — each code task runs `gofmt -l` + `golangci-lint run` on touched pkgs + `go build ./...` + the touched-package `go test` before its commit.
- **`reference_fuzzer_count_docs_drift`** — the T6 seed must NOT move the fuzzer count off 55; reconcile actual `^func Fuzz` before AND after.
- **`reference_detrand_race_catches_protojson_value_substring` / `reference_full_suite_race_after_background_mutator`** — the T8 six-gate runs `-race` on BOTH `internal/tracing` and `internal/filter/hcm`.
- **`reference_fatalf_makes_assertions_unreachable`** — `Errorf` per independent property in the fixture driver + `resolve_test.go`.
- **`reference_new_subpackage_pulls_transitive_module`** — `go mod tidy -diff` anticipated EMPTY (`CustomTag_Environment` getter + stdlib `os` are already-resolved; no NEW sub-package), but RE-CHECK `git diff go.mod` after tidy.
- **`next-prompt.txt` IS TRACKED** (`reference_next_prompt_tracked_despite_gitignore`) — edit it inside the stage worktree and fold into the squash; locate the stage squash by SUBJECT (`git log --grep`), never by position.
- **Worktree discipline** (`feedback_git_worktrees`, `feedback_subagent_worktree_detach`, `feedback_subagent_worktree_path_targeting`) — pin the canonical worktree root + worktree-relative paths; on a deliberate break, `git restore` only (no checkout-sha/amend) + re-verify the branch each task; verify the MAIN checkout stays clean.
- Read the phase-62 IMPL close (STATE.md prior-active-phase `phase 62 ... IMPL done`) + `docs/envoy-go/phases/62-tracing-custom-tags-request-header/PROGRESS.md` as the template — the SAME custom-tag engine, the SAME 9-task IMPL shape.

**ADR reservation ledger (provisional; each finalized at its IMPL in landing order):** ADR-0277 = phase-59 tracing-custom-tags-literal (LANDED) · ADR-0278 = xDS 60.1 (LANDED) · ADR-0279 = HTTP/3 61.1 (LANDED) · ADR-0280 = xDS 60.2 (LANDED) · ADR-0281 = HTTP/3 61.2 (LANDED) · ADR-0282 = HTTP/3 61.3 (LANDED) · ADR-0283 = phase-62 tracing-custom-tags-request-header (LANDED) · **ADR-0284 = phase-63 tracing-custom-tags-environment** (RESERVED — §Context DRAFTED at the SPEC [SPEC §13]; §Decision/§Consequences land at THIS phase-63 IMPL) · next-free unreserved **ADR-0285**.

**Current counts (VERIFIED at the phase-63 PLAN against master tip `33d5ed4c` — RE-VERIFY at session start, `git fetch` first):** fixtures **107** (`ls -d test/fixtures/[0-9]*/ | wc -l`; tail `0105-tracing-custom-tags-request-header`) · fuzzers **55** · DECISIONS tail **ADR-0283** (next-free **ADR-0284**) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** · go.mod modules **2** (`quic-go` direct + `qpack` indirect). A PLAN is DOCS-ONLY ⇒ ALL counts were UNCHANGED at that stage. **Anticipated at the phase-63 IMPL:** fixtures **107 → 108** (`0106-tracing-custom-tags-environment`) · fuzzers **55 (+0)** · stat **1201 (+0)** · DECISIONS tail **ADR-0284** · new packages **0** · new modules **0**; row 63 → `done`.

**Locating the phase-63 PLAN stage squash** (a router cannot name its own sha — identify by SUBJECT, never by position):
```sh
git log --oneline --grep='^phase 63 (tracing-custom-tags-environment) PLAN' -1
```

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-63 IMPL is next; row 63 is `in-progress`).
2. `docs/envoy-go/STATE.md` — the active-phase header (phase 63 PLAN done; NEXT = the phase-63 IMPL).
3. `docs/envoy-go/phases/63-tracing-custom-tags-environment/PLAN.md` — the 9-task TDD spine this session executes; `PROGRESS.md` — the scaffolded task checklist + the D-ENV-* dispositions (fill the baseline block at Task 1).
4. `docs/envoy-go/phases/63-tracing-custom-tags-environment/SPEC.md` — the PINNED design (D-ENV-RESOLVE-TIME=Option B, the §11 live-probe block incl. arm F/G, the §12 edit-site roster, the ADR-0284 §Context) the PLAN decomposes.
5. `docs/envoy-go/phases/62-tracing-custom-tags-request-header/{PLAN,PROGRESS}.md` + `docs/envoy-go/DECISIONS.md` ADR-0277/ADR-0283 — the LANDED custom_tag engine (`CustomTagSpec`/`ResolveCustomTags`/`upsertAttr`/first-wins-dedup), the seam this `environment` row extends; the phase-62 IMPL is the template.
6. `docs/envoy-go/ROADMAP.md` — the Observability family (row 63 + its narrowed-at-IMPL deferred sentence), the xDS + HTTP/3 live deferred sentences, and the never-opened gRPC/Runtime/WASM + Operational-tooling families.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the tracing `reference_*` traps (esp. `reference_tracing_custom_tag_override_builtin`, `reference_fuzzer_count_docs_drift`, `reference_detrand_race_catches_protojson_value_substring`) + the differential-harness traps.
