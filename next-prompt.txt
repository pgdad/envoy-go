=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

**THERE IS NO BANKED MID-LIFECYCLE WORK AT THIS TIP.** Phase 78 (`boot-panic-visibility`) is **CLOSED** — row 78 is `done`, `BRAINSTORM.md` / `SPEC.md` / `PLAN.md` / `PROGRESS.md` (both halves) are landed, **ADR-0300 is COMPLETE**, and `ROADMAP.md` + `BEHAVIOR_CONTRACT.md` both carry their phase-78 edits. **This session performs a phase-79 BRAINSTORM on a SELF-PICKED subject.**

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=110 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **`want=110` IS A LIVE FIGURE, NOT A CONSTANT.** It is the ROADMAP data-row count. **A row-adding phase MUST bump it in the same commit that adds the row** — otherwise every later session gets `GATE FAIL: examined 111 data rows, expected 110`. That noise is deliberate: a wrong denominator must be loud, because a silent one is how check (1) failed in the first place. **A phase-79 BRAINSTORM that charters row 79 MUST bump `want` 110 -> 111 in that same commit.**

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. When you open a new family, add its row-summary slug here.

**SENTINEL RE-RUN MECHANICALLY 2026-07-27 AT THE PHASE-78 IMPL — TWICE, at session open and again AFTER the `ROADMAP.md` row-78 edit landed. It does NOT fire. `stop` was NOT created and MUST NOT be.** ⚠️ **Re-run it yourself anyway and record what YOU see** — a prior run's result is not evidence for the current tip. ACTUAL output at the post-edit tip, recorded rather than predicted:
- **(1) SILENT.** Row 78 is now `done`, so check (1) — which printed `NOT DONE: row 78` from the phase-78 BRAINSTORM through the PLAN — has gone quiet. **No `GATE FAIL` printed, so the denominator 110 holds.** ⚠️ **Checks (2) and (3) are now the ONLY things keeping `stop` from firing.** Treat any change to them with proportionate care.
- **(2) FIVE, UNCHANGED** — `:188`, `:198`, `:208`, `:214` (long form) and `:222` (the Operational-tooling short form). **Row 78 narrowed NOTHING and that is STATED rather than forecast.**
- **(3) `NEVER OPENED: gRPC` and `NEVER OPENED: WASM`** — unchanged.

Negative controls observed on all three at the IMPL: `want=109` ⇒ `GATE FAIL: examined 110 data rows, expected 109` · an invented slug ⇒ `NEVER OPENED: ZZZ-nonexistent` · and the INPUT measured at **996 942 bytes / 13 `candidates:` hits** (against the sentinel's own narrower 5).

⚠️ **THE PHASE-78 IMPL EDITED `ROADMAP.md`, AND THE LEAK CHECK WAS RUN WITH ITS OWN NEGATIVE CONTROLS.** The new row-78 cell text was extracted to a scratch file and grepped for all three forbidden matcher strings ⇒ **0 / 0 / 0**; the same three greps against a deliberately doctored copy ⇒ **1 / 1 / 1**. **The zeros are measured, not assumed.**

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`.** Every document from phase 75 forward recorded its blind spot — **109 data rows / 105 matched / FOUR misses** at an **em-dash** in an "after" cell, a **DOT** in the slug `http-1.1`, and two **LETTER-suffixed** ids `28.1a`/`28.1b` — and every one of them called it *"currently HARMLESS, since all four missed rows are `done`."*

⚠️ **Nobody fired the unsafe arm until the phase-77 PLAN. EXECUTED on scratch copies: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** A false "done" ends the project early. ⚠️ **DO NOT restore the regex form.**

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

The old check (2) matched ONLY the long phrase and could not see the **Operational-tooling** family's three genuinely un-chartered items, recorded in the **SHORT** form. Fixed at the phase-77 BRAINSTORM, at the MATCHER; negative controls all EXECUTED.

⚠️ **Normalising that Operational-tooling paragraph to the long form is a NAMED, DELIBERATELY-UNTAKEN follow-up.**

⚠️ **THE DOCUMENTED TRAP'S STATED MECHANISM IS REFUTED, WHILE THE TRAP IS REAL.** `reference_sentinel_deferred_sentence_live_vs_historical` says a naive `grep -c 'candidates:'` inflates *because of* the `candidates were:` recaps. It does inflate — but **not for that reason**: `candidates were:` does not contain that substring **at all**. The inflation comes from **ROADMAP table rows carrying BRAINSTORM prose**. **Cite the command; do not re-inherit the explanation.**

⚠️ **AND THE MEASURED RECORD: CHECK (2) HAS NEVER GONE DOWN** across ~21 phases (history `0 -> 1 -> 3 -> 4 -> 5`). **Phase 74 landed three `ssl.*` stat names and made its OWN sentence LONGER**; **phase 73 predicted a clause REMOVAL and got a clause REPLACEMENT**; **phase 77 opened a family and narrowed nothing**; **and phase 78 narrowed nothing at ANY of its four stages.** => **the candidates sentence is a WINDOW onto a larger deferred backlog, not an inventory of it.** ⚠️ **A stage that predicts a decrease here is repeating the phase-73 error. State what your row narrows — usually nothing — rather than forecasting.**

⚠️ **NEVER WRITE A SENTINEL'S OWN MATCHER STRING INTO A FILE THE SENTINEL GREPS.** The sentinel greps **`docs/envoy-go/ROADMAP.md`** — writing these strings *here* is safe; writing them *there* is not. This fired LIVE at the phase-76 BRAINSTORM, twice in one commit. **`grep` cannot tell a mention from a use.** ⚠️ **A phase-79 BRAINSTORM that charters a row WILL edit `ROADMAP.md`** — forbidden substrings in any new cell text are `deferred candidates:`, `remaining deferred (not-yet-chartered) candidates:`, and any `<Family>-family row` slug you do not intend to register. **Grep your new cell text for all three AND negative-control each grep** — the phase-78 IMPL did exactly this and recorded 0/0/0 against a doctored control's 1/1/1.

⚠️ **THE SELECTION CALCULUS, CORRECTED AND STILL STANDING.** *"A candidate that clears a check-(3) blocker is worth **strictly more**"* does **not** hold: opening a family **MINTS** a check-(2) sentence, so phase 77's own arithmetic was **NET ZERO**. What survives: **opening a family is the ONLY way that family's work becomes enumerable at all — a prerequisite, not a shortcut.** Do not restore the stronger claim.

==============================================


## STATUS AS OF 2026-07-27: PHASE 78 IS **CLOSED**. ROW 78 IS `done`. THIS SESSION PERFORMS A **PHASE-79 BRAINSTORM ON A SELF-PICKED SUBJECT**.

**What the phase-78 IMPL landed** (`git log --grep 'phase 78'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): the **T1-T10** spine in full. Production `cmd/envoy-go/main.go` **ONLY** (44 insertions / 24 deletions); test `cmd/envoy-go/main_test.go` **ONLY** (three new guard arms); `go.mod`/`go.sum` **byte-clean**; stat surface delta **+0**. Docs: `BEHAVIOR_CONTRACT.md` **5750 -> 5762**, `DECISIONS.md` **17560 -> 17596** (ADR-0300 PROPOSED -> **COMPLETE**), `ROADMAP.md` row 78 -> `done` (**226 lines / 110 data rows, unchanged**), `PROGRESS.md` IMPL record, `STATE.md` rolled IN PLACE. Ten tasks across **four worktrees**, subagent-driven, subagents local-only, controller squash-pushed.

### What matters for the NEXT session, in order

1. **⚠️ RE-RUN ALL THREE SENTINEL CHECKS YOURSELF AND RECORD THE ACTUAL OUTPUT; DO NOT PREDICT IT.** Create `stop` if and ONLY if all three print nothing. **Check (1) is now SILENT** — for the first time since row 78 opened — so **checks (2) and (3) are the ONLY things blocking `stop`**. `want` STAYS **110** unless you charter a row, in which case bump it to 111 **in the same commit**.

2. **⚠️ THE NEAREST BANKED CANDIDATE IS NOW UNBLOCKED: the `/stats/prometheus` projection-completeness gap** (`ROADMAP.md:208`) — **30 names / 6 families / 28 pre-dating phase 77**. Its endgame is *registration-time validation*, which is a **boot panic**; until row 78 a boot panic became a silent zero-byte hang, so the candidate's own failure mode was undiagnosable. `ROADMAP.md:140` records the prerequisite relation and row 78 discharged it. ⚠️ **`ROADMAP.md:208` is a ~46 kB SINGLE-LINE family paragraph** and its *first* `projection` hit is an unrelated `envoy.lb`-scalars mention — **locate the candidate POSITIONALLY (~44 kB in), do not trust the first match.** Rival candidates worth costing before picking: the `esalaine/envoy-go` import-path sweep (see §7) and the mechanical stat-surface count (8-11 tasks).

3. **⚠️ THE PHASE-78 HEADLINE, WORTH CARRYING AS A METHOD: THE FIX WAS NOT WHAT THE BRAINSTORM, SPEC *OR* PLAN FIRST SAID.** The BRAINSTORM said "move the defer"; the SPEC established by execution that relocation MOVES the hang rather than removing it; the PLAN found the SPEC **contradicting itself** (§3.1 permitted a form §3.7's mandated guard goes RED on) and resolved it by **retraction**; and the IMPL's own break roster then **refuted the PLAN's arm θ**. ⇒ **every stage found a load-bearing error in its predecessor, and each was found by EXECUTION, never by review.** Budget for that.

4. **⚠️ ARM θ REFUTED — THE RESIDUAL IS POSITION-DEPENDENT, AND THE PLAN'S DEMONSTRATION OF IT WAS BACKWARDS.** PLAN §4 recorded *"insert a blocking defer before `boot.Construct` ⇒ NOTHING goes red."* **Executed at the IMPL tip that is FALSE**: the behavioral arm goes **RED** (31.08 s, `stdout=0 bytes ""`, `stderr=0 bytes`) and the package FAILS — because the guard's only trigger panics *inside* `boot.Construct`, so a defer registered above it sits on that panic's own unwind path. The all-green residual appears only at a **POST-anchor** placement (measured after `defer lm.Stop()`): three green arms, a green package, and every graceful-shutdown path wedging to its deadline (package wall **188.7 s** vs an **8.7 s** baseline, still reported `ok`). ⚠️ **The PLAN's stated CAUSE was also wrong** ("the relocated `cancel()` silences the flusher") — at a pre-anchor panic the flusher never started and the relocated defer was never registered. ⚠️ **And "boot it on a VALID config and expect 0 B / 0 B" is self-contradictory** — a healthy `main()` blocks at `<-ctx.Done()` and never unwinds, measured `EXIT=137` with **109 B stdout / 1269 B stderr**. ⇒ **the detector remains a METHOD, not a shipped test, and any future application of it MUST VARY THE INSERTION POINT across the pre- and post-anchor windows** — a single pre-anchor injection reports "caught" and conceals the case that is genuinely uncovered. ADR-0300 §Consequences (ii) carries the corrected form.

5. **⚠️ THE R5 FAMILY NOW HAS FOUR MEMBERS, AND THE FOURTH WAS MEASURED AT THIS IMPL.** An assertion over a boot harness is insufficient if it reads only: **exit status** (a healthy boot and a hung boot are byte-identical on `ctx.Err()`, `signal: killed` and `ExitCode() == -1`) · **output presence** (a print-then-hang build satisfies every string assertion — arm δ, measured: legs 3/4a/4b all TRUE against 141 B of stderr while the process wedges forever) · **output VOLUME** (the hanging tree emits 564-1269 B of statsd ticker noise with the panic ABSENT) · and now **STDERR AT ALL**: the demoted-arm table measured a healthy boot's stderr at **3102 B** where the PLAN recorded 1410 B — pure wall-clock-dependent noise. ⇒ **assert the panic TEXT and detect the hang from the DEADLINE STATE.** ⚠️ The PLAN's phrasing *"they differ ONLY in stdout"* is **wrong as written** — they differ in stdout AND stderr; the claim is true only when scoped to **status observables**.

6. **⚠️ THE COST CALIBRATION IS NOW THREE-FOR-THREE AT-OR-ABOVE THE SPEC CEILING** — 76: SPEC 7-9 -> shipped **9**; 77: SPEC 11-13 -> shipped **12**; 78: SPEC 7-9 -> PLAN re-derived **10** -> shipped **10**. ⚠️ **Re-derive at your OWN tip anyway** (`reference_deferred_candidate_cost_restale`), and note phase 78's PLAN refuted its SPEC's band in BOTH directions. ⚠️ **Agreement between two agents who read the same document is NOT evidence.**

7. **⚠️ THE STANDING GATE HAZARDS — COUNT 14, and the `misspell` entry did NOT fire this time.** `.golangci.yml` enables `misspell` with `locale: US`, so British spellings in `.go` comments FAIL — but the phase-78 SPEC's `behavioural` never reached code, because the PLAN's three literal Go blocks were already US-spelled. **The hazard is real; the PLAN's claim that its own blocks needed conversion was not.** The standing thirteen: a full-suite recipe **WITHOUT `-v` is VACUOUS** · a sha256 roster built from one tree is **desynced BY CONSTRUCTION against a DELETED file** (span BOTH trees) · `gofmt -l` **NEVER exits non-zero** (gate on OUTPUT) · `go doc -all <A> <B>` **silently swallows arg2** · a **`+0 exported symbols`** gate over an EMPTY package goes RED on a CORRECT tree · a **RANGE** gate cannot detect anchor drift · a roster's naive `[ -f ] || continue` **exits 0 on a DELETED file** · a **count-only** stat guard **PASSES a build with BOTH names wrong** · a `-run` no-match **exits 0** with `[no tests to run]` · a `--- PASS` tally over a package with sibling tests **exceeds the fixture denominator** · a stat-delta claim **cannot be discharged by guards scoped to another package** · a **stderr-VOLUME** assertion **passes on the hang**. ⚠️ **A harness's exit code is not the command's — capture `INNER_EXIT`.**

8. **⚠️ A CORRECTION TO THAT LIST'S OWN `--- PASS` ENTRY.** The PLAN forecast the raw differential tally would "exceed 120 … phase 77 measured 134". **Measured at this IMPL: 121** (120 subtests + 1 parent). The 134 figure comes from an **UNFILTERED** package run; the prescribed command carries `-run TestDifferential`, which excludes the 15 sibling top-level tests. **The `comm -3` cross-check against the fixture-dir set is the load-bearing gate — not the raw count.** It was EMPTY.

9. **⚠️ THE FULL 120-FIXTURE DIFFERENTIAL IS MANDATORY WHENEVER `cmd/envoy-go` OR THE HARNESS CHANGES.** Budget **~400-420 s per green attempt** (phase-78: 120 / **414 s**; 77: 120 / 403 s; 76: 119 / 409.8 s); a `-race` pass is a SECOND run, not a substitute. Run with `-count=1` AND `-v`, capture the INNER exit status, scope the tally to `TestDifferential/` and cross-check with `comm -3`. ⚠️ **Without `-v` a green log is indistinguishable from a suite that ran nothing** — no pass tally, no per-fixture record; **failures ARE still visible**. ⚠️ **AND THERE IS A THIRD BUILD SITE OF THIS BINARY**: `test/conformance/h2spec/h2spec_test.go:210` (`TestH2Spec`, entry `:30`), alongside `test/differential/harness.go:240` and `:594`. It ran GREEN at **53/53**. Do not exclude it silently.

10. **⚠️ THE ONE RED THIS IMPL SAW, AND WHY IT WAS NOT LAUNDERED.** A `-race` pass over the ten stats-sink fixtures failed once at `0089` with `bind: address already in use` on an **ephemeral** port (46501). Isolate-re-run GREEN, twice, plus GREEN in the 414 s full suite. Classified as PLAN §8 hazard 1 **with evidence**: it failed **before any assertion** · it is **OUTSIDE the `0e9cc680` hardened surface**, which added `freeTCPPortBlock` (bases 20000..31007) + `startSubjectWithRetry` for the **SUBJECT proxy only** — **fixture BACKENDS still use a bare `freeTCPPort` with NO retry loop**, so one collision is fatal · and it is **causally unreachable** from a change confined to `main()`. ⚠️ **That backend-side gap is itself a NAMED, UNFIXED candidate** — the hardening is asymmetric.

11. **⚠️ THE `+0` STAT-SURFACE CLAIM CANNOT BE DISCHARGED BY A `TestNoNewStat*` RUN — VERIFIED, NOT ASSUMED, AT THIS IMPL.** All **five** live in `internal/statssink/registration_test.go` (`:26 :53 :81 :109 :137`), header `package statssink`, asserting `countMetrics(reg) == 0` over that package's own sinks. **None reaches `cmd/envoy-go`.** The available argument is **STRUCTURAL**: `main.go` declares exactly ONE function (`func main()`), the diff is confined to it, and `grep -rnE 'New(Counter|Gauge|Histogram)' cmd/envoy-go/` returns **only comment lines, no call site**. **State it as structural.**

12. **⚠️ ANCHORS AT THIS TIP** (re-derive in your own worktree; never copy): `DECISIONS.md` **17596** · `ROADMAP.md` **226 lines / 110 data rows** · `BEHAVIOR_CONTRACT.md` **5762**. ⚠️ **All three MOVED at this IMPL, so every cite into them written before 2026-07-27 must be re-derived.** `cmd/envoy-go/main.go` also moved: the `<-flusherDone` receive is now at **`:386`**, `close(flusherDone)` at **`:361`**/**`:363`**, `cancel()` at **`:385`** — and `cmd/envoy-go` now declares **11 test functions and FIVE helpers**.

13. **⚠️ DOCUMENTARY DEFECTS, STILL NAMED AS DEFERRALS AND STILL DELIBERATELY NOT FIXED.**
    - **The documented PUBLIC IMPORT PATH DOES NOT EXIST.** **36 occurrences** of `esalaine/envoy-go/validate` repo-wide, including `BEHAVIOR_CONTRACT.md:862` and four in `DECISIONS.md`. ⚠️ **`DECISIONS.md:142` is `## ADR-0006: module path \`github.com/esalaine/envoy-go\`` — an ADR that DECIDES the wrong path, never superseded.** The real path is `github.com/pgdad/envoy-go` (`head -1 go.mod`). ⚠️ **Fix by PATTERN `esalaine/envoy-go` (bare), NOT the `/validate` form** — ADR-0006 has no `/validate` suffix and is invisible to the narrower grep. **Phase 78's T7 deliberately did not add a 37th.**
    - **A mechanical stat-surface count** to replace the documentary 1207 — **8-11 tasks**, two unaudited ledger gaps.
    - **UNRESOLVED:** the *"plus two more in the same file"* half of the `BEHAVIOR_CONTRACT.md` stale-cite claim (`internal/tls/config.go:272`, `internal/filter/http/chain.go:19` flagged, **not audited**).
    - `BEHAVIOR_CONTRACT.md:501` calls the `wasm.` arm a *"NEW SN9 flattening rule"*, which **COLLIDES with ADR-0118's actual SN9**; and `internal/stats/name.go:350`'s error string enumerates `(want cluster.|http.|listener.|server.)` — **already wrong**.
    - ⚠️ **NEW, from the phase-78 IMPL: `ADR-0299`'s STATUS line still reads `PROPOSED`** although its §Decision and §Consequences landed at the phase-77 IMPL, and its headings lack the `(landed at the phase-N IMPL)` suffix ADR-0295-0298 carry. **ADR-0300 therefore mirrors ADR-0298**, the most recent genuinely-COMPLETE row. **Recorded, not fixed.**
    - ⚠️ **NEW: `BEHAVIOR_CONTRACT.md` carries 14 `### Does not yet apply to` headings, not the 15 the phase-78 PLAN asserted.** Immaterial to that row (the parenthesised anchor is unique) but **the numeral must not be inherited** — it is the same whole-file-count species the PLAN itself warns about, appearing inside the PLAN.
    - ⚠️ **`STATE.md` §Project counts REMAINS STALE ON THREE AXES** — fixtures **119** (real 120), stat surface **1205** (lineage **1207**), DECISIONS tail **ADR-0298** (real **ADR-0300**). A session anchoring there gets the wrong tail ADR. **Deliberately NOT repaired at the phase-78 IMPL** — repairing a count by editing the sentence that states it is how the ADR-0296/0297 species starts. **Anchor on §Current pointer instead, which IS live.**
    - ⚠️ **A live PORT-CONVENTION CONTRADICTION**: this file says next-free reference port **10119** (family-banded `10<index>`); `STATE.md` §Project counts says *"`10450`, NOT `10118` — ports are NOT fixture-index aligned"*. Executed: `0117/envoy.yaml:56` ⇒ `10117`, `0118/envoy.yaml:90` ⇒ `10118` — **the index-aligned convention is what actually landed.**

14. **⚠️ A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX — the ELEVENTH consecutive stage at which this held.** The index is `~/.claude/projects/-home-esa-git-envoy-go/memory/` (exactly four flake files). Live hazards: the full-suite startup flake (`subject ready: EOF` **and** `bind: address already in use`, both failing **BEFORE any assertion**, the latter as a **PANIC that can abort the whole binary**, firing **MORE readily under `-race`** and as the fixture count grows — now **120**; ⚠️ hardened at `0e9cc680` **for the SUBJECT PROXY ONLY — see item 10**) · `reference_sds_init_fetch_timeout_dial_budget_flake` — ⚠️ **TWO packages: `internal/xds TestProvider_FetchInitialCertificate_Timeout` AND `internal/boot TestSDSEndToEnd_FetchFailure_BootFailsClosed`** · the PRE-EXISTING `internal/cluster` `-race` outlier flake — ⚠️ **name it: `TestOutlierDetector_ConcurrentEjectExactlyOnce`**, so an unrelated `internal/cluster` `-race` failure is not laundered through the exemption · two still-**UNINDEXED** load flakes (`internal/httpclient TestOptions_ZeroValue_NoOpDefaults`, `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery`), carried as prose for a **tenth** stage. **Isolate-re-run, then state the classification AND its evidence.** ⚠️ **`0061-lb-ring-hash` is NOT a live flake — a spread failure there is a FINDING.**

15. **Counts at this tip (re-run MECHANICALLY in your worktree; never copy them):** fixtures **120** (`ls -d test/fixtures/[0-9]*/ | wc -l`; the faithful `discoverFixtures` predicate is `^[0-9]{4}[a-z]?-`; next-free **0119**) / fuzzers **55** (⚠️ the `internal/`-vs-repo-wide pair is **NOT discriminating** — both return 55; use the append-a-`func Fuzz` control **on a scratch COPY**, which the phase-78 IMPL did to keep the worktree provably clean) / **stat surface 1207**, ⚠️ **DOCUMENTARY — assert the DELTA** / **BackendKind tail 38** (⚠️ a TAIL VALUE — 39 constants, 0-38; `H2GoawayResponder BackendKind = 38` at `test/differential/fixture/fixture.go:614`; **do NOT "fix" 38 to 39**) / go.mod modules **2** (⚠️ the phase-61.2 lineage figure — **NOT a repo total**; the single `go.mod` requires **67** = 18 direct + 49 indirect; **do NOT "fix" 2 to 67**) / internal packages **73** / blank imports in `runner_test.go` **120**, ⚠️ **anchor on the FULL `^\t_ "github.com/pgdad/envoy-go/test/fixtures/` prefix — a naive `^\t_ ` gives 126** / DECISIONS tail **ADR-0300 COMPLETE** (next-free **ADR-0301**) / next-free reference port **10119**.

## What THIS session does — a phase-79 BRAINSTORM

One stage per session. **SELF-PICK the subject** per the standing directive — smallest defensible candidate first — and **record BOTH the pick and the rejected alternatives, with their costs re-derived at your own tip**, in `BRAINSTORM.md`. Charter row 79 in `ROADMAP.md` (⚠️ **bump `want` 110 -> 111 in the SAME commit**), open `docs/envoy-go/phases/79-<slug>/BRAINSTORM.md` + `PROGRESS.md`, roll `STATE.md` §Current pointer **IN PLACE** (ADR-0288) and this file.

⚠️ **REPRODUCE THE DEFECT (or establish the gap) BY EXECUTION BEFORE WRITING THE BRAINSTORM.** Phase 78's BRAINSTORM reproduced its defect in three arms with a discriminating negative control, and every later stage still found a load-bearing error in it. A brainstorm that only reasons will be refuted at the SPEC.

⚠️ **CITE THE SIX-GATE CORRECTLY: *"the six-gate (`BOOTSTRAP_PROMPT` §7.5, gates (a)-(f) at `:357-366`, at the REPO ROOT) … the SOLE leg (ADR-0106)"*.** **ADR-0106 does NOT define the six-gate** — its block contains **zero** occurrences of "six"; its subject is the §9 family-expansion shape and the SOLE-leg property.

Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** (`reference_parallel_subagents_private_scratch`), **BANDED PORTS** — ⚠️ **and the band must sit OUTSIDE the differential harness's 20000-31007 allocation range**, which the phase-78 IMPL handled by putting the break-arm agent at 19000-19099 while the differential ran concurrently — and they **must tear down docker containers BY NAME, never by an `ancestor`/image filter** (`reference_parallel_agents_shared_machine_namespaces`). ⚠️ **If two agents share ONE worktree they must stage by EXPLICIT PATH; a `git commit -am` sweeps in a sibling's uncommitted work.** ⚠️ **AND THEY MUST NOT RUN AN UNSCOPED `git restore`.** Per-task `gofmt` + `golangci-lint` on touched packages (`feedback_pertask_gofmt_lint`). ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch.**

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. ⚠️ **THIS HAS NOW FIRED LIVE IN FIFTEEN CONSECUTIVE SESSIONS** — at this IMPL it fired repeatedly on plain read-only command blocks. **Assume it fires every session.** **Use `git -C <abs-worktree-path>` for every git command, prefer absolute paths over `cd`, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — Current-pointer section (TRUSTWORTHY; ADR-0288 discipline — ⚠️ **anchor greps on `^- **<field>`**, not bare; all seven fields verified singleton at the phase-78 IMPL close). ⚠️ **§Project counts is STALE ON THREE AXES** (item 13) — do not anchor on it.
2. `docs/envoy-go/ROADMAP.md` — the family paragraphs and their deferred-candidate sentences, to SELF-PICK from. ⚠️ **`:208` is a ~46 kB single line** — locate positionally.
3. `docs/envoy-go/phases/78-boot-panic-visibility/PROGRESS.md` — **the IMPL record half**, especially the arm-θ refutation, the demoted-arm table, and the ten "corrections this stage owes forward". It is the shape an IMPL record takes in this lineage.
4. `docs/envoy-go/DECISIONS.md` — **ADR-0300** (now COMPLETE; §Consequences (ii) carries the corrected residual) · **ADR-0045** (the split gate) · **ADR-0288** · ADR-0044 (⚠️ it does **NOT** contain the Context-draft discipline — use `ADR-0044-as-used`) · **ADR-0106** (⚠️ the SOLE-leg property, **not** the six-gate).
5. `docs/envoy-go/phases/77-runtime-static-layer/BRAINSTORM.md` and `docs/envoy-go/phases/78-boot-panic-visibility/BRAINSTORM.md` — the shape of a BRAINSTORM in this lineage, and how a self-pick records its rejected alternatives.
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — especially **`reference_probe_must_discriminate`** (necessary but NOT sufficient), **`reference_independent_probes_can_share_a_blind_axis`**, **`reference_probe_input_is_a_claim`**, **`reference_timeout_exit_124_shared_by_healthy_and_hung`**, **`reference_output_volume_is_not_output_content`**, **`reference_liveness_break_needs_failing_baseline`**, **`reference_gate_command_negative_control`**, **`reference_deliberate_break_wrong_assertion`**, `reference_empty_output_is_not_a_zero_result`, `reference_fatalf_makes_assertions_unreachable`, `reference_harness_exit_code_is_not_command_exit_code`, `reference_a_drift_correction_is_itself_a_claim`, `reference_stale_cite_recurs_fix_by_pattern`, `reference_deferred_candidate_cost_restale`, `feedback_brief_citations_not_evidence`, `reference_next_prompt_tracked_despite_gitignore`, `feedback_git_worktrees`, `feedback_execution_style`, and **`reference_bash_cwd_reset_commits_to_main`**.
