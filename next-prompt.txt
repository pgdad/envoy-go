START a fresh session for envoy-go. **The phase-42.2 (`hedging`) BRAINSTORM is COMPLETE, spec-document-reviewer APPROVED, and LANDED on master — `docs/envoy-go/phases/42.2-hedging/BRAINSTORM.md`.** It refined the parent-42 row's 42.1/42.2 split into a **42.1 / 42.2a / 42.2b THREE-leg** split. The next step is the **42.2a (`per_try_timeout`) SPEC** (the lifecycle valve is BRAINSTORM → SPEC; 42.2a is the FIRST sub-leg of the hedging leg — the per-attempt deadline, cross-side-faithful, NO concurrency). Confirm the latest master is the 42.2 BRAINSTORM commit via `git log --oneline -3` (a NEW sha — do NOT assume a literal value). Counts at tip (UNCHANGED at the BRAINSTORM): stat surface **1173** / fixtures **77** / fuzzers **42** / BackendKind tail **36** (`BlockingHoldResponder`) / DECISIONS tail **ADR-0249**, next-free **ADR-0250**.

## THE load-bearing finding (drives the whole leg)

Of `route.v3.HedgePolicy`'s three fields, **`initial_requests` (field 1) + `additional_request_chance` (field 2) are `#not-implemented-hide:` in the reference Envoy** — they exist in the API but are NOT wired. Only **`hedge_on_per_try_timeout` (field 3)** is implemented: on a per-try-timeout it leaves the slow attempt in flight + launches a concurrent hedge, first-success-wins. So the reference's hedging is per-try-timeout-TRIGGERED concurrency, NOT a fan-out. The user's deliberate scope IMPLEMENTS the full `HedgePolicy` (incl. the fan-out the reference no-ops) with a SPLIT verification (cross-side for `per_try_timeout`/`hedge_on_per_try_timeout`; subject-side for the fan-out — a recorded departure). See the BRAINSTORM §1, §2.5.

## NEXT STEP — write the phase-42.2a (`per_try_timeout`) SPEC

Author `docs/envoy-go/phases/42.2-hedging/SPEC.md` (42.2a shares the `42.2-hedging/` dir as the first sub-leg; 42.2b gets its own `42.2b-…/` dir). Execute the §10 **D-H-{PROTO,TIMEOUT,RETRY,STATS,DIFFERENTIAL}** per-try-timeout pins IN-SESSION live against `envoyproxy/envoy:contrib-v1.37.2` (ADR-0227) via the live-probe precedent (`reference_docker_probe_bridge_network` — a shared bridge + STRICT_DNS `direct_response` backends + `--mode validate`), and anchor the **ADR-0250** §Context draft. (The 42.2b hedging pins — esp. D-H-RACE, the concurrent collector — are the SEPARATE 42.2b BRAINSTORM/SPEC's obligation, a later sub-leg.) Mirror the 14-section phase-41/42.1 SPEC shape. Work in a WORKTREE (`feedback_git_worktrees`); commit docs-only; the spec-document-reviewer loop then the user-review gate.

**The 42.2a envelope (BRAINSTORM §1.1):** `RetryPolicy.per_try_timeout` (field 3, a `*durationpb.Duration`) parsed in `buildRouterActionWithVH` (`hcm/config.go`) + carried on the `RetryPolicy` (`retry.go`); the retry executor derives a per-attempt child `context.WithTimeout(reqCtx, perTryTimeout)` around each `doH{1,2}ClusterAction` call; on the per-attempt deadline a LOCAL-ORIGIN retriable timeout → the EXISTING 42.1 sequential retry (NO concurrency at 42.2a); the `upstream_rq_per_try_timeout` counter (via the 42.1 `EnsureRetryStats` scoping). **The load-bearing design point (D-H-RETRY):** distinguish a per-try-timeout (the CHILD ctx deadline — retriable) from a client cancel (the PARENT ctx — NOT retriable; the 42.1 H2 `Status:0` `matches(0,false)==false` path must NOT swallow a per-try timeout). The `0076-per-try-timeout` differential: a held `BlockingHoldResponder` (the `/__release` gate, NO new BackendKind) exceeds a small `per_try_timeout` deterministically + a fast healthy retry target ⇒ the per-try-timeout fires ⇒ recovery to downstream 200; cross-side; sleepless (poll-the-gauge + `/__release`, `reference_concurrency_differential_release_barrier`).

**SPEC-time obligations (D-H-*, BRAINSTORM §10):** D-H-PROTO (the `per_try_timeout`=3 / `per_try_idle_timeout`=13 / `hedge_policy`=27/17 roster; `go mod tidy -diff` EMPTY; `HedgePolicy.initial_requests` `gte:1` is PROTO-STATIC — defer the full HedgePolicy parse to 42.2b OR parse-and-carry now per the SPEC's call) · D-H-TIMEOUT (does envoy-go implement the global `RouteAction.timeout` field 8 AT ALL? the `per_try_timeout` default + the `min(per_try_timeout, remaining global)` interaction) · D-H-RETRY (the per-try-timeout-vs-client-cancel discriminator + the retriable-timeout classification — does it need a `retry_on` token or is a per-try-timeout retried unconditionally?) · D-H-STATS (the exact `upstream_rq_per_try_timeout` name + the counter→event mapping, live `/stats` scrape) · D-H-DIFFERENTIAL (the `0076` held-backend trigger value T, the cross-side assertions, the deliberate breaks).

## Exit posture (anticipated at the 42.2a IMPL — verify at the SPEC)

stat surface 1173 → **1174** (+1 `upstream_rq_per_try_timeout`, scoped) · fixtures 77 → **78** (`0076-per-try-timeout`) · fuzzers **42** (or 43) · BackendKind tail **36** (UNCHANGED — REUSE `BlockingHoldResponder`) · DECISIONS tail ADR-0249 → **ADR-0250** (next-free ADR-0251) · ZERO new packages/modules. **ROADMAP row 42 STAYS `in-progress`** (it flips `done` only when ALL THREE legs 42.1+42.2a+42.2b land — ADR-0106 + `reference_roadmap_split_phase_row_done`; the rows-36/39 precedent). After 42.2a lands, the next subject is the **42.2b (hedging) BRAINSTORM** (the concurrent `hedgeExecutor` + `hedge_on_per_try_timeout` + the fan-out, over the 42.2a deadline — ADR-0251).

## Honor throughout (the SPEC discipline)

- **Lifecycle = one stage per session.** `feedback_git_worktrees` · docs-only commit · `feedback_push_to_origin` (push after a clean docs commit on master) · the spec-document-reviewer loop (precisely-crafted review context, never session history) · the user-review gate before the PLAN.
- **Differential discipline (for the D-H-DIFFERENTIAL pin):** `reference_concurrency_differential_release_barrier` (the held `/__release` backend + poll-the-gauge, NEVER a sleep — the per-try-timeout fires while the backend holds) · `reference_round_robin_offset_randomized` (assert the offset-invariant downstream outcome, not host identity) · `reference_differential_run_selector` (the `0076` subtest is `TestDifferential/0076-…`) · `reference_docker_probe_bridge_network` (the live SPEC pins need Docker + `contrib-v1.37.2` over a shared bridge).
- **ADR/process:** ADR-0044 (§Context at the SPEC, §Decision/§Consequences IN-PLACE at the IMPL) · ADR-0227 (`contrib-v1.37.2`) · ADR-0249 (the 42.1 retry substrate 42.2a extends — the `retryExecutor`, the `matches(status, localOrigin)` classifier, the `EnsureRetryStats` scoping, the H2 `Status:0` ctx-cancel-not-retried path) · ADR-0076 (the 1 MiB body buffer — the replay source) · ADR-0106 + `reference_roadmap_split_phase_row_done` (row 42 flips `done` only when ALL THREE legs land).

## Read first (in order)

1. THIS FILE — the cold-start router (the 42.2 BRAINSTORM is DONE + reviewer-APPROVED + LANDED; the next step is the 42.2a SPEC).
2. `docs/envoy-go/phases/42.2-hedging/BRAINSTORM.md` — the settled forks (Q-scope/fanout-verify/concurrency/split2), the 42.2a/42.2b sub-split, the §1.1 42.2a envelope, the §10 D-H-* obligations, the #not-implemented-hide finding.
3. `docs/envoy-go/phases/42-retries-and-hedging/SPEC.md` — the 14-section SPEC shape to mirror (the AMEND-RT live-pin format; the §11 D-RT-* disposition; the §13 ADR §Context draft pattern).
4. `docs/envoy-go/phases/42-retries-and-hedging/README.md` + the 42.1 as-built `internal/filter/http/router/retry.go` (the `retryExecutorH1`/`retryExecutorH2` + `matches` + the per-attempt unit 42.2a threads the deadline into) + `router_h2.go` (the `Status:0` ctx-cancel-not-retried path the per-try-timeout must coexist with).
5. `docs/envoy-go/STATE.md` — active-phase `phase 42.2 (hedging) BRAINSTORM done`; the count figures (1173 / 77 / 42 / 36 / ADR-0249, next-free ADR-0250).
6. `docs/envoy-go/DECISIONS.md` (the ADR-0249 entry — the 42.1 retry substrate; the process ADRs; next-free ADR-0250) + `docs/envoy-go/BEHAVIOR_CONTRACT.md` (the retries subsection 42.2a extends).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `reference_concurrency_differential_release_barrier`, `reference_round_robin_offset_randomized`, `reference_docker_probe_bridge_network`).
