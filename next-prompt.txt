START a fresh session for envoy-go (router last updated 2026-06-24; the 43.2b PLAN-done content commit is `9869d08d`, beneath the router-roll HEAD `2d588b8a`). **The next action is the phase-43.2b (graceful GOAWAY rotation) IMPL** — execute the 10-task TDD spine in `PLAN-43.2b.md` SUBAGENT-DRIVEN (`superpowers:subagent-driven-development` — a fresh implementer per task + two-stage review). **Git hygiene:** the IMPL runs in a FRESH git worktree off master per `feedback_git_worktrees`; subagents commit LOCALLY only (`feedback_subagents_no_push`); the controller squashes + pushes at stage-close (`feedback_push_to_origin`); each task runs `gofmt -l` + `golangci-lint` on touched pkgs (`feedback_pertask_gofmt_lint`). Phase 43 (`connection-pooling`) is the FIFTH-and-FINAL Upstream-robustness-family row and is `in-progress`: **leg 43.1 DONE** (max_connections + max_pending_requests, ADR-0252) + **leg 43.2a DONE** (the H2 multiplex pool substrate, ADR-0253, SUPERSEDES ADR-0056) + **leg 43.2b PENDING** (this one — the LAST; BRAINSTORM + SPEC + PLAN done). **When 43.2b's IMPL lands, ROADMAP row 43 flips `done` and the Upstream-robustness family (phases 39–43) CLOSES** (`reference_roadmap_split_phase_row_done`).

First confirm the state via `git log --oneline -3` (expect the HEAD to be the router-roll `next-prompt.txt: roll … to the 43.2b IMPL`, with the `phase 43.2b (h2-connection-pool) PLAN …` content commit `9869d08d` directly beneath it; re-read the shas, do NOT assume). Counts at tip (UNCHANGED — docs-only PLAN): stat surface **1185** (H2 cluster; non-H2 1183) / fixtures **81** (tail `0079-h2-multiplex-pool`) / fuzzers **42** documented (actual `^func Fuzz`=43, `reference_fuzzer_count_docs_drift`) / BackendKind tail **37** (`H2HoldResponder`) / DECISIONS tail **ADR-0253**, next-free **ADR-0254**.

## NEXT STEP — the 43.2b IMPL (subagent-driven, 10 tasks)

The PLAN (`docs/envoy-go/phases/43.2-h2-connection-pool/PLAN-43.2b.md`) is the direct input — it is plan-document-reviewer APPROVED and ALL §12 D-questions are RESOLVED (do NOT re-litigate). Execute its 10-task spine in order; each task is TDD (test-first → run-to-fail → implement → run-to-pass → commit). **Re-confirm every file:line before editing — the tree evolves; the PLAN says so explicitly.** The spine:

1. **PROGRESS scaffolding** + green baselines + the final ADR-0045 split re-check (flat).
2. **Codec accessors** (`internal/filter/hcm/h2/client.go`): `GoneAway() bool` + `GoneAwayCh() <-chan struct{}` + **`Done() <-chan struct{}`** (the THIRD accessor — the watcher's exit signal; `cc.ctx` is unexported so the SPEC §3.3 `cc.ctx.Done()` is unreachable from package `cluster`). Read-only over the existing `goawayCh` (closed in `dispatchFrame`'s `GoAwayFrame` case ~444, NO `cc.cancel()` ⇒ `Closed()` stays false on a GOAWAY'd-alive conn). `-race`.
3. **Admission-skip + eviction generalization** (`internal/cluster/h2pool.go`): add `&& !pc.cc.GoneAway()` to the TWO ESTABLISHED-conn sites — `findStreamHitLocked` (~446) + `h2PromoteLocked`'s stream-slot branch (~131); generalize `makeRelease`'s eager-close (~623) `Closed() && inFlight==0` → `(Closed() || GoneAway()) && inFlight==0`. **`findConnectingHitLocked` STAYS GoneAway-guard-free** (a `connectingH2Conn` there has `cc == nil` — a guard would nil-deref + is vacuous; preserves the Task-9.5 coalescing, `reference_h2_pool_connect_coalescing`).
4. **The per-conn drain-watcher** `watchDrain` in `dialAndPool` AFTER the pool-insert (~601, the SOLE `h2Pool[addr] = append` insert site) — the LOAD-BEARING `-race -count=1` task: `select { <-GoneAwayCh() → re-lock, re-check `findPooledLocked != nil && inFlight==0`, evict + promote (idle prompt-close) ; <-Done() → exit }`. The `findPooledLocked != nil` re-check is the double-evict guard vs `EvictH2ConnOnError`/`makeRelease`.
5. **Codec reset-hooks** (`client.go`): `onRxReset`/`onTxReset func()` fields + the variadic `ClientConnOption`/`WithResetHooks` applied BEFORE `go cc.readLoop()` (~186, race-free) + nil-guarded calls at the rx site (`RSTStreamFrame` `cs.finish` ~394) and the tx site (`RoundTrip` ctx.Done `WriteRSTStream(id,CANCEL)` ~537). Codec stays stats-agnostic.
6. **Register `http2.rx_reset` + `http2.tx_reset`** (useH2-gated; 1185→1187) — Cluster fields + `registerClusterMetrics` `if c.useH2` block (`manager.go` ~191) + the `dialPooledH2To` wiring (`WithResetHooks(c.h2RxResetInc, c.h2TxResetInc)`; `DialH2` passes nil) threaded through `h2ConnFromDialed`; extend `TestRegisterClusterMetrics_H2Stats`.
7. **The GOAWAY/RST backend** — NEW BackendKind **38** `H2GoawayResponder` (raw-`golang.org/x/net/http2`-framer h2c server with `/__release` + `/__goaway`(NO_ERROR) + `/__rst`(INTERNAL_ERROR) gates; the kind-37 `http.Server`/`h2c` can't emit frames). `fixture.go` + `runner_test.go`.
8. **The `0080-h2-goaway-rotation` cross-side EXACT fixture** — an H2 (TLS+ALPN-h2) DOWNSTREAM listener (the 0079/0004 PKI shape — MANDATORY, `reference_h2_pool_downstream_codec_gate`) → an h2c upstream cluster; sequenced poll-to-converge barriers (establish → in-flight drain → idle drain → rx_reset prong → tx_reset prong → quiesce); assert cross-side EXACT on `upstream_cx_http2_total==2` + `http2.streams_active`→0 + `rx_reset==1` + `tx_reset==1` (NOT `cx_close_notify`/`cx_destroy_local`, D-H2B-CXSTATS). Model the driver on `0079`.
9. **`0080` deliberate breaks** (`-count=1`, revert with `git restore`) — 4 breaks (admission-skip, watcher idle-close, rx-hook, tx-hook) + 20/20 flake + `-race`.
10. **Completion bundle** — ADR-0254 §Decision/§Consequences (`ADR-0044`; §Context is in SPEC §13) + BEHAVIOR_CONTRACT delta (1185→1187) + STATE/ROADMAP (**row 43 → `done`; family 39–43 CLOSES**) + **fuzzer reconcile 42→43** (D-H2B-FUZZER-RECONCILE) + full 82-dir differential + six-gate. Then `superpowers:code-reviewer` over the whole diff + `superpowers:finishing-a-development-branch` → controller squash-merge + push.

## Honor throughout (the discipline)

- **Lifecycle = one stage per session.** The IMPL is the FULL 10-task spine: `feedback_git_worktrees` (fresh worktree off master) · `feedback_subagents_no_push` (subagents commit LOCALLY; controller squashes + pushes at stage-close) · `feedback_push_to_origin` · `feedback_execution_style` (subagent-driven) · `feedback_pertask_gofmt_lint` · `feedback_subagent_worktree_detach`/`feedback_subagent_worktree_path_targeting` (the worktree hygiene + path-pinning guards).
- **Process anchors:** ADR-0106 + `reference_roadmap_split_phase_row_done` (row 43 flips `done` only when ALL legs land — 43.1 + 43.2a done, 43.2b is the LAST); ADR-0080 (strict-reject); ADR-0044 (the ADR §Decision/§Consequences land at IMPL); ADR-0045 (the split gate — re-checked at PLAN, ships flat).
- **Differential discipline:** `reference_docker_probe_bridge_network` (the `0080` runs over a shared bridge — re-probe if a count is off) + `reference_concurrency_differential_release_barrier` (sleepless poll-to-converge) + `reference_concurrent_attempt_downstream_class_assertion` (assert the DOWNSTREAM class on the rx_reset prong) + `reference_differential_run_selector` (`-run 'TestDifferential/0080'`, NOT `'0080'`) + `reference_differential_break_protocol_count1` (`-count=1` on every break) + `reference_differential_fullsuite_startup_flake` (a `subject ready: EOF` is transient — isolate-re-run, NOT a regression) + `reference_fuzzer_count_docs_drift` (the 42→43 reconcile).
- **The H2 pool memory:** `reference_h2_pool_local_cap_driven` · `reference_h2_pool_connect_coalescing` (the connecting tier stays GoneAway-guard-free — Task 3) · `reference_h2_pool_downstream_codec_gate` (the `0080` MUST use an H2 downstream — Task 8) · `reference_h2_goaway_rotation_stats` (the rotation is `upstream_cx_*`-observed, NOT `http2.*`; only rx_reset+tx_reset live) · `reference_max_connections_soft_breaker` (the 43.1 contrast).

## Read first (in order)

1. THIS FILE — the cold-start router (43.2b PLAN is DONE + LANDED at `9869d08d`; the next action is the 43.2b IMPL, subagent-driven).
2. `docs/envoy-go/STATE.md` — active-phase `phase 43.2b (h2-connection-pool) PLAN done`; the §12 D-question resolutions + the count figures (1185 / 81 / 42 / 37 / ADR-0253, next-free ADR-0254).
3. `docs/envoy-go/phases/43.2-h2-connection-pool/PLAN-43.2b.md` — the 10-task TDD spine (the direct IMPL input; APPROVED).
4. `docs/envoy-go/phases/43.2-h2-connection-pool/SPEC-43.2b.md` — the SPEC (the §11 D-H2B-* EXECUTED pins, the §13 ADR-0254 §Context).
5. `docs/envoy-go/phases/43.2-h2-connection-pool/PLAN.md` — the 43.2a 12-task PLAN (the EXECUTION shape the 43.2b IMPL mirrors).
6. `docs/envoy-go/DECISIONS.md` — **ADR-0253** (the 43.2a as-built; its §Consequences "DEFERRED to 43.2b" is the charter) + next-free **ADR-0254**.
7. `internal/cluster/h2pool.go` (`dialAndPool` ~581 [watcher-spawn after the ~601 pool-insert], `findStreamHitLocked` ~444, `findConnectingHitLocked` ~456 [stays UNGUARDED], `h2PromoteLocked` ~124, `makeRelease` ~617, `evictH2ConnLocked` ~638, `EvictH2ConnOnError` ~665, `findPooledLocked` ~679) + `internal/filter/hcm/h2/client.go` (`goawayCh` ~78, `dispatchFrame` `GoAwayFrame` ~444 + `RSTStreamFrame` ~394 cases, `Closed()` ~614, `Close()` ~619, `RoundTrip` ctx.Done CANCEL ~537, `NewClientConn` ~153 + `go cc.readLoop()` ~186) + `internal/cluster/dial_h2.go` (`dialPooledH2To` ~73 → `h2ConnFromDialed` ~96 → `NewClientConn` ~133 wiring seam).
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. the H2-pool + differential + worktree refs named above).
