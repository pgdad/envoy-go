START a fresh session for envoy-go. **The phase-42.2b (hedging) PLAN is COMPLETE and LANDED on master.** It is the 13-task bite-sized TDD spine + the D-S422B-1..9 resolutions, plan-document-reviewer APPROVED. The next step is the **42.2b (hedging) IMPL** (the lifecycle valve is PLAN → IMPL for the same leg). Confirm the latest master is the 42.2b PLAN docs-only commit via `git log --oneline -3` (a NEW sha — do NOT assume a literal value). Counts at tip (UNCHANGED by the PLAN): stat surface **1181** / fixtures **78** (tail `0076-per-try-timeout`) / fuzzers **42** / BackendKind tail **36** (`BlockingHoldResponder`) / DECISIONS tail **ADR-0250**, next-free **ADR-0251**.

## NEXT STEP — the 42.2b (hedging) IMPL (`superpowers:subagent-driven-development`)

Execute `docs/envoy-go/phases/42.2b-hedging/PLAN.md` (read it FIRST — it carries the full code skeletons, the exact anchors, and the AMEND-H* invariants) task-by-task: a fresh implementer subagent per task + the two-stage review (spec-compliance then code-quality), the controller squashes + pushes at stage-close (`feedback_subagents_no_push`). The **concurrency tasks T3–T6** (the `hedgeExecutor` + the hedge-trigger timer + the first-acceptable collector + the budget-counted fan-out) and the **differential T9–T10** are the HIGHEST-risk — give them the most capable model + insist on `-race -count=1`.

**The 13-task spine:** T1 baselines/PROGRESS · T2 the `HedgePolicy` type + `triggersConcurrency()` + the `initial_requests>=1` reject (`internal/filter/http/router/hedge.go`) · T3 the `hp *HedgePolicy` struct field on `routerAction`/`routerActionH2` + the `hedgeExecutorH1` skeleton (goroutine + BUFFERED first-acceptable collector + loser cancel) · T4 the per-attempt HEDGE-TRIGGER timer + `hedge_on_per_try_timeout` · T5 `hedgeExecutorH2` (the H2 ctx-cancel-aware mirror) · T6 the BUDGET-COUNTED fan-out · T7 `parseHedgePolicy` + the vhost fallback + the `clusterRouteAction.hedgePolicy` carry (`hcm/config.go`/`actions.go`) · T8 wire the dispatch (widen `H{1,2}ClusterAction` + the closure-switch hedge branch BEFORE the `if a.rp != nil` retry branch at `router.go:578`) · T9 the `0077-hedge-on-per-try-timeout` cross-side fixture · T10 deliberate breaks (2) + 20-run flake + `-race` · T11 full 79-dir six-gate · T12 ADR-0251 body + BEHAVIOR_CONTRACT · T13 completion bundle + ROADMAP row 42 → `done`.

## THE load-bearing IMPL invariants (from SPEC §1.1 + the PLAN — guard at EVERY review)

- **AMEND-H1 (the #1 invariant):** the `hedgeExecutor` MUST NEVER call `IncUpstreamRqPerTryTimeout`. A hedged per-try-timeout increments `upstream_rq_retry` (+ `_backoff_exponential`, NO `time.Sleep`) and leaves the original in flight. The `0077` Break B (`IncUpstreamRqPerTryTimeout` in the hedge path ⇒ `upstream_rq_per_try_timeout != 0` cross-side ⇒ fail) + a unit test are the proofs. The hedge path does NOT reuse the 42.2a abandon-and-count race-robust discriminator (it uses a hedge-trigger TIMER).
- **Concurrency (PLAN I1):** the `time.AfterFunc` callback is a PURE non-blocking `hedgeReq <- struct{}{}` send; the COLLECTOR LOOP (`select` on `results` + `hedgeReq`) is the SOLE mutator of `hedgesLaunched`/`limitExceededOnce`/`Inc*`/`TryAcquireRetry`/`launch`. `-race`-clean. The result + hedgeReq channels are BUFFERED to `int(hp.InitialRequests)+maxHedges+1` (no loser blocks on send ⇒ no leak).
- **The H1/H2 cancel asymmetry (D-S422B-2):** `doH1ClusterAction` honors only `ctx.Deadline` (NOT `ctx.Done`) ⇒ an H1 loser completes on backend-response (the `0077` `/__release` guarantees termination); `doH2ClusterAction`'s `RoundTrip(ctx)` aborts on cancel. NO active H1-loser socket reset (a recorded ADR-0251 departure). The driver code stays UNCHANGED (reused concurrently).
- **AMEND-H2:** `upstream_rq_retry_limit_exceeded` under hedging = "no more hedges can launch" NOT "final failure" — the request awaits the first acceptable in-flight result.
- **AMEND-H5:** NO new stat — surface STAYS **1181**. **Byte-identical** when `hedge_policy` is nil/non-triggering (the full 78→79-dir gate; the nil-`hp` closure-switch fall-through). The 42.1 `retryExecutor` + the 42.2a per-attempt unit are SIBLINGS — UNCHANGED.
- **Budget-counted fan-out (D-S422B-3):** acquire-gates-spawn (`TryAcquireRetry` BEFORE the goroutine spawn).

## Anticipated at IMPL-done

fixtures 78 → **79** (`0077`; the fan-out UNIT-tested subject-side — NO `0078` dir); stat surface **1181 UNCHANGED**; fuzzers **42**; BackendKind tail **36** (REUSE `BlockingHoldResponder`); DECISIONS tail ADR-0250 → **ADR-0251** (§Decision/§Consequences IN-PLACE per ADR-0044); ZERO new packages/modules. **ROADMAP row 42 flips `in-progress → done`** (ALL THREE legs 42.1+42.2a+42.2b landed — ADR-0106 + `reference_roadmap_split_phase_row_done`); the Upstream-robustness family then has **1** candidate left {per-protocol connection pooling}. After the IMPL the `next-prompt.txt` rolls to a FRESH BRAINSTORM for a NEW subject (the row-41-IMPL precedent).

## Honor throughout (the IMPL discipline)

- **Lifecycle = one stage per session.** `feedback_git_worktrees` (a fresh worktree off master) · `feedback_subagents_no_push` (subagents commit LOCALLY; the controller squashes + pushes) · `feedback_pertask_gofmt_lint` (each task runs `gofmt -l` + `golangci-lint` on the touched pkgs) · `feedback_subagent_worktree_detach` (the Task-10 deliberate breaks use `git restore` — NO checkout-sha/amend; the controller re-verifies the branch) · `feedback_subagent_worktree_path_targeting` (pin the canonical worktree-relative paths) · `feedback_push_to_origin` (push after the clean squash on master).
- **Differential discipline:** `reference_concurrency_differential_release_barrier` (the held `/__release` backend — fire `/hedge` in a goroutine, poll-the-gauge until retry==N && limit==1, release, join; NEVER a sleep) · `reference_round_robin_offset_randomized` (single host ⇒ offset-irrelevant for `0077`) · `reference_differential_break_protocol_count1` (`-race` + `-count=1`) · `reference_differential_run_selector` (`TestDifferential/0077-…`) · `reference_fixture_workload_constant_desync` (single-source N/T/port) · `reference_differential_fullsuite_startup_flake` (an unrelated `subject ready: EOF` ⇒ isolate-re-run then full re-run) · `reference_per_try_timeout_504_reset_classification` (the 42.2b CORRECTION: the hedge path does NOT reuse the 42.2a abandon-and-count discriminator) · `reference_docker_probe_bridge_network` (the optional Task-9 IMPL re-probe).
- **ADR/process:** ADR-0044 (§Decision/§Consequences IN-PLACE at the IMPL; §Context promoted PROPOSED→ACCEPTED) · ADR-0249 (the 42.1 retry substrate) · ADR-0250 (the 42.2a per-attempt deadline) · ADR-0106 + `reference_roadmap_split_phase_row_done` (row 42 flips `done` only when ALL THREE legs land).

## Read first (in order)

1. THIS FILE — the cold-start router (the 42.2b PLAN is DONE + LANDED; the next step is the 42.2b IMPL).
2. `docs/envoy-go/phases/42.2b-hedging/PLAN.md` — the 13-task TDD spine + the full code skeletons (the `hedgeExecutor`, the collector, the hedge-trigger timer, the parse, the `0077` driver) + the D-S422B-1..9 resolutions + the execution notes.
3. `docs/envoy-go/phases/42.2b-hedging/SPEC.md` — §1.1 AMEND-H1..7 (the live SURPRISES the PLAN implements), §3 the architecture, §11 the D-H-* pins.
4. `internal/filter/http/router/{retry.go,router.go,router_h2.go,router_weighted.go}` (the SIBLING substrate the `hedgeExecutor` reuses concurrently — `do{H1,H2}ClusterAction` + `matches`; the closure switch at `router.go:578`; the `routerAction`/`routerActionH2` structs at `:753`/`:246`) + `hcm/config.go` (`parseRetryPolicy` `:615` + `buildRouteTable` `:412`/caller `:267`) + `hcm/actions.go` (`clusterRouteAction` `:201`) + `cluster.go` (`EnsureRetryStats` `:254` + the `Inc*`/`TryAcquireRetry`/`ReleaseRetry` methods).
5. `docs/envoy-go/STATE.md` — active-phase `phase 42.2b (hedging) PLAN done`; the count figures (1181 / 78 / 42 / 36 / ADR-0250, next-free ADR-0251).
6. `docs/envoy-go/DECISIONS.md` (ADR-0249 + ADR-0250; next-free ADR-0251) + `docs/envoy-go/BEHAVIOR_CONTRACT.md` (the `### Route — request recovery (retries)` subsection — T12 adds the hedging sub-block; the stat-surface block STAYS 1181).
7. The fixture templates: `test/fixtures/0076-per-try-timeout/` (the cross-side shape) + `test/fixtures/0074-circuit-breaker-max-requests/driver/driver.go` (the concurrent-poll-`/__release` driver model — `0077` is a HYBRID).
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `reference_per_try_timeout_504_reset_classification`, `reference_concurrency_differential_release_barrier`, `feedback_subagents_no_push`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_detach`).
