=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-CHECKED 2026-07-13 (after the phase-61.2 PLAN landed, run MECHANICALLY) — it does NOT fire. Do NOT create `stop`.** State:
- (1) prints `NOT DONE: row 61` — ONE `in-progress` row. Row 61 `http3-downstream-listener` is the HTTP/3-family opener; leg 61.1 (QUIC/UDP listen substrate) LANDED and leg 61.2 (H3 codec + HCM) now has BRAINSTORM+SPEC+PLAN (this stage landed the 61.2 PLAN), but the 61.2 IMPL and leg 61.3 (fixture + harness) are NOT yet done. Row 61 flips `done` only when ALL THREE legs land (ADR-0106). This ALONE blocks `stop`.
- (2) prints THREE live "candidates:" sentences — HTTP/3, xDS, and Observability ⇒ three families STAY OPEN. (`reference_sentinel_deferred_sentence_live_vs_historical`.)
- (3) prints `NEVER OPENED: gRPC`, `Runtime`, `WASM` ⇒ THREE families never opened. Operational-tooling also STAYS OPEN.

All three checks print ⇒ the sentinel does NOT fire.

**STATUS AS OF 2026-07-13: the phase-61.2 PLAN LANDED (a docs-only stage — `PLAN-61.2.md` decomposes SPEC-61's H3-codec/HCM-adapter leg into a 9-task TDD spine + `PROGRESS-61.2.md` scaffold; NO production `.go` changes; ROADMAP row 61 STAYS `in-progress`; DECISIONS tail UNCHANGED at ADR-0279).** The NEXT IMPLEMENTABLE STAGE is the **phase-61.2 IMPL** (`http3-h3-codec-hcm` leg 61.2 — execute `PLAN-61.2.md`'s 9-task spine: the H3 codec + HCM adapter serving an H3 GET into the EXISTING HCM → router → filter-chain path; lifecycle-state 3 for the 61.2 leg — a CODE stage). Row 61 flips `done` only when ALL three legs (61.1/61.2/61.3) land (ADR-0106).

**What the 61.2 PLAN delivered (for CONTEXT; the PLAN is the authoritative task spine — RE-READ it at IMPL start):**
- `docs/envoy-go/phases/61-http3-downstream-listener/PLAN-61.2.md` — a 9-task TDD spine. The CORE design (settled at PLAN time from a full source read against tip `222dbee2`): **quic-go's `http3.Server` IS the H3 codec** (framing + QPACK), yielding the stdlib `(*http.Request, http.ResponseWriter)` handler contract — so the HCM H3 arm is a **stdlib-typed dispatch method** (`ServeH3`→`runH3` in a NEW `internal/filter/hcm/h3dispatch.go`), NOT a codec package and importing ZERO quic-go. A NEW `network.H3Terminal` interface (`TerminalFilter` + `ServeH3(w,r)`) bridges the listener to the HCM terminal; `serveQUICConnection` (61.1 handshake-only) is extended to `http3.Server{Handler: http.HandlerFunc(h3t.ServeH3)}.ServeQUICConn(conn)`. quic-go stays confined to `internal/listener/quic.go`. The request side reuses the H1 `router.Action` path (ZERO `router.go` changes target); `writeH3Reply` is the `ActionResponse → http.ResponseWriter` adapter; the `codec_type HTTP3` reject is lifted (gated on a new `IsQUIC` context flag threaded `FactoryCtx → ListenerCtx` from the listener kind) + the HTTP3-on-non-QUIC config-parity reject; `emitAccessLogH3` (`Protocol="HTTP/3"`).
- `docs/envoy-go/phases/61-http3-downstream-listener/PROGRESS-61.2.md` — the baselines + task checklist + design pins + a RECORDED SPEC correction (SPEC §12's "`filter.go:112` ADD a `codecType==HTTP3` branch in `Handle`" is a re-derivation error — `Handle` takes a `net.Conn` never present on the QUIC path; the H3 entry is the `ServeH3`/`H3Terminal` seam) + the 61.1 review-pickup disposition (M6-2 ctx picked up at Task 7; M6-1/M-FB1/M-FB2 re-deferred).

**The phase-61.2 IMPL scope (RE-READ `PLAN-61.2.md` + `PROGRESS-61.2.md` in full at session start; RE-DERIVE every `file:line` against the master tip — `git fetch` first):**
- **This is a CODE stage** — execute `PLAN-61.2.md`'s Tasks 1–9 TDD, subagent-driven (`feedback_execution_style`), in a FRESH worktree off master (`.worktrees/phase-61.2-impl`, branch `phase-61-http3-h3-codec-hcm-impl`, per `feedback_git_worktrees`). Row 61 STAYS `in-progress` at the six-gate (flips `done` only when ALL three legs land, ADR-0106).
- **Task spine (see `PLAN-61.2.md` for the full code):** (1) PROGRESS scaffold [done, folded into this PLAN]; (2) thread `IsQUIC` through `network.FactoryCtx → hcm.ListenerCtx`; (3) lift the `codec_type HTTP3` reject (gated on `IsQUIC`) + the HTTP3-on-non-QUIC parity reject + UPDATE `config_test.go:207-208`; (4) `writeH3Reply` adapter; (5) the `runH3`/`ServeH3` arm + `emitAccessLogH3`; (6) the `network.H3Terminal` interface + `*Filter` satisfies it; (7) wire `serveQUICConnection → http3.Server.ServeQUICConn`; (8) the subject-side H3 GET→200 integration test (local quic-go `http3.Transport` client); (9) BEHAVIOR_CONTRACT + ADR-0281 + STATE + router roll.
- **ADR-0281 (a NEW per-leg codec-arm ADR — §Context/§Decision/§Consequences ALL land at this IMPL, ADR-0044).** The PLAN DECIDED a distinct ADR (not folded into ADR-0279) for the codec-arm seam. DECISIONS tail **ADR-0279 → ADR-0281**; next-free after **ADR-0282**.
- **Leg 61.3 (for CONTEXT — NOT this IMPL):** the differential fixture (`0102-http3-downstream-get`) + harness UDP/H3-client surgery (a shared `test/helpers/h3.go` `H3RoundTrip` + the harness's first non-TCP transport; three container-starters gain `/udp` exposure). 61.2 proves SUBJECT-side only.

**ADR reservation ledger (provisional; each finalized at its IMPL in landing order):** ADR-0277 = phase-59 (LANDED) · ADR-0278 = xDS 60.1 (LANDED) · ADR-0280 = xDS 60.2 (LANDED) · ADR-0279 = HTTP/3 61.1 (LANDED) · **ADR-0281 = HTTP/3 61.2 codec-arm (RESERVED by the 61.2 PLAN; lands at the 61.2 IMPL)** · next-free unreserved **ADR-0282**.

**Current counts (VERIFIED at the phase-61.2 PLAN session against master tip `222dbee2` — RE-VERIFY at session start, `git fetch` first):** fixtures **105** (tail `0103-xds-sds-server-cert`) · fuzzers **55** · DECISIONS tail **ADR-0279** (ADR-0281 reserved for THIS IMPL; next-free **ADR-0282**) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** · go.mod modules **1** (`quic-go v0.54.1`, landed at 61.1). Phase-61.2 is a CODE IMPL ⇒ anticipated exit: fixtures **105 (+0)** · fuzzers **55 (+0)** · BackendKind **38 (+0)** · stat surface **1201 (+0 RECOMMENDED**, +2 max if the IMPL registers `downstream_{cx,rq}_http3_total`) · new packages **+0** (`h3dispatch.go` folds into `hcm`) · modules **+0** · DECISIONS tail **ADR-0281** · row 61 STAYS `in-progress`.

**Locating the phase-61.2 PLAN stage squash** (a router cannot name its own sha — identify by SUBJECT, never by position):
```sh
git log --oneline --grep='^phase 61.2 (http3-h3-codec-hcm) PLAN' -1
```

**THE NEXT ACTION: the phase-61.2 IMPL (lifecycle-state 3 for the 61.2 leg — execute `PLAN-61.2.md`'s 9-task TDD spine; a CODE stage; row 61 STAYS `in-progress`).** After the 61.2 IMPL lands, the roller's next stage is the phase-61.3 PLAN (then the 61.3 IMPL, which flips row 61 `done`). The sentinel governs termination, not this list.

==============================================

## What THIS session does (the phase-61.2 IMPL)

Execute `PLAN-61.2.md`'s Tasks 1–9 (the H3 codec + HCM adapter — a third dispatch arm serving an H3 GET into the EXISTING HCM → router → filter-chain path), TDD + subagent-driven (`feedback_execution_style`), in a FRESH worktree off master (`.worktrees/phase-61.2-impl`, branch `phase-61-http3-h3-codec-hcm-impl`, per `feedback_git_worktrees`). This is a SINGLE STAGE (the IMPL — a CODE stage). Row 61 STAYS `in-progress` (flips `done` only when ALL three legs land, ADR-0106).

**Deliverables (per `PLAN-61.2.md` — the authoritative spine):**
- `internal/filter/hcm/h3dispatch.go` (NEW) — `ServeH3`/`runH3` (the dispatch arm modeled on H1 `dispatchRequest`, reusing the H1 `router.Action` path) + `writeH3Reply` (the `ActionResponse → http.ResponseWriter` adapter). Stdlib `net/http` only; ZERO quic-go import.
- `internal/filter/network/terminal.go` — the `network.H3Terminal` interface (`TerminalFilter` + `ServeH3(w,r)`); `internal/filter/network/types.go` — `FactoryCtx.IsQUIC`.
- `internal/filter/hcm/config.go` — `ListenerCtx.IsQUIC` + the `IsQUIC`-gated `codec_type HTTP3` accept / HTTP3-on-non-QUIC config-parity reject (lifting `config.go:240`); `filter.go` — the `IsQUIC` bridge; `accesslog_emit.go` — `emitAccessLogH3` (`Protocol="HTTP/3"`).
- `internal/listener/manager.go` — set `IsQUIC` in both `FactoryCtx` literals; `internal/listener/quic.go` — `quicChain()` + the `serveQUICConnection → http3.Server.ServeQUICConn` serve (the `http3` import is the ONLY new import; quic-go stays confined here; ctx-honoring per the M6-2 pickup).
- Tests: `internal/filter/hcm/h3dispatch_test.go` (`writeH3Reply` + `runH3` via `httptest` — no quic-go); `config_test.go` (flip `TestParseFilter_CodecTypeHTTP3`); `internal/filter/network/terminal_test.go` (interface shape); `internal/listener/quic_test.go` (`TestQUICListener_ServesH3GET` — the subject-side H3 GET→200 proof, local quic-go `http3.Transport` client, + -race).
- Docs (this IMPL): BEHAVIOR_CONTRACT HTTP/3 serve extension · ADR-0281 §Context/§Decision/§Consequences · PROGRESS-61.2 final · STATE · ROADMAP (row 61 STAYS `in-progress`) · `next-prompt.txt` roll to the 61.3 PLAN.

## Honor throughout (the discipline, unchanged)
- **Lifecycle = one stage per session.** This session: the phase-61.2 IMPL ONLY. After it lands: the phase-61.3 PLAN (or the roller's self-pick; the sentinel governs).
- **Process anchors:** ADR-0044 (ADR §Decision/§Consequences land at the IMPL) · ADR-0045 (the split escape-valve — CONSUMED for row 61) · ADR-0080 (distinct per-arm reject substrings) · ADR-0106 (row 61 flips `done` only when ALL legs land).
- **Six-gate + import hygiene (LOAD-BEARING):** `go list -deps ./internal/filter/hcm | grep -i quic-go || echo HCM-NO-QUICGO` (expect `HCM-NO-QUICGO` — the H3 arm is stdlib-only) · `go list -deps ./internal/tls | grep -i quic-go || echo TLS-NO-QUICGO` (the 61.1 gate) · quic-go appears ONLY in `./internal/listener` deps. `go mod tidy -diff` EMPTY (no module change in 61.2). fuzzers 55, fixtures 105, both +0.
- **`next-prompt.txt` IS TRACKED** (`reference_next_prompt_tracked_despite_gitignore`) — edit it inside the stage worktree and fold into the stage commit; locate commits by SUBJECT (`git log --grep`), never by position.
- **Deferred maintenance (carried; ride until the relevant row reopens the file):** the phase-57 statssink notes F-1/F-2/F-3 (fold into the next `udp.go`/statssink-driver-touching row — none through 61.2); the phase-60.2 final-review MINOR roll-up (T2/T5/T6/T7 — cosmetic/coverage, fold when the SDS files next reopen); the phase-61.1 review MINORS — M6-2 (`serveQUICConnection` discards ctx) is PICKED UP at the 61.2 IMPL Task 7; M6-1 (`quicAcceptLoop` no backoff), M-FB1 (`default_filter_chain` QUIC decode gap), M-FB2 (`quicTLSConfig`/`quicChain` map-nondeterminism) RE-DEFERRED to a 61.3/QUIC-robustness row (none is a security/resource/crash risk within the explicitly-deferred multi-chain scope).

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-61.2 IMPL is next; `PLAN-61.2.md` is the authoritative spine; leg 61.1 CLOSED + the 61.2 PLAN banked).
2. `docs/envoy-go/phases/61-http3-downstream-listener/PLAN-61.2.md` — the 9-task TDD spine (RE-READ in full; RE-DERIVE every `file:line` against the master tip — the hcm citations are stable since 61.1 did not touch hcm, but RE-CONFIRM).
3. `docs/envoy-go/phases/61-http3-downstream-listener/PROGRESS-61.2.md` — baselines + design pins + the SPEC correction + the review-pickup disposition + the six-gate.
4. `docs/envoy-go/phases/61-http3-downstream-listener/SPEC.md` — §3.3 (the H3 codec/HCM adapter), §11 arm reject-B (HTTP3-on-non-QUIC), §7 (the stat surface — asserted as a NAMED SUBSET), §12 (the source roster — NOTE the PLAN's recorded `filter.go:112` correction).
5. `docs/envoy-go/phases/61-http3-downstream-listener/PLAN-61.1.md` + `PROGRESS-61.1.md` — the landed 61.1 seams (the `kind` discriminant, `serveQUICConnection` handshake-only body to be REPLACED at Task 7, the quic-go confinement in `internal/listener/quic.go`).
6. `docs/envoy-go/DECISIONS.md` — the tail ADR-0279 (61.1) + ADR-0044/0045/0080/0106 (the process anchors); ADR-0281 is reserved for THIS IMPL (the codec-arm ADR); next-free ADR-0282.
7. `docs/envoy-go/ROADMAP.md` — row 61 `in-progress`; the HTTP/3-family row + the LIVE HTTP/3 deferred sentence. `docs/envoy-go/STATE.md` — the active-phase header (phase 61.2 PLAN done; NEXT = the phase-61.2 IMPL).
8. The seam source for the IMPL's `file:line` re-derivation: `internal/filter/hcm/connection.go` (`dispatchRequest` — the H1 model for `runH3`) · `internal/filter/hcm/h2dispatch.go` (`WriteH2`/`writeH2Reply` — the net.Conn-free precedent) · `internal/filter/hcm/config.go` (the `codec_type HTTP3` reject `:231-240` + `ListenerCtx` `:60`) · `internal/filter/hcm/filter.go` (`NewNetworkFactory` bridge `:46-57`; `Handle` `:112` — NOT branched) · `internal/filter/hcm/accesslog_emit.go` (`emitAccessLog`/`emitAccessLogH2` `:25`/`:85`) · `internal/filter/network/terminal.go` (the `TerminalFilter` interface) + `types.go` (`FactoryCtx`) · `internal/listener/quic.go` (the 61.1 `serveQUICConnection` `:92` + `quicTLSConfig` `:52`) · `internal/listener/manager.go` (the `FactoryCtx` literals `:494`/`:565`, `chainInfo`/`netChainFactory`).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the `reference_*` traps (esp. `reference_ondestroy_fires_once_encoder_unreachable`, `feedback_brief_citations_not_evidence`, `reference_next_prompt_tracked_despite_gitignore`, `reference_roadmap_split_phase_row_done`, `reference_sentinel_deferred_sentence_live_vs_historical`, `reference_fatalf_makes_assertions_unreachable`, `reference_differential_break_protocol_count1`, `reference_deliberate_break_wrong_assertion`, `feedback_pertask_gofmt_lint`).
