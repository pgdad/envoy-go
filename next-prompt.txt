=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, the active phase is fully landed, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action. (The sentinel does NOT apply right now — phase 50 is chartered and its BRAINSTORM has not yet been performed.)

**STATUS AS OF 2026-07-05: THE LOOP IS RE-OPENED — phase 50 (`stats-sink-dogstatsd-batching`) CHARTERED; the NEXT stage is its BRAINSTORM.**
- All PRIOR chartered ROADMAP rows (1–49) are `done` and LANDED: HEAD is `418ccf0e` (master == origin/master). **Re-verify with `git log --oneline -6` + `git status`; do NOT assume.**
- The Observability family STAYS OPEN. A human (re)opened the loop and chartered the next deferred Observability candidate — **`DogStatsdSink.max_bytes_per_datagram` batching** — as **phase 50 (`stats-sink-dogstatsd-batching`)**, the SEVENTH Observability-family row. It lifts the phase-49 STRICT-REJECT at `internal/bootstrap/bootstrap.go:591` (`parseDogStatsdSinkConfig`: `if dsd.GetMaxBytesPerDatagram() != nil { return fmt.Errorf(...) }`) into genuine multi-metric newline-batched UDP datagram packing over the LANDED phase-49 `internal/statssink/dogstatsd.go` `DogStatsdSink` emitter. The field is a `google.protobuf.UInt64Value` present ONLY on `envoy.config.metrics.v3.DogStatsdSink` (confirmed by reading `go-control-plane/envoy@v1.32.4/config/metrics/v3/stats.pb.go` — `StatsdSink` has no such field, so this row is dog_statsd-only, not a statsd-and-dog_statsd row).
- Counts at charter (re-verify, do NOT assume): stat surface **1200** (non-H2 **1196**) / fixtures **95** / fuzzers **52** / BackendKind **38** / DECISIONS **ADR-0266** (next-free **ADR-0267**).

=> The NEXT action is the **phase 50 (`stats-sink-dogstatsd-batching`) BRAINSTORM** (lifecycle-state 0 → 1). NOT the termination sentinel.

==============================================

## The NEXT action — the phase 50 (`stats-sink-dogstatsd-batching`) BRAINSTORM
Open phase 50 with `superpowers:brainstorming` (a BRAINSTORM is the next lifecycle stage; lifecycle-state 0 → 1; docs-only direct-on-master per the phase-44/45/46/47/48/49 family-row BRAINSTORM precedent). The pick is ALREADY made (dog_statsd `max_bytes_per_datagram` batching — a human decision via the re-open); the BRAINSTORM settles the SCOPE, not the "which row" question.

**Subject:** the `DogStatsdSink.max_bytes_per_datagram` field on the bootstrap `stats_sinks[]` entry lifted at phase 49 (`envoy.config.metrics.v3.DogStatsdSink`) — currently STRICT-REJECTED when explicitly set (`bootstrap.go:591`). The reference Envoy, when this field is set (or defaulted — PROBE the reference's actual default, historically documented as 1432 bytes but NOT yet verified live against `contrib-v1.37.2` for this proto), packs MULTIPLE DogStatsd lines into a single UDP datagram, newline-separated, up to the byte cap, flushing a fresh datagram whenever the next line would exceed it. envoy-go currently (phase 49) emits exactly one metric per datagram, unconditionally.

**Open questions for the BRAINSTORM to settle (Q0-style, confirm WITH the user):**
- Q1 — activation scope: does batching apply ONLY when `max_bytes_per_datagram` is explicitly set on the bootstrap config, or does the reference apply an implicit cap even when the field is absent (i.e., is "one metric per datagram" itself already a form of batching-disabled-by-default, or does the reference always batch up to some default)? PROBE live (`reference_docker_probe_bridge_network`) — this determines whether unset-config behavior changes at all.
- Q2 — packing algorithm: newline-join lines into a growing buffer; when appending the next line would push the buffer past the cap, flush the current buffer as one datagram and start a new one with that line. Confirm the reference's exact boundary rule (`<=` vs `<` the cap; whether a single line LONGER than the cap is still sent alone, oversized, or truncated/dropped) via a live probe with a deliberately tiny cap and a large tag-bearing line.
- Q3 — ordering/flush-boundary interaction: does batching apply WITHIN one flush cycle's snapshot walk only (never spanning two `stats_flush_interval` ticks), and does it interact with the phase-49 sink-private `deltaState`/tag extraction unchanged (batching is purely a TRANSPORT-layer concern over the already-formatted lines, not a semantic change to delta/tag computation)? Expect YES — batching should be orthogonal, applied AFTER line formatting.
- Q4 — config validation: is `max_bytes_per_datagram` accept-any-positive-value, or does the reference reject a pathologically small value (e.g., smaller than one line)? Confirm the strict-reject posture for degenerate values (ADR-0080).
- Q5 — split: likely a SINGLE FLAT ROW (a transport-layer buffering change over the landed phase-49 emitter — no new `Sink`, no new seam) with the ADR-0045 escape-valve pre-authorized; confirm at the SPEC.
- Q6 — proof-shape: extend the `0093-stats-sink-dogstatsd`-style cross-side differential (a NEW fixture, e.g. `0094`) with a small `max_bytes_per_datagram` cap — the driver-owned UDP receiver must SPLIT each received datagram's payload on `\n` before assigning individual metric lines (a receiver-side change, `reference_line_parser_extension_delimiter_reuse` — trace an example multi-line datagram before editing the parser); assert (a) the emitted line SET is unchanged from an unbatched run (same subset-by-name/tags), AND (b) at least one received datagram actually CONTAINS more than one line (proof that batching occurred, not just parsed-as-if-batched), AND (c) no datagram exceeds the configured cap.

**Anticipated (the BRAINSTORM does NOT freeze these — the SPEC/IMPL do):** a NEW buffering layer inside `internal/statssink/dogstatsd.go` (a per-flush accumulate-then-flush-on-overflow loop replacing the current one-line-per-`net.UDPConn.Write` call) + lifting the `bootstrap.go:591` reject into a `DogStatsdSinkConfig.MaxBytesPerDatagram` field (parsed via `GetMaxBytesPerDatagram().GetValue()`, `uint64`, 0/absent meaning "no cap — one metric per datagram, phase-49 behavior UNCHANGED") + a NEW differential fixture (95→96) exercising the cap; possibly +1 fuzzer covering the new field's parse path (52→53); DECISIONS tail → ADR-0267. NO new package, NO new go.mod module expected (a transport-layer change inside the existing `internal/statssink` package). Row 50 registers `in-progress` AT the BRAINSTORM commit (per the ROADMAP invariant — rows are created when a phase enters `in-progress`, NOT pre-populated) with the Observability-family `Next →` pointer; it flips `done` at the phase-50 IMPL six-gate (NO parent rollup per ADR-0106; if split, per-leg).

After the BRAINSTORM lands, ROLL THIS FILE FORWARD to point at the phase-50 SPEC (mirror the structure of any prior phase's post-BRAINSTORM SPEC router in git history — e.g. the 48.x/49.x SPEC routers).

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** The next stage is the phase-50 BRAINSTORM (`superpowers:brainstorming`). Lifecycle-state 0 → 1.
- **Subagent-driven execution** (`feedback_execution_style`) in a **fresh worktree** (`feedback_git_worktrees`) for any IMPL; subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit, re-runs the full suite on the frozen HEAD, does the deliberate-break verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). (BRAINSTORM/SPEC/PLAN are docs-only direct-on-master per the family-row precedent — no worktree needed.) Per-task gates: `gofmt -l` + `golangci-lint` on touched pkgs + `go vet` + `go build ./...` (`feedback_pertask_gofmt_lint`).
- **Process anchors:** ADR-0106 (per-leg rows) · ADR-0044 (ADR §Context-at-SPEC / §Decision+§Consequences-at-IMPL) · ADR-0045 (sub-split soft gate) · ADR-0080 (strict-reject) · ADR-0266 (the phase-49 dog_statsd sink this row extends) · next-free **ADR-0267**.
- **Differential discipline** (carries to any new fixture): `-run 'TestDifferential/<NNNN>'` NEVER bare (`reference_differential_run_selector`); `-count=1` on every break + `-race` run (`reference_differential_break_protocol_count1`); the full-suite `subject ready: EOF` startup flake is isolatable (`reference_differential_fullsuite_startup_flake`); the Docker bridge + decode-ran proof (`reference_docker_probe_bridge_network`); the host-gateway literal-IP helper for reference→host UDP reachability (`reference_host_gateway_ip_docker_desktop`); driver-owned receivers are NOT BackendKinds (`reference_differential_grpc_receiver_driver_owned`); the live reference is the wire truth (`reference_wire_format_both_sides_see_same_bytes`); periodic-sink two-receivers + hard-Close (`reference_periodic_sink_differential_two_receivers`); a burst-all-requests delta sink needs a post-convergence stability barrier (`reference_delta_sink_differential_stability_barrier`); DogStatsd tag order is the extractor's natural/unsorted order (`reference_dogstatsd_tag_order_unsorted`); extending a delimited line parser can break the CURRENT delimiter split — trace an example line first (`reference_line_parser_extension_delimiter_reuse`); the admin-interface wire-name collision on shared HCM stat names (`reference_admin_interface_wire_name_collision`).

## Read first (in order)
1. THIS FILE — the cold-start router (the loop is RE-OPENED; phase 50 `stats-sink-dogstatsd-batching` CHARTERED; the next action is its BRAINSTORM).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 49 IMPL done`) + the recorded NEXT (`phase 50 (stats-sink-dogstatsd-batching) BRAINSTORM`) + the counts (1200 / 95 / 52 / 38 / ADR-0266).
3. `docs/envoy-go/ROADMAP.md` — rows 1–49 all `done` (max row 49); the Observability family-progress note (the family STAYS OPEN; dog_statsd batching now chartered as phase 50 with the `Next →` pointer; the remaining deferred candidates).
4. `docs/envoy-go/DECISIONS.md` — **ADR-0266** (the phase-49 dog_statsd sink anchor this row extends) + next-free **ADR-0267** (the phase-50 batching anchor-to-be).
5. `docs/envoy-go/phases/49-stats-sink-dogstatsd/` (the SPEC/PLAN docs + `internal/statssink/dogstatsd.go` + `internal/bootstrap/bootstrap.go:577-605` `parseDogStatsdSinkConfig`) — the phase-49 emitter this batching layer extends; read `bootstrap.go:591`'s reject comment closely, it already documents the deferred-feature framing this row consumes.
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_subagent_autocommit_claudemd`, `feedback_pertask_gofmt_lint`, `reference_roadmap_split_phase_row_done`, `reference_dogstatsd_tag_order_unsorted`, `reference_line_parser_extension_delimiter_reuse`, `reference_periodic_sink_differential_two_receivers`, `reference_delta_sink_differential_stability_barrier`, `reference_host_gateway_ip_docker_desktop`, plus the differential-discipline references above).
