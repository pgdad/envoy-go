=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

**THERE IS NO BANKED MID-LIFECYCLE WORK AT THIS TIP.** Phase 77 (`runtime-static-layer`) is **CLOSED** — row 77 is `done`, ADR-0299 is COMPLETE, and **check (1) is SILENT**. **This session performs the phase-78 BRAINSTORM and MUST CHARTER A NEW ROW.** See item 2 below: this stage inherits a **named, measured, already-enforced** candidate, which is a stronger starting position than the usual self-pick.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=109 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **`want=109` IS A LIVE FIGURE, NOT A CONSTANT.** It is the ROADMAP data-row count. **A row-adding phase MUST bump it in the same commit that adds the row** — otherwise every later session gets `GATE FAIL: examined 110 data rows, expected 109`. That noise is deliberate: a wrong denominator must be loud, because a silent one is how check (1) failed in the first place. ⚠️ **The phase-78 BRAINSTORM WILL add a row, so it MUST bump `want` to 110 in the same commit.**

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. When you open a new family, add its row-summary slug here.

**SENTINEL RE-RUN MECHANICALLY 2026-07-27 AT THE PHASE-77 IMPL, BOTH AT SESSION OPEN AND AFTER THE `ROADMAP.md` EDIT LANDED. It does NOT fire. `stop` was NOT created and MUST NOT be.** ⚠️ **Re-run it yourself anyway and record what YOU see** — a prior run's result is not evidence for the current tip. ACTUAL output, recorded rather than predicted:
- **(1) SILENT** — row 77 is now `done`. ⚠️ **The silence was GUARDED against a false negative**: flipping row 139 back to `in-progress` on a scratch copy prints `NOT DONE: row 77`, so the silence is genuine and not a broken matcher. **Check (1) has closed for the fourth time in project history.**
- **(2) FIVE under the broadened matcher above, FOUR under the old one.** ⚠️ **BOTH ARE CORRECT — do NOT "fix" either count down.** Sites: `:187`, `:197`, `:207`, `:213` (long form) and **`:221`** (the Operational-tooling short form).
- **(3) `NEVER OPENED: gRPC` and `NEVER OPENED: WASM`** — `Runtime` is legitimately GONE, cleared by row 77. (The `WASM` line remains a ROADMAP **bookkeeping artifact** — `internal/wasm/` is a substantially-built wazero host and rows 25/25.1/25.2/25.3 are all `done` — deliberately LEFT AS-IS.)

⚠️ **THE PROJECT IS NOW HELD OPEN BY CHECKS (2) AND (3) ALONE.** With check (1) silent, a phase that neither charters a row nor narrows a candidates sentence leaves the sentinel exactly where it was. **The phase-78 BRAINSTORM must charter a row** — that is what re-opens check (1) and is the stage's whole purpose.

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`.** Every document from phase 75 forward recorded its blind spot — **109 data rows / 105 matched / FOUR misses** at `:31` (an **em-dash** in the "after" cell), `:35` (a **DOT** in the slug `http-1.1`), `:83`/`:84` (**LETTER-suffixed** ids `28.1a`/`28.1b`) — and every one of them called it *"currently HARMLESS, since all four missed rows are `done`."*

⚠️ **Nobody fired the unsafe arm until the phase-77 PLAN. EXECUTED on scratch copies: with all four blind-spot rows flipped to `in-progress` and row 77 `done`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** A false "done" ends the project early.

**The replacement above was armed on all five arms** (each of the four blind-spot rows named correctly; a deleted row ⇒ `GATE FAIL: examined 108 … expected 109`). ⚠️ **DO NOT restore the regex form.**

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

The old check (2) matched ONLY the long phrase and could not see the **Operational-tooling** family's three genuinely un-chartered items, recorded in the **SHORT** form in the same paragraph that reads `**The family STAYS OPEN**`. Fixed at the phase-77 BRAINSTORM, at the MATCHER; negative controls all EXECUTED.

⚠️ **Normalising that Operational-tooling paragraph to the long form is a NAMED, DELIBERATELY-UNTAKEN follow-up.**

⚠️ **THE DOCUMENTED TRAP'S STATED MECHANISM IS REFUTED, WHILE THE TRAP IS REAL.** `reference_sentinel_deferred_sentence_live_vs_historical` says a naive `grep -c 'candidates:'` inflates *because of* the `candidates were:` recaps. It does inflate — but **not for that reason**: `candidates were:` does not contain that substring **at all**. The inflation comes from **ROADMAP table rows carrying BRAINSTORM prose**. **Cite the command; do not re-inherit the explanation.**

⚠️ **AND THE MEASURED RECORD: CHECK (2) HAS NEVER GONE DOWN** across ~18 phases (history `0 → 1 → 3 → 4`). **Phase 74 landed three `ssl.*` stat names and made its OWN sentence LONGER**; **phase 73 predicted a clause REMOVAL and got a clause REPLACEMENT**; **phase 77 opened a family and narrowed nothing.** ⇒ **the candidates sentence is a WINDOW onto a larger deferred backlog, not an inventory of it.**

⚠️ **NEVER WRITE A SENTINEL'S OWN MATCHER STRING INTO A FILE THE SENTINEL GREPS.** The sentinel greps **`docs/envoy-go/ROADMAP.md`** — writing these strings *here* is safe; writing them *there* is not. This fired LIVE at the phase-76 BRAINSTORM, twice in one commit. **`grep` cannot tell a mention from a use.** ⚠️ **The phase-78 BRAINSTORM EDITS `ROADMAP.md` (it charters a row), so re-run ALL THREE checks AFTER your edits land, and verify mechanically that no matcher string leaked** — at the phase-77 IMPL that verification was done by confirming check (2) still returned exactly its five known sites and check (3) still reported gRPC/WASM, which a leaked `-family row` token would have silenced.

⚠️ **THE SELECTION CALCULUS, CORRECTED AND STILL STANDING.** *"A candidate that clears a check-(3) blocker is worth **strictly more**"* does **not** hold: opening a family **MINTS** a check-(2) sentence, so phase 77's own arithmetic was **3 never-opened + 3 sentences → 2 + 4 = 6 → 6, NET ZERO.** What survives: **opening a family is the ONLY way that family's work becomes enumerable at all — a prerequisite, not a shortcut.** Do not restore the stronger claim.

==============================================


## STATUS AS OF 2026-07-27: PHASE 77 IS **CLOSED**. ROW 77 IS `done`. THIS SESSION PERFORMS THE **PHASE-78 BRAINSTORM** AND MUST CHARTER A NEW ROW.

**What the phase-77 IMPL landed** (`git log --grep 'phase 77'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): the phase-01 wholesale `layered_runtime` boot-reject is **PARTIALLY LIFTED — the `static_layer` arm ONLY** — behind `internal/runtime.Snapshot` (recursive `Struct` → dotted-path flattening, **TWO** termination branches, distinct-key **UNION** across layers), with a **NINE-arm hand-written reject roster landed ATOMICALLY** in the same commit as the lift and **both** guarding test sites. **+2 stats (1205 → 1207) / +1 fixture (`0118-runtime-static-layer`, 119 → 120) / +0 fuzzers (55) / +0 BackendKinds (tail 38) / +0 go.mod modules / +0 new packages (73) / +0 new PUBLIC surface.** Five subagent dispatches → 10 local commits → ONE squash, two dispatches running CONCURRENTLY on disjoint file sets. **FOUR of the PLAN's own claims were REFUTED BY EXECUTION.**

### What matters for the NEXT session, in order

1. **⚠️ RE-RUN ALL THREE SENTINEL CHECKS YOURSELF AND RECORD THE ACTUAL OUTPUT; DO NOT PREDICT IT.** Create `stop` if and ONLY if all three print nothing. **The BRAINSTORM EDITS `ROADMAP.md`, so run them TWICE — at session open and after the edit lands.** After you charter a row, check (1) **RE-OPENS** (that is correct and intended), and **you must bump `want=109` to `want=110` in the SAME commit.**

2. **⚠️ THE PICK — THE STRONGEST CANDIDATE IS ALREADY MEASURED, NAMED, AND ENFORCED BY A LIVE TEST PIN: the `runtime.*` PROMETHEUS-EXPOSITION FIX.** Phase 77 registered `runtime.num_keys` / `runtime.num_layers` correctly and they serve correctly on `/stats` — **but BOTH are SILENTLY ABSENT from `/stats/prometheus`.** The **renderer** drops them: `internal/stats.ExtractTags` (`internal/stats/name.go`) dispatches on a fixed set of recognized top-level segments (`cluster.` / `http.` / `listener.` / `server.` / `wasm.`, plus the SN9 `local_ratelimit` default arm) and returns `stats: name "runtime.num_keys" has no recognized top-level segment`; `internal/stats.WriteProm` (`internal/stats/prom.go`) then **silently `return`s** on that error under a comment reading *"skip malformed names (defense-in-depth; should not occur)"*. **Nothing logs and nothing errors** — which is why it passed registration, the whole unit suite, `go vet` and `golangci-lint` alike.
   - **The reference publishes both names on `/stats/prometheus`; envoy-go publishes neither.** Measured cross-side at the phase-77 IMPL, and corroborated by a direct read of both call sites.
   - ⚠️ **THE DEFERRAL IS ALREADY ENFORCED, NOT PROSE.** Fixture `0118` reads its VALUES from flat `/stats` and separately PINS the asymmetry in `assertPrometheusExpositionDeparture` — the reference MUST still publish both prom names, the subject MUST still be missing both. **The day the projection learns `runtime.`, `0118` goes RED on purpose**, naming the follow-up. **Your row must delete that function and move the value assertions back to `/stats/prometheus`.**
   - ⚠️ **OPEN BY AUDITING WHETHER ANY *PRE-EXISTING* STAT NAME IS ALSO SILENTLY DROPPED.** That question is **UNVERIFIED** — phase 77 did not audit it and reported it as neither confirmed nor refuted. **It is the part that could make this row much larger than it looks**, and it is exactly the kind of question a BRAINSTORM should settle by execution before costing the row.
   - It is owed an **ADR-0061 amendment** (that ADR governs the name projection). `internal/stats` carries its own name-mapping tests.
   - ⚠️ **A silent-skip path under a "should not occur" comment is the GENERAL hazard here, and it is worth more than the instance.** Ask whether `WriteProm` should log or fail loudly at all.

3. **THE BANKED ALTERNATIVE: `fault.abort.grpc_status`, ~7-9 tasks / +0 on EVERY envelope axis.** Its current state is **worse than "ignored"** — `abortEnabled` is set only inside the `HttpStatus` type switch, so a `grpc_status` variant fires **no abort at all** and the request reaches the backend. A 4-arm codec×content-type probe found the reference branches on the request **`content-type`** not the codec, returns HTTP **200** in every arm, and emits `grpc-status`/`grpc-message` as response **HEADERS not trailers** — which **RETIRES the framework blocker**. ⚠️ **It is an HTTP-filters-family row and does NOT open the gRPC family**; the provenance of the contrary claim is a **ROUTING LABEL** in row 09's summary, and **reading a filing label as a charter is the error.**

4. ⚠️ **`reference_deferred_candidate_cost_restale`: RE-DERIVE EITHER CANDIDATE'S COST AT THE ADOPTING TIP.** Both figures above were derived at earlier tips. ⚠️ **AGREEMENT ACROSS DOCUMENTS IS NOT EVIDENCE — a claim that has survived several stages is MORE suspect than one written yesterday, not less.** Phase 77 refuted four of its own PLAN's claims and one instruction carried by four documents.

5. **⚠️ THE HEADLINE LESSON OF PHASE 77, AND IT IS TRANSFERABLE: A *CONFIG* COUNTERFACTUAL IS NOT AN *IMPLEMENTATION* COUNTERFACTUAL.** The PLAN declared the fixture **must stay green** under two breaks. One held; the other was **REFUTED** — the shipped arm carries `numerator` **alone**, so a pair-matching flattener recurses it and the total goes 6 → **8**. The error: a reference probe varied the **INPUT against correct code**, and that was read as an answer about **broken code against the shipped input**. **A reference probe can never perform the second — you cannot install a broken implementation in the reference binary.** ⚠️ **The sign was benign here (the fixture is a STRONGER detector than credited) and that is LUCK, NOT DESIGN**; the same confusion in the other direction ships a fixture that catches nothing while a document asserts it does. **Every MUST-STAY-GREEN declaration is a prediction owed a run.** See `reference_config_counterfactual_is_not_implementation_counterfactual`.

6. **⚠️ BROKEN-GATE COUNT: ELEVEN → THIRTEEN.** **TWELFTH — a full-suite recipe WITHOUT `-v` is VACUOUS**: `go test > full.log` yields a **2-line** log, so `grep -c -- '--- PASS'` **and** `grep -c -- '--- FAIL'` BOTH return **0** on a fully green run. **A "0 FAIL" reading from it proves nothing.** **THIRTEENTH — a sha256 byte-gate roster built from the tip's `git ls-files` alone is desynced BY CONSTRUCTION against a DELETED file** (phase 77 deleted `internal/runtime/doc.go`), so its DESYNC leg fires on a CORRECT tree; **the roster must span BOTH trees** (`ls-files` ∪ `ls-tree master`). And the standing ones: `gofmt -l` **NEVER exits non-zero** (gate on OUTPUT; the `&&` form is inert, re-reproduced) · `go doc -all <A> <B>` **silently swallows arg2** when arg1 is valid — ONE package per invocation · a **`+0 exported symbols` gate over an EMPTY package goes RED on a CORRECT tree** unless the lister's stdout goes straight to a file · a **RANGE** gate cannot detect anchor drift (content/symbol anchor instead) · a roster's naive `[ -f ] || continue` **exits 0 on a DELETED file** · a **count-only** stat guard **PASSES a build with BOTH names wrong**.

7. **⚠️ A STALE CITE RECURS — FIX BY PATTERN, NOT BY THE LINE A BRIEF NAMES.** Phase 77 found `bootstrap.go:499` in **ADR-0278 AND ADR-0280** (which re-uses its frame), and `manager.go:1078-1082` **twice in one paragraph**. **Grep the CITE STRING repo-wide, then prefer a SYMBOL anchor to a corrected number** — a corrected number is a fresh instance of the same defect with a longer fuse. ⚠️ **Resolving a cite means READING THE CODE IT NAMES**; a range check is a false negative, since `:1078-1082` stayed in range while landing on code that *contradicted* the claim.

8. **⚠️ ANCHORS DRIFT — AND THIS ROW MOVED SOME.** `BEHAVIOR_CONTRACT.md` is **5746 → 5750 (+4)**: **every by-line citation into it below the Phase 75 ledger entry shifts by 4.** `DECISIONS.md` is **17494 → 17531 (+37)**, all of it ADR-0299's tail, so **every ADR heading KEEPS its line number**. `ROADMAP.md` is **225 → 225 (+0)**. ⚠️ **Re-derive every count and cite at the tip that reads it**, and **never write a token into the file whose occurrences of that token you are counting** — that species self-falsified in ADR-0296 and twice in ADR-0297, and phase 77's own ledger line had to flag that it now matches its own probe string.

9. **⚠️ A HARNESS'S EXIT CODE IS NOT THE COMMAND'S.** Capture the inner status explicitly (`cmd; echo "EXIT=$?" >> log`) and **derive the PASS/FAIL tally from the log** — and per item 6, the log is only derivable if you passed `-v`.

10. **⚠️ A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX — the SEVENTH consecutive stage at which this held.** Live hazards: the full-suite startup flake (`subject ready: EOF` **and** `bind: address already in use`, both failing **BEFORE any assertion**, the latter as a **PANIC that can abort the whole binary**, firing **MORE readily under `-race`** and as the fixture count grows — now **120**) · `reference_sds_init_fetch_timeout_dial_budget_flake` · the PRE-EXISTING `internal/cluster` `-race` outlier flake · two still-**UNINDEXED** load flakes (`internal/httpclient TestOptions_ZeroValue_NoOpDefaults`, `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery`). **At the phase-77 IMPL the flake that fired was `0084-otlp-access-log`, which is NOT on any brief's list** — classified on evidence (fails before any assertion on an EPHEMERAL port; the same fixture PASSED at the same tip 7 minutes earlier; isolate-re-run **3× PASS**; untouched by the row). **Isolate-re-run, then state the classification AND its evidence.** ⚠️ **`0061-lb-ring-hash` is NOT a live flake — a spread failure there is a FINDING.**

11. **Counts at this tip (re-run MECHANICALLY in your worktree; never copy them):** fixtures **120** (`ls -d test/fixtures/[0-9]*/ | wc -l`; ⚠️ the faithful `discoverFixtures` predicate is `^[0-9]{4}[a-z]?-`; next-free **0119**) / fuzzers **55** (a seed is not a `func Fuzz`) / **stat surface 1207**, ⚠️ **DOCUMENTARY — assert the DELTA; the absolute rides TWO unaudited ledger gaps and the `1200 → 1201` one is documented NOWHERE in the file** / **BackendKind tail 38** (⚠️ a TAIL VALUE — the file declares **39** constants, 0-38; **do NOT "fix" 38 to 39**) / go.mod modules **2** (⚠️ the phase-61.2 lineage figure — **NOT a repo total**; the single `go.mod` requires **67** = 18 direct + 49 indirect in **TWO** `require` blocks; **do NOT "fix" 2 to 67**) / internal packages **73** / blank imports in `runner_test.go` **120**, ⚠️ **anchor on the FULL `^\t_ "github.com/pgdad/envoy-go/test/fixtures/` prefix — a naive `^\t_ ` gives 126** / exported symbols `internal/runtime` **5**, `internal/bootstrap` **11**, `validate` **2** / DECISIONS tail **ADR-0299 COMPLETE** (next-free **ADR-0300**) / next-free fixture index **0119** / next-free reference port **10119** (family-banded `10<index>`).

12. **⚠️ THREE DOCUMENTARY DEFECTS, STILL NAMED AS DEFERRALS AND STILL DELIBERATELY NOT FIXED.**
    - **The documented PUBLIC IMPORT PATH DOES NOT EXIST.** `head -1 go.mod` is `github.com/pgdad/envoy-go`; the docs say `github.com/esalaine/envoy-go/validate` on ~20 lines across four docs. ⚠️ **`DECISIONS.md:142` is `## ADR-0006: module path github.com/esalaine/envoy-go` — an ADR that DECIDES the wrong path, never superseded.** A session copying it writes code that does not compile.
    - **A mechanical stat-surface count** to replace the documentary 1207 — **8-11 tasks**, not the small row it sounds like (non-literal names, `statroster` table fan-out, registration functions passed as method values).
    - **UNRESOLVED, and reported as such:** the *"plus two more in the same file"* half of the `BEHAVIOR_CONTRACT.md` stale-cite claim. Phase 77 resolved the two cites it could name and **explicitly did not close the rest**. A spot-check flagged `internal/tls/config.go:272` (lands on a bare `}`) and `internal/filter/http/chain.go:19` (lands on a blank line) as suspicious — **not audited, not edited.**

## What THIS session does — the phase-78 BRAINSTORM

One stage per session. **Charter a new row**, self-picked per the standing directive, with the pick **and every rejected alternative** recorded alongside the evidence that settled each. Create `docs/envoy-go/phases/78-<slug>/BRAINSTORM.md`. Register the row in `ROADMAP.md` as `in-progress` — ⚠️ **and bump the sentinel's `want=109` to `want=110` in the SAME commit.** Roll `STATE.md` and this file.

⚠️ **`ADR-0089` stays BYTE-UNTOUCHED unless your row lands `/runtime` or `POST /runtime_modify`** — it defers both to this family, row 77 landed neither, so the un-defer is owed only by whichever row does.

⚠️ **Any new ADR carries NO whole-file grep count** — that species self-falsified in ADR-0296 ¶3 and ADR-0297 ¶7 **and** ¶9, and at the phase-76 BRAINSTORM it escalated from a wrong number to a flipped termination-sentinel check. Every count line-scoped or stated with no numeral.

Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** (`reference_parallel_subagents_private_scratch`) — and ⚠️ **if two agents share ONE worktree they must stage by EXPLICIT PATH; a `git commit -am` sweeps in a sibling's uncommitted work.** ⚠️ **AND THEY MUST NOT RUN AN UNSCOPED `git restore`** — at the phase-77 IMPL a `git restore` during a **NEGATIVE CONTROL** (not a deliberate break) wiped a task's still-uncommitted edits. **Nothing that will be undone with `git restore` may run against uncommitted work.** Per-task `gofmt` + `golangci-lint` on touched packages (`feedback_pertask_gofmt_lint`).

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. ⚠️ **THIS HAS NOW FIRED LIVE IN ELEVEN CONSECUTIVE SESSIONS** — at the phase-77 IMPL it fired repeatedly, including on plain read-only `cd <worktree> && ls`. **Assume it fires every session.** **Use `git -C <abs-worktree-path>` for every git command, prefer absolute paths over `cd`, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline — ⚠️ **anchor greps on `^- **<field>`**, not bare; all seven fields verified singleton at the phase-77 close) + §Project counts + the §Recent lineage.
2. `docs/envoy-go/phases/77-runtime-static-layer/PROGRESS.md` — **the full `# IMPL record`**: what was EXECUTED, the four refuted PLAN claims, the prometheus-exposition defect and why it was deferred-but-enforced, the two new broken gates, and the controller self-correction.
3. `docs/envoy-go/ROADMAP.md` — the family sections and their candidates sentences; pick from them, and **read `§Schema` before touching the table**.
4. `docs/envoy-go/DECISIONS.md` — **ADR-0299** (COMPLETE, at the tail) · **ADR-0061** (the stat NAME PROJECTION — the ADR your item-2 candidate must amend) · **ADR-0016** `:387` · **ADR-0089** `:3514` · **ADR-0106** `:4788` (the six-gate) · **ADR-0045** `:1466` (the split gate) · **ADR-0288** `:16983` · ADR-0044 (⚠️ it does **NOT** contain the §Context-draft discipline — use `ADR-0044-as-used`).
5. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — ⚠️ **5750 lines; every by-line citation below the Phase 75 ledger entry shifted +4 at phase 77.** The Phase 77 ledger entry and the prometheus-departure paragraph that follows it are the whole of the runtime documentary record; there is **no `## Runtime` narrative subsection**.
6. `docs/envoy-go/phases/76-lb-ring-hash-spread-margin/BRAINSTORM.md` — the shape of a BRAINSTORM in this lineage.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — especially **`reference_config_counterfactual_is_not_implementation_counterfactual`**, **`reference_stale_cite_recurs_fix_by_pattern`**, `reference_break_protocol_commit_first`, `reference_probe_must_discriminate`, `reference_gate_command_negative_control`, `reference_harness_exit_code_is_not_command_exit_code`, `reference_quoting_is_not_executing`, `feedback_brief_citations_not_evidence`, `reference_a_drift_correction_is_itself_a_claim`, `reference_deferred_candidate_cost_restale`, `reference_next_prompt_tracked_despite_gitignore`, `feedback_git_worktrees`, `feedback_execution_style`, and **`reference_bash_cwd_reset_commits_to_main`**.
