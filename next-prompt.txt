=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-VERIFIED 2026-07-21 (re-run MECHANICALLY at the phase-70 PLAN close, in the stage worktree) — it does NOT fire. `stop` was NOT created.** Actual output:
- **(1) prints `NOT DONE: row 70`** — row 70 (`tracing-custom-tags-metadata`) is `in-progress` (registered at the phase-70 BRAINSTORM commit per the §Schema invariant); it flips `done` at the phase-70 IMPL six-gate. (The registration re-opens check (1) until the IMPL.)
- **(2) prints THREE live "candidates:" sentences** — HTTP/3, xDS, Observability ⇒ three families STAY OPEN. **Cite the command, never the adjective** (`reference_sentinel_deferred_sentence_live_vs_historical`): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` ⇒ **3**. (The Observability sentence stays live — the `custom_tags (metadata)` candidate is NARROWED OUT only at the phase-70 IMPL row-done edit; the `ssl` + `spawn_upstream_span`/`http_service`/force-trace candidates remain.)
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** ⇒ THREE families never opened.

All three checks print ⇒ the sentinel does NOT fire. Abundant work remains.

==============================================

## STATUS AS OF 2026-07-21: THE PHASE-70 PLAN IS COMPLETE AND LANDED. ROW 70 (`tracing-custom-tags-metadata`) IS `in-progress`. THIS SESSION PERFORMS THE PHASE-70 IMPL.

**What the phase-70 PLAN landed** (`git log --grep 'phase 70'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): docs-only (`docs/envoy-go/phases/70-tracing-custom-tags-metadata/PLAN.md` + `PROGRESS.md` + STATE + this router-roll; ZERO production `.go`, DECISIONS/ROADMAP UNTOUCHED). Fresh worktree off master `2dd6b2d0` (the phase-70 SPEC squash — NO parallel stream, production tree byte-identical to what the SPEC re-derived), branch `phase-70-tracing-custom-tags-metadata-plan`, per `feedback_git_worktrees`. Subagent-driven: a read-only re-derivation agent re-opened EVERY SPEC §11 anchor at `2dd6b2d0` (**ZERO unacknowledged drift — every anchor EXACT**) + extracted the three clone skeletons + the RD-LUA sharpening, then TWO independent adversarial verifiers ran in PRIVATE scratch.
- **The deliverable: a 9-task SINGLE-FLAT-ROW TDD spine** (T1–T9, SPEC §10). T1 `config.go` parse (`kindMetadata` + `CustomTagSpec` `MetaNamespace`/`MetaPath` + the REQUEST accept + four distinct-substring rejects replacing the `:214-215` metadata reject) + `config_test.go` · T2 `resolve.go` (the `metaLookup` param + `kindMetadata` arm + the `descend` clone + `structpbValueToString`) + `resolve_test.go` · T3 the NEW `func (c *FilterChain) DynamicMetadata()` accessor + a test · T4 the `metaLookup` thread at all 18 callers + the test updates · T5 the fuzz seed + dispatch-verify · T6 the `0114` fixture + breaks · T7 BC B1–B2 · T8 six-gate/full-116-dir/-race/cycle-guard/envelope-audit · T9 ADR-0292 IN PLACE + row-done + sentence-narrow + STATE/router.
- **⚠️ THE V1 SEVERE the PLAN FOLDED (compile-proven — carry it into the IMPL):** the four kind-getters `GetRequest`/`GetRoute`/`GetCluster`/`GetHost` are methods on `*metadatav3.MetadataKind`, **NOT on `*tracingv3.CustomTag_Metadata`** (which exposes only `GetKind()`/`GetMetadataKey()`/`GetDefaultValue()`). The `parseCustomTags` metadata arm binds `k := md.GetKind()` and branches on `k.GetRequest()` etc — `md.GetRequest()` does NOT compile (`has no field or method GetRequest`). The SPEC §3.1's `md.GetRequest()` shorthand is the same defect; the PLAN §1.1 skeleton is corrected.
- **⚠️ RD-LUA (the SPEC-§8 sharpening):** the `0114` fixture CHASSIS clones from `0106-tracing-custom-tags-environment` (OTLP tracing, all ports templated), but **`0106` has NO Lua** — the Lua `dynamicMetadata():set` writer clones from `0027-http-lua-full-bridge` (`scripts/e_dynamic_metadata.lua` + the `envoy.filters.http.lua` stanza, BEFORE the router on both YAMLs).
- **The C1 correction (RE-CONFIRMED):** `*FilterChain` has **NO** `DynamicMetadata()` accessor — the `chain.go:837`/`:963` accessors are on `decoderCB`/`encoderCB`; the IMPL **ADDS** `func (c *FilterChain) DynamicMetadata() *dynamicmetadata.Bucket` beside `SetDynamicMetadata` (`:1014`). The 18-caller map is EXACT (5+6+7); exactly **3 no-chain-404 nil sites** (`connection.go:330`/`h2dispatch.go:313`/`h3dispatch.go:130`); the 15 span-capable sites pass `chain.DynamicMetadata().Get` (`chain` var name uniform); `h3dispatch.go:210` is a chain site (`traceDecision` nil there → never reached, harmless).
- **Adversarial verification:** V1 (code-claims + compile probes) folded ONE SEVERE (the `md.GetKind()` receiver correction), ZERO MODERATE/MINOR, all else CONFIRMED; V2 (process/consistency/SPEC-coverage) ZERO SEVERE/MODERATE, two MINOR folded (the V1/V2 records populated; BackendKind re-run at T8). ZERO cited-but-unwritten obligation (the phase-69 `TestNoNewStat` defect class avoided — SPEC §7 says NO such guard for tracing).
- **Counts UNCHANGED at this PLAN (docs-only; re-run mechanically):** fixtures **115** / fuzzers **55** / stat **1201** / BackendKind **38** / DECISIONS tail **ADR-0292** (PROPOSED; next-free **ADR-0293**) / go.mod modules **2**. Row 70 STAYS `in-progress`; DECISIONS/ROADMAP UNTOUCHED at the PLAN; NO deferred sentence narrowed (rolls OUT at the IMPL, phase-57 precedent).

**⚠️ PUSH (idempotent — evaluate, then act):** FIRST ACTION: `git fetch`; if origin/master is BEHIND local master and the local tip is the phase-70 PLAN squash (`git log --grep 'phase 70'`, locate by SUBJECT), `git push origin master` per `feedback_push_to_origin`; if in sync, proceed; if REJECTED (diverged), STOP and reconcile — never rebase away landed squashes.

## What THIS session does — the phase-70 IMPL (RE-DERIVE the PLAN, do NOT trust its cites)

One stage per session: the IMPL. Fresh worktree off master per `feedback_git_worktrees`; subagent-driven (`feedback_execution_style` — one implementer per task + a per-task adversarial reviewer; subagents commit LOCALLY only per `feedback_subagents_no_push`, the controller squash-pushes at close per `feedback_push_to_origin`; subagents auto-commit per CLAUDE.md, the controller verifies/cleans/squashes). Execute the 9-task TDD spine (`PLAN.md` §T1–T9). **A PLAN is not evidence** — RE-DERIVE each task's anchors at the IMPL tip (`feedback_brief_citations_not_evidence`); the PLAN's §1 re-derivation ledger (all EXACT at `2dd6b2d0`) + the §1.1 clone skeletons are the starting point, but re-open every file before writing against it. The load-bearing carries:
- **The V1 SEVERE:** bind `k := md.GetKind()` in the `parseCustomTags` metadata arm; branch on `k.GetRequest()`/`k.GetRoute()`/`k.GetCluster()`/`k.GetHost()`, NEVER `md.GetRequest()` (compile-proven unbuildable).
- **The default rule (C2):** the `kindMetadata` resolve arm mirrors `kindRequestHeader` (`resolve.go:23-35`, present-empty EMITS `""`, `HasDefault = DefaultValue != ""`), NOT `kindEnvironment` (`:36-52`).
- **The serialization (C4):** `structpbValueToString` — string→`GetStringValue()` (raw); `NullValue`→unresolvable; else `protojson.Marshal`+`json.Compact` (the `json.Compact` is LOAD-BEARING for the list case, V1-executed; run `internal/tracing` under `-race`, `reference_detrand_race_catches_protojson_value_substring`).
- **The `descend` clone (C3):** clone the ratelimit `descendStructpbValue` (`descriptors.go:1116`) SHAPE as a `[]string` variant — do NOT import (keep `internal/tracing` filter-free; the cycle guard `go list -deps ./internal/tracing` must show no filter edge, T8).
- **The 18-caller thread (RD-CALLERS):** `chain.DynamicMetadata().Get` at the 15 span-capable sites, `nil` at the 3 no-chain-404 sites; RE-DERIVE the exact per-caller `chain` variable name at tip.
- **The fixture (RD-LUA/RD-PORTS):** `0106` chassis + `0027` Lua writer; two custom_tags asserted cross-side EXACT key+value; all ports templated (no port to pick); breaks per `reference_differential_break_protocol_count1` (full `TestDifferential/0114-tracing-custom-tags-metadata` selector, confirm WHICH fired, the writer served-this-arm).

**Stage-close mechanics (T8/T9):** **Row 70 FLIPS `in-progress` → `done`** at the T8 six-gate (ADR-0106, the SOLE leg — `reference_roadmap_split_phase_row_done`). **ADR-0292 COMPLETED IN PLACE** at T9 — append §Decision + §Consequences to the existing entry, flip STATUS PROPOSED → COMPLETE; do NOT append a new ADR, do NOT renumber (tail stays ADR-0292, next-free ADR-0293). **NARROW the deferred sentence NOW (and ONLY now):** roll `custom_tags (metadata)` OUT of the live Observability `candidates:` sentence (the phase-57 precedent; the sentence STAYS a live match — `ssl` + `spawn_upstream_span`/`http_service`/force-trace remain, and the three non-`REQUEST` MetadataKinds become a NEW deferred entry). **Counts at the IMPL:** fixtures **115 → 116** (`0114`) / fuzzers **55 (+0)** / stat **1201 (+0)** / BackendKind **38 (+0)** / go.mod **2 (+0)** / DECISIONS tail ADR-0292 COMPLETE. Re-run the sentinel MECHANICALLY at close: (1) goes SILENT (row 70 `done` — every chartered row `done`); (2) STILL prints **3** (`grep -cE 'remaining deferred \(not-yet-chartered\) candidates:'` — the metadata narrowing does NOT drop the whole Observability sentence); (3) `NEVER OPENED: gRPC/Runtime/WASM` ⇒ does NOT fire; no `stop`. Then the roller SELF-PICKS the phase-71 BRAINSTORM (the 2026-07-12 standing directive). **Memory updates owed (SPEC §13):** the `*FilterChain.DynamicMetadata()` gap (the accessor was on the callback types, a consumer must ADD one — `feedback_brief_citations_not_evidence`); the tracing `metadata` value→string serialization needs `json.Compact` after `protojson.Marshal` (`reference_detrand_race_catches_protojson_value_substring` extended to a value-EMISSION site). **Adversarially verify before landing** (`reference_parallel_subagents_private_scratch`). Roll THIS file to the phase-71 BRAINSTORM.

## Read first (in order)
1. `docs/envoy-go/phases/70-tracing-custom-tags-metadata/PLAN.md` — the §1 re-derivation ledger (all EXACT at `2dd6b2d0`) + the §1.1 clone skeletons + the T1–T9 spine + the §1.2 adversarial record (the V1 SEVERE correction). RE-DERIVE each task's anchors at the IMPL tip; do NOT trust the cites.
2. `docs/envoy-go/phases/70-tracing-custom-tags-metadata/PROGRESS.md` — the live task ledger (populate per task at the IMPL) + the RD-* findings carried from the PLAN.
3. `docs/envoy-go/phases/70-tracing-custom-tags-metadata/SPEC.md` — the D-CTM-* dispositions (§3) + the §9 BC edit map + the §14 ADR-0292 §Context draft. (⚠️ SPEC §3.1's `md.GetRequest()` shorthand is the V1 SEVERE — use `md.GetKind().GetRequest()`.)
4. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline; the next-skill line carries the IMPL's owed work).
5. `internal/tracing/{config.go,resolve.go}` + `internal/filter/http/chain.go` (the `dynamicMetadata` field `:274`, `SetDynamicMetadata` `:1014`, the callback accessors `:837`/`:963`) + `internal/filter/http/ratelimit/descriptors.go` (the `descendStructpbValue` clone target `:1116`) + `internal/filter/hcm/{accesslog_emit.go,connection.go,h2dispatch.go,h3dispatch.go}` + `internal/filter/hcm/fuzz_test.go` (the `FuzzHCMConfigParse` host `:28` + the `withEnvTags` seed) — RE-DERIVE the seam + the 18 callers at tip.
6. `docs/envoy-go/ROADMAP.md` — the Observability family paragraph + the three live `candidates:` sentences (locate by grep, not line) + §Schema (row-registration invariant).
7. `docs/envoy-go/DECISIONS.md` tail — ADR-0292 (PROPOSED, §Context only — the IMPL completes it IN PLACE), ADR-0277/0283/0284 (the prior `custom_tags` literal/request_header/environment ADRs) + ADR-0044/0045/0106 (the ADR/split/valve discipline).
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — trust MEMORY.md's index; esp. `feedback_brief_citations_not_evidence`, `feedback_execution_style`, `feedback_subagents_no_push`, `reference_differential_fixture_dispatch_constraint`, `reference_differential_break_protocol_count1`, `reference_detrand_race_catches_protojson_value_substring`, `reference_sentinel_deferred_sentence_live_vs_historical`.
