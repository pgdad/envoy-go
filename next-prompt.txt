=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage. To use it to advance the project: read `docs/envoy-go/STATE.md` for the recorded NEXT, perform that one stage (the concrete action is spelled out below), land it, and re-roll this file to the following stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, the active phase is fully landed, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action. While ANY stage remains — as NOW: the **47.2b IMPL is the next-and-FINAL stage** of the SECOND-and-FINAL sub-leg of row 47 (its IMPL FLIPS row 47 `done`; the PLAN LANDED this session) — so tasks are NOT complete — do NOT create `stop`; perform the stage described below. NOTE: the 47.2b IMPL is the LAST chartered stage of row 47; AFTER it lands, CHECK the ROADMAP for whether any further Observability row is chartered (statsd / dog_statsd / OTLP-metrics / the tap filter are NOTED as future candidates but confirm whether any is CHARTERED/`pending` vs merely mentioned) — if one is chartered, roll to its BRAINSTORM; if NONE remains and STATE.md's NEXT is empty, the TERMINATION SENTINEL applies.
==============================================

RESUME a fresh session for envoy-go (router last updated 2026-06-29; the **phase 47.2b (`emit_tags_as_labels`) PLAN is DONE and LANDED** — docs-only direct-on-master; plan-document-reviewer APPROVED [all cited line numbers + code references VERIFIED accurate against live source — the 13 per-arm `name.go` citations, `bootstrap.go` `:273`/`:279`/`:454`/`:462`, `sink.go` `:82`/`:88`/`:68`/`:109-111`, the 5 sink-test call sites + `registration_test.go:33`, `runner_test.go:117-118`, `metricsservice.go` `:68-69`/`:180`/`:193`/`:236`; the five high-risk areas all covered; ONE advisory folded — the Task-6 `byKey` snippet's `ms` out-of-scope reference, corrected to HOIST `ms := f.GetMetric()` out of the `if`]). The **§12 D-questions are RESOLVED with concrete seams** (D-MS-SPLIT-2 no further split; **D-MS-LABEL-REUSE → the SHARED CORE [option a]** = refactor `internal/stats/name.go` `flattenToProm` into an exported `ExtractTags(internal) (residualDotted, []Label, err)` SN1–SN9+SN4 matcher consumed by BOTH `flattenToProm` [the Prometheus projection `"envoy_" + ReplaceAll(residual,".","_")`] AND the statssink `labelMapper` [the dotted projection]; the byte-identity GATE is `name_test.go`'s 53 functions + `prom_test.go` + the full prom differential, kept UNCHANGED; D-MSL-PLACEMENT = a sink-owned `labelMapper` in a NEW `internal/statssink/label.go` applied in `Submit` PARALLEL to the 47.2a `deltaState`, compose order delta-THEN-labels, NO shared-snapshot-slice mutation; D-MSL-RECEIVER-KEY = an ADDITIVE composite `name|sorted-labels` key + `FamilyWithLabels`; D-MSL-SUBSET-2XX = INCLUDE the 2xx two-label split; D-MSL-BOTH-KNOBS = accepted-and-composed, Submit smoke-test only). **ROW 47 STAYS `in-progress`** (flips `done` at THIS — the 47.2b — IMPL, the FINAL sub-leg per ADR-0106 + `reference_roadmap_split_phase_row_done`). The next action is the **47.2b (`emit_tags_as_labels`) IMPL** — build the 9-task TDD spine end-to-end.

HEAD will be the phase-47.2b PLAN docs-only commit atop the phase-47.2b SPEC commit `470966e3`. **Re-read the shas with `git log --oneline -6`, do NOT assume.** Confirm `git status` clean (the untracked `.claude/` is session cruft, leave it) + master == origin/master.

## What the 47.2b IMPL IS (the next-and-FINAL stage — subagent-driven in a worktree; FLIPS ROW 47 `done`)
Execute `docs/envoy-go/phases/47-stats-sink-metrics-service/PLAN-47.2b.md` — the 9-task TDD spine realizing SPEC-47.2b. **Re-read `PLAN-47.2b.md` first** (it carries the per-task code + exact citations + the resolved §12 D-questions + the byte-identity-guard discipline). The spine:
- **Task 1**: baselines (fixtures 92 / fuzzers 50 / ADR tail 0263) + PROGRESS + the FINAL ADR-0045 re-check (D-MS-SPLIT-2 — no further split).
- **Task 2**: lift the `bootstrap.go:454` `emit_tags_as_labels:true` reject → a parse-accept recording a NEW `StatsSinkConfig.EmitTagsAsLabels bool` (`:273`; **scalar `bool` via `.GetEmitTagsAsLabels()` — NOT a `*wrapperspb.BoolValue`**, CONTRAST the 47.2a `.GetValue()`); the append at `:460`–`:463` gains `EmitTagsAsLabels`. Replace the `emit_tags_as_labels` reject test with a parse-accepts test; KEEP every OTHER reject strict (`histogram_emit_mode` `:457`, transport `:444`, `google_grpc` `:448`, empty-cluster `:451`, sibling-TypeURL `:420`, `stats_flush_on_admin` `:412`).
- **Task 3**: the shared `ExtractTags` core refactor in `internal/stats/name.go` (D-MS-LABEL-REUSE option a) — convert each `flattenToProm` arm's `base = "envoy_..."` to the dotted `residual` + the early returns; `flattenToProm` becomes the thin `"envoy_" + ReplaceAll(residual,".","_")` projection. **HARD GATE: `go test ./internal/stats/ -count=1` GREEN with `name_test.go` expectations UNCHANGED** (byte-identity) + the full prom differential green; per-arm fallback if any arm diverges.
- **Task 4**: the sink-owned `labelMapper` (`internal/statssink/label.go`) — `apply()` projects `stats.ExtractTags` to the metrics_service form (dotted residual name + `"envoy." + TrimPrefix(key, "envoy_")` keys + SORTED `LabelPair`s; BOTH Counter and Gauge; SN4 multi-tag; untagged → shared-by-pointer passthrough; NO input mutation). TDD table-driven vs the §11 pinned forms.
- **Task 5**: thread the 4th bool (`NewMetricsServiceSink` gains `emitTagsAsLabels`; `sink.go` `labels *labelMapper` field; apply in `Submit` AFTER `delta` — delta-then-labels) + the `cmd/envoy-go/main.go:199` thread + fix the 6 call sites (5 test + main) + a both-knobs compose smoke; false,false ⇒ 47.1-identical.
- **Task 6**: the `test/helpers/metricsservice` label-aware surface — a `byKey map[string]familyValue` composite key + `FamilyWithLabels(name, labels)` (HOIST `ms := f.GetMetric()` out of the `if` per the folded advisory); ADDITIVE (0089/0090 non-regress).
- **Task 7**: the `0091-stats-sink-metrics-service-labels` fixture — clone the AUTHORITATIVE `0090/driver/driver.go` (two per-side receivers + hard `Close()`), DROP the delta-SUM stability barrier (CUMULATIVE `value==K` model), SWAP to the keyed `{residual-name, sorted labels}` split, swap the YAMLs `report_counters_as_deltas:true` → `emit_tags_as_labels:true`, **REGISTER `0091` in `test/differential/runner_test.go:118`** (the C1 vacuous-green guard — else a silently-skipped subtest).
- **Task 8 (CONTROLLER)**: the `0091` deliberate breaks ((a) full inlined name no labels / (b) Prometheus underscore key / (c) Prometheus flattened name / (d) dropped SN4 2xx label; `-count=1`; `-run 'TestDifferential/0091'` NEVER bare; `git restore` to undo) + 20/20 flake-soak + the FULL-package `-race` on BOTH `internal/statssink` AND `internal/stats`.
- **Task 9 (CONTROLLER)**: the full **93-dir** differential + the six-gate + the ADR-0264 §Decision/§Consequences body + BEHAVIOR_CONTRACT + STATE/ROADMAP. **ROADMAP row 47 FLIPS `done`** (the FINAL sub-leg per ADR-0106 + `reference_roadmap_split_phase_row_done`); the Observability family STAYS OPEN.

## Execution discipline (the IMPL — honor throughout)
- **Subagent-driven** (`feedback_execution_style` · `superpowers:subagent-driven-development`) in a FRESH worktree off master (`feedback_git_worktrees`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit + cleans leak files + re-runs the six-gate + the FULL-package `-race` + does the `0091` deliberate breaks ITSELF + squashes + pushes at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). Per-task `gofmt -l` + `golangci-lint` on touched packages (`feedback_pertask_gofmt_lint`). Worktree hygiene: `git restore` to undo breaks — NO checkout-sha/amend (`feedback_subagent_worktree_detach`); pin canonical worktree-relative paths + controller-verify the main checkout stays clean (`feedback_subagent_worktree_path_targeting`).
- **Process anchors:** ADR-0106 (per-leg rows — row 47 flips `done` at THIS IMPL) · ADR-0045 (sub-split — 47.2b is the final no-split sub-leg) · ADR-0044 (ADR §Decision+§Consequences-at-IMPL — ADR-0264 body lands HERE; §Context drafted at the 47.2b SPEC §13) · ADR-0080 (strict-reject — 47.2b LIFTS the `emit_tags_as_labels` reject `:454`, KEEPS the rest) · ADR-0061 (the SN1–SN9 rules — the tag-extraction substrate the label mapper REUSES via `ExtractTags`) · ADR-0059 (frozen Registry) · ADR-0060 (histograms deferred — moot) · ADR-0262 (47.1 anchor) · ADR-0263 (47.2a deltas anchor) · ADR-0264 (ANCHORED at this IMPL).
- **Differential discipline** (for `0091`): clone the AUTHORITATIVE `0090`/`0089` driver (two per-side receivers + hard `Close()` — `reference_periodic_sink_differential_two_receivers`); `0091` is CUMULATIVE-value (NO delta-SUM stability barrier — `reference_delta_sink_differential_stability_barrier` is a 47.2a/`0090`-only concern); a label sink differential needs a break the burst-snapshot can SEE — assert the emitted `LabelPair` SET (breaks (a)/(b) are load-bearing — the 47.1 no-labels inlining does NOT satisfy the keyed lookup); **REGISTER the driver in `runner_test.go`** (else vacuous green — `reference_differential_run_selector`); `-run 'TestDifferential/0091'` NEVER bare; `-count=1` on every break + `-race` (`reference_differential_break_protocol_count1`); the Docker bridge + decode-ran proof (`reference_docker_probe_bridge_network`); the gRPC metrics receiver is driver-owned NOT a BackendKind (`reference_differential_grpc_receiver_driver_owned`); assert the aggregated `{name,label-set,value}` payload NOT framing, label-set ordering NORMALIZED (`reference_streaming_sink_differential_framing`); the live reference is the wire truth (`reference_wire_format_both_sides_see_same_bytes`); one fixture dir = ONE runner branch (`reference_differential_fixture_dispatch_constraint`); the label mapper + delta map + `ExtractTags`-on-the-flush-path + writer goroutine + ticker are background mutators ⇒ FULL-package `-race` on BOTH `internal/statssink` AND `internal/stats` (`reference_full_suite_race_after_background_mutator`); the full-suite `subject ready: EOF` startup-race flake is isolate-re-run + full-re-run, NOT a regression (`reference_differential_fullsuite_startup_flake`).

## After the 47.2b IMPL lands
ROLL THIS FILE: row 47 is now `done`. CHECK `docs/envoy-go/ROADMAP.md` for any remaining CHARTERED Observability row (statsd / dog_statsd / OTLP-metrics / the tap filter — distinguish chartered/`pending` from merely mentioned). If a next row is chartered, roll the router to its BRAINSTORM. If NONE remains and STATE.md's NEXT is empty, the TERMINATION SENTINEL applies (create `stop`).

## Read first (in order)
1. THIS FILE — the cold-start router (the 47.2b PLAN is DONE + LANDED; row 47 `in-progress`; the next action is the 47.2b IMPL).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 47.2b PLAN done`) + the recorded NEXT (the 47.2b IMPL) + the counts (1200 / 92 / 50 / 38 / ADR-0263).
3. `docs/envoy-go/phases/47-stats-sink-metrics-service/PLAN-47.2b.md` — the 9-task TDD spine to EXECUTE (the per-task code + exact citations + the resolved §12 D-questions + the byte-identity-guard discipline + the "Notes carried from the SPEC" tail).
4. `docs/envoy-go/phases/47-stats-sink-metrics-service/SPEC-47.2b.md` — the charter (the §11 live-probe pin table + the §12 D-questions + the §10 per-task sketch + the ADR-0264 §Context draft §13).
5. `docs/envoy-go/phases/47-stats-sink-metrics-service/PLAN-47.2a.md` — the 9-task TDD spine SHAPE the 47.2b spine mirrors (the reject-lift + bool + the sink transform + the main thread + the `009N` differential + the receiver surface + breaks/flake/full-package-race + the ADR body).
6. `docs/envoy-go/DECISIONS.md` — **ADR-0263** (the 47.2a anchor; ADR-0264 body lands at THIS IMPL).
7. The as-built code 47.2b modifies: `internal/stats/name.go` (`flattenToProm` `:39` → refactor to `ExtractTags`; the SN-rule arms + the SN4 collapse `:346`), `internal/bootstrap/bootstrap.go` (the `emit_tags_as_labels` reject `:454` to LIFT; `StatsSinkConfig` `:273` [`ReportCountersAsDeltas` `:279`]; the append `:460`–`:463`), `internal/statssink/sink.go` (`MetricsServiceSink` `:64`; `delta *deltaState` `:68`; `delta.apply` in `Submit` `:109`–`:111` — the PARALLEL precedent for the `labelMapper`), `internal/statssink/mapping.go` (`snapshot` `:22`), `internal/statssink/delta.go` (the 47.2a transform shape `label.go` mirrors), `cmd/envoy-go/main.go:199` (the sink construction — gains a 4th bool).
8. `test/fixtures/0090-stats-sink-metrics-service-deltas/{driver/driver.go,envoy*.yaml,expectations.yaml}` (the FRESHEST differential template `0091` clones — two per-side receivers + hard `Close()`) + `test/helpers/metricsservice/metricsservice.go` (the receiver to extend with `FamilyWithLabels` — `s.fams` `:68`/`s.sums` `:69`/`StreamMetrics` `:160`/`Reset` `:236`) + `test/differential/runner_test.go` (`:117` the 0090 import — add the 0091 sibling at `:118`).
9. `docs/envoy-go/ROADMAP.md` — row 47 (`stats-sink-metrics-service`, `in-progress` → FLIPS `done` at this IMPL) + the Observability family-progress note.
10. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_subagents_no_push`, `feedback_subagent_autocommit_claudemd`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_detach`/`_path_targeting`, `reference_roadmap_split_phase_row_done`, `reference_stats_sink_metrics_service_probe`, `reference_periodic_sink_differential_two_receivers`, `reference_delta_sink_differential_stability_barrier` [a 47.2a/`0090`-only concern — `0091` is cumulative], `reference_differential_grpc_receiver_driver_owned`, `reference_streaming_sink_differential_framing`, `reference_docker_probe_bridge_network`, `reference_full_suite_race_after_background_mutator`, `reference_differential_break_protocol_count1`, `reference_differential_run_selector`, `reference_differential_fullsuite_startup_flake`, plus the differential-discipline references above).
