START a fresh cold-start session for envoy-go — **phase 31 (`network-filter-kafka-broker`) SPEC is DONE**: the FIFTH §9 Network-filters-family row has its authoritative SPEC. The D31-1..D31-8 empirical pins were EXECUTED LIVE against the contrib reference Envoy (`envoyproxy/envoy:contrib-v1.37.2`, ADR-0227) + go-control-plane `/contrib` v1.32.4 + upstream v1.37.2 contrib source (Kafka 3.9.1), the ADR-0228 §Context was anchored, and spec-document-reviewer APPROVED (first iteration). `kafka_broker` (`envoy.filters.network.kafka_broker`) lands as a new `internal/filter/network/kafkabroker/` package (the 9th network-filter built-in; consumer #3 of the ADR-0221 WriteFilter seam; the project's FIRST `/contrib` consumer). The next lifecycle step is the **phase-31 PLAN** (`superpowers:writing-plans` → `PLAN.md`).

The phase-31 SPEC landed at the SPEC squash on master (squash-merged from `phase-31-network-filter-kafka-broker-spec` + pushed by the controller; confirm via `git log --oneline -1`). Substantive predecessor on master: the phase-31 BRAINSTORM squash `78d5f36` (docs-only SHA-fill follow-ups trail it). Before that: the phase-30 IMPL squash `2d24f34` (phase 30 CLOSED). origin/master in sync after push.

## What the phase-31 SPEC settled (read it first)

The SPEC (`docs/envoy-go/phases/31-network-filter-kafka-broker/SPEC.md` + README) is a SINGLE flat §9 row (directly executable; SPEC → PLAN → IMPL — NOT a parent pre-split). It refined the BRAINSTORM with SEVEN empirical amendments (§1.1 AMEND-K1..K7) the PLAN MUST honor:

- **AMEND-K1** — the per-API-key stat segment is the `_request`/`_response`-SUFFIXED message-name snake-case: `kafka.<sp>.request.<msg>_request` / `kafka.<sp>.response.<msg>_response` (e.g. `request.produce_request`, `request.api_versions_request`) — NOT the bare api-key name (`request.produce`).
- **AMEND-K2** — the Prometheus exposition is FULLY INLINED with EMPTY labels (`envoy_kafka_<sp>_<rest>{}`) — this REFUTES the BRAINSTORM's label-hoist hypothesis. The `kafka.` `internal/stats/name.go` arm is the simplest INLINE arm (`CutPrefix("kafka.")` → `envoy_kafka_` + dots→underscores, no labels) — the zookeeper INLINE shape, NOT the mongo label-hoist shape.
- **AMEND-K3** — the roster is EAGER + **176 counters** (86 request + 86 response per-key + 4 fixed `request.unknown`/`request.failure`/`response.unknown`/`response.failure`; api_keys 71/72 telemetry-EXCLUDED → 86 per direction; the 86 `response.<msg>_response_duration` histograms DEFERRED per ADR-0060). Stat surface **360 → 536** (+176) — the project's largest single-phase stat jump. Eager creation at config parse (the mongo D-P1 precedent).
- **AMEND-K4** — `*.unknown` is keyed on **(api_key, api_version)** (an unknown VERSION of a known key also → unknown); `*.failure` is partly header-reproducible (a malformed request header → `request.failure`; a response whose `correlation_id` was never registered → `response.failure`) — so `response.failure` earns a `0053` differential arm (not purely unit-test).
- **AMEND-K5** — header-version is a `flexibleVersions(api_key)` predicate (tagged fields in the header iff `api_version ∈ flexibleVersions(api_key)`) with the SPECIAL CASE that ApiVersions(api_key 18) RESPONSE header suppresses tagged fields. Kafka source 3.9.1; api-key ceiling 87. (The api-key roster + flexibleVersions table are SPEC Appendices B/C.)
- **AMEND-K6** — the ONLY genuinely-new go.mod dep is `github.com/envoyproxy/go-control-plane/contrib v1.32.4` (kafka_broker/v3 is self-contained — cncf/xds/protoc-gen-validate/vtprotobuf/`/envoy` already in the closure); the @type carries the `extensions.` segment; the dep survives `go mod tidy` with its first consumer (the config parse + the `kafka_broker/v3` blank-import in `bootstrap.go`).
- **AMEND-K7** — the `stats.IsValidName` charset guard is satisfied BY CONSTRUCTION (the api-key names are table-bounded, never raw wire strings); only the config-derived `stat_prefix` needs the config-boundary guard. D31-6 confirmed NO close-direction counters (framework-zero-touch).

The envelope: HEADER-ONLY decode in both directions (request header `api_key` INT16 / `api_version` INT16 / `correlation_id` INT32 / `client_id` NULLABLE_STRING / tagged-fields; response header `correlation_id` INT32 / tagged-fields; 4-byte INT32 length prefix both); FULL per-API-key counter parity recovering the api_key on the response side via a `correlation_id → (api_key, api_version)` per-connection map (the mongo-29.2 correlation precedent under an ADR-0223 mutex); the four active features (`force_response_rewrite`/`id_based_broker_address_rewrite_spec`/`api_keys_allowed`/`api_keys_denied`) PARSE-ACCEPTED-behavior-DEFERRED (the broker-address-rewrite write-mutation flagged as a future framework-surgery sub-phase). ZERO framework-seam extension (pure consumer; ZERO-touch on the ADR-0226 async halt/resume seam — never-halting, byte-identical R1; the filter implements `WriteFilter` even where observation-only to get the 28.1b post-handoff read seam).

## What this session does — the phase-31 PLAN

Author `docs/envoy-go/phases/31-network-filter-kafka-broker/PLAN.md` (`superpowers:writing-plans`) from the phase-31 SPEC. Decompose the SPEC §10 ~13–18-task spine into bite-sized TDD tasks (each: failing test → run-fails → minimal impl → run-passes → commit), per the phase-29/28/27 §9-row PLAN precedent:

- **Task 1** baselines/anchors gate — re-pin fuzzers **39** / fixtures **54** (tail `0052`) / stat surface **360** / DECISIONS tail **ADR-0228** / BackendKind tail **30** against the IMPL-session tip via the canonical recipes; re-confirm `proto.MessageName(&kafka_brokerv3.KafkaBroker{})` + `/contrib v1.32.4` resolves; re-pin the as-built anchors (`builtins.go` registration site, `bootstrap.go` blank-import block, `name.go:290` default branch, `mongoproxy/stats.go` eager-roster shape).
- Then: the `/contrib v1.32.4` dep + the `kafka_broker/v3` blank-import; the `kafkabroker` package skeleton + TypeURL; the 5-field config parse + PGV arms (`stat_prefix` required → boot-reject; api_keys [0,32767]; the oneof + nested-rule arms) + the `stats.IsValidName(stat_prefix)` guard; the static `api_key → message-name` table (86 entries, Appendix B) + a byte-stable roster test; the `flexibleVersions(api_key)` predicate + the ApiVersions(18) response special-case (Appendix C) + a table test; the Kafka primitive decoder + the request-header + response-header decoders; the `correlation_id → (api_key, api_version)` per-connection map + the ADR-0223 mutex (+ a `-race` test); the EAGER 176-counter roster; the ReadFilter/WriteFilter glue (pure copying sniffer — always Continue, never mutate/close); the 9th `builtins.RegisterBuiltins` registration + main.go parity; the `kafka.` INLINE prom arm in `name.go`; the new BackendKind (the correlation-id-echoing TCP Kafka responder, value 31); the `0053-kafka-requests` cross-side fixture (request/unknown-key/unknown-version/failure/response/unregistered-correlation arms) + driver; the `0054-kafka-boot-reject` fixture; the 40th fuzzer `FuzzKafkaDecode`; full differential re-verify (54 prior dirs byte-exact + the 2 new) with `-count=1` deliberate-break liveness proofs; the completion bundle (BEHAVIOR_CONTRACT 31 bundle 360→536 + ADR-0228 §Decision/§Consequences body in-place + STATE/ROADMAP row 31 `in-progress → done` + the six-gate evidence).

Re-check the **ADR-0045 split-gate** at PLAN time (single row anticipated, ~805–1200 production LoC / ~13–18 tasks; the pre-authorized 31.1/31.2 split is the unconsumed escape valve — consume it only if the PLAN trips the gate). Honor AMEND-K1..K7 throughout. Run subagent-driven (`feedback_execution_style`); plan-document-reviewer pass (≤3 iterations); work in a git worktree (`feedback_git_worktrees`); subagents commit local-only (`feedback_subagents_no_push`); the controller squash-merges + pushes at stage-close (`feedback_push_to_origin`).

## Read first (in order)

1. THIS FILE — the phase-31 PLAN cold-start.
2. `docs/envoy-go/phases/31-network-filter-kafka-broker/SPEC.md` — the authoritative input: §1.1 (AMEND-K1..K7), §3 (the package + framework reuse + the single-row split disposition), §5/§6 (proto roster + PGV/boot-reject arms), §7 (the stat surface — the 176-counter eager roster + the `kafka.` INLINE prom arm), §8 (the `0053`/`0054` fixtures + the new BackendKind), §10 (the ~13–18-task spine the PLAN decomposes), §11 (the D31-1..D31-8 empirical-pin block), §12 (the PLAN/IMPL D-questions), §13 (RATIFIED-PENDING), §14 (the test surface + acceptance checklist), Appendices B (the 86-entry api-key table) + C (the flexibleVersions table).
3. `docs/envoy-go/phases/31-network-filter-kafka-broker/BRAINSTORM.md` — the charter (the Q0/Q1/Q-split decisions).
4. `docs/envoy-go/phases/29.1-network-filter-mongo-wire-and-requests/PLAN.md` + the phase-27/28.1 PLANs — the §9-row PLAN-shape precedents (bite-sized TDD task decomposition).
5. `internal/filter/network/mongoproxy/` (the §9-sniffer package-shape precedent for `kafkabroker` — `mongoproxy.go`/`config.go`/`codec.go`/`stats.go`/`filter.go` + the eager-roster `newMongoStats` + the ADR-0223 correlation mutex) + `internal/stats/name.go` (the tag-extractor arm pattern — note the zookeeper INLINE arm at the `.zookeeper.` segment is the precedent for the new `kafka.` arm, NOT the mongo label-hoist arm) + `internal/filter/network/builtins/builtins.go` (the registration site) + `internal/bootstrap/bootstrap.go` (the blank-import block).
6. `docs/envoy-go/STATE.md` — active-phase `phase 31 SPEC done`; lifecycle-state → phase-31 PLAN; next-skill `superpowers:writing-plans`.
7. `docs/envoy-go/ROADMAP.md` — row 31 (`in-progress`, depends-on 30) + the §9 Network-filters family ({redis, thrift} remain after kafka_broker).
8. `docs/envoy-go/DECISIONS.md` — ADR-0228 §Context (this phase's ADR — its §Decision/§Consequences body lands at the phase-31 IMPL per ADR-0044) + ADR-0106 (§9 flat-row) + ADR-0045 (split-gate) + ADR-0221 (WriteFilter seam) + ADR-0223 (per-connection mutex) + ADR-0060 (histograms deferred) + ADR-0227 (the contrib pin) + ADR-0044 (ADR §-body timing) + ADR-0080 (byte-stable rejects).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — network-filter-typeurl-extensions + wire-format-both-sides + dynamic-stat-name-charset-guard (note AMEND-K7: satisfied by construction here) + close-direction-framework-gap (note D31-6: no kafka close-direction counters) + docker-probe-bridge-network + network-chain-terminal-handoff-ends-ondata + differential-asserter-dispatch + differential-break-protocol-count1 + per-task-gofmt-lint + subagents-no-push + push-to-origin + git-worktrees + execution-style.

## Counts at master tip (phase 31 SPEC done — UNCHANGED until IMPL)

- stat surface **360** (→ **536** at IMPL, +176 eager); differential fixtures **54** (tail `0052-mongo-fault-delay`; → **56** at IMPL: `0053-kafka-requests` + `0054-kafka-boot-reject`); fuzzers **39** (→ **40** `FuzzKafkaDecode` at IMPL); BackendKind tail **30** (`TCPMongoResponder`; → **31** the correlation-id-echoing Kafka responder at IMPL); DECISIONS.md tail **ADR-0228** (§Context anchored at the SPEC; next-free **ADR-0229**; the §Decision/§Consequences body lands at IMPL).
- conformance h2spec 53/53 + proxy-wasm 10/10 (image-independent).
- reference pin: `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227).
- Toolchain: Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); go-control-plane `/envoy` v1.32.4 (ADR-0008); `/contrib` v1.32.4 confirmed resolvable + tidy-keepable with the first consumer (the phase-31 dep — added at the phase-31 IMPL).
- §9 Network-filters family: rows 26/27/28/29 DONE + row 30 (pin-refresh) DONE; **row 31 `kafka_broker` IN-PROGRESS** (BRAINSTORM + SPEC done; PLAN next); after phase 31 phase-done the candidates {redis, thrift} remain.
