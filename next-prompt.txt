=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-VERIFIED 2026-07-16 (re-run MECHANICALLY at the phase-66 SPEC) — it does NOT fire. `stop` was NOT created.** Actual output:
- **(1) prints `NOT DONE: row 66`** — row 66 stays `in-progress` through the SPEC (the IMPL six-gate flips it `done`, ADR-0106). **Check (1) BLOCKS `stop`.** It goes silent at the phase-66 IMPL — at which point checks (2)+(3) alone hold the loop open. A future roller MUST NOT read "(1) is silent" as "the project is done".
- **(2) prints THREE live "candidates:" sentences** — HTTP/3 (`:176`), xDS (`:186`), Observability (`:196`) ⇒ three families STAY OPEN. **UNCHANGED by row 66 and it stays unchanged at the IMPL** — `combined_validation_context` was never IN a sentence (a §8-tier pickup; the phase-64 precedent). **NOTE: the Observability line ALSO carries HISTORICAL `candidates were:` recaps — do not confuse them with the live sentence** (`reference_sentinel_deferred_sentence_live_vs_historical`; the live grep needs "candidates:" adjacent, exactly ONE live match per family).
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** ⇒ THREE families never opened. Operational-tooling also STAYS OPEN.

All three checks print ⇒ the sentinel does NOT fire. Abundant work remains.

**STATUS AS OF 2026-07-16: the phase-66 SPEC LANDED. ROADMAP row 66 is `in-progress`. ADR-0287's §Context is DRAFTED** (DECISIONS tail `ADR-0286 → ADR-0287`; next-free **ADR-0288**). The subject is `xds-sds-combined-validation-context` — lift the `commonTLSContextToConfig` `combined_validation_context is not supported in phase 03` reject (DOWNSTREAM + live-provider + `require_client_certificate: true` ONLY) and install the SDS-delivered CA as the downstream mTLS `ClientCAs`.

**⚠️ THE SPEC OVERTURNED THE BRAINSTORM'S IMPLEMENTATION PLAN. READ THE SPEC, NOT THE BRAINSTORM, FOR *HOW*.**
The BRAINSTORM's planned `proto.Clone` + `proto.Merge` apply-point **DOES NOT COMPILE**: `internal/xds`'s `SecretProvider.FetchInitialValidationContext` returns an **`*x509.CertPool`**, and `parseValidationSecret` consumes-and-discards the `CertificateValidationContext` message — **no message crosses the seam, so `proto.Merge` has no operand.** The BRAINSTORM ran `proto.Merge` exactly as it instructed itself to, and STILL planned unbuildable code, because it **CITED the seam's return type**. **The BRAINSTORM is authoritative for the SUBJECT and the ENVELOPE only; its §6 implementation roster is SUPERSEDED.**

**ADOPTED DESIGN — pool substitution, justified by an EQUIVALENCE THEOREM (SPEC §3.2), adversarially attacked without a counterexample:** the SDS pool wins; `default_validation_context.trusted_ca` is NOT read. Premises, each executed or re-derived: envoy-go honors ONLY `trusted_ca`; `proto.Merge` recurses into it as a message while its `specifier` **oneof** REPLACES (OBSERVED); a successful fetch **guarantees** that specifier is set (`dataSourceBytes` errors otherwise) ⇒ **merged pool ≡ SDS pool, exactly**; the hybrid-`DataSource` artifact (`watched_directory`) is inert. **Premise P5 is a COINCIDENCE, not a guarantee** — `main.go` passes the same `filepath.Dir(*cfgPath)` to BOTH `NewSDSProvider` and `boot.Construct`, and `dataSourceBytes`/`loadDataSource` are arm-for-arm identical. **If either diverges, the theorem is SILENTLY falsified** — the PLAN owes a comment at the apply-point naming P5.

**⚠️ WHAT THE PHASE-66 SPEC LEARNED THAT THE PLAN MUST NOT RE-LEARN:**
- **A LANDED CODE COMMENT IS NOT EVIDENCE — the new lesson, and it cost the SPEC its one SEVERE defect.** The SPEC asserted *"QUIC is the CVC guard's ONLY live consumer"*, marked it "RE-DERIVED and CONFIRMED", and was **FLATLY WRONG**: **`validate.Bootstrap` passes a nil provider too**, so `side=="downstream" && provider==nil` is reachable on an **ORDINARY TCP listener** (proven by EXECUTING `validate.BootstrapFile`). It inherited the claim from a **FALSE LANDED CODE COMMENT** — in the very doc region it audited and caught two OTHER stale comments in — and a THIRD comment in the same file **contradicts** it. **The file disagrees with itself and the SPEC picked the wrong sentence. It was bound VERBATIM for DECISIONS.md.** ⇒ the gate MUST keep the `provider != nil` conjunct (**TWO** consumers), and SPEC §11 item 7b fixes the false comment. **Walk the call graph; never trust the comment next to the code.**
- **A PROBE MUST DISCRIMINATE, NOT MERELY RUN.** The SPEC's `BoolValue` probe used ONE input (`true⊕false→true`) that **cannot distinguish the hypotheses** (`OR(T,F)=T` too) and drew a **negative conclusion** from it. All four combos: `BoolValue` IS extensionally OR. **`reference_vacuous_break_receiver_normalizes`, inside an empirical pin block.** Running is necessary; discriminating is what makes it evidence.
- **ENUMERATE BY REFLECTION, NEVER BY READING.** The SPEC claimed its field roster was "re-enumerated in full from the generated type" while carrying **8 of 15** — the exact accusation it levelled at the BRAINSTORM one clause earlier. `ProtoReflect().Descriptor().Fields()` settles it.
- **AN `internal/`-SCOPED GREP MISSES THE TOP-LEVEL `validate/` PACKAGE.** It hid the SEVERE defect above. **State negative-proving greps repo-wide.**
- **The SPEC's four defects ALL landed in sections where it was MOST confident it had already caught the error.** Recorded in SPEC §1.2 rather than hidden. **Attack this SPEC there first.**
- **`reference_sds_init_fetch_timeout_dial_budget_flake`** — if `TestProvider_FetchInitialCertificate_Timeout` fails under `-race`, it is PRE-EXISTING on master (observed once, 2026-07-16). Do NOT reflex-classify it as a phase-66 regression.

**Counts at phase-66 SPEC-DONE (UNCHANGED — docs-only; RE-VERIFY at session start, `git fetch` first):** fixtures **110** (`ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0108-xds-sds-validation-context`) · fuzzers **55** (`grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`) · DECISIONS tail **ADR-0288** (next-free **ADR-0289**) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** (docs-verified; there is NO mechanical counting command) · go.mod modules **2** (the phase-61.2 lineage figure — **NOT a repo total**; the single `go.mod` requires **67**).

**⚠️ `STATE.md` WAS COMPACTED AFTER THE SPEC LANDED — ADR-0288 (a housekeeping move between stages; NO phase state changed, row 66 untouched).** It was **750 KB**, 93% of it 152 never-retired `prior active-phase:` bullets — and prepending had buried TWO fossil "current state" blocks that still read `next-skill: the 42.2 hedging BRAINSTORM` / `the 46.2 SPEC` while the live phase was 66. **`grep 'next-skill:' STATE.md` returned TWO STALE HITS AND ZERO LIVE ONES**; `grep 'fuzzers:'` returned 48 and 42 when the truth was 55. Three contradictory "next" pointers in the file whose ONE invariant is to be the single source of truth for "what next".
- **Now: 35 KB (95.3% smaller), and a grep of it is TRUSTWORTHY** — exactly ONE live `next-skill:` / `lifecycle-state:` / `next-free ADR:` / count block, all in **§Current pointer** at the top.
- **NOTHING was deleted.** The entire pre-move file is preserved **VERBATIM, byte-for-byte** in **`docs/envoy-go/STATE_HISTORY.md`** (all 152 bullets, both fossil blocks) — "disk is the only memory" (`BOOTSTRAP_PROMPT.md` Step B). **Never grep `STATE_HISTORY.md` for a pointer or a count; everything in it is stale by construction.**
- **⚠️ THE RULE YOU MUST HONOR AT YOUR STAGE CLOSE: EDIT `§Current pointer` IN PLACE. Do NOT prepend a new block above it** — prepending is exactly what created the fossils. Demote the outgoing `active-phase:` bullet into **§Recent lineage**, and **cap that list at FIVE** (move the sixth to `STATE_HISTORY.md`). The cap and the edit-in-place rule are **conventions, not tooling-enforced** — if you prepend, the defect regenerates.
- **Out of scope, deliberately, and still true:** `BOOTSTRAP_PROMPT.md` Step B tells cold-start to read six files totalling **~6.2 MB ≈ 1.58M tokens** — more than a 1M context window, so **Step B is already physically unfollowable**; sessions necessarily grep/slice. `ROADMAP.md` (819 KB) and `DECISIONS.md` (3.4 MB) are append-only by invariants 2/4 and were **NOT** touched. ADR-0288 fixed one file that had drifted from its own charter; it did **not** fix cold start.

==============================================

## What THIS session does (the phase-66 PLAN)

Perform the **PLAN** stage (lifecycle-state 2 → 3) for **phase 66**. Docs-only (NO production `.go`). Fresh worktree off master, branch/worktree per `feedback_git_worktrees` (the phase-65 lineage is the naming precedent). The deliverable is `docs/envoy-go/phases/66-xds-sds-combined-validation-context/PLAN.md`. **Row 66 STAYS `in-progress`** (the IMPL flips it `done` at its six-gate, ADR-0106). **ADR-0287's §Context is ALREADY DRAFTED** — do NOT re-draft it; §Decision/§Consequences land at the IMPL.

**The subject AND the design are SETTLED — do NOT re-pick, do NOT re-open the seam.** Design A (pool substitution) is adopted and its theorem is proven and adversarially attacked. The PLAN's job is to DECOMPOSE it into a TDD task spine.

### The PLAN's obligations, in priority order

1. **RE-DERIVE the SPEC — do NOT execute it.** It carries FOUR self-recorded defects (§1.2), one SEVERE, and phase 65's IMPL found ELEVEN defects in a PLAN that had itself corrected five SPEC defects. **Default to REFUTED.** Start where the SPEC was most confident: §3.7 (the `validate` consumer), §3.2's theorem + **P5**, §3.9's departure, §5.2's PGV claim.
2. **Do NOT write `proto.Merge` / `proto.Clone` into any task.** The BRAINSTORM's §6 still instructs them; they do not compile (§3.1). The CVC branch is `FetchInitialValidationContext` → `cfg.ClientCAs`, full stop.
3. **The four BYPASSED sub-field rejects must each be shown RED FIRST.** They "pass" today for the wrong reason (the block is skipped under CVC), so a green test proves NOTHING. Each needs its OWN test (`reference_fatalf_makes_assertions_unreachable`: `Errorf` per property).
4. **E1/E2 — the two rejects the BRAINSTORM never named** (SPEC §6): CVC with a nil `default_validation_context`, and CVC with a nil `validation_context_sds_secret_config`. **BOTH halves are PGV-`required`; envoy-go runs NO PGV repo-wide.** Pin exact ADR-0080-distinct substrings and GREP-collision-check them (`reference_spec_drafted_identifier_collision_check`).
5. **Keep the `provider != nil` conjunct** — **TWO** live consumers (QUIC + `validate`), not one (SPEC §3.7). Decide + record the `validate/` disposition (SPEC §11 item 5b recommends: leave it rejecting, record the §9-item-7 boundary).
6. **Phrase §3.9's departure PER-SHAPE.** **Never** "envoy-go rejects where the reference rejects" — that is FALSE for the empty-dynamic case, where the reference **serves traffic against the default CA**.
7. **Fixture `0109`** (SPEC §8): the CA_X-served / CA_Y-default two-arm observable — **already OBSERVED cross-side-satisfiable**. It must NOT exercise an empty dynamic context on the cross-side path (the sides disagree there — that arm is a SUBJECT-SIDE unit test). The `structuralCheck` liveness break must be **RE-DEMONSTRATED**, not asserted.

**Honor throughout:**
- **Lifecycle = one stage per session.** This session: the PLAN ONLY (no code). After it lands: roll to the phase-66 IMPL.
- **Counts are UNCHANGED at a PLAN** (docs-only). Re-verify at session start and state them (`git fetch` first).
- **Process anchors:** ADR-0044 (§Context already drafted at the SPEC) · ADR-0045 (single-flat-row; escape-valve armable — the SPEC anticipates ~8–10 tasks and NO split; Design A SHRANK the row) · ADR-0106 (a row flips `done` only at its IMPL six-gate) · ADR-0080 (envoy-go-strict distinct-substring rejects) · ADR-0280 (the boot-FAIL DEPARTURE — §3.9's departure lands INSIDE this family) · ADR-0286 (phase 65 — **its "CVC" means `CertificateValidationContext`, NOT `CombinedValidationContext`**).
- **The D-tag family is `D-COMBVC-*`, deliberately NOT `D-CVC-*`** (ADR-0286 binds "CVC" to `CertificateValidationContext`).
- **The deferred sentence is NOT narrowed by this row, at ANY stage.** Do not fabricate a narrow.
- **`next-prompt.txt` IS TRACKED** (`reference_next_prompt_tracked_despite_gitignore`) — edit it inside the stage worktree and fold it into the stage squash; locate commits by SUBJECT (`git log --grep`), NEVER by position.
- **Worktree discipline** (`feedback_git_worktrees`, `feedback_subagent_worktree_detach`, `feedback_subagent_worktree_path_targeting`) — pin the canonical worktree root; the controller verifies the MAIN checkout stays clean; on a deliberate break restore with `git restore` only.
- **PLAN break INSTRUCTIONS must COMPILE** (`reference_plan_break_instructions_dont_compile`) — phase 65 hit 2 non-compiling + 2 impossible/vacuous breaks. A non-compiling break shows red that proves NOTHING. **And a break must DISCRIMINATE** (the SPEC's own `BoolValue` lapse). `-count=1` on every break (`reference_differential_break_protocol_count1`); `-run 'TestDifferential/0109-xds-sds-combined-validation-context'` (`reference_differential_run_selector`); confirm WHICH assertion fired (`reference_deliberate_break_wrong_assertion`); **a break that does NOT fire is a FINDING** — record it as an honest, UNCLAIMED gap.
- **Run an adversarial verification pass before landing.** It found ELEVEN defects in the BRAINSTORM's first draft and FOUR (one SEVERE) in the SPEC's. Ask a verifier to RE-DERIVE independently and **default to REFUTED** — do NOT ask it to confirm your citations, and do NOT hand it your line numbers.

**Memory updates owed at the IMPL (SPEC §13):** (i) NEW — *a driver-owned probe/SDS server needs fresh-per-arm discipline + a served-this-arm precondition assert*; `reference_probe_fresh_container_per_arm` covers only the CONTAINER (a stale server nearly produced a FALSE divergence). (ii) EXTEND `reference_quoting_is_not_executing` — *execute the semantics AND re-derive the SEAM's signature; and a probe must DISCRIMINATE*. (iii) NEW/EXTEND `feedback_brief_citations_not_evidence` — ***a landed CODE COMMENT is not evidence***. (iv) NEW — *an `internal/`-scoped grep MISSES top-level `validate/`*. (v) NEW — *the reference's PGV layer can foreclose a hazard Go-side `proto.Merge` reasoning predicts; probe before recording a Go-derived divergence*.

**ADR reservation ledger (provisional; each finalized at its IMPL in landing order):** ADR-0283 = phase-62 tracing-custom-tags-request-header (LANDED) · ADR-0284 = phase-63 tracing-custom-tags-environment (LANDED) · ADR-0285 = phase-64 tracing-max-path-tag-length (LANDED) · ADR-0286 = phase-65 xds-sds-validation-context (LANDED) · **ADR-0287 = phase-66 xds-sds-combined-validation-context** (§Context DRAFTED at the SPEC per ADR-0044; §Decision/§Consequences at the IMPL) · **ADR-0288 = the `STATE.md` → pointer + `STATE_HISTORY.md` archival move (LANDED — housekeeping, NOT a phase)** · next-free unreserved: **ADR-0289**.

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-66 PLAN is next; the subject AND the design are SETTLED).
2. `docs/envoy-go/phases/66-xds-sds-combined-validation-context/SPEC.md` — **the authority on HOW.** §1.2 (the SPEC's OWN four defects — attack there first) · §3.1 (why the BRAINSTORM's plan does not compile) · §3.2 (the equivalence theorem + **P5**) · §3.5 (the four BYPASSED rejects) · §3.6 (the pre-scan third arm) · §3.7 (**TWO** nil-provider consumers) · §3.9 (the ONE named departure, per-shape) · §5.2 (PGV ⇒ E1/E2) · §5.3 (the 15-field roster) · §6 (the reject roster) · §8 (fixture `0109`) · §10 (the ~8–10-task shape) · §11 (the edit-site roster incl. `validate/` + the THREE stale comments). **The single most important read.**
3. `docs/envoy-go/phases/66-xds-sds-combined-validation-context/BRAINSTORM.md` — **for the SUBJECT and the ENVELOPE ONLY. Its §6 implementation roster is SUPERSEDED and does not compile.** Read §2.1/§2.1.1 for the self-pick + the upstream-SDS overturn.
4. `docs/envoy-go/STATE.md` — the active-phase header (`phase 66 SPEC done`; NEXT = the phase-66 PLAN).
5. `docs/envoy-go/ROADMAP.md` — the §Schema invariant + row 66 + the xDS family (rows 60/60.1/60.2/65/66) + the THREE live deferred sentences.
6. `docs/envoy-go/phases/65-xds-sds-validation-context/PLAN.md` — **the FORMAT precedent** (the immediately-prior PLAN; its TDD spine shape).
7. `docs/envoy-go/phases/65-xds-sds-validation-context/PROGRESS.md` — the liveness-break log (incl. the breaks that did NOT fire), the `-race` finding, the `structuralCheck` trap DEMONSTRATION. The cautionary tale.
8. `docs/envoy-go/DECISIONS.md` — **ADR-0287 (§Context, drafted at the SPEC — do NOT re-draft)** + ADR-0286 (phase 65) + ADR-0280 (SDS wiring + the boot-FAIL departure) + ADR-0278 (the SDS substrate) + ADR-0147 (static-mTLS `ClientCAs`) + the ADR-0044/0045/0106/0080 process anchors.
9. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the xDS/SDS section (CONSUMED vs REJECTED today) + the recorded coverage boundaries.
10. `test/fixtures/0108-xds-sds-validation-context/` — the ~95%-reusable driver (PKI, `mtlsEcho`, `structuralCheck`, `normalizeTLSErr`, per-side SDS servers).
11. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the differential-harness `reference_*` traps + **`reference_plan_break_instructions_dont_compile`** + **`reference_sds_init_fetch_timeout_dial_budget_flake`** + **`reference_vacuous_break_receiver_normalizes`** (DEMONSTRATED at phase 65 on `0108`; `0109` inherits the trap — and the SPEC re-committed it in its own pin block) + **`reference_quoting_is_not_executing`** + **`feedback_brief_citations_not_evidence`**.
