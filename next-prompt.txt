=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-CHECKED 2026-07-12 (at the phase-58 IMPL six-gate, run MECHANICALLY) — it does NOT fire. Do NOT create `stop`.** The phase-58 IMPL landed (row 58 `done`); the state after it:
- (1) prints NOTHING — every ROADMAP row is now `done` (row 58 flipped `done` at the phase-58 IMPL six-gate).
- (2) prints EXACTLY ONE live "candidates:" sentence — the Observability family's `remaining deferred (not-yet-chartered) candidates: OTLP-metrics stats sink + tracing custom_tags/spawn_upstream_span/http_service/force-trace.` (the dog_statsd explicit-`max_bytes_per_datagram: 0` candidate was rolled OUT at the phase-58 IMPL). ⇒ Observability STAYS OPEN. (One live match, `reference_sentinel_deferred_sentence_live_vs_historical`.)
- (3) prints `NEVER OPENED: HTTP/3`, `gRPC`, `xDS`, `Runtime`, `WASM` ⇒ FIVE families never opened; Operational-tooling also STAYS OPEN.

Checks (2) and (3) print ⇒ the sentinel does NOT fire.

**STATUS AS OF 2026-07-12: phase 58 (`stats-sink-dogstatsd-explicit-zero`) is COMPLETE — the IMPL landed (row 58 `done`, lifecycle-state done). The next stage is a NEW phase-59 BRAINSTORM (the roller SELF-PICKS the subject per the standing directive).** The phase-58 IMPL was subagent-driven: the reject arm + test convert + 3 doc fixes (Task 2), the fuzz seed (Task 3), the four BEHAVIOR_CONTRACT edits (Task 4), verification + the full 103-dir differential (Task 5), ADR-0276 body + STATE/ROADMAP + PROGRESS close + this router roll (Task 6).

**Current counts (verified at the phase-58 IMPL six-gate, on the frozen HEAD):** fixtures **103** (tail `0101-stats-sink-graphite`) · fuzzers **54** · DECISIONS tail **ADR-0276** (next-free **ADR-0277**) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** · new Go packages **0** · new go.mod modules **0**. RE-VERIFY against the actual master tip at session start (`git fetch` first).

**Locating the phase-58 IMPL stage squash** (a router cannot name its own sha — identify by SUBJECT, never by position):
```sh
git log --oneline --grep='^phase 58 (stats-sink-dogstatsd-explicit-zero) IMPL' -1
```

**THE NEXT ACTION: the phase-59 BRAINSTORM (a NEW row; lifecycle-state 0 → 1).**

==============================================

## What THIS session does (the phase-59 BRAINSTORM)

Perform the BRAINSTORM for a NEW phase 59 — docs-only (a BRAINSTORM makes NO production `.go` changes), in a FRESH worktree off master (`.worktrees/phase-59-brainstorm`, branch `phase-59-<slug>-brainstorm`, per `feedback_git_worktrees`). Use `superpowers:brainstorming`.

**SELF-PICK THE SUBJECT (standing directive — smallest defensible candidate first; record the pick + the rejected alternatives IN THE BRAINSTORM).** The live deferred-candidate pool (ROADMAP sentinel check-(2)) is the Observability family's `OTLP-metrics stats sink + tracing custom_tags/spawn_upstream_span/http_service/force-trace`; Operational-tooling is also open, and five families (HTTP/3, gRPC, xDS, Runtime, WASM) are never-opened. Per "smallest defensible candidate first", INVESTIGATE the remaining Observability candidates' sizes at the BRAINSTORM before committing — a single tracing knob (e.g. a `custom_tags` header/literal tag or a force-trace decision) is plausibly smaller than a whole new OTLP-metrics gRPC stats sink (new proto type-URL + likely a new streaming receiver + a fixture), but VERIFY that against source before picking. Record the chosen subject and the rejected alternatives (and why) in the BRAINSTORM per ADR-0044. Opening a new family instead of continuing Observability is also defensible IF the smallest Observability candidate turns out large — weigh it explicitly.

**The BRAINSTORM's job** — frame the picked row's mission, non-purposes, the D-questions (the empirical uncertainties the SPEC will pin via live probes against `envoyproxy/envoy:contrib-v1.37.2`, `reference_probe_fresh_container_per_arm`), the anticipated split disposition (ADR-0045), and the next-free ADR anchor (**ADR-0277**). Draft the ROADMAP row (a new row NN opened `in-progress` per the project row-open convention) and add the new family's row-summary slug to the sentinel check-(3) list here IF a new family is opened. RE-DERIVE every `file:line` from source (`feedback_brief_citations_not_evidence`).

Then ROLL THIS FILE FORWARD to point at the phase-59 SPEC and land it (commit + push per `feedback_push_to_origin`).

## Honor throughout (the discipline, unchanged)
- **Lifecycle = one stage per session.** This session: the phase-59 BRAINSTORM ONLY. After it lands: the phase-59 SPEC.
- **Process anchors:** ADR-0044 (ADR §Context drafted at the SPEC, §Decision/§Consequences at the IMPL) · ADR-0045 (split gate ~15 tasks) · ADR-0080 (distinct per-arm substrings) · ADR-0106 (a row flips `done` at its IMPL six-gate). Next-free ADR is **ADR-0277**.
- **Subagent-driven execution** for the IMPL stages (`feedback_execution_style`, `feedback_subagent_autocommit_claudemd`, `feedback_subagents_no_push`): subagents commit LOCALLY only; the controller squashes at stage-close and pushes. Docs-only BRAINSTORM/SPEC/PLAN stages may be controller-authored.
- **`next-prompt.txt` IS TRACKED** — edit it inside the stage worktree, fold into the stage commit; locate commits by SUBJECT (`git log --grep`), never by position (`reference_next_prompt_tracked_despite_gitignore`).
- **Deferred maintenance (fold into the next `udp.go`/statssink-driver-touching row — carried from the phase-57 final review; phase 58 touched NEITHER so these are STILL OPEN):** F-1 the 0101 driver's counter tag snapshot records the EXPECTED map; F-2 `udp.go` `udpWriter` doc comment names two sinks (three embed it now); F-3 BEHAVIOR_CONTRACT 0101 note says "`differential.HostGatewayIP` helper" where the driver uses its LOCAL duplicate. (An OTLP-metrics or tracing row will likely NOT touch `udp.go` either — these ride until a statsd/graphite/dog_statsd sink row reopens.)

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-59 BRAINSTORM is next; SELF-PICK the subject).
2. `docs/envoy-go/ROADMAP.md` — the family sections + the LIVE deferred-candidate sentence (the self-pick pool); confirm row 58 is `done`.
3. `docs/envoy-go/STATE.md` — the active-phase header (phase 58 IMPL done; NEXT = the phase-59 BRAINSTORM).
4. `docs/envoy-go/DECISIONS.md` — the ADR-0276 tail (next-free **ADR-0277**); the Observability-family ADRs (ADR-0266/0267 dog_statsd, ADR-0275 graphite) for the sink substrate.
5. `docs/envoy-go/phases/58-stats-sink-dogstatsd-explicit-zero/{BRAINSTORM,SPEC,PLAN,PROGRESS}.md` — the just-completed row's shape (the BRAINSTORM template + how a self-pick records rejected alternatives).
6. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the stats-sink + tracing sections (what is already consumed vs deferred; the candidate pool's current disposition).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the `reference_*` traps (esp. the Observability-sink + tracing references).
