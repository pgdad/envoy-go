=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. **There IS banked work at this tip** — phase 76 is BRAINSTORMED and row 76 is `in-progress` — so this session **ADVANCES IT** and performs the **phase-76 SPEC**. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-RUN MECHANICALLY 2026-07-25 at the phase-76 BRAINSTORM close, TWICE (stage worktree AND landed master after the squash-push), with IDENTICAL output both times. It does NOT fire. `stop` was NOT created and MUST NOT be.** ⚠️ **Re-run it yourself anyway and record what YOU see** — a prior run's result is not evidence for the current tip. ACTUAL output, recorded rather than predicted:
- **(1) prints `NOT DONE: row 76`.** ⚠️ **This is the change of the stage.** Check (1) was SILENT at the phase-75 IMPL close (row 75 was the last non-`done` chartered row); **row 76's registration RE-OPENED it**, for the second time in the project's history. It stays live until row 76 flips `done` at the phase-76 IMPL.
- **(2) prints THREE live "candidates:" sentences** — HTTP/3, xDS, Observability. ⚠️ **Their anchors each moved +1** when row 76 was inserted at `:138`; RE-DERIVE them with `grep -n`. **Cite the command, never the adjective** (`reference_sentinel_deferred_sentence_live_vs_historical`): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` => **3**. ⚠️ A naive `grep -c 'candidates:'` returns a larger number, and the `candidates were:` recaps inside the Observability sentence are HISTORICAL.
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** => THREE families never opened. (The `WASM` line remains a ROADMAP **bookkeeping artifact** — `internal/wasm/` is a substantially-built wazero host and rows 25/25.1/25.2/25.3 are all `done` — deliberately LEFT AS-IS.)

All three print => the sentinel does NOT fire. Abundant work remains.

⚠️ **NEVER WRITE A SENTINEL'S OWN MATCHER STRING INTO A FILE THE SENTINEL GREPS. THIS FIRED LIVE AT THE PHASE-76 BRAINSTORM, TWICE, IN ONE COMMIT.** Row 76's first draft quoted check (3)'s marker phrase for the gRPC family — *inside the sentence explaining why quoting it would be gaming the check* — and check (3) came back with **gRPC silently GONE**. The second occurrence was subtler and worse: the summary also quoted **the command and its result** (`grep -c '<marker>' ROADMAP.md` → 0), a claim the document's own landing falsifies. **`grep` cannot tell a mention from a use.** Both were removed and the token is now deliberately NOT spelled anywhere in `ROADMAP.md`. ⇒ **Re-run every sentinel check AFTER your stage's edits land, not only at session open** — the pre-edit run is clean and meaningless, and this near-miss was invisible to review by eye because the sentence was *about* not doing the thing.

⚠️ **CHECK (1)'s BLIND SPOT — RE-DERIVE IT; NEVER COPY IT.** At this tip it is **108 data rows (`:31`-`:138`), 104 matched, FOUR misses** — `| 00 | bootstrap | — | done |` (the `[0-9.,  ]*` field does not match an **em-dash**), `| 04 | http-1.1 | 03 | done |` (the `[a-z0-9-]+` slug field does not match the **DOT**), and **`| 28.1a |` / `| 28.1b |`** (the `^\| [0-9.]+ \|` id field cannot match a **letter suffix**). All four are `done`, so there is no current impact. ⚠️ **This single figure was recorded WRONG in two consecutive lineages** (phase-74 PLAN: *"104 rows, 102 matched, TWO misses"*; the phase-75 BRAINSTORM router: *"106 rows, 102 matched"*, whose provenance claim was ALSO false). It was re-derived at the phase-75 IMPL (107/103/4) and again at this BRAINSTORM (108/104/4 — row 76's own addition moved it). **Derive it yourself.**

==============================================


## STATUS AS OF 2026-07-25: PHASE 76 IS BRAINSTORMED. ROW 76 IS `in-progress`. THIS SESSION PERFORMS THE **PHASE-76 SPEC**.

**What the phase-76 BRAINSTORM landed** (`git log --grep 'phase 76'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): the SELF-PICKED subject **`lb-ring-hash-spread-margin`**, a **Load-balancing-family MAINTENANCE row** — give `0061-lb-ring-hash`'s spread assertion a **DERIVED statistical margin** (`sourceIPs` **4 → 10**), land a **seeded collapse-rate MEASUREMENT** in `internal/cluster/ringhash_test.go`, and sweep the prose the fix falsifies. ~5-7 tasks; anticipated **+0 on every envelope axis, including +0 PRODUCTION `.go` FILES** — the first chartered row in this lineage expected to change no production code at all. Docs-only at the BRAINSTORM: exactly THREE files edited (`ROADMAP.md`, `STATE.md`, `next-prompt.txt`) plus the new `BRAINSTORM.md`; **DECISIONS.md BYTE-UNTOUCHED**.

### What matters for the NEXT session, in order

1. **⚠️ THE SENTINEL DID NOT FIRE AND `stop` WAS NOT CREATED. Re-run all three checks yourself and record the ACTUAL output; do not predict it.** ⚠️ **Check (1) is LIVE again** (`NOT DONE: row 76`) and stays live until the phase-76 IMPL. **Create `stop` if and ONLY if ALL THREE print nothing.** All three will print, so it will NOT fire. **Re-run them AGAIN after your own edits land** (see the hazard above).

2. **⚠️ THIS IS A SPEC — settle the docket, do not implement.** The BRAINSTORM's **D-RHSM-*** docket (`docs/envoy-go/phases/76-lb-ring-hash-spread-margin/BRAINSTORM.md` §7) is the stage's work list. Draft **ADR-0298 §Context** per ADR-0044-**as-used** if D-RHSM-ADR resolves YES. ⚠️ **The phase-75 PLAN "built the row it was only meant to plan" and the phase-75 SPEC ran two live probe fleets — this lineage's stages routinely exceed their nominal remit. That is acceptable when it is EXECUTED and RECORDED; it is not acceptable when it is asserted.**

3. **THE DOCKET, in the order the row's risk actually runs.**
   - **D-RHSM-CXTOTAL — ⚠️ EXECUTE, DO NOT REASON.** `AssertStats` asserts `cluster.c_echo.upstream_cx_total == totalConns` **cross-equal** (`0061/driver/driver.go:335`) and `upstream_rq_total == totalConns` **reference-side** (`:367`). Both move **64 → 160**. `tcp_proxy` is 1:1 downstream-conn→upstream-dial on both sides so this *should* hold — **it is the ONLY assertion whose VALUE changes, and it must be run.**
   - **D-RHSM-MARGIN.** State the target rate as a **POLICY** and DERIVE the constant from it, rather than picking the constant and reporting its rate. `sourceIPs = 10` → p ≈ 5.1e-5; `8` → p ≈ 4.6e-4 with 128 conns.
   - **D-RHSM-VERIFY.** Pin the measurement test's M, seed, asserted bar and location; confirm the **β-asymmetry** is executable as described.
   - **D-RHSM-LOOPBACK** (do `127.0.0.6`-`.11` bind as `LocalAddr` here?) · **D-RHSM-RUNTIME** (record the 64→160 wall-clock delta) · **D-RHSM-ADR** · **D-RHSM-SWEEP** (report-only: any sibling fixture with an underived probabilistic margin).

4. **⚠️ THE BREAK TRIO IS THE ROW'S DESIGN CORE, AND THE LOAD-BEARING BREAK IS AN ASYMMETRY, NOT A FAILURE.**
   - **α (liveness)** — neuter the ring lookup in `Pick` — replace the `m := sort.Search(...)` at `internal/cluster/ringhash.go:140` with `m := 0` so every key resolves to `ring[0]` (⚠️ `Pick` is DECLARED at `:133`; `:129` is its DOC COMMENT and `:142`'s `m = 0 // wrap` is the LEGITIMATE wrap — do not edit either). Expected: `subject spread: only 1 backend(s) nonzero, want >= 2 (ring collapsed?)`. ⚠️ **Confirm WHICH assertion fired** (`reference_deliberate_break_wrong_assertion`): under total collapse the **affinity** leg SURVIVES because 160 % 16 == 0 and **conservation** survives because the sum is unchanged. Run `-count=1` (`reference_differential_break_protocol_count1`).
   - **β (THE PROOF)** — with the measurement in place, revert `sourceIPs` 10 → 4. **Expected: the unit MEASUREMENT FAILS (`p̂ ≈ 0.037 ≫ 1e-3`) while the differential fixture stays GREEN.** That asymmetry demonstrates mechanically that the fixture alone cannot distinguish K=4 from K=10. Without β the fix is indistinguishable from a coincidence.
   - **γ (anti-vacuity)** — restore 10; confirm the measurement PASSES rather than never having run (`reference_liveness_break_needs_failing_baseline`).

5. **⚠️ A PASS-COUNT IS NOT A MARGIN — the project already fell into this once, and the evidence is in the fixture.** `0061/README.md:143-148` certifies the assertion *"overwhelmingly stable"* on a **`-count=20` → 20/20 PASS** run and calls the ring *"fixed"*. **Both are false.** The ring is keyed on the endpoint address **including its OS-ephemeral port** (`ringhash.go:88-91` + `test/differential/runner_test.go:272`), so it is a **fresh random 3-way partition on every run**; and `(1−0.0365)^20 ≈ 0.48`, so the check was **more likely than not to pass even if the assertion were exactly as broken as it in fact is.** ⇒ **do not verify this row by re-running it.** ⚠️ **The existing unit test cannot serve as the measurement either** — `internal/cluster/ringhash_test.go:50-64` builds `eps(3)` with **FIXED** addresses, holding fixed the exact variable the fixture randomizes.

6. **The diagnosis, and it was derived THREE independent ways that all agree — but RE-DERIVE it.** The governing variable is **K = distinct hash keys = `sourceIPs` = 4**, NOT the 64 connections: `HashSourceIP` (`internal/cluster/hash.go:133`) **STRIPS THE PORT**, so 16 connections from one source IP are **ONE** key. ⇒ **P(collapse) = 3·(1/3)^K = 3^(1−K)**. Analytic **0.0370**; controller Monte Carlo (200 000 trials/K, fixed seed, model built from the code) **0.0365**; an agent's independent replica (60 000 trials/K, real `xxHash64`, sanity-checked against the fixture's own 1026/342 ring-size gauge) **0.0349**. ⚠️ **`burstPerIP` MUST NOT MOVE** — it is the AFFINITY leg's discriminating modulus (`driver.go:284`); rebalancing to `8×8` to hold `totalConns=64` trades a 3.7% spread flake for a **~15× weaker affinity leg**. **`0062-lb-ring-hash-http` needs NOTHING** — identical assertion text but `hashValues = 16` ⇒ P ≈ 7e-8; the entire delta between a fixture that flaked three times and one that never has is **K**.

7. **⚠️ THE EXECUTABLE DELTA IS ONE INTEGER — verify this yourself before scoping edits.** `grep -n '\b64\b' test/fixtures/0061-lb-ring-hash/driver/driver.go` returns **7 hits, EVERY ONE A COMMENT**; every assertion — conservation (`:291`/`:302`), affinity (`:284`), both stat checks (`:335`/`:367`) — routes through `totalConns`/`burstPerIP`, and `expectations.yaml`'s six `64`s are all `#` comments. This is the **INVERSE** of `reference_fixture_workload_constant_desync`: the driver is already clean and only the NARRATION desyncs.

8. **⚠️ AFTER LANDING, SWEEP FOR PROSE THAT SAID THE FIXTURE DID NOT HAVE A MARGIN — and grep for the SHAPE of the old claim, not just its numerals** (`4`, `64`, "fixed ring", "20/20", "overwhelmingly stable"). A sweep that greps only the stale numerals cannot see a site falsified in the opposite direction — the phase-75 `0110/expectations.yaml` lesson, where a FOURTH stale site existed and was invisible because the sweep searched the STALE vocabulary.

9. **The rivals — ALL re-costed at the phase-76 BRAINSTORM; re-derive again if you touch them** (`reference_deferred_candidate_cost_restale`). **⚠️ The recommendation carried through three prior documents is REFUTED:** `fault.abort.grpc_status` does **NOT** open the gRPC family (that family charters bridge · gRPC-Web · gRPC-JSON transcoding · interop conformance, and a fault-injection knob delivers none of them), and its cost re-derived **10-13, not 7-9** — a live 23-arm fleet retired its ADR-0058 blocker (`grpc-status` is a response **HEADER** on both H1 and h2c, zero trailers on all 23 arms) but surfaced **three live divergences on already-shipped code**. `ssl.connection_error` **~10-13, NOT cheaper** — its recorded predicate is **INCOMPLETE** (an RST peer yields `*net.OpError`, for which BOTH `errors.Is(io.EOF)` and `errors.Is(io.ErrUnexpectedEOF)` are FALSE), needing `syscall.ECONNRESET` ⇒ **+1 PRODUCTION import**, and its `sslLeafRoster` is **INERT** for that leaf. `upstream_cluster` ~7-9 but **~85-100 lines across ~18 files**, central premise **UNVERIFIED**. `Listener.stat_prefix` ~7-10. **The Runtime family opening ~10-14 — the only evaluated candidate that GENUINELY clears a check-(3) blocker**, with SIX landed ADR-anchored `runtime_key` PARSE-REJECT arms already forward-pointing at it (ADR-0187 `DECISIONS.md:11280`, ADR-0195 `:12413`); ⚠️ but a read-only `/runtime` endpoint is **VACUOUS** alone and `/runtime_modify` is strictly larger.

10. **⚠️ AGREEMENT ACROSS DOCUMENTS IS NOT EVIDENCE.** The phase-76 session opened treating `fault.abort.grpc_status` as the presumptive pick because the phase-75 SPEC §13, the phase-75 PLAN §5 and the router all agreed. **All three cites trace to ONE unexecuted assertion**, and the refutation cost one `grep -c`. ⇒ **a claim that has survived several stages is MORE suspect than one written yesterday, not less** (`feedback_brief_citations_not_evidence`, `reference_brainstorm_adjective_acquires_adr_authority`).

11. **⚠️ A DOCUMENT'S OWN GREP CLAIM GOES FALSE WHEN THE DOCUMENT LANDS — the longest-running lesson in this lineage, now with a FOURTH instance.** ADR-0296 ¶3 (fixed at phase 74) · ADR-0297 ¶7 (fixed at phase 75) · ADR-0297 ¶9's *"the only two instances in the file"* (which phase 75's own blockquote made three) · **and now `ROADMAP.md` row 76's quoted `grep -c`, which flipped a TERMINATION SENTINEL rather than merely a number.** ⇒ **Never write a whole-file count into a document that the same commit changes.** State the property with NO number, or scope the grep to a line range. `reference_document_hygiene_claim_not_evidence`.

12. **⚠️ TWO GATE COMMANDS FROM THE PHASE-75 PLAN ARE BROKEN. DO NOT INHERIT THEM.** The **import gate FAILS CLOSED** (its `impblock` helper prints `FILENAME`, so a `/tmp` baseline vs an `internal/` HEAD differs on every line — 31 removed / 31 added, exit 1, on a **+0-import tree**; normalise to basenames and **re-run the negative control yourself** — a green gate is evidence only if you have seen it go red). **`go doc -all <pkgA> <pkgB>` FAILS OPEN** — `go doc` takes `<pkg> [symbol]`, so the second package is read as a SYMBOL: only `<pkgA>` is emitted, **exit 0, empty stderr**. **Audit each package SEPARATELY.** ⚠️ Phase 76 anticipates **+0 production files**, so both gates should be trivially green — **which is exactly when a broken gate goes unnoticed.** Negative-control them.

13. **⚠️ THE STAT-SURFACE ABSOLUTE TOTAL IS DOCUMENTARY, NOT MEASURED — and the chain is discontinuous in TWO places.** `1205` has **NO mechanical command**; it rides the recorded `1200 → 1201` gap **and** a second (`Phase 46.1b` closes at `1198` while `Phase 47.1` opens at `1200`, an unattributed **+2**). **Both are RECORDED in the ledger and NEITHER was back-filled.** Phase 76 asserts a **delta of ZERO**, which IS mechanically checkable. **Do not present `1205` as re-derived.** ⚠️ A **+0** row is an unusually cheap place to finally COUNT — but phase 76 does **not** charter that, and widening it is out of scope.

14. **The live hazards — never reflex-classify any of these as a regression.** the PRE-EXISTING `internal/cluster` `-race` flake (`TestOutlierDetector_ConcurrentEjectExactlyOnce`) · the full-suite startup flake (`subject ready: EOF`, and at phase 75 also `bind: address already in use` on an UNRELATED fixture, **failing BEFORE any assertion**) · `reference_sds_init_fetch_timeout_dial_budget_flake` (covers `internal/boot TestSDSEndToEnd_FetchFailure_BootFailsClosed` AND the original `internal/xds` test) · **`0061-lb-ring-hash` — ⚠️ THIS ROW'S OWN SUBJECT; a firing before the fix is EXPECTED at ~3.7%/run, and after the fix it is a FINDING** · and TWO still-**UNINDEXED** load flakes: `internal/httpclient TestOptions_ZeroValue_NoOpDefaults` and `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery`. ⚠️ **A stage brief's flake list is not the index — NEITHER flake that fired at phase 75 was on the PLAN's roster of six.** Isolate-re-run, then state the classification and its evidence.

15. **Counts at exit (re-run MECHANICALLY in the worktree; never copy them):** fixtures **119** (`ls -d test/fixtures/[0-9]*/ | wc -l`) / fuzzers **55** (`grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`) / **stat surface 1205** (BEHAVIOR_CONTRACT doc count; **NO mechanical command** — see item 13) / BackendKind tail **38** (a TAIL VALUE — the file declares 39 constants, 0-38) / go.mod modules **2** (the lineage figure; the single `go.mod` requires **67**) / DECISIONS tail **ADR-0297 COMPLETE** (next-free **ADR-0298**) / next-free reference port **10450** (⚠️ **NOT** `10118` — ports are NOT fixture-index aligned; phase 76 needs no new port).

## What THIS session does — the phase-76 SPEC

One stage per session. Write `docs/envoy-go/phases/76-lb-ring-hash-spread-margin/SPEC.md` settling the **D-RHSM-*** docket by EXECUTION where execution is possible (D-RHSM-CXTOTAL and D-RHSM-LOOPBACK both are), draft **ADR-0298 §Context** in `docs/envoy-go/DECISIONS.md` if D-RHSM-ADR resolves YES (per ADR-0044-**as-used**; the SPEC is where the §Context append lands — the phase-75 `2b5eff0a` precedent), and roll STATE + this file. **ROW 76 STAYS `in-progress`** and the ROADMAP is normally **BYTE-UNTOUCHED at a SPEC** (the phase-75 precedent). **NO deferred sentence is narrowed** (the phase-57 precedent) — the narrow lands at the IMPL only; check (2) must **STAY 3**.

**Stage-close mechanics (a SPEC):** STATE §Current rolled **IN PLACE** (lifecycle **1 → 2**; §Recent re-capped at FIVE **with its PREAMBLE updated** — the ADR-0288 rule). ⚠️ **The ADR-0288 singleton greps return 2, not 1**, for each of the three field-name tokens — and **the RULE STATEMENT is `STATE.md:7`, the FIRST hit**, with the live value second (a close script taking `head -1` reads the rule, not the value). Verify with `grep -n`, and **never "fix" the count to 1**; that would delete the rule. `next-prompt.txt` rolled to the **phase-76 PLAN**.

Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** (`reference_parallel_subagents_private_scratch`).

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. ⚠️ **THIS HAS NOW FIRED LIVE IN FOUR CONSECUTIVE SESSIONS — including at the phase-76 BRAINSTORM, where a `cd`-prefixed compound command reset the shell back to the MAIN checkout mid-stage (the tool reported it inline).** At the phase-75 IMPL's T10 `pwd` had silently reset to the MAIN checkout on `master` one step before a gate run; at the phase-72 close the same hazard put a stray commit on `master` AND made a "final gates" run validate master's pre-row tree — GREEN while proving nothing. **Use `git -C <abs-worktree-path>` for every git command, prefer absolute paths over `cd`, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline) + §Project counts + the §Recent lineage (five bullets: phase-76 BRAINSTORM + phase-75 IMPL + phase-75 PLAN + phase-75 SPEC + phase-75 BRAINSTORM).
2. `docs/envoy-go/phases/76-lb-ring-hash-spread-margin/BRAINSTORM.md` — **the whole document**, and especially §2.3 (the derivation and the fix), §2.5 (the verification design and the break trio), §2.6 (the rivals re-costed), §7 (the D-RHSM-* docket you must settle), and **§11.5** (the sentinel near-miss this stage committed against itself).
3. `docs/envoy-go/ROADMAP.md` — the §Schema invariant (`:21`), the **row-76 cell (`:138`, `in-progress`)** you will NOT edit at a SPEC, the family headings, and the THREE live `candidates:` sentences. ⚠️ **RE-DERIVE every anchor with `grep -n`** — row 76's insert shifted everything below `:138` by +1.
4. `test/fixtures/0061-lb-ring-hash/` in full — `driver/driver.go` (constants `:78-80`, the drive loop `:193-216`, `AssertDistribution` `:276-305`, `AssertStats` `:308+`), `expectations.yaml`, and `README.md` (⚠️ **`:143-148` is the refuted flake-check paragraph**). Plus `internal/cluster/ringhash.go` (`:85-95`, `:129`), `internal/cluster/hash.go:133`, `internal/cluster/ringhash_test.go:50-64`, and `test/differential/runner_test.go:272`.
5. `docs/envoy-go/phases/75-tls-no-certificate-stat/PROGRESS.md` — **the richest single source on how this lineage's stages fail**: what every task ACTUALLY did, which break fired and how, the fourteen refuted PLAN/SPEC claims, and the two broken gate commands.
6. `docs/envoy-go/DECISIONS.md` — **ADR-0297** in full as the immediately-prior ADR form to mirror (§Context ¶1-¶11 with ¶7/¶9 as CORRECTED, plus §Decision and §Consequences); ADR-0044 (⚠️ it does NOT contain the §Context-draft discipline — use `ADR-0044-as-used`) · ADR-0045 · ADR-0106 · ADR-0288. ⚠️ **If you write ADR-0298, put NO whole-file grep count in it** (item 11).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Load-bearing for this SPEC: **`reference_0061_ring_hash_spread_flake`** and **`reference_differential_band_sigma_margin`** (this row's subject), `reference_differential_break_protocol_count1`, `reference_deliberate_break_wrong_assertion`, `reference_liveness_break_needs_failing_baseline`, `reference_positive_arm_cannot_catch_overfiring`, `reference_fixture_workload_constant_desync`, `reference_differential_hash_key_cross_side_infeasible`, `reference_quoting_is_not_executing`, `feedback_brief_citations_not_evidence`, `reference_document_hygiene_claim_not_evidence`, `reference_probe_must_discriminate`, `reference_next_prompt_tracked_despite_gitignore`, `feedback_git_worktrees`, `feedback_execution_style`, and **`reference_bash_cwd_reset_commits_to_main`**.
