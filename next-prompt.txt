=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, the active phase is fully landed, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action. (The sentinel does NOT apply right now — see below.)

**STATUS AS OF 2026-07-06: phase 52 (`load-balancer-locality-weighted`) SPEC is LANDED (row 52 stays `in-progress`, lifecycle-state 1 → 2). The NEXT action is the phase-52 PLAN.**

- The SPEC (`docs/envoy-go/phases/52-load-balancer-locality-weighted/SPEC.md`, the 14-section phase-38/37 shape) executed all 8 BRAINSTORM D-LW questions IN-SESSION via two parallel probe agents live against `envoyproxy/envoy:contrib-v1.37.2` on a Docker bridge network (`reference_docker_probe_bridge_network`).
- **One BRAINSTORM assumption was REFUTED and CORRECTED (AMEND-LW2):** an omitted `load_balancing_weight` does NOT default to 1 — the reference assigns that locality ZERO load whenever a sibling locality has an explicit weight (confirmed both by the go-control-plane proto doc comment and live traffic: A=2-explicit/B-omitted → A got all 900 requests, B got 0); when NO locality anywhere sets a weight, the mechanism degrades to flat round-robin. envoy-go's design (§3.1 of the SPEC) unifies both outcomes with ONE rule — a locality's raw captured weight (0 if unset, zero substitution logic) naturally yields an empty weighted-draw bucket, and a zero TOTAL effective weight across every locality falls back to the flat all-endpoints child panic mode uses.
- **The hypothesized effective-weight formula was CONFIRMED exactly (AMEND-LW3):** `effective_weight = configured_weight × min(1, (overprovisioning_factor/100) × healthy_fraction)` matched a live 6-point degradation curve to within <1 percentage point at every measurement; an OPF-140-vs-100 A/B at an identical health state proved `ClusterLoadAssignment.Policy.overprovisioning_factor` (default 140) must be genuinely CONSUMED, not hardcoded.
- **Panic-mode interaction CONFIRMED (AMEND-LW4):** cluster-wide panic (the EXISTING `clusterHealth.inPanic` gate, unmodified) fully bypasses BOTH health-filtering and locality-weighting.
- **Stat-surface delta REFUTED DOWN to a confirmed ZERO (AMEND-LW5)** — no dedicated locality-weighted stat group exists in the reference; stat surface STAYS 1200 exactly (the family's THIRD zero-stat-delta phase).
- **Both planned rejects CONFIRMED as genuine envoy-go-strict departures (AMEND-LW1)** — the reference accepts both `lb_subset_config`+`locality_weighted_lb_config` together (silently overriding locality weighting) and `zone_aware_lb_config` alone (a fully separate, working feature); envoy-go explicitly rejects both rather than replicate the confusing silent behavior or build the separate algorithm.
- Architecture (D-LW7 CONFIRMED): `internal/cluster/locality.go`'s `localityWeightedLB` — a wrapper (the phase-38 `subsetLB` structural precedent) needing ZERO new `Pick` parameters; `Endpoint` grows `Locality`/`LocalityWeight` (the `Metadata` precedent); a second wrap-after-switch site in `manager.go`'s `buildCluster`. Anticipated ~220–320 prod LoC / 10 tasks (D-LW8 CONFIRMED) — NO pre-authorized split. Differential: the NEW `0095-lb-locality-weighted` fixture (2 localities × 5 hosts, 2:1 weight) — a per-side static-ratio band arm + a health-degradation shift arm reusing the phase-39 poll-to-convergence-and-warmup pattern.
- 1 ADR anticipated (**ADR-0269**, single-ADR reuse shape, confirmed correct; §Context drafted at SPEC §13, §Decision/§Consequences land at IMPL). A `general-purpose` spec-document-reviewer pass VERIFIED every file:line citation + proto field number + baseline count EXACT against HEAD — APPROVED-WITH-FOLDS after one non-blocking wording fold.
- Counts UNCHANGED at this SPEC (stat 1200 / fixtures 96 / fuzzers 52 / BackendKind 38 / DECISIONS tail ADR-0268, next-free ADR-0269). Anticipated at the phase-52 IMPL: stat 1200 (+0) / fixtures 97 (`0095`) / fuzzers 52 (+0) / BackendKind 38 (+0) / DECISIONS ADR-0269 (next-free ADR-0270); ZERO new packages, ZERO new go.mod deps.
- STATE.md/ROADMAP.md/next-prompt.txt updated (docs-only, authored in worktree `.worktrees/phase-52-spec` branch `phase-52-load-balancer-locality-weighted-spec`; controller squashes + pushes at stage-close per `feedback_push_to_origin`).

=> The NEXT action is **the phase-52 PLAN** — author `docs/envoy-go/phases/52-load-balancer-locality-weighted/PLAN.md` (`superpowers:writing-plans`), decomposing SPEC §10's ~10-task breakdown into a bite-sized TDD spine (every task showing complete, runnable code, no placeholders), resolving the SPEC's §12 PLAN/IMPL-level D-questions (D-LW-DUP the duplicate-locality-identity corner, D-LW-HEALTHALLOC the unconditional health-registry allocation, D-LW-OPF0 the explicit-zero-overprovisioning-factor corner), and re-confirming the FINAL ADR-0045 no-split disposition (§3.0 of the SPEC).

==============================================

## Read first (in order)
1. THIS FILE — the cold-start router.
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 52 SPEC done`) + history.
3. `docs/envoy-go/ROADMAP.md` — row 52 (`in-progress`, lifecycle-state 2).
4. `docs/envoy-go/phases/52-load-balancer-locality-weighted/SPEC.md` — the FULL 14-section SPEC (§3 has the `localityWeightedLB` indicative code shape; §10 the task breakdown; §11 the 8 resolved D-LW empirical pins with exact live-probe evidence; §12 the 3 PLAN-level D-questions still open; §13 the ADR-0269 §Context draft).
5. `docs/envoy-go/phases/52-load-balancer-locality-weighted/BRAINSTORM.md` — the original design dialogue, for context on WHY each decision was made.
6. `docs/envoy-go/phases/38-load-balancer-subset/PLAN.md` — the most recent same-family phase, useful as a PLAN shape template (the TDD task-spine convention, per-task gate discipline).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — re-check for any entries relevant to locality-weighted LB, health-check propagation warmup, or the `Endpoint`-construction-site-enumeration lesson (`reference_conn_wrap_method_no_promote`, generalized).

## Honor throughout (the discipline, unchanged for whichever phase comes next)
- **Lifecycle = one stage per session** (BRAINSTORM → SPEC → PLAN → IMPL).
- **Subagent-driven execution** (`feedback_execution_style`) in a **fresh worktree** (`feedback_git_worktrees`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit, re-runs the full suite on the frozen HEAD, does any deliberate-break verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). Per-task gates: `gofmt -l` + `golangci-lint` + `go vet` + `go build ./...` (`feedback_pertask_gofmt_lint`).
- **Process anchors:** ADR-0106 (per-leg rows) · ADR-0044 (ADR §Decision+§Consequences land at IMPL) · ADR-0045 (sub-split soft gate) · next-free **ADR-0269**.
- Re-verify baselines fresh at cold-start — do NOT trust any count in this file without re-running the baseline commands (`ls -d test/fixtures/*/ | wc -l`, `grep -rh '^func Fuzz' --include='*.go' . | wc -l`, etc.) against current HEAD.
