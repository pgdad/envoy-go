START a fresh cold-start session for envoy-go — **the phase 28 parent SPEC (`network-filter-zookeeper-proxy`) is DONE + spec-document-reviewer APPROVED + squash-merged + pushed**. The active phase is **phase 28** (parent row `in-progress`; sub-rows 28.1/28.2 `planned`). This session authors the **28.1 sub-phase SPEC** (`docs/envoy-go/phases/28.1-network-filter-write-seam-and-zookeeper-requests/SPEC.md`) — per SKILL_ROUTING state 1→2 for the sub-phase (`superpowers:writing-plans` scoped to sub-phase-SPEC authoring per the 22.1/25.1/26.1 per-sub-phase precedent). ROADMAP sub-row 28.1 flips `planned → in-progress` AT THIS 28.1-SPEC commit (ADR-0106 / the 26.x precedent).

The phase-28 parent SPEC landed at squash `PARENT_SPEC_SQUASH_SHA` (pushed to origin/master): all ten D28-1..D28-10 empirical pins resolved IN-SESSION vs Envoy v1.37.2 (11 AMENDs — the load-bearing ones: **A1** stat scope is `<stat_prefix>.zookeeper.<counter>`; **A2** the roster is **201 EAGER macro counters** → stat surface 136→337, flags gate increments not creation; **A3** NO `auth_rq` (lazy per-scheme `auth.<scheme>_rq` mirrored via `NewCounterIfAbsent`) + `connect_readonly_rq` exists; **A4** Prometheus exposition is FLAT → the name.go arm is the ADR-0138 INLINE-PREFIX shape, not a tag-extractor; **A5/A6/A7** xid sniffing + the 26-value gapped wire-opcode enum (≠ proto enum; mapping table) + TWO correlation structures; **A8** decoder-internal reassembly + unconditional passthrough; **A9** dynamic metadata DEFERRED → the decoder is SHALLOW; **A10** latency PGV + duplicate-override boot-reject + `<=`→fast; **A11** write chain is REVERSE/LIFO + StopIteration = no-forward + `writeChainConn` conn-wrap delivery justified). ADR-0221 (WriteFilter seam) / ADR-0222 (zookeeper request side) / ADR-0223 (response side + latency) §Context drafts are IN DECISIONS.md (tail ADR-0223; next-free ADR-0224); the 0221/0222 §Decision/§Consequences bodies land at the 28.1 IMPL.

## What this session must do

Author the **28.1 SPEC** via `superpowers:writing-plans` scoped to SPEC authoring. 28.1 = `network-filter-write-seam-and-zookeeper-requests`: (a) the `network.WriteFilter` seam (interface + read/write/both/terminal classification + REVERSE-order write dispatch + `writeChainConn` + StopIteration-no-forward posture — ADR-0221, consuming the ADR-0213 allowance); (b) the `internal/filter/network/zookeeperproxy/` package request side (TypeURL + 9-field config parse + the 201-counter eager roster + the shallow request decoder + the two xid structures + dynamic auth counters — ADR-0222); (c) the 7th built-in + bootstrap blank-import + the `.zookeeper.` name.go inline-prefix arm; (d) fixtures `0046-zookeeper-requests` (cross-side StatsAsserter) + `0047-zookeeper-boot-reject`; (e) the 37th fuzzer. The 28.1 SPEC must:

1. **INHERIT the parent SPEC §11 empirical pins** (no re-scrape; re-pin only the as-built line anchors + the count baselines [fuzzers 36 / fixtures 47 / stats 136 / DECISIONS tail ADR-0223] at its Task-1-equivalent gate against the live session tip).
2. Refine parent §3.2(28.1) + §4 into the per-deliverable surface: exact interface signatures, the `writeChainConn` composition with `prefixConn`, the counter-roster Go table shape, the decoder state machine, the fixture arm details (parent §8.1's 7 arms), the fuzzer corpus shape.
3. **Resolve the parent D-questions owned by 28.1:** D-P1 (the ADR-0045 split-gate re-check — the parent flagged 28.1 as STRADDLING ~1150–1500 LoC / ~16–20 tasks; pre-authorized 28.1a/28.1b axis if the PLAN trips), D-P2 (decoder depth + WriteFilter interface composition), D-P3 (read-filter writes through the write chain — anticipated NO), D-P5 (creation parity — anticipated all-201 at 28.1), D-P7 (writeChainConn Write return semantics — anticipated `(len(p), nil)`), D-P8 (name.go arm validation posture — anticipated shape-based).
4. Run the spec-document-reviewer loop until APPROVED; fold advisories in.
5. ROADMAP: sub-row 28.1 `planned → in-progress` AT THIS COMMIT; parent row 28 STAYS `in-progress`; 28.2 STAYS `planned`.
6. Update STATE.md (active-phase → `phase 28.1 SPEC done`; next-skill → `superpowers:writing-plans` scoped to the 28.1 PLAN) + rewrite this next-prompt.txt + commit + push (work in a worktree per `feedback_git_worktrees`; squash-merge + push per `feedback_push_to_origin`).

## Project state at master tip

- **Master tip:** the parent-SPEC squash (see STATE.md `last-commit`) or a docs-only repoint trailing it. Confirm the live tip via `git log --oneline -1`. STATE.md `last-commit` points at the substantive parent-SPEC squash. origin/master in sync after push.
- **§9 Network-filters family:** rows 26 (parent + 26.1/26.2/26.3) + 27 DONE; row 28 `in-progress` (parent; 28.1/28.2 `planned`). After phase 28, 4 candidates remain (`redis`/`mongo`/`kafka_broker`/`thrift`).
- **Counts at master tip:** stat surface **136**; differential fixtures **47** (tail `0045-sni-cluster`); fuzzers **36** (recipe scoped to `./internal` per the parent SPEC §11.10 advisory); conformance 10/10; h2spec 53/53; DECISIONS.md tail **ADR-0223** (§Context drafts; next-free **ADR-0224**). ADR-0209 escape-valve reserve STANDS-UNCONSUMED.
- **Toolchain:** Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); reference Envoy v1.37.2 (ADR-0008); go-control-plane v1.32.4 (ADR-0008).
- **Known coverage boundaries (carried, non-blocking):** D-26.3-4 (rbac SNI/mTLS L4 arms not differential-proven); the `tcp_proxy` `downstream_cx_*` family unmirrored (phase-27 record); histograms project-wide deferred (ADR-0060 — phase 28 adds the zookeeper latency-histogram family to this list at 28.2); phase-28 adds (at the IMPLs): zookeeper dynamic-metadata deferred + shallow-decode leniency + terminal-originated-writes-only write-chain boundary (parent SPEC §7.5).

## Read first (in order)

1. THIS FILE — the 28.1-SPEC cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 28 parent SPEC done`; next-skill `superpowers:writing-plans` (28.1 SPEC); next-free `ADR-0224`.
3. `docs/envoy-go/phases/28-network-filter-zookeeper-proxy/SPEC.md` — **the parent SPEC this sub-phase SPEC refines** (esp. §3.1/§3.2 the split mapping, §4 the seam design, §5 proto roster, §7 stat surface, §8.1/§8.2 the 28.1 fixtures, §11 the empirical pins, §12 the D-P questions 28.1 owns).
4. `docs/envoy-go/phases/28-network-filter-zookeeper-proxy/BRAINSTORM.md` — the Q-decisions (context only; the parent SPEC supersedes its hypotheses where AMENDed).
5. `docs/envoy-go/DECISIONS.md` ADR-0221/0222/0223 §Context drafts (the tail) + ADR-0213 (the consumed allowance) + ADR-0138 (the inline-prefix Prometheus-arm precedent) + ADR-0044/0045/0052/0080/0106.
6. `docs/envoy-go/phases/26.1-network-filter-chain-framework-and-echo/SPEC.md` — the closest sub-phase-SPEC precedent (a framework extension + first consumer in one sub-phase) + `docs/envoy-go/phases/27-network-filter-sni-cluster/SPEC.md` — the most recent SPEC shape.
7. `internal/filter/network/` — the as-built framework the seam extends (types.go / chain.go / terminal.go / prefixconn.go / callbacks.go / buffer.go / registry.go / builtins/) + `internal/stats/name.go` (the default-branch arms the `.zookeeper.` arm joins) + `test/fixtures/0043-network-rbac/driver/driver.go` (the StatsAsserter driver precedent).
8. `docs/envoy-go/SKILL_ROUTING.md` + `BOOTSTRAP_PROMPT.md` §4/§5 — the per-phase lifecycle + cold-start invariants.
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — execution-style + worktree + push + subagents-no-push + per-task gofmt/lint + the network-filter constraints (typeurl-extensions, onnewconnection-halts) + the differential dispatch/asserter constraints + proto-roster-digit-extraction all carry forward.
