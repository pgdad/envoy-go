START a fresh cold-start session for envoy-go — **phase 33 (`network-filter-thrift-proxy`) PLAN is DONE; the next lifecycle step is the phase-33 IMPL** (`superpowers:subagent-driven-development` — execute the 16-task PLAN task-by-task, a fresh subagent per task + review between tasks). The phase-33 PLAN squash LANDED on origin/master (`docs/envoy-go/phases/33-network-filter-thrift-proxy/PLAN.md` + the ROADMAP row-33 PLAN-DONE note + the STATE advance to `phase 33 PLAN done`; squash-merged from `phase-33-network-filter-thrift-proxy-plan` + pushed by the controller per `feedback_push_to_origin`; the PLAN worktree cleaned up + the branch deleted at stage-close — `git worktree list` is now just `[master]`). Confirm the literal live master tip via `git log --oneline -1`.

The phase-33 PLAN decomposed the SPEC §10 spine into **16 bite-sized TDD tasks**; the **ADR-0045 split-gate FINAL re-check at PLAN: NO SPLIT** (16 ≤ ~25 tasks / ~790–1275 ≤ ~1500 prod LoC; the 33.1/33.2 escape-valve UNCONSUMED — the kafka-31 precedent); the §12 D-questions resolved (D-S33-1..6); plan-document-reviewer APPROVED. Per `feedback_execution_style` the IMPL runs subagent-driven; per `feedback_git_worktrees` work in a worktree (anticipated `.worktrees/phase-33-network-filter-thrift-proxy-impl`, branch `phase-33-network-filter-thrift-proxy-impl`); per `feedback_subagents_no_push` subagents commit LOCAL-ONLY (the controller squash-merges + pushes at stage-close); per `feedback_pertask_gofmt_lint` each task runs `gofmt -l` + `golangci-lint run` on touched pkgs.

## What this session does — the phase-33 {thrift} IMPL

Execute `docs/envoy-go/phases/33-network-filter-thrift-proxy/PLAN.md` (`superpowers:subagent-driven-development`) — a fresh subagent per task; TDD per task (write the failing test → run-fail → minimal impl → run-pass → per-task `gofmt -l` + `golangci-lint` → LOCAL-ONLY commit). The 16 tasks land `envoy.filters.network.thrift_proxy` — the project's SECOND terminal routing proxy — at the as-pinned single-pair single-route-terminal MVP. The IMPL does NOT re-run the SPEC §11 empirical probes (the D-T1..D-T9 block + Appendix A are AUTHORITATIVE).

## The 16-task spine (PLAN §; do NOT re-derive — follow the PLAN's exact TDD steps + code)

  1. **Baselines/anchors gate** + create PROGRESS.md — re-confirm counts (fixtures 58 tail `0056` / fuzzers 41 / stat surface 1091 / BackendKind tail 32 / DECISIONS tail ADR-0231), re-pin the as-built anchors, establish the clean six-gate baseline.
  2. **Package skeleton + `TypeURL`** (a `proto.MessageName` pinning test — NEVER hand-typed) + the `thrift_proxy/v3` blank-import in `bootstrap.go` + confirm `go mod tidy` adds nothing (ZERO new dep).
  3. **Config parse** — `stat_prefix` required + `IsValidName` guard + transport/protocol accept `{AUTO,FRAMED}×{AUTO,BINARY}` else the envoy-go-strict DEPARTURE reject + `payload_passthrough` (byte-stable reject arms; a temporary `route.go` stub Task 4 replaces).
  4. **The `route_config` method-routing table** — exact `method_name` + empty=match-all → cluster; first-match; the route PGV reject arms.
  5. **Framed-transport frame decode + binary message-begin decode** (Appendix A; bounds-checked length prefix; bad-magic/bad-msgtype[`errInvalidType`]/truncated/oversized errors).
  6. **Reply classifier** (first-field peek → success/error) + the **`UnknownMethod` `AppException` encoder** (byte-identical to the reference miss-path reply; the frame-len is 75=`0x4b` for `no route for method 'somethingelse'`).
  7. **The EAGER 25-name stat roster** (24 counters + 1 gauge `request_active`; the histogram deferred; the 3 never-incremented close/drain counters).
  8. **The `TerminalFilter.Handle` pump** — route HIT round-trips via the REUSED `UpstreamConn` seam; route MISS writes the local `UnknownMethod` exception (keep conn open); decoding-error → `request_decoding_error` silent close; invalid-type → `request_invalid_type`; `request_active` balanced.
  9. **Register as the 11th built-in** (`builtins.go`; `main.go` needs NO change — it registers via `RegisterBuiltins`) + boot smoke.
  10. **The `thrift.` SINGLE-label-hoist Prometheus arm** in `internal/stats/name.go` (the redis-32 shape, after the `redis.` arm).
  11. **The `TCPThriftResponder` BackendKind (value 33)** — framed-binary canned REPLY echoing method + received `seq_id` (seq_id-agnostic) + an optional exception mode.
  12. **The `0057-thrift-roundtrip` cross-side fixture** — route-HIT round-trip + route-MISS local-exception + reply-EXCEPTION arms; downstream byte-equivalence + `StatsAsserter`; the per-side `cx_destroy`/`drain_close` boundary on the miss arm.
  13. **The `0058-thrift-boot-reject` fixture** (missing `stat_prefix`; per-side stderr parity) + the route/departure reject arms unit-tested.
  14. **The 42nd fuzzer `FuzzThriftDecode`** (no-panic / no-mutation / bounded-allocation; six corpus seeds).
  15. **Full differential re-verify** (60 dirs byte-exact) + the deliberate-break liveness proofs (`-count=1` per `reference_differential_break_protocol_count1`).
  16. **Completion bundle** — BEHAVIOR_CONTRACT 33 subsection (1091 → 1116) + the ADR-0231 §Decision/§Consequences body (ADR-0044 in-place) + STATE/ROADMAP row 33 `in-progress → done` (flat §9 row — NO parent rollup; the §9 Network-filters family CLOSES) + the six-gate evidence.

## Counts at master tip (phase 33 PLAN done — they advance at the IMPL six-gate)

- stat surface **1091** → **1116** (+25 = 24 counters + 1 gauge; the `request_time_ms` histogram deferred ADR-0060, not counted).
- differential fixtures **58** (tail `0056-redis-boot-reject`) → **60** (`0057`/`0058`).
- fuzzers **41** (tail `FuzzRESPDecode`) → **42** (`FuzzThriftDecode`).
- BackendKind tail **32** (`TCPRedisResponder`) → **33** (`TCPThriftResponder`).
- DECISIONS.md tail **ADR-0231** (§Context anchored at the SPEC; the §Decision/§Consequences body lands IN-PLACE at the IMPL per ADR-0044 — NO new number; next-free **ADR-0232**).
- reference pin: `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227; thrift_proxy is a CORE extension present in both the standard and contrib images; the project boots the contrib image).
- Toolchain: Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); go-control-plane `/envoy` v1.32.4 (ADR-0008) — thrift_proxy/v3 is a CORE `/envoy` subpackage → ZERO new go.mod dep.
- conformance h2spec 53/53 + proxy-wasm 10/10 (image-independent; UNAFFECTED by phase 33 — touches no HTTP/h2/proxy-wasm path).
- §9 Network-filters family: rows 26/27/28/29/30/31/32 DONE; **row 33 `in-progress` ({thrift}, the LAST candidate — the family CLOSES after phase 33 phase-done)**.

## Read first (in order)

1. THIS FILE — the phase-33 {thrift} IMPL cold-start.
2. `docs/envoy-go/phases/33-network-filter-thrift-proxy/PLAN.md` — **the authoritative 16-task TDD spine** (the file structure; the D-S33-1..6 resolutions; the ADR-0045 NO-SPLIT re-check; each task's exact failing-test + code + commands; the post-IMPL controller note).
3. `docs/envoy-go/phases/33-network-filter-thrift-proxy/SPEC.md` — the phase SPEC (§1.1 AMEND-T1..T7; §3 the REUSED seam + the package; §5/§6 the proto/reject rosters; §7 the 25-name roster + the `thrift.` hoist arm; §8 the `0057`/`0058` fixtures + the `TCPThriftResponder`; **§11 the D-T1..D-T9 empirical-pin block + Appendix A the framed×binary wire format — AUTHORITATIVE**).
4. `docs/envoy-go/STATE.md` — active-phase `phase 33 PLAN done`; lifecycle-state → the phase-33 IMPL; next-skill `superpowers:subagent-driven-development`; the counts.
5. `docs/envoy-go/ROADMAP.md` — row 33 `in-progress` + the PLAN-DONE note + the §9 candidate-roster paragraph (the family closes after 33).
6. `docs/envoy-go/DECISIONS.md` — **ADR-0231** (the thrift_proxy filter — §Context anchored; the §Decision/§Consequences body lands at THIS IMPL) + **ADR-0230** (the upstream-pool seam — REUSED unchanged) + ADR-0229 (redis_proxy — the precedent) + ADR-0106/0045/0044/0052/0060/0080.
7. `internal/filter/network/upstreampool.go` (the seam thrift REUSES unchanged) + `internal/filter/network/redisproxy/{redisproxy,config,resp,stats,filter,commands}.go` (the package shape thrift mirrors) + `internal/filter/network/builtins/builtins.go` (the registration site) + `internal/bootstrap/bootstrap.go` (the blank-import block ~line 119).
8. `internal/stats/name.go` (the `redis.` SINGLE-label-hoist arm at line ~315 the new `thrift.` arm mirrors) + `test/differential/fixture/fixture.go` (the `StatsAsserter` + the BackendKind enum, tail 32) + `test/fixtures/0055-redis-roundtrip/` (the cross-side fixture template for `0057`) + `test/fixtures/0056-redis-boot-reject/` (the boot-reject template for `0058`).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — especially `reference_close_direction_framework_gap`, `reference_wire_format_both_sides_see_same_bytes`, `reference_differential_reference_parses_full_message`, `reference_differential_break_protocol_count1` (the `-count=1` deliberate-break protocol), `reference_differential_asserter_dispatch` (cross-side → `StatsAsserter`), `reference_dynamic_stat_name_charset_guard`, `reference_network_filter_typeurl_extensions`, `reference_docker_probe_bridge_network`, `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_subagents_no_push`, `feedback_pertask_gofmt_lint`.

## Honor throughout

- `feedback_execution_style` (subagent-driven) · `feedback_git_worktrees` (work in a worktree) · `feedback_subagents_no_push` (subagents commit LOCAL-ONLY; the controller squash-merges + pushes at stage-close) · `feedback_push_to_origin` (after a clean landing on master, push without asking) · `feedback_pertask_gofmt_lint` (each task runs `gofmt -l` + `golangci-lint run` on touched packages).
- ADR-0106 (the §9 family flat-row discipline — phase 33 is a flat row) · ADR-0045 (split-gate re-checked at PLAN → NO SPLIT; the 33.1/33.2 escape-valve UNCONSUMED) · ADR-0230 (the upstream-pool seam — REUSED unchanged) · ADR-0044 (the ADR-0231 §Decision/§Consequences body lands at THIS IMPL — NO new number) · ADR-0052 (the atomic-landing six-gate) · ADR-0060 (histograms deferred) · ADR-0080 (byte-stable PARSE-REJECT wording) · ADR-0227 (the contrib pin) · ADR-0008 (the go-control-plane dep — ZERO new dep).
- At the phase-33 IMPL six-gate ROADMAP row 33 flips `in-progress → done` (a flat §9 row, NO parent rollup) — the §9 Network-filters family CLOSES (0 candidates remain). The counts advance (1116 / 60 / 42 / BackendKind 33).
