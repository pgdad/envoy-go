=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-CHECKED 2026-07-15 (after the phase-64 IMPL landed + ROADMAP row 64 flipped `done`, run MECHANICALLY) — it does NOT fire. Do NOT create `stop`.** State:
- (1) prints NOTHING — row 64 `tracing-max-path-tag-length` flipped `done` at its IMPL six-gate (ADR-0106, the SOLE leg); every ROADMAP row is now `done`. Check (1) NO LONGER blocks `stop` — but (2) and (3) still do.
- (2) prints THREE live "candidates:" sentences — HTTP/3, xDS, and Observability ⇒ three families STAY OPEN. The Observability sentence is UNCHANGED by this row (`max_path_tag_length` was a §8-tier candidate, NEVER in the live `custom_tags (metadata)` + OTLP-metrics + `spawn_upstream_span`/`http_service`/force-trace sentence). Each of these three families still carries un-chartered candidates.
- (3) prints `NEVER OPENED: gRPC`, `Runtime`, `WASM` ⇒ THREE families never opened. Operational-tooling also STAYS OPEN.

Checks (2) and (3) print ⇒ the sentinel does NOT fire. There is banked deferred work AND never-opened families — abundant work remains.

**STATUS AS OF 2026-07-15: the phase-64 IMPL LANDED and ROADMAP row 64 is `done`.** The `max_path_tag_length` tracing numeric knob is now HONORED — the SPEC-64 §10 9-task TDD spine landed clean (subagent-driven, SINGLE FLAT ROW, ADR-0045 escape-valve UNCONSUMED). The reject at `internal/tracing/config.go:112-114` was LIFTED to a resolve-to-scalar arm (`TracingConfig.MaxPathTagLength uint32`, default 256 / explicit incl. 0), and a shared provider-neutral `tracing.BuildHTTPURL(scheme, host, pathAndQuery, maxPathTagLen)` helper (new `internal/tracing/url.go`) byte-truncates the `http.url` built-in span attribute's `:path` (path+query) at the THREE `accesslog_emit.go` URL-build sites (H1/H2/H3), on BOTH the OTLP and Zipkin exporters; `BuildServerSpan`/`Span.Attrs`/`upsertAttr`/`ResolveCustomTags`/both exporters/the custom-tag machinery are UNCHANGED. Proven by the new `0107-tracing-max-path-tag-length` OTLP differential (cross-side `http.url` VALUE-equality on the truncated form — value-equality held, no authority-encoding fallback) plus `BuildHTTPURL`/config/span-emit/Zipkin unit tests + a `FuzzHCMConfigParse` seed; BEHAVIOR_CONTRACT flipped `max_path_tag_length` REJECT → CONSUMED. Six-gate GREEN + full 109-dir differential `ok ... 370.357s` exit 0 (108 pre-existing byte-stable, `0107` new+green). ADR-0285 landed; STATE flipped; ROW 64 `done`; router rolled to the roller's next self-pick BRAINSTORM. **WIRE-FORM learning (worth recalling for future tracing fixtures):** `max_path_tag_length` is a `*wrapperspb.UInt32Value` — its protojson bootstrap form is a BARE SCALAR (`max_path_tag_length: 16`), NOT the `{value: N}` object form used by `random_sampling` (a `Percent` MESSAGE); the object form FAILS to parse.

**Counts at phase-64 IMPL-DONE (VERIFIED on the landed tree; RE-VERIFY at session start, `git fetch` first):** fixtures **109** (`ls -d test/fixtures/[0-9]*/ | wc -l`; tail `0107-tracing-max-path-tag-length`) · fuzzers **55** · DECISIONS tail **ADR-0285** (next-free **ADR-0286**) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** · go.mod modules **2** (`quic-go` direct + `qpack` indirect).

==============================================

## What THIS session does (the roller's next self-pick BRAINSTORM)

Perform the BRAINSTORM stage (lifecycle-state 0 → 1) for the NEXT phase (phase 65). This is a DOCS-ONLY stage — NO production `.go` changes. Per the 2026-07-12 STANDING DIRECTIVE the loop is AUTONOMOUS: **the roller SELF-PICKS the next subject** — the smallest defensible candidate — and does NOT pause for a human pick. RECORD the pick AND the rejected alternatives in the BRAINSTORM (per `feedback_*` discipline + the standing directive). Run in a FRESH worktree off master, branch `phase-65-<slug>`, per `feedback_git_worktrees`.

**Candidate menu to weigh at the BRAINSTORM (survey ROADMAP §families + the three live deferred sentences + STATE's NEXT before picking; this list is a STARTING POINT, not a mandate):**
- **Observability family (STAYS OPEN)** — the SOLE remaining `custom_tags` type `metadata` (needs a dynamic-metadata subsystem envoy-go lacks — likely NOT the smallest) · the OTLP-metrics stats sink (a whole new sink) · tracing `spawn_upstream_span`/`http_service`/force-trace (each larger than a numeric knob).
- **HTTP/3 family (STAYS OPEN)** — upstream H3 cluster · alt-svc advertisement · 0-RTT/early-data · h3spec conformance · QuicProtocolOptions tuning · full QUIC transport-socket options · QUIC robustness (migration/RESET_STREAM/GOAWAY/datagrams/WebTransport).
- **xDS family (STAYS OPEN)** — SDS rotation · SDS validation_context/upstream SDS · CDS/EDS · LDS/RDS · ADS (muxed) · Delta xDS · RTDS/Runtime-layer · reconnection-backoff/initial_fetch_timeout edges · google_grpc transport.
- **Never-opened families** — gRPC · Runtime · WASM · (Operational-tooling STAYS OPEN too). Opening a new family is DEFENSIBLE but usually LARGER than banked in-family work; prefer the smallest cleanly-differential-provable candidate unless a new family's first row is genuinely smaller.

**Pick heuristics (the phase-59→64 precedent):** favor the smallest CLEANLY-DIFFERENTIAL-PROVABLE candidate that needs NO new infrastructure and rides an ALREADY-landed seam (the tracing custom_tags rows + this numeric-knob row are the model); a candidate provable with one fixture + unit tests beats one needing a new subsystem/sink/transport. Record WHY the pick is smallest-defensible and WHY each alternative was rejected (BRAINSTORM §2.1 precedent).

**Honor throughout:**
- **Lifecycle = one stage per session.** This session: the BRAINSTORM ONLY (docs-only; NO `.go`). After it lands: roll to the phase-65 SPEC.
- **The BRAINSTORM deliverable:** `docs/envoy-go/phases/65-<slug>/BRAINSTORM.md` — the subject, the self-pick rationale + rejected alternatives, the central design questions to PROBE at the SPEC (name them `D-<TAG>-*`), the anticipated envelope (fixtures/stats/fuzzers/packages/modules deltas), the anticipated split disposition (ADR-0045 single-flat-row vs a by-concern split), and the ADR reservation (next-free **ADR-0286**). Register the new ROADMAP row `in-progress` at the BRAINSTORM commit per the §Schema invariant (it flips `done` only at its IMPL six-gate, ADR-0106).
- **If the pick OPENS a never-chartered family** (gRPC/Runtime/WASM), ADD its family-row-summary slug to the sentinel check-(3) `for slug in ...` list in THIS file when you roll it forward (per the check-(3) note), so the sentinel keeps failing-safe.
- **Process anchors:** ADR-0044 (ADR §Context drafted at the SPEC, §Decision/§Consequences at the IMPL) · ADR-0045 (single-flat-row unless a second subsystem would strand) · ADR-0106 (row flips `done` at the IMPL six-gate) · `reference_roadmap_split_phase_row_done` · `feedback_brief_citations_not_evidence` (RE-DERIVE every `file:line` against the master tip; a citation is not evidence).
- **`next-prompt.txt` IS TRACKED** (`reference_next_prompt_tracked_despite_gitignore`) — edit it inside the stage worktree and fold into the stage squash; locate commits by SUBJECT (`git log --grep`), never by position.
- **Worktree discipline** (`feedback_git_worktrees`, `feedback_subagent_worktree_detach`, `feedback_subagent_worktree_path_targeting`) — pin the canonical worktree root; the controller verifies the MAIN checkout stays clean.

**ADR reservation ledger (provisional; each finalized at its IMPL in landing order):** ADR-0283 = phase-62 tracing-custom-tags-request-header (LANDED) · ADR-0284 = phase-63 tracing-custom-tags-environment (LANDED) · ADR-0285 = phase-64 tracing-max-path-tag-length (LANDED) · **next-free unreserved: ADR-0286** (the phase-65 subject reserves it at its BRAINSTORM/SPEC).

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-65 BRAINSTORM is next; the roller SELF-PICKS the subject).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 64 IMPL done`; NEXT = the roller's self-pick BRAINSTORM) + the prior-phase lineage.
3. `docs/envoy-go/ROADMAP.md` — the family sections + the THREE live deferred sentences (HTTP/3, xDS, Observability) + the §Schema invariant (register the new row `in-progress`) + the phase-59→64 tracing-row precedents.
4. `docs/envoy-go/DECISIONS.md` — ADR-0285 (the just-landed phase-64 entry, the newest tail) + the ADR-0044/0045/0106/0080 process anchors.
5. `docs/envoy-go/phases/64-tracing-max-path-tag-length/BRAINSTORM.md` — the FORMAT precedent for a self-pick BRAINSTORM (the pick rationale + rejected alternatives + the D-*-question framing + the anticipated envelope).
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the differential-harness `reference_*` traps + the tracing `reference_*` traps (esp. `reference_sentinel_deferred_sentence_live_vs_historical` for the check-(2) grep, and the family/split anchors).
