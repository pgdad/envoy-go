=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, the active phase is fully landed, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

**STATUS AS OF 2026-06-30: ALL CHARTERED WORK COMPLETE — TERMINATION SENTINEL ACTIVE.**
- Every chartered ROADMAP row is `done` (rows 1–48; row 48 `stats-sink-statsd` — the highest — flipped `done` at the phase-48 IMPL, the SOLE leg, ADR-0106 + `reference_roadmap_split_phase_row_done`).
- The active phase is fully landed: **phase 48 (`stats-sink-statsd`) IMPL** squashed (`ddd85596`), merged onto master + pushed (master == origin/master); six-gate GREEN (full 94-dir differential `ok ~285s` — `0092` cross-side delta-SUM-with-stability-barrier + the absolute-gauge subset; gofmt/vet/golangci-lint/build clean; full unit suite green; `-race` clean on `internal/statssink`; `0092` 3 deliberate breaks all LIVE + 20/20 flake-soak; `go mod tidy -diff` EMPTY). The two full-suite differential runs each flaked ONE unrelated fixture on a startup/readiness race (0025-http-adaptive-concurrency, then 0081-grpc-access-log), each 3/3 PASS in isolation — the documented `reference_differential_fullsuite_startup_flake`, NOT a regression. ANCHORS ADR-0265. Counts: stat 1200 (non-H2 1196) / fixtures 94 / fuzzers 51 / BackendKind 38 / DECISIONS ADR-0265 (next-free ADR-0266); ZERO new packages, ZERO new go.mod modules.
- STATE.md's recorded NEXT is **`NEXT → (none chartered)`**. The statsd UDP stats sink (ADR-0265) is envoy-go's SECOND `stats_sinks[]` consumer and FIRST UDP datagram seam, built additively over the phase-47 flush substrate.

=> The `stop` sentinel applies. There is NO next stage to perform autonomously.

The Observability FAMILY STAYS OPEN, but NO further row is CHARTERED: dog_statsd / graphite / OTLP-metrics sinks + the `tcp_cluster_name` statsd-over-TCP transport + the tap filter + tracing `custom_tags`/`spawn_upstream_span`/`http_service`/force-trace remain DEFERRED candidates (mentioned in the ROADMAP family-progress note, NOT `pending` rows with their own `Next →` brainstorm pointer). The autonomous router ADVANCES chartered stages; it does NOT self-charter new rows/families — that is a human decision.

## Two IMPL findings recorded at phase 48 (worth surfacing if the next Observability row touches the Docker differential harness or cluster gauges)
- **`differential.HostGatewayIP`** resolves the ACTUAL host-gateway literal IP via a throwaway `alpine:3` container (`getent hosts host.docker.internal` under the `host.docker.internal:host-gateway` ExtraHosts mapping) — NOT the bridge IPAM gateway. The bridge gateway (`172.17.0.1`) is VM-internal under Docker Desktop and unreachable from a reference container; the host-gateway (`192.168.65.2` on Desktop) is the reachable host address, and UDP container→host delivery to it was verified. Reuse this helper for any future row that needs a literal-IP host endpoint reachable from a reference container (the statsd sink rejects hostnames).
- **`cluster.<name>.membership_total`** is registered UNCONDITIONALLY for every cluster (== endpoint count); **`membership_healthy`** is registered ONLY on clusters WITH `health_checks` (`internal/cluster/manager.go`). A cross-side gauge assertion on a no-health-check cluster must use `membership_total`.

## To resume work (a human chartering the next phase)
To re-open the loop, charter the next stage, then re-roll this file to point at it:
1. Pick the next row to open (a deferred Observability candidate above, or a fresh feature family).
2. Add/flip its ROADMAP row to `pending` with a `Next → <row> BRAINSTORM` pointer, and set STATE.md's NEXT accordingly (lifecycle-state 0).
3. Replace this file's body with a cold-start router pointing at that BRAINSTORM (mirror the structure of any prior phase's `next-prompt.txt` in git history — e.g. the phase-48 SPEC/PLAN/IMPL routers).
4. Delete the `stop` file at the git root so the loop can advance again.
==============================================

## Read first (in order)
1. THIS FILE — the router (TERMINAL: all chartered work complete; `stop` sentinel active).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 48 (stats-sink-statsd) IMPL done`) + the recorded NEXT (`(none chartered)`) + the counts (1200 / 94 / 51 / 38 / ADR-0265).
3. `docs/envoy-go/ROADMAP.md` — all rows `done` (max row 48); the Observability family-progress note (statsd DONE at phase 48; family STAYS OPEN; deferred-row list).
4. `docs/envoy-go/DECISIONS.md` — ADR-0265 (the phase-48 statsd UDP stats-sink anchor; next-free ADR-0266).
5. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the `### Stats sinks — the statsd UDP sink` subsection (per phase 48 ADR-0265).
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_subagents_no_push`, `feedback_subagent_autocommit_claudemd`, `reference_roadmap_split_phase_row_done`, `reference_differential_fullsuite_startup_flake`, `reference_delta_sink_differential_stability_barrier`, `reference_periodic_sink_differential_two_receivers`, `reference_docker_probe_bridge_network`).
