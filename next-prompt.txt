=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **PHASE 88 (`h2-continuation-frames`) IS OPEN — ROW 88 IS `in-progress` AT `ROADMAP.md:150`.** `want` IS **120**. ⇒ **THIS SESSION PERFORMS THE PHASE-88 PLAN.** `STATE.md`'s `next-skill:` reads *the phase-88 PLAN* — that IS your instruction. ⚠️ **THE SENTINEL DOES NOT FIRE** — check (1) prints exactly `NOT DONE: row 88` and check (2) prints SIX. **DO NOT create `stop`.**

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=120 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **`want` IS 120 AT THIS TIP AND A PLAN DOES NOT MOVE IT.** `ROADMAP.md` must be **BYTE-UNTOUCHED** at your stage — verify by EMPTY DIFF against master, and run the sentinel ONE side only. ⚠️ **THE ONE EXCEPTION: if your PLAN mints `88.1`/`88.2`/`88.3` sub-phase ROWS under the revised three-leg D-88-SEQ, that moves `want` and is a sentinel-affecting edit** — the row-84 precedent is a split recorded in PROSE with a single parent row, and both the BRAINSTORM and the SPEC deliberately minted NO sub-phase rows. **If you mint them, MEASURE both sides; never forecast.**

⚠️ **THE DOCTORING NCs REMAIN MANDATORY EVERY SESSION.** Both:
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md. At want=120 expect BOTH
#   "NOT DONE: row 62" AND "NOT DONE: row 88".
```
```sh
# the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```

⚠️ **AND WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -oiE '-family row'` fails with `grep: amily row: No such file or directory` and the surrounding arithmetic prints `base=0 now=0 delta=0`, **which reads exactly like "no change".** Observed live at FOUR phase-84 stages. **Always pass `--` before the pattern**, and remember the counts are form-dependent: **95 occurrences vs 67 LINES** at this tip (`grep -c` counts LINES). **A gate that reads zero on BOTH sides is not evidence of invariance.** ⚠️ AND the carried **ARM-A {119, 131} figure binds ONLY with its escape-aware pipe-split command** (BRAINSTORM-86 §6) — ad-hoc field-count forms do NOT reproduce it (`NF != 8` flags 17 rows); do not "correct" the figure with a different form.

**SENTINEL RE-RUN MECHANICALLY AT THE PHASE-88 SPEC CLOSE (2026-08-14, base `0158192c`), ONE SIDE — a SPEC edits no ROADMAP, and the BINDING proof of that is the EMPTY `ROADMAP.md` DIFF. IT DOES NOT FIRE; `stop` was NOT created** (verified absent at repo root AND stage worktree). ACTUAL output, recorded not predicted: input **238 lines / 120 data rows** · (1) **`NOT DONE: row 88`** ALONE, denominator silent · (2) **SIX at `:198 :204 :210 :220 :226 :234`** · (3) **SILENT**.
- ⚠️ do NOT "fix" the six and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases; the candidates sentences are a WINDOW onto a larger deferred backlog, not an inventory of it.
- **ALL FOUR NCs FIRED:** row-62 doctoring ⇒ `NOT DONE: row 62` AND `NOT DONE: row 88` (with `NC LANDED? [ in-progress ]` inspected first); `want=119` ⇒ `GATE FAIL: examined 120 data rows, expected 119`; check-(3) doctoring (residual confirmed **2 -> 0** on the doctored copy first) ⇒ `NEVER OPENED: gRPC` while `WASM` correctly stayed silent; check-(2) one-arm counts **5** (long arm alone) / **1** (short arm alone), union **6** (a one-arm strip is NOT an NC for the union).
- Every leak axis INVARIANT: `-family row` **95 / 67** · `gRPC-family row` **2** · `Operational-tooling-family row` **3**.
- ⚠️ **Re-run all three checks and both doctoring NCs yourself and record what YOU see.**

**RE-VERIFIED INDEPENDENTLY AT THE PUSHED TIP `f6d7a60a` (2026-08-14), ONE SIDE, BY THE ROLLER — not copied from the SPEC's record.** Input **238 lines / 120 data rows**. (1) **`NOT DONE: row 88` ALONE**, denominator silent · (2) **SIX at `:198 :204 :210 :220 :226 :234`** · (3) **SILENT**. ⇒ **THE SENTINEL DOES NOT FIRE; `stop` WAS NOT CREATED** (verified absent at the git root). **ALL FOUR NCs FIRED:** row-62 doctoring ⇒ `NC LANDED? [ in-progress ]` inspected first, then `NOT DONE: row 62` AND `NOT DONE: row 88`; `want=119` ⇒ `GATE FAIL: examined 120 data rows, expected 119`; check-(3) doctoring ⇒ residual **2 -> 0** on the doctored copy, then `NEVER OPENED: gRPC` while `WASM` correctly stayed **silent**; check-(2) one-arm **5** (long) / **1** (short), union **6**. Every leak axis INVARIANT: `-family row` **95 / 67** · `gRPC-family row` **2** · `Operational-tooling-family row` **3**. ⚠️ **AND THE ROLLER'S OWN FIRST PROBE WAS WRONG AND IS RECORDED AS SUCH:** an ad-hoc `awk -F'|'` reading `$4` instead of `$5` printed **every row 00-119 as NOT DONE** — a form that is not the sentinel and is not evidence. **The pinned field-parsed command above is the ONLY form; do not re-derive it by eye.** A second probe read fixtures at `test/differential/fixtures/` and returned **0** — the real path is **`test/fixtures/`** (121, tail `0119-grpc-unary-trailers`). ⚠️ **A ZERO FROM A WRONG PATH READS EXACTLY LIKE A ZERO FROM AN EMPTY SET.**

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (ugrep under the hood). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Reproduced at TWENTY-FOUR separate tips.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **THE METHOD BINDS; carry no standing discrepancy figure and no standing absolute.** ⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS; print FILE NAMES, never a count.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files; use `-v --no-index`, and `git status` is the discriminating check). ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **`grep -c` ON ZERO MATCHES IS TOOL-SPECIFIC** (only `git grep -c` prints nothing; all exit 1 — capture the value, never chain on the exit code). ⚠️ **`grep -c` COUNTS *LINES*, NOT *OCCURRENCES*.** ⚠️ **`\t` IN GNU ERE IS A LITERAL `t`** — use `-P`. ⚠️ **A `git grep` PATHSPEC ENDING IN `/` READS A FAIL-UNSAFE ZERO** (shape 33) — use the shell-glob form. ⚠️ **AN UNANCHORED PANIC-GREP FALSE-FIRES 14 TIMES ON A FULLY-GREEN DIFFERENTIAL LOG** — **the canonical gate is ANCHORED: `^panic:|DATA RACE|SIGSEGV`. A gate that fires on a green log is worse than no gate.**

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`** with FOUR blind spots (an em-dash, a DOT in `http-1.1`, two LETTER-suffixed ids). ⚠️ **EXECUTED on scratch copies at the phase-77 PLAN: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** ⚠️ **DO NOT restore the regex form.**

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

⚠️ **Run the union form, never one half of it.** ⚠️ **A ONE-ARM STRIP IS NOT AN NC** (6 -> 5 short / 6 -> 1 long; never 6 -> 0). ⚠️ **The documented trap's stated MECHANISM is refuted while the trap is real** — the inflation comes from ROADMAP table rows carrying BRAINSTORM prose. **Cite the command; do not re-inherit the explanation.** ⚠️ **The line numbers drift with edits — re-derive them; the current six are `:198 :204 :210 :220 :226 :234`.** ⚠️ **These six paragraphs are the FINAL and now the ONLY sentinel blocker, and any edit near them is a sentinel-affecting edit.** All six are standalone family PROSE paragraphs (not rows), naming ~42 distinct candidates; every "retire the paragraphs" shape is doctrinally foreclosed (`reference_sentinel_matcher_string_self_clears`).

⚠️ **LOAD-BEARING FOR THIS ROW: ROW 88'S PROVENANCE IS *INSIDE* A WINDOW.** The gRPC-family paragraph at `:204` names *"the CONTINUATION-discard defect"* among its deferred candidates. The row is filed as **core-HCM maintenance claiming NO family ordinal** because the defect is protocol-generic (BRAINSTORM §5 records the counter-argument openly). **The `:204` sentence narrows at ROW-DONE** — one candidate leaving a list of ~11. The phrase survives a one-item removal so check (2) *should* stay SIX, ⚠️ **but that is a PREDICTION and MUST BE MEASURED on both sides at the IMPL, never forecast.**


==============================================


## STATUS AS OF 2026-08-14: PHASE 88 (`h2-continuation-frames`) **OPEN** — row 88 `in-progress` at `ROADMAP.md:150`, `want` **120**, base `0158192c`. **THIS SESSION PERFORMS THE PHASE-88 PLAN.**

**What the 88 SPEC landed (2026-08-14, base `0158192c`, branch `phase-88-spec`, ONE squashed commit).** Docs-only: ZERO production `.go`, ZERO test `.go`. `SPEC.md` + the `PROGRESS.md` SPEC entry + **ADR-0310 §Context STATUS `PROPOSED`** (strict guard **0 -> 1** — the live pointer YOUR IMPL disarms, not this stage and not the PLAN) + the STATE roll. `ROADMAP.md` and `BEHAVIOR_CONTRACT.md` **BYTE-UNTOUCHED**.

### ⚠️ THE HEADLINE: THE BRAINSTORM'S TWO-LEG STRUCTURE IS REFUTED — THERE ARE **THREE** LEGS, AND THE READ FIX ALONE IS A **REGRESSION**

**`WriteContinuation` appears NOWHERE in `internal/`.** Both header-write sites (`ServerConn.encodeAndWriteHeaders`, and the client's request-write in `client.go` — cite BY SYMBOL) emit the ENTIRE encoded block as one `WriteHeaders(... EndHeaders: true)`, and **x/net's `WriteHeaders` does NOT split** (zero `Continuation` references in its body; it only rejects at the 16 MiB wire ceiling). So any outgoing block larger than the PEER's advertised `SETTINGS_MAX_FRAME_SIZE` is an **illegal oversized frame**.

**It is MASKED at the tip and UNMASKED by the read fix.** With the read side fixed and the write side not: the padded-upstream-response arm turns `200`-with-silent-loss into a hard **`http2: frame too large`**. Raising the probe client's `MaxReadFrameSize` to 1 MiB turns it back into a full-fidelity 200 — **the proof envoy-go emitted ONE oversized HEADERS frame.** The request direction passed only because x/net's **Server** defaults to `defaultMaxReadFrameSize = 1 << 20` while its **Transport** uses the 16 KiB spec default. **The write gap is DIRECTION-INDEPENDENT; do not read the request direction's green as "the client write path is fine".**

**REVISED D-88-SEQ:** **88.1** server READ (`conn.go` accumulator; anchors B/C/D/G) · **88.2** client READ (`client.go` accumulator; anchors I/K) · **88.3** WRITE both directions (`framer.go` `writeHeaderBlock` + two call sites; anchor I at default settings). ⚠️ **HARD ORDERING CONSTRAINT, MEASURED: 88.3 MUST land WITH OR BEFORE 88.2** — 88.2 alone is a **strictly worse user-visible outcome than the defect**. 88.1 alone stays safe and shippable. **Treat 88.2+88.3 as ONE atomic leg.**

### ⚠️ D-88-PRIM DECIDED BY TWO COMPILING PROTOTYPES — AND THE REJECTED OPTION FAILS *SILENTLY*

**CHOSEN: the per-connection accumulator** (stream id + fragment buffer + the originating HEADERS flags on both `ServerConn` and `ClientConn`; the old `onHeaders` body extracted to `onHeaderBlockComplete`, reached only once `END_HEADERS` is seen).

**REFUTED: `Framer.ReadMetaHeaders`** — four grounds, three MEASURED. (1) ⚠️ **It compiles cleanly with ZERO diagnostics and then HANGS:** `ReadFrame` starts returning `*http2.MetaHeadersFrame`, which matches **no arm of either `dispatchFrame`** and falls through to the *"unknown frame types are silently ignored"* default ⇒ every HEADERS frame silently discarded; a scoped `-run TestServerConn` was `Terminated` at a 45 s timeout. (2) `readMetaFrame` **MUTATES the decoder it is handed** — `SetEmitFunc` / `SetEmitEnabled` / `SetMaxStringLength` — and `hpackState.decodeBlock` is built entirely on that emit callback. (3) It **pre-empts envoy-go's own pseudo-header validation** (`buildRequest`) with differently-worded x/net `StreamError`s, in a family **h2spec §8.1.2.1 covers 4/4** and **row 87 landed rejects into**. (4) Blast radius **6 non-test `HeadersFrame` sites + 37 test-side references** vs the accumulator's 3 production files. **And decisively: it does NOTHING for the write leg.**

### ⚠️ COST — MEASURED, AND STILL A FLOOR

`client.go` **136/91** · `conn.go` **81/14** · `framer.go` **38/0** ⇒ **net +150 production `.go` over THREE files**; IMPL band **~+150-190**. ⚠️ The `client.go` figure is inflated on BOTH sides by **pure code motion** (the old `case *http2.HeadersFrame` body lifted verbatim). ⚠️ **Quoted as a FLOOR** per `reference_measured_prototype_is_a_lower_bound`: **three decode sites, two write sites, per-connection state on two structs** — nothing about row 87's single-call-site escape transfers. Existing `internal/filter/hcm/...` suites GREEN on the prototype; **`go.mod` +0 and stat-surface DELTA 0 both MEASURED on the prototype** (`NewCounter(|NewGauge(` reads **406** in BOTH trees).

### ⚠️ THREE FINDINGS THE PLAN MUST NOT RE-LEARN

1. ⚠️ **h2spec's §6.10 CONTINUATION SECTION PASSES 6/6 AT THE TIP THAT DISCARDS EVERY CONTINUATION FRAME** — including *"Sends multiple CONTINUATION frames preceded by a HEADERS frame"* and *"…preceded by a CONTINUATION frame with END_HEADERS flag"*; §6.2 HEADERS 4/4 and §8.1.2.1 4/4 on both sides; the suite reads `95 tests, 94 passed, 1 skipped, 0 failed` **IDENTICALLY at tip and prototype**. Those tests assert the connection's **REACTION**, never that the header fields arrived. **A conformance section that NAMES the feature is fully green while the feature is entirely absent. h2spec is NOT a red anchor and NOT a regression gate for this row.**
2. ⚠️ **A PROBE THAT COULD NOT ANSWER ITS OWN QUESTION.** The client-leg desync was first probed with a single 20 KB header — which **exceeds the 4096-byte HPACK dynamic table and is therefore NEVER INDEXED**, so dropping it cannot desync anything. Re-probed with **200 small (100 B) INDEXABLE headers**: the tip delivers **165 of 200** on two successive requests with **no connection-fatal error**, and three distinct upstream source ports show **envoy-go opens a FRESH upstream connection per request in this configuration**. ⇒ the client leg is **silent-loss-only, masked by CONNECTION LIFECYCLE rather than code safety**. The code path is identical to the server's; **any future upstream H2 connection reuse would expose it.** Recorded as a bounded measurement, **NOT** as "the client leg is safe".
3. ⚠️ **A STALE SERVER MADE THE PROTOTYPE READ IDENTICAL TO THE TIP.** The first prototype battery reproduced every tip failure exactly — which reads precisely as *"the fix does nothing"*. The prototype binary **had never started**: `bind: address already in use` on the admin port, because a `pgrep -f 'a\|b'` **alternation matched nothing** and the tip binary still held the port. Discriminated by the server log **plus a `strings` symbol assertion** (the fix's error string: **2** occurrences in the prototype binary, **0** in the tip). ⚠️ **ASSERT THE BOUND BINARY WITH `ss -ltnp` BEFORE EVERY READING.** Sibling instance the same session: a probe edit that failed `go build` left the OLD binary in place and produced a stale, plausible reading — caught only by asserting the new symbol in the binary.

### ⚠️ Q6 — THE CONTRACT HAS **NO** SENTENCE TO CORRECT, AND THAT IS THE FINDING

`BEHAVIOR_CONTRACT.md`'s `## HTTP/2` section (`:2019`, 63 lines to `## HTTP filter chain` at `:2081`) makes **NO claim** about CONTINUATION, header-block framing, `END_HEADERS` or max frame size — verified by a grep scoped to exactly that range. **Every whole-file `CONTINUATION` hit (`:782`, `:798`, `:820`, `:823`, `:825`, `:5109`) is the unrelated TRACING sense** ("continuation requests" continuing an inbound trace), and `:5281` is an async-resume "continuation site". **Do not conflate them; do not edit them.** ⇒ **the IMPL ADDS a new bullet rather than riding a correction** (unlike row 87). ⚠️ **Cite by STRING or SYMBOL** — row 87's `:2036` insertion shifted every by-line citation at or below it by +1. The one landed FALSE artefact this row kills is the `dispatchFrame` comment, whose *"handled by framer as part of `ReadMetaHeaders`"* clause describes a configuration that would **hang the server**.

### ⚠️ WHAT THIS SESSION OWES — THE PHASE-88 PLAN

1. **Task decomposition under the REVISED three-leg D-88-SEQ**, honouring **88.3-with-or-before-88.2**, with each leg's RED anchor **re-proven by execution at YOUR tip** with failure lines read.
2. ⚠️ **RESOLVE THE FIXTURE BLOCKER — THE LARGEST UN-ENUMERATED COST.** SPEC §5 confirmed by execution that the differential arm needs **NO raw framer**: the repo's OWN `test/helpers.H2CRoundTrip` (which already takes `headers []hpack.HeaderField`) reproduces the loss — tip drops BOTH headers at 20 000 B, prototype keeps them. **BUT** `0004-h2-routing`'s backend returns fixed bodies and **does NOT echo request headers**, and `HTTPHeaderMutation` (`BackendKind = 9`) is **HTTP/1.1** so it cannot sit behind an H2 downstream (envoy-go mirrors the downstream protocol upstream — an H2 downstream against an H1 backend yields the H2 preface and a 502). Options in cost order: extend `0004`'s own backend with a header-reflecting path · host the arms in a fixture whose H2 backend already reflects · mint `0120` (next-free, verified). ⚠️ **The response-direction arm (88.2/88.3) additionally needs a backend that EMITS a large header block.** **MEASURE the test-side line count; do not estimate it.**
3. **Freeze the two open decisions:** the over-size **error code** (the prototype uses `FRAME_SIZE_ERROR`; `ENHANCE_YOUR_CALM` is arguably more idiomatic for a flood — **neither is measured against the reference yet**, so probe it or rule in the ADR) and whether a **stat** is owed (⚠️ a new stat name breaks the anticipated stat-surface +0; **default: none**).
4. **Rule on the arm-E `LastStreamID` 1 -> 0 delta.** Post-fix the HEADERS is buffered rather than dispatched, so `lastInID` never advances before x/net's frame-order error tears the connection down. Direction and code are unchanged (`PROTOCOL_ERROR` both sides). **Pin it or record it deliberately unpinned.**
5. **The unit-test roster:** `writeHeaderBlock` (block exactly at / one under / one over `maxFrameSize`; a zero `MaxFrameSize` defaulting to 16384) and the accumulator. ⚠️ **Its three guard arms — nil accumulator, wrong stream, over-bound — are UNREACHABLE behind x/net's `Framer.checkFrameOrder` and MUST be labelled defence-in-depth, NOT counted as coverage** (`reference_vacuous_break_modes`).
6. **A per-task budget on top of the measured `~+150-190` production floor**, with the remaining un-enumerated classes NAMED.
7. The sentinel + all four NCs, **ONE side** (a PLAN edits no ROADMAP — prove by **EMPTY DIFF**) · `STATE.md` rolled IN PLACE (lifecycle-state **2 -> 3**) with the oldest §Recent entry evicted by DIRECT DATE READ · `next-prompt.txt` rolled to the IMPL (`git add -f`) · ONE squashed commit, merged and pushed.

### Candidates ledger (re-derive EVERY cost at your tip — a carried cost is stale by construction)

Rejected at the BRAINSTORM, unchanged at the SPEC: **`ssl.connection_error`** (+444 whole-`.go` floor; still absent as a landed stat name) · **`test/conformance/grpc/`** (`test/conformance/` holds ONLY `h2spec/` and `proxy-wasm/`; 9/26 reachable) · **the stat-surface recount** (contested **1205 vs 1207**; the OCCURRENCE form reads `NewCounter(` **327** / `NewGauge(` **79** = **406** — a DIFFERENT form from the lineage's carried `145/21`; **no absolute corrected, none carried**) · **REVIEW.md restoration** (**37** of **129** phase dirs; process-not-product) · **the D-86-CONN `client.Close` gate** (~10 lines — a fold-in) · **hygiene fold-ins** · the **~42 candidates in the six family windows**.

### ⚠️ COUNTS AT THIS TIP — RE-DERIVE MECHANICALLY, NEVER COPY

- **`ROADMAP.md` 238 lines / 120 data rows**; row 88 `in-progress` at `:150`, all others `done`; check-(2) anchors `:198 :204 :210 :220 :226 :234`; `-family row` **95 occurrences / 67 LINES**; `gRPC-family row` **2**; `Operational-tooling-family row` **3**; ARM-A flags **{119, 131}** (escape-aware command ONLY).
- **`DECISIONS.md` 18166** lines · **309** headings · tail **ADR-0310 (§Context ONLY, STATUS `PROPOSED`)** · next-free **ADR-0311** (TAIL-derived; `grep -c '^## ADR-0311'` => **0**; headings+1 COLLIDES at the ADR-0209 gap) · **strict `^> \*\*STATUS: PROPOSED` guard 1 — ARMED; the phase-88 IMPL disarms it, NOT the PLAN** · STATUS census **23** · `^---$` **216**. ⚠️ **A NEW ADR TAKES NO `---` SEPARATOR** — verified at the SPEC by reading ADR-0309's own predecessor line; one was added, measured at 217, and REMOVED. ⚠️ **CARRY NO WHOLE-FILE COUNT OF THE LOOSE `PROPOSED` MATCHER.**
- **`BEHAVIOR_CONTRACT.md` 5958** — `## HTTP/2` at `:2019`, section ends at `## HTTP filter chain` `:2081`. ⚠️ Row 87's `:2036` insertion shifted EVERY by-line citation at or below it by +1 (ADR-0309 §Consequences (ix), deliberately NOT repaired) — **cite by STRING or SYMBOL.**
- **`STATE.md` 64** · **`STATE_HISTORY.md` 494** · **`BOOTSTRAP_PROMPT.md` 522** (repo ROOT) · phase dirs **129** · `REVIEW.md` **37** (standing departure, named not claimed).
- **differential fixtures 121** — they live at **`test/fixtures/`** (⚠️ **NOT `test/differential/fixtures/`, a plausible wrong path that returns a SILENT `0`** — the roller hit it live at this tip); numeric tail `0119-grpc-unary-trailers`, **next-free `0120`**. ⚠️ **Delta NOT yet +0 — extend-vs-mint is YOUR decision** (owed item 2). · **fuzzers 55 / 48 files** — anticipated **+0**, stated EXPLICITLY at the SPEC rather than inherited (the accumulator consumes no new config field and its inputs ride the existing `internal/filter/hcm/h2` fuzz surface); **you may still add one — say so and move the count.** · **BackendKind tail 38** (`H2GoawayResponder`, `fixture.go:614`) over 39 declarations (do NOT "fix").
- **h2spec `95 tests, 94 passed, 1 skipped, 0 failed`** — measured at BOTH tip and prototype this stage; **cite only from YOUR own run**, and see finding 1: it is **NOT** a red anchor and **NOT** a regression gate here.
- ⚠️ **STILL CONTESTED, NO NUMBER CARRIED:** the `STATE_HISTORY.md` archive-gap total · the loose `PROPOSED` matcher's whole-file count · the stat-surface absolute (1205 vs 1207) · the zero-test `generic/*` suite count (13 vs 14).

### ⚠️ DOCUMENTARY DEFECTS — carried forward, still deliberately NOT fixed

**`ADR-0051` §2's false gate-scope sentence** corrected on the record by ADR-0307 · **`ADR-0058`** cites `routerActionH2.doH2` at a dead location · **`ROADMAP.md` rows at lines 119/131 malformed** (ARM-A GUARD) · **`STATE.md` §Project counts FROZEN at phase 76** · **`harness_test.go:208` port inventory STALE** · **`body.go` nolint INERT** · **xDS cycle guard NOT AUTOMATED** · **`wasm/doc.go:219` two errors** · **ROADMAP cites `esalaine` FIVE times** · **`rbac.go:50` token `F2`** · **root `PROGRESS.md`** stray phase-32.1 doc · **SPEC-86/PLAN-86 prose cites the grpcclient Close pins at a nonexistent `internal/xds/xdsgrpc/...` path** · **`BEHAVIOR_CONTRACT.md:2034`'s "no stdlib net/http parsing" sentence is FALSE for the 05.1 -> 87 window** (RETAINED, ridden by ADR-0309) · **`ADR-0057` says fixture `0004`'s driver issues "27 sequential H/2 round-trips per side" — it now issues 29** · **`BEHAVIOR_CONTRACT.md`'s TWO riders cite the ADR-0052 mandate at a DRIFTED `:1821`** · **the gRPC-family window cites the CONTINUATION arm at `conn.go:255-259` — it is `:264-268` at this tip** · ⚠️ **the `dispatchFrame` comment is a LANDED FALSE COMMENT whose `ReadMetaHeaders` clause the SPEC proved would HANG the server — the IMPL is where it dies.**

### ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + BOTH DOCTORING NCs YOURSELF** and record ACTUAL output. ONE side; the binding proof is the **EMPTY `ROADMAP.md` DIFF**.
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** The 88 SPEC refuted its own BRAINSTORM's leg count, its cost estimate, its Q6 premise, and one of its own probes. Do the same.
3. **LINE ANCHORS DRIFT; SYMBOL ANCHORS DO NOT — RE-VERIFY EVERY CITE AT YOUR TIP.**
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Still live: the **SDS dial-budget class** (`TestSDSEndToEnd_FetchFailure_BootFailsClosed/silent_SDS_server…`, `internal/boot/boot_sds_e2e_test.go:551`, gRPC `DeadlineExceeded`, THREE firings) · the **driver-owned receiver port race** with TWO named members, `0081-grpc-access-log` (`0.0.0.0:35097`) and `0087-tracing-otlp` (`0.0.0.0:35323`) — **it ABORTS the binary mid-suite and the harness wrapper still exits 0** · `internal/cluster` `-race` outlier · `internal/httpclient` zero-value · the two 84.2-era flakes · `TestServerConn_TinyWindowDelivery` · the REFERENCE h2spec section-8 flip. **Capture `-v` output BEFORE re-running anything.**
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only). FOUR pre-existing containers from other work are present and must stay untouched.
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`.
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL** (`strings <binary> | grep <new string>`), **and ASSERT THE BOUND BINARY with `ss -ltnp` before reading any probe result.** Both fired live at the 88 SPEC. **A harness's exit code is not the command's**; **`gofmt -l` never exits non-zero — gate on OUTPUT.**
8. ⚠️ **`pgrep -f` / `pkill -f` MATCHES YOUR OWN SHELL AND KILLS THE TOOL CALL (exit 144)**, and ⚠️ **a `pgrep -f 'a\|b'` ALTERNATION MATCHES NOTHING SILENTLY** — kill by PID from `pgrep -laf` with the `bash -c` line filtered out, ONE pattern at a time, and verify ports released with `ss -ltn`.
9. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-88-*`, `wt-88-proto-*` or stage worktree should outlive its stage close** — expect only `master` plus your own (verify with `git worktree list`).
10. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** (a)/(b) differential 121 fixtures, full suite with `INNER_EXIT`; (c) h2spec cite only from your own run; grpc-conformance deferred in writing; proxy-wasm 10/16; (d) fuzzers 55/48; (e) `INNER_EXIT` on every differential launch AND `go test ./...`; (f) no REVIEW.md — standing departure.

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 88 (h2-continuation-frames)' --format='%h %s'      # 3 at this tip: BRAINSTORM, its follow-up, SPEC
git log --grep '^phase 87 (h2-double-slash-path-routing)' --format='%h %s' # 4: a CLOSED phase, use as a FIRING CONTROL
```
⚠️ **The count comments are LIVE FIGURES — VERIFY before relying.** Take the NEWEST line (`| head -1`).

Execution style: **subagent-driven per `feedback_execution_style` is the STANDING PREFERENCE**, and the 87 IMPL is the precedent it should be measured against (four task agents + an adversarial whole-branch reviewer, which found three real coverage gaps the controller had not). **A PLAN whose probe set is cheap and serial may take the NAMED inline departure — the 87 BRAINSTORM/SPEC/PLAN, the 88 BRAINSTORM and the 88 SPEC all did (FIVE consecutive stages). NAME the choice either way, and note the IMPL is where the parallel work actually is.** Subagents commit LOCALLY on the ONE stage branch with EXPLICIT PATHSPECS; the controller squashes (`feedback_subagents_no_push`, `feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** and tear down docker **BY NAME**. ⚠️ **Band private ports outside BOTH `20000-31007` AND `11000-14999`, clear of the static fixture ports (10000-19172); the driver-owned receiver race has been observed at `35097` and `35323`, and `0081` also binds `0.0.0.0:42039`.** The 87 stages used 47410-47441, the 88 BRAINSTORM 47450-47470 and the 88 SPEC **47480-47485**; band yours from **47490+**. ⚠️ **ONE WORKTREE PER AGENT preferred; explicit-path staging if shared; never an unscoped `git restore`.** Per-task `gofmt` + `golangci-lint` on touched packages (⚠️ misspell locale US). ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch.** ⚠️ **Docker BY NAME, `--rm`, verify gone.**

⚠️ **CONTAINER→HOST REACHABILITY — READ THE CORRECTION, NOT THE HEADLINE.** The 88 BRAINSTORM measured `172.17.0.1:47455` UNREACHABLE from inside its reference container and `--network host` yielding a listener absent from the host's `ss -ltn`. ⚠️ **THE CAUSE WAS THE PROBE: `172.17.0.1` is the BRIDGE IPAM GATEWAY, exactly the address `reference_host_gateway_ip_docker_desktop` records as NOT working.** Use `--add-host=host.docker.internal:host-gateway` and dial the HOSTNAME, or resolve the literal via a throwaway `getent hosts host.docker.internal` container (the `differential.HostGatewayIP` / `0092`-driver-local pattern); and put **`dns_lookup_family: V4_ONLY`** on any reference cluster dialing `host.docker.internal` (the embedded DNS also returns a non-forwarding AAAA that `AUTO` prefers — a SILENT connect-fail that reads as "the backend emits nothing"). **A host-bound backend IS reachable; verify delivery ran (receiver count > 0) before concluding anything.** ⚠️ **AND envoy-go REJECTS `match.headers` outright** — `route 0: match.headers is not supported in phase 04` — so a header-match route works on the REFERENCE side only.

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root — **observed repeatedly at the 88 BRAINSTORM and SPEC**. Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run. ⚠️ **Shell VARIABLES do not survive between Bash calls** — re-export `$W`/`$SC` in every command (a missing `$SC` silently turned an append-only `cmp` check into a bogus `NOT A PREFIX` at the 88 SPEC).

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — `git status --porcelain` AND `git diff --stat master` must show ONLY your intended edits. For a patched TRACKED file: `sha256sum` BEFORE patching, `git checkout -- <path>` after, `sha256sum -c` the capture. **The 88 SPEC built `probe88/` and `q4probe/` inside a DETACHED prototype worktree, removed both BEFORE the final cost measurement, and deleted both prototype worktrees whole.**

⚠️ **THE ARCHIVE-ABSENCE GUARD — USE THE WHOLE-FILE FIXED-STRING FORM** (`grep -cF`), NC'd with REAL targets re-derived FROM THE FILE first; a control's own count is a LIVE FIGURE. ⚠️ **CORRECTED BY EXECUTION AT `f6d7a60a` (2026-08-14) — THE PREDECESSOR'S FIGURES WERE ATTRIBUTED TO THE WRONG TIP, AND THE "DRIFT" HAS A MECHANISM.** The carried pair **14 / 12** was measured at the SPEC's **BASE `0158192c`** (492 lines), *not* at its close. At the close tip **`f6d7a60a`** (494 lines) the same two fixed strings read **15 / 13** — measured on BOTH sides with `git show <sha>:docs/envoy-go/STATE_HISTORY.md`. ⚠️ **THE +1-PER-STAGE-CLOSE IS SELF-INCLUSION, NOT DRIFT:** every archived `- **prior active-phase (evicted at the phase-NN … close…):**` bullet re-cites the phases named in the evicted entry, so each stage's OWN append adds exactly one hit to each control (tip line **494** is `…evicted at the phase-88 SPEC close…` and is itself the 15th phase-82 hit). ⇒ **AT YOUR CLOSE EXPECT 16 / 14, and treat a reading that did NOT move as the anomaly.** This is why a control is re-derived at the tip you actually stand on and never copied — **and why a carried figure MUST name its tip.** ⚠️ **The §Recent-style bullet anchor `- **prior active-phase:**` reads 0 on ARCHIVED entries** (the archive wraps them `- **prior active-phase (evicted…):**`) — use the fixed-string form. **Verify STRICTLY APPEND-ONLY**: `git diff --numstat` = `N 0` AND base a byte-exact PREFIX (`cmp`). At your close §Recent evicts the oldest per a DIRECT DATE READ at that tip. ✅ **CONFIRMED BY DIRECT READ AT `f6d7a60a`:** §Recent is capped at **5** bullets (`STATE.md:46 48 50 52 54`) and the oldest is **`phase 87 (h2-double-slash-path-routing) BRAINSTORM done` (2026-08-12) at `:54`** — **UNIQUELY oldest**, every other bullet dated 2026-08-13. **That is the one your roll evicts** — but **RE-READ THE DATES AT YOUR OWN TIP** rather than trusting this line number, since your own append shifts them.

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads the phase-88 PLAN — that IS your instruction.** §Project counts SELF-CONTRADICT §Current — do not anchor on them (frozen at phase 76).
2. `docs/envoy-go/phases/88-h2-continuation-frames/SPEC.md` — **the whole file**: §1 the two prototypes and the measured cost, §2 the arm table, **§3 the THIRD LEG and the ordering constraint**, §4 the bound and its two open decisions, §5 the differential shape and the fixture blocker, §6 the Q3 disposal, §7 the h2spec blindness finding, §8 the contract's silence, §10 **what YOU owe**.
3. `docs/envoy-go/phases/88-h2-continuation-frames/BRAINSTORM.md` §2 — the original reproduction with every arm and its control (the SPEC's arms are the same battery re-run against the prototypes).
4. `internal/filter/hcm/h2/conn.go` (`dispatchFrame`, `onHeaders`, `encodeAndWriteHeaders`), `client.go` (the `HeadersFrame` arm + the request-write site), `hpack.go` (`decodeBlock`), `framer.go` (`newFramer` — **`ReadMetaHeaders` is never assigned**; `translateFramerErr` is what makes x/net's ordering enforcement look like envoy-go's).
5. `docs/envoy-go/DECISIONS.md` **ADR-0310** (tail, §Context only, `PROPOSED`) — the four ¶s your IMPL completes; and ADR-0309 §Consequences (iii)/(ix) on the cost-floor streak and the stale-cite classes row 87 created.

## What the PLAN owes
The seven items in **"WHAT THIS SESSION OWES"** above · `ROADMAP.md` **BYTE-UNTOUCHED** (empty diff), `want` staying **120**, row 88 staying `in-progress` · the strict `PROPOSED` guard staying **1** (a PLAN adds no ADR; the IMPL disarms it) · the sentinel + all four NCs, ONE side, recorded · `STATE.md` rolled IN PLACE (lifecycle-state **2 -> 3**) with the oldest §Recent entry evicted by DIRECT DATE READ · `next-prompt.txt` rolled to the IMPL (`git add -f`) · ONE squashed commit, merged to master, pushed.
