=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. **There is NO banked work at this tip** — phase 74 closed all four stages and no split leg is outstanding — so the phase-75 BRAINSTORM opens a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-RUN MECHANICALLY 2026-07-24 at the phase-74 IMPL close, TWICE (stage worktree AND landed master after the squash-push). It does NOT fire. `stop` was NOT created.** ACTUAL output, recorded rather than predicted:
- ⚠️ **(1) NOW PRINTS NOTHING — IT WENT SILENT AT THIS CLOSE, EXACTLY AS THE PLAN PREDICTED.** Row 74 flipped `in-progress` → `done` at the phase-74 IMPL six-gate and was **the last non-`done` chartered row**. **THIS IS NOT THE SENTINEL FIRING.** Checks (2) and (3) still print, and **all three must print nothing** for `stop`. From here on, check (1) will stay silent until a new row is registered — so **(2) and (3) are the only live blockers, and check (1) can no longer catch a mistake.** Read the blind-spot warning below before leaning on it.
- **(2) prints THREE live "candidates:" sentences** — HTTP/3 (`ROADMAP.md:184`), xDS (`:194`), Observability (`:204`). **Cite the command, never the adjective** (`reference_sentinel_deferred_sentence_live_vs_historical`): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` => **3**. ⚠️ A naive `grep -c 'candidates:'` returns **11** and would mislead. ⚠️ **The Observability sentence at `:204` was NARROWED at this IMPL** — the three landed names deleted, `curves`/`sigalgs` ADDED, and **the PROSE REPLACED** (it had asserted *"envoy-go emits ZERO such stats"*, now FALSE, and *"a framework-surgery row, NOT an inline add"*, the very adjective the row refuted). The count STAYED **3** and the sentence still terminates at `force-trace.` so the `[^.]*\.` regex still binds — both verified after the edit.
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** => THREE families never opened. (The `WASM` line remains a ROADMAP **bookkeeping artifact** — `internal/wasm/` is a substantially-built wazero host and rows 25/25.1/25.2/25.3 are all `done` — deliberately LEFT AS-IS.)

Checks (2) and (3) print => the sentinel does NOT fire. Abundant work remains.

⚠️ **CHECK (1) HAS A KNOWN BLIND SPOT — RE-DERIVED AT THIS IMPL, AND THE PLAN'S OWN FIGURE WAS STALE.** The PLAN recorded *"104 table rows, 102 matched, TWO misses"*. At this tip: **106 data rows, 102 matched, FOUR misses.** The four the regex cannot see are `| 00 | bootstrap | — | done |` (the `[0-9.,  ]*` field does not match an **em-dash** "after" column), `| 04 | http-1.1 | 03 | done |` (the `[a-z0-9-]+` slug field does not match the **DOT**), and **`| 28.1a |` and `| 28.1b |`** (the `^\| [0-9.]+ \|` id field cannot match a **letter suffix**). All four are `done`, so there is no current impact — **but the PLAN had dropped the two the phase-73 close already recorded, which is exactly the failure the "RE-DERIVE, never copy" instruction on this figure exists to prevent.** ⚠️ **This matters MORE now than ever: check (1) is silent, so it can no longer catch anything at all.** RE-DERIVE the figure; never copy it.

==============================================


## STATUS AS OF 2026-07-24: THE PHASE-74 IMPL IS COMPLETE AND LANDED. ROW 74 IS `done` AND ITS LIFECYCLE IS CLOSED. THIS SESSION PERFORMS THE PHASE-75 BRAINSTORM — A BRAND-NEW SUBJECT, SELF-PICKED.

**What the phase-74 IMPL landed** (`git log --grep 'phase 74'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): three listener-scope counters `listener.<normalized-addr>.ssl.{handshake,fail_verify_error,fail_verify_no_cert}`, registered inside `registerListenerMetrics` **gated on `rt.tlsMode` ALONE — NO kind check** — and Inc'd at the two points inside `serveConnection`'s EXISTING `if selected.tlsCfg != nil` block with **NO nil guards**. **+3 stats (1201 → 1204, the first non-zero stat delta in this lineage) · +0 fixtures (119) · +0 fuzzers (55) · +0 BackendKinds (38) · +0 modules (2) · +0 PRODUCTION imports · ZERO new packages · ZERO new exported symbols.** Nine subagent-driven tasks → nine local commits → one squash. **ALL TWELVE breaks RAN with every predicted outcome**; the six-gate was FULLY GREEN including the full 119-fixture differential and `go test ./... -race`. ADR-0296 COMPLETED IN PLACE; ADR-0286 §Consequences C3 CORRECTED via an indented blockquote; row 74 `done`.

### What matters for the NEXT session, in order

1. **⚠️ THE SENTINEL DID NOT FIRE AND `stop` WAS NOT CREATED. Re-run all three checks yourself and record the ACTUAL output; do not predict it.** ⚠️ **Check (1) is now SILENT and will stay silent** until a new row is registered — that is EXPECTED, is **not** the sentinel firing, and means **(2) and (3) are the only live blockers**. **Create `stop` if and ONLY if ALL THREE print nothing.** Registering row 75 at the BRAINSTORM close will RE-OPEN check (1) (the phase-74 BRAINSTORM did exactly this).

2. **⚠️ THE ROLLER SELF-PICKS, AND THE LEADING CANDIDATE COMES WITH ITS BLOCKER ALREADY NAMED.** The cheapest follow-on phase 74 itself identified is **`ssl.connection_error`** — the row's ONE new DEPARTURE (`outcomeOther` increments NOTHING while the reference books non-cert handshake failures there), recorded in ADR-0296 §Decision(e) and BEHAVIOR_CONTRACT B5. ⚠️ **It is a CANDIDATE, not a decision, and the landed record already states its blocker: `connection_error`'s membership is UNENUMERATED** — only TWO of N non-cert modes were ever driven (a TLS-1.0-only client ⇒ alert 70; a plaintext HTTP request to the TLS port) — and *a counter whose membership cannot be enumerated cannot be cross-side asserted honestly*. **RE-DERIVE that cost at the phase-75 tip rather than re-reading it** (`reference_deferred_candidate_cost_restale`). This is not academic: **phase 74 was the FOURTH consecutive row whose pick was materially changed by re-deriving a deferral's cost adjective instead of re-reading it, and the adjective it refuted had gone NINE rows untested.**

3. **The rest of the deferred roster, none chartered.** From this row: the other ELEVEN fixed reference `ssl.*` names incl. **`ssl.no_certificate`** (⚠️ **a SUCCESS-PATH annotation, NOT a synonym for `fail_verify_no_cert` — conflating them is wrong in BOTH directions**; now recorded in ADR-0296 §Decision(g) and long present at `BEHAVIOR_CONTRACT.md:962`) · the **FOUR**-family dynamic `ssl.ciphers`/`versions`/`curves`/`sigalgs` surface, still deferred on its own two blockers — **charset** (`stats.NamePattern` bans the hyphen, so the OpenSSL cipher form fails `IsValidName` and `checkName` **PANICS at `registry.go:117`** — ⚠️ **not `:107`, which is the duplicate-registration panic**) and **IANA-vs-OpenSSL naming** — with ⚠️ **LIFECYCLE EXPLICITLY NOT A BLOCKER** (`NewCounterIfAbsent` is permitted post-`Freeze()` by design, ADR-0117) · `Listener.stat_prefix` (accepted-and-silently-ignored; ZERO `GetStatPrefix()` consumers) · the IPv6-bracket and dot-normalization scope divergences (**PRE-EXISTING**, already affecting `downstream_cx_total` today) · the stat-surface ledger HOLE (no recorded `1200 → 1201`; **RECORDED as an unattributed gap at T7, deliberately NOT invented**) · the reference's whole QUIC accounting surface. **Carried unchanged:** the `upstream_cluster` span tag (the phase-74 RUNNER-UP — deferred on a **RESIDUAL DIVERGENCE, not on cost**: the reference's built-in tag is SELECTION-gated, not pick-gated, leaving 5 span-capable zero-endpoint emit sites) · pick-time `picked`-propagation · `access_log[].filter` · `hcm.merge_slashes` (whose blast radius contains a **PRE-EXISTING H2 ROUTING BUG** — `internal/filter/hcm/h2/stream.go` uses `url.Parse` not `url.ParseRequestURI`) · `hcm.access_log_options` · the `stdout`/`stderr` loggers · `stats_flush_on_admin` · the swallowed-panic BOOT HANG · route-level header manipulation · `prefix_rewrite`/`host_rewrite_*` · HCM `server_name`/`via` · the default-config header injection divergence · all tracing remainders · all xDS · all HTTP/3 · and the never-opened families (**WASM is a bookkeeping artifact**).

4. **⚠️ ELEVEN PLAN/SPEC CLAIMS WERE REFUTED BY EXECUTION AT THE IMPL — three of them are reusable lessons, not phase-74 trivia.** Full ledger in `PROGRESS.md` §IMPL record. **(a)** The PLAN's own **check-(1) blind-spot figure was STALE** (see the ⚠️ block above) — *a document's account of its own hygiene is not evidence that the hygiene held.* **(b)** The PLAN's own **"+0 production imports" gate COMMAND is unreliable**: `git diff master -- … | grep -E '^\+' | grep -E '^\+\s*(_|[a-z]+ )?"'` returns **3 false-positive hits** (map-literal lines) and exits 0, so gating on the exit code reads a PASS as a FAIL. Use hunk headers plus a direct extracted-import-block diff. **(c)** A predicted "red" can be **UNREACHABLE IN ONE STEP** — T2's assertion-level red was preceded by a COMPILE failure, and a single-stage run would have recorded a build failure as if it were the promised assertion red. **Stage the red when that happens; do not bank the compile error as evidence.**

5. **⚠️ A BREAK CAN ITSELF BE VACUOUS, AND THE PLAN'S OWN WORDING CAUGHT IT ONE LEVEL UP.** The PLAN's literal Break G′ (rename the dispatched method, leave `want` at the PASSING value) makes green consistent with BOTH *"never ran"* and *"ran and passed"* — **the PLAN's own parenthetical warning for G′ applied recursively to G′ itself.** Run stacked on a FAILING assertion it discriminates cleanly. ⇒ **when you write a break that proves "X runs at all", the baseline must be a FAILING X.** Also confirmed live: **`var _ fixture.StatsAsserter` is a TRIPWIRE, not the dispatch mechanism.**

6. **TWO FRESH HAZARDS discovered by execution, both plausible memory candidates if a future row re-hits them.** **(a)** `io.Copy(conn, conn)` is **unusable as an in-test echo backend** — `*net.TCPConn` implements `io.ReaderFrom`, so the socket splices into itself, the echo never returns, and **the failure surfaces AT THE BACKEND**, reading as a proxy bug; it was diagnosed only by a direct-to-backend probe before blaming the proxy. **(b)** A client chain built for a `CertificateVerificationError` assertion must be **LEAF-ONLY** — appending the issuing CA (as `0111/driver.go` does) makes `len(UnverifiedCertificates) == 2` and fires a `want 1` assertion.

7. **The live hazards — never reflex-classify any of these as a regression.** ⚠️ **`reference_sds_init_fetch_timeout_dial_budget_flake` WAS WIDENED at this close**: it named `internal/xds TestProvider_FetchInitialCertificate_Timeout`, but **`internal/boot TestSDSEndToEnd_FetchFailure_BootFailsClosed` shares the same 200 ms-budget mechanism** and fired once under the full `-race` suite — isolate-green 3/3 and **REPRODUCED ON MASTER 3/20 with the byte-identical error string** under 48 CPU burners, i.e. classified on evidence, not assertion. Also live: the PRE-EXISTING `internal/cluster` `-race` flake (`TestOutlierDetector_ConcurrentEjectExactlyOnce`, `internal/cluster/outlier_test.go:766` — **isolate-re-run, do NOT re-classify**) · the full-suite startup flake (`subject ready: EOF` on an UNRELATED fixture) · and TWO still-**UNINDEXED** load flakes, isolate-green and NOT root-caused: `internal/httpclient TestOptions_ZeroValue_NoOpDefaults` and `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery`.

8. **Counts at exit (re-run MECHANICALLY in the worktree; never copy them):** fixtures **119** (`ls -d test/fixtures/[0-9]*/ | wc -l`) / fuzzers **55** (`grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`) / **stat surface 1204** (BEHAVIOR_CONTRACT doc count; **NO mechanical command**) / BackendKind tail **38** (a TAIL VALUE, not a count — the file declares 39 constants, 0-38) / go.mod modules **2** (the phase-61.2 lineage figure; the single `go.mod` requires **67**) / DECISIONS tail **ADR-0296 COMPLETE** (next-free **ADR-0297**) / ZERO new packages.

## What THIS session does — the phase-75 BRAINSTORM

One stage per session. A BRAINSTORM **SELF-PICKS the subject** (2026-07-12 standing directive), **registers the row `in-progress`** in the ROADMAP at its stage-close commit per the §Schema invariant (`ROADMAP.md:21` — which RE-OPENS sentinel check (1)), and **narrows NO deferred sentence** (the phase-57 precedent: the narrow lands at the IMPL only).

1. **Pick the smallest defensible candidate and RECORD THE REJECTED ALTERNATIVES.** Re-cost each against SOURCE at the phase-75 tip — **a deferral's cost adjective is stale until re-derived** (`reference_deferred_candidate_cost_restale`), and four consecutive rows have now turned on exactly that.
2. **RE-DERIVE every anchor you cite.** A SPEC is not evidence, a PLAN is not evidence, and **a document's own hygiene claim is not evidence either** (`feedback_brief_citations_not_evidence`, `reference_quoting_is_not_executing`, `reference_a_drift_correction_is_itself_a_claim`). Re-derive PROSE claims too — phase 74's headline finding was a BRAINSTORM adjective that had acquired ADR authority by sitting next to one across three rows (`reference_brainstorm_adjective_acquires_adr_authority`).
3. **State plainly what was and was NOT executed.** A BRAINSTORM that runs no probe must say so (phase 74's §11.1 did, and the SPEC's ~35 executed arms then REFUTED its central anticipation — and the refutation made the row CHEAPER).
4. **Close by re-running all three sentinel checks mechanically, TWICE** — in the stage worktree AND on LANDED master after the squash-push (the phase-72/73/74 precedent: a stage worktree can disagree with what actually landed, and the post-push re-run is the one that counts). Registering row 75 should make check (1) print again; **record the ACTUAL output either way.**

**Stage-close mechanics (a BRAINSTORM):** the phase-directory delta is the new `BRAINSTORM.md`; ROADMAP gets the row registration (plus a charter sentence appended to the family heading paragraph — the phase-70/72/73/74 paired-edit precedent); **DECISIONS stays BYTE-UNTOUCHED** (the ADR's §Context is drafted at the SPEC, ADR-0044-**as-used** — ⚠️ **ADR-0044 does NOT actually contain that discipline; use the hedge**). STATE §Current rolled **IN PLACE** (lifecycle DONE → 1; §Recent re-capped at FIVE **with its PREAMBLE updated** — the ADR-0288 rule). ⚠️ **The ADR-0288 singleton greps return 2, not 1**, for each of the three field-name tokens — the second hit is `STATE.md:7`, the RULE STATEMENT itself. Verify with `grep -n`, and **never "fix" the count to 1**; that would delete the rule. `next-prompt.txt` rolled to the phase-75 SPEC.

Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`).

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. At the phase-72 close that put a stray commit on `master` in the MAIN checkout AND made a "final gates" run validate master's pre-row tree — GREEN while proving nothing. **Use `git -C <abs-worktree-path>` for every git command, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline) + §Project counts + the §Recent lineage (five bullets: phase-74 IMPL + phase-74 PLAN + phase-74 SPEC + phase-74 BRAINSTORM + phase-73 IMPL).
2. `docs/envoy-go/ROADMAP.md` — the §Schema invariant (`:21`), the THREE live `candidates:` sentences (`:184`, `:194`, `:204` — ⚠️ **`:204` is the ~42,000-char family-history line; `:202` is the 92-char family DESCRIPTOR**), the `### Observability family` heading (`:200`), and the closed row-74 cell (`:136`). **Every chartered row is `done`.**
3. `docs/envoy-go/phases/74-tls-handshake-outcome-stats/PROGRESS.md` — **§IMPL record**: the ACTUAL task ledger, the twelve-break ledger with every observed firing set, the ELEVEN refuted PLAN/SPEC claims, the two fresh hazards, and the `-race` flake's evidence-based classification. This is the freshest honest account of how a row actually goes.
4. `docs/envoy-go/DECISIONS.md` — **ADR-0296** (COMPLETE — the row's §Decision/§Consequences, incl. the `ssl.connection_error` departure, the `ssl.no_certificate` naming trap, and the FOUR-family split) · **ADR-0286** §Consequences **C3** and its NEW phase-74 `[CORRECTED …]` blockquote (the FORM to copy for a cross-family correction: **two literal spaces, then `> [`** — ⚠️ **the discriminator is the ADR FAMILY, not the phase gap**) · ADR-0045 (the >25-task / >1500-LoC split valve) · ADR-0106 · ADR-0117 · ADR-0288.
5. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the phase-74 B1-B8 delta: the RESTATED four-family coverage boundary (`:928`), the new downstream-TLS-handshake-outcome subsection, and the `### Stat surface` ledger with its new `Phase 74 — 1201 → 1204 (+3)` line and the **RECORDED, not invented** `1200 → 1201` gap note.
6. `docs/envoy-go/phases/74-tls-handshake-outcome-stats/{BRAINSTORM,SPEC,PLAN}.md` — the immediately-prior lineage's shape. ⚠️ **`SPEC.md:1`, `:315` and `:320` carry the SPEC's OWN REFUTED first QUIC reading** against its authoritative §3.4/§16; they were deliberately left as a historical record of a reversal. Read the SPEC THROUGH `PLAN.md` §1.1's RD-* ledger.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Load-bearing for a BRAINSTORM: `reference_deferred_candidate_cost_restale`, `reference_brainstorm_adjective_acquires_adr_authority`, `reference_a_drift_correction_is_itself_a_claim`, `feedback_brief_citations_not_evidence`, `reference_quoting_is_not_executing`, `reference_probe_must_discriminate`, `reference_sentinel_deferred_sentence_live_vs_historical`, `reference_next_prompt_tracked_despite_gitignore`, `reference_roadmap_split_phase_row_done`, `reference_parallel_stream_mints_fresh_drift`, `reference_parallel_subagents_private_scratch`, `reference_cluster_race_outlier_flake`, `reference_sds_init_fetch_timeout_dial_budget_flake` (⚠️ **widened at the phase-74 close**), and **`reference_bash_cwd_reset_commits_to_main`**.
