=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-VERIFIED 2026-07-21 (re-run MECHANICALLY at the phase-71 SPEC close, in the stage worktree) — it does NOT fire. `stop` was NOT created.** Actual output:
- **(1) prints `NOT DONE: row 71`** — row 71 (`tracing-custom-tags-metadata-route`) stays `in-progress` (registered at the phase-71 BRAINSTORM commit per the ROADMAP §Schema invariant; the SPEC does NOT flip it). It flips `done` at the phase-71 IMPL six-gate.
- **(2) prints THREE live "candidates:" sentences** — HTTP/3, xDS, Observability ⇒ three families STAY OPEN. **Cite the command, never the adjective** (`reference_sentinel_deferred_sentence_live_vs_historical`): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` ⇒ **3**. (The Observability sentence still names the non-`REQUEST` MetadataKinds `ROUTE`/`CLUSTER`/`HOST`; the phase-71 pick `ROUTE` is NAMED in it and does NOT drop it at the SPEC — it narrows at the phase-71 IMPL row-done edit, the phase-57 precedent.)
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** ⇒ THREE families never opened.

Checks (2) + (3) print ⇒ the sentinel does NOT fire. Abundant work remains.

==============================================


## STATUS AS OF 2026-07-21: THE PHASE-71 SPEC IS COMPLETE AND LANDED. ROW 71 (`tracing-custom-tags-metadata-route`) STAYS `in-progress`. THIS SESSION PERFORMS THE PHASE-71 PLAN.

**What the phase-71 SPEC landed** (`git log --grep 'phase 71'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): the D-CTMR-* docket DISPOSED one-for-one via SIX EXECUTED live-probe arms (P1–P6 against `contrib-v1.37.2`, a STATIC route-config metadata source served — NO writer, unlike phase 70's Lua REQUEST arm — a standalone host OTLP-gRPC receiver + a raw-TCP Zipkin dumper, decode-ran asserted per arm) + a full read-only code re-derivation of every §6 anchor at tip. docs-only apart from the ADR-0293 §Context append; ZERO production `.go`.
- **HEADLINE — every anchor HELD; NO code-blocking correction.** P1: the reference BOOTS ROUTE/CLUSTER/HOST (`--mode validate` OK) while PGV-rejecting empty `metadata_key.key`/`path`/segment ⇒ lift ROUTE (real capability), keep CLUSTER/HOST as envoy-go-strict departures, the empty-structural rejects are parity. P2–P6: every phase-70 wire/serialize/default/pathwalk rule REPRODUCES exactly for ROUTE (literal-tag-name key + serialized string appended after built-ins on OTLP + in the Zipkin `tags` map; the P3 serialization table; the `request_header` default rule present-empty EMITS `""`; nested path-walk + unresolvable-segment→default).
- **FIVE sharpenings (none code-blocking):** **S1** — `routeMetaLookup` is a NEW nil-tolerant FOURTH param on `ResolveCustomTags` (ONE-arg `func(ns) (*structpb.Value, bool)`, distinct from the REQUEST two-arg `metaLookup`), descending the FULL `MetaPath` (the `[1:]` is a REQUEST-only Bucket-pre-keying artifact). **S2** — `structpb` is ABSENT from the three dispatch files + `chain.go`, so a NEW `func (c *FilterChain) RouteMetaLookup(ns string) (*structpb.Value, bool)` method (wrapping `RouteMetadata().GetFilterMetadata()[ns]` via `structpb.NewStructValue`) centralizes the import to `chain.go` and threads as `chain.RouteMetaLookup` — the ROUTE analogue of phase-70's `DynamicMetadata()` add. **S3** — the unset-kind reject splits (nil `*MetadataKind` = DEPARTURE [reference accepts, no `required` rule]; present-empty-oneof = PARITY [PGV "value is required"]) — wording only, both already reject. **S4** — the existing `withMetaTags` `meta_bad` ROUTE seed exercises the reject arm phase 71 REMOVES, so repurpose it (add a valid ROUTE accept `meta_route_ok` + repoint `meta_bad` to CLUSTER). **S5** — intra-custom-tag order is INTERNAL iteration order (not config order), so assertions are key-based.
- **The design:** lift the ROUTE reject (`config.go:245-246`) → an accept arm cloning the REQUEST accept (`:228-244`) building `CustomTagSpec{Kind: kindMetadataRoute, MetaNamespace, MetaPath (FULL), DefaultValue, HasDefault}`; `resolve.go` grows the `routeMetaLookup` 4th param + a `kindMetadataRoute` arm reusing `descend`/`structpbValueToString` VERBATIM and descending the FULL `MetaPath`; the NEW `*FilterChain.RouteMetaLookup()` method threaded at the 15 span-capable emit sites + `nil` at the 3 no-chain-404 sites; `CLUSTER`/`HOST`/unset-kind stay reject-loud (envoy-go-strict, ADR-0080).
- **Envelope (at the phase-71 IMPL):** ZERO new packages (edits in `internal/tracing` + `internal/filter/http` + `internal/filter/hcm`) / +0 stats / +0 fuzzers (the repurposed `FuzzHCMConfigParse` seed) / +0 BackendKinds / +0 go.mod modules (`corev3`/`structpb`/`type/metadata/v3`/`protojson` all already resolve; `chain.go` gains a `structpb` IMPORT of an existing module) / ONE new OTLP fixture `0115-tracing-custom-tags-metadata-route` (static-route-metadata positive arm + `default_value` arm; NO writer) ⇒ fixtures **116 → 117** / DECISIONS tail flipped **ADR-0292 → ADR-0293** (at THIS SPEC; §Decision + §Consequences land IN PLACE at the IMPL; next-free ADR-0294).

**⚠️ PUSH (idempotent — evaluate, then act):** FIRST ACTION: `git fetch`; if origin/master is BEHIND local master and the local tip is the phase-71 SPEC squash (`git log --grep 'phase 71'`, locate by SUBJECT), `git push origin master` per `feedback_push_to_origin`; if in sync, proceed; if REJECTED (diverged), STOP and reconcile — never rebase away landed squashes.

## What THIS session does — the phase-71 PLAN (RE-DERIVE the SPEC)

One stage per session: the PLAN. **RE-DERIVE the SPEC, do NOT execute it.** Decompose SPEC §10 into the SINGLE FLAT ROW ~9-task TDD spine, RE-DERIVING every §11 edit-site anchor at the tree at the SPEC squash (a SPEC cite is NOT evidence — `feedback_brief_citations_not_evidence`; re-open each anchor at tip). Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; docs-only (`PLAN.md` + `PROGRESS.md`; DECISIONS/ROADMAP UNTOUCHED at the PLAN — ADR-0293 stays PROPOSED, completed IN PLACE at the IMPL). Row 71 STAYS `in-progress`. Then roll THIS file to the phase-71 IMPL.

**The 9-task spine to decompose (SPEC §10):**
- **T1** — `config.go`: `kindMetadataRoute` const (iota==4 after `kindMetadata`) + the ROUTE accept arm (cloning the REQUEST `:228-244`) replacing the `:245-246` reject; CLUSTER/HOST/unset rejects UNCHANGED. `config_test.go`: accept ROUTE + reject CLUSTER/HOST/unset-A/unset-B/empty-namespace/empty-path/empty-segment (distinct substrings).
- **T2** — `resolve.go`: the `routeMetaLookup` 4th param + the `kindMetadataRoute` arm descending the FULL `MetaPath` (NOT `[1:]`), reusing `descend`/`structpbValueToString`. `resolve_test.go`: the ROUTE path-walk (single/multi/unresolvable), the P4 serialization table, the P5 default/omit/present-empty matrix, nil-`routeMetaLookup` + nil-`RouteMetadata` tolerance.
- **T3** — `internal/filter/http/chain.go`: add `func (c *FilterChain) RouteMetaLookup(ns string) (*structpb.Value, bool)` (S2) + the `structpb` import + a unit test (present ns → wrapped struct; absent ns / nil `RouteMetadata` → `(nil,false)`).
- **T4** — `accesslog_emit.go`: thread `routeMetaLookup` to the three emit methods; update ALL 18 caller sites (`nil` at the 3 no-chain 404 sites `connection.go:330`/`h2dispatch.go:313`/`h3dispatch.go:130`, `chain.RouteMetaLookup` at the 15 span-capable sites). `span_emit_test.go`/`accesslog_emit_test.go` callers updated (pass `nil`).
- **T5** — `FuzzHCMConfigParse` seed repurpose (S4: add a valid ROUTE accept + repoint `meta_bad` to CLUSTER) + dispatch-verify (+0 fuzzers).
- **T6** — the OTLP `0115-tracing-custom-tags-metadata-route` fixture (a static route `metadata.filter_metadata` block + two ROUTE custom_tags — one static-value hit, one `default_value` — cross-side EXACT key+value, NO Lua writer) + deliberate breaks (fixtures 116 → 117).
- **T7** — BEHAVIOR_CONTRACT B1-B2 (flip the ROUTE line to CONSUMED; narrow the departures to CLUSTER/HOST + unset-A).
- **T8** — six-gate + full 117-dir differential + `-race` (`internal/tracing` + `internal/filter/hcm` + `internal/filter/http`) + the cycle guard (`go list -deps ./internal/tracing` — no filter-pkg edge) + `go mod tidy -diff` empty + envelope audit (`internal/xds`/`internal/tls`/`internal/boot`/`internal/listener`/`internal/bootstrap` BYTE-UNTOUCHED).
- **T9** — ADR-0293 completed IN PLACE + ROADMAP row-71-done + the deferred-sentence narrow (roll the `ROUTE` MetadataKind OUT — the phase-57 precedent) + STATE/router (stage-close).

**Stage-close mechanics (the PLAN):** DECISIONS UNTOUCHED (ADR-0293 stays PROPOSED — §Decision/§Consequences land IN PLACE at the IMPL, no renumber; next-free ADR-0294). Row 71 STAYS `in-progress`. NO deferred-sentence narrow at the PLAN (the phase-57 precedent). STATE §Current rolled IN PLACE (lifecycle 2 → 3; lineage re-capped at five). next-prompt.txt rolled to the phase-71 IMPL. Re-run the sentinel MECHANICALLY at close: (1) `NOT DONE: row 71`; (2) **3**; (3) `NEVER OPENED: gRPC/Runtime/WASM`. Does NOT fire; no `stop`. **Adversarially verify before landing** (the phase-70 PLAN precedent — TWO independent verifiers in PRIVATE scratch, `reference_parallel_subagents_private_scratch`; V1 code-claims + by-execution compile probes RE-DERIVING each skeleton at tip, V2 process/consistency).

## Read first (in order)
1. `docs/envoy-go/phases/71-tracing-custom-tags-metadata-route/SPEC.md` — the D-CTMR-* dispositions (§3), the P1–P6 probe conclusions + the S1–S5 sharpenings (§1.1), the resolve-seam design (§3.3/§3.5), the fixture (§8), the task surface (§10), the edit-site roster (§11), the ADR-0293 §Context draft (§14).
2. `docs/envoy-go/phases/71-tracing-custom-tags-metadata-route/BRAINSTORM.md` — the D-CTMR-* docket origin + the rejected roster (§2.1).
3. `docs/envoy-go/ROADMAP.md` — row 71 (`in-progress`) + the live Observability `candidates:` sentence + §Schema.
4. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline) + the §Recent lineage (the phase-71 SPEC + BRAINSTORM + the phase-70 stages).
5. `docs/envoy-go/DECISIONS.md` tail — ADR-0293 (PROPOSED — the phase-71 §Context to complete at the IMPL) + ADR-0292 (the phase-70 `metadata`/`REQUEST` precedent) + ADR-0044/0045/0106/0165/0080.
6. The phase-70 PLAN (`docs/envoy-go/phases/70-tracing-custom-tags-metadata/PLAN.md`) for the PLAN shape + the T-spine decomposition structure to mirror.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — trust MEMORY.md's index; esp. `feedback_brief_citations_not_evidence`, `feedback_git_worktrees`, `feedback_execution_style`, `reference_filterchain_dynamicmetadata_accessor` (verify the RECEIVER — the `RouteMetaLookup` seam extends this), `reference_detrand_race_catches_protojson_value_substring`, `reference_parallel_subagents_private_scratch`.
