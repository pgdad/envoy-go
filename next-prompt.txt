=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-CHECKED 2026-07-12 (at the phase-58 BRAINSTORM, master `6d3a4330` pre-commit, run MECHANICALLY) — it does NOT fire. Do NOT create `stop`.** Evidence:
- (1) will now print `NOT DONE: row58` once the BRAINSTORM commit lands (row 58 registered `in-progress` per the §Schema invariant — a phase is open). This ALONE keeps the sentinel from firing; it clears when row 58 flips `done` at the phase-58 IMPL.
- (2) prints the Observability family's LIVE deferred-candidates sentence: OTLP-metrics stats sink + tracing `custom_tags`/`spawn_upstream_span`/`http_service`/force-trace + **the dog_statsd explicit-`max_bytes_per_datagram: 0` parity fix** — which is the SUBJECT of phase 58 but STAYS in the live sentence until the phase-58 IMPL rolls it OUT (the roll happens at row-done, `reference_sentinel_deferred_sentence_live_vs_historical`). ⇒ the **Observability family STAYS OPEN**. (NOTE: a phase-58 CHARTERED sentence was appended to the family paragraph but deliberately contains NO literal "candidates:" token, so check (2) still returns EXACTLY ONE live match. The historical "candidates were:"/"candidates now that…:" sentences are non-matching by construction.)
- (3) prints `NEVER OPENED: HTTP/3`, `gRPC`, `xDS`, `Runtime`, `WASM` ⇒ FIVE families never opened; the **Operational-tooling family** also STAYS OPEN (its deferred candidates live in its family section prose).

**STATUS AS OF 2026-07-12: phase 58 (`stats-sink-dogstatsd-explicit-zero`) is OPEN — the BRAINSTORM is DONE (row 58 `in-progress`, lifecycle-state 0 → 1). The next stage is the phase-58 SPEC.** Subject: the `dog_statsd` explicit-`max_bytes_per_datagram: 0` parity fix (a ONE-arm parse-reject mirroring the phase-57 graphite arm VERBATIM modulo the sink name; the smallest row yet).

**Current counts (verified at the phase-58 BRAINSTORM, master `6d3a4330` pre-commit):** fixtures **103** (tail `0101-stats-sink-graphite`) · fuzzers **54** · DECISIONS tail **ADR-0275** (next-free **ADR-0276**, chartered as the row-58 anchor) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201 (+0)** · new Go packages **0** · new go.mod modules **0**. RE-VERIFY against the actual master tip at session start (`git fetch` first; parallel workstreams land test-only commits).

**Locating the phase-58 BRAINSTORM stage squash** (a router cannot name its own sha — writing the sha changes it; identify by SUBJECT, never by position):
```sh
git log --oneline --grep='^phase 58 (stats-sink-dogstatsd-explicit-zero) BRAINSTORM' -1
```

**THE NEXT ACTION: the phase-58 SPEC (lifecycle-state 1 → 2).**

==============================================

## What THIS session does (the phase-58 SPEC)

Perform the SPEC for phase 58 (docs-only; NO production `.go` changes at a SPEC; fresh worktree per `feedback_git_worktrees`, e.g. `.worktrees/phase-58-spec`; row 58 STAYS `in-progress` — it flips `done` only at the phase-58 IMPL six-gate per ADR-0106, the SOLE leg; draft **ADR-0276 §Context** as the row's anchor per ADR-0044 [§Decision/§Consequences land at the IMPL; DECISIONS tail STAYS ADR-0275 at this SPEC]; the phase-57 `SPEC.md` is the template shape).

**The SPEC's job** — dispose the five BRAINSTORM D-DZ-* questions and pin the row end-to-end:
- **D-DZ-REJECTMSG** — ONE fresh-container live probe (`reference_probe_fresh_container_per_arm`) against `envoyproxy/envoy:contrib-v1.37.2` (`reference_envoy_contrib_image_tagging`): boot a bootstrap with a `dog_statsd` sink carrying an explicit `max_bytes_per_datagram: 0`, capture the reference's exact boot-reject wording (anticipated `Proto constraint validation failed (DogStatsdSinkValidationError.MaxBytesPerDatagram: value must be greater than 0)`). This confirms BEHAVIOR parity (both processes reject). envoy-go emits its OWN graphite-mirrored substring `dog_statsd max_bytes_per_datagram must be greater than 0` (NOT the PGV text — the phase-48/49/57 own-message posture). Also RE-DERIVE the `stats.pb.validate.go` line of the PGV `gt: 0` rule (SPEC-57 §11 A4b cited `1144-1150` — re-verify against source, `feedback_brief_citations_not_evidence`).
- **D-DZ-FIXTURE** — anticipated NO new fixture (a boot-reject is parse-time; the phase-49/50/57 reject rosters were proven by SUBJECT unit tests + fuzz seeds, NOT fixtures — a rejected bootstrap never reaches the differential runner). SPEC confirms fixtures STAY 103.
- **D-DZ-TESTROWS** — the CONVERT-not-ADD shape: delete `TestDogStatsdSink_AcceptMaxBytesPerDatagramZero` (`bootstrap_test.go:2550`), add an `explicit_max_bytes_per_datagram_zero` row to `TestDogStatsdSink_Rejects` (`bootstrap_test.go:2458`) with `errSubs: []string{"bootstrap:", "dog_statsd max_bytes_per_datagram must be greater than 0"}` (the graphite reject-row shape, `bootstrap_test.go:2890-2899`); KEEP `TestDogStatsdSink_AcceptMaxBytesPerDatagramAbsent` (`bootstrap_test.go:2528`).
- **D-DZ-FUZZSEED** — a SEED to the EXISTING `FuzzDogStatsdSinkConfigParse` (`dogstatsd_fuzz_test.go`), mirroring the graphite fuzzer's explicit-0 seed (`graphite_fuzz_test.go:63-70`); NOT a new fuzzer. Reconcile the running total (54) against actual `^func Fuzz` BEFORE and AFTER — it must NOT move (`reference_fuzzer_count_docs_drift`).
- **D-DZ-DOCSHAPE** — RE-DERIVE the full edit-site roster from source (BRAINSTORM §6): the reject arm in `parseDogStatsdSinkConfig` (`bootstrap.go:712`, before the append) + three `bootstrap.go` doc-comment sites (the `DogStatsdSinkConfig` struct doc `318-330`, the `parseDogStatsdSinkConfig` func doc `702-711`, the `parseGraphiteStatsdSinkConfig` NOTE `738-740` that becomes stale) + four `BEHAVIOR_CONTRACT.md` sites (`785` dog_statsd strict-rejects, `787` batching text, `821` graphite NOTE, `834` consumption summary). ADR-0275 §Consequences + SPEC-57 §2/§11 A4b name the gap HISTORICALLY — do NOT edit them.

Anticipated spine ~5-7 tasks (the smallest row yet), margin large under the ADR-0045 `~15` ceiling; D-DZ-SPLIT is a formality (single flat row, escape-valve unconsumed). Then ROLL THIS FILE FORWARD to the phase-58 PLAN (fold the roll into the stage commit) and land it (commit + push per `feedback_push_to_origin`).

## Honor throughout (the discipline, unchanged)
- **Lifecycle = one stage per session.** This session: the phase-58 SPEC ONLY. Subsequent sessions: PLAN → IMPL, one per session, router-driven.
- **Process anchors:** ADR-0044 (ADR §Context drafts at SPEC, §Decision/§Consequences at IMPL) · ADR-0045 (split gate ~15 tasks — this row anticipates ~5-7) · ADR-0080 (strict-reject anti-silent-divergence; DISTINCT per-arm substrings — `dog_statsd …` vs graphite's `graphite_statsd …`) · ADR-0106 (row 58 flips `done` at its IMPL six-gate). Next-free ADR is **ADR-0276** (the row-58 anchor).
- **Live probe at SPEC time** uses a FRESH container per arm (`reference_probe_fresh_container_per_arm`) against the `envoyproxy/envoy:contrib-v1.37.2` pin (`reference_envoy_contrib_image_tagging`). Only ONE arm is needed (D-DZ-REJECTMSG).
- **`next-prompt.txt` IS TRACKED** — edit it inside the stage worktree, fold into the stage commit; locate commits by SUBJECT (`git log --grep`), never by position (`reference_next_prompt_tracked_despite_gitignore`).
- **After any ROADMAP deferred-list edit (at the phase-58 IMPL, NOT the SPEC):** re-run the sentinel check-(2) grep — EXACTLY ONE live match, correct content (`reference_sentinel_deferred_sentence_live_vs_historical`). When row 58 lands `done`, the family sentence rolls the dog_statsd candidate OUT (OTLP + the tracing quartet remain ⇒ check (2) still prints ⇒ the sentinel still does not fire).
- **The NON-additive test edit is the row's load-bearing risk:** `TestDogStatsdSink_AcceptMaxBytesPerDatagramZero` asserts the OLD (buggy) accept behavior; it must be CONVERTED, not merely supplemented. Prove the new reject arm is LIVE with a `-count=1` deliberate break (`reference_differential_break_protocol_count1` + `reference_deliberate_break_wrong_assertion`).
- **Deferred maintenance (fold into the next `udp.go`/statssink-driver-touching row — NOT this one, which touches neither; from the phase-57 final review):** F-1 the 0101 driver's counter tag snapshot records the EXPECTED map; F-2 `udp.go` `udpWriter` doc comment names two sinks (three embed it now); F-3 BEHAVIOR_CONTRACT 0101 note says "`differential.HostGatewayIP` helper" where the driver uses its LOCAL duplicate. Recorded in BRAINSTORM §11 so they are not lost.

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-58 SPEC is next; subject: dog_statsd explicit-zero parity).
2. `docs/envoy-go/phases/58-stats-sink-dogstatsd-explicit-zero/BRAINSTORM.md` — the 12-section charter (§2.1 pick rationale, §2.4 the non-additive test, §6 the edit-site roster, §10 the D-DZ-* questions).
3. `docs/envoy-go/phases/57-stats-sink-graphite/SPEC.md` §2 + §11 A4b — the recorded gap + the graphite reject shape/probe this row mirrors (the SPEC template).
4. `internal/bootstrap/bootstrap.go` — `parseDogStatsdSinkConfig` (`:712`, the unchecked `GetValue()` consume) vs `parseGraphiteStatsdSinkConfig` (`:741`, the reject-arm template `:750-752`); the two struct docs (`:318-330`, `:342-346`) and the graphite NOTE (`:738-740`).
5. `internal/bootstrap/bootstrap_test.go` — `TestDogStatsdSink_Rejects` (`:2458`), `TestDogStatsdSink_AcceptMaxBytesPerDatagramZero` (`:2550`, to convert), `…Absent` (`:2528`, to keep), the graphite reject row (`:2890-2899`, the template).
6. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the four dog_statsd/graphite edit sites (`:785`, `:787`, `:821`, `:834`).
7. `docs/envoy-go/ROADMAP.md` — the Observability family section (the deferred-candidates LIVE sentence this row will edit at its IMPL) + row 58.
8. `docs/envoy-go/STATE.md` — the active-phase header (phase 58 BRAINSTORM done; SPEC next).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the `reference_*` traps.
