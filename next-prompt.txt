=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-VERIFIED 2026-07-21 (re-run MECHANICALLY at the phase-70 IMPL close, in the stage worktree) — it does NOT fire. `stop` was NOT created.** Actual output:
- **(1) prints NOTHING** — row 70 (`tracing-custom-tags-metadata`) FLIPPED `in-progress` → `done` at the phase-70 IMPL six-gate; every chartered ROADMAP row is now `done`. Check (1) is SILENT.
- **(2) prints THREE live "candidates:" sentences** — HTTP/3, xDS, Observability ⇒ three families STAY OPEN. **Cite the command, never the adjective** (`reference_sentinel_deferred_sentence_live_vs_historical`): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` ⇒ **3**. (The Observability sentence stayed live after the phase-70 IMPL narrow — the `custom_tags (metadata)` REQUEST-kind candidate rolled OUT; the three non-`REQUEST` MetadataKinds + the `ssl` + `spawn_upstream_span`/`http_service`/force-trace candidates remain.)
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** ⇒ THREE families never opened.

Checks (2) + (3) print ⇒ the sentinel does NOT fire. Abundant work remains.

==============================================


## STATUS AS OF 2026-07-21: THE PHASE-70 IMPL IS COMPLETE AND LANDED. ROW 70 (`tracing-custom-tags-metadata`) IS `done`. THIS SESSION PERFORMS THE PHASE-71 BRAINSTORM (the roller SELF-PICKS the subject).

**What the phase-70 IMPL landed** (`git log --grep 'phase 70'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): the tracing `custom_tags` `metadata` type for the `REQUEST` MetadataKind — the FOURTH tracing `custom_tags` row and the LAST `CustomTag` SOURCE type (all four now consumed). A 9-task TDD spine, subagent-driven (one implementer + a per-task adversarial reviewer), each task RE-DERIVING its anchors at the IMPL tip. **ROW 70 FLIPPED `in-progress` → `done`** at the six-gate (ADR-0106, the SOLE leg); ADR-0292 COMPLETED IN PLACE.
- **The mechanism:** a `kindMetadata` parse arm (`config.go` — accept `REQUEST` into `CustomTagSpec{MetaNamespace,MetaPath}`, reject `ROUTE`/`CLUSTER`/`HOST`/unset-kind with distinct substrings — an envoy-go-strict DEPARTURE where the reference boots them; empty ns/path/segment PGV-PARITY rejects) + a `ResolveCustomTags` `metaLookup` param & `kindMetadata` arm (`resolve.go` — the `request_header` default rule: present-empty EMITS `""`; a filter-free `descend` path-walk cloned from the ratelimit shape; `structpbValueToString` string→raw / else `protojson.Marshal`+`json.Compact`) + a NEW `func (c *FilterChain) DynamicMetadata()` accessor (`chain.go`) threaded at all 18 emit callers (15 `chain.DynamicMetadata().Get`, 3 no-chain-404 `nil`) + a `FuzzHCMConfigParse` seed + the OTLP fixture `0114` + BC B1–B2.
- **Six-gate ALL GREEN:** gofmt/vet/build/`go mod tidy -diff` EMPTY/golangci-lint + FULL **116-dir** differential `ok 381.376s` EXIT 0 + cycle guard (no filter edge — `internal/tracing` stays filter-free) + `-race` + envelope audit (7 functional production files; `internal/xds`/`internal/tls`/`internal/boot`/`internal/listener`/`internal/bootstrap`/`validate/` + `internal/dynamicmetadata`/`ratelimit`/`lua` BYTE-UNTOUCHED).
- **Counts at exit:** fixtures **115 → 116** (`0114-tracing-custom-tags-metadata`) / fuzzers **55 (+0)** / stat **1201 (+0)** / BackendKind **38 (+0)** / go.mod modules **2 (+0)** / DECISIONS tail **ADR-0292** COMPLETE (next-free **ADR-0293**).

**⚠️ PUSH (idempotent — evaluate, then act):** FIRST ACTION: `git fetch`; if origin/master is BEHIND local master and the local tip is the phase-70 IMPL squash (`git log --grep 'phase 70'`, locate by SUBJECT), `git push origin master` per `feedback_push_to_origin`; if in sync, proceed; if REJECTED (diverged), STOP and reconcile — never rebase away landed squashes.

## What THIS session does — the phase-71 BRAINSTORM (the roller SELF-PICKS)

One stage per session: the BRAINSTORM. **The loop is AUTONOMOUS (2026-07-12 standing directive) — the roller SELF-PICKS the next subject, does NOT pause for a human pick.** Pick the smallest defensible ROW-SIZED candidate first; RECORD the pick + the fully-adjudicated rejected roster in the BRAINSTORM (the phase-70 BRAINSTORM §2.1 precedent). Invoke `superpowers:brainstorming` first. Fresh worktree off master per `feedback_git_worktrees`; docs-only (ZERO production `.go`); register the new row `in-progress` per the ROADMAP §Schema invariant (this re-opens sentinel check (1) until that row's IMPL). Then roll THIS file to the phase-71 SPEC.

**The candidate landscape (re-derive at tip — a stale cost restales, `reference_deferred_candidate_cost_restale`):**
- **Three families carry live deferred `candidates:` sentences:** Observability (the three non-`REQUEST` MetadataKinds `ROUTE`/`CLUSTER`/`HOST` — now reject-only; the `ssl` handshake-outcome stat family — ADR-0286 C3 framework-surgery + a NEW stat surface; `spawn_upstream_span`; `http_service`; force-trace), HTTP/3 (upstream H3 cluster + alt-svc + 0-RTT + h3spec + QUIC robustness), xDS (SDS rotation + upstream SDS + CDS/EDS + LDS/RDS + ADS + Delta xDS + RTDS + reconnection-backoff + google_grpc).
- **Three families NEVER opened:** gRPC, Runtime, WASM — opening one is defensible but larger; prefer the smallest row-sized candidate unless a family-open is the smallest defensible unit.
- **Verify each candidate's COST at the current tip** (later landed rows may have built the substrate a deferred row needed) before picking; do NOT trust a deferral's own adjective.

**Stage-close mechanics (the BRAINSTORM):** register the new row `in-progress` (§Schema); NO deferred-sentence narrow at the BRAINSTORM (the phase-57 precedent — narrows only at the picked row's IMPL row-done edit); DECISIONS UNTOUCHED (the ADR §Context drafts at the SPEC per ADR-0044); STATE §Current rolled IN PLACE (lifecycle DONE → 1 for the new phase; lineage re-capped at five); next-prompt.txt rolled to the phase-71 SPEC. Re-run the sentinel MECHANICALLY at close: (1) prints `NOT DONE: row 71` (the registration re-opens it); (2) STILL prints **3** (a new pickup NAMED in a live `candidates:` sentence does NOT drop it — it narrows at the IMPL); (3) `NEVER OPENED: gRPC/Runtime/WASM` (unless the pick OPENS one — then add its row-summary slug to check (3) per the router's line-29 instruction). Does NOT fire; no `stop`.

## Read first (in order)
1. `docs/envoy-go/ROADMAP.md` — the three live `candidates:` sentences (locate by `grep -nE 'remaining deferred \(not-yet-chartered\) candidates:'`, not by line) + §Schema (row-registration invariant) + the Observability/HTTP/3/xDS family paragraphs + the never-opened families.
2. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline) + the §Recent lineage (the phase-70 IMPL + the prior stages).
3. `docs/envoy-go/DECISIONS.md` tail — ADR-0292 (the phase-70 IMPL, COMPLETE) + ADR-0044/0045/0106 (the ADR/split/valve discipline) + the family-relevant prior ADRs once the pick is made.
4. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — trust MEMORY.md's index; esp. `feedback_execution_style`, `feedback_git_worktrees`, `reference_deferred_candidate_cost_restale`, `reference_sentinel_deferred_sentence_live_vs_historical`, `feedback_brief_citations_not_evidence`, and the two phase-70 additions (the `*FilterChain.DynamicMetadata()` accessor gap; the tracing metadata `json.Compact` serialization).
5. The prior phase directories under `docs/envoy-go/phases/` for the BRAINSTORM shape (the phase-70 BRAINSTORM §2.1 rejected-roster precedent).
