START a fresh session for envoy-go (router last updated 2026-06-25; the **phase 44 (`grpc-access-log`) BRAINSTORM is DONE** — the BRAINSTORM-stage squash is `81afdd59` (`phase 44 (grpc-access-log) BRAINSTORM: … OPENS the Observability family`), landing `docs/envoy-go/phases/44-grpc-access-log/BRAINSTORM.md` + `README.md` + the STATE/ROADMAP rolls; `81afdd59` sits directly BENEATH the run of `next-prompt.txt:` docs-only tip-stamp commits at the tip — the master tip is the LATEST `next-prompt.txt:` commit (a roll or a tip-stamp fix), NOT `81afdd59` itself; re-read the shas, do NOT assume. The substantive predecessor BENEATH `81afdd59` is the 43.2b IMPL squash `676e17cf`, and the Upstream-robustness family — phases 39–43 — is CLOSED). **The Observability family is now OPEN** (phase 44 is its FIRST row; the family STAYS OPEN — OTLP / tracing / stats sinks / tap remain future rows). **The next action is the 44.1 (core) SPEC** — invoke `superpowers:writing-plans` scoped to SPEC authoring (the project's SPEC stage), execute the §10 D-ALS-* empirical pins IN-SESSION live against `contrib-v1.37.2`, anchor the **ADR-0255** §Context draft, then proceed PLAN → IMPL (each stage SUBAGENT-DRIVEN, `feedback_execution_style`). **Git hygiene:** all phase work runs in a FRESH git worktree off master (`feedback_git_worktrees`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the controller squashes + pushes at stage-close (`feedback_push_to_origin`); each code task runs `gofmt -l` + `golangci-lint` on touched pkgs (`feedback_pertask_gofmt_lint`).

First confirm the state via `git log --oneline -5` (expect HEAD to be the most recent `next-prompt.txt:` docs-only commit — the latest tip-stamp roll — with the phase-44 BRAINSTORM squash `81afdd59` sitting directly BENEATH the run of `next-prompt.txt:` commits, and the substantive 43.2b squash `676e17cf` BENEATH `81afdd59`; re-read the shas, do NOT assume — any number of docs-only `next-prompt.txt:` commits may stack atop `81afdd59`, which stays the stable BRAINSTORM anchor). Counts at the 44 BRAINSTORM (UNCHANGED, docs-only): stat surface **1187** (H2 cluster; non-H2 **1183**) / fixtures **82** (tail `0080-h2-goaway-rotation`) / fuzzers **43** / BackendKind tail **38** (`H2GoawayResponder`) / DECISIONS tail **ADR-0254**, next-free **ADR-0255**.

## NEXT STEP — the 44.1 (core) SPEC (`superpowers:writing-plans`, SPEC-scoped)

Phase 44 (`grpc-access-log`) opens the Observability family with a streaming **gRPC Access Log Service (ALS) sink** — `envoy.extensions.access_loggers.grpc.v3.HttpGrpcAccessLogConfig` driving `envoy.service.accesslog.v3.AccessLogService.StreamAccessLogs` (client-streaming, identifier-once) — split BY-CONCERN into 3 legs:

- **44.1 (core)** — config parse (`HttpGrpcAccessLogConfig` TypeURL → `ALSConfig`; HTTP-only; transport-V3-only; strict-reject `tcp_logs`/non-V3; LIFT gRPC ALS from the ADR-0041 silent-ignore set; blank-import the grpc config proto) + the `ALSClient` typed wrapper over the phase-18 `grpcclient.Dialer` (the `AuthClient` precedent, ADR-0158) + the `GrpcAccessLogSink` (the `AsyncFileSink` bounded-channel + writer-goroutine shape; stream lifecycle [lazy establish / identifier-once / reconnect-on-error]; the 10-field `Record`→`HTTPAccessLogEntry` structured mapping — NOTE `protocol_version` is the `HTTPAccessLogEntry_HTTPVersion` ENUM, NOT a passthrough string; a FIXED simple flush) + stats (`access_logs.grpc.*` — SPEC-pinned) + `cmd/envoy-go/main.go` dial-and-build wiring + the differential: a NEW ALS gRPC receiver **BackendKind 39** + fixture **`0081-grpc-access-log`** (poll-to-converge then cross-side EXACT on the deterministic structured-field subset). **ADR-0255** — ANCHORS the Observability family.
- **44.2 (buffering)** — honor `buffer_size_bytes` + `buffer_flush_interval` (replace the 44.1 fixed flush with size+interval triggers) + an extended differential prong. **ADR-0256**.
- **44.3 (header-capture)** — `additional_request_headers_to_log` + `additional_response_headers_to_log` (extend `accesslog.Record` to carry captured header values; capture in the H1/H2 emit hooks by configured name) → map into `request.request_headers`/`response.response_headers` + an extended differential prong. **ADR-0257**.

The 44.1 SPEC should: execute the §10 **D-ALS-{FIELDS,STATS,LIFECYCLE,RECEIVER}** live probes against `envoyproxy/envoy:contrib-v1.37.2` per `reference_docker_probe_bridge_network` (a shared bridge + an ALS-receiver hostname reachable from BOTH containers; verify the receiver actually received ≥1 entry from the reference — the decode-ran proof); pin the EXACT `HTTPAccessLogEntry` field population + sub-message nesting (which fields are deterministic vs which to leave unasserted) + the `protocol_version` enum mapping; pin the EXACT stat-name set + the `+N` surface delta; pin the `StreamAccessLogs` identifier-once framing; pin the ALS receiver BackendKind shape (in-process `AccessLogServiceServer` accumulating entries + a poll/assert surface); RE-CHECK the ADR-0045 split gate with real LoC; anchor the **ADR-0255** §Context draft.

## Honor throughout (the discipline)

- **Lifecycle = one stage per session.** SPEC → PLAN → IMPL, each subagent-driven, each squashed + pushed at stage-close. `feedback_git_worktrees` (fresh worktree off master) · `feedback_subagents_no_push` · `feedback_push_to_origin` · `feedback_execution_style` · `feedback_pertask_gofmt_lint` · `feedback_subagent_worktree_detach` / `feedback_subagent_worktree_path_targeting` (worktree hygiene + path-pinning; the controller verifies `git -C <main> status` clean after each task) · `reference_full_suite_race_after_background_mutator` (the sink's writer goroutine is a background mutator — after it lands, re-run the FULL package `-race`, NOT a `-run` subset).
- **Process anchors:** ADR-0106 (a new family opened at phase 44; row 44 flips `done` PER-LEG at each IMPL; the Observability family STAYS OPEN) · ADR-0080 (strict-reject — `tcp_logs`/non-V3) · ADR-0044 (the ADR §Decision/§Consequences land at IMPL; §Context at SPEC) · ADR-0045 (the split gate, re-checked at PLAN) · ADR-0041 (gRPC ALS LIFTED from the silent-ignore set) · ADR-0016 (the proto blank-import registry) · ADR-0067 (the access-log 10-operator `Record` shape) · ADR-0158 (the `grpcclient.Dialer` typed-wrapper layer).
- **Differential discipline (when 44.1 reaches the `0081` fixture):** `reference_docker_probe_bridge_network` (shared bridge + a receiver hostname reachable from BOTH containers; verify decode ran) · `reference_wire_format_both_sides_see_same_bytes` (adopt the reference `StreamAccessLogsMessage` framing verbatim — identifier-once) · `reference_concurrency_differential_release_barrier` (poll-to-converge until N entries arrive, NEVER a `time.Sleep`) · `reference_differential_run_selector` (`-run 'TestDifferential/0081'`, NOT a bare `'0081'`) · `reference_differential_break_protocol_count1` (`-count=1` on every deliberate-break AND every `-race` run) · `reference_differential_fullsuite_startup_flake` (a `subject ready: EOF` is a transient startup race — isolate-re-run) · `reference_dynamic_stat_name_charset_guard` (if any stat segment is wire/config-derived, guard through `stats.IsValidName` before `NewCounterIfAbsent`) · `reference_fuzzer_count_docs_drift` (reconcile the running `^func Fuzz` total if a fuzzer lands).

## Read first (in order)

1. THIS FILE — the cold-start router (phase 44 BRAINSTORM is DONE + LANDED; the Observability family OPEN; the next action is the 44.1 core SPEC).
2. `docs/envoy-go/phases/44-grpc-access-log/BRAINSTORM.md` — the parent BRAINSTORM (the §1–§12 design + the §10 D-ALS-* SPEC obligations + the 3-leg split + the load-bearing code/proto facts with file:line refs).
3. `docs/envoy-go/STATE.md` — the active-phase header (`phase 44 (grpc-access-log) BRAINSTORM done`, the Observability-family-opens note) + the count figures (1187 / 82 / 43 / 38 / ADR-0254, next-free ADR-0255).
4. `docs/envoy-go/ROADMAP.md` — row 44 (`grpc-access-log`, in-progress) + the `### Observability family` heading (FAMILY OPEN at phase 44).
5. `docs/envoy-go/BOOTSTRAP_PROMPT.md` — the lifecycle invariants (§4.1: STATE.md is the source of truth for "what next"; a session updates it before exiting).
6. `docs/envoy-go/DECISIONS.md` — the tail **ADR-0254** (the 43.2b as-built) + next-free **ADR-0255** (the 44.1 SPEC anchors its §Context draft).
7. The existing seams the SPEC builds on: `internal/accesslog/accesslog.go` (the `Sink` interface `:18` + the `Record` `:29`), `internal/accesslog/writer.go` (the `AsyncFileSink` pattern), `internal/filter/hcm/accesslog_emit.go` (the H1/H2 emit hooks), `internal/bootstrap/bootstrap.go` (`parseOneAccessLog` `:265`, the silent-ignore `:271`), `internal/grpcclient/grpcclient.go` (the `Dialer` `:85`/`:105` + `AuthClient` `:157`), `test/differential/fixture/fixture.go` (the `BackendKind` enum tail `:606`).
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. the worktree-hygiene, differential, and the `reference_wire_format_both_sides_see_same_bytes` / `reference_docker_probe_bridge_network` refs).
