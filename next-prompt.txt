=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-CHECKED 2026-07-13 (after the phase-61.3 IMPL landed + row 61 flipped `done`, run MECHANICALLY) — it does NOT fire. Do NOT create `stop`.** State:
- (1) prints NOTHING — row 61 `http3-downstream-listener` flipped `in-progress` → `done` at the 61.3 IMPL six-gate (ALL THREE legs 61.1/61.2/61.3 landed, ADR-0106). NO ROADMAP row is now non-`done`.
- (2) prints THREE live "candidates:" sentences — HTTP/3, xDS, and Observability ⇒ three families STAY OPEN. (`reference_sentinel_deferred_sentence_live_vs_historical`.)
- (3) prints `NEVER OPENED: gRPC`, `Runtime`, `WASM` ⇒ THREE families never opened. Operational-tooling also STAYS OPEN.

Checks (2) and (3) print ⇒ the sentinel does NOT fire.

**STATUS AS OF 2026-07-13: the phase-61.3 IMPL LANDED and ROADMAP row 61 is `done` — the entire HTTP/3 downstream-listener split (61.1 QUIC/UDP substrate + 61.2 H3 codec/HCM + 61.3 cross-side differential) is CLOSED.** The 61.3 IMPL added the differential harness's FIRST non-TCP transport (UDP/QUIC exposure + `test/helpers/h3.go` `H3RoundTrip`) + the `0104-http3-downstream-get` cross-side fixture (H3 GET→200 proven byte-stable vs `envoyproxy/envoy:contrib-v1.37.2`) + a stdlib-only `writeH3Reply` `server`/`content-length` fidelity pickup; ANCHORED ADR-0282. No banked mid-lifecycle split legs remain (no `in-progress` ROADMAP rows). Per the STANDING DIRECTIVE, the roller SELF-PICKS the next subject.

**THE ROLLER'S SELF-PICK (smallest-defensible next subject): the tracing `request_header` custom_tag source arm (Observability family).** Phase 59 (ADR-0277) landed tracing custom_tags with the LITERAL source type — the custom_tag engine (the KV-slice upsert-by-key + the OTLP `toProto` seam, `reference_tracing_custom_tag_override_builtin`). The next smallest genuine increment is adding the `request_header` custom_tag SOURCE arm (a span tag whose value is read from a named request header) — a small, well-scoped extension that reuses the whole landed engine and adds one source-resolution arm + one differential fixture. This is the smallest-defensible candidate among the open/never-opened families. **Rejected alternatives (to be FORMALIZED with rationale in the BRAINSTORM per the standing directive):** the `environment`/`metadata` custom_tag sources (bundle later — `metadata` needs a metadata-lookup path, larger); xDS SDS-rotation / SDS-validation_context (needs a reload/refetch path) or CDS/EDS/LDS/RDS/ADS (a large dynamic-config leg); HTTP/3 upstream-H3 cluster (needs a whole upstream QUIC transport socket + H3 upstream pool — large) / alt-svc / QuicProtocolOptions tuning; the never-opened gRPC / Runtime / WASM family openers (greenfield, larger than an increment on a landed engine). The BRAINSTORM MAY re-pick if a full source read shows the `request_header` arm is not in fact the smallest defensible slice — record the final pick + rejected alternatives there.

==============================================

## What THIS session does (the next-subject BRAINSTORM)

Perform the BRAINSTORM stage (lifecycle-state 0 → 1) for the roller's self-picked subject — the **tracing `request_header` custom_tag source arm** (Observability family) — using `superpowers:brainstorming` (REQUIRED before any creative/design work). This is a DOCS-ONLY stage in a FRESH worktree off master (`feedback_git_worktrees`); NO production `.go` changes. It authors `docs/envoy-go/phases/<NN>-<slug>/BRAINSTORM.md` (pick the next-free phase number — RE-DERIVE the next-free phase dir; phase 61 is the current tail) recording: the subject + why it is the smallest-defensible self-pick, the rejected alternatives (above, formalized), the design questions (the D-* list — how the reference resolves a `request_header` custom_tag; missing-header semantics; the source-type dispatch over the landed literal engine; the differential fixture shape; whether a SPLIT is needed [likely NOT — a single small arm]), and the anticipated counts. It registers the new ROADMAP row (`in-progress` per the §Schema invariant) under the Observability family (whose slug is already in the sentinel check (3) list). Land it (commit + push per `feedback_push_to_origin`) and ROLL THIS FILE to the SPEC stage.

**Honor throughout (the discipline, unchanged):**
- **Lifecycle = one stage per session.** This session: the BRAINSTORM ONLY. After it lands: roll to the SPEC.
- **Process anchors:** ADR-0044 (ADR §Decision/§Consequences land at the IMPL) · ADR-0045 (the split escape-valve, only if the slice exceeds ~15 tasks — unlikely for a single source arm) · ADR-0106 (a row flips `done` at its IMPL; a split-phase row only when ALL legs land) · `reference_roadmap_split_phase_row_done`.
- **`next-prompt.txt` IS TRACKED** (`reference_next_prompt_tracked_despite_gitignore`) — edit it inside the stage worktree and fold into the stage commit; locate commits by SUBJECT (`git log --grep`), never by position.
- **Worktree discipline** (`feedback_git_worktrees`, `feedback_subagent_worktree_detach`, `feedback_subagent_worktree_path_targeting`) — a BRAINSTORM is light, but keep the worktree hygiene; if subagent-driven, `feedback_execution_style` + `feedback_subagents_no_push`.

**ADR reservation ledger (provisional; each finalized at its IMPL in landing order):** ADR-0277 = phase-59 tracing-custom-tags-literal (LANDED) · ADR-0278 = xDS 60.1 (LANDED) · ADR-0279 = HTTP/3 61.1 (LANDED) · ADR-0280 = xDS 60.2 (LANDED) · ADR-0281 = HTTP/3 61.2 (LANDED) · ADR-0282 = HTTP/3 61.3 harness/differential (LANDED) · next-free unreserved **ADR-0283** (the next-subject phase anticipates it at its IMPL).

**Current counts (VERIFIED at the phase-61.3 IMPL against branch HEAD — RE-VERIFY at session start, `git fetch` first):** fixtures **106** (tail `0104-http3-downstream-get`) · fuzzers **55** · DECISIONS tail **ADR-0282** (next-free **ADR-0283**) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** · go.mod modules **2** (`quic-go v0.54.1` direct + `qpack v0.5.1` indirect, both confined to `internal/listener` production deps + `test/helpers` TEST deps). A BRAINSTORM is DOCS-ONLY ⇒ ALL counts UNCHANGED at this stage.

**Locating the phase-61.3 IMPL stage squash** (a router cannot name its own sha — identify by SUBJECT, never by position):
```sh
git log --oneline --grep='^phase 61.3 (http3-downstream-get-differential) IMPL' -1
```

## Read first (in order)
1. THIS FILE — the cold-start router (the next-subject BRAINSTORM is next; the HTTP/3 split 61.1/61.2/61.3 is CLOSED, row 61 `done`).
2. `docs/envoy-go/STATE.md` — the active-phase header (phase 61.3 IMPL done; NEXT = the roller's self-pick BRAINSTORM).
3. `docs/envoy-go/ROADMAP.md` — the Observability family + its LIVE deferred sentence (the `request_header`/`environment`/`metadata` custom_tag candidates); the row-registration §Schema invariant.
4. `docs/envoy-go/phases/59-tracing-custom-tags-literal/` (BRAINSTORM/SPEC/PLAN/PROGRESS) + `docs/envoy-go/DECISIONS.md` ADR-0277 — the LANDED custom_tag literal engine (the seam the `request_header` arm extends): the KV-slice upsert-by-key + the OTLP `toProto` seam (`reference_tracing_custom_tag_override_builtin`).
5. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the tracing `reference_*` traps (esp. `reference_tracing_custom_tag_override_builtin`, `reference_phase_46_tracing_split`, `reference_b3_parent_is_incoming_spanid`, `reference_tracing_upstream_cluster_framework_gap`) + the differential-harness traps for the fixture.
