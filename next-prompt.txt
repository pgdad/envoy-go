START a fresh cold-start session for envoy-go. **Nothing is in flight** — the phase-41 circuit-breakers row is COMPLETE and LANDED on `origin/master` (HEAD = `7f3c7c15`, the phase-41 IMPL squash). The next lifecycle step is a **FRESH BRAINSTORM for a NEW subject** (`superpowers:brainstorming`): **the human picks the subject** — do NOT pre-decide. Confirm the tip first via `git log --oneline -5` (HEAD `7f3c7c15` sits on the phase-41 PLAN/SPEC/BRAINSTORM docs commits + the prior substantive phase-40.3 IMPL squash `148f288`). Counts: stat surface **1163** / fixtures **76** / fuzzers **42** / BackendKind tail **36** (`BlockingHoldResponder`) / DECISIONS tail **ADR-0248**, next-free **ADR-0249**.

## The next-subject landscape (the human picks at the BRAINSTORM — do NOT pre-decide)

The **Upstream-robustness family** (opened phase 39) STAYS OPEN. After phase 41, **2** candidates remain:
- **retries + hedging** (`RouteAction.retry_policy` / `request_mirror_policies` / hedging) — the retry substrate the phase-41 `max_retries`/`retry_budget` budgets (parsed-but-deferred) would feed.
- **per-protocol connection pooling** (`Cluster.{typed_extension_protocol_options, http_protocol_options}` pool tuning) — **this is where the phase-41-DEFERRED `max_connections` + `max_pending_requests` enforcement lands** (the connection-pool/queue semantics). The reusable cross-Docker concurrency-differential pattern is captured in memory `reference_concurrency_differential_release_barrier` — those budgets fill the same way `0074` did.

Phase 39 also UNBLOCKED 3 health-gated **Load-balancing** candidates {locality-weighted LB, priority load balancing, panic thresholds}. The human picks among any of these (or a NEW family row) at the BRAINSTORM.

## What last landed (phase 41 IMPL — the durable record is `docs/envoy-go/phases/41-circuit-breakers/{SPEC,PLAN,PROGRESS,README}.md`; read those, not this summary)

Circuit breaking on the **`max_requests` keystone** (ADR-0248): a per-priority `circuitBreaker` counter struct on `Cluster` (`internal/cluster/circuitbreaker.go`) + a synchronous `TryAcquireRequest()`/`defer ReleaseRequest()` admission seam at `doH1ClusterAction`/`doH2ClusterAction` (fail-fast `503` + `upstream_rq_pending_overflow`++ + `rq_open`=1 on exhaustion; NO goroutine; byte-identical when no `circuit_breakers`). The SPEC narrowed the BRAINSTORM "core overflow trio" to `max_requests`-only per the live pins (AMEND-CB1) — the reference's fail-fast 503 comes ONLY from a request budget; `max_connections` pends + `max_pending_requests` needs the connection-pool queue envoy-go's synchronous model lacks, so cx/pending enforcement DEFERS to the future per-protocol connection-pooling row (registered emit-0 for parity). +14 stats (10 per-priority `*_open` gauges + 4 cluster overflow counters; only `circuit_breakers.default.rq_open` + `upstream_rq_pending_overflow` go LIVE). Cross-side differential `0074-circuit-breaker-max-requests` (+1 BackendKind `BlockingHoldResponder` 36, a re-armable `/__release` gate; sleepless release-barrier + poll-to-converge; sequential-per-side; 2 deliberate breaks + 20/20 flake-free). ZERO new packages/modules.

## Honor throughout (the BRAINSTORM discipline)

- **Lifecycle = one stage per session:** the BRAINSTORM next (`superpowers:brainstorming` — explore the subject/scope/split + verify the proto in the EXISTING go-control-plane v1.32.4 module cache + sketch the architecture/differential/ADR/count envelope; the SPEC follows in a later session). `feedback_git_worktrees` (any isolated work in a worktree) · `feedback_subagents_no_push` (task subagents commit locally only; the controller squashes + pushes at stage-close) · `feedback_push_to_origin` (after a clean local merge on master with tests green, push without asking).
- **ADR/process:** ADR-0044 (the §Context drafts at the SPEC, the §Decision/§Consequences bodies land IN-PLACE at the IMPL) · ADR-0045 (the split-gate) · ADR-0052 (the six-gate at the IMPL) · ADR-0080 (envoy-go's own byte-stable rejects) · ADR-0106 + `reference_roadmap_split_phase_row_done` (a split-phase row flips `done` only when ALL legs land; NO parent rollup) · ADR-0227 (`contrib-v1.37.2`).
- **At the BRAINSTORM:** `reference_docker_probe_bridge_network` (any live reference-Envoy probe runs over a shared Docker bridge network + a STRICT_DNS backend hostname; verify the decode/request path ran) · `reference_proto_roster_extraction_digits` (digit-inclusive regexes when scraping a proto/stat roster).

## Read first (in order)

1. THIS FILE — the cold-start router (nothing in flight; the next step is a FRESH BRAINSTORM for a new subject; the human picks among the 2 remaining Upstream-robustness candidates, the 3 unblocked Load-balancing candidates, or a new family row).
2. `docs/envoy-go/STATE.md` — active-phase `phase 41 (circuit-breakers) IMPL done`; the count footer (1163 / 76 / 42 / 36 / ADR-0248, next-free ADR-0249); the lifecycle routing (next-skill = a FRESH BRAINSTORM).
3. `docs/envoy-go/ROADMAP.md` — row 41 `done` + the §Upstream-robustness family heading (the family-progress note: active-HC DONE / outlier DONE / circuit breakers DONE; 2 candidates remain) + the open candidate rosters (the BRAINSTORM-subject landscape).
4. `docs/envoy-go/phases/41-circuit-breakers/{SPEC,PLAN,PROGRESS,README}.md` (the durable phase-41 record) — for the precedent shape of the NEXT BRAINSTORM/SPEC/PLAN/IMPL.
5. `docs/envoy-go/DECISIONS.md` (the ADR-0245/0246/0247 outlier triad + ADR-0248 circuit breakers + the process ADRs; next-free ADR-0249) + `docs/envoy-go/BEHAVIOR_CONTRACT.md` (the `### Cluster — load-shedding (circuit breakers)` subsection + the stat-surface block now at 1163).
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`.
