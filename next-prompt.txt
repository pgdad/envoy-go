=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage. To use it to advance the project: read `docs/envoy-go/STATE.md` for the recorded NEXT, perform that one stage (the concrete action is spelled out below), land it, and re-roll this file to the following stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, the active phase is fully landed, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action. While ANY stage remains — as NOW: the **47.2b is SPEC → PLAN → IMPL** (the SECOND-and-FINAL sub-leg of row 47 — its IMPL flips row 47 `done`; NO separate 47.2b BRAINSTORM — BRAINSTORM-47.2 already chartered both sub-legs), so tasks are NOT complete — do NOT create `stop`; perform the stage described below.
==============================================

RESUME a fresh session for envoy-go (router last updated 2026-06-29; the **phase 47.2a (`report_counters_as_deltas`) IMPL is DONE and LANDED** — squash `ae62d964`, ff'd onto master + pushed; the six-gate GREEN incl. the full **92-dir** differential [clean re-run after a transient `subject ready: EOF` startup-race flake on 3 UNRELATED fixtures — `reference_differential_fullsuite_startup_flake`], full-package `internal/statssink` `-race` clean, `go mod tidy -diff` EMPTY, and the `0090` two deliberate breaks [emit-absolute / skip-the-latch] both VERIFIED to FAIL via the NEW post-convergence stability barrier + 20/20 flake-soak; ANCHORS **ADR-0263**). The 47.2a leg lifted the `bootstrap.go:446` deltas reject (reference-parity-accept → a `StatsSinkConfig.ReportCountersAsDeltas bool`), added a per-sink `deltaState` (`map[string]uint64`) in a NEW `internal/statssink/delta.go` applied in `MetricsServiceSink.Submit`, threaded the bool through `cmd/envoy-go/main.go:199`, added the additive `FamilySum`/`Messages` receiver surface, and proved it with the `0090-stats-sink-metrics-service-deltas` cross-side EXACT delta-SUM differential. **A KEY in-session finding** (saved as `reference_delta_sink_differential_stability_barrier`): a burst-all-requests delta-sink differential CANNOT tell delta from absolute (the first flush's delta == the absolute value when all K requests land in one flush window), so the `0090` driver asserts the per-name running delta-SUM == K with a POST-CONVERGENCE STABILITY BARRIER (wait ≥2 further flushes via the new receiver `Messages()` counter; idle flushes add 0 under deltas, an absolute sink re-adds the cumulative) — without it the deliberate breaks were vacuous. **ROW 47 STAYS `in-progress`** (flips `done` at the FINAL sub-leg 47.2b IMPL per ADR-0106 + `reference_roadmap_split_phase_row_done`). The next action is the **47.2b (`emit_tags_as_labels`) SPEC** — the SECOND-and-FINAL sub-leg.

HEAD will be the phase-47.2a IMPL squash `ae62d964` atop the phase-47.2a PLAN `75c66cd7`. **Re-read the shas with `git log --oneline -6`, do NOT assume.** Confirm `git status` clean (the untracked `.claude/` is session cruft, leave it) + master == origin/master.

## What the 47.2b SPEC IS (the next stage — docs-only direct-on-master + a live probe)
**47.2b = `emit_tags_as_labels=true`** (anticipated **ADR-0264** — the next-free ADR; confirm via `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1` → expect ADR-0263). The SN-rule tag→`LabelPair` extraction: when the metrics_service `MetricsServiceConfig.emit_tags_as_labels` is true, the dotted-name tag SEGMENTS are emitted as `MetricFamily.metric[].label[]` (`LabelPair{name,value}`) pairs instead of being inlined into the family name. This is the leg that FLIPS ROW 47 `done`.

Author `docs/envoy-go/phases/47-stats-sink-metrics-service/SPEC-47.2b.md` (the 14-section shape of SPEC-47.1/47.2a). The charter:
- **LIFT** the `emit_tags_as_labels:true` strict-reject. It is NOW at `internal/bootstrap/bootstrap.go:454`–`:455` (it SHIFTED from `:449` after the 47.2a deltas-reject removal — `grep -n 'GetEmitTagsAsLabels' internal/bootstrap/bootstrap.go` to re-confirm). Replace with a parse-accept recording a NEW `StatsSinkConfig.EmitTagsAsLabels bool` (mirror the 47.2a `ReportCountersAsDeltas` precedent at `bootstrap.go:272`/`:455`/the 3 doc comments). KEEP every OTHER reject strict (non-default `histogram_emit_mode`, `stats_flush_on_admin`, non-V3, `google_grpc`, empty cluster, sibling TypeURLs).
- **The label extraction** reuses the `internal/stats/name.go` SN1–SN9 Prometheus name/label rules (ADR-0061; the `flattenToProm`-style extractor — re-verify the exact function name + line in `name.go`, the router's prior `:39` ref is STALE-until-checked). The exact emitted NAME + LABEL-KEY form is a **47.2b SPEC live-probe pin (D-MS-LABEL)** — probe `contrib-v1.37.2` with `emit_tags_as_labels:true` per `reference_docker_probe_bridge_network` to capture what the reference puts in `MetricFamily.name` vs `metric[].label[].{name,value}` (e.g. does `cluster.<name>.upstream_rq_total` become name=`cluster.upstream_rq_total` + label `{envoy.cluster_name: <name>}`, or some other split?). The refactor-shared-`name.go`-core-vs-parallel-extractor decision is **D-MS-LABEL-REUSE** (a PLAN/IMPL call).
- **The transform placement** parallels 47.2a: the label-emitting path is sink-parameterized (a `MetricsServiceConfig.emit_tags_as_labels`-driven mapping variant), composing on the `internal/statssink` mapping/sink. Consider the deltas+labels interaction (both knobs true) — likely accepted-and-composed but differential-pinned only if the probe finds a reason (forward from 47.2a §2).
- **The differential:** a NEW `0091-stats-sink-metrics-service-labels` cross-side EXACT fixture (fixtures **92 → 93**) asserting the emitted `LabelPair`s on the deterministic subset (clone the AUTHORITATIVE `0089`/`0090` driver — two per-side receivers + hard `Close()`; the receiver likely needs a label-aware surface, a PLAN call; register in `runner_test.go`). Assert the aggregated label payload, NOT framing (`reference_streaming_sink_differential_framing`).
- **Counts at the 47.2b SPEC** (docs-only — UNCHANGED): stat **1200** / fixtures **92** / fuzzers **50** / BackendKind **38** / DECISIONS **ADR-0263** (next-free **ADR-0264**). Anticipated at the 47.2b IMPL: fixtures **93** (`0091`), DECISIONS **ADR-0264**, **ROW 47 FLIPS `done`**.

Execute the §11 live pins IN-SESSION against `contrib-v1.37.2` per `reference_docker_probe_bridge_network` (a shared bridge; an h2c driver-owned MetricsService receiver dumping per-family `name` + `label[]`); anchor the **ADR-0264 §Context draft** (§13, body lands at the 47.2b IMPL per ADR-0044). Docs-only direct-on-master (the family-row precedent). spec-document-reviewer pass before landing (the 47.2a SPEC precedent). After it lands, ROLL THIS FILE to the **47.2b PLAN**.

## What 47.2b PLAN + IMPL are (the stages AFTER the SPEC)
- **47.2b PLAN** = a TDD spine (the 9-task PLAN-47.2a shape): the bootstrap reject-lift + `EmitTagsAsLabels` field; the label-extraction mapping variant (TDD against the SN-rule pins); the sink/main thread; the receiver label surface; the `0091` fixture (registered in `runner_test.go` — the C1 vacuous-green guard from 47.2a); the deliberate breaks + flake + full-package `-race`; the full **93-dir** differential + six-gate + ADR-0264 body. plan-document-reviewer pass. Docs-only direct-on-master.
- **47.2b IMPL** = subagent-driven (`feedback_execution_style` · `superpowers:subagent-driven-development`) in a FRESH worktree off master (`feedback_git_worktrees`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit + cleans leak files + re-runs the six-gate + the FULL-package `-race` + does the `0091` deliberate breaks ITSELF (`-count=1`; `-run 'TestDifferential/0091'` NEVER bare) + squashes + pushes at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). **47.2b IMPL FLIPS ROW 47 `done`** (per ADR-0106 + `reference_roadmap_split_phase_row_done`); the Observability family — check the ROADMAP for whether any row remains after 47, else the TERMINATION SENTINEL may apply.

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** The next stage is the 47.2b SPEC (lifecycle-state 0/1 → for the 47.2b sub-leg; the BRAINSTORM was the shared 47.2 one). After it lands, ROLL THIS FILE to the 47.2b PLAN.
- **SPEC/PLAN = docs-only direct-on-master** (the family-row precedent); the IMPL is subagent-driven in a worktree.
- **Process anchors:** ADR-0106 (per-leg rows — row 47 flips `done` at the 47.2b IMPL) · ADR-0045 (sub-split — 47.2b is the final no-split sub-leg) · ADR-0044 (ADR §Decision+§Consequences-at-IMPL — ADR-0264 §Context DRAFTED at the 47.2b SPEC §13, body at the 47.2b IMPL) · ADR-0080 (strict-reject — 47.2b LIFTS the `emit_tags_as_labels` reject [`bootstrap.go:454`], KEEPS the rest) · ADR-0061 (the SN1–SN9 Prometheus name/label rules — the tag-extraction substrate) · ADR-0064 (the `stats_sinks[]` silent-ignore — 47.1 LIFTED the metrics_service sink) · ADR-0059 (the in-tree `Registry`/`Walk`/`Freeze`) · ADR-0060 (histograms deferred — moot) · ADR-0262 (the 47.1 anchor) · ADR-0263 (the 47.2a deltas anchor) · ADR-0264 (ANCHORED at the 47.2b SPEC §13).
- **Differential discipline** (for the 47.2b `0091` fixture): clone the AUTHORITATIVE `0089`/`0090` driver (two per-side receivers + hard `Close()` — `reference_periodic_sink_differential_two_receivers`); **a delta/label sink differential needs a break that the burst-snapshot can SEE** — for labels, assert the emitted `LabelPair` set (a property absolute-name-inlining does NOT satisfy), and if any aggregation-over-flushes is involved re-apply the post-convergence stability barrier (`reference_delta_sink_differential_stability_barrier`); **REGISTER the driver in `test/differential/runner_test.go`** (else vacuous green — `reference_differential_run_selector`); `-run 'TestDifferential/0091'` NEVER bare; `-count=1` on every break + `-race` (`reference_differential_break_protocol_count1`); the Docker bridge + decode-ran proof (`reference_docker_probe_bridge_network`); the gRPC metrics receiver is driver-owned NOT a BackendKind (`reference_differential_grpc_receiver_driver_owned`); assert the aggregated payload NOT framing (`reference_streaming_sink_differential_framing`); the live reference is the wire truth (`reference_wire_format_both_sides_see_same_bytes`); one fixture dir = ONE runner branch (`reference_differential_fixture_dispatch_constraint`); the Flusher + sink writer goroutine + per-sink state are background mutators ⇒ FULL-package `-race` (`reference_full_suite_race_after_background_mutator`); the full-suite `subject ready: EOF` startup-race flake is isolate-re-run + full-re-run, NOT a regression (`reference_differential_fullsuite_startup_flake`).

## Read first (in order)
1. THIS FILE — the cold-start router (the 47.2a IMPL is DONE + LANDED `ae62d964`; row 47 `in-progress`; the next action is the 47.2b SPEC).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 47.2a IMPL done`) + the recorded NEXT (the 47.2b SPEC) + the counts (1200 / 92 / 50 / 38 / ADR-0263).
3. `docs/envoy-go/phases/47-stats-sink-metrics-service/BRAINSTORM-47.2.md` — the shared 47.2 brainstorm that chartered BOTH sub-legs (47.2b = `emit_tags_as_labels`, anticipated ADR-0264).
4. `docs/envoy-go/phases/47-stats-sink-metrics-service/SPEC-47.2a.md` + `PLAN-47.2a.md` — the 47.2a charter + spine (the SHAPE to mirror for 47.2b: the reject-lift + `StatsSinkConfig` bool + the mapping variant + the `009N` differential + the receiver surface).
5. `docs/envoy-go/DECISIONS.md` — **ADR-0263** (the 47.2a anchor; ADR-0264 §Context drafts at the 47.2b SPEC §13).
6. The as-built code 47.2b modifies: `internal/bootstrap/bootstrap.go` (the `emit_tags_as_labels` reject `:454`–`:455` to LIFT; `StatsSinkConfig` `:272`; the append `:455`+), `internal/statssink/mapping.go` (`snapshot` `:22` — the name/label emit arms the label variant changes), `internal/statssink/delta.go` (the 47.2a per-sink transform precedent), `internal/statssink/sink.go` (`MetricsServiceSink` — where a label-mode would thread, mirroring `delta *deltaState`), `internal/stats/name.go` (the SN1–SN9 extractors — re-verify the function name/line), `cmd/envoy-go/main.go:199` (the sink construction).
7. `test/fixtures/0090-stats-sink-metrics-service-deltas/{driver/driver.go,envoy*.yaml,expectations.yaml}` (the FRESHEST differential template `0091` clones — two per-side receivers + hard `Close()` + the NEW `Messages()` stability barrier + the `FamilySum` surface) + `test/helpers/metricsservice/metricsservice.go` (the receiver to extend with a label-aware surface) + `test/differential/runner_test.go` (`:117` the 0090 import — add the 0091 sibling).
8. `docs/envoy-go/ROADMAP.md` — row 47 (`stats-sink-metrics-service`, `in-progress`) + the Observability family-progress note (47.2a deltas DONE; 47.2b tags-as-labels REMAINING — the leg that flips row 47 done).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_subagents_no_push`, `feedback_subagent_autocommit_claudemd`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_detach`/`_path_targeting`, `reference_roadmap_split_phase_row_done`, `reference_stats_sink_metrics_service_probe`, `reference_periodic_sink_differential_two_receivers`, `reference_delta_sink_differential_stability_barrier`, `reference_differential_grpc_receiver_driver_owned`, `reference_streaming_sink_differential_framing`, `reference_docker_probe_bridge_network`, `reference_full_suite_race_after_background_mutator`, `reference_differential_break_protocol_count1`, `reference_differential_run_selector`, `reference_differential_fullsuite_startup_flake`, plus the differential-discipline references above).
