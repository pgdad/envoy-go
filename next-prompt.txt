START a fresh cold-start session for envoy-go — **phase 32.2 (`network-filter-redis-commands-and-stats`) PLAN is DONE; the next lifecycle step is the 32.2 IMPL** (`superpowers:subagent-driven-development`, executing the now-APPROVED 12-task PLAN task-by-task). The 32.2 PLAN LANDED (`docs/envoy-go/phases/32.2-network-filter-redis-commands-and-stats/PLAN.md`; plan-document-reviewer APPROVED iteration 2 — the one blocking issue [Task 3 Step 5 missed the 7 `resp_test.go` `decodeRequest` callers when changing the 3→4-value signature] fixed in the loop) and RESOLVED every PLAN-time D-question: D-S32.2-1 (a sorted `[]string` golden 180-name list + a derived `map[string]struct{}` lookup; lower-case ONCE at dispatch), D-S32.2-4 (`decodeRequest` extended to return `args [][]byte` — the cleaner no-double-parse path, option (a), refining the SPEC §3.1 "resp.go UNCHANGED" anticipation), D-S32.2-5 (a `commandVerdict{action,reply,closeAfter,statCmd}` struct; QUIT sets `closeAfter`), D-S32.2-6 (the NEW per-command/splitter/gauge assertions via `/stats/prometheus` label-aware; the 32.1 6 counters stay flat-`/stats`-scraped), D-S32.2-7 (two driver fields `refHeld`/`subjHeld` for the `cx_active` held-open arm). The PLAN squash LANDED at `dd340ff` on origin/master (squash-merged from `phase-32.2-network-filter-redis-commands-and-stats-plan` + pushed by the controller per `feedback_push_to_origin`; the PLAN worktree was cleaned up + the branch deleted at stage-close). At the PLAN commit ROADMAP sub-row 32.2 STAYS `in-progress` (the PLAN does NOT change the row status — it was flipped at the SPEC; parent row 32 STAYS `in-progress`; the `→ done` ROLLUP is the 32.2 IMPL's, ATOMIC with the parent). This docs-only SHA-fill trails the PLAN squash as the live master tip; confirm via `git log --oneline -1`.

The 32.2 IMPL's job is to EXECUTE the 12-task PLAN — NOT new design (the SPEC resolved all D-P-questions; the PLAN resolved all D-S-questions + the FINAL ADR-0045 split-gate re-check). The PLAN is the authoritative task list; the SPEC is the authoritative design source. Run subagent-driven (`superpowers:subagent-driven-development` per `feedback_execution_style`; fresh subagent per task + two-stage review) per the 32.1 IMPL / 29.x IMPL precedent.

## What this session does — the phase-32.2 IMPL

Run `superpowers:subagent-driven-development` scoped to the **phase-32.2 IMPL** (`network-filter-redis-commands-and-stats`). The IMPL executes the PLAN's 12 bite-sized TDD tasks (write failing test → run-it-fails → minimal impl → run-it-passes → commit), dispatching a fresh subagent per task with a two-stage review (code-reviewer between tasks). The PLAN-anchored task spine:

  1. First-action baselines/anchors gate (re-pin fixtures **58** + fuzzers **40** + stat surface **546** + BackendKind tail **32** + DECISIONS tail **ADR-0230** [next-free **ADR-0231**] + the §12.1 180-command table source re-check + a clean `go mod tidy` [ZERO new dep], against the live IMPL-session tip).
  2. `commands.go`: the static 180-command `supportedCommandList` golden + `supportedCommands` map + `commandSupported` + `TestCommandRoster_MatchesUpstream` (golden 180) + `TestCommandRoster_AllValidNames` (IsValidName by construction).
  3. `commands.go` + `resp.go`: the `commandVerdict` + `classify` dispatch + the extended local-reply set (ECHO/TIME/QUIT/HELLO) + the `decodeRequest` arg exposure (D-S32.2-4 — extend the signature; **update ALL callers incl. the 7 `resp_test.go` sites**) + the QUIT close signal (D-S32.2-5) + the arity/error helpers. (NOTE the Task 3 transient shim that keeps the package green before Task 6 wires the real pump — or combine Tasks 3+6 into one green step.)
  4. `stats.go`: the EAGER 540 per-command counters + the 2 `splitter.*` + 3 `REDIS_CLUSTER_STATS` (eager) + the per-command/splitter inc accessors (update the 32.1 `len(rs.counters)==6` test to `11`).
  5. `stats.go`: the 2 lifecycle gauges' inc/dec accessors (`cx_active`/`rq_active`) + the `downstream_cx_protocol_error` accessor.
  6. `filter.go`: the `Handle` pump wiring — the `classify` switch (unknown→`splitter.unsupported_command`+`-ERR unknown command`, bad-arity→`splitter.invalid_request`+`-invalid request`, valid→`command.<cmd>.total`+proxy+success/error) via a `serveRequest` helper (defer-balanced `rq_active`), the `cx_active`/`rq_active` gauge inc/dec, the `cx_protocol_error` increment, the QUIT close, the `doc.go` 32.2 forward-pointers resolved.
  7. `internal/stats/name.go`: the `redis.` LABEL-HOISTED arm (`envoy_redis_<leaf>{envoy_redis_prefix="<sp>"}` — the mongo `.rbac.` single-label hoist).
  8. `FuzzRESPDecode` (the 41st — no-panic / no-mutation / bounded-allocation over `decodeRequest`+`decodeReply`).
  9. `TCPRedisResponder` reply-table extension (`$-1` GET-miss [keyed on the first arg "nope"], `:1` INCR/DEL) in `runner_test.go::redisRespondLoop`.
  10. `0055` driver: the command-matrix arms (GET-hit/miss, INCR, DEL, ECHO valid/wrong-arity, QUIT, HELLO-3, HELLO-options, UNKNOWN, PING-with-arg) + the per-command/splitter stat assertions via `/stats/prometheus` (the kafka `0053` `canonicalize` mechanics; **the UNKNOWN arm confirms D-S32.2-2 byte-stable**).
  11. `0055` driver: the `downstream_cx_active` held-open gauge arm (==1 live both sides; the `refHeld`/`subjHeld` fields) + the `rq_active`/buffered quiesced-zero assertions + the R6 deliberate-break liveness recording (`-count=1`).
  12. Completion bundle: ADR-0229 §Decision/§Consequences 32.2 half (PARTIAL → ACCEPTED) in-place (ADR-0044) + the BEHAVIOR_CONTRACT 32.2 bundle + STATE.md + ROADMAP **parent row 32 + sub-row 32.2 ATOMIC ROLLUP** (`in-progress → done` / `in-progress → done`) + next-prompt.txt + the six-gate.

The full task detail (complete code blocks, exact file paths, test commands + expected output, the R6 break protocol) is in the PLAN — read it FIRST and follow it task-by-task.

## Key PLAN/SPEC pins to carry into the IMPL (the PLAN is the task list; the SPEC is the design source — read both)

- **The 180-command table** (SPEC §12.1; PLAN Task 2 has the exact Go literal) — the EXACT enumerated list (8 iterated `SupportedCommands` groups = 180). The golden for the `commands.go` `supportedCommandList` + `TestCommandRoster_MatchesUpstream`. The `.`-containing names (`bf.add`…, `info.shard`) flatten to `command_bf_add`/`command_info_shard` (dot→underscore) and pass IsValidName.
- **success/error semantics** (SPEC §4.2 / §12.2; PLAN Task 6) — single-server: `total` at dispatch; `success` on ANY decoded reply (incl. `-ERR`, `$-1`, `*-1`); `error` ONLY on a transport/pool failure (Send/decode error, conn closed before reply, unresolvable cluster). Fragmented MGET/MSET DEFERRED — proxied single-server.
- **The 4 gauges** (SPEC §4.4 / §12.3; PLAN Tasks 5/11) — only `downstream_cx_active` (Handle entry/exit) + `downstream_rq_active` (request received/reply sent) are filter-driven; the 2 `*_bytes_buffered` gauges stay exist-at-0 (framework-buffer-managed upstream coverage boundary). `downstream_cx_active` is the LIVE-mirrored gauge (held-open arm); the rest quiesced-zero.
- **The local-reply set** (SPEC §3.3 / §12.4; PLAN Task 3 `classify`) — ECHO (echoes arg; arity≠2→`splitter.invalid_request`) + QUIT (`+OK\r\n`+close) deterministic differential arms; TIME (wall-clock) NON-deterministic → UNIT-test shape-only; HELLO splits — `>2`-args→options-error, non-2/non-numeric proto (incl. `HELLO 3`)→NOPROTO, BUT `HELLO 2`/no-arg PROXIED (`command.hello.*` + forwarded; NOT a differential arm). PING/AUTH/ECHO/TIME/QUIT emit NO `command.*`; HELLO error-paths emit no `command.hello.*`.
- **The `redis.` arm** (SPEC §5; PLAN Task 7) — SINGLE-label hoist (`envoy_redis_<rest>{envoy_redis_prefix="<sp>"}`); the command name STAYS in the metric name (NOT a label). SHAPE-detection (dot-free `<prefix>`) via `strings.CutPrefix(internal, "redis.")`. Contrast the kafka INLINE arm + the mongo MULTI-label arm.
- **Stat surface 546 → 1091** (SPEC §7): 546 + 2 splitter + 3 REDIS_CLUSTER_STATS + (180×3=540) = 1091. Latency histograms (180) + `*_fault` counters (360) NOT counted (ADR-0060/faults).
- **NO new ADR / NO new fixture dir / NO new BackendKind / framework-zero-touch** (SPEC §11/§8.5/§8.4/§2.6): ADR-0229 body completes in-place (DECISIONS tail STAYS ADR-0230); `0055` extended (no new dir, fixtures STAY 58); `TCPRedisResponder` (32) reply-table extended (NOT re-numbered); the seam (`upstreampool.go`) + `internal/filter/network/` framework UNTOUCHED (only `redisproxy/` + `internal/stats/name.go` + test surface change).

## Honor throughout

- `feedback_execution_style` (the 32.2 IMPL runs subagent-driven; fresh subagent per task + two-stage review) · `feedback_git_worktrees` (work in a worktree) · `feedback_subagents_no_push` (subagents commit LOCAL-ONLY; the controller squash-merges + pushes at stage-close) · `feedback_push_to_origin` (after the 32.2 IMPL six-gate is green, push to origin/master) · `feedback_pertask_gofmt_lint` (each subagent task runs `gofmt -l` + `golangci-lint run` on touched packages) · `reference_differential_break_protocol_count1` (deliberate-break checks use `go test -count=1`) · `feedback_subagent_worktree_detach` (a deliberate break can detach HEAD; restore via `git restore`, the controller re-verifies the branch each task).
- ADR-0229 (the redis_proxy filter umbrella — the 32.2 half completes the PARTIAL body → ACCEPTED) · ADR-0230 (the upstream-pool seam — ACCEPTED; consumed UNCHANGED) · ADR-0044 (§Decision+§Consequences-at-IMPL) · ADR-0079 (two-step factory) · ADR-0080 (byte-stable rejects) · ADR-0052 (atomic landing + six-gate) · ADR-0045 (split-gate UNCONSUMED) · ADR-0106(d) (parent-row ROLLUP atomically with 32.2) · ADR-0060 (histograms deferred) · ADR-0227 (the contrib pin).
- Invariants that bite the 32.2 IMPL: `reference_dynamic_stat_name_charset_guard` (satisfied BY CONSTRUCTION here — static table; the `TestCommandRoster_AllValidNames` test pins it) · `reference_differential_asserter_dispatch` (the `0055` command matrix uses `StatsAsserter`; prove each new assertion LIVE) · `reference_differential_fixture_dispatch_constraint` (the command matrix extends the EXISTING `0055` cross-side dir; no new dir) · `reference_wire_format_both_sides_see_same_bytes` (the downstream RESPONSE bytes are the proof; adopt upstream framing verbatim — the UNKNOWN-command wording is captured live at Task 10) · `reference_close_direction_framework_gap` (AMEND-R9 — NO close-direction counters at 32.2) · `reference_docker_probe_bridge_network` (the `0055` differential boots the contrib reference in Docker; if Docker is unavailable, run the unit/fuzz tests + `go build ./test/...` and DEFER the full `0055` run to the six-gate, recording the deferral honestly).

## Read first (in order)

1. THIS FILE — the phase-32.2-IMPL cold-start.
2. `docs/envoy-go/phases/32.2-network-filter-redis-commands-and-stats/PLAN.md` — **THE 32.2 PLAN** (the authoritative task list — the 12-task TDD spine with complete code blocks, exact file paths, test commands + expected output; the PLAN-time D-question dispositions; the ADR-0045 split-gate FINAL re-check; the File Structure; the acceptance checklist).
3. `docs/envoy-go/phases/32.2-network-filter-redis-commands-and-stats/SPEC.md` — **THE 32.2 SPEC** (the authoritative design source: the command set §3, the roster §4, the `redis.` arm §5, the stat surface §7, the differential matrix §8, the fuzzer §9, the empirical-pin block §12, the task spine §14, the D-questions §13).
4. `docs/envoy-go/phases/32.1-network-filter-redis-upstream-pool-seam-and-codec/SPEC.md` + `PLAN.md` + `PROGRESS.md` — the 32.1 as-built (the codec §3.5, the seam §4, the 10-name roster §7, the Handle pump §3.7; the as-built deviations).
5. `docs/envoy-go/STATE.md` — active-phase `phase 32.2 PLAN done`; lifecycle-state → the 32.2 IMPL; next-skill `superpowers:subagent-driven-development`; counts (546 stats / 58 fixtures / 40 fuzzers / BackendKind tail 32 / DECISIONS tail ADR-0230, next-free ADR-0231).
6. `docs/envoy-go/ROADMAP.md` — parent row 32 `in-progress`; sub-row 32.1 `done`; sub-row 32.2 `in-progress`.
7. `docs/envoy-go/DECISIONS.md` — **ADR-0229** (PARTIAL — the 32.2 half completes at THIS 32.2 IMPL) + **ADR-0230** (ACCEPTED — the seam record) + ADR-0044 / ADR-0079 / ADR-0080 / ADR-0052 / ADR-0045 / ADR-0106 / ADR-0060.
8. `docs/envoy-go/phases/29.2-network-filter-mongo-responses-and-correlation/SPEC.md` + `PLAN.md` — the per-sub-phase IMPL→ROLLUP precedent (the gauge quiesced-point + held-arm design; the ZERO-framework-touch shape; the completion-bundle + parent-ROLLUP task).
9. `internal/filter/network/redisproxy/` — the as-built 32.1 source the 32.2 IMPL extends (filter.go + config.go + resp.go + commands.go + stats.go + doc.go + the `*_test.go` files); the seam `internal/filter/network/upstreampool.go` (UNCHANGED); `internal/stats/registry.go` (`NewCounterIfAbsent`/`NewGaugeIfAbsent`/`IsValidName`); `internal/stats/name.go` (the kafka INLINE + mongo `.rbac.` MULTI-label arms = the contrast for the new redis SINGLE-label hoist); `internal/stats/gauge.go`/`counter.go` (the `Inc`/`Dec`/`Add`/`Load` API).
10. `test/differential/fixture/fixture.go` (the `StatsAsserter` + `TCPRedisResponder` BackendKind 32) + `test/differential/runner_test.go::redisRespondLoop` (~line 2433 — the responder reply LOOP the table extends) + `test/fixtures/0055-redis-roundtrip/driver/driver.go` (the as-built cross-side driver the command matrix extends — the `respArray`/`respBulk`/`inline` builders + the `AssertStats`/`scrapeStats` mechanics + the R6 break-liveness recording) + `test/fixtures/0053-kafka-requests/driver/driver.go` (the `scrapeKafkaStats`/`canonicalize`/`httpGet` `/stats/prometheus` label-aware precedent).
11. An existing network-filter fuzzer (`internal/filter/network/kafkabroker/fuzz_test.go`) — the `FuzzRESPDecode` shape precedent.
12. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`.

## Counts at master tip (phase 32.2 PLAN — the 32.2 IMPL session opens from here)

- stat surface **546** (→ **1091** at the 32.2 IMPL: +2 splitter + 3 REDIS_CLUSTER_STATS + 540 per-command [180×3]; latency histograms + `*_fault` counters NOT counted per ADR-0060/faults); differential fixtures **58** (tail `0056-redis-boot-reject`; `0055` EXTENDED at the 32.2 IMPL — NO new dir); fuzzers **40** (tail `FuzzKafkaDecode`; → **41** `FuzzRESPDecode` at the 32.2 IMPL); BackendKind tail **32** (`TCPRedisResponder`; reply-table extended at 32.2 IMPL, NOT re-numbered); DECISIONS.md tail **ADR-0230** (ACCEPTED; next-free **ADR-0231** — NO new ADR at 32.2; ADR-0229 body completes in-place).
- conformance h2spec 53/53 + proxy-wasm 10/10 (image-independent; UNAFFECTED by phase 32 — touches no HTTP/h2/proxy-wasm path).
- reference pin: `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227).
- Toolchain: Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); go-control-plane `/envoy` v1.32.4 (ADR-0008) + `/contrib` v1.32.4 (phase-31 dep). redis_proxy is CORE `/envoy` (D32-1) → ZERO new go.mod dep at 32.2 IMPL.
- §9 Network-filters family: rows 26/27/28/29/30/31 DONE + **row 32 `redis_proxy` IN-PROGRESS (parent SPEC done; 32.1 IMPL done; sub-row 32.1 `done`; 32.2 SPEC done; 32.2 PLAN done; sub-row 32.2 `in-progress`)**. After phase 32 phase-done (at the 32.2 IMPL ROLLUP) the candidate {thrift} remains (the last §9 candidate — reuses the 32.1 upstream-pool seam).
