=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-CHECKED 2026-07-12 (at the phase-59 SPEC, run MECHANICALLY) — it does NOT fire. Do NOT create `stop`.** The phase-59 SPEC landed (docs-only; row 59 STAYS `in-progress`); the state after it:
- (1) prints `NOT DONE: row 59` — row 59 (`tracing-custom-tags-literal`) STAYS `in-progress` (a row flips `done` only at its IMPL six-gate per ADR-0106). This ALONE blocks `stop`. (UNCHANGED from the phase-59 BRAINSTORM state — a SPEC is docs-only and does not flip the row.)
- (2) prints EXACTLY ONE live "candidates:" sentence — the Observability family's `remaining deferred (not-yet-chartered) candidates: OTLP-metrics stats sink + tracing custom_tags/spawn_upstream_span/http_service/force-trace.` (UNCHANGED at the SPEC; `custom_tags` NARROWS to its non-literal types only at the phase-59 IMPL). ⇒ Observability STAYS OPEN. (One live match, `reference_sentinel_deferred_sentence_live_vs_historical`.)
- (3) prints `NEVER OPENED: HTTP/3`, `gRPC`, `xDS`, `Runtime`, `WASM` ⇒ FIVE families never opened; Operational-tooling also STAYS OPEN.

All three checks print ⇒ the sentinel does NOT fire.

**STATUS AS OF 2026-07-12: phase 59 (`tracing-custom-tags-literal`) is CHARTERED, BRAINSTORMED, and SPEC'd — the SPEC landed (docs-only; row 59 STAYS `in-progress`, lifecycle-state 1 → 2). The next stage is the phase-59 PLAN.** The SPEC disposed all ten D-CT-* questions: the four empirical arms were PINNED by LIVE probes against `envoyproxy/envoy:contrib-v1.37.2` (fresh container per arm). The load-bearing SPEC finding — **D-CT-PRECEDENCE CONTRADICTED the BRAINSTORM**: the reference OVERRIDES a built-in on a custom-tag key collision (last-write-wins), NOT append, so `BuildServerSpan` must UPSERT-by-key (else envoy-go's OTLP `toProto` duplicates the key). ADR-0277 §Context is DRAFTED in the SPEC (§13); §Decision/§Consequences land at the IMPL (ADR-0044). The change is a literal-parse + six reject arms in `NewConfig` (three type-DEPARTURE + three PGV-parity structural), a `CustomTags []KV` field on `TracingConfig`, the upsert append in `BuildServerSpan`, two `accesslog_emit.go` call-site threadings, ONE new OTLP fixture (`0102`), a `FuzzHCMConfigParse` seed, and the doc reconciliation.

**Current counts (verified at the phase-59 SPEC, on the frozen HEAD; UNCHANGED — a SPEC is docs-only):** fixtures **103** (tail `0101-stats-sink-graphite`) · fuzzers **54** · DECISIONS tail **ADR-0276** (next-free **ADR-0277**) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** · new Go packages **0** · new go.mod modules **0**. RE-VERIFY against the actual master tip at session start (`git fetch` first).

**Locating the phase-59 SPEC stage squash** (a router cannot name its own sha — identify by SUBJECT, never by position):
```sh
git log --oneline --grep='^phase 59 (tracing-custom-tags-literal) SPEC' -1
```

**THE NEXT ACTION: the phase-59 PLAN (lifecycle-state 2 → 3; row 59 stays `in-progress`).**

==============================================

## What THIS session does (the phase-59 PLAN)

Perform the PLAN for phase 59 — docs-only (a PLAN makes NO production `.go` changes), in a FRESH worktree off master (`.worktrees/phase-59-plan`, branch `phase-59-tracing-custom-tags-literal-plan`, per `feedback_git_worktrees`). The PLAN is a MECHANICAL TDD decomposition of SPEC §10 — **no empirical question remains** (all ten D-CT-* were disposed at the SPEC). Its job: turn the SPEC's ~11-task outline into an ordered, self-contained TDD spine (`superpowers:test-driven-development` + `superpowers:writing-plans`), RE-DERIVE every `file:line` against the actual master tip (`feedback_brief_citations_not_evidence` — the SPEC's citations are a starting point, NOT evidence), and scaffold `PROGRESS.md`. Then ROLL THIS FILE FORWARD to point at the phase-59 IMPL and land it (commit + push per `feedback_push_to_origin`).

**The TDD spine to decompose (from SPEC §10 — RE-READ it; the PLAN orders + sizes these):**
1. **`config.go` parse arms + `CustomTags` field + provider-neutral threading** — replace the wholesale reject (`config.go:82-83`) with the literal-parse + six reject arms (SPEC §3.1); add `TracingConfig.CustomTags []KV` (`config.go:24-32`); restructure the provider switch (`config.go:108-115`) to set `CustomTags` after the switch (NO change to `parseOTel`/`parseZipkin`); add the `tracingv3` import. `config_test.go`: accept a literal (assert on parsed `CustomTags`); reject each of the 6 arms with its ADR-0080-distinct substring; `-count=1` liveness break per reject row (or an isolating break confirming WHICH fires — `reference_deliberate_break_wrong_assertion`).
2. **`span.go` upsert append + param** — add the `customTags []KV` param to `BuildServerSpan` (`span.go:64`) + the `upsertAttr` helper (SPEC §3.3, last-write-wins matching the reference OVERRIDE); `span_test.go`: an append case + an override/collision case (ONE KV, the override value). Update the 6 `span_test.go` call sites (`:60,:151,:178,:193,:207,:283`) + the `zipkin_test.go:88` call site to the new signature.
3. **Zipkin encoder unit test** — a span with a literal tag encodes into the Zipkin `tags` map (`zipkin.go:87-94`), node_id/zone still dropped.
4. **Call-site threading** — `accesslog_emit.go:55`/`:116` pass `f.tracingConfig.CustomTags` (SPEC §3.4; nil-safe via the exporter⇒tracingConfig invariant).
5. **New OTLP fixture `0102-tracing-custom-tags-literal`** — cloned from `0087` (do NOT mutate `0087`/`0088`, `reference_differential_fixture_dispatch_constraint`); a NON-colliding literal tag; assert the `{key,value}` span attribute both sides by KEY with `Errorf` (`reference_fatalf_makes_assertions_unreachable`); `BackendCount` ≥ 1; prove the assertion LIVE via a `-count=1` break (`reference_differential_break_protocol_count1`; confirm WHICH assertion fires).
6. **`FuzzHCMConfigParse` seed** — one `custom_tags` seed via `mkHCM` (`fuzz_test.go:25`/`:27`); reconcile actual `^func Fuzz` == documented == **54** before AND after (`reference_fuzzer_count_docs_drift`).
7. **BEHAVIOR_CONTRACT edits** — `:686` (strict-reject roster) + `:739` (Zipkin deferred bullet), SPEC §9.
8. **Verify** — six-gate (gofmt / golangci-lint / go vet / build / `go mod tidy -diff` / full-package `-race` on `internal/tracing` + `internal/filter/hcm`, `reference_detrand_race_catches_protojson_value_substring` / `reference_full_suite_race_after_background_mutator`) + the full **104-dir** differential (byte-stable except `0102`).
9. **ADR-0277 body** (§Decision/§Consequences on the SPEC §13 §Context) + **STATE** + **ROADMAP** (row 59 → `done` per ADR-0106; the LIVE deferred sentence NARROWS `custom_tags` → the non-literal types — re-run the check-(2) grep, EXACTLY ONE live "candidates:" match, `reference_sentinel_deferred_sentence_live_vs_historical`) + **router roll**.

**The PLAN's job** — order these into a per-task TDD sequence (each a red→green with a liveness break where load-bearing), decide any task splits/merges (SPEC anticipates folding task 4 into task 2; the accept vs reject rows of task 1 may split), assign the ADR-0045 disposition (SINGLE FLAT ROW, escape-valve UNCONSUMED), RE-DERIVE every `file:line` against the master tip, and scaffold `PROGRESS.md`. Then ROLL THIS FILE FORWARD to point at the phase-59 IMPL and land it (commit + push per `feedback_push_to_origin`).

## Honor throughout (the discipline, unchanged)
- **Lifecycle = one stage per session.** This session: the phase-59 PLAN ONLY. After it lands: the phase-59 IMPL (subagent-driven).
- **Process anchors:** ADR-0044 (ADR §Context drafted at the SPEC, §Decision/§Consequences at the IMPL) · ADR-0045 (split gate ~15 tasks) · ADR-0080 (distinct per-arm substrings) · ADR-0106 (a row flips `done` at its IMPL six-gate). Next-free ADR is **ADR-0277**.
- **Subagent-driven execution** for the IMPL stage (`feedback_execution_style`, `feedback_subagent_autocommit_claudemd`, `feedback_subagents_no_push`): subagents commit LOCALLY only; the controller squashes at stage-close and pushes. Docs-only BRAINSTORM/SPEC/PLAN stages may be controller-authored.
- **`next-prompt.txt` IS TRACKED** — edit it inside the stage worktree, fold into the stage commit; locate commits by SUBJECT (`git log --grep`), never by position (`reference_next_prompt_tracked_despite_gitignore`).
- **Deferred maintenance (fold into the next `udp.go`/statssink-driver-touching row — carried from the phase-57 final review; phases 58 AND 59 touch NEITHER so these are STILL OPEN):** F-1 the 0101 driver's counter tag snapshot records the EXPECTED map; F-2 `udp.go` `udpWriter` doc comment names two sinks (three embed it now); F-3 BEHAVIOR_CONTRACT 0101 note says "`differential.HostGatewayIP` helper" where the driver uses its LOCAL duplicate. (A tracing row does NOT touch `udp.go`; these ride until the OTLP-metrics or another statsd/graphite/dog_statsd sink row reopens.)

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-59 PLAN is next; the subject is PICKED — tracing `custom_tags` literal; all D-CT-* disposed at the SPEC).
2. `docs/envoy-go/phases/59-tracing-custom-tags-literal/SPEC.md` — the disposed D-CT-* (§1), the change shape (§3: parse arms §3.1 / field+threading §3.2 / the UPSERT append §3.3 / call-site threading §3.4), the reject roster (§6), the fixture decision (§8), the test plan / per-task outline (§10), the SPEC-time probe evidence (§11), the RE-DERIVED edit-site roster (§12), and the ADR-0277 §Context draft (§13).
3. `docs/envoy-go/phases/59-tracing-custom-tags-literal/BRAINSTORM.md` — the mission framing (§1), the design decisions (§2), the edit-site roster (§6), the ADR anchor (§7), the deferred items (§8). NOTE: the BRAINSTORM anticipated APPEND on a collision; the SPEC OVERTURNED that to OVERRIDE/upsert (SPEC §1.1) — the SPEC governs.
4. `docs/envoy-go/ROADMAP.md` — row 59 `in-progress`; the Observability family prose; the LIVE deferred sentence (still ONE live match — `custom_tags` narrows at the IMPL, not the PLAN).
5. `docs/envoy-go/STATE.md` — the active-phase header (phase 59 SPEC done; NEXT = the phase-59 PLAN).
6. `docs/envoy-go/DECISIONS.md` — the ADR-0276 tail (next-free **ADR-0277**); ADR-0260 (the tracing engine/exporter seams) for the span-attribute substrate.
7. `internal/tracing/{config.go,span.go,zipkin.go}` + `internal/filter/hcm/{accesslog_emit.go,config.go}` — the parse home (`NewConfig`), the span-attribute seam (`Span.Attrs`/`BuildServerSpan`/`upsertAttr`), the Zipkin encoder, and the two `BuildServerSpan` call sites + the `f.tracingConfig`/`f.exporter` wiring the PLAN RE-DERIVES.
8. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the tracing section (`:686` strict-reject roster + `:739` Zipkin deferred bullet the IMPL edits).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the `reference_*` traps (esp. the tracing references + the differential-fixture/break-protocol/fuzzer-count traps).
