START a fresh cold-start session for envoy-go — **the phase-39 (`upstream-health-check`, active health checks) PLAN is DONE, LANDED on master, and PUSHED to `origin/master`; the next lifecycle step is the phase-39.1 IMPL** (`superpowers:subagent-driven-development` per `feedback_execution_style` — execute `docs/envoy-go/phases/39-upstream-health-check/PLAN.md` Tasks 1–16 in a fresh worktree; Task 1 creates PROGRESS.md; the controller squash-merges + pushes at stage-close per `feedback_push_to_origin`). The substantive master tip is the phase-39 PLAN-DONE bundle (`docs/envoy-go/phases/39-upstream-health-check/PLAN.md` + README + STATE + ROADMAP row 39 PLAN-DONE note; confirm via `git log --oneline -5`). The prior substantive tips are the phase-39 SPEC bundle + BRAINSTORM bundle and the phase-38.2 IMPL squash `14053d1`. ALL counts UNCHANGED at this PLAN: **stat surface 1125 / fixtures 67 / fuzzers 42 / BackendKind 33 / DECISIONS tail ADR-0241 (next-free ADR-0242).**

## What just landed (phase 39 PLAN — the durable record is `docs/envoy-go/phases/39-upstream-health-check/PLAN.md`; read it, not this summary)

SPEC §10 decomposed into **16 bite-sized TDD tasks**; the FINAL ADR-0045 split-gate re-check → **NO FURTHER SPLIT** (~500–800 prod LoC / 16 tasks — both axes under the gate; single flat 39.1 leg). The §12 **D-S39-1..D-S39-6 RESOLVED**: file placement = a NEW `internal/cluster/health.go` + a concrete `*clusterHealth` field on each of the six LB constructs (nil = today's behavior → byte-stable, every existing fixture byte-identical); hosts start HEALTHY + first-check-immediate (D-S39-2); envoy-go does its own byte-stable rejects + the tcp/grpc 39.1 DEPARTURE-reject (D-S39-3); the `0066` constants = 2-live/1-dead (66% > 50% → asserts FILTERING not panic), interval/timeout 0.5s, unhealthy_threshold 1, N=100, poll `membership_healthy==2` on both sides (D-S39-4); an injectable `probeOnce` + `StartHealthChecks` cancel-in-`Drain` + WaitGroup, no goroutine leak under -race (D-S39-5).

**The 16 tasks:** (1) baselines/PROGRESS; (2) `hostHealth` registry + first-check-immediate transitions; (3) `clusterHealth` view + strict-`<` panic threshold; (4) the HTTP probe codec (`GET path` + `expected_statuses`); (5) the `healthChecker` runtime (probeOnce + applyResult + stats); (6) parse `health_checks` + `healthy_panic_threshold` + the reject roster; (7) thread `*clusterHealth` into `buildLeafLB` + the six constructs (INERT — field added, Pick unchanged); (8) health-aware pick — round_robin/random/least_request (skip + panic); (9) health-aware pick — ring_hash/maglev walk-to-next-healthy + subset inheritance; (10) register the +7 stats (scoped to health-checked clusters); (11) `Manager.StartHealthChecks(ctx)`/`Drain` lifecycle + `cmd/envoy-go/main.go` boot call (after `bs.Stats.Freeze()`); (12) the `0066-health-check-http` poll-to-converge differential; (13) deliberate-break liveness + 20/20 flake; (14) full 68-dir differential + six-gate; (15) ADR-0242/0243 bodies + BEHAVIOR_CONTRACT delta; (16) completion bundle.

**The architecture (ADR-0242 + ADR-0243; §Context drafts in SPEC §13, bodies at this IMPL per ADR-0044):** a per-cluster `clusterHealth` registry (`map[string]*hostHealth` keyed by `Endpoint.Addr()`; atomic health bit + consecutive-result counters; seeded at `buildCluster`) + the per-cluster × per-`health_check` background goroutine runtime (`probeOnce` ticker; first-check-immediate then threshold-gated; `StartHealthChecks` post-`Freeze` boot start + `Drain` cancel/wait — the project's FIRST cluster-level background runtime) + the HTTP probe (a fresh-conn `GET path`) → ADR-0242; the health-aware pick (a build-time-injected `*clusterHealth` consulted at `Pick` by all six constructs — skip unhealthy + re-pick; ring_hash/maglev walk-to-next-healthy preserving key stability; `healthy_panic_threshold` default 50% strict-`<` fallback-to-all + `lb_healthy_panic`++; NOT a `Pick`-signature widening — health is cluster state; byte-stable surface; behavior-neutral when all healthy) → ADR-0243. ZERO new packages/go.mod modules; the +7 stats {`health_check.{attempt,success,failure,network_failure}` + `health_check.healthy` gauge + `membership_healthy` gauge + `lb_healthy_panic` counter} emitted ONLY on clusters with `health_checks`.

## What's next — the phase-39.1 IMPL (`superpowers:subagent-driven-development`)

Execute PLAN.md Tasks 1–16 in a fresh worktree (`feedback_git_worktrees`), one fresh implementer subagent per task + two-stage review (spec compliance → code quality), then a final holistic review. Honor: subagents commit LOCAL-only (`feedback_subagents_no_push`); per-task `gofmt -l` + `golangci-lint` (`feedback_pertask_gofmt_lint`); pin canonical paths for worktree subagents (`feedback_subagent_worktree_path_targeting`/`_detach`). The six-gate (ADR-0052) GREEN before landing; the full 68-dir differential is the real guard (behavior-neutrality when no `health_checks`); h2spec 53/53 + proxy-wasm 10/10 asserted-unaffected by change-scope. At the six-gate: fixtures 67 → 68 (`0066`), stat surface 1125 → 1132 (+7), DECISIONS tail ADR-0241 → ADR-0243 (next-free ADR-0244), fuzzers 42 / BackendKind 33 UNCHANGED. Row 39 (39.1 leg) flips `in-progress → done` (NO parent rollup per ADR-0106). The controller squash-merges to master + pushes at stage-close (`feedback_push_to_origin` + the 38.2 squash precedent). Then 39.2 (TCP + gRPC checkers reusing the 39.1 substrate) or the next subject.

## Read first (in order)

1. THIS FILE — the post-phase-39-PLAN cold-start (next: the phase-39.1 IMPL).
2. `docs/envoy-go/STATE.md` — active-phase `phase 39 (upstream-health-check) PLAN done`; the counts; the lifecycle routing (→ the 39.1 IMPL).
3. `docs/envoy-go/phases/39-upstream-health-check/PLAN.md` — **the IMPL plan** (the 16 tasks with full code + exact commands; the file structure; the D-S39 resolutions) + SPEC.md (the AMEND-HC1..HC8 pins + the §6 rejects + §7 stats + §8 `0066`) + BRAINSTORM.md + README.md.
4. `docs/envoy-go/ROADMAP.md` — row 39 (`in-progress`, PLAN-DONE note) + the §Upstream robustness family heading note.
5. `docs/envoy-go/DECISIONS.md` — the process ADRs (ADR-0106/0045/0044/0052/0080/0227) + ADR-0232 (byte-stable OPTION-C) + ADR-0235/0239 (the per-request pick-inputs the health view CONTRASTS) + ADR-0236/0238 (ring_hash/maglev — walk-to-next-healthy) + ADR-0230 (the prior framework-runtime seam) + ADR-0024.
6. `internal/cluster/{cluster.go,manager.go (buildLeafLB:225 / buildCluster:254 / registerClusterMetrics:99 / Drain:214 / extractEndpoints:557),loadbalancer.go,random.go,leastrequest.go,ringhash.go,maglev.go,subset.go (the leafFactory pattern),dial_h2.go}` + `internal/stats/{counter,gauge,registry}.go` (the stat API) + `internal/admin/admin.go` (`/stats`) + `cmd/envoy-go/main.go` (`bs.Stats.Freeze()`:272 → `<-ctx.Done()`:280 → `cm.Drain()`:296 — the StartHealthChecks slot) + `test/fixtures/0065-weighted-clusters/` (the cross-side HTTP differential precedent `0066` follows).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_execution_style`, `feedback_git_worktrees`, `feedback_subagents_no_push` + `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_path_targeting`/`_detach`, `reference_differential_break_protocol_count1`, `reference_differential_run_selector`, `reference_fixture_workload_constant_desync`, `reference_docker_probe_bridge_network`, `reference_differential_band_sigma_margin`.

## Honor throughout

- The lifecycle is BRAINSTORM → SPEC → PLAN → IMPL, each its own session. The next session is the phase-39.1 **IMPL** (`superpowers:subagent-driven-development` — execute PLAN Tasks 1–16).
- `feedback_git_worktrees` · `feedback_execution_style` (subagent-driven at the IMPL) · `feedback_subagents_no_push` · `feedback_push_to_origin` (the controller squash-merges + pushes at stage-close) · `feedback_pertask_gofmt_lint` · `feedback_subagent_worktree_path_targeting`/`_detach`.
- ADR-0004 (the empirical-pin discipline — EXECUTED at the SPEC) · ADR-0044 (ADR §Context at SPEC, bodies at IMPL in-place — ADR-0242/0243 bodies land at Task 15) · ADR-0045 (the split-gate — NO FURTHER SPLIT, single 39.1 leg) · ADR-0052 (the atomic-landing six-gate) · ADR-0080 (byte-stable parse-reject wording) · ADR-0106 (flat family row — NO parent rollup) · ADR-0227 (the contrib reference image `envoyproxy/envoy:contrib-v1.37.2`).
