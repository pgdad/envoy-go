=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-VERIFIED 2026-07-17 (re-run MECHANICALLY at the phase-67 PLAN close, in the stage worktree) — it does NOT fire. `stop` was NOT created.** Actual output:
- **(1) prints `NOT DONE: row 67`** — row 67 is `in-progress` (it flips `done` at the phase-67 IMPL six-gate — THIS session's T9). Check (1) BLOCKS `stop`.
- **(2) prints THREE live "candidates:" sentences** — HTTP/3, xDS, Observability ⇒ three families STAY OPEN. **NOTE: the Observability line ALSO carries HISTORICAL `candidates were:` recaps** (`reference_sentinel_deferred_sentence_live_vs_historical`; the live grep needs "candidates:" adjacent). Exact command, and ONLY it — **cite the command, never the adjective**: `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` ⇒ **3**. (Bare `grep -c 'candidates:'` returns a different, WRONG number.)
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** ⇒ THREE families never opened. Operational-tooling also STAYS OPEN.

All three checks print ⇒ the sentinel does NOT fire. Abundant work remains.

==============================================

## STATUS AS OF 2026-07-17: THE PHASE-67 PLAN IS COMPLETE AND LANDED AT THIS CLOSE. ROW 67 STAYS `in-progress`. THIS SESSION PERFORMS THE PHASE-67 IMPL (T1–T9; row 67 flips `done` at the six-gate).

**What the PLAN landed** (`docs/envoy-go/phases/67-tls-require-client-cert-false/PLAN.md` + `PROGRESS.md`; locate the squash by SUBJECT — `git log --grep 'phase 67'`, `reference_next_prompt_tracked_despite_gitignore`; never by position): a **9-task TDD spine (T1–T9)** decomposing SPEC §10, adversarially verified by THREE independent verifiers (PV1 code-claims BY EXECUTION — built the hoist from the PLAN's own sketch and PROVED the T1 flip roster complete: the untouched suite fails exactly and only the three roster tests; applied + executed every break; reproduced Break B's withholding physics from scratch · PV2 spine-logic · PV3 process) — **0 SEVERE, 5 MODERATE, ~10 minor, ALL corrected in the landed amendment**. The PLAN was drafted off `a15f4fca`, which includes a post-SPEC **PARALLEL work stream** (five test files + CI + TEST_GAP append) — absorbed as A1–A6, incl. the **B19 chartering** (a test-comment-only carve-out in `boot_sds_e2e_test.go:518-521` inside SPEC §15's `internal/boot` BYTE-UNTOUCHED pin, à la B16).

**⚠️ PUSH (idempotent — evaluate, then act):** FIRST ACTION: `git fetch`; if origin/master is BEHIND local master and the local tip is the phase-67 PLAN squash, `git push origin master` per `feedback_push_to_origin`; if in sync, proceed; if REJECTED (diverged), STOP and reconcile — never rebase away landed squashes.

**Key facts a cold session needs (RE-VERIFY MECHANICALLY; do not copy):**
- Counts at entry: fixtures **111** (`ls -d test/fixtures/[0-9]*/ | wc -l`; tail `0109-xds-sds-combined-validation-context`; **T4 makes it 112**) · fuzzers **55** (+0 this row; `grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`) · DECISIONS tail **ADR-0289** (next-free **ADR-0290**; **T9 COMPLETES ADR-0289 IN PLACE — the tail does NOT flip**; re-derive with `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1`) · BackendKind tail **38** (+0 — the 0110 driver is driver-owned) · stat surface **1201** (+0; docs-verified, NO mechanical command) · go.mod modules **2** (lineage figure; the single `go.mod` requires 67).
- **STATE.md discipline (ADR-0288):** §Current pointer reads `phase 67 PLAN done` / NEXT = the phase-67 IMPL. **EDIT §Current pointer IN PLACE — do NOT prepend**; demote the outgoing bullet into §Recent lineage, **cap at FIVE** (retire the oldest only if its substance is in `STATE_HISTORY.md` or move it there). Never grep `STATE_HISTORY.md` for a pointer or count.
- `.worktrees/phase-67-plan` and branch `phase-67-tls-require-client-cert-false-plan` are REMOVED at close (squash landed).

## What THIS session does — phase 67 IMPL (T1–T9)

One stage per session: IMPL ONLY. Fresh worktree off master, branch `phase-67-tls-require-client-cert-false-impl`, per `feedback_git_worktrees`; **subagent-driven per task** (`feedback_execution_style`), each task committing LOCALLY only (`feedback_subagents_no_push`; subagents auto-commit per CLAUDE.md — controller verifies, cleans leaks, squashes at stage-close per `feedback_subagent_autocommit_claudemd`); per-task gofmt + golangci-lint on touched packages (`feedback_pertask_gofmt_lint`); controller squash-pushes at close. Dispatch a GIT HYGIENE block to every subagent (`feedback_subagent_worktree_detach`, `feedback_subagent_worktree_path_targeting` — pin the worktree root; breaks run AFTER committing, `reference_break_protocol_commit_first`).

**Execute PLAN T1–T9 — RE-DERIVING each task's anchors before editing** (`feedback_brief_citations_not_evidence`; the PLAN's line cites are at `a15f4fca` and the squash may land on a moved master). The BINDING rules (full text in the PLAN — read it, don't trust this summary):

- **T1 is ATOMIC**: the hoist + three-way-across-FOUR-cells `ClientAuth` (true+anchor → `RequireAndVerifyClientCert` · false/absent+anchor → `VerifyClientCertIfGiven` · false/absent+no-anchor → `NoClientCert` · true+no-anchor → the RETAINED `:179-181` reject) + E3 retirement + the FULL flip roster in ONE commit. The roster (PROVEN complete by execution): `TestCVC_RequireFalse_Rejected_E3` flips · `TestCVC_RequireFalse_NeverYieldsNoClientCert` err-half inverts · the config_test.go INERT subtest's assertions invert (TWO observable inversions — ClientCAs nil, NoClientCert; the no-fetch observable needs optional fakeProvider instrumentation) · fuzz seed (i) comment flips. Stays-green roster incl. the three new SDS e2e tripwires + `TestNewManager_MultiChain_RequireClientCert_Errors` + 0108/0109 differential. The P5 comment moves byte-intact; retained rejects byte-diffed.
- **MECHANISM-CONDITIONALITY**: Breaks A/C/D are pre-verified-verbatim ONLY under the shared `installPool`/`clientAuthFor` closure shape; per-arm adjacency ⇒ Break A becomes one-swap-PER-ARM and C/D need IMPL-time compile re-verification (`reference_plan_break_instructions_dont_compile`). Break C's e2e firing set: ONLY the refuse subtest of `TestSDSEndToEnd_CVC_PoolSubstitution` fires (the accept subtest PASSES under the break). Every break `-count=1` (`reference_differential_break_protocol_count1`), confirm WHICH assertion fired (`reference_deliberate_break_wrong_assertion`), `Errorf` per property (`reference_fatalf_makes_assertions_unreachable`), commit BEFORE breaking (`reference_break_protocol_commit_first`).
- **T4/T5 fixture `0110-tls-require-client-cert-false`** (fixtures 111 → 112): CVC-primary three-arm verdict (trusted→ok+echo · untrusted→REJECTED · no-cert→ok+echo), 0109 chassis + **FORCED-SEND `GetClientCertificate`** untrusted arm, per-side failure pins, port 10446, bare-scalar wrapper YAML (`reference_protojson_wrapper_scalar_not_object`), served-this-arm assert, structuralCheck triplet, full selector `TestDifferential/0110-tls-require-client-cert-false` (`reference_differential_run_selector`), one dir = ONE runner branch, BackendCount ≥1, never assert `/listeners`/`total_listeners_active`.
- **T6/T7/T9 docs**: the five comment rewrites (B11/B16/B17/B18/B19 — B16 `provider.go:91-93` is the chartered `internal/xds` doc-comment-only exception; B19 the chartered `internal/boot` test-comment exception) · BC B1–B10/B13–B15 with the SPEC's pinned wording VERBATIM · the FOUR-site TEST_GAP sweep by CLAIM + grep · **B12 at T9** (the DECISIONS:16899 bracketed annotation — never silent) · the full-repo zero-LIVING-drift confirmation is a **T9 gate** with the PLAN's expected-hits classification table · **ADR-0289 COMPLETED IN PLACE at T9** (§Decision/§Consequences; no tail flip) · TWO memory updates owed per SPEC §13 (see the PLAN's T9).
- **T8 verify**: six-gate (gofmt silent · vet 0 · build 0 · `go mod tidy -diff` EMPTY · `git diff master -- go.mod go.sum` EMPTY · golangci-lint 0) + cycle guard (`go list -deps ./internal/xds`, no `...` → stats+xds ONLY) + the FULL 112-dir differential `-count=1` (isolate-re-run any `subject ready: EOF` startup flake, `reference_differential_fullsuite_startup_flake`) + `-race` on touched packages.
- **T9 stage-close**: ROADMAP row 67 flips `done` (the SOLE leg, ADR-0106) — check (1) goes SILENT; checks (2)+(3) still print ⇒ sentinel does NOT fire; NO deferred-sentence edit; STATE.md pointer in place; router rolls to the phase-68 BRAINSTORM (the roller SELF-PICKS per the standing directive — remember to re-verify the sentinel and record the pick + rejected alternatives).
- Run an adversarial verification pass before landing (PRIVATE scratch per verifier; the phase-66/67 precedent: verify the six-gate evidence, the break ledger's ACTUAL column, the roster completeness, the doc wording against the SPEC's pins).

## Read first (in order)
1. `docs/envoy-go/phases/67-tls-require-client-cert-false/PLAN.md` — the spine you execute (Global Constraints, the break protocol + MECHANISM-CONDITIONALITY, T1–T9) + `PROGRESS.md` (the ledgers you fill: task table, break ledger with expected assertions, six-gate checklist).
2. `docs/envoy-go/phases/67-tls-require-client-cert-false/SPEC.md` — §9 B1–B18 pinned replacement wording (T6/T7/T9 apply it VERBATIM); §3 the decisions' evidence.
3. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline).
4. `docs/envoy-go/ROADMAP.md` — row 67 + §Schema + the THREE live deferred sentences.
5. `docs/envoy-go/DECISIONS.md` — the ADR-0289 §Context DRAFT (T9 completes it) + ADR-0287/0286/0280. Tail ADR-0289 — re-derive.
6. `docs/envoy-go/BEHAVIOR_CONTRACT.md` :886-:950 + :1782/:1815-:1817 — the B-item landing region.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_*` + `reference_*`; trust MEMORY.md's index.
