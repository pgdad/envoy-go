=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-VERIFIED 2026-07-16 (re-run MECHANICALLY at the phase-66 IMPL PARTIAL-LAND, master `2c557804`) — it does NOT fire. `stop` was NOT created.** Actual output:
- **(1) prints `NOT DONE: row 66`** — row 66 is STILL `in-progress`: the phase-66 IMPL is **INCOMPLETE** (T1–T3 landed; **T4–T9 remain**), so the six-gate never ran and the row never flipped. **⚠️ It goes SILENT only when the phase-66 IMPL COMPLETES**, at which point checks (2)+(3) ALONE hold the loop open. A roller landing that IMPL MUST NOT read "(1) is finally silent" as "the project is done".
- **(2) prints THREE live "candidates:" sentences** — HTTP/3, xDS, Observability ⇒ three families STAY OPEN. **UNCHANGED by row 66 at every stage** — `combined_validation_context` was never IN a sentence (a §8-tier pickup; the phase-64 precedent). **NOTE: the Observability line ALSO carries HISTORICAL `candidates were:` recaps — do not confuse them with the live sentence** (`reference_sentinel_deferred_sentence_live_vs_historical`; the live grep needs "candidates:" adjacent). Exact command, and ONLY it — **cite the command, never the adjective** (PLAN F9): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` ⇒ **3**. (Bare `grep -c 'candidates:'` returns 5 — a different, WRONG number.)
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** ⇒ THREE families never opened. Operational-tooling also STAYS OPEN.

All three checks print ⇒ the sentinel does NOT fire. Abundant work remains.

==============================================

## ⚠️ STATUS AS OF 2026-07-16: THE PHASE-66 IMPL IS **PARTIALLY LANDED AND UNMERGED**. THIS IS THE ONE THING TO UNDERSTAND BEFORE ANYTHING ELSE.

The previous session performed the phase-66 IMPL **subagent-driven** and completed **Tasks 1–3 of the 9-task spine**. The session ended mid-Task-3-verification (an API cutoff), NOT at a stage close. **This session RESUMES that IMPL at Task 4 — it does NOT restart it, and does NOT open a new phase.**

**WHERE THE WORK IS — it is NOT on master:**
- **Branch `phase-66-impl`** (LOCAL only — deliberately NOT pushed; the controller squash-pushes at stage-close per `feedback_subagents_no_push`), **worktree `.worktrees/phase-66-impl`**, cut off master `2c557804`.
- Three commits, in order:
  - `57dbde17` — **T1**: lift the CVC reject to a gated no-op + E1/E2 + **E3**, atomically.
  - `44669a78` — **T2**: re-point the four sub-field rejects via the `inlineVC` selector.
  - `a315e810` — **T3**: the apply-point — the CVC arm in `NewDownstreamConfig`'s require block.
- **The tree at `a315e810` is GREEN and CLEAN** (re-verified this session): `gofmt` silent · `go vet` clean · `go build ./...` clean · `go test ./internal/tls/ -count=1` **ok**, with **19 CVC/inline/SDS-VC tests passing**.
- **Locate these commits by SUBJECT (`git log --grep`), never by position** (`reference_next_prompt_tracked_despite_gitignore`).

**⚠️ A DELIBERATE BREAK WAS LEFT UN-RESTORED IN THE WORKTREE BY THE CUTOFF, AND THE CONTROLLER RESTORED IT** (Break 2: `xds.ParseSDSConfig` replaced by a bare `GetName()`, clearly labelled `// BREAK 2:`). It was restored with **`git restore` only**; the worktree is clean and `xds.ParseSDSConfig` is back (grep ⇒ 4 hits). **The lesson generalizes: an agent killed mid-break leaves the tree BROKEN. At session start ALWAYS `git status` the stage worktree and diff any modification before trusting it — a green `go test` on a broken tree is not evidence, and a break committed by accident is a shipped defect.**

### What REMAINS — Tasks 4–9. Verified MECHANICALLY this session, not copied:
| Task | Status | Evidence |
|---|---|---|
| T1 lift + E1/E2 + E3 | ✅ DONE | `57dbde17`; 8 tests green |
| T2 re-point four rejects | ✅ DONE | `44669a78`; 4 verified-non-vacuous REDs + 5 tests green |
| T3 apply-point | ✅ **CODE DONE, BREAK EVIDENCE INCOMPLETE** | `a315e810`; 5 tests green — **see the break debt below** |
| **T4** boot pre-scan third arm | ❌ **NOT DONE** | `grep -c CombinedValidationContext internal/boot/boot.go` ⇒ **0** |
| **T5** fuzz SEEDS | ❌ **NOT DONE** | `grep -c -i combined internal/tls/fuzz_test.go` ⇒ **0** |
| **T6** fixture `0109` | ❌ **NOT DONE** | `test/fixtures/0109*` **ABSENT**; count still **110** |
| **T7** BEHAVIOR_CONTRACT | ❌ **NOT DONE** | CVC still recorded REJECTED (1 hit, the old entry) |
| **T8** three stale comments | ❌ **NOT DONE** | `grep -c 'exactly ONE live consumer' internal/tls/config.go` ⇒ **1** (the FALSE one) |
| **T9** verify + ADR + STATE + ROADMAP + PROGRESS + roll | ❌ **NOT DONE** | ADR-0287 carries `### Context` ONLY; row 66 `in-progress` |

**⚠️ T4 IS NOT COSMETIC — WITHOUT IT THE FEATURE IS UNREACHABLE FOR THE ORDINARY CASE.** `boot.NewSDSProvider`'s pre-scan does **not** look at `combined_validation_context` today, so a plain CVC listener yields `seen == 0` ⇒ a genuine nil provider ⇒ **T1's retained gate rejects it**. **EXECUTED and CONFIRMED this session.** CVC is currently reachable ONLY when some *other* position in the bootstrap contributes an SDS count. **T4 is what wires the row's headline feature.** This is not a defect in T1–T3; it is the spine's design. **Do not "discover" it as a bug.**

**T3's BREAK DEBT — OWED, and it is a REAL gap, not bookkeeping:**
- **Break 1 (Design C / pool-UNION, which `TestCVC_ServedPoolWins_DefaultTrustedCaNotRead` must refute) — its result was NEVER CAPTURED** (the cutoff). **RE-RUN it and record the TRUE result.** If test 2 does not fire, the theorem's observable is UNPROVEN — a **FINDING**.
- **Break 2 (bare `GetName()`) fired BOTH of `TestCVC_MalformedSDSConfig_Rejected`'s properties — but that is NOT proof both are live.** The cut-off agent caught this itself, and it was right: under Break 2 property (ii) (`tls: ` prefix) is **ENTAILED** by property (i) (rejects at all) — no error means no prefixed error, so (ii) cannot fail independently. **An ISOLATING break for the `tls: ` prefix is still OWED** (e.g. return the `xds:`-prefixed error UNWRAPPED ⇒ (ii) must fire while (i) stays green). **This is `reference_probe_must_discriminate` caught one level up, inside a break that "fired".** A break firing ≠ your assertion is live.
- The **nil-provider guard is DELIBERATELY UNPROVEN** — unreachable by construction (T1's gate rejects first). Mirrors the landed phase-65 arm. **Record it as such; do NOT manufacture a test that bypasses the gate to reach it.**

==============================================

## What THIS session does (RESUME the phase-66 IMPL at Task 4)

Perform **T4 → T9** of PLAN-66's spine, subagent-driven (`feedback_execution_style`; fresh subagent per task), **in the EXISTING worktree `.worktrees/phase-66-impl` on branch `phase-66-impl`** — do NOT cut a new one, do NOT re-do T1–T3. **ROW 66 FLIPS `in-progress` → `done` at the six-gate** (ADR-0106, the SOLE leg — `reference_roadmap_split_phase_row_done`). Then squash-merge to master, push (`feedback_push_to_origin`), and roll this file.

**The subject, the design, AND the task spine are SETTLED.** Design A (pool substitution) is adopted; its equivalence theorem has now survived **three** independent adversarial attacks — the third **corrected its premises** (see below). **Do NOT re-pick, do NOT re-open the seam, do NOT re-decompose.**

### The session's obligations, in priority order

1. **RE-DERIVE, do NOT execute. Default to REFUTED.** The rate is NOT falling: BRAINSTORM 11 defects → SPEC 4 → PLAN 9 (own first draft) + 6 SPEC corrections → **this session's adversarial pass found 4 MORE in the PLAN itself** (below). Attack where the document is most confident.
2. **⚠️ FIRST ACTION: `git status` the worktree** and diff any modification. The cutoff already left one break behind once.
3. **T3's break debt (above) MUST be discharged** — re-run Break 1, add the isolating `tls: `-prefix break. Record TRUE results in PROGRESS.
4. **`structuralCheck` must be RE-DEMONSTRATED on `0109`, not asserted** (T6): disable it, watch the served-CA break ship **PASS**, restore with **`git restore` only**, watch it FAIL.
5. **ADR-0287: COMPLETE IT IN PLACE** (§Decision + §Consequences). **Do NOT append a new ADR; do NOT re-number — collision.** See the ADR arithmetic below.
6. **STATE.md: EDIT `§Current pointer` IN PLACE — do NOT prepend** (ADR-0288). Demote the outgoing bullet into §Recent lineage; **cap at FIVE**.

### ⚠️ FOUR PLAN DEFECTS FOUND BY THIS SESSION'S ADVERSARIAL PASS — all EXECUTED, none from reading

- **🔴 THE EQUIVALENCE THEOREM'S PREMISES ARE INCOMPLETE — and the omitted premise is an IMPL obligation, not a proven fact.** The theorem (SPEC §3.2) rests on P1/P2/P3/P5. **A COUNTEREXAMPLE WAS EXECUTED against the theorem AS STATED:** a CVC whose `default_validation_context` carries `match_typed_subject_alt_names: evil.com` (or `verify_certificate_hash`) ⇒ a TRUE merge carries the field forward, the reject fires, **boot FAILS**; "use the SDS pool and never read the default" **never sees it** ⇒ the listener boots authenticating any CA_X-signed client with **NO SAN check** — *silently weaker than configured*. **The theorem needs an explicit FOURTH premise: the four sub-field rejects are RE-POINTED at `default_validation_context` (T2).** With T2 landed the differential is **36/36 EQUAL**; without it the divergence is LIVE. ⇒ **T2 is load-bearing for the THEOREM, not tidiness — and the T2-before-T3 order is what kept the hole from ever existing.** **ADR-0287 §Decision MUST state the fourth premise.** *(The apply-point comment landed at T3 already names it as P4 — keep it.)*
- **🔴 D2's E2-reachability table is WRONG: a SUFFICIENT condition stated as NECESSARY.** It says E2 fires "**ONLY** when ANOTHER **listener** supplies the global provider". **REFUTED by execution:** ONE listener with a cert-SDS `filter_chains[0]` **plus a pure-inline-CVC `default_filter_chain`** reaches E2 (`seen=1`, provider live). **Listener count is incidental.** The true condition: **any OTHER POSITION in the bootstrap contributes an SDS count** — another listener OR another filter chain in the SAME listener, `default_filter_chain` included. **Any test or doc pinning E2 to "multi-listener" describes a sufficient case as necessary.** Fix in T7/ADR-0287.
- **🟡 C2/D2's "upstream + QUIC keep the byte-identical message" — the UPSTREAM half is VACUOUS.** An upstream caller **never reaches the CVC arm at all**: `validation_context_type` is a oneof, so under CVC `GetValidationContext()` is nil and `NewUpstreamConfig` rejects earlier with `validation_context.trusted_ca is required` (EXECUTED). The ordering argument rests **entirely on QUIC + `validate` + `main.go`@`seen==0`**. **The conclusion (E1/E2 AFTER the gate) STANDS — only its stated justification was partly dead.** T1's `TestCVC_Upstream_KeepsByteIdenticalReject` pins the RETAINED GUARD (reached by driving the unexported fn directly), **not a live path** — it is already labelled; keep the label.
- **🟡 P1's phrasing is false as literally written.** "envoy-go honors ONLY `trusted_ca`; no other field affects behavior" — **4 fields affect behavior by REJECTING.** Correct statement: **only `trusted_ca` is HONORED; four more are REJECTED; the other ten are SILENTLY IGNORED by BOTH paths (a SHARED gap, not a divergence).** The 15-field roster IS correct (confirmed by reflection): repeated = exactly f2/f3/f9/f15; plain bool = exactly f8/f14.

### ⚠️ TWO NEW NAMED BOUNDARIES this session DISCOVERED (record in T7; do NOT fix — they widen the row)

- **The pre-scan MISSES a QUIC listener's inner `DownstreamTlsContext`** (it gates on the `DownstreamTlsContext` type_url; QUIC wraps one inside a `QuicDownstreamTransport`). **Benign and arguably LOAD-BEARING:** QUIC chains route to `NewQUICDownstreamConfig`, which hardcodes a nil provider, so counting that position would inflate `seen` and trip the `seen > 1` hard-fail for unrelated TCP listeners. *(A QUIC listener's `default_filter_chain` goes through plain `NewDownstreamConfig` and IS covered — consistent.)*
- **`match_subject_alt_names` (f9) is SILENTLY IGNORED while its sibling `match_typed_subject_alt_names` (f15) is REJECTED** — a live asymmetry on **both** paths, **pre-existing and outside this row**. Record; do not fix.

### Carried findings that remain BINDING

- **⚠️ BREAK PROTOCOL — COMMIT FIRST, THEN BREAK.** `git restore` restores from index/HEAD, so breaking an **uncommitted** tree and restoring **silently WIPES the work**. This cost T1 a full re-apply. **Order: verify → commit → break → `git restore` → re-verify.** *(New; not in PLAN-66. Honor it in every remaining task.)*
- **A LANDED CODE COMMENT IS NOT EVIDENCE — the FALSE one is STILL ON DISK** (`internal/tls/config.go`, "The guard below has exactly ONE live consumer: NewQUICDownstreamConfig"). It is **FALSE — THREE** live consumers (`NewQUICDownstreamConfig` literal nil · `validate.Bootstrap` nil · `main.go`'s ORDINARY path when `NewSDSProvider` returns `(nil,nil)` at `seen==0`), **plus TWO EXPORTED test-only constructors** (`listener.NewManager`, `NewManagerWithBaseDir`). **This comment took the SPEC in and produced its one SEVERE defect.** A *third* comment in the same file ("nil for upstream/**validate** callers") **contradicts it and is correct**. **T8 fixes the false one and KEEPS the correct one.** **Land the count as "three production paths, plus two exported test-only constructors" — state the SCOPE, or it is R9's defect again.** `SPEC §3.7`/§6/§9-item-1/§11-7b and the **ADR-0287 §Context draft** all still say **TWO** and MUST be reconciled before landing.
- **C1 is CONFIRMED EMPIRICALLY, twice.** T1's Break D reproduced it: with E3 deleted, `TestCVC_RequireFalse_NeverYieldsNoClientCert` fired `NewDownstreamConfig returned a cfg with ClientAuth == NoClientCert — a SILENTLY UNAUTHENTICATED listener`. **E3 is not theoretical. Never regress it.**
- **`reference_sds_init_fetch_timeout_dial_budget_flake`** — a `-race` failure in `TestProvider_FetchInitialCertificate_Timeout` is **PRE-EXISTING on master** (2026-07-16). Do NOT reflex-classify it as a phase-66 regression.
- **P5 is a COINCIDENCE with NO guard, and this session PROVED it fragile:** `internal/xds`'s `dataSourceBytes` and `internal/tls`'s `loadDataSource` are **behaviorally IDENTICAL across a 16-case differential** (differing only in error-string prefix), and `main.go` passes the byte-identical `filepath.Dir(*cfgPath)` to BOTH `NewSDSProvider` (`main.go:156`) and `boot.Construct` (`main.go:304`) — two independent arguments **148 lines apart, guarded by NOTHING**. **The MANDATORY P5 comment landed at T3 — keep it.** Consider recording the invariant in §Deferred.
- **Do NOT write `proto.Merge` / `proto.Clone`.** The BRAINSTORM's §6 still instructs them; **they do not compile** (SPEC §3.1). `internal/xds` stays **UNTOUCHED** — the row's defining property.

**Honor throughout:**
- **Lifecycle = one stage per session.** This session: the REST of the IMPL. It is the LAST stage of phase 66.
- **Counts:** fixtures **110 → 111** (`0109-xds-sds-combined-validation-context`) · fuzzers **55 (+0)** (seeds only) · stat surface **1201 (+0)** · BackendKind **38 (+0)** · go.mod **2 (+0)** · ZERO new packages · **ZERO new symbols in `internal/xds`**. **Re-verify MECHANICALLY; do not copy.**
- **The six-gate** (controller-run on the FROZEN HEAD): `gofmt -l internal/ test/ cmd/` SILENT · `go vet ./...` · `go build ./...` · `go mod tidy -diff` EMPTY **+** `git diff --exit-code master -- go.mod go.sum` EMPTY · `golangci-lint run ./...` · the **FULL 111-dir** differential `go test ./test/differential/ -count=1`. Plus the **cycle guard** (`go list -deps ./internal/xds`, **no `...`** ⇒ `internal/stats` + `internal/xds` ONLY) and **`-race`** on `./internal/tls/ ./internal/boot/`.
- **Process anchors:** ADR-0044 · ADR-0045 (NO split) · ADR-0106 · ADR-0080 · ADR-0280 · ADR-0286 (**its "CVC" means `CertificateValidationContext`**) · ADR-0288 (the STATE.md discipline).
- **Break protocol:** breaks must **COMPILE** (`reference_plan_break_instructions_dont_compile`) **and DISCRIMINATE** (`reference_probe_must_discriminate`); `-count=1` always; `-run 'TestDifferential/0109-xds-sds-combined-validation-context'` (**never bare `0109`** — `reference_differential_run_selector`); confirm **WHICH** assertion fired; **a break that does NOT fire is a FINDING** — record it UNCLAIMED.
- **Memory updates owed** (SPEC §13 + PLAN T9 + this session's): the five SPEC-listed ones, **plus** — *a landed code comment is not evidence* (demonstrated twice in one row); **C1's shape: when a row lifts a reject, ask what ELSE that reject was silently enforcing** (here: mTLS-actually-on); **commit-before-break**; **a break that fires can still prove nothing when one property ENTAILS another**; and **an agent killed mid-break leaves the tree broken — `git status` the worktree at session start**.
- **Run an adversarial verification pass before landing.** It found 11 defects in the BRAINSTORM, 4 in the SPEC, 9 in the PLAN's first draft, and **4 more in the landed PLAN this session**. **Ask a verifier to RE-DERIVE independently and default to REFUTED** — do NOT hand it your line numbers. **Give each parallel verifier a PRIVATE `mktemp -d`**: two agents sharing a scratchpad **contaminated each other's results and produced a FALSE NEGATIVE** this session, caught only because one verified its patch text in-place before trusting the run.

**⚠️ THE ADR ARITHMETIC — READ BEFORE YOU TOUCH `DECISIONS.md`. DECISIONS tail is `ADR-0288`; next-free is `ADR-0289`. `ADR-0287` ALREADY EXISTS, carrying §Context ONLY** (verified this session: `awk '/^## ADR-0287/,/^## ADR-0288/'` shows exactly one `### Context` heading). The phase-66 IMPL **COMPLETES ADR-0287 IN PLACE** (§Decision + §Consequences, per ADR-0044) — it does **NOT** append a new ADR, and **the tail does NOT flip**. **Appending "the next ADR" as 0288 is a COLLISION with the landed STATE.md-restore ADR.** **A router is not evidence either: re-derive with `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1`.**
**ADR reservation ledger:** ADR-0283/0284/0285/0286 (LANDED) · **ADR-0287 = phase-66** (§Context DRAFTED; **§Decision/§Consequences COMPLETE IN PLACE at THIS session**) · **ADR-0288 = the STATE.md → pointer archival move (LANDED)** · next-free unreserved: **ADR-0289**.

**Counts at this roll (RE-VERIFIED MECHANICALLY 2026-07-16; `git fetch` first and re-verify again):** fixtures **110** (`ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0108-xds-sds-validation-context`) · fuzzers **55** (`grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`) · DECISIONS tail **ADR-0288** (next-free **ADR-0289**) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** (docs-verified; there is NO mechanical counting command) · go.mod modules **2** (the phase-61.2 lineage figure — **NOT a repo total**; the single `go.mod` requires **67**).

**⚠️ `STATE.md` DISCIPLINE (ADR-0288).** It was **750 KB**, 93% of it 152 never-retired `prior active-phase:` bullets; prepending had buried TWO fossil "current state" blocks pointing at phases 42.2/46.2 while the live phase was 66 — three contradictory "next" pointers in the file whose ONE invariant is to be the single source of truth for "what next". **Now 35 KB and TRUSTWORTHY to grep** — exactly ONE live `next-skill:` / `lifecycle-state:` / `next-free ADR:` / count block, all in **§Current pointer** at the top. **NOTHING was deleted:** the entire pre-move file is preserved VERBATIM in **`docs/envoy-go/STATE_HISTORY.md`**. **Never grep `STATE_HISTORY.md` for a pointer or a count; everything in it is stale by construction.** **THE RULE AT YOUR STAGE CLOSE: EDIT `§Current pointer` IN PLACE. Do NOT prepend** — prepending is exactly what created the fossils. Demote the outgoing `active-phase:` bullet into **§Recent lineage**, and **cap that list at FIVE**. **Conventions, not tooling-enforced** — if you prepend, the defect regenerates.
**Out of scope, deliberately, and still true:** `BOOTSTRAP_PROMPT.md` Step B tells cold-start to read six files totalling **~6.2 MB ≈ 1.58M tokens** — more than a 1M context window, so **Step B is already physically unfollowable**; sessions necessarily grep/slice. `ROADMAP.md` (819 KB) and `DECISIONS.md` (3.4 MB) are append-only by invariants 2/4.

**⚠️ NOTE ON STATE.md's POINTER:** it currently reads `phase 66 PLAN done` / NEXT = the phase-66 IMPL. That is **stale in one respect only** — the IMPL is now **partially landed (T1–T3)**. It was deliberately NOT edited this roll, because STATE.md's §Current pointer records **stage** transitions and the IMPL stage has not closed. **THIS file is the authority on the T1–T3/T4–T9 split.** When the IMPL closes, update STATE.md's pointer to `phase 66 IMPL done` per the in-place rule above.

## Read first (in order)
1. THIS FILE — the cold-start router. **The phase-66 IMPL is RESUMING at T4; T1–T3 are LANDED on the unmerged local branch `phase-66-impl`. The subject, the design AND the task spine are SETTLED.**
2. **`git -C .worktrees/phase-66-impl status`** and **`git -C .worktrees/phase-66-impl log --oneline master..HEAD`** — confirm the tree is clean and the three commits are present, BEFORE reading anything else. A cutoff already left a break behind once.
3. `docs/envoy-go/phases/66-xds-sds-combined-validation-context/PLAN.md` — **the authority on WHAT TO DO**, as amended by the four defects above. **T4–T9** · C4 (**the ADR arithmetic — no tail flip**) · D2's reachability table (**now corrected — E2 is POSITION-scoped, not listener-scoped**) · D4 (fixture `0109`) · D6 (`seen==1`) · the self-review. **The single most important read.**
4. `docs/envoy-go/phases/66-xds-sds-combined-validation-context/SPEC.md` — **the authority on WHY** (where they conflict, **the PLAN wins**; where THIS FILE corrects the PLAN, **this file wins — its corrections were EXECUTED**). §3.1 (why the BRAINSTORM's plan does not compile) · §3.2 (the theorem + **P5**; **its premises are INCOMPLETE — see above**) · §3.5 · §3.6 (the pre-scan third arm ⇒ **T4**) · §3.7 (**⚠️ says TWO nil-provider consumers — it is THREE, +2 exported test-only ctors**) · §3.9 (the ONE named departure, per-shape) · §8 (fixture `0109`) · §11. **⚠️ §3.10 carries the SEVERE C1 defect — its `require==false` scoping is WRONG for CVC; E3 (landed at T1) is the fix.**
5. `docs/envoy-go/phases/66-xds-sds-combined-validation-context/BRAINSTORM.md` — **for the SUBJECT and the ENVELOPE ONLY. Its §6 implementation roster is SUPERSEDED and does not compile.**
6. `docs/envoy-go/STATE.md` — §Current pointer. **35 KB and TRUSTWORTHY to grep** (ADR-0288); see the staleness note above. `STATE_HISTORY.md` is the archive and every pointer in it is stale BY CONSTRUCTION.
7. `docs/envoy-go/ROADMAP.md` — the §Schema invariant + row 66 + the xDS family (rows 60/60.1/60.2/65/66) + the THREE live deferred sentences.
8. `docs/envoy-go/phases/65-xds-sds-validation-context/PROGRESS.md` — **the FORMAT precedent** for an IMPL's liveness-break log.
9. `docs/envoy-go/DECISIONS.md` — **ADR-0287 (§Context ONLY — COMPLETE IT IN PLACE; do NOT append)** + **ADR-0288 (the STATE.md discipline)** + ADR-0286 (phase 65) + ADR-0280 (SDS wiring + the boot-FAIL departure) + ADR-0278 (the SDS substrate) + ADR-0147 (static-mTLS `ClientCAs`) + the ADR-0044/0045/0106/0080 process anchors.
10. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the xDS/SDS section (CONSUMED vs REJECTED today) + the recorded coverage boundaries ⇒ **T7**.
11. `test/fixtures/0108-xds-sds-validation-context/` — the ~95%-reusable driver (PKI, `mtlsEcho`, `structuralCheck`, `normalizeTLSErr`, per-side SDS servers) ⇒ **T6**.
12. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the differential-harness `reference_*` traps.
