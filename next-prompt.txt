=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action. (The sentinel does NOT apply right now — see below. A cross-family audit confirms three families still carry non-empty deferred-candidate lists.)

**STATUS AS OF 2026-07-07: phase 53 (`load-balancer-priority`) IMPL is LANDED (row 53 flips `done`). Every currently-chartered ROADMAP row is `done` — but THREE families remain open with deferred, not-yet-chartered candidates. This is the "none chartered, awaiting a human pick" shape — NOT the termination sentinel.**

- The phase-53 IMPL landed (squashed + pushed to origin/master; worktree `.worktrees/phase-53-impl`, branch `phase-53-load-balancer-priority-impl`): a new `internal/cluster/priority.go` `priorityLB` WRAPPER `loadBalancer` (the phase-52 `localityWeightedLB` structural precedent, generalized from a flat weighted draw across peer localities to a CASCADING WATERFALL across ordered `LocalityLbEndpoints.priority` tiers) groups a cluster's endpoints by `Priority` tier and, on every `Pick`, computes each tier's effective capacity from its live per-host health fraction scaled by the EXISTING `overprovisioning_factor` (a phase-52 dependency reused verbatim, threading `(opf uint32, hasOPF bool)` per the D-LW-OPF0 precedent), cascades load across tiers via the CORRECTED (not naive-recursive) AMEND-P4 formula (each tier's load capped by its own capacity against the REMAINING budget — diverging from the naive reading only at 3+ tiers), and — on the genuinely NEW cluster-wide capacity-SHORTFALL condition `Σ_i min(100, overprovisioning × healthy_fraction_i) < 100` (AMEND-P1, REFUTING BOTH BRAINSTORM panic hypotheses) — falls back to a flat, health-ignoring, host-count-uniform draw across every host in every tier (a health=NIL child), reusing the EXISTING `lb_healthy_panic` stat. Decomposed into the pure `tierHealth`/`tierCapacity`/`cascadeLoads` functions (`tierHealth` a panic-DISABLED per-tier health VIEW, AMEND-P1-COROLLARY, improving on `locality.go`'s child-local-panic boundary) + `priorityGroup`/`priorityLB` + both constructors + the phase-53-LOCAL `priorityLeafFactory` type (kept separate from `subset.go`'s shared `leafFactory`, D-P-FACTORY) + `Pick`. ZERO new `Pick` parameters. `Endpoint` grows `Priority uint32` (the THIRD per-endpoint dimension after phase-38's `Metadata` and phase-52's `Locality`/`LocalityWeight`), captured by an extended `extractEndpoints`; `manager.go`'s `buildCluster` gains a THIRD wrap-after-switch site (a `distinctPriorities` helper gates it to fire only for a multi-tier-priority cluster) with TWO new envoy-go-strict composition rejects (`priority`+`locality_weighted_lb_config`; `priority`+`lb_subset_config` — both confirmed live departures from a reference that MEANINGFULLY COMPOSES both, AMEND-P2) + the D-P-HEALTHALLOC health-registry widening (the D-LW-HEALTHALLOC precedent reapplied to the same two pre-existing stat names `membership_healthy`/`lb_healthy_panic`). TWO genuine issues were found and fixed DURING implementation: a Task 1 documentation-accuracy fix (wrong construction-site file names/counts corrected against the real worktree) and a Task 9/10 flake investigation — a pre-existing residual `0096` reference-side arm(a) flake (~1-in-20-to-1-in-60) root-caused, after THREE falsified hypotheses, to `warmupUntilStable`'s K-consecutive-success gate being satisfied BY CHANCE before genuine LB-state convergence, fixed by raising `warmupStable` from 10 → 60 (NOT a retry/sleep), then 80/80 clean. The final whole-branch review returned Ready-to-merge with 2 trivial cosmetic doc findings, one fixed in a final small commit (`df848be5` — the ADR-0270 §Context full-failover-arm label corrected 100%/0% → 0%/100%). The `0096-lb-priority` cross-side differential (2 tiers × 5 hosts + a driver-owned toggle responder) proves a hard 100%/0% static arm + a hard 0%/100% full-failover arm, 20/20 flake-free and `-race` clean via TWO controller-reperformed deliberate breaks. **ANCHORS ADR-0270** (§Decision/§Consequences landed in-place per ADR-0044). Counts at IMPL exit: stat surface **1200** (+0 — the family's FOURTH zero-stat-delta phase) / fixtures **98** (`0096-lb-priority`) / fuzzers **52** (unchanged) / BackendKind **38** (unchanged) / DECISIONS tail **ADR-0270** (next-free **ADR-0271**); ZERO new packages, ZERO new go.mod modules. **ROW 53 (`load-balancer-priority`) FLIPS `done`** (the sole leg, ADR-0106; NO parent rollup). The controller independently re-ran the full `go test ./... -count=1` suite (incl. `-race -short`), the full 98-dir differential (`ok ... 309.233s`, 98/98 green, zero regressions to the prior 97 dirs — the wrap-after-switch only fires for a multi-tier-priority cluster), and both deliberate breaks on the frozen HEAD before squashing.
- **A full cross-family ROADMAP audit confirms every chartered row is `done`, but THREE families still carry non-empty deferred-candidate lists** (see the full list below) — **Network-filters**, **Upstream-robustness**, and **HTTP-filters** are the families that have actually CLOSED (0 candidates remaining, confirmed by their own explicit "FAMILY CLOSED"/"family CLOSES" narrative in ROADMAP.md). Load-balancing, Observability, and Operational tooling are NOT closed.
- STATE.md/ROADMAP.md updated and pushed (row 53 `done`; the new active-phase entry ANCHORS ADR-0270). PROGRESS.md for phase 53 closed out with final re-verified counts.

=> The NEXT action is **NOT a new IMPL, NOT the termination sentinel.** A human must pick ONE candidate from ANY of the three open families below (or open an entirely new family) to charter the next BRAINSTORM. Until that pick is made, there is no active phase to advance — this router should be re-invoked once a human has chosen, at which point the chosen candidate's BRAINSTORM becomes "the NEXT action."

==============================================

## Deferred, not-yet-chartered candidates across ALL open families (a human picks ONE to open the next BRAINSTORM)

**Load-balancing family** (opened phase 34; most recently touched by phase 53, 2026-07-07) — 1 candidate remaining (priority load balancing consumed by phase 53):
- **Panic thresholds as an independent construct** — `healthy_panic_threshold` is currently reused UNCHANGED/UNEXTENDED by every LB phase (round_robin/least_request/random/ring_hash/maglev/subset/locality-weighted/priority) via the existing `clusterHealth.inPanic` gate (and, at phase 53, the `priorityLB` cluster-wide capacity-shortfall bypass reuses the same `lb_healthy_panic` counter); a dedicated row would explore making it independently configurable/observable (e.g. `Cluster.CommonLbConfig.healthy_panic_threshold` override semantics beyond the current default-gate reuse).

**Observability family** (opened phase 44; most recently touched by phase 50, 2026-07-05) — 5 candidates:
- **`graphite` stats sink** — the fourth `stats_sinks[]` TypeURL sibling (after `metrics_service`/`statsd`/`dog_statsd`), currently silently-ignored/rejected at the bootstrap sibling-reject arm.
- **OTLP-metrics stats sink** — another `stats_sinks[]` sibling, distinct from the already-landed OTLP-*traces*/OTLP-*logs* work.
- **The plain-`statsd` `tcp_cluster_name` transport** — deferred at phase 48 (ADR-0080 strict-reject; `statsd` is UDP-only in envoy-go today).
- **Tracing extras** — `custom_tags`, `spawn_upstream_span`, `http_service`, force-trace — deferred since the tracing sub-phases (46.1/46.2).
- **The tap filter** — an entirely new HTTP/network filter, not yet brainstormed at all (a bigger lift than the sink/tracing rows above).

**Operational tooling family** (opened phase 51, 2026-07-05 — its sole row so far) — 3 candidates:
- **xDS-sourced (not just static-file) dry-validation** — extending `validate.Bootstrap`/`BootstrapFile` to accept config sourced from a live xDS management server, not just a local file/reader.
- **An admin-API-exposed live-reload-and-validate endpoint** — expose bootstrap re-validation over the admin HTTP interface (`/validate` or similar), not just the CLI `--mode validate` flag.
- **An RTDS/SDS validate companion** — extend the validate surface to runtime-config (RTDS) and secret-discovery (SDS) payloads, mirroring the bootstrap-validate shape.

**Families CONFIRMED fully CLOSED (0 remaining candidates — do NOT pick from these unless reopening deliberately):**
- **Network-filters** — closed 2026-06-10 after phase 33 (thrift_proxy); all 9 candidates (echo, direct_response, sni_cluster, rbac_network, zookeeper, mongo, kafka_broker, redis, thrift) chartered and done.
- **Upstream-robustness** — closed after phase 43; all 5 candidates (active health checks, outlier detection, circuit breakers, retries+hedging, per-protocol connection pooling) chartered and done.
- **HTTP-filters** — closed at phase 25.3; all candidates chartered and done.

None of the open-family candidates above is pre-chartered. **Do NOT invent/charter one yourself** — this is a human decision. If a human has NOT picked one when this router is next read, ask which candidate (from any of the three open families, or an entirely new family) to charter, then open its BRAINSTORM per the project's standard lifecycle (`superpowers:brainstorming` first).

## Honor throughout (the discipline, unchanged for whichever phase comes next)
- **Lifecycle = one stage per session** (BRAINSTORM → SPEC → PLAN → IMPL).
- **Subagent-driven execution** (`feedback_execution_style`) in a **fresh worktree** (`feedback_git_worktrees`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit, re-runs the full suite on the frozen HEAD, does any deliberate-break verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). Per-task gates: `gofmt -l` + `golangci-lint` + `go vet` + `go build ./...` (`feedback_pertask_gofmt_lint`).
- **Process anchors:** ADR-0106 (per-leg rows) · ADR-0044 (ADR §Decision+§Consequences land at IMPL) · ADR-0045 (sub-split soft gate) · next-free **ADR-0271**.
- Re-verify baselines fresh at cold-start — do NOT trust any count in this file without re-running the baseline commands (`ls -d test/fixtures/[0-9][0-9][0-9][0-9]* | wc -l`, `grep -rh '^func Fuzz' --include='*.go' . | wc -l`, etc.) against current HEAD.
- **Before ever creating the `stop` sentinel**, re-run this same cross-family audit (grep `docs/envoy-go/ROADMAP.md` for "family STAYS OPEN"/"FAMILY CLOSED" per family, read each open family's most recent phase narrative) — do NOT assume this file's candidate lists are still current; a human may have chartered one since this was last written.

## Read first (in order), once a human has picked a candidate
1. THIS FILE — the cold-start router (none chartered across 3 open families; awaiting a human pick).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 53 IMPL done`) + the full history.
3. `docs/envoy-go/ROADMAP.md` — confirm row 53 `done`; read whichever family's section the human picked for its full deferred-candidate narrative.
4. The most recently landed phase in the SAME family as the human's pick (e.g. `docs/envoy-go/phases/53-load-balancer-priority/` for a Load-balancing pick, `docs/envoy-go/phases/50-stats-sink-dogstatsd-batching/` for an Observability pick, `docs/envoy-go/phases/51-bootstrap-validate-mode/` for an Operational-tooling pick) — useful as a shape template.
5. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — search for `reference_*`/`feedback_*` entries tagged for whichever family/topic was picked.
