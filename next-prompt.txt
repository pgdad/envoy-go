=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-VERIFIED 2026-07-23 — TWICE: once at the phase-73 BRAINSTORM close in the stage worktree, and AGAIN on LANDED master AFTER the squash-push, with byte-identical output (the phase-72 precedent — a stage worktree can disagree with what actually landed, so the post-push re-run is the one that counts). It does NOT fire. `stop` was NOT created.** Actual output:
- **(1) prints `NOT DONE: row 73`** — row 73 (`tracing-custom-tags-metadata-cluster`) was REGISTERED `in-progress` at the BRAINSTORM stage-close commit per the ROADMAP §Schema invariant, which **RE-OPENS check (1)**. (Check (1) going silent is the NORMAL post-IMPL state and is never on its own a termination condition; all THREE checks must be silent.)
- **(2) prints THREE live "candidates:" sentences** — HTTP/3, xDS, Observability. **Cite the command, never the adjective** (`reference_sentinel_deferred_sentence_live_vs_historical`): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` => **3**. NO sentence was narrowed at this BRAINSTORM (the phase-57 precedent) — and that was proven MECHANICALLY, not asserted: the ROADMAP diff is exactly **2 insertions / 1 deletion**, the family-paragraph edit is a **PURE APPEND** (the old line is a byte-prefix of the new), and the deferred sentence is **BYTE-IDENTICAL** across the diff.
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** => THREE families never opened. (The `WASM` line remains a ROADMAP **bookkeeping artifact** — `internal/wasm/` is a substantially-built wazero host and rows 25/25.1/25.2/25.3 are all `done` — re-flagged at the phase-73 BRAINSTORM §2.1 and again deliberately LEFT AS-IS, since the stage-close mechanics depend on this output.)

Checks (1)+(2)+(3) print => the sentinel does NOT fire. Abundant work remains.

==============================================


## STATUS AS OF 2026-07-23: THE PHASE-73 BRAINSTORM IS COMPLETE AND LANDED. ROW 73 (`tracing-custom-tags-metadata-cluster`) IS `in-progress`. THIS SESSION PERFORMS THE PHASE-73 SPEC.

**What the phase-73 BRAINSTORM landed** (`git log --grep 'phase 73'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): the roller's AUTONOMOUS self-pick (no human pick, per the 2026-07-12 standing directive) of **`tracing-custom-tags-metadata-cluster`** — the tracing `custom_tags` `metadata` type for the **`CLUSTER`** MetadataKind. It is the NINETEENTH Observability-family row *(chain ordinal — the inherited off-by-one is recorded in BRAINSTORM §2.1)*, the SEVENTH tracing `custom_tags` capability, and the **FOURTH and LAST** of the four `metadata` MetadataKinds — **this row CLOSES the quartet** (`REQUEST`@70, `ROUTE`@71, `HOST`@72, `CLUSTER`@73). It lifts the `tracing: custom_tags metadata tag %q cluster kind unsupported` reject (`internal/tracing/config.go:268-269`) and resolves the SELECTED CLUSTER's static `metadata.filter_metadata[ns]` onto the ingress SERVER span as a `{tag,value}` attribute on BOTH the OTLP and Zipkin exporters via the shared `ResolveCustomTags` -> `Span.Attrs []KV` seam. After it lands, the ONLY remaining reject arms are the two unset-kind ones (`config.go:232-233` DEPARTURE / `:287-288` PARITY — the phase-71 S3 split).

- **THE PICK RESTS ON A STALE-COST CORRECTION — AND THE CORRECTION ITSELF WAS SCOPED DOWN BY THE ADVERSARIAL PASS. Read the scope, not the headline.** `CLUSTER`'s inherited *"MODERATE ~11-13 tasks, RE-PAYS the 18-caller thread"* adjective — carried in phase 72's BRAINSTORM, the ROADMAP, STATE.md and the previous router — is **FALSE**. The `router.go:255` fact it rested on is TRUE (the router publishes only `Picked() cluster.Endpoint`); the cost consequence drawn from it is not. **TWO seams reach the cluster without a `routeAction` interface method — but only ONE of them also avoids the thread.** A BRAINSTORM draft claiming both did was refuted INDEPENDENTLY by BOTH adversarial verifiers before landing (BRAINSTORM §11.4), one by BUILDING the row, the other by reading the document against its own §6/§10. **So the cost correction is SINGLE-SOURCED, and the row is SMALL on seam A only.** This is still the SECOND CONSECUTIVE row where re-deriving a deferral's cost shrank the pick from MODERATE to SMALL (`reference_deferred_candidate_cost_restale`).
- **⚠️ THE LOAD-BEARING SPEC QUESTION — D-CTMC-SEAM. The two seams are NOT cost-equivalent and NOT behaviorally equivalent. READ THIS BEFORE COSTING ANYTHING.**
  - **Seam A (endpoint-carried).** `extractEndpoints` has exactly ONE non-test call site — `internal/cluster/manager.go:433` — with the `*clusterv3.Cluster` in scope (`:429`), so the owning cluster's `filter_metadata` can ride phase 72's OWN `Endpoint` retention pattern: a second aliasing field beside `filterMetadata` (`cluster.go:72-79`), an `Endpoint.ClusterMetaLookup(ns)` accessor beside `MetaLookup` (`cluster.go:111-117`), and `picked.ClusterMetaLookup` at the three emit sites. Zero 18-caller thread, zero `routeAction`/`route.go`/`actions.go` edits, and **the `weightedClusterRouteAction` problem DISSOLVES** (the picked endpoint belongs to the cluster the selector actually chose). **BUT it INHERITS the BEHAVIOR_CONTRACT B2 zero-`Endpoint` departure — and inherits it WIDER than HOST does**, because the cluster is selected by the route BEFORE any endpoint is tried, so "no endpoint acquired => no cluster metadata" is incoherent in a way it is not for HOST.
  - **Seam B (route-carried). ⚠️ IT RE-PAYS THE 18-CALLER THREAD — do not price it as diff-comparable to seam A.** `clusterRouteAction` holds a live `*cluster.Cluster` (the field at `internal/filter/hcm/actions.go:183`; `:182` is the struct header) and the matched `entry` is in scope at ALL 5 H1 emit CALL SITES (`dispatchRequest` spans `connection.go:312-780`, `entry` bound at `:319`) and ALL 7 H3 sites (`h3dispatch.go:125`). H2 is the only split and its carrier template is already landed — `h2dispatch.go:158` is literally `routeMetadata: entry.metadata`, fielded `:250`, consumed `:375`. **BUT `entry` does not exist inside the emit helpers, where the three `ResolveCustomTags` calls live** — so seam B needs a NEW parameter on all three emit signatures threaded through all 18 call sites (proven by execution: `accesslog_emit.go:57:7: undefined: entry`). **Does NOT inherit B2.** Costs the full threading tax, an H2 carrier field, a type assertion, and a **weighted-route hole** (`internal/filter/http/router/router_weighted.go:119` selects `a := actions[sel.pick()]` INSIDE the returned closure) needing a departure or an ADR-0080 reject.
  - **DECIDE ON PROBED REFERENCE BEHAVIOR, NOT DIFF SIZE.** The three arms that DISCRIMINATE are (i) an upstream connect failure, (ii) a no-route 404, (iii) a weighted route. A happy-path-only probe is consistent with BOTH seams and therefore proves nothing (`reference_probe_must_discriminate`). **A HYBRID is admissible** (seam A's retention + a route-side fallback when `picked` is zero) and must be PRICED, not assumed away.
  - **Ordering note:** if the deferred pick-time `picked`-propagation row (B2) ever lands first, seam A's objection largely evaporates. This row does not depend on it — but do not "fix" the same gap twice.
- **⚠️ A SECOND CORRECTION, TO A LANDED CODE COMMENT** (`reference_code_comment_not_evidence`). `internal/filter/hcm/accesslog_emit.go:51`/`:112`/`:173` read `UpstreamCluster: "", // not available at this seam`. That is TRUE of the function's parameter list and **FALSE of reachability one layer up** — every caller has `entry` (H1/H3) or can carry it (H2). A comment with the authority of proximity had been discouraging a cheap row. Two precision notes: it sits on `tracing.SpanInputs`, **not** the access-log record (`accesslog.Record` has no `UpstreamCluster` field at all), and a cost agent's cites for it DRIFTED (it re-derived `:52`/`:112`/`:172`; the controller re-grepped to `:51`/`:112`/`:173`). **The `upstream_cluster` span TAG remains a SEPARATE, unchartered feature — this row does not fix it**, and phase 72's category-error warning stands.
- **⚠️ NOTHING IN THE BRAINSTORM IS EXECUTION EVIDENCE.** Unlike phase 72's, this BRAINSTORM did **NOT** build the row in scratch. The seam analysis is a READ-ONLY derivation and **no probe has confirmed that either seam compiles**, let alone produces a green cross-side assertion. BRAINSTORM §11.1 says so explicitly. Nothing there may be cited as buildability evidence.
- **The one genuinely NEW cost versus phase 72: a MANDATORY `meta_bad` fuzz repoint.** Phase 72's fuzz delta was purely additive because it removed the HOST arm, which no seed pointed at. Phase 73 removes the **CLUSTER** arm — exactly what `meta_bad` exercises — and after this row the only rejecting arms left are the two unset-kind ones. So: ADD a `meta_cluster_ok` accept seed AND REPOINT `meta_bad` to a nil/absent-kind arm, dispatch-verifying BOTH with the discrimination stated BEFORE the run (phase 72's T5 is the template, including the first-error-return trap: seeing the LATER seed's line at all proves the earlier one did not short-circuit).
- **Counts at exit (re-run MECHANICALLY in the worktree; docs-only, so all UNCHANGED):** fixtures **118** (tail `0116-tracing-custom-tags-metadata-host`) / fuzzers **55** / stat surface **1201** / BackendKind **38** / go.mod modules **2** (lineage figure; the single `go.mod` requires **67**) / DECISIONS tail **ADR-0294** COMPLETE (next-free **ADR-0295**). A contested fixture count (116/117/118 from three commands) was RESOLVED rather than silently picked — the canonical `ls -d test/fixtures/[0-9]*/ | wc -l` gives **118**; 116 excludes the suffixed siblings `0007a`/`0007b`, and 117 is the PRE-phase-72 figure.

## What THIS session does — the phase-73 SPEC

One stage per session. The SPEC **RE-DERIVES the BRAINSTORM rather than executing it** — a BRAINSTORM cite is not evidence (`feedback_brief_citations_not_evidence`), and anchors shift (`feedback_parallel_stream_mints_fresh_drift`: re-derive rosters at the SPEC tip, since a post-BRAINSTORM commit can re-mint retired drift).

**The owed roster:**
1. **RESOLVE D-CTMC-SEAM on PROBED reference behavior** — the load-bearing question above. This is the row's single most important deliverable.
2. **Run the D-CTMC-* live-probe arms** against `envoyproxy/envoy:contrib-v1.37.2`: D-CTMC-WIRE / D-CTMC-ZIPKIN-WIRE / D-CTMC-VALUE-SERIALIZE / D-CTMC-DEFAULT / D-CTMC-NOCLUSTER / D-CTMC-REJECT. Probe hygiene: a fresh container per arm (`feedback_probe_fresh_container_per_arm`), a bridge network (`reference_docker_probe_bridge_network`), the host-gateway literal + `dns_lookup_family: V4_ONLY` (`reference_host_gateway_ip_docker_desktop`), decode-ran asserted per arm, and every arm must DISCRIMINATE.
3. **RE-DERIVE every BRAINSTORM §6 edit site at the SPEC tip**, including: `kindMetadataCluster` APPENDED at **`iota == 6`** never inserted (a silent renumber fails NOWHERE loudly — phase 72's Break G is the precedent that catches it); the `resolve_test.go` caller count (raw `grep -c 'ResolveCustomTags(' ` => **50**, but phase 72's raw 33 held ONE `t.Errorf` STRING LITERAL => re-grep and exclude literals by an EXPLICIT LINE GATE, do NOT adopt the raw count); the `/cluster` reject-kind tests the row must FLIP (phase 72's `/host` set was EXACTLY TWO — enumerate the `/cluster` set at tip rather than assuming the same count); and the collision check for `kindMetadataCluster`/`clusterMetaLookup`/`ClusterMetaLookup` (ZERO hits at the BRAINSTORM tip — re-run anyway, `reference_spec_drafted_identifier_collision_check`).
4. **D-CTMC-STALECOMMENT — the repo-wide sweep.** This row lifts the LAST reject arm, so every comment enumerating rejecting kinds goes false. Phase 72 found NINE and they were a real defect class. Run the sweep; do not assume phase 72 caught them all.
5. **D-CTMC-FIXTURE** — `0117-tracing-custom-tags-metadata-cluster` on the `0116` chassis, source relocated to `clusters[].metadata`, **STRING values only** (`reference_structpb_tag_cross_side_string_only`: arbitrary reference key order, number-precision drift, Zipkin empty-value drop). **NAME the selected-vs-only-cluster limit UP FRONT** in `expectations.yaml` rather than having review extract it — that was the `0116` lesson. If seam B is chosen, the fixture SHOULD carry an arm proving resolution on a path where no endpoint was acquired, or the seam choice is unprovable.
6. **Draft ADR-0295 §Context** per ADR-0044 (the DECISIONS tail flips ADR-0294 -> ADR-0295 AT the SPEC commit; §Decision + §Consequences complete IN PLACE at the IMPL, no renumber; next-free after it is ADR-0296).

**Stage-close mechanics (the SPEC):** row 73 STAYS `in-progress` (it flips `done` only at the phase-73 IMPL six-gate, ADR-0106, the SOLE leg); **NO deferred sentence narrowed at ANY pre-IMPL stage** (phase-57 precedent); DECISIONS tail flips to **ADR-0295** (PROPOSED, §Context only — verify `grep -c '^### Decision'` inside the block is **0** so the IMPL's APPEND is sound); ROADMAP UNTOUCHED; STATE §Current rolled **IN PLACE** (lifecycle 1 -> 2; lineage re-capped at five **with its PREAMBLE updated** — the ADR-0288 rule, and a stale preamble was a REAL defect caught during phase 72); next-prompt.txt rolled to the phase-73 PLAN; every count re-run MECHANICALLY in the worktree, never copied.

**⚠️ A ROADMAP HAZARD FLAGGED NOW, FOR THE IMPL (not the SPEC).** The `CLUSTER` candidate rolls OUT of the live Observability `candidates:` sentence at the row-done edit — but **`CLUSTER` is the LAST item in its clause**, so that narrow REMOVES the clause rather than shortening it. The IMPL must confirm the sentence still parses and still carries at least one candidate, or sentinel check (2) changes count.

Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). A SPEC is **docs-only, ZERO production `.go`** (SPEC.md + the ADR-0295 §Context append).

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD — this bit phase 72 for real, and it FIRED AGAIN this session** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root** (`Shell cwd was reset to /home/esa/git/envoy-go`). At the phase-72 close that put a stray commit on `master` in the MAIN checkout AND made a "final gates" run validate master's pre-row tree — GREEN while proving nothing. **Use `git -C <abs-worktree-path>` for every git command, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline — exactly ONE live `next-skill:`/`lifecycle-state:`/`next-free ADR:`/count block) + §Project counts + the §Recent lineage (five bullets: phase-73 BRAINSTORM + phase-72 IMPL/PLAN/SPEC/BRAINSTORM).
2. `docs/envoy-go/phases/73-tracing-custom-tags-metadata-cluster/BRAINSTORM.md` — **§2.4 (D-CTMC-SEAM, the load-bearing question) and §7 (the D-CTMC-* docket) FIRST**, then §2.1/§11 (the pick + the fully-adjudicated rejected roster), §6 (anticipated edit sites), §11.1 (**what was NOT verified — read this before citing anything as evidence**), §11.2 (the controller's re-derivation of the agents' claims, incl. the one DRIFT), §12 (stage-close mechanics).
3. `docs/envoy-go/phases/72-tracing-custom-tags-metadata-host/` — `SPEC.md` (the live-probe docket format + §12/§13 deferred rosters), `PLAN.md`, `PROGRESS.md` (the per-task adversarial record, incl. the B2 departure retraction — the class of error to avoid).
4. `docs/envoy-go/ROADMAP.md` — §Schema (the row-registration invariant), the three live `candidates:` sentences, and row 72's closing summary as the shape a row-done cell should take.
5. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the tracing `custom_tags` section (B1-B4 as landed) + the departure roster. **B2's EXACT scope matters to this row's seam choice.**
6. `docs/envoy-go/DECISIONS.md` tail — **ADR-0294** (the HOST precedent, COMPLETE) + **ADR-0293**/**ADR-0292** (ROUTE/REQUEST) + ADR-0044 (§Context drafts at the SPEC) · ADR-0045 (the split valve) · ADR-0080 (envoy-go-strict) · ADR-0106 (row-done at the IMPL six-gate) · ADR-0288 (the STATE in-place rule).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — trust MEMORY.md's index; esp. `feedback_brief_citations_not_evidence`, `reference_quoting_is_not_executing`, `reference_deferred_candidate_cost_restale`, `reference_a_drift_correction_is_itself_a_claim`, `reference_code_comment_not_evidence`, `reference_probe_must_discriminate`, `reference_parallel_subagents_private_scratch`, `reference_spec_drafted_identifier_collision_check`, `reference_structpb_tag_cross_side_string_only`, `reference_sentinel_deferred_sentence_live_vs_historical`, `reference_cluster_race_outlier_flake`, and **`reference_bash_cwd_reset_commits_to_main`**. **THREE are load-bearing for THIS row specifically:** `reference_endpoint_metalookup_host_metadata_seam` (the per-MetadataKind accessor roster and WHY phase 72 skipped the 18-caller thread — the seam-A template, and its two traps: a method value on the ZERO `Endpoint` is NON-nil, and a nil-guard break must assert on the ACCESSOR'S OWN return or it is vacuous), `reference_envoy_lb_namespace_not_privileged` (ANY `filter_metadata` namespace is addressable — so the fixture must NOT assume `envoy.lb`), and **`reference_host_tag_resolves_at_pick_time`** (the EXACT scope of the B2 departure seam A would inherit — post-ACQUIRE 502s are PARITY, the 404-no-match arm emits NO SPAN AT ALL, and partitioning by `picked` alone is what produced two FALSE parity claims in a phase-72 draft; **partition by SPAN CAPABILITY instead**).
