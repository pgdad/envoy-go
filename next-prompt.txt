START a fresh cold-start session for envoy-go — **the phase-36 (`load-balancer-ring-hash`) SPEC is DONE and LANDED on master; the FINAL ADR-0045 decision CONSUMES the 36.1/36.2 by-plane split; the next lifecycle step is the phase-36.1 PLAN** (`superpowers:writing-plans` — decompose the SPEC §10 **36.1** spine into bite-sized TDD tasks; the kafka-31/thrift-33/least_request-34/random-35 PLAN precedent). The substantive master tip is the phase-36 SPEC squash **`273579d`** (`phase 36 SPEC: load-balancer-ring-hash — the Ketama consistent-hash ringHashLB EXTENDS the ADR-0232 seam's PICK-INPUT half via a ctx-carried hash key; D-RH1..D-RH8 executed; the FINAL ADR-0045 CONSUMES the 36.1/36.2 split; 2 ADRs 0235 seam + 0236 policy; +3 ring_hash_lb gauges; per-side affinity`; pushed to `origin/master`; confirm via `git log --oneline -3`). It carries `docs/envoy-go/phases/36-load-balancer-ring-hash/SPEC.md` (the 14-§ authoritative phase SPEC) + README.md (status 1 → 2) + the STATE.md advance (active-phase → `phase 36 (load-balancer-ring-hash) SPEC done`; next → the 36.1 PLAN) + the ROADMAP row-36 SPEC-done note (the split CONSUMED). The literal live master tip is this docs-only `next-prompt.txt` SHA-fill roll-forward commit (the substantive SPEC squash `273579d` is its parent; confirm via `git log --oneline -3`); the substantive predecessor below `273579d` is the phase-36 BRAINSTORM squash `ffb3a61`, and below that the phase-35 IMPL squash `4618841`.

## What this session does — author the phase-36.1 PLAN

The phase-36 SPEC is COMPLETE (the D-RH1..D-RH8 empirical pins executed IN-SESSION; spec-document-reviewer GREEN). The next session authors `docs/envoy-go/phases/36-load-balancer-ring-hash/PLAN.md` (the 36.1 leg) via `superpowers:writing-plans`, decomposing the SPEC §10 **36.1 task spine** (9 indicative tasks) into bite-sized TDD steps, and re-checking the ADR-0045 gate for the 36.1 leg (anticipated NO further split — ~360 prod LoC for 36.1 alone, well under the gate). The 36.2 PLAN follows AFTER the 36.1 IMPL (its own SPEC→PLAN→IMPL, per the consumed split).

## The SPEC-settled design (do NOT re-litigate — the PLAN decomposes §10's 36.1 spine into TDD tasks)

- **The seam EXTENSION (ADR-0235; AMEND-RH7)** — the unexported `loadBalancer.Pick` widens to **`Pick(hashKey uint64, hasHash bool) (Endpoint, func(), error)`** (the `(value, ok)` idiom; chosen over `*uint64`/`pickOptions`). The three incumbent policies (roundRobin/leastRequest/randomLB) add `_ uint64, _ bool` IGNORED params (behavior-neutral — all 62 fixtures byte-identical). The key rides `ctx`: an unexported `hashKeyCtxKey struct{}` + an EXPORTED additive `cluster.WithHashKey(ctx, key)` + an unexported `hashKeyFrom(ctx) (uint64, bool)`, all in `cluster.go` (the ADR-0219 ctx-carry precedent). `Dial`/`AcquireH1` extract+thread in 2 changed lines each; their EXPORTED signatures + `PickEndpoint()` (passes `(0,false)` → no-hash fallback) stay BYTE-STABLE. Only the two hash-producing consumers churn (tcp_proxy, the HTTP router); the others (thriftproxy/redisproxy/grpcclient/httpclient/dial_h2) thread ctx unchanged onto the no-hash path.
- **The `ringHashLB` policy (ADR-0236; AMEND-RH2)** — a Ketama ring built ONCE: per endpoint `ceil(minimum_ring_size/N)` entries (equal-weight MVP), each keyed `xxHash64("<addr>:<port>_<i>")` (the default `XX_HASH`; `murmurHash2(key, 0xc70f6907)` for `MURMUR_HASH_2`), sorted ascending. `Pick(hashKey, hasHash)` binary-searches the first ring point `>= hashKey` (`sort.Search`, wrap to 0); `hasHash==false` → `hashKey = rng()` (a uniform ring position — Envoy's no-hash fallback). REUSES the ADR-0232 RELEASE half unchanged (returns `noopRelease`). A pure-Go **`xxHash64` (seed 0, ~45 LoC, new `xxhash.go`)** is hand-rolled (`cespare/xxhash` absent from go.mod; a new dep for one call site is unwarranted — AMEND-RH7); the digest must match the reference for the ring to be a faithful reproduction.
- **Manager acceptance + the two-layer gate (AMEND-RH1/RH5)** — `case clusterv3.Cluster_RING_HASH` + `parseRingHashLbConfig` (self-supplies the 1024/8388608 defaults; mirrors the reference's TWO reject layers: the PGV `value must be less than or equal to 8388608` on each over-cap bound AND the runtime `ring hash: minimum_ring_size (N) > maximum_ring_size (M)` for min>max — PGV has NO min≤max rule); the reject TEXT extends `…, RANDOM` → `…, RANDOM, RING_HASH`; `TestManager_Error_UnsupportedLBPolicy` (`manager_test.go:320`) RETARGETS its trigger `Cluster_RING_HASH` → `Cluster_MAGLEV` + the new substring. The RingHashLbConfig rejects land UNIT-LEVEL (the phase-34 `choice_count` precedent — NO boot-reject dir).
- **The tcp_proxy `source_ip` plane (36.1; AMEND-RH3)** — `TcpProxy.hash_policy` parse + `key = xxHash64(ipOnly(downstream.RemoteAddr()))` (bare IP, NO port) + `cluster.WithHashKey(ctx, key)` before `eff.Dial(ctx)` (`internal/filter/tcpproxy/filter.go:127`).
- **+3 MIRRORED gauges (AMEND-RH4 — REFUTES the BRAINSTORM zero-delta)** — `cluster.<name>.ring_hash_lb.{size,min_hashes_per_host,max_hashes_per_host}` set once at ring build (default 3-host: 1026/342/342); surface 1116 → **1119** at the 36.1 IMPL; cross-side-EXACT (keyed on ring-config + host count only, NOT addresses) → a strong `StatsAsserter` cross-equal prong.
- **The differential `0061-lb-ring-hash` (AMEND-RH8 — cross-side host identity INFEASIBLE)** — chain `[tcp_proxy]` both sides, 3-endpoint RING_HASH cluster + `hash_policy:[{source_ip:{}}]`, `TCPEcho` backends (NO new BackendKind). The driver binds the SUBJECT to source IPs `127.0.0.2..5` (16/IP, 64 total — `net.Dialer{LocalAddr}`, proven feasible §11.8): a SUBJECT-SIDE affinity(EXACT, per-source-IP backend cardinality == 1)+spread(`>= 2` distinct) via `DistributionAsserter`; the reference (Docker-NAT'd to one source IP) is asserted on byte-equivalence + cross-side `StatsAsserter` (the 3 gauges cross-equal + cx/membership/quiesced; per-side `upstream_rq_total`). The affinity leg is DETERMINISTIC/EXACT (no σ-band); the spread is a coarse `>= 2` count. Deliberate-breaks (`-count=1`): scatter-the-key (affinity fails), collapse-the-ring (spread fails), drop-Inc / corrupt-gauge (stats fails).

## The 36.1 PLAN spine (SPEC §10 — decompose each into TDD steps)

1 baselines/anchors gate (fixtures 62, fuzzers 42, surface 1116, BackendKind 33, DECISIONS tail ADR-0234; the as-built anchors) + PROGRESS.md · 2 the pure-Go `xxHash64` (vs published vectors + the live D-RH4b key→host mapping) · 3 the seam extension (widen `Pick`; the 3 ignored-param policies byte-unchanged; the `WithHashKey`/`hashKeyFrom` helpers; thread `Dial`/`AcquireH1`/`PickEndpoint`) · 4 the `ringHashLB` (ring build + binary-search lookup + no-hash fallback + the 3 gauge values) · 5 manager acceptance + the two-layer gate + the reject text + the `manager_test.go` retarget + register the 3 gauges · 6 the tcp_proxy `source_ip` plane · 7 the `0061` fixture (subject-side affinity+spread + cross-side stats) · 8 deliberate-break liveness + ≥20-run flake check · 9 full differential re-verify (62 dirs byte-exact + `0061` green; surface 1119, fixtures 63) + the BEHAVIOR_CONTRACT 36 bundle + the ADR-0235 + ADR-0236 bodies (ADR-0044 in-place; tail → ADR-0236) + STATE/ROADMAP advance + the six-gate.

## Counts at master tip (phase 36 SPEC done — confirm at the 36.1 PLAN/IMPL baseline)

- stat surface **1116** (→ **1119** at the 36.1 IMPL — +3 `ring_hash_lb.*` gauges; AMEND-RH4; the FIRST non-zero LB-stat delta).
- differential fixtures **62** (tail `0060-lb-random`; `ls -d test/fixtures/[0-9]* | wc -l`). → **63** at the 36.1 IMPL (`0061-lb-ring-hash`) → **64** at the 36.2 IMPL (`0062-lb-ring-hash-http`); NO boot-reject dir.
- fuzzers **42** (tail `FuzzThriftDecode`). → **42** (no wire decode; a ring-lookup property test is unit-level).
- BackendKind tail **33** (`TCPThriftResponder`). → **33** (`TCPEcho` + existing HTTP backends reused).
- DECISIONS tail **ADR-0234** (next-free **ADR-0235**). The ADR-0235 (seam) + ADR-0236 (policy) §Context drafts are anchored in SPEC §13; the full bodies land at the 36.1 IMPL per ADR-0044 (tail → ADR-0236).
- reference pin: `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227).
- Toolchain: Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); go-control-plane `/envoy` v1.32.4 (ADR-0008) — ZERO new dep (D-RH1; xxHash64 hand-rolled, not a dep).

## Read first (in order)

1. THIS FILE — the post-phase-36-SPEC cold-start (next: the phase-36.1 PLAN).
2. `docs/envoy-go/phases/36-load-balancer-ring-hash/SPEC.md` — the authoritative phase SPEC (§1.1 AMEND-RH1..RH8; §3 the policy + seam-extension design; §3.0 the CONSUMED-split decision; §6 the two-layer gate; §7 the +3 gauges; §8 the affinity+spread fixtures; §10 the 36.1/36.2 PLAN spine; §11 the D-RH1..D-RH8 empirical-pin block; §13 the ADR-0235 + ADR-0236 §Context drafts).
3. `docs/envoy-go/phases/36-load-balancer-ring-hash/BRAINSTORM.md` — the settled design + the §10 D-RH obligations the SPEC executed.
4. `docs/envoy-go/STATE.md` — active-phase `phase 36 (load-balancer-ring-hash) SPEC done`; the lifecycle → the 36.1 PLAN routing; the counts.
5. `docs/envoy-go/ROADMAP.md` — row 36 `in-progress` (split CONSUMED) + the Load-balancing family-heading.
6. `docs/envoy-go/DECISIONS.md` — **ADR-0232** (the LB seam — its §Consequences PICK-INPUT-half anticipation phase 36 CONSUMMATES) + ADR-0233 (least_request — the `parseLeastRequestLbConfig` gate precedent) + ADR-0234 (random) + ADR-0024/0045/0044/0052/0080/0106/0219/0063.
7. `internal/cluster/{loadbalancer,leastrequest,random,manager,cluster}.go` (the seam `Pick()` funnel the SPEC widens + `noopRelease` + `parseLeastRequestLbConfig` + `newPCGRNG` + `manager.go:235` switch + `:257` reject text + `manager_test.go:320`) + the two consumers `internal/filter/tcpproxy/filter.go` (the `Dial(ctx)` site) + `internal/filter/http/router/router.go`.
8. The phase-34/35 phase directories (the family-template + the as-built precedents) + `test/fixtures/0059-lb-least-request/` + `0060-lb-random/` (the per-side band precedent the 0061 fixture follows).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_execution_style`, `feedback_git_worktrees`, `feedback_subagents_no_push`, `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_path_targeting`, `reference_differential_break_protocol_count1`, `reference_differential_asserter_dispatch`, `reference_differential_run_selector`, `reference_differential_band_sigma_margin`, `reference_differential_fixture_dispatch_constraint`, `reference_docker_probe_bridge_network`.

## Honor throughout

- `feedback_execution_style` (subagent-driven IMPL) · `feedback_git_worktrees` (work in a worktree) · `feedback_subagents_no_push` (subagents commit LOCAL-ONLY; the controller squash-merges + pushes at stage-close) · `feedback_push_to_origin` (after a clean landing on master with tests green, push without asking) · `feedback_pertask_gofmt_lint` · `feedback_subagent_worktree_path_targeting`.
- ADR-0106 (flat family row per LB policy — NO parent rollup) · ADR-0045 (the split-gate — re-check per sub-phase at the 36.1 PLAN) · ADR-0044 (ADR bodies at the IMPL, in-place) · ADR-0052 (the atomic-landing six-gate) · ADR-0080 (byte-stable parse-reject wording) · ADR-0227 (the contrib reference image; live probes per `reference_docker_probe_bridge_network`).
- The lifecycle is BRAINSTORM → SPEC → PLAN → IMPL, each its own session. The next session is the phase-36.1 **PLAN** (`superpowers:writing-plans` — decompose SPEC §10's 36.1 spine; the ADR-0045 gate re-check). 36.2 follows after the 36.1 IMPL (its own SPEC→PLAN→IMPL).
