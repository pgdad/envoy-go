START a fresh cold-start session for envoy-go — the **phase 26.3 PLAN** (`network-filter-rbac`) is DONE + plan-document-reviewer APPROVED + squash-merged + pushed; OPEN the **phase 26.3 IMPL** (`superpowers:subagent-driven-development` per `feedback_execution_style`), EXECUTING the 15-task bite-sized TDD PLAN at `docs/envoy-go/phases/26.3-network-filter-rbac/PLAN.md`. 26.3 is the THIRD and FINAL sub-phase of the phase-26 §9 Network-filters family; its phase-done flips parent row 26 `in-progress → done` ATOMICALLY (the 18/19/22/24/25 ROLLUP).

Phase 26.3 PLAN landed at squash `331c239` (pushed to origin/master): the 15-task TDD decomposition of the 26.3 SPEC §10 task spine; plan-document-reviewer APPROVED first iteration (one advisory + two recommendations FOLDED IN); ADR-0045 split-gate RE-CHECKED NO split (15 tasks / ~630-890 net-new LoC — the ~790 moved engine LoC is mechanical relocation re-verified byte-exact by the phase-16 fixtures [R4], NOT churn); PLAN-time D-26.3-1/-3/-7 resolved. ADR-0216 + ADR-0217 + ADR-0218 §Context already drafted in DECISIONS.md (tail ADR-0218; next-free ADR-0219); the §Decision/§Consequences bodies LAND at this IMPL (Task 15, in place per ADR-0044 — no new ADR number consumed).

## What this session must do: EXECUTE the phase-26.3 PLAN (IMPL)

Per SKILL_ROUTING (sub-row 26.3 at state 3 — PLAN.md exists, implementation incomplete → `superpowers:subagent-driven-development`), execute the 15 TDD tasks at `docs/envoy-go/phases/26.3-network-filter-rbac/PLAN.md`: fresh subagent per task + two-stage review; TDD-first on every task (write-failing-test → run-fails → minimal-impl → run-passes → commit). Subagents commit LOCAL-ONLY (`feedback_subagents_no_push` — a stray subagent push polluted origin/master in 25.3 IMPL); the controller squash-merges + pushes at stage-close. Append to `docs/envoy-go/phases/26.3-network-filter-rbac/PROGRESS.md` on each task completion. Work in a git worktree (`feedback_git_worktrees`; mirror `.worktrees/phase-26.3-network-filter-rbac-impl` off master). DO the full six-gate at Task 15 + the phase-done ROLLUP.

## The 15-task PLAN spine (the source of truth is PLAN.md — read it task-by-task)

1. First-action baselines + proto/anchor re-confirm (HARD GATE): re-grep fuzzers **35** / fixtures **44** (tail 0042) / stat surface **132** / DECISIONS.md tail **ADR-0218** via git-tracked enumeration; re-confirm the network-rbac TypeURL via `proto.MessageName` (memory `reference_network_filter_typeurl_extensions`); re-pin the engine + framework line anchors against the IMPL-session tip (the SPEC/PLAN anchors drifted slightly — Task 1 catches it); re-confirm AMEND-A6 (HTTP rbac emits zero dynamic metadata); six gates green at the tip.
2-3. The `internal/rbac/` engine MOVE: Task 2 the evaluator surface (`EvalContext` + the ~23 evaluators + `matchString`/`matchHeader`/`matchPath`/`matchCidr` + the `build*` funcs); Task 3 the compilers (`BuildRulesEngine`/`BuildMatcherEngine`) + the dual `Evaluate` entries + the `matcherCtxAdapter` bridge + `PerPolicyCounters` (the moved `incPolicy`/`sync.Map` — D-26.3-7). MECHANICAL move + export-rename; the moved unit tests are the failing→passing net; the HTTP package is intentionally build-broken between Tasks 2-3 and Task 5 (verify scoped to `./internal/rbac/...`).
4. The input-capability `Profile` (`ProfileHTTP`/`ProfileL4`) + `profile.permits(armKind)` at the HTTP-only arms in `buildOnePermission`/`buildOnePrincipal` (D-26.3-1; rules-path is the guaranteed reject target — the matcher-path carries no rbac-permission/principal leaf, its HTTP-only reject is vacuously satisfied, recorded in PROGRESS.md).
5. Consumer #1 migration: flip `internal/filter/http/rbac/` to import the aliased engine (`rbacengine`), pass `ProfileHTTP`, hold engine types + `*rbacengine.PerPolicyCounters`; `go build ./...` green again + import-cycle clean (D-26.3-6).
6. R4 re-verification gate: the phase-16 HTTP-rbac differential fixtures byte-exact green LIVE after the engine move (engine-correctness, NOT metadata — AMEND-A6) + the engine/consumer race gate.
7. The `NoFlush` close distinction (F3; D-26.3-2): `connection.Close(ct)` records the `CloseType` + `serveNetworkChain` honors it; a unit test distinguishes NoFlush from FlushWrite.
8-12. The `rbac_network` package (`internal/filter/network/rbac/`): Task 8 parse + `NewFactory(reg)` (closure-captured `*stats.Registry` — D-26.3-3) + the 4 static `<stat_prefix>.rbac.*` counters (`ProfileL4`; `stat_prefix` required; `delay_deny` reject); Task 9 the L4 `EvalContext` from `network.Connection`; Task 10 the OnData decision (ONE_TIME + CONTINUOUS; allow→Continue, enforced-deny→`rbac_deny_close`+`Close(NoFlush)`+StopIteration; **OnNewConnection MUST return Continue** per memory `reference_network_read_filter_onnewconnection_halts` — a StopIteration there sets sticky `connHalted` blocking all OnData; the sticky-halt regression test is mandatory); Task 11 the shadow-metadata writes (`Set("envoy.filters.network.rbac", "shadow_engine_result"/"shadow_effective_policy_id", …)`) + the `internal/dynamicmetadata/` doc generalization in place (ADR-0217/0044; NO code change to the package — R5); Task 12 register the 5th built-in in `builtins.RegisterBuiltins` + boot smoke (`[rbac_network, tcp_proxy]` — the first production mixed read→terminal chain) + the per-package 4-counter stat pin.
13. The 36th fuzzer `FuzzNetworkRBACConfigParse` (config-parse; model on the network direct_response fuzzer).
14. The +2 differential fixtures (`reference_differential_asserter_dispatch` + `reference_differential_fixture_dispatch_constraint`): the cross-side `00XX-network-rbac` (allow/deny/shadow with **StatsAsserter**, the first LIVE mixed read→terminal chain — R-M-LIVE; the differential bootstraps need a cluster) + the SEPARATE `00XX-network-rbac-boot-reject` dir (stat_prefix-missing PGV-mirror). PROVE the assertions are live (deliberate-break). The subject-side-only rejects (HTTP-only matcher, `delay_deny`) are UNIT-tested, NOT cross-side. Numbers pinned at IMPL (next free 0043/0044).
15. The BEHAVIOR_CONTRACT 26.3 bundle (§9/§14; stat table 132→136) + the ADR-0216/0217/0218 §Decision/§Consequences bodies (in place; tail STAYS ADR-0218) + the byte-stable reject-wording `TestParseRejectConstants_ByteStable` pin (D-P6) + STATE/ROADMAP phase-done advance + the parent-row-26 ROLLUP + the six-gate verification (build/vet/lint/`-race -short` + the FULL 46-fixture differential incl. the R4 phase-16 fixtures LIVE + conformance 10/10 + h2spec 53/53).

## Project state at master tip

- **Master tip:** `331c239` (the 26.3 PLAN squash) or a following next-prompt master-tip repoint (+1, expected, not chased). Confirm the live tip via `git log --oneline -1`. origin/master in sync after push. STATE.md `last-commit` points at the 26.3 PLAN squash.
- **§9 Network-filters family:** parent row 26 `in-progress`; 26.1 `done`; 26.2 `done`; 26.3 `in-progress` (SPEC + PLAN done; IMPL pending). Parent row 26 flips `in-progress → done` ATOMICALLY with 26.3 at 26.3 phase-done per the ROLLUP (18/19/22/24/25).
- **Counts at master tip:** stat surface **132** (→136 at 26.3 IMPL phase-done); differential fixtures **44** tail **0042** (→46); fuzzers **35** (→36); conformance 10/10; h2spec 53/53; DECISIONS.md tail **ADR-0218** (§Context for 0216/0217/0218 drafted; §Decision/§Consequences bodies land at THIS IMPL); next-free **ADR-0219**.
- **Toolchain:** Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); reference Envoy v1.37.2 (ADR-0008); go-control-plane v1.32.4 (ADR-0008).

## Read first (in order)

1. THIS FILE — the phase-26.3 IMPL cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 26.3 PLAN done; awaiting 26.3 IMPL`; next-skill `superpowers:subagent-driven-development`; next-free `ADR-0219`; last-commit the PLAN squash.
3. `docs/envoy-go/phases/26.3-network-filter-rbac/PLAN.md` — the 15-task TDD PLAN this session EXECUTES (the ADR-0045 split-gate check; the PLAN-time D-question resolutions; the engine-extraction MOVE/STAY design pin; the File Structure tables; Tasks 1-15 with full code/commands/expected-output).
4. `docs/envoy-go/phases/26.3-network-filter-rbac/SPEC.md` — the SPEC the PLAN decomposes (§3 engine extraction; §4 rbac_network + sink; §6 PARSE-REJECT; §7 stats; §8 fixtures; §11.1 D-S2 as-built surface; §12 D-26.3-1..7; §15 acceptance) + `docs/envoy-go/phases/26-network-filter-chain-and-rbac/SPEC.md` (parent — the inherited §5/§6/§7/§8 rosters + AMEND-A1..A11).
5. The CONSUMED + EXTRACTED surface: `internal/filter/http/rbac/{evaluator,rbac}.go` (the engine to MOVE — the `evalContext`@~60-121, the evaluators, the builders@~373/445, the matcher bridge@~866-897, `incPolicy`@~210-222, the L4 stubs@~965-1001, CEL silent-ignore@~420-424); `internal/filter/network/{callbacks,chain}.go` (the `Connection` accessors, the `CloseType`/NoFlush touchpoint, the `SetResponseCodeDetails` sink, the `prefixConn` handover); `internal/filter/network/builtins/builtins.go` (the 5th-built-in seam + `Deps.StatsRegistry`); `internal/filter/network/{echo,directresponse}/` (the factory + SetResponseCodeDetails-type-assert + Close-CloseType patterns); `internal/dynamicmetadata/{dynamicmetadata,doc}.go` (the connection-scoped reuse + the doc to generalize); `internal/listener/manager.go` `serveNetworkChain`@~1025 (the mixed read→terminal dispatch + the pure-read close).
6. `docs/envoy-go/DECISIONS.md` ADR-0216/0217/0218 (the §Context this IMPL fills — §Decision/§Consequences land at Task 15) + ADR-0213/0214/0215 (the framework) + ADR-0040 (CEL silent-ignore) + ADR-0044 (in-place ADR-body discipline) + ADR-0052 (atomic doc bundle) + ADR-0106 (ROADMAP flip); `docs/envoy-go/ROADMAP.md` rows 26/26.3; `BOOTSTRAP_PROMPT.md` §5 + §7.5; `docs/envoy-go/SKILL_ROUTING.md`.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — execution-style + worktree + push + subagents-no-push + network-filter-typeurl-extensions + network-read-filter-onnewconnection-halts (the `rbac_network` Continue-from-OnNewConnection constraint) + differential-asserter-dispatch + fixture-dispatch-constraint all apply.

After the 26.3 IMPL is complete + verified (state 3 → 4 → 5: `superpowers:verification-before-completion` + `superpowers:requesting-code-review` → REVIEW.md APPROVED) + squash-merged + pushed, advance STATE.md per BOOTSTRAP §5/§7.5 (sub-row 26.3 `in-progress → done` + parent row 26 `in-progress → done` ROLLUP) and rewrite next-prompt.txt for the next §9 Network-filters-family selection (6 candidates remain: redis/mongo/kafka_broker/thrift/zookeeper/sni_cluster) — likely a fresh phase-NN BRAINSTORM cold-start.
