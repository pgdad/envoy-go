=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action. (The sentinel does NOT apply right now — phase 54 IS `done` and the Load-balancing family HAS closed, BUT the Observability family and the Operational-tooling family both still carry non-empty deferred-candidate lists, so future work remains. Do NOT create `stop`.)

**STATUS AS OF 2026-07-08: phase 54 (`load-balancer-panic-threshold`) IMPL is DONE and LANDED (squash `e06e88ec`, row 54 `in-progress → done`, lifecycle-state 3 → done). The Load-balancing family has CLOSED (the FOURTH family to close, after HTTP-filters @ 25.3 / Network-filters @ 33 / Upstream-robustness @ 43). There is NO chartered phase currently in-progress.**

- **What just landed (phase 54, the EIGHTH and FINAL Load-balancing-family row):** `healthy_panic_threshold` turned from a consumed-but-never-proven field into a proven, hardened construct. Four evidence-driven corrections (AMEND-PT1 integer-truncation floor + integer cross-multiply / AMEND-PT2 `lb_healthy_panic` double-increment removal / AMEND-PT3 locality child-local-panic retrofit / AMEND-PT4 out-of-range `[0,100]` reject) + the first-ever differential proof (`0097-lb-panic-threshold`, three arms). Counts now: fixtures **99**, fuzzers **52**, stat surface **1200**, BackendKind **38**, DECISIONS tail **ADR-0271**.

==============================================

## THE NEXT ACTION: a human charters the next phase (the loop PAUSES here)

The autonomous loop self-advances through the STAGES of a chartered phase, but choosing the NEXT PHASE from the deferred-candidate pool is a HUMAN decision (the ROADMAP §9 convention: "Do not pre-populate rows for unbrainstormed phases"; phase 54 itself was "Picked by a human as the family's LAST remaining candidate"). With the Load-balancing family closed and NO phase in-progress, the project has reached a natural human-selection checkpoint.

**This session should:**
1. Re-verify the cold-start baselines (below) against master tip `e06e88ec`.
2. Surface to the human the OPEN families and their deferred-candidate lists so they can pick the next phase to charter. The two families with non-empty deferred candidates are:
   - **Observability** (tracing / access-log / stats-sink extensions — read the ROADMAP §9 Observability family heading + the phase-44..49 rows' "Deferred surface" notes for the live candidate list).
   - **Operational tooling** (the family opened at phase 51 `bootstrap-validate-mode`, ADR-0268 — read its "Deferred surface" note for candidates).
   Enumerate the actual candidates from `docs/envoy-go/ROADMAP.md` (do NOT trust this summary — read the source) and present them.
3. Once the human picks a candidate, register a new ROADMAP row `in-progress`, invoke `superpowers:brainstorming`, and perform that phase's BRAINSTORM (the lifecycle-stage-0 → 1 step). Then land it (commit + push) and ROLL THIS FILE FORWARD to point at that phase's SPEC.

If the human is not present to pick, do NOT guess a phase or invent a row — report that the Load-balancing family is closed, that no phase is chartered, and present the deferred-candidate menu, then stop and await selection. (The `stop` sentinel is NOT the right action — work remains; `stop` is only for total project completion.)

==============================================

## Honor throughout (the discipline, unchanged)
- **Lifecycle = one stage per session** (BRAINSTORM → SPEC → PLAN → IMPL). Once a phase is chartered: build the stage end-to-end, land it (commit + push per `feedback_push_to_origin`), then roll this file forward.
- **Subagent-driven execution** (`feedback_execution_style`) in a **fresh worktree** (`feedback_git_worktrees`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit, re-runs gates on the frozen HEAD, does deliberate-break/liveness verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). Per-task gates: `gofmt -l` + `golangci-lint` + `go vet` + `go build ./...` (`feedback_pertask_gofmt_lint`).
- **Process anchors:** ADR-0106 (per-leg rows) · ADR-0044 (ADR §Decision+§Consequences land at IMPL) · ADR-0045 (split soft gate) · ADR-0080 (byte-stable rejects) · ADR-0052 (atomic six-gate completion bundle) · next-free **ADR-0272**.
- Re-verify baselines fresh at cold-start — do NOT trust any count in this file without re-running the baseline commands against current HEAD:
  - `ls -d test/fixtures/[0-9][0-9][0-9][0-9]* | wc -l` → expect **99**
  - `grep -rh '^func Fuzz' --include='*.go' --exclude-dir=.worktrees . | wc -l` → expect **52**
  - DECISIONS tail: `grep -nE '^## ADR-0[0-9]+' docs/envoy-go/DECISIONS.md | tail -1` → expect **ADR-0271** (next-free ADR-0272)
  - BackendKind tail: `grep -n "BackendKind = " test/differential/fixture/fixture.go | tail -1` → expect **38**
  - stat surface → **1200**

## Read first (in order)
1. THIS FILE — the cold-start router (no chartered phase; human picks the next candidate).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 54 IMPL done`) + full history + the "Next → NO chartered phase remains" note.
3. `docs/envoy-go/ROADMAP.md` — row 54 `done`; the Load-balancing family CLOSED; the OPEN families (Observability, Operational-tooling) and their deferred-candidate lists (the source of the next-phase menu).
4. `docs/envoy-go/DECISIONS.md` — ADR-0271 (the phase-54 record) is the tail; ADR-0272 is next-free.
5. `docs/envoy-go/BOOTSTRAP_PROMPT.md` — the family-expansion + lifecycle discipline if any ambiguity about how to charter a new phase.
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the `reference_*` differential/probe lessons relevant to whichever candidate the human picks.
