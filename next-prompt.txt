START a fresh session for envoy-go. **The phase-43.1 (connection-pool budgets + pending wait-queue) SPEC is COMPLETE and LANDED on master, and ROW 43 (`connection-pooling`) is `in-progress`.** 43.1 is the FIRST by-concern leg of the FIFTH-and-FINAL Upstream-robustness-family row (43.1 = the budget substrate + pending wait-queue; 43.2 = the H2 multiplex pool, a later leg that SUPERSEDES ADR-0056). The next step is the **43.1 PLAN** (`superpowers:writing-plans`) — NOT another SPEC. Confirm the latest master is the phase-43.1 SPEC completion commit via `git log --oneline -3` (a NEW sha — do NOT assume a literal value). Counts at tip (UNCHANGED at the SPEC): stat surface **1181** / fixtures **79** (tail `0077-hedge-on-per-try-timeout`) / fuzzers **42** / BackendKind tail **36** (`BlockingHoldResponder`) / DECISIONS tail **ADR-0251**, next-free **ADR-0252**.

## NEXT STEP — author the 43.1 PLAN (the TDD spine)

Author `docs/envoy-go/phases/43-connection-pooling/PLAN.md` in the prevailing phase-41/42 PLAN shape (the ~14–18-task TDD spine; SPEC §10 sketches it), plan-document-reviewer-APPROVED, and run the FINAL ADR-0045 split-gate re-check (anticipated NO further split — ~250–400 prod LoC / ~14–18 tasks). **The load-bearing risk is the pending wait-queue concurrency** — resolve the SPEC §12 D-S431-* PLAN questions, especially:

- **D-S431-1 (THE risk)** — the pending wait-queue design: the wake primitive (`sync.Cond` vs a buffered-channel semaphore), wake fairness (FIFO vs LIFO), the permit-free-vs-idle-return wake coordination, the race-free acquire-after-wake, and the ctx-cancel-while-pending release. MUST be `-race`-clean (the 42.2b hedging-collector precedent: channels BUFFERED, a single mutator, no loser blocks). Plan unit tests that exercise acquire/pend/wake/queue-full/ctx-cancel under `-race`.
- **D-S431-2** — whether the permit logic is two call sites (`Cluster.Dial` + the `AcquireH1` pool-MISS) or a shared `createConn`-with-permit helper; whether a sibling `connpool.go` houses the wait-queue.
- **D-S431-3** — the EXACT stat-surface arithmetic (does `upstream_rq_pending_active`/`_total` register on every CB cluster ⇒ `0074` gains +2; the `0078` block size) — pin 1181 → the exact figure (anticipated ~1199).
- **D-S431-5** — `0078` constants (N / M / J / convergeDeadline / held-count / refContainerListenerPort) single-sourced; the STAGED drive order (fill conns → poll `cx_open` → fill queue → poll `rq_pending_open`/`pending_active==M` → fire J oversubscribers); the subject-side-exact assertions (cap never exceeded / queue depth M / J overflow).
- **D-S431-6** — `max_connections: 0` / `max_pending_requests: 0` disposition; whether TCP-cluster `circuit_breakers` engage the permit (incidental, not differential-pinned).

After the 43.1 PLAN: the 43.1 IMPL (`superpowers:subagent-driven-development` — fresh implementer per task + two-stage review; insist on `-race -count=1` for the wait-queue tasks), then the 43.2 (H2 multiplex pool) BRAINSTORM/SPEC/PLAN/IMPL.

## Architecture as SPEC'd (43.1 — ADR-0252)

**THE load-bearing SPEC finding (AMEND-CP1 — captured in the memory `reference_max_connections_soft_breaker`):** the live pins showed the reference's `max_connections` is a SOFT breaker (`upstream_cx_active` EXCEEDS the cap with timing slop — observed 3/5/8/9 at max=2; `cx_open`+`upstream_cx_overflow` are soft signals; the 503 comes from `upstream_rq_pending_overflow` with a timing-sensitive count) ⇒ a cross-side EXACT differential is INFEASIBLE. **USER-CONFIRMED:** envoy-go implements a CLEAN HARD-CAP `max_connections` + a bounded pending wait-queue + fail-fast 503 (a recorded DEPARTURE — more correct than the reference's racy soft behavior). EXTEND the phase-41 `cbPriority` struct with per-priority `activeConnections`/`pendingRequests` atomics + the `maxConnections`/`maxPendingRequests` budgets (phase 41 parses the `Thresholds` but stores NO fields for these two). The permit acquire-or-pend gates connection CREATION at `Cluster.Dial` (raw-dial, used by tcp_proxy + `DialH2`) + the `AcquireH1` pool-MISS; a pool HIT reuses an already-counted conn → NO permit (byte-neutral keep-alive). At the cap, the request PENDS (block its goroutine, bounded by `max_pending_requests`), woken on conn-Close (`connWithGauge`) or idle-return (`PutIdleH1`); on queue-full ⇒ fail-fast 503 + `upstream_rq_pending_overflow`. ACTIVATE the emit-0 `cx_open`/`rq_pending_open`/`upstream_cx_overflow` (store the handles + drive LIVE) + ADD `upstream_rq_pending_active` (gauge) + `upstream_rq_pending_total` (counter), CB-scoped. Byte-identical when no `circuit_breakers`. NO new config-reject (the budgets are phase-41-parsed; AMEND-CP6) + NO new fuzzer + REUSE `BlockingHoldResponder` 36 (AMEND-CP7). The `0078-connection-pool-max-connections` differential asserts ROBUST cross-side invariants (`cx_open`/`rq_pending_open` flip + ≥1 downstream-class 503 per `reference_concurrent_attempt_downstream_class_assertion` + `upstream_rq_pending_overflow` delta) + SUBJECT-side EXACT cap/queue; sleepless via the release-barrier + poll-the-gauge per `reference_concurrency_differential_release_barrier`.

## Honor throughout (the discipline)

- **Lifecycle = one stage per session.** `feedback_git_worktrees` (a fresh worktree off master) · `feedback_subagents_no_push` (subagents commit LOCALLY; the controller squashes + pushes at stage-close) · `feedback_push_to_origin` (push after a clean squash on master with tests green) · `feedback_execution_style` (subagent-driven execution).
- **PLAN-specific:** author in the phase-41/42 PLAN shape; resolve the D-S431-* questions; run the FINAL ADR-0045 split-gate re-check; plan-document-reviewer-APPROVED; the per-task complete code + the exit-delta table. The ADR-0252 §Decision/§Consequences land at the 43.1 IMPL (per ADR-0044; §Context already drafted in SPEC §13).
- **Process anchors:** ADR-0045 (split-gate re-check); ADR-0106 + `reference_roadmap_split_phase_row_done` (row 43 flips `done` PER-LEG; the family CLOSES only when BOTH 43.1+43.2 land); ADR-0080 (strict-reject); `reference_docker_probe_bridge_network` + `reference_concurrency_differential_release_barrier` + `reference_concurrent_attempt_downstream_class_assertion` (the concurrency-differential discipline); `reference_fixture_workload_constant_desync` (single-source the `0078` constants).

## Read first (in order)

1. THIS FILE — the cold-start router (the 43.1 SPEC is DONE + LANDED; row 43 is `in-progress`; the next step is the 43.1 PLAN).
2. `docs/envoy-go/STATE.md` — active-phase `phase 43.1 (connection-pooling) SPEC done`; the count figures (1181 / 79 / 42 / 36 / ADR-0251, next-free ADR-0252).
3. `docs/envoy-go/phases/43-connection-pooling/SPEC.md` — the 43.1 SPEC (the §1.1 AMEND-CP1..7 block; the §3 hard-cap + wait-queue architecture; the §7 stat surface; the §8 `0078` differential [robust cross-side + subject-exact, staged drive]; the §11 D-CP-* live-pin block; the §12 D-S431-* PLAN questions; the §13 ADR-0252 §Context draft).
4. `docs/envoy-go/phases/43-connection-pooling/BRAINSTORM.md` — the phase-43 charter (the by-concern split; the forks; note the SPEC's AMEND-CP1 SUPERSEDES the BRAINSTORM's PENDS-mirroring-the-reference design with the clean hard-cap).
5. `docs/envoy-go/phases/41-circuit-breakers/{PLAN,SPEC}.md` — the PLAN shape to mirror + the phase-41 `circuitBreaker` struct 43.1 extends.
6. `internal/cluster/circuitbreaker.go` + `internal/cluster/cluster.go` — the `cbPriority` struct + `registerStats` (the emit-0 handles to activate) + `Dial`/`AcquireH1`/`PutIdleH1`/`connWithGauge` (the permit/wake seams).
7. `docs/envoy-go/DECISIONS.md` — ADR-0248 (the phase-41 struct 43.1 extends) + the ADR-0044/0045/0080/0106/0227 anchors; next-free ADR-0252.
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `reference_max_connections_soft_breaker` [THE 43.1 load-bearing finding — the soft-breaker departure + the robust-cross-side/subject-exact differential model], `reference_concurrency_differential_release_barrier`, `reference_concurrent_attempt_downstream_class_assertion`, `reference_docker_probe_bridge_network`, `reference_fixture_workload_constant_desync`, `reference_roadmap_split_phase_row_done`, `feedback_git_worktrees`, `feedback_subagents_no_push`, `feedback_push_to_origin`, `feedback_execution_style`).
