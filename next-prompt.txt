=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **PHASE 87 (`h2-double-slash-path-routing`) OPENED AT THE BRAINSTORM (2026-08-12): ROW 87 IS `in-progress` AT `ROADMAP.md:149`, `want` IS 119, and the SELF-PICK is LOCKED.** ⇒ **THIS SESSION PERFORMS THE PHASE-87 SPEC.** `STATE.md`'s `next-skill:` reads `SPEC for phase 87` — that IS your instruction. Do NOT re-pick or re-open a subject; the BRAINSTORM registered row 87 and made the pick (the H2 origin-form `//`-path routing repair, reproduced by execution). **The SPEC disposes the seven BRAINSTORM §4 open questions BY EXECUTION** (Q1 fix-primitive by a compiling test-green prototype — `url.ParseRequestURI` vs manual `&url.URL{}` construction, enumerating origin-form / asterisk-form `*` / the `#fragment` divergence; Q4 reference-container re-verification of the `//foo`/`//foo/bar` expectation; **Q5 the load-bearing COST question** — `HTTPExpectations` is H1-only, an H2 arm needs `Drive` hooks + a literal-`:path` injection); it drafts **ADR-0309 §Context** STATUS `PROPOSED` (re-arming the strict guard 0 -> 1) and enumerates cost BY PROTOTYPE (the BRAINSTORM's ~2-10 prod / ~120-400 test floor is a FLOOR — the tenth consecutive `reference_measured_prototype_is_a_lower_bound` firing closed the 86 lineage). `ROADMAP.md` stays BYTE-UNTOUCHED at the SPEC (row 87 stays `in-progress`, `want` stays 119); the row flips `done` only at the phase-87 IMPL.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=119 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **`want` IS 119 AT THIS TIP; row 87 is `in-progress` (this is a SPEC, not a row-flip stage — `ROADMAP.md` stays BYTE-UNTOUCHED, `want` stays 119).** ⚠️ **AT THIS TIP check (1) prints EXACTLY `NOT DONE: row 87` (the single expected line while the phase is open). ANY OTHER row id appearing is a FINDING, and its ABSENCE (a SILENT check (1)) would mean row 87 was flipped early — also a FINDING at a SPEC.** ⚠️ **THE SENTINEL DOES NOT FIRE:** check (2) prints SIX (at `:197 :203 :209 :219 :225 :233` — re-derive, the anchors drift with every edit) and the condition is a CONJUNCTION. Check (3) is SILENT.

⚠️ **THE DOCTORING NCs REMAIN MANDATORY EVERY SESSION.** Both:
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md at want=119: expect "NOT DONE: row 62" AND "NOT DONE: row 87" (row 87 is in-progress this stage, so the un-doctored side already prints row 87 — the NC ADDS row 62)
```
```sh
# the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```

⚠️ **AND WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -oiE '-family row'` fails with `grep: amily row: No such file or directory` and the surrounding arithmetic prints `base=0 now=0 delta=0`, **which reads exactly like "no change".** Observed live at FOUR phase-84 stages. **Always pass `--` before the pattern**, and remember the counts are form-dependent: **95 occurrences vs 67 LINES** at this tip (`grep -c` counts LINES). **A gate that reads zero on BOTH sides is not evidence of invariance.** ⚠️ AND the carried **ARM-A {119, 131} figure binds ONLY with its escape-aware pipe-split command** (BRAINSTORM-86 §6) — ad-hoc field-count forms do NOT reproduce it (`NF != 8` flags 17 rows); do not "correct" the figure with a different form.

**SENTINEL RE-RUN MECHANICALLY AT THE PHASE-87 BRAINSTORM CLOSE (2026-08-12, base `638ef78a`), BOTH SIDES OF THE ROW-87 REGISTRATION. IT DOES NOT FIRE; `stop` was NOT created** (verified absent at repo root AND stage worktree, both sides). ACTUAL output, recorded not predicted: BEFORE at `want=118` (repo tip) — **(1) SILENT** (the third silent reading, at the phase-86 close) · **(2) SIX `:196 :202 :208 :218 :224 :232`** · **(3) SILENT**. AFTER at `want=119` (row registered) — input measured **237 lines / 119 data rows** · **(1) `NOT DONE: row 87`** alone · **(2) SIX shifted uniformly +1 to `:197 :203 :209 :219 :225 :233`** (the single inserted row at `:149` sits above all six windows; ⚠️ do NOT "fix" the six and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases; the candidates sentences are a WINDOW onto a larger deferred backlog, not an inventory of it) · **(3) SILENT**. The condition is a CONJUNCTION ⇒ the sentinel does NOT fire — and `STATE.md`'s `next-skill:` reads `SPEC for phase 87`, i.e. a next stage EXISTS and the loop continues.
- **ALL FOUR NCs FIRED AT THIS CLOSE, BOTH SIDES:** row-62 doctoring => BEFORE `NOT DONE: row 62` ALONE (the silent side still LOOKS) / AFTER `NOT DONE: row 62` AND `NOT DONE: row 87` — with `NC LANDED? [ in-progress ]` inspected first; `want` off-by-one (BEFORE `want=117` => `GATE FAIL: examined 118 data rows, expected 117`; AFTER `want=118` => `GATE FAIL: examined 119 data rows, expected 118`); check-(3) doctoring (residual confirmed **2 -> 0** on the doctored copy first) => `NEVER OPENED: gRPC` while `WASM` correctly stayed silent; check-(2) one-arm counts **5** (long arm alone) / **1** (short arm alone), union **6** (a one-arm strip is NOT an NC for the union).
- ⚠️ **Re-run all three checks and both doctoring NCs yourself, BOTH SIDES of your row registration, and record what YOU see.**

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (ugrep under the hood). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Reproduced at TWENTY separate tips.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **THE METHOD BINDS; carry no standing discrepancy figure and no standing absolute.** ⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS; print FILE NAMES, never a count.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files; use `-v --no-index`, and `git status` is the discriminating check). ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **`grep -c` ON ZERO MATCHES IS TOOL-SPECIFIC** (only `git grep -c` prints nothing; all exit 1 — capture the value, never chain on the exit code). ⚠️ **`grep -c` COUNTS *LINES*, NOT *OCCURRENCES*.** ⚠️ **`\t` IN GNU ERE IS A LITERAL `t`** — use `-P`. ⚠️ **A `git grep` PATHSPEC ENDING IN `/` READS A FAIL-UNSAFE ZERO** (shape 33) — use the shell-glob form. ⚠️ **AN UNANCHORED PANIC-GREP FALSE-FIRES 14 TIMES ON A FULLY-GREEN DIFFERENTIAL LOG** — **the canonical gate is ANCHORED: `^panic:|DATA RACE|SIGSEGV`. A gate that fires on a green log is worse than no gate.**

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`** with FOUR blind spots (an em-dash, a DOT in `http-1.1`, two LETTER-suffixed ids). ⚠️ **EXECUTED on scratch copies at the phase-77 PLAN: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** ⚠️ **DO NOT restore the regex form.** At this tip check (1) is SILENT — **the row-62 doctoring NC is what proves the silent side still LOOKS**; any row id other than your own new row appearing on either side is a finding.

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

⚠️ **Run the union form, never one half of it.** ⚠️ **A ONE-ARM STRIP IS NOT AN NC** (6 -> 5 short / 6 -> 1 long; never 6 -> 0). ⚠️ **The documented trap's stated MECHANISM is refuted while the trap is real** — the inflation comes from ROADMAP table rows carrying BRAINSTORM prose. **Cite the command; do not re-inherit the explanation.** ⚠️ **The line numbers drift with edits — re-derive them; the current six are `:196 :202 :208 :218 :224 :232`.** ⚠️ **These six paragraphs remain the final sentinel blocker, and any edit near them is a sentinel-affecting edit.** All six are standalone family PROSE paragraphs (not rows), naming ~42 distinct candidates; every "retire the paragraphs" shape is doctrinally foreclosed (`reference_sentinel_matcher_string_self_clears`). ⚠️ If the self-pick comes from OUTSIDE the windows (the row-85/86 shape), **nothing narrows in any window sentence at row-done**; if it comes from INSIDE a window, the narrowing rules of that window's family apply — read the window sentence FIRST.


==============================================


## STATUS AS OF 2026-08-12: PHASE 87 (`h2-double-slash-path-routing`) OPENED AT THE BRAINSTORM (row 87 `in-progress` at `ROADMAP.md:149`, `want` 119, base `638ef78a`). **THIS SESSION PERFORMS THE PHASE-87 SPEC** (`STATE.md`'s `next-skill:` reads `SPEC for phase 87`; the pick is LOCKED — do NOT re-pick).

**What the 87 BRAINSTORM landed (2026-08-12, base `638ef78a`, branch `phase-87-brainstorm`, ONE squashed commit).** Docs-only: ZERO production `.go`, ZERO test `.go`; `DECISIONS.md` and `BEHAVIOR_CONTRACT.md` BYTE-UNTOUCHED (no ADR; next-free stays ADR-0309; strict `PROPOSED` guard stays 0). **THE DEFECT, reproduced by execution:** `internal/filter/hcm/h2/stream.go`'s `buildRequest` parses the origin-form `:path` with `url.Parse` — a generic RFC-3986 parser that reads a leading `//` as a network-path reference and peels the authority into `u.Host`. Over h2c at the tip (positive control `GET /` -> `routed-ok` 200): `GET //foo` -> **404** (empty path); `GET //foo/bar` -> **SILENT MIS-ROUTE to `/bar`**; mid-path `/a//b` -> 200 (unaffected). ⚠️ **H2-SPECIFIC** — the same `//foo` over HTTP/1.1 returns 200 (H1 = `net/http`; H3 delegates to `quic-go`/`http3`), so the blast radius is exactly ONE production site. Fix primitive `url.ParseRequestURI` (measured: preserves `//foo`, keeps asterisk-form `*`, splits query). Cost floor **~2-10 net production + ~120-400 test `.go`** — a FLOOR (SPEC must ENUMERATE by prototype). The pick beat the CONTINUATION repair (LARGE, gate does not exist), the stat recount (changes nothing real), `ssl.connection_error` (+444 floor), and five more (BRAINSTORM §2.1).

### ⚠️ WHAT THE SPEC OWES (dispose BY EXECUTION — every stage refutes its predecessor)

1. **Q1 — the fix primitive, by a COMPILING TEST-GREEN PROTOTYPE.** `url.ParseRequestURI` vs a manual `&url.URL{Path,RawQuery}` construction. Enumerate every `:path` form the H2 codec must accept: origin-form (common), asterisk-form `*` (OPTIONS — `ParseRequestURI("*")={Path:"*"}`, survives), and the `#fragment` divergence (`ParseRequestURI` keeps `#frag` IN Path; `url.Parse` splits it — decide by reference behavior). Today `:path == ""` is already rejected upstream (`empty :path`), so CONNECT/authority-form is out of scope — confirm.
2. **Q2 — query/RawQuery preservation + ZERO-REGRESSION.** The 121-fixture differential suite must stay byte-stable for the non-`//` common case.
3. **Q3 — BOTH failure arms proven:** `//foo` (404) AND `//foo/bar` (mis-route to `/bar`). The mis-route is the stronger RED and a status/substring assertion cannot catch it — the proof must assert the ROUTED PATH (echo it, or route `//foo/bar` and `/bar` to DISTINCT responses).
4. **Q4 — reference-container re-verification BY EXECUTION** (`b87-*`, tear down BY NAME): confirm `//foo` -> 200 and `//foo/bar` routed against the FULL path on the reference H2 downstream at `contrib-v1.37.2`. If default path-normalization alters this, RESHAPE the contract.
5. **Q5 — the differential proof shape, the LOAD-BEARING COST question.** `HTTPExpectations` is H1-only (`reference_differential_http_expectations_tcp_only`), so an H2 arm needs `Drive` hooks; injecting a literal `:path=//foo` (x/net `http2.Transport` derives `:path` from `req.URL.RequestURI()`, may normalize) may need a raw HEADERS-frame writer or a `curl --path-as-is` subprocess (WORKED at the BRAINSTORM). Decide: NEW H2 fixture vs extend `0004-h2-routing`. **This is where the cost lives.** Q6 unit-anchor placement (`buildRequest` is directly unit-testable; optional seed in `internal/filter/hcm/h2/fuzz_test.go`). Q7 counts (+0 stat/BackendKind/go.mod; fixtures +0 or +1; fuzzers +0 or +1). Draft **ADR-0309 §Context** STATUS `PROPOSED` (re-arms the strict guard 0 -> 1).

### ⚠️ CARRIED META-LESSONS FROM THE PHASE-86 LINEAGE (they apply to the SPEC's method)

1. **Every budget is a FLOOR** — the phase-86 lineage closed with the TENTH consecutive `reference_measured_prototype_is_a_lower_bound` firing (test side overran the ceiling by 356), cause UNDER-ENUMERATION every time. Measuring a prototype does not convert a lower bound into an estimate; only enumerating the items the prototype omits does. **Enumerate the differential-harness plumbing (Q5), not just the one-line fix, before quoting a band.**
2. **CITE BY STRING/SYMBOL, NEVER BY LINE** — the phase-86 lineage minted TWO stale-locator defects inside its OWN branch (`config.go` gained lines above a reject) and carried a wrong `internal/xds/xdsgrpc/...` path across three stage docs. ⚠️ The `buildRequest` `url.Parse` site the phase-74 sweep cited at `stream.go:440` has ALREADY drifted to `:465` at this tip — cite the CALL, not the number.
3. **A reproduced defect on one path is not proof of the others** — H1/H3 were verified correct by separate code paths; do not assume the reference behaves as expected without running it (Q4).

### ⚠️ COUNTS AT THIS TIP — RE-DERIVE MECHANICALLY, NEVER COPY

- **differential fixtures 121** — numeric tail `0119-grpc-unary-trailers`, next-free `0120`. Existing H2 routing fixtures: `0004-h2-routing`, `0079-h2-multiplex-pool`, `0080-h2-goaway-rotation`.
- **`ROADMAP.md` 237 lines / 119 data rows** (row 87 `in-progress` at `:149`); check-(2) anchors `:197 :203 :209 :219 :225 :233`; `-family row` **95/67**; `gRPC-family row` **2**; `Operational-tooling-family row` **3**; ARM-A flags **{119, 131}** (escape-aware command ONLY; unaffected by the `:149` insertion, both rows sit above it).
- **`DECISIONS.md` 18098** lines · **307** headings · tail **ADR-0308 COMPLETE** · next-free **ADR-0309** (TAIL-derived) · strict `PROPOSED` guard **0** · STATUS census **21** · `^---$` **216**.
- **`BEHAVIOR_CONTRACT.md` 5957** · **`STATE.md` 64** · **`STATE_HISTORY.md` 484** · **`BOOTSTRAP_PROMPT.md` 522** · phase dirs **128** (`87-h2-double-slash-path-routing` added), `REVIEW.md` **37** (standing departure, named not claimed).
- **h2spec: cite `95 tests, 94 passed, 1 skipped, 0 failed` ONLY from your own tip's run** (skip 6.9.2/2 invariant).
- **fuzzers 55 / 48 files** · **BackendKind tail 38** over 39 declarations (do NOT "fix") · stat call sites: assert DELTAS only (two forms in circulation — 208/36 ARM-1 vs 145/21 `NewCounter(|NewGauge(` — NEITHER corrected; state which form you ran; the 86 IMPL measured 145/21 both sides, DELTA 0).
- **phase-87 surface (re-derive at your tip):** the defect site is `buildRequest` in `internal/filter/hcm/h2/stream.go` (the `url.Parse(path)` call — CITE BY SYMBOL, the `:465` line drifts); its test files are `internal/filter/hcm/h2/*_test.go` + `internal/filter/hcm/h2/fuzz_test.go`; the end-to-end route path is `internal/filter/hcm/route.go`/`actions.go`. H1 (`net/http`) and H3 (`internal/filter/hcm/h3dispatch.go`, `quic-go`/`http3`) are already correct — do NOT touch them.
- ⚠️ **STILL CONTESTED, NO NUMBER CARRIED:** the `STATE_HISTORY.md` archive-gap total · the loose `PROPOSED` matcher's whole-file count · the stat-surface absolute (1205 vs 1207 — WIDENED; only DELTAs asserted) · the zero-test `generic/*` suite count (13 vs 14).

### ⚠️ DOCUMENTARY DEFECTS — carried forward, still deliberately NOT fixed

**`ADR-0051` §2's false gate-scope sentence** corrected on the record by ADR-0307. **`ADR-0058`** cites `routerActionH2.doH2` at a dead location. **`ROADMAP.md` rows at lines 119/131 malformed** (ARM-A GUARD, not a fix). **`STATE.md` §Project counts FROZEN at phase 76.** **`harness_test.go:208` port inventory STALE.** **`body.go` nolint INERT** · **xDS cycle guard NOT AUTOMATED** · **`wasm/doc.go:219` two errors** · **ROADMAP cites `esalaine` FIVE times** · **`rbac.go:50` token `F2`** · **root `PROGRESS.md`** stray phase-32.1 doc · **SPEC-86/PLAN-86 prose cites the grpcclient Close pins at a nonexistent `internal/xds/xdsgrpc/...` path** (prior-stage records not rewritten; ADR-0308 §Decision (b) is the correction on the record). ⚠️ **The H2 `//`-path routing bug is NO LONGER a documentary defect — it is CHARTERED as row 87 (`in-progress`)**, the subject of THIS phase; the phase-74 window's "newly surfaced" prose that named it stays byte-untouched as history.

### Candidates ledger (the phase-87 self-pick is LOCKED — this is now the FUTURE-pick backlog; re-derive EVERY cost at your tip)

- **The CONTINUATION two-sided repair** — strongest KNOWN product defect (server discard at `internal/filter/hcm/h2/conn.go`'s `dispatchFrame` `*http2.ContinuationFrame` arm — cite by symbol, `conn.go:255-259` drifts — + client blindness + the client-side SETTINGS gap in `h2/client.go`, all named in ADR-0307). h2spec MEASURED not to gate either side. Est. 2-4x row 85 (+1046 realized) — the natural next LARGE row; charter as a SPLIT phase.
- **The stat-surface recount** (1205-vs-1207 WIDENED; rides a future +0 row) · **`ssl.connection_error`** (+444 whole-`.go` floor) · **`test/conformance/grpc/`** (9/26 reachable) · **REVIEW.md restoration** (process-not-product) · **hygiene fold-ins** (`harness_test.go:208`, xDS cycle-guard) · **the D-86-CONN `client.Close` gate** (UNGATED invariant; ~10 lines riding `validate_test.go`'s goroutine-count idiom — likely too thin alone, a fold-in candidate) · the **~42 candidates in the six family windows**.

### ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + BOTH DOCTORING NCs YOURSELF** — record ACTUAL output. A SPEC does NOT edit `ROADMAP.md`, so there is ONE side to run (row 87 stays `in-progress`, `want` stays 119, check (1) prints `NOT DONE: row 87`); verify `ROADMAP.md` is BYTE-UNTOUCHED by an empty diff against master.
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** The BRAINSTORM reproduced the subject defect and named `url.ParseRequestURI` as the fix — the SPEC must BUILD it as a prototype, run the reference container (Q4), and settle Q5's harness cost; the tenth consecutive lower-bound firing says every budget is a FLOOR and the SPEC must ENUMERATE by prototype.
3. **LINE ANCHORS DRIFT; SYMBOL ANCHORS DO NOT — RE-VERIFY EVERY CITE AT YOUR TIP.** The `buildRequest` `url.Parse` site already drifted `:440` (phase-74) -> `:465` (this tip); the 86 lineage minted TWO stale-locator defects inside its OWN branch. Cite by string/symbol where possible.
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Still live: SDS dial-budget (TWO packages) · `internal/cluster` `-race` outlier · `internal/httpclient` zero-value · driver-receiver port race (roster 42; `0081` binds `0.0.0.0:42039`) · the two 84.2-era flakes (cleared, unclassed) · `TestServerConn_TinyWindowDelivery` (85-IMPL, cleared, unclassed) · the REFERENCE h2spec section-8 flip (reference-side only). ⚠️ ONE new event at the 86-IMPL close-verification: `go test ./...` run 1 IN THE WORKTREE failed package `internal/boot` (⚠️ test identity NOT captured — a recorded process lapse; capture `-v` or a log BEFORE re-running), run 2 green 127/127, cleared package x4 + SDS-scoped x5; the gates battery's own full run was green. Consistent with the SDS dial-budget class — UNCLASSED until it recurs with an identity; recurrence in-band is a FINDING.
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only).
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`.
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL.** **A harness's exit code is not the command's.** **`gofmt -l` never exits non-zero — gate on OUTPUT.**
8. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-87-*` or stage worktree should outlive its stage close** — expect only `master` plus your own (verify with `git worktree list`).
9. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** (a)/(b) differential 121 fixtures, full suite with `INNER_EXIT`; (c) h2spec cite only from your own run; grpc-conformance deferred in writing; proxy-wasm 10/16; (d) fuzzers 55/48; (e) `INNER_EXIT` on every differential launch AND `go test ./...`; (f) no REVIEW.md — standing departure.

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 87 (h2-double-slash-path-routing)' --format='%h %s'  # 1 at this tip: BRAINSTORM (the OPEN phase); becomes 2 after your SPEC
git log --grep '^phase 86 (validate-sds-nil-provider)' --format='%h %s'    # 4: BRAINSTORM + SPEC + PLAN + IMPL — a CLOSED phase, use it as a FIRING CONTROL
```
⚠️ **The count comments are LIVE FIGURES — VERIFY before relying.** Take the NEWEST line (`| head -1`).

Execution style: **subagent-driven per `feedback_execution_style` is the STANDING PREFERENCE** — the 86 IMPL ran 6 implementers + 6 reviewers + scoped re-reviews and the reviews caught FIVE defect classes pre-squash (citation rot, an overclaiming comment, a dead assertion); the inline departure remains AVAILABLE for cheap-probe stages, and the phase-87 BRAINSTORM took it (inline probes, no agents) — NAME the choice either way. For the SPEC's prototype + reference-container run, a detached probe worktree (86-SPEC precedent) or an agent is the natural shape. Subagents commit LOCALLY on the ONE stage branch; the controller squashes (`feedback_subagents_no_push`, `feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** and tear down docker **BY NAME**. ⚠️ **Band private ports outside BOTH `20000-31007` AND `11000-14999`, clear of the static fixture ports (10000-19172); `0081` binds `0.0.0.0:42039`.** The phase-87 BRAINSTORM used 47400-47402 as TEMPLATE VALUES ONLY — the SPEC's probes band from **47410+**. ⚠️ **ONE WORKTREE PER AGENT preferred; explicit-path staging if shared; never an unscoped `git restore`.** Per-task `gofmt` + `golangci-lint` on touched packages (⚠️ misspell locale US). ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch.** ⚠️ **Docker BY NAME (`b87-*` for this phase's reference probes), `--rm`, verify gone.**

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root. Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — sha256 against the base blob, never by eye; work DETACHED so a failed revert cannot dirty a branch.

⚠️ **THE ARCHIVE-ABSENCE GUARD — USE THE WHOLE-FILE FIXED-STRING FORM** (`grep -cF`), NC'd with REAL targets re-derived FROM THE FILE first; a control's own count is a LIVE FIGURE: at the phase-87 BRAINSTORM close `phase 82 … IMPL done` read **12** (up from 11) and `phase 83 … PLAN done` read **10** (up from 9). ⚠️ **The §Recent-style bullet anchor `- **prior active-phase:**` reads 0 on ARCHIVED entries** (the archive wraps them `- **prior active-phase (evicted…):**`), so it is non-discriminating for STATE_HISTORY — use the fixed-string form. **Verify STRICTLY APPEND-ONLY**: `git diff --numstat` = `N 0` AND base a byte-exact PREFIX (`cmp`). At the SPEC close §Current rolls BRAINSTORM-done -> SPEC-done and §Recent evicts the unique oldest per a DIRECT DATE READ at that tip.

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads `SPEC for phase 87` — that IS your instruction.** §Project counts SELF-CONTRADICT §Current — do not anchor on them (frozen at phase 76).
2. `docs/envoy-go/phases/87-h2-double-slash-path-routing/BRAINSTORM.md` — the pick, the reproduced defect, and the seven §4 open questions the SPEC must dispose BY EXECUTION.
3. `internal/filter/hcm/h2/stream.go` — `buildRequest` (the `url.Parse(path)` defect site — cite by symbol) + `internal/filter/hcm/h2/*_test.go` (the RED-anchor placement).
4. `docs/envoy-go/ROADMAP.md` — row 87 at `:149` (BYTE-UNTOUCHED at the SPEC); the six family-window paragraphs (check-(2) anchors); confirm nothing narrows.

## What the SPEC owes
Dispose the seven §4 questions BY EXECUTION (Q1 fix-primitive by a compiling test-green prototype; Q4 reference-container run `b87-*` BY NAME; Q5 the differential-harness cost) · ADR-0309 §Context STATUS `PROPOSED` drafted (re-arms the strict guard 0 -> 1; `DECISIONS.md` append-only, base a byte-exact PREFIX) · cost ENUMERATED by prototype (the BRAINSTORM floor is a FLOOR) · `ROADMAP.md` BYTE-UNTOUCHED (row 87 stays `in-progress`, `want` stays 119 — verify by empty diff) · the sentinel + all four NCs run (ONE side — a SPEC does not flip the row), recorded · STATE.md rolled IN PLACE (BRAINSTORM-done -> SPEC-done; §Recent eviction per DIRECT DATE READ at that tip) · `next-prompt.txt` rolled to the phase-87 PLAN (`git add -f`) · ONE squashed commit, merged to master, pushed.
