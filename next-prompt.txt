START a fresh session for envoy-go (router last updated 2026-06-25; the **phase 44.1 (`grpc-access-log` core) IMPL is DONE + pushed to origin/master**). HEAD is the LATEST `next-prompt.txt:` docs-only tip-stamp commit; the **phase-44.1 IMPL squash `ec05a421`** (subject `phase 44.1 (grpc-access-log core) IMPL: the core gRPC ALS streaming sink — …`) is the stable anchor sitting directly BENEATH the run of `next-prompt.txt:` commits at the tip; re-read the shas with `git log --oneline -12`, do NOT assume — the master tip is the LATEST `next-prompt.txt:` commit (a roll or a tip-stamp fix), NOT the IMPL squash itself, and any number of docs-only `next-prompt.txt:` commits may stack ATOP the IMPL squash (which stays the stable anchor). BENEATH the IMPL squash is the phase-44.1 PLAN squash `5c7cf481` (`phase 44.1 (grpc-access-log core) PLAN: …`), and BENEATH that the phase-44.1 SPEC squash `a7a24d0c` then the phase-44 BRAINSTORM `81afdd59`. **The Observability family is OPEN** (phase 44 is its FIRST row; the family STAYS OPEN — 44.2 buffering / 44.3 header-capture / OTLP / tracing / stats sinks / tap remain future rows). **The next action is the 44.2 (buffering) SPEC** — the parent phase-44 BRAINSTORM already chartered the BY-CONCERN 3-leg split (44.1 core [LANDED] · 44.2 buffering · 44.3 header-capture), so 44.2 goes STRAIGHT to SPEC (no separate 44.2 brainstorm) per STATE.md's recorded NEXT action — but CONFIRM against `docs/envoy-go/STATE.md` (the source of truth for "what next") before committing to the stage. **Git hygiene:** all phase work runs in a FRESH git worktree off master (`feedback_git_worktrees`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the controller squashes + pushes at stage-close (`feedback_push_to_origin`); each code task runs `gofmt -l` + `golangci-lint` on touched pkgs (`feedback_pertask_gofmt_lint`); the controller verifies `git -C <main> status` clean + the worktree branch unchanged after each task (`feedback_subagent_worktree_detach` / `feedback_subagent_worktree_path_targeting`).

First confirm the state via `git log --oneline -12` (expect HEAD to be the LATEST `next-prompt.txt:` docs-only commit, with the phase-44.1 IMPL squash `ec05a421` sitting directly BENEATH the run of `next-prompt.txt:` commits, then the phase-44.1 PLAN squash `5c7cf481` + the phase-44.1 SPEC squash `a7a24d0c` + the phase-44 BRAINSTORM `81afdd59` beneath — the tip-stamp commits interleave with each squash; re-read the shas, do NOT assume). Counts at the 44.1 IMPL-done (as-built, pushed): stat surface **1189** (H2 cluster; non-H2 **1185**) / fixtures **83** (tail `0081-grpc-access-log`; NOTE: count fixtures with `ls -d test/fixtures/[0-9][0-9][0-9][0-9]* | wc -l` — `grep -cE '^[0-9]{4}-'` UNDERCOUNTS by 2, skipping `0007a`/`0007b`) / fuzzers **44** / BackendKind tail **38** (`H2GoawayResponder`; UNCHANGED — the ALS receiver is the driver-owned test-only `test/helpers/accessloggrpc`, NOT a BackendKind) / DECISIONS tail **ADR-0255**, next-free **ADR-0256**.

## NEXT STEP — the 44.2 (buffering) SPEC (`superpowers:writing-plans` predecessor is the SPEC stage)

The 44.2 leg adds the `buffer_size_bytes` / `buffer_flush_interval` flush triggers to the `GrpcAccessLogSink`, REPLACING the 44.1 FIXED simple flush (one entry per message). At 44.1 these two fields are PARSE-ACCEPTED-but-INERT (D-ALS-CONFIG-DEFER) — the parse arm already accepts them; 44.2 makes them LIVE. Anticipated ADR-0256. The SPEC stage executes the §11 D-* live-probe pins IN-SESSION against `envoyproxy/envoy:contrib-v1.37.2` per `reference_docker_probe_bridge_network` (how the reference batches entries into messages under a byte-cap + a flush-interval timer; the exact `HTTPAccessLogEntry` aggregation the `reference_streaming_sink_differential_framing` AMEND-ALS-3 discipline asserts — the PAYLOAD across messages, NOT the message/batch FRAMING which legitimately varies). A fresh worktree off master (`feedback_git_worktrees`).

Carry-forward design facts from the 44.1 as-built (ADR-0255):
- The sink's writer goroutine currently builds ONE `StreamAccessLogsMessage` per `Record` (a fixed simple flush). 44.2 introduces a buffer that accumulates entries and flushes on EITHER the byte-cap OR the interval timer — the `AsyncFileSink` does not buffer, so this is NEW sink machinery (a flush-on-timer goroutine path alongside the existing channel drain; mind the `-race` background-mutator gate `reference_full_suite_race_after_background_mutator` — the sink already has a writer goroutine + the bounded ctx-cancel Close `closeDrainGrace`).
- The `access_logs.grpc_access_log.{logs_written,logs_dropped}` counters stay (AMEND-ALS-1); buffering may add a flush-related stat (SPEC-pin it against the reference).
- `additional_request_headers_to_log` / `additional_response_headers_to_log` + the `AccessLog.filter` field stay PARSE-ACCEPT-but-INERT until 44.3 (header-capture).
- The `0081-grpc-access-log` differential is the byte-stability regression anchor for the sink; a 44.2 buffering differential (`0082-…`) asserts the cross-side entry PAYLOAD aggregated across the buffered messages (framing varies — AMEND-ALS-3).

## Honor throughout (the discipline)

- **Lifecycle = one stage per session.** This session is the 44.2 SPEC (lifecycle-state 0→1 for the 44.2 leg; row 44 STAYS `in-progress` — the Observability family STAYS OPEN). `feedback_git_worktrees` (fresh worktree off master) · `feedback_subagents_no_push` · `feedback_push_to_origin` · `feedback_execution_style` (the IMPL stage, later, runs subagent-driven).
- **Process anchors:** ADR-0106 (row 44 flips `done` PER-LEG; NO parent rollup — the family stays open until ALL legs + the family roster exhaust) · ADR-0044 (the ADR-0256 §Context drafted in the SPEC; §Decision/§Consequences land at the 44.2 IMPL) · ADR-0045 (the split gate — re-check 44.2 sizing at SPEC/PLAN) · ADR-0080 (strict-reject — 44.1 added the TcpGrpcAccessLogConfig reject; carry the strict-reject posture) · ADR-0016 (the proto blank-import registry) · ADR-0067 (the access-log 10-operator `Record` shape) · ADR-0158 (the `grpcclient` typed-wrapper layer — `ALSClient`) · ADR-0255 (the 44.1 as-built — the `GrpcAccessLogSink` / `ALSClient` / mapping / parse arm / the 2 static stats / the driver-owned receiver).
- **Streaming-sink differential discipline:** `reference_streaming_sink_differential_framing` (assert the PAYLOAD aggregated across messages, NOT framing — AMEND-ALS-3) · `reference_differential_grpc_receiver_driver_owned` (the ALS receiver is a driver-owned `test/helpers/accessloggrpc` server the proxy DIALS, NOT a BackendKind) · `reference_docker_probe_bridge_network` (the receiver reachable from BOTH `127.0.0.1` subject AND `host.docker.internal` reference; verify decode ran) · `reference_concurrency_differential_release_barrier` (poll-to-converge, NEVER `time.Sleep`) · `reference_differential_run_selector` (`-run 'TestDifferential/0082'`, NOT bare) · `reference_differential_break_protocol_count1` (`-count=1` on every break + `-race`) · `reference_differential_fullsuite_startup_flake` (a `subject ready: EOF` is a transient startup race — isolate-re-run) · the AMEND-ALS-2 query-less-path constraint carries (envoy-go `Record.Path` is path-only).

## Read first (in order)

1. THIS FILE — the cold-start router (phase 44.1 IMPL is DONE + pushed; the next action is the 44.2 buffering SPEC).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 44.1 (grpc-access-log) IMPL done`) + the recorded NEXT action + the count figures (source of truth for "what next" per BOOTSTRAP_PROMPT §4.1).
3. `docs/envoy-go/phases/44-grpc-access-log/BRAINSTORM.md` — the parent phase-44 brainstorm (the BY-CONCERN 3-leg split charter: 44.1 core / 44.2 buffering / 44.3 header-capture; the envelope forks).
4. `docs/envoy-go/phases/44-grpc-access-log/SPEC-44.1.md` + `PLAN-44.1.md` — the 44.1 design + the D-ALS-* resolutions (the buffering deferral D-ALS-CONFIG-DEFER; the §1.1 AMEND-ALS-1..4).
5. `docs/envoy-go/DECISIONS.md` — ADR-0255 (the 44.1 as-built; the §Decision/§Consequences) + next-free **ADR-0256** (the 44.2 buffering ADR).
6. `docs/envoy-go/ROADMAP.md` — row 44 (`grpc-access-log`, in-progress; leg 44.1 done) + the `### Observability family` heading.
7. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the `### Access log — gRPC Access Log Service (ALS) streaming sink` block + the stat-surface 1189 entry.
8. The 44.1 as-built seams the 44.2 SPEC builds on: `internal/accesslog/grpcsink.go` (the `GrpcAccessLogSink` — the bounded channel + writer goroutine + lazy-establish/identifier-once/reconnect + the `closeDrainGrace` bounded-Close; the FIXED simple flush 44.2 replaces), `internal/accesslog/mapping.go` (`buildHTTPAccessLogEntry`), `internal/accesslog/stats.go` (`RegisterGrpcSinkCounters`), `internal/bootstrap/bootstrap.go` (`parseGrpcAccessLog` — `buffer_*` PARSE-ACCEPT-INERT, the `httpGrpcAccessLogTypeURL`/`tcpGrpcAccessLogTypeURL` consts), `internal/grpcclient/grpcclient.go` (`ALSClient`), `test/helpers/accessloggrpc/` (the receiver accumulator), `test/fixtures/0081-grpc-access-log/` (the differential precedent — note the fixture's driver dir is `driver/`, the now-dominant convention).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `reference_streaming_sink_differential_framing`, `reference_differential_grpc_receiver_driver_owned`, `reference_docker_probe_bridge_network`, `reference_full_suite_race_after_background_mutator`, `reference_fuzzer_count_docs_drift`).
