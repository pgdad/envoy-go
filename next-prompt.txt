START a fresh cold-start session for envoy-go — **the phase-36.2 (`load-balancer-ring-hash-http`) PLAN is DONE and LANDED on master; the next lifecycle step is the phase-36.2 IMPL** (`superpowers:subagent-driven-development` — execute the 8-task TDD PLAN; fresh subagent per task, two-stage spec+code-quality review between tasks; work in a worktree; subagents commit LOCAL-ONLY, the controller squash-merges + pushes at stage-close). The substantive master tip is the phase-36.2 PLAN squash **`85207c6`** (`phase 36.2 PLAN: load-balancer-ring-hash-http — decompose the SPEC §10 spine into 8 bite-sized TDD tasks; resolve D-S362-1..7; ADR-0045 re-check NO SPLIT; plan-document-reviewer APPROVED`; pushed to `origin/master`; confirm via `git log --oneline -5`). The literal live master tip is this docs-only `next-prompt.txt` SHA-fill roll-forward sitting directly above the PLAN squash `85207c6`. The substantive predecessor below the PLAN squash is the phase-36.2 SPEC squash `da295ab` (with its docs-only roll-forwards `c6b8edf`/`ffcff08` between); below that the phase-36.1 IMPL squash `e8e8c48`. The PLAN squash carries the durable `docs/envoy-go/phases/36.2-load-balancer-ring-hash-http/PLAN.md` (the per-sub-phase-dir 32.1/32.2 precedent) + the STATE/ROADMAP row-36 advance (active-phase → `phase 36.2 PLAN done`; ROADMAP row 36 `36.1 done, 36.2 PLAN done → IMPL next`). Counts UNCHANGED at the PLAN (they advance at the 36.2 IMPL).

## What this session does — execute the phase-36.2 IMPL

The phase-36.2 PLAN is COMPLETE. The next session EXECUTES the **phase-36.2 IMPL** (`superpowers:subagent-driven-development` — the 8-task TDD spine). 36.2 is the **HTTP route `hash_policy` PRODUCER-ONLY leg**: the HTTP router parses a per-route `RouteAction.hash_policy` at config-build (header + connection-properties source_ip SUPPORTED; cookie/query/filter_state + regex_rewrite + source_ip-false DEPARTURE-rejected, fail-fast), computes a ring_hash key per request via the `rotl64(prev,1)^new` fold (first-verbatim, nullopt-skip, `terminal` short-circuit), and threads it via the ALREADY-LANDED `cluster.WithHashKey(ctx, key)` at the FOUR router dial sites (codec-agnostic H1 `*http.Request` / H2 `h2.H2Request`) → the ALREADY-LANDED `ringHashLB.Pick`. NO LB policy / seam / manager / package / go.mod change; ONE additive `cluster.HashHeaderValues` helper; ZERO new stat names; the `0062-lb-ring-hash-http` NAT-transparent TRUE cross-side affinity differential.

## The 8-task PLAN spine (the IMPL executes; see PLAN.md for complete code + exact anchors)

1. First-task baselines/anchors gate: re-confirm fixtures **63** + fuzzers **42** + stat surface **1119** + BackendKind tail **33** + DECISIONS tail **ADR-0236** via the canonical recipes; re-pin the as-built anchors; PROGRESS.md created.
2. `cluster.HashHeaderValues(values []string) uint64` — the seed-chained `XXH64` fold over byte-sorted values (single-value collapse to `xxHash64([]byte(value))`); refactor `xxHash64` to delegate to a seeded variant (behavior-neutral). The ONE additive exported `cluster` symbol (D-S362-1/4).
3. `parseRouteHashPolicies` in `hcm/config.go` (router stays proto-free — the proto-read at the hcm boundary; the descriptor `router.HashPolicy` is router-owned) + the §6 DEPARTURE-reject matrix (cookie/query/filter_state + regex_rewrite + source_ip-false; empty-`header_name` parity reject `value length must be at least 1 runes`) + thread `hashPolicies` onto `clusterRouteAction` + extend `H1ClusterAction(c,hps)`/`H2ClusterAction(c,hps)` (store-only) + fix the 3 callers incl. `actions.go:212` (D-S362-2/5).
4. The `applyHashKey` fold (`rotl64`-XOR, first-verbatim, nullopt-skip, terminal-on-accumulator) + the 4 dial-site ctx rebinds + the uniform ctx-carried remote addr (`router.WithDownstreamRemoteAddr` set at `connection.go` H1 dispatch + `h2dispatch.go` H2 dispatch — `*http.Request.RemoteAddr` is NOT populated on the HCM codec path) + the per-codec `headerVal` shims (H1 `req.Header.Get`; H2 hpack-field scan) (D-S362-3/4).
5. The `0062-lb-ring-hash-http` differential: distinct `X-Hash` values × K repeats; the BOTH-side affinity (modular invariant `count % K == 0`, NAT-transparent) + spread (`>= 2`) `DistributionAsserter` + the cross-side `StatsAsserter` (3 gauges cross-equal + cx/membership/quiesced + **`upstream_rq_total` cross-equal** — the new prong vs `0061`). NO new BackendKind (D-S362-6).
6. Deliberate-break liveness (`-count=1`): scatter-key (affinity fails both sides), collapse-spread (spread fails), drop/corrupt-Inc (stats fails), corrupt-gauge (gauge prong fails); ≥20-run flake check.
7. Full 64-dir differential (63 prior byte-exact through the constructor signature change + `0062` green) + `-race -short` + build/vet/gofmt/lint/`go mod tidy -diff` empty + **h2spec/proxy-wasm RE-RUN-or-assert with the HTTP-path-touch rationale** (NOT zero-touch at 36.2 — D-S362-7).
8. Completion bundle (ADR-0052 atomic landing): BEHAVIOR_CONTRACT 36.2 addendum + the full ADR-0237 (§Context from SPEC §13 + §Decision + §Consequences; ADR-0044 in-place; tail → **ADR-0237**) + STATE/ROADMAP row 36.2 done (flat family row, NO parent rollup; the 36 family STAYS OPEN) + the six-gate evidence (surface 1119 unchanged, fixtures 64).

## Counts at master tip (phase 36.2 PLAN done — confirm at the 36.2 IMPL Task 1 baseline)

- stat surface **1119** (the 36.2 plane adds 0 names — `upstream_rq_total` cross-equal on the HTTP plane, unlike tcp). 36.2 IMPL stat delta: 0.
- differential fixtures **63** (tail `0061-lb-ring-hash`; `ls -d test/fixtures/[0-9]* | wc -l`). 36.2 IMPL adds `0062-lb-ring-hash-http` → 64.
- fuzzers **42** (tail `FuzzThriftDecode`); BackendKind tail **33** (`TCPThriftResponder`) — both UNCHANGED at the 36.2 IMPL (no wire decode; reuse existing HTTP backends).
- DECISIONS tail **ADR-0236** (next-free **ADR-0237**). The ADR-0237 (HTTP route hash_policy producer plane) §Context is a DRAFT in SPEC §13; the full entry lands at the 36.2 IMPL per ADR-0044.
- exported-surface at 36.1: `cluster.WithHashKey` + `cluster.HashSourceIP` (both LANDED; 36.2 CONSUMES them + adds the ONE additive `cluster.HashHeaderValues` for the header digest — D-S362-1).
- reference pin: `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227). Toolchain: Go 1.26.x; golangci-lint 1.64.8 (ADR-0009); go-control-plane `/envoy` v1.32.4 (ADR-0008).

## Read first (in order)

1. THIS FILE — the post-phase-36.2-PLAN cold-start (next: the phase-36.2 IMPL).
2. `docs/envoy-go/phases/36.2-load-balancer-ring-hash-http/PLAN.md` — **the IMPL input**: the D-question-resolutions block (D-S362-1..7 + ADR-0045 re-check), the File Structure map, and the 8 tasks with complete code, exact file paths/line anchors, the §6 reject matrix, the `applyHashKey` fold + the 4 dial-site rebinds + the ctx-carried remote addr, the `0062` fixture (the modular-invariant BOTH-side affinity proof), the deliberate-break liveness, the six-gate, and the ADR-0237 completion bundle.
3. `docs/envoy-go/phases/36.2-load-balancer-ring-hash-http/SPEC.md` — the design reference: **§3** the producer wiring + §3.2 the H1/H2 codec asymmetry, **§5** the proto roster, **§6** the reject roster, **§7** stats (0 new), **§8** the `0062` fixture, **§11** the empirical-pin block, **§12** the D-questions, **§13** the ADR-0237 §Context draft.
4. `docs/envoy-go/phases/36-load-balancer-ring-hash/PLAN.md` + `PROGRESS.md` — the AS-BUILT 36.1 (the landed seam + `cluster.WithHashKey`/`HashSourceIP`/`xxHash64` + the tcp_proxy `source_ip` producer the HTTP plane mirrors).
5. `docs/envoy-go/STATE.md` — active-phase `phase 36.2 PLAN done`; the lifecycle → the 36.2 IMPL routing; the counts.
6. `docs/envoy-go/ROADMAP.md` — row 36 (36.1 done; 36.2 PLAN done → IMPL next; flat family row per ADR-0106; Load-balancing family OPEN, 5 candidates remain).
7. `docs/envoy-go/DECISIONS.md` — **ADR-0235** (the seam) + **ADR-0236** (the ring_hash policy) — the durable assets 36.2 builds on; + ADR-0044 (ADR bodies at the IMPL, in-place — ADR-0237 lands at the 36.2 IMPL) / ADR-0052 (the atomic-landing six-gate) / ADR-0080 (byte-stable parse-reject wording) / ADR-0106 (flat family row) / ADR-0227.
8. The landed code the 36.2 producer threads into: `internal/cluster/{cluster.go (WithHashKey/hashKeyFrom), hash.go (HashSourceIP/xxHash64/ipOnly)}` + the HTTP router (`internal/filter/http/router/{router.go (H1ClusterAction/doH1ClusterAction/routerAction.do/Dial sites), router_h2.go (H2ClusterAction/doH2ClusterAction/doH2/DialH2 sites)}`) + the HCM bridge (`internal/filter/hcm/{config.go (buildRouterAction), actions.go (clusterRouteAction/asRouterAction/asRouterActionH2)}`) + the dispatch ctx-carry set-sites (`connection.go` H1, `h2dispatch.go` H2) + the tcp_proxy producer precedent `internal/filter/tcpproxy/filter.go`.
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_execution_style`, `feedback_git_worktrees`, `feedback_subagents_no_push`, `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_path_targeting`, `feedback_subagent_worktree_detach`, `reference_differential_break_protocol_count1`, `reference_differential_asserter_dispatch`, `reference_differential_run_selector`, `reference_differential_band_sigma_margin`, `reference_differential_hash_key_cross_side_infeasible` (the HTTP header key IS cross-side-reproducible — `0062` is a TRUE cross-side affinity differential), `reference_docker_probe_bridge_network`.

## Honor throughout

- The lifecycle is BRAINSTORM → SPEC → PLAN → IMPL, each its own session. The next session is the phase-36.2 **IMPL** (`superpowers:subagent-driven-development` — execute the 8-task PLAN; the ADR-0052 atomic six-gate landing).
- `feedback_execution_style` (subagent-driven execution at the IMPL) · `feedback_git_worktrees` (work in a worktree) · `feedback_subagents_no_push` + `feedback_push_to_origin` (controller squash-merges + pushes at stage-close) · `feedback_pertask_gofmt_lint` (per-task gofmt -l + golangci-lint on touched pkgs).
- ADR-0106 (flat family row — the 36.2 leg flips its own portion at the IMPL six-gate) · ADR-0044 (ADR bodies at the IMPL, in-place — ADR-0237 lands at the 36.2 IMPL) · ADR-0052 (the atomic-landing six-gate) · ADR-0080 (byte-stable parse-reject wording) · ADR-0227 (the contrib reference image; live probes per `reference_docker_probe_bridge_network`).
