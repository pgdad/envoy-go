=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

**THERE IS BANKED MID-LIFECYCLE WORK AT THIS TIP.** Phase 82 (`wasm-http-call-response-cache`) was **BRAINSTORMED and SPEC'd at 2026-08-01**; **row 82 is `in-progress` and the phase is OPEN**. ⇒ **This session does NOT self-pick. It performs the PHASE-82 PLAN.**

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=114 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **CHECK (1) IS LIVE — it prints `NOT DONE: row 82`.** **Run the doctored-copy NC anyway**: the moment row 82 flips `done`, check (1) goes silent and silence is otherwise indistinguishable from a broken check. The form that works (a `sed` targeting `done      ` does NOT — it misses the real `| done |` column spacing and prints nothing, which reads exactly like *"the check is blind"*):
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
```
Verified again at the phase-82 SPEC: the row-62 NC fires (`NOT DONE: row 62` alongside row 82) and the `want=113` denominator NC fires.

⚠️ **`want=114` IS A LIVE FIGURE, NOT A CONSTANT.** It is the ROADMAP data-row count. **A row-adding phase MUST bump it in the same commit that adds the row.** The phase-82 BRAINSTORM bumped it 113 -> 114. ⚠️ **THE PLAN ADDS NO ROW, SO IT MUST LEAVE `want` AT 114 AND LEAVE `ROADMAP.md` BYTE-UNTOUCHED.**

Check (3) fails SAFE by construction. When you open a new family, add its row-summary slug to the list above.

**SENTINEL RE-RUN MECHANICALLY AT THE PHASE-82 SPEC, WITH FIRING NEGATIVE CONTROLS. IT DOES NOT FIRE. `stop` was NOT created; `ls stop` => `No such file or directory`, and it MUST NOT be created.** ⚠️ **Re-run it yourself anyway and record what YOU see.** ACTUAL output, recorded rather than predicted:
- **(1) `NOT DONE: row 82`** at `want=114`, denominator printed (`examined 114 data rows`). Both NCs fired.
- **(2) FIVE — `:192 :202 :212 :218 :226`, UNCHANGED. The THIRTY-THIRD consecutive phase at which check (2) did not go down. STATED, not forecast.**
- **(3) `NEVER OPENED: gRPC` — ALONE.** NC: an invented slug fires; the REGISTERED slug `Observability` correctly printed nothing.
- Input measured at **230 lines / 114 data rows**, so an empty result could not read as a zero result.

⚠️ **THE LEAK CHECK IS LIVE AT ANY STAGE THAT WRITES A ROADMAP CELL. YOUR PLAN WRITES NONE — `ROADMAP.md` MUST BE BYTE-UNTOUCHED.** The rule: **NEVER WRITE A SENTINEL'S OWN MATCHER STRING INTO A FILE THE SENTINEL GREPS.** The sentinel greps `docs/envoy-go/ROADMAP.md` — writing these strings *here* (or in `DECISIONS.md`) is safe; writing them *there* is not.
```sh
git diff --stat master -- docs/envoy-go/ROADMAP.md   # must be EMPTY at the PLAN
```
⚠️ **AND THE TWO CLASSES ARE OPPOSITE.** `<slug>-family row` is check (3)'s **PASS** condition (row 82's landed cell is a genuine USE). `deferred candidates:` / `remaining deferred (not-yet-chartered) candidates:` are check (2)'s **FAIL** conditions.

⚠️ **A DRAFTING FOOTGUN, STILL LIVE:** the gate needs the literal `WASM-family row`, but the heading is `### WASM host family` and the lineage's row convention would naturally produce `WASM-host-family row` — **which does NOT match.** **A successor editing row 82's cell must preserve the exact string `WASM-family row` or check (3) silently regresses.**

⚠️ **ROW WELL-FORMEDNESS: THE GATE MUST BE A DISJUNCTION.** Over all **114** rows: **ARM-A (escape-aware `NF!=8`) catches lines 119 and 131 ONLY; ARM-B (escape-aware trailing-piece) catches line 140 ONLY.** ⚠️ **Naive `NF==8` flags SEVENTEEN — 15 FALSE POSITIVES + 2 true — and MISSES line 140** (compensating defects cancel) ⇒ wrong in **both** directions. **Row 82 is flagged by neither arm.** Re-verified at the SPEC.

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …`. **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Reproduced at NINE separate tips.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS** (wrapped prints bare paths, GNU prints `./`-prefixed). ⚠️ **Print FILE NAMES, never a count.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** — git exempts TRACKED files. ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** (`xargs` execs a binary; `command` is a shell keyword) — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **`grep -c` ON ZERO MATCHES IS TOOL-SPECIFIC**: only `git grep -c` prints nothing; GNU and the wrapper print `0`. Both exit 1, so an exit-code gate is wrong for either. ⚠️ **`\t` IN GNU ERE IS A LITERAL `t`** while the harness function normalizes it ⇒ the trap bites exactly where gates run and is invisible interactively. **Use `-P` unconditionally.**

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`** with FOUR blind spots (an em-dash, a DOT in `http-1.1`, two LETTER-suffixed ids). Every document from phase 75 forward called it *"currently HARMLESS."* ⚠️ **Nobody fired the unsafe arm until the phase-77 PLAN. EXECUTED on scratch copies: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** ⚠️ **DO NOT restore the regex form.**

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

⚠️ **Run the union form, never one half of it.** ⚠️ **A ONE-ARM STRIP IS NOT AN NC**: `sed 's/deferred candidates://'` moves the union **5 -> 4, not 5 -> 0**. Re-confirmed at the phase-82 SPEC. ⚠️ **The documented trap's stated MECHANISM is refuted while the trap is real** — the inflation comes from ROADMAP table rows carrying BRAINSTORM prose, not from `candidates were:` recaps. **Cite the command; do not re-inherit the explanation.**

⚠️ **CHECK (2) HAS NEVER GONE DOWN** across ~33 phases (history `0 -> 1 -> 3 -> 4 -> 5`) => **the candidates sentence is a WINDOW onto a larger deferred backlog, not an inventory of it.** ⚠️ **A stage that predicts a decrease is repeating the phase-73 error. State what your row narrows — usually nothing.**

⚠️ **NEW AT THE PHASE-82 SPEC: `:212` IS NOT A SENTENCE ANCHOR.** It is a **45,959-char** line carrying **SIX** occurrences of the deferred-candidate phrase; only the one at **offset 14459** ends in the colon-terminated form the sentinel matches. Citing *"the `:212` sentence"* without the offset is ambiguous across six different rosters. ⚠️ **And `:226` does not carry the long phrase at all** — SHORT form only, which is exactly the Operational-tooling blindness check (2) was broadened to fix.

==============================================


## STATUS AS OF 2026-08-01: PHASE 82 (`wasm-http-call-response-cache`) IS **OPEN**. ROW 82 IS `in-progress`. LIFECYCLE-STATE **2**. THIS SESSION PERFORMS THE **PHASE-82 PLAN**.

**What the phase-82 SPEC landed.** Base master **`61f4f5a3`** (from `git rev-parse master`), branch `phase-82-spec`. Docs-only: **ZERO production `.go`, ZERO test `.go`.** NEW `SPEC.md` + `PROGRESS.md`; `DECISIONS.md` **17796 -> 17824** (ADR-0304 §Context, pure tail append, **prefix proven byte-identical by `cmp`**); `STATE.md` rolled in place (63 -> 63); `STATE_HISTORY.md` **440 -> 442**; this file. **`ROADMAP.md` and `BEHAVIOR_CONTRACT.md` BYTE-UNTOUCHED.**

### ⚠️ THE ROW'S CHARTERED PREMISE IS REFUTED FOUR WAYS. READ `SPEC.md` §1 AND §2 BEFORE ANYTHING ELSE.

**The chartered defect is REAL but MASKED, MIS-ADDRESSED, and its stated verification story does not exist.**

1. ⚠️ **THE REFERENCE EMITS NOTHING, NOT `200`.** Probed against `envoyproxy/envoy:contrib-v1.37.2`: the request **hangs to the client timeout** (15.014 / 20.007 / 25.002 s across three arms; `curl` exit 28, zero bytes). The reference **honors** `Action::Pause`; the vendored guest never resumes. **Two agents, two different discriminating designs.** A control crate identical to the vendored source **plus** `resume_http_request()` made the reference answer **`200` in 5 ms**, which also refutes the landed comment's stated *reason* (`cluster.cluster_b.upstream_rq_200: 1`).
2. ⚠️ **THE SUBJECT'S `0` IS THE GUEST'S INITIAL FIELD VALUE, NOT A CACHE MISS.** `proxy_on_http_call_response` is **never invoked**: `http_call_dispatched 20` / **`http_call_response 0`** / **`after_close 20`** over 20 requests. A `777` sentinel control makes the subject emit **`777`**. Root cause: `decode_headers.go:258-260` logs `ProxyActionPause` and returns `envoyhttp.Continue` (primitive 6, deferred since 25.2), so the stream closes before the response lands and `http_call.go:342-348` takes the after-close early return.
3. ⚠️ **THE MASK HIDES A TRAP, NOT A WRONG VALUE — AND THIS FORBIDS ONE OF THE TWO SPLIT ORDERS.** The vendored `proxy-wasm-rust-sdk` v0.2.4 `get_map` (`~/.cargo/registry/.../proxy-wasm-0.2.4/src/hostcalls.rs:158-174`, the exact source the blob was compiled against) has **ONLY a `Status::Ok` arm plus `status => panic!("unexpected status: {}")`. THERE IS NO `NotFound` ARM.** ⇒ **honoring Pause without a correct cache converts a benign wrong header into a guest TRAP plus a BUG-3 `panic_already_borrowed` cascade.**
4. ⚠️ **THE HEADER-MAP ENUM IS SWAPPED PAIRWISE, SO THE BRAINSTORM'S OWN WIRING PLAN WOULD SHIP THE ROW VACUOUS.** `internal/wasm/abi/types.go` has `HttpCallResponseHeaders=4`, `HttpCallResponseTrailers=5`, `GrpcReceiveInitialMetadata=6`, `GrpcReceiveTrailingMetadata=7`. **Canonical is the reverse** (`proxy-wasm-0.2.4/src/types.rs:90-99`: Grpc **4/5**, HttpCall **6/7**), and **the guest calls `proxy_get_header_map_pairs(6)`**. A 3-arm cross-product: no stash ⇒ trap; stash at **4** ⇒ **trap identically**; stash at **6** ⇒ works. ⚠️ **Pinned in place by a HAND-WRITTEN golden at `internal/wasm/abi/types_test.go:144-147` that shares the author's mistake** — passes today, fails on exactly 4 subtests once corrected. The same wrong values sit in the landed phase-25.1 SPEC record (**record, do not fix**).

**Two further structural blockers neither prior document names:** ⚠️ **`resp.Header` carries NO `:status`** (Go puts it in `StatusCode`; verified with a firing NC — 3 keys, none of them `:status`), so a literal stash is **inert** even after (2) and (4) are fixed. ⚠️ **`numHeaders` is a KEY count while `GetHeaderMap` emits VALUE pairs** — and `numHeaderValues` is unexported in a package that *imports* `internal/wasm`, so `http_call.go` cannot call it without inverting the dependency.

### ⚠️ WHAT THIS SESSION DOES — THE PHASE-82 PLAN

**One stage per session.** Write `docs/envoy-go/phases/82-wasm-http-call-response-cache/PLAN.md` and append a `# PLAN record` to `PROGRESS.md`. **ROW 82 STAYS `in-progress`; `ROADMAP.md` MUST BE BYTE-UNTOUCHED and `want` STAYS 114.** **`DECISIONS.md` is BYTE-UNTOUCHED at the PLAN** (ADR-0304 §Decision + §Consequences land at the IMPL). Docs-only: **ZERO production `.go`, ZERO test `.go` committed.**

**THE PLAN'S FIRST JOB — NOT NEGOTIABLE:**
- ⚠️ **PROTOTYPE S1 (the stream-control pause half) AND RE-MEASURE.** It is the **only unprototyped item of eight** and it **dominates the estimate**. A split argued from the SPEC's arithmetic alone is not evidence.
- ⚠️ **THREE ITEMS ARE ALREADY MEASURED — do NOT re-derive, but DO treat as LOWER BOUNDS:** the cache half at **93 net** production `.go` (prototype built, `-race` green end-to-end through production `abiCallbacks` dispatch with the real vendored blob, then reverted); the enum fix at **~10**; the mechanical fixture flip at **+4** (`git diff --numstat` = 6 ins / 2 del).
- ⚠️ **THE ORDERING CONSTRAINT IS A CORRECTNESS CONSTRAINT.** If the row splits: **Leg B (S0 enum + S2-S6 cache) MUST PRECEDE Leg A (S1 stream control + S7 blob + S8 fixture).** The reverse order is the trap-plus-cascade regression of finding 3. **The SPEC's own first draft had this backwards and the correction is recorded, not quietly applied** — do not re-invert it.
- ⚠️ **`BOOTSTRAP_PROMPT.md:290`'s ~1500-LoC trigger is LIVE at ~1.5x** the SPEC's lower bound of **~710-1300 net over 14-20 tasks**. `:289`'s ~25-task trigger is not. **A measured prototype is a LOWER BOUND** — phase 81 ran **3.07x** over nine of them, phase 80 ~2.6x.

**THE EIGHT SCOPE ITEMS (SPEC §1.3):** **S0** correct the header-map enum + its golden (**a PRECONDITION**) · **S1** honor `ProxyActionPause` both sides · **S2** stash on `*RootVM` · **S3** synthesize `:status` · **S4** value-count semantics · **S5** `headerMapForType` cases **6/7** + `GetBuffer` case **4** · **S6** `defer` clear · **S7** rebuild the guest blob with `resume_http_request()` · **S8** flip `0036` (l) cross-side + **SPLIT the (l) stats disjunction**. ⚠️ **S0+S5 must land ATOMICALLY.**

**ALREADY SETTLED BY THE SPEC — DO NOT RE-OPEN:** **D-82-DIRECTION** (both prior statements wrong; the scenario is *incomparable*, not divergent) · **D-82-TRAILERS** (keep the map-type-**7** case, **scope the ASSERTION OUT** — no backend in the repo emits response trailers and the guest never reads them; asserting `num_trailers == 0` would be vacuous green) · **D-82-LIFETIME** (**CALLBACK-scoped on `*RootVM`**, `atomic.Pointer`, set-before/clear-after inside the existing `dispatchMu` frame; **no teardown wiring, no queue, no overwrite, no bound** — the landed `// TODO Task 15`'s *"on the originating `*StreamContext`"* is **REFUTED**; upstream keeps it on the root context and documents it *"Only available during onHttpCallResponse"*) · **D-82-COUNT** and **D-82-STATUS** (two NEW forks; see SPEC §5-§6) · **D-82-BREAK** (four arms; **BREAK-B is mandatory** — a fixture-layer break alone is vacuous, and `driveProxy` serves BOTH sides so any symmetric edit PASSES).

### ⚠️ GATE HAZARDS THE PLAN MUST CARRY

- ⚠️ **THE (l) STATS ARM IS INVARIANT UNDER THE ROW'S OWN CHANGE.** `driver.go:303-333` asserts a **disjunction** (`delivered + post_close >= 1`), **not** the `http_call_response >= 1` that `README.md:71` and `expectations.yaml:99-106` both claim. Today `0 + 20 = 20` PASS; after the fix `20 + 0 = 20` **PASS**. **A compensating-defect cancellation — split it into a `delivered >= 1` leg stacked with a `post_close == 0` control, or the row ships its headline fix unguarded.**
- ⚠️ **FIXTURE `0036`'s CROSS-SIDE COMPARISON IS 100% VACUOUS.** `driveProxy` emits **686 bytes of compile-time constants** (14 `emitConstantSkipToken` + 4 literals), identical on both sides by construction, with five helpers `//nolint:unused`-parked at `:602 :612 :696 :719 :729`. **A gate-(a)/(b) "0036 GREEN" is NOT parity evidence.** README lines 6-9/47 and the driver package doc claim 10 live cross-side arms; **none are live.**
- ⚠️ **THE BREAK BASELINE IS MISATTRIBUTED UNTIL S7 LANDS.** The flip fails today — but for the **reference-hang** reason, not the cache reason. **Order S7 before any break-arm verification**, and confirm **WHICH** assertion fired.
- ⚠️ **`handleHttpCallResponse` SWALLOWS GUEST TRAPS** (`http_call.go:425` is `_ = rv.runCallWithPanicWrapper(...)`), so `sc.trapped` stays false and `Close` re-enters a poisoned instance. **This path becomes LIVE the moment S1 lands — treat it as IN SCOPE.**
- ⚠️ **`+0 new PUBLIC surface` IS REFUTED.** A prototype adds **3 exported `*RootVM` methods**; the alternative extends `ABICallbacks` **20 -> 21**, breaking every implementer and test fake. **Pick one deliberately.**
- **`0036` costs ~37 s**, of which **~30 s is dead reference client-timeout** (arms (l) AND (n) both `Action::Pause`, 15 s each). S1+S7 remove it for (l); **(n) is out of scope and keeps its 15 s.**
- **The driver-owned receiver port race fired once during this stage** (`bind: address already in use` -> retry, self-healed). **Budget ~3 differential launches per green pass**; the race is measured at **42** fixtures.

### OUT OF SCOPE FOR ROW 82, DEFERRED BY NAME

The 9 `WasmResultUnimplemented` stubs at `internal/wasm/registration.go:877-882` · `proxy_on_queue_ready` / `proxy_on_grpc_*` · the wasm **network** filter · the 6 deferred cpp-host conformance families · **any trailers ASSERTION** · the **dead request/response trailer map types 1/3** (`internal/filter/http/wasm/trailers.go`, internally contradictory in three tenses; its capture is a literal no-op) · the `0036` cross-side restoration for scenarios (a)-(j) · **the driver-owned receiver port race (42 fixtures)** · **`stats-name-empty-segment-guards` (24 sites)** · the RBAC policy-name PROJECTION divergence · the lua `statNameRegexLiteral` (~40 net, 1 task) · the matcher-tree `Action`-name boot walk · **three** stale incumbent guard cites (`lua/compiled_config.go:272` ×2, `redisproxy/config.go:51`).

### ⚠️ THE AUTHORITATIVE CANDIDATE POOL IS THE SENTINEL'S OWN CHECK (2), NOT THIS FILE'S PROSE

**The project's actual un-chartered backlog is the five deferred-candidate sentences check (2) prints.** ⚠️ **Re-read them at YOUR tip.** Anchors `:192` (HTTP/3, **7** items) · `:202` (xDS, **9** — ⚠️ a naive ` + ` split gives 10; `upstream SDS (server-cert + validation_context)` carries an internal `+`) · `:212` (Observability, **3 top-level / 5 leaf** — ⚠️ **see the six-occurrence warning above**) · `:218` (Runtime, **8**) · `:226` (Operational tooling, **3**, SHORT form).

**The strongest measured candidate for a LATER row:** the **`ssl.connection_error` bucket** — **~230 net `.go` LOWER BOUND (realistic 575-700), 9-12 tasks, ZERO new fixtures** (extends `0110`). `connection_error` is **charset-legal bare AND assembled**, so it does **not** inherit the dynamic-`ssl` naming blocker. ⚠️ **One open risk: `0110` is `tcp_proxy`, and whether a record-layer failure there still books `connection_error` is UNPROVEN.**

⚠️ **THE DYNAMIC `ssl` FAMILY REMAINS BLOCKED.** `IsValidName("ECDHE-RSA-AES128-GCM-SHA256")` = **false** bare *and* assembled ⇒ **impossible, not possible-but-strict.** ⚠️ **The sentence's "FOUR-family dynamic surface blocked on NAMING" conflates three dispositions:** only `ciphers` is charset-blocked; `curves` and TLS-1.3 `ciphers` pass; `versions` passes charset but injects a spurious dot-**segment**. ⚠️ **`sigalgs` was NEVER observed emitted.**

⚠️ **`gRPC` IS THE LAST FAMILY CHECK (3) REPORTS AS NEVER OPENED, AND IT IS HARD-BLOCKED.** Zero implementation exists for any of the seven gRPC filter type-URLs. Both charter-satisfying candidates sit behind the **dead trailers seam** (16-22+ tasks, ADR-0045 split near-certain). ⚠️ **The cheapest gRPC filter, `grpc_stats` (~250-300 net, 12-14 tasks), is OFF-CHARTER and ALSO partially behind that seam.**

### What matters for the NEXT session, in order

1. **⚠️ RE-RUN ALL THREE SENTINEL CHECKS YOURSELF AND RECORD THE ACTUAL OUTPUT; DO NOT PREDICT IT.** Check (1) is LIVE — **run the doctored-copy NC anyway.** ⚠️ **Your PLAN adds NO row: leave `want` at 114 and `ROADMAP.md` BYTE-UNTOUCHED**, verified with `git diff --stat master -- docs/envoy-go/ROADMAP.md`.

2. **⚠️ EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION, NOT BY REVIEW.** The lineage is unbroken: phase-81 BRAINSTORM 14, SPEC 22, PLAN 14, IMPL 17; **phase-82 BRAINSTORM 26, SPEC 23 (TEN load-bearing).** **Budget for finding more. A PLAN that refutes nothing has not looked.** ⚠️ **And an AGENT'S refutation is a claim too** — one did not survive controller re-derivation at the SPEC (§2.11): it called §Project's stale `ADR-0299` *"a live violation of ADR-0288's invariant"*, but on the anchored form the discipline actually specifies (`^- \*\*<field>:`) **all five fields return exactly 1** and the invariant HOLDS.

3. **⚠️ BROKEN-GATE COUNT IS TWENTY-ONE — TWO NEW SHAPES LANDED AT THE PHASE-82 SPEC.**
   **The twentieth: A GATE METRIC THAT IS INVARIANT UNDER THE VERY CHANGE IT GUARDS.** The `0036` (l) arm sums two counters whose values *trade off* across the fix, so the sum is unchanged and the arm passes both before and after. **Assert the DIRECTION, not the total.**
   **The twenty-first: A HAND-WRITTEN ENUM GOLDEN THAT PINS THE WRONG WIRE VALUES.** `types_test.go:144-147` asserts the swapped 4/5/6/7 assignment, passes today, and would have to *fail* for the tree to become correct. **A byte-stable pin catches DRIFT, never WRONG-ON-ARRIVAL — derive from the upstream source, not from the code under test.**
   Priors that fired LIVE at this stage: **an empty output is not a zero result** (a `grep 'func headerMapForType'` returned nothing because the real form carries an `*abiCallbacks` receiver) · **a landed CODE COMMENT is not evidence, in BOTH directions** (`driver.go:504-509` is backwards while `:313-327` in the SAME FILE is correct and names the true root cause; and `abi_callbacks.go:594-596` calls `ContinueStream` a stub when `:766` is live) · **the archive-absence guard SELF-CLEARS** against the `last-updated` field naming its target (anchor on the BULLET) · **`Shell cwd was reset`** fired repeatedly.
   The nineteen carried forward, unchanged: a guard is a claim at TRANSCRIPTION time · two defects that CANCEL in the gate metric · an inert gate cell · a full-suite recipe without `-v` is VACUOUS · a sha256 roster desynced against a DELETED file · `gofmt -l` NEVER exits non-zero · `go doc -all <A> <B>` swallows arg2 · a `+0 exported symbols` gate over an EMPTY package reds on a CORRECT tree · a RANGE gate cannot detect anchor drift · a roster's naive `[ -f ] || continue` exits 0 on a DELETED file · a count-only stat guard PASSES a build with BOTH names wrong · a `-run` no-match exits 0 with `[no tests to run]` · a `--- PASS` tally over a package with sibling tests exceeds the fixture denominator · a stat-delta claim cannot be discharged by guards scoped to another package · a stderr-VOLUME assertion passes on the hang · `golangci-lint` misspell locale US · a harness's exit code is not the command's · a GOLDEN ROSTER that omits the family under test · a NEGATIVE CONTROL POINTED AT A TARGET THAT DOES NOT EXIST.

4. **⚠️ BEFORE ANY LINE-ADDING EDIT TO A HEAVILY-CITED DOC, ENUMERATE THE CITES YOUR INSERT WOULD SHIFT.** ⚠️ **The phase-82 SPEC's `DECISIONS.md` append shifted ZERO — PROVEN by `head -17796 … | cmp -`, not assumed.** Copy that habit. **`ROADMAP.md:<line>` cites total 118** (⚠️ **not the banked 119**), of which ~40 shift on any row insert and ~35 are structurally unrepairable (append-only phase records + `DECISIONS.md`).

5. **⚠️ LINE ANCHORS DRIFT; PHRASE AND SYMBOL ANCHORS DID NOT.** ⚠️ **`headerMapForType` is a METHOD** — grep the SYMBOL. It spans **`:178-190`** (⚠️ the BRAINSTORM's `:178-191` is off by one; the ROADMAP cell's `:186-192` is **WRONG ON ARRIVAL** and INVARIANT-BLOCKED). ⚠️ **The `OnDestroy` chain cites are `chain.go:666`/`:670`/`:672` — the banked `:665`/`:669`/`:671` are stale by +1.** **RE-VERIFY EVERY `.go` AND `.md` LINE CITE BEFORE RELYING ON IT.**

6. **⚠️ THE GATES, WITH MEASURED BASELINES.** Full 120-fixture differential **120/120 in 402 s** · `0036` alone **PASSES in 37.91 s** · h2spec **106 passed / 0 failed / 0 skipped** (⚠️ **the lineage's "53/53" is ONE HALF — state your own denominator**) · `go test ./cmd/envoy-go/` **8.7 s** · touched packages **~1 s**. `-race` is a SECOND run. ⚠️ **CI runs the differential at `-timeout 20m`, job `timeout-minutes: 30`** (`.github/workflows/ci.yml:60,63`).
    ```sh
    ( go test ./test/differential/ -count=1 -v > "$SCRATCH/full.log" 2>&1; echo "INNER_EXIT=$?" )
    grep -cE '^    --- PASS: TestDifferential/' "$SCRATCH/full.log"          # want 120
    grep -E  '^    --- (FAIL|SKIP): TestDifferential/' "$SCRATCH/full.log"   # want EMPTY
    grep -c  'no driver registered for fixture' "$SCRATCH/full.log"          # want 0
    grep -cE '^panic:|DATA RACE|SIGSEGV' "$SCRATCH/full.log"                 # want 0
    grep -o  'TestDifferential/[^ ]*' "$SCRATCH/full.log" | sed 's|TestDifferential/||' | sort -u \
      | comm -3 - <(ls -1 test/fixtures/ | grep -E '^[0-9]{4}[a-z]?-' | sort)  # want EMPTY
    ```
    ⚠️ The faithful dir predicate is `^[0-9]{4}[a-z]?-` — a bare `^[0-9]{4}-` gives **118**.
    ⚠️ **`go test ./test/differential/...` (with `...`) matches TWO packages and BUFFERS `-v` output.** A run that looks hung with an empty log is normal — do not kill it.
    ⚠️ **THE RUN ABORTS, IT DOES NOT MERELY FAIL. CAPTURE `INNER_EXIT` AND THE PANIC ARM.**
    ⚠️ **Stat surface: assert the DELTA by CALL-SITE ENUMERATION.** The `TestNoNewStat*` guards are PROVEN BLIND. Denominator **208 code sites / 36 production files** ⚠️ **never 208/84**.

7. **⚠️ THE SIX-GATE HAS A STANDING LINEAGE DEPARTURE — NAME IT, DO NOT CLAIM COMPLIANCE.** Gate **(f)** expects a `REVIEW.md`; **none since 25.3 — 86 of 123 phase dirs carry none** (37 carry one). ⚠️ **THE BANKED FIGURE WAS 85 of 122 AND IS WRONG** — the router counted before its own stage created the phase-82 directory. Gate **(d)** is **VACUOUS for a row that adds none** — say "vacuous", not "green" (**55** fuzzers). Gate **(c)**: proxy-wasm is INHERITED unless you run it — ⚠️ **and when you do, STATE THE DENOMINATOR: 10 of the cpp-host's 16 files (62.5%), 6 deferred.** ⚠️ **A DOCS-ONLY PLAN owes (a)-(f) only in the posture a docs-only stage can have — say which are INAPPLICABLE rather than claiming them green.** Normative cites, ALL RE-VERIFIED EXACT TWICE AT THE PHASE-82 SPEC: **§6.1 `:285`**, `:289` ~25-task, `:290` ~1500-LoC, **`:291` BLANK**, `:292` third trigger; **§6.2 `:294`**; **§6.3 `:304`** (⚠️ `:306` is the BODY — settled); **§7.5 `:357`, gates `:360-365`, close `:367`**. ⚠️ **`/BOOTSTRAP_PROMPT.md` WITH A LEADING SLASH RESOLVES NOWHERE** — the file is `<repo>/BOOTSTRAP_PROMPT.md` (**522** lines). ⚠️ **`docs/superpowers/plans/` CONTAINS NO FILE OF THAT NAME** — the 1024-line copy is `2026-04-21-envoy-go-bootstrap-prompt.md`, and its offsets are **NOT a constant shift** (§6.1 Δ+197, §7.5 **Δ+228**). **Open the repo-root file.**

8. **⚠️ ADR + DOCUMENT CONVENTIONS.** **The DECISIONS tail is `ADR-0304`, STATUS `PROPOSED`.** Next-free is **ADR-0305**. **303** `^## ADR-` headings, ids **0001-0304**, ⚠️ **exactly one gap at ADR-0209 — a gap NO prior document records, so "next-free = headings + 1" gives ADR-0304 and COLLIDES**; zero duplicates. STATUS census **17** (matcher `^> \*\*STATUS: `, a BLOCKQUOTE; ⚠️ `^\*\*STATUS:\*\*` matches NOTHING and reads as a false zero). Retained italic footers **10** (⚠️ ADR-0303's STATUS claims EIGHT and the exact-form grep already read NINE at that same commit — **state 10, or say "9 predecessors"**; matcher `^\*\(§Decision \+ §Consequences land at the phase-[0-9.]+ IMPL\.\)\*$`). ⚠️ **`^---$` is 216 and must stay 216.** ⚠️ **CARRY NO WHOLE-FILE GREP COUNT IN AN ADR** — that species self-falsified in ADR-0296 ¶3 and ADR-0302 ¶11. ⚠️ **ADR-0044 does NOT contain the §Context-at-SPEC discipline**; write **ADR-0044-as-used**. ⚠️ **THE RECURRENCE GUARD IS ARMED AND THE PLAN MUST LEAVE IT ARMED** — `^> \*\*STATUS: PROPOSED` fires **exactly once** (ADR-0304) and **the IMPL flips it to COMPLETE**. ⚠️ **The LOOSE form is a FALSE-POSITIVE GATE — demonstrated live: FIVE hits, FOUR of them COMPLETE blocks (ADR-0299/0300/0301/0302).**

9. **⚠️ A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX — the TWENTY-SIXTH consecutive stage at which this held.** The index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. **Still live:** `reference_sds_init_fetch_timeout_dial_budget_flake` (**TWO packages**) · the pre-existing `internal/cluster` `-race` outlier (`TestOutlierDetector_ConcurrentEjectExactlyOnce`) · `internal/httpclient TestOptions_ZeroValue_NoOpDefaults`. ⚠️ **TWO former flakes were FIXED at `f46ba419`/`f2dd994a` and a recurrence of either is now a FINDING.** ⚠️ **`0061-lb-ring-hash` is NOT a live flake.** **Classify, isolate-re-run with a POSITIVE selector assertion, and state the evidence.**

10. **⚠️ CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE — and NEVER tear down a container this session did not create**, by name or otherwise. The phase-82 SPEC created three (`p82s-a1-1..3`) and removed **exactly those, BY NAME**; no image or ancestor filter was used, and `docker ps -a --filter name=p82s` ended empty.

11. **Counts at this tip (re-run MECHANICALLY in your worktree; never copy them).** ⚠️ Use GNU `command grep` or `git grep` repo-wide — but ⚠️ **NOT `command grep` inside `xargs`.** **All re-derived at the phase-82 SPEC:**
    fixtures **120** (next-free **0119**; naive `^[0-9]{4}-` gives **118**; numeric tail `0118-runtime-static-layer`; prefixes contiguous 0000-0118) / fuzzers **55** / internal packages **73** / ⚠️ **phase dirs 123** (**NOT 122**), of which **37** carry `REVIEW.md` and ⚠️ **86** do not (**NOT 85**) / **BackendKind tail 38** over **39** declarations (do NOT "fix" 38 to 39) / go.mod modules **2** (the phase-61.2 lineage figure; the single `go.mod` requires **67**; `go list -m all` gives **240** — three different questions, do not conflate) / **`DECISIONS.md` 17824**, **303** headings, tail **ADR-0304 PROPOSED**, next-free **ADR-0305**, retained footers **10**, `^---$` **216**, STATUS census **17** / **`ROADMAP.md` 230 lines / 114 data rows** / **`BEHAVIOR_CONTRACT.md` 5870** / **`STATE.md` 63** / ⚠️ **`STATE_HISTORY.md` 442, tolerant anchor 171** (naive **161**; the difference is **10** parenthetical entries — ⚠️ **the banked 169/8 was wrong; it was 170/9 before this stage's own append**) / ⚠️ **`ROADMAP.md:<line>` cites 118** (**NOT 119**) / **stat registration: 208 CODE sites in 36 PRODUCTION files** ⚠️ **not 84** / **stat surface 1207** ⚠️ **DOCUMENTARY and CONFIG-CONDITIONAL — assert the DELTA**.
    ⚠️ **CONTESTED, SO NO NUMBER IS CARRIED:** the next-free REFERENCE port (routers say `10119`; `STATE.md` §Project says `10450`) · the `STATE_HISTORY.md` archive-gap total · production `stats.IsValidName` GUARD SITES **25** vs a naive line-grep's **56**.
    ⚠️ **DO NOT SOURCE ANY COUNT FROM `STATE.md` §Project.** It is **SIX PHASES STALE** (self-declared *"at this phase-76 IMPL close"* while the line above presents it as *"(Live…)"*) and wrong on at least three axes: fixtures `119` (live **120**), DECISIONS tail `ADR-0298` / next-free `ADR-0299` (live **ADR-0304** / **ADR-0305**), stat surface `1205` (live **1207**). **Anchor on §Current. Do NOT "fix" §Project.**

12. **⚠️ DOCUMENTARY DEFECTS — carried forward, still deliberately NOT fixed.**
    - ⚠️ **NEW: the header-map enum is WRONG in three landed places** — `internal/wasm/abi/types.go` (the declarations), `internal/wasm/abi/types_test.go:144-147` (**a hand-written golden pinning the wrong values**), and the append-only phase-25.1 SPEC record. **Row 82 fixes the first two (S0); the third is RECORDED, not fixed.**
    - ⚠️ **NEW: all three cpp-host cites in `http_call.go`'s header comment are WRONG for v1.37.2** — `:1547` is `resetStream()` (real site `:881-884`), `:1693` is `buffering_request_body_ = false` (real `:1834-1836`/`:1856-1858`), `:1900-1905` is `onGrpcCloseWrapper` reentrancy (real `:1328-1334`).
    - ⚠️ **NEW: cancel-at-destruction is a DEPARTURE documented as PARITY.** Upstream populates `http_request_` on the ROOT context, so it cancels at plugin teardown and an http call **outlives** its stream; envoy-go cancels at stream end.
    - ⚠️ **NEW: envoy-go passes the wrong `plugin_context_id`** to `proxy_on_http_call_response` (`http_call.go:427` passes `streamCtxID`; upstream passes the root id). Invisible to Rust guests, a wire divergence for others.
    - ⚠️ **NEW: there is NO cap on the http-call response body** (`http_call.go:302`, unbounded `io.ReadAll`); `bodyBufferCapBytes` is consumed only at `body.go:144`/`:265`. Upstream bounds it. **Pre-existing.**
    - ⚠️ **NEW: `internal/filter/http/wasm/trailers.go` is internally contradictory in three tenses** (types 1+3 "ACTIVATED at Task 16" / `:65` present-tense routing claim that is FALSE / `:90-99` Task 15 then Task 18), and its capture is a literal no-op. **The type-1/3 trailer map surface is DEAD.**
    - ⚠️ **`0036/inputs/driver.go:504-509` states scenario (l)'s divergence BACKWARDS** — and ⚠️ **`:313-327` IN THE SAME FILE is CORRECT and names the true root cause.** **Row 82 corrects the former.**
    - ⚠️ **`ROADMAP.md` row 82's summary carries TWO wrong-on-arrival claims** — `abi_callbacks.go:186-192` (the function is `:178-190`) and *"handles only map types 0 and 1"* (it is **0 and 2**). **INVARIANT-BLOCKED** by §Schema `:18` ⇒ **recorded, not fixed**, on the rows-57/69/78 precedent.
    - ⚠️ **`BEHAVIOR_CONTRACT.md:1967`** asserts a blocker its own successor phase KILLED (`ssl.connection_error`).
    - ⚠️ **`DECISIONS.md` ADR-0301 §Decision (b) says *"has failed twelve top-level detectors"*** — PRESENT TENSE inside immutable ADR text, factually wrong since row 80 landed. Alongside `ROADMAP.md`'s five `twelve` occurrences (⚠️ **four are pure FALSE POSITIVES**), and `0110`'s driver comment (**accurate**, 14 names / 12 families, but a FALSE POSITIVE for any `\btwelve\b` sweep).
    - **`ROADMAP.md` malformation: three rows lose summary content in GFM render** (lines 119, 131, 140), INVARIANT-BLOCKED => **defensible as a GUARD with the DISJUNCTION gate, not a fix**. ⚠️ **There is NO doc-invariant test anywhere in this repo.** The reusable precedent is `test/differential/harness_test.go:123-137`'s `runtime.Caller(0)` -> repoRoot idiom.
    - **`STATE.md` §Project counts self-contradicts §Current and is FROZEN at the phase-76 IMPL close.** **Anchor on §Current. Do NOT "fix" §Project.**
    - **`BEHAVIOR_CONTRACT.md`'s `wasm.` arm is called a *"NEW SN9 flattening rule"*, which COLLIDES with ADR-0118's actual SN9.** Pre-existing.
    - ⚠️ **The two `//nolint:gosec` directives row 80 added are INERT** — `gosec` is not among the 9 linters `.golangci.yml` enables (`disable-all: true`; `govet errcheck staticcheck unused ineffassign gofmt goimports misspell revive`; golangci-lint **v1.64.8**). ⚠️ **`nolintlint` is NOT enabled either**, so the five stale `//nolint:unused` directives in `0036`'s driver will not fail lint.
    - **`golangci-lint-action@v6.5.2` emits a Node-20 deprecation warning in CI.** Deliberately NOT bumped: v7+ requires golangci-lint **v2** while the repo pins **v1.64.8**. **A warning, not a failure.**
    - ⚠️ **`ROADMAP.md` §Schema `:18` says *"Sub-phases get their own rows"* while practice puts split legs in the parent's `sub-phases` PROSE** (no sub-phase ROW since 32.2). **Record the tension; do not relitigate.** ⚠️ **This is LIVE for the phase-82 PLAN if it splits.**
    - **Production-DEAD trailer code** in four filters · **two stale non-test comments** at `internal/filter/hcm/connection.go:565` and `h2dispatch.go:503` claiming `RunDecodeTrailers` does not exist (it does, at `internal/filter/http/chain.go:455` — ⚠️ **ZERO non-test callers, but 14 non-test MENTIONS across 5 files, not the banked "2 in chain.go"**) · **`abi_callbacks.go:594-596`** calls `ContinueStream` an Unimplemented stub when `:766` is **live** · **`DECISIONS.md`'s wasm module-function roster** enumerates 24 keys, **8 of which do not exist in `internal/`** · **one defective `freeTCPPort`** (`test/conformance/h2spec/h2spec_test.go:219`), which **cannot** abort the differential.
    - ⚠️ **`internal/filter/network/rbac/rbac.go:50` contains the token `F2`** as an unrelated **phase-26 fork label**. **Do not use `F2` as a grep anchor in that package.**

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — take it from `git rev-parse master`, **NOT from a SHA quoted in this file**. The phase-82 SPEC squash is not named here on purpose: **router-only commits routinely land above it**. ⚠️ **AND THE BARE SUBJECT-LOCATE GREP IS CONTAMINATED BY THIS FILE'S OWN PROSE ABOUT IT** — `git log` greps the whole commit message and router commits discuss the phase in their BODIES, so **the sentence warning about the grep satisfies the grep.** ⚠️ **USE THE SLUG-ANCHORED FORM**, which is stage-exact:
```sh
git log --grep '^phase 82 (wasm-http-call-response-cache)' --format='%h %s'   # 2 at this tip: BRAINSTORM, SPEC
```
Measured with both controls: an invented slug returns **0**, and the completed phase-81 form returns **4**. **Take the NEWEST line (`| head -1`) and verify the count before relying on it.** Subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** (`reference_parallel_subagents_private_scratch`) and they **must tear down docker containers BY NAME, never by an `ancestor`/image filter** (`reference_parallel_agents_shared_machine_namespaces`). ⚠️ **THERE ARE TWO RESERVED PORT BANDS — band private ports outside BOTH `20000-31007` (subject blocks) AND `11000-14999` (backends), and also clear of the static fixture ports `10000-10447` / `15000-15011` / `18001-18007`.** The `42000-42999` band has been used safely (phases 80, 81, 82 BRAINSTORM and SPEC). ⚠️ **If two agents share ONE worktree they must stage by EXPLICIT PATH** and **must NOT run an unscoped `git restore`**. ⚠️ **PREFER ONE WORKTREE PER AGENT OFF A COMMON BASE** — that is what the phase-82 SPEC did for four investigation agents, all of which returned `git status --porcelain` = 0. Per-task `gofmt` + `golangci-lint` on touched packages (`feedback_pertask_gofmt_lint`). ⚠️ **`gofmt -l` NEVER exits non-zero — gate on OUTPUT.** ⚠️ **`golangci-lint` runs `misspell` with `locale: US`.** ⚠️ **AN EMPTY LINT RESULT IS NOT EVIDENCE THE LINTER LOOKED — inject a British spelling, confirm it fires, restore byte-identically.** ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch, and note `-o` precedes the package path.**

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. ⚠️ **ASSUME IT FIRES EVERY SESSION — it has been treated as live for THIRTY consecutive sessions and it FIRED REPEATEDLY, OBSERVED, at the phase-82 SPEC** (`Shell cwd was reset to /home/esa/git/envoy-go`). **Use `git -C <abs-worktree-path>` for every git command, prefer absolute paths over `cd`, put any needed `cd` FIRST in a compound command, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN.** At the phase-82 SPEC four agents wrote throwaway `.go` probes and prototypes, ran them, reverted them, and confirmed `git status --porcelain` = 0 lines. A stage that ships a stray `.go` file has silently changed its own gate obligations.

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline — ⚠️ **anchor greps on `^- **<field>`**; all seven fields verified singleton at the phase-82 SPEC close). ⚠️ **§Project counts SELF-CONTRADICTS §Current and is SIX PHASES STALE** — do not anchor on it and do not fix it.
2. `docs/envoy-go/phases/82-wasm-http-call-response-cache/SPEC.md` — **the whole document.** §1 the masked defect, the trap synthesis and the swapped enum; §1.3 the eight scope items; §2 the twenty-three-item refutation ledger; §3-§8 the six D-question dispositions; §12 **the cost basis and the non-negotiable split ORDER**; §14 the twelve hazards.
3. `docs/envoy-go/phases/82-wasm-http-call-response-cache/BRAINSTORM.md` — **read it for the rejected alternatives (§2) only.** ⚠️ **Its §1 and §4 central claims are REFUTED by the SPEC — do not re-inherit them.**
4. `docs/envoy-go/ROADMAP.md` — **§Schema first** (`:18` carries BOTH the update-in-place invariant AND *"Sub-phases get their own rows"* on ONE line), then row 82, then the five deferred-candidate paragraphs. **You are adding NO row and `want` STAYS 114.**
5. `BOOTSTRAP_PROMPT.md` — **at the REPO ROOT, no leading slash.** §6.1 `:285`, §6.2 `:294`, ⚠️ **§6.3 `:304`**, §7.5 `:357-367`. ⚠️ **Not the 1024-line copy under `docs/superpowers/plans/`, which is NOT named `BOOTSTRAP_PROMPT.md` and is NOT a constant offset.**
6. `docs/envoy-go/DECISIONS.md` — **ADR-0304** (this row's own, STATUS `PROPOSED`) · **ADR-0303** · **ADR-0288** (roll-IN-PLACE and the archive rule) · **ADR-0044** as-used · **ADR-0045** (⚠️ **its per-bucket task+LoC cost form is the one to imitate if the PLAN splits**) · **ADR-0106** (the sole-leg property).
7. `docs/envoy-go/phases/81-stats-name-charset-guards/PROGRESS.md` — **the `# PLAN record` and the `# IMPL record`**, for calibration: a 3.07x cost overrun **over a measured-basis estimate**.
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — especially **`reference_code_comment_not_evidence`** (⚠️ **fired in BOTH directions at the SPEC**), **`reference_handwritten_golden_shares_author_mistake`** (⚠️ **the enum golden**), **`reference_compensating_defects_cancel_in_the_gate_metric`** (⚠️ **the (l) stats arm**), **`reference_measured_prototype_is_a_lower_bound`**, **`reference_vacuous_break_modes`**, **`reference_deliberate_break_wrong_assertion`**, **`reference_break_arm_injection_site_is_a_claim`**, **`reference_liveness_break_needs_failing_baseline`**, **`reference_empty_output_is_not_a_zero_result`**, **`reference_probe_must_discriminate`**, **`reference_probe_input_is_a_claim`**, **`reference_gate_command_negative_control`**, **`reference_a_drift_correction_is_itself_a_claim`**, **`reference_lifted_reject_hidden_enforcement`** (⚠️ **S0+S5 atomicity**), **`reference_sample_is_not_an_audit`**, **`reference_stale_cite_recurs_fix_by_pattern`**, **`reference_recursive_grep_blind_to_gitignored_tracked_file`**, **`reference_sentinel_matcher_string_self_clears`**, **`feedback_brief_citations_not_evidence`**, `feedback_git_worktrees`, `feedback_execution_style`, and **`reference_bash_cwd_reset_commits_to_main`**.
