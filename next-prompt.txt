START a fresh cold-start session for envoy-go — **phase 31 (`network-filter-kafka-broker`) IMPL is DONE; phase 31 is CLOSED**: the FIFTH §9 Network-filters-family row LANDED. `kafka_broker` (`envoy.filters.network.kafka_broker`) shipped as a new `internal/filter/network/kafkabroker/` package — a passive both-direction Kafka HEADER-ONLY observability sniffer at FULL per-API-key request/response counter parity (the 9th built-in network filter; consumer #3 of the ADR-0221 WriteFilter seam; the project's FIRST `github.com/envoyproxy/go-control-plane/contrib v1.32.4` consumer). All 17 PLAN tasks landed, six gates GREEN, ROADMAP row 31 `in-progress → done` (a flat §9 row, NO parent rollup). The next lifecycle step is the **next §9-row BRAINSTORM** (`superpowers:brainstorming` — per `feedback_execution_style`; work in a git worktree per `feedback_git_worktrees`).

The phase-31 IMPL landed at the IMPL squash `<CONTROLLER FILLS THE SHA>` on master (squash-merged from `phase-31-network-filter-kafka-broker-impl` + pushed by the controller; a docs-only SHA-fill follow-up becomes the live tip — confirm via `git log --oneline -1`). Substantive predecessor on master: the phase-31 PLAN squash `12f7cac` (docs-only SHA-fill `596fccd` trails it). Before that: the phase-31 SPEC squash `6104036`; the phase-31 BRAINSTORM squash `78d5f36`; the phase-30 IMPL squash `2d24f34` (phase 30 CLOSED). origin/master in sync after push.

## What this session does — the next §9-row BRAINSTORM

The §9 Network-filters family stays OPEN with **2 candidates {redis, thrift}** — both terminal-proxy surfaces (command routing + upstream connection pooling) needing routing/pooling seams of their own; each carries forward UNBRAINSTORMED and needs its own brainstorm-time risk assessment. Open a fresh BRAINSTORM (`superpowers:brainstorming`) that settles, via a multi-question user dialogue mirroring the phase-29/31 §9-sniffer template:

- **Q0 subject selection** — redis vs thrift (the two remaining §9 candidates). NOTE the structural difference from every prior §9 row: zookeeper/mongo/kafka_broker were OBSERVATIONAL sniffers on a `[filter, tcp_proxy]` chain; redis and thrift are TERMINAL proxies (they route commands to upstreams + pool connections themselves). The brainstorm MUST assess whether the chosen subject needs NEW framework capability the as-built §9 framework does not provide (command-routing + per-key/per-shard upstream pooling — neither exists at master tip), and whether to scope an observational-only MVP first.
- **Q1 envelope** — the decode/counter/routing scope (mirror the kafka_broker header-only-vs-full-parse decision; for a terminal proxy also the routing + pooling scope).
- **Q-split** — single flat §9 row vs a pre-split (the ADR-0045 split-gate; redis/thrift may be heavier than kafka_broker given the routing/pooling seams).
- The empirical-pin obligations the SPEC will execute live against the contrib reference Envoy (`reference_docker_probe_bridge_network` — the contrib image already pinned at ADR-0227).

Register the new ROADMAP row `in-progress` + anchor the new ADR §Context (next-free **ADR-0229**) at the BRAINSTORM/SPEC per ADR-0044. Counts stay UNCHANGED at the BRAINSTORM (they advance at the IMPL).

## Honor throughout

- `feedback_execution_style` (subagent-driven IMPL when it gets there; the BRAINSTORM itself is a user dialogue) · `feedback_git_worktrees` · `feedback_subagents_no_push` (subagents commit local-only) · `feedback_push_to_origin` (the controller squash-merges + pushes at stage-close) · `feedback_pertask_gofmt_lint`.
- ADR-0106 (§9 family flat-row discipline) · ADR-0045 (split-gate) · ADR-0221 (WriteFilter conn-wrap seam) · ADR-0226 (async halt/resume seam) · ADR-0223 (per-connection mutex) · ADR-0060 (histograms deferred) · ADR-0044 (§Context-at-BRAINSTORM/SPEC, §Decision+§Consequences-at-IMPL) · ADR-0080 (byte-stable rejects).
- Project memories: `reference_network_filter_typeurl_extensions`, `reference_wire_format_both_sides_see_same_bytes`, `reference_close_direction_framework_gap`, `reference_docker_probe_bridge_network`, `reference_dynamic_stat_name_charset_guard`, `reference_network_chain_terminal_handoff_ends_ondata`, `reference_differential_asserter_dispatch`, `reference_differential_fixture_dispatch_constraint`, `reference_differential_break_protocol_count1`.

## Read first (in order)

1. THIS FILE — the §9-next-row BRAINSTORM cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 31 IMPL done — phase 31 CLOSED`; lifecycle-state → the §9-next-row BRAINSTORM; next-skill `superpowers:brainstorming`; the current counts (536 stats / 56 fixtures / 40 fuzzers / BackendKind tail 31 / DECISIONS tail ADR-0228, next-free ADR-0229).
3. `docs/envoy-go/ROADMAP.md` — the §9 Network-filters family roster ({redis, thrift} remain after kafka_broker) + the row-26..31 history (row 31 `done`).
4. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the `### envoy.filters.network.*` subsections (echo / sni_cluster / zookeeper_proxy / mongo_proxy / **kafka_broker** — the §9-row precedents) + the `## Network filters` framework subsections (the chain framework + the 28.1 conn-wrap seam + the 29.3 async halt/resume seam).
5. `docs/envoy-go/DECISIONS.md` — ADR-0106 / ADR-0045 / ADR-0221 / ADR-0226 / ADR-0223 / ADR-0060 / ADR-0044 / ADR-0080 + ADR-0228 (the kafka_broker filter — the most-recent §9-row decision record, body landed at the phase-31 IMPL).
6. The phase-31 + phase-29 phase directories (`docs/envoy-go/phases/31-network-filter-kafka-broker/` BRAINSTORM/SPEC/PLAN/PROGRESS + `docs/envoy-go/phases/29*/`) — the §9-row lifecycle precedents (BRAINSTORM → SPEC → PLAN → IMPL).
7. `internal/filter/network/kafkabroker/` + `internal/filter/network/mongoproxy/` (the §9 package-shape precedents) + `internal/filter/network/` framework (`chain.go`/`terminal.go`/`prefixconn.go`/`writeconn.go`/`readconn.go` — the read/write/terminal seams a terminal proxy would extend) + `internal/stats/name.go` (the prom-arm patterns).
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`.

## Counts at master tip (phase 31 CLOSED — UNCHANGED until the next IMPL)

- stat surface **536**; differential fixtures **56** (tail `0054-kafka-boot-reject`); fuzzers **40** (`FuzzKafkaDecode`); BackendKind tail **31** (`TCPKafkaResponder`); DECISIONS.md tail **ADR-0228** (next-free **ADR-0229**).
- conformance h2spec 53/53 + proxy-wasm 10/10 (image-independent).
- reference pin: `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227).
- Toolchain: Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); go-control-plane `/envoy` v1.32.4 (ADR-0008) + `/contrib` v1.32.4 (the phase-31 dep).
- §9 Network-filters family: rows 26/27/28/29 DONE + row 30 (pin-refresh) DONE + **row 31 `kafka_broker` DONE**. After phase 31 the candidates {redis, thrift} remain (both terminal-proxy surfaces).
