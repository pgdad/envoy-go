=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

**THERE IS BANKED MID-LIFECYCLE WORK AT THIS TIP.** Phase 82 (`wasm-http-call-response-cache`) was **BRAINSTORMED (2026-08-01), SPEC'd (2026-08-01) and PLANNED (2026-08-02)**; **row 82 is `in-progress` and the phase is OPEN**. ⇒ **This session does NOT self-pick. It performs the PHASE-82 IMPL — the FINAL stage, which CLOSES the phase.**

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=114 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **CHECK (1) IS LIVE — it prints `NOT DONE: row 82`.** ⚠️ **AND YOUR IMPL IS THE STAGE THAT SILENCES IT.** The moment row 82 flips `done`, check (1) goes silent, and **silence is otherwise indistinguishable from a broken check** — so the doctored-copy NC is MANDATORY at your stage, not optional. The form that works (a `sed` targeting `done      ` does NOT — it misses the real `| done |` column spacing and prints nothing, which reads exactly like *"the check is blind"*):
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
```
Verified again at the phase-82 PLAN: the row-62 NC fires (`NOT DONE: row 62` alongside row 82) and the `want=113` denominator NC fires.

⚠️ **`want=114` IS A LIVE FIGURE, NOT A CONSTANT.** It is the ROADMAP data-row count. **A row-adding phase MUST bump it in the same commit that adds the row.** The phase-82 BRAINSTORM bumped it 113 -> 114. ⚠️ **THE IMPL ADDS NO ROW — IT FLIPS ROW 82'S `status` CELL IN PLACE — SO `want` STAYS 114.**

Check (3) fails SAFE by construction. When you open a new family, add its row-summary slug to the list above.

**SENTINEL RE-RUN MECHANICALLY AT THE PHASE-82 PLAN, WITH FIRING NEGATIVE CONTROLS. IT DOES NOT FIRE. `stop` was NOT created; `ls stop` => `No such file or directory`, and it MUST NOT be created until row 82 is `done` AND checks (2) and (3) also go silent — which they will NOT at your stage.** ⚠️ **Re-run it yourself anyway and record what YOU see.** ACTUAL output, recorded rather than predicted:
- **(1) `NOT DONE: row 82`** at `want=114`, denominator printed (`examined 114 data rows`). Both NCs fired.
- **(2) FIVE — `:192 :202 :212 :218 :226`, UNCHANGED. The THIRTY-FOURTH consecutive phase at which check (2) did not go down. STATED, not forecast.**
- **(3) `NEVER OPENED: gRPC` — ALONE.** NC: an invented slug fires; the registered `WASM-family row` correctly printed nothing.
- Input measured at **230 lines / 114 data rows**, so an empty result could not read as a zero result.

⚠️ **THE LEAK CHECK GOES LIVE AT YOUR STAGE — THE IMPL WRITES ROW 82's CELL.** The rule: **NEVER WRITE A SENTINEL'S OWN MATCHER STRING INTO A FILE THE SENTINEL GREPS.** The sentinel greps `docs/envoy-go/ROADMAP.md` — writing these strings *here* (or in `DECISIONS.md`) is safe; writing them *there* is not.
⚠️ **AND THE TWO CLASSES ARE OPPOSITE.** `<slug>-family row` is check (3)'s **PASS** condition. `deferred candidates:` / `remaining deferred (not-yet-chartered) candidates:` are check (2)'s **FAIL** conditions.

⚠️ **A DRAFTING FOOTGUN, STILL LIVE AND NOW YOURS TO NOT BREAK:** check (3) needs the literal `WASM-family row`. **Controller-verified at the phase-82 PLAN: it occurs EXACTLY ONCE repo-wide in `ROADMAP.md`, at line 144, inside row 82's SUMMARY column (field 7); the `sub-phases` column (field 6) is EMPTY.** The heading is `### WASM host family` and the lineage's row convention would naturally produce `WASM-host-family row` — **which does NOT match.** **Your IMPL edits row 82's `status` cell (field 5); DO NOT disturb field 7, or check (3) silently regresses.**

⚠️ **ROW WELL-FORMEDNESS: THE GATE MUST BE A DISJUNCTION.** Over all **114** rows: **ARM-A (escape-aware, `pieces != 8` on a `|`-split) catches lines 119 and 131 ONLY; ARM-B (escape-aware trailing piece) catches line 140 ONLY.** ⚠️ **Naive `NF==8` flags SEVENTEEN — 15 FALSE POSITIVES + 2 true — and MISSES line 140** (compensating defects cancel) ⇒ wrong in **both** directions. **Row 82 is flagged by neither arm.** Re-verified at the PLAN.

⚠️ **A GATE-AUTHORING TRAP THE PHASE-82 PLAN'S CONTROLLER HIT LIVE AND RECORDED RATHER THAN HID:** its first ARM-A used the wrong pipe-split threshold (`!=9` instead of `!=8`) and flagged **113 of 114 rows**. **A gate that fires on almost everything reads as THOROUGH and is WORTHLESS. Only the printed denominator exposed it.** Print your denominators.

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …`. **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Reproduced at TEN separate tips.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS** (wrapped prints bare paths, GNU prints `./`-prefixed). ⚠️ **Print FILE NAMES, never a count.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** — git exempts TRACKED files. ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** (`xargs` execs a binary; `command` is a shell keyword) — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **`grep -c` ON ZERO MATCHES IS TOOL-SPECIFIC**: only `git grep -c` prints nothing; GNU and the wrapper print `0`. Both exit 1, so an exit-code gate is wrong for either. ⚠️ **AND `grep -c` COUNTS *LINES*, NOT *OCCURRENCES*** — re-demonstrated at the phase-82 PLAN: the `ROADMAP.md:<line>` cite census reads **117** by occurrence and **107** by `git grep -c` sum, because ten lines carry two cites each. ⚠️ **`\t` IN GNU ERE IS A LITERAL `t`** while the harness function normalizes it ⇒ the trap bites exactly where gates run and is invisible interactively. **Use `-P` unconditionally.**

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`** with FOUR blind spots (an em-dash, a DOT in `http-1.1`, two LETTER-suffixed ids). Every document from phase 75 forward called it *"currently HARMLESS."* ⚠️ **Nobody fired the unsafe arm until the phase-77 PLAN. EXECUTED on scratch copies: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** ⚠️ **DO NOT restore the regex form.**

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

⚠️ **Run the union form, never one half of it.** ⚠️ **A ONE-ARM STRIP IS NOT AN NC**: `sed 's/deferred candidates://'` moves the union **5 -> 4, not 5 -> 0**. Re-confirmed at the phase-82 PLAN. ⚠️ **The documented trap's stated MECHANISM is refuted while the trap is real** — the inflation comes from ROADMAP table rows carrying BRAINSTORM prose, not from `candidates were:` recaps. **Cite the command; do not re-inherit the explanation.**

⚠️ **CHECK (2) HAS NEVER GONE DOWN** across ~34 phases (history `0 -> 1 -> 3 -> 4 -> 5`) => **the candidates sentence is a WINDOW onto a larger deferred backlog, not an inventory of it.** ⚠️ **A stage that predicts a decrease is repeating the phase-73 error. State what your row narrows — usually nothing. Row 82 narrows NOTHING: the WASM family carries no deferred-candidate sentence at all.**

⚠️ **`:212` IS NOT A SENTENCE ANCHOR.** It is a **45,959-char** line carrying **SIX** occurrences of the deferred-candidate phrase; only the one at **offset 14459** ends in the colon-terminated form the sentinel matches. Citing *"the `:212` sentence"* without the offset is ambiguous across six different rosters. ⚠️ **And `:226` does not carry the long phrase at all** — SHORT form only, which is exactly the Operational-tooling blindness check (2) was broadened to fix.

==============================================


## STATUS AS OF 2026-08-02: PHASE 82 (`wasm-http-call-response-cache`) IS **OPEN**. ROW 82 IS `in-progress`. LIFECYCLE-STATE **3**. THIS SESSION PERFORMS THE **PHASE-82 IMPL — THE FINAL STAGE, WHICH CLOSES THE PHASE.**

**What the phase-82 PLAN landed.** Base master **`71fc86d7`** (from `git rev-parse master`), branch `phase-82-plan`. Docs-only: **ZERO production `.go`, ZERO test `.go`.** NEW `PLAN.md`; `PROGRESS.md` gained a `# PLAN record`; `STATE.md` rolled in place (63 -> 63); `STATE_HISTORY.md` **442 -> 444**; this file. **`ROADMAP.md`, `DECISIONS.md` and `BEHAVIOR_CONTRACT.md` BYTE-UNTOUCHED** — verified by `git diff --stat HEAD -- <all three>` returning EMPTY.

### ⚠️ THE PLAN'S HEADLINE: THE SPEC'S DOMINANT COST ITEM WAS OVER-ESTIMATED ~15x, SO THE SPLIT AXIS DISSOLVED. **READ `PLAN.md` §1 AND §4 BEFORE ANYTHING ELSE.**

**Five investigation agents, each in its own DETACHED worktree with private scratch and port bands in `42100-42349`. FOURTEEN refutations, SIX load-bearing.**

1. ⚠️ **S1 WAS PRICED AT 300-600 NET AND MEASURES 30.** Prototype built (`go build` / `vet` / `golangci-lint` / unit / `-race` all green), then reverted. **Because FOUR of the SIX production `ProxyActionPause` arms ALREADY HONOR PAUSE** — `body.go:227`/`:314` return `DataStopIterationAndBuffer`, `trailers.go:143`/`:200` return `TrailersStopIteration`. **Only the two HEADERS arms are deferred.** *"Stream control deferred since 25.2"* is **FALSE as a blanket claim**. ⇒ **the SPEC's Leg A / Leg B split axis dissolves; the PLAN does NOT split.**
2. ⚠️ **S1 ALONE HANGS *AND LEAKS*.** With S1 the callback fires (`http_call_response` 0 -> 1, `after_close` 20 -> 0) but the request hangs 10 s to client timeout, and **30+ s later `downstream_cx_active = 1` and `wasm_wazero_active = 1` with no `OnDestroy`.** `parkDecode` waits on `decodeResumeCh` or `ctx.Done()` and nothing cancels that ctx on downstream disconnect. **An unbounded connection + goroutine leak, one per request. NEW scope item S9 — no prior document names it.**
3. ⚠️ **THE SPEC'S OWN REPLACEMENT GATE GOES GREEN ON A TRAP.** `rv.stats.HttpCallResponseInc()` fires at `http_call.go:423`, **BEFORE** the guest call at `:425`. So §8.1's prescribed positive leg `http_call_response >= 1` is blind to the row's worst failure mode. **T4 exists to fix the ordering.**
4. ⚠️ **S5 SILENTLY OPENS A GUEST *WRITE* SURFACE.** `headerMapForType`'s `active` flag has **SEVEN** consumers — three getters (`:201 :235 :331`) and **FOUR MUTATORS** (`AddHeaderMapValue :257`, `ReplaceHeaderMapValue :272`, `RemoveHeaderMapValue :287`, `SetHeaderMapPairs :305`). A measured probe had the guest **mutating the stashed map**. Upstream documents it read-only. **T1 must add an explicit read-only guard.**
5. ⚠️ **THE GUEST BLOB IS BYTE-REPRODUCIBLE ONLY UNDER rustc 1.94.0 + `log 0.4.30`, AND NEITHER PIN IS RECORDED.** `scripts/.gitignore:7` excludes `Cargo.lock` (0 tracked repo-wide) so `log` floats; `scripts/README.md:24-25` says *"1.94.0 (or newer compatible)"* which is **measurably false in both directions** — the documented recipe **fails to link** on the installed default 1.96.0 (`undefined symbol: proxy_continue_stream` x15+). ⚠️ **AND NO CHECKSUM GATE EXISTS ANYWHERE**: `git grep '4e630adf\|139655'` ⇒ **zero hits repo-wide**; CI carries no Rust toolchain. **A wrong blob passes every gate silently.**
6. ⚠️ **THE ACCESSOR FORK HAS FOUR OPTIONS, NOT TWO, AND THE SPEC'S PREFERRED ONE IS DOMINATED.** All four built and compiled. A **single exported `*RootVM` method** is **76 net / +1 method / 0 implementer churn**; the SPEC's option A is 93 / +3 / 0. ⚠️ **`ABICallbacks` is 21 methods TODAY, not 20** (`go doc` ⇒ 21, NC fires) — option B is 21 -> **22**, blast radius **4 types** compiler-enumerated including `test/conformance/proxy-wasm/conformance.go:555` (test-support, **not** `_test.go`). The "20" traces to a **wrong landed comment** at `internal/wasm/doc.go:219`.

### ⚠️ WHAT THIS SESSION DOES — THE PHASE-82 IMPL

**One stage per session.** Execute `PLAN.md`'s **SIXTEEN tasks IN THE STATED ORDER**, append an `# IMPL record` to `PROGRESS.md`, complete **ADR-0304** (§Decision + §Consequences, IN PLACE after the retained footer), extend `BEHAVIOR_CONTRACT.md`, and **FLIP ROW 82 `in-progress` -> `done`, CLOSING THE PHASE.** ⚠️ **`want` STAYS 114** — the IMPL adds no row, it updates `status` in place (§Schema `:18` permits exactly `status` and `sub-phases`).

**⚠️ THE ORDER IS A CORRECTNESS CONSTRAINT, NOT A PREFERENCE.** T1-T4 (enum + cache + trap + counter ordering) MUST precede T5 (S1). The vendored `proxy-wasm-rust-sdk` v0.2.4 `get_map` (`~/.cargo/registry/.../proxy-wasm-0.2.4/src/hostcalls.rs:158-174`, controller-verified verbatim) has **ONLY a `Status::Ok` arm plus `status => panic!("unexpected status: {}")` — NO `NotFound` ARM**. Honoring Pause against an unwired cache converts a benign wrong header into a **guest TRAP plus a BUG-3 `panic_already_borrowed` poison cascade**. `get_map_bytes` (`:176-192`) is identically shaped.

**THE SIXTEEN TASKS:** **T1** S0 enum + S5 dispatch arms + the four-mutator read-only guard (**ATOMIC**) · **T2** the cache via the single-method accessor · **T3** trap propagation · **T4** `HttpCallResponseInc` ordering · **T5/T6** S1 headers only · **T7** S9 the parked-stream leak · **T8** the capability-enabled test harness · **T9-T12** tests · **T13** the blob **AND the two pins** · **T14** the fixture · **T15** the break roster · **T16** gates + ADR + row close.

**ALREADY SETTLED — DO NOT RE-OPEN:** **NO SPLIT** (band **~800-1175 net, budget ~1050, 16 tasks**; neither §6.1 trigger fires at 1.6x on tasks / 1.4x on LoC) · the **single-method `*RootVM` accessor** · **trap propagation REUSES `EnvoyGoFailuresInc()`** (already on `RootStatsRecorder` at `stats_recorder.go:140`, already registered as `envoy_go.failures` at `stats.go:109,464`) · **S1 is HEADERS-ONLY** · D-82-DIRECTION · D-82-TRAILERS · D-82-LIFETIME (callback-scoped on `*RootVM`, `atomic.Pointer`) · D-82-COUNT · D-82-STATUS · D-82-BREAK.

### ⚠️ GATE HAZARDS THE IMPL MUST CARRY

- ⚠️ **STAT SURFACE `+0` SURVIVES ONLY UNDER ONE DESIGN.** Reuse `EnvoyGoFailuresInc()`. **If you mint a dedicated `http_call_response_trap` counter, `+0` BREAKS and §9 must be amended.** ⚠️ **Assert the DELTA by CALL-SITE ENUMERATION** — the `TestNoNewStat*` guards are PROVEN BLIND. Denominator **208 code sites**; ⚠️ **35 CODE-BEARING production files of 36 matched** (`internal/stats/doc.go` contributes 0). ⚠️ **The discredited "84" is now IDENTIFIED: it is `git grep -l … -- '*.go' | wc -l`, a FILE count WITH TESTS INCLUDED — wrong on both axes.**
- ⚠️ **`decodeResumeCh` IS CHAIN-SCOPED**, declared at `chain.go:59` and constructed once per chain at `:303`. An unmatched `ContinueDecoding` **latches a token that spuriously un-parks a DIFFERENT filter** (extauthz, ratelimit, oauth2, bandwidthlimit all park on it). Gate the resume with `CompareAndSwap(true,false)`. Conversely the buffered-1 channel means an early resume is **latched, not lost** — there is no lost-wakeup race.
- ⚠️ **S1 BREAKS ADR-0071's SINGLE-GOROUTINE-PER-STREAM INVARIANT.** A plain `bool` paused flag **fires `-race` on the first request** (`ContinueStream` read from the dispatch goroutine vs `DecodeHeaders` write). `atomic.Bool` is required — **a SECOND, independent atomicity requirement beyond the stash's `atomic.Pointer`.** **ADR-0304 §Consequences must carry this.**
- ⚠️ **122 OF 128 `CallProxyOnRequestHeaders` GATE-SITES ACROSS 423 TESTS ARE CAP-DENIED**, from only **FOUR** distinct tests. `newTestCompiledConfig` (`dispatch_test.go:68`) sets no `capability_restriction_config` and `SandboxConfig.IsAllowed` is StrictDefaultDeny. ⇒ **`TestFilter_DecodeHeaders_Pause_LogAndContinue` is VACUOUS and always has been** (a `panic` in the Pause arm does NOT fire; a `panic` in `default:` DOES). **T8 exists to fix this and every later test task depends on it. It is the dominant test-cost driver.**
- ⚠️ **TWO LANDED TESTS GO VACUOUS-GREEN UNDER S5 AND MUST BE FIXED, NOT TRUSTED:** `abi_callbacks_test.go:237-255` asserts `ok == false` for six deferred map types **including 6 and 7** and still passes because the fixture leaves `filter.cfg` nil; `:333` requires `Unimplemented` from a mutator and passes for the same reason. **In production both surfaces activate.**
- ⚠️ **THE (l) COUNTER IS A RACE, SO `delivered=0` IS NOT A FAILING-FIRST ANCHOR.** An agent observed **3/5 delivered** across five fixture runs, and reported that every `delivered=1` run logged a Rust `panic_already_borrowed` cascade. ⚠️ **THIS IS AN AGENT MEASUREMENT THE CONTROLLER COULD NOT RE-DERIVE — the fixture prints those counters only on FAILURE and the differential log carries NO subject stderr (measured: 29-line logs, zero `unreachable`/`CallProxyOn` matches). The CHANNEL failed, not the claim. Capture the subject's stderr directly, and you MUST before relying on any (l)-arm baseline.** **BREAK-D is a coin flip until T5.**
- ⚠️ **BREAK-C IS ALREADY GREEN AND IS THE NEGATIVE CONTROL.** A flagrantly wrong value injected **symmetrically** (`status=999 body=BOGUS`) still PASSES, because one `driveProxy` serves both `DriveReference*` and `DriveSubject*`. **A fixture-layer break MUST branch on the `side string` parameter** — which `driveProxy(ctx, addrs, side string)` already threads. ⚠️ **BREAK-B is NOT RUNNABLE TODAY** — `headerMapForType` has only `case 0`/`case 2`/`default:`; there is nothing to revert until T1. **BREAK-B is MANDATORY once it exists.**
- ⚠️ **ORDER T13 (the blob) BEFORE ANY BREAK VERIFICATION** — until it lands, a red on the cross-side arm is misattributed to the reference hang. **Confirm WHICH assertion fired, not merely that one did.**
- ⚠️ **`0036`'s CROSS-SIDE COMPARISON IS 100% VACUOUS AND THIS IS NOW PROVEN BY EXECUTION.** `driveProxy` emits **686 bytes** of compile-time constants — ⚠️ **10 `emitConstantSkipToken` calls (arms a-j) + 4 `Fprintf` literals = 14 LINES**, not the SPEC's "14 calls plus 4 literals". ⚠️ **And removing all FIVE `//nolint:unused` directives leaves `golangci-lint` with ZERO findings** (NC: an injected unused func fires) — `classifyBody` calls the other three transitively, so **all five go live, not two**.
- ⚠️ **THE FLIP NEEDS A NEW `case "l":` IN `classifyBody` — THERE IS NONE TODAY.** Without it the flip emits `body=skip` and stays vacuous. That is part of what the measured `+4` *is*.
- **`0036` costs 34.2-35.3 s** (⚠️ **NOT 37**), of which **30.014 s** is dead reference client-timeout ((l) 15.000 + (n) 15.013). **S1 pushes it to 49.5 s** until T13 lands; **(n) is out of scope and keeps its 15 s.** Controller ran it **3/3 PASS** at this tip with `INNER_EXIT=0` each.

### OUT OF SCOPE FOR ROW 82, DEFERRED BY NAME

The 9 `WasmResultUnimplemented` stubs at `internal/wasm/registration.go:873-882` (⚠️ **not `:877-882`** — the "9 stubs" sentence is at `:875-876`) · `proxy_on_queue_ready` / `proxy_on_grpc_*` · the wasm **network** filter · the 6 deferred cpp-host conformance families · **any trailers ASSERTION** · the **dead request/response trailer map types 1/3** · the `0036` cross-side restoration for scenarios (a)-(j) · **the driver-owned receiver port race (42 fixtures)** · **`stats-name-empty-segment-guards` (24 sites)** · the RBAC policy-name PROJECTION divergence · the lua `statNameRegexLiteral` (~40 net, 1 task) · the matcher-tree `Action`-name boot walk · **three** stale incumbent guard cites (`lua/compiled_config.go:272` x2, `redisproxy/config.go:51`) · the unbounded `io.ReadAll` at `http_call.go:302` · the wrong `plugin_context_id` at `:427` · cancel-at-destruction as a DEPARTURE documented as parity · the three wrong cpp-host cites in `http_call.go`'s header · **the stale `allCallbacksNoOp` comment at `root_abi_callbacks.go:43`** (the type occurs exactly once repo-wide — in that comment) · **the wrong `doc.go:219` ABICallbacks count** · the 25.1/25.2 records carrying the swapped enum (append-only; record, do not fix).

### ⚠️ THE AUTHORITATIVE CANDIDATE POOL IS THE SENTINEL'S OWN CHECK (2), NOT THIS FILE'S PROSE

**The project's actual un-chartered backlog is the five deferred-candidate sentences check (2) prints.** ⚠️ **Re-read them at YOUR tip.** Anchors `:192` (HTTP/3, **7** items) · `:202` (xDS, **9** — ⚠️ a naive ` + ` split gives 10; `upstream SDS (server-cert + validation_context)` carries an internal `+`) · `:212` (Observability, **3 top-level / 5 leaf** — ⚠️ **see the six-occurrence warning above**) · `:218` (Runtime, **8**) · `:226` (Operational tooling, **3**, SHORT form).

**The strongest measured candidate for a LATER row:** the **`ssl.connection_error` bucket** — **~230 net `.go` LOWER BOUND (realistic 575-700), 9-12 tasks, ZERO new fixtures** (extends `0110`). `connection_error` is **charset-legal bare AND assembled**, so it does **not** inherit the dynamic-`ssl` naming blocker. ⚠️ **One open risk: `0110` is `tcp_proxy`, and whether a record-layer failure there still books `connection_error` is UNPROVEN.**

⚠️ **THE DYNAMIC `ssl` FAMILY REMAINS BLOCKED.** `IsValidName("ECDHE-RSA-AES128-GCM-SHA256")` = **false** bare *and* assembled ⇒ **impossible, not possible-but-strict.** ⚠️ **The sentence's "FOUR-family dynamic surface blocked on NAMING" conflates three dispositions:** only `ciphers` is charset-blocked; `curves` and TLS-1.3 `ciphers` pass; `versions` passes charset but injects a spurious dot-**segment**. ⚠️ **`sigalgs` was NEVER observed emitted.**

⚠️ **`gRPC` IS THE LAST FAMILY CHECK (3) REPORTS AS NEVER OPENED, AND IT IS HARD-BLOCKED.** Zero implementation exists for any of the seven gRPC filter type-URLs. Both charter-satisfying candidates sit behind the **dead trailers seam** (16-22+ tasks, ADR-0045 split near-certain). ⚠️ **The cheapest gRPC filter, `grpc_stats` (~250-300 net, 12-14 tasks), is OFF-CHARTER and ALSO partially behind that seam.**

### What matters for the NEXT session, in order

1. **⚠️ RE-RUN ALL THREE SENTINEL CHECKS YOURSELF AND RECORD THE ACTUAL OUTPUT; DO NOT PREDICT IT.** Check (1) is LIVE and **YOUR STAGE SILENCES IT** — the doctored-copy NC is MANDATORY, not optional. ⚠️ **Your IMPL writes row 82's `status` cell: the LEAK CHECK IS LIVE. Leave `want` at 114 and do NOT disturb field 7's `WASM-family row`.**

2. **⚠️ EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION, NOT BY REVIEW.** The lineage is unbroken: phase-81 BRAINSTORM 14, SPEC 22, PLAN 14, IMPL 17; **phase-82 BRAINSTORM 26, SPEC 23, PLAN 14 (SIX load-bearing).** **Budget for finding more. An IMPL that refutes nothing has not looked.** ⚠️ **And an AGENT'S refutation is a claim too** — at the PLAN one agent figure did not survive controller re-derivation (a guest-crate denominator of **25**; it is **35**), and one agent measurement **could not be re-derived through the available channel and was LABELLED as such rather than laundered.**

3. **⚠️ BROKEN-GATE COUNT IS TWENTY-THREE — TWO NEW SHAPES LANDED AT THE PHASE-82 PLAN.**
   **The twenty-second: A REPLACEMENT GATE THAT INHERITS THE DEFECT IT REPLACES.** SPEC §8.1 replaced a disjunction that was invariant under the row's change with a positive leg that is **green on a trap**. **Fixing a gate is not the same as fixing the gate's blindness — re-derive the NEW gate's failure modes, not just the old one's.**
   **The twenty-third: A GATE THAT FIRES ON ALMOST EVERY ROW READS AS THOROUGH AND IS WORTHLESS.** The controller's own first ARM-A flagged **113 of 114** rows on a wrong threshold. **Only the printed denominator exposed it.**
   Priors that fired LIVE at this stage: **a landed CODE COMMENT is not evidence — THREE TIMES** (`doc.go:219`'s wrong ABICallbacks count; `root_abi_callbacks.go:43`'s nonexistent `allCallbacksNoOp`; `ContinueStream`'s doc naming only the data-phase resume) · **a hand-written golden shares its author's mistake** · **a drift CORRECTION is itself a claim** (SPEC §2.12's 119->118 overshot to **117**; §2.23 "settled" the `:304`/`:306` dispute the WRONG way and "corrected" the RIGHT `headerMapForType` cite into the wrong one) · **`grep -c` counts LINES not occurrences** (107 vs 117) · **an empty output is not a zero result** (an agent read `envoy_go.failures = 0` and nearly concluded "no trap") · **a probe must DISCRIMINATE** · **vacuous break modes** · **`Shell cwd was reset`** fired repeatedly.
   The twenty-one carried forward, unchanged: a guard is a claim at TRANSCRIPTION time · two defects that CANCEL in the gate metric · an inert gate cell · a full-suite recipe without `-v` is VACUOUS · a sha256 roster desynced against a DELETED file · `gofmt -l` NEVER exits non-zero · `go doc -all <A> <B>` swallows arg2 · a `+0 exported symbols` gate over an EMPTY package reds on a CORRECT tree · a RANGE gate cannot detect anchor drift · a roster's naive `[ -f ] || continue` exits 0 on a DELETED file · a count-only stat guard PASSES a build with BOTH names wrong · a `-run` no-match exits 0 with `[no tests to run]` · a `--- PASS` tally over a package with sibling tests exceeds the fixture denominator · a stat-delta claim cannot be discharged by guards scoped to another package · a stderr-VOLUME assertion passes on the hang · `golangci-lint` misspell locale US · a harness's exit code is not the command's · a GOLDEN ROSTER that omits the family under test · a NEGATIVE CONTROL POINTED AT A TARGET THAT DOES NOT EXIST · a gate metric INVARIANT under the change it guards · a hand-written ENUM golden pinning WRONG wire values.

4. **⚠️ BEFORE ANY LINE-ADDING EDIT TO A HEAVILY-CITED DOC, ENUMERATE THE CITES YOUR INSERT WOULD SHIFT.** The phase-82 SPEC's `DECISIONS.md` append shifted ZERO — PROVEN by `head -17796 … | cmp -`, not assumed. Copy that habit. ⚠️ **`ROADMAP.md:<line>` cites total 117** (**NOT the banked 118, and NOT the earlier 119** — three arms agree on 117 across **46** files, with a firing NC), of which ~40 shift on any row insert and ~35 are structurally unrepairable.

5. **⚠️ LINE ANCHORS DRIFT; PHRASE AND SYMBOL ANCHORS DID NOT. SEVEN SPEC CITES ARE WRONG AND ARE CORRECTED HERE:** `http_call.go:406` -> **`:405`** (`len(resp.Header)`; `:406` is `bodySize` — **this was §2.6's own load-bearing cite**) · `types_test.go:144-147` -> **`:146-149`** · §6.3's anti-pattern PHRASE `:304` -> **`:306`** (`:304` is the HEADING) · `abi_callbacks.go:178-190` -> **`:178-191`** · `encode_headers.go:115-117` -> **`:116-118`** · `registration.go:877-882` -> **`:873-882`** · `25.1 SPEC:481-483` -> **`:481-484`**. ⚠️ **`headerMapForType` is a METHOD with an `*abiCallbacks` receiver — grep the SYMBOL.** ⚠️ **The `chain.go:666/670/672` "stale by +1" framing is WRONG** — the two banked sets index two DIFFERENT constructs (if-guards vs OnDestroy calls), not one shifted pair. **RE-VERIFY EVERY `.go` AND `.md` LINE CITE BEFORE RELYING ON IT.**

6. **⚠️ THE GATES, WITH MEASURED BASELINES.** Full 120-fixture differential **120/120 in 402 s** · `0036` alone **34.2-35.3 s** · h2spec **106 passed / 0 failed / 0 skipped** (⚠️ **the lineage's "53/53" is ONE HALF — state your own denominator**) · `go test ./cmd/envoy-go/` **8.7 s** · touched packages **~1 s**. `-race` is a SECOND run. ⚠️ **CI runs the differential at `-timeout 20m`, job `timeout-minutes: 30`** (`.github/workflows/ci.yml:60,63`).
    ```sh
    ( go test ./test/differential/ -count=1 -v > "$SCRATCH/full.log" 2>&1; echo "INNER_EXIT=$?" )
    grep -cE '^    --- PASS: TestDifferential/' "$SCRATCH/full.log"          # want 120
    grep -E  '^    --- (FAIL|SKIP): TestDifferential/' "$SCRATCH/full.log"   # want EMPTY
    grep -c  'no driver registered for fixture' "$SCRATCH/full.log"          # want 0
    grep -cE '^panic:|DATA RACE|SIGSEGV' "$SCRATCH/full.log"                 # want 0
    grep -o  'TestDifferential/[^ ]*' "$SCRATCH/full.log" | sed 's|TestDifferential/||' | sort -u \
      | comm -3 - <(ls -1 test/fixtures/ | grep -E '^[0-9]{4}[a-z]?-' | sort)  # want EMPTY
    ```
    ⚠️ The faithful dir predicate is `^[0-9]{4}[a-z]?-` — a bare `^[0-9]{4}-` gives **118**.
    ⚠️ **`go test ./test/differential/...` (with `...`) matches TWO packages and BUFFERS `-v` output.** A run that looks hung with an empty log is normal — do not kill it.
    ⚠️ **THE RUN ABORTS, IT DOES NOT MERELY FAIL. CAPTURE `INNER_EXIT` AND THE PANIC ARM.**
    ⚠️ **The differential log does NOT carry the subject's stderr** — measured at the PLAN (29-line logs, zero `unreachable`/`CallProxyOn` matches). **If you need guest-trap evidence, capture it another way.**

7. **⚠️ THE SIX-GATE HAS A STANDING LINEAGE DEPARTURE — NAME IT, DO NOT CLAIM COMPLIANCE.** Gate **(f)** expects a `REVIEW.md`; **none since 25.3 — 86 of 123 phase dirs carry none** (37 carry one). Gate **(d)** is **VACUOUS for a row that adds none** — say "vacuous", not "green" (**55** fuzzers; ⚠️ **scoped to `-- '*.go'` — unrestricted gives 161, of which 106 are Markdown code blocks**). Gate **(c)**: proxy-wasm is INHERITED unless you run it — ⚠️ **and when you do, STATE THE DENOMINATOR: 10 of the cpp-host's 16 files (62.5%), 6 deferred.** ⚠️ **YOUR IMPL OWES (a)-(f) FOR REAL** — it is the first phase-82 stage that commits `.go`. Normative cites, RE-VERIFIED at the PLAN: **§6.1 `:285`**, `:289` ~25-task, `:290` ~1500-LoC, **`:291` BLANK**, `:292` third trigger; **§6.2 `:294`**; **§6.3 `:304` is the HEADING and `:306` is the PHRASE**; **§7.5 `:357`, gates `:360-365`, close `:367`**. ⚠️ **`/BOOTSTRAP_PROMPT.md` WITH A LEADING SLASH RESOLVES NOWHERE** — the file is `<repo>/BOOTSTRAP_PROMPT.md` (**522** lines). ⚠️ **`docs/superpowers/plans/` CONTAINS NO FILE OF THAT NAME** — the 1024-line copy is `2026-04-21-envoy-go-bootstrap-prompt.md`, and its offsets are **NOT a constant shift** (§6.1 Δ+197, §7.5 **Δ+228**). **Open the repo-root file.**

8. **⚠️ ADR + DOCUMENT CONVENTIONS.** **The DECISIONS tail is `ADR-0304`, STATUS `PROPOSED` @`:17800` (enclosing heading `:17798`). Next-free is `ADR-0305`.** **303** `^## ADR-` headings, ids **0001-0304**, ⚠️ **exactly one gap at ADR-0209** — verified with firing NCs on both neighbours **plus** an independent full-range diff; ⚠️ **the 303-vs-0304 arithmetic is CONSISTENT *because of* the gap, so "next-free = headings + 1" gives ADR-0304 and COLLIDES. DO NOT "FIX" IT.** STATUS census **17** (matcher `^> \*\*STATUS: `, a BLOCKQUOTE; ⚠️ `^\*\*STATUS:\*\*` matches NOTHING and reads as a false zero). Retained italic footers **10** (⚠️ **ADR-0303's own "EIGHT" is a DIFFERENT DENOMINATOR (0293-0302), NOT an error**, and SPEC §11's *"state 9, or say 8 predecessors"* is **stale — it is 10**). ⚠️ **`^---$` is 216 and must stay 216.** ⚠️ **CARRY NO WHOLE-FILE GREP COUNT IN AN ADR** — that species self-falsified in ADR-0296 ¶3 and ADR-0302 ¶11. ⚠️ **ADR-0044 does NOT contain the §Context-at-SPEC discipline**; write **ADR-0044-as-used**. ⚠️ **THE RECURRENCE GUARD IS ARMED AND YOUR IMPL MUST DISARM IT** — `^> \*\*STATUS: PROPOSED` fires **exactly once** (ADR-0304) and **the IMPL flips it to COMPLETE**. ⚠️ **The LOOSE form is a FALSE-POSITIVE GATE — re-demonstrated live at the PLAN: FIVE hits, FOUR of them COMPLETE blocks (ADR-0299/0300/0301/0302).** ⚠️ **ADR-0304 §Consequences MUST carry the ADR-0071 single-goroutine invariant break.**

9. **⚠️ A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX — the TWENTY-SEVENTH consecutive stage at which this held.** The index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. **Still live:** `reference_sds_init_fetch_timeout_dial_budget_flake` (**TWO packages**) · the pre-existing `internal/cluster` `-race` outlier (`TestOutlierDetector_ConcurrentEjectExactlyOnce`) · `internal/httpclient TestOptions_ZeroValue_NoOpDefaults`. ⚠️ **TWO former flakes were FIXED at `f46ba419`/`f2dd994a` and a recurrence of either is now a FINDING.** ⚠️ **`0061-lb-ring-hash` is NOT a live flake.** ⚠️ **The driver-owned receiver port race did NOT fire in any of the 11 `0036` runs at this tip** (8 by an agent, 3 by the controller) — **that is not evidence it is fixed.** **Classify, isolate-re-run with a POSITIVE selector assertion, and state the evidence.**

10. **⚠️ CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE — and NEVER tear down a container this session did not create**, by name or otherwise. The phase-82 PLAN created two (`p82p-a2-ref-old`, `p82p-a2-ref-new`) plus a network (`p82p-a2-net`) and removed **exactly those, BY NAME**; no image or ancestor filter was used.
    ⚠️ **A MACHINE-STATE CHANGE WAS DELIBERATELY LEFT IN PLACE: `rustup toolchain 1.94.0-x86_64-unknown-linux-gnu` + its `wasm32-wasip1` target were INSTALLED at the PLAN and NOT removed** — it is the only toolchain that reproduces the vendored blobs, and **T13 needs it**. `rustup default` is unchanged (`stable`, 1.96.0).

11. **Counts at this tip (re-run MECHANICALLY in your worktree; never copy them).** ⚠️ Use GNU `command grep` or `git grep` repo-wide — but ⚠️ **NOT `command grep` inside `xargs`.** **All re-derived at the phase-82 PLAN:**
    fixtures **120** (next-free **0119**; naive `^[0-9]{4}-` gives **118**, the delta being `0007a-cors` + `0007b-iteration-probe`) / fuzzers **55** (⚠️ **`-- '*.go'`-scoped; unrestricted 161**) / internal packages **73** / phase dirs **123**, of which **37** carry `REVIEW.md` and **86** do not / **BackendKind tail 38** over **39** declarations (do NOT "fix" 38 to 39) / **guest crates 35** (10 conformance + 7 `0034` + 14 `0036` + 4 `0038`), **36 blobs** (`0039` carries a crate-less `probe.wasm`) / **`DECISIONS.md` 17824**, **303** headings, tail **ADR-0304 PROPOSED**, next-free **ADR-0305**, retained footers **10**, `^---$` **216**, STATUS census **17** / **`ROADMAP.md` 230 lines / 114 data rows** (⚠️ a numeric-id regex gives **112**, silently dropping `28.1a`/`28.1b`) / **`BEHAVIOR_CONTRACT.md` 5870** / **`STATE.md` 63** / **`STATE_HISTORY.md` 444, tolerant anchor 172** (naive **161**) / ⚠️ **`ROADMAP.md:<line>` cites 117** (**NOT 118**) / **stat registration: 208 CODE sites**, ⚠️ **35 code-bearing production files of 36 matched** / **stat surface 1207** ⚠️ **DOCUMENTARY and CONFIG-CONDITIONAL — assert the DELTA**.
    ⚠️ **CONTESTED, SO NO NUMBER IS CARRIED:** the next-free REFERENCE port (routers say `10119`; `STATE.md` §Project says `10450`) · the `STATE_HISTORY.md` archive-gap total · production `stats.IsValidName` GUARD SITES **25** vs a naive line-grep's **56**.
    ⚠️ **DO NOT SOURCE ANY COUNT FROM `STATE.md` §Project.** It is **SEVEN PHASES STALE** (self-declared *"at this phase-76 IMPL close"* while the line above presents it as *"(Live…)"*) and wrong on at least three axes. **Anchor on §Current. Do NOT "fix" §Project.**

12. **⚠️ DOCUMENTARY DEFECTS — carried forward, still deliberately NOT fixed.**
    - ⚠️ **The header-map enum is WRONG in three landed places** — `internal/wasm/abi/types.go` (declarations), `internal/wasm/abi/types_test.go:146-149` (**a hand-written golden pinning the wrong values**), and the append-only phase-25.1 SPEC record at `:481-484`. **Row 82 fixes the first two (T1); the third is RECORDED, not fixed.** ⚠️ **The blast radius is LARGER than the SPEC's list**: `abi_callbacks_test.go` has **5** mentions (`:244 :245 :246 :247 :333`), not 3, and `25.1 PROGRESS.md` (8), `25.2 PROGRESS.md`, `DECISIONS.md:17812`, `ROADMAP.md` row 82 and this file all carry the values.
    - ⚠️ **`internal/wasm/doc.go:219` says "ABICallbacks 13->20 methods"** — the interface has **21** today.
    - ⚠️ **`internal/filter/http/wasm/root_abi_callbacks.go:43` names a type `allCallbacksNoOp` that DOES NOT EXIST** — it occurs exactly once repo-wide, in that comment (NC: `rootABICallbacks` ⇒ 6 files).
    - ⚠️ **All three cpp-host cites in `http_call.go`'s header comment are WRONG for v1.37.2** — `:1547` is `resetStream()` (real `:881-884`), `:1693` is `buffering_request_body_ = false` (real `:1834-1836`/`:1856-1858`), `:1900-1905` is `onGrpcCloseWrapper` reentrancy (real `:1328-1334`).
    - ⚠️ **cancel-at-destruction is a DEPARTURE documented as PARITY**; **envoy-go passes the wrong `plugin_context_id`** (`http_call.go:427`); **there is NO cap on the http-call response body** (`:302`, unbounded `io.ReadAll`; upstream bounds it). All pre-existing.
    - ⚠️ **`scripts/README.md:24-25` says `rustc 1.94.0` "(or newer compatible)" — MEASURABLY FALSE IN BOTH DIRECTIONS**, and `scripts/.gitignore:7` excludes `Cargo.lock` with a rationale that DEFEATS the reproducibility it claims. **T13 fixes both.**
    - ⚠️ **`internal/filter/http/wasm/trailers.go` is internally contradictory in three tenses** and its capture is a literal no-op. **The type-1/3 trailer map surface is DEAD.**
    - ⚠️ **`0036/inputs/driver.go:504-509` states scenario (l)'s divergence BACKWARDS** — and ⚠️ **`:313-327` IN THE SAME FILE is CORRECT.** **Row 82 corrects the former (T14).** ⚠️ **`README.md:71` and `expectations.yaml:105` are inaccurate DESCRIPTIONS of accurate QUOTATIONS** and are wrong today in both directions.
    - ⚠️ **`ROADMAP.md` row 82's summary carries TWO wrong-on-arrival claims** — `abi_callbacks.go:186-192` (the method is `:178-191`) and *"handles only map types 0 and 1"* (it is **0 and 2**). **INVARIANT-BLOCKED** by §Schema `:18` ⇒ **recorded, not fixed**, on the rows-57/69/78 precedent.
    - ⚠️ **`BEHAVIOR_CONTRACT.md:1967`** asserts a blocker its own successor phase KILLED (`ssl.connection_error`).
    - ⚠️ **`DECISIONS.md` ADR-0301 §Decision (b) says *"has failed twelve top-level detectors"*** — PRESENT TENSE inside immutable ADR text, factually wrong since row 80 landed. Alongside `ROADMAP.md`'s five `twelve` occurrences (⚠️ **four are pure FALSE POSITIVES**), and `0110`'s driver comment (**accurate**, but a FALSE POSITIVE for any `\btwelve\b` sweep).
    - **`ROADMAP.md` malformation: three rows lose summary content in GFM render** (lines 119, 131, 140), INVARIANT-BLOCKED => **defensible as a GUARD with the DISJUNCTION gate, not a fix**. ⚠️ **There is NO doc-invariant test anywhere in this repo.** The reusable precedent is `test/differential/harness_test.go:123-137`'s `runtime.Caller(0)` -> repoRoot idiom.
    - **`STATE.md` §Project counts self-contradicts §Current and is FROZEN at the phase-76 IMPL close.** **Anchor on §Current. Do NOT "fix" §Project.**
    - **`BEHAVIOR_CONTRACT.md`'s `wasm.` arm is called a *"NEW SN9 flattening rule"*, which COLLIDES with ADR-0118's actual SN9.** Pre-existing.
    - ⚠️ **The two `//nolint:gosec` directives row 80 added are INERT** — `gosec` is not among the 9 linters `.golangci.yml` enables (`disable-all: true`; `govet errcheck staticcheck unused ineffassign gofmt goimports misspell revive`; golangci-lint **v1.64.8**). ⚠️ **`nolintlint` is NOT enabled either**, so `0036`'s five stale `//nolint:unused` directives will not fail lint.
    - **`golangci-lint-action@v6.5.2` emits a Node-20 deprecation warning in CI.** Deliberately NOT bumped: v7+ requires golangci-lint **v2** while the repo pins **v1.64.8**. **A warning, not a failure.**
    - ⚠️ **`ROADMAP.md` §Schema `:18` says *"Sub-phases get their own rows"* while practice puts split legs in the parent's `sub-phases` PROSE.** Controller-verified at the PLAN: **ZERO dotted rows exist above 32.2**, while rows 36/39/40/42/43/60 all carry prose legs. **Record the tension; do not relitigate.** **The phase-82 PLAN did NOT split, so this is inert for row 82.**
    - **Production-DEAD trailer code** in four filters · **two stale non-test comments** at `internal/filter/hcm/connection.go:565` and `h2dispatch.go:503` claiming `RunDecodeTrailers` does not exist (it does, at `internal/filter/http/chain.go:455` — **ZERO non-test callers, but 14 non-test MENTIONS across 5 files**) · **`abi_callbacks.go:594-596`** calls `ContinueStream` an Unimplemented stub when `:766` is **live** · **`DECISIONS.md`'s wasm module-function roster** enumerates 24 keys, **8 of which do not exist in `internal/`** · **one defective `freeTCPPort`** (`test/conformance/h2spec/h2spec_test.go:219`), which **cannot** abort the differential.
    - ⚠️ **`internal/filter/network/rbac/rbac.go:50` contains the token `F2`** as an unrelated **phase-26 fork label**. **Do not use `F2` as a grep anchor in that package.**

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — take it from `git rev-parse master`, **NOT from a SHA quoted in this file**. The phase-82 PLAN squash is not named here on purpose: **router-only commits routinely land above it**. ⚠️ **AND THE BARE SUBJECT-LOCATE GREP IS CONTAMINATED BY THIS FILE'S OWN PROSE ABOUT IT** — `git log` greps the whole commit message and router commits discuss the phase in their BODIES, so **the sentence warning about the grep satisfies the grep.** ⚠️ **USE THE SLUG-ANCHORED FORM**, which is stage-exact:
```sh
git log --grep '^phase 82 (wasm-http-call-response-cache)' --format='%h %s'   # 3 at this tip: BRAINSTORM, SPEC, PLAN
```
Measured with both controls: an invented slug returns **0**, and the completed phase-81 form returns **4**. **Take the NEWEST line (`| head -1`) and verify the count before relying on it.** Subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** (`reference_parallel_subagents_private_scratch`) and they **must tear down docker containers BY NAME, never by an `ancestor`/image filter** (`reference_parallel_agents_shared_machine_namespaces`). ⚠️ **THERE ARE TWO RESERVED PORT BANDS — band private ports outside BOTH `20000-31007` (subject blocks) AND `11000-14999` (backends), and also clear of the static fixture ports `10000-10447` / `15000-15011` / `18001-18007`.** The `42000-42999` band has been used safely (phases 80, 81, and all three phase-82 stages). ⚠️ **If two agents share ONE worktree they must stage by EXPLICIT PATH** and **must NOT run an unscoped `git restore`**. ⚠️ **PREFER ONE WORKTREE PER AGENT OFF A COMMON BASE** — that is what the phase-82 PLAN did for five investigation agents, all of which returned `git status --porcelain` = 0. Per-task `gofmt` + `golangci-lint` on touched packages (`feedback_pertask_gofmt_lint`). ⚠️ **`gofmt -l` NEVER exits non-zero — gate on OUTPUT.** ⚠️ **`golangci-lint` runs `misspell` with `locale: US`.** ⚠️ **AN EMPTY LINT RESULT IS NOT EVIDENCE THE LINTER LOOKED — inject a British spelling, confirm it fires, restore byte-identically.** ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch, and note `-o` precedes the package path.**

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. ⚠️ **ASSUME IT FIRES EVERY SESSION — it has been treated as live for THIRTY-ONE consecutive sessions and it FIRED, OBSERVED, at the phase-82 PLAN** (`Shell cwd was reset to /home/esa/git/envoy-go`). **Use `git -C <abs-worktree-path>` for every git command, prefer absolute paths over `cd`, put any needed `cd` FIRST in a compound command, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN.** At the phase-82 PLAN five agents wrote throwaway `.go` probes and full prototypes, ran them, reverted them, and confirmed `git status --porcelain` = 0 lines. A stage that ships a stray `.go` file has silently changed its own gate obligations.

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline — ⚠️ **anchor greps on `^- **<field>`**; all seven fields verified singleton at the phase-82 PLAN close). ⚠️ **§Project counts SELF-CONTRADICTS §Current and is SEVEN PHASES STALE** — do not anchor on it and do not fix it.
2. `docs/envoy-go/phases/82-wasm-http-call-response-cache/PLAN.md` — **the whole document.** §1 the fourteen-item re-derivation ledger (§1.1 S1's re-pricing, §1.2 the leak, §1.3 the green-on-trap gate, §1.4 the mutator surface, §1.5 the accessor, §1.7 the blob pins, §1.12 the cap-denied test population); §2 the global constraints; §3 the edit surface; **Tasks 1-16 IN ORDER**; §4 the band and **why it does NOT split**; §8 the gate hygiene and measured baselines.
3. `docs/envoy-go/phases/82-wasm-http-call-response-cache/SPEC.md` — **§1-§8 for the disposed D-questions and the trap synthesis.** ⚠️ **§12's cost table, §2.6's `:406` cite, §2.12's cite count, §2.23's two "settled" disputes, §8.1's replacement gate and §8.2's nolint claim are ALL CORRECTED BY `PLAN.md` §1 — read the PLAN's corrections alongside.**
4. `docs/envoy-go/phases/82-wasm-http-call-response-cache/BRAINSTORM.md` — **§2 (the rejected alternatives) only.** ⚠️ **Its §1 and §4 central claims were refuted at the SPEC — do not re-inherit them.**
5. `docs/envoy-go/ROADMAP.md` — **§Schema first** (`:18` carries BOTH the update-in-place invariant AND *"Sub-phases get their own rows"* on ONE line), then row 82 (line 144), then the five deferred-candidate paragraphs. **You flip `status` in place; `want` STAYS 114.**
6. `BOOTSTRAP_PROMPT.md` — **at the REPO ROOT, no leading slash.** §6.1 `:285`, §6.2 `:294`, §6.3 `:304` heading / **`:306` phrase**, §7.5 `:357-367`. ⚠️ **Not the 1024-line copy under `docs/superpowers/plans/`.**
7. `docs/envoy-go/DECISIONS.md` — **ADR-0304** (this row's own, STATUS `PROPOSED`, **you complete it**) · **ADR-0303** (the immediately-prior completion, for the append shape) · **ADR-0288** (roll-IN-PLACE and the archive rule) · **ADR-0044** as-used · **ADR-0071** (⚠️ **its single-goroutine-per-stream invariant is BROKEN by S1 — §Consequences must say so**) · **ADR-0106** (the sole-leg property).
8. `docs/envoy-go/phases/81-stats-name-charset-guards/PROGRESS.md` — **the `# IMPL record`**, for calibration: a **3.07x** cost overrun over a measured-basis estimate, recorded as a FINDING and NOT retro-split.
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — especially **`reference_code_comment_not_evidence`** (⚠️ **fired THREE times at the PLAN**), **`reference_handwritten_golden_shares_author_mistake`**, **`reference_compensating_defects_cancel_in_the_gate_metric`**, **`reference_measured_prototype_is_a_lower_bound`**, **`reference_a_drift_correction_is_itself_a_claim`**, **`reference_grep_c_zero_is_a_broken_gate`**, **`reference_vacuous_break_modes`**, **`reference_deliberate_break_wrong_assertion`**, **`reference_break_arm_injection_site_is_a_claim`**, **`reference_liveness_break_needs_failing_baseline`**, **`reference_empty_output_is_not_a_zero_result`**, **`reference_probe_must_discriminate`**, **`reference_gate_command_negative_control`**, **`reference_lifted_reject_hidden_enforcement`** (⚠️ **T1's atomicity**), **`reference_sample_is_not_an_audit`**, **`reference_stale_cite_recurs_fix_by_pattern`**, **`reference_recursive_grep_blind_to_gitignored_tracked_file`**, **`reference_sentinel_matcher_string_self_clears`**, **`feedback_brief_citations_not_evidence`**, `feedback_git_worktrees`, `feedback_execution_style`, and **`reference_bash_cwd_reset_commits_to_main`**.
