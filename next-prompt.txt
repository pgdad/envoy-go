=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: does NOT apply. Phase 53 (`load-balancer-priority`) is actively `in-progress` (lifecycle-state 1 → 2, SPEC done) — there is a clear next action (its PLAN). Do not check the cross-family audit again until phase 53 itself reaches phase-done with no further chartered work.

**STATUS AS OF 2026-07-07: phase 53's SPEC landed, executing ALL SIX BRAINSTORM D-P1..D-P6 empirical pins in-session via two parallel live-probe agents against `envoyproxy/envoy:contrib-v1.37.2`. The single most consequential finding REFUTED the BRAINSTORM's own D-P1 panic hypothesis outright.**

- The phase-53 SPEC landed (docs-only, worktree `.worktrees/phase-53-spec`, branch `phase-53-load-balancer-priority-spec`, squash-merged to master + pushed at commit `7a23a127`): `docs/envoy-go/phases/53-load-balancer-priority/SPEC.md`, the phase-52 14-section shape.
- **AMEND-P1 (the phase's single most consequential finding, D-P1 SPEC-BLOCKING):** BOTH BRAINSTORM candidate readings for the panic/per-tier-health interaction were REFUTED. The reference's real bypass condition is a CLUSTER-WIDE CAPACITY-SHORTFALL SUM — `Σ_i min(100, overprovisioning_factor × healthy_fraction_i) < 100` — decisively distinguished from the classic `healthy_panic_threshold` (a 3-tier control at 46.7% raw aggregate healthy fraction, under the classic 50% threshold, showed NO bypass because the capacity sum, 156, comfortably exceeded 100) and from an independent per-tier "local panic" concept (a tier at 20% healthy correctly restricted its own share to its 1 healthy host — 92/300 — its 4 unhealthy hosts getting ZERO, decisive per-host evidence). When the sum drops below 100 (strict `<`; exactly 100 does NOT bypass), the reference abandons priority AND health entirely, spreading flat by RAW HOST COUNT across every host in every tier, reusing the EXISTING `lb_healthy_panic` stat. This necessitated a genuinely NEW implementation primitive not present in phase 52's `locality.go`: a `tierHealth` panic-disabled per-tier health view (constructor-time only, zero `Pick`-signature impact) — a documented IMPROVEMENT over `locality.go`'s own accepted-but-unfixed "child-local-panic" coverage boundary, made possible because this SPEC's own live probe supplies the concrete evidence phase 52 lacked at ITS OWN SPEC time. The flat bypass-fallback child is built with a NIL health registry (a deliberate departure from `localityWeightedLB`'s own flat child, which safely reuses shared health only because ITS bypass condition is identical to its leaf's internal check by construction — priorityLB's differently-shaped bypass condition cannot make that same guarantee).
- **AMEND-P4 (D-P4 SPEC-BLOCKING):** the per-tier capacity formula CONFIRMED, but the cascade's remainder-aggregation step REFUTED + CORRECTED for 3+ tiers — a decisive 3-tier probe (P0=40%/P1=60%/P2=100%) distinguished a "naive recursive-fraction" reading (predicting P2=7.04%) from the CORRECT "cap each tier's load by its own capacity against the remaining budget" reading (predicting P2=0%) — observed P2=0.0% confirms the corrected reading. The two readings coincide at exactly 2 tiers (why the BRAINSTORM's hypothesis looked right there) but diverge at 3+.
- **AMEND-P2 (D-P2 SPEC-BLOCKING):** both composition rejects (multi-tier `priority` + `locality_weighted_lb_config`; multi-tier `priority` + `lb_subset_config`) CONFIRMED as genuine envoy-go-strict departures — MORE SPECIFIC than anticipated: the reference doesn't merely accept the combinations, it MEANINGFULLY COMPOSES them (priority selects the tier first, then locality-weighting/subset-selection operates WITHIN it — live-confirmed via working 3-layer boot+traffic tests for both).
- **AMEND-P3/P5/P6:** the `priority` proto field is a plain `uint32` (no wrapper-type ambiguity, simpler than `overprovisioning_factor`'s own D-LW-OPF0 wrinkle, which `priorityLB` still reuses verbatim); `go mod tidy` clean (D-P3). Stat surface delta REFUTED DOWN to a confirmed ZERO — the family's FOURTH zero-stat-delta LB phase (D-P5). LoC/task envelope CONFIRMED ~230-330 LoC / ~11-12 tasks, no split (D-P6).
- An independent `general-purpose` spec-document-reviewer pass (mirroring the phase-52 precedent) caught and the controller fixed BEFORE landing: a wrong `extractEndpoints` file:line citation (`manager.go:814-841`, not `:771-795`); a dropped `hasOPF` absent-vs-explicit-zero threading in the `newPriorityLB` sketch (now mirrors phase 52's D-LW-OPF0 exactly, reused verbatim); a `leafFactory` origin misattribution (defined in `subset.go:131`, merely reused by `locality.go`).
- 1 ADR anticipated: ADR-0270 (single-ADR reuse-shape phase, confirmed correct — zero `Pick`-signature change, only constructor-time wiring). §Context drafted at SPEC §13; §Decision/§Consequences land at IMPL.
- Anticipated at IMPL: fixtures 97 → 98 (`0096-lb-priority`); stat surface 1200 (+0, CONFIRMED); fuzzers 52 (+0); BackendKind tail 38 (+0); DECISIONS ADR-0269 → ADR-0270 (next-free ADR-0271). ZERO new packages, ZERO new go.mod deps.
- STATE.md/ROADMAP.md updated (row 53 STAYS `in-progress`, lifecycle-state 1 → 2; the active-phase entry rewritten to lead with SPEC-done, nesting the prior BRAINSTORM-done detail). This was a docs-only commit (no production code touched).

=> The NEXT action is **the phase-53 PLAN** (`superpowers:writing-plans` — decompose SPEC §10 into a bite-sized TDD task spine, the ring_hash-36/maglev-37/subset-38/locality-weighted-52 precedent). Decompose the SPEC's ~11-12 indicative tasks (§10) into concrete TDD steps; do a FINAL ADR-0045 split-gate re-check (anticipated to re-confirm §3.0's no-split disposition); resolve the SPEC §12 PLAN/IMPL D-questions (D-P-DUP, D-P-FACTORY, D-P-RETROFIT) with concrete code-level decisions; enumerate EVERY `Endpoint{}` construction site that needs the new `Priority` field (the phase-38/52 `reference_conn_wrap_method_no_promote`-generalized lesson, applied a THIRD time).

==============================================

## Read first (in order)
1. THIS FILE — the cold-start router (phase 53 chartered, SPEC done, PLAN is next).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 53 SPEC done`) + the full history, including the AMEND-P1..P6 findings.
3. `docs/envoy-go/ROADMAP.md` — confirm row 53 `in-progress` (lifecycle-state 1 → 2); the Load-balancing family section for full context (phases 34 through 53).
4. `docs/envoy-go/phases/53-load-balancer-priority/SPEC.md` — the phase to advance; §3.1's `priorityLB`/`tierHealth`/`cascadeLoads` code sketch is the PLAN's primary input; §10's per-task table is the PLAN's starting decomposition; §12 lists the PLAN/IMPL-level D-questions still open (D-P-DUP, D-P-FACTORY, D-P-RETROFIT).
5. `docs/envoy-go/phases/53-load-balancer-priority/BRAINSTORM.md` — background/rationale only; the SPEC supersedes its D-P1/D-P4 hypotheses (both were refuted/corrected — read the SPEC's AMEND blocks as authoritative, not the BRAINSTORM's original guesses).
6. `docs/envoy-go/phases/52-load-balancer-locality-weighted/PLAN.md` — the DIRECT structural precedent (the 10-task TDD spine `priorityLB`'s own PLAN reapplies one axis over, adjusted for the new `tierHealth`/`cascadeLoads` primitives and the corrected cascade).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — search for `reference_*`/`feedback_*` entries tagged for load-balancer/health/differential work (e.g. `reference_health_check_propagation_warmup`, `reference_differential_band_sigma_margin`, `reference_differential_break_protocol_count1`, `feedback_pertask_gofmt_lint`).

## Honor throughout (the discipline, unchanged for whichever stage comes next)
- **Lifecycle = one stage per session** (BRAINSTORM → SPEC → PLAN → IMPL). This session's job is ONLY the PLAN — do not proceed to IMPL in the same session.
- **Subagent-driven execution** (`feedback_execution_style`) in a **fresh worktree** (`feedback_git_worktrees`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit, re-runs the full suite on the frozen HEAD, does any deliberate-break verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). Per-task gates: `gofmt -l` + `golangci-lint` + `go vet` + `go build ./...` (`feedback_pertask_gofmt_lint`). Note: the PLAN stage itself is typically docs-only (like BRAINSTORM/SPEC) — subagent dispatch matters most at IMPL, but an independent plan-document-reviewer pass (the phase-52 precedent) is still worth running before landing.
- **Process anchors:** ADR-0106 (per-leg rows) · ADR-0044 (ADR §Decision+§Consequences land at IMPL) · ADR-0045 (sub-split soft gate) · next-free **ADR-0270**.
- Re-verify baselines fresh at cold-start — do NOT trust any count in this file without re-running the baseline commands (`ls -d test/fixtures/[0-9][0-9][0-9][0-9]* | wc -l`, `grep -rh '^func Fuzz' --include='*.go' . | wc -l`, etc.) against current HEAD.
- After the phase-53 PLAN lands, roll this file forward again to point at the phase-53 IMPL. The Load-balancing family's remaining deferred candidate (panic thresholds as an independent construct) and the other two open families' candidate lists (Observability — 5 candidates; Operational tooling — 3 candidates) stay parked, unaffected by this phase, until phase 53 itself reaches phase-done and a human is asked again.
