RESUME a fresh session for envoy-go (router last updated 2026-06-27; the **phase 46 (tracing) BRAINSTORM is DONE and LANDED** — a docs-only commit direct-on-master chartering the **THIRD Observability-family row** [`tracing` — HCM-native request tracing]. **ROW 46 (`tracing`) registers `in-progress`**; the Observability FAMILY STAYS OPEN (stats sinks / tap remain future rows). The BRAINSTORM (`docs/envoy-go/phases/46-tracing/BRAINSTORM.md`) was authored in the house 12-section shape (mirrors the 44/45 BRAINSTORMs) + spec-document-reviewer-APPROVED first-iteration [ALL load-bearing proto + code:line + count claims verified TRUE against the resolved `go-control-plane/envoy v1.32.4` + `go.opentelemetry.io/proto/otlp v1.0.0` modules and the as-built source; 1 advisory folded — an explicit span-end-time live-probe sub-prong into D-TRACE-SPAN]. **Q-decisions (live human dialogue):** (Q0) THIRD Observability row = tracing (over stats sinks / tap — pairs with the just-complete OTLP access-log sink + REUSES the `go.opentelemetry.io/proto/otlp` module already in the build graph: `trace/v1` + `collector/trace/v1` are siblings of the `logs/v1` packages 45.x consumed); (Q1) split = OTLP-first BY-EXPORTER 2-leg — **46.1** core (the NEW `internal/tracing` engine: the full-fidelity sampling/request-id decision + the span model + W3C `traceparent` propagation) + the OTLP gRPC exporter (UNARY `TraceService.Export`, a typed `OTLPTracesClient` over the phase-18 `grpcclient.Dialer` [ADR-0158] + the 45.x unary-Export batching-sink pattern) + the differential (ADR-0260); **46.2** the Zipkin JSON exporter (HTTP POST) + B3 propagation (ADR-0261) — with a **46.1a/46.1b sub-split contingency** under the ADR-0045 gate (the full-fidelity core is large: 46.1a = the sampling/request-id engine, 46.1b = span + propagation + OTLP exporter); (Q2) sampling depth = FULL fidelity (all three Percent knobs `client_sampling`/`random_sampling`/`overall_sampling` + the `x-client-trace-id` force + the `request_id_extension` UUID byte-14 sampling-decision encode + the incoming `traceparent` sampled-bit + the full decision precedence). HEAD is the **BRAINSTORM landing commit** (the `46-tracing/BRAINSTORM.md` + the ROADMAP row-46 + the STATE active-phase + this `next-prompt.txt` roll, ONE docs-only commit direct-on-master, pushed to origin). Re-read the shas with `git log --oneline -10`, do NOT assume — further docs-only `next-prompt.txt:` roll commits may stack ATOP this one. Counts at the BRAINSTORM (the exit invariant — re-verify, do NOT assume; UNCHANGED, docs-only): stat surface **1191** (H2 cluster; non-H2 **1187**) / fixtures **87** (`ls -d test/fixtures/[0-9][0-9][0-9][0-9]* | wc -l` == 87; tail `0085-otlp-access-log-operators`) / fuzzers **46** (`grep -rh '^func Fuzz' --include='*.go' . | wc -l` == 46) / BackendKind tail **38** / DECISIONS tail **ADR-0259**, next-free **ADR-0260**.

**ROW 44 (`grpc-access-log`) `done`**; **ROW 45 (`otlp-access-log`) `done`**; **ROW 46 (`tracing`) `in-progress`** (chartered at this BRAINSTORM; flips `done` once BOTH legs land per ADR-0106 + `reference_roadmap_split_phase_row_done`). The **Observability FAMILY STAYS OPEN**. **The next action is the 46.1 (core+OTLP) SPEC.** ANCHORS **ADR-0260**.

First confirm state via `git log --oneline -10` (expect HEAD at the phase-46 BRAINSTORM landing commit atop the 45.2 IMPL squash `59990afe`; counts as above; re-read the shas, do NOT assume). Confirm `git status` clean + master == origin/master.

## NEXT STEP — the 46.1 (core+OTLP) tracing SPEC
This is a SPEC stage — docs-only, direct-on-master (the house 14-section shape mirroring `SPEC-45.1.md`/`SPEC-45.2.md`). The deliverable is `docs/envoy-go/phases/46-tracing/SPEC-46.1.md` chartering the CORE+OTLP leg, ANCHORING the **ADR-0260 §Context draft** (PROPOSED; the §Decision/§Consequences body lands at the 46.1 IMPL per ADR-0044). **Execute the §10 D-TRACE-* live probes IN-SESSION** against `envoyproxy/envoy:contrib-v1.37.2` (ADR-0227) per `reference_docker_probe_bridge_network` (a shared bridge; a span-receiver hostname reachable from BOTH containers; verify the receiver actually received ≥1 span from the reference — decode-ran proof). The BRAINSTORM §10 obligations to pin: D-TRACE-SAMPLING (the three-Percent decision precedence + the incoming sampled-bit), D-TRACE-REQUESTID (the `request_id_extension` UUIDv4 + the byte-14 trace-decision nibble + the generate-vs-preserve trust rule), D-TRACE-PROPAGATION (the `traceparent` extract/inject + continue-vs-fresh trace-id), D-TRACE-SPAN (the OTLP `ResourceSpans/ScopeSpans/Span` nesting + span name/kind/attribute roster + the EXPLICIT span-end-time seam sub-prong [the folded advisory]), D-TRACE-OTLP-WIRE, D-TRACE-STATS (the HCM-scoped `tracing.*` roster + the `+N` surface delta), D-TRACE-REJECT (the accept/reject partition of the deferred provider/HCM sub-fields), D-TRACE-DIFFERENTIAL (force `random_sampling=100%`; assert the stable span-field subset + a trace-id continuation prong; payload-not-framing per `reference_streaming_sink_differential_framing`; driver-owned receiver per `reference_differential_grpc_receiver_driver_owned`), D-TRACE-SPLIT (the FINAL ADR-0045 46.1a/46.1b sub-split re-check with real LoC). Then the usual lifecycle: PLAN → IMPL (subagent-driven in a fresh worktree per `feedback_execution_style` · `feedback_git_worktrees`).

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** The next stage is the 46.1 SPEC (docs-only, direct-on-master; a fresh worktree only if code-adjacent probe artifacts land — the doc itself commits direct on master).
- **Process anchors:** ADR-0106 (per-leg roadmap rows; row 46 `in-progress`) · ADR-0045 (split-gate — the 46.1a/46.1b contingency) · ADR-0041/0080 (silent-ignore vs strict-reject — phase 46 LIFTS the HCM `tracing` field from silent-ignore) · ADR-0016 (proto blank-import registry) · ADR-0158 (the `grpcclient.Dialer` + typed-wrapper layer — the `OTLPTracesClient` precedent) · ADR-0258/0259 (the phase-45 OTLP sink + operator engine — the OTLP module + the unary-Export batching-sink the trace exporter reuses) · ADR-0044 (ADR §Context-at-SPEC, body-at-IMPL) · ADR-0227 (the contrib reference) · next-free **ADR-0260** (46.1 core+OTLP), then **ADR-0261** (46.2 Zipkin+B3).
- **The substrates the 46 SPEC builds on (verified at the BRAINSTORM):** the HCM `tracing` field (`bootstrap.go:354` HCM unmarshal — `tracing` currently silent-ignored) · the request-lifecycle seams (`internal/filter/hcm/connection.go:448` span-start `startTime`, `internal/filter/hcm/accesslog_emit.go:19`/`:45` span-end) · the no-`x-request-id` invariant phase 46 revises (`internal/filter/http/router/router.go:763`) · the `grpcclient.Dialer` + `NewOTLPLogsClient` typed-wrapper (`internal/grpcclient/grpcclient.go:107`/`:339`) · the `otlpsink.go` batching pattern (`NewOTLPAccessLogSink :81`/`Submit :111`/`run :155`/`Close :135`) · the `go.opentelemetry.io/proto/otlp v1.0.0` module (`trace/v1` `Span` + `collector/trace/v1` `TraceService.Export`, UNARY) · the tracing config protos at `go-control-plane/envoy v1.32.4` (`config/trace/v3/{opentelemetry,zipkin,http_tracer}.pb.go`; the HCM `Tracing` message). NEW: the `internal/tracing` package (engine + span model + propagation + OTLP exporter); the trace receiver is DRIVER-OWNED in `test/helpers/` (the `otlplogs` precedent — BackendKind STAYS 38).

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-46 BRAINSTORM is DONE + LANDED + pushed; row 46 `in-progress`; the next action is the 46.1 core+OTLP SPEC).
2. `docs/envoy-go/phases/46-tracing/BRAINSTORM.md` — the charter (the 2-leg split, the full-fidelity sampling decision, the §10 D-TRACE-* SPEC obligations, the §8 deferred items).
3. `docs/envoy-go/STATE.md` — the active-phase header (`phase 46 (tracing) BRAINSTORM done`) + the recorded NEXT (the 46.1 SPEC) + the count figures.
4. `docs/envoy-go/ROADMAP.md` — row 46 (`tracing`, now `in-progress`) + the `### Observability family` heading.
5. `docs/envoy-go/DECISIONS.md` — **ADR-0259** (the 45.2 as-built) + **ADR-0258** (45.1) + next-free **ADR-0260** (the 46.1 anchor).
6. `docs/envoy-go/phases/45-otlp-access-log/SPEC-45.1.md` / `SPEC-45.2.md` — the SPEC house shape (14 sections) + the §11 live-probe pattern the 46.1 SPEC mirrors.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `reference_streaming_sink_differential_framing`, `reference_differential_grpc_receiver_driver_owned`, `reference_docker_probe_bridge_network`, `reference_dynamic_stat_name_charset_guard`, `reference_wire_format_both_sides_see_same_bytes`, `feedback_execution_style`, `feedback_git_worktrees`, `feedback_subagents_no_push`, `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`).
