START a fresh cold-start session for envoy-go — **the phase-40.2 (`outlier-detection-other-consecutive`: the gateway + local-origin consecutive detectors + the split accounting switch) PLAN is DONE, LANDED on master (commit `104daf2`), and PUSHED to `origin/master`; the next lifecycle step is the phase-40.2 IMPL** (`superpowers:subagent-driven-development` — execute `PLAN.md` Tasks 1–12 in a fresh worktree, fresh subagent per task + two-stage review; six-gate + full 73-dir differential GREEN; ADR-0246 body IN-PLACE per ADR-0044; land + push) OR a new subject. Confirm the tip via `git log --oneline -5` (HEAD is the docs-only next-prompt roll-forward `89d30b1` sitting on the substantive master tip — the phase-40.2 PLAN `104daf2`; the prior substantive tips are the phase-40.2 SPEC `2919a3a` and the phase-40.1 IMPL squash `bb4da7b`). Counts at the 40.2 PLAN (UNCHANGED — docs-only): stat surface **1137** / fixtures **71** / fuzzers **42** / BackendKind tail **35** (`HTTP503Responder`) / DECISIONS tail **ADR-0245**, next-free **ADR-0246**.

## What just landed (phase 40.2 PLAN — the durable record is `docs/envoy-go/phases/40.2-outlier-detection-other-consecutive/{PLAN,SPEC,README}.md`; read those, not this summary)

The PLAN (`superpowers:writing-plans`) decomposes SPEC §10 into **12 bite-sized TDD tasks** over the 40.1 substrate (ADR-0245), reused UNCHANGED. plan-document-reviewer APPROVE-WITH-MINORS (the B1 `Endpoint`-not-comparable `IsZero()` fix + the M2 break-site pin applied; the `tryEject` refactor + the gateway-first ordering + the split branch + every cited file:line + the counts were all VERIFIED-correct against source). The FINAL ADR-0045 split-gate re-check → **NO FURTHER SPLIT** [~150–260 prod LoC / 12 tasks].

**The 12-task spine:** (1) baselines/PROGRESS; (2) `parseOutlierDetection` extension (7 gateway/local-origin/split fields + defaults + the 2 reject arms) + the `consecGw`/`consecLO` counters on `hostHealth`; (3) refactor the 40.1 inline eject into a reusable `tryEject(enforcing, enforcedCounter) bool` + the gateway-detector path `recordExternal5xx` (gateway-checked-FIRST) + the `record(ep, statusCode, localOriginErr)` 3-arg signature; (4) the local-origin detector `recordLocalOrigin` + the `split` branch; (5) the +4 stat registrations (1137→1141); (6) the seam — widen `AcquireH1` → `(*PooledH1Conn, Endpoint, error)` + surface the picked `ep` on the shared `Cluster.Dial`/`AcquireH1`/`DialH2` connect-failure paths + add `(Endpoint).IsZero()` + populate `LocalOriginErr: true` at the five router connection-failure sites; (7) the `0070` gateway fixture; (8) the `0071` local-origin fixture (split=true, `allocDeadPort` dead host); (9) `0070`/`0071` deliberate breaks (2 each) + 20-run flake; (10) widen the `0069` `StatsAsserter` (gateway-detected) + full 73-dir differential + six-gate; (11) ADR-0246 body + BEHAVIOR_CONTRACT; (12) completion bundle.

**Load-bearing IMPL facts (from the PLAN's D-S40.2-* resolutions + the reviewer's verified-correct list):**
- **The `tryEject` refactor** must preserve the 40.1 `consecutive_5xx` behavior EXACTLY (the cross-multiplied cap `(ejected+1)*100 > cap*total`, the CAS once-only eject, the double-count `ejections_enforced_total` + the per-detector enforced, the detected-Inc-before-already-ejected-guard ordering). The 40.1 `outlier_test.go` `record(ep, code)` calls become `record(ep, code, false)` (15 sites).
- **The gateway-first ordering** (`recordExternal5xx`): a gateway-class 5xx `{502,503,504}` increments `consecGw`; if it EJECTS, return before the 5xx detector (`detected_consecutive_5xx` stays 0 — the live pin); if it does NOT eject (detect-only `enforcing_consecutive_gateway_failure` default 0, the `0069`/`0070` path), FALL THROUGH so `consec5xx` also advances. A non-gateway 5xx resets `consecGw`.
- **The split branch:** split=true ⇒ `recordLocalOrigin` feeds ONLY the local-origin detector (and a completed external response resets `consecLO`); split=false ⇒ a `LocalOriginErr` result maps to `recordExternal5xx(statusCode)` where statusCode is the local-reply 502/503 (always gateway-class).
- **`Endpoint` is NOT comparable** (it carries a `Metadata map[string]SubsetValue`) → `ep != Endpoint{}` does NOT compile. Task 6 MUST add `func (e Endpoint) IsZero() bool { return e.Host == "" && e.Port == 0 }` and the router seam guards use `!ep.IsZero()`. The pick-failure paths already return a literal `Endpoint{}`.
- **`AcquireH1` has exactly TWO callers** (`router.go:610` live + `cluster_test.go`); `DialH2` already returns `Endpoint` (value-only change, no signature change). The legacy `(*routerAction).do`/`(*routerActionH2).doH2` drivers + `DialH2`'s legacy caller (`router_h2.go:271`) stay seam-free (they ignore the propagated `ep`). The H2 ctx-cancel sentinel path is a downstream cancel → NO seam call.
- **+4 stats** registered unconditionally on any outlier cluster (the reference exposes all `outlier_detection.*` names regardless of config): `ejections_{detected,enforced}_consecutive_gateway_failure` + `ejections_{detected,enforced}_consecutive_local_origin_failure`. The `0069` StatsAsserter is WIDENED in Task 10 (envoy-go now emits gateway-detected on `0069`'s 503s; remove the 40.1 exclusion).
- **`0070`/`0071` constants** single-sourced (`refContainerListenerPort` 0070=19159, 0071=19160 — verify next-free); `0070` uses `consecutive_5xx` HIGH (the 5xx detector can't fire → `detected_consecutive_5xx == 0` cross-asserted); `0071` uses split=true + the dead `allocDeadPort` host (BackendCount()==2, the `0066` shape). Both `StatsAsserter` (NOT `SubjectAsserter`).

## Honor throughout (the IMPL discipline)

- **Lifecycle = one stage per session:** the IMPL (`superpowers:subagent-driven-development` — fresh subagent per task + two-stage review). `feedback_git_worktrees` · `feedback_execution_style` · `feedback_push_to_origin` · `feedback_subagents_no_push` (subagents commit locally; the CONTROLLER squashes/ff-merges/pushes at stage-close) · `feedback_subagent_worktree_path_targeting` / `feedback_subagent_worktree_detach` (pin canonical paths; controller re-verifies the clean main repo).
- **Differential discipline:** `reference_differential_run_selector` (`-run 'TestDifferential/<NNNN>'` — the `TestDifferential/` prefix is REQUIRED; a bare `0070` matches ZERO subtests) · `reference_differential_break_protocol_count1` (`-count=1` on EVERY break run — go-test caching serves a stale PASS; the seam rides the LIVE `doH1/doH2ClusterAction` drivers, now ALSO at the connection-FAILURE sites) · `reference_health_check_propagation_warmup` (poll `ejections_active` to converge + warmup-until-K-200s + delta counters; widen deadlines not assertions on flake) · `reference_docker_probe_bridge_network` (Docker bridge; verify `upstream_rq_total>0` ref-side) · `reference_fixture_workload_constant_desync` (single-source the workload constants) · `reference_differential_asserter_dispatch` (cross-side ⇒ `StatsAsserter`) · `feedback_pertask_gofmt_lint` (each task runs gofmt -l + golangci-lint on touched pkgs) · `reference_percent_cap_cross_multiply` (the reused 40.1 cap).
- **ADR/process:** ADR-0044 (ADR-0246 §Context already drafted in SPEC §13; §Decision/§Consequences land IN-PLACE at this IMPL) · ADR-0052 (six-gate) · ADR-0080 (byte-stable rejects) · ADR-0106 + `reference_roadmap_split_phase_row_done` (row 40 STAYS `in-progress` after 40.2 — it flips `done` only when the 40.3 final leg lands) · ADR-0227 (`contrib-v1.37.2`).

## Read first (in order)

1. THIS FILE — the post-phase-40.2-PLAN cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 40.2 (outlier-detection-other-consecutive) PLAN done`; the count footer (1137 / 71 / 42 / 35 / ADR-0245, next-free ADR-0246); the lifecycle routing (next-skill = the 40.2 IMPL).
3. `docs/envoy-go/phases/40.2-outlier-detection-other-consecutive/PLAN.md` (the 12-task spine + the per-task code + the D-S40.2-1..7 resolutions + the Exit-deltas table) + `SPEC.md` (the §1.1 AMEND-OD2-1..6; the §3 architecture; the §5 proto roster fields 10–14; the §6 reject roster; the §7 stat surface +4; the §8 `0070`/`0071` fixtures; the §11 D-OD2-* live pins; the §13 ADR-0246 §Context draft).
4. `internal/cluster/{outlier.go,health.go,cluster.go,manager.go,dial_h2.go}` (the 40.1 `outlierConfig`/`parseOutlierDetection`/`outlierDetector.record`/`registerStats` the PLAN extends; the `tryEject`-refactor target; the `AcquireH1`/`Dial`/`DialH2` endpoint-surfacing sites; `RecordUpstreamResult`/`UpstreamResult`) + `internal/filter/http/router/{router.go,router_h2.go}` (the success sites :656/:101 + the five connection-failure sites the seam populates) + `test/fixtures/0069-outlier-detection-consecutive-5xx/driver/` (the template + the StatsAsserter to widen) + `test/fixtures/0066-health-check-http/driver/` (the `allocDeadPort` dead-host mechanism `0071` reuses) + `test/differential/{fixture/fixture.go,runner_test.go}` (the `HTTP503Responder` 35 + `PerHostBackendKind`).
5. `docs/envoy-go/DECISIONS.md` (ADR-0245 [the substrate] + the process ADRs) + `docs/envoy-go/BEHAVIOR_CONTRACT.md` (the `### Cluster — passive health (outlier detection)` subsection 40.2 extends).
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`.
