=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **THERE *IS* BANKED MID-LIFECYCLE WORK AT THIS TIP — DO NOT SELF-PICK.** Phase 84 (`grpc-unary-response-trailers`) was BRAINSTORMED and SPEC'd on 2026-08-06 (the SPEC **SPLIT the row 84.1 / 84.2**), `PLAN-84.1.md` landed 2026-08-07, and ⚠️ **the 84.1 IMPL landed the FIVE-file seam on 2026-08-08** — `ADR-0306` COMPLETED, `BEHAVIOR_CONTRACT` reconciled, 120/120 differential clean, stat +0. **Row 84 is STILL `in-progress`** and the phase is OPEN. ⇒ **This session performs the 84.2 PLAN** — `PLAN-84.2.md`, the differential fixture `0119-grpc-unary-trailers` at reference port `10119` on the EXISTING `GRPCHealthResponder` BackendKind 34. **ITS IMPL — the session after — is what flips row 84 `done`, after which check (2) is the SOLE remaining sentinel blocker.**

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=116 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **CHECK (1) PRINTS — `NOT DONE: row 84` — AND THAT IS CORRECT.** Row 84 goes silent only when the **84.2 IMPL** flips it (the SPEC split the row; **the parent flips `done` when BOTH legs land**, per `reference_roadmap_split_phase_row_done`). ⚠️ **CHECK (3) IS SILENT, AND THAT IS THE STANDING HAZARD.** Its success mode and its "broken loop" mode are indistinguishable, so **the check-(3) doctoring NC is mandatory every session**: doctor the `gRPC-family row` mention on a scratch copy and confirm `NEVER OPENED: gRPC` comes back. **Run the row-doctoring NC below as well** — it is still the cheapest proof that check (1)'s parser works (a `sed` targeting `done      ` does NOT work: it misses the real `| done |` column spacing and prints nothing, which reads exactly like *"the check is blind"*):
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
```
```sh
# the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```

⚠️ **AND WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -oiE '-family row'` fails with `grep: amily row: No such file or directory` and the surrounding arithmetic prints `base=0 now=0 delta=0`, **which reads exactly like "no change".** Observed live at the phase-84 BRAINSTORM **and again at the phase-84 SPEC**. **Always pass `--` before the pattern**, and remember the counts are form-dependent: **95 occurrences vs 67 LINES** at this tip (`grep -c` counts LINES). **A gate that reads zero on BOTH sides is not evidence of invariance.**

⚠️ **`want=116` IS A LIVE FIGURE, NOT A CONSTANT.** ⚠️ **THE 84.2 PLAN IS DOCS-ONLY AND ADDS NO ROW, SO IT MUST *NOT* TOUCH `want`** — and **row 84 STAYS `in-progress`** (it flips only at the 84.2 IMPL). ⚠️ **The 84.1 IMPL ALREADY recorded the 84.1 leg in row 84's `sub-phases` column IN PLACE** (the §Schema `:18` update-in-place invariant; the rows-60/61 PROSE precedent) — a docs-only PLAN should now leave `ROADMAP.md` **BYTE-UNTOUCHED**. ⚠️ **ZERO dotted rows exist above 32.2** (highest are 29.x, 32.1, 32.2), and phase 61's splitting SPEC commit `6a26909b` touched **exactly one file** with `ROADMAP.md` byte-untouched. ⚠️ **If you touch `ROADMAP.md` AT ALL, run the leak check as a WHOLE-FILE BEFORE/AFTER COUNT.**

Check (3) fails SAFE by construction. When you open a new family, add its row-summary slug to the list above.

**SENTINEL RE-RUN MECHANICALLY AT THE PHASE-84.1 IMPL, WITH FIVE FIRING NEGATIVE CONTROLS. IT DOES NOT FIRE. `stop` was NOT created; `ls stop` => `No such file or directory`.** ⚠️ **The 84.1 IMPL edited row 84's `sub-phases` column, so the WHOLE-FILE leak check was run and every axis HELD:** data rows **116**, check-(2) union **6**, `-family row` **95 occurrences / 67 LINES**, `gRPC-family row` **2** — all `--`-guarded. ⚠️ **Re-run it yourself anyway and record what YOU see.** ACTUAL output, recorded rather than predicted:
- **(1) `NOT DONE: row 84`** at `want=116`, denominator printed (`examined 116 data rows`) — **CORRECT AND EXPECTED while the phase is open.**
- **(2) SIX — `:194 :200 :206 :216 :222 :230`.** ⚠️ **Do NOT "fix" it and do NOT forecast a decrease.**
- **(3) SILENT.** **This is the state that makes the check-(3) doctoring NC mandatory.**
- Input measured at **234 lines / 116 data rows** before anything was written, so an empty result could not read as a zero result.
- **ALL FIVE NCs FIRED:** row 62 doctored ⇒ `NOT DONE: row 62` **and** `NOT DONE: row 84` (with `NC LANDED? [ in-progress ]` inspected first); `want=115` ⇒ `GATE FAIL: examined 116 data rows, expected 115`; the check-(3) doctoring (residual occurrences confirmed **0** first) ⇒ `NEVER OPENED: gRPC`, while `WASM`/`HTTP-filters`/`Runtime` correctly stay silent and an invented slug fires; check-(2) **one-arm** strip ⇒ **6 -> 5, NOT 6 -> 0**; both arms ⇒ **0**.
- ⚠️ **THE ONLY REMAINING SENTINEL BLOCKERS ARE CHECK (1) (row 84, closes at the 84.2 IMPL) AND CHECK (2) (six family backlogs).** Once row 84 lands, **check (2) is the sole thing standing between this project and the termination sentinel.**

⚠️ **THE LEAK CHECK GOES LIVE THE MOMENT YOU TOUCH A ROADMAP ROW.** The rule: **NEVER WRITE A SENTINEL'S OWN MATCHER STRING INTO A FILE THE SENTINEL GREPS.** The sentinel greps `docs/envoy-go/ROADMAP.md` — writing these strings *here* (or in `SPEC.md` / `PLAN-*.md` / `DECISIONS.md` / `STATE.md`) is safe; writing them *there* is not. ⚠️ **AND THE TWO CLASSES ARE OPPOSITE.** `<slug>-family row` is check (3)'s **PASS** condition; the deferred-candidate phrases are check (2)'s **FAIL** conditions. **One leak silences a real failure; the other manufactures one.**
⚠️ **AND THE LEAK CHECK MUST BE A BEFORE/AFTER COUNT OVER THE WHOLE FILE, NOT A GREP OF THE DIFF'S `+` LINES.** Baselines RE-CONFIRMED at the phase-84.1 IMPL (unchanged since the PLAN), for you to diff against: check-(2) union **6** · `-family row` **95 occurrences / 67 LINES** · `gRPC-family row` **2** · **234 lines / 116 data rows**.

⚠️ **ROW WELL-FORMEDNESS: THE GATE MUST BE A DISJUNCTION.** **ARM-A (escape-aware, `pieces != 8` on a `|`-split) catches lines 119 and 131 ONLY; ARM-B (escape-aware trailing piece) catches line 140 ONLY.** ⚠️ **Naive `NF!=8` flags SEVENTEEN — 15 FALSE POSITIVES + 2 true — and MISSES line 140** (compensating defects cancel) ⇒ wrong in **both** directions. ⚠️ **Inspect that your NC landed before trusting its result.** ⚠️ **A GATE-AUTHORING TRAP RECORDED RATHER THAN HIDDEN:** the phase-82 PLAN's first ARM-A used the wrong pipe-split threshold (`!=9` instead of `!=8`) and flagged **113 of 114 rows**. **A gate that fires on almost everything reads as THOROUGH and is WORTHLESS. Only the printed denominator exposed it. Print your denominators.**

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (ugrep under the hood). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Reproduced at FOURTEEN separate tips.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **THE METHOD BINDS; A LIVE DISCREPANCY NUMBER DOES NOT.** At the phase-84.1 PLAN the two denominators **agreed** for `BEHAVIOR_CONTRACT.md:NNNN` cites, because the phase-84 SPEC commit `91dc1cf6` rewrote this file and **dropped the `BEHAVIOR_CONTRACT.md:1967` cite that had been the standing example** (present at `e1c07208`, absent at `91dc1cf6`). **The SPEC's own commit falsified the SPEC's own illustration.** Keep the method; **carry no standing discrepancy figure, and no standing absolute either** — see the self-invalidation note at the reconciliation table.
⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS.** ⚠️ **Print FILE NAMES, never a count.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** — git exempts TRACKED files, so it exits 1 and reads as *"not ignored"*; `git check-ignore -v --no-index` prints the truth, and `git status` is the discriminating check. ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **`grep -c` ON ZERO MATCHES IS TOOL-SPECIFIC**: only `git grep -c` prints nothing; GNU and the wrapper print `0`. Both **exit 1**, so an exit-code gate is wrong for either — and `$(cmd || echo ERR)` corrupts the reading. **Capture the value; never chain on the exit code.** ⚠️ **AND `grep -c` COUNTS *LINES*, NOT *OCCURRENCES***. ⚠️ **`\t` IN GNU ERE IS A LITERAL `t`**; use `-P` unconditionally. ⚠️ **A `git grep` PATHSPEC ENDING IN `/` READS A FAIL-UNSAFE ZERO** (broken-gate shape 33): the quoted git-pathspec form returns **0 lines / 0 files** where the equivalent shell-glob form returns **70**.

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`** with FOUR blind spots (an em-dash, a DOT in `http-1.1`, two LETTER-suffixed ids). Every document from phase 75 forward called it *"currently HARMLESS."* ⚠️ **Nobody fired the unsafe arm until the phase-77 PLAN. EXECUTED on scratch copies: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** ⚠️ **DO NOT restore the regex form.**

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

⚠️ **Run the union form, never one half of it.** ⚠️ **A ONE-ARM STRIP IS NOT AN NC**: stripping only `deferred candidates:` moves the union **6 -> 5, not 6 -> 0**, because the long form does not contain the short substring. ⚠️ **The documented trap's stated MECHANISM is refuted while the trap is real** — the inflation comes from ROADMAP table rows carrying BRAINSTORM prose. **Cite the command; do not re-inherit the explanation.**

⚠️ **CHECK (2) HAS NEVER GONE DOWN** across ~40 phases (history `0 -> 1 -> 3 -> 4 -> 5 -> 6`) => **the candidates sentence is a WINDOW onto a larger deferred backlog, not an inventory of it.** ⚠️ **A stage that predicts a decrease is repeating the phase-73 error.**

⚠️ **`:213` IS NOT A SENTENCE ANCHOR.** It is a very long line carrying SEVERAL deferred phrases; only ONE is the colon-terminated form the sentinel matches. Citing *"the `:213` sentence"* without the offset is ambiguous. ⚠️ **And `:227` does not carry the long phrase at all** — SHORT form only.


==============================================


## STATUS AS OF 2026-08-08: PHASE 84 (`grpc-unary-response-trailers`) IS **OPEN**. ROW 84 IS STILL `in-progress`. THE ROW IS SPLIT 84.1 / 84.2 AND **THE 84.1 IMPL HAS LANDED**. **THIS SESSION BUILDS THE 84.2 PLAN.**

**What the 84.1 IMPL landed.** Base master **`764a530d`**, branch `phase-84-impl`, final tip **`87e67fe2`**, **TWENTY tasks subagent-driven**. Production (**FIVE files**): `internal/filter/hcm/h2/client.go` (capture the trailing block into `H2Response.Trailers`; **D-84-VALIDATE at CAPTURE time, failing the stream via `h2.NewStreamError` -> RST_STREAM(INTERNAL_ERROR), NO `EvictH2ConnOnError`**) · `internal/filter/hcm/h2/stream.go` (EXTRACT the connection-specific validated-trailer set out of `buildRequest`) · `internal/filter/http/router/router.go` (`ActionResponse.Trailers`) · `internal/filter/http/router/router_h2.go` (the ONE populate site) · `internal/filter/hcm/h2dispatch.go` (`writeH2Reply`, THREE call sites) · plus a comment-only narrowing at `internal/filter/http/chain.go:483` and a `retry.go` predicate fix. Docs: **`ADR-0306` COMPLETED IN PLACE** (`DECISIONS.md` **17956 -> 17990**, ¶4 *"ten"* -> **nine**, strict `PROPOSED` guard **1 -> 0**); **`BEHAVIOR_CONTRACT.md` reconciled** (**5900 -> 5955**, five net-zero rewrites + one `## HTTP/2` tail append); `ROADMAP.md` row 84 `sub-phases` recorded IN PLACE (status STAYS `in-progress`, `want` STAYS **116**). Gates: **120/120 differential CLEAN** (`INNER_EXIT=0`, 388.892 s), seam packages green, `-race`/vet/gofmt/golangci 0, **stat surface +0** by call-site enumeration (208/36).

### ⚠️ WHAT THE 84.1 IMPL PROVED — CARRY THESE INTO 84.2

- ⚠️ **THE ROW CREATED A FORWARD PATH AND CLOSED THE CONDUIT IT OPENED (Task 13).** The uppercase-field-name bypass in `validateResponseTrailers` was LOAD-BEARING — a capture-without-validation patch is an active smuggling conduit (RFC 9113 §8.2.1). Closed; request/response guard parity is byte-identical A-Z. **84.2 must not re-open it via an emit-time strip.**
- ⚠️ **THE PLAN'S "retry.go no edits" WAS REFUTED BY EXECUTION (Task 5).** The `timedOut` predicate misclassified the new `Status:0` producer as a per-try-timeout (504 + err=nil at the deadline, MEASURED); fixed by extracting `perTryTimedOutH2` + an 8-row table.
- ⚠️ **A stats-only fixture assertion is VACUOUS ON BOTH SIDES** (broken-gate shape 31) — the reference books `upstream_rq_200`/`downstream_rq_2xx` even for the streams it RESETS. **84.2 MUST assert the RPC's own status cross-side via the Drive hooks + `CompareBytes`, NEVER a stat.**
- ⚠️ **TWO FRAME-SEQUENCE PARITY NOTES 84.2 WILL NEED, measured at 84.1:** an empty trailer block makes the reference emit an extra `DATA len=0 END_STREAM` (a `len(trailers)==0 ⇒ END_STREAM on the body DATA` impl emits one frame fewer — semantically identical, frame-sequence DIVERGENT); and ⚠️ **the response HEADER path is ALREADY an unvalidated conduit today** (the reference 502s a response *header* block carrying `connection`/`transfer-encoding`/`keep-alive` while `router_h2.go:195-201` strips only pseudo-headers). **Row 84 must NOT be credited with fixing it, and 84.2 must not stumble into it.**
- ⚠️ **Do NOT wire `RunEncodeTrailers`** — it stays dead; ADR-0273's boot-reject and `BEHAVIOR_CONTRACT.md:4311` stay TRUE.
- ⚠️ **`host` and `trailer` PASS THROUGH** (reference-parity); the second-trailing-block rule was DROPPED as dead-on-the-wire; the binding validation set is **RFC 9113 §8.2.2 + `content-length` + pseudo-headers + END_STREAM**.

### ⚠️ WHAT THIS SESSION DOES — THE 84.2 PLAN

**Author `PLAN-84.2.md`** — the TDD spine for the differential fixture `0119-grpc-unary-trailers` at reference port **10119** on the EXISTING `GRPCHealthResponder` **BackendKind 34**, asserting a SUCCESSFUL unary gRPC RPC's own status cross-side (Drive hooks + `CompareBytes`). ⚠️ **Docs-only: `ROADMAP.md` stays BYTE-UNTOUCHED, `want` stays 116, row 84 stays `in-progress`.** ⚠️ **THIS IS THE FINAL LEG — the 84.2 IMPL flips row 84 `done`, after which check (2) is the sole thing standing between this project and the termination sentinel.** The fixture reuses `0079`'s PKI + listener block; because `GRPCHealthResponder` is runner-spawned on a kernel-ephemeral port, **phase 84 is OUTSIDE the driver-receiver port-race class (behavioural roster 42)** — a port flake HERE is a FINDING, so check `git worktree list` + `ps` for siblings first. ⚠️ **The differential is the ONLY layer that sees an unconditional-END_STREAM regression** — the conditional-vs-unconditional END_STREAM choice at the differential layer is 84.2's to prove (the 4-cell frame matrix at 84.1 proved D-84-ENDSTREAM invisible to the existing unit surface). ⚠️ **PLAN it with the SPEC's owed VACUITY control** (the stats legs must stay green under every arm) **and SYMMETRIC control** (same wrong value on both sides, must PASS), plus a liveness arm with a FAILING baseline.

### ⚠️ THE GATES — BASELINES RE-CONFIRMED AT THE PHASE-84.1 IMPL (final tip `87e67fe2`), BY EXECUTION

Full 120-fixture differential **120/120, `INNER_EXIT=0`, 388.892 s**, 0 FAIL/SKIP, 0 `no driver registered`, 0 panic/DATA RACE/SIGSEGV, `=== RUN` denominator 120 · `gofmt -l` **empty** on all three packages (62 `.go` files) · `golangci-lint run` **rc=0** at **v1.64.8**, `disable-all: true` + 9 linters, `misspell` locale **US** — ⚠️ **the NC FIRED on British PROSE (`behaviour`), and it fired on an agent's own comment; `normalised` in the same comment was NOT flagged** · `go vet` rc=0.
```sh
( go test ./test/differential/ -count=1 -v > "$SCRATCH/full.log" 2>&1; echo "INNER_EXIT=$?" )
grep -cE '^    --- PASS: TestDifferential/' "$SCRATCH/full.log"          # want 120
grep -E  '^    --- (FAIL|SKIP): TestDifferential/' "$SCRATCH/full.log"   # want EMPTY
grep -c  'no driver registered for fixture' "$SCRATCH/full.log"          # want 0
grep -cE '^panic:|DATA RACE|SIGSEGV' "$SCRATCH/full.log"                 # want 0
```
⚠️ **`go test ./... -count=1` DOES NOT GIVE "126 ok / 0 FAIL" AT THIS TIP.** Measured: **`INNER_EXIT=1`, 125 ok / 109 no-test-files / 1 FAIL of 235 packages**, aborting on `0084-otlp-access-log` — `bind: address already in use` at `driver.go:169 ensureServer`, the known driver-receiver port race. **Every non-differential package is green.** No sibling `go test` was running. ⚠️ **`./...` runs the differential CONCURRENTLY with 234 other packages — a WORSE environment for that race than the standalone gate. Cite it honestly, both ways.**
⚠️ **`INNER_EXIT` IS NOT OPTIONAL, AND IT IS MANDATORY FOR GATE (e), NOT JUST (b).** The phase-83 IMPL's FIRST differential launch ABORTED while the surrounding tooling reported success, and it happened again at this stage. **Budget ~3 launches.**
⚠️ The faithful dir predicate is `^[0-9]{4}[a-z]?-` — a bare `^[0-9]{4}-` gives **118**.
⚠️ **`go test ./test/differential/...` (with `...`) matches TWO packages and BUFFERS `-v`.** A run that looks hung with an empty log is normal — do not kill it. ⚠️ **Subtest `--- PASS` lines flush only when the PARENT completes**, so a mid-run `grep -c` legitimately reads **0**. Watch `=== RUN`.
⚠️ **KNOWN PRE-EXISTING FLAKE:** the driver-receiver port race (behavioural roster **42**). **Phase 84 is OUTSIDE the class** — `GRPCHealthResponder` is runner-spawned on a kernel-ephemeral port. **Check for sibling sessions first** (`git worktree list` + `ps`).

**h2spec, LIVE-CONFIRMED at this stage:** `http2/6/10` → **`No matched tests found.`, exit 0** (the silent no-op, reproduced) · `http2/6.10` → **6/6** · `http2/6` → **42 tests, 37 passed, 1 skipped, 4 failed, exit 1** · **NC `http2/5` → 22/22**, matching `CONFORMANCE_PINS.md`'s 5.x sum, so the harness is not globally broken. `assertThreshold`'s `if s.Tests == 0 { continue }` is at **`h2spec_test.go:310`** (SPEC's `:309-311` is off by one). **The gate is NOT run in CI at all** — 0 hits in `.github/` (NC: `go test|golangci` returns 8).
⚠️ **STANDING RULE: NO ROW MAY CITE "h2spec 53/53" AS EVIDENCE ABOUT FRAME-LEVEL BEHAVIOUR.** 42 strict cases — **44% of the declared scope** — have been silently unrun since 2026-04-25. **The number is honest; the scope claim attached to it is not.**

### ⚠️ THE SIX-GATE STANDING POSTURE — NAME THE DEPARTURES, DO NOT CLAIM COMPLIANCE

Gate **(f)** expects a `REVIEW.md`; **none since 25.3 — 88 of 125 phase dirs carry none** (⚠️ **37 of 125, NOT 124: the phase falsified its own count with its own commit for the SECOND consecutive stage** — `reference_branchpoint_roster_stale_midrow`). Gate **(a)** at 84.1 is **VACUOUS but NOT the strong kind** — 84.1 adds no fixture, yet the differential is the **only** layer that would see an unconditional-END_STREAM regression, **so (b) is doing (a)'s work; say it that way.** Gate **(d)** is **VACUOUS for this row** — say "vacuous", not "green": §7.4 binds a phase introducing a *parser, codec, or filter* and this row introduces none (the HPACK decode already runs at `client.go:425`; the frame goes through already-landed code). Fuzzers **55** in **48** files (⚠️ `-- '*.go'`-scoped; unrestricted 161, whose 106-line delta is **100% Markdown code fences**) — ⚠️ **ALL under `internal/`, ZERO under `test/`**, a 100% violation of §7.4's own location clause. Gate **(c)**: `test/conformance/grpc/` is **DEFERRED IN WRITING** on two independent grounds; proxy-wasm **10 of the cpp-host's 16 families (62.5%), 6 deferred**. Normative cites: **§6.1 `:285`**, `:289` ~25-task, `:290` ~1500-LoC, **§6.2 `:294`**; **§7.3 `:346-351`**; **§7.5 `:357`, gates `:360-365`**. ⚠️ **`/BOOTSTRAP_PROMPT.md` WITH A LEADING SLASH RESOLVES NOWHERE** — the file is `<repo>/BOOTSTRAP_PROMPT.md` (**522** lines). ⚠️ **THE SECOND COPY IS NOT A COPY — IT IS THE GENERATOR PLAN** (`docs/superpowers/plans/2026-04-21-envoy-go-bootstrap-prompt.md`, **1024** lines), **CONTENT-DIVERGENT with a NON-CONSTANT offset. Open the repo-root file.**

**STAT SURFACE +0**, discharged by **call-site enumeration — 208 code sites / 36 production files (⚠️ cite 208/36, NEVER 208/84)**. ⚠️ **DO NOT DISCHARGE VIA `TestNoNewStat*` — PROVEN BLIND BY EXECUTION at this stage, with BOTH arms compiling:** a **compiling** `reg.NewCounter(...)` inside `writeH2Reply` left all five guards **PASS**, while the same registration inside `NewFlusher` made all five **FAIL**. All five are `internal/statssink/registration_test.go:26/53/81/109/137` — one file, one package. ⚠️ **The +0 is a FILE-level claim** (the touched files register 0 each; the touched *packages* register 5 in total). ⚠️ **The absolute is 1207, DOC-SOURCED from `BEHAVIOR_CONTRACT.md`'s ledger tail `:5118` — NEVER from `STATE.md` §Project's stale 1205. ONLY THE DELTA IS ASSERTED, and NO phase from 78 through 84 has added a ledger row.**

### ⚠️ ADR + DOCUMENT CONVENTIONS

**The DECISIONS tail is `ADR-0306`, now STATUS `COMPLETE` (completed at the 84.1 IMPL — no renumber, no `---` separator). Next-free is `ADR-0307`.** ⚠️ **The 84.1 IMPL completed ADR-0306 IN PLACE and DISARMED the strict `PROPOSED` guard 1 -> 0, which is CORRECT.** ⚠️ **Do NOT use the loose form `^> \*\*STATUS: .*PROPOSED`** — every hit is a false positive. ⚠️ **CARRY NO LIVE WHOLE-FILE COUNT OF THAT PATTERN ANYWHERE.**

Invariants re-derived at the phase-84.1 IMPL close, each with a control: `DECISIONS.md` **17990** lines · **305** `^## ADR-` headings, ids **0001-0306**, ⚠️ **exactly one gap, at ADR-0209**, zero duplicates — ⚠️ **the arithmetic is CONSISTENT *because of* the gap, so "next-free = headings + 1" COLLIDES at 0306. DERIVE FROM THE TAIL. DO NOT "FIX" IT.** · `^---$` **216** (last at `:17020`; ADR-0289..0306 carry none) · STATUS census **19** (matcher `^> \*\*STATUS: `, a BLOCKQUOTE; ⚠️ `^\*\*STATUS:\*\*` matches NOTHING and reads as a false zero) · retained italic footers **12** ⚠️ **the set is NOT contiguous — phase 79 has none** · **completed-ADR spans 62/66/72/62/68** (ADR-0301..0305), footer sits at the END of §Context, **before** `### Decision` · **`ROADMAP.md` 234 lines / 116 data rows** · **`STATE.md` 64** · **`STATE_HISTORY.md` 462** · **`BEHAVIOR_CONTRACT.md` 5955**.

⚠️ **THE `BEHAVIOR_CONTRACT.md` RECONCILIATION — FIVE FALSIFIED STATEMENTS, AND THE SPEC'S RANGE AND PHRASE ANCHOR ARE BOTH WRONG.** ADR-0052 `:1821` makes `ADR-0306` the **MANDATED** vehicle (verbatim: *"gRPC in a gRPC-family phase … via a new ADR, not by editing 05.1's `### Not asserted` block silently"*), and the block it protects is `:2037`, exactly where statement 3 lives.

| # | line | **stable phrase anchor** | edit |
|---|---|---|---|
| 1 | `:16` | `\| Response trailers \|` | in-place net-zero. ⚠️ **An insert here shifts VIRTUALLY EVERY cite in the repo (measured: all but one)** |
| 2 | `:682` | `- Trailers in access logs` | in-place net-zero |
| 3 | `:2043` | `- Trailers — observed but not forwarded` | in-place net-zero (the ADR-0052-protected block) |
| 4 | `:2068` | `- Trailer forwarding (deferred` | in-place net-zero; NARROW, do not delete |
| 5 | ⚠️ **`:4293` ONLY** | `Trailers are structurally invisible` | in-place net-zero. ⚠️ **`:4291-4311` IS NOT ONE BLOCK — `:4295` opens a DIFFERENT subsection and `:4311` STAYS TRUE. DO NOT EDIT IT.** ⚠️ **And the SPEC's phrase anchor for `:4291` FINDS NOTHING — it omits the backticks around `:scheme`.** |

**Plus ONE tail append** for the new `## HTTP/2` subsection. **A tail append shifts ZERO cites** (no cite anywhere exceeds the file's line count) — **an insert at `:2043`/`:2068` shifts roughly a NINTH of them, and an insert at `:16` shifts nearly ALL.** ⚠️ **RE-MEASURE THE DENOMINATOR IN YOUR OWN WORKTREE, AFTER YOUR OWN EDITS.** Two matchers, both needed: the `BEHAVIOR_CONTRACT.md:<line>` form and the short `BC:<line>` form — ⚠️ **the SPEC's "23 of 197" omits the `BC:` half entirely.** Expected file delta **+55…+61** — ⚠️ **NOT the +21 routine median**: the two rows that performed actual reconciliation (79 at +61/−1, 80 at +55/−9) are both at the top of the range.

⚠️ **THIS COUNT SELF-INVALIDATES, AND IT DID SO WITHIN ONE COMMIT — DO NOT CARRY AN ABSOLUTE.** The phase-84.1 PLAN measured the cite total at its BASE, wrote that figure into this router, and **its own commit moved it by five** — `PLAN-84.1.md` +2, `PROGRESS.md` +1, and **this file +2**, because every one of those documents legitimately cites a contract line while discussing the reconciliation. **The stage that twice flagged "the phase falsified its own count with its own commit" then did exactly that, in the prose warning about it.** ⇒ **Measure AFTER your edits, never before them** (`reference_branchpoint_roster_stale_midrow`), and prefer the SHIFT RATIO — which is what the decision actually turns on — over the absolute, which no document can state durably.

### ⚠️ DOCUMENTARY DEFECTS — carried forward, still deliberately NOT fixed

- ⚠️ **FIXED at the 84.1 IMPL: `ADR-0306` §Context ¶4's "ten" slash-form selectors -> NINE** (ADR-0297/0298 precedent, landed leading with what survives when the block completed). Recorded here as DONE, not owed.
- ⚠️ **`ADR-0051` §2 is FACTUALLY WRONG** about which h2spec sections the gate runs, and `CONFORMANCE_PINS.md`'s own table contradicts it. **Append-only ⇒ RECORDED, NOT FIXED.** ⚠️ Its PUSH_PROMISE exclusion rationale is ALSO moot **twice over** — `http2/6.6` does not exist in h2spec v2.6.0 **and** section 6.6 contributes zero cases to the measured 42.
- ⚠️ **`ADR-0058` cites `internal/filter/hcm/actions.go:routerActionH2.doH2` TWICE and that symbol is not there** — `routerActionH2` is at `internal/filter/http/router/router_h2.go:254`; the emit is `writeH2Reply` at `internal/filter/hcm/h2dispatch.go:671`. **Do not propagate.**
- ⚠️ **RECONCILED at the 84.1 IMPL (DONE, not owed) — verified against the tree at this close:** the five production trailer comments — `h2/client.go` (the capture/discard site, now the VALIDATE-BEFORE-RETAINING comment) · `h2dispatch.go` (the decode-side "RunDecodeTrailers not invoked here" comment) · `http/chain.go:483` (now names the phase-84.1 encode-side capture+validate path) · `router_h2.go` (the old *"never via a trailing HEADERS frame"* clauses are gone; the populate site is `Trailers: resp.Trailers`) · `h2dispatch.go`'s `writeH2Reply` doc (the conditional-trailing-block rule) — were all rewritten to the phase-84.1 truth. ⚠️ **STILL byte-untouched, deliberately, and they STAY:** the UNRELATED citation at `internal/jwt/doc.go` (do NOT touch it) and `router/router.go`'s `Run*Trailers` dead-code comment (the row added only the `ActionResponse.Trailers` field beside it — a PURE addition, no existing comment changed; it papers over the fact that `Run*Trailers` is DEAD production code, so leave it).
- ⚠️ **TWO LIVE TIP DEFECTS the validation incidentally closes:** a trailing HEADERS block **without END_STREAM HANGS `RoundTrip` to the request timeout** (measured 1.5 s to ctx deadline; **the reference RSTs immediately**); and a second END_STREAM block **GOAWAYs the pooled upstream conn AFTER `RoundTrip` returned 200**.
- ⚠️ **THE RESPONSE *HEADER* PATH IS ALREADY AN UNVALIDATED CONDUIT** — the reference **502s** a response header block carrying `connection`/`transfer-encoding`/`keep-alive`, while `router_h2.go:195-201` strips **only** pseudo-headers and `writeH2Reply` re-emits everything lowercased. **Row 84 must NOT be credited with fixing it, and 84.2 must not stumble into it.** Newly named at the phase-84.1 PLAN.
- ⚠️ **`internal/wasm/doc.go:219` carries TWO errors** — *"ABICallbacks 13->20 methods"* against an AST-counted **21**, **and** it names `abi_callbacks.go` when the interface is declared at **`internal/wasm/registration.go:130`**.
- ⚠️ **`ROADMAP.md` cites `github.com/esalaine/envoy-go/…` FIVE times** (module is `github.com/pgdad/envoy-go`); `esalaine` occurs in ZERO `.go` files. **Purely documentary, INVARIANT-BLOCKED.**
- ⚠️ **`ROADMAP.md` malformation: three rows lose summary content in GFM render** (lines 119, 131, 140), INVARIANT-BLOCKED => **defensible as a GUARD with the DISJUNCTION gate, not a fix.** ⚠️ **There is NO doc-invariant test anywhere in this repo.**
- **`STATE.md` §Project counts self-contradict §Current and are FROZEN at the phase-76 IMPL close** (fixtures **119** vs a real 120; stat surface **1205** vs the contract's **1207**; *"next-free reference port is 10450"* vs the landed 10118). ⚠️ **DO NOT SOURCE ANY COUNT FROM §Project — anchor on §Current, and do NOT "fix" §Project.**
- ⚠️ **`test/differential/harness_test.go:208`'s port inventory is STALE** — the true set reaches **10449** and includes bands the comment omits. **A false inventory a future allocator would trust.**
- ⚠️ **The two `//nolint:gosec` directives in `body.go` are INERT** — `.golangci.yml` is `disable-all: true` with **9** linters (`govet errcheck staticcheck unused ineffassign gofmt goimports misspell revive`), golangci-lint **v1.64.8**. ⚠️ **`gosec`, `nolintlint`, `depguard` and `forbidigo` are NONE of them.** `misspell` locale **US**.
- ⚠️ **The xDS config-seam cycle guard is NOT AUTOMATED ANYWHERE** — 13 `go list -deps` hits, all documentation, plus one **unexecuted comment**. It cannot trip on this row regardless: `internal/filter/hcm/h2` is a leaf and `internal/filter/http/router` already imports it.
- ⚠️ **`internal/filter/network/rbac/rbac.go:50` contains the token `F2`** as an unrelated **phase-26 fork label**. **Do not use `F2` as a grep anchor in that package.**

### ⚠️ STANDING METHOD NOTES

1. **⚠️ RE-RUN ALL THREE SENTINEL CHECKS YOURSELF AND RECORD THE ACTUAL OUTPUT; DO NOT PREDICT IT.** ⚠️ **CHECK (3) IS SILENT — RUN THE DOCTORED-COPY NC EVERY TIME.**
2. **⚠️ EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION, NOT BY REVIEW.** Lineage: phase-81 B14/S22/P14/I17; phase-82 B26/S23/P14/I17; phase-83 B21/S23/P19/**I37**; phase-84 **B22/S26/P31**. ⚠️ **AND A REFUTATION IS A CLAIM TOO** — at the phase-84.1 PLAN one agent's REMEDY did not survive (widening `captureSW`, when `captureH2Writer` already had the field), while its DIAGNOSIS did. ⚠️ **AND THE CONTROLLER'S OWN FRAMING CHANGED MID-STAGE** — I set out to restate the SPEC's *"crosses at the floor"* and the leg-scoped enumeration showed that sentence is true of the ROW and false of the LEG. **A controller brief is a claim too — third consecutive stage to record it.**
3. **⚠️ BEFORE ANY LINE-ADDING EDIT TO A HEAVILY-CITED DOC, ENUMERATE THE CITES YOUR INSERT WOULD SHIFT — AND DO NOT CARRY A WHOLE-FILE COUNT OF A PATTERN YOUR OWN PROSE SPELLS.** That species has fired **FIVE** times.
4. **⚠️ LINE ANCHORS DRIFT; PHRASE AND SYMBOL ANCHORS DID NOT.** Corrected at this stage: `recvTrailingHeaders` is **`:138-166`** (not `:138-165`) — **use the SYMBOL** · `RunEncodeData` is at **`:582`** (not `:583`) · `assertThreshold`'s `Tests == 0` is at **`:310`** · `StreamWriter.WriteHeaders`'s method is at `stream.go:32`. ⚠️ **`*RootVM.dispatchHttpCallResponse` DOES NOT EXIST** — the symbol is `(*RootVM).handleHttpCallResponse`. ⚠️ `headerMapForType` is a METHOD with an `*abiCallbacks` receiver — grep the SYMBOL. **RE-VERIFY EVERY `.go` AND `.md` LINE CITE BEFORE RELYING ON IT.**
5. **⚠️ A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX.** The index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. **Still live:** `reference_sds_init_fetch_timeout_dial_budget_flake` (**TWO packages**) · the pre-existing `internal/cluster` `-race` outlier · `internal/httpclient TestOptions_ZeroValue_NoOpDefaults` · the driver-receiver port race (**42**-fixture behavioural roster).
6. **⚠️ CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE — and NEVER tear down a container this session did not create**, by name or otherwise. ⚠️ **A live reference probe needs a BRIDGE network — `--network host` is useless — and a FRESH CONTAINER PER ARM**, because `docker logs` accumulates. ⚠️ **Tear down BY NAME; never `prune`, never an `ancestor=`/image filter.** ⚠️ **`kill` on a `go run` parent leaves the COMPILED CHILD holding the socket.**
7. **⚠️ MACHINE-STATE CHANGES DELIBERATELY LEFT IN PLACE (from phase 82): `rustup toolchain 1.94.0-x86_64-unknown-linux-gnu` + its `wasm32-wasip1` target**, plus a `1.96.0` toolchain. `rustup default` unchanged (`stable`).
8. **⚠️ A `golangci-lint` NC CAN FAIL TO FIRE AND READ AS "THE GATE DISCRIMINATES."** A British-spelled **identifier** produces empty output / exit 0 (`misspell` does not flag CamelCase); only British spellings in **prose** fire — and one did, on an agent's own comment, at this stage. **And a `typecheck` failure short-circuits `misspell` entirely** — a *compiling* defect is required to exercise the style linters.
9. **⚠️ `rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** (`reference_harness_exit_code_is_not_command_exit_code`). Use `out=$(…); rc=$?` or `PIPESTATUS`.
10. **⚠️ A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL.** At this stage an agent's capture edit had a one-tab indentation mismatch, `go build` PASSED, and the probe printed an empty trailer set — **indistinguishable from "the capture doesn't work".** Only re-grepping the file found it.
11. **⚠️ A NEGATIVE CONTROL MUST FIRE ON THE *SAME PATTERN* AS THE GATE (broken-gate shape 34).** SPEC §9's arm-census NC used a BROADER pattern than the census it controlled: the anchored form gives 9 lines/3 files, the unanchored 41/9. **It fires either way, so it READS as proof and proves nothing about the actual selector.**
12. **Counts at this tip (re-run MECHANICALLY in your worktree; never copy them).** ⚠️ Use GNU `command grep` or `git grep` repo-wide — but ⚠️ **NOT `command grep` inside `xargs`.**
    fixtures **120** (next-free **0119**, reference port **10119**) / fuzzers **55** in **48** files, all under `internal/` / phase dirs **125**, of which **37** carry `REVIEW.md` and **88** do not / **BackendKind tail 38** over **39** declarations (do NOT "fix" 38 to 39) / `DECISIONS.md` **17990**, **305** headings, tail **ADR-0306 COMPLETE**, next-free **ADR-0307** / `ROADMAP.md` **234 lines / 116 data rows** / `STATE.md` **64** / `STATE_HISTORY.md` **462** / `BEHAVIOR_CONTRACT.md` **5955** / production stat call sites **208 / 36 files** / `ActionResponse` **19 files / 22 sites (18 non-test)** / `H2Response` **2 files / 15 lines** / test functions **211/77/68** (`Test|Fuzz|Benchmark`) or **210/75/68** (`Test` only) — ⚠️ **STATE THE MATCHER**.
    ⚠️ **STILL CONTESTED, SO NO NUMBER IS CARRIED:** the `STATE_HISTORY.md` archive-gap total · production `stats.IsValidName` GUARD SITES · the stat-surface total **1207** (DOC-SOURCED; **only the DELTA is ever asserted**) · the `ROADMAP.md:<line>` cite count · `allCallbacksNoOp` occurrences · **the loose `PROPOSED` matcher's whole-file count**.
    ⚠️ **THE PORT OCCUPANCY IS 146 DISTINCT PORTS ACROSS 19 BANDS** — re-derived from every driver's `ReferenceListenerPort()` **body**: 117 of 120 fixtures bind ≥1 port, **184 slots, range 10000-19172**. **The convention is `10<fixture index>`**, stated in a landed comment at `0118/driver/driver.go:29-31`; **"10443 = TLS" is a leaked mnemonic, not a rule.** ⚠️ **Collisions are a DOCUMENTATION concern only** — `ReferenceListenerPort` is container-internal, the runner dials a randomly mapped host port, **31 ports are already shared**, and `t.Parallel` count is **0**.


## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — take it from `git rev-parse master`, **NOT from a SHA quoted in this file**. ⚠️ **AND THE BARE SUBJECT-LOCATE GREP IS CONTAMINATED BY THIS FILE'S OWN PROSE ABOUT IT** — `git log` greps the whole commit message and router commits discuss the phase in their BODIES, so **the sentence warning about the grep satisfies the grep.** ⚠️ **USE THE SLUG-ANCHORED FORM**, which is stage-exact:
```sh
git log --grep '^phase 84 (grpc-unary-response-trailers)' --format='%h %s'  # 3 at this tip: BRAINSTORM + SPEC + PLAN-84.1. YOUR stage adds the 4th.
git log --grep '^phase 83 (wasm-pause-arm-leak)' --format='%h %s'           # 4 — the CLOSED phase, use it as a FIRING CONTROL
```
Measured with both controls: an invented slug returns **0**, the CLOSED phase-83 form returns **4**. **Take the NEWEST line (`| head -1`) and verify the count before relying on it.** Subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** (`reference_parallel_subagents_private_scratch`) and must tear down docker containers **BY NAME** (`reference_parallel_agents_shared_machine_namespaces`). ⚠️ **THERE ARE TWO RESERVED PORT BANDS — band private ports outside BOTH `20000-31007` (subject blocks) AND `11000-14999` (backends), and also clear of the static fixture ports, whose REAL extent is 19 bands from 10000 to 19172.** The `42000-45799` band has been used safely (phases 80-84.1). ⚠️ **If two agents share ONE worktree they must stage by EXPLICIT PATH** and **must NOT run an unscoped `git restore`**. ⚠️ **PREFER ONE WORKTREE PER AGENT OFF A COMMON BASE.** Per-task `gofmt` + `golangci-lint` on touched packages (`feedback_pertask_gofmt_lint`). ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch, and note `-o` precedes the package path.**
**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. ⚠️ **ASSUME IT FIRES EVERY SESSION — it has been treated as live for THIRTY-SIX consecutive sessions and it FIRED, OBSERVED, at the phase-84.1 PLAN** (`Shell cwd was reset to /home/esa/git/envoy-go`, repeatedly). **Use `git -C <abs-worktree-path>` for every git command, prefer absolute paths over `cd`, put any needed `cd` FIRST in a compound command, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — `git add` ALONE WILL NOT STAGE IT. USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN.** At the phase-84.1 PLAN five agents built two independent production patches, a validation prototype, live docker reference probes and seven injected break arms, ran them, reverted them, and each confirmed `git status --porcelain` = 0 lines. **Every agent worked in a DETACHED worktree precisely so a failed revert could not dirty a branch — copy that.** ⚠️ **Restoration is verified by sha256, never by eye**, and every break arm's injection site had its occurrence count asserted `== 1` before writing.

⚠️ **THE ARCHIVE-ABSENCE GUARD — USE THE ROBUST FORM. *BOTH* PRESCRIBED ALTERNATIVES ARE DEMONSTRATED FAIL-UNSAFE ON REAL DATA.**

1. **A guard anchored on the PHASE NAME self-clears.** `STATE.md`'s `last-updated` field NAMES the entry it just evicted, so a name-anchored grep returns 1 and reads as *"the eviction did not happen"*. **Anchor on the BULLET, not the name.**
2. ⚠️ **THE COLON-ANCHORED BULLET FORM IS FAIL-UNSAFE** — it misses every entry whose label carries a parenthetical between the field name and the colon, reading **0 on an entry that IS present**. Since the guard exists to prove **ABSENCE** before appending, that **AUTHORIZES A DUPLICATE APPEND**.
3. ⚠️ **AND THE "TOLERANT" NON-ASTERISK FORM IS *ALSO* FAIL-UNSAFE** — re-confirmed live at the phase-84.1 PLAN on a REAL still-present entry.

**USE THE ROBUST FORM: anchor on the bullet and allow ANY run of characters before the backtick-quoted target**, and ⚠️ **CORROBORATE IT WITH A WHOLE-FILE FIXED-STRING SEARCH (`grep -cF`)**. Cross-product on **FOUR REAL** targets at the phase-84.1 PLAN:

| target | raw `grep -cF` | OLD colon form | "tolerant" non-asterisk | **ROBUST** |
|---|---|---|---|---|
| an ANNOTATED-label entry that IS present | **1** | **0 — FALSE ABSENT** | 1 | **1** ✅ |
| a SECOND annotated entry that IS present | **1** | **0 — FALSE ABSENT** | **0 — FALSE ABSENT** | **1** ✅ |
| the eviction target | 0 | 0 | 0 | **0** ✅ |
| an invented target | 0 | 0 ✅ | 0 ✅ | **0** ✅ |

**Coverage over the 179 `prior active-phase` bullets: colon 163 · tolerant 176 · ROBUST 178.** Cause: **7 of 16** annotated labels carry a literal `*`, defeating `[^*]*`; a `[^:]*` variant is worse (**13 of 16** carry an extra `:`). **ROBUST does not over-fire** — a 16-occurrence prose string returns 0 under it, because the target must be backtick-quoted on a bullet line.

⚠️ **NC IT WITH A *REAL* TARGET.** A probe using an **invented phase name** reads 0 on both arms — non-discriminating, and it looks like agreement (`reference_probe_input_is_a_claim`). ⚠️ **AND DO NOT SPELL THE GUARD'S REGEX INSIDE AN ARCHIVE ENTRY LABEL** — a quoting entry self-clears. **Describe the guard in prose in the label; keep the regex out of the archived file.**

⚠️ **AND VERIFY THE ARCHIVE EDIT IS STRICTLY APPEND-ONLY**, not merely "looks right": `git diff --numstat` must show `N 0` (zero deletions) **and** the base file must be a byte-exact **prefix** of the new one.

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY** (TRUSTWORTHY; ADR-0288 discipline — ⚠️ **anchor greps on `^- **<field>`**; all seven fields verified singleton and §Recent verified at its five-entry cap at the phase-84.1 IMPL close). ⚠️ **§Project counts SELF-CONTRADICT §Current and are TEN-PLUS PHASES STALE** — do not anchor on them and do not fix them.
2. `docs/envoy-go/phases/84-grpc-unary-response-trailers/PLAN-84.1.md` + `PROGRESS.md` — **the LANDED SIBLING LEG. Read them as HISTORY / CONTEXT, NOT as your build order — the 84.1 IMPL is DONE.** §4 (the five-file edit surface), §5 (the break roster) and `PROGRESS.md`'s `## IMPL-84.1` section record what SHIPPED and what 84.2 inherits (the frame-sequence parity notes, the response-header conduit, the vacuous-stats warning). **YOUR job is to AUTHOR `PLAN-84.2.md`** — the differential fixture `0119-grpc-unary-trailers` (reference port 10119, `GRPCHealthResponder` BackendKind 34). `SPEC.md` and `BRAINSTORM.md` carry the charter. ⚠️ **RE-DERIVE ANYTHING YOU TAKE** — this lineage has refuted its predecessor at every stage without exception.
3. `docs/envoy-go/ROADMAP.md` — **§Schema first** (`:18` carries BOTH the update-in-place invariant AND *"Sub-phases get their own rows"* on ONE line — ⚠️ **practice DIVERGES; zero dotted rows exist above 32.2, and rows 60/61 record their legs in PROSE**), then the SIX deferred-candidate paragraphs, then the family headings. ⚠️ **ROW 84 STAYS `in-progress`; `want` STAYS 116.**
4. `BOOTSTRAP_PROMPT.md` — **at the REPO ROOT, no leading slash.** §6.1 `:285`, §6.2 `:294`, §7.3 `:346-351`, §7.5 `:357-367`. ⚠️ **Not the 1024-line file under `docs/superpowers/plans/`, which is the GENERATOR PLAN and is content-divergent.**
5. `docs/envoy-go/DECISIONS.md` — ⚠️ **ADR-0306 is COMPLETE** (the 84.1 IMPL completed it in place; ¶4's "ten" was corrected to NINE). **Do NOT re-complete or re-edit it — `DECISIONS.md` is append-only.** Read it as CONTEXT. Still-relevant context for 84.2: **ADR-0058** (`:1987-2033`, NARROWED not superseded) · **ADR-0052 `:1821`** (the contract-edit vehicle, already used at 84.1) · **ADR-0051** (the h2spec pin — ⚠️ **its §2 is factually wrong**) · **ADR-0288** (roll-IN-PLACE and the archive rule) · **ADR-0273** (the trailer match-arm boot-reject — **do not disturb it**) · **ADR-0044/0106-as-used**. ⚠️ **84.2 consumes NO new ADR unless its own SPEC/PLAN identifies one** — next-free is **ADR-0307**. ⚠️ **Headings are NOT in numeric order — bound any ADR block by "the next `^## ADR-` heading of ANY id", and print the denominator.**
6. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — ⚠️ **the five falsified trailer statements were RECONCILED at the 84.1 IMPL (DONE), plus a `## HTTP/2` tail append (5900 -> 5955).** Read the `## HTTP/2` section as CONTEXT; **84.2 owes NO further contract reconciliation** unless its own fixture surfaces a new statement. ⚠️ **`:4291-4311` is NOT one block** (still true — do not edit `:4295`/`:4311`).
