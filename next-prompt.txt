START a fresh cold-start session for envoy-go — **the phase-36.1 (`load-balancer-ring-hash`) IMPL is DONE and LANDED on master; the next lifecycle step is the phase-36.2 SPEC** (the HTTP route `hash_policy` plane — the second leg of the CONSUMED 36.1/36.2 split; its own SPEC → PLAN → IMPL, each its own session). The substantive master tip is the phase-36.1 IMPL squash **`e8e8c48`** (`phase 36.1 IMPL: load-balancer-ring-hash — land RING_HASH, the 4th LB policy and first consistent-hash one (Ketama ringHashLB + the ADR-0232 seam's PICK-INPUT-half extension via a ctx-carried hash key + the tcp_proxy source_ip plane)`; pushed to `origin/master`; confirm via `git log --oneline -3`). The literal live master tip is a docs-only `next-prompt.txt` roll-forward above that substantive squash: the prior SHA-fill commit **`36340df`** (which named `e8e8c48` as the substantive tip and routed to the 36.2 SPEC) sits directly above `e8e8c48`, and THIS roll-forward commit sits above `36340df` (confirm via `git log --oneline -4`); the substantive predecessor below `e8e8c48` is the docs-only `fb59ec3` and below that the phase-36.1 PLAN squash `f44b12c`. The substantive squash `e8e8c48` carries the complete tcp-plane consistent-hash LB: `internal/cluster/hash.go` (xxHash64 + murmurHash2), `ringhash.go` (the Ketama `ringHashLB`), the widened `loadBalancer.Pick(hashKey,hasHash)` seam + `cluster.WithHashKey`/`hashKeyFrom` ctx-carry (ADR-0235), `cluster.HashSourceIP`, the manager `case Cluster_RING_HASH` + two-layer gate + 3 `ring_hash_lb.*` gauges (ADR-0236), the tcp_proxy `source_ip` hash plane, and the `0061-lb-ring-hash` differential fixture (subject affinity via the count ≡ 0 mod 16 modular invariant) + its deliberate-break liveness. STATE/ROADMAP advanced (active-phase → `phase 36.1 IMPL done`; ROADMAP row 36.1 leg done, 36.2 in-progress).

## What this session does — author the phase-36.2 SPEC

The phase-36.1 IMPL is COMPLETE. The next session writes the **phase-36.2 SPEC** (`superpowers:brainstorming` first IF the 36.2 design needs settling beyond the parent phase-36 BRAINSTORM/SPEC §3.0 split charter, then `superpowers:writing-plans` is the PLAN step AFTER the SPEC). 36.2 = the **HTTP route `hash_policy` producer plane**: the HTTP router computes a ring_hash key from a per-route `RouteAction.hash_policy` (header / cookie / query-parameter / source_ip / connection-properties specifiers) and threads it via the ALREADY-LANDED `cluster.WithHashKey(ctx, key)` seam into `AcquireH1`/`Dial` → the ALREADY-LANDED `ringHashLB.Pick`. 36.2 is a PRODUCER-only leg — the ring policy, the seam, the gate, the gauges, and `HashSourceIP` are all landed at 36.1; 36.2 adds the HTTP-side key producer + its differential fixture(s). The PLAN estimated ~140 production LoC for the HTTP plane (anticipated NO ADR-0045 split; re-check at the 36.2 PLAN).

## Counts at master tip (phase 36.1 IMPL done — confirm at the 36.2 SPEC baseline)

- stat surface **1119** (DOC count in BEHAVIOR_CONTRACT.md; +3 `ring_hash_lb.*` gauges landed at 36.1). 36.2 stat delta: TBD at the SPEC (likely 0 — the HTTP producer reuses the existing ring_hash_lb gauges + cluster cx/rq stats; confirm against the live reference).
- differential fixtures **63** (tail `0061-lb-ring-hash`; `ls -d test/fixtures/[0-9]* | wc -l`). 36.2 adds ≥1 HTTP-route hash_policy fixture.
- fuzzers **42** (tail `FuzzThriftDecode`); BackendKind tail **33** (`TCPThriftResponder`).
- DECISIONS tail **ADR-0236** (next-free **ADR-0237**). ADR-0235 (seam) + ADR-0236 (ring_hash policy) are LANDED in full; 36.2 may add its own ADR for the HTTP hash_policy producer.
- exported-surface at 36.1: `cluster.WithHashKey` + `cluster.HashSourceIP` (both additive; the HTTP router at 36.2 REUSES these — header/cookie keys hash through the cluster-package surface or a new sibling helper).
- reference pin: `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227). Toolchain: Go 1.26.x; golangci-lint 1.64.8 (ADR-0009); go-control-plane `/envoy` v1.32.4 (ADR-0008).

## Read first (in order)

1. THIS FILE — the post-phase-36.1-IMPL cold-start (next: the phase-36.2 SPEC).
2. `docs/envoy-go/phases/36-load-balancer-ring-hash/SPEC.md` — the parent phase SPEC; **§3.0 the CONSUMED 36.1/36.2 split** (the 36.2 charter — the HTTP route hash_policy plane) + §3.2 the seam (now landed) + D-S36-7 (36.2 out of scope for the 36.1 PLAN).
3. `docs/envoy-go/phases/36-load-balancer-ring-hash/PLAN.md` + `PROGRESS.md` — the AS-BUILT 36.1 (the landed seam + ringHashLB + the source_ip producer the HTTP plane mirrors).
4. `docs/envoy-go/STATE.md` — active-phase `phase 36.1 IMPL done`; the lifecycle → the 36.2 SPEC routing; the counts.
5. `docs/envoy-go/ROADMAP.md` — row 36 (36.1 leg done; 36.2 leg in-progress; flat family row per ADR-0106; Load-balancing family OPEN, 5 candidates remain).
6. `docs/envoy-go/DECISIONS.md` — **ADR-0235** (the seam extension) + **ADR-0236** (the ring_hash policy) — the durable assets 36.2 builds on; + ADR-0232/0219 (the ctx-carry seam) + ADR-0106/0045/0044/0052/0080.
7. The landed code the 36.2 producer threads into: `internal/cluster/{cluster.go (WithHashKey/hashKeyFrom + Dial/AcquireH1 threading), ringhash.go, hash.go (HashSourceIP)}` + the HTTP router (`internal/filter/http/router/...` — the AcquireH1 consumer that 36.2 makes hash-aware) + the tcp_proxy producer precedent `internal/filter/tcpproxy/filter.go` (the source_ip parse + WithHashKey stuffing 36.2 mirrors on the HTTP side).
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_execution_style`, `feedback_git_worktrees`, `feedback_subagents_no_push`, `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_path_targeting`, `feedback_subagent_worktree_detach`, `reference_differential_break_protocol_count1`, `reference_differential_asserter_dispatch`, `reference_differential_run_selector`, `reference_differential_band_sigma_margin`, `reference_differential_hash_key_cross_side_infeasible` (the HTTP route hash_policy can use a REQUEST-HEADER key for a cross-side-reproducible differential — unlike source_ip; see the memory), `reference_docker_probe_bridge_network`.

## Honor throughout

- The lifecycle is BRAINSTORM → SPEC → PLAN → IMPL, each its own session. The next session is the phase-36.2 **SPEC** (settle the HTTP route hash_policy producer design against the live reference; resolve the specifier set [header/cookie/query/source_ip/connection-properties] + which to support vs DEPARTURE-reject; the differential fixture shape — a request-header key is cross-side-reproducible per `reference_differential_hash_key_cross_side_infeasible`).
- `feedback_execution_style` (subagent-driven execution at the IMPL) · `feedback_git_worktrees` (work in a worktree) · `feedback_subagents_no_push` + `feedback_push_to_origin` (controller squash-merges + pushes at stage-close) · `feedback_pertask_gofmt_lint`.
- ADR-0106 (flat family row — the 36.2 leg flips its own portion) · ADR-0045 (the split-gate — re-check at the 36.2 PLAN; anticipated NO split) · ADR-0044 (ADR bodies at the IMPL, in-place) · ADR-0052 (the atomic-landing six-gate) · ADR-0080 (byte-stable parse-reject wording) · ADR-0227 (the contrib reference image; live probes per `reference_docker_probe_bridge_network`).
