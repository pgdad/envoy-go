=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

**THERE IS BANKED MID-LIFECYCLE WORK AT THIS TIP.** Phase 81 (`stats-name-charset-guards`) was **CHARTERED and BRAINSTORMED at 2026-07-31**; **row 81 is `in-progress`**. ⇒ **This session performs the phase-81 SPEC.** Do NOT self-pick a new subject — advancing the banked work is the more defensible move, per the standing directive.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=113 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **`want=113` IS A LIVE FIGURE, NOT A CONSTANT.** It is the ROADMAP data-row count. **A row-adding phase MUST bump it in the same commit that adds the row** — otherwise every later session gets `GATE FAIL: examined 114 data rows, expected 113`. That noise is deliberate: a wrong denominator must be loud, because a silent one is how check (1) failed in the first place. **The phase-81 BRAINSTORM bumped 112 -> 113 when it chartered row 81.** ⚠️ **YOUR SPEC ADDS NO ROW, SO IT MUST LEAVE `want` AT 113** — as must the PLAN and the IMPL.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. When you open a new family, add its row-summary slug here.

**SENTINEL RE-RUN MECHANICALLY AT THE PHASE-81 BRAINSTORM, with FIRING negative controls. IT DOES NOT FIRE. `stop` was NOT created; `ls stop` => `No such file or directory`, and it MUST NOT be created.** ⚠️ **Re-run it yourself anyway and record what YOU see** — a prior run's result is not evidence for the current tip. ACTUAL output, recorded rather than predicted:
- **(1) prints `NOT DONE: row 81`** at `want=113` — correct; the row is `in-progress` and will stay so until the IMPL. NCs, ALL fired: `want=112` => `GATE FAIL: examined 113 data rows, expected 112` (**proving the bump was REQUIRED**); row 81 doctored to `done` on a scratch copy => **SILENT**; row **62** doctored on that same copy => `NOT DONE: row 62` (so the check still discriminates when otherwise silent).
- **(2) FIVE — `:191 :201 :211 :217 :225`** (long form at the first four, the Operational-tooling short form at the last) — **UNCHANGED**. **The twenty-eighth consecutive phase at which check (2) did not go down. STATED, not forecast.**
- **(3) `NEVER OPENED: gRPC` and `NEVER OPENED: WASM`** — unchanged. NCs: an invented slug => `NC NEVER OPENED: ZZZ-nonexistent`, while the REGISTERED slug `Observability` correctly printed nothing.
- Input measured at **228 lines / 112 data rows / 13** bare `candidates:` hits PRE-EDIT (**229 / 113** post-edit), so an empty result could not read as a zero result.

⚠️ **THE LEAK CHECK IS LIVE AT ANY STAGE THAT WRITES A ROADMAP CELL.** The rule: **NEVER WRITE A SENTINEL'S OWN MATCHER STRING INTO A FILE THE SENTINEL GREPS.** The sentinel greps `docs/envoy-go/ROADMAP.md` — writing these strings *here* is safe; writing them *there* is not. The leak is proven to **SILENCE CHECK (3) BY MENTION**, and it was **REPRODUCED LIVE AGAIN AT THE PHASE-81 BRAINSTORM**: doctoring row 81's cell to say `gRPC-family row` made `gRPC` **stop printing** while `WASM` kept printing. **`grep` cannot tell a mention from a use.** ⚠️ **A SPEC normally writes NO ROADMAP cell — if yours does not, say the check is INAPPLICABLE rather than "passed".**
```sh
# the row's cell only, not the whole file
awk -F'|' '/^\| *81 /' docs/envoy-go/ROADMAP.md > "$SCRATCH/row81.txt"
grep -coE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' "$SCRATCH/row81.txt"  # want 0
grep -oiE '(HTTP-filters|Network-filters|Load-balancing|Upstream-robustness|Observability|Operational-tooling|HTTP/3|gRPC|xDS|Runtime|WASM)-family row' "$SCRATCH/row81.txt" | sort -u
# ^ every slug printed MUST already be registered elsewhere in ROADMAP.md. Row 81 prints `Observability-family row`
#   ONLY, which is registered 52x elsewhere -- a USE, not a mention.
```
⚠️ **ROW WELL-FORMEDNESS: THE GATE MUST BE A DISJUNCTION.** Re-executed at the phase-81 BRAINSTORM over all **113** rows: **ARM-A (escape-aware `NF!=8`) catches 57/69 ONLY; ARM-B (escape-aware trailing-piece) catches 78 ONLY. Neither arm alone suffices**, and **row 81 is flagged by neither**. ⚠️ **NEW THIS STAGE: naive `NF==8` is worse than recorded — besides passing malformed row 78, it throws FIFTEEN FALSE POSITIVES** (rows `16 17 18 18.1 19 20 22.1 24.2 25.1 43 48 49 55 57 62 69 74`, whose summaries legitimately carry escaped `\|`). It is unusable in **both** directions. The compensating-defect shape was reproduced on a **synthetic row 81** carrying both defects: ARM-B fires, ARM-A stays silent, naive `NF==8` PASSES.

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files --hidden -I --exclude-dir=.git …` in ugrep-compatible mode. **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. The delta is exactly this file, reproduced at four separate tips:
```sh
grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort         # wrapped: ABSENT
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                    # direct access WORKS
```
⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING THE TWO ARMS.** The wrapped arm prints bare paths while GNU prints `./`-prefixed ones, so a naive `comm` reports **every file as "only in GNU"** and the real delta is invisible. ⚠️ **Print the FILE NAMES, never a count.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** — it reports `next-prompt.txt` as *"not ignored"* (exit 1) because **git exempts TRACKED files from `.gitignore`** while ugrep does not. **Reliable forms: `command grep -r`, `git grep`, an explicit file list, or a recursive grep handed the FILE path directly.**

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`.** Every document from phase 75 forward recorded its blind spot — **109 data rows / 105 matched / FOUR misses** at an **em-dash** in an "after" cell, a **DOT** in the slug `http-1.1`, and two **LETTER-suffixed** ids `28.1a`/`28.1b` — and every one of them called it *"currently HARMLESS, since all four missed rows are `done`."* ⚠️ **Nobody fired the unsafe arm until the phase-77 PLAN. EXECUTED on scratch copies: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** ⚠️ **DO NOT restore the regex form.**

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

The old check (2) matched ONLY the long phrase and could not see the **Operational-tooling** family's three genuinely un-chartered items, recorded in the **SHORT** form. Fixed at the phase-77 BRAINSTORM, at the MATCHER. ⚠️ **Run the union form above, never one half of it.** ⚠️ **A ONE-ARM STRIP IS NOT AN NC FOR CHECK (2)**: `sed 's/deferred candidates://'` moves the union count **5 -> 4, not 5 -> 0**, because the long form does not contain the short substring at all.

⚠️ **THE DOCUMENTED TRAP'S STATED MECHANISM IS REFUTED, WHILE THE TRAP IS REAL.** `reference_sentinel_deferred_sentence_live_vs_historical` says a naive `grep -c 'candidates:'` inflates *because of* the `candidates were:` recaps. It does inflate — but **not for that reason**: `candidates were:` does not contain that substring **at all**. The inflation comes from **ROADMAP table rows carrying BRAINSTORM prose**. **Cite the command; do not re-inherit the explanation.**

⚠️ **AND THE MEASURED RECORD: CHECK (2) HAS NEVER GONE DOWN** across ~28 phases (history `0 -> 1 -> 3 -> 4 -> 5`). => **the candidates sentence is a WINDOW onto a larger deferred backlog, not an inventory of it.** ⚠️ **A stage that predicts a decrease here is repeating the phase-73 error. State what your row narrows — usually nothing — rather than forecasting.** **Row 81 narrows NOTHING: the charset candidate sits on no family's deferred sentence.**

==============================================


## STATUS AS OF 2026-07-31: PHASE 81 (`stats-name-charset-guards`) IS **CHARTERED AND BRAINSTORMED**. ROW 81 IS `in-progress`. THIS SESSION PERFORMS THE **SPEC**.

**What the phase-81 BRAINSTORM landed.** Docs-only, base master **`4f8e159c`**, branch `phase-81-brainstorm`. **ZERO production `.go`, ZERO test `.go`.** New: `docs/envoy-go/phases/81-stats-name-charset-guards/{BRAINSTORM.md,PROGRESS.md}`. Modified: `ROADMAP.md` (row 81 `in-progress`, **228 -> 229 lines / 112 -> 113 data rows**), `STATE.md` (§Current rolled IN PLACE; §Recent add + evict), `STATE_HISTORY.md` (**430 -> 432**), this file. **`BEHAVIOR_CONTRACT.md` and `DECISIONS.md` BYTE-UNTOUCHED — verified at 0 diff bytes each, with a firing NC on `ROADMAP.md` at 31494 bytes.**

### ⚠️ WHAT THE ROW IS, AND WHY IT WAS PICKED

**A config-triggered, REQUEST-TIME, un-recovered process crash — PROVED BY EXECUTION at the BRAINSTORM with a firing NC**, not inherited:
```
NC OK: policy name "allow_admins" registered cleanly
CONFIRMED request-time PANIC: stats: invalid metric name:
  "http.myhcm.rbac.policy.allow-admins.allowed" (must match ^[a-zA-Z_]([a-zA-Z0-9_.]*[a-zA-Z0-9_])?$)
```
`internal/rbac/perpolicy.go:25` lazily registers `<base>.policy.<policyName>.<suffix>` on the **first matching request**, guarding `policyName` for **emptiness only**; `git grep -n 'IsValidName' -- internal/rbac/` prints **nothing**; `internal/stats/registry.go:115-119` **PANICS**. An idiomatic hyphenated Envoy policy name plus `track_per_rule_stats: true` **boots green and kills the process on first traffic**. There is **no `recover()` in the request path** — production `recover()` lives only in `internal/boot/boot.go` and the lua/wasm guest-VM wrappers. **Boot-time validation structurally cannot catch it**, because the name does not exist until a request matches.

**The row closes the EIGHT unguarded token sources** across six packages: RBAC policy name (`internal/rbac/perpolicy.go:25`, **request-time**) · mongo `stat_prefix` (`internal/filter/network/mongoproxy/config.go:81`) · zookeeper `stat_prefix` (`internal/filter/network/zookeeperproxy/config.go:169`) · wasm `PluginConfig.name` (`internal/filter/http/wasm/compiled_config.go:1048`, boot **and** request via per-route) · HTTP-rbac `rules_stat_prefix` and `shadow_rules_stat_prefix` (`internal/filter/http/rbac/rbac.go:224,225`) · `compressor_library.name` (`internal/filter/http/compressor/compressor.go:527`) · ratelimit `stat_prefix` (`internal/filter/http/ratelimit/compiled_config.go:467`). **All twelve of these cites were controller-verified at `4f8e159c`, not taken from a brief.**

⚠️ **THE EMPTY-SEGMENT HOLE IS EXPLICITLY OUT OF SCOPE.** All fifteen incumbent guards accept an interior `..` (`IsValidName("a..b") = true`, because `NamePattern` permits it). Phase 80 closed this for `sds.` only. Folding the retrofit in moves the estimate from **~900-1400** to **1800-2600** net `.go`, across the ~1500 split trigger. **It is deferred to a named successor row. Do not silently re-absorb it.**

### ⚠️ WHAT THIS SESSION DOES — the phase-81 SPEC

**One stage per session.** Write `docs/envoy-go/phases/81-stats-name-charset-guards/SPEC.md`, append the `# SPEC record` half to that directory's `PROGRESS.md`, roll `STATE.md` §Current **IN PLACE**, and roll this file. **`ROADMAP.md` row 81 STAYS `in-progress`** and `want` STAYS **113**. **ADR-0303's §Context lands at the SPEC** (ADR-0044-as-used); §Decision/§Consequences land at the IMPL. `BEHAVIOR_CONTRACT.md` is normally untouched at a SPEC — say so explicitly rather than claiming a gate.

**⚠️ DISPOSE D-81-HELPER FIRST. IT IS THE ROW'S ONLY LOAD-BEARING FORK.**
- A **shared `internal/stats` helper** honours the project's own *"one definition, no drift"* doctrine (`internal/stats/dynamic/dynamic.go:40-42`) — but **touching `internal/stats` makes the FULL 120-fixture differential AND an explicit h2spec run MANDATORY for every later stage of this row** (`internal/stats` links into `cmd/envoy-go` at `test/differential/harness.go:240`, `:594` and `test/conformance/h2spec/h2spec_test.go:210`; **h2spec is a FOURTH consumer NOT covered by `./test/differential/`**). Budget **~400-430 s** per green attempt, `-race` a SECOND run.
- **Per-package inlining** (the `internal/boot/boot.go:279-290` shape) keeps `internal/stats` byte-untouched and the gate per-package — but nine call sites inlining the same loop is the drift the doctrine forbids.
- **Decide it with the gate cost priced in. Do not discover it at the IMPL.**

**The other SPEC-level questions:** **D-81-WORDING** (eight byte-stable reject messages across six packages; incumbent wordings are not uniform) · **D-81-F-SITE** (⚠️ the request-time source **cannot return an error to a caller** — the guard must move to config-compile time in `internal/rbac/rbac.go`, or the emission must degrade to a skip; these are **observably different behaviours**, pick one deliberately) · **D-81-SANITIZE** (⚠️ **a landed doctrinal contradiction**: phase 80 records sanitizing as *"FORECLOSED by ADR-0065's own Context"* at `internal/boot/boot.go:264-266`, yet `internal/listener/manager.go:352` `normalizeAddr` **sanitizes** `:`->`_` and `.`->`_`. **Both are landed. Reconcile them or the row will re-litigate it mid-execution.**).

**Anticipated surface (BRAINSTORM figures — re-derive, do not inherit):** **8-11 tasks**, **~900-1400 net `.go`**, **ZERO new differential fixtures** (these are envoy-go-strict rejects where the reference **ACCEPTS**; a cross-side boot-reject dir is asymmetric by construction, and `BEHAVIOR_CONTRACT.md:931` says verbatim *"All boot-rejects are SUBJECT unit tests (no fixture dirs)"* — **controller-verified**), stat-surface delta **+0**, **+0** fuzzers / packages / go.mod modules / BackendKind, **ONE** ADR (**ADR-0303**). ⚠️ **No existing fixture is expected to red** (all 111 distinct fixture-YAML `stat_prefix` values are charset-clean; wasm plugin names are `plugin_a`/`plugin_bootreject`/`plugin_listener_default`; `compressor_library.name` is `text_optimized`; RBAC policy keys are `admin_users`/`p_allow`/`public_paths`) — **but that is ANTICIPATED, not proven; the differential was not run at the BRAINSTORM.**

### What matters for the NEXT session, in order

1. **⚠️ RE-RUN ALL THREE SENTINEL CHECKS YOURSELF AND RECORD THE ACTUAL OUTPUT; DO NOT PREDICT IT.** Create `stop` if and ONLY if all three print nothing. **All three print, so it cannot fire.** ⚠️ **Your SPEC adds no row — leave `want` at 113 and say so.**

2. **⚠️ EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION, NOT BY REVIEW.** The lineage record is unbroken: the phase-80 SPEC refuted nine BRAINSTORM claims; its PLAN refuted **nineteen**; its IMPL refuted **sixteen**, four load-bearing. **The phase-81 BRAINSTORM refuted FOURTEEN router claims, five load-bearing** (§3 below). **Budget for finding more. A SPEC that refutes nothing has not looked.**

3. **⚠️ THE PHASE-81 BRAINSTORM'S FIVE LOAD-BEARING REFUTATIONS — carry these forward, they are now FACT and must not be re-inherited in their old form.**
   - **`ROADMAP.md:76` does NOT decline a WASM row — it is a CATEGORY ERROR.** The quoted sentence is real (verified verbatim; it occurs **exactly once** in the file) but reads *"the FINAL §9 **HTTP-filters**-family row"*. **`ROADMAP.md:218` is a SEPARATE `### WASM host family` heading** (`:220` — *"Own multi-phase sub-project: ABI, engine binding, proxy-wasm conformance"*). A WASM-host row is **not** an HTTP-filters row. What `:76` correctly kills is the *rider*. ⇒ **WASM is a LIVE, defensible candidate** (`wasm-host-shared-queue`, **11-15 tasks / ~1400-2000 net `.go`**, with five landed ADR-anchored forward-pointers naming the family by name).
   - **"The five existing rejects" is wrong by 3× — there are FIFTEEN** production `stats.IsValidName` guard sites. Census controller-re-derived; full table at BRAINSTORM §2.2.
   - **The failure mode is a DIRECT `panic`, not a nil counter.** `checkName` panics (`internal/stats/registry.go:115-119`); registration **never returns nil**. `reference_nil_stats_counter_inc_crashes_goroutine` is the **wrong mechanism** for this surface — **there is no nil to defend against**, so the remedy is a boundary guard, never a nil-check.
   - **`--mode validate`'s defect runs the OPPOSITE way to the record.** Not false-accept — **false-REJECT**, **3/3** against the pinned reference `envoyproxy/envoy:contrib-v1.37.2`: envoy-go `EXIT=1` on fixtures `0103`/`0108`/`0109` while the reference prints `configuration OK`, `EXIT=0` (control: non-SDS `0005` => both OK). Those three are **live, passing differential fixtures**. Two of the three messages say *"is not supported in phase 03"* — telling a Gateway-API integrator envoy-go cannot do SDS validation contexts **at all**, when phases 65/66 landed exactly that. The premise (`validate/validate.go:48-49` passes a literal `nil` provider) is CONFIRMED; **the consequence was recorded backwards.**
   - **The bind-retry fix as prescribed CANNOT WORK.** All 26 backend arms spawn `exec.CommandContext(ctx, "go", "run", …)`, so `cmd.Start()` starts the **toolchain** and returns `nil` on a port collision; the failure surfaces later at the child's `Listen`. A `startSubjectWithRetry` equivalent keys on a start error **that never occurs** — a backend retry must key on **readiness failure or child exit**.

4. **⚠️ NINE MORE BRAINSTORM REFUTATIONS, recorded so no later stage re-derives them.**
   - **The archive gap is 57, not 58** (join key `(phase-id, STAGE)`, slug discarded). Two extractor traps: **slug drift** (git `phase 42.1 (retries)` vs archive `(retries-and-hedging)` — 31 spurious misses) and **TWO distinct archive bullet shapes** (`STATE_HISTORY.md:428/:430` merge annotation and bullet onto ONE line, inflating a naive count to 59). **"36 for phases 67-75" CONFIRMED exactly.** **57 is a LOWER BOUND** — 87 pre-slug-convention subjects are invisible to the canonical extractor.
   - ⚠️ **The obvious archive guard SELF-CLEARS.** Without the `(slug)` parens, `phase 77 PLAN done` matches **`STATE.md`'s own confession prose** at `:18`/`:20`. `reference_sentinel_matcher_string_self_clears`, in a NEW file.
   - ⚠️ **CI IS A SHALLOW CLONE** — `.github/workflows/ci.yml` uses `actions/checkout@v6` with **no `fetch-depth`** ⇒ depth 1, killing any `git log`-derived expectation set. The git-free alternative was tested and **measurably fails** (380 stages from disk vs **209** from git, because split legs share a parent directory).
   - ⚠️ **`grep -c … => 0` IS TOOL-SPECIFIC, and the landed memory overgeneralized.** Only **`git grep -c`** prints nothing on zero matches. **GNU `grep -c` prints `0`** (and `path:0` per file on a list or `-r`), as does the ugrep wrapper. **Both still exit 1**, so an exit-code gate is wrong for either. Measured across six forms; **the memory has been corrected.**
   - **`-count=6` IS NOT A GATE** — ~40 min to test for the *absence* of a rare event; six clean runs occur with p ≈ **0.53** even if the bug is untouched. A deterministic squat-arm + firing NC costs ~7 min.
   - **"close-then-rebind" is the CURRENT characterization, not stale** — the landed `f2dd994a` helper comment uses exactly that wording. What IS stale is the ephemeral-range/loopback framing.
   - **`harness_test.go`'s "4 more" call sites contain ZERO backend arms** (`:143`/`:144` a unit test, `:315` the internal fallback, `:342` the pinning test). True as a grep count, false as a statement of work.
   - **The `mongo collection names` hypothesis is REFUTED** (3 of 3 wire-derived tokens already guarded at `codec.go:477/503/517`, denominator stated); **`wasm plugin names` is CONFIRMED**.
   - **gRPC is HARD-BLOCKED more deeply than recorded.** Trailers die at **SIX** seams; `router.ActionResponse` has **no `Trailers` field**; `internal/filter/hcm/codec.go:74` `writeH1Reply` rewrites `Content-Length` unconditionally so **H1 physically cannot emit trailers**; and the one trailers-free charter item (`grpc-timeout`) is doubly blocked because **envoy-go has no route-level timeout mechanism at all**. True first row ≈ **20-30 tasks / 2500-4000 net `.go`** plus an ADR reversing ADR-0058.

5. **⚠️ BROKEN-GATE COUNT IS EIGHTEEN — no nineteenth shape at the phase-81 BRAINSTORM, but TWO priors fired LIVE.**
   - **The compensating-defect cancellation fired on a SYNTHETIC row 81** this stage authored: with an unescaped inner pipe AND a missing terminator, **ARM-B fires, ARM-A stays silent, naive `NF==8` PASSES**.
   - **The self-clearing matcher fired in `STATE.md`** — a new file for that shape (see §4).
   - The eighteen carried forward, unchanged: two defects that CANCEL in the gate metric · an inert gate cell · a full-suite recipe without `-v` is VACUOUS · a sha256 roster desynced against a DELETED file · `gofmt -l` NEVER exits non-zero (gate on OUTPUT) · `go doc -all <A> <B>` swallows arg2 · a `+0 exported symbols` gate over an EMPTY package reds on a CORRECT tree · a RANGE gate cannot detect anchor drift · a roster's naive `[ -f ] || continue` exits 0 on a DELETED file · a count-only stat guard PASSES a build with BOTH names wrong · a `-run` no-match exits 0 with `[no tests to run]` · a `--- PASS` tally over a package with sibling tests exceeds the fixture denominator · a stat-delta claim cannot be discharged by guards scoped to another package · a stderr-VOLUME assertion passes on the hang · `golangci-lint` runs `misspell` with `locale: US` · a harness's exit code is not the command's · a GOLDEN ROSTER that omits the family under test (⚠️ **shape seventeen SURVIVES ITS OWN REMEDY** — extending the roster is NECESSARY BUT NOT SUFFICIENT; a `(F,F)`-only cell restriction remains fatal) · a NEGATIVE CONTROL POINTED AT A TARGET THAT DOES NOT EXIST.

6. **⚠️ LINE ANCHORS DRIFT; PHRASE ANCHORS DID NOT.** Measured across phases 80-81: `name.go:490` became `:517`; the SN9-collision cite `:501` became `:555`, **and `:501` at the new tip is a real `wasm.` line, so a line-anchored reader lands on a PLAUSIBLE-LOOKING WRONG TARGET**; `STATE.md:48` is `:50`; `next-prompt.txt:150` was a **phantom cite**. **EVERY phrase anchor held. Prefer symbol/phrase anchors to any offset, and never a `+N` offset.** ⚠️ **The phase-81 BRAINSTORM's own cites are LINE-anchored into `.go` files — RE-VERIFY EACH before relying on it**; all twelve were exact at `4f8e159c`, but your SPEC's own edits will move them.

7. **⚠️ THE FULL 120-FIXTURE DIFFERENTIAL IS MANDATORY for any row touching `internal/stats`** — **which D-81-HELPER may or may not make true for this row; that is the fork.** It links into `cmd/envoy-go` at `test/differential/harness.go:240`, `:594` and `test/conformance/h2spec/h2spec_test.go:210`; **h2spec is a FOURTH consumer NOT covered by `./test/differential/`** — run it **explicitly**. Budget **~400-430 s** per green attempt (measured **402 s** at the phase-80 IMPL, **403.2 s** at `f2dd994a`, **427.8 s** in CI); `-race` is a SECOND run. ⚠️ **CI runs this job at `-timeout 20m` with job `timeout-minutes: 30`.** The recipe, with `comm -3` as the load-bearing gate:
    ```sh
    ( go test ./test/differential/ -count=1 -v > "$SCRATCH/full.log" 2>&1; echo "INNER_EXIT=$?" )
    grep -cE '^    --- PASS: TestDifferential/' "$SCRATCH/full.log"          # want 120
    grep -E  '^    --- (FAIL|SKIP): TestDifferential/' "$SCRATCH/full.log"   # want EMPTY
    grep -c  'no driver registered for fixture' "$SCRATCH/full.log"          # want 0
    grep -o  'TestDifferential/[^ ]*' "$SCRATCH/full.log" | sed 's|TestDifferential/||' | sort -u \
      | comm -3 - <(ls -1 test/fixtures/ | grep -E '^[0-9]{4}[a-z]?-' | sort)  # want EMPTY
    ```
    ⚠️ The faithful dir predicate is `^[0-9]{4}[a-z]?-` — a bare `^[0-9]{4}-` gives **118**.
    ⚠️ **`go test ./test/differential/...` (with `...`) matches TWO packages, so `go test` BUFFERS each package's `-v` output until that package finishes.** A run that looks hung for 7 minutes with an empty log is normal — do not kill it.
    ⚠️ **Stat surface: assert the DELTA.** The `TestNoNewStat*` guards are **PROVEN BLIND to `internal/stats`** (all five live in `internal/statssink/registration_test.go`): **enumerate the diff's registration call sites and show the set is EMPTY**, with a non-vacuity denominator (**508 call sites / 84 files** tree-wide; **210 production**). **Adding a guard is neither a PROJECTION nor a REGISTRATION change — it should be +0.**

8. **⚠️ COST AND SPLIT.** `/BOOTSTRAP_PROMPT.md` §6.1 gates on **the number the PLAN writes down**, not the realized figure. **Normative cites, re-verified EXACT at the phase-81 BRAINSTORM: `### 6.1 When to split` at `:285` of `/BOOTSTRAP_PROMPT.md` — AT THE REPO ROOT, not under `docs/envoy-go/`** (`:289` the ~25-task trigger, `:290` the ~1500-LoC trigger, **`:291` BLANK**, `:292` the third mid-execution trigger); six-gate **§7.5 heading `:357`, gates (a)-(f) `:360-365`, close `:367`**. ⚠️ **THE MARGIN ON THIS ROW IS THIN — ~900-1400 against ~1500, only ~1.1-1.7×.** Phase 80 estimated **~640** and realized **1636** (**2.6×**, every bucket over, errors COMPOUNDING, fixture bucket **4.1×**) — the lineage's first realized crossing. ⚠️ **IF YOUR SPEC'S OWN ESTIMATE LANDS ABOVE ~1200, SAY SO AND TELL THE PLAN TO SPLIT 81.1 (the eight sources) / 81.2 (the empty-segment retrofit) RATHER THAN ABSORB IT.** ⚠️ **Calibration says read a budget as "expect the ceiling"**: 76 ~7-9 -> 9 · 77 11-13 -> 12 · 78 7-9 -> 10 · 79 10-12 -> 12 · 80 11-14 -> 13. ⚠️ **The third §6.1 trigger DID fire at phase 80 (6 enumerated vs 17 executed) — RECORD such a firing, never absorb it.**

9. **⚠️ THE SIX-GATE HAS A STANDING LINEAGE DEPARTURE — NAME IT, DO NOT CLAIM COMPLIANCE.** Gate **(f)** expects a `REVIEW.md`; **no phase has had one since 25.3 — 84 of 121 phase dirs carry none.** Record it as a departure. Gate **(d)** (fuzzers) is **VACUOUS for a row that adds none** — **say "vacuous" rather than "green"**, and NC the matcher (55 `^func Fuzz` repo-wide). Gate **(c)**: proxy-wasm is **INHERITED, not re-run** — say so. **For a docs-only SPEC, (a)/(b)/(c)/(e) are NOT OWED — say "not owed", not "green".**

10. **⚠️ ADR + DOCUMENT CONVENTIONS.** Next-free is **ADR-0303**; tail is **ADR-0302**, STATUS **COMPLETE**. **`grep -c '^## ADR-0303'` => 0** (NC: `^## ADR-0302` => 1). **301** `^## ADR-` headings, ids **0001-0302**, **exactly one gap at ADR-0209**, zero duplicates — *"301 ADRs"* and *"tail ADR-0302"* are both true and must not be conflated. **STATUS census 15 COMPLETE / 0 PROPOSED.** ⚠️ **The retained italic footer convention: the footer-carrying block is `ADR-0294 … ADR-0300` — SEVEN. ADR-0301's STATUS says *"seven blocks"* then names the SIX-block range *"ADR-0295 through ADR-0300"*. Copy the FORM, not the miscount.** ⚠️ **CARRY NO WHOLE-FILE GREP COUNT IN AN ADR** — that species self-falsified in ADR-0296 ¶3, twice in ADR-0297, and again in ADR-0302 ¶11.

11. **⚠️ A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX — the TWENTY-FIRST consecutive stage at which this held.** The index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. **Still live:** `reference_sds_init_fetch_timeout_dial_budget_flake` (**TWO packages**) · the pre-existing `internal/cluster` `-race` outlier (`TestOutlierDetector_ConcurrentEjectExactlyOnce`) · `internal/httpclient TestOptions_ZeroValue_NoOpDefaults`. ⚠️ **TWO former flakes were FIXED at `f46ba419`/`f2dd994a` and a recurrence of either is now a FINDING, not a re-run:** `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery` and **the full-suite `bind: address already in use` BACKEND half** — **an in-band backend bind failure is a real finding.** ⚠️ **`0061-lb-ring-hash` is NOT a live flake — a spread failure there is a FINDING.** **Isolate-re-run, then state the classification AND its evidence.** **NO gate was run at the phase-81 BRAINSTORM (docs-only), so no flake could fire — that is an ABSENCE OF EVIDENCE, not evidence of absence.**

12. **Counts at this tip (re-run MECHANICALLY in your worktree; never copy them).** ⚠️ Use GNU `command grep` or `git grep` for anything repo-wide. **All re-derived at the phase-81 BRAINSTORM close:**
    fixtures **120** (next-free **0119**; faithful predicate `^[0-9]{4}[a-z]?-`, a bare `^[0-9]{4}-` gives **118**) / fuzzers **55** / internal packages **73** / blank imports in `runner_test.go` **120**, ⚠️ **anchor on the FULL `^\t_ "github.com/pgdad/envoy-go/test/fixtures/` prefix — a naive `^\t_ ` gives 126** — and ⚠️ **`\t` in GNU ERE is a literal `t`: use `-P`, or a bare `-cE '^\t_ '` silently returns 0** / **stat surface 1207**, ⚠️ **DOCUMENTARY and CONFIG-CONDITIONAL — assert the DELTA** / **BackendKind tail 38** (⚠️ a TAIL VALUE — **39** declarations, values 0-38 contiguous, **no `iota`**; do NOT "fix" 38 to 39) / go.mod modules **2** (the phase-61.2 lineage figure — the single `go.mod` requires **67**; do NOT "fix" 2 to 67) / **`DECISIONS.md` 17724**, **301** headings, tail **ADR-0302 COMPLETE**, next-free **ADR-0303** / **`ROADMAP.md` 229 lines / 113 data rows** / **`BEHAVIOR_CONTRACT.md` 5868** / **`STATE_HISTORY.md` 432** / next-free reference port **10119** / **THIRTEEN non-test top-level segment detectors in `ExtractTags`** (9 `HasPrefix` + 4 `CutPrefix`) plus **FOUR** mid-name detectors / **production `stats.IsValidName` guard sites 15** ⚠️ **not 5** / **production stat-registration call sites 210 of 508 tree-wide** (11 pure-literal, 199 non-literal, fanning out from ~19 distinct token sources).

13. **⚠️ DOCUMENTARY DEFECTS — carried forward, still deliberately NOT fixed.**
    - ⚠️ **`DECISIONS.md` ADR-0301 §Decision (b) says *"has failed twelve top-level detectors"* — PRESENT TENSE inside immutable ADR text, factually wrong since row 80 landed.** A **named permanent discrepancy**, alongside `ROADMAP.md`'s five `twelve` occurrences (⚠️ **four are pure FALSE POSITIVES** and the fifth is historically true, so **nothing is owed there on the merits**).
    - ⚠️ **`0110`'s driver comment *"envoy_sds_* metric names (twelve families…)"* is ACCURATE** (14 distinct names / 12 families) **but is a FALSE POSITIVE for any `\btwelve\b` top-level-detector sweep.**
    - **The `STATE_HISTORY.md` archive gap: 57 (not 58) missing bullets**, 36 of them for phases 67-75; **`phase 77 PLAN done` in NEITHER file, re-confirmed at the phase-81 BRAINSTORM and NOT widened.** Defensible ONLY with a `comm -13` recurrence guard — and that guard needs BOTH the `(slug)`-parens matcher AND a `fetch-depth: 0` line in `ci.yml`.
    - **`ROADMAP.md` malformation: rows 57, 69 and 78 lose summary content in GFM render.** All INVARIANT-BLOCKED by §Schema (*"Only `status` and `sub-phases` columns are updated in place"*, restated at `BOOTSTRAP_PROMPT.md:200`) => **defensible as a GUARD, not a fix**, with the DISJUNCTION gate. ⚠️ **There is NO doc-invariant test anywhere in this repo** — the 5 `_test.go` files naming these docs mention them in **comments only**. The reusable precedent is `test/differential/harness_test.go:123-137`'s `runtime.Caller(0)` -> repoRoot idiom, and `test/differential/` runs in CI's `-short` unit job at ~zero cost.
    - **`STATE.md` §Project counts self-contradicts §Current** and is **FROZEN at the phase-76 IMPL close.** **Anchor on §Current, which IS live.** ⚠️ **Do NOT "fix" §Project** — repairing a count by editing the sentence that states it is how the ADR-0296/0297 self-falsifying species starts.
    - **`BEHAVIOR_CONTRACT.md`'s `wasm.` arm is called a *"NEW SN9 flattening rule"*, which COLLIDES with ADR-0118's actual SN9.** Pre-existing; this is why row 80's `sds.` arm claims NO SN number at all.
    - ⚠️ **The two `//nolint:gosec` directives row 80 added are INERT** — `gosec` is not among the 9 linters `.golangci.yml` enables (`disable-all: true`).
    - **`golangci-lint-action@v6.5.2` emits a Node-20 deprecation warning in CI.** Deliberately NOT bumped: v7+ requires golangci-lint **v2** while the repo pins **v1.64.8**. **A warning, not a failure** — do not "fix" it as part of an unrelated row.
    - ⚠️ **`ROADMAP.md` §Schema `:18` says *"Sub-phases get their own rows"* while phase 80 argued the legs live in the parent's `sub-phases` PROSE.** Practice diverging from written doctrine. **Record the tension; do not relitigate.**
    - ⚠️ **NEW, found at the phase-81 BRAINSTORM:** **production-DEAD trailer code** in four filters (incl. `admission_control`'s entire deferred gRPC-status state machine) · **a VACUOUS differential arm**, `0036`'s `e_trailers_read`, asserting `x-trailer-count=0` on a trailer-less GET · **two stale non-test comments** at `internal/filter/hcm/connection.go:565` and `h2dispatch.go:503` claiming `RunDecodeTrailers` does not exist (it does, at `internal/filter/http/chain.go:455`) · **`DECISIONS.md:13179`** enumerates 24 wasm module-function keys, **8 of which do not exist in `internal/`** · **two sibling `freeTCPPort` helpers** (`cmd/envoy-go/main_test.go:180`, `test/conformance/h2spec/h2spec_test.go:219`) still carry BOTH pre-`f2dd994a` defects.

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — take it from `git rev-parse master`, **NOT from a SHA quoted in this file**. The phase-81 BRAINSTORM squash is **`3816a17f`**, but **router-only commits routinely land above it** (this very sentence is one), so a quoted SHA is stale the moment the router is touched again — **branch off the tip, not off the BRAINSTORM commit.** ⚠️ **AND THE SUBJECT-LOCATE GREP GOES NON-UNIQUE**: `git log --grep 'phase 81'` returned **exactly 1** at the BRAINSTORM close, but the NC on the completed phase 80 returned **5** — **once your SPEC lands it returns 2, so take the NEWEST line (`| head -1`) and verify the count before relying on it** (`reference_next_prompt_tracked_despite_gitignore`); subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** (`reference_parallel_subagents_private_scratch`) and they **must tear down docker containers BY NAME, never by an `ancestor`/image filter** (`reference_parallel_agents_shared_machine_namespaces`). ⚠️ **THERE ARE TWO RESERVED PORT BANDS — band private ports outside BOTH `20000-31007` (subject blocks) AND `11000-14999` (backends), and also clear of the static fixture ports `10000-10447` / `15000-15011` / `18001-18007`.** The `42000-42899` band has been used safely. ⚠️ **If two agents share ONE worktree they must stage by EXPLICIT PATH**, and **must NOT run an unscoped `git restore`**. Per-task `gofmt` + `golangci-lint` on touched packages (`feedback_pertask_gofmt_lint`) — **NOT owed for a docs-only SPEC.** ⚠️ **`gofmt -l` NEVER exits non-zero — gate on OUTPUT.** ⚠️ **`golangci-lint` runs `misspell` with `locale: US`** — do NOT paste plan prose into `.go` files. ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch, and note `-o` precedes the package path.**

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. ⚠️ **ASSUME IT FIRES EVERY SESSION — it has been treated as live for TWENTY-FIVE consecutive sessions and it FIRED AGAIN, OBSERVED THREE TIMES, at the phase-81 BRAINSTORM** (`Shell cwd was reset to /home/esa/git/envoy-go`). **Use `git -C <abs-worktree-path>` for every git command, prefer absolute paths over `cd`, put any needed `cd` FIRST in a compound command, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

⚠️ **IF YOU WRITE A PROBE INTO THE WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN.** The phase-81 BRAINSTORM wrote a `_test.go` probe into `internal/rbac/`, ran it, deleted it, and verified `git status --porcelain` = **0 lines** before committing. A docs-only stage that ships a stray `.go` file has silently changed its own gate obligations.

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline — ⚠️ **anchor greps on `^- **<field>`**; all seven fields verified singleton at the phase-81 BRAINSTORM close, NC both ways). ⚠️ **§Project counts SELF-CONTRADICTS §Current and is frozen at the phase-76 IMPL close** — do not anchor on it.
2. `docs/envoy-go/phases/81-stats-name-charset-guards/BRAINSTORM.md` — **the charter you are specifying.** §1 the proved crash · §2.2 the fifteen-guard census · §2.4 the eight sources · §3 the pick and the costed alternatives · **§4.1 the four D-questions, D-81-HELPER first** · §5 the fourteen refutations.
3. `docs/envoy-go/ROADMAP.md` — **§Schema first** (the row invariants), then **row 81** as the charter of record, then the five deferred-candidate paragraphs (`:191 :201 :211 :217 :225`).
4. `/BOOTSTRAP_PROMPT.md` — **at the REPO ROOT.** §6.1 `:285` (the split triggers), §6.2 (what a coherent split slice is), §7.5 `:357-367` (the six-gate).
5. `docs/envoy-go/DECISIONS.md` — **ADR-0288** (roll-IN-PLACE and the archive rule) · **ADR-0044** as-used (the §Context-at-SPEC / §Decision-at-IMPL split and the retained-footer convention) · **ADR-0045** (⚠️ it QUOTES the split gate rather than stating it) · **ADR-0302** (the phase-80 row — the model for a full ADR block) · **ADR-0065** (⚠️ the sanitizing foreclosure that D-81-SANITIZE must reconcile).
6. `docs/envoy-go/phases/80-stats-sds-projection/PROGRESS.md` — **the `# IMPL record`**, for calibration: sixteen PLAN refutations, the first realized ~1500-LoC crossing, and the shape-seventeen gate that **survived its own remedy**.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — especially **`reference_probe_must_discriminate`**, **`reference_probe_input_is_a_claim`**, **`reference_a_drift_correction_is_itself_a_claim`**, **`reference_grep_c_zero_is_a_broken_gate`** (⚠️ **corrected at this stage — the hazard is `git grep`-specific**), **`reference_sentinel_matcher_string_self_clears`**, **`reference_compensating_defects_cancel_in_the_gate_metric`**, **`reference_gate_command_negative_control`**, **`reference_empty_output_is_not_a_zero_result`**, **`reference_sample_is_not_an_audit`**, **`reference_harness_exit_code_is_not_command_exit_code`**, **`reference_recursive_grep_blind_to_gitignored_tracked_file`**, **`reference_dynamic_stat_name_charset_guard`**, **`reference_nil_stats_counter_inc_crashes_goroutine`** (⚠️ **the WRONG mechanism for this row — `checkName` PANICS, it does not return nil**), **`reference_stale_cite_recurs_fix_by_pattern`**, **`reference_deferred_candidate_cost_restale`**, **`feedback_brief_citations_not_evidence`**, `feedback_git_worktrees`, `feedback_execution_style`, and **`reference_bash_cwd_reset_commits_to_main`**.
