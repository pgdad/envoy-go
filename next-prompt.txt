START a fresh cold-start session for envoy-go — **the phase-39.2 (`health-check-tcp-grpc`, the TCP + gRPC active-HC checker codecs) SPEC is DONE, LANDED on master (commit `14cb8fc`), and PUSHED to `origin/master`; the next lifecycle step is the phase-39.2 PLAN** (`superpowers:writing-plans` scoped to PLAN authoring — decompose SPEC §10 into ~13–16 bite-sized TDD tasks; the FINAL ADR-0045 split-gate re-check, anticipated NO further split) OR a new subject / a now-unblocked health-gated LB candidate (`superpowers:brainstorming`). The substantive master tip is the phase-39.2 SPEC commit **`14cb8fc`** (the pre-authorized SECOND leg of the phase-39 by-codec split; the §11 D-TG1..D-TG7 empirical pins + the ADR-0244 §Context draft; confirm via `git log --oneline -5`). The prior substantive tips are the phase-39.1 IMPL squash `6714575` (the host health-state dimension + the cluster-level checker runtime + the health-aware LB pick; ADR-0242 + ADR-0243) and the phase-38.2 IMPL squash `14053d1`. Counts UNCHANGED at the 39.2 SPEC (stat surface **1132** / fixtures **68** / fuzzers **42** / BackendKind **33** / DECISIONS tail **ADR-0243**, next-free **ADR-0244**); the 39.2 IMPL will move them (see below).

## What just landed (phase 39.2 SPEC — the durable record is `docs/envoy-go/phases/39.2-health-check-tcp-grpc/SPEC.md` + README.md; read those, not this summary)

The SPEC charters the TWO remaining active-HC checker codecs over the UNCHANGED 39.1 substrate (the `internal/cluster/health.go` `clusterHealth`/`hostHealth` registry + the `healthChecker` runtime + the build-time-injected six-LB health view) — a CODEC-ONLY widening. The §11 D-TG1..D-TG7 empirical pins were EXECUTED IN-SESSION (parallel-subagent fan-out) live against `envoyproxy/envoy:contrib-v1.37.2` per `reference_docker_probe_bridge_network`. SPEC-firmed (AMEND-TG1..TG7):
- **`tcp_health_check`** — empty `tcp_health_check{}` = **connect-only** (MVP; a bare TCP connect is the liveness signal). Connect-refused ⇒ `failure`+`network_failure` (identical to the HTTP dial-failure classification). Payload `send`/`receive` parse-accepts (text+binary) but is DEFERRED → a `tcp_health_check` that sets `send`/`receive` DEPARTURE-rejects (the fail-fast deferred boundary, the 38.2 `cluster_header` precedent).
- **`grpc_health_check`** — a unary `grpc.health.v1.Health/Check` over H2 reading the `ServingStatus`: SERVING ⇒ healthy; NOT_SERVING (reachable, via `service_name`) ⇒ `failure` with **`network_failure` STAYING 0** (the application-vs-network discriminator); dead-port ⇒ both++. `service_name` supported (empty = the overall `""` service); `authority`/`initial_metadata` deferred. Uses `google.golang.org/grpc` + `grpc_health_v1` DIRECTLY (the turnkey client; ALREADY a direct dep — `internal/grpcclient`/extauthz/ratelimit import grpc; `go mod tidy -diff` EMPTY → **ZERO new module**). NO hand-roll → **NO new fuzzer** (no hand-rolled wire decoder to fuzz; the BRAINSTORM's `FuzzGrpcHealthResponse` is NOT warranted).
- **Architecture** — the 39.1 `parseHealthChecks` + `healthChecker` generalize to a kind-tagged `prober` dispatch (`httpProber` [the 39.1 `probeHTTP` body unchanged] / `tcpProber` / `grpcProber`); the common `interval`/`timeout`/`unhealthy`/`healthy` envelope lifts out of `httpHealthCheckCfg`. The runtime (`StartHealthChecks`/`Drain`), the `recordResult` transition model, the +7 stats, the panic threshold, and the six-LB health view are BYTE-STABLE. The only new wiring is the gRPC cluster-must-be-H2 reject (`<cluster> cluster must support HTTP/2 for gRPC healthchecking`, config-init NOT PGV — reuse the existing `extractH2Mode`). The 39.1 `only http_health_check is supported` reject is LIFTED.
- **Stats** — BOTH codecs reuse the 39.1 codec-agnostic set → **ZERO new stats** (surface 1132 → 1132).
- **Differential** — `0067-health-check-tcp` (cross-side connect-only; the live HTTP backends answer the TCP connect → **NO new BackendKind**) + `0068-health-check-grpc` (cross-side gRPC over H2; **+1 BackendKind** `GRPCHealthResponder` — an h2c server answering `grpc.health.v1.Health/Check` ⇒ SERVING AND returning 200 to plain-H2 data-plane requests). Both follow the `0066` poll-to-converge + warmup-gate + delta-counter protocol (gate on `membership_healthy` convergence + warmup, NOT raw `attempt` deltas — no-traffic deactivation, AMEND-TG7).
- **Split** — single flat 39.2 leg (~150–260 prod LoC / ~13–16 tasks, UNDER the ADR-0045 gate; NO TCP/gRPC sub-split). ONE ADR (ADR-0244 — the multi-codec checker prober dispatch; §Context draft at the SPEC §13, body at the IMPL per ADR-0044).

With 39.2 the active-HC checker roster (HTTP + TCP + gRPC) is COMPLETE — the phase-39 by-codec split fully consumed.

## What's next — the phase-39.2 PLAN (`superpowers:writing-plans`) OR a new subject

Decompose SPEC §10 into ~13–16 bite-sized TDD tasks (the anticipated spine: baselines/PROGRESS → the `prober` interface + dispatch generalization [68-dir differential GREEN after the refactor] → `tcpProber` + parse + `send`/`receive` reject + `0067` → `grpcProber` + the gRPC-must-be-H2 reject + the `service_name` arm → the +1 `GRPCHealthResponder` BackendKind + `0068` → full 70-dir re-verify + six-gate → ADR-0244 body + BEHAVIOR_CONTRACT 39.2 delta → completion bundle). Resolve the §12 D-S39.2-1..6 (the `prober`/`checkerEnvelope`/`checkerSpec` shape; the gRPC `authority`/`initial_metadata` disposition + `internal/grpcclient` reuse; the `codes.Unimplemented` classification edge; the `0068` constants + the `GRPCHealthResponder` h2c-mux shape; the gRPC-H2 detection site + reject wording; the FINAL ADR-0045 re-check). plan-document-reviewer loop. Counts stay UNCHANGED at the PLAN.

ALTERNATIVELY: the next Upstream-robustness-family row (4 candidates remain after 39.2: {outlier detection, circuit breakers, retries + hedging, per-protocol connection pooling}) OR a now-unblocked health-gated Load-balancing candidate ({locality-weighted LB, priority load balancing, panic thresholds} — the 39.1 keystone provides the health-state/healthy-fraction they key off) OR a wholly new ROADMAP family (xDS, gRPC, HTTP/3, Observability, Runtime). The subject is chosen at the next BRAINSTORM/SPEC per ADR-0106.

## Read first (in order)

1. THIS FILE — the post-phase-39.2-SPEC cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 39.2 (health-check-tcp-grpc) SPEC done`; the counts footer (fixtures 68 / fuzzers 42 / stat surface 1132 / BackendKind 33 / DECISIONS tail ADR-0243, next-free ADR-0244); the lifecycle routing.
3. `docs/envoy-go/phases/39.2-health-check-tcp-grpc/` — SPEC.md (the §1.1 AMEND-TG1..TG7 + the §3 architecture + the §6 reject roster + the §8 `0067`/`0068` design + the §11 live pins + the §13 ADR-0244 §Context draft) + README.md — the durable phase-39.2 SPEC record (the PLAN.md + PROGRESS.md follow at the PLAN/IMPL).
4. `docs/envoy-go/phases/39-upstream-health-check/` — the phase-39 BRAINSTORM (§1.4/§2.2 the by-codec split) + the 39.1 SPEC (AMEND-HC2/HC8) + the 39.1 PROGRESS.md (the as-built substrate 39.2 reuses).
5. `docs/envoy-go/ROADMAP.md` — row 39 (`in-progress`; the 39.1-leg DONE + the 39.2-SPEC-DONE notes; the 39.2 IMPL pending) + the §Upstream robustness family heading (4 candidates remain).
6. `docs/envoy-go/DECISIONS.md` — ADR-0242 + ADR-0243 (the 39.1 runtime + health-aware pick 39.2 reuses UNCHANGED) + the process ADRs (ADR-0106/0045/0044/0052/0080/0227/0004) + ADR-0230 (the prior framework-runtime seam).
7. `internal/cluster/{health.go,manager.go,cluster.go,loadbalancer.go,random.go,leastrequest.go,ringhash.go,maglev.go,subset.go,dial_h2.go}` + `internal/grpcclient/grpcclient.go` (the gRPC dial path the prober reuses) + `internal/admin/admin.go` (`/stats`) + `test/fixtures/0066-health-check-http/` (the poll-to-converge + warmup-gate differential precedent `0067`/`0068` follow) + `test/differential/fixture/fixture.go` (the backend registry the `GRPCHealthResponder` BackendKind extends).
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_execution_style`, `feedback_git_worktrees`, `feedback_subagents_no_push` + `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_path_targeting`/`_detach`, `reference_docker_probe_bridge_network`, `reference_health_check_propagation_warmup`, `reference_differential_break_protocol_count1`, `reference_differential_run_selector`, `reference_fixture_workload_constant_desync`.

## Honor throughout

- The lifecycle is BRAINSTORM → SPEC → PLAN → IMPL, each its own session. The next session is the phase-39.2 PLAN (`superpowers:writing-plans` scoped to PLAN authoring) OR a fresh BRAINSTORM for a new subject.
- `feedback_git_worktrees` · `feedback_execution_style` (subagent-driven at the IMPL) · `feedback_subagents_no_push` · `feedback_push_to_origin` (the controller squash-merges + pushes at stage-close — the 39.2 SPEC landed as commit `14cb8fc`) · `feedback_pertask_gofmt_lint`.
- ADR-0004 (the empirical-pin discipline — EXECUTED at this SPEC) · ADR-0044 (ADR §Context at SPEC, bodies at IMPL in-place) · ADR-0045 (the split-gate — the FINAL 39.2 re-check at the PLAN) · ADR-0052 (the atomic-landing six-gate) · ADR-0080 (byte-stable parse-reject wording) · ADR-0106 (flat family row — NO parent rollup) · ADR-0227 (the contrib reference image `envoyproxy/envoy:contrib-v1.37.2`).
