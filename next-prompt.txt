=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage. To use it to implement the next phase: read `docs/envoy-go/STATE.md` for the recorded NEXT, perform that one stage (the concrete action is spelled out below), land it, and re-roll this file to the following stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, the active phase is fully landed, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action. While ANY stage remains — as NOW: the **47.1 PLAN is pending** (and 47.2's full SPEC→PLAN→IMPL lifecycle after it), so tasks are NOT complete — do NOT create `stop`; perform the stage described below.
==============================================

RESUME a fresh session for envoy-go (router last updated 2026-06-29; the **phase 47.1 (stats-sink metrics_service) SPEC is DONE and LANDED** — docs-only direct-on-master, committed + pushed to origin). Phase 47 is the **FOURTH Observability-family row** (`stats-sink-metrics-service`); the family STAYS OPEN. **ROW 47 STAYS `in-progress`** (flips `done` at the FINAL leg 47.2 IMPL per ADR-0106 + `reference_roadmap_split_phase_row_done`). The next action is the **47.1 (core) PLAN**.

HEAD is the phase-47.1 SPEC docs commit atop the phase-47 BRAINSTORM squash `973208e1`. **Re-read the shas with `git log --oneline -6`, do NOT assume.** Confirm `git status` clean (the untracked `.claude/` is session cruft, leave it) + master == origin/master.

## What the 47.1 SPEC chartered (docs-only — `docs/envoy-go/phases/47-stats-sink-metrics-service/SPEC-47.1.md`, the 14-section SPEC-46.1 shape; the §11 D-MS-* pins EXECUTED in-session live vs `contrib-v1.37.2`)
- **Subject (ADR-0262):** lift the `metrics_service` `stats_sinks[]` TypeURL (`type.googleapis.com/envoy.config.metrics.v3.MetricsServiceConfig`) from the ADR-0064 silent-ignore set + a NEW `internal/statssink` package (the `stats_flush_interval`-driven `Flusher` walking the frozen `stats.Registry` [`Walk` `registry.go:134`] + the `MetricsServiceSink` over the ALS `GrpcAccessLogSink` **CLIENT-STREAMING** lifecycle [identifier-once + reconnect-resend; `internal/accesslog/grpcsink.go`] + the cumulative/no-labels `Counter`/`Gauge`→`io.prometheus.client.MetricFamily` mapping) + the `MetricsServiceClient` typed wrapper over `grpcclient.Dialer` (ADR-0158 — **mirrors the client-streaming `ALSClient` `grpcclient.go:279`, NOT the unary OTLP wrappers** — `StreamMetrics` is client-streaming exactly like `StreamAccessLogs`) + the `stats_sinks[]`/`stats_flush_interval` parse arm + the strict-reject arms + the post-`Freeze` `Flusher.Start` main wiring (`main.go:325`/`:327`) + the driver-owned `test/helpers/metricsservice` receiver + the `0089-stats-sink-metrics-service` cross-side EXACT differential.
- **§11 live-probe findings** (decode-ran proof: 13–20+ `StreamMetricsMessage`s, 88→222 `MetricFamily`/msg, `cluster.backend.upstream_rq_total.counter.value==7==K`): CONFIRMED the cumulative/no-labels default (full dotted name, tags INLINED, 0 LabelPairs, absolute values, `help` empty). FOUR amendments shaping the PLAN/IMPL:
  - **AMEND-MS-HISTOGRAM-PRESENT** — the reference EMITS `HISTOGRAM` families envoy-go CANNOT (ADR-0060) ⇒ the metric-type map is closed to `COUNTER`/`GAUGE` AND the `0089` differential asserts a deterministic **COUNTER/GAUGE NAME-SUBSET** (`cluster.<backend>.upstream_rq_total` / `http.<stat_prefix>.downstream_rq_total` / `downstream_rq_2xx`, all ==K), **NOT a whole-family-set comparison** (the surfaces differ cross-side — envoy-go's registry ≠ the reference's).
  - **AMEND-MS-TIMESTAMP-ALWAYS-SET** — every metric carries a flush-time `timestamp_ms`; the IMPL MUST populate it; the value is UNasserted cross-side.
  - **AMEND-MS-REFERENCE-STRICTER** — UNLIKE the permissive tracing reference, the metrics_service reference HARD-REJECTS a missing/unknown gRPC cluster + a deprecated non-V3 `transport_api_version` (exit 1) ⇒ envoy-go's `HasCluster`/Dialer-cluster-exists + the non-V3 reject are **REFERENCE-PARITY**, not envoy-go-strict departures. The envoy-go-STRICT rejects (ADR-0080) are the knob VALUES (`report_counters_as_deltas:true` / `emit_tags_as_labels:true` / non-default `histogram_emit_mode`) + the sibling sink TypeURLs (`reference_strict_reject_sibling_typeurl_gap` — the reference silently accepts a `StatsdSink`) + `stats_flush_on_admin`.
  - **AMEND-MS-NO-SELF-STATS** — ZERO `metrics_service`-scoped self-stats (only the generic `server.dropped_stat_flushes`) ⇒ the reference surface delta is **+0**; an envoy-go exporter-counter set (flush-success/drop) is an optional naming decision (N≈0–2, IMPL registration-test pinned), with the self-referential subtlety (a self-counter registered before Freeze appears in its own next flush).
  - Other pins CONFIRMED: identifier-once (msg #1, re-armed on reconnect) + auto-reconnect + one full-registry-snapshot batch per flush + the **5s** default `stats_flush_interval`.
- **D-MS-MODULE (CORRECTION):** `github.com/prometheus/client_model` MVS-resolves to **`v0.6.1`** (NOT the brainstorm's `v0.6.2` guess — pinned via `go list -m`), the SOLE new direct require (the metrics service/config protos resolve at the already-direct `go-control-plane/envoy v1.32.4`); NOT in `go.mod`/`go.sum` today; the go.sum hash + require promotion LAND AT THE 47.1 IMPL via `go get github.com/prometheus/client_model@v0.6.1 && go mod tidy` (with network — the in-session tidy/get were sandboxed-network-gated). The FIRST new go.mod module in the Observability family.

## The NEXT action — the 47.1 (core) PLAN (lifecycle-state 2 → 3)
Author `docs/envoy-go/phases/47-stats-sink-metrics-service/PLAN-47.1.md` (the ~10–13-task TDD spine, the PLAN-46.x shape). Use `superpowers:writing-plans`. Resolve the SPEC §12 PLAN/IMPL D-questions:
- **D-MS-SPLIT** — the FINAL ADR-0045 re-check with real LoC (a 47.1a/47.1b sub-split is NOT anticipated — one new package with three pieces + the parse arm + the typed wrapper + main wiring; confirm against the soft gate).
- **D-MS-CONFIG-HOME** — `StatsSinkConfig` in `internal/bootstrap` (parsed alongside the bootstrap) vs `internal/statssink` (consumed by bootstrap); the acyclic-import check (`bootstrap → statssink`; `statssink` imports `stats` + the metrics protos + reaches the gRPC client via a sink-facing interface seam — the 45.x `bootstrap → accesslog` acyclic precedent).
- **D-MS-SINK-BUFFER** — the `MetricsServiceSink` channel depth (a 1-deep flush handoff vs the ALS size/interval accumulator — the Flusher IS the trigger, so each flush is one already-complete batch).
- **D-MS-APIVERSION** — the `transport_api_version` accept set ({`AUTO`,`V3`} vs `V3`-only) + the `AUTO`(0)→V3 mapping (the GcpAccessLog-parity arm semantics).
- **D-MS-FLUSH-INERT** — a `stats_flush_interval` present WITHOUT a metrics_service sink: an inert no-op loop vs no loop (byte-stability).
- **D-MS-RECEIVER-WIRING** — the `0089` receiver reachability (shared bridge + hostname, the §11 precedent) + the `test/helpers/metricsservice` shape (the `test/helpers/otlptrace` `New`/`NewAtAddr`/`Count`/`Reset`/`Addr` precedent; the §11 probe receiver `scratchpad/probe/receiver/main.go` is the working seed) + the poll/assert surface (`Family(name)`/`Count()`).
- **D-MS-STATS-FINAL** — the exact +N (zero to match the reference vs an envoy-go exporter-counter set; the self-referential subtlety) via an IMPL registration test.
- **D-MS-REJECT-COVERAGE** — which boot-rejects earn a `0090`-class subject dir vs a subject unit test (the reference-parity cluster/non-V3 vs the envoy-go-strict knobs/siblings/flush-on-admin split; `reference_differential_fixture_dispatch_constraint` — one dir = one runner branch).
- **D-MS-FUZZER** — land `FuzzStatsSinkConfigParse` at 47.1 (fuzzers 49 → 50) vs defer; reconcile the documented-vs-actual `^func Fuzz` count regardless (`reference_fuzzer_count_docs_drift` — **49 actual**, verified in-session).
- **D-MS-FINAL-FLUSH** — whether 47.1 emits a final flush on `ctx.Done()` drain before the ticker stops (re-probe the reference if load-bearing).
The PLAN doc commits docs-only direct-on-master; a fresh worktree off master per `feedback_git_worktrees` for the PLAN-time code exploration (the IMPL worktree lands later). The ADR-0262 §Decision/§Consequences land at the 47.1 IMPL (ADR-0044) — the §Context draft is anchored in SPEC-47.1.md §13 (DECISIONS.md is NOT touched until the IMPL).

## Counts at the 47.1 SPEC (UNCHANGED — docs-only; re-verify, do NOT assume)
stat surface **1200** (non-H2 **1196**) / fixtures **90** (tail `0088-tracing-zipkin`; the count is "90 dirs under `test/fixtures/`" via `os.ReadDir` of ALL subdirs — NOT a 4-digit glob) / fuzzers **49** / BackendKind **38** / DECISIONS **ADR-0261** (next-free **ADR-0262**). Anticipated at the 47.1 IMPL: stat **1200+N** (N≈0–2) / fixtures **91** (`0089-stats-sink-metrics-service`) / fuzzers **49** (or 50) / BackendKind **38** (driver-owned receiver) / DECISIONS **ADR-0262** / **+1 go.mod module** (`prometheus/client_model v0.6.1`).

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** The next stage is the 47.1 PLAN. Lifecycle-state 2 → 3.
- **Subagent-driven execution** (`feedback_execution_style`) in a **fresh worktree** (`feedback_git_worktrees`) for the eventual IMPL; subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit, re-runs the full suite on the frozen HEAD, does the deliberate-break verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). Per-task gates: `gofmt -l` + `golangci-lint` on touched pkgs + `go vet` + `go build ./...` (`feedback_pertask_gofmt_lint`).
- **Process anchors:** ADR-0106 (per-leg rows) · ADR-0044 (ADR §Context-at-SPEC / §Decision+§Consequences-at-IMPL) · ADR-0045 (sub-split soft gate) · ADR-0080 (strict-reject) · ADR-0059 (the in-tree `Registry`/`Walk`) · ADR-0060 (histograms deferred — the COUNTER/GAUGE-only closure) · ADR-0064 (the `stats_sinks[]` silent-ignore — phase 47 LIFTS the metrics_service sink) · ADR-0158 (the `grpcclient.Dialer` typed-wrapper layer — the `ALSClient` client-streaming precedent) · ADR-0016 (proto blank-import registry) · next-free **ADR-0262**.
- **Differential discipline** (carries to the `0089` fixture): `-run 'TestDifferential/0089'` NEVER bare (`reference_differential_run_selector`); `-count=1` on every break + `-race` run (`reference_differential_break_protocol_count1`); the full-suite `subject ready: EOF` startup flake is isolatable (`reference_differential_fullsuite_startup_flake`); the Docker bridge + decode-ran proof (`reference_docker_probe_bridge_network`); the gRPC metrics receiver is driver-owned, NOT a BackendKind (`reference_differential_grpc_receiver_driver_owned`); assert the aggregated COUNTER/GAUGE NAME-SUBSET by dotted name NOT the whole family set NOR stream framing (`reference_streaming_sink_differential_framing` + AMEND-MS-HISTOGRAM-PRESENT); the live reference is the wire truth (`reference_wire_format_both_sides_see_same_bytes`); the `Flusher`+sink writer goroutine are background mutators ⇒ FULL-package `-race` (`reference_full_suite_race_after_background_mutator`).

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-47.1 SPEC is DONE + LANDED; row 47 `in-progress`; the next action is the 47.1 PLAN).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 47.1 SPEC done`) + the recorded NEXT (the 47.1 PLAN) + the counts (1200 / 90 / 49 / 38 / ADR-0261).
3. `docs/envoy-go/phases/47-stats-sink-metrics-service/SPEC-47.1.md` — the full charter (the §11 D-MS-* live pins + the FOUR amendments + the §12 PLAN questions to resolve + the §13 ADR-0262 §Context draft).
4. `docs/envoy-go/phases/47-stats-sink-metrics-service/BRAINSTORM.md` — the parent charter (the load-bearing facts).
5. `docs/envoy-go/ROADMAP.md` — row 47 (`stats-sink-metrics-service`, `in-progress`) + the Observability family-progress note.
6. The phase-44/45/46 PLANs (`PLAN-44.1.md` / `PLAN-45.1.md` / `PLAN-46.1a.md`/`PLAN-46.1b.md`) — the TDD task-spine shape + the streaming-sink + driver-owned-receiver precedents.
7. The as-built precedents: `internal/accesslog/grpcsink.go` (the ALS client-streaming sink template — the `MetricsServiceSink` model), `internal/grpcclient/grpcclient.go` (`ALSClient` `:279` — the `MetricsServiceClient` model), `internal/stats/registry.go` (`Walk` `:134`/`Freeze` `:209`), `cmd/envoy-go/main.go` (`:325`/`:327`/`:336`), `internal/bootstrap/bootstrap.go` (`Load` `:308`), `test/helpers/otlptrace` (the driver-owned-receiver model), the §11 probe seed `scratchpad/probe/receiver/main.go`.
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_subagent_autocommit_claudemd`, `feedback_pertask_gofmt_lint`, `reference_roadmap_split_phase_row_done`, `reference_differential_grpc_receiver_driver_owned`, `reference_streaming_sink_differential_framing`, `reference_strict_reject_sibling_typeurl_gap`, `reference_docker_probe_bridge_network`, plus the differential-discipline references above).
