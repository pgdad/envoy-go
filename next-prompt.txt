START a fresh cold-start session for envoy-go — **the phase 28.1b SPEC is DONE + squash-merged + pushed**. The active phase is **phase 28** (parent `in-progress`; sub-rows `28.1a` `done`, `28.1b` `in-progress`, `28.2` `planned`). This session authors the **28.1b PLAN** — per SKILL_ROUTING state 2 (SPEC.md exists, PLAN.md does not → `superpowers:writing-plans` → output PLAN.md), from the 28.1b SPEC's §10 task spine.

Phase 28.1b (`network-filter-read-seam-and-zookeeper-requests-proof`) is the second/closing half of the user-approved ADR-0045 28.1a/28.1b invoked split (DECISIONS.md ADR-0045 §AMEND). 28.1a (squash `8703aeb`) landed the WriteFilter seam + the complete zookeeperproxy request package + the DISABLED `0046` driver. The 28.1b SPEC (committed this past session) designs the read-side seam + the cross-side proof + the 28.1 completion bundle. **The PLAN translates the SPEC §10 ~10-task spine into bite-sized TDD tasks** (the SPEC's §10.1 split-gate check: ~80–100 production LoC, no further split — the PLAN re-checks).

## What this session must do

Author the **28.1b PLAN** via `superpowers:writing-plans` → `docs/envoy-go/phases/28.1b-network-filter-read-seam-and-zookeeper-requests-proof/PLAN.md`. The PLAN's tasks come from the SPEC §10 spine:

1. First-action baselines/anchors gate (SPEC §7.1 recipes re-run against the live IMPL tip).
2. `Buffer.TotalAppended()` (a monotonic appended-bytes counter on `internal/filter/network/buffer.go`) + monotonicity unit tests. Resolve D-S28.1b-1 (int vs int64) here — **spec-reviewer advisory: if int64 is chosen, the decoder's `chainConsumed` mark must widen in lockstep; bundle both widenings into ONE task (this one + task 3 may merge) to avoid a type-mismatch split across tasks.**
3. The decoder feed re-base (SPEC §3.3): `decodeOnData(chainBytes []byte, totalAppended int)` — the high-water mark re-based from physical-buffer-length onto TotalAppended; `zookeeperproxy.OnData` passes `buf.TotalAppended()`; mechanical decoder_test updates + NEW drain-regime tests (feed → drain → feed; the handoff-boundary sequence).
4. `internal/filter/network/readconn.go` — the `readChainConn` (SPEC §3.1; innermost wrap; replay-before-return; EOF endStream replay) + unit tests.
5. `chain.go` — `chainRuntime.replayRead` (SPEC §3.2: all read filters, chain order, Status ignored, drain-after-pass) + the `handleTerminal` read-wrap insertion under the SHARED `len(writeFilters) > 0` predicate (SPEC §3.4) + composition/back-compat/soundness-invariant unit tests (zero-write-filter chains get NEITHER wrap — the R1 test).
6. The SPEC §3.6 concurrency race test (synthetic wrapped chain, concurrent pumps over net.Pipe, `-race`). Resolve D-S28.1b-2 here.
7. `0046-zookeeper-requests` re-enable (uncomment the blank-import at `test/differential/runner_test.go:72-77`; replace the driver's DISABLED banner) + cross-side GREEN on all 7 arms + the R4 deliberate-break protocol + the fixture README (all deferred from 28.1a Task 16).
8. `0047-zookeeper-boot-reject` per the 28.1 SPEC §8.2 design verbatim (the `0044` driver is the 220-LoC template).
9. The 28.1 completion bundle: ADR-0221 §Decision/§Consequences body covering BOTH conn-wrap seam halves + ADR-0222 body, in place (no new ADR number — DECISIONS.md tail STAYS ADR-0223); the BEHAVIOR_CONTRACT 28.1 bundle incl. the stat-table roll **136 → 337**.
10. Six-gate (incl. the FULL 49-dir differential suite + the 47-dir R1 back-compat gate) + STATE.md + ROADMAP sub-row 28.1b `in-progress → done` (parent row 28 STAYS `in-progress` — the rollup is 28.2's, D-28.1b-4) + next-prompt.txt for the 28.2-SPEC cold-start.

The PLAN follows the project's TDD task format (failing test first, per-task gofmt + golangci-lint per `feedback_pertask_gofmt_lint`, PROGRESS.md append per task). Per `feedback_execution_style` the IMPL session runs `superpowers:subagent-driven-development`; per `feedback_subagents_no_push` subagents commit locally only.

## Load-bearing SPEC pins the PLAN must carry into tasks

1. **The wrap predicate is `len(rt.writeFilters) > 0`** — the SAME predicate as writeChainConn; both seams wrap together; readChainConn is INNERMOST (`writeChainConn(prefixConn(readChainConn(conn)))`) so prefixConn's replayed prefix bytes are NOT re-fed (already seen pre-handoff).
2. **The decoder-feed re-base equivalence**: on any never-drained execution `TotalAppended() == Len()`, so the re-based feed is byte-identical to the 28.1a feed — the existing decoder unit-test assertions must NOT change (only the call signature does).
3. **The soundness invariant**: the runtime never drains bytes a filter has not yet been shown (handoff drains after the final pre-handoff pass; replay drains after each replay pass). A tracking-filter unit test proves every appended byte is seen exactly once.
4. **The replay is observational**: Status ignored, post-handoff Close not acted on, ContinueReading meaningless — all documented BEHAVIOR_CONTRACT boundaries (SPEC §3.5), NOT silent.
5. **The 28.1b race surface is empty** (OnWrite is a no-op) — but the PLAN's completion-bundle task must ensure the BEHAVIOR_CONTRACT/ADR text carries the **28.2 synchronization forward-pointer** (the response decoder in OnWrite will race the replay-path request decoder on the correlation structures; ADR-0223/the 28.2 SPEC must add a mutex or equivalent).
6. **Arm-by-arm seam dependency for 0046** (SPEC §5.1): arms 1/5/6 were already green at 28.1a; arms 2/3/4 require the seam (multi-socket-read connections). The expected green table is PROGRESS.md Task 16's reference column (`ping_rq=1 getdata_rq=2 ... request_bytes=307`).
7. **Counts at 28.1b close**: 49 active fixture dirs (tail `0047`), 37 fuzzers (unchanged — no new fuzzer), stat table 337, DECISIONS tail ADR-0223 / next-free ADR-0224 (both unchanged).

## Project state at master tip

- **Master tip:** `bd5ef4b` (the docs-only STATE.md SHA-fill commit that trails the substantive 28.1b-SPEC squash `baf02ed` by +1). Confirm the live tip via `git log --oneline -1`; any further next-prompt repoint advances the tip +1 (expected, not chased). STATE.md `last-commit` points at the 28.1b-SPEC squash `baf02ed`; substantive predecessor is the 28.1a-IMPL squash `8703aeb`.
- **§9 Network-filters family:** rows 26 (parent + 26.1/26.2/26.3) + 27 DONE; row 28 `in-progress` (parent; 28.1a `done`, 28.1b `in-progress`, 28.2 `planned`). After phase 28, 4 candidates remain (`redis`/`mongo`/`kafka_broker`/`thrift`).
- **Counts at master tip:** stat surface **136** (rolls to **337** at the 28.1b completion bundle, gated on the 0046 cross-side proof); differential fixtures **47 active + 1 disabled** (`0046` re-enables + `0047` lands at 28.1b IMPL → 49 active); fuzzers **37** (canonical recipe `grep -rh "^func Fuzz" $(find ./internal -name fuzz_test.go) | wc -l`); conformance 10/10; h2spec 53/53; DECISIONS.md tail **ADR-0223** (next-free **ADR-0224**; ADR-0221 carries the 28.1b-SPEC §AMEND extending it to both seam halves). ADR-0209 escape-valve reserve STANDS-UNCONSUMED.
- **Toolchain:** Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); reference Envoy v1.37.2 (ADR-0008); go-control-plane v1.32.4 (ADR-0008).
- **Known coverage boundaries (carried, non-blocking):** D-26.3-4; the `tcp_proxy` `downstream_cx_*` family unmirrored; histograms deferred (ADR-0060); zookeeper dynamic-metadata deferred (AMEND-A9) + shallow-decode leniency + terminal-originated-writes-only + the write-only-filter boot boundary (28.1 SPEC §7.5) + the NEW post-handoff observational boundaries (28.1b SPEC §3.5).

## Read first (in order)

1. THIS FILE — the 28.1b-PLAN cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 28.1b SPEC done`; next-skill `superpowers:writing-plans` (28.1b PLAN); next-free `ADR-0224`.
3. `docs/envoy-go/phases/28.1b-network-filter-read-seam-and-zookeeper-requests-proof/SPEC.md` — THE document this PLAN translates (esp. §3 the seam design, §5 the fixtures, §6 the completion bundle, §7.1 the as-built anchors, §10 the task spine, §11 the test surface).
4. `docs/envoy-go/phases/28.1-network-filter-write-seam-and-zookeeper-requests/SPEC.md` (the master SPEC — §3/§4/§8 stay authoritative for the 28.1a-landed surface) + `PROGRESS.md` (the Task-16 BLOCKED record + divergence table + PoC evidence + the 28.1a closure entry).
5. `docs/envoy-go/DECISIONS.md` ADR-0045 §AMEND + ADR-0221 (§Context + the 28.1b §AMEND) + ADR-0222/0223 §Context drafts.
6. The as-built code the tasks touch: `internal/filter/network/chain.go` + `buffer.go` + `writeconn.go` + `prefixconn.go`, `internal/filter/network/zookeeperproxy/decoder.go` + `zookeeperproxy.go`, `internal/listener/manager.go` (`serveNetworkChain` — read-only context), `internal/filter/tcpproxy/filter.go` (`Handle` — the goroutine anchors), `test/differential/runner_test.go` (the commented 0046 import + the TCPSink arm), `test/fixtures/0046-zookeeper-requests/driver/driver.go`, `test/fixtures/0044-network-rbac-boot-reject/driver/driver.go` (the 0047 template).
7. `docs/envoy-go/phases/28-network-filter-zookeeper-proxy/SPEC.md` + `BRAINSTORM.md` — the parent SPEC (§11 empirical pins).
8. `docs/envoy-go/SKILL_ROUTING.md` + the BOOTSTRAP §4/§5 cold-start invariants.
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — execution-style + worktree + push + subagents-no-push + per-task gofmt/lint + the network-filter constraints + the differential dispatch/asserter constraints + proto-roster-digit-extraction all carry forward.
