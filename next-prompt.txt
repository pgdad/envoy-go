=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **PHASE 96 (`listener-default-chain-tlsmode`) IS OPEN AT LIFECYCLE-STATE 1. ROW 96 READS `in-progress` AT `ROADMAP.md:158`.** ⇒ **THIS SESSION RUNS THE PHASE-96 SPEC** (state 1 -> 2). The BRAINSTORM is `docs/envoy-go/phases/96-listener-default-chain-tlsmode/BRAINSTORM.md` — **761 lines**; its §10 is the nine-item list of what the SPEC owes.

⚠️ **THE MARGIN IS TWO.** Check (1) is NON-SILENT (`NOT DONE: row 96`, one line) and check (2) reads SIX. Check (3) is SILENT. ⚠️ **DELETING THE LAST DEFERRED-CANDIDATE LINE STILL ENDS THE PROJECT. DO NOT "TIDY" ONE.** NC-A and NC-B read **TWO** lines each at this tip. **MEASURE ALL THREE CHECKS AND ALL FOUR NCs YOURSELF BEFORE DOING ANYTHING ELSE.** ⚠️ **A SPEC DOES NOT TOUCH `ROADMAP.md`, SO THE SHAPES ABOVE SHOULD HOLD ACROSS YOUR STAGE — BUT MEASURE, DO NOT ASSUME, AND NOTE THAT WHEN ROW 96 EVENTUALLY FLIPS `done` AT THE IMPL, CHECK (1) GOES SILENT AND NC-A AND NC-B EACH DROP FROM TWO LINES TO ONE.**

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. Parses FIELDS instead of
#     matching a pattern, and asserts the row DENOMINATOR so a deleted row is
#     itself an error.
#     ⚠️ want IS 128 and ROADMAP.md IS 246 lines at the phase-96 BRAINSTORM close.
#     Row 96 is `in-progress`, so check (1) is NOT SILENT — it prints exactly one
#     line, `NOT DONE: row 96`. It WAS silent one close ago.
awk -F'|' -v want=128 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **CHECK (3) IS SILENT, AND A SILENT CHECK IS INDISTINGUISHABLE FROM A BROKEN ONE — THE NCs ARE THE ONLY EVIDENCE.** Run all four EVERY SESSION:
```sh
# NC-A — doctor a row to `in-progress`. ⚠️ READS *TWO* LINES NOW (`NOT DONE: row 62` from the
#        doctoring AND `NOT DONE: row 96`, which is genuinely in-progress). It read ONE before
#        the phase-96 row ADD. MEASURED at the phase-96 BRAINSTORM close, both before and after.
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md with want=128.

# NC-B — the DENOMINATOR, want=127 on the real file. ⚠️ READS *TWO* LINES NOW —
#        `NOT DONE: row 96` THEN `GATE FAIL: examined 128 data rows, expected 127`.
#        It read ONE before the ADD. ⚠️ **BOTH NC-A AND NC-B CHANGED SHAPE ACROSS THE ADD**,
#        as the phase-95 router forecast for a row change and the phase-96 BRAINSTORM confirmed
#        by execution on both sides.

# NC-C — the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```
⚠️ **AND RUN THE CHECK-(2) POSITIVE CONTROL — AND READ THE COMMAND BEFORE TRUSTING ITS RESULT.** The longer phrase does not CONTAIN the shorter one as a substring, so a control that substitutes only one phrase reports a residual of 5 and reads like a finding. Substitute BOTH and assert the substitution count:
```sh
sed -e 's/deferred candidates:/deferred candidatesXX/g' \
    -e 's/remaining deferred (not-yet-chartered) candidates:/remaining deferred (not-yet-chartered) candidatesXX/g' \
    docs/envoy-go/ROADMAP.md > "$SCRATCH/c2pc.md"
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' "$SCRATCH/c2pc.md" | wc -l  # want 0
grep -o 'candidatesXX' "$SCRATCH/c2pc.md" | wc -l                                                                # want 6 — PROVE it landed
```

⚠️ **WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -c '-family row'` fails (GNU: `grep: amily row: No such file or directory`; ugrep: `option -f: cannot read amily row` — **both rc=2**) and the surrounding arithmetic prints `0`, **which reads exactly like "no change."** **Always pass `--` before the pattern**; with `--` occurrences read **96** and LINES read **68**. **`grep -c` counts LINES, NOT OCCURRENCES,** and **`grep -c` on zero matches PRINTS `0` *AND* EXITS 1, so `$(cmd || echo 0)` EMITS TWO ZEROS** — capture with `v=$(cmd || true)`.

⚠️ **THE MALFORMED-ROW FIGURE RECONCILES ONLY UNDER AN ESCAPE-AWARE FIELD COUNT — AND THE ESCAPE-AWARE COMMAND ITSELF CAN BE BROKEN.** Malformed rows are **ROW IDs 57** and **69** (file lines 119 and 131). **Strip `\|` BEFORE splitting.** Row 57 reads **naive NF=13 / escape-aware NF=9**; row 69 reads **10 / 10**; rows 94, 95 and 96 read **8 / 8**; naive malformed count **17**, escape-aware **2** — UNCHANGED across the phase-96 ADD, verified on both sides. ⚠️ **The correct form is `sed 's/\\|//g' F | awk -F'|' '/^\| *[0-9]/ && NF!=8'` with NO file argument to awk** — passing the file makes awk IGNORE STDIN and print the NAIVE 17 under the escape-aware label. **CONFIRMED broken-when-misused at the phase-95 PLAN.** **Any gate must state WHICH FORM it uses and reconcile against the known baseline.**

⚠️ **AND DO NOT SUBSTITUTE A "REASONABLE EQUIVALENT" FOR CHECK (1)'s AWK.** `NF>=8 && $2 ~ /^ *[0-9]+ *$/` once printed **92**, not the true denominator. **Use the form above verbatim.**

**SENTINEL RUN MECHANICALLY AT THE PHASE-96 BRAINSTORM CLOSE, BOTH BEFORE AND AFTER THE ROW-96 ADD.** ACTUAL output, recorded not predicted — **and it is NOT identical across the insertion, because the ADD moved check (1) and BOTH denominator NCs:**
(1) **`NOT DONE: row 96`**, one line (it was SILENT before the ADD) · (2) **SIX** at `:206 :212 :218 :228 :234 :242` (was `:205 :211 :217 :227 :233 :241` — every anchor SHIFTED DOWN BY ONE) · (3) SILENT · NC-A **TWO** (was ONE) · NC-B **TWO** (was ONE) · NC-C fired (0 residual) · NC-D **96 / 68** · check-(2) positive control **6 -> 0 with 6 substitutions asserted**.
Per-line md5 of the six windows, **trailing newline INCLUDED** (`sed -n 'Np' f | md5sum`) — **ALL SIX BYTE-IDENTICAL across the insertion and back to the phase-95 SPEC close:**
`206 10d7807bf02d` · `212 4a92f7e62fc6` · `218 2a7eb298b9fd` · `228 242e53c6f7a3` · `234 b2680e6f4fbf` · `242 6caa1c3ce0e7`
⇒ **THE SENTINEL DOES NOT FIRE. `stop` WAS EVALUATED AND DELIBERATELY NOT CREATED** (verified absent at the git root and in every stage worktree).
⚠️ **THE md5 DIGEST IS METHOD-SENSITIVE:** the six match ONLY with the **trailing newline included**. **State the method whenever you quote the digest.**
⚠️ **THIS RUN DOES NOT CARRY FORWARD — run all three checks, all four NCs *and* the check-(2) positive control YOURSELF, at YOUR OWN tip.**

**RE-VERIFIED INDEPENDENTLY AFTER THE CLOSE, at the published tip `e99e9cb6`** (a second run at a later point, not a copy of the block above): checks (1)/(2)/(3) reproduced EXACTLY — `NOT DONE: row 96`, SIX at `:206 :212 :218 :228 :234 :242`, silent — all four NCs fired with the documented shapes (NC-A **TWO**, NC-B **TWO**, NC-C 0 residual, NC-D **96 / 68**), and the check-(2) positive control read **6 -> 0 with 6 substitutions asserted**. ⚠️ **AND EVERY FIGURE IN THIS FILE WAS RE-DERIVED AGAINST THE TREE IN THAT SAME PASS** — 23 counts plus all six window md5s, ALL reconciling: ROADMAP 246/128 with row 96 at line 158, `BRAINSTORM.md` 761, `STATE.md` 65, `STATE_HISTORY.md` 556 with 163 + 65 = 228, `DECISIONS.md` 18942 / 216 / 316 / 324 with tail `## ADR-0317`, `BEHAVIOR_CONTRACT.md` 5989, phase dirs 137, fixtures 122 with `0121` FREE, `internal/tls/config.go` 666, `internal/listener/manager.go` 1636, `go list` 237 / 235, fuzzers 56 / 48. **THE SENTINEL WAS EVALUATED A SECOND TIME AND `stop` WAS AGAIN DELIBERATELY NOT CREATED** (two independent reasons: check (1) is non-silent AND check (2) reads SIX). ⚠️ **THIS RE-VERIFICATION STILL DOES NOT CARRY FORWARD — it is provenance that the figures were true at `e99e9cb6`, NOT a substitute for your own run at YOUR tip.**

⚠️⚠️ **NEVER RE-SPELL A SENTINEL MATCH PHRASE INSIDE A SENTINEL WINDOW** (the phase-94 rule: the sentence asserting the sentinel could not move MOVED IT, six -> seven, inside `:226`). **A SPEC DOES NOT EDIT `ROADMAP.md`, SO THIS RULE IS DORMANT THIS SESSION — BUT IT GOES LIVE AGAIN AT THE IMPL.**

- ⚠️ **LIVE FIGURES RE-DERIVED AT THIS TIP (the phase-96 BRAINSTORM close):** `ROADMAP.md` **246** lines / **128** data rows / tail row **96** (**`in-progress`**) · `DECISIONS.md` **18942** · `^---$` **216** · `^## ADR-` **316** · bare `^## ` **324** · tail **ADR-0317** · next-free **ADR-0318** (`^## ADR-0318` reads **0**) · house `PROPOSED` **DISARMED at 0** and **PROVEN LIVE on a scratch copy** (append a house-form line ⇒ reads 1) · ADR-0231 decoy **1** at `:14866`, resolved BY BACKWARD HEADING SEARCH to `14864 ## ADR-0231` · middle-ground `^\*\*Status:\*\*.*PROPOSED` **23** (NEVER a gate) · `BEHAVIOR_CONTRACT.md` **5989**, and ⚠️ **`:1944` IS 1841 BYTES / 1830 CHARS — NOT the 357 the phase-95 router carried** (see method note 32) · `STATE.md` **65** (rolled IN PLACE) · `STATE_HISTORY.md` **556** (554 -> 556, **+2**), strict **163 (DELTA 0, as promised)** / parenthetical **64 -> 65** / loose **227 -> 228** (**163 + 65 = 228 exactly**) · phase dirs **137** · fixtures **122** (tail `0120-tls-connection-error`; **`0121` is the next free index**) · fixture extractor **122 = 122**, both `comm` directions EMPTY, split **98 `driver/` + 24 `inputs/`**, and the extractor **NC'd** (a rename fires BOTH `comm` arms while the count stays 122) · fuzzers **56 targets / 48 FILES** · `go.mod` **67 REQUIRE ENTRIES** (⚠️ the `grep -cE '^\s+[a-z0-9./-]+ v[0-9]'` form reads 62 — it drops FIVE entries with uppercase or `_`; see method note 33) · `go list ./...` **237** (**235** after excluding the two Docker drivers) · BackendKind tail **38** (`test/differential/fixture/fixture.go:614`) · `-family row` **96 / 68** · ⚠️ **`internal/tls/config.go` is 666 lines — NOT the 642 ROW 95 ASSERTS TWICE** (method note 32) · `internal/listener/manager.go` **1636**.
- ⚠️ **NO GATES WERE RUN AT THIS CLOSE, AND THAT IS THE STAGE SCOPE, NOT AN OMISSION.** A BRAINSTORM is docs-only; the six-gate posture belongs to the IMPL. The only executions were the BRAINSTORM's own probes (§2, §6.1) and `./internal/listener/` green after the prototype was reverted under `sha256sum -c`.
- ⚠️ **THE STAT SURFACE IS QUOTED AS A DELTA AND NEVER AS AN ABSOLUTE.** Three different absolutes are live at one tip. **Phase 96 is forecast to land +0 names** — the five `ssl.*` names already exist; what the row changes is WHETHER THEY ARE REGISTERED on a listener whose only TLS is its default chain. ⚠️ **THAT FORECAST IS NOT YET MEASURED CROSS-SIDE and it is the SPEC's blocking unknown.** The ledger is at `### Stat surface`, `BEHAVIOR_CONTRACT.md:5071`; by its own convention at `:5130` a `+0` row earns NO chain entry.
- ⚠️ do NOT "fix" the six in check (2) and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases; **no gate may rest on a ` + ` split** (wrong on 3 of the 6 windows).

==============================================

## STATUS AS OF 2026-09-07: PHASE 96 (`listener-default-chain-tlsmode`) **OPENED**. Lifecycle-state **DONE -> 1**; ROADMAP row 96 ADDED `in-progress`. **THE SENTINEL WAS RE-EVALUATED MECHANICALLY AFTER THE ADD AND DOES NOT FIRE** — check (2) reads SIX. `stop` was NOT created. **THIS SESSION RUNS THE PHASE-96 SPEC.**

**What phase 96 chartered, in one paragraph.** A `default_filter_chain` carrying a `transport_socket` on a listener whose `filter_chains[]` are plaintext or absent leaves `listenerRuntime.tlsMode` **false**, so the five `ssl.*` counters are never registered — while the per-connection Inc guard is a **different predicate** (`selected.tlsCfg != nil`) that is **true**. The first completed downstream TLS handshake then dereferences a nil `*stats.Counter` and **SIGSEGVs the PROCESS**, remotely, with no `recover()` anywhere on the path. **REPRODUCED BY EXECUTION on TWO distinct shapes** — zero `filter_chains[]`, and a plaintext chain made ineligible by `destination_port` (the exact INVERSE of the arrangement ADR-0080 §Consequences (c) blesses) — each panicking at `manager.go:1393` via `stats/counter.go:22`. **ROOT CAUSE: `anyTLS` was correct as a mixed-TLS cross-check accumulator and phase 74 REUSED it as the stat-registration gate, inheriting an invariant ADR-0080 §Decision 3 had already repealed at phase 07.2.**

### ⚠️ WHAT THE PHASE-96 BRAINSTORM REFUTED — READ THIS BEFORE TRUSTING ANY INHERITED FIGURE

**THIRTEEN claims refuted by execution.** The four load-bearing:

1. 🔴 **ROW 95 SHIPPED A FALSE LINE COUNT IN THE COMMIT THAT WROTE IT.** Row 95 asserts `internal/tls/config.go` went `582 -> 642` and separately convicts its own PLAN: *"`config.go` 629 prediction is STALE (it landed 642)"*. Measured at every phase-95 commit, the file is **666** at `0f3b98b3` — **the very commit that wrote the sentence**. Not stale by drift; **wrong at birth**, inside the row whose whole subject was a false present-tense claim. ⚠️ **A CORRECTION IS NOT A MEASUREMENT** — row 95 replaced 629 with 642 by reasoning about which prediction was newer, not by running `wc -l`.
2. 🔴 **`PROGRESS.md` §9 F-E's *"NOT a safety hole"* IS TRUE OF ONE SHAPE AND FALSE OF THE CLASS.** The phase-95 controller AND its scoped re-reviewer measured it independently and agreed — and both enumerated the **same single config**. A third member escapes the Start-time net entirely: a QUIC listener with a valid `filter_chains[0]` PLUS a `transport_socket`-less default chain BOOTS AND STARTS, because `quicTLSConfig()` falls through to the filter chain while `quicChain()` returns the default chain **unconditionally**. **The two accessors name DIFFERENT chains and neither comment mentions the other's predicate.** ⚠️ **TWO INDEPENDENT AGREEING MEASUREMENTS DO NOT MAKE A CLAIM GENERAL WHEN BOTH EXAMINED THE SAME SHAPE.**
3. ⚠️ **THE BANKED *"`NegotiatedProtocol` IS THE ONLY SUCH ASSERTION IN 122 FIXTURES"* IS FALSE** — FOUR assertion sites across THREE fixtures (`0004/driver.go:962` and `:1228`, `0119/driver.go:468`, `0120/driver.go:310`). Phase 95 narrowed a class into a claim that was **already false when written**. **STRUCK from the candidate list.**
4. ⚠️ **THE BANKED `len(helpText)` GUARD IS A VACUOUS-GUARD TRAP.** `TestHelpText_KeySetExact` already does set equality in BOTH directions, so a cardinality guard is **UNREACHABLE**. **STRUCK.** Its two ungated prose counts were re-verified and are **currently correct** (31 / 31 / 5; 10+1+1+3+1+10+5 = 31 reconciles).

Also refuted: the router's own `:1944` figure (**357** where the row it closed says 357 -> 1841; measured **1841 bytes / 1830 chars**, and the sibling `:1971` published figure is a CHAR count where this one is BYTES) · the banked port-race cost, wrong in its **count** (37 files, not ~36), its **split** (31 panic / 5 `fmt.Errorf` / 1 other, not 30/4) **AND its path** (`test/differential` holds NINE `.go` files; the drivers live under `test/fixtures/*/driver/` and `*/inputs/`, including FOUR UDP sites a TCP-only axis is blind to) · one of the four banked stale citations is **CORRECT** and the claim's own anchor for it is wrong (`:4482`, actually `:4597`), with a NEW stale one found at `manager_test.go:5571` · the `go.mod` require form drops FIVE entries by character class (62 vs **67**) · the banked SDS/ALPN set sizes are BOTH wrong (SDS **6**, or 5 scoped to TLS-SDS; downstream `alpn_protocols` in config YAML **2**, not five) though the intersection is genuinely EMPTY · and the reference REFUTES the framing offered for the router's own recommendation (below).

### ⚠️ WHAT PHASE 96's BRAINSTORM MEASURED THAT THE SPEC MUST NOT RE-DERIVE

- **The full divergence set between the `filter_chains[]` loop and the `default_filter_chain` block: TEN divergences, EIGHT DELIBERATE, TWO unexplained.** ⚠️ **D4 IS NOT THE BUG AND ITS COMMENT MUST NOT BE CHANGED** — ADR-0080 §Decision 3 authorises the mixed-TLS exemption VERBATIM. D8 (`filter_chain_match` ignored on the default slot) is **proto-mandated** by upstream's own *"The filter chain match is ignored in this field."* The two unexplained are **D1-TLSMODE (this row)** and **D10-QUICSEL (banked)**.
- **The reference, on the pinned digest `sha256:7edd5b0fd763…`** (verified against `ENVOY_TARGET.md:3-4`), for the sibling QUIC member: it **REJECTS** the no-`transport_socket` default chain at config-init with the **BYTE-IDENTICAL** message it uses for `filter_chains[]` (*"no transport socket specified for connection oriented UDP listener"*) — **it does not distinguish the two slots at all**, so a boot reject there is **PARITY, not a departure**. The larger hypothesis that the reference refuses `default_filter_chain` on a UDP listener outright is **REFUTED — it accepts it.** ⚠️ **THE DIVERGENCE BITES ONLY IN VALIDATE MODE**: `envoy-go -mode validate` returns rc=0 `configuration OK` where the reference returns rc=1. Serve-mode posture already agrees (envoy-go's Start failure aborts the process; the port is NOT left bound).
- **The cost FLOOR: ONE production line, `+1/-1`** — `tlsMode: anyTLS || (defaultChain != nil && defaultChain.tlsCfg != nil)`. With it the crashing shape SERVES and registers all five `ssl.*` names. **Explicitly a LOWER BOUND** ([[reference_measured_prototype_is_a_lower_bound]], thirteen consecutive rows).
- **The test-pin surface:** the `filter_chains[]` QUIC reject is pinned by **exactly one** test (`TestBuildListenerRuntime_QUICMandatoryTLS`); the Start-time `quic listener has no TLS config` reject is pinned by **NO test at all**; `TestListenerMetrics_GateMatchesInc` — the guard named for exactly this equivalence — is **VACUOUS on the only arm that matters** (all three arms build `filter_chains[]`-only listeners); and `TestParseDefaultFilterChain_Plaintext_WithTLSFilterChain` covers only the **safe** direction.
- **Fixture reality: the differential cannot see ANY of this.** `default_filter_chain` appears in ONE fixture (`0008`, fully **plaintext** — `grep -c transport_socket` reads 0); `quic_options` in ONE (`0104`, `filter_chains[]` only). **The intersection is EMPTY.** Deleting the fix leaves all 122 fixtures green.

### ⚠️ THE SPEC's BLOCKING UNKNOWN — DO NOT SKIP IT

**Does the reference emit `ssl.*` on a listener whose only TLS is its `default_filter_chain`?** UNMEASURED. It **chooses between the two fix shapes**: widening `tlsMode` registers five names; nil-guarding the five Inc sites registers zero. **Both stop the crash and they disagree about the stat surface.** ⚠️ **TWO REPAIRS THAT DIFFER ONLY IN AN UNMEASURED STAT SURFACE ARE NOT INTERCHANGEABLE, AND DIFF SIZE IS NOT THE TIEBREAKER.** A SPEC that lands the one-liner without this arm is asserting parity it has not measured.

### Banked, NOT chartered

- **D2-QUICTS — the QUIC no-`transport_socket` deferred reject.** ~4 lines mirroring the arm the loop already carries; **reference-confirmed PARITY**; validate-mode rc=0-vs-rc=1 is its only externally-observable half. **The SPEC may fold it in and must say either way.**
- **D10-QUICSEL — the `quicChain()`/`quicTLSConfig()` accessor split.** Repairing it means DECIDING multi-chain QUIC dispatch (ADR-0080's fallback semantics vs `quic.go:68-73`'s documented single-chain assumption). **Needs its own ADR.**
- ⚠️ **THE `stat_prefix` DUPLICATE-REGISTRATION PANIC — the strongest banked candidate, found incidentally while measuring the reference.** Two listeners sharing an HCM `stat_prefix` — a common, valid Envoy config the reference **ACCEPTS** — make envoy-go **panic rc=2** (`stats: duplicate metric registration: "http.ingress_http.downstream_rq_total"`, `internal/stats/registry.go:107` via `internal/filter/hcm/config.go:358`), including under `-mode validate`. Isolated on a QUIC-free two-listener arm, so it is not a confound. **It deserves its own row.**
- **The driver-owned receiver port race — 37 files** (31 panic / 5 `fmt.Errorf` / 1 other), under `test/fixtures/*/driver/` and `*/inputs/`, NOT `test/differential/`. ⚠️ **Its trigger has still NOT fired** (phase 95 ran two full 122/122 suites, no abort). Do not open it on the note alone.
- **An SDS + `alpn_protocols` fixture — the runner-up.** Intersection genuinely EMPTY; adds coverage rather than fixing a defect.
- The false invariant comments at `manager_test.go:2325-2332` and `:4993-4998` · the six stale citations plus the new `(:4484)` at `manager_test.go:5571` · `0108`'s three false `ssl.*` confessions · `0118/driver/driver.go:31`'s falsified band (0108-0113 is **10444-10449**, so 10450 is one PAST its end) · `0120/expectations.yaml`'s *"Phase 94 fixture"* first line · the nine-or-twelve history-class `GetConfigForClient` comment sites (**a CONTESTED count — quote NO number**) · `0061-lb-ring-hash`'s σ-margin second occurrence · 1xx interim responses · the H/1 no-`Host` divergence · the pooled-upstream-lifetime defect · the other TEN fixed `ssl.*` names and FOUR dynamic families (blocked on NAMING).

## ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + ALL FOUR NCs + THE CHECK-(2) POSITIVE CONTROL YOURSELF** and record ACTUAL output. ⚠️ **NC SHAPES CHANGE ACROSS A ROW CHANGE — NEVER INHERIT ONE.** They changed TWICE at phase 95 (the flip) and again at the phase-96 ADD, in the opposite direction.
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** 94 IMPL twelve · 95 BRAINSTORM nine · 95 SPEC eleven · 95 PLAN seventeen · 95 IMPL eighteen · **96 BRAINSTORM THIRTEEN** — and the SOURCE matters more than the count: three of the thirteen were in documents the router told the session to trust, including the router itself. ⚠️ **EXPECT TO BE REFUTED BY YOUR OWN REVIEW SEAM, NOT ONLY BY YOUR SUCCESSOR.** ⚠️ **AND WHEN TWO AGENTS CONTRADICT EACH OTHER, FIND THE VARIABLE RATHER THAN PICKING A WINNER.**
3. **LINE ANCHORS DRIFT; SYMBOL AND LITERAL-TEXT ANCHORS DO NOT — RE-VERIFY EVERY CITE AT YOUR TIP.** ⚠️ **PATHSPEC-SCOPE every symbol assertion and every `sed`.** ⚠️ **`internal/filter/hcm/chain.go` DOES NOT EXIST — it is `internal/filter/http/chain.go`** — but **`actions.go` IS in `hcm`.**
3a. ⚠️ **AN ANCHOR CAN BE NON-UNIQUE.** **Anchor on the ENCLOSING SYMBOL.**
3b. ⚠️ **CHARACTER-CLASS AND ESCAPE SELECTORS ARE THE FAIL-UNSAFE FAMILY.** `^[0-9]{4}-` on fixtures reads 120, dropping `0007a`/`0007b` (use `ls -d test/fixtures/*/ | wc -l`) · **a CASE-SENSITIVE GREP IS BLIND TO UPPERCASE PROSE — use `-i` when auditing comments** · and see note 33.
3c. ⚠️ **`feedback_brief_citations_not_evidence` APPLIES TO INCIDENTAL FIGURES — including every number in THIS file, in `BRAINSTORM.md`, and in every AGENT REPORT.** At the phase-96 BRAINSTORM four measurement agents ran; every headline figure was re-derived by the controller before use, **and doing so caught the router's own `:1944` and `config.go` figures.**
3d. ⚠️ **NO GATE MAY REST ON A ` + ` SPLIT.**
3e. ⚠️ **A GATE COMMAND CAN MATCH PROSE INSTEAD OF CODE.** Anchor fixture-set extraction on the blank-import LINE:
```sh
extract () { grep -oE '^[[:space:]]*_ "github\.com/pgdad/envoy-go/test/fixtures/[^/]+/(driver|inputs)"$' "$1" \
  | sed -E 's#.*/test/fixtures/##; s#/(driver|inputs)"$##' | sort; }
```
**At this tip: dirs 122 = imports 122 (`test/differential/runner_test.go`), both `comm` directions EMPTY**, split **98 `driver/` + 24 `inputs/`**. **NC the extractor** (rename one import in a SCRATCH COPY — it must fire in BOTH `comm` directions). ⚠️ **A COUNT-ONLY CHECK IS VACUOUS: the import count stays 122 under a rename** — RE-PROVEN at the phase-96 BRAINSTORM.
3f. ⚠️ **"AT THE RETURN" IS NOT A PLACEMENT.** Anchor installs on the ENTRY of the path that must be covered and PROVE the install with an arm that reddens without it.
3g. ⚠️ **A "CHEAP ASSERTION GAP" CAN BE A BEHAVIOUR DIVERGENCE WEARING A DISGUISE.** Measure the reference for THAT ARM before asserting any cross-side stat.
3i. ⚠️ **A LOOSE `--grep 'phase NN.*STAGE'` MATCHES THE WRONG STAGE.** **Anchor stage-commit locates on `^phase NN (slug) STAGE:`.**
3j. ⚠️ **`grep` IS A DIFFERENT PROGRAM IN A SUBAGENT SHELL THAN IN THE CONTROLLER SHELL.** In the CONTROLLER shell `grep` is a **function** resolving to **ugrep 7.8.4** which honours `.gitignore` and is therefore **BLIND to `next-prompt.txt`**; in a SUBAGENT shell `grep` is **`/usr/bin/grep` (GNU 3.11)** and sees it fine. **Both readings are correct; the variable is the SHELL, not the tree.** In the controller shell use `command grep -rl`, `/usr/bin/grep`, `git grep`, or a direct path; ⚠️ **`command grep` DOES NOT SURVIVE `xargs`.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files). **NAME THE SHELL AS WELL AS THE TOOL.** ⚠️ **The flag-trap error TEXT differs between the two (`option -f: cannot read amily row` vs `No such file or directory`) but rc=2 is identical** — do not gate on the message.
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Live: TWO SDS dial-budget flakes plus `TestSDSEndToEnd_FetchFailure_BootFailsClosed` · the driver-owned receiver port race · `internal/httpclient` zero-value · the two 84.2-era flakes · the REFERENCE h2spec section-8 flip · `TestOutlierDetector_ConcurrentEjectExactlyOnce` · `0061-lb-ring-hash`'s σ-margin · `TestP83_StopPauseTimer_IsAuthoritativeAgainstAnEnteredClosure` (wasm, 1/400) · **`internal/filter/hcm/h2 TestFramer_ReaderGoroutineDoesNotLeak`** — ⚠️ **ISOLATED AT THE PHASE-96 BRAINSTORM AND IT DID NOT REPRODUCE**: 5/5 and 257/257 green, both non-vacuous (`RUN` asserted beside `RC`). **BUT ITS GUARD CARRIES SLACK 2 OVER 40 CONNECTIONS AND REPORTED `delta=0` EVERY ITERATION, so a small real leak would sit INSIDE that margin. UNREPRODUCED IS NOT DISPROVEN; the registration STANDS.** ⚠️ **`TestServerConn_TinyWindowDelivery` IS NOT A FLAKE** — a recurrence is a REGRESSION of row 91. ⚠️ **A GREEN RERUN CLEARS NOTHING.**
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only). ⚠️ **AT THE PHASE-96 BRAINSTORM A SIBLING `curl-world` SESSION STILL OWNED SEVENTEEN CONTAINERS holding `18080-19000`, up 2 days.** ⚠️ **A `reaper_*` testcontainers Ryuk container is created BY THE DIFFERENTIAL ITSELF and is REUSED; leave it alone.** **Treat NO roster as stable.**
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`. ⚠️ **`INNER_EXIT` DOES NOT EXIST in this repo.**
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL, THEN DRIVE THE ARM.** **`gofmt -l` never exits non-zero — gate on OUTPUT.** ⚠️ **go test WITHOUT `-v` PRINTS ZERO `=== RUN`** — `RUN=0` beside `RC=0` is a VACUOUS GREEN. ⚠️ **A `-run` selector matching nothing prints `[no tests to run]` and EXITS 0.** ⚠️ **On `-v` output, unanchored `grep -c 'FAIL'` reads nonzero ON A FULLY GREEN TREE** — use `grep -cE '^(FAIL|--- FAIL)|^ *--- FAIL'`.
7b. ⚠️ **`grep -c` ON ZERO MATCHES PRINTS `0` AND EXITS 1** — capture, NEVER chain with `&&`, NEVER `$(… || echo 0)`.
7c. ⚠️ **A GREEN TEST RUN IS NOT EVIDENCE A SITE IS EXERCISED** — prove it with a **`panic()` REACHABILITY CONTROL**. **There is NO `recover()` in non-test `internal/tls` or `internal/listener`, so a panic aborts the binary — gate on `grep -cE '^panic:'`.** ⚠️ **AT PHASE 96 THAT FACT IS THE SUBJECT, NOT JUST THE METHOD.**
7d. ⚠️ **NC EVERY NEW TEST, AND NC YOUR OWN FIXES** — NEUTRALISE so the package still compiles; check the NC is EXECUTABLE.
7e. ⚠️ **A UNIT TABLE OVER A SHARED ASSERTION IS NOT A GATE ON PRODUCTION BEHAVIOUR** — `t.Helper()` gives each failure a DISTINCT call-site line.
7f. ⚠️ **A NC THAT LEAVES YOUR NEGATIVE CONTROL GREEN IS NOT EVIDENCE THAT CONTROL DOES ANY WORK.**
7g. ⚠️ **ASSERT *WHICH* ERROR FIRED, AND ALSO WHICH DID NOT** — ⚠️ **BUT NOT VIA A CLIENT-VISIBLE ERROR STRING IF THE ALERT CODE IS VERSION-DEPENDENT.** **Use the server-side counter triple.** ⚠️ **KEEP TABLE ROWS SINGLE-CAUSE.** ⚠️ **PREFER AN EXACT EQUALITY TO A FLOOR.**
7h. ⚠️ **A PROBE CLIENT CAN WITHHOLD THE VERY THING THE ARM EXISTS TO TEST.** ⚠️ **`openssl s_client` answers "did the handshake complete", NOT "was the connection served"** — drive a real request AFTER the handshake. ⚠️ **A TEST TABLE CAN CARRY AN INVENTED INPUT NO PRODUCTION PATH PRODUCES** — build contexts with the package's OWN helpers.
7i. ⚠️ **A GATE THAT READS 0 HAS NOT BEEN SHOWN TO WORK.** Run every silent gate over an input KNOWN to trip it. **The house `PROPOSED` guard reads 0 and was PROVEN LIVE at this close on a scratch copy; a 0 there is the RESTING STATE.**
7j. ⚠️ **A PIN CAN FAIL AGAINST *CORRECT* CODE.** **Before writing an expectation map, MEASURE the leaf set a completing handshake actually books.**
7k. ⚠️ **ASSERT THE POINTERS, NOT ONLY THE EFFECT.** At phase 96 `sslHandshake == nil` is the CAUSE and the SIGSEGV is the EFFECT; a probe that observes only the crash proves something happened, not what. Assert both, in that order ([[reference_nil_stats_counter_inc_crashes_goroutine]]).
8. ⚠️ **`pgrep -f` / `pkill -f` MATCHES YOUR OWN SHELL AND KILLS THE TOOL CALL (exit 144)**. **Kill ONLY PIDs you captured yourself at launch.** ⚠️ **`pgrep -la a b` ERRORS** — one pattern per call.
9. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-*`, `wt-*` or stage worktree should outlive its stage close** — expect only `master` (verify with `git worktree list`).
10. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** (a) differential — **122/122 expected**; (b) non-Docker sweep gated on `PIPESTATUS[0]` + a SET RECONCILIATION — **235 packages**; (c) h2spec `95 tests, 94 passed, 1 skipped, 0 failed`; (d) fuzzers **56 / 48**; (e) the ANCHORED panic gate `^panic:|DATA RACE|SIGSEGV`, **0** *and PROVEN LIVE*; (f) no REVIEW.md — standing departure. ⚠️ **`-race` on the DIFFERENTIAL suite is VACUOUS** — the subject is an unraced subprocess. **THE IMPL RUNS ALL SIX; A SPEC RUNS NONE, AND THAT IS SCOPE, NOT OMISSION.**
11. ⚠️ **THE DIFFERENTIAL'S FAILURE MODE IS A SILENT PASS.** `-count=1` IS NOT OPTIONAL. **ASSERT THE FIXTURE SET BY NAME, BOTH DIRECTIONS.** The full suite takes ~400s.
11b. ⚠️ **`go test ./...` DRIVES DOCKER — AND THERE ARE TWO DOCKER DRIVERS.** Exclude BOTH: `go list ./... | grep -vE '/test/differential$|/test/conformance/h2spec$'`.
12. ⚠️ **`-p` PUBLISHING WORKS; `--network host` IS WHAT DOES NOT.** ⚠️ **`STRICT_DNS` + `host.docker.internal` + `--add-host=host.docker.internal:host-gateway` IS REQUIRED for a container-to-host cluster HERE.** ⚠️ **THE REFERENCE CONTAINER CANNOT READ `filename:` CERT PATHS** — use `inline_string:` — ⚠️ **AND INDENT THE PEM ONE LEVEL DEEPER THAN `inline_string:`**. ⚠️ **A PROBE BACKEND MUST BIND `0.0.0.0`, NOT `127.0.0.1`.** ⚠️ **Keep PEM substitutions OUT of YAML comments.** ⚠️ **envoy-go REJECTS `match.headers`**, **refuses `codec_type: HTTP2` without TLS unless `--allow-h2c`**, **boots with `-c`, NOT `--config-path`**, **`access_log[].log_format` is BOOT-REJECTED**, **`TLSv1_0`/`TLSv1_1` in `tls_params` BOOT-REJECT**. ⚠️ **AN OMITTED `clusters` KEY BOOT-REJECTS envoy-go** — give a probe subject a placeholder STATIC cluster. ⚠️ **`ENVOY_TARGET.md` is at `docs/envoy-go/ENVOY_TARGET.md`, NOT the repo root.** ⚠️ **The pinned reference is `envoyproxy/envoy:contrib-v1.37.2`, digest `7edd5b0fd763…` — PIN BY DIGEST, verify against `ENVOY_TARGET.md:3-4`** (RE-VERIFIED IN USE at the phase-96 BRAINSTORM). ⚠️ **`timeout` rc=124 is shared by a HEALTHY server and a HUNG boot — read the OUTPUT (`starting main dispatch loop` / `envoy-go … ready`), never the code.** ⚠️ **AND envoy-go's validate mode is `-mode validate`, the reference's is `--mode validate`.**
13. ⚠️ **A RAW-FRAMER PEER MUST HOLD ONE `hpack.Decoder` PER CONNECTION.**
14. ⚠️ **`bash /dev/tcp` + `timeout cat <&3` INSIDE COMMAND SUBSTITUTION RETURNS SILENTLY EMPTY** — use Go for socket probes.
15. ⚠️ **GO DOES NOT FLAG UNUSED CONSTANTS.** Post-removal cleanup is grep-driven.
16. ⚠️ **`golangci-lint`'s misspell runs in locale US.** **Sweep British spellings in `.go` comments BEFORE the gate.** Markdown prose may use them freely.
17. ⚠️ **A GREEN RUN OF A CONTENTION-SENSITIVE TEST PROVES NOTHING.**
18. ⚠️ **AN ABSENCE CLAIM NEEDS A POSITIVE CONTROL AND A DISCRIMINATING NEGATIVE CONTROL.**
19. ⚠️ **DERIVE next-free ADR FROM THE TAIL, NEVER FROM THE HEADING COUNT.** The id space is SPARSE (exactly one gap, `0209`) and headings+1 (**316**+1) collides with `ADR-0317`, which is TAKEN.
20. ⚠️ **`reference_measured_prototype_is_a_lower_bound` HAS FIRED THIRTEEN CONSECUTIVE ROWS.** **ENUMERATE BY GREPPING THE LITERAL SYMBOL OUTWARD, CASE-INSENSITIVELY, PATHSPEC-SCOPED, AND BY COMPILING.**
21. ⚠️ **A MEASUREMENT THAT GOVERNS A DEFERRAL DECISION MUST LAND IN A GOVERNING DOCUMENT.** The phase-96 reference measurement lives in `BRAINSTORM.md` §2.4 and **must be carried into ADR-0318 at the SPEC** — a BRAINSTORM is not a governing document (the phase-94 lesson: a measurement that landed only in a BRAINSTORM was lost for seventeen rows).
22. ⚠️ **A STAGE'S FILE SCOPE IS ITSELF MEASURABLE, AND THE ROUTER CAN BE WRONG ABOUT IT.** `git show --numstat` on the precedent stage settles it. **MEASURED at the phase-96 BRAINSTORM across the 93/94/95 BRAINSTORM commits: EXACTLY FIVE FILES, identical in all three** — `ROADMAP.md` (+1), `STATE.md` (in place), `STATE_HISTORY.md` (+2), the new `BRAINSTORM.md`, `next-prompt.txt`. **For a SPEC, measure against the phase-94 and phase-95 SPEC commits — and measure MORE THAN ONE.** **A SPEC creates `SPEC.md` and drafts an ADR; it does NOT touch `ROADMAP.md`.**
23. ⚠️ **`git diff --stat` IS A SUM, NOT ADDITIONS.** Cost figures come from `--numstat`.
24. ⚠️ **AN UNESCAPED `|` IN A ROADMAP ROW PASSES CHECK (1) AND SILENTLY BREAKS THE FIELD COUNT.** Count fields (want **8**) under BOTH forms, BEFORE and AFTER installing. **Reword a pipe AWAY rather than escaping it.** **DORMANT at a SPEC; live again at the IMPL.**
25. ⚠️ **THE EVICTION CHECK IS A *PAIR*, AND THE BARE FORMS DO NOT ANSWER IT.** At the phase-96 close BOTH bare forms read **5** on `STATE.md`, and **both are invariant under which entry is evicted**. Only the LABEL-BOUND form discriminates, on BOTH files: the evictee's own label goes `STATE.md` **1 -> 0** and `STATE_HISTORY.md` **0 -> 1**. **VERIFIED IN BOTH DIRECTIONS at this close, with a fabricated-label NC (0 in both files) and a positive control on a sibling label that IS present (reads 1).**
26. ⚠️ **A DIRECT DATE READ CAN FAIL TO PICK THE EVICTEE.** The tie has now sat in the MIDDLE, AT THE OLDEST POSITION, ABSENT, and — at this close — **AS A THREE-WAY TIE AT THE HEAD**, across four consecutive closes. At this tip the five dates read `09-07, 09-07, 09-07, 09-06, 09-05`, so date and position still AGREE on the oldest, **but for a narrower reason than one close ago.** **That agreement is a coincidence of this tip, not a rule.** When a tie IS at the oldest position, fall back to LIST POSITION — the tail — never to a guess.
27. ⚠️ **A SYMBOL-SCOPED CLAIM IS NOT A PATH-SCOPED ONE.** **Enumerate the call sites and check each caller's own branch.** ⚠️ **Pathspec scoping that keeps a diff clean is the SAME scoping that hides its callers.**
28. ⚠️ **A `+0/+0` NEGATIVE-CONTROL ARM CAN BE SILENTLY DELETED WITH EVERY GATE STAYING GREEN.** **Diff the ARM ROSTER, not the counters**, and treat a shrinking roster as a finding even when every pin passes.
29. ⚠️ **A MULTI-INSERT LINE SHIFT IS BANDED, NOT A CONSTANT.** **Never hand a scalar shift for a multi-insert file — re-locate every cite by LITERAL TEXT (`grep -nF -- "<exact line>" <path>`).** Line numbers are derived data; the literal is the identity.
30. ⚠️ **A `go test` SELECTOR NAMING A PACKAGE THAT DOES NOT EXIST PRINTS `FAIL … [setup failed]` AND EXITS 1** — it reads exactly like a real red run. **Confirm a selector resolves (`go list ./path/...`) before believing a FAIL.** This is the mirror of the `-run` footgun, which fails the OTHER way (no match ⇒ EXIT 0).
31. ⚠️ **RECONCILE A CORRECTED CLAIM ACROSS ITS WHOLE OCCURRENCE SET.** **After correcting any claim, grep its distinctive phrase tree-wide and fix every hit, or state which hits you are deliberately leaving and why.**
32. ⚠️ **A ROW CAN SHIP A FALSE FIGURE IN THE COMMIT THAT WRITES IT, AND A "CORRECTION" IS NOT A MEASUREMENT.** Row 95 asserts `internal/tls/config.go` landed at **642** — measured **666** at `0f3b98b3`, the commit that wrote it — while convicting its own PLAN's 629 of being stale. And the phase-95 router carried `BEHAVIOR_CONTRACT.md:1944` as **357 chars** while the row it closed said 357 -> 1841; measured **1841 BYTES / 1830 CHARS**, with the published sibling `:1971` figure (**5098**) being a CHAR count. ⚠️ **A THREE-FIGURE GATE ON THOSE LINES WOULD FIRE AGAINST CORRECT CODE ON TWO OF THREE. NAME THE MEASURE (bytes or chars) OR THE FIGURE IS MEANINGLESS**, and **re-derive any count you quote by running the command at YOUR publishing tip.**
33. ⚠️ **A CHARACTER CLASS SILENTLY DROPS WHAT IT DOES NOT SPELL.** `grep -cE '^\s+[a-z0-9./-]+ v[0-9]'` on `go.mod` reads **62**; the count is **67**. The class excludes uppercase and `_`, dropping exactly `AdaLogics/go-fuzz-headers`, `Azure/go-ansiterm`, `Microsoft/go-winio`, `Microsoft/hcsshim`, `prometheus/client_model`. **Prefer a structural extractor (`awk` over the `require` blocks) to a character class, and diff the two when they disagree.**
34. ⚠️ **TWO INDEPENDENT AGREEING MEASUREMENTS DO NOT MAKE A CLAIM GENERAL.** Phase 95's controller and its scoped re-reviewer both measured F-E and agreed it was benign; **both enumerated the same single config**, and a third member of the class escapes the guard they relied on. **Agreement between readers is evidence about the shape they both examined, never about the class. Enumerate the class, THEN measure.**
35. ⚠️ **A GUARD'S NAME IS NOT ITS COVERAGE.** `TestListenerMetrics_GateMatchesInc` exists precisely to assert the equivalence phase 96 refutes, and every one of its arms builds a `filter_chains[]`-only listener, so its default-chain assertions never execute. **Read the arms, not the name.**
36. ⚠️ **A VARIABLE REUSED FOR A SECOND PURPOSE OUTLIVES THE INVARIANT THAT MADE IT SAFE.** `anyTLS` was correct as a mixed-TLS accumulator; phase 74 reused it as the stat-registration gate, inheriting an invariant ADR-0080 had already repealed. **When you gate a new feature on an existing boolean, prove that boolean still means what the new use needs, and NAME THE ADR THAT COULD REPEAL IT.**
37. ⚠️ **A FIX SHAPE CAN ENCODE A PARITY ANSWER NOBODY HAS MEASURED.** Two repairs that both stop a crash but disagree about the resulting stat surface are NOT interchangeable, **and diff size is not the tiebreaker. Measure the reference, then choose.**

## ⚠️ THE `PROPOSED` GUARD — CURRENTLY *DISARMED* (reads **0**); **THIS SPEC RE-ARMS IT**

| form | now | where |
|---|---|---|
| `^> \*\*STATUS: PROPOSED` — **the house form** | **DISARMED, reads 0** | phase 95 flipped it `PROPOSED` -> `ACCEPTED` in place under `## ADR-0317`. ⚠️ **A ZERO HERE IS THE EXPECTED RESTING STATE, NOT EVIDENCE THE GUARD WORKS** — it was **PROVEN LIVE at the phase-96 BRAINSTORM** by appending a house-form line to a SCRATCH COPY (0 -> 1). **THIS SPEC re-arms it 0 -> 1 when it drafts ADR-0318.** |
| `^\*\*Status:\*\* PROPOSED` — **the ADR-0231 decoy** | unchanged, reads 1 | `:14866`, resolved BY BACKWARD HEADING SEARCH to `14864 ## ADR-0231`, byte-untouched |
| `^\*\*Status:\*\*.*PROPOSED` — **the middle ground** | reads **23** | ⚠️ **NEVER A GATE** |

⚠️ **VERIFY BY LINE AND BY ADR, NEVER BY THE COUNT ALONE** — resolve any hit to its enclosing ADR by a BACKWARD heading search (`awk 'NR<=<HITLINE> && /^## ADR-/ {h=$0; n=NR} END {print n, h}'`). ⚠️ **NEVER gate on the UNANCHORED form.** ⚠️ **AND DO NOT QUOTE A COUNT FOR EITHER GATE FORM IN PROSE THE GREP MATCHES** — the phase-93 SPEC falsified itself doing exactly that. **ADR-0317 IS COMPLETE and ADR-0318 IS THE NEXT FREE ID.** `DECISIONS.md` is **BYTE-UNTOUCHED** by the phase-96 BRAINSTORM: `^---$` STAYS **216**, `^## ADR-` **316**, bare `^## ` **324**, tail **ADR-0317**.

## ⚠️ THE ARCHIVE GUARD

⚠️ **THE STRICT `163` IS NOT THE ENTRY COUNT.** Re-measured at THIS tip (phase-96 BRAINSTORM close, `STATE_HISTORY.md` **556**): loose **228** = strict **163** + parenthetical **65**, exactly — **the triple reconciles only under these ANCHORED-OCCURRENCE forms:**
```sh
H=docs/envoy-go/STATE_HISTORY.md
grep -oE '^- \*\*prior active-phase:\*\* ' $H | wc -l   # strict         163  <- DELTA 0 across a correct close
grep -oE '^- \*\*prior active-phase \('    $H | wc -l   # parenthetical   65  <- +1 per close
grep -oE '^- \*\*prior active-phase'       $H | wc -l   # loose          228  = 163 + 65
```
⚠️ The unanchored `grep -c 'prior active-phase'` counts LINES and coincides by accident; the colon form is a FOURTH regex. **Name the form or the figure is meaningless.** ⚠️ **`grep -F` WITH A PATTERN STARTING `- ` HITS THE FLAG TRAP** — pass `--` first. **The guard is SOUND as a DELTA-0 SHAPE check** (a correctly-shaped parenthetical append moves the STRICT form by 0 — verified again at this close) **but the number is a SUBSET.**

⚠️ **THE ARCHIVE'S POSITIVE CONTROLS ARE SELF-INCREMENTING.** This stage's archive line names NO control figure. ⚠️ **AND THE RAW LINE DELTA IS `+2`, NOT `+1`** — the entry is a blank line PLUS the entry line. **The phase-96 BRAINSTORM close: `STATE_HISTORY.md` 554 -> 556**, evicting the phase-94 PLAN entry (`2026-09-03`), with the strict guard DELTA 0 exactly as promised. **A SPEC close evicts the next-oldest entry if the five-entry cap still binds — but READ, DO NOT PREDICT.** ⚠️ **AND READ THE DATES *AND* THE POSITIONS: the post-roll §Recent list carries THREE entries dated `2026-09-07` at its HEAD**, so a date read now meets a THREE-WAY tie. It sits at the NEWEST positions, where it cannot affect which entry is oldest — but that is a fact to VERIFY at your tip, not inherit from this sentence.

## ⚠️ PORT BANDING

`net.ipv4.ip_local_port_range = 32768 60999`. ⇒ **BAND YOUR PROBE PORTS BELOW 32768.** ⚠️ **The differential harness reserves `20000..31007` (`harness_test.go:292-296`) and `11000..14999` (`:209-212`)**, so `15000-19000` is the ad-hoc band — ⚠️ **minus `18080-19000`, held by sibling `curl-world` containers.** **The phase-96 BRAINSTORM used `15100-15109` / `15200-15207` cleanly.** **Check availability with `ss -tan` AND `ss -uan` (ALL states), never `ss -ltn`.** ⚠️ **FOR A UNIT TABLE, USE PORT 0.** ⚠️ **`net.Pipe` DEADLOCKS a client-cert handshake — use a loopback TCP pair.** ⚠️ **FIXTURE ports follow the `10<index>` family band; the TLS/SDS fixtures `0108`-`0113` hold `10444`-`10449`, so `10450` is one PAST that band, not a member of it.**

## ⚠️ A RECURSIVE `grep` IN THE *CONTROLLER* SHELL IS BLIND TO `next-prompt.txt`

See method note **3j** — this is SHELL-DEPENDENT. In the controller shell `grep` is a **function** (ugrep 7.8.4, honours `.gitignore`); `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. `command grep -rl …` / `/usr/bin/grep` (GNU) sees it; direct-path access works. **In a SUBAGENT shell the bare `grep` is `/usr/bin/grep` and already sees it.** ⚠️ **`command grep` DOES NOT SURVIVE `xargs`.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files).

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 96 (listener-default-chain-tlsmode)' --format='%h %s'   # the SLUG form
git log --grep '^phase 96' --format='%h %s'                                    # the LOOSE form — RUN IT to CROSS-CHECK
```
⚠️ **RUN BOTH FORMS AND RECONCILE THE SETS. NO COUNT IS QUOTED HERE, DELIBERATELY — BOTH FORMS ROT.** ⚠️ **A `.*SPEC` / `.*PLAN` STYLE LOOSE GREP MATCHES THE WRONG STAGE** (method note 3i). **Take the tip from `git rev-parse master`.** ⚠️ **AND WHEN YOU LAND A CORRECTION, PUT THE FULL SLUG IN ITS SUBJECT.**

Execution style: **subagent-driven per `feedback_execution_style`**. Subagents commit LOCALLY on their own stage branch with EXPLICIT PATHSPECS; the controller merges and squashes (`feedback_subagents_no_push`, `feedback_push_to_origin`). **Pure MEASUREMENT agents should commit NOTHING and prove their tree clean.** ⚠️ **PARALLEL AGENTS IN ONE WORKTREE RACE THE GIT INDEX** — per-agent worktrees on their own branches, private scratch dirs, **Docker serialized to exactly ONE agent at a time** (four agents ran this way at the phase-96 BRAINSTORM). ⚠️ **VERIFY WHAT A SUBAGENT REPORTS — BUT ALSO BELIEVE IT WHEN IT REFUTES YOU.** At phase 96 an agent reported a remotely-triggerable SIGSEGV; the controller reproduced it independently from the code rather than from the report, and got the same panic. ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root** — build with `-o` into scratch.

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root — **it fired repeatedly again at the phase-96 BRAINSTORM.** Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` before any commit. ⚠️ **Shell VARIABLES do not survive between Bash calls** — re-export in every command. ⚠️ **AN UNQUOTED HEREDOC (`<<EOF`) COMMAND-SUBSTITUTES BACKTICKED TEXT** — use `<<'EOF'`.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE OR PROTOTYPE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — `sha256sum` BEFORE patching, `git checkout -- <path>` after, `sha256sum -c` the capture — ⚠️ **and COMMIT FIRST if anything else is pending, because `git checkout --` restores from HEAD.** ⚠️ **Also check the MAIN repo root, and `--untracked-files=all`.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads RUN THE PHASE-96 SPEC.** ⚠️ **Its §Recent is FIVE entries under the ADR-0288 cap; the head now carries THREE entries dated `2026-09-07`, so THE NEXT CLOSE INHERITS A THREE-WAY TIE — at the NEWEST positions, where it is harmless, but READ it, do not assume it away.**
2. `docs/envoy-go/phases/96-listener-default-chain-tlsmode/BRAINSTORM.md` — **761 lines. THE CHARTER.** ⚠️ **§10 is the nine-item list of what THIS stage owes**; §3.3 carries the four hazards; §2.4 carries the reference measurement that must be lifted into ADR-0318; §4 carries the rejected alternatives with re-derived costs.
3. `internal/listener/manager.go` — the `anyTLS` write inside the `filter_chains[]` loop, the single `tlsMode:` write site, `registerListenerMetrics`'s `if rt.tlsMode {` gate, and `serveConnection`'s `if selected.tlsCfg != nil {` Inc guard with its five `.Inc()` sites. ⚠️ **THIS IS THE SUBJECT'S PRIMARY SURFACE.** Anchor by LITERAL text; the line numbers in this file will rot.
4. `docs/envoy-go/DECISIONS.md` **ADR-0080** — §Decision 3 (independent TLS posture) and §Consequences (b) and (c). ⚠️ **IT AUTHORISES EIGHT OF THE TEN DIVERGENCES. DO NOT "FIX" D4.**
5. `internal/listener/quic.go` — `quicTLSConfig()` and `quicChain()`, the two accessors that disagree (D10-QUICSEL, banked), and the Start-time nil-TLS reject.
6. `internal/listener/manager_test.go` — the FALSE invariant prose at `:2325-2332`, `TestListenerMetrics_GateMatchesInc` and its vacuous default-chain arm, and `TestParseDefaultFilterChain_Plaintext_WithTLSFilterChain` (the safe direction only).
7. `docs/envoy-go/phases/95-tls-alpn-mismatch-fallback/PROGRESS.md` §8 and §9 — the precedent row's own refutations, **including F-E, which this BRAINSTORM refuted.**
8. ⚠️ **`BOOTSTRAP_PROMPT.md` IS AT THE REPO ROOT.** §5 the lifecycle state machine (appears VERBATIM TWICE, §5 and an appendix — **the second copy's offset is NON-constant; find it by heading, never by arithmetic**).

## What the next session owes
**RUN THE PHASE-96 SPEC (lifecycle 1 -> 2)** — ⚠️ **ONE STAGE PER SESSION.** · **MEASURE THE BLOCKING UNKNOWN FIRST** (does the reference emit `ssl.*` on a listener whose only TLS is its `default_filter_chain`?) and let it CHOOSE the fix shape; a SPEC that skips it asserts unmeasured parity · **DRAFT `ADR-0318`** (TAIL-derived, next-free; §Context only; house `PROPOSED` form, which this stage re-arms 0 -> 1 — and a 0 is the RESTING STATE, so prove the guard live on a scratch copy) and **CARRY THE §2.4 REFERENCE MEASUREMENT INTO IT**, because a BRAINSTORM is not a governing document (method note 21) · **PIN BOTH crashing shapes**, not just the zero-chain one — the `len(chains) == 0` guard is NOT the boundary · **DECIDE EXPLICITLY whether D2-QUICTS rides** and say why either way · **CORRECT the false invariant prose** at `manager_test.go:2325-2332` and `:4993-4998`, reconciling the corrected claim across its WHOLE occurrence set (method note 31) · **A SPEC DOES NOT TOUCH `ROADMAP.md`** — row 96 STAYS `in-progress`, `want` STAYS **128**, `ROADMAP.md` STAYS **246**; measure the SPEC file scope against the 94 and 95 SPEC commits with `git show --numstat`, more than one · **RE-RUN the sentinel + all four NCs + the check-(2) positive control YOURSELF** and record ACTUAL output; the shapes should hold across a SPEC, but MEASURE · **REFUTE YOUR PREDECESSOR BY EXECUTION** and record it — ⚠️ **and note this BRAINSTORM refuted the ROUTER ITSELF twice (method note 32), so re-derive every figure you quote from it** · `STATE.md` rolled IN PLACE with the oldest §Recent entry evicted — ⚠️ **read the DATES *and* the POSITIONS; there is now a THREE-WAY tie at the NEWEST positions** — and archived to `STATE_HISTORY.md` as **ONE INLINE LINE** in the PARENTHETICAL form, **strict guard 163 DELTA 0**, raw delta **+2**, ⚠️ **name NO positive-control figure in the archive line** · **ROLL THE §Recent PREAMBLE SENTENCE TOO, without spelling the evictee's label** · ⚠️ **RE-DERIVE ANY LINE COUNT YOU QUOTE IN `STATE.md` IN THE SAME COMMIT AS THE EDIT THAT MOVES IT** · `next-prompt.txt` rolled (`git add -f`) · ONE squashed commit, merged and pushed — ⚠️ **and if a late measurement invalidates something you already pushed, land a CLEARLY-LABELLED correction commit (WITH THE FULL SLUG IN ITS SUBJECT) rather than force-pushing or leaving it standing.**
