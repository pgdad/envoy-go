=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-VERIFIED 2026-07-18 (re-run MECHANICALLY at the phase-68 SPEC close, in the stage worktree) — it does NOT fire. `stop` was NOT created.** Actual output:
- **(1) prints `NOT DONE: row 68`** — row 68 stays `in-progress` through the SPEC (it flips `done` only at the phase-68 IMPL six-gate, ADR-0106, the SOLE leg).
- **(2) prints THREE live "candidates:" sentences** — HTTP/3, xDS, Observability ⇒ three families STAY OPEN. **Cite the command, never the adjective** (`reference_sentinel_deferred_sentence_live_vs_historical`): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` ⇒ **3**.
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** ⇒ THREE families never opened. Operational-tooling also STAYS OPEN.

Checks (2)+(3) print ⇒ the sentinel does NOT fire. Abundant work remains.

==============================================

## STATUS AS OF 2026-07-18: THE PHASE-68 SPEC IS COMPLETE AND LANDED. ROW 68 IS `in-progress`. THIS SESSION PERFORMS THE PHASE-68 PLAN.

**What the phase-68 SPEC landed** (`git log --grep 'phase 68'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): the SPEC (`docs/envoy-go/phases/68-tls-cvc-empty-dynamic-fallback/SPEC.md`) resolved the full D-EDF-* docket for `tls-cvc-empty-dynamic-fallback` and DRAFTED ADR-0290 §Context (the DECISIONS tail flipped ADR-0289 → ADR-0290 at the SPEC commit; next-free ADR-0291). **Headline resolutions, EXECUTED-evidence-backed:** **D-EDF-CLASSIFY → NARROW (mandatory)** — a discriminating SPEC-time probe (`envoyproxy/envoy:contrib-v1.37.2`, fresh container + fresh SotW SDS process per arm, forced-send clients) AND an INDEPENDENT from-scratch reproduction pinned it: the reference ACKs + falls back to `default_validation_context.trusted_ca` + SERVES ONLY on the specifier-unset shape (S1 — CA_A accepted, CA_B alert-48 rejected, no-cert rejected); a set-but-empty `trusted_ca:{inline_bytes:""}` (S2, "DataSource cannot be empty") and a corrupt PEM (S3, "Failed to load trusted CA certificates") are **NACKed WITHOUT fallback** → fail-closed per-connection. **The boot discriminator is ACK vs NACK (`update_success` vs `update_rejected`)**, NOT `init_fetch_timeout` (all arms responded promptly; S2/S3 bind then fail-closed, they do NOT init-hold — a sharpening of the BRAINSTORM). So envoy-go falls back ONLY on S1 via a new exported sentinel `xds.ErrEmptyValidationContext` (sited in `stream.go` beside `errValidation`; wrapped in `parseValidationSecret` at the specifier-unset site; preserved through `applyValidationResponse` via dual-`%w` because today's `%w: %v` at `stream.go:164` FLATTENS). **FIVE BRAINSTORM corrections:** C1 CLASSIFY resolved + discriminator sharpened · C2 the envelope edits TWO production files (`internal/tls/config.go` + `internal/xds/{secret,stream}.go`), not one · **C3 `test/helpers/sdsserver` IS TOUCHED** — a new `WithEmptyValidationContext` Option is required (`WithValidationContext(name,nil)` yields S2 not S1, empirically confirmed) · **C4 D-EDF-PGV is a NON-ISSUE** — E1 (`config.go:427-429`) already boot-rejects a CVC missing `default_validation_context` · C5 P4 is the load-bearing gating premise. **D-EDF-STREAMPOSTURE → option (b)** (accept + NAME the residual S1-only NACK/`update_rejected` divergence). **D-EDF-FETCHSHAPE → (a)**; **D-EDF-EMPTYBOTH** → the phase-67 no-anchor routing; **D-EDF-FIXTURE** `0111` (port 10447, three-arm forced-send); **D-EDF-FUZZSEED** +0 fuzzers. **Adversarially verified** by THREE independent verifiers (code-claims · process/consistency · from-scratch probe reproduction) — ZERO SEVERE/MODERATE, three MINOR precision notes folded in. Counts UNCHANGED (docs-only apart from the DECISIONS append): fixtures **112** · fuzzers **55** · stat **1201** · BackendKind **38** · go.mod modules **2** · DECISIONS tail **ADR-0290** (next-free **ADR-0291**).

**⚠️ PUSH (idempotent — evaluate, then act):** FIRST ACTION: `git fetch`; if origin/master is BEHIND local master and the local tip is the phase-68 SPEC squash, `git push origin master` per `feedback_push_to_origin`; if in sync, proceed; if REJECTED (diverged), STOP and reconcile — never rebase away landed squashes.

## What THIS session does — phase 68 PLAN

One stage per session: PLAN ONLY (docs-only — a `PLAN.md` + `PROGRESS.md` in the phase directory; ZERO production `.go`). Fresh worktree off master, branch `phase-68-tls-cvc-empty-dynamic-fallback-plan`, per `feedback_git_worktrees`. **RE-DERIVE the SPEC rather than execute it** — a router pointer is not evidence (`feedback_brief_citations_not_evidence`); where it cites, go look; where it claims control flow, walk the call graph; default to REFUTED (`reference_quoting_is_not_executing`). **Row 68 STAYS `in-progress`** (the IMPL flips it `done` at the six-gate). **ADR-0290 stays as its §Context draft** — the PLAN adds NO ADR content (§Decision/§Consequences land at the IMPL); the DECISIONS tail stays ADR-0290, next-free ADR-0291.

**The PLAN owes — decompose SPEC §10 into an ordered TDD spine (T1-T9-ish), RE-DERIVING each anchor:**
- **The NARROW-classifier spine** (SPEC §3.2/§3.8): T-order the `internal/xds` edits (define `ErrEmptyValidationContext` in `stream.go`; wrap it in `parseValidationSecret` at the specifier-unset site ONLY — NOT the `empty resources` branch, `stream.go:159-161`; dual-`%w` in `applyValidationResponse` at `stream.go:164`) BEFORE the `internal/tls/config.go` fallback branch that consumes it. Unit tests: S1 → both `errValidation` AND `ErrEmptyValidationContext` visible to `errors.Is`; S2 (`AppendCertsFromPEM`=false) + S3 → `errValidation` ONLY (no sentinel) → boot-FAIL.
- **The `config.go` fallback branch** (SPEC §3.1/§3.6): on `errors.Is(err, ErrEmptyValidationContext)`, fall back via `vct.CombinedValidationContext.GetDefaultValidationContext()` (`dvc`) → mirror the inline default arm (`config.go:199-214`): `dvc.GetTrustedCa()==nil` (empty-both) → require=true the retained `:203` reject / require=false `NoClientCert`; else `loadTrustedCAPool(dvc,…)` + `installPool` (the phase-67 three-way `ClientAuth`, byte-intact). The P5 block moves with the arm INTACT; the `:185-194` DEPARTURE comment rewritten cause-scoped (the S1 fallback is now MATCHED; timeout/S2/S3 stay boot-FAIL). **E1 (`config.go:427-429`) verified byte-identical — NO new PGV reject** (C4).
- **The `sdsserver.WithEmptyValidationContext` Option** (SPEC §8, C3): a new Option + a `buildResponse` branch serving `Secret{name, validation_context:{}}` with `trusted_ca` UNSET (the current `WithValidationContext` always emits a non-nil `trusted_ca` → cannot serve S1). A test-helper add; +0 production envelope; state it LOUDLY (the "sdsserver untouched" claim is corrected).
- **Fixture `0111-tls-cvc-empty-dynamic-fallback`** (SPEC §8): the `0110`/`0109` chassis, port **10447**, CVC `default=CA_A` + a served EMPTY dynamic VC, three-arm FORCED-SEND verdict at require=true (CA_A accept+echo / CA_B rejected alert 48 / no-cert rejected), `structuralCheck` triplet + per-side pins + served-this-arm assert. Deliberate breaks: served-CA swap (symmetric — `structuralCheck` must catch), forced-send→polite regression (must go vacuous-green and be CAUGHT). `-count=1`; full selector `TestDifferential/0111-…`; `Errorf` per property; confirm WHICH fired.
- **Fuzz** (SPEC §7): +0 fuzzers — a seed + a new dispatch side/provider mode whose provider returns an error satisfying `errors.Is(ErrEmptyValidationContext)` (a `&fakeProvider{vcErr: …}`), CVC with a valid `default` → the fallback fires → `err==nil`. **Dispatch-verify the named trap:** a fallback seed on `"downstream"` (nil provider) dies at the gate; on `"downstream-sds"` (`cvcFuzzProvider` valid pool) never reaches the empty branch. NEITHER exercises the fallback.
- **The BEHAVIOR_CONTRACT edits** (SPEC §9 B1-B4): B1 CVC Supported + empty-dynamic fallback clause · B2 BC:924 item 3(a) flip to CLOSED + (b)/(c) extended to name S2/S3 · B3 BC:938 item 10 NARROWED (the default's `trusted_ca` IS read on the S1 branch) · B4 the `0111` Differential-coverage paragraph. Land at the IMPL, atomically with ADR-0290 completion.
- **Deliberate breaks pre-verified COMPILING at the PLAN input stage** (`reference_plan_break_instructions_dont_compile`, `reference_break_protocol_commit_first`); the break protocol runs AFTER committing.
- **Envelope to PIN:** functional production edits in `internal/tls/config.go` AND `internal/xds/{secret,stream}.go` + **ONE new `internal/xds` exported symbol** (the streak break, stated LOUDLY; cycle guard STANDS TYPE-level — re-verify `go list -deps ./internal/xds`, no `...`) + ONE chartered `sdsserver` test-helper Option; `internal/boot`/`validate/`/`internal/listener` BYTE-UNTOUCHED; CVC-ONLY; +0 packages/modules/stats/BackendKinds; fixtures 112 → 113. Re-check `git diff go.mod` after tidy (`reference_new_subpackage_pulls_transitive_module`).

**Adversarial verification** at the PLAN per the project discipline (subagent-driven, `feedback_execution_style`, `reference_parallel_subagents_private_scratch`): independent verifiers re-derive the spine's order-soundness, the break-mechanism conditionality, and the flip roster. NO new probe is owed (the SPEC's Dossier P + the from-scratch reproduction settled CLASSIFY; the PLAN re-derives, it does not re-probe).

**Sentinel maintenance:** empty-dynamic/Design B is named in NO live `candidates:` sentence — do NOT narrow one (`reference_sentinel_deferred_sentence_live_vs_historical`). Check (1) keeps printing `NOT DONE: row 68` until the IMPL; (2)+(3) unchanged ⇒ sentinel still does NOT fire.

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline). The `next-skill` line carries the resolved D-EDF-* docket.
2. `docs/envoy-go/phases/68-tls-cvc-empty-dynamic-fallback/SPEC.md` — the FULL SPEC to RE-DERIVE into a task spine (the D-EDF-* dispositions §3, the BC edit map §9, the task sketch §10, the edit-site roster §11). RE-DERIVE, do not execute.
3. `docs/envoy-go/phases/67-tls-require-client-cert-false/PLAN.md` — the PLAN-format precedent (the 9-task TDD spine, the break-conditionality rule, the flip roster).
4. `docs/envoy-go/DECISIONS.md` — ADR-0290 §Context (the SPEC draft; the PLAN adds NO ADR content) + ADR-0287 (the CVC pool-substitution + P1-P5 lineage the row EXTENDS) + ADR-0289/0286.
5. `internal/tls/config.go` (the CVC arm ~:136-198, the fetch-error branch ~:183-198, E1 ~:427-429, the sub-field rejects ~:442-461, `loadTrustedCAPool` :298, `installPool`/`clientAuthFor` :78-96) + `internal/xds/{stream,secret,provider}.go` (`errValidation` stream.go:33, `applyValidationResponse` :158-167, `parseValidationSecret` secret.go:105) + `test/helpers/sdsserver/sdsserver.go` (`buildResponse` :136-168, `WithValidationContext` :62) + `internal/tls/fuzz_test.go` (`cvcFuzzProvider` :31, dispatch sides) + the `0110`/`0109` drivers — the PLAN re-derives the spine's anchors itself.
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_*` + `reference_*`; trust MEMORY.md's index.
