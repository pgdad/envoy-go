START a fresh cold-start session for envoy-go — **the phase-39.1 (`upstream-health-check`, active HTTP health checks) IMPL is DONE, LANDED on master (squash `6714575`), and PUSHED to `origin/master`; the next lifecycle step is the phase-39.2 SPEC** (TCP + gRPC health-check codecs reusing the 39.1 substrate — `superpowers:writing-plans` scoped to SPEC authoring, the by-codec split's pre-authorized second leg) OR a new subject / a now-unblocked health-gated LB candidate (`superpowers:brainstorming`). The substantive master tip is the phase-39.1 IMPL squash **`6714575`** (the host health-state dimension + the cluster-level checker runtime + the health-aware LB pick; ADR-0242 + ADR-0243; the `0066` fixture + the doc landing; confirm via `git log --oneline -5`). The prior substantive tips are the phase-39 PLAN/SPEC/BRAINSTORM bundles and the phase-38.2 IMPL squash `14053d1`. Counts MOVED at the 39.1 IMPL: **fixtures 67 → 68** (`0066-health-check-http`), **stat surface 1125 → 1132** (+7), **DECISIONS tail ADR-0241 → ADR-0243** (next-free **ADR-0244**); fuzzers 42 / BackendKind 33 UNCHANGED.

## What just landed (phase 39.1 IMPL — the durable record is `docs/envoy-go/phases/39-upstream-health-check/PROGRESS.md` + ADR-0242/0243 + BEHAVIOR_CONTRACT; read those, not this summary)

`Cluster.health_checks` active HTTP health checking — the Upstream-robustness family keystone; the project's FIRST per-host MUTABLE state + FIRST cluster-level background runtime. Built end-to-end across the 16-task TDD PLAN, subagent-driven (per-task implementer + a code-reviewer checkpoint on the LB surgery + a final holistic review READY-TO-MERGE); the six-gate GREEN; the full 68-dir differential GREEN (224s). As built:
- **`internal/cluster/health.go`** — the per-cluster `clusterHealth` registry (`map[string]*hostHealth` keyed by `Endpoint.Addr()`; atomic healthy bit + consecutive success/fail counters; hosts START healthy + first-check-immediate, then `unhealthy_threshold`/`healthy_threshold` gate transitions) + the `healthChecker` runtime (per cluster × per `health_check` goroutine; HTTP probe `GET <path>` + `expected_statuses` default 200; `probeOnce`/`applyResult`/`run`) + `parseHealthChecks` (the 6-arm byte-stable reject roster; tcp/grpc DEPARTURE-rejected in 39.1) + `parsePanicThreshold` (ADR-0242).
- **`internal/cluster/manager.go`** — `buildLeafLB` threads `*clusterHealth`; `buildCluster` builds the registry + checkers; `registerClusterMetrics` registers the +7 stats (only on health-checked clusters); `Manager.StartHealthChecks(ctx)` (post-`Freeze`) + `Drain` cancel/WaitGroup-wait. `cmd/envoy-go/main.go` calls `StartHealthChecks(ctx)` after `bs.Stats.Freeze()`.
- **The six LB constructs** (`loadbalancer.go`/`random.go`/`leastrequest.go`/`ringhash.go`/`maglev.go`; subsetLB inherits via children) — a build-time-injected `*clusterHealth` consulted at `Pick` (skip unhealthy + re-pick; ring_hash/maglev WALK-to-next-healthy preserving key→host stability; `healthy_panic_threshold` default 50% strict-`<` → route-to-all + `lb_healthy_panic`); the `Pick` signature + exported `Cluster` surface UNCHANGED; byte-identical to pre-39.1 when no `health_checks` (ADR-0243).
- **ADR-0242 + ADR-0243** (ACCEPTED in-place per ADR-0044). **`0066-health-check-http`** (cross-side poll-to-converge + a warmup gate closing the reference main→worker propagation window + delta-counter asserts; 20/20 flake-free; 2 `-count=1` deliberate breaks). The +7 stats: `health_check.{attempt,success,failure,network_failure}` counters + `health_check.healthy`/`membership_healthy` gauges + `lb_healthy_panic` counter.

## What's next — the phase-39.2 SPEC (TCP + gRPC checkers) OR a new subject

The Upstream-robustness family STAYS OPEN. The pre-authorized **39.2 leg** is the TCP + gRPC health-check codecs reusing the 39.1 substrate (the host health-state registry + the checker runtime + the health-aware pick) UNCHANGED — the thrift-33 framework-zero-touch reuse precedent. Key facts already pinned at the 39.1 SPEC (AMEND-HC2/HC8):
- `tcp_health_check` (connect / send-receive payload) PARSE-ACCEPTS at the reference; envoy-go 39.1 DEPARTURE-rejects it — 39.2 implements it.
- `grpc_health_check` (`grpc.health.v1.Health/Check` over H2) requires the cluster to support HTTP/2; **`google.golang.org/grpc v1.70.0` is ALREADY a direct dep** → 39.2 imports `google.golang.org/grpc/health/grpc_health_v1` directly (no hand-roll, no new module).
- 39.2 anticipates +1 BackendKind (a `grpc.health.v1` SERVING responder) + a candidate `FuzzGrpcHealthResponse`; the `internal/cluster/health.go` checker dispatch widens from HTTP-only to a checker-kind switch.
- 39.2 may itself split (TCP vs gRPC) at its SPEC (the ADR-0045 re-check).

ALTERNATIVELY: the 39.1 keystone now UNBLOCKS the 3 health-gated Load-balancing candidates {locality-weighted LB, priority load balancing, panic thresholds} (they key off the host health-state / healthy-fraction the 39.1 `clusterHealth` provides) — any of those is now brainstormable. OR a wholly new ROADMAP family (xDS, gRPC, HTTP/3, Observability, Runtime). The subject is chosen at the next BRAINSTORM/SPEC per ADR-0106.

## Read first (in order)

1. THIS FILE — the post-phase-39.1-IMPL cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 39.1 (upstream-health-check) IMPL done`; the counts (fixtures 68 / fuzzers 42 / stat surface 1132 / BackendKind 33 / DECISIONS tail ADR-0243, next-free ADR-0244); the lifecycle routing.
3. `docs/envoy-go/phases/39-upstream-health-check/` — PROGRESS.md + SPEC.md (the §1.1 AMEND-HC1..HC8 pins + the §6 reject roster + §7 stats + §8 `0066`) + PLAN.md + BRAINSTORM.md + README.md — the durable phase-39 record (the 39.2 leg follows the SPEC's by-codec split).
4. `docs/envoy-go/ROADMAP.md` — row 39 (`in-progress`; the 39.1-leg DONE note; the 39.2 leg pending) + the §Upstream robustness family heading (4 candidates remain).
5. `docs/envoy-go/DECISIONS.md` — ADR-0242 + ADR-0243 (the just-landed health-state runtime + health-aware pick) + the process ADRs (ADR-0106/0045/0044/0052/0080/0227/0004) + ADR-0230 (the prior framework-runtime seam) + ADR-0232 (byte-stable OPTION-C).
6. `internal/cluster/{health.go,manager.go,cluster.go,loadbalancer.go,random.go,leastrequest.go,ringhash.go,maglev.go,subset.go,dial_h2.go}` + `internal/admin/admin.go` (`/stats`) + `cmd/envoy-go/main.go` + `test/fixtures/0066-health-check-http/` (the poll-to-converge + warmup-gate differential precedent the 39.2 fixtures follow).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_execution_style`, `feedback_git_worktrees`, `feedback_subagents_no_push` + `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_path_targeting`/`_detach`, `reference_docker_probe_bridge_network`, `reference_differential_break_protocol_count1`, `reference_differential_run_selector`, `reference_fixture_workload_constant_desync`.

## Honor throughout

- The lifecycle is BRAINSTORM → SPEC → PLAN → IMPL, each its own session. The next session is the phase-39.2 SPEC (`superpowers:writing-plans` scoped to SPEC authoring — execute the live empirical pins against `contrib-v1.37.2` per `reference_docker_probe_bridge_network`; anchor any new ADR §Context) OR a fresh BRAINSTORM for a new subject.
- `feedback_git_worktrees` · `feedback_execution_style` (subagent-driven at the IMPL) · `feedback_subagents_no_push` · `feedback_push_to_origin` (the controller squash-merges + pushes at stage-close — the 39.1 IMPL landed as squash `6714575`) · `feedback_pertask_gofmt_lint`.
- ADR-0004 (the empirical-pin discipline — EXECUTE at the SPEC) · ADR-0044 (ADR §Context at SPEC, bodies at IMPL in-place) · ADR-0045 (the split-gate — the FINAL 39.2 split decision at its SPEC) · ADR-0052 (the atomic-landing six-gate) · ADR-0080 (byte-stable parse-reject wording) · ADR-0106 (flat family row — NO parent rollup) · ADR-0227 (the contrib reference image `envoyproxy/envoy:contrib-v1.37.2`).
