=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-VERIFIED 2026-07-17 (re-run MECHANICALLY at the phase-67 SPEC close, in the stage worktree) — it does NOT fire. `stop` was NOT created.** Actual output:
- **(1) prints `NOT DONE: row 67`** — row 67 is `in-progress` (it flips `done` only at the phase-67 IMPL six-gate). Check (1) BLOCKS `stop`.
- **(2) prints THREE live "candidates:" sentences** — HTTP/3, xDS, Observability ⇒ three families STAY OPEN. **NOTE: the Observability line ALSO carries HISTORICAL `candidates were:` recaps** (`reference_sentinel_deferred_sentence_live_vs_historical`; the live grep needs "candidates:" adjacent). Exact command, and ONLY it — **cite the command, never the adjective**: `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` ⇒ **3**. (Bare `grep -c 'candidates:'` returns a different, WRONG number.)
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** ⇒ THREE families never opened. Operational-tooling also STAYS OPEN.

All three checks print ⇒ the sentinel does NOT fire. Abundant work remains.

==============================================

## STATUS AS OF 2026-07-17: THE PHASE-67 SPEC IS COMPLETE AND LANDED AT THIS CLOSE. ROW 67 STAYS `in-progress`. THIS SESSION PERFORMS THE PHASE-67 PLAN.

**What the SPEC landed** (`docs/envoy-go/phases/67-tls-require-client-cert-false/SPEC.md`; locate the squash by SUBJECT — `git log --grep 'phase 67'`, `reference_next_prompt_tracked_despite_gitignore`; never by position): every BRAINSTORM §10 docket item DISPOSED with execution-backed evidence, adversarially verified by THREE independent verifiers (V1 code-claims / V2 probe reproduction with fresh PKI + own configs + own silent-SDS server, image digest MATCHED, ZERO refuting defects / V3 process-consistency) — **13 distinct defects found (1 SEVERE, 5 MODERATE), ALL corrected in the landed amendment**. Headlines:
- **D-RCCF-FETCHFAIL-POSTURE RESOLVED** by a discriminating 2×2 probe: the reference's SDS fetch-failure posture is **UNIFORM per resource type** — init-hold (port UNBOUND) → workers start + bind at `initial_fetch_timeout` → **fail closed PER-CONNECTION** (`downstream_context_secrets_not_ready`); it NEVER serves TLS without the resource. ADR-0280 and BC:886 are drift-free; **the phase-65-era "serves anyway" wording is the drift, at FIVE living sites** (BC:900 · the ADR-0286 D-SDSVC-FETCHTIMEOUT bullet · `config.go:115-117` · `internal/xds/provider.go:91-93` · `config_test.go:999` — the V1 SEVERE expanded the roster from three), replacement wording pinned VERBATIM at SPEC §9 B1–B18; the ADR-0286 bullet is corrected by BRACKETED ANNOTATION (never silently); **provider.go:91-93 is the ONE chartered doc-comment-only correction inside the otherwise zero-symbols/zero-functional-change `internal/xds` envelope**.
- **D-RCCF-ANCHORLESS-VC-FALSE probed (9 cells)**: the reference treats `validation_context: {}` exactly like NO validation config at EVERY require value — require=true + anchorless VC is **SILENTLY INEFFECTIVE on the reference (no CertificateRequest at all)** ⇒ envoy-go's retained `:179-181` reject is a **NEW named envoy-go-STRICT departure**; require=false/absent + anchorless VC boots `NoClientCert`.
- **The ClientAuth mapping is three-way across FOUR cells** (require=true+anchor → `RequireAndVerifyClientCert` · false/absent+anchor → `VerifyClientCertIfGiven` · false/absent+no-anchor → `NoClientCert` · true+no-anchor → the RETAINED boot reject).
- **Fixture `0110` is CVC-primary** with the **forced-send `GetClientCertificate` driver MANDATE** (0108/0109 both probed POLITE — closed question); **+0 fuzzers dispatch-verified** (SDS-shape seeds MUST ride `"downstream-sds"` or die vacuously at `:381`/`:398`); **D-RCCF-QUIC final call: OUT**; **nil-pool guard pinned against the `xds.SecretProvider` INTERFACE**; E3 flip roster BIGGER than the substring grep. **ADR-0289 §Context DRAFTED — the DECISIONS tail flipped ADR-0288 → ADR-0289 at the SPEC squash (next-free ADR-0290; the IMPL completes ADR-0289 IN PLACE, no further flip).**

**⚠️ PUSH (idempotent — evaluate, then act):** FIRST ACTION: `git fetch`; if origin/master is BEHIND local master and the local tip is the phase-67 SPEC squash, `git push origin master` per `feedback_push_to_origin`; if in sync, proceed; if REJECTED (diverged), STOP and reconcile — never rebase away landed squashes.

**Key facts a cold session needs (RE-VERIFY MECHANICALLY; do not copy):**
- Counts: fixtures **111** (`ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0109-xds-sds-combined-validation-context`) · fuzzers **55** (`grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`) · DECISIONS tail **ADR-0289** (next-free **ADR-0290**; the tail flipped AT the SPEC — re-derive with `grep -oE '^## ADR-[0-9]+' docs/envoy-go/DECISIONS.md | tail -1`; the IMPL completes ADR-0289 in place, NO further flip) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** (docs-verified; NO mechanical counting command) · go.mod modules **2** (the phase-61.2 lineage figure — NOT a repo total; the single `go.mod` requires 67).
- **STATE.md discipline (ADR-0288):** §Current pointer reads `phase 67 SPEC done` / NEXT = the phase-67 PLAN. **EDIT §Current pointer IN PLACE — do NOT prepend**; demote the outgoing bullet into §Recent lineage, **cap at FIVE**. `STATE_HISTORY.md` is the archive; every pointer in it is stale BY CONSTRUCTION — never grep it for a pointer or a count.
- `.worktrees/phase-67-spec` and branch `phase-67-tls-require-client-cert-false-spec` are REMOVED at close (squash landed; locate the squash by SUBJECT).

## What THIS session does — phase 67 PLAN

One stage per session: PLAN ONLY. Docs-only (`PLAN.md` + `PROGRESS.md` scaffold in the phase directory — the phase-65/66 pattern). Fresh worktree off master, branch `phase-67-tls-require-client-cert-false-plan`, per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; commit locally, controller squash-pushes (`feedback_subagents_no_push`, `feedback_push_to_origin`). Run an adversarial verification pass before landing (PRIVATE scratch per parallel verifier, `reference_parallel_subagents_private_scratch`; phases 64-67 averaged ~10-15 defects per stage document).

**The PLAN must RE-DERIVE the SPEC rather than execute it** (`feedback_brief_citations_not_evidence` — line numbers in SPEC §11 are cited off the pre-squash tree and can drift; symbols are the citation). The phase-66 lesson binds: knowing a failure mode by name does not prevent committing it — every SPEC edit-site, roster, and snippet must be re-derived/compiled before the PLAN adopts it. Specifically OWED:

- **Decompose SPEC §10's ~9-task TDD spine** into the PLAN's task list (T-numbered, RED-first where a behavior flips; the lift + E3 retirement is ONE atomic task; the D-RCCF-NILPOOL-GUARD unconstructibility test pinned against the `xds.SecretProvider` INTERFACE; the forced-send `GetClientCertificate` driver for `0110`-CVC-primary; the B1–B18 doc edits incl. the FIVE-site drift correction — the ADR-0286 bullet by bracketed annotation, `provider.go:91-93` as the chartered doc-comment-only `internal/xds` exception).
- **Every deliberate break must COMPILE** (`reference_plan_break_instructions_dont_compile`) — non-compiling red proves nothing; `-count=1` on every break (`reference_differential_break_protocol_count1`); confirm WHICH assertion fired (`reference_deliberate_break_wrong_assertion`); `Errorf` per independent property (`reference_fatalf_makes_assertions_unreachable`).
- **The E3 flip roster in FULL** (SPEC §3.8 — bigger than the substring grep): `TestCVC_RequireFalse_Rejected_E3` · `TestCVC_RequireFalse_NeverYieldsNoClientCert` (err-half inverts) · the config_test.go:1009-1041 SDS-VC "INERT" subtest (all three assertions invert) · fuzz seed (i)'s stale comment · the ENUMERATED 0109 comment sweep with its grep obligation (`grep -rn 'MANDATORY\|E3\|require_client_certificate' test/fixtures/0109-*/`).
- **Fuzz seeds ride the RIGHT dispatch side** (SPEC §7): SDS-shape require=false seeds on `"downstream-sds"`; a `"downstream"`-side SDS seed is VACUOUS (dies at the retained nil-provider gates). +0 fuzzers.
- **Identifier collision re-check** for every NEW test/helper name the PLAN itself coins (`reference_spec_drafted_identifier_collision_check`).
- **The retained-reject roster byte-diffed** (SPEC §3.8/§6): the nil-provider gates `:381`/`:398`, E1/E2 `:402-407`, the four sub-field rejects `:417-436`, and the require=true anchorless reject `:179-181` all stay BYTE-IDENTICAL.
- `0110` fixture discipline: `reference_differential_fixture_dispatch_constraint` (one dir = ONE runner branch) · `reference_differential_backendcount_min_one` · per-side failure pins (`reference_differential_reference_parses_full_message`) · never assert `/listeners`/`total_listeners_active` for warming · the wrapper field takes a BARE scalar in YAML (`reference_protojson_wrapper_scalar_not_object`).
- Sentinel: re-run MECHANICALLY at close. A PLAN completes nothing — check (1) keeps printing `NOT DONE: row 67` and checks (2)+(3) keep printing ⇒ do NOT create `stop`. No deferred-sentence edit at ANY stage (a §8-tier pickup; do not fabricate a narrow).

## Read first (in order)
1. `docs/envoy-go/phases/67-tls-require-client-cert-false/SPEC.md` — §3 the docket dispositions (§3.3 the five-site drift + pinned wording; §3.8 the flip roster); §9 the B1–B18 edit map; §10 the task sketch; §11 the edit-site roster; §15 the exit contract. §1.1/§1.2 carry the drift ledger + adversarial record.
2. `docs/envoy-go/phases/67-tls-require-client-cert-false/BRAINSTORM.md` — the chartered row (context; the SPEC supersedes it where they differ — see SPEC §1.1).
3. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY to grep; ADR-0288 discipline).
4. `docs/envoy-go/ROADMAP.md` — row 67 + the §Schema invariant + the THREE live deferred sentences.
5. `docs/envoy-go/DECISIONS.md` — the ADR-0289 §Context DRAFT at the tail (this row's charter) + ADR-0287/0286 (the siblings whose rejects/appliers this row reshapes) + ADR-0280 (the departure family). Tail ADR-0289; next-free ADR-0290 — re-derive, never trust this file.
6. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the TLS/SDS surface :886-:950 (the B1–B15 edit region) + :1817 (the B14 Scope-boundaries line).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_*` execution discipline + `reference_*` traps; trust MEMORY.md's index.
