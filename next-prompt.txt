=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **PHASE 88 (`h2-continuation-frames`) IS OPEN — ROW 88 IS `in-progress` AT `ROADMAP.md:150`.** `want` IS **120**. ⇒ **THIS SESSION PERFORMS THE PHASE-88 SPEC.** `STATE.md`'s `next-skill:` reads *the phase-88 SPEC* — that IS your instruction. ⚠️ **THE SENTINEL DOES NOT FIRE** — check (1) prints exactly `NOT DONE: row 88` and check (2) prints SIX. **DO NOT create `stop`.**

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=120 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **`want` IS 120 AT THIS TIP AND A SPEC DOES NOT MOVE IT.** `ROADMAP.md` must be **BYTE-UNTOUCHED** at your stage — verify by EMPTY DIFF against master, and run the sentinel ONE side only. ⚠️ **THE ONE EXCEPTION TO WATCH: if your SPEC confirms D-88-SEQ's split and mints `88.1`/`88.2` sub-phase ROWS, that moves `want` a SECOND time and is a sentinel-affecting edit** — the row-84 precedent is a split recorded in PROSE with a single parent row, and the BRAINSTORM deliberately minted NO sub-phase rows. **If you mint them, MEASURE both sides; never forecast.**

⚠️ **THE DOCTORING NCs REMAIN MANDATORY EVERY SESSION.** Both:
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md. At want=120 expect BOTH
#   "NOT DONE: row 62" AND "NOT DONE: row 88".
```
```sh
# the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```

⚠️ **AND WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -oiE '-family row'` fails with `grep: amily row: No such file or directory` and the surrounding arithmetic prints `base=0 now=0 delta=0`, **which reads exactly like "no change".** Observed live at FOUR phase-84 stages. **Always pass `--` before the pattern**, and remember the counts are form-dependent: **95 occurrences vs 67 LINES** at this tip (`grep -c` counts LINES). **A gate that reads zero on BOTH sides is not evidence of invariance.** ⚠️ AND the carried **ARM-A {119, 131} figure binds ONLY with its escape-aware pipe-split command** (BRAINSTORM-86 §6) — ad-hoc field-count forms do NOT reproduce it (`NF != 8` flags 17 rows); do not "correct" the figure with a different form.

**SENTINEL RE-RUN MECHANICALLY AT THE PHASE-88 BRAINSTORM CLOSE (2026-08-13, base `244e482f`), BOTH SIDES OF THE ROW REGISTRATION. IT DOES NOT FIRE; `stop` was NOT created** (verified absent at repo root AND stage worktree, both sides). ACTUAL output, recorded not predicted:
- **BEFORE at `want=119`** (input **237 lines / 119 data rows**): (1) **SILENT — the FIFTH silent reading in project history**, and CORRECT because every row really was `done` · (2) **SIX at `:197 :203 :209 :219 :225 :233`** · (3) SILENT.
- **AFTER at `want=120`** (input **238 lines / 120 data rows**): (1) **`NOT DONE: row 88`** ALONE, denominator silent · (2) **SIX shifted uniformly +1 to `:198 :204 :210 :220 :226 :234`** (the single inserted row at `:150` sits ABOVE all six windows) · (3) SILENT.
- ⚠️ do NOT "fix" the six and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases; the candidates sentences are a WINDOW onto a larger deferred backlog, not an inventory of it.
- **ALL FOUR NCs FIRED BOTH SIDES:** row-62 doctoring => BEFORE `NOT DONE: row 62` **ALONE — the proof the silent side still LOOKS**; AFTER `NOT DONE: row 62` AND `NOT DONE: row 88` (with `NC LANDED? [ in-progress ]` inspected first). `want` off-by-one => BEFORE `GATE FAIL: examined 119 data rows, expected 118`, AFTER `GATE FAIL: examined 120 data rows, expected 119`. check-(3) doctoring (residual confirmed **2 -> 0** on the doctored copy first) => `NEVER OPENED: gRPC` while `WASM` correctly stayed silent. check-(2) one-arm counts **5** (long arm alone) / **1** (short arm alone), union **6** (a one-arm strip is NOT an NC for the union).
- Every leak axis INVARIANT across the registration: `-family row` **95 / 67** · `gRPC-family row` **2** · `Operational-tooling-family row` **3**. ARM-A flags **{119, 131}** UNAFFECTED (those rows sit ABOVE the `:150` insertion).
- ⚠️ **Re-run all three checks and both doctoring NCs yourself and record what YOU see.**

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (ugrep under the hood). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Reproduced at TWENTY-THREE separate tips.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **THE METHOD BINDS; carry no standing discrepancy figure and no standing absolute.** ⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS; print FILE NAMES, never a count.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files; use `-v --no-index`, and `git status` is the discriminating check). ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **`grep -c` ON ZERO MATCHES IS TOOL-SPECIFIC** (only `git grep -c` prints nothing; all exit 1 — capture the value, never chain on the exit code). ⚠️ **`grep -c` COUNTS *LINES*, NOT *OCCURRENCES*.** ⚠️ **`\t` IN GNU ERE IS A LITERAL `t`** — use `-P`. ⚠️ **A `git grep` PATHSPEC ENDING IN `/` READS A FAIL-UNSAFE ZERO** (shape 33) — use the shell-glob form. ⚠️ **AN UNANCHORED PANIC-GREP FALSE-FIRES 14 TIMES ON A FULLY-GREEN DIFFERENTIAL LOG** — **the canonical gate is ANCHORED: `^panic:|DATA RACE|SIGSEGV`. A gate that fires on a green log is worse than no gate.**

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`** with FOUR blind spots (an em-dash, a DOT in `http-1.1`, two LETTER-suffixed ids). ⚠️ **EXECUTED on scratch copies at the phase-77 PLAN: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** ⚠️ **DO NOT restore the regex form.**

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

⚠️ **Run the union form, never one half of it.** ⚠️ **A ONE-ARM STRIP IS NOT AN NC** (6 -> 5 short / 6 -> 1 long; never 6 -> 0). ⚠️ **The documented trap's stated MECHANISM is refuted while the trap is real** — the inflation comes from ROADMAP table rows carrying BRAINSTORM prose. **Cite the command; do not re-inherit the explanation.** ⚠️ **The line numbers drift with edits — re-derive them; the current six are `:198 :204 :210 :220 :226 :234`.** ⚠️ **These six paragraphs are the FINAL and now the ONLY sentinel blocker, and any edit near them is a sentinel-affecting edit.** All six are standalone family PROSE paragraphs (not rows), naming ~42 distinct candidates; every "retire the paragraphs" shape is doctrinally foreclosed (`reference_sentinel_matcher_string_self_clears`).

⚠️ **NEW AND LOAD-BEARING FOR THIS ROW: UNLIKE ROW 87, ROW 88'S PROVENANCE IS *INSIDE* A WINDOW.** The gRPC-family paragraph at `:204` names *"the CONTINUATION-discard defect"* among its deferred candidates. The row is filed as **core-HCM maintenance claiming NO family ordinal** because the defect is protocol-generic (BRAINSTORM §5 records the counter-argument openly rather than hiding it). **The `:204` sentence narrows at ROW-DONE** — one candidate leaving a list of ~11. The phrase survives a one-item removal so check (2) *should* stay SIX, ⚠️ **but that is a PREDICTION and MUST BE MEASURED on both sides at the IMPL, never forecast.**


==============================================


## STATUS AS OF 2026-08-13: PHASE 88 (`h2-continuation-frames`) **OPEN** — row 88 `in-progress` at `ROADMAP.md:150`, `want` **120**, base `244e482f`. **THIS SESSION PERFORMS THE PHASE-88 SPEC.**

**What the 88 BRAINSTORM landed (2026-08-13, base `244e482f`, branch `phase-88-brainstorm`, ONE squashed commit).** Docs-only: ZERO production `.go`, ZERO test `.go`; `DECISIONS.md` and `BEHAVIOR_CONTRACT.md` **BYTE-UNTOUCHED** (a BRAINSTORM adds no ADR; next-free stays **ADR-0310**; the strict `PROPOSED` guard stays **0** — **YOUR SPEC RE-ARMS IT TO 1**). **SELF-PICKED** per the 2026-07-12 standing directive.

**THE HEADLINE: envoy-go DISCARDS HTTP/2 CONTINUATION frames on BOTH the downstream and the upstream path.** `internal/filter/hcm/h2/conn.go`'s `dispatchFrame` (cite **BY SYMBOL**) returns `nil` for `*http2.ContinuationFrame` behind a comment whose two clauses are **BOTH FALSE** — `ReadMetaHeaders` is assigned only in `test/differential/runner_test.go`, never in `internal/`, so the arm IS reached in normal usage — while `onHeaders` decodes with `HeadersEnded()` and **never branches on it**, dispatching the PARTIAL field set and leaving the HPACK dynamic table permanently diverged from the peer's. `client.go` carries the same defect with **no `ContinuationFrame` case at all** (`:417`/`:428`).

### ⚠️ THE FIVE FACTS THE BRAINSTORM PROVED BY EXECUTION — RE-PROVE THEM AT YOUR TIP

1. ⚠️ **THE DEFECT IS REACHABLE BY ORDINARY CLIENTS, NOT JUST A RAW FRAME WRITER — THIS IS THE FACT THAT CHANGED THE PRIORITY.** A stock `x/net/http2.Transport` splits a header block past the default 16384-byte `SETTINGS_MAX_FRAME_SIZE`. Measured: 1024/8192/15000 B -> header PRESENT; **20000/40000 B -> subject answers 200 while dropping `accept-encoding`, `user-agent` AND the padding header.** A large cookie or JWT suffices. Every prior stage treated CONTINUATION as exotic. **The threshold and which headers land where are ORDERING-DEPENDENT — re-derive, do not carry the table.**
2. ⚠️ **THE BLAST RADIUS OF ONE SPLIT REQUEST IS THE CONNECTION, NOT THE REQUEST.** One split request on stream 1, then a **perfectly ordinary un-split** request on stream 3 -> subject **`GOAWAY COMPRESSION_ERROR`** (`hcm: h2: h2: COMPRESSION_ERROR: HPACK decode failed`); reference 200 both streams.
3. ⚠️ **THE SILENT MODE IS THE DANGEROUS ONE.** `:path` in the CONTINUATION -> subject `RST_STREAM PROTOCOL_ERROR` (loud); a *header* in the CONTINUATION -> subject **200 with the header SILENTLY GONE**, no error, no log line. Reference 200 + header preserved on both.
4. ⚠️ **THE CLIENT LEG IS REAL TOO, AND IT HAS A BYPASS CONTROL.** A padded upstream RESPONSE header block -> `len(x-resp-pad)=0` through envoy-go at 20000/40000 B while a direct-to-backend control preserves 20000/40000. Status stayed 200 throughout.
5. ⚠️ **A COST REDUCTION FOUND BY EXECUTION — THE ROW DOES *NOT* OWE AN RFC 9113 §6.10 ORDERING STATE MACHINE.** An interleaved PING between HEADERS(`END_HEADERS=0`) and its CONTINUATION -> subject `GOAWAY PROTOCOL_ERROR`, reference the same. **But the subject's green is NOT envoy-go's doing:** the log reads `framer: connection-error code=1`, i.e. `translateFramerErr` mapping an error from **x/net's own `Framer.checkFrameOrder`** (`frame.go:519/543`, tracking `lastHeaderStream`). **Attributing it to envoy-go would repeat the `reference_code_comment_not_evidence` error the false comment already committed once.** What is owed is narrower: **accumulate fragments until `END_HEADERS`, decode once, per direction.**

⚠️ **ONE QUESTION LEFT OPEN AND DELIBERATELY NOT CLAIMED:** whether the CLIENT leg also desyncs its HPACK decoder the way the server leg provably does. Two consecutive large-response arms both succeeded, which is CONSISTENT WITH per-request upstream connections but does **not** establish it. **Settle it by probe; do not inherit either answer.**

### ⚠️ WHAT THIS SESSION OWES — THE PHASE-88 SPEC

Dispose the BRAINSTORM **§7** question set **BY EXECUTION** and draft **ADR-0310 §Context** with STATUS `PROPOSED` (strict guard **0 -> 1**):

1. **Q1 — the reassembly primitive, DECIDED BY COMPILING PROTOTYPE.** A per-connection pending-header-block accumulator (stream id + fragment buffer + the original HEADERS flags) vs assigning `Framer.ReadMetaHeaders` and letting x/net reassemble. ⚠️ **`ReadMetaHeaders` looks cheap but changes the frame type the ENTIRE dispatch switch receives (`*http2.MetaHeadersFrame`) and imposes its own limits — measure the blast radius before preferring it. PRICE BOTH.**
2. **Q2 — bounding the accumulator.** An unbounded buffer is a memory-exhaustion vector (the CONTINUATION-flood class). What limit, what error code, and does it need a stat? ⚠️ **A new stat name would break the anticipated stat-surface +0.**
3. **Q3 — the client-leg desync question above.** Settle by probe.
4. **Q4 — the differential proof shape.** ⚠️ **A stock transport with a padded header reproduces this WITHOUT a raw framer, so the cheap path is an EXTENSION of an existing fixture, not a new one** (the row-87 `0004` precedent) — **confirm by execution, do not assume.** `probe88` (raw x/net `Framer`+`hpack` h2c client, ~250 lines with the echo-backend serve mode) is the measured FALLBACK; **BRAINSTORM §2 describes it well enough to rebuild, and it was NOT landed.**
5. **Q5 — the reject-shape divergence.** Arm B is subject `RST_STREAM PROTOCOL_ERROR` vs reference 200; post-fix both should be 200. Confirm no NEW divergence appears, and decide differential-vs-unit per arm (the D-87-REJECT-SHAPE precedent).
6. **Q6 — the false comment and the contract.** The `dispatchFrame` comment must go. Does `BEHAVIOR_CONTRACT.md`'s `## HTTP/2` section carry a sentence this row refutes? ⚠️ **Grep for the SENTENCE; do not inherit the BRAINSTORM's framing** (`reference_brainstorm_adjective_acquires_adr_authority`).
7. **Q7 — D-88-SEQ.** Confirm or refute the two-leg split (88.1 downstream/server `conn.go`, 88.2 upstream/client `client.go`; 88.1 alone is shippable). **If you mint sub-phase ROWS, that is a second `want` move — MEASURE it.**
8. **Q8 — h2spec is NOT a red anchor.** ADR-0307 measured it not to gate either side. Re-confirm at your own tip and cite `95 tests, 94 passed, 1 skipped, 0 failed` **only from your own run**.

**Also owed:** anticipated counts each re-derived with its NC · the cost ENUMERATED BY PROTOTYPE (below) · `STATE.md` rolled IN PLACE (lifecycle-state 1 -> 2) with the oldest §Recent entry evicted by DIRECT DATE READ · `next-prompt.txt` rolled to the PLAN (`git add -f`) · ONE squashed commit, merged to master, pushed.

### ⚠️ THE COST FIGURE IS AN ESTIMATE, NOT A MEASUREMENT — AND THAT DISTINCTION IS THE POINT

The BRAINSTORM estimates **~+60-140 net production `.go` over TWO files / ~+300-700 net test**, and the carried "2-4x row 85 (+1046 realized)" figure is superseded because §3's §6.10 finding removes the largest imagined component. ⚠️ **NEITHER NUMBER IS MEASURED. YOUR SPEC MUST ENUMERATE BY COMPILING PROTOTYPE.** `reference_measured_prototype_is_a_lower_bound` fired **TEN consecutive times** before row 87 broke the streak, and ADR-0309 §Consequences (iii) records that row 87's escape rested on **ONE grep-confirmed call site measured twice by a working prototype**. **This row has at least THREE decode sites across TWO files plus per-connection state — nothing about row 87 licenses treating this estimate as a floor that holds.**

### Candidates ledger (re-derive EVERY cost at your tip — a carried cost is stale by construction)

All were re-derived at the BRAINSTORM and REJECTED; re-derive again if you revisit: **`ssl.connection_error`** (still absent as a landed stat name — 3 comment refs in `internal/listener/manager.go` at `:373`/`:411`/`:1292`, 1 in `quic_test.go:234`, 2 contract mentions; carried floor **+444 whole-`.go`**, LARGER than this row) · **`test/conformance/grpc/`** (`test/conformance/` holds ONLY `h2spec/` and `proxy-wasm/`; the client declared at `BOOTSTRAP_PROMPT.md:350` still does not exist; 9/26 reachable) · **the stat-surface recount** (contested **1205 vs 1207**; this row re-measured the OCCURRENCE form at `NewCounter(` **327** / `NewGauge(` **79** = **406**, a DIFFERENT form from the lineage's carried `145/21` — **no absolute corrected, none carried**; it rides a **+0** row and this row is not +0) · **REVIEW.md restoration** (**37** of **129** phase dirs; process-not-product, **no defect to reproduce**) · **the D-86-CONN `client.Close` gate** (`boot.NewValidateSDSProvider` at `internal/boot/boot.go:240`, still ungated, ~10 lines — **a fold-in, not a row**) · **hygiene fold-ins** (`harness_test.go:208` stale port inventory, the xDS cycle-guard automation) · the **~42 candidates in the six family windows**.

### ⚠️ COUNTS AT THIS TIP — RE-DERIVE MECHANICALLY, NEVER COPY

- **`ROADMAP.md` 238 lines / 120 data rows**; row 88 `in-progress` at `:150`, all others `done`; check-(2) anchors `:198 :204 :210 :220 :226 :234`; `-family row` **95 occurrences / 67 LINES**; `gRPC-family row` **2**; `Operational-tooling-family row` **3**; ARM-A flags **{119, 131}** (escape-aware command ONLY).
- **`DECISIONS.md` 18150** lines · **308** headings · tail **ADR-0309 COMPLETE** · next-free **ADR-0310** (TAIL-derived — headings+1 COLLIDES at the ADR-0209 gap; `grep -c '^## ADR-0310'` => **0**) · **strict `^> \*\*STATUS: PROPOSED` guard 0 — DISARMED; YOUR SPEC RE-ARMS IT** · STATUS census **22** · `^---$` **216**. ⚠️ **CARRY NO WHOLE-FILE COUNT OF THE LOOSE `PROPOSED` MATCHER** — ADR-0309's completed block NARRATES the word and has joined that matcher's standing false-positive set.
- **`BEHAVIOR_CONTRACT.md` 5958** — `## HTTP/2` at `:2019`. ⚠️ **Row 87's `:2036` insertion shifted EVERY by-line citation into this file at or below `:2036` by +1** (across `ROADMAP.md`, `DECISIONS.md`, `STATE_HISTORY.md` and ~12 historical phase docs), recorded in ADR-0309 §Consequences (ix) and deliberately NOT repaired — **cite by STRING or SYMBOL.**
- **`STATE.md` 64** · **`STATE_HISTORY.md` 492** · **`BOOTSTRAP_PROMPT.md` 522** (repo ROOT, not `docs/envoy-go/`) · phase dirs **129** · `REVIEW.md` **37** (standing departure, named not claimed).
- **differential fixtures 121** — numeric tail `0119-grpc-unary-trailers`, **next-free `0120`** (NC observed at the BRAINSTORM: `mkdir .../0120-nc-fake` => 122, removed => 121). ⚠️ **This row's fixture delta is NOT yet anticipated +0** — extend-vs-mint is YOUR decision. · **fuzzers 55 / 48 files** (⚠️ a reassembly buffer is a plausible fuzz target — **say yes or no explicitly, do not inherit +0**) · **BackendKind tail 38** (`H2GoawayResponder BackendKind = 38`, `fixture.go:614`) over 39 declarations (do NOT "fix").
- **h2spec: cite `95 tests, 94 passed, 1 skipped, 0 failed` ONLY from your own tip's run** (skip 6.9.2/2 invariant). **NOT a red anchor for this row.**
- ⚠️ **STILL CONTESTED, NO NUMBER CARRIED:** the `STATE_HISTORY.md` archive-gap total · the loose `PROPOSED` matcher's whole-file count · the stat-surface absolute (1205 vs 1207 — WIDENED; only DELTAs asserted) · the zero-test `generic/*` suite count (13 vs 14).

### ⚠️ DOCUMENTARY DEFECTS — carried forward, still deliberately NOT fixed

**`ADR-0051` §2's false gate-scope sentence** corrected on the record by ADR-0307 · **`ADR-0058`** cites `routerActionH2.doH2` at a dead location · **`ROADMAP.md` rows at lines 119/131 malformed** (ARM-A GUARD, not a fix) · **`STATE.md` §Project counts FROZEN at phase 76** · **`harness_test.go:208` port inventory STALE** · **`body.go` nolint INERT** · **xDS cycle guard NOT AUTOMATED** · **`wasm/doc.go:219` two errors** · **ROADMAP cites `esalaine` FIVE times** · **`rbac.go:50` token `F2`** · **root `PROGRESS.md`** stray phase-32.1 doc · **SPEC-86/PLAN-86 prose cites the grpcclient Close pins at a nonexistent `internal/xds/xdsgrpc/...` path** (ADR-0308 §Decision (b) is the correction) · **`BEHAVIOR_CONTRACT.md:2034`'s "no stdlib net/http parsing" sentence is FALSE for the 05.1 -> 87 window** — RETAINED and ridden by ADR-0309 · **`ADR-0057` says fixture `0004`'s driver issues "27 sequential H/2 round-trips per side" — it now issues 29** · **`BEHAVIOR_CONTRACT.md`'s TWO pre-existing riders cite the ADR-0052 mandate as ``ADR-0052 `:1821` `` while line 1821 is retry-policy / hedging prose** (the anchor has DRIFTED; ADR-0309's own rider deliberately names ADR-0052 with NO line anchor) · ⚠️ **NEW AT THE 88 BRAINSTORM: the gRPC-family window's cite of the CONTINUATION arm at `conn.go:255-259` has DRIFTED — it is `:264-268` at this tip** (recorded, NOT repaired; cite BY SYMBOL) · ⚠️ **and the `dispatchFrame` comment itself is a LANDED FALSE COMMENT** (both clauses refuted by execution) — **this row's SPEC is where it dies.**

### ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + BOTH DOCTORING NCs YOURSELF** and record ACTUAL output. A SPEC edits no ROADMAP, so ONE side — and the binding proof of that is the **EMPTY `ROADMAP.md` DIFF**.
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** The 88 BRAINSTORM refuted the carried cost estimate, the carried `:255-259` cite, and a landed code comment — and it caught its OWN first probe reading (a vacuous 502 on both arms) only because it ran a positive control. Do the same.
3. **LINE ANCHORS DRIFT; SYMBOL ANCHORS DO NOT — RE-VERIFY EVERY CITE AT YOUR TIP.**
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Still live: **the SDS dial-budget class** (named member `TestSDSEndToEnd_FetchFailure_BootFailsClosed/silent_SDS_server…`, `internal/boot/boot_sds_e2e_test.go:551`, gRPC `DeadlineExceeded`, THREE firings with a byte-identical signature) · **the driver-owned receiver port race** with TWO named members — `0081-grpc-access-log` (ALS, `0.0.0.0:35097`) and `0087-tracing-otlp` (OTLP, `0.0.0.0:35323`); **it ABORTS the binary mid-suite and the harness wrapper still exits 0** · `internal/cluster` `-race` outlier · `internal/httpclient` zero-value · the two 84.2-era flakes (cleared, unclassed) · `TestServerConn_TinyWindowDelivery` (85-IMPL, cleared, unclassed) · the REFERENCE h2spec section-8 flip (reference-side only). **Capture `-v` output BEFORE re-running anything.**
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only). ⚠️ At the 88 BRAINSTORM **FOUR pre-existing containers were present and left UNTOUCHED**, and a sibling `envoy-rust` Claude session has been live on this machine at recent stages.
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`.
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL.** **A harness's exit code is not the command's** — at the 87 IMPL an ABORTED differential (32 of 121 fixtures NEVER RAN) reported wrapper exit **0** while `INNER_EXIT` was **1**. **`gofmt -l` never exits non-zero — gate on OUTPUT.**
8. ⚠️ **`pgrep -f <pattern>` / `pkill -f` MATCHES YOUR OWN SHELL'S COMMAND LINE AND KILLS THE TOOL CALL (exit 144)** — **this fired LIVE at the 88 BRAINSTORM.** Kill background probes by **PID from `pgrep -laf` with the `bash -c` line filtered out**, and verify ports released with `ss -ltn`.
9. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-88-*` or stage worktree should outlive its stage close** — expect only `master` plus your own (verify with `git worktree list`).
10. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** (a)/(b) differential 121 fixtures, full suite with `INNER_EXIT`; (c) h2spec cite only from your own run; grpc-conformance deferred in writing; proxy-wasm 10/16; (d) fuzzers 55/48; (e) `INNER_EXIT` on every differential launch AND `go test ./...`; (f) no REVIEW.md — standing departure.

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 88 (h2-continuation-frames)' --format='%h %s'      # 1 at this tip: the BRAINSTORM
git log --grep '^phase 87 (h2-double-slash-path-routing)' --format='%h %s' # 4: a CLOSED phase, use as a FIRING CONTROL
```
⚠️ **The count comments are LIVE FIGURES — VERIFY before relying.** Take the NEWEST line (`| head -1`).

Execution style: **subagent-driven per `feedback_execution_style` is the STANDING PREFERENCE**, and the 87 IMPL is the precedent it should be measured against (four task agents + an adversarial whole-branch reviewer, which found three real coverage gaps the controller had not). **A SPEC whose probe set is cheap and serial may take the NAMED inline departure — the 87 BRAINSTORM/SPEC/PLAN and the 88 BRAINSTORM all did (FOUR consecutive stages). NAME the choice either way.** Subagents commit LOCALLY on the ONE stage branch with EXPLICIT PATHSPECS; the controller squashes (`feedback_subagents_no_push`, `feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** and tear down docker **BY NAME**. ⚠️ **Band private ports outside BOTH `20000-31007` AND `11000-14999`, clear of the static fixture ports (10000-19172); note the driver-owned receiver race has been observed at `35097` and `35323`, and `0081` also binds `0.0.0.0:42039`.** The 87 stages used 47410-47441 and the 88 BRAINSTORM used **47450-47470**; band yours from **47480+**. ⚠️ **ONE WORKTREE PER AGENT preferred; explicit-path staging if shared; never an unscoped `git restore`.** Per-task `gofmt` + `golangci-lint` on touched packages (⚠️ misspell locale US). ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch.** ⚠️ **Docker BY NAME, `--rm`, verify gone.**

⚠️ **A REFERENCE CONTAINER ON THIS HOST CANNOT REACH A HOST-BOUND BACKEND OVER THE DOCKER BRIDGE** — measured at the 88 BRAINSTORM: `172.17.0.1:47455` was **UNREACHABLE** from inside `b88-main` (verified with `/dev/tcp` from within the container), and `--network host` produced a container whose listener never appeared in the host's `ss -ltn`. **Plan reference-side observation that does NOT require a host backend** (the BRAINSTORM used a `match.headers` route on the reference side and named the probe asymmetry, with each side read against its OWN control). ⚠️ **AND envoy-go REJECTS `match.headers` outright** — `route 0: match.headers is not supported in phase 04`, measured — so that trick works on the reference side ONLY.

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root — **observed repeatedly at the 88 BRAINSTORM**. Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — `git status --porcelain` AND `git diff --stat master` must show ONLY your intended edits. For a patched TRACKED file: `sha256sum` BEFORE patching, `git checkout -- <path>` after, `sha256sum -c` the capture. **The 88 BRAINSTORM built `probe88/` inside the worktree and deleted it whole at close, with both checks verified.**

⚠️ **THE ARCHIVE-ABSENCE GUARD — USE THE WHOLE-FILE FIXED-STRING FORM** (`grep -cF`), NC'd with REAL targets re-derived FROM THE FILE first; a control's own count is a LIVE FIGURE: at the 88 BRAINSTORM close `phase 82 (wasm-http-call-response-cache) IMPL done` read **13** and `phase 83 (wasm-pause-arm-leak) PLAN done` read **11** — ⚠️ **BOTH DRIFTED UP** after three consecutive closes reading 12/10, which is exactly why a control is re-derived and never copied. ⚠️ **The §Recent-style bullet anchor `- **prior active-phase:**` reads 0 on ARCHIVED entries** (the archive wraps them `- **prior active-phase (evicted…):**`) — use the fixed-string form. **Verify STRICTLY APPEND-ONLY**: `git diff --numstat` = `N 0` AND base a byte-exact PREFIX (`cmp`). At your close §Recent evicts the oldest per a DIRECT DATE READ at that tip (⚠️ expect a **TWO-WAY TIE at 2026-08-12** between `phase 87 (h2-double-slash-path-routing) BRAINSTORM done` and `phase 86 (validate-sds-nil-provider) IMPL done`, resolved by LIST POSITION — the LAST entry — but **CONFIRM by direct read before removal**).

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads the phase-88 SPEC — that IS your instruction.** §Project counts SELF-CONTRADICT §Current — do not anchor on them (frozen at phase 76).
2. `docs/envoy-go/phases/88-h2-continuation-frames/BRAINSTORM.md` — **the whole file**; §2 is the reproduction with every arm and its control, §3 the §6.10 cost reduction, §4 the re-derived rejected alternatives, §5 the family-attribution counter-argument, §6 the counts and the cost-floor caveat, §7 **your question set**.
3. `docs/envoy-go/ROADMAP.md` — row 88 at `:150`; the six family-window paragraphs at `:198 :204 :210 :220 :226 :234` (check-(2) anchors) — ⚠️ **`:204` is the window this row's provenance came from.**
4. `internal/filter/hcm/h2/conn.go` (`dispatchFrame`, `onHeaders`), `internal/filter/hcm/h2/client.go` (`:417`/`:428`), `internal/filter/hcm/h2/hpack.go` (`decodeBlock`), `internal/filter/hcm/h2/framer.go` (`newFramer` — **`ReadMetaHeaders` is never assigned**, and `translateFramerErr` is what makes x/net's ordering enforcement look like envoy-go's).
5. `docs/envoy-go/DECISIONS.md` ADR-0309 (tail, COMPLETE) — §Consequences (iii) on the cost-floor streak and (ix) on the stale-cite classes row 87 created.

## What the SPEC owes
All eight §7 questions DISPOSED BY EXECUTION · the cost ENUMERATED BY COMPILING PROTOTYPE (not estimated) · **ADR-0310 §Context** appended STATUS `PROPOSED` with the strict guard re-armed **0 -> 1** · anticipated counts each with its NC · `ROADMAP.md` **BYTE-UNTOUCHED** (empty diff) with `want` staying **120**, row 88 staying `in-progress` · the sentinel + all four NCs, ONE side, recorded · `STATE.md` rolled IN PLACE (lifecycle-state **1 -> 2**) with the oldest §Recent entry evicted by DIRECT DATE READ · `next-prompt.txt` rolled to the PLAN (`git add -f`) · ONE squashed commit, merged to master, pushed.
