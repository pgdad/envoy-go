=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

**THERE IS BANKED WORK AT THIS TIP.** Phase 77 (`runtime-static-layer`) is BRAINSTORMED and **row 77 is registered `in-progress`**. **This session performs the phase-77 SPEC** — it advances banked mid-lifecycle work, which the standing directive ranks above opening a new subject. Do NOT self-pick a new subject.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form. See the §(2) note below.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-RUN MECHANICALLY 2026-07-26 at the phase-77 BRAINSTORM, AFTER the ROADMAP edit landed. It does NOT fire. `stop` was NOT created and MUST NOT be.** ⚠️ **Re-run it yourself anyway and record what YOU see** — a prior run's result is not evidence for the current tip. ACTUAL output, recorded rather than predicted:
- **(1) IS RE-OPENED: `NOT DONE: row 77`.** The phase-76 IMPL had silenced it by flipping the last chartered row `done`; row 77's registration re-opened it. ⚠️ **The new row was checked against check (1)'s blind spot BY EXECUTION before the check was trusted to track it** — the regex applied to the row line in isolation MATCHES, and a **negative control** (the same line reading `done`) printed **nothing**.
- **(2) prints FOUR under the old matcher and FIVE under the broadened one above.** ⚠️ **BOTH ARE INCREASES AND BOTH ARE CORRECT — do NOT "fix" either count down.** See the §(2) note.
- **(3) prints `NEVER OPENED: gRPC` and `NEVER OPENED: WASM`** — **`Runtime` is legitimately GONE**, cleared by row 77, which genuinely delivers the charter's first clause. (The `WASM` line remains a ROADMAP **bookkeeping artifact** — `internal/wasm/` is a substantially-built wazero host and rows 25/25.1/25.2/25.3 are all `done` — deliberately LEFT AS-IS.)

## ⚠️ THE §(2) NOTE — CHECK (2) WAS FAILING UNSAFE, AND WHAT ITS NUMBERS NOW MEAN

**The old check (2) matched ONLY the long phrase.** The **Operational-tooling** family records three genuinely un-chartered items — xDS-sourced dry-validation · an admin-API-exposed live-reload-and-validate endpoint · an RTDS/SDS validate companion — in the **SHORT** form, in the *same paragraph* that reads `**The family STAYS OPEN**`. The old matcher **could not see them**.

⚠️ **Check (3) fails SAFE by construction, and check (1)'s four-miss blind spot is currently HARMLESS (all four missed rows are `done`) — but check (2)'s blind spot was LIVE.** Had the visible sentences ever been emptied and the three families opened, `stop` would have fired with real un-chartered work on the books. **A false "done" ends the project early.**

**The fix was applied at the MATCHER, not at the prose** — it fixes the CLASS and immediately surfaces the INSTANCE with no ROADMAP edit. Negative controls, all EXECUTED at the phase-77 BRAINSTORM: a `candidates were:` recap line ⇒ **silent** · a short-form-only line ⇒ **1** · an empty file ⇒ **silent**.

⚠️ **Normalising that Operational-tooling paragraph to the long form is a NAMED, DELIBERATELY-UNTAKEN follow-up.** It would make the two matchers agree. It was left undone because it is a ROADMAP prose edit beyond a BRAINSTORM's chartered edit set — not because it is unnecessary.

⚠️ **THE DOCUMENTED TRAP'S STATED MECHANISM IS REFUTED, WHILE THE TRAP IS REAL.** `reference_sentinel_deferred_sentence_live_vs_historical` says a naive `grep -c 'candidates:'` inflates *because of* the `candidates were:` recaps. It does inflate — but **not for that reason**: `candidates were:` does not contain the substring `candidates:` **at all** and is itself invisible to the naive count. The inflation comes from **ROADMAP table rows carrying BRAINSTORM prose**. **Cite the command; do not re-inherit the explanation.**

⚠️ **AND THE MEASURED RECORD, which every future pick should weigh: CHECK (2) HAS NEVER GONE DOWN.** Obtained by replaying the sentinel over every ROADMAP commit: the sentence count was unchanged in **every** commit from 2026-07-12 through phase 76 — ~17 phases — with history `0 → 1 → 3` and **not one decrease**. **Phase 74 landed three `ssl.*` stat names and made its OWN sentence LONGER** (its family clause SPLIT into a dynamic half plus a new `connection_error` clause). **Phase 73 predicted a clause REMOVAL and got a clause REPLACEMENT**, because the tracing backlog held more items than the clause named — it still does (six follow-ons where the clause names three). ⇒ **the candidates sentence is a WINDOW onto a larger deferred backlog, not an inventory of it.** A narrowing row does **not** silence check (2), and narrowing has not converged in this lineage.

⚠️ **THE PRIOR ROUTER'S SELECTION CALCULUS IS REFUTED AS ARITHMETIC, AND THE CORRECTED VERSION IS WEAKER.** The phase-77 router claimed *"a candidate that clears a check-(3) blocker is worth **strictly more** than one that does not."* **It is not**: opening a family **MINTS** a check-(2) sentence, so the item count went **3 never-opened + 3 sentences → 2 + 4 = 6 → 6, NET ZERO.** What survives, and what actually justified the phase-77 pick: **opening a family is the ONLY way that family's work becomes enumerable at all — a prerequisite, not a shortcut.** Do not restore the stronger claim.

⚠️ **NEVER WRITE A SENTINEL'S OWN MATCHER STRING INTO A FILE THE SENTINEL GREPS.** The sentinel greps **`docs/envoy-go/ROADMAP.md`** — writing these strings *here* is safe; writing them *there* is not. This fired LIVE at the phase-76 BRAINSTORM, twice in one commit: row 76's first draft quoted check (3)'s marker phrase for the gRPC family — *inside the sentence explaining why quoting it would be gaming the check* — and check (3) came back with **gRPC silently GONE**. **`grep` cannot tell a mention from a use.** ⚠️ **A SPEC normally leaves `ROADMAP.md` BYTE-UNTOUCHED; if this one edits it, re-run every sentinel check AFTER your edits land.**

⚠️ **CHECK (1)'s BLIND SPOT — RE-DERIVE IT; NEVER COPY IT.** At this tip it is **109 data rows, 105 matched, FOUR misses** — `| 00 | bootstrap | — | done |` (the `[0-9.,  ]*` field does not match an **em-dash**), `| 04 | http-1.1 | 03 | done |` (the `[a-z0-9-]+` slug field does not match the **DOT**), and **`| 28.1a |` / `| 28.1b |`** (the `^\| [0-9.]+ \|` id field cannot match a **letter suffix**). All four are `done`, and **row 77 is among the MATCHED** — so there is no current impact, **but a NEW row whose id, slug or "after" cell takes any of those shapes would be INVISIBLE to check (1) and would read as `done` forever.** ⚠️ **This figure was recorded WRONG in two consecutive lineages** before being re-derived correctly at phases 75 and 76. **Derive it yourself.**

==============================================


## STATUS AS OF 2026-07-26: PHASE 77 IS **BRAINSTORMED**. ROW 77 IS `in-progress`. THIS SESSION PERFORMS THE **PHASE-77 SPEC**.

**What the phase-77 BRAINSTORM landed** (`git log --grep 'phase 77'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): **the FIRST Runtime-+-hot-restart-family row — THE FAMILY OPENS** — self-picked per the standing directive, from a state where **every chartered row was already `done`** (the first such registration in the project's history). Docs-only: ZERO production `.go`, ZERO test `.go`, `DECISIONS.md` **BYTE-UNTOUCHED**. Four read-only re-costing agents at tip `f48975c4` on disjoint remits with PRIVATE scratch, two running LIVE reference probe fleets.

**THE SUBJECT.** Today envoy-go **boot-REJECTS a config the reference accepts and serves** — `internal/bootstrap/bootstrap.go:568-569`, a raw-YAML **generic-map pre-check** that fires on mere key presence, **BEFORE** `protojson.Unmarshal`. Row 77 lifts that reject **for the `static_layer` arm ONLY** and lands an `internal/runtime` `Snapshot` (recursive Struct→dotted-path flattening at unbounded depth, distinct-key **UNION** across layers, precedence-collapsed) **in the phase-00 placeholder directory that ALREADY EXISTS** ⇒ **+0 new packages**. Anticipated envelope: **+2 stats (1205 → 1207: `runtime.num_keys`, `runtime.num_layers`) / +1 fixture (`0118`) / +0 fuzzers / +0 BackendKinds / +0 modules / +0 new PUBLIC surface** / ONE new **ADR-0299** claiming the FIRST family ordinal.

### What matters for the NEXT session, in order

1. **⚠️ RE-RUN ALL THREE SENTINEL CHECKS YOURSELF AND RECORD THE ACTUAL OUTPUT; DO NOT PREDICT IT.** Create `stop` if and ONLY if all three print nothing. A SPEC normally leaves `ROADMAP.md` byte-untouched, so one run at session open suffices — **unless you edit it**, in which case re-run after.

2. **⚠️ THIS IS A SPEC. SETTLE THE D-RSL-* DOCKET *BY EXECUTION*, NOT BY REASONING.** The phase-76 docket was settled by execution on every item, and **that is precisely what refuted its own headline claims.** The docket, from BRAINSTORM §7:
   - **D-RSL-EMPTY — ⚠️ THE LOAD-BEARING ITEM.** `layered_runtime` present with **ZERO declared layers** reads `num_layers: 1` with `layers: [""]` on the reference — shape-independent across three spellings — and a **200-vs-503 cross-product with BOTH controls** proves that phantom layer is a synthesized, **WRITABLE ADMIN** layer (`POST /runtime_modify` ⇒ **200** there; ⇒ **503 `No admin layer specified`** both when `layered_runtime` is absent AND when one static layer is declared — the latter being the decisive negative that rules out "auto-appended whenever layers exist"). Options: match it (⇒ envoy-go synthesizes a layer it cannot write to); **PARSE-REJECT the present-but-zero-declared case** (*recommended*); or leave it undefined (**rejected in advance — an undefined arm is a latent divergence**).
   - **D-RSL-STATS.** **+2** as hypothesised, or **+9** full reference name-parity with seven permanently-zero names? The reference emits **NINE** `runtime.*` names in **every** arm, and phase 41's *parse-and-register-emit-0-but-DEFER* is a real precedent — but the project asserts **NAMED SUBSETS** cross-side, never the full set, so +2 creates no name-set divergence.
   - **D-RSL-NUMKEYS-DEEP.** Do duplicate leaf paths from different nesting shapes in ONE layer (`a.b: 1` alongside `a: {b: 2}`) collide, error, or double-count? **NOT PROBED. OWED.**
   - **D-RSL-EMPTYSTR.** In a two-layer overlap, does a key set to `""` in the overriding layer **clear** or **shadow** the lower value? ⚠️ **`""` is ALSO the positional "not defined here" sentinel, so the `/runtime` body CANNOT discriminate** — probe `final_value`, not the body.
   - **D-RSL-REJECT.** Confirm the reject roster arm-by-arm with byte-stable wording, and ⚠️ **do NOT assume ADR-0016 covers unknown subfields once the pre-check is lifted — EXECUTE it.**
   - **D-RSL-SPLIT** · **D-RSL-ADR**.

3. **⚠️ THE 9-11 TASK FIGURE IS A FLOOR, NOT AN ESTIMATE — AND THE RECORDED ~10-14 IS SUPERSEDED FOR A REASON THAT MATTERS MORE THAN THE NUMBER.** The recorded figure was a **document read, i.e. a CARRIED claim**; 9-11 came from a fresh re-costing that found the groundwork already laid (the package directory exists, `bootstrapv3`/`structpb` are already imported by production code, `FuzzBootstrapLoad` already exists). ⚠️ **But the fresh figure ALSO under-counted: it did not price the bootstrap reject roster, which is this row's real hidden cost.** That roster must land **ATOMICALLY** with the lift (`reference_lifted_reject_hidden_enforcement`):
   - **three unsupported oneof arms** — `disk_layer`, `admin_layer`, `rtds_layer`. ⚠️ **`rtds_layer` matters most**: silently ignoring it means a config asking for **DYNAMIC** runtime quietly gets **STATIC** values.
   - **four semantic arms the reference enforces via PGV, which envoy-go does NOT run** (`reference_pgv_forecloses_go_hazard`) — empty `name`, missing `layer_specifier`, duplicate layer name, more than one admin layer. All four measured on the reference via `--mode validate`, with a **positive control** (an empty `layers: []` returning rc=0, so the gate is not stuck-red).
   - **two loader-time value arms** — a `static_layer` value that is a **list** or **null** boot-FAILS with `Invalid runtime entry value for <key>`; each reproduces alone, and the `null` case pairs with `reference_protojson_null_decodes_to_nil`.
   - ⚠️ **Today's message is an inline `fmt.Errorf`, NOT a named constant**, so the row **CONVERTS as well as adds**; pin with a `TestParseRejectConstants_ByteStable` per the landed discipline.
   - ⚠️ **`TestLoad_RejectsLayeredRuntime` must be REPLACED, not extended** — it asserts only that the error *contains* `layered_runtime` and, unlike its `dynamic_resources` sibling, does **not** assert the `"bootstrap: "` prefix.

4. **⚠️ THE SEMANTICS ARE ALREADY PINNED BY MEASUREMENT ACROSS ELEVEN LIVE ARMS — INHERIT THE RULES, RE-VERIFY THE NUMBERS.** `num_keys` = size of the **union of flattened leaf paths across all layers**; flattening is **unbounded depth**, dot-joined; a `{numerator,denominator}` Struct **TERMINATES** flattening and counts as exactly **ONE**; an empty-string value **still counts**; a key in N layers counts **ONCE**. `num_layers` = the **declared** count, in declaration order. `num_keys == len(entries)` and `num_layers == len(layers)` held in **every** arm. ⚠️ **A single-layer config CANNOT discriminate union from per-layer sum** — the **2-layer overlap** arm is load-bearing and must be in the fixture, alongside a **nested** key (pins leaf-flattening: 2 where naive gives 1) and a **fractional** key (pins the non-flattening exception: 1 where naive gives 2).

5. **⚠️ THE FIXTURE MUST NOT GO THROUGH `ProbeAdmin`.** `compareAdminResponses` (`test/differential/runner_test.go:1394`) compares the body **BYTE-EXACT**, and the reference `/runtime` body is **doubly non-deterministic**: JSON key order randomized **PER REQUEST** (three consecutive GETs gave three distinct md5s with an **identical** sort-keys md5) and the Struct debug-string prefix randomized **PER PROCESS** (flipping across a `docker restart`). ⇒ assert **STATS** via the existing `fixture.StatsAsserter` seam (dispatched at `runner_test.go:1347-1349` with **both** admin addrs) over `/stats/prometheus` on a **NAMED SUBSET** — the phase-75 `0110` Shape-A precedent. Both randomizations contaminate only the **body**, never the gauge values. Both gauge NAMES were measured present in **all eleven arms including the `layered_runtime`-absent arm (value 0)** ⇒ **no name-presence divergence**, so the name set may be asserted unconditionally.

6. **⚠️ THE BROKEN-GATE COUNT IS NOW EIGHT, AND THE NEWEST IS A FALSE NEGATIVE.** A `+0`-production stage is exactly where a broken gate hides.
   - **NEW (G-C): a RANGE gate CANNOT detect anchor drift.** An out-of-range check over every Go line-cite in `BEHAVIOR_CONTRACT.md` (`line > wc -l`) printed **NOTHING** on a tree carrying **three** stale cites, because a **+20** drift stays **in range**. ⚠️ The worst instance: `BEHAVIOR_CONTRACT.md:1857`'s structural evidence for the **settled QUIC `ssl.*` PARITY determination** cites two ranges that have **BOTH drifted +20**, one now landing on the **bind-failure unwind block**, which does not support the claim at all. **Only content-anchoring fixes this** (`reference_gate_command_negative_control`).
   - **`go doc -all` diffed RAW is NOT symbol-scoped and FALSE-POSITIVES on a correct tree** — RE-EXECUTED at this tip and reproduced: the `0061` driver emits **51** raw lines but only **1** symbol-scoped line, so any legitimate comment edit reads RED. **Symbol-scope it** (`^(func|type|var|const)` + indented exported members).
   - **`gofmt -l` NEVER exits non-zero** — RE-EXECUTED and reproduced (exit **0** while printing the unformatted file). Any gate chaining it with `&&` is **INERT**. Gate on OUTPUT: `[ "$(gofmt -l . | wc -l)" -eq 0 ]`.
   - **`go doc -all <pkgA> <pkgB>`'s fail-open is specific to the `./` PREFIX** — a BARE import path exits **1**; a `./`-prefixed one exits **0**, silently dropping the second argument **even when it names a directory that does not exist**. **Audit ONE package per invocation.**
   - **The import gate `impblock` has THREE defects** — it prints `FILENAME` (normalise to basenames) **and** matches only `^import \($`, emitting ZERO lines for single-line `import "testing"` files.
   - **A wrong-baseline caveat can name the WRONG sha** ⇒ **verify a baseline is an ancestor before trusting a warning about it.**
   - **A sha256 byte-untouched roster CANNOT be copied forward blindly** — set-difference it against the EDIT roster (`reference_plan_schedules_edits_to_a_byte_gated_file`). ⚠️ **`internal/bootstrap/` and `internal/runtime/` must NOT be byte-gated for this row.** ⚠️ **A DELETION reads as "no mismatch"** — the roster needs a **MISSING** leg as well as a **MISMATCH** leg.

7. **⚠️ A HARNESS'S EXIT CODE IS NOT THE COMMAND'S EXIT CODE.** At the phase-76 IMPL the background-task notification for a `-race` full suite reported **"exit code 0"**; that was the **WRAPPER shell's** status. The logged inner status was **1**, and the run had **PANICKED** at 84 of 119 subtests. **Always capture the inner status explicitly** (`cmd; echo "EXIT=$?" >> log`) and **derive the PASS/FAIL tally from the log.**

8. **⚠️ ANCHORS DRIFT UNDER A STAGE'S OWN EDITS, AND A FINDING CAN GO STALE THE SAME WAY.** A line cite drafted at the SPEC, for a file the IMPL then edits **above** that cite, is stale **BY CONSTRUCTION** rather than by neglect. Phase 76 saw a break target move `:295 → :307`, a *"ONE test of 403"* finding become *"TWO of 404"* **because that same row's own new test was the second**, and two greps go `2 → 9` and `3 → 16` because the row added the consumers. **Re-derive every count at the tip that reads it.**

9. **⚠️ SELF-FALSIFYING COUNTS REMAIN THE LINEAGE'S MOST PERSISTENT DEFECT SPECIES.** It has fired in ADR-0296 ¶3, ADR-0297 ¶7 **and** ¶9, at the phase-76 BRAINSTORM (escalating from a wrong NUMBER to a **flipped termination-sentinel check**), and at the phase-76 PLAN (recording three greps' counts in `STATE.md` changed each by +1). **Never write a token into the file whose occurrences of that token you are counting.** State the property with no numerals, or scope the count to a line. ⚠️ **The ADR-0288 singleton greps over `STATE.md` do NOT all return the same number** — the rule statement at `STATE.md:7` is the FIRST hit and the live value the second, so **a close script taking `head -1` reads the RULE, not the value**; anchor on `^- \*\*<field>` instead, and **never "fix" a count down**.

10. **⚠️ A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX — the FIFTH consecutive stage at which this held.** Phase 76's `-race` run failed on `0082-grpc-access-log-buffering`, **not on its roster by name** (its sibling `0083` fired the byte-identical signature at phase 75). **Isolate-re-run, then state the classification AND its evidence.** Live hazards: the PRE-EXISTING `internal/cluster` `-race` flake (`TestOutlierDetector_ConcurrentEjectExactlyOnce`) · the full-suite startup flake (`subject ready: EOF` **and** `bind: address already in use`, both failing **BEFORE any assertion**, the latter as a **PANIC that aborts the whole binary**, and it fires **MORE readily under `-race`** — three data points) · `reference_sds_init_fetch_timeout_dial_budget_flake` · two still-**UNINDEXED** load flakes (`internal/httpclient TestOptions_ZeroValue_NoOpDefaults`, `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery`). ⚠️ **`0061-lb-ring-hash` is NO LONGER a live flake** — after phase 76 a spread failure there is a **FINDING**, not a flake.

11. **Counts at exit (re-run MECHANICALLY in the worktree; never copy them):** fixtures **119** (`ls -d test/fixtures/[0-9]*/ | wc -l`; next-free **0118**) / fuzzers **55** (`grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`) / **stat surface 1205** (BEHAVIOR_CONTRACT doc count; ⚠️ **NO mechanical command**, and the chain is discontinuous in TWO recorded places — assert the DELTA, never the total) / **BackendKind tail 38** (⚠️ a TAIL VALUE — the file declares **39** constants, 0-38; **do NOT "fix" 38 to 39**) / go.mod modules **2** (⚠️ the phase-61.2 lineage figure — `quic-go` + `qpack` — **NOT a repo total**; the single `go.mod` requires **67** = 18 direct + 49 indirect; **do NOT "fix" 2 to 67**) / internal packages **73** / DECISIONS tail **ADR-0298 COMPLETE** (next-free **ADR-0299**, CLAIMED by row 77 but not yet drafted) / next-free reference port **10450** (⚠️ **NOT** fixture-index aligned).
    ⚠️ **A CONTROLLER SELF-CORRECTION worth carrying:** the phase-77 BRAINSTORM's first next-free-port derivation printed a wrong figure because `104[0-9][0-9]` matched digits inside unrelated prose in two fixtures. The real `104xx` maximum in use is **10449**. **A probe must discriminate** (`reference_probe_must_discriminate`).

12. **⚠️ TWO DOCUMENTARY DEFECTS FOUND AT THE BRAINSTORM, NAMED AS DEFERRALS AND DELIBERATELY NOT FIXED.** Do not silently absorb either; charter them or leave them named.
    - **The documented PUBLIC IMPORT PATH DOES NOT EXIST.** `ROADMAP.md` and `BEHAVIOR_CONTRACT.md` document the project's public package under a module path that `head -1 go.mod` **disagrees with**, while the live import in `cmd/envoy-go/main.go` and `internal/boot/boot.go` uses the real one. It occurs **20** times across FOUR docs. **A session copying the documented path writes code that does not compile.**
    - **`BEHAVIOR_CONTRACT.md:1857`'s two stale +20 cites** (item 6, G-C), plus two more stale cites in the same file. Fix by replacing line numbers with **symbol anchors** so they cannot drift again.

13. **The BANKED next subject, for whenever phase 77 closes: `fault.abort.grpc_status`, re-costed to ~7-9 tasks / +0 on EVERY envelope axis.** Its current state is **worse than "ignored"** — `abortEnabled` is set only inside the `HttpStatus` type switch, so a `grpc_status` variant fires **no abort at all** and the request reaches the backend. A 4-arm codec×content-type probe found the reference branches on the request **`content-type`** not the codec, returns HTTP **200** in every arm, and emits `grpc-status`/`grpc-message` as response **HEADERS not trailers** — which **RETIRES the framework blocker** that inflated its inherited ~10-13; `SendLocalReply` already expresses the shape. ⚠️ **It is an HTTP-filters-family row and does NOT open the gRPC family**, and the **PROVENANCE** of the three-stage claim that it does is now located: row 09's own summary carries `(gRPC family)` as a **ROUTING LABEL** in a list whose sibling entry labels runtime keys the same way. **Reading a filing label as a charter is the error.** The clinching evidence is the probe: a feature exercisable over plain HTTP/1.1 with **no gRPC client, backend, or framing anywhere** cannot open the gRPC family.

14. **Other rivals, re-costed at the BRAINSTORM — do NOT re-inherit their older figures.** `Listener.stat_prefix` **10-12** (⚠️ the reference **MERGES** two listeners sharing a prefix into ONE scope with the counter **SUMMED**, where envoy-go's registry **PANICS on duplicate registration**; and its charset guard is the **same validator** blocking the Observability family's `ssl.*` dynamic families, so **solving it once counts twice**) · `upstream_cluster` **6-8**, ~20-25 lines / 3 files (**not** ~85-100 / ~18 — phase 73 already built the seam), premise still **UNVERIFIED** pending one probe · `ssl.connection_error` **12-15** (⚠️ the reference's counter is BoringSSL **`SSL_ERROR_SSL`, NOT `SSL_ERROR_SYSCALL`** — RST / clean close / zero-byte close all increment **0** ⇒ `syscall.ECONNRESET` is needed to stop it **OVER**-firing, the deny-list needs **three** predicates and is **OPEN**, and ⚠️ **`tls.RecordHeaderError` is returned BY VALUE**, so a pointer-drafted predicate **compiles and never matches**) · a **maintenance bundle** of six items at 1-2 tasks each with **ZERO production lines**, all genuinely owed, none touching check (2) or (3).

## What THIS session does — the phase-77 SPEC

One stage per session. Settle the **D-RSL-*** docket **BY EXECUTION** (item 2), write `docs/envoy-go/phases/77-runtime-static-layer/SPEC.md`, draft **ADR-0299 §Context** in `DECISIONS.md` per **ADR-0044-as-used** (⚠️ **ADR-0044 itself does NOT contain that discipline**), and roll `STATE.md` (**lifecycle-state 1 → 2**) plus this file to the phase-77 PLAN. **`ROADMAP.md` stays BYTE-UNTOUCHED at a SPEC** and **ROW 77 STAYS `in-progress`**.

⚠️ **ADR-0299 claims the FIRST Runtime-family ordinal.** ⚠️ **ADR-0089 stays BYTE-UNTOUCHED and that is deliberate** — it defers `/runtime` and `POST /runtime_modify` to this family, but row 77 lands **neither**, so the un-defer is owed only by whichever later row does. ⚠️ **Write NO whole-file grep count into the ADR** (item 9).

Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** (`reference_parallel_subagents_private_scratch`) — and ⚠️ **if two agents share ONE worktree, they must stage by EXPLICIT PATH; a `git commit -am` sweeps in a sibling's uncommitted work.** Per-task `gofmt` + `golangci-lint` on touched packages (`feedback_pertask_gofmt_lint`).

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. ⚠️ **THIS HAS NOW FIRED LIVE IN EIGHT CONSECUTIVE SESSIONS** — including inside the phase-76 IMPL's own G0 gate tripwire, where bare `pwd` printed the MAIN checkout on `master` while `git -C` correctly reported the stage branch. **Use `git -C <abs-worktree-path>` for every git command, prefer absolute paths over `cd`, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline) + §Project counts + the §Recent lineage (five bullets: phase-77 BRAINSTORM + phase-76 IMPL + phase-76 PLAN + phase-76 SPEC + phase-76 BRAINSTORM).
2. `docs/envoy-go/phases/77-runtime-static-layer/BRAINSTORM.md` — **the primary input.** §0 the sentinel findings and the refuted selection calculus · §2.3 the eleven measured arms · §2.4 why the fixture cannot use `ProbeAdmin` · §2.6 the rivals with the evidence that settled each · §6 the anticipated edit sites and the reject roster · §7 the **D-RSL-*** docket.
3. `docs/envoy-go/ROADMAP.md` — row 77 at `:139`, the new **Runtime** family-open paragraph, and the **four** live long-form `candidates:` sentences (⚠️ **FIVE** matches under the broadened matcher).
4. `docs/envoy-go/phases/76-lb-ring-hash-spread-margin/PROGRESS.md` — **the richest single source on how this lineage's stages fail.** Its `# IMPL record` carries every task's ACTUAL result, which break fired and which deliberately did NOT, the **seventeen** refuted claims, and two broken gate commands.
5. `docs/envoy-go/DECISIONS.md` — **ADR-0187** `:11280` and **ADR-0195** `:12413` (the SIX `runtime_key` parse-reject arms forward-pointing at this family, both carrying an explicit *"Forward-pointer to the Runtime/RTDS family phase"*; ⚠️ **`parseRejectEnabledRuntimeKey` is the SAME identifier in BOTH packages**, so a roster keyed on constant name undercounts six to five) · **ADR-0089** `:3514` (the admin-endpoint deferral list, whose `/runtime` and `POST /runtime_modify` rows name this family BY NAME) · **ADR-0016** (unknown-field rejection — load-bearing once the pre-check is lifted) · **ADR-0278** `:16672` (already names the `internal/runtime` placeholder as this family's expansion) · ADR-0298 · ADR-0044 (⚠️ it does **NOT** contain the §Context-draft discipline — use `ADR-0044-as-used`) · ADR-0045 · ADR-0106 · ADR-0288.
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — especially `reference_lifted_reject_hidden_enforcement`, `reference_pgv_forecloses_go_hazard`, `reference_pinned_dep_missing_field_free_reject`, `reference_protojson_null_decodes_to_nil`, `reference_dynamic_stat_name_charset_guard`, `reference_gate_command_negative_control`, `reference_sentinel_matcher_string_self_clears`, `reference_quoting_is_not_executing`, `feedback_brief_citations_not_evidence`, `reference_deferred_candidate_cost_restale`, `reference_next_prompt_tracked_despite_gitignore`, `feedback_git_worktrees`, `feedback_execution_style`, and **`reference_bash_cwd_reset_commits_to_main`**.
