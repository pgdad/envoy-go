=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**Check (1) is NOT permanently silent.** It is silent only while no phase is open. Row 56 is `in-progress`, so **check (1) prints `NOT DONE: row 56`** and will keep doing so until the 56.2 IMPL flips it. **The sentinel is an AND of all three checks**; (2) and (3) carry the remaining work.

**SENTINEL RE-CHECKED 2026-07-10 (post-phase-56.1-IMPL) — it does NOT fire. Do NOT create `stop`.** Evidence:
- (1) prints `NOT DONE: row 56` ⇒ phase 56 is OPEN (the whole 56.1 leg — BRAINSTORM + SPEC + PLAN + IMPL — has landed; the 56.2 bodies leg remains).
- (2) prints the Observability family's deferred candidates ⇒ the **Observability family STAYS OPEN**.
- (3) prints `NEVER OPENED: HTTP/3`, `gRPC`, `xDS`, `Runtime`, `WASM` ⇒ FIVE families never opened; the **Operational-tooling family** also STAYS OPEN.

**STATUS AS OF 2026-07-10: phase 56.1 (`http-tap-filter`, HEADERS leg) IMPL is DONE and LANDED.** Row 56 is `in-progress`. Counts at exit: stat surface **1201** · fixtures **101** (tail `0099-http-tap-headers`) · fuzzers **53** (added `FuzzMatchPredicateCompile`) · BackendKind tail **38** (+0) · DECISIONS tail **ADR-0273** (next-free **ADR-0274**) · new Go packages **2** (`internal/headermatch`, `internal/matchpredicate`) + the filter package `internal/filter/http/tap`.

**Locating the stage squash (a router cannot name its own sha — writing the sha changes it).** Identify it by SUBJECT, never by sha and never by position:
```sh
git log --oneline --grep='^phase 56.1 (http-tap-filter, headers leg) IMPL' -1
```
This stage folded its own `next-prompt.txt` roll INTO the squash (per `reference_next_prompt_tracked_despite_gitignore`), so the squash **IS** the master tip — *unless* a router-only fixup commit sits on top. **Do not assume either shape.** The `--grep` above is correct in every shape.

**THE NEXT ACTION: the phase-56.2 (`http-tap-filter`, BODIES leg) SPEC.**

==============================================

## What THIS session does (a SPEC, lifecycle-state 1 → 2)

The phase-56 BRAINSTORM (Q0–Q6) was authored at the PHASE level and SPLIT BY CONCERN (Q6) into 56.1 (headers, DONE) and 56.2 (bodies). So 56.2 starts at SPEC — there is no separate 56.2 brainstorm. This session writes **`SPEC-56.2.md`** (docs-only, lifecycle-state 1 → 2). NO production code. Fresh worktree `.worktrees/phase-56.2-spec`, branch `phase-56.2-http-tap-filter-spec`. Row 56 STAYS `in-progress`.

1. Re-verify the cold-start baselines against the CURRENT master tip (`git log --oneline -1` — do not assume): fixtures **101**, fuzzers **53**, DECISIONS tail **ADR-0273**, BackendKind **38**, stat surface **1201**.
2. **Read the landed 56.1 artifacts as the foundation** — the 56.2 SPEC builds directly on the proven headers spine:
   - `docs/envoy-go/phases/56-http-tap-filter/SPEC-56.1.md` §2 (the bodies deferral) and §1.1 **AMEND-TAP-BODY** — the LOAD-BEARING finding: the reference ALWAYS captures bodies (no suppression knob; `max_buffered_rx_bytes: 0` + `max_buffered_tx_bytes: 0` still emits `{"as_string": "", "truncated": true}`), but a ZERO-LENGTH body OMITS the `body` field. 56.1 deliberately used GET→204 so no body was ever rendered; 56.2 is where a body EXISTS.
   - `docs/envoy-go/DECISIONS.md` **ADR-0273** (the landed headers-leg decision) — the marshal options, the `filePerTapSink`, the reject roster, the `record_*` flags, the coverage boundaries.
   - `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the phase-56.1 block (esp. the byte-exact `file_per_tap` rendering + its **detrand build-scoped caveat**: protojson `internal/detrand` randomizes inter-token whitespace per binary, so goldens compare CANONICAL JSON, not raw bytes).
   - `docs/envoy-go/phases/56-http-tap-filter/PROGRESS-56.1.md` §Findings log (the three IMPL findings, incl. the two vacuous-proof classes and the detrand decision) and the 14-row deliberate-break ledger — the traps to honor.
   - The landed code: `internal/filter/http/tap/{tap.go,trace.go,sink.go,config.go}`, `internal/headermatch/`, `internal/matchpredicate/`, and the `0099-http-tap-headers` fixture.
3. **Write `SPEC-56.2.md`** (+ update the phase `README.md`), covering the BODIES concern on top of the proven spine. The scope, per SPEC-56.1 §2 + README §"56.2":
   - **`Message.Body` population** — capture request body (`DecodeData`) and response body (`EncodeData`) bytes; render as `datatapv3.Body` with the `as_bytes`/`as_string` oneof.
   - **`OutputConfig.max_buffered_rx_bytes` / `max_buffered_tx_bytes`** (f2/f3, `*wrapperspb.UInt32Value`) — the per-direction buffer caps; `0` still captures an (empty, truncated) body per AMEND-TAP-BODY.
   - **`Body.truncated`** — set when the captured bytes hit the cap.
   - **The `JSON_BODY_AS_BYTES` (=0, default) vs `JSON_BODY_AS_STRING` (=1) distinction** — INDISTINGUISHABLE at 56.1 (no body), but at 56.2 they diverge: `_AS_BYTES` renders `{"as_bytes": "<base64>"}`, `_AS_STRING` renders `{"as_string": "<utf8>"}`. 56.1 ACCEPTED both; 56.2 must render each correctly. (The 3 `PROTO_*` formats stay rejected.)
   - **The data-hook seam**: 56.1's `DecodeData`/`EncodeData` are inert pass-throughs (`DataContinue`). 56.2 makes them BUFFER bytes into the pending trace, honoring the caps. Confirm the framework's data-hook contract (endStream signaling, chunk accumulation) against the landed HCM data plumbing — re-derive from source, do NOT trust 56.1's comments.
   - **A new differential fixture `0100-http-tap-bodies`** (fixtures 101 → 102) — drive a request WITH a body and a backend returning a body, assert the captured `request.body`/`response.body` cross-side (mind AMEND-TAP-BODY: assert the PAYLOAD/`as_string`, not framing; the reference always captures). Plan the byte-exact-vs-canonical-JSON comparison per the detrand finding. Consider a cap-hit arm proving `truncated: true`.
   - **ADR-0274** (§Context DRAFT at this SPEC per ADR-0044; §Decision/§Consequences at the 56.2 IMPL). Anticipated counts at the 56.2 IMPL: fixtures **102**, DECISIONS tail **ADR-0274**; stat surface likely **+0** (no new counter anticipated — CONFIRM by probe); fuzzers possibly +1 (a body-format renderer fuzzer — decide in the SPEC); BackendKind — decide whether the body-returning backend is a reuse or a new kind.
4. **This is a SPEC (docs-only).** No production code, no new fixtures land yet — the SPEC DESIGNS them. Empirical pins for the bodies behavior (does the reference set `truncated` at exactly the cap or cap+1? how does `as_string` handle non-UTF8? does `max_buffered_*: 0` truly emit an empty-truncated body?) should be PROBED against `envoyproxy/envoy:contrib-v1.37.2` (ADR-0227) on a Docker bridge network with a FRESH probe container per arm (`feedback_probe_fresh_container_per_arm`), exactly as SPEC-56.1 §11 did — and each pin carries its verbatim reference output.
5. Land the SPEC (commit + push per `feedback_push_to_origin`; docs-only), then **ROLL THIS FILE FORWARD to the phase-56.2 PLAN**.

## Honor throughout (the discipline, unchanged)
- **Lifecycle = one stage per session.** This session: the phase-56.2 SPEC ONLY (docs-only).
- **`next-prompt.txt` IS TRACKED** (it appears in `.gitignore` line 2, but a `.gitignore` entry does not apply to an already-tracked file — `git ls-files --error-unmatch next-prompt.txt` succeeds). Edit it inside the stage worktree and fold it into the squash (`reference_next_prompt_tracked_despite_gitignore`). A router can't name its own sha — locate the squash by SUBJECT (`git log --grep`), never by position.
- **A BRIEF'S CITATIONS ARE NOT EVIDENCE** (`feedback_brief_citations_not_evidence`). Re-derive every `file:line` from source; never verify a citation against the document that made it. This row has paid out THREE times now (a false double-`OnDestroy` at the SPEC; two vacuous-proof defects at the PLAN; two MORE vacuous-proof classes caught AT THE 56.1 IMPL by controller-run break verification — see PROGRESS-56.1 §Findings IMPL-1/IMPL-2).
- **Deliberate-break discipline** (for the eventual IMPL, but the SPEC must design breaks that can actually FIRE): a reject arm behind a defensive `default` needs an EXPLICIT-error-substring assertion or the break falls through and is VACUOUS (IMPL-1); a shared-value/timing test needs a predicate that genuinely spans both directions or a decoder-only path resolves alone (IMPL-2); a decode-based body check cannot tell OMITTED from `null` — pair it with a RAW-BYTES check (the 56.1 (5)/(5b) split). The `-run` selector must be the FULL `TestDifferential/<dir>` (`reference_differential_run_selector`); breaks use `-count=1` (`reference_differential_break_protocol_count1`).
- **protojson detrand** (ADR-0273 §Consequences): NEVER compare protojson output raw-byte-for-byte across build configs — use `encoding/json.Compact` canonicalization + detrand-robust substring pins. The differential decodes payloads and greps for keys; it never byte-compares.
- **Process anchors:** ADR-0106 (no parent rollup — row 56 flips `done` ONLY at the 56.2 IMPL) · ADR-0044 (§Context at SPEC; §Decision/§Consequences at IMPL) · ADR-0045 (the split gate is CONSUMED; 56.2 must NOT absorb 56.1 spillover) · ADR-0052 (atomic six-gate bundle) · ADR-0071 (two-step filter factory) · ADR-0080 (byte-stable rejects) · ADR-0196 (the encode-side `ResponseStatus()` accessor) · ADR-0227 (reference `envoyproxy/envoy:contrib-v1.37.2`) · ADR-0273 (the landed headers leg) · next-free **ADR-0274**.

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-56.2 SPEC is next).
2. `docs/envoy-go/phases/56-http-tap-filter/SPEC-56.1.md` — the headers-leg SPEC; esp. §1.1 AMEND-TAP-BODY, §2 (bodies deferral), §5 (the proto roster incl. `OutputConfig.max_buffered_*`, `OutputSink.Format`, `datatapv3.Body`), §11 (the probe method to reuse).
3. `docs/envoy-go/DECISIONS.md` — **ADR-0273** (the landed headers decision) + ADR-0044/0045/0052/0071/0080/0106/0196 for process.
4. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the phase-56.1 block (the marshal options + the detrand caveat + the reject roster + the coverage boundaries).
5. `docs/envoy-go/phases/56-http-tap-filter/PROGRESS-56.1.md` — §Findings log (IMPL-1/2/3) + the 14-row break ledger (the traps to honor).
6. The landed 56.1 code: `internal/filter/http/tap/`, `internal/headermatch/`, `internal/matchpredicate/`, `test/fixtures/0099-http-tap-headers/`.
7. `docs/envoy-go/STATE.md` — the active-phase header + full history.
8. `docs/envoy-go/ROADMAP.md` — §Schema, row 56, and the Observability family paragraph.
9. `docs/envoy-go/phases/56-http-tap-filter/README.md` — the phase orientation (56.1 done, 56.2 next).
10. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline and the `reference_*` traps.
