=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, the active phase is fully landed, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action. (The sentinel does NOT apply right now — phase 49 is chartered, its BRAINSTORM + SPEC + PLAN are LANDED, and the next stage is its IMPL.)

**STATUS AS OF 2026-07-02: phase 49 (`stats-sink-dogstatsd`) PLAN is LANDED; the NEXT stage is its IMPL.**
- The phase-49 PLAN landed: `docs/envoy-go/phases/49-stats-sink-dogstatsd/PLAN-49.md` (a 9-task TDD spine mirroring `PLAN-48.md`'s shape — one fewer task than 48 since `HostGatewayIP`'s pattern is DUPLICATED locally in the `0093` driver rather than built fresh; lifecycle-state 2 → 3; docs-only direct-on-master). It RESOLVES every §12 D-DSD-* PLAN question (see below). A `plan-document-reviewer` (general-purpose) pass APPROVED-WITH-FOLDS — ALL `bootstrap.go`/`main.go`/`internal/statssink`/`internal/stats` line citations + the ACTUAL landed `0092` driver code VERIFIED EXACT against HEAD, baselines (94 fixtures / 51 fuzzers) confirmed, the SN4 wire-name math independently hand-traced, the `hostGatewayIP` import-cycle reasoning confirmed by reading `runner_test.go`'s real package/imports — after ONE LOAD-BEARING fold (Task 2's regression check now explicitly names two PRE-EXISTING `bootstrap_test.go` sibling-reject tests, `sibling_unknown_sink`/`sibling_unknown_typeurl`, that use a `DogStatsdSink` config as their "still unsupported" example and will FLIP from reject to accept once this phase lands — a bare grep for old reject wording would NOT catch this) + four non-blocking folds (a `hostGatewayIP` line-span fix `508-586`→`508-589`; a wrong file-path fix `internal/bootstrap/registration_test.go`→`internal/statssink/registration_test.go`; two LoC-count off-by-ones; the post-Freeze `statsFlusher.Start` line numbers pre-supplied as `:384-393`). STATE.md active-phase header rolled to `phase 49 (stats-sink-dogstatsd) PLAN done`. **Re-verify with `git log --oneline -6` + `git status`; do NOT assume.**
- **A genuine wire-name bug was caught and fixed DURING the PLAN's own design** (before any test code existed against it): SPEC-49.md's §8.1/§11 D-DSD-SUBJECT text had named the `_2xx`-class HTTP counter's differential-subset entry as wire name `http.downstream_rq_2xx` — but `stats.ExtractTags`'s SN4 rule (`internal/stats/name.go:354-357`) REWRITES the residual to `http.downstream_rq_xx` and HOISTS the class digit into the `envoy.response_code_class` TAG (this was already visible in the SPEC's own live-probe evidence, just not reflected consistently in those two spots). Fixed via a follow-up SPEC-49.md commit (`b030df20`) BEFORE the PLAN's Task 7 `subsetNames`/`subsetTags` were written — the IMPL executes against the CORRECTED wire name throughout. **Do NOT regress this** — if re-deriving the `0093` fixture's subset names from scratch, the WIRE (post-`ExtractTags`) name for an `_Nxx`-suffixed counter is ALWAYS `..._xx` with the digit as a tag, never `..._Nxx` literally (contrast phase 48's plain-statsd sink, which applies NO tag extraction and DOES keep the literal `_2xx` in its wire name — a genuinely different mapping for the "same" underlying stat).
- **The §12 D-DSD-* PLAN questions are RESOLVED (do NOT re-litigate; the IMPL EXECUTES these):** D-DSD-SPLIT → NO sub-split (single flat row, ~250–320 LoC, 9 tasks; escape-valve UNCONSUMED). D-DSD-CONFIG-HOME → a parallel `DogStatsdSinkConfigs []DogStatsdSinkConfig` slice on `internal/bootstrap`; `main.go` runs THREE build loops into the ONE `statsSinks []Sink`. D-DSD-TAG-HELPER → the 1-line tag-key transform (`"envoy."+strings.TrimPrefix(key,"envoy_")`) stays DUPLICATED inline in `dogstatsd.go` (no shared helper with `label.go`'s `labelMapper` — a premature abstraction over a trivial expression, given the two call sites build different shapes: a `dto.LabelPair` field vs a literal wire-string segment). D-DSD-DELTA-REUSE → reuse `delta.go` `newDeltaState()`/`apply()` VERBATIM as a SECOND, INDEPENDENT sink-private instance (never shared with `StatsdSink`'s own instance — `DogStatsdSink` calls `newDeltaState()` fresh in its own constructor). D-DSD-ADDR → `UDPAddress string` host:port resolved at `NewDogStatsdSink` via `net.ResolveUDPAddr` (the `StatsdSinkConfig`/`NewStatsdSink` precedent, identical shape). D-DSD-RECEIVER-WIRING → the `0093` driver DUPLICATES the `0092` driver's LOCAL, unexported `hostGatewayIP` helper (`test/fixtures/0092-stats-sink-statsd/driver/driver.go:508-589`) VERBATIM — NOT the exported `differential.HostGatewayIP` (`test/differential/harness.go:503`), because `runner_test.go` (which lives in `package differential`) blank-imports the driver, so the driver importing `differential` back would risk a cycle; `test/helpers/statsdrecv` is EXTENDED in-place (a revised first-`|`-then-colon `ingest` split — the CURRENT last-colon split mis-parses a tagged line, since the `|#` tag suffix itself contains colons — plus a NEW `Tags(name) (map[string]string, bool)` accessor), backward-compatible with `0092`'s existing tagless usage. D-DSD-FUZZER → `FuzzDogStatsdSinkConfigParse` lands at 49 (fuzzers 51 → 52). D-DSD-STATS-FINAL → +0 via the existing `internal/statssink/registration_test.go` (`TestNoNewStat_StatsdRegistrationGuard` or its dog_statsd-aware successor). D-DSD-GAUGE-SUBSET → YES (`0093` adds `cluster.membership_total==1|g` tagged `{"envoy.cluster_name": <backend>}`, mirroring `0092`'s `membership_total`-not-`membership_healthy` precedent — `reference_membership_total_vs_healthy_gauge` — since the `0093` cluster, cloned from `0092`, carries no health checks).
- Counts at PLAN (re-verify, do NOT assume): stat surface **1200** (non-H2 **1196**) / fixtures **94** / fuzzers **51** / BackendKind **38** / DECISIONS **ADR-0265** (next-free **ADR-0266**).

=> The NEXT action is the **phase 49 (`stats-sink-dogstatsd`) IMPL** (lifecycle-state 3 → done). NOT the termination sentinel. NOT a new PLAN.

==============================================

## The NEXT action — the phase 49 (`stats-sink-dogstatsd`) IMPL
Execute `docs/envoy-go/phases/49-stats-sink-dogstatsd/PLAN-49.md` end-to-end (the 9-task TDD spine) **subagent-driven** (`feedback_execution_style`) in a **FRESH git worktree off master** (`feedback_git_worktrees` · `superpowers:using-git-worktrees`). This is the FINAL stage of phase 49 — its six-gate FLIPS ROW 49 `done` (the sole leg — ADR-0106; NO parent rollup; the Observability family STAYS OPEN) and ANCHORS ADR-0266 §Decision/§Consequences (ADR-0044). Lifecycle-state 3 → done.

**Execution mechanics (the controller discipline — honor verbatim):**
- **Subagent-driven** (`superpowers:subagent-driven-development`): dispatch a FRESH subagent per task (or a small batch); the CONTROLLER reviews between tasks.
- **Fresh worktree off master** (`feedback_git_worktrees`): create the worktree BEFORE Task 1; all task work lands there; the controller verifies the MAIN checkout stays clean + the branch undetached after each task (`feedback_subagent_worktree_detach` · `_path_targeting`).
- **Subagents commit LOCALLY only** (`feedback_subagents_no_push`); the global CLAUDE.md makes them AUTO-COMMIT (`feedback_subagent_autocommit_claudemd`) — do NOT fight it. The CONTROLLER verifies each commit (correct fileset, real non-vacuous tests via `-v` + read assertions, gates green), cleans stray next-task leak files, re-runs the FULL suite on the FINAL frozen HEAD, does the deliberate-break verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_push_to_origin`).
- **Per-task gates** (`feedback_pertask_gofmt_lint`): `gofmt -l` (empty) + `golangci-lint run` on touched pkgs + `go vet` + `go build ./...`.
- **Task 2's own gotcha** (folded into the PLAN by its reviewer): after the three-arm dispatch lands, run the FULL `internal/bootstrap` package (`go test ./internal/bootstrap/ -count=1`), not just a narrow `-run` filter — two PRE-EXISTING sibling-reject tests use a `DogStatsdSink` config as their "still unsupported" example and will silently flip from reject to accept otherwise.
- **Differential discipline (the `0093` task — Task 7):** `-run 'TestDifferential/0093'` NEVER bare (`reference_differential_run_selector`); `-count=1` on EVERY break + `-race` run (`reference_differential_break_protocol_count1`); the full-suite `subject ready: EOF` startup flake is isolatable (`reference_differential_fullsuite_startup_flake`); the Docker bridge + decode-ran proof — receiver datagram count > 0 (`reference_docker_probe_bridge_network`); the extended driver-owned UDP receiver is NOT a new BackendKind (`reference_differential_grpc_receiver_driver_owned`); the live reference is the wire truth (`reference_wire_format_both_sides_see_same_bytes`); two per-side receivers + hard Close (`reference_periodic_sink_differential_two_receivers`); the delta-sink stability barrier (`reference_delta_sink_differential_stability_barrier`); the `hostGatewayIP` LOCAL duplicate, NOT an import of `differential.HostGatewayIP` (`reference_host_gateway_ip_docker_desktop`). The CONTROLLER runs the live differential + the three deliberate breaks ((a) absolute-not-delta MUST fail the barrier; (b) dropped/wrong tags; (c) barrier-masks-(a)) + the 20/20 flake-soak + the FULL-package `internal/statssink` `-race` ITSELF.
- **The full-package `-race`** (`reference_full_suite_race_after_background_mutator`): the synchronous `DogStatsdSink` adds NO background mutator, but the `Flusher` ticker remains one (now feeding a THIRD periodic sink) — run the `-race` gate over the FULL `internal/statssink` package.

Anticipated counts at the IMPL six-gate (the PLAN's Global Constraints): stat **1200** (+0 — no self-stats, no sink cluster) / fixtures **95** (`0093-stats-sink-dogstatsd`) / fuzzers **52** (`FuzzDogStatsdSinkConfigParse`) / BackendKind **38** (the extended driver-owned UDP receiver) / DECISIONS **ADR-0266** (next-free ADR-0267); ZERO new packages, ZERO new go.mod modules (`go mod tidy -diff` EMPTY). **Row 49 FLIPS `done`** at this six-gate.

After the IMPL lands (squashed + pushed; ADR-0266 body + BEHAVIOR_CONTRACT + STATE/ROADMAP [row 49 `done`] all committed), ROLL THIS FILE FORWARD: CHECK the ROADMAP for any remaining CHARTERED Observability row — if a human has chartered the next deferred candidate (`max_bytes_per_datagram` batching / `graphite` / OTLP-metrics sinks / the plain-statsd `tcp_cluster_name` transport / tracing extras / the tap filter), roll to ITS BRAINSTORM; ELSE the TERMINATION SENTINEL applies (all chartered work complete — create the `stop` file and exit). Mirror the phase-48 IMPL → row-48-done → re-open precedent (`docs/envoy-go/STATE.md`'s `phase 48 (stats-sink-statsd) IMPL done` entry).

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** The next stage is the phase-49 IMPL (subagent-driven in a fresh worktree; FLIPS row 49 `done`). Lifecycle-state 3 → done.
- **Subagent-driven execution** (`feedback_execution_style`) in a **fresh worktree** (`feedback_git_worktrees`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit, re-runs the full suite on the frozen HEAD, does the deliberate-break verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). Per-task gates: `gofmt -l` + `golangci-lint` + `go vet` + `go build ./...` (`feedback_pertask_gofmt_lint`).
- **Process anchors:** ADR-0106 (per-leg rows — row 49 flips `done` at this IMPL) · ADR-0044 (ADR §Decision+§Consequences land at the IMPL) · ADR-0045 (sub-split soft gate — escape-valve UNCONSUMED, re-confirmed at PLAN Task 1) · ADR-0080 (strict-reject) · ADR-0064 (`stats_sinks[]` silent-ignore, now a THIRD consumer) · ADR-0060 (no histograms → no `|ms` timers) · next-free **ADR-0266** (ANCHORED at this IMPL).

## Read first (in order)
1. THIS FILE — the cold-start router (phase 49 `stats-sink-dogstatsd` PLAN LANDED; the next action is its IMPL).
2. `docs/envoy-go/phases/49-stats-sink-dogstatsd/PLAN-49.md` — the 9-task TDD spine + the D-DSD-* resolution block + the Global Constraints (counts) + the per-task code/tests/gates + the deliberate-break protocol + the Orientation "what already works" brief (READ THIS FIRST within the PLAN — it has the exact current line numbers for every seam).
3. `docs/envoy-go/phases/49-stats-sink-dogstatsd/SPEC-49.md` — the frozen scope + the §11 D-DSD-* live-probe pins (D-DSD-DELTA, D-DSD-TAGS-ORDER LOAD-BEARING) + the ADR-0266 §Context draft (§13).
4. `docs/envoy-go/STATE.md` — the active-phase header (`phase 49 (stats-sink-dogstatsd) PLAN done`) + the recorded NEXT (`phase 49 IMPL`) + the counts (1200 / 94 / 51 / 38 / ADR-0265).
5. `docs/envoy-go/ROADMAP.md` — row 49 `in-progress` (flips `done` at this IMPL); the Observability family note STAYS OPEN.
6. `docs/envoy-go/phases/48-stats-sink-statsd/PLAN-48.md` (+ the phase-48 IMPL router precedent in STATE.md) — the subagent-driven IMPL controller shape to mirror.
7. `internal/statssink/{flusher,sink,mapping,delta,label,statsd}.go` + `internal/stats/name.go` (`ExtractTags`, esp. the SN4 label-prepend at `:354-357`) + `internal/bootstrap/bootstrap.go` (the current three-arm-to-be dispatch + `parseStatsdSinkConfig` as the shape to mirror) + `cmd/envoy-go/main.go` (the sink-slice + flusher build) + `test/helpers/statsdrecv/statsdrecv.go` (the `ingest` parser to revise) + `test/fixtures/0092-stats-sink-statsd/driver/driver.go` (the `0093` clone template, INCLUDING its local `hostGatewayIP` helper) + `test/differential/harness.go` (`HostGatewayIP`, NOT imported by the driver) — the EXACT seams the PLAN cites.
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `reference_periodic_sink_differential_two_receivers`, `reference_delta_sink_differential_stability_barrier`, `reference_differential_grpc_receiver_driver_owned`, `reference_strict_reject_sibling_typeurl_gap`, `reference_docker_probe_bridge_network`, `reference_host_gateway_ip_docker_desktop` [NOW UPDATED with the import-cycle/local-duplicate correction], `reference_membership_total_vs_healthy_gauge`, `reference_dogstatsd_tag_order_unsorted`, `reference_line_parser_extension_delimiter_reuse`, plus the differential-discipline references above).
