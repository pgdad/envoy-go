=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-CHECKED 2026-07-12 (at the phase-59 PLAN, run MECHANICALLY) — it does NOT fire. Do NOT create `stop`.** The phase-59 PLAN landed (docs-only; row 59 STAYS `in-progress`); the state after it:
- (1) prints `NOT DONE: row 59` — row 59 (`tracing-custom-tags-literal`) STAYS `in-progress` (a row flips `done` only at its IMPL six-gate per ADR-0106). This ALONE blocks `stop`. (UNCHANGED from the phase-59 SPEC/BRAINSTORM state — a PLAN is docs-only and does not flip the row.)
- (2) prints EXACTLY ONE live "candidates:" sentence — the Observability family's `remaining deferred (not-yet-chartered) candidates: OTLP-metrics stats sink + tracing custom_tags/spawn_upstream_span/http_service/force-trace.` (UNCHANGED at the PLAN; `custom_tags` NARROWS to its non-literal types only at the phase-59 IMPL). ⇒ Observability STAYS OPEN. (One live match, `reference_sentinel_deferred_sentence_live_vs_historical`.)
- (3) prints `NEVER OPENED: HTTP/3`, `gRPC`, `xDS`, `Runtime`, `WASM` ⇒ FIVE families never opened; Operational-tooling also STAYS OPEN.

All three checks print ⇒ the sentinel does NOT fire.

**STATUS AS OF 2026-07-12: phase 59 (`tracing-custom-tags-literal`) is CHARTERED, BRAINSTORMED, SPEC'd, and PLANNED — the PLAN landed (docs-only; row 59 STAYS `in-progress`, lifecycle-state 2 → 3). The next stage is the phase-59 IMPL.** The PLAN is a MECHANICAL 8-task TDD spine over SPEC §10 (no empirical question remains — all ten D-CT-* were disposed at the SPEC). Every `file:line` was RE-DERIVED against master tip `36f012cf` this PLAN session (`feedback_brief_citations_not_evidence` — the SPEC's citations were a starting point, verified; incl. the `CustomTag_Literal_`/`CustomTag_RequestHeader` oneof-wrapper naming asymmetry in `custom_tag.pb.go`, and the existing `config_test.go:319-324` typeless `custom_tags` row that STAYS green under the new code). `PROGRESS.md` is scaffolded. ADR-0045: a SINGLE FLAT ROW (8 tasks, escape-valve UNCONSUMED).

**Current counts (verified at the phase-59 PLAN, on the frozen HEAD; UNCHANGED — a PLAN is docs-only):** fixtures **103** (tail `0101-stats-sink-graphite`) · fuzzers **54** · DECISIONS tail **ADR-0276** (next-free **ADR-0277**) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** · new Go packages **0** · new go.mod modules **0**. RE-VERIFY against the actual master tip at session start (`git fetch` first).

**Locating the phase-59 PLAN stage squash** (a router cannot name its own sha — identify by SUBJECT, never by position):
```sh
git log --oneline --grep='^phase 59 (tracing-custom-tags-literal) PLAN' -1
```

**THE NEXT ACTION: the phase-59 IMPL (lifecycle-state 3 → done; row 59 flips `done` at the IMPL six-gate per ADR-0106).**

==============================================

## What THIS session does (the phase-59 IMPL)

Perform the IMPL for phase 59 — SUBAGENT-DRIVEN (`feedback_execution_style`, `superpowers:subagent-driven-development`), in a FRESH worktree off master (`.worktrees/phase-59-impl`, branch `phase-59-tracing-custom-tags-literal-impl`, per `feedback_git_worktrees`). Execute `docs/envoy-go/phases/59-tracing-custom-tags-literal/PLAN.md` TASK-BY-TASK: each task is a fresh subagent that runs the TDD red→green (+ the `-count=1` liveness break where load-bearing) and commits LOCALLY only (`feedback_subagent_autocommit_claudemd`, `feedback_subagents_no_push`); the controller verifies each commit, cleans any leak files, re-verifies the branch (`feedback_subagent_worktree_detach`), pins worktree-relative paths (`feedback_subagent_worktree_path_targeting`), squashes at stage-close onto master, re-runs the six-gate + the full 104-dir differential on the FROZEN HEAD, and PUSHES (only the controller pushes).

**The 8-task TDD spine (from PLAN.md — RE-READ it; the D-CT-* are all disposed):**
1. **`config.go`** — `tracingv3` import + `CustomTags []KV` field (`:24-32`) + a `parseCustomTags` helper (6 ADR-0080-distinct arms: literal-accept + empty-tag/empty-literal-value/typeless PGV-parity + request_header/environment/metadata DEPARTURE) replacing the wholesale reject (`:82-83`) + the provider-switch restructure (`:108-115`, `parseOTel`/`parseZipkin` UNTOUCHED); accept-literal (OTel + Zipkin) + 6 reject sub-tests; `-count=1` break per arm.
2. **`span.go`** — a `customTags []KV` param on `BuildServerSpan` (`:64`) + an `upsertAttr` helper (last-write-wins, the reference OVERRIDE) + threading `f.tracingConfig.CustomTags` at both `accesslog_emit.go` sites (`:55`/`:116`, SPEC task 4 FOLDED) + all 7 test call-site updates (span_test ×6 `:60,151,178,193,207,283`, zipkin_test ×1 `:88` → pass `nil`); append + upsert-override tests; `-count=1` break on the override.
3. **Zipkin encoder unit test** — a literal tag surfaces in the `tags` map; node_id/zone stay dropped; `-count=1` break.
4. **New OTLP fixture `0102-tracing-custom-tags-literal`** — clone `0087` (do NOT mutate `0087`/`0088`, `reference_differential_fixture_dispatch_constraint`); NON-colliding literal tag; `Errorf`-per-property cross-side-by-KEY assertion (`reference_fatalf_makes_assertions_unreachable`); `BackendCount` ≥ 1 (`reference_differential_backendcount_min_one`); register with the runner; `-count=1` differential break confirming WHICH assertion fires (`reference_differential_break_protocol_count1`, `reference_deliberate_break_wrong_assertion`). Fixtures **103 → 104**.
5. **`FuzzHCMConfigParse` seed** — one custom_tags seed via `mkHCM` (`fuzz_test.go:25`); reconcile `^func Fuzz` == documented == **54** before AND after (`reference_fuzzer_count_docs_drift`); fuzzers STAY 54 (a seed never adds a fuzzer).
6. **BEHAVIOR_CONTRACT edits** — `:686` (strict-reject roster: remove `custom_tags` from the wholesale list + add the literal-consumed clause) + `:739` (Zipkin deferred bullet: narrow `custom_tags` to the non-literal types).
7. **Verify** — six-gate (gofmt / golangci-lint / go vet / build / `go mod tidy -diff` clean / full-package `-race` on `internal/tracing` + `internal/filter/hcm`, `reference_detrand_race_catches_protojson_value_substring` / `reference_full_suite_race_after_background_mutator`) + the full **104-dir** differential (byte-stable except `0102`).
8. **ADR-0277 body** (§Decision/§Consequences on the SPEC §13 §Context per ADR-0044) + **STATE** + **ROADMAP** (row 59 → `done` per ADR-0106; the LIVE deferred sentence NARROWS `custom_tags` → the non-literal types — re-run the check-(2) grep, EXACTLY ONE live "candidates:" match, `reference_sentinel_deferred_sentence_live_vs_historical`) + **PROGRESS close** + **router roll** (to the NEXT router decision — a new BRAINSTORM; the roller self-picks the smallest defensible candidate per the 2026-07-12 standing directive; the sentinel does NOT fire).

## Honor throughout (the discipline, unchanged)
- **Lifecycle = one stage per session.** This session: the phase-59 IMPL ONLY. After it lands: the next BRAINSTORM (roller self-picks the subject).
- **Process anchors:** ADR-0044 (ADR §Decision/§Consequences land at the IMPL) · ADR-0045 (split gate ~15 tasks; UNCONSUMED here) · ADR-0080 (distinct per-arm substrings — all six here) · ADR-0106 (a row flips `done` at its IMPL six-gate). Next-free ADR is **ADR-0277**.
- **Subagent-driven execution** (`feedback_execution_style`, `feedback_subagent_autocommit_claudemd`, `feedback_subagents_no_push`): subagents commit LOCALLY only; the controller squashes at stage-close and pushes. Watch for HEAD-detach on deliberate breaks (`feedback_subagent_worktree_detach`) and stale-path targeting (`feedback_subagent_worktree_path_targeting`).
- **`next-prompt.txt` IS TRACKED** — edit it inside the stage worktree, fold into the stage commit; locate commits by SUBJECT (`git log --grep`), never by position (`reference_next_prompt_tracked_despite_gitignore`).
- **Deferred maintenance (fold into the next `udp.go`/statssink-driver-touching row — carried from the phase-57 final review; phases 58 AND 59 touch NEITHER so these STAY OPEN):** F-1 the 0101 driver's counter tag snapshot records the EXPECTED map; F-2 `udp.go` `udpWriter` doc comment names two sinks (three embed it now); F-3 BEHAVIOR_CONTRACT 0101 note says "`differential.HostGatewayIP` helper" where the driver uses its LOCAL duplicate. (A tracing row does NOT touch `udp.go`; these ride until the OTLP-metrics or another statsd/graphite/dog_statsd sink row reopens.)

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-59 IMPL is next; the subject is PICKED — tracing `custom_tags` literal; all D-CT-* disposed at the SPEC; the 8-task spine is decomposed in PLAN.md).
2. `docs/envoy-go/phases/59-tracing-custom-tags-literal/PLAN.md` — the 8-task TDD spine (per-task files/interfaces/steps + full code), the RE-DERIVED edit-site roster, the proto oneof-wrapper footgun box, the existing-`custom_tags`-row NOTE, and the self-review.
3. `docs/envoy-go/phases/59-tracing-custom-tags-literal/PROGRESS.md` — the task checklist + the baseline block to fill at Task 1 + the liveness-break log.
4. `docs/envoy-go/phases/59-tracing-custom-tags-literal/SPEC.md` — the disposed D-CT-* (§1), the change shape (§3), the reject roster (§6), the fixture decision (§8), the test plan (§10), the SPEC-time probe evidence (§11), the RE-DERIVED edit-site roster (§12), and the ADR-0277 §Context draft (§13).
5. `docs/envoy-go/ROADMAP.md` — row 59 `in-progress`; the Observability family prose; the LIVE deferred sentence (still ONE live match — `custom_tags` narrows at the IMPL).
6. `docs/envoy-go/STATE.md` — the active-phase header (phase 59 PLAN done; NEXT = the phase-59 IMPL).
7. `docs/envoy-go/DECISIONS.md` — the ADR-0276 tail (next-free **ADR-0277**); ADR-0260 (the tracing engine/exporter seams) for the span-attribute substrate.
8. `internal/tracing/{config.go,span.go,zipkin.go}` + `internal/filter/hcm/{accesslog_emit.go,config.go}` — the parse home (`NewConfig`), the span-attribute seam (`Span.Attrs`/`BuildServerSpan`), the Zipkin encoder, and the two `BuildServerSpan` call sites + the `f.tracingConfig`/`f.exporter` wiring.
9. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the tracing section (`:686` strict-reject roster + `:739` Zipkin deferred bullet the IMPL edits).
10. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the `reference_*` traps (esp. the tracing references + the differential-fixture/break-protocol/fuzzer-count/fatalf traps).
