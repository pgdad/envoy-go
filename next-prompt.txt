START a fresh session for envoy-go (router last updated 2026-06-25; the **phase 44.1 (`grpc-access-log` core) PLAN is DONE + APPROVED**). HEAD is the LATEST `next-prompt.txt:` docs-only tip-stamp commit; the **phase-44.1 PLAN squash** (subject `phase 44.1 (grpc-access-log core) PLAN: …` — it landed `docs/envoy-go/phases/44-grpc-access-log/PLAN-44.1.md` + the STATE.md roll + this next-prompt.txt roll) is the stable anchor sitting directly BENEATH the run of `next-prompt.txt:` commits at the tip; re-read the shas with `git log --oneline -6`, do NOT assume — the master tip is the LATEST `next-prompt.txt:` commit (a roll or a tip-stamp fix), NOT the PLAN squash itself, and any number of docs-only `next-prompt.txt:` commits may stack ATOP the PLAN squash (which stays the stable anchor). BENEATH the PLAN squash is the phase-44.1 SPEC squash `a7a24d0c` (`phase 44.1 (grpc-access-log) SPEC: …`), and BENEATH that the phase-44 BRAINSTORM squash `81afdd59` then the 43.2b IMPL squash `676e17cf`. **The Observability family is OPEN** (phase 44 is its FIRST row; the family STAYS OPEN — 44.2 buffering / 44.3 header-capture / OTLP / tracing / stats sinks / tap remain future rows). **The next action is the 44.1 (core) IMPL** — invoke `superpowers:subagent-driven-development` (the project's IMPL stage), execute the ~12-task TDD spine in `PLAN-44.1.md` (a fresh implementer subagent per task, two-stage review between tasks), then land the completion bundle (ADR-0255 + BEHAVIOR_CONTRACT + STATE/ROADMAP). **Git hygiene:** all phase work runs in a FRESH git worktree off master (`feedback_git_worktrees`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the controller squashes + pushes at stage-close (`feedback_push_to_origin`); each code task runs `gofmt -l` + `golangci-lint` on touched pkgs (`feedback_pertask_gofmt_lint`); the controller verifies `git -C <main> status` clean + the worktree branch unchanged after each task (`feedback_subagent_worktree_detach` / `feedback_subagent_worktree_path_targeting` — the PLAN doc was first written to the MAIN checkout by mistake and relocated; PIN the worktree-relative path in every dispatch).

First confirm the state via `git log --oneline -6` (expect HEAD to be the LATEST `next-prompt.txt:` docs-only commit, with the phase-44.1 PLAN squash sitting directly BENEATH the run of `next-prompt.txt:` commits, then the phase-44.1 SPEC squash `a7a24d0c` + the phase-44 BRAINSTORM `81afdd59` + the 43.2b squash `676e17cf` beneath; re-read the shas, do NOT assume). Counts at the 44.1 PLAN (UNCHANGED, docs-only): stat surface **1187** (H2 cluster; non-H2 **1183**) / fixtures **82** (tail `0080-h2-goaway-rotation`; NOTE: count fixtures with `ls -d test/fixtures/[0-9][0-9][0-9][0-9]* | wc -l` — `grep -cE '^[0-9]{4}-'` UNDERCOUNTS by 2, skipping `0007a`/`0007b`) / fuzzers **43** / BackendKind tail **38** (`H2GoawayResponder`) / DECISIONS tail **ADR-0254**, next-free **ADR-0255**.

## NEXT STEP — the 44.1 (core) IMPL (`superpowers:subagent-driven-development`, the ~12-task spine in PLAN-44.1.md)

The PLAN (`docs/envoy-go/phases/44-grpc-access-log/PLAN-44.1.md`) is the executable spec: a ~12-task TDD spine, each task = failing test → minimal impl → green → commit. Execute it SUBAGENT-DRIVEN (`feedback_execution_style`), a fresh implementer per task. The §12 D-ALS-* PLAN questions are RESOLVED in the plan — honor them:

- **D-ALS-CONFIG-DEFER** → `buffer_*` / `additional_*_headers` / `filter` are PARSE-ACCEPT-but-INERT at 44.1; KEEP PARALLEL SLICES (`Bootstrap.ALSConfigs` distinct from `AccessLogConfigs`).
- **D-ALS-RECEIVER-WIRING** → a NEW driver-owned `test/helpers/accessloggrpc` receiver (the `0021` ext_authz precedent — driver allocates the port, `NewAtAddr("0.0.0.0:port")`, bakes it into BOTH YAMLs, holds the `*Server` for assertions). **NO NEW BackendKind — BackendKind STAYS 38** (this REVISES the SPEC's anticipated 39); the `0081` data-plane backend REUSES `HTTPFixedBody=4`. (If a driver-owned receiver proves infeasible at IMPL — it should not, per `0021` — fall back to a BackendKind 39 and update the counts; surface any such deviation.)
- **D-ALS-NODE** → source `identifier.node` from `bs.Proto.GetNode()` (id + cluster; nil-safe); UNasserted.
- **D-ALS-SPLIT-FINAL** → ships as ONE leg (~365 prod LoC, at the ADR-0045 soft gate).
- **D-ALS-FUZZER** → LAND the `FuzzParseHttpGrpcAccessLogConfig` parse fuzzer (fuzzers 43 → 44); re-verify `grep -rc '^func Fuzz' --include='*.go' . | awk -F: '{s+=$2} END{print s}'` == 44 at the completion task (`reference_fuzzer_count_docs_drift`).

Anticipated at the 44.1 IMPL: stat **1189** (+2 — `access_logs.grpc_access_log.{logs_written,logs_dropped}`) / fixtures **83** (`0081-grpc-access-log`) / fuzzers **44** / **BackendKind 38 (UNCHANGED)** / DECISIONS **ADR-0255**.

## Honor throughout (the discipline)

- **Lifecycle = one stage per session.** This session is the IMPL (the FINAL lifecycle stage of leg 44.1) — execute, six-gate, squash + push at stage-close, land the completion bundle. `feedback_git_worktrees` (fresh worktree off master) · `feedback_subagents_no_push` · `feedback_push_to_origin` · `feedback_execution_style` · `feedback_pertask_gofmt_lint` · `feedback_subagent_worktree_detach` / `feedback_subagent_worktree_path_targeting` (worktree hygiene + path-pinning — the controller verifies `git -C <main> status` clean after each task; PIN worktree-relative paths) · `reference_full_suite_race_after_background_mutator` (the sink's writer goroutine is a background mutator — after T6 lands AND at the completion six-gate, re-run the FULL `internal/accesslog` package `-race`, NOT a `-run` subset).
- **Process anchors:** ADR-0106 (row 44 flips `done` PER-LEG at this IMPL; the Observability family STAYS OPEN) · ADR-0080 (strict-reject — `tcp_logs`/non-V3/`google_grpc`/empty-cluster) · ADR-0044 (the ADR-0255 §Decision/§Consequences land at IMPL; §Context drafted in SPEC §13) · ADR-0045 (the split gate, re-checked at PLAN — one leg) · ADR-0041 (gRPC ALS LIFTED from the silent-ignore set) · ADR-0016 (the proto blank-import registry — blank-import the grpc config proto in `bootstrap.go`) · ADR-0067 (the access-log 10-operator `Record` shape) · ADR-0158 (the `grpcclient.Dialer`/`AuthClient` typed-wrapper layer — the `ALSClient` precedent).
- **Differential discipline (the `0081` fixture):** `reference_docker_probe_bridge_network` (the driver-owned receiver reachable from BOTH the subject host [`127.0.0.1`] AND the reference container [`host.docker.internal`]; verify decode ran — entries actually accumulated from BOTH sides) · `reference_wire_format_both_sides_see_same_bytes` (assert the `HTTPAccessLogEntry` 7-field PAYLOAD subset aggregated per-entry, NOT stream/message framing — AMEND-ALS-3) · `reference_concurrency_differential_release_barrier` (poll-to-converge until N entries arrive, NEVER a `time.Sleep`) · `reference_differential_run_selector` (`-run 'TestDifferential/0081'`, NOT bare `0081`) · `reference_differential_break_protocol_count1` (`-count=1` on every deliberate-break AND every `-race` run) · `reference_differential_fullsuite_startup_flake` (a `subject ready: EOF` is a transient startup race — isolate-re-run) · `reference_dynamic_stat_name_charset_guard` (NOT needed at 44.1 — the `access_logs.grpc_access_log.*` names are STATIC per AMEND-ALS-1).
- **The query-less-path constraint (AMEND-ALS-2):** the `0081` request MUST use a query-less path — envoy-go's `Record.Path` is path-only (`accesslog_emit.go:25`) while the reference's `request.path` carries the query string; a query-bearing request would diverge cross-side. A documented faithful constraint, not a bug.

## Read first (in order)

1. THIS FILE — the cold-start router (phase 44.1 PLAN is DONE + APPROVED; the next action is the 44.1 core IMPL).
2. `docs/envoy-go/phases/44-grpc-access-log/PLAN-44.1.md` — the executable ~12-task TDD spine (the §12 D-ALS-* resolutions + the per-task code sketches + the exact proto type/field references verified at PLAN against `go-control-plane/envoy v1.32.4`).
3. `docs/envoy-go/phases/44-grpc-access-log/SPEC-44.1.md` — the 44.1 SPEC (the §3 design + the §11 D-ALS-* empirical pins + the §1.1 AMEND-ALS-1..4 + the §13 ADR-0255 §Context draft).
4. `docs/envoy-go/STATE.md` — the active-phase header (`phase 44.1 (grpc-access-log) PLAN done`) + the count figures.
5. `docs/envoy-go/ROADMAP.md` — row 44 (`grpc-access-log`, in-progress) + the `### Observability family` heading (FAMILY OPEN at phase 44).
6. `docs/envoy-go/BOOTSTRAP_PROMPT.md` — the lifecycle invariants (§4.1: STATE.md is the source of truth for "what next"; a session updates it before exiting).
7. `docs/envoy-go/DECISIONS.md` — the tail **ADR-0254** (the 43.2b as-built) + next-free **ADR-0255** (the 44.1 IMPL lands its §Decision/§Consequences; §Context is drafted in SPEC §13).
8. The existing seams the IMPL builds on (verified at PLAN; re-confirm line numbers before editing): `internal/accesslog/accesslog.go` (`Sink` `:18` + `Record` `:29`), `internal/accesslog/writer.go` (the `AsyncFileSink` `:26` / `Submit` `:73` / `Close` `:88` / `run` `:101` pattern + `defaultChannelCapacity` `:13`), `internal/accesslog/stats.go` (`RegisterDroppedCounter` `:14`), `internal/filter/hcm/accesslog_emit.go` (the H1/H2 emit hooks `:18`/`:43`, `Record.Path = r.URL.Path` `:25`), `internal/bootstrap/bootstrap.go` (`parseOneAccessLog` `:265`, the silent-ignore `:271`, the file TypeURL `:145`, `AccessLogConfig` `:152`, the blank-imports `:39`/`:40`, the existing `accesslogv3`=`config/accesslog/v3` import `:8` ⇒ use a distinct alias for the grpc config proto), `internal/grpcclient/grpcclient.go` (the `Dialer` `:85`/`:105`/`:112`–`118` + `AuthClient` `:157`/`:178`/`:231`), `cmd/envoy-go/main.go` (the sink-build loop `:106`–`:119`/`:225`/`:232`), `test/helpers/extauthzgrpc/extauthzgrpc.go` (the in-process gRPC-server helper precedent), `test/fixtures/0021-http-ext-authz-grpc/` (the driver-owned-gRPC-server fixture precedent), `test/differential/fixture/fixture.go` (the `BackendKind` enum tail `H2GoawayResponder=38` `:606`; `HTTPFixedBody=4` `:156`).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. the worktree-hygiene, differential, and the `reference_wire_format_both_sides_see_same_bytes` / `reference_docker_probe_bridge_network` / `reference_full_suite_race_after_background_mutator` refs).
