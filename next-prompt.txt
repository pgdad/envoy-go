START a fresh cold-start session for envoy-go — **phase 30 (`pin-refresh-envoy-contrib`) is DONE + CLOSED**: the project's FIRST reference-image pin-refresh since ADR-0008 LANDED at ZERO production LoC. The SHA-pinned differential reference image is now **`envoyproxy/envoy:contrib-v1.37.2`** @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227, superseding ADR-0008 — the contrib variant of the SAME upstream version, a behavioral superset). All 6 PLAN tasks (T1→T6) ran green: the 54-fixture differential suite re-baselined byte-identical-PASS against the contrib pin (ZERO divergence), conformance re-ran green (h2spec 53/53, proxy-wasm 10/10), ADR-0227 §Decision/§Consequences body + the two BEHAVIOR_CONTRACT current-pin lines landed, the six-gate is GREEN LIVE on the contrib pin, ROADMAP row 30 flipped `→ done`. The next lifecycle step is the **phase-31 `kafka_broker` BRAINSTORM** — a new §9 Network-filters family row needs its OWN brainstorm (NOT a SPEC).

The phase-30 IMPL landed at the **30-IMPL squash `2d24f34`**; the docs-only SHA-fill follow-up `12937ec` is the live tip (confirm via `git log --oneline -1`). Substantive predecessor on master: the phase-30 PLAN squash `3420ed0` (the docs-only SHA-fill follow-up `e3994f1` trails it); before that the phase-30 SPEC squash `3a0207b`; before that the phase-30 BRAINSTORM squash `e2d566a`; before that the phase-29.3 IMPL squash `d671263` (phase 29 CLOSED). origin/master in sync after push.

## What phase 31 IS (the kafka_broker §9 family row)

Phase 31 is the next §9 Network-filters family row: **`kafka_broker`** (`envoy.filters.network.kafka_broker`), SELECTED at the phase-30 BRAINSTORM as the next §9 subject. Phase 30 (now CLOSED) was its standalone infra PREREQUISITE — the contrib reference image is now the differential pin, so the three kafka_broker contrib blockers identified at the phase-30 BRAINSTORM are now partly cleared:
- **the reference image** — RESOLVED by phase 30 (`kafka_broker` is a contrib-only extension absent from the standard image; the contrib image now booted by the differential harness includes it).
- **the go.mod dep** — phase 31 adds `github.com/envoyproxy/go-control-plane/contrib` v1.32.4 (confirmed resolvable) WITH its first consumer (an unused module dep cannot survive `go mod tidy` — so the dep + the first kafka_broker code land together).
- **the canonical v3 proto** — `envoy.extensions.filters.network.kafka_broker.v3.KafkaBroker` lives in that `/contrib` go module.

Phase 31 is a fresh §9 family row → it needs a BRAINSTORM (subject confirmation, the cross-side differential strategy, the stats-at-default-config envelope, the ADR-0045 split-gate sizing). Do NOT skip to a SPEC.

## What this session does — the phase-31 kafka_broker BRAINSTORM

Per SKILL_ROUTING (a new family row → `superpowers:brainstorming`), run the brainstorming skill: explore the kafka_broker subject, the Kafka wire-protocol decode scope (request/response API keys, the broker-side rewrite envelope), the cross-side differential vehicle against the contrib reference Envoy, the stats roster at default config, the BackendKind needed, the fuzzer, and the ADR-0045 split decision — then register ROADMAP row 31 and author the phase-31 BRAINSTORM.md. Work subagent-driven per `feedback_execution_style`.

## Read first (in order)

1. THIS FILE — the phase-31 BRAINSTORM cold-start.
2. `docs/envoy-go/phases/30-pin-refresh-envoy-contrib/SPEC.md` — **§1.3** (not-a-family-row landing — phase 30's forward-pointer to the kafka_broker row) + **§13** (counts UNCHANGED by phase 30).
3. `docs/envoy-go/phases/30-pin-refresh-envoy-contrib/BRAINSTORM.md` — **§2.1** (the three kafka_broker blockers — why phase 30 had to come first) + **§4** (the phase-31 forward-pointer for kafka_broker, non-binding — its own brainstorm is THIS session).
4. `docs/envoy-go/ENVOY_TARGET.md` — the current pin (now `envoyproxy/envoy:contrib-v1.37.2` @ `7edd5b0f…`, ADR-0227 — flipped at the phase-30 IMPL; `parseEnvoyTarget`/`StartReferenceProxy` boot this image for every reference-booting fixture) + doctrine D-3.7.
5. `docs/envoy-go/STATE.md` — active-phase `phase 30 IMPL done`; lifecycle-state phase 30 CLOSED; next-skill `superpowers:brainstorming` for phase-31 kafka_broker.
6. `docs/envoy-go/ROADMAP.md` — the §9 Network-filters family heading + roster: rows 26/27/28/29 DONE, row 30 (pin-refresh) DONE; the candidate roster after kafka_broker is **{redis, thrift}** (kafka_broker now UNBLOCKED, landing at phase 31).
7. `docs/envoy-go/DECISIONS.md` — ADR-0227 (the contrib pin, in full) + ADR-0008 (superseded) + ADR-0045 (split-gate) + ADR-0106 (§9 family) + ADR-0052 (atomic six-gate).
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — execution-style + worktree + push + subagents-no-push + the wire-format-both-sides + the dynamic-stat-name-charset-guard + the close-direction-framework-gap + the docker-probe-bridge-network + the network-filter-typeurl-extensions lessons (all recur for kafka_broker).

## Counts at master tip (phase 30 IMPL done — UNCHANGED by phase 30)

- stat surface **360**; differential fixtures **54** (tail `0052-mongo-fault-delay`); fuzzers **39**; BackendKind tail **30** (`TCPMongoResponder`); DECISIONS.md tail **ADR-0227** (next-free **ADR-0228**).
- conformance h2spec 53/53 + proxy-wasm 10/10.
- reference pin: `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227, the contrib variant of upstream v1.37.2; flipped at the phase-30 IMPL).
- Toolchain: Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); go-control-plane `/envoy` v1.32.4 (ADR-0008); `/contrib` v1.32.4 confirmed resolvable (the phase-31 dep — added at phase 31 with its first consumer, NOT before).
- §9 Network-filters family: rows 26/27/28/29 DONE + row 30 (pin-refresh) DONE; candidates {redis, thrift} remain after kafka_broker; **kafka_broker SELECTED**, landing at phase 31 (this brainstorm).
