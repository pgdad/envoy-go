START a fresh cold-start session for envoy-go — **the phase-39.2 (`health-check-tcp-grpc`, the TCP + gRPC active-HC checker codecs) IMPL is DONE, LANDED on master (squash commit `5ddc588`), and PUSHED to `origin/master`; with it the phase-39 (`upstream-health-check`) row is COMPLETE — both legs of the by-codec split LANDED (39.1 HTTP keystone + 39.2 TCP+gRPC), the active-HC checker roster (HTTP+TCP+gRPC) is CLOSED, and ROADMAP row 39 has flipped `in-progress → done` (the row-36 ring-hash by-plane-split precedent). The next lifecycle step is a NEW phase BRAINSTORM** (`superpowers:brainstorming`) — pick the next subject (see the menu below) — OR any new subject the user names. Confirm the tip via `git log --oneline -5` (the substantive master tip is the phase-39.2 IMPL squash **`5ddc588`**; the prior substantive tips are the phase-39.1 IMPL squash `6714575` and the phase-38.2 IMPL squash `14053d1`). As-built counts at the 39.2 IMPL: stat surface **1132** / fixtures **70** / fuzzers **42** / BackendKind tail **34** (`GRPCHealthResponder`) / DECISIONS tail **ADR-0244**, next-free **ADR-0245**.

## What just landed (phase 39.2 IMPL — the durable record is `docs/envoy-go/phases/39.2-health-check-tcp-grpc/{PROGRESS,SPEC,PLAN,README}.md`; read those, not this summary)

ADR-0244 — the multi-codec `prober` dispatch. A `prober` interface (`probe(addr) (ok, networkFailure bool)`) generalizes the 39.1 HTTP-only active-HC checker into a kind-tagged dispatch over the UNCHANGED 39.1 substrate (the `clusterHealth`/`hostHealth` registry, `recordResult` transitions, `StartHealthChecks`/`Drain`, the +7 stats, the panic threshold, the six-LB health view — all REUSED VERBATIM; only the per-checker field shape changed cfg→prober). Three codecs: `httpProber` (the 39.1 `probeHTTP` body, byte-identical — 0066 stays green); `tcpProber` (connect-only `net.Dialer.Dial`+close; refused ⇒ `failure`+`network_failure`); `grpcProber` (a unary `grpc.health.v1.Health/Check` over insecure H2; SERVING ⇒ healthy; a returned non-SERVING ⇒ `failure` only [network_failure STAYS 0 — the application-vs-network discriminator]; transport err ⇒ both++; `service_name` supported, authority/initial_metadata silent-ignored). `parseHealthChecks` → a per-arm http/tcp/grpc switch returning `([]checkerSpec, hasGrpc, error)`; the 39.1 `only http_health_check is supported` reject LIFTED; `buildCluster` rejects `grpc_health_check` on a non-H2 cluster (reuses `extractH2Mode`); the tcp `send`/`receive` payload is a config-build DEPARTURE-reject. Differential: +1 `GRPCHealthResponder` BackendKind (34 — an in-process h2c gRPC-SERVING + 200 responder) + two cross-side fixtures `0067-health-check-tcp` (reuses `HTTPEcho`) + `0068-health-check-grpc` (H2 upstream cluster, H1 downstream listener, per-side exact `cx_active` pin [ref 2 pooled / subj 0 idle-teardown, 20/20 zero-flap]). Both fixtures prove liveness via 2 deliberate breaks (converge-fail + warmup-fail) + 20/20 flake. Full **70-dir differential GREEN** + the six-gate GREEN; ZERO new go.mod modules, ZERO new stats, ZERO new fuzzers.

## The next-subject menu (the user picks at the BRAINSTORM; or a new subject)

The phase-39 active-HC checker roster is COMPLETE. The Upstream-robustness FAMILY (opened at phase 39) STAYS OPEN — **4 candidates remain**: {**outlier detection** (passive health — composes with the 39.1 host health-state dimension), **circuit breakers**, **retries + hedging**, **per-protocol connection pooling**}. ALSO now-UNBLOCKED by the 39.1 health-state keystone: the **3 health-gated Load-balancing candidates** {locality-weighted LB, priority load balancing, panic thresholds}. The natural follow-on is **outlier detection** (it reuses the host health-state primitive the most directly — passive vs active health), but the user decides. Do NOT presume — open the BRAINSTORM with the menu.

## Honor throughout (the lifecycle discipline)

- **Lifecycle = one stage per session:** BRAINSTORM→SPEC→PLAN→IMPL, each its own session, each landed+pushed at stage-close. `superpowers:brainstorming` for the next subject. `feedback_execution_style` (subagent-driven IMPL) · `feedback_git_worktrees` · `feedback_subagents_no_push` (task subagents commit LOCAL-only) · `feedback_push_to_origin` (controller squash-merges to master + pushes at stage-close).
- **SSOT + ADR discipline:** `docs/envoy-go/STATE.md` is the single source of truth for "what next" (read it FIRST). ADR §Context at SPEC, bodies at IMPL in-place (ADR-0044). The six-gate at IMPL (ADR-0052). Byte-stable rejects (ADR-0080). Flat family rows, a consumed split phase's row flips `done` (the row-36/row-39 precedent), NO parent rollup row (ADR-0106). The ADR-0045 split-gate (>~25 tasks OR >~1500 LoC) re-checked at each SPEC/PLAN.
- **Differential discipline:** `reference_differential_run_selector` (`-run 'TestDifferential/NNNN'`) · `reference_differential_break_protocol_count1` (`-count=1`) · `reference_docker_probe_bridge_network` (reference is a container; backends via `host.docker.internal`; verify `upstream_rq_total>0`) · `reference_health_check_propagation_warmup` (gate on convergence + warmup + delta counters) · `reference_fixture_workload_constant_desync` (single-source constants).

## Read first (in order)

1. THIS FILE — the post-phase-39.2-IMPL cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 39.2 (health-check-tcp-grpc) IMPL done`; the count footer (fixtures 70 / fuzzers 42 / stat surface 1132 / BackendKind 34 / DECISIONS tail ADR-0244, next-free ADR-0245); the lifecycle routing (next-skill = the next subject BRAINSTORM).
3. `docs/envoy-go/ROADMAP.md` — row 39 now `done` (both legs LANDED); the Upstream-robustness family open candidates; the prior rows for house style.
4. `docs/envoy-go/phases/39.2-health-check-tcp-grpc/` — the 39.2 PROGRESS/SPEC/PLAN/README (the multi-codec prober dispatch as-built) + `docs/envoy-go/phases/39-upstream-health-check/` (the 39.1 substrate 39.2 reused).
5. `internal/cluster/{health.go,manager.go}` — the as-built `prober` dispatch + the three probers + `parseHealthChecks` + the H2 reject (the substrate the next Upstream-robustness phase composes with) + `docs/envoy-go/DECISIONS.md` (ADR-0242/0243/0244 + the process ADRs) + `docs/envoy-go/BEHAVIOR_CONTRACT.md` (the active-health-checks subsection).
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the lifecycle-discipline set above.
