START a fresh cold-start session for envoy-go — **phase 32 (`network-filter-redis-proxy`) is DONE; the next lifecycle step is the phase-33 {thrift} network-filter BRAINSTORM** (`superpowers:brainstorming`). The phase-32.2 IMPL LANDED (`docs/envoy-go/phases/32.2-network-filter-redis-commands-and-stats/PROGRESS.md`; all 12 TDD tasks; six gates GREEN) and the parent-row-32 ROLLUP closed the SIXTH §9 Network-filters-family row (parent row 32 + sub-row 32.2 `in-progress → done` ATOMICALLY per ADR-0106(d)). `redis_proxy` is the project's 10th network-filter built-in + its FIRST terminal routing proxy, on the ADR-0230 upstream-pool seam. The §9 Network-filters family stays OPEN with **{thrift} the SOLE remaining candidate** — after it the family closes. This is a fresh cold-start for the phase-33 {thrift} BRAINSTORM; confirm the live master tip via `git log --oneline -1`.

The phase-33 BRAINSTORM is DESIGN-ONLY (`superpowers:brainstorming`) — no code. It settles the {thrift} envelope through a multi-question dialogue and registers the phase-33 ROADMAP row + anchors the next ADR's §Context. Per `feedback_execution_style` the downstream SPEC→PLAN→IMPL run subagent-driven; per `feedback_git_worktrees` work in a worktree.

## What this session does — the phase-33 {thrift} BRAINSTORM

Run `superpowers:brainstorming` scoped to the **phase-33 {thrift} network-filter** (`thrift_proxy`, `envoy.filters.network.thrift_proxy`, proto `envoy.extensions.filters.network.thrift_proxy.v3.ThriftProxy` — a `/contrib` extension, like kafka_broker). Thrift is the LAST §9 Network-filters-family candidate; after phase 33 phase-done the §9 family closes. The dialogue settles:

  - **Q0 — subject confirm.** `thrift_proxy` is the sole remaining §9 candidate. Confirm it (proto type URL via `proto.MessageName` per `reference_network_filter_typeurl_extensions`; the `extensions.` segment intrinsic).
  - **Q1 — the envelope.** Thrift is a terminal routing proxy (the redis-32 precedent — there is NO passive-observe MVP for a terminal proxy). The design surface: the transport×protocol matrix (`framed`/`unframed`/`header` transports × `binary`/`compact`/`twitter` protocols), method-name routing to an upstream cluster, the per-method/global stat roster (`<stat_prefix>.*`), and the nested thrift-filter sub-chain (router + rate-limit + header-to-metadata — anticipated deferred). What's in the MVP, what's parse-accept-behavior-deferred.
  - **Q-split.** Single unsplit row vs a 2-way pre-split (the redis-32 seam-first / kafka-31 single-row precedents); ADR-0045 split-gate at BRAINSTORM time.
  - **Q-seam.** REUSE-vs-EXTEND the 32.1 upstream-pool seam (ADR-0230 — redis-scoped but generalizable; thrift is its anticipated SECOND consumer; the YAGNI generalization gets consumed here if the multiplexing model differs).

Thrift is a `/contrib` extension → it CONSUMES the existing `github.com/envoyproxy/go-control-plane/contrib` v1.32.4 dep (added at phase 31 with kafka_broker) + the `envoyproxy/envoy:contrib-v1.37.2` reference image (phase 30, ADR-0227) — both already in place, **ZERO new infra** (contrast: kafka_broker needed phase-30 to land first). The empirical pins (the transport×protocol matrix, the stat roster, the method-routing model, the seam reuse) defer to the phase-33 SPEC, executed live against the contrib reference Envoy per `reference_docker_probe_bridge_network`.

## The phase-32 as-built to carry forward (the redis_proxy precedent the thrift BRAINSTORM mines)

- **redis_proxy DONE** — the 10th network-filter built-in + the FIRST terminal routing proxy. `internal/filter/network/redisproxy/` (config + RESP codec + the 180-command table + the `classify` dispatch + the stat roster + the `Handle` pump). The shape thrift's terminal-proxy package mirrors.
- **The upstream-pool seam (ADR-0230)** — `internal/filter/network/upstreampool.go` (`UpstreamDialFunc`/`UpstreamConn`/`NewUpstreamConn`/`Send`/`Reader`/`Close`; lazy dial; one-conn-per-downstream; FIFO/positional correlation; the dial-closure decoupling — `internal/filter/network` has NO `internal/cluster` import). Redis-scoped at 32.1; **thrift is the anticipated second consumer** (reuse if the model matches, extend if thrift needs multiplexing / a request id).
- **The `redis.` SINGLE-label-hoist prom arm** (`internal/stats/name.go` — `envoy_redis_<leaf>{envoy_redis_prefix="<sp>"}`, the mongo `.rbac.` shape). The three prom-arm shapes thrift picks from: kafka INLINE (empty labels) / mongo MULTI-label / redis SINGLE-label-hoist.
- **The differential surface** — `0055-redis-roundtrip` (cross-side; downstream-RESP byte-equivalence + `StatsAsserter` on `/stats` + `/stats/prometheus`) + `0056-redis-boot-reject`; the `TCPRedisResponder` BackendKind 32; `FuzzRESPDecode` (the 41st). The terminal-proxy differential template thrift extends (a new `TCPThriftResponder` BackendKind + a thrift fuzzer anticipated).
- **The §9 close-direction-zero-touch posture (AMEND-R9)** — redis_proxy added no `cx_destroy_*` counters. Thrift's close-direction needs assessing at BRAINSTORM (the `reference_close_direction_framework_gap` boundary).

## Counts at master tip (phase 32 phase-done — the phase-33 BRAINSTORM session opens from here)

- stat surface **1091** (BEHAVIOR_CONTRACT doc count; +545 at the 32.2 IMPL: 2 splitter + 3 REDIS_CLUSTER_STATS + 540 per-command [180×3]; latency histograms + `*_fault` counters NOT counted per ADR-0060/faults).
- differential fixtures **58** (canonical `ls -d test/fixtures/[0-9]* | wc -l`; tail `0056-redis-boot-reject`; `0055` extended in place at 32.2).
- fuzzers **41** (canonical `grep -rh "^func Fuzz" $(find ./internal -name fuzz_test.go) | wc -l`; tail `FuzzRESPDecode`).
- BackendKind tail **32** (`TCPRedisResponder`).
- DECISIONS.md tail **ADR-0230** (ACCEPTED; next-free **ADR-0231** — ADR-0229 now ACCEPTED, its 32.2 half landed in-place; NO new number consumed at phase 32. The phase-33 BRAINSTORM/SPEC anticipates its first new ADR at ADR-0231).
- reference pin: `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227 — the contrib image thrift's differential boots).
- Toolchain: Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); go-control-plane `/envoy` v1.32.4 (ADR-0008) + `/contrib` v1.32.4 (phase-31 dep — thrift_proxy is a `/contrib` subpackage → ZERO new go.mod dep anticipated).
- conformance h2spec 53/53 + proxy-wasm 10/10 (image-independent; UNAFFECTED by phase 32).
- §9 Network-filters family: rows 26/27/28/29/30/31/32 DONE; **{thrift} the SOLE remaining candidate** (the LAST — after phase 33 the family closes).

## Read first (in order)

1. THIS FILE — the phase-33 {thrift} BRAINSTORM cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 32.2 IMPL done`; lifecycle-state → the next phase ({thrift} remains); next-skill `superpowers:brainstorming`; the counts (1091 stats / 58 fixtures / 41 fuzzers / BackendKind tail 32 / DECISIONS tail ADR-0230, next-free ADR-0231).
3. `docs/envoy-go/ROADMAP.md` — the §9 Network-filters family (rows 26-32 DONE; the candidate-roster paragraph — {thrift} the sole open candidate, a terminal-proxy surface reusing the 32.1 seam).
4. `docs/envoy-go/DECISIONS.md` — **ADR-0230** (the upstream-pool seam — ACCEPTED; thrift's anticipated SECOND consumer) + **ADR-0229** (the redis_proxy filter + parent umbrella — ACCEPTED; the terminal-routing-proxy precedent) + **ADR-0228** (kafka_broker — the `/contrib` precedent) + ADR-0227 (the contrib pin) + ADR-0106(d) (the §9 family flat-row / parent-pre-split discipline) + ADR-0045 (the split-gate) + ADR-0044/0052/0060.
5. The phase-32 as-built: `docs/envoy-go/phases/32-network-filter-redis-proxy/` (BRAINSTORM + parent SPEC + README) + `32.1-network-filter-redis-upstream-pool-seam-and-codec/` (SPEC + PLAN + PROGRESS — the seam + codec) + `32.2-network-filter-redis-commands-and-stats/` (SPEC + PLAN + PROGRESS — the command set + roster + the `redis.` prom arm + the command matrix).
6. The phase-31 kafka_broker record: `docs/envoy-go/phases/31-network-filter-kafka-broker/` (the `/contrib v1.32.4` dep + the contrib reference image — the `/contrib` precedent thrift reuses) + the phase-30 pin-refresh (`docs/envoy-go/phases/30-pin-refresh-envoy-contrib/` — ADR-0227, `reference_envoy_contrib_image_tagging`).
7. `internal/filter/network/upstreampool.go` (the seam thrift reuses/extends) + `internal/filter/network/redisproxy/` (the terminal-proxy package shape) + `internal/filter/network/builtins/builtins.go` (the built-in registration) + `internal/bootstrap/bootstrap.go` (the blank-import TypeURL anchor).
8. `internal/stats/name.go` (the kafka INLINE / mongo MULTI-label / redis SINGLE-label-hoist prom-arm contrast — thrift picks one) + `test/differential/fixture/fixture.go` (the `StatsAsserter` + the BackendKind enum, tail 32).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — especially `reference_wire_format_both_sides_see_same_bytes` (the wire is shared — adopt upstream framing verbatim; recurs for thrift), `reference_differential_reference_parses_full_message` (cross-side fixtures need fully-valid frames + per-side `*.failure` pins; recurs for thrift), `reference_close_direction_framework_gap` (the §9 close-direction boundary), `reference_dynamic_stat_name_charset_guard` (wire-derived stat segments → IsValidName guard), `reference_network_filter_typeurl_extensions`, `reference_envoy_contrib_image_tagging`, `reference_docker_probe_bridge_network`, `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_subagents_no_push`, `feedback_pertask_gofmt_lint`.

## Honor throughout

- `feedback_execution_style` (the downstream SPEC→PLAN→IMPL run subagent-driven) · `feedback_git_worktrees` (work in a worktree) · `feedback_subagents_no_push` (subagents commit LOCAL-ONLY; the controller squash-merges + pushes at stage-close) · `feedback_push_to_origin` (after a clean landing on master with tests green, push without asking) · `feedback_pertask_gofmt_lint` (each task runs `gofmt -l` + `golangci-lint run` on touched packages).
- ADR-0106(d) (the §9 family flat-row / parent-pre-split discipline — phase 33 registers a row, parent-pre-split if a split is chosen) · ADR-0045 (the split-gate — re-check at BRAINSTORM) · ADR-0230 (the upstream-pool seam — reuse-or-extend at the second consumer) · ADR-0044 (§Context-at-SPEC / §Decision+§Consequences-at-IMPL) · ADR-0052 (the atomic-landing six-gate) · ADR-0060 (histograms deferred) · ADR-0227 (the contrib pin) · ADR-0008 (the go-control-plane dep — thrift_proxy is a `/contrib` subpackage, anticipated ZERO new dep).
- The BRAINSTORM is DESIGN-ONLY — it produces `docs/envoy-go/phases/33-network-filter-thrift-proxy/BRAINSTORM.md` (the 12-§ template mirroring the redis-32 / kafka-31 BRAINSTORMs), registers the ROADMAP phase-33 row (`in-progress`, parent-pre-split sub-rows if split), anchors the next ADR's §Context (anticipated ADR-0231), and routes to the phase-33 SPEC. spec/plan-document-reviewer gates apply at the SPEC/PLAN, not the BRAINSTORM.
