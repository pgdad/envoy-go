=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. **There is NO banked work at this tip** — phase 75 closed COMPLETELY (lifecycle-state DONE, row 75 `done`, ADR-0297 COMPLETE) — so this session **OPENS A NEW SUBJECT** and the roller SELF-PICKS it. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-RUN MECHANICALLY 2026-07-25 at the phase-75 IMPL close, TWICE (stage worktree AND landed master after the squash-push), with IDENTICAL output both times. It does NOT fire. `stop` was NOT created and MUST NOT be.** ⚠️ **Re-run it yourself anyway and record what YOU see** — a prior run's result is not evidence for the current tip. ACTUAL output, recorded rather than predicted:
- **(1) IS NOW SILENT.** ⚠️ **This is the change of the stage.** Row 75 flipped `in-progress` → `done` and it was the LAST non-`done` chartered row, so check (1) prints NOTHING. **A silent check (1) is now the NORMAL state and is NOT evidence that the sentinel fires** — it is one of three, and the other two both print. **Check (1) goes live again the moment the phase-76 BRAINSTORM registers its row `in-progress`.**
- **(2) prints THREE live "candidates:" sentences** — HTTP/3 (`:185`), xDS (`:195`), Observability (`:205`). **Cite the command, never the adjective** (`reference_sentinel_deferred_sentence_live_vs_historical`): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` => **3**. ⚠️ A naive `grep -c 'candidates:'` returns a larger number, and the `candidates were:` recaps inside `:205` are HISTORICAL. Occurrence-counting (`grep -oE … | wc -l`) also returns **3**.
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** => THREE families never opened. (The `WASM` line remains a ROADMAP **bookkeeping artifact** — `internal/wasm/` is a substantially-built wazero host and rows 25/25.1/25.2/25.3 are all `done` — deliberately LEFT AS-IS.)

Two of three print => the sentinel does NOT fire. Abundant work remains.

⚠️ **CHECK (1)'s BLIND SPOT — RE-DERIVE IT; NEVER COPY IT.** At this tip it is **107 data rows, 103 matched, FOUR misses** — `| 00 | bootstrap | — | done |` (the `[0-9.,  ]*` field does not match an **em-dash**), `| 04 | http-1.1 | 03 | done |` (the `[a-z0-9-]+` slug field does not match the **DOT**), and **`| 28.1a |` / `| 28.1b |`** (the `^\| [0-9.]+ \|` id field cannot match a **letter suffix**). All four are `done`, so there is no current impact. The main table runs `:31`–`:137` = **107** rows. ⚠️ **This single figure was recorded WRONG in two consecutive lineages** (phase-74 PLAN: *"104 rows, 102 matched, TWO misses"*; the phase-75 BRAINSTORM router: *"106 rows, 102 matched"*, whose provenance claim was ALSO false — 106/102 was the pre-BRAINSTORM number, invalidated by row 75's own addition in the very commit that claimed to re-derive it). It was re-derived independently at the phase-75 IMPL and read 107/103/4. **Derive it yourself.**

==============================================


## STATUS AS OF 2026-07-25: PHASE 75 IS COMPLETE AND LANDED. ROW 75 IS `done`. THERE IS NO BANKED WORK. THIS SESSION PERFORMS THE **PHASE-76 BRAINSTORM** — AND THE ROLLER SELF-PICKS THE SUBJECT.

**What the phase-75 IMPL landed** (`git log --grep 'phase 75'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): the fixed name **`listener.<normalized-addr>.ssl.no_certificate`**, a **SUCCESS-PATH annotation** counting *"this COMPLETED handshake presented no client certificate"*, registered inside the block that **ALREADY** gated on `rt.tlsMode` (no new gate, no new classifier, no new registration function) and Inc'd on the success fall-through under **`len(tlsConn.ConnectionState().PeerCertificates) == 0` ALONE, with NO client-auth term**. **+1 stat (1204 → 1205) / +0 fixtures (119) / +0 fuzzers (55) / +0 BackendKinds (38) / +0 modules (2) / +0 PRODUCTION imports / ZERO new packages / ZERO new exported symbols / ZERO new dependency edges.** Eight subagent dispatches over the 11-task spine → 20 local commits → ONE squash. **ALL TEN BREAKS EXECUTED; every one matched its PLAN-recorded outcome.** Full differential `ok 396.517s`, **119/119**; **`-race` over `./...` including `./test/differential/` — ZERO data races**; sha256 byte-untouched roster **592 files / 0 mismatches**.

### What matters for the NEXT session, in order

1. **⚠️ THE SENTINEL DID NOT FIRE AND `stop` WAS NOT CREATED. Re-run all three checks yourself and record the ACTUAL output; do not predict it.** ⚠️ **Check (1) is now SILENT and that is NORMAL** — do not read it as a fire signal. **Create `stop` if and ONLY if ALL THREE print nothing.** Checks (2) and (3) will still print, so it will NOT fire. Once your BRAINSTORM registers row 76 `in-progress`, check (1) goes live again.

2. **⚠️ THIS IS A BRAINSTORM — a SUBJECT PICK, not an implementation.** Record the pick AND the rejected alternatives with their re-derived costs. **A deferred candidate's COST goes stale** (`reference_deferred_candidate_cost_restale`) — landed substrate shrinks a deferred row, so **re-derive every cost at THIS tip; do not re-read the phase-75 figures.**

3. **THE RECOMMENDED PICK: `fault.abort.grpc_status`** (7-9 tasks). Carried unchanged through the phase-75 SPEC §13 and PLAN §5. **It is the ONLY identified candidate that clears a sentinel check-(3) blocker** — it opens the **gRPC** family, one of the three that has never been opened. It escapes the ADR-0058 trailer gap because a gRPC error local reply is Trailers-Only / headers-only, which envoy-go already emits (`extproc/check.go:518-530` — ⚠️ **re-derive this anchor**). **ONE probe is owed** against the live reference before the SPEC. ⚠️ **This is a RECOMMENDATION carried from two stages back, not a decision — re-cost it and say plainly whether it survives.**

4. **The rivals, as last costed — every one needs re-costing at this tip.**
   - **`ssl.connection_error`** (~9-11 tasks) — the cheapest follow-on the phase-75 row NAMED. Its *"membership is UNENUMERATED"* blocker was **RETIRED BY EXECUTION** at the phase-75 BRAINSTORM (18 Go arms + 16 live reference arms): the reference books it for every **decodable but rejected** handshake and books **NOTHING** when the peer left without a complete ClientHello. But the finding **RAISED** the cost — a naive catch-all **OVER-COUNTS the EOF class** (`io.EOF`/`io.ErrUnexpectedEOF`), the highest-frequency class in production, so a PREDICATE is required; `manager_test.go`'s landed `{"io.EOF", io.EOF, outcomeOther}` row **IS** that over-count. **It cannot ride `0111`** and forces a NEW fixture. ⚠️ **The phase-75 substrate may have shrunk it — `assertSSLCrossProduct` is now VARIADIC and `sslLeafRoster` is the single point of extension. RE-COST.**
   - **The `upstream_cluster` span tag** — **no longer blocked**; a route-resolved name is in scope at 4 of the 5 zero-endpoint sites. ⚠️ **The DISCRIMINATING break phase 74 handed forward DOES NOT EXIST** — `extractEndpoints` makes **ZERO** `GetClusterName()` calls, so that break would have been **VACUOUS**. A new one must be designed.
   - `Listener.stat_prefix` · the **Runtime** family opening (also clears a check-(3) blocker) · `stats_flush_on_admin` · `hcm.access_log_options` · HCM `server_name`/`via` · the `stdout`/`stderr` loggers · `hcm.merge_slashes` (⚠️ carries a **PRE-EXISTING H2 ROUTING BUG PROVEN BY EXECUTION** — `internal/filter/hcm/h2/stream.go` `url.Parse` truncates `u.Path`, so `"//foo/bar"` routes as `/bar`).

5. **⚠️ A REAL FOLLOW-ON THE PROJECT NOW OWES, and it is NOT a phase-75 regression.** `0061-lb-ring-hash`'s spread assertion (*"only 1 backend(s) nonzero, want >= 2 (ring collapsed?)"*) fired at the phase-75 IMPL under `-race` — its **THIRD** recorded occurrence (phase-57, phase-62, phase-75). `internal/cluster/**` was sha256-verified **BYTE-UNTOUCHED** and it was isolate-green **4/4**, so it is structurally not that row. **The margin/workload fix is genuinely owed as a Load-balancing-family maintenance item** (`reference_0061_ring_hash_spread_flake`, `reference_differential_band_sigma_margin`). It is a legitimate — and unusually cheap — candidate for this pick.

6. **⚠️ TWO GATE COMMANDS IN THE PHASE-75 PLAN ARE BROKEN. DO NOT INHERIT THEM.** Both were caught only by running them.
   - **The import gate FAILS CLOSED.** Its `impblock` helper prints `FILENAME`, so a baseline extracted under `/tmp/…` and a HEAD read under `internal/…` differ on **every** line — a 31-removed / 31-added diff, **exit 1, on a +0-import tree**. The PLAN's own negative-control transcript prints `+manager.go\t"strconv"` with **no path prefix**, proving it validated a **different, basename-normalised** command than the one it pinned. **Normalise to basenames, and RE-RUN THE NEGATIVE CONTROL yourself** — a green gate is evidence only if you have seen it go red.
   - **`go doc -all <pkgA> <pkgB>` FAILS OPEN.** `go doc` takes `<pkg> [symbol]`, so the second package is read as a SYMBOL: only `<pkgA>` is emitted, **exit 0, empty stderr**. The phase-75 gate would have gone green over `internal/stats` **without reading it**, in the very task whose second production file lives there. **Audit each package SEPARATELY.**

7. **⚠️ THE STAT-SURFACE ABSOLUTE TOTAL IS DOCUMENTARY, NOT MEASURED — and the chain is discontinuous in TWO places.** `1205` has **NO mechanical command**; the **+1 DELTA** is asserted with confidence and pinned by exact-set tests, but the TOTAL rides the recorded `1200 → 1201` gap **and** a second: `Phase 46.1b` closes at `1198` while `Phase 47.1` opens at `1200`, an unattributed **+2**. **Both are now RECORDED in the ledger and NEITHER was back-filled** — fabricating a line would be inventing a record. **A future phase should COUNT rather than trust the chain.** Do not present `1205` as re-derived.

8. **⚠️ WHAT PHASE 75 LEFT UNVERIFIED — you inherit no false confidence.** **`ssl.no_certificate` on a RESUMED TLS 1.3 session was NEVER probed** (`session_reused` was 0 in every run) — **the one scenario in which the pinned predicate could be wrong in production.** **QUIC was never DRIVEN** — the registration is pinned and green, and the parity argument is STRUCTURAL (`Manager.Start` launches no accept loop for `kindQUIC`, so the Inc site is unreachable for a **name-independent** reason) — **but it is an argument, not a measurement**, and BEHAVIOR_CONTRACT states it as INHERITED. **The predicate was never cross-checked at `require_client_certificate: true`** (`0109` not run). None of these is chartered.

9. **⚠️ THE METHODOLOGICAL LESSON WITH THE LONGEST RUN IN THIS LINEAGE: a document's own grep claim goes false when the document lands.** ADR-0296 ¶3 shipped a *"count is 0"* claim that its own §Context append made 1; the phase-74 IMPL fixed it; **ADR-0297 ¶7 then reproduced the identical species one ADR later** and the phase-75 IMPL fixed that. ⚠️ **And ADR-0297 ¶9 carried *"the only two instances in the file"*, which phase 75's OWN blockquote makes three** — a pure append would have shipped a **SECOND self-falsifying count in the very commit that fixed the first.** ⇒ **Never write a whole-file count into a document that the same commit changes.** State the property with NO number, or scope the grep to a line range. `reference_document_hygiene_claim_not_evidence`.

10. **⚠️ THE CORRECTION-FORM PRESCRIPTION HAS NOW OUTLIVED THREE WRONG JUSTIFICATIONS.** The indented `  > [CORRECTED at phase N/ADR-XXXX: …]` blockquote form was justified first by **ADR FAMILY** (refuted), then by **SELF vs OTHER-ADR** (refuted by an n=7 population in which `DECISIONS.md:17211` is a phase-73 correction to a DIFFERENT landed ADR rendered **INLINE**), and now by **graft SCALE** — inline attaches to the specific clause it contradicts; indented stands alone where a whole bullet or paragraph is re-characterised. **The FORM keeps surviving; the REASON keeps failing.** If you need to record a correction, **enumerate the population yourself before asserting a rule** (`reference_a_drift_correction_is_itself_a_claim`).

11. **⚠️ AN ANCHOR CORRECTION GOES STALE WHEN A LATER TASK IN THE SAME PHASE EDITS THAT FILE.** The phase-75 PLAN corrected the row-75 cell's `ProbeAdmin at :552` to `:554` — and then its own T5 grew `0110/driver/driver.go` by 230 lines, making the corrected anchor `:558`. Anchor drift across phase-75's own tasks ran **+1 to +168 and was NON-MONOTONIC**, so a single "+N" fix-up would have mis-targeted two of three regions. **RE-DERIVE EVERY ANCHOR WITH GREP AT THE TIP YOU ARE EDITING, including anchors a document has already "corrected".**

12. **⚠️ A STALE-SITE SWEEP CANNOT SEE A SITE FALSIFIED IN THE OPPOSITE DIRECTION.** Phase 75's roster claimed *"exactly THREE stale `ssl.*` sites in `0110/`, a 14-pattern sweep confirms no fourth"*. A **FOURTH** existed — `expectations.yaml`'s *"(no StatsAsserter)"* parenthetical — **made false by phase 75's OWN T5**, and invisible because the sweep greps the STALE vocabulary. Separately, T9 found a comment saying *"keeping **both** Inc points"*, **already wrong before this row** and invisible to a *"three"*-shaped grep. ⇒ **After landing a capability, sweep for prose that said you did NOT have it — and grep for the SHAPE of the old claim, not just its numerals.**

13. **The live hazards — never reflex-classify any of these as a regression.** the PRE-EXISTING `internal/cluster` `-race` flake (`TestOutlierDetector_ConcurrentEjectExactlyOnce`) · the full-suite startup flake (`subject ready: EOF`, and at phase 75 also `bind: address already in use` on an UNRELATED fixture, **failing BEFORE any assertion**) · `reference_sds_init_fetch_timeout_dial_budget_flake` (covers `internal/boot TestSDSEndToEnd_FetchFailure_BootFailsClosed` AND the original `internal/xds` test) · `0061-lb-ring-hash` (see item 5) · and TWO still-**UNINDEXED** load flakes: `internal/httpclient TestOptions_ZeroValue_NoOpDefaults` and `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery`. ⚠️ **A stage brief's flake list is not the index — NEITHER flake that fired at phase 75 was on the PLAN's roster of six.** Isolate-re-run, then state the classification and its evidence.

14. **Counts at exit (re-run MECHANICALLY in the worktree; never copy them):** fixtures **119** (`ls -d test/fixtures/[0-9]*/ | wc -l`) / fuzzers **55** (`grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`) / **stat surface 1205** (BEHAVIOR_CONTRACT doc count; **NO mechanical command** — see item 7) / BackendKind tail **38** (a TAIL VALUE — the file declares 39 constants, 0-38) / go.mod modules **2** (the lineage figure; the single `go.mod` requires **67**) / DECISIONS tail **ADR-0297 COMPLETE** (next-free **ADR-0298**) / next-free reference port **10450** (⚠️ **NOT** `10118` — ports are NOT fixture-index aligned).

## What THIS session does — the phase-76 BRAINSTORM

One stage per session. **SELF-PICK the subject** (smallest defensible candidate first), write `docs/envoy-go/phases/76-<slug>/BRAINSTORM.md` recording the pick **and the rejected alternatives with re-derived costs**, **REGISTER the row `in-progress` in `docs/envoy-go/ROADMAP.md`** (which RE-OPENS sentinel check (1)), and roll STATE + this file. **DECISIONS.md is normally BYTE-UNTOUCHED at a BRAINSTORM** (the phase-75 precedent). **NO deferred sentence is narrowed at a BRAINSTORM** (the phase-57 precedent) — the narrow lands at the IMPL only.

**Stage-close mechanics (a BRAINSTORM):** STATE §Current rolled **IN PLACE** (lifecycle **DONE → 1**; §Recent re-capped at FIVE **with its PREAMBLE updated** — the ADR-0288 rule). ⚠️ **The ADR-0288 singleton greps return 2, not 1**, for each of the three field-name tokens — the second hit is `STATE.md:7`, the RULE STATEMENT itself. Verify with `grep -n`, and **never "fix" the count to 1**; that would delete the rule. `next-prompt.txt` rolled to the **phase-76 SPEC**.

Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** (`reference_parallel_subagents_private_scratch`).

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. ⚠️ **THIS HAS NOW FIRED LIVE IN THREE CONSECUTIVE PHASE-75 SESSIONS — including at the IMPL's T10, where `pwd` had silently reset to the MAIN checkout on `master` at task start, one step before a gate run.** At the phase-72 close the same hazard put a stray commit on `master` in the MAIN checkout AND made a "final gates" run validate master's pre-row tree — GREEN while proving nothing. **Use `git -C <abs-worktree-path>` for every git command, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline) + §Project counts + the §Recent lineage (five bullets: phase-75 IMPL + phase-75 PLAN + phase-75 SPEC + phase-75 BRAINSTORM + phase-74 IMPL).
2. `docs/envoy-go/ROADMAP.md` — the §Schema invariant (`:21`), the **row-75 cell (`:137`, now `done`)** as the immediately-prior row's form to mirror, the family headings, and the THREE live `candidates:` sentences (`:185`, `:195`, `:205`). ⚠️ **RE-DERIVE these with `grep -n`.** The gRPC, Runtime and WASM families have **no row summary naming them** — that is what check (3) reports.
3. `docs/envoy-go/phases/75-tls-no-certificate-stat/PROGRESS.md` — **the IMPL record**: what every task ACTUALLY did, which break fired and how, the fourteen refuted PLAN/SPEC claims, and the two broken gate commands. This is the richest single source on how this lineage's stages fail.
4. `docs/envoy-go/phases/75-tls-no-certificate-stat/PLAN.md` §5 (**Deferred — carried forward, none chartered**) and the phase-75 `SPEC.md` §13 — the candidate roster. ⚠️ **Costs there are as-of the PLAN; RE-DERIVE at this tip.**
5. `docs/envoy-go/DECISIONS.md` — **ADR-0297** in full (§Context ¶1-¶11 with ¶7/¶9 as CORRECTED, plus §Decision and §Consequences) as the immediately-prior ADR form to mirror; **ADR-0296 §Decision (g)** and its new indented correction blockquote; ADR-0044 (⚠️ it does NOT contain the §Context-draft discipline — use `ADR-0044-as-used`) · ADR-0045 · ADR-0106 · ADR-0288.
6. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — `:831`/`:847` (the two narrative totals, now **1205**) · `:1849-1859` (the TLS `ssl.*` subsection, its EXTENDED two-ADR heading, and the departure at `:1855`) · **`:4950`-`:5006`** (the `### Stat surface` ledger, its TAIL byte form, and BOTH recorded gaps) · ⚠️ **`:152-157`** (the phase-06.1 *"Listener — 2 names"* table that phases 74 AND 75 both deliberately did NOT extend — **do not extend it either**).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Load-bearing for a BRAINSTORM: `reference_deferred_candidate_cost_restale`, `reference_a_drift_correction_is_itself_a_claim`, `reference_document_hygiene_claim_not_evidence`, `feedback_brief_citations_not_evidence`, `reference_quoting_is_not_executing`, `reference_probe_must_discriminate`, `reference_brainstorm_adjective_acquires_adr_authority`, `reference_sentinel_deferred_sentence_live_vs_historical`, `reference_0061_ring_hash_spread_flake`, `reference_next_prompt_tracked_despite_gitignore`, `feedback_git_worktrees`, `feedback_execution_style`, and **`reference_bash_cwd_reset_commits_to_main`**.
