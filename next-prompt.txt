START a fresh cold-start session for envoy-go — **the phase-39 (`upstream-health-check`, active health checks) BRAINSTORM is DONE, LANDED on master, and PUSHED to `origin/master`; the next lifecycle step is the phase-39 SPEC** (`superpowers:writing-plans` scoped to SPEC authoring — the kafka-31..weighted-clusters-38.2 precedent; execute the BRAINSTORM §10 D-HC1..D-HC8 empirical pins IN-SESSION against the contrib reference Envoy + anchor the ADR-0242/0243 §Context drafts + confirm the 39.1/39.2 ADR-0045 split). The substantive master tip is the phase-39 BRAINSTORM-DONE bundle (the charter `docs/envoy-go/phases/39-upstream-health-check/BRAINSTORM.md` + README + STATE + ROADMAP row 39 registered `in-progress` + the Upstream-robustness family-heading note; confirm via `git log --oneline -5`). The prior substantive tip is the phase-38.2 IMPL squash **`14053d1`** (the weighted-cluster routing primitive + `ClusterWeight.metadata_match`; ADR-0241). ALL counts UNCHANGED at this BRAINSTORM: **stat surface 1125 / fixtures 67 / fuzzers 42 / BackendKind 33 / DECISIONS tail ADR-0241 (next-free ADR-0242).**

## What just landed (phase 39 BRAINSTORM — the durable record is `docs/envoy-go/phases/39-upstream-health-check/BRAINSTORM.md` + README; read those, not this summary)

The **FIRST Upstream-robustness-family row** (the first NEW feature family since §9 closed at phase 33). Charters `Cluster.health_checks` (`[]core.config.core.v3.HealthCheck`, proto field 8 — VERIFIED in the core `/envoy v1.32.4` module cache: the `timeout`/`interval`/`unhealthy_threshold`/`healthy_threshold` envelope + the oneof `HealthChecker` ∈ {HTTP, TCP, gRPC} + `common_lb_config.healthy_panic_threshold` [`Percent`, default 50%] + the `HealthStatus` enum) — **active** upstream health checking + the project's FIRST per-host **health-state dimension** + the health-aware LB pick (all six constructs route only to healthy hosts, panic-threshold fallback). The family **keystone**: it UNBLOCKS the open Load-balancing family's 3 health-gated candidates {locality-weighted LB, priority load balancing, panic thresholds} and is the substrate {outlier detection, circuit breakers, retries + hedging, connection pooling} compose with.

**Q-decisions:** (Q0) NEW family = Upstream robustness (over xDS/gRPC/HTTP3 — the foundational reliability layer). (Q0b) first sub-project = active health checks (the keystone; over the cleaner retries/circuit-breakers). (Q1) checker roster = HTTP + TCP + gRPC, **split by codec**: **39.1** = the keystone (host health-state dimension + the cluster-level checker runtime [the project's FIRST background runtime — `Manager.StartHealthChecks(ctx)` post-`Freeze` boot start + `Manager.Drain()` cancel/wait] + the HTTP probe codec + the health-aware LB across all six constructs) + the `0066-health-check-http` differential (~500–800 prod LoC / ~14–20 tasks); **39.2** = TCP + gRPC checkers reusing the 39.1 substrate (the thrift-33 framework-zero-touch reuse; +1 BackendKind [gRPC SERVING responder] + a candidate `FuzzGrpcHealthResponse`). (Q2) health-aware LB = **Approach A: pick-time filter + panic fallback**, with the health view **build-time-injected** (host health is CLUSTER state, NOT a per-request `Pick` widening — the CONTRAST with the ADR-0235 hash-key / ADR-0239 subset-match ctx-carry pick-inputs; the `Pick` signature + exported `Cluster` surface stay BYTE-STABLE; behavior-neutral when all healthy → every existing fixture stays byte-identical; ring_hash/maglev walk-to-next-healthy preserves key stability).

**2 ADRs anticipated:** ADR-0242 (the host health-state dimension + the active-HC runtime — the state+runtime half) + ADR-0243 (the health-aware LB pick — Approach A). The phase-36/38 two-ADR shape; §Context at SPEC, bodies at the 39.1 IMPL per ADR-0044. A NEW `health_checks` parse-reject surface (envoy-go's own byte-stable rejects; D-HC2). Differential `0066-health-check-http` (a NEW poll-to-convergence determinism shape — a cluster with a LIVE + a DEAD unbound host; POLL `/stats` until the dead host is unhealthy on BOTH sides [no fixed sleep], THEN assert 100% of traffic on the live host + cross-side `health_check.*`/`membership_*` parity; NO new BackendKind/fuzzer at 39.1; 3 `-count=1` deliberate breaks).

## What's next — the phase-39 SPEC (`superpowers:writing-plans` scoped to SPEC authoring)

Author `docs/envoy-go/phases/39-upstream-health-check/SPEC.md`. EXECUTE the BRAINSTORM §10 **D-HC1..D-HC8** empirical pins IN-SESSION (parallel-subagent fan-out per the 25–38 SPEC precedent) against the contrib reference Envoy (`envoyproxy/envoy:contrib-v1.37.2`, ADR-0227) per `reference_docker_probe_bridge_network` (bridge network + STRICT_DNS backend hostnames + verify traffic ran via `downstream_cx_rx_bytes_total > 0`):

- **D-HC1** the initial host health state with active HC configured + the HEALTHY↔UNHEALTHY transition + default thresholds (confirm the MVP can model host state as a single bit; DEGRADED/DRAINING deferred).
- **D-HC2** the `health_checks` config surface + the PARSE/build reject set + wording (byte-stable; PARITY vs envoy-go-strict; boot-reject vs post-boot; the deferred TCP/gRPC 39.1 disposition); `expected_statuses` default; clean `go mod tidy`.
- **D-HC3** which timing fields `0066` needs (base `interval`/`timeout` only?) + the reference's first-check scheduling (the poll deadline).
- **D-HC4** the stat-surface delta (~+6 `health_check.*` + `membership_healthy` — exact names/count/types/increment semantics) + the runtime start/drain + the candidate `/clusters` per-host health readout.
- **D-HC5** the `healthy_panic_threshold` default (50%) + panic semantics + the `0066` host-count ratio so it asserts FILTERING not panic (e.g. 2 live + 1 dead → 66% > 50%).
- **D-HC6** the per-LB-construct skip semantics (round_robin/least_request/random/ring_hash/maglev walk-to-next-healthy) + behavior-neutrality when all healthy + the byte-stable `Cluster` surface (OPTION-C).
- **D-HC7** the `0066` design (host topology, the poll-to-converge predicate + deadline, the 100%-live assertion, the flapping-arm in-or-out decision, the deliberate-breaks, the ≥20-run flake protocol, `-run 'TestDifferential/0066'`) + the FINAL ADR-0045 39.1/39.2 split (anticipated 39.1 ~500–800 LoC / ~14–20 tasks; if it trips the gate, a further 39.1 split [state+runtime then health-aware-LB]).
- **D-HC8** confirm 39.1 adds ZERO new go.mod module; pin the 39.2 gRPC dep posture (direct `grpc_health_v1` vs hand-roll the message).

Anchor the **ADR-0242 + ADR-0243 §Context drafts** in SPEC §13 (DECISIONS tail STAYS ADR-0241; bodies at the 39.1 IMPL per ADR-0044). Confirm the 39.1/39.2 ADR-0045 split disposition. Row 39 STAYS `in-progress` at the SPEC commit (it flips per-leg at each IMPL six-gate — NO parent rollup per ADR-0106).

## Read first (in order)

1. THIS FILE — the post-phase-39-BRAINSTORM cold-start (next: the phase-39 SPEC).
2. `docs/envoy-go/STATE.md` — active-phase `phase 39 (upstream-health-check) BRAINSTORM done`; the counts (fixtures 67 / fuzzers 42 / stat surface 1125 / BackendKind 33 / DECISIONS tail ADR-0241, next-free ADR-0242); the lifecycle routing (→ the phase-39 SPEC).
3. `docs/envoy-go/phases/39-upstream-health-check/BRAINSTORM.md` + README.md — **the phase-39 charter** (the Q0/Q0b/Q1/Q2 decisions; the §2.1 proto verification; the §1.7/§2.3/§2.4 architecture; the §1.4/§2.2 split; the §2.6 reject surface; the §2.5/§6 differential; the §7 ADRs; the §10 D-HC1..D-HC8 pins).
4. `docs/envoy-go/ROADMAP.md` — row 39 (`in-progress`) + the §Upstream robustness family heading note (4 candidates remain after 39).
5. `docs/envoy-go/DECISIONS.md` — the process ADRs the SPEC honors (ADR-0106/0045/0044/0052/0004/0080/0227) + ADR-0232 (the byte-stable-surface OPTION-C the health-view injection reuses) + ADR-0235/0239 (the per-request pick-inputs the health view CONTRASTS) + ADR-0236/0238 (ring_hash/maglev — the policies that gain walk-to-next-healthy) + ADR-0230 (the prior framework-runtime seam) + ADR-0024 (the per-cluster LB-state scope).
6. `internal/cluster/{cluster.go,manager.go,loadbalancer.go,ringhash.go,maglev.go,leastrequest.go,random.go,subset.go,dial_h2.go}` + `internal/admin/admin.go` (`/stats`) + `cmd/envoy-go/main.go` (the boot sequence the `StartHealthChecks` call slots into).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_execution_style`, `feedback_git_worktrees`, `feedback_subagents_no_push` + `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_path_targeting`/`_detach`, and the differential references (surface them when phase 39 reaches its IMPL).

## Honor throughout

- The lifecycle is BRAINSTORM → SPEC → PLAN → IMPL, each its own session. The next session is the phase-39 **SPEC** (`superpowers:writing-plans` scoped to SPEC authoring — execute the §10 D-HC1..D-HC8 pins live; anchor the ADR-0242/0243 §Context drafts; confirm the 39.1/39.2 split).
- `feedback_git_worktrees` · `feedback_execution_style` (subagent-driven at the IMPL) · `feedback_subagents_no_push` · `feedback_push_to_origin` (the controller squash-merges + pushes at stage-close) · `feedback_pertask_gofmt_lint`.
- ADR-0004 (the empirical-pin discipline — EXECUTE at the SPEC) · ADR-0044 (ADR §Context at SPEC, bodies at IMPL in-place) · ADR-0045 (the split-gate — the FINAL 39.1/39.2 decision at the SPEC) · ADR-0052 (the atomic-landing six-gate) · ADR-0080 (byte-stable parse-reject wording) · ADR-0106 (flat family row — NO parent rollup, sibling rows NOT pre-populated) · ADR-0227 (the contrib reference image `envoyproxy/envoy:contrib-v1.37.2`).
