=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-CHECKED 2026-07-14 (after the phase-64 PLAN landed + row 64 STAYS in-progress, run MECHANICALLY) — it does NOT fire. Do NOT create `stop`.** State:
- (1) prints `NOT DONE: row 64` — row 64 `tracing-max-path-tag-length` STAYS `in-progress` (a row flips `done` only at its IMPL six-gate per ADR-0106; a PLAN is docs-only). This ALONE blocks `stop` (a mid-lifecycle phase is open).
- (2) prints THREE live "candidates:" sentences — HTTP/3, xDS, and Observability ⇒ three families STAY OPEN. The Observability sentence is UNCHANGED by this row (`max_path_tag_length` is a §8-tier candidate, NOT in the live `custom_tags (metadata)` + OTLP-metrics + `spawn_upstream_span`/`http_service`/force-trace sentence).
- (3) prints `NEVER OPENED: gRPC`, `Runtime`, `WASM` ⇒ THREE families never opened. Operational-tooling also STAYS OPEN.

Checks (1), (2) and (3) print ⇒ the sentinel does NOT fire.

**STATUS AS OF 2026-07-14: the phase-64 PLAN LANDED and ROADMAP row 64 STAYS `in-progress`.** The PLAN (`docs/envoy-go/phases/64-tracing-max-path-tag-length/PLAN.md`) + the scaffolded `PROGRESS.md` decompose SPEC §10 into a **9-task** TDD spine (SINGLE FLAT ROW, ADR-0045 escape-valve UNCONSUMED). Every `file:line` was RE-DERIVED against master tip `bb221ec5` (`feedback_brief_citations_not_evidence`) — ALL SPEC §12 citations HOLD. The task spine: **T1** `config.go` — add `TracingConfig.MaxPathTagLength uint32` (`:25-40`) + replace the `:112-114` reject with the resolve arm (default 256 / explicit incl. 0) set post-dispatch alongside `cfg.CustomTags` (`:154`); `config_test.go` remove the reject row (`:336-343`) → `TestNewConfigMaxPathTagLength`. **T2** NEW `internal/tracing/url.go` `BuildHTTPURL(scheme, host, pathAndQuery, maxPathTagLen)` (byte-truncate `pathAndQuery` FIRST then prepend `scheme://host`) + `url_test.go` matrix. **T3** rewire the THREE `accesslog_emit.go` URL-build sites (H1 `:40` / H2 `:93` / H3 `:162`) through `tracing.BuildHTTPURL(..., f.tracingConfig.MaxPathTagLength)` + `span_emit_test.go` H1/H2 truncation tests via the EXISTING `newTracingFilter`/`fakeExporter` seam. **T4** Zipkin encoder unit test. **T5** NEW OTLP fixture `0107-tracing-max-path-tag-length` (clone `0106`, `max_path_tag_length: {value: 16}`, long-ASCII-path GET, cross-side `http.url` VALUE-equality; register `runner_test.go:133`). **T6** `FuzzHCMConfigParse` seed (+`wrapperspb`; fuzzers stay 55). **T7** BEHAVIOR_CONTRACT REJECT→CONSUMED. **T8** verify (six-gate + 109-dir differential). **T9** ADR-0285 §Decision/§Consequences + STATE + ROADMAP (row 64 `done`) + PROGRESS close + router roll. **Two documented TRAPS:** the ZERO-VALUE CAP TRAP (a bare `TracingConfig{}` literal → `MaxPathTagLength==0` = empty-path truncation after T3; the existing bare-literal `span_emit_test.go` filters stay green ONLY because none assert `http.url`; NEW http.url-asserting tests MUST set the cap) and `span_test.go:100` (asserts `http.url` but calls `BuildServerSpan` directly with a pre-built URL ⇒ does NOT flow through `BuildHTTPURL`, STAYS green — do NOT edit). The `0107` fixture leads with `http.url` VALUE-equality and documents a `:path`-portion-equality fallback IF the run shows an authority-encoding divergence (the `0106` "value varies" note). Row 64 STAYS `in-progress`; STATE flipped; router rolled to the IMPL.

==============================================

## What THIS session does (the phase-64 IMPL)

Perform the IMPL stage (lifecycle-state 3 → done) for phase 64 (`tracing-max-path-tag-length`). This is a PRODUCTION-CODE stage: EXECUTE `docs/envoy-go/phases/64-tracing-max-path-tag-length/PLAN.md` task-by-task, SUBAGENT-DRIVEN (`feedback_execution_style` + `superpowers:subagent-driven-development`) in a FRESH worktree `.worktrees/phase-64-impl` off master (branch `phase-64-tracing-max-path-tag-length-impl`, `feedback_git_worktrees`). Each task is a fresh subagent that commits LOCALLY only (`feedback_subagent_autocommit_claudemd` + `feedback_subagents_no_push`); the controller verifies each commit, cleans any leak files, squashes at stage-close, RE-RUNS the six-gate + the full differential on the frozen HEAD, and PUSHES (`feedback_push_to_origin`). At stage-close, flip ROADMAP row 64 → `done` (ADR-0106, the SOLE leg), land ADR-0285 §Decision/§Consequences, close PROGRESS, and ROLL THIS FILE to the roller's next self-pick BRAINSTORM (per the 2026-07-12 standing directive — the sentinel governs termination).

**Execute the PLAN as written — it is a mechanical TDD spine (RE-DERIVED, no empirical question remains). Honor throughout:**
- **Lifecycle = one stage per session.** This session: the IMPL ONLY. After it lands: roll to the roller's next self-pick BRAINSTORM.
- **TDD per task** (`superpowers:test-driven-development`): red → green, with a `-count=1` liveness break where an assertion is load-bearing (`reference_differential_break_protocol_count1`, `reference_deliberate_break_wrong_assertion`, `reference_fatalf_makes_assertions_unreachable`). Confirm WHICH assertion fires. The `0107` differential break touches the ASSERTION's expected value ONLY (`reference_vacuous_break_receiver_normalizes`) and uses the FULL `-run 'TestDifferential/0107-tracing-max-path-tag-length'` selector (`reference_differential_run_selector`).
- **Per-task gates** (`feedback_pertask_gofmt_lint`): `gofmt -l` + `golangci-lint run` on touched pkgs + `go build ./...` + touched-pkg `go test` before each commit.
- **The two PLAN TRAPS** (ZERO-VALUE CAP TRAP; `span_test.go:100` stays green) + the `0107` authority-encoding fallback — the subagents must honor them (they are in the PLAN's Global Constraints).
- **Process anchors:** ADR-0044 (ADR §Decision/§Consequences at this IMPL; §Context drafted at the SPEC) · ADR-0045 (single-flat-row; escape-valve UNCONSUMED) · ADR-0106 (row flips `done` at the IMPL six-gate) · `reference_roadmap_split_phase_row_done` · ADR-0080 (NO new reject this row; the sibling rejects STAY distinct).
- **`next-prompt.txt` IS TRACKED** (`reference_next_prompt_tracked_despite_gitignore`) — edit it inside the stage worktree and fold into the stage squash; locate commits by SUBJECT (`git log --grep`), never by position.
- **Worktree discipline** (`feedback_git_worktrees`, `feedback_subagent_worktree_detach`, `feedback_subagent_worktree_path_targeting`) — pin the canonical worktree root; subagents write worktree-relative paths; the controller verifies the MAIN checkout stays clean; on a deliberate break restore with `git restore` only (no checkout-sha/amend) and re-verify the branch each task.
- **Tracing traps** (`reference_tracing_upstream_cluster_framework_gap` — `http.url` is a POPULATED built-in, the truncation acts on a real value, NOT the 4 EMPTY framework-gap attributes · `reference_tracing_custom_tag_override_builtin` — a custom `http.url` tag still upsert-overrides the truncated built-in).

**ADR reservation ledger (provisional; each finalized at its IMPL in landing order):** ADR-0277 = phase-59 tracing-custom-tags-literal (LANDED) · ADR-0283 = phase-62 tracing-custom-tags-request-header (LANDED) · ADR-0284 = phase-63 tracing-custom-tags-environment (LANDED) · **ADR-0285** = phase-64 tracing-max-path-tag-length (RESERVED; §Context at the SPEC — DONE; §Decision/§Consequences at THIS IMPL) · next-free unreserved after: **ADR-0286**.

**Current counts (VERIFIED at the phase-64 PLAN against master tip `bb221ec5`; a PLAN is DOCS-ONLY ⇒ all UNCHANGED — RE-VERIFY at session start, `git fetch` first):** fixtures **108** (`ls -d test/fixtures/[0-9]*/ | wc -l`; tail `0106-tracing-custom-tags-environment`) · fuzzers **55** · DECISIONS tail **ADR-0284** (next-free **ADR-0285**) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** · go.mod modules **2** (`quic-go` direct + `qpack` indirect). **Anticipated at IMPL-DONE:** fixtures **108 → 109** (`0107`) · fuzzers **55 (+0)** · DECISIONS tail **ADR-0285** (next-free ADR-0286) · stat **1201 (+0)** · packages/modules **+0** · row 64 → `done`.

**Locating the phase-64 PLAN stage squash** (a router cannot name its own sha — identify by SUBJECT, never by position):
```sh
git log --oneline --grep='^phase 64 (tracing-max-path-tag-length) PLAN' -1
```

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-64 IMPL is next; row 64 is `in-progress`).
2. `docs/envoy-go/phases/64-tracing-max-path-tag-length/PLAN.md` — the 9-task TDD spine (File Structure, the RE-DERIVED edit-site roster, the two TRAPS + the `0107` authority-encoding fallback in Global Constraints, Tasks 1–9 with full code).
3. `docs/envoy-go/phases/64-tracing-max-path-tag-length/PROGRESS.md` — the task checklist + the D-MPTL-* dispositions + the baseline block to fill at Task 1.
4. `docs/envoy-go/phases/64-tracing-max-path-tag-length/SPEC.md` — the DISPOSED D-MPTL-* roster (§11 live probes), the change shape (§3), ADR-0285 §Context (§13).
5. `docs/envoy-go/STATE.md` — the active-phase header (phase 64 PLAN done; NEXT = the phase-64 IMPL).
6. `docs/envoy-go/ROADMAP.md` — row 64 (`in-progress`) + the Observability family prose (the live deferred sentence UNCHANGED by this row).
7. `docs/envoy-go/phases/63-tracing-custom-tags-environment/PROGRESS.md` — the IMPL-stage FORMAT precedent (liveness-break log, verify evidence, landed-commit block, router roll).
8. `internal/tracing/config.go` (the reject at `:112-114`, `TracingConfig` `:25-40`, `cfg.CustomTags` `:154`), `internal/filter/hcm/accesslog_emit.go` (URL sites `:40`/`:93`/`:162`), `internal/filter/hcm/span_emit_test.go` (`fakeExporter`/`newTracingFilter`), `internal/tracing/config_test.go` (`TestNewConfigRejectArms` `:281`, the reject row `:336-343`), `internal/tracing/zipkin.go`/`zipkin_test.go`, `internal/filter/hcm/fuzz_test.go` (`:74`), `test/differential/runner_test.go` (`:133`) — RE-VERIFY every edit-site file:line against the master tip at session start.
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the tracing `reference_*` traps + the differential-harness traps (esp. `reference_differential_break_protocol_count1`, `reference_differential_run_selector`, `reference_vacuous_break_receiver_normalizes`, `reference_fatalf_makes_assertions_unreachable`, `reference_fuzzer_count_docs_drift`).
