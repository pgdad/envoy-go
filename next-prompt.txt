=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **PHASE 93 (`h2-local-reply-content-length`) IS OPEN AT LIFECYCLE-STATE 2.** Row 93 reads `in-progress` at `ROADMAP.md:155`, and the phase directory holds `BRAINSTORM.md` (**503 lines**) and `SPEC.md` (**409 lines**) and nothing else. ⇒ **THIS SESSION RUNS THE PHASE-93 PLAN (lifecycle-state 2 -> 3). DO NOT SELF-PICK.**

⚠️⚠️ **THE SENTINEL'S MARGIN IS TWO ONLY BECAUSE THIS ROW IS OPEN.** Check (1) went SILENT at the row-92 flip and was re-armed solely by row 93's insertion. **The moment row 93 flips `done`, check (2) is again the ONLY thing blocking termination.** Do NOT read a two-deep margin as comfort, and do NOT "tidy" a deferred-candidate line — deleting the last one ENDS THE PROJECT. ⚠️ **MEASURE ALL THREE YOURSELF BEFORE DOING ANYTHING ELSE; do not take this file's word for it.**

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE. This form parses FIELDS instead of matching a pattern, and
#     asserts the row DENOMINATOR so a deleted row is itself an error.
#     ⚠️ want IS 125 — row 93 was registered at the phase-93 BRAINSTORM and the
#     SPEC changed NO row.
awk -F'|' -v want=125 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **CHECK (1) PRINTS `NOT DONE: row 93` — ONE line.** That is CORRECT while row 93 is open. Run BOTH doctoring NCs EVERY SESSION anyway, because a check whose output you predicted is indistinguishable from a broken one:
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md.
```
⚠️ **NC-A READS **TWO** LINES** (`NOT DONE: row 62` THEN `NOT DONE: row 93`). ⚠️ **This shape HELD across the phase-93 SPEC because the SPEC changes no row — but it CHANGES THE MOMENT ROW 93 FLIPS. Re-measure, never inherit.**
```sh
# the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```
Also NC the DENOMINATOR (`want=124` on the real file must print BOTH `NOT DONE: row 93` AND `GATE FAIL: examined 125 data rows, expected 124`) — **NC-B is a TWO-line result.**

⚠️ **AND WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -c '-family row'` fails (GNU: `grep: amily row: No such file or directory`; ugrep: `option -f: cannot read amily row`) and the surrounding arithmetic prints `0`, **which reads exactly like "no change."** **Always pass `--` before the pattern**, and the counts are form-dependent: with `--`, occurrences read **95** and LINES read **67**. **`grep -c` counts LINES, NOT OCCURRENCES.** ⚠️ **AND `grep -c` PRINTS `0` *AND* EXITS 1, so `$(cmd || echo 0)` EMITS TWO ZEROS** — capture with `v=$(cmd || true)`.

⚠️ **THE MALFORMED-ROW FIGURE RECONCILES ONLY UNDER AN ESCAPE-AWARE FIELD COUNT.** Malformed rows are ids **57** (`NF=9`) and **69** (`NF=10`). ⚠️ **A NAIVE `awk -F'|'` `NF != 8` FORM READS SEVENTEEN.** **Strip `\|` BEFORE splitting.** The two forms **DISAGREE on row 57** but **AGREE on row 69**, so **any gate must state WHICH FORM it uses.** ⚠️ **COUNT A ROW'S FIELDS BEFORE INSTALLING ANY EDIT** — row 93 reads **NF=8 under BOTH forms**.

⚠️ **AND DO NOT SUBSTITUTE A "REASONABLE EQUIVALENT" FOR CHECK (1)'s AWK.** A measurement agent once tried `NF>=8 && $2 ~ /^ *[0-9]+ *$/`: it printed **92**, not the true denominator. **Use the form above verbatim.**

**SENTINEL RUN MECHANICALLY AT THE PHASE-93 SPEC (`56c4d255`).** ACTUAL output, recorded not predicted: (1) `NOT DONE: row 93` — ONE line at `want=125` · (2) **SIX** at `:203 :209 :215 :225 :231 :239`, **per-line md5 BASELINED, with a doctoring NC proving the comparator discriminates** · (3) SILENT. NC-A **TWO** lines, NC-B **TWO** lines, NC-C fired (`NEVER OPENED: gRPC`, WASM control **2**), NC-D long **5** / short **1** / union **6**.
⇒ **THE SENTINEL DOES NOT FIRE. `stop` WAS EVALUATED AND DELIBERATELY NOT CREATED** (verified absent at the git root and in all six stage worktrees).
⚠️ **RE-CONFIRMED AT THE PUBLISHING COMMIT `975e527e`, AFTER THE MERGE AND PUSH** — per `reference_cost_figure_measured_at_publishing_commit`, a figure measured only at the pre-commit tip is not evidence about the tree a later session actually reads. ALL THREE checks and ALL FOUR NCs were re-run on the pushed tree and every result was BYTE-IDENTICAL to the pre-commit run: (1) `NOT DONE: row 93` · (2) SIX at `:203 :209 :215 :225 :231 :239` · (3) SILENT; NC-A TWO lines, NC-B TWO lines, NC-C fired with WASM control **2**, NC-D **5 / 1**. **That the SPEC stage moved NO row is therefore MEASURED on both sides of its own commit, not assumed.** ⚠️ **This re-confirmation does NOT carry forward — the next stage must run all three checks and all four NCs ITSELF, at ITS OWN tip.**

- ⚠️ **LIVE FIGURES RE-DERIVED AT THIS TIP:** `ROADMAP.md` **243** / **125** data rows / tail row **93** (**BYTE-UNTOUCHED by the SPEC — sha256 identical**) · `DECISIONS.md` **18674** · `^---$` **216** · `^## ADR-` **314** · bare `^## ` **322** (314 + 8 `## Amendment`) · tail **ADR-0315** · next-free **ADR-0316** · `STATE.md` **63** · `STATE_HISTORY.md` **534**, strict **163 (DELTA 0)** / parenthetical **54** / bullet-anchored **217** · phase dirs **134** · fixtures **121** (tail `0119`, **`0120` FREE**) · fuzzers **56 targets / 48 FILES** · `BEHAVIOR_CONTRACT.md` **5966** (**BYTE-UNTOUCHED**) · `go.mod` **67** (18 direct + 49 indirect) · BackendKind tail **38** (39 declared) · `-family row` **95 / 67**.
- ⚠️ **THE STAT SURFACE IS QUOTED AS A DELTA AND NEVER AS AN ABSOLUTE.** THREE different absolutes are live in this tree at one tip. Per `reference_a_drift_correction_is_itself_a_claim`, on a contested count: **NO NUMBER.** ⚠️ **AND THE REFUTED STAT-SURFACE FIGURE MUST NOT BE SPELLED, IN EITHER ITS BARE OR ITS ARROW FORM — the phase-93 SPEC wrote a prohibition that QUOTED the token and thereby FALSIFIED ITSELF on its own grep; it was caught by running the gate on the file.**
- ⚠️ do NOT "fix" the six in check (2) and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases. ⚠️ **Cite ~40 only as "sentence-visible", never as the inventory** — and **no gate may rest on a ` + ` split, in any form.**

==============================================

## STATUS AS OF 2026-08-29: PHASE 93 (`h2-local-reply-content-length`) **OPEN AT STATE 2**. **THIS SESSION RUNS THE PHASE-93 PLAN.**

**What the 93 SPEC landed.** `SPEC.md` created (**409 lines**). **ADR-0315 §Context DRAFTED** (`DECISIONS.md` **18646 -> 18674**). **ZERO production `.go` bytes**; `ROADMAP.md`, `BEHAVIOR_CONTRACT.md`, `go.mod`, `go.sum` **BYTE-UNTOUCHED**. `STATE.md` rolled IN PLACE; `STATE_HISTORY.md` **532 -> 534**, strict guard **DELTA 0**.

### ⚠️ THE CHARTER, AND THE ONE THING THAT CHANGED
**On the HTTP/2 leg a locally generated reply emits a `Content-Length`, ALWAYS.** The fix lands in **`h2LocalReplyHeaders()`** and **deliberately NOT in `writeH2Reply`**.
⚠️ **THE SPEC REVERSED THE BRAINSTORM'S CHOSEN ARM. THE HELPER TAKES `bodyLen int`.** The BRAINSTORM picked a parameterless placeholder (`+3/-0`) and called the parameter *"strictly dominated … inert"* — **on COST ALONE, never pricing the CORRECTNESS axis.** ⇒ **`h2LocalReplyHeaders(bodyLen int)`: four sites pass `0`, three pass `len(bad502Body)`. `+10 / -8`, TWO production files, compiling prototype.**

### ⚠️ THE FINDINGS THE PLAN MUST NOT RE-LEARN

1. ⚠️ **THE PLACEHOLDER IS UNSAFE BECAUSE THE CARRIER IS READ BEFORE THE WRITER CORRECTS IT.** `chain.RunEncodeHeaders` runs ~26 lines BEFORE `writeH2Reply`, and **`writeH2Reply` builds its OWN field slice and never mutates the carrier**. All seven sites reach the encode chain — **proven by a `panic()` reachability control, not by reading.** ⇒ a placeholder ships a **SELF-CONTRADICTORY ACCESS-LOG RECORD** (`content-length: 0` beside `BytesSent=12` while the wire carries 12) on all three 502 sites **unconditionally**; and at `min_content_length ∈ [1,12]` it **SUPPRESSES a compression that BOTH today AND the truthful value perform**. **The truthful value is behaviour-preserving; the placeholder is not.**
2. ⚠️ **THE CONTROLLER'S TWO LEADING HAZARD HYPOTHESES WERE BOTH REFUTED BY EXECUTION.** The compressor counter-differential is **NIL at the default config** (both arms book the same two counters). And `buffer` is declared **`Encoder: nil`, decoder-only** — its header ref holds the REQUEST map. **The harm is the ACCESS LOG, which needs no compressor and no unusual config.**
3. ⚠️ **"THE H/1 `bodyLen` IS INERT TWICE OVER" IS TRUE IN BOTH HALVES AND FALSE IN ITS CONCLUSION.** Measured counterfactual: remove the parameter and a bodyless H/1 503 takes the COMPRESS path and is stamped `Content-Encoding: gzip` + `Vary`. **The parameter is inert on the WIRE and LOAD-BEARING at the ENCODE SEAM** — not for its value but because it makes the field PRESENT. **General lesson: a value a downstream layer overwrites is not thereby unobserved.**
4. ⚠️ **`ADR-0085` IS A MIS-ATTRIBUTION, AND IT IS THE CHARTER'S LOAD-BEARING RULE A.** `## ADR-0085` is *"Admin-mux reuse + LBP-1"* with **ZERO** matches for the doctrine. Both quoted sentences live under **`## ADR-0155`**, which merely CITES "per ADR-0085". **The doctrine IS ratified — in ADR-0155.** ⚠️ **`ROADMAP.md` row 93 and the BRAINSTORM both still carry the wrong attribution. Do not propagate it.**
5. ⚠️ **THE "TWO CONTRADICTORY RATIFIED RULES" CLAIM IS FALSE AS STATED AND INVERTS WHICH RULE IS OPERATIVE.** Carrier-construction and synthesis-fallback compose without conflict. Measured end-to-end through a real `FilterChain`: an empty-bodied `SendLocalReply` reaches the H/3 wire carrying **`content-length: 0`**. And `runH3` never installs the H/2 action (**0 occurrences of `SetH2Action` in `h3dispatch.go`**), so **no H/2 carrier can reach `writeH3Reply`.** ⇒ **the `h3dispatch` comment is a DEFECT TO REWORD, not a counter-rule to reconcile**, and its pin passes a **NIL carrier** so it neither contradicts nor detects the live behaviour. ⚠️ **What is genuinely UNMEASURED is narrower: what the REFERENCE emits on an H/3 empty-body local reply. The repo's only H/3 fixture is a `direct_response` GET WITH a body whose header-set equality is explicitly UNasserted.**
6. ⚠️ **THE INSTRUMENT CANNOT SEE THE DEFECT THE BRAINSTORM BUILT IT FOR.** Fixture `0004` drives **NONE** of the body-less sites — all five arms end on the **502** path where envoy-go DOES send a body, and the driver's only `503`/`504` mention is a **COMMENT**. ⇒ the row pins the **12-vs-87** departure and **NAMES the empty-body gap as OPEN**. Closing it needs new arms. **Do not claim it closed.**
7. ⚠️ **`+3 / -0` IS 1 OF AT LEAST 7 FILES AND WAS NEVER HONESTLY REACHABLE.** The helper's doc comment claims a *"three-header insertion order (Content-Type, Date, Server)"* that the fix falsifies; any de-rot costs deletions. MEASURED: production `+10/-8` (2 files) · unit test `+117/-0` · driver `+300/-44` · driver unit table `+57/-10`. ESTIMATED: README ~`+18/-8` · ADR §Context · contract rider ~`+3/-0` · **a `writeH2Reply` recompute pin ~`+60/-0` that NOBODY had priced.** ⚠️ **`reference_measured_prototype_is_a_lower_bound` FIRES A SIXTH CONSECUTIVE ROW, again by under-enumerating FILES.**
8. ⚠️ **NOTHING IN THE TREE PINS `writeH2Reply`'s RECOMPUTE-FOR-PRESENT-FIELD BEHAVIOUR**, and the row's entire correctness rests on it. The only evidence is a throwaway probe (a deliberate `999` came out as `12`; a pristine carrier came out with NO field; the unpatched tip read arity **0**). **LAND THAT PIN.**
9. ⚠️ **THE ROW INHERITS NO UNIT COVERAGE AND THE DRIVER'S OWN TABLE IS NOT A SUBSTITUTE.** Measured tree-wide with the fix reverted: **exactly ONE package reddens**, and `test/fixtures/0004-h2-routing/driver` stays **`ok`** — synthetic observations, re-confirming `reference_shared_assertion_vacuates_unit_table` **by live measurement**. The unit test is proven BOTH directions, with the **H/1 sibling as a control arm that must stay GREEN both ways**.
10. ⚠️ **A ROSTER BARRIER IS MANDATORY ON THE NEW INSTRUMENT.** Every per-arm assertion is a range loop, so **zero observations satisfies all of them** — measured: four pins passed on an empty slice, with one `wantErr` row passing for the wrong reason. Read the roster from the LIVE `p92Arms()`, never a literal.
11. ⚠️ **A LIVE H/2-ONLY BODY CORRUPTION WAS FOUND INCIDENTALLY AND IS BANKED, NOT CLAIMED.** On the tip a default-config compressor stamps `Content-Encoding: gzip` over an **UNCOMPRESSED** local-reply body (the H/1 leg does not have this, precisely because its composer makes the field PRESENT). **Either phase-93 design incidentally fixes it. Do not take credit for a fix the row did not design.**
12. ⚠️ **README `:147` IS REFUTED ON ITS CONSEQUENCE, NOT ITS COUNT.** The **seven** is right; *"changes every one of them"* is wrong — and under the SPEC's pick it is **half-true** (all seven DO change, gaining an argument, but not for the reason given). **Correct the clause precisely or ship a new falsehood in the commit that fixes the old one.**

### ⚠️ BANKED CANDIDATES
- **The empty-body defect, BOTH legs** — ten sites. Reference bodies MEASURED: 87 / 167 / 19 / 24 B. ⚠️ **The contract ALREADY RELAXES local-reply body bytes** (*"Status is asserted; body is relaxed"*) — a ratified relaxation, not an unnamed divergence. ⚠️ Adding a body emits a DATA frame where END_STREAM used to ride HEADERS.
- **The `Content-Encoding: gzip` over an uncompressed body corruption** (finding 11) — NEW.
- **The `h3dispatch.go` comment de-rot** (finding 5) — changes no behaviour.
- **The pooled-upstream-lifetime defect** — three sites, re-verified UNFIXED; reference side is a PREDICTION, three packages, probable SPLIT.
- **`ssl.connection_error`** — MEASURED ~4-6 production lines across 2 files, five `outcomeOther` rows already waiting. The best next IN-WINDOW pick.
- **1xx interim responses** — DROP-AND-DELIVER; ~5-15 lines, one symbol.
- **The H/1 no-`Host` divergence** — ⚠️ its `+90/-0` is a STALE PREDICTION.
- **`allocateOTLPPort`** — ⚠️ 26 driver files, not one site.
- **Counting the stat surface MECHANICALLY** — BLOCKED, not merely deferred.

## ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + ALL FOUR NCs YOURSELF** and record ACTUAL output. ⚠️ **The PLAN does NOT change a row's status, so measure your NO-OP too — a `ROADMAP.md` `--numstat` of nothing is itself the evidence.**
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** 92 IMPL twelve · 93 BRAINSTORM eleven · **93 SPEC ELEVEN, TWO of which were the CONTROLLER'S OWN hypotheses and ONE of which REVERSED the BRAINSTORM'S CENTRAL PICK.** Do the same, and record it when your agents refute you.
3. **LINE ANCHORS DRIFT; SYMBOL AND LITERAL-TEXT ANCHORS DO NOT — RE-VERIFY EVERY CITE AT YOUR TIP.** ⚠️ **PATHSPEC-SCOPE every symbol assertion.** ⚠️ **A RECEIVER WRITTEN `(c *FilterChain)` MUST USE `grep -F`, NOT `-E`.** ⚠️ **AT THE 93 SPEC, SIX BANKED ANCHORS WERE WRONG** — the two driver constants by **26 lines** (`:1382/:1383`, not `:1408/:1409`), and four pointing at COMMENT lines rather than declarations (`p92ContentLengthFields` `:1345`, `writeH3Reply` `:33`, `beginLocalReply` `:1288`, `directResponseAction.body()` `:91`). ⚠️ **`internal/filter/hcm/chain.go` DOES NOT EXIST — it is `internal/filter/http/chain.go`.** ⚠️ **`codec.go:87-89` is duplicated as a line-anchor INSIDE production code at `compressor.go:780`.**
3b. ⚠️ **CHARACTER-CLASS AND ESCAPE SELECTORS ARE THE FAIL-UNSAFE FAMILY.** `grep -cE '^\s+[a-z0-9./-]+ v[0-9]'` on `go.mod` reads 62 not 67 (misses uppercase/underscore paths) · `grep -oE '[A-Za-z]+ +BackendKind = 38'` TRUNCATES `H2` · `^[0-9]{4}-` reads 119, dropping `0007a`/`0007b` (use `ls -d test/fixtures/*/ | wc -l`) · ⚠️ **`\t` in a regex is TOOL-DEPENDENT: harness `grep` is ugrep 7.8.4, `/usr/bin/grep` is GNU 3.11. NAME THE TOOL.** Safe under both: `-P`, or `$(printf '\t')`.
3c. ⚠️ **`feedback_brief_citations_not_evidence` APPLIES TO INCIDENTAL FIGURES. Re-derive EVERY number you restate — including every number in THIS file.** ⚠️ **The 93 SPEC found the BRAINSTORM's "36 filter files" NOT REPRODUCIBLE — six scopes read 54/46/34/42/228/29. On a contested count: NO NUMBER.** The load-bearing fact was structural: **`SendLocalReply` has exactly ONE definition.**
3d. ⚠️ **NO GATE MAY REST ON A ` + ` SPLIT.**
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Live: TWO SDS dial-budget flakes (⚠️ **DO NOT reuse the `go list -deps` exclusion blanket — TRUE for `xds` (0), FALSE for `boot` (1)**) · the driver-owned receiver port race · `internal/httpclient` zero-value · the two 84.2-era flakes · the REFERENCE h2spec section-8 flip · `TestOutlierDetector_ConcurrentEjectExactlyOnce`. ⚠️ **`TestServerConn_TinyWindowDelivery` IS NOT A FLAKE — phase 91 FIXED a live production deadlock; a recurrence is a REGRESSION of row 91.**
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only). ⚠️ **THE FOREIGN-CONTAINER ROSTER IS NOT CLOSED AND IT CHANGED BETWEEN STAGES.** Seen and LEFT UNTOUCHED at the phase-93 SPEC: `curl-world-tls-1`, `curl-world-httpbin-1`, `infallible_booth`, `crazy_kare`, `golink-ai`, `quizzical_goldstine`. ⚠️ **`jolly_nobel` and the `reaper_*` present at the BRAINSTORM were GONE by the SPEC** — do not treat any roster as stable. Name your own with a distinctive per-stage prefix.
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`.
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL.** **`gofmt -l` never exits non-zero — gate on OUTPUT.** ⚠️ **`golangci-lint` DOES exit non-zero on findings in this build — but STILL gate on OUTPUT.** ⚠️ **go test WITHOUT `-v` PRINTS ZERO `=== RUN`** — `RUN=0` beside `RC=0` is a VACUOUS GREEN. ⚠️ **A `-run` selector matching nothing prints `[no tests to run]` and EXITS 0.** ⚠️ **On `-v` output, unanchored `grep -c 'FAIL'` reads nonzero ON A FULLY GREEN TREE** — use `grep -cE '^(FAIL|--- FAIL)|^ *--- FAIL'`.
7b. ⚠️ **`grep -c` ON ZERO MATCHES PRINTS `0` AND EXITS 1** — capture (`v=$(… || true)`), NEVER chain with `&&`, and NEVER write `$(… || echo 0)`, which emits TWO zeros.
7c. ⚠️ **A GREEN TEST RUN IS NOT EVIDENCE A SITE IS EXERCISED — IT MAY BE DEAD CODE.** Prove it with a **`panic()` REACHABILITY CONTROL** (the 93 SPEC used one to prove all seven local-reply sites reach the encode chain), and **do not skip the discriminating half**.
7d. ⚠️ **NC EVERY NEW TEST, NOT JUST THE RISKY-LOOKING ONES. AND NC YOUR OWN FIXES** — a NC that is a BUILD BREAK proves nothing; neutralise instead so the package still compiles.
7e. ⚠️ **A UNIT TABLE OVER A SHARED ASSERTION IS NOT A GATE ON PRODUCTION BEHAVIOUR** — re-confirmed BY MEASUREMENT at the 93 SPEC (finding 9).
8. ⚠️ **`pgrep -f` / `pkill -f` MATCHES YOUR OWN SHELL AND KILLS THE TOOL CALL (exit 144)**. **Kill ONLY PIDs you captured yourself** (from `$!`), and verify with `kill -0`. ⚠️ **Backgrounding an entire `&&` chain makes `$!` name the SUBSHELL and orphans the real process.**
9. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-*`, `wt-*` or stage worktree should outlive its stage close** — expect only `master` (verify with `git worktree list`).
10. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** (a)/(b) differential **121 fixtures** + full `go test ./...`, gated on **`PIPESTATUS[0]`** and a **SET RECONCILIATION** (⚠️ **NOT `INNER_EXIT`, which DOES NOT EXIST**); (c) h2spec cite ONLY from your own run — ⚠️ **h2spec is MEASURED BLIND to burst-drain ordering**; grpc-conformance deferred in writing; proxy-wasm **10 families measured** (NOT "10/16"); (d) fuzzers **56 / 48** today; (e) the ANCHORED panic gate `^panic:|DATA RACE|SIGSEGV` on every differential launch; (f) no REVIEW.md — standing departure. ⚠️ **`-race` on the DIFFERENTIAL suite is VACUOUS** — the subject is an unraced subprocess.
11. ⚠️ **THE FIXTURE INVOCATION: `go test ./test/differential/ -run 'TestDifferential/0004-h2-routing' -count=1 -v 2>&1`.** ⚠️ **`-count=1` IS NOT OPTIONAL** — the harness builds envoy-go as a **SUBPROCESS**, so a router edit is **not a compile-time input** to that test binary and the cache serves a stale PASS. **A fix whose own gate cannot see it is the most dangerous form of this trap.** ⚠️ **Gate (a)'s failure mode is a SILENT PASS** — the runner `t.Skipf`s an unregistered fixture and **NO fixture-count gate exists anywhere in the tree**. **ASSERT THE FIXTURE SET** (121 dirs vs 121 subtests vs 0 skips, reconciled BY NAME both directions) and assert `--- PASS: TestDifferential/0004-h2-routing` **explicitly**.
11b. ⚠️ **`go test ./...` DRIVES DOCKER — AND THERE ARE **TWO** DOCKER DRIVERS, NOT ONE.** Besides `test/differential`, **`test/conformance/h2spec/h2spec_test.go`** also drives Docker. **Exclude BOTH:** `go list ./... | grep -vE '/test/differential$|/test/conformance/h2spec$'`. **The BRAINSTORM's recipe named only the first.** Serialize Docker ownership across parallel agents.
12. ⚠️ **`-p` PUBLISHING WORKS; `--network host` IS WHAT DOES NOT.** ⚠️ A **STRICT_DNS** cluster whose name does not resolve **blocks listener init entirely** — prefer `STATIC` by IP. ⚠️ **`clusters: []` is BOOT-REJECTED.** ⚠️ **envoy-go REJECTS `match.headers`**, ⚠️ **refuses `codec_type: HTTP2` without TLS unless `--allow-h2c`**, ⚠️ **boots with `-c`, NOT `--config-path`**, and ⚠️ **`access_log[].log_format` is BOOT-REJECTED.** ⚠️ **`ENVOY_TARGET.md` is at `docs/envoy-go/ENVOY_TARGET.md`, NOT the repo root.** ⚠️ **A PROBE BACKEND MUST BIND `0.0.0.0`, NOT `127.0.0.1`.**
13. ⚠️ **A RAW-FRAMER PEER MUST HOLD ONE `hpack.Decoder` PER CONNECTION.** ⚠️ **Set `ReadMetaHeaders` to NIL when probing malformed shapes.** ⚠️ **`0004` is TLS+ALPN-h2, NOT h2c.**
14. ⚠️ **`bash /dev/tcp` + `timeout cat <&3` INSIDE COMMAND SUBSTITUTION RETURNS SILENTLY EMPTY.**
15. ⚠️ **GO DOES NOT FLAG UNUSED CONSTANTS.** Post-removal cleanup is grep-driven.
16. ⚠️ **`golangci-lint`'s misspell runs in locale US and FIRED ON THE 93 SPEC'S OWN PROTOTYPE** (`favour` -> `favor`). **Sweep British spellings in `.go` comments BEFORE the gate.** ⚠️ **Markdown prose may use them freely.**
17. ⚠️ **A GREEN RUN OF A CONTENTION-SENSITIVE TEST PROVES NOTHING.** Phase 91's subject cleared 5/5 uncontended while failing 9/32 contended on the SAME binary.
18. ⚠️ **AN ABSENCE CLAIM NEEDS A POSITIVE CONTROL AND A DISCRIMINATING NEGATIVE CONTROL.**
19. ⚠️ **DERIVE next-free ADR FROM THE TAIL, NEVER FROM THE HEADING COUNT.** The id space is SPARSE (exactly one gap, `0209`) and **headings+1 reads 315 — the TAIL ITSELF, a TAKEN id.**
20. ⚠️ **`reference_measured_prototype_is_a_lower_bound` HAS FIRED SIX CONSECUTIVE ROWS, ALWAYS BY UNDER-ENUMERATING *FILES*.** **ENUMERATE BY COMPILING PROTOTYPE.**

## ⚠️ THE `PROPOSED` GUARD — TWO DIFFERENT REGEXES, AND THE ROUTER CONFLATED THEM

⚠️ **THE PHASE-93 SPEC ARMED THE GUARD `0 -> 1`, NOT `1 -> 2` AS ITS ROUTER INSTRUCTED.** Measured on both sides:

| form | before | after | where |
|---|---|---|---|
| `^> \*\*STATUS: PROPOSED` — **the house form** | **0** | **1** | `:18650` under `## ADR-0315` (`:18648`) |
| `^\*\*Status:\*\* PROPOSED` — **the ADR-0231 decoy** | **1** | **1** | `:14866` under `## ADR-0231` (`:14864`), byte-untouched |
| union of both | 1 | 2 | — |

⚠️ **VERIFY BY LINE AND BY ADR, NEVER BY THE COUNT ALONE** — a session gating on the decoy form reads `1` after a correct arming and concludes it failed. A neutralising NC dropped the canonical form to **0** while the decoy **STAYED 1**. This is `ADR-0312 §Consequences (x)` firing a second time; it recorded the identical hazard in the DISARM direction. ⚠️ **NEVER gate on the UNANCHORED form (90 lines / 101 occurrences) nor on the middle-ground `^\*\*Status:\*\*.*PROPOSED` form (23).** **The IMPL DISARMS the house form `1 -> 0` when it completes ADR-0315 — and must verify BY LINE AND BY ADR.**

## ⚠️ THE ARCHIVE GUARD — TWO FINDINGS FROM THE 93 SPEC

⚠️ **THE STRICT `163` IS NOT THE ENTRY COUNT.** Measured: bullet-anchored **217** = strict **163** + parenthetical-shape **54**, exactly. **The guard is SOUND as a DELTA-0 SHAPE check** (a correctly-shaped parenthetical append moves it by 0) **but the number is a SUBSET.** The three forms disagree and **any gate must name WHICH it uses**; the colon form carries **two false positives**.

⚠️ **THE ARCHIVE'S POSITIVE CONTROLS ARE SELF-INCREMENTING — PREDICTED PROSPECTIVELY AND CONFIRMED AT THIS CLOSE.** The two controls read **8** and **1** immediately before the append and **9** and **2** immediately after, moved by the archive line that NAMES their subjects and by nothing else. **A control figure recorded in the file it measures is invalidated by the act of recording it.** ⚠️ **The same mechanism bites the eviction check:** the §Recent preamble NAMES the evictee, so a naive "is it gone?" grep on `STATE.md` reads **1**. **The DISCRIMINATING form is the strict `^- \*\*prior active-phase:\*\* \`<label>\`` form, which reads 0.**

## ⚠️ PORT BANDING

`net.ipv4.ip_local_port_range = 32768 60999`. ⇒ **BAND YOUR PROBE PORTS BELOW 32768.** ⚠️ **The differential harness reserves `20000..31007` (`harness_test.go:293`)**, so `12000-19000` is the safe band for ad-hoc probes — a failure OUTSIDE the harness band is an **out-of-band** occurrence, NOT an in-band recurrence. **Check availability with `ss -tan` (ALL states), never `ss -ltn`.** ⚠️ **FIXTURE ports are FAMILY-BANDED as `10<index>` — but `10120` is TAKEN (fixture `0028`); free in-band is `10126-10129`.** ⚠️ **`0120` as a DIRECTORY is FREE.**

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (**ugrep 7.8.4**). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **THE METHOD BINDS; carry no standing discrepancy figure and no standing absolute.** ⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS; print FILE NAMES, never a count.** ⚠️ **THE HARNESS `grep` STRIPS A LEADING `./`.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files; use `-v --no-index`, and `git status` is the discriminating check). ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **A DISCREPANCY MAY FAIL TO MANIFEST** — absence of a discrepancy is not evidence the blindness is gone.

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 93 (h2-local-reply-content-length)' --format='%h %s'   # the SLUG form
git log --grep '^phase 93' --format='%h %s'                                   # the LOOSE form — RUN IT to CROSS-CHECK
git log --grep '^phase 92 (h2-response-header-validation)' --format='%h %s'   # a CLOSED phase, use as a FIRING CONTROL
```
⚠️ **RUN BOTH FORMS AND RECONCILE THE SETS. NO COUNT IS QUOTED HERE, DELIBERATELY — BOTH FORMS ROT.** **A `| head -1` on ONE form can silently return a NON-TIP commit.** **Take the tip from `git rev-parse master`.** ⚠️ **AND WHEN YOU LAND A CORRECTION, PUT THE FULL SLUG IN ITS SUBJECT.**

Execution style: **subagent-driven per `feedback_execution_style`**. Subagents commit LOCALLY on the ONE stage branch with EXPLICIT PATHSPECS; the controller squashes (`feedback_subagents_no_push`, `feedback_push_to_origin`). **Pure MEASUREMENT agents should commit NOTHING and prove their tree clean.** ⚠️ **PARALLEL AGENTS IN ONE WORKTREE RACE THE GIT INDEX EVEN WITH DISJOINT PATHSPECS** — use **PER-AGENT WORKTREES ON THEIR OWN BRANCHES**, merged by the controller. ⚠️ **GIVE EACH PARALLEL AGENT ITS OWN PRIVATE SCRATCH DIR.** ⚠️ **AND VERIFY WHAT A SUBAGENT REPORTS — BUT ALSO BELIEVE IT WHEN IT REFUTES YOU:** at the phase-93 SPEC, agents refuted the controller's two leading hazard hypotheses AND its cite of the H/3 carrier source, and were right every time; a fourth reversed the BRAINSTORM's central design. Per-task `gofmt` + `golangci-lint` on touched packages, **gated on OUTPUT**. ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root** — build with `-o` into scratch.

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root — **observed AGAIN at the phase-93 SPEC, with the harness printing `Shell cwd was reset to /home/esa/git/envoy-go`**. Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` before any commit or gate run. ⚠️ **Shell VARIABLES do not survive between Bash calls** — re-export in every command. ⚠️ **AND AN UNQUOTED HEREDOC (`<<EOF`) COMMAND-SUBSTITUTES BACKTICKED TEXT** — use `<<'EOF'` and pass paths via the environment.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — `git status --porcelain` AND `git diff --stat master` must show ONLY your intended edits. For a patched TRACKED file: `sha256sum` BEFORE patching, `git checkout -- <path>` after, `sha256sum -c` the capture — ⚠️ **and COMMIT FIRST, because `git checkout --` restores from HEAD and will wipe uncommitted work.** ⚠️ **Also check the MAIN repo root, and `--untracked-files=all` to catch throwaway probe files.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads the phase-93 PLAN.** ⚠️ **`grep -c 'next-skill:'` reads 2: line 7 is the ADR-0288 RULE STATEMENT matching its own grep, and the live pointer is the other. That is CORRECT, not a violation.** ⚠️ **§Project counts is a PHASE-76-ERA SNAPSHOT and SAYS SO.**
2. `docs/envoy-go/phases/93-h2-local-reply-content-length/SPEC.md` — **the charter, the REVERSAL (§4), the instrument (§5), and §14 "What the PLAN owes" (eight items).**
3. `docs/envoy-go/phases/93-h2-local-reply-content-length/BRAINSTORM.md` — ⚠️ **READ IT KNOWING §3.2, §3.3 and §6.1's ARM CHOICE ARE REFUTED BY THE SPEC**, and that its ADR-0085 attribution is wrong.
4. `docs/envoy-go/DECISIONS.md` **ADR-0315** §Context (ten paragraphs, drafted at the SPEC) — and **ADR-0155** for the ratified `content-length: 0` doctrine the BRAINSTORM mis-attributed to ADR-0085.
5. `test/fixtures/0004-h2-routing/README.md` `:137-153` — the per-side pins and the documented departure. ⚠️ **Its `:147` sentence is refuted on its CONSEQUENCE and must be corrected precisely when the fix lands.**

## What the next session owes
**RUN THE PHASE-93 PLAN** (state 2 -> 3), producing `PLAN.md` and discharging the eight items in `SPEC.md` §14 · **RE-RUN the sentinel + all four NCs yourself** and record ACTUAL output — ⚠️ **NC-A and NC-B are BOTH two-line shapes while row 93 is open** · **DO NOT create `stop`** · **ROW 93 STAYS `in-progress`; `want` STAYS 125** — the PLAN adds no row, so a `ROADMAP.md` `--numstat` of nothing IS the evidence · **A PLAN ADDS NO ADR** — tail STAYS **ADR-0315**, next-free STAYS **ADR-0316**, `^---$` STAYS **216**, and the house `PROPOSED` guard STAYS **ARMED at 1** (verify BY LINE AND BY ADR; the decoy also reads 1) · **REFUTE YOUR PREDECESSOR BY EXECUTION** · `STATE.md` rolled IN PLACE with the oldest §Recent entry evicted by a **DIRECT DATE READ** (⚠️ **read the dates yourself — the last TWO closes both had a UNIQUE OLDEST that ALSO sat last, and that coincidence is exactly why position-by-convention must NOT be adopted**) and archived to `STATE_HISTORY.md` as **ONE INLINE LINE** whose label does NOT match `^- \*\*prior active-phase:\*\*`, so the strict guard stays at **163, DELTA 0** · ⚠️ **AND ROLL THE §Recent PREAMBLE SENTENCE TOO — the guard is structurally blind to it** · ⚠️ **AND IF YOU NAME A POSITIVE CONTROL IN THE ARCHIVE LINE, DISCLOSE THAT YOU HAVE JUST INCREMENTED IT** · `next-prompt.txt` rolled (`git add -f`) · ONE squashed commit, merged and pushed — ⚠️ **and if a late measurement invalidates something you already pushed, land a CLEARLY-LABELLED correction commit (WITH THE FULL SLUG IN ITS SUBJECT) rather than force-pushing or leaving it standing.**
