=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-RUN MECHANICALLY 2026-07-24 at the phase-74 SPEC close. It does NOT fire. `stop` was NOT created.** ACTUAL output, recorded rather than predicted:
- **(1) prints `NOT DONE: row 74`.** Row 74 stays `in-progress` — a SPEC never flips a row. It flips `done` only at the phase-74 IMPL six-gate.
- **(2) prints THREE live "candidates:" sentences** — HTTP/3 (`ROADMAP.md:184`), xDS (`:194`), Observability (`:204`). **Cite the command, never the adjective** (`reference_sentinel_deferred_sentence_live_vs_historical`): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` => **3**. ⚠️ A naive `grep -c 'candidates:'` returns **11** and would mislead; there is also exactly ONE historical `candidates were:` recap (at `:204`), which check (2) correctly does NOT match. **ROADMAP is BYTE-UNTOUCHED at this SPEC** (`git diff --stat -- docs/envoy-go/ROADMAP.md` EMPTY).
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** => THREE families never opened. (The `WASM` line remains a ROADMAP **bookkeeping artifact** — `internal/wasm/` is a substantially-built wazero host and rows 25/25.1/25.2/25.3 are all `done` — deliberately LEFT AS-IS, since the stage-close mechanics depend on this output.)

All three checks print => the sentinel does NOT fire. Abundant work remains.

⚠️ **CHECK (1) HAS A KNOWN BLIND SPOT — FOUR of 105 rows, for THREE distinct reasons** (PRE-EXISTING, no action owed). Check (1) sees **101 of 105** rows. The four it cannot see: `| 00 | bootstrap | — | done |` (the field `[0-9.,  ]*` does not match an **em-dash** "after" column) · `| 04 | http-1.1 | 03 | done |` (the slug field `[a-z0-9-]+` does not match the **dot** in `http-1.1`) · `| 28.1a |` and `| 28.1b |` (the number field `[0-9.]+` does not match a **LETTER SUFFIX**). All four are `done`, so there is no current impact — but check (1) is **not exhaustive**, and any future dotted slug, em-dash cell or letter-suffixed row number goes invisible too.

==============================================


## STATUS AS OF 2026-07-24: THE PHASE-74 SPEC IS COMPLETE AND LANDED. ROW 74 IS `in-progress`. THIS SESSION PERFORMS THE PHASE-74 PLAN.

**What the phase-74 SPEC landed** (`git log --grep 'phase 74'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): **exactly TWO files** — the new `docs/envoy-go/phases/74-tls-handshake-outcome-stats/SPEC.md` and the **ADR-0296 §Context append** to `DECISIONS.md` (a PURE APPEND: 28 insertions, ZERO deletion lines, with `ADR-0286` BYTE-IDENTICAL by sha256). **ZERO production `.go`. ROADMAP BYTE-UNTOUCHED. Row 74 STAYS `in-progress`.** The stage ran **EIGHT agents**, FOUR of them live-probe fleets against `envoyproxy/envoy:contrib-v1.37.2` on disjoint port ranges and container prefixes, executing **~35 arms** — a fresh container per arm, bridge networks, a positive control in every bootstrap, and every arm's discrimination criterion stated BEFORE the run.

### What matters for the NEXT session, in order

1. **⚠️ THE SENTINEL DID NOT FIRE AND `stop` WAS NOT CREATED.** Re-run all three checks yourself and record the ACTUAL output; do not predict it.

2. **⚠️ THE ROW'S SHAPE CHANGED AT THE SPEC — D-TLSHS-REGSCOPE WAS REFUTED, AND THE REFUTATION MADE THE ROW CHEAPER.** The BRAINSTORM anticipated *unconditional* registration ⇒ a flat +3. **The reference registers `listener.<addr>.ssl.*` TLS-CHAINS-ONLY.** The zero-visibility confound was eliminated FIRST (plain `/stats` demonstrably renders zeros — 429 lines vs 85 for `?usedonly`, 92 zero-valued `listener.*` names on the plaintext container), so an ABSENT name means genuinely NOT REGISTERED; then settled by ONE container / TWO listeners / ONE scrape with both positive controls green. **envoy-go MATCHES it, and the gate ALREADY EXISTS as a field** — `listenerRuntime.tlsMode` (`internal/listener/manager.go:145`, set `:639`), whose FIRST production consumer this row becomes, unambiguous because envoy-go REJECTS mixed TLS+plaintext chains on one listener (`:516-525`). ⇒ **the +3 is a NAME-surface delta, not a flat per-deployment one**; a plaintext-only deployment gains ZERO names.

3. **⚠️ THE SPEC REVERSED ITS OWN QUIC DECISION BEFORE LANDING — read SPEC §16 before re-deriving it, or you will reach the same wrong answer.** D-TLSHS-QUIC was FIRST resolved to gate QUIC out, reasoning that *"a permanently-zero counter is a false measurement while an absent one is an honest coverage gap."* A probe run specifically to settle it **REFUTED that on the facts**: the reference registers the full `ssl.*` family on a QUIC listener AT BOOT and **NEVER moves it** — `ssl.handshake: 0` after FIVE confirmed HTTP/3 connections, and `connection_error: 0` on a failure arm where the TCP comparator **in the same process and the same scrape** fired. ⇒ envoy-go doing the same is **EXACT PARITY**, and gating QUIC out would have been the DEPARTURE. **The gate is `rt.tlsMode` ALONE — no kind check** — provably sufficient because `startQUIC` HARD-ERRORS without a TLS config (`quic.go:33-36`), so every QUIC listener that boots has `tlsMode == true`. `internal/listener/quic.go` stays BYTE-UNTOUCHED.

4. **⚠️ NO BUILD OF ROW 74 EXISTS, in-tree or in scratch.** The classifier, the `tlsMode` gate, the name-set guard and the `0111` asserter are all **SPECIFIED and NONE is COMPILED**. **Nothing in SPEC §3 may be cited as execution evidence for buildability** (§1.2 says so explicitly). That is the PLAN's and the IMPL's job.

5. **⚠️ FIVE BRAINSTORM CITES WERE REFUTED — and since `git diff --stat de2d7737 HEAD` touches NO production `.go`, each was wrong WHEN WRITTEN, not drift.** `quicAcceptLoop` is `quic.go:88` not `:104` · `errors` is ALREADY imported (`manager.go:6`) ⇒ **+0 imports**, not +1 · **the stat SPELLING**: `normalizeAddr` underscores DOTS too, so envoy-go emits `listener.0_0_0_0_10000.…` where the reference emits `listener.0.0.0.0_10000.…` — a DELIBERATE documented divergence (`manager.go:326-332`) that **decides the fixture asserter's shape** · the QUIC framing · the ROADMAP anchors (`:199`→`:200`, `:203`→`:202`). **RE-DERIVE every SPEC anchor at the PLAN tip anyway — a SPEC is not evidence either** (`feedback_brief_citations_not_evidence`).

6. **⚠️ EDIT SITES THE BRAINSTORM OMITTED, all in SPEC §11 — the PLAN must task each.** `internal/stats/name.go:452-464` **`helpText` ×3** (⇒ `internal/stats` is **NOT** byte-untouched; without it `/stats/prometheus` degrades to `# HELP envoy_listener_ssl_handshake envoy_listener_ssl_handshake`) · TWO stale comments in the very file the row edits, `manager.go:172-174` and `:345`, both saying *"2 metrics per listener"* · `manager_test.go:1911-1913`/`:1928` `TestListenerManager_AllocatesTwoMetricsPerListener`, whose NAME says *"exactly the 2"* but whose BODY only asserts PRESENCE ⇒ **+3 produces NO RED from it** while its name silently goes false · and the `0111` retirable claim lives at **FOUR** sites, not two (`README.md:162-165`, `expectations.yaml:177-184`, `expectations.yaml:189`, **`envoy.yaml:24-25`**), **each BUNDLING a retirable half with a STILL-LIVE `/listeners` guard — a blanket delete drops a live guard.** Also the RD3 forced-send disclaimer (`driver.go:73-81`, `README.md:100-110`, `expectations.yaml:109-119`) **INVERTS** at the stats layer: forced-send becomes exactly the discriminator between `fail_verify_error` and `fail_verify_no_cert`.

7. **The live hazards — never reflex-classify any of these as a regression.** The PRE-EXISTING `internal/cluster` `-race` flake (`TestOutlierDetector_ConcurrentEjectExactlyOnce`, `internal/cluster/outlier_test.go:766` — **isolate-re-run, do NOT re-classify**) · the full-suite startup flake (`subject ready: EOF` on an UNRELATED fixture) · the SDS init-fetch dial-budget flake · and TWO still-**UNINDEXED** load flakes, isolate-green and NOT root-caused: `internal/httpclient TestOptions_ZeroValue_NoOpDefaults` and `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery`.

8. **The deferred roster.** The live Observability `candidates:` sentence still carries FOUR members. **NEWLY SURFACED at this SPEC and NOT chartered:** **`ssl.connection_error`** — the row's one new DEPARTURE and the cheapest identified follow-on (the reference books non-cert handshake failures there; envoy-go's `other` bucket increments NOTHING) · the other ELEVEN fixed reference `ssl.*` names incl. **`ssl.no_certificate`**, which the LANDED record already named at `BEHAVIOR_CONTRACT.md:962` · **`Listener.stat_prefix`** (accepted-and-silently-ignored; the reference honours it and drops the address from the scope entirely) · the IPv6-bracket and dot-normalization scope divergences (PRE-EXISTING, affecting `downstream_cx_total` today) · the **stat-surface ledger HOLE** (no recorded `1200 → 1201`; three stale `1200`s at `BEHAVIOR_CONTRACT.md:1429`/`:1463`/`:1495`) · the reference's whole QUIC accounting surface (`http.<prefix>.downstream_cx_ssl_total`, `http3.*`, `quic.connection.*`, `quic.dispatcher.*`, `quic_server_transport_socket_factory.*`), entirely unimplemented. **Carried unchanged:** the `upstream_cluster` span tag (the RUNNER-UP — deferred on a RESIDUAL DIVERGENCE, **not on cost**) · pick-time `picked`-propagation · `access_log[].filter` (10-13) · `hcm.merge_slashes` (10-13, carries a pre-existing H2 `url.Parse` routing bug) · `hcm.access_log_options` (9-11) · the `stdout`/`stderr` loggers (7-9) · `stats_flush_on_admin` (8-10) · the swallowed-panic BOOT HANG · route-level header manipulation · `prefix_rewrite`/`host_rewrite_*` · HCM `server_name`/`via` · the default-config header injection divergence · all tracing remainders · all xDS · all HTTP/3 · and the never-opened families (**WASM is a bookkeeping artifact**).

**Counts at exit (re-run MECHANICALLY in the worktree; never copy them):** fixtures **119** (`ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0117-tracing-custom-tags-metadata-cluster`) / fuzzers **55** (`grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`) / stat surface **1201** (BEHAVIOR_CONTRACT doc count; NO mechanical command) / BackendKind tail **38** / go.mod modules **2** (the phase-61.2 lineage figure; the single `go.mod` requires **67** — 18 direct, 49 indirect) / DECISIONS tail **ADR-0296** PROPOSED (next-free **ADR-0297**) / ZERO new packages. Phase-74 ANTICIPATIONS, **NOT YET LANDED**: **+3 stats (1201 → 1204)** · +0 fixtures (**119**) · +0 fuzzers (**55**) · +0 BackendKinds (**38**) · +0 modules (**2**) · **+0 imports** · ZERO new packages · **ZERO new exported symbols**.

## What THIS session does — the phase-74 PLAN

One stage per session. Row 74 STAYS `in-progress`; it flips `done` only at the phase-74 IMPL six-gate (ADR-0106, the SOLE leg — a SINGLE FLAT ROW, `reference_roadmap_split_phase_row_done`).

**The owed roster:**
1. **Decompose the ~9-task SINGLE FLAT ROW spine** sketched at SPEC §10, each task red-first → green → break-verified with its own pre-known red set and exit gate. ADR-0045's escape valve is armable-but-unconsumed: the production diff is confined to ONE file, and the one thing that could have forced a split (a posture requiring the QUIC runtime) does **not** — §3.4 needs no kind check at all.
2. **RE-DERIVE every SPEC anchor at the PLAN tip.** A SPEC is not evidence (`feedback_brief_citations_not_evidence`); re-derive PROSE claims too, not just line numbers. `reference_parallel_stream_mints_fresh_drift`: a post-SPEC commit can shift anchors — re-derive rosters at the PLAN tip.
3. **PIN THE SEVEN BREAKS (A-G) from SPEC §10, and note that D is framed BACKWARDS on purpose.** **A** collapse `other` into `verifyError` ⇒ fires on the non-cert arms and NOT the cert arms · **B** swap the `verifyError`/`noCert` arms ⇒ must fire in **EXACTLY TWO of the three** increment tests (the cross-product is what discriminates, `reference_probe_must_discriminate`) · **C** move the success `Inc` outside the `if selected.tlsCfg != nil` block ⇒ the plaintext test fires · **D** ⚠️ **THE DISTINGUISHING BREAK — ADD a `rt.kind != kindQUIC` gate** (i.e. implement the SPEC's REFUTED first reading) ⇒ the QUIC registration test must fire; it ADDS the wrong gate because the correct implementation has no kind check to delete · **E** drop the `rt.tlsMode` gate ⇒ the plaintext test fires and the QUIC test must **NOT** (if both fire they are entangled) · **F** hand-write the no-cert string ⇒ must be **SHOWN to leave the suite GREEN**, proving the live-handshake construction is load-bearing rather than decorative · **G** break one asserted counter inside `AssertStats` ⇒ confirm the **STATS** assertion fired, not a `CompareBytes` mismatch. Run breaks with `-count=1` AFTER committing (`reference_differential_break_protocol_count1`, `reference_break_protocol_commit_first`) and confirm WHICH assertion fired (`reference_deliberate_break_wrong_assertion`). ⚠️ A PLAN break INSTRUCTION that does not compile proves nothing (`reference_plan_break_instructions_dont_compile`) — substitute an equivalent rather than shipping red-that-does-not-build.
4. **The registration guard must assert a NAME SET, not a cardinality.** The landed precedent (`internal/statssink/registration_test.go:26`, one of FIVE guards all in that one file) counts metrics via `reg.Walk` and **never inspects `m.Name()`** ⇒ a count-only inversion would PASS with all three names misspelled. Assert EXACTLY THREE, spelled `…ssl.handshake` / `…ssl.fail_verify_error` / `…ssl.fail_verify_no_cert`, and separately pin that a plaintext listener adds ZERO and a QUIC listener adds all THREE at value ZERO.
5. **The classifier's named FRAGILITY is non-negotiable.** The no-cert arm is a bare `errors.New("tls: client didn't provide a certificate")` (`handshake_server.go:964`); `crypto/tls` exports FOUR error-ish symbols and **ZERO error VALUES**, so a string match is FORCED. **Its test MUST construct the error by running a LIVE in-process handshake, never by hand-writing the string.** Break F is what proves that. Also pin: `fail_verify_error` means *"certificate CHAIN VERIFICATION failed"*, **NOT** *"client cert rejected"* (a cert/private-key mismatch and a malformed DER never reach `certs[0].Verify()` and land in `other`) — the wording is load-bearing; and the classifier's accuracy is **CONDITIONAL** on the handshake ctx carrying no deadline (`crypto/tls/conn.go:1539-1547` lets a firing ctx REPLACE the outcome — demonstrated; bounded today because `internal/listener/` has ZERO production `WithTimeout`/`WithDeadline` and `cmd/envoy-go/main.go:339` is a cancel-only `signal.NotifyContext`).
6. **The `0111` asserter posture is DECIDED — do not re-open it, but DO re-derive its anchors.** Scrape **`/stats/prometheus`, NOT flat `/stats`** (the two sides normalize the listener address differently but both hoist it into a LABEL, leaving the metric NAME cross-side identical — the `0005` precedent), **Shape A** (scrape once, absolute counts, justified because nothing pre-moves `l_edf`'s `ssl.*`). ⚠️ `var _ fixture.StatsAsserter = (*edfDriver)(nil)` is **MANDATORY** — dispatch is a SILENT type assertion and compiler, `go vet` AND `golangci-lint` are all silent — and **the ABSENT check must be SEPARATE from the value check**, since a counter that fails to register reads `0 == 0` and passes **VACUOUSLY**. `fixture.TB` has only `Errorf`/`Fatalf`/`Helper` — no `Logf`, no `Cleanup` (`reference_fixture_tb_has_no_logf`); `t.Errorf` per property (`reference_fatalf_makes_assertions_unreachable`). Pin the full selector: `go test ./test/differential/ -count=1 -run 'TestDifferential/0111-tls-cvc-empty-dynamic-fallback' -v` (`reference_differential_run_selector`). **+0 fixtures (119), NO new YAML, NO new directory.**
7. **Task the BEHAVIOR_CONTRACT edit map B1-B8 explicitly** (SPEC §9), including ⚠️ **B7 — the stat-surface ledger HOLE**: `1201` lives at exactly TWO lines (`:831`, `:847`) → **1204**, there is NO recorded `1200 → 1201` step, and THREE stale `1200`s survive at `:1429`/`:1463`/`:1495`. The edit map is bigger than "change 1201 to 1204". Also **B4 must be written as SHARED PARITY, not an envoy-go departure** — that was the SPEC's own refuted first reading.
8. **Docs-only, ZERO production `.go`.** ROADMAP is UNTOUCHED at a PLAN and **row 74 STAYS `in-progress`**; **NO deferred sentence is narrowed at ANY pre-IMPL stage** (phase-57 precedent). ⚠️ The narrow was VERIFIED MECHANICALLY at the SPEC in a SCRATCH copy — check (2) **STAYS 3** and the `candidates:` marker survives — **but the mechanical check is not the whole edit**: after removing the three names the clause still asserts *"envoy-go emits ZERO such stats"* and *"a framework-surgery row, NOT an inline add"*, the first FALSE and the second the very adjective this row refutes. **The IMPL must replace the PROSE, and the PLAN must task that.** DECISIONS stays at **ADR-0296 PROPOSED** — the PLAN appends nothing to it.
9. **Close by re-running all three sentinel checks mechanically, TWICE** — in the stage worktree AND on LANDED master after the squash-push (the phase-72/73/74 precedent: a stage worktree can disagree with what actually landed, and the post-push re-run is the one that counts). **Create `stop` if and ONLY if all three print nothing.**

**Stage-close mechanics (a PLAN):** `PLAN.md` + `PROGRESS.md` are the ONLY two files in the phase-directory delta. STATE §Current rolled **IN PLACE** (lifecycle 2 → 3; the §Recent lineage re-capped at FIVE **with its PREAMBLE updated** — the ADR-0288 rule). ⚠️ **The ADR-0288 singleton greps return 2, not 1**, for each of the three field-name tokens — the second hit is `STATE.md:7`, the RULE STATEMENT itself. Verify with `grep -n`, and **never "fix" the count to 1**; that would delete the rule. `next-prompt.txt` rolled to the phase-74 IMPL.

Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`).

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. At the phase-72 close that put a stray commit on `master` in the MAIN checkout AND made a "final gates" run validate master's pre-row tree — GREEN while proving nothing. **Use `git -C <abs-worktree-path>` for every git command, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline) + §Project counts + the §Recent lineage (five bullets: phase-74 SPEC + phase-74 BRAINSTORM + phase-73 IMPL + phase-73 PLAN + phase-73 SPEC).
2. `docs/envoy-go/phases/74-tls-handshake-outcome-stats/SPEC.md` — **the source of truth for this row.** §1.1 (the drift ledger — FIVE refuted cites, EIGHT documentary findings) · §1.2 (**what was and was NOT executed — read this before citing anything as evidence**) · §3 (the D-TLSHS-* docket disposed one-for-one; §3.3 REGSCOPE, §3.4 QUIC, §3.5 the classifier + the `other` departure, §3.6 the split) · §7 (stat surface) · §8 (the `0111` asserter, its FOUR boundary sites and its mandatory guards) · §9 (the B1-B8 contract edit map) · §10 (the task spine + the seven breaks) · §11 (the edit-site roster + the BYTE-UNTOUCHED list) · §12 (the sentinel narrow, verified in scratch) · §14 (ADR-0296) · **§16 (the adversarial record — including the SPEC's reversal of its OWN QUIC decision; read it before re-deriving §3.4)**.
3. `docs/envoy-go/phases/74-tls-handshake-outcome-stats/BRAINSTORM.md` — the row's charter and the fully-adjudicated rejected roster. ⚠️ **Read it THROUGH the SPEC's §1.1 drift ledger** — five of its cites are refuted and its §10 blast-radius list is incomplete.
4. `docs/envoy-go/ROADMAP.md` — the §Schema invariant, the THREE live `candidates:` sentences (`:184`, `:194`, `:204`), and the row-74 cell (`:136`). **Row 74 is `in-progress`; every other chartered row is `done`.**
5. `docs/envoy-go/DECISIONS.md` — **ADR-0296** (PROPOSED, §Context only — the shape the IMPL appends into) + **ADR-0286** §Context `:16890` / §Consequences **C3** `:16908` (the boundary this row retires — read what it ACTUALLY says, and note the phase-67 `[CORRECTED …]` blockquote at `:16901`, which is the FORM phase 74's own correction must follow) · ADR-0045 · ADR-0106 · ADR-0117 · ADR-0288. ⚠️ **ADR-0044 does NOT contain the §Context-draft discipline the project cites it for** — use "ADR-0044-as-used".
6. `docs/envoy-go/BEHAVIOR_CONTRACT.md:916` / `:918` / `:928` / `:962` — the phase-65 three-arm narration, the phase-67 init-hold departure, the C3 coverage boundary to be retired-and-RESTATED, and the line that **already names `ssl.no_certificate`**.
7. `test/fixtures/0111-tls-cvc-empty-dynamic-fallback/` — `README.md` (esp. `:100-110` and `:162-165`), `driver/driver.go` (the self-contained PKI generator; the RD3 note `:73-81`; forced client-cert send `:520`; `BackendCount` `:293`), `envoy.yaml:24-25`, `envoy-go.yaml:70`, `expectations.yaml:109-119`/`:133-153`/`:177-184`/`:189`.
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — trust MEMORY.md's index (all 25 names cited by this lineage were verified to EXIST at the SPEC). Load-bearing here: `reference_probe_must_discriminate`, `reference_go_client_cert_withholding`, `reference_dynamic_stat_name_charset_guard`, `reference_deferred_candidate_cost_restale`, `reference_brainstorm_adjective_acquires_adr_authority`, `reference_code_comment_not_evidence`, `feedback_brief_citations_not_evidence`, `reference_quoting_is_not_executing`, `reference_differential_asserter_dispatch`, `reference_panic_counter_differential_delta_assertion`, `reference_fixture_tb_has_no_logf`, `reference_fatalf_makes_assertions_unreachable`, `reference_differential_run_selector`, `reference_differential_break_protocol_count1`, `reference_break_protocol_commit_first`, `reference_deliberate_break_wrong_assertion`, `reference_plan_break_instructions_dont_compile`, `reference_spec_drafted_identifier_collision_check`, `reference_parallel_stream_mints_fresh_drift`, `reference_cluster_race_outlier_flake`, and **`reference_bash_cwd_reset_commits_to_main`**.
