=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-VERIFIED 2026-07-16 (re-run MECHANICALLY against master `be419023` at the phase-65 PLAN — all three checks still print) — it does NOT fire. Do NOT create `stop`.** State:
- (1) prints `NOT DONE: row 65` — row 65 `xds-sds-validation-context` STAYS `in-progress` (it flips `done` only at its IMPL six-gate, ADR-0106). Check (1) BLOCKS `stop`.
- (2) prints THREE live "candidates:" sentences — HTTP/3 (`:175`), xDS (`:185`), Observability (`:193`) ⇒ three families STAY OPEN. The xDS sentence is UNCHANGED by this PLAN (it narrows at the phase-65 IMPL). **NOTE: line `:193` ALSO carries a HISTORICAL `candidates were:` recap — do not confuse it with the live sentence** (`reference_sentinel_deferred_sentence_live_vs_historical`).
- (3) prints `NEVER OPENED: gRPC`, `Runtime`, `WASM` ⇒ THREE families never opened. Operational-tooling also STAYS OPEN.

Checks (1), (2) and (3) print ⇒ the sentinel does NOT fire. Row 65 is mid-lifecycle (BRAINSTORM + SPEC + PLAN done, IMPL to go) AND there is banked deferred work AND never-opened families — abundant work remains.

**STATUS AS OF 2026-07-16: the phase-65 PLAN LANDED and ROADMAP row 65 is `in-progress`.** The PLAN (`docs/envoy-go/phases/65-xds-sds-validation-context/PLAN.md`) decomposes SPEC §10 into an 11-task TDD spine, and `PROGRESS.md` is scaffolded with the task checklist + the fill-at-IMPL evidence blocks.

**⚠️ THE PLAN CORRECTED FIVE SUBSTANTIVE SPEC DEFECTS.** Every SPEC §12 `file:line` was RE-DERIVED from source by three independent readers (`feedback_brief_citations_not_evidence`). The SPEC's line numbers are MOSTLY exact — but five claims did NOT hold, and three of them change the shipped design. **The IMPL MUST follow PLAN.md, NOT SPEC §8/§10, where they differ:**
- **C1** — `ParseSDSConfig` has **14** reject arms (`internal/xds/config.go:22-79`), not the SPEC §6's "9". It is REUSED verbatim (no build impact); the correction exists so no task tries to "mirror the 9".
- **C2 🔴 (highest-risk trap)** — the T5 arm-5 test-flip would be **VACUOUS**. `internal/tls/config_test.go:936-957` passes `&tlsv3.SdsSecretConfig{Name: "validation-secret"}` with **NO `sds_config`**; once the path routes through `xds.ParseSDSConfig`, that input fires `xds: sds: SdsSecretConfig sds_config is required` (`config.go:33`) and NEVER reaches the provider. **The input MUST be REBUILT with a full `sds_config`** (via `sdsSecretConfig`, `config_test.go:813`) or the reject→ACCEPT flip proves NOTHING. The PLAN's T5 Step 5 break #1 is the vacuity proof.
- **C3 🔴** — **`ssl.fail_verify_error` DOES NOT EXIST in envoy-go.** Grep-confirmed: ZERO `ssl.*` stats anywhere in `internal/`+`cmd/`; the ONLY listener-scope counter is `downstream_cx_total` (`internal/listener/manager.go:353`). SPEC §8 / D-SDSVC-NEGATIVE designs the negative arm around a subject-side `StatsAsserter` on that counter — it was observed on the **REFERENCE** (§11 arm 2) and parity was ASSUMED. **That assertion cannot be written.** CORRECTION: the negative arm asserts a driver-observed **NORMALIZED accept/reject CONTRAST**, CROSS-SIDE — strictly STRONGER than a subject-only stat. The missing `ssl.*` handshake-outcome family is a **NAMED COVERAGE BOUNDARY** (`reference_close_direction_framework_gap` precedent), recorded + added to the Observability deferred sentence at T11. **Do NOT add the stat inline** — that is a framework-surgery row and would blow the +0-stat envelope.
- **C4** — the two clone-parents use **INCOMPATIBLE PKI models** (`0103` COMMITS `pki/*.pem`; `0018` GENERATES at `init()` and `.gitignore`s them), and `0018`'s DTC is at `envoy-go.yaml:164-174` and is **`filename:`-based, NOT "inline"**. Also: `0103`'s driver is `driver/driver.go` (NOT `inputs/`), and `CompareBytes` is `test/differential/diff.go:19` (NOT the fixture package). Resolved by **D2**.
- **C5** — `NewQUICDownstreamConfig` is `:90-113` (the SPEC's `:87-88` is the doc comment); its `:108` call passes `side=="downstream"` **with a nil provider**, which makes §3.3a's `|| provider == nil` clause **CORRECTNESS, not defensiveness**. The inline-CVC block closes at `:246`; `loadDataSource` is `datasource.go:20`; `stream_test.go`/`provider_test.go` span `:55-179`/`:59-160` (the SPEC's `:166`/`:137` are where the LAST test BEGINS — wrong as insertion anchors); "ALL test fakes" is exactly **ONE** (`fakeProvider`, `config_test.go:796-806`; exactly two `SecretProvider` implementers repo-wide).

**PLAN-time design decisions (SPEC-delegated or C3/C4-forced) — D1-D6, argued in PLAN.md:** **D1** the `tls.v3.Secret` type URL is SHARED by both oneof arms (no new type URL; `sdsserver`'s generic derivation `:133` needs NO change) · **D2** `0108` PKI is generated **IN-MEMORY** in `ensure()`, **NO `pki/` dir** (5 artifacts; server cert injected **`inline_string`** — NOT `inline_bytes`, which protojson demands base64 for — into both yamls ⇒ no `HostMount`, no mount, no `.gitignore` upkeep) · **D3** `0108` serves ONE secret per side ⇒ `sdsserver`'s flat state STAYS flat (do NOT generalize to multi-secret — the deferred compose-two edge; it would risk the passing `0103`) · **D4** the observable is a NORMALIZED two-arm verdict in ONE byte stream (`good=ok echo=…` + `bad=rejected`; **the handshake-failure TEXT is NEVER asserted — the reference/BoringSSL sends `unknown ca`, envoy-go/Go sends `bad certificate`**) · **D5** extend `SecretProvider` (cost: exactly TWO impls) rather than fork a second interface · **D6** the `sds.<name>.*` scope keys on NAME only (no action; noted for a future compose-two row).

**Counts at phase-65 PLAN-DONE (VERIFIED on the landed tree; RE-VERIFY at session start, `git fetch` first):** fixtures **109** (`ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0107-tracing-max-path-tag-length`) · fuzzers **55** · DECISIONS tail **ADR-0285** (next-free **ADR-0286**) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** · go.mod modules **2** (`quic-go` direct + `qpack` indirect). Counts are UNCHANGED at a PLAN (docs-only).

==============================================

## What THIS session does (the phase-65 IMPL)

Perform the IMPL stage (lifecycle-state 3 → done) for phase 65 (`xds-sds-validation-context`). Execute `docs/envoy-go/phases/65-xds-sds-validation-context/PLAN.md` **task-by-task, T1 → T11**, subagent-driven (`feedback_execution_style`) — each task a FRESH subagent that commits LOCALLY only (`feedback_subagents_no_push`); the controller verifies each commit, cleans leak files, squashes at stage-close, re-runs the suite on the frozen HEAD, and pushes (`feedback_push_to_origin`). Run in a FRESH worktree off master, branch `phase-65-xds-sds-validation-context-impl`, worktree `.worktrees/phase-65-impl`, per `feedback_git_worktrees`.

**Row 65 flips `in-progress` → `done` at this IMPL's six-gate** (ADR-0106, the SOLE leg — `reference_roadmap_split_phase_row_done`). **ANCHORS ADR-0286** (§Decision/§Consequences land in `DECISIONS.md`; §Context was drafted at the SPEC, SPEC §13, per ADR-0044).

**The 11-task spine (PLAN.md is authoritative — it carries the full test code, the exact patches, and the per-task liveness breaks):**
- **T1** `internal/xds/secret.go` — `parseValidationSecret` + the CVC-reject roster + the FIRST production `crypto/x509` import in `internal/xds`.
- **T2** `internal/xds/stream.go` — `fetchValidationSecret` + `applyValidationResponse` (byte-parallel to `:38-95`, SHARING `errValidation` + `secretTypeURL()`).
- **T3** `internal/xds/provider.go` — `FetchInitialValidationContext` + the `SecretProvider` interface method **AND** `internal/tls`'s `fakeProvider` impl — **ONE COMMIT** (the interface change breaks `internal/tls`'s build otherwise).
- **T4** `internal/tls/config.go` — the `:227-228` reject → a no-op gated `side != "downstream" || provider == nil`.
- **T5** `internal/tls/config.go` — the `ClientCAs` apply-point in `NewDownstreamConfig` (`:67-79`) **+ the C2-corrected arm-5 rebuild**.
- **T6** `internal/boot/boot.go` — the pre-scan (`:138`) detects the validation arm.
- **T7** `test/helpers/sdsserver` — `WithValidationContext` + the `buildResponse` branch.
- **T8** fixture `0108-xds-sds-validation-context` (+ register at `runner_test.go` after `:134`).
- **T9** fuzz SEEDS (count STAYS **55**).
- **T10** `BEHAVIOR_CONTRACT.md` **`:881`** — REJECT → CONSUMED.
- **T11** verify (six-gate + the cycle guard + the full **110**-dir differential) + ADR-0286 + STATE + ROADMAP row 65 `done` + the sentinel narrow at **`:185`** + PROGRESS close + router roll.

**⚠️ LOAD-BEARING ORDERING (PLAN.md §File Structure).** T1/T2/T3 are ADDITIVE — the build stays green after each because the new symbols have no caller, and the landed `*stdtls.Certificate` chain is never touched (the `0103` differential cannot regress). The ONE exception: **T3's interface change breaks `internal/tls`'s build the moment it lands** — so T3 MUST include the `fakeProvider` method in the SAME commit. T4+T5 wire the chain into `internal/tls`. **T6 is what makes a validation-only-SDS listener actually BUILD a provider** — without it, `NewSDSProvider` returns `(nil, nil)` → `seen==0` → `NewDownstreamConfig` gets `nil` → T5's nil-provider reject fires. **So T8's fixture depends on T5 AND T6 both landing**; running T8 before T6 fails with `requires a live SDS provider` and will LOOK like a fixture bug.

**⚠️ THE RESIDUAL RISK (the single most likely place phase 65 ships a vacuous test).** T8's liveness breaks (1) "serve a DIFFERENT CA over SDS" and (2) "sign `client_bad` with the SERVED CA" are **SYMMETRIC** — they change BOTH sides identically, so a pure `CompareBytes` fixture still compares EQUAL and PASSES (`reference_vacuous_break_receiver_normalizes`). Without an additional **in-driver STRUCTURAL check** (the subject's own bytes match `good=ok` + `bad=rejected`), `0108` proves only "both sides agree", NOT "the SDS-served CA is the actual trust anchor" — the entire point of the row. **RESOLVE THIS and record the chosen mechanism in PROGRESS.** If breaks (1)/(2) cannot be made to fail, the fixture is vacuous and the row is NOT done.

**Honor throughout:**
- **Lifecycle = one stage per session.** This session: the IMPL ONLY. After it lands: the sentinel does NOT fire (checks (2)+(3) still print) ⇒ the roller SELF-PICKS the phase-66 subject per the standing directive and rolls to a phase-66 BRAINSTORM.
- **The 60.2 cycle guard STANDS** (`reference_xds_config_seam_transitive_cycle_guard`) — `internal/xds` must NOT import `internal/tls`; the CA-pool build is DUPLICATED in `internal/xds`. VERIFIED at this PLAN: `go list -deps ./internal/xds` (NO `...`) → `internal/stats` + `internal/xds` ONLY. T11 re-verifies; `internal/tls` MUST NOT appear.
- **The `"tls: "` fuzz invariant** — `FuzzTLSContextParse` (`internal/tls/fuzz_test.go:78-96`) asserts EVERY error begins `tls: `. `ParseSDSConfig`/`FetchInitialValidationContext` return `xds: `-prefixed errors ⇒ T5 MUST wrap them (`"tls: downstream: %w"`, the landed cert path's precedent at `config.go:213`).
- **Collision-check STANDS** — all five new names (`parseValidationSecret`/`applyValidationResponse`/`fetchValidationSecret`/`FetchInitialValidationContext`/`WithValidationContext`) RE-GREPPED FREE at this PLAN; zero hits in ANY `.go` file (`reference_spec_drafted_identifier_collision_check`).
- **Process anchors:** ADR-0044 (ADR §Decision/§Consequences at the IMPL) · ADR-0045 (single-flat-row; escape-valve armable-UNCONSUMED) · ADR-0106 (row flips `done` at the IMPL six-gate) · ADR-0080 (envoy-go-strict distinct-substring rejects) · ADR-0280 (the boot-FAIL-on-fetch-timeout DEPARTURE, extended unchanged) · `reference_roadmap_split_phase_row_done` · `feedback_brief_citations_not_evidence`.
- **Differential-harness traps** (each cited at its task in PLAN.md): `reference_differential_run_selector` (FULL `-run 'TestDifferential/0108-xds-sds-validation-context'`, NEVER bare) · `reference_differential_break_protocol_count1` · `reference_deliberate_break_wrong_assertion` · `reference_vacuous_break_receiver_normalizes` (the residual risk above) · `reference_fatalf_makes_assertions_unreachable` · `reference_differential_backendcount_min_one` · `reference_differential_grpc_receiver_driver_owned` (BackendKind STAYS 38) · `reference_periodic_sink_differential_two_receivers` · `reference_full_suite_race_after_background_mutator` · `reference_differential_fullsuite_startup_flake` · `reference_fuzzer_count_docs_drift` (55 before AND after).
- **`next-prompt.txt` IS TRACKED** (`reference_next_prompt_tracked_despite_gitignore`) — edit it inside the stage worktree and fold into the stage squash; locate commits by SUBJECT (`git log --grep`), never by position.
- **Worktree discipline** (`feedback_git_worktrees`, `feedback_subagent_worktree_detach`, `feedback_subagent_worktree_path_targeting`) — pin the canonical worktree root; the controller verifies the MAIN checkout stays clean; on a deliberate break restore with `git restore` only.

**ADR reservation ledger (provisional; each finalized at its IMPL in landing order):** ADR-0283 = phase-62 tracing-custom-tags-request-header (LANDED) · ADR-0284 = phase-63 tracing-custom-tags-environment (LANDED) · ADR-0285 = phase-64 tracing-max-path-tag-length (LANDED) · **ADR-0286 = phase-65 xds-sds-validation-context** (§Context DRAFTED at the SPEC, SPEC §13; **§Decision/§Consequences land at THIS IMPL** per ADR-0044 — and MUST record the three PLAN-time corrections that changed the shipped design: C3 the `ssl.*` gap, C2 the arm-5 vacuity trap, D2 the in-memory PKI; DECISIONS tail flips **ADR-0285 → ADR-0286**) · **next-free unreserved: ADR-0287**.

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-65 IMPL is next).
2. `docs/envoy-go/phases/65-xds-sds-validation-context/PLAN.md` — **the PRIMARY input and the AUTHORITY**: the 11-task TDD spine with full test code, exact patches, per-task liveness breaks, the §"PLAN-time corrections to SPEC-65" block (C1-C6), the §"PLAN-time design decisions" block (D1-D6), the File Structure + build-ordering call-outs, and the self-review. **Where PLAN.md and SPEC §8/§10 differ, PLAN.md WINS** (it re-derived the sites; the SPEC did not).
3. `docs/envoy-go/phases/65-xds-sds-validation-context/PROGRESS.md` — the task checklist + the fill-at-IMPL baseline / liveness-break / verify-evidence blocks + the residual-risk note. Fill it as you go; close it at T11.
4. `docs/envoy-go/STATE.md` — the active-phase header (`phase 65 PLAN done`; NEXT = the phase-65 IMPL) + the prior-phase lineage.
5. `docs/envoy-go/phases/65-xds-sds-validation-context/SPEC.md` — the design authority for §3 (the reject-lift + parallel applier + apply-point + boot pre-scan), §6 (the reject roster), §7 (+0 stats), §11 (the live-probe dispositions), §13 (the ADR-0286 §Context draft). **§8 (fixture) and §12 (edit-site roster) are SUPERSEDED where PLAN.md's C1-C5 corrections apply.**
6. `docs/envoy-go/phases/64-tracing-max-path-tag-length/PROGRESS.md` — the FORMAT precedent for an IMPL's PROGRESS close (the liveness-break log + the verify-evidence block + the landed-commit ledger).
7. `docs/envoy-go/ROADMAP.md` — the xDS family section (`:185` the live deferred sentence to narrow at T11) + the §Schema invariant (row 65 is `in-progress`) + the phase-60 SDS precedents (rows 60/60.1/60.2).
8. `docs/envoy-go/DECISIONS.md` — ADR-0278 (SDS substrate) + ADR-0280 (SDS server-cert wiring + the boot-FAIL departure) + ADR-0147 (static-mTLS `ClientCAs`) + the ADR-0044/0045/0106/0080 process anchors.
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the differential-harness `reference_*` traps + `reference_xds_config_seam_transitive_cycle_guard`.
