=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

**THERE IS NO BANKED MID-LIFECYCLE WORK AT THIS TIP.** Phase 80 (`stats-sds-projection`) **CLOSED at the IMPL**; row 80 is `done`. **Every chartered ROADMAP row is now `done`, and check (1) prints NOTHING for the first time in this lineage.** ⚠️ **The sentinel STILL DOES NOT FIRE — checks (2) and (3) both print.** ⇒ **This session SELF-PICKS a new subject and performs its BRAINSTORM.** See §THE PICK below, which costs the live candidates so you do not have to re-derive them cold.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=112 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **`want=112` IS A LIVE FIGURE, NOT A CONSTANT.** It is the ROADMAP data-row count. **A row-adding phase MUST bump it in the same commit that adds the row** — otherwise every later session gets `GATE FAIL: examined 113 data rows, expected 112`. That noise is deliberate: a wrong denominator must be loud, because a silent one is how check (1) failed in the first place. **The phase-80 BRAINSTORM bumped 111 -> 112 when it chartered row 80; the SPEC, the PLAN and the IMPL each left it at 112.** ⚠️ **YOUR BRAINSTORM CHARTERS A NEW ROW, SO IT MUST BUMP `want` 112 -> 113 IN THE SAME COMMIT.**

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. When you open a new family, add its row-summary slug here.

**SENTINEL RE-RUN MECHANICALLY AFTER THE PHASE-80 ROADMAP FLIP, with FIRING negative controls. IT DOES NOT FIRE. `stop` was NOT created; `ls stop` => `No such file or directory`, and it MUST NOT be created.** ⚠️ **Re-run it yourself anyway and record what YOU see** — a prior run's result is not evidence for the current tip. ACTUAL output, recorded rather than predicted:
- **(1) prints NOTHING** at `want=112` — ⚠️ **A FIRST FOR THIS LINEAGE: every chartered row is `done`.** NCs, both fired: `want=111` => `GATE FAIL: examined 112 data rows, expected 111`; doctoring row **76** to `in-progress` on a scratch copy => `NC NOT DONE: row 76` (the script self-reported `rows doctored: 1`). ⚠️ **Because (1) is now silent, a doctored-copy NC is the ONLY way to prove it still discriminates. RUN ONE.**
- **(2) FIVE — `:190 :200 :210 :216 :224`** (long form at the first four, the Operational-tooling short form at the last) — **UNCHANGED**. **The twenty-seventh consecutive phase at which check (2) did not go down. STATED, not forecast.**
- **(3) `NEVER OPENED: gRPC` and `NEVER OPENED: WASM`** — unchanged. NCs: an invented slug => `NC NEVER OPENED: ZZZ-nonexistent`, while the REGISTERED slug `Observability` correctly printed nothing.
- Input measured at **228 lines / 112 data rows / 13** bare `candidates:` hits (against the sentinel's own narrower 5), so an empty result could not read as a zero result.

⚠️ **THE LEAK CHECK IS LIVE AT ANY STAGE THAT WRITES A ROADMAP CELL — AND YOUR BRAINSTORM WRITES A NEW ROW.** The rule: **NEVER WRITE A SENTINEL'S OWN MATCHER STRING INTO A FILE THE SENTINEL GREPS.** The sentinel greps `docs/envoy-go/ROADMAP.md` — writing these strings *here* is safe; writing them *there* is not. The leak is proven to **SILENCE CHECK (3) BY MENTION**, not merely to be greppable, and it was **REPRODUCED LIVE AT THE PHASE-80 IMPL**: doctoring row 80's cell to say `gRPC-family row` made `gRPC` **stop printing** while `WASM` kept printing, and check (2) went **5 -> 6**. **`grep` cannot tell a mention from a use.**
```sh
# the row's cell only, not the whole file  (substitute YOUR new row id for 81)
awk -F'|' '/^\| *81 /' docs/envoy-go/ROADMAP.md > "$SCRATCH/row81.txt"
grep -coE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' "$SCRATCH/row81.txt"  # want 0
grep -oiE '(HTTP-filters|Network-filters|Load-balancing|Upstream-robustness|Observability|Operational-tooling|HTTP/3|gRPC|xDS|Runtime|WASM)-family row' "$SCRATCH/row81.txt" | sort -u
# ^ every slug printed MUST already be registered elsewhere in ROADMAP.md. NC both greps on a doctored copy,
#   AND re-run check (3) on the doctored file to prove the slug would actually silence it.
```
⚠️ **THE NEW ROW'S CELL MUST ALSO BE WELL-FORMED, AND THE GATE MUST BE A DISJUNCTION.** Rows 57, 69 and 78 all lose summary content in GFM render, and **row 78's `NF==8` PASSES because an unescaped inner `|` and a missing trailing `|` CANCEL** (broken-gate shape sixteen). **EXECUTED AT THE PHASE-80 IMPL over all 112 rows: ARM-A (escape-aware `NF!=8`) catches 57/69 ONLY; ARM-B (escape-aware trailing-piece) catches 78 ONLY. Neither arm alone suffices.** The compensating-defect NC (inject an inner pipe + strip the trailing one) makes naive `NF==8` **pass a malformed row**, exactly as on real row 78. **Escape inner pipes; terminate the row with `|`; gate with BOTH arms.**

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt` — RE-CONFIRMED LIVE AT THE IMPL

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files --hidden -I --exclude-dir=.git …` in ugrep-compatible mode. **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. The delta is exactly this file, reproduced at three separate tips:
```sh
grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort         # wrapped: ABSENT
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                    # direct access WORKS
```
⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING THE TWO ARMS.** The wrapped arm prints bare paths while GNU prints `./`-prefixed ones, so a naive `comm` reports **every file as "only in GNU"** and the real delta is invisible. **That happened at the PLAN** and was caught by re-running with `sed 's|^\./||'`. ⚠️ **Print the FILE NAMES, never a count.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** — it reports `next-prompt.txt` as *"not ignored"* (exit 1) because **git exempts TRACKED files from `.gitignore`** while ugrep does not. **Reliable forms: `command grep -r`, `git grep`, an explicit file list, or a recursive grep handed the FILE path directly.**

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`.** Every document from phase 75 forward recorded its blind spot — **109 data rows / 105 matched / FOUR misses** at an **em-dash** in an "after" cell, a **DOT** in the slug `http-1.1`, and two **LETTER-suffixed** ids `28.1a`/`28.1b` — and every one of them called it *"currently HARMLESS, since all four missed rows are `done`."* ⚠️ **Nobody fired the unsafe arm until the phase-77 PLAN. EXECUTED on scratch copies: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** ⚠️ **DO NOT restore the regex form.** ⚠️ **AND THIS MATTERS MORE NOW THAN EVER: check (1) is SILENT at this tip, so a fail-unsafe form would be indistinguishable from the correct one. Only a doctored-copy NC discriminates.**

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

The old check (2) matched ONLY the long phrase and could not see the **Operational-tooling** family's three genuinely un-chartered items, recorded in the **SHORT** form. Fixed at the phase-77 BRAINSTORM, at the MATCHER. ⚠️ **Run the union form above, never one half of it.** ⚠️ **A ONE-ARM STRIP IS NOT AN NC FOR CHECK (2)**: `sed 's/deferred candidates://'` moves the union count **5 -> 4, not 5 -> 0**, because the long form does not contain the short substring at all. **Re-confirmed by direct test: `printf 'remaining deferred (not-yet-chartered) candidates:\n' | grep -c 'deferred candidates:'` => 0.**

⚠️ **THE DOCUMENTED TRAP'S STATED MECHANISM IS REFUTED, WHILE THE TRAP IS REAL.** `reference_sentinel_deferred_sentence_live_vs_historical` says a naive `grep -c 'candidates:'` inflates *because of* the `candidates were:` recaps. It does inflate — but **not for that reason**: `candidates were:` does not contain that substring **at all**. The inflation comes from **ROADMAP table rows carrying BRAINSTORM prose**. **Cite the command; do not re-inherit the explanation.**

⚠️ **AND THE MEASURED RECORD: CHECK (2) HAS NEVER GONE DOWN** across ~27 phases (history `0 -> 1 -> 3 -> 4 -> 5`). => **the candidates sentence is a WINDOW onto a larger deferred backlog, not an inventory of it.** ⚠️ **A stage that predicts a decrease here is repeating the phase-73 error. State what your row narrows — usually nothing — rather than forecasting.**

⚠️ **THE MENU IS NOT COSTED — MEASURED.** `grep -obE '[0-9]+(-[0-9]+)? tasks'` returns **no match** on FOUR of the five paragraphs: HTTP/3 (7 uncosted candidates), xDS (9), Runtime (4) and Operational-tooling (3) carry **zero** task bands. **Only the Observability line carries costs.** ⚠️ **A deferred candidate's COST goes stale** — re-derive at the current tip, never inherit.

⚠️ **THE SELECTION CALCULUS, CORRECTED AND STILL STANDING.** *"A candidate that clears a check-(3) blocker is worth **strictly more**"* does **not** hold: opening a family **MINTS** a check-(2) sentence, so phase 77's own arithmetic was **NET ZERO**. What survives: **opening a family is the ONLY way that family's work becomes enumerable at all — a prerequisite, not a shortcut.**

==============================================


## STATUS AS OF 2026-07-30: PHASE 80 IS **CLOSED**. ROW 80 IS `done`. EVERY CHARTERED ROW IS `done`. THIS SESSION **SELF-PICKS A NEW SUBJECT** AND PERFORMS ITS **BRAINSTORM**.

**What the phase-80 IMPL landed.** **Eleven task commits squashed**, base master **`28aa342d`**. **Realized: 1979 insertions / 114 deletions over 20 files; `.go` only = 1701 / 65 = 1636 net.** Production `.go`: `internal/stats/name.go` (the `sds.` SN1 arm, the `12 -> 13` constant, both doc blocks, `helpText` 25 -> 30), `internal/boot/boot.go` (the boot-boundary reject, BEFORE the dial), `internal/admin/stats.go`. Test `.go`: `name_test.go`, `helptext_test.go`, `promskip_test.go`, `golden_bytemirror_test.go`, plus two NEW `internal/boot` test files and `internal/xds/stats_test.go`. Fixtures: `0110` **and `0111`** drivers + their `expectations.yaml`/`README.md` prose reversals, and `0118`'s comment-only roster update. Docs: `ROADMAP.md` (row 80 -> `done`), `BEHAVIOR_CONTRACT.md`, `DECISIONS.md` (ADR-0302 §Decision + §Consequences + STATUS flip, the ADR-0299 rider, the ¶11 figure correction), `STATE.md`, **`STATE_HISTORY.md` 428 -> 430**, `PROGRESS.md`, this file.

**ALL GATES GREEN.** Full 120-fixture differential **120/120 in 402 s** (`comm -3` EMPTY, FAIL/SKIP EMPTY, `no driver registered` 0) · h2spec **53/53** run explicitly · `go test ./internal/...` 0 FAIL · `-race` **0 DATA RACE** · `go vet` 0 bytes · `gofmt -l` output EMPTY over **eight** packages · `golangci-lint` own exit 0 / 0 bytes · `go.mod`/`go.sum` **0 bytes** · **stat-surface delta +0 PROVEN BY CALL-SITE ENUMERATION** (production registration set = ∅). **Zero known flakes fired**, including `reference_sds_init_fetch_timeout_dial_budget_flake`, which is live for that row's subject.

### ⚠️ THE PICK — SELF-PICK, AND THE CANDIDATES ARE ALREADY COSTED

**Every chartered row is `done`, so there is no "advance the banked work" option. You MUST open a new row.** ⚠️ **Record the pick AND the rejected alternatives in the BRAINSTORM**, per the standing directive. **Smallest defensible candidate first.** The live menu, with what is KNOWN about each — ⚠️ **re-derive every cost at YOUR tip; a deferred candidate's cost goes stale**:

- **⚠️ THE TWO CHECK-(3) BLOCKERS — `gRPC` and `WASM` — are the only work that makes a family enumerable at all.** ⚠️ **`gRPC` is HARD-BLOCKED, evidence-backed and re-confirmed:** `\.RunEncodeTrailers(` => **0** non-test / 1 test; `\.RunDecodeTrailers(` => **0** non-test / 1 test; both declared, **neither production-reachable**. NC: `\.RunEncodeHeaders(` non-test => **4**. **The old 16-22+ band is NOT re-derivable — flagged, not inherited.** ⚠️ **`WASM`'s row-summary rider stays DECLINED ON THE MERITS** — `ROADMAP.md:76` (row 25.3) says verbatim *"phase 25 is the FINAL §9 HTTP-filters-family row"*, **and writing the marker would silence check (3) BY MENTION** (proven by NC). **Opening WASM for real is a genuine work row, not a one-line marker.**
- **The general dynamic-token charset exposure across six families.** Phase 80 brought `sds.` into line but **did NOT audit whether the five existing rejects are complete**, nor whether wire-derived segments (mongo collection names, wasm plugin names) are reachable with an invalid name. **A direct, well-scoped successor to row 80** — and the one whose prior art is freshest.
- **The `STATE_HISTORY.md` archive gap** — 36 bullets for phases 67-75, 58 overall; **`phase 77 PLAN done` is in NEITHER file** (re-confirmed at the IMPL with a firing NC; the archive demonstrably carries PLAN entries for 78, 63, 62, 60.1, 56.1, 46.2, 42.2b, 42.2a, 42.1). **4-6 tasks, defensible ONLY if it lands the `comm -13` set-difference as a recurrence guard.**
- **`ROADMAP.md`'s malformed rows 57/69/78** — invariant-blocked by §Schema, so **defensible as a GUARD, not a fix**, and the gate must be the **DISJUNCTION proven at the phase-80 IMPL** (ARM-A catches 57/69 only, ARM-B catches 78 only).
- **The differential bind RETRY gap** — ⚠️ **the ALLOCATOR half LANDED at `f2dd994a`** (band `11000..14999` + wildcard probe + a pinning test with a firing NC), and ⚠️ **the old "8-12 tasks" figure and the "close-then-rebind" characterization are STALE — do not inherit either.** What REMAINS: a window persists between the probe's `Close` and the child's bind, and the child is `go run`, so the gap spans a **BUILD**. **26 of the 29 `freeTCPPort(t)` call sites in `runner_test.go` are backend-startup arms with NO retry loop**; `harness_test.go` holds 4 more. The fix is a `startSubjectWithRetry` equivalent. ⚠️ **NOT verifiable by a single green suite run** — a prior instance needed `-count=6`.
- **Full hyphen fidelity** — needs `NamePattern` relaxation (a `checkName`-panics invariant, repo-wide) or a raw-bytes seam through the `Registry`/`ExtractTags` seam. **The reference serves `envoy_sds_update_success{envoy_xds_resource_name="server-cert"}` with the hyphen `od`-verified verbatim while envoy-go boot-FAILS** — a DOCUMENTED envoy-go-strict departure, not a bug.
- **`--mode validate` cannot validate ANY SDS bootstrap** — `validate.Bootstrap` passes a nil provider, so `boot.NewSDSProvider` is never reached.
- **A mechanical stat-surface recount** — **8-11 tasks, defensible only if reframed as an EXECUTABLE enumerator** (dump `Registry` keys after a boot). A grep-based recount covers ~0.8 % of the surface and **stays rejected**.
- **The PUBLIC IMPORT PATH defect — NOT DEFENSIBLE AS A PHASE: there is no green.** `DECISIONS.md:142` is an immutable ADR that must *name* the wrong path; 7 hits sit inside CLOSED rows' summary cells that §Schema forbids editing; the root `PROGRESS.md` hits are pasted `go test` output; **any `count==0` guard is unsatisfiable by construction.** `go build ./...` exits 0 under the real path; **0** occurrences in compiled Go.
- **Normalizing the Operational-tooling short-form deferred-candidate paragraph** to the long form (cosmetic; **note it MINTS nothing and NARROWS nothing**).

### What matters for the NEXT session, in order

1. **⚠️ RE-RUN ALL THREE SENTINEL CHECKS YOURSELF AND RECORD THE ACTUAL OUTPUT; DO NOT PREDICT IT.** Create `stop` if and ONLY if all three print nothing. **Checks (2) and (3) print, so it cannot fire.** ⚠️ **Check (1) is now SILENT — so it is indistinguishable from a broken check. A DOCTORED-COPY NC IS MANDATORY, not optional.** ⚠️ **Your BRAINSTORM charters a row, so it MUST bump `want` 112 -> 113 in the SAME commit.**

2. **⚠️ THE BRAINSTORM IS DOCS-ONLY.** ZERO production `.go`, ZERO test `.go`. `BEHAVIOR_CONTRACT.md` and `DECISIONS.md` stay **BYTE-UNTOUCHED** (a BRAINSTORM authors no ADR; ADR-0044-as-used means the §Context block lands at the SPEC and §Decision/§Consequences at the IMPL). **`ROADMAP.md` IS touched** — the new row, `in-progress`, plus the `want` bump.

3. **⚠️ EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION, NOT BY REVIEW.** The lineage record is unbroken: the phase-80 SPEC refuted nine BRAINSTORM claims; the PLAN refuted **nineteen** SPEC claims and five of its own probes; **the IMPL refuted SIXTEEN PLAN claims**, four of them load-bearing. **Budget for finding more. A BRAINSTORM that refutes nothing has not looked.**

4. **⚠️ THE PHASE-80 IMPL'S FOUR LOAD-BEARING REFUTATIONS — the shapes most likely to recur.**
   - **A SECOND CROSS-SIDE DEPARTURE, NAMED BY NO DESIGN DOCUMENT, found only by mirroring a fixture the PLAN priced as optional.** `0111` serves an EMPTY `validation_context`: **the reference ACKs it and books `update_success`; envoy-go REJECTS it and books `update_rejected`** — the two sides book the same event under **opposite counter names**. ⇒ copying `0110`'s floor set into `0111` is **RED ON ARRIVAL**. **The "optional" leg was load-bearing.**
   - **A PLAN's code block does not compile, and following it literally would have SHIPPED the anti-pattern the same PLAN forbids.** Its G6 rows used a `wantErr` field the target table does not have, and the target loop used `t.Fatalf` — so appending under it would have made later assertions dead code. `reference_plan_break_instructions_dont_compile`, fired on a *task* block rather than a break block.
   - **A DRIFT CORRECTION IS ITSELF A CLAIM, AND THIS ONE WAS WRONG.** PLAN §1.12's *"it is never 45"* is **FALSE** — the figure IS 45 at the PLAN's own squash tip, because the PLAN's own commit moved it 43 -> 45. **A stage that "corrects" a figure must measure at the tip the figure was written at.**
   - **A HAND-WRITTEN GOLDEN MOVES IN LOCKSTEP WITH THE THING IT GUARDS.** Break arm γ's predicted third test does **not** fire, because the byte-stability golden was bumped alongside the constant. **What actually caught the defect is the AST-deriving guard that hard-codes nothing.** `reference_handwritten_golden_shares_author_mistake`, firing on the very guard whose file header describes the hazard.

5. **⚠️ BROKEN-GATE COUNT IS EIGHTEEN — no nineteenth shape was found at the phase-80 IMPL, but TWO priors fired LIVE and one SURVIVED ITS OWN REMEDY.**
   - ⚠️ **SHAPE SEVENTEEN SURVIVES ITS FIX.** A golden roster omitting the family under test was reproduced live (roster un-extended => **1134 bytes in BOTH worlds**, arm present or deleted, while the `1134 -> 1135` NC fires). **But with the roster FULLY EXTENDED, the `(F,F)` cell STILL passed under an arm-deletion break** while the other three failed. ⇒ **extending the roster is NECESSARY BUT NOT SUFFICIENT; a `(F,F)`-only cell restriction remains fatal.**
   - ⚠️ **`gofmt -l` PRINTING WHILE EXITING 0 was observed directly in an NC.** **Gate on OUTPUT.**
   - ⚠️ **`git grep -c … => 0` IS A BROKEN GATE AS USUALLY WRITTEN.** With zero matching lines it **prints NOTHING and exits 1** — it never prints `0`. And `-c` counts **LINES, not occurrences**: it read **3** against **4** live `79.1` occurrences, so it **structurally cannot distinguish "3 of 4 repaired" from "4 of 4."** Use `grep -o … | wc -l` plus per-phrase probes.
   - The eighteen carried forward, unchanged: two defects that CANCEL in the gate metric (`NF==8` passes row 78) · an inert gate cell · a full-suite recipe without `-v` is VACUOUS · a sha256 roster desynced against a DELETED file · `gofmt -l` NEVER exits non-zero · `go doc -all <A> <B>` swallows arg2 · a `+0 exported symbols` gate over an EMPTY package reds on a CORRECT tree · a RANGE gate cannot detect anchor drift · a roster's naive `[ -f ] || continue` exits 0 on a DELETED file · a count-only stat guard PASSES a build with BOTH names wrong · a `-run` no-match exits 0 with `[no tests to run]` · a `--- PASS` tally over a package with sibling tests exceeds the fixture denominator · a stat-delta claim cannot be discharged by guards scoped to another package · a stderr-VOLUME assertion passes on the hang · `golangci-lint` runs `misspell` with `locale: US` · a harness's exit code is not the command's · a GOLDEN ROSTER that omits the family under test · **a NEGATIVE CONTROL POINTED AT A TARGET THAT DOES NOT EXIST** (shape eighteen — an NC must assert its own target EXISTS and its command EXITED ZERO before its empty output is read as a result).

6. **⚠️ LINE ANCHORS DRIFT MID-ROW; PHRASE ANCHORS DID NOT.** Measured at the phase-80 IMPL: `golden_bytemirror_test.go:354` is a **closing brace** · `name.go:490` became **`:517`** once an earlier task landed above it · the SN9-collision cite `:501` became **`:555`**, **and `:501` at the new tip is a real `wasm.` line, so a line-anchored reader lands on a PLAUSIBLE-LOOKING WRONG TARGET** · `STATE.md:48` is `:50` · `next-prompt.txt:150` was a **phantom cite** (eight `twelve` in the file, none on `:150`). **EVERY phrase anchor held. Prefer symbol/phrase anchors to any offset, and never a `+N` offset — a `+54` shift landed six lines short in a document's tail because a second hunk added six more.**

7. **⚠️ THE FULL 120-FIXTURE DIFFERENTIAL IS MANDATORY for any row touching `internal/stats`** — it links into `cmd/envoy-go` at `test/differential/harness.go:240`, `:594` and `test/conformance/h2spec/h2spec_test.go:210`; **h2spec is a FOURTH consumer NOT covered by `./test/differential/`** — run it **explicitly**. Budget **~400-430 s** per green attempt (measured **402 s** at the phase-80 IMPL, **403.2 s** at `f2dd994a`, **427.8 s** in CI); `-race` is a SECOND run. ⚠️ **CI runs this job at `-timeout 20m` with job `timeout-minutes: 30`** — if your local run approaches either, that is a finding, not a budget to raise reflexively. The recipe, with `comm -3` as the load-bearing gate (120 with one fixture renamed and another skipped still reads 120):
    ```sh
    ( go test ./test/differential/ -count=1 -v > "$SCRATCH/full.log" 2>&1; echo "INNER_EXIT=$?" )
    grep -cE '^    --- PASS: TestDifferential/' "$SCRATCH/full.log"          # want 120
    grep -E  '^    --- (FAIL|SKIP): TestDifferential/' "$SCRATCH/full.log"   # want EMPTY
    grep -c  'no driver registered for fixture' "$SCRATCH/full.log"          # want 0
    grep -o  'TestDifferential/[^ ]*' "$SCRATCH/full.log" | sed 's|TestDifferential/||' | sort -u \
      | comm -3 - <(ls -1 test/fixtures/ | grep -E '^[0-9]{4}[a-z]?-' | sort)  # want EMPTY
    ```
    ⚠️ The faithful dir predicate is `^[0-9]{4}[a-z]?-` — a bare `^[0-9]{4}-` gives **118**.
    ⚠️ **`go test ./test/differential/...` (with `...`) matches TWO packages, so `go test` BUFFERS each package's `-v` output until that package finishes.** A run that looks hung for 7 minutes with an empty log is normal, not a stall — do not kill it and do not conclude "no output" means "no progress".
    ⚠️ **Stat surface: assert the DELTA.** ⚠️ **The `TestNoNewStat*` guards are PROVEN BLIND to `internal/stats`** (all five live in `internal/statssink/registration_test.go` and none reaches it): **enumerate the diff's registration call sites and show the set is EMPTY**, with a non-vacuity denominator (the same matcher tree-wide = **508 call sites / 84 files**). Adding a `switch` arm is a **PROJECTION** change, not a **REGISTRATION** change.

8. **⚠️ COST AND SPLIT — AND THE PHASE-80 IMPL PRODUCED THE LINEAGE'S FIRST REALIZED ~1500-LoC CROSSING.** PLAN §4 estimated **~640 net `.go`**; realized **1636 net** — **over by 136**, with **EVERY bucket over and the errors COMPOUNDING** (production 1.6×, test 2.0×, **fixture 4.1×**). ⚠️ **The split did NOT retroactively fire, and that is correct**: `/BOOTSTRAP_PROMPT.md` §6.1 gates *"`PLAN.md` **estimates** exceed ~1500 lines … of net change"* — **the number the PLAN writes down**, not the realized figure. But **1636 is the first crossing** (comparables 1406, 1446; phase 79 realized 1532). ⚠️ **THE THIRD §6.1 TRIGGER (mid-execution, >~10 sub-steps) DID FIRE, on the golden task: 6 enumerated vs 17 executed (~1.7× the threshold).** It stayed atomic and was right not to split — **but RECORD such a firing, never absorb it.** ⚠️ **Calibration says read a budget as "expect the ceiling"**: 76 ~7-9 -> 9 · 77 11-13 -> 12 · 78 7-9 -> 10 · 79 10-12 -> 12 · **80 11-14 -> 13**. ⚠️ **Normative cites, verified EXACT: `### 6.1 When to split` at `:285` of `/BOOTSTRAP_PROMPT.md` — AT THE REPO ROOT, not under `docs/envoy-go/`** (`:289` the ~25-task trigger, `:290` the ~1500-LoC trigger, **`:291` BLANK**, `:292` the third trigger); six-gate **§7.5 heading `:357`, gates (a)-(f) `:360-365`, close `:367`**. **`ADR-0045` QUOTES rather than states; `ADR-0106` defines nothing about phase-done gates.**

9. **⚠️ THE SIX-GATE HAS A STANDING LINEAGE DEPARTURE — NAME IT, DO NOT CLAIM COMPLIANCE.** Gate **(f)** expects a `REVIEW.md`; **phase 80 has none, and NO phase has had one since 25.3 — 84 of 121 phase dirs carry none.** Record it as a departure. Gate **(d)** (fuzzers) is **VACUOUS for a row that adds none** — **say "vacuous" rather than "green"**, and NC the matcher (55 `^func Fuzz` repo-wide; the phase-80 IMPL's NC was 11 `+func Test` added). Gate **(c)**: proxy-wasm is **INHERITED, not re-run** — say so.

10. **⚠️ ADR + DOCUMENT CONVENTIONS.** Next-free is **ADR-0303**; tail is **ADR-0302**, STATUS now **COMPLETE**. **`grep -c '^## ADR-0303'` => 0** (NC: `^## ADR-0302` => 1). **301** `^## ADR-` headings, ids **0001-0302**, **exactly one gap at ADR-0209**, zero duplicates — *"301 ADRs"* and *"tail ADR-0302"* are both true and must not be conflated. **STATUS census is now 15 COMPLETE / 0 PROPOSED** — ⚠️ **the ADR-0299 rider LANDED at the phase-80 IMPL, so that stale `PROPOSED` is GONE; do not re-report it as an open defect.** The guard *"a `PROPOSED` STATUS must carry no §Decision"* was run over all 301 blocks, named ADR-0299 and no other pre-edit, and is silent post-edit, NC'd both ways. ⚠️ **The retained italic footer convention: the footer-carrying block is `ADR-0294 … ADR-0300` — SEVEN** (per-block scan re-derived at the IMPL: 0293=0, 0294-0300=**1 each**, **0301=0** the recorded departure, 0302=1). **ADR-0301's STATUS says *"seven blocks"* then names the SIX-block range *"ADR-0295 through ADR-0300"*. Copy the FORM, not the miscount.** ⚠️ **CARRY NO WHOLE-FILE GREP COUNT IN AN ADR** — that species self-falsified in ADR-0296 ¶3 and twice in ADR-0297, **and again in ADR-0302 ¶11**, whose figure is now recorded as a **moving measurement with its tip named** rather than a constant.

11. **⚠️ A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX — the TWENTIETH consecutive stage at which this held.** The index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. **Still live:** `reference_sds_init_fetch_timeout_dial_budget_flake` (**TWO packages**; it did NOT fire at the SPEC, the PLAN or the IMPL) · the pre-existing `internal/cluster` `-race` outlier (`TestOutlierDetector_ConcurrentEjectExactlyOnce`) · `internal/httpclient TestOptions_ZeroValue_NoOpDefaults`. ⚠️ **TWO former flakes were FIXED at `f46ba419`/`f2dd994a` and a recurrence of either is now a FINDING, not a re-run:** `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery` and **the full-suite `bind: address already in use` BACKEND half** (banded `11000..14999` + wildcard probe) — **an in-band backend bind failure is a real finding.** The `subject ready: EOF` half was hardened at `0e9cc680` and likewise warrants suspicion, not acceptance. ⚠️ **`0061-lb-ring-hash` is NOT a live flake — a spread failure there is a FINDING.** **Isolate-re-run, then state the classification AND its evidence.** **NO flake fired at the phase-80 IMPL**, across a 120-fixture differential, a `-race` run and ~15 package runs: `address already in use` = **0** occurrences in the 161 KB log (BOTH bands clean) and `subject ready: EOF` = **0**.

12. **Counts at this tip (re-run MECHANICALLY in your worktree; never copy them).** ⚠️ Use GNU `command grep` or `git grep` for anything repo-wide. **All re-derived at the phase-80 IMPL close:**
    fixtures **120** (next-free **0119**; faithful predicate `^[0-9]{4}[a-z]?-`, a bare `^[0-9]{4}-` gives **118**) / fuzzers **55** (⚠️ the `internal/`-vs-repo-wide pair is NOT discriminating — both 55) / internal packages **73** / blank imports in `runner_test.go` **120**, ⚠️ **anchor on the FULL `^\t_ "github.com/pgdad/envoy-go/test/fixtures/` prefix — a naive `^\t_ ` gives 126** — and ⚠️ **`\t` in GNU ERE is a literal `t`: use `-P`, or a bare `-cE '^\t_ '` silently returns 0** / **stat surface 1207**, ⚠️ **DOCUMENTARY and CONFIG-CONDITIONAL — assert the DELTA** / **BackendKind tail 38** (⚠️ a TAIL VALUE — **39** declarations, values 0-38 contiguous, **no `iota`**; do NOT "fix" 38 to 39) / go.mod modules **2** (the phase-61.2 lineage figure — the single `go.mod` requires **67**; do NOT "fix" 2 to 67) / **`DECISIONS.md` 17724**, **301** headings, tail **ADR-0302 COMPLETE**, next-free **ADR-0303** / **`ROADMAP.md` 228 lines / 112 data rows** / **`BEHAVIOR_CONTRACT.md` 5868** / **`STATE_HISTORY.md` 430** / next-free reference port **10119** / ⚠️ **THIRTEEN non-test top-level segment detectors in `ExtractTags`** (9 `HasPrefix` + 4 `CutPrefix`) plus **FOUR** mid-name detectors, **code and error string AGREE** — ⚠️ **row 80 moved this 12 -> 13 and the mid-name FOUR is UNCHANGED because `sds.` is a ROOT** / `helpText` **30** keys and `helpTextRoster` **30** entries / `goldenRoster` **14** entries with `goldenTaggedIdx = {0,1,13}` / `goldenOTLPCells` pinned at **1212 / 1320 / 1184 / 1292** (⚠️ **RE-MEASURE, never compute — OTLP absolutes carry an unnamed 15-char version string, so absolutes are tree-local while deltas reproduce**) / spelled-`twelve` file count **39** (⚠️ **a MOVING measurement: 42 @`53855de0`, 43 @`7d014546`, 45 @`28aa342d`, 38 @`a412ab38`, 39 at HEAD — always name the tip**) / `79.1` in `BEHAVIOR_CONTRACT.md` **0** (⚠️ **`ROADMAP.md` row 79 still carries three, §Schema-INVARIANT-BLOCKED and named as a permanent discrepancy; the phase-79 phase documents' occurrences are HISTORICAL**).

13. **⚠️ DOCUMENTARY DEFECTS — carried forward, still deliberately NOT fixed.**
    - ⚠️ **`DECISIONS.md` ADR-0301 §Decision (b) says *"has failed twelve top-level detectors"* — PRESENT TENSE inside immutable ADR text, and it went factually WRONG when row 80 landed.** A **named permanent discrepancy**, alongside `ROADMAP.md`'s five occurrences (⚠️ **four of which are pure FALSE POSITIVES** — *"charter on TWELVE counts"*, *"Twelve controller-reperformed deliberate breaks"*, *"TWELVE MINOR"*, *"TWELVE tasks executed"* — with the fifth historically true, so **nothing there needs a `12 -> 13` move on the merits**, quite apart from the §Schema invariant).
    - ⚠️ **A NEW NEEDLE COLLISION, named at the IMPL:** `0110`'s driver comment *"envoy_sds_* metric names (twelve families…)"* is **ACCURATE** — the reference exposes **14 distinct `envoy_sds_*` names / 12 families**, the `update_duration` histogram contributing `_sum`/`_bucket`/`_count` — **but it is a FALSE POSITIVE for any future `\btwelve\b` top-level-detector sweep.**
    - **The `STATE_HISTORY.md` archive gap: 36 bullets for phases 67-75 and 58 overall**; `phase 77 PLAN done` in NEITHER file. **4-6 tasks, defensible ONLY with a `comm -13` recurrence guard.** The phase-80 IMPL honored the eviction rule for its own bullet (`phase 79 … SPEC done`, **428 -> 430**, absence proven with a firing sibling NC, body `cmp`-verified byte-identical) and **did not widen the gap.**
    - **`ROADMAP.md` malformation: rows 57, 69 and 78 lose summary content in GFM render.** All INVARIANT-BLOCKED by §Schema => **defensible as a GUARD, not a fix**, and the right gate is the **DISJUNCTION proven at the IMPL** over all 112 rows.
    - **`STATE.md` §Project counts self-contradicts §Current** inside the same file and is **FROZEN at the phase-76 IMPL close.** **Anchor on §Current, which IS live.** ⚠️ **Do NOT "fix" §Project** — repairing a count by editing the sentence that states it is how the ADR-0296/0297 self-falsifying species starts. ⚠️ **§Recent's own descriptive prose is ALSO a drift surface** — it was found stale at the PLAN and rolled at the IMPL.
    - **`BEHAVIOR_CONTRACT.md`'s `wasm.` arm is called a *"NEW SN9 flattening rule"*, which COLLIDES with ADR-0118's actual SN9.** Pre-existing; **this is why row 80's `sds.` arm claims NO SN number at all.**
    - ⚠️ **The two `//nolint:gosec` directives row 80 added are INERT** — `gosec` is not among the 9 linters `.golangci.yml` enables (`disable-all: true`). Harmless; noted so no one reads them as active suppressions.
    - **`golangci-lint-action@v6.5.2` emits a Node-20 deprecation warning in CI.** Deliberately NOT bumped: v7+ requires golangci-lint **v2** while the repo pins **v1.64.8**, so a bump forces a config migration. **It is a warning, not a failure** — do not "fix" it as part of an unrelated row.
    - ⚠️ **`ROADMAP.md` §Schema `:18` says *"Sub-phases get their own rows"* while phase 80's BRAINSTORM argued the legs live in the parent's `sub-phases` PROSE.** That is **practice diverging from written doctrine.** **Record the tension; do NOT relitigate row 80's id.**

## What THIS session does — SELF-PICK a subject, then its BRAINSTORM

One stage per session. **Pick from §THE PICK above (or a better candidate you can defend), record the pick AND the rejected alternatives with their costs, and write `docs/envoy-go/phases/<N>-<slug>/BRAINSTORM.md`.** Then: charter the row in `ROADMAP.md` as `in-progress` (⚠️ **escape inner pipes, terminate with `|`, run the leak check and BOTH well-formedness arms**), **bump the sentinel `want` 112 -> 113 IN THE SAME COMMIT**, roll `STATE.md` §Current **IN PLACE** (ADR-0288 — ⚠️ **§Recent is at its five-entry cap; a close that adds a bullet MUST evict the oldest to `STATE_HISTORY.md` VERBATIM. RE-DERIVE which bullet is oldest at YOUR tip — the IMPL already moved one — verify it is ABSENT from the archive BEFORE moving it, and NC the probe against a sibling that IS present**), create `PROGRESS.md` with the `# BRAINSTORM record` half, and roll this file. **`BEHAVIOR_CONTRACT.md` and `DECISIONS.md` stay BYTE-UNTOUCHED.**

Fresh worktree off master **at the phase-80 IMPL squash tip** per `feedback_git_worktrees` (⚠️ **locate it by SUBJECT — `git log --grep 'phase 80'` — never by position**, `reference_next_prompt_tracked_despite_gitignore`); subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** (`reference_parallel_subagents_private_scratch`) and they **must tear down docker containers BY NAME, never by an `ancestor`/image filter** (`reference_parallel_agents_shared_machine_namespaces`). ⚠️ **THERE ARE TWO RESERVED PORT BANDS — band private ports outside BOTH `20000-31007` (subject blocks) AND `11000-14999` (backends), and also clear of the static fixture ports `10000-10447` / `15000-15011` / `18001-18007`.** The phase-80 IMPL's agents used `42000-42899` safely. ⚠️ **If two agents share ONE worktree they must stage by EXPLICIT PATH** — this worked cleanly at the IMPL across **four** concurrent pairings, including a break agent running `.go` edits alongside a doc agent. ⚠️ **AND THEY MUST NOT RUN AN UNSCOPED `git restore`.** Per-task `gofmt` + `golangci-lint` on touched packages (`feedback_pertask_gofmt_lint`) — **NOT owed for a docs-only BRAINSTORM.** ⚠️ **`gofmt -l` NEVER exits non-zero — gate on OUTPUT.** ⚠️ **`golangci-lint` runs `misspell` with `locale: US`** — do NOT paste plan prose into `.go` files. ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch, and note `-o` precedes the package path.**

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. ⚠️ **ASSUME IT FIRES EVERY SESSION — it has been treated as live for TWENTY-FOUR consecutive sessions and it FIRED AGAIN, OBSERVED, at the phase-80 IMPL** (`Shell cwd was reset to /home/esa/git/envoy-go`). **Use `git -C <abs-worktree-path>` for every git command, prefer absolute paths over `cd`, put any needed `cd` FIRST in a compound command, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — Current-pointer section (TRUSTWORTHY; ADR-0288 discipline — ⚠️ **anchor greps on `^- **<field>`**; all seven fields verified singleton at the phase-80 IMPL close, NC both ways). ⚠️ **§Project counts SELF-CONTRADICTS §Current and is frozen at the phase-76 IMPL close** — do not anchor on it.
2. `docs/envoy-go/ROADMAP.md` — **§Schema first** (the row invariants you are about to write against), then the five deferred-candidate paragraphs (`:190 :200 :210 :216 :224`) which are the menu, then **row 80** as the model for a well-formed closing cell.
3. `docs/envoy-go/phases/80-stats-sds-projection/PROGRESS.md` — **the `# IMPL record`.** The most relevant document in the repo for calibrating what a stage is expected to find: **sixteen PLAN refutations, four load-bearing**, the first realized ~1500-LoC crossing, and the shape-seventeen gate that **survived its own remedy**.
4. `/BOOTSTRAP_PROMPT.md` — **at the REPO ROOT.** §6.1 `:285` (the split triggers), §6.2 (what a coherent split slice is), §7.5 `:357-367` (the six-gate).
5. `docs/envoy-go/DECISIONS.md` — **ADR-0288** (roll-IN-PLACE and the archive rule) · **ADR-0044** as-used (the §Context-at-SPEC / §Decision-at-IMPL split and the retained-footer convention) · **ADR-0045** (⚠️ it QUOTES the split gate rather than stating it) · **ADR-0302** (the phase-80 row, now COMPLETE — the model for a full ADR block).
6. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the phase-80 section, which records **TWO departures**, one of them found only by execution at the IMPL.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — especially **`reference_a_drift_correction_is_itself_a_claim`**, **`reference_handwritten_golden_shares_author_mistake`**, **`reference_golden_roster_omits_family_under_test`**, **`reference_branchpoint_roster_stale_midrow`**, **`reference_stale_cite_recurs_fix_by_pattern`**, **`reference_gate_command_negative_control`**, **`reference_probe_must_discriminate`**, **`reference_probe_input_is_a_claim`**, **`reference_empty_output_is_not_a_zero_result`**, **`reference_sample_is_not_an_audit`**, **`reference_harness_exit_code_is_not_command_exit_code`**, **`reference_sentinel_matcher_string_self_clears`**, **`reference_recursive_grep_blind_to_gitignored_tracked_file`**, **`reference_compensating_defects_cancel_in_the_gate_metric`**, **`reference_deferred_candidate_cost_restale`**, **`reference_plan_break_instructions_dont_compile`**, **`reference_differential_run_selector`**, **`reference_differential_fullsuite_startup_flake`**, `feedback_brief_citations_not_evidence`, `feedback_git_worktrees`, `feedback_execution_style`, and **`reference_bash_cwd_reset_commits_to_main`**.
