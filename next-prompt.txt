=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-VERIFIED 2026-07-23 (re-run MECHANICALLY at the phase-72 PLAN close, in the stage worktree, by the controller) — it does NOT fire. `stop` was NOT created.** Actual output:
- **(1) prints `NOT DONE: row 72`** — row 72 (`tracing-custom-tags-metadata-host`) is `in-progress` and STAYS so through the PLAN; it flips `done` at the phase-72 IMPL six-gate (ADR-0106, the SOLE leg). Every other chartered ROADMAP row is `done`.
- **(2) prints THREE live "candidates:" sentences** — HTTP/3, xDS, Observability => three families STAY OPEN. **Cite the command, never the adjective** (`reference_sentinel_deferred_sentence_live_vs_historical`): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` => **3**. (The Observability sentence still names `CLUSTER`/`HOST`; the `HOST` candidate rolls OUT — narrowing it to `CLUSTER`-only — at the phase-72 IMPL row-done edit. The phase-57 precedent: NO deferred sentence is narrowed at ANY pre-IMPL stage.)
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** => THREE families never opened. (The `WASM` line is a ROADMAP **bookkeeping artifact** — `internal/wasm/` is 21,550 LOC of wazero host and rows 25/25.1/25.2/25.3 are all `done` — flagged at the phase-72 BRAINSTORM §2.1 for a future correction and deliberately LEFT AS-IS, since the stage-close mechanics depend on this output.)

Checks (2) + (3) print => the sentinel does NOT fire. Abundant work remains.

==============================================


## STATUS AS OF 2026-07-23: THE PHASE-72 PLAN IS COMPLETE AND LANDED. ROW 72 (`tracing-custom-tags-metadata-host`) STAYS `in-progress`. THIS SESSION PERFORMS THE PHASE-72 IMPL — THE FINAL STAGE OF THE LIFECYCLE.

**What the phase-72 PLAN landed** (`git log --grep 'phase 72'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): the NINE-task SINGLE-FLAT-ROW TDD spine (T1-T9) decomposing SPEC §10, with every SPEC §3/§9/§11 anchor RE-DERIVED at `2a82cc7b` by TWO independent read-only agents on disjoint remits. Docs-only: `PLAN.md` + `PROGRESS.md` — ZERO production `.go`, DECISIONS/ROADMAP UNTOUCHED.

- **RE-DERIVATION RESULT: 68 of 69 anchors EXACT-HOLD; ONE MINOR drift (RD-LIT); ZERO SPEC claims refuted; FIVE NEW FINDINGS.** The findings matter more than the drift.
- **F1/F2 — THREE UNNAMED EDIT-SITE OBLIGATIONS the SPEC does not scope** (folded into T2): two stale `config_test.go` doc comments (`:627-629`, `:754-757`) asserting HOST rejects, and **SEVEN stale production comment lines in FOUR groups** in `config.go` (`:48-51`, `:69`, `:73`/`:74`/`:75`/`:76`, `:178-183` — notably **`:182`** `// metadata CLUSTER/HOST/unset kinds reject LOUDLY`), all FALSE the moment the accept lands (`reference_code_comment_not_evidence`). Plus a THIRD, uncited unset-kind regression guard at `config_test.go:460-464` that must stay green.
- **F4 — ⚠️ THE LANDED ADR-0294 §Context CARRIES A CLAIM ITS OWN SPEC REFUTED.** It says the closure is *"mandatory rather than stylistic, since `internal/tracing` must not import `internal/cluster` (the cycle guard)"*. SPEC §16's V1 finding M3 refuted this, and BOTH the PLAN and its V1 verifier re-executed it: **`internal/cluster` does not depend on `internal/tracing`, so a direct import builds clean — there is NO cycle.** The closure preserves a **self-imposed LAYERING rule** that the `go list -deps` gate asserts. **T9 must correct the §Context wording and must NOT propagate the claim into §Decision.** *(This router previously repeated the stale claim too; it has been removed here.)*
- **F5 — a gofmt trap and an over-stated guard justification.** (a) SPEC §3.2's quoted HOST skeleton compresses `if b { return ... }` onto single lines, which **gofmt EXPANDS** — the PLAN §1.2 carries the byte-accurate multi-line forms; paste THOSE. (b) `structpb.NewStructValue(nil)` really is non-nil, **but the nil guard is NOT observably load-bearing through `ResolveCustomTags`** — `descend`'s first hop returns `(nil,false)` without panicking and `MetaPath` is min-1 by parse validation. **Keep the guard, but T1's Break C must assert on `MetaLookup`'s OWN return; a span-routed break is VACUOUS.**
- **RD-LIT (the one drift):** SPEC §3.5's "all **167** test literals are keyed" reproduces under NO grep (two agents measured 177/209 and 177/177; plain `Endpoint{` gives 229). **State the claim, cite no number** — the load-bearing part is CONFIRMED: **zero positional/unkeyed `Endpoint` literals exist**, so adding a field is zero-positional-breakage.
- **THE CENTRAL COST CLAIM IS CONFIRMED BY HASH** (V1 built the row from the PLAN's own skeletons in private scratch): `go build`/`vet` EXIT 0 · `go mod tidy -diff` EMPTY · the layering check EMPTY · the predicted red set **EXACTLY TWO** `/host` subtests (printing `Kind:5` => iota==5) · and **sha256 proof that `connection.go`, `h2dispatch.go`, `h3dispatch.go`, `accesslog_emit_test.go`, `chain.go` and `subset.go` are byte-identical to baseline.** The 18 emit callers and the 29 emit test callers are genuinely untouched.
- **⚠️ TWO TRAPS V1 CAUGHT IN THE PLAN ITSELF — the PLAN's own corrections needed correcting.** (i) An earlier draft "corrected" the stale fuzz comment anchor to `fuzz_test.go:93-94`; **that was WRONG — `CLUSTER/HOST` is on `:92`, so the SPEC's `:92-93` was right**, and `:93-94` holds no such token (an `old_string` built on it would miss the stale claim and garble the adjacent CORRECT dispatch-order sentence). (ii) **T4's red is a COMPILE error, not a value mismatch** — after T3 `internal/filter/hcm` does NOT compile (three `not enough arguments in call to tracing.ResolveCustomTags` at `:57`/`:118`/`:179`). The distinction that makes phase 72 cheap is that the **emit SIGNATURES** are unchanged; the **`ResolveCustomTags` arity** changes exactly as in 70/71. **A PLAN is not evidence either — re-derive.**

## What THIS session does — the phase-72 IMPL (EXECUTE the PLAN, re-deriving as you go)

One stage per session: the IMPL — **the FINAL stage; row 72 FLIPS `done` at its six-gate.** Execute T1-T9 from `PLAN.md`, each task red-first -> green -> break-verified -> adversarially reviewed. **RE-DERIVE each task's anchors at the IMPL tip** (`feedback_brief_citations_not_evidence`, `reference_quoting_is_not_executing`) — and note this PLAN is the first in the lineage whose OWN drift correction was refuted, so treat its cites with the same scepticism it asks you to apply to the SPEC's. Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style` (one implementer per task + a per-task adversarial reviewer); subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`).

**The NINE-task spine (PLAN §Task 1-9 — full step-by-step detail lives there):**
- **T1** `internal/cluster`: the `Endpoint.filterMetadata` field + nil-safe `MetaLookup` accessor + the `manager.go:884` populate. **Step 0 re-runs the FOUR collision greps.** `:883` (the `envoy.lb` projection), `:754` (`defaultSubset`) and `subset.go` BYTE-UNCHANGED. ONE new import (`structpb`). Breaks A/B/**C (assert on the accessor's return — F5b)**/**D (not expressible at the call site; the substitution is PRE-NAMED)**.
- **T2** `internal/tracing/config.go`: `kindMetadataHost` **APPENDED at iota==5** (never inserted) + the HOST accept arm replacing the `:267-268` reject, cloning the ROUTE accept `:248-264` in its **gofmt-clean** form. Branch on **`k.GetHost()`** on the bound `k := md.GetKind()` — `md.GetHost()` does not exist (`*CustomTag_Metadata` has exactly three getters). Pre-known red = EXACTLY TWO `/host` subtests. **+ the 9 stale comments (F1/F2).** Breaks E/F/**G (the only assertion that catches a silent renumber)**/H.
- **T3** `internal/tracing/resolve.go`: the nil-tolerant one-arg `hostMetaLookup` **5th** param + the `kindMetadataHost` arm descending the **FULL** `MetaPath` (NOT `[1:]`), reusing `descend`/`structpbValueToString` VERBATIM; the `:12-31` doc gains a HOST clause; 32 real `resolve_test.go` callers += trailing `nil` (raw grep 33 — `:106` is a string literal). NO new import. Breaks I (the crux)/J/**K (it COMPILES — no substitution needed)**.
- **T4** `internal/filter/hcm/accesslog_emit.go:57`/`:118`/`:179` ONLY — append `picked.MetaLookup`. **The 3 signatures + all 18 callers + the 29 emit test callers stay BYTE-UNTOUCHED — verify BY HASH at Step 4; a failure REFUTES the row's headline claim and must be reported, not worked around.** Live HOST-span tests for H1/H2/H3 **independently** + a first-class zero-`picked` test (**the 5 span-capable zero sites — NOT the raw 11-of-18**). Build `picked` via `Cluster.PickEndpoint()`, NOT a new test constructor (which would mint a third exported symbol). Breaks L/M.
- **T5** the purely-ADDITIVE `meta_host_ok` fuzz seed (**`meta_bad` STAYS on CLUSTER**) + the stale **`fuzz_test.go:92-93`** comment narrow (do NOT disturb the adjacent dispatch-order sentence sharing `:93`) + dispatch-verify. +0 fuzzers.
- **T6** the `0116-tracing-custom-tags-metadata-host` OTLP fixture on the `0115` chassis: the metadata block MOVED onto `lb_endpoints[0].metadata` (sibling of `endpoint:`, 16-space indent) in BOTH YAMLs + both `kind:` flipped to `host: {}`. **STRING values only.** Registration at `runner_test.go:143`. Breaks N/O/**P (the wrong-namespace anti-vacuity control)**/Q. ⚠️ **`0064-lb-subset` is DRIVER-GENERATED (no YAML files) and its precedent covers `envoy.lb` ONLY** — a non-`envoy.lb` namespace cross-side is NEW ground; if the reference does not resolve it, **STOP and report**.
- **T7** BEHAVIOR_CONTRACT **B1-B4** — **keep B2's scope EXACT**: the pick-time departure is EXECUTED-proven for **connect-failure 503 only**; pool-overflow and circuit-breaker are **UNPROBED**; the 404 no-match (no span at all) and zero-endpoint (boot-rejected) arms have **NO envoy-go counterpart and must NOT be written as parity**.
- **T8** the six-gate (gofmt/vet/build/lint · `go mod tidy -diff` empty · the FULL **118-dir** differential · `-race` on the three touched packages · the layering check via `go list -deps`, no `...`) + the envelope audit **pinning the SHAPE for `internal/cluster`** (one field + one accessor + one import + one changed line) since BYTE-UNTOUCHED is unassertable there. ⚠️ **`internal/cluster` `-race` is NEWLY load-bearing** and carries a PRE-EXISTING flake (`TestOutlierDetector_ConcurrentEjectExactlyOnce`, `outlier_test.go:766`, reproduced on the UNMODIFIED baseline) — **isolate-re-run; do NOT re-classify as a phase-72 regression.**
- **T9** ADR-0294 completed IN PLACE — **APPEND** `### Decision (landed at the phase-72 IMPL)` + `### Consequences` after the hand-off line; **NO second `### Decision` heading** (verified: `grep -c '^### Decision'` inside the block => 0); no renumber; **CORRECT the §Context's refuted "cycle guard" justification (F4)** + **ROADMAP row 72 -> `done`** + **the deferred-sentence narrow (`CLUSTER`/`HOST` -> `CLUSTER`-only)** + STATE/router/memories.

**Stage-close mechanics (the IMPL):** **ROW 72 FLIPS `in-progress` -> `done`** at the six-gate. ADR-0294 COMPLETED IN PLACE (tail stays ADR-0294; next-free ADR-0295). **The deferred sentence narrows NOW** (and only now). STATE §Current rolled IN PLACE (lifecycle 3 -> DONE; lineage re-capped at five **with its PREAMBLE updated** — the ADR-0288 rule). next-prompt.txt rolled to the **phase-73 BRAINSTORM** (the roller SELF-PICKS per the 2026-07-12 standing directive). Re-run the sentinel MECHANICALLY: (1) goes SILENT once row 72 flips; (2) still => **3**; (3) `NEVER OPENED: gRPC/Runtime/WASM` — does NOT fire; no `stop`. Re-run every count in the worktree (fixtures => **118**, fuzzers => 55, DECISIONS tail => ADR-0294). **Adversarially verify before landing** (the phase-70/71/72 precedent — independent verifiers in PRIVATE scratch, `reference_parallel_subagents_private_scratch`).

**Candidate pool for the phase-73 self-pick (re-derive each candidate's cost at the CURRENT tip — do NOT trust these adjectives):**
- **Observability (family OPEN):** the LAST `metadata` MetadataKind — **`CLUSTER`** (MODERATE ~11-13 tasks; CLOSES the quartet; needs a metadata field + accessor on `cluster.Cluster`, a `routeAction` method + a `SetClusterMetadata`/`ClusterMetaLookup` pair **RE-PAYING the 18-caller thread**, and a disposition for `weightedClusterRouteAction`'s per-request in-closure selection) · **pick-time `picked` propagation on upstream-failure paths** (surfaced at the phase-72 SPEC — a PRE-EXISTING divergence; ~4 production lines since `ep` is already in scope at the pool-overflow and connect-failure returns, but it flips three access-log consumers and breaks three landed unit tests, and NO differential fixture pins it; **a strong small FUTURE pick — probe the reference's pool-overflow and circuit-breaker arms first, which phase 72 left unprobed**) · `spawn_upstream_span` (LARGE) · OTel `http_service` / `sampler` / `resource_detectors` (MODERATE each) · tracing `verbose` (MODERATE) · force-trace (LARGEST) · the `ssl` handshake-outcome stat family (LARGE — ADR-0286 C3 framework surgery CONFIRMED) · **`access_log[].filter`** (a LIVE behavioral divergence — a KNOWN proto field the strict pass ACCEPTS but ZERO production `GetFilter()` consumers; **flagged a strong FUTURE pick**) · **`stats_flush_on_admin`** · the Zipkin empty-string tag drop + the serialization boundaries (B3 — documented, not chartered) · a 6th stats sink — **UNAVAILABLE**, the only unconsumed `stats_sinks[]` extension is `wasm`.
- **xDS (family OPEN):** upstream SDS (a boot-ORDER blocker) · CDS/EDS · LDS/RDS · ADS (muxed) · Delta xDS · RTDS/Runtime-layer · `google_grpc`. *(Reconnection-backoff was REJECTED at phase 72 as larger-than-it-looks. Mind the `grpcclient->cluster->tls` cycle constraint — `reference_xds_config_seam_transitive_cycle_guard`.)*
- **HTTP/3 (family OPEN):** `QuicProtocolOptions` tuning (SMALL but needs ~2 tasks of NEW connection-reusing harness first; timing-flaky observables) · upstream H3 cluster · alt-svc · 0-RTT/early-data · h3spec conformance gate · QUIC robustness.
- **Never-opened families:** `Runtime` (genuinely unbuilt — `internal/runtime/` = `doc.go` only) · `gRPC` (gated on an HCM response-trailer framework row — `RunDecodeTrailers`/`RunEncodeTrailers` have ZERO production callers) · `WASM` (a ROADMAP bookkeeping artifact — substantially BUILT; a docs correction, not a work row).
- **Other deferred riders:** the RBAC `require=false` fixture rider · DataSource `environment_variable` (D-ENV-HARNESS-gated) · `watched_directory`/SDS rotation · the `internal/cluster` `-race` flake (test hardening).

## Read first (in order)
1. `docs/envoy-go/phases/72-tracing-custom-tags-metadata-host/PLAN.md` — the spine. §1.1 the RD-* ledger (68/69 EXACT-HOLD; the ONE drift), §1.2 the **gofmt-clean, misspell-clean** clone skeletons (paste THESE, not the SPEC's), §1.3 the five PLAN-surfaced findings, §1.4 the adversarial record, Global Constraints + the Break protocol + the Identifier roster, the File-structure block, T1-T9, and the Self-review table.
2. `docs/envoy-go/phases/72-tracing-custom-tags-metadata-host/PROGRESS.md` — the ledger you FILL as you go (red-first verbatims, WHICH break fired, substitutions, the T4 hash verification, the T5 dispatch-verify result, the six-gate evidence).
3. `docs/envoy-go/phases/72-tracing-custom-tags-metadata-host/SPEC.md` — the disposed `D-CTMH-*` docket (§3), the S1-S8 ledger (§1.1), the edit-site roster + the span-capability partition (§11), the fixture design (§8), the BC edit map (§9), the `-race` hazard (§10), the deferred rosters (§12, §13). **Read it WITH the PLAN's corrections in hand** (RD-LIT; F1/F2/F4/F5).
4. `docs/envoy-go/ROADMAP.md` — row 72 (`in-progress` -> flips `done` this session) + the live Observability `candidates:` sentence (narrows THIS session) + §Schema.
5. `docs/envoy-go/DECISIONS.md` tail — **ADR-0294** (PROPOSED, §Context only, ending with the `*(§Decision + §Consequences land at the phase-72 IMPL.)*` hand-off line; **APPEND** §Decision + §Consequences, do NOT create a second §Decision heading, do NOT renumber, and **CORRECT the "cycle guard" sentence**) + **ADR-0293**/**ADR-0292** (the ROUTE/REQUEST precedents) + ADR-0044 · ADR-0045 · ADR-0080 · ADR-0106 · ADR-0165.
6. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline) + the §Recent lineage + the §Project counts.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — trust MEMORY.md's index; esp. `feedback_brief_citations_not_evidence`, `reference_quoting_is_not_executing`, `feedback_git_worktrees`, `feedback_execution_style`, `feedback_subagents_no_push`, `reference_route_metadata_resolve_full_metapath`, `reference_filterchain_dynamicmetadata_accessor`, `reference_parallel_subagents_private_scratch`, `reference_next_prompt_tracked_despite_gitignore`, `reference_break_protocol_commit_first`, `reference_differential_break_protocol_count1`, `reference_deliberate_break_wrong_assertion`, `reference_differential_run_selector`, `reference_plan_break_instructions_dont_compile`, `reference_probe_must_discriminate`, `reference_vacuous_break_receiver_normalizes`, `reference_fuzzer_count_docs_drift`, `reference_code_comment_not_evidence`, `reference_cluster_race_outlier_flake`, `reference_structpb_tag_cross_side_string_only`.
