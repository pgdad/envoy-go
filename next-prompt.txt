=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

**THERE IS BANKED MID-LIFECYCLE WORK AT THIS TIP AND ADVANCING IT IS THE STAGE.** Phase 79 (`stats-prometheus-projection`) is **OPEN**: row 79 is `in-progress`, `BRAINSTORM.md` and `SPEC.md` are landed, and `PLAN.md` / `PROGRESS.md` do NOT exist. **This session performs the phase-79 PLAN.** Do NOT open a new subject.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=111 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **`want=111` IS A LIVE FIGURE, NOT A CONSTANT.** It is the ROADMAP data-row count. **A row-adding phase MUST bump it in the same commit that adds the row** — otherwise every later session gets `GATE FAIL: examined 112 data rows, expected 111`. That noise is deliberate: a wrong denominator must be loud, because a silent one is how check (1) failed in the first place. **The phase-79 BRAINSTORM bumped 110 -> 111 when it chartered row 79. The phase-79 SPEC added NO row and did NOT touch `want`. The phase-79 PLAN adds NO row either and MUST NOT touch it.**

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. When you open a new family, add its row-summary slug here.

**SENTINEL RE-RUN MECHANICALLY 2026-07-28 AT THE PHASE-79 SPEC. It does NOT fire. `stop` was NOT created and MUST NOT be.** ⚠️ **Re-run it yourself anyway and record what YOU see** — a prior run's result is not evidence for the current tip. ACTUAL output, recorded rather than predicted:
- **(1) `NOT DONE: row 79`.** **No `GATE FAIL` at `want=111`.** Negative controls: `want=109`, `want=110` AND `want=112` each ⇒ `GATE FAIL: examined 111 data rows, expected <want>`.
- **(2) FIVE, at `:189`, `:199`, `:209`, `:215` (long form) and `:223` (the Operational-tooling short form) — UNCHANGED.** **Row 79 narrows NOTHING and that is STATED rather than forecast.**
- **(3) `NEVER OPENED: gRPC` and `NEVER OPENED: WASM`** — unchanged. NC: an invented slug ⇒ `NEVER OPENED: ZZZ-nonexistent`. ⚠️ **The WASM rider stays DECLINED on the merits** (item 14 below).
- Input measured at **227 lines / 1 003 291 bytes / 13** bare `candidates:` hits (against the sentinel's own narrower 5), so an empty result could not read as a zero result.

⚠️ **`ROADMAP.md` WAS BYTE-UNTOUCHED AT THE SPEC**, so the leak-check hazard was dormant for one stage. **It re-arms at the IMPL**, which flips row 79 to `done`: the new cell text must contain ZERO occurrences of the two deferred-candidate phrases and no unregistered `<Family>-family row` slug, each grep negative-controlled against a deliberately doctored copy. **`grep` cannot tell a mention from a use.**

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`.** Every document from phase 75 forward recorded its blind spot — **109 data rows / 105 matched / FOUR misses** at an **em-dash** in an "after" cell, a **DOT** in the slug `http-1.1`, and two **LETTER-suffixed** ids `28.1a`/`28.1b` — and every one of them called it *"currently HARMLESS, since all four missed rows are `done`."*

⚠️ **Nobody fired the unsafe arm until the phase-77 PLAN. EXECUTED on scratch copies: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** A false "done" ends the project early. ⚠️ **DO NOT restore the regex form.**

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

The old check (2) matched ONLY the long phrase and could not see the **Operational-tooling** family's three genuinely un-chartered items, recorded in the **SHORT** form. Fixed at the phase-77 BRAINSTORM, at the MATCHER; negative controls all EXECUTED. ⚠️ **Run the union form above, never one half of it** — an agent running only the long-phrase regex sees FOUR lines, not five.

⚠️ **Normalising that Operational-tooling paragraph to the long form is a NAMED, DELIBERATELY-UNTAKEN follow-up.**

⚠️ **THE DOCUMENTED TRAP'S STATED MECHANISM IS REFUTED, WHILE THE TRAP IS REAL.** `reference_sentinel_deferred_sentence_live_vs_historical` says a naive `grep -c 'candidates:'` inflates *because of* the `candidates were:` recaps. It does inflate — but **not for that reason**: `candidates were:` does not contain that substring **at all**. The inflation comes from **ROADMAP table rows carrying BRAINSTORM prose**. **Cite the command; do not re-inherit the explanation.**

⚠️ **AND THE MEASURED RECORD: CHECK (2) HAS NEVER GONE DOWN** across ~22 phases (history `0 -> 1 -> 3 -> 4 -> 5`). **Phase 74 landed three `ssl.*` stat names and made its OWN sentence LONGER**; **phase 73 predicted a clause REMOVAL and got a clause REPLACEMENT**; **phase 77 opened a family and narrowed nothing**; **phase 78 narrowed nothing at ANY of its four stages**; **phase 79's BRAINSTORM chartered a row straight out of the `:209` paragraph and STILL narrowed nothing**; **and the phase-79 SPEC narrowed nothing either** — the candidate sentence keeps its text until the row CLOSES. => **the candidates sentence is a WINDOW onto a larger deferred backlog, not an inventory of it.** ⚠️ **A stage that predicts a decrease here is repeating the phase-73 error. State what your row narrows — usually nothing — rather than forecasting.**

⚠️ **NEVER WRITE A SENTINEL'S OWN MATCHER STRING INTO A FILE THE SENTINEL GREPS.** The sentinel greps **`docs/envoy-go/ROADMAP.md`** — writing these strings *here* is safe; writing them *there* is not.

⚠️ **THE SELECTION CALCULUS, CORRECTED AND STILL STANDING.** *"A candidate that clears a check-(3) blocker is worth **strictly more**"* does **not** hold: opening a family **MINTS** a check-(2) sentence, so phase 77's own arithmetic was **NET ZERO**. What survives: **opening a family is the ONLY way that family's work becomes enumerable at all — a prerequisite, not a shortcut.** Do not restore the stronger claim.

==============================================


## STATUS AS OF 2026-07-28: PHASE 79 IS **OPEN**. ROW 79 IS `in-progress`. THIS SESSION PERFORMS THE **PHASE-79 PLAN**.

**What the phase-79 SPEC landed** (`git log --grep 'phase 79'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): **FIVE files — ONE MORE than the phase-76/77/78 SPEC precedent, for a stated reason** — `docs/envoy-go/phases/79-stats-prometheus-projection/SPEC.md` (NEW), `DECISIONS.md` (ADR-0301 §Context, **17596 -> 17628 lines**), `STATE.md` (§Current rolled IN PLACE per ADR-0288), **`STATE_HISTORY.md` (412 -> 414)**, and this file. ⚠️ **Those three prior SPEC commits were each exactly FOUR files and none touched `STATE_HISTORY.md`** (verified by `git show --stat` on all three). This one is five because **§Recent lineage was AT its five-entry cap**, so the phase-77 BRAINSTORM bullet had to be evicted **verbatim** (10 077 bytes) before the phase-79 BRAINSTORM bullet could take its place. ⚠️ **Both this file and SPEC §13 initially said "four, matching the precedent" — a hygiene claim already falsified by the commit that carried it. Corrected before landing; expect the same species and check your own file set rather than the sentence describing it.** **`ROADMAP.md` and `BEHAVIOR_CONTRACT.md` BYTE-UNTOUCHED. ZERO production `.go`, ZERO test `.go` committed.** Four investigation agents on disjoint remits, each in its own detached worktree with private scratch and a private port band, plus controller re-derivation.

### What matters for the NEXT session, in order

1. **⚠️ RE-RUN ALL THREE SENTINEL CHECKS YOURSELF AND RECORD THE ACTUAL OUTPUT; DO NOT PREDICT IT.** Create `stop` if and ONLY if all three print nothing. **Check (1) is LIVE** (`NOT DONE: row 79`). `want` STAYS **111** — **the PLAN adds no row and must not touch it.**

2. **⚠️ THE SUBJECT IS FIXED AND THE STAGE IS THE PLAN. DO NOT RE-OPEN THE SEARCH, AND DO NOT RE-LITIGATE THE D-SPP DOCKET — IT IS DISPOSED.** Read `docs/envoy-go/phases/79-stats-prometheus-projection/SPEC.md` in full; it is the input. ⚠️ **But a SPEC's citations are not evidence either** (`feedback_brief_citations_not_evidence`) — **re-derive every load-bearing figure at YOUR tip.** The record across phases 78 and 79 is that **every stage has found a load-bearing error in its predecessor, and each was found by EXECUTION, never by review.** The phase-79 SPEC refuted **nine** BRAINSTORM claims and added two the BRAINSTORM never made. **Budget for finding one more — and note the SPEC's own §14 records TWO controller self-corrections against itself.**

3. **⚠️ THE D-SPP DOCKET AS DISPOSED — carry these; a PLAN that re-inherits the BRAINSTORM's positions will be wrong.**
   - **D-SPP-1 — CLOSED IN THE ROW'S FAVOUR BY EXECUTION.** Measured against a LIVE pinned `envoyproxy/envoy:contrib-v1.37.2`, six arms, **fresh container per arm**, bridge network, torn down **BY NAME**, teardown proof EMPTY. **`runtime.`, `access_logs.` and `tracing.` are ALL THREE BYTE-MIRROR** — the reference INLINES the middle segment and attaches ZERO labels (`envoy_access_logs_grpc_access_log_logs_written{} 3`, `envoy_tracing_zipkin_spans_sent{} 3`, `envoy_runtime_num_keys{} 6`). The middle segment is the **SINK TYPE**, proven by grep COUNT: the distinctive operator-chosen names appear **ZERO** times in dumps of 828-1159 lines. The `runtime.` positive control reproduced `0118`'s pin exactly, which is what licenses the other arms. ⇒ **THREE arms ship; nothing moves to 79.1 on this account.** `ExtractTags` must return `residual == input, labels == nil` — the exact SN5 shape. **Do NOT re-run the docker probe; it is done.**
   - **D-SPP-2 — RECORDED FOR 79.1.** The rule is *"distinct secrets reaching `internal/boot/boot.go:201`"*, NOT *"distinct secrets in the corpus"* — the corpus rule yields `6 x 5 = 30` and reads as confirmation of the 30-name total **by coincidence**. Four contributing secrets confirmed: `server_cert` (`0103`), `validation_ca` (`0108`/`0109`), `rccf_validation_ca` (`0110`), `edf_validation_ca` (`0111`).
   - **D-SPP-3 — RESOLVED TO THE LOG, AND THE COUNTER IS DISQUALIFIED BECAUSE IT DEADLOCKS.** ⚠️ **This is the stage's second-biggest finding and the BRAINSTORM never named it.** `Registry.Walk` (`internal/stats/registry.go:139-140`) holds `r.mu.RLock()` for the whole iteration; `getOrRegister` (`:179`) takes `r.mu.Lock()`; Go's `RWMutex` is **not reentrant**. A lazily-registered counter at the skip site **HANGS the admin scrape** — proven under `go test -timeout 20s` with a goroutine trace naming both lock sites. Three further executed disqualifiers: the counter's own value is **WALK-ORDER DEPENDENT** (registered first ⇒ always a full scrape stale; registered last ⇒ current), it is **self-referentially trapped** (absent from its own output under a naive name), and it would **owe a `helpText` entry** or ship degraded. ⇒ **an aggregated `log.Printf`, ONE line per `WriteProm` call, names sorted and joined.** ⚠️ **`sync.Once` is REJECTED on executed grounds** — it makes the signal order-dependent and untestable beyond one test. ⚠️ **Stat surface is now `+0` UNCONDITIONALLY** (1207 -> 1207).
   - **D-SPP-4 — YES, BUT WITH THE *NINE*-SEGMENT ENUMERATION.** ⚠️ **The BRAINSTORM's proposed repair is ITSELF WRONG.** There are **NINE** recognized top-level segments, not five: `cluster. http. listener. server. wasm.` **plus `mongo. kafka. redis. thrift.`**, which are `default`-branch `CutPrefix` detectors (`name.go:286 :306 :323 :340`). A naive `+wasm.` edit ships a **FIFTH** generation of a wrong list. **No test asserts the string** (verified repo-wide; the two in-fixture hits are a YAML comment and README prose, neither containing the `(want …)` parenthetical). ⚠️ **SHIP A BYTE-STABLE GUARD ON THE CORRECTED STRING** — without one this row's own fix is as unguarded as the defect it repairs.
   - **D-SPP-5 — CONVERT the `0118` pin, do NOT delete it.** ⚠️ *"Delete it in favour of the generic prometheus comparison"* is **REFUTED — no such comparison exists.** `0118`'s `ProbeAdmin` returns `/ready` **only**; there is no generic prometheus differential anywhere in the runner, so deleting leaves that surface with **zero** assertions on **either** side. Liveness was PROVEN at the SPEC by the full red-then-green cycle **before** any edit.

4. **⚠️ THE SPEC'S OWN HEADLINE FINDINGS THE PLAN MUST NOT LOSE.**
   - **`BEHAVIOR_CONTRACT.md:5020` IS A MANDATORY EDIT ON THE `+0` PATH**, and the BRAINSTORM's edit-site table had **no `BEHAVIOR_CONTRACT.md` row at all**. `:5020` is the phase-77 departure note closing *"**The day `internal/stats` learns `runtime.`, fixture 0118 goes RED on purpose**, naming the follow-up."* **Phase 79 is that day.** It must record the departure CLOSED for three roots and **PERSISTING for `sds.`**.
   - **THE DIFFERENTIAL SUITE IS STRUCTURALLY BLIND TO TWO OF THE THREE ARMS.** **10** fixtures configure `stats_sinks`; **0 of 10** configure a tracer or a gRPC/OTel access logger. ⇒ **a fixture-level sink audit would be VACUOUS**; the gate must be a **UNIT-level byte test in `internal/statssink`**, asserting the WHOLE emitted set (not per-name presence — `reference_stat_count_guard_blind_to_rename`), with `cluster.*` and `listener_manager.*` as stacked controls and the OTLP 2x2 knob cross-product. ⚠️ **Run the tag-hoisting NEGATIVE CONTROL at PLAN time** — it moved all four sinks (152 diff lines) and is what proves the gate can fail.
   - **ONE OF THE TEN NAMES HAS NO REFERENCE COUNTERPART.** The reference's zipkin family has **no `spans_dropped`**; envoy-go registers `tracing.zipkin.spans_dropped`. **NINE of ten have a counterpart; one is envoy-go-only.** Any text saying *"these ten now match the reference"* is wrong and a cross-side assertion on that name is unsatisfiable.
   - **THE REFERENCE EMITS ZERO `# HELP` LINES** (0 on all six dumps). The `helpText` leg is an **envoy-go-internal quality choice that WIDENS an existing departure** — it cannot be justified as parity, and the contract states it as a departure.
   - **ZERO-LABEL BRACE DIVERGENCE:** reference writes `envoy_runtime_num_keys{} 6`; envoy-go **omits `{}`**. The parity flip is safe only through `0118`'s existing parser (`driver.go:399-403`), **never** a raw-line comparison. ⚠️ **AND `scrapeProm` (`:407-447`) is LABEL-BLIND** — it splits at `{` (`:429-435`), so a parity assertion built on it passes silently against a future hoisting arm.
   - **FAILURE-ATTRIBUTION:** `0118` break failures report at **`runner_test.go:1349`**, NOT `driver.go:308` — `t.Helper()` plus the `fixture.TB` indirection collapses the driver frames. **A gate grepping a `driver.go` line number will not match.**

5. **⚠️ THE TWO DESIGN ARTIFACTS THAT WERE BUILT AND EXECUTED AT THE SPEC — INHERIT THEM, DO NOT RE-DESIGN THEM.**
   - **The observability stacked control.** Capture via `log.SetOutput(&buf)` + `log.SetFlags(0)` restored by `t.Cleanup` (confirmed `-race` clean; must NOT be `t.Parallel()`). Registry = one projecting name (`server.live`) + one dropped name (`runtime.num_keys`). **FOUR assertions, each its own `t.Errorf`** (`reference_fatalf_makes_assertions_unreachable`): exactly-one line · it names the dropped name · **it does NOT name the projecting name** · a liveness cross-check that the projecting name really is in the exposition. **Executed matrix:** positive PASS; over-fire-aggregate FAIL on the **negative leg**; over-fire-per-name FAIL on the **count leg**; never-fire (the shipped tip) FAIL on the **count leg**. ⚠️ **The two over-fire arms trip DIFFERENT legs, and a positive-only test passes ALL THREE negative controls.**
   - **The `helpText` reverse guards — TWO of them, and one is provably insufficient.** Guard A = golden **key-set equality** (missing/extra reported separately, never a count). Guard B = drives the **real projection** and asserts no `# HELP` line is **self-equal**. ⚠️ **The four-arm matrix's decisive row: a key TYPO'd *and the typo copy-pasted into the golden list* PASSES Guard A and is caught ONLY by Guard B.** ⚠️ **The roster must be DERIVED from `internal/listener/manager.go:381` `normalizeAddr` and the landed golden at `name_test.go:10`, not hand-written** — a hand-written roster produced a **false positive on 6 names** at the SPEC (`reference_probe_input_is_a_claim`, which fired in practice).

6. **⚠️ THE `+0` STAT-SURFACE CLAIM IS NOW UNCONDITIONAL, AND THE `TestNoNewStat*` BLINDNESS IS PROVEN RATHER THAN ASSERTED.** All five guards live in `internal/statssink/registration_test.go` (`:26 :53 :81 :109 :137`, `package statssink`, the exhaustive repo-wide set) and none reaches `internal/stats`. Executed **with a failing baseline**: a genuine new registration injected into `internal/stats` leaves **all five GREEN** — ⚠️ **and they were green against a build that DEADLOCKS `WriteProm`** — while the same registration moved into `NewRegistry()` turns **all five RED**. ⇒ their green is **genuine blindness, not a no-run**, the blindness **cuts both ways**, and **the argument must be STRUCTURAL**: enumerate the diff's registration call sites and show the set is empty.

7. **⚠️ ANCHORS AT THIS TIP, RE-DERIVED — AND FOUR INHERITED CITES ARE WRONG.** `internal/stats/name.go`: `ExtractTags` at **`:47`**, `switch {` at **`:50`** (⚠️ **NOT `~:100-110`, which is inside the `wasm.` comment block**), arms at `:51 :61 :83 :93 :97`, `default:` at `:132`, terminal error at **`:350`**, `flattenToProm` at **`:370`** (call at `:371`), `helpText` **`:458-476`** (15 entries), prose *"Of the 15 entries"* at **`:448`**, SN-rule doc block **`:24-46`**. `internal/stats/prom.go`: comment **`:18-22`**, skip block **`:39-41`** with the bare `return` at **`:40`** (⚠️ **it is a per-metric `continue` inside the `Walk` closure, NOT an early exit from `WriteProm`**). `0118/driver/driver.go`: pin blocks **`:142-162`** and **`:164-169`** (⚠️ **NOT `:142-166`; `:166` is mid-sentence**), call site `:266`, function `:269-315`, `scrapeProm` `:407-447`. `name_test.go`: **959** lines, **55** `^func Test`, **56** top-level funcs, **0** `^func Fuzz`; non-colliding negatives at `:141` and `:956`. **Exactly FIVE non-test `ExtractTags` call sites repo-wide** — `name.go:371` plus `internal/statssink/{label.go:38, dogstatsd.go:82, graphite.go:67, otlp.go:189}`.

8. **⚠️ THE BAND: THE SPEC RE-DERIVED IT AND DID NOT ADOPT THE BRAINSTORM'S. SPEC band 10-12, budget 12.** The BRAINSTORM said 9-11; an independent agent derived **11-13** from an 11-task roster **without knowledge of the D-SPP-1 result**, and that roster's T5 was the dockerized probe — **already discharged at the SPEC** — while both of its ceiling contingencies (a per-arm task split; a ledger split) are **foreclosed** by the byte-mirror result and by D-SPP-3 landing at +0. **10-12 is that derivation, not a split-the-difference.** ⚠️ **RE-DERIVE IT AGAIN ANYWAY** (`reference_deferred_candidate_cost_restale`) — phase 78's PLAN refuted its own SPEC's band in BOTH directions. ⚠️ **CALIBRATION CORRECTED: it is TWO-for-three at or above the SPEC ceiling, not three-for-three** — 76: 7-9 -> **9** (at); 77: 11-13 -> **12** (*inside*, one below); 78: 7-9 -> PLAN 10 -> **10** (above). **If your re-derivation disagrees, the disagreement IS the PLAN's headline — say so rather than reconciling silently.**

9. **⚠️ THE STANDING GATE HAZARDS — COUNT 14.** A full-suite recipe **WITHOUT `-v` is VACUOUS** · a sha256 roster built from one tree is **desynced BY CONSTRUCTION against a DELETED file** · `gofmt -l` **NEVER exits non-zero** (gate on OUTPUT) · `go doc -all <A> <B>` **silently swallows arg2** · a **`+0 exported symbols`** gate over an EMPTY package goes RED on a CORRECT tree · a **RANGE** gate cannot detect anchor drift · a roster's naive `[ -f ] || continue` **exits 0 on a DELETED file** · a **count-only** stat guard **PASSES a build with BOTH names wrong** · a `-run` no-match **exits 0** with `[no tests to run]` · a `--- PASS` tally over a package with sibling tests **exceeds the fixture denominator** · a stat-delta claim **cannot be discharged by guards scoped to another package** · a **stderr-VOLUME** assertion **passes on the hang** · `golangci-lint` runs **`misspell` with `locale: US`** — ⚠️ **LIVE this row (it edits Go comments AND an error string); it FIRED at the SPEC on *"signalled"*. Do not paste SPEC prose into code** · **a harness's exit code is not the command's** — capture `INNER_EXIT`.

10. **⚠️ THE FULL 120-FIXTURE DIFFERENTIAL IS MANDATORY** — `internal/stats` links into `cmd/envoy-go`, built at **THREE** sites: `test/differential/harness.go:240`, `:594`, and `test/conformance/h2spec/h2spec_test.go:210` (`TestH2Spec` entry `:30`). ⚠️ **h2spec is a FOURTH consumer of the same binary and is NOT covered by `./test/differential/`** — run it explicitly rather than excluding it silently. Budget **~400-420 s** per green attempt; `-race` is a **SECOND** run, not a substitute. **Verified at this tip: fixture dirs 120, blank imports 120, `comm -3` EMPTY.** The mandated recipe:
    ```sh
    ( go test ./test/differential/ -count=1 -v > "$SCRATCH/full.log" 2>&1; echo "INNER_EXIT=$?" )
    grep -cE '^    --- PASS: TestDifferential/' "$SCRATCH/full.log"          # want 120
    grep -E  '^    --- (FAIL|SKIP): TestDifferential/' "$SCRATCH/full.log"   # want EMPTY
    grep -c  'no driver registered for fixture' "$SCRATCH/full.log"          # want 0
    grep -o  'TestDifferential/[^ ]*' "$SCRATCH/full.log" | sed 's|TestDifferential/||' | sort -u \
      | comm -3 - <(ls -1 test/fixtures/ | grep -E '^[0-9]{4}[a-z]?-' | sort)  # want EMPTY
    ```
    **The `comm -3` cross-check is the load-bearing gate, not the raw count** — 120 with one fixture renamed and another skipped still reads 120.

11. **⚠️ ADR + DOCUMENT CONVENTIONS.** **ADR-0301 §Context is LANDED**; the PLAN touches `DECISIONS.md` **only if** it must correct a §Context paragraph in place (the ADR-0297 ¶7/¶9 precedent, **leading with what survives**). §Decision + §Consequences append **IN PLACE at the IMPL** (**ADR-0044-as-used**; ⚠️ ADR-0044 does **not** itself contain that discipline). Next-free is **ADR-0302**. Block shape verified at the SPEC close: em-dash **U+2014** byte-identical to ADR-0300's, **1** STATUS blockquote, **1** `### Context`, **0** `### Decision`/`### Consequences`, **0** `---`, **13** §Context paragraphs. ⚠️ **THE DOCUMENTED ADR RANGE-EXTRACTION HAZARD IS REFUTED AT THIS TIP** — `^## ADR-0107` matches **1** line (`:4304`), `:4858` begins `##` at byte 0, **zero** lines in the file begin space-then-`#`. **`^## ADR-` is a safe anchor.** *(ADR-0209 has no heading by design; `## ADR-0127 v2:` at `:5973` is the sole heading matching neither form.)* ⚠️ **CITE THE SIX-GATE CORRECTLY: `BOOTSTRAP_PROMPT.md` §7.5, heading `:357`, gates (a)-(f) at `:360-365`, closing sentence `:367`**, at the REPO ROOT with the `.md` extension — re-verified at this SPEC. **ADR-0106 does NOT define the six-gate** (it governs the SOLE-leg property). ⚠️ **AND ADR-0045 does NOT state the ~25-task / ~1500-LoC split gate — it QUOTES it** (`DECISIONS.md:1466`, quote at `:1475`) from `BOOTSTRAP_PROMPT.md`, which carries the string at `:225` and `:472`. **Citing the figures to ADR-0045 alone is a laundered cite.**

12. **⚠️ THE PLAN CREATES `PLAN.md` **AND** `PROGRESS.md`.** `PROGRESS.md` is born at the PLAN — verified across the whole project (`grep -rln '^# BRAINSTORM record' docs/envoy-go/phases/*/PROGRESS.md` ⇒ **0** files across 120 phase dirs). The PLAN's file set is `PLAN.md` (NEW) + `PROGRESS.md` (NEW) + `STATE.md` + `next-prompt.txt`, optionally `DECISIONS.md`. **`ROADMAP.md` and `BEHAVIOR_CONTRACT.md` stay BYTE-UNTOUCHED at the PLAN** (row 79 stays `in-progress`); the row flips to `done` at the IMPL only.

13. **⚠️ ANCHORS AT THIS TIP** (re-derive in your own worktree; never copy): `DECISIONS.md` **17628** (tail **ADR-0301 PROPOSED**, next-free **ADR-0302**) · `ROADMAP.md` **227 lines / 111 data rows** · `BEHAVIOR_CONTRACT.md` **5762** · `STATE_HISTORY.md` **414** · `STATE.md` §Current bullets at **`:15-21`**, closing `---` at **`:23`**, §Recent lineage bullets at **`:46 :48 :50 :52 :54`** (AT the five-cap — **a close that adds a bullet must evict the oldest to `STATE_HISTORY.md`**). ⚠️ **`ROADMAP.md:209` IS A ~46 kB SINGLE LINE CARRYING SIX DISTINCT TASK BANDS** (`5-7`, `9–13`, `10–14`, `7–11`, `8–12`, `11-14`); the projection band **`11-14`** sits at character offset **~45763** of a **45 959**-character line. **A line-scoped grep returns the WRONG one — locate it POSITIONALLY.** ⚠️ **AND `cut -c` IS BYTE-BASED WHILE THE LINE IS MULTIBYTE UTF-8** — a byte-offset slice truncates mid-word and reads as a corrupted file. **The controller made both errors at the SPEC and caught them by re-reading with character offsets; both are recorded in SPEC §14.**

14. **⚠️ DOCUMENTARY DEFECTS, STILL NAMED AS DEFERRALS AND STILL DELIBERATELY NOT FIXED.**
    - ⚠️ **`ROADMAP.md`'s row 78 is the ONLY MALFORMED ROW of 111** — `NF=8` with **2 877 characters** of IMPL summary in field 8 and **no trailing `|`**, so GFM drops the entire phase-78 IMPL summary from the rendered table. **NOT FIXED**: the §Schema invariant at `:18` forbids editing a closed row's `summary` cell. **Row 79's shape is canonical and was re-verified at the SPEC: 7 pipes, `NF=8`, field 8 EMPTY, pipe-terminated.**
    - **The documented PUBLIC IMPORT PATH DOES NOT EXIST** — **36 live occurrences across SEVEN files** (`DECISIONS.md` 11, root `PROGRESS.md` 8, `ROADMAP.md` 7, `next-prompt.txt` 4, `STATE_HISTORY.md` 3, `BEHAVIOR_CONTRACT.md` 2, one fixture README 1). **ZERO in compiled Go and `go build ./...` exits 0** under the real path `github.com/pgdad/envoy-go`. ⚠️ **`DECISIONS.md:142` is an ADR that DECIDES the wrong path, never superseded.** ⚠️ **All 8 root `PROGRESS.md` hits are PASTED `go test` OUTPUT LINES** — rewriting recorded output is a different doctrinal act from correcting a statement. ⚠️ **Fix by PATTERN `esalaine/envoy-go` (bare), NOT the `/validate` form.** **SPEC 8-10, expect 10-11.**
    - **A mechanical stat-surface count** to replace the documentary 1207. **210** registration sites (not 214); **27**-row ledger (not 17) at `BEHAVIOR_CONTRACT.md:4962`, rows `:4966`-`:5018`, plus a 28th detached at `:805`; **5** `statroster.New` fan-out sites (zookeeper alone **201** names); **2** method-value registrations invisible to any regex. ⚠️ **The +3 unattributed gap CONFIRMED EXACTLY** (`:5008` ends 1198; `:5010` opens 1200; `:5012` ends 1200; `:5014` opens 1201). ⚠️ **NEW at the SPEC: the non-H2 PARALLEL TOTAL was ABANDONED three rows ago** — the newest `non-H2` figure is `:5010` (phase 47.1), while the tail rows `:5014`/`:5016`/`:5018` carry none, **so a future `+1` row appends a single-absolute row and need not touch a non-H2 total.** **8-11 tasks, pure meta-work.**
    - **Symmetric bind hardening** — `mustAllocatePort()` in **10** drivers (not 42); the racy helper copy-pasted under FOUR names, ~15 definitions. *"Fixture BACKENDS have no retry"* is OVERBROAD: `fixture.TCPEcho`/`HTTPEcho` hold the listener open for the test's lifetime and cannot close-then-rebind race; **only `fixture.HTTPSH2` (`runner_test.go:288-293`)** does. **Still rejected: it CANNOT be verified by a green suite run** (`0e9cc680` needed `-count=6`).
    - **Opening the gRPC family — HARD-BLOCKED.** `\.RunEncodeTrailers(` ⇒ **0** non-test, 1 test; ⚠️ **the DECODE side is equally dead** — `\.RunDecodeTrailers(` is also 0 non-test, 1 test. **The entire trailers pair is unreachable from production code.** Both charter-satisfying candidates sit behind it: **16-22+ tasks.**
    - **The WASM row-summary rider stays DECLINED on the merits** — `ROADMAP.md:76` declares phase 25 the FINAL §9 HTTP-filters-family row and rows 25.x use "family" 23 times to declare it CLOSED, so registering WASM is a **doctrine adjudication against a landed closure statement**, not a string edit — and writing the marker would silence check (3) **BY MENTION**. **Do not re-adopt it as cheap.**
    - **UNRESOLVED:** the *"plus two more in the same file"* half of the `BEHAVIOR_CONTRACT.md` stale-cite claim (`internal/tls/config.go:272`, `internal/filter/http/chain.go:19` flagged, **not audited**).
    - `BEHAVIOR_CONTRACT.md:501` calls the `wasm.` arm a *"NEW SN9 flattening rule"*, which **COLLIDES with ADR-0118's actual SN9**.
    - **`ADR-0299`'s STATUS line still reads `PROPOSED`** although its §Decision and §Consequences landed at the phase-77 IMPL. **Recorded, not fixed.**
    - **`BEHAVIOR_CONTRACT.md` carries 14 `### Does not yet apply to` headings**, not 15.
    - ⚠️ **`internal/admin/stats.go:12-22`'s parenthetical is STALE** — *"(the redis. Prometheus tag-extractor arm is 32.2)"*; that arm LANDED (`name.go:323`). **This one IS in scope for the IMPL** (SPEC §3.5).
    - ⚠️ **`STATE.md` §Project counts REMAINS STALE ON FOUR AXES** — fixtures **119** (real 120) · stat surface **1205** (lineage 1207) · DECISIONS tail **ADR-0298** (real **ADR-0301**) · and `:33`'s *"next-free REFERENCE port is `10450`"*, **REFUTED** by `0118/driver/driver.go:26-35`'s own `refListenerPort = 10118` (right for the TLS band, wrong as a general claim; **next-free is `10119`**). **Deliberately NOT repaired — repairing a count by editing the sentence that states it is how the ADR-0296/0297 species starts. Anchor on §Current, which IS live.**

15. **⚠️ A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX — the THIRTEENTH consecutive stage at which this held.** The index is `~/.claude/projects/-home-esa-git-envoy-go/memory/` (exactly four flake files). Live hazards: the full-suite startup flake (`subject ready: EOF` **and** `bind: address already in use`, both failing **BEFORE any assertion**, the latter a **PANIC that can abort the whole binary**, firing **MORE readily under `-race`** and as the fixture count grows — now **120**; hardened at `0e9cc680` **for the SUBJECT PROXY ONLY**) · `reference_sds_init_fetch_timeout_dial_budget_flake` — **TWO packages** · the PRE-EXISTING `internal/cluster` `-race` outlier flake (`TestOutlierDetector_ConcurrentEjectExactlyOnce`) · ⚠️ **`internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery` FIRED AT THIS SPEC and is now EVIDENCED rather than carried as prose**: `go test ./internal/... -count=1` exited 1 on attempt 1, `go list -deps` shows that package has **ZERO** dependency on `internal/stats`, the isolated re-run was green and attempt 2 gave 70 `ok`. **Still UNINDEXED**, as is `internal/httpclient TestOptions_ZeroValue_NoOpDefaults`. **Isolate-re-run, then state the classification AND its evidence.** ⚠️ **`0061-lb-ring-hash` is NOT a live flake — a spread failure there is a FINDING.**

16. **Counts at this tip (re-run MECHANICALLY in your worktree; never copy them):** fixtures **120** (`ls -d test/fixtures/[0-9]*/ | wc -l`; faithful predicate `^[0-9]{4}[a-z]?-`; next-free **0119**) / fuzzers **55** (⚠️ the `internal/`-vs-repo-wide pair is **NOT discriminating** — both return 55; use the append-a-`func Fuzz` control **on a scratch COPY**) / **stat surface 1207**, ⚠️ **DOCUMENTARY and CONFIG-CONDITIONAL — assert the DELTA** / **BackendKind tail 38** (⚠️ a TAIL VALUE — 39 constants, 0-38; **do NOT "fix" 38 to 39**) / go.mod modules **2** (⚠️ the phase-61.2 lineage figure — **NOT a repo total**; the single `go.mod` requires **67**; **do NOT "fix" 2 to 67**) / internal packages **73** / blank imports in `runner_test.go` **120**, ⚠️ **anchor on the FULL `^\t_ "github.com/pgdad/envoy-go/test/fixtures/` prefix — a naive `^\t_ ` gives 126** / DECISIONS tail **ADR-0301 PROPOSED** (next-free **ADR-0302**) / next-free reference port **10119** (⚠️ the convention IS `10<fixture index>`; `grep -rn '10119' test/` ⇒ 0, NC `10118` ⇒ 5).

## What THIS session does — the phase-79 PLAN on `stats-prometheus-projection`

One stage per session. Write `docs/envoy-go/phases/79-stats-prometheus-projection/PLAN.md` **and create `PROGRESS.md`**, deriving the task roster from `SPEC.md` §9 (**10-12, budget 12**) with each task carrying its own gate. Roll `STATE.md` §Current pointer **IN PLACE** (ADR-0288 — ⚠️ **§Recent lineage is AT its five-entry cap; a close that adds a bullet must evict the oldest to `STATE_HISTORY.md`**) and this file. **`ROADMAP.md` and `BEHAVIOR_CONTRACT.md` stay BYTE-UNTOUCHED.**

⚠️ **THE PLAN'S JOB IS TO RE-DERIVE, NOT TO RATIFY.** Phase 78's PLAN refuted its own SPEC's arm-θ demonstration and its band in both directions; this SPEC refuted **nine** BRAINSTORM claims and two of its own. **A PLAN that only reasons will be refuted at the IMPL.** ⚠️ **PLAN break INSTRUCTIONS don't compile** (`reference_plan_break_instructions_dont_compile`) — where a break arm is specified, substitute an equivalent that does, and **confirm WHICH assertion fired** (`reference_deliberate_break_wrong_assertion`). ⚠️ **A break arm's INJECTION SITE is itself a claim** (`reference_break_arm_injection_site_is_a_claim`) — vary it.

Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** (`reference_parallel_subagents_private_scratch`), **BANDED PORTS** — ⚠️ **the band must sit OUTSIDE the differential harness's 20000-31007 allocation range** — and they **must tear down docker containers BY NAME, never by an `ancestor`/image filter** (`reference_parallel_agents_shared_machine_namespaces`). ⚠️ **If two agents share ONE worktree they must stage by EXPLICIT PATH; a `git commit -am` sweeps in a sibling's uncommitted work.** ⚠️ **AND THEY MUST NOT RUN AN UNSCOPED `git restore`.** Per-task `gofmt` + `golangci-lint` on touched packages (`feedback_pertask_gofmt_lint`). ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch.**

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. ⚠️ **ASSUME IT FIRES EVERY SESSION — it has been treated as live for SEVENTEEN consecutive sessions.** **Use `git -C <abs-worktree-path>` for every git command, prefer absolute paths over `cd`, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — Current-pointer section (TRUSTWORTHY; ADR-0288 discipline — ⚠️ **anchor greps on `^- **<field>`**, not bare; all seven fields verified singleton at the phase-79 SPEC close, bullets at `:15-21`). ⚠️ **§Project counts is STALE ON FOUR AXES** (item 14) — do not anchor on it.
2. `docs/envoy-go/phases/79-stats-prometheus-projection/SPEC.md` — **the input to this stage.** §1.1-§1.3 are the three headlines; §1.4 is the 16-row drift ledger; §3 disposes the D-SPP docket one-for-one; §9 is the task roster and the band disagreement; §14 is the adversarial-pass record including two controller self-corrections.
3. `docs/envoy-go/phases/79-stats-prometheus-projection/BRAINSTORM.md` — ⚠️ **READ IT ONLY THROUGH THE SPEC'S §1.4 DRIFT LEDGER.** Nine of its load-bearing claims are refuted or corrected; a PLAN that cites it directly will inherit a wrong figure.
4. `docs/envoy-go/phases/78-boot-panic-visibility/PLAN.md` — the shape a PLAN takes in this lineage, and how it records a refutation of its own SPEC.
5. `docs/envoy-go/DECISIONS.md` — **ADR-0301** (§Context, this row) · **ADR-0300** (the NEW-form template; §Consequences (ii) carries the corrected injected-defer residual) · **ADR-0045** (⚠️ it QUOTES the split gate, it does not state it) · **ADR-0288** (`:17010`, the roll-IN-PLACE rule) · ADR-0044 (⚠️ it does **NOT** contain the Context-draft discipline — use `ADR-0044-as-used`) · **ADR-0106** (⚠️ the SOLE-leg property, **not** the six-gate) · **ADR-0264** (metrics_service label mapping, which `internal/statssink/label.go` implements).
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — especially **`reference_positive_arm_cannot_catch_overfiring`**, **`reference_liveness_break_needs_failing_baseline`**, **`reference_deliberate_break_wrong_assertion`**, **`reference_probe_input_is_a_claim`**, **`reference_probe_must_discriminate`**, **`reference_independent_probes_can_share_a_blind_axis`**, **`reference_stat_count_guard_blind_to_rename`**, **`reference_gate_command_negative_control`**, **`reference_empty_output_is_not_a_zero_result`**, **`reference_deferred_candidate_cost_restale`**, **`reference_plan_break_instructions_dont_compile`**, `reference_sample_is_not_an_audit`, `reference_a_drift_correction_is_itself_a_claim`, `reference_stale_cite_recurs_fix_by_pattern`, `reference_fatalf_makes_assertions_unreachable`, `reference_golangci_misspell_locale_us`, `feedback_brief_citations_not_evidence`, `reference_next_prompt_tracked_despite_gitignore`, `feedback_git_worktrees`, `feedback_execution_style`, and **`reference_bash_cwd_reset_commits_to_main`**.
