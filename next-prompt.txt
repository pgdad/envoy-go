START a fresh cold-start session for envoy-go — phase 26 (`network-filter-chain-and-rbac`) PARENT SPEC is DONE; author the 26.1 sub-phase SPEC.

This is a per-sub-phase SPEC cold-start. **Phase 26 PARENT SPEC is DONE + squash-merged to master + pushed to origin** (parent-SPEC squash `89fc507`; origin/master at the same after the stage-close follow-up). The parent SPEC resolved the BRAINSTORM §10 D1–D8 empirical pins IN-SESSION against Envoy v1.37.2 + go-control-plane v1.32.4, formalized the 3-way split surface-mapping, and anchored the ADR-0213 + ADR-0214 §Context drafts in DECISIONS.md. STATE.md points at `phase 26 parent SPEC done; awaiting 26.1 SPEC`, next-skill `superpowers:writing-plans` (scoped to 26.1 SPEC), next-free `ADR-0215` (DECISIONS.md tail at ADR-0214).

## What this session must do: author the phase-26.1 sub-phase SPEC

Per SKILL_ROUTING state 1 (sub-row 26.1 in ROADMAP, its directory does not yet exist), the next-skill is **`superpowers:writing-plans` scoped to 26.1 SPEC authoring** (`docs/envoy-go/phases/26.1-network-filter-chain-framework-and-echo/SPEC.md`), per the phase-22.1/25.1 per-sub-phase-SPEC precedent. A 26.1 BRAINSTORM is NOT needed — the parent BRAINSTORM + parent SPEC settled the design (Q1-Q6 locked; AMEND-A1..A11 anchored; ADR-0213+0214 §Context drafted). The 26.1 SPEC:
1. INHERITS the parent SPEC's §5 proto roster + §6 PARSE-REJECT roster + §7 stat surface + §8 fixture taxonomy + §11 empirical-pin block + §12 D-questions + §13 RATIFIED-PENDING items. It refines per-Task-level surface only; it does NOT re-execute the empirical pins.
2. Creates the `docs/envoy-go/phases/26.1-network-filter-chain-framework-and-echo/` directory + opens sub-row 26.1 (`planned → in-progress` per ADR-0106).
3. Anchors the 26.1 task breakdown for the NEW `internal/filter/network/` framework + `echo` + `direct_response` + the dual-dispatch wiring in `manager.go` + the `cmd/envoy-go/main.go` boot-wiring.

This session:
1. Reads STATE.md + the parent SPEC + the BRAINSTORM + ROADMAP rows 26/26.1 + BOOTSTRAP_PROMPT.md §5 + SKILL_ROUTING.md to confirm the parent-SPEC→26.1-SPEC routing.
2. Confirms baseline GREEN at master tip (`go build ./... && go vet ./... && golangci-lint run && go test -race -short ./...` + the differential/h2spec/conformance gates carried from 25.3 — the phase-26 parent SPEC is docs-only).
3. Invokes `superpowers:writing-plans` scoped to 26.1 SPEC authoring (in a worktree per the standing preference; the SPEC stage is doc-authoring, no production code).

## Phase 26.1 scope (settled at parent SPEC §3.2 + §4.1 + §4.2; ADR-0213 + ADR-0214)

26.1 `network-filter-chain-framework-and-echo` lands the NEW L4 read-filter chain framework + the family's first two trivial filters:
- **NEW `internal/filter/network/`**: the `ReadFilter` interface (`OnNewConnection() Status` + `OnData(buf, endStream) Status` → `Continue`/`StopIteration` — TWO-value enum per §11.5 D5, because L4 buffering is connection-level not filter-level); `ReadFilterCallbacks` (a `Connection()` accessor with `Write`/`Close`/local+remote addr/`RequestedServerName`/`DownstreamPrincipals`, `ContinueReading()`, and **`DynamicMetadata() *dynamicmetadata.Bucket`** — shaped NOW so 26.3 needs no revision, per the SPEC-BLOCKING D4 resolution AMEND-A5); the per-connection read-filter chain runner (sequential dispatch + connection-level read buffering on `StopIteration`, mirroring `internal/listener/listenerfilter/pipeline.go`); the per-connection runtime context (owns the `*dynamicmetadata.Bucket`); the freeze-after-boot threaded-constructor `*network.Registry` (mirrors `internal/listener/listenerfilter/registry.go` + ADR-0072/0079).
- **`echo`** (`envoy.filters.network.echo.v3.Echo`, EMPTY config) — `OnData` writes bytes back, drains, returns `StopIteration`.
- **`direct_response`** (`envoy.extensions.filters.network.direct_response.v3.Config` — note `Config`, NOT `DirectResponse`, per AMEND-A1; field `response` DataSource) — logic in `OnNewConnection`: write response + `endStream=true`, set ResponseCodeDetails `DirectResponse`, close `FlushWrite`, return `StopIteration`; no delay.
- **Dual-dispatch in `manager.go`** (per ADR-0214 + parent SPEC §3.2): the NEW read-filter chain dispatch is wired ALONGSIDE the existing terminal-filter path — a chain whose `filters[0]` resolves in the frozen `*network.Registry` uses the new chain; `tcp_proxy`/HCM keep the EXISTING `buildTerminalFilter`+`Handle` path UNTOUCHED (the migration is 26.2). This confines 26.1's blast radius to new code.
- **Boot-wiring in `cmd/envoy-go/main.go`**: construct `*network.Registry` → Register echo + direct_response → Freeze → thread into `listener.NewManagerWithBaseDirAndAllowH2C(...)`.
- **Fixtures (+3)**: echo cross-side + direct_response cross-side + a direct_response boot-reject (empty `response.specifier` PGV-mirror, per AMEND-A2). **Fuzzer (+1)**: the 35th project fuzzer (master tip = 34, per AMEND/§11.8 — REFUTES the BRAINSTORM's 35).
- **ADR-0213 + ADR-0214 §Decision/§Consequences bodies** land at 26.1 IMPL (§Context already drafted in DECISIONS.md at the parent SPEC).

Read-filter-ONLY (write-filter deferred + API-revision allowance, ADR-0213). NO per-route surface (§2.2 / D6).

## Key empirical pins ALREADY RESOLVED at the parent SPEC (DO NOT re-litigate; see parent SPEC §11)

- direct_response message = `Config` (AMEND-A1); echo empty + vacuous PGV (AMEND-A2); two-value Status enum + connection-level buffering + onNewConnection-eager-at-accept + ContinueReading-resumes-at-NEXT-filter (D5); echo/direct_response semantics (D7); no per-route (D6); D4 SPEC-BLOCKING resolved → conn-metadata sink REUSES `internal/dynamicmetadata/` at connection scope (NOT a new package), `ReadFilterCallbacks.DynamicMetadata()` shaped at 26.1 (AMEND-A5). The 26.1 SPEC inherits all of these.

## Project state at master tip (`89fc507`)

- **Master tip:** `89fc507` (the phase-26 parent-SPEC squash) + the stage-close follow-up (STATE SHA-fill + this next-prompt rewrite). Run `git log --oneline -1` to confirm the actual tip (the follow-up commit advances the tip one past `89fc507`; the caveat is expected). origin/master at the follow-up tip after push.
- **§9 Network-filters family OPEN:** parent row 26 `in-progress`; sub-rows 26.1/26.2/26.3 `planned`. 6 family candidates remain after phase 26 (redis, mongo, kafka_broker, thrift, zookeeper, sni_cluster).
- **Counts at master tip (carried from 25.3):** stat surface **132**; differential fixtures **41**; fuzzers **34** (NOT 35 — the parent SPEC §11.8 refuted the BRAINSTORM hypothesis); conformance 10/10; h2spec 53/53; DECISIONS.md tail **ADR-0214** (ADR-0213+0214 §Context drafts landed at the parent SPEC), next-free **ADR-0215**. Anticipated 26.1 deltas: fixtures 41 → 44; fuzzers 34 → 35; stats unchanged at 132; ADR-0213+0214 §Decision/§Consequences bodies land.
- **Toolchain:** Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); reference Envoy v1.37.2 (ADR-0008); go-control-plane v1.32.4 (ADR-0008).
- **Six gates GREEN at master tip** (the phase-26 parent SPEC is docs-only; code is unchanged since 25.3 IMPL phase-done).

## Read first (in order)

1. THIS FILE — the phase-26.1 SPEC cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 26 parent SPEC done; awaiting 26.1 SPEC`; next-skill `superpowers:writing-plans` (26.1 SPEC); next-free `ADR-0215`; last-commit `89fc507`.
3. `docs/envoy-go/phases/26-network-filter-chain-and-rbac/SPEC.md` — the PARENT SPEC (the §3.1 surface-mapping, §4.1 framework primitive, §4.2 echo+direct_response, §5/§6/§7/§8 rosters, §10 ADR map, §11 empirical-pin block, §12 D-questions, §13/§14/§15). The 26.1 SPEC inherits this.
4. `docs/envoy-go/phases/26-network-filter-chain-and-rbac/BRAINSTORM.md` — the settled design (Q1-Q6; §7 ADR numbering).
5. `docs/envoy-go/ROADMAP.md` — rows 26 / 26.1.
6. `BOOTSTRAP_PROMPT.md` §5 (state machine) + §4.1 STATE-update invariant + ADR-0004/0005 (autonomous SPEC/PLAN adaptation).
7. `docs/envoy-go/SKILL_ROUTING.md` — confirm state-1 → `superpowers:writing-plans` (SPEC) for sub-row 26.1.
8. `docs/envoy-go/DECISIONS.md` — ADR-0213 + ADR-0214 §Context drafts (tail); the registry/iteration/extraction ADR lineage (ADR-0038/0059/0071/0072/0079/0207).
9. The existing code the 26.1 SPEC pins against: `internal/listener/manager.go` (the dual-dispatch wiring target: `filterHandler`@44, `filterRegistry`@102, `buildTerminalFilter`@569, `Handle`@1005, `serveConnection`), `internal/filter/http/{types,registry,callbacks,chain}.go` (the HTTP framework being mirrored), `internal/listener/listenerfilter/{registry,pipeline,types}.go` (the closest structural analogue), `internal/dynamicmetadata/dynamicmetadata.go` (the REUSED sink), `cmd/envoy-go/main.go` (boot-wiring pattern @129-213).
10. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — execution-style + worktree + push + differential-asserter-dispatch + fixture-dispatch-constraint memories all apply to 26.1.

After the 26.1 SPEC is authored + reviewed + committed + squash-merged + pushed, advance STATE.md per BOOTSTRAP §5 and rewrite next-prompt.txt for the 26.1 PLAN cold-start.
