START a fresh session for envoy-go. **The phase-43 (per-protocol connection pooling) BRAINSTORM is COMPLETE and LANDED on master, and ROW 43 (`connection-pooling`) is `in-progress`.** Phase 43 is the FIFTH-and-FINAL Upstream-robustness-family row; it lands (i) the phase-41-DEFERRED `max_connections` + `max_pending_requests` enforcement over the connection-pool QUEUE the synchronous-acquire model lacks, and (ii) the H2 MULTIPLEX connection pool ADR-0056 deferred + EXPLICITLY named the upstream-robustness family to supersede. The BRAINSTORM settled a **PRE-AUTHORIZED BY-CONCERN 2-leg split**: **43.1 = the budget substrate + pending wait-queue** (ADR-0252) and **43.2 = the H2 multiplex pool** (ADR-0253; SUPERSEDES ADR-0056). The next step is the **43.1 (budget + pending queue) SPEC** — NOT another BRAINSTORM (the subject + ambition + split are settled). Confirm the latest master is the phase-43 BRAINSTORM completion commit via `git log --oneline -3` (a NEW sha — do NOT assume a literal value). Counts at tip (UNCHANGED at the BRAINSTORM): stat surface **1181** / fixtures **79** (tail `0077-hedge-on-per-try-timeout`) / fuzzers **42** / BackendKind tail **36** (`BlockingHoldResponder`) / DECISIONS tail **ADR-0251**, next-free **ADR-0252**.

## NEXT STEP — author the 43.1 (budget substrate + pending wait-queue) SPEC

Author `docs/envoy-go/phases/43-connection-pooling/SPEC.md` (the 43.1 leg) in the prevailing phase-41/42 14-section SPEC shape, get it spec-document-reviewer-APPROVED, and EXECUTE the §10 D-CP-* live-pin obligations IN-SESSION against `envoyproxy/envoy:contrib-v1.37.2` per `reference_docker_probe_bridge_network` (a shared bridge + a held/slow backend + a gateway Envoy with `circuit_breakers{max_connections, max_pending_requests}` routes; `/stats`-delta + `--mode validate` probes), then anchor the ADR-0252 §Context draft. The BRAINSTORM §10 enumerates the leg's pins; the load-bearing 43.1 ones:

- **D-CP-LIFECYCLE** — the precise `max_connections` PEND semantics (does it count pooled-idle H1 conns or only in-use; the wake fairness FIFO-vs-LIFO; the wait-queue-full-vs-permit-free race; the context-cancel-while-pending release point; whether the reference's pend is observable as a `cx_open`/`rq_pending_open` gauge transition BEFORE the overflow — the differential's mid-state barrier); the exact 503-on-queue-full emission point.
- **D-CP-STATS** — the emit-0 activations (`cx_open`/`rq_pending_open`/`upstream_cx_overflow`) + any NEW connection-pool stat names a live `/stats` scrape reveals (`upstream_rq_pending_active`/`_total`, `upstream_cx_connect_*`); the exact surface arithmetic (anticipated ~1195). **NB (a folded BRAINSTORM advisory):** `upstream_cx_overflow` is a SOFT-overflow (cap-hit/pend) counter, NOT a failure counter — the 503-on-queue-full path fires `upstream_rq_pending_overflow`; do NOT wire `upstream_cx_overflow` to the 503 path.
- **D-CP-REJECT** — confirm NO new config-reject surface (the phase-41 `circuit_breakers` rejects already cover `max_connections`/`max_pending_requests`; these are existing parsed fields gaining ENFORCEMENT, not parsing); anticipated NO new fuzzer.
- **D-CP-DIFFERENTIAL** / **D-CP-BACKEND** — the `0078-connection-pool-max-connections` cross-side fixture (REUSE `BlockingHoldResponder` 36): fill `max_connections=N` with N held conns (poll `cx_open==1`), fire M more → PEND (poll `rq_pending_open==1`), fire the (N+M+1)th → cross-side 503 on the DOWNSTREAM class + `upstream_rq_pending_overflow` delta; `/__release` → drain. Sleepless (release-barrier + poll-the-gauge per `reference_concurrency_differential_release_barrier`; sequential-per-side); assert the DOWNSTREAM 2xx/503 class NOT the over-counting upstream class (`reference_concurrent_attempt_downstream_class_assertion`).

(D-CP-H2POOL is a 43.2 obligation — defer it to the 43.2 SPEC.) After the 43.1 SPEC: the 43.1 PLAN (`superpowers:writing-plans`), then the 43.1 IMPL (`superpowers:subagent-driven-development`), then the 43.2 BRAINSTORM/SPEC/PLAN/IMPL legs.

## Architecture as brainstormed (43.1)

EXTEND the phase-41 `circuitBreaker`/`cbPriority` struct (`internal/cluster/circuitbreaker.go`) with per-priority `activeConnections`/`pendingRequests` atomics + the parsed `maxConnections`/`maxPendingRequests` budgets (phase 41 parses the `Thresholds` but stores NO fields for these two). The `max_connections` cap try-acquires a connection PERMIT at the connection-CREATION boundary (`Cluster.Dial` raw-dial + the `AcquireH1` pool-MISS slow path; an H1 pool HIT reuses an already-counted conn and needs NO permit, since idle pooled conns keep `upstream_cx_active` incremented). At the cap, the request does NOT 503 — it joins a BOUNDED wait-queue (`pendingRequests < maxPendingRequests`), blocking its own goroutine until a permit frees (conn Close via `connWithGauge`) or an idle conn returns (`PutIdleH1`); on the queue itself being full ⇒ fail-fast 503 + `upstream_rq_pending_overflow`. Byte-identical when no `circuit_breakers` (the nil-guard). The pending wait-queue is the project's FIRST standing upstream queue.

## Honor throughout (the discipline)

- **Lifecycle = one stage per session.** `feedback_git_worktrees` (a fresh worktree off master) · `feedback_subagents_no_push` (subagents commit LOCALLY; the controller squashes + pushes at stage-close) · `feedback_push_to_origin` (push after a clean squash on master with tests green) · `feedback_execution_style` (subagent-driven execution).
- **SPEC-specific:** author in the phase-41/42 14-section SPEC shape; EXECUTE the §10 D-CP-* pins live against `contrib-v1.37.2`; anchor the ADR-0252 §Context (defer §Decision/§Consequences to the 43.1 IMPL per ADR-0044); the FINAL ADR-0045 split-gate re-check (a leg may sub-split if its as-scoped size warrants — the 42.2a/42.2b precedent).
- **Process anchors:** ADR-0045 (the split-gate re-check at SPEC/PLAN); ADR-0106 + `reference_roadmap_split_phase_row_done` (row 43 flips `done` PER-LEG; the Upstream-robustness family CLOSES only when BOTH 43.1+43.2 land); ADR-0080 (strict-reject duplicates); `reference_docker_probe_bridge_network` (live reference-Envoy probes); `reference_concurrency_differential_release_barrier` + `reference_concurrent_attempt_downstream_class_assertion` (the concurrency-differential discipline).

## Read first (in order)

1. THIS FILE — the cold-start router (the phase-43 BRAINSTORM is DONE + LANDED; row 43 is `in-progress`; the next step is the 43.1 SPEC).
2. `docs/envoy-go/STATE.md` — active-phase `phase 43 (connection-pooling) BRAINSTORM done`; the count figures (1181 / 79 / 42 / 36 / ADR-0251, next-free ADR-0252).
3. `docs/envoy-go/phases/43-connection-pooling/BRAINSTORM.md` — the phase-43 charter (the by-concern split; the three forks; the §10 D-CP-* SPEC obligations; the §5 stat hypothesis; the §6 `0078`/`0079` fixtures; the §7 ADR-0252/ADR-0253 anticipations).
4. `docs/envoy-go/ROADMAP.md` — row 43 `in-progress` + the Upstream-robustness family-progress note (the family CLOSES at phase 43 phase-done).
5. `docs/envoy-go/phases/41-circuit-breakers/{SPEC,README}.md` — the SPEC shape to mirror + the AMEND-CB1 deferral this row discharges (`max_connections` pends, `max_pending_requests` needs the queue).
6. `docs/envoy-go/DECISIONS.md` — ADR-0248 (the phase-41 `circuitBreaker` struct 43.1 extends) + ADR-0056 (per-request fresh H2 dial — 43.2 SUPERSEDES it) + the ADR-0044/0045/0080/0106/0227 process anchors; next-free ADR-0252.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `reference_concurrency_differential_release_barrier`, `reference_concurrent_attempt_downstream_class_assertion`, `reference_docker_probe_bridge_network`, `reference_roadmap_split_phase_row_done`, `feedback_git_worktrees`, `feedback_subagents_no_push`, `feedback_push_to_origin`, `feedback_execution_style`).
