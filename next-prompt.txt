=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action. (The sentinel does NOT apply right now — phase 54 is chartered and mid-lifecycle (PLAN done, IMPL next), and Observability + Operational tooling both carry non-empty deferred-candidate lists.)

**STATUS AS OF 2026-07-08: phase 54 (`load-balancer-panic-threshold`) PLAN is DONE (row 54 STAYS `in-progress`, lifecycle-state 2 → 3). The NEXT action is the phase-54 IMPL.**

- **The phase:** `healthy_panic_threshold` as an independent construct — the EIGHTH and FINAL Load-balancing-family row (the family CLOSES at phase-done). Scope: the first-ever differential proof (`0097-lb-panic-threshold`) + FOUR evidence-driven corrections the SPEC's live probes pinned. The PLAN (`docs/envoy-go/phases/54-load-balancer-panic-threshold/PLAN.md`, FF-merge `08cb03cd`) is an 11-task bite-sized TDD spine — the IMPL EXECUTES it task-by-task.
- **The IMPL session** runs `superpowers:subagent-driven-development` (`feedback_execution_style`) in a **fresh worktree** (`feedback_git_worktrees`). Subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit, re-runs the six gates on the frozen HEAD, does the deliberate-break verification ITSELF (the `0097` breaks (a)/(b)/(c) `-count=1` per `reference_differential_break_protocol_count1` + the AMEND-PT2/PT3 unit scratch-reverts), and SQUASHES + PUSHES at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). Per-task gates: `gofmt -l` + `golangci-lint` + `go vet` + `go build ./...` (`feedback_pertask_gofmt_lint`). Honor `feedback_subagent_worktree_path_targeting` (PROGRESS.md pinned at `docs/envoy-go/phases/54-load-balancer-panic-threshold/PROGRESS.md`; worktree-relative paths) + `feedback_subagent_worktree_detach` (deliberate-break checks get a GIT HYGIENE `git restore`, no checkout-sha/amend; controller re-verifies the branch each task).
- **The PLAN's 11-task spine (decomposes SPEC §10; see PLAN.md for the full per-task TDD steps + complete code):**
  1. Baselines/anchors gate + PROGRESS.md (re-verify fixtures 98, fuzzers 52, stat 1200, BackendKind 38, DECISIONS tail ADR-0270; enumerate the sweep sets).
  2. **AMEND-PT1** floor + integer cross-multiply + **D-PT-STORE** type change (`clusterHealth.panicThreshold float64` → `panicThresholdPercent int`; `parsePanicThreshold` floors + returns int; `inPanic` = `100*avail < percent*total` strict `<`; sweep all 25 `newClusterHealth(…,0.5)→50` test sites + the `tierHealth` literal).
  3. Parse-path presence-semantics coverage through `panicGate` (disable-at-0 / exactly-at-threshold / absent-defaults-50%).
  4. **AMEND-PT4** standalone out-of-range `[0,100]` reject (**D-PT-REJECT-PLACEMENT**: `validatePanicThresholdRange` unconditional early in `buildCluster`; byte-stable ADR-0080; fires for a plain no-HC cluster).
  5. **AMEND-PT3** locality retrofit (**D-PT-TIERHEALTH-HOME**: relocate `tierHealth` to `health.go` + rename `priorityLeafFactory→healthLeafFactory` to `loadbalancer.go` shared + health-parameterize `newLocalityWeightedLB(WithRNG)` + the `manager.go` locality closure; per-locality children → `tierHealth(health)`, flat child keeps shared health; `subsetLB` UNCHANGED + a degraded-subset-still-flattens regression; the retrofit assertion scratch-revert-proven live). **REORDERED before AMEND-PT2.**
  6. **AMEND-PT2** double-increment fix (delete the outer `panicInc()` at `locality.go:144`; the shared-health flat child's `panicGate` provides the single increment → `2N→N`; delta==N scratch-revert-proven live, using the Task-5 `healthLeafFactory` signature + a real `roundRobin` flat child).
  7. `0097-lb-panic-threshold` three-arm fixture + driver (3 health-checked clusters × 5 hosts, degrade 2/5; A thr80 PANICS all-5-hosts / B absent-50 no-panic / C 60.9 floor-60 no-panic — the integer-truncation discriminator; poll `membership_healthy`→3 → warmup → drive → `StatsAsserter` + per-host tallies; register in `runner_test.go`; BackendKind tail STAYS 38).
  8. `0097` deliberate breaks (a hardcode-50 / b skip-degrade / c revert-floor→C-panics) `-count=1` + ≥20-run flake-free + `-race`.
  9. Full 99-dir differential + the six gates (incl. the FULL-package `-race` per `reference_full_suite_race_after_background_mutator` — health-check goroutines are background mutators).
  10. ADR-0271 body (§Decision/§Consequences, ADR-0044 — §Context at SPEC §13) + `BEHAVIOR_CONTRACT.md` panic-construct delta (ADR-0052).
  11. Completion bundle: STATE/ROADMAP advance, row-54 `in-progress → done`, the **Load-balancing family CLOSE**, counts (fixtures 98→99, DECISIONS ADR-0270→ADR-0271) — BY THE CONTROLLER at stage-close.
- **The four §12 D-PT-* questions are already RESOLVED in the PLAN** (D-PT-STORE / D-PT-REJECT-PLACEMENT / D-PT-TIERHEALTH-HOME / D-PT-UNIT-VS-DIFF — see the PLAN's D-question section); the IMPL implements them, does NOT re-litigate them. NO live probes at the IMPL (the SPEC executed all six pins; the PLAN needs none).

==============================================

## Honor throughout (the discipline, unchanged)
- **Lifecycle = one stage per session** (BRAINSTORM → SPEC → PLAN → IMPL). This session: the phase-54 IMPL ONLY; build it end-to-end, land it (squash + push), then roll this file forward (on IMPL-done, re-check the TERMINATION SENTINEL — phase 54 is the last chartered LB row; re-verify Observability + Operational-tooling deferred lists before any `stop`).
- **Subagent-driven execution** (`feedback_execution_style`) in a **fresh worktree** (`feedback_git_worktrees`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit, re-runs gates on the frozen HEAD, does deliberate-break verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). Per-task gates: `gofmt -l` + `golangci-lint` + `go vet` + `go build ./...` (`feedback_pertask_gofmt_lint`).
- **Process anchors:** ADR-0106 (per-leg rows) · ADR-0044 (ADR §Decision+§Consequences land at IMPL) · ADR-0045 (split soft gate — the PLAN's FINAL re-check re-confirmed NO split) · ADR-0080 (byte-stable rejects) · ADR-0052 (atomic six-gate completion bundle) · next-free **ADR-0271** (§Context DRAFT anchored in SPEC §13; body lands at IMPL Task 10).
- Re-verify baselines fresh at cold-start — do NOT trust any count in this file without re-running the baseline commands (`ls -d test/fixtures/[0-9][0-9][0-9][0-9]* | wc -l` → expect 98, `grep -rh '^func Fuzz' --include='*.go' --exclude-dir=.worktrees . | wc -l` → expect 52, DECISIONS tail HEADER → `## ADR-0270` [ADR-0271 appears only as a forward-reference string], stat surface → 1200) against current HEAD.

## Read first (in order)
1. THIS FILE — the cold-start router (phase-54 IMPL is next).
2. `docs/envoy-go/phases/54-load-balancer-panic-threshold/PLAN.md` — the IMPL's direct input: the Global Constraints, the D-question resolutions, the File Structure, the 11 per-task TDD sections (complete code + exact commands + commit messages), the Self-review + the FINAL ADR-0045 re-check.
3. `docs/envoy-go/phases/54-load-balancer-panic-threshold/SPEC.md` — the authoritative source behind the PLAN (§3 the four corrections + exact source citations, §6 the reject roster, §8 the `0097` fixture design, §11 the D-PT1..D-PT6 live evidence, §13 the ADR-0271 §Context DRAFT).
4. `docs/envoy-go/STATE.md` — the active-phase header (`phase 54 PLAN done`) + full history.
5. `docs/envoy-go/ROADMAP.md` — row 54 `in-progress`; the Load-balancing family section (CLOSES at phase-done).
6. `docs/envoy-go/phases/53-load-balancer-priority/PLAN.md` — the shape template the phase-54 PLAN mirrors; its `tierHealth`/`priorityLeafFactory` are directly load-bearing for AMEND-PT3's factory change (the phase-54 IMPL relocates + shares them).
7. The as-built source the PLAN's tasks touch: `internal/cluster/health.go` (`inPanic`/`parsePanicThreshold`/`panicGate`/`clusterHealth`/`newClusterHealth`), `internal/cluster/locality.go` (`localityWeightedLB.Pick` + the constructors + the factory closure), `internal/cluster/priority.go` (`tierHealth`/`priorityLeafFactory`), `internal/cluster/loadbalancer.go` (`loadBalancer`/`roundRobin`), `internal/cluster/manager.go` (the three `parsePanicThreshold` sites + the three wrap closures), `internal/cluster/subset.go` (`leafFactory`, unchanged), `test/fixtures/0096-lb-priority/driver/driver.go` (the `0097` harness template), `test/differential/runner_test.go` (the blank-import aggregator).
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — especially `reference_percent_threshold_integer_truncation` + `reference_percent_cap_cross_multiply` (the AMEND-PT1 discipline), `reference_health_check_propagation_warmup` (the `0097` convergence+warmup gate), `reference_round_robin_offset_randomized`, `reference_fixture_workload_constant_desync`, `reference_full_suite_race_after_background_mutator`, `reference_differential_*`, `feedback_*`.
