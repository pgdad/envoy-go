START a fresh session for envoy-go. **A brainstorm DIALOGUE for phase 42 (`retries + hedging`) already happened (2026-06-19) — the subject is CHOSEN and ALL forks are SETTLED — but the `BRAINSTORM.md` charter was NEVER WRITTEN.** Nothing was committed: the tip is UNCHANGED at `d62806ae` (the cold-start-router doc) on top of the phase-41 IMPL squash `7f3c7c15` (confirm via `git log --oneline -5`). **The next step is to WRITE the phase-42 BRAINSTORM charter from the settled decisions below** (`superpowers:brainstorming` is effectively mid-checklist: forks DONE → write the doc → spec-document-reviewer loop → commit docs-only), NOT to re-pick the subject or re-brainstorm. Counts at tip (UNCHANGED — this is a pre-doc state): stat surface **1163** / fixtures **76** / fuzzers **42** / BackendKind tail **36** (`BlockingHoldResponder`) / DECISIONS tail **ADR-0248**, next-free **ADR-0249**.

## What was SETTLED in the brainstorm dialogue (do NOT re-litigate — the human answered every fork)

**Subject (human-picked):** **retries + hedging** — the FOURTH Upstream-robustness-family row (after active-HC @39 / outlier @40 / circuit-breakers @41). Picked over the other remaining candidate (per-protocol connection pooling) + the 3 health-gated Load-balancing candidates {locality-weighted, priority, panic}.

**Row + split (Fork 1 — human picked "pre-authorized split"):** ROADMAP `42 | retries-and-hedging | 41 | in-progress`, a flat family row WITH a **pre-authorized 42.1/42.2 by-feature split** (the phase-36/38/39 valve):
- **42.1 = the retry loop** — `RouteAction.retry_policy` (route.v3.RetryPolicy, field 9; VirtualHost field-16 fallback): `retry_on` + `num_retries` + `retry_back_off` + **retry_budget activation**.
- **42.2 = hedging** — `HedgePolicy` (RouteAction field 27: `initial_requests`/`additional_request_chance`/`hedge_on_per_try_timeout`) + the project's FIRST **`per_try_timeout`** (RetryPolicy field 3), reusing the 42.1 attempt substrate concurrently.
- `request_mirror_policies` (RouteAction field 30, shadow traffic) **DEFERRED** to a future row.

**42.1 architecture (ADR-0249; possibly +ADR-0250 for retry_budget — anticipate 1–2):** a `retryExecutor` WRAPPING the EXISTING single-attempt `doH1ClusterAction` (`router.go:588`) / `doH2ClusterAction` (`router_h2.go:57`). The wrapped call ALREADY buffers the full response into `ActionResponse{Status,Headers,Body,Close}` (`router.go:119`) → the loop classifies `Status` against a parsed `retry_on` bitset; on a retriable outcome with attempts + budget remaining it sleeps the exponential backoff, re-picks via the LB, and replays the ALREADY-buffered request body (fresh `bytes.Reader` — the body is fully buffered at `connection.go:568-600`, ADR-0076 1 MiB cap; over-cap ⇒ non-retriable, a SPEC pin). Each attempt re-runs circuit-breaker admission (`TryAcquireRequest`) + `RecordUpstreamResult` (so BOTH circuit breaking and outlier detection see EVERY attempt). A `retryPolicy` struct carried on `clusterRouteAction` (`hcm/actions.go:201`), parsed in `buildRouterAction` (`hcm/config.go:536`). **Byte-identical when no `retry_policy`** (nil-guard). ZERO new package (retry executor in the router pkg; parse in hcm; budget activation in `internal/cluster`).

**retry_budget activation (Fork: human picked "activate" — the phase-41 handoff):** the phase-41 `circuitBreaker` gains a cluster-level atomic active-retry counter; a retry try-acquires a budget slot (`max(min_retry_concurrency, budget_percent% × active requests)`) before each attempt; on exhaustion ⇒ no retry + `upstream_rq_retry_overflow`++ + `rq_retry_open`=1 — the phase-41 EMIT-0 stats (`circuitbreaker.go:78,83`) flip **LIVE**. Static `num_retries` caps independently ⇒ `upstream_rq_retry_limit_exceeded` on exhaustion.

**Backoff (Fork: human picked "include"):** exponential backoff (`retry_back_off` base/max interval, reference default ~25ms/250ms, full jitter). The differential is COUNT-based, so backoff (delay-only) never perturbs determinism.

**retry_on subset (self-answered; SPEC pins exact enforced set):** MVP-enforce the connection/5xx-class tokens (`5xx`, `gateway-error`, `connect-failure`, `reset`, `retriable-status-codes` + `retriable_status_codes[]`); parse-accept the rest (`retriable-headers`, `envoy-ratelimited`, gRPC tokens). `retry_on` is freeform (NO PGV) ⇒ thin reject surface.

**Response flags (self-answered — the phase-41 CB4 precedent):** the `RX`/`URX` retry access-log flags are a RECORDED DEPARTURE (envoy-go has NO response-flags plumbing — `RESPONSE_FLAGS` hardcoded to `-`). The differential asserts stats + final status, NEVER the access-log line.

**Differential (Fork: human picked "reuse per-host 503 topology" — NO new BackendKind):** `0075-retry-…`: a cluster with one `HTTP503Responder` host (BackendKind 35, `PerHostBackendKind`) under deterministic round-robin so the first pick hits the 503 host ⇒ retry re-picks a healthy host ⇒ 200; cross-side assert final 200 + `upstream_rq_retry` delta + `upstream_rq_total`. A fully-deterministic EXHAUSTION arm (single 503 host, `num_retries=N` ⇒ final 503 + `upstream_rq_retry==N` + `upstream_rq_retry_limit_exceeded==1`) lands as a second arm (one-vs-two dirs a SPEC pin). Sleepless/count-based; RR-determinism cross-side is a SPEC live-pin.

**Count envelope at the 42.1 IMPL** (from tip 1163 / 76 / 42 / 36 / ADR-0248): stat surface **1163 → ~1168** (phase-41 `rq_retry_open` + `upstream_rq_retry_overflow` flip LIVE [no count delta] + ~+4–6 new retry counters: `upstream_rq_retry` / `_retry_success` / `_retry_limit_exceeded` / `_retry_backoff_exponential` / `_retry_backoff_ratelimited` — exact roster a SPEC pin D-RT-STATS) · fixtures **76 → 77 (or 78)** · fuzzers **42 (or 43** — candidate `retry_policy` config-parse fuzzer**)** · BackendKind **36 UNCHANGED** · DECISIONS tail **ADR-0248 → ADR-0249 (or 0250)** · **ZERO** new packages / go.mod modules.

**Proto VERIFIED at the brainstorm** (in the EXISTING `go-control-plane/envoy v1.32.4` cache — ZERO new module): `RouteAction.{retry_policy=9, hedge_policy=27, request_mirror_policies=30}`; `VirtualHost.{retry_policy=16, hedge_policy=17}`; `RetryPolicy.{retry_on=1, num_retries=2, per_try_timeout=3, retry_back_off=8, retriable_status_codes=7, retriable_headers=9, retriable_request_headers=10, per_try_idle_timeout=13}`; `RetryPolicy_RetryBackOff.{base_interval=1, max_interval=2}`; `HedgePolicy.{initial_requests=1, additional_request_chance=2, hedge_on_per_try_timeout=3}` (PGV: `initial_requests >= 1` the only real arm; `retry_on` unvalidated).

## NEXT STEP — write the phase-42 BRAINSTORM charter

Author `docs/envoy-go/phases/42-retries-and-hedging/BRAINSTORM.md` in the 12-section phase-41 shape (`docs/envoy-go/phases/41-circuit-breakers/BRAINSTORM.md` is the section-for-section template), reframed as the FOURTH Upstream-robustness row: a retry-loop WRAPPER over the existing single-attempt router seam (NOT a new cluster-accounting struct), reusing the buffered-body + circuit-breaker-admission + `RecordUpstreamResult` seams, activating the phase-41 retry_budget substrate, with a count-based per-host-503 differential. Enumerate the §10 empirical pins **D-RT-PROTO / D-RT-RETRYON / D-RT-STATS / D-RT-BUDGET / D-RT-DIFFERENTIAL** (executed IN-SESSION at the 42.1 SPEC against `envoyproxy/envoy:contrib-v1.37.2` per `reference_docker_probe_bridge_network`; anchor the ADR-0249 §Context draft). Record the 42.1/42.2 split as pre-authorized (the final ADR-0045 re-check at the SPEC/PLAN). Then dispatch the spec-document-reviewer (per the brainstorming-skill loop), fix to approval, and commit docs-only. ROADMAP row 42 registers `in-progress`; ALL counts stay UNCHANGED at the BRAINSTORM commit. The SPEC (42.1) follows in a later session.

## Honor throughout (the BRAINSTORM discipline)

- **Lifecycle = one stage per session.** `feedback_git_worktrees` (isolated work in a worktree) · `feedback_subagents_no_push` (task subagents commit locally only; the controller squashes + pushes at stage-close) · `feedback_push_to_origin` (after a clean local commit on master with tests green, push without asking) · `feedback_execution_style` (subagent-driven over inline).
- **ADR/process:** ADR-0044 (§Context at the SPEC, §Decision/§Consequences IN-PLACE at the IMPL) · ADR-0045 (the split-gate) · ADR-0052 (the six-gate at the IMPL) · ADR-0080 (envoy-go's own byte-stable rejects) · ADR-0106 + `reference_roadmap_split_phase_row_done` (a split-phase row flips `done` only when ALL legs land; NO parent rollup) · ADR-0227 (`contrib-v1.37.2`).
- **At the BRAINSTORM:** `reference_docker_probe_bridge_network` (any live reference-Envoy probe runs over a shared Docker bridge network + a STRICT_DNS backend hostname; verify the decode/request path ran) · `reference_proto_roster_extraction_digits` (digit-inclusive regexes when scraping a proto/stat roster).

## Read first (in order)

1. THIS FILE — the cold-start router (phase-42 subject CHOSEN + forks SETTLED in a prior dialogue; the next step is to WRITE the BRAINSTORM.md, NOT re-brainstorm).
2. `docs/envoy-go/STATE.md` — active-phase `phase 41 (circuit-breakers) IMPL done`; the count footer (1163 / 76 / 42 / 36 / ADR-0248, next-free ADR-0249).
3. `docs/envoy-go/ROADMAP.md` — row 41 `done` + the §Upstream-robustness family heading (active-HC / outlier / circuit-breakers DONE; **2** candidates remained, retries+hedging now in-flight) + the candidate rosters.
4. `docs/envoy-go/phases/41-circuit-breakers/{BRAINSTORM,SPEC,PLAN,PROGRESS,README}.md` (the durable phase-41 record) — for the precedent shape of the phase-42 BRAINSTORM/SPEC/PLAN/IMPL.
5. `docs/envoy-go/DECISIONS.md` (the ADR-0248 circuit-breaker entry + the process ADRs; next-free ADR-0249) + `docs/envoy-go/BEHAVIOR_CONTRACT.md`.
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`.
