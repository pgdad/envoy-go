START a fresh cold-start session for envoy-go — **the phase-40.1 (`outlier-detection-consecutive-5xx`, passive ejection on the `consecutive_5xx` detector) PLAN is DONE, LANDED on master (commit `2100435`), and PUSHED to `origin/master`; the next lifecycle step is the phase-40.1 IMPL** (`superpowers:subagent-driven-development` — execute `docs/envoy-go/phases/40.1-outlier-detection-consecutive-5xx/PLAN.md` Tasks 1–14 in a FRESH git worktree; fresh subagent per task + two-stage spec-then-quality review; controller squash/ff-merge/push at stage-close) OR a new subject. Confirm the tip via `git log --oneline -5` (the substantive master tip is the phase-40.1 PLAN commit **`2100435`**; the prior substantive tips are the phase-40.1 SPEC `a350f0e` and the phase-40 BRAINSTORM `332bdce`). Counts UNCHANGED at the 40.1 PLAN: stat surface **1132** / fixtures **70** / fuzzers **42** / BackendKind tail **34** / DECISIONS tail **ADR-0244**, next-free **ADR-0245**.

## What just landed (phase 40.1 PLAN — the durable record is `docs/envoy-go/phases/40.1-outlier-detection-consecutive-5xx/PLAN.md`; read that + SPEC.md, not this summary)

The PLAN decomposes SPEC §10 into **14 bite-sized TDD tasks** and RESOLVES the §12 D-S40.1-1..6. The FINAL ADR-0045 split-gate re-check → **NO FURTHER SPLIT** (~270–360 prod LoC / 14 tasks; single flat 40.1 leg). plan-document-reviewer APPROVED (5 Minors applied — the `acceptHTTP503Counting` no-counter signature, the `newPCGRNG`→`[0,100)` enforceRoll adapter, the `ejectedCount` cap-helper reconciled in the detector sketch, the CAS-guarded once-only eject, the stat-surface-as-documented-running-total+delta not a phantom command).

**The 14-task spine (the PLAN has the full per-task code + file structure):** (1) baselines/PROGRESS; (2) the ejection dimension on `hostHealth` (`ejected`/`unejectAtNanos`/`ejectCount`/`consec5xx`) + `isEjected`(lazy-uneject in `isEjected` itself, CAS-guarded gauge dec)/`available`/`availableCount`/`ejectedCount` on `clusterHealth` + an injectable `nowNanos` clock + unit tests; (3) the `available` predicate move across the FIVE leaf consult sites (`loadbalancer.go:61`/`random.go:55`/`ringhash.go:153`/`maglev.go:60`/`leastrequest.go:104`; subset inherits) + the `inPanic`-denominator `healthyCount`→`availableCount` split (the `membership_healthy` gauge STAYS `healthyCount`) + the **byte-stability gate (full 70-dir GREEN)**; (4) `parseOutlierDetection` (new `internal/cluster/outlier.go`) + the 4-arm reject roster + the `clusterHealth`-creation widening (`manager.go:370` → `health_checks` OR `outlier_detection`); (5) the `outlierDetector` consecutive_5xx detect/eject/cap/enforce-roll/double-count + unit tests (CAS-guarded once-only eject; overflow at cap; reset-on-2xx; detect-vs-enforce); (6) the `RecordUpstreamResult` seam + `UpstreamResult` on `Cluster` (no-op when no detector) + the `newPCGRNG`→`[0,100)` enforceRoll + detector construction in the manager; (7) the two LIVE router success sites (`router.go:655` after `IncStatusClass(resp.StatusCode)` + `router_h2.go:100` after `IncStatusClass(resp.Status)` — NOT the test-only legacy `do`/`doH2`, the dead-driver trap); (8) the +5 stat registrations (scoped to outlier clusters; 1132→1137); (9) the `HTTP503Responder` BackendKind 35 + the new OPTIONAL `PerHostBackendKind` per-index runner override + `acceptHTTP503Counting`; (10) the `0069` cross-side traffic-driven-ejection fixture (the `0066` poll-to-converge + warmup + delta-counter template); (11) `0069` deliberate-break (A: detector no-op → `ejections_active` never converges; B: LB ignores `available` → 5xx leak/warmup never stabilizes) + 20-run flake (`-count=1`, `TestDifferential/0069`); (12) full 71-dir differential + six-gate; (13) ADR-0245 body + BEHAVIOR_CONTRACT passive-health subsection (stat 1132→1137); (14) completion bundle.

**The D-S40.1-1..6 resolutions (baked into the tasks; see PLAN §"D-question resolutions"):** (1) house reject wording `cluster: %q: outlier_detection: <reason>` (the `parseHealthChecks` precedent); (2) `consecutive_5xx` absent (nil wrapper) ⇒ default 5 / explicit 0 ⇒ detector OFF; (3) `enforce = enforcing>=100 || (enforcing!=0 && enforceRoll() < enforcing)`, the PCG roll injectable for unit tests, short-circuited for 0/≥100 (so `0069` is deterministic); (4) `0069` constants single-sourced (`refContainerListenerPort 19158`); (5) the mixed `{2×HTTPEcho, 1×HTTP503Responder}` topology via the new OPTIONAL `PerHostBackendKind{ BackendKindAt(i int) BackendKind }` interface consulted per-index in the runner spawn loop (defaults to the uniform `BackendKind()` → byte-stable for the 70 existing fixtures; HONORS the SPEC's +1 BackendKind 34→35 over the rejected driver-self-spawn alternative); (6) NO FURTHER SPLIT.

## Architecture recap (ADR-0245 — §Context anchored in SPEC §13; body lands at this IMPL per ADR-0044)

- The per-request **passive seam** `cluster.RecordUpstreamResult(ep, UpstreamResult{StatusCode, LocalOriginErr})` (LocalOriginErr unread at 40.1 — reserved for 40.2) — fires on EVERY response at the two live-driver success sites; a no-op when `c.outlier == nil`.
- The **ejection dimension** on the existing `hostHealth`, DISTINCT from the active-HC `healthy` bit. **The lazy un-eject lives in `isEjected(ep)`** (the load-bearing un-eject path — an ejected host gets no further traffic, so it rejoins on the next LB scan past it once `base_ejection_time` elapses).
- The **`available = isHealthy && !isEjected`** LB-pick predicate; `inPanic` moves to `availableCount`; `isHealthy`/`membership_healthy` STAY active-HC-only → **byte-identical when no `outlier_detection`**.
- **AMEND-OD1..OD6 (SPEC §1.1):** LAZY un-eject (goroutine-free; `interval` parse-accepted, sweep DEFERRED); flat `base_ejection_time` (backoff+decay DEFERRED); `max_ejection_percent` cap `(ejected+1)*100/total <= cap`; +5 stats with the double-count REPLICATED (the 503-trips-gateway-detected reference behavior NOT replicated); seam at the live success sites only; +1 `HTTP503Responder` BackendKind.

Anticipated at the 40.1 IMPL: fixtures 70 → 71 (`0069`), BackendKind tail 34 → 35 (`HTTP503Responder`), DECISIONS tail ADR-0244 → ADR-0245 (next-free ADR-0246), stat surface 1132 → 1137 (+5), fuzzers 42 (UNCHANGED), ZERO new packages + ZERO new go.mod modules. ROADMAP row 40 STAYS `in-progress` (40.2/40.3 legs pending — per `reference_roadmap_split_phase_row_done` the row flips `done` only when ALL three legs land; this is NOT the final leg).

## Honor throughout (the lifecycle discipline)

- **IMPL discipline:** `feedback_execution_style` (subagent-driven) · `feedback_git_worktrees` (fresh worktree) · `feedback_subagents_no_push` (subagents commit locally; controller pushes at stage-close) · `feedback_subagent_worktree_path_targeting` + `feedback_subagent_worktree_detach` (pin canonical + worktree-relative paths; GIT HYGIENE block; controller re-verifies branch each task) · `feedback_pertask_gofmt_lint` (each task runs gofmt -l + golangci-lint on touched pkgs) · `feedback_push_to_origin` (push at stage-close without asking).
- **Differential discipline:** `reference_differential_run_selector` (`-run 'TestDifferential/0069'`, NOT bare `0069`) · `reference_differential_break_protocol_count1` (`-count=1` on every break; AND the dead-driver trap — the seam lives in `doH1/doH2ClusterAction`, NOT the legacy `do`/`doH2`; prove liveness via the 0069 break-A) · `reference_docker_probe_bridge_network` (the `0069` cross-side needs Docker + `contrib-v1.37.2` + a shared bridge + host.docker.internal) · `reference_health_check_propagation_warmup` (poll `ejections_active` to converge + a 503-tolerant warmup-until-K-consecutive-200s gate + delta counters — NOT a fixed sleep) · `reference_fixture_workload_constant_desync` (single-source the `0069` constants) · `reference_differential_asserter_dispatch` (cross-side via `StatsAsserter`, NOT `SubjectAsserter`).
- **ADR/process:** ADR-0044 (ADR-0245 §Context done at SPEC; body at this IMPL) · ADR-0052 (six-gate) · ADR-0080 (byte-stable rejects) · ADR-0106 + `reference_roadmap_split_phase_row_done` (row 40 flips `done` only when all THREE legs land) · ADR-0227 (`contrib-v1.37.2`).

## Read first (in order)

1. THIS FILE — the post-phase-40.1-PLAN cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 40.1 (outlier-detection-consecutive-5xx) PLAN done`; the count footer (1132 / 70 / 42 / 34 / ADR-0244, next-free ADR-0245); the lifecycle routing (next-skill = the 40.1 IMPL).
3. `docs/envoy-go/phases/40.1-outlier-detection-consecutive-5xx/PLAN.md` — the 14-task TDD spine + the per-task code + the D-S40.1-1..6 resolutions + the file structure + the exit deltas. PLUS `SPEC.md` (the §1.1 AMEND-OD1..OD6; §3 architecture; §5 proto; §6 rejects; §7 stats; §8 `0069`; §11 D-OD-* pins) + the phase-40 `BRAINSTORM.md` (parent charter).
4. `internal/cluster/{health.go,cluster.go,manager.go}` (the `hostHealth`/`clusterHealth` registry the ejection dimension extends; `isHealthy`/`healthyCount`/`inPanic`/`recomputeMembership` at `health.go:70-102`; the `clusterHealth`-creation site `manager.go:370-373`; `parseHealthChecks` the parse precedent; `parsePanicThreshold`) + `internal/cluster/{loadbalancer,random,ringhash,maglev,leastrequest}.go` (the five leaf `isHealthy→available` sites) + `internal/filter/http/router/{router.go,router_h2.go}` (the `doH1/doH2ClusterAction` seam sites :655/:100 — NOT the legacy `do`/`doH2`) + `test/differential/fixture/fixture.go` (BackendKind tail 34) + `test/differential/runner_test.go` (the spawn loop ~:178, `acceptHTTPEchoCounting` :1515, the `GRPCHealthResponder` case :933) + `test/fixtures/0066-health-check-http/driver/driver.go` (the poll-to-converge driver template `0069` adapts).
5. `docs/envoy-go/DECISIONS.md` (ADR-0242/0243/0244 + the process ADRs; ADR-0245 §Context draft in SPEC §13) + `docs/envoy-go/BEHAVIOR_CONTRACT.md` (the active-health-checks subsection the passive subsection joins).
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`.
