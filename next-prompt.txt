=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

**THERE IS BANKED MID-LIFECYCLE WORK AT THIS TIP.** Phase 81 (`stats-name-charset-guards`) was **CHARTERED and BRAINSTORMED at 2026-07-31** and **SPEC'd at 2026-07-31**; **row 81 is `in-progress`**. ⇒ **This session performs the phase-81 PLAN.** Do NOT self-pick a new subject.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=113 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **`want=113` IS A LIVE FIGURE, NOT A CONSTANT.** It is the ROADMAP data-row count. **A row-adding phase MUST bump it in the same commit that adds the row** — otherwise every later session gets `GATE FAIL: examined 114 data rows, expected 113`. That noise is deliberate: a wrong denominator must be loud, because a silent one is how check (1) failed in the first place. **The phase-81 BRAINSTORM bumped 112 -> 113; the SPEC left it at 113.** ⚠️ **YOUR PLAN ADDS NO ROW, SO IT MUST LEAVE `want` AT 113** — the IMPL leaves it at 113 too (it flips row 81's STATUS, it does not add a row).

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. When you open a new family, add its row-summary slug here.

**SENTINEL RE-RUN MECHANICALLY AT THE PHASE-81 SPEC, with FIRING negative controls. IT DOES NOT FIRE. `stop` was NOT created; `ls stop` => `No such file or directory`, and it MUST NOT be created.** ⚠️ **Re-run it yourself anyway and record what YOU see** — a prior run's result is not evidence for the current tip. ACTUAL output, recorded rather than predicted:
- **(1) prints `NOT DONE: row 81`** at `want=113` — correct; the row is `in-progress` and will stay so until the IMPL. NCs, ALL fired: `want=112` => `GATE FAIL: examined 113 data rows, expected 112`; row 81 doctored to `done` on a scratch copy => **SILENT**; row **62** doctored on that same copy => `NOT DONE: row 62` (so the check still discriminates when otherwise silent).
- **(2) FIVE — `:191 :201 :211 :217 :225`** — **UNCHANGED**. **The twenty-ninth consecutive phase at which check (2) did not go down. STATED, not forecast.**
- **(3) `NEVER OPENED: gRPC` and `NEVER OPENED: WASM`** — unchanged. NCs: an invented slug => `NC NEVER OPENED: ZZZ-nonexistent`, while the REGISTERED slug `Observability` correctly printed nothing.
- Input measured at **229 lines / 113 data rows**, so an empty result could not read as a zero result.

⚠️ **THE LEAK CHECK IS LIVE AT ANY STAGE THAT WRITES A ROADMAP CELL.** The rule: **NEVER WRITE A SENTINEL'S OWN MATCHER STRING INTO A FILE THE SENTINEL GREPS.** The sentinel greps `docs/envoy-go/ROADMAP.md` — writing these strings *here* is safe; writing them *there* is not. The leak is proven to **SILENCE CHECK (3) BY MENTION** (reproduced live at the phase-81 BRAINSTORM: doctoring row 81's cell to say `gRPC-family row` made `gRPC` stop printing while `WASM` kept printing). **`grep` cannot tell a mention from a use.** ⚠️ **A PLAN normally writes NO ROADMAP cell — if yours does not, say the check is INAPPLICABLE rather than "passed".** The phase-81 SPEC said exactly that, correctly.
```sh
# the row's cell only, not the whole file
awk -F'|' '/^\| *81 /' docs/envoy-go/ROADMAP.md > "$SCRATCH/row81.txt"
grep -coE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' "$SCRATCH/row81.txt"  # want 0
grep -oiE '(HTTP-filters|Network-filters|Load-balancing|Upstream-robustness|Observability|Operational-tooling|HTTP/3|gRPC|xDS|Runtime|WASM)-family row' "$SCRATCH/row81.txt" | sort -u
# ^ every slug printed MUST already be registered elsewhere in ROADMAP.md. Row 81 prints `Observability-family row`
#   ONLY, which is registered elsewhere -- a USE, not a mention.
```
⚠️ **ROW WELL-FORMEDNESS: THE GATE MUST BE A DISJUNCTION.** Re-executed at the phase-81 SPEC over all **113** rows: **ARM-A (escape-aware `NF!=8`) catches 57 (NF=9) and 69 (NF=10) ONLY; ARM-B (escape-aware trailing-piece) catches 78 ONLY. Neither arm alone suffices**, and **row 81 is flagged by neither**. ⚠️ **PRECISION CORRECTION LANDED AT THE SPEC: naive `NF==8` flags SEVENTEEN rows — `16 17 18 18.1 19 20 22.1 24.2 25.1 43 48 49 55 57 62 69 74` — of which FIFTEEN are FALSE POSITIVES and TWO (57, 69) are TRUE.** Earlier routers presented that list as the false-positive set; it is the **flag set**. Row **78 is absent from it** (compensating defects cancel). ⇒ the naive form is wrong in **both** directions: **15 FP + 1 FN**.

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files --hidden -I --exclude-dir=.git …` in ugrep-compatible mode. **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. The delta is exactly this file, reproduced at five separate tips:
```sh
grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort         # wrapped: ABSENT
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                    # direct access WORKS
```
⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING THE TWO ARMS.** The wrapped arm prints bare paths while GNU prints `./`-prefixed ones, so a naive `comm` reports **every file as "only in GNU"** and the real delta is invisible. ⚠️ **Print the FILE NAMES, never a count.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** — it reports `next-prompt.txt` as *"not ignored"* (exit 1) because **git exempts TRACKED files from `.gitignore`** while ugrep does not. **Reliable forms: `command grep -r`, `git grep`, an explicit file list, or a recursive grep handed the FILE path directly.**

⚠️ **AND `grep -c` ON ZERO MATCHES IS TOOL-SPECIFIC.** Only **`git grep -c`** prints nothing. **GNU `grep -c` prints `0`** (and `path:0` per file on a list or `-r`), as does the ugrep wrapper. **Both still exit 1**, so an exit-code gate is wrong for either — this fired live again at the phase-81 SPEC.

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`.** Every document from phase 75 forward recorded its blind spot — **109 data rows / 105 matched / FOUR misses** at an **em-dash** in an "after" cell, a **DOT** in the slug `http-1.1`, and two **LETTER-suffixed** ids `28.1a`/`28.1b` — and every one of them called it *"currently HARMLESS, since all four missed rows are `done`."* ⚠️ **Nobody fired the unsafe arm until the phase-77 PLAN. EXECUTED on scratch copies: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** ⚠️ **DO NOT restore the regex form.**

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

The old check (2) matched ONLY the long phrase and could not see the **Operational-tooling** family's three genuinely un-chartered items, recorded in the **SHORT** form. Fixed at the phase-77 BRAINSTORM, at the MATCHER. ⚠️ **Run the union form above, never one half of it.** ⚠️ **A ONE-ARM STRIP IS NOT AN NC FOR CHECK (2)**: `sed 's/deferred candidates://'` moves the union count **5 -> 4, not 5 -> 0**, because the long form does not contain the short substring at all.

⚠️ **THE DOCUMENTED TRAP'S STATED MECHANISM IS REFUTED, WHILE THE TRAP IS REAL.** `reference_sentinel_deferred_sentence_live_vs_historical` says a naive `grep -c 'candidates:'` inflates *because of* the `candidates were:` recaps. It does inflate — but **not for that reason**: `candidates were:` does not contain that substring **at all**. The inflation comes from **ROADMAP table rows carrying BRAINSTORM prose**. **Cite the command; do not re-inherit the explanation.**

⚠️ **AND THE MEASURED RECORD: CHECK (2) HAS NEVER GONE DOWN** across ~29 phases (history `0 -> 1 -> 3 -> 4 -> 5`). => **the candidates sentence is a WINDOW onto a larger deferred backlog, not an inventory of it.** ⚠️ **A stage that predicts a decrease here is repeating the phase-73 error. State what your row narrows — usually nothing — rather than forecasting.** **Row 81 narrows NOTHING**, and the SPEC ADDED three named deferred candidates without touching the sentence (see §4 below).

==============================================


## STATUS AS OF 2026-07-31: PHASE 81 (`stats-name-charset-guards`) IS **BRAINSTORMED AND SPEC'D**. ROW 81 IS `in-progress`. THIS SESSION PERFORMS THE **PLAN**.

**What the phase-81 SPEC landed.** Docs-only, base master **`aab596e4`** (taken from `git rev-parse master`, **NOT** the BRAINSTORM squash `3816a17f`, which sat two router-only commits below the tip), branch `phase-81-spec`. **ZERO production `.go`, ZERO test `.go`.** New: `docs/envoy-go/phases/81-stats-name-charset-guards/SPEC.md`. Modified: `PROGRESS.md` (**+135**, the repo's FIRST `# SPEC record`), `DECISIONS.md` (**17724 -> 17766**, ADR-0303 §Context, a PURE APPEND), `STATE.md` (§Current rolled IN PLACE, §Recent add + evict, **64 -> 64**), `STATE_HISTORY.md` (**432 -> 434**), this file. **`ROADMAP.md` and `BEHAVIOR_CONTRACT.md` BYTE-UNTOUCHED — verified with `git diff --stat` EMPTY on both, against a firing NC on `PROGRESS.md`.**

### ⚠️ THE FIVE THINGS THE SPEC PROVED THAT CHANGE THE ROW

1. ⚠️ **THE COUNT IS NINE, NOT EIGHT — SOURCE `F` IS TWO INDEPENDENT SOURCES.** `BuildMatcherEngine` (`internal/rbac/rbac.go:147-153`) does **only** `matcher.New(m, []string{actionTypeURL})` — a TypeURL allowlist. `Evaluate` (`:249`/`:251`) returns `action.GetName()`, read out of the matcher tree's terminal `Action` proto **at REQUEST time**, and **nothing enumerates those names at boot**. **Proved by execution with a firing NC** (`allow_admins` registers cleanly; `allow-admins` panics). ⇒ **a matcher-based RBAC config still panics the process under ANY boot-only remedy.**
2. ⚠️ **THE REFERENCE ACCEPTS THE HYPHEN VERBATIM, AND THE REAL GAP IS PROJECTION, NOT CHARSET.** Controller-run, `envoyproxy/envoy:contrib-v1.37.2`, three arms + a firing control (track absent => both surfaces EMPTY): the reference boots, serves 200, emits `http.myhcm.rbac.policy.allow-admins.allowed: 1`, and renders `envoy_http_rbac_policy_allowed{envoy_rbac_policy_name="allow-admins",…} 1`. **Its metric NAME is policy-name-INDEPENDENT; the name is hoisted into a LABEL, which has no charset to violate.** envoy-go **flattens** it and has **no `rbac_policy_name` extraction at all**. ⇒ **any reject is an envoy-go-strict DEPARTURE, not a fix, and must be written up as one.**
3. ⚠️ **THE CORPUS DOES NOT SURVIVE — ONE PROVEN HARD RED, INVISIBLE TO THE DIFFERENTIAL.** `internal/filter/http/ratelimit/compiled_config_test.go:574` `StatPrefix: "tenant-foo"`, asserted at `:616-617`. `IsValidName("tenant-foo") = false`. Green today; guard **H** makes `:579`'s `t.Fatalf` live. **`0032`/`0033` set only the HCM `stat_prefix`, so no differential run can find it.** The BRAINSTORM's *"no existing fixture should red"* is narrowly true (fixture YAML **is** clean) and **materially wrong as a corpus claim**.
4. ⚠️ **D-81-HELPER'S TRADEOFF DOES NOT EXIST.** `go list -deps ./cmd/envoy-go` = **560** packages containing **8 of 8** (all seven target packages **and** `internal/stats`); NCs fired (`test/differential`, `test/conformance/h2spec`, an invented package: all ABSENT). Both designs perturb the same binary `harness.go:240`, `:594` and `h2spec_test.go:210` build. **The gate obligation is IDENTICAL; the measured asymmetry is 11.09 s.**
5. ⚠️ **D-81-SANITIZE DISSOLVES ON SCOPE.** ADR-0065's rejection is a **TWO-LIMB CONJUNCTION** — token hoisted verbatim into a pinned label **AND** the map non-injective over its domain — and **§Decision cites `normalizeAddr` APPROVINGLY**. `normalizeAddr` fails limb (ii): its input is `ln.Addr().String()` captured **post-bind** at `manager.go:1085`, never operator bytes. **There is no contradiction.** ⇒ **ADR-0302 §Consequences (vii) item 5's *"internally inconsistent"* is PARTIALLY REFUTED.** Measured doctrine ratio **1 sanitizer : 15 rejecters**.

### ⚠️ WHAT THIS SESSION DOES — the phase-81 PLAN

**One stage per session.** Write `docs/envoy-go/phases/81-stats-name-charset-guards/PLAN.md`, append the `# PLAN record` half to that directory's `PROGRESS.md`, roll `STATE.md` §Current **IN PLACE** (with the §Recent add + evict), and roll this file. **`ROADMAP.md` row 81 STAYS `in-progress`**, `want` STAYS **113**, and `BEHAVIOR_CONTRACT.md` + `DECISIONS.md` are normally **BYTE-UNTOUCHED at a PLAN** (ADR-0303 is `PROPOSED`; its §Decision/§Consequences land at the **IMPL**) — say so explicitly rather than claiming a gate.

**THE SIX DISPOSITIONS ARE MADE. DO NOT RE-LITIGATE THEM — EXECUTE AGAINST THEM.**

| question | disposition | why it must not reopen |
|---|---|---|
| **D-81-HELPER** | **INLINE** — reuse the exported `stats.IsValidName`; add NO new exported symbol | the gate cost is symmetric (§4 above); price it ONCE for the row |
| **D-81-F-SITE** | **TWO PARTS, ATOMIC** — (a) a `trackPerRuleStats`-gated boot reject at `internal/filter/http/rbac/rbac.go buildCompiledConfig` + (b) a **skip-and-log backstop** at `internal/rbac/perpolicy.go PerPolicyCounters.Inc` | (b) alone reverses row 80's doctrine; **(a) alone leaves F2 crashing the process** |
| **D-81-SANITIZE** | **REJECT, never sanitize** — all nine sources satisfy BOTH ADR-0065 limbs | the contradiction is dissolved; re-opening it re-litigates a settled reading |
| **D-81-WORDING** | eight byte-stable wordings, **family-split preserved** (network filters: no-echo const; HTTP/core: echo `%q`), identifier-collision-checked | see SPEC §6 for the literals |
| **D-81-DEPTH** *(NEW at the SPEC)* | **TABLE-DRIVEN-SHARED** | worth **~450-500 net lines** and the entire split decision; **left unstated the PLAN defaults to the phase-80 per-source-deep shape and the row CROSSES ~1500** |
| **D-81-EMPTY** *(NEW at the SPEC)* | **SKIP-IF-EMPTY** — every guard is `if tok != "" && !stats.IsValidName(assembled)` | else **7 tests red** and **ADR-0132 §Decision (v)** (the deliberate `compressor..gzip.` D5 pin) is contradicted |

**COST THE SPEC WROTE DOWN: band ~850-1200 net `.go`, budget ~1000, ~14 tasks.** ⚠️ `/BOOTSTRAP_PROMPT.md` §6.1 gates on **the number the PLAN writes down**, not the realized figure. Neither trigger fires: **~14 vs ~25 tasks (1.8×)**, **~1000 vs ~1500 net (1.25-1.75×)**. Three independent measurements reconciled: ~30/source (~240 for eight), **174 for F1 alone** (fully built, 5 test arms), 850-1000 shared-depth / 1390-1500 per-source-deep. **The spread IS D-81-DEPTH.**
⚠️ **IF YOUR OWN ENUMERATION LANDS ABOVE ~1200, SPLIT — BUT NOT ON THE AXIS EARLIER ROUTERS PRESCRIBED.** *"81.1 sources / 81.2 the empty-segment retrofit"* is **REFUTED and is not a split**: the retrofit is already OUT of scope, and re-badging it would park **row 81 `in-progress` until the retrofit lands** (§6.2 items 4-5), re-coupling the availability fix to work it was deliberately decoupled from. **The coherent axis is INSIDE the sources: 81.1 `rbac-stat-name-guards` = F1 + F2 + D + E · 81.2 `filter-stat-prefix-guards` = A + B + C + G + H.**
⚠️ **Calibration.** Phase 80 estimated ~640 and realized **1636 (2.56×)** — but its **fixture bucket was 631 of 1636 (38%) at 4.10×, and phase 81 has +0 fixtures**, so the applicable fixture-excluded multiplier is **1.97×**. The prototype figures above are **realized-basis**, so do NOT re-multiply them. Read a budget as *"expect the ceiling"*: 76 ~7-9 -> 9 · 77 11-13 -> 12 · 78 7-9 -> 10 · 79 10-12 -> 12 · 80 11-14 -> 13. ⚠️ **The third §6.1 trigger (mid-execution, >~10 sub-steps) FIRED at phase 80 (6 enumerated vs 17 executed) — RECORD such a firing, never absorb it.**

### What matters for the NEXT session, in order

1. **⚠️ RE-RUN ALL THREE SENTINEL CHECKS YOURSELF AND RECORD THE ACTUAL OUTPUT; DO NOT PREDICT IT.** Create `stop` if and ONLY if all three print nothing. **All three print, so it cannot fire.** ⚠️ **Your PLAN adds no row — leave `want` at 113 and say so.**

2. **⚠️ EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION, NOT BY REVIEW.** The lineage record is unbroken: the phase-80 SPEC refuted nine BRAINSTORM claims; its PLAN refuted **nineteen**; its IMPL **sixteen**. The phase-81 BRAINSTORM refuted **fourteen** router claims; **the phase-81 SPEC refuted TWENTY-TWO**, eight load-bearing. **Budget for finding more. A PLAN that refutes nothing has not looked.**

3. **⚠️ THE SPEC'S LOAD-BEARING REFUTATIONS — carry these forward, they are now FACT and must not be re-inherited in their old form.**
   - **The count is NINE across SEVEN packages** (the BRAINSTORM said eight across six). Only D/E share a package.
   - **The BRAINSTORM's F site is WRONG.** `internal/rbac BuildRulesEngine` is shared (3 non-test call sites: HTTP `:235`, HTTP-shadow `:254`, network `:162`) and **blind to `track_per_rule_stats`**; network rbac **never constructs `PerPolicyCounters`** (`Inc` has exactly **2** non-test call sites, both `http/rbac/rbac.go:732` and `:760`). A guard there rejects configs that **cannot panic**.
   - ⚠️ **The per-route tier compiles at REQUEST time** — `resolvePerRouteConfig` (`:346`, from `DecodeHeaders` `:798`) -> `buildCompiledPerRoute` (`:413`) -> `buildCompiledConfig(…, isPerRoute=true)`; rbac registers **no `RegisterPerRouteValidator`** while **21** files repo-wide do. Recorded nowhere before the SPEC.
   - **`BEHAVIOR_CONTRACT.md:931` is a SCOPE OVER-READ and a NON-UNIQUE anchor.** The sentence occurs **FIVE** times (`:841 :863 :877 :913 :931`), **all five inside a stats-sink subsection**, and it is **not honoured tree-wide** — **13 `*-boot-reject` fixture dirs exist**. The conclusion (zero new fixtures) survives on a far stronger, on-family citation: **`test/fixtures/0044-network-rbac-boot-reject/driver/driver.go:22-26`**, which names *"invalid stat_prefix"* verbatim as a **subject-side-only reject covered by unit tests, NOT cross-side fixtures**. **Cite `0044`, never `:931`.**
   - **`~19` distinct token sources is REFUTED — there are 23.** 15 guard SITES ⇒ **13** distinct guarded tokens (two duplicate guards); + 8 unguarded = **21** `IsValidName`-class; **+2 protected by another mechanism** (a closed-set allowlist in `zookeeperproxy/stats.go`; `normalizeAddr`, the tree's ONLY sanitizer) = **23**. The coincidence `15+8=23` is **accidental** and must not be quoted as the reconciliation.
   - **The 15 split 11 REJECT / 4 SKIP**, and **all four SKIP sites sit on post-Freeze / request-time paths** — four landed precedents for the F2 backstop.
   - **The incumbent template's own rationale is WRONG.** `network/rbac/rbac.go:105`'s *"a bare-prefix check would mis-accept"* measures **MIS-ACCEPTS = 0, OVER-REJECTS = 1** on a literal suffix; the mis-accept is real only when a SECOND variable segment sits inside the name. The rule is **"guard EVERY variable segment"**. ⚠️ **And the assembled form is the WEAKER arm on the deferred defect** — it accepts `foo.` as `foo..rbac.allowed`, so **row 81's guards inherit the interior-empty-segment hole BY CONSTRUCTION**. **The canonical anchor is ADR-0065 §Consequences (b)** (which states the longest-assembled-name rule *with its argument*), **not that comment**.
   - **The four §4.2 rosters are wrong as rosters, right as properties**: **77** distinct fixture-YAML `stat_prefix` (not 111), **16**/**59** wasm plugin names (not 3), **22** RBAC policy keys (not 3). `compressor_library.name = text_optimized` stands.
   - **The router's SDS "misleading messages" cites point at the WRONG FILES.** `internal/xds/stats.go` has **no error message at all**; the `"is not supported in phase 03"` strings live in `internal/tls/config.go:437`/`:454` (+`:114 :171 :388`), and `boot.go:161-162` only **quotes** one in a comment. Those are `--mode validate` scope, **out of row 81's remit**.
   - **The empty-segment hole is INTERIOR-ONLY** (`a.`, `.a`, `..`, `""` all already rejected) **and its target is GENERATED, not authored** — a config-token sweep for `..` finds **zero** across 1830 values and is structurally blind.
   - **`ST1005` is NOT enforced** (probed: `errors.New("Invalid stat prefix.")` ⇒ exit 0); **`misspell` locale US IS** (NC fired). **No enabled linter constrains error wording beyond spelling — do not claim a gate that does not exist.**
   - **The family ordinal is the TWENTY-FOURTH**, re-derived from ROADMAP's own ordinal cells (row 79 TWENTY-SECOND, row 80 TWENTY-THIRD); `TWENTY-FIFTH` ⇒ **0 hits**. The SPEC's own drafting brief said twenty-fifth and was refuted by its writer.

4. **⚠️ THREE NEW DEFERRED CANDIDATES THE SPEC NAMED. They are recorded in SPEC §13 and do NOT sit on any family's deferred-candidate sentence, so check (2) is unmoved.**
   - **A LIVE CROSS-SIDE STAT-NAME DIVERGENCE.** `http/rbac/rbac.go:500-506` `namespacePrefix` returns the literal `"rbac"` on an empty `rules_stat_prefix`, justified by a code comment asserting the C++ filter does the same. **Refuted by the reference itself** — an arm with no `rules_stat_prefix` emitted `http.myhcm.rbac.policy.allow-admins.allowed`, a **single** `rbac` segment, where `baseStatPrefix` makes envoy-go emit `http.myhcm.rbac.rbac.policy.…` (DOUBLE). **The differential cannot see it**: `0018/envoy.yaml` sets an explicit prefix at `:107 :127 :247` and its header says so deliberately. `reference_code_comment_not_evidence`.
   - **THE RBAC POLICY-NAME PROJECTION GAP** (§2 above) — the reference hoists to `envoy_rbac_policy_name`; envoy-go flattens.
   - **THE MATCHER-TREE `Action`-NAME BOOT WALK**, which would let F2 be boot-rejected instead of skipped.
   Plus the previously-named successor **`stats-name-empty-segment-guards`** — ⚠️ **which is BLOCKED in a way the BRAINSTORM did not know: it CONTRADICTS ADR-0132 §Decision (v)**, `DECISIONS.md:6291`, pinning a **deliberate, reference-probed** `compressor..gzip.` double dot, plus `compressor.go:526`'s *"DO NOT collapse"* and `compressor_test.go:2373-2375`'s self-described *"LOAD-BEARING D5 behavioral pin"*. The successor must carve out the compressor or supersede that ADR.

5. **⚠️ BROKEN-GATE COUNT IS EIGHTEEN — no nineteenth shape at the phase-81 SPEC, but THREE priors fired LIVE.**
   - a **`-run` no-match** printed `testing: warning: no tests to run` / `[no tests to run]` and **EXITED 0**;
   - a **negative control whose OWN INPUT was wrong** — a `.replace()` hit a comment line instead of the config, so the arm read as *"extractor blind"* when the injection had never landed (`reference_probe_input_is_a_claim`); fixing it exposed a **real** extractor blind spot on single-line `map[string]*P{…}` literals, whose repair surfaced **5 additional** policy names (17 -> 22);
   - **`grep -c` on zero matches printed `0` while exiting 1.**
   The eighteen carried forward, unchanged: two defects that CANCEL in the gate metric · an inert gate cell · a full-suite recipe without `-v` is VACUOUS · a sha256 roster desynced against a DELETED file · `gofmt -l` NEVER exits non-zero (gate on OUTPUT) · `go doc -all <A> <B>` swallows arg2 · a `+0 exported symbols` gate over an EMPTY package reds on a CORRECT tree · a RANGE gate cannot detect anchor drift · a roster's naive `[ -f ] || continue` exits 0 on a DELETED file · a count-only stat guard PASSES a build with BOTH names wrong · a `-run` no-match exits 0 with `[no tests to run]` · a `--- PASS` tally over a package with sibling tests exceeds the fixture denominator · a stat-delta claim cannot be discharged by guards scoped to another package · a stderr-VOLUME assertion passes on the hang · `golangci-lint` runs `misspell` with `locale: US` · a harness's exit code is not the command's · a GOLDEN ROSTER that omits the family under test (⚠️ **shape seventeen SURVIVES ITS OWN REMEDY**) · a NEGATIVE CONTROL POINTED AT A TARGET THAT DOES NOT EXIST.

6. **⚠️ LINE ANCHORS DRIFT; PHRASE AND SYMBOL ANCHORS DID NOT.** Every anchor in `SPEC.md` was verified at **`aab596e4`** — but **the SPEC's own commit moved `DECISIONS.md` by +42 lines**, so any `DECISIONS.md:NNNNN` cite above `:17724` is already stale for a later reader. **Prefer the SPEC's §1 and §2.9 SYMBOL anchors to any offset, and never a `+N` offset.** ⚠️ **RE-VERIFY EVERY `.go` LINE CITE BEFORE RELYING ON IT** — the row's own edits will move them.

7. **⚠️ THE FULL 120-FIXTURE DIFFERENTIAL IS MANDATORY FOR THIS ROW, AND SO IS AN EXPLICIT h2spec RUN** — settled at the SPEC, and **not** contingent on any design fork: `internal/stats` **and all seven target packages** link into `cmd/envoy-go` (`harness.go:240`, `:594`; `h2spec_test.go:210`), and **h2spec is a FOURTH consumer NOT covered by `./test/differential/`**. Budget **~400-430 s** per green attempt (measured 402 s / 403.2 s / 427.8 s in CI); `-race` is a SECOND run. ⚠️ **CI runs this job at `-timeout 20m` with job `timeout-minutes: 30`.** The recipe, with `comm -3` as the load-bearing gate:
    ```sh
    ( go test ./test/differential/ -count=1 -v > "$SCRATCH/full.log" 2>&1; echo "INNER_EXIT=$?" )
    grep -cE '^    --- PASS: TestDifferential/' "$SCRATCH/full.log"          # want 120
    grep -E  '^    --- (FAIL|SKIP): TestDifferential/' "$SCRATCH/full.log"   # want EMPTY
    grep -c  'no driver registered for fixture' "$SCRATCH/full.log"          # want 0
    grep -o  'TestDifferential/[^ ]*' "$SCRATCH/full.log" | sed 's|TestDifferential/||' | sort -u \
      | comm -3 - <(ls -1 test/fixtures/ | grep -E '^[0-9]{4}[a-z]?-' | sort)  # want EMPTY
    ```
    ⚠️ The faithful dir predicate is `^[0-9]{4}[a-z]?-` — a bare `^[0-9]{4}-` gives **118**.
    ⚠️ **`go test ./test/differential/...` (with `...`) matches TWO packages, so `go test` BUFFERS each package's `-v` output until that package finishes.** A run that looks hung for 7 minutes with an empty log is normal — do not kill it.
    ⚠️ **But do NOT budget the differential as EVIDENCE for this row's correctness.** **Sources F1/F2 and H have ZERO fixture coverage** — `track_per_rule_stats` appears **once** in all of `test/`, as PROSE in `0018-http-rbac/README.md:293` (NC: `rules_stat_prefix` ⇒ 43 hits), and `0032`/`0033` never set the ratelimit filter's own `stat_prefix`. **The differential can neither regress on nor cover the headline crash or the one proven red.** It is a regression anchor only; **unit tests are the sole venue.**
    ⚠️ **Stat surface: assert the DELTA.** The `TestNoNewStat*` guards are **PROVEN BLIND to `internal/stats`** (all five live in `internal/statssink/registration_test.go`): **enumerate the diff's registration call sites and show the set is EMPTY**, against a **208-code-site / 84-file** denominator (**210** grep-hits, of which **2** are comment lines in `internal/stats/doc.go:20,21`). **Adding a guard registers no name — it should be +0.**

8. **⚠️ THE SIX-GATE HAS A STANDING LINEAGE DEPARTURE — NAME IT, DO NOT CLAIM COMPLIANCE.** Gate **(f)** expects a `REVIEW.md`; **no phase has had one since 25.3 — 84 of 121 phase dirs carry none.** Record it as a departure. Gate **(d)** (fuzzers) is **VACUOUS for a row that adds none** — **say "vacuous" rather than "green"**, and NC the matcher (**55** `^func Fuzz` repo-wide). Gate **(c)**: proxy-wasm is **INHERITED, not re-run** — say so. **For a docs-only PLAN, (a)/(b)/(c)/(e) are NOT OWED — say "not owed", not "green".** Normative cites re-verified EXACT at the phase-81 SPEC: **§6.1 `:285`** of `/BOOTSTRAP_PROMPT.md` **AT THE REPO ROOT** (`:289` the ~25-task trigger, `:290` the ~1500-LoC trigger, **`:291` BLANK**, `:292` the third trigger); **§6.2 `:294`** (*"How to split"* — the coherent-slice obligation is item **3**); **§6.3 `:306`** (*"There is no third option"*); six-gate **§7.5 heading `:357`, gates (a)-(f) `:360-365`, close `:367`**.

9. **⚠️ ADR + DOCUMENT CONVENTIONS.** **The DECISIONS tail is now `ADR-0303`, STATUS `PROPOSED` — the ONLY `PROPOSED` block in the file.** Next-free is **ADR-0304** (`grep -c '^## ADR-0304'` => 0; NC `^## ADR-0303` => 1). **302** `^## ADR-` headings, ids **0001-0303**, **exactly one gap at ADR-0209**, zero duplicates. STATUS census **16 total = 15 COMPLETE + 1 PROPOSED**. ⚠️ **ADR-0303's §Decision and §Consequences land at the IMPL, NOT at your PLAN** — and the STATUS word must flip `PROPOSED` -> `COMPLETE` in that same commit (**ADR-0299 shipped stale at `PROPOSED` for two full rows** before ADR-0302 fixed it; the mechanical recurrence guard is *"a block whose STATUS reads `PROPOSED` must carry no `### Decision` heading"*). ⚠️ **The retained italic footer count is NINE** — ADR-0294..0300 (the contiguous SEVEN) **plus ADR-0302 plus ADR-0303**; **ADR-0301 carries none**. Earlier routers said SEVEN, which was true of the contiguous run and **stale as a total**. ⚠️ **`^---$` is 216 and must stay 216** — the horizontal-rule convention was abandoned after ADR-0295. ⚠️ **CARRY NO WHOLE-FILE GREP COUNT IN AN ADR** — that species self-falsified in ADR-0296 ¶3 and again in ADR-0302 ¶11; **enumerate by site instead.** ⚠️ **ADR-0044 does NOT contain the §Context-at-SPEC discipline** (read in full — it is titled *"BEHAVIOR_CONTRACT HTTP/1.1 subsection"*); always write **ADR-0044-as-used** with the caveat. ⚠️ **ADR-0045 QUOTES rather than states the split gate AND mis-locates it to "§5 state 2"** — **quote `/BOOTSTRAP_PROMPT.md:289-290` directly, never ADR-0045's paraphrase.** Its §Context is nonetheless the lineage's canonical worked cost example (per-bucket task AND LoC counts, summed, both thresholds named) and **ADR-0303 §Context reproduces that form**.

10. **⚠️ A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX — the TWENTY-SECOND consecutive stage at which this held.** The index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. **Still live:** `reference_sds_init_fetch_timeout_dial_budget_flake` (**TWO packages**) · the pre-existing `internal/cluster` `-race` outlier (`TestOutlierDetector_ConcurrentEjectExactlyOnce`) · `internal/httpclient TestOptions_ZeroValue_NoOpDefaults`. ⚠️ **TWO former flakes were FIXED at `f46ba419`/`f2dd994a` and a recurrence of either is now a FINDING, not a re-run:** `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery` and the full-suite `bind: address already in use` **BACKEND** half. ⚠️ **`0061-lb-ring-hash` is NOT a live flake — a spread failure there is a FINDING.** **Isolate-re-run, then state the classification AND its evidence.** **NOTHING FIRED at the phase-81 SPEC** across a targeted 3-fixture differential subset, the nine source packages and `internal/stats`.

11. **Counts at this tip (re-run MECHANICALLY in your worktree; never copy them).** ⚠️ Use GNU `command grep` or `git grep` for anything repo-wide. **All re-derived at the phase-81 SPEC close:**
    fixtures **120** (next-free **0119**; faithful predicate `^[0-9]{4}[a-z]?-`, a bare `^[0-9]{4}-` gives **118**) / fuzzers **55** / internal packages **73** / blank imports in `runner_test.go` **120**, ⚠️ **anchor on the FULL `^\t_ "github.com/pgdad/envoy-go/test/fixtures/` prefix — a naive `^\t_ ` gives 126** — and ⚠️ **`\t` in GNU ERE is a literal `t`: use `-P`, or a bare `-cE '^\t_ '` silently returns 0** / **stat surface 1207**, ⚠️ **DOCUMENTARY and CONFIG-CONDITIONAL — assert the DELTA** / **BackendKind tail 38** (⚠️ a TAIL VALUE — **39** declarations, values 0-38 contiguous, **no `iota`**; do NOT "fix" 38 to 39) / go.mod modules **2** (the phase-61.2 lineage figure — the single `go.mod` requires **67**; do NOT "fix" 2 to 67) / **`DECISIONS.md` 17766**, **302** headings, tail **ADR-0303 PROPOSED**, next-free **ADR-0304**, retained footers **9**, `^---$` **216** / **`ROADMAP.md` 229 lines / 113 data rows** / **`BEHAVIOR_CONTRACT.md` 5868** / **`STATE.md` 64** / **`STATE_HISTORY.md` 434** / **THIRTEEN non-test top-level segment detectors in `ExtractTags`** (9 `HasPrefix` + 4 `CutPrefix`) plus **FOUR** mid-name detectors / **production `stats.IsValidName` guard sites 15** ⚠️ **not 5** / **production stat-registration call sites 210 grep-hits, 208 CODE sites, of 508 tree-wide** / **distinct non-literal token sources 23** ⚠️ **not ~19**.
    ⚠️ **CONTESTED, SO NO NUMBER IS CARRIED: the next-free REFERENCE port.** Earlier routers say `10119`; `STATE.md` §Project counts says *"Next-free REFERENCE port is `10450`, NOT `10118`"*. Both are landed and they disagree. **Row 81 adds no fixture and needs neither** — if a later row does, **re-derive it from `test/fixtures/` rather than adopting either** (`reference_a_drift_correction_is_itself_a_claim`: contested counts get no number).

12. **⚠️ DOCUMENTARY DEFECTS — carried forward, still deliberately NOT fixed.**
    - ⚠️ **`DECISIONS.md` ADR-0301 §Decision (b) says *"has failed twelve top-level detectors"* — PRESENT TENSE inside immutable ADR text, factually wrong since row 80 landed.** A **named permanent discrepancy**, alongside `ROADMAP.md`'s five `twelve` occurrences (⚠️ **four are pure FALSE POSITIVES** and the fifth is historically true, so **nothing is owed there on the merits**).
    - ⚠️ **`0110`'s driver comment *"envoy_sds_* metric names (twelve families…)"* is ACCURATE** (14 distinct names / 12 families) **but is a FALSE POSITIVE for any `\btwelve\b` top-level-detector sweep.**
    - **The `STATE_HISTORY.md` archive gap: 57 (not 58) missing bullets**, 36 of them for phases 67-75; **`phase 77 PLAN done` in NEITHER file, re-confirmed at the phase-81 SPEC and NOT widened** (the archive-bullet form `phase 77 (.*) PLAN done` is **0 in both files**, with firing NCs on the phase-77 BRAINSTORM/SPEC/IMPL siblings; the `1` hit in `STATE.md` is its own **confession prose**, not a bullet). Defensible ONLY with a `comm -13` recurrence guard — and that guard needs BOTH the `(slug)`-parens matcher AND a `fetch-depth: 0` line in `ci.yml` (**CI is a shallow clone**: `actions/checkout@v6` with no `fetch-depth`).
    - **`ROADMAP.md` malformation: rows 57, 69 and 78 lose summary content in GFM render.** All INVARIANT-BLOCKED by §Schema (*"Only `status` and `sub-phases` columns are updated in place"*) => **defensible as a GUARD, not a fix**, with the DISJUNCTION gate. ⚠️ **There is NO doc-invariant test anywhere in this repo.** The reusable precedent is `test/differential/harness_test.go:123-137`'s `runtime.Caller(0)` -> repoRoot idiom.
    - **`STATE.md` §Project counts self-contradicts §Current** and is **FROZEN at the phase-76 IMPL close.** **Anchor on §Current, which IS live.** ⚠️ **Do NOT "fix" §Project** — repairing a count by editing the sentence that states it is how the ADR-0296/0297 self-falsifying species starts. The phase-81 SPEC left it **byte-identical, proven by sha256 against `HEAD`**.
    - **`BEHAVIOR_CONTRACT.md`'s `wasm.` arm is called a *"NEW SN9 flattening rule"*, which COLLIDES with ADR-0118's actual SN9.** Pre-existing.
    - ⚠️ **The two `//nolint:gosec` directives row 80 added are INERT** — `gosec` is not among the 9 linters `.golangci.yml` enables (`disable-all: true`; the nine are `govet errcheck staticcheck unused ineffassign gofmt goimports misspell revive`; golangci-lint **v1.64.8**).
    - **`golangci-lint-action@v6.5.2` emits a Node-20 deprecation warning in CI.** Deliberately NOT bumped: v7+ requires golangci-lint **v2** while the repo pins **v1.64.8**. **A warning, not a failure.**
    - ⚠️ **`ROADMAP.md` §Schema `:18` says *"Sub-phases get their own rows"* while phase 80 argued the legs live in the parent's `sub-phases` PROSE.** Practice diverging from written doctrine. **Record the tension; do not relitigate.**
    - **Production-DEAD trailer code** in four filters (incl. `admission_control`'s entire deferred gRPC-status state machine) · **a VACUOUS differential arm**, `0036`'s `e_trailers_read` · **two stale non-test comments** at `internal/filter/hcm/connection.go:565` and `h2dispatch.go:503` claiming `RunDecodeTrailers` does not exist (it does, at `internal/filter/http/chain.go:455`) · **`DECISIONS.md`'s wasm module-function roster** enumerates 24 keys, **8 of which do not exist in `internal/`** · **two sibling `freeTCPPort` helpers** (`cmd/envoy-go/main_test.go:180`, `test/conformance/h2spec/h2spec_test.go:219`) still carry BOTH pre-`f2dd994a` defects.
    - ⚠️ **NEW at the phase-81 SPEC:** **`guest-side-config`** in `internal/filter/http/wasm/compiled_config_test.go:1147` looks like a charset violation and is **NOT** one — it is a `PluginConfig{Name:…}` used only as an opaque `anypb` payload for `PluginConfig.configuration`, while guard C reads the **outer** `pc.GetName()`. **Recorded so a later sweep does not "fix" it.**

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — take it from `git rev-parse master`, **NOT from a SHA quoted in this file**. The phase-81 SPEC squash is not named here on purpose: **router-only commits routinely land above it**, so a quoted SHA is stale the moment the router is touched again — **branch off the tip.** ⚠️ **AND THE BARE SUBJECT-LOCATE GREP IS CONTAMINATED BY THIS FILE'S OWN PROSE ABOUT IT — MEASURED, NOT PREDICTED.** The phase-81 SPEC's own first draft of this sentence said `git log --grep 'phase 81'` returns **2** at this tip. **It returns 4.** `git log` greps the **whole commit message**, and two router commits (`aab596e4`, `b66486a8`) discuss the matcher in their BODIES — *"`git log --grep 'phase 81'` returns exactly 1 commit today"* — so **the sentence warning about the grep satisfies the grep**. `reference_sentinel_matcher_string_self_clears`, in a new carrier: a **commit body**. ⚠️ **USE THE SLUG-ANCHORED FORM INSTEAD**, which is stage-exact:
```sh
git log --grep '^phase 81 (stats-name-charset-guards)' --format='%h %s'   # 2 at this tip: SPEC, BRAINSTORM
```
Measured with both controls: the completed **phase 80** form returns **4** (IMPL/PLAN/SPEC/BRAINSTORM — a firing positive control), and an invented slug `^phase 81 (zzz-nonexistent)` returns **0**. It returns **3** once your PLAN lands. **Take the NEWEST line (`| head -1`) and verify the count before relying on it** (`reference_next_prompt_tracked_despite_gitignore`). Subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** (`reference_parallel_subagents_private_scratch`) and they **must tear down docker containers BY NAME, never by an `ancestor`/image filter** (`reference_parallel_agents_shared_machine_namespaces`). ⚠️ **THERE ARE TWO RESERVED PORT BANDS — band private ports outside BOTH `20000-31007` (subject blocks) AND `11000-14999` (backends), and also clear of the static fixture ports `10000-10447` / `15000-15011` / `18001-18007`.** The `42000-42999` band has been used safely (the phase-81 SPEC used `42000-42499` for five agents and `42900-42901` for the controller's docker arms). ⚠️ **If two agents share ONE worktree they must stage by EXPLICIT PATH**, and **must NOT run an unscoped `git restore`** — the phase-81 SPEC ran two writer agents concurrently in one worktree on disjoint file sets and this held. Per-task `gofmt` + `golangci-lint` on touched packages (`feedback_pertask_gofmt_lint`) — **NOT owed for a docs-only PLAN.** ⚠️ **`gofmt -l` NEVER exits non-zero — gate on OUTPUT.** ⚠️ **`golangci-lint` runs `misspell` with `locale: US`** — do NOT paste plan prose into `.go` files. ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch, and note `-o` precedes the package path.**

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. ⚠️ **ASSUME IT FIRES EVERY SESSION — it has been treated as live for TWENTY-SIX consecutive sessions and it FIRED AGAIN, OBSERVED, at the phase-81 SPEC** (`Shell cwd was reset to /home/esa/git/envoy-go`). **Use `git -C <abs-worktree-path>` for every git command, prefer absolute paths over `cd`, put any needed `cd` FIRST in a compound command, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN.** At the phase-81 SPEC, five agents wrote `_test.go` probes and prototype edits into their own detached worktrees, ran them, reverted **by explicit path** with `sha256sum` byte-identity, and **all five reported `git status --porcelain` = 0 lines**. A docs-only stage that ships a stray `.go` file has silently changed its own gate obligations.

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline — ⚠️ **anchor greps on `^- **<field>`**; all seven fields verified singleton at the phase-81 SPEC close, with firing NCs both ways). ⚠️ **§Project counts SELF-CONTRADICTS §Current and is frozen at the phase-76 IMPL close** — do not anchor on it.
2. `docs/envoy-go/phases/81-stats-name-charset-guards/SPEC.md` — **the specification you are planning.** §0 the sentinel · §1 the NINE sources with SYMBOL anchors · §2 the twenty-two refutations · **§3-§8 the SIX dispositions — these are SETTLED** · §9 blast radius incl. the proven RED · §10 the fixture posture and the zero-coverage finding · §11 contract/ADR edits owed at the IMPL · §12 cost and the refuted split axis · §13 what the row names but does not fix · §14 the hazards written FOR YOU.
3. `docs/envoy-go/phases/81-stats-name-charset-guards/PROGRESS.md` — the `# BRAINSTORM record` and the `# SPEC record`.
4. `docs/envoy-go/ROADMAP.md` — **§Schema first** (the row invariants), then **row 81**, then the five deferred-candidate paragraphs (`:191 :201 :211 :217 :225`).
5. `/BOOTSTRAP_PROMPT.md` — **at the REPO ROOT.** §6.1 `:285` (the split triggers), §6.2 `:294` (item 3 is the coherent-slice obligation), §6.3 `:306`, §7.5 `:357-367` (the six-gate).
6. `docs/envoy-go/DECISIONS.md` — **ADR-0303** (the block you must NOT complete yet) · **ADR-0288** (roll-IN-PLACE and the archive rule) · **ADR-0065** (⚠️ the TWO-LIMB reading, and §Consequences (b), the canonical longest-assembled-name anchor) · **ADR-0132 §Decision (v)** (the compressor D5 pin that blocks the successor row) · **ADR-0044** as-used · **ADR-0045** (⚠️ it QUOTES and MIS-LOCATES the split gate).
7. `docs/envoy-go/phases/80-stats-sds-projection/PROGRESS.md` — **the `# PLAN record`**, for calibration: nineteen SPEC refutations and the split arithmetic that mixed two bases.
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — especially **`reference_probe_must_discriminate`**, **`reference_probe_input_is_a_claim`** (⚠️ **fired LIVE at the SPEC**), **`reference_a_drift_correction_is_itself_a_claim`**, **`reference_grep_c_zero_is_a_broken_gate`**, **`reference_code_comment_not_evidence`** (⚠️ **fired THREE times at the SPEC**), **`reference_gate_command_negative_control`**, **`reference_empty_output_is_not_a_zero_result`**, **`reference_sample_is_not_an_audit`**, **`reference_differential_run_selector`** (⚠️ **fired LIVE**), **`reference_recursive_grep_blind_to_gitignored_tracked_file`**, **`reference_dynamic_stat_name_charset_guard`**, **`reference_nil_stats_counter_inc_crashes_goroutine`** (⚠️ **the WRONG mechanism for this row — `checkName` PANICS, it does not return nil**), **`reference_stale_cite_recurs_fix_by_pattern`**, **`reference_spec_drafted_identifier_collision_check`**, **`feedback_brief_citations_not_evidence`**, `feedback_git_worktrees`, `feedback_execution_style`, and **`reference_bash_cwd_reset_commits_to_main`**.
