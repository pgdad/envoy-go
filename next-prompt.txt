=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **PHASE 96 (`listener-default-chain-tlsmode`) IS OPEN AT LIFECYCLE-STATE 3. ROW 96 READS `in-progress` AT `ROADMAP.md:158`.** ⇒ **THIS SESSION RUNS THE PHASE-96 IMPL** (state 3 -> DONE). The PLAN is `docs/envoy-go/phases/96-listener-default-chain-tlsmode/PLAN.md` — **1666 lines**; its §5 is a **19-task** TDD spine, its §4.1 is the stable-anchor table, its §6 is the CORRECTED twelve-row negative-control roster, and its §0 carries FIFTEEN refutations of the SPEC. ⚠️ **THE TASK ORDER IS LOAD-BEARING: Tasks 1-5 LAND AND RUN AT THE UN-FIXED TIP, because the un-fixed tip IS the negative control for roster rows 1-3 and it is CONSUMED the moment Task 6 widens the predicate.**

⚠️ **THE MARGIN IS TWO.** Check (1) is NON-SILENT (`NOT DONE: row 96`, one line) and check (2) reads SIX. Check (3) is SILENT. ⚠️ **DELETING THE LAST DEFERRED-CANDIDATE LINE STILL ENDS THE PROJECT. DO NOT "TIDY" ONE.** NC-A and NC-B read **TWO** lines each at this tip. **MEASURE ALL THREE CHECKS AND ALL FOUR NCs YOURSELF BEFORE DOING ANYTHING ELSE.** ⚠️⚠️ **THE IMPL FLIPS ROW 96 TO `done`, AND THAT CHANGES THE SHAPES *MID-STAGE*: CHECK (1) GOES SILENT, AND NC-A AND NC-B EACH DROP FROM TWO LINES TO ONE. MEASURE BEFORE *AND* AFTER YOUR OWN `ROADMAP.md` EDIT — THIS IS THE FIRST STAGE IN THREE WHERE THE SHAPES MOVE INSIDE THE SESSION.**

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. Parses FIELDS instead of
#     matching a pattern, and asserts the row DENOMINATOR so a deleted row is
#     itself an error.
#     ⚠️ want IS 128 and ROADMAP.md IS 246 lines at the phase-96 PLAN close.
#     Row 96 is STILL `in-progress`, so check (1) is NOT SILENT — it prints exactly
#     one line, `NOT DONE: row 96`. ⚠️ IT GOES SILENT WHEN THE IMPL FLIPS THE ROW.
awk -F'|' -v want=128 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **CHECK (3) IS SILENT, AND A SILENT CHECK IS INDISTINGUISHABLE FROM A BROKEN ONE — THE NCs ARE THE ONLY EVIDENCE.** Run all four EVERY SESSION:
```sh
# NC-A — doctor a row to `in-progress`. ⚠️ READS *TWO* LINES (`NOT DONE: row 62` from the
#        doctoring AND `NOT DONE: row 96`, which is genuinely in-progress). It read ONE before
#        the phase-96 row ADD. MEASURED at the phase-96 PLAN close, before and after this stage's edits.
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md with want=128.

# NC-B — the DENOMINATOR, want=127 on the real file. ⚠️ READS *TWO* LINES —
#        `NOT DONE: row 96` THEN `GATE FAIL: examined 128 data rows, expected 127`.

# NC-C — the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```
⚠️ **AND RUN THE CHECK-(2) POSITIVE CONTROL — AND READ THE COMMAND BEFORE TRUSTING ITS RESULT.** The longer phrase does not CONTAIN the shorter one as a substring, so a control that substitutes only one phrase reports a residual of 5 and reads like a finding. Substitute BOTH and assert the substitution count:
```sh
sed -e 's/deferred candidates:/deferred candidatesXX/g' \
    -e 's/remaining deferred (not-yet-chartered) candidates:/remaining deferred (not-yet-chartered) candidatesXX/g' \
    docs/envoy-go/ROADMAP.md > "$SCRATCH/c2pc.md"
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' "$SCRATCH/c2pc.md" | wc -l  # want 0
grep -o 'candidatesXX' "$SCRATCH/c2pc.md" | wc -l                                                                # want 6 — PROVE it landed
```

⚠️ **WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -c '-family row'` fails (GNU: `grep: amily row: No such file or directory`; ugrep: `option -f: cannot read amily row` — **both rc=2**) and the surrounding arithmetic prints `0`, **which reads exactly like "no change."** **Always pass `--` before the pattern**; with `--` occurrences read **96** and LINES read **68**. **`grep -c` counts LINES, NOT OCCURRENCES,** and **`grep -c` on zero matches PRINTS `0` *AND* EXITS 1, so `$(cmd || echo 0)` EMITS TWO ZEROS** — capture with `v=$(cmd || true)`.

⚠️ **THE MALFORMED-ROW FIGURE RECONCILES ONLY UNDER AN ESCAPE-AWARE FIELD COUNT — AND THE ESCAPE-AWARE COMMAND ITSELF CAN BE BROKEN.** Malformed rows are **ROW IDs 57** and **69** (file lines 119 and 131). **Strip `\|` BEFORE splitting.** Row 57 reads **naive NF=13 / escape-aware NF=9**; row 69 reads **10 / 10**; rows 94, 95 and 96 read **8 / 8**; naive malformed count **17**, escape-aware **2**. ⚠️ **The correct form is `sed 's/\\|//g' F | awk -F'|' '/^\| *[0-9]/ && NF!=8'` with NO file argument to awk** — passing the file makes awk IGNORE STDIN and print the NAIVE 17 under the escape-aware label. **CONFIRMED broken-when-misused at the phase-95 PLAN.** **Any gate must state WHICH FORM it uses and reconcile against the known baseline.**

⚠️ **AND DO NOT SUBSTITUTE A "REASONABLE EQUIVALENT" FOR CHECK (1)'s AWK.** `NF>=8 && $2 ~ /^ *[0-9]+ *$/` once printed **92**, not the true denominator. **Use the form above verbatim.**

**SENTINEL RUN MECHANICALLY AT THE PHASE-96 PLAN CLOSE, BOTH BEFORE AND AFTER THIS STAGE'S EDITS.** ACTUAL output, recorded not predicted — **and it is IDENTICAL on both sides, because a PLAN moves nothing in `ROADMAP.md`, which was PROVEN by re-running rather than assumed:**
(1) **`NOT DONE: row 96`**, one line · (2) **SIX** at `:206 :212 :218 :228 :234 :242` · (3) SILENT · NC-A **TWO** · NC-B **TWO** · NC-C fired (0 residual) · NC-D **96 / 68** · check-(2) positive control **6 -> 0 with 6 substitutions asserted**.
Per-line md5 of the six windows, **trailing newline INCLUDED** (`sed -n 'Np' f | md5sum`) — **ALL SIX BYTE-IDENTICAL to the phase-96 SPEC close and back to the phase-95 SPEC close:**
`206 10d7807bf02d` · `212 4a92f7e62fc6` · `218 2a7eb298b9fd` · `228 242e53c6f7a3` · `234 b2680e6f4fbf` · `242 6caa1c3ce0e7`
⇒ **THE SENTINEL DOES NOT FIRE, FOR TWO INDEPENDENT REASONS** (check (1) is non-silent AND check (2) reads SIX). **`stop` WAS EVALUATED AND DELIBERATELY NOT CREATED** (verified absent at the git root and in every stage worktree).
⚠️ **THE md5 DIGEST IS METHOD-SENSITIVE:** the six match ONLY with the **trailing newline included**. **State the method whenever you quote the digest.**
⚠️ **THIS RUN DOES NOT CARRY FORWARD — run all three checks, all four NCs *and* the check-(2) positive control YOURSELF, at YOUR OWN tip.**

⚠️⚠️ **NEVER RE-SPELL A SENTINEL MATCH PHRASE INSIDE A SENTINEL WINDOW** (the phase-94 rule: the sentence asserting the sentinel could not move MOVED IT, six -> seven, inside `:226`). **A PLAN DOES NOT EDIT `ROADMAP.md`, SO THIS RULE IS DORMANT THIS SESSION — BUT IT GOES LIVE AGAIN AT THE IMPL.**

- ⚠️ **LIVE FIGURES RE-DERIVED AT THIS TIP (the phase-96 PLAN close):** `ROADMAP.md` **246** lines / **128** data rows / tail row **96** (**`in-progress`**) · `DECISIONS.md` **18972** (BYTE-UNTOUCHED by this stage) · `^---$` **216** · `^## ADR-` **317** · bare `^## ` **325** · tail **ADR-0318** · next-free **ADR-0319** (`^## ADR-0319` reads **0**) · ⚠️ **house `PROPOSED` guard STAYS ARMED — a PLAN adds no ADR — and it resolves BY BACKWARD HEADING SEARCH to `## ADR-0318` at `:18944`; verify BY LINE AND BY ADR** · ADR-0231 decoy **1** at `:14866`, resolved to `14864 ## ADR-0231`, byte-untouched · middle-ground `^\*\*Status:\*\*.*PROPOSED` (NEVER a gate) · `BEHAVIOR_CONTRACT.md` **5989** (BYTE-UNTOUCHED) · `STATE.md` **65** (rolled IN PLACE, net 0) · `STATE_HISTORY.md` **560** (558 -> 560, **+2**), strict **163 (DELTA 0, as promised)** / parenthetical **66 -> 67** / loose **229 -> 230** (**163 + 67 = 230 exactly**) · `PLAN.md` **1666** · `SPEC.md` **668** · `BRAINSTORM.md` **761** · phase dirs **137** (a PLAN adds a FILE, not a directory) · fixtures **122** (tail `0120-tls-connection-error`; **`0121` is CLAIMED by `SPEC.md` §6 and STILL NOT CREATED — the IMPL creates it, taking the count to 123**) · fixture extractor **122 = 122**, both `comm` directions EMPTY, split **98 `driver/` + 24 `inputs/`**, and the extractor **NC'd** (a rename fires BOTH `comm` arms while the count stays 122) · fuzzers **56 targets / 48 FILES** · `go.mod` **67 REQUIRE ENTRIES** under a structural `awk` extractor (⚠️ the character-class form reads 62 — see method note 33) · `go list ./...` **237** (**235** after excluding the two Docker drivers) · BackendKind tail **38** (`test/differential/fixture/fixture.go:614`) · `-family row` **96 / 68** · `internal/tls/config.go` **666** (⚠️ NOT the 642 row 95 asserts twice) · `internal/listener/manager.go` **1636** (UNCHANGED — a PLAN lands ZERO `.go` bytes) · **D4-block literal-anchored digest `9154e453c99515d96a5d4bd9aea17fcd9b70c39682bf4418f40e0136a8ec0817` with uniqueness 1** · `internal/listener/quic.go` whole-file `071cb7a536872da1605f3ef213187141b7aa19029ffd001569e4bda7845b0edb`.
- ⚠️ **NO GATES WERE RUN AT THIS CLOSE, AND THAT IS THE STAGE SCOPE, NOT AN OMISSION.** A PLAN is docs-only; the six-gate posture belongs to the IMPL, and **YOUR STAGE RUNS ALL SIX** (`PLAN.md` §5 Task 18). The only executions at this close were this stage's own measurement agents — one reference-container arm set on ports 15500-15599 (containers removed BY NAME; the one-line predicate applied TEMPORARILY and reverted under `sha256sum -c`) and two read-only measurement agents that committed nothing and proved their trees clean.
- ⚠️ **THE STAT SURFACE IS QUOTED AS A DELTA AND NEVER AS AN ABSOLUTE.** Three different absolutes are live at one tip. **Phase 96 lands +0 stat NAMES** — the five `ssl.*` names already exist; what the row changes is WHETHER THEY ARE REGISTERED on a listener whose only TLS is its default chain. ⚠️ **THAT IS NO LONGER THE BLOCKING UNKNOWN — IT IS MEASURED** (`SPEC.md` §0.1). The ledger is at `### Stat surface`, `BEHAVIOR_CONTRACT.md:5071`; ⚠️ **the claim that "a `+0` row earns NO chain entry, per the convention at `:5130`" is REFUTED — `:5130` is a BLANK LINE and phases 44.2, 44.3, 45.2, 47.1 and 51 each carry a `+0, UNCHANGED` chain entry.** `SPEC.md` §11 row 11 decides it on the merits.
- ⚠️ do NOT "fix" the six in check (2) and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases; **no gate may rest on a ` + ` split** (wrong on 3 of the 6 windows).

==============================================

## STATUS AS OF 2026-09-08: PHASE 96 (`listener-default-chain-tlsmode`) **PLAN DONE**. Lifecycle-state **2 -> 3**; ROADMAP row 96 STAYS `in-progress`. **THE SENTINEL WAS RE-EVALUATED MECHANICALLY BEFORE AND AFTER THIS STAGE'S EDITS AND DOES NOT FIRE.** `stop` was NOT created. **THIS SESSION RUNS THE PHASE-96 IMPL.**

**What the PLAN decided, in one paragraph.** A **19-task** TDD spine, ordered so that **Tasks 1-5 land and RUN at the UN-FIXED TIP** — the un-fixed tip IS the negative control for roster rows 1-3 and it is destroyed by Task 6's one-line fix, so it must be spent first. The BOOTSTRAP **§6.1 split gate was EVALUATED AND NOT TRIPPED**: 19 tasks against ~25, and ≈ **+1350** on the accounting under which the MEASURED structural precedent `0a985a35` — the phase-94 IMPL, which created fixture `0120` from nothing, added `manager_test.go` arms for the same counter family and edited a predicate in the same function of the same file — scores **+1635** and was NOT split. **`0121`'s expectation map is MEASURED ON BOTH SIDES**, at N=1 and N=3, on the pinned digest: `ssl.handshake` and `ssl.no_certificate` each read **N** while the other three read **0**, identical cross-side — so `SPEC.md` §6's pin instruction is CONFIRMED and the `{handshake: N, rest: 0}` shape would indeed have failed against correct code. **DRIVE COUNT DECIDED: N = 3**, because `1` cannot discriminate a per-connection counter from a fire-once one.

### ⚠️ WHAT THE PHASE-96 PLAN REFUTED — READ THIS BEFORE TRUSTING ANY INHERITED FIGURE

**FIFTEEN claims refuted by execution.** The five load-bearing:

1. 🔴 **`SPEC.md` §10 SHIPS EIGHT FALSE FIGURES AT ITS OWN PUBLISHING COMMIT.** §10 is headed *"Counts, re-derived at THIS stage's own tip"* and names `f6b50462` — but the SPEC LANDS at `da6ea191`, whose own diff adds ADR-0318 §Context (**+30**) and an archive row (**+2**). Measured on both sides with `git show <sha>:<path>`: `DECISIONS.md` 18942 -> **18972** · `^## ADR-` 316 -> **317** · bare `^## ` 324 -> **325** · tail ADR-0317 -> **ADR-0318** · next-free ADR-0318 -> **ADR-0319** · `STATE_HISTORY.md` 556 -> **558** · parenthetical 65 -> **66** · loose 228 -> **229**. ⚠️ **A STAGE THAT MEASURES ITSELF BEFORE ITS OWN EDITS PUBLISHES A SNAPSHOT OF A TREE THAT NO LONGER EXISTS.** The other twenty-four §10 figures are CONFIRMED exact, and the archive TRIPLE reconciles at BOTH tips — only the operands moved.
2. 🔴 **THE FALSE-INVARIANT OCCURRENCE SET IS TEN, NOT NINE.** `ROADMAP.md:136` — row 74's notes cell — carries the corollary VERBATIM (*"`startQUIC` hard-errors without a TLS config, so every QUIC listener that boots has `tlsMode == true`"*), and the SPEC's own exclusion rule (*"historical copies under `docs/envoy-go/phases/`"*) DOES NOT REACH IT. All nine SPEC cites are EXACT at tip, so this is an ADDITION, not a correction — **and `PLAN.md` Task 16 makes it ride on the row-96 flip, which touches that file anyway.** ⚠️ **AND THE PHRASE VARIES**: a tree-wide search for the exact string finds only `DECISIONS.md`, `ROADMAP.md` and `manager.go`; sites 3 and 9 spell it differently. **Sweep the CLAIM, not one of its spellings, and sweep CASE-INSENSITIVELY** — `manager_test.go:2327` spells it `EQUIVALENT`.
3. 🔴 **`TestListenerMetrics_GateMatchesInc` HAS NO TABLE AND NO SHARED ASSERTION BODY.** `SPEC.md` §5.1 says *"ADD arm (d)/(e)/(f)"* and requires *"`t.Helper()` on any shared assertion body"* — a framing that presumes a table-driven test. `:2341-2493` is **three literal `t.Run` blocks with fully inlined, duplicated bodies**; `[]struct{`, `for _, tc := range` and `t.Helper()` each read **ZERO** in that range. The `t.Helper()` clause is **an instruction to CREATE something that does not exist**, and is VACUOUS if the new arms are inlined. `PLAN.md` §11 states the deliberate deviation rather than making it silently.
4. 🔴 **ARM (f) MUST CALL `mgr.Start(ctx)` OR IT IS RED REGARDLESS OF THE FIX.** `registerListenerMetrics` (`manager.go:394`) has exactly TWO call sites — `manager.go:1179` inside `Manager.Start` and **`quic.go:45` inside `startQUIC`** — and both run at **Start**, never at `NewManager`. The template `SPEC.md` §5.1 points arm (f) at, `manager_test.go:1062`, calls only `NewManager`. **A copied arm reads five nil pointers whether or not the predicate is widened, so NC roster row 3 would fire for the WRONG REASON.**
5. 🔴 **THE BYTE-UNTOUCHED ROSTER AND THE EDIT ROSTER INTERSECT AT `internal/listener/manager.go` ALONE — AND BOTH OBVIOUS GUARDS FAIL AGAINST CORRECT CODE.** A whole-file `sha256sum` is GUARANTEED to change (edit-roster rows 1 and 2 modify that file). A line-scoped `sed -n '746,747p' | sha256sum` is ALSO unsafe, because row 2 rewrites `:384-393` **ABOVE** the D4 block and any net delta shifts it. **The working guard is two-part and literal-anchored** — a uniqueness check (`grep -c -F '<anchor>'`, want 1) plus `grep -A1 -F '<anchor>' | sha256sum`, measured **`9154e453c99515d96a5d4bd9aea17fcd9b70c39682bf4418f40e0136a8ec0817`** — **and it needs its own liveness proof.** For `quic.go`, `internal/tls/**` (8 files) and `internal/stats/**` (23 files) a whole-file digest IS correct: no edit-roster row names them.

Also refuted: **`SPEC.md` §11's *"the PLAN lands `PLAN.md` only"* is UNDER-ENUMERATED** — a PLAN lands FOUR files (`PLAN.md`, `STATE.md`, `STATE_HISTORY.md` at exactly +2/-0, `next-prompt.txt`), measured across `e69f1c58` `90010c4c` `db539e7d` `f647dd72`; its four NEGATIVES are correct in 4/4 · **the project's recorded four-precedent PLAN set is {94, 93, 75, 77}**, and `bae5e24d`/`acedfd2b` PREDATE ADR-0288, so neither touches `STATE_HISTORY.md` and both create `PROGRESS.md` — inheriting that set gives the wrong verdict on exactly the two conditional files · **a slugless PLAN-stage CORRECTION commit exists** (`963d472d`) and `^phase 92 (` is blind to it · **`BEHAVIOR_CONTRACT.md:1973` is stale on TWO axes** — it says *"all four counters"* where `:1967` six lines above says *"All five"* · **`manager.go:766` puts the default chain into `chainByName`**, so arm (c)'s tlsCfg-nil loop would fire on the new arms, and **arm (b)'s `:2401-2403` is a SECOND dead assertion** which would be a FALSE POSITIVE if it went live (ADR-0080 §Decision 3 authorises exactly what it forbids) — `PLAN.md` Task 5 DELETES it · **`no_filter_chain_match` is a NAME-level divergence**, not a value one: the subject has NO production site (the string occurs once in the whole repo outside `docs/`, in a test comment), so a `== 0` cross-side pin is SILENTLY VACUOUS, not red · **the reference's seventeen `ssl.*` names are POST-DRIVE; at boot it registers FOURTEEN** (`ssl.ciphers.*`, `ssl.curves.*`, `ssl.versions.*` appear only after a handshake) · **the crash site is `manager.go:1393`, the SUCCESS-path `rt.sslHandshake.Inc()`**, not the `fail_verify_*` classifier sites · **`counterValue` vs `pollCounter` is a DISCRIMINATION requirement** — `pollCounter` returns a silent 0 for both *registered-and-zero* and *never-registered* · **`SPEC.md` §11 row 8 mis-files an edit**: `DECISIONS.md:17359` resolves to **ADR-0297**, not ADR-0296 · **`buildListenerRuntime` without `WithCtx` DOES NOT EXIST** · and **site 4's comment block runs `:2325-2340`, not `:2325-2332`**, so the stale cites at `:2337` fall OUTSIDE the edit range as §11 row 6 states it.

### ⚠️ WHAT THE IMPL MUST NOT RE-DERIVE

- **The reference measurement** (`SPEC.md` §0.1 and **ADR-0318 §Context ¶2**, where it lives as a GOVERNING record — method note 21) **and `0121`'s expectation map** (`PLAN.md` §3.1, measured on BOTH sides at N=1 and N=3, with the passing map and the failing map both written out). **DRIVE COUNT IS DECIDED: N = 3.**
- **The stage file-scope split** (`PLAN.md` §0.2, measured across the FOUR modern PLAN commits). **The IMPL's roster is `SPEC.md` §11's fifteen rows** as amended by `PLAN.md` §0.6 (row 14 gains site 10), §0.14 (row 8's filing) and §0.15 (row 6's range).
- **The ten-site occurrence set and the byte-untouched roster** (`SPEC.md` §0.3 + `PLAN.md` §0.6, §0.13). **The intersection is `manager.go` ALONE, and the working guard is in `PLAN.md` §0.13 with its digest already measured.**
- **The fixture port `10127`** — NOT `10121` (`0028` holds `10120`-`10125`) and NOT `10126` (`0120` holds it). Censused free in CODE SCOPE, and the census was NC'd against `10126`, which correctly reads as taken. **Re-census at your tip; do not re-argue the choice.**
- **D4-MIXEDTLS is NOT the bug and its comment block at `manager.go:746-747` MUST NOT CHANGE** — ADR-0080 §Decision 3 authorises the exemption VERBATIM, and §Decision 3 is SYMMETRIC where §Consequences (c)'s illustration is one-sided.

### Banked, NOT chartered

- **D2-QUICTS — the QUIC no-`transport_socket` deferred reject.** **DECIDED AT THE SPEC: IT DOES NOT RIDE** (`SPEC.md` §4). Reference-confirmed PARITY; fails CLOSED; its only externally-observable half is `-mode validate` rc=0 where the reference's `--mode validate` returns rc=1. Already banked in a governing document (ADR-0317 `D-ALPNFB-TCPONLY`).
- **D10-QUICSEL — the `quicChain()`/`quicTLSConfig()` accessor split.** Needs its own ADR (multi-chain QUIC dispatch vs ADR-0080's fallback semantics). ⚠️ **This row makes the disagreement OBSERVABLE without resolving it.**
- ⚠️ **THE `stat_prefix` DUPLICATE-REGISTRATION PANIC — the strongest banked candidate.** Two listeners sharing an HCM `stat_prefix` — a common, valid Envoy config the reference **ACCEPTS** — make envoy-go **panic rc=2** (`stats: duplicate metric registration`, `internal/stats/registry.go:107` via `internal/filter/hcm/config.go:358`), including under `-mode validate`. **It deserves its own row.**
- **The driver-owned receiver port race — 37 files** (31 panic / 5 `fmt.Errorf` / 1 other), under `test/fixtures/*/driver/` and `*/inputs/`, NOT `test/differential/`. ⚠️ **Its trigger has still NOT fired.** Do not open it on the note alone.
- **An SDS + `alpn_protocols` fixture — the runner-up.** Intersection genuinely EMPTY; adds coverage rather than fixing a defect.
- **D8-FCM's PGV-strictness nit** — a structurally invalid `filter_chain_match` inside `default_filter_chain` is PGV-rejected by the reference and silently ignored here. **UNMEASURED against a live reference.**
- The six stale citations plus `manager_test.go:5571` · `0108`'s three false `ssl.*` confessions · `0118/driver/driver.go:31`'s falsified band (0108-0113 is **10444-10449**, so 10450 is one PAST its end) · `0120/expectations.yaml`'s *"Phase 94 fixture"* first line · the nine-or-twelve history-class `GetConfigForClient` comment sites (**a CONTESTED count — quote NO number**) · `0061-lb-ring-hash`'s σ-margin second occurrence · 1xx interim responses · the H/1 no-`Host` divergence · the pooled-upstream-lifetime defect · the other TEN fixed `ssl.*` names and FOUR dynamic families (blocked on NAMING).
## ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + ALL FOUR NCs + THE CHECK-(2) POSITIVE CONTROL YOURSELF** and record ACTUAL output. ⚠️ **NC SHAPES CHANGE ACROSS A ROW CHANGE — NEVER INHERIT ONE.** They changed TWICE at phase 95 (the flip) and again at the phase-96 ADD, in the opposite direction.
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** 94 IMPL twelve · 95 BRAINSTORM nine · 95 SPEC eleven · 95 PLAN seventeen · 95 IMPL eighteen · 96 BRAINSTORM thirteen · 96 SPEC ten · **96 PLAN FIFTEEN** — and the SOURCE matters more than the count: at the 96 PLAN, **FIVE were in the SPEC's own §10 and §11 (the two sections it told the PLAN to trust verbatim)**, and one was a whole site the SPEC's enumeration rule structurally could not reach. ⚠️ **EXPECT TO BE REFUTED BY YOUR OWN REVIEW SEAM, NOT ONLY BY YOUR SUCCESSOR.** ⚠️ **AND WHEN TWO AGENTS CONTRADICT EACH OTHER, FIND THE VARIABLE RATHER THAN PICKING A WINNER.**
3. **LINE ANCHORS DRIFT; SYMBOL AND LITERAL-TEXT ANCHORS DO NOT — RE-VERIFY EVERY CITE AT YOUR TIP.** ⚠️ **PATHSPEC-SCOPE every symbol assertion and every `sed`.** ⚠️ **`internal/filter/hcm/chain.go` DOES NOT EXIST — it is `internal/filter/http/chain.go`** — but **`actions.go` IS in `hcm`.**
3a. ⚠️ **AN ANCHOR CAN BE NON-UNIQUE.** **Anchor on the ENCLOSING SYMBOL.**
3b. ⚠️ **CHARACTER-CLASS AND ESCAPE SELECTORS ARE THE FAIL-UNSAFE FAMILY.** `^[0-9]{4}-` on fixtures reads 120, dropping `0007a`/`0007b` (use `ls -d test/fixtures/*/ | wc -l`) · **a CASE-SENSITIVE GREP IS BLIND TO UPPERCASE PROSE — use `-i` when auditing comments** · and see note 33.
3c. ⚠️ **`feedback_brief_citations_not_evidence` APPLIES TO INCIDENTAL FIGURES — including every number in THIS file, in `BRAINSTORM.md`, and in every AGENT REPORT.** At the phase-96 BRAINSTORM four measurement agents ran; every headline figure was re-derived by the controller before use, **and doing so caught the router's own `:1944` and `config.go` figures.**
3d. ⚠️ **NO GATE MAY REST ON A ` + ` SPLIT.**
3e. ⚠️ **A GATE COMMAND CAN MATCH PROSE INSTEAD OF CODE.** Anchor fixture-set extraction on the blank-import LINE:
```sh
extract () { grep -oE '^[[:space:]]*_ "github\.com/pgdad/envoy-go/test/fixtures/[^/]+/(driver|inputs)"$' "$1" \
  | sed -E 's#.*/test/fixtures/##; s#/(driver|inputs)"$##' | sort; }
```
**At this tip: dirs 122 = imports 122 (`test/differential/runner_test.go`), both `comm` directions EMPTY**, split **98 `driver/` + 24 `inputs/`**. **NC the extractor** (rename one import in a SCRATCH COPY — it must fire in BOTH `comm` directions). ⚠️ **A COUNT-ONLY CHECK IS VACUOUS: the import count stays 122 under a rename** — RE-PROVEN at the phase-96 BRAINSTORM.
3f. ⚠️ **"AT THE RETURN" IS NOT A PLACEMENT.** Anchor installs on the ENTRY of the path that must be covered and PROVE the install with an arm that reddens without it.
3g. ⚠️ **A "CHEAP ASSERTION GAP" CAN BE A BEHAVIOUR DIVERGENCE WEARING A DISGUISE.** Measure the reference for THAT ARM before asserting any cross-side stat.
3i. ⚠️ **A LOOSE `--grep 'phase NN.*STAGE'` MATCHES THE WRONG STAGE.** **Anchor stage-commit locates on `^phase NN (slug) STAGE:`.**
3j. ⚠️ **`grep` IS A DIFFERENT PROGRAM IN A SUBAGENT SHELL THAN IN THE CONTROLLER SHELL.** In the CONTROLLER shell `grep` is a **function** resolving to **ugrep 7.8.4** which honours `.gitignore` and is therefore **BLIND to `next-prompt.txt`**; in a SUBAGENT shell `grep` is **`/usr/bin/grep` (GNU 3.11)** and sees it fine. **Both readings are correct; the variable is the SHELL, not the tree.** In the controller shell use `command grep -rl`, `/usr/bin/grep`, `git grep`, or a direct path; ⚠️ **`command grep` DOES NOT SURVIVE `xargs`.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files). **NAME THE SHELL AS WELL AS THE TOOL.** ⚠️ **The flag-trap error TEXT differs between the two (`option -f: cannot read amily row` vs `No such file or directory`) but rc=2 is identical** — do not gate on the message.
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Live: TWO SDS dial-budget flakes plus `TestSDSEndToEnd_FetchFailure_BootFailsClosed` · the driver-owned receiver port race · `internal/httpclient` zero-value · the two 84.2-era flakes · the REFERENCE h2spec section-8 flip · `TestOutlierDetector_ConcurrentEjectExactlyOnce` · `0061-lb-ring-hash`'s σ-margin · `TestP83_StopPauseTimer_IsAuthoritativeAgainstAnEnteredClosure` (wasm, 1/400) · **`internal/filter/hcm/h2 TestFramer_ReaderGoroutineDoesNotLeak`** — ⚠️ **ISOLATED AT THE PHASE-96 BRAINSTORM AND IT DID NOT REPRODUCE**: 5/5 and 257/257 green, both non-vacuous (`RUN` asserted beside `RC`). **BUT ITS GUARD CARRIES SLACK 2 OVER 40 CONNECTIONS AND REPORTED `delta=0` EVERY ITERATION, so a small real leak would sit INSIDE that margin. UNREPRODUCED IS NOT DISPROVEN; the registration STANDS.** ⚠️ **`TestServerConn_TinyWindowDelivery` IS NOT A FLAKE** — a recurrence is a REGRESSION of row 91. ⚠️ **A GREEN RERUN CLEARS NOTHING.**
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only). ⚠️ **AT THE PHASE-96 SPEC A SIBLING `curl-world` SESSION STILL OWNED ~SEVENTEEN CONTAINERS holding `18080-19000`.** ⚠️ **A `reaper_*` testcontainers Ryuk container is created BY THE DIFFERENTIAL ITSELF and is REUSED; leave it alone.** **Treat NO roster as stable, and remove ONLY the containers you named yourself.**
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`. ⚠️ **`INNER_EXIT` DOES NOT EXIST in this repo.**
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL, THEN DRIVE THE ARM.** **`gofmt -l` never exits non-zero — gate on OUTPUT.** ⚠️ **go test WITHOUT `-v` PRINTS ZERO `=== RUN`** — `RUN=0` beside `RC=0` is a VACUOUS GREEN. ⚠️ **A `-run` selector matching nothing prints `[no tests to run]` and EXITS 0.** ⚠️ **On `-v` output, unanchored `grep -c 'FAIL'` reads nonzero ON A FULLY GREEN TREE** — use `grep -cE '^(FAIL|--- FAIL)|^ *--- FAIL'`.
7b. ⚠️ **`grep -c` ON ZERO MATCHES PRINTS `0` AND EXITS 1** — capture, NEVER chain with `&&`, NEVER `$(… || echo 0)`.
7c. ⚠️ **A GREEN TEST RUN IS NOT EVIDENCE A SITE IS EXERCISED** — prove it with a **`panic()` REACHABILITY CONTROL**. **There is NO `recover()` in non-test `internal/tls` or `internal/listener`, so a panic aborts the binary — gate on `grep -cE '^panic:'`.** ⚠️ **AT PHASE 96 THAT FACT IS THE SUBJECT, NOT JUST THE METHOD.**
7d. ⚠️ **NC EVERY NEW TEST, AND NC YOUR OWN FIXES** — NEUTRALISE so the package still compiles; check the NC is EXECUTABLE.
7e. ⚠️ **A UNIT TABLE OVER A SHARED ASSERTION IS NOT A GATE ON PRODUCTION BEHAVIOUR** — `t.Helper()` gives each failure a DISTINCT call-site line.
7f. ⚠️ **A NC THAT LEAVES YOUR NEGATIVE CONTROL GREEN IS NOT EVIDENCE THAT CONTROL DOES ANY WORK.**
7g. ⚠️ **ASSERT *WHICH* ERROR FIRED, AND ALSO WHICH DID NOT** — ⚠️ **BUT NOT VIA A CLIENT-VISIBLE ERROR STRING IF THE ALERT CODE IS VERSION-DEPENDENT.** **Use the server-side counter triple.** ⚠️ **KEEP TABLE ROWS SINGLE-CAUSE.** ⚠️ **PREFER AN EXACT EQUALITY TO A FLOOR.**
7h. ⚠️ **A PROBE CLIENT CAN WITHHOLD THE VERY THING THE ARM EXISTS TO TEST.** ⚠️ **`openssl s_client` answers "did the handshake complete", NOT "was the connection served"** — drive a real request AFTER the handshake. ⚠️ **A TEST TABLE CAN CARRY AN INVENTED INPUT NO PRODUCTION PATH PRODUCES** — build contexts with the package's OWN helpers.
7i. ⚠️ **A GATE THAT READS 0 HAS NOT BEEN SHOWN TO WORK.** Run every silent gate over an input KNOWN to trip it. **The house `PROPOSED` guard reads 0 and was PROVEN LIVE at this close on a scratch copy; a 0 there is the RESTING STATE.**
7j. ⚠️ **A PIN CAN FAIL AGAINST *CORRECT* CODE.** **Before writing an expectation map, MEASURE the leaf set a completing handshake actually books.**
7k. ⚠️ **ASSERT THE POINTERS, NOT ONLY THE EFFECT.** At phase 96 `sslHandshake == nil` is the CAUSE and the SIGSEGV is the EFFECT; a probe that observes only the crash proves something happened, not what. Assert both, in that order ([[reference_nil_stats_counter_inc_crashes_goroutine]]).
8. ⚠️ **`pgrep -f` / `pkill -f` MATCHES YOUR OWN SHELL AND KILLS THE TOOL CALL (exit 144)**. **Kill ONLY PIDs you captured yourself at launch.** ⚠️ **`pgrep -la a b` ERRORS** — one pattern per call.
9. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-*`, `wt-*` or stage worktree should outlive its stage close** — expect only `master` (verify with `git worktree list`).
10. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** (a) differential — **122/122 today, 123/123 once `SPEC.md` §6's fixture `0121` lands**; (b) non-Docker sweep gated on `PIPESTATUS[0]` + a SET RECONCILIATION — **235 packages**; (c) h2spec `95 tests, 94 passed, 1 skipped, 0 failed`; (d) fuzzers **56 / 48**; (e) the ANCHORED panic gate `^panic:|DATA RACE|SIGSEGV`, **0** *and PROVEN LIVE*; (f) no REVIEW.md — standing departure. ⚠️ **`-race` on the DIFFERENTIAL suite is VACUOUS** — the subject is an unraced subprocess. **THE IMPL RUNS ALL SIX; A SPEC AND A PLAN RUN NONE, AND THAT IS SCOPE, NOT OMISSION.**
11. ⚠️ **THE DIFFERENTIAL'S FAILURE MODE IS A SILENT PASS.** `-count=1` IS NOT OPTIONAL. **ASSERT THE FIXTURE SET BY NAME, BOTH DIRECTIONS.** The full suite takes ~400s.
11b. ⚠️ **`go test ./...` DRIVES DOCKER — AND THERE ARE TWO DOCKER DRIVERS.** Exclude BOTH: `go list ./... | grep -vE '/test/differential$|/test/conformance/h2spec$'`.
12. ⚠️ **`-p` PUBLISHING WORKS; `--network host` IS WHAT DOES NOT.** ⚠️ **`STRICT_DNS` + `host.docker.internal` + `--add-host=host.docker.internal:host-gateway` IS REQUIRED for a container-to-host cluster HERE.** ⚠️ **THE REFERENCE CONTAINER CANNOT READ `filename:` CERT PATHS** — use `inline_string:` — ⚠️ **AND INDENT THE PEM ONE LEVEL DEEPER THAN `inline_string:`**. ⚠️ **A PROBE BACKEND MUST BIND `0.0.0.0`, NOT `127.0.0.1`.** ⚠️ **Keep PEM substitutions OUT of YAML comments.** ⚠️ **envoy-go REJECTS `match.headers`**, **refuses `codec_type: HTTP2` without TLS unless `--allow-h2c`**, **boots with `-c`, NOT `--config-path`**, **`access_log[].log_format` is BOOT-REJECTED**, **`TLSv1_0`/`TLSv1_1` in `tls_params` BOOT-REJECT**. ⚠️ **AN OMITTED `clusters` KEY BOOT-REJECTS envoy-go** — give a probe subject a placeholder STATIC cluster. ⚠️ **`ENVOY_TARGET.md` is at `docs/envoy-go/ENVOY_TARGET.md`, NOT the repo root.** ⚠️ **The pinned reference is `envoyproxy/envoy:contrib-v1.37.2`, digest `7edd5b0fd763…` — PIN BY DIGEST, verify against `ENVOY_TARGET.md:3-4`** (RE-VERIFIED IN USE at the phase-96 BRAINSTORM). ⚠️ **`timeout` rc=124 is shared by a HEALTHY server and a HUNG boot — read the OUTPUT (`starting main dispatch loop` / `envoy-go … ready`), never the code.** ⚠️ **AND envoy-go's validate mode is `-mode validate`, the reference's is `--mode validate`.**
13. ⚠️ **A RAW-FRAMER PEER MUST HOLD ONE `hpack.Decoder` PER CONNECTION.**
14. ⚠️ **`bash /dev/tcp` + `timeout cat <&3` INSIDE COMMAND SUBSTITUTION RETURNS SILENTLY EMPTY** — use Go for socket probes.
15. ⚠️ **GO DOES NOT FLAG UNUSED CONSTANTS.** Post-removal cleanup is grep-driven.
16. ⚠️ **`golangci-lint`'s misspell runs in locale US.** **Sweep British spellings in `.go` comments BEFORE the gate.** Markdown prose may use them freely.
17. ⚠️ **A GREEN RUN OF A CONTENTION-SENSITIVE TEST PROVES NOTHING.**
18. ⚠️ **AN ABSENCE CLAIM NEEDS A POSITIVE CONTROL AND A DISCRIMINATING NEGATIVE CONTROL.**
19. ⚠️ **DERIVE next-free ADR FROM THE TAIL, NEVER FROM THE HEADING COUNT.** The id space is SPARSE (exactly one gap, `0209`). At this tip `^## ADR-` reads **317** and headings+1 is **318**, which collides with **ADR-0318 — the TAIL, and TAKEN.** The correct derivation is `grep -oE '^## ADR-[0-9]+' … | tail -1` ⇒ `## ADR-0318`, next-free **ADR-0319**, confirmed by `grep -c '^## ADR-0319'` ⇒ **0**. ⚠️ **The collision figures ROT — re-derive both at your tip rather than quoting these.**
20. ⚠️ **`reference_measured_prototype_is_a_lower_bound` HAS FIRED THIRTEEN CONSECUTIVE ROWS, AND THE PHASE-96 SPEC MADE IT FOURTEEN BEFORE THE CODE WAS WRITTEN** — the BRAINSTORM's one-line floor excluded a fixture, an inverted assertion, three new test arms and a nine-site prose reconciliation. **ENUMERATE BY GREPPING THE LITERAL SYMBOL OUTWARD, CASE-INSENSITIVELY, PATHSPEC-SCOPED, AND BY COMPILING.**
21. ⚠️ **A MEASUREMENT THAT GOVERNS A DEFERRAL DECISION MUST LAND IN A GOVERNING DOCUMENT.** The phase-96 reference measurement lives in `BRAINSTORM.md` §2.4 and **must be carried into ADR-0318 at the SPEC** — a BRAINSTORM is not a governing document (the phase-94 lesson: a measurement that landed only in a BRAINSTORM was lost for seventeen rows).
22. ⚠️ **A STAGE'S FILE SCOPE IS ITSELF MEASURABLE, AND THE ROUTER CAN BE WRONG ABOUT IT — IT WAS, TWICE IN TWO ROWS.** `git show --numstat` on the precedent stages settles it, and **MEASURE MORE THAN ONE**. MEASURED at the phase-96 SPEC across the 92/93/94/95 SPEC commits: **EXACTLY FIVE FILES, identical in all four** — `DECISIONS.md` (+28..+30, additions only), `STATE.md` (in place), `STATE_HISTORY.md` (+2), the new `SPEC.md`, `next-prompt.txt` — **and ZERO `.go` bytes**, with `ROADMAP.md` and `BEHAVIOR_CONTRACT.md` BYTE-UNTOUCHED. **For a PLAN, measure against the phase-94 and phase-95 PLAN commits; the phase-95 measurement across FOUR precedent PLAN commits says `PLAN.md` ONLY.**
23. ⚠️ **`git diff --stat` IS A SUM, NOT ADDITIONS.** Cost figures come from `--numstat`.
24. ⚠️ **AN UNESCAPED `|` IN A ROADMAP ROW PASSES CHECK (1) AND SILENTLY BREAKS THE FIELD COUNT.** Count fields (want **8**) under BOTH forms, BEFORE and AFTER installing. **Reword a pipe AWAY rather than escaping it.** **DORMANT at a SPEC; live again at the IMPL.**
25. ⚠️ **THE EVICTION CHECK IS A *PAIR*, AND THE BARE FORMS DO NOT ANSWER IT.** At the phase-96 close BOTH bare forms read **5** on `STATE.md`, and **both are invariant under which entry is evicted**. Only the LABEL-BOUND form discriminates, on BOTH files: the evictee's own label goes `STATE.md` **1 -> 0** and `STATE_HISTORY.md` **0 -> 1**. **VERIFIED IN BOTH DIRECTIONS at this close, with a fabricated-label NC (0 in both files) and a positive control on a sibling label that IS present (reads 1).**
26. ⚠️ **A DIRECT DATE READ CAN FAIL TO PICK THE EVICTEE.** The tie has now sat in the MIDDLE, AT THE OLDEST POSITION, ABSENT, AS A THREE-WAY TIE AT THE HEAD, and — at this close — **AS A FOUR-WAY TIE AT THE HEAD**, across five consecutive closes. At this tip the five dates read `09-07, 09-07, 09-07, 09-07, 09-06`, so date and position still AGREE on the oldest, **but for a narrower reason again.** **That agreement is a coincidence of this tip, not a rule.** When a tie IS at the oldest position, fall back to LIST POSITION — the tail — never to a guess.
27. ⚠️ **A SYMBOL-SCOPED CLAIM IS NOT A PATH-SCOPED ONE.** **Enumerate the call sites and check each caller's own branch.** ⚠️ **Pathspec scoping that keeps a diff clean is the SAME scoping that hides its callers.**
28. ⚠️ **A `+0/+0` NEGATIVE-CONTROL ARM CAN BE SILENTLY DELETED WITH EVERY GATE STAYING GREEN.** **Diff the ARM ROSTER, not the counters**, and treat a shrinking roster as a finding even when every pin passes.
29. ⚠️ **A MULTI-INSERT LINE SHIFT IS BANDED, NOT A CONSTANT.** **Never hand a scalar shift for a multi-insert file — re-locate every cite by LITERAL TEXT (`grep -nF -- "<exact line>" <path>`).** Line numbers are derived data; the literal is the identity.
30. ⚠️ **A `go test` SELECTOR NAMING A PACKAGE THAT DOES NOT EXIST PRINTS `FAIL … [setup failed]` AND EXITS 1** — it reads exactly like a real red run. **Confirm a selector resolves (`go list ./path/...`) before believing a FAIL.** This is the mirror of the `-run` footgun, which fails the OTHER way (no match ⇒ EXIT 0).
31. ⚠️ **RECONCILE A CORRECTED CLAIM ACROSS ITS WHOLE OCCURRENCE SET.** **After correcting any claim, grep its distinctive phrase tree-wide and fix every hit, or state which hits you are deliberately leaving and why.**
32. ⚠️ **A ROW CAN SHIP A FALSE FIGURE IN THE COMMIT THAT WRITES IT, AND A "CORRECTION" IS NOT A MEASUREMENT.** Row 95 asserts `internal/tls/config.go` landed at **642** — measured **666** at `0f3b98b3`, the commit that wrote it — while convicting its own PLAN's 629 of being stale. And the phase-95 router carried `BEHAVIOR_CONTRACT.md:1944` as **357 chars** while the row it closed said 357 -> 1841; measured **1841 BYTES / 1830 CHARS**, with the published sibling `:1971` figure (**5098**) being a CHAR count. ⚠️ **A THREE-FIGURE GATE ON THOSE LINES WOULD FIRE AGAINST CORRECT CODE ON TWO OF THREE. NAME THE MEASURE (bytes or chars) OR THE FIGURE IS MEANINGLESS**, and **re-derive any count you quote by running the command at YOUR publishing tip.**
33. ⚠️ **A CHARACTER CLASS SILENTLY DROPS WHAT IT DOES NOT SPELL.** `grep -cE '^\s+[a-z0-9./-]+ v[0-9]'` on `go.mod` reads **62**; the count is **67**. The class excludes uppercase and `_`, dropping exactly `AdaLogics/go-fuzz-headers`, `Azure/go-ansiterm`, `Microsoft/go-winio`, `Microsoft/hcsshim`, `prometheus/client_model`. **Prefer a structural extractor (`awk` over the `require` blocks) to a character class, and diff the two when they disagree.**
34. ⚠️ **TWO INDEPENDENT AGREEING MEASUREMENTS DO NOT MAKE A CLAIM GENERAL.** Phase 95's controller and its scoped re-reviewer both measured F-E and agreed it was benign; **both enumerated the same single config**, and a third member of the class escapes the guard they relied on. **Agreement between readers is evidence about the shape they both examined, never about the class. Enumerate the class, THEN measure.**
35. ⚠️ **A GUARD'S NAME IS NOT ITS COVERAGE — AND A DEAD ASSERTION CAN BE INVERTED AS WELL AS ABSENT, OR SIMPLY WRONG.** `TestListenerMetrics_GateMatchesInc` exists to assert the equivalence phase 96 refutes, and every landed arm builds a `filter_chains[]`-only listener, so its default-chain assertions never execute. **`manager_test.go:2468-2470` asserts the CRASHING configuration is a FAILURE and must be INVERTED; `:2401-2403` is its MIRROR, equally dead, and would be a FALSE POSITIVE if it ever went live — ADR-0080 §Decision 3 authorises exactly what it forbids — so it is DELETED, not inverted.** **Read the arms, then read what each one would assert if it ran.**
36. ⚠️ **A VARIABLE REUSED FOR A SECOND PURPOSE OUTLIVES THE INVARIANT THAT MADE IT SAFE.** `anyTLS` was correct as a mixed-TLS accumulator; phase 74 reused it as the stat-registration gate, inheriting an invariant ADR-0080 had already repealed. **When you gate a new feature on an existing boolean, prove that boolean still means what the new use needs, and NAME THE ADR THAT COULD REPEAL IT.**
37. ⚠️ **A FIX SHAPE CAN ENCODE A PARITY ANSWER NOBODY HAS MEASURED.** Two repairs that both stop a crash but disagree about the resulting stat surface are NOT interchangeable, **and diff size is not the tiebreaker. Measure the reference, then choose.**
38. ⚠️ **AN OCCURRENCE SET CAN BE NORMATIVE, NOT MERELY COMMENTARY.** The phase-96 false invariant lives in ADR-0296 §Decision (a), ADR-0296 §Context ¶8(ii) and `BEHAVIOR_CONTRACT.md:1973` as well as in six `.go` comments. **Sweep `docs/` with the same case-insensitive breadth as the code, resolve every markdown hit to its enclosing ADR by BACKWARD heading search, and say which hits you are deliberately leaving and why** — several nearby statements are TRUE and must not be "corrected".
39. ⚠️ **A BARE-TOKEN GREP'S FALSE POSITIVES ARE CONFIG-DEPENDENT, SO A KNOWN WITNESS IS NOT A GATE.** An unanchored `grep ssl` over a PLAINTEXT listener's `/stats` hit `server.accesslog_dropped` (`acce-ssl-og`) on one rig and `downstream_cx_ssl_active`/`_total` on another. **Anchor (`^listener\.[^:]*\.ssl\.`) and NC the anchored form against the arm that must read zero.**
40. ⚠️ **A COUNT WITHOUT ITS MATCHER IS MEANINGLESS, AND TWO CORRECT COUNTS CAN DISAGREE.** The reference's listener-scope `ssl.*` family reads **17** under the anchored form and **20** under a looser one that folds in `server_ssl_socket_factory.*`, a DIFFERENT scope. Both are true. **Name the matcher beside every figure** (the sibling of note 32's bytes-vs-chars).
41. ⚠️ **AN ILLUSTRATION IN §CONSEQUENCES IS NOT THE LIMIT OF A §DECISION.** ADR-0080 §Decision 3 authorises an independent default-chain TLS posture in BOTH directions; its §Consequences (c) illustrates only ONE, and the shape that crashes is the other. **Read the decision, not the example.**
42. ⚠️ **A SECTION HEADED *"RE-DERIVED AT THIS STAGE'S OWN TIP"* IS FALSE IF IT NAMES A COMMIT THAT IS NOT THE ONE IT SHIPS ON.** `SPEC.md` §10 measured at `f6b50462` and published at `da6ea191`; the publishing diff itself moved EIGHT of its thirty-two figures. **A stage's own edits are part of its tip. Re-measure AFTER writing, in the SAME commit** — and note that the SPEC's `+30` ADR append and `+2` archive append are exactly the two edits every stage makes, so this trap is STRUCTURAL, not a one-off ([[reference_row_can_ship_false_figure_at_birth]]).
43. ⚠️ **AN ENUMERATION'S EXCLUSION RULE CAN HAVE A HOLE ITS AUTHOR DID NOT SEE.** `SPEC.md` §0.3 excused *"historical copies under `docs/envoy-go/phases/`"* and thereby did not reach `ROADMAP.md:136`, which is a live governing document carrying the identical false claim. **State the exclusion as a PREDICATE and then test every live file against it**, rather than listing the directories you happen to be thinking of. ⚠️ **AND SWEEP THE CLAIM, NOT ONE SPELLING OF IT** — three live carriers here use three different wordings, and one uses UPPERCASE.
44. ⚠️ **A TEST-STRUCTURE ASSUMPTION IS A CLAIM AND MUST BE READ, NOT PRESUMED.** `SPEC.md` §5.1 instructed *"ADD arm (d)"* and *"`t.Helper()` on any shared assertion body"*; the target test has **no table and no shared body**, so one instruction is a metaphor and the other is vacuous. **Before writing a task against a test, grep it for `[]struct{`, `for _, tc := range` and `t.Helper()`, and say what you found.**
45. ⚠️ **A REGISTRATION-TIME SYMBOL IS NOT A CONSTRUCTION-TIME SYMBOL.** `registerListenerMetrics` runs at `Manager.Start`, from TWO call sites (`manager.go:1179` and `quic.go:45`), never at `NewManager`. A test copied from a `NewManager`-only template asserts nil pointers **regardless of the fix**, so its negative control fires for the wrong reason. **Enumerate the call sites and ask WHEN each runs, not only WHERE it is.**
46. ⚠️ **A CROSS-SIDE PIN ON A NAME ONE SIDE NEVER EMITS IS SILENTLY VACUOUS, NOT RED.** `no_filter_chain_match` is emitted at 0 by the reference and **not at all** by the subject — the string has no production site in this repo. Because a prometheus scrape map returns the zero value for a missing key, `got[name] != 0` PASSES. **Before pinning a stat cross-side, prove the NAME exists on both sides**; a value-level warning is not the same finding as a name-level absence. And its sibling: **the reference registers 14 listener-scope `ssl.*` names at boot and 17 only after a handshake** — three are dynamic families — so a presence set asserted PRE-drive fails against correct code.
## ⚠️ THE `PROPOSED` GUARD — **STILL ARMED; THE PHASE-96 IMPL DISARMS IT**

| form | now | where |
|---|---|---|
| `^> \*\*STATUS: PROPOSED` — **the house form** | **ARMED** | the phase-96 SPEC drafted `ADR-0318` §Context and armed it; **the PLAN added no ADR, so it is UNCHANGED**. It resolves BY BACKWARD HEADING SEARCH to `## ADR-0318` at `:18944`. ⚠️ **A ZERO HERE IS THE RESTING STATE, NOT EVIDENCE THE GUARD IS BROKEN** — PROVEN LIVE again at the phase-96 PLAN close on a scratch copy (append a house-form line ⇒ it moves). **THE PHASE-96 IMPL DISARMS IT** when it flips ADR-0318 to `ACCEPTED` in place (`PLAN.md` Task 15). |
| `^\*\*Status:\*\* PROPOSED` — **the ADR-0231 decoy** | unchanged, reads 1 | `:14866`, resolved BY BACKWARD HEADING SEARCH to `14864 ## ADR-0231`, byte-untouched |
| `^\*\*Status:\*\*.*PROPOSED` — **the middle ground** | a large number | ⚠️ **NEVER A GATE** |

⚠️ **VERIFY BY LINE AND BY ADR, NEVER BY THE COUNT ALONE** — resolve any hit to its enclosing ADR by a BACKWARD heading search (`awk 'NR<=<HITLINE> && /^## ADR-/ {h=$0; n=NR} END {print n, h}'`). ⚠️ **NEVER gate on the UNANCHORED form.** ⚠️ **AND DO NOT QUOTE A COUNT FOR EITHER GATE FORM IN PROSE THE GREP MATCHES** — the phase-93 SPEC falsified itself doing exactly that, which is why no figure is written in the table above. **ADR-0318 §Context IS DRAFTED (11 paragraphs) AND ITS §Decision + §Consequences ARE APPENDED IN PLACE AT THE IMPL, after the RETAINED italic footer — NO renumber and NO `---` separator. ADR-0319 IS THE NEXT FREE ID.** `DECISIONS.md` at this close: **18972** (+30), `^---$` STAYS **216**, `^## ADR-` **317**, bare `^## ` **325**, tail **ADR-0318**. ⚠️ **A PLAN ADDS NO ADR — every one of those figures must STAY across your stage.**

## ⚠️ THE ARCHIVE GUARD

⚠️ **THE STRICT `163` IS NOT THE ENTRY COUNT.** Re-measured at THIS tip (phase-96 PLAN close, `STATE_HISTORY.md` **560**): loose **230** = strict **163** + parenthetical **67**, exactly — **the triple reconciles only under these ANCHORED-OCCURRENCE forms:**
```sh
H=docs/envoy-go/STATE_HISTORY.md
grep -oE '^- \*\*prior active-phase:\*\* ' $H | wc -l   # strict         163  <- DELTA 0 across a correct close
grep -oE '^- \*\*prior active-phase \('    $H | wc -l   # parenthetical   67  <- +1 per close
grep -oE '^- \*\*prior active-phase'       $H | wc -l   # loose          230  = 163 + 67
```
⚠️ The unanchored `grep -c 'prior active-phase'` counts LINES and coincides by accident; the colon form is a FOURTH regex. **Name the form or the figure is meaningless.** ⚠️ **`grep -F` WITH A PATTERN STARTING `- ` HITS THE FLAG TRAP** — pass `--` first. **The guard is SOUND as a DELTA-0 SHAPE check** (a correctly-shaped parenthetical append moves the STRICT form by 0 — verified again at this close) **but the number is a SUBSET.**

⚠️ **THE ARCHIVE'S POSITIVE CONTROLS ARE SELF-INCREMENTING.** This stage's archive line names NO control figure. ⚠️ **AND THE RAW LINE DELTA IS `+2`, NOT `+1`** — the entry is a blank line PLUS the entry line. **The phase-96 PLAN close: `STATE_HISTORY.md` 558 -> 560**, evicting the phase-95 BRAINSTORM entry (`2026-09-06`), with the strict guard DELTA 0 exactly as promised. **An IMPL close evicts the next-oldest entry if the five-entry cap still binds — but READ, DO NOT PREDICT.** ⚠️⚠️ **AND THE DATE TIE IS NOW TOTAL: after this roll ALL FIVE §Recent entries share `2026-09-07`**, because the PLAN's own entry is dated `2026-09-08` and lives in §Current, not §Recent. **A DIRECT DATE READ CANNOT PICK AN EVICTEE AT YOUR TIP AT ALL — FALL BACK TO LIST POSITION, THE TAIL, NEVER TO A GUESS.** One close earlier FOUR shared the newest date and the unique oldest was still the tail, so date and position AGREED; they no longer can. The shape has been the MIDDLE, the OLDEST POSITION, ABSENT, a THREE-WAY head tie, a FOUR-WAY head tie, and now a FIVE-WAY TOTAL tie across seven consecutive closes. ⚠️ **THE BARE FORMS ANSWER NOTHING** — both read FIVE on `STATE.md` and both are invariant under which entry is evicted. **Only the LABEL-BOUND PAIR discriminates**, run on BOTH files, with a fabricated-label NC and a positive control on a sibling label that IS present.

## ⚠️ PORT BANDING

`net.ipv4.ip_local_port_range = 32768 60999`. ⇒ **BAND YOUR PROBE PORTS BELOW 32768.** ⚠️ **The differential harness reserves `20000..31007` (`harness_test.go:292-296`) and `11000..14999` (`:209-212`)**, so `15000-19000` is the ad-hoc band — ⚠️ **minus `18080-19000`, held by sibling `curl-world` containers.** **The phase-96 BRAINSTORM used `15100-15109` / `15200-15207`; the phase-96 SPEC used `15300-15399` (agent) and `15400-15407` (controller), all cleanly.** **Check availability with `ss -tan` AND `ss -uan` (ALL states), never `ss -ltn`.** ⚠️ **FOR A UNIT TABLE, USE PORT 0.** ⚠️ **`net.Pipe` DEADLOCKS a client-cert handshake — use a loopback TCP pair.** ⚠️ **FIXTURE ports follow the `10<index>` family band, BUT THE BAND IS NOT INDEX-ALIGNED WHERE IT COLLIDES**: `0028` holds `10120`-`10125` as a contiguous six-listener run and `0120` holds `10126`, so **`SPEC.md` §6 assigns `0121` the port `10127`** — the minimal index-preserving repair, on the `0120` precedent. Censused free in CODE SCOPE, and the census was NC'd against `10126`, which correctly reads as taken. ⚠️ **The TLS/SDS fixtures `0108`-`0113` hold `10444`-`10449`, so `10450` is one PAST that band, not a member of it.**

## ⚠️ A RECURSIVE `grep` IN THE *CONTROLLER* SHELL IS BLIND TO `next-prompt.txt`

See method note **3j** — this is SHELL-DEPENDENT. In the controller shell `grep` is a **function** (ugrep 7.8.4, honours `.gitignore`); `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. `command grep -rl …` / `/usr/bin/grep` (GNU) sees it; direct-path access works. **In a SUBAGENT shell the bare `grep` is `/usr/bin/grep` and already sees it.** ⚠️ **`command grep` DOES NOT SURVIVE `xargs`.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files). ⚠️ **ugrep ALSO REJECTS SOME COMPLEX `grep -oE` PATTERNS with `exceeds complexity limits` where GNU grep accepts them** — observed at the phase-96 SPEC; fall back to `/usr/bin/grep` or `awk`.

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 96 (listener-default-chain-tlsmode)' --format='%h %s'   # the SLUG form
git log --grep '^phase 96' --format='%h %s'                                    # the LOOSE form — RUN IT to CROSS-CHECK
```
⚠️ **RUN BOTH FORMS AND RECONCILE THE SETS. NO COUNT IS QUOTED HERE, DELIBERATELY — BOTH FORMS ROT.** ⚠️ **A `.*SPEC` / `.*PLAN` STYLE LOOSE GREP MATCHES THE WRONG STAGE** (method note 3i). **Take the tip from `git rev-parse master`.** ⚠️ **AND WHEN YOU LAND A CORRECTION, PUT THE FULL SLUG IN ITS SUBJECT.**

Execution style: **subagent-driven per `feedback_execution_style`**. Subagents commit LOCALLY on their own stage branch with EXPLICIT PATHSPECS; the controller merges and squashes (`feedback_subagents_no_push`, `feedback_push_to_origin`). **Pure MEASUREMENT agents should commit NOTHING and prove their tree clean.** ⚠️ **PARALLEL AGENTS IN ONE WORKTREE RACE THE GIT INDEX** — per-agent worktrees on their own branches, private scratch dirs, **Docker serialized to exactly ONE agent at a time** (two agents ran this way at the phase-96 SPEC). ⚠️ **VERIFY WHAT A SUBAGENT REPORTS — BUT ALSO BELIEVE IT WHEN IT REFUTES YOU.** At the phase-96 SPEC an agent reported that the reference emits `ssl.*` for the default-chain-only shape; the controller reproduced it independently from configs it wrote itself, on a disjoint port band, and got the same answer — **and in doing so found that the agent's incidental `20` and the controller's `17` were both correct under different matchers.** ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root** — build with `-o` into scratch.

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root — **it fired repeatedly again at the phase-96 SPEC.** Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` before any commit. ⚠️ **Shell VARIABLES do not survive between Bash calls** — re-export in every command. ⚠️ **AN UNQUOTED HEREDOC (`<<EOF`) COMMAND-SUBSTITUTES BACKTICKED TEXT** — use `<<'EOF'`.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE OR PROTOTYPE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — `sha256sum` BEFORE patching, `git checkout -- <path>` after, `sha256sum -c` the capture — ⚠️ **and COMMIT FIRST if anything else is pending, because `git checkout --` restores from HEAD.** ⚠️ **Also check the MAIN repo root, and `--untracked-files=all`.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads RUN THE PHASE-96 IMPL.** ⚠️ **Its §Recent is FIVE entries under the ADR-0288 cap, and ALL FIVE now share the date `2026-09-07` — a TOTAL tie, so a date read cannot pick an evictee at all. FALL BACK TO LIST POSITION, THE TAIL.**
2. `docs/envoy-go/phases/96-listener-default-chain-tlsmode/PLAN.md` — **1666 lines. THE CONTRACT FOR THIS STAGE.** ⚠️ **§5 is the 19-task spine and ITS ORDER IS LOAD-BEARING**; §4.1 is the stable-anchor table (use it instead of every line number in this file); §6 is the CORRECTED twelve-row NC roster (three re-specified, three added); §0 carries the FIFTEEN refutations, of which §0.1, §0.6, §0.8, §0.9 and §0.13 change what the IMPL must do; §3 carries the MEASURED `0121` expectation map and the drive count.
3. `docs/envoy-go/phases/96-listener-default-chain-tlsmode/SPEC.md` — **668 lines. THE DESIGN.** ⚠️ **Its §11 is the fifteen-row IMPL edit roster — READ IT THROUGH `PLAN.md` §0.6 / §0.14 / §0.15, which amend three of its rows.** ⚠️ **Its §10 is STALE IN EIGHT PLACES** (`PLAN.md` §0.1); use `PLAN.md` §7 instead.
4. `docs/envoy-go/phases/96-listener-default-chain-tlsmode/BRAINSTORM.md` — **761 lines. THE CHARTER.** ⚠️ **Its §6.3 "+0 fixtures" forecast and its §10 item 7 are SUPERSEDED by `SPEC.md` §6 and §0.2.**
5. `internal/listener/manager.go` — the single `tlsMode:` write at `:825`, `registerListenerMetrics`'s `if rt.tlsMode {` at `:398`, `serveConnection`'s `if selected.tlsCfg != nil {` at `:1363` and its five `.Inc()` sites, ⚠️ **the CRASH site `rt.sslHandshake.Inc()` at `:1393`**, ⚠️ **`chainByName[defaultSpec.Name] = defaultChain` at `:766`**, and ⚠️ **the D4 block at `:746-747` that MUST NOT CHANGE.** Anchor by LITERAL text; every line number here will rot the moment Task 6 lands.
6. `docs/envoy-go/DECISIONS.md` **ADR-0318** (§Context drafted at the SPEC; §Decision + §Consequences APPENDED IN PLACE at Task 15, after the RETAINED italic footer, no renumber and NO `---` separator) and **ADR-0080** §Decision 3 + §Consequences (b) and (c). ⚠️ **ADR-0080 AUTHORISES EIGHT OF THE TEN DIVERGENCES. DO NOT "FIX" D4.**
7. `docs/envoy-go/DECISIONS.md` **ADR-0296** §Decision (a) (`:17296`) and §Context ¶8(ii) (`:17276`) — **the NORMATIVE carriers.** ⚠️ **ITS DECISION SURVIVES; ONLY ITS JUSTIFICATION IS REPEALED — LEAD WITH WHAT SURVIVES.** ⚠️ **And `:17359` is in ADR-0297, not ADR-0296** (`PLAN.md` §0.14).
8. `internal/listener/manager_test.go` — `TestListenerMetrics_GateMatchesInc` (`:2341-2493`), ⚠️ **which has NO TABLE and NO SHARED ASSERTION BODY**; the INVERTED assertion at `:2468-2470`; ⚠️ **its MIRROR at `:2401-2403`, which Task 5 DELETES**; and the §5.2 templates `startOneWayTLSListener` (`:4877`), `driveALPNAuthProbe` (`:5704`), `assertSSLCrossProduct` (`:4755`), `sslLeafRoster` (`:4725`), `connPair` (`:4407` — a REAL loopback pair; ⚠️ **`net.Pipe` DEADLOCKS a client-cert handshake**).
9. ⚠️ **`BOOTSTRAP_PROMPT.md` IS AT THE REPO ROOT.** §5 the lifecycle state machine (state 3 -> `superpowers:subagent-driven-development` or `superpowers:executing-plans`, TDD per task, appending to `PROGRESS.md`) — §5 appears VERBATIM TWICE, §5 and the §11 appendix; **the second copy's offset is NON-constant; find it by heading, never by arithmetic**. ⚠️ **§6.1's MID-EXECUTION split trigger is still live: if any single task's sub-steps blow past ~10, split THEN** (`PLAN.md` §1.2 names Tasks 10 and 2 as the likeliest).

## What the next session owes
**RUN THE PHASE-96 IMPL (lifecycle 3 -> DONE)** — ⚠️ **ONE STAGE PER SESSION.** · **EXECUTE `PLAN.md` §5's NINETEEN TASKS IN ORDER** — ⚠️ **THE ORDER IS LOAD-BEARING: Tasks 1-5 LAND AND RUN AT THE UN-FIXED TIP, because the un-fixed tip IS the negative control for NC roster rows 1-3 and Task 6 DESTROYS IT** · **RUN ALL SIX GATES AND NAME DEPARTURES RATHER THAN CLAIMING COMPLIANCE** (`PLAN.md` Task 18): differential **123/123** once `0121` lands, the non-Docker sweep on `PIPESTATUS[0]` + a SET RECONCILIATION at **235** packages, h2spec `95 tests, 94 passed, 1 skipped, 0 failed`, fuzzers **56 / 48**, the ANCHORED panic gate `^panic:|DATA RACE|SIGSEGV` at **0** *and PROVEN LIVE*, and no `REVIEW.md` as the standing departure · ⚠️ **CREATE FIXTURE `0121-listener-default-chain-tls` AT REFERENCE PORT `10127`, WITH ALL THREE REGISTRATION GATES** — the directory, the `fixture.RegisterFixture` call, **and the blank import in `test/differential/runner_test.go`, which is SILENTLY GREEN if missed** · **PIN `ssl.handshake` AND `ssl.no_certificate` AT 3 AND THE OTHER THREE AT 0** — MEASURED on both sides at `PLAN.md` §3.1; ⚠️ **do NOT pin `no_filter_chain_match`, not even `== 0`** · ⚠️ **CORRECT THE TENTH OCCURRENCE SITE AT `ROADMAP.md:136`**, which `SPEC.md` §0.3 could not reach · ⚠️ **CORRECT `BEHAVIOR_CONTRACT.md:1973` ON BOTH AXES** — it says *"all four counters"* where `:1967` says *"All five"* · ⚠️ **DELETE `manager_test.go:2401-2403`**, do not invert it · ⚠️ **ARM (f) MUST CALL `mgr.Start(ctx)`** · ⚠️ **USE `assertSSLCrossProduct`, NOT `pollCounter`**, or the non-mover assertions pass VACUOUSLY at the un-fixed tip · ⚠️ **ROW 96 -> `done` AND ITS "+0 fixtures" CELL -> "+1 fixtures"**; count row fields (want **8**) under BOTH forms BEFORE and AFTER, and **reword a pipe away rather than escaping it** · ⚠️ **NEVER RE-SPELL A SENTINEL MATCH PHRASE INSIDE A SENTINEL WINDOW — THIS RULE IS LIVE AT YOUR STAGE** · ⚠️ **RE-RUN the sentinel + all four NCs + the check-(2) positive control BEFORE *AND AFTER* YOUR `ROADMAP.md` EDIT** — the shapes MOVE INSIDE YOUR SESSION: check (1) goes SILENT and NC-A and NC-B each drop from TWO lines to ONE · **DISARM the house `PROPOSED` guard** by completing ADR-0318 in place (no renumber, no `---`, `^---$` STAYS 216) and **NC it live** · **ASSERT THE BYTE-UNTOUCHED ROSTER** — whole-file `sha256sum` for `quic.go`, `internal/tls/**`, `internal/stats/**`, and **the TWO-PART LITERAL-ANCHORED guard for the D4 block, because both obvious guards fail against correct code** · **REFUTE YOUR PREDECESSOR BY EXECUTION** and record it — ⚠️ **the PLAN refuted FIVE claims inside the two SPEC sections it was told to trust verbatim, so re-derive every figure you quote from either document** · `PROGRESS.md` created (the IMPL owns it) · `STATE.md` rolled IN PLACE with the oldest §Recent entry evicted — ⚠️ **ALL FIVE DATES NOW TIE, so use LIST POSITION and the LABEL-BOUND PAIR across BOTH files** — and archived to `STATE_HISTORY.md` as **ONE INLINE LINE** in the PARENTHETICAL form, **strict guard 163 DELTA 0**, raw delta **+2**, ⚠️ **name NO positive-control figure in the archive line** · **ROLL THE §Recent PREAMBLE SENTENCE TOO, without spelling the evictee's label** · ⚠️ **RE-DERIVE ANY LINE COUNT YOU QUOTE IN `STATE.md` IN THE SAME COMMIT AS THE EDIT THAT MOVES IT** — `PLAN.md` §0.1 is this project's freshest demonstration of what happens otherwise · `next-prompt.txt` rolled (`git add -f`) · ONE squashed commit, merged and pushed — ⚠️ **and if a late measurement invalidates something you already pushed, land a CLEARLY-LABELLED correction commit (WITH THE FULL SLUG IN ITS SUBJECT) rather than force-pushing or leaving it standing.**
