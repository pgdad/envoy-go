START a fresh cold-start session for envoy-go — **phase 31 (`network-filter-kafka-broker`) BRAINSTORM is DONE**: the FIFTH §9 Network-filters-family row is opened. `kafka_broker` (`envoy.filters.network.kafka_broker`) is the new subject — a passive header-only Kafka sniffer landing as a new `internal/filter/network/kafkabroker/` package (the 9th network-filter built-in; consumer #3 of the ADR-0221 WriteFilter seam; the project's FIRST `github.com/envoyproxy/go-control-plane/contrib` consumer). Phase 30 (the contrib-image pin-refresh to `envoyproxy/envoy:contrib-v1.37.2`, ADR-0227, CLOSED) was its standalone infra prerequisite — the contrib reference image is now the differential pin. The next lifecycle step is the **phase-31 SPEC** (`superpowers:writing-plans` scoped to SPEC authoring).

The phase-31 BRAINSTORM landed at the BRAINSTORM squash `78d5f36` (the live master tip; squash-merged from `phase-31-network-filter-kafka-broker-brainstorm` + pushed by the controller; confirm via `git log --oneline -1`). Substantive predecessor on master: the phase-30 IMPL squash `2d24f34` (phase 30 CLOSED; the docs-only SHA-fill follow-up `0effb7e` trails it). origin/master in sync after push.

## What phase 31 IS (the kafka_broker §9 family row)

Phase 31 lands `envoy.filters.network.kafka_broker` (proto `envoy.extensions.filters.network.kafka_broker.v3.KafkaBroker`, in the `/contrib v1.32.4` module) at the BRAINSTORM-settled envelope:
- **HEADER-ONLY decode** (generic across all API keys): request header = `api_key` int16 / `api_version` int16 / `correlation_id` int32 / `client_id` nullable-string [hdr v1+] / tagged fields [hdr v2 flexible]; response header = `correlation_id` int32 + optional tagged fields; 4-byte length prefix on both. NO per-API-key body decode.
- **FULL per-API-key `request.<key>` / `response.<key>` counter parity in BOTH directions** under `kafka.<stat_prefix>.`, recovering the api_key on the response side via a `correlation_id`→`api_key` per-connection map (the mongo-29.2 correlation precedent under an ADR-0223-style per-connection mutex) + `request.unknown`/`request.failure` + `response.unknown`/`response.failure`.
- The four active features (`force_response_rewrite`, `id_based_broker_address_rewrite_spec`, `api_keys_allowed`, `api_keys_denied`) PARSE-ACCEPTED, kept at defaults in fixtures, BEHAVIOR DEFERRED (the mongo `header_delay` precedent; the broker-address-rewrite would MUTATE the write buffer = a NEW framework capability, flagged as a future surgery sub-phase). `request.failure`/`response.failure` a unit-tested coverage boundary; histograms deferred (ADR-0060).
- The project's FIRST `/contrib` go.mod dep — added WITH its first consumer (an unused module dep cannot survive `go mod tidy`).
- A SINGLE unsplit §9 row (header-only kafka is much leaner than mongo — no BSON parser, trivial response header, no fault-delay seam; ~600–1100 LoC / ~12–18 tasks, under the ADR-0045 gate) + a PRE-AUTHORIZED 31.1-request / 31.2-response split axis as the escape valve.
- Cross-side `StatsAsserter` differential (UNBLOCKED by phase 30) over hermetic synthesized frames (`0053-kafka-requests` + possibly `0054-kafka-boot-reject`) + a NEW BackendKind (a correlation-id-echoing TCP Kafka responder, anticipated value 31) + the 40th fuzzer `FuzzKafkaDecode` + the `kafka.` Prometheus tag-extractor arm + the `stats.IsValidName` charset guard. Anticipated 1 ADR — **ADR-0228**.

ZERO framework-seam touch (pure consumer; ZERO-touch on the ADR-0226 async halt/resume seam — never-halting, byte-identical R1; the filter implements `WriteFilter` even where observation-only to get the 28.1b post-handoff read seam).

## What this session does — the phase-31 SPEC

Per the project's brainstorm→SPEC precedent (`superpowers:writing-plans` scoped to SPEC authoring), author `docs/envoy-go/phases/31-network-filter-kafka-broker/SPEC.md` (+ README) from the phase-31 BRAINSTORM. EXECUTE the §10 empirical pins IN-SESSION (parallel-subagent fan-out per the 25/26/27/28/29 SPEC precedent) against the contrib reference Envoy (`envoyproxy/envoy:contrib-v1.37.2`, ADR-0227) + go-control-plane `/contrib` v1.32.4 bindings, via the bridge-network live-probe precedent (`reference_docker_probe_bridge_network` — STRICT_DNS backend; verify decode ran via `downstream_cx_rx_bytes_total > 0`):
- **D31-1** (BLOCKING) — verify the TypeURL via `proto.MessageName` on `kafka_brokerv3.KafkaBroker` (the `extensions.` lesson — NOT the docs string); confirm `/contrib v1.32.4` resolves AND `go mod tidy` keeps it with the first consumer.
- **D31-2** (BLOCKING) — live-probe the EXACT stat roster: the api_key→name table, `request.*`/`response.*` naming, eager-vs-dynamic creation, the Prometheus label-hoist form (the mongo D-P2 lesson — dynamic segments may be FULLY label-hoisted, not in the name).
- **D31-3** — the `(api_key, api_version) → request/response header-version` table incl. flexible/tagged-field framing + the wire layout (adopt upstream's framing verbatim, `reference_wire_format_both_sides_see_same_bytes`).
- **D31-4** — the `request.failure`/`response.failure` coverage boundary + the `request.unknown`/`response.unknown` header-derived semantics.
- **D31-5** — the `stats.IsValidName` charset-guard placement at the codec boundary (`reference_dynamic_stat_name_charset_guard`).
- **D31-6** — confirm whether any kafka counter is close-direction-keyed (`reference_close_direction_framework_gap`; likely NOT — defer to a surgery boundary if so).
- **D31-7** — the 5-field config PGV arms (`stat_prefix` required → boot-reject) + the parse-accept-defer arms for the four active features + the `0054` include-or-fold decision.
- **D31-8** — the ADR-0045 task/LoC gate-check confirming phase 31 fits as a single row (whether the pre-authorized 31.1/31.2 split is consumed).

Anchor the **ADR-0228** §Context draft (§Decision/§Consequences body lands at the IMPL per ADR-0044). Re-check the ADR-0045 split-gate. Work subagent-driven (`feedback_execution_style`); spec-document-reviewer pass.

## Read first (in order)

1. THIS FILE — the phase-31 SPEC cold-start.
2. `docs/envoy-go/phases/31-network-filter-kafka-broker/BRAINSTORM.md` — the full charter: the Q0/Q1/Q-split decisions (§2), the framework-survey (§3), the stat-surface hypothesis (§5), the fixture envelope (§6), the anticipated ADR-0228 (§7), the deferred items (§8), and the §10 D31-1..D31-8 empirical pins this SPEC executes.
3. `docs/envoy-go/phases/30-pin-refresh-envoy-contrib/SPEC.md` §1.3 (not-a-family-row) + §13 (counts) + `docs/envoy-go/phases/30-pin-refresh-envoy-contrib/BRAINSTORM.md` §2.1 (the three kafka_broker blockers) + §4 (the phase-31 forward-pointer, now REFINED by the phase-31 BRAINSTORM).
4. `docs/envoy-go/ENVOY_TARGET.md` — the current pin (`envoyproxy/envoy:contrib-v1.37.2` @ `7edd5b0f…`, ADR-0227; `parseEnvoyTarget`/`StartReferenceProxy` boot it for every reference-booting fixture) + doctrine D-3.7.
5. `docs/envoy-go/STATE.md` — active-phase `phase 31 BRAINSTORM done`; lifecycle-state → phase-31 SPEC; next-skill `superpowers:writing-plans`.
6. `docs/envoy-go/ROADMAP.md` — row 31 (`in-progress`, depends-on 30) + the §9 Network-filters family heading + candidate roster (kafka_broker IN-PROGRESS/landing; {redis, thrift} remain after phase 31).
7. `docs/envoy-go/DECISIONS.md` — ADR-0106 (§9 family flat-row discipline) + ADR-0045 (split-gate) + ADR-0221 (WriteFilter seam) + ADR-0223 (per-connection mutex) + ADR-0060 (histograms deferred) + ADR-0227 (the contrib pin) + ADR-0044 (ADR §-body timing).
8. The mongoproxy package `internal/filter/network/mongoproxy/` (the §9-sniffer package-shape precedent for `kafkabroker`) + `internal/stats/name.go` (the `.mongo.`/`.zookeeper.` tag-extractor arm pattern for the new `kafka.` arm).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — network-filter-typeurl-extensions + wire-format-both-sides + dynamic-stat-name-charset-guard + close-direction-framework-gap + docker-probe-bridge-network + network-chain-terminal-handoff-ends-ondata + differential-asserter-dispatch + differential-break-protocol-count1 + per-task-gofmt-lint + subagents-no-push + push-to-origin + git-worktrees + execution-style (all recur for kafka_broker).

## Counts at master tip (phase 31 BRAINSTORM done — UNCHANGED until IMPL)

- stat surface **360**; differential fixtures **54** (tail `0052-mongo-fault-delay`); fuzzers **39**; BackendKind tail **30** (`TCPMongoResponder`); DECISIONS.md tail **ADR-0227** (next-free **ADR-0228**, anticipated for phase 31).
- conformance h2spec 53/53 + proxy-wasm 10/10 (image-independent).
- reference pin: `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227, the contrib variant of upstream v1.37.2).
- Toolchain: Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); go-control-plane `/envoy` v1.32.4 (ADR-0008); `/contrib` v1.32.4 confirmed resolvable (the phase-31 dep — added at phase 31 IMPL with its first consumer, NOT before).
- §9 Network-filters family: rows 26/27/28/29 DONE + row 30 (pin-refresh) DONE; **row 31 `kafka_broker` IN-PROGRESS** (this brainstorm); after phase 31 phase-done the candidates {redis, thrift} remain.
