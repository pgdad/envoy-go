=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-CHECKED 2026-07-13 (after the phase-61.1 PLAN landed, run MECHANICALLY) — it does NOT fire. Do NOT create `stop`.** The 61.1 PLAN is docs-only, so the sentinel state is UNCHANGED from before it landed. State:
- (1) prints `NOT DONE: row 61` — ONE `in-progress` row. Row 61 `http3-downstream-listener` is the HTTP/3-family opener; its FIRST leg (61.1) now has BRAINSTORM + SPEC + PLAN landed (this stage), NO IMPL yet. Row 61 flips `done` only when ALL THREE legs (61.1/61.2/61.3) land (ADR-0106). This ALONE blocks `stop`.
- (2) prints THREE live "candidates:" sentences — HTTP/3, xDS, and Observability ⇒ three families STAY OPEN. (`reference_sentinel_deferred_sentence_live_vs_historical`.)
- (3) prints `NEVER OPENED: gRPC`, `Runtime`, `WASM` ⇒ THREE families never opened. Operational-tooling also STAYS OPEN.

All three checks print ⇒ the sentinel does NOT fire.

**STATUS AS OF 2026-07-13: the phase-61.1 PLAN LANDED (the QUIC/UDP listen-substrate leg decomposed into a 9-task TDD spine + PROGRESS scaffold; docs-only; ROADMAP row 61 STAYS `in-progress`; DECISIONS tail STAYS ADR-0280 — ADR-0279 lands at the 61.1 IMPL).** The NEXT IMPLEMENTABLE STAGE is the **phase-61.1 IMPL** (`http3-quic-substrate` leg 61.1 — execute `PLAN-61.1.md` task-by-task; lifecycle-state 3 → done for the 61.1 leg). This is the roller's continuation of the banked HTTP/3 family (the most defensible next stage per the standing directive). Row 61 flips `done` only when ALL three legs (61.1/61.2/61.3) land (ADR-0106).

**What the 61.1 PLAN delivered (for CONTEXT — the IMPL executes it):**
- `docs/envoy-go/phases/61-http3-downstream-listener/PLAN-61.1.md` — a 9-task TDD spine (RE-DERIVED against master tip `728732a2`; the phase-60 legs shifted SPEC-61's `cbda648b`-cited numbers — the `transport_protocol` reject moved `685-689`→`690-695`; the reject test `1187-1196`→`1175-1199`; `NewDownstreamConfig` gained the 60.2 `provider` param). quic-go v0.54.1's REAL API (`quic.Listen`/`(*quic.Listener).Accept`→`*quic.Conn`/`(*quic.Conn).ConnectionState().TLS`/`DialAddr`) + the go-control-plane QUIC proto getters (`UdpListenerConfig.GetQuicOptions`, `QuicDownstreamTransport.GetDownstreamTlsContext`, the `QuicProtocolOptions` tuning getters) were VERIFIED at PLAN time (an isolated scratch-module `go doc` for quic-go — NOT the repo's go.mod; `go doc` for the protos).
- `docs/envoy-go/phases/61-http3-downstream-listener/PROGRESS-61.1.md` — the baselines + the design pins + the anticipated exit counts (new go.mod module `quic-go`; +0 packages/fixtures/fuzzers/BackendKind; +0 stat surface — the QUIC listener reuses `registerListenerMetrics`; ADR-0279 at the IMPL).

**The phase-61.1 IMPL scope (RE-READ PLAN-61.1 + PROGRESS-61.1 + SPEC-61 at session start; `git fetch` first):**
- **This is a CODE stage** — execute `PLAN-61.1.md` Tasks 2–9 (Task 1 = the PROGRESS scaffold, folded into the PLAN commit), subagent-driven (`feedback_execution_style`), in a FRESH worktree off master (`.worktrees/phase-61.1-impl`, branch `phase-61-http3-quic-substrate-impl`, per `feedback_git_worktrees`). Row 61 STAYS `in-progress`.
- **The deliverable (leg 61.1 — QUIC/UDP listen substrate):** the FIRST external go.mod module `github.com/quic-go/quic-go v0.54.1` (the last release keeping the project's `go 1.23` directive — do NOT bump it); a UDP/QUIC listen path parallel to the TCP one (`listenerRuntime.kind` discriminant from `udp_listener_config.quic_options` presence; `Start` branches `net.Listen("tcp",…)` vs `net.ListenUDP` + `quic.Listen`; a sibling `quicAcceptLoop`/`serveQUICConnection` in a same-package `internal/listener/quic.go`, quic-go confined there); the `transport_protocol "quic"` reject lift; a NEW `internal/tls.NewQUICDownstreamConfig` unwrapping `QuicDownstreamTransport` → the inner `DownstreamTlsContext` → the SHARED `commonTLSContextToConfig` (ALPN `h3`, mandatory TLS); the config-parity mandatory-TLS reject + the ADR-0080 strict-reject roster (0-RTT + `quic_options` tuning sub-fields); the reused per-listener `downstream_cx_total`. Verified SUBJECT-side by a handshake integration test (a local quic-go client completes the QUIC/`h3`-ALPN/TLS-1.3 handshake) — **NO HTTP serving yet (that is 61.2).** ANCHORS **ADR-0279** (§Decision/§Consequences authored at THIS IMPL per ADR-0044; §Context re-uses SPEC-61 §13; DECISIONS tail **ADR-0280 → ADR-0279**, out of numeric order per the reservation ledger; next-free **ADR-0281**).
- **Legs 61.2/61.3 (for CONTEXT — NOT this IMPL):** 61.2 = the H3 codec + HCM adapter (a THIRD dispatch arm `internal/filter/hcm/h3` modeled on `WriteH2`; lift the `codec_type HTTP3` reject at `hcm/config.go:240`; the HTTP3-on-non-QUIC config-parity reject); 61.3 = the differential fixture + harness UDP/H3-client surgery (a shared `test/helpers/h3.go` `H3RoundTrip` + the harness's first non-TCP transport; the `0102-http3-downstream-get` cross-side fixture). The 61.1 IMPL lands ONLY 61.1.

**ADR reservation ledger (provisional; each finalized at its IMPL in landing order):** ADR-0277 = phase-59 (LANDED) · ADR-0278 = xDS 60.1 (LANDED) · ADR-0280 = xDS 60.2 (LANDED) · **ADR-0279 = HTTP/3 61.1 — the NEXT ADR to land, at the 61.1 IMPL (out of numeric order after ADR-0280 per the parallel-fan-out reservation)** · next-free unreserved **ADR-0281** (the 61.2 codec-arm decision MAY consume it, or fold into ADR-0279 — the 61.2 PLAN/IMPL decide).

**Current counts (VERIFIED at the phase-61.1 PLAN session against master tip `728732a2` — RE-VERIFY at session start, `git fetch` first):** fixtures **105** (tail `0103-xds-sds-server-cert`) · fuzzers **55** · DECISIONS tail **ADR-0280** (next-to-land **ADR-0279** = HTTP/3 61.1; next-free **ADR-0281**) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** · new go.mod modules **0** (the 61.1 IMPL adds the FIRST — `quic-go v0.54.1`). At the 61.1 IMPL exit: new go.mod modules **+1**; fixtures/fuzzers/BackendKind/stat-surface/packages UNCHANGED (all +0 — the QUIC listener reuses the per-listener metrics; no fixture until 61.3).

**Locating the phase-61.1 PLAN stage squash** (a router cannot name its own sha — identify by SUBJECT, never by position):
```sh
git log --oneline --grep='^phase 61.1 (http3-quic-substrate) PLAN' -1
```

**THE NEXT ACTION: the phase-61.1 IMPL (lifecycle-state 3 for the 61.1 leg — execute PLAN-61.1 Tasks 2–9; the FIRST external go.mod module + the QUIC/UDP listen substrate + the handshake proof; row 61 STAYS `in-progress`).** After the 61.1 IMPL lands, the roller's next stage is the phase-61.2 PLAN (the H3 codec + HCM adapter). The sentinel governs termination, not this list.

==============================================

## What THIS session does (the phase-61.1 IMPL)

Execute `docs/envoy-go/phases/61-http3-downstream-listener/PLAN-61.1.md` task-by-task (Tasks 2–9), subagent-driven (`feedback_execution_style`), in a FRESH worktree off master (`.worktrees/phase-61.1-impl`, branch `phase-61-http3-quic-substrate-impl`, per `feedback_git_worktrees`). This is a SINGLE STAGE (the 61.1 IMPL — the QUIC/UDP listen substrate). Row 61 STAYS `in-progress` (flips `done` only when ALL three legs land, ADR-0106).

**Deliverables (per PLAN-61.1):**
- Production: `internal/tls/config.go` (`NewQUICDownstreamConfig` + the 0-RTT reject), `internal/listener/manager.go` (the `kind` discriminant + the QUIC transport-socket branch + the mandatory-TLS reject + the `Start`/`Stop`/`Listeners` kind-awareness + `validateQUICOptions` + the `transport_protocol "quic"` lift), `internal/listener/quic.go` (NEW — the UDP/QUIC accept path; the sole quic-go import), `go.mod`/`go.sum` (the FIRST external module `quic-go v0.54.1`).
- Tests: the `parseChainSpec` QUIC-accept flip, `NewQUICDownstreamConfig` unit tests, the `kind`/mandatory-TLS runtime tests, the module compile-gate, the subject-side handshake integration test (local quic-go client, ALPN `h3` + TLS 1.3, `-race`-clean), the strict-reject arm tests.
- Docs: `docs/envoy-go/BEHAVIOR_CONTRACT.md` (the HTTP/3 QUIC-substrate section), `docs/envoy-go/DECISIONS.md` (ADR-0279 §Decision/§Consequences), `PROGRESS-61.1.md` (task check-off), `docs/envoy-go/STATE.md` (active-phase header, controller), `docs/envoy-go/ROADMAP.md` (row 61 STAYS `in-progress` — verify, no flip), and the router roll (this file → the phase-61.2 PLAN).

## Honor throughout (the discipline, unchanged)
- **Lifecycle = one stage per session.** This session: the phase-61.1 IMPL ONLY. After it lands: the phase-61.2 PLAN (or the roller's self-pick; the sentinel governs).
- **Process anchors:** ADR-0044 (ADR §Decision/§Consequences land at the IMPL — ADR-0279 body at THIS 61.1 IMPL) · ADR-0045 (the split escape-valve — CONSUMED for row 61) · ADR-0080 (distinct per-arm reject substrings — the QUIC strict-reject roster) · ADR-0106 (row 61 flips `done` only when ALL legs land).
- **`next-prompt.txt` IS TRACKED** (`reference_next_prompt_tracked_despite_gitignore`) — edit it inside the stage worktree and fold into the stage squash; locate commits by SUBJECT (`git log --grep`), never by position.
- **TDD (`superpowers:test-driven-development`) + per-task gates (`feedback_pertask_gofmt_lint`):** every code task is red→green; every code task ends with `gofmt -l` + `golangci-lint run` on touched packages + `go vet` + `go build ./...`. quic-go confined to `internal/listener/quic.go` (`go list -deps ./internal/tls | grep -i quic-go` prints NOTHING). The `go` directive stays `go 1.23.0`.
- **Deferred maintenance (carried; ride until the relevant row reopens the file):** the phase-57 statssink notes F-1/F-2/F-3 (fold into the next `udp.go`/statssink-driver-touching row — phases 58/59/60.x/61.1 touch NONE); the phase-60.2 final-review MINOR roll-up (T2/T5/T6/T7 — all cosmetic/coverage, DEFERRED; fold opportunistically when the relevant SDS file next reopens — 61.1 touches none).

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-61.1 IMPL is next; PLAN-61.1 + PROGRESS-61.1 are banked; row 61 STAYS in-progress).
2. `docs/envoy-go/phases/61-http3-downstream-listener/PLAN-61.1.md` — the 9-task TDD spine (the authoritative execution doc; RE-DERIVE every `file:line` against the tip at IMPL start, `feedback_brief_citations_not_evidence`).
3. `docs/envoy-go/phases/61-http3-downstream-listener/PROGRESS-61.1.md` — the baselines + design pins + the six-gate + the anticipated exit counts.
4. `docs/envoy-go/phases/61-http3-downstream-listener/SPEC.md` — §3.0 (the split), §2.4/§4.1 (the `quic-go v0.54.1` module decision), §3.2 (the listen seam), §3.4 (mandatory TLS), §5 (the wire shape), §6 (the reject roster), §11 (the LIVE-probe pins), §13 (ADR-0279 §Context).
5. `docs/envoy-go/DECISIONS.md` — the tail ADR-0280 + ADR-0044/0045/0080/0106 (the process anchors); ADR-0279 is RESERVED (its body lands at THIS IMPL).
6. `docs/envoy-go/ROADMAP.md` — row 61 `in-progress` (the HTTP/3-family opener); the HTTP/3-family row + the LIVE HTTP/3 deferred sentence.
7. `docs/envoy-go/STATE.md` — the active-phase header (phase 61.1 PLAN done; NEXT = the phase-61.1 IMPL).
8. The seam source (RE-DERIVE `file:line` against the tip): `internal/listener/manager.go` (the TCP listen path + `parseChainSpec` transport_protocol switch + `buildListenerRuntimeWithCtx` + `Start`/`Stop`/`Listeners`) · `internal/tls/config.go` (`NewDownstreamConfig` + `commonTLSContextToConfig` + the ALPN `NextProtos` seam) · `internal/filter/hcm/config.go:240` (the `codec_type HTTP3` reject — 61.2 context) · `go.mod` (the `go 1.23.0` directive; the FIRST module lands at THIS IMPL).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the `reference_*` traps (esp. `feedback_brief_citations_not_evidence`, `feedback_pertask_gofmt_lint`, `reference_next_prompt_tracked_despite_gitignore`, `reference_roadmap_split_phase_row_done`, `reference_sentinel_deferred_sentence_live_vs_historical`, `reference_fatalf_makes_assertions_unreachable`).
