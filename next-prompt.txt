=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-VERIFIED 2026-07-24 at the phase-73 SPEC close — TWICE: once in the stage worktree, and AGAIN on LANDED master AFTER the squash-push, with byte-identical output (the phase-72/73 precedent — a stage worktree can disagree with what actually landed, so the post-push re-run is the one that counts). It does NOT fire. `stop` was NOT created.** Actual output:
- **(1) prints `NOT DONE: row 73`** — row 73 (`tracing-custom-tags-metadata-cluster`) is `in-progress`, registered at the BRAINSTORM commit per the ROADMAP §Schema invariant. (Check (1) going silent is the NORMAL post-IMPL state and is never on its own a termination condition; all THREE checks must be silent.)
- **(2) prints THREE live "candidates:" sentences** — HTTP/3, xDS, Observability. **Cite the command, never the adjective** (`reference_sentinel_deferred_sentence_live_vs_historical`): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` => **3**. NO sentence was narrowed at this SPEC (the phase-57 precedent), and that was proven MECHANICALLY, not asserted: `git diff master..HEAD -- docs/envoy-go/ROADMAP.md` is EMPTY.
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** => THREE families never opened. (The `WASM` line remains a ROADMAP **bookkeeping artifact** — `internal/wasm/` is a substantially-built wazero host and rows 25/25.1/25.2/25.3 are all `done` — deliberately LEFT AS-IS, since the stage-close mechanics depend on this output.)

Checks (1)+(2)+(3) print => the sentinel does NOT fire. Abundant work remains.

==============================================


## STATUS AS OF 2026-07-24: THE PHASE-73 SPEC IS COMPLETE AND LANDED. ROW 73 (`tracing-custom-tags-metadata-cluster`) IS `in-progress`. THIS SESSION PERFORMS THE PHASE-73 PLAN.

**What the phase-73 SPEC landed** (`git log --grep 'phase 73'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): `SPEC.md` + the ADR-0295 §Context append. Docs-only, ZERO production `.go`, ROADMAP UNTOUCHED. SIX agents ran the stage — three read-only (anchor re-derivation · a BY-EXECUTION costing agent that BUILT seam A end-to-end and priced seam B and the hybrid by execution · a repo-wide stale-comment and governance-anchor sweep) and three live-probe fleets running **21 EXECUTED arms** against `envoyproxy/envoy:contrib-v1.37.2` — then TWO independent adversarial verifiers against the committed draft, **both of whom found real defects (ZERO SEVERE, TWELVE MODERATE, ELEVEN MINOR, all folded; SPEC §16)**.

### The four things the PLAN must carry forward, in order of how badly getting them wrong would hurt

1. **⚠️ D-CTMC-SEAM IS RESOLVED — SEAM A (endpoint-carried) — AND THE PROBE INVERTED THE BRAINSTORM'S ANTICIPATION. Do not re-open it; do re-derive its anchors.** The BRAINSTORM expected the reference to resolve a CLUSTER tag at ROUTE-SELECTION time, which would have favoured the route-carried seam on fidelity. **It does not.** The reference gates a CLUSTER tag on the **PICKED UPSTREAM HOST**, exactly as it gates a HOST tag — cluster SELECTED but no host picked falls to `default_value` **while `upstream_cluster` sits on the same span**; host PICKED but connect REFUSED **RESOLVES**. So seam A is **the FAITHFUL seam, not the cheap compromise**. ⚠️ **But read SPEC §3.3's scope note before quoting the headline:** of the three no-pick arms only the **circuit-breaker** one DISCRIMINATES the pick-gate — the two zero-host arms share an empty-host-set confound AND are BOOT-REJECTED by envoy-go, so they are **N/A, not parity**, and the "seam B manufactures THREE divergences" framing was over-claimed 3× and is corrected to **ONE**. That over-claim had already landed in the ADR and was corrected there too.
2. **⚠️ THE POPULATE-LOOP PLACEMENT IS LOAD-BEARING AND A NAIVE READING SILENTLY BREAKS THREE LB SHAPES.** Seam A populates `Endpoint.clusterFilterMetadata` with a loop in `buildCluster` AFTER the `extractEndpoints` call — **NOT** via a third `extractEndpoints` parameter, which would drag 10 `manager_test.go` callers and a `structpb` import. **The loop must sit IMMEDIATELY after the `err != nil` guard at `manager.go:433-436` and BEFORE ANY LB CONSTRUCTION.** `buildCluster` hands the slice to `buildLeafLB` (`:472`), `newSubsetLB` (`:481`, which VALUE-COPIES each `ep`), `newLocalityWeightedLB` (`:516`) and `newPriorityLB` (`:550`) — a loop placed after `:472` leaves the subset / locality-weighted / priority LBs carrying a **ZEROED field** while the round-robin fast path stays green. No landed test catches that, and the pool-HIT provenance test would NOT catch it either. **T1 owes an explicit subset-LB assertion.**
3. **⚠️ THE DEPARTURE ROSTER IS NOT WHAT EVERY PRIOR DOCUMENT SAID, AND THE FUZZ REPOINT TARGET THE BRAINSTORM PROPOSED IS THE WRONG ONE.** After this row, **TWO reject ARMS remain DEPARTURES across THREE config spellings** — `config.go:233`'s fully-unset `kind` (two spellings) and a **NEWLY DISCOVERED, never-recorded** departure: an **entirely-absent `metadata_key`**, which the reference ACCEPTS and envoy-go rejects `empty namespace`. It was invisible because envoy-go emits ONE identical string across a PARITY case (present-but-empty) and a DEPARTURE case (omitted). Consequently: the `meta_bad` fuzz seed carries **no `MetadataKey` at all**, so post-lift it keeps rejecting **SILENTLY** on `empty namespace` with nothing going red — and the BRAINSTORM's proposed nil-`kind` repoint target is **itself a DEPARTURE arm**. **The correct PARITY target is `Kind: &metadatav3.MetadataKind{}`** (present, oneof unset), arm-DISCRIMINATED on both sides.
4. **⚠️ THREE STALE CLAIMS IN THE LANDED ROADMAP ROW-73 CELL — DO NOT PROPAGATE ANY OF THEM.** (a) its narrow-hazard sentence (*"a narrow that REMOVES the clause… since `CLUSTER` is its last item"*) — **REFUTED**: `CLUSTER` is last inside the *parenthetical*, but the `metadata` item is the FIRST of a four-member slash-list, so the narrow SHORTENS the sentence, leaves **four** candidates, and **check (2) STAYS 3**; SPEC §12 pins the exact `old_string`/`new_string`, verified `count == 1` and verified by APPLYING it in a scratch copy. (b) its *"the ONLY remaining reject arms are the two unset-kind ones"* clause — refuted by (3). (c) its `0117` description as a single-cluster *"static `clusters[].metadata` hit arm + a `default_value` arm"* — superseded by the TWO-CLUSTER decision. All three are corrected at the **IMPL**, not the PLAN.

**Everything else the PLAN must decompose is in SPEC §10** — the **NINE-task** single-flat-row TDD spine (T1-T9), a near-verbatim phase-72 clone. Key pins: `kindMetadataCluster` APPENDED at **iota == 6** (EXECUTION-proven, the red printing `Kind:6`), never inserted, and the const block is **NOT a pure-append diff** (gofmt re-aligns `config.go:58-61`; measured `+21/−5`) · the pre-known red is **EXACTLY TWO** `/cluster` subtests whose TABLE rows are `config_test.go:639`/`:764`, **byte-identical to each other** so an `old_string` on either alone is non-unique, while the FAILURE prints at `:648`/`:773` · **49** real `resolve_test.go` callers (raw grep **50**; `:107` is a `t.Errorf` literal, `:106` is its call — do NOT adopt 50) · the FULL `MetaPath`, never `[1:]` · **the T4 envelope gate is TWO-PART and the parts are not interchangeable** — sha256 byte-identity on **SEVEN** production files (`connection.go`, `h2dispatch.go`, `h3dispatch.go`, `chain.go`, `subset.go`, `actions.go`, `route.go`) and **CALLER-SET invariance (13 + 21 = 34), NOT a hash**, on `accesslog_emit_test.go`/`span_emit_test.go`, which T4 itself ADDS tests to (a draft asserted a sha256 gate there and it is unexecutable by construction) · fixture `0117` carries **TWO clusters behind two path-prefix routes onto the SAME single backend** (deterministic, zero LB spread, `BackendCount` 1) so it DISCRIMINATES *selected* from *only* — the limitation `0116` had to confess — with **STRING values only**, an arbitrary non-`envoy.lb` namespace, and FOUR breaks including the selection break · BEHAVIOR_CONTRACT **B1 and B2 edit the SAME physical line (`:688`) and must be applied as ONE `old_string`**, and B1's sentence has a false first half AND a partly-false second half.

**Live hazards to carry:** the PRE-EXISTING `internal/cluster` `-race` flake (`TestOutlierDetector_ConcurrentEjectExactlyOnce`, `outlier_test.go:766` — isolate-re-run, do NOT re-classify) · the full-suite startup flake · the SDS init-fetch dial-budget flake · and **TWO newly-observed, UNINDEXED load flakes** in byte-identical packages, isolate-green and NOT root-caused: `internal/httpclient TestOptions_ZeroValue_NoOpDefaults` and `internal/filter/hcm/h2 TestServerConn_TinyWindowDelivery`.

**Counts at exit (re-run MECHANICALLY in the worktree; docs-only, so all UNCHANGED apart from the ADR tail):** fixtures **118** (tail `0116-tracing-custom-tags-metadata-host`; `0117` and reference port `10117` both FREE) / fuzzers **55** / stat surface **1201** / BackendKind **38** / go.mod modules **2** (lineage figure; the single `go.mod` requires **67**) / DECISIONS tail **ADR-0295** PROPOSED (next-free **ADR-0296**).

## What THIS session does — the phase-73 PLAN

One stage per session. The PLAN **RE-DERIVES the SPEC rather than executing it** — a SPEC cite is not evidence (`feedback_brief_citations_not_evidence`), anchors shift (`feedback_parallel_stream_mints_fresh_drift`: re-derive rosters at the PLAN tip, since a post-SPEC commit can re-mint retired drift), and quoting is not executing (`reference_quoting_is_not_executing`).

**The owed roster:**
1. **Decompose SPEC §10's T1-T9 into a TDD spine** — each task red-first → green → break-verified, each with its own pre-known red set, its breaks, and its exit gate. The phase-72 `PLAN.md` is the format template.
2. **RE-DERIVE every SPEC §11 edit site at the PLAN tip**, including the four collision greps (`kindMetadataCluster` / `clusterMetaLookup` / `ClusterMetaLookup` / `clusterFilterMetadata`, all 0 at the SPEC tip — re-run anyway, `reference_spec_drafted_identifier_collision_check`), the 18-emit-site roster with its 6-PRE-Decide / 12-span-capable partition, and the `/cluster` and `resolve_test.go` counts by EXPLICIT LINE GATE.
3. **Pin every break so it COMPILES.** A break whose instructions do not compile proves nothing (`reference_plan_break_instructions_dont_compile`); a break that fires the wrong assertion proves nothing either (`reference_deliberate_break_wrong_assertion`); run breaks with `-count=1` AFTER committing (`reference_break_protocol_commit_first`, `reference_differential_break_protocol_count1`). T1's guard break must assert on the ACCESSOR'S OWN RETURN or it is VACUOUS, and a method value on the ZERO `Endpoint` is NON-nil (`reference_endpoint_metalookup_host_metadata_seam` — but note that memory's own "a CLUSTER row RE-PAYS the thread" claim is REFUTED by this row; SPEC §13's memory-write obligation covers it).
4. **State T3's entry condition honestly:** after T3 `internal/filter/hcm` does NOT compile (three `not enough arguments in call to tracing.ResolveCustomTags`). That is the **`ResolveCustomTags` arity** changing exactly as in 70/71/72 — do NOT conflate it with the EMIT-signature arity, which is genuinely unchanged and is this row's cost claim. Phase 72's V1 caught exactly that conflation in its PLAN.
5. **List the PLAN's OWN stage-close obligations** — phase 72's V2 found a PLAN that listed none.

**Stage-close mechanics (the PLAN):** row 73 STAYS `in-progress` (it flips `done` only at the phase-73 IMPL six-gate, ADR-0106, the SOLE leg); **NO deferred sentence narrowed at ANY pre-IMPL stage** (phase-57 precedent); **DECISIONS UNTOUCHED** (ADR-0295 stays PROPOSED — verify `grep -c '^### Decision'` inside the block is **0** so the IMPL's APPEND is sound); ROADMAP UNTOUCHED; STATE §Current rolled **IN PLACE** (lifecycle 2 -> 3; lineage re-capped at five **with its PREAMBLE updated** — the ADR-0288 rule). ⚠️ **The ADR-0288 singleton check returns 2, not 1**, for each of `next-skill:` / `lifecycle-state:` / `next-free ADR:` — the second hit is `STATE.md:7`, the RULE STATEMENT itself. Verify with `grep -n`, and never "fix" the count to 1. next-prompt.txt rolled to the phase-73 IMPL; every count re-run MECHANICALLY in the worktree, never copied.

Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). A PLAN is **docs-only, ZERO production `.go`** (`PLAN.md` + `PROGRESS.md`).

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. At the phase-72 close that put a stray commit on `master` in the MAIN checkout AND made a "final gates" run validate master's pre-row tree — GREEN while proving nothing. **Use `git -C <abs-worktree-path>` for every git command, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline) + §Project counts + the §Recent lineage (five bullets: phase-73 SPEC + phase-73 BRAINSTORM + phase-72 IMPL/PLAN/SPEC).
2. `docs/envoy-go/phases/73-tracing-custom-tags-metadata-cluster/SPEC.md` — **§3.3 (D-CTMC-SEAM, RESOLVED — read its scope note, not just its headline) and §10 (the nine-task spine) FIRST**, then §1.1 (the twelve-row drift ledger — what the BRAINSTORM got wrong), §3.4 (the retention shape + the load-bearing loop placement), §3.6 (the departure/parity roster), §7 (the fuzz repoint), §8 (the two-cluster fixture), §9 (the contract edit map + the B1/B2 collision), §11 (the edit roster + the two-part envelope gate), §12 (the pinned ROADMAP narrow), §13 (deferred + the memory writes), §16 (**the adversarial ledger — read this before citing anything as settled**).
3. `docs/envoy-go/phases/73-tracing-custom-tags-metadata-cluster/BRAINSTORM.md` — §2.4 and §11.4, **read AGAINST the SPEC's §1.1 ledger**: several of its load-bearing claims are refuted, and the SPEC records which.
4. `docs/envoy-go/phases/72-tracing-custom-tags-metadata-host/` — `PLAN.md` (the format template), `PROGRESS.md` (the per-task adversarial record).
5. `docs/envoy-go/DECISIONS.md` tail — **ADR-0295** (PROPOSED, §Context only) + **ADR-0294** (the HOST precedent, COMPLETE) + ADR-0044 · ADR-0045 · ADR-0080 · ADR-0106 · ADR-0288.
6. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — `:688` (the `custom_tags` paragraph, B1+B2's shared line), `:690` (the phase-72 B2 departure clause this row EXTENDS), `:692`, `:745`.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — trust MEMORY.md's index. Load-bearing for THIS row: `reference_endpoint_metalookup_host_metadata_seam` (the accessor template — **and note the SPEC refuted its "CLUSTER re-pays the thread" claim**), `reference_host_tag_resolves_at_pick_time` (the B2 scope this row extends), `reference_envoy_lb_namespace_not_privileged`, `reference_route_metadata_resolve_full_metapath`, `reference_structpb_tag_cross_side_string_only`, `reference_probe_must_discriminate`, `reference_a_drift_correction_is_itself_a_claim`, `reference_lifted_reject_hidden_enforcement` (ask what ELSE the reject silently enforced — the `meta_bad` seed is exactly that class), `reference_cluster_race_outlier_flake`, and **`reference_bash_cwd_reset_commits_to_main`**.
