=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **PHASE 86 (`validate-sds-nil-provider`) IS OPEN AT THE BRAINSTORM (2026-08-10): ROW 86 IS `in-progress` AT `ROADMAP.md:148` AND `want` IS 118.** ⇒ **THIS SESSION PERFORMS THE PHASE-86 SPEC** (lifecycle-state 1 -> 2). `STATE.md`'s `next-skill:` reads `SPEC for phase 86` — that IS your instruction. The SPEC's obligations are enumerated in §What-the-SPEC-owes below and in BRAINSTORM §4/§10.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=118 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **`want` IS 118 AT THIS TIP** (row 86 `in-progress`, all 117 others `done`). ⚠️ **AT THIS TIP CHECK (1) PRINTS EXACTLY `NOT DONE: row 86`** — the expected line while the phase is open; ANY OTHER row id appearing is a FINDING. ⚠️ **THE SENTINEL DOES NOT FIRE AT THIS TIP:** check (1) prints, check (2) prints SIX (at `:196 :202 :208 :218 :224 :232` — re-derive, the anchors drift with every edit), and the condition is a CONJUNCTION. Check (3) is SILENT. A SPEC is docs-only and leaves `ROADMAP.md` BYTE-UNTOUCHED (row 86 stays `in-progress`; `want` stays 118) — verify by empty diff at your close.

⚠️ **THE DOCTORING NCs REMAIN MANDATORY EVERY SESSION.** Both:
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md at want=118: expect "NOT DONE: row 62" AND "NOT DONE: row 86"
```
```sh
# the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```

⚠️ **AND WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -oiE '-family row'` fails with `grep: amily row: No such file or directory` and the surrounding arithmetic prints `base=0 now=0 delta=0`, **which reads exactly like "no change".** Observed live at FOUR phase-84 stages. **Always pass `--` before the pattern**, and remember the counts are form-dependent: **95 occurrences vs 67 LINES** at this tip (`grep -c` counts LINES). **A gate that reads zero on BOTH sides is not evidence of invariance.**

**SENTINEL RE-RUN MECHANICALLY AT THE PHASE-86 BRAINSTORM CLOSE (2026-08-10, base `766d98ad`), BEFORE AND AFTER THE ROW-86 REGISTRATION. IT DOES NOT FIRE; `stop` was NOT created** (`ls stop` => `No such file or directory`, repo root AND stage worktree, before and after). ACTUAL output, recorded not predicted: input measured **235 lines / 117 data rows** first; BEFORE at `want=117`: **(1) SILENT** — every chartered row `done`, the second-ever silent reading, denominator assertion silent; **(2) SIX — `:195 :201 :207 :217 :223 :231`**; **(3) SILENT**. AFTER at `want=118`: **(1) `NOT DONE: row 86`** — the single expected line; **(2) SIX — anchors shifted by the row insert to `:196 :202 :208 :218 :224 :232`** (⚠️ do NOT "fix" it and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases; the candidates sentences are a WINDOW onto a larger deferred backlog, not an inventory of it); **(3) SILENT**. The condition is a CONJUNCTION ⇒ the sentinel does NOT fire.
- **ALL FOUR NCs FIRED ON BOTH SIDES OF THE REGISTRATION:** row-62 doctoring => before `NOT DONE: row 62` ALONE, after `NOT DONE: row 62` AND `NOT DONE: row 86` — with `NC LANDED? [ in-progress ]` inspected first; `want` off-by-one => `GATE FAIL: examined 117 data rows, expected 116` before / `examined 118 data rows, expected 117` after; check-(3) doctoring (residual `gRPC-family row` confirmed **2 -> 0** on the doctored copy first) => `NEVER OPENED: gRPC` while `WASM` correctly stayed silent; check-(2) one-arm counts **5** (long arm alone) / **1** (short arm alone), union **6** (a one-arm strip is NOT an NC for the union).
- ⚠️ **Re-run all three checks and both doctoring NCs yourself and record what YOU see.**

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (ugrep under the hood). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Reproduced at NINETEEN separate tips.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **THE METHOD BINDS; carry no standing discrepancy figure and no standing absolute.** ⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS; print FILE NAMES, never a count.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files; use `-v --no-index`, and `git status` is the discriminating check). ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **`grep -c` ON ZERO MATCHES IS TOOL-SPECIFIC** (only `git grep -c` prints nothing; all exit 1 — capture the value, never chain on the exit code). ⚠️ **`grep -c` COUNTS *LINES*, NOT *OCCURRENCES*.** ⚠️ **`\t` IN GNU ERE IS A LITERAL `t`** — use `-P`. ⚠️ **A `git grep` PATHSPEC ENDING IN `/` READS A FAIL-UNSAFE ZERO** (shape 33) — use the shell-glob form. ⚠️ **AN UNANCHORED PANIC-GREP FALSE-FIRES 14 TIMES ON A FULLY-GREEN DIFFERENTIAL LOG** — **the canonical gate is ANCHORED: `^panic:|DATA RACE|SIGSEGV`. A gate that fires on a green log is worse than no gate.**

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`** with FOUR blind spots (an em-dash, a DOT in `http-1.1`, two LETTER-suffixed ids). ⚠️ **EXECUTED on scratch copies at the phase-77 PLAN: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** ⚠️ **DO NOT restore the regex form.** At this tip check (1) prints exactly `NOT DONE: row 86` — **any OTHER row id appearing there is a finding** — and the row-62 doctoring NC is what proves it still LOOKS.

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

⚠️ **Run the union form, never one half of it.** ⚠️ **A ONE-ARM STRIP IS NOT AN NC** (6 -> 5 short / 6 -> 1 long; never 6 -> 0). ⚠️ **The documented trap's stated MECHANISM is refuted while the trap is real** — the inflation comes from ROADMAP table rows carrying BRAINSTORM prose. **Cite the command; do not re-inherit the explanation.** ⚠️ **The line numbers drift with edits — re-derive them; the current six are `:196 :202 :208 :218 :224 :232`.** ⚠️ **These six paragraphs remain the final sentinel blocker, and any edit near them is a sentinel-affecting edit.** All six are standalone family PROSE paragraphs (not rows), naming ~42 distinct candidates; every "retire the paragraphs" shape is doctrinally foreclosed (`reference_sentinel_matcher_string_self_clears`). ⚠️ **Row 86's pick came from OUTSIDE the windows** (BRAINSTORM-83 §5.6 provenance, the phase-85 shape) — **nothing narrows in any window sentence at row-86-done**, and the Op-tooling window's "an RTDS/SDS validate companion" is ADJACENT but DISTINCT (feature vs repair; BRAINSTORM-86 §2) — its sentence stays untouched.


==============================================


## STATUS AS OF 2026-08-10: PHASE 86 OPEN AT THE BRAINSTORM (row 86 `in-progress` at `ROADMAP.md:148`, base `766d98ad`). **THIS SESSION PERFORMS THE PHASE-86 SPEC.**

**What the 86 BRAINSTORM landed (2026-08-10, base `766d98ad`, branch `phase-86-brainstorm`, ONE squashed commit).** Docs-only: `BRAINSTORM.md` + `PROGRESS.md` in `docs/envoy-go/phases/86-validate-sds-nil-provider/`, row 86 registered, `want` 117 -> 118, `STATE.md` rolled in place, PLAN-84.2 evicted to `STATE_HISTORY.md` (474 -> 476, strictly append-only, numstat `2 0`, cmp-prefix verified, robust absence guard 0/0 with controls 8/8, 6/6, 0/0 — both non-invented controls DRIFTED UP again). `DECISIONS.md`/`BEHAVIOR_CONTRACT.md` BYTE-UNTOUCHED. ⚠️ **NAMED DEPARTURE: no investigation agents** — probes run INLINE by the controller (binary built at the tip into scratch; four `--mode validate` executions; code reads; zero repo-tree edits by probes).

**THE HEADLINE: the `--mode validate` SDS divergence is THREE-ARMED, not one-armed — ALL THREE ARMS REPRODUCED BY EXECUTION at `766d98ad`, plus a positive control:**

| arm | shape | `--mode validate` (MEASURED) | reject site |
|---|---|---|---|
| A | `tls_certificate_sds_secret_configs` (fixture `0103`'s shape) | exit 1 — `SDS-delivered certificate requires a live SDS provider (unavailable in this mode)` | `internal/tls/config.go:387-389` |
| B | `validation_context_sds_secret_config` (`0108`'s shape) | exit 1 — `SDS-bound validation_context_sds_secret_config is not supported in phase 03` | `config.go:436-438` |
| C | `combined_validation_context` (`0109`'s shape) | exit 1 — `combined_validation_context is not supported in phase 03` | `config.go:453-455` |
| control | static file-based TLS | exit 0 — `configuration OK` | rejects are NOT vacuous |

All three rejects key on `provider == nil`; `validate/validate.go:48-49` threads a LITERAL nil into `boot.Construct`; the ordinary boot path HONORS all three shapes (phases 60.2/65/66 — `main.go:156` builds a live provider via `boot.NewSDSProvider`'s pre-scan). User-facing on the package's own Gateway-API charter (phase 51, ADR-0268).

### ⚠️ WHAT THE BRAINSTORM ESTABLISHED BEYOND THE CARRIED RECORD (refutation ledger, BRAINSTORM §5)

- **The carried one-arm framing (`config.go:436` alone) is REFUTED** — three arms; every prior cost figure inherited the under-count.
- **A pure reject-lift is wrong in BOTH directions:** UNDER-rejects without boot-parity (`boot.NewSDSProvider`'s pre-scan enforces the node arm-7 requirement, the one-SDS-secret cap, and the `xds.ParseSDSConfig` arms — validate runs NONE of them; the row's contract is **validate accepts iff boot accepts, modulo fetch/dial**); OVER-lifts if keyed on `provider == nil` (OTHER consumers must keep rejecting BYTE-IDENTICAL: QUIC `manager.go:567`, exported test-only constructors `listener.NewManager`/`NewManagerWithBaseDir` — enumerated at the `config.go:407-435`/`:447-452` comments, re-verified by call-site grep). `reference_lifted_reject_hidden_enforcement` engaged PROSPECTIVELY.
- **Both fetch sites must be skipped:** the blocking `FetchInitialCertificate` at `config.go:390` (arm A) and `NewDownstreamConfig`'s `require_client_certificate` fetch-and-install block (arms B/C). `ParseSDSConfig` already runs BEFORE the arm-A reject (`config.go:383-386`). The phase-60.2 no-dial decision SURVIVES — the reject was its over-broad implementation.
- **Coverage hole MEASURED ZERO:** no SDS test in `validate/validate_test.go` (432 lines) or `cmd/envoy-go/main_test.go` (1502 lines). The three rejects are the row's RED anchors.
- **Cost floor re-derived ~50-90 net production + ~150-300 test `.go`** (carried ~30-40/60-120 REFUTED as central; the EIGHTH consecutive `reference_measured_prototype_is_a_lower_bound` firing — cause always under-ENUMERATION; likeliest unenumerated lines: the pre-scan-reuse refactor in `internal/boot`, and the combined-arm fetch path).
- **CARRIED, NOT re-verified (the SPEC owes them):** the reference-side "validates OK" (BRAINSTORM-83 §5.6 — needs the reference container); the boot-side live-dial discriminator run (BRAINSTORM-84 §2.2 — re-confirmed by code read only); `go list -deps ./validate` carrying `internal/xds` (zero-new-package-edges claim).

### What the SPEC owes (BRAINSTORM §4's seven questions + §10)

1. **Fix mechanism (Q1)** — enumerate + MEASURE: (a) sentinel validate-only `SecretProvider` (zero signature changes); (b) threaded mode flag (`Construct -> NewManagerWithBaseDirAndAllowH2C -> buildListenerRuntimeWithCtx -> NewDownstreamConfig -> commonTLSContextToConfig`); (c) no-fetch placeholder provider (REJECT-leaning: fabricates state). Hard invariant: the discriminator is NOT `provider == nil`; QUIC/test-constructor nil-rejects stay BYTE-IDENTICAL with guard-preservation tests atomic with the lift.
2. **Boot-parity surface (Q2) BY EXECUTION** — boot each negative shape (missing node, >1 SDS secret, ill-shaped/unknown SDS cluster) and record what boot rejects at BUILD time vs DIAL time; only build-time rejects belong to validate.
3. **Reference re-verification (Q3)** — pinned `contrib-v1.37.2` `--mode validate` on all three arm shapes + the negative shapes; record per-side (pins style: observations, never expectations).
4. **Contract sweep (Q4)** — every `BEHAVIOR_CONTRACT.md`/ADR statement about validate-mode behavior (ADR-0268, ADR-0280, phase-60.2 Task 5); which the new ADR reconciles vs records.
5. **Test placement (Q5)** + which existing tests already pin the QUIC nil-reject. **Error-message stability (Q6)** — the ADR-0080 byte-identical reject-string constraint set. **Anticipated counts (Q7)** — +0 fixtures / fuzzers / stat surface / BackendKind / go.mod, each re-checked not assumed.
6. **ADR-0308 §Context drafted STATUS `PROPOSED`** — re-arms the strict `^> \*\*STATUS: PROPOSED` guard **0 -> 1** (the live pointer the phase-86 IMPL disarms). Next-free ADR-0308 is DERIVED FROM THE TAIL (`## ADR-0307`; headings+1 COLLIDES at the ADR-0209 gap).
7. **Cost ENUMERATION by prototype** — the phase-84/85 lineage: a SPEC that estimates instead of enumerating ships a floor as a budget.

### ⚠️ COUNTS AT THIS TIP — RE-DERIVE MECHANICALLY, NEVER COPY

**HOUSE RULE: every number below is a LIVE FIGURE. Re-run it in your own worktree with its negative control observed.**

- **differential fixtures 121** — numeric tail `0119-grpc-unary-trailers`, next-free `0120`.
- **`ROADMAP.md` 236 lines / 118 data rows** (row 86 `in-progress` at `:148`, all others `done`); check-(2) anchors `:196 :202 :208 :218 :224 :232`; `-family row` **95/67**; `gRPC-family row` **2**; `Operational-tooling-family row` **3**; ARM-A well-formedness flags **{119, 131} ONLY** (escape-aware pipe-split, field count != 8; row 86 at `:148` is clean). ⚠️ A SPEC leaves ALL of these BYTE-UNTOUCHED.
- **`DECISIONS.md` 18050** lines · **306** `^## ADR-` headings · tail **`ADR-0307` COMPLETE** · `^## ADR-0308` = **0** · strict `PROPOSED` guard **0 — DISARMED; your SPEC's ADR-0308 §Context re-arms it 0 -> 1**.
- **`BEHAVIOR_CONTRACT.md` 5955** · **`STATE.md` 64** · **`STATE_HISTORY.md` 476** · **`BOOTSTRAP_PROMPT.md` 522** at the repo ROOT · phase dirs **127**, `REVIEW.md` **37** (standing departure, named not claimed).
- **h2spec: cite `95 tests, 94 passed, 1 skipped, 0 failed` ONLY from your own tip's run** (skip 6.9.2/2 invariant; the harness guards itself — 3 layers + reverse). Reference (`contrib-v1.37.2`): 95/82/1/12, RECORD-only (`CONFORMANCE_PINS.md`).
- **stat call sites 208 / 36 production files** · **fuzzers 55 / 48 files** · **BackendKind tail 38** over 39 declarations (do NOT "fix").
- **validate surface at this tip:** `validate/validate.go` **64** lines · `validate/validate_test.go` **432** (zero SDS) · `cmd/envoy-go/main_test.go` **1502** (zero SDS) · `internal/tls/config.go` **529**.
- ⚠️ **STILL CONTESTED, NO NUMBER CARRIED:** the `STATE_HISTORY.md` archive-gap total · the loose `PROPOSED` matcher's whole-file count · the stat-surface absolute (1205 vs 1207 — WIDENED; only DELTAs are asserted) · the zero-test `generic/*` suite count (13 vs 14).

### ⚠️ DOCUMENTARY DEFECTS — carried forward, still deliberately NOT fixed

**`ADR-0051` §2's false gate-scope sentence** corrected ON THE RECORD by ADR-0307 (the sentence stays as history). **`ADR-0058`** cites `routerActionH2.doH2` at a dead location (`router_h2.go:254` / `h2dispatch.go:720` are real). **`ROADMAP.md` rows at lines 119/131 malformed** (ARM-A GUARD, not a fix). **`STATE.md` §Project counts FROZEN at phase 76** (do not source; do not fix). **`harness_test.go:208` port inventory STALE.** **`body.go` nolint directives INERT** · **xDS cycle guard NOT AUTOMATED** · **`wasm/doc.go:219` two errors** · **ROADMAP cites `esalaine` FIVE times** (module is `pgdad`) · **`rbac.go:50` token `F2`** (don't grep-anchor) · **root `PROGRESS.md`** stray phase-32.1 doc.

### Candidates ledger (for the FUTURE post-86 pick; re-derive EVERY cost at that tip)

- **The CONTINUATION two-sided repair** — strongest KNOWN product defect (server discard `conn.go:255-259` + client blindness + the client-side SETTINGS gap `h2/client.go:376-407`, all named in ADR-0307). h2spec MEASURED not to gate either side. Est. 2-4x row 85 (+1046 realized) — the natural next LARGE row; charter as a SPLIT phase.
- **The stat-surface recount** (1205-vs-1207 WIDENED; rides a future +0 row) · **`ssl.connection_error`** (+444 whole-`.go` floor) · **`test/conformance/grpc/`** (9/26 reachable) · **REVIEW.md restoration** (process-not-product) · **hygiene fold-ins** (`harness_test.go:208`, xDS cycle-guard) · the **~42 candidates in the six family windows**.

### ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + BOTH DOCTORING NCs YOURSELF** — record ACTUAL output.
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** Lineage 86: B (three arms reproduced; one-arm framing and its cost refuted). The SPEC's job: measure Q1's mechanism options, Q2's boot-parity surface, Q3's reference behavior — a BRAINSTORM claim a SPEC repeats without executing is a CLAIM.
3. **LINE ANCHORS DRIFT; SYMBOL ANCHORS DID NOT — RE-VERIFY EVERY CITE AT YOUR TIP.** Key sites: `validate/validate.go:48-49` · `internal/tls/config.go:387-389 :436-438 :453-455 :390` · `manager.go:567 :573 :655` · `main.go:156` · `boot.NewSDSProvider` (`internal/boot/boot.go`).
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Still live: SDS dial-budget (TWO packages) · `internal/cluster` `-race` outlier · `internal/httpclient` zero-value · driver-receiver port race (roster 42; `0081` binds `0.0.0.0:42039`) · the two 84.2-era flakes (cleared, unclassed) · `TestServerConn_TinyWindowDelivery` (85-IMPL, cleared, unclassed) · the REFERENCE h2spec section-8 flip (reference-side only).
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only).
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`.
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL.** **A harness's exit code is not the command's.** **`gofmt -l` never exits non-zero — gate on OUTPUT.**
8. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-86-*` or stage worktree should outlive its stage close** — expect only `master` plus your own.
9. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** (a)/(b) differential 121 fixtures, full suite with `INNER_EXIT`; (c) h2spec cite only from your own run; grpc-conformance deferred in writing; proxy-wasm 10/16; (d) fuzzers 55/48; (e) `INNER_EXIT` on every differential launch AND `go test ./...`; (f) no REVIEW.md — standing departure.

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 86 (validate-sds-nil-provider)' --format='%h %s'   # 1 at this tip: the BRAINSTORM. An invented slug returns 0.
git log --grep '^phase 85 (h2spec-selector-repair)' --format='%h %s'      # 4 — a CLOSED phase, use it as a FIRING CONTROL
```
⚠️ **The count comments are LIVE FIGURES — VERIFY before relying.** Take the NEWEST line (`| head -1`).

Subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** and tear down docker **BY NAME**. ⚠️ **Band private ports outside BOTH `20000-31007` AND `11000-14999`, clear of the static fixture ports (10000-19172); `0081` binds `0.0.0.0:42039`.** Phase-85 used `46500-46999`; the 86 BRAINSTORM used `47000-47014` as TEMPLATE VALUES ONLY (validate binds nothing — never bound) — **the next session that binds real ports should band from `47100-47199`.** ⚠️ **ONE WORKTREE PER AGENT preferred; explicit-path staging if shared; never an unscoped `git restore`.** Per-task `gofmt` + `golangci-lint` on touched packages (⚠️ misspell locale US). ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch.** ⚠️ **The SPEC's reference-container run (Q3) needs docker; tag/tear down containers BY NAME (`s86-*` suggested).**

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root — observed live again at the 86 BRAINSTORM (every compound command's trailing check printed the repo root). Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — sha256 against the base blob, never by eye; work DETACHED so a failed revert cannot dirty a branch.

⚠️ **THE ARCHIVE-ABSENCE GUARD — USE THE ROBUST FORM** (bullet-anchored + whole-file fixed-string, NC'd with a REAL target re-derived FROM THE FILE first; a control's own count is a LIVE FIGURE: `phase 82 … IMPL done` read **8** at this close, up from 7; `phase 83 … PLAN done` read **6**, up from 5). **Verify STRICTLY APPEND-ONLY**: `git diff --numstat` = `N 0` AND base a byte-exact PREFIX (`cmp`). (A SPEC that evicts nothing from `STATE.md` §Recent needs none of this — the five-entry cap binds only when §Current rolls a NEW stage in, which the SPEC close does.)

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads `SPEC for phase 86` — that IS your instruction.** §Project counts SELF-CONTRADICT §Current — do not anchor on them (frozen at phase 76).
2. `docs/envoy-go/phases/86-validate-sds-nil-provider/BRAINSTORM.md` — the charter, the three reproduced arms, §4's seven questions, §5's carried-not-verified list.
3. `docs/envoy-go/ROADMAP.md` — §Schema (`:18`) and row 86 at `:148` (BYTE-UNTOUCHED by a SPEC).
4. `docs/envoy-go/DECISIONS.md` — ADR-0268 (phase 51, the validate charter), ADR-0280 (`:16708` — the SDS config seam and the validate nil mention), ADR-0307 tail (form for COMPLETE blocks; next-free ADR-0308 from the tail).
5. `validate/validate.go` + `internal/tls/config.go:355-500` + `internal/boot/boot.go` (`NewSDSProvider`) — the code the SPEC decides over.

## After the SPEC lands — the roll-forward
Row 86 STAYS `in-progress`; `want` stays 118; `ROADMAP.md` BYTE-UNTOUCHED (verify by empty diff). `DECISIONS.md` gains ONLY the ADR-0308 §Context draft (STATUS `PROPOSED`; strict guard 0 -> 1, APPEND-ONLY, tail-derived numbering, no `---`). Re-run the sentinel + NCs and record ACTUAL output (expect (1) `NOT DONE: row 86` · (2) SIX · (3) SILENT ⇒ no fire). **Roll this file to the phase-86 PLAN**, carrying: the SPEC's disposed questions and measured figures, the sentinel state you observed, the re-derived counts at your tip, the refutation ledger (extend it with every BRAINSTORM claim your SPEC moved), and the flake register verbatim.
