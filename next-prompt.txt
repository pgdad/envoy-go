=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-CHECKED 2026-07-12 (at the phase-58 SPEC, master `6628e17f` pre-commit, run MECHANICALLY) — it does NOT fire. Do NOT create `stop`.** The SPEC is docs-only and did NOT touch the ROADMAP, so the state is unchanged from the phase-58 BRAINSTORM:
- (1) prints `NOT DONE: row58` — row 58 is `in-progress` (a phase is open); clears when row 58 flips `done` at the phase-58 IMPL.
- (2) prints the Observability family's LIVE deferred-candidates sentence (OTLP-metrics + the tracing quartet + the dog_statsd explicit-`max_bytes_per_datagram: 0` parity fix — the phase-58 subject, which STAYS in the live sentence until the phase-58 IMPL rolls it OUT at row-done). EXACTLY ONE live "candidates:" match (verified). ⇒ Observability STAYS OPEN.
- (3) prints `NEVER OPENED: HTTP/3`, `gRPC`, `xDS`, `Runtime`, `WASM` ⇒ FIVE families never opened; Operational-tooling also STAYS OPEN.

**STATUS AS OF 2026-07-12: phase 58 (`stats-sink-dogstatsd-explicit-zero`) is OPEN — the SPEC is DONE (row 58 `in-progress`, lifecycle-state 1 → 2). The next stage is the phase-58 PLAN.** ALL FIVE D-DZ-* questions are DISPOSED at the SPEC (D-DZ-REJECTMSG PINNED via a live probe; FIXTURE/TESTROWS/FUZZSEED/DOCSHAPE decided) — NO empirical question remains; the PLAN is a mechanical decomposition.

**Current counts (verified at the phase-58 SPEC, master `6628e17f` pre-commit):** fixtures **103** (tail `0101-stats-sink-graphite`) · fuzzers **54** · DECISIONS tail **ADR-0275** (next-free **ADR-0276**, the row-58 anchor) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201 (+0)** · new Go packages **0** · new go.mod modules **0**. RE-VERIFY against the actual master tip at session start (`git fetch` first).

**Locating the phase-58 SPEC stage squash** (a router cannot name its own sha — identify by SUBJECT, never by position):
```sh
git log --oneline --grep='^phase 58 (stats-sink-dogstatsd-explicit-zero) SPEC' -1
```

**THE NEXT ACTION: the phase-58 PLAN (lifecycle-state 2 → 3).**

==============================================

## What THIS session does (the phase-58 PLAN)

Perform the PLAN for phase 58 (docs-only; NO production `.go` changes at a PLAN; fresh worktree per `feedback_git_worktrees`, e.g. `.worktrees/phase-58-plan`; row 58 STAYS `in-progress`; write `PLAN.md` + a `PROGRESS.md` scaffold — the phase-57 `PLAN.md`/`PROGRESS.md` are the template shape; DECISIONS tail STAYS ADR-0275 per ADR-0044).

**The PLAN's job** — decompose SPEC-58 into a ~5-7-task TDD spine (SPEC-58 §10 sketches it). Every task is mechanical (no empirical questions survive the SPEC). RE-DERIVE every `file:line` cite from source AGAIN (`feedback_brief_citations_not_evidence` — a brief's citation is not evidence; the SPEC's roster is §12 but re-verify against the actual master tip):
- **T1** baselines + count reconciliation (fixtures 103 / fuzzers 54 actual `^func Fuzz` / stat 1201).
- **T2** the reject arm in `parseDogStatsdSinkConfig` (`bootstrap.go:712`, before the append `:721`) + the three `bootstrap.go` doc-comment fixes (struct doc `:326`, func doc `:702-711`, graphite NOTE `:738-740`).
- **T3** the TEST CONVERT (the row's load-bearing edit): delete `TestDogStatsdSink_AcceptMaxBytesPerDatagramZero` (`bootstrap_test.go:2550`), add an `explicit_max_bytes_per_datagram_zero` row to `TestDogStatsdSink_Rejects` (`:2458`, `errSubs: []string{"bootstrap:", "dog_statsd max_bytes_per_datagram must be greater than 0"}`); KEEP `…Absent` (`:2528`); prove the arm LIVE with a `-count=1` deliberate break (`reference_differential_break_protocol_count1` + `reference_deliberate_break_wrong_assertion`).
- **T4** a SEED (explicit `max_bytes_per_datagram: 0`) to the EXISTING `FuzzDogStatsdSinkConfigParse` (`dogstatsd_fuzz_test.go`) — NOT a new fuzzer; count STAYS 54 (`reference_fuzzer_count_docs_drift`, re-count after).
- **T5** the four `BEHAVIOR_CONTRACT.md` edits (`:785` dog_statsd rejects [+ the OPTIONAL in-passing "three sinks"→"four" staleness fix], `:787` batching text, `:821` graphite NOTE, `:834` consumption summary).
- **T6** ADR-0276 body (§Decision/§Consequences on the SPEC §13 §Context) + STATE/ROADMAP (row 58 `done`, the deferred-list roll [dog_statsd OUT], then re-run the sentinel check-(2) grep — EXACTLY ONE live "candidates:" match, `reference_sentinel_deferred_sentence_live_vs_historical`) + the router roll to the phase-58 IMPL.

The PLAN pins the exact task boundaries + the six-gate. Re-check the ADR-0045 split gate (a formality — a single flat row of ~5-7 tasks, margin large; escape-valve UNCONSUMED). Then ROLL THIS FILE FORWARD to the phase-58 IMPL (fold the roll into the stage commit) and land it (commit + push per `feedback_push_to_origin`).

## Honor throughout (the discipline, unchanged)
- **Lifecycle = one stage per session.** This session: the phase-58 PLAN ONLY. Next session: the phase-58 IMPL (subagent-driven per `feedback_execution_style`, `superpowers:subagent-driven-development`).
- **Process anchors:** ADR-0044 (ADR §Context at SPEC — DONE; §Decision/§Consequences at IMPL) · ADR-0045 (split gate ~15 tasks — this row ~5-7) · ADR-0080 (distinct per-arm substrings — `dog_statsd …` vs graphite's `graphite_statsd …`) · ADR-0106 (row 58 flips `done` at its IMPL six-gate). Next-free ADR is **ADR-0276** (the row-58 anchor).
- **The IMPL is subagent-driven** (`feedback_execution_style`, `feedback_subagent_autocommit_claudemd`, `feedback_subagents_no_push`): subagents commit LOCALLY only; the controller squashes at stage-close and pushes. Watch the worktree hygiene traps (`feedback_subagent_worktree_detach`, `feedback_subagent_worktree_path_targeting`) even though this row is tiny.
- **The NON-additive test edit is the row's load-bearing risk:** `TestDogStatsdSink_AcceptMaxBytesPerDatagramZero` asserts the OLD (buggy) accept behavior; CONVERT it, do not merely supplement. Prove the new reject arm LIVE with `-count=1`.
- **`next-prompt.txt` IS TRACKED** — edit it inside the stage worktree, fold into the stage commit; locate commits by SUBJECT (`git log --grep`), never by position (`reference_next_prompt_tracked_despite_gitignore`).
- **After the phase-58 IMPL ROADMAP deferred-list edit:** re-run the sentinel check-(2) grep — EXACTLY ONE live match, correct content. When row 58 lands `done`, the family sentence rolls the dog_statsd candidate OUT (OTLP + the tracing quartet remain ⇒ check (2) still prints ⇒ the sentinel still does not fire).
- **Deferred maintenance (fold into the next `udp.go`/statssink-driver-touching row — NOT this one, which touches neither; from the phase-57 final review):** F-1 the 0101 driver's counter tag snapshot records the EXPECTED map; F-2 `udp.go` `udpWriter` doc comment names two sinks (three embed it now); F-3 BEHAVIOR_CONTRACT 0101 note says "`differential.HostGatewayIP` helper" where the driver uses its LOCAL duplicate. Recorded in BRAINSTORM §11.

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-58 PLAN is next; subject: dog_statsd explicit-zero parity).
2. `docs/envoy-go/phases/58-stats-sink-dogstatsd-explicit-zero/SPEC.md` — the pinned SPEC (§10 the task sketch, §11 the live-probe pins, §12 the edit-site roster, §13 the ADR-0276 §Context draft).
3. `docs/envoy-go/phases/58-stats-sink-dogstatsd-explicit-zero/BRAINSTORM.md` — the charter (§2.4 the non-additive test, §6 the edit-site roster).
4. `docs/envoy-go/phases/57-stats-sink-graphite/{PLAN.md,PROGRESS.md}` — the template shape (a TDD spine + a progress scaffold).
5. `internal/bootstrap/bootstrap.go` — `parseDogStatsdSinkConfig` (`:712`, the unchecked `GetValue()` `:724`) vs the graphite arm template (`:751`); the doc-comment sites (`:326`, `:702-711`, `:738-740`).
6. `internal/bootstrap/bootstrap_test.go` — `TestDogStatsdSink_Rejects` (`:2458`), `…AcceptMaxBytesPerDatagramZero` (`:2550`, convert), `…Absent` (`:2528`, keep), the graphite reject row (`:2890-2899`, template).
7. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the four edit sites (`:785`, `:787`, `:821`, `:834`).
8. `docs/envoy-go/{ROADMAP.md,STATE.md}` — the Observability family section + row 58; the active-phase header (SPEC done; PLAN next).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the `reference_*` traps.
