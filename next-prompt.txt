START a fresh cold-start session for envoy-go — phase 26.1 (`network-filter-chain-framework-and-echo`) PLAN is DONE; execute the 26.1 IMPL.

This is a per-sub-phase IMPL cold-start. **Phase 26.1 PLAN is DONE + squash-merged to master + pushed to origin** (PLAN squash + stage-close follow-up). The PLAN (`docs/envoy-go/phases/26.1-network-filter-chain-framework-and-echo/PLAN.md`, ~1400 lines, **17 bite-sized TDD tasks**) decomposes the 26.1 SPEC §10 task spine, was plan-document-reviewer APPROVED (2 iterations), checked the ADR-0045 split-gate (NO split: 17 tasks / ~850-1020 net-new LoC), and RESOLVED the PLAN-time D-questions (D-P26.1-1 drainable `*Buffer`; D-P26.1-2 inline DataSource 4-arm modeled on `internal/tls/datasource.go` + `network.FactoryCtx.BaseDir` refinement). STATE.md points at `phase 26.1 PLAN done; awaiting 26.1 IMPL`, next-skill `superpowers:subagent-driven-development` (per `feedback_execution_style`), next-free `ADR-0215` (DECISIONS.md tail at ADR-0214 — the ADR-0213+0214 §Decision/§Consequences bodies land at 26.1 IMPL Task 17, NOT a new number).

## What this session must do: execute the phase-26.1 IMPL

Per SKILL_ROUTING state 3 (26.1 PLAN.md exists, implementation incomplete), the next-skill is **`superpowers:subagent-driven-development`** (per the `feedback_execution_style` memory — fresh subagent per task, two-stage review between tasks; subagents commit LOCAL-ONLY per `feedback_subagents_no_push`, the controller pushes at stage-close). Execute the PLAN's 17 tasks in order, TDD-first per `superpowers:test-driven-development` on every task.

1. **IMPL Task 1 is a HARD FIRST-ACTION GATE** (PLAN Task 1 / SPEC §11.1 D-S1): re-pin the four baselines via the DETERMINISTIC git-tracked commands (the repo root has nested `.worktrees/` checkouts that inflate a naive `find .` — use `git ls-files '*fuzz_test.go' | xargs grep -h "^func Fuzz" | wc -l`):
   - fuzzers = **34** (the new `FuzzNetworkFilterConfigParse` is the 35th)
   - numbered fixture dirs = **41**, tail = **0039** (26.1 lands 0040/0041/0042 → 44)
   - stat surface = **132** (stays 132)
   - DECISIONS.md tail = **ADR-0214**
   ALSO re-pin the `manager.go` line numbers the PLAN cites (@182 chainInfo, @261/@273/@302 ctors, @444/@503 buildTerminalFilter call sites, @585 unknown-filter error, @1004-1005 serveConnection step-7) — `manager.go` may have shifted since the SPEC pin. If ANY baseline/line drifts, STOP and reconcile before asserting deltas.
2. Confirm baseline GREEN at master tip BEFORE new code (`go build ./... && go vet ./... && golangci-lint run && go test -race -short ./...`; the differential suite is Docker-driven + skipped under `-short`, run it explicitly with `go test ./test/differential/ -run TestDifferential -v` for the R4 back-compat gate).
3. Work in a worktree per the standing preference (`feedback_git_worktrees`). Execute tasks 1→17; append to PROGRESS.md on each task completion.

## Phase 26.1 IMPL scope (settled at the 26.1 SPEC + the 26.1 PLAN; ADR-0213 + ADR-0214)

The PLAN's 17 tasks (see PLAN.md for exact file paths + complete code + commands + expected output):
- **Task 1** D-S1 baseline re-pin gate (above).
- **Tasks 2-6** NEW `internal/filter/network/` framework: `doc.go`+`types.go` (Status/ReadFilter/factory types/`FactoryCtx{BaseDir string}`); `buffer.go` drainable `*Buffer`; `callbacks.go` (`ReadFilterCallbacks`+`Connection`+`CloseType`); `registry.go` freeze-after-boot `*Registry` (mirror `listenerfilter/registry.go` + `http/registry.go` `KnownTypeURLs`); `chain.go` chain runner + per-connection runtime context (REUSED `*dynamicmetadata.Bucket` + `responseCodeDetails` sink pinned in Task 6 + `closeRequested`).
- **Tasks 7-9** `echo` (OnData write-back+drain+StopIteration) + `direct_response` (Config + inline DataSource 4-arm + OnNewConnection write/FlushWrite-close + PARSE-REJECT byte-stable table).
- **Tasks 10-11** `manager.go` dual-dispatch: `chainInfo.netChainFactory` field + `netReg` 11th ctor param (+ `nil` in thinner variants + the 3 OTHER ctor callers `internal/admin/admin_helpers_test.go`/`internal/admin/listeners_test.go`/`internal/listener/manager_test.go`) + build-time pre-check at @444/@503 + `network-filter-mixed-chain-unsupported` arm + `serveReadFilterChain` read loop + `serveConnection` step-7 dual-branch.
- **Task 12** boot-wiring in `cmd/envoy-go/main.go` (mirror the `lfReg` block @198-200; append `netReg` to the ctor call @213).
- **Task 13** 35th fuzzer `FuzzNetworkFilterConfigParse`.
- **Tasks 14-16** differential fixtures via the REAL `fixture.Driver` API (`init()`-registered driver packages + `runner_test.go` blank-imports; container-shaped bootstraps): `0040-network-echo/driver/` (cross-side), `0041-network-direct-response/driver/` (cross-side), `0042-network-direct-response-boot-reject/inputs/` (boot-reject — implements `BootRejectScript()`+`ExpectedBootErrorSubstring()`; D-P26.1-4 empirical substring pinned against the DOCKERIZED v1.37.2 reference).
- **Task 17** docs bundle: BEHAVIOR_CONTRACT.md 26.1 (4 edits) + ADR-0213 + ADR-0214 §Decision/§Consequences bodies (tail STAYS ADR-0214, no new number) + STATE/ROADMAP advance + six-gate verification (fuzzers 35, fixtures 44, stats 132, conformance 10/10, h2spec 53/53, R4 back-compat green).

Read-filter-ONLY (write-filter deferred, ADR-0213). NO per-route surface. `tcp_proxy`/HCM UNTOUCHED (migration is 26.2). Stat surface stays 132.

## Project state at master tip

- **Master tip:** the phase-26.1-PLAN stage-close commit (run `git log --oneline -1` to confirm the actual tip; a tip-repoint follow-up may advance it +1). Substantive predecessor: the phase-26.1-PLAN squash (lands PLAN.md + STATE advance + this next-prompt rewrite). Earlier: `ea77585` (26.1-SPEC squash; tip-repoint trail `50ede88`/`37d8be1`/`1428eab`). origin/master in sync after push.
- **§9 Network-filters family OPEN:** parent row 26 `in-progress`; sub-row 26.1 `in-progress` (SPEC+PLAN done); 26.2/26.3 `planned`.
- **Counts at master tip:** stat surface **132**; differential numbered-fixtures **41** (tail **0039**); fuzzers **34**; conformance 10/10; h2spec 53/53; DECISIONS.md tail **ADR-0214**, next-free **ADR-0215**. Anticipated 26.1 IMPL deltas: fixtures 41 → 44 (0040/0041/0042); fuzzers 34 → 35; stats unchanged at 132; ADR-0213+0214 §Decision/§Consequences bodies land.
- **Toolchain:** Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); reference Envoy v1.37.2 (ADR-0008); go-control-plane v1.32.4 (ADR-0008).

## Read first (in order)

1. THIS FILE — the phase-26.1 IMPL cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 26.1 PLAN done; awaiting 26.1 IMPL`; next-skill `superpowers:subagent-driven-development`; next-free `ADR-0215`.
3. `docs/envoy-go/phases/26.1-network-filter-chain-framework-and-echo/PLAN.md` — the 17-task TDD plan (exact file paths + complete code + commands + expected output; the ADR-0045 split-gate check; the D-question resolutions; the File Structure map).
4. `docs/envoy-go/phases/26.1-network-filter-chain-framework-and-echo/SPEC.md` — the 26.1 SPEC (the §3.1 API signatures the PLAN copies verbatim; §3.3 chain runner; §3.5 dual-dispatch; §4 filters; §6 PARSE-REJECT; §8 fixtures; §11.1 D-S1 baselines; §12 D-questions; §13 R1-R7; §15 acceptance).
5. `docs/envoy-go/phases/26-network-filter-chain-and-rbac/SPEC.md` — the PARENT SPEC (inherited §5/§6/§7/§8/§11/§12/§13 rosters).
6. `docs/envoy-go/ROADMAP.md` rows 26/26.1; `BOOTSTRAP_PROMPT.md` §5 (state machine) + §4.1 STATE invariant + ADR-0004/0005 (autonomous IMPL adaptation) + ADR-0045 (split-gate); `docs/envoy-go/SKILL_ROUTING.md` (confirm state-3 → subagent-driven-development).
7. The precedent code the PLAN pins against: `internal/listener/listenerfilter/{registry,pipeline,types}.go`; `internal/filter/http/{types,registry,callbacks}.go` + a trivial filter (`buffer/` or `cors/`) + `buffer/fuzz_test.go`; `internal/listener/manager.go` (dual-dispatch target); `internal/filter/tcpproxy/filter.go` (`Handle` — untouched); `internal/dynamicmetadata/dynamicmetadata.go` (REUSED sink); `internal/tls/datasource.go` (`loadDataSource` — the DataSource 4-arm precedent for direct_response); `cmd/envoy-go/main.go` (boot-wiring, `lfReg` mirror @198-200); `test/fixtures/0000-tcp-echo/driver/driver.go` + `test/fixtures/0033-http-ratelimit-boot-reject/inputs/driver.go` + `test/differential/{fixture/fixture.go,harness.go,runner_test.go}` (the REAL differential-fixture harness).
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — execution-style + worktree + push + subagents-no-push + differential-asserter-dispatch + fixture-dispatch-constraint + **network-factoryctx-basedir** (the 26.1 D-question resolutions) all apply.

After the 26.1 IMPL is complete + verified + reviewed + squash-merged + pushed, advance STATE.md per BOOTSTRAP §5 (26.1 phase-done; ROADMAP sub-row 26.1 `in-progress → done`; parent row 26 STAYS `in-progress`) and rewrite next-prompt.txt for the 26.2 SPEC cold-start (`superpowers:writing-plans` scoped to the 26.2 per-sub-phase SPEC; consumes ADR-0215).
