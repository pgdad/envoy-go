=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-VERIFIED 2026-07-19 (re-run MECHANICALLY at the phase-69 BRAINSTORM close, in the stage worktree, POST row-69 registration) — it does NOT fire. `stop` was NOT created.** Actual output:
- **(1) prints `NOT DONE: row 69`** — the freshly-registered row 69 is `in-progress` (registration re-opens check (1) per the §Schema invariant; every OTHER chartered row is `done`).
- **(2) prints THREE live "candidates:" sentences** — HTTP/3, xDS, Observability ⇒ three families STAY OPEN. **Cite the command, never the adjective** (`reference_sentinel_deferred_sentence_live_vs_historical`): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` ⇒ **3**.
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** ⇒ THREE families never opened.

Checks (1)+(2)+(3) print ⇒ the sentinel does NOT fire. Abundant work remains.

==============================================

## STATUS AS OF 2026-07-19: THE PHASE-69 BRAINSTORM IS COMPLETE AND LANDED. ROW 69 (`stats-sink-otlp`) IS `in-progress`. THIS SESSION PERFORMS THE PHASE-69 SPEC.

**What the phase-69 BRAINSTORM landed** (`git log --grep 'phase 69'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): the roller SELF-PICKED **`stats-sink-otlp`** — the OpenTelemetry OTLP metrics stats sink `envoy.stat_sinks.open_telemetry`, the FIFTH `stats_sinks[]` consumer, the next Observability-family row — per the 2026-07-12 standing directive, with the FULL rejected roster adjudicated in BRAINSTORM §2.1 (SDS rotation = a multi-leg live-reconfig row, `watched_directory` first · DataSource `environment_variable` D-ENV-HARNESS-gated · tracing `metadata` plumbing unbuilt · tiny riders not row-sized, the require=false RBAC/principal fixture arm named the best RIDER · QuicProtocolOptions near-zero differential value · upstream SDS VALUE-cycle blocked, restated not re-derived · the `ssl` family framework-surgery per ADR-0286 C3). The twice-deferred "loses on SIZE" adjective was re-derived STALE at tip (`reference_deferred_candidate_cost_restale`): every hard part is landed substrate — the phase-47 Sink/Flusher subsystem + ADR-0263 `deltaState` + ADR-0264 `ExtractTags`/`labelMapper` + TypeURL dispatch/sibling-reject + the grpcclient unary-OTLP precedent + `go.opentelemetry.io/proto/otlp v1.0.0` DIRECT (`collector/metrics/v1` vendored ⇒ +0 modules anticipated). **PINNED by EXECUTED probes** (Dossier P, `envoyproxy/envoy:contrib-v1.37.2`, fresh container per arm, throwaway per-arm unary-Export Go receivers with served-this-arm asserts — 16 numbered conclusions, compressed): gRPC-only transport (PGV boot-reject when `protocol_specifier` absent) · ONE unary Export per flush, 1 resourceMetrics/1 scopeMetrics/empty scope · `telemetry.sdk.*` resource attrs only · used-only where used ≠ non-zero and the set GROWS · the two BoolValue knobs DOC-DEFAULT TRUE are REAL (baseline = tag-extracted names + tags-as-attributes; both-false the discriminating arm) · same-name stats = SEPARATE Metric entries keyed by attributes · Counter → monotonic Sum / Gauge → Gauge / histograms → Histogram (19 default bounds; envoy-go has NO histograms, ADR-0060 ⇒ a NAMED COVERAGE BOUNDARY; `bootstrap.go:596` REFUTED the accept-inert lean — D-OTLPM-HISTO leans REJECT) · default CUMULATIVE, `report_counters_as_deltas` → DELTA with `isMonotonic` retained + startTime chaining, the two delta knobs INDEPENDENT · `prefix.` composes · the reference's factor-1000 µs `startTimeUnixNano` bug is NOT cloned (per-side structural assertion, NEVER cross-side startTime equality) · fail-open + retry-per-flush, no sink-health stat (⇒ +0 stats) · the sink cluster's own stats are exported (feedback loop — assert application-cluster subsets) · P-16: Docker Desktop AAAA gotcha ⇒ `dns_lookup_family: V4_ONLY` on sink clusters in all future probes. **The D-OTLPM docket to the SPEC:** HISTO · SKEW (`resource_detectors`/`custom_metric_conversions` are v1.37.2-only, absent from the pinned v1.32.4 six-field `SinkConfig`) · STARTTIME · DUPNAME · NODE · SPLIT (BY-CONCERN 2-leg anticipated, collapse-to-flat argued honestly) · FIXTURE (`0112`, port 10448; fixtures 113 → 114–115 RANGE) · FUZZSEED · the RBAC RIDER. **Envelope:** ZERO new production packages (`internal/statssink/otlp.go` new FILE + one `internal/bootstrap` parse arm + a ~40-line `internal/grpcclient` stanza + one `cmd/envoy-go` build-loop arm; new TEST-ONLY `test/helpers/otlpmetrics`); new exports ONLY in the existing statssink/grpcclient rosters — the `internal/xds` zero-new-symbol discipline UNTOUCHED. **Adversarially verified before landing** (zero SEVERE; one MODERATE §8-roster-carry + three MINOR corrections folded in; design direction unchanged). **Row 69 registered `in-progress`; NO deferred sentence narrowed pre-IMPL** (the verified phase-57 graphite precedent — "OTLP-metrics stats sink" rolls OUT of ROADMAP:198 at the phase-69 IMPL row-done edit only). Counts at exit (all +0): fixtures **113** · fuzzers **55** · stat **1201** (doc count) · BackendKind **38** · go.mod modules **2** · DECISIONS tail **ADR-0290** (next-free ADR-0291).

**⚠️ PUSH (idempotent — evaluate, then act):** FIRST ACTION: `git fetch`; if origin/master is BEHIND local master and the local tip is the phase-69 BRAINSTORM squash (`git log --grep 'phase 69'`, locate by SUBJECT), `git push origin master` per `feedback_push_to_origin`; if in sync, proceed; if REJECTED (diverged), STOP and reconcile — never rebase away landed squashes.

## What THIS session does — phase 69 SPEC

One stage per session: SPEC ONLY (docs-only apart from the DECISIONS §Context append; ZERO production `.go`). Fresh worktree off master per `feedback_git_worktrees`; subagent-driven (`feedback_execution_style`); adversarially verified before landing; row 69 STAYS `in-progress`; sentinel re-run mechanically; roll this router to the phase-69 PLAN.

**The SPEC owes (the phase-66/67/68 precedent):**
- **RE-DERIVE the BRAINSTORM against the tree at the new tip** — do not execute it (`feedback_brief_citations_not_evidence`; every `file:line` re-opened; a parallel stream can mint fresh drift, `feedback_parallel_stream_mints_fresh_drift`).
- **Resolve EVERY D-OTLPM-* docket item** — with discriminating probes where a behavioral claim gates a decision (`reference_probe_must_discriminate`; fresh container per arm, `feedback_probe_fresh_container_per_arm`; served-this-arm asserts; `dns_lookup_family: V4_ONLY` on sink clusters per P-16): D-OTLPM-HISTO (lean REJECT per the checked `bootstrap.go:596` phase-47 precedent) · D-OTLPM-SKEW · D-OTLPM-STARTTIME · D-OTLPM-DUPNAME · D-OTLPM-NODE · D-OTLPM-SPLIT (the ADR-0045 valve arms at the PLAN) · D-OTLPM-FIXTURE · D-OTLPM-FUZZSEED (dispatch-verify that seeds REACH the new arm) · the RBAC RIDER (attach if ≤1 task, else stays deferred).
- **Draft ADR-0291 §Context** — the DECISIONS tail flips ADR-0290 → ADR-0291 AT THE SPEC COMMIT per ADR-0044; next-free becomes ADR-0292.
- **Pin the BEHAVIOR_CONTRACT edit map** (the OTLP sink section: six-field surface, histogram boundary, startTime posture, fail-open).
- **The task-surface + split decision** (D-OTLPM-SPLIT: BY-CONCERN 2-leg vs SINGLE FLAT ROW, decided with task-count evidence in hand).
- **The deferred roster §13** (carry the BRAINSTORM §8 roster — including the S2/S3 boot-FAIL and D-EDF-STREAMPOSTURE option-(a) items the verifier restored — minus anything this SPEC consumes).
- Adversarial verification before landing; row 69 STAYS `in-progress`; sentinel re-run MECHANICALLY (expect (1) `NOT DONE: row 69`, (2) three sentences, (3) gRPC/Runtime/WASM); roll this router to the phase-69 PLAN.

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline; the next-skill line carries the owed roster).
2. `docs/envoy-go/ROADMAP.md` — row 69 + the Observability family paragraph (locate the live `candidates:` sentence by grep, not line) + §Schema.
3. `docs/envoy-go/DECISIONS.md` — ADR-0262/0263/0264 (the sink lineage: flush subsystem / deltas / tags) + ADR-0286 §Consequences C3 (`ssl` family posture) + ADR-0290 (the just-completed row) + ADR-0045 (split valve) + ADR-0044 (§Context-at-SPEC).
4. `docs/envoy-go/phases/69-stats-sink-otlp/BRAINSTORM.md` — the full D-OTLPM docket + the 16 probe conclusions + the §8 deferred roster.
5. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — trust MEMORY.md's index; esp. `reference_stats_sink_emits_used_only`, `reference_periodic_sink_differential_two_receivers`, `reference_delta_sink_differential_stability_barrier`, `reference_cluster_sink_dial_unaccounted`, `reference_protojson_wrapper_scalar_not_object`, `reference_deferred_candidate_cost_restale`.
