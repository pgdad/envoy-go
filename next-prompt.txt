START a fresh session for envoy-go. **The phase-43.1 (connection-pool budgets + pending wait-queue) PLAN is COMPLETE, plan-document-reviewer-APPROVED, and LANDED on master, and ROW 43 (`connection-pooling`) is `in-progress`.** 43.1 is the FIRST by-concern leg of the FIFTH-and-FINAL Upstream-robustness-family row (43.1 = the budget substrate + pending wait-queue; 43.2 = the H2 multiplex pool, a later leg that SUPERSEDES ADR-0056). The next step is the **43.1 IMPL** (`superpowers:subagent-driven-development` — fresh implementer per task + two-stage review) executing `docs/envoy-go/phases/43-connection-pooling/PLAN.md` (12 tasks). Confirm the latest master is the phase-43.1 PLAN completion commit via `git log --oneline -3` (a NEW sha — do NOT assume a literal value). Counts at tip (UNCHANGED at the PLAN): stat surface **1181** / fixtures **79** (tail `0077-hedge-on-per-try-timeout`) / fuzzers **42** / BackendKind tail **36** (`BlockingHoldResponder`) / DECISIONS tail **ADR-0251**, next-free **ADR-0252**.

## NEXT STEP — execute the 43.1 IMPL (the 12-task TDD spine)

Execute `docs/envoy-go/phases/43-connection-pooling/PLAN.md` task-by-task via `superpowers:subagent-driven-development` (a fresh worktree off master per `feedback_git_worktrees`; subagents commit LOCALLY only per `feedback_subagents_no_push`; the controller squashes + pushes at stage-close per `feedback_push_to_origin`). The PLAN's D-S431-* resolutions are baked into the tasks — follow them. **The load-bearing risk is the pending wait-queue concurrency (Tasks 3/5/6) — insist on `-race -count=1`** (the `-count=1` defeats go-test caching across the iterative break/fix loop, `reference_differential_break_protocol_count1`).

The 12 tasks: (1) PROGRESS + baselines; (2) parse `max_connections`/`max_pending_requests` + build `connPool` (defaults 1024); (3) **the `connPool` wait-queue primitive** (`connpool.go`: `acquireConnOrPend`/`releaseConn`/ctx-cancel + stat transitions; `-race` matrix — acquire/pend/wake/queue-full/ctx-cancel/max_connections:0); (4) activate `cx_open`/`rq_pending_open`/`upstream_cx_overflow` + add `upstream_rq_pending_active`/`_total` (**1181 → 1183**) + registration test; (5) the permit overlay at `Dial` + `AcquireH1` MISS + `connDec` compose + byte-stability gate; (6) the `PutIdleH1` idle-return wake + pend/wake integration test; (7) the overflow → 503 routing (router H1 `AcquireH1` + H2 `DialH2` via the `cluster.IsConnPoolOverflow` sentinel + the 2 legacy sites); (8) the `0078` cross-side fixture + the `/__release_sticky` backend control; (9) `0078` deliberate breaks + 20-run flake + `-race`; (10) full 80-dir differential + six-gate; (11) ADR-0252 body + BEHAVIOR_CONTRACT (1181→1183); (12) completion bundle + ROADMAP leg 43.1 → `done` (row 43 STAYS `in-progress`).

## Architecture as PLANNED (43.1 — ADR-0252)

**THE load-bearing SPEC finding (AMEND-CP1 — `reference_max_connections_soft_breaker`):** the reference's `max_connections` is a SOFT breaker (`cx_active` EXCEEDS the cap with timing slop) ⇒ cross-side EXACT connection-pool differentials infeasible; envoy-go implements a CLEAN HARD-CAP + bounded-queue DEPARTURE (USER-CONFIRMED). **The PLAN's concurrency design (D-S431-1):** a sibling `connpool.go` `connPool` = a `sync.Mutex` + a FIFO of per-waiter BUFFERED cap-1 channels; a connection-creation permit is try-acquired at the connection-CREATION boundary (`Cluster.Dial` raw-dial + the `AcquireH1` pool-MISS; a pool HIT reuses an already-counted conn ⇒ NO permit — byte-neutral keep-alive). At the cap the request PENDS (bounded by `max_pending_requests`), woken via a DIRECT permit-handoff on conn-Close (the `connWithGauge` dec closure composed via `Cluster.connDec`) or on idle-return (`PutIdleH1` closes-and-wakes when a waiter is queued — routing BOTH wake sources through the single permit mechanism); ctx-cancel-while-pending uses a re-lock + `removeWaiterLocked`, and if already-dequeued a `<-ch` drain + `releaseConn` give-back (so the raced permit never leaks). Queue-full ⇒ a `cluster.IsConnPoolOverflow` sentinel ⇒ a fail-fast 503 + `upstream_rq_pending_overflow` (the shared phase-41 counter; AMEND-CP2). The `connPool` (DEFAULT-only) is referenced from `circuitBreaker` as `pool *connPool` (D-S431-2 — a sibling, NOT a `cbPriority` extension). Stat surface **1181 → 1183** (D-S431-3 — activate 3 existing shapes +0; add 2 new shapes; the SPEC's ~1199 SUPERSEDED). `0078` (D-S431-5): `refContainerListenerPort = 19167`; the staged drive (subject-exact: fill N → poll cx_open+cx_active==N → fill M → poll rq_pending_open+pending_active==M → fire J; reference-robust: fill N → poll cx_open → burst refOversub) + a STICKY-release `/__release_sticky` backend control (the re-armable `/__release` would stall the post-release waiter redials; `0074`'s `/__release` UNCHANGED; BackendKind tail STAYS 36). NO new config-reject + NO new fuzzer + REUSE `BlockingHoldResponder` 36. Byte-identical when no `circuit_breakers`.

## Honor throughout (the discipline)

- **Lifecycle = one stage per session.** `feedback_git_worktrees` (a fresh worktree off master) · `feedback_subagents_no_push` (subagents commit LOCALLY; the controller squashes + pushes at stage-close) · `feedback_push_to_origin` (push after a clean squash on master with tests green) · `feedback_execution_style` (subagent-driven execution) · `feedback_pertask_gofmt_lint` (each task runs gofmt -l + golangci-lint on touched pkgs).
- **IMPL-specific:** fresh implementer per task + two-stage review; `-race -count=1` for the wait-queue tasks (3/5/6); the per-task byte-stability gate (the full 79-dir differential stays GREEN — `0074` now builds a `connPool` at default 1024 + registers the 2 new zero stats; the reference emits them too). The ADR-0252 §Decision/§Consequences land at the IMPL (per ADR-0044; §Context drafted in SPEC §13).
- **Process anchors:** ADR-0106 + `reference_roadmap_split_phase_row_done` (row 43 flips `done` only when BOTH 43.1+43.2 land); ADR-0080 (strict-reject); `reference_docker_probe_bridge_network` + `reference_concurrency_differential_release_barrier` + `reference_concurrent_attempt_downstream_class_assertion` (the concurrency-differential discipline); `reference_fixture_workload_constant_desync` (single-source the `0078` constants); `reference_differential_run_selector` (`-run 'TestDifferential/0078'`, NOT `-run '0078'`); `reference_differential_break_protocol_count1` (`-count=1` on every break run).

## Read first (in order)

1. THIS FILE — the cold-start router (the 43.1 PLAN is DONE + APPROVED + LANDED; row 43 is `in-progress`; the next step is the 43.1 IMPL).
2. `docs/envoy-go/STATE.md` — active-phase `phase 43.1 (connection-pooling) PLAN done`; the count figures (1181 / 79 / 42 / 36 / ADR-0251, next-free ADR-0252) + the D-S431-* resolutions.
3. `docs/envoy-go/phases/43-connection-pooling/PLAN.md` — the 12-task TDD spine (the D-question resolutions block; the file structure; the per-task complete code; the exit-delta table).
4. `docs/envoy-go/phases/43-connection-pooling/SPEC.md` — the 43.1 SPEC (the §1.1 AMEND-CP1..7; the §3 hard-cap + wait-queue architecture; the §7 stat surface; the §8 `0078` differential; the §13 ADR-0252 §Context draft).
5. `internal/cluster/circuitbreaker.go` + `internal/cluster/cluster.go` — the phase-41 `circuitBreaker`/`cbPriority` struct 43.1 extends + the `Dial`/`AcquireH1`/`PutIdleH1`/`connWithGauge` permit/wake seams; `internal/cluster/dial_h2.go` (the `DialH2` overflow seam) + `internal/filter/http/router/{router,router_h2}.go` (the 503-routing sites).
6. `docs/envoy-go/DECISIONS.md` — ADR-0248 (the phase-41 struct 43.1 extends) + the ADR-0044/0045/0080/0106/0227 anchors; next-free ADR-0252.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `reference_max_connections_soft_breaker` [THE 43.1 load-bearing finding], `reference_concurrency_differential_release_barrier`, `reference_concurrent_attempt_downstream_class_assertion`, `reference_docker_probe_bridge_network`, `reference_fixture_workload_constant_desync`, `reference_differential_break_protocol_count1`, `reference_differential_run_selector`, `reference_roadmap_split_phase_row_done`, `feedback_git_worktrees`, `feedback_subagents_no_push`, `feedback_push_to_origin`, `feedback_execution_style`, `feedback_pertask_gofmt_lint`).
