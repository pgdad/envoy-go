START a fresh cold-start session for envoy-go — **the phase-35 (`load-balancer-random`) PLAN is DONE and LANDED on master; the next lifecycle step is the phase-35 IMPL** (`superpowers:subagent-driven-development` — execute the 8-task PLAN; single flat row; NO escape valve; the Load-balancing family stays OPEN — 6 candidates remain after 35). The substantive master tip is the phase-35 PLAN squash **`1f22dbe`** (`phase 35 PLAN: load-balancer-random — decompose the SPEC §10 spine into 8 bite-sized TDD tasks; resolve D-S35-1..4; ADR-0045 re-check → NO SPLIT`; pushed to `origin/master`; confirm via `git log --oneline -3`). It carries `docs/envoy-go/phases/35-load-balancer-random/PLAN.md` + the README.md status advance (2 → 3) + the STATE advance to `phase 35 PLAN done`. The literal live master tip is the docs-only SHA-fill commit **`5b198ff`** (`next-prompt.txt: SHA-fill the literal live tip — name 1f22dbe (the phase-35 PLAN squash) …`; it trails `1f22dbe` and named it as the substantive tip; confirm via `git log --oneline -3`). Below it sits the phase-35 SPEC squash `bdef11a` (the authoritative SPEC.md), and below that the phase-35 BRAINSTORM squash `c5c6c55`, and the phase-34 IMPL squash `b086137` (the `leastRequest` policy + the ADR-0232 LB acquire/release seam + `newPCGRNG` that phase 35 REUSES).

## What this session does — execute the phase-35 IMPL (`superpowers:subagent-driven-development`)

The next session is the **phase-35 IMPL**: execute the 8-task PLAN subagent-driven (`feedback_execution_style`) in a worktree (`feedback_git_worktrees`); each task is write-failing-test → run-fail → minimal-impl → run-pass → per-task `gofmt -l` + `golangci-lint` (`feedback_pertask_gofmt_lint`) → LOCAL-ONLY commit (`feedback_subagents_no_push`); the controller squash-merges + pushes at stage-close (`feedback_push_to_origin`). The full ADR-0234 entry (§Context + §Decision + §Consequences, status PROPOSED → ACCEPTED) lands at this IMPL per ADR-0044 (DECISIONS tail ADR-0233 → **ADR-0234**; next-free ADR-0235). ROADMAP row 35 flips `in-progress → done` at the six-gate (a flat family row — NO parent rollup per ADR-0106).

## The 8-task PLAN spine (`docs/envoy-go/phases/35-load-balancer-random/PLAN.md` — AUTHORITATIVE for the IMPL)

1. **Baselines/anchors gate + PROGRESS.md** — re-confirm fixtures **61** (tail `0059`) + fuzzers **42** + stat surface **1116** + BackendKind tail **33** + DECISIONS tail **ADR-0233** via the canonical recipes; re-pin the as-built anchors (`loadbalancer.go` `noopRelease`/`roundRobin.Pick`, `leastrequest.go:63` `newPCGRNG` + `newLeastRequestWithRNG`, `manager.go:234` switch + `:252` reject text, `manager_test.go:320` `TestManager_Error_UnsupportedLBPolicy`); confirm the reject-text blast radius is exactly 3 sites; create PROGRESS.md.
2. **The `randomLB` stateless type** (`internal/cluster/random.go` NEW) — one draw `rng() % n`, pick `endpoints[i]`, return the shared `noopRelease`; REUSES `newPCGRNG` verbatim (D-S35-2: call directly, accept the shared seed-error message, NO wrapper); empty-set → `Endpoint{}, noopRelease, errNoEndpoints` parity. Unit tests via a deterministic injectable RNG (`newRandomWithRNG`). NOTE: `seqRNG`/`eps` already exist in `leastrequest_test.go` (same package) — REUSE, do NOT redeclare.
3. **Manager acceptance** (`manager.go` + `manager_test.go`) — the bare `case clusterv3.Cluster_RANDOM` (NO `parseRandomLbConfig` — RANDOM has no config message); the reject text extends `(supported: ROUND_ROBIN, LEAST_REQUEST)` → `(…, RANDOM)`; the **DOUBLY-hit retarget** of `TestManager_Error_UnsupportedLBPolicy` (trigger `Cluster_RANDOM` → `Cluster_RING_HASH` since RANDOM is now accepted, AND extend the pinned substring to `"ROUND_ROBIN, LEAST_REQUEST, RANDOM"` — both edits load-bearing); the mismatched-oneof-under-RANDOM silent-ignore test.
4. **In-process anti-skew integration test + RANDOM boot smoke** (`random_test.go` + `manager_test.go`) — RANDOM follows the draw and does NOT avoid a held endpoint (the contrapositive of least_request's skew test — D-S35-4); the `-count=1` liveness proof is a temporary load-avoidance break; the boot smoke builds a 3-endpoint RANDOM Manager and exercises `PickEndpoint`.
5. **The `0060-lb-random` fixture** (`test/fixtures/0060-lb-random/`) — transpose `0059` (drop `least_request_lb_config`; `lb_policy: RANDOM`); the REUSED hold-4 + burst-60 + drain stimulus with the assertion INVERTED — the SECOND band-based `AssertDistribution` (conservation `sum == 64` + per-host floor `>= 12` + per-host ceiling `<= 32`, PER SIDE) + the cross/per-side `StatsAsserter` prong (cx_total/membership/quiesced-active cross-equal; `upstream_rq_total` per-side ref=64/subj=0). Reuses `TCPEcho` 0 (NO new BackendKind). Use `-run 'TestDifferential/0060'` (`reference_differential_run_selector`).
6. **Band tuning + 3 `-count=1` deliberate breaks** — ≥20-run flake-free per side; (i) consult-the-counters [wire the RANDOM case to `newLeastRequest` → floor leg FAILS — the canonical anti-skew break]; (ii) pin-to-endpoints[0] [floor + ceiling FAIL]; (iii) dropped-Inc [stats prong FAILS]. (`reference_differential_break_protocol_count1` generalized to BAND assertions.)
7. **Full differential re-verify** — all 62 dirs (61 prior byte-exact through the new manager case — the seam is unchanged → behavior-neutrality is structural — + `0060` green); `-race -short`; `go mod tidy -diff` empty; h2spec 53/53 + proxy-wasm 10/10 unaffected.
8. **Completion bundle** (ADR-0052 atomic landing) — the BEHAVIOR_CONTRACT `random` subsection + the line-899 reject-text update; the full ADR-0234 entry (§Context from SPEC §13 + §Decision + §Consequences; ADR-0044 in-place; tail → ADR-0234); STATE/ROADMAP row 35 `in-progress → done` (fixtures 61 → 62; stat surface 1116 zero-delta; the SECOND no-fuzzer/no-BackendKind phase); the six-gate evidence.

## The §12 D-S35-1..4 — RESOLVED at the PLAN (the IMPL honors)

- **D-S35-1** file placement: `random.go` NEW sibling; the manager case + reject text in `manager.go`; the retarget in `manager_test.go`.
- **D-S35-2** `newRandom` calls `newPCGRNG()` DIRECTLY and ACCEPTS the shared seed-error message (`"cluster: least_request: seed rng"`), NO wrapper (the path is reachable only on a `crypto/rand` failure — effectively unreachable; a wrapper would double-wrap or require editing `newPCGRNG`, which AMEND-R5 forbids; a recorded cosmetic note in BEHAVIOR_CONTRACT/PROGRESS).
- **D-S35-3** band {K=4, S=60, sum==64, floor 12 ≈2.5σ, ceiling 32 ≈2.8σ} + the tuning protocol (≥20 flake-free per side + the 3 `-count=1` breaks).
- **D-S35-4** a standalone deterministic-RNG anti-skew unit test (RANDOM does NOT avoid a held endpoint), distinct from the `0060` fixture.
- ADR-0045 split-gate FINAL re-check → **NO SPLIT** (8 tasks / ~45–60 prod LoC — both legs far under; NO escape valve).

## Counts at master tip (phase 35 PLAN done — they advance only at the phase-35 IMPL)

- stat surface **1116** (anticipated UNCHANGED at phase 35 — the SECOND zero-delta phase; AMEND-R4).
- differential fixtures **61** (tail `0059-lb-least-request`; `ls -d test/fixtures/[0-9]* | wc -l`). Anticipated 61 → 62 at the IMPL (`0060-lb-random`). NO boot-reject dir.
- fuzzers **42** (tail `FuzzThriftDecode`; anticipated UNCHANGED — no wire decode).
- BackendKind tail **33** (`TCPThriftResponder`; anticipated UNCHANGED — `0060` reuses `TCPEcho` kind-0).
- DECISIONS tail **ADR-0233** (next-free **ADR-0234** — the ADR-0234 §Context is a DRAFT in SPEC §13; the full entry + §Decision/§Consequences body land at the phase-35 IMPL per ADR-0044).
- reference pin: `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227).
- Toolchain: Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); go-control-plane `/envoy` v1.32.4 (ADR-0008; `Cluster.LbPolicy RANDOM` is in the pinned module; ZERO new go.mod dep at 35).
- conformance h2spec 53/53 + proxy-wasm 10/10.

## Read first (in order)

1. THIS FILE — the post-phase-35-PLAN cold-start (next: the phase-35 IMPL).
2. `docs/envoy-go/STATE.md` — active-phase `phase 35 (load-balancer-random) PLAN done`; lifecycle-state `phase 35 PLAN done — SKILL_ROUTING → the phase-35 IMPL`; the counts.
3. `docs/envoy-go/phases/35-load-balancer-random/PLAN.md` — **AUTHORITATIVE for the IMPL.** The 8-task spine (write-failing-test → run-fail → minimal-impl → run-pass → gofmt/lint → LOCAL-ONLY commit); the File Structure table; the D-S35-1..4 resolutions; the exact code snippets (the `randomLB` type, the manager case, the anti-skew band `AssertDistribution`); the deliberate-break protocol.
4. `docs/envoy-go/phases/35-load-balancer-random/SPEC.md` — the authoritative phase SPEC (AMEND-R1..R6; §3 the policy + seam-reuse design; §6 the reject matrix; §8 the `0060` anti-skew band-arm; §11 the D-R1..D-R6 empirical pins; §13 the ADR-0234 §Context DRAFT).
5. `docs/envoy-go/DECISIONS.md` — **ADR-0232** (the LB seam — REUSED unchanged; the §Consequences "the non-keyed RANDOM drops in at zero cost") + ADR-0233 (the least_request policy — the RNG posture random reuses) + ADR-0024 (UNAMENDED) + ADR-0045/0044/0052/0080/0106/0060.
6. `internal/cluster/{loadbalancer,leastrequest,manager,cluster}.go` — the reused seam (`Pick() (Endpoint, func(), error)` + `noopRelease`), `newPCGRNG`/`newLeastRequestWithRNG`, the lb_policy switch (`manager.go:234`) + the reject text (`:252`); `internal/cluster/manager_test.go:320` (`TestManager_Error_UnsupportedLBPolicy` — the doubly-hit retarget).
7. `test/fixtures/0059-lb-least-request/` — the held-conn skew driver `0060` transposes (the assertion INVERTED) + the band-`AssertDistribution` + `StatsAsserter` pattern.
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_execution_style`, `feedback_git_worktrees`, `feedback_subagents_no_push`, `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_path_targeting`, `reference_differential_break_protocol_count1`, `reference_differential_asserter_dispatch`, `reference_differential_run_selector`, `reference_docker_probe_bridge_network`.

## Honor throughout

- `feedback_execution_style` (subagent-driven) · `feedback_git_worktrees` (work in a worktree) · `feedback_subagents_no_push` (subagents commit LOCAL-ONLY; the controller squash-merges + pushes at stage-close) · `feedback_push_to_origin` (after a clean landing on master with tests green, push without asking) · `feedback_pertask_gofmt_lint` · `feedback_subagent_worktree_path_targeting`.
- ADR-0106 (a flat family row per LB policy — NO parent rollup) · ADR-0045 (the split-gate — re-checked, NO SPLIT) · ADR-0044 (ADR bodies at the IMPL, in-place) · ADR-0052 (the atomic-landing six-gate) · ADR-0080 (byte-stable parse-reject wording) · ADR-0227 (the contrib reference image; live probes per `reference_docker_probe_bridge_network`).
- The lifecycle is BRAINSTORM → SPEC → PLAN → IMPL, each its own session per the established cadence. The next session is the phase-35 **IMPL**.
