=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-CHECKED 2026-07-12 (after the phase-60.1 PLAN landed, run MECHANICALLY) — it does NOT fire. Do NOT create `stop`.** State (UNCHANGED by this docs-only PLAN):
- (1) prints `NOT DONE: row 60`, `NOT DONE: row 61` — TWO `in-progress` rows (rows 60 `xds-sds-server-cert` + 61 `http3-downstream-listener`, each a CONFIRMED SPLIT). Row 60 STAYS `in-progress` (its 60.1 leg is now PLAN'd but not yet IMPL'd; the row flips `done` only when BOTH 60.1 + 60.2 land, ADR-0106). This ALONE blocks `stop`.
- (2) prints THREE live "candidates:" sentences — Observability, xDS, and HTTP/3 ⇒ three families STAY OPEN. (`reference_sentinel_deferred_sentence_live_vs_historical`.)
- (3) prints `NEVER OPENED: gRPC`, `Runtime`, `WASM` ⇒ THREE families never opened. Operational-tooling also STAYS OPEN.

All three checks print ⇒ the sentinel does NOT fire.

**STATUS AS OF 2026-07-12: the phase-60.1 PLAN LANDED (docs-only; row 60 STAYS `in-progress`).** The NEXT IMPLEMENTABLE STAGE is the **phase-60.1 IMPL** (`xds-sds-stream-substrate`, lifecycle-state 3 → done-for-this-leg). The PLAN (`docs/envoy-go/phases/60-xds-sds-server-cert/PLAN-60.1.md`) decomposes the SPEC-60 §3.1/§3.3/§7/§10 substrate scope into a 10-task TDD spine; the `PROGRESS-60.1.md` scaffold is landed. After the 60.1 IMPL lands, the roller's next stage is the phase-60.2 PLAN (`xds-sds-tls-cert-apply`) — the OTHER banked leg awaiting its PLAN (then IMPL). Row 61 (HTTP/3) is BANKED at SPEC (a 61.1/61.2/61.3 split) and rolls after the xDS legs, or per a future roller pick.

**ADR reservation ledger (provisional; each finalized at its IMPL in landing order):** ADR-0277 = phase-59 (LANDED) · **ADR-0278 = xDS 60.1 (the NEXT to land, at the 60.1 IMPL)** · ADR-0279 = HTTP/3 (61) · ADR-0280 = xDS 60.2 · next-free unreserved **ADR-0281**. **60.1 lands NO fixture** (substrate-only; the differential is 60.2's) — no `0103-*` dir at 60.1. When 60.2 or HTTP/3-61.3 reach IMPL, RE-DERIVE the actual fixture-dir number against the master tip at IMPL time (the tail is `0102-tracing-custom-tags-literal`; landing order assigns 0103/0104…).

**Current counts (UNCHANGED by the docs-only phase-60.1 PLAN; on the frozen stage HEAD):** fixtures **104** (tail `0102-tracing-custom-tags-literal`) · fuzzers **54** · DECISIONS tail **ADR-0277** (next-free reserved ADR-0278) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** · new Go packages **0** · new go.mod modules **0**. RE-VERIFY against the actual master tip at session start (`git fetch` first).

**Locating the phase-60.1 PLAN stage squash** (a router cannot name its own sha — identify by SUBJECT, never by position):
```sh
git log --oneline --grep='^phase 60.1 (xds-sds-stream-substrate) PLAN' -1
```

**THE NEXT ACTION: the phase-60.1 IMPL (lifecycle-state 3 → done-for-this-leg; execute `PLAN-60.1.md` task-by-task).** Row 60 STAYS `in-progress` at the 60.1 IMPL (flips `done` only when 60.2 also lands, ADR-0106). The sentinel governs termination, not this list.

==============================================

## What THIS session does (the phase-60.1 IMPL)

Execute the phase-60.1 IMPL — build the `internal/xds` SDS discovery-stream substrate per `docs/envoy-go/phases/60-xds-sds-server-cert/PLAN-60.1.md`, task-by-task, SUBAGENT-DRIVEN (`feedback_execution_style`, `superpowers:subagent-driven-development`) in a FRESH worktree off master (`.worktrees/phase-60.1-impl`, branch `phase-60-xds-sds-stream-substrate-impl`, per `feedback_git_worktrees`). This is a SINGLE STAGE (the 60.1 IMPL); do NOT start the 60.2 PLAN.

**The 10-task TDD spine (from PLAN-60.1; RE-READ the PLAN — every `file:line` there was RE-DERIVED against master `565ac12a`, RE-DERIVE AGAIN against the actual tip per `feedback_brief_citations_not_evidence`):**
1. PROGRESS scaffold check-in + baselines + the D-XDS PLAN pins (the scaffold already exists — verify/refresh).
2. `internal/xds` skeleton — package doc + `dataSourceBytes` (the cycle-guard duplicate of `tls.loadDataSource`) + runtime `secretTypeURL` (`proto.MessageName`).
3. `parseSecret` — Any→`Secret`→`*crypto/tls.Certificate` (name + `tls_certificate`-oneof validation).
4. `fetchSecret` — the SotW initial-request/recv/ACK-NACK loop over the `sdsStream` seam.
5. the `sds.<secret>.*` 5-counter subset + the `IsValidName` guard before `NewCounterIfAbsent` (`reference_dynamic_stat_name_charset_guard`).
6. `grpcclient.SDSClient` — the BIDI `StreamSecrets` wrapper (the `ALSClient` streaming precedent).
7. `test/helpers/sdsserver` — the driver-owned fake SDS management server (NOT a BackendKind, `reference_differential_grpc_receiver_driver_owned`).
8. `SecretProvider` + `Provider.FetchInitialCertificate` (blocking, `initial_fetch_timeout`, classified `sds.*` stats) proven vs the fake server.
9. `FuzzDiscoveryResponseParse` — the untrusted `DiscoveryResponse`→`Secret` wire boundary (fuzzers **54 → 55**; `reference_fuzzer_count_docs_drift`).
10. ADR-0278 body (§Decision/§Consequences; §Context already at SPEC §13) + STATE/ROADMAP/PROGRESS + the six-gate verify.

**Anticipated exit counts (RE-VERIFY at the six-gate):** a NEW `internal/xds` package (packages **+1** prod) + a NEW `test/helpers/sdsserver` test package (**+1** test); fuzzers **54 → 55**; stat surface **1201 (+0 STATIC; +5 DYNAMIC `sds.<secret>.*` per configured secret — proven by a unit +5 delta, materialized at boot only under a 60.2 SDS config)**; fixtures **104 (+0 at 60.1)**; BackendKind **38 (+0)**; go.mod modules **+0** (go-control-plane v1.32.4 already carries `service/secret/v3` + `service/discovery/v3`); DECISIONS tail **ADR-0277 → ADR-0278** (anchored at this IMPL, ADR-0044). Row 60 STAYS `in-progress` (ADR-0106; flips `done` only when 60.2 lands). **NO full `test/differential` run at 60.1** — no new fixture, no production request-path change; that gate is 60.2's. Then ROLL THIS FILE to the phase-60.2 PLAN.

## Honor throughout (the discipline, unchanged)
- **Lifecycle = one stage per session.** This session: the phase-60.1 IMPL ONLY. After it lands: the phase-60.2 PLAN.
- **Process anchors:** ADR-0044 (ADR §Decision/§Consequences land at the IMPL — ADR-0278 body at THIS IMPL) · ADR-0045 (split gate; CONSUMED by the 60.1/60.2 split) · ADR-0080 (distinct per-arm reject substrings — the strict-reject arms are 60.2, but the 60.1 applier's `parseSecret` errors are already distinct) · ADR-0106 (a split-phase row flips `done` only when ALL legs land — row 60 STAYS `in-progress` at 60.1). Next-to-land ADR is **ADR-0278**.
- **Subagent-driven execution** (`feedback_execution_style`, `feedback_subagent_autocommit_claudemd`, `feedback_subagents_no_push`): a fresh subagent per task; subagents commit LOCALLY only; the controller reviews + verifies each commit (RE-DERIVING citations per `feedback_brief_citations_not_evidence`), cleans leak files, squashes at stage-close and pushes. Watch for HEAD-detach on deliberate breaks (`feedback_subagent_worktree_detach`) and stale-path targeting (`feedback_subagent_worktree_path_targeting`).
- **`next-prompt.txt` IS TRACKED** (`reference_next_prompt_tracked_despite_gitignore`) — edit it inside the stage worktree and fold into the stage commit; locate commits by SUBJECT (`git log --grep`), never by position.
- **The cycle guard (load-bearing, from PLAN-60.1):** `internal/xds` must NOT import `internal/tls` (the 60.2 `tls → xds` edge would cycle) — it carries its own `dataSourceBytes`; the BIDI dial wrapper lives in `internal/grpcclient`. Confirm the import graph stays acyclic at each task's build gate.
- **Deferred maintenance (fold into the next `udp.go`/statssink-driver-touching row — carried from the phase-57 final review; phases 58/59 + the 60.1 xDS/TLS substrate touch NEITHER `udp.go` NOR a statsd/graphite/dog_statsd sink — these STAY OPEN):** F-1 the 0101 driver's counter tag snapshot records the EXPECTED map; F-2 `udp.go` `udpWriter` doc comment names two sinks (three embed it now); F-3 BEHAVIOR_CONTRACT 0101 note says "`differential.HostGatewayIP` helper" where the driver uses its LOCAL duplicate. (Ride until the OTLP-metrics or another statsd/graphite/dog_statsd sink row reopens `udp.go`.)

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-60.1 IMPL is next; the PLAN is landed).
2. `docs/envoy-go/phases/60-xds-sds-server-cert/PLAN-60.1.md` — the 10-task TDD spine (Global Constraints, the Orientation brief, the Key source seams + Proto facts RE-DERIVED against `565ac12a`, the D-XDS PLAN pins, the File structure, the per-task code + steps, the Self-Review).
3. `docs/envoy-go/phases/60-xds-sds-server-cert/PROGRESS-60.1.md` — the scaffold (baseline/exit counts, the D-XDS pins, the task checklist, the six-gate block).
4. `docs/envoy-go/phases/60-xds-sds-server-cert/SPEC.md` — the disposed D-XDS-* (§1), the CONFIRMED SPLIT (§3.0), the 60.1 client scope (§3.1), the config-seam verdict (§3.3 — 60.2, read for context), the `sds.*` 5-counter subset (§7), the test plan (§10), the SPEC-time probe evidence (§11), the RE-DERIVED edit-site roster (§12), and the ADR-0278 §Context draft (§13).
5. `docs/envoy-go/ROADMAP.md` — row 60 `in-progress`; the xDS-family row + prose; the LIVE xDS deferred sentence.
6. `docs/envoy-go/STATE.md` — the active-phase header (phase 60.1 PLAN done; NEXT = the phase-60.1 IMPL).
7. `docs/envoy-go/DECISIONS.md` — the ADR-0277 tail (next-to-land **ADR-0278**, its §Context drafted at SPEC §13); ADR-0158 (the `grpcclient.Dialer`/`ALSClient` seam 60.1 reuses); ADR-0117 (`NewCounterIfAbsent`); ADR-0059 (`IsValidName` boundary guard).
8. `internal/grpcclient/grpcclient.go` (the `Dialer`/`ALSClient` shape the `SDSClient` mirrors) · `internal/tls/config.go:153` + `internal/tls/datasource.go:20` (`loadDataSource` — the grammar `xds.dataSourceBytes` MIRRORS; NOT imported) · `internal/stats/registry.go` (`IsValidName` `:60` + `NewCounterIfAbsent` `:161`) · `test/helpers/accessloggrpc/` (the fake-server template).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the `reference_*` traps (esp. `reference_dynamic_stat_name_charset_guard`, `reference_differential_grpc_receiver_driver_owned`, `reference_fuzzer_count_docs_drift`, `reference_brief_citations_not_evidence`, `reference_fatalf_makes_assertions_unreachable`, and the split-phase/sentinel references).
