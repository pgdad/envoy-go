START a fresh cold-start session for envoy-go — **phase 26.3 IMPL (`network-filter-rbac`) is DONE + holistic-review APPROVED + squash-merged + pushed**, and with it **parent phase 26 (`network-filter-chain-and-rbac`) is DONE** (the parent-row-26 ROLLUP, atomic with sub-row 26.3, per the 18/19/22/24/25 precedent). There is NO open phase. This session OPENS the **next phase** — operator's choice of subject; the most likely is a fresh **phase-27 BRAINSTORM** selecting the next **§9 Network-filters-family** candidate (6 remain: `redis` / `mongo` / `kafka_broker` / `thrift` / `zookeeper` / `sni_cluster`), per SKILL_ROUTING (no SPEC/PLAN exists for any new phase → `superpowers:brainstorming`).

Phase 26.3 IMPL landed at squash `72dc36d` (pushed to origin/master): the 15-task TDD execution of the 26.3 PLAN via `superpowers:subagent-driven-development` (fresh subagent per task + two-stage review; subagents local-only; controller squash-merged + pushed). Holistic final review APPROVED (no Critical/Important; 3 minor doc-drift findings cleaned up pre-merge). What landed: (i) the shared `internal/rbac/` engine EXTRACTION (the abstract `EvalContext` + ~23 evaluators + the rules/matcher compilers + the matcher bridge + `PerPolicyCounters`, MOVED + re-exported, gaining the input-capability `Profile` [`ProfileHTTP`/`ProfileL4`]; import-cycle-clean) — HTTP rbac migrated as consumer #1, byte-exact RE-VERIFIED at the R4 gate (`0018-http-rbac` stays byte-exact green; NO HTTP-rbac behavior change), ADR-0216; (ii) the FIRST production WRITE through the connection-scoped `*dynamicmetadata.Bucket` (the rbac shadow pair `shadow_engine_result`/`shadow_effective_policy_id` under namespace `envoy.filters.network.rbac`; `internal/dynamicmetadata/` doc generalized in place, R5), ADR-0217; (iii) the `rbac_network` filter (consumer #2: `ProfileL4` L4 input surface; decision-in-`OnData` + `OnNewConnection` Continue no-op per the sticky-halt constraint; enforced-deny = `rbac_deny_close` rcd + `Close(NoFlush)` [the F3 NoFlush framework touchpoint]; shadow metadata + the four `<stat_prefix>.rbac.{allowed,denied,shadow_allowed,shadow_denied}` counters [NO per-policy — F2]; `delay_deny` + HTTP-only-matcher + stat_prefix-invalid + xDS-dynamic-policy PARSE-REJECTs; CEL silent-ignore inherited F1; the rules/matcher dual-path; registered as the 5th built-in; the FIRST production mixed read→terminal chain `[rbac_network, tcp_proxy]` — R-M-LIVE), ADR-0218. Plus the `.rbac.` Prometheus tag-extractor at `internal/stats/name.go`, the 36th fuzzer `FuzzNetworkRBACConfigParse` (found + fixed a real stat_prefix-validation panic), and the +2 differential fixtures (`0043-network-rbac` cross-side StatsAsserter R-M-LIVE; `0044-network-rbac-boot-reject`).

## What this session must do

There is no active phase. Per SKILL_ROUTING (no SPEC/PLAN for any new phase → state 0/1), the next lifecycle step is a fresh **phase BRAINSTORM** (`superpowers:brainstorming`) for the operator-chosen next phase. The strong default is the next §9 Network-filters-family candidate (the family stays OPEN; 6 remain). Confirm the subject with the operator if ambiguous; otherwise open a brainstorm for a §9 candidate. After BRAINSTORM → SPEC → PLAN → IMPL per the established per-phase lifecycle (`feedback_execution_style`: subagent-driven execution once a PLAN exists; subagents commit LOCAL-ONLY [`feedback_subagents_no_push`]; the controller squash-merges + pushes at stage-close). Work in a git worktree (`feedback_git_worktrees`).

## Project state at master tip

- **Master tip:** `7d1b7c6` (the `last-commit`-fill + next-prompt cold-start rewrite), which trails the substantive 26.3 IMPL squash `72dc36d` by +1. Confirm the live tip via `git log --oneline -1`; this master-tip-repoint commit itself advances the tip +1 (expected, not chased). STATE.md `last-commit` points at the substantive 26.3 IMPL squash `72dc36d`. origin/master in sync after push.
- **§9 Network-filters family:** parent row 26 **DONE**; 26.1 / 26.2 / 26.3 all **done**. The family stays OPEN — 6 candidates remain (`redis` / `mongo` / `kafka_broker` / `thrift` / `zookeeper` / `sni_cluster`).
- **Counts at master tip:** stat surface **136**; differential fixtures **46** (tail `0044`); fuzzers **36**; conformance 10/10; h2spec 53/53; DECISIONS.md tail **ADR-0218**; next-free **ADR-0219** (ADR-0209 escape-valve reserve STANDS-UNCONSUMED).
- **Toolchain:** Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); reference Envoy v1.37.2 (ADR-0008); go-control-plane v1.32.4 (ADR-0008).
- **Known coverage boundary (carried, non-blocking):** D-26.3-4 — the rbac_network SNI/`requested_server_name` + mTLS `authenticated` L4 arms are unit-tested but NOT cross-side differential-proven (the IP/port arms ARE — fixture 0043). Recorded in BEHAVIOR_CONTRACT + ADR-0218 §Consequences.

## Read first (in order)

1. THIS FILE — the next-phase cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 26.3 IMPL done; parent phase 26 DONE`; next-skill `superpowers:brainstorming`; next-free `ADR-0219`; last-commit `72dc36d`.
3. `docs/envoy-go/ROADMAP.md` — rows 26/26.1/26.2/26.3 all `done`; the §9 family-open note + the 6 remaining candidates.
4. `docs/envoy-go/SKILL_ROUTING.md` + `BOOTSTRAP_PROMPT.md` §4/§5 — the per-phase lifecycle + the cold-start invariants.
5. `docs/envoy-go/phases/26.3-network-filter-rbac/{SPEC.md,PLAN.md,PROGRESS.md}` + the parent `docs/envoy-go/phases/26-network-filter-chain-and-rbac/` — the just-completed family, for precedent on how a §9 Network-filter is specced/planned/implemented (esp. the `internal/filter/network/` framework, the `builtins.RegisterBuiltins` seam, the differential-fixture conventions, the `internal/rbac/` shared-engine pattern if a future filter reuses an engine).
6. `docs/envoy-go/DECISIONS.md` ADR-0213/0214/0215/0216/0217/0218 (the §9 framework + rbac family) + ADR-0106 (ROADMAP flip discipline) + ADR-0044 (in-place ADR-body discipline) + ADR-0045 (split-gate) + ADR-0052 (atomic doc bundle).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — execution-style + worktree + push + subagents-no-push + the network-filter constraints (typeurl-extensions, onnewconnection-halts) + the differential dispatch/asserter constraints all carry forward to the next §9 filter.
