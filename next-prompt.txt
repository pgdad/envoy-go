=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-VERIFIED 2026-07-16 (re-run MECHANICALLY at the phase-66 BRAINSTORM) — it does NOT fire. `stop` was NOT created.** Actual output:
- **(1) prints `NOT DONE: row 66`** — row 66 registered `in-progress` at the BRAINSTORM commit per the §Schema invariant. **Check (1) BLOCKS `stop` again** (it was silent at the phase-65 IMPL). It goes silent once row 66 flips `done` at its IMPL six-gate — at which point checks (2)+(3) alone hold the loop open again. A future roller MUST NOT read "(1) is silent" as "the project is done".
- **(2) prints THREE live "candidates:" sentences** — HTTP/3 (`:175`), xDS (`:185`), Observability (`:193`) ⇒ three families STAY OPEN. **UNCHANGED by row 66** — `combined_validation_context` was never IN a sentence (a §8-tier pickup from phase 65's deferred roster; the phase-64 precedent). **NOTE: the Observability line ALSO carries HISTORICAL `candidates were:` recaps — do not confuse them with the live sentence** (`reference_sentinel_deferred_sentence_live_vs_historical`; the live grep needs "candidates:" adjacent, and there must be exactly ONE live match per family).
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** ⇒ THREE families never opened. Operational-tooling also STAYS OPEN.

All three checks print ⇒ the sentinel does NOT fire. Abundant work remains.

**STATUS AS OF 2026-07-16: the phase-66 BRAINSTORM LANDED and ROADMAP row 66 is `in-progress`.** The subject is `xds-sds-combined-validation-context` — the FOURTH xDS-family row, and the FIRST SDS row that adds **ZERO new symbols to `internal/xds`**. It lifts the `commonTLSContextToConfig` `combined_validation_context is not supported in phase 03` reject (DOWNSTREAM + live-provider + `require_client_certificate: true` ONLY; upstream + QUIC keep the BYTE-IDENTICAL substring, ADR-0080) and merges the SDS-delivered `CertificateValidationContext` phase 65 landed onto the inline `default_validation_context`, installed as the downstream mTLS `ClientCAs`. CVC is a **LISTENER-CONFIG shape, not a SERVED-SECRET shape**: the served `tls.v3.Secret` wire is byte-identical to `0108`'s, so `internal/xds`, `parseValidationSecret`, `FetchInitialValidationContext` and `test/helpers/sdsserver` are all UNTOUCHED.

**⚠️ WHAT THE PHASE-66 BRAINSTORM LEARNED THAT THE SPEC MUST NOT RE-LEARN:**
- **QUOTING IS NOT EXECUTING — the new lesson, and the SPEC's first obligation.** This BRAINSTORM was ADVERSARIALLY VERIFIED before landing and **ELEVEN defects were found in its OWN first draft**. The severe ones ALL trace to ONE omission: **it quoted `proto.Merge`'s documentation instead of RUNNING it.** Running it exposed that (a) `trusted_ca` is a **message**, so Merge **recursively merges** it rather than "singular-overriding" as the proto's own bullet claims — inverting the draft's central reasoning; (b) the default's non-oneof sub-fields (`watched_directory`) **SURVIVE** a dynamic override into a **hybrid `DataSource` neither side authored**; (c) Merge **mutates dst**. **Where a claim is about EXECUTABLE semantics, only execution settles it. The SPEC must RUN `proto.Merge`/`proto.Clone` against real `CertificateValidationContext` messages — do NOT cite them.**
- **A BRAINSTORM is not evidence either.** The project already knows a brief's, a SPEC's, and a PLAN's citations are not evidence (`feedback_brief_citations_not_evidence`). This row proved a BRAINSTORM's are not either. **RE-DERIVE this router AND the BRAINSTORM from source.**
- **A "verbatim" quote can be silently TRUNCATED — and that is worse than a wrong line number.** The draft omitted the proto comment's trailing sentence (*"**The resulting** `CertificateValidationContext` is used to perform certificate validation"* — singular), which largely ANSWERED the very question it then elevated to "THE central probe". **The document manufactured its own central risk by narrowing a quote it labelled verbatim.** Quote to the end of the comment block, always.
- **CITE BY SYMBOL — vindicated MECHANICALLY this session.** The CVC reject is cited as `:229-230` by BOTH ADR-0286 and BRAINSTORM-65; the real line is **`:281`**. Two landed documents carry the same stale number. Line numbers drift every phase; symbols do not.
- **`reference_xds_config_seam_transitive_cycle_guard` is a TYPE-level guard and does NOT cover VALUE-level cycles.** The router's provisional upstream-SDS pick was overturned because `boot.NewSDSProvider` needs `grpcclient.New(cm)` needs the very `*cluster.Manager` being constructed — the import graph is FINE (`cluster` already reaches `xds` transitively), so the memory's check passes while the row is still blocked. Recorded BRAINSTORM §2.1.1.
- **A silence is not a scope decision.** CVC + `require_client_certificate` false/absent is silently IGNORED (no anchor, no error) — inherited from phase 65, whose §8 carried the item forward. The draft DROPPED that carry-forward while claiming a complete cross-reference. **The SPEC must dispose D-COMBVC-REQUIRE-FALSE EXPLICITLY** — scope it out as a named boundary or lift it.
- **`reference_sds_init_fetch_timeout_dial_budget_flake`** — if `TestProvider_FetchInitialCertificate_Timeout` fails under `-race`, it is PRE-EXISTING on master (observed once, 2026-07-16). Read the memory; do NOT reflex-classify it as a phase-66 regression.

**Counts at phase-66 BRAINSTORM-DONE (UNCHANGED — docs-only; RE-VERIFY at session start, `git fetch` first):** fixtures **110** (`ls -d test/fixtures/[0-9]*/ | wc -l`; numeric tail `0108-xds-sds-validation-context`) · fuzzers **55** (`grep -rn '^func Fuzz' --include='*.go' internal/ | wc -l`) · DECISIONS tail **ADR-0286** (next-free **ADR-0287**) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** (docs-verified against `BEHAVIOR_CONTRACT.md`; there is NO mechanical counting command) · go.mod modules **2** (the phase-61.2 lineage figure: `quic-go` direct + `qpack` indirect — **NOT a repo total**; the single `go.mod` requires 67 modules).

==============================================

## What THIS session does (the phase-66 SPEC)

Perform the **SPEC** stage (lifecycle-state 1 → 2) for **phase 66**. Docs-only (NO production `.go`). Fresh worktree off master, branch/worktree per `feedback_git_worktrees` (the phase-65 lineage is the naming precedent). The deliverable is `docs/envoy-go/phases/66-xds-sds-combined-validation-context/SPEC.md`. **Row 66 STAYS `in-progress`** (the IMPL flips it `done` at its six-gate, ADR-0106). **ADR-0287's §Context drafts at THIS SPEC** per ADR-0044; §Decision/§Consequences land at the IMPL.

**The subject is SETTLED — do NOT re-pick.** The BRAINSTORM self-picked, recorded its rejected alternatives with sizing, and overturned the router's prior provisional pick on evidence. The SPEC's job is to DISPOSE the open questions EMPIRICALLY, not to revisit the subject.

### The SPEC's obligations, in priority order

1. **RUN `proto.Merge` / `proto.Clone`, do NOT cite them (D-COMBVC-HYBRID-DS, D-COMBVC-CLONE).** Write a throwaway Go probe against real `tlsv3.CertificateValidationContext` messages and PIN, from OBSERVED output: the recursive-message-merge behavior of `trusted_ca` (`*core.v3.DataSource`); whether the `specifier` oneof replaces; which non-oneof sub-fields SURVIVE (`watched_directory` is the known one); that `Merge` mutates `dst`; the required `proto.Clone` guard + operand order. **This is the single highest-value thing the SPEC does, and the BRAINSTORM's own worst defect was skipping it.**
2. **LIVE-PROBE the reference for the hybrid-`DataSource` question (D-COMBVC-HYBRID-DS).** `envoyproxy/envoy:contrib-v1.37.2`, **FRESH container per arm** (`reference_probe_fresh_container_per_arm`). Does the reference honor a `watched_directory` that arrived only via the DEFAULT half of a CVC whose dynamic half overrode the specifier? `watched_directory` is inert in envoy-go and LIVE in the reference (rotation) — if it is honored, that is a real cross-side divergence needing honoring, rejection, or a NAMED DEPARTURE.
3. **Dispose D-COMBVC-REQUIRE-FALSE EXPLICITLY.** Not by silence. See above.
4. **Dispose the rest:** D-COMBVC-MERGERULES (only 1 of 3 rules reachable, and NOT the one the proto names — a NAMED COVERAGE BOUNDARY) · -REJECT-REPOINT (the four inline sub-field rejects are BYPASSED under the CVC oneof; each needs its OWN test) · -PRESCAN (the third arm + the optional-inner nil-check, no phase-65 analogue) · -EMPTY-DYNAMIC · -PUREINLINE · -OVERRIDE-OBSERVABLE · -STRUCTURAL (MANDATORY — and its necessity must be SHOWN, not asserted) · -UPSTREAM-DEAD (RE-DERIVE; phase 65 shipped this claim wrong into a code comment) · -SERVED-SUBFIELDS · -REFMERGE (**DEMOTED** — a cheap confirmation, NOT a gate) · -FETCHTIMEOUT · -SINGLESLOT · -FIXTURE · -STATS · -FUZZSEED · -SPLIT · -DOCSHAPE. All enumerated in BRAINSTORM §10, ranked by blast radius.
5. **RE-DERIVE the §6 edit-site roster** from source (`feedback_brief_citations_not_evidence`) — and **prefer citing by SYMBOL**.

**Honor throughout:**
- **Lifecycle = one stage per session.** This session: the SPEC ONLY (no PLAN, no code). After it lands: roll to the phase-66 PLAN.
- **Counts are UNCHANGED at a SPEC** (docs-only). Re-verify them at session start and state them; do not copy them from this router without checking (`git fetch` first).
- **Process anchors:** ADR-0044 (the §Context draft lands at the SPEC) · ADR-0045 (single-flat-row bias; escape-valve armable — the BRAINSTORM anticipates ~6–9 tasks and NO split) · ADR-0106 (a row flips `done` only at its IMPL six-gate) · ADR-0080 (envoy-go-strict distinct-substring rejects) · ADR-0280 (the boot-FAIL-on-fetch-timeout DEPARTURE — anticipated to extend unchanged; CONFIRM, do not assume) · ADR-0286 (phase 65 — **note its "CVC" means `CertificateValidationContext`, NOT `CombinedValidationContext`**; its "SHARED gap" sentence does NOT license this row) · `reference_roadmap_split_phase_row_done`.
- **The D-tag family is `D-COMBVC-*`, deliberately NOT `D-CVC-*`** — ADR-0286 already binds "CVC" to `CertificateValidationContext` (`reference_spec_drafted_identifier_collision_check` applied to PROSE). Do not conflate the two senses.
- **The deferred sentence is NOT narrowed by this row, at ANY stage** — `combined_validation_context` was never in one. Do not fabricate a narrow.
- **`next-prompt.txt` IS TRACKED** (`reference_next_prompt_tracked_despite_gitignore`) — a `.gitignore` entry does not apply to an already-tracked file. Edit it inside the stage worktree and fold it into the stage squash; locate commits by SUBJECT (`git log --grep`), NEVER by position ("the tip's PARENT" is not a general rule).
- **Worktree discipline** (`feedback_git_worktrees`, `feedback_subagent_worktree_detach`, `feedback_subagent_worktree_path_targeting`) — pin the canonical worktree root; the controller verifies the MAIN checkout stays clean; on a deliberate break restore with `git restore` only (no checkout-sha, no `--amend`, no detached HEAD).
- **Run an adversarial verification pass before landing.** It found ELEVEN defects in this BRAINSTORM's first draft, including the one that would have mis-shaped the whole row. Ask a verifier to RE-DERIVE independently and default to REFUTED — do NOT ask it to confirm your citations, and do NOT hand it your line numbers.

**ADR reservation ledger (provisional; each finalized at its IMPL in landing order):** ADR-0283 = phase-62 tracing-custom-tags-request-header (LANDED) · ADR-0284 = phase-63 tracing-custom-tags-environment (LANDED) · ADR-0285 = phase-64 tracing-max-path-tag-length (LANDED) · ADR-0286 = phase-65 xds-sds-validation-context (LANDED) · **ADR-0287 = phase-66 xds-sds-combined-validation-context** (§Context drafts at THIS SPEC per ADR-0044; §Decision/§Consequences at the IMPL) · next-free unreserved: **ADR-0288**.

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-66 SPEC is next; the subject is SETTLED).
2. `docs/envoy-go/phases/66-xds-sds-combined-validation-context/BRAINSTORM.md` — **the subject, the envelope, the D-COMBVC-* roster (§10, ranked by blast radius), the self-pick + rejected alternatives (§2.1), the upstream-SDS overturn (§2.1.1), and §11's "quoting is not executing" lesson.** The single most important read.
3. `docs/envoy-go/STATE.md` — the active-phase header (`phase 66 BRAINSTORM done`; NEXT = the phase-66 SPEC) + the prior-phase lineage.
4. `docs/envoy-go/ROADMAP.md` — the §Schema invariant + row 66 + the xDS family section (rows 60/60.1/60.2/65/66 — the SDS precedents) + the THREE live deferred sentences.
5. `docs/envoy-go/phases/65-xds-sds-validation-context/SPEC.md` — **the FORMAT precedent** (the immediately-prior SPEC; how it structured its live-probe dispositions).
6. `docs/envoy-go/phases/65-xds-sds-validation-context/PROGRESS.md` — the liveness-break log (incl. the breaks that did NOT fire), the `-race` finding, the `structuralCheck` trap DEMONSTRATION. The cautionary tale.
7. `docs/envoy-go/DECISIONS.md` — ADR-0286 (phase 65) + ADR-0280 (SDS wiring + the boot-FAIL departure) + ADR-0278 (the SDS substrate) + ADR-0147 (static-mTLS `ClientCAs`) + the ADR-0044/0045/0106/0080 process anchors.
8. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the xDS/SDS section (CONSUMED vs REJECTED today) + the recorded coverage boundaries.
9. `test/fixtures/0108-xds-sds-validation-context/` — the ~95%-reusable driver (PKI, `mtlsEcho`, `structuralCheck`, `normalizeTLSErr`, per-side SDS servers).
10. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the differential-harness `reference_*` traps + **`reference_xds_config_seam_transitive_cycle_guard`** (note: TYPE-level only — see §2.1.1) + **`reference_plan_break_instructions_dont_compile`** + **`reference_sds_init_fetch_timeout_dial_budget_flake`** + **`reference_vacuous_break_receiver_normalizes`** (DEMONSTRATED at phase 65 on `0108` — the CVC fixture inherits the trap).
