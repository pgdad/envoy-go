START a fresh session for envoy-go. **The phase-43.2a (H2 multiplex pool substrate) IMPL is COMPLETE, fully reviewed (per-task spec+quality + a final holistic review, all APPROVED), the six-gate GREEN + the full 81-dir differential GREEN, and LANDED on master, and ROW 43 (`connection-pooling`) is `in-progress` with LEG 43.1 DONE + LEG 43.2a DONE + LEG 43.2b PENDING.** Phase 43 is the FIFTH-and-FINAL Upstream-robustness-family row; 43.2 (the H2 multiplex pool, SUPERSEDING ADR-0056) was SPLIT at SPEC into **43.2a** (the core multiplex pool substrate — JUST LANDED) and **43.2b** (graceful GOAWAY rotation + reset/goaway stats + drain lifecycle — the remaining leg, ADR-0254). The next step is the **43.2b BRAINSTORM** (`superpowers:brainstorming`). Confirm the latest master is the phase-43.2a IMPL completion (squash) commit via `git log --oneline -3` (a NEW sha — do NOT assume a literal value). Counts at tip: stat surface **1185** (+2 for an H2 cluster: `upstream_cx_http2_total` + `http2.streams_active`; non-H2 clusters stay 1183) / fixtures **81** (tail `0079-h2-multiplex-pool`) / fuzzers **42** (documented; actual `^func Fuzz`=43 per `reference_fuzzer_count_docs_drift` — 43.2a added none) / BackendKind tail **37** (`H2HoldResponder`) / DECISIONS tail **ADR-0253**, next-free **ADR-0254**.

## NEXT STEP — the 43.2b BRAINSTORM

Brainstorm phase **43.2b** (`superpowers:brainstorming` → spec-document-reviewer; a docs commit on master) — the graceful GOAWAY-driven connection rotation leg that ADR-0253 §Consequences DEFERRED. The 43.2a pool ships MINIMAL liveness only (`ClientConn.Closed()`-skip + evict-on-RoundTrip-error); 43.2b adds the Envoy-faithful drain lifecycle. Anticipated envelope (re-scope at the brainstorm): (1) **lazy GOAWAY rotation** — a pooled conn that received GOAWAY (the codec's `goawayCh` signal) is marked draining, skipped by the admission scan, and closed when its last in-flight stream finishes (the `releaseStream`→`evictH2ConnLocked` seam already exists for the closed-conn case; 43.2b extends it to the draining case); a replacement opens on-demand. (2) The **`http2.*` reset/goaway stat subtree** that 43.2a left unregistered — `http2.rx_reset`/`tx_reset`/`goaway_sent`/`stream_refused_errors`/… (the exact set + the `+N` pinned at the 43.2b SPEC via a live `contrib-v1.37.2` probe per `reference_docker_probe_bridge_network`). (3) Possibly **`min(local,peer)` proactive enforcement** (AMEND-H2-5 deferred — avoid the peer `REFUSED_STREAM` by capping at the stricter of local/peer `max_concurrent_streams`) — a brainstorm scoping call. (4) Possibly **31-bit stream-ID-exhaustion retirement** (the M-12 follow-up noted in Task 11 — a long-lived conn eventually exhausts the stream-ID space and should retire via GOAWAY). The 43.2b differential needs a **GOAWAY-emitting H2 backend** (extend the Task-8 `H2HoldResponder` BackendKind 37, or a new kind). Anchors **ADR-0254**. When 43.2b lands, **row 43 flips `done`** and the Upstream-robustness family (phases 39–43) CLOSES (`reference_roadmap_split_phase_row_done`).

## What 43.2a delivered (the substrate 43.2b builds on — ADR-0253, SUPERSEDES ADR-0056)

A per-endpoint H2 multiplex connection pool (`internal/cluster/h2pool.go`) over the 43.1 permit substrate (ADR-0252). The as-built four-tier admission ladder in `AcquireH2Stream(ctx)`: **(A) stream-HIT** (ride a live conn with `inFlight < C`) → **(B) connecting-HIT/attach** (attach to an establishing conn's promised slot — the Task-9.5 connect-time dial COALESCING that makes K concurrent acquires open exactly `ceil(K/C)` conns, not one-per-stream) → **(C) MISS dial-as-initiator** (`tryAcquireConnSlot` reserves a 43.1 permit, dial the picked ep) → **(D) PEND** in the SEPARATE stream-aware queue (woken on stream-free via `h2PromoteLocked` OR conn-close; bounded by `max_pending_requests`; queue-full → `errConnPoolOverflow` → 503). A SINGLE ctx-aware `c.lb.Pick(hashKeyFrom(ctx),…,subsetMatchFrom(ctx),…)` (the Task-7.5 fix) is threaded through the whole ladder so conns are keyed+attributed to the dialed endpoint and hash/subset/source_ip affinity is honored. The per-conn stream budget is the cluster's OWN `http2_protocol_options.max_concurrent_streams` (the LOCAL cap C; AMEND-H2-1, the live-probe reversal). Two conserved resources (the 43.1 permit + the LB-pick release) each fire EXACTLY ONCE per acquire — traced on every path; `-race -count=20` clean. The router rewire (`router_h2.go`) moved BOTH H2 sites off `DialH2`+`defer cc.Close()` onto `AcquireH2Stream`+`release()`+evict-on-error; the legacy `doH2` site was proven dead + REMOVED (completing the ADR-0056 supersession in code). **The H2 pool engages ONLY for an H2 (TLS+ALPN-h2) DOWNSTREAM listener** — envoy-go picks the router action by the downstream codec; there is NO H1-down→H2-up bridge (a SPEC §8.1 correction found+recorded at IMPL). Stats: `upstream_cx_http2_total` + `http2.streams_active` (useH2-gated; NO `_http2_active`; 1183→1185). Differential `0079-h2-multiplex-pool` (cross-side EXACT `ceil(K/C)` conns + `http2.streams_active==K` both sides + an overflow prong → downstream 503 + `upstream_rq_pending_overflow`; the `H2HoldResponder` BackendKind 37 h2c hold backend with `SETTINGS_MAX_CONCURRENT_STREAMS=1000` so the LOCAL cap binds). 3 deliberate-break proofs + 20/20 flake gate + `-race` clean.

## Honor throughout (the discipline)

- **Lifecycle = one stage per session.** A BRAINSTORM is a docs commit DIRECT on master (no worktree needed for docs-only) — but `feedback_git_worktrees` applies to the later 43.2b PLAN/IMPL stages · `feedback_subagents_no_push` (subagents commit LOCALLY; the controller squashes + pushes at stage-close) · `feedback_push_to_origin` (push after a clean commit on master with tests green) · `feedback_execution_style` (subagent-driven execution for IMPL) · `feedback_pertask_gofmt_lint` (gofmt -l + golangci-lint per task).
- **Process anchors:** ADR-0106 + `reference_roadmap_split_phase_row_done` (row 43 flips `done` only when ALL legs land — 43.1 + 43.2a done, 43.2b is the LAST); ADR-0080 (strict-reject); ADR-0044 (the ADR §Decision/§Consequences land at IMPL; §Context drafts at SPEC); `reference_docker_probe_bridge_network` (the live `contrib-v1.37.2` probe for the 43.2b GOAWAY-stat pins) + `reference_concurrency_differential_release_barrier` + `reference_concurrent_attempt_downstream_class_assertion` (the differential discipline); `reference_differential_run_selector` + `reference_differential_break_protocol_count1` + `reference_differential_fullsuite_startup_flake` (a `subject ready: EOF` is a transient — isolate-re-run, NOT a regression; bit the 43.2a IMPL full-suite run); `reference_fuzzer_count_docs_drift` (the documented 42 vs actual 43 — reconcile when a phase next touches the fuzzer count).
- **The H2 pool memory:** `reference_h2_pool_local_cap_driven` (the local-cap-driven `ceil(K/C)` finding — the load-bearing 43.2a/43.2b architecture); `reference_max_connections_soft_breaker` (the 43.1 contrast — 43.2a's stream/conn counts are cross-side EXACT, the OPPOSITE of 43.1's soft-breaker robust prong).

## Read first (in order)

1. THIS FILE — the cold-start router (43.2a IMPL is DONE + LANDED; row 43 is `in-progress` with legs 43.1+43.2a done, 43.2b pending; the next step is the 43.2b BRAINSTORM).
2. `docs/envoy-go/STATE.md` — active-phase `phase 43.2a (h2-connection-pool) IMPL done`; the count figures (1185 / 81 / 42 / 37 / ADR-0253, next-free ADR-0254).
3. `docs/envoy-go/DECISIONS.md` — **ADR-0253** (the 43.2a architecture as-built, the substrate 43.2b extends) + **ADR-0056** (now SUPERSEDED by ADR-0253); next-free **ADR-0254** (the 43.2b GOAWAY-rotation anchor).
4. `docs/envoy-go/phases/43.2-h2-connection-pool/SPEC.md` (§1.1 AMEND-H2-4 the 43.2a/43.2b split; AMEND-H2-2 the deferred `http2.*` reset/goaway subtree; AMEND-H2-5 the deferred peer-min) + `PROGRESS.md` (the full 43.2a task record incl. the mid-IMPL 7.5 + 9.5 fixes + the M-12/Task-11 stream-ID-exhaustion note).
5. `internal/cluster/h2pool.go` (the as-built pool — the `h2Connecting` tier, `AcquireH2Stream`, `releaseStream`/`makeRelease`, `evictH2ConnLocked` [the eviction seam 43.2b's draining-close extends]) + `internal/filter/hcm/h2/client.go` (the `ClientConn` — `goawayCh` [the GOAWAY signal 43.2b consumes], `Closed()`, the `Close`→GOAWAY-emit path; `nextStreamID` [the 31-bit exhaustion note]).
6. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the `### Cluster — HTTP/2 upstream multiplex connection pool` subsection (43.2a) the 43.2b leg extends.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `reference_h2_pool_local_cap_driven`, `reference_roadmap_split_phase_row_done`, `reference_docker_probe_bridge_network`, `reference_differential_fullsuite_startup_flake`, `reference_concurrency_differential_release_barrier`, `reference_concurrent_attempt_downstream_class_assertion`, `reference_differential_run_selector`, `reference_differential_break_protocol_count1`, `reference_fuzzer_count_docs_drift`, `feedback_git_worktrees`, `feedback_subagents_no_push`, `feedback_push_to_origin`, `feedback_execution_style`, `feedback_pertask_gofmt_lint`).
