=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**Check (1) is NON-SILENT** (row 57 `stats-sink-graphite` is `in-progress`; check (1) prints `NOT DONE: row 57`). **The sentinel does NOT fire** — an open row alone blocks it, and (2)+(3) still carry work besides. Do NOT create `stop`.

**SENTINEL RE-CHECKED 2026-07-11 (post-phase-57-PLAN) — it does NOT fire. Do NOT create `stop`.** Evidence:
- (1) prints `NOT DONE: row 57` ⇒ phase 57 is OPEN (the IMPL is the next stage). Blocks `stop` outright.
- (2) prints the Observability family's deferred candidates ⇒ the **Observability family STAYS OPEN** (OTLP-metrics sink + tracing `custom_tags`/`spawn_upstream_span`/`http_service`/force-trace; the IMPL ADDS the dog_statsd explicit-`max_bytes_per_datagram: 0` parity candidate per SPEC-57 §2).
- (3) prints `NEVER OPENED: HTTP/3`, `gRPC`, `xDS`, `Runtime`, `WASM` ⇒ FIVE families never opened; the **Operational-tooling family** also STAYS OPEN.

**STATUS AS OF 2026-07-11: phase 57 (`stats-sink-graphite`) is OPEN — row 57 is `in-progress`; the BRAINSTORM, the SPEC, and the PLAN have LANDED.** The PLAN session resolved all three SPEC §12 D-questions: **D-GR-BATCHSHARE → GENERALIZE** (hoist `appendLine`/`flush` into shared `appendBatchLine`/`flushBatch` free functions in `internal/statssink/udp.go`; zero direct test call sites verified ⇒ dog_statsd tests stay byte-untouched, doubling as the refactor's regression proof); **D-GR-SPLIT → NO sub-split** (11 tasks, margin 4 under the ADR-0045 ceiling); **break-(b) → the SPEC §8.1 shape is VACUOUS** (`statsdrecv.ingestLine` parses a `|#`-formatted line into the IDENTICAL tag buckets — a PLAN-time re-derivation catch per `feedback_brief_citations_not_evidence`) and is REPLACED by (b1) tag-drop + (b2) an UnparsedCount-ISOLATING extra-malformed-line break, plus (c1)/(c2) isolating the two batching proofs and non-break (d) order-reversal-stays-green. `docs/envoy-go/phases/57-stats-sink-graphite/PLAN.md` is the 11-task TDD spine; `PROGRESS.md` is scaffolded (baselines fill at IMPL Task 1).

**Current counts (verified at the phase-57 PLAN; UNCHANGED — docs-only):** fixtures **102** (tail `0100-http-tap-bodies`) · fuzzers **53** · DECISIONS tail **ADR-0274** (next-free **ADR-0275**) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** · new Go packages **0** · new go.mod modules **0**. Anticipated at IMPL exit: fixtures **103** (`0101-stats-sink-graphite`) · fuzzers **54** · DECISIONS tail **ADR-0275** · stat surface **1201 (+0)** · BackendKind **38 (+0)** · **+0 modules/packages**.

**Locating the stage squash (a router cannot name its own sha — writing the sha changes it).** Identify it by SUBJECT, never by sha and never by position:
```sh
git log --oneline --grep='^phase 57 (stats-sink-graphite) PLAN' -1
```
This stage folded its own `next-prompt.txt` roll INTO the squash (per `reference_next_prompt_tracked_despite_gitignore`). The squash is NOT necessarily the master tip: parallel-workstream commits can land on top of it. ALWAYS `git fetch` + re-verify counts against the ACTUAL current master tip at the start of the IMPL session.

**THE NEXT ACTION: the phase-57 IMPL (lifecycle-state 3 → done) — execute PLAN.md task-by-task, subagent-driven.**

==============================================

## What THIS session does (the phase-57 IMPL, lifecycle-state 3 → done)

Phase 57 is OPEN (row 57 `in-progress`; BRAINSTORM + SPEC + PLAN landed). This session performs the **IMPL**: execute `docs/envoy-go/phases/57-stats-sink-graphite/PLAN.md`'s 11 tasks. Fresh worktree per `feedback_git_worktrees` (e.g. `.worktrees/phase-57-impl`, branch `phase-57-stats-sink-graphite-impl`). **Skill gates: `superpowers:subagent-driven-development` (per `feedback_execution_style`) + `superpowers:test-driven-development` per task + `superpowers:verification-before-completion` at close.**

1. **Read PLAN.md FIRST** — the Global Constraints, the Orientation brief (the verified `file:line` map), the D-question resolutions (BATCHSHARE generalize / no-split / the redesigned break roster), then the 11 tasks. PROGRESS.md's baseline block fills at Task 1.
2. **Subagent execution discipline** (the PLAN's header block): per-task briefs pin the CANONICAL WORKTREE ROOT + worktree-relative paths (`feedback_subagent_worktree_path_targeting`); every brief carries the GIT-HYGIENE block (`git restore` only — no `checkout <sha>`/`--amend`; `feedback_subagent_worktree_detach`); subagents commit LOCALLY only (`feedback_subagents_no_push`, `feedback_subagent_autocommit_claudemd` — don't fight the auto-commit, VERIFY each one); per-task `gofmt -l` + `golangci-lint` on touched packages (`feedback_pertask_gofmt_lint`); reviewers RE-DERIVE the brief's citations from source (`feedback_brief_citations_not_evidence`).
3. **Task 9 (deliberate breaks) is CONTROLLER-EXECUTED, never delegated** — five breaks + one non-break, each confirmed firing its OWN assertion (`reference_differential_break_protocol_count1` `-count=1`; `reference_deliberate_break_wrong_assertion`); 20/20 flake gate; FULL-package `-race` (`reference_full_suite_race_after_background_mutator`).
4. **The six-gate at Task 10**, then Task 11 lands ADR-0275 §Decision/§Consequences (ADR-0044), BEHAVIOR_CONTRACT, STATE.md, ROADMAP (**row 57 flips `done`** — ADR-0106, sole leg; the family deferred-list rolls graphite OUT and the dog_statsd explicit-zero parity candidate IN), PROGRESS close, and ROLLS THIS FILE FORWARD (the next router decision: the Observability family and Operational-tooling stay open, five families never opened — the sentinel will NOT fire; do NOT create `stop`).
5. **Land the IMPL** (the controller squashes the worktree branch onto master + pushes per `feedback_push_to_origin`; the router roll folds into the squash).

## Honor throughout (the discipline, unchanged)
- **Lifecycle = one stage per session.** This session: the phase-57 IMPL.
- **Differential traps:** `reference_differential_run_selector` (`-run 'TestDifferential/0101'`, NEVER bare) · `reference_differential_break_protocol_count1` · `reference_deliberate_break_wrong_assertion` · `reference_fatalf_makes_assertions_unreachable` · `reference_delta_sink_differential_stability_barrier` · `reference_periodic_sink_differential_two_receivers` · `reference_streaming_sink_differential_framing` · `reference_stats_sink_emits_used_only` · `reference_admin_interface_wire_name_collision` · `reference_membership_total_vs_healthy_gauge` · `reference_framing_break_needs_unparsed_counter` · `reference_differential_backendcount_min_one` · `reference_differential_fixture_dispatch_constraint` · `reference_differential_asserter_dispatch` · `reference_fixture_workload_constant_desync` (numReq=7 + the ~172-char cluster name + cap 200 are COUPLED constants — named once, Task 8) · `reference_differential_fullsuite_startup_flake` · `reference_host_gateway_ip_docker_desktop` · `reference_dogstatsd_tag_order_unsorted` (graphite sibling: cross-side tag assertions SET-based ONLY).
- **`next-prompt.txt` IS TRACKED** — edit it inside the stage worktree, fold into the squash; locate commits by SUBJECT (`git log --grep`), never by position.
- **Process anchors:** ADR-0044 (ADR-0275 lands here) · ADR-0045 (escape-valve unconsumed) · ADR-0080 · ADR-0106 (row 57 flips `done` here). Next-free ADR is **ADR-0275**.

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-57 IMPL is next).
2. `docs/envoy-go/phases/57-stats-sink-graphite/PLAN.md` — the 11-task spine (Global Constraints · Orientation · D-question resolutions · the redesigned Task-9 break roster).
3. `docs/envoy-go/phases/57-stats-sink-graphite/SPEC.md` — the pinned design (§3), the empirical pins (§11), the differential envelope (§8).
4. `docs/envoy-go/phases/57-stats-sink-graphite/PROGRESS.md` — the scaffold (baselines fill at Task 1; break log fills at Task 9).
5. The reuse substrate — `internal/statssink/{dogstatsd.go,delta.go,udp.go}` + `internal/bootstrap/bootstrap.go` + `cmd/envoy-go/main.go` + `test/helpers/statsdrecv/statsdrecv.go` + `test/fixtures/0094-stats-sink-dogstatsd-batching/` (the 0101 clone base).
6. `docs/envoy-go/STATE.md` — the active-phase header (phase 57 PLAN done).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the `reference_*` traps.
