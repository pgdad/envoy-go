=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, the active phase is fully landed, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action. (The sentinel does NOT apply right now — see below.)

**STATUS AS OF 2026-07-06: phase 52 (`load-balancer-locality-weighted`) IMPL is LANDED (row 52 flips `done`). NO phase is currently chartered. This is the "none chartered, awaiting a human pick" shape — NOT the termination sentinel.**

- The phase-52 IMPL landed (squashed + pushed to origin/master): a new `internal/cluster/locality.go` `localityWeightedLB` wrapper `loadBalancer` (the phase-38.1 `subsetLB` structural precedent, generalized to a build-time-fixed locality identity) partitions a cluster's endpoints by `LocalityLbEndpoints.locality` identity and weighted-random-draws a locality per `Pick`, scaled by that locality's live healthy-host fraction via the confirmed `effective_weight = configured_weight × min(1, (overprovisioning_factor/100) × healthy_fraction)` formula — delegating to a per-locality child `loadBalancer` built by the EXISTING `buildLeafLB` factory, with a `flat` all-endpoints fallback for the panic-mode and zero-total-weight paths. ZERO new `Pick` parameters (D-LW7). `Endpoint` grows `Locality`/`LocalityWeight`; `manager.go`'s `buildCluster` gains a second wrap-after-switch site gated by two new envoy-go-strict rejects (`zone_aware_lb_config` unsupported; `lb_subset_config` combined with `locality_weighted_lb_config` rejected). Two design upgrades landed beyond the SPEC's own leaning (D-LW-HEALTHALLOC's documented health-registry-widening side effect; D-LW-OPF0's absent-vs-explicit-zero `overprovisioning_factor` distinction); a child-local-panic coverage boundary documented and tested, not fixed. The new `test/fixtures/0095-lb-locality-weighted` cross-side differential (2 localities × 5 hosts, 2:1 weight) proves both a static weight-ratio band arm and a health-degradation shift band arm, each 20/20 flake-free and `-race` clean, via two controller-reperformed deliberate breaks. **ANCHORS ADR-0269** (§Decision/§Consequences landed). Counts at IMPL exit: stat surface **1200** (+0) / fixtures **97** (`0095`) / fuzzers **52** (unchanged) / BackendKind **38** (unchanged) / DECISIONS tail **ADR-0269** (next-free **ADR-0270**); ZERO new packages, ZERO new go.mod modules. **ROW 52 (`load-balancer-locality-weighted`) FLIPS `done`** (the sole leg, ADR-0106; NO parent rollup). The controller independently re-ran the full `go test ./... -count=1` suite, the full 97-dir differential (both plain `-count=1` and `-race -count=1`), and both deliberate breaks on the frozen HEAD before squashing — all green (several transient full-suite-load flakes were observed across the two differential runs — `0018-http-rbac`, `0027-http-lua-full-bridge`, `0028-http-lua-multi-script-and-per-route`, `0036-http-wasm-body-and-advanced`, `0048-zookeeper-responses` — each isolate-re-run-confirmed PASS in isolation, matching the known `reference_differential_fullsuite_startup_flake` pattern, none a phase-52 regression; none of them touch `internal/cluster`).
- **The Load-balancing family STAYS OPEN** with 2 remaining deferred-but-not-yet-chartered candidates (unchanged since phase 38/52 opened): **priority load balancing** (`LocalityLbEndpoints.priority`/`proximity`-based selection) and **panic thresholds** as an independently-configurable/observable construct beyond the existing `healthy_panic_threshold` gate reused as-is by every LB phase so far. Neither is pre-chartered — a human picks.
- STATE.md/ROADMAP.md updated and pushed (row 52 `done`; the new active-phase entry ANCHORS ADR-0269). PROGRESS.md for phase 52 closed out with final re-verified counts.

=> The NEXT action is **NOT a new IMPL, NOT the termination sentinel.** A human must pick the next Load-balancing candidate (priority load balancing / panic thresholds) — or open an entirely different family/candidate — to charter the next BRAINSTORM. Until that pick is made, there is no active phase to advance — this router should be re-invoked once a human has chosen, at which point the chosen candidate's BRAINSTORM becomes "the NEXT action."

==============================================

## Deferred, not-yet-chartered Load-balancing candidates (a human picks ONE to open the next BRAINSTORM)

These are the same 2 candidates left over after phase 38 (subset LB) and now also after phase 52 (locality-weighted LB):

- **Priority load balancing** — `LocalityLbEndpoints.priority`/`HostSet` priority-tier selection (overflow/failover across priority levels), deferred at both phase 38 and phase 52 (`LocalityLbEndpoints.priority`/`proximity` explicitly out of scope at phase 52's BRAINSTORM).
- **Panic thresholds as an independent construct** — `healthy_panic_threshold` is currently reused UNCHANGED and UNEXTENDED by every LB phase (round_robin/least_request/random/ring_hash/maglev/subset/locality-weighted) via the existing `clusterHealth.inPanic` gate; a dedicated row would explore making it independently configurable/observable/testable in its own right (e.g. `Cluster.CommonLbConfig.healthy_panic_threshold` override semantics beyond the current default-gate reuse).

None of these is pre-chartered. **Do NOT invent/charter one yourself** — this is a human decision. If a human has NOT picked one when this router is next read, ask which candidate (or a different one entirely, from any family) to charter, then open its BRAINSTORM per the project's standard lifecycle (`superpowers:brainstorming` first).

## Honor throughout (the discipline, unchanged for whichever phase comes next)
- **Lifecycle = one stage per session** (BRAINSTORM → SPEC → PLAN → IMPL).
- **Subagent-driven execution** (`feedback_execution_style`) in a **fresh worktree** (`feedback_git_worktrees`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit, re-runs the full suite on the frozen HEAD, does any deliberate-break verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). Per-task gates: `gofmt -l` + `golangci-lint` + `go vet` + `go build ./...` (`feedback_pertask_gofmt_lint`).
- **Process anchors:** ADR-0106 (per-leg rows) · ADR-0044 (ADR §Decision+§Consequences land at IMPL) · ADR-0045 (sub-split soft gate) · next-free **ADR-0270**.
- Re-verify baselines fresh at cold-start — do NOT trust any count in this file without re-running the baseline commands (`ls -d test/fixtures/[0-9][0-9][0-9][0-9]* | wc -l`, `grep -rh '^func Fuzz' --include='*.go' . | wc -l`, etc.) against current HEAD.

## Read first (in order), once a human has picked a candidate
1. THIS FILE — the cold-start router (none chartered; awaiting a human pick).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 52 IMPL done`) + the full history.
3. `docs/envoy-go/ROADMAP.md` — confirm row 52 `done`, read the Load-balancing family note for the 2 remaining deferred candidates.
4. `docs/envoy-go/phases/52-load-balancer-locality-weighted/` (BRAINSTORM/SPEC/PLAN/PROGRESS) — the most recent landed phase, useful as a shape template for whichever candidate is picked next, especially if it's **priority load balancing** (closely related: also consumes `LocalityLbEndpoints` fields, also a per-cluster wrapper-shaped construct).
5. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — especially the `reference_*` entries tagged for load-balancer/health-check/differential-band work, which will likely carry forward to the next LB-shaped candidate.
