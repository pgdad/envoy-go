=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

✅ **PHASE 99 (`chain-match-sni-longest-suffix`) IS OPEN — ITS `SPEC.md` LANDED 2026-09-21 AND ROW 99 STILL READS `in-progress` AT `ROADMAP.md:161`. LIFECYCLE-STATE IS `2`.** ⇒ **THIS SESSION WRITES PHASE 99's `PLAN.md`** (BOOTSTRAP §5 state 2 -> 3, with the §6.1 split gate). ⚠️ **THERE IS NO SUBJECT TO PICK AND NO SHAPE TO CHOOSE — the BRAINSTORM picked the subject and the SPEC chose the repair shape BY MEASURING THE REFERENCE; do NOT re-open either.** The sentinel does NOT fire: check (1) reads ONE line (`NOT DONE: row 99`) and check (2) still prints SIX (`:209 :215 :221 :231 :237 :245`). **The phase-99 SPEC (`SPEC.md`, **697** lines) is your INPUT; its §15 is the list of EIGHT items the PLAN owes. `BRAINSTORM.md` is EVIDENCE, and its `22 0` prototype is REFUTED (`SPEC.md` §0.1) — do not plan from it.**

🔴 **YOUR STAGE IS THE PLAN. IT DISCHARGES `SPEC.md` §15 AS A TDD SPINE, EVALUATES BOOTSTRAP §6.1's SPLIT GATE WITH A COMMAND, WRITES NO PRODUCTION CODE AND RUNS NONE OF THE SIX GATES.** ⚠️ **A PLAN NEITHER ADDS NOR FLIPS A ROW, so every NC shape below should read UNCHANGED from the SPEC close — MEASURE IT, do not inherit it.** ⚠️ **THE SPEC's §5.2 `O1` ROW IS *PREDICTED*, NOT MEASURED — and §11's NC roster has rows the inverted patch CANNOT score; plan the un-fixed tip FIRST and score PER ARM.** ⚠️ **AND CHECK THIS FILE FOR A SPENT STAGE'S IMPERATIVE BEFORE OBEYING ANY LINE IN IT (method note 68).**

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. Parses FIELDS instead of
#     matching a pattern, and asserts the row DENOMINATOR so a deleted row is
#     itself an error.
#     ⚠️ want IS 131 and ROADMAP.md IS 249 lines at the phase-99 SPEC close (unmoved).
#     ⚠️ CHECK (1) READS ONE LINE — `NOT DONE: row 99` — because row 99 was ADDED
#     `in-progress` at that close. That ONE line is correct; a SILENT check (1)
#     at a PLAN tip would be a FINDING. NC-A and NC-B below stay MANDATORY.
#     ⚠️ A PLAN neither adds nor flips a row: `want` STAYS 131 at your close.
#     Only the IMPL's FLIP of row 99 takes check (1) back to SILENT.
awk -F'|' -v want=131 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️⚠️ **CHECK (2)'s SIX IS THE ONLY STRUCTURAL BARRIER, AND RIGHT NOW IT IS THE ONLY NON-SILENT CHECK AT ALL** — with row 98 `done`, checks (1) and (3) are both SILENT, so **two of the three sentinel checks are currently indistinguishable from broken ones.** That is not a reason to relax: it is precisely why the NCs are mandatory. **RUN ALL FOUR NCs EVERY SESSION:**
```sh
# NC-A — doctor a row to `in-progress`. ⚠️ READS *TWO* LINES at this tip
#        (`NOT DONE: row 62`, `NOT DONE: row 99`). MEASURED on BOTH sides of the
#        phase-99 ADD: ONE before, TWO after; TWO at the SPEC close. A PLAN should leave it at TWO.
#        NEVER INHERIT AN NC SHAPE ACROSS A ROW CHANGE.
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md with want=131.

# NC-B — the DENOMINATOR, want=130 on the real file. ⚠️ READS *TWO* LINES at
#        this tip — `NOT DONE: row 99` and `GATE FAIL: examined 131 data rows,
#        expected 130`. It read ONE before the phase-99 ADD (at want=129).
#        ⚠️ Re-derive `want` from YOUR tip; a PLAN does not move it.

# NC-C — the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```
⚠️ **AND RUN THE CHECK-(2) POSITIVE CONTROL — AND READ THE COMMAND BEFORE TRUSTING ITS RESULT.** The longer phrase does not CONTAIN the shorter one as a substring, so a control that substitutes only one phrase reports a residual of 5 and reads like a finding. Substitute BOTH and assert the substitution count:
```sh
sed -e 's/deferred candidates:/deferred candidatesXX/g' \
    -e 's/remaining deferred (not-yet-chartered) candidates:/remaining deferred (not-yet-chartered) candidatesXX/g' \
    docs/envoy-go/ROADMAP.md > "$SCRATCH/c2pc.md"
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' "$SCRATCH/c2pc.md" | wc -l  # want 0
grep -o 'candidatesXX' "$SCRATCH/c2pc.md" | wc -l                                                                # want 6 — PROVE it landed
```

⚠️ **WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -c '-family row'` fails (GNU: `grep: amily row: No such file or directory`; ugrep: `option -f: cannot read amily row` — **both rc=2**) and the surrounding arithmetic prints `0`, **which reads exactly like "no change."** **Always pass `--` before the pattern**; with `--` occurrences read **96** and LINES read **68** — ⚠️ **UNMOVED by the phase-98 AND phase-99 row ADDs, measured on both sides of each.**

⚠️ **THE MALFORMED-ROW FIGURE RECONCILES ONLY UNDER AN ESCAPE-AWARE FIELD COUNT — AND THE ESCAPE-AWARE COMMAND ITSELF CAN BE BROKEN.** Malformed rows are **ROW IDs 57** and **69**, at file lines **119** and **131** (**UNMOVED by the row-98 and row-99 ADDs, which land BELOW them**). **Strip `\|` BEFORE splitting.** Row 57 reads escape-aware NF=9; row 69 reads 10; rows 74, 94, 95, 96, 97, 98 and **99** read **8**. ⚠️ **The correct form is `sed 's/\\|//g' F | awk -F'|' '/^\| *[0-9]/ && NF!=8'` with NO file argument to awk** — passing the file makes awk IGNORE STDIN and print the NAIVE count under the escape-aware label.

⚠️ **AND DO NOT SUBSTITUTE A "REASONABLE EQUIVALENT" FOR CHECK (1)'s AWK.** `NF>=8 && $2 ~ /^ *[0-9]+ *$/` once printed **92**, not the true denominator. **Use the form above verbatim.**

**SENTINEL RUN MECHANICALLY AT THE PHASE-99 SPEC CLOSE, BEFORE ANY EDIT AND AGAIN IN THE PUBLISHING TREE, WITH `/usr/bin/grep`.** ACTUAL output, recorded not predicted — **both runs identical, as a stage that touches no `ROADMAP.md` byte must read:**
(1) **ONE** (`NOT DONE: row 99`) · (2) **SIX** at `:209 :215 :221 :231 :237 :245` · (3) **SILENT** · NC-A **TWO** (`NOT DONE: row 62`, `NOT DONE: row 99`; substitution inspected first: `NC LANDED? [ in-progress ]`) · NC-B **TWO** at `want=130` (`NOT DONE: row 99`, `GATE FAIL: examined 131 data rows, expected 130`) · NC-C **FIRED** (residual 0) · NC-D **96 / 68** under `--` · check-(2) positive control **6 substitutions ASSERTED, residual 0** · escape-aware malformed set **exactly {57, 69}** at file lines **119** (NF 9) and **131** (NF 10) · row 99 NF **8** · fixture set **125 = 125**, both `comm` directions EMPTY.
Per-line md5 of the six windows, **trailing newline INCLUDED** (`sed -n 'Np' f | md5sum`, first 12 hex) — **BYTE-IDENTICAL ACROSS SIX CONSECUTIVE STAGES (phase-98 BRAINSTORM through phase-99 SPEC), INCLUDING ACROSS THE phase-99 ADD, WHICH MOVED THE LINES BUT NOT THE BYTES:**
`209 10d7807bf02d` · `215 4a92f7e62fc6` · `221 2a7eb298b9fd` · `231 242e53c6f7a3` · `237 b2680e6f4fbf` · `245 6caa1c3ce0e7`
⇒ **THE SENTINEL DOES NOT FIRE. `stop` WAS EVALUATED AND DELIBERATELY NOT CREATED** (verified absent at the git root and in the stage worktree).
⇒ ⚠️⚠️ **THIS RUN DOES NOT CARRY FORWARD. RUN ALL THREE CHECKS, ALL FOUR NCs AND THE CHECK-(2) POSITIVE CONTROL YOURSELF, AT YOUR OWN TIP.** ⚠️ **THE MARGIN REMAINS ONE.**
⚠️⚠️ **YOUR STAGE IS THE PLAN: IT NEITHER ADDS NOR FLIPS A ROW, so every shape above should read UNCHANGED at your close — `want` 131, `ROADMAP.md` 249, check (1) ONE, NC-A TWO, NC-B TWO at want=130, windows at `:209 … :245`. MEASURE IT; an unchanged shape is a measurement, not an inheritance.** The phase-98 SPEC and PLAN and the phase-99 SPEC measured their shapes UNCHANGED this way.
⚠️ **IF YOU EVER ADD A ROW, COUNT ITS FIELDS UNDER BOTH FORMS BEFORE INSTALLING IT** — an unescaped `|` PASSES check (1) and silently breaks the field count.
⚠️ **THE MARGIN IS STILL ONE.** ⚠️⚠️ **DELETING THE LAST DEFERRED-CANDIDATE LINE ENDS THE PROJECT. DO NOT "TIDY" ONE, AND DO NOT "FIX" THE SIX.** History `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases; **no gate may rest on a ` + ` split** (wrong on 3 of the 6 windows).
⚠️⚠️ **CHECK (2) MATCHES THE WHOLE FILE, NOT ONLY THOSE SIX LINES.** A new or edited row whose summary cell spelled either match phrase would mint a SEVENTH hit and read as a finding. **The phase-98 row's cell was written with this in mind and spells NEITHER phrase — gated before installing.** ⚠️ **NEVER RE-SPELL A SENTINEL MATCH PHRASE INSIDE A SENTINEL WINDOW, AND NEVER SPELL ONE IN A NEW ROW CELL EITHER.**
⚠️ **THE md5 DIGEST IS METHOD-SENSITIVE:** the six match ONLY with the **trailing newline included**. **State the method whenever you quote the digest.**
⚠️ **AUDIT THIS FILE'S OWN MEMORY CITATIONS BEFORE TRUSTING THEM.** Extract every `` `reference_*` ``/`` `feedback_*` ``/`` `topic_*` `` slug and test each against `~/.claude/projects/-home-esa-git-envoy-go/memory/<slug>.md`. A router that names a file which does not exist is the same defect class as a stale line anchor. **All cited slugs resolved at the phase-98 SPEC close.**

⚠️⚠️ **A STALE PRESENT-TENSE CLAIM SITS INSIDE SENTINEL WINDOW `:231` (was `:230`, and `:229` before that — every row ADD shifts it) AND IS DELIBERATELY UNFIXED.** It says the duplicate-`stat_prefix` panic is a *"swallowed-panic BOOT HANG"* that *"neither crashes nor boots"*. **Row 78 fixed that on 2026-07-27, and the phase-97 BRAINSTORM re-measured it as rc=2 in 0 seconds on BOTH the boot and validate paths.** ⚠️ **A MEASUREMENT AGENT REPORTED THAT CLAIM ABSENT FROM THAT LINE AND WAS WRONG — it searched for the panic MESSAGE while the line spells the CONFIG FIELD.** **When an agent reports an absence, ask what it SPELLED.** Repairing it means editing a line the sentinel matches on a margin of one: **record it, do not tidy it.**

⚠️ **NEVER RE-SPELL A SENTINEL MATCH PHRASE INSIDE A SENTINEL WINDOW** (the phase-94 rule: the sentence asserting the sentinel could not move MOVED IT, six -> seven).

- ⚠️ **LIVE FIGURES RE-DERIVED AT THIS TIP (the phase-99 SPEC close), EACH BY RUNNING ITS COMMAND IN THE COMMIT THAT SHIPS THESE EDITS:** `ROADMAP.md` **249** lines / **131** data rows / tail row **99** (**`in-progress`**) at file line **161** (UNMOVED) · `DECISIONS.md` **19381** (**+22**, `## ADR-0321` §Context), `^---$` **216** (UNMOVED), `^## ADR-` **320**, bare `^## ` **328**, tail **ADR-0321** (**`PROPOSED` — the house guard is RE-ARMED; the IMPL disarms it**), next-free **ADR-0322** · `BEHAVIOR_CONTRACT.md` **5996** (UNMOVED; the IMPL edits `:4367-4368` and writes the ledger entry after `:5146`) · `STATE.md` **66** (rolled IN PLACE) · `STATE_HISTORY.md` **580 -> 582** (**+2**), strict **163 (DELTA 0)** / parenthetical **78** / loose **241** (**163 + 78 = 241** exactly) · phase dirs **140** (unmoved) · fixtures **125** (tail `0123-listener-transport-protocol`, **`0124` FREE and CHARTERED to this row as `0124-listener-sni-longest-suffix`**; registered **125 = 125**, both `comm` directions EMPTY) · the five-selector listener roster reads **398** `=== RUN`, 0 RED, under the tip, P1, P2 AND the inverted P2 (`SPEC.md` §3.3) · `-family row` **96 / 68** under `--` · non-Docker packages, fuzzers and h2spec were NOT re-run (a SPEC runs no gate; last measured **239** / **56 targets, 48 FILES** / **`95 tests, 94 passed, 1 skipped, 0 failed`** at the phase-98 IMPL).
- ⚠️ **A BRAINSTORM, A SPEC AND A PLAN RUN NONE OF THE SIX GATES, AND AN IMPL RUNS ALL SIX — THAT IS SCOPE, NOT OMISSION. ⚠️⚠️ YOUR STAGE IS THE PLAN AND RUNS NONE OF THEM; SAY SO RATHER THAN CLAIMING COMPLIANCE.** 🔴 **TWO OF THE THREE STANDING DEPARTURES WERE RE-DIAGNOSED AT THE phase-99 BRAINSTORM (`BRAINSTORM.md` §0.1-0.4):** `golangci-lint` **RUNS** as `GOTOOLCHAIN=go1.26.2 golangci-lint run ./...` (rc=0, zero findings, LIVE under a planted-violation control), and gate (b)'s compressor RED **passes under `GOTOOLCHAIN=go1.26.2`** — one toolchain move (1.26.2 -> 1.27.1) under two gates. **Both are OWED to THIS row's IMPL as a fold-in, CHARTERED at `SPEC.md` §10 (the compressor re-point is `53 49`, test-only, measured green on both toolchains and RED under two plumbing mutations); YOUR PLAN GIVES IT ITS OWN TASKS.** The third departure (no `REVIEW.md`, NONE of 93-98) is unchanged.
- ⚠️ **THE STAT SURFACE IS QUOTED AS A DELTA AND NEVER AS AN ABSOLUTE.** Three different absolutes are live at one tip. **The phase-99 SPEC CONFIRMED `+0` NAMES by three measurements (`SPEC.md` §9)** — the close path increments no stat, no non-test Go file names an `ambiguous`/`no_filter_chain` stat, and the repair touches no registry. The IMPL writes a `+0, UNCHANGED` ledger entry in the phase-96/97/98 form.

==============================================

## STATUS AS OF 2026-09-21: PHASE 99 (`chain-match-sni-longest-suffix`) **SPEC DONE — LIFECYCLE-STATE `2`**. Row 99 reads `in-progress` at `ROADMAP.md:161`. **THE SENTINEL WAS RUN MECHANICALLY AND DOES NOT FIRE.** `stop` was NOT created. **THIS SESSION WRITES PHASE 99's `PLAN.md`.**

**What the phase-99 SPEC decided, in one paragraph.** `breakTie` slot 2 will rank each tied chain by **the `server_names` pattern that MATCHED the SNI** — exact, else the LONGEST matching `*.` suffix, else `*` — and compare suffix LENGTH on equal rank. **Measured on the reference (by digest `7edd5b0fd763…`) on TCP AND QUIC, both declaration orders, with matched negatives**; two arms separate it from "the chain's best pattern" (M1/M2 on the RANK axis, M3/M4 on the LENGTH axis). **The shape (P2) is `26 23` in `chainmatch.go`**, built, driven end to end on both transports and reverted, agreeing with the reference on every arm both sides ran. **`+0` stat names CONFIRMED.** Fixture `0124-listener-sni-longest-suffix` chartered (four TCP listeners, reference ports `15124 15225 15226 15227`). `ADR-0321` §Context drafted (guard RE-ARMED). The toolchain fold-in chartered (`SPEC.md` §10).

### ⚠️ WHAT THE PHASE-99 SPEC REFUTED — ELEVEN CLAIMS

**Full detail in `99/SPEC.md` §0. The load-bearing ones:**

1. 🔴 **The BRAINSTORM's `22 0` prototype (P1) IS THE WRONG REPAIR.** It kept whole-set RANK; on `["x.test","*.foo.test"]` vs `["*.b.foo.test"]`, SNI `a.b.foo.test`, it serves X where the reference serves Y in BOTH orders. **Method note 37 fired a FOURTH row running.**
2. 🔴 **P2 is NOT purely additive: `O1` (`["*.foo.test","q.test"]` vs `["*.foo.test"]`) goes SERVED -> CLOSED.** The reference REFUSES that class at validate (arm R1: `multiple filter chains with overlapping matching rules are defined`). Declared in ADR-0321, not hidden; the boot reject is the banked duplicate-matcher row.
3. ⚠️ **NEW: `SelectChain`'s pairwise fold is ORDER-DEPENDENT** — an early nil `breakTie` returns `ErrAmbiguousChainMatch` before a strictly better later chain is compared (F1: an ALPN tie hides a `server_names` chain; subject-only). **BANKED.**
4. ⚠️ **The QUIC path is broken at the tip too** (`H3 error (0x0)` + the ambiguous log) and the reference's QUIC ordering is the TCP one — owed item 2 discharged BY MEASUREMENT.
5. ⚠️ **`manager.go:chainSpecificityRank` does not exist** (deleted at phase 07.2, `ee45f35f`); `chainmatch.go:25-28` and `:304` cite it — a premise of the controller's OWN brief, refuted by its agent.
6. ⚠️ **The occurrence set GREW by five** (`chainmatch.go:185-188`, `manager.go:1061-1064`, `quic.go:181-183` found by READING, `DECISIONS.md:3165`, `:19184`), and **`BEHAVIOR_CONTRACT.md:4368` is misleading by omission, not false** — edit by ADDING a sentence.
7. ⚠️ **The compressor test was BLIND before it was red** — a pooled-writer plumbing break PASSED it under go1.26.2; the XFL-byte re-point catches both mutations on both toolchains.
8. ⚠️ **The controller's own first draft of the NC roster was wrong** — it put `M1/M2` under the rank-mutant; under P2 both M1 chains match through a WILDCARD, tie on rank, and LENGTH decides (method note 76, again).

### ⚠️ WHAT THE PHASE-99 SPEC ESTABLISHED — **EVIDENCE YOU MAY CITE, NOT WORK YOU OWE**

- **`99/SPEC.md` §2 — the reference, both transports, by digest**; §3 — the subject under tip / P1 / P2 / inverted P2, both transports; §4.1 — the P2 code verbatim; §5.2 — the MEASURED per-arm matrix (tip 17 RED, P1 6, P2 0, inverted 17); §6 — the occurrence TABLES with a disposition per site; §11 — the five-row NC roster.
- ⚠️ **Inherit §6's TABLES as a FLOOR (method note 77); use matchers only for GROWTH.**

### ⚠️ WHAT THE PHASE-99 BRAINSTORM ESTABLISHED — **EVIDENCE, NOT INSTRUCTIONS**

- **`99/BRAINSTORM.md` §2.2 — the original both-sides table**; §0.1-0.4 — the toolchain re-diagnosis; §0.14 — the reference does not count a no-match close in `downstream_cx_total`. ⚠️ **Its §6 prototype and §3.1 framing are SUPERSEDED by `SPEC.md` §0.1-0.2.**

### ⚠️ WHAT PHASE 98 ESTABLISHED — **EVIDENCE YOU MAY CITE, NOT WORK YOU OWE**

- **`98/SPEC.md` §2 — the reference on BOTH transports**, controls first, BY DIGEST: TCP T0-T5 and QUIC Q0-Q3. **The `transport_protocol` comparison is CASE-SENSITIVE** (`RAW_BUFFER` -> DEFAULT), unlike SNI.
- **`98/PLAN.md` §7 — a COMMENT-ONLY diff gate with four controls, shown to FIRE** (A empty-diff `inspected 0`, B comment edit rc=0, C code edit rc=1, D stacked rc=1). **Reusable verbatim; its `inspected N` counter is why an empty diff cannot read as a pass.**
- **`98/PROGRESS.md` — the richest available IMPL record**: a 20-task spine task by task, an un-fixed-tip roster captured before it became unrecoverable, seven NC roster rows each proven EXECUTABLE before its colour was believed, and a six-gate sweep that NAMES its departures. **Read for METHOD; a closed phase's documents are EVIDENCE, NOT INSTRUCTIONS.**
- **`ADR-0320` (`ACCEPTED`) — the chain-match transport-protocol contract**, whose §Consequences names the one behaviour change able to move traffic on a config that booted before this row.

### Banked, NOT chartered

- 🔴 **`listener_filters_timeout` IS NOT ENFORCED ON THE SUBJECT — NEW AT THE PHASE-98 PLAN, AND BOTH SIDES ARE ALREADY MEASURED.** On a listener with `listener_filters_timeout: 1s` and `tls_inspector`, a client that stays silent for **TEN SECONDS** is still served — three arms (immediate / 2.5 s / 10 s) all read `INDEXED`, `downstream_cx_total: 3`. **Mechanism:** `peekerConn.Peek` delegates straight to `bufio.Reader.Peek` with **no deadline**; there are **ZERO** `SetReadDeadline` sites in non-test `internal/listener`; and `Pipeline.Run` checks `ctx.Err()` only **AFTER `Inspect` RETURNS**. A silent client parks a goroutine indefinitely. **The REFERENCE** times out at 1000 ms (`listener filter times out after 1000 ms`, `fallback to default listener filter`), books `downstream_pre_cx_timeout: 1`, and under `continue_on_listener_filters_timeout: false` **DROPS the connection** (`downstream_cx_total: 0`). **It falsifies `BEHAVIOR_CONTRACT.md:4359`** (*"honored in [1s, 60s] envelope"*), which is **RECORDED, NOT REPAIRED** — `98/PLAN.md` Task 17 Step 6 forbade repairing it inside row 98, and **row 98 is now CLOSED — so it is freely chartérable — **and the phase-99 BRAINSTORM adjudicated it NEXT IN LINE after row 99** (`99/BRAINSTORM.md` §4.4).** ⚠️ **SPLIT BEFORE PRICING:** (a) the timeout is advisory-only; (b) `continue_on…: false` is unreachable for a silent client, so the reference's DROP never happens; (c) the subject emits **no `downstream_pre_cx_timeout` stat at all** (`git grep -c 'pre_cx' -- '*.go'` → **0 files**; positive control `downstream_cx_total` → 3), so that counter **cannot** be pinned cross-side without adding a NAME. It is also a **resource-exhaustion** characteristic, which argues for its own row rather than a fold-in. Everything a BRAINSTORM needs is in `98/PLAN.md` §0.1, §0.3 and §10.
- 🔴 **THE TOOLCHAIN FOLD-IN — RE-DIAGNOSED AT THE phase-99 BRAINSTORM AND OWED TO ROW 99's IMPL, NOT BANKED AS A ROW.** Gate (b)'s `TestEncodeData_LevelMapping_DifferentGzippedSizes` RED and `golangci-lint`'s `sync/atomic` export-data failure are ONE event: the local toolchain moved **go1.26.2 -> go1.27.1**. Lint runs green and LIVE under `GOTOOLCHAIN=go1.26.2`; the compressor test passes under it. **Repair shape (the router's own option (b)): re-point the assertion to "the level is PLUMBED", which cannot rot again; run lint under the pinned toolchain.** ⚠️ **A v2 linter move is NOT this fold-in** — 97 new findings, ADR-gated. `99/BRAINSTORM.md` §0.1-0.4 and §10 item 8.
- ⚠️ **`TestEnvoyGoBinary_TwoListenerCutover` PORT FLAKE — NEW at the phase-98 SPEC.** Fired twice in one session (`bind 127.0.0.1:36601` at the un-fixed tip, `:33215` under a prototype), both inside the ephemeral range; three isolated reruns green. **A green rerun clears nothing.** Same class as the driver-owned receiver port race; not yet in any flake register.
- ✅ **SNI LONGEST-SUFFIX PRECEDENCE — CHARTERED AS ROW 99.** No longer banked; see the STATUS section.
- ⚠️ **THE ORDER-DEPENDENT PAIRWISE FOLD IN `SelectChain` — NEW AT THE phase-99 SPEC (`SPEC.md` §0.4), SUBJECT-SIDE ONLY.** `SelectChain` returns `ErrAmbiguousChainMatch` on the FIRST nil `breakTie`, before a later chain that outranks both is compared: F1 A{`application_protocols:[h2]`}, B{`[http/1.1]`}, C{`["*.foo.test"]`} CLOSES while F2 (C declared first) serves C, at the tip AND under the row-99 repair. **The reference was NOT driven on F1** (its nested descent would pick C at the `server_names` level — INFERRED). A different function body and arm set from row 99; ⚠️ **measure the reference BEFORE chartering.**
- ⚠️ **`server_names` PARTIAL-WILDCARD ACCEPTANCE.** Reference rejects `*`, `bar.*.test`, `*foo.test`, `foo.test.*`; envoy-go accepts all of them plus `**.test`, `*.` and `""`. The repair deletes envoy-go's `"*"` tier, documented at **eight** sites (ADR-0033 clause 9, ADR-0078 clause 9, `BEHAVIOR_CONTRACT.md`, `chainmatch.go` comments — **the union of TWO matchers, neither complete alone**) and pinned by two `chainmatch_test.go` assertions plus three `quic_test.go` comments.
- ⚠️ **SNI CASE-INSENSITIVITY — MEASURED ON BOTH SIDES AT THE phase-99 BRAINSTORM, AND DELIBERATELY *NOT* FOLDED INTO ROW 99** (against this file's old "land WITH, not alone"; `99/BRAINSTORM.md` §1.3). The reference folds PATTERN and INPUT, wildcard and exact, and its duplicate check too; envoy-go folds neither. A pattern-only fold (`4 1` in `manager.go`) fixes the pattern arm and **NOT** an uppercase SNI on the wire — the input side runs through `tls_inspector` AND the QUIC SNI path. 0 tests pin case at all.
- ⚠️ **NESTED-DESCENT PRECEDENCE — NEW AT THE phase-99 BRAINSTORM, the largest chain-match divergence known.** envoy-go scores a flat presence bitmask and consults finer grain only on exact bitmask ties; the reference descends dimension by dimension and **does not backtrack** (reference arm G3: `*.b.foo.test` vs `*.foo.test`+`application_protocols:[h2]`, SNI `x.foo.test` offering `http/1.1` only ⇒ reference CLOSES). Subject side and every mixed-dimension shape UNMEASURED. `99/BRAINSTORM.md` §0.12, §4.3.
- ⚠️ **DUPLICATE-MATCHER REJECT PARITY — NEW AT THE phase-99 BRAINSTORM.** The reference rejects OVERLAPPING server_names sets (`multiple filter chains with overlapping matching rules are defined`) and case-variant ones; envoy-go rejects only structurally IDENTICAL specs and boots the overlapping one. `99/BRAINSTORM.md` §0.13.
- ⚠️ **`catchAllCount` IS DEAD CODE.** `0 / 7`, 0 tests RED, NOT CONSTRUCTIBLE, and its only guard is vacuous. **Cheapest by every figure and REJECTED as a row because it has NO cross-side surface** — a fold-in, not a phase.
- ⚠️ **THE HCM `stat_prefix` DUPLICATE-REGISTRATION PANIC — measured at phase 97 (`BRAINSTORM.md` §4.1) and NOT re-derived at phase 98, deliberately.** Ten files register through the panicking constructor; the repair mints a boot panic `TestMain_BootPanicIsVisible` says must be RE-POINTED; it needs **TWO** fixture directories at a measured floor of ~1359-1393 added lines each. ⚠️⚠️ **FIXTURE `0122` SITS ONE IDENTIFIER AWAY FROM IT**: `chain_indexed` and `chain_default` MUST differ, and that is load-bearing.
- ⚠️⚠️ **THE DRIVER-OWNED RECEIVER PORT RACE — EVIDENCED, NOT HYPOTHETICAL.** It fired **TWICE at TWO fixtures in ONE session** at the phase-97 IMPL (`0085` port 41177, `0082` port 41561, both inside `net.ipv4.ip_local_port_range` 32768-60999) and its failure mode is **MASKING** — the abort stops the run and **everything after it is unmeasured** (the second abort masked **40** fixtures including the row's own). It is a HOST-PORT bind by the Go driver, **NOT a container publish** — do not blame `curl-world-*`. **36** files under the banked narrow predicate, **37** under a wider one; ⚠️ **the 31 / 5 / 0 split is PER FILE, not per bind site** — the same 36 files carry **43** bind sites and split **36 / 7 / 0** that way. **Name the unit or the split is meaningless.**
- ⚠️ **PER-CONNECTION TLS *IDENTITY* DISPATCH — the residual phase 97 left, STATED not hidden.** Selection is per connection; the certificate is chosen once at Start. **NOT MEASURED** — reasoned from the mechanism. Closing it means a per-client config callback that **ADR-0317 `D-ALPNFB-TCPONLY` forbids on this path** and that an existing `manager_test.go` pin asserts stays nil.
- ⚠️ **THE STAMPED QUIC ALPN CONSTANT IS A NEW ASSUMPTION, NOT A REUSED INVARIANT** — the value lives in no production Go file. Correct for an `h3` listener because negotiation can only yield it or fail, but **a listener with a different ALPN would have its `application_protocols` dimension evaluated against a hardcoded one.** **DELIBERATELY NOT REPAIRED: changing the shape without re-measuring would encode an UNMEASURED PARITY ANSWER.**
- ⚠️ **A MEASURED PHASE-97 COVERAGE GAP: zeroing the destination and source address fields on the per-connection path reddens NO arm**, because the only `destination_port` arms are nil-conn arms. Closing it means pinning `source_type`, **which the reference was never measured on** — the deferral is about MEASUREMENT, not mechanism.
- ⚠️ **THE DIFFERENTIAL HARNESS CANNOT CARRY TWO UDP LISTENERS, OR A MIXED TCP+UDP PAIR.** Three named edits close it (`97/PLAN.md` §3); the reason is `runFixture`'s transport-blind single-valued address dispatch, **not** port plumbing. Also `freeTCPPortBlock` probes only TCP, so a derived port used for a QUIC listener is never UDP-probed.
- ⚠️ **THE `0066`/`0067`/`0068`/`0071` DEAD-PORT FALSE-GREEN — a WORSE failure mode than the port race.** They allocate a port, close it, and rely on nothing listening. The port is inside the ephemeral range. If anything grabs it first the fixture asserts the OPPOSITE of its proposition — a silent wrong **PASS**.
- ⚠️ **THE BYTE-UNTOUCHED / EDIT ROSTER PAIR IS NOT A PARTITION** — at phase 97 `BEHAVIOR_CONTRACT.md` was touched and sat on neither. A roster-DOCUMENTATION gap, recorded not repaired.
- **D2-QUICTS** — reference-confirmed with its exact message, `no transport socket specified for connection oriented UDP listener`, rc=1 at listener-add. envoy-go accepts, boots and serves. Fails CLOSED. **Consumed as a phase-97 test fixture without being repaired.**
- **envoy-go emits no `listener.<addr>.http.<prefix>.*` scope at all** — the reference emits **12** such lines for a two-chain QUIC listener and the subject **0**.
- **Duplicate listener ADDRESSES** — the reference rejects them in `--mode validate`; envoy-go accepts (rc=0).
- **An SDS by `alpn_protocols` fixture** — intersection CONFIRMED empty; ~1200-1400 added lines; coverage, not a defect.
- **ADR-0025's stale `Accepted` status** and **the ADR-0044 misattribution** — recorded at `97/SPEC.md` §0.4 and §0.10, neither repaired. ⚠️ **Quote NO count of the misattributing lines.**
- **The `registry.go` citation set** — `ROADMAP.md:136` col 6095 (wrong), `DECISIONS.md:2356` (wrong), and the ADR-0296 §Context one deliberately left in place.
- **`ROADMAP.md:136` AND `:139` CARRY THE SAME STALE SWALLOWED-PANIC CLAIM AS `:231` (`:230` before the row-99 ADD)** — `:136` was REPAIRED at the phase-97 IMPL; `:139` and `:231` were not, and `:231` sits inside a sentinel window on a margin of one. **RECORD IT, DO NOT TIDY IT.**
- `0120/expectations.yaml`'s stale SCOPE · the drifted `manager_test.go` citation now at `:5832` · `0108`'s three false `ssl.*` confessions · `0118/driver/driver.go:31`'s falsified band · the `GetConfigForClient` history-class comment sites (**a CONTESTED count — quote NO number**) · `0061-lb-ring-hash`'s sigma-margin second occurrence · 1xx interim responses · the H/1 no-`Host` divergence · the pooled-upstream-lifetime defect · the TEN fixed `ssl.*` names and FOUR dynamic families (blocked on NAMING) · the DYNAMIC half of the downstream TLS `ssl` stat family (blocked on the stats-name charset banning the hyphen).

## ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + ALL FOUR NCs + THE CHECK-(2) POSITIVE CONTROL YOURSELF** and record ACTUAL output. ⚠️ **NC SHAPES CHANGE ACROSS A ROW CHANGE — NEVER INHERIT ONE.** They changed at the phase-98 BRAINSTORM's ADD (check (1) SILENT -> ONE, NC-A/NC-B ONE -> TWO), were MEASURED UNCHANGED at the phase-98 SPEC and PLAN (neither adds a row nor flips one), and **changed back at the phase-98 IMPL's FLIP (check (1) ONE -> SILENT, NC-A and NC-B TWO -> ONE), measured on both sides.** **They changed AGAIN at the phase-99 BRAINSTORM's ADD (check (1) SILENT -> ONE, NC-A and NC-B ONE -> TWO, `want` 130 -> 131, `ROADMAP.md` 248 -> 249), measured on both sides.** **The phase-99 SPEC measured them UNCHANGED.** ⚠⚠ **YOUR STAGE IS THE PLAN, WHICH NEITHER ADDS NOR FLIPS A ROW: every shape should read UNCHANGED (check (1) ONE, NC-A TWO, NC-B TWO at want=130). MEASURE IT — an unchanged shape is a measurement, not an inheritance.**
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** 95 BRAINSTORM nine · 95 SPEC eleven · 95 PLAN seventeen · 95 IMPL eighteen · 96 BRAINSTORM thirteen · 96 SPEC ten · 96 PLAN fifteen · 96 IMPL six · 97 BRAINSTORM twelve · 97 SPEC eleven · 97 PLAN sixteen · 97 IMPL fourteen · 98 BRAINSTORM thirteen · 98 SPEC ten · 98 PLAN fourteen · **99 BRAINSTORM fourteen · 99 SPEC eleven (its §0.1 refuted the BRAINSTORM's own prototype against the reference)** · **98 IMPL EIGHTEEN, every one of them against a GOVERNING DOCUMENT or the router — including a fixture `expectations.yaml` that contradicted the PLAN and was itself the wrong document, and a PLAN code block NOT INSTALLABLE AS PRINTED** — and the earlier chain: **98 PLAN FOURTEEN, NINE by its own agents — including `SPEC.md` §14 ITEM 1's OWN INSTRUCTION and the fixture cost floors THIS FILE quotes** — and the SOURCE matters more than the count: **THREE of the sixteen came from that stage's OWN agents**, one of which corrected its own summary line against its own table mid-report, and one of which refuted a STANDING METHOD NOTE. ⚠️ **EXPECT TO BE REFUTED BY YOUR OWN REVIEW SEAM, NOT ONLY BY YOUR SUCCESSOR** — third consecutive row. ⚠️ **AND WHEN TWO PARTIES CONTRADICT EACH OTHER, FIND THE VARIABLE RATHER THAN PICKING A WINNER.**
3. **LINE ANCHORS DRIFT; SYMBOL AND LITERAL-TEXT ANCHORS DO NOT — RE-VERIFY EVERY CITE AT YOUR TIP.** ⚠️ **PATHSPEC-SCOPE every symbol assertion and every `sed`.** ⚠️ **`internal/filter/hcm/chain.go` DOES NOT EXIST — it is `internal/filter/http/chain.go`** — but **`actions.go` IS in `hcm`.** ⚠️ **PROVEN AGAIN AT PHASE 96: `manager.go:1393` -> `:1406` and the D4 block `:746` -> `:759`, both moved by the SAME stage's own edits.**
3a. ⚠️ **AN ANCHOR CAN BE NON-UNIQUE.** **Anchor on the ENCLOSING SYMBOL.**
3b. ⚠️ **CHARACTER-CLASS AND ESCAPE SELECTORS ARE THE FAIL-UNSAFE FAMILY.** `^[0-9]{4}-` on fixtures reads 121, dropping `0007a`/`0007b` (use `ls -d test/fixtures/*/ | wc -l`) · **a CASE-SENSITIVE GREP IS BLIND TO UPPERCASE PROSE — use `-i` when auditing comments** (`0008`'s `EXACTLY one chain` was invisible to a case-sensitive sweep) · ⚠️ **AND `[ \t]` INSIDE A GNU ERE BRACKET EXPRESSION IS A LITERAL BACKSLASH AND `t`, NOT A TAB.** See also notes 33 and 56.
3c. ⚠️ **`feedback_brief_citations_not_evidence` APPLIES TO INCIDENTAL FIGURES — including every number in THIS file and in every AGENT REPORT.**
3d. ⚠️ **NO GATE MAY REST ON A ` + ` SPLIT.**
3e. ⚠️ **A GATE COMMAND CAN MATCH PROSE INSTEAD OF CODE.** Anchor fixture-set extraction on the blank-import LINE:
```sh
extract () { grep -oE '^[[:space:]]*_ "github\.com/pgdad/envoy-go/test/fixtures/[^/]+/(driver|inputs)"$' "$1" \
  | sed -E 's#.*/test/fixtures/##; s#/(driver|inputs)"$##' | sort; }
```
🔴 **CORRECTED TWICE, AND THE SECOND CORRECTION IS THIS ONE.** The note read `123 / 123` and `99 + 24` for two rows while this file's own live-figures bullet already said `124`, so the router DISAGREED WITH ITSELF inside one document; that was fixed to `124 / 124` and `100 + 24` at the phase-98 PLAN — **and phase 98's own fixture `0123` moved it again.** Re-run VERBATIM at the phase-98 IMPL tip AND the phase-99 BRAINSTORM tip: **dirs 125 = imports 125**, both `comm` directions EMPTY, split **101 `driver/` + 24 `inputs/`**. **The GATE was sound throughout; only the NUMBERS rot, and they rot once per fixture-adding row.** ⚠️ **A PLAN ADDS NO FIXTURE (the SPEC CHARTERED `0124`; the IMPL adds it), so your stage should read 125 / 125 unchanged — CONFIRM IT, do not assume it.** The phase-99 SPEC read it 125 / 125. The phase-99 BRAINSTORM's rename NC read `1 / 1`. **NC the extractor** (rename one import in a SCRATCH COPY). ⚠️ **A COUNT-ONLY CHECK IS VACUOUS: the import count stays constant under a rename.** ⚠️ **BUT A DELETION FIRES ONLY `comm -23`** — "both directions" belongs to the RENAME control alone.
3f. ⚠️ **"AT THE RETURN" IS NOT A PLACEMENT.** Anchor installs on the ENTRY of the path that must be covered and PROVE the install with an arm that reddens without it.
3g. ⚠️ **A "CHEAP ASSERTION GAP" CAN BE A BEHAVIOUR DIVERGENCE WEARING A DISGUISE.** Measure the reference for THAT ARM before asserting any cross-side stat.
3i. ⚠️ **A LOOSE `--grep 'phase NN.*STAGE'` MATCHES THE WRONG STAGE.** **Anchor stage-commit locates on `^phase NN (slug) STAGE:`** — ⚠️ **and see method note 57: the anchored form is BLIND where the author omitted the stage word.**
3j. 🔴 **REFUTED AT THE PHASE-97 PLAN — `grep` IS THE *SAME* WRAPPER IN BOTH SHELLS.** This note used to say the subagent shell carries `/usr/bin/grep` (GNU 3.11) while the controller carries a **ugrep** function. **An agent ran `type grep` in its own shell and found the same function** (`exec -a ugrep "$_cc_bin" -G --ignore-files --hidden -I --exclude-dir=.git …`). ugrep honours `.gitignore`, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway** — so **a recursive bare `grep` is BLIND to it in EVERY shell in this environment.** Use `/usr/bin/grep`, `command grep -rl`, `git grep`, or a direct path, **in the controller AND in every brief you issue**. ⚠️ **`command grep` DOES NOT SURVIVE `xargs`.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files). ⚠️ **ugrep ALSO REJECTS SOME COMPLEX `grep -oE` PATTERNS with `exceeds complexity limits` where GNU accepts them.** **NAME THE BINARY, NOT THE SHELL.**
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Live: TWO SDS dial-budget flakes plus `TestSDSEndToEnd_FetchFailure_BootFailsClosed` · the driver-owned receiver port race · `internal/httpclient` zero-value · the two 84.2-era flakes · the REFERENCE h2spec section-8 flip · `TestOutlierDetector_ConcurrentEjectExactlyOnce` · `0061-lb-ring-hash`'s σ-margin · `TestP83_StopPauseTimer_IsAuthoritativeAgainstAnEnteredClosure` (wasm, 1/400) · `internal/filter/hcm/h2 TestFramer_ReaderGoroutineDoesNotLeak`. ⚠️ **`TestServerConn_TinyWindowDelivery` IS NOT A FLAKE** — a recurrence is a REGRESSION of row 91. ⚠️ **A GREEN RERUN CLEARS NOTHING.**
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only). ⚠️ **AT THE PHASE-97 SPEC A SIBLING `curl-world` SESSION STILL OWNED SEVENTEEN CONTAINERS**, and a foreign `envoyproxy/envoy:v1.33.0` container appeared and exited inside the measurement window without being touched. ⚠️ **A `reaper_*` testcontainers Ryuk container is created BY THE DIFFERENTIAL ITSELF and is REUSED; leave it alone.**
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`. ⚠️ **`INNER_EXIT` DOES NOT EXIST in this repo.**
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL, THEN DRIVE THE ARM.** **`gofmt -l` never exits non-zero — gate on OUTPUT.** ⚠️ **go test WITHOUT `-v` PRINTS ZERO `=== RUN`** — `RUN=0` beside `RC=0` is a VACUOUS GREEN. ⚠️ **A `-run` selector matching nothing prints `[no tests to run]` and EXITS 0.** ⚠️ **On `-v` output, unanchored `grep -c 'FAIL'` reads nonzero ON A FULLY GREEN TREE** — use `grep -cE '^(FAIL|--- FAIL)|^ *--- FAIL'`.
7b. ⚠️ **`grep -c` ON ZERO MATCHES PRINTS `0` AND EXITS 1** — capture, NEVER chain with `&&`, NEVER `$(… || echo 0)`.
7c. ⚠️ **A GREEN TEST RUN IS NOT EVIDENCE A SITE IS EXERCISED** — prove it with a **`panic()` REACHABILITY CONTROL**. **There is NO `recover()` in non-test `internal/tls` or `internal/listener`, so a panic aborts the binary — gate on `grep -cE '^panic:|DATA RACE|SIGSEGV'`.** ⚠️⚠️ **AND A FAIL-FAST CONTROL MEASURES ONE SITE PER RUN — SEE NOTE 55.**
7d. ⚠️ **NC EVERY NEW TEST, AND NC YOUR OWN FIXES** — NEUTRALISE so the package still compiles; check the NC is EXECUTABLE. ⚠️⚠️ **AND CHECK THAT YOUR NC *CAN* FIRE AT ALL.** **Before running an NC, ask what mechanism would carry its mutation to a failure — if you cannot name one, the control is vacuous.**
7e. ⚠️ **A UNIT TABLE OVER A SHARED ASSERTION IS NOT A GATE ON PRODUCTION BEHAVIOUR** — a shared `t.Helper()` body collapses every property onto ONE line, so **make each message name its property**.
7f. ⚠️ **A NC THAT LEAVES YOUR NEGATIVE CONTROL GREEN IS NOT EVIDENCE THAT CONTROL DOES ANY WORK.**
7g. ⚠️ **ASSERT *WHICH* ERROR FIRED, AND ALSO WHICH DID NOT.** ⚠️ **KEEP TABLE ROWS SINGLE-CAUSE.** ⚠️ **PREFER AN EXACT EQUALITY TO A FLOOR.**
7h. ⚠️ **A PROBE CLIENT CAN WITHHOLD THE VERY THING THE ARM EXISTS TO TEST.** ⚠️ **`openssl s_client` answers "did the handshake complete", NOT "was the connection served"** — drive a real request AFTER the handshake. ⚠️⚠️ **AND A PROBE CLIENT CAN OMIT AN INPUT AND MAKE A WHOLE ARM VACUOUS**: the `0104` driver does not set `ServerName`, so every `server_names` arm at the phase-97 SPEC would have been vacuous had the client not set SNI explicitly. **Name the inputs your probe sends, not only the ones it asserts.**
7i. ⚠️ **A GATE THAT READS 0 HAS NOT BEEN SHOWN TO WORK.** Run every silent gate over an input KNOWN to trip it.
7j. ⚠️ **A PIN CAN FAIL AGAINST *CORRECT* CODE.** **Before writing an expectation map, MEASURE the leaf set a completing handshake actually books.**
7k. ⚠️ **ASSERT THE POINTERS, NOT ONLY THE EFFECT** ([[reference_nil_stats_counter_inc_crashes_goroutine]]).
8. ⚠️ **`pgrep -f` / `pkill -f` MATCHES YOUR OWN SHELL AND KILLS THE TOOL CALL (exit 144)**. **Kill ONLY PIDs you captured yourself at launch.** ⚠️ **`pgrep -la a b` ERRORS** — one pattern per call.
9. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-*`, `wt-*` or stage worktree should outlive its stage close** — expect only `master` (verify with `git worktree list`) — 🔴 **AND IT WAS FALSE AT THE phase-99 BRAINSTORM's START: `wt-phase-98-impl` had outlived the phase-98 close** (clean, nothing unmerged; removed, branch kept). **A stage close must `git worktree remove` its own worktree.** ⚠️⚠️ **THAT CLAIM IS ABOUT WORKTREES AND ONLY WORKTREES — DO NOT AUDIT IT WITH `git branch`.** At the phase-97 SPEC's pushed tip `git worktree list` reads exactly one entry, the canonical root on `master`, while `git branch` reads **173** local branches of which **83** are NOT merged into `master`, including spent re-evaluation branches sitting **0 commits ahead**. **The branch graveyard is deliberate history and is NOT a stage-close defect; scoring note 9 on branches manufactures a false alarm.**
10. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** Re-derived at THIS tip (the phase-98 IMPL close, which RAN all six): (a) differential **125 PASS / 0 FAIL / 0 SKIP** in `427.280s`, registration **125 = 125 = 125** three-way, asserted by the fixture-set SET-DIFFERENCE in BOTH `comm` directions, never by the exit code; (b) the non-Docker sweep gated on `PIPESTATUS[0]` + a SET RECONCILIATION — **239 packages** (⚠️ **NOT the 238 this note carried; `0123`'s own driver package moved it, exactly as phase 97's fixture moved it from 237**) — 🔴 **AND GATE (b) IS RED**, at `internal/filter/http/compressor`, for a **toolchain** reason this row did not cause (see the banked list); (c) h2spec `95 tests, 94 passed, 1 skipped, 0 failed`, the skip being 6.9.2/2 — ⚠️ **a NON-verbose run prints NO summary line at all, only `ok … Ns`**; (d) fuzzers **56 targets / 48 FILES** — ⚠️ **48 is FILES and 56 is TARGETS, do not conflate them**; (e) the ANCHORED panic gate `^panic:|DATA RACE|SIGSEGV`, **0** *and PROVEN LIVE*; (f) no `REVIEW.md` — a **STANDING DEPARTURE**, **37** of **139** phase dirs carry one and NONE of 93-98 do. 🔴 **`golangci-lint` DOES RUN — as `GOTOOLCHAIN=go1.26.2 golangci-lint run ./...` (re-diagnosed at the phase-99 BRAINSTORM, §0.1; the old "CANNOT RUN AT ALL" was the ambient go1.27.1, not the linter).** Row 99's IMPL owes it as a fold-in, with the planted-violation positive control repeated. ⚠️ **`-race` on the DIFFERENTIAL suite is VACUOUS** — the subject there is an unraced subprocess; run `-race` on the FULL `internal/listener` package instead. **AN IMPL RUNS ALL SIX; A BRAINSTORM, SPEC AND PLAN RUN NONE, AND THAT IS SCOPE, NOT OMISSION. ⚠️⚠️ YOUR STAGE IS THE PLAN AND RUNS NONE OF THEM.**
11. ⚠️ **THE DIFFERENTIAL'S FAILURE MODE IS A SILENT PASS.** `-count=1` IS NOT OPTIONAL. **ASSERT THE FIXTURE SET BY NAME, BOTH DIRECTIONS.** The full suite takes ~400s.
11b. ⚠️ **`go test ./...` DRIVES DOCKER — AND THERE ARE TWO DOCKER DRIVERS.** Exclude BOTH: `go list ./... | grep -vE '/test/differential$|/test/conformance/h2spec$'`.
12. ⚠️ **`-p` PUBLISHING WORKS; `--network host` IS WHAT DOES NOT.** For a UDP/QUIC listener publish UDP (`-p <port>:<port>/udp`) AND the admin TCP port. ⚠️ **`STRICT_DNS` + `host.docker.internal` + `--add-host=host.docker.internal:host-gateway` IS REQUIRED for a container-to-host cluster HERE.** ⚠️ **THE REFERENCE CONTAINER CANNOT READ `filename:` CERT PATHS** — use `inline_string:` — ⚠️ **AND INDENT THE PEM ONE LEVEL DEEPER THAN `inline_string:`**. ⚠️ **A PROBE BACKEND MUST BIND `0.0.0.0`, NOT `127.0.0.1`.** ⚠️ **Keep PEM substitutions OUT of YAML comments.** ⚠️ **envoy-go REJECTS `match.headers`**, **refuses `codec_type: HTTP2` without TLS unless `--allow-h2c`**, **boots with `-c`, NOT `--config-path`**, **`access_log[].log_format` is BOOT-REJECTED**, **`TLSv1_0`/`TLSv1_1` in `tls_params` BOOT-REJECT**. ⚠️ **AN OMITTED `clusters` KEY BOOT-REJECTS envoy-go** — give a probe subject a placeholder STATIC cluster, and `BackendCount()` must be **≥ 1**. ⚠️ **`ENVOY_TARGET.md` is at `docs/envoy-go/ENVOY_TARGET.md`, NOT the repo root.** ⚠️ **The pinned reference is `envoyproxy/envoy:contrib-v1.37.2`, digest `7edd5b0fd763…` — PIN BY DIGEST, verify against `ENVOY_TARGET.md:3-4`** (RE-VERIFIED IN USE at the phase-97 SPEC). ⚠️ **`timeout` rc=124 is shared by a HEALTHY server and a HUNG boot — read the OUTPUT, never the code.** ⚠️ **AND envoy-go's validate mode is `-mode validate`, the reference's is `--mode validate`.** ⚠️ **A `direct_response` status of `111` came back as `200` on the H3 path while `222` came back as `222` — CHOOSE NON-1xx SENTINEL STATUSES.**
13. ⚠️ **A RAW-FRAMER PEER MUST HOLD ONE `hpack.Decoder` PER CONNECTION.**
14. ⚠️ **`bash /dev/tcp` + `timeout cat <&3` INSIDE COMMAND SUBSTITUTION RETURNS SILENTLY EMPTY** — use Go for socket probes.
15. ⚠️ **GO DOES NOT FLAG UNUSED CONSTANTS.** Post-removal cleanup is grep-driven.
16. ⚠️ **`golangci-lint`'s misspell runs in locale US.** **Sweep British spellings in `.go` comments BEFORE the gate.** Markdown prose may use them freely.
17. ⚠️ **A GREEN RUN OF A CONTENTION-SENSITIVE TEST PROVES NOTHING.**
18. ⚠️ **AN ABSENCE CLAIM NEEDS A POSITIVE CONTROL AND A DISCRIMINATING NEGATIVE CONTROL.**
19. ⚠️ **DERIVE next-free ADR FROM THE TAIL, NEVER FROM THE HEADING COUNT.** The id space is SPARSE (exactly one gap, `0209`). The correct derivation is `grep -oE '^## ADR-[0-9]+' … | tail -1` ⇒ `## ADR-0319`, next-free **ADR-0320**, confirmed by `grep -c '^## ADR-0320'` ⇒ **0**. ⚠️ **AND THE HEADING REGEX ITSELF IS HOLED**: `^## ADR-[0-9]+[:—]` misses `## ADR-0127 v2`, which carries a version suffix between the id and the colon. ⚠️ **Re-derive at your tip rather than quoting these.**
20. ⚠️ **`reference_measured_prototype_is_a_lower_bound` HAS NOW FIRED TWENTY-ONE CONSECUTIVE ROWS (at the phase-99 SPEC the `22 0` floor was not merely low but the WRONG SHAPE — P2 is `26 23`), AND AT THE PHASE-98 PLAN IT FIRED IN THE OTHER DIRECTION TOO: an inherited FLOOR can be an OVERSTATEMENT.** The `1359`/`1393` fixture floors this file quotes are `0120`/`0121` **dir-only** figures, both **PKI-inflated**; the NEWEST fixture, `0122`, is **946**. **Re-derive a floor by SHAPE, not by recency — and check what the quoted number MEASURES.** Row 97's floor moved **12x** BRAINSTORM -> SPEC on a WRONG shape; **row 98's `+1/-6` became `+4/-9` on a WIDER shape** because the BRAINSTORM's positive control never drove the subject. **ENUMERATE BY GREPPING THE LITERAL SYMBOL OUTWARD, CASE-INSENSITIVELY, PATHSPEC-SCOPED, AND BY COMPILING — and treat every cost estimate as a FLOOR.**
21. ⚠️ **A MEASUREMENT THAT GOVERNS A DEFERRAL DECISION MUST LAND IN A GOVERNING DOCUMENT.** `SPEC.md` §2 is carried into ADR-0319 §Context ¶2 for exactly this reason.
22. ⚠️ **A STAGE'S FILE SCOPE IS ITSELF MEASURABLE, AND THE GOVERNING DOCUMENT CAN BE WRONG ABOUT IT — TWICE IN A ROW.** `git show --numstat` on the precedent stages settles it, and **MEASURE MORE THAN ONE**. A SPEC's scope is **five** files, measured across FOUR (`307f2e3d` `9ed6a620` `da6ea191` `331c3524`) — ⚠️ **the four agree on PATHS, not on the `DECISIONS.md` line count (all `+30`; the phase-98 SPEC `+22`)**; a PLAN's is **four**, measured across **FIVE** precedents (`e69f1c58` `90010c4c` `db539e7d` `f647dd72` `0eab42e3`), each reading `STATE_HISTORY.md` `2 0`. ⚠️ **AN IMPL'S IS THE BIG ONE — MEASURE IT YOURSELF against `c7bd2880` (phase 96) and `0a985a35` (phase 94)**, which touch **20** and **29** paths respectively. **Wording is not scope.**
23. ⚠️ **`git diff --stat` IS A SUM, NOT ADDITIONS.** Cost figures come from `--numstat`.
24. ⚠️⚠️ **AN UNESCAPED `|` IN A ROADMAP ROW PASSES CHECK (1) AND SILENTLY BREAKS THE FIELD COUNT.** ⚠️ **IT IS DORMANT AT YOUR STAGE (a PLAN edits no row) AND LIVE AGAIN AT row 99's IMPL, which rewrites the cell at the flip.** Count fields (want **8**) under BOTH the naive and the escape-aware form BEFORE installing, and **assert the new cell spells NEITHER sentinel match phrase** — check (2) matches the WHOLE FILE, not only the six windows. Rows 98 and 99 were gated that way before insertion and read **8 under both**; row 98 still read 8 after its IMPL rewrote the cell. **Reword a pipe AWAY rather than escaping it.** ⚠️ **THIS FIRED AGAINST THE PHASE-96 IMPL'S OWN AUTHOR**, who wrote a Go `||` into row 96's narrative. **It is dormant at the SPEC and the PLAN, which edit no row, and live at any BRAINSTORM (which adds one) or IMPL (which flips one).**
25. ⚠️ **THE EVICTION CHECK IS A *PAIR*, AND THE BARE FORMS DO NOT ANSWER IT.** Both bare forms read the same value on `STATE.md` and both are invariant under which entry is evicted. Only the LABEL-BOUND form discriminates, on BOTH files: the evictee's own label goes `STATE.md` **1 -> 0** and `STATE_HISTORY.md` **0 -> 1**. **VERIFIED IN BOTH DIRECTIONS at this close**, with a fabricated-label NC reading 0 in BOTH files and a positive control on a sibling label that IS present in the archive. ⚠️ **The sibling positive control must name an ARCHIVED label, not a §Recent one.**
26. ⚠️⚠️ **A DIRECT DATE READ SOMETIMES PICKS THE EVICTEE AND SOMETIMES CANNOT — READ THE SHAPE EVERY TIME.** 🔴 **AND THIS NOTE WAS ITSELF WRONG AT THE PHASE-98 IMPL CLOSE, IN A WAY WORTH NAMING:** it predicted a **FOUR-WIDE** tie for that close because it restated the PLAN close's **PRE**-roll list as though it were that close's **POST**-roll list, silently dropping the newest entry; `98/PLAN.md` Task 20 Step 2 inherited the same error, while this file's own closing bullet and `STATE.md`'s preamble said **three**. **Measured at the IMPL close: one `09-16`, one `09-12`, THREE `09-09` — a THREE-WIDE tie at the tail**, so the date narrowed the field to three and **LIST POSITION picked the tail**. ⚠️ **A PROJECTED HISTOGRAM IS NOT A MEASURED ONE, AND A PROJECTION THAT FORGETS THE ENTRY YOUR OWN CLOSE IS ABOUT TO PROMOTE IS WRONG BY EXACTLY ONE, EVERY TIME.** ⚠️ **The phase-99 BRAINSTORM measured that TWO-WIDE tie as predicted and broke it by list position; the post-roll list now reads `09-20, 09-20, 09-16, 09-12, 09-09` — a UNIQUE tail, so YOUR close's date read CAN pick alone. MEASURE IT; INHERIT NO SHAPE, INCLUDING THIS ONE.**
27. ⚠️ **A SYMBOL-SCOPED CLAIM IS NOT A PATH-SCOPED ONE.** **Enumerate the call sites and check each caller's own branch.** ⚠️ **AND ONE SYMBOL CAN RUN AT TWO MOMENTS**: `quicTLSConfig` is called at Start AND per connection, which is the whole reason phase 97's repair splits the two accessors instead of collapsing them.
28. ⚠️ **A `+0/+0` NEGATIVE-CONTROL ARM CAN BE SILENTLY DELETED WITH EVERY GATE STAYING GREEN.** **Diff the ARM ROSTER, not the counters.**
29. ⚠️ **A MULTI-INSERT LINE SHIFT IS BANDED, NOT A CONSTANT.** **Never hand a scalar shift for a multi-insert file — re-locate every cite by LITERAL TEXT (`grep -nF -- "<exact line>" <path>`).**
30. ⚠️ **A `go test` SELECTOR NAMING A PACKAGE THAT DOES NOT EXIST PRINTS `FAIL … [setup failed]` AND EXITS 1.** **Confirm a selector resolves (`go list ./path/...`) before believing a FAIL.** This is the mirror of the `-run` footgun, which fails the OTHER way (no match ⇒ EXIT 0).
31. ⚠️ **RECONCILE A CORRECTED CLAIM ACROSS ITS WHOLE OCCURRENCE SET, CASE-INSENSITIVELY** — and state which hits you are deliberately leaving and why.
32. ⚠️ **A ROW CAN SHIP A FALSE FIGURE IN THE COMMIT THAT WRITES IT, AND A "CORRECTION" IS NOT A MEASUREMENT.** **NAME THE MEASURE (bytes or chars) OR THE FIGURE IS MEANINGLESS**, and **re-derive any count you quote by running the command at YOUR publishing tip, in the SAME commit as the edit that moves it.** ⚠️ **The phase-97 SPEC's own `SPEC.md` line count was patched in AFTER the last section was appended and re-verified before committing.**
33. ⚠️ **A CHARACTER CLASS SILENTLY DROPS WHAT IT DOES NOT SPELL.** `grep -cE '^\s+[a-z0-9./-]+ v[0-9]'` on `go.mod` reads **62**; the count is **67**. **RE-CONFIRMED AT THE PHASE-97 SPEC — and see note 56 for how an agent "refuted" it.** **Prefer a structural extractor (`awk` over the `require` blocks) to a character class, and diff the two when they disagree.**
34. ⚠️ **TWO INDEPENDENT AGREEING MEASUREMENTS DO NOT MAKE A CLAIM GENERAL.** **Enumerate the class, THEN measure.**
35. ⚠️ **A GUARD'S NAME IS NOT ITS COVERAGE — AND A DEAD ASSERTION CAN BE INVERTED AS WELL AS ABSENT.** **Read the arms, then read what each one would assert if it ran.**
36. ⚠️ **A VARIABLE REUSED FOR A SECOND PURPOSE OUTLIVES THE INVARIANT THAT MADE IT SAFE.** **When you gate a new feature on an existing boolean, prove that boolean still means what the new use needs, and NAME THE ADR THAT COULD REPEAL IT.**
37. ⚠️ **A FIX SHAPE CAN ENCODE A PARITY ANSWER NOBODY HAS MEASURED.** ⚠️⚠️ **THIS HAS NOW FIRED THREE ROWS RUNNING, AND AT PHASE 97 IT KILLED THE PREDECESSOR'S PROTOTYPE OUTRIGHT.** **Measure the reference, then choose — and diff size is never the tiebreaker.**
38. ⚠️ **AN OCCURRENCE SET CAN BE NORMATIVE, NOT MERELY COMMENTARY.** **Sweep `docs/` with the same case-insensitive breadth as the code, resolve every markdown hit to its enclosing ADR by BACKWARD heading search, and say which hits you are deliberately leaving and why.**
39. ⚠️ **A BARE-TOKEN GREP'S FALSE POSITIVES ARE CONFIG-DEPENDENT, SO A KNOWN WITNESS IS NOT A GATE.** **Anchor and NC the anchored form against the arm that must read zero.**
40. ⚠️ **A COUNT WITHOUT ITS MATCHER IS MEANINGLESS, AND TWO CORRECT COUNTS CAN DISAGREE.** **Name the matcher AND the moment beside every figure.**
41. ⚠️ **AN ILLUSTRATION IN §CONSEQUENCES IS NOT THE LIMIT OF A §DECISION.** **Read the decision, not the example.**
42. ⚠️ **A SECTION HEADED *"RE-DERIVED AT THIS STAGE'S OWN TIP"* IS FALSE IF IT NAMES A COMMIT THAT IS NOT THE ONE IT SHIPS ON** ([[reference_row_can_ship_false_figure_at_birth]]).
43. ⚠️ **AN ENUMERATION'S EXCLUSION RULE CAN HAVE A HOLE ITS AUTHOR DID NOT SEE.** **State the exclusion as a PREDICATE and then test every live file against it.**
44. 🔴 **CORRECTED AT THE PHASE-97 PLAN — THE `[]struct{` ZERO IS A *SPACE*, NOT A FOLLOWING-LINE BRACE.** This note used to say `manager_test.go`'s six tables read zero under `[]struct{` *"because their brace sits on the FOLLOWING line"*, and the phase-97 SPEC's §14 inherited that and prescribed a following-line matcher. **BOTH ARE WRONG: `\[\]struct[ ]*$` reads 0 as well.** gofmt writes **`[]struct {` with a SPACE**. The matchers that are correct by construction are **`\[\]struct[ ]*\{`** and the bare **`\[\]struct`**, each reading **6** at `:1092 :1484 :4777 :5444 :5691 :6102` with loops at `:1109 :1559 :4792 :5456 :5752 :6157`, all binding `tc` (the `tt` idiom appears nowhere in the package). ⚠️ **A MATCHER'S VOCABULARY CAN BE WRONG IN A DIRECTION ITS AUTHOR DID NOT PREDICT — and a PRESCRIPTION inherits the error.** Also measured: **both QUIC test files are table-FREE under every form.**
45. ⚠️ **A REGISTRATION-TIME SYMBOL IS NOT A CONSTRUCTION-TIME SYMBOL.** **Enumerate the call sites and ask WHEN each runs, not only WHERE it is.**
46. ⚠️ **A CROSS-SIDE PIN ON A NAME ONE SIDE NEVER EMITS IS SILENTLY VACUOUS, NOT RED.** **Before pinning a stat cross-side, prove the NAME exists on both sides.** ⚠️ **RE-CONFIRMED AT PHASE 97: for a two-chain QUIC listener the reference emits ~156 lines across the two HCM scopes and a 12-line listener-qualified scope; envoy-go emits TEN lines and NO listener-qualified HCM scope at all. PIN A NAMED SUBSET.** ⚠️ **And the listener ADDRESS TOKEN differs cross-side even where both emit a listener scope.**
47. ⚠️⚠️ **A LIFECYCLE STATE DEFINED BY WORK REMAINING IS RE-ENTRANT, AND STATE 3 IS THE ONE WHERE IT BITES.** **A 20-TO-24-TASK SPINE WILL NOT FINISH IN ONE SESSION.** The discriminator is `PROGRESS.md` and the ROADMAP row, **not the session boundary** — a session that runs out of budget mid-spine leaves the state at **3**, says which task it reached, and does NOT flip the row. ⚠️ **THIS IS NOT YOUR STAGE'S HAZARD DIRECTLY: a PLAN is state 2 -> 3 and is NOT re-entrant this way (see 47a) — but the spine you write is what the IMPL will run out of budget inside, so ORDER IT so any stopping point leaves a coherent, committed tip, and evaluate BOOTSTRAP §6.1 WITH A COMMAND.** ⚠️ **Evaluate the context budget BEFORE starting a task, not after** (`checking-context-budget`).
47a. ⚠️ **A SPEC AND A PLAN ARE NOT RE-ENTRANT THE SAME WAY** — their discriminator is the EXISTENCE of `SPEC.md` / `PLAN.md`, not work remaining. Do not leave a partial one.
48. ⚠️ **PARALLELISE ACROSS DISJOINT FILE SETS, NOT INSIDE ONE**, with **Docker serialized to exactly ONE agent**. ⚠️ **AND KEEP A LOAD-BEARING ORDER STRICTLY SERIAL inside its own worktree.**
49. ⚠️⚠️ **AN AGENT'S ABSENCE CLAIM IS ONLY AS GOOD AS ITS MATCHER'S VOCABULARY.** **When an agent reports an absence, ask what it SPELLED before believing what it did not find** — and when two parties disagree, FIND THE VARIABLE rather than picking a winner.
50. ⚠️ **A GREEN PACKAGE UNDER A BEHAVIOUR-INVERTING PATCH IS A COVERAGE FINDING, NOT A PASS.** `go test ./internal/listener/...` is rc=0 both before and after a patch that reverses which filter chain serves every QUIC request — **CONFIRMED FIRST-HAND at the phase-97 SPEC with a `+5 / -0` inverting patch.** **Run your fix AND your un-fix through the same suite and compare; identical greens mean the suite is BLIND.**
51. ⚠️ **AN UNRELATED BOOT REJECT MASKS THE SITE UNDER TEST.** **Prove your reproduction REACHED the site — by the message, never by the exit code.**
52. ⚠️ **SPLIT A BANKED VERDICT BEFORE PRICING IT.**
53. ⚠️ **A COST ADJECTIVE ROTS IN THE BLAST RADIUS, NOT ONLY IN THE COUNT.** **Before pricing a repair, grep for the guard whose trigger it removes.**
54. ⚠️ **CHOOSE NON-1xx SENTINEL STATUSES IN PROBES.** See note 12.
55. ⚠️⚠️ **A SINGLE POSITIVE ARM CANNOT DISTINGUISH "THE INPUT WAS STAMPED AND MATCHED" FROM "THE DIMENSION IS UNENFORCED" — PAIR EVERY DIMENSION WITH A MATCHED NEGATIVE ON A BYTE-IDENTICAL SUBJECT.** At the phase-97 SPEC an arm showing the reference serving a `transport_protocol: "quic"` chain was ambiguous until the `"tls"` arm fell to the default chain; the same pairing was needed on the ALPN and SNI axes. ⚠️ **AND THE SAME LOGIC APPLIES TO A FAIL-FAST REACHABILITY CONTROL: with a `panic()` in TWO sites the run aborts on the first and says NOTHING about the second. ISOLATE — one site per run.**
56. ⚠️⚠️ **AN AGENT CAN "REFUTE" AN INHERITED FIGURE BY RUNNING A DIFFERENT COMMAND THAN THE ONE THE CLAIM NAMES.** At the phase-97 SPEC an agent reported method note 33's `62` non-reproducible; it had run `^\t[a-z]` where the note names `^\s+[a-z0-9./-]+ v[0-9]`. Both counts were correct and the note was right. **ASK WHAT IT RAN, NOT ONLY WHAT IT FOUND** — the sibling of note 49, and now the second consecutive row on which a stage's own agent produced the refutation.
57. ⚠️ **THE ANCHORED STAGE-COMMIT FORM IS BLIND WHERE AN AUTHOR OMITTED THE STAGE WORD.** Phase 95's IMPL landed as `0f3b98b3` with a subject carrying no stage token at all, so `git log --grep '^phase 95 (.*) IMPL'` reads **ZERO**. **Run the anchored AND the loose form and RECONCILE THE SETS**, and **when you land a stage, put both the FULL SLUG and the STAGE WORD in its subject.**
58. ⚠️ **A ROW THAT AGREES FOR A REASON THE MECHANISM CANNOT SUPPLY IS A FALSE AGREEMENT.** envoy-go answers *"default chain"* on every QUIC arm, so it coincidentally matches the reference wherever the reference also picks the default — while evaluating nothing. **Before reading agreement as evidence, ask whether the subject could have answered anything else.**
59. ⚠️ **ADR-0044 DOES NOT CONTAIN THE ADR-DRAFTING DISCIPLINE THIS PROJECT CITES IT FOR** — CONFIRMED by reading it in full at the phase-97 SPEC; it is about the BEHAVIOR_CONTRACT HTTP/1.1 subsection. The real discipline is the `> **STATUS: PROPOSED` blockquote plus a **RETAINED** italic footer (the ADR-0294-through-0319 shared block form), which the IMPL appends **after**, never replaces, with **no `**Status:**` line, no renumber and no `---`**. **No ADR documents it.** Recorded, not repaired; ⚠️ **quote NO count of the misattributing lines — any figure would be falsified by its own landing.**
60. ⚠️ **THERE ARE FOUR FIXTURE REGISTRATION GATES, NOT THREE, AND EVERY ONE OF THEM SKIPS RATHER THAN FAILS.** (1) `fixture.RegisterFixture` in the driver `init()`; (2) the blank import in `test/differential/runner_test.go`; (3) **byte-identity between the directory name and the registered string**; (4) the `NNNN-` / `NNNNa-` directory-name shape `discoverFixtures` enumerates. Gates 1-3 converge on a `t.Skipf`; **gate 4 produces no subtest and therefore not even a skip line.** **Score them on the fixture-set set-difference, never on the exit code.**
61. ⚠️⚠️ **AN ARM THAT IS GREEN BEFORE THE FIX IS NOT NECESSARILY A TDD FAILURE — IT MAY BE THE FALSE-AGREEMENT CLASS SHOWING UP INSIDE YOUR OWN SUITE.** At phase 97 the subject answers *"default chain"* on EVERY QUIC arm, so **four of the ten new arms (b, e, g, i) are GREEN at the un-fixed tip, structurally**, because *default* happens to be the right answer there. **Their only falsifiability is an NC roster row, which must therefore be scored PER ARM, not per run.** ⚠️ **Before reading a pre-fix green as a bug in the arm, ask whether the subject could have answered anything else.**
62. ⚠️ **A GOVERNING DOCUMENT'S ROSTER CAN CONTRADICT ITSELF ACROSS TWO SECTIONS, AND THE INTERSECTION IS THE FINDING.** Phase 96 §0.13 found the byte-untouched roster and the edit roster meeting at `manager.go`; phase 97 found the byte-untouched roster meeting the **PROSE-RECONCILIATION** set at the same file. **Enumerate the occurrence set of every claim your repair falsifies, resolve each hit to its file, and SET-DIFFERENCE that against the byte-untouched roster BEFORE writing the task list.** ⚠️ **A COMMENT-ONLY EDIT IS A DEFENSIBLE RESOLUTION IF, AND ONLY IF, THE COMMENT-ONLY CONSTRAINT IS MECHANICALLY GATED** — and that gate must itself be run over an input known to trip it.
63. ⚠️ **A SENTENCE CAN BE FALSIFIED BY A REPAIR IN TWO INDEPENDENT HALVES, AND A HALF-REPAIR MINTS A NEW STALE CLAIM.** *"`quicTLSConfig()` returns `rt.defaultChain.tlsCfg` FIRST, before consulting `chainByName`"* is falsified BOTH by the new precedence AND by `chainByName` no longer being consulted at all. **Say which halves die and which survive; LEAD WITH WHAT SURVIVES.** ⚠️ **AND ADJUDICATE PAST-TENSE NARRATION SEPARATELY** — two of the five hits narrate a *previously fixed* hole and are untouched by the repair.
64. ⚠️ **NAME THE MEASURE OR THE FIGURE IS MEANINGLESS — AND "LINE LENGTH" HAS FOUR ANSWERS.** `ROADMAP.md:229` reads **47234** bytes INCLUDING its trailing newline, **47233** excluding it, **46753** characters under `LC_ALL=C.UTF-8 awk length()`, and **47233** under `LC_ALL=C awk`. **The router's inherited figure reconciles only as bytes-including-newline.** ⚠️ **AND A STALE CLAIM'S OCCURRENCE SET IS RARELY THE ONE LINE THAT WAS NAMED** — the swallowed-panic claim lives on **FOUR** ROADMAP lines (136, 139, 159, 229) across TWO different phrases, and only `:229` sits inside a sentinel window.
65. ⚠️ **A HARNESS LIMITATION CAN LIVE IN THE DISPATCH LAYER WHILE THE PLUMBING IS ALREADY GENERAL.** The differential's Docker publishing and address maps are N-capable for UDP; what collapses a two-UDP fixture is `runFixture`'s **transport-blind, single-valued address dispatch** at two sites plus a fixture-wide nullary `ReferenceListenerIsUDP` marker. ⚠️ **RULE OUT THE OBVIOUS NON-CAUSES EXPLICITLY** — publishing and readiness were both fine, and there is **no per-listener readiness probe of any transport** in this harness at all.
66. ⚠️ **COUNT YOUR OWN DOCUMENT'S STRUCTURE WITH A COMMAND, NOT WITH YOUR MEMORY OF WRITING IT.** The phase-97 PLAN's self-review first claimed *"the largest tasks carry nine sub-steps"*; `awk` over `^- \[ \] ` per `^### Task ` heading read **eleven** for two of them, past `BOOTSTRAP_PROMPT.md` §6.1's ~10 line. **The false figure was caught before publishing only because the command was run.** ⚠️ **Every threshold your own stage sits near must be MEASURED and STATED, including the ones you would rather round down.**
67. ⚠️⚠️ **AN INVENTED GUARD FORM DOES NOT REPORT A WRONG NUMBER — IT REPORTS A DISARMED GATE AND A FALSE ZERO.** At the phase-97 router roll BOTH house guards were re-spelled from memory instead of copied from this file. The archive triple read **264 / 0 / 268** where the house forms read **163 / 71 / 234** ⚠️ **(that parenthetical 71 was the reading AT THAT TIP and is HISTORICAL — the live triple is now 163 / 73 / 236; see THE ARCHIVE GUARD)** — a **parenthetical ZERO**, the same false-zero class `PLAN.md` §0.1 refuted in `manager_test.go`, one stage later and in a different file. Worse, `^\*\*Status:\*\* PROPOSED` over `DECISIONS.md` returned **exactly ONE hit, at `:14866`, resolving to `## ADR-0231`** — which reads as *“the house guard is DISARMED and the tail ADR is unguarded.”* **It is ARMED**: the house form is `^> \*\*STATUS: PROPOSED`, it hits `:19054`, and that resolves by backward heading search to `## ADR-0319`. **COPY THE GUARD COMMAND, NEVER PARAPHRASE IT — a guard you re-spell can report the ABSENCE of the thing it guards, and an absence reads as permission.**
68. ⚠️⚠️ **A ROLLER THAT LEAVES A SPENT STAGE'S IMPERATIVE STANDING HAS MINTED A STALE INSTRUCTION — AND IT IS WORSE THAN A STALE FIGURE, BECAUSE A READER OBEYS IT.** The phase-97 IMPL rolled this file's header, live figures and owed-work list, and **still left FOUR blocks addressed to a stage that no longer exists**: a `DO NOT RE-DERIVE` directive for a closed phase's SPEC/PLAN, a `WHAT THE IMPL MUST NOT RE-DERIVE` section, a six-gate note reading **123/123** and **237** after the row had moved both, and a `PROPOSED` guard section headed **ARMED, AND YOUR STAGE DISARMS IT** when the row had already disarmed it. **Caught only by a separate audit pass, three days later, reading the file as the NEXT session would.** ⚠️ **WHEN YOU ROLL THIS FILE, GREP IT FOR `YOUR STAGE`, FOR YOUR OWN STAGE WORD, AND FOR EVERY FIGURE YOUR ROW MOVED — then read the result as the next reader, who has no idea which sentences you meant to leave.** ⚠️ **A `+0` claim about this file is exactly as falsifiable as one about the tree.**
69. ⚠️⚠️ **A BANKED "UNMEASURED" CLAIM CAN BE FALSE IN THE DIRECTION THAT KILLS THE CANDIDATE — AND THE REPAIR IT IMPLIES CAN BE AFFIRMATIVELY WRONG.** This file banked, three times over two phases, that the reference's `*.` SNI suffix is single-label. Measured at the phase-98 BRAINSTORM: **it is multi-label, exactly like envoy-go's**, and implementing the banked repair would have INTRODUCED a divergence into code that already agreed. **MEASURE A BANKED CLAIM BEFORE ADOPTING IT, NOT AFTER CHARTERING IT** — and note that the same measurement surfaced a *different*, real divergence on the same axis (longest-suffix precedence) that no document had named. **A wrong banked framing can hide a real defect behind a fake one.**
70. ⚠️⚠️ **ONLY THE REVERSAL ARM SEPARATES "LONGEST WINS" FROM "FIRST WINS."** Two chains that both match one input tell you nothing about precedence until you reorder the array and change **nothing else**. At phase 98 the reference returned the same chain under both orders on byte-length-identical configs — that, plus a third arm proving the losing chain still serves other traffic, is what makes "longest-suffix, order-independent" a measurement rather than a guess. **A single ordering is one sample of a two-sample question.**
71. ⚠️⚠️ **A TEST CAN BE SATISFIED BY ITS OWN FIXTURE'S NAME.** `TestNewManager_MultiChain_TooManyCatchAlls_Errors` guards on `strings.Contains(err.Error(), "catch")` and names its listener `l_2catch`, so the error message it claims to pin is irrelevant: measured with the production check deleted, `contains catch: true` **from the listener name alone**, and the guard does not fire. **When a guard uses a substring predicate, enumerate everything else in scope that contains the substring** — the sibling of the vacuous-golden-roster class, one level lower.
72. ⚠️⚠️ **A CENSUS INSTRUMENT CAN COUNT ITSELF, AND THIS FILE HAS DONE IT.** The port-band census sentence claimed **two** `156xx` hits; re-run, it read **three**, because the sentence spells both numbers and is a hit in its own census. **The figure was false at its own publishing commit.** Same self-incrementing class as the archive's positive controls. **Whenever you record a count of a token in a file that will contain your recording, name what each hit IS and expect your own sentence to be one of them** — or record no number.
73. ⚠️ **`NOT CONSTRUCTIBLE` IS A REAL VERDICT AND IT NEEDS A NAMED MECHANISM, NOT A SWEEP.** Phase 98 proved `catchAllCount > 1` unreachable without the identical-spec check with a 91-pair sweep — but the sweep alone would have been an ABSENCE claim. What makes it a proof is naming the foreclosing mechanism: `chainSpecKey` short-circuits `if s.Empty { return "EMPTY" }`, so any two Empty specs collapse to one constant key unconditionally. **State the mechanism, then let the sweep corroborate it.**
74. ⚠️⚠️ **A PROBE THAT STOPS ONE STEP SHORT OF THE PROPERTY IS NOT A CONTROL.** The phase-98 BRAINSTORM's positive-control row drove a request on the reference and only BUILT a manager on the subject (`build OK`). The second divergence of the row sat in exactly that cell, and the fixture chartered on top of it would have been RED against its own repair. **Drive BOTH sides to the observable, every arm.**
75. ⚠️⚠️ **A CHARTERED GATE THAT CANNOT BE BUILT HONESTLY IS A SCOPE SIGNAL.** When the matched-positive arm a row needs fails against the row's own repair, the options are (a) widen the repair, or (b) bend the gate around the divergence (here: add `tls_inspector`). **(b) produces a GREEN fixture over a live divergence in the exact dimension it claims to cover — the worst outcome available.** Choose on the gate's honesty, not on size.
76. ⚠️ **AN NC ROW CAN BE WRONG ABOUT WHAT *DOESN'T* REDDEN AS EASILY AS ABOUT WHAT DOES — AND A VALUE-PRESERVING MUTATION IS INVISIBLE TO EVERY ARM WHOSE INPUT ALREADY HAS THAT VALUE.** The phase-98 SPEC's first draft had both errors in one row (§0.10). **For each row, trace the mutation through EVERY arm, including the ones listed under "must NOT redden".**

77. ⚠⚠ **A GOVERNING DOCUMENT'S OWN INSTRUCTION CAN BE THE DEFECT, AND OBEYING IT LITERALLY CAN DELETE WORK.** `98/SPEC.md` §14 item 1 told the PLAN to *"re-run §6's matchers — the union may grow."* Run as written, **all six are BLIND to `types.go:44-46`**, one of the code sites §6 itself lists as a mandated EDIT. The tables had been built partly by READING, so they are a **SUPERSET** of what the matchers produce. **Inherit the TABLES; use the matchers only to look for GROWTH. A re-derivation that LOSES a row is not a re-derivation, it is a DELETION** — and it arrives wearing the authority of the document that asked for it.
78. ⚠️ **A LINE-BASED MATCHER IS BLIND TO A CLAIM THAT SPANS A LINE BREAK.** `git grep` and `grep` match within one line. The `types.go` claim reads `… or "" if no listener` / `// filter inspected the connection.` across two lines, so `no listener.?filter` cannot span it — while a **positive control** (`git grep -nF -- 'no listener'` on the same file) proves the file is perfectly reachable. **When a matcher reports an absence, ask whether the thing it sought could fit on ONE LINE.**
79. ⚠️ **A MATCHER'S *ESCAPING* CAN BE WRONG WHERE ITS VOCABULARY IS RIGHT.** `§6`'s M1 is the literal `must be "tls"`; the Go source writes `must be \"tls\"`, so M1 reads **ZERO** against the very production file it is named for, and `manager.go:998` is reachable only through the escaped spelling. **Run BOTH spellings of any literal that crosses a language boundary.** Sibling of notes 49 and 56, one level lower: not what it SPELLED, but how it QUOTED.
80. ⚠⚠ **A COMMENT-ONLY EDIT CAN STILL BREAK FIFTEEN CITATIONS — "comment-only" AND "line-count-neutral" ARE TWO INDEPENDENT CONSTRAINTS, AND ONE GATE CANNOT CATCH BOTH.** `quic_test.go` carries **15** `chainmatch.go:<line>` cites, **all below line 32**, while the mandated comment edit sits at `:30-32`. A four-line comment replacing a three-line one **passes** a comment-only gate and staleys all fifteen in the commit that "reconciles" the prose. **Gate the SHAPE (`3	3`) as well as the KIND.**
81. ⚠⚠ **A NAME-PRESENCE PIN CAN BE VACUOUS ON *BOTH* SIDES.** Method note 46 warns that pinning a name one side never emits is silently vacuous. The phase-98 PLAN measured the **opposite** failure: both sides publish the zero-serving chain's `http.<prefix>.downstream_rq_total` **before any traffic at all** (a pre-traffic reference scrape read **168** matching lines with zero connections), because the per-chain HCM scope is created at **CONFIG** time, not at first request. **So "the name is present" is satisfied from BOOT and proves nothing. The only non-vacuous pin is the VALUE, and a value needs its MIRROR ARM.** ⚠️ **And `stats sinks emit only USED stats` does NOT transfer to the admin `/stats` or `/stats/prometheus` endpoints on either side.**
82. ⚠⚠ **A CONFIG KNOB CAN BE PARSED, RANGE-VALIDATED, DOCUMENTED AS HONORED — AND NEVER ENFORCED.** envoy-go parses `listener_filters_timeout`, rejects it outside `[1s, 60s]`, stores it, passes it to `Pipeline.Run`, and `Pipeline.Run` builds a real `context.WithTimeout` from it — **and it still never fires**, because the deadline is only consulted AFTER the blocking call returns and the blocking call has no deadline of its own. `BEHAVIOR_CONTRACT.md:4359` says it is *"honored"*. **A knob's plumbing is not its enforcement: follow the value to the thing it is supposed to INTERRUPT, and drive an arm that makes it interrupt.**
83. ⚠⚠ **A "TOOL CANNOT RUN" VERDICT CAN BE AN UNNAMED *VARIABLE*, AND THE OBVIOUS REPAIR FOR IT CAN BE WRONG.** For two stages this file recorded `golangci-lint` as unrunnable and gate (b) as red for "a toolchain reason", as TWO departures. At the phase-99 BRAINSTORM one variable — `GOTOOLCHAIN=go1.26.2` — turned BOTH green, and the lint zero was proven live by a planted-violation control. **Rebuilding the linter under the new toolchain (the obvious fix) still failed**, because the broken reader was vendored. **Before banking "cannot run", find the variable that separates the last green run from this one, and test the fix you would reach for FIRST.**
84. ⚠️ **A PROBE CLIENT CAN REWRITE THE INPUT BEFORE IT LEAVES THE MACHINE.** curl LOWERCASES the SNI it sends (captured with `openssl s_server -tlsextdebug`), so every "uppercase SNI" arm driven with curl is silently a lowercase arm. **Capture what the client SENT, on the wire, before believing an arm about it** — sibling of note 7h.
85. ⚠️ **READ THE TEST BEFORE PRESCRIBING ITS RE-POINT.** The phase-99 BRAINSTORM's first draft said `TestSelectChainAmbiguousReturnsError` must be re-pointed because the repair removes ambiguity; reading its arms showed an identical-`transport_protocol` shape the repair cannot reach. **A test's NAME is its subject; its INPUT is its coverage** (note 35).
86. ⚠️⚠️ **A PROTOTYPE THAT GETS EVERY ARM ITS AUTHOR RAN RIGHT CAN STILL BE THE WRONG SHAPE — THE ARM THAT KILLS IT IS THE ONE NOBODY RAN.** The phase-99 BRAINSTORM's `22 0` agreed with the reference on all fourteen of its rows; the mixed exact+wildcard arm it had itself named as INFERRED (§0.11) was the one that refuted it. **An arm a document lists as "inferred, not measured" is the next measurement, not a footnote.**
87. ⚠️ **A REPAIR CAN MOVE A CONFIG FROM "SERVED" TO "CLOSED" AND STILL BE THE PARITY DIRECTION — OR NEITHER.** P2 closes `O1`, which the tip served by an accident of whole-set rank; the reference REFUSES `O1`'s class at validate, so neither side matches. **Before calling a served->closed change a regression, measure what the REFERENCE does with that CONFIG — including whether it boots at all.**
88. ⚠️ **A NON-COMPILING PLANTED-VIOLATION CONTROL IS A MASKED CONTROL.** A lint control with an unused variable fires only `typecheck`, which hides every other linter; the control is valid only when the target linters each fire on a file that COMPILES (`SPEC.md` §10). Sibling of method note 51, in tooling.

## ⚠️ THE `PROPOSED` GUARD — **RE-ARMED AT THE phase-99 SPEC UNDER `## ADR-0321`; THE phase-99 IMPL DISARMS IT (a PLAN does not touch it)**

| form | now | where |
|---|---|---|
| `^> \*\*STATUS: PROPOSED` — **the house form** | ✅ **ARMED — ONE line, resolving BY BACKWARD HEADING SEARCH to `## ADR-0321`** | The phase-99 SPEC drafted `## ADR-0321` §Context with the `> **STATUS: PROPOSED` blockquote plus the RETAINED italic footer (the ADR-0294-through-0320 shared form), **no `**Status:**` line, no `---`**. **YOUR STAGE — THE PLAN — LEAVES IT ARMED** (a PLAN's scope does not include `DECISIONS.md`; measured: the phase-98 PLAN `b4c5e97a` touches FOUR paths and NOT `DECISIONS.md`). The phase-99 IMPL appends §Decision + §Consequences AFTER the footer and flips `PROPOSED` -> `ACCEPTED` in place, which DISARMS it. ⚠️⚠️ **A DISARMED HOUSE GUARD AND A BROKEN MATCHER READ IDENTICALLY — `0` is both.** Distinguish them by reading the TAIL ADR's status directly, never by the count. |
| `^\*\*Status:\*\* PROPOSED` — **the ADR-0231 decoy** | unchanged, still hits | `:14866`, resolved BY BACKWARD HEADING SEARCH to `## ADR-0231`; **BYTE-UNTOUCHED across all four phase-98 stages and the phase-99 BRAINSTORM and SPEC** (still `:14866` at the SPEC close) |
| `^\*\*Status:\*\*.*PROPOSED` — **the middle ground** | a large number | ⚠️ **NEVER A GATE** |

⚠️⚠️ **THE POLARITY HAS INVERTED AGAIN.** Now BOTH forms hit — the house form at `## ADR-0321` and the decoy at `## ADR-0231` — so **running only the decoy reads as "armed" for the WRONG reason, and running only a broken house matcher reads as "disarmed".** **Verify BY LINE AND BY ADR, never by the count, in EITHER direction.** ⚠️ **COPY THE GUARD COMMAND, NEVER PARAPHRASE IT** — at the phase-97 router roll both house guards were re-spelled from memory and reported a **parenthetical ZERO** and a DISARMED gate that was in fact ARMED.

⚠️ **VERIFY BY LINE AND BY ADR, NEVER BY THE COUNT ALONE** (`awk 'NR<=<HITLINE> && /^## ADR-/ {h=$0; n=NR} END {print n, h}'`). ⚠️ **NEVER gate on the UNANCHORED form.** ⚠️ **AND DO NOT QUOTE A COUNT FOR EITHER GATE FORM IN PROSE THE GREP MATCHES.** `DECISIONS.md` at this close: **19381**, `^---$` **216**, `^## ADR-` **320**, bare `^## ` **328**, tail **ADR-0321** (`PROPOSED`), next-free **ADR-0322** — the phase-99 SPEC moved all but `^---$`, re-derived in its own commit. ⚠️ **A PLAN MOVES NONE OF THEM; the IMPL moves the line count only** (no new heading, no `---`). ⚠️ **NEVER derive next-free from the heading count** — the id space is sparse at the `0209` gap — ⚠️ **and the heading regex `^## ADR-[0-9]+[:—]` is itself holed by `## ADR-0127 v2`.**
⚠️ **THE `BEHAVIOR_CONTRACT.md` LEDGER QUESTION IS SETTLED AND DISCHARGED — DO NOT RE-LITIGATE IT.** A `+0` row DOES earn a ledger chain entry, and **EIGHT** `+0, UNCHANGED` entries now exist (44.2, 44.3, 45.2, 47.1, 51, 96, 97, 98). **Follow the phase-96/97/98 FORM, not the sibling `A → B` form, and quote NO ABSOLUTE** — three mutually inconsistent stat-surface absolutes are live in this tree at one tip, and on a contested count the answer is **no number**. ✅ **The phase-99 SPEC CONFIRMED `+0` NAMES (`SPEC.md` §9) and specified the entry the IMPL writes after `BEHAVIOR_CONTRACT.md:5146`.**

## ⚠️ THE ARCHIVE GUARD

⚠️ **THE STRICT `163` IS NOT THE ENTRY COUNT.** Re-measured at THIS tip (phase-99 **SPEC** close, `STATE_HISTORY.md` **582**): loose **241** = strict **163** + parenthetical **78**, exactly — **the triple reconciles only under these ANCHORED-OCCURRENCE forms:**
```sh
H=docs/envoy-go/STATE_HISTORY.md
grep -oE '^- \*\*prior active-phase:\*\* ' $H | wc -l   # strict         163  <- DELTA 0 across a correct close
grep -oE '^- \*\*prior active-phase \('    $H | wc -l   # parenthetical   78  <- +1 per close
grep -oE '^- \*\*prior active-phase'       $H | wc -l   # loose          241  = 163 + 78
```
⚠️ The unanchored `grep -c 'prior active-phase'` counts LINES and coincides by accident; the colon form is a FOURTH regex. **Name the form or the figure is meaningless.** ⚠️ **`grep -F` WITH A PATTERN STARTING `- ` HITS THE FLAG TRAP** — pass `--` first. **The guard is SOUND as a DELTA-0 SHAPE check** but the number is a SUBSET. ⚠️⚠️ **AND A SELF-WRITTEN SUBSTITUTE IS WORSE THAN A WRONG ONE:** at `01e9d72b` the invented forms `^- \*\*` / `^- \(` / `^- ` read **264 / 0 / 268** against the house forms — **a parenthetical ZERO, which reads like the archive lost every entry of that shape.** Method note 67.

⚠️ **THE ARCHIVE'S POSITIVE CONTROLS ARE SELF-INCREMENTING.** The house rule is **name the LABEL, never the COUNT**: any FIGURE recorded in the file the control measures is invalidated by the act of recording it. ⚠️ **THE RAW LINE DELTA IS `+2`, NOT `+1`** — a blank line PLUS the entry line. **The phase-99 SPEC close: `STATE_HISTORY.md` 580 -> 582**, evicting the phase-97 IMPL entry, strict **163, DELTA 0**, parenthetical **77 -> 78**, loose **240 -> 241**.
✅ **THAT CLOSE'S DATE READ PICKED ALONE** — the MEASURED pre-roll list was **TWO `09-20`, one `09-16`, one `09-12`, one `09-09`**: a UNIQUE tail. ⚠️ **The post-roll list reads one `09-21`, TWO `09-20`, one `09-16`, one `09-12` — the tail (`09-12`, the phase-98 BRAINSTORM) is UNIQUE again, so YOUR close's date read CAN pick alone.** ⚠️ **MEASURE IT ANYWAY, AND REMEMBER THE CLASS (method note 26): a projection that forgets the entry your OWN close promotes is wrong by exactly one. YOUR close promotes the phase-99 SPEC entry (`09-21`) to the head.**
⚠️ **THE BARE FORMS ANSWER NOTHING.** **Only the LABEL-BOUND PAIR discriminates**, run on BOTH files, with a fabricated-label NC reading zero in both and a positive control on a label that IS present in the archive and ABSENT from §Recent. **Both directions were verified at the phase-99 SPEC close**: the evictee's own label went `STATE.md` **1 -> 0** and `STATE_HISTORY.md` **0 -> 1**, the fabricated-label NC read **0 / 0**, and the positive control read **0 in `STATE.md` / present in the archive**.

## ⚠️ PORT BANDING

`net.ipv4.ip_local_port_range = 32768 60999`. ⇒ **BAND YOUR PROBE PORTS BELOW 32768.** ⚠️ **The differential harness reserves `20000..31007` (`harness_test.go:292-296`) and `11000..14999` (`:209-212`)**, so `15000-19000` is the ad-hoc band — ⚠️ **minus `18080-19000`, held by sibling `curl-world` containers, and minus the FIXTURE `15xxx` literals.**
🔴 **THE `15000-19000` CONVENTION IS EXHAUSTED BELOW 16000.** Bands SPENT: phase-96 IMPL `15500-15599`, phase-97 BRAINSTORM `15700-15799`, phase-97 SPEC `15800-15899`, phase-97 IMPL `15900-15999`, phase-98 BRAINSTORM `15600-15699`, phase-98 SPEC `16000-16099`, phase-98 PLAN `16100-16199`, phase-99 BRAINSTORM `16200-16299`, **phase-99 SPEC `16300-16399`** (reference agent `16301`/`16302`/`16311`/`16312`, subject agent `16350-16399`, all cleanly; left only client TIME-WAIT). ⇒ **YOUR STAGE SHOULD TAKE `16400-16499` IF IT PROBES AT ALL** — ⚠️ **this sentence spells both ends of that band, so it is a hit in YOUR census. CENSUS IT YOURSELF (`git grep -In '164[0-9][0-9]' -- test/ internal/ cmd/`), SCOPE IT, USE `-I`, AND SAY WHAT EACH HIT IS** — at the SPEC the `163xx` census hit only the literal `16384` (byte sizes), and `164xx` will likely hit similar non-port literals. ⚠️ **Fixture `0124`'s chartered reference ports are `15124` (primary, by convention) plus `15225`, `15226`, `15227` (`SPEC.md` §7.2: ZERO files at the SPEC tip apart from `15124`'s two reserving-prose hits in `0123`) — not an ad-hoc band. RE-CENSUS at your tip.**
⚠️⚠️ **AND THE CENSUS INSTRUMENT CAN COUNT ITSELF.** This file's previous census sentence claimed **TWO** `156xx` hits; re-run at the phase-98 BRAINSTORM it read **THREE**, because that sentence spells both numbers and is therefore a hit in its own census. **The figure was false at its own publishing commit.** Same self-incrementing class as the archive's positive controls. **Name what each hit IS, and expect your own sentence to be one of them.**
⚠️⚠️ **THE FIXTURE PORT BANDS ARE FAMILY CONVENTIONS WITH LOCAL EXCEPTIONS, NOT INVARIANTS — CENSUS THE BAND YOU ARE JOINING.** **28 distinct `15xxx` literals** live under `test/fixtures/*/driver` and `*/inputs`, spanning `15000`-`15104` plus `15360`, with collisions at `15000` and `15050`. What IS true: the highest reference port is **`15122`** (fixture `0122`, phase 97), and the convention that produced it is `15000 + <fixture index>` — **a derived observation NO document states.** ⇒ **fixture `0123` is CHARTERED at reference ports `15123` (primary, on convention) plus `15223` and `15224` (off-convention so `0124`/`0125` keep theirs) — all read ZERO files at the SPEC tip under `git grep -c '\b<port>\b' -- test/ internal/ cmd/`; RE-CENSUS before use.** The TLS/SDS fixtures `0108`-`0113` hold `10444`-`10449` and `0103` holds `10443`; **`10450` IS HELD BY NOTHING**, its only occurrence under `test/` being `0118/driver/driver.go:31`'s own warning comment.
**Check availability with `ss -tan` AND `ss -uan` (ALL states), never `ss -ltn`.** ⚠️ **FOR A UNIT TABLE, USE PORT 0.** ⚠️ **`net.Pipe` DEADLOCKS a client-cert handshake — use a loopback TCP pair.**

## ⚠️ A RECURSIVE `grep` IS BLIND TO `next-prompt.txt` IN **EVERY** SHELL HERE

🔴 **CORRECTED AT THE PHASE-97 PLAN — see method note 3j.** This section used to say the blindness was CONTROLLER-ONLY and that a subagent shell carried GNU `grep`. **It does not.** `type grep` in a subagent shell returns the same function wrapping **ugrep 7.8.4** (`-G --ignore-files --hidden -I --exclude-dir=.git`), which honours `.gitignore`; `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. `/usr/bin/grep` (GNU 3.11), `command grep -rl`, `git grep` and direct-path access all see it. ⚠️ **`command grep` DOES NOT SURVIVE `xargs`.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files). ⚠️ **ugrep ALSO REJECTS SOME COMPLEX `grep -oE` PATTERNS with `exceeds complexity limits` where GNU grep accepts them.** **NAME THE BINARY IN EVERY BRIEF YOU ISSUE, AND CROSS-CHECK A FIGURE UNDER BOTH BEFORE TRUSTING EITHER.**

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 99 (chain-match-sni-longest-suffix)' --format='%h %s'   # the SLUG form — the phase-99 BRAINSTORM and SPEC landed on it
git log --grep '^phase 99' --format='%h %s'                 # the LOOSE form — RUN IT to CROSS-CHECK; it must list the phase-99 BRAINSTORM AND SPEC
```
⚠️ **RUN BOTH FORMS AND RECONCILE THE SETS. NO COUNT IS QUOTED HERE, DELIBERATELY — BOTH FORMS ROT, A SLUGLESS CORRECTION COMMIT IS INVISIBLE TO THE ANCHORED FORM, AND A COMMIT MISSING ITS *STAGE WORD* IS INVISIBLE TO A STAGE-ANCHORED ONE** (method note 57; the live witness is phase 95's IMPL, `0f3b98b3`). **Take the tip from `git rev-parse master`.**
⚠⚠ **SHARPENED AT THE PHASE-98 PLAN — THE BLINDNESS IS IN THE *STAGE-WORD FILTER*, NOT IN THE LOOSE FORM.** `git log --grep '^phase 95 (.*) IMPL'` reads **ZERO** (phase 95's IMPL is `0f3b98b3`, whose subject carries no stage token), but `git log --grep '^phase 95'` reads **six and DOES contain it**. So the loose form is not the cure by itself: **post-filtering it on the same stage word (`| grep -i IMPL`) reproduces the blindness exactly.** **Reconcile the SETS, not the greps** — list the loose form's commits and read their subjects.

Execution style: **subagent-driven per `feedback_execution_style`**. Subagents commit LOCALLY on their own stage branch with EXPLICIT PATHSPECS; the controller merges and squashes (`feedback_subagents_no_push`, `feedback_push_to_origin`). **Pure MEASUREMENT agents should commit NOTHING and prove their tree clean.** ⚠️ **PARALLEL AGENTS IN ONE WORKTREE RACE THE GIT INDEX** — per-agent worktrees on their own branches, private scratch dirs, **Docker serialized to exactly ONE agent at a time.** ⚠️ **THE PHASE-98 BRAINSTORM RAN TWO AGENTS IN PARALLEL SAFELY BY SPLITTING ON THE DOCKER AXIS** — one Docker-only reference agent and one Docker-free subject agent, disjoint port bands, disjoint worktrees. **That split is the reusable pattern.**
⚠️ **VERIFY WHAT A SUBAGENT REPORTS — BUT ALSO BELIEVE IT WHEN IT REFUTES YOU.** At the phase-98 BRAINSTORM the subject agent refuted **both** hypotheses its own brief offered for the load-bearing arm (neither "order-dependent" nor "longest-suffix" — the answer was an ambiguity error) and was RIGHT. At the phase-96 IMPL an agent refuted the controller's brief and was RIGHT; at the phase-97 BRAINSTORM one was WRONG because its matcher was blind to the wording; at the phase-97 SPEC one was WRONG because it ran a different command than the claim named (notes 49 and 56). **Verify BOTH directions against the source.** ⚠️ **An agent's own SUMMARY LINE can disagree with its own TABLE — count the table.** ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root** — build with `-o` into scratch.

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root — **it fired on roughly every other call at the phase-98 BRAINSTORM, with an explicit `Shell cwd was reset` line.** Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` before any commit. ⚠️ **Shell VARIABLES do not survive between Bash calls** — re-export in every command. ⚠️ **AN UNQUOTED HEREDOC (`<<EOF`) COMMAND-SUBSTITUTES BACKTICKED TEXT** — use `<<'EOF'`.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE OR PROTOTYPE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — or better, **build it in a THROWAWAY worktree and remove that worktree**, which is what both phase-98 agents did. ⚠️ **Also check the MAIN repo root, and `--untracked-files=all`** — note the pre-existing untracked `.claude/` directory is NOT yours and is present in every snapshot; filter it out rather than "cleaning" it.

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads WRITE `PLAN.md` FOR PHASE 99.** Its §Recent is FIVE entries under the ADR-0288 cap, and its `lifecycle-state:` reads **2**.
2. `docs/envoy-go/ROADMAP.md` — **row 99 at `:161` and the six deferred-candidate windows (`:209 :215 :221 :231 :237 :245`)**. ⚠️⚠️ **DO NOT EDIT, TIDY OR REFLOW THE WINDOWS. THE MARGIN IS ONE, AND DELETING THE LAST ONE ENDS THE PROJECT.** A PLAN edits no row.
3. `docs/envoy-go/phases/99-chain-match-sni-longest-suffix/SPEC.md` — **697 lines, YOUR INPUT.** §4.1 the P2 code · §5.2 the per-arm matrix · §6 the occurrence TABLES · §7 fixture `0124` · §10 the toolchain fold-in · §11 the NC roster · **§15 the EIGHT items your PLAN owes.** ⚠️ **It is a CLAIM like any other: its `O1` row is PREDICTED, its `26 23` is a FLOOR, and its §6 tables are one stage's union.**
4. `docs/envoy-go/phases/99-chain-match-sni-longest-suffix/BRAINSTORM.md` — **537 lines, EVIDENCE.** ⚠️ **Its §6 `22 0` prototype is REFUTED (`SPEC.md` §0.1). Do not plan from it.**
5. `docs/envoy-go/phases/98-chain-match-transport-protocol-reject/PLAN.md` — **the FORM your stage must produce** (a 20-task TDD spine; §7's COMMENT-ONLY diff gate with four controls is REUSABLE for `quic.go`). ⚠️ **Read for SHAPE, not as instructions (method note 77).** Precedent PLAN scope: **four** files (method note 22).
6. `BOOTSTRAP_PROMPT.md` **at the repo root** — **§5 state 2 -> 3** (`SPEC.md` exists, `PLAN.md` does not ⇒ write `PLAN.md`) and **§6.1's split gate (~25 tasks / ~1500 LoC) — EVALUATE IT WITH A COMMAND over your own task list.** ⚠️ **§5 appears VERBATIM TWICE — find the second copy by HEADING, never by arithmetic.**
7. `docs/envoy-go/DECISIONS.md` — **`## ADR-0321`** (the tail, `PROPOSED`, §Context ¶1-¶7), **ADR-0081** clauses 4-5 (`:3152-3157`, amended by ADR-0321), **ADR-0078** clause 9 (`:3208`).
8. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — **5996 lines**; `### Chain-match algorithm` at `:4363-4369` (the IMPL edits `:4367` and ADDS to `:4368`); the ledger's phase-98 `+0` entry at `:5146`. ⚠️ **`:4359` IS KNOWN FALSE** (`listener_filters_timeout` *"honored"*) and is deliberately unrepaired.

## What the next session owes
**WRITE `PLAN.md` FOR PHASE 99 (lifecycle-state 2 -> 3)** — ⚠️ **ONE STAGE PER SESSION.** Concretely, discharge `SPEC.md` §15:
1. 🔴 **ORDER THE SPINE SO THE UN-FIXED TIP IS MEASURED FIRST, PER ARM** — land §5.1 (with the NEW `O1` row), §5.3's QUIC arm and fixture `0124`, run them RED, record which rows are RED and which are structurally green, THEN land P2. ⚠️ **SCORE THE `O1` ROW BEFORE BELIEVING ITS PREDICTED COLOUR.**
2. **Inherit `SPEC.md` §6's TABLES** — re-anchor every site by LITERAL TEXT at your tip; matchers for GROWTH only (method note 77).
3. **Gate `quic.go`'s comment-only, LINE-COUNT-NEUTRAL edit mechanically** (`98/PLAN.md` §7, NC'd) — census `quic_test.go`'s cites into `quic.go` first (method note 80).
4. **Build §11's FIVE NC rows as executable patches** and prove each CAN fire (method note 7d) — ⚠️ **row 1 (inversion) cannot score the `R_*` rows and reddens `M1/M2` for a LENGTH reason; rows 2 and 3 exist for that.**
5. **Prove the per-chain counter NAMES exist on both sides** before `0124` pins VALUES (method notes 46, 81).
6. **Evaluate BOOTSTRAP §6.1 WITH A COMMAND** (`SPEC.md` §12.3: phase 98's IMPL landed ~1846 non-PROGRESS added lines unsplit; this row is the same order).
7. **The toolchain FOLD-IN as its own tasks** (`SPEC.md` §10): the XFL re-point RED-first under go1.27.1, both plumbing mutations, lint under `GOTOOLCHAIN=go1.26.2` with a COMPILING planted control (method note 88).
8. **Keep §0.4's order-dependent fold OUT of the spine** — banked above.
9. **A PLAN RUNS NONE OF THE SIX GATES AND WRITES NO PRODUCTION CODE — NAME THAT AS SCOPE.**
- `STATE.md` rolled **IN PLACE** with the oldest §Recent entry evicted — ⚠️ **the LABEL-BOUND PAIR across BOTH files, a fabricated-label NC and a positive control naming an ARCHIVED label.** Archive as **ONE INLINE LINE** in the PARENTHETICAL form, **strict guard DELTA 0**, raw delta **+2**, naming the LABEL and NO FIGURE. ⚠️ **MEASURE the histogram including the entry YOUR close promotes.**
- ⚠️ **A PLAN's FILE SCOPE IS FOUR PATHS** (method note 22: `e69f1c58` `90010c4c` `db539e7d` `f647dd72` `0eab42e3`, each `STATE_HISTORY.md` `2 0`) — **`git show --numstat` them; wording is not scope.**
- **ROLL THE §Recent PREAMBLE SENTENCE TOO, without spelling the evictee's label** · ⚠️ **RE-DERIVE ANY LINE COUNT YOU QUOTE IN THE SAME COMMIT AS THE EDIT THAT MOVES IT** · `next-prompt.txt` rolled (`git add -f`) · ONE squashed commit, merged and pushed, **with the FULL SLUG *AND* THE STAGE WORD in its subject** · ⚠️ **`git worktree remove` YOUR OWN STAGE WORKTREE AT CLOSE.**
- ⚠⚠ **WHEN YOU ROLL THIS FILE, GREP IT FOR `YOUR STAGE`, FOR YOUR OWN STAGE WORD, AND FOR EVERY FIGURE YOUR ROW MOVED** — then read the result as the next reader, who has no idea which sentences you meant to leave (method note 68).
