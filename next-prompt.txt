=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-VERIFIED 2026-07-19 (re-run MECHANICALLY at the phase-68 IMPL close, in the stage worktree, POST row-68 flip) — it does NOT fire. `stop` was NOT created.** Actual output:
- **(1) prints NOTHING** — every chartered row (incl. row 68) is `done`. Check (1) alone no longer blocks `stop`; checks (2)+(3) do.
- **(2) prints THREE live "candidates:" sentences** — HTTP/3, xDS, Observability ⇒ three families STAY OPEN. **Cite the command, never the adjective** (`reference_sentinel_deferred_sentence_live_vs_historical`): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` ⇒ **3**.
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** ⇒ THREE families never opened.

Checks (2)+(3) print ⇒ the sentinel does NOT fire. Abundant work remains.

==============================================

## STATUS AS OF 2026-07-19: THE PHASE-68 IMPL IS COMPLETE AND LANDED. ROW 68 IS `done`. THIS SESSION PERFORMS THE PHASE-69 BRAINSTORM (the roller SELF-PICKS the subject).

**What the phase-68 IMPL landed** (`git log --grep 'phase 68'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): the full 9-task spine, subagent-driven with per-task adversarial reviews + a final whole-branch review. **T1** `internal/xds`: exported `ErrEmptyValidationContext` beside `errValidation`, wrapped ONLY at the fully-absent-`trusted_ca` (S1) site in `parseValidationSecret` — **NARROWED at the final whole-branch review**: the PLAN's S1b disjunct (`trusted_ca:{}`, specifier-unset) was landed then REMOVED pre-merge as a Critical — it contradicted the ADR-0287/SPEC-66 §3.9(b) LIVE probe (the reference PGV-NACKs that served shape: `specifier … is required`); S1b now stays boot-FAIL beside S2/S3, and ADR-0290/BC/STATE were reconciled (see `reference_sds_empty_ack_narrow_classifier` — classify each served shape by REFERENCE evidence, never by internal parser routing). Dual-`%w` in `applyValidationResponse` (the `applyResponse` twins + empty-resources wrap untouched — RD-DUPSITE honored); `provider.go` BYTE-UNTOUCHED (NACK posture stands, D-EDF-STREAMPOSTURE (b)). **T2** `internal/tls/config.go`: the CVC empty-dynamic fallback (`errors.Is` sole gate → `default_validation_context.trusted_ca` via `loadTrustedCAPool`+`installPool`; empty-both → the phase-67 no-anchor routing, reject byte-identical; S2/S3/timeout stay boot-FAIL; P5 block byte-intact; every path returns). **T3** `sdsserver.WithEmptyValidationContext` (S1, distinct from `WithValidationContext(name,nil)`=S2; Break F proved the discriminator). **T4** fuzz `"downstream-sds-empty"` side + seed, all three dispatch arms verified (+0 fuzzers). **T5/T6** fixture `0111-tls-cvc-empty-dynamic-fallback` (fixtures 112→113, port 10447; first-run PASS; Break G union-pool FIRED the untrusted-arm structuralCheck in forced-send mode, the bare-polite swap was the EXPECTED non-fire = the RD3 confirmation; Break H triplet + per-side pin + served-this-arm all fired; a reviewer re-executed Break I live, exact match). **T7** BC B1–B4 (item 3(a) CLOSED; item 10 NARROWED; the old item-3(b) `trusted_ca:{}` boot-FAIL naming was retired at T7 then RE-REVERSED at the final review — S1b IS boot-FAIL; BC item 3 now names it beside S2/S3 with the PGV reason). **T8** six-gate ALL GREEN + full differential `ok 379.894s` (113 dirs) + `-race` + cycle guard (stats+xds only) + envelope audit exact. **T9** ADR-0290 COMPLETED IN PLACE (STATUS COMPLETE; tail ADR-0290, next-free ADR-0291) + row 68 → `done` + STATE/ROADMAP rolled + memory updates (`reference_go_client_cert_withholding` extended with RD3; NEW `reference_sds_empty_ack_narrow_classifier`). Counts at exit: fixtures **113** · fuzzers **55** · stat **1201** · BackendKind **38** · go.mod modules **2** · DECISIONS tail **ADR-0290 COMPLETE**.

**⚠️ PUSH (idempotent — evaluate, then act):** FIRST ACTION: `git fetch`; if origin/master is BEHIND local master and the local tip is the phase-68 IMPL squash, `git push origin master` per `feedback_push_to_origin`; if in sync, proceed; if REJECTED (diverged), STOP and reconcile — never rebase away landed squashes.

## What THIS session does — phase 69 BRAINSTORM

One stage per session: BRAINSTORM ONLY (docs-only, ZERO production `.go`). Fresh worktree off master per `feedback_git_worktrees`; subagent-driven (`feedback_execution_style`); adversarially verified before landing; register the new row `in-progress` in ROADMAP at stage-close; roll this router to the phase-69 SPEC.

**The roller SELF-PICKS the subject per the 2026-07-12 standing directive** — smallest defensible candidate first, the FULL rejected roster adjudicated in the BRAINSTORM (not one-liners). Live candidate pools (from sentinel check 2 + check 3): the three open families' deferred lists (HTTP/3 · xDS · Observability, quoted in ROADMAP) + the three never-opened families (gRPC · Runtime · WASM). Standing adjudication hints the pick must engage (re-derive, don't re-litigate):
- **OTLP-metrics stats sink** was named "the strongest FUTURE pick" at the phase-68 BRAINSTORM adjudication (a NEW multi-leg sink — it lost on SIZE, not value; ADR-0045 split valve relevant).
- **Upstream SDS** carries a flagged VALUE-level-cycle block (the phase-67 roll: the `internal/xds` cycle guard is TYPE-level only and PASSES a value-level cycle — do NOT re-derive this from scratch, `reference_xds_config_seam_transitive_cycle_guard`).
- `reference_deferred_candidate_cost_restale` — re-derive every candidate's COST at the current tip (phase-68 just built the S1 classifier + the sdsserver S1 Option + the 0111 CVC chassis; anything touching served-empty/CVC shapes is now CHEAPER).
- The downstream `ssl` handshake-outcome stat family is chartered as a framework-surgery row, NOT an inline add (ADR-0286 C3) — do not shrink it by adjective.

**BRAINSTORM owes (the phase-66/67/68 precedent):** the pick + full rejected-roster adjudication; EXECUTED reference evidence where a behavioral claim gates the design (fresh container per arm, `feedback_probe_fresh_container_per_arm`, `reference_probe_must_discriminate`); constructibility dossier (envelope, new symbols/packages stated LOUDLY, cycle-guard posture); open questions docketed to the SPEC; anticipated counts; adversarial verification before landing; ROADMAP row registered `in-progress`; sentinel re-run mechanically; router rolled to the phase-69 SPEC.

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline).
2. `docs/envoy-go/ROADMAP.md` — the three live `candidates:` sentences (HTTP/3 / xDS / Observability — locate by grep, not line) + §Schema.
3. `docs/envoy-go/DECISIONS.md` — ADR-0290 (the just-completed row: the S1 classifier + fallback lineage) + ADR-0287/0286 (CVC + SDS-VC substrate) + ADR-0045 (split valve) + ADR-0106.
4. `docs/envoy-go/phases/68-tls-cvc-empty-dynamic-fallback/{SPEC.md §13, PROGRESS.md}` — the deferred roster carried + what the IMPL actually landed.
5. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_*` + `reference_*`; trust MEMORY.md's index. NEW this close: `reference_sds_empty_ack_narrow_classifier`; `reference_go_client_cert_withholding` §RD3.
