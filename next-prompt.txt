START a fresh cold-start session for envoy-go — **the phase-37 (`load-balancer-maglev`) BRAINSTORM is DONE and LANDED on master; the next lifecycle step is the phase-37 SPEC** (`superpowers:writing-plans` scoped to SPEC authoring — execute the §10 D-M1..D-M6 empirical pins live against the contrib reference Envoy, anchor the ADR-0238 §Context draft, confirm the single-flat-row ADR-0045 disposition). The substantive master tip is the phase-37 BRAINSTORM squash **`768ecfc`** (`phase 37 BRAINSTORM: load-balancer-maglev — the seam-REUSING hash policy (the cheap follow-on); ROADMAP row 37 in-progress; ADR-0238 anticipated`; pushed to `origin/master`; confirm via `git log --oneline -5`). The literal live master tip is this docs-only `next-prompt.txt` SHA-fill commit sitting directly above the BRAINSTORM squash `768ecfc` (it names `768ecfc` — the phase-37 BRAINSTORM squash that registered ROADMAP row 37 and routed this cold-start to the phase-37 SPEC — as the substantive tip). The phase-37 BRAINSTORM (`superpowers:brainstorming`, interactive with the live human + the spec-document-reviewer gate PASS first pass) settled, for the project's FIFTH LB policy + SECOND consistent-hashing policy `Cluster.LbPolicy MAGLEV` (`envoy.config.cluster.v3`, enum value 5): (Q0) maglev — **the cheap follow-on** ADR-0235/ADR-0236 §Consequences named — REUSES the ADR-0235 hash-key seam (`Pick(hashKey uint64, hasHash bool)`) + BOTH ADR-0237 producer planes (tcp `source_ip` + HTTP route `hash_policy`) UNCHANGED, swapping the Ketama RING for a fixed-size MAGLEV lookup table behind the SAME `Pick` seam (a new `maglevLB` in `internal/cluster/maglev.go` mirroring `ringHashLB` — built once, immutable, injected-rng; `Pick` indexes `table[hashKey % M]`, no-hash → uniform random table index; returns `noopRelease`); (Q1) **a configured non-prime `table_size` is DEPARTURE-rejected** fail-fast at config-build (accept unset→default-65537 / any prime ≤ 5000011 / the cap 5000011; the reference accepts a composite verbatim — a RECORDED DEPARTURE; the permutation requires `M` prime for a terminating full-cycle fill — each `skip ∈ [1, M-1]` is coprime to a prime `M`); a SINGLE flat row (NO split — the producers already exist; ~120–220 prod LoC / ~8–10 tasks, well under the ADR-0045 gate); a header-key NAT-transparent TRUE cross-side affinity+spread differential `0063-lb-maglev` (the `0062` convention reused verbatim, retargeted to a MAGLEV cluster); 1 ADR anticipated (ADR-0238 — the policy ONLY, NO seam ADR [ADR-0235 reused], NO producer ADR [ADR-0237 reused]; the phase-35 single-ADR-on-reuse shape). The v1.32.4 surface VERIFIED at the BRAINSTORM: enum value 5, WITH a `Cluster.MaglevLbConfig` message [a SINGLE `table_size` field, `UInt64Value`, proto field 1, PGV `value must be less than or equal to 5000011`; **NO `hash_function` knob** — maglev always uses `xxHash64`]. The substantive predecessor below the BRAINSTORM squash is the phase-36.2 IMPL squash `7a38a20` (the HTTP route `hash_policy` producer; ADR-0237), with the docs-only SHA-fill tips `a043417`/`aa734ea`/`3c667db` between.

## What this session does — author the phase-37 SPEC

The phase-37 BRAINSTORM is DONE (lifecycle-state 0 → 1; `docs/envoy-go/phases/37-load-balancer-maglev/BRAINSTORM.md` + README.md). The next session runs `superpowers:writing-plans` scoped to **SPEC authoring** (the kafka-31/thrift-33/least_request-34/random-35/ring_hash-36 precedent — a single flat row), producing `docs/envoy-go/phases/37-load-balancer-maglev/SPEC.md`. The SPEC executes the BRAINSTORM §10 **D-M1..D-M6** empirical pins IN-SESSION (parallel-subagent fan-out) against the contrib reference image (`envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8`, ADR-0227) + go-control-plane `/envoy` v1.32.4 bindings, via the live-probe precedent (`reference_docker_probe_bridge_network` — bridge network + STRICT_DNS backend hostnames + verify traffic ran via `downstream_cx_rx_bytes_total > 0`), then anchors the ADR-0238 §Context draft + ends with the spec-document-reviewer gate.

The D-M1..D-M6 pins (BRAINSTORM §10): **D-M1** re-pin the exact v1.32.4 surface (enum 5; `MaglevLbConfig.table_size` `UInt64Value` field 1, default 65537 [proto- or self-supplied?], PGV `≤ 5000011`; NO `hash_function`; clean `go mod tidy`; oneof silent-ignore; whether the reference boot-rejects `table_size > 5000011`). **D-M2** *(SPEC-BLOCKING)* the EXACT Maglev table-build algorithm — the per-host `offset`/`skip` `xxHash64` seed derivations, the hashed endpoint-identifier string (`address:port`? hostname?), the populate loop, the `table[hashKey % M]` lookup, the no-hash random fallback; decide cross-side byte-exact feasibility (anticipated YES — the `X-Hash` key is NAT-transparent, the `0062` posture); confirm the populate-loop termination (M prime → full-cycle → terminates). **D-M3** confirm the LANDED ADR-0237 HTTP-header producer (`cluster.HashHeaderValues` + `applyHashKey`) feeds maglev's `Pick` IDENTICALLY (a confirmation, NOT a build). **D-M4** the stat-surface delta (anticipated +2 `maglev_lb.*` gauges — `min_entries_per_host`/`max_entries_per_host`, no `size` gauge; mirror; cross-side-exact); pin the `0063` `StatsAsserter` set. **D-M5** *(SPEC-BLOCKING)* the reject contract — the 3-site blast radius (`manager.go:275` reject text `…, MAGLEV` + `manager_test.go:320` doubly-hit retarget MAGLEV → CLUSTER_PROVIDED + substring + `BEHAVIOR_CONTRACT.md`); the `table_size` primality DEPARTURE reject wording (envoy-go-strict — the reference accepts a composite → a subject-side unit test, NOT a cross-side dir); the PGV-cap reject wording (`value must be less than or equal to 5000011`). **D-M6** *(SPEC-BLOCKING)* the `0063` design — the `N`×`K` header-value design (anticipated mirroring `0062`'s N=16×K=16=256), the affinity modular invariant (`count ≡ 0 mod N`), the spread band, the deliberate-breaks (scatter-the-key / collapse-the-table), the ≥20-run flake-free protocol; the ADR-0045 envelope re-check (anticipated single flat row, NO split); confirm ADR-0024 unamended; whether a `FuzzMaglevLookup` is warranted (anticipated NO).

## Counts at master tip (phase 37 BRAINSTORM done — confirm at the SPEC)

- stat surface **1119** (`ls`/recipe per BEHAVIOR_CONTRACT). Anticipated 1119 → ~1121 at the IMPL (+2 `maglev_lb.*` gauges — D-M4 pins). UNCHANGED at the BRAINSTORM.
- differential fixtures **64** (tail `0062-lb-ring-hash-http`; `ls -d test/fixtures/[0-9]* | wc -l`). Anticipated 64 → 65 at the IMPL (`0063-lb-maglev`). UNCHANGED at the BRAINSTORM.
- fuzzers **42** (tail `FuzzZookeeperResponseDecode` — sorted-unique; canonical recipe `grep -rh "^func Fuzz" $(find ./internal -name fuzz_test.go) | wc -l`). Anticipated UNCHANGED (no wire decode). BackendKind tail **33** (`TCPThriftResponder`; anticipated UNCHANGED — `0063` reuses the `0062` HTTP backends).
- DECISIONS tail **ADR-0237** (next-free **ADR-0238**). STAYS ADR-0237 at the BRAINSTORM; ADR-0238 (the maglev policy) §Context anchored at the SPEC, the body at the IMPL per ADR-0044. ADR-0235 (the hash-key seam) + ADR-0236 (the ring_hash policy — the STYLE TEMPLATE for ADR-0238) + ADR-0237 (the HTTP producer) are the durable LB assets maglev REUSES unchanged.
- reference pin: `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227). Toolchain: Go 1.26.x; golangci-lint 1.64.8 (ADR-0009); go-control-plane `/envoy` v1.32.4 (ADR-0008 — `MaglevLbConfig` already in the pinned module).

## Read first (in order)

1. THIS FILE — the post-phase-37-BRAINSTORM cold-start (next: the phase-37 SPEC — `superpowers:writing-plans` scoped to SPEC authoring).
2. `docs/envoy-go/phases/37-load-balancer-maglev/BRAINSTORM.md` — the phase-37 charter (the Q0/Q1 decisions; the v1.32.4 enum-5 + `Cluster.MaglevLbConfig` verification [§2.1]; the seam-REUSE rationale [§1.7 / §3.1]; the `table_size` primality DEPARTURE [§2.3]; the header-key cross-side affinity+spread differential design [§2.5/§6]; the single anticipated ADR [§7]; the §10 D-M1..D-M6 pins this SPEC executes) + README.md.
3. `docs/envoy-go/STATE.md` — active-phase `phase 37 (load-balancer-maglev) brainstorm done`; the lifecycle-state (the family stays OPEN, 4 candidates remain after 37); the counts.
4. `docs/envoy-go/ROADMAP.md` — row 37 (`in-progress`; flat family row, NO split; the Load-balancing family OPEN, 4 candidates remain after 37) + the family-roster `maglev IN-PROGRESS` paragraph.
5. `docs/envoy-go/DECISIONS.md` — **ADR-0235** (the hash-key seam — REUSED unchanged; the §Consequences "maglev reuses unchanged" claim) + **ADR-0236** (the ring_hash policy — the STYLE TEMPLATE for the ADR-0238 §Context draft) + **ADR-0237** (the HTTP route hash_policy producer — REUSED unchanged); + ADR-0044 (ADR §Context at SPEC, body at IMPL) / ADR-0052 (atomic six-gate landing) / ADR-0080 (byte-stable parse-reject wording) / ADR-0106 (flat family row) / ADR-0227 (the contrib reference image) / ADR-0045 (the SPEC split-gate re-check) / ADR-0004 (the empirical-pin discipline).
6. `docs/envoy-go/phases/36-load-balancer-ring-hash/` + `phases/36.2-load-balancer-ring-hash-http/` — the AS-BUILT ring_hash family (the landed seam + `ringHashLB` + the BOTH hash producer planes maglev reuses; the SPEC.md is the SHAPE TEMPLATE for the phase-37 SPEC).
7. The landed LB code maglev plugs into: `internal/cluster/{cluster.go (the Pick seam + hashKeyFrom/WithHashKey), ringhash.go (ringHashLB — the STRUCTURAL TEMPLATE maglev mirrors as a table), hash.go (xxHash64/HashSourceIP/HashHeaderValues), manager.go (the lb-policy gate + parseRingHashLbConfig precedent + the reject text + manager_test.go:320 retarget), loadbalancer.go}` + the LANDED producers `internal/filter/{tcpproxy/filter.go, http/router/router.go}` (UNCHANGED) + `test/fixtures/0062-lb-ring-hash-http/` (the `0063` precedent).
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_execution_style` (subagent-driven at the IMPL), `feedback_git_worktrees`, `feedback_subagents_no_push` + `feedback_push_to_origin` (controller squash-merges + pushes at stage-close), `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_path_targeting`/`_detach`, `reference_differential_break_protocol_count1`, `reference_differential_run_selector`, `reference_differential_band_sigma_margin`, `reference_differential_hash_key_cross_side_infeasible` (a maglev header-key fixture is NAT-transparent like 0062 → TRUE cross-side affinity), `reference_fixture_workload_constant_desync` (keep `0063` N/K/totalReqs synced with any hand-rolled count slices), `reference_proto_roster_extraction_digits` (digit-inclusive regexes for the maglev proto roster), `reference_docker_probe_bridge_network`.

## Honor throughout

- The lifecycle is BRAINSTORM → SPEC → PLAN → IMPL, each its own session. The next session is the phase-37 **SPEC** (`superpowers:writing-plans` scoped to SPEC authoring — execute D-M1..D-M6 live, anchor the ADR-0238 §Context draft, confirm the single-flat-row ADR-0045 disposition, end with the spec-document-reviewer gate; the PLAN/IMPL follow in their own sessions).
- `feedback_execution_style` · `feedback_git_worktrees` · `feedback_subagents_no_push` + `feedback_push_to_origin` · `feedback_pertask_gofmt_lint`.
- ADR-0106 (flat family row) · ADR-0044 (ADR §Context at SPEC, body at IMPL, in-place) · ADR-0052 (the atomic-landing six-gate) · ADR-0080 (byte-stable parse-reject wording) · ADR-0227 (the contrib reference image; live probes per `reference_docker_probe_bridge_network`) · ADR-0045 (the SPEC split-gate re-check) · ADR-0004 (the empirical-pin discipline).
