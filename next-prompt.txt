START a fresh cold-start session for envoy-go — **the phase-41 circuit-breakers PLAN is COMPLETE and LANDED on master** (the THIRD Upstream-robustness-family row; `docs/envoy-go/phases/41-circuit-breakers/PLAN.md` — a 12-task TDD spine implementing SPEC §10, the D-S41-1..7 resolutions, the FINAL ADR-0045 NO-SPLIT re-check, plan-reviewer-APPROVED — + the STATE/ROADMAP/next-prompt roll, all docs-only; counts UNCHANGED). **The next lifecycle step is the phase-41 IMPL** — `superpowers:subagent-driven-development`: execute the 12-task PLAN (fresh subagent per task + two-stage review), the six-gate at the close (ADR-0052), land the ADR-0248 §Decision/§Consequences bodies IN-PLACE (ADR-0044), flip ROADMAP row 41 → `done`. The IMPL needs **Docker + `contrib-v1.37.2`** for the `0074` cross-side fixture (`reference_docker_probe_bridge_network`). Confirm the tip via `git log --oneline -6` (HEAD is the phase-41 PLAN docs commit — the `41-circuit-breakers/PLAN.md` + the STATE/ROADMAP/next-prompt roll — sitting on the phase-41 SPEC commit `6f7d436` + the BRAINSTORM `306e26a` + the substantive phase-40.3 IMPL squash `148f288`; NO substantive code has landed since `148f288` — the BRAINSTORM, SPEC, and PLAN are all docs-only). Counts UNCHANGED at this PLAN: stat surface **1149** / fixtures **75** / fuzzers **42** / BackendKind tail **35** (`HTTP503Responder`) / DECISIONS tail **ADR-0247**, next-free **ADR-0248**.

## What just landed (the phase-41 PLAN — the durable record is `docs/envoy-go/phases/41-circuit-breakers/PLAN.md`; read that + the SPEC, not this summary)

The PLAN decomposes SPEC §10 into **12 bite-sized TDD tasks** (complete code in each), implementing circuit breaking on the **`max_requests` keystone** (SPEC AMEND-CB1 — cx/pending enforcement deferred): a per-priority `circuitBreaker` counter struct on `Cluster`; a synchronous non-blocking `TryAcquireRequest()` at router admission + `defer ReleaseRequest()`; fail-fast `503` + `upstream_rq_pending_overflow`++ + `rq_open`=1; the full +14 stat surface registered for parity.

**The 12-task spine:** (1) baselines/PROGRESS; (2) `parseCircuitBreakers` (`thresholds[]` keyed by priority + 1024 defaults + the 3 reject arms + per_host_thresholds silent-ignore) + the `circuitBreaker`/`cbPriority` structs; (3) the `tryAcquire`/`release` CAS + the `rq_open` gauge + the `upstream_rq_pending_overflow` counter (+ `max_requests:0` reject-all + a `-race` test); (4) the +14 stat registrations (scoped `if c.circuitBreaker != nil`; 10 `*_open` gauges × {default,high} + 4 cluster overflow counters; only `default.rq_open` + `upstream_rq_pending_overflow` LIVE, 12 emit-0; 1149→1163); (5) `Cluster.TryAcquireRequest`/`ReleaseRequest` + `buildCluster` parse+attach + the full-75-dir byte-stability gate; (6) the admission try-acquire + `defer ReleaseRequest` + the overflow 503 at `doH1ClusterAction`(`router.go:591`)/`doH2ClusterAction`(`router_h2.go:60`) (after `IncUpstreamRqTotal`, before the upstream call; NO `IncStatusClass` on overflow); (7) the +1 `BlockingHoldResponder` BackendKind 36 (the re-armable `/__release` gate + `acceptBlockingHold`) + the runner spawn arm; (8) the `0074` cross-side fixture; (9) `0074` deliberate breaks (2) + 20-run flake; (10) full 76-dir differential + six-gate; (11) ADR-0248 body + BEHAVIOR_CONTRACT (1149→1163); (12) completion bundle + ROADMAP row 41 → `done`.

**The D-S41-1..7 resolutions (baked into the tasks):**
- **D-S41-1** house reject wording `cluster: %q: circuit_breakers: <reason>` (priority-range / retry_budget-percent / strict duplicate-priority).
- **D-S41-2** `max_requests` absent ⇒ 1024 default; explicit 0 ⇒ a hard reject-all cap.
- **D-S41-3** the `UO` access-log flag is a **recorded DEPARTURE** — `ActionResponse` + the accesslog `Record` have NO response-flags field, and the formatter hardcodes `RESPONSE_FLAGS` to `-`, so UO needs new cross-cutting plumbing (out of scope). envoy-go emits the `503` STATUS + the overflow counter + `rq_open` (the observable contract); the `0074` differential asserts that pair, NEVER the access-log line.
- **D-S41-4** ★ the `BlockingHoldResponder` holds `GET /` on a re-armable gate freed by `GET /__release`; the in-process backend binds `0.0.0.0:0` (reachable by the subject on `127.0.0.1` + the reference container on `host.docker.internal` — the proven 0066 mechanism); the driver hits `127.0.0.1:<backendPort>/__release` for BOTH sides; **sequential-per-side** testing (subject then reference; backend idle between) + **poll-`rq_open`→1** confirms N-in-flight SLEEPLESSLY (no `time.Sleep`).
- **D-S41-5** the `0074` constants single-sourced; `refContainerListenerPort = 19163` (next-free after 0073's 19162); `maxRequests` = the N held.
- **D-S41-6** `circuitBreaker.prio[2]` indexed by RoutingPriority (0=DEFAULT, 1=HIGH); HIGH parses + registers its stat tree but `tryAcquire(1)` is never called.
- **D-S41-7** FINAL ADR-0045 re-check → **NO SPLIT** (~260–360 prod LoC / 12 tasks).

plan-document-reviewer APPROVED (all code anchors + the proto getters [`GetCircuitBreakers`/`GetThresholds`/`GetPriority`/`GetMaxRequests`/`GetRetryBudget().GetBudgetPercent().GetValue()` float64; `RoutingPriority_DEFAULT=0`/`HIGH=1`] + the `tryAcquire`/`release` CAS + the admission defer-ordering [overflow early-return BEFORE the defer registers → no double-release] + the `acceptBlockingHold` re-armable gate + the `0074` backend-control-port release + the +14 math + the D-resolutions all VERIFIED against source; 1 advisory `budget_percent`-is-float64 test-construction note applied).

## The IMPL obligations (execute the 12-task PLAN; NO re-design)

- Execute each task TDD-style (failing test → implement → pass → commit), fresh subagent per task + two-stage review (`feedback_execution_style`). The controller squashes the 12 task commits + pushes at stage-close (`feedback_subagents_no_push` · `feedback_push_to_origin`).
- The load-bearing gates: Task 5 (the full-75-dir byte-stability gate — the nil-guard makes the admission path byte-identical when no `circuit_breakers`); Task 8 (the `0074` cross-side fixture — needs Docker + `contrib-v1.37.2`); Task 9 (2 deliberate breaks with `-count=1` + 20-run flake — `reference_differential_break_protocol_count1`); Task 10 (the six-gate — watch for the unrelated `subject ready: EOF` full-suite startup flake, `reference_differential_fullsuite_startup_flake`).
- The exit deltas: stat 1149→**1163** (+14), fixtures 75→**76** (`0074`), BackendKind 35→**36** (`BlockingHoldResponder`), DECISIONS ADR-0247→**ADR-0248** (next-free ADR-0249), fuzzers **42** (unchanged), ZERO new packages/modules. ROADMAP row 41 `in-progress → done` (a flat un-split family row, NO parent rollup per ADR-0106; the family stays open — 2 candidates remain after 41).

## Honor throughout (the IMPL discipline)

- **Lifecycle = one stage per session:** the IMPL next (`superpowers:subagent-driven-development`). `feedback_git_worktrees` (the IMPL runs in a worktree) · `feedback_subagents_no_push` (task subagents commit locally only; the controller squashes + pushes at stage-close) · `feedback_push_to_origin` (after a clean local merge on master with tests green, push without asking) · `feedback_subagent_worktree_path_targeting` + `feedback_subagent_worktree_detach` (pin canonical paths; controller re-verifies the branch each task) · `feedback_pertask_gofmt_lint` (each task runs gofmt -l + golangci-lint on touched pkgs).
- **ADR/process:** ADR-0044 (the ADR-0248 §Decision/§Consequences bodies land IN-PLACE at the IMPL; the §Context is in SPEC §13) · ADR-0052 (the six-gate) · ADR-0080 (the byte-stable rejects) · ADR-0106 + `reference_roadmap_split_phase_row_done` (row 41 flips `done` at its single IMPL six-gate; NO parent rollup) · ADR-0227 (`contrib-v1.37.2`).
- **At the differential:** `reference_docker_probe_bridge_network` (the `0074` reference container reaches the shared `BlockingHoldResponder` over the bridge via `host.docker.internal`; the in-process backend binds `0.0.0.0`) · `reference_differential_band_sigma_margin` (the `0074` is SLEEPLESS — the `/__release` barrier + poll-`rq_open`-to-converge) · `reference_differential_asserter_dispatch` (use `StatsAsserter`, cross-side) · `reference_differential_run_selector` (the `TestDifferential/0074` prefix) · `reference_fixture_workload_constant_desync` (single-source the constants).

## Read first (in order)

1. THIS FILE — the post-phase-41-PLAN cold-start (the PLAN is DONE + LANDED; the next step is the phase-41 IMPL — execute the 12-task spine).
2. `docs/envoy-go/phases/41-circuit-breakers/PLAN.md` — the 12-task TDD spine + the D-S41-1..7 resolutions + the complete per-task code + the file structure + the exit-delta table. The IMPL executes THIS. Read it, not the summary above.
3. `docs/envoy-go/phases/41-circuit-breakers/SPEC.md` — the durable SPEC (the §1.1 AMEND-CB1..7 block, the §3 architecture, the §5 proto roster, the §6 reject roster, the §7 +14 stat surface, the §8 `0074` differential, the §11 live-pin block, the §13 ADR-0248 §Context).
4. `docs/envoy-go/STATE.md` — active-phase `phase 41 (circuit-breakers) PLAN done`; the count footer (1149 / 75 / 42 / 35 / ADR-0247, next-free ADR-0248); the lifecycle routing (next-skill = the 41 IMPL).
5. `docs/envoy-go/ROADMAP.md` — row 41 `in-progress` (the PLAN-DONE note) + the §Upstream-robustness family heading.
6. The phase-40.1 IMPL record `docs/envoy-go/phases/40.1-outlier-detection-consecutive-5xx/{PLAN,PROGRESS}.md` (the keystone-IMPL precedent shape — the parse/stat/seam/fixture task execution, the `HTTP503Responder` BackendKind + the cross-side fixture) + `docs/envoy-go/DECISIONS.md` (next-free ADR-0248) + `docs/envoy-go/BEHAVIOR_CONTRACT.md` (the stat-surface block at 1149 — the IMPL advances it to 1163).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`.
