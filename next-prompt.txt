START a fresh cold-start session for envoy-go — **the phase-36.2 (`load-balancer-ring-hash-http`) SPEC is DONE and LANDED on master; the next lifecycle step is the phase-36.2 PLAN** (`superpowers:writing-plans` — decompose the SPEC §10 spine into bite-sized TDD tasks; the ADR-0045 gate re-check, anticipated NO split [~150 prod LoC / ~6–8 tasks]). The substantive master tip is the phase-36.2 SPEC squash **`da295ab`** (`phase 36.2 SPEC: load-balancer-ring-hash-http — author the HTTP route hash_policy producer plane SPEC`; pushed to `origin/master`; confirm via `git log --oneline -3`). THIS roll-forward commit sits directly above `da295ab`; the substantive predecessor below it is the phase-36.1 IMPL squash `e8e8c48` (with two docs-only `next-prompt.txt` roll-forwards — `36340df`, `9beb78b` — between them). The SPEC squash `da295ab` carries the durable `docs/envoy-go/phases/36.2-load-balancer-ring-hash-http/SPEC.md` (the per-sub-phase-dir 32.1/32.2 precedent) + the STATE/ROADMAP row-36 advance (active-phase → `phase 36.2 SPEC done`; ROADMAP row 36 `36.1 done, 36.2 SPEC done → PLAN next`). Counts UNCHANGED at the SPEC (they advance at the 36.2 IMPL).

## What this session does — author the phase-36.2 PLAN

The phase-36.2 SPEC is COMPLETE. The next session writes the **phase-36.2 PLAN** (`superpowers:writing-plans` — NO brainstorm needed: the parent phase-36 BRAINSTORM covered both planes; 36.2 is producer wiring against the LANDED seam). 36.2 is the **HTTP route `hash_policy` PRODUCER-ONLY leg**: the HTTP router parses a per-route `RouteAction.hash_policy` at config-build (header + connection-properties source_ip SUPPORTED; cookie/query/filter_state + regex_rewrite DEPARTURE-rejected, fail-fast), computes a ring_hash key per request via the `rotl64(prev,1)^new` fold (first-verbatim, nullopt-skip, `terminal` short-circuit), and threads it via the ALREADY-LANDED `cluster.WithHashKey(ctx, key)` at the FOUR router dial sites (codec-agnostic H1 `*http.Request` / H2 `h2.H2Request`) → the ALREADY-LANDED `ringHashLB.Pick`. NO LB policy / seam / manager / package / go.mod change; ≤1 additive `cluster.HashHeaderValues` helper; ZERO new stat names; the `0062-lb-ring-hash-http` NAT-transparent TRUE cross-side affinity differential.

## Counts at master tip (phase 36.2 SPEC done — confirm at the 36.2 PLAN baseline)

- stat surface **1119** (DOC count in BEHAVIOR_CONTRACT.md; the 36.2 plane adds 0 names — `upstream_rq_total` cross-equal on the HTTP plane, unlike tcp). 36.2 IMPL stat delta: 0.
- differential fixtures **63** (tail `0061-lb-ring-hash`; `ls -d test/fixtures/[0-9]* | wc -l`). 36.2 IMPL adds `0062-lb-ring-hash-http` → 64.
- fuzzers **42** (tail `FuzzThriftDecode`); BackendKind tail **33** (`TCPThriftResponder`) — both UNCHANGED at the 36.2 IMPL (no wire decode; reuse existing HTTP backends).
- DECISIONS tail **ADR-0236** (next-free **ADR-0237**). The ADR-0237 (HTTP route hash_policy producer plane) §Context is a DRAFT in SPEC §13; the full entry lands at the 36.2 IMPL per ADR-0044.
- exported-surface at 36.1: `cluster.WithHashKey` + `cluster.HashSourceIP` (both LANDED; 36.2 CONSUMES them + MAY add one additive `cluster.HashHeaderValues` for the header digest — D-S362-1).
- reference pin: `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227). Toolchain: Go 1.26.x; golangci-lint 1.64.8 (ADR-0009); go-control-plane `/envoy` v1.32.4 (ADR-0008).

## Read first (in order)

1. THIS FILE — the post-phase-36.2-SPEC cold-start (next: the phase-36.2 PLAN).
2. `docs/envoy-go/phases/36.2-load-balancer-ring-hash-http/SPEC.md` — the 36.2 SPEC (the PLAN input): **§3** the producer wiring (config-build parse + DEPARTURE-reject; the per-request `applyHashKey` fold at the 4 dial sites; §3.2 the H1/H2 codec asymmetry), **§5** the proto roster, **§6** the reject roster, **§7** stats (0 new), **§8** the `0062` fixture, **§10** the ~6–8-task spine the PLAN decomposes, **§11** the empirical-pin block, **§12** the PLAN/IMPL D-questions (D-S362-1 the header-hash exported surface; D-S362-3 the H1/H2 helper asymmetry; D-S362-4 the multi-value fold; D-S362-6 the affinity attribution; D-S362-7 the conformance-gate disposition), **§13** the ADR-0237 §Context draft.
3. `docs/envoy-go/phases/36-load-balancer-ring-hash/SPEC.md` — the parent SPEC; **§3.0** the CONSUMED 36.1/36.2 split charter + the AMEND-RH pins.
4. `docs/envoy-go/phases/36-load-balancer-ring-hash/PLAN.md` + `PROGRESS.md` — the AS-BUILT 36.1 (the landed seam + `cluster.WithHashKey`/`HashSourceIP`/`xxHash64` + the tcp_proxy `source_ip` producer the HTTP plane mirrors).
5. `docs/envoy-go/STATE.md` — active-phase `phase 36.2 SPEC done`; the lifecycle → the 36.2 PLAN routing; the counts.
6. `docs/envoy-go/ROADMAP.md` — row 36 (36.1 done; 36.2 SPEC done → PLAN next; flat family row per ADR-0106; Load-balancing family OPEN, 5 candidates remain).
7. `docs/envoy-go/DECISIONS.md` — **ADR-0235** (the seam) + **ADR-0236** (the ring_hash policy) — the durable assets 36.2 builds on; + ADR-0045 (the split-gate, re-check at the PLAN) / ADR-0044 / ADR-0052 / ADR-0080 / ADR-0106.
8. The landed code the 36.2 producer threads into: `internal/cluster/{cluster.go (WithHashKey/hashKeyFrom), hash.go (HashSourceIP/xxHash64/ipOnly)}` + the HTTP router (`internal/filter/http/router/{router.go (H1ClusterAction/doH1ClusterAction:504/AcquireH1:509/do:641/Dial:662), router_h2.go (H2ClusterAction/doH2ClusterAction:57/DialH2:62/doH2:214/DialH2:233)}`) + the HCM bridge (`internal/filter/hcm/{config.go (buildRouterAction), actions.go (clusterRouteAction/asRouterAction:234/asRouterActionH2:247)}`) + the tcp_proxy producer precedent `internal/filter/tcpproxy/filter.go` (the source_ip parse at :69-82 + the WithHashKey stuff at :146-148 the HTTP side mirrors). H1 remote-addr ctx-carry `connection.go:384`; H2 `h2dispatch.go:337`.
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_execution_style`, `feedback_git_worktrees`, `feedback_subagents_no_push`, `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_path_targeting`, `feedback_subagent_worktree_detach`, `reference_differential_break_protocol_count1`, `reference_differential_asserter_dispatch`, `reference_differential_run_selector`, `reference_differential_band_sigma_margin`, `reference_differential_hash_key_cross_side_infeasible` (the HTTP header key is cross-side-reproducible — `0062` is a TRUE cross-side affinity differential), `reference_docker_probe_bridge_network`.

## Honor throughout

- The lifecycle is BRAINSTORM → SPEC → PLAN → IMPL, each its own session. The next session is the phase-36.2 **PLAN** (`superpowers:writing-plans` — the SPEC §10 spine into bite-sized TDD tasks; the ADR-0045 gate re-check per ADR-0045, anticipated NO split).
- `feedback_execution_style` (subagent-driven execution at the IMPL) · `feedback_git_worktrees` (work in a worktree) · `feedback_subagents_no_push` + `feedback_push_to_origin` (controller squash-merges + pushes at stage-close) · `feedback_pertask_gofmt_lint`.
- ADR-0106 (flat family row — the 36.2 leg flips its own portion at the IMPL) · ADR-0045 (the split-gate — re-check at the 36.2 PLAN; anticipated NO split) · ADR-0044 (ADR bodies at the IMPL, in-place — ADR-0237 lands at the 36.2 IMPL) · ADR-0052 (the atomic-landing six-gate) · ADR-0080 (byte-stable parse-reject wording) · ADR-0227 (the contrib reference image; live probes per `reference_docker_probe_bridge_network`).
