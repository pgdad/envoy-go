START a fresh cold-start session for envoy-go — **the phase-38.1 (`load-balancer-subset`) PLAN is DONE and LANDED on master; the next lifecycle step is the phase-38.1 IMPL** (`superpowers:subagent-driven-development` per `feedback_execution_style` — the kafka-31/thrift-33/least_request-34/random-35/ring_hash-36/maglev-37 IMPL precedent). Execute `docs/envoy-go/phases/38-load-balancer-subset/PLAN.md` Tasks 1–14 in a fresh worktree (`feedback_git_worktrees`), one fresh subagent per task + two-stage review (`feedback_execution_style`), subagents commit LOCAL-ONLY (`feedback_subagents_no_push`), the controller squash-merges + pushes at stage-close (`feedback_push_to_origin`). Task 1 creates `PROGRESS.md` + re-pins the as-built anchors against the IMPL-session tip. The substantive master tip is the phase-38.1 PLAN-done docs bundle **`4e96220`** (`phase 38.1 PLAN: load-balancer-subset — SPEC §10 → 14 bite-sized TDD tasks; FINAL ADR-0045 re-check NO FURTHER SPLIT; plan-document-reviewer APPROVED`; `docs/envoy-go/phases/38-load-balancer-subset/PLAN.md` NEW + README.md status 2 → 3 + STATE row-38 PLAN-done advance + ROADMAP row-38 PLAN-done note; pushed to `origin/master`; confirm via `git log --oneline -5`). The prior substantive tip is the phase-38 SPEC-done bundle **`f52a8ea`** (`phase 38 SPEC: load-balancer-subset …`), above the docs-only SHA-fill chain. This paragraph is the docs-only `next-prompt.txt` SHA-fill that NAMES `4e96220` the substantive PLAN-done live tip; it sits directly above `4e96220`. ALL counts UNCHANGED at this PLAN (a docs-only commit — stat surface 1121 / fixtures 65 / fuzzers 42 / BackendKind tail 33 / DECISIONS tail ADR-0238, next-free ADR-0239; the +4 `lb_subsets_*` / fixtures 65 → 66 / DECISIONS → ADR-0240 / stat surface → 1125 land at the 38.1 IMPL).

## What this is (phase 38.1 PLAN — what was decided)

The phase-38.1 PLAN decomposes SPEC §10's spine into **14 bite-sized TDD tasks** (each: write the failing test → run it fails → minimal impl → run it passes → commit; per-task `gofmt -l` + `golangci-lint` on touched pkgs per `feedback_pertask_gofmt_lint`). The FINAL ADR-0045 split-gate re-check → **NO FURTHER SPLIT within 38.1** (~300–450 prod LoC / 14 tasks — both axes under the `> ~25 tasks OR > ~1500 LoC` gate). The §12 D-S38-1..D-S38-6 questions are RESOLVED (the property test FOLDED into Tasks 6/7; the SPEC's verify/completion bundle SPLIT into Tasks 13/14 — the phase-35/36/37 precedent). plan-document-reviewer APPROVED first pass (the 3 advisory snippet-accuracy fixes applied: `Counter.Add(uint64)` / `Counter.Load()` not `.Value()` [`internal/stats/counter.go:27/30`] / the real `doH1ClusterAction`+`doH2ClusterAction` dispatch names + the `routerAction`/`routerActionH2` structs + the 3 `hcm/actions.go` call sites :213/:236/:249 — there is NO `doH1ClusterActionDirect`).

The 14-task spine (the durable record is PLAN.md — read it, not this summary):
1. Baselines/anchors gate + PROGRESS.md (re-confirm fixtures 65 / fuzzers 42 / stat surface 1121 / BackendKind 33 / DECISIONS tail ADR-0238; re-pin the as-built line anchors; confirm the reject surface is EMPTY [supported-list + `TestManager_Error_UnsupportedLBPolicy` UNTOUCHED — AMEND-SS1]).
2. `SubsetMatch` / `SubsetValue` (scalar union) / `valueEqual` (numbers double-valued — int 1 ≡ 1.0, AMEND-SS2) / `key()` canonical / `ScalarsFromStruct` (the shared structpb→scalar lowering) — NEW `internal/cluster/subset.go`.
3. `Endpoint.Metadata map[string]SubsetValue` + the `extractEndpoints` `envoy.lb` capture (the FIRST per-endpoint dimension beyond `Host:Port`; non-scalar dropped; `Addr()` unchanged; the construction-site sweep per `reference_conn_wrap_method_no_promote`).
4. The seam-input widening: `loadBalancer.Pick(hashKey, hasHash)` → `Pick(hashKey, hasHash, match SubsetMatch, hasMatch bool)` + the 5 leaf signatures (behavior-neutral) + `WithSubsetMatch`/`subsetMatchFrom` (the `hashKeyCtxKey` template) + the 3 funnels (`Dial`:233/`AcquireH1`:287/`PickEndpoint`:196). Exported `Cluster` byte-stable (OPTION-C).
5. The `buildLeafLB(c, name, endpoints)` extraction (the `lb_policy` switch lifted to run over an arbitrary sub-slice; CLUSTER_PROVIDED/unsupported reject HERE before any wrap — ZERO new reject arm).
6. `subsetLB` enumeration + build (per selector × distinct value-tuple a child via the factory; host missing a key excluded; fallback child {NO_FALLBACK nil / ANY_ENDPOINT all / DEFAULT_SUBSET default-match, zero-match → nil}; `numSubsets` ×1; folded enumeration property test).
7. `subsetLB.Pick` (match→child delegate [hashKey passthrough] / fallback / NO_FALLBACK→errNoEndpoints; `selected`/`fallbackC` Inc mutually-exclusive, nil-guarded; folded resolution property test, D-S38-6).
8. Manager `parseLbSubsetConfig` + the wrap-after-switch (the §6 accept/reject matrix — CLUSTER_PROVIDED rejects via the leaf factory; empty keys/selectors/dups/fallback-default mismatches ACCEPT, AMEND-SS1).
9. The 4 `lb_subsets_*` registrations (the `*subsetLB` type-assert in `registerClusterMetrics`; `active`(gauge)/`created`(counter) Set/Add to `numSubsets` ×1 — NOT the reference 33× artifact, AMEND-SS4; `selected`/`fallback` counters allocated + INJECTED onto the live `subsetLB`, D-S38-4; surface 1121 → 1125).
10. The HTTP `metadata_match` producer (`parseRouteSubsetMatch` — scalar lower + the `router-metadata-match-nonscalar` reject [D-S38-2] + the `clusterRouteAction.subsetMatch` field + `H1ClusterAction`/`H2ClusterAction` widen to `(c, hps, subsetMatch)` + the dispatch thread `WithSubsetMatch` route-static, NO per-request fold — AMEND-SS6).
11. The `0064-lb-subset` fixture (STATIC-config NAT-transparent TRUE cross-side subset affinity — SET-membership `/v1`,`/v2` + within-subset spread + ANY_ENDPOINT fallback `/none` + the cross-side `selected`/`fallback` `StatsAsserter` + `/health` byte-equiv; reuses `HTTPEcho`, BackendKind STAYS 33; fixtures 65 → 66).
12. Deliberate-break liveness (`-count=1`, `reference_differential_break_protocol_count1`): drop-the-`metadata_match` (affinity) / misroute-the-fallback (fallback + stats) / stats-perturb; ≥20-run flake; `totalReqs` DERIVED (`reference_fixture_workload_constant_desync`).
13. Full 66-dir differential re-verify + `-race -short` + h2spec/proxy-wasm asserted-unaffected (the regression gate).
14. The completion bundle (ADR-0052 atomic six-gate): BEHAVIOR_CONTRACT subset subsection + the 4 stats + departures (the §9 bundle); the full ADR-0239 + ADR-0240 entries (ADR-0044 in-place; tail → ADR-0240); STATE/ROADMAP row 38 (38.1 leg) `in-progress → done` (flat family row — NO parent rollup, ADR-0106).

## The load-bearing SPEC pins the IMPL MUST honor (SPEC §11 AMEND-SS1..SS7)

- **AMEND-SS1 — ZERO new cluster reject arm.** The only reference reject (`lb_subset_config` + `CLUSTER_PROVIDED`) is ALREADY covered by envoy-go's pre-existing unsupported-policy gate (the `buildLeafLB` switch `default` fires first). The `Cluster.LbPolicy` supported-list + `TestManager_Error_UnsupportedLBPolicy` STAY UNTOUCHED (subset is not an enum value — the 34/35/36/37 doubly-hit retarget lineage BREAKS). Empty keys / empty selectors / duplicate selectors / fallback-default mismatches ALL ACCEPT (do NOT over-reject). The ONE new arm is producer-side: `router-metadata-match-nonscalar` (unit-level). ZERO new go.mod dep (`go mod tidy -diff` empty).
- **AMEND-SS2 — numbers double-valued, exact key-set match.** int 1 ≡ double 1.0 (just float64; nothing to canonicalize). bool/string typed. Scalar values only in the MVP (list/struct + `list_as_any` deferred).
- **AMEND-SS3 — fallback.** NO_FALLBACK no-host → `errNoEndpoints` → the existing 503/`UH` (`lb_subsets_fallback` STAYS 0). ANY_ENDPOINT → spread over ALL. DEFAULT_SUBSET → the default match (zero-match degrades to NO_FALLBACK).
- **AMEND-SS4 — +4 stats, active/created ×1 NOT 33×.** `lb_subsets_active`(gauge)+`created`/`selected`/`fallback`(counters). `selected`/`fallback` request-counted (cross-side-equal in `0064`); `active`/`created` = the ×1 distinct-subset count (the reference's 33× magnitude is a recorded DEPARTURE, NOT cross-equaled). `selected`/`fallback` are the FIRST stats a `loadBalancer` Inc's from its own `Pick` path (injected at register — D-S38-4; ADR-0024 noted in ADR-0240, not amended).
- **AMEND-SS5 — child policy.** Each subset (+ fallback) balanced by the cluster's `lb_policy` via `buildLeafLB(sub)`; hashKey routes WITHIN a subset; all 5 children exercisable.
- **AMEND-SS6 — route-static producer, byte-stable Cluster.** The match parsed ONCE at config-build into a proto-free `cluster.SubsetMatch` on `clusterRouteAction`, threaded VERBATIM per request (NO per-request fold — the contrast with `applyHashKey`). OPTION-C (ctx-carried).
- **AMEND-SS7 — `0064` design + no FuzzSubsetResolve.** TRUE cross-side SET-membership affinity (the `reference_differential_hash_key_cross_side_infeasible` posture is INVERTED — STATIC config is NAT-transparent). The resolution property test is unit-level (folded). 38.2 (weighted-cluster routing + `ClusterWeight.metadata_match`) pre-authorized to its own SPEC/PLAN/IMPL.

## Read first (in order)

1. THIS FILE — the post-phase-38.1-PLAN cold-start (next: the phase-38.1 IMPL — `superpowers:subagent-driven-development`).
2. `docs/envoy-go/STATE.md` — active-phase `phase 38 (load-balancer-subset) PLAN done`; the counts; the lifecycle routing (→ the 38.1 IMPL).
3. `docs/envoy-go/phases/38-load-balancer-subset/PLAN.md` — **the phase-38.1 PLAN** (the File Structure table; the 14 TDD tasks with full code + exact as-built anchors; the D-S38-1..6 resolutions; the ADR-0045 NO-FURTHER-SPLIT verdict; the verification six-gate) + SPEC.md (the §3 architecture; §6 reject roster; §7 the 4 stats + the 33× departure; §8 the `0064`; §11 D-SS1..D-SS7; §12 D-questions; §13 ADR-0239/0240 §Context drafts) + BRAINSTORM.md + README.md.
4. `docs/envoy-go/ROADMAP.md` — row 38 `in-progress` (flat family row; the 38.1 leg flips `→ done` at the IMPL six-gate; 38.2 pre-authorized).
5. `docs/envoy-go/DECISIONS.md` — ADR-0235 (the hash-key seam — the seam-input STYLE TEMPLATE the ADR-0239 draft mirrors) + ADR-0236/0238 (ring_hash/maglev — the per-subset CHILD policies + the gauge-register precedent) + ADR-0237 (the HTTP route producer — the `metadata_match` producer STYLE TEMPLATE) + ADR-0232 (OPTION-C + `noopRelease`) + ADR-0024 (per-cluster LB-state scope — NOTED at the IMPL in ADR-0240) + the process ADRs (0044/0045/0052/0080/0106/0004/0227).
6. `internal/cluster/{cluster.go [Endpoint:33 + the Pick funnels :196/:233/:287 + hashKeyFrom/WithHashKey:172-185], manager.go [the lb_policy switch :251-291 (→ buildLeafLB) + extractEndpoints:517/:533 + registerClusterMetrics:99 (the maglev/ring_hash gauge type-assert :110/:118) + parseRingHashLbConfig:363/parseMaglevLbConfig:407], ringhash.go:60/129, maglev.go:33/39, leastrequest.go:81, random.go:37, loadbalancer.go [the Pick interface :21, roundRobin.Pick :39, noopRelease :26]}` + `internal/stats/{registry.go:79/95, counter.go:22/27/30}` + the producer sites `internal/filter/hcm/{actions.go [clusterRouteAction:201, the 3 ClusterAction call sites :213/:236/:249], config.go [buildRouterAction:536 + parseRouteHashPolicies:566 + the non-Cluster reject :540]}` + `internal/filter/http/router/{router.go [applyHashKey:514 + WithHashKey:544 + H1ClusterAction:558 + doH1ClusterAction:588 + routerAction:716], router_h2.go [H2ClusterAction:39 + doH2ClusterAction:57 + routerActionH2:211]}` + `test/fixtures/0063-lb-maglev/` (the cross-side HTTP-route differential precedent `0064` reuses) + `test/differential/fixture/fixture.go [DistributionAsserter:57, StatsAsserter:75, BackendKind tail :562]`.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — `feedback_execution_style` (subagent-driven IMPL), `feedback_git_worktrees`, `feedback_subagents_no_push` + `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_path_targeting`/`_detach`, and the differential references (`_break_protocol_count1`, `_run_selector`, `_band_sigma_margin`, `_hash_key_cross_side_infeasible` [INVERTED here — static config is NAT-transparent], `_fixture_workload_constant_desync`, `_docker_probe_bridge_network`, `_asserter_dispatch`, `_fixture_dispatch_constraint`, `_conn_wrap_method_no_promote` [generalized — the `Endpoint.Metadata` field threads through every construction site]).

## Honor throughout

- The lifecycle is BRAINSTORM → SPEC → PLAN → IMPL, each its own session. The next session is the phase-38.1 **IMPL** (`superpowers:subagent-driven-development` — execute PLAN.md Tasks 1–14; Task 1 creates PROGRESS.md + re-pins anchors). Then 38.2.
- `feedback_git_worktrees` (the IMPL works in its own worktree) · `feedback_execution_style` (one fresh subagent per task + two-stage review) · `feedback_subagents_no_push` (subagents commit local-only) · `feedback_push_to_origin` (the controller pushes at stage-close) · `feedback_pertask_gofmt_lint` (every task: `gofmt -l` + `golangci-lint` on touched pkgs) · `feedback_subagent_worktree_path_targeting`/`_detach`.
- ADR-0106 (flat family row + the pre-authorized 38.1/38.2 split) · ADR-0044 (ADR §Context at SPEC [DONE — §13], bodies at the 38.1 IMPL Task 14, in-place) · ADR-0045 (the split-gate — FINAL 38.1/38.2 + NO-FURTHER-SPLIT-within-38.1 CONSUMED at the PLAN) · ADR-0052 (the atomic-landing six-gate, at Task 14) · ADR-0080 (byte-stable parse-reject wording — the one producer-side `router-metadata-match-nonscalar` arm) · ADR-0227 (the contrib reference image) · ADR-0004 (the empirical-pin discipline — DONE at the SPEC §11).
