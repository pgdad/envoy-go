=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **PHASE 89 (`h2-decode-filter-mutations`) IS OPEN AT LIFECYCLE-STATE 3 — ROW 89 IS `in-progress` AT `ROADMAP.md:151`.** `want` IS **121**. ⇒ **THIS SESSION PERFORMS THE PHASE-89 IMPL, WHICH CLOSES THE ROW.** This is BANKED mid-lifecycle work: per the standing directive, advancing it OUTRANKS opening a new subject. **DO NOT self-pick.** ⚠️ **THE SENTINEL DOES NOT FIRE AT YOUR START — check (1) PRINTS `NOT DONE: row 89`, check (2) prints SIX, check (3) is SILENT. DO NOT create `stop`.** ⚠️ **MEASURE IT YOURSELF BEFORE DOING ANYTHING ELSE; do not take the previous stage's word for it.**

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=121 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **`want` IS 121 AT THIS TIP AND THE IMPL MOVES IT ZERO.** The IMPL FLIPS row 89 `in-progress` -> `done`; it does NOT add a row. **`want` stays 121 and the row DENOMINATOR stays 121.** Run the sentinel and both NCs on **BOTH SIDES** of the row flip and record ACTUAL output for each — before the flip check (1) prints `NOT DONE: row 89`, after it check (1) goes **SILENT**, at which point the row-62 NC is once more the ONLY thing distinguishing a green board from a broken check.

⚠️ **THE DOCTORING NCs REMAIN MANDATORY EVERY SESSION.** Both:
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md. BEFORE the flip expect "NOT DONE: row 62" AND
# "NOT DONE: row 89"; AFTER the flip expect "NOT DONE: row 62" ALONE.
```
```sh
# the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```

⚠️ **AND WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -oiE '-family row'` fails with `grep: amily row: No such file or directory` and the surrounding arithmetic prints `base=0 now=0 delta=0`, **which reads exactly like "no change".** Observed live at FOUR phase-84 stages and again at the phase-88 PLAN. **Always pass `--` before the pattern**, and remember the counts are form-dependent: **95 occurrences vs 67 LINES** at this tip (`grep -c` counts LINES). **A gate that reads zero on BOTH sides is not evidence of invariance.** ⚠️ AND the carried **ARM-A {119, 131} figure binds ONLY with its escape-aware pipe-split command** (BRAINSTORM-86 §6) — ad-hoc field-count forms do NOT reproduce it (an `NF!=8` form reads **17**); do not "correct" the figure with a different form.

**SENTINEL RUN MECHANICALLY AT THE PHASE-89 PLAN (2026-08-16, base `733f9830`). IT DOES NOT FIRE; `stop` was NOT created** (verified absent at repo root). ACTUAL output, recorded not predicted: input 239 lines / 121 data rows · (1) **`NOT DONE: row 89` ALONE**, denominator silent · (2) **SIX at `:199 :205 :211 :221 :227 :235`** · (3) **SILENT**. **`ROADMAP.md` diff against master EMPTY (0 bytes)** — a PLAN edits no row, so there is only ONE side to measure and the empty diff is what makes that legitimate.
- **ALL FOUR NCs FIRED.** row-62 doctoring with `NC LANDED? [ in-progress ]` inspected FIRST ⇒ `NOT DONE: row 62` **AND** `NOT DONE: row 89` · denominator ⇒ `want=120` gave `NOT DONE: row 89` **plus** `GATE FAIL: examined 121 data rows, expected 120` · check-(3) doctoring (residual **2 -> 0** confirmed on the doctored copy first) ⇒ `NEVER OPENED: gRPC` with WASM correctly silent · check-(2) one-arm counts **5** (long) / **1** (short), union **6** (a one-arm strip is NOT an NC for the union).
- Every leak axis at this tip: `-family row` **95 / 67** · `gRPC-family row` **2** · `Operational-tooling-family row` **3**.
- **ROW 89 IS WELL-FORMED: `fields=8`, with row 88 as the control also reading `fields=8`.**
- ⚠️ do NOT "fix" the six and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases; the candidate sentences are a WINDOW onto a larger deferred backlog, not an inventory of it (`reference_sentinel_matcher_string_self_clears`). ⚠️ **Row 89's own provenance is OUTSIDE every window** (measured at the BRAINSTORM: 0 hits for `h2dispatch` / `SetH2Request` / `decode-side filter mutation` at all six anchors) — **no window narrows at row-done**, and the row flip is not a sentinel-affecting edit.

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (ugrep under the hood). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Reproduced at TWENTY-NINE separate tips.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **THE METHOD BINDS; carry no standing discrepancy figure and no standing absolute.** ⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS; print FILE NAMES, never a count.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files; use `-v --no-index`, and `git status` is the discriminating check). ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **`grep -c` ON ZERO MATCHES IS TOOL-SPECIFIC** (only `git grep -c` prints nothing; all exit 1 — capture the value, never chain on the exit code). ⚠️ **`grep -c` COUNTS *LINES*, NOT *OCCURRENCES*.** ⚠️ **`\t` IN GNU ERE IS A LITERAL `t` — use `-P`.** ⚠️ **A `git grep` PATHSPEC ENDING IN `/` READS A FAIL-UNSAFE ZERO** (shape 33) — use the shell-glob form. ⚠️ **AN UNANCHORED PANIC-GREP FALSE-FIRES 14 TIMES ON A FULLY-GREEN DIFFERENTIAL LOG** — **the canonical gate is ANCHORED: `^panic:|DATA RACE|SIGSEGV`. A gate that fires on a green log is worse than no gate.** ⚠️ **A `\b`-ANCHORED FORM AND A BARE-SUBSTRING FORM CAN DISAGREE AND BOTH BE WRONG:** `\bSetRequest\b` read **0** test files, the substring form read **4**, and all four are **`SetRequestCtx`** — a different symbol on a different type. **Print the MATCHED FORMS (`grep -o 'Sym[A-Za-z]*' | sort -u`) and carry a LIVE positive control** — ⚠️ **and CHECK THE CONTROL ITSELF: the phase-89 PLAN found the SPEC's `SetAction = 2` control was VACUOUS (both hits COMMENT PROSE), and a `buildRequest` control reading 6 was really 3 + three `kafkabroker` hits on a DIFFERENT package's function.** ⚠️ **AND `STATE.md` QUOTES ITS OWN MATCHERS:** an unanchored `grep -c 'next-skill:'` reads **2** on the UNMODIFIED file (line 7 is the ADR-0288 rule quoting the token); the anchored `^- \*\*next-skill:\*\*` reads **1**.

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`** with FOUR blind spots (an em-dash, a DOT in `http-1.1`, two LETTER-suffixed ids). ⚠️ **EXECUTED on scratch copies at the phase-77 PLAN: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** ⚠️ **DO NOT restore the regex form.** ⚠️ **NOTE THE POSTURE: with row 89 `in-progress`, check (1) PRINTS in the healthy case, so it has its own liveness — but THE MOMENT YOUR IMPL FLIPS ROW 89 `done` the board is all-done and check (1) goes SILENT again, at which point the row-62 NC is once more the ONLY thing distinguishing a green board from a broken check. Run it on BOTH sides.**

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

⚠️ **Run the union form, never one half of it.** ⚠️ **A ONE-ARM STRIP IS NOT AN NC** (6 -> 5 short / 6 -> 1 long; never 6 -> 0). ⚠️ **The documented trap's stated MECHANISM is refuted while the trap is real** — the inflation comes from ROADMAP table rows carrying BRAINSTORM prose. **Cite the command; do not re-inherit the explanation.** ⚠️ **The line numbers drift with edits — re-derive them; the current six are `:199 :205 :211 :221 :227 :235`.** All six are standalone family PROSE paragraphs (not rows).

⚠️ **THE INVENTORY IS ~59, NOT ~42.** The long-carried "~42" is the **SENTENCE-VISIBLE** subset only: 43 items by name-level count inside the six live `candidates:` sentences (42 if you collapse `/runtime` + `POST /runtime_modify` into the single clause the sentence writes). **Window `:221` (Observability) names SIXTEEN FURTHER un-chartered items OUTSIDE its live sentence.** **Cite ~42 only as "sentence-visible", never as the inventory.**

⚠️ **AND TWO WINDOW CANDIDATES ARE STALE — THE WINDOWS DO NOT SELF-CLEAN.** Window `:221` still lists (a) *"a pre-existing H2 `//`-path routing bug — `h2/stream.go:440` uses `url.Parse`"*, which **ROW 87 CLOSED** (`internal/filter/hcm/h2/stream.go:478` now calls `url.ParseRequestURI`), and (b) the **`/stats/prometheus` projection-completeness gap**, which **ROWS 79 AND 80 CONSUMED**. **A candidate's presence in a window is NOT evidence it is still open — re-derive at your tip before costing it.** Neither was edited (a window edit is sentinel-affecting).

⚠️ **THE FOUR PHASE-88 DEFERRED CANDIDATES live in ADR-0310 §Consequences (x)**, NOT in a ROADMAP window — stream-scoped header-overflow parity via a decode-and-discard drain · `max_request_headers_kb` config parity + the `http2.header_overflow` stat · `SETTINGS_MAX_HEADER_LIST_SIZE` advertisement · **decode-side filter mutations never reaching the H2 upstream request — THIS IS ROW 89, AND YOUR IMPL CLOSES IT.** ⚠️ **ADR-0310's FIFTH carried class — "a split header block on a stream being concurrently RESET" — IS REFUTED** (phase-89 BRAINSTORM §10.3): `checkFrameOrder` in the pinned `x/net@v0.34.0/http2/frame.go:543` makes any non-CONTINUATION frame a connection PROTOCOL_ERROR while a block is open and `AllowIllegalReads` is never set, so the interleaving is UNREACHABLE. **Do not carry it forward as "unprobed".**


==============================================


## STATUS AS OF 2026-08-16: PHASE 89 (`h2-decode-filter-mutations`) **OPEN AT LIFECYCLE-STATE 3** — row 89 `in-progress` at `ROADMAP.md:151`, `want` **121**, base `733f9830`. **THIS SESSION WRITES THE IMPL AND CLOSES THE ROW.**

**What the 89 PLAN landed (2026-08-16, base `733f9830`, branch `phase-89-plan`, ONE squashed commit).** Docs-only: `phases/89-h2-decode-filter-mutations/PLAN.md` (new, **578** lines), `PROGRESS.md` (144 -> 212), `STATE.md` (rolled IN PLACE, still **64** lines), `STATE_HISTORY.md` (`numstat 2 0`, 502 -> 504), `next-prompt.txt`. **ZERO production `.go`, ZERO test `.go`. `ROADMAP.md`, `DECISIONS.md` AND `BEHAVIOR_CONTRACT.md` ALL BYTE-UNTOUCHED** (EMPTY diff against master). **A PLAN adds no ADR: next-free stays `ADR-0312`; the strict `^> **STATUS: PROPOSED` guard STAYS ARMED at 1 — YOUR IMPL DISARMS IT (1 -> 0).**

Method: **SUBAGENT-DRIVEN** — five probe agents, five disjoint detached worktrees, disjoint port bands 47800-47879, private scratch each; **none committed, each proved its tree clean.** The controller re-derived every load-bearing claim by execution, which produced two refutations no agent made.

### ⚠️ THE HEADLINE: THE PLAN REFUTED ITS OWN SPEC ON THREE DECISIONS, AND ONE CHANGES THE ALGORITHM

1. ⚠️ **D-89-DUP — DUPLICATE-NAME COLLAPSE IS MEASURED AND THE SPEC'S RULE IS REFUTED.** Reference Envoy is an **ORDERED MULTIMAP**: a *replaced* name is **removed everywhere and ONE copy appended at the TAIL** (`x-dup` at `[04]`/`[06]` -> a single `REPL` at `[07]`); *untouched* duplicates stay at their original non-adjacent positions; nothing is comma-joined or collapsed to position 0. Same on H2->H1, H1->H2, H1->H1. **ALL THREE prototypes built at the SPEC and PLAN rewrite at the FIRST occurrence and therefore DIVERGE — this is the one algorithmic change the IMPL must carry over every measured prototype.** Two corroborations: the subject's H2 **passthrough** ordering ALREADY matches the reference exactly, and **the repo's own `upsertH2Header` already implements drop-everywhere-then-append-at-tail** (the reconciler differs only in building a FRESH slice instead of `fields[:0]`). ⚠️ **The H1 leg is NOT a usable ordering anchor** — it groups duplicates adjacently and emits names SORTED (Go `net/http` map semantics), already lossy on this input.
2. ⚠️ **THE CONTRACT EDIT'S "EXACT SUBSTRING" IS NOT UNIQUE, AND THE SPEC'S GATE IS BLIND TO THE FAILURE.** `, H2 differential coverage.` occurs **THREE** times: `BEHAVIOR_CONTRACT.md:32` `header_mutation` (this row's), `:33` `local_ratelimit`, `:34` `csrf`. A naive global `sed` leaves residual **0** and silently closes two carve-outs this row does not own; the scoped edit leaves residual **2**. **BOTH have line delta 0**, so the zero-line-delta gate cannot tell them apart. ⇒ **anchor on the ROW** (`^| HTTP filter \`envoy.filters.http.header_mutation\` |` reads **1**) and **GATE ON THE RESIDUAL COUNT READING 2, NOT 0.**
3. ⚠️ **SPEC §4.4's TWO NUMBERS ARE BOTH WRONG.** `emitAccessLogH2` has **SIX** production call sites (`h2dispatch.go:318 :401 :542 :609 :616 :645`), not five. And there are **THREE** `req.Headers` readers, not two — the third is `reqHeaderLookupH2` (`accesslog_emit.go:230`), feeding tracing `custom_tags` (`:118`) and **`request_headers_to_log`** (`:135`). ⇒ **the fix changes access logs, spans, `request_headers_to_log` AND custom-tag resolution.** The PLAN states it; the IMPL must contract it.

### DECISIONS FROZEN — the IMPL implements these, it does not re-open them

| id | decision |
|---|---|
| **D-89-SHAPE** (SPEC) | DELTA against a pre-decode snapshot of `c.req.Header`. Verbatim `ReconcileOrderedHeaders` reuse and full rebuild both REFUTED BY EXECUTION. Slice-native REJECTED on **MEASURED blast radius — NEVER "per ADR-0071"**. |
| **D-89-SITE** (SPEC) | Keep the existing `SetH2Request` at `:457`; **RE-ISSUE after `RunDecodeData`** (`:551-555`) and before `rf.RunAction` (`:563`), over a **FRESH** slice. Do NOT move it. |
| **D-89-PSEUDO** (SPEC) | Skip every `:`-prefixed key — a **BYTE TEST on the map key**, never a three-name enumeration. |
| **D-89-PROOF** (SPEC) | Extend `0004-h2-routing` IN PLACE. Every count axis +0; `0120` stays UNCONSUMED. |
| **D-89-BODY / D-89-STAT** (SPEC) | Decode-side body mutation OUT of charter. No new stat; DELTA **0**. |
| **D-89-DUP** (PLAN) | **TAIL-APPEND, not first-position collapse.** Added names sorted for determinism (a NAMED deferral — the reference's multi-add order is NOT MEASURED). |
| **D-89-VALIDATE** (PLAN) | Outbound RFC 9113 §8.2.2 guard is **IN**, **VALUE-AWARE**, **EXPORTED** as `h2.IsIllegalH2RequestHeader(name, value)`, and **DROPS** rather than rejects. MEASURED **+22** lines / one extra file. |
| **D-89-HOST** (PLAN) | **SKIP `host`.** Record the residual divergence in the contract or the row's claim ships over-broad. |
| **D-89-CASE** (PLAN) | Emit **lowercase** — measured that NOTHING below the reconciler does. |
| **D-89-DOC** (SPEC+PLAN) | Close the carve-out **IN PLACE, zero line delta, ROW-anchored, residual-count gate = 2**. The mirror sentence at `:829` **STAYS — EXACTLY TRUE.** |

### ⚠️ TEN FINDINGS THE IMPL MUST NOT RE-LEARN

1. ⚠️ **THE OUTBOUND HAZARD IS FOUR, NOT ONE, AND THE FIX INTRODUCES IT.** Measured on the DELTA prototype: `connection` and `transfer-encoding` LEAK; **`te: gzip` LEAKS TOO and a NAME-ONLY GUARD STRUCTURALLY CANNOT CATCH IT** (`te` is deliberately excluded from `isConnectionSpecificField` because it is value-conditional); **header-name CASE is a fourth hazard** — `header_mutation` canonicalizes config keys (`http.CanonicalHeaderKey`, `:145`/`:158`) and **nothing below the reconciler lowercases** (`h2/client.go`: `headers = append(headers, req.Headers...)`, verbatim). ⚠️ **Against a CONFORMANT peer the leak is `400` + body `request header "Connection" is not valid in HTTP/2` + backend request count ZERO** (control: 200 / count 1). `keep-alive`/`proxy-connection`/`upgrade` were config-validated but **NOT wire-measured — INFERRED**. **A stacked over-firing control is MANDATORY: `te: trailers` must be KEPT.**
2. ⚠️ **THE TRACING PIN IS SPLIT ACROSS TWO FILES.** `TestWriteH2_Tracing*` is 2 rows in `tracing_zipkin_dispatch_test.go` (`:163`, `:210`) and 2 in `connection_test.go` (`:855`, `:883`). `-run TestWriteH2_Tracing` denominator **4**, 4/4 PASS at the tip and on both prototypes. **These four ALREADY catch a whole-map projection (SPEC measured 4-of-4 FAIL on the full-rebuild shape) — do NOT understate that.** What they do NOT cover: their chains are **router-only**, so the IMPL owes a FIFTH row in each file with a `[headerMutatingFilter, router]` chain.
3. ⚠️ **THE ASSERTION PRIMITIVE IS INSUFFICIENT.** `h2HeaderValue` (`connection_test.go:726-735`) returns only the **FIRST** case-insensitive match — it cannot see the `[a, c, c]` duplication the `fields[:0]` idiom produces and cannot distinguish "removed" from "present later in the slice". **Any removal arm written with it alone is VACUOUS. Write a count-and-set helper.**
4. ⚠️ **NO EXISTING TEST DOUBLE MUTATES THE DECODE MAP** — all four in-package `DecodeHeaders` stubs are read-only. Clone `orderRecordingFilter` (`chain_dispatch_test.go:26-61`), the smallest complete `HTTPFilter` shape.
5. ⚠️ **THE OBSERVATION SEAM:** `captureH2Action(&captured)` (`connection_test.go:845-853`) installed on a **STRUCT-LITERAL** `&chainDispatchAction{f:…, action:…, req: hreq, routeIdx: 0}` — `disp.Match(req)` builds the action internally and CANNOT be given a capture closure. `c.req` must be non-nil. There is **no exported accessor** for `rf.h2Req`. ⚠️ **`RunAction` is IDEMPOTENT** (`router.go:306`) — the re-issued Set must precede the single `RunAction` call.
6. ⚠️ **THE `RunDecodeData` EARLY-EXIT ARM IS SILENTLY VACUOUS UNLESS `hasBody` IS TRUE** (`h2dispatch.go:515`/`:551`). And the `RunDecodeHeaders`-error exit is reachable ONLY via `TerminateStream` / unknown-status / ctx-cancel — **use `TerminateStream`, NOT ctx-cancel** (which needs a goroutine + deadline and cannot distinguish "parked" from "returned late").
7. ⚠️ **`0004`'s TWO YAMLs SAY "documentation only" BUT ARE THE LIVE TEMPLATES**, and `renderBootstrap` does **POSITIONAL FIRST-OCCURRENCE** `port_value: 0` replacement (`driver.go:188`) — **inserting anything containing `port_value: 0` above an existing occurrence silently reassigns ports.** `driver_test.go`'s `TestRenderBootstrap_*` redden first; run them early. `expectations.yaml` is **PROSE, not machine-evaluated**, and its HTTPExpectations clause is FALSE.
8. ⚠️ **`0012`'s enumeration pattern SORTS the names** (`backends/backend.go:26`), which makes a reflected body ORDER-INSENSITIVE. **D-89-DUP makes ORDER load-bearing for the first time in this lineage**, so the PLAN's design is: sorted block for cross-side byte equality **PLUS** a prefix-filtered `order=` line (`x-p89-`) immune to each proxy's own header additions.
9. ⚠️ **`--allow-h2c` IS NOT PASSED BY THE HARNESS** (`harness.go:252` and `:606` are `exec.CommandContext(ctx, bin, "-c", cfgPath)` and nothing else). A downstream-H2 arm MUST use **TLS+ALPN**; `0004` already does. ⚠️ And **`codec_type: AUTO` + `--allow-h2c` DOES NOT give plaintext H2** — only the `HTTP2` arm consults `AllowH2C`.
10. ⚠️ **A PHANTOM SYMBOL SITS IN THE VERY BLOCK YOU EDIT.** `h2dispatch.go:462`, `:479`, `:491` cite `h2.parseHeadersForRequest`, one with a hard line number; `git grep -c '^func.*parseHeadersForRequest'` ⇒ **0** against a positive control of **1** for `buildH2Request`. **Do NOT read those comments as a map of the code.** Recorded, NOT fixed.

### ⚠️ COST — THREE SAMPLES, AND THE SPREAD IS THE FINDING

SPEC **+162** @ `c1284a03` (69 comment / 5 blank / 24 brace / **64 substantive**) · agent D **+158** @ `733f9830` (84 comment / 6 blank / 19 brace / **49 substantive**) · agent B reconciler-only **+121**, reconciler+validation **+127 / +16**. ⚠️ **The near-agreement of 162 and 158 is PARTLY COINCIDENCE — the totals differ by 4 while the SUBSTANTIVE counts differ by 15.** **Quote +162 (the highest) as the floor.** ⚠️ **AND IT IS STILL A FLOOR because NONE of the three implements D-89-DUP's tail-append rule.** Remainder enumerated in PLAN §8.

**IMPL bands:** production **~+185-250** · unit **~+250-450** (revised UP from the SPEC's ~+150-350 — the roster grew) · differential **~+145-200** (anchored on phase 88's **MEASURED** `+172/-7` into the SAME fixture — `git show --numstat 4eab3f72 -- test/fixtures/0004-h2-routing/`).

### ⚠️ COUNTS AT THIS TIP — RE-DERIVE MECHANICALLY, NEVER COPY

- **`ROADMAP.md` 239 lines / 121 data rows**; row 89 `in-progress` at `:151` (`fields=8`, well-formed), all others `done`; check-(2) anchors `:199 :205 :211 :221 :227 :235`; `-family row` **95 occurrences / 67 LINES**; `gRPC-family row` **2**; `Operational-tooling-family row` **3**; ARM-A flags **{119, 131}** (escape-aware command ONLY).
- **`DECISIONS.md` 18228** lines · **310** headings · tail **ADR-0311** at `:18210` (§Context only, PROPOSED) · next-free **ADR-0312** (TAIL-derived; `grep -c '^## ADR-0312'` => **0**; headings+1 COLLIDES at the ADR-0209 gap) · **strict `^> \*\*STATUS: PROPOSED` guard 1 — ARMED at `:18212`; YOUR IMPL DISARMS IT 1 -> 0** · STATUS census **24** · `^---$` **216**. ⚠️ **A NEW ADR TAKES NO `---` SEPARATOR** and ADR-0311 is COMPLETED IN PLACE after its retained italic footer — no renumber. ⚠️ **CARRY NO WHOLE-FILE COUNT OF THE LOOSE `PROPOSED` MATCHER.**
- **`BEHAVIOR_CONTRACT.md` 5960** — the IMPL's edits: (a) the **ROW-ANCHORED, IN-PLACE** deletion of `, H2 differential coverage.` from `:32` ⇒ **ZERO line delta, residual count 2**; (b) **TWO ADDED sentences** (the `host` no-op, and the access-log / span / `request_headers_to_log` / `custom_tags` change) — ⚠️ **those ARE line-adding; state the delta and re-anchor any by-line cite at or below the insertion point.** Phase 88 shifted these anchors +2, phase 87 +1. **CITE BY STRING OR SYMBOL.**
- **`STATE.md` 64** · **`STATE_HISTORY.md` 504** (archive labels **201**) · **`BOOTSTRAP_PROMPT.md` 522** (repo ROOT) · phase dirs **130** · `REVIEW.md` **37** (standing departure, named not claimed).
- **differential fixtures 121** at **`test/fixtures/`** (⚠️ **NOT `test/differential/fixtures/`, a plausible wrong path that returns a SILENT `0`**); numeric tail `0119-grpc-unary-trailers`, next-free **`0120` STAYS UNCONSUMED**. · **fuzzers 55 / 48 files.** · **BackendKind tail 38** (`H2GoawayResponder`, `fixture.go:614`). · **fixture blank imports 121** — the NARROWED form `grep -cP '^\t_ "[^"]*test/fixtures/'` => **121**; ⚠️ **the unnarrowed `grep -cP '^\t_ "'` READS 123 AND IS REFUTED**. · **`0004`'s blank import is at `runner_test.go:30`.** · **`0004` issues 31 round-trips per side** (9+9+9+2+1+1, counted by call site x literal loop bound) — ⚠️ **`ADR-0057` (`DECISIONS.md:2056`) still says 27 and is STALE; `README.md:36`'s "first 27 requests" is CORRECT as a prefix — do NOT "fix" it; `expectations.yaml:10` already says 31.**
- **stat surface: the COMMAND binds, not the number.** `grep -ro --include='*.go' -e 'NewCounter(' -e 'NewGauge(' .` => **406** occurrences (`NewCounter(` 327 + `NewGauge(` 79); `-rn` => **404** lines. **Measured 406 on BOTH prototypes ⇒ DELTA 0.**
- **The slice-only-writer gate reads 5 at this tip and MUST READ 6 at row-done** (the reconciler is the sixth writer) — `git grep -nE '\.Headers[[:space:]]*=[^=]' -- 'internal/filter/hcm/*.go' 'internal/filter/hcm/h2/*.go' | grep -v '_test\.go' | grep -v 'resp\.Headers' | wc -l`. **State the new number; a gate whose reading silently moves is not a gate.** ⚠️ **NAMED BLIND AXES: element-level in-place mutation (`.Headers[i].Value = …`) and any writer outside `internal/filter/hcm/`.**
- ⚠️ **STILL CONTESTED, NO NUMBER CARRIED:** the `STATE_HISTORY.md` archive-gap total · the loose `PROPOSED` matcher's whole-file count · the stat-surface DOC-side absolute (1205 vs 1207) · the zero-test `generic/*` suite count (13 vs 14).

### ⚠️ NOT MEASURED — do not treat any of these as settled

- **`keep-alive` / `proxy-connection` / `upgrade` on the wire** (config-validated only — INFERRED).
- **The reference's ordering for MULTIPLE simultaneously-added names.** The sort is chosen for determinism, not measured fidelity.
- Whether the reference applies decode-side mutations over downstream H2 with **wasm** or **extproc** (Lua and `header_mutation` WERE measured, both legs).
- **Runtime cost of two per-request `http.Header` deep copies** — no benchmark was run.
- The `wasm`/`lua`/`extproc` guest-boundary cost of the REJECTED slice-native shape.

### ⚠️ DOCUMENTARY DEFECTS — carried forward, still deliberately NOT fixed

**NEW AT THIS STAGE:** ⚠️ **the PHANTOM `h2.parseHeadersForRequest` cited at `h2dispatch.go:462`/`:479`/`:491`** · ⚠️ **`0004/expectations.yaml:3-7`'s HTTPExpectations clause is FALSE** (`driver.go:498-504` says it is intentionally NOT implemented) — **the row edits that file and should fix it** · ⚠️ **TWO PRE-EXISTING SUBJECT `host`/`:authority` BUGS found with no filter at all**: inbound `:authority` **and** `host` ⇒ envoy-go projects **BOTH** upstream while the reference DROPS the regular `host`; inbound `host` **only** ⇒ envoy-go emits **`:authority = ""` (EMPTY)** while the reference **PROMOTES** it. **Independent of phase 89; named, not chartered.**
**CARRIED:** `internal/filter/http/types.go`'s FALSE *"per ADR-0071's filter API stability"* comment (⚠️ **write "rejected on measured blast radius — 33 production + 38 test files, ~101 mutation call sites, and an `OrderedHeaders` type carrying only `Get`/`ToHTTPHeader`". NEVER "per ADR-0071"**) · `header_mutation` rejecting `remove` of a protected header where the reference config-ACCEPTS it · `RunAction`'s unguarded H2 arm · `ADR-0051` §2 · `ADR-0058`'s dead `routerActionH2.doH2` location · **`ROADMAP.md` rows 119/131 malformed (ARM-A GUARD)** · **`STATE.md` §Project counts FROZEN at phase 76** (they self-contradict §Current — do not anchor on them) · `harness_test.go` port inventory STALE · `body.go` nolint INERT · xDS cycle guard NOT AUTOMATED · `wasm/doc.go:219` two errors · ROADMAP cites `esalaine` FIVE times · `rbac.go:50` token `F2` · root `PROGRESS.md` stray phase-32.1 doc · SPEC-86/PLAN-86 cite the grpcclient Close pins at a nonexistent `internal/xds/xdsgrpc/...` path · `BEHAVIOR_CONTRACT.md`'s "no stdlib net/http parsing" sentence (RETAINED, ridden by ADR-0309) · `ADR-0057`'s "27 round-trips" · the TWO riders citing the ADR-0052 mandate at a DRIFTED `:1821` · window `:221` carries TWO CLOSED candidates.

### ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + BOTH DOCTORING NCs YOURSELF, ON BOTH SIDES OF THE ROW FLIP**, and record ACTUAL output.
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** The 89 PLAN refuted the SPEC's duplicate-collapse rule, its contract-edit anchor, both of its §4.4 numbers, its tracing-pin file list, its `SetAction` positive control, and a STANDING METHOD NOTE. Do the same.
3. **LINE ANCHORS DRIFT; SYMBOL ANCHORS DO NOT — RE-VERIFY EVERY CITE AT YOUR TIP.**
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Still live: the **SDS dial-budget class** (`internal/boot/boot_sds_e2e_test.go:551`) · the **driver-owned receiver port race** (`0081-grpc-access-log` `0.0.0.0:35097`, `0087-tracing-otlp` `0.0.0.0:35323`; **it ABORTS the binary mid-suite and the harness wrapper still exits 0**) · `internal/cluster` `-race` outlier · `internal/httpclient` zero-value · the two 84.2-era flakes · `TestServerConn_TinyWindowDelivery` · the REFERENCE h2spec section-8 flip. **Capture `-v` output BEFORE re-running anything.**
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only). ZERO containers were running at this stage's start and ZERO at its close.
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`.
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL** (⚠️ **QUALIFIED**), **and ASSERT THE BOUND BINARY with `ss -ltnp` before reading any probe result.** **A harness's exit code is not the command's**; **`gofmt -l` never exits non-zero — gate on OUTPUT.** ⚠️ **A `-run` selector matching nothing prints `[no tests to run]` and EXITS 0 — quote the `=== RUN` DENOMINATOR on every census.**
8. ⚠️ **`pgrep -f` / `pkill -f` MATCHES YOUR OWN SHELL AND KILLS THE TOOL CALL (exit 144)**, and ⚠️ **a `pgrep -f 'a\|b'` ALTERNATION MATCHES NOTHING SILENTLY** — kill by PID from `pgrep -laf` with the `bash -c` line filtered out, ONE pattern at a time, and verify ports released with `ss -ltn`.
9. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-89-*`, `wt-89-*` or stage worktree should outlive its stage close** — expect only `master` plus your own (verify with `git worktree list`).
10. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** (a)/(b) differential 121 fixtures, full suite with `INNER_EXIT`; (c) **h2spec cite ONLY from your own run — and note that its harness configures `envoy.filters.http.router` ALONE with `direct_response` routes and never goes upstream, so it is STRUCTURALLY incapable of anchoring this row**; grpc-conformance deferred in writing; proxy-wasm 10/16; (d) fuzzers 55/48; (e) `INNER_EXIT` on every differential launch AND `go test ./...`; (f) no REVIEW.md — standing departure.
11. ⚠️ **THE ARCHIVE-ABSENCE GUARD: USE THE OWN-LABEL FORM, AND NC IT.** The whole-file `grep -cF` form is FAIL-UNSAFE (an evicted label can ALREADY appear in a previous bullet's PROSE). The working form: `grep -oP '`[^`]+`(?= \(\d{4}-\d{2}-\d{2}\))'` — it read **200** labels before this row's append and **201** after, with the evictee verified ABSENT (0) first and the NC positive-control fired on `phase 87 (h2-double-slash-path-routing) IMPL done`, which IS present. ⚠️ **§Recent is capped at FIVE. This stage faced NO TIE** — a DIRECT DATE READ gave 2026-08-15 x2 / 2026-08-14 x2 / **2026-08-13 x1**, a UNIQUE oldest. ⚠️ **The tie shape does NOT carry** (SPEC-89 two-way, BRAINSTORM-89 three-way, IMPL-88 four-way). **Read all five dates DIRECTLY at YOUR tip before removing anything.** ⚠️ **And census `STATE.md`'s lineage bullets LINE-ANCHORED** — the file quotes its own matchers.
12. ⚠️ **CORRECTED AT THIS STAGE — `-p` PUBLISHING WORKS; `--network host` IS WHAT DOES NOT.** The carried note claimed published ports were unreachable too. **REFUTED by execution:** `docker run -d --rm --name <n> -p 47899:80 nginx:alpine` then `curl 127.0.0.1:47899` ⇒ **HTTP=200**, with the host's `ss -ltn` showing the port. The real mechanism is narrower: **`--network host` does not share the host netns here AND it SILENTLY IGNORES `-p`**, so a probe combining them reads as "publishing is unreachable". **Host-gateway is `192.168.65.2`** — a Docker-Desktop-style VM daemon, NOT a `172.17.x` bridge gateway, which is exactly WHY host networking fails. **Both `-p` publishing and `--add-host=host.docker.internal:host-gateway` are working recipes.** ⚠️ Still true: a **STRICT_DNS** cluster whose name does not resolve **blocks listener init entirely** (Envoy logs "starting main dispatch loop" while NO listener is bound) — use STATIC, or put **`dns_lookup_family: V4_ONLY`** on any cluster dialing `host.docker.internal`, and **verify the listener is bound before believing any probe result**.
13. ⚠️ **NEW — A RAW-FRAMER BACKEND MUST HOLD ONE `hpack.Decoder` PER CONNECTION.** The HPACK dynamic table is **CONNECTION-scoped**; a fresh decoder per request decodes only the FIRST request on a pooled connection and then yields `invalid indexed representation index NN` with a **truncated field list — which reads exactly like "headers were lost"**. One probe caught this in its own instrument, fixed it, re-ran everything, and asserted an `HPACK-ERR` count of **0**, with a client->backend DIRECT negative control reproducing `a=1,b=2,a=3` at indices 4/5/6 verbatim. ⚠️ **AND A RAW-FRAMER BACKEND IS MANDATORY** — a `net/http`+`h2c` backend normalizes case, hides pseudo-headers and destroys order, showing GREEN for an illegal wire encoding. **Assert the backend RECEIVED something (n>0) BEFORE interpreting what it received.**

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 89 (h2-decode-filter-mutations)' --format='%h %s'   # 3 at this tip: BRAINSTORM, SPEC, PLAN
git log --grep '^phase 88 (h2-continuation-frames)' --format='%h %s'       # 6: a CLOSED phase, use as a FIRING CONTROL
```
⚠️ **The count comments are LIVE FIGURES — VERIFY before relying.** Take the NEWEST line (`| head -1`).

Execution style: **subagent-driven per `feedback_execution_style`**. Subagents commit LOCALLY on the ONE stage branch with EXPLICIT PATHSPECS; the controller squashes (`feedback_subagents_no_push`, `feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** and tear down docker **BY NAME**. ⚠️ **Band private ports outside BOTH `20000-31007` AND `11000-14999`, clear of the static fixture ports (10000-19172); the driver-owned receiver race has been observed at `35097` and `35323`, and `0081` also binds `0.0.0.0:42039`.** The 87 stages used 47410-47441, the 88 stages 47450-47559, the 89 BRAINSTORM 47560-47599, the 89 SPEC 47600-47719, the 89 PLAN 47800-47899; band yours from **47900+**. ⚠️ **ONE WORKTREE PER AGENT preferred; explicit-path staging if shared; never an unscoped `git restore`.** Per-task `gofmt` + `golangci-lint` on touched packages (⚠️ **misspell runs in locale US and has fired UNFORCED — sweep British spellings in `.go` comments BEFORE the lint gate**). ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch.** ⚠️ **Docker BY NAME, `--rm`, verify gone.**

⚠️ **AND envoy-go REJECTS `match.headers` outright** — `route 0: match.headers is not supported in phase 04` — so a header-match route works on the REFERENCE side only. ⚠️ **AND `clusters: []` is BOOT-REJECTED even when every route is a `direct_response`** (`cluster manager: zero clusters in bootstrap`) — a dummy cluster is required.

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root — **observed repeatedly at every phase-88 AND phase-89 stage, and the harness ANNOUNCES it** (`Shell cwd was reset to /home/esa/git/envoy-go`). Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run. ⚠️ **Shell VARIABLES do not survive between Bash calls** — re-export `$W`/`$SC` in every command.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — `git status --porcelain` AND `git diff --stat master` must show ONLY your intended edits. For a patched TRACKED file: `sha256sum` BEFORE patching, `git checkout -- <path>` after, `sha256sum -c` the capture. ⚠️ **At the 89 PLAN all five probe agents ended with EMPTY `status --porcelain` and `diff --stat`.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads the phase-89 IMPL.** §Project counts SELF-CONTRADICT §Current — do not anchor on them (frozen at phase 76).
2. `docs/envoy-go/phases/89-h2-decode-filter-mutations/PLAN.md` — **§0 is the three refutations; §2 is the seven frozen decisions; §3 is the slice-only-writer inventory and its gate; §4 is the ordered T1-T8 tasks; §5 is the unit roster; §6 is the `0004` arm roster with seven break arms at seven NAMED injection sites; §7 is the docs text; §8 is the cost; §9 is the gates, the green baseline and the corrected method note; §10 is what is NOT measured.**
3. `docs/envoy-go/phases/89-h2-decode-filter-mutations/SPEC.md` — §3 D-89-SHAPE with all three prototypes; §4 D-89-SITE and the aliasing table; §5 D-89-PSEUDO; §6 D-89-PROOF; §14 the eleven SPEC findings.
4. `docs/envoy-go/phases/89-h2-decode-filter-mutations/PROGRESS.md` — the PLAN entry: the three refutations, the eight controller traps, the sentinel output, the probe hygiene.
5. `docs/envoy-go/DECISIONS.md` **ADR-0311** (tail at `:18210`, §Context only, STATUS **PROPOSED**) — ⚠️ **YOUR IMPL COMPLETES IT IN PLACE after the retained italic footer and flips the strict guard 1 -> 0.**
6. `BOOTSTRAP_PROMPT.md` §4-§7 (repo ROOT) — the lifecycle contract and the six-gate posture.

## What the IMPL owes
**ONE ATOMIC COMMIT** delivering PLAN §4's T1-T8, TDD held INSIDE it: the **RED census observed FIRST** with denominators against the green baseline (`hcm` **301** / `hcm/h2` **192** / `router` **122**, 3 `ok` / 0 `FAIL`), **the fifth tracing row in BOTH `connection_test.go` and `tracing_zipkin_dispatch_test.go` inside the RED set** · the reconciler in `h2dispatch.go` with the **TAIL-APPEND** rule, the `host` skip, explicit **lowercasing**, a **FRESH** slice (never `fields[:0]`), the re-issued `SetH2Request` after `RunDecodeData`, and the **exported value-aware** `h2.IsIllegalH2RequestHeader` guard **DROPPING** illegal fields · the **13-row** reconciler table with a **count-and-set** helper (not `h2HeaderValue`) · the three early-exit arms (⚠️ `RunDecodeData`'s is **vacuous unless `hasBody`**) · the `SetH2Request` arm **in `hcm`** · the `buildH2Request` characterization pin (⚠️ **PASSES at the tip — do NOT report it inside the RED census**) · the `0004` extension with **eight arms and seven break arms at seven DISTINCT injection sites**, one break per run with `-count=1`, confirming WHICH assertion fired · **the ROW-ANCHORED contract edit gated on a RESIDUAL COUNT of 2, not 0** · the **two ADDED contract sentences** (line-adding — state the delta) · **ADR-0311 completed IN PLACE with the strict guard 1 -> 0** · **row 89 flipped `done`** at `ROADMAP.md:151` with `want` **121 -> 121** · the slice-only-writer gate re-read at **6** and STATED · **cost re-measured at the IMPL's OWN publishing commit** · `PROGRESS.md` appended · `STATE.md` rolled IN PLACE (lifecycle-state **3 -> DONE**) with the oldest §Recent entry evicted by a DIRECT DATE READ · `next-prompt.txt` rolled (`git add -f`) · ONE squashed commit, merged and pushed. ⚠️ **Then RE-EVALUATE THE SENTINEL: with row 89 `done` the board is all-done and check (1) goes SILENT — but check (2) still prints SIX, so the sentinel STILL DOES NOT FIRE and the next session SELF-PICKS phase 90.**
