START a fresh cold-start session for envoy-go — **phase 27 (`network-filter-sni-cluster`) SPEC is DONE + committed + pushed** (master tip `85116a3`, the substantive SPEC squash being `e7c59e9` — the tip trails it via docs-only next-prompt repoints), and the phase is OPEN/`in-progress`. There is an active phase. This session does the **27 PLAN** — per SKILL_ROUTING state 2 → 3 (`SPEC.md` exists, `PLAN.md` does NOT) the next lifecycle step is `superpowers:writing-plans` → output `PLAN.md`, with the ADR-0045 split-gate re-check at PLAN time. Author `docs/envoy-go/phases/27-network-filter-sni-cluster/PLAN.md` decomposing the SPEC §10 spine into bite-sized TDD tasks (per the `feedback_execution_style` subagent-driven discipline, executed only AFTER the PLAN exists).

The phase-27 SPEC settled (all eight §10 D27-1..D27-8 empirical pins resolved IN-SESSION against Envoy v1.37.2 per ADR-0004): `sni_cluster` (`envoy.filters.network.sni_cluster`, type `…sni_cluster.v3.SniCluster` — an EMPTY proto, `@type` via `proto.MessageName`) is a config-less read-filter that in `OnNewConnection` reads `Connection.RequestedServerName()` and, if non-empty, calls a NEW `SetUpstreamCluster(name)` on `ReadFilterCallbacks` with the SNI VERBATIM, then returns `Continue` (no sticky halt). The override is a NARROW typed `chainRuntime.upstreamClusterOverride` field (NOT a general filter-state primitive — Q2/YAGNI), threaded to the terminal `tcp_proxy` via the call `ctx` (`handleTerminal` wraps it; `network.UpstreamClusterOverride(ctx)`; the `TerminalFilter.Handle` signature is UNCHANGED — no HCM ripple). `tcp_proxy` is made per-connection-resolving (retain `*cluster.Manager` + boot-resolved `defaultCluster`; override-then-fallback at `Handle`; unknown override cluster → downstream close / zero bytes [Envoy's `downstream_cx_no_route` family is NOT mirrored — surface +0]; back-compat byte-exact via the EXISTING 0000/0001/0002 + 26.x fixtures). ADR-0219 (override seam + tcp_proxy resolution) + ADR-0220 (sni_cluster filter) §Context are DRAFTED in DECISIONS.md (tail ADR-0220; next-free ADR-0221; §Decision/§Consequences bodies land at 27 IMPL per ADR-0044).

## What this session must do

There IS an active phase (27, `in-progress`, SPEC done). Per SKILL_ROUTING state 2 → 3 the next step is the **PLAN** via `superpowers:writing-plans` (→ `PLAN.md`). Read the SPEC first; then decompose its §10 spine (~9–14 bite-sized TDD tasks) — the indicative spine: (1) baselines/anchors gate (re-pin fuzzers 36 / fixtures 46 tail 0044 / stats 136 / DECISIONS tail ADR-0220 / next-free ADR-0221 + the as-built line anchors + `proto.MessageName`); (2) the override field + `SetUpstreamCluster` ON `ReadFilterCallbacks` + concrete `*callbacks` impl; (3) the `network.UpstreamClusterOverride` context accessor + `handleTerminal` ctx-threading; (4) `tcp_proxy` per-connection resolution (retain `cm` + `defaultCluster`; override-then-fallback + unknown→close + **back-compat unit tests**); (5) the `internal/filter/network/snicluster/` config-less filter (`OnNewConnection` SNI→override+`Continue`; no-op `OnData`/`OnDestroy`; prove the `SetUpstreamCluster` call is LIVE); (6) registration as the 6th built-in (`builtins.RegisterBuiltins` + main.go parity); (7) the `0045-sni-cluster` 3-arm cross-side TLS fixture (route / fallback / unknown-close); (8) back-compat differential re-verify; (9) the completion bundle (BEHAVIOR_CONTRACT 27 subsection + the `tcp_proxy` amendment + ADR-0219/0220 §Decision/§Consequences bodies + STATE/ROADMAP row-27 phase-done + six-gate). **RE-CHECK the ADR-0045 split-gate at PLAN time** (working estimate LOW — single phase; the SPEC §3.0 LoC/task envelope holds). Then run the `plan-document-reviewer` loop (per the writing-plans Plan Review Loop), advance STATE, commit, push. Subagents commit LOCAL-ONLY [`feedback_subagents_no_push`]; the controller squash-merges + pushes at stage-close [`feedback_push_to_origin`]; work in a git worktree [`feedback_git_worktrees`].

## Project state at master tip

- **Master tip:** `85116a3` (a docs-only next-prompt master-tip repoint), which trails the substantive phase-27 SPEC squash `e7c59e9` (the SPEC + ADR-0219/0220 §Context + STATE/ROADMAP advance live there). Confirm the live tip via `git log --oneline -1`; any further next-prompt-only repoint advances the tip +1 (expected, not chased; per the 26.x precedent). STATE.md `last-commit` points at the SPEC squash `e7c59e9`. origin/master in sync after push.
- **§9 Network-filters family:** parent row 26 DONE; row 27 (`network-filter-sni-cluster`) `in-progress` (SPEC done). The family stays OPEN — after phase-27 phase-done, 5 candidates remain (`redis`/`mongo`/`kafka_broker`/`thrift`/`zookeeper`).
- **Counts at master tip (unchanged by SPEC):** stat surface **136**; differential fixtures **46** (tail `0044`); fuzzers **36**; conformance 10/10; h2spec 53/53; DECISIONS.md tail **ADR-0220** (§Context drafted; bodies at IMPL); next-free **ADR-0221**.
- **Anticipated phase-27 deltas at IMPL:** fixtures 46 → 47 (one `0045-sni-cluster` cross-side multi-cluster TLS dir; NO boot-reject — config-less); stat surface 136 (+0); fuzzers 36 (+0 — DEFERRED, echo-parity config-less); ADRs +2 bodies (ADR-0219 + ADR-0220; NO new number consumed at IMPL).
- **Toolchain:** Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); reference Envoy v1.37.2 (ADR-0008); go-control-plane v1.32.4 (ADR-0008).

## Read first (in order)

1. THIS FILE — the 27-PLAN cold-start.
2. `docs/envoy-go/phases/27-network-filter-sni-cluster/SPEC.md` — the settled SPEC (16 §§ + 1 appendix; §3 the override seam shaping [D27-3], §3.3 tcp_proxy per-connection resolution, §4 the filter, §8 the fixture taxonomy, §10 the per-task spine, §11 the empirical-pin evidence, §12 the PLAN/IMPL D-questions).
3. `docs/envoy-go/phases/27-network-filter-sni-cluster/BRAINSTORM.md` — the 3 Q-decisions the SPEC refines.
4. `docs/envoy-go/STATE.md` — active-phase `phase 27 SPEC done`; next-skill `superpowers:writing-plans` (scoped to 27 PLAN); next-free `ADR-0221`; last-commit the SPEC squash.
5. `docs/envoy-go/ROADMAP.md` — row 27 `in-progress` (SPEC DONE note); the §9 family-open note + the 5 remaining post-27 candidates.
6. `docs/envoy-go/SKILL_ROUTING.md` + `BOOTSTRAP_PROMPT.md` §4/§5 — the per-phase lifecycle (state 2 → 3 PLAN + the GATE) + the cold-start invariants.
7. The AS-BUILT framework the PLAN targets: `internal/filter/network/chain.go` (the `chainRuntime` struct @127, `handleTerminal` @209, the concrete `*callbacks` @321, the `SetResponseCodeDetails` type-assert sink @351 — the rejected-pattern reference); `internal/filter/network/callbacks.go` (the `ReadFilterCallbacks` interface @16 + `DynamicMetadata()` @25 the on-interface precedent + `RequestedServerName()` @69); `internal/filter/network/terminal.go` (the `TerminalFilter.Handle` signature @42); `internal/filter/tcpproxy/filter.go` (the `Filter` struct @26 / `NewFilter` @47 / `NewNetworkFactory` @81 / `Handle` @94 — the per-connection-resolution target); `internal/filter/network/echo/echo.go` (the config-less template); `internal/filter/network/builtins/builtins.go` (the 5 built-ins; sni_cluster = 6th); `internal/listener/manager.go` (`serveNetworkChain` @1025; the post-OnNewConnection `TerminalReady` mixed handoff @1046).
8. `docs/envoy-go/DECISIONS.md` ADR-0219 + ADR-0220 (the §Context drafts — bodies at IMPL) + ADR-0213/0214/0215 (the §9 framework) + ADR-0044 (in-place ADR-body discipline) + ADR-0045 (split-gate — re-check at PLAN) + ADR-0052 (atomic doc bundle) + ADR-0005 (writing-plans-for-PLAN) + ADR-0079 (two-step factory) + ADR-0080 (byte-stable wording).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — execution-style + worktree + push + subagents-no-push + the network-filter constraints (typeurl-extensions, onnewconnection-halts) + the differential dispatch/asserter constraints + per-task gofmt/lint — all carry forward to the 27 PLAN/IMPL.
