=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, the active phase is fully landed, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action. (The sentinel does NOT apply right now — phase 48 is chartered, its BRAINSTORM + SPEC + PLAN are LANDED, and the next stage is its IMPL.)

**STATUS AS OF 2026-06-30: phase 48 (`stats-sink-statsd`) PLAN is LANDED; the NEXT stage is its IMPL.**
- The phase-48 PLAN landed: `docs/envoy-go/phases/48-stats-sink-statsd/PLAN-48.md` (a 10-task TDD spine mirroring the single-flat-row `PLAN-47.1.md`; lifecycle-state 2 → 3; docs-only direct-on-master). It RESOLVES every §12 D-SD-* PLAN question (see below). A `plan-document-reviewer` (general-purpose) pass APPROVED-WITH-FOLDS — ALL `bootstrap.go`/`main.go`/`internal/statssink` line citations VERIFIED EXACT against HEAD, the import-cycle reasoning sound, baselines (93 fixtures / 50 fuzzers) confirmed, the Docker API surface available, the five high-risk areas covered — after two NON-BLOCKING folds were applied (the Task-7 `NewDockerClientWithOpts` return-type NOTE; a note to refresh a stale `statssink_fuzz_test.go:110` seed comment). STATE.md active-phase header rolled to `phase 48 (stats-sink-statsd) PLAN done`. **Re-verify with `git log --oneline -6` + `git status`; do NOT assume.**
- **The §12 D-SD-* PLAN questions are RESOLVED (do NOT re-litigate; the IMPL EXECUTES these):** D-SD-SPLIT → NO sub-split (single flat row, ~200–250 LoC; escape-valve UNCONSUMED). D-SD-CONFIG-HOME → a parallel `StatsdSinkConfigs []StatsdSinkConfig` slice on `internal/bootstrap`; `main.go` runs TWO build loops into the ONE `statsSinks []Sink`. D-SD-LIFECYCLE-SHAPE → SYNCHRONOUS per-flush `Write` (no channel/goroutine; UDP never blocks + serial `Submit`; NO background mutator; `Write`-error rate-limit-logged via the `sink.go` `lastDropLog`/`dropLogIntervalNanos` idiom). D-SD-DELTA-REUSE → reuse `delta.go` `newDeltaState()`/`apply()` VERBATIM, sink-private, ALWAYS-on. D-SD-ADDR → `UDPAddress string` host:port (`fmt.Sprintf` at parse, NO new import) resolved at `NewStatsdSink`. D-SD-RECEIVER-WIRING → a NEW `differential.HostGatewayIP(ctx)` harness helper (Docker-inspect the `bridge` IPAM gateway) feeds the reference a LITERAL IP (statsd rejects hostnames); receiver binds `0.0.0.0`; two per-side receivers; reachability proven at the `0092` task; user-defined-bridge fallback flagged. D-SD-FUZZER → `FuzzStatsdSinkConfigParse` (50 → 51). D-SD-STATS-FINAL → +0 via the existing surface test. D-SD-GAUGE-SUBSET → YES (`0092` adds `cluster.<backend>.membership_healthy == 1|g`).
- Counts at PLAN (re-verify, do NOT assume): stat surface **1200** (non-H2 **1196**) / fixtures **93** / fuzzers **50** / BackendKind **38** / DECISIONS **ADR-0264** (next-free **ADR-0265**).

=> The NEXT action is the **phase 48 (`stats-sink-statsd`) IMPL** (lifecycle-state 3 → done). NOT the termination sentinel. NOT a new PLAN.

==============================================

## The NEXT action — the phase 48 (`stats-sink-statsd`) IMPL
Execute `docs/envoy-go/phases/48-stats-sink-statsd/PLAN-48.md` end-to-end (the 10-task TDD spine) **subagent-driven** (`feedback_execution_style`) in a **FRESH git worktree off master** (`feedback_git_worktrees` · `superpowers:using-git-worktrees`). This is the FINAL stage of phase 48 — its six-gate FLIPS ROW 48 `done` (the sole leg — ADR-0106; NO parent rollup; the Observability family STAYS OPEN) and ANCHORS ADR-0265 §Decision/§Consequences (ADR-0044). Lifecycle-state 3 → done.

**Execution mechanics (the controller discipline — honor verbatim):**
- **Subagent-driven** (`superpowers:subagent-driven-development`): dispatch a FRESH subagent per task (or a small batch); the CONTROLLER reviews between tasks.
- **Fresh worktree off master** (`feedback_git_worktrees`): create the worktree BEFORE Task 1; all task work lands there; the controller verifies the MAIN checkout stays clean + the branch undetached after each task (`feedback_subagent_worktree_detach` · `_path_targeting`).
- **Subagents commit LOCALLY only** (`feedback_subagents_no_push`); the global CLAUDE.md makes them AUTO-COMMIT (`feedback_subagent_autocommit_claudemd`) — do NOT fight it. The CONTROLLER verifies each commit (correct fileset, real non-vacuous tests via `-v` + read assertions, gates green), cleans stray next-task leak files, re-runs the FULL suite on the FINAL frozen HEAD, does the deliberate-break verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_push_to_origin`).
- **Per-task gates** (`feedback_pertask_gofmt_lint`): `gofmt -l` (empty) + `golangci-lint run` on touched pkgs + `go vet` + `go build ./...`.
- **Differential discipline (the `0092` task — Task 8):** `-run 'TestDifferential/0092'` NEVER bare (`reference_differential_run_selector`); `-count=1` on EVERY break + `-race` run (`reference_differential_break_protocol_count1`); the full-suite `subject ready: EOF` startup flake is isolatable (`reference_differential_fullsuite_startup_flake`); the Docker bridge + decode-ran proof — receiver datagram count > 0 (`reference_docker_probe_bridge_network`); the driver-owned UDP receiver is NOT a BackendKind (`reference_differential_grpc_receiver_driver_owned`); the live reference is the wire truth (`reference_wire_format_both_sides_see_same_bytes`); two per-side receivers + hard Close (`reference_periodic_sink_differential_two_receivers`); the delta-sink stability barrier (`reference_delta_sink_differential_stability_barrier`). The CONTROLLER runs the live differential + the three deliberate breaks ((a) absolute-not-delta MUST fail the barrier; (b) wrong-prefix-or-`|g`-for-counter; (c) barrier-masks-(a)) + the 20/20 flake-soak + the FULL-package `internal/statssink` `-race` ITSELF.
- **The full-package `-race`** (`reference_full_suite_race_after_background_mutator`): the synchronous `StatsdSink` adds NO background mutator, but the `Flusher` ticker remains one — run the `-race` gate over the FULL `internal/statssink` package, not a `-run` subset.

Anticipated counts at the IMPL six-gate (the PLAN's Global Constraints): stat **1200** (+0 — no self-stats, no sink cluster) / fixtures **94** (`0092-stats-sink-statsd`) / fuzzers **51** (`FuzzStatsdSinkConfigParse`) / BackendKind **38** (driver-owned UDP receiver) / DECISIONS **ADR-0265** (next-free ADR-0266); ZERO new packages, ZERO new go.mod modules (`go mod tidy -diff` EMPTY). **Row 48 FLIPS `done`** at this six-gate.

After the IMPL lands (squashed + pushed; ADR-0265 body + BEHAVIOR_CONTRACT + STATE/ROADMAP [row 48 `done`] all committed), ROLL THIS FILE FORWARD: CHECK the ROADMAP for any remaining CHARTERED Observability row — if a human has chartered the next deferred candidate (dog_statsd / graphite / OTLP-metrics sinks / the tap filter / tracing `custom_tags`/`spawn_upstream_span`/`http_service`/force-trace), roll to ITS BRAINSTORM; ELSE the TERMINATION SENTINEL applies (all chartered work complete — create the `stop` file and exit). Mirror a prior phase's post-IMPL router in git history (e.g. the 47.2b IMPL → row-47-done → re-open precedent).

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** The next stage is the phase-48 IMPL (subagent-driven in a fresh worktree; FLIPS row 48 `done`). Lifecycle-state 3 → done.
- **Subagent-driven execution** (`feedback_execution_style`) in a **fresh worktree** (`feedback_git_worktrees`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit, re-runs the full suite on the frozen HEAD, does the deliberate-break verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). Per-task gates: `gofmt -l` + `golangci-lint` + `go vet` + `go build ./...` (`feedback_pertask_gofmt_lint`).
- **Process anchors:** ADR-0106 (per-leg rows — row 48 flips `done` at this IMPL) · ADR-0044 (ADR §Decision+§Consequences land at the IMPL) · ADR-0045 (sub-split soft gate — escape-valve UNCONSUMED, re-confirmed at PLAN Task 1) · ADR-0080 (strict-reject) · ADR-0064 (`stats_sinks[]` silent-ignore, now a SECOND consumer) · ADR-0060 (no histograms → no `|ms` timers) · next-free **ADR-0265** (ANCHORED at this IMPL).

## Read first (in order)
1. THIS FILE — the cold-start router (phase 48 `stats-sink-statsd` PLAN LANDED; the next action is its IMPL).
2. `docs/envoy-go/phases/48-stats-sink-statsd/PLAN-48.md` — the 10-task TDD spine + the D-SD-* resolution block + the Global Constraints (counts) + the per-task code/tests/gates + the deliberate-break protocol.
3. `docs/envoy-go/phases/48-stats-sink-statsd/SPEC-48.md` — the frozen scope + the §11 D-SD-* live-probe pins (D-SD-DELTA → DELTA load-bearing) + the ADR-0265 §Context draft (§13).
4. `docs/envoy-go/STATE.md` — the active-phase header (`phase 48 (stats-sink-statsd) PLAN done`) + the recorded NEXT (`phase 48 IMPL`) + the counts (1200 / 93 / 50 / 38 / ADR-0264).
5. `docs/envoy-go/ROADMAP.md` — row 48 `in-progress` (flips `done` at this IMPL); the Observability family note STAYS OPEN.
6. `docs/envoy-go/phases/47-stats-sink-metrics-service/PLAN-47.2a.md` (+ the 47.2b IMPL router precedent in STATE.md) — the subagent-driven IMPL controller shape to mirror; the `deltaState` pattern landed at 47.2a (`internal/statssink/delta.go`).
7. `internal/statssink/{flusher,sink,mapping,delta}.go` + `internal/bootstrap/bootstrap.go` (`metricsServiceTypeURL` `:214` + `parseStatsSinks` `:415` + the `:430` single-URL gate the PLAN replaces + `StatsSinkConfig` `:274` + `StatsdSinkConfigs`-sibling `:348`) + `cmd/envoy-go/main.go` (`:189-202` the sink build; `:212-217` the drain defer; `:373-380` the post-Freeze `Start`) + `test/fixtures/0090-stats-sink-metrics-service-deltas/driver/driver.go` (the `0092` clone template) + `test/helpers/metricsservice/metricsservice.go` (the `statsdrecv` analog) + `test/differential/harness.go` (the `HostGatewayIP` home) — the EXACT seams the PLAN cites.
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_subagents_no_push`, `feedback_subagent_autocommit_claudemd`, `feedback_subagent_worktree_detach`, `feedback_subagent_worktree_path_targeting`, `feedback_pertask_gofmt_lint`, `reference_delta_sink_differential_stability_barrier`, `reference_periodic_sink_differential_two_receivers`, `reference_differential_grpc_receiver_driver_owned`, `reference_docker_probe_bridge_network`, `reference_differential_run_selector`, `reference_differential_break_protocol_count1`, `reference_full_suite_race_after_background_mutator`, `reference_differential_fullsuite_startup_flake`, `reference_strict_reject_sibling_typeurl_gap`, `reference_fuzzer_count_docs_drift`).
