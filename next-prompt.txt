START a fresh cold-start session for envoy-go — the **phase 26.3 SPEC** (`network-filter-rbac`) is DONE + spec-document-reviewer APPROVED + squash-merged + pushed; OPEN the **phase 26.3 PLAN** (`superpowers:writing-plans`), the bite-sized TDD decomposition of the 26.3 SPEC §10 task spine. 26.3 is the THIRD and FINAL sub-phase of the phase-26 §9 Network-filters family; its phase-done flips parent row 26 `in-progress → done` (the 18/19/22/24/25 ROLLUP).

Phase 26.3 SPEC landed at squash `ef22eb7` (pushed to origin/master): the per-sub-phase SPEC (16 §§ + 2 appendices) inherits the parent SPEC §5/§6/§7/§8/§11/§12/§13 rosters + refines the `rbac_network` surface against the AS-BUILT 26.1/26.2 framework + the phase-16 RBAC engine; ADR-0216 + ADR-0217 + ADR-0218 §Context drafted into DECISIONS.md (tail ADR-0215 → ADR-0218; next-free ADR-0219; §Decision/§Consequences bodies land at 26.3 IMPL per ADR-0044).

## What this session must do: AUTHOR the phase-26.3 PLAN

Per SKILL_ROUTING (sub-row 26.3 at state 2 — SPEC.md exists, PLAN.md does not → `superpowers:writing-plans`), author `docs/envoy-go/phases/26.3-network-filter-rbac/PLAN.md` from the 26.3 SPEC §10 task spine (~16-22 bite-sized TDD tasks), per the 22.1/25.1/26.1/26.2 per-sub-phase-PLAN precedent. RE-CHECK the ADR-0045 split-gate (anticipated NO split — net-new basis ~630-890 LoC / ~16-22 tasks fits, per SPEC §3.0 D-P1). Run the plan-document-reviewer loop to APPROVED. Work in a git worktree (`feedback_git_worktrees`; mirror `.worktrees/phase-26.3-network-filter-rbac-plan` off master). DO NOT start IMPL — this session authors the PLAN only.

## Phase 26.3 scope (from the 26.3 SPEC — the three deliverables)

1. **The shared `internal/rbac/` engine extraction** (ADR-0216) — MOVE + re-export the phase-16 engine (the abstract `EvalContext` interface [`internal/filter/http/rbac/evaluator.go:60-121`]; the `permissionEvaluator`/`principalEvaluator` interfaces + the ~23 evaluators; the rules-path `buildCompiledRulesEngine` [`rbac.go:373`] + the matcher-path `buildCompiledMatcherEngine` [`rbac.go:445`] + the `matcherCtxAdapter` bridge [`rbac.go:860-897`]; the per-policy `incPolicy`/`sync.Map` machinery [`rbac.go:165-222`]) into NEW `internal/rbac/` (~790 LoC moved; ~400 stays). ADD an input-capability `Profile` (`ProfileHTTP`/`ProfileL4`) on the builders so the L4 consumer PARSE-REJECTs the HTTP-only arms (`header`/`url_path`/`uri_template`) at compile WITHOUT changing consumer #1 (R4 byte-exact). Migrate HTTP rbac as consumer #1 (re-verified byte-exact by its phase-16 differential fixtures — R4, engine-correctness NOT metadata).
2. **The connection-scoped dynamic-metadata writes** (ADR-0217) — the FIRST production WRITE through the 26.1-shaped `ReadFilterCallbacks.DynamicMetadata()` (the rbac shadow pair `shadow_engine_result`/`shadow_effective_policy_id` under namespace `envoy.filters.network.rbac`); `internal/dynamicmetadata/` REUSED at connection scope (NO new package — the `chainRuntime` already owns the per-connection bucket), doc generalized to scope-agnostic in place (ADR-0044). NO in-repo reader (observable via the `shadow_*` stats + unit tests).
3. **The `rbac_network` filter** (ADR-0218) — `internal/filter/network/rbac/` consumer #2: parse (`stat_prefix` PGV-required; `delay_deny` PARSE-REJECT; `ProfileL4` HTTP-only-arm reject; CEL silent-ignore inherited); the L4 `EvalContext` from `network.Connection` (`LocalAddr`/`RemoteAddr`/`RequestedServerName`/`DownstreamPrincipals`); decision-in-`OnData` (AMEND-A8; **CRITICAL** — `OnNewConnection` MUST return `Continue`, never `StopIteration`, per memory `reference_network_read_filter_onnewconnection_halts` — a halt there sets the sticky `connHalted` flag blocking all `OnData`); allow → `Continue` → terminal (the FIRST production mixed read→terminal chain via the 26.2 `prefixConn` handover — R-M-LIVE); enforced-deny → `SetResponseCodeDetails("rbac_deny_close")` + `Close(NoFlush)` (the F3 NoFlush framework touchpoint — un-collapse the 26.1/26.2 `FlushWrite≡NoFlush` per `chain.go:362-368`); shadow → metadata + `shadow_*` stats; the four `<stat_prefix>.rbac.{allowed,denied,shadow_allowed,shadow_denied}` static counters (132 → 136); NO per-policy counters (no `track_per_rule_stats` in the network proto — F2); the `rules`/`matcher` dual-path; register the 5th built-in via `builtins.RegisterBuiltins`. TypeURL via `proto.MessageName` (memory `reference_network_filter_typeurl_extensions` — the `extensions.` form). The 36th fuzzer `FuzzNetworkRBACConfigParse` + the +2 differential fixtures (cross-side allow/deny/shadow with `StatsAsserter`; boot-reject — memory `reference_differential_asserter_dispatch` + `reference_differential_fixture_dispatch_constraint`; differential bootstraps need a cluster).

## Project state at master tip

- **Master tip:** `c4e5f20` (a next-prompt SHA-fill repoint; trails the substantive 26.3 SPEC squash `ef22eb7` by +1). Confirm the live tip via `git log --oneline -1` — any further next-prompt repoint advances the tip +1 (expected, not chased). origin/master in sync after push. STATE.md `last-commit` points at the substantive SPEC squash `ef22eb7` (not the repoint).
- **§9 Network-filters family:** parent row 26 `in-progress`; 26.1 `done`; 26.2 `done`; 26.3 `in-progress` (flipped `planned → in-progress` at the 26.3 SPEC commit per ADR-0106). Parent row 26 flips `in-progress → done` ATOMICALLY with 26.3 at 26.3 phase-done (ROLLUP 18/19/22/24/25).
- **Counts at master tip:** stat surface **132** (→136 at 26.3 IMPL); differential fixtures **44** tail **0042** (→46); fuzzers **35** (→36); conformance 10/10; h2spec 53/53; DECISIONS.md tail **ADR-0218** (§Context for 0216/0217/0218 drafted; §Decision/§Consequences bodies land at 26.3 IMPL); next-free **ADR-0219**.
- **Toolchain:** Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); reference Envoy v1.37.2 (ADR-0008); go-control-plane v1.32.4 (ADR-0008).

## Read first (in order)

1. THIS FILE — the phase-26.3 PLAN cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 26.3 SPEC done; awaiting 26.3 PLAN`; next-skill the 26.3 PLAN authoring; next-free `ADR-0219`; last-commit the SPEC squash.
3. `docs/envoy-go/phases/26.3-network-filter-rbac/SPEC.md` — the SPEC this PLAN decomposes (§10 task spine = the anticipated 15-task spine; §3 engine extraction; §4 rbac_network + sink; §6 PARSE-REJECT; §7 stats; §8 fixtures; §11.1 D-S2 as-built surface; §12 D-26.3-1..7; §13 RATIFIED-PENDING; §15 acceptance checklist).
4. `docs/envoy-go/phases/26-network-filter-chain-and-rbac/SPEC.md` (parent — the inherited §5/§6/§7/§8/§11/§12/§13 rosters + AMEND-A1..A11) + `docs/envoy-go/phases/26.2-network-filter-registry-migration/PLAN.md` (the per-sub-phase-PLAN precedent shape).
5. The CONSUMED surface: `internal/filter/network/{types,terminal,chain,prefixconn,registry,callbacks}.go` + `internal/filter/network/builtins/builtins.go` (the 5th-built-in seam + `Deps.StatsRegistry`); `internal/filter/http/rbac/{evaluator,rbac}.go` (the engine to EXTRACT — the `evalContext`@60-121, L4 stubs@965-988, `incPolicy`@165-222, matcher@445/824, CEL silent-ignore@419-424); `internal/dynamicmetadata/` (the connection-scoped reuse); `internal/listener/manager.go` `serveNetworkChain` (the mixed read→terminal dispatch).
6. `docs/envoy-go/DECISIONS.md` ADR-0216/0217/0218 (the §Context this PLAN's IMPL fills) + ADR-0213/0214/0215 (the framework the filter builds on) + ADR-0040 (silent-ignore — CEL) + ADR-0045 (split-gate — re-check) + ADR-0044 (in-place ADR-body discipline) + ADR-0106 (ROADMAP flip); `docs/envoy-go/ROADMAP.md` rows 26/26.3; `BOOTSTRAP_PROMPT.md` §5; `docs/envoy-go/SKILL_ROUTING.md`.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — execution-style + worktree + push + subagents-no-push + network-filter-typeurl-extensions + network-read-filter-onnewconnection-halts (the `rbac_network` Continue-from-OnNewConnection constraint) + differential-asserter-dispatch + fixture-dispatch-constraint all apply.

After the 26.3 PLAN is APPROVED + squash-merged + pushed, advance STATE.md per BOOTSTRAP §5 (sub-row 26.3 STAYS `in-progress`; lifecycle-state → PLAN-done/awaiting-IMPL) and rewrite next-prompt.txt for the 26.3 IMPL cold-start (`superpowers:subagent-driven-development` over the 26.3 PLAN's TDD tasks; the §9-family ROLLUP lands at 26.3 IMPL phase-done).
