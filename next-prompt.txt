=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage. To use it to advance the project: read `docs/envoy-go/STATE.md` for the recorded NEXT, perform that one stage (the concrete action is spelled out below), land it, and re-roll this file to the following stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, the active phase is fully landed, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action. While ANY stage remains — as NOW: the **47.2 (deltas + tags-as-labels) lifecycle is pending** (its full BRAINSTORM→SPEC→PLAN→IMPL, the FINAL leg that flips row 47 `done`), so tasks are NOT complete — do NOT create `stop`; perform the stage described below.
==============================================

RESUME a fresh session for envoy-go (router last updated 2026-06-29; the **phase 47.1 (stats-sink metrics_service) IMPL is DONE and LANDED** — squash `77f776bd`, fast-forwarded onto master + pushed to origin; six-gate GREEN incl. the full 91-dir differential `ok 278.548s` + full-package `internal/statssink` `-race` + the `0089` four deliberate breaks all LIVE subject-side + 20/20 flake-soak; **ANCHORS ADR-0262**). Phase 47 is the **FOURTH Observability-family row** (`stats-sink-metrics-service`); the family STAYS OPEN. **ROW 47 STAYS `in-progress`** (flips `done` at the FINAL leg 47.2 IMPL per ADR-0106 + `reference_roadmap_split_phase_row_done`). The next action is the **47.2 (deltas + tags-as-labels) BRAINSTORM** — open the SECOND-and-FINAL leg of row 47.

HEAD is the phase-47.1 IMPL squash commit `77f776bd` atop the phase-47.1 PLAN commit `2d06351f`. **Re-read the shas with `git log --oneline -6`, do NOT assume.** Confirm `git status` clean (the untracked `.claude/` is session cruft, leave it) + master == origin/master.

## What 47.1 LANDED (the substrate the 47.2 brainstorm builds on)
The 47.1 (core) leg shipped the cumulative/no-labels `metrics_service` stats sink: the NEW `internal/statssink` package (`mapping.go` `snapshot` — the `Counter`→`COUNTER` absolute / `Gauge`→`GAUGE` type-switch map, full dotted name, ZERO labels, flush-time `timestamp_ms`, no help; `sink.go` `MetricsServiceSink` — the ALS `GrpcAccessLogSink` client-streaming template SIMPLIFIED to a 1-deep handoff [bounded chan cap 8 + writer goroutine + identifier-once + reconnect-resend + drop-newest + idempotent `Close`; a `metricsClient` seam]; `flusher.go` `Flusher` — the `stats_flush_interval` ticker walking the frozen `Registry` post-`Freeze`) + the `MetricsServiceClient` CLIENT-streaming typed wrapper (`internal/grpcclient`) + the `stats_sinks[]`/`stats_flush_interval` parse arm + the strict-reject arms (`internal/bootstrap` — `StatsSinkConfig`/`FlushInterval` HERE; accept {AUTO,V3}) + `FuzzStatsSinkConfigParse` + the post-`Freeze` per-`StatsSinkConfig` sink build + `Flusher.Start` in `cmd/envoy-go/main.go` (REFERENCE-PARITY `log.Fatalf` on a missing/non-H2 cluster) + the driver-owned `test/helpers/metricsservice` h2c receiver + the `0089-stats-sink-metrics-service` cross-side EXACT differential (over TWO driver-owned per-side receivers — the periodic flush means the reference streams the whole test, so NOT the single shared receiver of the event-driven OTLP/Zipkin trace fixtures; the receiver hard-stops via `Close()` NOT `GracefulStop`). The knob VALUES `report_counters_as_deltas:true` / `emit_tags_as_labels:true` / non-default `histogram_emit_mode` + `stats_flush_on_admin` + the sibling sink TypeURLs are all STRICT-REJECTED at 47.1 (ADR-0080).

## What 47.2 IS (the deferred leg — chartered by THIS brainstorm)
**47.2 = deltas + tags-as-labels** (anticipated **ADR-0263**), per the §Q-split (ADR-0045) recorded at the phase-47 BRAINSTORM + the ADR-0262 §Consequences deferral set:
- **`report_counters_as_deltas=true`** — per-counter LAST-FLUSH delta state (each flush emits `current - last_flushed`, not the absolute value). The new state is a per-sink (or per-counter) last-value map kept across flushes; lift the 47.1 STRICT-REJECT of `report_counters_as_deltas:true`.
- **`emit_tags_as_labels=true`** — SN-rule tag→`LabelPair` extraction (the dotted name's tag SEGMENTS become `MetricFamily.metric[].label[]` pairs instead of being inlined into the name). Reuse the phase-06.1 stat-name tag-extraction rules (the `internal/stats/name.go` SN-rules); lift the 47.1 STRICT-REJECT of `emit_tags_as_labels:true`.
- Re-probe the reference (contrib-v1.37.2) per `reference_docker_probe_bridge_network` to PIN the delta semantics (reset-on-flush? monotonic-delta? first-flush behavior?) + the tags-as-labels name/label split (which segments become labels; how the family NAME changes). The `0089` shape is the differential template (a SECOND fixture `0090-…` OR an extended `0089` arm — decide at the brainstorm; note `reference_differential_fixture_dispatch_constraint`).
- The brainstorm should also settle: is 47.2 ONE leg or a further split (deltas vs tags-as-labels are independent knobs — an ADR-0045 sub-split candidate)? Does the delta state interact with the +0-self-stats posture (D-MS-STATS-FINAL)? Does tags-as-labels need a `histogram_emit_mode` re-look (still moot — ADR-0060)?

## The NEXT action — the 47.2 BRAINSTORM (lifecycle-state 0 → 1 for the 47.2 leg)
Run `superpowers:brainstorming` for the 47.2 leg. Produce `docs/envoy-go/phases/47-stats-sink-metrics-service/BRAINSTORM-47.2.md` (the 12-section phase-44/45/46 family-row brainstorm shape; the §Q-decisions interactive with the human — esp. the deltas-vs-tags-as-labels split question + the delta-state design + the differential shape). **DOCS-ONLY direct-on-master** (the phase-44/45/46 + 47.1 BRAINSTORM/SPEC/PLAN precedent — brainstorm is docs-only; NO worktree unless code-adjacent probe artifacts land). Anchor the anticipated **ADR-0263** §Context as a DRAFT (the body lands at the 47.2 IMPL per ADR-0044). Verify all load-bearing claims (the `stats.Registry`/`name.go` SN-rule citations, the metrics_service proto field numbers, the 47.1 as-built seams) against disk before writing — the BRAINSTORM-reviewer (general-purpose) discipline. **ROW 47 STAYS `in-progress`** (47.2 is the FINAL leg — flips it `done` only at the 47.2 IMPL). After it lands, ROLL THIS FILE to the **47.2 SPEC**.

## Counts at the 47.1 IMPL (post-landing — re-verify, do NOT assume)
stat surface **1200** (non-H2 **1196**; D-MS-STATS-FINAL +0) / fixtures **91** (tail `0089-stats-sink-metrics-service`; `ls -d test/fixtures/*/ | wc -l`) / fuzzers **50** (`grep -rh '^func Fuzz' --include='*.go' . | wc -l`) / BackendKind **38** (`H2GoawayResponder = 38`) / DECISIONS **ADR-0262** (next-free **ADR-0263**); `github.com/prometheus/client_model v0.6.1` NOW in go.mod (direct); `go-control-plane/envoy v1.32.4`; `internal/statssink` is the new package. The 47.2 BRAINSTORM is docs-only ⇒ counts UNCHANGED at this stage.

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** The next stage is the 47.2 BRAINSTORM (lifecycle-state 0 → 1 for the 47.2 leg). After it lands, ROLL THIS FILE to the 47.2 SPEC.
- **Brainstorm = docs-only direct-on-master** (the family-row precedent); a fresh worktree (`feedback_git_worktrees` · `superpowers:using-git-worktrees`) ONLY if code-adjacent probe artifacts land. The SPEC/PLAN are docs-only too; the IMPL is subagent-driven (`feedback_execution_style` · `superpowers:subagent-driven-development`) in a worktree, subagents commit LOCALLY only (`feedback_subagents_no_push`), the CONTROLLER squashes + pushes at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`).
- **Process anchors:** ADR-0106 (per-leg rows — row 47 flips `done` at the 47.2 IMPL) · ADR-0045 (sub-split soft gate — re-decide deltas-vs-tags-as-labels at the brainstorm) · ADR-0044 (ADR §Decision+§Consequences-at-IMPL — ADR-0263 §Context DRAFT at the brainstorm) · ADR-0080 (strict-reject — 47.2 LIFTS the deltas/tags-as-labels rejects) · ADR-0059 (the in-tree `Registry`/`Walk`/`Freeze`) · ADR-0060 (histograms deferred — still moot) · ADR-0064 (the `stats_sinks[]` silent-ignore — 47.1 LIFTED the metrics_service sink) · ADR-0158 (the `grpcclient.Dialer` typed-wrapper) · next-free **ADR-0263** (ANCHORED at the 47.2 IMPL).
- **Differential discipline** (for the eventual 47.2 fixture): the TWO-per-side-receiver pattern (the periodic-flush correction — NOT the single shared receiver of the event-driven trace fixtures) + the hard `Close()` teardown; `-run 'TestDifferential/00NN'` NEVER bare (`reference_differential_run_selector`); `-count=1` on every break + `-race` (`reference_differential_break_protocol_count1`); the Docker bridge + decode-ran proof (`reference_docker_probe_bridge_network`); the gRPC metrics receiver is driver-owned NOT a BackendKind (`reference_differential_grpc_receiver_driver_owned`); assert the aggregated subset by dotted name NOT the whole set NOR framing (`reference_streaming_sink_differential_framing`); the live reference is the wire truth (`reference_wire_format_both_sides_see_same_bytes`); the Flusher+sink writer goroutine are background mutators ⇒ FULL-package `-race` (`reference_full_suite_race_after_background_mutator`).

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-47.1 IMPL is DONE + LANDED; row 47 `in-progress`; the next action is the 47.2 BRAINSTORM — deltas + tags-as-labels).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 47.1 IMPL done`) + the recorded NEXT (the 47.2 BRAINSTORM) + the counts (1200 / 91 / 50 / 38 / ADR-0262).
3. `docs/envoy-go/DECISIONS.md` — **ADR-0262** (the 47.1 anchor — its §Decision/§Consequences incl. the deferred 47.2 set: deltas / tags-as-labels / histogram_emit_mode / stats_flush_on_admin / sibling sinks).
4. `docs/envoy-go/phases/47-stats-sink-metrics-service/SPEC-47.1.md` + `PLAN-47.1.md` + `PROGRESS-47.1.md` (the 47.1 charter + the as-built — the substrate + the §Q-split recording 47.2; the §11 D-MS-* live pins).
5. `docs/envoy-go/ROADMAP.md` — row 47 (`stats-sink-metrics-service`, `in-progress`) + the Observability family-progress note + the recorded 47.1 IMPL annotation.
6. The phase-47 BRAINSTORM (`docs/envoy-go/phases/47-stats-sink-metrics-service/BRAINSTORM.md`) — the §Q-split chartering 47.1/47.2 + the anticipated ADR-0263.
7. The 47.1 as-built code: `internal/statssink/{mapping,sink,flusher}.go`, `internal/grpcclient/grpcclient.go` (`MetricsServiceClient`), `internal/bootstrap/bootstrap.go` (`parseStatsSinks`/`parseMetricsServiceConfig` + the strict-reject arms 47.2 will lift), `internal/stats/name.go` (the SN-rule tag-extraction 47.2's tags-as-labels reuses), `cmd/envoy-go/main.go` (the boot wiring), `test/helpers/metricsservice`, `test/fixtures/0089-stats-sink-metrics-service`.
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_subagents_no_push`, `feedback_subagent_autocommit_claudemd`, `feedback_pertask_gofmt_lint`, `feedback_subagent_worktree_detach`/`_path_targeting`, `reference_roadmap_split_phase_row_done`, `reference_stats_sink_metrics_service_probe`, `reference_differential_grpc_receiver_driver_owned`, `reference_streaming_sink_differential_framing`, `reference_strict_reject_sibling_typeurl_gap`, `reference_docker_probe_bridge_network`, `reference_full_suite_race_after_background_mutator`, plus the differential-discipline references above).
