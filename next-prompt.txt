START a fresh cold-start session for envoy-go — **phase 32.1 (`network-filter-redis-upstream-pool-seam-and-codec`) SPEC is DONE; the next lifecycle step is the 32.1 PLAN** (bite-sized TDD tasks). The 32.1 SUB-PHASE SPEC settled the upstream connection-pool / cluster-routing seam's exact exported surface + multiplexing model (D-P32-3), the RESP partial-frame reassembly model (D-P32-4), the EAGER fixed-roster posture + the 32.1 10-name subset (D-P32-1), the boot-reject fixture arms (D-P32-5), the 32.1 local-reply subset PING+AUTH (D-P32-6), and ANCHORED the ADR-0230 §Context. The next session authors the **32.1 PLAN** (`superpowers:writing-plans` scoped to the sub-phase PLAN — per `feedback_execution_style`; work in a git worktree per `feedback_git_worktrees`), turning the SPEC §14 ~16-task spine into bite-sized TDD tasks. The 32.1 IMPL (subagent-driven per `feedback_execution_style`) then fills the ADR-0230 §Decision/§Consequences body + the ADR-0229 §Decision/§Consequences (32.1 half) in place.

The phase-32.1 SPEC landed at the phase-32.1 SPEC squash on master (squash-merged from `phase-32.1-network-filter-redis-upstream-pool-seam-and-codec-spec` + pushed to origin/master by the controller; the NEW `docs/envoy-go/phases/32.1-network-filter-redis-upstream-pool-seam-and-codec/SPEC.md` + the ADR-0230 §Context in DECISIONS.md + ROADMAP sub-row-32.1 `planned → in-progress` + STATE/next-prompt advance; spec-document-reviewer APPROVED). It is the substantive master tip; the docs-only SHA-fill follow-up trails it — confirm via `git log --oneline -1`. Substantive predecessor on master: the phase-32 PARENT SPEC squash `0f7888d`; earlier the phase-32 BRAINSTORM squash `f473c4e`; earlier the phase-31 IMPL squash `928287a` (phase 31 CLOSED). origin/master in sync after push.

## What this session does — the phase-32.1 PLAN

Author the 32.1 PLAN at `docs/envoy-go/phases/32.1-network-filter-redis-upstream-pool-seam-and-codec/PLAN.md` — bite-sized TDD tasks (the `superpowers:writing-plans` task structure: write-failing-test → run-fail → minimal-impl → run-pass → commit) from the SPEC §14 ~16-task spine. Per the 29.1-PLAN precedent (`docs/envoy-go/phases/29.1-network-filter-mongo-wire-and-requests/PLAN.md` — read it as the per-sub-phase-PLAN lifecycle model):

- **Task 1 = the first-action baselines/anchors gate** (re-pin against the live IMPL-session tip): fixtures **56** (tail `0054`) + fuzzers **40** + stat surface **536** + BackendKind tail **31** + DECISIONS tail **ADR-0230** (next-free **ADR-0231**) + the `proto.MessageName(&redis_proxyv3.RedisProxy{})` TypeURL pinning test + a clean `go mod tidy` (ZERO new go.mod module — redis_proxy is CORE `/envoy v1.32.4`, AMEND-R1/R8) + the SPEC §11.1 as-built anchors.
- Then the spine: the `redisproxy` package skeleton + TypeURL + `NewFactory(cm, reg)`; the config parse (the PGV-required `stat_prefix`/`settings`/`settings.op_timeout`/`catch_all_route.cluster` arms + the runtime no-upstream reject + the unknown-catch_all-cluster TOLERANCE + the `IsValidName` guard + the byte-stable `redis_proxy:` reject constants); the **RESP codec** (`resp.go` — request decode + reply decode + encode; value types + null sentinels + inline; the STREAMING-READER partial-frame model — the terminal owns the conn, NO private buffer, D-P32-4); the **upstream-pool seam** (`internal/filter/network/upstreampool.go` — `UpstreamDialFunc` + `UpstreamConn` + lazy dial + single-flight `Send`/`Reader` + `Close`; one-conn-per-downstream; NO `internal/cluster` import in `internal/filter/network`; D-P32-3 + the ADR-0230 §Context); the PING/AUTH local-reply set (`commands.go`; D-P32-6); the 10-name eager roster (`stats.go`; D-P32-1); the `TerminalFilter.Handle` pump (`filter.go`); the 10th built-in + the `redis_proxy/v3` blank-import; the `TCPRedisResponder` BackendKind (value 32); `0055-redis-roundtrip` (cross-side; flat-`/stats` StatsAsserter + the downstream-RESP byte-equivalence prong — the §9 FIRST; PING + proxied SET/GET arms) + `0056-redis-boot-reject` (the `stat_prefix` arm); the completion bundle (ADR-0230 §Decision/§Consequences body + the BEHAVIOR_CONTRACT 32.1 bundle + STATE/ROADMAP + the six-gate).
- The 32.1 PLAN **re-checks the ADR-0045 split-gate** (SPEC §14.1: ~940-1420 prod LoC / ~16 tasks → fits; the **32.1a/32.1b** escape-valve split is pre-authorized — 32.1a = the package + seam + codec + builtins/bootstrap [a bootable, unit-proven redis_proxy]; 32.1b = the `TCPRedisResponder` + `0055` + `0056` + the completion bundle).

The 32.1 PLAN runs NO new docker probes (the parent §11 D32-1..D32-8 block + the 32.1 SPEC are the authoritative records). It MAY re-read the as-built source corpus if a task-decomposition question needs it.

## Honor throughout

- `feedback_execution_style` (subagent-driven IMPL when it gets there) · `feedback_git_worktrees` · `feedback_subagents_no_push` (subagents commit local-only) · `feedback_push_to_origin` (the controller squash-merges + pushes at stage-close) · `feedback_pertask_gofmt_lint`.
- ADR-0230 (the upstream-pool seam — the §Context just anchored) · ADR-0229 (the redis_proxy filter umbrella) · ADR-0044 (§Context-at-SPEC, §Decision+§Consequences-at-IMPL — both bodies land at the 32.1 IMPL) · ADR-0079 (two-step factory) · ADR-0080 (byte-stable rejects) · ADR-0052 (atomic landing + six-gate) · ADR-0045 (split-gate) · ADR-0106(d) · ADR-0219 (the routing precedent) · ADR-0223 (the per-conn mutex — for the DEFERRED deep-pending-queue model, NOT the single-flight MVP) · ADR-0060 (histograms deferred) · ADR-0227 (the contrib pin).
- Project memories: `reference_network_filter_typeurl_extensions`, `reference_wire_format_both_sides_see_same_bytes` (EXTENDS to the downstream RESPONSE bytes redis_proxy generates), `reference_close_direction_framework_gap` (does NOT bite — AMEND-R9), `reference_dynamic_stat_name_charset_guard` (the per-command roster is 32.2; the `IsValidName(stat_prefix)` guard is the only 32.1 use), `reference_differential_asserter_dispatch`, `reference_differential_fixture_dispatch_constraint`, `reference_differential_break_protocol_count1`, `reference_differential_reference_parses_full_message`.

## Read first (in order)

1. THIS FILE — the phase-32.1-PLAN cold-start.
2. `docs/envoy-go/phases/32.1-network-filter-redis-upstream-pool-seam-and-codec/SPEC.md` — **THIS SUB-PHASE SPEC** (the package §3, the codec §3.5 [D-P32-4], the seam §4 [D-P32-3 + the ADR-0230 §Context blueprint], the stats §7, the fixtures §8, the ADR anchor map §10, the as-built anchors §11.1, the D-questions §12 [the 32.1-additive D-S32.1-1..6 for PLAN/IMPL], the task spine §14 + the split-gate re-check §14.1, the test surface §16).
3. `docs/envoy-go/phases/32-network-filter-redis-proxy/SPEC.md` — **THE PARENT SPEC** (the inherited §5 proto / §6 reject / §7 stat / §8 fixture rosters + the §11 D32-1..D32-8 empirical-pin block [AMEND-R1..R9]).
4. `docs/envoy-go/STATE.md` — active-phase `phase 32.1 SPEC done`; lifecycle-state → the 32.1 PLAN; next-skill `superpowers:writing-plans`; counts (536 stats / 56 fixtures / 40 fuzzers / BackendKind tail 31 / DECISIONS tail ADR-0230, next-free ADR-0231).
5. `docs/envoy-go/ROADMAP.md` — parent row 32 + sub-row 32.1 (`in-progress`, SPEC-DONE note) + sub-row 32.2 (`planned`).
6. `docs/envoy-go/DECISIONS.md` — **ADR-0230** (the §Context just anchored — the upstream-pool seam) + ADR-0229 (the filter umbrella) + ADR-0044 / ADR-0079 / ADR-0080 / ADR-0052 / ADR-0045 / ADR-0219 / ADR-0223 / ADR-0060 / ADR-0227.
7. `docs/envoy-go/phases/29.1-network-filter-mongo-wire-and-requests/PLAN.md` — the per-sub-phase-PLAN lifecycle precedent (the bite-sized-TDD task shape + the Task-1 baselines gate + the completion-bundle task).
8. `internal/filter/network/terminal.go` + `upstreamcluster.go` (the `TerminalFilter` + the override-seam decoupling precedent) + `internal/filter/tcpproxy/filter.go` (the terminal dial + the cluster-resolve-and-REJECT contrast for the unknown-cluster tolerance) + `internal/cluster/manager.go` + `cluster.go` (`Manager.Get` + `Cluster.Dial` + `IncUpstreamRqTotal` — the dial path + the upstream stat reuse) + `internal/filter/network/kafkabroker/` + `mongoproxy/` (the §9 package-shape precedents) + `internal/filter/network/builtins/builtins.go` + `internal/bootstrap/bootstrap.go` (the 10th-built-in + blank-import sites) + `test/differential/fixture/fixture.go` (the `StatsAsserter` in-band scrape + the BackendKind roster, next-free 32).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`.

## Counts at master tip (phase 32.1 SPEC — UNCHANGED until the 32.1 IMPL)

- stat surface **536** (→ 546 at the 32.1 IMPL: +10 fixed); differential fixtures **56** (tail `0054-kafka-boot-reject`; → 58 at the 32.1 IMPL); fuzzers **40** (`FuzzKafkaDecode`; `FuzzRESPDecode` is 32.2); BackendKind tail **31** (`TCPKafkaResponder`; → 32 `TCPRedisResponder` at the 32.1 IMPL); DECISIONS.md tail **ADR-0230** (the upstream-pool seam §Context anchored at THIS 32.1 SPEC; next-free **ADR-0231**).
- conformance h2spec 53/53 + proxy-wasm 10/10 (image-independent).
- reference pin: `envoyproxy/envoy:contrib-v1.37.2` @ `sha256:7edd5b0fd763d32c3dfcfd0061f9c2ea63eebd8cdf7f88d974d3adfc99453be8` (ADR-0227).
- Toolchain: Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); go-control-plane `/envoy` v1.32.4 (ADR-0008) + `/contrib` v1.32.4 (the phase-31 dep). redis_proxy is CORE `/envoy` (D32-1 CONFIRMED) → NO new dep.
- §9 Network-filters family: rows 26/27/28/29/30/31 DONE + **row 32 `redis_proxy` IN-PROGRESS (parent SPEC + 32.1 SPEC done; 32.1 sub-row `in-progress`, 32.2 `planned`)**. After phase 32 the candidate {thrift} remains (the last §9 candidate — reuses the 32.1 upstream-pool seam).
