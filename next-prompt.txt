START a fresh cold-start session for envoy-go — **phase 27 (`network-filter-sni-cluster`) BRAINSTORM is DONE + committed + pushed**, and the phase is OPEN/`in-progress`. There is an active phase. This session does the **27 SPEC** — per SKILL_ROUTING state 1 → 2 (`SPEC.md` does not exist, `BRAINSTORM.md` does) the next lifecycle step is `superpowers:writing-plans` scoped to **SPEC authoring** (NOT a fresh brainstorm — the brainstorm is done; the design is settled). Author `docs/envoy-go/phases/27-network-filter-sni-cluster/SPEC.md` from the BRAINSTORM, executing the §10 D27-1..D27-8 empirical-pin obligations IN-SESSION against reference Envoy v1.37.2 per ADR-0004, and anchoring the ADR-0219 + ADR-0220 §Context drafts into DECISIONS.md (§Decision/§Consequences bodies land at 27 IMPL per ADR-0044).

Phase 27 was brainstormed via a 3-question dialogue (operator selected `sni_cluster` from the 6 remaining §9 Network-filters candidates). The settled design: `sni_cluster` (`envoy.filters.network.sni_cluster`, type `…filters.network.sni_cluster.v3.SniCluster` — an EMPTY proto) is a **config-less read-filter** that in `OnNewConnection` reads the TLS SNI (`Connection.RequestedServerName()`) and publishes it **VERBATIM as a per-connection upstream-cluster-override** (then `Continue` — no sticky halt per `reference_network_read_filter_onnewconnection_halts`); the terminal `tcp_proxy` consumes the override to replace its configured cluster (empty/absent SNI → configured fallback; SNI naming an unknown cluster → connection close). The 3 Q-decisions: **Q1** full upstream parity as ONE flat phase (no pre-split; ADR-0045 gate LOW, ~180–400 net-new LoC / ~10–16 tasks); **Q2** a **NARROW typed per-connection cluster-override** carried on the `chainRuntime`, keyed by the upstream-canonical `envoy.tcp_proxy.cluster`, with a `ReadFilterCallbacks` writer accessor + a `TerminalFilter` reader-threading — NO general connection-scoped filter-state primitive (extract-at-second-consumer/YAGNI; API-revision allowance), NO dynamic-metadata reuse (Envoy uses filter-state=control, not metadata=observability, for this); **Q3** all THREE behavior arms cross-side byte-exact proven (route / fallback / unknown-cluster-close).

## What this session must do

There IS an active phase (27, `in-progress`, BRAINSTORM done). Per SKILL_ROUTING state 1 → 2 the next step is the **SPEC** via `superpowers:writing-plans` scoped to SPEC authoring (per the single-phase precedent — 23/26.x SPEC sessions). Read the BRAINSTORM first; then author the SPEC executing the §10 D27-1..D27-8 empirical pins IN-SESSION against Envoy v1.37.2 (parallel-subagent fan-out per the prior SPEC precedent). After SPEC → PLAN → IMPL per the established per-phase lifecycle (`feedback_execution_style`: subagent-driven execution once a PLAN exists; subagents commit LOCAL-ONLY [`feedback_subagents_no_push`]; the controller squash-merges + pushes at stage-close [`feedback_push_to_origin`]). Work in a git worktree (`feedback_git_worktrees`). The SPEC stage closes with the `spec-document-reviewer` loop (per the brainstorming/SPEC discipline) + STATE advance + commit + push.

## The §10 D27-1..D27-8 SPEC empirical-pin obligations (from the BRAINSTORM)

- **D27-1** — confirm `…sni_cluster.v3.SniCluster` is an EMPTY message + its exact `@type` URL via `proto.MessageName` (NOT the SPEC string — network-filter type URLs carry the `extensions.` segment per `reference_network_filter_typeurl_extensions`); blank-import the proto; the differential bootstrap needs a cluster.
- **D27-2** — Envoy's exact SNI→cluster mechanism: confirm `sni_cluster` sets the connection-lifespan filter-state key `envoy.tcp_proxy.cluster` (`PerConnectionCluster`) with the SNI VERBATIM as the cluster name, read by `tcp_proxy` at upstream-connection time to override its configured cluster.
- **D27-3** *(seam-shaping)* — the writer-API name on `ReadFilterCallbacks` (e.g. `SetUpstreamCluster(name string)`) + the terminal-reader threading mechanism (chainRuntime-owned value read at terminal dispatch vs `context.Value` vs a terminal-side callbacks accessor).
- **D27-4** — `tcp_proxy` behavior when the override names an unknown cluster: confirm Envoy closes the downstream connection (vs falling back) + whether any stat (`downstream_cx_no_route`/`cluster_not_found` or similar) is touched.
- **D27-5** — `tcp_proxy` precedence when override == configured cluster, and empty/absent-SNI → fallback semantics.
- **D27-6** — confirm `sni_cluster` acts in `onNewConnection` + returns `Continue` (no halt) in upstream.
- **D27-7** — confirm network filters carry no per-route / `typed_per_filter_config` surface.
- **D27-8** — the task/LoC envelope (SPEC §3.0) confirming the single-phase ADR-0045 fit; the fuzzer decision (working hypothesis: a config-less-parse `FuzzSniClusterConfigParse` is low-value → DEFER; fuzzers stay 36 unless added).

## Project state at master tip

- **Master tip:** `38fe573` (the next-prompt master-tip repoint), which trails the substantive phase-27 BRAINSTORM squash `d13bd9c` by +2 (via the `f76f78b` `last-commit`-fill + 27-SPEC cold-start rewrite). Confirm the live tip via `git log --oneline -1`; any further next-prompt-only repoint advances the tip +1 (expected, not chased; per the 26.3 `72dc36d`←`7d1b7c6`←`ec66a87` precedent). STATE.md `last-commit` points at the substantive BRAINSTORM squash `d13bd9c`. origin/master in sync after push.
- **§9 Network-filters family:** parent row 26 DONE; row 27 (`network-filter-sni-cluster`) `in-progress` (BRAINSTORM done). The family stays OPEN — after phase-27 phase-done, 5 candidates remain (`redis`/`mongo`/`kafka_broker`/`thrift`/`zookeeper`).
- **Counts at master tip (unchanged by BRAINSTORM):** stat surface **136**; differential fixtures **46** (tail `0044`); fuzzers **36**; conformance 10/10; h2spec 53/53; DECISIONS.md tail **ADR-0218**; next-free **ADR-0219** (ADR-0219 [override seam] + ADR-0220 [sni_cluster filter] ANTICIPATED; ADR-0209 escape-valve reserve STANDS-UNCONSUMED).
- **Anticipated phase-27 deltas at IMPL:** fixtures 46 → 47 (one cross-side multi-cluster TLS dir; NO boot-reject — config-less); stat surface 136 (+0); fuzzers 36 (→37 only if SPEC adds the low-value config-less-parse fuzzer); ADRs +2 (ADR-0219 + ADR-0220).
- **Toolchain:** Go 1.26.2; golangci-lint 1.64.8 (ADR-0009); reference Envoy v1.37.2 (ADR-0008); go-control-plane v1.32.4 (ADR-0008).

## Read first (in order)

1. THIS FILE — the 27-SPEC cold-start.
2. `docs/envoy-go/phases/27-network-filter-sni-cluster/BRAINSTORM.md` — the settled design (3 Q-decisions + §10 D27-1..D27-8 SPEC pins + §7 anticipated ADRs).
3. `docs/envoy-go/STATE.md` — active-phase `phase 27 BRAINSTORM done`; next-skill `superpowers:writing-plans` (scoped to 27 SPEC); next-free `ADR-0219`; last-commit `d13bd9c`.
4. `docs/envoy-go/ROADMAP.md` — row 27 `in-progress`; the §9 family-open note + the 5 remaining post-27 candidates.
5. `docs/envoy-go/SKILL_ROUTING.md` + `BOOTSTRAP_PROMPT.md` §4/§5 — the per-phase lifecycle (state 1 → 2 SPEC) + the cold-start invariants.
6. The AS-BUILT framework the SPEC refines against: `internal/filter/network/` (the read-filter chain framework, `ReadFilterCallbacks`, `Connection.RequestedServerName()`, the `TerminalFilter` seam, `builtins.RegisterBuiltins`); `internal/filter/tcpproxy/filter.go` (the boot-static cluster resolution at `NewFilter`/`Handle` that phase 27 makes per-connection); the phase-26.3 directory `docs/envoy-go/phases/26.3-network-filter-rbac/` (the most-recent §9 SPEC/PLAN precedent + the connection-scoped dynamic-metadata sibling).
7. `docs/envoy-go/DECISIONS.md` ADR-0213/0214/0215/0216/0217/0218 (the §9 framework + rbac family) + ADR-0106 (ROADMAP flip discipline) + ADR-0044 (in-place ADR-body discipline) + ADR-0045 (split-gate) + ADR-0052 (atomic doc bundle) + ADR-0004 (empirical-pin-against-reference-Envoy discipline) + ADR-0005 (writing-plans-for-SPEC).
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — execution-style + worktree + push + subagents-no-push + the network-filter constraints (typeurl-extensions, onnewconnection-halts) + the differential dispatch/asserter constraints + per-task gofmt/lint — all carry forward to the 27 SPEC/PLAN/IMPL.
