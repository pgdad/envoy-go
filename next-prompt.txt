=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-CHECKED 2026-07-13 (after the phase-62 BRAINSTORM landed + row 62 registered `in-progress`, run MECHANICALLY) — it does NOT fire. Do NOT create `stop`.** State:
- (1) prints `NOT DONE: row 62` — the phase-62 row registers `in-progress` at its BRAINSTORM commit per the ROADMAP §Schema invariant (it flips `done` at the phase-62 IMPL). At least one non-`done` row ⇒ the sentinel is blocked.
- (2) prints THREE live "candidates:" sentences — HTTP/3, xDS, and Observability ⇒ three families STAY OPEN. (`reference_sentinel_deferred_sentence_live_vs_historical`.)
- (3) prints `NEVER OPENED: gRPC`, `Runtime`, `WASM` ⇒ THREE families never opened. Operational-tooling also STAYS OPEN.

Any of (1)/(2)/(3) printing ⇒ the sentinel does NOT fire.

**STATUS AS OF 2026-07-13: the phase-62 BRAINSTORM LANDED and ROADMAP row 62 (`tracing-custom-tags-request-header`) is registered `in-progress`.** The BRAINSTORM SELF-PICKED (loop AUTONOMOUS per the standing directive) the smallest CLEANLY-DIFFERENTIAL-PROVABLE candidate from the live Observability deferred sentence — the tracing `custom_tags` **`request_header`** source arm (the THIRTEENTH Observability-family row, the SECOND `custom_tag` source type after the phase-59 `literal`/ADR-0277, the FIRST PER-REQUEST-resolved custom tag) — over `environment` (STATIC-resolvable so fewer prod LoC, BUT its differential drags a harness env-var-injection wrinkle — NOT cleanly-provable-smaller) / `metadata` (needs a dynamic-metadata lookup path) / `spawn_upstream_span`/`http_service`/force-trace / OTLP-metrics / opening a new family. The design is SETTLED at BRAINSTORM time (full source read against `9f2494ed`): lift the one `request_header` reject (`internal/tracing/config.go:155-156`); read a named request header (`CustomTag_Header{name, default_value}`) into a `{tag, value}` span attribute via the LANDED `Span.Attrs []KV`/`upsertAttr` seam (both OTLP + Zipkin); proto-authoritative missing-header semantics (present → value; absent + default → default; absent + empty default → OMIT); the CENTRAL design decision (D-RH-CONFIGMODEL) is the config model — the phase-59 STATIC `TracingConfig.CustomTags []KV` becomes an ORDERED `[]CustomTagSpec` + a per-request `ResolveCustomTags` helper threaded at the THREE `accesslog_emit.go` call sites (H1 `:55`/H2 `:116`/H3 `:177` — the H3 site added at phase 61.2). Per the STANDING DIRECTIVE, the lifecycle advances THIS phase to its SPEC (no new subject — mid-lifecycle work is more defensible than opening a brand-new one).

==============================================

## What THIS session does (the phase-62 SPEC)

Perform the SPEC stage (lifecycle-state 1 → 2) for the phase-62 subject — the **tracing `request_header` custom_tag source arm** (Observability family). This is a DOCS-ONLY stage in a FRESH worktree off master (`feedback_git_worktrees`); NO production `.go` changes. It authors `docs/envoy-go/phases/62-tracing-custom-tags-request-header/SPEC.md`, DISPOSING every D-RH-* open question from the BRAINSTORM §10 via LIVE PROBES against `envoyproxy/envoy:contrib-v1.37.2` (`reference_probe_fresh_container_per_arm`, `reference_envoy_contrib_image_tagging`, `reference_docker_probe_bridge_network`, `reference_host_gateway_ip_docker_desktop`):
- **D-RH-WIRE** — how the reference emits a `request_header` custom tag on the OTLP span (key = the `tag`, value = the header value as a STRING `AnyValue`, appended via upsert-by-key). Probe with a configured `request_header` custom_tag + a request carrying the header, observed via the `test/helpers/otlptrace` receiver.
- **D-RH-MISSING** (×3 arms) — present → value; absent + `default_value` set → default; absent + empty default → OMIT the tag (proto doc `custom_tag.pb.go:269-271`). Fresh container each arm.
- **D-RH-MULTIVALUE** — a header sent twice: FIRST value vs comma-joined. Probe; the resolution picks per the answer.
- **D-RH-PRECEDENCE** — upsert-by-key vs a colliding built-in + config order across a cross-type same-key collision (a `literal` + a same-key `request_header`, BOTH orders). Pins Option A-vs-B for D-RH-CONFIGMODEL.
- **D-RH-REJECT** — `environment`/`metadata` reject substrings UNCHANGED; the empty-`request_header.name` structural reject (PGV `Header.name` `min_len:1` + pattern, RE-DERIVE `custom_tag.pb.validate.go:583-594`); confirm (one probe arm) the reference ACCEPTS `request_header` (the departure genuinely narrows).
Plus the DESIGN pins: **D-RH-CONFIGMODEL** (the ordered `[]CustomTagSpec` vs two-slice; anticipated ordered-spec for order-correctness) + **D-RH-RESOLVE-SEAM** (the `ResolveCustomTags(specs, headerLookup) []KV` helper location/signature + the THREE call-site threadings with `reqHeaderLookupH1`/`reqHeaderLookupH2`; confirm `BuildServerSpan` stays unchanged) + **D-RH-FIXTURE** (ONE new OTLP fixture, `0105` anticipated; default/omit unit-tested; Zipkin unit-tested) + **D-RH-FUZZSEED** (a `FuzzHCMConfigParse` seed, no new fuzzer; count stays 55) + **D-RH-SPLIT** (SINGLE FLAT ROW anticipated, ~10–14 tasks). RE-DERIVE every BRAINSTORM §6 edit site against source (`feedback_brief_citations_not_evidence`). DRAFT **ADR-0283 §Context** (§Decision/§Consequences at the IMPL per ADR-0044 — NOT at this SPEC). Land it (commit + push per `feedback_push_to_origin`) and ROLL THIS FILE to the PLAN stage.

**Honor throughout (the discipline, unchanged):**
- **Lifecycle = one stage per session.** This session: the SPEC ONLY. After it lands: roll to the PLAN.
- **Process anchors:** ADR-0044 (ADR §Decision/§Consequences land at the IMPL) · ADR-0045 (the split escape-valve, only if the slice exceeds ~15 tasks — unlikely for a single source arm) · ADR-0106 (a row flips `done` at its IMPL; the SOLE-leg row 62 has NO parent rollup) · `reference_roadmap_split_phase_row_done`.
- **`next-prompt.txt` IS TRACKED** (`reference_next_prompt_tracked_despite_gitignore`) — edit it inside the stage worktree and fold into the stage commit; locate commits by SUBJECT (`git log --grep`), never by position.
- **Worktree discipline** (`feedback_git_worktrees`, `feedback_subagent_worktree_detach`, `feedback_subagent_worktree_path_targeting`) — a SPEC with live probes is heavier than a BRAINSTORM; keep worktree hygiene, and VERIFY the main checkout stays clean (edit worktree-relative paths only — the phase-62 BRAINSTORM caught a STATE.md main-checkout mis-target and reverted it). If subagent-driven, `feedback_execution_style` + `feedback_subagents_no_push`.
- **The tracing traps** (`reference_tracing_custom_tag_override_builtin` — the upsert-by-key seam is already landed and reused; `reference_tracing_upstream_cluster_framework_gap` — the 4 EMPTY built-ins are a SEPARATE per-request gap, do NOT conflate a header read with them; `reference_differential_fixture_dispatch_constraint`; `reference_fuzzer_count_docs_drift`; `reference_fatalf_makes_assertions_unreachable`; `reference_sentinel_deferred_sentence_live_vs_historical`).

**ADR reservation ledger (provisional; each finalized at its IMPL in landing order):** ADR-0277 = phase-59 tracing-custom-tags-literal (LANDED) · ADR-0278 = xDS 60.1 (LANDED) · ADR-0279 = HTTP/3 61.1 (LANDED) · ADR-0280 = xDS 60.2 (LANDED) · ADR-0281 = HTTP/3 61.2 (LANDED) · ADR-0282 = HTTP/3 61.3 harness/differential (LANDED) · **ADR-0283 = phase-62 tracing-custom-tags-request-header** (§Context drafts at THIS SPEC; §Decision/§Consequences at the phase-62 IMPL) · next-free unreserved **ADR-0284**.

**Current counts (VERIFIED at the phase-62 BRAINSTORM against master tip `9f2494ed` — RE-VERIFY at session start, `git fetch` first):** fixtures **106** (`ls -d test/fixtures/[0-9]*/ | wc -l`; tail `0104-http3-downstream-get`; a `^[0-9]{4}-` grep under-counts to 104 — the count includes the lettered `0007a`/`0007b`) · fuzzers **55** · DECISIONS tail **ADR-0282** (next-free **ADR-0283**) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** · go.mod modules **2** (`quic-go v0.54.1` direct + `qpack v0.5.1` indirect). A SPEC is DOCS-ONLY ⇒ ALL counts UNCHANGED at this stage.

**Locating the phase-62 BRAINSTORM stage squash** (a router cannot name its own sha — identify by SUBJECT, never by position):
```sh
git log --oneline --grep='^phase 62 (tracing-custom-tags-request-header) BRAINSTORM' -1
```

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-62 SPEC is next; the BRAINSTORM landed, row 62 `in-progress`).
2. `docs/envoy-go/phases/62-tracing-custom-tags-request-header/BRAINSTORM.md` — the 12-section design (the D-RH-* open questions §10; the edit-site roster §6; the config-model decision §2.5; the resolution seam §2.6; the rejected alternatives §2.1).
3. `docs/envoy-go/STATE.md` — the active-phase header (phase 62 BRAINSTORM done; NEXT = the phase-62 SPEC).
4. `docs/envoy-go/ROADMAP.md` — row 62 + the Observability family prose + its LIVE deferred sentence (the `request_header`/`environment`/`metadata` candidates); the row-registration §Schema invariant.
5. `docs/envoy-go/phases/59-tracing-custom-tags-literal/` (SPEC/PLAN) + `docs/envoy-go/DECISIONS.md` ADR-0277 — the LANDED literal engine (the seam the `request_header` arm extends): `parseCustomTags` (`internal/tracing/config.go:139`), `BuildServerSpan`/`upsertAttr` (`internal/tracing/span.go:64`/`:121`), the THREE `accesslog_emit.go` call sites, `reqHeaderLookupH1`/`reqHeaderLookupH2`.
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the tracing `reference_*` traps + the differential-harness traps for the fixture + the probe traps for the D-RH-* live arms.
