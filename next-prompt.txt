=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **THERE IS BANKED MID-LIFECYCLE WORK AT THIS TIP: PHASE 85 (`h2spec-selector-repair`) IS OPEN AT LIFECYCLE-STATE 3.** The phase-85 PLAN landed 2026-08-09 (the row's ENTIRE change set was BUILT AND RUN by probe agents — gate `95/94/1/0` five times — then destroyed; row 85 stays `in-progress` at `ROADMAP.md:147`; `want` stays 117). ⇒ **THIS SESSION PERFORMS THE PHASE-85 IMPL — the SINGLE leg (D-85-SEQ), ONE atomic commit.** `STATE.md`'s `next-skill:` reads `IMPL — phase 85 (h2spec-selector-repair)` — that IS your instruction. The task list is `PLAN.md` §5 (seven tasks, TDD order INSIDE the leg, gate evidence LAST); budget **~760-900 net `.go`** (PLAN §7; the measured +757 prototype is itself a LOWER BOUND).

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=117 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **`want` IS 117** (set at the phase-85 BRAINSTORM, which added row 85). ⚠️ **AT THIS TIP CHECK (1) PRINTS `NOT DONE: row 85` — THE CORRECT, EXPECTED OUTPUT WHILE PHASE 85 IS OPEN.** ⚠️ **THIS SESSION'S IMPL FLIPS ROW 85 `done` — AFTER the flip, check (1) goes SILENT for only the second time in project history. THAT IS NOT THE SENTINEL FIRING: the condition is a CONJUNCTION and check (2) still prints SIX** (at `:195 :201 :207 :217 :223 :231` pre-edit — re-derive, the anchors drift with every edit). Check (3) is SILENT. The phase-84.2 IMPL close is the precedent: (1) SILENT · (2) SIX · (3) SILENT ⇒ does NOT fire, `stop` NOT created. ⚠️ **The ONLY sanctioned `ROADMAP.md` edit this session is row 85's `status` field, in place** — the whole-file leak check runs before AND after (lines 235, rows 117, union 6, `-family row` 95/67, `gRPC-family row` 2, `Operational-tooling-family row` 3, ARM-A flags 119+131 only; row 85's summary deliberately reads `Operational-tooling-family MAINTENANCE row`, which matches NEITHER the check-(3) pattern NOR `-family row`).

⚠️ **THE DOCTORING NCs REMAIN MANDATORY EVERY SESSION** — and doubly so this session, because after the flip checks (1)+(3) are BOTH silent and a broken check is nearly indistinguishable from a healthy one. Both:
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md at want=117: pre-flip expect BOTH "NOT DONE: row 62" AND
# "NOT DONE: row 85"; post-flip expect "NOT DONE: row 62" ALONE
```
```sh
# the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```

⚠️ **AND WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -oiE '-family row'` fails with `grep: amily row: No such file or directory` and the surrounding arithmetic prints `base=0 now=0 delta=0`, **which reads exactly like "no change".** Observed live at FOUR phase-84 stages. **Always pass `--` before the pattern**, and remember the counts are form-dependent: **95 occurrences vs 67 LINES** at this tip (`grep -c` counts LINES). **A gate that reads zero on BOTH sides is not evidence of invariance.**

**SENTINEL RE-RUN MECHANICALLY AT THE PHASE-85 PLAN CLOSE (2026-08-09, base `be018027`). IT DOES NOT FIRE; `stop` was NOT created** (`ls stop` => `No such file or directory`, repo root AND stage worktree; `ROADMAP.md` verified BYTE-IDENTICAL to master by empty diff). ACTUAL output, recorded not predicted: input measured **235 lines / 117 data rows** first; **(1) `NOT DONE: row 85`** at `want=117` — the single EXPECTED line, denominator assertion silent; **(2) SIX — `:195 :201 :207 :217 :223 :231`** (⚠️ do NOT "fix" it and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases; the candidates sentences are a WINDOW onto a larger deferred backlog, not an inventory of it); **(3) SILENT**. The condition is a CONJUNCTION and checks (1) and (2) print ⇒ the sentinel does NOT fire.
- **ALL FOUR NCs FIRED:** row-62 doctoring => `NOT DONE: row 62` AND `NOT DONE: row 85`, with `NC LANDED? [ in-progress ]` inspected first; `want=116` => `GATE FAIL: examined 117 data rows, expected 116`; check-(3) doctoring (residual `gRPC-family row` confirmed **2 -> 0** on the doctored copy first) => `NEVER OPENED: gRPC` while `WASM` correctly stayed silent; check-(2) one-arm counts **5** (long arm alone) / **1** (short arm alone), union **6** (a one-arm strip is NOT an NC for the union).
- ⚠️ **Re-run all three checks and both doctoring NCs yourself and record what YOU see — BEFORE and AFTER the row-85 flip.**

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (ugrep under the hood). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Reproduced at EIGHTEEN separate tips.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **THE METHOD BINDS; carry no standing discrepancy figure and no standing absolute.** ⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS; print FILE NAMES, never a count.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files; use `-v --no-index`, and `git status` is the discriminating check). ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **`grep -c` ON ZERO MATCHES IS TOOL-SPECIFIC** (only `git grep -c` prints nothing; all exit 1 — capture the value, never chain on the exit code). ⚠️ **`grep -c` COUNTS *LINES*, NOT *OCCURRENCES*.** ⚠️ **`\t` IN GNU ERE IS A LITERAL `t`** — use `-P`. ⚠️ **A `git grep` PATHSPEC ENDING IN `/` READS A FAIL-UNSAFE ZERO** (shape 33) — use the shell-glob form. ⚠️ **AN UNANCHORED PANIC-GREP FALSE-FIRES 14 TIMES ON A FULLY-GREEN DIFFERENTIAL LOG** — **the canonical gate is ANCHORED: `^panic:|DATA RACE|SIGSEGV`. A gate that fires on a green log is worse than no gate.**

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`** with FOUR blind spots (an em-dash, a DOT in `http-1.1`, two LETTER-suffixed ids). ⚠️ **EXECUTED on scratch copies at the phase-77 PLAN: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** ⚠️ **DO NOT restore the regex form.** Pre-flip, check (1)'s single expected line is `NOT DONE: row 85` — **any OTHER row id appearing there is a finding.** Post-flip it is SILENT — and the row-62 doctoring NC is what proves it still LOOKS.

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

⚠️ **Run the union form, never one half of it.** ⚠️ **A ONE-ARM STRIP IS NOT AN NC** (6 -> 5 short / 6 -> 1 long; never 6 -> 0). ⚠️ **The documented trap's stated MECHANISM is refuted while the trap is real** — the inflation comes from ROADMAP table rows carrying BRAINSTORM prose. **Cite the command; do not re-inherit the explanation.** ⚠️ **The line numbers drift with edits — re-derive them; the current six are `:195 :201 :207 :217 :223 :231`.** ⚠️ **These six paragraphs remain the final sentinel blocker, and any edit near them is a sentinel-affecting edit.** All six are standalone family PROSE paragraphs (not rows), naming ~42 distinct candidates; every "retire the paragraphs" shape is doctrinally foreclosed (`reference_sentinel_matcher_string_self_clears`). Row 85's candidate came from SPEC-84 §13, OUTSIDE the windows; no window sentence is edited by this phase, at any stage — **including the IMPL's row flip.**


==============================================


## STATUS AS OF 2026-08-09: PHASE 85 (`h2spec-selector-repair`) IS **OPEN AT LIFECYCLE-STATE 3** (PLAN done). ROW 85 IS **`in-progress`** at `ROADMAP.md:147`. **THIS SESSION PERFORMS THE PHASE-85 IMPL AND FLIPS THE ROW `done`.**

**What the 85 PLAN landed (2026-08-09, base `be018027`, branch `phase-85-plan`).** Docs-only, ZERO `.go`: `docs/envoy-go/phases/85-h2spec-selector-repair/PLAN.md` (471 lines — nine-finding ledger, two probe verdicts, seven-task decomposition with measured code, executed break roster, measured cost) + `PROGRESS.md` rolled; `STATE.md` rolled IN PLACE (64 lines, seven singletons verified); `STATE_HISTORY.md` **470 -> 472** (PLAN-84.1 evicted at the five-entry cap; strictly append-only, numstat `2 0`, base a byte-exact PREFIX via `cmp`; absence guard 0/0 with THREE controls re-derived FROM THE FILE: phase-82-IMPL-done **6/6** — drifted UP from 5 again — phase-83-PLAN-done **4/4** up from 3, invented label 0/0). `ROADMAP.md`, `DECISIONS.md`, `BEHAVIOR_CONTRACT.md` **BYTE-UNTOUCHED**; strict `PROPOSED` guard stays **1**. **Method: TWO probe agents in DETACHED worktrees off `be018027`** (bands 46700-46799 / 46800-46899, `p2p85-*` containers removed BY NAME, both worktrees destroyed with byte-exact sha256 proof — P1's twice; nothing committed or pushed by either). **P1 BUILT AND RAN THE ENTIRE CHANGE SET** (11 arms RED -> fixes green -> harness repaired -> gate `95 tests, 94 passed, 1 skipped, 0 failed` FIVE times -> three guard NCs fired). **P2 measured the two SPEC questions at the frame level.** ⚠️ **The two agents CONFLICTED on one load-bearing claim and EXECUTION resolved it** (PLAN §1.3/§2.2).

### ⚠️ WHAT THE PLAN MEASURED AND DECIDED — CARRY THESE; THE IMPL EXECUTES PLAN §5's SEVEN TASKS AGAINST THEM

- ⚠️ **THE FOUR MOVED CLAIMS (PLAN §1):** (1) **6.5.2/2 passes GENUINELY** — x/net v0.34.0 `parseSettingsFrame` rejects IWS>2^31-1 at frame-PARSE time; `translateFramerErr` already yields GOAWAY(FLOW_CONTROL_ERROR); the SPEC's conditional validator arm dissolves into 3-line defense-in-depth; **a REAL wrong-code HANDSHAKE defect surfaced instead** (`readClientSettings` `settings.go:80-83` blanket-wraps parse errors as PROTOCOL_ERROR; RFC wants the framer's own code — fix: 4-line `translateFramerErr` reuse + 9/2 `Run` step-3 plumbing; the handshake IWS arm is its RED anchor). (2) **the IWS=0 quirk is REAL but UNIT-ONLY-DISCRIMINABLE** — P2's "co-requisite for 95-green" REFUTED by P1 (95/94/1/0 three times WITHOUT it); the announced-flag fix is IN as an RFC MUST, gated by its unit arm; ⚠️ **seeding and the walk's effective-old MUST share ONE computation or 6.9.2/1 reds.** (3) ⚠️ **the SPEC's layer-2 guard is DELETABLE ONE ROSTER ENTRY AT A TIME** — the roster-drop NC did NOT fire as SPEC'd; the REVERSE check (every running `http2/*` suite must be rostered, ~9 lines) is MANDATORY (PLAN Task 3 has the code). (4) **the roster is 31 `http2/*` suites, NOT 24** — the 31-entry map with measured minimums (sums to 95) is in PLAN Task 3 Step 3; ⚠️ `gofmt -w` the literal or golangci fires.
- ⚠️ **THE BREAK ROSTER (PLAN §6): 11 arms proven RED by execution** (3 mid-conn validation, 4 handshake incl. the wrong-code IWS signature `GOAWAY code = PROTOCOL_ERROR, want FLOW_CONTROL_ERROR`, 3 walk arms, the quirk arm), **2 reclassified GREEN-ON-ARRIVAL regression pins** (arm 5 mid-conn IWS — x/net parse guard; arm 7d new-stream seeding — already correct; ⚠️ counting either as a RED anchor misstates the TDD ordering), 1 boundary-control pair. **Re-prove the 11 REDs at YOUR tip before greening** (`reference_break_roster_goes_stale_within_its_own_row`).
- ⚠️ **THE GUARD NCs (PLAN Task 4, measured outputs quoted there): NC-1** doctored selector -> layer-1 line + h2spec runs only **86** tests (the OLD harness would be silently green); **NC-2** roster-drop fires ONLY via the reverse check; **NC-3** min-raise fires the count comparison. Re-fire all three on the landed tree; restore by checksum.
- ⚠️ **COST (PLAN §7): measured net +757 `.go`** (production +127, harness +126, tests +504 in ONE new file) vs the SPEC's ~420-750 band — the SEVENTH consecutive `reference_measured_prototype_is_a_lower_bound` firing, cause under-ENUMERATION. **IMPL budget ~760-900 net `.go`; overrun beyond it is a recordable finding, not silently absorbed.**
- **D-85-SEQ binds:** ONE leg, ONE atomic commit — unit arms RED, production fixes, harness repair, guard NCs, ci.yml step (~13 lines, `differential` job, PLAN Task 5 has the YAML), docs, ADR-0307 completion, row flip — **gate evidence `95 tests, 94 passed, 1 skipped, 0 failed` run LAST and quoted in the commit message** (the skip is 6.9.2/2, invariant). Subagents commit LOCALLY only; controller squashes to ONE commit, merges, pushes.
- **D-85-SWEEP reconcile set (FIVE files):** `h2spec.go` (the fix) · `CONFORMANCE_PINS.md` APPEND-style (the 2026-04-25 53/53 record STAYS; append corrected 31-suite table + new run record + reference-observation ¶: reference fails {6.3/1, 6.7/2, 6.9.1/2, 6.9.1/3} disjoint, twelfth slot FLAKY in section 8, bridge-vs-host-gateway caveat) · `BEHAVIOR_CONTRACT.md` `:2052-2054` + `:2056` ONLY riding ADR-0307 (⚠️ enumerate by-line cites the insert would shift FIRST; the five historical 53/53 ¶s at `:1464 :1669 :4263 :5002 :5009` are RECORDED, not rewritten) · `STATE.md:38` (the living false `h2spec 53/53` line flips at THIS close) · `.github/workflows/ci.yml`. **Everything else RECORD** (DECISIONS 26x, ROADMAP 33x/31 historical rows — retro-editing them is the leak-audit hazard — phases 527x/226 files, STATE_HISTORY, `docs/TEST_GAP_ANALYSIS.md:28`).
- **ADR-0307 completion IN PLACE:** §Decision + §Consequences appended after the RETAINED italic footer, no renumber, no `---` (the ADR-0294-0306 shared block form); STATUS `PROPOSED` -> `COMPLETE`; ⚠️ **strict guard `^> \*\*STATUS: PROPOSED` 1 -> 0 IS CORRECT AT THIS STAGE ONLY.** §Decision content: the eight D-85-* dispositions AS EXECUTED, incl. the PLAN's four moved claims (the §Context already quotes ADR-0051's false sentence). Record the quirk as *"gate-invariant, unit-gated"* and the client-side symmetric gap (`client.go:376-407`) as OUT beside the CONTINUATION backlog.
- **D-85-CLIENT stays OUT** (h2spec cannot gate the client side; one-sided-gate shape). **NO differential fixture** (+0 held, D-85-WALK). **Stat surface +0** — assert by call-site enumeration **208/36 at base AND tip**, NEVER `TestNoNewStat*`.

### ⚠️ COUNTS AT THIS TIP — RE-DERIVE MECHANICALLY, NEVER COPY

**HOUSE RULE: every number below is a LIVE FIGURE. Re-run it in your own worktree with its negative control observed.**

- **differential fixtures 121** — numeric tail `0119-grpc-unary-trailers`, next-free `0120`. **This row lands +0 fixtures.**
- **`ROADMAP.md` 235 lines / 117 data rows**; row 85 at `:147`; check-(2) anchors `:195 :201 :207 :217 :223 :231`; `-family row` **95/67**; `gRPC-family row` **2**; `Operational-tooling-family row` **3**; ARM-A flags **119+131 ONLY**. ⚠️ The row flip must keep ALL of these except the row's own status field.
- **`DECISIONS.md` 18010** lines · **306** `^## ADR-` headings · tail **`ADR-0307` PROPOSED** · `^## ADR-0308` = **0** · strict `PROPOSED` guard **1** (LIVE until this session's completion edit). ⚠️ next-free ADR-0308 is DERIVED FROM THE TAIL (headings+1 COLLIDES at the ADR-0209 gap).
- **`BEHAVIOR_CONTRACT.md` 5955** · **`STATE.md` 64** · **`STATE_HISTORY.md` 472** · **`BOOTSTRAP_PROMPT.md` 522** at the repo ROOT (no leading slash) · phase dirs **126**, `REVIEW.md` **37** (standing departure, named not claimed).
- **h2spec (BY EXECUTION at the PLAN):** corrected gate **95/94/1/0** on the PROTOTYPE tree (five runs) · **95/90/1/4** on the UNFIXED tree ({6.5.2/1, 6.5.2/3, 6.5.2/4, 6.9.2/1}) · declared selectors **9** slash-form to flip · roster **31** suites · reference **95/82/1/12** with flaky twelfth slot (RECORD only).
- **stat call sites 208 / 36 production files** · **fuzzers 55 / 48 files** (`FuzzFrameStream` explores the new reject paths free; no new fuzzer owed) · **BackendKind tail 38** over 39 declarations (do NOT "fix").
- ⚠️ **STILL CONTESTED, NO NUMBER CARRIED:** the `STATE_HISTORY.md` archive-gap total · the loose `PROPOSED` matcher's whole-file count · the stat-surface absolute (1205 vs 1207 — WIDENED; only the DELTA is asserted) · the zero-test `generic/*` suite count (13 vs 14 — guard filters on `package` prefix `http2/` either way).

### ⚠️ DOCUMENTARY DEFECTS — carried forward, still deliberately NOT fixed

- **`ADR-0051` §2 FACTUALLY WRONG** on gate scope (`DECISIONS.md:1747-1750`; ADR-0307 quotes it; this session's ADR completion is the correction vehicle); ADR-0052 `:1803` repeats it. **`ADR-0058`** cites `routerActionH2.doH2` at a location that doesn't exist (`router_h2.go:254` / `h2dispatch.go:720` are real). **`ROADMAP.md` rows 119/131 malformed** (ARM-A GUARD, not a fix). **`STATE.md` §Project counts FROZEN at phase 76** (do not source; do not fix) — ⚠️ **`STATE.md:38` is the LIVING false line and flips at THIS session's close.** **`harness_test.go:208` port inventory STALE.** **`body.go` nolint directives INERT** · **xDS cycle guard NOT AUTOMATED** · **`wasm/doc.go:219` two errors** · **ROADMAP cites `esalaine` FIVE times** (module is `pgdad`) · **`rbac.go:50` token `F2`** (don't grep-anchor) · **root `PROGRESS.md`** stray phase-32.1 doc.

### ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + BOTH DOCTORING NCs YOURSELF, BEFORE AND AFTER THE FLIP** — record ACTUAL output.
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** Lineage: 85-B (gate run both ways, 4 hidden failures) · 85-S (reference measured, JUnit root cause captured, guard key corrected) · 85-P (**the whole change set BUILT AND RUN; four claims moved; the P1-vs-P2 conflict resolved by execution — a probe agent's inference about a cell it never ran is a CLAIM**). The IMPL refutes the PLAN where execution at ITS tip disagrees — and records it.
3. **LINE ANCHORS DRIFT; SYMBOL ANCHORS DID NOT.** Re-verified at the PLAN: selectors `h2spec.go:21-37`, zero-case skip `h2spec_test.go:310-312`, JUnit structs `:262-280`, `assertThreshold` `:295-351`, `onSettings` `conn.go:507-538` (IWS case `:519-520`), seeding `conn.go:372-375`, `Run` step 3 `conn.go:117-120`, `readClientSettings` `settings.go:79-108`, `window` `flow.go:12-16`, `safeAddInt32` `conn.go:25-31`, streams map `conn.go:50`, `pendingDispatch` `conn.go:60-66`. **RE-VERIFY EVERY CITE AT YOUR TIP.**
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Still live: SDS dial-budget (TWO packages) · `internal/cluster` `-race` outlier · `internal/httpclient` zero-value · driver-receiver port race (roster 42; `0081` binds `0.0.0.0:42039`) · the two 84.2-era flakes (cleared, unclassed) · the REFERENCE h2spec section-8 flip (reference-side only; subject zero-variance).
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only).
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`.
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL.** **A harness's exit code is not the command's.** **`gofmt -l` never exits non-zero — gate on OUTPUT.**
8. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-85-*` worktree should outlive its stage close** — expect only `master` plus your own.
9. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** (a)/(b) differential 121 fixtures, +0, full suite with `INNER_EXIT` at this IMPL; (c) ⚠️ **this row's IMPL RETIRES the "NO ROW MAY CITE h2spec 53/53" caveat — cite the NEW figure only from YOUR OWN tip's run, never the prototype's**; grpc-conformance deferred in writing; proxy-wasm 10/16; (d) fuzzers 55/48, no new parse surface; (e) `INNER_EXIT` on every differential launch AND `go test ./...`; (f) no REVIEW.md — standing departure.

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 85 (h2spec-selector-repair)' --format='%h %s'   # 3 at this tip: BRAINSTORM + SPEC + PLAN. An invented slug returns 0.
git log --grep '^phase 84 (grpc-unary-response-trailers)' --format='%h %s'  # 6 — a CLOSED phase, use it as a FIRING CONTROL
```
⚠️ **The count comments are LIVE FIGURES — VERIFY before relying.** Take the NEWEST line (`| head -1`).

Subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** and tear down docker **BY NAME**. ⚠️ **Band private ports outside BOTH `20000-31007` AND `11000-14999`, clear of the static fixture ports (10000-19172); `0081` binds `0.0.0.0:42039`.** Phase-85 stages used `46500-46599`, `46600-46699`, `46700-46899` cleanly — the IMPL should band from **46900-46999** (the h2spec harness itself picks ephemeral ports; banding matters only for manual boots). ⚠️ **ONE WORKTREE PER AGENT preferred; explicit-path staging if shared; never an unscoped `git restore`.** Per-task `gofmt` + `golangci-lint` on touched packages (⚠️ misspell locale US; the roster literal needs `gofmt -w`). ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch.**

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root — **observed live AGAIN at this PLAN (every compound command's trailing check printed the repo root).** Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — sha256 against the base blob, never by eye; work DETACHED so a failed revert cannot dirty a branch.

⚠️ **THE ARCHIVE-ABSENCE GUARD — USE THE ROBUST FORM** (bullet-anchored + whole-file fixed-string, NC'd with a REAL target re-derived FROM THE FILE first; a control's own count is a LIVE FIGURE: phase-82-IMPL read **6** at this close, up from 5; phase-83-PLAN read **4**, up from 3). **Verify STRICTLY APPEND-ONLY**: `git diff --numstat` = `N 0` AND base a byte-exact PREFIX (`cmp`).

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads `IMPL — phase 85 (h2spec-selector-repair)` — that IS your instruction.** §Project counts SELF-CONTRADICT §Current — do not anchor on them; `STATE.md:38` is the living false line YOUR close flips.
2. `docs/envoy-go/phases/85-h2spec-selector-repair/PLAN.md` — **the binding task list**: §1 the nine-finding ledger, §2 the two probe verdicts, §3 global constraints, §4 file structure, §5 the seven tasks WITH the measured code (validator, plumbing, `window.adjust`, walk, JUnit structs, guard incl. REVERSE layer 2, 31-entry roster, ci.yml step), §6 break roster, §7 cost + budget, §8 close mechanics, §9 sentinel record. Then `SPEC.md` for the D-85-* dispositions it executes.
3. `docs/envoy-go/ROADMAP.md` — §Schema first (`:18`), row 85 at `:147`; the ONLY edit is its status field.
4. The code sites (method note 3) — re-verify every cite at your tip.
5. `docs/envoy-go/DECISIONS.md` tail — ADR-0307 `PROPOSED` §Context (your completion target); ADR-0051 `:1747-1750` context.
6. `docs/envoy-go/CONFORMANCE_PINS.md` — refresh procedure `:7-18`, the 18-suite table + first-run record (STAY; you APPEND).

## After the IMPL lands — the roll-forward
Row 85 flips `done`; check (1) goes SILENT (the second time ever); check (2) still prints SIX ⇒ **the sentinel does NOT fire and `stop` is NOT created** — re-run everything and record ACTUAL output. **PHASE 85 CLOSES; NO banked mid-lifecycle work remains** ⇒ the roller **SELF-PICKS** the next subject per the standing directive (smallest defensible candidate; record pick + rejected alternatives in the new BRAINSTORM). Known candidates on the board (re-derive costs at your tip — `reference_deferred_candidate_cost_restale`): the CONTINUATION two-sided repair (strongest product defect, 2-4x row 85, now with the client-side SETTINGS gap named beside it in ADR-0307), the stat-surface recount (1205-vs-1207 WIDENED), the `validate` nil-`sdsProvider` bug, `ssl.connection_error` (+444 floor), `test/conformance/grpc/` (9/26), REVIEW.md restoration, plus the ~42 candidates in the six family windows. **Roll this file to the new BRAINSTORM**, carrying: the sentinel state you observed (both NCs, before AND after the flip), the realized cost vs the ~760-900 budget, the gate figure FROM YOUR TIP, any PLAN claims that did not survive execution, and the re-derived counts.
