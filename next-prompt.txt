START a fresh session for envoy-go (router last updated 2026-06-25; the **phase 44.3 (`grpc-access-log` header-capture) PLAN is DONE + pushed to origin/master**). HEAD is the **phase-44.3 PLAN commit** (subject `phase 44.3 (grpc-access-log header-capture) PLAN: the ~9-task TDD spine + the SPEC §12 D-HDR-* resolutions — ANCHORS the 44.3 IMPL`, a docs-only direct-on-master commit `e8c42d28` — NOT a squash); it is the stable anchor at/near the tip. Re-read the shas with `git log --oneline -27`, do NOT assume — any number of docs-only `next-prompt.txt:` commits may stack ATOP the PLAN commit (which stays the stable anchor). BENEATH the 44.3 PLAN commit is the run of `next-prompt.txt:` commits, then the **phase-44.3 SPEC commit `6c0caafe`** (`phase 44.3 (grpc-access-log header-capture) SPEC: …`, a docs-only direct-on-master commit, ~depth 7), then the **phase-44.2 IMPL squash `c4055b20`** (`phase 44.2 (grpc-access-log buffering) IMPL: …`, ~depth 10), the 44.2 PLAN squash `b8cc53ca`, the 44.2 SPEC squash `8d5cdb4a`, the 44.1 IMPL squash `ec05a421`, the 44.1 PLAN squash `5c7cf481`, the 44.1 SPEC squash `a7a24d0c`, the phase-44 BRAINSTORM `81afdd59` (~depth 25, near the bottom of the -27 window — the tip-stamp/roll commits interleave with each squash, so re-read; depths are written for the post-this-commit reality). The `-27` window keeps every named anchor visible with a ~2-slot margin under the BRAINSTORM; a further `next-prompt.txt:` tip-stamp atop this roll would push each anchor one deeper, so bump the depth if the BRAINSTORM slips out of view. **The Observability family is OPEN** (phase 44 is its FIRST row; 44.3 is the FINAL chartered LEG of row 44, so the 44.3 IMPL six-gate flips ROW 44 → `done` but the FAMILY STAYS OPEN — OTLP / tracing / stats sinks / tap remain future rows). **The next action is the 44.3 (header-capture) IMPL** — execute the ~9-task TDD spine in `docs/envoy-go/phases/44-grpc-access-log/PLAN-44.3.md` subagent-driven (`feedback_execution_style`/`feedback_subagents_no_push`/`feedback_pertask_gofmt_lint`), in a FRESH git worktree off master (`feedback_git_worktrees`); the controller squashes + pushes at stage-close (`feedback_push_to_origin`). CONFIRM against `docs/envoy-go/STATE.md` (the source of truth for "what next") before committing to the stage.

First confirm the state via `git log --oneline -27` (expect HEAD at/near the phase-44.3 PLAN commit `e8c42d28`, with the 44.3 SPEC commit `6c0caafe` [~depth 7] + the 44.2 IMPL squash `c4055b20` [~depth 10] beneath, down to the phase-44 BRAINSTORM `81afdd59` [~depth 25]; re-read the shas, do NOT assume). Counts at the 44.3 PLAN-done (docs-only — UNCHANGED): stat surface **1189** (H2 cluster; non-H2 **1185**) / fixtures **84** (tail `0082-grpc-access-log-buffering`; NOTE: count with `ls -d test/fixtures/[0-9][0-9][0-9][0-9]* | wc -l` — `grep -cE '^[0-9]{4}-'` UNDERCOUNTS by 2, skipping `0007a`/`0007b`) / fuzzers **44** (`^func Fuzz` == 44) / BackendKind tail **38** (`H2GoawayResponder`; the ALS receiver is the driver-owned test-only `test/helpers/accessloggrpc`, NOT a BackendKind) / DECISIONS tail **ADR-0256**, next-free **ADR-0257** (the anticipated 44.3 header-capture ADR — §Context already drafted in SPEC-44.3 §13; §Decision/§Consequences land at the 44.3 IMPL per ADR-0044).

## NEXT STEP — the 44.3 (header-capture) IMPL (`superpowers:subagent-driven-development`)

Execute `docs/envoy-go/phases/44-grpc-access-log/PLAN-44.3.md` — the ~9-task TDD spine — subagent-driven (a fresh implementer per task), in a fresh worktree off master. The 44.3 leg makes `additional_request_headers_to_log` / `additional_response_headers_to_log` (PARSE-ACCEPT-but-INERT through 44.1/44.2) LIVE: lift the two header-name lists into `ALSConfig` (lowercased) + extend `Record` with two captured-header maps (`RequestHeaders`/`ResponseHeaders`) + the shared EXPORTED `accesslog.CaptureHeaders` helper (lowercase-key / comma-join / omit-absent / empty-for-present-empty — AMEND-HDR-1/2/3) + the emit-hook capture at H1/H2 (gated on a non-empty capture union DERIVED on the HCM `Filter` from the sinks via a `headerCaptureSink` interface — byte-stable nil-maps when empty) THREADING the final response headers through the ~12 emit call sites (a NEW uniform `filter_http.OrderedHeaders` param, `nil` at the 404/500 error sites) + the `buildHTTPAccessLogEntry` per-sink filter + the `NewGrpcAccessLogSink` signature extension + the `0083-grpc-access-log-headers` differential. ANCHORS **ADR-0257**; the 44.3 IMPL six-gate flips ROW 44 → `done` (the FINAL chartered leg) ⇒ but the Observability FAMILY STAYS OPEN.

The ~9-task TDD spine (PLAN-44.3 §): T1 PROGRESS scaffold + baselines + the final ADR-0045 re-check · T2 the `ALSConfig` header-name parse arm (lowercased) [TDD] + header-bearing fuzzer seed corpus (NO new `^func Fuzz`) · T3 the `Record` two-map extension + the EXPORTED `CaptureHeaders` helper (exhaustive table tests) [TDD] · T4 the emit-hook capture (H1+H2) + the response-header threading through the ~12 sites + the capture union on the `Filter` [TDD; no-capture path BYTE-STABLE] · T5 the `buildHTTPAccessLogEntry` per-sink filter + the `NewGrpcAccessLogSink` signature + the `CaptureRequest/ResponseHeaderNames` methods [TDD, full-package `-race`] · T6 the `main.go` wiring pass-through · T7 the `0083` cross-side fixture (VERIFY the backend-origin `content-type` at Step 1; captured request maps + the `content-type` response map + the absent-name OMIT proof, poll-to-converge, query-less, REUSES `HTTPFixedBody`) · T8 `0083` deliberate breaks (`-count=1`, incl the disable-capture/lowercase/comma-join/omit-absent breaks) + 20/20 flake + the FULL-package `-race` · T9 full 85-dir differential + six-gate + ADR-0257 body + BEHAVIOR_CONTRACT + STATE/ROADMAP + the fuzzer-count re-verify (ROADMAP row 44 → `done` [FINAL leg]; the Observability family STAYS OPEN).

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** This session is the 44.3 IMPL (lifecycle-state 2 → 3 for the 44.3 leg; **row 44 flips `done`** at this IMPL six-gate — the FINAL chartered leg). `feedback_git_worktrees` (a fresh worktree off master; the controller squashes the worktree branch into ONE atomic commit + pushes) · `feedback_subagents_no_push` (subagents commit LOCALLY only) · `feedback_execution_style` (subagent-driven, a fresh implementer per task) · `feedback_pertask_gofmt_lint` (each task: gofmt -l + golangci-lint + go vet + go build on touched pkgs).
- **Process anchors:** ADR-0106 (row 44 flips `done` PER-LEG; NO parent rollup — 44.3 is the FINAL chartered leg, so its IMPL six-gate flips row 44 → `done` ⇒ but the Observability FAMILY STAYS OPEN) · ADR-0044 (the ADR-0257 §Decision/§Consequences land at THIS IMPL; §Context already drafted in SPEC-44.3 §13) · ADR-0045 (the split gate — final re-check at PLAN done; ~135 prod LoC, one leg) · ADR-0080 (strict-reject — UNCHANGED; header capture adds NO reject) · ADR-0255 (the 44.1 as-built sink) · ADR-0256 (the 44.2 as-built buffering) · ADR-0257 (the anticipated 44.3 header-capture ADR).
- **Streaming-sink + header-capture differential discipline:** `reference_streaming_sink_differential_framing` (assert the PAYLOAD aggregated across messages, NOT framing) · `reference_differential_grpc_receiver_driver_owned` (the ALS receiver is the driver-owned `test/helpers/accessloggrpc`, NOT a BackendKind — NO helper change at 44.3; the driver reads `GetRequestHeaders()`/`GetResponseHeaders()` off `Entries()`) · `reference_docker_probe_bridge_network` (shared-bridge + container-hostname; the host-gateway gotcha) · `reference_concurrency_differential_release_barrier` (poll-to-converge, NEVER `time.Sleep`) · `reference_differential_run_selector` (`-run 'TestDifferential/0083'`, NOT bare) · `reference_differential_break_protocol_count1` (`-count=1` on every deliberate break + `-race` run) · `reference_full_suite_race_after_background_mutator` (the sink writer goroutine + the 44.2 ticker are background mutators — run the FULL `internal/accesslog` package `-race`) · `reference_fuzzer_count_docs_drift` (re-verify `^func Fuzz` == 44 at completion). The AMEND-HDR-4 backend-origin-response-headers-only constraint carries into the `0083` drive (capture `content-type`, AVOID `server`/`date`/`x-envoy-*`).

## Read first (in order)
1. THIS FILE — the cold-start router (phase 44.3 PLAN is DONE + pushed; the next action is the 44.3 header-capture IMPL).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 44.3 (grpc-access-log) PLAN done`) + the recorded NEXT action (the 44.3 header-capture IMPL) + the count figures (source of truth for "what next").
3. `docs/envoy-go/phases/44-grpc-access-log/PLAN-44.3.md` — the ~9-task TDD spine to execute (the Orientation zero-context brief + the D-HDR-* resolutions + the per-task TDD structure + the verified source seams). THIS IS THE PRIMARY DRIVER.
4. `docs/envoy-go/phases/44-grpc-access-log/SPEC-44.3.md` — the SPEC the PLAN decomposes (esp. §3 the design, §8 the `0083` differential, §11 the D-ALS-HEADERS pins [AMEND-HDR-1..5], §13 the ADR-0257 §Context draft).
5. `docs/envoy-go/DECISIONS.md` — ADR-0256 (the 44.2 as-built) + next-free **ADR-0257** (§Context drafted in SPEC-44.3 §13; body lands at THIS IMPL per ADR-0044).
6. `docs/envoy-go/ROADMAP.md` — row 44 (`grpc-access-log`, in-progress; legs 44.1 + 44.2 done, 44.3 SPEC+PLAN done, 44.3 IMPL remains) + the `### Observability family` heading.
7. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the `### Access log — gRPC Access Log Service (ALS) streaming sink` block (the §9 SPEC-44.3 contract-delta target — the header-capture addition) + the stat-surface 1189 entry.
8. The 44.1/44.2 as-built seams the 44.3 IMPL touches (all verified at PLAN time, re-confirm line numbers before editing): `internal/bootstrap/bootstrap.go` (`ALSConfig` [`:190`] + `parseGrpcAccessLog` [`:361`]), `internal/accesslog/accesslog.go` (`Record` [`:29`]), `internal/accesslog/mapping.go` (`buildHTTPAccessLogEntry` [`:47`]), `internal/accesslog/grpcsink.go` (`NewGrpcAccessLogSink` [`:71`]), `internal/filter/hcm/accesslog_emit.go` (`emitAccessLog` [`:18`]/`emitAccessLogH2` [`:43`]/`h2UserAgent` [`:67`]) + the ~12 emit call sites (`connection.go:322/442/536/637/715` + `h2dispatch.go:304/386/470/516/522/550`; `resp.Headers`/`lrHeaders` are `filter_http.OrderedHeaders` at all action+local-reply sites), `internal/filter/hcm/config.go` (`Filter` [`:91`]/`accessLog []accesslog.Sink` [`:110`]/`parseFilterWithCtx` [`:191`]/`:285`), `cmd/envoy-go/main.go` (`NewGrpcAccessLogSink` call [`:138`]), `test/helpers/accessloggrpc/accessloggrpc.go` (`Entries()` [`:160`] — NO helper change), `test/fixtures/0081-grpc-access-log/` + `0082-grpc-access-log-buffering/` (the differential precedents `0083` copies; `fixture.HTTPFixedBody` backend).
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `reference_streaming_sink_differential_framing`, `reference_differential_grpc_receiver_driver_owned`, `reference_docker_probe_bridge_network`, `reference_fuzzer_count_docs_drift`, `feedback_execution_style`, `feedback_git_worktrees`, `feedback_subagents_no_push`, `feedback_pertask_gofmt_lint`).
