=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, the active phase is fully landed, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action. (The sentinel does NOT apply right now — see below.)

**STATUS AS OF 2026-07-06: phase 52 (`load-balancer-locality-weighted`) PLAN is LANDED (row 52 stays `in-progress`, lifecycle-state 2 → 3). The NEXT action is the phase-52 IMPL.**

- The PLAN (`docs/envoy-go/phases/52-load-balancer-locality-weighted/PLAN.md`, a 10-task TDD spine mirroring the phase-38.1 PLAN's structural shape) decomposes SPEC §10's ~10-11 indicative tasks 1:1, with two adjustments: the `overprovisioning_factor` capture folded into Task 5 (its only consumer), and `locality.go`'s construct split into Task 3 (construction/grouping) + Task 4 (`Pick`) — exactly as SPEC §10's own Task 4 row anticipated.
- **All 3 SPEC §12 PLAN-level D-questions are RESOLVED with concrete code + dedicated unit tests (do NOT re-litigate; the IMPL EXECUTES these):**
  - **D-LW-DUP** → last-write-wins in endpoint-encounter order (`newLocalityWeightedLBWithRNG`'s map-overwrite), proven by `TestNewLocalityWeightedLB_DuplicateLocality_LastWriteWins`.
  - **D-LW-HEALTHALLOC** → confirmed safe against the real `health.go` code; ALSO surfaced a genuine SPEC-uncalled-out side effect: `registerClusterMetrics`'s existing unconditional `if c.health != nil` block now ALSO registers `membership_healthy`/`lb_healthy_panic` for a locality-weighted cluster with ZERO `health_checks` configured (a widened CONDITION on two pre-existing stat names, not a stat-surface count change) — proven by `TestManager_LocalityWeighted_WidensHealthWithoutHealthChecks`, documented in the Task 10 BEHAVIOR_CONTRACT delta.
  - **D-LW-OPF0** → UPGRADED beyond the SPEC's own leaning-to-accept-the-departure default: `newLocalityWeightedLB` threads `(opf uint32, hasOPF bool)` (not the SPEC's bare `uint32`) since `manager.go:417`'s wrapper pointer is already in scope before `.GetValue()` — an explicit `overprovisioning_factor: 0` is now honored LITERALLY (the whole mechanism degrades to flat fallback unconditionally), not silently defaulted to 140 — proven by `TestPick_ExplicitZeroOPF_AlwaysFallsBackToFlat`.
- **A previously-undocumented structural finding, recorded as a coverage boundary (NOT a bug to fix):** a locality's own leaf child (the UNCHANGED `buildLeafLB`/`roundRobin.Pick`) evaluates its OWN `clusterHealth.inPanic` over its OWN endpoint sub-slice — a locality can locally panic-spray even while the wrapper's cluster-wide panic gate stays closed. Verified harmless to the `0095` differential (synthetic backends always answer 200 regardless of health-check status); captured by `TestPick_ChildLocalityCanLocallyPanicIndependently`. Fixing it would contradict the ZERO-new-`Pick`-parameter design (D-LW7) — out of scope for phase 52.
- Also confirmed at Task 1: every EXISTING `Endpoint{}` construction site (across `internal/cluster/*.go`/`*_test.go` + `internal/filter/**`) uses KEYED literals, so adding `Locality`/`LocalityWeight` requires editing EXACTLY ONE production call site (`extractEndpoints`) — a verified finding, not a broad sweep.
- **FINAL ADR-0045 re-check: NO split** — 10 tasks / ~230-330 prod LoC, comfortably under the gate (the phase-35/37 single-flat-row precedent). An independent `plan-document-reviewer` (general-purpose) pass **APPROVED with ZERO blocking issues and ZERO non-blocking folds** — every file:line citation (incl. proto types against the pinned go-control-plane v1.32.4 module cache), cross-task signature consistency, the arm(a)/arm(b) statistical-band math (independently recomputed), both Task 9 deliberate breaks' targeting, and a placeholder scan all verified clean.
- Counts UNCHANGED at this PLAN (docs-only, worktree `.worktrees/phase-52-plan` branch `phase-52-load-balancer-locality-weighted-plan`; controller squashed + pushed at stage-close per `feedback_push_to_origin`): stat surface **1200** / fixtures **96** / fuzzers **52** / BackendKind **38** / DECISIONS tail **ADR-0268** (next-free **ADR-0269**). Anticipated at the phase-52 IMPL: stat **1200** (+0) / fixtures **97** (`0095-lb-locality-weighted`) / fuzzers **52** (+0) / BackendKind **38** (+0) / DECISIONS **ADR-0269** (next-free ADR-0270); ZERO new packages, ZERO new go.mod deps.
- STATE.md/ROADMAP.md/next-prompt.txt updated (docs-only).

=> The NEXT action is **the phase-52 IMPL** — execute `docs/envoy-go/phases/52-load-balancer-locality-weighted/PLAN.md`'s 10-task TDD spine end-to-end, **subagent-driven** (`feedback_execution_style`) in a **FRESH worktree off master** (`feedback_git_worktrees`; e.g. `.worktrees/phase-52-impl` branch `phase-52-load-balancer-locality-weighted-impl`). Fresh subagent per task (or `superpowers:subagent-driven-development`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit, re-runs the full suite + the FULL 97-dir differential on the frozen HEAD, does the Task 9 deliberate-break verification ITSELF (`-run 'TestDifferential/0095'`, never bare `-run '0095'`, per `reference_differential_run_selector`; `-count=1` per `reference_differential_break_protocol_count1`), squashes, and pushes at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). ANCHORS ADR-0269 §Decision/§Consequences per ADR-0044 — FLIPS ROADMAP row 52 `done` (NO parent rollup per ADR-0106; the Load-balancing family STAYS OPEN with 2 remaining candidates: priority load balancing, panic thresholds).

==============================================

## Read first (in order)
1. THIS FILE — the cold-start router.
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 52 PLAN done`) + history.
3. `docs/envoy-go/ROADMAP.md` — row 52 (`in-progress`, lifecycle-state 3).
4. `docs/envoy-go/phases/52-load-balancer-locality-weighted/PLAN.md` — the FULL 10-task PLAN (the authoritative task-by-task spec for the IMPL: exact file paths, complete runnable code per step, exact test commands + expected output, per-task gofmt/lint/vet gates, the D-question resolutions, the File Structure table, the FINAL ADR-0045 re-check, and the closing Verification checklist / six-gate).
5. `docs/envoy-go/phases/52-load-balancer-locality-weighted/SPEC.md` — the authoritative WHY behind the PLAN's design choices (§3.1 the indicative `locality.go` sketch the PLAN refines; §11 the 8 resolved D-LW empirical pins with exact live-probe evidence; §13 the ADR-0269 §Context draft the IMPL's Task 10 promotes to a full entry).
6. `docs/envoy-go/phases/52-load-balancer-locality-weighted/BRAINSTORM.md` — the original design dialogue, for context on WHY each decision was made.
7. `docs/envoy-go/phases/38-load-balancer-subset/PLAN.md` + its PROGRESS.md — the most recent same-family phase, useful as an IMPL-execution shape template (the TDD task-spine convention, per-task gate discipline, the completion-bundle shape).
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — re-check for any entries relevant to locality-weighted LB, health-check propagation warmup, differential band-margin tuning, or the `Endpoint`-construction-site-enumeration lesson (`reference_conn_wrap_method_no_promote`, generalized).

## Honor throughout (the discipline, unchanged for whichever phase comes next)
- **Lifecycle = one stage per session** (BRAINSTORM → SPEC → PLAN → IMPL).
- **Subagent-driven execution** (`feedback_execution_style`) in a **fresh worktree** (`feedback_git_worktrees`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit, re-runs the full suite on the frozen HEAD, does any deliberate-break verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). Per-task gates: `gofmt -l` + `golangci-lint` + `go vet` + `go build ./...` (`feedback_pertask_gofmt_lint`).
- **Process anchors:** ADR-0106 (per-leg rows) · ADR-0044 (ADR §Decision+§Consequences land at IMPL) · ADR-0045 (sub-split soft gate, re-confirmed NO-split at this PLAN) · ADR-0052 (atomic six-gate completion bundle) · next-free **ADR-0269** (lands at this IMPL).
- Re-verify baselines fresh at cold-start — do NOT trust any count in this file without re-running the baseline commands (`ls -d test/fixtures/*/ | wc -l`, `grep -rh '^func Fuzz' --include='*.go' . | wc -l`, etc.) against current HEAD.
