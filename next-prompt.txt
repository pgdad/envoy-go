=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, the active phase is fully landed, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action. (The sentinel does NOT apply right now — see below.)

**STATUS AS OF 2026-07-05: phase 50 (`stats-sink-dogstatsd-batching`) IMPL is LANDED (row 50 flips `done`). NO phase is currently chartered. This is the "none chartered, awaiting a human pick" shape — NOT the termination sentinel.**

- The phase-50 IMPL landed (squashed + pushed to origin/master at commit `169c181d`, superseding the intermediate PLAN commit `7081608e`): lifts the `internal/bootstrap/bootstrap.go` strict-reject on an explicit `dog_statsd` `max_bytes_per_datagram` into an honored `DogStatsdSinkConfig.MaxBytesPerDatagram uint64` field, rewrites `DogStatsdSink.Submit` into a buffer-accumulate-then-flush-on-overflow pair (`appendLine`/`flush`, STRICT `>` boundary), threads the field through `cmd/envoy-go/main.go`, adds two additive `test/helpers/statsdrecv` accessors, and proves it all via the new `0094-stats-sink-dogstatsd-batching` cross-side differential (both deliberate breaks confirmed FAIL, independently re-verified by the controller; 20/20 flake-soak; full-package `-race` clean). **ANCHORS ADR-0267** (§Decision/§Consequences landed). Counts at IMPL exit: stat surface **1200** (non-H2 **1196**, +0) / fixtures **96** (`0094`) / fuzzers **52** (unchanged) / BackendKind **38** (unchanged) / DECISIONS tail **ADR-0267** (next-free **ADR-0268**); ZERO new packages, ZERO new go.mod modules. **ROW 50 (`stats-sink-dogstatsd-batching`) FLIPS `done`** (the sole leg, ADR-0106; NO parent rollup). The controller independently re-ran the full 96-dir differential, the full non-differential test suite (110 packages), the full-package `-race`, and both deliberate breaks on the frozen HEAD before squashing — all green (a machine-contention flake in an UNRELATED test, `internal/filter/hcm/h2`'s `TestServerConn_TinyWindowDelivery`, was isolate-rerun and confirmed pre-existing, not a phase-50 regression).
- **EVERY ROADMAP row (00 through 50, all sub-phases) is now `done`** — re-verified via `grep -n '^| [0-9]' docs/envoy-go/ROADMAP.md` showing 82 rows, all `done`. **This does NOT mean all work is complete** — the router's long-standing invariant is that "none chartered" ≠ "all work complete." The Observability family STAYS OPEN with several deferred-but-not-yet-chartered candidates (see below) that a human has not yet picked from. No human has signaled in this session that the project itself is finished. **Do NOT create the `stop` file.** This mirrors the phase-48-IMPL→row-48-done precedent exactly (STATE.md recorded "NEXT: none chartered" and the loop re-opened DIRECTLY at the next BRAINSTORM with no stop file) — NOT the transient `418ccf0e`→`7e593d84` stop-then-reopen detour after phase 49, which this file's own PLAN-stage authoring already flagged as the wrong shape to repeat. (A `CORRECTION` note is recorded in STATE.md's phase-50 IMPL entry, since the phase-50 IMPL's own drafted STATE.md text initially reasoned toward the termination sentinel and had to be overruled by the controller at stage-close — read it for the full reasoning if picking this back up.)
- STATE.md/ROADMAP.md/BEHAVIOR_CONTRACT.md/DECISIONS.md (ADR-0267) all updated and pushed. PROGRESS-50.md closed out with final re-verified counts.

=> The NEXT action is **NOT a new IMPL, NOT the termination sentinel.** A human must pick the next Observability candidate (or open a new family) to charter a BRAINSTORM. Until that pick is made, there is no active phase to advance — this router should be re-invoked once a human has chosen, at which point the chosen candidate's BRAINSTORM becomes "the NEXT action."

==============================================

## Deferred, not-yet-chartered Observability candidates (a human picks ONE to open the next BRAINSTORM)

These are the SAME candidates that have been deferred since phase 48 opened the Observability family, still not chartered as ROADMAP rows:

- **`graphite` stats sink** — the fourth `stats_sinks[]` TypeURL sibling (after `metrics_service`/`statsd`/`dog_statsd`), currently silently-ignored/rejected at the bootstrap sibling-reject arm.
- **OTLP-metrics stats sink** — another `stats_sinks[]` sibling, distinct from the landed OTLP-*traces*/OTLP-*logs* work in prior families.
- **The plain-`statsd` `tcp_cluster_name` transport** — deferred at phase 48 (ADR-0080 strict-reject; `statsd` is UDP-only in envoy-go today).
- **Tracing extras** — `custom_tags`, `spawn_upstream_span`, `http_service`, force-trace — deferred since the tracing sub-phases (46.1/46.2).
- **The tap filter** — an entirely new HTTP/network filter, not yet brainstormed at all (a bigger lift than the above sink/tracing rows).

None of these is pre-chartered. **Do NOT invent/charter one yourself** — this is a human decision. If a human has NOT picked one when this router is next read, ask which candidate (or a different one entirely) to charter, then open its BRAINSTORM per the project's standard lifecycle (`superpowers:brainstorming` first).

## Honor throughout (the discipline, unchanged for whichever phase comes next)
- **Lifecycle = one stage per session** (BRAINSTORM → SPEC → PLAN → IMPL).
- **Subagent-driven execution** (`feedback_execution_style`) in a **fresh worktree** (`feedback_git_worktrees`); subagents commit LOCALLY only (`feedback_subagents_no_push`); the CONTROLLER verifies each commit, re-runs the full suite on the frozen HEAD, does any deliberate-break verification ITSELF, and SQUASHES + PUSHES at stage-close (`feedback_subagent_autocommit_claudemd` · `feedback_push_to_origin`). Per-task gates: `gofmt -l` + `golangci-lint` + `go vet` + `go build ./...` (`feedback_pertask_gofmt_lint`).
- **Process anchors:** ADR-0106 (per-leg rows) · ADR-0044 (ADR §Decision+§Consequences land at IMPL) · ADR-0045 (sub-split soft gate) · next-free **ADR-0268**.
- Re-verify baselines fresh at cold-start — do NOT trust any count in this file without re-running the baseline commands (`ls -d test/fixtures/*/ | wc -l`, `grep -rh '^func Fuzz' --include='*.go' . | wc -l`, etc.) against current HEAD.

## Read first (in order), once a human has picked a candidate
1. THIS FILE — the cold-start router (none chartered; awaiting a human pick).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 50 IMPL done`) + the CORRECTION note + the full history of prior "none chartered" reopenings (esp. the phase-48→49 precedent).
3. `docs/envoy-go/ROADMAP.md` — confirm all 82 rows `done`, read the Observability family note for the deferred-candidate list.
4. `docs/envoy-go/phases/50-stats-sink-dogstatsd-batching/` (BRAINSTORM/SPEC/PLAN/PROGRESS) — the most recent landed phase, useful as a shape template for whichever candidate is picked next (especially if it's another `stats_sinks[]` sibling like `graphite`/OTLP-metrics, which would closely mirror phases 47-50's structure).
5. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — especially the `reference_*` entries tagged for stats-sink/differential work, which will likely carry forward to the next sink-shaped candidate.
