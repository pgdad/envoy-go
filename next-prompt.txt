START a fresh cold-start session for envoy-go — **the phase-40.3 (`outlier-detection-statistical`: the `success_rate` + `failure_percentage` statistical detectors + the per-interval cross-host aggregation runtime — the FIRST outlier goroutine) PLAN is DONE, LANDED on master (squash commit `42bcf63`), and PUSHED to `origin/master`; the next lifecycle step is the phase-40.3 IMPL** (`superpowers:subagent-driven-development` — execute `docs/envoy-go/phases/40.3-outlier-detection-statistical/PLAN.md` Tasks 1–13 end-to-end, a FRESH SUBAGENT PER TASK + two-stage review; six-gate + full 75-dir differential GREEN; ADR-0247 §Decision/§Consequences IN-PLACE; ROADMAP row 40 → `done`) OR a new subject. Confirm the tip via `git log --oneline -5` (HEAD is the docs-only next-prompt roll-forward sitting on the substantive phase-40.3 PLAN squash `42bcf63`, whose parent is the phase-40.3 SPEC squash `154165f`; the prior substantive tips are the phase-40.2 IMPL `4aed3e1` and the phase-40.2 PLAN `104daf2`). Counts at the 40.3 PLAN (UNCHANGED — PLAN is docs-only): stat surface **1141** / fixtures **73** / fuzzers **42** / BackendKind tail **35** (`HTTP503Responder`) / DECISIONS tail **ADR-0246**, next-free **ADR-0247**.

## What just landed (phase 40.3 PLAN — the durable record is `docs/envoy-go/phases/40.3-outlier-detection-statistical/{PLAN,SPEC,README}.md`; read those, not this summary)

The PLAN (`superpowers:writing-plans`) decomposes SPEC §10 into **13 bite-sized TDD tasks** and resolves the §12 D-S40.3-1..8 PLAN/IMPL questions. The FINAL ADR-0045 split-gate re-check → **NO FURTHER SPLIT** (~170–230 prod LoC / 13 tasks; single flat 40.3 leg consuming the 3-leg by-detector-class split). plan-document-reviewer APPROVE-WITH-MINORS (M1 the statistical-handle test fixture + m2 `outlierStatNames()` naming/anchors applied; the threshold arithmetic, the population-stddev pin, the cross-multiplied FP `>=` boundary, the SPEC faithfulness, the source citations [`record`/`tryEject`/`hostHealth`/`Drain`/`StartHealthChecks`/`main.go:274`/`loadbalancer.go:61`], and the concurrency model all VERIFIED-correct against source).

**The 13-task spine:** (1) baselines/PROGRESS; (2) `parseOutlierDetection` extension (the 8 statistical fields + `interval`-as-load-bearing + defaults + the 6 reject arms); (3) the windowed `(intervalTotal, intervalSuccess)` `atomic.Uint64` counters on `hostHealth` + the success/failure classification side-effect at the TOP of `record` (counts on EVERY path); (4) `evalSuccessRate` (eligible set + cross-host mean + **POPULATION stddev** + `mean−(stdev_factor/1000)×stddev` threshold + negative-threshold no-op + the minimum_hosts/request_volume gates) + the +2 SR handles + a `withSuccessRateHandles()` test helper; (5) `evalFailurePercentage` (the integer cross-multiplied `(total−success)*100 >= threshold*total` `>=` boundary) + the +2 FP handles + `withFailurePercentageHandles()`; (6) the per-interval `sweep()` (snapshot+Swap-reset + both evaluators in order; `tryEject` CAS once-only per host per sweep) + the sweep goroutine `run(ctx)` + `Manager.StartOutlierDetection`/`odCancel`/`odWG` + the `Drain` join + the `main.go` boot call (after `StartHealthChecks`) — `-race`-clean, no leak; (7) the +8 statistical stat registrations (extend `registerStats` + `outlierStatNames()`; 1141→1149; the 4 local-origin names registered logic-deferred-emit-0); (8) the `0072` success-rate fixture (K=5+1 via `PerHostBackendKind`+`HTTP503Responder`; consecutive detectors OFF; the single-sourced `reqVolume` volume-floor fix makes the detect-only `ejections_detected_failure_percentage >= 1` cross-assertion LIVE); (9) the `0073` failure-percentage fixture (`enforcing_success_rate: 0` ⇒ failure_percentage the sole enforcer); (10) `0072`/`0073` deliberate breaks (2 each) + 20-run flake; (11) full **75**-dir differential + six-gate (the AMEND-OD3-7 retro-invariant: `0069`/`0070`/`0071` UNCHANGED); (12) ADR-0247 body + BEHAVIOR_CONTRACT (1141→1149); (13) completion bundle + ROADMAP row 40 → `done`.

**The D-S40.3-1..8 resolutions (baked into the tasks):** (1) the six house reject strings (`enforcing_success_rate`/`enforcing_failure_percentage`/`failure_percentage_threshold`/`enforcing_local_origin_success_rate`/`enforcing_failure_percentage_local_origin > 100` + `max_ejection_time <= 0s`; NO `success_rate_stdev_factor` reject — 0 accepted); (2) `intervalTotal`/`intervalSuccess` `atomic.Uint64` Add/Swap-to-0 (the minimal race-clean form); (3) the window accumulation at the TOP of `record`, success = NOT localOriginErr AND NOT 5xx, counts on EVERY path; (4) `StartOutlierDetection` a sibling of `StartHealthChecks` with parallel `odCancel`/`odWG`, one goroutine per outlier cluster (no immediate t=0 sweep), `Drain` joins both, boot call at `main.go:274`; (5) the `0072`/`0073` constants single-sourced — K=5 healthy + 1 bad ⇒ threshold ≈0.125 > 0, `minimum_hosts` 2, `reqVolume` 10 single-sourced across BOTH detectors' volume floors (the §8.1 vacuousness fix), `interval` 1s, `base_ejection_time` 30s, ports 19161/19162, the interval-window timing flake-gated not assumption-gated; (6) `0072`/`0073` as TWO dirs; (7) NO FURTHER SPLIT; (8) register all 8 statistical names incl. the 4 always-0 local-origin (confirmed no `0069`/`0070`/`0071` asserter break).

## Honor throughout (the IMPL discipline)

- **Lifecycle = one stage per session:** the IMPL (`superpowers:subagent-driven-development` — execute PLAN.md Tasks 1–13, a fresh subagent per task + two-stage review). `feedback_git_worktrees` · `feedback_subagents_no_push` (task subagents commit locally only; the controller squashes + pushes at stage-close) · `feedback_push_to_origin` (after a clean local merge on master with the full 75-dir differential green, push without asking) · `feedback_pertask_gofmt_lint` (each task runs gofmt -l + golangci-lint on touched pkgs).
- **ADR/process:** ADR-0044 (ADR-0247 §Context already drafted in SPEC §13; §Decision/§Consequences land IN-PLACE at Task 12) · ADR-0052 (the six-gate at Task 11) · ADR-0080 (byte-stable rejects) · ADR-0106 + `reference_roadmap_split_phase_row_done` (row 40 flips `done` at the 40.3 IMPL six-gate — the FINAL leg; NO parent rollup) · ADR-0227 (`contrib-v1.37.2`).
- **At the IMPL (forward):** `reference_differential_run_selector` (`-run 'TestDifferential/0072'`) · `reference_differential_break_protocol_count1` (`-count=1` on every break run) · `reference_health_check_propagation_warmup` (poll-`ejections_active`-to-converge + warmup; widen timing [lower `reqVolume` / raise `interval` / widen deadline] not assertions) · `reference_docker_probe_bridge_network` · `reference_fixture_workload_constant_desync` (single-source the workload constants incl. both volume floors) · `reference_differential_asserter_dispatch` (cross-side ⇒ `StatsAsserter`) · `reference_percent_cap_cross_multiply` (the reused cap + the FP `>=` boundary).

## Read first (in order)

1. THIS FILE — the post-phase-40.3-PLAN cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 40.3 (outlier-detection-statistical) PLAN done`; the count footer (1141 / 73 / 42 / 35 / ADR-0246, next-free ADR-0247); the lifecycle routing (next-skill = the 40.3 IMPL).
3. `docs/envoy-go/phases/40.3-outlier-detection-statistical/PLAN.md` (the 13-task TDD spine + the D-S40.3-1..8 resolutions + the per-task code/file-structure + the exit-delta table) + the SPEC.md (the §1.1 AMEND-OD3-1..7; the §3 architecture; the §5 proto roster; the §6 six reject arms; the §7 stat surface +8; the §8 `0072`/`0073` fixtures; the §11 D-OD3-* live pins; the §13 ADR-0247 §Context) + the README.
4. `internal/cluster/{outlier.go,health.go,manager.go,cluster.go}` (the `outlierConfig`/`parseOutlierDetection`/`outlierDetector`/`tryEject`/`record`/`registerStats` the IMPL extends; the `hostHealth` the windowed counters land on; the `StartHealthChecks`/`Drain`/`hcWG`/`hcCancel` lifecycle the sweep goroutine mirrors) + `internal/cluster/outlier_test.go` (the `newDetectorFixture` + `withGatewayHandles` test-helper precedent + `outlierStatNames()`) + `test/fixtures/0069-outlier-detection-consecutive-5xx/driver/` (the StatsAsserter + poll-to-converge + `PerHostBackendKind` template the `0072`/`0073` drivers reuse) + `cmd/envoy-go/main.go` (~line 274, the `StartHealthChecks` boot site).
5. `docs/envoy-go/DECISIONS.md` (ADR-0245 + ADR-0246 [the substrate] + the process ADRs) + `docs/envoy-go/BEHAVIOR_CONTRACT.md` (the `### Cluster — passive health (outlier detection)` subsection the 40.3 IMPL extends + the stat-surface block 1141 → 1149).
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`.
