=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-PLAN'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **PHASE 95 (`tls-alpn-mismatch-fallback`) IS OPEN AT LIFECYCLE-STATE 1. ROW 95 READS `in-progress` AT `ROADMAP.md:157`.** ⇒ **THIS SESSION RUNS THE PHASE-95 SPEC** (state 1 -> 2). Its nine owed items are at `docs/envoy-go/phases/95-tls-alpn-mismatch-fallback/BRAINSTORM.md` §10.

⚠️ **THE MARGIN IS TWO AGAIN.** Check (1) is NON-SILENT (`NOT DONE: row 95`, one line) and check (2) reads SIX. Check (3) is SILENT. ⚠️ **DELETING THE LAST DEFERRED-CANDIDATE LINE STILL ENDS THE PROJECT. DO NOT "TIDY" ONE.** NC-A and NC-B read **TWO** lines each at this tip (re-measured on both sides of the row-95 ADD). **MEASURE ALL THREE CHECKS AND ALL FOUR NCs YOURSELF BEFORE DOING ANYTHING ELSE.** A SPEC touches neither `ROADMAP.md` nor row 95's status, so every figure below should hold at your tip — VERIFY, do not inherit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. Parses FIELDS instead of
#     matching a pattern, and asserts the row DENOMINATOR so a deleted row is
#     itself an error.
#     ⚠️ want IS 127 and ROADMAP.md IS 245 lines at the phase-95 BRAINSTORM close.
awk -F'|' -v want=127 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **CHECK (3) IS SILENT, AND A SILENT CHECK IS INDISTINGUISHABLE FROM A BROKEN ONE — THE NCs ARE THE ONLY EVIDENCE.** Run all four EVERY SESSION:
```sh
# NC-A — doctor a row to `in-progress`. ⚠️ NOW READS *TWO* LINES (`NOT DONE: row 62` AND `NOT DONE: row 95`).
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md with want=127.

# NC-B — the DENOMINATOR, want=126 on the real file. ⚠️ NOW READS *TWO* LINES
#        (`NOT DONE: row 95` then `GATE FAIL: examined 127 data rows, expected 126`).

# NC-C — the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```
⚠️ **AND RUN THE CHECK-(2) POSITIVE CONTROL — AND READ THE COMMAND BEFORE TRUSTING ITS RESULT.** The longer phrase does not CONTAIN the shorter one as a substring, so a control that substitutes only one phrase reports a residual of 5 and reads like a finding. Substitute BOTH and assert the substitution count:
```sh
sed -e 's/deferred candidates:/deferred candidatesXX/g' \
    -e 's/remaining deferred (not-yet-chartered) candidates:/remaining deferred (not-yet-chartered) candidatesXX/g' \
    docs/envoy-go/ROADMAP.md > "$SCRATCH/c2pc.md"
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' "$SCRATCH/c2pc.md" | wc -l  # want 0
grep -o 'candidatesXX' "$SCRATCH/c2pc.md" | wc -l                                                                # want 6 — PROVE it landed
```

⚠️ **WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -c '-family row'` fails and the surrounding arithmetic prints `0`, **which reads exactly like "no change."** **Always pass `--` before the pattern**; with `--` occurrences read **96** and LINES read **68**. **`grep -c` counts LINES, NOT OCCURRENCES,** and **`grep -c` on zero matches PRINTS `0` *AND* EXITS 1, so `$(cmd || echo 0)` EMITS TWO ZEROS** — capture with `v=$(cmd || true)`.

⚠️ **THE MALFORMED-ROW FIGURE RECONCILES ONLY UNDER AN ESCAPE-AWARE FIELD COUNT — AND THE ESCAPE-AWARE COMMAND ITSELF CAN BE BROKEN.** Malformed rows are **ROW IDs 57** and **69** (file lines 119 and 131). **Strip `\|` BEFORE splitting.** Row 57 reads **naive NF=13 / escape-aware NF=9**; row 69 reads **10 / 10**; rows 94 and 95 read **8 / 8**; naive malformed count **17**, escape-aware **2**. ⚠️ **AT THE PHASE-95 BRAINSTORM THE ESCAPE-AWARE COMMAND WAS RUN AS `sed 's/\\|//g' F | awk … F` — WITH THE FILE ALSO PASSED AS AN ARGUMENT — so `awk` IGNORED STDIN and printed the NAIVE 17 under the escape-aware label.** It was caught only because the baseline (2) was known. **The correct form is `sed 's/\\|//g' F | awk -F'|' '/^\| *[0-9]/ && NF!=8'` with NO file argument to awk. Any gate must state WHICH FORM it uses and reconcile against the known baseline.**

⚠️ **AND DO NOT SUBSTITUTE A "REASONABLE EQUIVALENT" FOR CHECK (1)'s AWK.** `NF>=8 && $2 ~ /^ *[0-9]+ *$/` once printed **92**, not the true denominator. **Use the form above verbatim.**

**SENTINEL RUN MECHANICALLY AT THE PHASE-95 BRAINSTORM, ON BOTH SIDES OF THE ROW ADD.** ACTUAL output, recorded not predicted:
**BEFORE** (`want=126`, at `7964f620`) — (1) SILENT · (2) **SIX** at `:204 :210 :216 :226 :232 :240` · (3) SILENT · NC-A **ONE** · NC-B **ONE** · NC-C fired (0 residual) · NC-D **96 / 68** · check-(2) positive control **6 -> 0 with 6 substitutions**.
**AFTER** (`want=127`) — (1) `NOT DONE: row 95` ONE line · (2) **SIX** at `:205 :211 :217 :227 :233 :241` · (3) SILENT · NC-A ⚠️ **TWO** · NC-B ⚠️ **TWO** · NC-C fired (0 residual) · NC-D **96 / 68** · check-(2) positive control **6 -> 0 / 6**.
Per-line md5 of the six windows, **trailing newline INCLUDED** (`sed -n 'Np' f | md5sum`) — **ALL SIX BYTE-IDENTICAL to the phase-94 close** (only the LINE NUMBERS moved, by +1):
`205 10d7807bf02d` · `211 4a92f7e62fc6` · `217 2a7eb298b9fd` · `227 242e53c6f7a3` · `233 b2680e6f4fbf` · `241 6caa1c3ce0e7`
⇒ **THE SENTINEL DOES NOT FIRE. `stop` WAS EVALUATED AND DELIBERATELY NOT CREATED** (verified absent at the git root and in the stage worktree).
⚠️ **THE md5 DIGEST IS METHOD-SENSITIVE:** the six match ONLY with the **trailing newline included**. **State the method whenever you quote the digest.**
⚠️ **THIS RUN DOES NOT CARRY FORWARD — run all three checks, all four NCs *and* the check-(2) positive control YOURSELF, at YOUR OWN tip.**

⚠️⚠️ **NEVER RE-SPELL A SENTINEL MATCH PHRASE INSIDE A SENTINEL WINDOW** (the phase-94 rule: the sentence asserting the sentinel could not move MOVED IT, six -> seven, inside `:226`). A SPEC does not edit `ROADMAP.md`, so this rule is dormant for this session — but it applies to ANY prose you put on a window line, ever.

- ⚠️ **LIVE FIGURES RE-DERIVED AT THIS TIP:** `ROADMAP.md` **245** lines / **127** data rows / tail row **95** (`in-progress`) · `DECISIONS.md` **18872** · `^---$` **216** · `^## ADR-` **315** · bare `^## ` **323** · tail **ADR-0316** · next-free **ADR-0317** (`^## ADR-0317` reads **0**) · house `PROPOSED` **0 (DISARMED)** · ADR-0231 decoy **1** at `:14866` · `BEHAVIOR_CONTRACT.md` **5989** · `STATE.md` **65** (rolled IN PLACE) · `STATE_HISTORY.md` **548** (546 -> 548, +2), strict **163 (DELTA 0)** / parenthetical **61** / loose **224** (**163 + 61 = 224 exactly**) · phase dirs **136** · fixtures **122** (tail `0120-tls-connection-error`; **`0121` is the next free index — and THIS ROW DOES NOT TAKE IT**, it extends `0120`) · fuzzers **56 targets / 48 FILES** · `go.mod` **67 REQUIRE ENTRIES** (⚠️ the `grep -cE '^\s+[a-z0-9./-]+ v[0-9]'` form reads 62) · `go list ./...` **237** (**235** after excluding the two Docker drivers) · BackendKind tail **38** · `-family row` **96 / 68**.
- ⚠️ **THE STAT SURFACE IS QUOTED AS A DELTA AND NEVER AS AN ABSOLUTE.** Three different absolutes are live at one tip. **This row lands +0 names.** The one exception is the ledger's own internal chain at `BEHAVIOR_CONTRACT.md`'s `### Stat surface` (tail: the phase-94 entry) — check its convention before deciding whether a `+0` row needs an entry.
- ⚠️ do NOT "fix" the six in check (2) and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases; **no gate may rest on a ` + ` split** (wrong on 3 of the 6 windows).

==============================================

## STATUS AS OF 2026-09-06: PHASE 95 (`tls-alpn-mismatch-fallback`) **OPEN, lifecycle-state 1 (BRAINSTORM done)**. **THIS SESSION RUNS THE SPEC.**

**What the BRAINSTORM found — the subject in one paragraph.** When a downstream TLS client offers ALPN values that overlap NONE of the chain's `alpn_protocols`, the pinned reference (`contrib-v1.37.2`, digest verified) **COMPLETES the handshake with no protocol selected and SERVES the connection**: `ssl.handshake +1`, `ssl.connection_error +0`, HTTP 200 over HTTP/1.1 via the AUTO codec. envoy-go **ABORTS** with `no_application_protocol` and, since phase 94, **books `ssl.connection_error +1`**. Six arms per side, TLS-1.0 positive control per side; four arms agree cross-side, the two non-overlapping offers (`foo`, `foo,bar`) diverge on the connection outcome AND both counters. The tree records this as PARITY at `internal/listener/tls_handshake_negative_test.go:117-118`. **That comment is FALSE, and the test it heads (`…_ALPNNegotiationFailure_Aborts`, `t.Fatal` at `:169`) pins the WRONG side.**

**Cost floor, MEASURED by a built-run-reverted prototype:** ONE production file, `internal/tls/config.go`, `+31/-0` — a `GetConfigForClient` fallback that returns `cfg.Clone()` with `NextProtos = nil` when `hi.SupportedProtos` is non-empty and overlaps none of the server list. It matched the reference on all six arms. ⚠️ **PLACEMENT: at the ENTRY of `NewDownstreamConfig`, immediately after `commonTLSContextToConfig` returns (`config.go:53-56`). NOT at a `return`: the function has SIX return sites (`:129 :196 :221 :230 :249 :259`) and the first prototype, inserted before the FIRST, landed inside the SDS validate-mode early return, built, booted, and changed NOTHING.** QUIC (`NewQUICDownstreamConfig`, `:268`) is a separate builder and is NOT touched — RFC 9001 §8.1 mandates the alert there, Go enforces it (`handshake_server.go:336-338`), and `TestQUICListener_ALPNMismatch_RefusedAndListenerSurvives` pins it.

**Test-pin surface, measured with the prototype in place:** selector `ALPNNegotiationFailure|SSLConnectionErrorCounter|QUICListener_ALPNMismatch|TestNewManager_LiveHandshake` ran **11** tests, exactly **ONE** red (the `_Aborts` test). The six-row counter table, its stacked control and the QUIC test are green. ⚠️ **That is a SELECTOR run — a LOWER BOUND on red tests. The PLAN must run the package sweep.**

### ⚠️ WHAT THE SPEC OWES (from `BRAINSTORM.md` §10 — re-read it, this list is a pointer)

1. **Draft `ADR-0317`** — §Context only, `PROPOSED` in the **house form** `^> \*\*STATUS: PROPOSED` (reads 0 now; the ADR-0231 decoy at `:14866` reads 1). ⚠️ **Do NOT quote a count for either form in prose the grep matches.**
2. **The rule as code** — entry-point install, TCP-only, no-op on empty/overlapping offers, clone with `NextProtos = nil` otherwise; decide per-chain pre-build vs per-handshake `Clone()` and say why.
3. **FLIP the `_Aborts` test** — rename, invert, hoist the inline `stats.NewRegistry()` at `:130` into a local, assert `NegotiatedProtocol == ""`, `ssl.connection_error == 0`, `ssl.handshake == 2`. **The tip is the NC.** Add a SEVENTH row to `TestServeConnection_SSLConnectionErrorCounter` (`manager_test.go:5209`) via an ALPN-capable listener helper (none exists; `mkDownstreamTSInlineALPN` at `tls_handshake_negative_test.go:88` is reusable) — ⚠️ **AND a CONTROL row for the `http/1.1`-vs-`h2`-only shape, which Go's `http11fallback` (`handshake_server.go:342-358`) ALREADY lets through.** A fix that "works" only there is the stdlib, not the row.
4. **`0120` arm (vi)** — valid client cert + ALPN offer `["bogus/9"]` against a chain that gains `alpn_protocols` on BOTH YAMLs; expected `connection_error +0 / handshake +1` both sides after the fix, `+1 / +0` on the subject at the tip. ⚠️ **`driver.go:73-92` says its pins are ARM ARITHMETIC — RE-MEASURE ALL SIX ARMS PER SIDE.** `+0` fixtures, `+0` BackendKinds, port `10126` unchanged.
5. **Extend `BEHAVIOR_CONTRACT.md:1944` WITHIN-LINE** (one paragraph line; it promised *"if a later phase asserts ALPN negotiation, it adds a fixture opt-in and extends this subsection"* — this is that phase). No departure to name: after the row there is none.
6. **SDS interplay** — reason about `Clone()` under a live provider in the ADR; record, do not solve (no fixture combines SDS and `alpn_protocols`).
7. **The full EDIT roster of `BRAINSTORM.md` §3.4**, set-differenced against any byte-untouched gate; correct the three comments (`manager.go:138`, `tls/config.go:25`, `tls/doc.go:5`) that describe a `GetConfigForClient` callback that does not exist — this row installs the FIRST real one.
8. **NC every new pin; neutralise, never revert.**
9. **Re-derive every count in `BRAINSTORM.md` §6.4 at the SPEC's own tip.**

### Banked, NOT chartered (carried forward with costs re-derived at the BRAINSTORM)

- ⚠️ **THE DRIVER-OWNED RECEIVER PORT RACE — the runner-up, REJECTED FOR SIZE, COST NOW MEASURED: ~36 driver files** (30 `panic` the binary via `panic(fmt.Sprintf("driver: start …`; four `fmt.Errorf` variants degrade to one failing subtest). Idiom: `net.Listen("tcp","127.0.0.1:0")` → read → `Close()` → re-bind `0.0.0.0:%d`, inside `ip_local_port_range` `32768 60999`, loopback probe for a wildcard bind, no retry (`harness_test.go:240-244` predicts it). Two fix shapes: a banded allocator in `test/differential/fixture/` + a 2-line swap per driver, or bind `0.0.0.0:0` and read the port back from each helper's `Addr()` (every helper has one; every template already substitutes the port). ⚠️ **The memory note `reference_driver_receiver_port_race_aborts_binary` said FOURTEEN — corrected to ~36 at this close.** **The most defensible NEXT pick if a full differential run aborts again.**
- `0108`'s two *"emits NO `ssl.*` stats whatsoever"* confessions (false since phase 74) · no `len(helpText)` guard + two ungated prose counts in `internal/stats/name.go` · `0118:31`'s falsified *"TLS/SDS band"* · `0061-lb-ring-hash`'s σ-margin second occurrence · 1xx interim responses · the H/1 no-`Host` divergence · the pooled-upstream-lifetime defect · the other TEN fixed `ssl.*` names and FOUR dynamic families (blocked on NAMING).

## ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + ALL FOUR NCs + THE CHECK-(2) POSITIVE CONTROL YOURSELF** and record ACTUAL output. ⚠️ **NC SHAPES CHANGE ACROSS A ROW FLIP OR ADD — NEVER INHERIT ONE.** A SPEC changes none of them; PROVE that rather than assume it.
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** 93 IMPL nine · 94 BRAINSTORM eleven · 94 SPEC twelve · 94 PLAN sixteen · 94 IMPL twelve · **95 BRAINSTORM NINE**, two of them against the router's own candidate description. ⚠️ **AND WHEN TWO AGENTS CONTRADICT EACH OTHER, FIND THE VARIABLE RATHER THAN PICKING A WINNER** — at phase 95 a memory note (14) and a fresh enumeration (36) disagreed on the same defect; the variable was *enumerate by ONE helper's name* vs *enumerate by the defect's SHAPE*. Both reported truthfully about different things.
3. **LINE ANCHORS DRIFT; SYMBOL AND LITERAL-TEXT ANCHORS DO NOT — RE-VERIFY EVERY CITE AT YOUR TIP.** ⚠️ **PATHSPEC-SCOPE every symbol assertion and every `sed`.** ⚠️ **`internal/filter/hcm/chain.go` DOES NOT EXIST — it is `internal/filter/http/chain.go`** — but **`actions.go` IS in `hcm`.**
3b. ⚠️ **CHARACTER-CLASS AND ESCAPE SELECTORS ARE THE FAIL-UNSAFE FAMILY.** `^[0-9]{4}-` on fixtures reads 120, dropping `0007a`/`0007b` (use `ls -d test/fixtures/*/ | wc -l`) · harness `grep` is ugrep 7.8.4, `/usr/bin/grep` is GNU 3.11 — **NAME THE TOOL** · **a CASE-SENSITIVE GREP IS BLIND TO UPPERCASE PROSE — use `-i` when auditing comments.**
3c. ⚠️ **`feedback_brief_citations_not_evidence` APPLIES TO INCIDENTAL FIGURES — including every number in THIS file and every number in an AGENT REPORT.** At phase 95 an agent stated next-free ADR *"in the ADR-0292 area"*; it is **ADR-0317**.
3d. ⚠️ **NO GATE MAY REST ON A ` + ` SPLIT.**
3e. ⚠️ **A GATE COMMAND CAN MATCH PROSE INSTEAD OF CODE, AND IT READS LIKE A REAL FINDING.** Anchor fixture-set extraction on the blank-import LINE:
```sh
extract () { grep -oE '^[[:space:]]*_ "github\.com/pgdad/envoy-go/test/fixtures/[^/]+/(driver|inputs)"$' "$1" \
  | sed -E 's#.*/test/fixtures/##; s#/(driver|inputs)"$##' | sort; }
```
**At this tip: dirs 122 = imports 122, both `comm` directions EMPTY**, layout split **98 `driver/` + 24 `inputs/`**. **NC the extractor** (rename one import).
3f. ⚠️ **"AT THE RETURN" IS NOT A PLACEMENT.** A function with six exits has no "the return". Anchor installs on the ENTRY of the path that must be covered and PROVE the install with an arm that reddens without it (phase 95 §0.4, §9.2).
3g. ⚠️ **A "CHEAP ASSERTION GAP" CAN BE A BEHAVIOUR DIVERGENCE WEARING A DISGUISE.** An unasserted counter increment is a coverage gap ONLY if the increment is correct. **Measure the reference for THAT ARM before asserting any cross-side stat** — the banked candidate 1 would have pinned a divergence as parity.
3h. ⚠️ **THE STDLIB CAN COINCIDENTALLY MATCH THE REFERENCE ON ONE SHAPE.** Go's `http11fallback` makes exactly one non-overlapping ALPN shape succeed today; a table with only that shape proves nothing (`reference_coincidental_method_agreement`).
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Live: TWO SDS dial-budget flakes plus `TestSDSEndToEnd_FetchFailure_BootFailsClosed` (recurred at the 94 IMPL) · the driver-owned receiver port race (twice at close range; aborted a full run at the 94 IMPL) · `internal/httpclient` zero-value · the two 84.2-era flakes · the REFERENCE h2spec section-8 flip · `TestOutlierDetector_ConcurrentEjectExactlyOnce` · `0061-lb-ring-hash`'s σ-margin · `TestP83_StopPauseTimer_IsAuthoritativeAgainstAnEnteredClosure` (wasm, 1/400). ⚠️ **`TestServerConn_TinyWindowDelivery` IS NOT A FLAKE** — a recurrence is a REGRESSION of row 91. ⚠️ **A GREEN RERUN CLEARS NOTHING.**
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only). ⚠️ **AT THE PHASE-95 BRAINSTORM A SIBLING `curl-world` SESSION OWNED SEVENTEEN CONTAINERS holding `18080-19000`** and was present throughout. ⚠️ **A `reaper_*` testcontainers Ryuk container is created BY THE DIFFERENTIAL ITSELF and is REUSED; leave it alone.** **Treat NO roster as stable.**
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`. ⚠️ **`INNER_EXIT` DOES NOT EXIST in this repo.**
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL, THEN DRIVE THE ARM.** At phase 95 a prototype built, booted and served with its callback never installed. **`gofmt -l` never exits non-zero — gate on OUTPUT.** ⚠️ **go test WITHOUT `-v` PRINTS ZERO `=== RUN`** — `RUN=0` beside `RC=0` is a VACUOUS GREEN. ⚠️ **A `-run` selector matching nothing prints `[no tests to run]` and EXITS 0.** ⚠️ **On `-v` output, unanchored `grep -c 'FAIL'` reads nonzero ON A FULLY GREEN TREE** — use `grep -cE '^(FAIL|--- FAIL)|^ *--- FAIL'`.
7b. ⚠️ **`grep -c` ON ZERO MATCHES PRINTS `0` AND EXITS 1** — capture, NEVER chain with `&&`, NEVER `$(… || echo 0)`.
7c. ⚠️ **A GREEN TEST RUN IS NOT EVIDENCE A SITE IS EXERCISED** — prove it with a **`panic()` REACHABILITY CONTROL**.
7d. ⚠️ **NC EVERY NEW TEST, AND NC YOUR OWN FIXES** — NEUTRALISE so the package still compiles; check the NC is EXECUTABLE (phase 94's NC 9 was self-contradictory and had to be split).
7e. ⚠️ **A UNIT TABLE OVER A SHARED ASSERTION IS NOT A GATE ON PRODUCTION BEHAVIOUR** — `t.Helper()` gives each failure a DISTINCT call-site line.
7f. ⚠️ **A NC THAT LEAVES YOUR NEGATIVE CONTROL GREEN IS NOT EVIDENCE THAT CONTROL DOES ANY WORK** — you need a SECOND, ISOLATING NC, and the control cell is worth running even when the PLAN does not ask.
7g. ⚠️ **ASSERT *WHICH* ERROR FIRED, AND ALSO WHICH DID NOT.** ⚠️ **KEEP TABLE ROWS SINGLE-CAUSE.** ⚠️ **PREFER AN EXACT EQUALITY TO A FLOOR.**
7h. ⚠️ **A PROBE CLIENT CAN WITHHOLD THE VERY THING THE ARM EXISTS TO TEST** — force-send and PRINT what was sent. ⚠️ **`openssl s_client` answers "did the handshake complete", NOT "was the connection served"** — at phase 95 a Go probe that sent `GET /` after the handshake is what turned "no alert" into "200 OK over HTTP/1.1". ⚠️ **A TEST TABLE CAN CARRY AN INVENTED INPUT NO PRODUCTION PATH PRODUCES** — build on PRODUCTION-REPRESENTATIVE values.
7i. ⚠️ **A GATE THAT READS 0 HAS NOT BEEN SHOWN TO WORK.** Run every silent gate over an input KNOWN to trip it.
8. ⚠️ **`pgrep -f` / `pkill -f` MATCHES YOUR OWN SHELL AND KILLS THE TOOL CALL (exit 144)**. **Kill ONLY PIDs you captured yourself at launch.** ⚠️ **`pgrep -la a b` ERRORS (*"only one pattern can be provided"*)** — one pattern per call.
9. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-*`, `wt-*` or stage worktree should outlive its stage close** — expect only `master` (verify with `git worktree list`).
10. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** (a) differential — **122/122 expected**; (b) non-Docker sweep gated on `PIPESTATUS[0]` + a SET RECONCILIATION — **235 packages**; (c) h2spec `95 tests, 94 passed, 1 skipped, 0 failed`; (d) fuzzers **56 / 48**; (e) the ANCHORED panic gate `^panic:|DATA RACE|SIGSEGV`, **0** *and PROVEN LIVE*; (f) no REVIEW.md — standing departure. ⚠️ **`-race` on the DIFFERENTIAL suite is VACUOUS** — the subject is an unraced subprocess. A SPEC runs none of these; it names them.
11. ⚠️ **THE DIFFERENTIAL'S FAILURE MODE IS A SILENT PASS.** `-count=1` IS NOT OPTIONAL (the subject is a SUBPROCESS; the cache serves a stale PASS). **ASSERT THE FIXTURE SET BY NAME, BOTH DIRECTIONS, AGAINST THE FIXTURES ON DISK.** The full suite takes ~400s.
11b. ⚠️ **`go test ./...` DRIVES DOCKER — AND THERE ARE TWO DOCKER DRIVERS.** Exclude BOTH: `go list ./... | grep -vE '/test/differential$|/test/conformance/h2spec$'`.
12. ⚠️ **`-p` PUBLISHING WORKS; `--network host` IS WHAT DOES NOT.** ⚠️ **`STRICT_DNS` + `host.docker.internal` + `--add-host=host.docker.internal:host-gateway` IS REQUIRED for a container-to-host cluster HERE** (VM-backed Docker; `docker0` DOES NOT EXIST on the host). ⚠️ **THE REFERENCE CONTAINER CANNOT READ `filename:` CERT PATHS** — use `inline_string:` — ⚠️ **AND INDENT THE PEM ONE LEVEL DEEPER THAN `inline_string:`**: at phase 95 a PEM indented shallower than its key ended the block scalar early and the reference reported `yaml-cpp: … illegal map value` at a line INSIDE the PEM. ⚠️ **A PROBE BACKEND MUST BIND `0.0.0.0`, NOT `127.0.0.1`.** ⚠️ **Keep PEM substitutions OUT of YAML comments.** ⚠️ **envoy-go REJECTS `match.headers`**, **refuses `codec_type: HTTP2` without TLS unless `--allow-h2c`**, **boots with `-c`, NOT `--config-path`**, **`access_log[].log_format` is BOOT-REJECTED**, **`TLSv1_0`/`TLSv1_1` in `tls_params` BOOT-REJECT** (`internal/tls/params.go`) — a bad-version arm is produced CLIENT-side. ⚠️ **AN OMITTED `clusters` KEY BOOT-REJECTS envoy-go** (`cluster manager: cluster: zero clusters in bootstrap`, exit 1) — measured at phase 95 with the key ABSENT; the inherited *"`clusters: []` did not reproduce"* note concerns the EMPTY-LIST form, a DIFFERENT input. Give a probe subject a placeholder STATIC cluster. ⚠️ **`ENVOY_TARGET.md` is at `docs/envoy-go/ENVOY_TARGET.md`, NOT the repo root.** ⚠️ **The pinned reference is `envoyproxy/envoy:contrib-v1.37.2`, digest `7edd5b0fd763…` — PIN BY DIGEST, verify against `ENVOY_TARGET.md:4`.**
13. ⚠️ **A RAW-FRAMER PEER MUST HOLD ONE `hpack.Decoder` PER CONNECTION.** ⚠️ **`0004` is TLS+ALPN-h2, NOT h2c — and it is the ONLY fixture with downstream `alpn_protocols`, all on the overlapping success path.**
14. ⚠️ **`bash /dev/tcp` + `timeout cat <&3` INSIDE COMMAND SUBSTITUTION RETURNS SILENTLY EMPTY** — use Go for socket probes (a 40-line `crypto/tls` dialer answered every phase-95 arm).
15. ⚠️ **GO DOES NOT FLAG UNUSED CONSTANTS.** Post-removal cleanup is grep-driven.
16. ⚠️ **`golangci-lint`'s misspell runs in locale US.** **Sweep British spellings in `.go` comments BEFORE the gate.** Markdown prose may use them freely.
17. ⚠️ **A GREEN RUN OF A CONTENTION-SENSITIVE TEST PROVES NOTHING.**
18. ⚠️ **AN ABSENCE CLAIM NEEDS A POSITIVE CONTROL AND A DISCRIMINATING NEGATIVE CONTROL.**
19. ⚠️ **DERIVE next-free ADR FROM THE TAIL, NEVER FROM THE HEADING COUNT.** The id space is SPARSE (exactly one gap, `0209`) and **headings+1 reads 316 — a TAKEN id.**
20. ⚠️ **`reference_measured_prototype_is_a_lower_bound` HAS FIRED ELEVEN CONSECUTIVE ROWS, ALWAYS BY UNDER-ENUMERATING *FILES*, *SITES* OR *SYMBOLS*.** The phase-95 pin surface (§6.2) is a SELECTOR run and therefore a floor. **ENUMERATE BY GREPPING THE LITERAL CLAIM TEXT OUTWARD FROM ANY PINNED MAP AND BY COMPILING.**
21. ⚠️ **A MEASUREMENT THAT GOVERNS A DEFERRAL DECISION MUST LAND IN A GOVERNING DOCUMENT** (`ADR-0316 §Context ¶2`). The phase-95 reference measurement (§2) MUST reach `ADR-0317` and `BEHAVIOR_CONTRACT.md:1944` at the SPEC — do not let it die in the BRAINSTORM.
22. ⚠️ **A STAGE'S FILE SCOPE IS ITSELF MEASURABLE.** `git show --numstat` on the precedent stage settles it. **For a SPEC, measure against the phase-94 SPEC commit `307f2e3d`** — a SPEC touches `DECISIONS.md` (the new ADR §Context) and its phase dir; it does NOT touch `ROADMAP.md` or `BEHAVIOR_CONTRACT.md` (both BYTE-UNTOUCHED at the 94 SPEC — verify with `git show --numstat 307f2e3d`).
23. ⚠️ **`git diff --stat` IS A SUM, NOT ADDITIONS.** Cost figures come from `--numstat`.
24. ⚠️ **AN UNESCAPED `|` IN A ROADMAP ROW PASSES CHECK (1) AND SILENTLY BREAKS THE FIELD COUNT.** Reword a pipe AWAY rather than escaping it. Dormant for a SPEC.

## ⚠️ THE `PROPOSED` GUARD — CURRENTLY DISARMED; THIS SPEC RE-ARMS IT

| form | now | where |
|---|---|---|
| `^> \*\*STATUS: PROPOSED` — **the house form** | ⚠️ **DISARMED (reads 0)** | disarmed at the phase-94 IMPL; **this SPEC re-arms it 0 -> 1 by drafting ADR-0317 §Context** |
| `^\*\*Status:\*\* PROPOSED` — **the ADR-0231 decoy** | unchanged, reads 1 | `:14866` under `## ADR-0231`, byte-untouched |

⚠️ **VERIFY BY LINE AND BY ADR, NEVER BY THE COUNT ALONE** — resolve any hit to its enclosing ADR by a BACKWARD heading search (`awk 'NR<=<HITLINE> && /^## ADR-/ {h=$0; n=NR} END {print n, h}'`). ⚠️ **NEVER gate on the UNANCHORED form nor the middle-ground `^\*\*Status:\*\*.*PROPOSED` form.** ⚠️ **AND DO NOT QUOTE A COUNT FOR EITHER FORM IN PROSE THE GREP MATCHES** — the phase-93 SPEC falsified itself doing exactly that. **The ADR-0316 precedent: §Context drafted at the SPEC after the RETAINED italic footer convention; no `---` separator, so `^---$` STAYS 216 and `^## ADR-` goes 315 -> 316.**

## ⚠️ THE ARCHIVE GUARD

⚠️ **THE STRICT `163` IS NOT THE ENTRY COUNT.** Re-measured at this tip: loose **224** = strict **163** + parenthetical **61**, exactly — **the triple reconciles only under these ANCHORED-OCCURRENCE forms:**
```sh
H=docs/envoy-go/STATE_HISTORY.md
grep -oE '^- \*\*prior active-phase:\*\* ' $H | wc -l   # strict         163
grep -oE '^- \*\*prior active-phase \('    $H | wc -l   # parenthetical   61
grep -oE '^- \*\*prior active-phase'       $H | wc -l   # loose          224  = 163 + 61
```
⚠️ The unanchored `grep -c 'prior active-phase'` counts LINES and coincides by accident; the colon form `grep -c 'prior active-phase:'` is a FOURTH regex. **Name the form or the figure is meaningless.** ⚠️ **`grep -F` WITH A PATTERN STARTING `- ` HITS THE FLAG TRAP** — pass `--` first. **The guard is SOUND as a DELTA-0 SHAPE check** (a correctly-shaped parenthetical append moves the STRICT form by 0 — verified at this close) **but the number is a SUBSET.**

⚠️ **THE ARCHIVE'S POSITIVE CONTROLS ARE SELF-INCREMENTING.** This stage's archive line names NO control figure. ⚠️ **The eviction check:** the DISCRIMINATING form is the strict `^- \*\*prior active-phase:\*\* \`<label>\`` form; **at this close BOTH the strict and the naive form read 0 on `STATE.md`** — the preamble describes the evictee as *"the PLAN stage of the phase TWO before the current one"* without spelling its label. **Do that.**

⚠️ **THE DATE SHAPE REPEATED FOR THE FIRST TIME IN SIX CLOSES — AND A REPEAT IS NOT A RULE.** A DIRECT DATE READ at this close gave `2026-09-03`, `2026-09-02`, `2026-09-01` x2, `2026-08-31`: **the oldest is UNIQUE and is ALSO the tail entry**, the tie at `2026-09-01` is NOT at the oldest position. Same shape as the phase-94 IMPL close. **INHERIT NO SHAPE; READ THE DATES YOURSELF.** A SPEC close evicts the phase-93 IMPL entry (`2026-09-01`) — ⚠️ **which SHARES its date with the phase-94 BRAINSTORM entry; whether that tie is AT THE OLDEST POSITION depends on what this session appends — READ, DO NOT PREDICT.**

## ⚠️ PORT BANDING

`net.ipv4.ip_local_port_range = 32768 60999`. ⇒ **BAND YOUR PROBE PORTS BELOW 32768.** ⚠️ **The differential harness reserves `20000..31007` (`harness_test.go:292-296`) and `11000..14999` (`:209-212`)**, so `12000-19000` is the ad-hoc band — ⚠️ **minus `18080-19000`, held by the sibling `curl-world` containers; phase 95 used `13100/13101` and `13110/13111`.** **Check availability with `ss -tan` (ALL states), never `ss -ltn`.** ⚠️ **FOR A UNIT TABLE, USE PORT 0** — `connPair` and `startOneWayTLSListener` bind `127.0.0.1:0`. ⚠️ **FIXTURE ports follow the `10<index>` family band; `0120` holds `10126` and THIS ROW KEEPS IT.** ⚠️ **The driver-owned receiver band sits INSIDE the ephemeral range** — see the banked runner-up above.

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function** (ugrep 7.8.4, `--ignore-files`, honours `.gitignore`); `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. `command grep -rl …` (GNU) sees it; direct-path access works. ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files).

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 95 (tls-alpn-mismatch-fallback)' --format='%h %s'   # the SLUG form
git log --grep '^phase 95' --format='%h %s'                                # the LOOSE form — RUN IT to CROSS-CHECK
```
⚠️ **RUN BOTH FORMS AND RECONCILE THE SETS. NO COUNT IS QUOTED HERE, DELIBERATELY — BOTH FORMS ROT.** **Take the tip from `git rev-parse master`.** ⚠️ **AND WHEN YOU LAND A CORRECTION, PUT THE FULL SLUG IN ITS SUBJECT.**

Execution style: **subagent-driven per `feedback_execution_style`**. Subagents commit LOCALLY on their own stage branch with EXPLICIT PATHSPECS; the controller merges and squashes (`feedback_subagents_no_push`, `feedback_push_to_origin`). **Pure MEASUREMENT agents should commit NOTHING and prove their tree clean.** ⚠️ **PARALLEL AGENTS IN ONE WORKTREE RACE THE GIT INDEX** — per-agent worktrees on their own branches, private scratch dirs, **Docker serialized to exactly ONE agent at a time**. ⚠️ **VERIFY WHAT A SUBAGENT REPORTS — BUT ALSO BELIEVE IT WHEN IT REFUTES YOU, AND ARBITRATE BY MEASURING WHEN TWO OF THEM REFUTE EACH OTHER.** ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root** — build with `-o` into scratch.

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root — **it fired on every backgrounded launch at the phase-95 BRAINSTORM.** Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` before any commit. ⚠️ **Shell VARIABLES do not survive between Bash calls** — re-export in every command. ⚠️ **AN UNQUOTED HEREDOC (`<<EOF`) COMMAND-SUBSTITUTES BACKTICKED TEXT** — use `<<'EOF'`.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE OR PROTOTYPE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — `sha256sum` BEFORE patching, `git checkout -- <path>` after, `sha256sum -c` the capture — ⚠️ **and COMMIT FIRST if anything else is pending, because `git checkout --` restores from HEAD.** ⚠️ **Also check the MAIN repo root, and `--untracked-files=all`.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads RUN THE PHASE-95 SPEC.** ⚠️ **`grep -c 'next-skill:'` reads 2: one is the ADR-0288 RULE STATEMENT matching its own grep, and the live pointer is the other.** ⚠️ **§Project counts is a PHASE-76-ERA SNAPSHOT and SAYS SO.**
2. `docs/envoy-go/phases/95-tls-alpn-mismatch-fallback/BRAINSTORM.md` — **314 lines**. §0 the nine refutations; §2 the two-sided measurement and the RULE (§2.3); §3 the mechanism, placement and the §3.4 EDIT ROSTER; §6 the prototype and the pin surface; §10 what the SPEC owes; §9.5 the three stale `GetConfigForClient` comments.
3. `docs/envoy-go/ROADMAP.md:157` — row 95's registration narrative (the pick and the rejected runner-up).
4. `docs/envoy-go/phases/94-tls-connection-error-stat/SPEC.md` — the precedent SPEC shape (500 lines; §2.2 the six-row unit table this row extends; §11 the pinned edit map that UNDER-ENUMERATED two sites — do better by grepping the literal claim text outward).
5. `docs/envoy-go/DECISIONS.md` **ADR-0316** — the phase-94 decision; **§Consequences (vi)** at `:18862` frames the ALPN arm as a coverage gap — RETAIN verbatim (append-only), and let ADR-0317 §Context correct the framing.
6. `docs/envoy-go/BEHAVIOR_CONTRACT.md:1944` — the ALPN paragraph this row extends WITHIN-LINE (one enormous line; `sed`-style line replacement destroys neighbouring propositions). `:1961` is the ALPN-consumer paragraph (ADR-0050 / ADR-0083) and should need no edit.
7. `internal/tls/config.go:42-60` (`NewDownstreamConfig` entry), `:268` (`NewQUICDownstreamConfig`, untouched), `:580` (`NextProtos` append); `internal/listener/manager.go:1337` (the handshake site), `:1352-1359` (the phase-94 Inc); `internal/listener/tls_handshake_negative_test.go:88-170`; `internal/listener/manager_test.go:4754` (`startOneWayTLSListener`) and `:5209` (the counter table); `test/fixtures/0120-tls-connection-error/driver/driver.go:73-92`.
8. ⚠️ **`BOOTSTRAP_PROMPT.md` IS AT THE REPO ROOT** (`git ls-files | grep -i bootstrap` is the discriminating check). §5 the lifecycle state machine (appears VERBATIM TWICE, §5 and §11 — the second is a DUPLICATE); §6.1 the split-gate trigger (~25 tasks OR ~1500 net lines).

## What the next session owes
**RUN THE PHASE-95 SPEC** (state 1 -> 2) — ⚠️ **ONE STAGE PER SESSION.** Draft **ADR-0317 §Context** (house `PROPOSED` form, re-arming the guard 0 -> 1, verified BY LINE AND BY ADR) · write the rule as code and the entry-point placement (§10 items 2-3) · design the test flip, the seventh table row AND the `http/1.1`-vs-`h2` control row · design `0120` arm (vi) with ALL SIX arms re-measured per side · extend `BEHAVIOR_CONTRACT.md:1944` WITHIN-LINE · the §3.4 edit roster set-differenced against byte-untouched gates · **RE-RUN the sentinel + all four NCs + the check-(2) positive control yourself** (a SPEC moves none of them — PROVE it) · **REFUTE YOUR PREDECESSOR BY EXECUTION** and record it · `STATE.md` rolled IN PLACE with the oldest §Recent entry evicted by a **DIRECT DATE READ** (⚠️ a tie at `2026-09-01` exists — check whether it lands AT the oldest position after your append) and archived to `STATE_HISTORY.md` as **ONE INLINE LINE** in the PARENTHETICAL form, strict guard **163, DELTA 0** · **ROLL THE §Recent PREAMBLE SENTENCE TOO, without spelling the evictee's label** · **NAME NO POSITIVE-CONTROL FIGURE IN THE ARCHIVE LINE** · `next-prompt.txt` rolled (`git add -f`) · ONE squashed commit, merged and pushed — ⚠️ **and if a late measurement invalidates something you already pushed, land a CLEARLY-LABELLED correction commit (WITH THE FULL SLUG IN ITS SUBJECT) rather than force-pushing or leaving it standing.**
