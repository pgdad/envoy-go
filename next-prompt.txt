=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

⚠️ **PHASE 89 (`h2-decode-filter-mutations`) IS OPEN AT LIFECYCLE-STATE 1 — ROW 89 IS `in-progress` AT `ROADMAP.md:151`.** `want` IS **121**. ⇒ **THIS SESSION PERFORMS THE PHASE-89 SPEC.** This is BANKED mid-lifecycle work: per the standing directive, advancing it OUTRANKS opening a new subject. **DO NOT self-pick.** ⚠️ **THE SENTINEL DOES NOT FIRE AT YOUR START — check (1) PRINTS `NOT DONE: row 89` (it is no longer silent, because a row is genuinely in-progress), check (2) prints SIX, check (3) is SILENT. DO NOT create `stop`.** ⚠️ **MEASURE IT YOURSELF BEFORE DOING ANYTHING ELSE; do not take the previous stage's word for it.**

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=121 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **`want` IS 121 AT THIS TIP AND A SPEC MOVES IT ZERO.** A SPEC adds no ROADMAP row; row 89 STAYS `in-progress` and `ROADMAP.md` should end the stage **BYTE-UNTOUCHED** (prove it with an EMPTY DIFF against master, not by inspection). Only a BRAINSTORM that opens a row moves `want`.

⚠️ **THE DOCTORING NCs REMAIN MANDATORY EVERY SESSION.** Both:
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
# then run check (1) on $SCRATCH/nc.md. Expect "NOT DONE: row 62" AND "NOT DONE: row 89"
# at this tip (row 89 is in-progress). ⚠️ CHECK (1) HAS ITS OWN LIVENESS BACK THIS ROW --
# it PRINTS in the healthy case -- but run the NC anyway: it is the only thing that
# distinguishes "row 62 is done" from "the doctoring silently failed to land".
```
```sh
# the MANDATORY check-(3) NC, because check (3) is silent
sed 's/gRPC-family row/gRPC-XXXXXX row/g' docs/envoy-go/ROADMAP.md > "$SCRATCH/c3nc.md"
grep -oi -- 'gRPC-family row' "$SCRATCH/c3nc.md" | wc -l          # NC LANDED? want 0
grep -qi -- 'gRPC-family row' "$SCRATCH/c3nc.md" || echo 'NEVER OPENED: gRPC   <- NC FIRED'
```

⚠️ **AND WATCH THE FLAG TRAP WHEN YOU COUNT `-family row`.** A leading `-` makes the pattern look like an OPTION: `grep -oiE '-family row'` fails with `grep: amily row: No such file or directory` and the surrounding arithmetic prints `base=0 now=0 delta=0`, **which reads exactly like "no change".** Observed live at FOUR phase-84 stages and again at the phase-88 PLAN. **Always pass `--` before the pattern**, and remember the counts are form-dependent: **95 occurrences vs 67 LINES** at this tip (`grep -c` counts LINES). **A gate that reads zero on BOTH sides is not evidence of invariance.** ⚠️ AND the carried **ARM-A {119, 131} figure binds ONLY with its escape-aware pipe-split command** (BRAINSTORM-86 §6) — ad-hoc field-count forms do NOT reproduce it (an `NF!=8` form reads **17**); do not "correct" the figure with a different form.

**SENTINEL RE-RUN MECHANICALLY ON BOTH SIDES OF THE PHASE-89 BRAINSTORM's ROADMAP EDIT (2026-08-15, base `125f0714`). IT DOES NOT FIRE; `stop` was NOT created** (verified absent at repo root AND stage worktree). ACTUAL output, recorded not predicted — **BEFORE (`want=120`):** input 238 lines / 120 data rows · (1) **SILENT** (the SEVENTH silent reading in project history) · (2) **SIX at `:198 :204 :210 :220 :226 :234`** · (3) **SILENT**. **AFTER (`want=121`):** 239 / 121 · (1) **`NOT DONE: row 89` ALONE** · (2) **SIX at `:199 :205 :211 :221 :227 :235`** · (3) **SILENT**. `ROADMAP.md` numstat **`1 0`** — strictly additive.
- ⚠️ **THE WINDOW ANCHORS MOVED +1 AND THAT WAS PREDICTED IN THE BRAINSTORM §5 AND THEN MEASURED, NEVER FORECAST.** The COUNT stays SIX and the CONTENT is untouched; only the line numbers shift. **Any citation of `:198 …` is now STALE — the current six are `:199 :205 :211 :221 :227 :235`.** Re-derive at your tip regardless.
- ⚠️ do NOT "fix" the six and do NOT forecast a decrease — history `0 -> 1 -> 3 -> 4 -> 5 -> 6` across ~40 phases; the candidate sentences are a WINDOW onto a larger deferred backlog, not an inventory of it (`reference_sentinel_matcher_string_self_clears`).
- **ALL FOUR NCs FIRED ON BOTH SIDES.** row-62 doctoring with `NC LANDED? [ in-progress ]` inspected FIRST ⇒ BEFORE `NOT DONE: row 62` ALONE, AFTER `NOT DONE: row 62` **AND** `NOT DONE: row 89`; denominator ⇒ BEFORE `want=119` gave `GATE FAIL: examined 120 data rows, expected 119` as its ONLY output, AFTER `want=120` gave `NOT DONE: row 89` **plus** `GATE FAIL: examined 121 data rows, expected 120`; check-(3) doctoring (residual **2 -> 0** confirmed on the doctored copy first) ⇒ `NEVER OPENED: gRPC` with WASM correctly silent; check-(2) one-arm counts **5** (long) / **1** (short), union **6** (a one-arm strip is NOT an NC for the union).
- Every leak axis INVARIANT on both sides: `-family row` **95 / 67** · `gRPC-family row` **2** · `Operational-tooling-family row` **3**.
- ⚠️ **Re-run all three checks and both doctoring NCs yourself, BOTH SIDES of any ROADMAP edit, and record what YOU see.**

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files …` (ugrep under the hood). **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Reproduced at TWENTY-SEVEN separate tips.
```sh
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort   # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                      # direct access WORKS
```
⚠️ **THE METHOD BINDS; carry no standing discrepancy figure and no standing absolute.** ⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING ARMS; print FILE NAMES, never a count.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** (git exempts TRACKED files; use `-v --no-index`, and `git status` is the discriminating check). ⚠️ **`command grep` DOES NOT SURVIVE `xargs`** — inside `xargs` use `git grep` or `/usr/bin/grep`. ⚠️ **`grep -c` ON ZERO MATCHES IS TOOL-SPECIFIC** (only `git grep -c` prints nothing; all exit 1 — capture the value, never chain on the exit code). ⚠️ **`grep -c` COUNTS *LINES*, NOT *OCCURRENCES*.** ⚠️ **`\t` IN GNU ERE IS A LITERAL `t` — use `-P`.** ⚠️ **A `git grep` PATHSPEC ENDING IN `/` READS A FAIL-UNSAFE ZERO** (shape 33) — use the shell-glob form. ⚠️ **AN UNANCHORED PANIC-GREP FALSE-FIRES 14 TIMES ON A FULLY-GREEN DIFFERENTIAL LOG** — **the canonical gate is ANCHORED: `^panic:|DATA RACE|SIGSEGV`. A gate that fires on a green log is worse than no gate.**

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`** with FOUR blind spots (an em-dash, a DOT in `http-1.1`, two LETTER-suffixed ids). ⚠️ **EXECUTED on scratch copies at the phase-77 PLAN: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** ⚠️ **DO NOT restore the regex form.** ⚠️ **NOTE THE POSTURE CHANGE THIS ROW: with row 89 `in-progress`, check (1) PRINTS in the healthy case, so it has its own liveness again — but that is TEMPORARY. The moment row 89 flips `done` the board is all-done and check (1) goes SILENT again, at which point the row-62 NC is once more the ONLY thing distinguishing a green board from a broken check.**

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

⚠️ **Run the union form, never one half of it.** ⚠️ **A ONE-ARM STRIP IS NOT AN NC** (6 -> 5 short / 6 -> 1 long; never 6 -> 0). ⚠️ **The documented trap's stated MECHANISM is refuted while the trap is real** — the inflation comes from ROADMAP table rows carrying BRAINSTORM prose. **Cite the command; do not re-inherit the explanation.** ⚠️ **The line numbers drift with edits — re-derive them; the current six are `:199 :205 :211 :221 :227 :235`.** All six are standalone family PROSE paragraphs (not rows).

⚠️ **THE INVENTORY IS ~59, NOT ~42 — CORRECTED AT THE PHASE-89 BRAINSTORM BY A FULL RE-READ.** The long-carried "~42" is the **SENTENCE-VISIBLE** subset only: 43 items by name-level count inside the six live `candidates:` sentences (42 if you collapse `/runtime` + `POST /runtime_modify` into the single clause the sentence writes). **Window `:221` (Observability) names SIXTEEN FURTHER un-chartered items OUTSIDE its live sentence.** **Cite ~42 only as "sentence-visible", never as the inventory.**

⚠️ **AND TWO WINDOW CANDIDATES ARE STALE — THE WINDOWS DO NOT SELF-CLEAN.** Window `:221` still lists (a) *"a pre-existing H2 `//`-path routing bug — `h2/stream.go:440` uses `url.Parse`"*, which **ROW 87 CLOSED** (`internal/filter/hcm/h2/stream.go:478` now calls `url.ParseRequestURI`), and (b) the **`/stats/prometheus` projection-completeness gap**, which **ROWS 79 AND 80 CONSUMED** (`ExtractTags` carries landed arms for `runtime.`/`access_logs.`/`tracing.` at `name.go:133-143` and `sds.` hoisting `envoy_xds_resource_name` at `:144-165`; `WriteProm` no longer bare-returns — it collects `skipped` and logs an aggregate at `prom.go:65`/`:82`). **A candidate's presence in a window is NOT evidence it is still open — re-derive at your tip before costing it.** Neither was edited (a window edit is sentinel-affecting).

⚠️ **THE FOUR PHASE-88 DEFERRED CANDIDATES live in ADR-0310 §Consequences (x) (`DECISIONS.md:18206`), NOT in a ROADMAP window** — stream-scoped header-overflow parity via a decode-and-discard drain · `max_request_headers_kb` config parity + the `http2.header_overflow` stat · `SETTINGS_MAX_HEADER_LIST_SIZE` advertisement · **decode-side filter mutations never reaching the H2 upstream request — THIS IS NOW ROW 89 AND IS NO LONGER DEFERRED.** ⚠️ **AND ADR-0310's FIFTH carried class — "a split header block on a stream being concurrently RESET" — IS REFUTED BY CODE READING at the phase-89 BRAINSTORM (§10.3): `checkFrameOrder` in the pinned `x/net@v0.34.0/http2/frame.go:543` makes any non-CONTINUATION frame a connection PROTOCOL_ERROR while a block is open and `AllowIllegalReads` is never set, so the interleaving is UNREACHABLE; the LOCAL-reset case is already handled deliberately at `client.go:604-615` and `conn.go:440`. It is NOT a defect. Do not carry it forward as "unprobed".**


==============================================


## STATUS AS OF 2026-08-15: PHASE 89 (`h2-decode-filter-mutations`) **OPEN AT LIFECYCLE-STATE 1** — row 89 `in-progress` at `ROADMAP.md:151`, `want` **121**, base `125f0714`. **THIS SESSION WRITES THE SPEC.**

**What the 89 BRAINSTORM landed (2026-08-15, base `125f0714`, branch `phase-89-h2-decode-filter-mutations-brainstorm`, ONE squashed commit).** Docs-only: `phases/89-h2-decode-filter-mutations/BRAINSTORM.md` + `PROGRESS.md` (new), `ROADMAP.md` (`numstat 1 0`), `STATE.md` (rolled in place, still 64 lines), `STATE_HISTORY.md` (`numstat 2 0`, 498 -> 500), `next-prompt.txt`. **ZERO production `.go`, ZERO test `.go`. `DECISIONS.md` and `BEHAVIOR_CONTRACT.md` BYTE-UNTOUCHED** — a BRAINSTORM adds no ADR.

**THE CHARTER: decode-side filter header mutations never reach the upstream request on the HTTP/2 downstream leg.** `internal/filter/hcm/h2dispatch.go:457` calls `rf.SetH2Request(h2req)` BEFORE `chain.RunDecodeHeaders(ctx, c.req.Header, …)` at `:518`; the router consumes that snapshot at `rf.RunAction(ctx)` (`:563`). ⚠️ **THE MECHANISM IS TWO INDEPENDENT CONTAINERS WITH NO WRITE-BACK, NOT VALUE-VS-POINTER** — `buildH2Request` (`h2/stream.go:331`) builds the ORDERED `[]hpack.HeaderField` the upstream is emitted from; `buildRequest` (`:389`) independently builds the `http.Header` MAP the chain mutates. The `SetH2Request` value receiver (`internal/filter/http/router/router.go:246`) is a SYMPTOM: an `http.Header` is a map and would mutate through a copy.

### ⚠️ METHOD NOTE — SUBAGENT-DRIVEN, AND THE RE-DERIVATION EARNED ITS KEEP TWICE

Three read-only sizing agents on ONE shared detached worktree, one reproduction agent on its own, the stage branch in a third; **none committed**. The controller re-derived every load-bearing claim by execution. **It corrected the sizing agent's value-vs-pointer framing to the true two-container mechanism**, and **it caught a malformed-row defect in its OWN row-89 text before it landed** (see trap 1). Do the same with everything below.

### ⚠️ SIX FINDINGS THE SPEC MUST NOT RE-LEARN

1. ⚠️ **A MARKDOWN TABLE ROW CANNOT CARRY AN UNESCAPED `|`, AND IT FAILS SILENTLY FOR CHECK (1).** The drafted row-89 summary held a grep alternation `` `h2dispatch|SetH2Request|…` ``; markdown read those pipes as CELL DELIMITERS and the row measured **`fields=10`** against a well-formed **8** (row 88 as the control) — a THIRD malformed row alongside the documented `{119, 131}` pair. **Check (1) would still have PASSED**, because the stray pipes fell after field 5. **Count fields BEFORE installing any ROADMAP row.**
2. ⚠️ **AN ENCODE-SIDE CONTROL DOES NOT PROVE THE DECODE CHAIN RAN.** A present response header is consistent with a decode chain that never executed. Only a later filter *observing* the mutation — the `[header_mutation, rbac(iff x-probe), router]` arm returning 200 while the backend saw nothing, **paired with a negative control returning 403** — establishes that the mutation exists and is dropped at the emit boundary. **A positive control on the wrong axis is not a control.**
3. ⚠️ **THE REPRODUCTION'S FIRST BACKEND WAS THE WRONG PROTOCOL AND THE FAILURE LOOKED EXACTLY LIKE THE DEFECT** — a plain `net/http` (H1-only) backend gave **502 / `hcm: h2: EOF` with ZERO backend requests**. **Assert the backend RECEIVED something before interpreting what it received.** (Cause: `doH2ClusterAction` -> `AcquireH2Stream` at `internal/filter/http/router/router_h2.go:120` goes upstream over H2 unconditionally, without consulting `Cluster.UseH2()` — the MIRROR of a candidate the gRPC window already names at `connection.go:467`. **Recorded as an observation, NOT chartered, and the reference side was NOT measured.**)
4. ⚠️ **TWO OF THREE CODECS ARE ALREADY CORRECT — AND THE H3 FACT WAS PRODUCED AT THE BRAINSTORM, NOT CARRIED.** H1 `connection.go:468`/`:571` and H3 `h3dispatch.go:217`/`:263` both hand the decode chain the SAME object. **Keep H3 explicitly OUT of charter.**
5. ⚠️ **THE CONTRACT HAS NO FALSE SENTENCE TO CORRECT — IT HAS AN HONEST CARVE-OUT.** `BEHAVIOR_CONTRACT.md`'s `header_mutation` row asserts equivalence on post-mutation request headers "visible at upstream backend" and then ends **"NOT asserted: … H2 differential coverage."** **The row CLOSES that carve-out.** ⚠️ **Do NOT report this as a false-contract row** — that would repeat the phase-88 error of asserting a divergence the reference does not have. The mirror sentence at `BEHAVIOR_CONTRACT.md:829` (tracing inject: slice mutated, map not) is the INVERSE asymmetry; decide in writing whether the fix makes it stale.
6. ⚠️ **`SETTINGS_MAX_HEADER_LIST_SIZE` MUST NOT BE VERIFIED BY A TOKEN GREP** — a whole-file grep is self-falsifying (`conn.go:46` and `continuation_test.go:557` spell it in PROSE while the parameter is absent from every send site and all three apply switches). **Verify by SYMBOL ABSENCE.**

### Candidates ledger (re-derive EVERY cost at your tip — a carried cost is stale by construction)

**Runner-up, strongest:** `server_name` + `server_header_transformation` (~+80-130 prod, +2 config fields, **+1 BackendKind** — no existing BackendKind emits a `Server` response header; envoy-go appends only-if-absent at `codec.go:100-104` where Envoy defaults OVERWRITE; cross-package blast radius into the deliberately-decoupled `router`) · **best second pick:** `access_log[].filter` (13 oneof arms exact at `accesslog.pb.go:421-483`; `parseOneAccessLog` `bootstrap.go:1135-1174` never calls `al.GetFilter()`; cleanly splittable to a 2-arm `status_code_filter`+`duration_filter` phase since `Record.ResponseCode`/`Record.Duration` already exist; ~+130-190 with 11 strict-reject arms and a global sink-flattening hazard) · `max_request_headers_kb` + `http2.header_overflow` (**breaks stat-surface +0**, supersedes ADR-0041, **INVERTS `TestServerConn_Continuation_AcceptsPastReferenceLimit`** at `continuation_test.go:1165`) · stream-scoped overflow via a decode-and-discard drain (⚠️ **its ADR-0310 "cost un-enumerated" is now PARTIALLY enumerated: `hpack.Decoder.SetEmitEnabled`, vendored `x/net@v0.34.0/http2/hpack/hpack.go:140`, advances the dynamic table while suppressing materialization — the drain primitive is free; still ~+130-220 over both legs**) · `SETTINGS_MAX_HEADER_LIST_SIZE` (~+15-25, a fold-in, and **incoherent alone** — advertising a limit `maxHeaderBlockSize` does not enforce; **its reference side was NOT measured**) · `ssl.connection_error` (+444 whole-`.go` floor) · `test/conformance/grpc/` (9/26 reachable; `test/conformance/` still only `doc.go`/`h2spec`/`proxy-wasm`) · **REVIEW.md restoration** (**37** of **129**; process-not-product) · **the D-86-CONN `client.Close` gate** (~10 lines, UNGATED at `internal/boot/boot.go:248`) · **hygiene fold-ins** (`harness_test.go` port inventory still cites `10000..10447` — STALE; xDS cycle guard not automated) · the **~59 candidates in the six family windows, TWO OF THEM STALE**.

### ⚠️ COUNTS AT THIS TIP — RE-DERIVE MECHANICALLY, NEVER COPY

- **`ROADMAP.md` 239 lines / 121 data rows**; row 89 `in-progress` at `:151`, all others `done`; check-(2) anchors `:199 :205 :211 :221 :227 :235`; `-family row` **95 occurrences / 67 LINES**; `gRPC-family row` **2**; `Operational-tooling-family row` **3**; ARM-A flags **{119, 131}** (escape-aware command ONLY; an `NF!=8` form reads 17 — **do not "correct" the figure with it**).
- **`DECISIONS.md` 18208** lines (⚠️ **the previous router carried 18206 — REFUTED at this tip by direct measurement; `125f0714` touched ONLY `next-prompt.txt`, numstat `1 0`, so it is not tip drift**) · **309** headings · tail **ADR-0310 (COMPLETE)** · next-free **ADR-0311** (TAIL-derived; `grep -c '^## ADR-0311'` => **0**; headings+1 COLLIDES at the ADR-0209 gap) · **strict `^> \*\*STATUS: PROPOSED` guard 0 — DISARMED, and THE SPEC RE-ARMS IT 0 -> 1** · STATUS census **23** (all COMPLETE) · `^---$` **216**. ⚠️ **A NEW ADR TAKES NO `---` SEPARATOR.** ⚠️ **CARRY NO WHOLE-FILE COUNT OF THE LOOSE `PROPOSED` MATCHER** — it reads **1** at `DECISIONS.md:14866`, an **ADR-0231 (phase-33)** entry in the older non-blockquote form. **Only the strict form is the guard; do not "fix" 14866 and do not report the guard as armed.**
- **`BEHAVIOR_CONTRACT.md` 5960** — ⚠️ **the phase-88 bullets were inserted MID-FILE, so every by-line citation at or below the `## HTTP/2` asserted-equivalence list shifted +2, one row after phase 87 shifted the same anchors +1. CITE BY STRING OR SYMBOL.**
- **`STATE.md` 64** · **`STATE_HISTORY.md` 500** · **`BOOTSTRAP_PROMPT.md` 522** (repo ROOT) · phase dirs **130** · `REVIEW.md` **37** (standing departure, named not claimed).
- **differential fixtures 121** at **`test/fixtures/`** (⚠️ **NOT `test/differential/fixtures/`, a plausible wrong path that returns a SILENT `0`**); numeric tail `0119-grpc-unary-trailers`, next-free **`0120` STAYS UNCONSUMED**. · **fuzzers 55 / 48 files.** · **BackendKind tail 38** (`H2GoawayResponder`, `fixture.go:614`) over 39 declarations (do NOT "fix"). · **fixture blank imports 121** — ⚠️ **THE ROUTER'S OLD GATE `grep -cP '^\t_ "'` READS 123 AND IS REFUTED**; the two extras are non-fixture imports at `runner_test.go:157` (`internal/filter/http/lua`) and `:168` (`internal/filter/http/ratelimit`). **The correct gate is the NARROWED form `grep -cP '^\t_ "[^"]*test/fixtures/'` => 121**, matching the 121 fixture dirs 1:1. · **fixture ports are family-banded `10<index>`, NEVER max+1.** · **fixture `codec_type` census: HTTP1 270 · AUTO 6 · HTTP3 3 · HTTP2 ZERO** — downstream H2 is reached ONLY via `codec_type: AUTO` + listener ALPN `["h2","http/1.1"]` (`0004-h2-routing/envoy-go.yaml:36`/`:48`); plaintext H2 needs `--allow-h2c` (`internal/filter/hcm/config.go:239`).
- **stat surface: the COMMAND binds, not the number.** `grep -ro --include='*.go' -e 'NewCounter(' -e 'NewGauge(' .` => **406** occurrences (`NewCounter(` 327 + `NewGauge(` 79); `-rn` => **404** lines; `git grep -o` tracked-only => **406**; zero untracked `.go`. ⚠️ **The phase-88 IMPL's 405/403 does NOT reproduce on either axis.** Assert the DELTA, and if you cite an absolute, cite the command with it.
- ⚠️ **STILL CONTESTED, NO NUMBER CARRIED:** the `STATE_HISTORY.md` archive-gap total · the loose `PROPOSED` matcher's whole-file count · the stat-surface DOC-side absolute (1205 vs 1207) · the zero-test `generic/*` suite count (13 vs 14).

### ⚠️ DOCUMENTARY DEFECTS — carried forward, still deliberately NOT fixed

**`ADR-0051` §2's false gate-scope sentence** corrected on the record by ADR-0307 · **`ADR-0058`** cites `routerActionH2.doH2` at a dead location · **`ROADMAP.md` rows at lines 119/131 malformed** (ARM-A GUARD) · **`STATE.md` §Project counts FROZEN at phase 76** (they self-contradict §Current — do not anchor on them) · **`harness_test.go` port inventory STALE** · **`body.go` nolint INERT** · **xDS cycle guard NOT AUTOMATED** · **`wasm/doc.go:219` two errors** · **ROADMAP cites `esalaine` FIVE times** · **`rbac.go:50` token `F2`** · **root `PROGRESS.md`** stray phase-32.1 doc · **SPEC-86/PLAN-86 prose cites the grpcclient Close pins at a nonexistent `internal/xds/xdsgrpc/...` path** · **`BEHAVIOR_CONTRACT.md`'s "no stdlib net/http parsing" sentence is FALSE for the 05.1 -> 87 window** (RETAINED, ridden by ADR-0309) · **`ADR-0057` says fixture `0004`'s driver issues "27 sequential H/2 round-trips per side" — it now issues 31** (⚠️ **`README.md:34`'s "27" is CORRECT for the pre-phase-87 prefix — do NOT "fix" it**) · **`BEHAVIOR_CONTRACT.md`'s TWO riders cite the ADR-0052 mandate at a DRIFTED `:1821`** · ⚠️ **NEW, RECORDED NOT FIXED: window `:221` carries TWO CLOSED candidates** (the H2 `//`-path bug closed by row 87; the `/stats/prometheus` projection gap consumed by rows 79/80).

### ⚠️ STANDING METHOD NOTES

1. **RE-RUN ALL THREE SENTINEL CHECKS + BOTH DOCTORING NCs YOURSELF, BOTH SIDES** of any ROADMAP edit, and record ACTUAL output. **A SPEC edits no ROADMAP — the binding proof is an EMPTY `ROADMAP.md` diff against master.**
2. **EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION.** The 89 BRAINSTORM refuted its own sizing agent's causal framing, three router counts, one ADR-0310 "unprobed" class, and two window candidates — and caught a malformed row in its own text. Do the same.
3. **LINE ANCHORS DRIFT; SYMBOL ANCHORS DO NOT — RE-VERIFY EVERY CITE AT YOUR TIP.** The six window anchors moved +1 THIS ROW.
4. **A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX** — the index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. Still live: the **SDS dial-budget class** (`internal/boot/boot_sds_e2e_test.go:551`) · the **driver-owned receiver port race** (`0081-grpc-access-log` `0.0.0.0:35097`, `0087-tracing-otlp` `0.0.0.0:35323`; **it ABORTS the binary mid-suite and the harness wrapper still exits 0**) · `internal/cluster` `-race` outlier · `internal/httpclient` zero-value · the two 84.2-era flakes · `TestServerConn_TinyWindowDelivery` · the REFERENCE h2spec section-8 flip. **Capture `-v` output BEFORE re-running anything.**
5. **CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE; never tear down a container this session did not create** (BY NAME only). ⚠️ **A sibling `envoy-rust` session was ACTIVE throughout the phase-89 BRAINSTORM and was checked rather than blamed.** ZERO containers were running at that stage's start.
6. **`rc=$?` AFTER A PIPE RETURNS THE LAST COMMAND'S STATUS** — use `out=$(…); rc=$?` or `PIPESTATUS`.
7. **A BUILD IS NOT EVIDENCE THE EDIT LANDED — ASSERT THE SYMBOL** (⚠️ **QUALIFIED**: `strings <binary> | grep -c 'writeHeaderBlock'` reads **9 on an UNMODIFIED tip** from Go's bundled `net/http`), **and ASSERT THE BOUND BINARY with `ss -ltnp` before reading any probe result.** **A harness's exit code is not the command's**; **`gofmt -l` never exits non-zero — gate on OUTPUT.**
8. ⚠️ **`pgrep -f` / `pkill -f` MATCHES YOUR OWN SHELL AND KILLS THE TOOL CALL (exit 144)**, and ⚠️ **a `pgrep -f 'a\|b'` ALTERNATION MATCHES NOTHING SILENTLY** — kill by PID from `pgrep -laf` with the `bash -c` line filtered out, ONE pattern at a time, and verify ports released with `ss -ltn`.
9. **Machine-state deliberately in place (phase 82): rustup 1.94.0 + wasm32-wasip1, and a 1.96.0 toolchain.** ⚠️ **No `phase-89-*` or stage worktree should outlive its stage close** — expect only `master` plus your own (verify with `git worktree list`).
10. **THE SIX-GATE POSTURE: name departures, do not claim compliance.** (a)/(b) differential 121 fixtures, full suite with `INNER_EXIT`; (c) h2spec cite only from your own run — **ADR-0307 records it as MEASURED NOT to gate either side, so it is NOT a red anchor**; grpc-conformance deferred in writing; proxy-wasm 10/16; (d) fuzzers 55/48; (e) `INNER_EXIT` on every differential launch AND `go test ./...`; (f) no REVIEW.md — standing departure.
11. ⚠️ **THE ARCHIVE-ABSENCE GUARD: USE THE OWN-LABEL FORM, AND NC IT.** The whole-file `grep -cF` form is FAIL-UNSAFE (an evicted label can ALREADY appear in a previous bullet's PROSE, reading as "already archived, skip the append" and LOSING the entry). The working form extracts each bullet's OWN backticked label **anchored as the one directly followed by a parenthesized date**: `grep -oP '`[^`]+`(?= \(\d{4}-\d{2}-\d{2}\))'` — it read **198** labels before this row's append and **199** after. **NC it with a label that IS present** (done here: the positive control fired on `phase 87 … SPEC done`). ⚠️ **And census `STATE.md`'s lineage bullets LINE-ANCHORED** — an unanchored form read 6 where 5 exist, because the file's own prose quotes the matcher.

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — from `git rev-parse master`, **NOT a SHA quoted in this file**. ⚠️ **USE THE SLUG-ANCHORED SUBJECT-LOCATE FORM** (the bare grep is contaminated by this file's own prose):
```sh
git log --grep '^phase 89 (h2-decode-filter-mutations)' --format='%h %s'   # 1 at this tip: BRAINSTORM
git log --grep '^phase 88 (h2-continuation-frames)' --format='%h %s'       # 6: a CLOSED phase, use as a FIRING CONTROL
```
⚠️ **The count comments are LIVE FIGURES — VERIFY before relying.** Take the NEWEST line (`| head -1`).

Execution style: **subagent-driven per `feedback_execution_style`**. Subagents commit LOCALLY on the ONE stage branch with EXPLICIT PATHSPECS; the controller squashes (`feedback_subagents_no_push`, `feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** and tear down docker **BY NAME**. ⚠️ **Band private ports outside BOTH `20000-31007` AND `11000-14999`, clear of the static fixture ports (10000-19172); the driver-owned receiver race has been observed at `35097` and `35323`, and `0081` also binds `0.0.0.0:42039`.** The 87 stages used 47410-47441, the 88 stages 47450-47559, the 89 BRAINSTORM 47560-47599; band yours from **47600+**. ⚠️ **ONE WORKTREE PER AGENT preferred; explicit-path staging if shared; never an unscoped `git restore`.** Per-task `gofmt` + `golangci-lint` on touched packages (⚠️ **misspell runs in locale US and has fired UNFORCED — sweep British spellings in `.go` comments BEFORE the lint gate**). ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch.** ⚠️ **Docker BY NAME, `--rm`, verify gone.**

⚠️ **CONTAINER→HOST REACHABILITY — READ THE CORRECTION, NOT THE HEADLINE.** `172.17.0.1` is the BRIDGE IPAM GATEWAY and is documented NOT to work here. Use `--add-host=host.docker.internal:host-gateway` and dial the HOSTNAME, or resolve the literal via a throwaway `getent hosts host.docker.internal` container; and put **`dns_lookup_family: V4_ONLY`** on any reference cluster dialing `host.docker.internal` (the embedded DNS also returns a non-forwarding AAAA that `AUTO` prefers — a SILENT connect-fail that reads as "the backend emits nothing"). **A host-bound backend IS reachable; verify delivery ran (receiver count > 0) before concluding anything.** ⚠️ **AND envoy-go REJECTS `match.headers` outright** — `route 0: match.headers is not supported in phase 04` — so a header-match route works on the REFERENCE side only. ⚠️ **AND `clusters: []` is BOOT-REJECTED even when every route is a `direct_response`** (`cluster manager: zero clusters in bootstrap`) — a dummy cluster is required.

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`): the Bash tool's cwd silently resets to the repo root — **observed repeatedly at every phase-88 AND phase-89 stage**. Use `git -C <abs-worktree-path>` for every git command; tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run. ⚠️ **Shell VARIABLES do not survive between Bash calls** — re-export `$W`/`$SC` in every command.

⚠️ **`next-prompt.txt` IS TRACKED BUT GITIGNORED — USE `git add -f next-prompt.txt`.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN** — `git status --porcelain` AND `git diff --stat master` must show ONLY your intended edits. For a patched TRACKED file: `sha256sum` BEFORE patching, `git checkout -- <path>` after, `sha256sum -c` the capture. ⚠️ **The phase-89 BRAINSTORM's four probe agents patched NOTHING — every config, binary and log lived in private scratch, and both probe worktrees ended with EMPTY `status --porcelain` and `diff --stat`.**

⚠️ **THE ARCHIVE-ABSENCE GUARD — SEE STANDING METHOD NOTE 11.** **Verify STRICTLY APPEND-ONLY**: `git diff --numstat` = `N 0` AND base a byte-exact PREFIX (`cmp`). At your close §Recent evicts the oldest per a DIRECT DATE READ at that tip. ✅ **CONFIRMED BY DIRECT READ AT THE PHASE-89 BRAINSTORM CLOSE:** §Recent is capped at **5** bullets; this stage faced a **THREE-WAY TIE** at 2026-08-13 (**NOT the four-way tie the phase-88 IMPL faced — the tie shape did NOT carry**) and resolved it by **LIST POSITION (the LAST entry)**, evicting `phase 87 (h2-double-slash-path-routing) PLAN done`. ⚠️ **Read all five dates DIRECTLY at YOUR tip before removing anything.**

## Read first (in order)
1. `docs/envoy-go/STATE.md` — **§Current pointer ONLY**. ⚠️ **`next-skill:` reads the phase-89 SPEC.** §Project counts SELF-CONTRADICT §Current — do not anchor on them (frozen at phase 76).
2. `docs/envoy-go/phases/89-h2-decode-filter-mutations/BRAINSTORM.md` — **§2 is the reproduction with all seven arms quoted; §3 is the mechanism; §4 is the rejected-alternatives ledger with every cost re-derived; §7 is the NINE questions this SPEC owes.**
3. `docs/envoy-go/phases/89-h2-decode-filter-mutations/PROGRESS.md` — the BRAINSTORM entry: the sentinel both-sides table, the four controller-side traps, the anticipated counts.
4. `docs/envoy-go/DECISIONS.md` **ADR-0310** (tail, COMPLETE) — §Consequences (x) is this row's provenance; (vi)/(vii) carry the break-protocol and `-run`-denominator lessons.
5. `BOOTSTRAP_PROMPT.md` §4-§7 (repo ROOT) — the lifecycle contract and the six-gate posture.

## What the SPEC owes
`docs/envoy-go/phases/89-h2-decode-filter-mutations/SPEC.md` disposing **all nine** BRAINSTORM §7 questions **BY EXECUTION, not by argument** · **Q1 decided by COMPILING PROTOTYPE** (delta-against-snapshot vs full rebuild vs slice-native chain — price all three; settle whether `filter_http.ReconcileOrderedHeaders` at `h2dispatch.go:612` is reusable verbatim on the decode side) · **Q3 is the named under-enumeration risk** (whether `:path`/`:authority` mutations must route to the H2Request SCALAR fields) · **the cost MEASURED, not estimated** — the BRAINSTORM's ~+50-90 is a LOWER BOUND (`reference_measured_prototype_is_a_lower_bound` fired on BOTH axes at the phase-88 IMPL) · **ADR-0311 §Context drafted with the strict `PROPOSED` guard RE-ARMED 0 -> 1** · `PROGRESS.md` appended · `STATE.md` rolled IN PLACE (lifecycle-state **1 -> 2**) with the oldest §Recent entry evicted by a DIRECT DATE READ · `next-prompt.txt` rolled (`git add -f`) · ONE squashed commit, merged and pushed. ⚠️ **A SPEC MOVES `want` ZERO AND EDITS NO ROADMAP ROW — prove it with an EMPTY `ROADMAP.md` diff, and still run the sentinel + both NCs and record ACTUAL output.**
