=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-CHECKED 2026-07-14 (after the phase-64 BRAINSTORM landed + row 64 registered `in-progress`, run MECHANICALLY) — it does NOT fire. Do NOT create `stop`.** State:
- (1) prints `NOT DONE: row 64` — row 64 `tracing-max-path-tag-length` was registered `in-progress` AT the phase-64 BRAINSTORM commit per the §Schema invariant. This ALONE blocks `stop` (a mid-lifecycle phase is open).
- (2) prints THREE live "candidates:" sentences — HTTP/3, xDS, and Observability ⇒ three families STAY OPEN. The Observability sentence is UNCHANGED (`custom_tags (metadata)` + OTLP-metrics + `spawn_upstream_span`/`http_service`/force-trace) — phase 64 (`max_path_tag_length`) is a §8-tier candidate, NOT in the live sentence, so it does NOT narrow it.
- (3) prints `NEVER OPENED: gRPC`, `Runtime`, `WASM` ⇒ THREE families never opened. Operational-tooling also STAYS OPEN.

Checks (1), (2) and (3) print ⇒ the sentinel does NOT fire.

**STATUS AS OF 2026-07-14: the phase-64 BRAINSTORM LANDED and ROADMAP row 64 is registered `in-progress`.** The roller SELF-PICKED (AUTONOMOUS, no human pick) the smallest CLEANLY-DIFFERENTIAL-PROVABLE remaining tracing candidate from a full source read of the tracing reject surface — the tracing **`max_path_tag_length`** knob (the FIFTEENTH Observability-family row, the FIRST tracing NUMERIC-knob row) — over `metadata` (needs a dynamic-metadata subsystem envoy-go lacks + not cleanly provable — the LAST/biggest `custom_tag` source) / `spawn_upstream_span`/`http_service`/`verbose`/force-trace/OTel `sampler`/`resource_detectors` (larger) / OTLP-metrics sink (whole new sink) / opening a never-chartered family (all larger; recorded BRAINSTORM §2.1). **Subject:** lift the `internal/tracing/config.go:112-114` `max_path_tag_length is unsupported` reject and HONOR the knob — truncate the `:path` portion of the ALREADY-emitted `http.url` built-in span attribute (`span.go:70`, built inline at the THREE `accesslog_emit.go` URL-construction sites `:40`/H2/H3) to N bytes (default 256; explicit 0 = empty path) on BOTH the OTLP and Zipkin exporters. It is the smallest cleanly-provable tracing increment: `http.url` is ALREADY asserted cross-side and PASSES for short paths in the landed OTLP tracing differentials, so the ONLY new variable is the truncation; it needs NO new infrastructure; and it additionally CLOSES a latent default-256 divergence (envoy-go emits UNTRUNCATED today). Unlike phases 62/63, this row touches NEITHER the custom-tag parse NOR the `ResolveCustomTags` seam — it sits one layer out on the `http.url` built-in's URL construction (D-MPTL-TRUNC-LOCATION: a shared `tracing` helper at the three URL-build sites — a small mechanical 3-site edit, NOT a zero-touch row). Anticipated a SINGLE FLAT ROW (ADR-0285; ~8–12 tasks; ONE new OTLP fixture [`0107` anticipated] / +0 stats / +0 fuzzers / +0 packages / +0 modules). Row 64 STAYS `in-progress`; STATE flipped; router rolled to the SPEC.

==============================================

## What THIS session does (the phase-64 SPEC)

Perform the SPEC stage (lifecycle-state 1 → 2) for phase 64 (`tracing-max-path-tag-length`). This is a DOCS-ONLY stage in a FRESH worktree off master (`feedback_git_worktrees`); NO production `.go` changes. Author `docs/envoy-go/phases/64-tracing-max-path-tag-length/SPEC-64.md` DISPOSING every D-MPTL-* open question from the BRAINSTORM via LIVE PROBES against the pinned reference `envoyproxy/envoy:contrib-v1.37.2` (`reference_envoy_contrib_image_tagging`; fresh container per arm, `reference_probe_fresh_container_per_arm`; shared bridge + reachable receiver, `reference_docker_probe_bridge_network`/`reference_host_gateway_ip_docker_desktop`; verify the OTLP span decode ACTUALLY ran). Draft ADR-0285 §Context (NOT §Decision/§Consequences — those land at the IMPL per ADR-0044). Land it (commit + push per `feedback_push_to_origin`) and ROLL THIS FILE to the PLAN stage.

**The D-MPTL-* arms to DISPOSE (from BRAINSTORM §10 — probe each live):**
- **D-MPTL-REFTRUNC** (CENTRAL provability probe) — CONFIRM the reference OTLP (and Zipkin) tracer truncates the `http.url` tag at `max_path_tag_length` (anticipated YES via Envoy `Http::Utility::buildOriginalUri`, applied by the generic `HttpTracerUtility` for all tracers). Configure an explicit small cap + a long path; observe `http.url` via `test/helpers/otlptrace`. **If the probe shows NO truncation (recall wrong), RE-SCOPE the row.**
- **D-MPTL-TARGET** — WHAT is truncated: the `:path` (path+query) portion BEFORE the `scheme://host` prefix (anticipated), NOT the whole URL. Probe a long path with a short host (assert the host survives).
- **D-MPTL-DEFAULT** — the ABSENT default: 256 (truncate — anticipated) vs unlimited. Probe a > 256 path with NO explicit knob. The load-bearing behavior-change decision (honoring it fixes the latent divergence; byte-stable for the < 256 corpus).
- **D-MPTL-ZERO** — an EXPLICIT `max_path_tag_length: 0`: truncate path to empty (`scheme://host` — anticipated) vs "unlimited". Probe.
- **D-MPTL-QUERY** — is the query string included in the truncated `:path` (anticipated YES — `:path` = path+query = `r.URL.RequestURI()`). Probe a cut landing inside the query.
- **D-MPTL-TRUNCUNIT** — bytes vs runes for the substring (anticipated BYTES; an ASCII-path fixture sidesteps). Confirm.
- **D-MPTL-TRUNC-LOCATION** — a shared `tracing` helper at the three URL-build sites (Option A, LEAN) vs threading path+cap through `SpanInputs` into `BuildServerSpan` (Option B). Pin the helper name (GREP-collision-checked in `internal/tracing`, `reference_spec_drafted_identifier_collision_check`) + signature + the three RE-DERIVED `accesslog_emit.go` call-site lines.
- **D-MPTL-FIXTURE** — ONE new OTLP fixture (`0107` anticipated; RE-DERIVE the next-free number): a long path + a small explicit cap, cross-side `http.url` VALUE-equality (deterministic — NO harness env-injection). New dir (`reference_differential_fixture_dispatch_constraint`); break-prove live (`reference_differential_break_protocol_count1` `-count=1`; `reference_differential_run_selector` — `-run 'TestDifferential/0107-tracing-max-path-tag-length'`, NEVER bare). H1/H2-over-TCP so `reference_differential_http_expectations_tcp_only` does not bite (assert in the `otlptrace` receiver).
- **D-MPTL-DEFAULT-PROOF** — prove the default-256 truncation via a UNIT test (LEAN) vs a second > 256-path fixture. If it balloons into a second fixture leg, re-arm the ADR-0045 escape valve (D-MPTL-SPLIT).
- **D-MPTL-REJECT** — CONFIRM no new structural reject is needed (a `UInt32Value` with no PGV min/max on this field; absent + explicit-0 both valid). RE-DERIVE against the PROJECT-pinned go-control-plane (`/envoy v1.32.4`).
- **D-MPTL-FUZZSEED** — a SEED to the EXISTING `FuzzHCMConfigParse` (NOT a new fuzzer); fuzzer count STAYS 55 (`reference_fuzzer_count_docs_drift` — reconcile before AND after).
- **D-MPTL-SPLIT** — the ADR-0045 disposition (SINGLE FLAT ROW anticipated, ~8–12 tasks).
- **D-MPTL-DOCSHAPE** — RE-DERIVE the full §6 edit-site roster against the current master tip (`feedback_brief_citations_not_evidence` — the BRAINSTORM's citations are NOT evidence; re-derive `config.go:112-114`, `span.go:70`, the three `accesslog_emit.go` URL-build lines, `config_test.go:340` reject test).

**Honor throughout (the discipline, unchanged):**
- **Lifecycle = one stage per session.** This session: the SPEC ONLY. After it lands: roll to the PLAN.
- **Process anchors:** ADR-0044 (ADR §Decision/§Consequences land at the IMPL; the SPEC drafts only §Context) · ADR-0045 (the split escape-valve, only if the slice exceeds ~15 tasks — anticipated NOT consumed) · ADR-0106 (a row flips `done` at its IMPL) · `reference_roadmap_split_phase_row_done`.
- **`next-prompt.txt` IS TRACKED** (`reference_next_prompt_tracked_despite_gitignore`) — edit it inside the stage worktree and fold into the stage commit; locate commits by SUBJECT (`git log --grep`), never by position.
- **Worktree discipline** (`feedback_git_worktrees`, `feedback_subagent_worktree_detach`, `feedback_subagent_worktree_path_targeting`) — if subagent-driven, `feedback_execution_style` + `feedback_subagents_no_push`.
- **Tracing traps to re-read** (`reference_tracing_upstream_cluster_framework_gap` — `http.url` is a POPULATED built-in, NOT one of the 4 EMPTY framework-gap attributes, so the truncation acts on a real value; do NOT conflate · `reference_tracing_custom_tag_override_builtin` — a custom_tag keyed `http.url` still upsert-overrides the truncated built-in).

**ADR reservation ledger (provisional; each finalized at its IMPL in landing order):** ADR-0277 = phase-59 tracing-custom-tags-literal (LANDED) · ADR-0283 = phase-62 tracing-custom-tags-request-header (LANDED) · ADR-0284 = phase-63 tracing-custom-tags-environment (LANDED) · **ADR-0285** = phase-64 tracing-max-path-tag-length (RESERVED; §Context at the SPEC, §Decision/§Consequences at the IMPL) · next-free unreserved after: **ADR-0286**.

**Current counts (VERIFIED at the phase-64 BRAINSTORM against master tip `e0864177`; a BRAINSTORM is DOCS-ONLY ⇒ all UNCHANGED — RE-VERIFY at session start, `git fetch` first):** fixtures **108** (`ls -d test/fixtures/[0-9]*/ | wc -l`; tail `0106-tracing-custom-tags-environment`) · fuzzers **55** · DECISIONS tail **ADR-0284** (next-free **ADR-0285**) · BackendKind tail **38** (`H2GoawayResponder`) · stat surface **1201** · go.mod modules **2** (`quic-go` direct + `qpack` indirect). A SPEC is DOCS-ONLY ⇒ ALL counts UNCHANGED at that stage.

**Locating the phase-64 BRAINSTORM stage squash** (a router cannot name its own sha — identify by SUBJECT, never by position):
```sh
git log --oneline --grep='^phase 64 (tracing-max-path-tag-length) BRAINSTORM' -1
```

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-64 SPEC is next; row 64 is `in-progress`).
2. `docs/envoy-go/phases/64-tracing-max-path-tag-length/BRAINSTORM.md` — the D-MPTL-* roster (§10), the subject + rejected alternatives (§2.1), the edit-site enumeration (§6), the framework survey (§3), the prior-phase lessons (§11).
3. `docs/envoy-go/STATE.md` — the active-phase header (phase 64 BRAINSTORM done; NEXT = the phase-64 SPEC).
4. `docs/envoy-go/ROADMAP.md` — row 64 (`in-progress`) + the Observability family prose (phase-64 charter sentence); the live deferred sentences (UNCHANGED by this row).
5. `docs/envoy-go/phases/59-tracing-custom-tags-literal/`, `62-tracing-custom-tags-request-header/`, `63-tracing-custom-tags-environment/` (SPEC/PLAN) + `docs/envoy-go/DECISIONS.md` ADR-0277/ADR-0283/ADR-0284 — the LANDED tracing engine + the SPEC-probe methodology precedent (live probes against the contrib image, ADR §Context-at-SPEC).
6. `internal/tracing/config.go` (the `NewConfig` reject roster + `parseOTel`/`parseZipkin`), `internal/tracing/span.go` (`BuildServerSpan`/`http.url`), `internal/filter/hcm/accesslog_emit.go` (the three URL-construction sites) — RE-DERIVE every §6 edit-site file:line against the current master tip.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — the `feedback_*` execution discipline + the tracing `reference_*` traps + the differential-harness traps (esp. `reference_differential_fixture_dispatch_constraint`, `reference_differential_break_protocol_count1`, `reference_differential_run_selector`, `reference_probe_fresh_container_per_arm`, `reference_docker_probe_bridge_network`).
