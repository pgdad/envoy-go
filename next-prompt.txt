RESUME a fresh session for envoy-go (router last updated 2026-06-26; the **phase 45.1 (`otlp-access-log` core) SPEC is DONE and LANDED** — `docs/envoy-go/phases/45-otlp-access-log/SPEC-45.1.md` written in the house 14-section SPEC shape (mirrors `SPEC-44.1.md`), the §11 D-OTLP-* live probes EXECUTED IN-SESSION against `envoyproxy/envoy:contrib-v1.37.2`, spec-document-reviewer loop run (NO BLOCKERs; 5 minor citation/wording fixes folded), ROADMAP row 45 SPEC-annotated (STAYS `in-progress` — 45.1 is NOT the final leg), STATE.md rolled, committed + pushed. **The next action is the phase 45.1 (core) PLAN.**). HEAD is at the **next-prompt.txt roll commit** atop the **phase-45.1 SPEC commit** (subject `phase 45.1 (otlp-access-log) SPEC: the core OTLP access-log sink …`), which sits atop the docs-only tip-stamp `95dc14e2` + the **phase-45 BRAINSTORM `97d57ae7`** (the stable anchor), then the checkpoint run, then the **phase-44.3 IMPL squash `4abf2c49`**. Re-read the shas with `git log --oneline -12`, do NOT assume — any number of docs-only `next-prompt.txt:` commits may stack ATOP the SPEC commit. Counts are UNCHANGED from the BRAINSTORM (the SPEC is docs-only): stat surface **1189** (non-H2 **1185**) / fixtures **85** (`ls -d test/fixtures/[0-9][0-9][0-9][0-9]* | wc -l` == 85; tail `0083-grpc-access-log-headers`) / fuzzers **44** (`^func Fuzz` == 44) / BackendKind tail **38** / DECISIONS tail **ADR-0257**, next-free **ADR-0258**.

**ROW 44 (`grpc-access-log`) is `done`**; **ROW 45 (`otlp-access-log`) is `in-progress`** (the SPEC-45.1 ANNOTATED it but it STAYS `in-progress`; it flips `done` at the 45.2 IMPL — the FINAL leg, per-leg ADR-0106 + `reference_roadmap_split_phase_row_done`). The **Observability FAMILY STAYS OPEN** (OTLP access log [active] / OTel-Zipkin tracing / stats sinks / tap filter remain rows). **The active action is the phase 45.1 (core) PLAN** — the FIRST leg of the by-concern 2-leg OTLP split. Invoke `superpowers:writing-plans` (PLAN stage): author `docs/envoy-go/phases/45-otlp-access-log/PLAN-45.1.md` in the house TDD-task-spine shape (mirror `PLAN-44.1.md`), resolve the §12 D-OTLP-* PLAN questions, do the FINAL ADR-0045 split re-check with real LoC (D-OTLP-SPLIT-FINAL), run the plan-document-reviewer loop, register on ROADMAP/STATE, push. THEN roll this router to the 45.1 IMPL (subagent-driven per `feedback_execution_style`, a fresh worktree per `feedback_git_worktrees`).

First confirm state via `git log --oneline -12` (expect HEAD at the next-prompt.txt roll over the phase-45.1 SPEC commit over the tip-stamp `95dc14e2` + the BRAINSTORM `97d57ae7`; counts as above; re-read the shas, do NOT assume).

## THE CHARTERED PHASE-45.1 DESIGN (from the LANDED SPEC-45.1.md — do NOT re-derive; the §11 D-OTLP-* live probes are PINNED; the PLAN resolves only the §12 D-OTLP-* PLAN questions + decomposes the TDD spine)

The SPEC-45.1 CORRECTED the BRAINSTORM's hypothesized mapping with live evidence (contrib-v1.37.2, 2026-06-26; decode-ran proof `access_logs.open_telemetry_access_log.logs_written: 13`). The built-in OTLP record is FAR LEANER than the brainstorm assumed:
- **The built-in `LogRecord` carries ONLY `time_unix_nano`** (← `Record.StartTime`) — NO `observed_time_unix_nano`, NO `severity_number`/`severity_text` (AMEND-OTLP-SEVERITY — never set; D-OTLP-SEVERITY DISSOLVED), NO `body`, NO `LogRecord.attributes`. The brainstorm's method/path/authority/response_code/severity built-ins DO NOT EXIST — those are 45.2 operator templating.
- **The "built-in labels" are 4 `Resource.attributes`** (always all 4 keys, empty when unset): `log_name`(← common_config.log_name) / `zone_name`(← node.locality.zone) / `cluster_name`(← node.cluster) / `node_name`(← node.id). `disable_builtin_labels: true` ⇒ EMPTY `Resource` (drops all 4 wholesale; `time_unix_nano` untouched). `disable_builtin_labels` is CONSUMED at 45.1.
- **Export envelope (AMEND-OTLP-EXPORT-SHAPE):** one `ExportLogsServiceRequest` → one `ResourceLogs{ resource{attributes}, scopeLogs:[ one ScopeLogs{ logRecords:[N] } ] }`; `ScopeLogs.scope` ABSENT. UNARY `Export` BATCHES; framing varies → the `0084` differential asserts the aggregated per-record PAYLOAD (Resource labels + `time_unix_nano` presence + record count + `logs_written`), NOT framing.
- **Stats (AMEND-OTLP-STATS):** `access_logs.open_telemetry_access_log.{logs_written,logs_dropped}` (NOT the brainstorm's `access_logs.open_telemetry.*`); STATIC; `logs_written` == records (batch-invariant). `stat_prefix` injects an infix (`…open_telemetry_access_log.<stat_prefix>.…`) — DEFERRED parse-accept-but-inert at 45.1 (fixed names; +2 → **1191**, non-H2 **1187**).

**45.1 chartered build (SPEC §3):**
- **Config parse** (`internal/bootstrap/bootstrap.go`): `otlpAccessLogTypeURL` const + a blank-import of `…/access_loggers/open_telemetry/v3` + `parseOpenTelemetryAccessLog` → a new `OTLPConfig{ClusterName,LogName,BufferSizeBytes,BufferFlushInterval,DisableBuiltinLabels}` + `Bootstrap.OTLPConfigs []OTLPConfig`. Lift OTLP from the ADR-0041 silent-ignore arm (`bootstrap.go:336`). The `common_config`(1) IS the SAME `CommonGrpcAccessLogConfig` `parseGrpcAccessLog` (`:371`) reads ⇒ the V2/`google_grpc`/empty-cluster rejects + the buffer-field defaults (`:391`–`:403`) are the SAME logic (D-OTLP-CONFIG-DEFER: factor a shared `parseCommonGrpcAccessLogConfig` helper vs duplicate). STRICT-REJECT the per-sibling `body`(2)/`attributes`(3)/`resource_attributes`(4) [45.2, per `reference_strict_reject_sibling_typeurl_gap`] + `formatters`(7); PARSE-ACCEPT `disable_builtin_labels`(5); PARSE-ACCEPT-INERT `stat_prefix`(6).
- **`OTLPLogsClient`** (`internal/grpcclient/grpcclient.go`, the `ALSClient` `:259` shape but UNARY): `NewOTLPLogsClient(d,clusterName)` + `Export(ctx,*ExportLogsServiceRequest)` + sync.Once `Close` (`ALSClient.Close` `:305`). Typed-import `collogspb "go.opentelemetry.io/proto/otlp/collector/logs/v1"`. NO Dialer change.
- **`OTLPAccessLogSink`** (`internal/accesslog`, the `GrpcAccessLogSink` `:48`/`:166` shape — channel + writer-goroutine + 44.2 size/interval/close BUFFER): an `otlpClient` interface seam (the `alsClient` `:22` precedent); accumulates `*logspb.LogRecord`; flushes a batch via `client.Export` (byte-sum = Σ `proto.Size(logRecord)`); `logsWritten.Add(len(batch))` on successful flush (batch-invariant); retry-once on Export error (NO stream / NO identifier — simpler than ALS); idempotent `Close`. PARALLEL impl, NOT literal reuse of `run()` (streaming-tied).
- **Mapping + Resource** (SPEC §3.4/§3.5): `LogRecord.time_unix_nano` ONLY; the 4 `Resource.attributes` (gated by `disableBuiltinLabels`); empty `ScopeLogs.scope`. Node-locality sourcing (D-OTLP-NODE): `main.go:132` builds a minimal `&corev3.Node{Id,Cluster}` — 45.1 needs `node.locality.zone` too, source from `bs.Proto.GetNode()` directly.
- **Stats:** a new `accesslog.RegisterOTLPSinkCounters` (the `RegisterGrpcSinkCounters` `stats.go:23` shape) — `access_logs.open_telemetry_access_log.{logs_written,logs_dropped}`, +2 → 1191.
- **Main wiring** (`cmd/envoy-go/main.go`): **HAZARD** — the `dialer := grpcclient.New(cm)` is INSIDE `if len(bs.ALSConfigs) > 0 {` (`:129`-`:130`); HOIST it to fire when `len(bs.ALSConfigs) > 0 || len(bs.OTLPConfigs) > 0` (build once, share). Per-`OTLPConfig`: `NewOTLPLogsClient` → `NewOTLPAccessLogSink` → append to `sinks`; the defer-LIFO close loop (`:141`) already closes every sink.
- **go.mod:** PROMOTE `go.opentelemetry.io/proto/otlp v1.0.0` transitive→direct (the ONE departure; already in the cache + go.sum). NOTE: the working-tree `go.sum` already carries the otlp/proto hash lines (a pre-existing modification, left unstaged at the SPEC stage — the IMPL reconciles via `go mod tidy`).
- **Differential `0084-otlp-access-log`** (fixtures 85→86): H1 downstream → backend, HCM `access_log[]` OTLP (log_name set, no body/attributes/disable_builtin_labels) → the driver-owned `test/helpers/otlplogs` receiver (an in-process `LogsServiceServer` accumulating `LogRecord`s — BackendKind STAYS 38 per `reference_differential_grpc_receiver_driver_owned`). Poll-to-converge (release barrier, NO sleep); assert aggregated Resource labels + `time_unix_nano` presence + count + `logs_written`. `disable_builtin_labels` empty-Resource covered by a subject unit test (one fixture dir = one runner branch — `reference_differential_fixture_dispatch_constraint`).
- **Fuzzer `FuzzParseOpenTelemetryAccessLogConfig`** (44→45; reconcile the `^func Fuzz` total per `reference_fuzzer_count_docs_drift`).

## SPEC §12 D-OTLP-* PLAN questions (resolve at the 45.1 PLAN)
`D-OTLP-CONFIG-DEFER` (shared `parseCommonGrpcAccessLogConfig` helper vs duplicate) · `D-OTLP-STATS-PREFIX` (confirm `stat_prefix` parse-accept-inert at 45.1 vs honor-the-infix-now with the `IsValidName` guard + per-`stat_prefix` registration) · `D-OTLP-RECEIVER-WIRING` (the `0084` receiver reachability + the `test/helpers/otlplogs` shape) · `D-OTLP-NODE` (node-locality `zone_name` sourcing) · `D-OTLP-SPLIT-FINAL` (the FINAL ADR-0045 re-check with real LoC, ≈260–320 prod LoC anticipated) · `D-OTLP-FUZZER` (land the fuzzer at 45.1 vs defer).

## NEXT STEP — the phase-45.1 (core) PLAN
Invoke `superpowers:writing-plans` (PLAN stage): (1) a fresh worktree off master per `feedback_git_worktrees` (the PLAN doc commits direct on master per the 44.x rhythm); (2) author `PLAN-45.1.md` in the `PLAN-44.1.md` TDD-task-spine shape (~9–11 tasks: PROGRESS+baselines+go.mod promotion+ADR-0045 re-check; the `OTLPConfig` parse arm + rejects + fuzzer [TDD]; the `OTLPLogsClient` [TDD]; the `OTLPAccessLogSink` [TDD, -race]; the built-in mapping + Resource + disable_builtin_labels [TDD table-driven]; the 2 stats + registration test; the main wiring + dialer hoist + node-locality; the `test/helpers/otlplogs` receiver; the `0084` fixture + disable_builtin_labels unit test; deliberate breaks + 20/20 + FULL-package -race; full 86-dir + six-gate; ADR-0258 body + BEHAVIOR_CONTRACT + STATE/ROADMAP); (3) resolve the §12 D-OTLP-* PLAN questions; (4) run the plan-document-reviewer loop; (5) register on ROADMAP + roll STATE.md; (6) push per `feedback_push_to_origin`. THEN roll this router to the 45.1 IMPL.

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** This stage is the phase-45.1 PLAN (lifecycle-state 2 → 3 for the 45.1 leg). `feedback_git_worktrees` · `feedback_execution_style` (subagent-driven once the PLAN exists) · `feedback_push_to_origin` · `feedback_subagents_no_push` · `feedback_pertask_gofmt_lint`.
- **Process anchors:** ADR-0106 (per-leg roadmap rows; family expansion has no parent rollup) · ADR-0045 (the split-gate — re-checked at the PLAN) · ADR-0041/0080 (silent-ignore vs strict-reject for access-log type-URLs) · ADR-0255 (44.1 ALS sink) · ADR-0256 (44.2 buffering) · ADR-0257 (44.3 header-capture) · next-free **ADR-0258** (45.1 OTLP core, §Context anchored in SPEC-45.1) then **ADR-0259** (45.2 operator engine).

## Read first (in order)
1. THIS FILE — the cold-start router (the phase-45.1 SPEC is DONE; the next action is the 45.1 core PLAN — write `PLAN-45.1.md` → plan-review loop → roll to the 45.1 IMPL).
2. `docs/envoy-go/phases/45-otlp-access-log/SPEC-45.1.md` — the chartered core leg (the §1.1 AMEND-OTLP-* empirical corrections + the §3 build + the §11 D-OTLP-* PINS + the §12 PLAN questions + the ADR-0258 §Context draft §13).
3. `docs/envoy-go/STATE.md` — the active-phase header (`phase 45.1 (otlp-access-log) SPEC done`) + the recorded NEXT (the 45.1 PLAN).
4. `docs/envoy-go/ROADMAP.md` — row 45 (`otlp-access-log`, `in-progress`, SPEC-annotated) + the `### Observability family` heading.
5. `docs/envoy-go/phases/44-grpc-access-log/PLAN-44.1.md` — the house PLAN shape the 45.1 PLAN mirrors + the as-built ALS subsystem the OTLP row reuses (`internal/accesslog/grpcsink.go`, `internal/grpcclient/grpcclient.go`, `internal/bootstrap/bootstrap.go`).
6. `docs/envoy-go/DECISIONS.md` — ADR-0255/0256/0257 (the 44.x as-built) + next-free **ADR-0258**.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `reference_streaming_sink_differential_framing`, `reference_differential_grpc_receiver_driver_owned`, `reference_docker_probe_bridge_network`, `reference_strict_reject_sibling_typeurl_gap`, `reference_dynamic_stat_name_charset_guard`, `reference_fuzzer_count_docs_drift`, `reference_differential_fixture_dispatch_constraint`, `reference_full_suite_race_after_background_mutator`, `feedback_execution_style`, `feedback_git_worktrees`, `feedback_subagents_no_push`, `feedback_pertask_gofmt_lint`).
