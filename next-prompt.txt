=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-VERIFIED 2026-07-22 (re-run MECHANICALLY at the phase-71 IMPL close, in the stage worktree, by the controller) — it does NOT fire. `stop` was NOT created.** Actual output:
- **(1) prints NOTHING** — row 71 (`tracing-custom-tags-metadata-route`) flipped `in-progress` → `done` at the phase-71 IMPL six-gate; every other chartered ROADMAP row is already `done`. Check (1) is SILENT.
- **(2) prints THREE live "candidates:" sentences** — HTTP/3, xDS, Observability ⇒ three families STAY OPEN. **Cite the command, never the adjective** (`reference_sentinel_deferred_sentence_live_vs_historical`): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` ⇒ **3**. (The Observability sentence was NARROWED at this row-done edit — the `ROUTE` MetadataKind rolled OUT; the residual `CLUSTER`/`HOST` MetadataKinds + `spawn_upstream_span`/`http_service`/force-trace + the `ssl` family keep it a live match.)
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** ⇒ THREE families never opened.

Checks (2) + (3) print ⇒ the sentinel does NOT fire. Abundant work remains.

==============================================


## STATUS AS OF 2026-07-22: THE PHASE-71 IMPL IS COMPLETE AND LANDED. ROW 71 (`tracing-custom-tags-metadata-route`) IS `done`. THIS SESSION PERFORMS THE PHASE-72 BRAINSTORM (the roller SELF-PICKS the subject).

**What the phase-71 IMPL landed** (`git log --grep 'phase 71'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): the 9-task SINGLE-FLAT-ROW TDD spine (T1–T9) — the tracing `custom_tags` `metadata` type's `ROUTE` MetadataKind, CONSUMED. **ROW 71 FLIPPED `in-progress` → `done`** at the six-gate (ADR-0106, the SOLE leg). ADR-0293 COMPLETED IN PLACE (§Decision + §Consequences; STATUS PROPOSED → COMPLETE; tail stays ADR-0293, next-free ADR-0294).
- **The spine (each red-first → green → break-verified → per-task adversarial review, RE-DERIVING anchors at the IMPL tip — 32/32 EXACT-HOLD, ZERO drift, no correction needed):** T1 (`9f96354a`) `config.go` `kindMetadataRoute` + the ROUTE accept arm cloning REQUEST (MetaPath FULL, NO new import) replacing the `:245-246` reject; CLUSTER/HOST/unset UNCHANGED · T2 (`4da435b2`) `resolve.go` the one-arg `routeMetaLookup` 4th param + the `kindMetadataRoute` arm descending the FULL `MetaPath` (`descend`/`structpbValueToString` reused VERBATIM); the crux Break D reproduced · T3 (`3c0acfef`) the NEW `func (c *FilterChain) RouteMetaLookup()` accessor + the `structpb` import to `chain.go` · T4 (`040f67d4`) the `routeMetaLookup` thread at all 18 callers (`chain.RouteMetaLookup` at 15, a 2nd `nil` at the 3 no-chain sites, the 3rd `metaLookup` untouched) + a live ROUTE-metadata-span test · T5 (`8147ed9e`) the `withMetaTags` seed repurpose, dispatch-verified (+0 fuzzers) · T6 (`3776a7e8`) the OTLP `0115-tracing-custom-tags-metadata-route` fixture (static route metadata, NO writer; breaks J/K/L fired LIVE) · T7 (`383cc974`) BC B1–B2 · T8 the six-gate · T9 ADR-0293 IN PLACE + this roll.
- **Six-gate ALL GREEN:** gofmt/vet/build clean; `go mod tidy -diff` EMPTY + `go.mod`/`go.sum` NO DIFF vs master (+0 modules); golangci-lint 0; the FULL **117-dir** differential `ok 388.647s` EXIT 0; cycle guard (no `internal/filter` edge — `internal/tracing` stays filter-free); `-race` on the 3 touched packages; envelope audit (`internal/xds`/`internal/tls`/`internal/boot`/`internal/listener`/`internal/bootstrap`/`validate/` + `internal/dynamicmetadata`/`ratelimit`/`lua` BYTE-UNTOUCHED).
- **Counts at exit:** fixtures **116 → 117** (`0115-tracing-custom-tags-metadata-route`) / fuzzers **55 (+0)** / stat surface **1201 (+0)** / BackendKind **38 (+0)** / go.mod modules **2 (+0)** / DECISIONS tail **ADR-0293** COMPLETE (next-free **ADR-0294**). Two of the four `metadata` MetadataKinds (`REQUEST`@70, `ROUTE`@71) now consumed. The Observability family STAYS OPEN.

**⚠️ PUSH (idempotent — evaluate, then act):** FIRST ACTION: `git fetch`; if origin/master is BEHIND local master and the local tip is the phase-71 IMPL squash (`git log --grep 'phase 71'`, locate by SUBJECT), `git push origin master` per `feedback_push_to_origin`; if in sync, proceed; if REJECTED (diverged), STOP and reconcile — never rebase away landed squashes.

## What THIS session does — the phase-72 BRAINSTORM (the roller SELF-PICKS)

One stage per session: the BRAINSTORM. The loop is AUTONOMOUS (the 2026-07-12 standing directive) and the sentinel does NOT fire — so **SELF-PICK the next subject: the smallest defensible ROW-SIZED candidate**, established by a read-only cost-assessment at the current master tip (NOT by an adjective carried from a prior deferral — `reference_deferred_candidate_cost_restale`; a later landed row can have shrunk or grown a deferred candidate's cost). **Record the pick AND the fully-adjudicated rejected roster in the BRAINSTORM (§2.1).** Use `superpowers:brainstorming`. Fresh worktree off master per `feedback_git_worktrees`; docs-only (ZERO production `.go`); subagent-driven per `feedback_execution_style`. Then roll THIS file to the phase-72 SPEC.

**Candidate pool (re-derive each candidate's cost at the CURRENT tip — do not trust these adjectives):**
- **Observability (family OPEN):** the residual `metadata` MetadataKinds — `CLUSTER` (the `upstream_cluster` framework gap — the selected cluster's identity/metadata is NOT plumbed to the span-emit seam; `accesslog_emit.go` hard-codes `UpstreamCluster: ""` — LARGE) · `HOST` (only the reduced `envoy.lb` scalar-subset map in scope at the seam, not the full `*corev3.Metadata` — MODERATE) · `spawn_upstream_span` (a new CLIENT-span model — MOD-LARGE) · `http_service` (a new HTTP-protobuf transport — MODERATE) · force-trace (unbuilt internal-request detection — LARGEST) · the `ssl` handshake-outcome stat family (ADR-0286 C3, framework surgery) · a 6th stats sink.
- **xDS (family OPEN):** reconnection-backoff/`initial_fetch_timeout` edges · upstream SDS (server-cert + validation_context) · CDS/EDS · LDS/RDS · ADS (muxed) · Delta xDS · RTDS/Runtime-layer · google_grpc transport. *(Note the `grpcclient→cluster→tls` cycle constraint — `reference_xds_config_seam_transitive_cycle_guard`; upstream SDS is a VALUE-level cycle the TYPE-level guard passes.)*
- **HTTP/3 (family OPEN):** upstream H3 cluster · alt-svc advertisement · 0-RTT/early-data · h3spec conformance gate · QuicProtocolOptions tuning · full QUIC transport-socket options · QUIC robustness (migration/RESET_STREAM/GOAWAY/datagrams/WebTransport).
- **Never-opened families:** gRPC · Runtime · WASM — opening one is DEFENSIBLE but LARGER than a follow-on within an open family; weigh accordingly.
- **Other deferred riders:** the RBAC `require=false` fixture rider · DataSource `environment_variable` (D-ENV-HARNESS-gated) · `watched_directory`/SDS rotation.

**Stage-close mechanics (the BRAINSTORM):** register the picked row `in-progress` in ROADMAP §Schema at the stage-close commit (per the §Schema invariant — a new subject re-opens sentinel check (1)); NO deferred sentence is narrowed at the BRAINSTORM (the phase-57 precedent — narrowing happens at the picked row's IMPL row-done edit); DECISIONS UNTOUCHED (the next ADR's §Context drafts at the SPEC per ADR-0044; next-free is ADR-0294). STATE §Current rolled IN PLACE (lifecycle DONE → 1; lineage re-capped at five). next-prompt.txt rolled to the phase-72 SPEC. Re-run the sentinel MECHANICALLY at close (with the new row registered `in-progress`): (1) prints `NOT DONE: row <NN>`; (2) **3** (or as adjusted if the pick's family sentence changes — keep the count honest); (3) `NEVER OPENED: gRPC/Runtime/WASM` (unless the pick opens one). Does NOT fire; no `stop`.

## Read first (in order)
1. `docs/envoy-go/ROADMAP.md` — the §Schema (row registration invariant; "Families are not rows"), the family headings + their live `candidates:` sentences (the deferred rosters to pick from), and every row's status (all `done`).
2. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline) + the §Recent lineage (the last five stages) + the §Project counts.
3. `docs/envoy-go/DECISIONS.md` tail — ADR-0293 (COMPLETE — the phase-71 `metadata`/`ROUTE` precedent) + ADR-0292 (phase-70 `metadata`/`REQUEST`) + ADR-0044/0045/0080/0106/0165.
4. The phase-71 lifecycle docs (`docs/envoy-go/phases/71-tracing-custom-tags-metadata-route/{BRAINSTORM,SPEC,PLAN,PROGRESS}.md`) for the row-pick + stage shape to mirror.
5. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — trust MEMORY.md's index; esp. `feedback_git_worktrees`, `feedback_execution_style`, `feedback_subagents_no_push`, `reference_deferred_candidate_cost_restale` (re-derive candidate cost at tip), `reference_sentinel_deferred_sentence_live_vs_historical`, `reference_filterchain_dynamicmetadata_accessor` (extended at phase 71 with the ROUTE `RouteMetaLookup` analogue), `feedback_brief_citations_not_evidence`.
