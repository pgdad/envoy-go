START a fresh cold-start session for envoy-go — **the phase-40.1 (`outlier-detection-consecutive-5xx`, passive ejection on the `consecutive_5xx` detector) SPEC is DONE, LANDED on master (commit `a350f0e`), and PUSHED to `origin/master`; the next lifecycle step is the phase-40.1 PLAN** (`superpowers:writing-plans` — decompose SPEC §10 into ~12–16 bite-sized TDD tasks; the FINAL ADR-0045 split-gate re-check; resolve the §12 D-S40.1-1..6) OR a new subject. Confirm the tip via `git log --oneline -5` (the substantive master tip is the phase-40.1 SPEC commit **`a350f0e`**; the prior substantive tips are the phase-40 BRAINSTORM `332bdce` and the phase-39.2 IMPL squash `5ddc588`). Counts UNCHANGED at the 40.1 SPEC: stat surface **1132** / fixtures **70** / fuzzers **42** / BackendKind tail **34** / DECISIONS tail **ADR-0244**, next-free **ADR-0245**.

## What just landed (phase 40.1 SPEC — the durable record is `docs/envoy-go/phases/40.1-outlier-detection-consecutive-5xx/{SPEC,README}.md`; read those, not this summary)

Phase 40.1 = the keystone leg of the phase-40 (`outlier-detection`) 3-leg by-detector-class split: PASSIVE ejection of hosts returning `consecutive_5xx` consecutive 5xx on real traffic. The §11 **D-OD-PROTO/STATS/LIFECYCLE/REJECT/BACKEND/SEAM pins were EXECUTED IN-SESSION** live against `envoyproxy/envoy:contrib-v1.37.2` per `reference_docker_probe_bridge_network` (a 3-backend cluster {2×200, 1×503}; `/clusters` + `/stats` scrapes; `--mode validate` reject probes; the go-control-plane v1.32.4 cache).

**Architecture (ADR-0245 — §Context anchored in SPEC §13; body at the 40.1 IMPL per ADR-0044):**
- The per-request **passive seam** `cluster.RecordUpstreamResult(ep, UpstreamResult{StatusCode, LocalOriginErr})` — placed in the LIVE drivers `doH1ClusterAction` (`router.go:655`) + `doH2ClusterAction` (`router_h2.go:100`) success sites ONLY (NOT the test-only legacy `do`/`doH2`); fires on every response; `picked` is guaranteed non-zero there; weighted routes covered free.
- The **ejection dimension** on the existing `hostHealth`: `ejected atomic.Bool` / `unejectAtNanos atomic.Int64` / `ejectCount atomic.Uint32` / `consec5xx atomic.Uint32` — DISTINCT from the active-HC `healthy` bit (time-based recovery, not consecutive-success).
- The **`available = isHealthy && !isEjected`** LB-pick predicate. The five leaf consult sites move `isHealthy → available` (`loadbalancer.go:61`, `random.go:55`, `ringhash.go:153`, `maglev.go:60`, `leastrequest.go:104`; `subsetLB` inherits via children). `inPanic` moves to a NEW `availableCount` helper; `isHealthy`/`membership_healthy` (the gauge) STAY active-HC-only → byte-identical when no `outlier_detection`. **The lazy un-eject lives in `isEjected(ep)`** (clears an expired ejection on every pick-path scan past the host — the load-bearing un-eject path, since an ejected host gets no further `RecordUpstreamResult` traffic).
- `clusterHealth` creation widened to `health_checks` OR `outlier_detection` (`manager.go:370-373`).

**SPEC-firmed (AMEND-OD1..OD6):** (OD1) LAZY un-eject (the reference is interval-sweep-driven ~`base+1×interval`) — goroutine-free; `interval` parse-accepted, sweep role DEFERRED; the recovery arm DEFERRED in `0069`. (OD2) flat `base_ejection_time` (the `base × num_ejections` backoff + its per-healthy-interval decay both need the sweep → DEFERRED; `ejectCount` tracked, unused for the deadline). (OD3) `max_ejection_percent` cap = eject iff `(ejected+1)*100/total <= cap`, else `ejections_overflow++` (1/3 → cap≥34 ejects; cap 33 overflows); `0069` uses cap 100. (OD4) stat surface **+5 → 1137**: `ejections_active` (gauge) + `ejections_enforced_total` + `ejections_overflow` + `ejections_detected_consecutive_5xx` + `ejections_enforced_consecutive_5xx`; the double-count (`enforced_total` + `enforced_consecutive_5xx` both ++) REPLICATED; the legacy + other-variant stats DEFERRED; the reference's 503-trips-`ejections_detected_consecutive_gateway_failure` NOT replicated (no 40.1 gateway detector) → `0069` excludes it. (OD5) seam at the live success sites only. (OD6) +1 BackendKind `HTTP503Responder` (in-process always-503); 2 healthy reuse `HTTPEcho`. Proto: `outlier_detection` = `Cluster` field 19, ZERO new go.mod module; defaults confirmed (consecutive_5xx 5 / interval 10s / base_ejection_time 30s / max_ejection_percent 10 / enforcing_consecutive_5xx 100). spec-document-reviewer APPROVED (advisory fixes applied — the pick-path lazy-uneject + the `availableCount` split).

## The 40.1 PLAN obligations (the §12 D-S40.1-* + the spine)

Resolve at the PLAN: D-S40.1-1 (the exact `cluster: %q: outlier_detection: …` house reject wording); D-S40.1-2 (`consecutive_5xx == 0`/absent → off vs default 5); D-S40.1-3 (the `enforcing_consecutive_5xx` roll — ≥100 always / <100 a PRNG roll [the `randomLB`/weighted precedent] OR defer intermediate enforcement); D-S40.1-4 (`0069` constants single-sourced); D-S40.1-5 (the `HTTP503Responder` shape + body-attribution helper); D-S40.1-6 (the FINAL ADR-0045 split-gate re-check — anticipated NO FURTHER SPLIT). The §10 anticipated spine (~12–16 tasks): (1) baselines/PROGRESS; (2) the ejection fields + `isEjected`(with lazy un-eject)/`available`/`availableCount` + unit tests; (3) the `available` predicate move across the five leaf constructs + the `inPanic`→`availableCount` move + byte-stability proof (full 70-dir GREEN); (4) `parseOutlierDetection` + reject roster + registry-creation widening; (5) the `outlierDetector` consecutive_5xx detect/eject/cap logic + unit tests (overflow, reset-on-2xx, double-count); (6) the `RecordUpstreamResult` seam at the two live router sites; (7) the +5 stat registrations; (8) the +1 `HTTP503Responder` BackendKind; (9) `0069`; (10) `0069` break + 20-run flake; (11) full 71-dir + six-gate; (12) ADR-0245 body + BEHAVIOR_CONTRACT; (13) completion.

Anticipated at the 40.1 IMPL: fixtures 70 → 71 (`0069`), BackendKind tail 34 → 35 (`HTTP503Responder`), DECISIONS tail ADR-0244 → ADR-0245 (next-free ADR-0246), stat surface 1132 → 1137 (+5), fuzzers 42 (UNCHANGED), ZERO new packages + ZERO new go.mod modules.

## Honor throughout (the lifecycle discipline)

- **Lifecycle = one stage per session:** the PLAN (`superpowers:writing-plans`) decomposes SPEC §10; plan-document-reviewer loop; land + push. `feedback_git_worktrees` · `feedback_execution_style` (subagent-driven IMPL next) · `feedback_push_to_origin` · `feedback_subagents_no_push`.
- **IMPL-time discipline (next session after the PLAN):** `reference_differential_run_selector` (`-run 'TestDifferential/0069'`) · `reference_differential_break_protocol_count1` (`-count=1`; AND the dead-driver trap — the seam lives in `doH1/doH2ClusterAction`, NOT the legacy `do`/`doH2`; prove liveness via the `0069` traffic assertion) · `reference_docker_probe_bridge_network` · `reference_health_check_propagation_warmup` (the `0069` traffic-driven ejection gates on `ejections_active` convergence + warmup + delta counters) · `reference_fixture_workload_constant_desync` · `feedback_pertask_gofmt_lint`.
- **ADR/process:** ADR-0044 (§Context at SPEC done; body at IMPL) · ADR-0045 (split-gate re-check at the PLAN) · ADR-0052 (six-gate) · ADR-0080 (byte-stable rejects) · ADR-0106 + `reference_roadmap_split_phase_row_done` (row 40 flips `done` only when all THREE legs land) · ADR-0227 (`contrib-v1.37.2`).

## Read first (in order)

1. THIS FILE — the post-phase-40.1-SPEC cold-start.
2. `docs/envoy-go/STATE.md` — active-phase `phase 40.1 (outlier-detection-consecutive-5xx) SPEC done`; the count footer (1132 / 70 / 42 / 34 / ADR-0244, next-free ADR-0245); the lifecycle routing (next-skill = the 40.1 PLAN).
3. `docs/envoy-go/phases/40.1-outlier-detection-consecutive-5xx/SPEC.md` (+ README.md) — the §1.1 AMEND-OD1..OD6; the §3 architecture; the §5 proto roster; the §6 reject roster; the §7 stat surface +5; the §8 `0069` + the `HTTP503Responder`; the §11 D-OD-* pins; the §12 D-S40.1-* PLAN questions; the §13 ADR-0245 §Context. Plus `docs/envoy-go/phases/40-outlier-detection/BRAINSTORM.md` (the parent charter).
4. `internal/cluster/{health.go,cluster.go,manager.go}` (the `hostHealth`/`clusterHealth` registry the ejection dimension extends; `isHealthy`/`healthyCount`/`inPanic` at `health.go:70-94`; the `clusterHealth`-creation widening at `manager.go:370-373`; `parseHealthChecks` the parse precedent) + `internal/cluster/{loadbalancer,random,ringhash,maglev,leastrequest,subset}.go` (the five leaf `isHealthy→available` sites) + `internal/filter/http/router/{router.go,router_h2.go}` (the `doH1/doH2ClusterAction` seam sites :655/:100 — NOT the legacy `do`/`doH2`) + `test/differential/fixture/fixture.go` (BackendKind tail 34) + `test/differential/runner_test.go` (the in-process backend spawn precedent) + `test/fixtures/0066-health-check-http/driver/driver.go` (the poll-to-converge driver template `0069` adapts).
5. `docs/envoy-go/DECISIONS.md` (ADR-0242/0243/0244 + the process ADRs) + `docs/envoy-go/BEHAVIOR_CONTRACT.md` (the active-health-checks subsection the passive subsection joins).
6. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/`.
