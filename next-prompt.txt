=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM -> SPEC -> PLAN -> IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle, advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

**THERE IS BANKED MID-LIFECYCLE WORK AT THIS TIP.** Phase 82 (`wasm-http-call-response-cache`) was **BRAINSTORMED at 2026-08-01**; **row 82 is `in-progress` and the phase is OPEN**. ⇒ **This session does NOT self-pick. It performs the PHASE-82 SPEC.**

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
#     FIELD-PARSED form, installed at the phase-77 PLAN. The old REGEX form was
#     FAIL-UNSAFE -- see the section-(1) note below. This form parses FIELDS
#     instead of matching a pattern, and asserts the row DENOMINATOR so a
#     deleted row is itself an error.
awk -F'|' -v want=114 '
  /^\| *[0-9]/ { n++; id=$2; st=$5
    gsub(/^[ \t]+|[ \t]+$/,"",id); gsub(/^[ \t]+|[ \t]+$/,"",st)
    if (st !~ /^done/) print "NOT DONE: row " id }
  END { if (n != want) print "GATE FAIL: examined " n " data rows, expected " want }
' docs/envoy-go/ROADMAP.md

# (2) no family still carries deferred candidates
#     BROADENED AT THE PHASE-77 BRAINSTORM. The old form matched ONLY the long
#     phrase and was therefore BLIND to the Operational-tooling family, which records
#     three genuinely un-chartered candidates in the SHORT form.
grep -noE 'deferred candidates:|remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP SchemaSection: "Families are not rows").
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

⚠️ **CHECK (1) IS LIVE AGAIN — it prints `NOT DONE: row 82`.** It was SILENT at the phase-81 close, and that silence is what made a doctored-copy NC mandatory. **Keep running the NC anyway**: the moment row 82 flips `done`, check (1) goes silent again and silence is otherwise indistinguishable from a broken check. The form that works (a `sed` targeting `done      ` does NOT — it misses the real `| done |` column spacing and prints nothing, which reads exactly like *"the check is blind"*):
```sh
awk -F'|' 'BEGIN{OFS="|"} /^\| *62 /{$5=" in-progress "} {print}' docs/envoy-go/ROADMAP.md > "$SCRATCH/nc.md"
awk -F'|' '/^\| *62 /{print "NC LANDED? ["$5"]"}' "$SCRATCH/nc.md"   # INSPECT BEFORE TRUSTING THE RESULT
```
Verified at the phase-82 BRAINSTORM: the row-62 NC fires (`NOT DONE: row 62` alongside row 82), and the `want=113` denominator NC fires.

⚠️ **`want=114` IS A LIVE FIGURE, NOT A CONSTANT.** It is the ROADMAP data-row count. **A row-adding phase MUST bump it in the same commit that adds the row** — otherwise every later session gets `GATE FAIL: examined 115 data rows, expected 114`. **The phase-82 BRAINSTORM bumped it 113 -> 114 because it chartered row 82.** ⚠️ **THE SPEC ADDS NO ROW, SO IT MUST LEAVE `want` AT 114 AND LEAVE `ROADMAP.md` BYTE-UNTOUCHED.**

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. When you open a new family, add its row-summary slug here.

**SENTINEL RE-RUN MECHANICALLY AT THE PHASE-82 BRAINSTORM, AFTER THE ROW INSERT, WITH FIRING NEGATIVE CONTROLS. IT DOES NOT FIRE. `stop` was NOT created; `ls stop` => `No such file or directory`, and it MUST NOT be created.** ⚠️ **Re-run it yourself anyway and record what YOU see.** ACTUAL output, recorded rather than predicted:
- **(1) `NOT DONE: row 82`** at `want=114`. NCs both fired (above).
- **(2) FIVE — ⚠️ THE ANCHORS MOVED TO `:192 :202 :212 :218 :226`** (they were `:191 :201 :211 :217 :225`; row 82's insert shifted every line at or below 144 by +1). **UNCHANGED IN COUNT. The THIRTY-SECOND consecutive phase at which check (2) did not go down. STATED, not forecast.**
- **(3) ⚠️ `NEVER OPENED: gRPC` — ALONE. WASM IS GONE.** Row 82 is **the FIRST row in ~31 phases to move a sentinel check.** NC: an invented slug fires; the REGISTERED slug `Observability` correctly printed nothing.
- Input measured at **230 lines / 114 data rows**, so an empty result could not read as a zero result.

⚠️ **THE LEAK CHECK IS LIVE AT ANY STAGE THAT WRITES A ROADMAP CELL. YOUR SPEC WRITES NONE — `ROADMAP.md` MUST BE BYTE-UNTOUCHED — so verify that instead.** The rule: **NEVER WRITE A SENTINEL'S OWN MATCHER STRING INTO A FILE THE SENTINEL GREPS.** The sentinel greps `docs/envoy-go/ROADMAP.md` — writing these strings *here* is safe; writing them *there* is not. The leak is proven to **SILENCE CHECK (3) BY MENTION**, reproduced live again at the phase-82 BRAINSTORM.
```sh
git diff --stat master -- docs/envoy-go/ROADMAP.md   # must be EMPTY at the SPEC
```
⚠️ **AND THE TWO CLASSES ARE OPPOSITE — DO NOT CONFLATE THEM.** `<slug>-family row` is check (3)'s **PASS** condition, so writing it as part of a genuine chartered row is a **USE**. `deferred candidates:` / `remaining deferred (not-yet-chartered) candidates:` are check (2)'s **FAIL** conditions — writing either into `ROADMAP.md`, even as a quotation, makes the sentinel strictly worse.

⚠️ **A DRAFTING FOOTGUN, CAUGHT BY EXECUTION AT THE BRAINSTORM AND WORTH KEEPING:** the gate needs the literal `WASM-family row`, but the heading is `### WASM host family` and the lineage's row convention would naturally produce `WASM-host-family row` — **which does NOT match.** Both forms were tested; only `WASM-family row` passes. **A successor editing row 82's cell must preserve that exact string or check (3) silently regresses.**

⚠️ **ROW WELL-FORMEDNESS: THE GATE MUST BE A DISJUNCTION.** Over all **114** rows: **ARM-A (escape-aware `NF!=8`) catches lines 119 and 131 ONLY; ARM-B (escape-aware trailing-piece) catches line 140 ONLY. Neither arm alone suffices.** ⚠️ **Naive `NF==8` flags SEVENTEEN rows — of which FIFTEEN are FALSE POSITIVES and TWO are TRUE** — and the ARM-B row **is absent from it** (compensating defects cancel). ⇒ wrong in **both** directions: **15 FP + 1 FN**. **Row 82 was written with NF=8, no literal `|` in its summary, and is flagged by neither arm.**

## ⚠️ A RECURSIVE `grep` IN THIS HARNESS IS BLIND TO `next-prompt.txt`

`grep` is a shell **function**, not GNU grep: it execs `claude -G --ignore-files --hidden -I --exclude-dir=.git …` in ugrep-compatible mode. **`--ignore-files` honours `.gitignore`**, `.gitignore:2` lists `next-prompt.txt`, and the file is **TRACKED anyway**. Reproduced at EIGHT separate tips:
```sh
grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort         # wrapped: ABSENT
command grep -rl 'STANDING DIRECTIVE (human, 2026-07-12)' . | sed 's|^\./||' | sort # GNU: PRESENT
grep -c 'STANDING DIRECTIVE (human, 2026-07-12)' next-prompt.txt                    # direct access WORKS
```
⚠️ **NORMALIZE THE PATH FORM BEFORE COMPARING THE TWO ARMS.** The wrapped arm prints bare paths while GNU prints `./`-prefixed ones, so a naive `comm` reports **every file as "only in GNU"**. ⚠️ **Print the FILE NAMES, never a count.** ⚠️ **`git check-ignore` REASSURES YOU WRONGLY** — it reports `next-prompt.txt` as *"not ignored"* (exit 1) because **git exempts TRACKED files from `.gitignore`** while ugrep does not. **Reliable forms: `command grep -r`, `git grep`, an explicit file list, or a recursive grep handed the FILE path directly.**

⚠️ **BUT `command grep` DOES NOT SURVIVE `xargs`.** New at the phase-82 BRAINSTORM: `xargs -0 command grep -lE …` matched **NOTHING** against a 144-file input, because `xargs` execs a binary and `command` is a shell keyword. Four re-derivation arms silently read `0` until the input denominator exposed them. **Inside `xargs`, use `git grep` or an absolute `/usr/bin/grep`.**

⚠️ **AND `grep -c` ON ZERO MATCHES IS TOOL-SPECIFIC.** Only **`git grep -c`** prints nothing. **GNU `grep -c` prints `0`** (and `path:0` per file on a list or `-r`), as does the ugrep wrapper. **Both still exit 1**, so an exit-code gate is wrong for either.

⚠️ **AND `\t` IN GNU ERE IS A LITERAL `t` — BUT THE TRAP FIRES IN THE OPPOSITE DIRECTION FROM THE DOCUMENTED WARNING.** The harness `grep` **shell function** NORMALIZES `\t`, while `command grep -cE '^\t_ '` returns 0. ⇒ **the trap bites exactly where gates run** (CI, scripts, `git grep`) and is **invisible interactively**. **Use `-P` unconditionally.**

## ⚠️ THE SECTION-(1) NOTE — CHECK (1) WAS FAILING UNSAFE, AND THE PHASE-77 PLAN IS WHERE IT WAS FIRED

**The old form was a `grep -oE` regex piped into `awk`.** Every document from phase 75 forward recorded its blind spot — **109 data rows / 105 matched / FOUR misses** at an **em-dash** in an "after" cell, a **DOT** in the slug `http-1.1`, and two **LETTER-suffixed** ids `28.1a`/`28.1b` — and every one called it *"currently HARMLESS, since all four missed rows are `done`."* ⚠️ **Nobody fired the unsafe arm until the phase-77 PLAN. EXECUTED on scratch copies: with all four blind-spot rows flipped to `in-progress`, the old check printed NOTHING — `stop` WOULD HAVE BEEN CREATED with FOUR not-done rows on the books.** ⚠️ **DO NOT restore the regex form.**

## ⚠️ THE SECTION-(2) NOTE — WHAT ITS NUMBERS MEAN

The old check (2) matched ONLY the long phrase and could not see the **Operational-tooling** family's three genuinely un-chartered items, recorded in the **SHORT** form. Fixed at the phase-77 BRAINSTORM, at the MATCHER. ⚠️ **Run the union form above, never one half of it.** ⚠️ **A ONE-ARM STRIP IS NOT AN NC FOR CHECK (2)**: `sed 's/deferred candidates://'` moves the union count **5 -> 4, not 5 -> 0**, because the long form does not contain the short substring at all. Re-confirmed at the phase-82 BRAINSTORM.

⚠️ **THE DOCUMENTED TRAP'S STATED MECHANISM IS REFUTED, WHILE THE TRAP IS REAL.** `reference_sentinel_deferred_sentence_live_vs_historical` says a naive `grep -c 'candidates:'` inflates *because of* the `candidates were:` recaps. It does inflate — but **not for that reason**: `candidates were:` does not contain that substring **at all**. The inflation comes from **ROADMAP table rows carrying BRAINSTORM prose**. **Cite the command; do not re-inherit the explanation.**

⚠️ **AND THE MEASURED RECORD: CHECK (2) HAS NEVER GONE DOWN** across ~32 phases (history `0 -> 1 -> 3 -> 4 -> 5`). => **the candidates sentence is a WINDOW onto a larger deferred backlog, not an inventory of it.** ⚠️ **A stage that predicts a decrease here is repeating the phase-73 error. State what your row narrows — usually nothing — rather than forecasting.** ⚠️ **The WASM family carries NO deferred sentence at all**, so row 82 could not have consumed one; it moved check (3) instead, which is a different lever.

==============================================


## STATUS AS OF 2026-08-01: PHASE 82 (`wasm-http-call-response-cache`) IS **OPEN**. ROW 82 IS `in-progress`. THIS SESSION PERFORMS THE **PHASE-82 SPEC**.

**What the phase-82 BRAINSTORM landed.** Base master **`4596adfe`** (from `git rev-parse master`), branch `phase-82-brainstorm`. Docs-only: **ZERO production `.go`, ZERO test `.go`.** NEW `docs/envoy-go/phases/82-wasm-http-call-response-cache/BRAINSTORM.md`; `ROADMAP.md` **229 -> 230 lines / 113 -> 114 data rows** (row 82 registered `in-progress`); `STATE.md` rolled in place; `STATE_HISTORY.md` **438 -> 440**; this file. `BEHAVIOR_CONTRACT.md` and `DECISIONS.md` **BYTE-UNTOUCHED**.

### ⚠️ THE ROW, AND THE FIVE THINGS A SUCCESSOR MUST NOT RE-INHERIT IN THEIR OLD FORM

**The defect.** envoy-go's proxy-wasm host tells a guest how large an HTTP-call response is, then returns nothing when the guest reads it. `internal/wasm/http_call.go:403-409` hands the guest true `(numHeaders, bodySize, numTrailers)` counts; the `// TODO Task 15` stash at `:411-417` **never landed**; and — walked one layer up, then one further, because a code comment is not evidence — `abi_callbacks.go:186-192` `headerMapForType` handles **only** map types 0 and 1 with a `default:` returning `(nil, false)`, `:200-204` `GetHeaderMap` converts that to `WasmResultNotFound`, and `:647-661` `GetBuffer` swallows `HttpCallResponseBody`. **A SILENT WRONG-ANSWER divergence, not a loud reject.**

1. ⚠️ **THE FAILING-FIRST ANCHOR IS ALREADY LANDED, AND A BACKWARDS COMMENT IS SUPPRESSING IT.** The vendored 61-line Rust guest `test/fixtures/0036-http-wasm-body-and-advanced/scripts/l_httpcall_success/src/lib.rs` already calls `get_http_call_response_headers()` and emits `x-httpcall-status` from the parsed `:status`. So the **subject** emits `0` and the reference emits `200`. But `0036/inputs/driver.go:504-509` states the divergence **BACKWARDS** — *"subject may return 200 from cluster_b echo; reference V8 may return 0"* — and that hedged, unmeasured comment is the stated reason scenario (l) is classified **SUBJECT-ONLY** and never compared cross-side. ⇒ **the row needs NO new guest blob and NO new fixture.**
2. ⚠️ **THE ROUTER'S OWN PREFERRED CANDIDATE WAS REJECTED ON MEASURED COST, AND ITS ROSTER WAS 3x WRONG.** The driver-owned receiver port race is **42 fixtures, not 14** (0 false positives, **28 false negatives**; a coarser controller re-derivation gives 45, same conclusion), spanning far more than the three claimed families. Cheapest defensible fix **~730 net `.go` LOWER BOUND -> 1800-2200 realized** — **band-crossing**. It also narrows **neither** sentinel check and belongs to **none** of the eleven families, and both precedent harness-hardening commits (`0e9cc680`, `f2dd994a`) landed with **no ROADMAP row at all**. ⚠️ **It splits into THREE classes with TWO failure modes:** 36 Class-A1 (probe loopback, bind wildcard — `f2dd994a` defect (1), never propagated to drivers), 2 Class-A2, and **4 Class-B** (`0066`/`0067`/`0068`/`0071`) whose `allocDeadPort` failure is **inverted and silent** and **immune to the hold-the-listener fix**. ⚠️ **Only ONE defective `freeTCPPort` survives** (`test/conformance/h2spec/h2spec_test.go:219`) and it **cannot abort the differential** — different binary, `t.Fatalf` not `panic`. The real surface is **38 hand-rolled inline allocators**. **`test/` contains ZERO `recover()`** (NC: 51 repo-wide).
3. ⚠️ **THE EMPTY-SEGMENT SUCCESSOR CROSSES THE SPLIT GATE ON BOTH AXES, AND ITS BLOCKER'S PREMISE IS UNEVIDENCED.** Not "~700-900 net": **24 guard sites** (14 incumbents + **10** call sites — F1 occupies two, `rbac.go:371` and `:377`), **~2400 net LOWER BOUND, 22-30 tasks**. And **ADR-0132 §Decision (v)** (`DECISIONS.md:6291`, cite still correct) says *"MUST mirror this exact shape"* — but **no phase-14 probe ever set an empty `compressor_library.name`** (both set `text_optimized`), so its SPEC's *"empirically confirmed via probeC"* is **false**, and fixture `0016` sets a non-empty name on **both** sides ⇒ **zero cross-side enforcement**. ⚠️ **AND IT IS NOT THE ONLY BLOCKER: `internal/filter/http/lua` carries a SECOND, AMEND-2-ratified consecutive-dot shape** (`lua/stats.go:72`, 8 counters plus a double-empty `http..lua..` variant). **DISPOSABLE BY CARVE-OUT over TWO packages; superseding is not required to charter.**
4. ⚠️ **LINE CITES INTO `ROADMAP.md` ARE STALE THE MOMENT ANY ROW LANDS, AND MOST ARE UNREPAIRABLE.** Measured: **119** `ROADMAP.md:<line>` cites repo-wide; row 82's insert shifted **40** of them, and **35 are structurally unrepairable** — they live in append-only phase records (phases 66, 73-81) and `DECISIONS.md`. Only the 5 in this file could be fixed. **This is structural to EVERY row-adding phase and no prior document records it.** ⇒ **`### WASM host family` is now `:220` (was 219), `### gRPC family` is `:194` (was 193), and the check-(2) anchors are `:192 :202 :212 :218 :226`.** **Prefer PHRASE anchors.**
5. ⚠️ **THREE OF THIS STAGE'S OWN INSTRUMENTS WERE BROKEN, AND ONLY THE DENOMINATOR CAUGHT THEM.** Two extractors returned EMPTY and were broken, not zero — the ADR STATUS census (real form is a `> **STATUS: ` blockquote, not `^**STATUS:**`; census **16, all COMPLETE**) and the retained-footer matcher (real form `*(§Decision + §Consequences land at the phase-N IMPL.)*`; count **9**). And ⚠️ **the ADR recurrence guard as naively transcribed is a FALSE-POSITIVE GATE** — matching `PROPOSED` anywhere in the STATUS line fires on **four COMPLETE ADRs on a clean tree**, because those blockquotes discuss the word in prose. Anchored on the STATUS **word** (`^> \*\*STATUS: PROPOSED`) it is silent on clean and fires exactly once on a doctored copy. **A guard must be re-verified in BOTH directions after transcription, not merely after authoring.**

### ⚠️ WHAT THIS SESSION DOES — THE PHASE-82 SPEC

**One stage per session.** Write `docs/envoy-go/phases/82-wasm-http-call-response-cache/SPEC.md` and draft **ADR-0304 §Context** (ADR-0044-**as-used**; ⚠️ **ADR-0044 does NOT itself contain that discipline** — always write it with the caveat). **ROW 82 STAYS `in-progress`; `ROADMAP.md` MUST BE BYTE-UNTOUCHED and `want` STAYS 114.** Docs-only: **ZERO production `.go`, ZERO test `.go`.**

**THE D-QUESTIONS THE BRAINSTORM LEAVES:**
- ⚠️ **D-82-DIRECTION — SETTLE IT BY EXECUTION, NOT BY READING.** The call graph says subject `0` / reference `200`; the landed driver comment says the reverse. **Probe `envoyproxy/envoy:contrib-v1.37.2` with a discriminating control** (`reference_probe_must_discriminate`); a probe that cannot tell the two apart proves nothing, and ⚠️ **the probe's INPUT is a claim too** (`reference_probe_input_is_a_claim`).
- ⚠️ **D-82-TRAILERS — CHECK FOR A VACUOUS ARM BEFORE SCOPING IT.** `RunEncodeTrailers`/`RunDecodeTrailers` have **ZERO non-test callers** (2 non-test *mentions*, both in `internal/filter/http/chain.go` — its own definition and doc comment). If `resp.Trailer` is populated by `net/http` independently of that seam the arm is fine; if not it is **structurally dead** and must be scoped OUT rather than shipped vacuous (`reference_vacuous_break_modes`).
- **D-82-LIFETIME** — where the stash is cleared, and whether a second `dispatch_http_call` on the same stream overwrites or queues. Bears on whether the row is 7 or 9 tasks.
- **D-82-BREAK** — the deliberate-break arm. `0036`'s README §"Deliberate-break liveness verification" makes this mandatory for every asserter arm.

**ALREADY BUILT, DO NOT RE-DERIVE:** `internal/wasm/abi/body_bridge.go:104` **already** admits `WasmBufferTypeHttpCallResponseBody` in its accepted-type switch, so **the ABI shim needs no change** — the gap is purely stash + three dispatch arms.

**ANTICIPATED SURFACE:** **+0 stats · +0 fixtures (120) · +0 fuzzers (55) · +0 BackendKind (tail 38) · +0 packages (73) · +0 go.mod modules · +0 new PUBLIC surface · NO new guest blob.** **Band ~110-350 net `.go`, budget ~250, 7-9 tasks; NEITHER §6.1 trigger fires. NO SPLIT.** ⚠️ **THAT FIGURE IS A LOWER BOUND** (`reference_measured_prototype_is_a_lower_bound`) — phase 81 measured nine prototypes and still ran **3.07x** over them. **State your own basis; do not inherit this one.**

**Verified toolchain fact:** Rust **1.96.0** with the `wasm32-wasip1` target is installed, and the guest build recipe (`0036/scripts/README.md:26-51`, `proxy-wasm-rust-sdk =0.2.4`, blobs vendored + committed) is reproducible. **CI carries NO Rust toolchain** — blobs are built locally and vendored. **Row 82 needs no new blob.**

### OUT OF SCOPE FOR ROW 82, DEFERRED BY NAME

The 9 `WasmResultUnimplemented` stubs at `internal/wasm/registration.go:877-882` (shared-queue ×4, outbound-gRPC ×5) · `proxy_on_queue_ready` and the `proxy_on_grpc_*` guest callbacks (**0** hits repo-wide; NC `proxy_on_http_call_response` => 15) · the wasm **network** filter (no `envoy.filters.network.wasm`; the 5 TCP callbacks absent) · the 6 deferred cpp-host conformance families · **the driver-owned receiver port race (42 fixtures)** · **`stats-name-empty-segment-guards` (24 sites)** · the RBAC policy-name PROJECTION divergence · the lua `statNameRegexLiteral` (~40 net, 1 task — **fold into any later row**) · the matcher-tree `Action`-name boot walk · **three** stale incumbent guard cites (`lua/compiled_config.go:272` ×2 and `redisproxy/config.go:51`, all citing the dead `manager.go:205`; live sites are `internal/filter/hcm/config.go:267` and `internal/cluster/manager.go:419`, and `internal/filter/network/hcm/` does not exist).

### ⚠️ THE AUTHORITATIVE CANDIDATE POOL IS THE SENTINEL'S OWN CHECK (2), NOT THIS FILE'S PROSE

**The project's actual un-chartered backlog is the five deferred-candidate sentences check (2) prints.** ⚠️ **Re-read them at YOUR tip; the line numbers move whenever a row is appended — they moved at THIS row.** Anchors now `:192 :202 :212 :218 :226` (HTTP/3, xDS, Observability, Runtime, Operational-tooling).

⚠️ **AND THE ROUTER'S OLD SUMMARY TABLE OF `:211` WAS WRONG — DO NOT RESTORE IT.** Read at the phase-82 tip, the **Observability** sentence names **FOUR** un-chartered items, not one: the dynamic `ssl` family; **the uncounted non-certificate handshake-failure bucket (`connection_error`)**; tracing `spawn_upstream_span`; and `http_service`/force-trace.

**The strongest measured candidate on that sentence, for a LATER row:** the **`ssl.connection_error` bucket** — **~230 net `.go` LOWER BOUND (realistic 575-700), 9-12 tasks, ZERO new fixtures** (extends `0110`, whose asserter already ignores the `envoy_listener_address` label). Established at the BRAINSTORM: `connection_error` is **charset-legal bare AND assembled**, so it does **not** inherit the dynamic-`ssl` naming blocker; the reference's bucket boundary was enumerated over **six live arms**; and Go's error taxonomy partitions it exactly with `io.EOF || io.ErrUnexpectedEOF || syscall.ECONNRESET`. ⚠️ **One open risk: `0110` is `tcp_proxy`, and whether a record-layer failure there still books `connection_error` is UNPROVEN** — one probe arm settles it.

⚠️ **AND THE DYNAMIC `ssl` FAMILY REMAINS BLOCKED — PHASE 81 DID NOT UNBLOCK IT.** `IsValidName("ECDHE-RSA-AES128-GCM-SHA256")` = **false** bare *and* assembled; a hyphen is outside the character class at **every** position, so the segment-position rule cannot help ⇒ **still impossible, not possible-but-strict.** Blocker 2 (Go IANA vs Envoy OpenSSL spellings) binds only TLS <= 1.2 — `0x1302` spells `TLS_AES_256_GCM_SHA384` identically in both. ⚠️ **And the sentence's "FOUR-family dynamic surface blocked on NAMING" conflates three dispositions:** only `ciphers` is charset-blocked; `curves` (`X25519`) and TLS-1.3 `ciphers` pass; `versions` (`TLSv1.2`) passes charset but injects a spurious dot-**segment** (an SN3 tag-extraction problem). ⚠️ **`sigalgs` was NEVER observed emitted** under any of six live reference arms — inherited, not measured.

⚠️ **`gRPC` IS THE LAST FAMILY CHECK (3) REPORTS AS NEVER OPENED, AND IT IS HARD-BLOCKED.** `### gRPC family` (`:194`) names gRPC bridge, gRPC-Web, gRPC-JSON transcoding, interop conformance — **zero implementation exists** for any of the seven gRPC filter type-URLs. Existing gRPC surface (ALS, ext_authz, ext_proc, ratelimit, metrics_service, xDS) is envoy-go acting as a **client**, which is why it exists with no gRPC-family row. Both charter-satisfying candidates sit behind the **dead trailers seam** (16-22+ tasks, ADR-0045 split near-certain). ⚠️ **The cheapest gRPC filter, `grpc_stats` (~250-300 net, 12-14 tasks), is OFF-CHARTER and is ALSO partially behind that seam** — it reads `grpc-status` from response trailers for non-trailers-only responses. **Identified, not probed.**

### What matters for the NEXT session, in order

1. **⚠️ RE-RUN ALL THREE SENTINEL CHECKS YOURSELF AND RECORD THE ACTUAL OUTPUT; DO NOT PREDICT IT.** Check (1) is LIVE again (`NOT DONE: row 82`) — **run the doctored-copy NC anyway.** ⚠️ **Your SPEC adds NO row: leave `want` at 114 and `ROADMAP.md` BYTE-UNTOUCHED**, and verify that with `git diff --stat master -- docs/envoy-go/ROADMAP.md`.

2. **⚠️ EVERY STAGE'S JOB IS TO REFUTE ITS PREDECESSOR BY EXECUTION, NOT BY REVIEW.** The lineage record is unbroken: phase-80 SPEC refuted nine BRAINSTORM claims, its PLAN nineteen, its IMPL sixteen; the phase-81 BRAINSTORM refuted fourteen router claims, the SPEC twenty-two, the PLAN fourteen, the IMPL seventeen; **the phase-82 BRAINSTORM refuted twenty-six, six load-bearing.** **Budget for finding more. A SPEC that refutes nothing has not looked.**

3. **⚠️ BROKEN-GATE COUNT IS NINETEEN — a NEW shape landed at the phase-82 BRAINSTORM.** **The nineteenth: A GUARD IS A CLAIM AT TRANSCRIPTION TIME, NOT ONLY AT AUTHORING TIME** — the ADR recurrence guard, correct as designed, became a false-positive gate when re-typed with a looser anchor, and fired on four COMPLETE ADRs on a clean tree. **Re-verify a transcribed guard in BOTH directions.**
   Priors that fired LIVE at this stage: **an empty output is not a zero result** (three times — two ADR extractors and the `xargs`/`command` arms) · **a `command grep` idiom that does not survive `xargs`** · **an archive-absence guard that SELF-CLEARS** against the prose recording the eviction (anchor on the BULLET) · **an identity check whose first MISMATCH was the CHECK's fault, not the migration's** (prefix stripped on one side only, delta exactly 26 bytes) · **a self-inflicted stale cite caught BEFORE landing** (the row cited `ROADMAP.md:219`, which its own insert moved to 220; replaced with a phrase anchor).
   The eighteen carried forward, unchanged: two defects that CANCEL in the gate metric · an inert gate cell · a full-suite recipe without `-v` is VACUOUS · a sha256 roster desynced against a DELETED file · `gofmt -l` NEVER exits non-zero (gate on OUTPUT) · `go doc -all <A> <B>` swallows arg2 · a `+0 exported symbols` gate over an EMPTY package reds on a CORRECT tree · a RANGE gate cannot detect anchor drift · a roster's naive `[ -f ] || continue` exits 0 on a DELETED file · a count-only stat guard PASSES a build with BOTH names wrong · a `-run` no-match exits 0 with `[no tests to run]` · a `--- PASS` tally over a package with sibling tests exceeds the fixture denominator · a stat-delta claim cannot be discharged by guards scoped to another package · a stderr-VOLUME assertion passes on the hang · `golangci-lint` misspell locale US · a harness's exit code is not the command's · a GOLDEN ROSTER that omits the family under test · a NEGATIVE CONTROL POINTED AT A TARGET THAT DOES NOT EXIST.

4. **⚠️ BEFORE ANY LINE-ADDING EDIT TO A HEAVILY-CITED DOC, ENUMERATE THE CITES YOUR INSERT WOULD SHIFT AND CHECK WHETHER THEY ARE REPAIRABLE.** Measured at this stage for `ROADMAP.md`: **119 cites, 40 shifted, 35 unrepairable.** The phase-81 precedent for `BEHAVIOR_CONTRACT.md` (nine cites, all in append-only docs; relocated to `### Phase 16 forward-pointer notes` at 5526, shifting nothing) is the habit to copy. ⚠️ **Your SPEC drafts ADR-0304 §Context into `DECISIONS.md` — an APPEND at the tail shifts nothing, but verify that rather than assuming it.**

5. **⚠️ LINE ANCHORS DRIFT; PHRASE AND SYMBOL ANCHORS DID NOT.** ⚠️ **`checkName` is at `registry.go:115-119`, NOT `113-118`** — `:113` is its doc comment. ⚠️ **`### WASM host family` moved 219 -> 220 and `### gRPC family` 193 -> 194 at THIS row.** **Prefer symbol anchors. RE-VERIFY EVERY `.go` AND `.md` LINE CITE BEFORE RELYING ON IT.**

6. **⚠️ THE GATES, WITH MEASURED BASELINES.** Full 120-fixture differential **120/120 in 402 s** · h2spec **106 passed / 0 failed / 0 skipped** (⚠️ **the lineage's "53/53" is ONE HALF of that total — state your own method and denominator**) · `go test ./cmd/envoy-go/` **8.7 s** (**the THIRD consumer of the binary; it boots real configs**) · the touched packages **~1 s**. `-race` is a SECOND run. ⚠️ **CI runs the differential at `-timeout 20m`, job `timeout-minutes: 30`** (verified exact, `.github/workflows/ci.yml:60,63`; `golangci-lint-action@v6.5.2`, `version: v1.64.8`).
    ```sh
    ( go test ./test/differential/ -count=1 -v > "$SCRATCH/full.log" 2>&1; echo "INNER_EXIT=$?" )
    grep -cE '^    --- PASS: TestDifferential/' "$SCRATCH/full.log"          # want 120
    grep -E  '^    --- (FAIL|SKIP): TestDifferential/' "$SCRATCH/full.log"   # want EMPTY
    grep -c  'no driver registered for fixture' "$SCRATCH/full.log"          # want 0
    grep -cE '^panic:|DATA RACE|SIGSEGV' "$SCRATCH/full.log"                 # want 0
    grep -o  'TestDifferential/[^ ]*' "$SCRATCH/full.log" | sed 's|TestDifferential/||' | sort -u \
      | comm -3 - <(ls -1 test/fixtures/ | grep -E '^[0-9]{4}[a-z]?-' | sort)  # want EMPTY
    ```
    ⚠️ The faithful dir predicate is `^[0-9]{4}[a-z]?-` — a bare `^[0-9]{4}-` gives **118**.
    ⚠️ **`go test ./test/differential/...` (with `...`) matches TWO packages and BUFFERS `-v` output.** A run that looks hung with an empty log is normal — do not kill it.
    ⚠️ **THE RUN ABORTS, IT DOES NOT MERELY FAIL. CAPTURE `INNER_EXIT` AND THE PANIC ARM.** **Budget ~3 launches per green pass** — the race is now measured at **42** fixtures.
    ⚠️ **Stat surface: assert the DELTA by CALL-SITE ENUMERATION.** The `TestNoNewStat*` guards are PROVEN BLIND BY EXECUTION. Denominator **208 code sites / 36 production files** ⚠️ **never 208/84**. ⚠️ **ARM 1 on a clean tree is VACUOUS** — assert the input diff is `> 0` **and** negative-control the matcher against a synthetic registration line.

7. **⚠️ THE SIX-GATE HAS A STANDING LINEAGE DEPARTURE — NAME IT, DO NOT CLAIM COMPLIANCE.** Gate **(f)** expects a `REVIEW.md`; **none since 25.3 — 85 of 122 phase dirs carry none** (37 carry one). Record it as a departure. Gate **(d)** is **VACUOUS for a row that adds none** — say "vacuous", not "green" (**55** fuzzers repo-wide). Gate **(c)**: proxy-wasm is **INHERITED, not re-run** unless you run it — ⚠️ **and when you do, STATE THE DENOMINATOR: it is 10 of the cpp-host's 16 files (62.5%), 6 deferred** (`test/conformance/proxy-wasm/README.md:14`). The bare "all 10 families PASS" drops that denominator. ⚠️ **A DOCS-ONLY SPEC owes (a)-(f) only in the posture a docs-only stage can have — say which are INAPPLICABLE rather than claiming them green.** Normative cites, ALL RE-VERIFIED EXACT AT THE PHASE-82 BRAINSTORM: **§6.1 `:285`** (`:289` the ~25-task trigger, `:290` the ~1500-LoC trigger, **`:291` BLANK**, `:292` the third trigger); **§6.2 `:294`**; ⚠️ **§6.3 IS `:304`, NOT `:306`** — the router carried `:306` and it is wrong; six-gate **§7.5 heading `:357`, gates (a)-(f) `:360-365`, close `:367`**. ⚠️ **`/BOOTSTRAP_PROMPT.md` WITH A LEADING SLASH RESOLVES NOWHERE** — the file exists only at `<repo>/BOOTSTRAP_PROMPT.md` (**522** lines). ⚠️ **The second copy, `docs/superpowers/plans/2026-04-21-envoy-go-bootstrap-prompt.md`, is 1024 lines and NO constant offset corrects it.** **Open the repo-root file.**

8. **⚠️ ADR + DOCUMENT CONVENTIONS.** **The DECISIONS tail is `ADR-0303`, STATUS `COMPLETE`; there is NO `PROPOSED` block in the file.** Next-free is **ADR-0304** (`grep -c '^## ADR-0304'` => 0; NC `^## ADR-0303` => 1). **302** `^## ADR-` headings, ids **0001-0303**, **exactly one gap at ADR-0209**, zero duplicates. STATUS census **16, all COMPLETE** — ⚠️ **the matcher is `^> \*\*STATUS: `, a BLOCKQUOTE; `^\*\*STATUS:\*\*` matches NOTHING and reads as a zero result.** ⚠️ **The retained italic footer count is NINE** (ADR-0294..0300, 0302, 0303; ADR-0301 carries none) — ⚠️ **the matcher is `^\*\(§Decision \+ §Consequences land at the phase-[0-9.]+ IMPL\.\)\*$`.** ⚠️ **`^---$` is 216 and must stay 216.** ⚠️ **CARRY NO WHOLE-FILE GREP COUNT IN AN ADR** — that species self-falsified in ADR-0296 ¶3 and again in ADR-0302 ¶11; **enumerate by site instead.** ⚠️ **ADR-0044 does NOT contain the §Context-at-SPEC discipline**; always write **ADR-0044-as-used** with the caveat. ⚠️ **RE-ARM the recurrence guard when you draft ADR-0304 — and anchor it on the STATUS WORD (`^> \*\*STATUS: PROPOSED`), because the loose form fires on four COMPLETE ADRs on a clean tree.**

9. **⚠️ A STAGE BRIEF'S FLAKE LIST IS NOT THE INDEX — the TWENTY-FIFTH consecutive stage at which this held.** The index is `~/.claude/projects/-home-esa-git-envoy-go/memory/`. **Still live:** `reference_sds_init_fetch_timeout_dial_budget_flake` (**TWO packages**) · the pre-existing `internal/cluster` `-race` outlier (`TestOutlierDetector_ConcurrentEjectExactlyOnce`) · `internal/httpclient TestOptions_ZeroValue_NoOpDefaults`. ⚠️ **TWO former flakes were FIXED at `f46ba419`/`f2dd994a` and a recurrence of either is now a FINDING.** ⚠️ **`0061-lb-ring-hash` is NOT a live flake — a spread failure there is a FINDING.** ⚠️ **The driver-owned receiver race is now enumerated at FORTY-TWO fixtures** (item 2). **Classify, isolate-re-run with a POSITIVE selector assertion, and state the evidence.**

10. **⚠️ CHECK FOR SIBLING SESSIONS BEFORE BLAMING YOUR OWN ROW FOR A PORT FLAKE — and NEVER tear down a container this session did not create**, by name or otherwise (`reference_parallel_agents_shared_machine_namespaces`). The phase-82 BRAINSTORM created six (`a2-probe-1..6`) and removed **exactly those, BY NAME**; no image or ancestor filter was used.

11. **Counts at this tip (re-run MECHANICALLY in your worktree; never copy them).** ⚠️ Use GNU `command grep` or `git grep` for anything repo-wide — but ⚠️ **NOT `command grep` inside `xargs`.** **All re-derived at the phase-82 BRAINSTORM:**
    fixtures **120** (next-free **0119**; bare `^[0-9]{4}-` gives **118**; numeric tail `0118-runtime-static-layer`) / fuzzers **55** / internal packages **73** / phase dirs **122**, of which **37** carry `REVIEW.md` and **85** do not / **BackendKind tail 38** over **39** declarations (do NOT "fix" 38 to 39) / go.mod modules **2** (the phase-61.2 lineage figure — the single `go.mod` requires **67**; do NOT "fix" 2 to 67) / **`DECISIONS.md` 17796**, **302** headings, tail **ADR-0303 COMPLETE**, next-free **ADR-0304**, retained footers **9**, `^---$` **216**, STATUS census **16 COMPLETE** / **`ROADMAP.md` 230 lines / 114 data rows** / **`BEHAVIOR_CONTRACT.md` 5870** / **`STATE.md` 63** / **`STATE_HISTORY.md` 440** / **`ROADMAP.md:<line>` cites 119**, of which **40** shift on a row insert and **35** are unrepairable / **stat registration: 208 CODE sites in 36 PRODUCTION files** ⚠️ **not 84** / **stat surface 1207** ⚠️ **DOCUMENTARY and CONFIG-CONDITIONAL — assert the DELTA**.
    ⚠️ **CONTESTED OR UNREPRODUCIBLE, SO NO NUMBER IS CARRIED:** the next-free REFERENCE port (routers say `10119`; `STATE.md` §Project says `10450`) · the `STATE_HISTORY.md` archive-gap total · **production `stats.IsValidName` GUARD SITES 25 vs a naive line-grep's 56** — these measure different things and must not be conflated.
    ⚠️ **THE `STATE_HISTORY.md` EXTRACTOR TRAP:** naive `^- \*\*prior active-phase:\*\*` ⇒ **161**; parenthetical-tolerant `^- \*\*prior active-phase[^*]*:\*\*` ⇒ **169** ⚠️ **(not the router's 168 — 161 naive plus exactly 8 parenthetical evictions, measured at 438 lines with a firing NC; now 440 lines after this stage's own eviction).** Any archive gate must use the tolerant anchor.

12. **⚠️ DOCUMENTARY DEFECTS — carried forward, still deliberately NOT fixed.**
    - ⚠️ **NEW: `BEHAVIOR_CONTRACT.md:1967` asserts a blocker its own successor phase KILLED** — *"still blocked on enumerating its membership"* / *"the full membership is UNENUMERATED"* for `ssl.connection_error`, contradicted by phase 75's own 18-Go + 16-reference arms and again by a fresh 6-arm probe. **A landed contract document carrying a stale blocker.** Anyone costing that candidate from it alone gets the wrong answer.
    - ⚠️ **NEW: `0036/inputs/driver.go:504-509` states scenario (l)'s divergence BACKWARDS** and is the stated reason the scenario is SUBJECT-ONLY. **Row 82 corrects it.**
    - ⚠️ **`DECISIONS.md` ADR-0301 §Decision (b) says *"has failed twelve top-level detectors"* — PRESENT TENSE inside immutable ADR text, factually wrong since row 80 landed.** A **named permanent discrepancy**, alongside `ROADMAP.md`'s five `twelve` occurrences (⚠️ **four are pure FALSE POSITIVES**).
    - ⚠️ **`0110`'s driver comment *"envoy_sds_* metric names (twelve families…)"* is ACCURATE** (14 distinct names / 12 families) **but is a FALSE POSITIVE for any `\btwelve\b` sweep.**
    - **`ROADMAP.md` malformation: three rows lose summary content in GFM render** (now at lines 119, 131, 140). All INVARIANT-BLOCKED by §Schema (*"Only `status` and `sub-phases` columns are updated in place"*) => **defensible as a GUARD, not a fix**, with the DISJUNCTION gate. ⚠️ **There is NO doc-invariant test anywhere in this repo.** The reusable precedent is `test/differential/harness_test.go:123-137`'s `runtime.Caller(0)` -> repoRoot idiom.
    - **`STATE.md` §Project counts self-contradicts §Current and is FROZEN at the phase-76 IMPL close.** **Anchor on §Current, which IS live.** ⚠️ **Do NOT "fix" §Project.**
    - **`BEHAVIOR_CONTRACT.md`'s `wasm.` arm is called a *"NEW SN9 flattening rule"*, which COLLIDES with ADR-0118's actual SN9.** Pre-existing.
    - ⚠️ **The two `//nolint:gosec` directives row 80 added are INERT** — `gosec` is not among the 9 linters `.golangci.yml` enables (`disable-all: true`; the nine are `govet errcheck staticcheck unused ineffassign gofmt goimports misspell revive`; golangci-lint **v1.64.8**).
    - **`golangci-lint-action@v6.5.2` emits a Node-20 deprecation warning in CI.** Deliberately NOT bumped: v7+ requires golangci-lint **v2** while the repo pins **v1.64.8**. **A warning, not a failure.**
    - ⚠️ **`ROADMAP.md` §Schema `:18` says *"Sub-phases get their own rows"* while phase 80 argued the legs live in the parent's `sub-phases` PROSE.** Practice diverging from written doctrine. **Record the tension; do not relitigate.**
    - **Production-DEAD trailer code** in four filters · **a VACUOUS differential arm**, `0036`'s `e_trailers_read` ⚠️ **(relevant to D-82-TRAILERS)** · **two stale non-test comments** at `internal/filter/hcm/connection.go:565` and `h2dispatch.go:503` claiming `RunDecodeTrailers` does not exist (it does, at `internal/filter/http/chain.go:455` — ⚠️ **though it has ZERO non-test callers**) · **`DECISIONS.md`'s wasm module-function roster** enumerates 24 keys, **8 of which do not exist in `internal/`** · **two sibling `freeTCPPort` helpers** — ⚠️ **REFUTED: only ONE still carries both defects** (`test/conformance/h2spec/h2spec_test.go:219`), and it cannot abort the differential.
    - **`guest-side-config`** in `internal/filter/http/wasm/compiled_config_test.go:1147` looks like a charset violation and **IS one as a string** (`IsValidName` = **false**) — but guard C reads the **outer** `pc.GetName()`, so it is exempt **by call graph, not by the string being harmless**.
    - ⚠️ **`internal/filter/network/rbac/rbac.go:50` contains the token `F2`** as an unrelated **phase-26 fork label**. **Do not use `F2` as a grep anchor in that package.**

## Mechanics

Fresh worktree off **the CURRENT master tip** per `feedback_git_worktrees` — take it from `git rev-parse master`, **NOT from a SHA quoted in this file**. The phase-82 BRAINSTORM squash is not named here on purpose: **router-only commits routinely land above it**, so a quoted SHA is stale the moment the router is touched again — **branch off the tip.** ⚠️ **AND THE BARE SUBJECT-LOCATE GREP IS CONTAMINATED BY THIS FILE'S OWN PROSE ABOUT IT.** `git log` greps the **whole commit message**, and router commits discuss the phase in their BODIES, so **the sentence warning about the grep satisfies the grep**. ⚠️ **USE THE SLUG-ANCHORED FORM INSTEAD**, which is stage-exact:
```sh
git log --grep '^phase 82 (wasm-http-call-response-cache)' --format='%h %s'   # 1 at this tip: BRAINSTORM
```
Measured with both controls: an invented slug returns **0**, and the completed phase-81 form returns **4**. **Take the NEWEST line (`| head -1`) and verify the count before relying on it.** Subagent-driven per `feedback_execution_style`; subagents commit LOCALLY only (`feedback_subagents_no_push`); controller squash-push at close (`feedback_push_to_origin`). Parallel subagents need **PRIVATE scratch** (`reference_parallel_subagents_private_scratch`) and they **must tear down docker containers BY NAME, never by an `ancestor`/image filter** (`reference_parallel_agents_shared_machine_namespaces`). ⚠️ **THERE ARE TWO RESERVED PORT BANDS — band private ports outside BOTH `20000-31007` (subject blocks) AND `11000-14999` (backends), and also clear of the static fixture ports `10000-10447` / `15000-15011` / `18001-18007`.** The `42000-42999` band has been used safely (phases 80, 81 and 82). ⚠️ **If two agents share ONE worktree they must stage by EXPLICIT PATH**, and **must NOT run an unscoped `git restore`**. ⚠️ **PREFER ONE WORKTREE PER AGENT OFF A COMMON BASE** — that is what the phase-82 BRAINSTORM did for four investigation agents, all of which returned `git status --porcelain` = 0. Per-task `gofmt` + `golangci-lint` on touched packages (`feedback_pertask_gofmt_lint`). ⚠️ **`gofmt -l` NEVER exits non-zero — gate on OUTPUT.** ⚠️ **`golangci-lint` runs `misspell` with `locale: US`.** ⚠️ **AN EMPTY LINT RESULT IS NOT EVIDENCE THE LINTER LOOKED — inject a British spelling, confirm it fires, restore byte-identically.** ⚠️ **`go build ./cmd/envoy-go/` drops an untracked binary in the worktree root — build with `-o` into scratch, and note `-o` precedes the package path.**

**⚠️ CONTROLLER-SIDE WORKTREE HAZARD** (`reference_bash_cwd_reset_commits_to_main`). The Bash tool's cwd persists between calls, but **a `cd` outside the primary working directory silently resets it back to the repo root**. ⚠️ **ASSUME IT FIRES EVERY SESSION — it has been treated as live for TWENTY-NINE consecutive sessions and it FIRED REPEATEDLY, OBSERVED, at the phase-82 BRAINSTORM** (`Shell cwd was reset to /home/esa/git/envoy-go`). **Use `git -C <abs-worktree-path>` for every git command, prefer absolute paths over `cd`, put any needed `cd` FIRST in a compound command, and tripwire with `pwd` + `git rev-parse --abbrev-ref HEAD` (must be the stage branch, NEVER `master`) + `git rev-list --count <base>..HEAD` before any commit or gate run.**

⚠️ **IF YOU WRITE A PROBE INTO A WORKTREE, DELETE IT AND PROVE THE TREE IS CLEAN.** At the phase-82 BRAINSTORM three agents wrote throwaway `.go` probes, ran them, deleted them, and confirmed `git status --porcelain` = 0 lines. A stage that ships a stray `.go` file has silently changed its own gate obligations.

## Read first (in order)
1. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline — ⚠️ **anchor greps on `^- **<field>`**; all seven fields verified singleton at the phase-82 BRAINSTORM close). ⚠️ **§Project counts SELF-CONTRADICTS §Current and is frozen at the phase-76 IMPL close** — do not anchor on it.
2. `docs/envoy-go/phases/82-wasm-http-call-response-cache/BRAINSTORM.md` — **the whole document.** §1 is the defect and the already-landed anchor; §2 the pick and the eight rejected alternatives with re-derived costs; §4.1 the four D-questions your SPEC must dispose; §5 the twenty-six-item refutation ledger.
3. `docs/envoy-go/ROADMAP.md` — **§Schema first** (the row invariants), then row 82, then the five deferred-candidate paragraphs (**`:192 :202 :212 :218 :226`** — ⚠️ **re-derive; they move**). **You are adding NO row and `want` STAYS 114.**
4. `BOOTSTRAP_PROMPT.md` — **at the REPO ROOT, no leading slash.** §6.1 `:285`, §6.2 `:294`, ⚠️ **§6.3 `:304` (NOT `:306`)**, §7.5 `:357-367`. ⚠️ **Not the 1024-line copy under `docs/superpowers/plans/`.**
5. `docs/envoy-go/DECISIONS.md` — **ADR-0303** (the prior row; its §Decision (a) states the segment-position rule) · **ADR-0288** (roll-IN-PLACE and the archive rule) · **ADR-0065 §Consequences (b) `:2379`** · **ADR-0132 §Decision (v) `:6291`** (⚠️ **cite still correct, but its reference-parity premise is UNEVIDENCED — see item 3**) · **ADR-0044** as-used · **ADR-0045**.
6. `docs/envoy-go/phases/81-stats-name-charset-guards/PROGRESS.md` — **the `# IMPL record`**, for calibration: seventeen refutations, five load-bearing, and a 3.07x cost overrun **over a measured-basis estimate**.
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — especially **`reference_code_comment_not_evidence`** (⚠️ **this row's entire basis — the landed comment is BACKWARDS**), **`reference_probe_must_discriminate`**, **`reference_probe_input_is_a_claim`**, **`reference_gate_command_negative_control`**, **`reference_empty_output_is_not_a_zero_result`** (⚠️ **fired THREE times at the BRAINSTORM**), **`reference_vacuous_break_modes`** (⚠️ **D-82-TRAILERS is a candidate**), **`reference_measured_prototype_is_a_lower_bound`**, **`reference_harness_exit_code_is_not_command_exit_code`**, **`reference_stale_cite_recurs_fix_by_pattern`** (⚠️ **caught BEFORE landing again — see item 4**), **`reference_sample_is_not_an_audit`** (⚠️ **the proxy-wasm denominator is 10 of 16**), **`reference_a_drift_correction_is_itself_a_claim`**, **`reference_grep_c_zero_is_a_broken_gate`**, **`reference_differential_run_selector`**, **`reference_recursive_grep_blind_to_gitignored_tracked_file`**, **`reference_sentinel_matcher_string_self_clears`**, **`reference_differential_grpc_receiver_driver_owned`**, **`feedback_brief_citations_not_evidence`**, `feedback_git_worktrees`, `feedback_execution_style`, and **`reference_bash_cwd_reset_commits_to_main`**.
