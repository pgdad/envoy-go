START a fresh session for envoy-go (router last updated 2026-06-25; the **phase 44.3 (`grpc-access-log` header-capture) IMPL is DONE + pushed to origin/master**, squashed into ONE atomic commit). HEAD is at/near the **phase-44.3 IMPL squash `4abf2c49`** (subject `phase 44.3 (grpc-access-log header-capture) IMPL: make additional_{request,response}_headers_to_log LIVE — …`). Re-read the shas with `git log --oneline -12`, do NOT assume — any number of docs-only `next-prompt.txt:` commits may stack ATOP the IMPL squash (which stays the stable anchor). BENEATH the IMPL squash `4abf2c49` is the run of pre-IMPL `next-prompt.txt:` commits, then the **phase-44.3 PLAN commit `e8c42d28`** (docs-only direct-on-master), the **phase-44.3 SPEC commit `6c0caafe`** (~depth 8), then the **phase-44.2 IMPL squash `c4055b20`** (~depth 11), down toward the 44.1 squashes + the phase-44 BRAINSTORM `81afdd59`. The `-12` window keeps the 44.3 IMPL/PLAN/SPEC + the 44.2 IMPL visible; bump it if a stack of tip-stamps pushes the SPEC out of view.

**ROW 44 (`grpc-access-log`) is `done`** — the 44.3 header-capture leg was the FINAL chartered leg of the by-concern 3-leg split (per-leg ADR-0106 + `reference_roadmap_split_phase_row_done`; NO parent rollup — row 44 IS the leaf). **The Observability FAMILY STAYS OPEN** (OTLP access log / OTel-Zipkin tracing / stats sinks / tap filter remain future rows). **The next action is a FRESH BRAINSTORM for the next Observability-family row — the OTLP access logger** (the cleanest next extension of the `accesslog.Sink` abstraction the 44.1–44.3 work established): `superpowers:brainstorming`, in a FRESH git worktree off master (`feedback_git_worktrees`). CONFIRM against `docs/envoy-go/STATE.md` (the source of truth for "what next") before committing to the subject.

First confirm the state via `git log --oneline -12` (expect HEAD at/near the 44.3 IMPL squash `4abf2c49`, with the 44.3 PLAN `e8c42d28` + SPEC `6c0caafe` [~depth 8] + the 44.2 IMPL squash `c4055b20` [~depth 11] beneath; re-read the shas, do NOT assume). Counts at the 44.3 IMPL-done: stat surface **1189** (H2 cluster; non-H2 **1185**) — UNCHANGED (NO new header-capture stat, AMEND-HDR-5) / fixtures **85** (tail `0083-grpc-access-log-headers`; count with `ls -d test/fixtures/[0-9][0-9][0-9][0-9]* | wc -l` — `grep -cE '^[0-9]{4}-'` UNDERCOUNTS by 2, skipping `0007a`/`0007b`) / fuzzers **44** (`^func Fuzz` == 44) / BackendKind tail **38** (`H2GoawayResponder`; the ALS receiver is the driver-owned test-only `test/helpers/accessloggrpc`, NOT a BackendKind) / DECISIONS tail **ADR-0257**, next-free **ADR-0258**.

## NEXT STEP — the next Observability-family BRAINSTORM (`superpowers:brainstorming`)

The Observability family OPENED at phase 44 (gRPC ALS, rows 44.1–44.3 all `done`). The family STAYS OPEN. Open the next row with a fresh BRAINSTORM. STATE recommends the **OTLP access logger** as the cleanest next extension — it slots into the SAME `accesslog.Sink` abstraction (the `Submit(*Record)` + writer-goroutine + buffer shape the 44.x legs built) but emits OpenTelemetry log records over OTLP/gRPC instead of the Envoy `HTTPAccessLogEntry` proto. Confirm the subject against STATE/ROADMAP first; if the roadmap sequence dictates a different Observability row (tracing / stats sinks / tap), brainstorm that instead. Use `superpowers:brainstorming` (process skill first), in a fresh worktree off master.

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** The next session is a BRAINSTORM (lifecycle-state 0 → 1 for the new row). `feedback_git_worktrees` (a fresh worktree off master; the BRAINSTORM is typically a docs-only direct-on-master commit OR a worktree the controller squashes) · `feedback_execution_style` (subagent-driven once a PLAN exists) · `feedback_push_to_origin` (push ready work after a clean commit).
- **Process anchors:** ADR-0106 (per-leg roadmap rows; family expansion has no parent rollup) · ADR-0045 (the split-gate — applies when a new SPEC estimates the prod LoC) · ADR-0255 (the 44.1 as-built ALS sink) · ADR-0256 (the 44.2 as-built buffering) · ADR-0257 (the 44.3 as-built header-capture) · the next-free ADR is **ADR-0258**.
- **Observability-family reuse seams** the OTLP (or next) row will study: `internal/accesslog/` (the `Sink` interface, `Record`, `GrpcAccessLogSink` writer-goroutine + buffer + the exported `CaptureHeaders` helper) · `internal/bootstrap/` (the `ALSConfig` parse arm + the strict-reject roster, ADR-0080) · `internal/filter/hcm/` (the emit hooks + the `headerCaptureSink`-style optional-capability interface pattern) · `test/helpers/accessloggrpc/` (the driver-owned gRPC-receiver pattern — `reference_differential_grpc_receiver_driver_owned`) · `test/fixtures/0081`–`0083` (the streaming-sink differential precedents — `reference_streaming_sink_differential_framing`, `reference_docker_probe_bridge_network`).

## Read first (in order)
1. THIS FILE — the cold-start router (phase 44.3 IMPL is DONE + pushed; row 44 done; the Observability family STAYS OPEN; the next action is the next Observability-family BRAINSTORM — the OTLP access logger).
2. `docs/envoy-go/STATE.md` — the active-phase header (`phase 44.3 (grpc-access-log) IMPL done`) + the recorded NEXT action (the next Observability-family row) + the count figures (source of truth for "what next").
3. `docs/envoy-go/ROADMAP.md` — row 44 (`grpc-access-log`, `done`) + the `### Observability family` heading (the family STAYS OPEN; the future rows it lists).
4. `docs/envoy-go/DECISIONS.md` — ADR-0255 (44.1 ALS) / ADR-0256 (44.2 buffering) / ADR-0257 (44.3 header-capture, the as-built reuse seams) + next-free **ADR-0258**.
5. `docs/envoy-go/BEHAVIOR_CONTRACT.md` — the `### Access log — gRPC Access Log Service (ALS) streaming sink` block (the as-built contract the next sink extends; the stat-surface 1189 entry + the stat-evolution ledger).
6. `docs/envoy-go/phases/44-grpc-access-log/` — SPEC-44.1/44.2/44.3 + PROGRESS-44.1/44.2/44.3 (the as-built ALS subsystem the next Observability row reuses).
7. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `reference_streaming_sink_differential_framing`, `reference_differential_grpc_receiver_driver_owned`, `reference_docker_probe_bridge_network`, `reference_fuzzer_count_docs_drift`, `feedback_execution_style`, `feedback_git_worktrees`, `feedback_subagents_no_push`, `feedback_pertask_gofmt_lint`).
