=== AUTONOMOUS LOOP CONTROL (read FIRST) ===
This file is a SELF-ADVANCING router for driving the project forward one stage at a time. Each session performs EXACTLY ONE lifecycle stage of the active phase (BRAINSTORM → SPEC → PLAN → IMPL — one stage per session, the project discipline), lands it (commit + push per `feedback_push_to_origin`), then ROLLS THIS FILE FORWARD to point at the next stage.

**STANDING DIRECTIVE (human, 2026-07-12): the loop runs AUTONOMOUSLY until the termination sentinel fires.** When a phase completes and the sentinel does NOT fire, the roller SELF-PICKS the next subject — smallest defensible candidate first (record the pick + the rejected alternatives in the BRAINSTORM) — and does NOT pause for a human pick. When there is banked, already-SPEC'd work mid-lifecycle (a split leg awaiting its PLAN/IMPL), advancing THAT is more defensible than opening a brand-new subject. When the sentinel DOES fire, create the `stop` file per the rule below and exit.

TERMINATION SENTINEL: if — and ONLY if — ALL work is complete (every chartered ROADMAP row/phase is `done`, EVERY family's deferred-candidate list is ALSO empty, and STATE.md's NEXT shows there is NO next stage left to perform), create an empty file named `stop` at the git root with `: > /home/esa/git/envoy-go/stop` and EXIT without further action.

Evaluate it MECHANICALLY, not by eye. All three checks must print NOTHING:
```sh
# (1) every ROADMAP row is `done`
grep -oE '^\| [0-9.]+ \| [a-z0-9-]+ \| [0-9.,  ]* \| (planned|in-progress|blocked|done[^|]*)' docs/envoy-go/ROADMAP.md \
  | awk -F'|' '{gsub(/ /,"",$5); if ($5 !~ /^done/) print "NOT DONE: row"$2}'

# (2) no family still carries deferred candidates
grep -oE 'remaining deferred \(not-yet-chartered\) candidates:[^.]*\.' docs/envoy-go/ROADMAP.md

# (3) every WORK family has been opened, i.e. at least one row summary names it
#     ("<Family>-family row"). NOTE: family HEADINGS never contain rows -- rows live
#     in the main table (ROADMAP §Schema: "Families are not rows"). Do NOT try to
#     count rows under a `### ... family` heading; that check is meaningless.
#     `Deprecated / edge features` is intentionally out of scope and excluded.
for slug in HTTP-filters Network-filters Load-balancing Upstream-robustness Observability \
            Operational-tooling HTTP/3 gRPC xDS Runtime WASM; do
  grep -qi -- "$slug-family row" docs/envoy-go/ROADMAP.md || echo "NEVER OPENED: $slug"
done
```
If ANY check prints output, the sentinel does NOT fire: proceed with the stage below and do NOT create `stop`.

Check (3) fails SAFE by construction: a future family whose rows use a slug not listed above reads as "never opened", which BLOCKS `stop` rather than falsely triggering it. A false "not done" costs one wasted session; a false "done" ends the project early. When you open a new family, add its row-summary slug here.

**SENTINEL RE-VERIFIED 2026-07-22 (re-run MECHANICALLY at the phase-71 PLAN close, in the stage worktree, by BOTH the controller and the V2 adversarial verifier) — it does NOT fire. `stop` was NOT created.** Actual output:
- **(1) prints `NOT DONE: row 71`** — row 71 (`tracing-custom-tags-metadata-route`) stays `in-progress` (registered at the phase-71 BRAINSTORM commit per the ROADMAP §Schema invariant; neither the SPEC nor the PLAN flips it). It flips `done` at the phase-71 IMPL six-gate.
- **(2) prints THREE live "candidates:" sentences** — HTTP/3, xDS, Observability ⇒ three families STAY OPEN. **Cite the command, never the adjective** (`reference_sentinel_deferred_sentence_live_vs_historical`): `grep -cE 'remaining deferred \(not-yet-chartered\) candidates:' docs/envoy-go/ROADMAP.md` ⇒ **3**. (The Observability sentence still names the non-`REQUEST` MetadataKinds `ROUTE`/`CLUSTER`/`HOST`; the phase-71 pick `ROUTE` is NAMED in it and does NOT drop it at the SPEC or PLAN — it narrows at the phase-71 IMPL row-done edit, the phase-57 precedent.)
- **(3) prints `NEVER OPENED: gRPC`, `NEVER OPENED: Runtime`, `NEVER OPENED: WASM`** ⇒ THREE families never opened.

Checks (2) + (3) print ⇒ the sentinel does NOT fire. Abundant work remains.

==============================================


## STATUS AS OF 2026-07-22: THE PHASE-71 PLAN IS COMPLETE AND LANDED. ROW 71 (`tracing-custom-tags-metadata-route`) STAYS `in-progress`. THIS SESSION PERFORMS THE PHASE-71 IMPL.

**What the phase-71 PLAN landed** (`git log --grep 'phase 71'`; locate by SUBJECT — `reference_next_prompt_tracked_despite_gitignore`, never by position): the 9-task SINGLE-FLAT-ROW TDD spine (T1–T9) decomposing SPEC §10, RE-DERIVED against the tree at `60f12146` (the SPEC squash; NO parallel stream — production tree byte-identical to what the SPEC re-derived). docs-only (`PLAN.md` + `PROGRESS.md`); ZERO production `.go`, DECISIONS/ROADMAP UNTOUCHED.
- **HEADLINE: ZERO unacknowledged drift.** A read-only re-derivation agent re-opened EVERY SPEC §11 anchor at tip — **32/32 anchor groups EXACT-HOLD** (the tip is one docs-only commit ahead of the SPEC's cited `fbc86063`; no line shifted). The SPEC's cites adopted verbatim. `go build ./...` clean; collisions (`kindMetadataRoute`/`routeMetaLookup`/`RouteMetaLookup`) 0.
- **The RD-* ledger (PLAN §1):** RD-EXACT (32/32) · RD-KIND (`customTagKind` `:53-58`, `kindMetadataRoute` at iota==4; `CustomTagSpec` already carries `MetaNamespace`/`MetaPath`/`DefaultValue`/`HasDefault`) · RD-REJECT (the ROUTE reject `config.go:245-246` REPLACED by an accept arm cloning REQUEST `:228-244`; CLUSTER `:247-248`/HOST `:249-250`/unset-A `:226-227`/unset-B `:251-252`/empty-tag `:198-200`/first-wins `:257-261` UNCHANGED) · **RD-NOIMPORT-CFG** (config.go adds ZERO imports — `k.GetRoute()` on the inferred `k := md.GetKind()`) · RD-RESOLVE-4TH (`ResolveCustomTags` `:26` 3-param → add a 4th one-arg `routeMetaLookup`, the 3rd `metaLookup` untouched) · RD-ROUTE-ARM (the arm descends the FULL `MetaPath`, NOT `[1:]` — the REQUEST slice is a Bucket-pre-keying artifact) · RD-DESCEND/RD-SERIALIZE (`descend` `:100-114` + `structpbValueToString` `:123-140` reused VERBATIM; NO new import in resolve.go) · **RD-SEAM (S2)** (`structpb` ABSENT from `chain.go`; ADD `func (c *FilterChain) RouteMetaLookup()` beside `DynamicMetadata()` `:1022`, wrapping `RouteMetadata().GetFilterMetadata()[ns]` via `structpb.NewStructValue`; the ONLY new import in the whole plan) · RD-CALLERS (18 = 5+6+7; exactly 3 no-chain-404 nil sites `connection.go:330`/`h2dispatch.go:313`/`h3dispatch.go:130`; the 4th arg APPENDS `chain.RouteMetaLookup` at 15, the 3rd untouched) · RD-FUZZ (repurpose `withMetaTags` `:94-108` — add `meta_route_ok`, repoint `meta_bad` Route→Cluster; `parseCustomTags` `config.go:126` < `provider required` `:136`) · RD-FIXTURE (the `0114` chassis has a Lua writer to DROP; `0115` uses static route metadata) · RD-TESTCALLERS (20 `ResolveCustomTags(` sites in `resolve_test.go` + emit callers in `accesslog_emit_test.go`/`span_emit_test.go` += trailing `nil`) · RD-MOD (+0 modules — the only new import is `structpb` into `chain.go`, existing).
- **Adversarially verified before landing** (TWO independent verifiers in PRIVATE scratch, `reference_parallel_subagents_private_scratch`): **V1** — code-claims + by-execution compile probes EXECUTION-CONFIRMED the pivotal S1 full-path-descent equivalence (`descend(structpb.NewStructValue(nsStruct), ["outer","inner"])` yields `"deep"` == REQUEST's `descend(metaLookup-result, ["inner"])` yields `"deep"`, EQUAL; single-segment resolves; unresolvable→false; `NewStructValue` signature confirmed) — **BUILDABLE, ZERO SEVERE/MODERATE/MINOR**; **V2** — process/consistency, all eight stage-close mechanics PASS — **ZERO SEVERE/MODERATE/MINOR**.
- **Envelope (at the phase-71 IMPL):** ZERO new packages (edits in `internal/tracing` + `internal/filter/http` + `internal/filter/hcm`) / +0 stats / +0 fuzzers (the repurposed `FuzzHCMConfigParse` seed) / +0 BackendKinds / +0 go.mod modules (`chain.go` gains a `structpb` IMPORT of an existing module) / ONE new OTLP fixture `0115-tracing-custom-tags-metadata-route` (static-route-metadata positive arm + `default_value` arm; NO writer) ⇒ fixtures **116 → 117** / DECISIONS tail **ADR-0293** stays PROPOSED (§Decision + §Consequences land IN PLACE at the IMPL; next-free ADR-0294).

**⚠️ PUSH (idempotent — evaluate, then act):** FIRST ACTION: `git fetch`; if origin/master is BEHIND local master and the local tip is the phase-71 PLAN squash (`git log --grep 'phase 71'`, locate by SUBJECT), `git push origin master` per `feedback_push_to_origin`; if in sync, proceed; if REJECTED (diverged), STOP and reconcile — never rebase away landed squashes.

## What THIS session does — the phase-71 IMPL (RE-DERIVE the PLAN, do NOT trust it)

One stage per session: the IMPL. Execute the 9-task SINGLE-FLAT-ROW TDD spine (T1–T9) from `PLAN.md`. **A PLAN is not evidence either — RE-DERIVE each task's anchors at the IMPL tip** (`feedback_brief_citations_not_evidence`; where it cites, go look; where it claims control flow, walk the call graph; default to REFUTED). Fresh worktree off master per `feedback_git_worktrees`; subagent-driven per `feedback_execution_style` (one implementer per task + a per-task adversarial reviewer, each RE-DERIVING its anchors at tip); subagents commit LOCALLY only (`feedback_subagents_no_push`), controller squash-push at close (`feedback_push_to_origin`); TDD (`superpowers:test-driven-development` — red-first per task). Then roll THIS file to the phase-72 BRAINSTORM.

**The 9-task spine to EXECUTE (PLAN §Task 1–9; RE-DERIVE each anchor at tip):**
- **T1** — `config.go`: `kindMetadataRoute` const (iota==4 after `kindMetadata`) + the ROUTE accept arm (cloning the REQUEST `:228-244`, `Kind: kindMetadataRoute`, MetaPath FULL) replacing the `:245-246` reject; CLUSTER/HOST/unset rejects UNCHANGED; **NO new import** (`k.GetRoute()` on `k := md.GetKind()`). `config_test.go`: accept ROUTE + reject CLUSTER/HOST/unset-A/unset-B/empty-namespace/empty-path/empty-segment (distinct substrings). Breaks A/B/C.
- **T2** — `resolve.go`: the one-arg `routeMetaLookup` 4th param + the `kindMetadataRoute` arm descending the FULL `MetaPath` (NOT `[1:]`), reusing `descend`/`structpbValueToString` VERBATIM; **NO new import**. `resolve_test.go`: the ROUTE path-walk (single/multi/unresolvable), the serialization table, the default/omit/present-empty matrix, nil-`routeMetaLookup` tolerance; the 20 existing callers += trailing `nil`. Breaks D (wrong slice) / E (default rule) / F (nil-tolerance). `-race`.
- **T3** — `internal/filter/http/chain.go`: add `func (c *FilterChain) RouteMetaLookup(ns string) (*structpb.Value, bool)` (S2) beside `DynamicMetadata()` `:1022` + **ADD the `structpb` import** + a unit test (present ns → wrapped struct; absent ns / nil `RouteMetadata` → `(nil,false)`). Break G (nil-guard).
- **T4** — `accesslog_emit.go`: thread `routeMetaLookup` to the three emit methods (`:27/:87/:149`) → 4th `ResolveCustomTags` arg (`:57/:118/:179`); update ALL 18 caller sites — APPEND a 2nd `nil` at the 3 no-chain 404 sites, APPEND `chain.RouteMetaLookup` at the 15 span-capable sites (the 3rd `metaLookup` arg UNTOUCHED). `span_emit_test.go`/`accesslog_emit_test.go` callers += trailing `nil` + a live ROUTE-metadata-span test. Breaks H/I. `-race`.
- **T5** — `FuzzHCMConfigParse` seed repurpose (S4: add a valid ROUTE accept `meta_route_ok` + repoint `meta_bad` Route→Cluster) + dispatch-verify (+0 fuzzers, 55→55).
- **T6** — the OTLP `0115-tracing-custom-tags-metadata-route` fixture (the `0114` chassis WITHOUT the Lua writer + a static route `metadata.filter_metadata` block + two ROUTE custom_tags — `route_hit` static-value hit, `route_default` `default_value` — cross-side EXACT key+value, NO writer) + deliberate breaks J/K/L (fixtures 116 → 117, full `TestDifferential/0115-...` selector).
- **T7** — BEHAVIOR_CONTRACT B1-B2 (flip the ROUTE line to CONSUMED; narrow the departures to CLUSTER/HOST + unset-A).
- **T8** — six-gate + full 117-dir differential + `-race` (`internal/tracing` + `internal/filter/hcm` + `internal/filter/http`) + the cycle guard (`go list -deps ./internal/tracing` — no filter-pkg edge) + `go mod tidy -diff` empty + envelope audit (`internal/xds`/`internal/tls`/`internal/boot`/`internal/listener`/`internal/bootstrap` BYTE-UNTOUCHED). **ROW 71 FLIPS `done`.**
- **T9** — ADR-0293 completed IN PLACE (§Decision + §Consequences; STATUS PROPOSED→COMPLETE; tail stays ADR-0293, next-free ADR-0294 — no renumber) + ROADMAP row-71-done + the deferred-sentence narrow (roll the `ROUTE` MetadataKind OUT — the phase-57 precedent) + STATE/router + memory updates (stage-close).

**Stage-close mechanics (the IMPL):** ADR-0293 COMPLETES IN PLACE (no new ADR, no renumber; next-free ADR-0294). **Row 71 FLIPS `in-progress` → `done`** at the six-gate (ADR-0106, the SOLE leg). NARROW the deferred sentence AT the row-done edit (roll the `ROUTE` MetadataKind OUT — the sentence stays a live `candidates:` match; the `CLUSTER`/`HOST` MetadataKinds + `spawn_upstream_span`/`http_service`/force-trace + `ssl` remain). STATE §Current rolled IN PLACE (lifecycle 3 → DONE; lineage re-capped at five). next-prompt.txt rolled to the phase-72 BRAINSTORM (the roller SELF-PICKS per the 2026-07-12 standing directive). Memory updates owed (SPEC §13 / PLAN T9): (i) the `*FilterChain.RouteMetaLookup()` seam — the ROUTE analogue of `DynamicMetadata()` (extends `reference_filterchain_dynamicmetadata_accessor`); (ii) the ROUTE resolve descends the FULL `MetaPath` (the REQUEST `[1:]` slice is a Bucket-pre-keying artifact). Re-run the sentinel MECHANICALLY at close: (1) SILENT (row 71 `done`, every other row `done`); (2) **3**; (3) `NEVER OPENED: gRPC/Runtime/WASM`. Does NOT fire; no `stop`. **Adversarially verify before landing** (a per-task reviewer + a final consistency pass).

## Read first (in order)
1. `docs/envoy-go/phases/71-tracing-custom-tags-metadata-route/PLAN.md` — the 9-task spine (§Task 1–9), the RD-* re-derivation ledger (§1, 32/32 EXACT-HOLD), the §1.1 clone skeletons (the ROUTE accept arm, the `kindMetadataRoute` resolve arm, the `RouteMetaLookup` accessor, the fuzz repurpose), the Global Constraints + Break protocol, the File structure, the Self-review table.
2. `docs/envoy-go/phases/71-tracing-custom-tags-metadata-route/PROGRESS.md` — the Task ledger to fill + the Findings-carried-from-the-PLAN (RD-*) to RE-VERIFY at tip.
3. `docs/envoy-go/phases/71-tracing-custom-tags-metadata-route/SPEC.md` — the D-CTMR-* dispositions (§3), the P1–P6 probe conclusions + the S1–S5 sharpenings (§1.1), the fixture (§8), the pinned §9 BC edit map, the ADR-0293 §Context (§14, to complete IN PLACE at T9).
4. `docs/envoy-go/ROADMAP.md` — row 71 (`in-progress` → `done` at T8) + the live Observability `candidates:` sentence (narrow the `ROUTE` MetadataKind OUT at T9) + §Schema.
5. `docs/envoy-go/STATE.md` — §Current pointer (TRUSTWORTHY; ADR-0288 discipline) + the §Recent lineage.
6. `docs/envoy-go/DECISIONS.md` tail — ADR-0293 (PROPOSED — complete IN PLACE at T9) + ADR-0292 (the phase-70 `metadata`/`REQUEST` precedent) + ADR-0044/0045/0106/0165/0080.
7. The phase-70 IMPL (`git show --stat` the phase-70 IMPL squash; `docs/envoy-go/phases/70-tracing-custom-tags-metadata/PROGRESS.md`) for the IMPL execution shape + the per-task break/review structure to mirror.
8. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` — trust MEMORY.md's index; esp. `feedback_brief_citations_not_evidence`, `feedback_git_worktrees`, `feedback_execution_style`, `feedback_subagents_no_push`, `reference_filterchain_dynamicmetadata_accessor` (verify the RECEIVER — the `RouteMetaLookup` seam extends this), `reference_detrand_race_catches_protojson_value_substring`, `reference_parallel_subagents_private_scratch`, `reference_break_protocol_commit_first`.
