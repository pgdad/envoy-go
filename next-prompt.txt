RESUME a fresh session for envoy-go (router last updated 2026-06-28; the **phase 46.1b (span emission + OTLP export) PLAN is DONE and LANDED** — docs-only direct-on-master `8f2e26a1`, plan-document-reviewer APPROVED after one fold-iteration). 46.1b is the COMPLETING sub-leg of the 46.1 (core+OTLP) by-exporter leg: it consumes the 46.1a `tracing.Decision` to BUILD a per-request `ingress`/SERVER span (the concrete 16-attr roster; start at dispatch, end at the `accesslog_emit.go` stream-complete seam) and EXPORT sampled spans over the UNARY `TraceService.Export` (the reused 44.2/45.1 bounded-channel batching sink). **The next action is the 46.1b IMPL** — execute the 12-task TDD spine in `docs/envoy-go/phases/46-tracing/PLAN-46.1b.md`, subagent-driven in a FRESH worktree off master (`feedback_execution_style` · `feedback_git_worktrees`). The 46.1b IMPL **CLOSES ADR-0260** (its §Decision/§Consequences body lands at the completion task). **ROW 46 (`tracing`) STAYS `in-progress`** (row 46 flips `done` only at the FINAL leg 46.2 IMPL per ADR-0106 + `reference_roadmap_split_phase_row_done`); the Observability FAMILY STAYS OPEN.

HEAD is the **46.1b PLAN landing `8f2e26a1`** (docs-only: `PLAN-46.1b.md` + the STATE/ROADMAP prepend) atop the **46.1a IMPL squash `b09dd404`**. **Re-read the shas with `git log --oneline -8`, do NOT assume.** Confirm `git status` clean (the untracked `.claude/` is session cruft, leave it) + master == origin/master.

**As-built at 46.1a (the substrate the 46.1b IMPL builds on — each VERIFIED at the PLAN review against master; re-confirm line numbers before editing, files evolve):** the `internal/tracing` package — `decision.go` `Decide(h, cfg, rng) Decision` where `Decision{Sample, Reason, Class, Continued, TraceID[16], SpanID[8], ParentSpanID[8], TraceState, RequestID}` is FULLY POPULATED (the span's inputs; do NOT modify `decision.go`) · `config.go` `TracingConfig{ClientSampling, RandomSampling, OverallSampling, ServiceName, ClusterName}` (`ServiceName`/`ClusterName` parsed-but-UNUSED at 46.1a — 46.1b's Resource/dial consume them) · `stats.go` `HCMCounters` (the 5 HCM-scoped) · `propagation.go`/`requestid.go`/`rand.go`. The HCM dispatch seams where the `Decision` is computed-then-DISCARDED: `connection.go:509` (H1, in `dispatchRequest`) + `h2dispatch.go:410` (H2, in `WriteH2`, value-copy `h2req` re-`SetH2Request`). The span-END seam (stream-complete): `accesslog_emit.go:19` (`emitAccessLog`, H1) / `:45` (`emitAccessLogH2`, H2) — both OPEN with `if statusCode == 0 || len(f.accessLog) == 0 { return }` (the span block goes AHEAD of that early-return — PLAN-review #1 Critical; else the export is dead code because a tracing HCM has no `access_log` block). The templates: `grpcclient.go:339` (`OTLPLogsClient` → mirror for `OTLPTracesClient`) · `internal/accesslog/otlpsink.go` 217 LoC (→ mirror for `OTLPExporter`) · `test/helpers/otlplogs` (→ mirror for `test/helpers/otlptrace`) · `test/fixtures/0084-otlp-access-log/` (→ the `0087` driver-owned-receiver + shared-bridge shape). The boot wiring: `cmd/envoy-go/main.go:130` (the `dialer` hoist) + `builtins.go:263`/`:55` (the single `builtins.RegisterBuiltins`→`hcm.NewNetworkFactory` HCM-factory closure — the ONLY seam the `ExporterProvider` threads through; NOT a 2nd `NewManagerWithBaseDirAndAllowH2C` param).

**The 12-task spine (PLAN-46.1b.md):** PROGRESS scaffold → `OTLPTracesClient` UNARY wrapper → `Span` model + 16-attr roster + `toProto` → `OTLPExporter` (bounded-channel + writer-goroutine + size/interval/close batching + retry-once + drop-newest + idempotent Close; **full-package `-race`** — the writer goroutine is a background mutator, `reference_full_suite_race_after_background_mutator`) → the 2 tracer-scoped `tracing.opentelemetry.{spans_sent,spans_dropped}` counters → the `ExporterProvider` (per-cluster memoized; lazy counters; the unknown/non-H2 cluster boot-reject over the exported `tracing.TracesClient` acyclic seam) → thread into `Filter.exporter` via `builtins.Deps` → the span-end wiring (carry the `Decision` to `accesslog_emit.go`; H1 frame-local param / H2 `chainDispatchAction` field; build+sample+export at the emit-seam HEAD; byte-stable no-tracing) → `main.go` boot wiring (hoist `dialer`, build the provider UNCONDITIONALLY, join `CloseAll` to the defer-LIFO) → the driver-owned `test/helpers/otlptrace` receiver → the `0087-tracing-otlp` cross-side span differential (poll-to-converge per `reference_concurrency_differential_release_barrier`; aggregated per-span PAYLOAD not framing per `reference_streaming_sink_differential_framing`; the continuation prong; subject `spans_sent`) → `0087` deliberate breaks (`-count=1`) + 20/20 flake + the six-gate + **ADR-0260 §Decision/§Consequences body (CLOSES the leg)** + BEHAVIOR_CONTRACT + STATE/ROADMAP + the fuzzer-count reconcile.

**Anticipated counts at the 46.1b IMPL-done (the exit invariant — re-verify, do NOT assume):** stat surface **1198** (H2 cluster; non-H2 **1194**; +2 the process-global `tracing.opentelemetry.{spans_sent,spans_dropped}`, register LAZILY only when an exporter builds) / fixtures **89** (tail `0087-tracing-otlp`) / fuzzers **48** (UNCHANGED — D-TRACE-FUZZER: no new fuzzer) / BackendKind **38** (the OTLP trace receiver is driver-owned, `reference_differential_grpc_receiver_driver_owned`) / DECISIONS **ADR-0260** (CLOSED; next-free **ADR-0261** = the 46.2 Zipkin+B3 anchor). **ZERO new Go packages** (span/exporter in `internal/tracing`); **ZERO new go.mod modules** (`go mod tidy -diff` EMPTY — the OTLP trace protos `trace/v1` + `collector/trace/v1` resolve at the already-direct `go.opentelemetry.io/proto/otlp v1.0.0`).

## Honor throughout (the discipline)
- **Lifecycle = one stage per session.** The next stage is the 46.1b IMPL (subagent-driven in a fresh worktree; the doc-side completion task closes ADR-0260 + advances STATE/ROADMAP/BEHAVIOR_CONTRACT/counts).
- **Execution-chaos lesson from 46.1a (HEED THIS — `feedback_subagent_autocommit_claudemd`):** the repo's global CLAUDE.md ("after successfully testing changes, add the files to git and commit them") makes dispatched subagents AUTO-COMMIT their work (and a stray async subagent even made an uncontrolled follow-up commit). Do NOT fight it with "don't commit" instructions. Instead: LET subagents commit, the CONTROLLER verifies EACH resulting commit (correct fileset, real non-vacuous tests — run `-v` + read the assertions, gates green), cleans stray next-task leak files between tasks (`git clean -fd <pkg>` / `git checkout -- <file>`), and re-runs the FULL suite on the FINAL FROZEN HEAD (a transient inconsistent mid-execution state caused a spurious full-suite failure at 46.1a). The controller does the deliberate-break verification ITSELF (never a subagent — an auto-commit mid-break would commit broken production code). Per-task commit messages don't matter — the controller SQUASHES at stage-close.
- **Subagents commit LOCALLY only** (`feedback_subagents_no_push`); the controller PUSHES at stage-close (`feedback_push_to_origin` — after a clean local squash on master with the six-gate green, push without asking).
- **Per-task gates** (`feedback_pertask_gofmt_lint`): `gofmt -l` + `golangci-lint run` on touched pkgs + `go vet` + `go build ./...`.
- **Differential selector** (`reference_differential_run_selector`): always `-run 'TestDifferential/0087'`, NEVER bare `'0087'` (matches ZERO subtests → vacuous green).
- **Break protocol** (`reference_differential_break_protocol_count1`): every deliberate-break + every `-race` run uses `-count=1`.
- **Process anchors:** ADR-0106 (per-leg rows; row 46 `in-progress`) · ADR-0044 (ADR §Context-at-SPEC [done] / §Decision+§Consequences body-at-IMPL — **ADR-0260 body lands at THIS 46.1b IMPL**) · ADR-0158 (the `grpcclient.Dialer` typed-wrapper — `OTLPTracesClient`) · ADR-0258/0259 (the phase-45 OTLP sink batching pattern the exporter reuses) · ADR-0016 (proto blank-import — already landed at 46.1a) · ADR-0080 (strict-reject — already landed at 46.1a) · next-free **ADR-0260** (CLOSED at this IMPL), then **ADR-0261** (46.2 Zipkin+B3).

## Read first (in order)
1. THIS FILE — the cold-start router (the 46.1b PLAN is DONE + LANDED `8f2e26a1`; the next action is the 46.1b IMPL; row 46 `in-progress`).
2. `docs/envoy-go/phases/46-tracing/PLAN-46.1b.md` — the 12-task TDD spine to EXECUTE (the Orientation brief, the D-question resolutions, the file-structure decomposition, the per-task failing-test→impl→commit steps, the PLAN-review folds).
3. `docs/envoy-go/phases/46-tracing/SPEC-46.1.md` — the charter (the §11 D-TRACE-SPAN 16-attr roster + D-TRACE-OTLP-WIRE + D-TRACE-REJECT pins; the §13 ADR-0260 §Context draft the IMPL completes).
4. `docs/envoy-go/phases/46-tracing/PLAN-46.1a.md` + the as-built `internal/tracing` package — the `Decision` shape + the dispatch seams the span model builds on.
5. `docs/envoy-go/STATE.md` — the active-phase header (`phase 46.1b PLAN done`) + the recorded NEXT (the 46.1b IMPL) + the count figures.
6. `docs/envoy-go/ROADMAP.md` — row 46 (`tracing`, `in-progress`, the 46.1b PLAN-done stage-note) + the `### Observability family` heading.
7. `docs/envoy-go/DECISIONS.md` — **ADR-0259** + next-free **ADR-0260** (the 46.1 anchor — the §Decision/§Consequences body lands at THIS IMPL).
8. `docs/envoy-go/phases/45-otlp-access-log/PLAN-45.1.md` + `internal/accesslog/otlpsink.go` + `internal/grpcclient/grpcclient.go` + `test/helpers/otlplogs/` + `test/fixtures/0084-otlp-access-log/` — the exporter / typed-client / receiver / differential templates the 46.1b IMPL mirrors.
9. Project memories at `~/.claude/projects/-home-esa-git-envoy-go/memory/` (esp. `reference_phase_46_tracing_split`, `reference_streaming_sink_differential_framing`, `reference_differential_grpc_receiver_driver_owned`, `reference_docker_probe_bridge_network`, `reference_concurrency_differential_release_barrier`, `reference_full_suite_race_after_background_mutator`, `reference_dynamic_stat_name_charset_guard`, `reference_differential_fullsuite_startup_flake`, `reference_differential_run_selector`, `reference_differential_break_protocol_count1`, `feedback_subagent_autocommit_claudemd`, `feedback_execution_style`, `feedback_git_worktrees`, `feedback_push_to_origin`, `feedback_pertask_gofmt_lint`, `feedback_subagents_no_push`).
